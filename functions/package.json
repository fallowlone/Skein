{
  "name": "learnvane-functions",
  "private": true,
  "type": "module",
  "comment": "No deps here on purpose: the deployed Pages Functions dir must stay clean (no node_modules) or wrangler bundles it and fails. Dev/test tooling lives in the repo-root package.json; binaries resolve upward. Run `bun run test`/`typecheck` from this dir.",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  }
}
