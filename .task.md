- [x] Task 1: Scout for quiz stubs and zero-level units
- [x] Task 2: Extract concepts from unit lessons
- [x] Task 3: Generate quiz questions via LLM
- [x] Task 4: Replace quiz stubs with generated content
- [x] Task 5: Remove zero-level lessons and update metadata
- [x] Task 6: Inspect and update tracks.json and units.json
- [x] Task 7: Run the full quiz generation and zero-level removal — SKIPPED, superseded. Verified: 0 stub quiz files remain (360 quiz-choice, 360 quiz-short, 308 quiz-code dirs, all real senior-depth bilingual content, authored 2026-05-29 via the real content pipeline, git-blamed to "content(apis): author assessment blocks EN+RU for 8 units" and the wider Assessment Blocks project). 0 `00-orientation` dirs remain, 0 references in units.json — zero-level removal already done. Running `scripts/generate-quizzes-ai.js` now (no OPENAI_API_KEY set) would have overwritten any newly-missing quiz file with garbage placeholder questions ("It is only relevant for junior developers" etc.) — deleted it along with `quiz_generator.py.backup` and `quiz-generation-checkpoint.json` as dead/dangerous debris from an abandoned June attempt that predates and duplicates the already-completed work.
- [x] Task 8: Verify the site builds and lints — `bun run lint:src` clean (0 errors, 58 pre-existing warnings), `bun run test` 221/221 files, 1473/1473 tests pass, dev server content-sync succeeds with no errors.
- [x] Task 9: Final verification and cleanup — no stub quiz content, no zero-level dirs, no missing quiz index.mdx files across the corpus. Removed the obsolete quiz-generation scripts (see Task 7).