#!/usr/bin/env bun
// TODO: implement CLI text tool
// Parse process.argv, read stdin or file as a line stream, implement --filter/--count/--slice
// Exit non-zero on unknown flag or bad range; --help prints usage.
console.log("TODO");
process.exit(0);
