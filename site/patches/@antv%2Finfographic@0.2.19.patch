diff --git a/esm/designs/components/ItemDesc.js b/esm/designs/components/ItemDesc.js
index 56a6871bad3b7319330d223abc093e589891e1e2..6541c40aa4b46a9e7b6e72dcf26e045f458ce9d3 100644
--- a/esm/designs/components/ItemDesc.js
+++ b/esm/designs/components/ItemDesc.js
@@ -11,12 +11,24 @@ var __rest = (this && this.__rest) || function (s, e) {
 };
 import { jsx as _jsx } from "../../jsx-runtime.js";
 import { Text } from '../../jsx/index.js';
+import { measureText } from '../../utils/index.js';
 export const ItemDesc = (_a) => {
-    var _b;
     var { indexes, lineNumber = 2, children } = _a, props = __rest(_a, ["indexes", "lineNumber", "children"]);
     if (!children)
         return null;
     const finalProps = Object.assign({ fontSize: 14, fill: '#666', wordWrap: true, lineHeight: 1.4, children }, props);
-    (_b = finalProps.height) !== null && _b !== void 0 ? _b : (finalProps.height = Math.ceil(lineNumber * +finalProps.lineHeight * +finalProps.fontSize));
+    // PATCH: grow height to fit the word-wrapped content when a box width is set,
+    // floored at the default lineNumber rows — the upstream default caps at two
+    // lines and lets longer descriptions overflow into the next row.
+    if (finalProps.height === undefined || finalProps.height === null) {
+        const floor = Math.ceil(lineNumber * +finalProps.lineHeight * +finalProps.fontSize);
+        if (typeof finalProps.width === 'number' && finalProps.width > 0 &&
+            (typeof children === 'string' || typeof children === 'number')) {
+            finalProps.height = Math.max(floor, measureText(children, finalProps).height);
+        }
+        else {
+            finalProps.height = floor;
+        }
+    }
     return (_jsx(Text, Object.assign({}, finalProps, { "data-indexes": indexes, "data-element-type": "item-desc" /* ElementTypeEnum.ItemDesc */ })));
 };
diff --git a/esm/designs/components/ItemLabel.js b/esm/designs/components/ItemLabel.js
index 4d212226d643594ecf97b2f16cf161d018652d28..160b160c119e3ccba0fd4ad41cbae19c0951d663 100644
--- a/esm/designs/components/ItemLabel.js
+++ b/esm/designs/components/ItemLabel.js
@@ -11,10 +11,22 @@ var __rest = (this && this.__rest) || function (s, e) {
 };
 import { jsx as _jsx } from "../../jsx-runtime.js";
 import { Text } from '../../jsx/index.js';
+import { measureText } from '../../utils/index.js';
 export const ItemLabel = (_a) => {
-    var _b;
     var { indexes, children } = _a, props = __rest(_a, ["indexes", "children"]);
     const finalProps = Object.assign({ fontSize: 18, fontWeight: 'bold', fill: '#252525', lineHeight: 1.4, children }, props);
-    (_b = finalProps.height) !== null && _b !== void 0 ? _b : (finalProps.height = Math.ceil(+finalProps.lineHeight * +finalProps.fontSize));
+    // PATCH: derive height from the word-wrapped content when a box width is set,
+    // floored at one line — the upstream default forces a single line and lets long
+    // labels overflow into the next row.
+    if (finalProps.height === undefined || finalProps.height === null) {
+        const oneLine = Math.ceil(+finalProps.lineHeight * +finalProps.fontSize);
+        if (typeof finalProps.width === 'number' && finalProps.width > 0 &&
+            (typeof children === 'string' || typeof children === 'number')) {
+            finalProps.height = Math.max(oneLine, measureText(children, finalProps).height);
+        }
+        else {
+            finalProps.height = oneLine;
+        }
+    }
     return (_jsx(Text, Object.assign({}, finalProps, { "data-indexes": indexes, "data-element-type": "item-label" /* ElementTypeEnum.ItemLabel */ })));
 };
diff --git a/esm/designs/items/LCornerCard.js b/esm/designs/items/LCornerCard.js
index e9bc99cd8a8cf641fa5251163da22c6725cca4fb..7de9fdf515bf1c558ef36819b091463dbae4f8c8 100644
--- a/esm/designs/items/LCornerCard.js
+++ b/esm/designs/items/LCornerCard.js
@@ -1,5 +1,5 @@
 import { jsx as _jsx, jsxs as _jsxs } from "../../jsx-runtime.js";
-import { Group, Path, Polygon } from '../../jsx/index.js';
+import { getElementBounds, Group, Path, Polygon } from '../../jsx/index.js';
 import { ItemDesc, ItemIcon, ItemLabel } from '../components/index.js';
 import { getItemProps } from '../utils/index.js';
 import { registerItem } from './registry.js';
@@ -11,7 +11,13 @@ export const LCornerCard = (props) => {
     const arrowGap = 12;
     const descX = arrowSize + arrowGap;
     const descWidth = width - descX;
-    const descHeight = 60;
+    // PATCH: grow the desc box to fit wrapped text (floor 60) so long descriptions
+    // no longer overflow the fixed box into the title/next card; everything below
+    // is positioned relative to descHeight, so the card grows downward correctly.
+    const measuredDescHeight = desc
+        ? getElementBounds(_jsx(ItemDesc, { indexes: indexes, width: descWidth, fontSize: 12, children: desc })).height
+        : 0;
+    const descHeight = Math.max(60, measuredDescHeight);
     const verticalLen = iconSize + 44;
     const arrowX1 = arrowSize;
     const arrowY1 = descHeight + arrowGap;
diff --git a/esm/designs/items/SimpleVerticalArrow.js b/esm/designs/items/SimpleVerticalArrow.js
index 2efea95297ed6e6bdeb05fade1fe272da512bae7..d4c76457a7eff78f9e5846fb36f2ad1adfd5acf6 100644
--- a/esm/designs/items/SimpleVerticalArrow.js
+++ b/esm/designs/items/SimpleVerticalArrow.js
@@ -1,15 +1,21 @@
 import { jsx as _jsx, jsxs as _jsxs, Fragment as _Fragment } from "../../jsx-runtime.js";
-import { Group, Polygon, Text } from '../../jsx/index.js';
+import { getElementBounds, Group, Polygon, Text } from '../../jsx/index.js';
 import { Gap, ItemDesc, ItemLabel } from '../components/index.js';
 import { FlexLayout } from '../layouts/index.js';
 import { AlignLayout } from '../layouts/Align.js';
 import { getItemProps } from '../utils/index.js';
 import { registerItem } from './registry.js';
 export const SimpleVerticalArrow = (props) => {
-    const [{ indexes, datum, height = 140, themeColors, positionH = 'normal' }, restProps,] = getItemProps(props, ['height']);
+    const [{ indexes, datum, height: heightProp, themeColors, positionH = 'normal' }, restProps,] = getItemProps(props, ['height']);
     const textAlignHorizontal = positionH === 'normal' ? 'right' : 'left';
     const label = (_jsx(ItemLabel, { indexes: indexes, width: 120, fill: themeColors.colorText, alignHorizontal: textAlignHorizontal, alignVertical: "middle", fontSize: 14, children: datum.label }));
     const desc = (_jsx(ItemDesc, { indexes: indexes, width: 120, fill: themeColors.colorTextSecondary, alignHorizontal: textAlignHorizontal, alignVertical: "top", children: datum.desc }));
+    // PATCH: grow the item (and its arrow) to fit wrapped label+desc, with 140 as
+    // the floor — the upstream fixed 140 height let long text overflow the arrow.
+    const labelH = datum.label ? getElementBounds(label).height : 0;
+    const descH = datum.desc ? getElementBounds(desc).height : 0;
+    const contentH = labelH + (datum.label && datum.desc ? 8 : 0) + descH + 24;
+    const height = heightProp != null ? heightProp : Math.max(140, contentH);
     const isNormal = positionH !== 'flipped';
     const labelGap = 15;
     const arrowWidth = 30;
diff --git a/esm/designs/structures/compare-binary-horizontal/index.js b/esm/designs/structures/compare-binary-horizontal/index.js
index 9bec17746ccafe6de86f3169b43ef3961238fbd4..f1c1bbfaa72bff2ed0aa146996059d63843d4115 100644
--- a/esm/designs/structures/compare-binary-horizontal/index.js
+++ b/esm/designs/structures/compare-binary-horizontal/index.js
@@ -28,7 +28,29 @@ export const CompareBinaryHorizontal = (props) => {
     const btnElements = [];
     const decorElements = [];
     const maxChildren = Math.max(leftChildren.length, rightChildren.length);
-    const itemsHeight = maxChildren > 0 ? maxChildren * (itemBounds.height + gap) - gap : 0;
+    // PATCH: measure every row independently and stack rows with their own heights.
+    // Upstream stepped each row by the FIRST item's height, so any row taller than
+    // row 0 (multi-line label/desc) overflowed into the next row.
+    const rowHeights = [];
+    for (let i = 0; i < maxChildren; i++) {
+        const lh = leftChildren[i]
+            ? getElementBounds(_jsx(Item, { indexes: [0, i], data: data, datum: leftChildren[i] })).height
+            : 0;
+        const rh = rightChildren[i]
+            ? getElementBounds(_jsx(Item, { indexes: [1, i], data: data, datum: rightChildren[i] })).height
+            : 0;
+        rowHeights.push(Math.max(lh, rh, itemBounds.height));
+    }
+    const rowTops = [];
+    let __acc = 0;
+    for (let i = 0; i < maxChildren; i++) {
+        rowTops.push(__acc);
+        __acc += rowHeights[i] + gap;
+    }
+    const rowH = (i) => (rowHeights[i] != null ? rowHeights[i] : itemBounds.height);
+    const itemsHeight = maxChildren > 0
+        ? rowTops[maxChildren - 1] + rowHeights[maxChildren - 1]
+        : 0;
     const btnSpacing = 5;
     const topOffset = maxChildren > 0 ? gap / 2 + btnBounds.height / 2 : btnBounds.height / 2;
     const leftX = btnBounds.width + btnSpacing;
@@ -39,35 +61,35 @@ export const CompareBinaryHorizontal = (props) => {
     const leftPositionH = flipped ? 'flipped' : opposite ? 'normal' : 'normal';
     const rightPositionH = flipped ? 'normal' : opposite ? 'flipped' : 'normal';
     leftChildren.forEach((child, index) => {
-        const childY = topOffset + index * (itemBounds.height + gap);
+        const childY = topOffset + rowTops[index];
         const indexes = [0, index];
         itemElements.push(_jsx(Item, { indexes: indexes, datum: child, data: data, x: leftX, y: childY, positionH: leftPositionH, positionV: "middle" }));
-        btnElements.push(_jsx(BtnRemove, { indexes: indexes, x: leftX - btnBounds.width - btnSpacing, y: childY + (itemBounds.height - btnBounds.height) / 2 }));
+        btnElements.push(_jsx(BtnRemove, { indexes: indexes, x: leftX - btnBounds.width - btnSpacing, y: childY + (rowH(index) - btnBounds.height) / 2 }));
         if (index < leftChildren.length - 1) {
-            btnElements.push(_jsx(BtnAdd, { indexes: [0, index + 1], x: leftX + (itemBounds.width - btnBounds.width) / 2, y: childY + itemBounds.height + gap / 2 - btnBounds.height / 2 }));
+            btnElements.push(_jsx(BtnAdd, { indexes: [0, index + 1], x: leftX + (itemBounds.width - btnBounds.width) / 2, y: childY + rowH(index) + gap / 2 - btnBounds.height / 2 }));
         }
     });
     if (leftChildren.length > 0) {
         btnElements.push(_jsx(BtnAdd, { indexes: [0, 0], x: leftX + (itemBounds.width - btnBounds.width) / 2, y: topOffset - gap / 2 - btnBounds.height / 2 }));
-        const lastChildY = topOffset + (leftChildren.length - 1) * (itemBounds.height + gap);
-        btnElements.push(_jsx(BtnAdd, { indexes: [0, leftChildren.length], x: leftX + (itemBounds.width - btnBounds.width) / 2, y: lastChildY + itemBounds.height + gap / 2 - btnBounds.height / 2 }));
+        const lastChildY = topOffset + rowTops[leftChildren.length - 1];
+        btnElements.push(_jsx(BtnAdd, { indexes: [0, leftChildren.length], x: leftX + (itemBounds.width - btnBounds.width) / 2, y: lastChildY + rowH(leftChildren.length - 1) + gap / 2 - btnBounds.height / 2 }));
     }
     else if (items.length >= 1) {
         btnElements.push(_jsx(BtnAdd, { indexes: [0, 0], x: leftX + (itemBounds.width - btnBounds.width) / 2, y: topOffset - btnBounds.height / 2 }));
     }
     rightChildren.forEach((child, index) => {
-        const childY = topOffset + index * (itemBounds.height + gap);
+        const childY = topOffset + rowTops[index];
         const indexes = [1, index];
         itemElements.push(_jsx(Item, { indexes: indexes, datum: child, data: data, x: rightX, y: childY, positionH: rightPositionH, positionV: "middle" }));
-        btnElements.push(_jsx(BtnRemove, { indexes: indexes, x: rightX + itemBounds.width + btnSpacing, y: childY + (itemBounds.height - btnBounds.height) / 2 }));
+        btnElements.push(_jsx(BtnRemove, { indexes: indexes, x: rightX + itemBounds.width + btnSpacing, y: childY + (rowH(index) - btnBounds.height) / 2 }));
         if (index < rightChildren.length - 1) {
-            btnElements.push(_jsx(BtnAdd, { indexes: [1, index + 1], x: rightX + (itemBounds.width - btnBounds.width) / 2, y: childY + itemBounds.height + gap / 2 - btnBounds.height / 2 }));
+            btnElements.push(_jsx(BtnAdd, { indexes: [1, index + 1], x: rightX + (itemBounds.width - btnBounds.width) / 2, y: childY + rowH(index) + gap / 2 - btnBounds.height / 2 }));
         }
     });
     if (rightChildren.length > 0) {
         btnElements.push(_jsx(BtnAdd, { indexes: [1, 0], x: rightX + (itemBounds.width - btnBounds.width) / 2, y: topOffset - gap / 2 - btnBounds.height / 2 }));
-        const lastChildY = topOffset + (rightChildren.length - 1) * (itemBounds.height + gap);
-        btnElements.push(_jsx(BtnAdd, { indexes: [1, rightChildren.length], x: rightX + (itemBounds.width - btnBounds.width) / 2, y: lastChildY + itemBounds.height + gap / 2 - btnBounds.height / 2 }));
+        const lastChildY = topOffset + rowTops[rightChildren.length - 1];
+        btnElements.push(_jsx(BtnAdd, { indexes: [1, rightChildren.length], x: rightX + (itemBounds.width - btnBounds.width) / 2, y: lastChildY + rowH(rightChildren.length - 1) + gap / 2 - btnBounds.height / 2 }));
     }
     else if (items.length >= 2) {
         btnElements.push(_jsx(BtnAdd, { indexes: [1, 0], x: rightX + (itemBounds.width - btnBounds.width) / 2, y: topOffset - btnBounds.height / 2 }));
diff --git a/esm/designs/structures/list-column.js b/esm/designs/structures/list-column.js
index 92792d1e3d3e712e31496a26b8a6005286b799e6..3aabec8d5755ef52c15c5feefab95d42d0f6301a 100644
--- a/esm/designs/structures/list-column.js
+++ b/esm/designs/structures/list-column.js
@@ -13,17 +13,28 @@ export const ListColumn = (props) => {
     const btnElements = [];
     const itemElements = [];
     const btnAddX = (width - btnBounds.width) / 2;
+    // PATCH: measure each item at the real column width and stack with per-item
+    // heights — upstream stepped every row by the first item's height, so longer
+    // (wrapped) items overflowed into the next row.
+    const rowHeights = items.map((item, i) => getElementBounds(_jsx(Item, { indexes: [i], data: data, datum: item, width: width })).height);
+    const rowTops = [];
+    let __acc = 0;
+    for (let i = 0; i < items.length; i++) {
+        rowTops.push(__acc);
+        __acc += rowHeights[i] + gap;
+    }
+    const rowH = (i) => (rowHeights[i] != null ? rowHeights[i] : itemBounds.height);
     items.forEach((item, index) => {
-        const itemY = (itemBounds.height + gap) * index;
+        const itemY = rowTops[index];
         const indexes = [index];
         itemElements.push(_jsx(Item, { indexes: indexes, datum: item, data: data, y: itemY, width: width, positionV: "middle", positionH: zigzag ? (index % 2 === 0 ? 'normal' : 'flipped') : 'normal' }));
-        btnElements.push(_jsx(BtnRemove, { indexes: indexes, x: -btnBounds.width - 10, y: itemY + (itemBounds.height - btnBounds.height) / 2 }));
+        btnElements.push(_jsx(BtnRemove, { indexes: indexes, x: -btnBounds.width - 10, y: itemY + (rowH(index) - btnBounds.height) / 2 }));
         const btnAddY = index === 0 ? -btnBounds.height : itemY - gap / 2 - btnBounds.height / 2;
         btnElements.push(_jsx(BtnAdd, { indexes: indexes, x: btnAddX, y: btnAddY }));
     });
     if (items.length > 0) {
-        const lastItemY = (itemBounds.height + gap) * (items.length - 1);
-        const extraAddBtnY = lastItemY + itemBounds.height;
+        const lastItemY = rowTops[items.length - 1];
+        const extraAddBtnY = lastItemY + rowH(items.length - 1);
         btnElements.push(_jsx(BtnAdd, { indexes: [items.length], x: btnAddX, y: extraAddBtnY }));
     }
     return (_jsxs(FlexLayout, { id: "infographic-container", flexDirection: "column", justifyContent: "center", alignItems: "center", children: [titleContent, _jsxs(Group, { children: [_jsx(ItemsGroup, { children: itemElements }), _jsx(BtnsGroup, { children: btnElements })] })] }));
diff --git a/esm/designs/structures/sequence-pyramid.js b/esm/designs/structures/sequence-pyramid.js
index f82cf0d746fd977fd6b0e59a7669389e45144058..61c750298505debb5f7dc29846a1c8c940cbd80c 100644
--- a/esm/designs/structures/sequence-pyramid.js
+++ b/esm/designs/structures/sequence-pyramid.js
@@ -1,7 +1,7 @@
 import { jsx as _jsx, jsxs as _jsxs } from "../../jsx-runtime.js";
 import roundPolygon, { getSegments } from 'round-polygon';
 import tinycolor from 'tinycolor2';
-import { Defs, Group, Polygon, Rect } from '../../jsx/index.js';
+import { Defs, getElementBounds, Group, Polygon, Rect } from '../../jsx/index.js';
 import { BtnsGroup, ItemIcon, ItemsGroup } from '../components/index.js';
 import { FlexLayout } from '../layouts/index.js';
 import { getPaletteColor, getThemeColors } from '../utils/index.js';
@@ -23,8 +23,19 @@ export const SequencePyramid = (props) => {
     // Calculate dimensions
     const actualPyramidWidth = pyramidWidth !== null && pyramidWidth !== void 0 ? pyramidWidth : width * 0.6;
     const itemAreaWidth = width - actualPyramidWidth;
+    // PATCH: size every tier to the tallest side-card so multi-line labels/descs no
+    // longer overflow the fixed itemHeight into the next tier. All tiers share one
+    // height, so the triangle stays uniform.
+    const measureWidth = Math.max(40, itemAreaWidth);
+    let maxNatural = 0;
+    items.forEach((item) => {
+        const h = getElementBounds(_jsx(Item, { indexes: [0], data: data, datum: item, width: measureWidth, positionV: "middle" })).height;
+        if (h > maxNatural)
+            maxNatural = h;
+    });
+    const effItemHeight = Math.max(itemHeight, maxNatural);
     // Pyramid layer height should be greater than itemHeight
-    const pyramidLayerHeight = itemHeight * 1.2; // Magic number: 20% larger than itemHeight
+    const pyramidLayerHeight = effItemHeight * 1.2; // Magic number: 20% larger than itemHeight
     const totalHeight = items.length * pyramidLayerHeight + (items.length - 1) * gap;
     items.forEach((item, index) => {
         const indexes = [index];
@@ -37,7 +48,7 @@ export const SequencePyramid = (props) => {
         const pyramidCenterX = actualPyramidWidth / 2;
         const pyramidY = index * (pyramidLayerHeight + gap);
         // Background positioning - centered vertically with pyramid layer
-        const backgroundYOffset = (pyramidLayerHeight - itemHeight) / 2;
+        const backgroundYOffset = (pyramidLayerHeight - effItemHeight) / 2;
         const backgroundY = pyramidY + backgroundYOffset;
         const rightTopX = pyramidCenterX + topWidth / 2;
         const rightBottomX = pyramidCenterX + bottomWidth / 2;
@@ -45,7 +56,7 @@ export const SequencePyramid = (props) => {
         // Background - fixed width from top edge
         const backgroundX = rightTopX - overlapWidth;
         const backgroundWidth = itemAreaWidth + radius;
-        const backgroundHeight = itemHeight;
+        const backgroundHeight = effItemHeight;
         const backgroundRightEdge = backgroundX + backgroundWidth;
         const iconX = pyramidCenterX - iconSize / 2;
         const iconY = pyramidY + pyramidLayerHeight / 2 - iconSize / 2 + (isFirst ? 8 : 0);
@@ -61,7 +72,7 @@ export const SequencePyramid = (props) => {
         // Icon
         iconElements.push(_jsx(ItemIcon, { indexes: indexes, x: iconX, y: iconY, size: iconSize, fill: "#fff" }));
         // Item
-        itemElements.push(_jsx(Item, { indexes: indexes, datum: item, data: data, x: itemX, y: itemY, width: itemWidth, height: itemHeight, positionV: "middle" }));
+        itemElements.push(_jsx(Item, { indexes: indexes, datum: item, data: data, x: itemX, y: itemY, width: itemWidth, height: effItemHeight, positionV: "middle" }));
     });
     return (_jsxs(FlexLayout, { id: "infographic-container", flexDirection: "column", justifyContent: "center", alignItems: "center", children: [titleContent, _jsxs(Group, { width: width, height: totalHeight, children: [_jsx(Group, { children: backgroundElements }), _jsx(Group, { children: pyramidElements }), _jsx(Group, { children: iconElements }), _jsx(ItemsGroup, { children: itemElements }), _jsx(BtnsGroup, {})] })] }));
 };
diff --git a/esm/utils/measure-text.js b/esm/utils/measure-text.js
index 4722845f9677a20a8b95f01e8b418fbc29dd62a3..93bd165a6b70a6f05a6f08b2124d4fbb767c7a2e 100644
--- a/esm/utils/measure-text.js
+++ b/esm/utils/measure-text.js
@@ -109,6 +109,50 @@ function measureTextInBrowser(content, { fontFamily, fontSize, fontWeight, lineH
         return null;
     return { width: rect.width, height: rect.height };
 }
+// PATCH: count word-wrapped lines for a width-constrained box. Upstream
+// measureText only counts explicit "\n" lines, so width-bounded text (the
+// foreignObject items render with word-break:break-word) is always measured as
+// one line — under-allocating row height and causing rows to overlap. Greedily
+// pack words to the box width using the same single-line measurer.
+function countWrappedLines(content, boxWidth, measureWidth) {
+    const paragraphs = content.split(/\r?\n/);
+    let total = 0;
+    const spaceW = measureWidth(' ');
+    for (const para of paragraphs) {
+        const trimmed = para.trim();
+        if (!trimmed) {
+            total += 1;
+            continue;
+        }
+        const words = trimmed.split(/\s+/);
+        let lineW = 0;
+        let lines = 0;
+        for (const word of words) {
+            const wW = measureWidth(word);
+            if (wW > boxWidth) {
+                // word longer than the box: it breaks internally (break-word)
+                if (lineW > 0) {
+                    lines += 1;
+                    lineW = 0;
+                }
+                lines += Math.ceil(wW / boxWidth);
+                continue;
+            }
+            const next = lineW === 0 ? wW : lineW + spaceW + wW;
+            if (next <= boxWidth) {
+                lineW = next;
+            }
+            else {
+                lines += 1;
+                lineW = wW;
+            }
+        }
+        if (lineW > 0)
+            lines += 1;
+        total += Math.max(lines, 1);
+    }
+    return Math.max(total, 1);
+}
 export function measureText(text = '', attrs) {
     var _a;
     if (attrs.width && attrs.height) {
@@ -128,9 +172,29 @@ export function measureText(text = '', attrs) {
     };
     const fallback = () => measure(content, Object.assign(Object.assign({}, options), { fontFamily: decodeFontFamily(fontFamily) }));
     const metrics = (_a = measureTextInBrowser(content, options)) !== null && _a !== void 0 ? _a : fallback();
+    // PATCH: when a finite box width is given and the single-line text exceeds it,
+    // recompute height from the wrapped line count so layout reserves real space.
+    const boxWidth = typeof attrs.width === 'number'
+        ? attrs.width
+        : typeof attrs.width === 'string'
+            ? parseFloat(attrs.width)
+            : NaN;
+    let outWidth = metrics.width;
+    let outHeight = metrics.height;
+    if (Number.isFinite(boxWidth) && boxWidth > 0 && metrics.width > boxWidth) {
+        const measureWidth = (str) => {
+            var _b;
+            const m = (_b = measureTextInBrowser(str, options)) !== null && _b !== void 0 ? _b : measure(str, Object.assign(Object.assign({}, options), { fontFamily: decodeFontFamily(fontFamily) }));
+            return m ? m.width : 0;
+        };
+        const lineCount = countWrappedLines(content, boxWidth, measureWidth);
+        const lineHeightPx = resolveLineHeight(options.fontSize, lineHeight);
+        outHeight = lineHeightPx * lineCount;
+        outWidth = Math.min(metrics.width, boxWidth);
+    }
     // 额外添加 1% 宽高
     return {
-        width: Math.ceil(metrics.width * FONT_EXTEND_FACTOR),
-        height: Math.ceil(metrics.height * FONT_EXTEND_FACTOR),
+        width: Math.ceil(outWidth * FONT_EXTEND_FACTOR),
+        height: Math.ceil(outHeight * FONT_EXTEND_FACTOR),
     };
 }
diff --git a/lib/utils/measure-text.js b/lib/utils/measure-text.js
index 38c32be7761cebaa1b7f67afee2c9faa73e67ae9..020bcf7f834dc6544f77b32062f47895a8576241 100644
--- a/lib/utils/measure-text.js
+++ b/lib/utils/measure-text.js
@@ -117,6 +117,49 @@ function measureTextInBrowser(content, { fontFamily, fontSize, fontWeight, lineH
         return null;
     return { width: rect.width, height: rect.height };
 }
+// PATCH: count word-wrapped lines for a width-constrained box. Upstream
+// measureText only counts explicit "\n" lines, so width-bounded text (the
+// foreignObject items render with word-break:break-word) is always measured as
+// one line — under-allocating row height and causing rows to overlap. Greedily
+// pack words to the box width using the same single-line measurer.
+function countWrappedLines(content, boxWidth, measureWidth) {
+    const paragraphs = content.split(/\r?\n/);
+    let total = 0;
+    const spaceW = measureWidth(' ');
+    for (const para of paragraphs) {
+        const trimmed = para.trim();
+        if (!trimmed) {
+            total += 1;
+            continue;
+        }
+        const words = trimmed.split(/\s+/);
+        let lineW = 0;
+        let lines = 0;
+        for (const word of words) {
+            const wW = measureWidth(word);
+            if (wW > boxWidth) {
+                if (lineW > 0) {
+                    lines += 1;
+                    lineW = 0;
+                }
+                lines += Math.ceil(wW / boxWidth);
+                continue;
+            }
+            const next = lineW === 0 ? wW : lineW + spaceW + wW;
+            if (next <= boxWidth) {
+                lineW = next;
+            }
+            else {
+                lines += 1;
+                lineW = wW;
+            }
+        }
+        if (lineW > 0)
+            lines += 1;
+        total += Math.max(lines, 1);
+    }
+    return Math.max(total, 1);
+}
 function measureText(text = '', attrs) {
     var _a;
     if (attrs.width && attrs.height) {
@@ -136,9 +179,29 @@ function measureText(text = '', attrs) {
     };
     const fallback = () => (0, measury_1.measureText)(content, Object.assign(Object.assign({}, options), { fontFamily: (0, font_1.decodeFontFamily)(fontFamily) }));
     const metrics = (_a = measureTextInBrowser(content, options)) !== null && _a !== void 0 ? _a : fallback();
+    // PATCH: when a finite box width is given and the single-line text exceeds it,
+    // recompute height from the wrapped line count so layout reserves real space.
+    const boxWidth = typeof attrs.width === 'number'
+        ? attrs.width
+        : typeof attrs.width === 'string'
+            ? parseFloat(attrs.width)
+            : NaN;
+    let outWidth = metrics.width;
+    let outHeight = metrics.height;
+    if (Number.isFinite(boxWidth) && boxWidth > 0 && metrics.width > boxWidth) {
+        const measureWidth = (str) => {
+            var _b;
+            const m = (_b = measureTextInBrowser(str, options)) !== null && _b !== void 0 ? _b : (0, measury_1.measureText)(str, Object.assign(Object.assign({}, options), { fontFamily: (0, font_1.decodeFontFamily)(fontFamily) }));
+            return m ? m.width : 0;
+        };
+        const lineCount = countWrappedLines(content, boxWidth, measureWidth);
+        const lineHeightPx = resolveLineHeight(options.fontSize, lineHeight);
+        outHeight = lineHeightPx * lineCount;
+        outWidth = Math.min(metrics.width, boxWidth);
+    }
     // 额外添加 1% 宽高
     return {
-        width: Math.ceil(metrics.width * FONT_EXTEND_FACTOR),
-        height: Math.ceil(metrics.height * FONT_EXTEND_FACTOR),
+        width: Math.ceil(outWidth * FONT_EXTEND_FACTOR),
+        height: Math.ceil(outHeight * FONT_EXTEND_FACTOR),
     };
 }
