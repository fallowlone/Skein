---
import { t, type Locale } from "../../i18n";
type Props = { lang: Locale };
const { lang } = Astro.props;
---
<div data-lesson-section="check" class="lk-section">
  <div class="lk-label">{t("lesson.check", lang)}</div>
  <slot />
</div>
