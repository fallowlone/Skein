---
import { t, type Locale } from "../../i18n";
type Props = { lang: Locale };
const { lang } = Astro.props;
---
<div data-lesson-section="worked-example" class="lk-panel">
  <div class="lk-label">{t("lesson.workedExample", lang)}</div>
  <div class="lk-body"><slot /></div>
</div>
