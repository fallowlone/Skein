---
import { t, type Locale } from "../../i18n";
type Props = { lang: Locale };
const { lang } = Astro.props;
---
<div data-lesson-section="trace" class="lk-section">
  <div class="lk-label">{t("lesson.trace", lang)}</div>
  <div class="lk-body"><slot /></div>
</div>
