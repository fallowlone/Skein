// Placeholder for P0.10. Full implementation in P1.1.
import type { JSX } from "preact";

type Props = { lang: "en" | "ru" };

export default function Pretest({ lang }: Props): JSX.Element | null {
  // Returns null until P1.1 builds the interactive 3-Q diagnostic.
  return null;
}
