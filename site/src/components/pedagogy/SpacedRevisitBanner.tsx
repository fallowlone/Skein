// Placeholder for P0.9. Full implementation in P1.10.
type Props = { lang: "en" | "ru" };
export default function SpacedRevisitBanner({ lang }: Props): null {
  // Renders nothing until P1.10 wires real history-aware logic.
  return null;
}
