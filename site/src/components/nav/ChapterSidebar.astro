---
// Placeholder for P0.9. Full implementation in P0.11.
import type { Locale } from "../../i18n";
type Props = { lang: Locale; pillarSlug: string; chapterSlug: string; currentPiece?: string };
const { pillarSlug, chapterSlug } = Astro.props;
---
<div class="text-sm text-bbg-muted">
  <div class="font-bold text-bbg-ink">{chapterSlug}</div>
  <div class="text-xs">{pillarSlug}</div>
  <div class="mt-2 italic text-xs">Sidebar populates in P0.11.</div>
</div>
