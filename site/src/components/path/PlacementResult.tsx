// temporary stub — replaced by the real report in Task 8
import type { Locale } from "~/i18n";

export default function PlacementResult(_: { lang: Locale; priors: Map<string, number> }) {
  return null;
}
