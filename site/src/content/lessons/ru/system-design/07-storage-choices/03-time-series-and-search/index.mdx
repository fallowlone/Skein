---
slug: "03-time-series-and-search"
lang: ru
track: "system-design"
unit: "07-storage-choices"
order: 3
title: "Time-series и поисковые хранилища"
summary: "Time-series хранилища оптимизированы под запись (LSM, даунсэмплинг, retention) для append-only метрик; поисковые движки инвертируют текст в индекс терминов, где SQL LIKE рушится. Специализированное хранилище несёт двойную запись — добавляй, лишь когда primary не тянет."
estMin: 18
status: ready
lessonType: topic
level: middle
concepts:
  - time-series-db
  - lsm-tree
  - downsampling-retention
  - inverted-index
  - search-engine
  - dual-write-consistency
prereqs:
  - 04-data-distribution/01-replication
  - 07-storage-choices/01-sql-vs-nosql
  - 07-storage-choices/02-blob-and-object
sources:
  - https://aws.amazon.com/nosql/
  - https://docs.aws.amazon.com/timestream/latest/developerguide/what-is-timestream.html
  - https://www.elastic.co/guide/en/elasticsearch/reference/current/documents-indices.html
---
import Hook from "~/components/lesson/Hook.astro";
import Crux from "~/components/prose/Crux.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Inset from "~/components/lesson/Inset.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import MetaphorComplete from "~/components/pedagogy/MetaphorComplete.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";
import Infographic from "~/components/diagram/Infographic.astro";
import specializeProgression from "./infographics/specialize-progression.svg?raw";

<Hook>
Продакт-менеджер попросил две «маленькие» фичи: дашборд с CPU каждого сервера за последние 90 дней и поисковую строку, что находит товары по любому слову в названии или описании. Обе легли на существующий primary Postgres, потому что «это просто ещё таблицы». Таблица метрик росла на 50 миллионов строк в день, и её индексы молотили диск; поиск гнал `WHERE description LIKE '%word%'`, под который база не может использовать индекс, так что каждый поиск становился полным сканом таблицы, виснувшим под нагрузкой. Ни одна фича не была сложной — их просто просили у движка, построенного под другую форму работы. Реляционный primary — блестящее транзакционное хранилище, и плохой пожарный шланг метрик, и плохой поисковый движок, и никакой объём индексов это не меняет.
</Hook>

<Crux>Time-series — под запись метрик; поиск инвертирует текст под полнотекст. Специализируй, лишь когда primary не тянет; владей двойной записью.</Crux>

<Explanation>

## Time-series: другая форма записи

Через десять минут ты поймёшь, почему обе фичи из вступления были дорогими, какое свойство хранения каждая нарушала — и как выглядит правильное решение, когда те же требования появятся снова.

**Time-series данные** — поток измерений с метками времени, только на добавление: метрики серверов, показания IoT-сенсоров, события приложения, биржевые тики. Их форма необычна: записи многократно превосходят чтения, почти ничто никогда не обновляется и не удаляется поштучно, а запросы почти всегда — «агрегируй эту метрику по *этому окну времени*». **Time-series база** (Prometheus, InfluxDB, TimescaleDB, AWS Timestream) построена вокруг этой формы, и три свойства её определяют:

- **Хранение, оптимизированное под запись (LSM-деревья).** Вместо обновления данных на месте (что значит случайный seek диска на запись — медленно) **log-structured merge tree** буферизует записи в памяти и сбрасывает их на диск как отсортированные неизменяемые файлы, сливая их в фоне. В результате записи — последовательные append, единственное, что и шпиндельные, и твердотельные диски делают быстрее всего, поэтому LSM-хранилище впитывает куда большую пропускную способность записи, чем традиционное обновление на месте (B-дерево). Обмен — больше работы при чтении и во время фоновой компакции, и это нормально, потому что time-series тяжело на запись.
- **Даунсэмплинг.** Тебе не нужно посекундное разрешение для данных за прошлый год — оно нужно за последний час. Даунсэмплинг предагрегирует старые данные в более грубые корзины (посекундно → поминутно → почасово), сжимая их на порядки, сохраняя тренд.
- **Retention (срок хранения).** Старые сырые данные автоматически отбрасываются после окна (держи 7 дней сырыми, 90 дней по 1 минуте, 2 года по 1 часу). Retention — первоклассная декларативная функция: хранилище само удаляет устаревшие данные, так что хранение остаётся ограниченным вместо вечного роста.

Вместе эти три свойства означают, что time-series хранилище принимает данные на полной скорости, пока объём остаётся ограниченным и запросы по любому окну времени работают быстро — без малейшего давления на транзакционный primary. Без retention таблица растёт бесконечно; без даунсэмплинга старые данные в посекундном разрешении жрут место и тормозят диапазонные запросы; без LSM-записи диск становится узким местом ещё до того, как данные успели лечь.

Реляционный primary *может* хранить метрики, но это не та форма: B-tree индексы деградируют, когда миллиарды строк churn-ятся, нет встроенного даунсэмплинга или retention, а пожарный шланг записи конкурирует с транзакционным трафиком за тот же буфер-кеш и WAL — ровно то отравление, что ты видел с блобами в **02-blob-and-object**.

## Поиск: почему `LIKE` — это не поиск

Поисковая строка, что находит документы по любому слову, выглядит как запрос к базе, но это принципиально другой паттерн доступа. SQL-ный `WHERE description LIKE '%word%'` вынуждает **полный скан**: ведущий wildcard означает, что никакой B-tree индекс не поможет, так что движок читает и подстрочно сравнивает каждую строку. Он также не даёт ранжирования по релевантности, нет стемминга («running» матчит «run»), нет терпимости к опечаткам, нет многополевого скоринга. Работает на тысяче строк и рушится на миллионе.

**Поисковый движок** (Elasticsearch/OpenSearch, Lucene, собственный полнотекст Postgres через `tsvector`) решает это, строя **инвертированный индекс**. Где обычный индекс отображает строка → значения, инвертированный — наоборот: для каждого *термина* (слова) он хранит список документов, которые его содержат. Так «найди документы, содержащие `payment` и `failed`» становится двумя дешёвыми lookup-ами списков, пересечёнными — без скана. Поверх инвертированного индекса движок наслаивает то, чего `LIKE` никогда не сможет: токенизацию и стемминг (так «payments» находит «payment»), скоринг релевантности (какой документ — *лучший* матч, а не просто *какой-то*), нечёткий матч под опечатки и агрегации (фасеты/фильтры). Поэтому ты тянешься к поисковому движку в момент, когда «поиск» значит больше, чем «точный префиксный матч по одной колонке».

<Inset kind="why" lang="ru">
Почему инвертированный индекс быстр для текста там, где B-дерево безнадёжно? B-дерево индексирует *значение* колонки слева-направо, так что оно может найти строки, где колонка *начинается с* «pay», за логарифмическое время — но у `'%word%'` wildcard *слева*, так что нет префикса для seek и дерево бесполезно; ты сканируешь всё. Инвертированный индекс обходит это, индексируя *содержимое*, а не значения: при записи движок токенизирует каждый документ в термины и для каждого термина дописывает id документа в его posting-список. Запрос «payment failed» затем тянет два готовых posting-списка и пересекает их — работа пропорциональна тому, сколько документов содержат эти слова, а не размеру всего корпуса. Цена платится при записи (каждый документ надо проанализировать и обновить индекс) и в хранении (индекс может соперничать с данными по размеру), и именно поэтому поиск живёт в *отдельном, амортизированном по записи* движке, а не как фича, прикрученная к транзакционному primary.
</Inset>

<FlowDiagram
  label="Три нагрузки маршрутизируются в три purpose-built хранилища из одного приложения: транзакционные чтения и записи в реляционный primary, пожарный шланг метрик в оптимизированное под запись time-series хранилище, и текст документов в инвертированный индекс поискового движка."
  perRow={3}
  hue="var(--d-data)"
  nodes={[
    { id: "app", label: "приложение", sub: "три формы доступа" },
    { id: "oltp", label: "реляционный primary", sub: "ACID-строки, джойны" },
    { id: "ts", label: "time-series хранилище", sub: "LSM, даунсэмплинг, retention" },
    { id: "metrics", label: "пожарный шланг метрик", sub: "append-only, тяжело на запись" },
    { id: "search", label: "поисковый движок", sub: "инвертированный индекс, релевантность" },
    { id: "text", label: "полнотекстовые запросы", sub: "LIKE так не может" },
  ]}
  edges={[
    { from: "app", to: "oltp", label: "OLTP" },
    { from: "app", to: "ts", label: "метрики" },
    { from: "metrics", to: "ts" },
    { from: "app", to: "search", label: "поиск" },
    { from: "text", to: "search" },
  ]}
  caption="Каждое хранилище подходит под форму записи/чтения, которую остальные тянут плохо — и каждое новое хранилище, в которое пишет приложение, это ещё одна копия истины, которую надо держать в синхроне с primary. Специализация покупает скорость и платит согласованностью двойной записи."
/>

## Senior-решение: специализировать или перегрузить primary?

Каждое специализированное хранилище — реальная цена: ещё одна система запускать, мониторить, бэкапить, защищать и (трудная часть) держать в синхроне. Так что вопрос никогда не «лучше ли time-series база на метриках?» (очевидно да), а **«мой primary правда не тянет эту нагрузку, и стоит ли провал второй системы?»** Честная прогрессия:

1. **Начни на primary.** У Postgres удивительно способный полнотекстовый поиск (`tsvector`) и time-series расширения (TimescaleDB). Для умеренного масштаба одно хранилище куда дешевле головной боли согласованности двух.
2. **Специализируй, когда бьёт *измеренный* предел** — пожарный шланг метрик вытесняет транзакционный трафик, или `LIKE`-поиски сканируют миллионы строк и отваливаются по таймауту. Предел, а не модное слово, оправдывает второе хранилище.
3. **Считай специализированное хранилище производным индексом, а не источником истины.** Реляционный primary остаётся системой записи (source of truth); поисковый движок и time-series хранилище — оптимизированные под чтение *проекции* его. Это ключевая рамка: если поисковый индекс потерян, ты пересобираешь его из primary — ты не теряешь данные, лишь доступность этого пути запроса.

<Infographic
  svg={specializeProgression}
  label="Трёхшаговая эскалирующая лестница добавления специализированного хранилища: шаг первый — начни на primary (одно хранилище, Postgres tsvector и TimescaleDB достаточны на умеренном масштабе); шаг второй — специализируй, лишь когда бьёт измеренный предел: поток метрик вытесняет OLTP или LIKE упирается в таймаут; шаг третий — считай новое хранилище производным, оптимизированным под чтение индексом с primary как источником истины, пересобираемым из него при потере."
  caption="Измеренный предел, а не модное слово, оправдывает второе хранилище — и новое хранилище это производный индекс, а не второй источник истины."
  hue="var(--d-infra)"
/>

## Проблема двойной записи

В момент, когда запись должна лечь в *два* хранилища — строка в Postgres и документ в Elasticsearch — у тебя проблема **двойной записи (dual-write)**, и она сложнее, чем кажется. Наивный код пишет в оба последовательно: `db.save(order); search.index(order)`. Но это две системы без общей транзакции. Если процесс падает между двумя вызовами, или запись в поиск провалилась, а запись в БД прошла, хранилища расходятся — заказ существует, но не ищется, тихо, навсегда. Слепой ретрай может задвоить запись; обернуть их в «транзакцию» невозможно через две системы (распределённой транзакции тут нет). Надёжные паттерны делают *primary* единственным источником истины и выводят второе хранилище из его журнала записи:

- **Change Data Capture (CDC)** — хвостуй журнал репликации базы (её WAL/binlog) и стримь каждое закоммиченное изменение в поисковое/time-series хранилище. Коммит БД — единственное атомарное событие; обновление индекса — нижестоящий потребитель, который может ретраить и переигрывать.
- **Transactional outbox (транзакционный исходящий ящик)** — в той же транзакции БД, что пишет заказ, пиши ещё и событие «проиндексируй этот заказ» в таблицу `outbox`; отдельный релей читает outbox и толкает в поисковый движок. Поскольку строка заказа и строка outbox коммитятся атомарно, событие нельзя потерять.

Оба разделяют один принцип: **никогда не доверяй двум независимым записям остаться согласованными — сделай одно хранилище авторитетным и распространяй из его коммита.** Принять, что производное хранилище *в итоге* согласовано (новый заказ ищется через секунду), обычно нормально; притворяться, что двойная запись атомарна, — баг.

<Inset kind="mistake" lang="ru">
Классический провал двойной записи — «best-effort» обновление индекса, зарытое в обработчике запроса: пиши строку, потом выстрели вызов индексации поиска и проигнорируй (или просто залогируй) его провал, чтобы «держать запрос быстрым». Работает в каждом тесте и демо, потому что тесты не падают посреди обработчика, а сеть не рвётся в CI. В проде это дрейфует медленно — пара документов не индексируется при каждом деплое, икота очереди тут, таймаут там — и через три месяца «поиск теряет часть результатов» — неотлаживаемая загадка, потому что ничто не записало, *какие* записи потеряны. Починка — не больше ретраев в обработчике; это убрать двойную запись целиком, выведя индекс из журнала коммитов БД (CDC или outbox), так что сам акт коммита данных *есть* акт, гарантирующий их индексацию.
</Inset>

<Quiz
  id="07-time-series-and-search-quiz-1"
  lessonSlug="03-time-series-and-search"
  lang="ru"
  question="Твоя таблица метрик на реляционном primary берёт 50 млн append-only строк/день, и её B-tree индексы теперь молотят диск, конкурируя с транзакционным трафиком. Почему time-series хранилище — правильный ход, механически?"
  choices={[
    { label: "Его LSM-дерево превращает записи в последовательные append (быстро на любом диске) вместо обновлений B-tree на месте, а встроенные даунсэмплинг + retention ограничивают данные — совпадая с append-only, тяжёлой на запись, оконной по времени формой метрик.", correct: true },
    { label: "Time-series хранилища — просто более быстрые базы; перенеси туда всё.", misconception: "Они не универсально быстрее — они оптимизированы под одну форму (append-only, тяжело на запись, оконные по времени агрегаты) и хуже на транзакционных обновлениях строк и джойнах. Ты переносишь нагрузку метрик, а не OLTP." },
    { label: "Добавь больше B-tree индексов к существующей таблице, чтобы ускорить записи.", misconception: "Больше индексов делает ЗАПИСИ медленнее (каждая запись обновляет каждый индекс) — обратное тому, что нужно тяжёлому на запись шлангу. Механическая починка — append-стиль хранения LSM плюс даунсэмплинг/retention, чего реляционному primary не хватает." },
    { label: "Храни метрики как блобы в объектном хранилище.", misconception: "Объектное хранилище держит непрозрачные байты по ключу без агрегации или оконного по времени запроса — ты не сможешь гнать «средний CPU за 90 дней». Нагрузке нужен LSM-инжест и даунсэмплинг time-series движка, а не блоб-хранилище." },
  ]}
/>

<Quiz
  id="07-time-series-and-search-quiz-2"
  lessonSlug="03-time-series-and-search"
  lang="ru"
  question="Ты пишешь каждый новый заказ в Postgres, потом вызываешь индекс поиска в том же обработчике и логируешь-и-игнорируешь любой провал, «чтобы держать запрос быстрым». Месяцы спустя поиск непредсказуемо теряет часть заказов. В чём корневая причина и починка?"
  choices={[
    { label: "Двойная запись без общей транзакции: когда вызов индекса проваливается или процесс падает между записями, хранилища тихо расходятся. Починка — сделать БД авторитетной и вывести индекс из её журнала коммитов через CDC или транзакционный outbox.", correct: true },
    { label: "Поисковый движок багован; смени вендора.", misconception: "Ни один вендор это не чинит — баг архитектурный. Две независимые записи без общей транзакции разойдутся при любом падении или провале. Починка — перестать двойную запись и вывести индекс из коммита БД (CDC/outbox)." },
    { label: "Добавь агрессивные ретраи к вызову индекса в обработчике.", misconception: "Ретраи в обработчике не переживут падение процесса между двумя записями и рискуют задвоить индексацию; они ещё и замедляют запрос, который ты пытался держать быстрым. Выведи индекс из журнала коммитов, так что коммит данных гарантирует индексацию." },
    { label: "Оберни обе записи в одну транзакцию базы.", misconception: "Нет транзакции, охватывающей Postgres и поисковый движок — это отдельные системы без общего коммита. Нужна строка outbox, коммитящаяся атомарно ВМЕСТЕ с заказом, потом релей в индекс, или CDC с WAL." },
  ]}
/>

<MetaphorComplete
  id="07-time-series-and-search-metaphor-1"
  lessonSlug="03-time-series-and-search"
  lang="ru"
  setup="Поисковый движок находит документы по любому слову без скана корпуса, потому что строит _______ индекс: для каждого термина он хранит список содержащих его документов, так что запрос пересекает готовые списки вместо подстрочного сравнения каждой строки, как обязан SQL LIKE."
  accepted={["инвертированный", "инвертированный индекс", "обратный", "инвертированного"]}
  canonical="инвертированный"
  explanation="Инвертированный индекс отображает термин → список содержащих его документов (posting-список), построенный при записи токенизацией каждого документа. Многословный запрос тянет и пересекает эти posting-списки — работа пропорциональна совпавшим документам, а не всему корпусу — и поддерживает стемминг, скоринг релевантности и нечёткий матч, чего SQL LIKE с ведущим wildcard не может."
/>

</Explanation>

<KeyTakeaway>Time-series подходят append-only метрикам: LSM-append, даунсэмплинг, retention. Поиск строит инвертированный индекс, где LIKE сканирует. Специализируй на пределе как производный индекс; двойную запись — через CDC/outbox.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="07-time-series-and-search-retrieval"
  lang="ru"
  questions={[
    { q: "Какие три свойства подгоняют time-series хранилище под метрики и почему реляционный primary подходит плохо?", a: "(1) Оптимизированное под запись LSM-хранилище: записи буферизуются и сбрасываются как отсортированные неизменяемые файлы, становясь последовательными append (быстрейшее на любом диске) вместо случайных обновлений B-tree на месте — куда выше пропускная способность записи для append-only шланга. (2) Даунсэмплинг: старые данные предагрегируются в грубые корзины (посек → помин → почас), сжимаясь на порядки. (3) Retention: устаревшие сырые данные авто-отбрасываются по объявленному окну, так что хранение ограничено. Реляционный primary подходит плохо: B-tree индексы деградируют под миллиардами churn-ящихся строк, нет встроенного даунсэмплинга/retention, а пожарный шланг записи конкурирует с транзакционным трафиком за буфер-кеш и WAL." },
    { q: "Почему SQL LIKE — не поиск и как инвертированный индекс это чинит?", a: "WHERE col LIKE '%word%' имеет ведущий wildcard, так что префиксный seek B-tree не применим и движок полностью сканирует каждую строку; он также не даёт ранжирования по релевантности, стемминга, терпимости к опечаткам или многополевого скоринга. Поисковый движок строит инвертированный индекс: термин → posting-список содержащих его документов, построенный при записи токенизацией каждого документа. Запрос тянет и пересекает posting-списки (работа пропорциональна совпавшим документам, а не размеру корпуса) и наслаивает стемминг, скоринг релевантности, нечёткий матч и агрегации. Цена — при записи и в хранении индекса, поэтому поиск живёт в отдельном амортизированном по записи движке." },
    { q: "Сформулируй проблему двойной записи и два надёжных паттерна, что её решают.", a: "Когда запись должна лечь в два хранилища (строка в Postgres и документ в поисковом/time-series хранилище), нет общей транзакции, так что падение или провал второй записи тихо их расходит. Починка — сделать одно хранилище авторитетным (реляционный primary = source of truth) и вывести второе как в-итоге-согласованный индекс из его журнала коммитов: (1) Change Data Capture — хвостуй WAL/binlog и стримь каждое закоммиченное изменение в индекс, который может ретраить/переигрывать; (2) транзакционный outbox — пиши событие «проиндексируй» в таблицу outbox в ТОЙ ЖЕ транзакции, что и данные, потом релей толкает его в индекс. Оба превращают единственный коммит БД в событие, гарантирующее индексацию — никогда не доверяй двум независимым записям." },
  ]}
/>

<Recap lang="ru">
Реляционный primary — блестящее транзакционное хранилище и плохой пожарный шланг метрик и плохой поисковый движок. **Time-series хранилища** (Prometheus, InfluxDB, Timescale, Timestream) совпадают с append-only формой метрик тремя свойствами: **LSM-дерево**, превращающее записи в быстрые последовательные append, **даунсэмплинг**, сжимающий старые данные в грубые корзины, и **retention**, ограничивающий хранение авто-отбросом устаревших данных. **Поисковые движки** (Elasticsearch/OpenSearch, Lucene, Postgres `tsvector`) заменяют вынуждающий скан SQL-ный `LIKE` **инвертированным индексом** — термин → posting-список документов — так что полнотекстовые запросы пересекают готовые списки и получают стемминг, скоринг релевантности и нечёткий матч, платя цену при записи. Senior-решение не «лучше ли специализированное хранилище» (лучше), а «мой primary *измеримо* не тянет эту нагрузку?» — начни на primary, специализируй на реальном пределе и считай новое хранилище **производным индексом** с primary как источником истины. Эта рамка вскрывает реальную опасность: **проблему двойной записи**. Две независимые записи без общей транзакции тихо расходятся при любом падении или провале, так что ты никогда не делаешь вторую запись best-effort — ты выводишь её из журнала коммитов БД через **CDC** или **транзакционный outbox**, делая сам акт коммита данных актом, гарантирующим их индексацию. Теперь, когда встретишь обработчик, который пишет в БД, затем стреляет в индекс поиска и логирует провал — ты знаешь, в чём баг, почему он всплывёт в проде через месяцы, и какие два паттерна убирают его на уровне архитектуры.
</Recap>
