---
slug: "01-sql-vs-nosql"
lang: ru
track: "system-design"
unit: "07-storage-choices"
order: 1
title: "SQL против NoSQL"
summary: "Реляционка держит связи через ACID и схему; семейства NoSQL (документное, KV, wide-column, граф) меняют джойны и сильную согласованность на раскладку под паттерны доступа со scale-out. Senior-выбор — какую цену ты можешь платить, а не что моднее."
estMin: 18
status: ready
lessonType: topic
level: middle
concepts:
  - relational-model
  - document-store
  - wide-column
  - acid-vs-base
  - access-pattern-design
  - polyglot-persistence
prereqs:
  - 04-data-distribution/02-sharding-and-partitioning
  - 04-data-distribution/04-cap-and-pacelc
sources:
  - https://aws.amazon.com/nosql/
  - https://docs.aws.amazon.com/amazondynamodb/latest/developerguide/SQLtoNoSQL.html
  - https://docs.aws.amazon.com/amazondynamodb/latest/developerguide/bp-modeling-nosql-B.html
---
import Hook from "~/components/lesson/Hook.astro";
import Crux from "~/components/prose/Crux.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Inset from "~/components/lesson/Inset.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import MetaphorComplete from "~/components/pedagogy/MetaphorComplete.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import Infographic from "~/components/diagram/Infographic.astro";
import nosqlFamilies from "./infographics/nosql-families.svg?raw";

<Hook>
Команда запустила соцпродукт на документной базе с одной озвученной причиной: «не хотим возиться с миграциями». Год было блаженство — выкатил фичу, добавил поле, никакого `ALTER TABLE`. Потом продукт задал вопрос, которого запуск не предусматривал: *кто на кого подписан и что в тренде среди их подписок?* Это обход графа и агрегация по документам, и хранилище не умело ни того, ни другого, не вытащив половину датасета в приложение и не соединив его руками. Схема, которую они «избежали», не исчезла — она переехала из базы в тысячу хрупких путей кода, каждый из которых обязан помнить форму. Они не выбрали базу; они отложили решение, и счёт пришёл с процентами.
</Hook>

<Crux>Реляционка держит ACID; NoSQL меняет джойны и согласованность на раскладку под паттерны доступа со scale-out. Выбирай по цене, не по моде.</Crux>

<Explanation>

## Реляционный дефолт и что он даёт

Через десять минут ты будешь знать, какое именно свойство базы превращает отложенное решение в юридический риск — и какое можно безопасно разменять.

**Реляционная база** (Postgres, MySQL) хранит данные как таблицы типизированных строк, и связи между ними — первоклассны: ты **нормализуешь**, чтобы убрать дублирование, а потом собираешь обратно **джойнами** во время запроса. Движок держит **схему** (колонки, типы, ограничения) и даёт **ACID**-транзакции — атомарные «всё или ничего» записи, согласованность против твоих ограничений, изоляцию параллельных транзакций, долговечный коммит. Это не legacy-балласт; это контракт. База гарантирует, что перевод, списывающий с одного счёта и зачисляющий на другой, либо полностью происходит, либо полностью нет; что внешний ключ никогда не висит в пустоту; что две параллельные брони не схватят последнее место одновременно. Этот контракт и делает реляционку правильным *дефолтом* для всего, где деньги, остатки или важные связи — ты пишешь инвариант один раз, и движок держит его для каждого пути кода навсегда.

Цена в том, что реляционная модель предполагает *гибкие, произвольные* вопросы — `JOIN` чего угодно во время чтения — и эта гибкость стоит на масштабе: джойны по шардированному датасету дороги (строки лежат на разных машинах), а один primary, обслуживающий все записи, имеет потолок (урок из **04-data-distribution/02-sharding-and-partitioning**). Реляционка отлична, пока объём записей или размер данных не выходят за пределы того, что держит один primary плюс read-реплики.

## Семейства NoSQL: четыре разные ставки

Прежде чем потянуться к NoSQL, спроси себя: какой именно паттерн доступа делает реляционный движок не тем инструментом? Ответ определяет нужное семейство — они не взаимозаменяемы.

«NoSQL» — это не одно. Это четыре семейства, каждое оптимизировано под свою форму доступа:

- **Документное** (MongoDB, DocumentDB) — хранит самодостаточные JSON-подобные документы. Ты **денормализуешь**: вкладываешь связанные данные внутрь одного документа, чтобы чтение было одним lookup, без джойна. Хорошо, когда сущность читается целиком (страница товара, профиль пользователя).
- **Ключ-значение** (DynamoDB в простейшем режиме, Redis) — гигантская хеш-карта: дал ключ, получил блоб. Простейшая, быстрейшая модель; никаких запросов *внутрь* значения. Хорошо для сессий, корзин, lookup по известному id.
- **Wide-column** (Cassandra, Bigtable) — строки по ключу партиции с разреженными гибкими колонками; построены, чтобы впитывать огромную пропускную способность записи по кластеру без единого primary. Хорошо для лент и пожарных шлангов событий с метками времени.
- **Граф** (Neptune, Neo4j) — узлы и рёбра как первоклассные граждане, так что обходы («друзья друзей, кому нравится X») дёшевы. Хорошо для социальных графов, схем мошенничества, путей рекомендаций — ровно тот вопрос, на который команда из вступления не смогла ответить.

Смотри на этот список и замечай общее: каждое семейство выигрывает на конкретной, известной форме чтения и проигрывает, когда ты задаёшь вопрос, под который оно не проектировалось.

<Infographic
  svg={nosqlFamilies}
  label="Майнд-карта с корнем NoSQL, ветвящимся на четыре семейства — Документное (MongoDB, читает целый денормализованный документ одним lookup), Ключ-значение (Redis/DynamoDB, хеш-карта по id), Wide-column (Cassandra/Bigtable, огромный throughput записи без единого primary) и Граф (Neptune/Neo4j, дешёвые обходы) — каждое подписано той формой доступа, под которую построено."
  caption="NoSQL — не одно: четыре разные ставки, каждая быстра на своей известной форме доступа и слаба на вопросах, под которые не проектировалась."
  hue="var(--d-infra)"
/>

Объединяющая тема: хранилища NoSQL построены, чтобы **масштабироваться вширь** на commodity-кластерах и быть **быстрыми для известного паттерна доступа**, отказываясь от способности реляционного движка отвечать на вопросы, которые ты не запланировал. AWS описывает семейство так же — purpose-built движки, гибкие схемы, горизонтальный масштаб — в обмен на ослабление части гарантий согласованности, которые держит реляционный движок.

## ACID против BASE: согласованность, которой ты торгуешь

Самое глубокое отличие — не форма данных, а контракт согласованности. Реляционные системы целятся в **ACID**. Многие распределённые NoSQL вместо этого предлагают **BASE**: **B**asically **A**vailable (в основном доступна), **S**oft state (мягкое состояние), **E**ventually consistent (в итоге согласована). При BASE запись может быть видна не каждому читателю сразу; реплики сходятся «в итоге» (обычно за миллисекунды). Это не неряшливость — это прямое следствие **CAP/PACELC** (урок из **04-data-distribution/04-cap-and-pacelc**): хранилище, остающееся доступным при сетевом разделении, *не может* быть ещё и сильно согласованным. NoSQL-хранилища, ставящие доступность и устойчивость к разделению выше, выбирают итоговую согласованность намеренно, и многие (DynamoDB, Cassandra) дают включить более сильную согласованность на запрос за надбавку к задержке и стоимости.

Так что вопрос SQL-против-NoSQL на самом деле: *терпит ли твоя предметная область итоговую согласованность на этих данных?* Счётчик «лайков», устаревший на 200 мс, — нормально. Баланс счёта, устаревший на 200 мс во время снятия, — это иск.

<Inset kind="why" lang="ru">
Почему «взяли Mongo, чтобы избежать схем» так надёжно бьёт обратно? Потому что schema-on-write (проверка схемы при записи, реляционка) и schema-on-read (проверка схемы при чтении, документная база) не убирают схему — они переносят *того, кто её держит*. При schema-on-write база отвергает кривые данные на входе, один раз. При schema-on-read каждый читатель обязан защитно обрабатывать любую историческую форму, которую данные когда-либо принимали: документ из v1 без поля `country`, v2, где `country` была строкой, v3, где она стала объектом. Через пять лет и сорок деплоев твоя «бессхемная» коллекция имеет дюжину неявных форм, а логика валидации размазана по кодовой базе вместо одного объявленного места. Гибкость на записи — это заём под чтение, и проценты платит каждый инженер, кто потом трогает данные.
</Inset>

## Дизайн под паттерны доступа: настоящий навык NoSQL

Хорошо использовать NoSQL — это не «реляционка без джойнов», а перевёрнутый процесс проектирования. В реляционном моделировании ты проектируешь *данные* (нормализуешь в чистые сущности), а потом пишешь любые нужные запросы. В NoSQL-моделировании ты проектируешь *запросы первыми*: перечисляешь каждый паттерн доступа, который будет у приложения, и затем формируешь хранение так, чтобы каждый был одним дешёвым lookup. Руководство AWS по DynamoDB прямо об этом — ты моделируешь таблицу вокруг паттернов доступа, часто схлопывая много типов сущностей в одну таблицу с тщательно выбранными ключами партиции и сортировки, чтобы запрос возвращал ровно то, что нужно одному экрану, уже соединённым.

Это по-настоящему мощно — так хранилище отдаёт персональную ленту сотням миллионов пользователей с задержкой в единицы миллисекунд. Но у этого жёсткий край: оно работает только если ты *знаешь паттерны доступа заранее*, и жестоко наказывает, когда появляется новый. Новый запрос, не совпадающий с дизайном ключей, вынуждает либо дорогой полный скан, либо вторичный индекс (больше стоимости, больше итоговой согласованности), либо миграцию данных. Гибкость реляционки — ровно то, чем NoSQL торгует, поэтому NoSQL блестит на хорошо понятных высоконагруженных паттернах и больно кусает на меняющихся, исследовательских продуктах.

<FlowDiagram
  label="Одни и те же данные интернет-магазина смоделированы двумя способами. Реляционная модель нормализует в отдельные таблицы Users, Orders, Items, соединяемые при чтении; документная денормализует весь заказ — снимок клиента и позиции — в один самодостаточный документ, читаемый одним запросом."
  perRow={3}
  hue="var(--d-data)"
  nodes={[
    { id: "rel", label: "РЕЛЯЦИОННАЯ", sub: "нормализация + join при чтении" },
    { id: "users", label: "таблица users", sub: "id, имя, email" },
    { id: "orders", label: "таблица orders", sub: "FK → user, FK → items" },
    { id: "doc", label: "ДОКУМЕНТ", sub: "денормализация, 1 чтение" },
    { id: "orderdoc", label: "документ заказа", sub: "вкладывает user + позиции" },
    { id: "read", label: "1 lookup", sub: "без джойна, форма готова" },
  ]}
  edges={[
    { from: "rel", to: "users" },
    { from: "rel", to: "orders" },
    { from: "users", to: "orders", label: "JOIN" },
    { from: "doc", to: "orderdoc" },
    { from: "orderdoc", to: "read" },
  ]}
  caption="Реляционка меняет работу при чтении (джойн) на гибкость и отсутствие дублирования; документ меняет дублирование и стоимость «обнови везде» на одно быстрое чтение. Ничто не бесплатно — ты выбираешь, какую цену платить и на какой стороне раскола чтение/запись."
/>

## Полиглот-персистентность: перестань выбирать одно

Зрелый ответ на «SQL или NoSQL?» обычно — *оба*. **Полиглот-персистентность** означает использование более одного хранилища в одной системе, каждое — под то, в чём оно лучше: реляционный primary под заказы и деньги (ACID), key-value кеш под сессии, wide-column под пожарный шланг событий, поисковый движок под полнотекст, граф под социальные рёбра. Цена — операционная: больше систем запускать, мониторить и держать в синхроне, а опасная часть — держать данные согласованными *между* хранилищами (проблема двойной записи, разбирается в **03-time-series-and-search**). Но альтернатива — гнать каждую нагрузку через один движок — гарантирует, что хотя бы для половины из них ты используешь не тот инструмент. Senior-дизайн — про композицию хранилищ, а не коронацию одного.

<TradeoffMatrix
  id="01-sql-vs-nosql-tradeoff"
  lessonSlug="01-sql-vs-nosql"
  lang="ru"
  prompt="Финтех-стартап строит леджер, записывающий каждый дебет и кредит между счетами. Схема фиксирована и понятна. Нужны атомарные многострочные транзакции (дебет A, кредит B) и строгая консистентность каждого чтения. Какое хранилище подходит?"
  options={[
    { name: "Реляционная база (Postgres)", summary: "ACID-транзакции обеспечивают инвариант дебет-кредит в движке, а не в коде приложения.", justification: "Реляционная база даёт атомарные многострочные транзакции нативно: дебет и кредит либо оба коммитятся, либо оба откатываются, и ни один конкурентный читатель никогда не увидит промежуточного состояния. Схема — первоклассное ограничение: болтающийся внешний ключ или отрицательный баланс нарушат CHECK-ограничение до записи. Строгая консистентность по умолчанию: каждое чтение видит последнюю закоммиченную запись. Для фиксированной схемы с инвариантами корректности, которые обязаны выполняться в каждом пути кода, реляционка — это именно правильный инструмент.", correct: true },
    { name: "Wide-column хранилище (Cassandra)", summary: "Огромный throughput записи, но атомарные многострочные транзакции и строгая консистентность требуют усилий.", justification: "Cassandra оптимизирована под гигантский throughput записи без единого primary-узла. Однако атомарность через несколько partition-ключей (дебет счёт A на одной партиции, кредит счёт B на другой) требует lightweight-транзакций (LWT) или саг на уровне приложения — оба сложны и дороги. Дефолтная консистентность Cassandra — eventual; строгие чтения требуют quorum-флагов, добавляющих задержку. Это неверное первичное хранилище для леджера, где корректность — главный приоритет." },
    { name: "Документная база (MongoDB)", summary: "Гибкая схема и атомарность в рамках одного документа — но кросс-документные транзакции возможны, однако сложнее.", justification: "MongoDB добавила мульти-документные транзакции в v4.0, так что атомарность дебет-кредит выразить можно. Однако сила документной модели — читать самодостаточную сущность одним lookup'ом, а не обеспечивать ссылочную целостность между сущностями. Валидация схемы opt-in (JSON Schema validators), а не принудительна по умолчанию — значит деплой, случайно пропустивший поле, молча проходит. Для фиксированной схемы с жёсткими инвариантами принудительное schema-on-write реляционки проще и устойчивее." },
    { name: "Key-value хранилище (Redis)", summary: "Суб-миллисекундная задержка для lookup'ов — но нет нативных атомарных кросс-ключевых транзакций, а durability в памяти ограничена.", justification: "Redis — это хеш-таблица: дай ключ, получи blob, суб-миллисекунда. MULTI/EXEC даёт оптимистичную атомарность по нескольким ключам, но Redis прежде всего in-memory хранилище — durability зависит от настроек fsync и AOF/RDB-снапшотов, вводящих окна восстановления с возможной потерей данных. Для леджера, где каждый дебет и кредит обязан пережить сбой, нужна disk-first ACID база; Redis — правильный уровень для сессий, кешей и лидербордов, где небольшая потеря данных приемлема." },
  ]}
/>

<Inset kind="mistake" lang="ru">
Самая дорогая ошибка SQL-против-NoSQL — выбор под *масштаб, которого у тебя ещё нет*. Команда читает, что Cassandra отдаёт миллионы записей в секунду, и тянется к ней в первый день — когда у них 50 записей в секунду и ноль понимания своих паттернов доступа. Они купили операционное бремя и жёсткость под паттерны горизонтального хранилища ради проблемы, которой не будет годами, отдав при этом джойны, транзакции и гибкость запросов, которые ранней стадии продукта нужны *больше всего* (его требования меняются еженедельно). Честный дефолт — одна реляционная база, пока *измеренное* узкое место — реальный потолок записи, реальная стена задержки — не назовёт конкретное NoSQL-хранилище, которое его решает. Преждевременный NoSQL — это преждевременная оптимизация в одежде модного слова.
</Inset>

<Quiz
  id="07-sql-vs-nosql-quiz-1"
  lessonSlug="01-sql-vs-nosql"
  lang="ru"
  question="Платёжная фича должна гарантировать, что списание с одного счёта и зачисление на другой либо оба происходят, либо ни одно, без промежуточного состояния, видимого другим транзакциям. Какое свойство хранилища тут не обсуждается?"
  choices={[
    { label: "ACID-транзакции (атомарность + изоляция) — обычно реляционка, потому что инвариант «всё или ничего» по нескольким строкам должен держать движок, а не надежда в коде приложения.", correct: true },
    { label: "Итоговая согласованность (BASE) — она быстрее, и балансы сойдутся.", misconception: "Схождение «в итоге» — ровно то, что недопустимо для денег: окно, где один счёт списан, а другой ещё нет, видимое параллельному чтению, — реальный баг с правовыми последствиями. Этому инварианту нужны ACID-атомарность + изоляция." },
    { label: "Горизонтальный масштаб записи — взять wide-column, чтобы не было узкого места.", misconception: "Масштаб записи не отвечает на требование — это инвариант корректности по двум строкам. Wide-column, оптимизированный под пропускную способность записи, обычно ослабляет ту самую многострочную транзакционную гарантию, что тут нужна." },
    { label: "Гибкая схема, чтобы добавлять поля без миграций.", misconception: "Гибкость схемы не относится к требованию. Необсуждаемое свойство — атомарные, изолированные многострочные транзакции, это ACID-гарантия, а не схемная." },
  ]}
/>

<Quiz
  id="07-sql-vs-nosql-quiz-2"
  lessonSlug="01-sql-vs-nosql"
  lang="ru"
  question="Команда выбрала документную базу «чтобы избежать схем». Два года спустя каждый путь чтения усеян проверками на отсутствующие и разнотипные поля. Что на самом деле произошло?"
  choices={[
    { label: "Схема не исчезла — она переехала из schema-on-write (держит база один раз) в schema-on-read (защитно перепроверяет каждый читатель), так что валидация размазана по кодовой базе.", correct: true },
    { label: "Выбрали не ту документную базу; другая осталась бы чистой.", misconception: "Ни одно документное хранилище не убирает проблему схемы — она присуща schema-on-read. Без проверки на записи накопленные исторические формы приходится обрабатывать при чтении независимо от вендора." },
    { label: "Документные базы просто не могут хранить согласованные данные.", misconception: "Могут; многие предлагают опциональную валидацию схемы. Проблема тут в том, что команда отказалась от проверки на записи, неявная схема дрейфовала, и за это платит каждый читатель. Схема переехала, а не исчезла." },
    { label: "Команде надо было денормализовать сильнее.", misconception: "Денормализация — про форму чтения, а не про принуждение наличия/типов полей. Больше вложенности не починит дрейфующие формы полей; починит валидация на записи (или реляционная схема)." },
  ]}
/>

<MetaphorComplete
  id="07-sql-vs-nosql-metaphor-1"
  lessonSlug="01-sql-vs-nosql"
  lang="ru"
  setup="В реляционном моделировании ты проектируешь данные первыми и запрашиваешь свободно; в NoSQL ты переворачиваешь это — проектируешь _______ первыми, затем формируешь хранение так, чтобы каждый был одним дешёвым lookup, поэтому новый незапланированный дорог."
  accepted={["паттерны доступа", "паттерн доступа", "запросы", "паттерны", "запрос"]}
  canonical="паттерны доступа"
  explanation="NoSQL-моделирование начинается с паттернов доступа: перечисли каждый запрос приложения, затем выбери ключи партиции/сортировки так, чтобы каждый был одним дешёвым lookup с уже соединёнными данными. Мощь (чтения в единицы мс на масштабе) — из знания паттернов заранее; цена — новый паттерн, не совпавший с дизайном ключей, вынуждает скан, вторичный индекс или миграцию."
/>

</Explanation>

<KeyTakeaway>Реляционка — дефолт: ACID + схема держат инварианты для каждого пути кода. NoSQL меняет джойны и согласованность на раскладку под паттерны доступа — мощно при известных, больно при меняющихся. Зрелые системы — полиглот.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="07-sql-vs-nosql-retrieval"
  lang="ru"
  questions={[
    { q: "Назови четыре семейства NoSQL и форму доступа, под которую построено каждое.", a: "Документное (MongoDB) — самодостаточные денормализованные документы, читаемые целиком, один lookup, без джойна. Ключ-значение (DynamoDB простой режим, Redis) — хеш-карта, lookup по известному id, быстрейшее, без запросов внутрь значения. Wide-column (Cassandra, Bigtable) — строки по ключу партиции, построены впитывать огромную пропускную способность записи по кластеру без единого primary; идеально для пожарных шлангов событий и лент. Граф (Neptune, Neo4j) — узлы и рёбра как первоклассные граждане, обходы (друзья друзей) дёшевы. Все четыре масштабируются вширь и быстры для известного паттерна ценой произвольной гибкости запросов реляционки." },
    { q: "В чём обмен ACID-против-BASE и как CAP его навязывает?", a: "ACID (реляционка) даёт атомарные, согласованные, изолированные, долговечные транзакции — движок держит инварианты для каждого пути кода. BASE (многие распределённые NoSQL) — Basically Available, Soft state, Eventually consistent: запись не мгновенно видна везде, реплики сходятся после короткой задержки. CAP/PACELC навязывает выбор: хранилище, остающееся доступным при сетевом разделении, не может быть ещё и сильно согласованным, поэтому хранилища с приоритетом доступности выбирают итоговую согласованность (часто опциональную сильнее на запрос за плату). Вопрос дизайна — терпят ли данные устаревание: счётчик лайков да, баланс счёта нет." },
    { q: "Почему дизайн под паттерны доступа режет в обе стороны и что такое полиглот-персистентность?", a: "Переворот дизайна (моделируй запросы первыми, формируй хранение так, чтобы каждый был одним дешёвым уже соединённым lookup) — так NoSQL отдаёт ленты с задержкой в единицы мс сотням миллионов пользователей — но работает только если паттерны известны заранее и наказывает новый незапланированный запрос сканом, вторичным индексом или миграцией. Полиглот-персистентность — зрелый ответ: используй несколько хранилищ, каждое под то, в чём лучше (реляционка под деньги, KV под сессии, wide-column под события, поиск под текст, граф под рёбра), приняв больше операционной поверхности и межхранилищную проблему двойной записи в обмен на правильный инструмент под каждую нагрузку." },
  ]}
/>

<Recap lang="ru">
**Реляционка** — правильный дефолт: схема и **ACID**-транзакции позволяют объявить инвариант один раз и заставить движок держать его для каждого пути кода навсегда — это необходимо для денег, остатков и важных связей. Её цена — джойны по шардированному датасету дороги, а единственный primary записи имеет потолок. **NoSQL** — четыре разные ставки: **документное** (денормализация, чтение целиком), **ключ-значение** (хеш-карта, lookup по id), **wide-column** (огромная пропускная способность записи, без единого primary) и **граф** (дешёвые обходы) — все построены **масштабироваться вширь** и быть быстрыми для *известного* паттерна доступа ценой произвольной гибкости запросов и, обычно, сильной согласованности (**BASE**, навязанной CAP). Провал «взяли Mongo, чтобы избежать схем» — маркер: схема не исчезает, а переезжает из одной проверки на записи в защитную перепроверку в каждом читателе. Настоящий навык NoSQL — дизайн **под паттерны доступа** — мощный на известном масштабе, больной при меняющихся паттернах. Senior-ответ редко — одно хранилище: **полиглот-персистентность** компонует правильный движок под каждую нагрузку, платя операционной поверхностью и межхранилищной проблемой двойной записи — выбирая по *измеренной* цене, а не по тому, какая база в моде. Теперь, когда на ревью дизайна прозвучит «SQL или NoSQL?», твой первый ход — назвать конкретный паттерн доступа и допустимость итоговой согласованности: эти два ответа сами выберут хранилище.
</Recap>
