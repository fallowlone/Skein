---
slug: "quiz-choice"
lang: ru
track: "system-design"
unit: "07-storage-choices"
order: 4
title: "Выбор хранилища: обзор с выбором ответа"
summary: "Синтез всего раздела хранилищ в формате с выбором ответа: SQL против NoSQL и обмен ACID/BASE, дизайн под паттерны доступа, блобы против объектного хранилища с presigned-ссылками, долговечность против доступности, специализированные хранилища и проблема двойной записи."
estMin: 13
status: ready
lessonType: topic
level: senior
concepts:
  - acid-vs-base
  - access-pattern-design
  - object-storage
  - durability-vs-availability
  - dual-write-consistency
  - polyglot-persistence
prereqs:
  - 07-storage-choices/01-sql-vs-nosql
  - 07-storage-choices/02-blob-and-object
  - 07-storage-choices/03-time-series-and-search
sources:
  - https://aws.amazon.com/nosql/
  - https://docs.aws.amazon.com/AmazonS3/latest/userguide/Welcome.html
  - https://www.elastic.co/guide/en/elasticsearch/reference/current/documents-indices.html
---
import Hook from "~/components/lesson/Hook.astro";
import Crux from "~/components/prose/Crux.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Шесть вопросов через весь раздел. Каждый — решение, которое ты принимаешь на дизайн-ревью или у доски: какое хранилище подходит нагрузке, где живут байты, какой согласованностью ты торгуешь и как два хранилища держатся в синхроне — не определение для пересказа.
</Hook>

<Crux>Шесть решений: ACID-нагрузка, ловушка паттернов доступа, где живут байты, долговечность против доступности, спец-хранилище и двойная запись.</Crux>

<Explanation>

Убедись, что умеешь разместить нагрузку на оси SQL/NoSQL по нужной согласованности, заметить, когда дизайн под паттерны доступа укусит, держать байты вне базы, разделить долговечность и доступность, выбрать специализированное хранилище на измеренном пределе и избежать тихого расхождения двойной записи.

<Quiz
  id="07-storage-choice-1"
  lessonSlug="quiz-choice"
  lang="ru"
  question="Фича должна атомарно перевести деньги между двумя строками без промежуточного состояния, видимого параллельным транзакциям. Какое свойство не обсуждается и какой класс хранилища его обычно даёт?"
  choices={[
    { label: "ACID-атомарность + изоляция, держимые движком — обычно реляционка. Инвариант «всё или ничего» по нескольким строкам нельзя оставлять коду приложения.", correct: true },
    { label: "BASE итоговая согласованность — быстрее, и балансы сойдутся.", misconception: "Схождение «в итоге» — ровно то окно, что недопустимо для денег: одна строка списана, другая ещё нет, видимо параллельному чтению. Этому нужны ACID-атомарность + изоляция, а не BASE." },
    { label: "Горизонтальный масштаб записи через wide-column.", misconception: "Масштаб записи не отвечает на инвариант корректности по двум строкам. Wide-column, заточенный под пропускную способность записи, обычно ослабляет многострочную транзакцию, что тут нужна." },
    { label: "Гибкая схема, чтобы добавлять поля без миграций.", misconception: "Гибкость схемы не относится к атомарному движению денег. Необсуждаемое — атомарные, изолированные многострочные транзакции, это ACID-гарантия." },
  ]}
/>

<Quiz
  id="07-storage-choice-2"
  lessonSlug="quiz-choice"
  lang="ru"
  question="Ты смоделировал key-value таблицу под «получить элемент по id». Полгода спустя продукту нужно «перечислить все элементы клиента, новые сверху», а клиента в ключе нет. Что предсказывает дизайн под паттерны доступа?"
  choices={[
    { label: "У запроса нет ключа для seek, так что на существующей таблице он деградирует в полный скан; реальные варианты — вторичный индекс (больше стоимости, итоговая согласованность), редизайн ключа или миграция — ничто не бесплатно.", correct: true },
    { label: "Ничего — NoSQL запрашивает любое поле так же быстро, как реляционный WHERE.", misconception: "Не так. Без совпадающего ключа хранилище может только сканировать. Дизайн под паттерны доступа быстр для запланированных и наказывает незапланированные — противоположность произвольной гибкости реляционки." },
    { label: "Просто добавь B-tree индекс, как в SQL, и решено дёшево.", misconception: "Вторичные индексы NoSQL — не бесплатные реляционные B-деревья: они добавляют стоимость и обычно итогово согласованы. Суть остаётся — новый паттерн не заложили в дизайн, так что он дорог." },
    { label: "Переведи всю систему на граф.", misconception: "Запрос «список по клиенту» — не обход; граф не подходит. Урок в том, что дизайн ключей не предусмотрел этот паттерн, а не в том, что семейство неверно." },
  ]}
/>

<Quiz
  id="07-storage-choice-3"
  lessonSlug="quiz-choice"
  lang="ru"
  question="Видеоприложение хранит многогигабайтные загрузки как BYTEA в Postgres, «чтобы байты и метаданные были в одной транзакции». Что ломается на масштабе и какова починка?"
  choices={[
    { label: "Бэкапы раздуваются, буфер-кеш отравлен байтами блобов, WAL/репликация раздуваются, а пул соединений голодает. Починка: байты в объектном хранилище под ключом, маленькая строка метаданных в БД, отдача через presigned-ссылки.", correct: true },
    { label: "Ничего — держать байты и метаданные транзакционными — чистый дизайн.", misconception: "Эта единственная «выгода» затмевается затратами: раздутые бэкапы, отравленный буфер-кеш, раздутый WAL и голодание пула. Базы — для маленьких структурированных строк; крупные байты — в объектном хранилище." },
    { label: "Просто добавь read-реплики, чтобы размазать чтения блобов.", misconception: "Реплики наследуют то же раздувание (и ты гонишь каждый блоб в поток репликации). Структурная починка — убрать блобы из БД целиком: метаданные в БД, байты в объектном хранилище." },
    { label: "Храни байты на локальном диске сервера приложения.", misconception: "Локальный диск цепляется к одной машине, не долговечен при смерти машины и ломается с добавлением второго узла. Объектное хранилище — машинонезависимый долговечный дом для крупных байтов." },
  ]}
/>

<Quiz
  id="07-storage-choice-4"
  lessonSlug="quiz-choice"
  lang="ru"
  question="«S3 долговечен на одиннадцать девяток, так что наши картинки переживут любой сбой, и межрегиональная репликация не нужна». В чём точная поправка?"
  choices={[
    { label: "Долговечность (байты всё ещё существуют, ~11 девяток) — не доступность (байты достижимы сейчас, часто ~3 девятки). Сбой региона оставляет данные долговечными, но недостижимыми; пережить его — нужна межрегиональная репликация.", correct: true },
    { label: "Верно — одиннадцать девяток значит ещё и всегда достижимо.", misconception: "Долговечность и доступность — разные гарантии. Одиннадцать девяток — про то, чтобы НЕ ПОТЕРЯТЬ данные; достижимость — отдельное число пониже, а сбой региона ломает достижимость, не теряя байта." },
    { label: "Долговечность не важна; для картинок важна только задержка.", misconception: "Долговечность важна (поэтому загрузки не теряются), но утверждение спутало её с доступностью. Починка — разделить «всё ещё существует» и «достижимо сейчас», потом добавить межрегиональную репликацию под сбои." },
    { label: "Перенеси картинки в базу ради безопасности при сбое.", misconception: "Это возвращает все провалы блобов-в-базе и не улучшает региональную доступность. Правильный ответ — межрегиональная репликация объектного хранилища." },
  ]}
/>

<Quiz
  id="07-storage-choice-5"
  lessonSlug="quiz-choice"
  lang="ru"
  question="Поисковая строка гонит `WHERE description LIKE '%word%'` по таблице на 3 млн строк с B-tree индексом на description и отваливается по таймауту под нагрузкой. Каков вердикт и правильный инструмент?"
  choices={[
    { label: "Ведущий wildcard делает B-tree бесполезным, вынуждая полный скан всех 3 млн строк на запрос. Используй инвертированный индекс (поисковый движок или Postgres tsvector): термин → posting-список, плюс стемминг и релевантность, чего LIKE не может.", correct: true },
    { label: "Добавь больше B-tree индексов на description, и он их использует.", misconception: "Никакой B-tree не помогает LIKE с ведущим wildcard — нет префикса для seek, так что сканирует в любом случае. Починка — инвертированный индекс, что отображает термины в документы, а не ещё B-tree." },
    { label: "Всё нормально; LIKE — это настоящий поиск, просто нужен инстанс побольше.", misconception: "Инстанс побольше отодвигает стену, не убирает её: это всё ещё O(строк) на запрос и без ранжирования, стемминга и терпимости к опечаткам. LIKE — не поиск; инвертированный индекс — да." },
    { label: "Храни описания в объектном хранилище, чтобы разгрузить БД.", misconception: "У объектного хранилища нет возможности запроса вообще — нет WHERE, нет текстового матча. Нагрузке нужен инвертированный индекс поискового движка, а не блоб-хранилище." },
  ]}
/>

<Quiz
  id="07-storage-choice-6"
  lessonSlug="quiz-choice"
  lang="ru"
  question="Ты пишешь заказ в Postgres, потом best-effort индексируешь его в Elasticsearch (логируешь и игнорируешь провалы), чтобы держать запрос быстрым. Месяцы спустя поиск непредсказуемо теряет заказы. Корневая причина и починка?"
  choices={[
    { label: "Двойная запись без общей транзакции: падение или провал вызова индекса тихо расходит хранилища. Сделай Postgres авторитетным и выведи индекс из его коммита через CDC или транзакционный outbox.", correct: true },
    { label: "Поисковый движок багован; смени вендора.", misconception: "Ни один вендор это не чинит — это архитектурное. Две независимые записи без общей транзакции расходятся при любом падении или провале. Выведи индекс из коммита БД (CDC/outbox)." },
    { label: "Добавь агрессивные ретраи к вызову индекса в обработчике.", misconception: "Ретраи не переживут падение между двумя записями и рискуют задвоить индексацию, замедляя запрос, что ты хотел держать быстрым. Выведи индекс из журнала коммитов, так что коммит данных гарантирует индексацию." },
    { label: "Оберни обе записи в одну транзакцию базы.", misconception: "Нет транзакции, охватывающей Postgres и Elasticsearch — отдельные системы, нет общего коммита. Нужна строка outbox, коммитящаяся атомарно с заказом, или CDC с WAL." },
  ]}
/>

</Explanation>

<KeyTakeaway>Выбор хранилища — подгонка нагрузки под гарантии: ACID под деньги, паттерны доступа лишь при известных, байты в объектном хранилище, долговечность ≠ доступность, спец-хранилища на пределе, двойная запись из коммита.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="07-storage-choice-retrieval"
  lang="ru"
  questions={[
    { q: "Почему «долговечно на одиннадцать девяток» — не «всегда доступно» и что закрывает разрыв?", a: "Долговечность (~11 девяток) — вероятность того, что байты всё ещё существуют и не повреждены — достигается репликацией по дискам/площадкам. Доступность — вероятность достать их прямо сейчас, куда меньшее число (~3 девятки), потому что сбой региона может сделать совершенно долговечные данные недостижимыми, не потеряв байта. Разрыв закрывается межрегиональной репликацией плюс путём чтения с failover — отдельный платный выбор." },
    { q: "Как держать поисковый индекс согласованным с базой без двойной записи?", a: "Никогда не делай best-effort две независимые записи — они тихо расходятся при падении или провале второй записи. Сделай базу единственным источником истины и выведи индекс из её коммита: транзакционный outbox (пиши строку «проиндексируй» в той же транзакции, релей её толкает) или Change Data Capture (хвостуй WAL/binlog и стримь закоммиченные изменения). В любом случае единственный коммит БД — событие, гарантирующее индексацию, а индекс становится в-итоге-согласованной производной проекцией, пересобираемой из primary." },
  ]}
/>

<Recap lang="ru">
Сквозная линия — выбор хранилища это подгонка нагрузки под гарантии, которые хранилище реально даёт. Деньгам нужны **ACID**-атомарность и изоляция (реляционка), а не BASE — схождение «в итоге» это окно, которого нельзя допустить. Дизайн **под паттерны доступа** NoSQL быстр для запланированных паттернов и зверский для незапланированных, так что новый запрос без совпадающего ключа деградирует в скан. Крупные байты никогда не идут в базу (раздутые бэкапы, отравленный буфер-кеш, раздутый WAL, голодание пула) — держи **метаданные в БД, байты в объектном хранилище**, отдавая через **presigned-ссылки**. **Долговечность — не доступность**: одиннадцать девяток значат, что байты не потеряются, а не что регион не может погаснуть, для чего и нужна межрегиональная репликация. Специализированное хранилище (time-series, поиск) заслуживает место на **измеренном пределе** и считается **производным индексом**, а **проблема двойной записи** решается выводом этого индекса из одного авторитетного коммита через CDC или транзакционный outbox — никогда две независимые записи.
</Recap>
