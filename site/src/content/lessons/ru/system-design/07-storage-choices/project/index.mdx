---
slug: "project"
lang: ru
track: "system-design"
unit: "07-storage-choices"
order: 7
title: "Выбор хранилища: полиглот медиа-сервис"
summary: "Практический проект: собери бэкенд обмена медиа, что кладёт каждую нагрузку на правильное хранилище — реляционные метаданные, байты в объектном хранилище через presigned-ссылки, поисковый индекс — и осознанно решает двойную запись между базой и индексом."
estMin: 240
status: ready
lessonType: topic
level: senior
concepts:
  - polyglot-persistence
  - metadata-bytes-split
  - presigned-urls
  - inverted-index
  - dual-write-consistency
  - acid-vs-base
prereqs:
  - 07-storage-choices/01-sql-vs-nosql
  - 07-storage-choices/02-blob-and-object
  - 07-storage-choices/03-time-series-and-search
sources:
  - https://docs.aws.amazon.com/AmazonS3/latest/userguide/PresignedUrlUploadObject.html
  - https://docs.aws.amazon.com/AmazonS3/latest/userguide/Welcome.html
  - https://www.elastic.co/guide/en/elasticsearch/reference/current/documents-indices.html
---
import Hook from "~/components/lesson/Hook.astro";
import Crux from "~/components/prose/Crux.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import ProjectBrief from "~/components/pedagogy/ProjectBrief.astro";

<Hook>
Прочитать, что надо отделить метаданные от байтов и вывести поисковый индекс из коммита БД — не то же, что собрать систему, которая правда это делает и остаётся согласованной, когда процесс падает между двумя записями. Собери небольшой бэкенд обмена медиа, что кладёт каждую нагрузку на правильное хранилище, отдаёт байты через presigned-ссылки, и затем докажи, что твой поисковый индекс не может тихо разойтись с базой, даже когда ты убиваешь релей посреди записи.
</Hook>

<Crux>Собери полиглот медиа-бэкенд — метаданные в БД, байты в объектном хранилище через presigned, производный поиск — докажи, что он не дрейфует.</Crux>

<Explanation>

Этот проект делает весь раздел операционным: ты разместишь каждую нагрузку на хранилище, чьи гарантии ей подходят, удержишь крупные байты вне базы, а затем встретишь проблему двойной записи лоб в лоб, выведя поисковый индекс из коммита базы и доказав — осознанно уронив процесс — что он сходится.

<ProjectBrief
  id="07-storage-project"
  lessonSlug="project"
  lang="ru"
  objective="Собери небольшой бэкенд обмена медиа (любой язык), что хранит метаданные файлов в реляционной базе, байты файлов в объектном хранилище с отдачей через presigned-ссылки и полнотекстовый поисковый индекс по названиям/подписям — затем сделай поисковый индекс производной, в-итоге-согласованной проекцией базы, что доказуемо не может тихо с ней разойтись."
  requirements={[
    "Смоделируй реляционную схему медиа-элементов: id, owner_id, title, caption, content_type, size_bytes, object_key, status, created_at. НЕ храни байты файлов в базе — только object_key, указывающий на них. Обоснуй, почему каждая колонка остаётся в реляционном хранилище (запрашиваемо, джойнится, проверяются права).",
    "Реализуй загрузку прямо в объектное хранилище: endpoint, что авторизует пользователя, пишет строку метаданных со status='pending' и возвращает короткоживущую presigned PUT-ссылку на один ключ объекта. Клиент грузит байты прямо в хранилище; ни байт не стримится через сервер приложения.",
    "Реализуй подтверждение загрузки: по завершении (событие хранилища или коллбэк клиента) переключи строку в status='complete'. Добавь уборщик, что удаляет строки, застрявшие в 'pending' дольше таймаута, плюс осиротевшие объекты, сверяя разрыв метаданные↔байты.",
    "Отдавай скачивания через presigned GET-ссылки, держа контроль доступа на пользователя в момент подписи; проверь (например, логами или метриками), что байты файлов никогда не проходят через уровень приложения.",
    "Добавь полнотекстовый поиск по title + caption с поисковым движком (Elasticsearch/OpenSearch) или Postgres tsvector/GIN. Подтверди, что LIKE с ведущим wildcard полностью сканировал бы, а твой запрос по инвертированному индексу — нет.",
    "Сделай поисковый индекс ПРОИЗВОДНОЙ проекцией базы, НЕ вторым источником истины: питай его из коммита БД через транзакционный outbox (строка «проиндексируй этот элемент», пишемая в той же транзакции, что и медиа-строка, дренируемая релеем) или Change Data Capture.",
    "Напиши тест согласованности: убей релей (или процесс) между коммитом БД и пушем в индекс, затем перезапусти его и покажи, что индекс сходится к базе без вручную потерянных элементов.",
    "Задокументируй решение по хранилищам: для каждой нагрузки (метаданные, байты, поиск) укажи выбранное хранилище и конкретное свойство, что его обосновало, и назови, какую модель согласованности даёт каждый кусок (ACID для метаданных, итоговая для производного индекса).",
  ]}
  acceptance={[
    "Реляционная схема, держащая только метаданные (без байтов файлов), с обоснованием каждой колонки и колонкой object_key, указывающей на байты в объектном хранилище.",
    "Рабочий поток загрузки, где клиент PUT-ит байты прямо в объектное хранилище через presigned-ссылку, приложение пишет метаданные pending→complete, а уборщик сверяет провальные загрузки — продемонстрировано от начала до конца на многомегабайтном файле, что ни разу не стримится через уровень приложения.",
    "Endpoint полнотекстового поиска на инвертированном индексе с короткой заметкой, показывающей, почему эквивалентный запрос LIKE полностью сканировал бы, а запрос по инвертированному индексу — нет.",
    "Поисковый индекс, питаемый из коммита БД (транзакционный outbox или CDC), с убранной из обработчиков запросов двойной записью — нигде нет best-effort «проиндексировать и игнорировать провалы».",
    "Воспроизводимый тест согласованности, что роняет релей/процесс посреди записи и показывает, что индекс сходится к базе после перезапуска, плюс абзац объяснения, почему он не может тихо разойтись.",
    "Короткий разбор, сопоставляющий каждую нагрузку её хранилищу и модели согласованности (ACID-метаданные, в-итоге-согласованный производный поиск), изложенный на высоте композиции.",
  ]}
  stretch={[
    "Добавь lifecycle-тиринг: политику (реальную или симулированную), что двигает объекты в класс нечастого доступа/архив по возрасту, и измерь изменение стоимости хранения против держания всего горячим.",
    "Добавь обработку долговечность-против-доступности: настрой (или симулируй) межрегиональную репликацию объектного хранилища и опиши путь чтения с failover, что держит байты достижимыми во время сбоя региона.",
    "Добавь второе производное хранилище: time-series под счётчики просмотров/метрики на элемент, питаемое тем же способом (из журнала коммитов), и покажи даунсэмплинг + retention, ограничивающие его хранение.",
    "Добавь граф- или рекомендательный запрос («элементы, что также понравились тем, кому понравился этот») и обсуди, какое семейство хранилищ его обслужило бы и почему реляционный self-join не масштабировался бы — оставаясь на высоте композиции.",
  ]}
/>

</Explanation>

<KeyTakeaway>Полиглот медиа-бэкенд делает раздел операционным: метаданные в БД, байты в объектном хранилище через presigned, поиск в производном индексе из коммита БД — двойная запись убрана и, уронив релей, доказана сходящейся.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="07-storage-project-retrieval"
  lang="ru"
  questions={[
    { q: "Почему отделять метаданные от байтов и как поток загрузки держит байты вне уровня приложения?", a: "Байты в базе раздувают бэкапы, отравляют буфер-кеш, раздувают WAL/репликацию и душат пул соединений — так что байты идут в объектное хранилище под ключом, а маленькая строка метаданных (владелец, title, content_type, размер, object_key, status) живёт в реляционной БД, где её запрашиваемо, джойнится и проверяются права. Поток загрузки держит байты вне уровня приложения presigned PUT-ссылкой: приложение авторизует запрос, пишет строку метаданных pending и возвращает короткоживущую подписанную ссылку на один ключ; клиент грузит прямо в хранилище, затем событие подтверждения переключает строку в complete, а уборщик сверяет провалы. Ни байт не стримится через сервер приложения." },
    { q: "Как сделать поисковый индекс согласованным с базой без двойной записи и как это доказать?", a: "Сделай базу единственным источником истины и выведи индекс из её коммита — никогда best-effort вторую запись в обработчике. Транзакционный outbox пишет строку «проиндексируй этот элемент» в ТОЙ ЖЕ транзакции, что и медиа-строка (атомарный коммит), а релей дренирует outbox в поисковый движок, ретраясь до успеха; альтернативно Change Data Capture хвостует WAL/binlog. Доказываешь крэш-тестом: убей релей или процесс между коммитом БД и пушем в индекс, перезапусти и покажи, что индекс сходится к базе без вручную потерянных элементов — потому что долговечная строка outbox (или WAL) всё ещё несёт ожидающее изменение." },
    { q: "Какую модель согласованности даёт каждый кусок этой системы и почему это правильная композиция?", a: "Метаданные в реляционном хранилище — ACID — сильные, транзакционные, источник истины о том, что существует и кто владеет, что правильно для прав и инварианта pending→complete. Байты файлов в объектном хранилище высоко долговечны (и ты добавляешь межрегиональную репликацию под доступность). Поисковый индекс — в-итоге-согласованная производная проекция — новый элемент ищется через мгновение после коммита, что приемлемо, потому что поиск терпит короткий лаг, а индекс всегда пересобираем из primary. Композиция сильной согласованности там, где требуется, и итоговой там, где терпимо, и есть полиглот-обмен, оплаченный операционной поверхностью и осознанной обработкой двойной записи." },
  ]}
/>

<Recap lang="ru">
Этот проект превращает раздел хранилищ в рабочую систему. Ты размещаешь каждую нагрузку на хранилище, чьи гарантии ей подходят: **метаданные в реляционной базе** (ACID, запрашиваемо, источник истины о том, что существует и кто владеет), **байты в объектном хранилище** под ключом (никогда в БД), отдаваемые через **presigned-ссылки**, чтобы клиент грузил и скачивал напрямую, а stateless-уровень приложения ни разу не проксировал байт — со статусом pending→complete и уборщиком, сверяющим разрыв метаданные↔байты. Поиск работает на **производном инвертированном индексе** (поисковый движок или `tsvector`), с подтверждением, что он избегает полного скана, что вынудил бы LIKE с ведущим wildcard. Сердце проекта — **проблема двойной записи**: ты убираешь best-effort запись индекса из обработчиков запросов и вместо этого выводишь индекс из коммита базы через **транзакционный outbox** или **CDC**, затем *доказываешь* схождение, осознанно уронив релей посреди записи и наблюдая, как индекс догоняет после перезапуска. Результат — система, что компонует **сильную согласованность там, где требуется, и итоговую там, где терпимо** — полиглот-обмен — и инженер, что собрал её раз, тянется к правильному хранилищу под нагрузку и никогда не доверяет двум независимым записям остаться в синхроне.
</Recap>
