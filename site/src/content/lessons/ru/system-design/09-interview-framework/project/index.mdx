---
slug: "project"
lang: ru
track: "system-design"
unit: "09-interview-framework"
order: 8
title: "Фреймворк интервью: полный дизайн от начала до конца"
summary: "Практический проект: возьми продуктовый промпт и прогони весь фреймворк как письменный дизайн-док — проясни требования, оцени, нарисуй HLD, копни в сложный компонент и найди узкое место, называя каждый трейдофф — затем защити его от сценария роста 10x."
estMin: 240
status: ready
lessonType: topic
level: senior
concepts:
  - functional-requirements
  - access-patterns
  - estimation-in-interview
  - high-level-design
  - bottleneck-identification
  - tradeoffs
prereqs:
  - 09-interview-framework/01-requirements
  - 09-interview-framework/02-estimation
  - 09-interview-framework/03-hld-and-deep-dive
  - 09-interview-framework/04-bottlenecks-and-tradeoffs
sources:
  - https://github.com/donnemartin/system-design-primer
  - https://sre.google/sre-book/table-of-contents/
  - https://aws.amazon.com/builders-library/
---
import Hook from "~/components/lesson/Hook.astro";
import Crux from "~/components/prose/Crux.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import ProjectBrief from "~/components/pedagogy/ProjectBrief.astro";

<Hook>
Прочитать фреймворк — не то же, что прогнать его под давлением пустой страницы и тикающих часов. Возьми один продуктовый промпт и произведи дизайн-док, что произвёл бы сильный кандидат — требования, оценки, обоснованный HLD, один настоящий deep-dive, явный анализ узкого места и трейдоффов — а затем сделай то, что отделяет проход от оффера: защити его от «а если вырастет в 10×?», назвав ровно какой ресурс ломается первым и что ты обменяешь, чтобы починить.
</Hook>

<Crux>Прогони фреймворк на одном промпте как письменный док: требования, оценка, HLD, один deep-dive, узкие места и трейдоффы, защита от 10x.</Crux>

<Explanation>

Этот проект делает весь юнит операциональным. Ты не построишь работающую систему — ты произведёшь артефакт, что производит сеньор в (и после) интервью: плотный дизайн-док, что проходит фреймворк по порядку, обосновывает каждое решение числом или режимом отказа, называет, что каждый выбор отдаёт, и переживает вопрос о росте. Дисциплина — это суть: каждая секция должна быть следствием предыдущей.

<ProjectBrief
  id="09-interview-framework-project"
  lessonSlug="project"
  lang="ru"
  objective="Выбери один промпт системного дизайна (например, сокращатель ссылок, фото-лента, диспетчеризация ride-share, чат-сервис или rate limiter) и напиши полный дизайн-док, что прогоняет весь фреймворк интервью от начала до конца: прояснённые требования, оценку, что меняет дизайн, обоснованный высокоуровневый дизайн, один deep-dive реально сложного компонента, явный анализ узкого места и трейдоффов и защиту от роста 10x. Док должен читаться как трасса сильного интервью на 45 минут, сделанная разборчивой."
  requirements={[
    "Требования: напиши проясняющий проход. Раздели функциональные требования (фичи как глаголы пользователя) и нефункциональные (масштаб, p99 задержка, SLO доступности, согласованность, долговечность), сформулируй каждую нефункциональную цель числом и отскоупь промпт до одного-двух интересных ядер — явно перечислив, что ты НЕ проектируешь.",
    "Сущности и паттерны доступа: назови 3–6 ключевых сущностей и для каждой охарактеризуй паттерн чтения/записи (read- или write-heavy, по какому ключу, какой формы, отношение) — и скажи, что доминирующий паттерн значит для хранилища, до того как что-то рисовать.",
    "Оценка: оцени только числа, что меняют дизайн — пиковый read QPS, пиковый write QPS, хранилище за окно хранения (по классам данных), размер горячего набора и полосу — округлённые до одной значащей цифры, размеренные на пик с заявленным множителем. Для каждого числа напиши однострочное решение, что оно форсирует.",
    "Высокоуровневый дизайн: нарисуй (текстом или диаграммой) сквозной скелет — клиент → CDN/LB → stateless сервис-слой → слой данных, с кэшем/очередью/CDN, добавленными ТОЛЬКО где число или режим отказа их оправдал. Аннотируй каждую коробку однострочным обоснованием её существования.",
    "API и модель данных: набросай ключевой API (функциональные глаголы как эндпоинты, отметив идемпотентность и разбиение чтение/запись) и модель данных (сущности как таблицы с ключами) и выбери и обоснуй ключ партиции/шарда против доминирующего паттерна доступа.",
    "Deep-dive: выбери один-два реально сложных компонента (тот, что пометила оценка) и проинженерь их вглубь — структуры данных, ключевой трейдофф (например, fan-out-on-write против -on-read), режимы отказа горячего ключа / thundering herd / ребаланса и числа. Скажи, почему выбрал этот компонент.",
    "Узкое место и трейдоффы: определи единственный ресурс, что ломается первым при целевой нагрузке дизайна (и отметь, почему средняя метрика может его прятать), и напиши явную книгу трейдоффов — для каждого крупного выбора, что он покупает и что стоит (согласованность против доступности, задержка против стоимости, простота против масштаба).",
    "Защита от 10x: пересчитай ключевые оценки при 10x нагрузке, пройди путь, чтобы найти ресурс, что теперь ломается первым, назови точечную починку и новый трейдофф, что она вносит, и скажи, куда узкое место смещается дальше и что каскадирует, если ничего не делать.",
  ]}
  acceptance={[
    "Секция требований, что разделяет функциональные и нефункциональные, формулирует каждую нефункциональную цель числом и явно скоупит в и из — читаемая за две минуты.",
    "Секция оценки, где каждая цифра округлена до одной значащей цифры, размерена на пик и сопряжена с конкретным решением дизайна, что она форсирует (например, '~150K пиковый read QPS → кэш + fan-out путь чтения').",
    "HLD, где у каждой коробки однострочное обоснование (число или режим отказа), без необоснованных компонентов, плюс набросок API и модель данных с обоснованным ключом шарда.",
    "Deep-dive, что идёт вглубь по одному компоненту — его структура данных, ключевой трейдофф с обоими взвешенными вариантами и названные режимы отказа с числами — не поверхностный тур по многим компонентам.",
    "Книга трейдоффов, называющая для каждого крупного выбора, что он покупает и что стоит; и утверждение об узком месте, называющее единственный связывающий ресурс и почему среднее может его прятать.",
    "Защита от 10x, что пересчитывает оценку, называет новый связывающий ресурс и его точечную починку с новым трейдоффом и говорит, куда узкое место переезжает и каков каскад отказа, если ничего не менять.",
  ]}
  stretch={[
    "Напиши тот же дизайн-док для второго, контрастного промпта, где ключевой трейдофф переворачивается (например, платёжная/леджер-система, что требует строгой согласованности там, где лента требовала конечной), и сравни, как требования изменили архитектуру.",
    "Добавь к каждой секции список «что бы я спросил у интервьюера» — проясняющие вопросы, чьи ответы сильнее всего изменили бы дизайн — чтобы показать, что ты можешь вести фазу требований, а не только отвечать на неё.",
    "Прогони сценарий пика-и-ретраев: опиши, как клиентские ретраи во время инцидента толкают предложенную нагрузку вверх на пике и ускоряют коллапс очередей, и какие механизмы backpressure / circuit-breaking / ограниченных ретраев ты добавил бы, чтобы разорвать петлю brownout-в-outage.",
    "Запиши 10-минутный устный разбор дока, будто презентуешь интервьюеру, тренируя чтение комнаты: называй выводы первыми, предлагай выводы цифр только по запросу и объявляй выбор deep-dive вслух.",
  ]}
/>

</Explanation>

<KeyTakeaway>Прогони фреймворк один раз как письменный док: требования → оценка → обоснованный HLD → deep-dive → книга трейдоффов → защита от 10x. Каждая секция — следствие предыдущей; каждая коробка обоснована, каждый выбор оценён.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="09-interview-framework-project-retrieval"
  lang="ru"
  questions={[
    { q: "Каковы упорядоченные секции дизайн-дока и почему каждая должна быть следствием предыдущей?", a: "Требования (функциональные против нефункциональных числами, отскоплено) → сущности и паттерны доступа → оценка (только числа, меняющие дизайн, на пик) → HLD (обоснованный скелет) + API/модель данных с ключом шарда → deep-dive (один сложный компонент, проинженеренный вглубь) → книга узкого места и трейдоффов → защита от 10x. Каждая — следствие, потому что архитектура — следствие ограничений: паттерн доступа выбирает хранилище, оценка форсирует решения кэш/шард, сложная часть HLD выбирает deep-dive, а связывающий ресурс дизайна — это то, что стрессит вопрос про 10x. Пропусти секцию — и следующая необоснована." },
    { q: "Как выбрать и проинженерить deep-dive, чтобы он читался как сеньорский, не поверхностный?", a: "Выбери компонент, что пометила оценка или сделал неудобным паттерн доступа — горячий путь чтения, схему шардинга, строго-согласованную запись, fan-out знаменитости — и объяви выбор. Затем иди глубоко: структура данных, ключевой трейдофф с ОБОИМИ взвешенными вариантами (скорость чтения fan-out-on-write против усиления записи, против дешёвых записей fan-out-on-read против дорогих чтений, разрешённое гибридом) и режимы отказа с числами (горячий ключ, thundering herd при истечении кэша, обработанный коалесингом запросов, стоимость ребаланса при сплите шарда). Глубина по одному компоненту — сильнейший сигнал; тур по десяти компонентам или список ops-баззвордов — слабейший." },
    { q: "Что содержит сильная защита от 10x и какой мидловой версии она должна избежать?", a: "Пересчитай ключевые оценки при 10x, пройди путь запроса, чтобы найти ресурс, что теперь ломается первым (stateless-слой масштабируется out, так что почти всегда stateful — primary на запись, горячий шард, кэш, что больше не влезает в RAM одного узла), назови точечную починку и новый трейдофф, что она вносит (шардинг слоя записи делает кросс-шард запросы дорогими → денормализуй), и скажи, куда узкое место смещается дальше и что каскадирует, если ничего не делать (кривая очередей в вертикаль, ретраи из brownout в outage). Мидловая версия, которой надо избежать, — рефлекторное «добавим серверов», что не называет ни конкретного ресурса, ни трейдоффа." },
  ]}
/>

<Recap lang="ru">
Этот проект превращает фреймворк интервью в повторяемый артефакт. Возьми один промпт и напиши дизайн-док по порядку, каждая секция — следствие предыдущей: **проясни требования** (функциональные против нефункциональных числами, отскоплено в и из); назови **сущности и паттерны доступа** (паттерн доступа выбирает хранилище); **оцени** только числа, меняющие дизайн (пиковый read/write QPS, хранилище по классам, горячий набор, полоса — на пик, каждое сопряжено с решением, что оно форсирует); нарисуй **обоснованный HLD**, где каждая коробка заслуживает место, с наброском API и ключом шарда под доминирующий паттерн доступа; **копни** в один реально сложный компонент (структура данных, ключевой трейдофф с обоими взвешенными вариантами, режимы отказа с числами); напиши **книгу трейдоффов и утверждение об узком месте**, называя единственный связывающий ресурс и что каждый выбор стоит; и наконец **защити от 10x** пересчётом, назвав новый связывающий ресурс, его починку, новый трейдофф и куда узкое место смещается дальше. Сделать это один раз, письменно, — вот что превращает фреймворк из прочитанного в прогоняемое — а сквозная линия, сигнал сеньорности, постоянна: обоснуй каждую коробку числом, назови цену каждого трейдоффа и никогда не отвечай на вопрос о росте «добавим серверов».
</Recap>
