---
slug: "quiz-code"
lang: ru
track: "system-design"
unit: "09-interview-framework"
order: 7
title: "Фреймворк интервью: прикладные числа и конфиг"
summary: "Читай артефакты реального интервью — хелпер оценки, заметку требований, расчёт ёмкости, конфиг, что кодирует трейдофф — и рассуждай, как сеньор: какое число меняет дизайн, что конфиг отдаёт, куда смещается узкое место."
estMin: 14
status: ready
lessonType: topic
level: senior
concepts:
  - estimation-in-interview
  - capacity-planning
  - access-patterns
  - bottleneck-identification
  - tradeoffs
  - high-level-design
prereqs:
  - 09-interview-framework/01-requirements
  - 09-interview-framework/02-estimation
  - 09-interview-framework/03-hld-and-deep-dive
  - 09-interview-framework/04-bottlenecks-and-tradeoffs
sources:
  - https://github.com/donnemartin/system-design-primer
  - https://aws.amazon.com/builders-library/
  - https://sre.google/sre-book/service-level-objectives/
---
import Hook from "~/components/lesson/Hook.astro";
import Crux from "~/components/prose/Crux.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Фреймворк живёт в числах и конфиге, не в прозе. Оценка, что переводится в число серверов, заметка требований, что прячет пропущенную цель, конфиг, что молча кодирует трейдофф. Читай каждый артефакт, делай арифметику и рассуждение в уме и выбирай ответ, на который сеньор подписался бы у доски.
</Hook>

<Crux>Читай оценку, заметку требований, расчёт ёмкости и конфиг — затем выбирай решение дизайна, что числа реально поддерживают.</Crux>

<Explanation>

Практикуй петлю, что прогоняешь у доски: найди число, что меняет дизайн, заметь пропущенное требование, переведи нагрузку в ёмкость и прочти, что конфиг отдаёт — выбирая ход, что поддерживают арифметика и трейдофф.

## Сниппет 1 — хелпер оценки

```python
def design_changing_numbers(dau, posts_per_day, read_write_ratio):
    writes_per_sec = dau * posts_per_day / 86_400
    reads_per_sec  = writes_per_sec * read_write_ratio
    peak = lambda x: round(x * 3)          # множитель пика 3x
    return {"write_avg": round(writes_per_sec), "read_avg": round(reads_per_sec),
            "write_peak": peak(writes_per_sec), "read_peak": peak(reads_per_sec)}

# 50M DAU, 2 поста/день, чтений 50x записей
print(design_changing_numbers(50_000_000, 2, 50))
```

<Quiz
  id="09-interview-code-1"
  lessonSlug="quiz-code"
  lang="ru"
  question="Читая до одной значащей цифры, какое выходное число первым меняет дизайн и на что?"
  choices={[
    { label: "read_peak ≈ 175 000 (~150–200K). Одна БД его не обслужит, так что оно форсирует кэш и fan-out/предвычисленный путь чтения — первое число, меняющее дизайн.", correct: true },
    { label: "write_avg ≈ 1 000, что форсирует шардинг слоя записи немедленно.", misconception: "~1 000 записей/с (≈3K на пике) комфортно под потолком низких тысяч одной primary — шардинг пока не форсирован. Число, меняющее дизайн, — ~150–200K пиковый read QPS, форсирующий кэш." },
    { label: "Ничего — хелпер возвращает догадки, так что ни одно число не может изменить дизайн.", misconception: "Догадки на уровне порядка — ровно то, что меняет дизайн: ~150–200K пиковых чтений против ёмкости одной БД — явное пересечение порога, форсирующее кэш/fan-out путь. Вся работа оценки — это вскрыть." },
    { label: "write_peak ≈ 3 000, что за пределом одной primary.", misconception: "~3 000 записей/с — ровно у, а не за пределом комфортных низких тысяч одной primary — на грани, стоит следить, но не первое, что меняет дизайн. ~150–200K пиковый read QPS меняет, форсируя кэш." },
  ]}
/>

## Сниппет 2 — заметка требований

```yaml
# требования, зафиксированные до рисования
functional:
  - пользователи постят сообщения
  - пользователи читают ленту
non_functional:
  scale: 50M DAU
  latency_p99_read_ms: 150
  # consistency: <-- не зафиксирована
  availability_slo: "99.9%"
```

<Quiz
  id="09-interview-code-2"
  lessonSlug="quiz-code"
  lang="ru"
  question="Каков самый последствийный пробел в этой заметке требований и почему он важен до рисования?"
  choices={[
    { label: "Пропущена цель по согласованности — должна ли лента быть строго свежей или терпит секунды устаревания, решает, можно ли делать fan-out и кэшировать или нужно координировать. Это меняет архитектуру, поэтому фиксируй её первой.", correct: true },
    { label: "Ничего важного не пропущено — масштаба, задержки и SLO достаточно для дизайна.", misconception: "Пропущена цель согласованности, и она нагруженная: терпящая устаревание даёт fan-out и кэш; строгая свежесть форсирует координацию. От неё зависят две разные архитектуры, поэтому её надо зафиксировать до рисования." },
    { label: "Пропущен язык программирования.", misconception: "Язык не меняет высокоуровневую архитектуру. Последствийный пробел — цель согласованности, что решает fan-out-и-кэш против координации — фиксируй её, не язык." },
    { label: "Пропущено точное число микросервисов.", misconception: "Число сервисов — следствие дизайна, не входное требование. Пропущенный вход, что меняет дизайн, — цель согласованности." },
  ]}
/>

## Сниппет 3 — расчёт ёмкости

```python
peak_read_qps   = 200_000
per_node_qps    = 8_000          # точка изгиба кривой очередей
cache_hit_rate  = 0.90

origin_qps = peak_read_qps * (1 - cache_hit_rate)
nodes      = -(-origin_qps // per_node_qps)     # деление с округлением вверх
print(origin_qps, nodes)         # что напечатает и что обоснует?
```

<Quiz
  id="09-interview-code-3"
  lessonSlug="quiz-code"
  lang="ru"
  question="Что это напечатает и какое решение дизайна обоснует?"
  choices={[
    { label: "20000 и 3. Только 10% чтений (20K/с) доходят до origin, так что ~3 узла (добавь запас). Падение на порядок с флота ~25 узлов обосновывает сам кэш.", correct: true },
    { label: "200000 и 25 — кэш не снижает нагрузку origin.", misconception: "Расчёт применяет 90% попаданий: origin видит 200K × 0,10 = 20K/с, не 200K. Это ~3 узла — а разрыв с ~25 узлами без кэша ровно то, что обосновывает кэш." },
    { label: "20000 и 3, но провижинить всё равно стоит на полные 200K для надёжности.", misconception: "Провижининг origin на полные 200K сводит на нет смысл кэша и тратит флот. Размеряй origin под трафик промахов (20K → ~3 узла плюс запас); отдельно обеспечь плавную деградацию при отказе кэша." },
    { label: "Нельзя сказать — hit rate просто догадка.", misconception: "Это догадка уровня порядка, чего достаточно: при ~90% попаданий origin видит ~20K/с → ~3 узла. Суть в том, что кэш превращает флот ~25 узлов в горстку — разница, меняющая дизайн." },
  ]}
/>

## Сниппет 4 — конфиг, что кодирует трейдофф

```yaml
# путь чтения ленты
read_from: cache
cache_ttl_seconds: 30
on_cache_miss: read_replica       # async-реплика, может отставать от primary
write_path: fan_out_on_write_async
```

<Quiz
  id="09-interview-code-4"
  lessonSlug="quiz-code"
  lang="ru"
  question="Какой трейдофф кодирует этот конфиг и как ты сформулируешь его интервьюеру?"
  choices={[
    { label: "Он выбирает согласованность-в-конечном-счёте для ленты (TTL кэша 30с + async-реплика + async fan-out) — покупает задержку чтения и доступность ценой устаревания до ~30с+. Скажи: «чтения могут отставать на десятки секунд, что ок для ленты, но я изменил бы это для чего-то транзакционного».", correct: true },
    { label: "Он гарантирует строгую согласованность, ведь читает из кэша.", misconception: "Кэш с TTL 30с плюс async-реплика и async fan-out — противоположность строгой согласованности: он намеренно допускает устаревание. Честная формулировка называет это окно устаревания ценой задержки чтения и доступности, что он покупает." },
    { label: "У него нет трейдоффа — кэширование строго лучше.", misconception: "Каждый кэш кодирует трейдофф. Этот покупает задержку чтения и доступность, но стоит свежести (окно устаревания ~30с+) плюс заботы об инвалидации и thundering herd. Подать его строго лучшим — вернейший знак, что не продумал." },
    { label: "Он делает записи медленнее, чтобы чтения были быстрее, без иной цены.", misconception: "Async fan-out не замедляет путь записи (он async); реальная цена — устаревание на чтениях: лента может отставать на десятки секунд. Назови трейдофф согласованности — он тут и важен." },
  ]}
/>

</Explanation>

<KeyTakeaway>Фреймворк сводится к чтению артефактов: какой выход оценки пересекает порог, какое требование пропущено, как расчёт переводит нагрузку в узлы, что конфиг отдаёт. Читай числа, называй решение и его цену.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="09-interview-code-retrieval"
  lang="ru"
  questions={[
    { q: "По выходу хелпера оценки как заметить число, что меняет дизайн?", a: "Ищи цифру, что пересекает порог ёмкости. Пиковый read QPS в сотнях тысяч пересекает «одна БД это не обслужит» → форсирует кэш/fan-out путь; это обычно первое число, меняющее дизайн. Темп записи около потолка низких тысяч одной primary — на грани (следи), а в десятках тысяч форсирует шардинг. Число, что не пересекает порог (скромный темп записи, точные байты метаданных), ничего не меняет и не должно вести дизайн. Читай до одной значащей цифры и размеряй на пик." },
    { q: "Как перевести hit rate кэша в число origin-узлов и что обосновывает результат?", a: "origin_qps = peak_read_qps × (1 − hit_rate); nodes = ceil(origin_qps / per_node_qps), плюс запас (целься в ~70%, ниже изгиба очередей). Пример: 200K пик × 10% промах = 20K на origin / 8K на узел ≈ 3 узла. Результат обосновывает сам кэш: без него нужно ~25 узлов, так что падение на порядок — факт, меняющий дизайн. Отдельно размеряй RAM кластера кэша под горячий набор и обеспечь плавную деградацию origin при отказе кэша, чтобы кэш не стал новым SPOF." },
    { q: "Как вычитать трейдофф из конфига и как его сформулировать?", a: "Найди, что конфиг отдаёт. Путь чтения с TTL кэша, async-репликой на промахе и async fan-out выбирает согласованность-в-конечном-счёте: покупает задержку чтения и доступность ценой окна устаревания (тут десятки секунд). Сформулируй с ценой и границей: «чтения могут отставать на ~30с+, приемлемо для ленты, но я перешёл бы на синхронное/строго-согласованное для чего-то транзакционного вроде баланса». Назвать цену и когда ты изменил бы решение — сеньорский сигнал: каждый компонент что-то покупает и что-то стоит." },
  ]}
/>

<Recap lang="ru">
Каждый шаг фреймворка проявляется в артефактах, что ты читаешь у доски. **Хелпер оценки** выдаёт число, что либо пересекает порог (~150–200K пиковый read QPS форсирует кэш/fan-out путь), либо нет (скромный темп записи ничего не меняет) — читай до одной значащей цифры и найди то, что двигает дизайн. **Заметка требований** судится по пропущенному: брошенная цель согласованности нагруженная, ведь терпимость к устареванию решает fan-out-и-кэш против координации. **Расчёт ёмкости** переводит нагрузку в ёмкость — 90% попаданий кэша превращают флот ~25 origin-узлов в ~3, что ровно обосновывает кэш — и ты всё равно размеряешь RAM кэша и страхуешься, чтобы он не стал SPOF. **Конфиг** молча кодирует трейдофф (TTL + async-реплика + async fan-out — это согласованность-в-конечном-счёте), и сеньорский ход — назвать цену (окно устаревания) и границу, где ты перевернул бы его (транзакционные данные). Читай числа, делай математику, называй решение и его цену — это и есть фреймворк в действии.
</Recap>
