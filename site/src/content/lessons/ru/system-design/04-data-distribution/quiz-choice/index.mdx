---
slug: "quiz-choice"
lang: ru
track: "system-design"
unit: "04-data-distribution"
order: 5
title: "Распределение данных: выбор из вариантов"
summary: "Синтез по всему юниту распределения данных в формате выбора: лаг репликации и потерянные записи, стратегии партиционирования и горячий шард, consistent hashing против mod-N и выбор CAP/PACELC по кейсу."
estMin: 13
status: ready
lessonType: topic
level: senior
concepts:
  - replication-lag
  - hot-shard
  - consistent-hashing
  - cap-theorem
  - pacelc
  - quorum
prereqs:
  - 04-data-distribution/01-replication
  - 04-data-distribution/02-sharding-and-partitioning
  - 04-data-distribution/03-consistent-hashing
  - 04-data-distribution/04-cap-and-pacelc
sources:
  - https://www.allthingsdistributed.com/files/amazon-dynamo-sosdp2007.pdf
  - https://jepsen.io/consistency
  - https://docs.aws.amazon.com/amazondynamodb/latest/developerguide/HowItWorks.ReadConsistency.html
---
import Hook from "~/components/lesson/Hook.astro";
import Crux from "~/components/prose/Crux.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Шесть вопросов поперёк всего юнита. Каждый — решение, что ты принимаешь, выбирая режим репликации, ключ партиционирования, маршрутизацию ключей к узлам или метку консистентности хранилища — не определение для зубрёжки, а компромисс, на который senior-инженер идёт при реальном отказе.
</Hook>

<Crux>Шесть решений: потерянная запись при failover, горячий шард, решардинг, кольцо, кворум и метка CAP/PACELC по кейсу.</Crux>

<Explanation>

Подтверди, что умеешь проследить потерянную запись сквозь async-failover, диагностировать горячий шард по ключу партиционирования, выбрать consistent hashing вместо mod-N, посчитать пересечение кворума и поместить нагрузку на спектр консистентности с прямым пониманием CAP и PACELC.

<Quiz
  id="04-data-distribution-choice-1"
  lessonSlug="quiz-choice"
  lang="ru"
  question="Async-реплицируемая база подтверждает записи #51–53 клиентам, затем лидер падает, отправив лишь до #50. Повышается фолловер на #50. Какое описание верно?"
  choices={[
    { label: "Записи #51–53 потеряны: подтверждены клиентам, но не реплицированы, так что у нового лидера их нет. Ack при async-репликации — не долговечная запись; синхронная реплика сделала бы ack означающим «на двух узлах».", correct: true },
    { label: "Новый лидер запросит #51–53 у клиентов.", misconception: "Клиенты не держат переигрываемый лог подтверждённых записей, и база их об этом не просила. Записи были лишь на мёртвом лидере и пропали. Долговечность нужна синхронная реплика, а не переигрыш клиента." },
    { label: "Ничего не потеряно — успешные ответы гарантируют долговечность.", misconception: "Это ловушка: при async-репликации ack отправлен до того, как запись долговечна где-либо кроме лидера. Сбой в этом окне тихо теряет подтверждённые записи." },
    { label: "Записи в безопасности, ведь чтения eventual-консистентны.", misconception: "Eventual-консистентность — про сходимость чтений, а не про восстановление записей, которые никогда не реплицировались. У #51–53 нет копии для сходимости; они потеряны." },
  ]}
/>

<Quiz
  id="04-data-distribution-choice-2"
  lessonSlug="quiz-choice"
  lang="ru"
  question="Ты шардируешь пользователей по user_id, ровно по числу. Один шард встаёт на 100% CPU, пока остальные дремлют, сразу после того, как аккаунт знаменитости стал вирусным. Что происходит и чистейшая мера?"
  choices={[
    { label: "Горячий шард из-за перекоса: ровное число ключей не значит ровную нагрузку, и весь трафик одного ключа-знаменитости ложится на его один шард. Разбей горячий ключ (суффикс + fan-in), закешируй его или изолируй на выделенном шарде.", correct: true },
    { label: "Шарды несбалансированы по числу строк; ребалансируй число строк.", misconception: "Числа уже ровны — проблема в НАГРУЗКЕ, сконцентрированной на одном горячем ключе, а не в распределении строк. Ребаланс чисел не уберёт трафик знаменитости с её шарда." },
    { label: "Лаг репликации перегружает фолловер; добавь фолловеров.", misconception: "Это проблема нагрузки горячего ключа на одном шарде, а не репликация. Больше фолловеров масштабируют чтения широко, но не облегчают единственный ключ, чей трафик весь идёт на один шард." },
    { label: "Переключись с hash на range-партиционирование, чтобы размазать нагрузку.", misconception: "Range не помог бы — единственный ключ знаменитости всё равно ложится на один шард независимо от схемы. Надо размазать именно этот ключ: разбить, закешировать или изолировать." },
  ]}
/>

<Quiz
  id="04-data-distribution-choice-3"
  lessonSlug="quiz-choice"
  lang="ru"
  question="Флот кеша Redis маршрутизирует ключи через hash(key) mod N. Добавление одного узла (N: 16 → 17) рушит hit rate почти до нуля. Какая починка и почему она работает?"
  choices={[
    { label: "Используй consistent hashing: положи узлы и ключи на кольцо и маршрутизируй ключ к следующему узлу по часовой. Тогда добавление узла двигает лишь ~1/N ключей вместо переотображения почти всех, как делает изменённый делитель mod-N.", correct: true },
    { label: "Прогрей новый узел; промахнулась лишь его доля ключей.", misconception: "При mod-N смена делителя переотображает ПОЧТИ ВСЕ ключи, так что промахивается по сути каждый запрос — не лишь доля нового узла. Прогрев одного узла не починит переотображение всего флота; consistent hashing предотвращает его." },
    { label: "Добавь больше реплик каждого узла кеша.", misconception: "Реплики не меняют функцию маршрутизации; крах — от mod-N, переотображающего каждый ключ при смене N. Структурная починка — consistent hashing, развязывающий маршрутизацию и N." },
    { label: "Увеличь TTL кеша, чтобы ключи жили дольше.", misconception: "Длинные TTL не помогут, когда каждый ключ теперь хешируется на неправильный узел — записи есть, но их ищут на неправильном узле. Consistent hashing держит ключи на их узле при смене членства." },
  ]}
/>

<Quiz
  id="04-data-distribution-choice-4"
  lessonSlug="quiz-choice"
  lang="ru"
  question="В leaderless-хранилище N = 5 реплик. Ты хочешь, чтобы чтение гарантированно видело последнюю подтверждённую запись. Какая пара (W, R) даёт это с наибольшей доступностью записи?"
  choices={[
    { label: "W = 3, R = 3 (R + W = 6 > 5): множества чтения и записи обязаны пересечься хотя бы в одном узле, гарантируя, что чтение видит последнюю запись, при этом W = 3 терпит больше потери узлов на записи, чем W = 4 или 5.", correct: true },
    { label: "W = 1, R = 1 (R + W = 2): максимально доступно.", misconception: "R + W = 2 не > N = 5, так что множества чтения и записи могут не пересекаться — чтение может вовсе упустить последнюю запись. Гарантия пересечения требует R + W > N." },
    { label: "W = 5, R = 5: сильнейшее возможное.", misconception: "R + W = 10 > 5 гарантирует пересечение, но W = 5 требует каждую реплику на запись — наименьшая доступность записи, обратное цели. W = 3, R = 3 тоже пересекается с большим запасом записи." },
    { label: "W = 2, R = 2 (R + W = 4): достаточно.", misconception: "R + W = 4 не > 5, так что множества могут не пересечься и чтение может упустить последнюю запись. Нужна сумма строго больше N; минимум здесь 6, например W = 3, R = 3." },
  ]}
/>

<Quiz
  id="04-data-distribution-choice-5"
  lessonSlug="quiz-choice"
  lang="ru"
  question="Коллега говорит «мы на Cassandra, значит мы AP — отдали консистентность». Используя CAP и PACELC точно, какая поправка точнее всего?"
  choices={[
    { label: "AP описывает поведение лишь ВО ВРЕМЯ партиции (отдавать возможно устаревшие данные, а не отказывать). Постоянный повседневный компромисс — ветвь «иначе» PACELC — это задержка против консистентности: при здоровой сети более строгое чтение всё равно стоит задержки координации. Cassandra — PA/EL.", correct: true },
    { label: "Верно — у Cassandra вообще нет консистентности.", misconception: "Cassandra предлагает настраиваемую консистентность (уровни кворума на запрос); это не «нет консистентности». И AP — лишь стойка во время партиции, а повседневный компромисс — задержка против консистентности (else PACELC), что утверждение упускает." },
    { label: "Неверно — Cassandra на самом деле CP.", misconception: "Cassandra склоняется к AP при партиции (остаётся доступной, отдавая возможно устаревшие чтения). Поправка не в смене буквы, а в том, что AP описывает лишь случай партиции, а EL PACELC описывает остальное." },
    { label: "Неважно; CAP и PACELC — одно и то же.", misconception: "Нет: CAP покрывает лишь выбор C-против-A во время партиции; PACELC добавляет ветвь «иначе» (задержка против консистентности в норме), что и игнорирует утверждение коллеги." },
  ]}
/>

<Quiz
  id="04-data-distribution-choice-6"
  lessonSlug="quiz-choice"
  lang="ru"
  question="Ты выбираешь уровень консистентности на тип данных для одного продукта. Какое назначение отражает принцип юнита «выбирай по кейсу»?"
  choices={[
    { label: "Баланс счёта / декремент склада → линеаризуемая (CP/EC); счётчик лайков или лента активности → eventual (AP/EL); пользователь, правящий свой профиль → read-your-writes. Согласуй каждый данные с их реальной потребностью корректности.", correct: true },
    { label: "Сделай всё линеаризуемым на всякий случай.", misconception: "Требовать линеаризуемость на счётчике лайков или ленте — купить задержку и сниженную доступность CP/EC для данных, которым она не нужна, делая весь продукт медленнее и хрупче при партиции без выгоды." },
    { label: "Сделай всё eventual ради максимального масштаба.", misconception: "Гонять баланс или склад на eventual-консистентном хранилище приглашает двойные списания и перепродажи при партиции. Деньги и склад нужны линеаризуемость; лишь данные низкой ставки должны быть eventual." },
    { label: "Используй read-your-writes везде как компромисс.", misconception: "Read-your-writes — гарантия на клиента, слишком слабая для баланса (нужна глобальная линеаризуемость) и избыточная для публичного счётчика лайков (eventual достаточно). Один уровень на всё — ошибка." },
  ]}
/>

</Explanation>

<KeyTakeaway>Распределять данные — выбирать, какое расхождение терпеть: async-failover теряет записи, кривой ключ даёт горячий шард, mod-N губит решардинг, R + W > N даёт пересечение, CAP/PACELC выбирают консистентность по кейсу.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="04-data-distribution-choice-retrieval"
  lang="ru"
  questions={[
    { q: "Почему async-реплицируемый failover теряет подтверждённые записи и что это предотвращает?", a: "Лидер может подтвердить записи, ещё не отправленные ни одному фолловеру; если он падает в этом окне, повышенный фолловер отстал и эти подтверждённые записи просто не существуют (а если старый лидер вернётся, они могут конфликтовать с новыми и быть отброшены). Правило: при async-репликации ack — НЕ долговечная запись. Предотвращение: хотя бы одна синхронная реплика, чтобы ack означал «на двух узлах», плюс failover-инструментарий, не повышающий слишком отставшего фолловера и огораживающий старого лидера против split-brain." },
    { q: "Дай минимальный кворум для гарантированно свежего чтения при N = 5 и наибольшей доступности записи.", a: "Нужно R + W > N = 5, чтобы множества чтения и записи пересеклись хотя бы в одном узле и чтение видело последнюю подтверждённую запись. Минимальная сумма — 6; W = 3, R = 3 её удовлетворяет, держа наибольшую доступность записи (W = 4 или 5 терпели бы меньше отказов узлов на записи). W = 1/R = 1 не годится (сумма 2, множества могут не пересечься, чтение может упустить запись)." },
  ]}
/>

<Recap lang="ru">
Сквозная мысль — распределять данные значит серией решений выбирать, какое расхождение терпеть. **Репликация** копирует данные ради чтений и выживания, но async-репликация подтверждает до долговечности, так что **failover** может потерять подтверждённые записи — держи синхронную реплику. **Шардинг** дробит данные по ключу, и ровное число ключей всё равно даёт **горячий шард** при перекосе (знаменитость), что чинится разбивкой, кешированием или изоляцией горячего ключа. **Consistent hashing** маршрутизирует ключи по кольцу, так что добавление узла двигает лишь ~1/N, тогда как `hash mod N` переотобразил бы почти все и снёс бы кеш. **Leaderless-кворум** гарантирует свежее чтение, когда **R + W > N** (множества чтения и записи пересекаются). А **CAP** — лишь выбор C-против-A во время партиции, тогда как **PACELC** добавляет повседневный компромисс задержка-против-консистентности — так что **выбирай по кейсу**: линеаризуемая для денег и склада, eventual для счётчиков лайков и лент, read-your-writes для собственных правок пользователя.
</Recap>
