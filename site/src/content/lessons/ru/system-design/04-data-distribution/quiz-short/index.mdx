---
slug: "quiz-short"
lang: ru
track: "system-design"
unit: "04-data-distribution"
order: 6
title: "Распределение данных: свободное припоминание"
summary: "Промпты на свободное припоминание по юниту распределения данных. Сначала ответь своими словами — режимы репликации, горячий шард, хеш-кольцо, неравенство кворума, CAP и PACELC — затем открой образец и сверься."
estMin: 14
status: ready
lessonType: topic
level: senior
concepts:
  - replication-lag
  - hot-shard
  - consistent-hashing
  - quorum
  - cap-theorem
  - pacelc
prereqs:
  - 04-data-distribution/01-replication
  - 04-data-distribution/02-sharding-and-partitioning
  - 04-data-distribution/03-consistent-hashing
  - 04-data-distribution/04-cap-and-pacelc
sources:
  - https://www.allthingsdistributed.com/files/amazon-dynamo-sosdp2007.pdf
  - https://jepsen.io/consistency
  - https://aws.amazon.com/builders-library/challenges-with-distributed-systems/
---
import Hook from "~/components/lesson/Hook.astro";
import Crux from "~/components/prose/Crux.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";

<Hook>
Припоминание бьёт перечитывание. На каждый промпт скажи или напиши полный ответ из памяти — включая неравенство и режим отказа — прежде чем открыть образец. Усилие реконструкции и заставляет лаг репликации, хеш-кольцо и PACELC прилипнуть.
</Hook>

<Crux>Реконструируй спину юнита: режимы репликации и потерянные записи, горячий шард, кольцо, R + W > N и CAP против PACELC.</Crux>

<Explanation>

Реконструируй спину юнита, не подглядывая: почему каждая копия отстаёт и как failover теряет записи, почему ровное число ключей всё равно даёт горячую точку, почему mod-N губит решардинг и кольцо его чинит, что гарантирует неравенство кворума и чем CAP отличается от PACELC.

<RetrievalDrawer
  client:load
  id="04-data-distribution-short-retrieval"
  lang="ru"
  questions={[
    { q: "Сравни синхронную и асинхронную репликацию и объясни, как failover теряет подтверждённые записи.", a: "Синхронная: лидер ждёт подтверждения фолловера перед ответом клиенту, так что подтверждённая запись долговечна на ≥2 узлах — но медленный/упавший фолловер блокирует запись (доступность записи связана с самой медленной репликой). Асинхронная: лидер подтверждает сразу и шлёт в фоне, так что быстро и доступно, но фолловеры отстают и ack может быть лишь на лидере. Потеря при failover: если лидер подтверждает записи, не отправленные им, затем падает, повышенный фолловер отстал и эти подтверждённые записи пропадают — ack при async-репликации НЕ долговечная запись. Защита: синхронная реплика (ack = на двух узлах) и fencing/кворумное повышение против split-brain." },
    { q: "Что такое read-your-writes и как его сохранить при лаге репликации?", a: "Read-your-writes (чтение-после-записи): пользователь, только что записавший, должен видеть свою запись при следующем чтении. Лаг ломает это, когда чтение попадает на async-фолловер, не применивший запись, и изменение будто пропадает. Починки про маршрутизацию, не про скорость: читать собственные недавние данные пользователя с лидера короткое окно, маршрутизировать на фолловер, догнавший позицию записи в логе (токен версии), или прибить пользователя к одной реплике. Это гарантия свежести на клиента, слабее глобальной линеаризуемости." },
    { q: "Почему ровное число ключей всё равно создаёт горячий шард и как это смягчить?", a: "Потому что ровное партиционирование по ЧИСЛУ ключей не ровно по НАГРУЗКЕ: один ключ-знаменитость (пользователь/товар/видео) может получать на порядки больше трафика, чем средний, и насыщать свой один шард, пока остальные дремлют — перекос. Хеширование не помогает, ведь весь трафик одного горячего ключа всё равно идёт на один шард, который им владеет. Меры размазывают горячий ключ: разбить случайным суффиксом и свести чтения веером, закешировать перед шардом или изолировать на выделенном шарде (тривиально при directory). Рефлекс: равномерное распределение ключей ≠ равномерная нагрузка." },
    { q: "Почему hash(key) mod N губит решардинг и как хеш-кольцо это чинит (с виртуальными узлами)?", a: "Mod-N вшивает N в маршрутизацию, так что смена числа узлов переотображает почти каждый ключ разом — почти полный переезд данных для шардированной БД и мгновенный снос кеша, где каждый запрос промахивается. Consistent hashing хеширует узлы и ключи на кольцо и маршрутизирует ключ к следующему узлу по часовой; каждый узел владеет непрерывной дугой, так что добавление/удаление узла трогает лишь одну дугу — двигается ~1/N ключей. Виртуальные узлы размещают каждый физический узел во многих точках кольца (~150), что балансирует нагрузку многими малыми дугами и размазывает нагрузку мёртвого узла по многим преемникам вместо раздавливания одного соседа." },
    { q: "Сформулируй неравенство кворума и сравни CAP с PACELC.", a: "Кворум: с N репликами, если R + W > N множества чтения и записи пересекаются хотя бы в одном узле, так что чтение видит последнюю подтверждённую запись (например, N = 5, W = 3, R = 3). CAP: во время сетевой партиции надо выбрать Консистентность (линеаризуемость) или Доступность — P обязателен, так что «выбери два» неверно, и выбор часто на операцию. PACELC её расширяет: если Partition → A против C (CAP); Else → задержка против консистентности — ведь даже при здоровой сети строгое чтение должно координироваться (лидер или кворум), что стоит задержки. Ветвь «иначе» доминирует на практике, ведь каждое чтение её платит, а партиции редки. Выбирай по кейсу вдоль спектра linearizable → eventual." },
  ]}
/>

</Explanation>

<KeyTakeaway>Реконструируй каждый ответ из памяти — режимы репликации и потерянные записи, горячий шард, кольцо, R + W > N, CAP против PACELC — и держишь спину юнита: распределять данные — выбирать, какое расхождение терпеть.</KeyTakeaway>

<Recap lang="ru">
Если ты смог реконструировать каждый ответ из памяти, ты держишь спину юнита: **репликация** копирует данные ради чтений и выживания, но async-репликация подтверждает до долговечности, так что **failover** теряет подтверждённые записи (держи синхронную реплику; маршрутизируй собственные чтения пользователя на актуальный узел ради read-your-writes). **Шардинг** по ключу всё равно даёт **горячий шард** при перекосе, ведь ровное число ключей не ровная нагрузка — разбей, закешируй или изолируй ключ-знаменитость. **Consistent hashing** кольцует узлы и ключи, так что добавление одного двигает лишь ~1/N (виртуальные узлы балансируют дуги и размазывают отказ), тогда как `mod N` переотобразил бы почти все и снёс бы кеш. **Кворум** гарантирует свежее чтение, когда **R + W > N**. А **CAP** — лишь выбор C-против-A во время партиции, тогда как **PACELC** добавляет повседневный компромисс задержка-против-консистентности — так что выбирай по кейсу вдоль спектра **linearizable → eventual**. Нить, связывающая всё: на этой глубине распределение — намеренный выбор, какая неконсистентность приемлема для каждого куска данных, а не поведение, что ты обнаруживаешь в проде.
</Recap>
