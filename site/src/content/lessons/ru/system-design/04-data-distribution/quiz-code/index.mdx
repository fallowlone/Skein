---
slug: "quiz-code"
lang: ru
track: "system-design"
unit: "04-data-distribution"
order: 7
title: "Распределение данных: чтение конфига и кода"
summary: "Читай реальный конфиг репликации, выбор ключа партиционирования, функцию маршрутизации и настройку кворума, затем рассуждай: найди риск потери записи, горячий шард, ловушку mod-N решардинга и гарантирует ли кворум свежее чтение."
estMin: 14
status: ready
lessonType: topic
level: senior
concepts:
  - async-replication
  - partition-key
  - consistent-hashing
  - quorum
  - hot-shard
  - pacelc
prereqs:
  - 04-data-distribution/01-replication
  - 04-data-distribution/02-sharding-and-partitioning
  - 04-data-distribution/03-consistent-hashing
  - 04-data-distribution/04-cap-and-pacelc
sources:
  - https://www.allthingsdistributed.com/files/amazon-dynamo-sosdp2007.pdf
  - https://docs.aws.amazon.com/amazondynamodb/latest/developerguide/HowItWorks.ReadConsistency.html
  - https://github.com/donnemartin/system-design-primer
---
import Hook from "~/components/lesson/Hook.astro";
import Crux from "~/components/prose/Crux.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Баги распределения живут в конфиге и коде маршрутизации: режим репликации, ключ партиционирования, модуль, число кворума. Читай каждый сниппет, проследи режим отказа, который он подразумевает, и выбери ответ, на который senior-инженер пойдёт на ревью.
</Hook>

<Crux>Найди режим репликации, ключ, маршрутизацию или кворум в конфиге, проследи режим отказа и выбери починку, которую механизм поддерживает.</Crux>

<Explanation>

Отработай цикл, что гоняешь на ревью дизайна или инцидента: найди решение распределения в конфиге или коде, проследи его режим отказа и выбери изменение, которое механизм реально поддерживает.

## Сниппет 1 — конфиг репликации

```yaml
# репликация базы
replication:
  mode: async          # лидер подтверждает до получения записи фолловерами
  followers: 3
  sync_replicas: 0     # ни один не обязан подтвердить
# при отказе лидера: автоповышение самого догнавшего фолловера
```

<Quiz
  id="04-data-distribution-code-1"
  lessonSlug="quiz-code"
  lang="ru"
  question="Какой риск долговечности несёт этот конфиг и какое минимальное изменение?"
  choices={[
    { label: "При sync_replicas: 0 и режиме async лидер подтверждает до того, как у любого фолловера есть запись, так что падение лидера теряет каждую подтверждённую-но-неотправленную запись при failover. Поставь sync_replicas: 1, чтобы ack означал запись минимум на двух узлах.", correct: true },
    { label: "Риска нет — автоповышение самого догнавшего фолловера предотвращает потерю данных.", misconception: "Повышение самого догнавшего фолловера минимизирует потерю, но не может восстановить записи, дошедшие лишь до мёртвого лидера. При sync_replicas: 0 эти подтверждённые записи пропали. Нужна синхронная реплика, чтобы сам ack подразумевал долговечность." },
    { label: "Риск — устаревание чтений; добавь больше фолловеров.", misconception: "Больше фолловеров масштабируют чтения, но не делают ack долговечным — они все async здесь. Риск — ПОТЕРЯ ЗАПИСЕЙ при failover, что чинится требованием синхронной реплики, а не добавлением async." },
    { label: "Переключи все 3 фолловера на синхронные ради безопасности.", misconception: "Все-синхронные связывают доступность записи с самым медленным из 3 фолловеров — одна медленная реплика тормозит каждую запись. Минимальная безопасная починка — sync_replicas: 1 (полусинхрон): долговечно на двух узлах, не держа записи в заложниках у всех трёх." },
  ]}
/>

## Сниппет 2 — ключ партиционирования

```python
# таблица events шардирована; выбери ключ шарда
def shard_for(event):
    return hash(event.created_at_day) % NUM_SHARDS   # шард по дню
# самый горячий запрос: SELECT ... WHERE tenant_id = ? AND created_at_day = ?
```

<Quiz
  id="04-data-distribution-code-2"
  lessonSlug="quiz-code"
  lang="ru"
  question="Что не так с шардированием events по created_at_day при такой нагрузке?"
  choices={[
    { label: "Шардинг по дню воронкой сводит все сегодняшние записи на один шард (движущаяся горячая точка), а самый горячий запрос фильтрует ещё и по tenant_id, так что его нельзя маршрутизировать на один шард лишь по дню. Шардируй по tenant_id (или композиту tenant+время), чтобы размазать записи и держать горячий запрос одношардовым.", correct: true },
    { label: "Ничего — хеширование дня размазывает нагрузку ровно по шардам.", misconception: "Хеширование дня размазывает РАЗНЫЕ дни по шардам, но все записи одного дня делят ключ, так что сегодняшние записи все бьют в один шард — горячая точка записи. И запросу нужен tenant_id, по которому ключ-день не маршрутизирует." },
    { label: "Модуль должен быть простым числом; смени NUM_SHARDS.", misconception: "Простой модуль нерелевантен этому багу. Проблема в ВЫБОРЕ ключа (день) против паттерна доступа (tenant + день): сегодняшние записи концентрируются и горячий запрос нельзя маршрутизировать одношардово." },
    { label: "Добавь вторичный индекс на tenant_id и оставь ключ-день.", misconception: "Вторичный индекс на не-ключе шардинга локален на шард (scatter-gather для опроса) или отдельный глобальный индекс — он не чинит горячую точку записи от ключа-дня. Перевод ключа на tenant_id адресует и размазывание, и запрос." },
  ]}
/>

## Сниппет 3 — функция маршрутизации

```python
# клиент кеша выбирает узел для каждого ключа
def node_for(key, nodes):
    return nodes[hash(key) % len(nodes)]   # nodes — живой автомасштабируемый список

# автоскейлер добавляет/убирает узлы кеша по нагрузке
```

<Quiz
  id="04-data-distribution-code-3"
  lessonSlug="quiz-code"
  lang="ru"
  question="Флот кеша автомасштабируется, так что len(nodes) меняется в рантайме. Что вызывает эта маршрутизация и какая починка?"
  choices={[
    { label: "Каждое событие автоскейла меняет len(nodes), так что hash(key) % len(nodes) переотображает почти все ключи разом и hit rate рушится каждый раз. Замени mod-N на consistent hashing (кольцо), чтобы смена членства двигала лишь ~1/N ключей.", correct: true },
    { label: "Ничего — хеш-функция держит ключи на том же узле.", misconception: "Нет: делитель len(nodes) меняется при каждом событии скейла, так что hash(key) % len(nodes) переназначает почти каждый ключ. Это проблема «перехешировать всё»; mod-N небезопасен для живого меняющегося набора узлов." },
    { label: "Это вызывает неровную нагрузку; смени на лучшую хеш-функцию.", misconception: "Хеш в порядке; проблема — зависимость mod-N от len(nodes), что переотображает всё при смене числа. Лучший хеш не поможет — consistent hashing (развязка маршрутизации и N) поможет." },
    { label: "Прибей len(nodes) к константе в формуле.", misconception: "Хардкод числа ломает маршрутизацию в тот момент, когда реальный набор узлов отличается от константы (ключи маршрутизируются на узлы, которых может не быть). Верная починка — consistent hashing, обрабатывающий меняющийся набор узлов by design." },
  ]}
/>

## Сниппет 4 — настройка кворума

```python
# leaderless key-value хранилище
N = 5          # реплик на ключ
W = 2          # узлов должны подтвердить запись
R = 2          # узлов должны ответить на чтение
# требование продукта: чтение всегда должно видеть последнюю подтверждённую запись
```

<Quiz
  id="04-data-distribution-code-4"
  lessonSlug="quiz-code"
  lang="ru"
  question="Удовлетворяет ли W = 2, R = 2 при N = 5 требованию, и если нет — какая минимальная верная настройка?"
  choices={[
    { label: "Нет: R + W = 4 не > N = 5, так что множества чтения и записи могут не пересекаться и чтение может упустить последнюю запись. Минимальная починка — сделать R + W > 5, например W = 3, R = 3, чтобы множества пересеклись хотя бы в одном узле.", correct: true },
    { label: "Да — любой кворум гарантирует свежие чтения.", misconception: "Лишь кворумы с R + W > N гарантируют пересечение. Здесь R + W = 4 ≤ 5, так что двухузловое множество чтения может вовсе разминуться с двухузловым множеством записи; чтение может вернуть устаревшие данные. Надо поднять сумму выше N." },
    { label: "Нет; поставь W = 5, чтобы каждая реплика имела запись.", misconception: "W = 5 гарантирует свежие чтения (любой R пересекается), но требует все реплики на запись — наименьшая доступность записи, больше нужного. R + W > 5 достаточно; W = 3, R = 3 удовлетворяет с лучшей доступностью записи." },
    { label: "Нет; снизь R до 1, чтобы чтения были быстрее.", misconception: "Снижение R даёт R + W = 3, всё ещё ≤ 5 и всё ещё без пересечения — чтения становятся быстрее И менее корректными. Чтобы гарантировать свежесть, надо увеличить сумму выше N, например W = 3, R = 3." },
  ]}
/>

</Explanation>

<KeyTakeaway>Каждое решение распределения читается в конфиге: sync_replicas ради долговечности, ключ партиционирования ради горячих точек, функция маршрутизации ради боли решардинга, R + W vs N ради свежести чтений. Читай механизм.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="04-data-distribution-code-retrieval"
  lang="ru"
  questions={[
    { q: "Как читать риск долговечности по конфигу репликации?", a: "Смотри на режим и число обязательных синхронных реплик. mode: async с sync_replicas: 0 значит, что лидер подтверждает до того, как у любого фолловера есть запись, так что падение до отправки теряет каждую подтверждённую-но-нереплицированную запись при failover — ack не долговечная запись. Минимальная починка — sync_replicas: 1 (полусинхрон): ack теперь означает запись минимум на двух узлах, без связки все-синхронных, привязывающей доступность записи к самому медленному фолловеру." },
    { q: "Почему hash(key) % len(nodes) небезопасен для автомасштабируемого набора узлов и что его заменяет?", a: "Потому что len(nodes) — делитель, и каждое добавление/удаление его меняет и переотображает почти все ключи разом — крах hit rate кеша при каждом событии скейла или почти полный переезд данных для шардированного хранилища. Consistent hashing его заменяет: узлы и ключи кладутся на кольцо, и ключ маршрутизируется к следующему узлу по часовой, так что смена членства трогает лишь одну дугу и двигает ~1/N ключей. Виртуальные узлы затем держат дуги сбалансированными и размазывают нагрузку мёртвого узла." },
    { q: "Имея N, W, R, как понять, гарантированно ли свежи чтения, и какова минимальная починка, когда нет?", a: "Чтение гарантированно видит последнюю подтверждённую запись тогда и только тогда, когда R + W > N, что заставляет множества чтения и записи пересечься хотя бы в одном узле. При N = 5, W = 2, R = 2 даёт R + W = 4 ≤ 5 — не гарантировано; множества могут не пересечься и чтение может вернуть устаревшее. Минимальная починка поднимает сумму выше N, например W = 3, R = 3 (сумма 6), что пересекается, держа больше доступности записи, чем W = 5." },
  ]}
/>

<Recap lang="ru">
Каждое решение распределения в этом юните — то, что можно прочитать прямо из конфига или кода маршрутизации. **Конфиг репликации** с `mode: async` и `sync_replicas: 0` подтверждает до того, как у любого фолловера есть запись, так что failover теряет подтверждённые записи — починка — одна синхронная реплика. **Ключ партиционирования**, выбранный против нагрузки (шардинг events по дню, когда горячий запрос фильтрует по tenant), создаёт горячую точку записи и немаршрутизируемый запрос — переведи ключ под паттерн доступа. **Функция маршрутизации** `hash(key) % len(nodes)` над автомасштабируемым набором узлов переотображает почти все ключи при каждом событии скейла — замени её кольцом consistent hashing, чтобы двигалось лишь ~1/N. А **кворум** гарантирует свежее чтение лишь когда `R + W > N` — `W = 2, R = 2, N = 5` (сумма 4) нет, а `W = 3, R = 3` (сумма 6) да. Senior-привычка — найти решение распределения в конфиге, проследить его режим отказа и выбрать починку, которую механизм поддерживает, — а не ту, что его прячет.
</Recap>
