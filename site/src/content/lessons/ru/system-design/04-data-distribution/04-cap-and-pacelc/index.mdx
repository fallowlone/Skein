---
slug: "04-cap-and-pacelc"
lang: ru
track: "system-design"
unit: "04-data-distribution"
order: 4
title: "CAP и PACELC"
summary: "CAP узка: лишь во время сетевой партиции надо выбирать консистентность или доступность. PACELC её достраивает — иначе, без партиции, ты всё равно меняешь задержку на консистентность. Выбирай по кейсу вдоль спектра linearizable → eventual."
estMin: 20
status: ready
lessonType: topic
level: middle
concepts:
  - cap-theorem
  - partition-tolerance
  - pacelc
  - consistency-models
  - linearizability
  - eventual-consistency
prereqs:
  - 04-data-distribution/01-replication
  - 04-data-distribution/02-sharding-and-partitioning
sources:
  - https://jepsen.io/consistency
  - https://www.allthingsdistributed.com/files/amazon-dynamo-sosdp2007.pdf
  - https://docs.aws.amazon.com/amazondynamodb/latest/developerguide/HowItWorks.ReadConsistency.html
  - https://aws.amazon.com/builders-library/challenges-with-distributed-systems/
---
import Hook from "~/components/lesson/Hook.astro";
import Crux from "~/components/prose/Crux.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Inset from "~/components/lesson/Inset.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import MetaphorComplete from "~/components/pedagogy/MetaphorComplete.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import Infographic from "~/components/diagram/Infographic.astro";
import pacelcQuadrant from "./infographics/pacelc-quadrant.svg?raw";

<Hook>
Senior-инженер говорит на ревью дизайна: «Мы на Cassandra, значит выбрали AP над CP — отдали консистентность ради доступности». Звучит авторитетно и неверно так, что это важно. CAP форсирует этот выбор лишь *во время сетевой партиции* — редкого события. Остальные 99,9% времени, когда сеть здорова, реальный, постоянный компромисс команды — это **задержка против консистентности**: каждое чтение либо платит за координацию реплик, либо возвращает возможно устаревшие данные быстро. CAP описывает момент кризиса; решение, формирующее систему каждый день, — то, о котором CAP даже не упоминает. PACELC упоминает. Разобраться в этих двух — это разница между карго-культом выбора базы и реальным пониманием того, что обещает твоё хранилище.
</Hook>

<Crux>CAP: при партиции выбирай консистентность или доступность. PACELC добавляет иначе — задержка против консистентности. Выбирай по кейсу.</Crux>

<Explanation>

## CAP, сформулированная точно

Теорема CAP (Брюер; доказана Гилбертом и Линчем) — о трёх свойствах распределённого хранилища. Прежде чем правильно использовать эти метки на ревью дизайна, нужно знать, что именно означает каждая — и что она не означает:

- **Консистентность (C)** — каждое чтение видит самую свежую запись (конкретно — *линеаризуемость*: система ведёт себя так, будто копия данных одна и операции идут в едином порядке). Заметь: это *не* C из ACID — буква та же, идея другая.
- **Доступность (A)** — каждый запрос к неупавшему узлу получает не-ошибочный ответ (пусть и не свежайшие данные).
- **Терпимость к партиции (P)** — система продолжает работать, когда сеть роняет или задерживает сообщения между узлами.

Реальное содержание теоремы узко и условно: **когда случается сетевая партиция, надо выбрать между C и A — оба нельзя.** Если два узла не могут общаться и запись легла на один, другой либо отказывается отвечать (жертвуя A ради консистентности → **CP**), либо отвечает своими возможно устаревшими данными (жертвуя C ради доступности → **AP**). Вот и всё. Доказательство почти тривиально, едва сформулировано так: партиционированный узел, всё же отвечающий, либо лжёт (не C), либо встаёт (не A).

<FlowDiagram
  label="Сетевая партиция раскалывает кластер; запись доходит до одной стороны, затем чтение приходит на другую, которая должна выбрать: отказаться отвечать ради консистентности (CP) или ответить устаревшими данными ради доступности (AP)."
  perRow={3}
  hue="var(--d-net)"
  nodes={[
    { id: "write", label: "запись X=2", sub: "сторона A" },
    { id: "partition", label: "ПАРТИЦИЯ", sub: "стороны молчат" },
    { id: "read", label: "чтение X", sub: "сторона B" },
    { id: "cp", label: "CP: отказ", sub: "нет устар. ответа" },
    { id: "ap", label: "AP: ответ X=1", sub: "устаревший, но жив" },
    { id: "choose", label: "надо выбрать", sub: "C или A" },
  ]}
  edges={[
    { from: "write", to: "partition", label: "раскол" },
    { from: "partition", to: "read", label: "изоляция" },
    { from: "read", to: "choose" },
    { from: "choose", to: "cp", label: "взять C" },
    { from: "choose", to: "ap", label: "взять A" },
  ]}
  caption="Во время партиции изолированная сторона не видит новую запись, так что либо отказывает (CP, консистентно, но недоступно), либо отдаёт старое значение (AP, доступно, но устаревшее). CAP — лишь этот момент; когда сеть здорова, теорема молчит."
/>

## Почему CAP так часто перевирают

Три мифа, все стоит разучить:

1. **«Выбери два из трёх».** Уронить P нельзя. В любой реальной распределённой системе сетевые партиции *будут* — это факт физики и эксплуатации, а не выбор дизайна (Amazon Builders' Library прямо говорит, что независимые отказы и партиции неизбежны). Так что P обязателен, и живой выбор — лишь C-против-A *пока идёт партиция*. «Выбери два» звучит так, будто CA — опция; для распределённого хранилища это не так.
2. **«Моя база AP/CP, точка».** Выбор может быть *на операцию*, а не на систему. DynamoDB по умолчанию делает eventual-консистентные чтения (AP-подобно), но предлагает флаг `ConsistentRead` для строго консистентных чтений той же таблицы. Многие системы дают выбирать консистентность на запрос, так что навешивание одной буквы на всю базу прячет реальное настраиваемое решение.
3. **CAP описывает всю систему.** Она описывает поведение лишь *во время партиции*. Инженер из hook схлопнул «что мы делаем в кризис» в «что есть наша система» и упустил компромисс, реально присутствующий каждый день, — ровно то, что добавляет PACELC.

Все три мифа вытекают из одного корня: CAP воспринимают как широкую классификацию системы, а не как условное утверждение — только о моменте партиции. Когда увидишь метку «AP» или «CP» в дизайн-доке, твой следующий вопрос: «на операцию или на систему?» и «а что происходит, когда сеть здорова?»

## PACELC: остальное время

PACELC (Абади) расширяет CAP на нормальную работу:

```
PAC | ELC
Если Partition  → выбери Availability или Consistency
Else (норма)    → выбери Latency      или Consistency
```

Читай как: *если есть Partition, меняй A против C (это CAP); Else (иначе) меняй L против C.* Ветвь «иначе» — это инсайт. При здоровой сети строго консистентное чтение всё равно должно координироваться через реплики (ждать кворум, говорить с лидером) — что стоит **задержки**. Чтобы быстрее, читаешь с близкой реплики, которая может слегка устареть, — что стоит **консистентности**. Так что даже при нуле партиций ты постоянно выбираешь, сколько задержки платить за сколько свежести. Этот компромисс присутствует на каждом запросе, поэтому формирует систему куда сильнее, чем редкая партиция.

Базы получают двусоставную метку: DynamoDB и Cassandra — **PA/EL** (доступны при партиции, низкая задержка, но устаревшее иначе); строго консистентное хранилище вроде однолидерной SQL-базы или Spanner склоняется к **PC/EC** (консистентно при партиции, консистентно, но выше задержка иначе). Метка наконец схватывает *и* кризисное, *и* повседневное поведение.

<Infographic
  svg={pacelcQuadrant}
  label="Квадрант 2x2 для PACELC: верхний ряд — во время партиции (PC, выбираем консистентность, отказывая в устаревших ответах; PA, выбираем доступность, отдавая устаревшие данные); нижний ряд — штатная работа без партиции (EC, выбираем консистентность, координируя чтения и платя латентностью; EL, выбираем латентность, читая ближайшую возможно устаревшую реплику)."
  caption="PACELC — это два компромисса, а не один: ряд партиции (PC против PA) — это CAP; ряд «иначе» (EC против EL) — выбор латентность-против-консистентности, который делает каждое здоровое чтение. DynamoDB/Cassandra — в PA/EL, Spanner/однолидерная SQL — в PC/EC."
  hue="var(--d-infra)"
/>

<Inset kind="why" lang="ru">
Почему строгая консистентность стоит задержки даже когда ничего не сломано? Потому что «каждое чтение видит свежайшую запись» требует, чтобы чтение было уверено, что нигде нет более свежей записи, — а единственный способ убедиться — координация: маршрутизировать через единственного лидера (урок 01) или читать с кворума реплик (R + W > N), оба добавляют round trips, а через регион эти round trips — те самые ~150 мс «скорости света» из юнита масштабирования. Eventual-консистентное чтение пропускает координацию и отвечает с ближайшей копии немедленно — быстро, но может упустить ещё распространяющуюся запись. Так что задержка-против-консистентности — не изъян реализации; это та же физика, что и лаг репликации, со стороны чтения. Ветвь «иначе» PACELC лишь именует цену координации, всегда там присутствующую, партиция или нет.
</Inset>

## Спектр консистентности и выбор по кейсу

C-против-A и L-против-C не бинарны; это **спектр** моделей консистентности (Jepsen каталогизирует их как свойства безопасности — какие истории система вправе порождать). От сильнейших к слабейшим, несущие ступени:

- **Линеаризуемая (строгая)** — чтения всегда видят свежайшую запись; система ведёт себя как одна копия. Сильнейшая, дороже всех, наименее доступна при партиции.
- **Последовательная / каузальная** — операции уважают консистентный порядок (каузальная: следствия не появляются раньше причин — ты никогда не видишь ответ раньше сообщения, на которое он отвечает).
- **Read-your-writes / монотонные чтения** — гарантии на клиента (видишь свои записи; чтения не идут назад) без глобальной силы — починка-маршрутизация урока 01 живёт здесь.
- **Eventual** — реплики сходятся, *если записи прекратятся*, без обещаний порядка или свежести тем временем. Слабейшая, дешевейшая, доступнейшая.

Senior-ход — **выбирать по кейсу**, а не по компании:

- Баланс банка или декремент склада, который не должен перепродать → **линеаризуемая / CP / EC**: корректность важнее доступности и задержки.
- Счётчик лайков, лента, метка «последний раз в сети», счётчик просмотров страницы товара → **eventual / AP / EL**: пара секунд устаревания невидима пользователю и покупает огромную доступность и скорость.
- Пользователь, редактирующий собственный профиль → **read-your-writes** достаточно: он должен видеть своё изменение, но глобальная строгая консистентность избыточна.

Поэтому одна компания держит Postgres для заказов и Cassandra/DynamoDB для ленты активности: точку на спектре выбирают данные, а не догма.

<TradeoffMatrix
  id="04-cap-pacelc-tradeoff"
  lessonSlug="04-cap-and-pacelc"
  lang="ru"
  prompt="Интернет-магазин управляет тремя типами данных: (A) счётчики остатков для баннеров 'последние 3 на складе', (B) авторитетное уменьшение остатка, предотвращающее оверселл при покупке, (C) счётчик просмотров товара в листинге. Какая модель консистентности подходит для (B)?"
  options={[
    { name: "Линеаризуемость (строгая) для уменьшения остатка (B)", summary: "Каждое уменьшение видит актуальный счётчик; движок не допускает, чтобы два одновременных покупателя оба увидели 'в наличии' и оба прошли.", justification: "Уменьшение остатка, предотвращающее оверселл, — это инвариант корректности: два одновременных уменьшения последнего товара не должны оба проходить. Линеаризуемость — чтения всегда видят последнюю запись, операции выглядят мгновенными в глобальном порядке — единственный уровень консистентности, делающий это безопасным без блокировок на уровне приложения. По PACELC это PC/EC: при партиции система отказывает в уменьшении вместо риска устаревшего счётчика, а при каждом здоровом чтении платит за координацию (кворум или round trip до лидера) ради свежести. Это правильный выбор, потому что цена ошибки (оверселл, чарджбеки, репутационный ущерб) многократно превышает ценовую премию за задержку.", correct: true },
    { name: "Eventual-консистентность для уменьшения остатка (B)", summary: "Быстро и доступно, но два покупателя могут оба увидеть 'в наличии' в окне репликации и оба завершить покупку.", justification: "Eventual-консистентность позволяет репликам сходиться с задержкой. Для счётчика просмотров или 'последнего появления' сотни миллисекунд устаревания невидимы. Для уменьшения остатка это окно — именно момент, когда два одновременных запроса могут оба прочитать старый счётчик (скажем, 1), оба решить 'в наличии' и оба уменьшить — оставив склад на -1 и одного покупателя недовольным. Модель BASE верна для (A) и (C), но риск для (B), имеющего жёсткое требование корректности. Выбрать eventual для уменьшения — это ошибка 'запустить деньги на eventual-consistent хранилище', о которой предупреждает урок." },
    { name: "Read-your-writes для уменьшения остатка (B)", summary: "Гарантирует, что покупатель видит собственную последнюю запись — но не что одновременные покупатели видят записи друг друга.", justification: "Read-your-writes — per-session гарантия: после записи твои последующие чтения отражают эту запись. Она ничего не говорит о том, что видит *другая* одновременная сессия. Два покупателя в разных сессиях могут оба получить устаревшее чтение с разных реплик и оба уменьшить последний товар. Read-your-writes — правильный уровень для 'пользователь видит только что сохранённое изменение профиля', а не для предотвращения гонки двух независимых акторов на общем остатке. Для уменьшения нужна глобальная гарантия порядка — линеаризуемость." },
  ]}
/>

<Inset kind="mistake" lang="ru">
Дорогая ошибка — применять один уровень консистентности ко всей системе. Требовать линеаризуемость везде «на всякий случай» — значит купить цену задержки и доступности CP/EC на данных, которым она не нужна (твоему счётчику просмотров — нет), делая весь продукт медленнее и хрупче при партиции без выгоды. Обратная ошибка не лучше: гонять деньги или склад на eventual-консистентном хранилище, потому что оно «web scale», а затем обнаружить, что перепродал билеты или дал балансу уйти в минус при партиции. Согласуй модель консистентности с реальным требованием корректности каждого куска данных — и помни, что она часто настраиваема на запрос (ConsistentRead DynamoDB, уровни кворума), так что можно держать большинство чтений дёшевыми и заставить платить лишь те немногие, что обязаны быть строгими. Вся машинерия, что *реализует* строгую консистентность при партиции — кворумы, выбор лидера, Raft, — это работа трека distributed; здесь навык — выбрать правильную точку, а не построить её.
</Inset>

<Quiz
  id="04-cap-pacelc-quiz-1"
  lessonSlug="04-cap-and-pacelc"
  lang="ru"
  question="Коллега говорит «CAP значит выбери два из C, A, P». Почему это вводящая в заблуждение формулировка для реальной распределённой системы?"
  choices={[
    { label: "Партиции неизбежны, так что P не опционален — нельзя выбрать CA. Реальный условный выбор — C против A лишь ВО ВРЕМЯ партиции; остальное время CAP молчит (это и добавляет ветвь задержка-против-консистентности PACELC).", correct: true },
    { label: "Всё нормально; ты честно выбираешь любые два и роняешь третий.", misconception: "Для одномашинного хранилища P можно пропустить, но любая распределённая система ИСПЫТАЕТ партиции, так что P обязателен. «Выбери два» ошибочно подразумевает, что CA достижим; реальный выбор — C против A при партиции." },
    { label: "Потому что с достатком реплик можно иметь все три разом.", misconception: "Никакая репликация не побьёт теорему: при партиции изолированный узел должен либо лгать (не C), либо встать (не A). Ошибка «выбери два» не в том, что три недостижимы, — в том, что P не свободный выбор." },
    { label: "Потому что CAP опровергнута и целиком заменена PACELC.", misconception: "PACELC расширяет CAP, а не опровергает. Выбор C-против-A во время партиции из CAP всё ещё держится; PACELC лишь добавляет ветвь «иначе» (задержка против консистентности в норме), которую CAP опускает." },
  ]}
/>

<Quiz
  id="04-cap-pacelc-quiz-2"
  lessonSlug="04-cap-and-pacelc"
  lang="ru"
  question="Твоя сеть идеально здорова — ни одной партиции за квартал. По PACELC, какой компромисс твоя строго консистентная база всё равно делает на каждом чтении и почему?"
  choices={[
    { label: "Задержка против консистентности (ветвь «ELC»): строго консистентное чтение должно координироваться — через лидера или кворум — что добавляет round trips, так что консистентность стоит задержки даже без партиции. Чтение близкой устаревшей реплики было бы быстрее, но неконсистентно.", correct: true },
    { label: "Никакого — без партиции нет компромисса CAP, так что чтения бесплатны.", misconception: "A-против-C из CAP применима лишь при партиции, но суть PACELC в том, что ветвь ИНАЧЕ активна всегда: строгие чтения платят задержкой за координацию. «Нет партиции» не значит «нет компромисса» — значит компромисс задержка-против-консистентности." },
    { label: "Доступность против терпимости к партиции.", misconception: "Этот компромисс возникает лишь во время партиции (ветвь PAC). При здоровой сети ты на ветви ELC, меняя задержку против консистентности, а не доступность против терпимости к партиции." },
    { label: "Пропускная способность против хранения.", misconception: "PACELC о задержке против консистентности в норме, а не о пропускной способности против хранения. Цена строгого чтения — round trips координации, что оно платит ради гарантии свежести." },
  ]}
/>

<MetaphorComplete
  id="04-cap-pacelc-metaphor-1"
  lessonSlug="04-cap-and-pacelc"
  lang="ru"
  setup="PACELC читается так: если есть Partition, меняй доступность на консистентность; _______, меняй задержку на консистентность — так что здоровая сеть всё равно форсирует постоянный выбор между быстрыми-но-устаревшими и медленными-но-свежими чтениями."
  accepted={["иначе", "else", "в противном случае", "else (без партиции)", "иначе (нет партиции)"]}
  canonical="иначе"
  explanation="«E» в PACELC — это «Else» (иначе) — нормальный случай без партиции, который CAP игнорирует. В нём строго консистентное чтение должно координироваться (лидер или кворум), что стоит задержки, тогда как устаревшее близкое чтение быстро. Именование ветви «иначе» — весь вклад PACELC: повседневный компромисс, а не лишь кризисный."
/>

</Explanation>

<KeyTakeaway>CAP условна: лишь при партиции выбор C или A («выбери два» неверно — P обязателен). PACELC добавляет иначе: без партиции — задержка vs консистентность. Выбирай по кейсу: строго для денег, eventual для лайков.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="04-cap-pacelc-retrieval"
  lang="ru"
  questions={[
    { q: "Сформулируй CAP точно и объясни три частых способа её переврать.", a: "CAP: распределённое хранилище не может иметь и Консистентность (линеаризуемость — каждое чтение видит свежайшую запись), и Доступность (каждый неупавший узел отвечает не-ошибкой) во время сетевой Партиции; оно должно выбрать C (отказать в устаревшем ответе → CP) или A (ответить устаревшим → AP). Перевирания: (1) «выбери два из трёх» — неверно, ведь партиции неизбежны, так что P обязателен и CA не опция; выбор — C против A лишь при партиции. (2) «моя БД AP/CP, точка» — выбор часто на операцию (ConsistentRead DynamoDB). (3) «CAP описывает всю систему» — она описывает лишь поведение при партиции; норму добавляет PACELC." },
    { q: "Что PACELC добавляет к CAP и почему ветвь «иначе» доминирует на практике?", a: "PACELC: если Partition → доступность против консистентности (это CAP); Else (норма) → задержка против консистентности. Ветвь «иначе» — инсайт: даже при здоровой сети строго консистентное чтение должно координироваться (через лидера или кворум, R+W>N), что добавляет round trips и задержку; более быстрое чтение с близкой реплики может быть устаревшим. Поскольку партиции редки, а каждое чтение платит (или пропускает) координацию, компромисс задержка-против-консистентности формирует систему куда сильнее, чем выбор во время партиции. Метки: DynamoDB/Cassandra — PA/EL; Spanner/однолидерная SQL склоняются к PC/EC — схватывая и кризис, и повседневность." },
    { q: "Набросай спектр консистентности и как выбирают точку по кейсу.", a: "От сильнейших к слабейшим: линеаризуемая/строгая (чтения всегда видят свежайшую запись, ведёт себя как одна копия — дороже всех, наименее доступна при партиции); последовательная/каузальная (операции уважают порядок; каузальная не покажет следствие раньше причины); read-your-writes / монотонные чтения (гарантии на клиента, где живёт починка-маршрутизация урока 01); eventual (реплики сходятся лишь если записи прекратятся, без обещаний свежести — дешевейшая, доступнейшая). Выбирай по кейсу, не по компании: линеаризуемая/CP/EC для баланса банка или склада, что не должен перепродать; eventual/AP/EL для счётчика лайков, ленты или «последний раз в сети», где секунды устаревания невидимы; read-your-writes для пользователя, правящего свой профиль. Одна компания держит Postgres для заказов и Cassandra для ленты, ведь точку выбирают данные, а не догма." },
  ]}
/>

<Recap lang="ru">
**CAP** узка и условна: лишь *во время сетевой партиции* распределённое хранилище должно выбрать между **Консистентностью** (линеаризуемость — каждое чтение видит свежайшую запись, не C из ACID) и **Доступностью** (каждый неупавший узел отвечает), давая CP (отказ в устаревшем) или AP (отдать устаревшее). Её широко перевирают — «выбери два из трёх» неверно, ведь партиции неизбежны, так что **P обязателен**; метка часто **на операцию** (`ConsistentRead` DynamoDB); и CAP ничего не говорит о нормальной работе. **PACELC** её достраивает: *если Partition → A против C; Else → задержка против консистентности.* Ветвь «иначе» доминирует на практике, ведь каждое чтение при здоровой сети всё равно выбирает между координацией ради свежести (задержка) и чтением близкой устаревшей копии (скорость) — та же физика, что и лаг репликации, со стороны чтения. Консистентность — **спектр**: линеаризуемая → последовательная/каузальная → read-your-writes/монотонные → eventual (свойства безопасности Jepsen; см. jepsen.io) — и senior-навык в том, чтобы **выбирать по кейсу**: строго для денег и склада, eventual для счётчиков лайков и лент, read-your-writes для собственных правок пользователя, часто настраиваемо на запрос. Применять один уровень везде — дорогая ошибка в обе стороны. Машинерия, что *реализует* строгую консистентность при партиции — кворумы, выбор лидера, Raft, — это работа трека **distributed**; работа этого юнита — выбрать правильную точку на спектре, раз уж данные распределены. Теперь, когда на ревью услышишь «мы выбрали AP» или «мы выбрали CP», ты спросишь: при партиции или всегда? — и какой уровень консистентности каждый запрос действительно требует.
</Recap>
