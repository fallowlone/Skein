---
slug: "project"
lang: ru
track: "system-design"
unit: "04-data-distribution"
order: 8
title: "Распределение данных: шардируй, реплицируй и сломай"
summary: "Практический проект: возьми датасет, спроектируй ключ партиционирования и репликацию, построй крошечное шардированное хранилище на consistent hashing, затем намеренно вызови горячий шард, потерю записи при failover и устаревшее кворумное чтение — и докажи, что меры работают."
estMin: 240
status: ready
lessonType: topic
level: senior
concepts:
  - partition-key
  - consistent-hashing
  - replication-lag
  - quorum
  - hot-shard
  - cap-theorem
prereqs:
  - 04-data-distribution/01-replication
  - 04-data-distribution/02-sharding-and-partitioning
  - 04-data-distribution/03-consistent-hashing
  - 04-data-distribution/04-cap-and-pacelc
sources:
  - https://www.allthingsdistributed.com/files/amazon-dynamo-sosdp2007.pdf
  - https://jepsen.io/consistency
  - https://aws.amazon.com/builders-library/challenges-with-distributed-systems/
---
import Hook from "~/components/lesson/Hook.astro";
import Crux from "~/components/prose/Crux.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import ProjectBrief from "~/components/pedagogy/ProjectBrief.astro";

<Hook>
Прочитать, что async-failover теряет записи и что mod-N рушит решардинг, — не то же, что увидеть это на хранилище, которое ты построил. Возьми датасет, спроектируй ключ партиционирования и репликацию, реализуй крошечное шардированное key-value хранилище на consistent hashing, а затем намеренно воспроизведи три фирменных отказа юнита — горячий шард, потерю записи и устаревшее кворумное чтение — и докажи, что твои меры реально закрывают каждый.
</Hook>

<Crux>Построй шардированное consistent-hashed хранилище с репликацией, затем воспроизведи горячий шард, потерю записи и устаревшее чтение.</Crux>

<Explanation>

Этот проект делает весь юнит операциональным: ты выберешь ключ партиционирования и режим репликации, замаршрутизируешь ключи кольцом, а затем вызовешь каждый отказ намеренно, чтобы мера была чем-то измеренным, а не прочитанным.

<ProjectBrief
  id="04-data-distribution-project"
  lessonSlug="project"
  lang="ru"
  objective="Спроектируй и построй крошечное распределённое key-value хранилище (любой язык, в процессе или мультипроцессно), которое шардирует данные по выбранному ключу партиционирования, маршрутизирует ключи через consistent hashing и реплицирует каждый шард — затем намеренно воспроизведи горячий шард, потерю записи при failover и устаревшее кворумное чтение, и продемонстрируй меру для каждого."
  requirements={[
    "Выбери датасет и паттерн доступа (например, мультитенантные события, соцпосты или хранилище URL). Напиши абзац брифа с доминирующим запросом, отношением чтений к записям и кардинальностью кандидатов в ключи — затем обоснуй выбранный ключ партиционирования из паттерна доступа (не из удобства).",
    "Реализуй маршрутизацию consistent hashing: кольцо с виртуальными узлами (например, ~100 на физический узел). Покажи, что добавление и удаление узла двигает лишь ~1/N ключей, измерив долю ключей, сменивших владельца, и сравни с базлайном hash(key) mod N, переотображающим почти все.",
    "Реплицируй каждый шард с лидером и фолловерами, поддерживая и синхронный (ack после подтверждения одного фолловера), и асинхронный (ack сразу, отправка в фоне) режимы с настраиваемой задержкой репликации.",
    "Воспроизведи горячий шард: пошли перекошенную нагрузку, где один ключ-знаменитость берёт большую долю трафика, и покажи насыщение одного шарда, пока остальные дремлют. Затем примени меру (разбей ключ суффиксом и fan-in, закешируй или изолируй) и покажи размазанную нагрузку.",
    "Воспроизведи потерю записи: в async-режиме подтверди запись, урони её до отправки, убей лидера, повысь фолловера и покажи, что подтверждённая запись пропала. Затем переключись на полусинхрон (одна синхронная реплика) и покажи, что тот же сценарий больше не теряет запись.",
    "Реализуй leaderless кворумные чтения/записи с настраиваемыми N, W, R. Воспроизведи устаревшее чтение при R + W ≤ N (множество чтения не пересекается с множеством записи), затем поставь R + W > N и покажи, что чтение всегда видит последнюю подтверждённую запись.",
    "Напиши заметку об архитектурных решениях: ключ партиционирования и почему, режим репликации и его компромисс долговечность/задержка, кворум (N, W, R) и что он гарантирует, и в какой точке CAP/PACELC сидит каждый класс данных.",
  ]}
  acceptance={[
    "Абзац брифа с доминирующим запросом, отношением чтений к записям и кардинальностью ключа плюс обоснованный ключ партиционирования, выбранный из паттерна доступа.",
    "Измерение, показывающее, что consistent hashing двигает ~1/N ключей при смене членства против базлайна mod-N, двигающего почти все — с приведёнными реальными долями.",
    "Воспроизведённый горячий шард (нагрузка одного шарда сильно выше остальных при перекосе) и измерение после, показывающее, что мера размазала нагрузку.",
    "Воспроизведённая потеря записи при async-failover и повтор при полусинхроне, где та же подтверждённая запись выживает — с точной последовательностью, давшей каждый исход.",
    "Демонстрация кворума: устаревшее чтение при R + W ≤ N и гарантированно свежее чтение при R + W > N, с использованными значениями N, W, R.",
    "Короткая заметка об архитектурных решениях, связывающая каждый выбор (ключ, режим репликации, кворум, точка CAP/PACELC) с конкретной гарантией или компромиссом, что он покупает.",
  ]}
  stretch={[
    "Добавь split-brain: раздели кластер на партиции, дай обеим сторонам принимать записи, затем заживи и покажи разошедшиеся истории — и реализуй fencing или кворумное повышение, не дающее второму лидеру принимать записи.",
    "Добавь путь решардинга: вырасти кольцо с N до N+2 узлов под живыми записями через двойную запись или change-data-capture и покажи, что ни одна подтверждённая запись не потеряна во время cutover.",
    "Добавь bounded-load consistent hashing (ограничь каждый узел c×среднее и переливай по часовой) и покажи, что он укрощает горячий шард для маршрутизации запросов, сохраняя свойство движения ~1/N.",
    "Добавь мульти-лидерный режим с двумя регионами, принимающими записи, инжектируй конфликтующую параллельную запись и реализуй разрешение конфликта (last-write-wins против вектора версий) — обсуждая, какое тихо теряет данные.",
  ]}
/>

</Explanation>

<KeyTakeaway>Построенное хранилище делает юнит операциональным: выбери ключ, маршрутизируй кольцом, реплицируй каждый шард — затем воспроизведи горячий шард, потерю записи и устаревшее чтение, и докажи каждую меру измерением.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="04-data-distribution-project-retrieval"
  lang="ru"
  questions={[
    { q: "Как показать, что consistent hashing бьёт mod-N при смене членства?", a: "Замаршрутизируй ключи обоими способами, затем добавь (или убери) один узел и измерь долю ключей, чей владелец сменился. С consistent hashing на кольце с виртуальными узлами лишь ~1/N ключей меняют владельца (одна дуга передана); с hash(key) mod N делитель меняется, так что почти все ключи переотображаются. Привести две доли рядом (например, ~6% против ~94% для N≈16) делает свойство конкретным — и то же измерение объясняет, почему mod-N кеш сносит hit rate при каждом событии скейла." },
    { q: "Какая точная последовательность даёт потерю записи при async-failover и как полусинхрон её чинит?", a: "В async-режиме: прими запись и подтверди её клиенту, но урони/задержи её до отправки любому фолловеру; убей лидера; повысь самого догнавшего фолловера (который запись не получил); прочитай ключ и увидь, что подтверждённая запись пропала. При полусинхроне (sync_replicas = 1) ack удерживается, пока один фолловер не подтвердит, так что подтверждённая запись на двух узлах до того, как клиент услышит успех — повтори тот же kill-and-promote, и запись выживает, ведь у повышенного фолловера она уже была. Вывод: ack долговечен лишь настолько, насколько долговечна репликация за ним." },
    { q: "Как показать, что кворум гарантирует (или не гарантирует) свежее чтение?", a: "Поставь N реплик с настраиваемыми W и R. При R + W ≤ N (например, N=5, W=2, R=2) построй случай, где W узлов, принявших запись, и R узлов, отвечающих на чтение, не пересекаются — чтение возвращает устаревшие данные, доказывая отсутствие пересечения. Затем поставь R + W > N (например, W=3, R=3, сумма 6 > 5): теперь любое множество чтения обязано пересечься с любым множеством записи хотя бы в одном узле, так что чтение всегда видит последнюю подтверждённую запись. Демонстрация — неравенство, ставшее физическим: пересечение — это гарантия." },
  ]}
/>

<Recap lang="ru">
Этот проект превращает юнит распределения данных в то, что ты эксплуатировал, а не лишь прочитал. Ты **выбираешь ключ партиционирования** из доминирующего паттерна доступа и кардинальности, **маршрутизируешь ключи по кольцу consistent hashing** с виртуальными узлами (и измеряешь движение ~1/N против базлайна mod-N, переотображающего почти все), и **реплицируешь каждый шард** в синхронном и асинхронном режимах. Затем ты воспроизводишь три фирменных отказа юнита намеренно и закрываешь каждый: **горячий шард** при перекосе (смягчён разбивкой, кешированием или изоляцией ключа-знаменитости), **потерю записи** при async-failover (починена полусинхроном, чтобы ack означал «на двух узлах») и **устаревшее кворумное чтение** при `R + W ≤ N` (починено `R + W > N`, чтобы множества чтения и записи пересекались). Заметка об архитектурных решениях связывает каждый выбор — ключ, режим репликации, кворум, точку CAP/PACELC — с конкретной гарантией, что он покупает. Инженер, однажды построивший и сломавший это хранилище, распределяет данные намеренно, выбирая, какое расхождение терпеть, заранее, вместо того чтобы обнаруживать потерянную запись или снесённый кеш во время failover в 3 ночи.
</Recap>
