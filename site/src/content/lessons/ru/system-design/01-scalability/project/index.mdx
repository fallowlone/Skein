---
slug: "project"
lang: ru
track: "system-design"
unit: "01-scalability"
order: 8
title: "Масштабируемость: план ёмкости с нуля"
summary: "Практический проект: возьми бриф продукта, сделай полный план ёмкости на салфетке (QPS, объём, трафик, память), затем подтверди предсказания закона Литтла и очередей крошечным нагрузочным тестом."
estMin: 240
status: ready
lessonType: topic
level: senior
concepts:
  - back-of-envelope
  - littles-law
  - queueing-delay
  - latency-hierarchy
  - capacity-planning
  - significant-figures
prereqs:
  - 01-scalability/01-latency-vs-throughput
  - 01-scalability/02-vertical-vs-horizontal
  - 01-scalability/03-back-of-envelope
  - 01-scalability/04-numbers-to-know
sources:
  - https://highscalability.com/google-pro-tip-use-back-of-the-envelope-calculations-to-choo/
  - https://aws.amazon.com/builders-library/timeouts-retries-and-backoff-with-jitter/
  - https://github.com/donnemartin/system-design-primer
---
import Hook from "~/components/lesson/Hook.astro";
import Crux from "~/components/prose/Crux.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import ProjectBrief from "~/components/pedagogy/ProjectBrief.astro";

<Hook>
Прочитать, что оценивать надо до проектирования — не то же, что выдать план ёмкости, под которым подпишется staff-инженер. Возьми реалистичный по форме бриф продукта, оцени его от начала до конца на бумаге, а затем докажи два самых рискованных предсказания — потолок пропускной способности и колено очередей — нагрузочным тестом, что напишешь сам.
</Hook>

<Crux>Преврати бриф в план ёмкости (QPS, объём, трафик, память), затем подтверди потолок закона Литтла и кривую очередей нагрузочным тестом.</Crux>

<Explanation>

Этот проект делает весь раздел операционным: ты по-настоящему применишь дисциплину оценки, обоснуешь каждый порог дизайна числом, а затем измеришь, чтобы подтвердить два предсказания, что вероятнее всего тебя укусят — потолок пропускной способности по закону Литтла и колено задержки по кривой очередей.

<ProjectBrief
  id="01-scalability-project"
  lessonSlug="project"
  lang="ru"
  objective="Сделай полный план ёмкости на салфетке для выбранного продукта (например приложение обмена фото, сокращатель ссылок или чат), затем собери небольшой нагрузочный тест, что эмпирически подтверждает твой потолок пропускной способности по закону Литтла и колено задержки по кривой очередей — замыкая петлю между оценкой и измерением."
  requirements={[
    "Выбери продукт и напиши бриф в один абзац с входными допущениями, из которых будешь оценивать: всего пользователей, активных в день, активность на пользователя (чтений и записей/день), средний размер объекта и окно хранения (например 5 лет). Объяви каждое допущение явно.",
    "Оцени QPS: выведи средний write QPS и read QPS из цифр активности (запросов/сутки ÷ ~10^5 с), примени множитель пик-к-среднему (обоснуй 3–10×) и округли каждый результат до одной значащей цифры.",
    "Оцени объём за окно хранения, разделяя текст/метаданные и медиа, если уместно, и спроецируй вперёд — назови, какой класс данных доминирует и на сколько порядков.",
    "Оцени исходящую полосу (пиковый read QPS × средний размер ответа) и память горячего набора, что хочешь кэшировать, затем сопоставь каждое с порогом: влезает на одну машину против нужен флот, влезает в RAM одного узла против нужен шардированный кэш, влезает на один primary БД против нужен шардинг.",
    "Запиши вытекающие архитектурные решения коротким списком, каждое обоснованное конкретным числом («300К read QPS → путь чтения с кэшем/fan-out, ведь одна БД упирается в низкие тысячи»).",
    "Собери крошечный сервис (любой язык) с пулом воркеров/соединений фиксированного размера и искусственной задержкой на запрос, которой управляешь, затем напиши генератор нагрузки, что наращивает предложенную нагрузку и пишет пропускную способность и задержку p50/p99.",
    "Подтверди закон Литтла: задай известный размер пула L и задержку на запрос W, предскажи потолок λ = L/W и покажи, что измеренная макс. пропускная способность совпадает с ним в пределах допуска оценки.",
    "Подтверди кривую очередей: наращивай утилизацию и построй p99 против неё, показав нелинейное колено (мягко до ~70%, затем круто вверх), и определи утилизацию, на которой ограничил бы автоскейлер.",
  ]}
  acceptance={[
    "Одностраничный план ёмкости с QPS (средн + пик), объёмом/хранением, трафиком и памятью, каждая цифра округлена до одной значащей цифры и каждое допущение названо — читается за две минуты.",
    "Список архитектурных решений, где каждое решение цитирует конкретное число, что им движет, включая, какой ресурс ломается первым и какой класс данных доминирует в объёме.",
    "Рабочий нагрузочный тест, чья измеренная макс. пропускная способность совпадает с предсказанием закона Литтла λ = L/W (в пределах заявленного допуска), с размером пула и задержкой, что её дали.",
    "График (или таблица) задержка-против-утилизации, что наглядно показывает колено очередей — p99 ровный до ~70%, затем круто растёт — плюс однострочная рекомендация цели автоскейлера и обоснование.",
    "Короткий разбор: один абзац о том, что вскрыла одна лишь оценка до любых измерений, и один — где измерение разошлось с салфеткой и почему.",
  ]}
  stretch={[
    "Добавь сценарий пик-и-ретраи: внеси короткий затык, включи клиентские ретраи и покажи, как ретраи при насыщении толкают предложенную нагрузку вверх и ускоряют коллапс очередей — петля обратной связи просадка-в-сбой.",
    "Сравни вертикаль и горизонталь: прогони ту же нагрузку против одного большого пула против нескольких малых пулов за round-robin балансировщиком и обсуди, где налог координации USL начал бы кусаться при большем числе узлов.",
    "Добавь осознание coordinated omission: прогони тест раз closed-loop (следующий запрос после возврата прошлого) и раз open-loop (фиксированный темп прибытия) и покажи, как closed-loop p99 занижает реальный хвост во время затыка.",
    "Пересчитай план при допущении роста 10× и определи первый порог, что пересекаешь (шард кэша, шард БД, мульти-регион), показав, как оценка меняет дизайн по мере масштабирования входов.",
  ]}
/>

</Explanation>

<KeyTakeaway>План ёмкости делает раздел операционным: оцени четыре величины, обоснуй каждый порог числом, затем измерь, чтобы подтвердить потолок закона Литтла и колено очередей — сначала оценка, потом валидация.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="01-scalability-project-retrieval"
  lang="ru"
  questions={[
    { q: "Какие четыре величины оценивает план ёмкости и как превратить каждую в решение дизайна?", a: "QPS (запросов/сутки ÷ ~10^5, × множитель пика), объём (размер объекта × темп × окно хранения, по классам данных), трафик (пиковый read QPS × размер ответа) и память горячего набора. Каждая сопоставляется порогу: пиковый QPS против потолка низких тысяч одного primary БД (→ кэш/fan-out или шардинг), горячий набор против ~256 ГБ RAM одного узла (→ один кэш или шардированный), исходящая против NIC/CDN. Выход плана — список решений, где каждый выбор цитирует число, что его вынудило." },
    { q: "Как подтвердить закон Литтла и колено очередей в нагрузочном тесте?", a: "Закон Литтла: задай известный размер пула L и управляемую задержку на запрос W, предскажи потолок λ = L/W, затем наращивай предложенную нагрузку и подтверди, что измеренная макс. пропускная способность совпадает с ним в пределах допуска. Колено очередей: наращивай утилизацию и построй p99 против неё — ровно до ~70%, затем круто вверх (W = 1/(μ−λ)), и утилизация, где гнётся, — там ограничиваешь автоскейлер. Смысл — замкнуть петлю: салфетка предсказывает, тест подтверждает." },
    { q: "Почему сначала оценка, а потом измерение, а не просто измерение?", a: "Оценка — рассуждение на уровне порядков, что отсеивает неверные дизайны за минуты — до того, как потратишь спринт на сборку неверной формы. Измерение медленнее и говорит лишь о том, что ты уже собрал. Оценка первой вскрывает, какой ресурс ломается первым и какое предсказание рискованнее, поэтому измерение прицельно: подтверждаешь именно потолок пропускной способности и колено очередей, а не бенчмаришь вслепую." },
  ]}
/>

<Recap lang="ru">
Этот проект превращает раздел масштабируемости в воспроизводимый рабочий процесс: возьми бриф продукта, **оцени четыре величины** (QPS с множителем пика, объём за окно хранения, трафик, память горячего набора) до одной значащей цифры и переведи каждую в архитектурное решение, обоснованное конкретным числом — какой ресурс ломается первым, какой класс данных доминирует, где лежат пороги (одна машина, один кэш-узел, один primary БД). Затем **замкни петлю измерением**: собери небольшой сервис с известным размером пула и управляемой задержкой, подтверди, что измеренная пропускная способность совпадает с потолком **закона Литтла** λ = L/W, и построй p99 против утилизации, чтобы увидеть **колено очередей**, что предсказывает кривая M/M/1, выбрав по нему цель автоскейлера. Сначала оценка, потом тест — в этом вся дисциплина: салфетка говорит форму и самые рискованные предсказания; нагрузочный тест их подтверждает. Инженер, что собрал это однажды, размеряет системы осознанно, а не открывает потолок во время инцидента в 3 часа ночи.
</Recap>
