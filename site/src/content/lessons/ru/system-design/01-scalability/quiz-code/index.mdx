---
slug: "quiz-code"
lang: ru
track: "system-design"
unit: "01-scalability"
order: 7
title: "Масштабируемость: чтение чисел и конфигов"
summary: "Читай реальную математику ёмкости, конфиг и код, затем делай арифметику: потолок по закону Литтла, бюджет очередей, хелпер оценки и проверка задержки, что отвергает дизайн."
estMin: 14
status: ready
lessonType: topic
level: senior
concepts:
  - littles-law
  - queueing-delay
  - back-of-envelope
  - latency-hierarchy
  - capacity-planning
  - significant-figures
prereqs:
  - 01-scalability/01-latency-vs-throughput
  - 01-scalability/03-back-of-envelope
  - 01-scalability/04-numbers-to-know
sources:
  - https://highscalability.com/google-pro-tip-use-back-of-the-envelope-calculations-to-choo/
  - https://aws.amazon.com/builders-library/timeouts-retries-and-backoff-with-jitter/
  - https://github.com/donnemartin/system-design-primer
---
import Hook from "~/components/lesson/Hook.astro";
import Crux from "~/components/prose/Crux.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Баги масштабирования живут в числах, а не в прозе: размер пула, цель утилизации, шаг округления, вызов на запрос в далёкий регион. Читай каждый сниппет, делай арифметику в голове и выбирай ответ, под которым подпишется senior-инженер.
</Hook>

<Crux>Читай конфиг, найди L/W/μ/λ или паттерн доступа, сделай арифметику и выбери фикс, что уважает математику — а не тот, что её прячет.</Crux>

<Explanation>

Отработай цикл, что прогоняешь на ревью дизайна или ёмкости: найди числа в коде, примени закон Литтла, или кривую очередей, или иерархию задержек, и выбери изменение, что арифметика реально поддерживает.

## Сниппет 1 — хелпер размера пула

```python
def max_throughput_rps(pool_size: int, avg_latency_ms: float) -> float:
    # Закон Литтла: λ = L / W, где W в секундах
    return pool_size / (avg_latency_ms / 1000)

print(max_throughput_rps(pool_size=200, avg_latency_ms=40))   # что напечатает?
```

<Quiz
  id="01-scalability-code-1"
  lessonSlug="quiz-code"
  lang="ru"
  question="Что вернёт max_throughput_rps(200, 40) и что это значит?"
  choices={[
    { label: "5 000 — потолок λ = L / W = 200 / 0,040 с = 5 000 RPS; за этим пул насыщается и запросы встают в очередь.", correct: true },
    { label: "8 000 — 200 × 40.", misconception: "Это умножение; закон Литтла делит. λ = L / W = 200 / (40/1000) = 200 / 0,04 = 5 000 RPS. Умножение конкурентности на задержку здесь не имеет смысла." },
    { label: "5 — 200 / 40, считая задержку секундами.", misconception: "Задержка в миллисекундах; хелпер делит на 1000, чтобы получить секунды. 200 / 0,04 = 5 000 RPS, не 5. Следи за единицами — в этом весь смысл /1000." },
    { label: "200 — размер пула это и есть предел пропускной способности.", misconception: "Размер пула это L (конкурентность), не пропускная способность. Каждый слот оборачивается каждые 40 мс, поэтому λ = L / W = 5 000 RPS. Пул отдаёт куда больше одного запроса на слот в секунду." },
  ]}
/>

## Сниппет 2 — цель утилизации

```yaml
# конфиг автоскейлера
target_cpu_utilization: 0.95   # «использовать железо полностью»
# время обслуживания без нагрузки ~20 мс; SLO p99 <= 200 мс
```

<Quiz
  id="01-scalability-code-2"
  lessonSlug="quiz-code"
  lang="ru"
  question="Команда поставила цель автоскейлера 95% CPU ради экономии. По модели очередей, каков риск и фикс?"
  choices={[
    { label: "При 95% утилизации W = 1/(μ−λ) делает задержку ~в 20× больше времени без нагрузки → ~400 мс, пробивая SLO p99 200 мс. Снизь цель до ~60–70%, чтобы автоскейлер добавлял ёмкость до колена очередей.", correct: true },
    { label: "Риска нет — 95% эффективно, а задержка важна лишь при 100%.", misconception: "Задержка уходит в вертикаль задолго до 100%. При 95% хвост M/M/1 ~в 20× больше 20 мс без нагрузки ≈ 400 мс, за SLO 200 мс. Запас не растрата; это как ты держишь хвост." },
    { label: "Поднять цель до 99% для ещё большей эффективности.", misconception: "Это сильно хуже: при 99% хвост ~в 100× без нагрузки ≈ 2 с. Фикс в обратную сторону — цель ~60–70%, чтобы скейл сработал до вертикали кривой." },
    { label: "SLO неверный; ослабь его до 500 мс.", misconception: "Ослаблять SLO под плохую цель утилизации — задом наперёд. Ставь цель так, чтобы держать запас (~60–70%) и выполнять нужный SLO; не двигай планку, чтобы оправдать колено очередей." },
  ]}
/>

## Сниппет 3 — хелпер оценки

```python
def daily_to_qps(daily_requests: float, peak_multiplier: float = 3) -> dict:
    avg = daily_requests / 86_400
    return {"avg_qps": round(avg), "peak_qps": round(avg * peak_multiplier)}

# 1,5 миллиарда запросов/сутки
print(daily_to_qps(1_500_000_000))
```

<Quiz
  id="01-scalability-code-3"
  lessonSlug="quiz-code"
  lang="ru"
  question="Примерно что вернёт daily_to_qps(1_500_000_000) и как это верно прочитать?"
  choices={[
    { label: "~17 000 средн и ~52 000 пик. Читай до одной значащей цифры: ~20К среднее, ~50К пик — размеряй систему под ~50К пик, не под среднее.", correct: true },
    { label: "~17 361 средн и ~52 083 пик — провижинить ровно под 17 361.", misconception: "Две ошибки: ложная точность (вход — догадка, неси одну значащую цифру — ~20К/~50К) и размер по среднему. Провижинишь под ~50К ПИК, не под среднее." },
    { label: "~1 500 000 000 QPS — это и есть темп запросов.", misconception: "Это запросов в СУТКИ. Хелпер делит на 86 400, чтобы получить в секунду: ~17 000 QPS в среднем. Всегда переводи суточные суммы в секунды до рассуждений о нагрузке." },
    { label: "~17 000 QPS, а peak_multiplier не нужен.", misconception: "Множитель пика — самая важная часть: трафик всплесковый, поэтому размеряешь под ~50К пик. Убрать его — ровно ошибка среднее-против-пика, что роняет системы на всплеске." },
  ]}
/>

## Сниппет 4 — вызов на запрос

```python
# сервис в us-east-1 (Вирджиния); этот стор в eu-central-1 (Франкфурт)
def handle_request(req):
    user = remote_store.get(req.user_id)      # Вирджиния -> Франкфурт -> Вирджиния
    prefs = remote_store.get(req.prefs_id)    # Вирджиния -> Франкфурт -> Вирджиния
    return render(user, prefs)                # два синхронных межрегиональных вызова
```

<Quiz
  id="01-scalability-code-4"
  lessonSlug="quiz-code"
  lang="ru"
  question="По числам задержек, каков пол задержки этого хендлера и каков верный фикс?"
  choices={[
    { label: "Пол ~300 мс — каждый межрегиональный round-trip ~150 мс, и их два подряд. Фикс — разместить реплику в us-east-1 (или собрать в один вызов и перенести данные ближе); кэш результата не побьёт скорость света при промахе.", correct: true },
    { label: "~1 мс — это просто чтения, они быстрые.", misconception: "Сами чтения дёшевы, но каждое — это ~150 мс трансатлантический round-trip. Два подряд ≈ 300 мс пол, заданный скоростью света, а не тем, насколько «быстро» чтение." },
    { label: "~300 мс, чинится добавлением CPU сервису.", misconception: "Пол — это распространение по сети через океан, а не CPU. Быстрее CPU не укоротят путь. Надо перенести данные в регион сервиса или перестать звать межрегионально на запрос." },
    { label: "~150 мс — считается только первый вызов.", misconception: "Два вызова последовательны, поэтому их round-trip складываются: ~150 мс + ~150 мс ≈ 300 мс. Сбор в один round-trip срезал бы до ~150 мс; co-location убирает межрегиональную цену целиком." },
  ]}
/>

</Explanation>

<KeyTakeaway>Любое решение масштабирования сводится к арифметике в конфиге: λ = L/W для потолка, W = 1/(μ−λ) для запаса, суточные ÷ 86 400 × пик для нагрузки и ~150 мс × прыжки для межрегиона. Читай числа, не прозу.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="01-scalability-code-retrieval"
  lang="ru"
  questions={[
    { q: "Дан размер пула и средняя задержка — как посчитать потолок пропускной способности и в чём ловушка единиц?", a: "λ = L / W (закон Литтла), где W в СЕКУНДАХ. Классический баг — оставить W в миллисекундах: 200 / 40 = 5 неверно; 200 / 0,040 = 5 000 RPS верно. Всегда переводи задержку в секунды до деления. Размер пула это L (конкурентность), не пропускная способность — каждый слот оборачивается каждые W секунд." },
    { q: "Почему цель автоскейлера 95% CPU опасна и какая цель держит SLO?", a: "Задержка очереди нелинейна: W = 1/(μ−λ). При 95% хвост ~в 20× больше времени без нагрузки (например 20 мс → ~400 мс), за SLO p99 200 мс; при 99% ~в 100× (~2 с). Цель ~60–70%, чтобы автоскейлер добавлял ёмкость до вертикали кривой. Запас — как ты выполняешь хвостовой SLO, а не растраченное железо." },
    { q: "Каков пол задержки двух синхронных межрегиональных вызовов подряд и фикс?", a: "Каждый round-trip Вирджиния↔Франкфурт ~150 мс (скорость света на ~9 000 км в одну сторону), и два подряд складываются в ~300 мс — пол, что кэш не побьёт при промахе. Фикс — разместить реплику в регионе сервиса или собрать в один round-trip (~150 мс) и в идеале сделать некритичные вызовы асинхронными. Числа отвергают паттерн межрегиона-на-запрос с порога." },
  ]}
/>

<Recap lang="ru">
Любое решение масштабирования в этом разделе сводится к арифметике, что читаешь прямо с конфига. **Закон Литтла** (λ = L/W, где W в секундах) превращает размер пула и задержку в потолок пропускной способности — и ловушка единиц в том, чтобы забыть перевести миллисекунды в секунды. **Кривая очередей** (W = 1/(μ−λ)) говорит, что цель утилизации 95% пробивает узкий SLO p99, ведь хвост там ~в 20× без нагрузки, поэтому ставишь ~60–70%. **Код оценки на салфетке** (суточные ÷ 86 400, затем × множитель пика, прочитано до одной значащей цифры) даёт нагрузку, под которую размеряешься — пик, никогда среднее. А **числа задержек** (~150 мс на межрегиональный round-trip, ~100 случайных seek/с на HDD) дают отвергнуть межрегиональный вызов на запрос или чтение «диск-на-запрос» в одном предложении. Senior-привычка — найти числа, сделать математику и выбрать фикс, что арифметика поддерживает, а не тот, что её замазывает.
</Recap>
