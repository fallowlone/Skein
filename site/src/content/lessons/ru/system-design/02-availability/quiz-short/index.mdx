---
slug: "quiz-short"
lang: ru
track: "system-design"
unit: "02-availability"
order: 5
title: "Доступность: обзор со свободным припоминанием"
summary: "Подсказки на свободное припоминание через весь раздел доступности. Ответь сначала своими словами — включая арифметику — затем открой образец и сверься: SLO/SLA, девятки и бюджеты ошибок, резервирование и коррелированный отказ, failover и split-brain."
estMin: 14
status: ready
lessonType: topic
level: senior
concepts:
  - sli-slo-sla
  - error-budget
  - redundancy
  - correlated-failure
  - split-brain
  - mttr-mtbf
prereqs:
  - 02-availability/01-sla-slo-sli
  - 02-availability/02-redundancy-and-spof
  - 02-availability/03-failover-and-fault-tolerance
sources:
  - https://sre.google/sre-book/service-level-objectives/
  - https://aws.amazon.com/builders-library/timeouts-retries-and-backoff-with-jitter/
  - https://aws.amazon.com/builders-library/static-stability-using-availability-zones/
---
import Hook from "~/components/lesson/Hook.astro";
import Crux from "~/components/prose/Crux.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";

<Hook>
Припоминание бьёт перечитывание. Для каждой подсказки скажи или напиши полный ответ по памяти — включая арифметику — прежде чем откроешь образец. Усилие реконструкции математики SLO, размера резервирования и того, почему failover вызывает split-brain, и заставляет это держаться.
</Hook>

<Crux>Реконструируй костяк раздела: SLI/SLO/SLA, девятки и бюджет ошибок, резервирование и коррелированный отказ, failover, split-brain и MTTR.</Crux>

<Explanation>

Реконструируй костяк раздела, не подглядывая: как связаны три service-level термина и где ставить каждый, чего стоят девятки и как складывается доступность, что покупает бюджет ошибок, как размерять резервирование и почему реплики в одной AZ не зарезервированы, и как failover может вызвать тот самый сбой, что должен был предотвратить.

<RetrievalDrawer
  client:load
  id="02-availability-short-retrieval"
  lang="ru"
  questions={[
    { q: "Определи SLI, SLO и SLA, скажи, как они связаны, и где ставишь SLO.", a: "SLI (индикатор) — измеренное число: отношение хороших событий к валидным, например доля запросов, обслуженных корректно под 300 мс. SLO (цель) — внутренняя цель на этот SLI, например 99,9% за 28 дней. SLA (соглашение) — контракт с клиентом со штрафами за промах, например кредиты ниже 99,5%. Ты измеряешь SLI, целишься в SLO, обещаешь SLA — и ставишь SLO жёстче SLA, чтобы внутренний пробой был ранним предупреждением с запасом среагировать до пробоя контракта и кредитов. А SLI должен отражать то, что чувствует пользователь, измеренный близко к нему, иначе два других мерят пустоту." },
    { q: "Дай таблицу девяток для простоя в месяц и объясни, почему каждая девятка стоит ~10× больше.", a: "99% ≈ 7,2 ч/мес, 99,9% ≈ 43 мин, 99,99% ≈ 4,3 мин, 99,999% ≈ 26 с — каждая девятка режет допустимый простой в 10×. Стоимость лезет на порядок за девятку, ведь доступность складывается УМНОЖЕНИЕМ по зависимостям (четыре последовательных звена по 99,9% → 0,999⁴ ≈ 99,6%, хуже любого звена), так что выход на четыре девятки сквозь значит, что каждое звено должно бить четыре девятки или быть зарезервировано с автоматическим failover быстрее реакции человека. Три девятки терпят ручной откат; четыре нуждаются в автоматике; пять убирают человека из петли вовсе — эта автоматика и мульти-региональное резервирование и есть стоимость." },
    { q: "Что такое бюджет ошибок и как он меняет поведение команды?", a: "Бюджет ошибок — это 100% − SLO: объём отказа, разрешённый до пробоя цели (0,1% запросов для SLO 99,9%; ~4,3 мин/мес для 99,99%). Он переформулирует надёжность с «никогда не падать» на «оставаться в бюджете» и делает надёжность тратимым ресурсом: пока бюджет есть — катишь фичи и рискуешь; как исчерпан — политика замораживает рискованные запуски, пока он не пополнится. Спор скорость-против-стабильности решает число, а не мнение, а вечно неистраченный бюджет сигналит переинвестирование в надёжность, которую пользователи не просили." },
    { q: "Объясни размер резервирования (N/N+1/N+2), active-active против active-passive и почему реплики в одной AZ не зарезервированы.", a: "N — ровно пиковая ёмкость, без запаса (не зарезервировано); N+1 переживает одну потерю при сохранении ПИКА (размеряй под пик, а не среднее); N+2 переживает потерю, пока другой узел уже лежит. Active-active: все копии обслуживают за балансировщиком — мгновенный failover, но платишь за каждую копию и решаешь конфликты записи. Active-passive: одна обслуживает, резерв ждёт — дешевле и прост по записи, но failover берёт время, а негоняный резерв может быть тихо сломан. Две реплики в одной AZ не зарезервированы, ведь делят домен отказа: одно событие кладёт обе (коррелированный отказ), так что математика на независимости невалидна. Разноси копии по AZ/регионам, спроектированным отказывать независимо, и сужай радиус поражения, чтобы ограничить отказы, что не предотвратить." },
    { q: "Различи failover и отказоустойчивость, объясни split-brain и его починку, и ловушку retry storm.", a: "Failover — реактивное восстановление с коротким окном (высокая доступность); отказоустойчивость маскирует отказ без окна через живое резервирование (например active-active) дороже. Split-brain — failover, продвигающий новый primary, пока старый жив (сетевое разделение), так что два primary принимают записи и расходятся без автослияния — теряется корректность, не доступность. Лучшее обнаружение не чинит («мёртв» и «недостижим» неразличимы ожиданием); кворум (продвигается лишь большинство) или fencing/STONITH (сначала отрезать старый primary) делает неверный вызов безвредным. Retry storm: на failover каждый запрос таймаутится и ретраит в унисон на разогревающийся резерв, так что ограничивай таймауты, ограничивай ретраи (бюджет ретраев), используй экспоненциальный backoff С jitter, ретрай лишь идемпотентных операций и опирайся на graceful degradation. Доступность ≈ MTBF/(MTBF+MTTR), так что вкладывайся в быстрое отрепетированное восстановление (MTTR)." },
  ]}
/>

</Explanation>

<KeyTakeaway>Реконструируй каждый ответ по памяти — SLI/SLO/SLA, девятки и бюджет ошибок, резервирование и коррелированный отказ, failover и split-brain, MTTR — и держишь костяк раздела: доступность это рассуждение до инцидента.</KeyTakeaway>

<Recap lang="ru">
Если ты смог реконструировать каждый ответ по памяти, ты держишь костяк раздела. Три service-level термина связаны в одну сторону — измеряй **SLI**, целься в **SLO**, обещай **SLA** — и SLO стоит жёстче SLA как раннее предупреждение. **Девятки** (99,9% ≈ 43 мин/мес → 99,999% ≈ 26 с) стоят порядок каждая, ведь доступность **умножается** по зависимостям, а **бюджет ошибок** (100% − SLO) превращает надёжность в тратимый ресурс. **Резервирование** размеряют до N+1 под пик и размещают в независимых доменах отказа, ведь реплики в одной AZ — это **коррелированный отказ**, а не резервирование. **Failover** (реактивный, с окном восстановления) отличается от **отказоустойчивости** (замаскированной, без окна), и failover, что не отличит мёртв от недостижим, вызывает **split-brain** — чинится кворумом или fencing, а не лучшими таймаутами — тогда как **retry storm** на failover укрощают ограниченными таймаутами и ограниченными ретраями с backoff и jitter. Объединяющая идея: **доступность ≈ MTBF/(MTBF+MTTR)**, так что раз отказы неизбежны, лучшая инвестиция обычно — быстрое отрепетированное восстановление — и всё это рассуждение, которое делаешь до инцидента, а не поведение, что обнаруживаешь во время него.
</Recap>
