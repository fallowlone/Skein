---
slug: "quiz-code"
lang: ru
track: "system-design"
unit: "02-availability"
order: 6
title: "Доступность: чтение чисел и конфигов"
summary: "Читай реальную математику доступности, конфиг и код, затем делай арифметику: расчёт бюджета ошибок, сложение последовательной доступности, проверку размера резервирования и retry-конфиг, что превращает медленную зависимость в сбой."
estMin: 14
status: ready
lessonType: topic
level: senior
concepts:
  - error-budget
  - availability-math
  - redundancy
  - retry-storm
  - timeouts
  - split-brain
prereqs:
  - 02-availability/01-sla-slo-sli
  - 02-availability/02-redundancy-and-spof
  - 02-availability/03-failover-and-fault-tolerance
sources:
  - https://aws.amazon.com/builders-library/timeouts-retries-and-backoff-with-jitter/
  - https://sre.google/sre-book/service-level-objectives/
  - https://aws.amazon.com/builders-library/implementing-health-checks/
---
import Hook from "~/components/lesson/Hook.astro";
import Crux from "~/components/prose/Crux.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Инциденты доступности живут в числах, не в прозе: бюджет SLO, цепочка зависимостей, перемноженная вместе, пул, размеренный до N вместо N+1, retry-политика без backoff. Читай каждый сниппет, делай арифметику в голове и выбирай ответ, под которым подписался бы senior-дежурный.
</Hook>

<Crux>Найди числа в конфиге — бюджет, произведение доступности, N-против-N+1, ретраи — сделай арифметику и выбери починку по математике.</Crux>

<Explanation>

Практикуй петлю, что гоняешь во время ревью доступности или инцидента: локализуй числа, примени математику девяток/бюджета ошибок/сложения или правила ретраев-и-таймаутов и выбери изменение, что арифметика реально поддерживает, — а не то, что звучит успокаивающе.

## Сниппет 1 — хелпер бюджета ошибок

```python
def error_budget_minutes(slo: float, window_days: int = 28) -> float:
    # допустимый простой = (1 - SLO) * окно
    return (1 - slo) * window_days * 24 * 60

print(round(error_budget_minutes(0.999), 1))   # что напечатает?
```

<Quiz
  id="02-availability-code-1"
  lessonSlug="quiz-code"
  lang="ru"
  question="Что вернёт error_budget_minutes(0.999) за окно в 28 дней и что это значит?"
  choices={[
    { label: "≈40,3 минуты — SLO 99,9% разрешает 0,1% простоя, и 0,001 × 28 × 24 × 60 ≈ 40,3 мин; это бюджет ошибок, который можно потратить до пробоя цели.", correct: true },
    { label: "≈4 минуты — 99,9% разрешает около 4 минут в месяц.", misconception: "~4,3 мин это ЧЕТЫРЕ девятки (99,99%) за ~30 дней. Три девятки (99,9%) за 28 дней это 0,001 × 28 × 24 × 60 ≈ 40,3 мин. Следи, какую девятку считаешь." },
    { label: "≈403 минуты — 1% простоя.", misconception: "Это две девятки (99%, то есть 0,01). SLO 99,9% использует 0,001, давая ≈40,3 мин за 28 дней, не 403. Ты ошибся в десять раз — на целую девятку." },
    { label: "0 — SLO 99,9% значит ноль допустимого простоя.", misconception: "99,9% явно разрешает 0,1% отказывать. Бюджет — 0,001 × 28 × 24 × 60 ≈ 40,3 мин — тратимый бюджет ошибок, не ноль. Трактовка как ноль выбрасывает весь смысл SLO." },
  ]}
/>

## Сниппет 2 — цепочка зависимостей

```python
# запрос должен пройти все четыре, чтобы успеть
chain = {"lb": 0.999, "app": 0.999, "cache": 0.999, "db": 0.999}

avail = 1.0
for a in chain.values():
    avail *= a
print(round(avail, 4))   # что напечатает и это >= SLO 99,9%?
```

<Quiz
  id="02-availability-code-2"
  lessonSlug="quiz-code"
  lang="ru"
  question="Что это напечатает и держит ли путь SLO 99,9%?"
  choices={[
    { label: "≈0,996 (99,6%) — доступность умножается по четырём последовательным зависимостям, так что 0,999⁴ ≈ 99,6%, что НИЖЕ SLO 99,9%; ты добавил бы резервирование или убрал хоп.", correct: true },
    { label: "0,999 (99,9%) — путь так же доступен, как каждый компонент, так что ровно держит SLO.", misconception: "Последовательная доступность умножается, а не копирует значение одного звена. 0,999⁴ ≈ 99,6%, ниже SLO 99,9%. Каждая добавленная зависимость снижает доступность пути." },
    { label: "≈0,9996 — четыре девятки, уверенно выше SLO.", misconception: "Добавление последовательных компонентов СНИЖАЕТ доступность, а не повышает. Произведение — 0,999⁴ ≈ 99,6%, не 99,96%. Нельзя выйти на четыре девятки из четырёх звеньев по три девятки последовательно." },
    { label: "Нельзя сказать — зависит от порядка зависимостей.", misconception: "Для последовательного пути произведение не зависит от порядка: 0,999⁴ ≈ 99,6% при любой последовательности. Суть в том, что оно ниже SLO, так что цепочке нужно резервирование или меньше хопов." },
  ]}
/>

## Сниппет 3 — конфиг резервирования

```yaml
# конфиг автоскейлера / ёмкости
peak_required_instances: 6     # инстансов нужно нести пик
desired_instances: 6           # сколько реально гоняем
# заметка: "мы гоняем несколько инстансов, значит слой зарезервирован"
```

<Quiz
  id="02-availability-code-3"
  lessonSlug="quiz-code"
  lang="ru"
  question="Команда зовёт слой зарезервированным, ведь гоняет несколько инстансов. По уроку резервирования, что не так и какова починка?"
  choices={[
    { label: "desired равен peak_required, так что это N, а не N+1 — потеря одного инстанса в пик роняет ниже ёмкости, и выжившие могут схлопнуться по кривой очередей. Поставь desired_instances в 7 (N+1) или больше для N+2.", correct: true },
    { label: "Ничего — 6 инстансов зарезервированы, ведь их больше одного.", misconception: "Резервирование значит пережить потерю при сохранении ПИКА, для чего нужен запасной. При desired = peak = 6 потеря одного оставляет 5 на пик из 6: под ёмкостью. Это N, а не N+1." },
    { label: "Снизь desired_instances до 5 ради экономии; резервирование про бэкап, а не ёмкость.", misconception: "Это хуже — теперь ты под пиком даже при нуле отказов. Резервирование ИМЕННО про запас ёмкости, размеренный под пик: нужен N+1 = 7, а не меньше пика." },
    { label: "Поставь desired_instances в 6, но добавь второй балансировщик; это делает слой N+1.", misconception: "Второй балансировщик убирает другой SPOF (балансировщик), но не чинит резервирование инстансов: 6 инстансов на пик из 6 — всё ещё N. Нужен 7-й инстанс для N+1 в этом слое." },
  ]}
/>

## Сниппет 4 — retry-политика

```python
# клиент, зовущий нижестоящий сервис
RETRY_CONFIG = {
    "max_attempts": 5,
    "timeout_seconds": 30,     # на попытку
    "backoff": "none",         # ретрай немедленно
    "jitter": False,
}
# нижестоящий сервис только что начал отдавать медленные ответы
```

<Quiz
  id="02-availability-code-4"
  lessonSlug="quiz-code"
  lang="ru"
  question="Нижестоящий сервис замедляется. Что эта retry-политика с ним делает и какова верная конфигурация?"
  choices={[
    { label: "Вызывает retry storm: таймауты 30 с пришпиливают потоки, пока 5 немедленных, без jitter ретраев в унисон умножают нагрузку на борющийся сервис, гоня просадку в сбой. Чини короткими ограниченными таймаутами, малым бюджетом ретраев, экспоненциальным backoff и jitter — и ретрай лишь идемпотентные вызовы.", correct: true },
    { label: "Делает вызов надёжнее — 5 попыток максимизируют шанс успеха.", misconception: "При перегрузке долбёж 5× немедленно умножает нагрузку ровно тогда, когда зависимость меньше всего может это вынести (retry storm). Надёжность даёт ограниченный, с backoff, с jitter ретрай, а не более упорный." },
    { label: "Безвредно — ретраи просто используют запас ёмкости на нижестоящем.", misconception: "У перегруженной зависимости нет запаса ёмкости; ретраи — добавленная нагрузка на отказывающую систему, ускоряющая коллапс. Поэтому нужен бюджет ретраев плюс backoff и jitter." },
    { label: "Единственная проблема — max_attempts; снизь до 3, и всё нормально.", misconception: "Меньше попыток помогает чуть-чуть, но таймаут 30 с (исчерпание потоков) и ретраи без backoff/без jitter в унисон — бо́льшие проблемы. Чини таймауты, добавь экспоненциальный backoff С jitter и бюджет ретраев." },
  ]}
/>

</Explanation>

<KeyTakeaway>Каждое решение доступности сводится к арифметике конфига: (1−SLO)×окно для бюджета ошибок, произведение звеньев для доступности пути, desired-против-peak для N-против-N+1, таймаут × ретраи для шторма. Читай числа.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="02-availability-code-retrieval"
  lang="ru"
  questions={[
    { q: "Как посчитать бюджет ошибок из SLO и какова частая ошибка на одну девятку?", a: "Бюджет простоя = (1 − SLO) × окно. Для 99,9% за 28 дней: 0,001 × 28 × 24 × 60 ≈ 40,3 мин; для 99,99% это 0,0001 × то же ≈ 4 мин. Классическая ошибка — промахнуться на одну девятку: использовать 0,01 (две девятки), когда имел в виду 0,001 (три девятки), ошибка в 10×. Всегда выравнивай число девяток с десятичной: 99% → 0,01, 99,9% → 0,001, 99,99% → 0,0001." },
    { q: "Почему путь из четырёх звеньев по 99,9% промахивается мимо SLO 99,9% и каковы починки?", a: "Доступность умножается по последовательным зависимостям: 0,999⁴ ≈ 0,996 (99,6%), ниже SLO 99,9%, ведь каждая добавленная зависимость — ещё одна вещь, что может уронить запрос. Починки: убрать хопы с критического пути, сделать звено зарезервированным, чтобы его отказ не валил запрос (превращая последовательную зависимость в параллельную/терпимую), или вытолкнуть медленную/опциональную зависимость с синхронного пути (закэшировать, сделать async, плавно деградировать). К SLO не выйти улучшением прозы — только изменением графа зависимостей или его резервирования." },
    { q: "Что превращает медленный нижестоящий в сбой в retry-конфиге и какова безопасная конфигурация?", a: "Длинные таймауты на попытку пришпиливают потоки, пока пул не исчерпан (один медленный бэкенд → полный сбой), а немедленные, без jitter ретраи умножают нагрузку на борющийся сервис в унисон — retry storm, что гонит просадку в сбой. Безопасный конфиг — короткие, осознанные таймауты; бюджет ретраев, что ограничивает ретраи как долю трафика (а не фиксированным числом на запрос); экспоненциальный backoff, чтобы попытки разносились; jitter, чтобы клиенты не синхронизировались; circuit breakers, чтобы перестать долбить мёртвую зависимость; и ретраи лишь на идемпотентных операциях. Это ровно рецепт AWS timeouts-retries-backoff-with-jitter." },
  ]}
/>

<Recap lang="ru">
Каждое решение доступности в этом разделе сводится к арифметике, что читается прямо с конфига. **Бюджет ошибок** — это `(1 − SLO) × окно` — 99,9% за 28 дней ≈ 40 минут — и классический баг — промах на одну девятку (0,01 против 0,001, ошибка в 10×). Доступность пути — это **произведение** его последовательных звеньев, так что цепочка из четырёх звеньев по 99,9% — лишь ~99,6% и промахивается мимо SLO 99,9%; чинишь убиранием хопов или добавлением резервирования, а не правкой прозы. **Резервирование** — это N+1, так что `desired_instances == peak_required` — это N — не зарезервировано — и потеря одного в пик кренит в коллапс очередей; ты провижинишь запасной. А **retry-политика** с длинными таймаутами и немедленными, без jitter ретраями превращает медленную зависимость в retry storm и сбой — починка это короткие ограниченные таймауты, бюджет ретраев, экспоненциальный backoff с jitter, circuit breakers и ретраи лишь идемпотентного. Senior-привычка та же, что в разделе масштабируемости: найди числа, сделай математику и выбери починку, что поддерживает арифметика, а не ту, что звучит безопасно.
</Recap>
