---
slug: "project"
lang: ru
track: "system-design"
unit: "02-availability"
order: 7
title: "Доступность: план SLO и failover с нуля"
summary: "Практический проект: возьми бриф продукта, выведи его SLI/SLO и бюджет ошибок, выследи каждую единую точку отказа и спроектируй резервирование и failover, чтобы их убрать, затем докажи, что твой failover и деградация работают, малым chaos-тестом, что инъецирует отказы."
estMin: 240
status: ready
lessonType: topic
level: senior
concepts:
  - sli-slo-sla
  - error-budget
  - redundancy
  - failover
  - split-brain
  - graceful-degradation
prereqs:
  - 02-availability/01-sla-slo-sli
  - 02-availability/02-redundancy-and-spof
  - 02-availability/03-failover-and-fault-tolerance
sources:
  - https://sre.google/workbook/implementing-slos/
  - https://aws.amazon.com/builders-library/timeouts-retries-and-backoff-with-jitter/
  - https://aws.amazon.com/builders-library/static-stability-using-availability-zones/
---
import Hook from "~/components/lesson/Hook.astro";
import Crux from "~/components/prose/Crux.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import ProjectBrief from "~/components/pedagogy/ProjectBrief.astro";

<Hook>
Прочитать, что надо определять SLO и убирать единые точки отказа, — не то же, что выдать дизайн доступности, под которым подпишется staff-инженер. Возьми реалистичный по форме бриф продукта, напиши его SLI и SLO с бюджетом ошибок, замапь и устрани каждый SPOF верным резервированием и failover, а затем докажи два самых рискованных утверждения — что failover реально восстанавливает и что система деградирует, а не темнеет — малым chaos-тестом, что напишешь сам.
</Hook>

<Crux>Преврати бриф в SLO и бюджет ошибок, устрани каждый SPOF резервированием и failover, затем chaos-тести восстановление и деградацию.</Crux>

<Explanation>

Этот проект делает весь раздел операционным: ты выберешь SLI, ориентированные на пользователя, и поставишь SLO под SLA, выведешь бюджет ошибок, выследишь SPOF и спроектируешь резервирование и failover, что держатся под коррелированным отказом, а затем измеришь — инъецируешь отказы и подтвердишь, что система восстанавливается в пределах цели и деградирует плавно, а не отказывает целиком.

<ProjectBrief
  id="02-availability-project"
  lessonSlug="project"
  lang="ru"
  objective="Сделай полный дизайн доступности для выбранного продукта (например сокращатель ссылок, платёжный API или чат-бэкенд): определи его SLI, SLO и бюджет ошибок; замапь и устрани каждую единую точку отказа уместно размеренным резервированием и планом failover; затем собери малый chaos-тест, что инъецирует отказы и эмпирически подтверждает, что failover восстанавливает в пределах цели и что система деградирует плавно — замыкая петлю между дизайном и отказами, которые он берётся пережить."
  requirements={[
    "Выбери продукт и напиши бриф в один абзац: что он делает, критический пользовательский путь (например «сократить ссылку и редиректнуть») и любой контрактный SLA, который примешь (например 99,9% месячной доступности с кредитами). Объяви допущения явно.",
    "Определи 2-3 SLI, что отражают то, что чувствует ПОЛЬЗОВАТЕЛЬ, каждый как отношение хороших событий к валидным, измеренное близко к пользователю (например «доля редирект-запросов, обслуженных < 200 мс с 2xx/3xx»), и скажи точно, где мерил бы каждый.",
    "Поставь SLO для каждого SLI, выбери окно измерения и поставь его намеренно ЖЁСТЧЕ принятого SLA; посчитай итоговый бюджет ошибок в минутах/запросах за окно и скажи, что делал бы, когда он наполовину истрачен и когда исчерпан.",
    "Нарисуй архитектуру и прогони мысленный эксперимент SPOF над каждым прямоугольником и линией («если это умрёт сейчас, продолжим обслуживать?»); выдай таблицу каждого найденного SPOF, включая общую инфраструктуру (единственный LB, primary БД, DNS, NAT) и не-машинные (вендор, единственный владелец).",
    "Для каждого SPOF укажи устранение: резервирование, размеренное до N+1 под ПИК, размещённое в НЕЗАВИСИМЫХ доменах отказа (разные AZ), и расстановку (active-active против active-passive) с обоснованным трейдоффом стоимость/время-восстановления.",
    "Напиши план failover для stateful-слоя: механизм обнаружения (health check/heartbeat) и его трейдофф таймаута, шаги продвижения/перенаправления и защиту от split-brain (кворум или fencing). Укажи таймауты, бюджет ретраев и backoff-с-jitter, чтобы failover не запустил retry storm.",
    "Перечисли поведения graceful degradation: для каждой зависимости — что система делает, когда та лежит (например отдать кэшированные/популярные результаты, режим только-чтение), чтобы смерть зависимости была пропавшей фичей, а не пустой страницей.",
    "Собери крошечный сервис (любой язык) с зарезервированным компонентом, который можешь убить (например два бэкенда за прокси или пара primary/резерв), плюс chaos-скрипт, что инъецирует отказы: убить узел, добавить задержку, разделить сеть и принудить зависимость лечь.",
    "Подтверди восстановление: убей активный узел под нагрузкой, измерь время восстановления (время до первого успешного запроса после убийства) и сравни с твоей целью failover; покажи прожиг бюджета ошибок, что вызвал инцидент.",
    "Подтверди деградацию и поведение ретраев: принуди некритическую зависимость лечь и покажи, что система отдаёт деградировавший-но-работающий ответ; затем покажи, что твой конфиг таймаут/ретрай/backoff предотвращает retry storm во время отказа (сравни с и без backoff-и-jitter).",
  ]}
  acceptance={[
    "Лист SLI/SLO/бюджета-ошибок: 2-3 ориентированных на пользователя SLI с тем, где каждый измеряется, SLO на SLI жёстче SLA, посчитанный бюджет ошибок за окно и политика для наполовину истраченного и исчерпанного бюджета — читается за две минуты.",
    "Таблица SPOF, что покрывает всю архитектуру (включая общую инфру и не-машинные SPOF), каждая строка с устранением: уровень резервирования (N+1/N+2), размещение по доменам отказа и active-active против active-passive с заявленным трейдоффом.",
    "План failover, называющий механизм обнаружения и его трейдофф таймаута, шаги продвижения/перенаправления, защиту от split-brain (кворум или fencing) и настройки таймаут/бюджет-ретраев/backoff-с-jitter, что удерживают восстановление от превращения в retry storm.",
    "Рабочий chaos-тест, чьё измеренное время восстановления после убийства активного узла отчитано против цели failover, плюс прожиг бюджета ошибок, что съел смоделированный инцидент.",
    "Демонстрация graceful degradation (принуждённая лечь зависимость даёт деградировавший-но-работающий ответ) и безопасности ретраев (тот же отказ с и без backoff-и-jitter, показывающий, что шторм предотвращён).",
    "Короткий разбор: один абзац о том, какой SPOF был наименее очевидным и как ты его нашёл, и один — где измеренное chaos-тестом восстановление разошлось с допущением дизайна и почему.",
  ]}
  stretch={[
    "Добавь тест коррелированного отказа: размести обе реплики в одном смоделированном домене отказа, положи домен и покажи, что «зарезервированная» пара отказывает вместе; затем перемести их в независимые домены и покажи, что переживает — оцифровав разницу в доступности.",
    "Добавь демонстрацию split-brain: раздели primary от резерва без fencing/кворума и покажи расхождение; затем включи fencing или кворум и покажи, что неверный вызов «мёртв» становится безвредным.",
    "Смоделируй девятки: из измеренного MTBF (инъецированной частоты отказов) и MTTR (измеренного восстановления) посчитай доступность ≈ MTBF/(MTBF+MTTR) и покажи, какая инвестиция — отказывать реже против восстанавливаться быстрее — двигает достижимый SLO сильнее.",
    "Добавь open-loop воспроизведение retry storm: держи зависимость медленной, прогони клиентов с наивными немедленными ретраями против бюджета ретраев с backoff-и-jitter и построй усиление нагрузки, что каждый производит на борющейся зависимости.",
  ]}
/>

</Explanation>

<KeyTakeaway>Дизайн доступности делает раздел операционным: SLO под SLA с бюджетом ошибок, каждый SPOF убран резервированием N+1 в независимых доменах, failover с защитой от split-brain и деградация — затем chaos-тест это доказывает.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="02-availability-project-retrieval"
  lang="ru"
  questions={[
    { q: "Что дизайн доступности производит до любого chaos-теста и как каждая часть связана?", a: "Он производит: ориентированные на пользователя SLI (отношения хороших/валидных, измеренные близко к пользователю), SLO, поставленные жёстче SLA, с посчитанным бюджетом ошибок и политикой траты, таблицу SPOF, покрывающую всю архитектуру (включая общую инфру и не-машинные SPOF), устранение на каждый SPOF (резервирование N+1 в независимых доменах отказа, active-active против active-passive с трейдоффом), план failover (обнаружение + трейдофф таймаута, продвижение/перенаправление, защита от split-brain через кворум/fencing, storm-безопасные таймауты/ретраи/backoff-с-jitter) и поведения graceful degradation. Каждое связано: SLO задаёт планку, бюджет ошибок — это запас, устранение SPOF поднимает пол, а деградация сдерживает остаток." },
    { q: "Как подтвердить failover и graceful degradation в chaos-тесте и что измеряешь?", a: "Failover: убей активный узел под нагрузкой и измерь время восстановления (время до первого успешного запроса после убийства) против цели, плюс прожиг бюджета ошибок, что вызвал инцидент. Деградация: принуди некритическую зависимость лечь и покажи деградировавший-но-работающий ответ вместо пустой страницы. Безопасность ретраев: воспроизведи отказ с и без backoff-и-jitter и покажи, что шторм предотвращён. Стретч: размести реплики в одном против независимых доменах отказа, чтобы продемонстрировать коррелированный отказ, и раздели с/без fencing, чтобы продемонстрировать split-brain, становящийся безвредным." },
    { q: "Почему проектировать SLO и устранение SPOF сначала, а потом chaos-тестить, а не просто chaos-тестить?", a: "Дизайн — это рассуждение, что устраняет целые классы сбоев на бумаге — таблица SPOF и верное резервирование/failover предотвращают отказы, что иначе обнаружишь в проде, а SLO/бюджет-ошибок говорит, сколько отказа вообще приемлемо. Chaos-тест медленнее и говорит лишь о том, что ты уже построил; дизайн сначала делает тест прицельным — ты проверяешь два самых рискованных утверждения (что failover восстанавливает в пределах цели и что система деградирует, а не темнеет), а не тыкаешь случайно. Дисциплина — дизайн-потом-валидация: бумажный дизайн говорит, что должно пережить, chaos-тест подтверждает, что переживает." },
  ]}
/>

<Recap lang="ru">
Этот проект превращает раздел доступности в повторяемый воркфлоу: возьми бриф продукта, определи **ориентированные на пользователя SLI** (отношения хороших/валидных, измеренные близко к пользователю), поставь **SLO жёстче SLA** и посчитай **бюджет ошибок** с политикой траты. Затем **выследи каждый SPOF** мысленным экспериментом над каждым прямоугольником и линией — включая общую инфраструктуру и не-машинные SPOF — и убери каждый **резервированием, размеренным до N+1 под пик, размещённым в независимых доменах отказа**, выбирая active-active или active-passive с заявленным трейдоффом. Напиши **план failover** (обнаружение и его трейдофф таймаута, продвижение/перенаправление, защита от **split-brain** через кворум или fencing и storm-безопасные таймауты/ретраи/backoff-с-jitter) и поведения **graceful degradation**, чтобы смерть зависимости была пропавшей фичей, а не пустой страницей. Затем **замкни петлю chaos-тестом**: убей активный узел и измерь восстановление против цели, принуди зависимость лечь и подтверди деградацию, и воспроизведи retry storm с и без backoff-и-jitter. Дизайн сначала и chaos-тест вторым — вся дисциплина: бумажный дизайн говорит, что должно пережить, тест подтверждает, что переживает. Инженер, что построил это однажды, проектирует под отказ осознанно, а не обнаруживает свои SPOF и свой split-brain во время инцидента в 3 ночи.
</Recap>
