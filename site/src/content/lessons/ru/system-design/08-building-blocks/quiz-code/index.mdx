---
slug: "quiz-code"
lang: ru
track: "system-design"
unit: "08-building-blocks"
order: 8
title: "Строительные блоки: чтение кода и конфигов"
summary: "Читай реальный код лимитера, конфиг ID, вызов размера bloom-фильтра и запрос geohash, затем выбери ответ, на который подписался бы senior: гонка потерянного обновления, фрагментирующий индекс ключ, насыщенный фильтр и пропущенный граничный сосед."
estMin: 14
status: ready
lessonType: topic
level: senior
concepts:
  - distributed-rate-limiting
  - index-locality
  - false-positive-rate
  - boundary-problem
  - fencing-token
  - fail-open
prereqs:
  - 08-building-blocks/01-rate-limiter
  - 08-building-blocks/02-unique-id-generation
  - 08-building-blocks/03-bloom-filters
  - 08-building-blocks/05-leader-election
sources:
  - https://stripe.com/blog/rate-limiters
  - https://en.wikipedia.org/wiki/Bloom_filter
  - https://martin.kleppmann.com/2016/02/08/how-to-do-distributed-locking.html
---
import Hook from "~/components/lesson/Hook.astro";
import Crux from "~/components/prose/Crux.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Баги строительных блоков прячутся в коде, не в прозе: read-then-write, что не атомарен, первичный ключ, фрагментирующий индекс, фильтр, размеренный под неверный N, запрос, забывший соседние ячейки. Читай каждый сниппет, найди дефект и выбери починку, на которую подписался бы senior.
</Hook>

<Crux>Читай лимитер, конфиг ID, размер фильтра и запрос geohash; найди гонку, фрагментирующий ключ, насыщение и пропущенного соседа.</Crux>

<Explanation>

Практикуй цикл ревью, что ты гоняешь на реальном коде блоков: найди операцию, проверь, атомарна ли она / упорядочена по времени / верно размерена / учитывает границу, и выбери изменение, которого механизм действительно требует.

## Сниппет 1 — инкремент лимитера

```python
def allow(key, limit):
    n = redis.get(key) or 0          # чтение
    if int(n) < limit:
        redis.set(key, int(n) + 1)   # modify-write (отдельная операция)
        return True
    return False
```

<Quiz
  id="08-building-blocks-code-1"
  lessonSlug="quiz-code"
  lang="ru"
  question="Этот лимитер работает на многих серверах к одному Redis. Под конкурентностью какой баг вводит read-then-write и какова починка?"
  choices={[
    { label: "Гонка потерянного обновления: два запроса оба GET один n ниже лимита и оба SET n+1, так что один инкремент теряется, и клиент превышает лимит. Чини атомарным redis.incr(key) (или Lua-скриптом), затем сравнивай.", correct: true },
    { label: "Бага нет — Redis сериализует команды, так что это уже безопасно.", misconception: "Redis сериализует отдельные команды, но GET и SET здесь — ОТДЕЛЬНЫЕ команды с зазором между ними. Два запроса могут оба сделать GET одного значения до того, как любой сделает SET. Нужен один атомарный INCR (или Lua), не две операции." },
    { label: "Баг в использовании строки вместо целочисленного типа.", misconception: "Преобразование int() в порядке. Настоящий баг в том, что чтение и запись — две неатомарные операции, поэтому параллельные запросы гоняются в потерянное обновление. Используй атомарный INCR." },
    { label: "Он будет недосчитывать, отвергая валидные запросы.", misconception: "Он ПЕРЕсчитывает клиента (пускает слишком много): параллельные запросы оба видят устаревший низкий n и оба пускают. Починка для потерянного обновления — атомарная инкремент-и-проверка." },
  ]}
/>

## Сниппет 2 — первичный ключ

```sql
-- высоконагруженная таблица событий, InnoDB (кластеризованный PK)
CREATE TABLE events (
  id   UUID DEFAULT gen_random_uuid()  PRIMARY KEY,  -- случайный UUIDv4
  body JSONB,
  ts   TIMESTAMPTZ
);
```

<Quiz
  id="08-building-blocks-code-2"
  lessonSlug="quiz-code"
  lang="ru"
  question="При миллионах вставок/час этот кластеризованный случайно-UUID первичный ключ вредит. В чём проблема и минимальное изменение?"
  choices={[
    { label: "Случайные ключи UUIDv4 разбрасывают вставки по всему кластеризованному B-дереву (расщепления страниц, потеря локальности кэша, фрагментация таблицы). Перейди на упорядоченный по времени ID (UUIDv7), чтобы вставки приземлялись у правого края индекса.", correct: true },
    { label: "UUID слишком велики; сожми до 32-битного int.", misconception: "Ширина вторична. Удар по пропускной способности — фрагментация от СЛУЧАЙНЫХ ключей на кластеризованном индексе. Упорядоченный по времени UUIDv7 сохраняет выгоды UUID, восстанавливая вставки у правого края." },
    { label: "gen_random_uuid() слишком медленный для вызова на вставку.", misconception: "Стоимость генерации пренебрежима рядом с фрагментацией индекса от случайных ключей. Используй UUIDv7 (упорядоченный по времени), чтобы починить паттерн вставки, не ускорить генерацию." },
    { label: "Ничего — случайные ключи распределяют нагрузку ровно, это хорошо.", misconception: "На кластеризованном индексе разброс вставок по всему дереву — ПЛОХО: он вызывает расщепления страниц и рушит локальность кэша. Нужны последовательные вставки у правого края, что даёт упорядоченный по времени UUIDv7." },
  ]}
/>

## Сниппет 3 — размер bloom-фильтра

```python
# размерен один раз при старте
bf = BloomFilter(expected_items=1_000, fp_rate=0.01)
# ...позже, в проде, он принимает каждый ключ события
for key in stream:        # миллионы ключей со временем
    bf.add(key)
```

<Quiz
  id="08-building-blocks-code-3"
  lessonSlug="quiz-code"
  lang="ru"
  question="Фильтр размерен под 1 000 элементов, но принимает миллионы. Что происходит и что остаётся гарантированным?"
  choices={[
    { label: "Он насыщается: установлено куда больше бит, чем планировалось, поэтому доля ложноположительных лезет к 100%, и фильтр отвечает «возможно» почти на всё (бесполезен). Гарантия нет-ложноотрицательных всё ещё держится. Перестрой под истинное N (или используй scalable filter).", correct: true },
    { label: "Он начинает давать ложноотрицательные, пропуская добавленные ключи.", misconception: "Насыщение никогда не вызывает ложноотрицательных — эта гарантия структурна (добавление лишь ставит биты). Оно вызывает рост доли ложноПОЛОЖИТЕЛЬНЫХ к 100%, делая фильтр бесполезным, но никогда не ошибающимся про «точно нет»." },
    { label: "Он автоматически увеличивается под новое число элементов.", misconception: "Простой фильтр Блума не может увеличиваться. За своим размером N он насыщается, и доля ложноположительных лезет. Надо перестроить больший (или использовать scalable bloom filter, сцепляющий бо́льшие фильтры)." },
    { label: "Ничего не меняется; fp_rate зафиксирован на 1% навсегда.", misconception: "1% держится лишь при размеренном N. Вставь в 1000× больше — и массив насыщается, толкая долю ложноположительных к 100%. Размеряй под реальное N." },
  ]}
/>

## Сниппет 4 — запрос близости

```python
def nearby(lat, lng):
    cell = geohash_encode(lat, lng, precision=6)   # ячейка ~1 км
    return db.query(
        "SELECT * FROM drivers WHERE geohash LIKE %s", cell + '%'
    )   # лишь собственная ячейка пассажира
```

<Quiz
  id="08-building-blocks-code-4"
  lessonSlug="quiz-code"
  lang="ru"
  question="Этот возвращает ноль водителей, даже когда один припаркован через улицу. Что не так и какова починка?"
  choices={[
    { label: "Проблема границы: водитель чуть за краем ячейки имеет другой префикс geohash, поэтому одноклеточный LIKE его пропускает. Запрашивай ячейку пассажира ПЛЮС её 8 соседних ячеек, затем ранжируй по точному расстоянию.", correct: true },
    { label: "precision=6 слишком груб; используй более длинный geohash.", misconception: "Точность не чинит границу: водитель через ЛЮБОЙ край ячейки всё равно имеет другой префикс. Починка — расширить запрос до 8 соседних ячеек (сетка 3×3), не менять точность." },
    { label: "LIKE не может использовать индекс, поэтому сканирует всё.", misconception: "Префиксный LIKE ('cell%') отлично использует строковый/B-tree индекс — в этом весь смысл geohash. Баг — запрос лишь одной ячейки; включи 8 соседей." },
    { label: "Ему нужен ещё фильтр точного расстояния, но выбор ячейки в порядке.", misconception: "Ранжирование по точному расстоянию тоже нужно, но баг пропущенного соседа — это проблема границы: одноклеточный запрос не видит водителя за краем. Сперва добавь 8 соседних ячеек, затем ранжируй по расстоянию." },
  ]}
/>

</Explanation>

<KeyTakeaway>Каждое решение в коде: атомарен ли инкремент лимитера, упорядочен ли PK по времени, размерен ли фильтр под реальное N, включает ли запрос geohash 8 соседей? Найди операцию, проверь инвариант, выбери починку механизма.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="08-building-blocks-code-retrieval"
  lang="ru"
  questions={[
    { q: "Как заметить и починить гонку распределённого лимитера в коде?", a: "Ищи чтение и запись как отдельные операции (GET затем SET или проверка-затем-инкремент) к общему хранилищу. Под конкурентностью два запроса оба читают устаревший счёт ниже лимита и оба пишут, теряя инкремент — клиент переливает. Чини, схлопнув в одну атомарную операцию: Redis INCR (затем сравнить) или Lua-скрипт, делающий refill и decrement ведра на сервере за один round trip." },
    { q: "Почему случайно-UUID кластеризованный первичный ключ вредит пропускной способности вставок и минимальная починка?", a: "На кластеризованном индексе строки таблицы хранятся в порядке ключа, так что случайный UUIDv4 каждый раз вставляется в случайный лист — разбрасывая записи по всему B-дереву, вызывая расщепления страниц и рушля локальность буферного кэша. Монотонный/упорядоченный по времени ключ добавляется к правому краю (одна горячая страница). Минимальная починка: сменить PK на упорядоченный по времени ID (UUIDv7) — тот же тип UUID, но timestamp в старших битах восстанавливает вставки у правого края." },
    { q: "Что происходит, когда фильтр Блума размерен под слишком мало элементов, и что сохраняется?", a: "Он насыщается: установлено куда больше бит, чем планировалось, поэтому k позиций несвязанного ключа вероятно все в 1, и доля ложноположительных лезет к 100% — фильтр отвечает «возможно» почти на всё и теряет ценность. Гарантия нет-ложноотрицательных сохраняется всё время (добавление лишь ставит биты). Простой фильтр не может увеличиваться; перестрой под истинное N или используй scalable bloom filter." },
  ]}
/>

<Recap lang="ru">
Каждое решение по блоку в этом юните читается прямо с кода. **Лимитер**, делающий отдельные чтение и запись на флоте, имеет **гонку потерянного обновления** — схлопни в **атомарный** INCR или Lua-скрипт. **Случайно-UUID кластеризованный первичный ключ** фрагментирует B-дерево на высоких темпах вставки — перейди на **упорядоченный по времени** UUIDv7 ради вставок у правого края. **Фильтр Блума**, размеренный под слишком мало элементов, **насыщается** (доля ложноположительных → 100%, но никогда ложноотрицательных) — перестрой под истинное N. А **запрос близости geohash**, сканирующий лишь ячейку пассажира, попадает в **проблему границы** — включи **8 соседних ячеек**, затем ранжируй по точному расстоянию. Senior-привычка одна и та же каждый раз: найди операцию, назови инвариант, что она должна держать (атомарность / упорядоченность по времени / верный размер / учёт границы), и выбери починку, его восстанавливающую.
</Recap>
