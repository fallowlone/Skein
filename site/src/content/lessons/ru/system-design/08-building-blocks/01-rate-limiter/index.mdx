---
slug: "01-rate-limiter"
lang: ru
track: "system-design"
unit: "08-building-blocks"
order: 1
title: "Rate limiter"
summary: "Rate limiter ограничивает, как быстро клиент бьёт по тебе. Token bucket и leaky bucket сглаживают всплески; фиксированное окно простое, но даёт спайк на границе; скользящее окно это чинит. На масштабе счётчик живёт в общем хранилище, и гонка read-modify-write — это вся проблема."
estMin: 18
status: ready
lessonType: topic
level: middle
concepts:
  - token-bucket
  - leaky-bucket
  - sliding-window
  - distributed-rate-limiting
  - retry-after
  - fail-open
prereqs:
  - 04-data-distribution/03-consistent-hashing
sources:
  - https://stripe.com/blog/rate-limiters
  - https://blog.cloudflare.com/counting-things-a-lot-of-different-things/
  - https://github.com/donnemartin/system-design-primer
---
import Hook from "~/components/lesson/Hook.astro";
import Crux from "~/components/prose/Crux.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Inset from "~/components/lesson/Inset.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import MetaphorComplete from "~/components/pedagogy/MetaphorComplete.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import Infographic from "~/components/diagram/Infographic.astro";
import boundarySpike from "./infographics/boundary-spike.svg?raw";

<Hook>
В ночном скрипте клиента был цикл ретраев без backoff. Один неудачный деплой на их стороне — и он застучал по эндпоинту оформления заказа со скоростью 9 000 запросов в секунду — для одного аккаунта. Эндпоинт был тяжёлым по CPU, пул воркеров насытился, задержка очереди ушла в вертикаль, и оформление у *всех остальных* клиентов начало отваливаться по таймауту. Починка была не «больше серверов» — один арендатор всегда обгонит любой фиксированный флот. Починка была rate limiter: маленький дешёвый шлюз, говорящий «именно ты получаешь N в секунду», ещё до того, как запрос дойдёт до дорогого кода. Интересна не идея — а то, что счётчик обязан быть корректным на всех серверах флота одновременно, и вот тут становится сложно.
</Hook>

<Crux>Rate limiter ограничивает темп клиента на краю. Бакеты сглаживают всплески; сложное — один общий счётчик, корректный на всём флоте.</Crux>

<Explanation>

## Зачем вообще ограничивать

Rate limiter защищает общий ресурс от монополизации — случайной (зацикленный ретрай) или намеренной (абьюз, перебор учёток, скрейпинг). Без него спайк одного клиента становится сбоем для всех: дорогой эндпоинт насыщается, кривая очередей уходит в вертикаль, и хорошие клиенты едят задержку, вызванную одним плохим. Stripe описывает ровно это — «сбойный скрипт пользователя, случайно шлющий кучу запросов» — и считает request rate limiter самым важным, который надо строить первым.

Лимитер — это *шлюз*, а не ёмкость. Если клиентам реально нужна эта пропускная способность и разнесение запросов меняет их результат (события реального времени), лимитер — не тот инструмент: нужна ёмкость или другой протокол. Задача лимитера — отвергнуть нагрузку, которую ты *не можешь* обслужить дёшево, с чётким сигналом, до того как она стоила тебе дорогой работы.

## Четыре алгоритма, два поведения

Алгоритмы делятся на два семейства: те, что сглаживают всплески, и те, что считают по окну.

**Token bucket.** Ведро хранит до `B` токенов и пополняется на `R` токенов/секунду. Каждый запрос берёт один токен; если ведро пусто — отказ. Ведро позволяет клиенту накопить неиспользованный лимит и потратить его **всплеском** до `B`, затем осесть на устойчивом темпе `R`. Это алгоритм, который Stripe использует в проде (централизованный bucket-хост, токены капают внутрь). Это дефолт для API, потому что он совпадает с тем, как ведёт себя реальный трафик: всплесковый, но ограниченный.

**Leaky bucket.** Запросы входят в очередь (ведро) и вытекают с фиксированным темпом, как вода через дырку. Переполнение отбрасывается. Он даёт идеально *ровный* выходной темп — хорошо для защиты downstream, ненавидящего всплески, — но добавляет задержку очереди и, в отличие от token bucket, не пропускает всплески. Token bucket пускает всплеск до `B`; leaky bucket его сглаживает.

**Fixed window (фиксированное окно).** Считай запросы за фиксированный интервал часов (например, в минуту); сбрасывай счётчик на границе. Тривиально — одно целое на клиента — но у него острый изъян, разобранный ниже.

**Sliding window (скользящее окно).** Два дешевле-чем-точных варианта чинят изъян фиксированного окна. **Скользящий лог** хранит метку времени на запрос и считает те, что в хвостовом окне — точно, но хранит каждый запрос. **Счётчик скользящего окна** смешивает счёт текущего и предыдущего фиксированных окон по тому, как далеко ты внутри окна — приближённо, но O(1) памяти. Cloudflare популяризовал приближение счётчиком именно потому, что хранить лог на клиента не масштабируется на миллионы ключей.

<Inset kind="edgecase" lang="ru">
Граничный спайк фиксированного окна. Лимит «100 запросов/минуту» с фиксированными окнами позволяет клиенту послать 100 в 11:00:59 и ещё 100 в 11:01:00 — **200 запросов за одну секунду**, оседлав границу, при этом технически не нарушив правило «в минуту». Счётчик каждой минуты невиновен; всплеск живёт в шве между ними. Этот 2× спайк — ровно та нагрузка, ради которой ты покупал лимитер. Скользящее окно (лог или счётчик) убирает шов, измеряя окно, что движется с *сейчас*, а не прыгает по часам. Если запомнить лишь одну причину опасности фиксированных окон — то эту.
</Inset>

<Infographic
  svg={boundarySpike}
  label="Столбчатая диаграмма запросов за одну секунду, оседлавшую минуту часов: фиксированное окно с лимитом 100 в минуту позволяет клиенту послать 100 прямо перед сбросом и 100 сразу после — 200 запросов примерно за секунду, тогда как лимит в минуту и предел скользящего окна оба остаются на 100."
  caption="Фиксированное окно соблюдает 100/мин, но на стыке клиент выдаёт 200 за ~1 секунду — тот самый 2x-всплеск, который скользящее окно убирает, измеряя окно, движущееся с «сейчас»."
  hue="var(--d-infra)"
/>

## Где ставить шлюз

Лимитер живёт как можно дальше **наружу** и как можно **раньше** — в идеале на краю (CDN/WAF) или на API-шлюзе, до того как запрос займёт слот соединения, воркер или запрос к БД на защищаемом сервисе. Внутри сервиса лимитер всё ещё защитит БД, но потратит дешёвые ресурсы (TLS-рукопожатие, поток-воркер) на запросы, которые ты всё равно отвергнешь. Общее правило: отвергай как можно ближе к клиенту, чтобы отказ был как можно дешевле.

Но *самый внешний* слой часто не имеет контекста для лимита по нужному ключу (ID пользователя, API-ключ, арендатор), поэтому реальные системы слоят лимитеры: грубый per-IP лимит на краю для поглощения потопов, затем точный per-API-key лимит на шлюзе, затем иногда лимитер конкурентности на сервисе для дорогих эндпоинтов. Stripe гоняет четыре вида в связке ровно по этой причине.

<FlowDiagram
  label="Запрос проходит три слоя ограничения подряд — грубый per-IP лимит на краю CDN, точный per-API-key лимит на шлюзе на базе общего счётчика Redis и лимитер конкурентности на сервисе для дорогих эндпоинтов — прежде чем достигнуть защищаемого обработчика."
  perRow={5}
  hue="var(--d-net)"
  nodes={[
    { id: "client", label: "клиент", sub: "API-ключ + IP" },
    { id: "edge", label: "CDN / WAF", sub: "per-IP, грубо" },
    { id: "gw", label: "API-шлюз", sub: "per-key, token bucket" },
    { id: "svc", label: "сервис", sub: "лимит конкурентности" },
    { id: "handler", label: "обработчик", sub: "дорогая работа" },
    { id: "redis", label: "Redis", sub: "общий счётчик" },
  ]}
  edges={[
    { from: "client", to: "edge" },
    { from: "edge", to: "gw" },
    { from: "gw", to: "svc" },
    { from: "svc", to: "handler" },
    { from: "gw", to: "redis", label: "атомарный incr" },
  ]}
  caption="Лимитеры слоятся: грубо и дёшево на краю, точно per-key на шлюзе (где живёт общий счётчик), конкурентность на сервисе. Отвергай так рано и так далеко наружу, как позволяет контекст ключа, чтобы отвергнутый запрос почти ничего не стоил."
/>

## Распределённый счётчик и гонка

Один сервер с одним счётчиком в памяти — легко. Флот из пятидесяти серверов за балансировщиком — реальная проблема: каждый запрос по одному API-ключу может приземлиться на любой сервер, поэтому счёт должен быть **общим**, иначе пятьдесят серверов каждый держат «100/мин» и клиент реально получает 5 000/мин. Стандартный ответ — общее хранилище, обычно Redis, держащее по счётчику на ключ.

Теперь гонка. Наивная последовательность — read-modify-write:

```
n = GET key          # сервер A читает 99
n = GET key          # сервер B тоже читает 99 (параллельно)
if n < 100:          # оба видят 99 < 100
    SET key n+1      # оба пишут 100 — один инкремент ПОТЕРЯН
    allow
```

Два параллельных запроса оба читают 99, оба решают, что они под лимитом, оба пускают — клиент получил 101. Под высокой конкурентностью утечка сильная. Починка — **атомарность**: сделать инкремент-и-проверку единой неделимой операцией. `INCR` в Redis атомарен, поэтому частый паттерн — `INCR` затем сравнение, или маленький Lua-скрипт, делающий весь refill-and-decrement token bucket на стороне сервера за один round trip (Redis исполняет скрипт атомарно). Гонка — вся причина, почему распределённый rate limiting сложнее, чем кажется: алгоритм тривиален; сделать его корректно без потерянного обновления — нет.

<Inset kind="mistake" lang="ru">
Что происходит, когда хранилище лимитов падает? Если твой лимитер делает `INCR` к Redis на критическом пути, а Redis недоступен, наивная реализация либо ошибётся на каждом запросе (ты положил себя сам ради соблюдения *лимита*), либо повиснет на таймауте (хуже). Правило Stripe: **fail open** (отказывай в сторону «пропустить»). Оберни лимитер так, чтобы при ошибке или таймауте хранилища запрос *пропускался*, а не блокировался — лимитер это ремень безопасности, а не несущая стена, и он никогда не должен быть причиной падения API. Дополни kill switch (фича-флаг, чтобы отключить сбойный лимитер) и коротким таймаутом, чтобы медленный Redis не добавлял задержку каждому вызову.
</Inset>

<TradeoffMatrix
  id="01-rate-limiter-algorithm"
  lessonSlug="01-rate-limiter"
  lang="ru"
  prompt="Публичный REST API обслуживает мобильных клиентов, которые шлют пачечный трафик: при открытии приложения пользователь отправляет 12 запросов за первую секунду, затем 1–2 в минуту. API должен разрешать этот всплеск, но ограничивать устойчивый темп до 60 запросов/мин. Какой алгоритм подходит?"
  options={[
    { name: "Token bucket (корзина токенов)", summary: "Разрешает всплески до размера корзины, затем применяет устойчивый темп — именно правильная форма для пачечных клиентов.", justification: "Token bucket пополняется со скоростью 1 токен/сек (60/мин устойчивый темп) и удерживает корзину из 12 токенов. Всплеск из 12 запросов при открытии приложения опустошает корзину мгновенно — все 12 разрешены. Последующие запросы поступают по скорости пополнения. Это точно соответствует требованию: всплеск разрешён, устойчивый темп ограничен. Именно этот алгоритм Stripe использует в продакшене — реальный API-трафик пачечный, и штрафовать за легитимный всплеск значит наказывать нормальное поведение.", correct: true },
    { name: "Fixed window counter (фиксированное окно)", summary: "Считает запросы по минуте часов — просто, но граничный всплеск позволяет двойной лимит за ~1 секунду.", justification: "Фиксированное окно сбрасывает счётчик каждые 60 секунд. Всплеск из 12 разрешается в любом окне с оставшимся бюджетом. Но граничная проблема означает, что клиент может послать 60 запросов в конце одной минуты и 60 в начале следующей — 120 за ~1 секунду. Кроме того, фиксированные окна не моделируют квоту всплеска как самостоятельную концепцию: они применяют поминутный подсчёт, не разграничивая 12 запросов сразу и 1 каждые 5 секунд." },
    { name: "Leaky bucket (дырявая корзина)", summary: "Производит идеально ровный выходной темп — но намеренно сглаживает всплески, отклоняя 12-запросный открывающий спайк.", justification: "Leaky bucket ставит запросы в очередь и сливает их с фиксированной скоростью. Всплеск из 12 запросов при открытии приложения поставит 11 из них в очередь (только 1 сливается немедленно), добавляя задержку к первому взаимодействию. Leaky bucket — правильный выбор, когда нижестоящая система не терпит никаких всплесков, а не когда цель — разрешать всплески, ограничивая при этом устойчивый темп." },
    { name: "Sliding window counter (скользящее окно)", summary: "Закрывает граничный шов фиксированного окна, но не моделирует квоту всплеска как самостоятельную концепцию.", justification: "Скользящее окно смешивает два соседних подсчёта фиксированного окна, приближая плавное окно и закрывая граничный двукратный всплеск. Это точнее фиксированного окна и требует O(1) памяти. Но, как и фиксированное окно, оно считает запросы за скользящий интервал — не различает «всплеск из 12, затем простой» и «12 за минуту». Для предотвращения граничного спайка sliding window лучше fixed window; для явного разрешения всплесков до квоты при соблюдении устойчивого темпа token bucket — прямой механизм." },
  ]}
/>

## Сказать клиенту: 429 и Retry-After

Когда отвергаешь — скажи точно. Стандарт — HTTP **429 Too Many Requests** с заголовком **`Retry-After`**, говорящим клиенту, сколько секунд подождать перед повтором (некоторые API также шлют `X-RateLimit-Remaining` / `-Reset`, чтобы хороший клиент сам притормозил *до* удара в стену). Это важно, потому что альтернатива — голая ошибка без подсказки — толкает клиентов в тесные циклы ретраев, которые *добавляют* нагрузку ровно тогда, когда ты уже перегружен, превращая brownout в полный сбой. Точный 429 с `Retry-After` даёт хорошему клиенту откатиться чисто; вместе с экспоненциальным backoff и jitter на стороне клиента это и держит ограниченную систему стабильной, а не дёргающейся.

<Quiz
  id="01-rate-limiter-quiz-1"
  lessonSlug="01-rate-limiter"
  lang="ru"
  question="Лимит 100 запросов/минуту использует фиксированное окно, сбрасывающееся на минуте часов. Каков худший всплеск, который клиент может легитимно послать, и за какой промежуток?"
  choices={[
    { label: "200 запросов за ~1 секунду — 100 в конце одного окна (например, 11:00:59) и 100 в начале следующего (11:01:00), оседлав границу.", correct: true },
    { label: "100 запросов, всегда — лимит 100/мин и он строго соблюдается.", misconception: "Лимит соблюдается по окну, а не через шов между окнами. Клиент может выстрелить 100 прямо перед сбросом и 100 сразу после — 200 примерно за секунду — ни разу не нарушив счёт одной минуты." },
    { label: "Безлимитно — фиксированные окна вообще ничего не ограничивают.", misconception: "Они ограничивают каждое окно сотней. Изъян уже: 2× спайк, оседлавший границу. Скользящие окна закрывают этот шов; фиксированные — нет." },
    { label: "Проблемы всплеска нет; счётчик сбрасывается чисто.", misconception: "Чистый сброс И ЕСТЬ проблема — он создаёт шов. 100 перед сбросом плюс 100 после = 200 за ~1 с, ровно тот спайк, что лимитер должен был остановить." },
  ]}
/>

<Quiz
  id="01-rate-limiter-quiz-2"
  lessonSlug="01-rate-limiter"
  lang="ru"
  question="На флоте из 50 серверов каждый сервер читает общий счётчик, проверяет, что он ниже лимита, затем пишет инкрементированное значение. Под конкурентностью клиент превышает лимит. В чём починка?"
  choices={[
    { label: "Сделать инкремент-и-проверку единой атомарной операцией (Redis INCR или Lua-скрипт, делающий refill и decrement ведра на стороне сервера), чтобы никакие два запроса не могли оба прочитать один устаревший счёт.", correct: true },
    { label: "Дать каждому серверу свой счётчик и поделить лимит на 50.", misconception: "Статическое деление per-server тратит ёмкость (трафик не балансируется ровно — сервер без трафика «теряет» свою долю) и всё равно дрейфует под неравной нагрузкой. Настоящая починка — атомарная операция на одном общем счётчике." },
    { label: "Добавить блокировку, которую каждый сервер берёт перед чтением счётчика.", misconception: "Грубый распределённый лок на запрос сериализует всё и становится узким местом. Лок не нужен — нужен атомарный примитив (INCR / атомарный Lua), дающий корректность без сериализации всего флота." },
    { label: "Прочитать счётчик дважды и усреднить результаты.", misconception: "Двойное чтение не делает read-modify-write атомарным; оба чтения всё равно могут увидеть устаревшие данные. Потерянное обновление идёт из зазора между чтением и записью, который закрывает только атомарность." },
  ]}
/>

<MetaphorComplete
  id="01-rate-limiter-metaphor-1"
  lessonSlug="01-rate-limiter"
  lang="ru"
  setup="_______ хранит до B токенов и пополняется на R в секунду; каждый запрос тратит один токен и отвергается, если ведро пусто — поэтому клиент может всплеснуть до B после тишины, но держит лишь R во времени."
  accepted={["token bucket", "token-bucket", "токен-бакет"]}
  canonical="token bucket"
  explanation="Token bucket — продакшен-дефолт (его использует Stripe): ёмкость B задаёт максимальный всплеск, темп пополнения R — устойчивый темп. Он совпадает с реальным всплесковым-но-ограниченным трафиком, в отличие от leaky bucket, который всплески полностью сглаживает."
/>

</Explanation>

<KeyTakeaway>Rate limiter гейтит клиента на краю. Token bucket пускает всплеск до B при темпе R; скользящие окна бьют фиксированные (2× спайк на границе). На масштабе — общий счётчик, атомарная инкремент-и-проверка, fail open.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="01-rate-limiter-retrieval"
  lang="ru"
  questions={[
    { q: "Сравни token bucket, leaky bucket, fixed window и sliding window.", a: "Token bucket: ёмкость B (макс. всплеск), пополнение R/с; пускает всплеск до B, затем оседает на R — продакшен-дефолт (Stripe). Leaky bucket: очередь, вытекающая с фиксированным темпом, даёт идеально ровный выход, но без всплесков и с задержкой очереди. Fixed window: один счётчик на интервал часов — тривиально, но допускает 2× граничный спайк. Sliding window: лог (точно, хранит каждый запрос) или счётчик (O(1), смешивает текущее+предыдущее окно) — закрывает граничный шов." },
    { q: "Почему распределённый rate limiting сложен и в чём починка?", a: "На флоте любой запрос по ключу может приземлиться на любой сервер, поэтому счёт должен быть общим (иначе N серверов каждый пускает полный лимит). Наивный read-modify-write гоняется: два запроса оба читают 99, оба пускают, клиент получает 101 — потерянное обновление. Починка — атомарность: Redis INCR или Lua-скрипт, делающий refill-and-decrement на сервере за один атомарный round trip. Алгоритм тривиален; корректность под конкурентностью — вся проблема." },
    { q: "Как лимитер должен вести себя при падении хранилища и как отвергать?", a: "Fail open: при ошибке или таймауте Redis пропускай запрос, а не блокируй — лимитер это ремень безопасности, не несущая часть, и не должен быть причиной падения API (дополни kill switch и коротким таймаутом). Когда отвергает — возвращай HTTP 429 с Retry-After (и в идеале X-RateLimit-Remaining/-Reset), чтобы хороший клиент откатился с экспоненциальным backoff + jitter, а не дёргался в тесном цикле, добавляя нагрузку." },
  ]}
/>

<Recap lang="ru">
**Rate limiter** — дешёвый шлюз, ограничивающий темп каждого клиента до того, как запрос дойдёт до дорогой работы, защищая общий ресурс от спайка одного арендатора — случайного или враждебного. Четыре алгоритма, два поведения: **token bucket** (ёмкость B = макс. всплеск, пополнение R = устойчивый темп; продакшен-дефолт) и **leaky bucket** сглаживают всплески; **fixed window** тривиален, но допускает **2× граничный спайк** (100 прямо перед сбросом + 100 сразу после), который **sliding window** (лог или счётчик) закрывает. Ставь его **как можно дальше наружу и как можно раньше** — грубо per-IP на краю, точно per-key на шлюзе, конкурентность на сервисе — чтобы отказ был дёшев. На масштабе счётчик должен быть **общим** (один счётчик Redis на ключ), а наивный read-modify-write **гоняется** в потерянные обновления, поэтому инкремент-и-проверка должны быть **атомарными** (INCR или атомарный Lua-скрипт). Наконец, **fail open** при падении хранилища и отвергай через **429 + Retry-After**, чтобы хорошие клиенты откатывались, а не дёргались. Теперь, когда встретишь сервис, где один клиент валит всех остальных — сначала проверь, есть ли rate limiter, а затем — действительно ли счётчик общий на весь флот.
</Recap>
