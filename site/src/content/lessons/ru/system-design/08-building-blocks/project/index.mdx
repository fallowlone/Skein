---
slug: "project"
lang: ru
track: "system-design"
unit: "08-building-blocks"
order: 9
title: "Строительные блоки: распределённый rate limiter от начала до конца"
summary: "Практический проект: построй корректный распределённый token-bucket лимитер — общий счётчик, атомарный инкремент, 429 + Retry-After, fail-open — затем докажи, что гонка ушла под конкурентной нагрузкой, и порассуждай, как остальные блоки юнита сложатся в тот же сервис."
estMin: 240
status: ready
lessonType: topic
level: senior
concepts:
  - token-bucket
  - distributed-rate-limiting
  - retry-after
  - fail-open
  - fencing-token
  - false-positive-rate
prereqs:
  - 08-building-blocks/01-rate-limiter
  - 08-building-blocks/02-unique-id-generation
  - 08-building-blocks/03-bloom-filters
  - 08-building-blocks/04-geohashing
  - 08-building-blocks/05-leader-election
sources:
  - https://stripe.com/blog/rate-limiters
  - https://blog.cloudflare.com/counting-things-a-lot-of-different-things/
  - https://github.com/donnemartin/system-design-primer
---
import Hook from "~/components/lesson/Hook.astro";
import Crux from "~/components/prose/Crux.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import ProjectBrief from "~/components/pedagogy/ProjectBrief.astro";

<Hook>
Прочитать, что гонка распределённого счётчика — сложная часть rate limiting, не то же самое, что увидеть, как наивный лимитер протекает под нагрузкой, а затем увидеть, как атомарная версия держит линию. Построй настоящий token-bucket лимитер на общем хранилище, эмпирически продемонстрируй гонку потерянного обновления неатомарной версией, почини её и добавь продакшен-поведения — 429 + Retry-After, fail-open — отделяющие игрушку от того, что ты бы выкатил.
</Hook>

<Crux>Построй распределённый token-bucket лимитер, докажи гонку эмпирически, почини атомарной операцией и добавь 429 + Retry-After и fail-open.</Crux>

<Explanation>

Этот проект делает центральный урок юнита операционным: алгоритм тривиален, но корректность под конкурентностью — вся проблема. Ты построишь лимитер, *измеришь* гонку, починишь её атомарно и закалишь — затем порассуждаешь, как остальные блоки юнита встанут в тот же шлюз.

<ProjectBrief
  id="08-building-blocks-project"
  lessonSlug="project"
  lang="ru"
  objective="Построй и закали распределённый token-bucket лимитер на общем хранилище (Redis или эквивалент), эмпирически продемонстрируй гонку read-modify-write в наивной версии и докажи, что она исчезает в атомарной версии, и реализуй продакшен-поведения отказа и устойчивости — замыкая петлю между алгоритмом и сервисом, который ты бы реально гонял."
  requirements={[
    "Реализуй token-bucket лимитер с ведром на ключ: ёмкость B (макс. всплеск) и темп пополнения R (устойчивый темп), на общем хранилище, чтобы несколько инстансов приложения делили один счётчик на ключ.",
    "Сначала построй НАИВНУЮ версию, делающую неатомарный read-then-check-then-write (GET → сравнение → SET), чтобы гонка потерянного обновления присутствовала по построению.",
    "Напиши генератор нагрузки, выстреливающий параллельные запросы по одному ключу от многих воркеров/инстансов сразу, и записывай, сколько запросов реально пропущено против настроенного лимита.",
    "Продемонстрируй гонку: покажи, что под высокой конкурентностью наивный лимитер пускает измеримо БОЛЬШЕ лимита (потерянное обновление), и количественно оцени перелив как функцию конкурентности.",
    "Почини атомарной операцией: перепиши горячий путь как единую атомарную инкремент-и-проверку (атомарный INCR + сравнение или Lua-скрипт, делающий refill-and-decrement ведра на сервере за один round trip), и перезапусти ту же нагрузку, чтобы показать, что перелив ушёл.",
    "Реализуй контракт отказа: на отказ возвращай HTTP 429 с верным Retry-After (секунды до пополнения достаточного числа токенов) и выставляй заголовки X-RateLimit-Remaining / -Reset.",
    "Реализуй fail-open: оберни вызов хранилища коротким таймаутом, чтобы при ошибке или медленности хранилища запрос ПРОПУСКАЛСЯ (не блокировался и не отдавал 500), и добавь kill-switch флаг плюс метрику, считающую события fail-open.",
    "Добавь заметку о слоях: помести грубый per-IP лимит и точный per-key лимит концептуально подряд (край против шлюза) и объясни, какой ключ использует каждый слой и почему.",
  ]}
  acceptance={[
    "Рабочий token-bucket лимитер (ёмкость B, пополнение R) на общем хранилище, обслуживающий несколько инстансов к одному счётчику на ключ.",
    "Воспроизводимый эксперимент: таблица или график, показывающие рост перелива наивного лимитера с конкурентностью и атомарную версию, держащую на уровне лимита или ниже под той же нагрузкой — с приведёнными числами.",
    "Корректный путь отказа: ответы 429 несут точный Retry-After, а заголовки позволяют клиенту сам тормозить до удара в стену.",
    "Путь fail-open, доказанный тестом инъекции отказа: когда хранилище заставлено ошибаться или таймаутить, запросы пропускаются (проверено), метрика fail-open растёт, и kill switch отключает лимитер.",
    "Короткий разбор: один абзац с количественной оценкой гонки и почему атомарность её убирает, и один абзац про слои (per-IP край против per-key шлюз) и что каждый защищает.",
  ]}
  stretch={[
    "Добавь fencing для единичной задачи: введи маленький кусок работы, что должен идти ровно на одном инстансе (например, периодическая чистка вёдер), избери лидера через lease и защити общий ресурс монотонно растущим fencing token — затем симулируй застрявшего лидера и покажи, что операция с устаревшим token отвергается.",
    "Добавь страж на фильтре Блума: перед лимитером используй фильтр Блума из «известных абьюзных ключей» (или, наоборот, валидных API-ключей), чтобы дёшево отвергать очевидный трафик до касания счётчика — размерь его под целевую долю ложноположительных и объясни, почему ложноположительный здесь безвреден.",
    "Сравни алгоритмы эмпирически: реализуй лимитер фиксированного окна рядом с token bucket и продемонстрируй 2× граничный спайк (100 перед сбросом + 100 после), которого нет у token bucket / скользящего окна.",
    "Добавь упорядоченный по времени ID запроса (UUIDv7 или Snowflake) в каждую строку лога запросов и покажи, как сортируемые ID позволяют range-scan лога по времени для реконструкции, какие именно запросы лимитер пропустил во время спайка.",
  ]}
/>

</Explanation>

<KeyTakeaway>Построение лимитера делает урок конкретным: алгоритм тривиален, гонка — работа. Измерь перелив в наивной версии, убей его одной атомарной операцией, затем добавь 429 + Retry-After и fail-open.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="08-building-blocks-project-retrieval"
  lang="ru"
  questions={[
    { q: "Как эмпирически продемонстрировать, а затем починить гонку распределённого лимитера?", a: "Демонстрация: построй наивный лимитер, делающий неатомарный GET → сравнение → SET к общему хранилищу, выстрели много параллельных запросов по одному ключу от нескольких воркеров и посчитай пропущенные против лимита — под высокой конкурентностью он пускает измеримо больше (потерянное обновление), и перелив растёт с конкурентностью. Починка: перепиши горячий путь как единую атомарную операцию — атомарный INCR + сравнение или Lua-скрипт, делающий refill-and-decrement на сервере за один round trip — и перезапусти ту же нагрузку, чтобы показать исчезновение перелива." },
    { q: "Какие продакшен-поведения отделяют игрушечный лимитер от пригодного к выкатке?", a: "Корректный контракт отказа — HTTP 429 с точным Retry-After (секунды до пополнения достаточного числа токенов) плюс X-RateLimit-Remaining/-Reset, чтобы хорошие клиенты сами тормозили и откатывались с jitter, а не дёргались в тесном цикле. И устойчивость fail-open — короткий таймаут хранилища, пропуск запроса при любой ошибке/таймауте хранилища (никогда блокировка или 500), метрика fail-open и kill switch — чтобы лимитер был ремнём безопасности, никогда причиной падения API. Плюс слои: грубо per-IP на краю, точно per-key на шлюзе." },
    { q: "Как остальные блоки юнита сложились бы в тот же шлюз?", a: "Фильтр Блума может дёшево предотвергать известно-плохие (или невалидные) ключи до счётчика, где ложноположительный безвреден (обычный поиск). Упорядоченный по времени ID (UUIDv7/Snowflake) в каждой строке лога запросов держит лог сортируемым для range-scan по времени. Выбор лидера + fencing token защищают любую единичную задачу обслуживания (например, чистку вёдер) от split-brain. А geohashing применился бы, если бы шлюз делал ещё и маршрутизацию по близости. Каждый блок стережёт свой инвариант; вместе они образуют слой надёжности шлюза." },
  ]}
/>

<Recap lang="ru">
Этот проект превращает юнит строительных блоков в то, что можно гонять и измерять. Ты строишь **token-bucket** лимитер (ёмкость B, пополнение R) на **общем хранилище**, затем намеренно делаешь **гонку** видимой: наивная read-then-write версия пускает **измеримо больше** лимита под конкурентностью (потерянное обновление), и перелив растёт с нагрузкой. Затем ты **чинишь её одной атомарной операцией** — атомарным INCR или Lua-скриптом, делающим refill-and-decrement за один round trip — и перезапускаешь ту же нагрузку, чтобы показать исчезновение перелива. Это центральная истина юнита, сделанная эмпирической: алгоритм тривиален, корректность под конкурентностью — работа. Затем ты добавляешь поведения, делающие его пригодным к выкатке — **429 + Retry-After** и самоторможение по заголовкам, и устойчивость **fail-open** с коротким таймаутом, метрикой и kill switch — и слоишь грубый per-IP и точный per-key лимиты. Наконец ты рассуждаешь о композиции: **фильтр Блума** для дешёвого предотвержения, **упорядоченные по времени ID** для range-scan лога, и **выбор лидера + fencing token** для защиты единичной задачи обслуживания от split-brain. Инженер, построивший это однажды, больше никогда не путает «я написал rate limiter» с «я написал *корректный, распределённый* rate limiter».
</Recap>
