---
slug: "01-caching-strategies"
lang: ru
track: "system-design"
unit: "05-caching-at-scale"
order: 1
title: "Стратегии кэширования"
summary: "Cache-aside, read-through, write-through, write-back и write-around не взаимозаменяемы: каждая по-своему прокладывает пути чтения и записи и даёт своё обещание — и нарушает своё — про свежесть, цену промаха и поведение при смерти кэша."
estMin: 18
status: ready
lessonType: topic
level: middle
concepts:
  - cache-aside
  - read-through
  - write-through
  - write-back
  - write-around
  - read-write-path
prereqs: []
sources:
  - https://docs.aws.amazon.com/AmazonElastiCache/latest/mem-ug/Strategies.html
  - https://redis.io/docs/latest/develop/use/patterns/
  - https://aws.amazon.com/builders-library/caching-challenges-and-strategies/
---
import Hook from "~/components/lesson/Hook.astro";
import Crux from "~/components/prose/Crux.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Inset from "~/components/lesson/Inset.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import MetaphorComplete from "~/components/pedagogy/MetaphorComplete.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import Infographic from "~/components/diagram/Infographic.astro";
import writePathTradeoff from "./infographics/write-path-tradeoff.svg?raw";

<Hook>
Команда добавила кэш перед сервисом товаров и увидела падение p99 на 80% — явная победа, выкаченная в пятницу. В понедельник поддержка сообщила, что у горстки товаров цена устарела на трое суток, а одному клиенту выставили неверную сумму. Ничего не «сломалось»: кэш делал ровно то, что построили — грузил значение при первом чтении и больше никогда его не обновлял. Они выбрали *стратегию* кэширования, не осознав, что выбор вообще был. Вопрос не «кэшировать ли?», а «где данные входят в кэш, где выходят и кто отвечает за их честность?»
</Hook>

<Crux>Каждая стратегия по-своему прокладывает путь чтения/записи. Выбирай по тому, где лягут свежесть, цена промаха и риск потери кэша.</Crux>

<Explanation>

## Стратегия кэширования — это схема разводки двух путей

Когда ставишь кэш перед медленным хранилищем, надо ответить на два независимых вопроса. При **чтении**: кто идёт в базу при промахе кэша — приложение или сам кэш? При **записи**: куда попадает новое значение — в кэш, в базу или в оба, и в каком порядке? Именованные стратегии — cache-aside, read-through, write-through, write-back, write-around — это просто небольшой набор разумных ответов на эти два вопроса. Это не флажки, а сам поток данных, и выбор одной фиксирует твою гарантию свежести, штраф за промах и поведение при смерти узла кэша.

Пять стратегий делятся чисто: **cache-aside** и **read-through** — паттерны *пути чтения* (как данные *попадают* в кэш), а **write-through**, **write-back** и **write-around** — паттерны *пути записи* (что запись делает с кэшем). Реальные системы комбинируют по одному из каждого — самая частая боевая пара — cache-aside на чтении с write-around или TTL на записи.

К концу урока ты поймёшь, какой именно выбор сделала команда из вступления — и как делать его осознанно для своих данных.

## Cache-aside (lazy loading): оркестрирует приложение

В **cache-aside** — AWS зовёт это *lazy loading* — логикой владеет приложение. При чтении: спроси кэш; при попадании верни; при промахе сходи в базу, запиши результат в кэш и верни. Кэш — тупой key-value ящик, ничего не знающий про твою базу.

```
ЧТЕНИЕ (cache-aside):
  v = cache.get(key)
  if v is null:               # промах
      v = db.read(key)        #   поход в БД
      cache.set(key, v, ttl)  #   заполнить
  return v
```

Достоинства — ровно те, что перечисляет AWS: **кэшируются только запрошенные данные** (рабочий набор заполняется естественно, ты не греешь то, что никто не читает), и **сбой узла переживаем** — свежий пустой узел просто даёт промахи, которые его заполняют, с деградацией задержки, но без сбоя. Цена: каждый промах — **штраф в три похода** (промах кэша, чтение БД, запись в кэш), и поскольку при изменении *базы* кэш не обновляется, кэшированные значения **устаревают**, пока не добавишь TTL или инвалидацию. Это устаревание и есть баг из вступления.

## Read-through: оркестрирует кэш

**Read-through** переносит логику промаха *в слой кэша* (или библиотеку/провайдер перед ним). Приложение всегда зовёт только `cache.get(key)`; при промахе сам кэш грузит из базы, сохраняет и возвращает. Поток данных идентичен cache-aside — разница в том, *кто пишет код*. Read-through централизует логику загрузки, чтобы все вызывающие вели себя одинаково и нельзя было выкатить сервис, забывший заполнить кэш при промахе; плата — нужен провайдер кэша с поддержкой этого и теряется свобода формировать каждый запрос.

<Inset kind="why" lang="ru">
Почему cache-aside доминирует, несмотря на аккуратность read-through? Потому что cache-aside держит кэш и базу **расцепленными**: кэш может быть общим Redis/Memcached, приложение может кэшировать *вычисленное* значение (склеенный, отрендеренный или агрегированный объект без единой строки БД за ним), и логику загрузки можно менять для каждой точки вызова. Read-through сцепляет кэш с известным загрузчиком — чисто для «кэшируй одну таблицу по первичному ключу», но неудобно, когда кэшируемый объект выводится из пяти запросов. Большинство крупных систем выбирают расцепленный путь и платят за него дисциплиной — каждая точка записи обязана инвалидировать или ставить TTL — и поэтому инвалидация становится самым трудным уроком раздела.
</Inset>

## Три стратегии записи: куда ложится запись

Когда ты трогаешь путь записи, ты решаешь не просто куда сохранить данные, а насколько кэш останется честным, какую производительность получишь и чем рискуешь при сбое. У этого решения есть имя. На пути записи вопрос — что `write(key, value)` делает с кэшем. Три ответа:

- **Write-through** — пиши кэш *и* базу синхронно, на каждой записи. Кэш **никогда не устаревает** (обновляется вместе с БД), но каждая запись платит **два похода**, и кэш заполняется данными, которые могут вообще не прочитать (*cache churn*) — поэтому write-through обычно идёт с TTL, вытесняющим непрочитанный мусор. AWS отмечает компромисс ровно так: записи замедляются, но пользователи терпят задержку записи лучше, чем чтения.
- **Write-back (write-behind)** — пиши кэш *сейчас*, верни вызывающему и сбрось в базу *асинхронно* (батчем, позже). Это даёт **минимальную задержку записи и поглощает всплески записи**, склеивая много обновлений в меньше походов в БД. Опасность жестокая: если узел кэша умрёт до сброса, эти подтверждённые записи **пропадут**. Write-back меняет надёжность на скорость, поэтому подходит метрикам, счётчикам и просмотрам — не деньгам.
- **Write-around** — пиши *только* базу, минуя кэш; значение войдёт в кэш позже, лениво, при следующем чтении (через cache-aside). Это избегает забивания кэша данными с интенсивной записью, которые скоро не читают, ценой гарантированного **промаха на первом чтении** после записи. Подходит данным «записал-раз-читаю-может-быть» вроде логов.

Вместе эти три стратегии означают, что ты меняешь задержку записи, надёжность и свежесть между собой. Когда встретишь write-back в проектировании, первый вопрос — какие именно данные мы можем позволить себе потерять, если узел упадёт до сброса?

<Infographic
  svg={writePathTradeoff}
  label="Трёхстороннее сравнение стратегий записи, каждая показана с тем, что держит и чем жертвует: write-through держит свежесть, но жертвует задержкой записи (два синхронных похода); write-back держит минимальную задержку, но жертвует надёжностью (несброшенные записи теряются при крахе кэша); write-around держит кэш свободным от churn, но жертвует свежестью первого чтения (гарантированный промах после каждой записи)."
  caption="Каждая стратегия записи жертвует ровно одним из быстро, свежо, надёжно — варианта со всеми тремя нет, поэтому жертву выбирают по классу данных."
  hue="var(--d-infra)"
/>

<TradeoffMatrix
  id="01-caching-write-strategy"
  lessonSlug="01-caching-strategies"
  lang="ru"
  prompt="Сервис оформления заказов кеширует цены товаров. Обновление цены должно отображаться в течение нескольких секунд, кеш должен переживать перезапуск Redis без выдачи неверных данных, а задержка записи должна оставаться ниже 50 мс. Какая стратегия записи подходит?"
  options={[
    { name: "Write-through", summary: "Обновляет кеш и БД синхронно — никогда не устаревает, но каждая запись платит два похода.", justification: "Write-through пишет кеш и базу данных за одну операцию записи перед возвратом вызывающему. Кеш всегда консистентен с БД по записям, так что обновление цены видно в рамках того же цикла запроса — хорошо в пределах секунд. При перезапуске узла Redis данные всё равно в базе, а кеш заполняется при следующем чтении. Цена — два синхронных похода на запись, примерно удваивающих задержку, но для типичных обновлений цен это остаётся далеко ниже 50 мс при нормальной БД. Это правильный выбор для данных с высокими требованиями корректности, таких как цены.", correct: true },
    { name: "Write-back (write-behind)", summary: "Минимальная задержка записи, но подтверждённые записи могут потеряться при сбое кеша до сброса.", justification: "Write-back пишет только в кеш и возвращается немедленно, сбрасывая в базу асинхронно. Это даёт минимальную задержку и поглощает всплески, но требование гласит, что кеш должен выживать при перезапуске Redis без выдачи неверных данных — write-back этого гарантировать не может. Записи, не успевшие сброситься, теряются при перезапуске узла. Write-back подходит для счётчиков и метрик, где потеря нескольких инкрементов допустима; не для цен, где потерянная или устаревшая запись означает неверную сумму оплаты." },
    { name: "Write-around", summary: "Записи минуют кеш полностью — гарантирует промах при чтении после каждого обновления цены.", justification: "Write-around пишет только в базу и оставляет кеш с прежним значением до истечения TTL или явной инвалидации. Кеш будет выдавать устаревшие цены всё время действия TTL — это нарушает требование «в течение секунд», если TTL не очень короткий (что сводит к минимуму ценность кеша). Write-around предназначен для данных с интенсивной записью, которые редко читаются сразу после записи, а не для цен, читаемых на каждой странице товара." },
    { name: "Cache-aside с TTL 5 секунд, без инвалидации", summary: "Просто — но устаревшие цены могут отдаваться до 5 секунд после каждого обновления.", justification: "Cache-aside с коротким TTL ограничивает устаревание, но не устраняет его: цена может быть неверной до 5 секунд при каждом обновлении. Для страницы оформления заказа, где цена определяет платёж, даже 5-секундная устаревшая цена — риск. Cache-aside это паттерн пути чтения; он ничего не говорит о том, как записи попадают в кеш. Чтобы надёжно обеспечить гарантию «в течение секунд», сочетай cache-aside на чтении с write-through (или явной инвалидацией при записи), а не с одним только TTL." },
  ]}
/>

<FlowDiagram
  label="Пять стратегий на двух путях. Верхний ряд — путь чтения: cache-aside даёт приложению грузить из БД при промахе, а read-through — самому кэшу. Нижний ряд — путь записи: write-through бьёт кэш и БД синхронно, write-back бьёт кэш сейчас и сбрасывает в БД позже, write-around минует кэш и пишет только БД."
  perRow={3}
  hue="var(--d-data)"
  nodes={[
    { id: "ca", label: "cache-aside", sub: "приложение грузит при промахе" },
    { id: "rt", label: "read-through", sub: "кэш грузит при промахе" },
    { id: "store", label: "база", sub: "источник истины" },
    { id: "wt", label: "write-through", sub: "кэш + БД синхронно" },
    { id: "wb", label: "write-back", sub: "кэш сейчас, БД позже" },
    { id: "wa", label: "write-around", sub: "только БД, мимо кэша" },
  ]}
  edges={[
    { from: "ca", to: "store", label: "промах → чтение" },
    { from: "rt", to: "store", label: "промах → чтение" },
    { from: "wt", to: "store", label: "синхронно" },
    { from: "wb", to: "store", label: "асинхр. сброс" },
    { from: "wa", to: "store", label: "запись" },
  ]}
  caption="Паттерны чтения (cache-aside, read-through) решают, кто грузит при промахе; паттерны записи (through, back, around) решают, что запись делает с кэшем. Ты комбинируешь по одному — например, cache-aside + write-around + TTL — рабочая лошадка."
/>

## Консистентность: что обещает и нарушает каждая стратегия

Весь смысл выбора в том, что каждая стратегия даёт своё обещание свежести:

- **Cache-aside / write-around / read-through** все допускают **устаревшие чтения** между изменением БД и следующим обновлением кэша — им нужен TTL или явная инвалидация, чтобы ограничить устаревание. Это частый, принятый компромисс: пара секунд устаревания за огромную пропускную способность чтения.
- **Write-through** — единственная, кто держит кэш **непрерывно консистентным с БД на записях** — но она *не* защищает от обновления, обходящего кэш (батч-задача, второй сервис, пишущий прямо в БД), что тихо возвращает устаревание.
- **Write-back** **неконсистентна с БД по дизайну** во время окна сброса и рискует **потерей данных** при сбое кэша — самая сильная производительность, самая слабая надёжность.

Senior-формулировка: нет варианта «свежо, быстро и надёжно»; ты выбираешь, чем из трёх пожертвовать, для каждого класса данных. Цены и балансы хотят write-through или явной инвалидации; список трендов норм на cache-aside с коротким TTL; счётчик просмотров — хрестоматийный write-back.

<Inset kind="mistake" lang="ru">
Дорогая ошибка — применять *одну* стратегию ко *всем* данным. Команды выбирают «cache-aside с TTL в час» глобально и потом обнаруживают, что баланс счёта пользователя кэшируется так же, как баннер на главной — и платёж всплывает с часовым опозданием. Кэширование — поклассовое: классифицируй каждую кэшируемую вещь по тому, сколько устаревания она терпит и насколько дорого неверное значение. Дорогие, нетерпимые данные (деньги, права, остаток на чекауте) получают write-through или активную инвалидацию; дешёвые, терпимые (рекомендации, счётчики, отрендеренные фрагменты) — cache-aside с TTL. Выбрать одну глобальную стратегию — значит выбрать неверную для большинства своих данных.
</Inset>

<Quiz
  id="05-caching-strategies-quiz-1"
  lessonSlug="01-caching-strategies"
  lang="ru"
  question="Сервис кэширует балансы счетов через cache-aside с TTL в час, без инвалидации. Пользователь пополняет баланс записью, идущей прямо в базу. Что покажет следующее чтение и почему?"
  choices={[
    { label: "Старый баланс, до часа — cache-aside обновляется только при промахе, а ключ никто не инвалидировал, так что устаревшее значение отдаётся до истечения TTL.", correct: true },
    { label: "Новый баланс сразу — кэш всегда отражает последнюю запись в БД.", misconception: "Cache-aside НЕ следит за базой. Запись, обошедшая кэш, оставляет старое значение в кэше до истечения или явной инвалидации. Этот зазор — допускаемое стратегией устаревшее чтение." },
    { label: "Ошибку, потому что кэш и БД расходятся.", misconception: "Ничего не падает — кэш спокойно отдаёт своё (устаревшее) значение, не зная, что БД изменилась. Опасность cache-aside без инвалидации — тихое устаревание, а не ошибка." },
    { label: "Новый баланс, потому что TTL форсирует обновление на каждом чтении.", misconception: "TTL вызывает перезагрузку только ПОСЛЕ истечения; внутри часа чтения — это попадания, отдающие кэшированное. Чтобы отразить запись раньше, инвалидируй ключ или используй write-through." },
  ]}
/>

<Quiz
  id="05-caching-strategies-quiz-2"
  lessonSlug="01-caching-strategies"
  lang="ru"
  question="Нужна минимально возможная задержка записи для высоконагруженного счётчика просмотров, и пара секунд потери данных при редком крахе кэша приемлема. Какая стратегия записи подходит и каков её риск?"
  choices={[
    { label: "Write-back: обнови кэш сейчас и сбрось в БД асинхронно батчами — минимальная задержка записи, склеивает всплески, ценой потери несброшенных записей при смерти узла кэша.", correct: true },
    { label: "Write-through: она самая быстрая, ведь пишет всё разом.", misconception: "Write-through — самый МЕДЛЕННЫЙ путь записи: два синхронных похода (кэш + БД) на каждой записи. Она самая консистентная, не самая быстрая. Для минимальной задержки записи нужен write-back." },
    { label: "Write-around: пропуск кэша делает записи мгновенными.", misconception: "Write-around пишет только БД, так что задержка записи — это задержка БД без выгоды кэша, и она гарантирует промах на следующем чтении. Быстрый кэшированный счётчик так не получить." },
    { label: "Cache-aside: это стратегия записи без риска надёжности.", misconception: "Cache-aside — паттерн ПУТИ ЧТЕНИЯ; про записи он ничего не говорит. Низколатентный лоссовый паттерн записи — write-back, чей риск как раз потеря несброшенных записей." },
  ]}
/>

<MetaphorComplete
  id="05-caching-strategies-metaphor-1"
  lessonSlug="01-caching-strategies"
  lang="ru"
  setup="В _______ кэшировании (AWS зовёт это lazy loading) приложение грузит значение из базы только при промахе чтения, затем сохраняет — поэтому кэшируются лишь запрошенные данные, а пустой узел просто дозаполняется, но значения устаревают, пока их не ограничит TTL или инвалидация."
  accepted={["cache-aside", "cache aside", "lazy loading", "lazy-loading"]}
  canonical="cache-aside"
  explanation="Cache-aside (lazy loading) кладёт логику загрузки-при-промахе в приложение: спроси кэш, при промахе прочти БД и заполни кэш. Кэшируются лишь запрошенные данные, переживаются пустые узлы, но поскольку за изменениями БД кэш не следит, допускаются устаревшие чтения до истечения TTL или инвалидации ключа."
/>

</Explanation>

<KeyTakeaway>Cache-aside/read-through грузят при промахе; write-through/back/around решают, что запись делает с кэшем. Комбинируй по одному на класс, жертвуя свежестью, промахом или надёжностью. Варианта «быстро-свежо-надёжно» нет.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="05-caching-strategies-retrieval"
  lang="ru"
  questions={[
    { q: "Сравни cache-aside и read-through — поток данных одинаков, что отличается?", a: "Оба грузят из БД при промахе и сохраняют результат; поток данных идентичен. Разница в том, кто владеет кодом: cache-aside кладёт логику загрузки-при-промахе в приложение (кэш остаётся тупым key-value ящиком, расцеплённым с БД, можно кэшировать вычисленные значения), а read-through толкает её в слой/провайдер кэша, чтобы все вызывающие вели себя одинаково. Cache-aside доминирует из-за расцепления — ценой дисциплины: каждая точка записи обязана инвалидировать или ставить TTL." },
    { q: "Сравни write-through, write-back и write-around на пути записи.", a: "Write-through пишет кэш и БД синхронно на каждой записи — никогда не устаревает, но два похода на запись и cache churn (ставь TTL). Write-back пишет кэш сейчас и сбрасывает в БД асинхронно — минимальная задержка записи, склеивает всплески, но теряет несброшенные записи при смерти кэша (для счётчиков/метрик, не денег). Write-around пишет только БД и пускает значение в кэш лениво при следующем чтении — без забивания данными с интенсивной записью, ценой гарантированного промаха на первом чтении после записи." },
    { q: "Какую свежесть/надёжность обещает каждая стратегия и как выбирать?", a: "Cache-aside/read-through/write-around все допускают устаревшие чтения, пока их не ограничит TTL или инвалидация. Write-through держит кэш консистентным с БД на записях (но не против записей, обходящих кэш). Write-back неконсистентна в окне сброса и рискует потерей данных. Варианта «быстро-свежо-надёжно» нет, поэтому выбор поклассовый: дорогие/нетерпимые к устареванию (деньги, права, остаток) → write-through или активная инвалидация; дешёвые/терпимые (рекомендации, счётчики, фрагменты) → cache-aside + короткий TTL." },
  ]}
/>

<Recap lang="ru">
Стратегия кэширования — это **схема разводки двух путей**, а не флажок. На пути чтения **cache-aside** (lazy loading) даёт приложению грузить из БД при промахе и заполнять кэш — кэшируются лишь запрошенные данные, переживаются пустые узлы, но допускаются устаревшие чтения; **read-through** переносит ту же логику загрузки-при-промахе в слой кэша ради консистентности ценой сцепления. На пути записи **write-through** пишет кэш и БД синхронно (никогда не устаревает, но два похода и churn — ставь TTL), **write-back** пишет кэш сейчас и сбрасывает БД позже (минимальная задержка, склеивает всплески, но теряет несброшенные записи при крахе), **write-around** пишет только БД и пускает значение в кэш лениво (без churn, но гарантированный промах на первом чтении). Каждая даёт своё обещание про **свежесть, цену промаха и надёжность**, и варианта «быстро-свежо-надёжно» разом нет — поэтому ты комбинируешь один паттерн чтения и один записи **на каждый класс данных**, никогда одну глобальную стратегию на всё. Ограничение устаревания, что допускают ленивые паттерны, — тема остального раздела: вытеснение и TTL, а затем по-настоящему трудная проблема инвалидации. Теперь, когда встретишь кэш-баг в проде, первый вопрос такой: какая стратегия была выбрана и было ли окно устаревания тем, на что команда реально соглашалась?
</Recap>
