---
slug: "quiz-choice"
lang: ru
track: "system-design"
unit: "05-caching-at-scale"
order: 5
title: "Кэширование при масштабе: обзор с выбором ответа"
summary: "Синтез всего раздела кэширования в формате с выбором ответа: стратегии чтения/записи и их консистентность, вытеснение и TTL, арифметика hit ratio, шардирование консистентным хешированием, стампида и анти-паттерн источника истины."
estMin: 13
status: ready
lessonType: topic
level: senior
concepts:
  - cache-aside
  - eviction-policy
  - hit-ratio
  - consistent-hashing
  - cache-stampede
  - cache-invalidation
prereqs:
  - 05-caching-at-scale/01-caching-strategies
  - 05-caching-at-scale/02-eviction-and-ttl
  - 05-caching-at-scale/03-distributed-cache
  - 05-caching-at-scale/04-cache-invalidation
sources:
  - https://docs.aws.amazon.com/AmazonElastiCache/latest/mem-ug/Strategies.html
  - https://redis.io/docs/latest/develop/reference/eviction/
  - https://aws.amazon.com/builders-library/caching-challenges-and-strategies/
---
import Hook from "~/components/lesson/Hook.astro";
import Crux from "~/components/prose/Crux.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Шесть вопросов через весь раздел. Каждый — решение, что ты принимаешь, добавляя кэш, ревьюя дизайн кэширования или отлаживая его в проде: не определение для пересказа, а компромисс консистентности, арифметика hit ratio и сбои, видимые лишь при масштабе.
</Hook>

<Crux>Шесть решений по разделу: обещание стратегии записи, ловушка вытеснения, математика hit ratio, выбор шардирования, стампида и анти-паттерн.</Crux>

<Explanation>

Убедись, что умеешь выбрать стратегию чтения/записи по её обещанию консистентности, выбрать политику вытеснения и TTL, превратить изменение hit ratio в нагрузку БД, шардировать кэш, не уничтожив его, распознать и починить стампиду и отвергнуть анти-паттерн кэш-как-источник-истины.

<Quiz
  id="05-caching-choice-1"
  lessonSlug="quiz-choice"
  lang="ru"
  question="Нужно, чтобы кэш никогда не устаревал относительно базы на записях, а задержка записи не важна. Какая стратегия записи подходит и какова её цена?"
  choices={[
    { label: "Write-through: пиши кэш и БД синхронно на каждой записи, так что кэш никогда не устаревает — ценой двух походов на запись и cache churn (ставь TTL, чтобы вытеснять непрочитанное).", correct: true },
    { label: "Write-back: держит кэш свежим, ведь пишет кэш первым.", misconception: "Write-back пишет кэш сейчас и сбрасывает БД ПОЗЖЕ, так что в окне сброса кэш и БД расходятся, а крах теряет несброшенные записи. Она наименее консистентна, не самая свежая. Синхронная свежесть — это write-through." },
    { label: "Write-around: запись только в БД держит кэш актуальным.", misconception: "Write-around полностью минует кэш на записи, так что кэшированное значение (если есть) устаревает и следующее чтение промахивается. Это противоположность поддержания свежести. Для никогда-не-устаревает-на-записи нужен write-through." },
    { label: "Cache-aside: она обновляет кэш на каждой записи.", misconception: "Cache-aside — паттерн ПУТИ ЧТЕНИЯ и не следит за БД на записях, так что допускает устаревшие чтения до TTL/инвалидации. Стратегия, держащая кэш свежим на каждой записи — это write-through." },
  ]}
/>

<Quiz
  id="05-caching-choice-2"
  lessonSlug="quiz-choice"
  lang="ru"
  question="Кэш на пределе памяти всё вытесняет главную (читаемую постоянно, вставленную при старте), сохраняя отчёт раз-в-день. Какая политика вытеснения настроена и какой должна быть?"
  choices={[
    { label: "Настроен FIFO — он вытесняет самую старую ВСТАВЛЕННУЮ запись независимо от использования, так что рано-вставленную главную выкидывает. Переключи на LRU (или LFU), что держит недавно/часто читаемую главную и вытесняет холодный отчёт.", correct: true },
    { label: "Настроен LRU; переключи на FIFO.", misconception: "Наоборот. LRU держит недавно-используемые элементы (главная бы выжила). Вытеснение постоянно-читаемого, но рано-вставленного ключа — подпись FIFO; чини переключением НА LRU/LFU, а не на FIFO." },
    { label: "Настроен random; переключи на noeviction.", misconception: "Паттерн (всегда рано-вставленный горячий ключ) — это FIFO, не random, а noeviction заставляет записи ПАДАТЬ под давлением вместо вытеснения — неверно для кэша. Используй LRU/LFU, чтобы доступ решал, что остаётся." },
    { label: "Кэш слишком мал; политика не важна.", misconception: "Размер важен, но конкретный симптом — вытеснение горячего ключа при сохранении холодного — это баг политики (FIFO игнорирует доступ). LRU/LFU держали бы главную даже при том же размере, вытесняя холодный отчёт." },
  ]}
/>

<Quiz
  id="05-caching-choice-3"
  lessonSlug="quiz-choice"
  lang="ru"
  question="Кэш перед 100 000 чтений/с падает с hit ratio 99% до 90% после деплоя. Во сколько раз растёт нагрузка чтения на базу?"
  choices={[
    { label: "В 10×: доля промахов идёт с 1% до 10%, так что чтения БД идут с ~1 000/с до ~10 000/с — база чувствует долю промахов, и она размерена в расчёте на ~99% попаданий.", correct: true },
    { label: "~в 1,1×: падение hit ratio на 9 пунктов — это ~9% изменения.", misconception: "Нагрузка БД масштабируется с долей ПРОМАХОВ, а не попаданий. Промахи идут 1% → 10%, скачок в 10× (1 000 → 10 000 чтений/с), хотя hit ratio сдвинулся лишь на 9 пунктов. Эта асимметрия — вся опасность." },
    { label: "Без изменений: кэш всё ещё отдаёт 90% чтений.", misconception: "Те 90% попаданий норм; важны другие 10% — теперь доходящие до БД вместо 1%. Это 10× прежнего объёма промахов на БД, размеренной под ~1% промахов, поэтому она плавится." },
    { label: "В 2×: промахи примерно удвоились.", misconception: "Промахи не удвоились — они пошли с 1% до 10%, рост в 10×, не в 2×. Считай долю промахов (100% − hit ratio) и фактор нагрузки: 10%/1% = 10×." },
  ]}
/>

<Quiz
  id="05-caching-choice-4"
  lessonSlug="quiz-choice"
  lang="ru"
  question="Ты шардируешь кэш и хочешь, чтобы добавление или удаление узла тревожило как можно меньше кэша. Какая схема и почему не очевидная?"
  choices={[
    { label: "Консистентное хеширование: смена узла ремаппит лишь ~1/N ключей (одну дугу кольца), так что шторм промахов ограничен. Модуло (hash % N) ремаппит почти каждый ключ при смене N, массово промахивая весь кэш и штурмуя БД.", correct: true },
    { label: "Модуло (hash % N): оно просто и ровно разносит ключи.", misconception: "Модуло разносит ровно, но катастрофично при смене топологии: когда N меняется, hash % N меняется почти для всех ключей, так что весь кэш ремаппится и промахивается разом. Консистентное хеширование ограничивает ремаппинг ~1/N (одной дугой)." },
    { label: "Range-партиционирование по префиксу ключа: оно держит связанные ключи вместе.", misconception: "Range-партиционирование помогает локальности, но не адресует проблему rescale и склонно создавать горячие диапазоны. Для кэша нужно консистентное хеширование, чтобы смена узла двигала лишь ~1/N ключей, а не ремаппила всё." },
    { label: "Только репликация: копии переживают потерю узла.", misconception: "Репликация про выживание, не про распределение ключей — она не решает, какой узел владеет ключом. Тебе всё равно нужна схема шардирования, и для кэша ею должно быть консистентное хеширование, чтобы ограничить ущерб rescale." },
  ]}
/>

<Quiz
  id="05-caching-choice-5"
  lessonSlug="quiz-choice"
  lang="ru"
  question="Один горячий ключ с TTL 60с отдаёт 50 000 RPS; каждые 60с базу бьёт всплеск тысяч идентичных запросов. Что это и самая прямая починка?"
  choices={[
    { label: "Стампида кэша (thundering herd): при истечении тысячи параллельных чтений промахиваются и пересчитывают одно значение разом. Коалесцируй их через single-flight, чтобы один запрос пересчитывал, пока остальные ждут результат.", correct: true },
    { label: "Нормальное поведение истечения; просто подними TTL.", misconception: "Длиннее TTL делает стампиду реже, а не исчезнувшей — она всё равно детонирует при каждом истечении. Структурная починка — коалесцирование запросов (single-flight), схлопывающее N параллельных промахов в один вызов БД." },
    { label: "Шторм вытеснения; подними maxmemory.", misconception: "Скачок периодичен ровно на границе TTL, что есть подпись стампиды при истечении, а не вытеснения от давления памяти. Коалесцируй параллельные пересчёты; больше памяти не адресует синхронный промах." },
    { label: "Ремаппинг консистентного хеширования; ребалансируй кольцо.", misconception: "В топологии ничего не менялось — период совпадает с TTL, так что это стампида при истечении горячего ключа. Чини через single-flight (или вероятностный ранний пересчёт), а не ребалансировкой кольца." },
  ]}
/>

<Quiz
  id="05-caching-choice-6"
  lessonSlug="quiz-choice"
  lang="ru"
  question="Дизайн пишет заказы только в Redis, подтверждает клиенту и сбрасывает в Postgres асинхронно, читая Redis как авторитетное значение. Каково ключевое возражение?"
  choices={[
    { label: "Это делает кэш источником истины: вытеснение/перезапуск Redis до сброса теряет подтверждённые заказы (кэш не надёжен), а любой прямой читатель БД видит устаревшие данные в лаге. Кэш должен быть одноразовой копией, сбрасываемой в любой момент с восстановлением корректных ответов.", correct: true },
    { label: "Это норм — это просто write-back кэширование.", misconception: "Write-back приемлем КАК КЭШ, но чтение его как АВТОРИТЕТНОГО значения и ACK до надёжности — анти-паттерн. Подтверждай лишь когда надёжное хранилище имеет запись, и держи кэш сбрасываемым." },
    { label: "Redis слишком медленный, чтобы быть авторитетным.", misconception: "Дело не в скорости — Redis быстр. Дело в надёжности и авторитетности: делать волатильный кэш источником истины теряет подтверждённые данные при крахе и оставляет прямых читателей БД устаревшими. Держи БД авторитетной." },
    { label: "Postgres не угонится за темпом сброса.", misconception: "Дело не в пропускной способности; в надёжности и консистентности. Дизайн кэш-как-источник-истины теряет данные при вытеснении/крахе и отдаёт расходящиеся значения читателям БД. Пиши надёжно сначала, потом кэшируй." },
  ]}
/>

</Explanation>

<KeyTakeaway>Кэширование — набор осознанных компромиссов: консистентность записи, учитывающее доступ вытеснение, доля промахов, что чувствует БД, консистентное хеширование, single-flight против стампиды, кэш как одноразовая копия.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="05-caching-choice-retrieval"
  lang="ru"
  questions={[
    { q: "Почему падение hit ratio 99%→90% плавит базу?", a: "База чувствует долю ПРОМАХОВ, а не попаданий. 99% попаданий — это 1% промахов; 90% попаданий — 10% промахов — рост в 10× чтений, доходящих до базы, размеренной в расчёте, что кэш поглощает ~99%. Так что падение hit ratio, выглядящее как 9 пунктов — это 10× всплеск нагрузки БД. Всегда рассуждай о промахах и следи за hit ratio, ведь кэш скрывает, насколько хрупко хранилище под ним." },
    { q: "Почему шардировать кэш консистентным хешированием, а не модуло, и как остановить стампиду горячего ключа?", a: "Модуло (hash % N) ремаппит почти каждый ключ при смене N, так что scale-up массово промахивает весь кэш и штурмует БД; консистентное хеширование двигает лишь ~1/N ключей (одну дугу кольца), а виртуальные узлы выравнивают нагрузку. Стампида (thundering herd) — это N параллельных чтений, пересчитывающих один истёкший горячий ключ разом — останови её коалесцированием/single-flight (один пересчёт, остальные ждут), вероятностным ранним пересчётом или локами." },
  ]}
/>

<Recap lang="ru">
Сквозная мысль: кэширование — набор **осознанных компромиссов**, а не переключатель. **Стратегия записи** фиксирует обещание консистентности: write-through никогда-не-устаревает-на-записи (два похода, churn), write-back быстрее всех, но лоссова и неконсистентна в окне сброса, write-around минует кэш (промах на первом чтении) — а read-path cache-aside допускает устаревшие чтения до TTL/инвалидации. **Вытеснение** должно учитывать доступ (LRU/LFU, не FIFO, никогда `noeviction` для кэша), а **TTL** ограничивает устаревание, но должен быть с джиттером. Число, что важно — это **hit ratio**, ведь база чувствует долю *промахов*: 99%→90% попаданий — это 10× всплеск нагрузки БД. Масштаб вширь требует **консистентного хеширования** (при rescale двигается лишь ~1/N ключей против массового ремаппинга модуло), переживает потерю узла **репликацией** (асинхронной, потому слегка устаревшей) и должен защищать **стампиду** через single-flight. А глубочайшее правило — что кэш это **одноразовая копия** авторитетного надёжного хранилища, никогда не источник истины. Прими эти шесть решений верно — и кэш делает чтения быстрыми, не делая систему хрупкой.
</Recap>
