---
slug: "04-cache-invalidation"
lang: ru
track: "system-design"
unit: "05-caching-at-scale"
order: 4
title: "Инвалидация кэша"
summary: "Держать кэш честным — по-настоящему трудная часть. TTL, явная инвалидация и версионированные ключи меняют устаревание на сложность, гонки на записи дают устаревшие чтения, а глубочайшая ловушка — считать кэш источником истины вместо одноразовой копии."
estMin: 20
status: ready
lessonType: topic
level: middle
concepts:
  - cache-invalidation
  - versioned-keys
  - write-time-races
  - negative-caching
  - stale-reads
  - source-of-truth
prereqs:
  - 05-caching-at-scale/01-caching-strategies
  - 05-caching-at-scale/02-eviction-and-ttl
  - 05-caching-at-scale/03-distributed-cache
sources:
  - https://aws.amazon.com/builders-library/caching-challenges-and-strategies/
  - https://redis.io/docs/latest/develop/use/patterns/
  - https://docs.aws.amazon.com/AmazonElastiCache/latest/mem-ug/Strategies.html
---
import Hook from "~/components/lesson/Hook.astro";
import Crux from "~/components/prose/Crux.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Inset from "~/components/lesson/Inset.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import MetaphorComplete from "~/components/pedagogy/MetaphorComplete.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
Команда выкатила явную инвалидацию: каждый раз при изменении базы они удаляли соответствующий ключ кэша, чтобы следующее чтение перезагрузило свежее. Чище, чем TTL, думали они. Неделями работало. Потом пользователь сообщил, что его *старое* фото профиля всё возвращается через минуты после смены. Причина — гонка: чтение промахнулось по ключу и начало грузить старое значение из базы ровно в тот миг, когда запись обновила базу и удалила ключ. Медленное чтение завершилось последним и записало *устаревшее* значение, что оно достало, обратно в теперь пустой кэш — заново отравив его на весь TTL. Ничего не сломалось; порядок просто не повезло. Вот почему «в computer science две трудные вещи: инвалидация кэша и именование» — это шутка, которую инженеры рассказывают, морщась.
</Hook>

<Crux>Инвалидация трудна: TTL, явное удаление или версии меняют устаревание на сложность. Гонки дают устаревание. Кэш — не источник истины.</Crux>

<Explanation>

## Почему инвалидация — трудная проблема

Каждый кэш держит *копию* данных, живущих где-то ещё. В момент изменения оригинала копия неверна — а кэш не в курсе, ведь (как показал урок про стратегии) ленивые кэши не следят за базой. **Инвалидация кэша** — это дисциплина вынуть неверную копию из кэша, вовремя, не сломав корректность и не молотя бэкенд. Она знаменито трудна не потому, что какая-то одна техника сложна, а потому, что сидит ровно там, где две независимые временны́е линии — твои записи и твои чтения — непредсказуемо переплетаются по распределённой системе. Есть три семейства подходов, и каждое меняет одни и те же две вещи: **устаревание** (насколько неверным может быть чтение?) против **сложности/сцепления** (сколько путь записи должен знать о кэше?).

## Три подхода

Прежде чем выбирать, спроси себя: насколько неверным может быть чтение и кто заплатит, если инвалидация пропустила точку? Ответ скажет, какой компромисс тебе подходит.

- **Истечение по TTL** — пусть каждая запись истекает через N секунд; путь записи *не делает ничего*. Это проще всего и максимально расцеплено: кэш самоисцеляется, писатели его не трогают. Цена — ограниченное устаревание: значение может быть неверным до TTL. Верный дефолт для данных, терпящих секунды-минуты лага (ленты, листинги, отрендеренные фрагменты).
- **Явная инвалидация** — на каждой записи активно удалять (или обновлять) затронутый ключ кэша, чтобы следующее чтение перезагрузило. Устаревание падает к нулю, но теперь **путь записи сцеплен с кэшем**: он должен знать каждый ключ, выведенный из изменённых данных, и единственная пропущенная точка инвалидации оставляет ключ устаревшим навсегда (до его TTL, если есть). В этом сцеплении живёт большинство багов кэша.
- **Версионированные ключи** — впеки версию в сам ключ: `user:42:v7`. Запись ничего не удаляет; она инкрементит версию (`v7 → v8`), так что чтения новой версии просто *промахиваются* по ключу, что никогда не записывался, и грузят свежее, а старые записи `v7` теперь сироты, со временем вытесняемые. Это полностью обходит гонку удаления (ты никогда не мутируешь существующий ключ) ценой оставленного мусора на вытеснение и нужды где-то отслеживать текущую версию.

Senior-формулировка зеркалит урок про стратегии: бесплатного варианта нет. TTL прост, но устаревает; явная свежа, но сцеплена и подвержена гонкам; версионирование без гонок, но оставляет сирот и нуждается в источнике версий. Выбираешь поклассово и обычно комбинируешь — явную инвалидацию *с* TTL-подстраховкой, чтобы пропущенная инвалидация самоисправилась, а не отравляла навсегда.

<Inset kind="why" lang="ru">
Почему явная инвалидация гоняется, и почему *версионирование* уходит от гонки? Гонка — классический переплёт чтения-записи: читатель промахивается по ключу K и начинает медленную загрузку *старого* значения БД; тем временем писатель обновляет БД и удаляет K. Если медленное чтение завершается *после* удаления, оно записывает старое значение обратно в пустой кэш — устаревшая запись, созданная корректным кодом в невезучем порядке (вступление). Удаление это не починит, ведь читатель уже держит устаревшее значение в полёте. Версионированные ключи уходят от этого, потому что писатель инкрементит до `v8`, а устаревшее значение читателя пишется в *старый* ключ `v7` — который ни одно новое чтение не запросит. Новая версия — свежий, никогда не записанный ключ, так что её первое чтение — чистый промах против текущего состояния БД. Ты сменил «мутируй общий ключ и надейся, что порядок добрый» на «пиши в новёхонький ключ на версию», у которого нет порядка, чтобы ошибиться.
</Inset>

## Устаревшие чтения, негативное кэширование и dogpile

Ещё три опасности сидят вокруг инвалидации:

- **Устаревшие чтения** — принятая цена TTL/ленивых стратегий, но они становятся *багом корректности*, когда класс данных их не терпит (баланс-показанный-с-часовым-опозданием из урока про стратегии). Починка не «никогда не устаревать» — а сопоставить бюджет устаревания данным: ограничь коротким TTL или плати цену сцепления явной инвалидации там, где это реально важно.
- **Негативное кэширование** — кэшировать *отсутствие* значения («не найдено»), чтобы повторные поиски отсутствующего ключа не молотили базу каждый раз. Жизненная защита от потока промахов по несуществующим ключам (например, атакующий запрашивает случайные ID), но у неё своя проблема инвалидации: когда сущность наконец *создаётся*, слишком долгий TTL негативного кэша её прячет. Негативным записям нужны короткие TTL или явная инвалидация при создании.
- **Dogpile** — стампида из урока про распределённый кэш сама по себе событие инвалидации: инвалидация (удаление) горячего ключа создаёт мгновенный зазор, который N параллельных читателей бросаются заполнить. Так что инвалидация и защита от стампиды сцеплены: агрессивное «удаляй на каждой записи» на горячем ключе *и есть* триггер стампиды — поэтому горячие ключи часто предпочитают обновление-на-месте или версионирование удалению.

<FlowDiagram
  label="Гонка на записи, дающая устаревшее чтение. Читатель промахивается по ключу K и начинает медленную загрузку старого значения базы. Параллельно писатель обновляет базу и удаляет K. Медленное чтение завершается последним и записывает старое значение, что достало, обратно в теперь пустой кэш, заново отравляя K устаревшими данными."
  perRow={3}
  hue="var(--d-data)"
  nodes={[
    { id: "miss", label: "читатель промах K", sub: "t0" },
    { id: "slowread", label: "грузит СТАРОЕ", sub: "из БД, медленно" },
    { id: "repoison", label: "пишет СТАРОЕ в кэш", sub: "t3 — снова устар." },
    { id: "write", label: "писатель обновл. БД", sub: "t1" },
    { id: "delete", label: "удаляет K", sub: "t2 — слишком рано" },
    { id: "version", label: "фикс: инкр. K:v+1", sub: "нет общего ключа для гонки" },
  ]}
  edges={[
    { from: "miss", to: "slowread" },
    { from: "slowread", to: "repoison", label: "завершается последним" },
    { from: "write", to: "delete" },
    { from: "delete", to: "version", label: "удаление не чинит" },
  ]}
  caption="Медленное чтение в полёте держит старое значение; удаление ключа не отзовёт его, так что оно записывается устаревшим. Версионированные ключи убирают общий мутируемый ключ, за который дрались две линии — устаревшая запись ложится на осиротевшую старую версию, что ни одно новое чтение не запросит."
/>

## Глубочайший анти-паттерн: кэш как источник истины

Самая опасная ошибка — не гонка; это категориальная ошибка. Кэш — это **одноразовая копия** авторитетных данных, живущих в надёжном хранилище. Считай его *источником истины* — пиши только в кэш и лениво сбрасывай, или предполагай «если оно в кэше, оно верно» — и ты построил систему, теряющую данные при перезапуске кэша (он не надёжен), отдающую что попало закэшировалось, даже когда БД не согласна, и неперестраиваемую с нуля, ведь никто не знает реального состояния. Каждая стратегия инвалидации выше предполагает, что база авторитетна, а кэш расходуем: ты должен иметь возможность **сбросить весь кэш в любой момент** и чтобы система восстановилась (медленно, через промахи) к *корректным* ответам. Если сброс кэша потерял бы данные или отдавал бы неверные результаты бесконечно — это не кэш, а ненадёжная primary-база, и ты унаследовал все её проблемы консистентности без единой её гарантии.

<Inset kind="mistake" lang="ru">
Конкретная версия ловушки источника-истины: использовать write-back кэширование (из урока про стратегии) и потом читать из кэша как из авторитетного, пока асинхронный сброс в БД лагает или тихо падает. Кэш показывает «верное» число, база — старее, и любой процесс, читающий БД напрямую (отчёт, второй сервис, бэкап), видит неверное значение — а если кэш вытеснит или упадёт до сброса, подтверждённая запись просто пропала. Правило, предотвращающее весь этот класс багов: записи подтверждаются только когда они в надёжном хранилище, а кэш считается слоем производительности, который можно сбросить в любой момент. Кэш делает чтения быстрыми; он не делает данные существующими.
</Inset>

<Quiz
  id="05-cache-invalidation-quiz-1"
  lessonSlug="04-cache-invalidation"
  lang="ru"
  question="С явной инвалидацией «удаляй ключ на каждой записи» устаревшее значение пользователя изредка возвращается сразу после обновления. Что происходит и какой подход убирает гонку?"
  choices={[
    { label: "Гонка на записи: параллельное медленное чтение загрузило старое значение до удаления, потом записало его обратно в пустой кэш после удаления. Версионированные ключи убирают гонку — инкременть версию, чтобы устаревшая запись читателя легла на осиротевший старый ключ, который новые чтения не запрашивают.", correct: true },
    { label: "Удаление не сработало; просто повтори удаление.", misconception: "Удаление сработало — проблема в медленном чтении, что уже держало СТАРОЕ значение и записало его обратно ПОСЛЕ удаления. Повтор удаления не помогает, ведь читатель заново отравляет ключ. Версионирование (или delete-after-write + двойное удаление) убирает общий мутируемый ключ, за который гонка." },
    { label: "TTL слишком длинный; поставь ноль.", misconception: "TTL в ноль полностью побеждает кэш (каждое чтение промахивается). Баг не в длине TTL — а в параллельном чтении, пишущем устаревшее значение обратно после инвалидирующего удаления. Используй версионированные ключи, чтобы не было общего ключа для гонки." },
    { label: "Кэшировать эти данные невозможно.", misconception: "Они очень кэшируемы — нужна лишь схема инвалидации без гонки удаления. Версионированные ключи (инкремент версии на записи) дают инвалидацию без гонок, ведь устаревшая запись ложится на старую, теперь осиротевшую версию, что ни одно новое чтение не спросит." },
  ]}
/>

<Quiz
  id="05-cache-invalidation-quiz-2"
  lessonSlug="04-cache-invalidation"
  lang="ru"
  question="Архитектор предлагает писать все обновления только в Redis и сбрасывать в Postgres асинхронно, затем читать из Redis как авторитетное значение. Каково ключевое возражение?"
  choices={[
    { label: "Это делает кэш источником истины: перезапуск Redis теряет несброшенные записи (он не надёжен), а любой читатель Postgres видит устаревшие данные в лаге сброса. Кэш должен быть одноразовой копией, которую можно сбросить в любой момент и всё равно восстановить корректные ответы.", correct: true },
    { label: "Redis слишком медленный, чтобы быть источником истины.", misconception: "Возражение не в скорости — Redis быстр. Возражение в надёжности и авторитетности: кэш — расходуемая копия, так что делать его источником истины значит, что перезапуск теряет подтверждённые записи, а другие читатели БД видят неверные значения. БД должна оставаться авторитетной." },
    { label: "Ничего плохого; это просто write-back кэширование.", misconception: "Write-back кэширование норм КАК КЭШ, но чтение из него как из АВТОРИТЕТНОГО значения — анти-паттерн. Подтверждай записи только когда надёжны, и считай кэш сбрасываемым. Иначе крах до сброса теряет данные без всякой их записи." },
    { label: "Postgres не угонится за записями Redis.", misconception: "Дело не в пропускной способности; в надёжности и авторитетности. Если кэш — источник истины, сброс или крах теряет данные, а прямые читатели БД устаревают. Починка — держать надёжное хранилище авторитетным, а кэш одноразовым." },
  ]}
/>

<MetaphorComplete
  id="05-cache-invalidation-metaphor-1"
  lessonSlug="04-cache-invalidation"
  lang="ru"
  setup="Чтобы инвалидировать без гонки удаления, впеки версию в ключ (user:42:v7) и инкременть её на каждой записи; это работает, потому что устаревшая запись читателя ложится на теперь осиротевшую старую _______, которую ни одно новое чтение не запросит — нет общего мутируемого ключа, за который дрались бы две линии."
  accepted={["версию", "версия", "версия ключа", "версионированный ключ", "старую версию"]}
  canonical="версию"
  explanation="Версионированные ключи превращают каждую запись в свежий, никогда не записанный ключ (v7 → v8). Параллельное медленное чтение может записать своё устаревшее значение лишь в старую версию v7, которую новые чтения не спрашивают, так что это безвредный мусор на вытеснение. Новая версия — чистый промах против текущего состояния БД: без гонок, ценой осиротевших старых версий и нужды где-то хранить текущий номер версии."
/>

</Explanation>

<KeyTakeaway>Инвалидация меняет устаревание на сцепление: TTL (просто, устаревает), явное удаление (свежо, сцеплено, гонки), версии (без гонок, оставляют сирот). Комбинируй явную + TTL. Кэш — одноразовая копия, не источник истины.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="05-cache-invalidation-retrieval"
  lang="ru"
  questions={[
    { q: "Почему инвалидация кэша трудна и что меняют три подхода?", a: "Кэш держит копию данных, живущих где-то ещё; при изменении оригинала копия тихо неверна, а вынуть её вовремя сквозь переплетающиеся линии чтения/записи — трудная часть. Три подхода меняют устаревание на сложность/сцепление: TTL (путь записи ничего не делает, самоисцеляется, но ограниченное устаревание), явная инвалидация (удаление на записи, почти нулевое устаревание, но путь записи должен знать каждый выведенный ключ — пропущенная точка оставляет ключ устаревшим навсегда), версионированные ключи (инкремент версии на записи, без гонок, но оставляет сирот и нуждается в источнике версий). Бесплатного варианта нет, поэтому выбираешь поклассово и обычно комбинируешь явную + TTL-подстраховку." },
    { q: "Объясни гонку на записи и почему версионированные ключи её избегают.", a: "Читатель промахивается по ключу K и начинает медленную загрузку СТАРОГО значения БД; параллельно писатель обновляет БД и удаляет K. Если медленное чтение завершается после удаления, оно пишет старое значение обратно в пустой кэш, заново отравляя K на его TTL — устаревшая запись от корректного кода в невезучем порядке. Удаление не чинит, ведь читатель уже держит устаревшее значение в полёте. Версионированные ключи избегают: писатель инкрементит до v8, так что устаревшая запись читателя ложится на осиротевший v7, что новые чтения не спрашивают, а новый v8 — чистый промах против текущего состояния БД." },
    { q: "Что такое негативное кэширование, связь dogpile-как-инвалидации и анти-паттерн источника истины?", a: "Негативное кэширование хранит отсутствие значения, чтобы повторные поиски отсутствующего ключа не били в БД (защита от потоков случайных ID) — но слишком долгий негативный TTL прячет значение, как только оно создано, поэтому ставь короткие TTL или инвалидируй при создании. Связь dogpile: инвалидация горячего ключа создаёт мгновенный зазор, который N читателей бросаются заполнить — стампида — поэтому горячие ключи часто предпочитают обновление-на-месте/версионирование удалению. Анти-паттерн источника истины: кэш — одноразовая копия надёжного хранилища; если запись только в кэш (или чтение его как авторитетного) значит, что сброс/перезапуск теряет данные или отдаёт неверное — это не кэш, а ненадёжная primary-БД. Ты должен мочь сбросить весь кэш в любой момент и восстановить корректные ответы." },
  ]}
/>

<Recap lang="ru">
Инвалидация кэша — по-настоящему трудная часть кэширования: не потому, что какая-то одна техника сложна, а потому, что она живёт там, где твои **линии чтения и записи переплетаются** по распределённой системе, а кэш тихо держит копию, что становится неверной в момент изменения оригинала. Три подхода меняют **устаревание на сложность/сцепление**: **TTL** (путь записи ничего не делает, самоисцеляется, но ограниченно устаревает), **явная инвалидация** (удаление на записи, почти свежо, но путь записи сцеплен с каждым выведенным ключом и пропущенная точка устаревает навсегда), **версионированные ключи** (инкремент версии на записи, без гонок, но оставляет сирот). **Гонка на записи** — медленное чтение, пишущее старое значение обратно *после* инвалидирующего удаления — и есть причина, почему версионирование, никогда не мутирующее общий ключ, бьёт удаление; практический дефолт — явная инвалидация *с TTL-подстраховкой*. Вокруг сидят **устаревшие чтения** (сопоставь бюджет классу данных), **негативное кэширование** (кэшируй отсутствие, но с короткими TTL, чтобы создания не прятались) и **dogpile** (инвалидация горячего ключа *и есть* триггер стампиды). Глубочайшая ловушка — категориальная ошибка считать кэш **источником истины**: кэш — одноразовая копия авторитетного надёжного хранилища, и ты должен мочь сбросить его целиком в любой момент и восстановить корректные ответы — иначе ты построил ненадёжную базу в одежде кэша. На этом раздел про кэширование завершён: стратегия, вытеснение/TTL, распределение и честность копии. Теперь, когда встретишь в ревью системы записи, подтверждаемые до попадания в надёжное хранилище, задай один вопрос: если кэш перезапустится прямо сейчас — пропадут ли данные? Если да — перед тобой ненадёжная база в одежде кэша.
</Recap>
