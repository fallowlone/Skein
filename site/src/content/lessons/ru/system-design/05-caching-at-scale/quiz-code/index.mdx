---
slug: "quiz-code"
lang: ru
track: "system-design"
unit: "05-caching-at-scale"
order: 7
title: "Кэширование при масштабе: чтение кода и конфига"
summary: "Читай реальный код и конфиг кэширования, затем рассуждай о поведении: гонка устаревшего чтения, расчёт от доли промахов к нагрузке БД, misconfiguration noeviction и стампида горячего ключа в пути чтения."
estMin: 14
status: ready
lessonType: topic
level: senior
concepts:
  - cache-aside
  - hit-ratio
  - cache-stampede
  - maxmemory-policy
  - cache-invalidation
  - request-coalescing
prereqs:
  - 05-caching-at-scale/01-caching-strategies
  - 05-caching-at-scale/02-eviction-and-ttl
  - 05-caching-at-scale/03-distributed-cache
  - 05-caching-at-scale/04-cache-invalidation
sources:
  - https://docs.aws.amazon.com/AmazonElastiCache/latest/mem-ug/Strategies.html
  - https://redis.io/docs/latest/develop/reference/eviction/
  - https://aws.amazon.com/builders-library/caching-challenges-and-strategies/
---
import Hook from "~/components/lesson/Hook.astro";
import Crux from "~/components/prose/Crux.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Баги кэширования живут в коде и конфиге, не в прозе: пропущенная инвалидация, синхронный путь промаха, дефолтная политика вытеснения, чтение горячего ключа без коалесцирования. Читай каждый сниппет, рассуждай, что он делает под нагрузкой, и выбирай ответ, на который подписался бы senior-инженер.
</Hook>

<Crux>Читай код, найди стратегию и сбой, что она зовёт, затем выбери починку, что поведение реально поддерживает — а не ту, что его прячет.</Crux>

<Explanation>

Практикуй цикл, что прогоняешь на ревью дизайна кэширования или инцидента: найди стратегию в коде, предскажи её поведение под конкурентностью и нагрузкой и выбери изменение, что механика реально поддерживает.

## Сниппет 1 — порядок инвалидации

```python
# чтение cache-aside + инвалидация «удаляй на записи»
def get_user(uid):
    u = cache.get(f"user:{uid}")
    if u is None:                       # промах
        u = db.read_user(uid)           # медленная загрузка ТЕКУЩЕГО значения
        cache.set(f"user:{uid}", u)     # заполнить (без TTL)
    return u

def update_user(uid, patch):
    db.write_user(uid, patch)           # новое значение в БД
    cache.delete(f"user:{uid}")         # инвалидировать
```

<Quiz
  id="05-caching-code-1"
  lessonSlug="quiz-code"
  lang="ru"
  question="Под конкурентностью устаревшее значение пользователя изредка переживает обновление навсегда. В чём баг и самая надёжная починка?"
  choices={[
    { label: "Гонка на записи: параллельный читатель может загрузить СТАРОЕ значение до delete из update_user, потом записать его обратно ПОСЛЕ delete — а без TTL оно живёт вечно. Используй версионированные ключи (или хотя бы добавь TTL-подстраховку), чтобы устаревшая запись не отравила живой ключ.", correct: true },
    { label: "Ничего плохого; удаление-на-записи корректно.", misconception: "Оно корректно в изоляции, но гоняется под конкурентностью: медленный читатель, держащий старое значение, может записать его после delete, и без TTL оно никогда не самоисправится. Версионированные ключи убирают общий мутируемый ключ; TTL-подстраховка хотя бы ограничивает ущерб." },
    { label: "Чтение не должно заполнять кэш.", misconception: "Заполнение при промахе — нормальный cache-aside. Баг в переплёте set медленного чтения с delete записи, плюс нет TTL для восстановления. Чини гонку (версионированные ключи) и добавь TTL-подстраховку, а не шаг заполнения." },
    { label: "Добавь больше узлов кэша.", misconception: "Больше узлов не чинит логическую гонку на одном ключе — устаревший set-после-delete случается независимо от числа узлов. Используй версионированные ключи, чтобы устаревшая запись легла на осиротевшую старую версию, и добавь TTL, чтобы пропущенная инвалидация самоисцелилась." },
  ]}
/>

## Сниппет 2 — математика hit ratio

```python
def db_reads_per_sec(total_rps: float, hit_ratio: float) -> float:
    miss_rate = 1.0 - hit_ratio
    return total_rps * miss_rate

before = db_reads_per_sec(80_000, 0.99)   # ?
after  = db_reads_per_sec(80_000, 0.90)   # ?
```

<Quiz
  id="05-caching-code-2"
  lessonSlug="quiz-code"
  lang="ru"
  question="Что возвращают before и after и каков вывод для базы?"
  choices={[
    { label: "before = 800/с (1% от 80к), after = 8 000/с (10% от 80к) — рост в 10× нагрузки БД от падения hit ratio, выглядящего как лишь 9 пунктов. База чувствует долю промахов, и она размерена под ~1%.", correct: true },
    { label: "before ≈ 79 200, after ≈ 72 000 — большинство чтений бьёт в БД.", misconception: "Ты посчитал чтения, отданные из КЭША (hit_ratio × total), а не промахи, доходящие до БД. db_reads = total × (1 − hit_ratio): 800, затем 8 000. БД видит промахи, что подскочили в 10×." },
    { label: "before = 800, after = 8 000, но падение на 9 пунктов незначительно.", misconception: "Числа верны, но вывод неверен: 800 → 8 000 это рост нагрузки в 10×, не незначительный. Изменение hit ratio выглядит малым именно потому, что опасность живёт на стороне промахов, что умножилась вдесятеро." },
    { label: "Оба ~0; кэш поглощает всё.", misconception: "Поглощаются лишь попадания; промахи проваливаются. При 99% всё равно 800 промахов/с, а при 90% — 8 000, скачок в 10×. Считай total × (1 − hit_ratio), чтобы увидеть, что БД реально чувствует." },
  ]}
/>

## Сниппет 3 — конфиг кэша Redis

```ini
# инстанс redis, используемый чисто как кэш
maxmemory 16gb
maxmemory-policy noeviction        # оставлен дефолт
# приложение делает SET ... EX 600 для каждого ключа
```

<Quiz
  id="05-caching-code-3"
  lessonSlug="quiz-code"
  lang="ru"
  question="Что происходит, когда этот кэш заполняет свои 16ГБ, и какой должна быть политика?"
  choices={[
    { label: "С noeviction записи (SET) начинают ПАДАТЬ, как только память полна, вместо вытеснения холодных ключей — неверно для кэша. Ставь allkeys-lru (или allkeys-lfu), чтобы любой ключ мог быть вытеснен и кэш продолжал отдавать.", correct: true },
    { label: "noeviction безопаснее всего — он защищает твои данные.", misconception: "noeviction защищает надёжное хранилище отказом от записей под давлением, но кэш расходуем: ты ХОЧЕШЬ, чтобы он сбрасывал холодные ключи и продолжал отдавать. Падающие SET под нагрузкой — это сбой. Используй allkeys-lru/lfu для кэша." },
    { label: "Он тихо перезаписывает самые старые ключи; менять не нужно.", misconception: "noeviction НЕ перезаписывает ничего — он отклоняет новые записи с ошибкой, когда полон. Чтобы освободить место сбросом холодных ключей, надо задать политику вытеснения вроде allkeys-lru/lfu." },
    { label: "Переключи на volatile-lru для безопасности.", misconception: "volatile-lru вытесняет лишь ключи с TTL; любой ключ без TTL невытесняем и тихо забивает память, пока вытеснение не остановится. Раз ты хочешь любой ключ вытесняемым для чистого кэша, используй allkeys-lru/lfu." },
  ]}
/>

## Сниппет 4 — путь чтения горячего ключа

```python
# страница товара вирусного айтема, пересчёт ~300мс
def get_page(pid):
    html = cache.get(f"page:{pid}")
    if html is None:                    # промах
        html = render_page(pid)         # дорого: 6 запросов + рендер
        cache.set(f"page:{pid}", html, ex=30)
    return html
# на пике ~40 000 параллельных запросов на ОДИН pid
```

<Quiz
  id="05-caching-code-4"
  lessonSlug="quiz-code"
  lang="ru"
  question="Каждые 30 секунд бэкенд бьют тысячи одновременных вызовов render_page на один pid. В чём сбой и починка?"
  choices={[
    { label: "Стампида кэша: когда page:{pid} истекает, все ~40 000 параллельных читателей промахиваются и каждый разом зовёт render_page. Добавь коалесцирование/single-flight, чтобы один запрос пересчитывал, пока остальные ждут этот результат (опционально вероятностный ранний пересчёт, чтобы обновлять до истечения).", correct: true },
    { label: "Просто подними ex до 300; проблема уйдёт.", misconception: "Длиннее TTL лишь делает стампиду каждые 300с вместо 30с — она всё равно детонирует при истечении тысячами одновременных рендеров. Коалесцируй промахи (single-flight), чтобы параллельные читатели делили один пересчёт." },
    { label: "render_page слишком медленный; оптимизируй до 30мс.", misconception: "Быстрее рендер уменьшает каждую стампиду, но не останавливает тысячи одновременных пересчётов одного значения. Структурная починка — коалесцирование параллельных промахов в один пересчёт, как бы быстр ни был render_page." },
    { label: "Шардируй кэш, чтобы ключ разнёсся.", misconception: "Один ключ нельзя разбить по шардам — шардирование не помогает горячему ключу. Периодический скачок при истечении — это стампида; чини через single-flight (и подумай о near-кэше/репликации горячего ключа для постоянной нагрузки)." },
  ]}
/>

</Explanation>

<KeyTakeaway>Каждое решение всплывает в коде: порядок инвалидации с гонкой, доля промахов, множащая нагрузку БД в 10×, noeviction, валящий записи, горячий ключ без коалесцирования. Читай механику, бери структурную починку.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="05-caching-code-retrieval"
  lang="ru"
  questions={[
    { q: "Почему удаление-на-записи гоняется и какова надёжная починка?", a: "Параллельный читатель может загрузить СТАРОЕ значение, потом записать его в кэш ПОСЛЕ delete писателя, заново отравив ключ — а без TTL оно живёт вечно. Надёжная починка — версионированные ключи (инкремент версии на записи, чтобы устаревший set лёг на осиротевшую старую версию, что новые чтения не спрашивают); как минимум добавь TTL-подстраховку, чтобы пропущенная/потерянная инвалидация самоисцелилась за TTL." },
    { q: "Как из hit ratio посчитать нагрузку БД и почему асимметрия опасна?", a: "Чтения БД = total_rps × (1 − hit_ratio). При 80 000 RPS, 99% попаданий → 800 промахов/с; 90% попаданий → 8 000 промахов/с — скачок в 10× от падения hit ratio на 9 пунктов. Опасно, потому что БД размерена в расчёте на ~99% попаданий, так что мелкая на вид регрессия hit ratio множит её нагрузку вдесятеро; рассуждай о стороне промахов, а не попаданий." },
    { q: "Почему noeviction неверен для кэша и как остановить стампиду горячего ключа в пути чтения?", a: "noeviction заставляет Redis отклонять записи, когда память полна, вместо сброса холодных ключей — верно для надёжного хранилища, сбой для кэша; используй allkeys-lru/lfu, чтобы любой ключ был вытесняем. Стампида горячего ключа случается, когда дорогой ключ истекает и тысячи параллельных читателей пересчитывают его разом; останови коалесцированием/single-flight (один пересчёт, остальные ждут результат), опционально вероятностным ранним пересчётом, чтобы горячее значение обновлялось до того, как отсутствует." },
  ]}
/>

<Recap lang="ru">
Каждое решение кэширования в этом разделе всплывает прямо в коде и конфиге. **Инвалидация удалением-на-записи** гоняется под конкурентностью — медленный читатель записывает старое значение обратно после delete, и без TTL оно не восстанавливается — поэтому используешь версионированные ключи (и TTL-подстраховку). **Математика доли промахов** (`чтения БД = total × (1 − hit_ratio)`) показывает, почему 99%→90% попаданий это 10× всплеск нагрузки БД: база чувствует сторону промахов, размеренная под ~1%. **Дефолт `noeviction`** заставляет кэш Redis валить записи под давлением памяти вместо сброса холодных ключей, так что чистый кэш хочет `allkeys-lru/lfu`. А **путь чтения горячего ключа** без коалесцирования штурмует бэкенд при каждом истечении — тысячи одновременных пересчётов одного значения — чинится single-flight (один пересчёт, остальные ждут) и опционально вероятностным ранним пересчётом. Senior-привычка — читать механику, предсказывать поведение под нагрузкой и конкурентностью и выбирать структурную починку, что код поддерживает — а не ту (бо́льший TTL, больше узлов, быстрее рендер), что лишь её замазывает.
</Recap>
