---
slug: "01-what-is-system-design"
lang: ru
track: "system-design"
unit: "00-start-here"
order: 1
title: "Что такое system design"
summary: "System design — не зубрёжка известных архитектур, а дисциплина рассуждения от требований к узким местам и к компромиссам в условиях масштаба и отказов. Правильный ответ редко один — есть только защитимые. Этому кодинг уже не учит."
estMin: 16
status: ready
lessonType: topic
level: middle
concepts:
  - system-design
  - requirements
  - bottlenecks
  - tradeoffs
  - scale-and-failure
  - defensible-decisions
prereqs: []
sources:
  - https://github.com/donnemartin/system-design-primer
  - https://www.allthingsdistributed.com/2006/03/a_word_on_scalability.html
---
import Hook from "~/components/lesson/Hook.astro";
import Crux from "~/components/prose/Crux.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Inset from "~/components/lesson/Inset.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import MetaphorComplete from "~/components/pedagogy/MetaphorComplete.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";
import Infographic from "~/components/diagram/Infographic.astro";
import requirementToDecision from "./infographics/requirement-to-decision.svg?raw";

<Hook>
Двух инженеров просят «сделать сокращатель ссылок». Первый открывает редактор и пишет маршрут: хеширует длинный URL, сохраняет пару в таблицу и редиректит по выборке. На ноутбуке работает за двадцать минут. Второй спрашивает: сколько записей в секунду, сколько чтений, насколько коротким должен быть код, что будет, если два запроса дадут одинаковый хеш, что будет, когда база упадёт в три ночи? Первый написал код. Второй занимался system design — и только его версия переживёт контакт с миллионом пользователей. Зазор между этими двумя ответами — и есть весь предмет этого трека. К концу урока ты поймёшь, откуда этот зазор берётся и как начать его закрывать.
</Hook>

<Crux>System design — рассуждение от требований к узким местам и компромиссам под масштабом и отказами; ответ редко один, есть лишь защитимые.</Crux>

<Explanation>

## Это не зубрёжка

Когда садишься на интервью по system design — или пишешь реальный дизайн-документ — самая дорогая ошибка: считать это упражнением на запоминание. Распространённое заблуждение: system design — это запомнить, как устроены Twitter или Netflix, и пересказать диаграмму. Это противоположность навыка. Реальные архитектуры — ответы на *конкретные* ограничения: определённое соотношение чтений и записей, определённый бюджет задержки, определённая команда и бюджет; копируя ответ без вопроса, получаешь карго-культовый дизайн, который не подходит никому. System design — не свод фактов для заучивания; это **способ рассуждать**, применимый к задаче, которой ты раньше не видел.

Рассуждение идёт в одну сторону, и порядок важен:

1. **Требования** — что система обязана делать и насколько хорошо? (функциональные и нефункциональные)
2. **Узкие места** — при этих требованиях и заявленном масштабе какой ресурс сдаст первым?
3. **Компромиссы** — чем платишь (консистентность, простота, стоимость, задержка), чтобы снять это узкое место?

Пропустишь шаг 1 — оптимизируешь не то. Пропустишь шаг 2 — масштабируешь часть, которая и не собиралась ломаться. Шаг 3 — где видна сеньорность: бесплатных побед почти не бывает, есть лишь выбранная цена.

## Кодинг против проектирования

Кодинг спрашивает: «выдаёт ли эта функция правильный результат?» — вопрос с бинарным ответом, который проверяется юнит-тестом. Проектирование спрашивает: «выдержит ли эта *система* нагрузку, которой не видела, отказы, которые ты не предскажешь, и рост, который не остановишь?» — вопрос, на который ни один тест не вернёт зелёный. Функция либо корректна, либо нет. Дизайн же всегда лишь *защитим* или нет: он явно проговаривает свои допущения, называет нагрузку, на которую рассчитан, и говорит, чем и почему жертвует.

Поэтому «у меня на ноутбуке работает» — это начало разговора, а не конец. У ноутбука один пользователь, нет конкурентности, нет сетевого разделения, нет диска, что заполняется в полночь. Проектирование — это практика рассуждать обо всём, что ноутбук от тебя спрятал. Поймаешь себя на мысли «тесты зелёные — готово»: спроси себя — готово при каком масштабе, при каком отказе, для каких пользователей?

<Inset kind="why" lang="ru">
Почему «правильный ответ редко один»? Потому что каждый значимый выбор проектирования меняет одно желательное свойство на другое, и какой обмен *правилен* — зависит целиком от выданных тебе требований, а не от технологии. Сильная консистентность или низкая задержка? Больше денормализации (быстрые чтения, болезненные записи) или строгая нормализация (чистые записи, медленнее чтения)? Монолит (просто, один деплой, один домен отказа) или сервисы (независимое масштабирование, налог распределённых систем)? Ни у чего из этого нет универсально правильной стороны. Сеньорный ход — не «знать ответ», а вытащить компромисс вслух, привязать его к заявленному требованию и выбрать сторону, которой это требование требует. Junior говорит «возьмём Postgres». Сеньор говорит «Postgres, потому что нужны транзакции, и наши 5k записей/с влезают в один primary — если записи вырастут в 10×, вернёмся к шардированию».
</Inset>

## Конкретный мини-пример

Возьмём сокращатель. Интервью по требованиям выявляет: 100 чтений на 1 запись, редиректы должны ощущаться мгновенно (p99 — 99-й перцентиль задержки — ниже 50 мс), коды короткие, и сбой редиректа куда хуже сбоя создания. *Теперь* дизайн пишется сам из требований, а не из туториала:

- 100:1 с перевесом чтений → поставь кэш перед чтениями; рабочий набор горячих ссылок живёт в памяти.
- «мгновенный редирект» → путь чтения должен избегать медленной выборки; предвычисляй и кэшируй, не вычисляй на горячем пути.
- «короткие коды» → не хешируй (коллизии, длина); выдавай счётчик и кодируй его в base-62.
- «сбой редиректа — худшее» → сделай путь чтения самым реплицируемым, самым кэшируемым, наименее хрупким компонентом, а путь записи пусть деградирует первым.

<Infographic
  svg={requirementToDecision}
  label="Двухколоночное сравнение: каждое заявленное требование сокращателя ссылок сопоставлено с решением дизайна, которое оно диктует — 100 чтений на 1 запись ведут к кэшу перед чтениями; мгновенный редирект (p99 ниже 50 мс) ведёт к предвычислению и отказу от вычислений на горячем пути; короткие коды ведут к счётчику плюс base-62 вместо хеширования; «сбой редиректа — худшее» ведёт к тому, чтобы путь чтения был самым реплицируемым компонентом, а путь записи деградировал первым."
  caption="Четыре требования слева диктуют решение справа — дизайн считывается из требований, а не вспоминается из туториала."
  hue="var(--d-infra)"
/>

<FlowDiagram
  label="Поток рассуждения в system design: стартуй от функциональных и нефункциональных требований, выведи масштаб, найди ресурс, что сдаст первым, затем выбери компромиссы, чтобы его снять — и возвращайся, когда требования меняются."
  perRow={5}
  hue="var(--d-infra)"
  nodes={[
    { id: "req", label: "требования", sub: "функц. + NFR" },
    { id: "scale", label: "масштаб", sub: "чтения/записи, p99" },
    { id: "bottleneck", label: "узкое место", sub: "что сдаст первым?" },
    { id: "tradeoff", label: "компромиссы", sub: "выбранная цена" },
    { id: "design", label: "защитимый дизайн", sub: "допущения названы" },
  ]}
  edges={[
    { from: "req", to: "scale" },
    { from: "scale", to: "bottleneck" },
    { from: "bottleneck", to: "tradeoff" },
    { from: "tradeoff", to: "design" },
    { from: "design", to: "req", label: "требования меняются → пересмотр" },
  ]}
  caption="Тот же сокращатель при требовании с перевесом записей или сильной консистентности дал бы другой дизайн. Поток фиксирован; ответ — нет. Поэтому мы рассуждаем, а не зубрим."
/>

Суть не в том, что это *тот самый* дизайн сокращателя. Суть в том, что другой набор требований — скажем, ингест аналитики с перевесом записей или коды, которые нельзя угадать, — переворачивает половину этих решений. Поток остался тем же; ответ изменился, потому что изменились требования. Это и есть system design.

## Что покрывает этот трек

Это трек **основ**: несущие концепции, которые ты переиспользуешь в любом дизайне, независимо от конкретного продукта. Ранние юниты строят словарь и физику — масштабируемость (задержка, пропускная способность, оси масштабирования, числа, которые должен знать каждый инженер), затем распределение данных, кэширование, консистентность, коммуникация, надёжность и остальное. Поздний юнит (юнит 09) берёт расплывчатое рассуждение выше и закаляет его в явную, повторяемую **рамку проектирования** — чек-лист, по которому ты идёшь под давлением. Отдельный трек **кейсов** затем тратит этот словарь на полные сквозные дизайны (сокращатель, лента, ограничитель частоты, чат), где ты видишь, как основы складываются в реальные архитектуры.

<Quiz
  id="01-what-is-system-design-quiz-1"
  lessonSlug="01-what-is-system-design"
  lang="ru"
  question="Интервьюер просит спроектировать сервис обмена фото. Какой первый ход — это ход system design, а не ход кодинга?"
  choices={[
    { label: "Спросить соотношение чтений/записей, цели по задержке и масштаб до того, как что-то рисовать — вывести дизайн из требований.", correct: true },
    { label: "Сразу набросать ту самую архитектуру, которой, как известно, пользуется Instagram.", misconception: "Пересказ известной архитектуры — ловушка зубрёжки. Тот дизайн отвечает на конкретные ограничения Instagram, а не на те, что тебе только что выдали. Стартуй от требований, а не от запомненной диаграммы." },
    { label: "Сразу писать код эндпоинта загрузки, чтобы на доске было что-то конкретное.", misconception: "Прыжок к коду — ход кодинга, а не проектирования. Ты оптимизируешь эндпоинт, не зная масштаба и того, какой ресурс сдаст первым. Требования идут раньше любого компонента." },
    { label: "Выбрать самую модную БД и обосновать вокруг неё остальную систему.", misconception: "Выбор технологии первым переворачивает поток. БД должна выпасть из требований (консистентность, темп записей, паттерн доступа), а не диктовать их. Это дизайн от технологии, а не от требований." },
  ]}
/>

<Quiz
  id="01-what-is-system-design-quiz-2"
  lessonSlug="01-what-is-system-design"
  lang="ru"
  question="Почему в system design «правильный ответ редко один, есть лишь защитимые»?"
  choices={[
    { label: "Потому что каждый значимый выбор меняет одно хорошее свойство на другое, и какой обмен правилен — зависит от выданных тебе требований.", correct: true },
    { label: "Потому что область незрелая и лучшие практики ещё не согласованы.", misconception: "Дело не в незрелости. Даже со зрелыми, хорошо понятными приёмами правильный выбор переворачивается с требованиями — сильная консистентность против низкой задержки, монолит против сервисов. Вариативность присуща компромиссам, а не признак неустоявшейся области." },
    { label: "Потому что интервьюеры хотят увидеть, умеешь ли ты спорить, независимо от технической сути.", misconception: "Защитимость — не риторика ради риторики. Она означает привязку реального компромисса к реальному требованию. Суть техническая: одна и та же задача при разных требованиях действительно имеет разные правильные дизайны." },
    { label: "Потому что единственный оптимальный дизайн всегда есть; инженеры лишь спорят, какой именно.", misconception: "Единственного оптимума обычно нет. Версия одного продукта с перевесом чтений и с перевесом записей хотят разных дизайнов. «Защитимый» существует именно потому, что оптимум относителен к требованиям, а не абсолютен." },
  ]}
/>

<MetaphorComplete
  id="01-what-is-system-design-metaphor-1"
  lessonSlug="01-what-is-system-design"
  lang="ru"
  setup="Кодинг спрашивает, возвращает ли функция правильный результат — бинарный, проверяемый вопрос. Проектирование спрашивает, выдержит ли система нагрузку и отказы, которых не видела, поэтому дизайн никогда не просто «корректен»; он может быть лишь _______ — он называет свои допущения, заявляет масштаб, на который рассчитан, и обосновывает, чем жертвует."
  accepted={["защитим", "защитимый", "защитимым", "defensible"]}
  canonical="защитим"
  explanation="Защитимый дизайн явно называет допущения, привязывает каждый выбор к заявленному требованию и называет принятую цену. В отличие от юнит-теста, ни один прогон не вернёт зелёный для дизайна — единственный доступный вердикт в том, держится ли рассуждение, что и значит «защитимый»."
/>

</Explanation>

<KeyTakeaway>Не заученные архитектуры, а рассуждение от требований → к узким местам → к компромиссам под масштабом и отказами. Кодинг спрашивает «корректно ли?»; проектирование — «выдержит ли?»; ответ защитим, не абсолютен.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="01-what-is-system-design-retrieval"
  lang="ru"
  questions={[
    { q: "Каков трёхшаговый поток рассуждения в system design, и почему важен порядок?", a: "Требования → узкие места → компромиссы. Сначала зафиксируй, что система обязана делать и насколько хорошо (функциональные + нефункциональные). Затем при этом масштабе найди, какой ресурс сдаст первым. Затем выбери, чем платить (консистентность, простота, стоимость, задержка), чтобы его снять. Пропустишь требования — оптимизируешь не то; пропустишь узкие места — масштабируешь то, что и не собиралось ломаться; компромиссы — где видна сеньорность, ведь бесплатных побед почти не бывает." },
    { q: "Чем проектирование отличается от кодинга?", a: "Кодинг спрашивает «выдаёт ли функция правильный результат?» — бинарный, проверяемый юнит-тестом вопрос. Проектирование спрашивает «выдержит ли система нагрузку, отказы и рост, которых не видела?» — вопрос, на который ни один тест не вернёт зелёный. Функция корректна или нет; дизайн всегда лишь защитим: называет допущения, заявляет целевую нагрузку и обосновывает, чем жертвует. «Работает на ноутбуке» — начало разговора, а не конец." },
    { q: "Почему в system design правильный ответ редко один?", a: "Потому что каждый значимый выбор меняет одно желательное свойство на другое (консистентность против задержки, денормализация против нормализации, монолит против сервисов), и какой обмен правилен — зависит целиком от выданных требований, а не от технологии. Сеньорный ход — вытащить компромисс вслух, привязать к требованию и выбрать сторону, которой оно требует, а не вспоминать «ответ»." },
  ]}
/>

<Recap lang="ru">
System design — **не зубрёжка**: не запоминание того, как устроен Twitter, а **способ рассуждать**, применимый к задачам, которых ты не видел. Он идёт в фиксированном порядке: **требования → узкие места → компромиссы** под давлением **масштаба и отказов**. **Кодинг** задаёт бинарный, проверяемый вопрос «корректна ли функция?»; **проектирование** — «выдержит ли система?», на который ни один прогон не вернёт зелёный, поэтому дизайн всегда лишь **защитим**: явный о своих допущениях, целевой нагрузке и принятой цене. Поскольку каждый реальный выбор меняет одно хорошее свойство на другое, **правильный ответ редко один** — версия одного продукта с перевесом чтений и с перевесом записей хотят разных дизайнов. Этот трек **основ** строит переиспользуемые концепции (масштабируемость, распределение данных, кэширование, консистентность, коммуникация, надёжность); **юнит 09** закаляет расплывчатое рассуждение отсюда в явную, повторяемую рамку; а отдельный трек **кейсов** тратит этот словарь на полные сквозные дизайны. Теперь, когда услышишь «нам нужен Kafka» — или поймаешь эту мысль у себя — ты распознаешь ход кодинга: выбор технологии до требований, в обход рассуждения, которое делает выбор защитимым.
</Recap>
