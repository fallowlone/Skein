---
slug: "quiz-choice"
lang: ru
track: "system-design"
unit: "06-async-messaging"
order: 5
title: "Асинхронность и сообщения: обзор с выбором ответа"
summary: "Синтез всего асинхронного раздела в формате с выбором ответа: семантика доставки и effectively-once, очередь против лога и реплей, хореография против оркестрации, outbox против двойной записи и ловушка бэклога безграничной очереди."
estMin: 13
status: ready
lessonType: topic
level: senior
concepts:
  - delivery-semantics
  - log-vs-broker
  - choreography-vs-orchestration
  - outbox-pattern
  - backpressure
  - idempotency
prereqs:
  - 06-async-messaging/01-message-queues
  - 06-async-messaging/02-pub-sub
  - 06-async-messaging/03-event-driven
  - 06-async-messaging/04-backpressure
sources:
  - https://aws.amazon.com/builders-library/avoiding-insurmountable-queue-backlogs/
  - https://docs.aws.amazon.com/prescriptive-guidance/latest/cloud-design-patterns/transactional-outbox.html
  - https://kafka.apache.org/documentation/
---
import Hook from "~/components/lesson/Hook.astro";
import Crux from "~/components/prose/Crux.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Шесть вопросов через весь раздел. Каждый — решение, которое ты принимаешь, ставя очередь в дизайн, выбирая стиль координации или ревьюя асинхронный конвейер: не определение для пересказа, а выбор за семантикой доставки, outbox и ловушкой бэклога.
</Hook>

<Crux>Шесть решений по разделу: гарантия доставки, очередь против лога, стиль координации, outbox и катастрофа безграничной очереди.</Crux>

<Explanation>

Подтверди, что умеешь выбрать семантику доставки и сделать её effectively-once, выбрать очередь против лога из требования реплея, разместить процесс на оси хореография/оркестрация, распознать и починить двойную запись через outbox и отвергнуть безграничную очередь по её поведению с бэклогом.

<Quiz
  id="06-async-choice-1"
  lessonSlug="quiz-choice"
  lang="ru"
  question="Вендор рекламирует «exactly-once доставку» для своей очереди. Как это точно читать и что твой consumer всё равно обязан делать?"
  choices={[
    { label: "Читать как effectively-once: сообщение может быть доставлено более раза, но его эффект может случиться один раз, если consumer идемпотентен (дедуп по стабильному бизнес-ключу). Проектировать под дубли всё равно надо.", correct: true },
    { label: "Принять буквально — отключить дедуп, раз брокер гарантирует отсутствие дублей.", misconception: "Никакой брокер не сделает доставку exactly-once по ненадёжной сети, потому что обработку и ack нельзя сделать атомарными. Убрав дедуп, ты получишь двойное действие при переотправке. «Exactly-once» — это effectively-once, обеспеченный в твоём consumer'е." },
    { label: "Это значит at-most-once, так что можно терять сообщения, но никогда не дублировать.", misconception: "Маркетинг exactly-once означает, что ЭФФЕКТ случается раз, а не что сообщения теряются. At-most-once (с потерями) — другая, более слабая гарантия. Механизм — идемпотентность + дедуп поверх доставки at-least-once." },
    { label: "Это невозможно, значит фича фейковая и бесполезная.", misconception: "Она реальна и полезна — она связывает дедуп на стороне брокера с доставкой, приближая once-эффект — но гарантия это effectively-once, и твой consumer всё равно обязан быть идемпотентным. Не отметай её; просто читай верно." },
  ]}
/>

<Quiz
  id="06-async-choice-2"
  lessonSlug="quiz-choice"
  lang="ru"
  question="Надо подключить новый consumer, что переобработает события за последние 14 дней, чтобы построить свежую проекцию. Какая подложка сообщений поддерживает это напрямую?"
  choices={[
    { label: "Log-based система (Kafka/Kinesis) с удержанием ≥14 дней: новая consumer-группа стартует со старого офсета и переигрывает, потому что лог удерживает сообщения независимо от потребления.", correct: true },
    { label: "Классический брокер (RabbitMQ/SNS): pub/sub хранит каждое сообщение для каждого будущего подписчика.", misconception: "Брокер удаляет каждую копию при ack; удержание — не его задача, а у подписки, что не существовала 14 дней назад, нечего реиграть. Бэкфилл требует удержанного, реплеябельного лога." },
    { label: "Любая очередь — просто переложи старые события от producer'а заново.", misconception: "У producer'а больше нет 14 дней прошлых событий; они были транзиентны. Перевывод и перепубликация истории — ровно та ручная боль, которой избегает лог. Используй лог с достаточным удержанием." },
    { label: "Ни одна — историческая переобработка в сообщениях невозможна.", misconception: "Это first-class на логе: новый consumer читает с офсета 0 (или с метки времени) и переигрывает. Log-модель существует ровно ради лёгкого бэкфилла и переобработки." },
  ]}
/>

<Quiz
  id="06-async-choice-3"
  lessonSlug="quiz-choice"
  lang="ru"
  question="7-шаговому процессу заказа нужны компенсирующие действия при сбое (возврат, освобождение товара), и дежурный должен отвечать «где сейчас заказ #X?». Какой стиль координации подходит?"
  choices={[
    { label: "Оркестрация: центральная сага ведёт шаги, держит состояние процесса (так у «где он?» есть ответ) и запускает компенсации при сбое — верно для сложного, не-должного-завершиться-наполовину потока.", correct: true },
    { label: "Хореография, потому что больше развязки всегда лучше.", misconception: "Развязка здесь не бесплатна: по 7 реагирующим сервисам сквозная логика и состояние процесса не живут нигде, а компенсации разбросаны. Это ровно случай, ради которого существует оркестрация." },
    { label: "Хореография, затем восстанавливать поток из логов по всем 7 сервисам по запросу.", misconception: "Раскопки по логам 7 сервисов на каждый запрос — симптом отсутствующей оркестрации. Координатор держит состояние процесса напрямую, не заставляя перестраивать его каждый раз." },
    { label: "Синхронный request/response, чтобы поток был трассируемым.", misconception: "Синхронная цепочка возвращает связность по доступности, что async убрала (один медленный шаг блокирует всё), и всё равно нужны компенсации. Оркестрация сохраняет асинхронную развязку, централизуя поток." },
  ]}
/>

<Quiz
  id="06-async-choice-4"
  lessonSlug="quiz-choice"
  lang="ru"
  question="Обработчик делает `db.commit(order)`, затем `broker.publish(event)`; изредка заказ сохранён, но событие так и не срабатывает. В чём баг и стандартная починка?"
  choices={[
    { label: "Проблема двойной записи — две системы, нет общей транзакции, падение в зазоре теряет событие. Чинится транзакционным outbox: вставь событие в таблицу outbox в ТОЙ ЖЕ локальной транзакции, что и заказ, затем отдельный relay публикует его.", correct: true },
    { label: "Шаткая сеть; оберни publish в ретраи.", misconception: "Если процесс умирает до того, как publish вообще запустится, ретраить нечего — заказ уже закоммичен. Нужно событие, сохранённое атомарно с заказом (outbox), а не просто отретраенное." },
    { label: "Публикуй до коммита — тогда оно всегда срабатывает.", misconception: "Это переворачивает в фантомное событие — падение или откат после публикации испускает событие для несуществующего заказа. Ни один порядок не безопасен; outbox делает обе записи одной транзакцией." },
    { label: "Используй двухфазный коммит между БД и брокером.", misconception: "Большинство брокеров не поддерживают 2PC с твоей БД, а 2PC медленен и хрупок (падение координатора блокирует участников). Outbox — стандартный, более лёгкий ответ." },
  ]}
/>

<Quiz
  id="06-async-choice-5"
  lessonSlug="quiz-choice"
  lang="ru"
  question="Конвейер использует безграничную очередь, «чтобы никогда не терять события». Downstream застывает на 30 минут в пик. Каков вероятный исход и корневая починка?"
  choices={[
    { label: "Огромный бэклог тихо накапливается, затем вычерпывается часами (восстановление требует ~2× ёмкости на столько, на сколько ты отстал) и может быть устаревшим/бесполезным — краткий инцидент становится длинным. Корневая починка: ограничить очередь (полна = fail fast = backpressure) и сбрасывать малоценную нагрузку.", correct: true },
    { label: "Ничего — очередь защищает тебя; события просто приходят чуть позже.", misconception: "При устойчивом прибытие > обслуживания бэклог растёт без границы, а 10× бэклог вычерпывается 10× длительности всплеска. «Чуть позже» становится на часы позже и, возможно, бесполезным. Безграничная очередь производит инцидент подлиннее." },
    { label: "Producer'ы автоматически замедляются, когда очередь полна.", misconception: "Безграничная очередь никогда не полна, поэтому никогда не отталкивает — это и есть баг. Backpressure случается лишь когда очередь ограничена и enqueue быстро падает." },
    { label: "Просто добавить consumer'ов после инцидента, чтобы догнать.", misconception: "Это помогает вычерпать, но не предотвращает катастрофу: во время затыка безграничная очередь уже спрятала перегрузку и раздула бэклог. Нужны граница + сброс нагрузки, чтобы сбой остался малым." },
  ]}
/>

<Quiz
  id="06-async-choice-6"
  lessonSlug="quiz-choice"
  lang="ru"
  question="На топике Kafka с 6 партициями нужно обрабатывать события каждого аккаунта по порядку, распараллеливая между аккаунтами. Как ключевать и потреблять?"
  choices={[
    { label: "Партиционировать по account_id, чтобы события каждого аккаунта оставались упорядочены внутри одной партиции, и запустить consumer-группу с до 6 consumer'ов, чтобы разные партиции обрабатывались параллельно.", correct: true },
    { label: "Случайная партиция на сообщение для баланса нагрузки и пересортировка по времени.", misconception: "Случайное партиционирование разбрасывает события аккаунта по партициям без межпартиционного порядка, а пересортировка по времени хрупка и stateful. Ключуй по account_id, чтобы держать пер-аккаунтный порядок." },
    { label: "Одна партиция ради гарантированного глобального порядка.", misconception: "Одна партиция даёт полный порядок, но убивает параллелизм — её обрабатывает лишь один consumer. Ключуй по account_id над несколькими партициями: порядок внутри аккаунта, параллелизм между аккаунтами." },
    { label: "Добавить больше consumer'ов, чем партиций, для большего параллелизма.", misconception: "Consumer'ы сверх числа партиций простаивают — каждая партиция принадлежит ровно одному consumer'у в группе. Параллелизм ограничен числом партиций; добавляй партиции, а не лишних consumer'ов." },
  ]}
/>

</Explanation>

<KeyTakeaway>Асинхронный дизайн — череда честных компромиссов: at-least-once + идемпотентность для effectively-once, лог для реплея, оркестрация для сложных потоков, outbox против двойной записи и граница на каждой очереди.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="06-async-choice-retrieval"
  lang="ru"
  questions={[
    { q: "Почему «exactly-once доставка» на деле effectively-once и чего это от тебя требует?", a: "Обработку и ack нельзя сделать атомарными по ненадёжной сети, поэтому падение всегда рискует дублем или потерей — никакой брокер не доставит exactly-once. «Exactly-once» означает, что ЭФФЕКТ случается раз: ты принимаешь доставку at-least-once и делаешь consumer идемпотентным (дедуп по стабильному бизнес-ключу), чтобы переотправки были безвредны. Дубли рано или поздно гарантированы; твой код — место, где обеспечивается once-эффект." },
    { q: "Как outbox побеждает проблему двойной записи и какую гарантию даёт?", a: "Обработчик должен обновить БД и опубликовать событие, но это отдельные системы без общей транзакции — падение в зазоре теряет событие или испускает фантомное. Транзакционный outbox пишет бизнес-строку и outbox-строку в ОДНОЙ локальной транзакции (обе или ни одной), затем отдельный relay публикует из outbox. Он возвращает атомарность, держа обе записи в системе, у которой есть транзакции. Relay публикует at-least-once, поэтому гарантия это «никогда не потеряно», а не «никогда не дублировано» — consumer'ы всё равно обязаны быть идемпотентны." },
  ]}
/>

<Recap lang="ru">
Сквозная линия этого раздела в том, что асинхронность покупает развязку и платит семантикой, под которую надо проектировать. Очередь превращает синхронную зависимость по доступности в асинхронную по задержке, но доставка честно **at-least-once** (или с потерями at-most-once), поэтому **exactly-once — это effectively-once** — идемпотентные consumer'ы с дедуп-ключом. **Pub/sub** рассылает веером по подписчикам, и глубокий раскол — **брокер против лога**: лог удерживает сообщения и отслеживает пер-consumer офсеты, делая **реплей и бэкфилл** first-class, а брокер с удалением-при-потреблении не может. Координация многошаговых процессов — **хореография** (развязано, простые потоки) против **оркестрации** (сага, держащая состояние процесса и компенсации — для сложных потоков). **Транзакционный outbox** побеждает **проблему двойной записи**, делая изменение состояния и событие одной локальной транзакцией с асинхронным relay, гарантируя «никогда не потеряно, возможно дублировано». А каждой очереди нужна **граница**: безграничная очередь — латентная катастрофа, отмывающая краткий затык в долгий бэклог, поэтому ты ограничиваешь, сбрасываешь нагрузку, лимитируешь темп по арендатору и алармишь на лаг. На этой глубине senior-ход — назвать компромисс до добавления компонента.
</Recap>
