---
slug: "quiz-short"
lang: ru
track: "system-design"
unit: "06-async-messaging"
order: 6
title: "Асинхронность и сообщения: обзор со свободным припоминанием"
summary: "Подсказки для свободного припоминания через весь асинхронный раздел. Ответь на каждую своими словами сначала — включая механизм и режим отказа — затем открой образец и сравни."
estMin: 14
status: ready
lessonType: topic
level: senior
concepts:
  - delivery-semantics
  - log-vs-broker
  - choreography-vs-orchestration
  - outbox-pattern
  - backpressure
  - idempotency
prereqs:
  - 06-async-messaging/01-message-queues
  - 06-async-messaging/02-pub-sub
  - 06-async-messaging/03-event-driven
  - 06-async-messaging/04-backpressure
sources:
  - https://aws.amazon.com/builders-library/avoiding-insurmountable-queue-backlogs/
  - https://docs.aws.amazon.com/prescriptive-guidance/latest/cloud-design-patterns/transactional-outbox.html
  - https://kafka.apache.org/documentation/
---
import Hook from "~/components/lesson/Hook.astro";
import Crux from "~/components/prose/Crux.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";

<Hook>
Припоминание бьёт перечитывание. На каждую подсказку скажи или напиши полный ответ по памяти — механизм и сбой, что он предотвращает, — прежде чем открыть образец. Усилие реконструкции и есть то, что закрепляет семантику доставки, outbox и ловушку бэклога.
</Hook>

<Crux>Реконструируй хребет раздела: семантика доставки, очередь против лога, хореография против оркестрации, outbox и backpressure.</Crux>

<Explanation>

Реконструируй хребет раздела, не подглядывая: что развязывает очередь и чего это стоит, почему exactly-once — это effectively-once, чем лог отличается от очереди, два стиля координации, как outbox побеждает двойную запись и почему безграничная очередь — латентная катастрофа.

<RetrievalDrawer
  client:load
  id="06-async-short-retrieval"
  lang="ru"
  questions={[
    { q: "Что развязывает очередь сообщений и чего развязка тебе стоит?", a: "Она развязывает producer и consumer во ВРЕМЕНИ: producer кладёт сообщение и возвращается; consumer обрабатывает позже; каждое сообщение идёт одному consumer'у и удаляется. Это превращает синхронную зависимость по доступности (запрос падает, если downstream лежит) в асинхронную по задержке (работа просто приходит позже), давая выравнивание нагрузки, изоляцию отказов и независимое масштабирование. Цена: теперь надо рассуждать о семантике доставки (дубли, порядок), добавленная сквозная задержка, согласованность в конечном счёте и новый компонент в эксплуатации." },
    { q: "Объясни at-least-once против at-most-once и почему exactly-once на деле effectively-once.", a: "Семантика берётся из того, КОГДА ты делаешь ack. Ack-до-обработки = at-most-once (без дублей, возможна потеря при падении). Ack-после-обработки = at-least-once (без потерь, возможны дубли). Третьего честного варианта нет, потому что обработку и ack нельзя сделать атомарными по ненадёжной сети. «Exactly-once» = effectively-once: сообщение может быть доставлено более раза, но его ЭФФЕКТ случается один раз — через идемпотентность (применение дважды == один раз) плюс дедуп по стабильному бизнес-ключу. Это свойство твоего consumer'а, а не настройка брокера. Механика at-least-once: visibility timeout (двигатель переотправки — heartbeat'ь долгие задачи), DLQ (отвод ядовитых сообщений, аларм на глубину), порядок по ключу." },
    { q: "Чем лог отличается от очереди и что это даёт?", a: "Очередь — труба доставки: сообщения протекают и удаляются при потреблении; состояние — «что осталось сделать». Лог — append-only, удержанная запись: сообщения остаются, и каждый consumer отслеживает свой офсет; состояние — «где каждый читатель». Поскольку лог удерживает данные, РЕПЛЕЙ (сброс офсета) и БЭКФИЛЛ (новый consumer стартует с офсета 0) first-class — невозможны на брокере с удалением-при-потреблении. Pub/sub на логе: consumer-ГРУППЫ дают fan-out между группами и деление нагрузки внутри группы (партиция на consumer'а); порядок держится ВНУТРИ партиции (ключуй по сущности), параллелизм между партициями." },
    { q: "Определи хореографию против оркестрации и правило большого пальца, и что добавляют event sourcing/CQRS.", a: "Хореография: нет центрального мозга; каждый сервис реагирует на события и испускает свои, и логика — сумма реакций — максимально развязано, отлично для простых потоков, но для сложных процессов сквозная логика и состояние процесса не живут нигде. Оркестрация: центральная сага ведёт шаги и запускает компенсирующие действия при сбое — поток в одном читаемом месте, идеален для сложных/не-должных-завершиться-наполовину процессов. Правило: хореография для простых малошаговых реакций; оркестрация, когда есть компенсации или кто-то должен ответить «где это в потоке?». Event sourcing хранит события как источник истины (переигрываемые для вывода состояния) на удержанном логе; CQRS разделяет write- и read-модели — оба оправдывают сложность лишь когда аудит или независимое масштабирование того требуют." },
    { q: "Что такое проблема двойной записи, как outbox её решает и почему согласованность в конечном счёте — проблема UX?", a: "Двойная запись: обработчик должен обновить БД И опубликовать событие, две системы без общей транзакции, поэтому падение в зазоре теряет событие или испускает фантомное. Транзакционный outbox пишет бизнес-строку и outbox-строку в ОДНОЙ локальной транзакции (обе или ни одной), затем отдельный relay публикует из outbox — возвращая атомарность внутри той единственной системы, у которой есть транзакции. Relay at-least-once, поэтому гарантирует «никогда не потеряно», а не «никогда не дублировано» — consumer'ы обязаны быть идемпотентны. Согласованность в конечном счёте (остальная система узнаёт факт после того, как он случился) протекает в UX: проектируй оптимистичный UI, состояния обработки и read-your-own-writes, а не притворяйся, что система синхронна." },
    { q: "Почему безграничная очередь — латентная катастрофа и что восстанавливает обратную связь?", a: "Очередь-системы бимодальны: почти пуста = быстрый режим; как только прибытие > обслуживания на устойчивый период, бэклог растёт без границы, а сквозная задержка лезет вверх — и ничего не выглядит сломанным, пока не пересечёшь черту. Восстановление жестоко: отстав на час, нужна ~2× ёмкость на час, 10× всплеск вычерпывается 10× длительности, данные могут устареть, а внутрипамятная очередь может упасть по OOM. Она отмывает краткий сбой в длинный. Починки: ограничь каждую очередь (полна = fail fast = сигнал backpressure), сбрасывай малоценную нагрузку дёшево на входной двери, лимитируй темп/изолируй по арендатору (shuffle-sharding), pull-управление потоком с алярмом на лаг consumer'а и страхуй ретраи (backoff + jitter, бюджет ретраев, circuit breaker'ы), чтобы шторм ретраев не усилил перегрузку." },
  ]}
/>

</Explanation>

<KeyTakeaway>Реконструируй каждый ответ по памяти — семантика доставки, очередь против лога, стили координации, outbox и backpressure — и ты держишь хребет раздела: асинхронность покупает развязку и оплачивается семантикой.</KeyTakeaway>

<Recap lang="ru">
Если ты смог реконструировать каждый ответ по памяти, ты держишь хребет раздела. **Очередь** развязывает producer и consumer во времени, превращая синхронную зависимость по доступности в асинхронную по задержке — оплачено **семантикой доставки**: at-least-once (без потерь, дубли) — безопасный дефолт, а **exactly-once — это effectively-once** через идемпотентность + дедуп, на котором работают visibility timeout, DLQ и порядок по ключу. **Pub/sub** рассылает веером, а **лог** отличается от **очереди** удержанием сообщений с пер-consumer офсетами, делая **реплей/бэкфилл** first-class. Многошаговые потоки — **хореография** (развязано, просто) или **оркестрация** (сага с состоянием процесса и компенсациями, для сложных потоков), с **event sourcing/CQRS** на удержанном логе. **Outbox** побеждает **проблему двойной записи**, делая изменение состояния и событие одной локальной транзакцией плюс асинхронный relay (никогда не потеряно, возможно дублировано → идемпотентные consumer'ы), а итоговая **согласованность в конечном счёте** — проблема UX. Наконец, каждой очереди нужна **граница**, иначе она отмывает краткий затык в долгий бэклог; ты ограничиваешь, сбрасываешь, лимитируешь темп и алармишь на лаг. Нить: на этой глубине ты называешь компромисс до добавления компонента.
</Recap>
