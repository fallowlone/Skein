---
slug: "quiz-code"
lang: ru
track: "system-design"
unit: "06-async-messaging"
order: 7
title: "Асинхронность и сообщения: чтение кода и конфига"
summary: "Прочитай реальный код consumer'а, конфиг брокера и обработчик outbox, затем выбери ответ, на который подписался бы senior: баг порядка ack, дубль из-за visibility timeout, двойная запись из inline-публикации и безграничный буфер."
estMin: 14
status: ready
lessonType: topic
level: senior
concepts:
  - delivery-semantics
  - visibility-timeout
  - outbox-pattern
  - backpressure
  - idempotency
  - dead-letter-queue
prereqs:
  - 06-async-messaging/01-message-queues
  - 06-async-messaging/03-event-driven
  - 06-async-messaging/04-backpressure
sources:
  - https://aws.amazon.com/builders-library/avoiding-insurmountable-queue-backlogs/
  - https://docs.aws.amazon.com/AWSSimpleQueueService/latest/SQSDeveloperGuide/sqs-visibility-timeout.html
  - https://docs.aws.amazon.com/prescriptive-guidance/latest/cloud-design-patterns/transactional-outbox.html
---
import Hook from "~/components/lesson/Hook.astro";
import Crux from "~/components/prose/Crux.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Баги сообщений живут в коде и конфиге, а не в прозе: куда ты поставил ack, во что выставлен visibility timeout, владеет ли relay публикацией и есть ли у буфера граница. Прочитай каждый сниппет, протрассируй падение и выбери ответ, на который подписался бы senior-инженер.
</Hook>

<Crux>Найди порядок ack, таймаут, место публикации или отсутствующую границу, затем выбери починку, которую требует режим отказа.</Crux>

<Explanation>

Потренируй петлю, что ты гоняешь на дизайн- или код-ревью: найди несущую строку, протрассируй, что делает с ней падение, и выбери изменение, что уважает семантику доставки, а не прячет её.

## Сниппет 1 — порядок ack

```python
def handle(msg):
    queue.ack(msg)          # подтверждено первым
    charge_card(msg.amount) # затем побочный эффект
```

<Quiz
  id="06-async-code-1"
  lessonSlug="quiz-code"
  lang="ru"
  question="Какую семантику доставки это даёт и в чём риск для платёжного обработчика?"
  choices={[
    { label: "At-most-once: ack до побочного эффекта означает, что падение между ack и charge_card ТЕРЯЕТ списание без переотправки. Для платежа нужен at-least-once (ack после) плюс идемпотентность, чтобы переотправка не списала дважды.", correct: true },
    { label: "At-least-once — ack гарантирует, что сообщение обработано.", misconception: "Ack не запускает списание; он велит брокеру забыть сообщение. Падение после ack, но до charge_card теряет работу без переотправки. Ack-первым — это at-most-once (с потерями), обратное заявленному." },
    { label: "Exactly-once, потому что ack случается раз.", misconception: "Нет exactly-once по ненадёжной сети. Ack-первым — это at-most-once (возможна потеря); чтобы приблизить once-эффект, ты делаешь ack ПОСЛЕ обработки (at-least-once) и дедуплицируешь. Позиция ack решает гарантию." },
    { label: "Всё нормально — платежи должны ack'ать рано, чтобы быстро освобождать очередь.", misconception: "Освобождение очереди до запуска списания — ровно так ты тихо теряешь платёж при падении. Ack после списания (at-least-once) и дедуп по idempotency-ключу, чтобы переотправка была безвредна." },
  ]}
/>

## Сниппет 2 — visibility timeout

```yaml
# SQS consumer
visibility_timeout: 30      # секунд
# измеренное время обработки задачи: p50 = 12с, p99 = 75с
# обработчик НЕ идемпотентен
```

<Quiz
  id="06-async-code-2"
  lessonSlug="quiz-code"
  lang="ru"
  question="С этими числами что происходит с самыми медленными задачами и какова починка?"
  choices={[
    { label: "Задачи около p99 (75с) пробивают 30с таймаут, пока ещё работают, поэтому сообщение всплывает, и второй воркер обрабатывает его параллельно — дубли. Починка: подними таймаут выше p99 и heartbeat'ь, продлевая его в долгих задачах, и сделай обработчик идемпотентным.", correct: true },
    { label: "Ничего — очередь ждёт, пока воркер закончит, прежде чем переотправлять.", misconception: "Брокер знает лишь, что таймаут истёк без удаления; он не отличит медленного воркера от упавшего. На 30с он переотправляет, и 75с задача бежит дважды параллельно. Размерь таймаут выше p99 и heartbeat'ь." },
    { label: "Медленные задачи уходят в DLQ за медлительность.", misconception: "DLQ для сообщений, что ПАДАЮТ раз за разом, а не для медленных. Медленная, но успешная задача просто переотправляется (дублируется), когда истекает visibility timeout. Подними таймаут / heartbeat." },
    { label: "Понизь таймаут до 10с, чтобы ретраи случались раньше.", misconception: "Это делает куда хуже — теперь даже p50 задачи (12с) дублируются. Таймаут должен быть ВЫШЕ реального времени обработки (p99), а не ниже него, плюс heartbeat и идемпотентность." },
  ]}
/>

## Сниппет 3 — обработчик outbox

```python
with db.transaction() as tx:
    tx.insert(order)
    tx.insert(outbox_row(OrderPlaced))   # атомарно с заказом — хорошо
    broker.publish(OrderPlaced)          # также опубликовано inline, «ради экономии хопа»
    tx.commit()
# отдельный relay ТОЖЕ читает outbox и публикует
```

<Quiz
  id="06-async-code-3"
  lessonSlug="quiz-code"
  lang="ru"
  question="Outbox-строка записана верно, так почему inline `broker.publish` — баг?"
  choices={[
    { label: "Он возвращает двойную запись: inline-публикация — отдельная система без общей транзакции с коммитом, поэтому падение между publish и commit испускает фантомное событие (и relay тоже публикует → дубли). Обработчик должен делать ТОЛЬКО локальную транзакцию; отдельный relay владеет всей публикацией.", correct: true },
    { label: "Всё нормально и быстрее — inline-публикация просто доставляет событие раньше.", misconception: "Она тихо воссоздаёт ровно проблему, которую outbox убирает. Падение после inline-публикации, но до commit испускает событие для откатившегося заказа (фантом), и relay дважды публикует. Удали inline-публикацию." },
    { label: "Баг — это outbox-строка; она не нужна, если публикуешь inline.", misconception: "Наоборот: outbox-строка — корректная, атомарная часть. Inline-публикация — баг двойной записи. Оставь outbox + relay; удали inline-вызов." },
    { label: "Добавь ретрай вокруг inline-публикации, чтобы сделать её надёжной.", misconception: "Ретрай нетранзакционной публикации не делает её атомарной с коммитом; она всё равно рискует фантомным событием и всё равно дублирует relay. Починка структурна: публикует только relay." },
  ]}
/>

## Сниппет 4 — буфер

```javascript
const queue = []                       // в памяти, без макс. размера
function enqueue(job) { queue.push(job) }   // никогда не отвергает
// воркер вычерпывает ~1k/с; producer'ы могут рвануть до ~8k/с во время всплесков
// симптом: во время всплесков RSS пода лезет вверх, пока его не убьют по OOM
```

<Quiz
  id="06-async-code-4"
  lessonSlug="quiz-code"
  lang="ru"
  question="Почему под падает под всплесками и какое изменение верно?"
  choices={[
    { label: "Безграничная внутрипамятная очередь при устойчивом прибытие > обслуживания растёт без предела, поэтому RSS лезет вверх до OOM — замедление становится жёстким падением. Ограничь очередь: когда полна, enqueue быстро падает (reject/429 или блок) — это сигнал backpressure — и используй долговечный брокер, если буферизованная работа должна пережить падение.", correct: true },
    { label: "Ничего не сломано; просто дай поду больше памяти.", misconception: "Больше памяти лишь оттягивает OOM под устойчивой перегрузкой — безграничная очередь растёт без предела. Починка — граница, применяющая backpressure, а не большая куча, что наполняется медленнее." },
    { label: "Воркер слишком медленный; добавь воркеров и оставь безграничную очередь.", misconception: "Быстрее вычерпывание помогает, но без границы достаточно большой всплеск всё равно раздувает очередь до OOM. Нужны граница + fail-fast, чтобы producer'ы чувствовали backpressure во время всплеска, а не просто больше consumer'ов." },
    { label: "Сделай очередь ещё больше, чтобы всплески всегда влезали.", misconception: "Нет размера, что «всегда влезает» при устойчивом прибытие > обслуживания — очередь растёт без предела по определению. Граница (и сброс при заполнении) — то, что держит сбой малым, а не больший буфер." },
  ]}
/>

</Explanation>

<KeyTakeaway>Каждый баг сообщений — несущая строка: позиция ack чинит семантику, visibility timeout чинит дубли, место публикации чинит двойную запись, граница очереди чинит бэклог.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="06-async-code-retrieval"
  lang="ru"
  questions={[
    { q: "Как позиция ack решает семантику доставки и что верно для платежа?", a: "Ack ДО побочного эффекта = at-most-once: падение в зазоре теряет работу без переотправки. Ack ПОСЛЕ = at-least-once: падение переотправляет, поэтому без потерь, но возможны дубли. Для платежа нужен at-least-once (никогда тихо не терять списание) плюс идемпотентность / дедуп-ключ, чтобы переотправка не списала дважды. Позиция одной строки переворачивает гарантию." },
    { q: "Почему inline-публикация в обработчике outbox возвращает двойную запись?", a: "Outbox работает, потому что бизнес-строка и outbox-строка — одна локальная транзакция (атомарна). Inline broker.publish — отдельная система без общей транзакции с коммитом, поэтому падение между publish и commit испускает фантомное событие для откатившейся записи, и оно также дублирует то, что опубликует relay. Дисциплина строга: обработчик делает ТОЛЬКО локальную транзакцию; ОТДЕЛЬНЫЙ relay владеет всей публикацией. Inline-вызов не даёт ничего и ломает гарантию." },
  ]}
/>

<Recap lang="ru">
Каждое решение по сообщениям в этом разделе всплывает как несущая строка, что ты читаешь прямо из кода. **Позиция ack** задаёт семантику доставки — ack-первым это at-most-once (с потерями), ack-последним это at-least-once (дубли), а платёж хочет последнее плюс идемпотентность. **Visibility timeout** должен сидеть выше реального p99 времени обработки и heartbeat'иться, иначе медленные задачи переотправляются и бегут дважды параллельно. **Outbox** работает, лишь если обработчик делает *только* локальную транзакцию, а *отдельный* relay публикует — inline-публикация тихо возвращает **двойную запись** (фантомные события плюс дубли relay). А внутрипамятная очередь **без границы** растёт без предела под устойчивой перегрузкой, пока процесс не упадёт по OOM, поэтому ты ограничиваешь её и быстро падаешь (сигнал backpressure), сбрасывая при заполнении и используя долговечный брокер, если буферизованная работа должна пережить падение. Senior-привычка — найти несущую строку, протрассировать, что делает с ней падение, и выбрать починку, которую требует режим отказа, — а не ту, что её прячет.
</Recap>
