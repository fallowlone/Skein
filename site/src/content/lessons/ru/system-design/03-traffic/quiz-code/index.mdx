---
slug: "quiz-code"
lang: ru
track: "system-design"
unit: "03-traffic"
order: 7
title: "Трафик и периметр: чтение конфига и кода"
summary: "Читай реальный конфиг балансировщика, шлюза и CDN, затем принимай решение: лгущий health-check, несовпадение алгоритма, шлюз в один инстанс и заголовок кэша, который течёт или устаревает."
estMin: 14
status: ready
lessonType: topic
level: senior
concepts:
  - load-balancing-algorithms
  - health-checks
  - api-gateway
  - cache-control-ttl
  - cache-invalidation
  - single-point-of-failure
prereqs:
  - 03-traffic/01-load-balancing
  - 03-traffic/02-reverse-proxy-and-gateway
  - 03-traffic/03-cdn
sources:
  - https://docs.nginx.com/nginx/admin-guide/load-balancer/http-load-balancer/
  - https://developer.mozilla.org/en-US/docs/Web/HTTP/Caching
  - https://www.cloudflare.com/learning/cdn/glossary/reverse-proxy/
---
import Hook from "~/components/lesson/Hook.astro";
import Crux from "~/components/prose/Crux.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Баги трафика живут в конфиге, не в прозе: health-check, лишь открывающий сокет, алгоритм, балансирующий не то, шлюз без реплики, заголовок кэша, кэширующий не тот ответ. Читай каждый сниппет, рассуждай, что реально происходит под нагрузкой, и выбирай ответ, под которым подпишется старший инженер.
</Hook>

<Crux>Читай конфиг, найди уровень, алгоритм, число реплик или заголовок кэша и выбери фикс, уважающий режим отказа, а не прячущий его.</Crux>

<Explanation>

Практикуй цикл, который гоняешь на ревью дизайна или конфига: найди настройку балансировщика, шлюза или CDN, рассуждай, как она ведёт себя под нагрузкой и отказом, и выбери изменение, которое поведение реально поддерживает.

## Сниппет 1 — health-check

```yaml
upstream api {
  server 10.0.1.10:8080;
  server 10.0.1.11:8080;
}
# проверка доступности
location /healthz { return 200 "ok"; }   # статичный 200, ничего не трогает
# у узла 10.0.1.11 исчерпан пул БД; каждый реальный запрос отдаёт 500
```

<Quiz
  id="03-traffic-code-1"
  lessonSlug="quiz-code"
  lang="ru"
  question="Узел .11 проходит /healthz и остаётся в ротации, отдавая пользователям 500-е. Почему и каков фикс?"
  choices={[
    { label: "Чек возвращает статичный 200 и никогда не дёргает цепочку зависимостей, поэтому узел с исчерпанным пулом БД «проходит». Сделай /healthz реально пробящим критические зависимости (дешёвая проверка БД/кэша) и возвращающим не-200 при отказе; добавь пассивные чеки по реальной доле ошибок/задержке.", correct: true },
    { label: "Чек в порядке; перезапусти узел .11.", misconception: "Перезапуск — разовый; чек продолжит проходить в следующий раз, когда зависимость откажет, потому что он ничего не тестирует. Почини чек, чтобы он пробил цепочку зависимостей и LB выкидывал нездоровые узлы автоматически." },
    { label: "Перейди на TCP-чек.", misconception: "TCP-чек ещё мельче — он лишь доказывает, что порт принимает соединение. Фикс — более глубокий HTTP-чек, трогающий БД/кэш, а не более мелкий." },
    { label: "Добавь узлов, чтобы 500-е разбавились.", misconception: "Больше узлов не чинит чек, не умеющий обнаружить отказ; плохой узел остаётся в ротации, отдавая 500-е. Сделай health-check осмысленным и добавь пассивное выкидывание по доле ошибок." },
  ]}
/>

## Сниппет 2 — выбор алгоритма

```nginx
# кэш-уровень: каждый узел держит горячее подмножество ключей в памяти
upstream cache_tier {
  least_conn;                 # балансировать по наименьшему числу активных соединений
  server cache-1; server cache-2; server cache-3; # ... 12 узлов
}
# наблюдается: hit ratio ужасен (~10%); почти каждый запрос промахивается
```

<Quiz
  id="03-traffic-code-2"
  lessonSlug="quiz-code"
  lang="ru"
  question="Кэш-уровень использует least_conn, а hit ratio ~10%. Что не так и какой алгоритм нужен?"
  choices={[
    { label: "least_conn балансирует нагрузку ровно, но игнорирует ключ, поэтому данный ключ попадает каждый раз на разный узел и почти всегда промахивается. Используй консистентное хеширование (хеш по ключу кэша), чтобы тот же ключ надёжно бил по узлу, его закэшировавшему.", correct: true },
    { label: "Всё нормально; 10% hit ratio нормален для кэша.", misconception: "10% катастрофичен для горячего кэша — значит ключи не липнут к узлам. Причина — балансировка по соединениям вместо ключа; консистентное хеширование чинит локальность." },
    { label: "Добавь кэш-узлов, чтобы поднять hit ratio.", misconception: "Больше узлов делает хуже при least_conn — ключи разлетаются ещё шире. Проблема в алгоритме, игнорирующем ключ, а не в числе узлов; перейди на консистентное хеширование." },
    { label: "Перейди на round-robin ради равномерного распределения.", misconception: "Round-robin тоже игнорирует ключ, поэтому проблема локальности остаётся и hit ratio низок. Нужна привязка ключа — консистентное хеширование — а не другой алгоритм, балансирующий по числу." },
  ]}
/>

## Сниппет 3 — деплой шлюза

```yaml
# kubernetes deployment для API-шлюза (перед 15 сервисами)
kind: Deployment
metadata: { name: api-gateway }
spec:
  replicas: 1                 # один инстанс
  strategy: { type: Recreate } # убить старый под, затем стартовать новый
# все 15 сервисов достижимы только через этот шлюз
```

<Quiz
  id="03-traffic-code-3"
  lessonSlug="quiz-code"
  lang="ru"
  question="В чём риск этого деплоя шлюза и каков минимальный фикс?"
  choices={[
    { label: "replicas: 1 со стратегией Recreate делает шлюз единой точкой отказа с простоем на каждом деплое — когда единственный под умирает, все 15 сервисов недостижимы. Подними replicas (≥2–3) за балансировщиком сервиса и используй RollingUpdate, чтобы всегда была живая реплика.", correct: true },
    { label: "Риска нет — один шлюз проще, и Kubernetes быстро его перезапустит.", misconception: "Даже быстрый перезапуск — полный сбой всех 15 сервисов, пока единственный под лежит, а Recreate гарантирует разрыв на каждом деплое. Одна реплика — учебный SPOF; гоняй несколько за LB." },
    { label: "Перейди на StatefulSet.", misconception: "StatefulSet не адресует доступность здесь — шлюз stateless. Фикс — несколько реплик с rolling update, а не стабильные идентичности/хранилище." },
    { label: "Увеличь лимиты CPU и памяти пода.", misconception: "Больше ресурсов помогает пропускной способности, не доступности. Один бóльший под всё равно единая точка отказа; нужна не одна реплика с rolling-деплоем." },
  ]}
/>

## Сниппет 4 — заголовок кэша

```http
# ответ для GET /account/orders  (показывает заказы залогиненного пользователя)
HTTP/1.1 200 OK
Cache-Control: public, max-age=600
# отдаётся через разделяемый CDN; заголовка Vary нет
```

<Quiz
  id="03-traffic-code-4"
  lessonSlug="quiz-code"
  lang="ru"
  question="Этот аутентифицированный ответ на пользователя несёт Cache-Control: public на разделяемом CDN. Каково последствие и фикс?"
  choices={[
    { label: "Разделяемый edge ключуется по URL и кэширует заказы Пользователя A, затем отдаёт их Пользователю B, запросившему тот же путь — утечка данных. Фикс: Cache-Control: private (только браузер) или no-store, и Vary: Cookie/Authorization, чтобы ни один разделяемый кэш его не хранил.", correct: true },
    { label: "Это просто ускоряет страницу; CDN разделяет пользователей автоматически.", misconception: "Разделяемый кэш ключуется по URL, а не по пользователю. public + нет Vary значит он может отдать заказы одного пользователя другому по тому же пути. Персонализированные ответы никогда не public на разделяемом кэше." },
    { label: "Снизь max-age до 1 для безопасности.", misconception: "Короткий TTL всё равно отдаёт закэшированную копию любому, кто бьёт по URL в окне — включая другого пользователя. Проблема в public на разделяемом кэше, не в окне свежести; используй private/no-store." },
    { label: "Нормально, потому что заказы редко меняются.", misconception: "Частота изменений неважна; проблема в том, что ответ на пользователя. Даже неизменные данные текут, если Пользователь B получает закэшированные заказы Пользователя A. Помечай private/no-store и Vary по аутентификации." },
  ]}
/>

</Explanation>

<KeyTakeaway>Каждое решение по трафику — настройка в конфиге: health-check, пробящий реальную зависимость, алгоритм под цель, шлюз с репликами и заголовок кэша, не делающий ответ на пользователя public.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="03-traffic-code-retrieval"
  lang="ru"
  questions={[
    { q: "Почему статичный-200 health-check лжёт и что делает чек осмысленным?", a: "Статичный 200 (или голый TCP-коннект) лишь доказывает, что процесс слушает; он никогда не дёргает путь запроса или зависимости, поэтому узел с лежащей БД/кэшем всё равно «проходит», отдавая 500-е. Осмысленный чек — HTTP-эндпоинт, трогающий критическую цепочку зависимостей (дешёвая проба БД/кэша) и возвращающий не-200, когда что-то недоступно, в паре с пассивными чеками, выкидывающими узел по реальной доле ошибок или задержке." },
    { q: "Когда least-connections/round-robin дают ужасный hit ratio кэша и каков фикс?", a: "На кэш- или шард-уровне, где каждый узел держит подмножество ключей: least-connections и round-robin балансируют по нагрузке/числу и игнорируют ключ, поэтому данный ключ попадает каждый раз на разный узел и почти всегда промахивается (~10% hit ratio). Фикс — консистентное хеширование по ключу, шлющее тот же ключ на узел, его закэшировавший, и переотображающее лишь ~1/N при смене состава — храня локальность, которую алгоритмы, балансирующие по числу, разрушают." },
    { q: "Почему ответ на пользователя с Cache-Control: public на разделяемом CDN опасен и как починить?", a: "Разделяемый edge ключуется по URL, а не по пользователю, поэтому public аутентифицированный ответ кэшируется и затем отдаётся другому пользователю, запросившему тот же путь — реальный класс багов с утечкой данных. Фикс: помечай Cache-Control: private (кэшируемо лишь в собственном браузере пользователя) или no-store, и добавь Vary: Cookie/Authorization, чтобы ни один разделяемый кэш не хранил и не ключевал его неверно. Контент на пользователя никогда не public на разделяемом кэше." },
  ]}
/>

<Recap lang="ru">
Каждое решение по трафику в этом юните сводится к настройке, которую можно прочесть прямо из конфига. **Health-check**, возвращающий статичный 200 (или просто открывающий сокет), лжёт — он никогда не трогает цепочку зависимостей, поэтому узел, отдающий 500-е, остаётся в ротации; сделай его пробящим реальную БД/кэш и добавь пассивное выкидывание. **Алгоритм** должен совпадать с целью: least-connections/round-robin балансируют по числу и разрушают hit ratio кэш-уровня, где **консистентное хеширование** по ключу хранит локальность. **Шлюз** при `replicas: 1` — учебная **единая точка отказа** с простоем на каждом деплое — гоняй несколько реплик за балансировщиком с rolling update. А ответ на пользователя с `Cache-Control: public` на разделяемом CDN — это **утечка данных**, ждущая случиться — используй `private`/`no-store` и `Vary`. Старший навык — найти настройку, рассудить, как она ведёт себя под нагрузкой и отказом, и выбрать фикс, который поведение поддерживает — а не тот, что его прячет.
</Recap>
