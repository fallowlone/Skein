---
slug: "quiz-choice"
lang: ru
track: "system-design"
unit: "03-traffic"
order: 5
title: "Трафик и периметр: обзор с выбором ответа"
summary: "Синтез с выбором ответа по всему юниту трафика: L4 против L7 и алгоритмы балансировки, обратный прокси против шлюза и концентрируемый им SPOF, разделение mesh, а также hit ratio, TTL и что не кэшировать на CDN."
estMin: 13
status: ready
lessonType: topic
level: senior
concepts:
  - l4-vs-l7
  - load-balancing-algorithms
  - api-gateway
  - service-mesh
  - cache-hit-ratio
  - cache-invalidation
prereqs:
  - 03-traffic/01-load-balancing
  - 03-traffic/02-reverse-proxy-and-gateway
  - 03-traffic/03-cdn
sources:
  - https://www.cloudflare.com/learning/performance/types-of-load-balancing-algorithms/
  - https://www.cloudflare.com/learning/cdn/what-is-a-cdn/
  - https://docs.aws.amazon.com/whitepapers/latest/microservices-on-aws/api-gateway.html
---
import Hook from "~/components/lesson/Hook.astro";
import Crux from "~/components/prose/Crux.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Шесть вопросов через весь юнит. Каждый — решение, которое принимаешь, размещая балансировщик, выбирая алгоритм, решая, окупается ли шлюз, или задавая политику кэша CDN — не определение для зубрёжки, а компромиссы L4/L7, алгоритмов, SPOF, mesh и экономики hit ratio под реальной нагрузкой.
</Hook>

<Crux>Шесть решений через юнит: уровень, алгоритм, радиус поражения шлюза, разделение mesh, число hit ratio и что не кэшировать.</Crux>

<Explanation>

Подтверди, что умеешь выбрать верный уровень и алгоритм для балансировщика, увидеть, почему единственный шлюз — SPOF и когда вместо него подходит mesh, провести арифметику hit ratio и отвергнуть кэширование ответа на пользователя на разделяемом edge.

<Quiz
  id="03-traffic-choice-1"
  lessonSlug="quiz-choice"
  lang="ru"
  question="Нужно маршрутизировать запросы по URL-пути и читать cookie маршрутизации, а ещё хочешь ретраи на уровне запроса. Какой уровень балансировщика требуется и какова цена?"
  choices={[
    { label: "Уровень 7 — он терминирует и парсит каждый HTTP-запрос, чтобы увидеть путь и cookie и ретраить на уровне запроса, ценой CPU/задержки (проксирует два соединения на запрос).", correct: true },
    { label: "Уровень 4 — он быстрее и всё равно прочтёт путь.", misconception: "L4 маршрутизирует только по IP/порту и никогда не парсит HTTP-байты, поэтому не видит путь или cookie и не ретраит запрос. Маршрутизация по содержимому требует L7." },
    { label: "Любой уровень; маршрутизация по пути от уровня не зависит.", misconception: "Путь и cookie лежат внутри HTTP-сообщения; L4-балансировщик пересылает соединение, не читая его. Чтобы маршрутизировать по содержимому, надо быть на L7." },
    { label: "Уровень 7, и бесплатно, ведь парсинг дёшев.", misconception: "Способность реальна, но не бесплатна: L7 терминирует TLS/TCP, парсит каждый запрос и проксирует два соединения, стоя CPU и задержки. Этот компромисс и есть причина существования L4." },
  ]}
/>

<Quiz
  id="03-traffic-choice-2"
  lessonSlug="quiz-choice"
  lang="ru"
  question="В round-robin пуле из десяти один узел деградирует до 5 с/запрос (остальные ~50 мс), но продолжает принимать соединения. Что с p99 сервиса и какой алгоритм чинит?"
  choices={[
    { label: "Round-robin всё равно шлёт ему 1/10 трафика, поэтому ~10% запросов получают 5 с и p99 сервиса ≈ 5 с. Алгоритм с учётом нагрузки — least-connections или least-response-time/EWMA — обходит его.", correct: true },
    { label: "Round-robin обнаруживает медленный узел и пропускает автоматически.", misconception: "Round-robin считает запросы, а не времена ответа; он продолжает слать медленному узлу равную долю. Нужен алгоритм с учётом нагрузки или пассивный health-check с учётом задержки." },
    { label: "Один медленный узел из десяти не сдвинет хвост.", misconception: "Он доминирует в хвосте: десятая часть запросов получает ответ 5 с, поэтому p99 сервиса идёт за медленным узлом. Хвост следует за худшим сервером, на который ты продолжаешь слать." },
    { label: "Узел мгновенно выкидывается за медлительность.", misconception: "Медленный — не падающий: базовый health-check видит, что он принимает соединения и отвечает (поздно), и оставляет в ротации. Реагирует только алгоритм с учётом нагрузки или проба с учётом задержки." },
  ]}
/>

<Quiz
  id="03-traffic-choice-3"
  lessonSlug="quiz-choice"
  lang="ru"
  question="Единственный API-шлюз перед 15 сервисами падает на плохом пуше конфига; каждый сервис здоров, но все 15 недостижимы. Какое свойство и какой фикс?"
  choices={[
    { label: "Шлюз — единая точка отказа (весь трафик воронкой через него). Фикс: гонять избыточный, горизонтально масштабируемый пул шлюзов за своим балансировщиком с health-checks и раскатывать конфиг поэтапно (канарейка).", correct: true },
    { label: "Сервисы упали; перезапусти их.", misconception: "Сервисы были здоровы — шлюз не мог в них маршрутизировать. Сконцентрированный отказ — это SPOF шлюза; перезапуск здоровых сервисов его не лечит." },
    { label: "Убери шлюз; шлюзы всегда обуза.", misconception: "Централизация (аутентификация/rate-limit/маршрутизация один раз) ценна. Фикс — устранить SPOF одного инстанса избыточным пулом и поэтапными раскатами, а не удалить." },
    { label: "Верни аутентификацию в каждый сервис.", misconception: "Это воссоздаёт дублирование, которое шлюз решил, и оставляет SPOF маршрутизации. Сделай уровень шлюза избыточным и раскатывай конфиг безопасно." },
  ]}
/>

<Quiz
  id="03-traffic-choice-4"
  lessonSlug="quiz-choice"
  lang="ru"
  question="У сорока сервисов интенсивный трафик сервис-к-сервису с отсутствующими ретраями, несогласованным mTLS и без трассировки. Публичному API тоже нужна одна дверь аутентификации/rate-limit. Какая форма верна?"
  choices={[
    { label: "Шлюз для north-south (клиенты → платформа: аутентификация, публичные rate-limit, публичный API, TLS) И service mesh для east-west (sidecar'ы на инстанс: mTLS, ретраи, таймауты, трассировка) — две плоскости, каждая владеет своей заботой.", correct: true },
    { label: "Положи всё в шлюз — аутентификацию плюс все ретраи и mTLS для внутренних вызовов.", misconception: "Гнать east-west трафик через шлюз создаёт бог-шлюз и центральное узкое место/SPOF. East-west заботы — в sidecar'ах mesh на инстанс, не в центральном уровне." },
    { label: "Одна service mesh; она и публичный API представит.", misconception: "Mesh ведёт east-west (сервис-к-сервису) заботы; это не публичная дверь. Тебе всё равно нужен шлюз для north-south аутентификации, публичных rate-limit и публичной поверхности API." },
    { label: "Ни то ни другое — просто добавь ретраи в код каждого сервиса.", misconception: "Это снова проблема дублирования (расходящиеся копии, исходные инциденты). Mesh стандартизирует east-west ретраи/mTLS/трассировку централизованно-конфигурируемо, но локально-исполняемо; шлюз владеет публичным API." },
  ]}
/>

<Quiz
  id="03-traffic-choice-5"
  lessonSlug="quiz-choice"
  lang="ru"
  question="CDN при hit ratio 90% шлёт 20 000 RPS промахов на origin. Чистка ключей поднимает его до 95%. Что примерно с нагрузкой origin?"
  choices={[
    { label: "Промахи origin примерно вдвое: 10% → 5% трафика, то есть ~20 000 → ~10 000 RPS. Экономика hit ratio нелинейна — каждый шаг к 100% делит остаточный трафик origin вдвое.", correct: true },
    { label: "Нагрузка origin падает ~5%, под 5 пунктов прироста доли.", misconception: "Это линейно по доле ПРОМАХОВ (10% → 5%, то есть вдвое), а не по доле попаданий. Нагрузка origin примерно вдвое. Эта нелинейность и есть причина, почему малые приросты окупаются." },
    { label: "Ничего — hit ratio влияет лишь на задержку.", misconception: "Hit ratio — это ровно разгрузка origin: попадание — запрос, который origin не видит. 90% → 95% делит долю промахов вдвое и примерно вдвое срезает RPS origin." },
    { label: "Нагрузка origin удваивается, потому что кэшируется больше.", misconception: "Наоборот — больше кэшируется значит меньше промахов доходит до origin. 90% → 95% делит долю промахов вдвое, поэтому нагрузка origin примерно вдвое падает." },
  ]}
/>

<Quiz
  id="03-traffic-choice-6"
  lessonSlug="quiz-choice"
  lang="ru"
  question="Инженер хочет закэшировать аутентифицированную страницу аккаунта (показывает заказы залогиненного пользователя) на разделяемом CDN ради скорости. Каков вердикт и почему?"
  choices={[
    { label: "Отвергнуть: разделяемый edge, ключуясь по URL, может отдать заказы Пользователя A Пользователю B — утечка данных. Помечай Cache-Control: private (только браузер) или no-store, и Vary по аутентификации; кэшируй контент на пользователя только в его (браузерном) кэше.", correct: true },
    { label: "Нормально — кэширование ускоряет, и CDN автоматически разделяет пользователей.", misconception: "Разделяемый кэш ключуется по URL, а не по пользователю; без private/no-store он может отдать страницу одного пользователя другому. Персонализированные ответы никогда не живут в разделяемом кэше." },
    { label: "Нормально, если поставить TTL 1 секунда.", misconception: "Короткий TTL всё равно отдаёт закэшированную копию любому, кто запросит этот URL в окне — включая другого пользователя. Дело не в свежести, а в том, что данные на пользователя нельзя делить." },
    { label: "Отвергнуть, потому что аутентифицированные страницы нельзя кэшировать нигде.", misconception: "Вердикт верный, причина нет: их можно кэшировать в собственном браузере пользователя (Cache-Control: private). Правило — нет РАЗДЕЛЯЕМОГО кэширования контента на пользователя, а не нет кэширования вообще." },
  ]}
/>

</Explanation>

<KeyTakeaway>Решения по трафику — компромиссы до нагрузки: верный уровень и алгоритм, избыточный шлюз против SPOF, mesh для east-west, математика hit ratio и никакого кэширования данных на пользователя на разделяемом edge.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="03-traffic-choice-retrieval"
  lang="ru"
  questions={[
    { q: "Почему алгоритм с учётом нагрузки бережёт хвост задержки там, где round-robin нет?", a: "Round-robin считает запросы и шлёт медленному узлу равную долю независимо от того, насколько он медленный, поэтому p99 сервиса идёт за худшим узлом. Least-connections и least-response-time/EWMA учитывают нагрузку: медленный узел копит открытые соединения или высокую измеренную задержку и перестаёт получать долю, так что трафик обходит его, а хвост остаётся под контролем." },
    { q: "Как отвергнуть кэширование ответа на пользователя на разделяемом CDN одним предложением?", a: "Разделяемый edge ключуется по URL, а не по пользователю, поэтому без Cache-Control: private/no-store (и Vary по аутентификации) он может отдать аутентифицированную страницу Пользователя A Пользователю B по тому же URL — утечка данных; контент на пользователя можно кэшировать только в собственном браузере, никогда на разделяемом edge." },
  ]}
/>

<Recap lang="ru">
Сквозная линия — трафик и периметр это компромиссы, которые улаживаешь до прихода нагрузки. **Балансировщику** нужен верный уровень (L4 быстр и слеп к протоколу, L7 учитывает содержимое ценой CPU) и верный **алгоритм** (учитывающие нагрузку least-connections/EWMA берегут хвост; консистентное хеширование хранит локальность кэша). **Шлюз** централизует аутентификацию/rate-limit/маршрутизацию — выигрыш консистентности, становящийся единой точкой отказа, поэтому он избыточен и тонок, тогда как **service mesh** ведёт east-west заботы в sidecar'ах на инстанс. **CDN** живёт или умирает на своём **hit ratio**, чья экономика нелинейна (90% → 95% делит трафик origin вдвое), и дисциплина — кэшировать одинаковое для всех и безопасное при устаревании и **никогда не кэшировать данные на пользователя или обязанные быть верными на разделяемом edge**.
</Recap>
