---
slug: "project"
lang: ru
track: "system-design"
unit: "03-traffic"
order: 8
title: "Трафик и периметр: план балансировщика и периметра"
summary: "Практический проект: подними реальный балансировщик перед небольшим пулом, докажи, что алгоритм с учётом нагрузки бьёт round-robin при неровном узле, затем спроектируй и измерь политику кэша CDN — hit ratio, TTL и что никогда не должно попасть на разделяемый edge."
estMin: 240
status: ready
lessonType: topic
level: senior
concepts:
  - load-balancing-algorithms
  - health-checks
  - cache-hit-ratio
  - cache-control-ttl
  - cache-invalidation
  - single-point-of-failure
prereqs:
  - 03-traffic/01-load-balancing
  - 03-traffic/02-reverse-proxy-and-gateway
  - 03-traffic/03-cdn
sources:
  - https://docs.nginx.com/nginx/admin-guide/load-balancer/http-load-balancer/
  - https://www.cloudflare.com/learning/cdn/what-is-caching/
  - https://aws.amazon.com/builders-library/timeouts-retries-and-backoff-with-jitter/
---
import Hook from "~/components/lesson/Hook.astro";
import Crux from "~/components/prose/Crux.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import ProjectBrief from "~/components/pedagogy/ProjectBrief.astro";

<Hook>
Прочитать, что алгоритм с учётом нагрузки бережёт твой хвост и что hit ratio CDN нелинеен, — не то же, что доказать это на работающей системе. Подними реальный балансировщик перед небольшим пулом, впрысни один медленный узел и смотри, как расходятся round-robin и least-response-time — затем спроектируй политику кэша для смешанного приложения и измерь hit ratio, который реально получаешь.
</Hook>

<Crux>Гоняй балансировщик над пулом, докажи, что учёт нагрузки бьёт round-robin при медленном узле, затем спроектируй и измерь политику кэша CDN.</Crux>

<Explanation>

Этот проект делает весь юнит операционным: ты сконфигурируешь реальный балансировщик, продемонстрируешь разницу хвоста задержки между слепым и учитывающим нагрузку алгоритмом, проверишь, что осмысленный health-check выкидывает сломанный узел, а затем спроектируешь политику кэша на ответ и измеришь её hit ratio — включая доказательство того, что никогда не должно достигать разделяемого edge.

<ProjectBrief
  id="03-traffic-project"
  lessonSlug="project"
  lang="ru"
  objective="Построй небольшой, но реальный периметр трафика: балансировщик (nginx/HAProxy/Envoy или облачный LB) перед пулом одинаковых инстансов приложения, эмпирически продемонстрируй, что алгоритм с учётом нагрузки бережёт хвост задержки там, где round-robin нет, проверь выкидывание по health-check, а затем спроектируй и измерь политику кэша в стиле CDN для смешанного приложения — замыкая петлю между утверждениями юнита и наблюдаемым поведением."
  requirements={[
    "Подними балансировщик перед как минимум тремя одинаковыми инстансами приложения (контейнеры подойдут), каждый с тривиальным эндпоинтом и настраиваемой задержкой на запрос. Подтверди, что пул отдаёт трафик через один виртуальный адрес.",
    "Напиши генератор нагрузки, держащий фиксированный темп предложения (open-loop) и записывающий пропускную способность и p50/p99, чтобы честно сравнивать алгоритмы без coordinated omission.",
    "Впрысни неровный узел: сделай один инстанс отвечающим ~в 50–100× медленнее (имитируя паузы GC или деградировавший хост), пока он всё ещё принимает соединения, затем прогони нагрузочный тест раз под round-robin и раз под least-connections или least-response-time/EWMA.",
    "Сравни два прогона: покажи, что round-robin держит p99 сервиса у задержки медленного узла (он получает равную долю), тогда как алгоритм с учётом нагрузки обходит его и держит p99 у задержки здоровых узлов.",
    "Сконфигурируй осмысленный health-check (HTTP-эндпоинт, пробящий зависимость, а не голый TCP/статичный-200 чек), затем сломай эту зависимость на одном узле и покажи, что балансировщик выкидывает его, а трафик продолжается на остальных.",
    "Спроектируй политику кэша CDN на ответ для смешанного приложения с: фингерпринтованными статичными ассетами, публичной страницей (одинакова для всех), аутентифицированной страницей на пользователя, обязанным быть верным значением и эндпоинтом записи. Укажи Cache-Control/TTL/Vary для каждого и что никогда не должно попасть в разделяемый кэш.",
    "Подними кэширующий обратный прокси (или используй CDN) перед публичным контентом и измерь cache hit ratio под нагрузкой; затем улучши его (тюнинг TTL и нормализация ключей кэша) и запиши hit ratio до/после и подразумеваемое снижение нагрузки origin.",
    "Запиши математику ёмкости failover: при N узлах на твоей измеренной пиковой утилизации посчитай, могут ли выжившие вобрать долю одного потерянного узла без перехода колена кривой очередей, и укажи запас N−1, который провижинишь.",
  ]}
  acceptance={[
    "Работающий балансировщик + пул, достижимый через один адрес, с показанной конфигурацией алгоритма и health-check.",
    "Два результата нагрузочного теста (round-robin против учёта нагрузки) под одним и тем же впрыснутым медленным узлом, с p50/p99 и пропускной способностью, наглядно показывающие, что p99 round-robin идёт за медленным узлом, а алгоритм с учётом нагрузки держит хвост у здоровых узлов.",
    "Демонстрация того, что поломка пробимой зависимости узла заставляет балансировщик выкинуть его (логи или метрики), с продолжением трафика на оставшихся узлах и восстановлением, когда узел вылечен.",
    "Одностраничная таблица политики кэша, покрывающая все пять типов контента с явными Cache-Control/TTL/Vary, и однострочным обоснованием на строку, включая, какие ответы никогда не кэшируемы на разделяемом edge и почему (утечка / корректность / запись).",
    "Измеренный hit ratio кэша до/после для публичного контента с внесённым изменением, плюс подразумеваемое снижение RPS origin (показывающее понимание нелинейной экономики доли промахов).",
    "Короткая записка по failover: расчёт запаса N−1 и рекомендуемый размер пула, привязанные к риску давки.",
  ]}
  stretch={[
    "Добавь консистентное хеширование: поставь кэш-уровень по ключу за балансировщиком, покажи, что хеширование по ключу даёт высокий hit ratio, тогда как least-connections его обрушивает, и измерь, сколько ключей переотображается при добавлении/удалении узла (~1/N).",
    "Добавь сценарий давки: истеки TTL горячего объекта на многих edge/прокси-кэшах разом, понаблюдай синхронный всплеск origin, затем включи объединение запросов (и/или origin shield) и покажи, что origin видит один запрос вместо многих.",
    "Сделай шлюз избыточным: гоняй два инстанса балансировщика/шлюза за вышестоящим балансировщиком, убей один под нагрузкой и покажи, что полного сбоя нет — контрастируя с SPOF одного инстанса.",
    "Добавь джиттер-ретраи в генератор нагрузки и покажи, как синхронные (без джиттера) ретраи при отказе узла ускоряют каскад, тогда как джиттер размазывает нагрузку восстановления и избегает петли обратной связи brownout-в-сбой.",
  ]}
/>

</Explanation>

<KeyTakeaway>План периметра делает юнит операционным: докажи, что учёт нагрузки бьёт round-robin при медленном узле, проверь выкидывание по health-check, спроектируй и измерь политику кэша CDN и её hit ratio.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="03-traffic-project-retrieval"
  lang="ru"
  questions={[
    { q: "Как эмпирически показать, что алгоритм с учётом нагрузки бережёт хвост задержки?", a: "Поставь перед одинаковым пулом балансировщик, добавь генератор нагрузки, держащий фиксированный open-loop темп и записывающий p50/p99, затем впрысни один узел, отвечающий ~в 50–100× медленнее, пока он всё ещё принимает соединения. Прогони раз под round-robin и раз под least-connections/least-response-time. Round-robin держит p99 сервиса у задержки медленного узла (он получает равную 1/N долю); алгоритм с учётом нагрузки обходит медленный узел (его очередь соединений или измеренная задержка растёт) и держит p99 у здоровых узлов. Это расхождение и есть доказательство." },
    { q: "Что задаёт здравая политика кэша CDN на каждый тип ответа?", a: "Cache-Control, TTL и Vary для каждого: фингерпринтованные ассеты получают длинный max-age + immutable; публичная страница — короткий-средний s-maxage плюс пёрдж при публикации; аутентифицированная страница на пользователя — private или no-store с Vary по аутентификации (никогда не разделяемая); обязанное быть верным значение — no-store (или крошечный микро-TTL); эндпоинт записи — не кэшируется. Правило: кэшируй одинаковое для всех и безопасное при устаревании; никогда не кэшируй на пользователя, обязанное быть верным или записи на разделяемом edge." },
    { q: "Почему измерять hit ratio и считать запас N−1, а не доверять дефолтам?", a: "Экономика hit ratio нелинейна — улучшение 90% → 95% делит трафик origin вдвое — поэтому измерение до/после изменения TTL/ключа квантует реальное снижение нагрузки origin вместо догадок. А запас N−1 — защита от давки на failover: на твоей измеренной пиковой утилизации выжившие должны вобрать долю потерянного узла без перехода колена кривой очередей, поэтому считаешь запасную ёмкость и размеряешь пул под неё, а не открываешь каскад в проде." },
  ]}
/>

<Recap lang="ru">
Этот проект превращает юнит трафика и периметра в повторяемый воркфлоу: подними **балансировщик** над одинаковым пулом, затем **докажи** центральное утверждение юнита, впрыснув один медленный узел и показав, что **round-robin** прикрепляет p99 сервиса к нему, тогда как **учитывающий нагрузку** алгоритм (least-connections/least-response-time) обходит его. Проверь, что **осмысленный health-check** — пробящий реальную зависимость, а не статичный 200 — **выкидывает** сломанный узел и трафик продолжается. Затем спроектируй **политику кэша CDN на ответ** для смешанного приложения (фингерпринтованные ассеты, публичная страница, страница на пользователя, обязанное быть верным значение, эндпоинт записи), измерь **hit ratio** до и после улучшения TTL/ключа и переведи его в нелинейное снижение нагрузки origin. Наконец, сделай **математику ёмкости failover N−1**, чтобы выжившие вобрали потерянный узел без каскада давки. Делать конфигурацию и измерение, а не только читать утверждения, — вся дисциплина: инженер, построивший это однажды, выбирает уровни, алгоритмы, шлюзы и политики кэша намеренно, а не открывает их режимы отказа во время инцидента.
</Recap>
