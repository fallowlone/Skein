---
concepts:
- vpc
- subnets
- internet-gateway
- nat-gateway
- vpc-endpoints
- SAA-C03:Design Secure Architectures
deepensInto: []
estMin: 18
lang: ru
lessonType: topic
level: middle
order: 1
prereqs: []
slug: "01-vpc-and-subnets"
sources:
- https://docs.aws.amazon.com/vpc/latest/userguide/vpc-nat-gateway.html
- https://docs.aws.amazon.com/vpc/latest/privatelink/gateway-endpoints.html
- https://docs.aws.amazon.com/vpc/latest/userguide/subnet-basics.html
spiral: []
status: ready
summary: 'VPC — изолированная виртуальная сеть, задаваемая через CIDR; подсети режут её по AZ, а «публичная» против «приватной» решается маршрутизацией, а не флагом. Освой IGW, NAT и gateway-эндпоинты — или получи пятизначный счёт за egress.'
title: 'VPC и подсети: CIDR, маршрутизация и ловушка egress через NAT'
track: "aws"
unit: "04-networking"
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
Дата-команда гоняет ночной Spark-джоб в приватной подсети, читающий и пишущий терабайты в S3. Год всё работает безупречно. Потом финансы пересылают счёт AWS с одной строкой, обведённой красным: десятки тысяч долларов под «NAT Gateway — data processing». Код никто не менял. Произошло вот что: каждый байт в S3 всё это время маршрутизировался через NAT Gateway, а NAT берёт плату за обработку данных по гигабайтам поверх почасовой ставки. Этому трафику NAT вообще не был нужен — у S3 есть бесплатный VPC Gateway Endpoint, который полностью уводит его мимо NAT. Одна запись в таблице маршрутизации, добавленная за пять минут, стёрла эту строку из счёта. Урок жёсткий и частый: в VPC то, куда идут твои пакеты, решается маршрутизацией, которую настроил ты, и неверный маршрут по умолчанию — это молчаливый счёт.
</Hook>

<Crux>VPC — изолированная сеть по CIDR; подсети — срезы по AZ. Публичная против приватной — чистая маршрутизация, и она же решает счёт.</Crux>

<Explanation>

## CIDR, подсети и правило «одна AZ»

**VPC** (Virtual Private Cloud) — логически изолированная виртуальная сеть, которую ты выделяешь внутри AWS-региона. Её адресное пространство ты задаёшь блоком CIDR, например `10.0.0.0/16` — это даёт 65 536 приватных IPv4-адресов для раздачи. Ничто внутри одной VPC не достучится до другой VPC или интернета, пока ты явно не проложишь путь; изоляция — это поведение по умолчанию.

Ресурсы ты кладёшь не прямо в VPC — ты кладёшь их в **подсети**, которые режут CIDR VPC на меньшие диапазоны вроде `10.0.1.0/24`. Самый важный структурный факт: **подсеть целиком живёт в одной зоне доступности (AZ) и не может охватывать несколько AZ.** AZ — это изолированный кластер дата-центров, поэтому если ты разместишь все подсети в одной AZ и у этой AZ случится сбой, весь твой слой погаснет. Поэтому высокая доступность не обсуждается: разнеси подсети минимум по двум AZ и держи копию каждого слоя в каждой.

```text
VPC CIDR        10.0.0.0/16          (65 536 IP, один регион)
  AZ us-east-1a
    public      10.0.0.0/24          веб / балансировщик / NAT
    private     10.0.1.0/24          app-серверы, базы данных
  AZ us-east-1b
    public      10.0.2.0/24
    private     10.0.3.0/24
```

CIDR подсети должен быть подмножеством CIDR VPC, и они не могут пересекаться между собой. AWS также резервирует первые четыре и последний IP в каждой подсети, поэтому `/24` даёт 251 пригодный адрес, а не 256 — деталь, которая кусает, когда подсеть нарезана впритык под autoscaling-парк.

## «Публичная» и «приватная» — это маршрутизация, а не флаг

Нет галочки, которая делает подсеть публичной. **Подсеть публична, только если верны две вещи:** её связанная **таблица маршрутизации** отправляет `0.0.0.0/0` (весь трафик в интернет) на **Internet Gateway (IGW)**, *и* у инстансов в ней есть публичные IP-адреса. Убери любое — и та же подсеть фактически приватна. IGW — горизонтально масштабируемый, избыточный компонент VPC, дающий двунаправленную связь с интернетом; AWS не берёт почасовую плату за сам шлюз (ты платишь обычные тарифы за передачу данных).

Каждая подсеть связана ровно с одной **таблицей маршрутизации** — списком правил `назначение → цель`, оцениваемых по самому длинному совпадающему префиксу. У каждой таблицы есть неизменяемый **локальный маршрут** для CIDR VPC, именно он позволяет любой подсети говорить с любой другой подсетью внутри VPC без дополнительной настройки.

```text
# Таблица маршрутизации публичной подсети
Назначение      Цель
10.0.0.0/16     local              # внутри VPC, всегда есть
0.0.0.0/0       igw-0abc123        # интернет, в обе стороны

# Таблица маршрутизации приватной подсети
Назначение      Цель
10.0.0.0/16     local
0.0.0.0/0       nat-0def456        # только исходящий, через NAT в публичной подсети
```

**NAT Gateway** — это как приватные инстансы достукиваются *наружу* в интернет — за обновлениями ОС, в реестры пакетов, к сторонним API — не будучи доступными *из* интернета. По документации AWS, инстансы в приватной подсети подключаются наружу через NAT Gateway, но внешние сервисы не могут инициировать соединение обратно. Публичный NAT Gateway обязан жить в **публичной подсети** и иметь Elastic IP, а его маршрут в итоге отправляет трафик на IGW. Это управляемый, **зональный** ресурс: он существует в одной AZ.

<FlowDiagram
  label="Пути трафика в двухслойной VPC: публичная подсеть в интернет через IGW, egress приватной подсети через NAT и S3 через бесплатный gateway-эндпоинт"
  hue="var(--d-systems)"
  nodes={[
    { id: "app", label: "App-инстанс", sub: "приватная подсеть 10.0.1.0/24" },
    { id: "nat", label: "NAT Gateway", sub: "публичная подсеть, зональный, за ГБ" },
    { id: "igw", label: "Internet Gateway", sub: "нет почасовой платы за шлюз" },
    { id: "net", label: "Интернет", sub: "API, обновления, реестры" },
    { id: "vpce", label: "S3 Gateway Endpoint", sub: "без доп. платы" },
    { id: "s3", label: "Amazon S3", sub: "остаётся в сети AWS" },
  ]}
  edges={[
    { from: "app", to: "nat", label: "0.0.0.0/0 egress" },
    { from: "nat", to: "igw", label: "транслировано" },
    { from: "igw", to: "net", label: "наружу" },
    { from: "app", to: "vpce", label: "prefix list pl-s3" },
    { from: "vpce", to: "s3", label: "бесплатно, мимо NAT" },
  ]}
  caption="App ходит в общий интернет через NAT (тариф за ГБ), но достаёт S3 через gateway-эндпоинт, который ничего не стоит и никогда не касается NAT. Маршрутизация выбирает путь — а путь выбирает счёт."
/>

## Ловушка стоимости egress и как эндпоинты её убивают

Вот ловушка, рождающая пятизначные сюрпризы. NAT Gateway тарифицируется по **двум** осям: почасовая плата за каждый шлюз *плюс* плата за **обработку данных** по гигабайтам за каждый прошедший через него байт (цены иллюстративные и зависят от региона — смотри [страницу цен Amazon VPC](https://aws.amazon.com/vpc/pricing/) для актуальных ставок). Когда приватный инстанс говорит с S3, DynamoDB или другим высокообъёмным сервисом через дефолтный маршрут `0.0.0.0/0` на NAT, каждый гигабайт облагается этой платой за обработку — и в масштабе терабайтов она перекрывает саму стоимость вычислений.

Решение — **VPC Gateway Endpoint**. По документации AWS, gateway-эндпоинты дают связь с **S3 и DynamoDB** «без необходимости в internet gateway или NAT-устройстве», и за их использование **нет дополнительной платы**. Включаешь ты его выбором таблиц маршрутизации своих приватных подсетей; AWS автоматически добавляет маршрут, у которого назначение — управляемый AWS **prefix list** для сервиса, а цель — эндпоинт. Поскольку маршрутизация идёт по самому длинному префиксу, трафик в S3 теперь идёт по бесплатному маршруту эндпоинта вместо дефолтного NAT — и целиком остаётся в сети AWS.

Для сервисов без gateway-эндпоинта **Interface Endpoints (PrivateLink)** ставят ENI в твоей подсети, чтобы трафик к этому сервису не выходил в публичный интернет (у них своя почасовая + поГБ-плата, но они избегают платы NAT за обработку и интернет-пути).

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Компонент пути</th>
        <th class="border border-border px-3 py-2 text-left">Направление</th>
        <th class="border border-border px-3 py-2 text-left">Форма стоимости</th>
        <th class="border border-border px-3 py-2 text-left">Вопрос HA</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">Internet Gateway</td>
        <td class="border border-border px-3 py-2">Входящий + исходящий</td>
        <td class="border border-border px-3 py-2">Нет почасовой платы за шлюз; есть за передачу данных</td>
        <td class="border border-border px-3 py-2">Региональный, избыточный</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">NAT Gateway</td>
        <td class="border border-border px-3 py-2">Только исходящий</td>
        <td class="border border-border px-3 py-2">Почасовая + за ГБ обработки данных</td>
        <td class="border border-border px-3 py-2">Зональный — по одному на AZ для HA</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Gateway Endpoint (S3/DynamoDB)</td>
        <td class="border border-border px-3 py-2">Только к сервису</td>
        <td class="border border-border px-3 py-2">Без дополнительной платы</td>
        <td class="border border-border px-3 py-2">На таблицу маршрутизации, без ENI</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Interface Endpoint (PrivateLink)</td>
        <td class="border border-border px-3 py-2">Только к сервису</td>
        <td class="border border-border px-3 py-2">Почасовая + за ГБ (без платы NAT)</td>
        <td class="border border-border px-3 py-2">По одному ENI на AZ для HA</td>
      </tr>
    </tbody>
  </table>
</div>

<Inset kind="why" lang="ru">
Почему у NAT отдельная история про отказ по AZ? NAT Gateway зональный — он живёт ровно в одной AZ. Если ты построишь один NAT Gateway в `us-east-1a` и направишь приватные таблицы маршрутизации всех AZ на него, ты получишь сразу два режима отказа. Первый: если `us-east-1a` падает, инстансы в `1b` и `1c` теряют весь egress, хотя их собственная AZ здорова — единственный NAT это AZ-ограниченная единая точка отказа. Второй: в нормальной работе трафик из инстансов `1b`/`1c` пересекает границы AZ, чтобы дойти до этого NAT, а межзональный трафик сам по себе тарифицируется за ГБ. Решение — по одному NAT Gateway на AZ, где приватная таблица каждой AZ указывает на NAT в её *собственной* AZ: это убирает и SPOF, и межзональную плату за данные одним движением.
</Inset>

<TradeoffMatrix
  id="01-vpc-and-subnets-tradeoff-1"
  lessonSlug="01-vpc-and-subnets"
  lang="ru"
  prompt="Приватные app-серверы в трёх AZ читают и пишут 40 ТБ/месяц в S3 и ещё вызывают несколько сторонних HTTPS-API. Нужен egress, который высокодоступен и не финансово радиоактивен. Выбери основной дизайн."
  options={[
    { name: "S3 Gateway Endpoint для S3 плюс по одному NAT Gateway на AZ для сторонних API", summary: "Бесплатный эндпоинт для основного объёма, NAT только для остального.", correct: true, justification: "Верный выбор. 40 ТБ трафика в S3 — это бомба по стоимости, а S3 gateway-эндпоинт не несёт дополнительной платы и полностью уводит трафик мимо NAT, поэтому этот объём перестаёт накручивать плату за обработку данных и остаётся в сети AWS. У сторонних HTTPS-API нет gateway-эндпоинта, поэтому им NAT действительно нужен — и ты держишь по одному NAT Gateway на AZ, чтобы egress каждой AZ переживал отказ соседа и избегал межзональной платы за данные. Это делит трафик по тому, что реально нужно каждому пути: бесплатный эндпоинт для тяжёлого известного сервиса, HA-NAT для длинного хвоста." },
    { name: "Маршрутизировать весь egress, включая S3, через единственный NAT Gateway", summary: "Один NAT, проще всего настроить.", justification: "Это ровно та ловушка. Каждый из 40 ТБ в S3 облагается платой NAT за обработку данных по ГБ без всякой причины, ведь бесплатный gateway-эндпоинт существует. Хуже того, единственный NAT — это AZ-ограниченная единая точка отказа, и инстансы в двух других AZ платят межзональную плату за данные, чтобы до него дойти. Простота здесь покупает тебе пятизначную строку счёта и аварию, которая только и ждёт случиться." },
    { name: "По одному NAT Gateway на AZ для всего, без эндпоинтов", summary: "HA-NAT, без особой маршрутизации.", justification: "Лучше единственного NAT, потому что высокодоступно и избегает межзональных прыжков, но всё равно маршрутизирует все 40 ТБ трафика S3 через NAT и платит за обработку данных по ГБ за каждый байт. Ты решил доступность и проигнорировал стоимость. Добавление бесплатного S3 gateway-эндпоинта убирает крупнейшую плату одной правкой таблицы маршрутизации." },
    { name: "Дать app-серверам публичные IP в публичных подсетях", summary: "NAT вообще не нужен.", justification: "Неверно по безопасности и всё равно не бесплатно. Публичные подсети открывают app-серверы для входящих интернет-соединений, а это ровно то, что приватные подсети плюс NAT существуют предотвращать для бэкенд-слоёв. Ты также всё равно будешь платить за передачу данных и выбросишь ту изоляцию, которая делает архитектуру защищаемой. Дизайн egress — не повод выставлять бэкенды в интернет." },
  ]}
/>

<Quiz
  id="01-vpc-and-subnets-quiz-1"
  lessonSlug="01-vpc-and-subnets"
  lang="ru"
  question="Что на самом деле делает подсеть «публичной» в VPC?"
  choices={[
    { label: "Её таблица маршрутизации отправляет 0.0.0.0/0 на Internet Gateway, а у её инстансов есть публичные IP — публичная/приватная определяется маршрутизацией, а не настройкой", correct: true },
    { label: "Галочка «публичная», которую переключаешь при создании подсети", misconception: "Такого флага нет. AWS решает достижимость по тому, на что указывает таблица маршрутизации подсети. Подсеть с маршрутом 0.0.0.0/0 на Internet Gateway публична; убери этот маршрут — и идентичная подсеть приватна." },
    { label: "Размещение подсети в особой публичной зоне доступности", misconception: "AZ не бывают публичными или приватными. Любая AZ может держать и публичные, и приватные подсети; разница чисто в том, доходит ли таблица маршрутизации до Internet Gateway, а не в том, в какой AZ сидит подсеть." },
    { label: "Подключение к подсети NAT Gateway", misconception: "NAT Gateway даёт приватным подсетям только исходящий доступ в интернет; он не делает подсеть публичной. Публичная подсеть маршрутизируется на Internet Gateway и допускает возможность входящих соединений — противоположность тому, что даёт NAT." },
  ]}
/>

<Quiz
  id="01-vpc-and-subnets-quiz-2"
  lessonSlug="01-vpc-and-subnets"
  lang="ru"
  question="Приватные инстансы гонят 30 ТБ/месяц в S3, и строка NAT Gateway за обработку данных огромна. Самый дешёвый верный фикс?"
  choices={[
    { label: "Добавить S3 VPC Gateway Endpoint, чтобы трафик S3 полностью обходил NAT без дополнительной платы", correct: true },
    { label: "Купить NAT Gateway покрупнее, чтобы снизить ставку за ГБ", misconception: "NAT Gateway — управляемый сервис без типа инстанса, который можно укрупнить, и его плата за обработку данных по ГБ применяется независимо от пропускной способности. Масштабирование его вверх не снизит плату за ГБ — верный ход в том, чтобы увести S3 мимо NAT через бесплатный gateway-эндпоинт." },
    { label: "Перенести инстансы в публичные подсети, чтобы они били прямо в S3", misconception: "Это открывает бэкенд-инстансы для входящего интернет-трафика и всё равно не использует бесплатный путь эндпоинта; ты заплатишь за передачу данных и потеряешь изоляцию. Gateway-эндпоинт держит инстансы приватными и убирает плату." },
    { label: "Сжимать данные перед отправкой — с стоимостью NAT больше ничего не сделать", misconception: "Сжатие лишь уменьшает объём по краям и игнорирует настоящую проблему: этот трафик вообще не должен ходить через NAT. S3 gateway-эндпоинт устраняет плату за обработку данных для S3 начисто, чего никаким сжатием не достичь." },
  ]}
/>

</Explanation>

<KeyTakeaway>VPC — изолированная сеть, задаваемая через CIDR (напр. 10.0.0.0/16); подсети режут её, и каждая живёт в одной AZ, поэтому разноси по AZ ради HA. Подсеть публична, только если её таблица маршрутизации отправляет 0.0.0.0/0 на Internet Gateway и у инстансов есть публичные IP — маршрутизация определяет публичную против приватной. Приватные подсети выходят наружу через зональный NAT Gateway (почасовая + за ГБ), поэтому держи по одному на AZ, чтобы избежать AZ-SPOF и межзональной платы. Ловушка egress: S3/DynamoDB через NAT накручивает плату за обработку данных — используй бесплатный VPC Gateway Endpoint, чтобы полностью обойти NAT.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="01-vpc-and-subnets-retrieval"
  lang="ru"
  questions={[
    { q: "Что определяет публичную против приватной подсети и как приватные подсети достукиваются до интернета?", a: "Публичная против приватной решается маршрутизацией, а не настройкой. Подсеть публична, только когда её связанная таблица маршрутизации отправляет 0.0.0.0/0 на Internet Gateway И у её инстансов есть публичные IP-адреса; убери любое — и та же подсеть ведёт себя как приватная. У каждой таблицы маршрутизации также есть неизменяемый локальный маршрут для CIDR VPC, который позволяет всем подсетям в VPC достукиваться друг до друга без лишней настройки. У приватной подсети нет маршрута 0.0.0.0/0 на IGW, поэтому, чтобы выйти наружу (за обновлениями ОС, к API), она маршрутизирует 0.0.0.0/0 на NAT Gateway, живущий в публичной подсети. NAT позволяет инстансам инициировать исходящие соединения, но блокирует непрошеные входящие — эта асимметрия и есть весь смысл держать бэкенд-слои приватными, при этом давая им тянуть обновления." },
    { q: "Объясни ловушку стоимости egress через NAT и как VPC-эндпоинты её решают, плюс правило высокой доступности NAT.", a: "NAT Gateway тарифицируется двумя способами: почасовая плата за шлюз плюс плата за обработку данных по ГБ за каждый прошедший байт (иллюстративно, зависит от региона — смотри страницу цен VPC). Когда приватные инстансы шлют высокообъёмный трафик в S3 или DynamoDB через дефолтный маршрут 0.0.0.0/0 на NAT, каждый гигабайт облагается, рождая пятизначные сюрпризы в счёте на масштабе терабайтов. Решение — VPC Gateway Endpoint для S3/DynamoDB: по документации AWS он не требует internet gateway или NAT-устройства и не несёт дополнительной платы — ты добавляешь его в приватные таблицы маршрутизации, AWS вставляет маршрут на prefix list сервиса, и совпадение по самому длинному префиксу отправляет этот трафик по бесплатному эндпоинту, оставаясь в сети AWS. Для других сервисов interface-эндпоинт (PrivateLink) держит трафик вне интернета. Правило HA: NAT Gateway зональный, поэтому единственный NAT — это AZ-ограниченная единая точка отказа и вынуждает межзональную плату за данные из других AZ; держи по одному NAT Gateway на AZ, где приватная таблица каждой AZ указывает на её собственный NAT." },
  ]}
/>

<Recap lang="ru">
VPC — логически изолированная виртуальная сеть, которую ты задаёшь блоком CIDR вроде 10.0.0.0/16, и ресурсы ты кладёшь в подсети, режущие этот CIDR на меньшие диапазоны. Структурное правило, управляющее каждым решением о доступности, — что подсеть целиком живёт в одной зоне доступности и не может охватывать несколько AZ, поэтому ты разносишь подсети минимум по двум AZ, чтобы пережить сбой зоны. Публичная против приватной — не флаг, а следствие маршрутизации: подсеть публична, только когда её таблица маршрутизации отправляет 0.0.0.0/0 на Internet Gateway и у её инстансов есть публичные IP, тогда как приватная подсеть выходит наружу через зональный NAT Gateway, сидящий в публичной подсети, который позволяет инстансам инициировать соединения наружу, но блокирует непрошеные входящие. NAT Gateway — ещё и то место, где деньги тихо горят, потому что он берёт плату за час и за ГБ обработанных данных, поэтому маршрутизация высокообъёмного трафика S3 или DynamoDB через него рождает пятизначные сюрпризы, которые бесплатный VPC Gateway Endpoint устраняет, отправляя этот трафик по маршруту через prefix list, который никогда не касается NAT и остаётся в сети AWS. Доверши картину правилом высокой доступности — по одному NAT Gateway на AZ, где таблица каждой AZ указывает на её собственный NAT — чтобы убрать и единую точку отказа, и межзональную плату за данные, и у тебя VPC, чьи пути и чей счёт выбраны намеренно.
</Recap>
