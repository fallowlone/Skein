---
concepts:
- security-groups
- network-acls
- stateful-vs-stateless
- ephemeral-ports
- defense-in-depth
- SAA-C03:Design Secure Architectures
deepensInto: []
estMin: 17
lang: ru
lessonType: topic
level: middle
order: 2
prereqs: []
slug: "02-security-groups-and-nacls"
sources:
- https://docs.aws.amazon.com/vpc/latest/userguide/vpc-security-groups.html
- https://docs.aws.amazon.com/vpc/latest/userguide/vpc-network-acls.html
- https://docs.aws.amazon.com/vpc/latest/userguide/nacl-rules.html
spiral: []
status: ready
summary: 'Два пакетных фильтра: security group (инстанс, stateful, только allow, ссылается на другую SG) и network ACL (подсеть, stateless, упорядоченный, умеет deny). Stateful против stateless кусает больнее всего: не откроешь эфемерные порты — ответы исчезнут.'
title: 'Security groups и network ACL: два фильтра, две модели состояния'
track: "aws"
unit: "04-networking"
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
Команда запирает подсеть кастомным network ACL: входящее правило 100 разрешает 443 из интернета, остальное запрещено. Веб-серверы в этой подсети перестают отвечать. Не медленно — полностью. curl снаружи висит и отваливается по таймауту. Security group открыта на 443 настежь, таблица маршрутов в порядке, серверы здоровы. Через часы захватов трафика кто-то замечает: SYN приходит, SYN-ACK уходит с инстанса, но до клиента не доходит. NACL разрешил входящий запрос на 443 — но он так и не разрешил исходящий ответ, а тот уходит на эфемерном порту вроде 54321, а не на 443. NACL — stateless: он не помнит входящее соединение, поэтому ответный пакет — это совершенно новый поток, попавший в неявный deny. Одно исходящее правило на порты 1024-65535 всё починило. Баг был не в опечатке. Он был в том, что забыли: один из двух твоих фильтров не имеет памяти.
</Hook>

<Crux>Два фильтра: security group (инстанс, stateful, allow) и NACL (подсеть, stateless, упорядоченный). Кусает stateful против stateless.</Crux>

<Explanation>

## Два уровня, две совершенно разные модели состояния

Почему правильно настроенная security group иногда не доставляет трафик? Потому что второй фильтр — на границе подсети — живёт по совершенно другим правилам.

Пакет, идущий к твоему EC2-инстансу, пересекает два фильтра, и ведут они себя совсем по-разному. **Network ACL** охраняет границу подсети — он инспектирует трафик, входящий в подсеть и выходящий из неё. **Security group** охраняет ENI (Elastic Network Interface, эластичный сетевой интерфейс), прикреплённый к самому инстансу. Пакет из интернета сначала попадает в NACL, затем в SG; ответ уходит сначала через SG, затем через NACL. Оба должны пропустить трафик, иначе он умирает.

Определяющее различие — **состояние**. Security group **stateful**: если она разрешает входящий запрос, ответ автоматически разрешён обратно независимо от исходящих правил — а если она разрешает исходящий запрос (а дефолтная SG разрешает весь egress), ответ разрешён обратно независимо от входящих правил. SG помнит соединение. Network ACL **stateless**: у него нет памяти о прошлых пакетах. Если ты разрешил входящий 443, ты отдельно должен разрешить *исходящий* обратный трафик, и этот обратный трафик уходит не на 443 — он уходит на **эфемерном порту**. Клиент открыл соединение с высокого случайного порта, поэтому ответ сервера адресован именно этому порту. AWS рекомендует открывать **1024-65535** для эфемерного обратного трафика, потому что разные клиенты выбирают из разных диапазонов: NAT gateway и многие ELB используют 1024-65535, ядра Linux обычно 32768-60999, а Windows-клиенты 49152-65535. Открывай объединение, иначе часть клиентов тихо ломается.

<FlowDiagram
  label="Пакет запроса и его ответ, пересекающие network ACL подсети и security group инстанса, демонстрируя ловушку stateless обратного трафика"
  hue="var(--d-systems)"
  nodes={[
    { id: "client", label: "Клиент", sub: "src порт 54321, dst 443" },
    { id: "naclIn", label: "NACL входящий", sub: "правило разрешает 443, stateless" },
    { id: "sgIn", label: "SG входящий", sub: "разрешает 443, stateful, помнит поток" },
    { id: "app", label: "Инстанс / ENI", sub: "отвечает: src 443, dst 54321" },
    { id: "sgOut", label: "SG исходящий", sub: "авто-разрешён (stateful)" },
    { id: "naclOut", label: "NACL исходящий", sub: "ДОЛЖЕН разрешить 1024-65535 или дроп" },
  ]}
  edges={[
    { from: "client", to: "naclIn", label: "запрос" },
    { from: "naclIn", to: "sgIn", label: "подсеть к ENI" },
    { from: "sgIn", to: "app", label: "доставка" },
    { from: "app", to: "sgOut", label: "ответ" },
    { from: "sgOut", to: "naclOut", label: "ENI к подсети" },
    { from: "naclOut", to: "client", label: "эфемерный порт" },
  ]}
  caption="SG помнит поток, поэтому ответ спокойно уходит обратно. NACL — нет: ответ — это новый пакет на эфемерном порту, и если у NACL нет исходящего правила на 1024-65535, он дропается на границе подсети."
/>

## Как оцениваются правила и что каждый фильтр умеет выразить

Эти два фильтра даже *решают* по-разному. **Security group работает только на allow и оценивается как единое целое.** В SG нельзя написать deny-правило — её единственный глагол «разреши это». Каждое правило по всем SG, прикреплённым к ENI, объединяется: если *хоть одно* правило пропускает пакет — он проходит. Нет порядка и нет первого совпадения; это одно большое ИЛИ из allow-правил с неявным deny для всего несовпавшего. Дефолтная SG, идущая с VPC, разрешает **никакой входящий** трафик и **весь исходящий**, и для удобства содержит самоссылающееся правило, чтобы участники могли общаться друг с другом.

**Network ACL — упорядоченный список и поддерживает явный deny.** У каждого правила номер от 1 до 32766; AWS оценивает **сначала самый низкий номер, и первое совпадение выигрывает** — дальнейшие правила не проверяются. Поскольку он умеет говорить *deny*, NACL может то, чего SG фундаментально не может: заблокировать конкретный плохой IP или CIDR. Поставь DENY для `198.51.100.0/24` правилом 90, выше твоего ALLOW `0.0.0.0/0` правилом 100 — и этот диапазон отрезан, пока все остальные проходят. Каждый NACL завершается неудаляемым правилом `*`, которое отклоняет всё несовпавшее. Дефолтный NACL разрешает весь входящий и исходящий; **кастомный** NACL начинает с запрета всего, пока ты не добавишь правила.

```text
# Кастомный NACL — ВХОДЯЩИЕ правила (оценка от низкого к высокому, первое совпадение выигрывает)
Rule   Type        Port        Source            Allow/Deny
90     HTTPS       443         198.51.100.0/24   DENY    # сначала блокируем один плохой CIDR
100    HTTPS       443         0.0.0.0/0         ALLOW   # все остальные могут достучаться до 443
130    Ephemeral   1024-65535  0.0.0.0/0         ALLOW   # обратный трафик для исходящих соединений
*      ALL         ALL         0.0.0.0/0         DENY    # неявное, удалить нельзя

# Кастомный NACL — ИСХОДЯЩИЕ правила (stateless: ты ДОЛЖЕН открыть обратный путь)
Rule   Type        Port        Destination       Allow/Deny
100    Ephemeral   1024-65535  0.0.0.0/0         ALLOW   # ответы на входящий 443 уходят здесь
110    HTTPS       443         0.0.0.0/0         ALLOW   # если инстанс сам инициирует исходящий 443
*      ALL         ALL         0.0.0.0/0         DENY
```

Когда обнаруживаешь, что хардкодишь IP-диапазон в правило security group, спроси себя: можно ли сослаться на роль вместо адреса? Ответ почти всегда «да». Идиоматичный приём least-privilege живёт на стороне SG: **ссылайся на другую security group как на источник** вместо CIDR. Вместо «разреши 5432 от 10.0.2.0/24» на SG базы напиши «разреши 5432 от SG app-уровня». Теперь любой инстанс с SG app-уровня дотянется до Postgres, а больше никто — и правило продолжает работать, пока app-уровень масштабируется и его IP меняются. Ты никогда не хардкодишь адрес; ты выдаёшь доступ *роли*.

```bash
# Доступ к БД по least-privilege: разрешить Postgres ТОЛЬКО инстансам из SG app-уровня
aws ec2 authorize-security-group-ingress \
  --group-id sg-db0000000000db \
  --protocol tcp --port 5432 \
  --source-group sg-app00000000app
```

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Свойство</th>
        <th class="border border-border px-3 py-2 text-left">Security group</th>
        <th class="border border-border px-3 py-2 text-left">Network ACL</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">Работает на уровне</td>
        <td class="border border-border px-3 py-2">Инстанс / ENI</td>
        <td class="border border-border px-3 py-2">Подсеть</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Состояние</td>
        <td class="border border-border px-3 py-2">Stateful — обратный трафик авто-разрешён</td>
        <td class="border border-border px-3 py-2">Stateless — разрешай оба направления + эфемерные порты</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Глаголы правил</td>
        <td class="border border-border px-3 py-2">Только allow (нет deny)</td>
        <td class="border border-border px-3 py-2">Allow и deny</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Оценка</td>
        <td class="border border-border px-3 py-2">Все правила объединены (любой allow пропускает)</td>
        <td class="border border-border px-3 py-2">Нумерованные, низший первым, первое совпадение выигрывает</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Может ссылаться на SG как источник</td>
        <td class="border border-border px-3 py-2">Да — идиома least-privilege</td>
        <td class="border border-border px-3 py-2">Нет — только CIDR</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Дефолт (дефолтный объект VPC)</td>
        <td class="border border-border px-3 py-2">Нет входящего, весь исходящий</td>
        <td class="border border-border px-3 py-2">Разрешить всё в обе стороны</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Дефолт (кастомный объект)</td>
        <td class="border border-border px-3 py-2">Нет входящего, весь исходящий</td>
        <td class="border border-border px-3 py-2">Запретить всё, пока не добавишь правила</td>
      </tr>
    </tbody>
  </table>
</div>

<Inset kind="why" lang="ru">
Зачем нам два пересекающихся фильтра? Защита в глубину с разделением труда. Security group — твой скальпель: least-privilege на ресурс, выраженный против ролей (других SG), а не адресов, и stateful, так что про обратные пути не приходится думать. NACL — твой грубый барьер на всю подсеть: он умеет deny — поэтому может в чёрную дыру отправить известный плохой CIDR или навязать грубое правило «эти два уровня никогда не общаются», которое ни одна отдельная SG не гарантирует, потому что SG умеет только выдавать, но не запрещать. Большинство команд держат пермиссивные дефолтные NACL и делают всю реальную работу в SG, обращаясь к NACL только когда нужен deny или ограничение радиуса поражения на уровне подсети. Использовать NACL там, где место SG, — так и оказываешься в 2 часа ночи за дебагом дропнутого эфемерного обратного трафика.
</Inset>

<TradeoffMatrix
  id="02-security-groups-and-nacls-tradeoff-1"
  lessonSlug="02-security-groups-and-nacls"
  lang="ru"
  prompt="Нужно заблокировать один конкретный вредоносный /24 от доступа ко всем инстансам в подсети, без исключений, независимо от любого allow-правила, которое кто-то добавит позже. Какой фильтр справится?"
  options={[
    { name: "Deny-правило в network ACL подсети, с номером ниже allow-правил", summary: "Stateless, на всю подсеть, умеет deny.", correct: true, justification: "Верный инструмент. Только network ACL умеет выразить deny, и работает он на границе подсети, поэтому блок покрывает все инстансы разом и его нельзя отменить слишком широким правилом security group на каком-то одном инстансе. Дай DENY для /24 номер ниже, чем твой ALLOW 0.0.0.0/0, и первое-совпадение-выигрывает отрежет CIDR ещё до того, как allow вообще будет оценён. Именно для этого NACL и нужны: грубый, умеющий deny барьер на всю подсеть." },
    { name: "Убрать вредный CIDR из каждой security group", summary: "Ужать allow-правила SG.", justification: "Невозможно по построению. Security group работают только на allow — глагола deny нет. Нельзя «вычесть» CIDR из allow-правила; максимум — сузить каждое allow-правило в каждой SG, чтобы исключить этот диапазон, что хрупко, легко пропустить на новой SG и тривиально открывается обратно любым, кто добавит 0.0.0.0/0 позже. Весь смысл нужды в гарантированном блоке в том, что SG его дать не могут." },
    { name: "Самоссылающееся правило security group", summary: "Allow от той же SG.", justification: "Неверный механизм для блокировки. Самоссылка SG — это ALLOW-паттерн least-privilege (участники общаются друг с другом); он выдаёт доступ, а не запрещает. Он ничего не делает для остановки внешнего вредоносного /24 и вообще не инструмент deny." },
    { name: "Отцепить security group у инстансов", summary: "Убрать SG целиком.", justification: "Катастрофично и всё равно неверно. Снятие SG не запрещает плохой CIDR, и попутно ты теряешь весь остальной контроль доступа. Ты сломаешь легитимный трафик, всё ещё не имея способа выразить точечный deny, который даёт только NACL." },
  ]}
/>

<Quiz
  id="02-security-groups-and-nacls-quiz-1"
  lessonSlug="02-security-groups-and-nacls"
  lang="ru"
  question="Кастомный network ACL подсети разрешает входящий TCP 443 от 0.0.0.0/0, и security group тоже разрешает 443, но внешние клиенты всё равно не могут завершить запрос. Самая вероятная причина?"
  choices={[
    { label: "У NACL нет исходящего правила, разрешающего эфемерные обратные порты (1024-65535), поэтому ответ дропается на границе подсети — NACL stateless и не авто-разрешает обратный трафик", correct: true },
    { label: "Security group stateless и забыла правило для обратного трафика", misconception: "Security group stateful, а не stateless — они автоматически разрешают обратный трафик для разрешённого потока. Компонент, забывающий обратный трафик, — это network ACL, который stateless и которому нужно явное исходящее правило для эфемерных портов." },
    { label: "NACL нужно второе входящее правило на 443 с большим номером", misconception: "Входящий 443 уже разрешён; дублирующее правило с большим номером ничего не меняет, потому что первое-совпадение-выигрывает уже пропустило входящий запрос. Недостающий кусок — исходящее правило на эфемерные порты для ответа." },
    { label: "Security group не могут разрешать порт 443", misconception: "Security group разрешают любой указанный порт, включая 443. Проблема не во входящем правиле SG; она в том, что stateless NACL не пропускает исходящий ответ на его эфемерном порту." },
  ]}
/>

<Quiz
  id="02-security-groups-and-nacls-quiz-2"
  lessonSlug="02-security-groups-and-nacls"
  lang="ru"
  question="В security group твоей базы какое правило лучше всего выражает «только app-уровень может подключаться к Postgres» и продолжает работать при автомасштабировании app-уровня?"
  choices={[
    { label: "Разрешить входящий TCP 5432 с security group app-уровня в качестве источника (ссылка на source-group)", correct: true },
    { label: "Разрешить входящий TCP 5432 от IP-адресов текущих app-инстансов списком CIDR", misconception: "Хардкод IP ломается в тот же миг, когда app-уровень масштабируется или заменяет инстансы и их адреса меняются. Ссылка на SG app-уровня как источник выдаёт доступ роли, а не адресам, поэтому переживает автомасштабирование автоматически." },
    { label: "Добавить deny-правило в SG базы для всего, кроме app-уровня", misconception: "У security group нет глагола deny — нельзя написать «запретить всё, кроме». Least-privilege выражают, разрешая только конкретный источник (SG app-уровня); всё неразрешённое неявно запрещено." },
    { label: "Открыть 5432 для 0.0.0.0/0 и положиться на фильтрацию NACL", misconception: "Открыть порт базы всему интернету — классическая ошибка экспозиции; пермиссивный NACL не спасёт, а от least-privilege на ресурс ты отказался. Ссылайся на SG app-уровня вместо этого." },
  ]}
/>

</Explanation>

<KeyTakeaway>Два фильтра, две модели состояния. Security group: уровень инстанса/ENI, stateful (обратный трафик авто-разрешён), только allow, все правила объединены, может ссылаться на другую SG как источник — твой скальпель least-privilege. Network ACL: уровень подсети, stateless (открой эфемерные обратные порты 1024-65535, иначе ответы дропаются), упорядоченный от низкого к высокому, первое-совпадение-выигрывает, умеет явный deny — твой барьер на всю подсеть для блокировки CIDR. Least-privilege делай в SG; к NACL обращайся только когда нужен deny или ограничение радиуса поражения подсети.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="02-security-groups-and-nacls-retrieval"
  lang="ru"
  questions={[
    { q: "Сопоставь security group и network ACL по состоянию, глаголам правил, оценке, области и дефолтам — и объясни ловушку эфемерных портов.", a: "Security group фильтрует на уровне инстанса/ENI; network ACL — на границе подсети. Состояние — ключевой водораздел: SG stateful — разреши входящий (или исходящий) поток, и его ответ автоматически разрешён обратно независимо от правил противоположного направления — тогда как NACL stateless и не помнит ничего, поэтому оба направления надо разрешать явно. SG работают только на allow и без порядка: каждое правило по всем прикреплённым SG объединяется, любой allow пропускает, несовпавший трафик неявно запрещён. NACL — упорядоченный нумерованный список (1-32766), оценивается сначала по низшему номеру, первое-совпадение-выигрывает, и поддерживает явный deny. Область: SG может ссылаться на другую SG как источник (идиома least-privilege); NACL принимает только CIDR. Дефолты: дефолтная SG VPC разрешает никакой входящий и весь исходящий; дефолтный NACL разрешает всё в обе стороны, но кастомный NACL запрещает всё, пока не добавишь правила. Ловушка эфемерных портов: поскольку NACL stateless, ответ на разрешённый входящий 443 уходит на высоком случайном порту (исходный порт клиента), а не на 443 — поэтому надо добавить исходящее правило на эфемерные порты 1024-65535, иначе ответ дропается на границе подсети и соединение виснет." },
    { q: "Когда реально используешь NACL вместо просто security group и почему SG этого не может?", a: "Обращайся к NACL, когда нужно то, что SG фундаментально не умеет выразить: явный deny или грубый барьер на всю подсеть, независимый от конфига какого-либо одного инстанса. SG работает только на allow — она умеет выдавать доступ, но никогда не запрещать — поэтому не может заблокировать конкретный вредоносный CIDR; любой инстанс всё ещё может быть достижим из этого диапазона, если его SG разрешает порт. NACL может поставить DENY для этого /24 с номером ниже allow-правил, и поскольку он сидит на границе подсети, блок покрывает все инстансы разом и не может быть открыт обратно слишком широким правилом SG. NACL также полезны, чтобы навязать разделение уровней («эти две подсети никогда не общаются») как ограничение радиуса поражения. Обычная практика: всю работу по least-privilege делать в SG (stateful, ссылки на роли, без рассуждений об обратном пути) и держать NACL пермиссивными, кроме мест, где конкретно нужен deny — потому что stateless-фильтр там, где место stateful, и приводит к дебагу дропнутого эфемерного обратного трафика." },
  ]}
/>

<Recap lang="ru">
Твои ресурсы AWS сидят за двумя пакетными фильтрами с противоположными характерами. Security group охраняет ENI инстанса: она stateful, поэтому ответ на любой разрешённый поток автоматически пропускается в обратную сторону; она работает только на allow, каждое прикреплённое правило объединяется, а несовпавший трафик неявно запрещён; и она может назвать другую security group источником, что есть идиоматичный паттерн least-privilege — «разреши 5432 от SG app-уровня» переживает автомасштабирование, потому что выдаёт доступ роли, а не адресу. Network ACL охраняет границу подсети: он stateless и не помнит ничего, поэтому надо разрешать оба направления и, критически, открывать эфемерные обратные порты 1024-65535, иначе ответы на разрешённое входящее соединение тихо дропаются; его правила нумерованы и оцениваются сначала по низшему, первое-совпадение-выигрывает; и он поддерживает явный deny, что позволяет ему заблокировать конкретный плохой CIDR — то, чего security group не сможет никогда, потому что у неё нет глагола deny. Используй оба в связке: SG — точный stateful скальпель на ресурс, где живёт почти весь реальный контроль доступа, а NACL — грубый, умеющий deny барьер на всю подсеть, к которому обращаешься только когда надо отправить CIDR в чёрную дыру или ограничить радиус поражения. Сеньорский инстинкт — помнить, у какого фильтра есть память, а у какого нет. Теперь, когда увидишь соединение, которое молча зависает после того, как SYN-ACK ушёл с инстанса, — сначала проверяй исходящие правила NACL на эфемерные порты, прежде чем грешить на security group.
</Recap>
