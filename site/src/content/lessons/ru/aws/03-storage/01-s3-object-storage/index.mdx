---
concepts:
- object-storage
- storage-classes
- lifecycle-policies
- presigned-urls
- durability-vs-availability
- SAA-C03:Design Cost-Optimized Architectures
- CLF-C02:Cloud Technology and Services
deepensInto: []
estMin: 18
lang: ru
lessonType: topic
level: middle
order: 1
prereqs: []
slug: "01-s3-object-storage"
sources:
- https://docs.aws.amazon.com/AmazonS3/latest/userguide/storage-class-intro.html
- https://docs.aws.amazon.com/AmazonS3/latest/userguide/optimizing-performance.html
- https://docs.aws.amazon.com/AmazonS3/latest/userguide/ShareObjectPreSignedURL.html
- https://aws.amazon.com/s3/pricing/
spiral: []
status: ready
summary: 'S3 — плоское объектное хранилище: бакеты, ключи, классы хранения с трейдоффами извлечения/мин. срока, lifecycle-политики, presigned-URL, 11 девяток надёжности (не доступности) и строгая консистентность. Выбирай класс по паттерну доступа, а не по надежде.'
title: 'Объектное хранилище S3: классы, lifecycle, presigned-URL, надёжность'
track: "aws"
unit: "03-storage"
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import StackDiagram from "~/components/diagram/StackDiagram.astro";

<Hook>
Дата-команда переносит пять лет сырых логов событий — 400 ТБ, читаемых пару раз в год для комплаенса — в S3 и оставляет в Standard, потому что «это же просто S3». Месячный счёт за один этот бакет читается как ещё одна ставка в штат. Тогда кто-то пишет lifecycle-правило, сбрасывающее всё старше 90 дней в Glacier Flexible Retrieval, и чувствует себя умником. Через шесть недель аудитору нужен срез прошлогодних данных *сейчас*, команда запускает массовый restore по миллионам объектов и получает удар дважды: restore идёт часами, а плата за извлечение по центам за ГБ на сотнях терабайт даёт всплеск счёта, который никто не закладывал. Ни первый выбор, ни фикс не были неверны из-за S3. Они были неверны, потому что никто не прочитал паттерн доступа и не подобрал под него класс хранения — единственное решение, которое S3 навязывает тебе и тихо тарифицирует каждый месяц, что ты ошибаешься.
</Hook>

<Crux>S3 — плоское объектное хранилище. Рычаг стоимости и латентности — класс хранения: выбирай по реальному паттерну доступа, не наугад.</Crux>

<Explanation>

## Бакеты, ключи и плоское пространство ключей

S3 — *объектное* хранилище, не файловая система. Объект — это байты плюс метаданные плюс ключ, и он лежит в бакете — глобально-именованном, привязанном к региону контейнере. Настоящих директорий нет. Ключ `logs/2026/06/04/app.log` выглядит как путь, но это единая плоская строка; слэши — просто символы. Консоль рисует дерево папок для удобства, разбивая по `/`, но под капотом есть одно гигантское лексикографическое пространство ключей и ничего больше. Это важно, потому что почти всё, что масштабируется в S3, масштабируется по **префиксу** — ведущей части ключа, — а не по папке, потому что папок не существует.

Эта плоская модель формирует и производительность. S3 даёт как минимум **3 500 запросов PUT/COPY/POST/DELETE и 5 500 запросов GET/HEAD в секунду на префикс**, и число префиксов ничем не ограничено. Так что пропускную способность по запросам ты *проектируешь*, разнося ключи по префиксам, а не упираешься в фиксированную квоту. Бакет, который гонит каждую запись через один горячий префикс (скажем, ключи, начинающиеся с одной и той же даты), начнёт троттлить с `503 SlowDown` задолго до того, как бакет «заполнится», — потому что у бакета нет лимита размера, есть только лимиты запросов на префикс.

```bash
# Ключ — это одна плоская строка; «папки» — просто слэши в ней.
aws s3api put-object \
  --bucket app-prod-events \
  --key "logs/2026/06/04/ingest-7f3a.json.gz" \
  --body ingest-7f3a.json.gz \
  --storage-class STANDARD

# Листинг по префиксу — это и есть то, что «ls папки» делает под капотом.
aws s3api list-objects-v2 \
  --bucket app-prod-events \
  --prefix "logs/2026/06/04/" \
  --query "Contents[].{Key:Key,Class:StorageClass,Bytes:Size}"
```

С декабря 2020 года S3 также даёт **строгую консистентность read-after-write** бесплатно, на каждом запросе, в каждом регионе. После успешного `PUT` — будь то новый объект, перезапись или удаление — любой последующий `GET`, `HEAD` или `LIST` немедленно видит актуальное состояние. Старые оговорки об eventual consistency («сразу после записи можно прочитать устаревший или отсутствующий объект») ушли. Обходные пути read-your-write больше не нужны; если чтение сегодня выглядит устаревшим, баг в твоём коде или слое кэширования, а не в S3.

## Классы хранения: трейдофф извлечения против мин. срока

Каждый объект несёт **класс хранения**, и этот единственный атрибут — главный рычаг стоимости. Классы разменивают три вещи: цену хранения за ГБ, латентность извлечения и **минимальный срок хранения**, за который ты платишь, даже если удалишь раньше. Числа ниже — из документации AWS; *долларовые* цифры иллюстративны и зависят от региона — всегда сверяйся со [страницей цен S3](https://aws.amazon.com/s3/pricing/).

**S3 Standard** — класс по умолчанию: хранится в **≥3 зонах доступности (AZ)**, доступ за миллисекунды, без минимального срока, без платы за извлечение. Это верный дом для горячих данных и неверный для всего холодного — ты платишь премиальную цену за байты, которые никто не читает.

**Standard-IA** (Infrequent Access) сохраняет ту же ≥3-AZ устойчивость и доступ за миллисекунды, но режет цену хранения в обмен на **плату за извлечение за ГБ** и **минимальный срок 30 дней**. **One Zone-IA** ещё дешевле, потому что хранит в **одной-единственной AZ** — тот же *дизайн* надёжности, но если эта AZ физически утрачена, данные исчезли. **Intelligent-Tiering** обходит угадывание: за небольшую плату за мониторинг на объект он автоматически перемещает каждый объект между тирами доступа по мере смены паттерна, **без платы за извлечение и без минимального срока** — безопасный дефолт, когда паттерн действительно неизвестен.

Семейство Glacier — для архивов. **Glacier Instant Retrieval** сохраняет доступ за миллисекунды (мин. срок 90 дней) для редко читаемых данных, которые всё же нужны быстро. **Glacier Flexible Retrieval** (мин. 90 дней) и **Glacier Deep Archive** (мин. 180 дней) — настоящее холодное хранилище: объекты *архивированы* и недоступны в реальном времени — нужно вызвать `RestoreObject` и подождать **минуты-часы** (Flexible) или **часы** (Deep Archive), прежде чем читать, и сверх того заплатить за извлечение за ГБ. Именно в эту плату за извлечение и латентность и врезалась команда из Hook.

<StackDiagram
  label="Классы хранения S3, упорядоченные от самого горячего и дорогого сверху до самого холодного и дешёвого снизу"
  hue="var(--d-systems)"
  layers={[
    { label: "S3 Standard", note: "горячие данные, доступ за мс, ≥3 AZ, без мин. срока и платы за извлечение" },
    { label: "Standard-IA / Intelligent-Tiering", note: "редкий / неизвестный доступ, мс, ≥3 AZ, мин. 30 дней (IA)" },
    { label: "One Zone-IA", note: "пересоздаваемые редкие данные, мс, 1 AZ — теряются при отказе этой AZ" },
    { label: "Glacier Instant Retrieval", note: "архив, доступ за мс, мин. 90 дней, плата за извлечение" },
    { label: "Glacier Flexible / Deep Archive", note: "архив, restore за минуты–часы / часы, мин. 90 / 180 дней" },
  ]}
  caption="Более холодные классы разменивают латентность доступа и более длинное обязательство по мин. сроку на меньшую цену за ГБ. Выигрывает самый дешёвый класс, который терпит твой паттерн доступа; неверный стоит тебе на каждом чтении или каждом раннем удалении."
/>

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Класс</th>
        <th class="border border-border px-3 py-2 text-left">AZ</th>
        <th class="border border-border px-3 py-2 text-left">Латентность извлечения</th>
        <th class="border border-border px-3 py-2 text-left">Мин. срок</th>
        <th class="border border-border px-3 py-2 text-left">Плата за извлечение</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">Standard</td>
        <td class="border border-border px-3 py-2">&ge;3</td>
        <td class="border border-border px-3 py-2">Миллисекунды</td>
        <td class="border border-border px-3 py-2">Нет</td>
        <td class="border border-border px-3 py-2">Нет</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Standard-IA</td>
        <td class="border border-border px-3 py-2">&ge;3</td>
        <td class="border border-border px-3 py-2">Миллисекунды</td>
        <td class="border border-border px-3 py-2">30 дней</td>
        <td class="border border-border px-3 py-2">За ГБ</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">One Zone-IA</td>
        <td class="border border-border px-3 py-2">1</td>
        <td class="border border-border px-3 py-2">Миллисекунды</td>
        <td class="border border-border px-3 py-2">30 дней</td>
        <td class="border border-border px-3 py-2">За ГБ</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Intelligent-Tiering</td>
        <td class="border border-border px-3 py-2">&ge;3</td>
        <td class="border border-border px-3 py-2">Миллисекунды (активные тиры)</td>
        <td class="border border-border px-3 py-2">Нет</td>
        <td class="border border-border px-3 py-2">Нет (плата за мониторинг)</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Glacier Instant Retrieval</td>
        <td class="border border-border px-3 py-2">&ge;3</td>
        <td class="border border-border px-3 py-2">Миллисекунды</td>
        <td class="border border-border px-3 py-2">90 дней</td>
        <td class="border border-border px-3 py-2">За ГБ</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Glacier Flexible Retrieval</td>
        <td class="border border-border px-3 py-2">&ge;3</td>
        <td class="border border-border px-3 py-2">Минуты–часы (restore)</td>
        <td class="border border-border px-3 py-2">90 дней</td>
        <td class="border border-border px-3 py-2">За ГБ</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Glacier Deep Archive</td>
        <td class="border border-border px-3 py-2">&ge;3</td>
        <td class="border border-border px-3 py-2">Часы (restore)</td>
        <td class="border border-border px-3 py-2">180 дней</td>
        <td class="border border-border px-3 py-2">За ГБ</td>
      </tr>
    </tbody>
  </table>
</div>

<Inset kind="why" lang="ru">
**Надёжность (durability) и доступность (availability) — разные числа, и команды их путают.** *Надёжность* — рассчитана на **одиннадцать девяток (99,999999999%)** во всех классах, даже One Zone-IA — это вероятность, что твои байты выживут: шансы потерять объект настолько малы, что статистически близки к нулю. *Доступность* — это вероятность, что ты сможешь достучаться до объекта *прямо сейчас*, и она различается по классам (S3 Standard целится в 99,99%, Standard-IA — 99,9%, One Zone-IA — 99,5%). Так что One Zone-IA так же *надёжен*, как Standard-IA на бумаге — пока единственная AZ, где он лежит, не разрушена физически, после чего надёжность перестаёт иметь значение и ты просто потерял данные. Одиннадцать девяток защищают от отказа дисков и устройств между AZ; они не защищают One Zone-IA от потери его единственной AZ и никогда не защищают тебя от собственного `DeleteObject`, плохого lifecycle-правила или ransomware. Надёжность — это не бэкап.
</Inset>

## Lifecycle-политики и presigned-URL

Объекты между классами руками перемещают редко. **Lifecycle-конфигурация** на бакете делает это по расписанию: переводит объекты в более холодный класс через N дней и истекает (удаляет) их через M дней. Так ты кодируешь «горячее месяц, редкое квартал, архив год, исчезает через семь лет» одним правилом, а не cron-задачей.

```json
{
  "Rules": [
    {
      "ID": "events-cooling",
      "Filter": { "Prefix": "logs/" },
      "Status": "Enabled",
      "Transitions": [
        { "Days": 30,  "StorageClass": "STANDARD_IA" },
        { "Days": 90,  "StorageClass": "GLACIER" },
        { "Days": 365, "StorageClass": "DEEP_ARCHIVE" }
      ],
      "Expiration": { "Days": 2555 }
    }
  ]
}
```

Второй рабочий инструмент — **presigned-URL**. По умолчанию каждый объект приватен — читать его может только владелец. Presigned-URL встраивает ограниченную по времени криптографическую подпись (SigV4), так что любой держатель URL может выполнить *одну конкретную операцию* — `GET` для скачивания или `PUT` для загрузки — пока она не истечёт. Подпись несёт права того, кто сгенерировал URL, поэтому она никогда не даёт доступа больше, чем уже есть у подписавшего. Стандартный паттерн: твой сервер подписывает `PUT`-URL и отдаёт его браузеру, который загружает файл **прямо в S3** — серверы приложения никогда не касаются байтов, так что загрузка на 2 ГБ не занимает процесс приложения и не идёт в твой egress. Срок истечения — это и есть вся модель безопасности: presigned-URL из SDK/CLI может жить **до 7 дней (604 800 секунд)**; консоль ограничивает 12 часами. Подписывай коротко.

```python
import boto3
s3 = boto3.client("s3")

# PUT-URL с ограниченным сроком: отдай его браузеру, чтобы он загружал
# прямо в S3. Истекает через 15 минут; несёт права подписавшего.
upload_url = s3.generate_presigned_url(
    ClientMethod="put_object",
    Params={"Bucket": "app-prod-uploads", "Key": "user/42/avatar.png"},
    ExpiresIn=900,  # секунды; макс. 604800 (7 дней) для SigV4
)
```

<TradeoffMatrix
  id="01-s3-object-storage-tradeoff-1"
  lessonSlug="01-s3-object-storage"
  lang="ru"
  prompt="Логи комплаенса: ~50 ТБ/год, пишутся один раз, почти никогда не читаются, но когда аудитор просит — нужно выдать срез в течение дня-двух (несколько часов ожидания допустимы). Хранение 7 лет. Выбери класс хранения для остывших данных."
  options={[
    { name: "Glacier Flexible Retrieval (переход через lifecycle)", summary: "Дешёвый архив, restore за минуты–часы.", correct: true, justification: "Верный выбор. Данные пишутся один раз и почти не читаются, так что нужен самый дешёвый архивный тир, чью латентность извлечения ты терпишь. Glacier Flexible Retrieval восстанавливает за минуты-часы — внутри SLA «в течение дня-двух» — за долю цены хранения Standard, а его минимальный срок 90 дней нерелевантен при хранении 7 лет. Плату за извлечение за ГБ ты платишь только на редком аудиторском срезе, а не на всех 50 ТБ. Lifecycle-правило переводит логи туда автоматически после остывания." },
    { name: "S3 Standard", summary: "Горячий тир, доступ за мс, без платы за извлечение.", justification: "Верно по латентности, неверно по стоимости. Standard берёт премиальную цену за ГБ каждый месяц за 50 ТБ/год, читаемые раз в год — ты переплачиваешь многократно за мгновенный доступ, которым почти не пользуешься. Standard — для горячих данных; платить его ставку хранения за холодные логи комплаенса 7 лет — ровно первая ошибка команды из Hook." },
    { name: "Glacier Deep Archive", summary: "Самый дешёвый архив, restore за часы.", justification: "Самое дешёвое хранение, но подвох в латентности извлечения. Стандартный restore Deep Archive занимает часы (до ~12), а его минимальный срок 180 дней тут не мешает. Защитимо, если «день-два» действительно значит, что ты можешь ждать многие часы каждый раз — но Flexible Retrieval даёт более быстрый restore при всё ещё низкой цене, так что Deep Archive выигрывает, только если доступ ещё реже и тебе никогда не нужен оборот в минуты. Разумный второй выбор, не самый безопасный первый." },
    { name: "One Zone-IA", summary: "Более дешёвый IA, одна AZ.", justification: "Неверно по надёжности. One Zone-IA хранит в одной AZ, так что физическая потеря AZ уничтожает единственную копию — недопустимо для логов комплаенса, которые ты обязан по закону хранить 7 лет и не можешь пересоздать. Он к тому же дороже Glacier для настолько холодных данных. One Zone-IA — для пересоздаваемых, редко читаемых данных, а не для незаменимых архивов." },
  ]}
/>

<Quiz
  id="01-s3-object-storage-quiz-1"
  lessonSlug="01-s3-object-storage"
  lang="ru"
  question="Твой бакет троттлит с 503 SlowDown под тяжёлой нагрузкой на запись, хотя бакет вовсе не близок к «заполнению». Самая вероятная причина?"
  choices={[
    { label: "Слишком много запросов гонится через один префикс — S3 масштабируется до ~3 500 записей / 5 500 чтений в секунду на префикс, так что один горячий префикс троттлит, пока другие простаивают", correct: true },
    { label: "Бакет упёрся в лимит размера хранилища", misconception: "У бакетов нет лимита размера — можно хранить неограниченно объектов и байтов. S3 троттлит по частоте запросов на префикс, а не по общему размеру бакета. Фикс — разнести ключи по большему числу префиксов, а не «освободить место»." },
    { label: "S3 позволяет лишь одну запись за раз на бакет", misconception: "S3 массово конкурентен. Лимит — ~3 500 записей/сек на префикс, и число префиксов не ограничено, так что общая пропускная способность бакета фактически безгранична, если разносить ключи по префиксам." },
    { label: "Строгая консистентность сериализует все записи глобально", misconception: "Строгая консистентность read-after-write бесплатна и не сериализует записи и не снижает пропускную способность. 503 SlowDown — сигнал частоты запросов на префикс, не связанный с моделью консистентности." },
  ]}
/>

<Quiz
  id="01-s3-object-storage-quiz-2"
  lessonSlug="01-s3-object-storage"
  lang="ru"
  question="Коллега говорит: «S3 надёжен на одиннадцать девяток, так что бэкапы нам не нужны». Почему это неверно?"
  choices={[
    { label: "Надёжность в одиннадцать девяток защищает от аппаратной потери устройства/AZ, а не от удалений, плохих lifecycle-правил или ransomware — надёжность не бэкап, а One Zone-IA всё ещё может потерять свою единственную AZ", correct: true },
    { label: "Одиннадцать девяток на самом деле значат, что данные сохранны лишь на 99%", misconception: "Одиннадцать девяток (99,999999999%) — реальная, крайне высокая цель надёжности, и математика верна. Изъян не в числе; он в допущении, что надёжность покрывает логическую потерю (твои удаления, баги, атаки), а она не покрывает." },
    { label: "Надёжность и доступность — одно и то же, так что это уже бэкап", misconception: "Это разные метрики: надёжность — выживут ли байты, доступность — сможешь ли достучаться сейчас. Ни одна не равна бэкапу — бэкап защищает от логической потери, которую надёжность игнорирует." },
    { label: "S3 хранит данные только 30 дней", misconception: "S3 хранит объекты, пока ты (или lifecycle-правило истечения) не удалишь их — автоматического удаления через 30 дней нет. 30 дней — это минимальный порог тарификации для IA-классов, а не лимит хранения." },
  ]}
/>

</Explanation>

<KeyTakeaway>S3 — плоское объектное хранилище; пропускная способность масштабируется по префиксу (~3 500 записей / 5 500 чтений в секунду). Класс хранения — рычаг стоимости: выбирай по паттерну доступа, разменивая латентность извлечения и мин. срок на цену. Lifecycle остужает/истекает данные; presigned-URL разгружают загрузки/скачивания. Одиннадцать девяток надёжности — выживание байтов, не доступность и не бэкап.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="01-s3-object-storage-retrieval"
  lang="ru"
  questions={[
    { q: "Пройди по классам хранения S3 и трём вещам, которые они разменивают, затем дай правило выбора.", a: "У каждого объекта есть класс хранения, и он разменивает цену хранения за ГБ против латентности извлечения и минимального срока, за который платишь, даже если удалишь раньше. Standard: ≥3 AZ, доступ за мс, без минимума, без платы за извлечение — горячие данные. Standard-IA: та же ≥3-AZ устойчивость и доступ за мс, но дешевле хранение, плата за извлечение за ГБ и минимум 30 дней. One Zone-IA: ещё дешевле, потому что хранит в одной AZ — тот же дизайн надёжности в одиннадцать девяток, но данные исчезают при физической потере этой AZ, так что только для пересоздаваемых данных. Intelligent-Tiering: малая плата за мониторинг на объект, авто-перемещение между тирами, без платы за извлечение, без минимума — дефолт, когда паттерн неизвестен. Glacier Instant Retrieval: доступ за мс, минимум 90 дней, плата за извлечение — редко читается, но нужно быстро. Glacier Flexible Retrieval (минимум 90 дней) и Deep Archive (минимум 180 дней) архивированы, не в реальном времени: вызываешь RestoreObject и ждёшь минуты-часы (Flexible) или часы (Deep Archive) плюс плата за извлечение за ГБ. Правило: выбирай самый дешёвый класс, чью латентность извлечения и обязательство по мин. сроку терпит твой реальный паттерн доступа — никогда не оставляй холодные данные в Standard и никогда не архивируй в Flexible/Deep данные, которые могут понадобиться за секунды." },
    { q: "Различи надёжность и доступность в S3 и объясни, почему одиннадцать девяток — не бэкап.", a: "Надёжность — вероятность, что байты выживут — рассчитана на одиннадцать девяток (99,999999999%) во всех классах, включая One Zone-IA, через репликацию по устройствам (а для ≥3-AZ классов — по зонам доступности). Доступность — вероятность достучаться до объекта прямо сейчас, и она различается по классам: Standard целится в 99,99%, Standard-IA — 99,9%, One Zone-IA — 99,5%. Они ортогональны — высокая надёжность при меньшей доступности значит, что данные почти наверняка существуют, но могут быть кратко недостижимы. Одиннадцать девяток защищают только от аппаратной потери; они ничего не делают против логической потери — твоего DeleteObject, сработавшего по ошибке lifecycle-истечения, перезаписи или ransomware, шифрующего объекты. А для One Zone-IA единственная AZ — реальный домен отказа: потеряй её физически, и дизайн в одиннадцать девяток не поможет. Так что надёжность — не бэкап; для настоящей защиты нужны версионирование, MFA-delete, репликация в другой аккаунт/регион или Object Lock." },
  ]}
/>

<Recap lang="ru">
S3 — объектное хранилище с единым плоским пространством ключей: ключи лишь выглядят как пути, и всё, что масштабируется, делает это по префиксу, давая примерно 3 500 записей и 5 500 чтений в секунду на префикс без лимита размера бакета и с бесплатной строгой консистентностью read-after-write с декабря 2020 года. Решение, которое S3 навязывает, — класс хранения, главный рычаг стоимости: Standard для горячих данных (≥3 AZ, миллисекунды, без минимума и платы за извлечение); Standard-IA и One Zone-IA для редкого доступа при меньшей цене хранения, но с платой за извлечение за ГБ и минимумом 30 дней, где единственная AZ у One Zone-IA означает, что физическая потеря AZ уничтожает данные; Intelligent-Tiering для авто-перемещения объектов без платы за извлечение и минимума, когда паттерн неизвестен; и семейство Glacier для архивов — Instant Retrieval сохраняет доступ за миллисекунды (минимум 90 дней), тогда как Flexible Retrieval (минимум 90 дней, restore за минуты-часы) и Deep Archive (минимум 180 дней, restore за часы) — настоящее холодное хранилище с платой за извлечение за ГБ, которая подстерегает команды, архивирующие данные, что позже понадобятся быстро. Lifecycle-конфигурации переводят и истекают объекты по расписанию, так что остывание кодируется одним правилом; presigned-URL отдают браузеру подписанный GET или PUT с ограниченным сроком (до 7 дней), чтобы загрузки и скачивания обходили твои серверы целиком. И надёжность — не доступность и не бэкап: одиннадцать девяток значат, что байты выживут при аппаратном отказе, но это никогда не защищает от твоих удалений, плохого lifecycle-правила, ransomware или — для One Zone-IA — потери единственной AZ, где лежит единственная копия. Весь навык — сначала прочитать паттерн доступа и дать ему выбрать класс, потому что S3 тарифицирует тебя каждый месяц, что ты угадываешь неверно.
</Recap>
