---
concepts:
- ebs
- efs
- instance-store
- volume-types
- storage-selection
- SAA-C03:Design High-Performing Architectures
deepensInto: []
estMin: 18
lang: ru
lessonType: topic
level: middle
order: 2
prereqs: []
slug: "02-block-and-file"
sources:
- https://docs.aws.amazon.com/ebs/latest/userguide/ebs-volume-types.html
- https://docs.aws.amazon.com/ebs/latest/userguide/general-purpose.html
- https://docs.aws.amazon.com/efs/latest/ug/performance.html
- https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/instance-store-lifetime.html
- https://aws.amazon.com/ebs/pricing/
spiral: []
status: ready
summary: 'EBS, EFS и instance store — три модели подключения, а не три скорости. EBS — блок одной AZ для одного инстанса; EFS — NFS на много AZ для многих; instance store — эфемерный локальный NVMe, который можно потерять. Выбирай по подключению и долговечности, а не по сырым IOPS.'
title: 'Блочное и файловое хранилище: EBS, EFS и instance store'
track: "aws"
unit: "03-storage"
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
Команда гоняет самостоятельно поднятый Postgres на инстансе i3, потому что «локальный NVMe безумно быстрый» — и он правда такой: сотни тысяч IOPS при микросекундной латентности. Два года ни одного промаха. Потом плановая чистка по оптимизации затрат помечает инстанс как избыточный, и инженер жмёт Stop, собираясь уменьшить размер и запустить обратно. Stop криптографически стирает каждый блок instance store. Том возвращается пустым. Снапшота не было, потому что instance store снапшотить нельзя, а ночной логический дамп тихо падал три недели подряд. Быстрый диск никогда не был проблемой. Проблема в том, что «быстрый локальный диск» и «долговечные данные» — это разные оси, и команда поставила production-базу на том, который платформа намеренно проектирует как выбрасываемый. Выбор хранилища в AWS — сначала про подключение и долговечность, и только потом про скорость.
</Hook>

<Crux>EBS, EFS и instance store — три модели подключения: блок в одной AZ, общий файл на много AZ, эфемерный локальный, — не три скорости.</Crux>

<Explanation>

## Блок против файла против объекта, затем три сервиса

Прежде чем выбрать сервис, нужно понять, какую абстракцию ты вообще покупаешь — потому что подключить не ту к своей нагрузке — ровно так команды получают быстрый диск, который стирает себя сам, или файловую систему там, где должен стоять том базы.

В каждом дизайне AWS всплывают три абстракции хранения. **Блочное** хранилище отдаёт ОС сырые блоки фиксированного размера; ОС кладёт сверху файловую систему (ext4, xfs), и устройство выглядит как локальный диск, с которого можно грузиться и держать базу. **Файловое** хранилище отдаёт общее иерархическое пространство имён по сетевому протоколу (NFS, SMB), так что многие машины монтируют одно дерево и видят записи друг друга. **Объектное** хранилище (S3) держит целые неизменяемые блобы за HTTP-API без семантики файловой системы — отлично для ассетов и бэкапов, бесполезно как том базы, потому что нет частичной записи на месте. Этот урок — про блочную и файловую половину: тома, которые EC2-инстанс реально монтирует.

В этой половине AWS даёт три сервиса, и ловушка — ранжировать их по IOPS. Ранжируй их по **модели подключения и долговечности**:

- **EBS** — сетевой блок, живёт в **одной зоне доступности (AZ)**, подключается к **одному инстансу за раз** (с узким исключением Multi-Attach). Долговечен: переживает инстанс.
- **EFS** — управляемая файловая система **NFS** (Network File System, сетевая файловая система), охватывает **несколько AZ**, **многие инстансы монтируют её одновременно**, автомасштабирует ёмкость. Долговечна и общая.
- **Instance store** — **эфемерный** локальный NVMe, физически подключённый к хосту. Самый быстрый, но данные **исчезают** при stop, hibernate, terminate или отказе диска.

Вместе эти три варианта закрывают все модели подключения: одиночный долговечный узел, множество узлов с общим доступом и ультрабыстрый scratch. Аварии происходят, когда один вариант используют для задачи другого — чаще всего скорость instance store принимают за замену долговечности EBS.

<FlowDiagram
  label="Три модели подключения хранилища: сетевой блок EBS к одному инстансу, общий NFS EFS для многих, эфемерный локальный instance store"
  hue="var(--d-systems)"
  nodes={[
    { id: "ebs", label: "Том EBS", sub: "сетевой блок, 1 AZ" },
    { id: "i1", label: "EC2 инстанс A", sub: "один подключатель" },
    { id: "efs", label: "Файловая система EFS", sub: "NFS, мультизональная" },
    { id: "i2", label: "Парк EC2", sub: "многие монтируют" },
    { id: "host", label: "NVMe хоста", sub: "физически подключён" },
    { id: "i3", label: "EC2 инстанс C", sub: "эфемерно, теряется при stop" },
  ]}
  edges={[
    { from: "ebs", to: "i1", label: "подключается к одному" },
    { from: "efs", to: "i2", label: "монтируют все" },
    { from: "host", to: "i3", label: "локально, не долговечно" },
  ]}
  caption="EBS — долговечный блок для одного инстанса в одной AZ; EFS — долговечный общий файл для парка и нескольких AZ; instance store — самый быстрый путь, но выбрасывается, как только инстанс останавливается."
/>

## EBS: сетевой блок, который тюнишь числом

EBS — диск по умолчанию для одиночного stateful-узла: базы, брокера сообщений, загрузочного тома. Он **сетевой**, поэтому переживает инстанс: завершишь EC2-машину — том (если не delete-on-termination) всё ещё на месте, готов к повторному подключению. Он **однозональный** и привязан к **одному инстансу за раз**; исключение — **Multi-Attach** на io1/io2, который позволяет до 16 Nitro-инстансов в одной AZ делить один том, но только если приложение приносит собственную кластеризацию и I/O-fencing — это не «общее хранилище», а строительный блок для кластерного ПО.

**Типы** тома — настоящий рычаг сеньорского уровня, ведь gp3 отвязал производительность от размера. Том **gp3** включает базу в **3000 IOPS и 125 МиБ/с бесплатно при любом размере**, и ты выделяешь больше **независимо** — до 80 000 IOPS и 2000 МиБ/с — не наращивая диск. Это и есть ключевой фикс относительно **gp2**, где производительность была приварена к размеру по 3 IOPS/ГиБ, а маленькие тома опирались на баланс **burst-кредитов**: том gp2 меньше 1 ТиБ мог бёрстить до 3000 IOPS лишь пока были I/O-кредиты, и занятый маленький том, исчерпавший `BurstBalance`, проваливался обратно к крошечной базе (база gp2 на 100 ГиБ — всего 300 IOPS) — классический инцидент с обрывом латентности в 2 часа ночи. Для устойчиво высоких IOPS с жёсткой латентностью есть **io1/io2** (provisioned IOPS, io2 Block Express до 256 000 IOPS при средней латентности менее 500 микросекунд и долговечности 99,999%). Для дешёвых последовательных нагрузок на пропускную способность — большие логи, дата-лейки — выигрывают по цене **HDD**-типы **st1** (throughput-optimized, до 500 МиБ/с) и **sc1** (cold, самый дешёвый).

Тома EBS также **меняют размер онлайн**: можно нарастить ёмкость, сменить тип и поменять IOPS/пропускную способность через Elastic Volumes, пока инстанс работает. А **снапшоты** — инкрементальные бэкапы в S3; восстановление тома из снапшота — **ленивая загрузка**: том готов сразу, но блоки подтягиваются из S3 при первом обращении, поэтому свежее восстановление кажется медленным, пока не прогреется (инициализируй его или используй Fast Snapshot Restore, если нужна полная производительность с нулевого блока).

```bash
# gp3 с базой 3000 IOPS / 125 МиБ/с, включённой бесплатно при любом размере
aws ec2 create-volume \
  --volume-type gp3 --size 200 --availability-zone us-east-1a

# Поднять IOPS и пропускную способность независимо от размера — онлайн, без отключения
aws ec2 modify-volume \
  --volume-id vol-0abc123 --iops 9000 --throughput 500

# Снапшоты — инкрементальные бэкапы в S3; восстановление лениво грузит блоки при первом чтении
aws ec2 create-snapshot --volume-id vol-0abc123 \
  --description "nightly db volume backup"
```

<Inset kind="why" lang="ru">
Почему свежий том, восстановленный из снапшота, медленный при первом доступе? Снапшот лежит в S3 как набор блоков. Когда ты создаёшь из него том, EBS не копирует всё заранее — он предъявляет том сразу и **лениво подгружает** каждый блок из S3 при первом чтении. Поэтому первый проход по восстановленной базе (полный скан таблицы, переиндексация) платит S3-round-trip за каждый холодный блок и ощущается куда медленнее, чем подсказывают выделенные IOPS тома. Как только блок тронут — он локальный и быстрый. Чтобы избежать обрыва при реальном восстановлении, либо прогрей том, прочитав его целиком, либо включи Fast Snapshot Restore, чтобы блоки были доступны на полной производительности с самого начала.
</Inset>

## EFS и instance store: общий файл против выбрасываемого локального

**EFS** — ответ, когда **многим инстансам нужны одни и те же файлы одновременно**: парк веб-серверов, отдающих одни загрузки, кластер CMS, контейнерные таски с общим персистентным состоянием. Это управляемая файловая система **NFS**, которая **охватывает несколько AZ**, **эластично автомасштабирует** ёмкость по мере записи (без выделения размера) и принимает **тысячи одновременных соединений**. Цена — физика: достучаться до неё по NFS через сеть значит **более высокую латентность, чем у локального тома EBS** (примерно единицы миллисекунд на первый байт чтения на классе Standard и десятки мс на классе Infrequent Access), и за ГБ она дороже EBS. У пропускной способности есть режимы — **Elastic** (автомасштаб, оплата за перемещённые байты, лучше для всплесковой/непредсказуемой нагрузки) и **Provisioned** (фиксируешь нижнюю границу пропускной способности независимо от размера) — а **lifecycle-политика** может состаривать холодные файлы до классов **Infrequent Access (IA)** и Archive, срезая стоимость. Ошибка, которой надо избегать, — тянуться к EFS как к тому одиночной базы: латентность NFS и отсутствие блочной семантики делают его неверным инструментом там, где место EBS.

**Instance store** — противоположный размен: **эфемерный локальный NVMe, физически подключённый к хосту**, поэтому он даёт **самые высокие IOPS и самую низкую латентность** из всего здесь — и AWS сотрёт его без церемоний. Данные **переживают перезагрузку (reboot)**, но **криптографически стираются при stop, hibernate или terminate** и просто исчезают при **отказе нижележащего диска**. Его нельзя снапшотить, нельзя отключить и переподключить, нельзя добавить после запуска. Это делает его верным ровно для одной категории: данные, которые можно позволить себе потерять, — кэши, scratch-пространство, spill/temp-файлы, буферы или данные, уже **реплицированные** по парку (узел Cassandra или Kafka, чья долговечность идёт от репликации, а не от диска). Положи на него основную базу или единственную копию чего-либо — и ты собрал ту самую военную историю из начала урока.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Измерение</th>
        <th class="border border-border px-3 py-2 text-left">EBS</th>
        <th class="border border-border px-3 py-2 text-left">EFS</th>
        <th class="border border-border px-3 py-2 text-left">Instance store</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">Тип</td>
        <td class="border border-border px-3 py-2">Сетевой блок</td>
        <td class="border border-border px-3 py-2">Файл NFS (общий)</td>
        <td class="border border-border px-3 py-2">Локальный блок (эфемерный)</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Охват</td>
        <td class="border border-border px-3 py-2">Одна AZ</td>
        <td class="border border-border px-3 py-2">Несколько AZ (Regional)</td>
        <td class="border border-border px-3 py-2">Один хост</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Подключатели</td>
        <td class="border border-border px-3 py-2">Один инстанс (io2 Multi-Attach: до 16)</td>
        <td class="border border-border px-3 py-2">Многие, одновременно</td>
        <td class="border border-border px-3 py-2">Один, фиксирован при запуске</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Долговечность</td>
        <td class="border border-border px-3 py-2">Переживает инстанс; снапшоты в S3</td>
        <td class="border border-border px-3 py-2">Переживает инстанс; мультизональна</td>
        <td class="border border-border px-3 py-2">Теряется при stop/terminate/отказе диска</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Латентность / IOPS</td>
        <td class="border border-border px-3 py-2">Низкие мс; тюнинг до 256k IOPS (io2)</td>
        <td class="border border-border px-3 py-2">Выше (NFS через сеть)</td>
        <td class="border border-border px-3 py-2">Самая низкая латентность, высшие IOPS</td>
      </tr>
    </tbody>
  </table>
</div>

<TradeoffMatrix
  id="02-block-and-file-tradeoff-1"
  lessonSlug="02-block-and-file"
  lang="ru"
  prompt="Парк контейнерных веб-серверов за балансировщиком должен читать и писать одни и те же загруженные пользователями файлы, и загрузка с одного узла должна стать видна остальным за секунды. Выбери хранилище."
  options={[
    { name: "Файловая система EFS, монтируемая каждым таском", summary: "Мультизональный NFS, многие одновременные монтировщики.", correct: true, justification: "Верный выбор. Определяющее требование — многие инстансы читают и пишут одно общее файловое дерево с межузловой видимостью, и это ровно EFS: управляемая NFS-файловая система, охватывающая несколько AZ, принимающая тысячи одновременных соединений и автомасштабирующая ёмкость по мере роста набора загрузок. Каждый узел видит каждую запись через общее пространство имён. Ты принимаешь более высокую латентность и стоимость за ГБ, чем у EBS, — это верный размен для общего контента в парке." },
    { name: "Один том EBS gp3, подключённый ко всем узлам", summary: "Быстрый блок, расшаренный на парк.", justification: "Не работает. Обычный том EBS подключается ровно к одному инстансу за раз; смонтировать его на парк нельзя. io2 Multi-Attach допускает до 16 инстансов в одной AZ, но лишь как строительный блок для кластерного ПО с собственным I/O-fencing — он даёт общее блочное устройство, а не общую файловую систему, так что два узла, пишущие один файл, портят его без кластер-осведомлённой ФС сверху. Не тот инструмент для общих файлов на весь парк." },
    { name: "Instance store на каждом узле", summary: "Самый быстрый локальный NVMe на узел.", justification: "Неверно по двум причинам. Instance store локален для каждого хоста, поэтому загрузки узла A вообще не видны узлу B — общего пространства имён нет. И он эфемерен: любой узел, который остановится или завершится, теряет свои файлы. Быстро, но не решает ни шаринг, ни долговечность, а в них всё требование." },
    { name: "Бакет S3, в который приложение читает и пишет на каждый запрос", summary: "Объектное хранилище, бесконечно общее.", justification: "Соблазнительно и иногда верно для загрузок — но нагрузка как сформулирована требует POSIX-файловой семантики (серверы читают и пишут одни файлы как смонтированный диск). S3 — объектное хранилище без частичной записи на месте и без поведения ФС; ретрофитить его значит переписать приложение под объектный API. Если приложению действительно нужна смонтированная общая ФС сейчас, EFS подходит без переписывания; S3 — ответ, только если редизайнить вокруг объектов." },
  ]}
/>

<Quiz
  id="02-block-and-file-quiz-1"
  lessonSlug="02-block-and-file"
  lang="ru"
  question="Нужно 9000 устойчивых IOPS с тома 200 ГиБ для одноузловой базы, с низкой латентностью и без сюрпризов-обрывов. Что подходит лучше всего?"
  choices={[
    { label: "gp3 с выделением 9000 IOPS независимо от размера 200 ГиБ — база 3000 IOPS бесплатно, а IOPS тюнишь без наращивания диска", correct: true },
    { label: "gp2 размером 200 ГиБ, в расчёте на бёрст", misconception: "У тома gp2 на 200 ГиБ база всего 600 IOPS (3 IOPS/ГиБ), и бёрстить до 3000 он может лишь пока есть I/O-кредиты. Удержать 9000 IOPS на gp2 такого размера невозможно, а занятый маленький gp2 исчерпывает BurstBalance и проваливается к базе — ровно тот обрыв латентности, которого надо избегать. gp3 отвязывает IOPS от размера." },
    { label: "Instance store, потому что у локального NVMe высшие IOPS", misconception: "У instance store действительно высшие сырые IOPS, но он эфемерен — единственная копия одноузловой базы будет стёрта при следующем stop, hibernate или отказе диска, и снапшотить его нельзя. Связывающее ограничение для основной базы — долговечность, а не пиковые IOPS." },
    { label: "EFS, потому что он автомасштабирует производительность", misconception: "EFS — общая NFS-файловая система с более высокой латентностью, чем локальный том EBS, и без блочной семантики; это неверная форма для латентно-чувствительной одноузловой базы. EFS — для многих инстансов, делящих файлы, а не для диска базы одного инстанса." },
  ]}
/>

<Quiz
  id="02-block-and-file-quiz-2"
  lessonSlug="02-block-and-file"
  lang="ru"
  question="Узел Cassandra хранит данные на NVMe instance store и год отлично работает. Почему это может быть разумным дизайном, а не военной историей из хука?"
  choices={[
    { label: "Cassandra реплицирует данные по узлам, поэтому долговечность кластера идёт от репликации, а не от диска — потеря эфемерного тома одного узла переживаема, ведь копии есть на других", correct: true },
    { label: "Данные instance store на самом деле переживают stop и terminate для нагрузок баз данных", misconception: "Они никогда не переживают stop, hibernate или terminate независимо от нагрузки — каждый блок криптографически стирается. Дизайн безопасен только потому, что Cassandra держит реплики на других узлах, а не потому, что том выживает." },
    { label: "Тома instance store можно снапшотить в S3, как EBS", misconception: "Instance store нельзя снапшотить, отключить или переподключить. Его безопасность тут целиком от репликации на уровне кластера, а не от какого-либо механизма бэкапа на самом томе." },
    { label: "EFS автоматически смонтирован снизу как долговечная подстраховка", misconception: "Никакой автоматической подстраховки EFS под instance store нет. Долговечность — свойство фактора репликации Cassandra по узлам, который делает потерю любого одиночного эфемерного диска восстановимой." },
  ]}
/>

</Explanation>

<KeyTakeaway>Три модели подключения, а не три скорости. EBS = долговечный блок одной AZ для одного инстанса; gp3 тюнит IOPS независимо от размера. EFS = NFS на много AZ, который шарят многие. Instance store = локальный NVMe, быстрейший, стирается при stop. База к EBS; общие файлы парка к EFS; scratch к instance store.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="02-block-and-file-retrieval"
  lang="ru"
  questions={[
    { q: "Сравни EBS, EFS и instance store по модели подключения и долговечности и скажи, когда верен каждый.", a: "Они различаются сперва тем, как подключаются и насколько долговечны, а не сырой скоростью. EBS — сетевой блок, живёт в одной зоне доступности и подключается к одному инстансу за раз (io1/io2 Multi-Attach — узкое исключение: до 16 Nitro-инстансов в одной AZ для кластерного ПО с собственным I/O-fencing). Он переживает инстанс, инкрементально снапшотится в S3, меняет размер онлайн через Elastic Volumes, а gp3 позволяет выделять IOPS (до 80 000) и пропускную способность (до 2000 МиБ/с) независимо от бесплатной базы 3000 IOPS / 125 МиБ/с. Используй его для одиночного stateful-узла — базы, брокера, загрузочного тома. EFS — управляемая NFS-файловая система, охватывает несколько AZ, автомасштабирует ёмкость и позволяет многим инстансам монтировать её одновременно с тысячами соединений; она дороже за ГБ и имеет выше латентность, чем локальный том EBS, потому что доступ через сеть, а lifecycle-политика может переместить холодные файлы в Infrequent Access. Используй её, когда парку нужно делить одни файлы. Instance store — эфемерный локальный NVMe, физически подключённый к хосту: высшие IOPS и низшая латентность, но данные криптографически стираются при stop, hibernate или terminate, исчезают при отказе диска и не снапшотятся. Используй только для кэшей, scratch, temp/spill или данных, уже реплицированных по парку." },
    { q: "Назови три классических режима отказа — потеря данных instance store, EFS-там-где-место-EBS и исчерпание бёрста gp2 — и фикс каждого.", a: "Первый — класть долговечные данные на instance store: ощущается отлично, ведь локальный NVMe — самый быстрый диск, но первый же Stop или terminate криптографически стирает каждый блок, и снапшотить нельзя, поэтому основная база или единственная копия чего-либо уничтожается тихо. Фикс: держи долговечные одноузловые данные на EBS (который переживает инстанс и снапшотится в S3), а instance store оставь под кэши, scratch или уже реплицированные данные. Второй — использовать EFS там, где нужна латентность EBS: монтирование латентно-чувствительной одноузловой базы на EFS платит латентность NFS-через-сеть и теряет блочную семантику, поэтому медленнее локального тома EBS. Фикс: EBS для диска одного инстанса, EFS — только когда многим инстансам надо делить файлы. Третий — недовыделенные burst-кредиты gp2: маленький занятый том gp2 (у gp2 на 100 ГиБ база всего 300 IOPS) бёрстит до 3000 IOPS лишь пока есть I/O-кредиты, потом проваливается к базе, когда BurstBalance исчерпан, — обрыв латентности в 2 часа ночи. Фикс: gp3, у которого нет модели бёрста и который держит базу 3000 IOPS плюс всё, что выделишь (до 80 000), независимо от размера." },
  ]}
/>

<Recap lang="ru">
EBS, EFS и instance store — три модели подключения, а не три точки на регуляторе скорости — ранжируй их сперва по подключению и долговечности. EBS — сетевой блок в одной AZ, привязанный к одному инстансу за раз (io2 Multi-Attach — узкое исключение для кластерного ПО), который переживает инстанс, инкрементально снапшотится в S3 с ленивой загрузкой при восстановлении, меняет размер онлайн, а с gp3 позволяет тюнить IOPS до 80 000 и пропускную способность до 2000 МиБ/с независимо от размера поверх бесплатной базы 3000 IOPS / 125 МиБ/с — фикс обрыва на burst-кредитах gp2. EFS — управляемая мультизональная NFS-файловая система, которую многие инстансы монтируют одновременно и которая автомасштабирует ёмкость, разменивая выше латентность и стоимость за ГБ на шаринг по всему парку, с режимами пропускной способности Elastic или Provisioned и lifecycle в Infrequent Access. Instance store — эфемерный локальный NVMe с высшими IOPS и низшей латентностью, но каждый блок стирается при stop, hibernate, terminate или отказе диска, и снапшотить его нельзя, так что он только для кэшей, scratch или реплицированных данных. Правило выбора: том одноузловой базы идёт на EBS; общий контент на парк идёт на EFS; ультранизколатентный scratch, который можно позволить себе потерять, идёт на instance store. Режимы отказа — потеря базы из-за Stop на instance store, оплата латентности NFS там, где место EBS, и провал маленького gp2 при исчерпании burst-кредитов — все идут от выбора по сырой скорости вместо подключения и долговечности. Указанные цены (gp3 около 0,08 USD за ГБ-месяц, снапшоты около 0,05 USD за ГБ-месяц в US-East-1) иллюстративны и зависят от региона — сверяйся со страницей цен EBS. Теперь, когда на ревью увидишь решение о хранилище, первые вопросы — переживёт ли оно Stop и нужен ли общий доступ на весь парк; IOPS идут третьим.
</Recap>
