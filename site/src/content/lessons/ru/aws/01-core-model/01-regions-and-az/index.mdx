---
concepts:
- regions
- availability-zones
- multi-az-high-availability
- regional-vs-global-services
- data-residency
- SAA-C03:Design Resilient Architectures
deepensInto: []
estMin: 16
lang: ru
lessonType: topic
level: middle
order: 1
prereqs: []
slug: 01-regions-and-az
sources:
- https://docs.aws.amazon.com/whitepapers/latest/aws-fault-isolation-boundaries/availability-zones-and-regions.html
- https://docs.aws.amazon.com/AmazonRDS/latest/UserGuide/Concepts.MultiAZ.html
- https://aws.amazon.com/about-aws/global-infrastructure/regions_az/
spiral: []
status: ready
summary: Инфраструктура AWS — иерархия доменов отказа и задержки. Регион — изолированная
  географическая область, зона доступности — независимый ЦОД внутри неё. Размещай
  сервис в ≥2 зонах, иначе отказ одной зоны кладёт всё.
title: Регионы и зоны доступности
track: aws
unit: 01-core-model
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
Инстансы EC2 крутились в `us-east-1a`. База RDS крутилась в `us-east-1a`. Балансировщик указывал на `us-east-1a`. Всё работало безупречно — до утра, когда одна зона доступности в `us-east-1` потеряла питание. Каждый слой стека был в этой одной зоне, поэтому весь продукт погас, пока конкурент в том же регионе, размазавший нагрузку по трём зонам, не уронил ни одного запроса. Архитектура была неправильной не потому, что была сложной. Она была неправильной, потому что у неё не было второго домена отказа.
</Hook>

<Crux>Выбирай регион ради задержки, закона и цены; размещай сервис в ≥2 зонах доступности, чтобы отказ одного ЦОД не положил тебя.</Crux>

<Explanation>

## Регион, зона, edge: иерархия отказа и задержки

Глобальное присутствие AWS — это не одно облако. Это намеренно слоёный набор границ изоляции, и твоя задача как архитектора — разместить нагрузку на правильной границе под тот радиус поражения и задержку, которые ты можешь стерпеть.

**Регион** — это независимая географическая область: `us-east-1` (Сев. Вирджиния), `eu-central-1` (Франкфурт), `ap-southeast-1` (Сингапур). Регионы изолированы друг от друга по дизайну: они не делят ЦОДы, и AWS не реплицирует твои данные между ними автоматически. Регион выбирают по трём причинам. **Задержка** — размести вычисления ближе к пользователям. **Резидентность данных и закон** — GDPR или банковский регулятор могут запретить вывоз клиентских данных из Франкфурта, а поскольку данные остаются в регионе, пока *ты сам* их не скопируешь, выбор `eu-central-1` — это уже мера комплаенса. **Цена** — один и тот же тип инстанса в разных регионах может стоить заметно по-разному.

**Зона доступности (Availability Zone, AZ)** — это один или несколько обособленных ЦОД *внутри* региона, у каждого независимые питание, охлаждение и физическая сеть, разнесённые достаточно далеко, чтобы пожар, наводнение или отказ питания в одном вряд ли задели другой. В каждом регионе минимум три зоны (у большинства — от трёх до шести). Два свойства, делающие зоны рабочей лошадкой отказоустойчивого дизайна, по дизайну в противоречии: зоны **отказывают независимо** (гарантия изоляции), но при этом соединены **высокоскоростными приватными линками с низкой задержкой** (round trip в единицы миллисекунд), так что синхронный standby базы можно держать в другой зоне без налога на задержку, который дало бы пересечение региона.

Под регионом лежат две конструкции «только про задержку», которые стоит назвать. **Edge-локации** — это точки присутствия CDN CloudFront, их сотни, гораздо больше, чем регионов, и они кэшируют контент близко к пользователям. **Local Zones** выносят кусок вычислений и хранилища в город ради нагрузок со сверхнизкой задержкой. Обе снижают задержку; ни та, ни другая не место для долговечного состояния.

<FlowDiagram
  label="Иерархия изоляции AWS: от региона вниз через его независимые зоны доступности к подсетям, которые ты размещаешь внутри каждой зоны."
  hue="var(--d-infra)"
  perRow={3}
  nodes={[
    { id: "region", label: "Регион", sub: "eu-central-1", col: 1, row: 0 },
    { id: "az-a", label: "AZ-a", sub: "набор ЦОД 1", col: 0, row: 1 },
    { id: "az-b", label: "AZ-b", sub: "набор ЦОД 2", col: 1, row: 1 },
    { id: "az-c", label: "AZ-c", sub: "набор ЦОД 3", col: 2, row: 1 },
    { id: "sn-a", label: "Подсеть a", sub: "10.0.1.0/24", col: 0, row: 2 },
    { id: "sn-b", label: "Подсеть b", sub: "10.0.2.0/24", col: 1, row: 2 },
    { id: "sn-c", label: "Подсеть c", sub: "10.0.3.0/24", col: 2, row: 2 },
  ]}
  edges={[
    { from: "region", to: "az-a" },
    { from: "region", to: "az-b" },
    { from: "region", to: "az-c" },
    { from: "az-a", to: "sn-a" },
    { from: "az-b", to: "sn-b" },
    { from: "az-c", to: "sn-c" },
  ]}
  caption="Регион содержит несколько независимых зон; подсеть живёт ровно в одной зоне. Размазывание подсетей по зонам — это и есть превращение VPC в multi-AZ, домен отказа, поверх которого ты строишь."
/>

## Региональный по умолчанию, глобальный — по исключению

Большинство сервисов AWS **региональны**: когда ты создаёшь инстанс EC2, базу RDS, очередь SQS или бакет S3, он существует в одном регионе и управляется control plane этого региона. Для отказоустойчивости из этого следует резкий вывод — региональный сервис доступен ровно настолько, насколько доступны зоны, по которым ты его размазал. **Для высокой доступности нужно проектировать минимум на две зоны доступности**, потому что развёртывание в одной зоне наследует доступность одного ЦОД, а ЦОДы отказывают.

Небольшой набор сервисов **глобален**, с единым control plane на все регионы: **IAM** (твои пользователи, роли и политики действуют на весь аккаунт), **Route 53** (DNS с health-чеками, способными переключать трафик между регионами) и **CloudFront** (CDN, отдаваемый из edge-локаций повсюду). Понимание, в какую корзину попадает сервис, говорит тебе, где его домен отказа и нужно ли вообще думать про multi-Region для него.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Область</th>
        <th class="border border-border px-3 py-2 text-left">Примеры</th>
        <th class="border border-border px-3 py-2 text-left">Домен отказа</th>
        <th class="border border-border px-3 py-2 text-left">Что ты обязан сделать</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">Зональный</td>
        <td class="border border-border px-3 py-2">инстанс <code>EC2</code>, том EBS, одна подсеть</td>
        <td class="border border-border px-3 py-2">Одна зона</td>
        <td class="border border-border px-3 py-2">Сам реплицируй ресурс в ≥2 зоны</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Региональный</td>
        <td class="border border-border px-3 py-2"><code>S3</code>, SQS, DynamoDB, RDS Multi-AZ, ALB</td>
        <td class="border border-border px-3 py-2">Покрывает зоны одного региона</td>
        <td class="border border-border px-3 py-2">Включи/настрой его multi-AZ опцию; выбери регион под закон и задержку</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Глобальный</td>
        <td class="border border-border px-3 py-2">IAM, Route&nbsp;53, CloudFront</td>
        <td class="border border-border px-3 py-2">Все регионы</td>
        <td class="border border-border px-3 py-2">Ничего ради зональной устойчивости; используется для маршрутизации между регионами</td>
      </tr>
    </tbody>
  </table>
</div>

<Inset kind="why" lang="ru">
Почему AWS просто не реплицирует всё между регионами за тебя? Потому что межрегиональная репликация — это трейдофф, который ты обязан взять на себя, а не дефолт. Копирование данных во второй регион добавляет **межрегиональную сетевую задержку** (десятки-сотни миллисекунд, намного выше линка в единицы мс между зонами) и **стоимость egress** (платишь за гигабайт за вынос данных из региона). Это ещё и может сломать комплаенс, который держится на том, что данные *никогда* не покидают свой регион. Поэтому AWS держит твои данные внутри одного региона, пока ты явно не включишь репликацию, — делая «остаться в регионе» безопасным, дешёвым и законным дефолтом, а «уйти в multi-Region» — намеренным, посчитанным по деньгам решением ради disaster recovery или глобальной задержки.
</Inset>

## Конкретный multi-AZ дизайн

Отказоустойчивость перестаёт быть абстракцией в тот момент, когда ты рисуешь VPC. Возьми классический трёхуровневый веб-сервис в `eu-central-1` и размажь его по двум зонам (три лучше для систем на кворуме):

- **VPC** с одной подсетью на зону на уровень — скажем, публичная подсеть в `eu-central-1a` и `eu-central-1b` под балансировщик и приватная подсеть в каждой зоне под app-серверы и базу.
- **Application Load Balancer (ALB)**, подключённый к *обеим* публичным подсетям. ALB — региональный сервис, запускающий узлы в каждой включённой зоне; он делает health-чеки таргетов и перестаёт маршрутизировать в зону, чьи инстансы нездоровы.
- **Auto Scaling group** из app-серверов, размазанная по обеим приватным подсетям, чтобы ёмкость пережила потерю одной зоны.
- **RDS Multi-AZ**: primary в `eu-central-1a` с **синхронным standby** в `eu-central-1b`. AWS реплицирует записи на standby и при отказе primary автоматически делает failover, переключая DNS-имя базы на standby — обычно за минуту-две, без потери подтверждённых данных, потому что репликация была синхронной.

Сценарий отказа, против которого это защищает, конкретен: если `eu-central-1a` гаснет, ALB перестаёт слать трафик на нездоровые таргеты, в Auto Scaling group остаются серверы в `eu-central-1b`, а RDS повышает standby. Продукт остаётся на живой зоне. Сравни с версией из хука на одной зоне: каждый уровень в одной зоне означает, что **отказ зоны — это полный отказ**, переключаться не на что. Это ровно та отказоустойчивость, которую проверяет домен SAA-C03 «Design Resilient Architectures»: верный ответ почти всегда включает размазывание нагрузки по нескольким зонам.

<TradeoffMatrix
  id="01-regions-and-az-tradeoff-1"
  lessonSlug="01-regions-and-az"
  lang="ru"
  prompt="Регулируемому финтеху из ЕС нужна высокодоступная прод-база. Клиентские данные обязаны оставаться в ЕС. Какое развёртывание подходит лучше?"
  options={[
    { name: "RDS Multi-AZ в eu-central-1 (primary в одной зоне, синхронный standby в другой)", summary: "HA внутри одного региона ЕС.", correct: true, justification: "Это стандартный сеньорский ответ. Синхронный standby во второй зоне даёт автоматический failover без потери подтверждённых данных при отказе зоны primary, поэтому отказ одного ЦОД переживаем. Поскольку оба узла живут в eu-central-1, клиентские данные не покидают ЕС — требование резидентности удовлетворяется самим выбором региона. Зоны соединены приватной сетью с задержкой в единицы мс, так что синхронная репликация не вводит налог на задержку. Это закрывает HA и комплаенс одновременно." },
    { name: "Один большой инстанс RDS в eu-central-1a с ежедневными снапшотами", summary: "Одна зона, только бэкапы.", justification: "По резидентности соответствует, но не высокодоступно. Всё сидит в одной зоне, поэтому отказ этой зоны — полный отказ, а восстановление из ежедневного снапшота теряет до суток записей и занимает куда больше времени, чем автоматический failover. Снапшоты — это disaster recovery, а не высокая доступность; они не защищают от повседневного отказа одной зоны, о котором, по сути, и спрашивают." },
    { name: "Primary RDS в eu-central-1 с синхронным standby в us-east-1", summary: "Межрегиональный синхронный standby.", justification: "Две проблемы. Во-первых, репликация клиентских данных в us-east-1 выносит их за пределы ЕС, ломая требование резидентности. Во-вторых, синхронная репликация между регионами платит десятки-сотни мс межрегиональной задержки на каждой записи, убивая пропускную способность — синхронный standby это внутрирегиональный, межзональный паттерн, а не межрегиональный." },
    { name: "Инстансы EC2 со своей самописной базой, все в eu-central-1a за одним ALB", summary: "Самоуправляемо, одна зона.", justification: "Ты берёшь на себя репликацию, failover и патчинг, и всё равно разместил всё в одной зоне — поэтому отказ зоны это полный отказ, несмотря на операционную нагрузку. ALB может покрывать зоны, но ему некуда маршрутизировать, если все узлы базы в той единственной зоне, что упала. Это ловушка одной зоны плюс лишняя работа." },
  ]}
/>

<Quiz
  id="01-regions-and-az-quiz-1"
  lessonSlug="01-regions-and-az"
  lang="ru"
  question="Весь твой стек — EC2, RDS и балансировщик — крутится в одной зоне доступности. Эта зона теряет питание. Что произойдёт и в чём ошибка дизайна?"
  choices={[
    { label: "Всё приложение ляжет, потому что каждый уровень делил один домен отказа; фикс — размазать вычисления, базу (Multi-AZ) и балансировщик минимум по двум зонам", correct: true },
    { label: "Ничего — AWS автоматически перенесёт твои ресурсы в другую зону при отказе одной", misconception: "AWS не переносит твои зональные ресурсы молча. Инстанс EC2 или RDS на одной зоне живёт в одной зоне; если эта зона отказала, ресурс недоступен. Устойчивость между зонами это то, что ты проектируешь, а не то, что AWS делает за тебя по умолчанию." },
    { label: "Ляжет только база; EC2 и балансировщик продолжат обслуживать", misconception: "Все три уровня были в одной зоне, поэтому все три разом теряют свой ЦОД. Выжившего уровня нет — отказ полный. Независимый отказ помогает, только если ты реально разместил ресурсы в разных зонах." },
    { label: "Route 53 автоматически переключит трафик на другой регион", misconception: "Route 53 может переключать между регионами, только если ты построил второй регион для переключения и настроил health-чеки. У развёртывания в одном регионе и одной зоне нет резервной цели, поэтому DNS-failover некуда слать трафик." },
  ]}
/>

</Explanation>

<KeyTakeaway>Регион = изолированная географическая область (выбирай под задержку, закон, цену; данные остаются в нём, пока ты не реплицируешь). Зона = независимый ЦОД внутри региона, соединённый линками с низкой задержкой. Большинство сервисов региональны, поэтому размещай в ≥2 зонах, иначе отказ одной зоны — полный отказ. IAM, Route 53, CloudFront глобальны.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="01-regions-and-az-retrieval"
  lang="ru"
  questions={[
    { q: "Почему региональный сервис нужно проектировать минимум на две зоны доступности и что делает RDS Multi-AZ, когда его зона отказывает?", a: "Региональный сервис вроде RDS или EC2 в конечном счёте работает в физических ЦОД, а зона доступности — это граница отказа для этих ЦОД: у каждой зоны независимые питание, охлаждение и сеть, поэтому одна может отказать, пока остальные работают. Если положить всю нагрузку в одну зону, ты наследуешь доступность одного ЦОД — когда у этой зоны отказ питания или сети, твой ресурс просто недоступен и переключаться не на что, поэтому стек из хука, целиком в одной зоне, погас полностью. Размещение минимум в двух зонах означает, что потеря одной зоны оставляет живую копию. RDS Multi-AZ реализует ровно это: он держит primary в одной зоне и синхронный standby в другой, реплицируя каждую подтверждённую запись на standby. Когда зона primary отказывает, RDS автоматически делает failover, переключая DNS-endpoint базы на standby — обычно за минуту-две — и, поскольку репликация была синхронной, потери подтверждённых данных нет. Зоны соединены приватной сетью с задержкой в единицы миллисекунд, поэтому синхронная репликация между ними не вводит ощутимого штрафа на задержку, почему это и внутрирегиональный, межзональный паттерн, а не межрегиональный." },
    { q: "Чем регион отличается от зоны, какие сервисы AWS глобальны, а не региональны, и почему резидентность данных следует из выбора региона?", a: "Регион — это независимая географическая область, например eu-central-1 или us-east-1; регионы не делят ЦОДы, и AWS не реплицирует твои данные между ними автоматически. Зона доступности — это один или несколько обособленных ЦОД внутри одного региона, изолированных ради независимого отказа, но соединённых с соседними зонами высокоскоростными приватными линками с низкой задержкой. То есть регион — грубая граница, которую ты выбираешь под задержку, закон и цену, а зона — тонкая граница, по которой ты размазываешь сервис ради высокой доступности. Большинство сервисов региональны — EC2, RDS, S3, SQS, DynamoDB — то есть существуют в одном регионе и доступны ровно настолько, насколько доступны зоны, по которым ты их размазал. Небольшой набор глобален, с единым control plane на все регионы: IAM (пользователи, роли, политики на весь аккаунт), Route 53 (DNS и межрегиональный failover) и CloudFront (CDN, отдаваемый из edge-локаций). Резидентность данных следует из выбора региона, потому что твои данные физически остаются внутри региона, в котором ты создал ресурс, пока ты явно не реплицируешь их в другое место, — поэтому выбор eu-central-1 без репликации наружу сам по себе мера комплаенса, держащая данные клиентов ЕС в ЕС, тогда как уход в multi-Region это намеренное решение, добавляющее межрегиональную задержку и стоимость egress за гигабайт." },
  ]}
/>

<Recap lang="ru">
Глобальная инфраструктура AWS — это вложенный набор доменов отказа и задержки. Регион — изолированная географическая область; регионы не делят ЦОДы, и твои данные остаются внутри одного, пока ты не реплицируешь их, что делает выбор региона твоим рычагом для задержки, закона (резидентность данных) и цены. Внутри каждого региона минимум три зоны доступности — обособленные ЦОД с независимыми питанием, охлаждением и сетью, но соединённые приватными линками с задержкой в единицы миллисекунд, так что они отказывают независимо и при этом поддерживают синхронную репликацию между собой. Большинство сервисов AWS региональны и доступны ровно настолько, насколько доступны зоны, по которым ты их размазал, поэтому высокая доступность означает проектирование минимум на две зоны: подсети по зонам, ALB, покрывающий их, Auto Scaling group из app-серверов в каждой и RDS Multi-AZ с синхронным standby, делающим failover автоматически. Небольшой набор сервисов глобален — IAM, Route 53, CloudFront. Сценарий, которого стоит бояться, — развёртывание на одной зоне: когда эта зона гаснет, гаснет и весь продукт, потому что нет второго домена отказа, на который можно опереться. Multi-Region — отдельный, намеренный выбор, покупающий disaster recovery и глобальную задержку ценой межрегиональной задержки и стоимости egress, — ровно тот тип рассуждений об отказоустойчивой архитектуре, который ждёт SAA-C03.
</Recap>
