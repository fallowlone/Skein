---
concepts:
- recursion
- base_case
- recursive_case
estMin: 18
lang: ru
mathPrereqs: []
order: 1
prereqs:
- 02-arrays-strings
slug: 01-what-is-recursion
sources:
- https://en.wikipedia.org/wiki/Recursion_(computer_science)
- https://www.geeksforgeeks.org/dsa/what-is-base-case-in-recursion/
status: ready
summary: 'Функция, вызывающая саму себя для решения меньшей версии той же задачи.
  Две обязательные части: базовый случай (остановиться) и рекурсивный случай (сжать
  и вызвать себя).'
title: Что такое рекурсия?
track: algorithms
unit: 04-recursion-backtracking
---



import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Idea from "~/components/lesson/Idea.astro";
import Code from "~/components/lesson/Code.astro";
import Trace from "~/components/lesson/Trace.astro";
import Complexity from "~/components/lesson/Complexity.astro";
import Check from "~/components/lesson/Check.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import AnnotatedCode from "~/components/algo/AnnotatedCode.astro";
import AlgoTrace from "~/components/algo/AlgoTrace.astro";
import CodeRunner from "~/components/algo/CodeRunner.astro";
import AlgoPractice from "~/components/algo/AlgoPractice.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DiagramFrame from "~/components/diagram/DiagramFrame.astro";

<Hook>
Представь себе поиск выхода из лабиринта. Одна тактика: на каждом перекрёстке пробуй один путь, и если тот упирается в тупик, вернись и попробуй другой. Ты применяешь одну и ту же стратегию (исследовать, откатиться, повторить) снова и снова. В этом суть рекурсии — функция, которая решает задачу, вызывая саму себя на меньшей версии той же задачи, доверяя, что она сработает для меньшей версии так же, как для целого.
</Hook>

<Goal lang="ru">
После этого урока ты поймёшь, что такое рекурсия: как функция может вызвать саму себя, что такое базовый случай и рекурсивный случай и почему оба обязательны, как работает рекурсивный прыжок веры (доверься, что рекурсивный вызов сделает правильное для меньшего входа), и когда рекурсия сияет — когда задача естественно разбивается на меньшие копии самой себя.
</Goal>

<Idea lang="ru">
**Что такое рекурсия?**

К концу этого раздела ты будешь точно знать, когда брать рекурсию и почему она работает — следующие уроки строятся прямо поверх этого.

**Рекурсия** — это когда функция вызывает саму себя для решения меньшей версии той же задачи. Вместо решения задачи целиком ты разбиваешь её на две части:

1. **Базовый случай:** Версия задачи настолько маленькая, что ты можешь ответить на неё сразу, без рекурсии.
2. **Рекурсивный случай:** Функция вызывает саму себя на меньшем входе, потом использует тот ответ, чтобы построить ответ для твоего текущего входа.

Обе части обязательны. Без базового случая функция вызывает себя бесконечно. Без сжимающегося рекурсивного случая ты никогда не достигнешь базовый — ты зацикливаешься иначе. Вместе они образуют контракт: базовый случай гарантирует остановку, рекурсивный — прогресс к ней. Нарушь любой — функция никогда не вернётся.

**Две обязательные части**

Посмотрим на конкретный пример: вычисление факториала числа.

Факториал определён как: 5! = 5 × 4 × 3 × 2 × 1 = 120. В общем виде, n! = n × (n - 1)!.

<AnnotatedCode
  code={`function factorial(n) {
  // Базовый случай: остановиться здесь
  if (n <= 1) {
    return 1;
  }
  // Рекурсивный случай: сжать и вызвать себя
  return n * factorial(n - 1);
}`}
  annotations={[
    { line: 2, text: "Базовый случай: самый маленький вход, который мы обрабатываем напрямую. Без рекурсии." },
    { line: 5, text: "Рекурсивный случай: вызвать factorial на меньшем входе, потом использовать результат." }
  ]}
/>

Почему это работает? Когда ты вызываешь `factorial(4)`, она говорит: «Мне нужно 4 × factorial(3).» Ты доверяешь, что `factorial(3)` вернёт 6 (правильный ответ для 3!). Потом ты умножаешь 4 × 6 = 24. Это доверие — что рекурсивный вызов сделает правильное для меньшего входа — называется **рекурсивным прыжком веры**.

**Почему прыжок веры работает**

Ключевая идея: каждый рекурсивный вызов передаёт меньший вход. В конце концов, вход сжимается до 1 (или 0), и базовый случай его ловит. Тогда цепь вызовов разворачивается, каждый строит свой ответ из ответа меньшего ниже.

Проследим `factorial(4)`:

```
factorial(4) → нужно 4 × factorial(3)
  factorial(3) → нужно 3 × factorial(2)
    factorial(2) → нужно 2 × factorial(1)
      factorial(1) → базовый случай, return 1
    factorial(2) → return 2 × 1 = 2
  factorial(3) → return 3 × 2 = 6
factorial(4) → return 4 × 6 = 24
```

**Рекурсия vs итерация**

Много задач можно решить обоими способами. Например, факториал можно вычислить цикло́м:

```
result = 1
for i from 2 to 4:
  result = result × i
return result
```

Это итеративно (используется цикл, функция не вызывает саму себя). Это использует меньше памяти (нет нескольких вызовов функции на стеке) и иногда быстрее. Но это менее интуитивно, когда задача естественно самоподобна — как обход деревьев, где каждый узел содержит меньшие поддеревья, или merge sort, что расщепляет массив пополам рекурсивно.

Рекурсия сияет, когда структура задачи отражает рекурсию: задача разбивается на меньшие копии самой себя. Деревья, графы, перестановки и алгоритмы разделяй-и-властвуй естественны для рекурсии.
</Idea>

<DiagramFrame
  label="Рекурсия factorial(4). Идя ВНИЗ по левому столбцу, каждый вызов делегирует меньшей копии самого себя: factorial(4) вызывает factorial(3) вызывает factorial(2) вызывает factorial(1). factorial(1) — это базовый случай и возвращает 1 напрямую, без дальнейшего вызова. Затем идя ВВЕРХ по правому столбцу, каждый вызов комбинирует возвращённое значение: 1 становится 2 умножить на 1 равно 2, становится 3 умножить на 2 равно 6, становится 4 умножить на 6 равно 24."
  hue="var(--d-systems)"
  caption="Вниз слева: сжимаемся к базовому случаю (каждый вызов делегирует меньшей копии). Вверх справа: комбинируем возвраты (каждый уровень умножает n на ответ ниже). Базовый случай factorial(1) = 1 — это шарнир, где спуск превращается в подъём с комбинированием."
>
  <svg viewBox="0 0 520 440" width="520" height="440" font-family="var(--font-mono, monospace)" role="presentation">
    <defs>
      <marker id="rec-arrow-ru" viewBox="0 0 10 10" refX="9" refY="5" markerWidth="7" markerHeight="7" orient="auto-start-reverse">
        <path d="M 0 0 L 10 5 L 0 10 z" fill="var(--d-systems)" />
      </marker>
    </defs>

    <text x="120" y="26" text-anchor="middle" font-size="13" font-weight="700" fill="var(--ink, #111)">{"вызов вниз (сжатие)"}</text>
    <text x="400" y="26" text-anchor="middle" font-size="13" font-weight="700" fill="var(--ink, #111)">{"возврат вверх (комбинирование)"}</text>

    <rect x="40" y="44" width="160" height="48" rx="8" fill="var(--card, #fff)" stroke="var(--d-systems)" stroke-width="2" />
    <text x="120" y="74" text-anchor="middle" font-size="15" fill="var(--ink, #111)">{"factorial(4)"}</text>

    <rect x="64" y="134" width="160" height="48" rx="8" fill="var(--card, #fff)" stroke="var(--d-systems)" stroke-width="2" />
    <text x="144" y="164" text-anchor="middle" font-size="15" fill="var(--ink, #111)">{"factorial(3)"}</text>

    <rect x="88" y="224" width="160" height="48" rx="8" fill="var(--card, #fff)" stroke="var(--d-systems)" stroke-width="2" />
    <text x="168" y="254" text-anchor="middle" font-size="15" fill="var(--ink, #111)">{"factorial(2)"}</text>

    <rect x="112" y="314" width="200" height="56" rx="8" fill="var(--d-systems)" stroke="var(--d-systems)" stroke-width="2" />
    <text x="212" y="338" text-anchor="middle" font-size="15" font-weight="700" fill="#fff">{"factorial(1) = 1"}</text>
    <text x="212" y="357" text-anchor="middle" font-size="12" fill="#fff">{"БАЗОВЫЙ СЛУЧАЙ — return 1"}</text>

    <line x1="120" y1="92" x2="144" y2="132" stroke="var(--d-systems)" stroke-width="2" marker-end="url(#rec-arrow-ru)" />
    <line x1="144" y1="182" x2="168" y2="222" stroke="var(--d-systems)" stroke-width="2" marker-end="url(#rec-arrow-ru)" />
    <line x1="168" y1="272" x2="200" y2="312" stroke="var(--d-systems)" stroke-width="2" marker-end="url(#rec-arrow-ru)" />

    <text x="116" y="118" text-anchor="start" font-size="11" fill="var(--muted, #666)">{"нужно 4 × factorial(3)"}</text>
    <text x="140" y="208" text-anchor="start" font-size="11" fill="var(--muted, #666)">{"нужно 3 × factorial(2)"}</text>
    <text x="164" y="298" text-anchor="start" font-size="11" fill="var(--muted, #666)">{"нужно 2 × factorial(1)"}</text>

    <line x1="312" y1="342" x2="392" y2="262" stroke="var(--d-systems)" stroke-width="2" marker-end="url(#rec-arrow-ru)" />
    <line x1="392" y1="244" x2="392" y2="184" stroke="var(--d-systems)" stroke-width="2" marker-end="url(#rec-arrow-ru)" />
    <line x1="392" y1="166" x2="392" y2="106" stroke="var(--d-systems)" stroke-width="2" marker-end="url(#rec-arrow-ru)" />

    <rect x="352" y="224" width="120" height="40" rx="8" fill="var(--card, #fff)" stroke="var(--d-systems)" stroke-width="1" />
    <text x="412" y="249" text-anchor="middle" font-size="13" fill="var(--ink, #111)">{"2 × 1 = 2"}</text>

    <rect x="352" y="144" width="120" height="40" rx="8" fill="var(--card, #fff)" stroke="var(--d-systems)" stroke-width="1" />
    <text x="412" y="169" text-anchor="middle" font-size="13" fill="var(--ink, #111)">{"3 × 2 = 6"}</text>

    <rect x="352" y="64" width="120" height="40" rx="8" fill="var(--card, #fff)" stroke="var(--d-systems)" stroke-width="1" />
    <text x="412" y="89" text-anchor="middle" font-size="13" font-weight="700" fill="var(--ink, #111)">{"4 × 6 = 24"}</text>

    <text x="486" y="89" text-anchor="start" font-size="12" font-weight="700" fill="var(--d-systems)">{"← результат"}</text>

    <text x="318" y="392" text-anchor="middle" font-size="11" fill="var(--muted, #666)">{"return 1"}</text>
  </svg>
</DiagramFrame>

<Code lang="ru">
Кодим рекурсию на JavaScript и строим интуицию с помощью двух примеров.

**Пример 1: Факториал**

```js
function factorial(n) {
  // Базовый случай: n <= 1 возвращает 1
  if (n <= 1) {
    return 1;
  }
  // Рекурсивный случай: n * factorial(n - 1)
  return n * factorial(n - 1);
}

console.log(factorial(4)); // 24
console.log(factorial(5)); // 120
```

**Пример 2: Сумма целых чисел от 1 до n**

Вместо цикла используй рекурсию:

```js
function sumTo(n) {
  // Базовый случай: сумма до 1 это 1
  if (n <= 1) {
    return 1;
  }
  // Рекурсивный случай: n + сумма всего ниже n
  return n + sumTo(n - 1);
}

console.log(sumTo(5)); // 5 + 4 + 3 + 2 + 1 = 15
console.log(sumTo(10)); // 55
```

Паттерн одинаков: базовый случай для самого маленького входа, рекурсивный случай, что сжимает вход и вызывает саму себя.

Попробуй обе функции:

<CodeRunner
  id="cr-recursion-1"
  lang="ru"
  code={`function factorial(n) {
  if (n <= 1) return 1;
  return n * factorial(n - 1);
}

function sumTo(n) {
  if (n <= 1) return 1;
  return n + sumTo(n - 1);
}

console.log("factorial(4) =", factorial(4));
console.log("factorial(5) =", factorial(5));
console.log("sumTo(5) =", sumTo(5));
console.log("sumTo(10) =", sumTo(10));
`}
/>
</Code>

<Trace lang="ru">
Проследим `factorial(4)` шаг за шагом, показывая разворачивание рекурсивных вызовов.

<AlgoTrace
  id="trace-factorial"
  lang="ru"
  code={[
    "function factorial(n) {",
    "  if (n <= 1) return 1;",
    "  return n * factorial(n - 1);",
    "}"
  ]}
  frames={[
    {
      caption: "Вызовом factorial(4). 4 > 1, поэтому входим в рекурсивный случай.",
      line: 3,
      cells: ["n=4"]
    },
    {
      caption: "Вызовом factorial(3). 3 > 1, поэтому входим в рекурсивный случай.",
      line: 3,
      cells: ["n=4", "n=3"]
    },
    {
      caption: "Вызовом factorial(2). 2 > 1, поэтому входим в рекурсивный случай.",
      line: 3,
      cells: ["n=4", "n=3", "n=2"]
    },
    {
      caption: "Вызовом factorial(1). 1 <= 1, поэтому попадаем в базовый случай. Return 1.",
      line: 2,
      cells: ["n=4", "n=3", "n=2", "n=1→1"]
    },
    {
      caption: "factorial(2) получает 1. Вычисляем 2 * 1 = 2. Return 2.",
      line: 3,
      cells: ["n=4", "n=3", "n=2→2"]
    },
    {
      caption: "factorial(3) получает 2. Вычисляем 3 * 2 = 6. Return 6.",
      line: 3,
      cells: ["n=4", "n=3→6"]
    },
    {
      caption: "factorial(4) получает 6. Вычисляем 4 * 6 = 24. Return 24.",
      line: 3,
      cells: ["n=4→24"]
    }
  ]}
/>
</Trace>

<Complexity lang="ru">
**Время:** Каждый рекурсивный вызов обрабатывает одно значение входа. Для `factorial(n)` есть n вызовов (от n вниз до 1), каждый делает постоянную работу (одно умножение и одно сравнение). Итого: O(n) время.

**Место:** Каждый вызов функции занимает место на стеке вызовов. Самая глубокая рекурсия идёт от n до 1, поэтому глубина стека O(n). Это означает O(n) памяти для стека.

Контрастируй это с итеративным цикло́м: O(n) время, но O(1) памяти. Рекурсия меняет память на ясность, когда задача естественно рекурсивна.

**Правило большого пальца:** Используй рекурсию, когда задача естественно разбивается на меньшие копии самой себя (деревья, графы, backtracking). Более чистая логика стоит дополнительной памяти стека — если только рекурсия не очень глубокая (сотни тысяч вызовов), что может переполнить стек.
</Complexity>

<AlgoPractice
  id="practice-recursion-1"
  lessonSlug="01-what-is-recursion"
  lang="ru"
  problems={[
    {
      kind: "numeric",
      prompt: "Сколько рекурсивных вызовов происходит при вызове factorial(5)?",
      answer: 5,
      hint: "factorial(5) вызывает factorial(4), что вызывает factorial(3), и т.д., вниз до factorial(1). Это 5 вызовов всего."
    },
    {
      kind: "choice",
      prompt: "Что случится, если ты забудешь базовый случай?",
      choices: [
        "Функция возвращает 0",
        "Функция зацикливается бесконечно, пока стек не переполнится",
        "Функция возвращает -1",
        "Компилятор ловит ошибку перед запуском"
      ],
      answer: 1,
      hint: "Без базового случая нет точки остановки. Функция вызывает себя бесконечно, и в конце концов стек вызовов исчерпывается (stack overflow)."
    },
    {
      kind: "choice",
      prompt: "В функции sumTo, что такое базовый случай?",
      choices: [
        "if (n <= 1) return 1;",
        "return n + sumTo(n - 1);",
        "if (n > 1) return n;",
        "return sumTo(n - 2);"
      ],
      answer: 0,
      hint: "Базовый случай — это условие, что останавливает рекурсию. Это оператор if, что обрабатывает самый маленький вход без повторного вызова функции."
    },
    {
      kind: "choice",
      prompt: "Почему factorial(n) вызывает factorial(n - 1), а не factorial(n) снова?",
      choices: [
        "Потому что factorial(n) и factorial(n - 1) одно и то же",
        "Потому что сжимание входа приближает тебя к базовому случаю. Вызвать factorial(n) снова зацикливает бесконечно.",
        "Потому что компилятор это не позволяет",
        "Потому что n - 1 всегда меньше"
      ],
      answer: 1,
      hint: "Рекурсивный случай должен передать меньший вход, гарантированно приближающий к базовому случаю. Вызвать factorial(n) снова не сжало бы задачу — вход остаётся тем же, и ты зацикливаешься бесконечно."
    },
    {
      kind: "order",
      prompt: "Расставь шаги, что происходят при вызове factorial(3):",
      items: [
        "factorial(3) вызывает factorial(2)",
        "factorial(2) вызывает factorial(1)",
        "factorial(1) попадает в базовый случай, возвращает 1",
        "factorial(2) получает 1, вычисляет 2 * 1 = 2, возвращает 2",
        "factorial(3) получает 2, вычисляет 3 * 2 = 6, возвращает 6"
      ],
      hint: "Рекурсия идёт вниз первой (вызовы продолжают сжимать вход), потом поднимается обратно (каждый вызов вычисляет и возвращает)."
    }
  ]}
/>

<Inset kind="mistake" lang="ru">
**Ошибка: нет базового случая.** Эта функция не имеет базового случая:

```js
function badFactorial(n) {
  return n * badFactorial(n - 1);
}
```

Она вызывает себя бесконечно. Хотя n сжимается, нет условия остановки. В конце концов стек вызовов переполняется и программа падает. **Каждая рекурсивная функция ДОЛЖНА иметь базовый случай.**
</Inset>

<Inset kind="mistake" lang="ru">
**Ошибка: вход не сжимается.** Эта функция вызывает себя на том же входе:

```js
function badLoop(n) {
  if (n <= 0) return 1;
  return badLoop(n); // БАГ: n не сжимается
}
```

Хотя есть базовый случай, рекурсивный вызов передаёт тот же n, не n - 1. Базовый случай никогда не достигается (или только если вход уже ≤ 0). **Рекурсивный случай должен сжимать вход.**
</Inset>

<Check lang="ru">
  <Quiz
    id="quiz-recursion-1"
    pieceSlug="01-what-is-recursion"
    lang="ru"
    question="Ты пишешь рекурсивную функцию, что считает вниз от n до 1. Что должно быть истинным?"
    choices={[
      {
        label: "Она имеет базовый случай (остановиться на 1) и рекурсивный случай сжимает вход (n - 1)",
        correct: true
      },
      {
        label: "Она должна быть быстрее, чем цикл",
        misconception: "Рекурсия и цикло́м часто имеют одинаковую временн́ую сложность. Рекурсия меняет место на ясность, не скорость."
      },
      {
        label: "Она должна иметь больше одного базового случая",
        misconception: "Одного базового случая обычно достаточно. Тебе нужен хотя бы один, не больше одного."
      },
      {
        label: "Это работает только, если ты используешь конкретный язык программирования",
        misconception: "Рекурсия — это концепция, что работает на любом языке. Паттерн одинаков: функция вызывает саму себя на меньшем входе."
      }
    ]}
  />
</Check>

<Recap lang="ru">
**Рекурсия** — это когда функция вызывает саму себя для решения меньшей версии той же задачи.

**Базовый случай:** Самый маленький вход, что функция может обработать напрямую, без рекурсии. Пример: `if (n <= 1) return 1;`

**Рекурсивный случай:** Функция вызывает саму себя на меньшем входе, потом использует результат, чтобы построить ответ для текущего входа. Пример: `return n * factorial(n - 1);`

**Оба обязательны:** Нет базового случая → бесконечная рекурсия (падение). Вход не сжимается → бесконечная рекурсия (падение).

**Рекурсивный прыжок веры:** Доверься, что рекурсивный вызов возвращает правильный ответ для меньшего входа. Это позволяет тебе сосредоточиться на: «Дан ответ для n - 1, как я построю ответ для n?»

**Рекурсия vs итерация:** Обе могут решить ту же задачу. Итерация более экономна по памяти (нет дополнительного стека вызовов). Рекурсия более интуитивна для задач с рекурсивной структурой (деревья, графы, backtracking).

**Почему рекурсия важна:** Деревья естественно зовут рекурсию (каждое поддерево — меньшее дерево). Merge sort и quicksort проще понять рекурсивно. Backtracking исследует все пути, вызывая себя на каждом выборе. Следующие уроки учат стек вызовов, backtracking и обход деревьев — всё построено на этом основании.

Теперь, когда встретишь функцию, вызывающую саму себя, ищи в первую очередь эти две части: где она останавливается и как каждый вызов уменьшает задачу? Если что-то из этого отсутствует — ты нашёл баг ещё до запуска кода.
</Recap>
