---
concepts:
- base-case
- recursive-case
- call-stack
- recursion-tree
- backtracking
- pruning
estMin: 13
lang: ru
level: middle
order: 8
prereqs: []
slug: quiz-short
sources:
- https://en.wikipedia.org/wiki/Recursion_(computer_science)
- https://www.geeksforgeeks.org/dsa/introduction-to-backtracking-2/
status: ready
summary: "Промпты на свободное припоминание по всему юниту. Сначала ответьте своими словами, затем откройте модельный ответ и сравните."
title: 'Рекурсия и backtracking: тест на припоминание'
track: algorithms
unit: 04-recursion-backtracking
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";

<Hook>
Припоминание лучше перечитывания. На каждый промпт скажите или запишите полный ответ по памяти, прежде чем откроете модельный ответ. Именно усилие извлечения закрепляет материал.
</Hook>

<Goal lang="ru">
Восстановите по памяти ключевые механизмы юнита: две обязательные части рекурсии, как call stack превращает глубину в память, как дерево рекурсии превращает ветвление во время, и шаблон backtracking с pruning.
</Goal>

<RetrievalDrawer
  client:visible
  id="04-recursion-backtracking-short-retrieval"
  lang="ru"
  questions={[
    { q: "Каковы две обязательные части любой рекурсивной функции и что идёт не так, если одной из них нет?", a: "Каждой рекурсии нужны base case и recursive case. Base case — это вход настолько малый, что на него можно ответить напрямую без дальнейшей рекурсии (например, factorial(1) возвращает 1); recursive case вызывает функцию на строго меньшем входе и комбинирует этот результат. Если base case отсутствует, функция вызывает себя бесконечно и стек переполняется. Если recursive case на самом деле не сужает вход к base case (передаёт тот же или больший аргумент), base case никогда не достигается, и вы тоже получаете переполнение. И корректность, и завершение зависят от наличия base case И реального прогресса к нему на каждом рекурсивном вызове." },
    { q: "Объясните recursive leap of faith и почему он упрощает написание рекурсивного кода.", a: "Recursive leap of faith — это доверие к тому, что рекурсивный вызов вернёт правильный ответ для меньшего входа, без мысленной трассировки каждого вложенного вызова. Когда вы пишете factorial(n), вы предполагаете, что factorial(n - 1) уже корректно возвращает (n-1)!, поэтому вам остаётся написать лишь n * factorial(n - 1). Это работает, потому что каждый вызов передаёт строго меньший вход, значит, цепочка обязана достичь base case, а индукция гарантирует: если base case верен и каждый шаг корректно комбинирует меньший ответ, то верно и всё целиком. Это позволяет рассуждать об одном уровне за раз вместо всего дерева вызовов." },
    { q: "Как call stack определяет пространственную сложность рекурсивной функции и что такое stack overflow?", a: "Каждый вызов функции кладёт на стек фрейм с её параметрами, локальными переменными и адресом возврата; фрейм снимается, когда вызов возвращается. Во время рекурсии одновременно живы фреймы всех вызовов на текущем пути корень-лист, поэтому пиковое число фреймов равно максимальной глубине рекурсии. Это делает пространственную сложность O(максимальная глубина): factorial(n) — это O(n) по памяти, потому что вложенность достигает n. Стек имеет фиксированный размер (обычно несколько мегабайт). Если рекурсия уходит глубже, чем туда влезает, происходит stack overflow и программа падает. Поэтому рекурсия, чья глубина масштабируется с недоверенным входом, рискованна." },
    { q: "Как прочитать временную сложность рекурсивной функции по её дереву рекурсии?", a: "Нарисуйте дерево, где каждый узел — один вызов, а его дети — вызовы, которые он делает. Стоимость задают два числа: branching factor b (рекурсивных вызовов на одно обращение) и глубина d (уровней до base case). Число узлов примерно b^d, а общее время — это число узлов умножить на работу на узел. Так factorial (b = 1, d = n) имеет около n узлов и это O(n); наивный fibonacci (b = 2, d = n) имеет около 2^n узлов и это O(2^n), хотя оба вкладываются лишь на n. Важно: глубина задаёт память (стек), но число узлов задаёт время, поэтому функция может быть дешёвой по памяти и катастрофической по времени." },
    { q: "Сформулируйте шаблон backtracking и объясните, почему шаг undo обязателен.", a: "Backtracking — это обход в глубину всех частичных решений по схеме choose-explore-undo: если частичное решение полное, запишите его копию и вернитесь; иначе для каждого доступного выбора сделайте выбор, рекурсируйте, чтобы исследовать его, затем отмените выбор перед следующим. Undo (например, current.pop() после current.push(item)) обязателен, потому что частичное решение — это общее изменяемое состояние между соседними ветками. Без него остатки выборов из одной ветки протекают в следующую, поэтому поздние ветки строятся на устаревшем состоянии и результаты испорчены. Также нужно записывать копию частичного решения, а не ссылку, иначе все сохранённые результаты указывают на один и тот же изменяемый массив." },
    { q: "Что такое pruning в backtracking и чем различаются subsets, permutations и combinations в одном шаблоне?", a: "Pruning означает проверку перед рекурсией того, может ли текущее частичное решение ещё привести к валидному полному; если нет, вы пропускаете всё поддерево. Именно это превращает backtracking из полного перебора в нечто практичное (например, N-Queens пропускает расстановки, которые уже конфликтуют). Три классические задачи делят скелет choose-explore-undo, но различаются выбором и правилом остановки. Subsets ветвятся include/exclude на каждый элемент и останавливаются на глубине n, давая 2^n выходов. Permutations выбирают, какой неиспользованный элемент (отслеживаемый массивом used) идёт следующим, и останавливаются, когда длина равна n, давая n! выходов. Combinations идут вперёд от start index, никогда не возвращаясь к ранним элементам, и останавливаются на размере k, давая C(n, k) выходов и естественный prune, когда осталось слишком мало элементов для достижения k." },
  ]}
/>

<Recap lang="ru">
Если вы восстановили каждый ответ по памяти, вы держите хребет юнита: base case плюс настоящий прогресс обеспечивают завершение рекурсии; call stack превращает максимальную глубину в O(глубина) памяти; дерево рекурсии превращает branching factor и глубину во время b^d; а backtracking — это тот же рекурсивный скелет, применённый как choose-explore-undo, с pruning для пропуска обречённых веток и копиями для безопасной записи результатов. Subsets, permutations и combinations — это один шаблон с разными правилами выбора.
</Recap>
