---
concepts:
- base-case
- call-stack
- recursion-tree
- backtracking
- pruning
estMin: 14
lang: ru
level: middle
order: 9
prereqs: []
slug: quiz-code
sources:
- https://en.wikipedia.org/wiki/Recursion_(computer_science)
- https://codejeet.com/blog/backtracking-template-for-coding-interviews
status: ready
summary: "Читайте реальные рекурсивные сниппеты и дерево рекурсии, предсказывайте поведение или стоимость и выбирайте баг или исправление с наибольшим эффектом."
title: 'Рекурсия и backtracking: чтение кода'
track: algorithms
unit: 04-recursion-backtracking
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Баги рекурсии находят чтением кода и трассировкой вызовов, а не догадками. Читайте каждый сниппет, предсказывайте, что он делает, и выбирайте ответ, который аккуратный инженер защитил бы на ревью.
</Hook>

<Goal lang="ru">
Отработайте цикл, который вы запускаете на любой рекурсивной функции: найдите base case, проверьте, что recursive case делает прогресс, посчитайте ветвление, чтобы прочитать стоимость, и убедитесь, что backtracking восстанавливает состояние перед следующим выбором.
</Goal>

## Сниппет 1 — отсутствующий прогресс

```js
function sumDigits(n) {
  if (n === 0) return 0;       // base case
  return (n % 10) + sumDigits(n / 10);   // recursive case
}

sumDigits(123);
```

<Quiz
  id="04-recursion-backtracking-code-1"
  lessonSlug="04-recursion-backtracking"
  lang="ru"
  question="Это должно суммировать десятичные цифры n. Что происходит на самом деле и почему?"
  choices={[
    { label: "Рекурсия идёт вечно и переполняет стек: n / 10 — деление с плавающей точкой, поэтому 123 становится 12.3, затем 1.23 и никогда не достигает ровно 0. Исправление — Math.floor(n / 10) (целочисленное деление).", correct: true },
    { label: "Возвращает 6 правильно.", misconception: "Так было бы, если бы n / 10 было целочисленным делением. В JavaScript / — деление с плавающей точкой, поэтому n никогда не достигает ровно 0 и base case не срабатывает; рекурсия не завершается." },
    { label: "Возвращает неправильную сумму, но всё же завершается.", misconception: "Завершения нет вовсе. Поскольку n никогда не становится ровно 0, ни один base case не срабатывает, и call stack растёт, пока не переполнится." },
    { label: "Base case неправильный; должно быть n <= 0.", misconception: "Расширение base case лишь маскирует симптом. Реальный дефект в том, что recursive case не шагает к 0 целыми шагами. Используйте целочисленное деление, чтобы аргумент сужался к 0." },
  ]}
/>

## Сниппет 2 — дерево рекурсии

```
fib(4)
├─ fib(3)
│  ├─ fib(2)
│  │  ├─ fib(1)
│  │  └─ fib(0)
│  └─ fib(1)
└─ fib(2)
   ├─ fib(1)
   └─ fib(0)
```

<Quiz
  id="04-recursion-backtracking-code-2"
  lessonSlug="04-recursion-backtracking"
  lang="ru"
  question="Читая это дерево вызовов наивного fib(4), какое утверждение верно?"
  choices={[
    { label: "fib(2) вычисляется дважды, а fib(1) трижды: одни и те же подзадачи пересчитываются в соседних ветках. Эта избыточность и есть причина, почему число узлов растёт как 2^n; мемоизация каждого результата сводит его к O(n).", correct: true },
    { label: "Каждая подзадача вычисляется ровно один раз, поэтому работа O(n).", misconception: "Посмотрите ещё раз: fib(2) встречается дважды, а fib(1) трижды. У наивной версии нет памяти, поэтому перекрывающиеся подзадачи пересчитываются, давая экспоненциальную работу." },
    { label: "Дерево показывает, что call stack держит 9 фреймов одновременно.", misconception: "9 узлов — это всего вызовов за время, а не одновременных фреймов. Стек держит лишь один путь корень-лист за раз, поэтому максимальная глубина здесь около 4." },
    { label: "Дерево неверно, потому что fib должен ветвиться на три.", misconception: "fib(n) вызывает fib(n-1) и fib(n-2): branching factor 2. Дерево верное; урок — в повторяющихся подзадачах, а не в числе ветвей." },
  ]}
/>

## Сниппет 3 — утёкшее состояние

```js
function subsets(arr) {
  const result = [], current = [];
  function go(i) {
    if (i === arr.length) { result.push(current); return; }
    current.push(arr[i]);
    go(i + 1);
    current.pop();
    go(i + 1);
  }
  go(0);
  return result;
}
```

<Quiz
  id="04-recursion-backtracking-code-3"
  lessonSlug="04-recursion-backtracking"
  lang="ru"
  question="Структура choose-explore-undo выглядит верно. Почему это возвращает массив пустых (или одинаковых) подмножеств?"
  choices={[
    { label: "result.push(current) сохраняет ссылку на один общий массив, а не копию. Каждый push записывает один и тот же объект, а поздние pop изменяют его, поэтому все сохранённые подмножества оказываются одинаковыми (в конце пустыми). Исправление: push копии, result.push([...current]).", correct: true },
    { label: "Undo (current.pop()) стоит не в том месте.", misconception: "Pop корректно стоит между ветками include и exclude; порядок choose-explore-undo верен. Баг — в хранении живой ссылки вместо снимка-копии." },
    { label: "go(i + 1) должно быть go(i) в одной из веток.", misconception: "Обе ветки должны переходить к i + 1, чтобы продвигаться по элементам; вызов go(i) не делал бы прогресса. Дефект — отсутствующая копия при записи готового подмножества." },
    { label: "Ему нужен массив used, как у permutations.", misconception: "Subsets ветвятся include/exclude по индексу и не нуждаются в массиве used. Единственный изъян — push общего current по ссылке вместо копирования." },
  ]}
/>

## Сниппет 4 — вопрос о pruning

```js
function combine(arr, k) {
  const result = [];
  function go(start, current) {
    if (current.length === k) { result.push([...current]); return; }
    for (let i = start; i < arr.length; i++) {
      current.push(arr[i]);
      go(i + 1, current);
      current.pop();
    }
  }
  go(0, []);
  return result;
}
```

<Quiz
  id="04-recursion-backtracking-code-4"
  lessonSlug="04-recursion-backtracking"
  lang="ru"
  question="Это корректно генерирует C(n, k) комбинаций. Что меняет добавление `if (current.length + (arr.length - start) < k) return;` в начало go?"
  choices={[
    { label: "Это подрезает (prune) ветки, которые уже не могут достичь k элементов, сокращая напрасную рекурсию, когда осталось слишком мало элементов; множество выходов идентично, просто делается меньше тупиковых вызовов.", correct: true },
    { label: "Это меняет результат, добавляя более короткие комбинации.", misconception: "Base case всё равно записывает только при current.length === k, поэтому выход не меняется. Проверка лишь пропускает ветки, которые заведомо не могут достичь размера k." },
    { label: "Это вносит дубликаты комбинаций.", misconception: "Start index уже предотвращает дубликаты, идя только вперёд. Prune не может добавить дубликаты; он лишь избегает веток со слишком малым числом оставшихся элементов." },
    { label: "Это неверно, потому что может пропустить валидные комбинации.", misconception: "Если current.length плюс все оставшиеся элементы всё равно меньше k, никакое завершение этой ветки не достигнет k, поэтому ничего валидного не пропускается. Это корректный prune." },
  ]}
/>

<Recap lang="ru">
Любой баг рекурсии читается одинаково: убедитесь, что base case действительно достижим (деление с плавающей точкой в Сниппете 1 никогда не даёт 0), следите за перекрывающимися подзадачами, которые делают дерево экспоненциальным (повторяющиеся вызовы fib в Сниппете 2), копируйте частичное решение при записи вместо хранения общей ссылки (Сниппет 3) и добавляйте pruning, который пропускает заведомо мёртвые ветки, не меняя выхода (Сниппет 4). Трассируйте вызовы, исправьте прогресс или состояние, затем перепроверьте.
</Recap>
