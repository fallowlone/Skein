---
concepts:
- base-case
- recursive-case
- call-stack
- recursion-tree
- backtracking
- pruning
estMin: 13
lang: ru
level: middle
order: 7
prereqs: []
slug: quiz-choice
sources:
- https://en.wikipedia.org/wiki/Recursion_(computer_science)
- https://www.geeksforgeeks.org/dsa/introduction-to-backtracking-2/
status: ready
summary: "Тест с множественным выбором по всему юниту: base case и recursive case, глубина call stack, сложность по дереву рекурсии, шаблон backtracking и pruning."
title: 'Рекурсия и backtracking: тест с множественным выбором'
track: algorithms
unit: 04-recursion-backtracking
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Шесть вопросов через весь юнит. Каждый — это решение, которое вы принимаете, когда реально пишете рекурсивный код: не определение для заучивания, а суждение о корректности, стоимости или о том, какое исправление верное.
</Hook>

<Goal lang="ru">
Убедитесь, что вы связываете четыре идеи юнита: base case и сужающийся recursive case обеспечивают завершение рекурсии, call stack задаёт память, дерево рекурсии задаёт время, а backtracking с pruning превращает полный перебор в нечто практичное.
</Goal>

<Quiz
  id="04-recursion-backtracking-choice-1"
  lessonSlug="04-recursion-backtracking"
  lang="ru"
  question="У рекурсивной функции есть base case, но она всё равно переполняет стек на любом входе, даже на крошечном. В чём наиболее вероятная причина?"
  choices={[
    { label: "Recursive case не сужает вход к base case (например, вызывает f(n) вместо f(n - 1)), поэтому условие base case никогда не достигается.", correct: true },
    { label: "Base case возвращает неправильное значение.", misconception: "Неправильный возврат base case даёт неправильный ответ, а не переполнение стека. Переполнение означает, что рекурсия не останавливается, а это проблема прогресса в recursive case." },
    { label: "В функции не хватает цикла.", misconception: "Рекурсии не нужен цикл. Дефект в том, что каждый вызов должен приближаться к base case; если аргумент не сужается, base case недостижим." },
    { label: "Стек языка просто слишком мал; в коде ничего не сломано.", misconception: "Если переполнение происходит даже на крошечных входах, значит, рекурсия не делает прогресса. Корректная сужающаяся рекурсия на малом n никогда бы не переполнила стек." },
  ]}
/>

<Quiz
  id="04-recursion-backtracking-choice-2"
  lessonSlug="04-recursion-backtracking"
  lang="ru"
  question="factorial(n) и наивный fibonacci(n) оба рекурсируют до глубины n. Почему factorial нормален для больших n, а fibonacci нет?"
  choices={[
    { label: "Оба используют O(n) памяти на стеке, но factorial ветвится один раз на вызов (время O(n)), а fibonacci дважды (время O(2^n)). Узкое место — время, а не стек.", correct: true },
    { label: "Fibonacci использует гораздо больше памяти на стеке, потому что делает два вызова на уровень.", misconception: "Глубина стека задаётся самым глубоким путём, а не общим числом вызовов. Оба достигают глубины n, значит, оба используют O(n) стека. Разница в общей работе — числе узлов дерева рекурсии." },
    { label: "У factorial есть base case, а у fibonacci нет.", misconception: "У обоих есть base case и оба завершаются. Fibonacci медленный, потому что branching factor 2 делает дерево рекурсии экспоненциальным, а не потому, что он не останавливается." },
    { label: "Fibonacci рекурсирует глубже, чем factorial.", misconception: "Они рекурсируют до одной и той же глубины n. Именно branching factor (2 против 1), а не глубина, заставляет число узлов fibonacci взрываться до O(2^n)." },
  ]}
/>

<Quiz
  id="04-recursion-backtracking-choice-3"
  lessonSlug="04-recursion-backtracking"
  lang="ru"
  question="У рекурсивной функции branching factor b = 3 и глубина d = 12, на вызов делается O(1) работы. Примерно сколько вызовов она делает и о чём это говорит?"
  choices={[
    { label: "Около b^d = 3^12 (примерно 530 000) вызовов. Число узлов — это branching factor в степени глубины, поэтому время экспоненциально и практично лишь для малых входов.", correct: true },
    { label: "Около b на d = 36 вызовов; умножить branching factor на глубину.", misconception: "Branching factor и глубина умножаются на каждом уровне, а не один раз. Каждый уровень умножает число узлов на b, давая b^d, а не b на d." },
    { label: "Около d^b = 12^3 (примерно 1700) вызовов.", misconception: "Основание — это branching factor, а показатель — глубина: b^d = 3^12, а не d^b. Перестановка их сильно занижает оценку." },
    { label: "Ровно 12 вызовов, по одному на уровень глубины.", misconception: "Это глубина стека, а не число вызовов. При ветвлении на каждом уровне до b раз больше узлов, чем на предыдущем." },
  ]}
/>

<Quiz
  id="04-recursion-backtracking-choice-4"
  lessonSlug="04-recursion-backtracking"
  lang="ru"
  question="В шаблоне backtracking choose-explore-undo что ломается, если опустить шаг undo (push в current, но никогда pop)?"
  choices={[
    { label: "Состояние из одной ветки протекает в соседние: частичное решение никогда не сбрасывается, поэтому поздние выборы строятся поверх устаревших ранних, и результаты испорчены.", correct: true },
    { label: "Рекурсия никогда не завершается.", misconception: "Завершение контролируется base case, а не undo. Без undo рекурсия всё равно останавливается — она лишь по пути выдаёт неправильные частичные решения." },
    { label: "Ничего; undo — это лишь оптимизация скорости.", misconception: "Undo необходим для корректности при общем изменяемом состоянии. Общий массив current должен быть чистым перед следующим соседним выбором, иначе каждая поздняя ветка наследует оставшиеся элементы." },
    { label: "Перестаёт работать pruning.", misconception: "Pruning — это проверка is-valid перед рекурсией; она независима от undo. Пропуск undo портит частичное решение, а это отдельный дефект." },
  ]}
/>

<Quiz
  id="04-recursion-backtracking-choice-5"
  lessonSlug="04-recursion-backtracking"
  lang="ru"
  question="Subsets, permutations и combinations используют один и тот же шаблон backtracking. Что на самом деле их различает?"
  choices={[
    { label: "Различаются выбор и правило остановки: subsets ветвятся include/exclude на каждый элемент (2^n), permutations выбирают, какой неиспользованный элемент идёт следующим (n!), combinations идут вперёд от start index до размера k (C(n,k)). Скелет choose-explore-undo одинаков.", correct: true },
    { label: "Subsets используют итерацию, permutations и combinations — рекурсию.", misconception: "Все три написаны одним и тем же рекурсивным скелетом backtracking в этом юните. Меняются логика выбора и base case, а не рекурсия против итерации." },
    { label: "Только combinations можно подвергнуть pruning; остальные два нельзя.", misconception: "У combinations есть естественный prune (осталось слишком мало элементов, чтобы достичь k), но pruning — это общая идея backtracking. Определяющее различие — правило выбора, а не кто может делать prune." },
    { label: "Permutations — частный случай subsets с тем же числом выходов.", misconception: "У них разные размеры выхода: 2^n subsets против n! permutations. Они различаются, потому что для permutations порядок важен, а для subsets — нет." },
  ]}
/>

<Quiz
  id="04-recursion-backtracking-choice-6"
  lessonSlug="04-recursion-backtracking"
  lang="ru"
  question="Корректный рекурсивный обход дерева работает в тестах, но коллега говорит, что перед продакшеном его надо переписать итеративно. Когда это оправдано?"
  choices={[
    { label: "Когда глубина рекурсии может расти с размером недоверенного входа: глубокий или враждебный вход способен загнать стек за его лимит в несколько мегабайт и уронить процесс. Ограниченную малую глубину можно оставить рекурсивной.", correct: true },
    { label: "Всегда, потому что рекурсия по своей природе медленнее итерации.", misconception: "Рекурсия и итерация обычно имеют одинаковую временную сложность. Беспокойство — это память на стеке при большой глубине, а не сырая скорость; неглубокая рекурсия не нуждается в переписывании." },
    { label: "Никогда, потому что корректную рекурсивную функцию всегда безопасно отгружать.", misconception: "Корректная на малых входах всё равно может переполнить стек при большой глубине. Если глубина масштабируется со входом, итеративная версия с явным стеком убирает этот жёсткий лимит." },
    { label: "Только если у функции отсутствует base case.", misconception: "Отсутствующий base case — это баг, который надо исправить как есть, а не компромисс рекурсия-против-итерации. Реальный триггер для итеративного переписывания — неограниченная глубина стека." },
  ]}
/>

<Recap lang="ru">
Сквозная линия юнита — одна цепочка: base case плюс сужающийся recursive case обеспечивают завершение рекурсии; call stack делает глубину стоимостью O(глубина) по памяти; дерево рекурсии делает общую работу равной branching factor в степени глубины по времени; а backtracking применяет этот рекурсивный скелет (choose, explore, undo) с pruning, чтобы пропускать обречённые ветки. Сначала верно задайте base case и прогресс, затем читайте время по дереву, а память — по стеку.
</Recap>
