---
concepts:
- visited-set-placement
- bfs-mark-on-enqueue
- dijkstra-negative-edges
- topological-sort-cycle
estMin: 14
lang: ru
level: senior
order: 9
prereqs: []
slug: quiz-code
sources:
- https://en.wikipedia.org/wiki/Breadth-first_search
- https://en.wikipedia.org/wiki/Dijkstra%27s_algorithm
- https://cp-algorithms.com/graph/topological-sort.html
status: ready
summary: "Читайте код графа и лог сборки, предсказывайте поведение и выбирайте самое результативное исправление: баг с visited-set, баг расстояний в BFS, Dijkstra на отрицательных рёбрах и цикл в topological sort."
title: 'Графы: чтение кода и багов'
track: algorithms
unit: 09-graphs
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Баги в графах прячутся в том, где вы помечаете вершину, из какой очереди извлекаете и каким весам доверяете. Прочтите каждый фрагмент, предскажите его поведение на циклическом или взвешенном графе и выберите исправление, которое senior-инженер сделает первым.
</Hook>

<Goal lang="ru">
Отработайте цикл, который вы запускаете на каждом дефекте графа: проследите фрагмент на маленьком циклическом или взвешенном примере, найдите нарушенный инвариант и возьмите наименьшее корректное исправление.
</Goal>

## Фрагмент 1 — DFS, который зависает

```js
function dfs(v, adj, visited) {
  for (const w of adj[v]) {   // пометка — ПОСЛЕ рекурсии ниже
    if (!visited.has(w)) {
      dfs(w, adj, visited);
    }
  }
  visited.add(v);             // помечается только на выходе
}
// в adj есть цикл: { 0: [1], 1: [2], 2: [0] }
```

<Quiz
  id="09-graphs-code-1"
  lessonSlug="09-graphs"
  lang="ru"
  question="Запустите это на циклическом графе 0->1->2->0, начиная с 0. Что произойдёт и как исправить?"
  choices={[
    { label: "Бесконечная рекурсия. v добавляется в visited только ПОСЛЕ рекурсии соседей, поэтому на цикле 0->1->2->0 ни одна вершина не помечена до повторного входа. Исправление: ставьте visited.add(v) в НАЧАЛЕ функции, до перебора соседей.", correct: true },
    { label: "Всё работает; пометка на выходе — это стандартный post-order для topological sort.", misconception: "PUSH в post-order для topo sort происходит после того, как вход тоже защищён проверкой visited в начале. Здесь пометки на входе нет вообще, поэтому цикл вечно входит в 0 заново. Помечайте на входе, чтобы предотвратить повторный вход." },
    { label: "Он посещает каждую вершину дважды, но всё равно завершается.", misconception: "Он никогда не завершается: без пометки до рекурсивного вызова 0->1->2->0->1->2... рекурсирует без предела, пока не переполнится стек. Пометка должна предшествовать циклу по соседям." },
    { label: "Баг в том, что используется Set вместо булева массива.", misconception: "Set — вполне корректная структура visited. Дефект во времени пометки — после рекурсии вместо до неё, — а не в типе данных." },
  ]}
/>

## Фрагмент 2 — BFS кратчайшего пути, дающий неверные расстояния

```js
function bfs(src, adj) {
  const dist = { [src]: 0 };
  const queue = [src];
  while (queue.length) {
    const v = queue.shift();
    for (const u of adj[v]) {
      dist[u] = dist[v] + 1;   // ставим каждый раз, когда видим u
      queue.push(u);           // и ставим в очередь каждый раз
    }
  }
  return dist;
}
```

<Quiz
  id="09-graphs-code-2"
  lessonSlug="09-graphs"
  lang="ru"
  question="На графе, где у вершины несколько входящих рёбер, этот BFS завышает расстояния и может не завершиться на цикле. В чём именно баг?"
  choices={[
    { label: "Он не проверяет, была ли u уже достигнута, поэтому перезаписывает dist[u] на более поздних (длинных) приходах и повторно ставит u в очередь. Исправление: защитить через if (!(u in dist)) перед установкой dist и постановкой в очередь — первое достижение и есть кратчайшее, пометка на enqueue.", correct: true },
    { label: "Для соседей нужно dist[v] - 1, а не dist[v] + 1.", misconception: "Сосед на одно ребро ДАЛЬШЕ от источника, поэтому dist[v] + 1 верно. Настоящий баг — отсутствие проверки «уже посещён», из-за которой поздние длинные приходы перезаписывают первый (кратчайший)." },
    { label: "BFS вообще не умеет вычислять расстояния; это может только Dijkstra.", misconception: "Обычный BFS вычисляет корректные невзвешенные кратчайшие пути именно потому, что первое достижение — кратчайшее, но только если защищаться от повторного посещения. Алгоритм верен; в этой реализации забыли защиту." },
    { label: "Нужно извлекать из конца очереди, чтобы получить порядок FIFO.", misconception: "shift() уже удаляет из НАЧАЛА (FIFO), что верно для BFS. Извлечение из конца сделало бы это стеком (DFS). Баг — отсутствие проверки visited, а не конец очереди." },
  ]}
/>

## Фрагмент 3 — Dijkstra на графе с ребром-возвратом

```js
// adj как [сосед, вес]: A:[[B,1]], B:[[C,-3]], A:[[C,4]]
// settled — Set; как только вершину извлекли, она окончательна
const [d, u] = heap.pop();
if (settled.has(u)) continue;
settled.add(u);                 // расстояние u ЗАФИКСИРОВАНО здесь
for (const [v, w] of adj[u]) {
  if (dist[u] + w < dist[v]) { dist[v] = dist[u] + w; heap.push([dist[v], v]); }
}
```

<Quiz
  id="09-graphs-code-3"
  lessonSlug="09-graphs"
  lang="ru"
  question="С ребром B->C веса -3 Dijkstra фиксирует C через A->C (стоимость 4) до того, как обнаружит A->B->C (стоимость 1 + (-3) = -2). В чём корневая причина и какое исправление верное?"
  choices={[
    { label: "Шаг фиксации предполагает, что добавленная стоимость только увеличивает путь, поэтому после извлечения вершины её расстояние окончательно. Отрицательное ребро это нарушает: A->B->C дешевле, но найден после того, как C уже могла быть зафиксирована. Здесь Dijkstra — неверный алгоритм; для отрицательных весов нужен Bellman-Ford.", correct: true },
    { label: "Добавьте отрицательный вес как положительный, взяв сначала его модуль.", misconception: "Смена знака меняет задачу: возврат -3 не то же самое, что стоимость +3, и кратчайшие пути станут неверными. Отрицательные рёбра требуют алгоритма, который их допускает (Bellman-Ford), а не трюка со знаком." },
    { label: "Уберите проверку «if settled», чтобы зафиксированные вершины можно было релаксировать снова.", misconception: "Релаксация зафиксированных вершин ломает гарантии завершения и всё равно не даёт корректного доказательства кратчайшего пути при отрицательных весах — может зациклиться на отрицательном цикле. Структурный ответ — Bellman-Ford, а не снятие проверки." },
    { label: "Баг — строгое < в релаксации; должно быть <=.", misconception: "Использование <= лишь добавляет избыточные обновления при равной стоимости; оно не восстанавливает инвариант фиксации при отрицательных рёбрах. Проблема — отрицательные веса против Dijkstra, решается Bellman-Ford." },
  ]}
/>

## Фрагмент 4 — topological sort, возвращающий мусор на цикле

```js
function topo(adj) {
  const inDeg = {};
  for (const v in adj) inDeg[v] = 0;
  for (const v in adj) for (const w of adj[v]) inDeg[w]++;
  const q = Object.keys(adj).filter(v => inDeg[v] === 0);
  const order = [];
  while (q.length) {
    const v = q.shift(); order.push(v);
    for (const w of adj[v]) if (--inDeg[w] === 0) q.push(w);
  }
  return order;                 // возвращает order безусловно
}
// adj = { a:[b], b:[c], c:[a], d:[a] }  // a->b->c->a — цикл
```

<Quiz
  id="09-graphs-code-4"
  lessonSlug="09-graphs"
  lang="ru"
  question="На этом графе (a->b->c->a — цикл, плюс d->a) что вернёт topo и какое исправление верное?"
  choices={[
    { label: "Он вернёт ['d'] — только у d in-degree 0; a, b, c застряли в цикле и никогда не достигают in-degree 0. Функция молча возвращает частичный список. Исправление: если order.length < числа вершин, в графе есть цикл, поэтому верните null (или бросьте исключение) вместо частичного порядка.", correct: true },
    { label: "Он вернёт ['a','b','c','d'], потому что алгоритм Кана всегда даёт полный порядок.", misconception: "Алгоритм Кана даёт полный порядок только для DAG. Здесь цикл a->b->c->a держит эти три на ненулевом in-degree, поэтому выводится только d. Функция должна распознать короткий вывод как цикл." },
    { label: "Он вернёт [], потому что цикл делает in-degree каждой вершины не меньше 1.", misconception: "У d in-degree 0 (на d ничто не указывает), поэтому d выводится. Результат — частичный ['d'], а не пустой. Исправление — проверка по числу оставшихся (leftover count), а не предположение о пустом результате." },
    { label: "Он бросит исключение во время выполнения, когда дойдёт до цикла.", misconception: "Ничто не бросается — цикл while просто опустошает очередь и выходит с частичным порядком. Опасность как раз в том, что он падает МОЛЧА; проверку order.length < V нужно добавить самому." },
  ]}
/>

<Recap lang="ru">
Каждый фрагмент нарушил один инвариант. DFS должен помечать вершину на ВХОДЕ, до рекурсии, иначе цикл входит снова вечно. BFS кратчайшего пути должен защищать первое достижение (пометка на enqueue), чтобы более длинный поздний маршрут не перезаписал кратчайший. Шаг фиксации Dijkstra верен только при неотрицательных весах; отрицательное ребро означает Bellman-Ford. А topological sort по Кану должен проверять число оставшихся — короткий вывод это цикл, а не корректный порядок. Проследите на крошечном циклическом или взвешенном графе, назовите нарушенный инвариант и примените наименьшее исправление.
</Recap>
