---
concepts:
- adjacency-tradeoffs
- bfs-vs-dfs
- topological-sort
- dijkstra-settle-invariant
- union-find-alpha
- shortest-path-boundaries
estMin: 13
lang: ru
level: senior
order: 8
prereqs: []
slug: quiz-short
sources:
- https://en.wikipedia.org/wiki/Dijkstra%27s_algorithm
- https://cp-algorithms.com/graph/topological-sort.html
- https://en.wikipedia.org/wiki/Disjoint-set_data_structure
status: ready
summary: "Подсказки на свободное припоминание по всему юниту графов. Сначала ответьте своими словами, затем откройте модельный ответ и сравните."
title: 'Графы: тест на свободное припоминание'
track: algorithms
unit: 09-graphs
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";

<Hook>
Припоминание сильнее перечитывания. Для каждой подсказки скажите или запишите полный ответ по памяти, прежде чем открыть модельный ответ, — именно усилие припоминания закрепляет материал.
</Hook>

<Goal lang="ru">
Восстановите ключевые механизмы юнита — компромиссы представлений, BFS против DFS, topological sort, инвариант фиксации Dijkstra и амортизированную оценку union-find — не заглядывая в уроки.
</Goal>

<RetrievalDrawer
  client:visible
  id="09-graphs-short-retrieval"
  lang="ru"
  questions={[
    { q: "Когда выбирать adjacency list вместо adjacency matrix и сколько стоит каждое?", a: "adjacency list хранит для каждой вершины список её соседей: O(V + E) памяти, перебор соседей за O(degree), но проверка наличия ребра — O(degree), потому что приходится сканировать список. adjacency matrix — сетка V на V: O(V в квадрате) памяти независимо от числа рёбер, поиск ребра за O(1), но перебор соседей — O(V), так как сканируется целая строка. Выбирайте list для разрежённых графов (E намного меньше V в квадрате) — большинство реальных сетей вроде социальных или дорожных, — потому что matrix тратит квадратичную память и медленнее на переборе соседей. Выбирайте matrix только для плотных графов или когда поиск ребра за O(1) — доминирующая операция." },
    { q: "BFS и DFS оба работают за O(V + E). Когда важен порядок и что даёт каждый?", a: "DFS использует стек (стек вызовов при рекурсии) и ныряет вглубь по одной ветви до отката; BFS использует очередь FIFO и обходит по уровням — все вершины на расстоянии d до d+1. Различается именно порядок. Уровневый порядок BFS означает, что первое достижение вершины — по наименьшему числу рёбер, поэтому BFS решает невзвешенные кратчайшие пути; DFS — нет. Структура DFS в глубину естественно даёт порядок завершения (post-order), который ведёт topological sort и обнаружение циклов через обратные рёбра. Обоим нужен visited set на циклическом графе, иначе они зациклятся. Берите BFS для расстояния по числу рёбер и уровней, DFS для глубины, упорядочивания и структуры циклов." },
    { q: "Что такое topological order, когда он существует и как два алгоритма обнаруживают цикл?", a: "topological order перечисляет каждую вершину направленного графа так, что для каждого ребра u -> v вершина u стоит перед v. Он существует тогда и только тогда, когда граф — DAG (без цикла), и обычно не единственный. Алгоритм Кана (Kahn) вычисляет in-degree, ставит в очередь все вершины с in-degree 0, затем повторно извлекает одну в вывод и уменьшает in-degree её соседей, ставя в очередь достигших 0; если выведено меньше V вершин, остаток застрял в цикле и порядка нет. Вариант на DFS заталкивает каждую вершину на выходе (post-order), затем разворачивает; обратное ребро к вершине, ещё находящейся в стеке рекурсии, сигнализирует о цикле. Оба работают за O(V + E) времени и O(V) памяти и служат детекторами циклов." },
    { q: "Сформулируйте инвариант фиксации (settle) Dijkstra и объясните, почему он рушится при отрицательном ребре.", a: "Dijkstra держит предварительное dist[] (источник 0, остальные бесконечность) в min-priority-queue. Инвариант фиксации: когда вершина u извлечена как минимум heap, dist[u] окончательно, потому что любой альтернативный маршрут к u должен покинуть зафиксированную область через некоторую незафиксированную вершину с предварительным расстоянием не меньше dist[u], а так как рёбра только добавляют стоимость, такой маршрут стоит не меньше. Отрицательное ребро ломает посылку «рёбра только добавляют стоимость»: маршрут, найденный после фиксации u, может включать отрицательное ребро, понижающее истинное расстояние u ниже закреплённого значения. Поэтому Dijkstra может зафиксировать неверное расстояние. Исправление — Bellman-Ford, который релаксирует каждое ребро V-1 раз (O(V*E)) и обнаруживает отрицательные циклы." },
    { q: "Почему амортизированная стоимость union-find — O(alpha(n)), а не истинное O(1) или O(log n)?", a: "Union-Find хранит лес parent[]; find идёт к корню, union подвешивает один корень под другой. С одним union by rank/size деревья остаются высотой O(log n). Добавление path compression (перенаправление узлов к корню во время find) снижает АМОРТИЗИРОВАННУЮ стоимость на операцию до O(alpha(n)) — обратной функции Аккермана, которая меньше 5 для любого n, помещающегося во вселенной, — почти константа. Это лучше O(log n), потому что две оптимизации усиливают друг друга. Но это доказуемо не истинное O(1) на вызов: Тарьян доказал нижнюю границу alpha(n) для этой структуры, и оценка амортизирована по последовательности, а не гарантирована для каждого вызова. Уберите обе оптимизации — и find деградирует до O(n) на вырожденной цепочке." },
    { q: "Связные компоненты можно искать через BFS/DFS или через union-find. Когда брать каждое?", a: "Берите паттерн BFS/DFS «перебор и обход», когда граф СТАТИЧЕН: у вас все рёбра, вы перебираете вершины, запускаете обход из каждой непосещённой и считаете одну компоненту на запуск (общий visited set предотвращает двойной счёт), всё за O(V + E). Берите union-find, когда связность ДИНАМИЧНА: рёбра приходят со временем и после каждого нужно сразу отвечать «та же компонента?». DSU инкрементально объединяет через union и отвечает find(a) === find(b) за амортизированное O(alpha(n)) без повторного обхода, тогда как повторять BFS после каждого ребра стоит O(V + E) на ребро. DSU также обеспечивает обнаружение цикла в неориентированном графе (ребро, концы которого уже делят корень, замыкает цикл) и Kruskal's MST." },
  ]}
/>

<Recap lang="ru">
Если вы смогли восстановить каждый ответ по памяти, вы держите хребет юнита: компромиссы представлений (list для разрежённых, matrix для плотных или O(1) поиска), BFS для путей с наименьшим числом рёбер и DFS для глубины и упорядочивания, topological sort со встроенным обнаружением циклов, инвариант фиксации Dijkstra и почему неотрицательные веса обязательны, и амортизированное O(alpha(n)) у union-find для динамической связности. Повторяющийся урок — подбирать алгоритм под форму задачи до написания кода.
</Recap>
