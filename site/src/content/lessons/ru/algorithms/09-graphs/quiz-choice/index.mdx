---
concepts:
- adjacency-list-vs-matrix
- bfs-dfs-order
- topological-sort
- dijkstra-invariant
- union-find
- shortest-path-weights
estMin: 13
lang: ru
level: senior
order: 7
prereqs: []
slug: quiz-choice
sources:
- https://en.wikipedia.org/wiki/Dijkstra%27s_algorithm
- https://cp-algorithms.com/graph/topological-sort.html
- https://en.wikipedia.org/wiki/Disjoint-set_data_structure
status: ready
summary: "Синтез по всему юниту графов в виде теста: компромиссы представлений, BFS против DFS, topological sort, инвариант фиксации Dijkstra и union-find."
title: 'Графы: тест с множественным выбором'
track: algorithms
unit: 09-graphs
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Шесть вопросов, пронизывающих весь юнит. Каждый выбирает алгоритм или представление, которого реально требует задача, — это не определение для пересказа, а решение по моделированию, которое нужно обосновать.
</Hook>

<Goal lang="ru">
Убедитесь, что вы связываете выбор представления, порядок обхода, упорядочивание, кратчайшие пути со весами и динамическую связность — синтез, к которому вели восемь уроков.
</Goal>

<Quiz
  id="09-graphs-choice-1"
  lessonSlug="09-graphs"
  lang="ru"
  question="В социальном графе 50 миллионов пользователей и ~200 связей у каждого (то есть E порядка 5 миллиардов направленных записей, намного меньше V в квадрате). Горячая операция — «перебрать друзей пользователя». Какое представление и почему?"
  choices={[
    { label: "adjacency list — это O(V + E) памяти (здесь реально; матрице нужно V в квадрате = 2,5 квадриллиона ячеек) и перебор соседей вершины за O(degree), как раз горячая операция.", correct: true },
    { label: "adjacency matrix, потому что поиск ребра за O(1) всегда самый быстрый вариант.", misconception: "Матрице V на V здесь нужно ~2,5 квадриллиона ячеек — её невозможно хранить. Граф разрежённый (E намного меньше V в квадрате), поэтому adjacency list выигрывает и по памяти, и на самой горячей операции — переборе соседей." },
    { label: "Список рёбер (edge list), потому что он хранит каждую связь ровно один раз.", misconception: "edge list компактен, но не умеет перебирать друзей одного пользователя без сканирования всех E рёбер. Для операции «перечислить соседей вершины» это O(E), намного хуже, чем O(degree) у списка." },
    { label: "Любое подойдёт одинаково; выбор косметический на этом масштабе.", misconception: "На этом масштабе матрица даже не помещается в память, а перебор соседей — O(V) на матрице против O(degree) на списке. Выбор решающий, а не косметический." },
  ]}
/>

<Quiz
  id="09-graphs-choice-2"
  lessonSlug="09-graphs"
  lang="ru"
  question="Нужно найти маршрут между двумя точками на карте с наименьшим числом сегментов дороги, игнорируя расстояние. Нужно только число переходов. Какой алгоритм взять первым и что делает его корректным?"
  choices={[
    { label: "BFS — он извлекает вершины в неубывающем порядке расстояния, поэтому первое достижение вершины происходит по наименьшему числу рёбер. Число переходов — это в точности невзвешенный кратчайший путь.", correct: true },
    { label: "Dijkstra, потому что это общий алгоритм кратчайшего пути и он никогда не ошибается.", misconception: "Dijkstra здесь корректен, но избыточен: при всех весах, равных 1, он вырождается в BFS, платя лишний множитель log V за heap впустую. Для числа переходов первым берут BFS." },
    { label: "DFS, потому что он обходит граф так же полно, как BFS.", misconception: "DFS посещает все достижимые вершины, но в глубину; первый маршрут DFS до вершины не кратчайший. Только порядок BFS по уровням гарантирует наименьшее число рёбер." },
    { label: "topological sort, раз он упорядочивает все вершины.", misconception: "topological sort упорядочивает DAG по направлению зависимостей; он ничего не говорит о числе переходов и не определён на графе с циклами, а в дорожной сети циклы есть." },
  ]}
/>

<Quiz
  id="09-graphs-choice-3"
  lessonSlug="09-graphs"
  lang="ru"
  question="Система сборки запускает алгоритм Кана (Kahn) на своём DAG зависимостей, и выходной порядок содержит только 18 из 20 целей сборки. О чём это говорит?"
  choices={[
    { label: "Две цели сидят в циклической зависимости. Вершины внутри цикла никогда не достигают in-degree 0, поэтому их не помещают в очередь — недостающие две и есть те, что застряли в цикле (или ниже него по потоку).", correct: true },
    { label: "Алгоритм Кана недетерминирован и просто потерял две цели в этом запуске.", misconception: "Алгоритм Кана никогда не теряет вершину, которую можно законно упорядочить. Короткий вывод — однозначный сигнал: оставшиеся вершины образуют цикл или зависят от него, поэтому корректного порядка для них нет." },
    { label: "У двух недостающих целей in-degree 0, и их пропустили как корни.", misconception: "Вершины с in-degree 0 — ПЕРВЫЕ, кого помещают в очередь и выводят, а не пропускают. Недостающие — наоборот: их in-degree никогда не падает до 0, потому что цикл постоянно их подпитывает." },
    { label: "С графом всё в порядке; просто добавьте две недостающие цели в конец.", misconception: "Добавление в конец даёт порядок, нарушающий зависимость, потому что они в цикле и не имеют согласованной позиции. Правильный ответ — сообщить о циклической зависимости, а не сочинять порядок." },
  ]}
/>

<Quiz
  id="09-graphs-choice-4"
  lessonSlug="09-graphs"
  lang="ru"
  question="В графе цен некоторые рёбра представляют возвраты (отрицательная стоимость). Инженер запускает Dijkstra и иногда получает завышенные ответы. Почему и как это исправить?"
  choices={[
    { label: "Шаг фиксации (settle) у Dijkstra закрепляет расстояние вершины как окончательное при извлечении, полагая, что более дешёвого маршрута позже не появится. Отрицательное ребро может понизить расстояние ПОСЛЕ фиксации, ломая этот инвариант. Нужен Bellman-Ford, который релаксирует каждое ребро V-1 раз и допускает отрицательные веса.", correct: true },
    { label: "Min-heap не умеет хранить отрицательные числа, поэтому расстояния переполняются.", misconception: "Heap прекрасно хранит отрицательные числа. Настоящий дефект — инвариант фиксации: settle закрепляет расстояние, которое позднее отрицательное ребро может подрезать. Ошибка алгоритмическая, не числовая." },
    { label: "Dijkstra требует, чтобы граф был неориентированным; направленные рёбра-возвраты его путают.", misconception: "Dijkstra корректно работает на направленных графах. Проблема именно в отрицательных весах, а не в направлении рёбер. Исправление — Bellman-Ford, а не превращение графа в неориентированный." },
    { label: "Ничего не сломано; запустите Dijkstra из каждой вершины и возьмите минимум.", misconception: "Запуск Dijkstra из каждого источника всё равно применяет сломанный инвариант фиксации на каждом запуске, поэтому отрицательные рёбра по-прежнему дают неверные ответы. Структурное исправление — Bellman-Ford." },
  ]}
/>

<Quiz
  id="09-graphs-choice-5"
  lessonSlug="09-graphs"
  lang="ru"
  question="Рёбра приходят по одному в потоке, и после каждого нужно мгновенно отвечать «уже ли u и v в одной компоненте?». Какая структура подходит и почему повторный запуск BFS — неверный выбор?"
  choices={[
    { label: "Union-Find (DSU). Он инкрементально объединяет множества через union(u, v) и отвечает на связность как find(u) === find(v) за амортизированное почти константное время. Повторять BFS после каждого ребра — O(V + E) на ребро, расточительно для онлайн-потока.", correct: true },
    { label: "adjacency matrix, потому что ячейка [u][v] отвечает на связность за O(1).", misconception: "Ячейка матрицы говорит лишь о наличии ПРЯМОГО ребра u-v, а не о связности u и v через путь. Достижимость всё равно требует обхода; именно DSU инкрементально отслеживает принадлежность к компоненте." },
    { label: "BFS из u после каждого ребра, ведь это просто и корректно.", misconception: "Корректно, но цена — O(V + E) на запрос, повторяемая для каждого приходящего ребра. DSU отвечает на тот же запрос за амортизированное O(alpha(n)) без повторного обхода — ради этого DSU и существует." },
    { label: "topological sort, пересчитываемый после каждого ребра.", misconception: "topological sort упорядочивает DAG; он не отвечает на неориентированную связность и не определён, как только поток вводит цикл. DSU — структура для динамической связности." },
  ]}
/>

<Quiz
  id="09-graphs-choice-6"
  lessonSlug="09-graphs"
  lang="ru"
  question="Коллега утверждает, что его union-find работает за O(1) на операцию, потому что он добавил path compression. Какова точная сложность и что упущено или преувеличено?"
  choices={[
    { label: "С union by rank/size И path compression вместе оценка — амортизированное O(alpha(n)): почти константа (alpha меньше 5 для любого практического n), но доказуемо НЕ истинное O(1) на вызов и лучше, чем O(log n). Один лишь path compression без union by rank полную оценку не гарантирует.", correct: true },
    { label: "Это ровно O(1) на операцию, как только добавлен path compression; утверждение верно.", misconception: "Тарьян доказал нижнюю границу alpha(n) для этой структуры, поэтому это доказуемо НЕ истинное константное время на вызов. Честная оценка — амортизированное O(alpha(n))." },
    { label: "Это O(log n) на операцию, как у сбалансированного дерева.", misconception: "O(log n) — это оценка для union by rank В ОДИНОЧКУ. С добавленным path compression амортизированная оценка падает ниже до O(alpha(n)). Назвать O(log n) — занизить оптимизированную структуру." },
    { label: "Это O(n), потому что path compression удлиняет пути, по которым он проходит.", misconception: "path compression УКОРАЧИВАЕТ пути, перенаправляя узлы к корню; он улучшает время, а не ухудшает. O(n) — это цена неоптимизированной вырожденной цепочки, противоположность тому, что даёт сжатие." },
  ]}
/>

<Recap lang="ru">
Сквозная линия юнита — одно дерево решений по моделированию: выбирайте представление по плотности (list для разрежённых, matrix для плотных или O(1) поиска ребра); выбирайте обход по нужде (BFS для путей с наименьшим числом рёбер и уровней, DFS для глубины и структуры циклов); добавьте веса — и BFS уступает Dijkstra, но только при неотрицательных рёбрах, иначе Bellman-Ford. topological sort упорядочивает DAG и попутно ловит циклы, а union-find отвечает на динамическую связность за амортизированное O(alpha(n)) там, где повторять BFS было бы расточительно. Подбирайте алгоритм под форму задачи до того, как написать строку кода.
</Recap>
