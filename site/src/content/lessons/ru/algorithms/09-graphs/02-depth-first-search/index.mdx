---
slug: 02-depth-first-search
lang: ru
track: algorithms
unit: 09-graphs
order: 2
title: "Поиск в глубину (DFS)"
summary: "Алгоритм обхода графа, который исследует насколько возможно по каждой ветви перед отходом назад. Критическое отличие от деревьев: множество посещённых предотвращает бесконечные циклы. Рекурсивный и итеративный подходы. O(V+E) время, O(V) память."
estMin: 28
status: ready
prereqs: ["01-graph-representations"]
mathPrereqs: []
concepts: ["depth-first_search", "dfs", "graph_traversal", "visited_set", "recursive", "iterative", "stack", "backtracking", "cycle", "reachability"]
sources:
  - https://en.wikipedia.org/wiki/Depth-first_search
  - https://www.geeksforgeeks.org/depth-first-search-or-dfs-for-a-graph/
  - https://opendsa-server.cs.vt.edu/ODSA/Books/Everything/html/GraphTraversal.html
  - https://williamfiset.com/depth-first-search/
---

import Hook from "../../../../../../components/lesson/Hook.astro";
import Goal from "../../../../../../components/lesson/Goal.astro";
import Idea from "../../../../../../components/lesson/Idea.astro";
import Code from "../../../../../../components/lesson/Code.astro";
import Trace from "../../../../../../components/lesson/Trace.astro";
import Complexity from "../../../../../../components/lesson/Complexity.astro";
import Check from "../../../../../../components/lesson/Check.astro";
import Recap from "../../../../../../components/lesson/Recap.astro";
import Inset from "../../../../../../components/lesson/Inset.astro";
import AnnotatedCode from "../../../../../../components/algo/AnnotatedCode.astro";
import AlgoTrace from "../../../../../../components/algo/AlgoTrace.astro";
import CodeRunner from "../../../../../../components/algo/CodeRunner.astro";
import AlgoPractice from "../../../../../../components/algo/AlgoPractice.astro";
import Quiz from "../../../../../../components/pedagogy/Quiz.astro";
import Term from "../../../../../../components/prose/Term.astro";

<Hook>
Ты знаешь как обходить дерево: выбери корень, посети его детей, потом их детей, в некотором порядке. Поиск в глубину (DFS) расширяет это на графы. Но есть критическое отличие: граф может иметь циклы. В дереве, есть ровно один путь между двумя узлами — раз ты посетил узел, ты никогда его не видишь снова. В графе с циклами (A → B → A), ты мог бы зацикливаться навсегда, посещая одни и те же вершины снова и снова. Решение простое и критичное: отслеживай какие вершины ты уже посетил и никогда не посещай их дважды. Этот урок учит DFS в его двух формах — рекурсивный (естественный и элегантный) и итеративный с явным стеком (яснее о порядке исследования) — и показывает почему множество посещённых вершин критично.
</Hook>

<Goal lang="ru">
После этого урока ты можешь написать DFS как рекурсивную функцию и как итеративную функцию с явным стеком. Ты понимаешь почему множество посещённых вершин необходимо для графов (чтобы предотвратить бесконечные циклы на циклических графах), хотя деревья его не требуют. Ты можешь трассировать DFS на конкретном графе со списком смежности и предсказать порядок в котором вершины посещаются. Ты понимаешь сложность DFS O(V+E) по времени и O(V) по памяти, и ты можешь определить когда DFS это инструмент для использования: исследование всех вершин достижимых из начальной вершины, или проверка связности. Следующий урок это поиск в ширину (BFS), что исследует слой за слоем вместо глубины.
</Goal>

<Idea lang="ru">
**Что это поиск в глубину?**

<Term slug="depth_first_search">Поиск в глубину (DFS)</Term> это обход графа, что исследует насколько возможно по каждому <Term slug="edge">ребру</Term> перед <Term slug="backtracking">отходом назад</Term>. Он посещает каждую <Term slug="vertex">вершину</Term> достижимую от начальной <Term slug="vertex">вершины</Term>, в порядке поиска в глубину.

**Почему DFS это другое от обхода дерева**

В дереве, раз ты посетил узел, нет другого пути к нему — невозможно посетить его дважды. В графе с циклами, одна вершина может быть достижима через множественные пути. Если ты не отслеживаешь посещённые вершины, ты будешь зацикливаться навсегда:

```
Дерево (нет циклов):       Граф (имеет цикл):
    1                          0
   / \                         /|\
  2   3                       1 2 3
                              |/
                              1 ← цикл!
```

В дереве, ты посещаешь 1, потом 2 (раз), потом 3 (раз). Готово.

В графе, начиная от 0, ты мог бы идти 0 → 1 → 1 → 1 ... (бесконечный цикл если ты не отмечаешь 1 как посещённо).

Исправление: поддерживай <Term slug="visited_set">множество посещённых вершин</Term> — множество (или булев массив) записывающее какие вершины ты уже посетил. Перед посещением вершины, проверь есть ли она в множестве посещённых. Если да, пропусти её. Если нет, отметь её посещённо и исследуй её соседей.

**Рекурсивный DFS: простой и естественный**

Рекурсивный подход зеркалит обход дерева:

1. Отметь текущую <Term slug="vertex">вершину</Term> как посещённо.
2. Для каждого <Term slug="neighbor">соседа</Term> текущей вершины:
   - Если сосед не посещён, рекурсивно посети его.

Это всё. Рекурсивный вызов стек естественно управляет отходом назад. Когда рекурсивный вызов возвращается, ты автоматически отходишь назад чтобы исследовать других соседей.

**Итеративный DFS: явный стек**

Рекурсивная версия это элегантна но полагается на стек вызовов функции. Итеративная версия делает стек явным: добавь начальную вершину на стек, потом:

1. Пока <Term slug="stack">стек</Term> не пуст:
   - Извлеки вершину.
   - Если она не посещена, отметь её посещённо и добавь всех её непосещённых соседей на стек.

Порядок в котором ты добавляешь соседей влияет на порядок в котором они исследуются (LIFO из стека).

**Пример графа**

Давайте используем простой ориентированный граф с 4 вершинами и 5 рёбрами:

```
Список смежности:
0: [1, 2]
1: [3]
2: [3]
3: [2]

Визуально:
  0 → 1
  ↓   ↓
  2 → 3
  ↑   ↓
  +---+
```

Вершина 3 имеет ребро назад к 2 (цикл). Начиная от вершины 0, DFS посетит 0, потом 1 или 2 (в зависимости от порядка соседей), потом 3, потом назад к 2 если не ещё посещена, или пропусти её если уже посещена.

**Рекурсивный DFS на примере**

```
посещённо = {}
Начни с 0.
- Отметь 0 посещённо.
- Сосед 1: не посещён, рекурсируй.
  - Отметь 1 посещённо.
  - Сосед 3: не посещён, рекурсируй.
    - Отметь 3 посещённо.
    - Сосед 2: не посещён, рекурсируй.
      - Отметь 2 посещённо.
      - Сосед 3: уже посещён, пропусти.
      - Готово с 2.
    - Готово с 3.
  - Готово с 1.
- Сосед 2: уже посещён, пропусти.
- Готово с 0.
Финальный порядок посещения: 0 → 1 → 3 → 2.
```

</Idea>

<Code lang="ru">
**Рекурсивный DFS**

<AnnotatedCode
  code={`function dfsRecursive(vertex, adj, visited) {
  // Отметь эту вершину как посещённо
  visited.add(vertex);
  
  // Для каждого соседа этой вершины
  for (const neighbor of adj[vertex]) {
    // Если сосед не посещён, рекурсируй
    if (!visited.has(neighbor)) {
      dfsRecursive(neighbor, adj, visited);
    }
  }
}

// Чтобы начать DFS из вершины:
const visited = new Set();
dfsRecursive(startVertex, adj, visited);`}
  annotations={[
    { line: 1, text: "Берёт вершину, список смежности и множество посещённых вершин." },
    { line: 2, text: "Отметь эту вершину посещённо (перед исследованием соседей, не после)." },
    { line: 5, text: "Итерируй всех соседей текущей вершины." },
    { line: 7, text: "Рекурсируй только в непосещённых соседей." },
    { line: 11, text: "Вызови с начальной вершиной и пустым множеством посещённых." }
  ]}
/>

**Итеративный DFS с явным стеком**

<AnnotatedCode
  code={`function dfsIterative(startVertex, adj) {
  const visited = new Set();
  const stack = [startVertex];
  
  while (stack.length > 0) {
    const vertex = stack.pop();
    
    // Только процессируй если ещё не посещена
    if (!visited.has(vertex)) {
      visited.add(vertex);
      
      // Добавь непосещённых соседей на стек (в обратном порядке для согласованного исследования)
      for (let i = adj[vertex].length - 1; i >= 0; i--) {
        const neighbor = adj[vertex][i];
        if (!visited.has(neighbor)) {
          stack.push(neighbor);
        }
      }
    }
  }
  
  return visited;
}`}
  annotations={[
    { line: 1, text: "Итеративная версия используя явный стек." },
    { line: 2, text: "Инициализируй множество посещённых вершин." },
    { line: 3, text: "Инициализируй стек с начальной вершиной." },
    { line: 5, text: "Пока стек не пуст, извлеки вершину." },
    { line: 9, text: "Проверь есть ли эта вершина уже посещена." },
    { line: 10, text: "Отметь её посещённо." },
    { line: 13, text: "Добавь непосещённых соседей. Мы итерируем в обратном порядке чтобы сохранить один и тот же порядок исследования как рекурсивный DFS." },
    { line: 20, text: "Верни множество всех посещённых вершин." }
  ]}
/>

**Запуск DFS на графе**

<CodeRunner
  code={`// Построй граф как список смежности
const adj = {
  0: [1, 2],
  1: [3],
  2: [3],
  3: [2]
};

// Рекурсивный DFS
function dfsRecursive(vertex, adj, visited) {
  visited.add(vertex);
  console.log(vertex);
  for (const neighbor of adj[vertex]) {
    if (!visited.has(neighbor)) {
      dfsRecursive(neighbor, adj, visited);
    }
  }
}

// Итеративный DFS
function dfsIterative(startVertex, adj) {
  const visited = new Set();
  const stack = [startVertex];
  
  while (stack.length > 0) {
    const vertex = stack.pop();
    
    if (!visited.has(vertex)) {
      visited.add(vertex);
      console.log(vertex);
      
      for (let i = adj[vertex].length - 1; i >= 0; i--) {
        const neighbor = adj[vertex][i];
        if (!visited.has(neighbor)) {
          stack.push(neighbor);
        }
      }
    }
  }
  
  return visited;
}

console.log("Рекурсивный DFS начиная от 0:");
const visited1 = new Set();
dfsRecursive(0, adj, visited1);

console.log("\\nИтеративный DFS начиная от 0:");
const visited2 = dfsIterative(0, adj);`}
  expected={`Рекурсивный DFS начиная от 0:
0
1
3
2

Итеративный DFS начиная от 0:
0
1
3
2`}
/>

</Code>

<Trace lang="ru">
<AlgoTrace
  id="dfs-recursive-trace"
  lang="ru"
  code={[
    "adj = { 0: [1, 2], 1: [3], 2: [3], 3: [2] };",
    "visited = new Set();",
    "dfsRecursive(0, adj, visited);"
  ]}
  frames={[
    { caption: "Вызови dfsRecursive(0). Отметь 0 посещённо. visited = {0}. Процессируй соседа 1 (не посещён).", line: 3, cells: [{ value: "0", mark: "active" }, { value: "visited={0}" }] },
    { caption: "Рекурсируй в 1. Отметь 1 посещённо. visited = {0, 1}. Процессируй соседа 3 (не посещён).", line: 3, cells: [{ value: "0,1", mark: "active" }, { value: "visited={0,1}" }] },
    { caption: "Рекурсируй в 3. Отметь 3 посещённо. visited = {0, 1, 3}. Процессируй соседа 2 (не посещён).", line: 3, cells: [{ value: "0,1,3", mark: "active" }, { value: "visited={0,1,3}" }] },
    { caption: "Рекурсируй в 2. Отметь 2 посещённо. visited = {0, 1, 3, 2}. Процессируй соседа 3 (уже посещён, пропусти).", line: 3, cells: [{ value: "0,1,3,2", mark: "active" }, { value: "visited={0,1,3,2}" }] },
    { caption: "Готово с 2. Верни к 3. Готово с 3. Верни к 1. Готово с 1. Верни к 0.", line: 3, cells: [{ value: "0,1,3,2", mark: "done" }, { value: "visited={0,1,3,2}" }] },
    { caption: "Назад в 0. Процессируй соседа 2 (уже посещён, пропусти). Готово. Порядок посещения: 0, 1, 3, 2.", line: 3, cells: [{ value: "0,1,3,2", mark: "done" }, { value: "visited={0,1,3,2}" }] }
  ]}
/>

<AlgoTrace
  id="dfs-iterative-trace"
  lang="ru"
  code={[
    "adj = { 0: [1, 2], 1: [3], 2: [3], 3: [2] };",
    "stack = [0];",
    "visited = new Set();",
    "dfsIterative(0, adj);"
  ]}
  frames={[
    { caption: "Инициализируй stack = [0], visited = {}. Извлеки 0, не посещена. Отметь 0 посещённо. Добавь соседей 2, потом 1 (обратный порядок). stack = [2, 1].", line: 4, cells: [{ value: "stack=[2,1]", mark: "active" }, { value: "visited={0}" }] },
    { caption: "Извлеки 1 из стека. Не посещена. Отметь 1 посещённо. Добавь соседа 3. stack = [2, 3].", line: 4, cells: [{ value: "stack=[2,3]", mark: "active" }, { value: "visited={0,1}" }] },
    { caption: "Извлеки 3 из стека. Не посещена. Отметь 3 посещённо. Добавь соседа 2 (если не посещена). 2 не посещена ещё, добавь её. stack = [2, 2].", line: 4, cells: [{ value: "stack=[2,2]", mark: "active" }, { value: "visited={0,1,3}" }] },
    { caption: "Извлеки 2 из стека. Не посещена. Отметь 2 посещённо. Добавь соседа 3 (не посещена? 3 уже посещена, пропусти). stack = [2].", line: 4, cells: [{ value: "stack=[2]", mark: "active" }, { value: "visited={0,1,3,2}" }] },
    { caption: "Извлеки 2 из стека. Уже посещена. Пропусти. stack = [].", line: 4, cells: [{ value: "stack=[]", mark: "done" }, { value: "visited={0,1,3,2}" }] },
    { caption: "Стек пуст. Готово. Порядок посещения (по выводу): 0, 1, 3, 2.", line: 4, cells: [{ value: "done", mark: "done" }, { value: "visited={0,1,3,2}" }] }
  ]}
/>
</Trace>

<Complexity lang="ru">
**Сложность по времени: O(V + E)**

DFS посещает каждую вершину раз (V вершин) и каждое ребро дважды (раз с каждого конца). На самом деле, каждое ребро исследуется раз в направлении списка смежности. Для ориентированного графа, каждое ребро исследуется ровно раз когда мы итерируем соседей его исходной вершины. Итоговая работа: V посещений + E итераций рёбер = **O(V + E)**.

Пример: Граф с 5 вершинами и 7 рёбрами использует O(5 + 7) = O(12) операций — независимо от того редкий ли граф или плотный.

**Сложность по памяти: O(V)**

Множество посещённых вершин хранит до V записей (одна за вершину): O(V).

Стек рекурсии (в рекурсивном DFS) может быть такой же глубокий как V в наихудшем случае (длинная цепь: 0 → 1 → 2 → ... → V-1): O(V).

Явный стек (в итеративном DFS) может удерживать до V вершин одновременно (в графе с высокой ветвлением): O(V).

Итоговая память: **O(V)**.

**Почему множество посещённых вершин критично**

Без множества посещённых вершин, DFS на циклическом графе будет зацикливаться навсегда:

```
Пример: 0 → 1 → 0 (2-цикл)
Без множества посещённых вершин:
  Посети 0, иди к 1.
  Посети 1, иди к 0.
  Посети 0, иди к 1.
  Посети 1, иди к 0.
  ... (бесконечно)

С множеством посещённых {0, 1}:
  Посети 0, отметь посещённо.
  Иди к 1 (не посещена).
  Посети 1, отметь посещённо.
  Иди к 0 (уже посещена), пропусти.
  Готово.
```

Деревья не имеют циклов, так обход дерева не строго требует множество посещённых вершин (ты не переучитаешь родителя). Но отмечание вершин при посещении их это всё ещё хорошая практика и делает код яснее.

</Complexity>

<AlgoPractice
  id="practice-dfs"
  lessonSlug="02-depth-first-search"
  lang="ru"
  problems={[
    {
      kind: "choice",
      prompt: "В графе с циклом (например, 0 → 1 → 0), что происходит если ты запустишь DFS без множества посещённых вершин?",
      choices: [
        "DFS посетит каждую вершину раз и потом остановится.",
        "DFS будет зацикливаться бесконечно, переживая одни и те же вершины.",
        "DFS обнаружит цикл и отметит его как невалидный.",
        "DFS посетит цикл дважды и потом остановится."
      ],
      answer: 1,
      hint: "Без множества посещённых вершин, DFS не имеет способа знать что оно уже посещало вершину. Он будет следовать цикл бесконечно: 0 → 1 → 0 → 1 → 0 → ..."
    },
    {
      kind: "choice",
      prompt: "Какая сложность по времени DFS на графе с V вершинами и E рёбрами?",
      choices: [
        "O(V)",
        "O(E)",
        "O(V + E)",
        "O(V × E)"
      ],
      answer: 2,
      hint: "DFS посещает каждую из V вершин раз и исследует каждое из E рёбер раз. Итого: O(V + E)."
    },
    {
      kind: "choice",
      prompt: "В рекурсивном DFS, какая структура данных используется имплицитно чтобы управлять отходом назад?",
      choices: [
        "Очередь",
        "Куча",
        "Стек вызовов функций (программный стек)",
        "Матрица смежности"
      ],
      answer: 2,
      hint: "Когда ты вызываешь dfsRecursive(neighbor), новый стек кадров создаётся. Когда рекурсия возвращается, программа автоматически отходит назад к вызывающему кадру."
    },
    {
      kind: "choice",
      prompt: "Какая сложность по памяти DFS, включая множество посещённых вершин и стек (рекурсивный или итеративный)?",
      choices: [
        "O(1)",
        "O(V)",
        "O(E)",
        "O(V²)"
      ],
      answer: 1,
      hint: "Множество посещённых вершин хранит до V записей. Рекурсия или явный стек может быть такой же глубокий как V в длинной цепи. Итого: O(V)."
    },
    {
      kind: "choice",
      prompt: "Если ты хочешь DFS исследовать соседей в одном и том же порядке для обеих рекурсивной и итеративной версий, как ты должен добавлять соседей на стек в итеративной версии?",
      choices: [
        "Добавь их в порядке появления в списке смежности.",
        "Добавь их в обратном порядке (итерируй список в обратном).",
        "Отсортируй их алфавитически перед добавлением.",
        "Добавь их один за раз в отдельных итерациях."
      ],
      answer: 1,
      hint: "Стек это LIFO (последний вошёл, первый вышел). Чтобы исследовать соседей в том же порядке как рекурсивный DFS (что обрабатывает их в порядке), добавь их в обратном. Последний добавленный будет извлечён первым."
    },
    {
      kind: "numeric",
      prompt: "Граф имеет 6 вершин и 9 рёбер. Какая примерная сложность по времени O(V + E) в терминах итогового числа посещений вершин и исследований рёбер?",
      answer: 15,
      hint: "V + E = 6 + 9 = 15. DFS посещает каждую вершину раз и исследует каждое ребро раз. Итого операций: O(15)."
    }
  ]}
/>

<Inset type="why">
**Почему DFS и не BFS?** Оба DFS и BFS исследуют все достижимые вершины и имеют одну и ту же сложность по времени O(V + E). DFS это проще кодировать (особенно рекурсивно) и использует меньше памяти когда граф это очень глубокий и узкий (длинная цепь). BFS это лучше когда ты нужна кратчайший путь (количество рёбер) или хочешь исследовать слой-за-слоем. За теперь, DFS это твой инструмент для исследования всех достижимых вершин и обнаружения циклов.
</Inset>

<Inset type="mistake">
**Забывание множество посещённых вершин.** Это соблазнительно пропустить множество посещённых вершин на малых примерах или когда ты думаешь "граф не имеет циклов." Но циклы это легко пропустить, и даже ациклические графы могут иметь множественные пути к одной вершине (DAG — ориентированный ациклический граф). Всегда включай множество посещённых вершин. Это дешево (O(V) память) и спасает тебя от бесконечных циклов.
</Inset>

<Inset type="edgecase">
**Несвязные графы.** Если граф имеет множественные несвязные компоненты, DFS из одной вершины только посетит вершины достижимые от этой вершины. Чтобы посетить все вершины в несвязном графе, ты должен начать новый DFS из непосещённой вершины. Это основание алгоритма связные-компоненты (идущего в позднем уроке).
</Inset>

<Inset type="practice">
**DFS для достижимости.** DFS отвечает вопрос: "Начиная от вершины A, какие вершины я могу достичь?" Если ты хочешь знать есть ли путь от A к B, запусти DFS от A и проверь есть ли B в множестве посещённых. Если да, есть путь. Если нет, нет пути.
</Inset>

<Check lang="ru">
  <Quiz
    id="quiz-dfs"
    pieceSlug="02-depth-first-search"
    lang="ru"
    question="Объясни что такое DFS, почему множество посещённых вершин необходимо для графов (но не деревьев), и опиши сложность по времени и памяти."
    choices={[
      {
        label: "DFS это обход графа, что исследует насколько возможно по каждому ребру перед отходом назад. В дереве, есть ровно один путь к каждому узлу, так ты никогда не посещаешь один и тот же узел дважды. Но графы могут иметь циклы: A → B → A. Без множество посещённых вершин, DFS будет зацикливаться навсегда переживая A и B. Множество посещённых вершин отслеживает какие вершины ты уже посетил и предотвращает переучитывание. Сложность по времени: O(V + E) потому что ты посещаешь каждую вершину раз (V) и исследуешь каждое ребро раз (E). Сложность по памяти: O(V) для множество посещённых вершин, стека рекурсии, или явного стека.",
        correct: true
      },
      {
        label: "DFS значит 'directed-first search' и используется только на ориентированных графах. Деревья это неориентированные, так DFS не применяется к ним. Множество посещённых вершин это опционально и только улучшает производительность. Сложность по времени и памяти зависит от размера графа.",
        misconception: "DFS значит 'depth-first search' и работает на обоих ориентированных и неориентированных графах. Деревья это особый случай графов (ациклические), так DFS работает на деревьях тоже. Множество посещённых вершин это критично, не опционально, чтобы предотвратить бесконечные циклы на циклах."
      },
      {
        label: "DFS исследует вершины в порядке поиска в ширину, посещая всех соседей текущей вершины перед движением к их соседям. Множество посещённых вершин предотвращает отход назад.",
        misconception: "Это поиск в ширину (BFS), не DFS. DFS идёт глубоко по каждой ветви сначала. Множество посещённых вершин предотвращает переучитывание, не отход назад."
      },
      {
        label: "DFS это алгоритм сортировки, что заказывает вершины по расстоянию от начала. Это похоже на алгоритм сортировки пузырьком.",
        misconception: "DFS это алгоритм обхода, не сортировки. Он не заказывает вершины; он просто посещает все достижимые вершины. Сортировка пузырьком не связана."
      }
    ]}
  />
</Check>

<Recap lang="ru">
<Term slug="depth_first_search">Поиск в глубину (DFS)</Term> исследует граф идя насколько возможно по каждому <Term slug="edge">ребру</Term> перед отходом назад. Это использует <Term slug="visited_set">множество посещённых вершин</Term> чтобы предотвратить переучитывание вершин — критично для графов с циклами, в отличие деревьев.

Две реализации:
1. **Рекурсивный DFS**: Естественный и простой; стек вызовов управляет отходом назад.
2. **Итеративный DFS**: Используёт явный <Term slug="stack">стек</Term>; яснее о состоянии.

Оба посещают каждую <Term slug="vertex">вершину</Term> раз и исследуют каждое ребро раз. Время: **O(V + E)**. Память: **O(V)** для множество посещённых вершин и стека.

Используй DFS чтобы исследовать все достижимые вершины, обнаружить связность, или проверить пути. **Следующий урок:** поиск в ширину (BFS) — исследуй слой за слоем вместо глубины.
</Recap>
