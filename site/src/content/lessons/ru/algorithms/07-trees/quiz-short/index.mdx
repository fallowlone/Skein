---
concepts:
- binary-tree
- traversal-order
- bst-invariant
- balance
- bfs-vs-dfs
- tree-dp
estMin: 13
lang: ru
level: middle
order: 8
prereqs: []
slug: quiz-short
sources:
- https://en.wikipedia.org/wiki/Binary_search_tree
- https://en.wikipedia.org/wiki/Tree_traversal
- https://en.wikipedia.org/wiki/Tree_diameter
status: ready
summary: "Промпты на свободное припоминание по юниту деревьев — рекурсивная структура, три порядка DFS, инвариант BST и баланс, BFS и паттерн tree DP. Сначала ответь, потом раскрой."
title: 'Деревья: тест на припоминание'
track: algorithms
unit: 07-trees
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";

<Hook>
Припоминание сильнее перечитывания. Для каждого промпта произнеси или запиши полный ответ по памяти, прежде чем открыть модельный ответ — именно усилие припоминания закрепляет структуру.
</Hook>

<Goal lang="ru">
Восстанови хребет юнита — почему рекурсия подходит дереву, для чего три порядка DFS, инвариант BST и оговорка про баланс, когда BFS лучше DFS, и паттерн tree DP — не заглядывая назад в уроки.
</Goal>

<RetrievalDrawer
  client:load
  id="07-trees-short-retrieval"
  lang="ru"
  questions={[
    { q: "Почему рекурсия — естественный способ обработки binary tree?", a: "Binary tree определено рекурсивно: оно либо пустое, либо узел с левым поддеревом и правым поддеревом, каждое из которых само является binary tree. Структура данных самоподобна, поэтому алгоритм той же формы идеально подходит — обработай узел, затем рекурсивно левое поддерево, затем правое. Базовый случай — пустое поддерево (null). Этот шаблон «реши поддеревья, затем скомбинируй с узлом» покрывает почти любую задачу: высота, число узлов, поиск, валидация и tree DP — все это его частные случаи с разными базовыми значениями и шагами комбинирования." },
    { q: "Назови три traversal в глубину и для чего каждый.", a: "Pre-order (node, left, right) посещает родителя до детей — для копирования дерева или любого распространения сверху вниз, где узлу нужен контекст предков. In-order (left, node, right) посещает узел между его поддеревьями — на BST выдаёт значения в порядке возрастания (sorted). Post-order (left, right, node) посещает узел только после обоих детей — для вычислений снизу вверх: освобождение дерева, подсчёт результатов поддерева (высоты, суммы). Все три O(n) по времени и O(h) по памяти стека; различаются лишь тем, КОГДА посещается узел относительно поддеревьев." },
    { q: "Сформулируй инвариант BST и что он даёт.", a: "Для каждого узла все значения в левом поддереве строго меньше значения узла, а все значения в правом поддереве строго больше (инвариант держится рекурсивно для каждого поддерева). Это позволяет поиску, вставке и удалению идти по одному пути от корня к листу: в каждом узле одно сравнение говорит идти влево или вправо, поэтому обе стороны никогда не исследуются. Стоимость O(h) — длина пути. In-order traversal у BST выдаёт sorted-порядок, то есть тот же инвариант, прочитанный как последовательность." },
    { q: "Почему O(h) не всегда означает O(log n) для BST и каков худший случай?", a: "O(h) логарифмично, только когда дерево сбалансировано, то есть h ~ log n. Инвариант BST ничего не говорит о форме. Если вставлять ключи в отсортированном (или обратно отсортированном) порядке, каждый новый ключ идёт в одну сторону, и дерево вырождается в цепочку высотой n-1, поэтому поиск и вставка становятся O(n) — не лучше связного списка. Это классический худший случай и вся мотивация для самобалансирующихся деревьев (AVL, red-black), которые перестраиваются при вставке, чтобы держать h ~ log n." },
    { q: "Когда выбирать level-order (BFS) вместо traversal в глубину и что заставляет его работать?", a: "Выбирай BFS всякий раз, когда ответ зависит от глубины или расстояния от корня: группировка узлов по уровням, минимальная глубина, вид справа (right-side view), кратчайшее невзвешенное расстояние. Он работает потому, что очередь FIFO держит текущий фронт: извлекаешь узел, добавляешь его детей в конец, поэтому все узлы глубины d обрабатываются раньше любого узла глубины d+1. Чтобы получить узлы сгруппированными по уровням, сделай снимок размера очереди в начале каждого раунда и обработай ровно столько. Память O(w) — максимальная ширина уровня (до ~n/2 в сбалансированном дереве)." },
    { q: "Опиши паттерн tree DP и почему возвращаемое значение отличается от ответа.", a: "Tree DP решает задачу за один проход post-order, где каждый узел возвращает локальную информацию родителю, а боковая переменная накапливает глобальный ответ. Возвращаемое значение и ответ различаются, потому что то, что родитель может переиспользовать, не равно полному локальному результату. Классический пример — diameter: длиннейший путь узла внутри использует оба поддерева (leftHeight + rightHeight), но родитель может продлить вверх лишь ОДНУ ветвь, поэтому узел возвращает свою высоту и обновляет общий максимум путём через себя. Та же форма у max-path-sum. Это избегает двух проходов и работает за O(n) по времени, O(h) по памяти." },
  ]}
/>

<Recap lang="ru">
Если ты смог восстановить каждый ответ по памяти, ты держишь хребет юнита: binary tree определено рекурсивно, поэтому рекурсия (реши поддеревья, скомбинируй с узлом) — универсальный шаблон; три порядка DFS различаются лишь тем, когда посещается узел, и in-order читает BST отсортированным; инвариант BST покупает операции O(h), но только баланс держит h около log n; BFS с очередью и снимком размера на каждом уровне — инструмент, когда важна глубина; а tree DP возвращает продлеваемую локальную информацию вверх, накапливая настоящий глобальный ответ сбоку.
</Recap>
