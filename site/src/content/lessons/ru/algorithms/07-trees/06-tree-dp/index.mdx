---
concepts:
- dynamic_programming
- tree
- recursion
- return_value
- side_variable
- diameter
- height_balanced
- postorder
estMin: 28
lang: ru
mathPrereqs: []
order: 6
prereqs:
- 01-the-binary-tree
- 02-traversals
- 03-level-order-traversal
- 04-recursion-on-trees
- 05-binary-search-trees
slug: 06-tree-dp
sources:
- https://en.wikipedia.org/wiki/Tree_diameter
- https://www.techiedelight.com/diameter-of-binary-tree/
- https://www.geeksforgeeks.org/diameter-of-binary-tree/
- https://www.enjoyalgorithms.com/blog/diameter-of-binary-tree/
status: ready
summary: 'Узел возвращает одно вверх (высоту), отслеживая другое в боковой переменной
  (диаметр). Классический пример: самый длинный путь между двумя узлами в двоичном
  дереве. Одно решение, два ответа, один проход.'
title: Динамическое программирование на деревьях
track: algorithms
unit: 07-trees
---



import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Idea from "~/components/lesson/Idea.astro";
import Code from "~/components/lesson/Code.astro";
import Trace from "~/components/lesson/Trace.astro";
import Complexity from "~/components/lesson/Complexity.astro";
import Check from "~/components/lesson/Check.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import AnnotatedCode from "~/components/algo/AnnotatedCode.astro";
import AlgoTrace from "~/components/algo/AlgoTrace.astro";
import CodeRunner from "~/components/algo/CodeRunner.astro";
import AlgoPractice from "~/components/algo/AlgoPractice.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";
import Term from "~/components/prose/Term.astro";
import DiagramFrame from "~/components/diagram/DiagramFrame.astro";

<Hook>
Вы решали задачи на деревьях, спрашивая оба поддерева о значении и комбинируя ответы. Но что если финальный ответ НЕ тот, что вы возвращаете? Что если узел должен возвратить свою высоту родителю, но вычислить что-то ещё — например, самый длинный путь через него — и положить то в боковую переменную? Это динамическое программирование на деревьях: рекурсивный вызов узла отвечает на один вопрос (возврат вверх), в то время как глобальная или боковая переменная отслеживает другой вопрос (актуальный ответ). Классический пример — диаметр двоичного дерева, самый длинный путь между любыми двумя узлами, где каждый узел возвращает свою высоту но отслеживает самый длинный путь, проходящий через него.
</Hook>

<Goal lang="ru">
После этого урока вы сможете реализовать рекурсивные функции на деревьях, где возвращаемое значение отличается от финального ответа, узнать и применить паттерн DP на деревьях (постпорядковая рекурсия + боковая переменная), решить задачу диаметра двоичного дерева и трассировать её правильно, реализовать проверку, что дерево высотно-сбалансировано, предсказать и проверить временную и пространственную сложность алгоритмов DP на деревьях, и расширить паттерн на вариации (самый длинный путь с суммой, диаметр в направленных деревьях).
</Goal>

<Idea lang="ru">
**Что такое DP на деревьях (динамическое программирование на деревьях)?**

<Term slug="dynamic_programming">DP на деревьях</Term> — техника, где рекурсивная функция на дереве решает две задачи одновременно:
1. **Возвращаемое значение** (отправляется вверх к родителю).
2. **Боковое вычисление** (отслеживается глобально или в изменяемой переменной).

Ключевой инсайт: рекурсивный вызов узла возвращает одну информацию вверх, в то время как боковая переменная (или глобальное состояние) накапливает финальный ответ.

**Паттерн**

```
function treeDp(node) {
  // Базовый случай
  if (node is null) return baseValue;
  
  // Рекурсируй на поддеревья
  leftInfo = treeDp(node.left);
  rightInfo = treeDp(node.right);
  
  // Комбинируй для возврата (отправляется вверх)
  returnValue = combine(leftInfo, rightInfo, node);
  
  // Обнови боковой ответ (глобальный ответ)
  globalAnswer = updateAnswer(globalAnswer, leftInfo, rightInfo, node);
  
  return returnValue;
}
```

На каждом узле:
- **Рекурсируй вниз** на оба поддерева (постпорядок).
- **Вычисли возвращаемое значение**, которое имеет смысл для родителя (например, высота).
- **Обнови боковую переменную** с финальным ответом (например, самый длинный путь).

**Пример: диаметр двоичного дерева**

<Term slug="diameter">Диаметр</Term> двоичного дерева — самый длинный путь между любыми двумя узлами, измеренный в рёбрах. Этот путь может или не может проходить через корень.

Для каждого узла спрашиваем: «Какой самый длинный путь проходит через тебя?» Ответ — сумма высот двух поддеревьев плюс 2 ребра (по одному к каждому ребёнку), или ноль если узел — лист.

Но чтобы ответить на этот вопрос для каждого узла, мы должны возвратить что-то другое родителю: **высоту поддерева**. Итак:
- **Возвращаем**: высота поддерева.
- **Боковая переменная**: самый длинный путь, найденный пока (диаметр).

**Диаметр в узле**

Для узла с высотой левого поддерева `lh` и высотой правого поддерева `rh`:
- Самый длинный путь через узел = `lh + rh + 2` (в рёбрах).
  (Значение `+2` учитывает рёбра от узла вниз к прямым детям.)
- Высота поддерева, укоренённого в узле = `1 + max(lh, rh)`.

**Соглашение о высоте**

**Листовой узел** (без детей) имеет высоту 0. **Пустое поддерево** (null) имеет высоту -1. Это делает математику рабочей: узел с двумя пустыми детьми возвращает `1 + max(-1, -1) = 0`, что правильно для листа.

</Idea>

<DiagramFrame label="DP на дереве, вычисленное снизу вверх на дереве из пяти узлов. Каждый узел хранит высоту своего поддерева — значение DP, возвращаемое родителю. Два листа внизу, узел 4 и узел 5, начинают с базового значения высота 0. Их родитель узел 2 комбинирует их как 1 плюс max из 0 и 0, что даёт высоту 1. Узел 3 — лист с базовым значением высота 0. Корень узел 1 комбинирует своих детей: узел 2 с высотой 1 и узел 3 с высотой 0 как 1 плюс max из 1 и 0, что даёт высоту 2. Значения текут вверх от листьев к корню." hue="var(--d-systems)" caption="DP на дереве снизу вверх: листья задают базовое значение h=0; каждый родитель сворачивает высоты детей как h = 1 + max(левая, правая). Значение корня (h=2) вычисляется последним, после того как оба поддерева отчитались вверх — постпорядок.">
  <svg viewBox="0 0 380 260" width="380" height="260" style="display:block;margin:0 auto;max-width:100%;height:auto;">
    <path d="M190 44 L110 120" fill="none" stroke="var(--hairline-strong)" stroke-width="1.4" />
    <path d="M190 44 L270 120" fill="none" stroke="var(--hairline-strong)" stroke-width="1.4" />
    <path d="M110 120 L70 196" fill="none" stroke="var(--hairline-strong)" stroke-width="1.4" />
    <path d="M110 120 L150 196" fill="none" stroke="var(--hairline-strong)" stroke-width="1.4" />
    <text x="245" y="24" text-anchor="middle" font-size="9" font-family="var(--font-body)" fill="var(--muted)">{"значения текут вверх ↑"}</text>
    <circle cx="190" cy="44" r="19" fill="var(--card-2)" stroke="var(--d-systems)" stroke-width="1.8" />
    <text x="190" y="42" text-anchor="middle" font-size="13" font-family="var(--font-body)" fill="var(--ink)">1</text>
    <text x="190" y="56" text-anchor="middle" font-size="10" font-family="var(--font-body)" fill="var(--d-systems)">{"h=2"}</text>
    <text x="246" y="48" text-anchor="start" font-size="9" font-family="var(--font-body)" fill="var(--muted)">{"1 + max(1, 0) = 2"}</text>
    <circle cx="110" cy="120" r="19" fill="var(--card-2)" stroke="var(--hairline-strong)" stroke-width="1.2" />
    <text x="110" y="118" text-anchor="middle" font-size="13" font-family="var(--font-body)" fill="var(--ink)">2</text>
    <text x="110" y="132" text-anchor="middle" font-size="10" font-family="var(--font-body)" fill="var(--d-systems)">{"h=1"}</text>
    <text x="84" y="106" text-anchor="end" font-size="9" font-family="var(--font-body)" fill="var(--muted)">{"1 + max(0, 0)"}</text>
    <circle cx="270" cy="120" r="19" fill="var(--card-2)" stroke="var(--hairline-strong)" stroke-width="1.2" />
    <text x="270" y="118" text-anchor="middle" font-size="13" font-family="var(--font-body)" fill="var(--ink)">3</text>
    <text x="270" y="132" text-anchor="middle" font-size="10" font-family="var(--font-body)" fill="var(--d-systems)">{"h=0"}</text>
    <circle cx="70" cy="196" r="19" fill="var(--card-2)" stroke="var(--hairline-strong)" stroke-width="1.2" />
    <text x="70" y="194" text-anchor="middle" font-size="13" font-family="var(--font-body)" fill="var(--ink)">4</text>
    <text x="70" y="208" text-anchor="middle" font-size="10" font-family="var(--font-body)" fill="var(--d-systems)">{"h=0"}</text>
    <circle cx="150" cy="196" r="19" fill="var(--card-2)" stroke="var(--hairline-strong)" stroke-width="1.2" />
    <text x="150" y="194" text-anchor="middle" font-size="13" font-family="var(--font-body)" fill="var(--ink)">5</text>
    <text x="150" y="208" text-anchor="middle" font-size="10" font-family="var(--font-body)" fill="var(--d-systems)">{"h=0"}</text>
    <text x="110" y="244" text-anchor="middle" font-size="9" font-family="var(--font-body)" fill="var(--muted)">{"листья: базовое значение h=0"}</text>
  </svg>
</DiagramFrame>

<Code lang="ru">
**Пример 1: Диаметр двоичного дерева**

<AnnotatedCode
  code={`function diameterOfBinaryTree(root) {
  let maxDiameter = 0;
  
  function dfs(node) {
    // Базовый случай: пустое поддерево имеет высоту -1
    if (node === null) return -1;
    
    // Рекурсируй на оба поддерева
    const leftHeight = dfs(node.left);
    const rightHeight = dfs(node.right);
    
    // Обнови глобальный ответ:
    // Самый длинный путь через этот узел (в рёбрах)
    const diameterThroughNode = leftHeight + rightHeight + 2;
    maxDiameter = Math.max(maxDiameter, diameterThroughNode);
    
    // Возврати высоту этого поддерева (отправляется родителю)
    return 1 + Math.max(leftHeight, rightHeight);
  }
  
  dfs(root);
  return maxDiameter;
}`}
  annotations={[
    { line: 1, text: "Функция вычисляет диаметр (самый длинный путь в рёбрах) дерева." },
    { line: 2, text: "Боковая переменная: отслеживает самый длинный путь, найденный пока." },
    { line: 5, text: "Базовый случай: пустое поддерево имеет высоту -1 (делает математику чистой)." },
    { line: 8, text: "Рекурсируй влево: получи высоту левого поддерева." },
    { line: 9, text: "Рекурсируй вправо: получи высоту правого поддерева." },
    { line: 12, text: "Самый длинный путь через ЭТОТ узел — (высота левая) + (высота правая) + 2 ребра." },
    { line: 13, text: "Обнови боковую переменную: помни самый длинный путь, видимый где-либо в дереве." },
    { line: 16, text: "Возврати высоту этого поддерева (то, что нужно родителю для решения его задачи)." }
  ]}
/>

Пример: Для дерева:
```
      1
     / \
    2   3
   / \
  4   5
```

Структура дерева:
- Узел 1 — корень.
- Узел 1's левое поддерево (укоренённое в 2) и правое поддерево (укоренённое в 3).
- Узел 2's левое поддерево (укоренённое в 4, лист) и правое поддерево (укоренённое в 5, лист).

Высоты (лист = 0, null = -1):
- Узел 4 (лист): высота = 0
- Узел 5 (лист): высота = 0
- Узел 3 (лист): высота = 0
- Узел 2 (два ребёнка): высота = 1 + max(0, 0) = 1
- Узел 1 (два ребёнка): высота = 1 + max(1, 0) = 2

Диаметры (самый длинный путь через каждый узел):
- Узел 4: диаметр = -1 + -1 + 2 = 0 (лист, путь — просто сам себя)
- Узел 5: диаметр = -1 + -1 + 2 = 0 (лист, путь — просто сам себя)
- Узел 3: диаметр = -1 + -1 + 2 = 0 (лист, путь — просто сам себя)
- Узел 2: диаметр = 0 + 0 + 2 = 2 (путь от 4 → 2 → 5 — 2 ребра)
- Узел 1: диаметр = 1 + 0 + 2 = 3 (путь от 4 → 2 → 1 → 3 — 3 ребра)

Самый длинный путь во всём дереве — 3 ребра: 4 → 2 → 1 → 3.

**Пример 2: Высотно-сбалансированное двоичное дерево**

Дерево <Term slug="height_balanced">высотно-сбалансировано</Term>, если для каждого узла высоты левого и правого поддеревьев отличаются не более чем на 1. Мы можем проверить это с помощью DP на деревьях: возвращаем высоту если сбалансировано, или -1 как сентинель для «несбалансировано».

<AnnotatedCode
  code={`function isBalanced(root) {
  function dfs(node) {
    // Базовый случай: пустое поддерево сбалансировано с высотой -1
    if (node === null) return -1;
    
    // Рекурсируй на левое поддерево
    const leftHeight = dfs(node.left);
    // Если левое несбалансировано, распространи сентинель -1
    if (leftHeight === -1) return -1;
    
    // Рекурсируй на правое поддерево
    const rightHeight = dfs(node.right);
    // Если правое несбалансировано, распространи сентинель -1
    if (rightHeight === -1) return -1;
    
    // Проверь, сбалансирован ли этот узел
    if (Math.abs(leftHeight - rightHeight) > 1) {
      return -1; // Не сбалансировано
    }
    
    // Возврати высоту этого поддерева
    return 1 + Math.max(leftHeight, rightHeight);
  }
  
  return dfs(root) !== -1; // true если высота была вычислена, false если -1 (несбалансировано)
}`}
  annotations={[
    { line: 1, text: "Функция проверяет, высотно-сбалансировано ли дерево, и возвращает true/false." },
    { line: 3, text: "Базовый случай: пустое дерево сбалансировано с высотой -1." },
    { line: 6, text: "Рекурсируй влево; получи высоту (или -1 если несбалансировано)." },
    { line: 8, text: "Проверка сентинеля: если левое поддерево несбалансировано, распространи -1 вверх." },
    { line: 11, text: "Рекурсируй вправо; получи высоту (или -1 если несбалансировано)." },
    { line: 13, text: "Проверка сентинеля: если правое поддерево несбалансировано, распространи -1 вверх." },
    { line: 16, text: "Если абсолютная разница высот больше 1, этот узел несбалансирован." },
    { line: 17, text: "Возврати -1 (сентинель) чтобы распространить несбалансированное состояние вверх." },
    { line: 21, text: "Возврати высоту этого поддерева (финальный ответ для родителя)." },
    { line: 24, text: "Если dfs(root) не -1, дерево сбалансировано." }
  ]}
/>

Пример: Для дерева:
```
      1
     / \
    2   3
   /
  4
```

- Узел 4 (лист): высота = 0, сбалансирован.
- Узел 3 (лист): высота = 0, сбалансирован.
- Узел 2 (левый ребёнок 4, нет правого): высота = 0, высота левого = 0, высота правого = -1. Разница = |0 - (-1)| = 1 ≤ 1, сбалансирован. Возвращает высоту 1.
- Узел 1 (левый ребёнок 2, правый ребёнок 3): высота левого = 1, высота правого = 0. Разница = |1 - 0| = 1 ≤ 1, сбалансирован. Возвращает высоту 2.

Результат: isBalanced(root) = true.

**Запусти оба на конкретных деревьях**

<CodeRunner
  code={`class Node {
  constructor(value, left = null, right = null) {
    this.value = value;
    this.left = left;
    this.right = right;
  }
}

// Функция диаметра
function diameterOfBinaryTree(root) {
  let maxDiameter = 0;
  
  function dfs(node) {
    if (node === null) return -1;
    const leftHeight = dfs(node.left);
    const rightHeight = dfs(node.right);
    const diameterThroughNode = leftHeight + rightHeight + 2;
    maxDiameter = Math.max(maxDiameter, diameterThroughNode);
    return 1 + Math.max(leftHeight, rightHeight);
  }
  
  dfs(root);
  return maxDiameter;
}

// Функция проверки сбалансированности
function isBalanced(root) {
  function dfs(node) {
    if (node === null) return -1;
    const leftHeight = dfs(node.left);
    if (leftHeight === -1) return -1;
    const rightHeight = dfs(node.right);
    if (rightHeight === -1) return -1;
    if (Math.abs(leftHeight - rightHeight) > 1) return -1;
    return 1 + Math.max(leftHeight, rightHeight);
  }
  return dfs(root) !== -1;
}

// Построй дерево:
//      1
//     / \\
//    2   3
//   / \\
//  4   5

let node4 = new Node(4);
let node5 = new Node(5);
let node2 = new Node(2, node4, node5);
let node3 = new Node(3);
let root = new Node(1, node2, node3);

console.log("Диаметр:", diameterOfBinaryTree(root));
console.log("Сбалансировано:", isBalanced(root));`}
  expected={`Диаметр: 3
Сбалансировано: true`}
/>

</Code>

<Trace lang="ru">
<AlgoTrace
  id="diameter-trace"
  lang="ru"
  code={[
    "let root = new Node(1, new Node(2, new Node(4), new Node(5)), new Node(3));",
    "diameterOfBinaryTree(root);"
  ]}
  frames={[
    { caption: "Вызови dfs(1, корень). Рекурсируй влево на узел 2.", line: 2, cells: [{ value: "dfs(1)", mark: "waiting" }] },
    { caption: "Вызови dfs(2). Рекурсируй влево на узел 4.", line: 2, cells: [{ value: "dfs(1)", mark: "waiting" }, { value: "dfs(2)", mark: "waiting" }] },
    { caption: "Вызови dfs(4). Узел 4 — лист. Рекурсируй влево (null) → высота -1.", line: 2, cells: [{ value: "dfs(1)", mark: "waiting" }, { value: "dfs(2)", mark: "waiting" }, { value: "dfs(4) left", mark: "waiting" }] },
    { caption: "Рекурсируй вправо на узел 4 (null) → высота -1. Диаметр через 4 = -1 + -1 + 2 = 0. Возврати высоту 1 + max(-1, -1) = 0.", line: 2, cells: [{ value: "dfs(1)", mark: "waiting" }, { value: "dfs(2)", mark: "waiting" }, { value: "dfs(4) → 0", mark: "done" }] },
    { caption: "Вернись к dfs(2). Теперь рекурсируй вправо на узел 5.", line: 2, cells: [{ value: "dfs(1)", mark: "waiting" }, { value: "dfs(2)", mark: "waiting" }, { value: "dfs(5)", mark: "waiting" }] },
    { caption: "Вызови dfs(5). Узел 5 — лист. Диаметр = 0. Возврати высоту 0.", line: 2, cells: [{ value: "dfs(1)", mark: "waiting" }, { value: "dfs(2)", mark: "waiting" }, { value: "dfs(5) → 0", mark: "done" }] },
    { caption: "Вернись к dfs(2). leftHeight=0, rightHeight=0. Диаметр через 2 = 0 + 0 + 2 = 2. Возврати высоту 1 + max(0, 0) = 1.", line: 2, cells: [{ value: "dfs(1)", mark: "waiting" }, { value: "dfs(2) → 1", mark: "done" }, { value: "maxDiameter = 2" }] },
    { caption: "Вернись к dfs(1). Теперь рекурсируй вправо на узел 3.", line: 2, cells: [{ value: "dfs(1)", mark: "waiting" }, { value: "dfs(3)", mark: "waiting" }] },
    { caption: "Вызови dfs(3). Узел 3 — лист. Диаметр = 0. Возврати высоту 0.", line: 2, cells: [{ value: "dfs(1)", mark: "waiting" }, { value: "dfs(3) → 0", mark: "done" }] },
    { caption: "Вернись к dfs(1). leftHeight=1, rightHeight=0. Диаметр через 1 = 1 + 0 + 2 = 3. maxDiameter = 3. Возврати высоту 1 + max(1, 0) = 2.", line: 2, cells: [{ value: "dfs(1) → 2", mark: "done" }, { value: "maxDiameter = 3" }] },
    { caption: "Все узлы посещены. Финальный диаметр = 3 (самый длинный путь от узла 4 → 2 → 1 → 3).", line: 2, cells: [{ value: "dfs(1) → 2", mark: "done" }, { value: "Final result: 3", mark: "done" }] }
  ]}
/>
</Trace>

<Complexity lang="ru">
**Временная сложность**

Алгоритмы DP на деревьях посещают каждый узел ровно один раз. На каждом узле мы выполняем O(1) работу (сравнения, арифметика, обновления переменных). Так как есть n узлов, общее время — **O(n)**.

**Пространственная сложность**

Оба алгоритма используют рекурсию, поэтому глубина стека вызовов равна высоте дерева. В худшем случае (вырожденное дерево) высота — O(n). В сбалансированном дереве высота — O(log n).

Таким образом, пространственная сложность — **O(h)**, где h — высота дерева.

</Complexity>

<AlgoPractice
  id="practice-tree-dp-1"
  lessonSlug="06-tree-dp"
  lang="ru"
  problems={[
    {
      kind: "choice",
      prompt: "В паттерне DP на деревьях, что узел возвращает своему родителю?",
      choices: [
        "Финальный ответ (диаметр, самая длинная сумма пути и т.д.)",
        "Богатую информацию, нужную родителю для решения его собственной подзадачи (например, высоту поддерева)",
        "Случайное значение, которое позже отбрасывается",
        "Максимальное значение в поддереве"
      ],
      answer: 1,
      hint: "Узел возвращает информацию, которая помогает родителю вычислить свой собственный результат. Для диаметра каждый узел возвращает свою высоту, не сам диаметр. Диаметр отслеживается в боковой переменной."
    },
    {
      kind: "choice",
      prompt: "Для дерева с такой структурой, какой диаметр (в рёбрах)?\n```\n    1\n   /\n  2\n /\n3\n```",
      choices: [
        "0",
        "1",
        "2",
        "3"
      ],
      answer: 2,
      hint: "Самый длинный путь — от узла 3 к узлу 1: 3 → 2 → 1, что составляет 2 ребра. Высоты: узел 3 (лист) = 0, узел 2 (один ребёнок) = 1, узел 1 (один ребёнок) = 2. Диаметр в узле 1 = (высота левого=1) + (высота правого=-1) + 2 = 2."
    },
    {
      kind: "choice",
      prompt: "Почему сентинельное значение (как -1) полезно в проверке высотно-сбалансированного дерева?",
      choices: [
        "Это позволяет нам кодировать две информации (высота И статус сбалансированности) в одном возвращаемом значении",
        "Это делает код быстрее",
        "Это исключает необходимость в рекурсии",
        "Это гарантирует, что дерево всегда сбалансировано"
      ],
      answer: 0,
      hint: "Если мы возвращаем -1, это сигнализирует 'несбалансировано' и мы можем распространить это вверх без отдельного булева значения. Если мы возвращаем неотрицательное число, мы знаем, что поддерево сбалансировано и число — высота. Это обычная техника DP на деревьях."
    },
    {
      kind: "choice",
      prompt: "В алгоритме диаметра, почему базовый случай возвращает -1 (не 0)?",
      choices: [
        "Потому что отрицательные числа быстрее вычислять",
        "Чтобы высота листового узла была 0 (лист имеет -1 + -1 + 1 = 0 высоту). Делает математику согласованной.",
        "Чтобы указать, что дерево пусто",
        "Потому что высота пустого дерева всегда -1 по определению"
      ],
      answer: 1,
      hint: "С null = -1, листовой узел (оба ребёнка null) имеет высоту = 1 + max(-1, -1) = 0. Это правильно: высота листа — 0 (это единственный узел, ноль рёбер от себя к себе). Без этого соглашения, математика требовала бы особых случаев."
    },
    {
      kind: "choice",
      prompt: "Которая из этих НЕ задача DP на деревьях (где возвращаемое значение ≠ финальный ответ)?",
      choices: [
        "Диаметр двоичного дерева (возврати высоту, отслеживай диаметр)",
        "Проверь, высотно-сбалансировано ли дерево (возврати высоту или -1, проверь сбалансированность на каждом узле)",
        "Считай общее количество узлов в дереве (возврати счёт, нет другого ответа)",
        "Найди самый длинный путь с суммой от корня к листу"
      ],
      answer: 2,
      hint: "Подсчёт узлов — простая рекурсия: вы возвращаете счёт (1 + leftCount + rightCount), и это возвращаемое значение ЯВЛЯЕТСЯ финальным ответом. Никакая боковая переменная не нужна. Это постпорядковая рекурсия, но не DP на деревьях (возвращаемое значение = ответ)."
    }
  ]}
/>

<Inset type="why">
**Почему DP на деревьях важна.** Много сложных задач на деревьях требуют вычисления двух вещей одновременно: локальной информации (высота, счёт), что течёт вверх, и глобальной информации (диаметр, самый длинный путь), что отслеживается на стороне. Без DP на деревьях, вы могли бы пройти по дереву дважды — раз для высот, раз для диаметра. DP на деревьях решает оба за один проход. Вот почему DP на деревьях — необходимый инструмент для задач интервью и компетитивного программирования.
</Inset>

<Inset type="mistake">
**Путница: смешивание возвращаемого значения с ответом.** Новички часто думают, что возвращаемое значение — ЭТО финальный ответ. Но в DP на деревьях, возвращаемое значение — локальная информация (высота), и финальный ответ — в боковой переменной (диаметр). Возвращаемое значение — то, что нужно родителю; боковая переменная — то, что вы пришли найти. Держи их отдельно.
</Inset>

<Inset type="edgecase">
**Краевой случай: дерево с одним узлом.** Если дерево — просто один узел, диаметр — 0 (самый длинный путь — сам узел, ноль рёбер). С нашим соглашением о высоте (null = -1, лист = 0), вычисление диаметра даёт 0 + 0 + 2 = 2, что неправильно! Исправление: учти это, начиная maxDiameter с 0 и убеждаясь, что вычисление диаметра правильно захватывает единый узел. Наш код делает это: единственный узел's диаметр = (-1) + (-1) + 2 = 0. Это правильно! Единственный узел имеет диаметр 0 рёбер (путь — просто сам себя).
</Inset>

<Check lang="ru">
  <Quiz
    id="quiz-tree-dp-1"
    pieceSlug="06-tree-dp"
    lang="ru"
    question="Объясни паттерн DP на деревьях: что такое возвращаемое значение против боковой переменной, и почему этот паттерн решает сложные задачи на деревьях за один проход?"
    choices={[
      {
        label: "Возвращаемое значение — богатая информация (как высота), которая помогает родителю решить его подзадачу. Боковая переменная отслеживает глобальный ответ (как диаметр). Один DFS-проход вычисляет оба: высоты текут вверх, диаметр обновляется на каждом узле. Это избегает нужды в двух отдельных проходах (один для высот, один для диаметра).",
        correct: true
      },
      {
        label: "Возвращаемое значение ЯВЛЯЕТСЯ финальным ответом (диаметр, самый длинный путь и т.д.), и боковая переменная — просто для отладки.",
        misconception: "Возвращаемое значение — локальная информация (высота) отправляется родителю. Боковая переменная (или изменяемый параметр) держит финальный ответ. Они не одно и то же."
      },
      {
        label: "DP на деревьях — то же самое, что простая рекурсия на деревьях; нет разницы между возвращаемым значением и ответом.",
        misconception: "В простой рекурсии (как считание узлов), возвращаемое значение ЯВЛЯЕТСЯ ответом. В DP на деревьях, возвращаемое значение и ответ — разные. DP на деревьях использует постпорядковую рекурсию + боковую переменную."
      },
      {
        label: "Возвращаемое значение игнорируется, и только боковая переменная имеет значение.",
        misconception: "Обе важны. Возвращаемое значение используется родителем (например, чтобы вычислить его собственный диаметр). Боковая переменная накапливает глобальный ответ. Обе нужны."
      }
    ]}
  />
</Check>

<Recap lang="ru">
**Динамическое программирование на деревьях** комбинирует рекурсию с боковой переменной для решения задач на деревьях за один проход. Паттерн: каждый узел **возвращает** локальную информацию (как высоту) своему родителю, в то время как **обновляет** боковую переменную с глобальным ответом (как диаметр). Классический пример — **диаметр двоичного дерева**, самый длинный путь между любыми двумя узлами. Проверка высотно-сбалансированного дерева использует **сентинельное значение** (-1) чтобы кодировать и высоту и статус сбалансированности в одном возвращаемом значении. DP на деревьях — необходимо для сложных задач на деревьях: это избегает множественных проходов и обнаруживает элегантную, двух-частную структуру решения. Теперь, когда вы увидите задачу на дерево, где «ответ не всегда проходит через корень», спросите себя: что именно узел должен вернуть родителю — и что записать в боковую переменную? Это разделение и есть вся техника.
</Recap>
