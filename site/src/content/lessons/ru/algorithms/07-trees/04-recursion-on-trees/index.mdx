---
concepts:
- recursion
- tree
- subtree
- base_case
- return_value
- combine
- height
- postorder
estMin: 24
lang: ru
mathPrereqs: []
order: 4
prereqs:
- 01-the-binary-tree
- 02-traversals
- 03-level-order-traversal
slug: 04-recursion-on-trees
sources:
- https://www.techiedelight.com/calculate-height-binary-tree-iterative-recursive/
- https://www.enjoyalgorithms.com/blog/find-height-of-a-binary-tree/
- https://algocademy.com/blog/how-to-handle-recursive-tree-problems-a-comprehensive-guide/
- https://medium.com/@shaswata.ssaha/understanding-recursive-tree-traversal-in-counting-nodes-9fe44e3511b7
status: ready
summary: 'Решай задачи на деревьях, задавая один и тот же вопрос к обоим поддеревьям,
  потом комбинируя их ответы с значением текущего узла. Базовый случай — пустое поддерево.
  Два примера: вычисление высоты и подсчёт узлов. Ментальная модель: возвращай информацию
  вверх; рекурсируй вниз.'
title: Рекурсия на деревьях
track: algorithms
unit: 07-trees
---



import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Idea from "~/components/lesson/Idea.astro";
import Code from "~/components/lesson/Code.astro";
import Trace from "~/components/lesson/Trace.astro";
import Complexity from "~/components/lesson/Complexity.astro";
import Check from "~/components/lesson/Check.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import AnnotatedCode from "~/components/algo/AnnotatedCode.astro";
import AlgoTrace from "~/components/algo/AlgoTrace.astro";
import CodeRunner from "~/components/algo/CodeRunner.astro";
import AlgoPractice from "~/components/algo/AlgoPractice.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Вы знаете три способа ходить по дереву: прямой, симметричный, обратный. Но ходить — это не решать. Чтобы решить задачу на дереве — найти высоту, подсчитать узлы, проверить, сбалансировано ли оно — нужно думать как дерево. Дерево это узел с двумя поддеревьями. Поэтому чтобы решить для узла, задай один и тот же вопрос обоим поддеревьям, потом скомбинируй их ответы с собственным значением узла. Это рекурсия на деревьях, и это самый мощный инструмент для задач на деревьях.
</Hook>

<Goal lang="ru">
После этого урока вы сможете писать рекурсивные функции для решения задач на деревьях, работая снизу вверх (постorder), распознавать и реализовывать паттерн «рекурсируй на поддеревьях и комбинируй», трассировать рекурсивную функцию дерева, чтобы проверить её корректность, объяснять, почему возврат значения от каждого поддерева — ключ к решению задачи, и выбирать правильный базовый случай (пустое поддерево) и правило комбинирования для вашей задачи.
</Goal>

<Idea lang="ru">
**Что означает «рекурсия на деревьях»?**

**Рекурсия на деревьях** — это способ решать задачи, где вы:
1. Решаете задачу для левого поддерева (рекурсивно).
2. Решаете задачу для правого поддерева (рекурсивно).
3. Комбинируете оба ответа с информацией о текущем узле.
4. Возвращаете комбинированный результат вверх по стеку вызовов.

Ключевая идея: дерево — это просто узел с двумя поддеревьями. Поэтому предположи, что твоя функция уже работает на поддеревьях, и построй ответ для текущего узла из этих результатов.

**Базовый случай: пустое поддерево**

Каждая рекурсивная функция дерева нуждается в базовом случае. В задачах на деревья базовый случай — это когда поддерево равно null (пусто). В этой точке ты возвращаешь значение, что имеет смысл для твоей задачи — часто 0 для подсчётов или -1 для высот.

**Комбинирование результатов: мышление постorder**

Порядок, в котором ты посещаешь дерево, имеет значение. Чтобы комбинировать результаты из поддеревьев с текущим узлом, ты должен:
1. Рекурсивно получить левый результат.
2. Рекурсивно получить правый результат.
3. Комбинировать их с текущим узлом.

Это **postorder**: левое поддерево, правое поддерево, потом сам узел.

**Ментальная модель**

Думай о рекурсии как о сообщениях, текущих ВВЕРХ по дереву:
- Каждый лист отправляет вверх своё базовое значение.
- Каждый внутренний узел спрашивает обоих детей «какой ответ на твоё поддерево?», потом комбинирует ответы и отправляет результат вверх своему родителю.
- Корень возвращает ответ для всего дерева.

</Idea>

<Code lang="ru">
**Пример 1: Вычисление высоты дерева**

**Высота** дерева — это самый длинный путь от корня до листа.

Рекурсивная идея: высота узла = 1 + max(высота левого поддерева, высота правого поддерева).

<AnnotatedCode
  code={`function height(node) {
  // Базовый случай: пустое дерево
  if (node === null) return -1;
  
  // Рекурсивный случай: спроси оба поддерева за их высоты
  const leftHeight = height(node.left);
  const rightHeight = height(node.right);
  
  // Комбинируй: возьми макс и добавь 1
  return 1 + Math.max(leftHeight, rightHeight);
}`}
  annotations={[
    { line: 1, text: "Функция вычисляет высоту поддерева, корень которого node." },
    { line: 3, text: "Базовый случай: пустое дерево имеет высоту -1. Это делает математику рабочей: высота одного узла = 1 + max(-1, -1) = 0." },
    { line: 6, text: "Рекурсивно спроси левое поддерево за его высоту." },
    { line: 7, text: "Рекурсивно спроси правое поддерево за его высоту." },
    { line: 10, text: "Комбинируй: высота этого узла — это 1 плюс высота более высокого поддерева." }
  ]}
/>

Пример: Для дерева:
```
      1
     / \
    2   3
   /
  4
```

- `height(4)` это лист: его два null ребёнка каждый возвращает -1, поэтому он возвращает 1 + max(-1, -1) = 0. Узел 4 имеет высоту 0.
- `height(2)` спрашивает height(4) и height(null), получает 0 и -1, возвращает 1 + max(0, -1) = 1. Узел 2 имеет высоту 1.
- `height(3)` это лист: возвращает 1 + max(-1, -1) = 0. Узел 3 имеет высоту 0.
- `height(1)` спрашивает height(2) и height(3), получает 1 и 0, возвращает 1 + max(1, 0) = 2. Дерево имеет высоту 2.

**Пример 2: Подсчёт узлов в дереве**

**Подсчёт** узлов: count = 1 (для этого узла) + count левого поддерева + count правого поддерева.

<AnnotatedCode
  code={`function countNodes(node) {
  // Базовый случай: пустое дерево имеет 0 узлов
  if (node === null) return 0;
  
  // Рекурсивный случай: спроси оба поддерева за их подсчёт узлов
  const leftCount = countNodes(node.left);
  const rightCount = countNodes(node.right);
  
  // Комбинируй: 1 (текущий узел) + левый + правый
  return 1 + leftCount + rightCount;
}`}
  annotations={[
    { line: 1, text: "Функция возвращает число узлов в поддереве, корень которого node." },
    { line: 3, text: "Базовый случай: пустое дерево имеет 0 узлов." },
    { line: 6, text: "Рекурсивно подсчитай узлы в левом поддереве." },
    { line: 7, text: "Рекурсивно подсчитай узлы в правом поддереве." },
    { line: 10, text: "Комбинируй: добавь 1 для текущего узла плюс все узлы в обоих поддеревьях." }
  ]}
/>

Пример: Для того же дерева:
- `countNodes(4)` это лист: его null ребёнка каждый возвращает 0, поэтому он возвращает 1 + 0 + 0 = 1.
- `countNodes(2)` спрашивает countNodes(4) и countNodes(null), получает 1 и 0, возвращает 1 + 1 + 0 = 2. Поддерево с корнем 2 имеет 2 узла.
- `countNodes(3)` возвращает 1 + 0 + 0 = 1. Поддерево с корнем 3 имеет 1 узел.
- `countNodes(1)` спрашивает countNodes(2) и countNodes(3), получает 2 и 1, возвращает 1 + 2 + 1 = 4. Всё дерево имеет 4 узла.

**Запуск обеих функций на конкретном дереве**

<CodeRunner
  code={`class Node {
  constructor(value, left = null, right = null) {
    this.value = value;
    this.left = left;
    this.right = right;
  }
}

function height(node) {
  if (node === null) return -1;
  const leftHeight = height(node.left);
  const rightHeight = height(node.right);
  return 1 + Math.max(leftHeight, rightHeight);
}

function countNodes(node) {
  if (node === null) return 0;
  const leftCount = countNodes(node.left);
  const rightCount = countNodes(node.right);
  return 1 + leftCount + rightCount;
}

// Построим дерево:
//      1
//     / \\
//    2   3
//   /
//  4

let node4 = new Node(4);
let node2 = new Node(2, node4);
let node3 = new Node(3);
let root = new Node(1, node2, node3);

console.log("Высота дерева:", height(root));
console.log("Подсчёт узлов:", countNodes(root));`}
  expected={`Высота дерева: 2
Подсчёт узлов: 4`}
/>

</Code>

<Trace lang="ru">
<AlgoTrace
  id="tree-recursion-height-trace"
  lang="ru"
  code={[
    "let root = new Node(1, new Node(2, new Node(4)), new Node(3));",
    "height(root);"
  ]}
  frames={[
    { caption: "Вызовите height(1). Нужны высоты левого (2) и правого (3).", line: 2, cells: [{ value: "height(1)", mark: "waiting" }] },
    { caption: "Вызовите height(2). Нужны высоты левого (4) и правого (null).", line: 2, cells: [{ value: "height(1)", mark: "waiting" }, { value: "height(2)", mark: "waiting" }] },
    { caption: "Вызовите height(4). Нужны высоты левого (null) и правого (null).", line: 2, cells: [{ value: "height(1)", mark: "waiting" }, { value: "height(2)", mark: "waiting" }, { value: "height(4)", mark: "waiting" }] },
    { caption: "height(4): оба ребёнка — null, каждый возвращает -1 (базовый случай). Возвращает 1 + max(-1, -1) = 0.", line: 2, cells: [{ value: "height(1)", mark: "waiting" }, { value: "height(2)", mark: "waiting" }, { value: "height(4) → 0", mark: "done" }] },
    { caption: "Вернулись в height(2). Левая высота = 0, правая высота (null) = -1. Возвращает 1 + max(0, -1) = 1.", line: 2, cells: [{ value: "height(1)", mark: "waiting" }, { value: "height(2) → 1", mark: "done" }, { value: "height(4) → 0", mark: "done" }] },
    { caption: "Вызовите height(3). Левое и правое — null. Базовый случай возвращает -1. Возвращает 1 + max(-1, -1) = 0.", line: 2, cells: [{ value: "height(1)", mark: "waiting" }, { value: "height(2) → 1", mark: "done" }, { value: "height(3) → 0", mark: "done" }] },
    { caption: "Вернулись в height(1). Левая высота = 1, правая высота = 0. Возвращает 1 + max(1, 0) = 2.", line: 2, cells: [{ value: "height(1) → 2", mark: "done" }, { value: "height(2) → 1", mark: "done" }, { value: "height(3) → 0", mark: "done" }] }
  ]}
/>

<AlgoTrace
  id="tree-recursion-count-trace"
  lang="ru"
  code={[
    "let root = new Node(1, new Node(2, new Node(4)), new Node(3));",
    "countNodes(root);"
  ]}
  frames={[
    { caption: "Вызовите countNodes(1). Нужны подсчёты из левого (2) и правого (3).", line: 2, cells: [{ value: "countNodes(1)", mark: "waiting" }] },
    { caption: "Вызовите countNodes(2). Нужны подсчёты из левого (4) и правого (null).", line: 2, cells: [{ value: "countNodes(1)", mark: "waiting" }, { value: "countNodes(2)", mark: "waiting" }] },
    { caption: "Вызовите countNodes(4). Левое и правое — null.", line: 2, cells: [{ value: "countNodes(1)", mark: "waiting" }, { value: "countNodes(2)", mark: "waiting" }, { value: "countNodes(4)", mark: "waiting" }] },
    { caption: "countNodes(4) попадает в базовый случай (null). Возвращает 0. Возвращает 1 + 0 + 0 = 1 (только узел 4).", line: 2, cells: [{ value: "countNodes(1)", mark: "waiting" }, { value: "countNodes(2)", mark: "waiting" }, { value: "countNodes(4) → 1", mark: "done" }] },
    { caption: "Вернулись в countNodes(2). Левый = 1, правый (null) = 0. Возвращает 1 + 1 + 0 = 2.", line: 2, cells: [{ value: "countNodes(1)", mark: "waiting" }, { value: "countNodes(2) → 2", mark: "done" }, { value: "countNodes(4) → 1", mark: "done" }] },
    { caption: "Вызовите countNodes(3). Левое и правое — null. Возвращает 1 + 0 + 0 = 1.", line: 2, cells: [{ value: "countNodes(1)", mark: "waiting" }, { value: "countNodes(2) → 2", mark: "done" }, { value: "countNodes(3) → 1", mark: "done" }] },
    { caption: "Вернулись в countNodes(1). Левый = 2, правый = 1. Возвращает 1 + 2 + 1 = 4. Общий подсчёт узлов.", line: 2, cells: [{ value: "countNodes(1) → 4", mark: "done" }, { value: "countNodes(2) → 2", mark: "done" }, { value: "countNodes(3) → 1", mark: "done" }] }
  ]}
/>
</Trace>

<Complexity lang="ru">
**Сложность по времени**

Обе функции `height` и `countNodes` посещают каждый узел в дереве ровно один раз. Каждый узел обрабатывается один раз: мы вычисляем максимум (для высоты) или добавляем подсчёт (для подсчёта узлов) за O(1) времени. Для дерева с n узлами, сложность по времени — **O(n)**.

**Сложность по памяти**

Место приходит из стека вызовов рекурсии. В худшем случае, глубина стека вызовов равна высоте дерева. В сбалансированном дереве высота O(log n). В перекошенном дереве (как связный список) высота O(n). Поэтому сложность по памяти — **O(h)**, где h — высота дерева.

</Complexity>

<AlgoPractice
  id="practice-tree-recursion-1"
  lessonSlug="04-recursion-on-trees"
  lang="ru"
  problems={[
    {
      kind: "choice",
      prompt: "Для дерева со структурой:\n    5\n   / \\\n  3   7\n /   / \\\n2   6   8\n\nКакова высота дерева?",
      choices: [
        "1",
        "2",
        "3",
        "4"
      ],
      answer: 1,
      hint: "Высота — это самый длинный путь от корня к листу. Путь 5 → 3 → 2 имеет длину 2 (число рёбер). Используя height = 1 + max(левая, правая), лист 2 имеет высоту -1, узел 3 имеет высоту 0, узел 5 имеет высоту 1."
    },
    {
      kind: "choice",
      prompt: "В функции height, почему мы возвращаем -1 для пустого поддерева вместо 0?",
      choices: [
        "Потому что отрицательные числа представляют пустые деревья.",
        "Чтобы одиночный узел (с двумя null детьми) возвращал высоту 1 + max(-1, -1) = 0. Это делает высоты правильными.",
        "Потому что -1 — обычное соглашение в алгоритмах.",
        "Чтобы указать ошибку в дереве."
      ],
      answer: 1,
      hint: "Одиночный изолированный узел должен иметь высоту 0 (нет рёбер ниже него). Если null возвращает 0, то height(лист) = 1 + max(0, 0) = 1, что неправильно. Если null возвращает -1, то height(лист) = 1 + max(-1, -1) = 0, что правильно."
    },
    {
      kind: "choice",
      prompt: "Для countNodes, что возвращает каждый рекурсивный вызов?",
      choices: [
        "Значение узла.",
        "True или false, в зависимости от того, существует ли узел.",
        "Общее число узлов в том поддереве, включая текущий узел.",
        "Высота поддерева."
      ],
      answer: 2,
      hint: "countNodes(node) возвращает, сколько узлов в поддереве с корнем node. Для листа, это 1. Для узла с детьми, это 1 + левый подсчёт + правый подсчёт."
    },
    {
      kind: "choice",
      prompt: "Когда вы вызываете height на корне дерева высоты 2, какое поддерево исследуется первым?",
      choices: [
        "Всегда левое поддерево.",
        "Всегда правое поддерево.",
        "Левое поддерево вызывается первым, но вы не можете определить высоту корня до того, как правое поддерево тоже вызовется и возвращит результат.",
        "Оба вызываются одновременно."
      ],
      answer: 2,
      hint: "Функция вызывает height(node.left) первой, но этот вызов идёт глубоко перед возвращением. Всё же, функция не может вычислить 1 + max(leftHeight, rightHeight) до того, как rightHeight тоже вычислится. Порядок исследования левый-первый из-за порядка рекурсивного вызова."
    },
    {
      kind: "choice",
      prompt: "Какова сложность по памяти функции height для перекошенного двоичного дерева (где каждый узел имеет только правого ребёнка)?",
      choices: [
        "O(1)",
        "O(log n)",
        "O(n)",
        "O(n²)"
      ],
      answer: 2,
      hint: "Перекошенное дерево имеет высоту n-1 (одно ребро за уровень). Стек вызовов рекурсии растёт так же глубоко, как высота дерева, поэтому память O(n)."
    }
  ]}
/>

<Inset type="why">
**Почему рекурсия на деревьях?** Итеративные решения на деревьях часто требуют стека или очереди, чтобы отслеживать узлы, которые вы ещё не обработали. Рекурсия делает это отслеживание для вас, используя стек вызовов. Она также совпадает с структурой дерева: дерево — это узел плюс два поддерева, поэтому рекурсивная структура отражает структуру данных.
</Inset>

<Inset type="edgecase">
**Пустые деревья и одиночные узлы.** Если корень null, базовый случай возвращает немедленно. Если дерево это одиночный узел, оба ребёнка null, рекурсивные вызовы попадают в базовый случай, и вы комбинируете базовые значения с текущим узлом. Никакой специальной обработки не нужно.
</Inset>

<Inset type="practice">
**Трассируй руками.** Нарисуй маленькое дерево (3–4 узла). Выбери задачу (высота, подсчёт или простое свойство). Выполни рекурсию руками: при каждом вызове, отметь рекурсивные вызовы, жди их возвращения, потом комбинируй. Смотри, как значения текут вверх по дереву. Это строит интуицию.
</Inset>

<Check lang="ru">
  <Quiz
    id="quiz-tree-recursion-1"
    pieceSlug="04-recursion-on-trees"
    lang="ru"
    question="Какова ключевая идея, что делает рекурсию на деревьях рабочей?"
    choices={[
      {
        label: "Дерево — это узел плюс два поддерева. Чтобы решить задачу для дерева, предположи, что ты уже можешь решить её для любого поддерева, реши оба поддерева, скомбинируй ответы с текущим узлом, и возвращай комбинированный результат.",
        correct: true
      },
      {
        label: "Рекурсия всегда быстрее итерации для задач на деревьях.",
        misconception: "Рекурсия и итерация могут иметь похожую временную сложность. Выбор зависит от читаемости и структуры задачи, не только от производительности."
      },
      {
        label: "Базовый случай всегда должен возвращать 0.",
        misconception: "Базовый случай возвращает значение, что имеет смысл для твоей задачи. Для высоты это -1; для подсчёта это 0; для других задач это может отличаться."
      },
      {
        label: "Ты должен обрабатывать узлы в конкретном порядке: pre-order, in-order или post-order.",
        misconception: "Для рекурсии на деревьях, postorder (левое, правое, узел) естественен, потому что ты комбинируешь результаты детей с текущим узлом. Другие порядки для других задач."
      }
    ]}
  />
</Check>

<Recap lang="ru">
**Рекурсия на деревьях** решает задачи, задавая один и тот же вопрос к обоим поддеревьям, комбинируя их ответы с текущим узлом, и возвращая комбинированный результат. Базовый случай — всегда пустое поддерево (null). Два рабочих примера — высота и countNodes — показывают паттерн: предположи, что оба поддерева возвращают значение, скомбинируй эти значения с информацией о текущем узле (max для высоты, сумма для подсчёта), и возвращай результат. Сложность по времени **O(n)** (посетить каждый узел один раз). Сложность по памяти **O(h)** (глубина стека рекурсии = высота дерева). Этот паттерн расширяется на любую задачу на дереве: проверить свойство, суммировать значения, найти максимум и т.д. Следующий урок охватывает двоичные деревья поиска: деревья с конкретным свойством (значения в левом поддереве меньше, значения в правом больше), что позволяет искать за **O(log n)** времени.
</Recap>
