---
concepts:
- binary-tree
- traversal-order
- bst-invariant
- balance
- bfs-vs-dfs
- tree-dp
estMin: 13
lang: ru
level: middle
order: 7
prereqs: []
slug: quiz-choice
sources:
- https://en.wikipedia.org/wiki/Binary_search_tree
- https://en.wikipedia.org/wiki/Tree_traversal
- https://en.wikipedia.org/wiki/Tree_(graph_theory)
status: ready
summary: "Синтез юнита деревьев в формате выбора — структура узла, выбор traversal, инвариант BST, баланс и высота, BFS против DFS и паттерн tree DP."
title: 'Деревья: тест с выбором ответа'
track: algorithms
unit: 07-trees
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Шесть вопросов, проходящих сквозь весь юнит. Каждый — это решение, которое ты принимаешь, когда берёшь дерево в реальном коде: не определение для пересказа, а какой traversal, какой инвариант, какая сложность реально в игре.
</Hook>

<Goal lang="ru">
Убедись, что ты связываешь структуру узла, выбор traversal, инвариант BST, баланс, BFS против DFS и паттерн tree DP — синтез, к которому вели отдельные уроки.
</Goal>

<Quiz
  id="07-trees-choice-1"
  lessonSlug="07-trees"
  lang="ru"
  question="Нужно вернуть значения binary search tree в порядке возрастания. Какой один traversal делает это напрямую и почему?"
  choices={[
    { label: "In-order (left, node, right). Инвариант BST кладёт все меньшие значения в левое поддерево, а большие — в правое, поэтому обход left-node-right выдаёт значения по неубыванию за один проход O(n).", correct: true },
    { label: "Pre-order (node, left, right), потому что корень посещается первым, а корень — медиана.", misconception: "Корень BST не медиана — это просто первое вставленное значение. Pre-order выдаёт корень раньше любого значения из левого поддерева, поэтому вывод не отсортирован. Только in-order даёт сортированный порядок." },
    { label: "Level-order (BFS), потому что обходит дерево сверху вниз по порядку.", misconception: "Level-order группирует узлы по глубине, а не по значению. Узел на глубине 1 может быть меньше или больше узла на глубине 2. Порядок BFS не связан с сортированным порядком в BST." },
    { label: "Post-order (left, right, node), потому что корень идёт последним и он максимум.", misconception: "Корень не минимум и не максимум BST. Post-order выдаёт оба ребёнка перед узлом, поэтому последовательность не отсортирована." },
  ]}
/>

<Quiz
  id="07-trees-choice-2"
  lessonSlug="07-trees"
  lang="ru"
  question="Коллега вставляет ключи 1, 2, 3, 4, 5, 6, 7 в пустой BST именно в этом порядке, а затем жалуется, что поиск медленный. Что произошло и какова высота?"
  choices={[
    { label: "Каждый ключ больше всех предыдущих, поэтому каждая вставка идёт вправо: дерево вырождается в правонаклонную цепочку из 7 узлов высотой 6. Поиск и вставка теперь O(n), а не O(log n) — вставка в отсортированном порядке это классический худший случай BST.", correct: true },
    { label: "Всё в порядке; BST из 7 узлов всегда имеет высоту 3, поэтому поиск O(log n).", misconception: "Высота зависит от порядка вставки, а не только от числа узлов. Только сбалансированная форма даёт высоту ~log n; отсортированная вставка даёт цепочку высотой n-1." },
    { label: "Инвариант BST нарушен, поэтому дерево испорчено.", misconception: "Инвариант (left < node < right) держится идеально — каждый узел больше своего родителя. Дерево — корректный BST; оно просто вырождено, и это проблема баланса, а не нарушение инварианта." },
    { label: "Поиск медленный, потому что in-order traversal это O(n).", misconception: "Поиск не обходит всё дерево — он идёт по одному пути от корня к цели. Медленность это длина пути: в цепочке высотой 6 этот путь до 7 узлов, то есть O(n)." },
  ]}
/>

<Quiz
  id="07-trees-choice-3"
  lessonSlug="07-trees"
  lang="ru"
  question="Нужно сгруппировать каждый узел по его глубине — весь уровень 0, затем весь уровень 1 и так далее. Какой подход подходит и какова ключевая деталь реализации?"
  choices={[
    { label: "Level-order traversal (BFS) с очередью. Захвати размер очереди в начале каждой внешней итерации; извлеки ровно столько узлов, чтобы завершить целый уровень до любого более глубокого узла, добавляя детей по ходу.", correct: true },
    { label: "Рекурсивный in-order traversal, записывая глубину каждого узла по ходу.", misconception: "In-order это обход в глубину: он спускается по левому хребту до того, как тронет правого ребёнка, поэтому узлы выходят перемешанными между уровнями. Пришлось бы группировать по глубине постфактум — BFS даёт группы по уровням напрямую." },
    { label: "Pre-order traversal — он посещает корень первым, а это уровень 0.", misconception: "Посещение корня первым не даёт группировки по уровням. После корня pre-order ныряет во всё левое поддерево (все глубины) до того, как увидит правого ребёнка. DFS не может завершить уровень прежде, чем уйти глубже." },
    { label: "BFS, но можно не отслеживать размер очереди — порядок сам даёт уровни.", misconception: "Простой BFS даёт правильный порядок посещения, но не границы уровней. Без снимка размера в начале каждого уровня нельзя понять, где один уровень кончается и начинается следующий." },
  ]}
/>

<Quiz
  id="07-trees-choice-4"
  lessonSlug="07-trees"
  lang="ru"
  question="В стандартном паттерне «рекурсия и комбинирование» для деревьев, что возвращает базовый случай для пустого поддерева при вычислении высоты (при соглашении, что лист имеет высоту 0)?"
  choices={[
    { label: "-1. При null = -1 лист вычисляет 1 + max(-1, -1) = 0, что и есть правильная высота листа. Сентинел -1 делает шаг комбинирования единообразным без отдельной обработки листьев.", correct: true },
    { label: "0, потому что пустое поддерево очевидно содержит ноль узлов.", misconception: "Высота считает рёбра, а не узлы. Если бы null возвращал 0, лист вычислил бы 1 + max(0, 0) = 1, переоценив на единицу. Подсчёт узлов это другая задача с другим базовым случаем." },
    { label: "null, и вызывающий проверяет его перед комбинированием.", misconception: "Возврат null заставляет добавлять отдельную ветку при каждом комбинировании — именно этого числовой сентинел -1 (или 0 для подсчёта) и призван избежать. Смысл соглашения в единообразной арифметике." },
    { label: "1, чтобы лист вышел с высотой 2.", misconception: "Это двойной счёт. Весь смысл выбора -1 для null в том, чтобы лист оказался на высоте 0 при заявленном в вопросе соглашении." },
  ]}
/>

<Quiz
  id="07-trees-choice-5"
  lessonSlug="07-trees"
  lang="ru"
  question="При вычислении diameter binary tree (длиннейший путь между любыми двумя узлами) почему каждый рекурсивный вызов возвращает высоту поддерева, а diameter держится в боковой переменной?"
  choices={[
    { label: "Длиннейший путь через узел это leftHeight + rightHeight (плюс два ребра к детям), но родитель не может переиспользовать этот целый путь — он может продлить только одну ветвь вниз. Поэтому каждый узел возвращает высоту (что нужно родителю) и обновляет общий максимум путём через узел (настоящий ответ): паттерн tree DP, один проход O(n), возвращаемое значение отличается от глобального ответа.", correct: true },
    { label: "Каждый узел должен просто возвращать diameter напрямую; боковая переменная избыточна.", misconception: "Если бы узел возвращал diameter, родитель не смог бы его продлить — путь через родителя использует высоту вниз одного ребёнка, а не полный diameter ребёнка. Это разные величины, поэтому паттерн их и разделяет." },
    { label: "Возврат высоты это оптимизация; можно было бы сделать два отдельных прохода.", misconception: "Два прохода (один для высот, один для diameter) обходят дерево дважды и заново выводят уже вычисленные высоты. Весь смысл tree DP в одном проходе: вернуть локальную информацию вверх, накопить глобальный ответ сбоку." },
    { label: "Diameter всегда проходит через корень, поэтому важно только значение корня.", misconception: "Длиннейший путь может лежать целиком внутри поддерева и вообще не касаться корня. Именно поэтому максимум надо обновлять в каждом узле, а не только в корне." },
  ]}
/>

<Quiz
  id="07-trees-choice-6"
  lessonSlug="07-trees"
  lang="ru"
  question="Два инженера сравнивают рекурсивный in-order traversal и level-order (BFS) обход одного и того же сбалансированного дерева с n узлами. Какова доминирующая дополнительная память каждого?"
  choices={[
    { label: "In-order использует O(h) памяти стека вызовов (h = высота, ~log n при балансе); BFS использует O(w) памяти очереди, где w — максимальная ширина уровня, которая для сбалансированного дерева ~n/2 на нижнем уровне. Поэтому BFS может тратить намного больше памяти на широком дереве, хотя оба O(n) по времени.", correct: true },
    { label: "Оба используют O(n) дополнительной памяти, потому что оба посещают n узлов.", misconception: "Посещение n узлов это стоимость по времени, а не по памяти. Рекурсия держит только текущий путь от корня к узлу (O(h)); BFS держит только текущий фронт (O(w)). Ни тот, ни другой в общем случае не держит все n узлов сразу." },
    { label: "In-order использует O(1) памяти, потому что у рекурсии нет накладных расходов.", misconception: "Рекурсия расходует стек вызовов — один кадр на уровень на активном пути, то есть O(h). На вырожденном (цепочка) дереве это O(n) и может переполнить стек." },
    { label: "BFS использует O(h), а in-order использует O(w); просто перепутаны местами.", misconception: "Наоборот: глубина DFS ограничена высотой h (стек), а ширина BFS ограничена самым широким уровнем w (очередь). На сбалансированном дереве h ~ log n, а w ~ n/2." },
  ]}
/>

<Recap lang="ru">
Сквозная линия юнита — одно соответствие: форма данных диктует инструмент. Binary tree это узел с двумя поддеревьями, поэтому рекурсия — естественный обход; три порядка DFS различаются лишь тем, *когда* посещается узел, и in-order — тот, что читает BST отсортированным. Инвариант BST покупает поиск O(h) — но только баланс держит h около log n, а вставка в отсортированном порядке это худший случай, обрушивающий её в O(n). BFS со снимком размера на каждом уровне — инструмент, когда важна глубина, меняющий стек на очередь. А tree DP возвращает локальную информацию вверх, накапливая глобальный ответ сбоку — один проход, две величины.
</Recap>
