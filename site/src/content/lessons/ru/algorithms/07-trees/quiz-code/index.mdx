---
concepts:
- traversal-order
- bst-invariant
- bst-validation
- tree-dp
- recursion
estMin: 14
lang: ru
level: middle
order: 9
prereqs: []
slug: quiz-code
sources:
- https://en.wikipedia.org/wiki/Tree_traversal
- https://en.wikipedia.org/wiki/Binary_search_tree
- https://en.wikipedia.org/wiki/Tree_diameter
status: ready
summary: "Прочитай четыре небольших сниппета про деревья — traversal, багованный валидатор BST, рекурсивную высоту и tree DP для суммы пути — и предскажи вывод или назови фикс с наибольшим рычагом."
title: 'Деревья: чтение кода и трассировка'
track: algorithms
unit: 07-trees
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Баги в деревьях прячутся в порядке двух рекурсивных вызовов и в разнице между локальной проверкой и глобальной. Прочитай каждый сниппет, прогони его в голове и выбери, что внимательный инженер заключит или починит первым.
</Hook>

<Goal lang="ru">
Потренируй главную петлю чтения кода деревьев: предскажи точный вывод traversal, заметь баг «на одно поддерево» в валидаторе BST, разбери базовый случай рекурсивной высоты и распознай, когда возвращаемое значение tree DP должно отличаться от ответа.
</Goal>

## Сниппет 1 — порядок traversal

```js
function walk(node) {
  if (node === null) return;
  walk(node.left);
  console.log(node.value);
  walk(node.right);
}

//        5
//       / \
//      3   8
//     / \   \
//    2   4   9
walk(root);
```

<Quiz
  id="07-trees-code-1"
  lessonSlug="07-trees"
  lang="ru"
  question="Какую точную последовательность это печатает и что это за traversal?"
  choices={[
    { label: "2, 3, 4, 5, 8, 9 — это in-order (left, node, right). Поскольку дерево это BST, in-order выдаёт значения в порядке возрастания.", correct: true },
    { label: "5, 3, 2, 4, 8, 9 — pre-order, ведь корень печатается первым.", misconception: "Pre-order печатал бы node.value до рекурсии влево. Здесь console.log стоит между walk(node.left) и walk(node.right), поэтому левое поддерево печатается полностью раньше узла — это in-order, а не pre-order." },
    { label: "2, 4, 3, 9, 8, 5 — post-order, дети перед узлом.", misconception: "Post-order печатает узел после обоих рекурсивных вызовов. Этот код печатает между двумя вызовами, поэтому 5 не последняя. Порядок in-order: 2, 3, 4, 5, 8, 9." },
    { label: "5, 3, 8, 2, 4, 9 — level-order, сверху вниз.", misconception: "Level-order требует очереди. Это рекурсия, то есть обход в глубину, поэтому breadth-first порядок по уровням она дать не может." },
  ]}
/>

## Сниппет 2 — баг валидатора BST

```js
function isBST(node) {
  if (node === null) return true;
  if (node.left && node.left.value >= node.value) return false;
  if (node.right && node.right.value <= node.value) return false;
  return isBST(node.left) && isBST(node.right);
}

//        5
//       / \
//      3   8
//         /
//        4      // 4 < 5, но сидит в ПРАВОМ поддереве 5
```

<Quiz
  id="07-trees-code-2"
  lessonSlug="07-trees"
  lang="ru"
  question="Этот валидатор возвращает true для дерева выше, которое НЕ является корректным BST. В чём баг?"
  choices={[
    { label: "Он проверяет каждый узел только против непосредственных детей, а не против диапазона, унаследованного от предков. Локально 4 — корректный левый ребёнок 8, но он живёт в правом поддереве 5, поэтому должен быть > 5. Фикс — передавать вниз границу (min, max) и проверять node.value против неё в каждом узле.", correct: true },
    { label: "Сравнения должны быть строгими (> и <), а не >= и <=.", misconception: "Строгость оператора влияет лишь на обработку дубликатов. Структурный баг остаётся: узел может пройти каждую проверку родитель-ребёнок и всё равно нарушить границу предка. Нужна передаваемая граница (min, max)." },
    { label: "Надо использовать in-order traversal вместо рекурсии.", misconception: "Проверка через in-order — один из корректных фиксов, но заявленный баг конкретен: этот код валидирует только локальные пары родитель-ребёнок. Метод границы (min, max) прямо чинит эту логику." },
    { label: "Базовый случай должен возвращать false для пустого дерева.", misconception: "Пустое дерево — корректный BST, поэтому true верно. Изменение базового случая сломало бы корректные деревья и ничего не делает с отсутствующей границей предка." },
  ]}
/>

## Сниппет 3 — рекурсивная высота

```js
function height(node) {
  if (node === null) return 0;       // заметь: возвращает 0, не -1
  return 1 + Math.max(height(node.left), height(node.right));
}

// одиночный лист
console.log(height(leaf));
```

<Quiz
  id="07-trees-code-3"
  lessonSlug="07-trees"
  lang="ru"
  question="При базовом случае null, возвращающем 0, что печатает height(leaf) и какое соглашение это подразумевает?"
  choices={[
    { label: "1. Лист вычисляет 1 + max(0, 0) = 1, поэтому эта функция считает узлы на длиннейшем пути от корня к листу (высота в узлах, лист = 1). Если же нужна высота в рёбрах, где лист = 0, базовый случай null должен возвращать -1.", correct: true },
    { label: "0, потому что у листа нет детей, поэтому max равен 0.", misconception: "Функция прибавляет 1 за текущий узел перед возвратом, поэтому лист возвращает 1 + max(0, 0) = 1, а не 0. Ноль — это то, что возвращают его null-дети." },
    { label: "-1, стандартный сентинел пустого поддерева.", misconception: "Этот базовый случай возвращает 0, а не -1. -1 — сентинел для высоты в рёбрах; при 0 здесь получается высота в узлах, и лист равен 1." },
    { label: "Бросает исключение, потому что у Math.max нет аргументов на листе.", misconception: "Math.max вызывается с двумя значениями: height(null) и height(null), каждое 0. Пустого вызова не происходит, поэтому он чисто возвращает 1." },
  ]}
/>

## Сниппет 4 — tree DP для суммы пути

```js
function maxPathSum(root) {
  let best = -Infinity;
  function gain(node) {
    if (node === null) return 0;
    const left = Math.max(gain(node.left), 0);   // отбросить отрицательные ветви
    const right = Math.max(gain(node.right), 0);
    best = Math.max(best, node.value + left + right);  // путь ЧЕРЕЗ узел
    return node.value + Math.max(left, right);          // путь, который родитель может продлить
  }
  gain(root);
  return best;
}
```

<Quiz
  id="07-trees-code-4"
  lessonSlug="07-trees"
  lang="ru"
  question="Почему gain() возвращает node.value + max(left, right), а best обновляется через node.value + left + right?"
  choices={[
    { label: "Путь, который изгибается через узел, может использовать обоих детей (node + left + right), но такой изогнутый путь нельзя продлить вверх — родитель может присоединиться только к одной ветви вниз. Поэтому gain возвращает значение одной ветви, которое родитель переиспользует, а best записывает лучший изгиб где угодно. Тот же tree-DP разрез, что и в задаче diameter.", correct: true },
    { label: "Это баг; обе строки должны прибавлять left + right.", misconception: "Если бы gain возвращал left + right, родитель строил бы путь, который раздваивается в этом узле, а затем раздваивается ещё выше — это уже не простой путь. Путь входит в узел только с одной стороны, поэтому возврат вверх должен использовать одну ветвь." },
    { label: "Зажимы max(..., 0) — это и есть настоящая логика; иначе обе формулы эквивалентны.", misconception: "Зажимы лишь отбрасывают отрицательные ветви; они не делают формулы равными. Через узел (обе ветви) и продлеваемое вверх (одна ветвь) — действительно разные величины." },
    { label: "best надо возвращать из gain, а не читать из замыкания.", misconception: "best должен быть единым общим аккумулятором на весь обход, что и даёт переменная замыкания. Возврат его из gain смешал бы локальное возвращаемое значение с глобальным ответом — именно тот баг, которого паттерн избегает." },
  ]}
/>

<Recap lang="ru">
Чтение кода деревьев сводится к нескольким рефлексам: найди строку посещения относительно двух рекурсивных вызовов, чтобы назвать traversal (и помни, что in-order на BST отсортирован); никогда не доверяй валидатору BST, который проверяет только родитель-против-ребёнка — корректность требует передаваемой границы (min, max); читай базовый случай null, чтобы понять, считается ли высота в узлах (возврат 0) или в рёбрах (возврат -1); и в tree DP следи за намеренным разрывом между тем, что узел возвращает родителю (одна продлеваемая ветвь), и тем, что он вносит в глобальный ответ (полный изгиб через него).
</Recap>
