---
concepts:
- level-order
- breadth-first
- BFS
- queue
- FIFO
- visit
- traversal
estMin: 22
lang: ru
mathPrereqs: []
order: 3
prereqs:
- 01-the-binary-tree
- 02-traversals
slug: 03-level-order-traversal
sources:
- https://www.geeksforgeeks.org/dsa/level-order-tree-traversal/
- https://www.enjoyalgorithms.com/blog/level-order-traversal-of-binary-tree/
- https://neetcode.io/problems/level-order-traversal-of-binary-tree/question
- https://www.geeksforgeeks.org/dsa/level-order-traversal-line-line-set-3-using-one-queue/
status: ready
summary: 'Посещение узлов слой за слоем, сверху вниз, слева направо, с помощью очереди.
  Альтернатива обходу в глубину, основанная на поиске в ширину. Почему очереди производят
  поуровневый обход, основной цикл и ключевой вариант: отслеживание размера очереди
  в начале каждого уровня.'
title: Поуровневый обход (BFS)
track: algorithms
unit: 07-trees
---



import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Idea from "~/components/lesson/Idea.astro";
import Code from "~/components/lesson/Code.astro";
import Trace from "~/components/lesson/Trace.astro";
import Complexity from "~/components/lesson/Complexity.astro";
import Check from "~/components/lesson/Check.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import AnnotatedCode from "~/components/algo/AnnotatedCode.astro";
import AlgoTrace from "~/components/algo/AlgoTrace.astro";
import CodeRunner from "~/components/algo/CodeRunner.astro";
import AlgoPractice from "~/components/algo/AlgoPractice.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Вы знаете три способа рекурсивно ходить по дереву — прямой, симметричный, обратный — все исследуют глубоко один ветвь, прежде чем вернуться. Но что если вам нужно увидеть все узлы на глубине 2 перед любым на глубине 3? Что если вам нужны узлы сгруппированы по слоям? Обходы в глубину не могут на это ответить. Вам нужен **поиск в ширину**: обход на основе очереди, который посещает узлы слой за слоем, слева направо, от корня вниз. Это поуровневый обход, и он раскрывает задачи, где позиция и глубина каждого узла имеют значение.
</Hook>

<Goal lang="ru">
После этого урока вы сможете реализовать поуровневый обход с помощью очереди, проследить алгоритм на конкретном дереве, объяснить, почему очередь производит поуровневый порядок, а рекурсия производит обход в глубину, реализовать вариант «обработка одного уровня за раз» путём отслеживания размера очереди, и выбрать поуровневый обход для задач, которым нужно понять дерево слой за слоем.
</Goal>

<Idea lang="ru">
**Что такое поуровневый обход?**

**Поуровневый обход** (также называемый **поиск в ширину** или **BFS**) посещает каждый узел в дереве слой за слоем, сверху вниз, слева направо. В отличие от рекурсивных обходов в глубину, которые вы видели в последнем уроке, поуровневый обход использует **очередь** — контейнер первый вход, первый выход — для управления порядком посещения.

**Почему очередь производит поуровневый порядок**

Магия поуровневого обхода — это свойство FIFO очереди. Когда вы обрабатываете узел и добавляете его детей в очередь, дети идут в конец очереди. Все узлы на текущем уровне ещё в очереди, впереди их детей. Поэтому вы обрабатываете все узлы на уровне 1, потом все на уровне 2, и так далее — вы никогда не пропускаете вперёд на более глубокий уровень, пока текущий уровень не исчерпан.

Контраст с рекурсией: когда вы вызываете `preOrder(node.left)`, вы сразу же ныряете глубоко в левое поддерево перед тем, как вообще увидеть правого ребёнка. Рекурсия — это обход в глубину. Очередь — это поиск в ширину.

**Основной поуровневый цикл**

1. Создайте очередь и добавьте корень.
2. Пока очередь не пуста:
   - Извлеките узел из очереди.
   - Обработайте его (запишите его значение).
   - Поместите в очередь его левого ребёнка (если он существует).
   - Поместите в очередь его правого ребёнка (если он существует).
3. Готово.

**Обработка одного уровня за раз**

Часто вам хочется сгруппировать узлы по уровню — все узлы уровня 1 вместе, все узлы уровня 2 вместе. Хитрость: в начале каждого уровня запишите размер очереди. Это число говорит вам ровно, сколько узлов на текущем уровне. Повторите это число раз, извлекая и помещая в очередь детей. Когда вы повторили `size` раз, все дети (следующий уровень) находятся в очереди, и вы циклируете снова.

Этот вариант необходим для задач, таких как «вернуть поуровневый обход как список списков» или «найти ширину каждого уровня».

</Idea>

<Code lang="ru">
**Базовый поуровневый обход**

Простейшая форма: поместите корень в очередь, потом извлекайте, обрабатывайте и помещайте в очередь детей до тех пор, пока очередь не пуста.

<AnnotatedCode
  code={`function levelOrder(root) {
  if (root === null) return [];
  
  const result = [];
  const queue = [root]; // Начните с корня
  
  while (queue.length > 0) {
    const node = queue.shift(); // Извлеките из начала
    
    // Обработайте узел
    result.push(node.value);
    
    // Поместите в очередь детей
    if (node.left !== null) queue.push(node.left);
    if (node.right !== null) queue.push(node.right);
  }
  
  return result;
}`}
  annotations={[
    { line: 1, text: "Функция принимает корень дерева." },
    { line: 2, text: "Если дерево пусто (null корень), верните пустой результат." },
    { line: 5, text: "Используйте массив в качестве очереди. Начните с корня." },
    { line: 7, text: "Циклируйте, пока очередь не пуста." },
    { line: 8, text: "Извлеките (удалите) узел из начала." },
    { line: 11, text: "Обработайте узел: запишите его значение." },
    { line: 14, text: "Поместите в очередь левого ребёнка, если он существует." },
    { line: 15, text: "Поместите в очередь правого ребёнка, если он существует." },
    { line: 18, text: "Верните результат: узлы в поуровневом порядке." }
  ]}
/>

Пример: Для дерева:
```
    1
   / \
  2   3
 /
4
```

Поуровневый обход посещает в этом порядке: **1 → 2 → 3 → 4**.

**Поуровневый обход с группировкой (один уровень за раз)**

Чтобы вернуть узлы, сгруппированные по уровню, запишите размер очереди в начале каждой итерации. Этот размер — это количество узлов на текущем уровне.

<AnnotatedCode
  code={`function levelOrderByLevel(root) {
  if (root === null) return [];
  
  const result = [];
  const queue = [root];
  
  while (queue.length > 0) {
    const levelSize = queue.length; // Сколько узлов на этом уровне
    const currentLevel = []; // Узлы этого уровня
    
    // Обработайте все узлы на текущем уровне
    for (let i = 0; i < levelSize; i++) {
      const node = queue.shift(); // Извлеките из начала
      currentLevel.push(node.value); // Запишите значение узла
      
      // Поместите в очередь детей для следующего уровня
      if (node.left !== null) queue.push(node.left);
      if (node.right !== null) queue.push(node.right);
    }
    
    // После обработки levelSize узлов, очередь теперь содержит только следующий уровень
    result.push(currentLevel);
  }
  
  return result;
}`}
  annotations={[
    { line: 1, text: "Функция возвращает список списков: каждый внутренний список — это один уровень." },
    { line: 2, text: "Если дерево пусто, верните пустой результат." },
    { line: 5, text: "Используйте массив в качестве очереди. Начните с корня." },
    { line: 7, text: "Циклируйте, пока очередь не пуста." },
    { line: 8, text: "Запишите размер очереди — это количество узлов на текущем уровне." },
    { line: 11, text: "Цикл от 0 до levelSize: обработайте ровно столько узлов." },
    { line: 12, text: "Извлеките узел из начала." },
    { line: 13, text: "Запишите его значение в список текущего уровня." },
    { line: 16, text: "Поместите в очередь его детей; они будут обработаны в следующей итерации." },
    { line: 20, text: "После цикла for добавьте узлы текущего уровня в результат." }
  ]}
/>

Пример: Для того же дерева:
```
    1
   / \
  2   3
 /
4
```

Результат: **[[1], [2, 3], [4]]** — каждый уровень как отдельный список.

**Запуск обоих на конкретном дереве**

<CodeRunner
  code={`class Node {
  constructor(value, left = null, right = null) {
    this.value = value;
    this.left = left;
    this.right = right;
  }
}

function levelOrder(root) {
  if (root === null) return [];
  
  const result = [];
  const queue = [root];
  
  while (queue.length > 0) {
    const node = queue.shift();
    result.push(node.value);
    
    if (node.left !== null) queue.push(node.left);
    if (node.right !== null) queue.push(node.right);
  }
  
  return result;
}

function levelOrderByLevel(root) {
  if (root === null) return [];
  
  const result = [];
  const queue = [root];
  
  while (queue.length > 0) {
    const levelSize = queue.length;
    const currentLevel = [];
    
    for (let i = 0; i < levelSize; i++) {
      const node = queue.shift();
      currentLevel.push(node.value);
      
      if (node.left !== null) queue.push(node.left);
      if (node.right !== null) queue.push(node.right);
    }
    
    result.push(currentLevel);
  }
  
  return result;
}

// Построим дерево:
//      1
//     / \\
//    2   3
//   /
//  4

let node4 = new Node(4);
let node2 = new Node(2, node4);
let node3 = new Node(3);
let root = new Node(1, node2, node3);

console.log("Простой поуровневый обход:");
console.log(levelOrder(root));

console.log("\\nПоуровневый обход по уровням:");
console.log(levelOrderByLevel(root));`}
  expected={`Простой поуровневый обход:
[ 1, 2, 3, 4 ]

Поуровневый обход по уровням:
[ [ 1 ], [ 2, 3 ], [ 4 ] ]`}
/>

</Code>

<Trace lang="ru">
<AlgoTrace
  id="levelorder-trace-basic"
  lang="ru"
  code={[
    "let root = new Node(1, new Node(2, new Node(4)), new Node(3));",
    "levelOrder(root);"
  ]}
  frames={[
    { caption: "Начало: queue = [1]. Извлеките 1, обработайте его. Поместите в очередь левого (2) и правого (3). Queue = [2, 3].", line: 2, cells: [{ value: "1", mark: "done" }] },
    { caption: "Queue = [2, 3]. Извлеките 2, обработайте его. Поместите в очередь левого (4). Queue = [3, 4].", line: 2, cells: [{ value: "1", mark: "done" }, { value: "2", mark: "done" }] },
    { caption: "Queue = [3, 4]. Извлеките 3, обработайте его. Нет детей. Queue = [4].", line: 2, cells: [{ value: "1", mark: "done" }, { value: "2", mark: "done" }, { value: "3", mark: "done" }] },
    { caption: "Queue = [4]. Извлеките 4, обработайте его. Нет детей. Queue = [].", line: 2, cells: [{ value: "1", mark: "done" }, { value: "2", mark: "done" }, { value: "3", mark: "done" }, { value: "4", mark: "done" }] },
    { caption: "Очередь пуста. Готово. Порядок посещения: 1, 2, 3, 4.", line: 2, cells: [{ value: "1", mark: "done" }, { value: "2", mark: "done" }, { value: "3", mark: "done" }, { value: "4", mark: "done" }] }
  ]}
/>

<AlgoTrace
  id="levelorder-trace-bylevel"
  lang="ru"
  code={[
    "let root = new Node(1, new Node(2, new Node(4)), new Node(3));",
    "levelOrderByLevel(root);"
  ]}
  frames={[
    { caption: "Начало: queue = [1]. levelSize = 1. Извлеките 1, обработайте его. Поместите в очередь 2, 3. Queue = [2, 3]. Уровень 0 готов: [1].", line: 2, cells: [{ value: "1", mark: "done" }] },
    { caption: "Queue = [2, 3]. levelSize = 2. Извлеките 2, обработайте его. Поместите в очередь 4. Queue = [3, 4].", line: 2, cells: [{ value: "1", mark: "done" }, { value: "2", mark: "done" }] },
    { caption: "Извлеките 3, обработайте его. Нет детей. Queue = [4]. Уровень 1 готов: [2, 3].", line: 2, cells: [{ value: "1", mark: "done" }, { value: "2", mark: "done" }, { value: "3", mark: "done" }] },
    { caption: "Queue = [4]. levelSize = 1. Извлеките 4, обработайте его. Нет детей. Queue = []. Уровень 2 готов: [4].", line: 2, cells: [{ value: "1", mark: "done" }, { value: "2", mark: "done" }, { value: "3", mark: "done" }, { value: "4", mark: "done" }] },
    { caption: "Очередь пуста. Готово. Результат: [[1], [2, 3], [4]].", line: 2, cells: [{ value: "1", mark: "done" }, { value: "2", mark: "done" }, { value: "3", mark: "done" }, { value: "4", mark: "done" }] }
  ]}
/>
</Trace>

<Complexity lang="ru">
**Сложность по времени**

Поуровневый обход посещает каждый узел в дереве ровно один раз. Каждый узел извлекается один раз и помещается в очередь один раз. Вся работа, проделанная на узел (обработка, проверки детей), — O(1). Поэтому для дерева с n узлами время **O(n)**.

**Сложность по памяти**

Место, используемое очередью, — это основной фактор. В любой момент очередь содержит узлы из не более чем двух последовательных уровней. В худшем случае максимальная ширина дерева — наибольшее число узлов на любом отдельном уровне — определяет максимальный размер очереди.

Для дерева с максимальной шириной w сложность пространства — **O(w)**.

В сбалансированном двоичном дереве w — это примерно n / 2 (последний уровень имеет много узлов), поэтому O(w) всё ещё лучше, чем O(n) пространства рекурсии в худшем случае. В искривлённом дереве (где каждый узел имеет только одного ребёнка) w = 1, поэтому поуровневый обход использует **O(1)** памяти — намного лучше, чем стек рекурсии O(n).

</Complexity>

<AlgoPractice
  id="practice-levelorder-1"
  lessonSlug="03-level-order-traversal"
  lang="ru"
  problems={[
    {
      kind: "choice",
      prompt: "Для дерева:\n    5\n   / \\\n  3   7\n / \\\n2   4\n\nКакой поуровневый обход?",
      choices: [
        "5, 3, 7, 2, 4",
        "2, 3, 4, 5, 7",
        "5, 3, 2, 4, 7",
        "2, 4, 3, 7, 5"
      ],
      answer: 0,
      hint: "Поуровневый обход идёт сверху вниз, слева направо. Посетите 5 (корень), потом 3 и 7 (уровень 1), потом 2 и 4 (уровень 2)."
    },
    {
      kind: "choice",
      prompt: "Почему очередь производит поуровневый порядок, а рекурсия производит обход в глубину?",
      choices: [
        "Очереди быстрее рекурсии.",
        "Свойство FIFO очереди гарантирует, что все узлы текущего уровня обработаны перед их детьми. Рекурсия ныряет глубоко в одного ребёнка перед исследованием другого.",
        "Рекурсия не может обработать все узлы на одном уровне.",
        "Очереди используют меньше памяти, чем рекурсия."
      ],
      answer: 1,
      hint: "Думайте о порядке операций. В очереди вы извлекаете все узлы уровня 1 перед тем, как любые узлы уровня 2 войдут в очередь. В рекурсии вы вызываете левое поддерево полностью перед вызовом правого."
    },
    {
      kind: "choice",
      prompt: "В варианте поуровневого обхода 'по уровням', что представляет levelSize?",
      choices: [
        "Общее количество узлов в дереве.",
        "Количество узлов в целой очереди.",
        "Количество узлов на текущем уровне — сколько раз извлекать и помещать в очередь в этой итерации.",
        "Глубина дерева."
      ],
      answer: 2,
      hint: "Мы запишем queue.length в начале каждой итерации. Это говорит нам, сколько узлов ждут — ровно узлы текущего уровня."
    },
    {
      kind: "choice",
      prompt: "После обработки levelSize узлов и помещения в очередь всех их детей, что содержит очередь?",
      choices: [
        "Узлы из текущего и следующего уровней смешаны вместе.",
        "Только узлы следующего уровня.",
        "Только листовые узлы.",
        "Очередь пуста."
      ],
      answer: 1,
      hint: "Вы извлекли levelSize узлов (текущий уровень). Вы поместили в очередь всех их детей. Те дети все на следующем уровне. Поэтому очередь теперь содержит только следующий уровень."
    },
    {
      kind: "choice",
      prompt: "Для дерева с максимальной шириной w (самым большим количеством узлов на любом уровне), какова сложность по памяти поуровневого обхода?",
      choices: [
        "O(1)",
        "O(log n)",
        "O(w)",
        "O(n)"
      ],
      answer: 2,
      hint: "Очередь содержит не более w узлов в любой момент. Поэтому память O(w)."
    }
  ]}
/>

<Inset type="why">
**Почему не рекурсия?** Рекурсия исследует в глубину: она идёт полностью вниз по левому поддереву перед исследованием правого. Если вам нужно узнать уровень каждого узла или сгруппировать узлы по глубине, рекурсия скрывает эту структуру. Очередь делает структуру уровня явной: извлеките уровень k, поместите в очередь уровень k+1.
</Inset>

<Inset type="practice">
**Проследите вариант 'по уровням' сами.** Нарисуйте дерево с 3 уровнями. Для поуровневого обхода 'по уровням', нарисуйте состояние очереди после обработки каждого уровня. Смотрите, как levelSize = queue.length запишет количество текущего уровня. Это главная идея.
</Inset>

<Inset type="edgecase">
**Пустые деревья и одиночные узлы.** Если корень null, верните немедленно (пустой результат). Если дерево — одиночный узел, очередь содержит только этот узел, цикл запускается один раз, и готово. Оба случая обработаны алгоритмом без специального кода.
</Inset>

<Check lang="ru">
  <Quiz
    id="quiz-levelorder-1"
    pieceSlug="03-level-order-traversal"
    lang="ru"
    question="Почему поуровневый обход полезен, даже хотя он и обходы в глубину оба посещают все узлы?"
    choices={[
      {
        label: "Потому что поуровневый обход явно обрабатывает узлы по уровню. Задачи, которым нужно понять структуру дерева слой за слоем (ширина каждого уровня, значения сгруппированы по глубине, расстояние от корня), естественны для поуровневого обхода. Обходы в глубину скрывают структуру уровня.",
        correct: true
      },
      {
        label: "Потому что поуровневый обход всегда быстрее обходов в глубину.",
        misconception: "Оба имеют временную сложность O(n). Разница в том, какие задачи каждый естественно решает, не в скорости."
      },
      {
        label: "Потому что очередь — это структура данных, а рекурсия — нет.",
        misconception: "Рекурсия — это техника, использующая стек вызовов (структура данных). Оба действительны; они подходят для разных задач."
      },
      {
        label: "Потому что поуровневый обход может обрабатывать деревья с циклами, но обходы в глубину не могут.",
        misconception: "Деревья не имеют циклов по определению. Ни один обход не обрабатывает циклы; оба разработаны для ациклических структур."
      }
    ]}
  />
</Check>

<Recap lang="ru">
**Поуровневый обход** (BFS) посещает узлы слой за слоем, сверху вниз, слева направо, используя свойство FIFO очереди. В отличие от рекурсивных обходов в глубину, поуровневый обход делает структуру слоя дерева явной. Базовый алгоритм: поместите в очередь корень, циклируйте, пока очередь не пуста, извлеките и обработайте узел, потом поместите в очередь его детей. Чтобы сгруппировать узлы по уровню, запишите размер очереди в начале каждой итерации — это число точно говорит вам, сколько узлов на текущем уровне. Сложность по времени **O(n)** (посетите каждый узел один раз). Сложность по памяти **O(w)**, где w — максимальная ширина дерева — лучше, чем O(n) пространства рекурсии во многих случаях. Следующий урок охватывает рекурсию на деревьях: решение задач на деревьях путём объединения результатов, возвращённых из поддеревьев.
</Recap>
