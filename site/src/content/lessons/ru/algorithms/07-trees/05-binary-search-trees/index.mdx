---
concepts:
- binary_tree
- bst_property
- ordering_invariant
- search
- insertion
- in_order_traversal
- balanced
- degenerate
- height
estMin: 26
lang: ru
mathPrereqs: []
order: 5
prereqs:
- 01-the-binary-tree
- 02-traversals
- 03-level-order-traversal
- 04-recursion-on-trees
slug: 05-binary-search-trees
sources:
- https://en.wikipedia.org/wiki/Binary_search_tree
- https://www.geeksforgeeks.org/dsa/complexity-different-operations-binary-tree-binary-search-tree-avl-tree/
- https://www.freecodecamp.org/news/binary-search-trees-bst-explained-with-examples/
- https://www.enjoyalgorithms.com/blog/insertion-in-binary-search-tree/
status: ready
summary: Двоичное дерево, где каждое значение в левом поддереве меньше узла и в правом
  больше. Это свойство порядка делает поиск и вставку O(h). Обход inorder даёт отсортированный
  порядок. Высота варьируется от O(log n) для сбалансированных до O(n) для вырожденных
  деревьев.
title: Двоичные деревья поиска
track: algorithms
unit: 07-trees
---



import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Idea from "~/components/lesson/Idea.astro";
import Code from "~/components/lesson/Code.astro";
import Trace from "~/components/lesson/Trace.astro";
import Complexity from "~/components/lesson/Complexity.astro";
import Check from "~/components/lesson/Check.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import AnnotatedCode from "~/components/algo/AnnotatedCode.astro";
import AlgoTrace from "~/components/algo/AlgoTrace.astro";
import CodeRunner from "~/components/algo/CodeRunner.astro";
import AlgoPractice from "~/components/algo/AlgoPractice.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";
import Term from "~/components/prose/Term.astro";
import DiagramFrame from "~/components/diagram/DiagramFrame.astro";

<Hook>
До сих пор вы ходили по деревьям, строили их, измеряли их высоту. Но поиск в дереве — в любом дереве — требует ходить по каждой ветке. Двоичное дерево поиска это меняет. Оно упорядочивает значения: меньшее слева, большее справа. Это свойство позволяет вам искать, сравнивая, идя влево или вправо, никогда не возвращаясь. Поиск становится O(h). И так как сбалансированное двоичное дерево поиска имеет высоту O(log n), вы ищите за логарифмическое время.
</Hook>

<Goal lang="ru">
После этого урока вы сможете определить свойство BST (левое поддерево меньше, правое больше), реализовать поиск и вставку в BST, трассировать обе операции, чтобы проверить корректность, объяснить, почему обход в порядке inorder дерева поиска даёт отсортированный порядок, предсказать временную сложность поиска и вставки (O(h) где h — высота), и признать риск того, что несбалансированное дерево может выродиться до O(n) высоты.
</Goal>

<Idea lang="ru">
**Что такое двоичное дерево поиска?**

<Term slug="binary_search_tree">Двоичное дерево поиска (BST)</Term> — это двоичное дерево со свойством: для каждого узла все значения в левом поддереве меньше значения узла, а все значения в правом поддереве больше значения узла.

Это **свойство порядка** означает, что дерево кодирует отсортированный порядок. Это также означает, что вы можете искать, сравнивая: если вы ищите значение меньше текущего узла, идите влево; если больше, идите вправо. Если равно, вы нашли его. Так как вы никогда не возвращаетесь, поиск быстр.

**Свойство BST (левое < узел < правое)**

Для каждого узла:
- Все значения в левом поддереве < значение узла.
- Все значения в правом поддереве > значение узла.
- Оба левое и правое поддеревья также являются BST-ами (свойство применяется рекурсивно).

Пример:
```
      5
     / \
    3   8
   / \ / \
  1  4 7  9
```

Здесь узел 5 имеет левое поддерево {1, 3, 4} (все < 5) и правое поддерево {7, 8, 9} (все > 5). Узел 3 имеет левое поддерево {1} (< 3) и правое поддерево {4} (> 3).

**Поиск в BST**

Чтобы найти значение:
1. Начните с корня.
2. Сравните цель с значением текущего узла.
3. Если равна, нашли его.
4. Если цель меньше, рекурсируйте на левое поддерево.
5. Если цель больше, рекурсируйте на правое поддерево.
6. Если вы достигли null, значение не в дереве.

На каждом шаге вы исключаете половину оставшегося дерева (или большую часть, если дерево не идеально сбалансировано). Это даёт O(h) сравнений, где h — высота.

**Вставка в BST**

Чтобы вставить значение:
1. Начните с корня.
2. Сравните значение с текущим узлом.
3. Если значение меньше, рекурсируйте влево; если больше, рекурсируйте вправо.
4. Когда вы достигнете null, это место, где вы прикрепляете новый лист.

Вставка также занимает O(h) времени, потому что вы должны найти место вставки, сравнивая на каждом уровне.

**Обход в порядке inorder даёт отсортированный порядок**

Обход в порядке inorder (левое, текущее, правое) BST посещает узлы в восходящем порядке. Это потому что:
- Вы посещаете левое поддерево (все значения меньше узла).
- Потом сам узел.
- Потом правое поддерево (все значения больше узла).

Поэтому значения появляются в отсортированном порядке.

**Высота критична**

Времена поиска и вставки зависят от высоты h:
- **Сбалансированное дерево**: высота O(log n), поэтому операции O(log n).
- **Вырожденное дерево** (например, все левые дети): высота O(n), поэтому операции O(n).

Если вы вставляете значения в отсортированном порядке [1, 2, 3, 4, 5], дерево вырождается в связный список, и вы теряете логарифмическое-временное преимущество. Самобалансирующиеся деревья (AVL, красно-чёрные) сохраняют баланс, но они выходят за рамки этого урока.

<DiagramFrame
  label="Двоичное дерево поиска с корнем 5, левым ребёнком 3, правым ребёнком 8 и листьями 1, 4, 7, 9. Поиск цели 4 выделен как путь: в корне 5 сравнение 4 < 5 ведёт влево к узлу 3; в узле 3 сравнение 4 > 3 ведёт вправо к узлу 4, где 4 равно 4 и цель найдена. Путь проходит 3 из 4 уровней, показывая, что поиск стоит O(высоты)."
  hue="var(--d-systems)"
  caption="Поиск цели 4. На каждом узле одно сравнение решает направление: 4 < 5 идём влево, 4 > 3 идём вправо, 4 = 4 найдено. Инвариант BST (левое поддерево < узел < правое поддерево) означает, что неверное поддерево никогда не содержит цель, поэтому путь ограничен высотой — поиск это O(высоты)."
>
  <svg viewBox="0 0 640 340" width="640" height="340" xmlns="http://www.w3.org/2000/svg" font-family="var(--font-body, sans-serif)">
    <defs>
      <marker id="bst-arrow-ru" markerWidth="9" markerHeight="9" refX="7" refY="4" orient="auto">
        <path d="M0,0 L8,4 L0,8 Z" fill="var(--d-systems)" />
      </marker>
    </defs>

    <text x="320" y="24" text-anchor="middle" font-size="13" font-weight="600" fill="var(--ink, currentColor)">цель = 4</text>

    <line x1="320" y1="74" x2="190" y2="124" stroke="var(--d-systems)" stroke-width="3" marker-end="url(#bst-arrow-ru)" />
    <line x1="320" y1="74" x2="450" y2="124" stroke="var(--hairline-2, #888)" stroke-width="1.5" />
    <line x1="180" y1="166" x2="120" y2="214" stroke="var(--hairline-2, #888)" stroke-width="1.5" />
    <line x1="180" y1="166" x2="240" y2="214" stroke="var(--d-systems)" stroke-width="3" marker-end="url(#bst-arrow-ru)" />
    <line x1="460" y1="166" x2="400" y2="214" stroke="var(--hairline-2, #888)" stroke-width="1.5" />
    <line x1="460" y1="166" x2="520" y2="214" stroke="var(--hairline-2, #888)" stroke-width="1.5" />

    <text x="240" y="106" text-anchor="middle" font-size="13" font-weight="600" fill="var(--d-systems)">{"4 < 5"}</text>
    <text x="225" y="198" text-anchor="middle" font-size="13" font-weight="600" fill="var(--d-systems)">{"4 > 3"}</text>

    <circle cx="320" cy="50" r="26" fill="var(--card, #fff)" stroke="var(--d-systems)" stroke-width="3" />
    <text x="320" y="56" text-anchor="middle" font-size="17" font-weight="700" fill="var(--ink, currentColor)">5</text>

    <circle cx="180" cy="140" r="26" fill="var(--card, #fff)" stroke="var(--d-systems)" stroke-width="3" />
    <text x="180" y="146" text-anchor="middle" font-size="17" font-weight="700" fill="var(--ink, currentColor)">3</text>

    <circle cx="460" cy="140" r="26" fill="var(--card, #fff)" stroke="var(--hairline-2, #888)" stroke-width="1.5" />
    <text x="460" y="146" text-anchor="middle" font-size="17" font-weight="700" fill="var(--muted, #888)">8</text>

    <circle cx="110" cy="240" r="26" fill="var(--card, #fff)" stroke="var(--hairline-2, #888)" stroke-width="1.5" />
    <text x="110" y="246" text-anchor="middle" font-size="17" font-weight="700" fill="var(--muted, #888)">1</text>

    <circle cx="250" cy="240" r="26" fill="var(--card, #fff)" stroke="var(--d-systems)" stroke-width="3" />
    <text x="250" y="246" text-anchor="middle" font-size="17" font-weight="700" fill="var(--ink, currentColor)">4</text>
    <text x="250" y="288" text-anchor="middle" font-size="12" font-weight="600" fill="var(--d-systems)">{"4 = 4 найдено"}</text>

    <circle cx="390" cy="240" r="26" fill="var(--card, #fff)" stroke="var(--hairline-2, #888)" stroke-width="1.5" />
    <text x="390" y="246" text-anchor="middle" font-size="17" font-weight="700" fill="var(--muted, #888)">7</text>

    <circle cx="530" cy="240" r="26" fill="var(--card, #fff)" stroke="var(--hairline-2, #888)" stroke-width="1.5" />
    <text x="530" y="246" text-anchor="middle" font-size="17" font-weight="700" fill="var(--muted, #888)">9</text>

    <text x="612" y="146" text-anchor="end" font-size="11" fill="var(--muted, #888)">{"левое поддерево < 5 < правое поддерево"}</text>
  </svg>
</DiagramFrame>

</Idea>

<Code lang="ru">
**Пример 1: Поиск значения в BST**

<AnnotatedCode
  code={`function search(node, target) {
  // Базовый случай: значение не найдено
  if (node === null) return false;
  
  // Проверь текущий узел
  if (node.value === target) return true;
  
  // Рекурсируй влево если цель меньше
  if (target < node.value) {
    return search(node.left, target);
  }
  
  // Рекурсируй вправо если цель больше
  return search(node.right, target);
}`}
  annotations={[
    { line: 1, text: "Функция поиска: принимает узел и целевое значение." },
    { line: 3, text: "Базовый случай: если узел null, значение не было найдено." },
    { line: 6, text: "Если цель равна значению узла, мы нашли его." },
    { line: 9, text: "Если цель меньше, она может быть только в левом поддереве (свойство BST)." },
    { line: 10, text: "Рекурсируй на левое поддерево." },
    { line: 13, text: "Иначе, цель должна быть больше, поэтому рекурсируй на правое поддерево." }
  ]}
/>

Пример: Поиск 4 в дереве:
```
      5
     / \
    3   8
   / \ / \
  1  4 7  9
```

- Начните с 5. Цель (4) < 5, идите влево.
- На 3. Цель (4) > 3, идите вправо.
- На 4. Цель (4) == 4, вернулась true.

Поиск занял 3 сравнения.

**Пример 2: Вставка значения в BST**

<AnnotatedCode
  code={`function insert(node, value) {
  // Базовый случай: прикрепи новый узел здесь
  if (node === null) {
    return new Node(value);
  }
  
  // Рекурсируй влево если значение меньше
  if (value < node.value) {
    node.left = insert(node.left, value);
  }
  // Рекурсируй вправо если значение больше
  else if (value > node.value) {
    node.right = insert(node.right, value);
  }
  // Игнорируй дубликаты (опционально; некоторые BST их позволяют)
  
  return node;
}`}
  annotations={[
    { line: 1, text: "Функция вставки: добавь значение в дерево с корнем node." },
    { line: 3, text: "Базовый случай: если узел null, создай и верни новый узел с этим значением." },
    { line: 7, text: "Если значение меньше текущего узла, оно принадлежит левому поддереву." },
    { line: 8, text: "Рекурсивно вставь в левое поддерево и обнови левого ребёнка." },
    { line: 10, text: "Если значение больше, оно принадлежит правому поддереву." },
    { line: 12, text: "Рекурсивно вставь в правое поддерево и обнови правого ребёнка." },
    { line: 16, text: "Верни (возможно обновленный) узел." }
  ]}
/>

Пример: Вставить 6 в дерево:
```
      5
     / \
    3   8
   / \ / \
  1  4 7  9
```

- Начните с 5. Значение (6) > 5, идите вправо.
- На 8. Значение (6) < 8, идите влево.
- На 7. Значение (6) < 7, идите влево.
- На null. Создайте новый узел со значением 6.

Результат:
```
      5
     / \
    3   8
   / \ / \
  1  4 7  9
      /
     6
```

Вставка также заняла 4 шага (сравнения + навигация по дереву).

**Запуск поиска и вставки на конкретном дереве**

<CodeRunner
  code={`class Node {
  constructor(value, left = null, right = null) {
    this.value = value;
    this.left = left;
    this.right = right;
  }
}

function search(node, target) {
  if (node === null) return false;
  if (node.value === target) return true;
  if (target < node.value) return search(node.left, target);
  return search(node.right, target);
}

function insert(node, value) {
  if (node === null) return new Node(value);
  if (value < node.value) {
    node.left = insert(node.left, value);
  } else if (value > node.value) {
    node.right = insert(node.right, value);
  }
  return node;
}

function inorder(node, result = []) {
  if (node === null) return result;
  inorder(node.left, result);
  result.push(node.value);
  inorder(node.right, result);
  return result;
}

// Построим дерево:
//      5
//     / \\
//    3   8
//   / \\ / \\
//  1  4 7  9

let node1 = new Node(1);
let node4 = new Node(4);
let node3 = new Node(3, node1, node4);
let node7 = new Node(7);
let node9 = new Node(9);
let node8 = new Node(8, node7, node9);
let root = new Node(5, node3, node8);

console.log("Поиск 4:", search(root, 4));
console.log("Поиск 6:", search(root, 6));

// Вставим 6
root = insert(root, 6);
console.log("После вставки 6:");
console.log("Обход inorder:", inorder(root).join(", "));`}
  expected={`Поиск 4: true
Поиск 6: false
После вставки 6:
Обход inorder: 1, 3, 4, 5, 6, 7, 8, 9`}
/>

</Code>

<Trace lang="ru">
<AlgoTrace
  id="bst-search-trace"
  lang="ru"
  code={[
    "let root = new Node(5, new Node(3, new Node(1), new Node(4)), new Node(8, new Node(7), new Node(9)));",
    "search(root, 4);"
  ]}
  frames={[
    { caption: "Вызовите search(root=5, target=4). Сравните 4 с 5. 4 < 5, идите влево.", line: 2, cells: [{ value: "search(5)", mark: "waiting" }] },
    { caption: "Вызовите search(node=3, target=4). Сравните 4 с 3. 4 > 3, идите вправо.", line: 2, cells: [{ value: "search(5)", mark: "waiting" }, { value: "search(3)", mark: "waiting" }] },
    { caption: "Вызовите search(node=4, target=4). Сравните 4 с 4. Нашли! Вернулась true.", line: 2, cells: [{ value: "search(5)", mark: "waiting" }, { value: "search(3)", mark: "waiting" }, { value: "search(4) → true", mark: "done" }] },
    { caption: "Вернулась true до search(3). Она возвращает true к search(5).", line: 2, cells: [{ value: "search(5) → true", mark: "done" }, { value: "search(3) → true", mark: "done" }, { value: "search(4) → true", mark: "done" }] }
  ]}
/>

<AlgoTrace
  id="bst-insert-trace"
  lang="ru"
  code={[
    "let root = new Node(5, new Node(3, new Node(1), new Node(4)), new Node(8, new Node(7), new Node(9)));",
    "insert(root, 6);"
  ]}
  frames={[
    { caption: "Вызовите insert(root=5, value=6). Сравните 6 с 5. 6 > 5, идите вправо.", line: 2, cells: [{ value: "insert(5)", mark: "waiting" }] },
    { caption: "Вызовите insert(node=8, value=6). Сравните 6 с 8. 6 < 8, идите влево.", line: 2, cells: [{ value: "insert(5)", mark: "waiting" }, { value: "insert(8)", mark: "waiting" }] },
    { caption: "Вызовите insert(node=7, value=6). Сравните 6 с 7. 6 < 7, идите влево.", line: 2, cells: [{ value: "insert(5)", mark: "waiting" }, { value: "insert(8)", mark: "waiting" }, { value: "insert(7)", mark: "waiting" }] },
    { caption: "Вызовите insert(node=null, value=6). Базовый случай: создайте новый node(6). Вернулась new Node(6).", line: 2, cells: [{ value: "insert(5)", mark: "waiting" }, { value: "insert(8)", mark: "waiting" }, { value: "insert(7)", mark: "waiting" }, { value: "insert(null) → Node(6)", mark: "done" }] },
    { caption: "Вернулась к insert(7): прикрепи новый узел как левого ребёнка 7. Вернулась обновленная 7.", line: 2, cells: [{ value: "insert(5)", mark: "waiting" }, { value: "insert(8)", mark: "waiting" }, { value: "insert(7) → updated", mark: "done" }, { value: "insert(null) → Node(6)", mark: "done" }] },
    { caption: "Вернулась к insert(8): прикрепи обновленное левое поддерево. Вернулась обновленная 8.", line: 2, cells: [{ value: "insert(5)", mark: "waiting" }, { value: "insert(8) → updated", mark: "done" }, { value: "insert(7) → updated", mark: "done" }, { value: "insert(null) → Node(6)", mark: "done" }] },
    { caption: "Вернулась к insert(5): прикрепи обновленное правое поддерево. Вернулась корень.", line: 2, cells: [{ value: "insert(5) → done", mark: "done" }, { value: "insert(8) → updated", mark: "done" }, { value: "insert(7) → updated", mark: "done" }, { value: "insert(null) → Node(6)", mark: "done" }] }
  ]}
/>
</Trace>

<Complexity lang="ru">
**Временная сложность: поиск и вставка**

Обе операции поиска и вставки проходят путь от корня к либо найденному узлу (поиск), либо к листу (вставка). Число сравнений равно глубине этого пути, что максимум — высота h дерева.

- **Лучший случай (сбалансированное дерево)**: Высота O(log n), поэтому поиск и вставка оба O(log n).
- **Средний случай (случайные вставки)**: Высота примерно O(log n), поэтому оба O(log n).
- **Худший случай (вырожденное дерево)**: Высота O(n) (дерево — связный список), поэтому оба O(n).

Для операций в коде этого урока (поиск и вставка), временная сложность — **O(h)**, где h — высота.

**Пространственная сложность**

И поиск, и вставка используют рекурсию, поэтому глубина стека вызовов — O(h). Обход inorder также использует O(h) рекурсивную глубину.

</Complexity>

<AlgoPractice
  id="practice-bst-1"
  lessonSlug="05-binary-search-trees"
  lang="ru"
  problems={[
    {
      kind: "choice",
      prompt: "Какое из этих деревьев удовлетворяет свойству BST?",
      choices: [
        "Дерево с корнем 10, левым ребёнком 5, правым ребёнком 15, и правый ребёнок 5 — это 12.",
        "Дерево с корнем 10, левым ребёнком 15, и правым ребёнком 5.",
        "Дерево с корнем 10, левым ребёнком 8, правым ребёнком 12, и правый ребёнок 8 — это 9.",
        "Дерево с корнем 10, левым ребёнком 8, правым ребёнком 12, и правый ребёнок 12 — это 11."
      ],
      answer: 2,
      hint: "Свойство BST требует, что ВСЕ значения в левом поддереве меньше узла, и ВСЕ значения в правом поддереве больше. В вариант 0 (корень 10, левый 5, правый 15, левый-правый-ребёнок 12): 12 в левом поддереве, но 12 > 10, что нарушает свойство BST для корня. Вариант 1 имеет 15 слева (15 > 10), нарушая свойство. Вариант 3 имеет 11 как правого ребёнка 12, но 11 < 12, так что он должен быть слева. Вариант 2 (корень 10, левый 8, правый 12, правый ребёнок 8 — это 9) удовлетворяет BST: 8 < 10 < 12, и 8 < 9 < 10."
    },
    {
      kind: "choice",
      prompt: "Вы ищите значение 6 в этом BST. На каждом шаге, сколько узлов вы сравниваете?",
      choices: [
        "1 (вы находите его немедленно)",
        "Число узлов на пути от корня к узлу, содержащему 6",
        "Все узлы в дереве",
        "log(n) узлов, всегда"
      ],
      answer: 1,
      hint: "Каждое сравнение говорит вам идти влево или вправо. Если вы в корне и цель меньше, вы идите влево без проверки правого поддерева. Вы сравниваете только узлы на пути от корня к цели (или к null, если цель не найдена)."
    },
    {
      kind: "choice",
      prompt: "Что производит обход в порядке inorder BST?",
      choices: [
        "Случайный порядок значений",
        "Значения в восходящем порядке (отсортированные)",
        "Значения в нисходящем порядке",
        "Значения сгруппированные по глубине"
      ],
      answer: 1,
      hint: "Обход inorder посещает левое поддерево, потом текущий узел, потом правое поддерево. Так как значения левого поддерева меньше и значения правого поддерева больше, узлы посещаются в восходящем порядке."
    },
    {
      kind: "choice",
      prompt: "BST построен вставкой значений в этом порядке: [1, 2, 3, 4, 5]. Какова высота получившегося дерева?",
      choices: [
        "0 (это один узел)",
        "2",
        "4 (дерево — это связный список: высота = 5 - 1)",
        "log(5), примерно 2.3"
      ],
      answer: 2,
      hint: "Когда значения вставляются в отсортированном порядке, каждое новое значение больше предыдущего корня, поэтому оно идёт вправо. Узел 1 — это корень, 2 — правый ребёнок 1, 3 — правый ребёнок 2, и т.д. Дерево — это цепь: 1 → 2 → 3 → 4 → 5. Цепь из 5 узлов имеет высоту 4 (4 ребра)."
    },
    {
      kind: "choice",
      prompt: "В худшем случае (вырожденное BST), какова временная сложность поиска значения?",
      choices: [
        "O(1)",
        "O(log n)",
        "O(n)",
        "O(n²)"
      ],
      answer: 2,
      hint: "В вырожденном дереве каждый узел имеет только одного ребёнка (как связный список). Чтобы найти значение, вы можете должны проверить каждый узел, так что худший случай — O(n)."
    }
  ]}
/>

<Inset type="why">
**Зачем BST?** Неупорядоченные двоичные деревья позволяют вам ходить, но не искать эффективно. Если вы хотите искать без проверки каждого узла, вам нужна структура: упорядочение. Свойство BST даёт вам эту структуру. На каждом узле вы знаете, какое поддерево исследовать, поэтому вы сокращаете проблему (в сбалансированном дереве) на каждом шаге.
</Inset>

<Inset type="edgecase">
**Вырожденные BST.** Если вы вставляете значения в отсортированном порядке, BST вырождается в связный список. Высота становится O(n), и вы теряете логарифмическое преимущество. Это почему существуют самобалансирующиеся деревья (AVL, красно-чёрные) — они сохраняют баланс. Но для этого урока, просто признайте, что высота имеет значение.
</Inset>

<Inset type="mistake">
**Путаница BST с гарантией отсортированного порядка.** BST НЕ гарантирует, что стандартный обход дерева (как level-order) производит отсортированный порядок. Только обход inorder производит отсортированный порядок. Если вы хотите отсортировать последовательность, вы строите BST и обходите его inorder. Но BST — это не в первую очередь алгоритм сортировки — это динамическая структура данных для быстрого поиска.
</Inset>

<Check lang="ru">
  <Quiz
    id="quiz-bst-1"
    pieceSlug="05-binary-search-trees"
    lang="ru"
    question="Что такое свойство BST и почему оно позволяет быстрый поиск?"
    choices={[
      {
        label: "Для каждого узла все значения в левом поддереве меньше узла, и все значения в правом поддереве больше. На каждом шаге сравнение говорит вам, какое поддерево содержит цель (если она существует), поэтому вы никогда не исследуете ненужные части дерева. Это сокращает поиск до O(h), где h — высота дерева.",
        correct: true
      },
      {
        label: "Свойство BST гарантирует, что дерево сбалансировано и поиск всегда O(log n).",
        misconception: "Свойство BST одно не гарантирует баланс. Вырожденное BST может иметь высоту O(n). Только самобалансирующиеся BST-ы (AVL, красно-чёрные) гарантируют O(log n) операции."
      },
      {
        label: "Свойство BST в том, что дерево отсортировано в level-order (поиск в ширину).",
        misconception: "Свойство BST — это об отношении узел-ребёнок, не об порядке обхода. Обход inorder (не level-order) BST даёт отсортированный порядок."
      },
      {
        label: "Свойство BST означает, что каждое значение появляется только один раз в дереве.",
        misconception: "Свойство BST — это об упорядочении значений, не об уникальности. BST может позволить дубликаты (сохранённые в одном из поддеревьев) или запретить их, в зависимости от реализации."
      }
    ]}
  />
</Check>

<Recap lang="ru">
**Двоичные деревья поиска** организуют данные со свойством порядка: левое поддерево < узел < правое поддерево. Это свойство позволяет **быстрый поиск** и **вставку** за **O(h)** времени, где h — высота дерева. Для сбалансированного дерева h — это O(log n), делая операции логарифмическими. **Обход в порядке inorder** BST даёт значения в отсортированном порядке — следствие свойства порядка. Критическая оговорка: если дерево несбалансировано (вырожено), высота становится O(n), и все операции деградируют. **Самобалансирующиеся деревья** (AVL, красно-чёрные) сохраняют баланс автоматически, но они значительно более сложны и выходят за рамки этого урока. Следующий урок охватывает **древовидное динамическое программирование** — техники решения рекурсивных задач на деревьях с мемоизацией.
</Recap>
