---
concepts:
- traversal
- pre-order
- in-order
- post-order
- depth-first
- recursive_function
- visit
estMin: 20
lang: ru
mathPrereqs: []
order: 2
prereqs:
- 01-the-binary-tree
slug: 02-traversals
sources:
- https://en.wikipedia.org/wiki/Tree_traversal
- https://www.enjoyalgorithms.com/blog/binary-tree-traversals-preorder-inorder-postorder/
- https://opendsa-server.cs.vt.edu/ODSA/Books/Everything/html/BinaryTreeTraversal.html
- https://www.geeksforgeeks.org/dsa/tree-traversals-inorder-preorder-and-postorder/
status: ready
summary: 'Три способа систематически посетить узлы дерева: прямой (узел, левое, правое),
  симметричный (левое, узел, правое) и обратный (левое, правое, узел). Каждый как
  рекурсивная функция; каждый решает разные задачи в зависимости от потребности в
  информации от предков или потомков.'
title: Обходы дерева
track: algorithms
unit: 07-trees
---



import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Idea from "~/components/lesson/Idea.astro";
import Code from "~/components/lesson/Code.astro";
import Trace from "~/components/lesson/Trace.astro";
import Complexity from "~/components/lesson/Complexity.astro";
import Check from "~/components/lesson/Check.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import AnnotatedCode from "~/components/algo/AnnotatedCode.astro";
import AlgoTrace from "~/components/algo/AlgoTrace.astro";
import CodeRunner from "~/components/algo/CodeRunner.astro";
import AlgoPractice from "~/components/algo/AlgoPractice.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Вы знаете, как ходить влево и вправо по дереву, следуя указателям. Но в каком порядке вы посещаете узлы? Смотрите на узел в первую очередь, или исследуете его детей сначала? Разные порядки отвечают на разные вопросы. Посетите узел раньше его детей — и вы сможете построить копию сверху вниз. Посетите узел после его левого ребёнка в двоичном дереве поиска — и вы получите отсортированный порядок. Посетите его после обоих детей — и вы сможете вычислить результаты поддерева снизу вверх. Этот урок учит три канонических порядка и почему вы выбрали бы каждый.
</Hook>

<Goal lang="ru">
После этого урока вы сможете написать три обхода в глубину (прямой, симметричный и обратный порядки) как рекурсивные функции, проследить каждый на конкретном дереве, чтобы предсказать порядок посещения, объяснить, когда и почему используется каждый, и распознать, когда задача требует определённого порядка обхода.
</Goal>

<Idea lang="ru">
**Что такое обход?**

**Обход** — это систематическое посещение каждого узла в дереве. Порядок, в котором вы посещаете узлы, имеет значение, потому что он определяет, какая информация доступна при обработке каждого узла.

Существует много порядков обхода, но три наиболее распространённых — это **обходы в глубину**, которые посещают корень раньше, чем исследуют всех его потомков. Они различаются **когда** корень посещается относительно его детей:

- **Прямой порядок**: посетить корень, потом левое поддерево, потом правое. (Узел, Левое, Правое)
- **Симметричный порядок**: посетить левое поддерево, потом корень, потом правое. (Левое, Узел, Правое)
- **Обратный порядок**: посетить левое поддерево, потом правое, потом корень. (Левое, Правое, Узел)

Все три — **рекурсивные**: чтобы обойти дерево, обработайте корень согласно порядку, потом рекурсивно обойдите левое и правое поддеревья.

**Почему три разных порядка?**

Каждый порядок — это инструмент для разных задач. Прямой порядок обрабатывает родителя раньше его детей (полезно для копирования дерева или отметки расстояния от корня). Симметричный порядок в двоичном дереве поиска даёт отсортированный порядок — значение узла выводится между всеми меньшими значениями (в левом поддереве) и всеми большими значениями (в правом поддереве). Обратный порядок обрабатывает родителя после обоих детей (полезно для удаления дерева или вычисления высот поддеревьев). Выбор зависит от того, нужна ли вам информация от предков, отсортированный вывод или результаты от потомков.

</Idea>

<Code lang="ru">
**Прямой порядок: обработка корня в первую очередь**

В прямом порядке посетите текущий узел, потом рекурсивно обойдите его левое поддерево, потом его правое поддерево.

<AnnotatedCode
  code={`function preOrder(node) {
  if (node === null) return;
  
  // Посетить (обработать) узел
  console.log(node.value);
  
  // Обойти левое поддерево
  preOrder(node.left);
  
  // Обойти правое поддерево
  preOrder(node.right);
}`}
  annotations={[
    { line: 1, text: "Рекурсивная функция: принимает узел (или null для пустого поддерева)." },
    { line: 2, text: "Базовый случай: если узел null, нечего посещать." },
    { line: 5, text: "Посетить текущий узел (здесь вывести его значение)." },
    { line: 8, text: "Рекурсивно обойти левое поддерево." },
    { line: 11, text: "Рекурсивно обойти правое поддерево." }
  ]}
/>

Пример: Для дерева:
```
    1
   / \
  2   3
 /
4
```

Прямой порядок посещает в этом порядке: **1 → 2 → 4 → 3**.

**Симметричный порядок: посещение узла между его детьми**

<AnnotatedCode
  code={`function inOrder(node) {
  if (node === null) return;
  
  // Обойти левое поддерево
  inOrder(node.left);
  
  // Посетить (обработать) узел
  console.log(node.value);
  
  // Обойти правое поддерево
  inOrder(node.right);
}`}
  annotations={[
    { line: 1, text: "Рекурсивная функция: принимает узел (или null для пустого поддерева)." },
    { line: 2, text: "Базовый случай: если узел null, нечего посещать." },
    { line: 5, text: "Рекурсивно обойти левое поддерево в первую очередь." },
    { line: 8, text: "Посетить текущий узел после его левого поддерева." },
    { line: 11, text: "Рекурсивно обойти правое поддерево." }
  ]}
/>

Пример: Для того же дерева:
```
    1
   / \
  2   3
 /
4
```

Симметричный порядок посещает в этом порядке: **4 → 2 → 1 → 3**.

**Обратный порядок: посещение узла после обоих детей**

<AnnotatedCode
  code={`function postOrder(node) {
  if (node === null) return;
  
  // Обойти левое поддерево
  postOrder(node.left);
  
  // Обойти правое поддерево
  postOrder(node.right);
  
  // Посетить (обработать) узел
  console.log(node.value);
}`}
  annotations={[
    { line: 1, text: "Рекурсивная функция: принимает узел (или null для пустого поддерева)." },
    { line: 2, text: "Базовый случай: если узел null, нечего посещать." },
    { line: 5, text: "Рекурсивно обойти левое поддерево." },
    { line: 8, text: "Рекурсивно обойти правое поддерево." },
    { line: 11, text: "Посетить текущий узел после обоих поддеревьев." }
  ]}
/>

Пример: Для того же дерева:
```
    1
   / \
  2   3
 /
4
```

Обратный порядок посещает в этом порядке: **4 → 2 → 3 → 1**.

**Запуск всех трёх на конкретном дереве**

<CodeRunner
  code={`class Node {
  constructor(value, left = null, right = null) {
    this.value = value;
    this.left = left;
    this.right = right;
  }
}

function preOrder(node) {
  if (node === null) return;
  console.log(node.value);
  preOrder(node.left);
  preOrder(node.right);
}

function inOrder(node) {
  if (node === null) return;
  inOrder(node.left);
  console.log(node.value);
  inOrder(node.right);
}

function postOrder(node) {
  if (node === null) return;
  postOrder(node.left);
  postOrder(node.right);
  console.log(node.value);
}

// Построим дерево:
//      1
//     / \
//    2   3
//   /
//  4

let node4 = new Node(4);
let node2 = new Node(2, node4);
let node3 = new Node(3);
let root = new Node(1, node2, node3);

console.log("Прямой порядок:");
preOrder(root);

console.log("\nСимметричный порядок:");
inOrder(root);

console.log("\nОбратный порядок:");
postOrder(root);`}
  expected={`Прямой порядок:
1
2
4
3

Симметричный порядок:
4
2
1
3

Обратный порядок:
4
2
3
1`}
/>

</Code>

<Trace lang="ru">
<AlgoTrace
  id="preorder-trace"
  lang="ru"
  code={[
    "let root = new Node(1, new Node(2, new Node(4)), new Node(3));",
    "preOrder(root);"
  ]}
  frames={[
    { caption: "Начало на корне (узел 1). Посетите его в первую очередь (прямой порядок).", line: 2, cells: [{ value: "1", mark: "active" }] },
    { caption: "Идём влево к узлу 2. Посетите его.", line: 2, cells: [{ value: "1", mark: "done" }, { value: "2", mark: "active" }] },
    { caption: "Идём влево к узлу 4. Посетите его.", line: 2, cells: [{ value: "1", mark: "done" }, { value: "2", mark: "done" }, { value: "4", mark: "active" }] },
    { caption: "Узел 4 не имеет левого ребёнка. Идём вправо (null), возвращаемся к узлу 2.", line: 2, cells: [{ value: "1", mark: "done" }, { value: "2", mark: "done" }, { value: "4", mark: "done" }] },
    { caption: "Узел 2 не имеет правого ребёнка. Возвращаемся к узлу 1.", line: 2, cells: [{ value: "1", mark: "done" }, { value: "2", mark: "done" }, { value: "4", mark: "done" }] },
    { caption: "Идём вправо к узлу 3. Посетите его.", line: 2, cells: [{ value: "1", mark: "done" }, { value: "2", mark: "done" }, { value: "4", mark: "done" }, { value: "3", mark: "active" }] },
    { caption: "Узел 3 не имеет детей. Готово. Порядок посещения: 1, 2, 4, 3.", line: 2, cells: [{ value: "1", mark: "done" }, { value: "2", mark: "done" }, { value: "4", mark: "done" }, { value: "3", mark: "done" }] }
  ]}
/>

<AlgoTrace
  id="inorder-trace"
  lang="ru"
  code={[
    "let root = new Node(1, new Node(2, new Node(4)), new Node(3));",
    "inOrder(root);"
  ]}
  frames={[
    { caption: "Начало на корне (узел 1). Идём влево в первую очередь (симметричный порядок).", line: 2, cells: [{ value: "1", mark: "active" }] },
    { caption: "На узле 2. Идём влево в первую очередь.", line: 2, cells: [{ value: "1", mark: "active" }, { value: "2", mark: "active" }] },
    { caption: "На узле 4. Идём влево (null), потом посетите узел 4.", line: 2, cells: [{ value: "1", mark: "active" }, { value: "2", mark: "active" }, { value: "4", mark: "active" }] },
    { caption: "Посетили 4. Идём вправо от узла 4 (null). Возвращаемся к узлу 2, посетите его.", line: 2, cells: [{ value: "1", mark: "active" }, { value: "2", mark: "active" }, { value: "4", mark: "done" }] },
    { caption: "Посетили 2. Идём вправо от узла 2 (null). Возвращаемся к узлу 1, посетите его.", line: 2, cells: [{ value: "1", mark: "active" }, { value: "2", mark: "done" }, { value: "4", mark: "done" }] },
    { caption: "Посетили 1. Идём вправо к узлу 3.", line: 2, cells: [{ value: "1", mark: "done" }, { value: "2", mark: "done" }, { value: "4", mark: "done" }, { value: "3", mark: "active" }] },
    { caption: "На узле 3. Идём влево (null), посетите узел 3. Идём вправо (null). Готово. Порядок посещения: 4, 2, 1, 3.", line: 2, cells: [{ value: "1", mark: "done" }, { value: "2", mark: "done" }, { value: "4", mark: "done" }, { value: "3", mark: "done" }] }
  ]}
/>

<AlgoTrace
  id="postorder-trace"
  lang="ru"
  code={[
    "let root = new Node(1, new Node(2, new Node(4)), new Node(3));",
    "postOrder(root);"
  ]}
  frames={[
    { caption: "Начало на корне (узел 1). Идём влево в первую очередь, не посещайте пока (обратный порядок).", line: 2, cells: [{ value: "1", mark: "active" }] },
    { caption: "На узле 2. Идём влево в первую очередь.", line: 2, cells: [{ value: "1", mark: "active" }, { value: "2", mark: "active" }] },
    { caption: "На узле 4. Идём влево (null).", line: 2, cells: [{ value: "1", mark: "active" }, { value: "2", mark: "active" }, { value: "4", mark: "active" }] },
    { caption: "Идём вправо от узла 4 (null). Теперь посетите узел 4 (обратный порядок: после детей).", line: 2, cells: [{ value: "1", mark: "active" }, { value: "2", mark: "active" }, { value: "4", mark: "active" }] },
    { caption: "Посетили 4. Возвращаемся к узлу 2. Идём вправо (null). Теперь посетите узел 2.", line: 2, cells: [{ value: "1", mark: "active" }, { value: "2", mark: "active" }, { value: "4", mark: "done" }] },
    { caption: "Посетили 2. Возвращаемся к узлу 1. Идём вправо к узлу 3.", line: 2, cells: [{ value: "1", mark: "active" }, { value: "2", mark: "done" }, { value: "4", mark: "done" }, { value: "3", mark: "active" }] },
    { caption: "На узле 3. Идём влево (null), вправо (null). Посетите узел 3. Возвращаемся к узлу 1, посетите его. Готово. Порядок посещения: 4, 2, 3, 1.", line: 2, cells: [{ value: "1", mark: "active" }, { value: "2", mark: "done" }, { value: "4", mark: "done" }, { value: "3", mark: "done" }] }
  ]}
/>
</Trace>

<Complexity lang="ru">
**Сложность по времени**

Каждый из трёх обходов посещает каждый узел в дереве ровно один раз. Если дерево имеет n узлов, мы делаем n посещений плюс рекурсивные вызовы для достижения этих узлов. Каждый узел обрабатывается один раз, поэтому время **O(n)** для всех трёх обходов.

**Сложность по памяти**

Используемая память — это глубина стека вызовов. Когда рекурсия идёт вниз по дереву, каждый рекурсивный вызов остаётся в стеке вызовов до его возврата. Максимальная глубина стека вызовов равна высоте дерева, h.

- Лучший случай (сбалансированное дерево): **O(log n)** памяти для высоты log n.
- Худший случай (цепь): **O(n)** памяти для высоты n (если каждый узел имеет только одного ребёнка).

В типичных сбалансированных деревьях **O(log n)** памяти. В искривлённых деревьях **O(n)** памяти.

</Complexity>

<AlgoPractice
  id="practice-traversals-1"
  lessonSlug="02-traversals"
  lang="ru"
  problems={[
    {
      kind: "choice",
      prompt: "Для дерева:    2\n   / \\\n  1   3\n\nКакой из этих это прямой порядок?",
      choices: [
        "1, 2, 3",
        "2, 1, 3",
        "1, 3, 2",
        "3, 2, 1"
      ],
      answer: 1,
      hint: "Прямой порядок посещает узел в первую очередь, потом левое, потом правое. Начните с узла 2 (корень)."
    },
    {
      kind: "choice",
      prompt: "Для того же дерева, какой из этих это симметричный порядок?",
      choices: [
        "2, 1, 3",
        "1, 2, 3",
        "3, 1, 2",
        "1, 3, 2"
      ],
      answer: 1,
      hint: "Симметричный порядок посещает левое в первую очередь, потом узел, потом правое. Идите влево к узлу 1."
    },
    {
      kind: "choice",
      prompt: "Для того же дерева, какой из этих это обратный порядок?",
      choices: [
        "2, 1, 3",
        "1, 2, 3",
        "1, 3, 2",
        "3, 1, 2"
      ],
      answer: 2,
      hint: "Обратный порядок посещает обоих детей раньше родителя. Обработайте левое (1), правое (3), потом узел (2)."
    },
    {
      kind: "choice",
      prompt: "Когда вы обходите двоичное дерево поиска симметричным порядком, какое свойство имеет вывод?",
      choices: [
        "Вывод имеет случайный порядок.",
        "Вывод находится в восходящем (отсортированном) порядке.",
        "Вывод находится в обратном алфавитном порядке.",
        "Вывод сгруппирован по уровню глубины."
      ],
      answer: 1,
      hint: "В дереве поиска все левые значения меньше и все правые значения больше. Симметричный порядок (левое, узел, правое) уважает это."
    },
    {
      kind: "choice",
      prompt: "Прямой порядок наиболее полезен для какой задачи?",
      choices: [
        "Получение значений в отсортированном порядке из дерева поиска.",
        "Копирование дерева или обработка предков раньше потомков.",
        "Удаление дерева от листьев вверх.",
        "Нахождение высоты каждого узла."
      ],
      answer: 1,
      hint: "Прямой порядок посещает родителей раньше детей, поэтому вы можете обработать информацию родителя перед тем, как нужны его дети."
    }
  ]}
/>

<Inset type="mistake">
**Не путайте названия порядков.** Pre- означает «раньше» (посетить узел раньше детей). Post- означает «после» (посетить после детей). In- означает «между» (посетить между левым и правым детьми). Мнемоника: Pre (PREсентировать корень), In (In между), Post (POST-датировано, прибывает последним).
</Inset>

<Inset type="practice">
**Проследите сами.** Нарисуйте двоичное дерево с 5 узлами. Для каждого обхода (прямой, симметричный, обратный) проследите порядок посещения вручную. Запишите последовательность. Это самый быстрый способ закрепить различия в порядках.
</Inset>

<Inset type="edgecase">
**Пустые деревья и одиночные узлы.** Если дерево пусто (null), обход ничего не делает. Если дерево состоит из одного узла без детей, все три обхода посещают только этот узел. Граничные случаи делают хорошие тестовые входы.
</Inset>

<Check lang="ru">
  <Quiz
    id="quiz-traversals-1"
    pieceSlug="02-traversals"
    lang="ru"
    question="Почему нам нужны три разных порядка обхода вместо одного?"
    choices={[
      {
        label: "Потому что разные задачи требуют посещения узлов в разных порядках относительно их детей. Прямой порядок обрабатывает родителей раньше детей (копирование, сверху вниз). Симметричный порядок в дереве поиска даёт отсортированный вывод. Обратный порядок обрабатывает родителей после детей (удаление, результаты снизу вверх). Каждый порядок — это инструмент для разных задач.",
        correct: true
      },
      {
        label: "Потому что один обход быстрее других.",
        misconception: "Все три имеют одинаковую временную сложность O(n). Разница в порядке посещений, не в скорости."
      },
      {
        label: "Потому что некоторые деревья имеют прямой порядок, некоторые имеют симметричный порядок, некоторые имеют обратный.",
        misconception: "Дерево не является неотъемлемо одного типа или другого. Любое дерево может быть обойдено в любом из трёх порядков. Порядок — это выбор на основе задачи."
      },
      {
        label: "Потому что обратный порядок всегда лучший, но прямой и симметричный порядки хранятся для обратной совместимости.",
        misconception: "Нет одного обхода, который был бы «лучшим». Каждый разработан для определённых случаев использования. Выбор правильного — это часть дизайна алгоритма."
      }
    ]}
  />
</Check>

<Recap lang="ru">
**Обход** — это систематическое посещение каждого узла в дереве. Три обхода в глубину различаются **когда** они посещают узел относительно его детей. **Прямой порядок** посещает узел в первую очередь, потом левое, потом правое (полезно для копирования, обработки сверху вниз). **Симметричный порядок** посещает левое, узел, правое (даёт отсортированный порядок в дереве поиска). **Обратный порядок** посещает левое, правое, узел (полезно для удаления, вычисления результатов поддерева снизу вверх). Все три рекурсивны: обработайте корень согласно порядку, потом рекурсивно обойдите поддеревья. Все три работают в **O(n) времени** (посетите каждый узел один раз) и **O(h) памяти** (глубина рекурсии равна высоте дерева). Следующий урок охватывает обход уровень за уровнем (поиск в ширину), который использует очередь вместо рекурсии.
</Recap>
