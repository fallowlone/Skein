---
concepts:
- tree
- binary_tree
- node
- root
- leaf
- parent
- child
- sibling
- depth
- height
- subtree
- level
estMin: 22
lang: ru
mathPrereqs: []
order: 1
prereqs:
- 01-what-is-recursion
slug: 01-the-binary-tree
sources:
- https://en.wikipedia.org/wiki/Binary_tree
- https://www.geeksforgeeks.org/binary-tree-data-structure/
- https://www.programiz.com/dsa/binary-tree
- https://www.happycoders.eu/data-structures/binary-tree/
- https://www.freecodecamp.org/news/binary-trees-explained/
status: ready
summary: 'Дерево - иерархия узлов: один корень вверху, каждый указывает на дочерние
  узлы ниже. Двоичное дерево ограничивает каждый узел максимум 2 детьми (слева и справа).
  Деревья подходят для рекурсии - обработайте узел, затем рекурсивно его левое и правое
  поддеревья.'
title: Двоичное дерево
track: algorithms
unit: 07-trees
---



import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Idea from "~/components/lesson/Idea.astro";
import Code from "~/components/lesson/Code.astro";
import Trace from "~/components/lesson/Trace.astro";
import Complexity from "~/components/lesson/Complexity.astro";
import Check from "~/components/lesson/Check.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import AnnotatedCode from "~/components/algo/AnnotatedCode.astro";
import AlgoTrace from "~/components/algo/AlgoTrace.astro";
import CodeRunner from "~/components/algo/CodeRunner.astro";
import AlgoPractice from "~/components/algo/AlgoPractice.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DiagramFrame from "~/components/diagram/DiagramFrame.astro";

<Hook>
Вы знаете, что связный список — это цепь узлов, каждый указывает на следующий. Теперь представьте структуру, где каждый узел указывает не на один следующий узел, а на два: левого и правого ребёнка. Это дерево. Вместо линейной цепи вы получаете ветвящуюся структуру — один корневой узел вверху, разветвляющийся вниз в листья. Деревья позволяют организовать иерархии (как файловая система), и они так естественно подходят рекурсии, что обработка дерева кажется дыханием.
</Hook>

<Goal>
После этого урока вы сможете объяснить, что такое дерево, отличить двоичное дерево от других деревьев, использовать терминологию (корень, лист, родитель, ребёнок, глубина, высота, поддерево, уровень), реализовать узел с левым и правым указателями, навигировать по двоичному дереву, следуя указателям на детей, и распознать, когда дерево — правильная структура данных.
</Goal>

<Idea>
**Что такое дерево?**

**Дерево** — это иерархия **узлов**, соединённых **рёбрами**. Один узел особенный: **корень**, вверху. Каждый другой узел имеет ровно одного **родителя** (узел, который на него указывает). Узел может иметь ноль, одного или несколько **детей** (узлы, на которые он указывает).

Визуально:
```
      1 (корень)
     / \
    2   3
   / \
  4   5
```

Здесь узел 1 — корень. Узел 2 — родитель узлов 4 и 5. Узел 4 — дитя узла 2. Узлы 4 и 5 — **братья** (они имеют одного и того же родителя).

**Что такое двоичное дерево?**

**Двоичное дерево** — это дерево, где каждый узел имеет **максимум 2 детей**. Мы называем их **левым ребёнком** и **правым ребёнком**. Узел может иметь:
- Нет детей (лист)
- Одного ребёнка (только левого или только правого)
- Двух детей (и левого, и правого)

Двоичное дерево выше:
```
      1
     / \
    2   3
   / \
  4   5
```

Узел 1 имеет левого ребёнка (2) и правого ребёнка (3). Узел 2 имеет двух детей. Узел 3 не имеет никого. Узлы 4 и 5 — листья.

**Терминология дерева**

- **Корень**: Самый верхний узел (нет родителя).
- **Лист**: Узел без детей.
- **Внутренний узел**: Узел, имеющий хотя бы одного ребёнка.
- **Родитель / Дитя**: Если узел A указывает на узел B, A — родитель и B — дитя.
- **Брат**: Два узла с одним и тем же родителем.
- **Предок / Потомок**: Узел A — предок B, если A находится на пути от B вверх к корню. B — потомок A.
- **Уровень**: Все узлы на одном расстоянии от корня находятся на одном уровне. Корень находится на уровне 0.

**Глубина vs. Высота** (это сбивает с толку всех)

- **Глубина** узла = количество рёбер от корня вниз к этому узлу. Корень имеет глубину 0.
- **Высота** узла = количество рёбер от этого узла вниз к его самому дальнему листовому потомку. Лист имеет высоту 0.
- **Высота дерева** = высота корня.

Подсказка памяти: Глубина — это как глубоко вы спустились *вниз* от корня. Высота — это насколько высоко поддерево *поднимается* ниже вас.

Пример:
```
      1  (глубина=0, высота=2)
     / \
    2   3  (глубина=1, высота=1 для узла 2; высота=0 для узла 3)
   / \
  4   5  (глубина=2, высота=0 для обоих)
```

Узел 4 имеет глубину 2 (два ребра от корня: 1→2→4) и высоту 0 (нет детей). Узел 2 имеет глубину 1 и высоту 1 (одно ребро вниз к своему самому дальнему листу).

**Поддеревья и рекурсия**

**Поддерево** — это узел плюс все его потомки. Левое поддерево узла — это поддерево, укоренённое в его левом ребёнке. Правое поддерево укоренено в его правом ребёнке.

Двоичное дерево определяется *рекурсивно*:
- Пустое дерево — это двоичное дерево.
- Дерево с корнем и левым поддеревом (которое является двоичным деревом) и правым поддеревом (которое является двоичным деревом) — это двоичное дерево.

Это рекурсивное определение — ключевой момент: чтобы обработать любое дерево, вы обрабатываете корень, затем рекурсивно обрабатываете левое поддерево, затем правое поддерево. Эта структура — почему рекурсия и деревья созданы друг для друга.

</Idea>

<DiagramFrame label="Двоичное дерево из шести узлов. Корень со значением 1 находится вверху и имеет двух детей: внутренний узел 2 слева и лист 3 справа. Узел 2 имеет двух детей-листьев, 4 и 5. Линии, соединяющие родителя с ребёнком, — это рёбра; узлы без детей (3, 4, 5) — это листья." hue="var(--d-systems)" caption="Каноническое двоичное дерево: один корень вверху, каждый узел имеет максимум два ребёнка (левого и правого), рёбра соединяют родителя с ребёнком, а листья внизу не имеют собственных детей.">
  <svg viewBox="0 0 360 230" width="360" height="230" style="display:block;margin:0 auto;max-width:100%;height:auto;">
    <path d="M180 38 L104 110" fill="none" stroke="var(--hairline-strong)" stroke-width="1.4" />
    <path d="M180 38 L256 110" fill="none" stroke="var(--hairline-strong)" stroke-width="1.4" />
    <path d="M104 110 L64 182" fill="none" stroke="var(--hairline-strong)" stroke-width="1.4" />
    <path d="M104 110 L144 182" fill="none" stroke="var(--hairline-strong)" stroke-width="1.4" />
    <text x="156" y="68" text-anchor="end" font-size="9" font-family="var(--font-body)" fill="var(--muted)">ребро</text>
    <circle cx="180" cy="38" r="18" fill="var(--card-2)" stroke="var(--d-systems)" stroke-width="1.6" />
    <text x="180" y="42" text-anchor="middle" font-size="13" font-family="var(--font-body)" fill="var(--ink)">1</text>
    <text x="180" y="14" text-anchor="middle" font-size="9" font-family="var(--font-body)" fill="var(--muted)">корень</text>
    <circle cx="104" cy="110" r="18" fill="var(--card-2)" stroke="var(--hairline-strong)" stroke-width="1.2" />
    <text x="104" y="114" text-anchor="middle" font-size="13" font-family="var(--font-body)" fill="var(--ink)">2</text>
    <text x="76" y="100" text-anchor="end" font-size="9" font-family="var(--font-body)" fill="var(--muted)">внутренний узел</text>
    <circle cx="256" cy="110" r="18" fill="var(--card-2)" stroke="var(--hairline-strong)" stroke-width="1.2" />
    <text x="256" y="114" text-anchor="middle" font-size="13" font-family="var(--font-body)" fill="var(--ink)">3</text>
    <circle cx="64" cy="182" r="18" fill="var(--card-2)" stroke="var(--hairline-strong)" stroke-width="1.2" />
    <text x="64" y="186" text-anchor="middle" font-size="13" font-family="var(--font-body)" fill="var(--ink)">4</text>
    <circle cx="144" cy="182" r="18" fill="var(--card-2)" stroke="var(--hairline-strong)" stroke-width="1.2" />
    <text x="144" y="186" text-anchor="middle" font-size="13" font-family="var(--font-body)" fill="var(--ink)">5</text>
    <text x="104" y="220" text-anchor="middle" font-size="9" font-family="var(--font-body)" fill="var(--muted)">листья (без детей)</text>
  </svg>
</DiagramFrame>

<Code>
**Представление узла в коде**

В связном списке каждый узел хранил значение и указатель на следующий узел. В двоичном дереве каждый узел хранит значение и указатели на левого и правого ребёнка:

<AnnotatedCode
  code={`class Node {
  constructor(value, left = null, right = null) {
    this.value = value;
    this.left = left;
    this.right = right;
  }
}

// Построим дерево:
//      1
//     / \\
//    2   3
//   / \\
//  4   5

let node4 = new Node(4);
let node5 = new Node(5);
let node2 = new Node(2, node4, node5);
let node3 = new Node(3);
let root = new Node(1, node2, node3);`}
  annotations={[
    { line: 1, text: "Конструктор Node хранит value, left и right." },
    { line: 7, text: "Строим снизу вверх: листья в первую очередь." },
    { line: 13, text: "Корень — точка входа в целое дерево." }
  ]}
/>

**Навигирование по дереву, следуя указателям**

Чтобы прочитать дерево, вы следуете связям родитель→ребёнок. Каждый узел — это один шаг вниз:

<AnnotatedCode
  code={`// Доступ к узлам, следуя указателям left/right
let root = new Node(1, new Node(2, new Node(4), new Node(5)), new Node(3));

// Навигирование к левому ребёнку корня
let leftChild = root.left;   // Узел 2

// Навигирование к левому ребёнку узла 2
let leftGrandchild = root.left.left;  // Узел 4

// Навигирование к правому ребёнку узла 2
let rightGrandchild = root.left.right;  // Узел 5

// Каждая связь — это один прыжок указателя
console.log(leftChild.value);         // 2
console.log(leftGrandchild.value);    // 4`}
  annotations={[
    { line: 5, text: "root.left следует указателю left от корня." },
    { line: 8, text: "root.left.left — это прыжок через два указателя." },
    { line: 11, text: "root.left.right прыгает влево, затем вправо." },
    { line: 14, text: "Каждая навигация обращается к свойству value." }
  ]}
/>

**Простой помощник: проверка, является ли узел листом**

Лист не имеет детей (и left, и right равны null). Вот небольшая вспомогательная функция:

<AnnotatedCode
  code={`function isLeaf(node) {
  return node.left === null && node.right === null;
}

let root = new Node(1, new Node(2, new Node(4), new Node(5)), new Node(3));

console.log(isLeaf(root.left.left));  // true (узел 4 — лист)
console.log(isLeaf(root.left));       // false (узел 2 имеет детей)
console.log(isLeaf(root));            // false (корень имеет детей)`}
  annotations={[
    { line: 1, text: "Лист — это узел без детей." },
    { line: 2, text: "Проверьте, оба ли указателя равны null." },
    { line: 6, text: "Построим дерево." },
    { line: 8, text: "Узел 4 не имеет детей, поэтому isLeaf вернёт true." }
  ]}
/>

<CodeRunner
  code={`class Node {
  constructor(value, left = null, right = null) {
    this.value = value;
    this.left = left;
    this.right = right;
  }
}

function isLeaf(node) {
  return node.left === null && node.right === null;
}

let node4 = new Node(4);
let node5 = new Node(5);
let node2 = new Node(2, node4, node5);
let node3 = new Node(3);
let root = new Node(1, node2, node3);

console.log("Узел 4 — лист:", isLeaf(node4));
console.log("Узел 2 — лист:", isLeaf(node2));
console.log("Корень — лист:", isLeaf(root));`}
  expected={`Узел 4 — лист: true
Узел 2 — лист: false
Корень — лист: false`}
/>

</Code>

<Trace>
<AlgoTrace
  id="binary-tree-navigation"
  lang="ru"
  code={[
    "let root = new Node(1, new Node(2, new Node(4), new Node(5)), new Node(3));",
    "let target = root.left.right;  // Навигирование к узлу 5",
    "console.log(target.value);"
  ]}
  frames={[
    { caption: "Начало: root указывает на узел 1", line: 1, cells: ["1"] },
    { caption: "Следуем root.left: прыгаем к узлу 2", line: 2, cells: [{ value: "1", mark: "active" }, { value: "2", mark: "active" }] },
    { caption: "Следуем .right на узле 2: прыгаем к узлу 5", line: 2, cells: [{ value: "2", mark: "active" }, { value: "5", mark: "active" }] },
    { caption: "Обращение к узлу 5 через root.left.right", line: 2, cells: [{ value: "5", mark: "done" }] },
    { caption: "Прочитаем значение узла 5: 5", line: 3, cells: [{ value: "5", mark: "done" }] }
  ]}
/>
</Trace>

<Complexity>
**Сложность по времени**

Построение дерева с нуля (создание n узлов) занимает O(n) времени. Навигирование по дереву, следуя прыжкам указателей (например, `root.left.right`), занимает O(1) за один прыжок. Следование по пути длины d занимает O(d). Вспомогательная функция `isLeaf` — O(1).

**Время = O(1)** за один прыжок указателя; **O(n)** для построения всего дерева.

**Сложность по памяти**

Память используется для хранения структуры дерева: каждый из n узлов занимает O(1) пространства. Само дерево использует O(n) памяти.

**Память = O(n)** для самой структуры дерева. Отдельные операции типа `isLeaf` используют O(1) дополнительного пространства.

</Complexity>

<AlgoPractice
  id="practice-binarytree-1"
  lessonSlug="01-the-binary-tree"
  lang="ru"
  problems={[
    {
      kind: "choice",
      prompt: "Что такое глубина узла?",
      choices: [
        "Количество детей, которое оно имеет",
        "Количество рёбер от корня вниз к этому узлу",
        "Количество предков, которое оно имеет",
        "Расстояние от этого узла до ближайшего листа"
      ],
      answer: 1,
      hint: "Глубина измеряет расстояние *вверх* от узла к корню."
    },
    {
      kind: "choice",
      prompt: "Что такое высота узла?",
      choices: [
        "Количество рёбер от корня вниз к этому узлу",
        "Количество детей, которое оно имеет",
        "Количество рёбер от этого узла вниз к его самому дальнему листовому потомку",
        "Уровень, на котором он находится"
      ],
      answer: 2,
      hint: "Высота измеряет расстояние *вниз* от узла к самому дальнему листу ниже него."
    },
    {
      kind: "choice",
      prompt: "Какова высота листового узла?",
      choices: [
        "0",
        "1",
        "Глубина узла",
        "Высота его родителя минус 1"
      ],
      answer: 0,
      hint: "Лист не имеет детей, поэтому самый дальний потомок — это сам себя (0 рёбер в сторону)."
    },
    {
      kind: "choice",
      prompt: "Как в коде вы представляете узел двоичного дерева?",
      choices: [
        "Класс со свойствами value, next и prev",
        "Класс со свойствами value, left и right",
        "Массив [value, children...]",
        "Класс со свойствами value, parent и depth"
      ],
      answer: 1,
      hint: "Узел двоичного дерева указывает максимум на 2 детей, поэтому мы используем левый и правый указатели."
    },
    {
      kind: "choice",
      prompt: "Какова худшая высота двоичного дерева с 7 узлами?",
      choices: [
        "2",
        "3",
        "6",
        "7"
      ],
      answer: 2,
      hint: "Если все 7 узлов образуют цепь (один ребёнок на узел), высота составляет 6 рёбер (7 - 1)."
    }
  ]}
/>

<Inset type="mistake">
**Глубина ≠ Высота.** Глубина измеряет расстояние *от корня вниз к вам*. Высота измеряет расстояние *от вас вниз к самому дальнему листу*. Они указывают в противоположные направления.
</Inset>

<Inset type="edge-case">
**Пустое дерево.** Дерево может быть пустым (нет узлов). Высота пустого дерева иногда определяется как -1, иногда как 0, в зависимости от соглашения. Всегда проверяйте определение в вашей задаче.
</Inset>

<Inset type="practice">
**Нарисуйте дерево.** Нарисуйте двоичное дерево с 5 узлами. Обозначьте корень, листья и один внутренний узел. Затем вычислите глубину и высоту каждого узла. Это самый быстрый способ закрепить терминологию.
</Inset>

<Check lang="ru">
  <Quiz
    id="quiz-binarytree-1"
    pieceSlug="01-the-binary-tree"
    lang="ru"
    question="Почему дерево естественно подходит для рекурсивных алгоритмов?"
    choices={[
      {
        label: "Потому что дерево определяется рекурсивно: чтобы обработать дерево, вы обрабатываете корень, затем рекурсивно обрабатываете левое поддерево, затем правое поддерево. Рекурсивная структура данных совпадает с рекурсивной структурой алгоритма.",
        correct: true
      },
      {
        label: "Потому что деревья легче понять, чем циклы.",
        misconception: "Лёгкость понимания варьируется от человека к человеку. Настоящая причина — структурная: деревья иерархичны и самоподобны, как рекурсивное определение."
      },
      {
        label: "Потому что рекурсия всегда быстрее, чем итерация.",
        misconception: "Рекурсия не всегда быстрее. Она использует стек вызовов, что может быть медленнее и имеет ограничение глубины. Рекурсия и итерация — это инструменты с разными компромиссами."
      },
      {
        label: "Потому что деревья имеют листья, а рекурсия — это о достижении дна чего-либо.",
        misconception: "Рекурсия — не о достижении дна; это решение задачи путём разбиения на идентичные меньшие версии. Деревья просто естественно подходят под этот паттерн."
      }
    ]}
  />
</Check>

<Recap lang="ru">
**Дерево:** иерархия узлов с одним корнем вверху. **Двоичное дерево** ограничивает каждый узел максимум 2 детьми (слева и справа). Деревья используют терминологию: корень, лист, родитель, ребёнок, брат, предок, потомок, уровень. **Глубина** — это количество рёбер от корня вниз к узлу; **высота** — это количество рёбер от узла вниз к его самому дальнему листу. **Поддерево** — это узел плюс все его потомки. В коде представьте узел как `{ value, left, right }`, где left и right равны null, если у узла нет этого ребёнка. Навигируйте по дереву, следуя указателям left и right от корня. Лист — это любой узел, где и left, и right равны null. Двоичные деревья — это основание для куч, двоичных деревьев поиска и многих других алгоритмов. Далее вы научитесь **обходам**: как посетить все узлы в определённом порядке.
</Recap>
