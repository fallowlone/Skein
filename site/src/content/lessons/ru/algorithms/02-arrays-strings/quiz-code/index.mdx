---
concepts:
- two-pointers
- sliding-window
- in-place
- prefix-sums
estMin: 14
lang: ru
level: middle
order: 9
prereqs: []
slug: quiz-code
sources:
- https://labuladong.online/en/algo/essential-technique/array-two-pointers-summary/
- https://algomap.io/problems/minimum-size-subarray-sum
status: ready
summary: "Читай реальные сниппеты на two pointers, sliding window, prefix sum и in-place, затем предскажи вывод, назови сложность или найди баг, который поймает тест."
title: 'Массивы и строки: чтение кода'
track: algorithms
unit: 02-arrays-strings
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Приём легко проговорить и легко сломать. Читай каждый сниппет как на ревью: проследи указатели, проверь границу и реши, делает ли цикл то, что думает его автор.
</Hook>

<Goal lang="ru">
Потренируй чтение приёмов юнита в коде — предсказание вывода, точную сложность и поиск off-by-one или отсутствующего предусловия до того, как это уйдёт в прод.
</Goal>

## Сниппет 1 — вариативное окно

```js
function minSubarrayLen(arr, target) {
  let left = 0, sum = 0, best = Infinity;
  for (let right = 0; right < arr.length; right++) {
    sum += arr[right];
    while (sum >= target) {
      best = Math.min(best, right - left + 1);
      sum -= arr[left];
      left++;
    }
  }
  return best === Infinity ? 0 : best;
}
// minSubarrayLen([2, 3, 1, 2, 4, 3], 7)
```

<Quiz
  id="02-arrays-strings-code-1"
  lessonSlug="02-arrays-strings"
  lang="ru"
  question="Что вернёт этот вызов и какова временная сложность алгоритма?"
  choices={[
    { label: "Вернёт 2 — окно [4, 3] даёт сумму 7 при длине 2 — и алгоритм O(n): right сдвигается n раз, а left — не более n раз суммарно, поэтому внутренний while амортизирован, а не вложен.", correct: true },
    { label: "Вернёт 3 (окно [1, 2, 4]); алгоритм O(n²) из-за внутреннего while.", misconception: "[4, 3] = 7 даёт длину 2, что лучше [1, 2, 4]. И это O(n): left движется только вперёд за весь прогон, поэтому внутренний цикл амортизирован по n суммарным сдвигам — не O(n²)." },
    { label: "Вернёт 0, потому что ни один подмассив не достигает 7.", misconception: "Несколько подмассивов достигают 7 — например [4, 3]. Функция возвращает 0 только когда ни одно окно не подходит, а здесь это не так." },
    { label: "Вернёт 2, но сложность O(n log n) из-за повторного Math.min.", misconception: "Длина верна, но Math.min — O(1), и здесь нет ни сортировки, ни деления пополам. Оценка O(n) идёт из движения указателей только вперёд." },
  ]}
/>

## Сниппет 2 — уплотнение in-place

```js
function removeVal(arr, val) {
  let write = 0;
  for (let read = 0; read < arr.length; read++) {
    if (arr[read] !== val) {
      arr[write] = arr[read];
      write++;
    }
  }
  return write;
}
// const a = [3, 2, 2, 3]; const k = removeVal(a, 3);
```

<Quiz
  id="02-arrays-strings-code-2"
  lessonSlug="02-arrays-strings"
  lang="ru"
  question="После вызова — чему равно k и каково состояние первых k ячеек a?"
  choices={[
    { label: "k = 2, и a[0..1] = [2, 2]. Write-указатель сдвигается только на «нужном» элементе, поэтому два значения, не равные 3, уплотнены в начало; ячейки от индекса k и далее устарели и по контракту игнорируются.", correct: true },
    { label: "k = 2, и a = [2, 2, 2, 3] целиком — алгоритм очищает хвост.", misconception: "Техника write-указателя не очищает хвост; она гарантирует только, что a[0..k-1] — это сохранённые значения. В хвостовых ячейках остаётся то, что туда записали последним, и их нужно игнорировать." },
    { label: "k = 4, потому что цикл выполняется четыре раза.", misconception: "k — это write-указатель, а не счётчик итераций. Он сдвигается только когда arr[read] !== val, поэтому в конце равен 2 — числу сохранённых элементов." },
    { label: "k = 2, и a[0..1] = [3, 3] — он оставляет удаляемое значение.", misconception: "Условие сохраняет элементы, НЕ равные val, поэтому 3 отбрасываются, а 2 сохраняются. a[0..1] = [2, 2]." },
  ]}
/>

## Сниппет 3 — two-sum с концов

```js
function twoSum(arr, target) {
  let left = 0, right = arr.length - 1;
  while (left < right) {
    const sum = arr[left] + arr[right];
    if (sum === target) return [left, right];
    else if (sum < target) left++;
    else right--;
  }
  return null;
}
// twoSum([8, 2, 5, 1, 7], 9)
```

<Quiz
  id="02-arrays-strings-code-3"
  lessonSlug="02-arrays-strings"
  lang="ru"
  question="Массив [8, 2, 5, 1, 7] не отсортирован. Корректная пара с суммой 9 существует (8 + 1 или 2 + 7). Что этот код реально вернёт и почему?"
  choices={[
    { label: "Вернёт null. При left/right на 8 и 7 сумма 15 > 9, значит right-- к 1; затем 8 + 1 = 9 найдено — но на других неотсортированных входах правило движения отбрасывает жизнеспособные пары. Здесь он случайно попадает в [0, 3]; сниппет несостоятелен на неотсортированных данных.", misconception: "Проследи: 8 + 7 = 15 > 9 → right-- ; 8 + 1 = 9 → вернёт [0, 3]. Он возвращает пару по удаче. Реальный урок — корректность не гарантирована на неотсортированном входе." },
    { label: "На этом входе вернёт [0, 3] — 8 + 1 = 9 — но лишь по удаче; правило движения несостоятельно на неотсортированных данных, поэтому на других неотсортированных массивах он может вернуть null, даже когда пара есть. Сначала сортируй (или используй хеш-множество), чтобы сделать его корректным.", correct: true },
    { label: "Он бросит исключение, потому что left обгонит right до нахождения пары.", misconception: "Условие цикла left < right не даёт указателям пересечься; цикл выходит чисто и возвращает null, если пара не найдена. Исключения нет." },
    { label: "Вернёт [1, 4] (2 + 7 = 9), потому что two pointers всегда находит лексикографически первую пару.", misconception: "Two pointers не даёт такой гарантии порядка на неотсортированных данных. Проследив этот вход, он доходит до 8 + 1 = 9 и возвращает [0, 3] — а на других входах может пропустить пару вовсе." },
  ]}
/>

## Сниппет 4 — запрос суммы на диапазоне через prefix sum

```js
function buildPrefix(arr) {
  const p = new Array(arr.length + 1).fill(0);
  for (let i = 0; i < arr.length; i++) p[i + 1] = p[i] + arr[i];
  return p;
}
function rangeSum(p, i, j) { return p[j] - p[i - 1]; }
// const p = buildPrefix([2, 5, 1, 3, 4]);  // p = [0, 2, 7, 8, 11, 15]
// rangeSum(p, 1, 3)  // задумано: arr[1] + arr[2] + arr[3] = 9
```

<Quiz
  id="02-arrays-strings-code-4"
  lessonSlug="02-arrays-strings"
  lang="ru"
  question="buildPrefix корректна, но в rangeSum есть off-by-one. Что вернёт rangeSum(p, 1, 3) и какой фикс?"
  choices={[
    { label: "Вернёт p[3] − p[0] = 8 − 0 = 8, а не задуманные 9. Формула запроса должна соответствовать конвенции n+1, где p[k] — сумма первых k элементов: rangeSum должна быть p[j + 1] − p[i], то есть p[4] − p[1] = 11 − 2 = 9.", correct: true },
    { label: "Вернёт 9 корректно; бага нет.", misconception: "Подставь: p[3] − p[0] = 8 − 0 = 8, а не 9. Индексы сдвинуты на один, потому что p[k] суммирует первые k элементов — запросу нужно p[j+1] − p[i]." },
    { label: "Бросит исключение на p[i - 1] при i = 1, потому что p[0] не определён.", misconception: "p[0] определён и равен 0 — buildPrefix его заполняет. Краша нет; баг — тихий неверный ответ 8 из-за рассогласованной индексации." },
    { label: "Вернёт 6, потому что prefix sum всегда недосчитывает первый элемент.", misconception: "Prefix sum не недосчитывает систематически; формула просто должна соответствовать конвенции. Здесь p[3] − p[0] = 8; фикс — p[j+1] − p[i] = 9." },
  ]}
/>

<Recap lang="ru">
Чтение приёмов в коде — там, где прячутся баги. Вариативное окно — O(n), потому что left движется только вперёд: внутренний while амортизирован, а не вложен. Уплотнение in-place гарантирует только a[0..k-1]; хвост устарел по контракту. Two-sum с концов несостоятелен на неотсортированном входе, даже когда случайно возвращает пару. А запросы prefix sum живут или умирают на конвенции n+1: p[k] — сумма первых k элементов, поэтому формула диапазона — p[j+1] − p[i]. Проследи указатели и проверь границу, прежде чем доверять циклу.
</Recap>
