---
concepts:
- contiguous-memory
- two-pointers
- sliding-window
- prefix-sums
- in-place
- string-immutability
estMin: 13
lang: ru
level: middle
order: 8
prereqs: []
slug: quiz-short
sources:
- https://en.wikipedia.org/wiki/Array_(data_structure)
- https://usaco.guide/silver/prefix-sums
- https://www.geeksforgeeks.org/dsa/window-sliding-technique/
status: ready
summary: "Промпты на свободное припоминание по юниту «Массивы и строки». Сначала ответь своими словами, затем раскрой модельный ответ и сравни."
title: 'Массивы и строки: свободное припоминание'
track: algorithms
unit: 02-arrays-strings
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";

<Hook>
Припоминание сильнее перечитывания. На каждый промпт проговори или запиши полный ответ по памяти, прежде чем открыть модельный — именно усилие припоминания закрепляет приём и его предусловие.
</Hook>

<Goal lang="ru">
Восстанови стержень юнита — почему непрерывность задаёт всю стоимость, почему two pointers и sliding window — O(n), что меняет prefix sum, чего стоит in-place и в чём ловушка конкатенации строк — не подглядывая в уроки.
</Goal>

<RetrievalDrawer
  client:visible
  id="02-arrays-strings-short-retrieval"
  lang="ru"
  questions={[
    { q: "Почему непрерывность массива даёт и силу (O(1) индексация), и слабость (O(n) вставка в середину)?", a: "Непрерывность означает, что элементы лежат в соседних ячейках памяти одинакового размера, поэтому адрес элемента i равен base + i × element_size — прямой расчёт без сканирования, что даёт O(1) чтение и запись по индексу. Та же раскладка — слабость: чтобы вставить или удалить в середине, нужно сохранить ячейки непрерывными, поэтому каждый элемент после точки изменения сдвигается на одну ячейку — это O(n). Append — исключение, O(1) амортизированно: он пишет в следующую свободную ячейку и платит O(n) лишь на редком расширении-с-копированием, когда ёмкость исчерпана, размазывая это по многим append. Один факт — соседство — объясняет всю таблицу стоимости." },
    { q: "Two pointers бывает в двух формах. Назови их, для чего каждая, и предусловие, на котором держится two-sum с концов.", a: "С концов (opposite-ends): left стартует с 0, right с конца, и они сходятся по правилу — для проверки палиндрома, разворота in-place и two-sum на отсортированном массиве. В одном направлении (fast/slow): оба стартуют с начала, fast разведывает вперёд, slow записывает — для уплотнения in-place, например удаления дубликатов или значения. Обе заменяют вложенный перебор пар за O(n²) одним проходом за O(n), потому что каждый указатель сдвигается не более n раз. Предусловие two-sum с концов — массив отсортирован: только тогда сдвиг left вверх надёжно увеличивает сумму, а сдвиг right вниз — уменьшает. На неотсортированных данных это правило некорректно и может пропускать пары." },
    { q: "Почему sliding window — O(n), а не O(n²), даже когда в вариативной версии есть вложенный while?", a: "Окно несёт текущее состояние (сумму, счётчик), которое обновляется за O(1) на шаг: добавляем новый правый элемент и, при сжатии, убираем левый. И left, и right движутся только вперёд, никогда назад. Поэтому за весь прогон right сдвигается не более n раз и left — не более n раз; суммарное движение ограничено 2n. Внутренний while тем самым амортизирован: он делит единый бюджет движения left вперёд, а не запускает свежий проход за O(n) на каждую внешнюю итерацию. Это монотонное движение только вперёд и схлопывает наивный подход «пересчитать каждый подмассив» за O(n²) до O(n)." },
    { q: "Что покупает prefix-массив, чего он стоит и когда это неподходящий инструмент?", a: "Построй prefix один раз, где prefix[k] — сумма первых k элементов (длина n+1, prefix[0] = 0), за O(n) времени и O(n) памяти. Тогда любая сумма на диапазоне от i до j равна prefix[j+1] − prefix[i] за O(1) — вычитание сокращает всё до индекса i. Для q запросов это превращает O(n·q) в O(n + q), огромный выигрыш при большом q. Это классический компромисс время–память: тратим память и один проход, чтобы повторные запросы были бесплатны. Неподходящий инструмент, когда запросов всего один-два, или когда массив часто меняется — любое обновление элемента делает невалидными все нижестоящие значения prefix и вынуждает O(n)-перестройку, поэтому он хорош только на статичных данных." },
    { q: "Что точно означает in-place, чего он стоит и назови две ключевые техники in-place из юнита.", a: "In-place означает перестройку массива через перезапись его собственных ячеек, используя лишь O(1) вспомогательной памяти — пара указателей и временных переменных — вместо построения нового массива за O(n). Цена — он разрушает исходный вход, поэтому, если вызывающему он ещё нужен, сначала копируй. Две ключевые техники: (1) write-указатель — указатель сканирования читает, а write-указатель помечает, куда идёт следующий сохранённый элемент, для уплотнения, фильтрации или удаления значения/дубликатов; и (2) обмен двумя указателями — указатели с концов меняются местами и идут внутрь, для разворота in-place. Это важнее всего на больших данных и в средах с ограниченной памятью, где лишний O(n)-буфер непозволителен." },
    { q: "Строка алгоритмически — это массив, но построение её через повторный += в цикле — O(n²). Объясни почему и дай фикс.", a: "Строки неизменяемы: символ нельзя поменять на месте, поэтому каждый += выделяет совершенно новую строку и копирует в неё все накопленные ранее символы. Итерация i копирует около i символов, поэтому суммарная работа 1 + 2 + ... + n = O(n²) — для 10 000 добавлений это примерно 50 миллионов копий, спрятанных в безобидном на вид коде. Фикс — складывать куски в массив (push — O(1) амортизированно, без копирования) и один раз join(''), это один проход за O(n), строящий финальную строку. Кроме этой ловушки, строки поддерживают O(1) индексацию и все приёмы юнита 02 — two pointers для палиндромов, sliding window для подстрок, проход для подсчёта символов." },
  ]}
/>

<Recap lang="ru">
Если ты смог восстановить каждый ответ по памяти, ты держишь стержень юнита: непрерывность задаёт всю таблицу стоимости (O(1) индекс, O(n) сдвиг, O(1) амортизированный append); two pointers и sliding window достигают O(n) движением только вперёд за один проход; two-sum с концов требует отсортированного входа; prefix sum меняет O(n) памяти на O(1) запросы по диапазону на статичных данных; in-place покупает O(1) памяти ценой разрушения входа; а строки — это массивы, что наказывают конкатенацию в цикле за O(n²) — складывай в массив и join.
</Recap>
