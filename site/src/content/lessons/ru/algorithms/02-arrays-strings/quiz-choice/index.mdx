---
concepts:
- two-pointers
- sliding-window
- prefix-sums
- in-place
- amortized-time
- string-immutability
estMin: 13
lang: ru
level: middle
order: 7
prereqs: []
slug: quiz-choice
sources:
- https://usaco.guide/silver/prefix-sums
- https://www.geeksforgeeks.org/dsa/window-sliding-technique/
- https://labuladong.online/en/algo/essential-technique/array-two-pointers-summary/
status: ready
summary: "Синтез юнита «Массивы и строки» в формате выбора: цена непрерывности, two pointers, sliding window, prefix sum, запись in-place и ловушка конкатенации строк."
title: 'Массивы и строки: тест с выбором ответа'
track: algorithms
unit: 02-arrays-strings
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Шесть вопросов через весь юнит. Каждый — это выбор, который ты делаешь у доски или на код-ревью: не определение для зубрёжки, а правильный приём и то условие, на которое он молча опирается.
</Hook>

<Goal lang="ru">
Убедись, что связываешь модель стоимости непрерывного массива с приёмами поверх неё — two pointers, sliding window, prefix sum, запись in-place и строки-как-массивы — и узнаёшь предусловие, которое каждый из них тихо предполагает.
</Goal>

<Quiz
  id="02-arrays-strings-choice-1"
  lessonSlug="02-arrays-strings"
  lang="ru"
  question="Профайлер показывает горячий цикл, который на каждый запрос вставляет по индексу 0 в массив из 1M элементов. Почему это узкое место и какой правильный фикс?"
  choices={[
    { label: "Вставка в начало — O(n), потому что каждый существующий элемент сдвигается на одну ячейку вправо; при 1M это ~1M копий за вызов. Дописывай в конец (append, O(1) амортизированно) и читай результат в обратном порядке, либо возьми структуру с O(1) вставкой в начало.", correct: true },
    { label: "Индексация стала медленной, потому что массив большой — на таком размере чтение уже не O(1).", misconception: "Индексация — O(1) независимо от размера: address = base + i × element_size, без сканирования. Здесь цена — это сдвиг, который навязывает вставка в начало, а не сам доступ." },
    { label: "Массив нужно сначала отсортировать, чтобы вставка стала быстрой.", misconception: "Сортировка не меняет цену вставки — вставка в середину или начало отсортированного массива всё равно сдвигает O(n) элементов. Сортировка помогает поиску, а не вставке." },
    { label: "Всё нормально; вставка в динамический массив всегда O(1) амортизированно, как append.", misconception: "O(1) амортизированно — только у append. Вставка в начало или середину — O(n) из-за сдвига, как бы массив ни рос." },
  ]}
/>

<Quiz
  id="02-arrays-strings-choice-2"
  lessonSlug="02-arrays-strings"
  lang="ru"
  question="Ты применяешь two pointers с концов (two-sum) для поиска пары с заданной суммой, но он пропускает пары, которые ты видишь глазами. Самая вероятная причина?"
  choices={[
    { label: "Массив не отсортирован. Two-sum с концов опирается на инвариант: сдвиг left вверх увеличивает сумму, сдвиг right вниз уменьшает её — это верно только на отсортированных данных, поэтому на неотсортированном массиве правило движения некорректно и пары теряются.", correct: true },
    { label: "Two pointers умеет найти всего одну пару, поэтому любой пропуск ожидаем.", misconception: "Сходящийся проход рассматривает все жизнеспособные пары за один проход и не останавливается после первой. Дефект — запуск на неотсортированном входе, где правило движения больше не отслеживает направление суммы." },
    { label: "Оба указателя должны стартовать с индекса 0, а не с концов.", misconception: "Оба с индекса 0 — это форма same-direction (fast/slow) для уплотнения in-place. Two-sum требует схождения с концов; баг — отсутствие предусловия сортировки, а не стартовые позиции." },
    { label: "Two pointers — это O(n²), поэтому он не укладывается во время и возвращает частичный результат.", misconception: "Two pointers — O(n): каждый указатель сдвигается не более n раз. Тут он не «зависает» — он возвращает неверные ответы, потому что нарушено предусловие сортировки." },
  ]}
/>

<Quiz
  id="02-arrays-strings-choice-3"
  lessonSlug="02-arrays-strings"
  lang="ru"
  question="Для «наименьший подмассив с суммой ≥ target» на положительных целых вариативное окно (sliding window) работает за O(n), хотя внутри есть вложенный while. Почему это не O(n²)?"
  choices={[
    { label: "left никогда не идёт назад. За весь прогон right сдвигается не более n раз и left — не более n раз, поэтому суммарное движение указателей ограничено 2n. Внутренний while делит единый бюджет с внешним for, а не перезапускает его.", correct: true },
    { label: "Внутренний while на каждую итерацию внешнего цикла выполняется константное число раз, значит он O(1) каждый раз.", misconception: "Внутренний цикл может прокрутиться много раз на одном шаге и ноль раз на другом. Оценка O(n) идёт из того, что суммарное движение left вперёд ≤ n за весь прогон, а не из ограничения на итерацию." },
    { label: "Окно имеет фиксированный размер k, поэтому каждый шаг — O(1).", misconception: "Это вариативное окно — его длина меняется. Окно фиксированного размера — другая форма; здесь оценка O(n) держится на том, что left и right каждый сдвигаются вперёд не более n раз суммарно." },
    { label: "Sliding window требует, чтобы массив был отсортирован, и это убирает внутренний цикл.", misconception: "Sliding window работает на неотсортированных массивах и сохраняет внутренний цикл сжатия. Оценка амортизирована по монотонному движению указателей только вперёд, а не из-за сортировки." },
  ]}
/>

<Quiz
  id="02-arrays-strings-choice-4"
  lessonSlug="02-arrays-strings"
  lang="ru"
  question="Сервис отвечает на тысячи запросов суммы на диапазоне по логу фиксированной длины. Сейчас каждый запрос проходит диапазон. Когда предвычисление prefix sum — правильный выбор и чего оно стоит?"
  choices={[
    { label: "Когда массив статичен, а запросов много: построй prefix-массив один раз за O(n) времени и O(n) памяти, затем отвечай на каждую сумму диапазона за O(1) через prefix[j+1] − prefix[i]. Итог падает с O(n·q) до O(n + q).", correct: true },
    { label: "Всегда — prefix sum делает любую операцию с массивом O(1).", misconception: "Prefix sum ускоряет только запросы вида «сумма на диапазоне» на статичном массиве. Он ничего не даёт для вставки, поиска или поэлементного обновления, а любое изменение элемента вынуждает O(n)-перестройку." },
    { label: "Здесь никогда, потому что построение prefix — O(n), а это медленнее одного запроса.", misconception: "Построение — O(n) однократно, но амортизируется по тысячам O(1)-запросов. При большом q предвычисление многократно окупается." },
    { label: "Только если массив отсортирован, чтобы накопленные суммы были монотонными.", misconception: "Prefix sum не требует сортировки — он работает на любых значениях, включая отрицательные. Единственное реальное требование — массив остаётся статичным между запросами." },
  ]}
/>

<Quiz
  id="02-arrays-strings-choice-5"
  lessonSlug="02-arrays-strings"
  lang="ru"
  question="Ревьюер отмечает, что твой in-place «удалить все нули» через write-указатель мутирует массив вызывающего, ломая последующее чтение оригинала. Как правильно сформулировать компромисс?"
  choices={[
    { label: "In-place покупает O(1) вспомогательной памяти ценой перезаписи входа, и плата — его разрушение. Если вызывающему нужен оригинал, сначала копируй (const backup = [...arr]); иначе разрушающая запись — это ровно то, что и задумано.", correct: true },
    { label: "Техника write-указателя багованная — для безопасности она должна строить новый массив.", misconception: "Построение нового массива — это альтернатива с O(n) памяти, а не починка бага. Подход с write-указателем корректен; вопрос — в контракте, может ли вызывающий сохранять оригинал." },
    { label: "In-place-алгоритмы всегда быстрее, так что мутация — приемлемая цена за скорость.", misconception: "Преимущество — память (O(1) вспомогательной), а не гарантированная скорость. Реальный компромисс — сэкономленная память против перезаписи исходного входа." },
    { label: "Write-указатель должен идти назад от конца, чтобы не мутировать вход.", misconception: "Направление не отменяет мутацию — любое уплотнение in-place перезаписывает ячейки. Чтобы сохранить оригинал, копируй его заранее, а не меняй направление прохода." },
  ]}
/>

<Quiz
  id="02-arrays-strings-choice-6"
  lessonSlug="02-arrays-strings"
  lang="ru"
  question="Функция собирает большой CSV через result += line внутри цикла по n строкам и неожиданно медленна на масштабе. Что происходит и какой фикс?"
  choices={[
    { label: "Строки неизменяемы, поэтому каждый += выделяет новую строку и копирует всё накопленное ранее; итерация i копирует ~i символов, в сумме O(n²). Складывай куски в массив и один раз join('') — это O(n).", correct: true },
    { label: "Цикл — O(n), а медлительность — это просто запись на диск, а не конкатенация.", misconception: "Сама конкатенация — O(n²): каждый += копирует всю строку целиком. Даже без ввода-вывода квадратичное копирование доминирует на масштабе." },
    { label: "Индексация в растущей строке медленна, поэтому кэшируй s.length в переменную.", misconception: "Индексация и length — O(1). Цена — повторные полные копии, которые навязывает неизменяемость на каждом +=, а не доступ к индексу или длине." },
    { label: "Замена += на оператор + делает это O(n).", misconception: "+ и += делают одно и то же выделение-и-копирование на неизменяемых строках — оба O(n²) в цикле. Фикс — push в массив плюс один join, а не другой оператор." },
  ]}
/>

<Recap lang="ru">
Сквозная линия юнита — одна модель стоимости и приёмы, что её используют. Непрерывность даёт O(1) индексацию, но O(n) сдвиги, поэтому вставка в начало/середину — ловушка, а append дёшев. Two pointers превращают вложенный перебор пар в один проход — но two-sum с концов требует отсортированного входа. Sliding window — O(n), потому что указатели идут только вперёд. Prefix sum меняет O(n) памяти на O(1) запросы по диапазону на статичных данных. Запись in-place покупает O(1) памяти ценой оригинала. А строки — это массивы с одним укусом: неизменяемость делает конкатенацию в цикле O(n²) — складывай в массив и join.
</Recap>
