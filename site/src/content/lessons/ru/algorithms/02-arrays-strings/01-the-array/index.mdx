---
concepts:
- array
- index
- contiguous_memory
- dynamic_array
- amortized_time
estMin: 17
lang: ru
mathPrereqs: []
order: 1
prereqs:
- 04-big-o-notation
slug: 01-the-array
sources:
- https://en.wikipedia.org/wiki/Array_(data_structure)
- https://www.freecodecamp.org/news/data-structures-101-arrays-a-visual-introduction-for-beginners-7f013bcc355a
- https://dev.to/ggorantala/array-strengths-weaknesses-and-big-o-complexity-analysis-4aho
- https://www.interviewcake.com/concept/java/dynamic-array-amortized-analysis
status: ready
summary: 'Массив — это ряд ячеек в памяти, в каждой ячейке одно значение. Ячейки стоят
  рядом (контигуозны), поэтому ты можешь прыгнуть в любую за O(1). Поняв это, ты поймёшь
  стоимость каждой операции: чтение/запись O(1), поиск O(n), вставка в середину O(n).'
title: Массив
track: algorithms
unit: 02-arrays-strings
---



import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Idea from "~/components/lesson/Idea.astro";
import Code from "~/components/lesson/Code.astro";
import Trace from "~/components/lesson/Trace.astro";
import Complexity from "~/components/lesson/Complexity.astro";
import Check from "~/components/lesson/Check.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import AnnotatedCode from "~/components/algo/AnnotatedCode.astro";
import AlgoTrace from "~/components/algo/AlgoTrace.astro";
import StructureFigure from "~/components/algo/StructureFigure.astro";
import CodeRunner from "~/components/algo/CodeRunner.astro";
import AlgoPractice from "~/components/algo/AlgoPractice.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Представь ряд пронумерованных ящиков, в каждом по значению. Твой массив — это именно так: последовательность ящиков в памяти, все в ряд. Эта простота — источник мощи массива. Ящики стоят рядом, поэтому компьютер находит любой ящик мгновенно.
</Hook>

<Goal lang="ru">
После этого урока ты сможешь представить массив как контигуозную память, объяснить, почему индексирование O(1), измерить стоимость операций чтения, записи, поиска, вставки, удаления и добавления, и понять, почему сила массива (быстрый доступ) и слабость (медленная вставка в середину) идут из одного факта: контигуозность.
</Goal>

<Idea lang="ru">
**Массив** — это последовательность элементов в контигуозной (соседней) памяти. "Контигуозная" значит элементы стоят рядом: ячейка 0 по адресу 1000, ячейка 1 по адресу 1004, ячейка 2 по адресу 1008 и так далее. Каждый элемент одного размера (например, 4 байта на 32-битное число).

**Почему контигуозность важна:** Компьютер считает адрес любого элемента простой формулой:

```
адрес элемента[i] = базовый_адрес + (i × размер_элемента)
```

Например, массив начинается по адресу 1000, каждый элемент 4 байта:
- `массив[0]` по адресу 1000 + (0 × 4) = 1000
- `массив[1]` по адресу 1000 + (1 × 4) = 1004
- `массив[5]` по адресу 1000 + (5 × 4) = 1020

Компьютер прыгает прямо по адресу 1020, не читая другие элементы. Это **O(1)**.

**Операции с массивом и их стоимость:**

1. **Чтение или запись по индексу** — O(1). Вычислить адрес, получить или записать значение. Готово.

2. **Поиск значения** — O(n). Ты не знаешь, какой индекс содержит цель, поэтому сканируешь с начала, пока не найдёшь (или не кончится массив). В худшем случае ищешь все n элементов.

<AnnotatedCode
  code={`function search(arr, target) {
  for (let i = 0; i < arr.length; i++) {
    if (arr[i] === target) return i;
  }
  return -1;  // не найдено
}`}
  annotations={[
    { line: 2, text: "Цикл по каждому элементу (O(n) в худшем случае)" },
    { line: 3, text: "O(1) поиск по индексу внутри цикла" }
  ]}
/>

3. **Вставка в середину** — O(n). Чтобы вставить в индекс 3, сдвигаешь все элементы с индексов 3, 4, 5, ... на один шаг вправо. Если в массиве 100 элементов, сдвигаешь ~97. Сдвиг — вот стоимость.

4. **Удаление из середины** — O(n). То же самое: сдвигаешь все элементы после удаляемого на один шаг влево, закрыв дыру.

5. **Добавление в конец (append)** — O(1) амортизировано. Просто положи новый элемент в следующую свободную ячейку. Сдвигать ничего не надо (если вместимость не исчерпана, что бывает редко).

**Суть:** Контигуозность даёт тебе O(1) чтение, но O(n) запись в середину. Это основной трейдофф массива.

**Фиксированный vs динамический массив:**

**Фиксированный массив** имеет вместимость, установленную при создании. Когда полон, ничего не добавишь.

**Динамический массив** (как в JavaScript) растёт автоматически. Когда вместимость исчерпана, он выделяет больший блок, копирует все элементы, потом удаляет старый. Копирование стоит O(n), но происходит редко — только когда вместимость кончилась. Если массив каждый раз удваивает вместимость, стоимость копирования размазывается тонко по множеству добавлений. Мы говорим, что **среднее время на добавление O(1) амортизировано**.
</Idea>

<StructureFigure
  indices={true}
  cells={[
    { value: "10" },
    { value: "20" },
    { value: "30" },
    { value: "40" },
    { value: "50" },
  ]}
  caption="Массив — это контигуозная память с индексацией от 0: ячейки одного размера стоят вплотную, без зазоров. Блок начинается с базового адреса; при ячейках по 4 байта от адреса 1000 элемент i лежит по адресу база + i x 4 (то есть 1000, 1004, 1008, 1012, 1016). Так как этот адрес — одно умножение со сложением, компьютер достаёт любой элемент за один шаг — O(1) случайный доступ."
/>

<Code lang="ru">
Запрограммируем базовые операции с массивом:

**O(1) индексирование:**
```js
function get(arr, i) {
  return arr[i];  // Мгновенно: вычислить адрес, вернуть значение
}
```

**O(n) линейный поиск:**
```js
function search(arr, target) {
  for (let i = 0; i < arr.length; i++) {
    if (arr[i] === target) {
      return i;
    }
  }
  return -1;  // не найдено
}
```

**O(n) вставка на индекс i:**
```js
function insertAt(arr, i, value) {
  // Сдвинуть все элементы с индекса i и далее на один шаг вправо
  for (let j = arr.length - 1; j >= i; j--) {
    arr[j + 1] = arr[j];
  }
  // Поместить новое значение
  arr[i] = value;
}
```

Цикл сдвига идёт с конца назад (чтобы не перезаписать). Для массива из 100 элементов вставка на индекс 0 сдвигает все 100. Для 1000 элементов — все 1000. Это O(n).

Попробуй вызвать эти функции:

<CodeRunner
  id="cr-array-ops"
  lang="ru"
  code={`const arr = [10, 20, 30, 40];

// O(1): получить элемент по индексу 2
console.log("Элемент по индексу 2:", get(arr, 2));

// O(n): найти индекс значения 30
console.log("Индекс 30:", search(arr, 30));

// O(n): вставить 25 на индекс 2
insertAt(arr, 2, 25);
console.log("После insertAt(arr, 2, 25):", arr);

function get(arr, i) {
  return arr[i];
}

function search(arr, target) {
  for (let i = 0; i < arr.length; i++) {
    if (arr[i] === target) return i;
  }
  return -1;
}

function insertAt(arr, i, value) {
  for (let j = arr.length - 1; j >= i; j--) {
    arr[j + 1] = arr[j];
  }
  arr[i] = value;
}`}
/>
</Code>

<Trace lang="ru">
Посмотри функцию `insertAt` пошагово. Вставляем значение 25 на индекс 2 в массив [10, 20, 30, 40]:

<AlgoTrace
  id="trace-insert"
  lang="ru"
  code={[
    "function insertAt(arr, i, value) {",
    "  for (let j = arr.length - 1; j >= i; j--) {",
    "    arr[j + 1] = arr[j];",
    "  }",
    "  arr[i] = value;",
    "}"
  ]}
  frames={[
    {
      caption: "Начало: arr = [10, 20, 30, 40], вставляем 25 на индекс 2.",
      line: 1,
      cells: [
        { value: "10", mark: "value" },
        { value: "20", mark: "value" },
        { value: "30", mark: "target" },
        { value: "40", mark: "value" }
      ]
    },
    {
      caption: "Итерация j = 3: сдвинуть arr[3] = 40 на arr[4]. Массив растёт на одну ячейку.",
      line: 3,
      cells: [
        "10",
        "20",
        "30",
        { value: "40", mark: "done" },
        { value: "40", mark: "shifted" }
      ]
    },
    {
      caption: "Итерация j = 2: сдвинуть arr[2] = 30 на arr[3].",
      line: 3,
      cells: [
        "10",
        "20",
        { value: "30", mark: "done" },
        { value: "30", mark: "shifted" },
        "40"
      ]
    },
    {
      caption: "Цикл кончился. Теперь arr[2] пусто. Положим значение 25 на arr[2].",
      line: 5,
      cells: [
        "10",
        "20",
        { value: "25", mark: "done" },
        "30",
        "40"
      ]
    },
    {
      caption: "Готово: arr = [10, 20, 25, 30, 40]. Мы сдвинули 2 элемента. На большом массиве — намного больше.",
      line: 6,
      cells: [
        "10",
        "20",
        "25",
        "30",
        "40"
      ]
    }
  ]}
/>
</Trace>

<Complexity lang="ru">
Таблица временной сложности операций с массивом:

| Операция | Пример | Время |
|---|---|---|
| Получить элемент | `arr[5]` | O(1) |
| Установить элемент | `arr[5] = 99` | O(1) |
| Поиск значения | `search(arr, 99)` | O(n) |
| Вставить на индекс i | `insertAt(arr, 2, 50)` | O(n) |
| Удалить на индексе i | `deleteAt(arr, 2)` | O(n) |
| Добавить в конец (push) | `arr.push(99)` | O(1) амортизировано |

**Почему append O(1) амортизировано?** В динамическом массиве добавление бесплатно, пока есть свободная вместимость. Когда вместимость кончилась, массив удваивает размер и копирует все n элементов (O(n) работы). Но это происходит редко — один раз на каждые n добавлений. Распределённая по n операциям, стоимость копирования в среднем O(1) на добавление.

**Основная идея:** Случайный доступ дёшев. Последовательная модификация дорога. Это делает массивы идеальными для операций с читаем и ужасными для частых вставок в середину.
</Complexity>

<AlgoPractice
  id="practice-array-1"
  lessonSlug="01-the-array"
  lang="ru"
  problems={[
    {
      kind: "numeric",
      prompt: "Массив из 1000 элементов начинается по адресу 2000 в памяти. Каждый элемент занимает 8 байт. Какой адрес у элемента массив[10]?",
      answer: 2080,
      hint: "адрес = база + (индекс × размер_элемента) = 2000 + (10 × 8)"
    },
    {
      kind: "choice",
      prompt: "Почему чтение массив[i] всегда O(1), даже для очень больших массивов?",
      choices: [
        "Потому что JavaScript автоматически оптимизирует доступ к массиву",
        "Потому что адрес памяти можно вычислить прямо, без сканирования других элементов",
        "Потому что массив помнит, где лежит каждый элемент",
        "Потому что современные компьютеры работают быстро"
      ],
      answer: 1,
      hint: "Ключ — контигуозность: адрес = база + (i × размер_элемента). Сканирование не нужно."
    },
    {
      kind: "choice",
      prompt: "Ты хочешь вставить элемент на индекс 0 в массив из 500 элементов. Сколько элементов должно сдвинуться?",
      choices: [
        "0 — просто положи в начало",
        "1 — только элемент на индексе 0",
        "500 — все элементы сдвинутся вправо",
        "499 — все, кроме первого"
      ],
      answer: 2,
      hint: "Чтобы освободить место на индексе 0, все 500 существующих элементов должны сдвинуться вправо на одну позицию."
    },
    {
      kind: "choice",
      prompt: "Что значит 'амортизировано O(1)' для добавления в динамический массив?",
      choices: [
        "Добавление всегда занимает O(1) времени",
        "В среднем добавление O(1), хотя иногда происходит resize (O(n))",
        "Добавление занимает меньше времени, чем O(n)",
        "Динамические массивы не занимают реальную память, поэтому время не имеет значения"
      ],
      answer: 1,
      hint: "Большинство добавлений мгновенны (есть свободная вместимость). Редкие resize O(n), но размазаны по множеству операций."
    },
    {
      kind: "order",
      prompt: "Расставь эти операции с массивом от быстрейшей к медленнейшей (от лучшей к худшей сложности):",
      items: [
        "Добавить в конец (динамический массив)",
        "Прочитать элемент по индексу",
        "Найти значение",
        "Вставить в начало"
      ],
      hint: "O(1) амортизировано < O(1) < O(n) < O(n). Первые два — постоянное время. Поиск и вставка — линейные."
    }
  ]}
/>

<Inset kind="edgecase" lang="ru">
**Фиксированные массивы кончаются.** Если массив вместит 4 элемента, а ты пытаешься добавить 5-й, получишь ошибку или потеряешь элемент (в языке, как C). JavaScript массивы всегда динамические — они молча растут. Но рост не бесплатен; он стоит O(n) работы. Поэтому добавление "амортизировано" O(1), а не всегда O(1).
</Inset>

<Inset kind="mistake" lang="ru">
**Путаешь размер массива и стоимость.** Вставить в массив из 10 элементов намного быстрее, чем в массив из 1 миллиона. Но оба случая "O(n)". Не думай, что O(n) значит "время не важно"; это значит время растёт пропорционально n. На 1 миллион элементов ты реально сдвинешь 1 миллион раз.
</Inset>

<Check lang="ru">
  <Quiz
    id="quiz-array-1"
    pieceSlug="01-the-array"
    lang="ru"
    question="Почему контигуозность массива (элементы в соседней памяти) даёт ему и силу, и слабость?"
    choices={[
      {
        label: "Контигуозность позволяет быстрое индексирование (O(1)), но вынуждает медленную вставку в середину (O(n)), потому что нужны сдвиги",
        correct: true
      },
      {
        label: "Контигуозность не связана со стоимостью вставки; вставка медленна, потому что компьютеры медленные",
        misconception: "Стоимость вставки идёт из необходимости сдвигать элементы. Контигуозность — вот почему сдвиги требуются."
      },
      {
        label: "Контигуозность не имеет слабости; массивы всегда лучше других структур",
        misconception: "Массивы быстро читают, но медленно вставляют в середину. У разных структур разные трейдоффы."
      },
      {
        label: "Контигуозность значит, что массив может хранить только один тип элемента",
        misconception: "Контигуозность — это расположение в памяти, не тип элемента. Современные массивы могут хранить разные типы (в JavaScript)."
      }
    ]}
  />
</Check>

<Recap lang="ru">
**Массив** — это ряд контигуозных ячеек памяти, все одного размера.

Ключевые факты:
1. **Контигуозная память:** Элементы лежат рядом, позволяя вычислить адрес: `база + (индекс × размер_элемента)`.
2. **O(1) чтение и запись:** Получить или изменить элемент по индексу за постоянное время — без сканирования.
3. **O(n) поиск:** Найти значение нужно сканировать элементы один за другим (худший случай: все n).
4. **O(n) вставить/удалить в середину:** Надо сдвинуть все элементы после изменения, линейно в худшем случае.
5. **O(1) амортизировано добавить:** Бесплатно большую часть времени; редко вызывает resize, стоимость размазана.
6. **Трейдофф:** Контигуозность даёт быстрый доступ, но медленную модификацию. Выбери массивы для операций, где много чтения.

Дальше ты узнаешь приёмы, что используют это знание: как быстрее искать (бинарный поиск), как быстрее менять (два указателя, скользящее окно), и когда использовать другие структуры (списки, очереди) вместо массива.
</Recap>
