---
concepts:
- sliding_window
- window
- subarray
- constraint
- two_pointers
estMin: 20
lang: ru
mathPrereqs: []
order: 4
prereqs:
- 03-two-pointers
slug: 04-sliding-window
sources:
- https://www.geeksforgeeks.org/dsa/window-sliding-technique/
- https://levelup.gitconnected.com/maximum-sum-in-a-sliding-window-of-length-w-69f4e8b86af4
- https://algomap.io/problems/minimum-size-subarray-sum
status: ready
summary: 'Окно [left, right] скользит по массиву. Вместо пересчёта свойства для каждого
  подмассива (O(n²)), ты скользишь и обновляешь за O(1) за шаг, итого O(n). Два варианта:
  окно фиксированного размера (длина k) и переменного размера (растёшь/сжимаешь по
  условию).'
title: Скользящее окно
track: algorithms
unit: 02-arrays-strings
---



import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Idea from "~/components/lesson/Idea.astro";
import Code from "~/components/lesson/Code.astro";
import Trace from "~/components/lesson/Trace.astro";
import Complexity from "~/components/lesson/Complexity.astro";
import Check from "~/components/lesson/Check.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import AnnotatedCode from "~/components/algo/AnnotatedCode.astro";
import AlgoTrace from "~/components/algo/AlgoTrace.astro";
import CodeRunner from "~/components/algo/CodeRunner.astro";
import AlgoPractice from "~/components/algo/AlgoPractice.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DiagramFrame from "~/components/diagram/DiagramFrame.astro";

<Hook>
У тебя есть массив, и нужно найти максимальную сумму любых 5 подряд идущих элементов. Наивный подход: пересчитать сумму каждого окна длины 5 с нуля. Это O(n × 5) = O(n). Но если ты уже знаешь сумму одного окна? Ты можешь убрать левый элемент и добавить новый правый, обновив сумму за O(1). Скользи окно по массиву, обновляя раз за шаг. Итого: O(n) вместо пересчёта. Вот техника скользящего окна.
</Hook>

<Goal lang="ru">
После этого урока ты сможешь узнавать, когда применяется скользящее окно, программировать окна фиксированного размера (постоянная длина k) и переменного размера (растут и сжимаются по условию), разобраться, почему они сводят O(n²) или хуже к O(n), и решить классические задачи: максимальную сумму k подряд идущих элементов и наименьший подмассив с суммой ≥ цель.
</Goal>

<Idea lang="ru">
**Скользящее окно** — техника, где контигуозный диапазон [left, right] движется по массиву. Вместо пересчёта свойства (скажем, суммы) для каждого возможного подмассива — что стоит O(n²) или хуже — ты держишь это свойство, скользя окно, и обновляешь его за O(1) за шаг.

**Два основных варианта:**

**1. Окно фиксированного размера (постоянная длина k)**

Окно всегда длины k. Начни, вычислив свойство (скажем, сумму) для первых k элементов. Потом скользи:
- Убери элемент на позиции `left` из свойства.
- Добавь элемент на позиции `left + k` к свойству.
- Продвинь `left` на 1.

Повторяй, пока правый край не дойдёт до конца массива.

```js
let left = 0;
let windowSum = sum of arr[0..k-1];
let maxSum = windowSum;

for (let right = k; right < arr.length; right++) {
  // Убери arr[left], добавь arr[right]
  windowSum = windowSum - arr[left] + arr[right];
  maxSum = Math.max(maxSum, windowSum);
  left++;
}
```

**2. Окно переменного размера (растёшь и сжимаешь по условию)**

Окно растёт, когда ты двигаешь `right` вперёд. Когда условие становится ложным (скажем, сумма превышает цель), окно сжимается, и ты двигаешь `left` вперёд. Продолжай, пока `right` не дойдёт до конца.

```js
let left = 0;
let windowSum = 0;
let result = /* храни результаты */;

for (let right = 0; right < arr.length; right++) {
  // Расти: добавь arr[right]
  windowSum += arr[right];
  
  // Сжимайся, пока условие нарушено
  while (windowSum > target && left <= right) {
    windowSum -= arr[left];
    left++;
  }
  
  // Обработай валидное окно
  result.push({ left, right, sum: windowSum });
}
```

**Почему это работает:**

Наивный подход проверяет каждый возможный подмассив. Для n элементов — O(n²) подмассивов. Пересчитать свойство для каждого — O(n²) или O(n³).

Скользящее окно: окно несёт текущее состояние (сумму, счётчик и т.д.). Каждый скользок — добавить или убрать один элемент — обновляет состояние за O(1). Окно скользит по массиву максимум дважды (left и right двигаются максимум n раз каждый), итого O(n).

**Ключевое озарение: состояние инкрементально.**

Если знаешь сумму [left, right], сумма [left+1, right] это `sum - arr[left]`. Сумма [left, right+1] это `sum + arr[right+1]`. Эти обновления O(1), что позволяет всему алгоритму быть O(n).
</Idea>

<DiagramFrame label="Скользящее окно над массивом [2, 3, 1, 2, 4, 3]. Одно непрерывное окно покрывает ячейки от индекса left=1 до индекса right=3, со значениями 3, 1, 2. Правый граничный указатель стоит под индексом 3 и расширяет окно, двигаясь вправо, чтобы добавить следующий элемент; левый граничный указатель стоит под индексом 1 и сжимает окно, двигаясь вправо, чтобы убрать элемент, который покидает окно, когда ограничение нарушается. Так как right и left каждый продвигается максимум n раз и никогда не идёт назад, каждый элемент входит в окно один раз и выходит один раз, поэтому весь проход — O(n)." hue="var(--d-systems)" caption="Непрерывное окно [left..right] над массивом. right расширяет окно, чтобы втянуть новый элемент; left сжимает его, чтобы выбросить один элемент, когда ограничение нарушается. Текущий агрегат (сумма) обновляется за O(1) за движение. Каждый указатель двигается только вперёд, поэтому каждый элемент входит и выходит из окна ровно один раз — весь проход O(n).">
  <svg viewBox="0 0 480 300" width="480" height="300" style="display:block;margin:0 auto;max-width:100%;height:auto;">
    <text x="240" y="24" text-anchor="middle" font-size="11" font-family="var(--font-body)" fill="var(--muted)">массив — окно сейчас покрывает индексы 1..3</text>

    <rect x="124" y="62" width="172" height="60" rx="6" fill="none" stroke="var(--d-systems)" stroke-width="2" />
    <text x="210" y="56" text-anchor="middle" font-size="10" font-family="var(--font-body)" fill="var(--d-systems)">{"окно [left..right], текущая сумма = 6"}</text>

    <rect x="72" y="70" width="52" height="44" rx="4" fill="var(--card-2)" stroke="var(--hairline-strong)" stroke-width="1.2" />
    <text x="98" y="97" text-anchor="middle" font-size="15" font-family="var(--font-body)" fill="var(--muted)">2</text>
    <text x="98" y="134" text-anchor="middle" font-size="10" font-family="var(--font-body)" fill="var(--muted)">0</text>

    <rect x="128" y="70" width="52" height="44" rx="4" fill="var(--card-2)" stroke="var(--d-systems)" stroke-width="1.6" />
    <text x="154" y="97" text-anchor="middle" font-size="15" font-family="var(--font-body)" fill="var(--ink)">3</text>
    <text x="154" y="134" text-anchor="middle" font-size="10" font-family="var(--font-body)" fill="var(--muted)">1</text>

    <rect x="184" y="70" width="52" height="44" rx="4" fill="var(--card-2)" stroke="var(--d-systems)" stroke-width="1.6" />
    <text x="210" y="97" text-anchor="middle" font-size="15" font-family="var(--font-body)" fill="var(--ink)">1</text>
    <text x="210" y="134" text-anchor="middle" font-size="10" font-family="var(--font-body)" fill="var(--muted)">2</text>

    <rect x="240" y="70" width="52" height="44" rx="4" fill="var(--card-2)" stroke="var(--d-systems)" stroke-width="1.6" />
    <text x="266" y="97" text-anchor="middle" font-size="15" font-family="var(--font-body)" fill="var(--ink)">2</text>
    <text x="266" y="134" text-anchor="middle" font-size="10" font-family="var(--font-body)" fill="var(--muted)">3</text>

    <rect x="296" y="70" width="52" height="44" rx="4" fill="var(--card-2)" stroke="var(--hairline-strong)" stroke-width="1.2" />
    <text x="322" y="97" text-anchor="middle" font-size="15" font-family="var(--font-body)" fill="var(--muted)">4</text>
    <text x="322" y="134" text-anchor="middle" font-size="10" font-family="var(--font-body)" fill="var(--muted)">4</text>

    <rect x="352" y="70" width="52" height="44" rx="4" fill="var(--card-2)" stroke="var(--hairline-strong)" stroke-width="1.2" />
    <text x="378" y="97" text-anchor="middle" font-size="15" font-family="var(--font-body)" fill="var(--muted)">3</text>
    <text x="378" y="134" text-anchor="middle" font-size="10" font-family="var(--font-body)" fill="var(--muted)">5</text>

    <text x="154" y="160" text-anchor="middle" font-size="11" font-family="var(--font-body)" fill="var(--d-systems)">left</text>
    <path d="M154 176 L154 150" fill="none" stroke="var(--d-systems)" stroke-width="1.6" marker-end="url(#sw-ptr)" />

    <text x="266" y="160" text-anchor="middle" font-size="11" font-family="var(--font-body)" fill="var(--d-systems)">right</text>
    <path d="M266 176 L266 150" fill="none" stroke="var(--d-systems)" stroke-width="1.6" marker-end="url(#sw-ptr)" />

    <path d="M276 206 L346 206" fill="none" stroke="var(--d-systems)" stroke-width="1.6" marker-end="url(#sw-arrow)" />
    <text x="311" y="200" text-anchor="middle" font-size="10" font-family="var(--font-body)" fill="var(--d-systems)">расширь right</text>
    <text x="311" y="222" text-anchor="middle" font-size="9" font-family="var(--font-body)" fill="var(--muted)">добавь arr[right]</text>

    <path d="M214 206 L144 206" fill="none" stroke="var(--hairline-strong)" stroke-width="1.6" marker-end="url(#sw-arrow-mut)" />
    <text x="179" y="200" text-anchor="middle" font-size="10" font-family="var(--font-body)" fill="var(--muted)">сожми left</text>
    <text x="179" y="222" text-anchor="middle" font-size="9" font-family="var(--font-body)" fill="var(--muted)">убери arr[left]</text>

    <text x="240" y="262" text-anchor="middle" font-size="11" font-family="var(--font-body)" fill="var(--ink)">оба указателя двигаются только вперёд (никогда назад)</text>
    <text x="240" y="282" text-anchor="middle" font-size="11" font-family="var(--font-body)" fill="var(--d-systems)">каждый элемент входит раз, выходит раз → O(n)</text>

    <defs>
      <marker id="sw-ptr" viewBox="0 0 10 10" refX="9" refY="5" markerWidth="7" markerHeight="7" orient="auto-start-reverse">
        <path d="M0 0 L10 5 L0 10 z" fill="var(--d-systems)" />
      </marker>
      <marker id="sw-arrow" viewBox="0 0 10 10" refX="9" refY="5" markerWidth="7" markerHeight="7" orient="auto-start-reverse">
        <path d="M0 0 L10 5 L0 10 z" fill="var(--d-systems)" />
      </marker>
      <marker id="sw-arrow-mut" viewBox="0 0 10 10" refX="9" refY="5" markerWidth="7" markerHeight="7" orient="auto-start-reverse">
        <path d="M0 0 L10 5 L0 10 z" fill="var(--hairline-strong)" />
      </marker>
    </defs>
  </svg>
</DiagramFrame>

<Code lang="ru">
Запрограммируем окна фиксированного и переменного размера.

**1. Фиксированное: максимальная сумма k подряд идущих элементов**

Дан `arr = [1, 4, 2, 10, 2, 3, 1, 0, 20]` и `k = 3`, найди максимальную сумму.

```js
function maxSumFixedWindow(arr, k) {
  if (arr.length < k) return null;
  
  // Вычисли сумму первого окна
  let windowSum = 0;
  for (let i = 0; i < k; i++) {
    windowSum += arr[i];
  }
  
  let maxSum = windowSum;
  
  // Скользи окно
  for (let right = k; right < arr.length; right++) {
    // Убери самый левый, добавь новый самый правый
    windowSum = windowSum - arr[right - k] + arr[right];
    maxSum = Math.max(maxSum, windowSum);
  }
  
  return maxSum;
}
```

**2. Переменное: наименьший подмассив с суммой ≥ цель**

Дан `arr = [2, 3, 1, 2, 4, 3]` и `target = 7`, найди наименьший подмассив с суммой ≥ 7.

```js
function minSubarrayLength(arr, target) {
  let left = 0;
  let windowSum = 0;
  let minLength = Infinity;
  
  for (let right = 0; right < arr.length; right++) {
    // Расти: добавь правый элемент
    windowSum += arr[right];
    
    // Сжимайся, пока сумма окна слишком большая и валидная
    while (windowSum >= target && left <= right) {
      // Запиши это валидное окно
      minLength = Math.min(minLength, right - left + 1);
      
      // Сжимайся: убери левый элемент
      windowSum -= arr[left];
      left++;
    }
  }
  
  return minLength === Infinity ? 0 : minLength;
}
```

Попробуй запустить:

<CodeRunner
  id="cr-sliding-window"
  lang="ru"
  code={`function maxSumFixedWindow(arr, k) {
  if (arr.length < k) return null;
  
  let windowSum = 0;
  for (let i = 0; i < k; i++) {
    windowSum += arr[i];
  }
  
  let maxSum = windowSum;
  
  for (let right = k; right < arr.length; right++) {
    windowSum = windowSum - arr[right - k] + arr[right];
    maxSum = Math.max(maxSum, windowSum);
  }
  
  return maxSum;
}

function minSubarrayLength(arr, target) {
  let left = 0;
  let windowSum = 0;
  let minLength = Infinity;
  
  for (let right = 0; right < arr.length; right++) {
    windowSum += arr[right];
    
    while (windowSum >= target && left <= right) {
      minLength = Math.min(minLength, right - left + 1);
      windowSum -= arr[left];
      left++;
    }
  }
  
  return minLength === Infinity ? 0 : minLength;
}

const arr = [1, 4, 2, 10, 2, 3, 1, 0, 20];
console.log("Макс сумма 3 подряд:", maxSumFixedWindow(arr, 3));

const arr2 = [2, 3, 1, 2, 4, 3];
console.log("Мин длина подмассива (сумма >= 7):", minSubarrayLength(arr2, 7));
console.log("Мин длина подмассива (сумма >= 15):", minSubarrayLength(arr2, 15));`}
/>
</Code>

<Trace lang="ru">
Трассируем **окно фиксированного размера** на `[1, 4, 2, 10, 2, 3, 1, 0, 20]` с `k = 3`:

<AlgoTrace
  id="trace-fixed-window"
  lang="ru"
  code={[
    "function maxSumFixedWindow(arr, k) {",
    "  let windowSum = arr[0] + arr[1] + arr[2];",
    "  let maxSum = windowSum;",
    "  for (let right = k; right < arr.length; right++) {",
    "    windowSum = windowSum - arr[right - k] + arr[right];",
    "    maxSum = Math.max(maxSum, windowSum);",
    "  }",
    "  return maxSum;",
    "}"
  ]}
  frames={[
    {
      caption: "Старт: arr = [1, 4, 2, 10, 2, 3, 1, 0, 20], k = 3. Первое окно: [1, 4, 2], сумма = 7.",
      line: 1,
      cells: [
        { value: "1", mark: "active" },
        { value: "4", mark: "active" },
        { value: "2", mark: "active" },
        "10",
        "2",
        "3",
        "1",
        "0",
        "20"
      ]
    },
    {
      caption: "right = 3: убери arr[0] = 1, добавь arr[3] = 10. сумма = 7 - 1 + 10 = 16.",
      line: 5,
      cells: [
        "1",
        { value: "4", mark: "active" },
        { value: "2", mark: "active" },
        { value: "10", mark: "active" },
        "2",
        "3",
        "1",
        "0",
        "20"
      ]
    },
    {
      caption: "right = 4: убери arr[1] = 4, добавь arr[4] = 2. сумма = 16 - 4 + 2 = 14.",
      line: 5,
      cells: [
        "1",
        "4",
        { value: "2", mark: "active" },
        { value: "10", mark: "active" },
        { value: "2", mark: "active" },
        "3",
        "1",
        "0",
        "20"
      ]
    },
    {
      caption: "right = 5: убери arr[2] = 2, добавь arr[5] = 3. сумма = 14 - 2 + 3 = 15.",
      line: 5,
      cells: [
        "1",
        "4",
        "2",
        { value: "10", mark: "active" },
        { value: "2", mark: "active" },
        { value: "3", mark: "active" },
        "1",
        "0",
        "20"
      ]
    },
    {
      caption: "right = 8 (последний): убери arr[5] = 3, добавь arr[8] = 20. сумма = 1 - 3 + 20 = 18. maxSum = 18.",
      line: 5,
      cells: [
        "1",
        "4",
        "2",
        "10",
        "2",
        "3",
        { value: "1", mark: "active" },
        { value: "0", mark: "active" },
        { value: "20", mark: "found" }
      ]
    }
  ]}
/>

Теперь трассируем **окно переменного размера** на `[2, 3, 1, 2, 4, 3]` ища сумму ≥ 7:

<AlgoTrace
  id="trace-var-window"
  lang="ru"
  code={[
    "function minSubarrayLength(arr, target) {",
    "  let left = 0, windowSum = 0, minLength = Infinity;",
    "  for (let right = 0; right < arr.length; right++) {",
    "    windowSum += arr[right];",
    "    while (windowSum >= target && left <= right) {",
    "      minLength = Math.min(minLength, right - left + 1);",
    "      windowSum -= arr[left];",
    "      left++;",
    "    }",
    "  }",
    "  return minLength;",
    "}"
  ]}
  frames={[
    {
      caption: "Старт: arr = [2, 3, 1, 2, 4, 3], target = 7. left = 0, right = 0.",
      line: 2,
      cells: [
        { value: "2", mark: "active" },
        "3",
        "1",
        "2",
        "4",
        "3"
      ]
    },
    {
      caption: "right = 0,1,2: windowSum = 2 + 3 + 1 = 6. Ещё < 7, продолжай.",
      line: 4,
      cells: [
        { value: "2", mark: "active" },
        { value: "3", mark: "active" },
        { value: "1", mark: "active" },
        "2",
        "4",
        "3"
      ]
    },
    {
      caption: "right = 3: добавь arr[3] = 2. windowSum = 8 ≥ 7. Окно [2,3,1,2] длины 4. Сжимайся.",
      line: 6,
      cells: [
        "2",
        { value: "3", mark: "value" },
        { value: "1", mark: "value" },
        { value: "2", mark: "active" },
        "4",
        "3"
      ]
    },
    {
      caption: "Сжимайся: left++, windowSum = 8 - 2 = 6. Теперь < 7, стоп.",
      line: 8,
      cells: [
        "2",
        { value: "3", mark: "active" },
        { value: "1", mark: "active" },
        { value: "2", mark: "active" },
        "4",
        "3"
      ]
    },
    {
      caption: "right = 4: добавь arr[4] = 4. windowSum = 10 ≥ 7. Окно [3,1,2,4] длины 4.",
      line: 6,
      cells: [
        "2",
        { value: "3", mark: "value" },
        { value: "1", mark: "value" },
        { value: "2", mark: "value" },
        { value: "4", mark: "active" },
        "3"
      ]
    },
    {
      caption: "Сжимайся: left++, windowSum = 10 - 3 = 7. Окно [1,2,4] длины 3. minLength = 3.",
      line: 6,
      cells: [
        "2",
        "3",
        { value: "1", mark: "active" },
        { value: "2", mark: "active" },
        { value: "4", mark: "found" },
        "3"
      ]
    },
    {
      caption: "Продолжай сжиматься: left++, windowSum = 7 - 1 = 6. Теперь < 7, стоп.",
      line: 8,
      cells: [
        "2",
        "3",
        "1",
        { value: "2", mark: "active" },
        { value: "4", mark: "found" },
        "3"
      ]
    }
  ]}
/>
</Trace>

<Complexity lang="ru">
Окна фиксированного и переменного размера обрабатывают каждый элемент массива максимум дважды (раз при расширении, раз при сжатии или скользе):

| Задача | Время | Место | Замечания |
|---|---|---|---|
| Макс сумма k подряд (фиксированное) | O(n) | O(1) | Однопроход, обновление O(1) за шаг |
| Мин длина подмассива (переменное) | O(n) | O(1) | left и right двигаются максимум n раз итого |
| Макс строка без повтора | O(n) | O(26) или O(k) | Размер алфавита ограничивает место |

**Почему O(n), а не O(n²)?**

Наивный подход: для каждой начальной позиции left вычисли свойство для всех конечных позиций right. Это O(n²).

Скользящее окно: указатели left и right двигаются максимум n раз каждый (никогда назад, всегда вперёд или стоят). Свойство обновляется за O(1) за движение. Итого: O(n) + O(n) = O(n).

**Почему окно никогда не "раскользывается" назад:**

В окне переменного размера left никогда не двигается назад. Раз элемент покинул окно, сумма окна уменьшится; нам никогда не нужно его добавлять обратно. Эта монотонность гарантирует, что left и right вместе двигаются максимум 2n раз.
</Complexity>

<AlgoPractice
  id="practice-sliding-window-1"
  lessonSlug="04-sliding-window"
  lang="ru"
  problems={[
    {
      kind: "choice",
      prompt: "В окне фиксированного размера длины k, сколько времени требуется обновить сумму окна, когда ты скользишь с позиции i на i+1?",
      choices: [
        "O(k) — ты должен пересчитать сумму всех k элементов",
        "O(1) — убери элемент, что уходит, и добавь элемент, что входит",
        "O(log k) — бинарный поиск в окне",
        "O(n) — зависит от размера массива"
      ],
      answer: 1,
      hint: "Ключевое озарение: ты знаешь старую сумму. Просто убери элемент у левого края и добавь элемент у нового правого края. Две операции, постоянное время."
    },
    {
      kind: "numeric",
      prompt: "На массиве [1, 2, 3, 4, 5], какова максимальная сумма любых 2 подряд идущих элементов?",
      answer: 9,
      hint: "Окна: [1,2]=3, [2,3]=5, [3,4]=7, [4,5]=9. Макс 9."
    },
    {
      kind: "choice",
      prompt: "В окне переменного размера, почему левый указатель никогда не двигается назад (только вперёд или стоит)?",
      choices: [
        "Потому что дизайнер алгоритма так выбрал",
        "Потому что если окно было слишком большим и мы его сжали, добавление ещё элементов справа только сделает его больше, никогда меньше",
        "Потому что массив отсортирован",
        "Потому что мы можем двигать только один указатель за раз"
      ],
      answer: 1,
      hint: "Раз мы решили, что окно невалидно (скажем, сумма слишком большая), сжатие путём движения left делает его валидным. Расширение снова (движение right) может только увеличить сумму дальше, так что окно никогда не станет валидным только путём расширения. left не идёт назад."
    },
    {
      kind: "numeric",
      prompt: "На массиве [2, 3, 1, 2, 4, 3], какова наименьшая длина окна с суммой ≥ 7?",
      answer: 3,
      hint: "Возможные окна: [2,3,1,2]=8 (дл 4), [3,1,2,4]=10 (дл 3), [1,2,4]=7 (дл 3). Минимум 3."
    },
    {
      kind: "choice",
      prompt: "Какое утверждение лучше всего объясняет, почему скользящее окно сводит O(n²) к O(n)?",
      choices: [
        "Окно меньше, чем массив",
        "Каждый элемент добавляется и удаляется максимум раз, так что итого операций O(n), а не O(n²)",
        "Окно скользит быстрее на современных компьютерах",
        "Ты не нужно проверять весь массив"
      ],
      answer: 1,
      hint: "Наивный: n начальных позиций × n конечных позиций = O(n²). Скользящее: добавить и удалить = 2n операций = O(n)."
    }
  ]}
/>

<Inset kind="mistake" lang="ru">
**Забыть сжать окно переменного размера.** Если ты расширяешь правый указатель, но никогда не двигаешь левый, ты будешь добавлять элементы вечно и не будешь записывать валидные окна. Для "наименьший подмассив с суммой ≥ цель" ты должен сжимать (двигать left) как только сумма выполнит условие, чтобы записать минимальное окно.
</Inset>

<Inset kind="edgecase" lang="ru">
**Пустой результат.** Для окон фиксированного размера, если массив имеет меньше k элементов, нет окна — верни null или пустой результат. Для окон переменного размера, если ни одно окно не выполнит условие (скажем, ни один подмассив с суммой ≥ цель), верни 0 или -1 сигнализируя "не найдено." Всегда проверь эти граничные случаи перед возвратом.
</Inset>

<Check lang="ru">
  <Quiz
    id="quiz-sliding-window-1"
    pieceSlug="04-sliding-window"
    lang="ru"
    question="Почему техника скользящего окна так эффективна для задач вроде 'максимальная сумма k подряд идущих элементов'?"
    choices={[
      {
        label: "Потому что она пересчитывает сумму для каждого окна вместо её сохранения",
        misconception: "Это была бы O(n*k), а не O(n). Суть в том, чтобы НЕ пересчитывать."
      },
      {
        label: "Потому что она держит сумму инкрементально — убирая один элемент и добавляя один, оба O(1) операции",
        correct: true
      },
      {
        label: "Потому что окно всегда меньше, чем массив",
        misconception: "Размер окна k фиксирован, но это одно не снижает сложность. Инкрементальное обновление делает."
      },
      {
        label: "Потому что массив должен быть отсортирован",
        misconception: "Скользящее окно работает на неотсортированных массивах. Сортировка не требуется."
      }
    ]}
  />
</Check>

<Recap lang="ru">
**Скользящее окно** движет контигуозный диапазон [left, right] по массиву, обновляя свойство (скажем, сумму) инкрементально по дороге.

Ключевые факты:

1. **Окно фиксированного размера:** Окно всегда длины k. Скользи путём удаления самого левого элемента и добавления следующего справа. Обновляй за O(1) за шаг.

2. **Окно переменного размера:** Расширь правый край, пока условие не нарушится. Сжимай левый край, пока условие не выполнится. Продолжай, пока right не дойдёт до конца.

3. **Почему O(n)?** Свойство окна обновляется за O(1) за шаг (добавить и удалить один элемент). Указатели двигаются максимум n раз каждый (никогда назад). Итого: O(n).

4. **Левый указатель не идёт назад (окно переменного размера).** Раз окно валидно и мы начали сжиматься, расширение снова путём движения right только сделает его больше, никогда меньше. left двигается только вперёд.

5. **Инкрементальное состояние — ключ.** Если знаешь сумму [left, right], сумма [left+1, right] это `sum - arr[left]`. Пересчёт не нужен.

Дальше ты исследуешь хеширование (где окно скользит по набору уникальных элементов) и продвинутые техники окна (макс строка без повтора и другие задачи со строками). Теперь, когда встретишь задачу на лучший подмассив или подстроку по какому-то ограничению, спроси себя: мне нужно проверять каждый возможный диапазон, или я могу сдвигать окно и обновлять за O(1)? Если свойство меняется на краях на один элемент — скользящее окно твой ответ.
</Recap>
