---
concepts:
- in_place
- write_pointer
- swap
- auxiliary_space
estMin: 17
lang: ru
mathPrereqs: []
order: 6
prereqs:
- 03-two-pointers
slug: 06-in-place
sources:
- https://www.geeksforgeeks.org/dsa/in-place-algorithm/
- https://medium.com/@kanika04/leetcode-27-remove-element-the-easiest-pattern-youre-probably-overthinking-25dd2ae86a29
status: ready
summary: 'Изменяй массив без создания нового. Используй O(1) дополнительной памяти,
  перезаписывая ячейки на месте: своп двух элементов, указатель записи для сжатия
  массива или разворот двумя противоположными указателями. На месте экономит память,
  но уничтожает исходный ввод.'
title: Работа на месте
track: algorithms
unit: 02-arrays-strings
---



import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Idea from "~/components/lesson/Idea.astro";
import Code from "~/components/lesson/Code.astro";
import Trace from "~/components/lesson/Trace.astro";
import Complexity from "~/components/lesson/Complexity.astro";
import Check from "~/components/lesson/Check.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import AnnotatedCode from "~/components/algo/AnnotatedCode.astro";
import AlgoTrace from "~/components/algo/AlgoTrace.astro";
import CodeRunner from "~/components/algo/CodeRunner.astro";
import AlgoPractice from "~/components/algo/AlgoPractice.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DiagramFrame from "~/components/diagram/DiagramFrame.astro";

<Hook>
Нужно переставить массив — развернуть его, убрать элементы, сдвинуть нули в конец. Лёгкий путь: построить новый выходной массив, O(n) памяти. Сложный путь: переставить ячейки исходного массива прямо, используя только несколько указателей и временных переменных. O(1) дополнительной памяти. Сложный путь называют модификацией на месте, и это важно, когда память дефицитна или датасет огромен.
</Hook>

<Goal lang="ru">
После урока ты сможешь объяснить, что значит "на месте" (переставка используя O(1) дополнительной памяти), узнавать задачи, что разрешают на-месте решения, кодировать три основных приёма (своп, сжатие указателем записи и разворот двумя противоположными указателями), разобраться в трейдоффе память-сложность и увидеть, почему на-месте алгоритмы уничтожают исходный массив.
</Goal>

<Idea lang="ru">
**Модификация на месте** означает переставку массива перезаписывая его собственные ячейки, используя только O(1) дополнительной памяти (несколько указателей и временных переменных). Альтернатива — построение нового выходного массива — стоит O(n) дополнительной памяти.

**Контраст: наивный против на-месте**

Предположим, хочешь убрать все нули из `[1, 0, 2, 0, 3, 0, 4]` и сжать массив.

*Наивный подход:*
```js
const result = [];
for (const x of arr) {
  if (x !== 0) result.push(x);
}
// result = [1, 2, 3, 4], но мы создали новый массив (O(n) память)
```

*На-месте подход:*
```js
let write = 0;  // Указатель: куда писать следующий сохранённый элемент
for (let i = 0; i < arr.length; i++) {
  if (arr[i] !== 0) {
    arr[write] = arr[i];  // Перезапиши на месте
    write++;
  }
}
// arr[0..write-1] = [1, 2, 3, 4], новый массив не создан (O(1) память)
```

Оба дают одинаковый логический результат; один использует O(n) память, другой O(1).

**Почему на-месте важно?**

- **Большие датасеты:** 1 ГБ массива, выделить ещё 1 ГБ дорого.
- **Лимиты памяти:** Встроенные системы или облачные функции с тугим бюджетом.
- **Сохранение ввода:** Иногда ты *хочешь* сохранить исходный и платишь память. На-месте уничтожает его.

**Три основных на-месте приёма**

**1. Своп**

Обмен двух ячеек напрямую:
```js
[arr[i], arr[j]] = [arr[j], arr[i]];  // Своп в одну строку
```

Применяется в: развороте массива, переставке по чётности (чётные потом нечётные), внутри-месте сортировке.

**2. Приём указателя записи**

Указатель (`write`) отмечает, куда должен идти следующий сохранённый элемент. Указатель сканирования (`i`) движется по массиву. Когда сканирование находит хранимый, перезапиши в позицию write и продвинь write.

```js
let write = 0;
for (let i = 0; i < arr.length; i++) {
  if (condition(arr[i])) {
    arr[write] = arr[i];
    write++;
  }
}
```

Применяется в: удалении дубликатов (из урока 03), удалении целевого значения, фильтрации.

**3. Разворот двумя противоположными указателями**

Используй противоположные концы (из урока 03), своп концов, двигайся внутрь:

```js
let left = 0, right = arr.length - 1;
while (left < right) {
  [arr[left], arr[right]] = [arr[right], arr[left]];
  left++;
  right--;
}
```

Применяется в: развороте массива или строки на месте.

<DiagramFrame label="Разворот массива на месте двумя сходящимися указателями. Один массив из 8 ячеек, индексы от 0 до 7, разворачивается свопом зеркальных позиций: ячейка 0 с ячейкой 7, ячейка 1 с ячейкой 6, ячейка 2 с ячейкой 5, ячейка 3 с ячейкой 4. Четыре дуги над массивом соединяют каждую зеркальную пару; каждая дуга — это своп, обменивающий i-ю и (n-1-i)-ю ячейки на месте. Указатель i стартует у левого конца и движется вправо; указатель n-1-i стартует у правого конца и движется влево; они сходятся к середине, и цикл останавливается, когда они встречаются. Второй массив не выделяется: переставка происходит внутри того же блока ячеек, поэтому дополнительная память O(1)." hue="var(--d-systems)" caption="Разворот на месте: своп i-й и (n-1-i)-й ячеек (зеркальные позиции), затем сдвиг двух указателей внутрь, пока они не встретятся. Всё происходит внутри одного массива — без копии, O(1) доп. память.">
  <svg viewBox="0 0 640 280" width="640" height="280" style="display:block;margin:0 auto;max-width:100%;height:auto;">
    <text x="320" y="22" text-anchor="middle" font-size="12" font-family="var(--font-body)" fill="var(--ink)">{"reverse(arr): своп зеркальных ячеек, указатели сходятся"}</text>
    <path d="M108 150 Q304 40 500 150" fill="none" stroke="var(--hairline-strong)" stroke-width="1.6" stroke-dasharray="5 4" marker-start="url(#dotEnd)" marker-end="url(#dotEnd)" />
    <path d="M164 150 Q304 70 444 150" fill="none" stroke="var(--hairline-strong)" stroke-width="1.6" stroke-dasharray="5 4" marker-start="url(#dotEnd)" marker-end="url(#dotEnd)" />
    <path d="M220 150 Q304 98 388 150" fill="none" stroke="var(--hairline-strong)" stroke-width="1.6" stroke-dasharray="5 4" marker-start="url(#dotEnd)" marker-end="url(#dotEnd)" />
    <path d="M276 150 Q304 120 332 150" fill="none" stroke="var(--d-systems)" stroke-width="2.2" marker-start="url(#dotHot)" marker-end="url(#dotHot)" />
    <text x="304" y="112" text-anchor="middle" font-size="9" font-family="var(--font-body)" fill="var(--d-systems)">{"своп i <-> n-1-i"}</text>
    <rect x="80" y="150" width="56" height="46" fill="var(--card-2)" stroke="var(--d-systems)" stroke-width="1.6" />
    <rect x="136" y="150" width="56" height="46" fill="var(--card-2)" stroke="var(--hairline-strong)" stroke-width="1.2" />
    <rect x="192" y="150" width="56" height="46" fill="var(--card-2)" stroke="var(--hairline-strong)" stroke-width="1.2" />
    <rect x="248" y="150" width="56" height="46" fill="var(--card-2)" stroke="var(--hairline-strong)" stroke-width="1.2" />
    <rect x="304" y="150" width="56" height="46" fill="var(--card-2)" stroke="var(--hairline-strong)" stroke-width="1.2" />
    <rect x="360" y="150" width="56" height="46" fill="var(--card-2)" stroke="var(--hairline-strong)" stroke-width="1.2" />
    <rect x="416" y="150" width="56" height="46" fill="var(--card-2)" stroke="var(--hairline-strong)" stroke-width="1.2" />
    <rect x="472" y="150" width="56" height="46" fill="var(--card-2)" stroke="var(--d-systems)" stroke-width="1.6" />
    <text x="108" y="179" text-anchor="middle" font-size="14" font-family="var(--font-body)" fill="var(--ink)">1</text>
    <text x="164" y="179" text-anchor="middle" font-size="14" font-family="var(--font-body)" fill="var(--ink)">2</text>
    <text x="220" y="179" text-anchor="middle" font-size="14" font-family="var(--font-body)" fill="var(--ink)">3</text>
    <text x="276" y="179" text-anchor="middle" font-size="14" font-family="var(--font-body)" fill="var(--ink)">4</text>
    <text x="332" y="179" text-anchor="middle" font-size="14" font-family="var(--font-body)" fill="var(--ink)">5</text>
    <text x="388" y="179" text-anchor="middle" font-size="14" font-family="var(--font-body)" fill="var(--ink)">6</text>
    <text x="444" y="179" text-anchor="middle" font-size="14" font-family="var(--font-body)" fill="var(--ink)">7</text>
    <text x="500" y="179" text-anchor="middle" font-size="14" font-family="var(--font-body)" fill="var(--ink)">8</text>
    <text x="108" y="212" text-anchor="middle" font-size="9" font-family="var(--font-body)" fill="var(--muted)">0</text>
    <text x="164" y="212" text-anchor="middle" font-size="9" font-family="var(--font-body)" fill="var(--muted)">1</text>
    <text x="220" y="212" text-anchor="middle" font-size="9" font-family="var(--font-body)" fill="var(--muted)">2</text>
    <text x="276" y="212" text-anchor="middle" font-size="9" font-family="var(--font-body)" fill="var(--muted)">3</text>
    <text x="332" y="212" text-anchor="middle" font-size="9" font-family="var(--font-body)" fill="var(--muted)">4</text>
    <text x="388" y="212" text-anchor="middle" font-size="9" font-family="var(--font-body)" fill="var(--muted)">5</text>
    <text x="444" y="212" text-anchor="middle" font-size="9" font-family="var(--font-body)" fill="var(--muted)">6</text>
    <text x="500" y="212" text-anchor="middle" font-size="9" font-family="var(--font-body)" fill="var(--muted)">7</text>
    <text x="80" y="244" text-anchor="start" font-size="11" font-family="var(--font-body)" fill="var(--d-systems)">{"i"}</text>
    <line x1="92" y1="240" x2="156" y2="240" stroke="var(--d-systems)" stroke-width="1.6" marker-end="url(#arrowR)" />
    <text x="528" y="244" text-anchor="end" font-size="11" font-family="var(--font-body)" fill="var(--d-systems)">{"n-1-i"}</text>
    <line x1="516" y1="240" x2="452" y2="240" stroke="var(--d-systems)" stroke-width="1.6" marker-end="url(#arrowL)" />
    <text x="304" y="244" text-anchor="middle" font-size="9" font-family="var(--font-body)" fill="var(--muted)">{"сходятся"}</text>
    <text x="320" y="270" text-anchor="middle" font-size="10" font-family="var(--font-body)" fill="var(--muted)">{"тот же массив — новый массив не выделяется — O(1) доп. память"}</text>
    <defs>
      <marker id="arrowR" markerWidth="8" markerHeight="8" refX="6" refY="4" orient="auto"><path d="M1 1 L6 4 L1 7" fill="none" stroke="var(--d-systems)" stroke-width="1.6" /></marker>
      <marker id="arrowL" markerWidth="8" markerHeight="8" refX="6" refY="4" orient="auto"><path d="M1 1 L6 4 L1 7" fill="none" stroke="var(--d-systems)" stroke-width="1.6" /></marker>
      <marker id="dotEnd" markerWidth="7" markerHeight="7" refX="3" refY="3" orient="auto"><circle cx="3" cy="3" r="2.4" fill="var(--hairline-strong)" /></marker>
      <marker id="dotHot" markerWidth="7" markerHeight="7" refX="3" refY="3" orient="auto"><circle cx="3" cy="3" r="2.6" fill="var(--d-systems)" /></marker>
    </defs>
  </svg>
</DiagramFrame>

**Цена: на-месте уничтожает ввод**

На-месте алгоритм **изменяет исходный массив**. Если нужны и исходный и результат, скопируй до вызова алгоритма. Это трейдофф: O(1) дополнительная память ценой уничтожающегося ввода.
</Idea>

<Code lang="ru">
Запрограммируем три классические на-месте задачи.

**1. Развернуть массив на месте (своп двумя противоположными указателями):**

```js
function reverseInPlace(arr) {
  let left = 0, right = arr.length - 1;
  while (left < right) {
    [arr[left], arr[right]] = [arr[right], arr[left]];
    left++;
    right--;
  }
}
```

**2. Убрать целевое значение на месте (указатель записи):**

```js
function removeValue(arr, val) {
  let write = 0;
  for (let i = 0; i < arr.length; i++) {
    if (arr[i] !== val) {
      arr[write] = arr[i];
      write++;
    }
  }
  return write;  // Новая длина (элементы что сохранить)
}
```

**3. Сдвинуть все нули в конец на месте (указатель записи):**

```js
function moveZeros(arr) {
  let write = 0;
  // Первый проход: сожми ненулевые в начало
  for (let i = 0; i < arr.length; i++) {
    if (arr[i] !== 0) {
      arr[write] = arr[i];
      write++;
    }
  }
  // Второй проход: заполни остальное нулями
  while (write < arr.length) {
    arr[write] = 0;
    write++;
  }
}
```

Запусти эти функции:

<CodeRunner
  id="cr-in-place"
  lang="ru"
  code={`function reverseInPlace(arr) {
  let left = 0, right = arr.length - 1;
  while (left < right) {
    [arr[left], arr[right]] = [arr[right], arr[left]];
    left++;
    right--;
  }
}

function removeValue(arr, val) {
  let write = 0;
  for (let i = 0; i < arr.length; i++) {
    if (arr[i] !== val) {
      arr[write] = arr[i];
      write++;
    }
  }
  return write;
}

function moveZeros(arr) {
  let write = 0;
  for (let i = 0; i < arr.length; i++) {
    if (arr[i] !== 0) {
      arr[write] = arr[i];
      write++;
    }
  }
  while (write < arr.length) {
    arr[write] = 0;
    write++;
  }
}

const arr1 = [1, 2, 3, 4, 5];
reverseInPlace(arr1);
console.log("Развёрнуто:", arr1);

const arr2 = [1, 0, 2, 0, 3, 0, 4];
const len = removeValue(arr2, 0);
console.log("После убрать 0:", arr2.slice(0, len));

const arr3 = [1, 0, 2, 0, 3, 0, 4];
moveZeros(arr3);
console.log("Нули сдвинуты:", arr3);`}
/>
</Code>

<Trace lang="ru">
Проследим **убрать значение** на `[1, 0, 2, 0, 3, 0, 4]` убирая 0:

<AlgoTrace
  id="trace-remove-value"
  lang="ru"
  code={[
    "function removeValue(arr, val) {",
    "  let write = 0;",
    "  for (let i = 0; i < arr.length; i++) {",
    "    if (arr[i] !== val) {",
    "      arr[write] = arr[i];",
    "      write++;",
    "    }",
    "  }",
    "  return write;",
    "}"
  ]}
  frames={[
    {
      caption: "Старт: arr = [1, 0, 2, 0, 3, 0, 4], val = 0. write = 0, i = 0.",
      line: 1,
      cells: [
        { value: "1", mark: "active" },
        "0",
        "2",
        "0",
        "3",
        "0",
        "4"
      ]
    },
    {
      caption: "i = 0: arr[0] = 1 ≠ 0. Сохрани. arr[0] = 1. write = 1.",
      line: 5,
      cells: [
        { value: "1", mark: "done" },
        "0",
        "2",
        "0",
        "3",
        "0",
        "4"
      ]
    },
    {
      caption: "i = 1: arr[1] = 0 = 0. Пропусти. Ко i = 2.",
      line: 4,
      cells: [
        "1",
        { value: "0", mark: "active" },
        "2",
        "0",
        "3",
        "0",
        "4"
      ]
    },
    {
      caption: "i = 2: arr[2] = 2 ≠ 0. Сохрани. arr[1] = 2. write = 2.",
      line: 5,
      cells: [
        "1",
        { value: "2", mark: "done" },
        "2",
        "0",
        "3",
        "0",
        "4"
      ]
    },
    {
      caption: "i = 3: arr[3] = 0 = 0. Пропусти. Ко i = 4.",
      line: 4,
      cells: [
        "1",
        "2",
        { value: "2", mark: "active" },
        "0",
        "3",
        "0",
        "4"
      ]
    },
    {
      caption: "i = 4: arr[4] = 3 ≠ 0. Сохрани. arr[2] = 3. write = 3.",
      line: 5,
      cells: [
        "1",
        "2",
        { value: "3", mark: "done" },
        "0",
        "3",
        "0",
        "4"
      ]
    },
    {
      caption: "i = 5: arr[5] = 0 = 0. Пропусти. Ко i = 6.",
      line: 4,
      cells: [
        "1",
        "2",
        "3",
        { value: "0", mark: "active" },
        "3",
        "0",
        "4"
      ]
    },
    {
      caption: "i = 6: arr[6] = 4 ≠ 0. Сохрани. arr[3] = 4. write = 4. Верни 4.",
      line: 5,
      cells: [
        "1",
        "2",
        "3",
        { value: "4", mark: "done" },
        "3",
        "0",
        "4"
      ]
    },
    {
      caption: "Финал: arr[0..3] = [1, 2, 3, 4] (сохранённые элементы). Длина = 4. O(1) памяти.",
      line: 9,
      cells: [
        { value: "1", mark: "done" },
        { value: "2", mark: "done" },
        { value: "3", mark: "done" },
        { value: "4", mark: "done" },
        "3",
        "0",
        "4"
      ]
    }
  ]}
/>

Теперь проследим **разворот на месте** на `[1, 2, 3, 4, 5]`:

<AlgoTrace
  id="trace-reverse"
  lang="ru"
  code={[
    "function reverseInPlace(arr) {",
    "  let left = 0, right = arr.length - 1;",
    "  while (left < right) {",
    "    [arr[left], arr[right]] = [arr[right], arr[left]];",
    "    left++;",
    "    right--;",
    "  }",
    "}"
  ]}
  frames={[
    {
      caption: "Старт: arr = [1, 2, 3, 4, 5]. left = 0, right = 4.",
      line: 1,
      cells: [
        { value: "1", mark: "active" },
        "2",
        "3",
        "4",
        { value: "5", mark: "active" }
      ]
    },
    {
      caption: "Своп arr[0] и arr[4]: [5, 2, 3, 4, 1]. left = 1, right = 3.",
      line: 4,
      cells: [
        { value: "5", mark: "done" },
        "2",
        "3",
        "4",
        { value: "1", mark: "done" }
      ]
    },
    {
      caption: "left = 1, right = 3. Своп arr[1] и arr[3]: [5, 4, 3, 2, 1]. left = 2, right = 2.",
      line: 4,
      cells: [
        "5",
        { value: "4", mark: "done" },
        "3",
        { value: "2", mark: "done" },
        "1"
      ]
    },
    {
      caption: "left = 2, right = 2. Условие цикла ложно (2 < 2 ложь). Стоп.",
      line: 3,
      cells: [
        "5",
        "4",
        { value: "3", mark: "done" },
        "2",
        "1"
      ]
    },
    {
      caption: "Финал: arr = [5, 4, 3, 2, 1]. Развёрнут на месте с O(1) памятью.",
      line: 8,
      cells: [
        { value: "5", mark: "done" },
        { value: "4", mark: "done" },
        { value: "3", mark: "done" },
        { value: "2", mark: "done" },
        { value: "1", mark: "done" }
      ]
    }
  ]}
/>
</Trace>

<Complexity lang="ru">
| Задача | Время | Память (ввод) | Память (дополнительно) | Заметки |
|---|---|---|---|---|
| Развернуть массив на месте | O(n) | O(n) | O(1) | Два указателя своп; только left и right переменные |
| Убрать значение на месте | O(n) | O(n) | O(1) | Указатель записи + указатель сканирования; новый массив нет |
| Сдвинуть нули в конец на месте | O(n) | O(n) | O(1) | Два прохода: сжатие, потом заполнение |
| Убрать значение (построить новый массив) | O(n) | O(n) | O(n) | Создаёт новый выходной массив (не на-месте) |

**Почему это лучше наивного подхода?**

Наивное удаление массива строит свежий массив (O(n) дополнительная память). Удаление на-месте перезаписывает исходный, используя только указатель записи (O(1) дополнительная память).

Для маленьких массивов разница ничтожна. Для 1 ГБ массива выделить ещё 1 ГБ запрещённо.

**Трейдофф память-время:**

- **На-месте:** O(1) дополнительная память, но уничтожает ввод.
- **Наивный (построить новый):** O(n) дополнительная память, но ввод безопасен.

Выбирай по тому, можешь ли позволить потерять исходный.
</Complexity>

<AlgoPractice
  id="practice-in-place-1"
  lessonSlug="06-in-place"
  lang="ru"
  problems={[
    {
      kind: "choice",
      prompt: "Почему на-месте алгоритм убрать значение использует указатель записи?",
      choices: [
        "Чтобы считать сколько элементов было убрано",
        "Чтобы отметить куда должен идти следующий сохранённый элемент, сжимая массив без построения нового",
        "Чтобы измерить сколько памяти мы сэкономили",
        "Чтобы помнить исходное значение до перезаписи"
      ],
      answer: 1,
      hint: "Указатель записи отслеживает 'границу' куда копируются сохранённые элементы. Он растёт только когда находим элемент стоящий сохранения."
    },
    {
      kind: "choice",
      prompt: "Какова дополнительная память сложность разворота массива на месте двумя указателями?",
      choices: [
        "O(n) потому что массив сам O(n)",
        "O(log n) потому что указатели используют стек памяти",
        "O(1) потому что используем только два указателя (left и right) независимо от размера массива",
        "O(n/2) потому что разворачиваем только половину массива"
      ],
      answer: 2,
      hint: "Дополнительная память означает память вне ввода. Используем только left и right переменные, не новый массив."
    },
    {
      kind: "numeric",
      prompt: "После вызова removeValue([1, 0, 2, 0, 3], 0), какую длину возвращает функция?",
      answer: 3,
      hint: "Сохранённые элементы: 1, 2, 3. Функция возвращает финальную позицию указателя записи, что есть 3."
    },
    {
      kind: "choice",
      prompt: "Когда запускаешь на-месте алгоритм, что происходит с исходным массивом?",
      choices: [
        "Он копируется в резервное хранилище до старта алгоритма",
        "Он только-читаемый и никогда не изменяется",
        "Он изменяется на месте; исходные данные перезаписываются",
        "Он обменивается на новый массив после финиша алгоритма"
      ],
      answer: 2,
      hint: "На-месте означает алгоритм переставляет исходные ячейки. Если нужен исходный, скопируй первым."
    },
    {
      kind: "choice",
      prompt: "Приём указателя записи наиболее полезен когда:",
      choices: [
        "Массив уже отсортирован",
        "Ты хочешь сохранить исходный массив",
        "Тебе нужно фильтровать или сжимать массив и экономить память",
        "Массив содержит только положительные числа"
      ],
      answer: 2,
      hint: "Указатель записи блистает когда нужна экономия памяти (O(1) против O(n)) и не возражаешь уничтожить ввод."
    }
  ]}
/>

<Inset kind="mistake" lang="ru">
**Забыть что на-месте алгоритмы уничтожают ввод.** Ты вызываешь `removeValue(myArray, 5)` думая что позже можешь ещё использовать `myArray`. Но алгоритм изменяет `myArray` напрямую. Если нужен был исходный, надо копировать сначала: `const backup = [...myArray]; removeValue(myArray, 5);`. Это частая ошибка в интервью и боевом коде.
</Inset>

<Inset kind="edgecase" lang="ru">
**Пустые или однородные массивы.** Если массив пуст, цикл не запускается и write остаётся 0 (или функция возвращает 0). Если массив одного элемента и разворачиваешь или убираешь значение, указатели встречаются или проходят мимо сразу, массив неизменен. Оба случая обрабатываются верно.
</Inset>

<Check lang="ru">
  <Quiz
    id="quiz-in-place-1"
    pieceSlug="06-in-place"
    lang="ru"
    question="Главное преимущество на-месте алгоритма в сравнении с построением нового выходного массива?"
    choices={[
      {
        label: "Он быстрее потому что свопы дешевле выделения массива",
        misconception: "Скорость варьируется. Реальное преимущество память: O(1) дополнительная вместо O(n)."
      },
      {
        label: "Он использует O(1) дополнительную память вместо O(n), что важно для больших датасетов или систем с ограниченной памятью",
        correct: true
      },
      {
        label: "Он сохраняет исходный массив чтобы потом его использовать",
        misconception: "На-месте алгоритмы уничтожают ввод. Если хочешь сохранить исходный, скопируй сначала."
      },
      {
        label: "Он работает на отсортированных и неотсортированных массивах одинаково",
        misconception: "Работает ли алгоритм на отсортированных или неотсортированных данных зависит от алгоритма, не от того что на-месте."
      }
    ]}
  />
</Check>

<Recap lang="ru">
**Модификация на месте** переставляет массив без построения нового, используя только O(1) дополнительную память (несколько указателей и переменных).

Ключевые факты:

1. **Своп:** Прямой обмен ячеек используя `[a, b] = [b, a]` (или временную переменную). Применяется в разворотах и переставках.

2. **Приём указателя записи:** Указатель отмечает куда писать следующий сохранённый элемент. Указатель сканирования движется по массиву. Когда сканирование находит хранимый, пиши его в позицию write и продвинь write. Применяется к фильтрации, удалению дубликатов и сжатию.

3. **Разворот двумя противоположными указателями:** Противоположные концы своп и движение внутрь. O(n) время, O(1) дополнительная память.

4. **Трейдофф:** На-месте экономит память но уничтожает исходный массив. Если нужны оба, скопируй сначала.

5. **Когда это важно:** Большие датасеты, лимиты памяти, встроенные системы. Для маленьких массивов экономия памяти ничтожна.

Дальше увидишь идеи на-месте расширенные на связные списки (где структура уже "на месте") и на алгоритмы сортировки что обещают O(log n) или O(1) дополнительную память. Теперь, когда увидишь алгоритм, что выделяет новый массив такого же размера, что и вход, спроси себя: может ли указатель записи сжать это на месте? Если да — ты только что сократил потребление памяти вдвое.
</Recap>
