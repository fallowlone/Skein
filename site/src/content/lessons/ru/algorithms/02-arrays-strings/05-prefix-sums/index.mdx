---
concepts:
- prefix_sum
- prefix_array
- range_query
- precomputation
estMin: 18
lang: ru
mathPrereqs: []
order: 5
prereqs:
- 02-traversal
slug: 05-prefix-sums
sources:
- https://usaco.guide/silver/prefix-sums
- https://www.geeksforgeeks.org/dsa/understanding-prefix-sums/
status: ready
summary: Предвычисли префиксный массив раз. Потом любой запрос суммы диапазона стоит
  O(1) вычитанием вместо O(n) проходом. Меняем O(n) памяти на мгновенные ответы.
title: Префиксные суммы
track: algorithms
unit: 02-arrays-strings
---



import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Idea from "~/components/lesson/Idea.astro";
import Code from "~/components/lesson/Code.astro";
import Trace from "~/components/lesson/Trace.astro";
import Complexity from "~/components/lesson/Complexity.astro";
import Check from "~/components/lesson/Check.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import AnnotatedCode from "~/components/algo/AnnotatedCode.astro";
import AlgoTrace from "~/components/algo/AlgoTrace.astro";
import CodeRunner from "~/components/algo/CodeRunner.astro";
import AlgoPractice from "~/components/algo/AlgoPractice.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DiagramFrame from "~/components/diagram/DiagramFrame.astro";

<Hook>
У тебя есть массив чисел и поток вопросов: «Какая сумма элементов от индекса 3 до 7?» потом «Какая сумма от индекса 1 до 4?» потом «От 5 до 9?» Считать каждую сумму проходом подмассива стоит O(n) за запрос. Но что если ты предвычислишь накопленную сумму на каждой позиции? Тогда ты ответишь на каждый запрос за O(1) вычитанием. Ты платишь O(n) времени раз, чтобы собрать префиксный массив, и потом получишь O(1) за запрос навечно. Это приём префиксных сумм.
</Hook>

<Goal lang="ru">
После этого урока ты сможешь узнавать, когда задача на диапазонные запросы выигрывает от префиксных сумм, собирать префиксный массив за O(n) времени, отвечать на запросы сумм диапазона за O(1) вычитанием, разобраться в уходе за off-by-one (используя префиксный массив длины n+1), и видеть, как та же идея обобщается на префиксные счётчики и 2D сетки.
</Goal>

<Idea lang="ru">
**Префиксная сумма** — техника предвычисления (precomputation), где ты собираешь массив, что хранит накопленную сумму от начала массива до каждого индекса.

Спроси себя: когда тебе вообще может понадобиться ответить на сотню запросов о суммах диапазонов на одном массиве? Аналитические дашборды, агрегация временных рядов, спортивное программирование — все эти задачи упираются в этот приём. Без префиксных сумм каждый запрос повторяет одинаковый проход. С ними — платишь один раз и отвечаешь вечно.

**Задача без префиксных сумм:**

Дан массив `arr = [2, 5, 1, 3, 4]`, ты получаешь много запросов на суммы диапазонов. Например:
- Сумма от индекса 1 до 3? Ты проходишь и считаешь arr[1] + arr[2] + arr[3] = 5 + 1 + 3 = 9.
- Сумма от индекса 0 до 2? Ты проходишь и считаешь arr[0] + arr[1] + arr[2] = 2 + 5 + 1 = 8.

Каждый запрос стоит O(n) в худшем случае. С q запросами итого O(n × q). Это масштабируется плохо.

**Идея: предвычисли накопленные суммы**

Собери префиксный массив `prefix` длины n+1 (не n, а n+1!), где:
- `prefix[0] = 0` (сумма нулевых элементов)
- `prefix[1] = arr[0]` (сумма первых 1 элемента)
- `prefix[2] = arr[0] + arr[1]` (сумма первых 2 элементов)
- `prefix[k] = arr[0] + arr[1] + ... + arr[k-1]` (сумма первых k элементов)

Для `arr = [2, 5, 1, 3, 4]` префиксный массив это:
```
prefix = [0, 2, 7, 8, 11, 15]
```

Теперь, чтобы найти сумму элементов от индекса i до j (включительно), используй эту формулу:
```
sum(i, j) = prefix[j+1] - prefix[i]
```

Почему? Потому что:
- `prefix[j+1]` = сумма первых j+1 элементов = arr[0] + ... + arr[j]
- `prefix[i]` = сумма первых i элементов = arr[0] + ... + arr[i-1]
- Вычитаем: arr[i] + arr[i+1] + ... + arr[j]

**Пример:**
- Сумма от индекса 1 до 3: `prefix[4] - prefix[1] = 11 - 2 = 9` ✓
- Сумма от индекса 0 до 2: `prefix[3] - prefix[0] = 8 - 0 = 8` ✓

**Уход за off-by-one:**

Ключ в том, чтобы использовать префиксный массив длины n+1 (не n). Так `prefix[i]` всегда представляет сумму первых i элементов, и формула `sum(i, j) = prefix[j+1] - prefix[i]` работает чисто на всех краях без особых случаев.

**Обмен времени и памяти:**

- Подготовка: O(n) времени на сборку префиксного массива.
- Память: O(n) дополнительной памяти на префиксный массив.
- За запрос: O(1) времени.
- Итого для q запросов: O(n + q) вместо O(n × q).

Это классический приём предвычисления: потратишь время и память один раз заранее, потом переиспользуешь результат много раз даром. Ты видел похожую идею в two-pointer и sliding window (двигаешься инкрементально, чтобы не пересчитывать). Здесь ты предвычисляешь, чтобы превратить повторную работу в мгновенные поиски.

<DiagramFrame label="Префиксные суммы: исходный массив arr = [2, 5, 1, 3, 4] нарисован сверху как пять ячеек с индексами от 0 до 4. Под ним префиксный массив длины шесть, индексы от 0 до 5, со значениями 0, 2, 7, 8, 11, 15. prefix[0] = 0 закрепляет массив, а каждая следующая префиксная ячейка это накопленная сумма: prefix[i] = prefix[i-1] + arr[i-1]. Пунктирные стрелки показывают, как каждый элемент массива стекает вниз в префиксную ячейку, которую он продолжает: 2 попадает в prefix[1], +5 даёт prefix[2] = 7, +1 даёт prefix[3] = 8, +3 даёт prefix[4] = 11, +4 даёт prefix[5] = 15. Один запрос суммы диапазона выделен: сумма arr от индекса 1 до 3 равна prefix[4] минус prefix[1], то есть 11 минус 2 = 9, вычислено за O(1) вычитанием двух префиксных ячеек вместо сложения подмассива." hue="var(--d-systems)" caption="Собери раз, запрашивай вечно. Префиксный массив хранит накопленную сумму: prefix[i] = prefix[i-1] + arr[i-1], так что prefix[k] хранит сумму первых k элементов. Тогда любая сумма диапазона это одно вычитание за O(1): сумма arr[l..r] = prefix[r+1] - prefix[l]. Здесь sum(1..3) = prefix[4] - prefix[1] = 11 - 2 = 9 = arr[1] + arr[2] + arr[3].">
  <svg viewBox="0 0 720 380" width="720" height="380" style="display:block;margin:0 auto;max-width:100%;height:auto;">
    <text x="360" y="28" text-anchor="middle" font-size="13" font-family="var(--font-body)" fill="var(--ink)">{"prefix[i] = prefix[i-1] + arr[i-1]"}  —  накопленная сумма раз, суммы диапазонов вечно</text>
    <text x="64" y="92" text-anchor="start" font-size="12" font-family="var(--font-body)" fill="var(--muted)">{"arr"}</text>
    <rect x="120" y="64" width="80" height="56" rx="6" fill="var(--card-2)" stroke="var(--hairline-strong)" stroke-width="1.2" />
    <text x="160" y="98" text-anchor="middle" font-size="17" font-family="var(--font-body)" fill="var(--ink)">2</text>
    <text x="160" y="138" text-anchor="middle" font-size="11" font-family="var(--font-body)" fill="var(--muted)">{"arr[0]"}</text>
    <rect x="200" y="64" width="80" height="56" rx="6" fill="var(--card-2)" stroke="var(--hairline-strong)" stroke-width="1.2" />
    <text x="240" y="98" text-anchor="middle" font-size="17" font-family="var(--font-body)" fill="var(--ink)">5</text>
    <text x="240" y="138" text-anchor="middle" font-size="11" font-family="var(--font-body)" fill="var(--muted)">{"arr[1]"}</text>
    <rect x="280" y="64" width="80" height="56" rx="6" fill="var(--card-2)" stroke="var(--hairline-strong)" stroke-width="1.2" />
    <text x="320" y="98" text-anchor="middle" font-size="17" font-family="var(--font-body)" fill="var(--ink)">1</text>
    <text x="320" y="138" text-anchor="middle" font-size="11" font-family="var(--font-body)" fill="var(--muted)">{"arr[2]"}</text>
    <rect x="360" y="64" width="80" height="56" rx="6" fill="var(--card-2)" stroke="var(--hairline-strong)" stroke-width="1.2" />
    <text x="400" y="98" text-anchor="middle" font-size="17" font-family="var(--font-body)" fill="var(--ink)">3</text>
    <text x="400" y="138" text-anchor="middle" font-size="11" font-family="var(--font-body)" fill="var(--muted)">{"arr[3]"}</text>
    <rect x="440" y="64" width="80" height="56" rx="6" fill="var(--card-2)" stroke="var(--hairline-strong)" stroke-width="1.2" />
    <text x="480" y="98" text-anchor="middle" font-size="17" font-family="var(--font-body)" fill="var(--ink)">4</text>
    <text x="480" y="138" text-anchor="middle" font-size="11" font-family="var(--font-body)" fill="var(--muted)">{"arr[4]"}</text>
    <path d="M160 120 Q200 185 240 250" fill="none" stroke="var(--d-systems)" stroke-width="1.8" stroke-dasharray="5 4" marker-end="url(#psFlow)" />
    <path d="M240 120 Q280 185 320 250" fill="none" stroke="var(--d-systems)" stroke-width="1.8" stroke-dasharray="5 4" marker-end="url(#psFlow)" />
    <path d="M320 120 Q360 185 400 250" fill="none" stroke="var(--d-systems)" stroke-width="1.8" stroke-dasharray="5 4" marker-end="url(#psFlow)" />
    <path d="M400 120 Q440 185 480 250" fill="none" stroke="var(--d-systems)" stroke-width="1.8" stroke-dasharray="5 4" marker-end="url(#psFlow)" />
    <path d="M480 120 Q520 185 560 250" fill="none" stroke="var(--d-systems)" stroke-width="1.8" stroke-dasharray="5 4" marker-end="url(#psFlow)" />
    <text x="206" y="200" text-anchor="middle" font-size="11" font-family="var(--font-body)" fill="var(--d-systems)">{"+2"}</text>
    <text x="286" y="200" text-anchor="middle" font-size="11" font-family="var(--font-body)" fill="var(--d-systems)">{"+5"}</text>
    <text x="366" y="200" text-anchor="middle" font-size="11" font-family="var(--font-body)" fill="var(--d-systems)">{"+1"}</text>
    <text x="446" y="200" text-anchor="middle" font-size="11" font-family="var(--font-body)" fill="var(--d-systems)">{"+3"}</text>
    <text x="526" y="200" text-anchor="middle" font-size="11" font-family="var(--font-body)" fill="var(--d-systems)">{"+4"}</text>
    <text x="64" y="278" text-anchor="start" font-size="12" font-family="var(--font-body)" fill="var(--muted)">{"prefix"}</text>
    <rect x="120" y="250" width="80" height="56" rx="6" fill="var(--card-2)" stroke="var(--hairline-strong)" stroke-width="1.2" />
    <text x="160" y="284" text-anchor="middle" font-size="17" font-family="var(--font-body)" fill="var(--ink)">0</text>
    <text x="160" y="324" text-anchor="middle" font-size="11" font-family="var(--font-body)" fill="var(--muted)">{"prefix[0]"}</text>
    <rect x="200" y="250" width="80" height="56" rx="6" fill="var(--accent-ghost)" stroke="var(--d-systems)" stroke-width="2.4" />
    <text x="240" y="284" text-anchor="middle" font-size="17" font-family="var(--font-body)" fill="var(--ink)">2</text>
    <text x="240" y="324" text-anchor="middle" font-size="11" font-family="var(--font-body)" fill="var(--d-systems)">{"prefix[1]"}</text>
    <rect x="280" y="250" width="80" height="56" rx="6" fill="var(--card-2)" stroke="var(--hairline-strong)" stroke-width="1.2" />
    <text x="320" y="284" text-anchor="middle" font-size="17" font-family="var(--font-body)" fill="var(--ink)">7</text>
    <text x="320" y="324" text-anchor="middle" font-size="11" font-family="var(--font-body)" fill="var(--muted)">{"prefix[2]"}</text>
    <rect x="360" y="250" width="80" height="56" rx="6" fill="var(--card-2)" stroke="var(--hairline-strong)" stroke-width="1.2" />
    <text x="400" y="284" text-anchor="middle" font-size="17" font-family="var(--font-body)" fill="var(--ink)">8</text>
    <text x="400" y="324" text-anchor="middle" font-size="11" font-family="var(--font-body)" fill="var(--muted)">{"prefix[3]"}</text>
    <rect x="440" y="250" width="80" height="56" rx="6" fill="var(--accent-ghost)" stroke="var(--d-systems)" stroke-width="2.4" />
    <text x="480" y="284" text-anchor="middle" font-size="17" font-family="var(--font-body)" fill="var(--ink)">11</text>
    <text x="480" y="324" text-anchor="middle" font-size="11" font-family="var(--font-body)" fill="var(--d-systems)">{"prefix[4]"}</text>
    <rect x="520" y="250" width="80" height="56" rx="6" fill="var(--card-2)" stroke="var(--hairline-strong)" stroke-width="1.2" />
    <text x="560" y="284" text-anchor="middle" font-size="17" font-family="var(--font-body)" fill="var(--ink)">15</text>
    <text x="560" y="324" text-anchor="middle" font-size="11" font-family="var(--font-body)" fill="var(--muted)">{"prefix[5]"}</text>
    <text x="360" y="360" text-anchor="middle" font-size="13" font-family="var(--font-body)" fill="var(--ink)">{"sum(1..3) = prefix[4] - prefix[1] = 11 - 2 = 9"}</text>
    <defs>
      <marker id="psFlow" markerWidth="9" markerHeight="9" refX="6" refY="4" orient="auto"><path d="M1 1 L7 4 L1 7" fill="none" stroke="var(--d-systems)" stroke-width="1.6" /></marker>
    </defs>
  </svg>
</DiagramFrame>
</Idea>

<Code lang="ru">
Давай кодировать приём префиксных сумм.

**1. Собери префиксный массив:**

```js
function buildPrefix(arr) {
  const n = arr.length;
  const prefix = new Array(n + 1).fill(0);
  
  for (let i = 0; i < n; i++) {
    prefix[i + 1] = prefix[i] + arr[i];
  }
  
  return prefix;
}
```

**2. Отвечай на запросы сумм диапазонов:**

```js
function rangeSum(prefix, i, j) {
  // Сумма элементов от индекса i до j (включительно)
  return prefix[j + 1] - prefix[i];
}
```

**Полный пример:**

```js
const arr = [2, 5, 1, 3, 4];
const prefix = buildPrefix(arr);

console.log(prefix);  // [0, 2, 7, 8, 11, 15]
console.log(rangeSum(prefix, 1, 3));  // 5 + 1 + 3 = 9
console.log(rangeSum(prefix, 0, 2));  // 2 + 5 + 1 = 8
console.log(rangeSum(prefix, 0, 4));  // 2 + 5 + 1 + 3 + 4 = 15
console.log(rangeSum(prefix, 2, 4));  // 1 + 3 + 4 = 8
```

Попробуй запустить это:

<CodeRunner
  id="cr-prefix-sums"
  lang="ru"
  code={`function buildPrefix(arr) {
  const n = arr.length;
  const prefix = new Array(n + 1).fill(0);
  
  for (let i = 0; i < n; i++) {
    prefix[i + 1] = prefix[i] + arr[i];
  }
  
  return prefix;
}

function rangeSum(prefix, i, j) {
  return prefix[j + 1] - prefix[i];
}

const arr = [2, 5, 1, 3, 4];
const prefix = buildPrefix(arr);

console.log("Префиксный массив:", prefix);
console.log("Sum(1, 3):", rangeSum(prefix, 1, 3));
console.log("Sum(0, 2):", rangeSum(prefix, 0, 2));
console.log("Sum(0, 4):", rangeSum(prefix, 0, 4));
console.log("Sum(2, 4):", rangeSum(prefix, 2, 4));`}
/>
</Code>

<Trace lang="ru">
Давай трассировать сборку префиксного массива для `[2, 5, 1, 3, 4]`:

<AlgoTrace
  id="trace-prefix-build"
  lang="ru"
  code={[
    "function buildPrefix(arr) {",
    "  const prefix = new Array(arr.length + 1).fill(0);",
    "  for (let i = 0; i < arr.length; i++) {",
    "    prefix[i + 1] = prefix[i] + arr[i];",
    "  }",
    "  return prefix;",
    "}"
  ]}
  frames={[
    {
      caption: "Начало: arr = [2, 5, 1, 3, 4]. Создай префиксный массив длины 6, инициализированный нулями.",
      line: 1,
      cells: [
        { value: "0", mark: "active" },
        "?",
        "?",
        "?",
        "?",
        "?"
      ]
    },
    {
      caption: "i = 0: prefix[1] = prefix[0] + arr[0] = 0 + 2 = 2.",
      line: 4,
      cells: [
        { value: "0", mark: "value" },
        { value: "2", mark: "active" },
        "?",
        "?",
        "?",
        "?"
      ]
    },
    {
      caption: "i = 1: prefix[2] = prefix[1] + arr[1] = 2 + 5 = 7.",
      line: 4,
      cells: [
        "0",
        { value: "2", mark: "value" },
        { value: "7", mark: "active" },
        "?",
        "?",
        "?"
      ]
    },
    {
      caption: "i = 2: prefix[3] = prefix[2] + arr[2] = 7 + 1 = 8.",
      line: 4,
      cells: [
        "0",
        "2",
        { value: "7", mark: "value" },
        { value: "8", mark: "active" },
        "?",
        "?"
      ]
    },
    {
      caption: "i = 3: prefix[4] = prefix[3] + arr[3] = 8 + 3 = 11.",
      line: 4,
      cells: [
        "0",
        "2",
        "7",
        { value: "8", mark: "value" },
        { value: "11", mark: "active" },
        "?"
      ]
    },
    {
      caption: "i = 4: prefix[5] = prefix[4] + arr[4] = 11 + 4 = 15. Готово.",
      line: 4,
      cells: [
        "0",
        "2",
        "7",
        "8",
        "11",
        { value: "15", mark: "found" }
      ]
    }
  ]}
/>

Теперь трассируй запрос суммы диапазона. Найди сумму от индекса 1 до 3:

<AlgoTrace
  id="trace-prefix-query"
  lang="ru"
  code={[
    "function rangeSum(prefix, i, j) {",
    "  return prefix[j + 1] - prefix[i];",
    "}"
  ]}
  frames={[
    {
      caption: "Запрос: сумма элементов от индекса 1 до 3. prefix = [0, 2, 7, 8, 11, 15].",
      line: 1,
      cells: [
        { value: "0", mark: "value" },
        { value: "2", mark: "active" },
        "7",
        "8",
        "11",
        "15"
      ]
    },
    {
      caption: "i = 1, j = 3. Вычисли j + 1 = 4. Посмотри prefix[4] = 11 (сумма первых 4 элементов).",
      line: 2,
      cells: [
        "0",
        { value: "2", mark: "value" },
        "7",
        "8",
        { value: "11", mark: "active" },
        "15"
      ]
    },
    {
      caption: "Посмотри prefix[1] = 2 (сумма первых 1 элемента). Вычти: 11 - 2 = 9.",
      line: 2,
      cells: [
        "0",
        { value: "2", mark: "active" },
        "7",
        "8",
        { value: "11", mark: "found" },
        "15"
      ]
    },
    {
      caption: "Результат: 9. Это arr[1] + arr[2] + arr[3] = 5 + 1 + 3 = 9. ✓",
      line: 2,
      cells: [
        "0",
        { value: "2", mark: "value" },
        "7",
        "8",
        { value: "11", mark: "found" },
        "15"
      ]
    }
  ]}
/>
</Trace>

<Complexity lang="ru">
| Операция | Время | Память | Замечания |
|---|---|---|---|
| Собери префиксный массив | O(n) | O(n) | Однопроход, накопляй сумму |
| Запрос суммы диапазона | O(1) | O(1) | Два поиска и одно вычитание |
| Итого q запросов | O(n + q) | O(n) | Подготовка раз, запросы много раз |

**Сравнение с наивным подходом:**

| Подход | Время подготовки | За запрос | Итого для q запросов |
|---|---|---|---|
| Наивный (проход каждого запроса) | O(1) | O(n) | O(n × q) |
| Префиксная сумма | O(n) | O(1) | O(n + q) |

Если q большой (много запросов), префиксные суммы выигрывают в огромной мере. Если q = 1000 и n = 100, наивный это 100 000 операций; префиксная сумма это 1 100 операций. Почти 100× быстрее.

**Когда это стоит?**

Используй префиксные суммы, если:
- Ты имеешь много запросов сумм диапазонов на одном массиве.
- Массив не меняется (статический).

Не используй, если:
- Ты имеешь только один или два запроса.
- Массив меняется часто (ты должен пересобрать prefix при каждом изменении, стоит O(n)).
</Complexity>

<AlgoPractice
  id="practice-prefix-sums-1"
  lessonSlug="05-prefix-sums"
  lang="ru"
  problems={[
    {
      kind: "numeric",
      prompt: "Собери префиксный массив для [3, 1, 4, 1, 5]. Что такое prefix[3]?",
      answer: 8,
      hint: "prefix[3] = сумма первых 3 элементов = 3 + 1 + 4 = 8."
    },
    {
      kind: "numeric",
      prompt: "Используя префиксный массив [0, 3, 4, 8, 9, 14] (из массива [3, 1, 4, 1, 5]), какова сумма от индекса 1 до 3?",
      answer: 6,
      hint: "sum(1, 3) = prefix[4] - prefix[1] = 9 - 3 = 6. Это arr[1] + arr[2] + arr[3] = 1 + 4 + 1 = 6."
    },
    {
      kind: "choice",
      prompt: "Почему мы создаём префиксный массив длины n+1 вместо n?",
      choices: [
        "Чтобы обрабатывать 1-индексированные массивы",
        "Чтобы хранить нуль в начале, так формула sum(i, j) = prefix[j+1] - prefix[i] работает чисто на всех краях",
        "Потому что JavaScript массивы всегда имеют длину n+1",
        "Чтобы избежать отрицательных индексов"
      ],
      answer: 1,
      hint: "С prefix[0] = 0, любая сумма диапазона вычитается корректно без особых случаев. Для 0-индексированных массивов это стандартно."
    },
    {
      kind: "numeric",
      prompt: "Массив имеет 10 000 элементов. Ты будешь отвечать на 5 000 запросов сумм диапазонов. Примерно насколько раз быстрее наивный подход с префиксными суммами за запрос?",
      answer: 10000,
      hint: "Наивный: 5 000 запросов × 10 000 операций за запрос = 50 000 000. Префиксная сумма: 10 000 подготовки + 5 000 запросов × 1 = 15 000. Отношение: 50 000 000 / 15 000 ≈ 3 333×. (Ответ примерно 10 000, размер массива, в терминах ускорения за запрос.)"
    },
    {
      kind: "choice",
      prompt: "Если исходный массив меняется (например, ты обновляешь arr[3]), что происходит с префиксным массивом?",
      choices: [
        "Это не имеет значения; запросы всё ещё работают корректно",
        "Префиксный массив должен быть пересобран за O(n) времени, потому что все префиксные значения после индекса 3 теперь устаревают",
        "Только prefix[3] нужно обновить",
        "Префиксный массив становится невалидным и не может быть использован"
      ],
      answer: 1,
      hint: "Изменение одного элемента инвалидирует все префиксные значения ниже по потоку. Ты должен пересобрать весь массив. Вот почему префиксные суммы работают лучше на статических массивах."
    }
  ]}
/>

<Inset kind="mistake" lang="ru">
**Off-by-one индексирование.** Самая частая ошибка: путаница в том, какой индекс префиксного массива использовать. Помни: `prefix[k]` = сумма первых k элементов (индексы 0 до k-1). Чтобы запросить сумму от индекса i до j, вычти `prefix[j+1] - prefix[i]`. Если ты используешь `prefix[j] - prefix[i-1]`, ты получишь неправильный результат. Всегда рисуй это с маленьким примером сначала.
</Inset>

<Inset kind="edgecase" lang="ru">
**Запрос от индекса 0.** Если ты хочешь сумму от индекса 0 до j (начало массива), формула это `prefix[j+1] - prefix[0]`. Поскольку `prefix[0] = 0`, это упрощается до `prefix[j+1]`, что правильно. Нуль в начале обрабатывает этот граничный случай автоматически.
</Inset>

<Check lang="ru">
  <Quiz
    id="quiz-prefix-sums-1"
    pieceSlug="05-prefix-sums"
    lang="ru"
    question="Почему техника префиксной суммы хороший пример обмена времени и памяти, что ты учил в Big-O?"
    choices={[
      {
        label: "Потому что она использует дополнительную память (O(n)) чтобы сэкономить время на запросах (O(1) вместо O(n))",
        correct: true
      },
      {
        label: "Потому что она использует меньше памяти, чем наивный подход",
        misconception: "Префиксные суммы используют O(n) дополнительную память. Обмен это: больше памяти за быстрые запросы."
      },
      {
        label: "Потому что она работает только на больших массивах",
        misconception: "Префиксные суммы работают на массивах любого размера. Выгода зависит от количества запросов, не размера массива."
      },
      {
        label: "Потому что она избегает использования массива",
        misconception: "Префиксные суммы создают новый массив размера n+1. Они используют массив, не избегают его."
      }
    ]}
  />
</Check>

<Recap lang="ru">
Техника **префиксной суммы** предвычисляет накопленные суммы чтобы отвечать на запросы сумм диапазонов за O(1).

Ключевые факты:

1. **Префиксный массив:** `prefix[k]` = сумма первых k элементов (arr[0] до arr[k-1]). Инициализируй `prefix[0] = 0`. Собери за O(n) времени.

2. **Формула суммы диапазона:** сумма элементов от индекса i до j = `prefix[j+1] - prefix[i]`. Это работает благодаря нулю в начале.

3. **Почему это работает:** Вычитание двух накопленных сумм отменяет часть до индекса i, оставляя только часть от i до j.

4. **Обмен времени и памяти:** Потратишь O(n) времени и O(n) памяти на сборку. Потом каждый запрос это O(1). Итого для q запросов: O(n + q) вместо O(n × q).

5. **Когда использовать:** Много запросов на статический массив. Если массив меняется, ты должен пересобрать.

6. **Обобщение:** Та же идея расширяется на префиксные счётчики (как много элементов в диапазоне удовлетворяют условие?), 2D префиксные суммы (суммы прямоугольных областей), и больше. Паттерн всегда тот же: предвычисли раз, переиспользуй навечно.

Дальше ты будешь исследовать хеширование (сохранение и получение значений по ключу за O(1)) и потом структуры данных как стеки и очереди. Теперь, когда увидишь код, что проходит подмассив внутри цикла запросов, ты распознаешь запах O(n × q) и потянешься к префиксному массиву — один раз собрал, потом мгновенные ответы.
</Recap>
