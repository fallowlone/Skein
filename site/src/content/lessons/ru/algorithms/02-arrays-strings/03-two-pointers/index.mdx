---
concepts:
- two_pointers
- opposite_ends
- fast_slow
- in_place
estMin: 19
lang: ru
mathPrereqs: []
order: 3
prereqs:
- 02-traversal
slug: 03-two-pointers
sources:
- https://dev.to/mohamad_mhana/two-pointer-technique-mastering-the-opposite-ends-pattern-2bdj
- https://labuladong.online/en/algo/essential-technique/array-two-pointers-summary/
- https://www.baeldung.com/java-two-pointer-technique
status: ready
summary: 'Держи два индекса в массиве и двигай их по правилу. Превращает O(n²) вложенных
  циклов в O(n) однопроходное решение. Два формата: противоположные концы (указатели
  с обоих концов) и одно направление (оба спереди, один быстрее).'
title: Два указателя
track: algorithms
unit: 02-arrays-strings
---



import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Idea from "~/components/lesson/Idea.astro";
import Code from "~/components/lesson/Code.astro";
import Trace from "~/components/lesson/Trace.astro";
import Complexity from "~/components/lesson/Complexity.astro";
import Check from "~/components/lesson/Check.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import AnnotatedCode from "~/components/algo/AnnotatedCode.astro";
import AlgoTrace from "~/components/algo/AlgoTrace.astro";
import CodeRunner from "~/components/algo/CodeRunner.astro";
import AlgoPractice from "~/components/algo/AlgoPractice.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DiagramFrame from "~/components/diagram/DiagramFrame.astro";

<Hook>
У тебя есть массив и задача, что выглядит как два вложенных цикла — может, ты сравниваешь каждую пару элементов. Тогда кто-то показывает другой путь: используй два указателя вместо одного, начни с противоположных концов и иди к середине. Вложенный цикл исчезает. Однопроходный обход массива его заменяет. Вот техника двух указателей.
</Hook>

<Goal lang="ru">
После этого урока ты сможешь узнавать, когда применяются два указателя, программировать два основных формата (противоположные концы и одно направление), разобраться, почему они превращают O(n²) в O(n), и решить классические задачи: проверить палиндром, найти две суммы в отсортированном массиве и убрать дубликаты на месте.
</Goal>

<Idea lang="ru">
Техника **двух указателей** использует два индекса в массиве вместо одного. Мощь приходит из движения указателей по правилу: если условие истинно, двигай один; если ложно, двигай другой. Это правило делает работу вложенного цикла в однопроходном обходе.

**Два основных формата:**

**1. Формат противоположных концов**

Начни один указатель в начале (`left = 0`) и один в конце (`right = arr.length - 1`). Двигай их друг к другу:

```js
let left = 0, right = arr.length - 1;
while (left < right) {
  // Обработай arr[left] и arr[right]
  // Двигай left или right по условию
  if (condition) {
    left++;
  } else {
    right--;
  }
}
```

Частое применение: **проверить палиндром** (совпадают ли концы?), или **найти пару с суммой-целью** (если пара мала, двигай left вправо; если велика, двигай right влево).

**2. Формат одного направления** (быстрый и медленный)

Начни оба указателя с начала. Один (медленный) продвигается осторожно; другой (быстрый) бежит вперёд:

```js
let slow = 0, fast = 0;
while (fast < arr.length) {
  // Двигай быстрый указатель
  fast++;
  // Условно двигай медленный
  if (condition) {
    slow++;
  }
}
```

Частое применение: **убрать дубликаты из отсортированного массива на месте** (медленный отмечает конец уникальных элементов; быстрый скользит вперёд и копирует новые уникальные значения назад в позицию медленного).

**Почему это работает:**

Наивный вложенный цикл проверяет каждую пару: для каждого `i` проверь все `j`, где `j > i`. Это O(n²).

Трюк двух указателей работает из-за *инварианта*: правило, что управляет движением указателей, гарантирует, что ты никогда не нужно перепроверять комбинацию. Для противоположных концов, когда left двигается вправо и right влево, зазор сужается; ты посещаешь каждую позицию ровно один раз. Для одного направления быстрый разведывает вперёд и передаёт находки медленному, что их записывает; опять же, однопроход, без возвращения назад.

**Важное предусловие для противоположных концов два-сумма:**

Если ты ищешь два элемента, что дают целевую сумму, используя противоположные концы, массив **должен быть отсортирован**. Без сортировки правило "двигай left, если сумма мала" не гарантирует, что найдёшь все пары. Два-сумма противоположных концов работает только на отсортированных данных.
</Idea>

<DiagramFrame
  label="Противоположные концы, два указателя на отсортированном массиве [1, 3, 5, 7, 9] для цели 12. Левый указатель начинает с первой ячейки, правый — с последней. На каждом шаге сумма в двух указателях сравнивается с целью: 1 плюс 9 равно 10, что меньше 12, поэтому левый указатель сдвигается на одну ячейку вправо; теперь 3 плюс 9 равно 12, что равно цели, значит пара найдена. Указатели сходятся с обоих концов за один проход, O(n)."
  hue="var(--d-systems)"
  caption="Два-сумма с противоположных концов на отсортированном массиве. Окно между left и right сужается с обоих концов: сумма мала — left вправо, сумма велика — right влево. Указатели могут только сходиться, поэтому каждую ячейку посещают один раз — один проход, O(n) — а не O(n-квадрат) проверки каждой пары."
>
  <svg viewBox="0 0 560 250" width="560" height="250">
    <text x="40" y="18" font-size="11" font-family="var(--font-body)" fill="var(--muted)">отсортированный массив, target = 12</text>

    <text x="40" y="44" font-size="11" font-family="var(--font-mono)" fill="var(--d-systems)">шаг 1</text>
    <rect x="40" y="52" width="80" height="46" rx="6" fill="var(--card-2)" stroke="var(--d-systems)" stroke-width="1.6" />
    <rect x="120" y="52" width="80" height="46" rx="6" fill="var(--card-2)" stroke="var(--hairline-strong)" stroke-width="0.8" />
    <rect x="200" y="52" width="80" height="46" rx="6" fill="var(--card-2)" stroke="var(--hairline-strong)" stroke-width="0.8" />
    <rect x="280" y="52" width="80" height="46" rx="6" fill="var(--card-2)" stroke="var(--hairline-strong)" stroke-width="0.8" />
    <rect x="360" y="52" width="80" height="46" rx="6" fill="var(--card-2)" stroke="var(--d-systems)" stroke-width="1.6" />
    <text x="80" y="81" text-anchor="middle" font-size="15" font-family="var(--font-mono)" fill="var(--ink)">1</text>
    <text x="160" y="81" text-anchor="middle" font-size="15" font-family="var(--font-mono)" fill="var(--muted)">3</text>
    <text x="240" y="81" text-anchor="middle" font-size="15" font-family="var(--font-mono)" fill="var(--muted)">5</text>
    <text x="320" y="81" text-anchor="middle" font-size="15" font-family="var(--font-mono)" fill="var(--muted)">7</text>
    <text x="400" y="81" text-anchor="middle" font-size="15" font-family="var(--font-mono)" fill="var(--ink)">9</text>

    <text x="80" y="118" text-anchor="middle" font-size="11" font-family="var(--font-mono)" fill="var(--d-systems)">left</text>
    <text x="400" y="118" text-anchor="middle" font-size="11" font-family="var(--font-mono)" fill="var(--d-systems)">right</text>
    <path d="M 92 113 L 116 113" stroke="var(--d-systems)" stroke-width="1.6" marker-end="url(#tp-arrow)" />
    <path d="M 388 113 L 364 113" stroke="var(--d-systems)" stroke-width="1.6" marker-end="url(#tp-arrow)" />
    <text x="240" y="140" text-anchor="middle" font-size="11.5" font-family="var(--font-body)" fill="var(--ink)">1 + 9 = 10 &lt; 12 {"->"} двигаем left вправо</text>

    <text x="40" y="174" font-size="11" font-family="var(--font-mono)" fill="var(--d-systems)">шаг 2</text>
    <rect x="40" y="182" width="80" height="46" rx="6" fill="var(--card-2)" stroke="var(--hairline-strong)" stroke-width="0.8" />
    <rect x="120" y="182" width="80" height="46" rx="6" fill="var(--card-2)" stroke="var(--accent)" stroke-width="1.8" />
    <rect x="200" y="182" width="80" height="46" rx="6" fill="var(--card-2)" stroke="var(--hairline-strong)" stroke-width="0.8" />
    <rect x="280" y="182" width="80" height="46" rx="6" fill="var(--card-2)" stroke="var(--hairline-strong)" stroke-width="0.8" />
    <rect x="360" y="182" width="80" height="46" rx="6" fill="var(--card-2)" stroke="var(--accent)" stroke-width="1.8" />
    <text x="80" y="211" text-anchor="middle" font-size="15" font-family="var(--font-mono)" fill="var(--muted)">1</text>
    <text x="160" y="211" text-anchor="middle" font-size="15" font-family="var(--font-mono)" fill="var(--ink)">3</text>
    <text x="240" y="211" text-anchor="middle" font-size="15" font-family="var(--font-mono)" fill="var(--muted)">5</text>
    <text x="320" y="211" text-anchor="middle" font-size="15" font-family="var(--font-mono)" fill="var(--muted)">7</text>
    <text x="400" y="211" text-anchor="middle" font-size="15" font-family="var(--font-mono)" fill="var(--ink)">9</text>
    <text x="160" y="248" text-anchor="middle" font-size="11" font-family="var(--font-mono)" fill="var(--accent)">left</text>
    <text x="400" y="248" text-anchor="middle" font-size="11" font-family="var(--font-mono)" fill="var(--accent)">right</text>
    <text x="280" y="170" text-anchor="middle" font-size="11.5" font-family="var(--font-body)" fill="var(--accent)">3 + 9 = 12 = target {"->"} найдено, один проход</text>

    <defs>
      <marker id="tp-arrow" viewBox="0 0 10 10" refX="9" refY="5" markerWidth="7" markerHeight="7" orient="auto-start-reverse">
        <path d="M 0 0 L 10 5 L 0 10 z" fill="var(--d-systems)" />
      </marker>
    </defs>
  </svg>
</DiagramFrame>

<Code lang="ru">
Запрограммируем три классические задачи двух указателей:

**1. Проверить палиндром (противоположные концы):**
```js
function isPalindrome(s) {
  let left = 0, right = s.length - 1;
  while (left < right) {
    if (s[left] !== s[right]) {
      return false;  // Несовпадение: не палиндром
    }
    left++;
    right--;
  }
  return true;  // Все пары совпали
}
```

**2. Два-сумма на отсортированном массиве (противоположные концы):**
```js
function twoSum(arr, target) {
  // ПРЕДУСЛОВИЕ: arr должен быть отсортирован
  let left = 0, right = arr.length - 1;
  while (left < right) {
    const sum = arr[left] + arr[right];
    if (sum === target) {
      return [left, right];  // Нашли пару
    } else if (sum < target) {
      left++;  // Сумма мала, двигай left вправо
    } else {
      right--;  // Сумма велика, двигай right влево
    }
  }
  return null;  // Пара не найдена
}
```

**3. Убрать дубликаты из отсортированного массива на месте (одно направление):**
```js
function removeDuplicates(arr) {
  // ПРЕДУСЛОВИЕ: arr отсортирован
  let slow = 0;
  for (let fast = 1; fast < arr.length; fast++) {
    if (arr[fast] !== arr[slow]) {
      slow++;
      arr[slow] = arr[fast];  // Скопируй уникальный элемент в позицию slow
    }
  }
  return slow + 1;  // Длина уникальных элементов
}
```

Запусти эти функции:

<CodeRunner
  id="cr-two-pointers"
  lang="ru"
  code={`function isPalindrome(s) {
  let left = 0, right = s.length - 1;
  while (left < right) {
    if (s[left] !== s[right]) return false;
    left++;
    right--;
  }
  return true;
}

function twoSum(arr, target) {
  let left = 0, right = arr.length - 1;
  while (left < right) {
    const sum = arr[left] + arr[right];
    if (sum === target) return [left, right];
    else if (sum < target) left++;
    else right--;
  }
  return null;
}

function removeDuplicates(arr) {
  let slow = 0;
  for (let fast = 1; fast < arr.length; fast++) {
    if (arr[fast] !== arr[slow]) {
      slow++;
      arr[slow] = arr[fast];
    }
  }
  return slow + 1;
}

console.log("'racecar' палиндром?", isPalindrome("racecar"));
console.log("'hello' палиндром?", isPalindrome("hello"));

const sorted = [1, 3, 5, 7, 9];
console.log("Два-сумма 12 в [1, 3, 5, 7, 9]:", twoSum(sorted, 12));
console.log("Два-сумма 20 в [1, 3, 5, 7, 9]:", twoSum(sorted, 20));

const dup = [1, 1, 2, 2, 3, 3];
const len = removeDuplicates(dup);
console.log("После убрать дубликаты:", dup.slice(0, len), "длина:", len);`}
/>
</Code>

<Trace lang="en">
Проследим **противоположные концы два-сумма** на `[1, 3, 5, 7, 9]` ища цель 12:

<AlgoTrace
  id="trace-two-sum"
  lang="ru"
  code={[
    "function twoSum(arr, target) {",
    "  let left = 0, right = arr.length - 1;",
    "  while (left < right) {",
    "    const sum = arr[left] + arr[right];",
    "    if (sum === target) return [left, right];",
    "    else if (sum < target) left++;",
    "    else right--;",
    "  }",
    "  return null;",
    "}"
  ]}
  frames={[
    {
      caption: "Старт: arr = [1, 3, 5, 7, 9], target = 12. left = 0, right = 4.",
      line: 1,
      cells: [
        { value: "1", mark: "active" },
        "3",
        "5",
        "7",
        { value: "9", mark: "active" }
      ]
    },
    {
      caption: "sum = 1 + 9 = 10. 10 < 12, поэтому двигаем left вправо.",
      line: 6,
      cells: [
        "1",
        { value: "3", mark: "active" },
        "5",
        "7",
        { value: "9", mark: "active" }
      ]
    },
    {
      caption: "sum = 3 + 9 = 12. 12 === 12! Нашли пару на индексах [1, 4].",
      line: 5,
      cells: [
        "1",
        { value: "3", mark: "found" },
        "5",
        "7",
        { value: "9", mark: "found" }
      ]
    },
    {
      caption: "Вернули [1, 4]. Готово в однопроходном обходе.",
      line: 5,
      cells: [
        "1",
        { value: "3", mark: "done" },
        "5",
        "7",
        { value: "9", mark: "done" }
      ]
    }
  ]}
/>

Теперь проследим **одно направление убрать дубликаты** на `[1, 1, 2, 2, 3]`:

<AlgoTrace
  id="trace-remove-dup"
  lang="ru"
  code={[
    "function removeDuplicates(arr) {",
    "  let slow = 0;",
    "  for (let fast = 1; fast < arr.length; fast++) {",
    "    if (arr[fast] !== arr[slow]) {",
    "      slow++;",
    "      arr[slow] = arr[fast];",
    "    }",
    "  }",
    "  return slow + 1;",
    "}"
  ]}
  frames={[
    {
      caption: "Старт: arr = [1, 1, 2, 2, 3], slow = 0, fast = 1.",
      line: 1,
      cells: [
        { value: "1", mark: "value" },
        { value: "1", mark: "active" },
        "2",
        "2",
        "3"
      ]
    },
    {
      caption: "fast = 1: arr[1] = 1, arr[0] = 1. Одинаково, пропусти. Двигай fast на 2.",
      line: 4,
      cells: [
        { value: "1", mark: "value" },
        "1",
        { value: "2", mark: "active" },
        "2",
        "3"
      ]
    },
    {
      caption: "fast = 2: arr[2] = 2, arr[0] = 1. Разные! slow++. arr[1] = 2.",
      line: 6,
      cells: [
        { value: "1", mark: "done" },
        { value: "2", mark: "value" },
        "2",
        "2",
        "3"
      ]
    },
    {
      caption: "fast = 3: arr[3] = 2, arr[1] = 2. Одинаково, пропусти. Двигай fast на 4.",
      line: 4,
      cells: [
        "1",
        { value: "2", mark: "value" },
        "2",
        { value: "2", mark: "active" },
        "3"
      ]
    },
    {
      caption: "fast = 4: arr[4] = 3, arr[1] = 2. Разные! slow++. arr[2] = 3.",
      line: 6,
      cells: [
        "1",
        "2",
        { value: "3", mark: "value" },
        "2",
        "3"
      ]
    },
    {
      caption: "Цикл конец. slow = 2, вернули slow + 1 = 3. Уникальные элементы: [1, 2, 3].",
      line: 9,
      cells: [
        { value: "1", mark: "done" },
        { value: "2", mark: "done" },
        { value: "3", mark: "done" },
        "2",
        "3"
      ]
    }
  ]}
/>
</Trace>

<Complexity lang="ru">
Оба формата посещают каждый элемент ограниченное число раз:

| Задача | Время | Память | Замечания |
|---|---|---|---|
| Проверка палиндрома (противоположные концы) | O(n) | O(1) | Однопроход, два указателя |
| Два-сумма отсортированного массива (противоположные концы) | O(n) | O(1) | Требует отсортированный массив |
| Убрать дубликаты на месте (одно направление) | O(n) | O(1) | Меняет массив, возвращает новую длину |
| Перевернуть массив на месте (противоположные концы) | O(n) | O(1) | Менять концы, двигаться внутрь |

**Почему O(n), не O(n²)?**

Наивная проверка каждой пары — это O(n²): для каждого left, проверь все right.

Два указателя: left и right вместе посещают каждую позицию ровно один раз, когда они сходятся (противоположные концы) или когда быстрый разведывает и медленный записывает (одно направление). Нет вложенного цикла, однопроход через массив.
</Complexity>

<AlgoPractice
  id="practice-two-pointers-1"
  lessonSlug="03-two-pointers"
  lang="ru"
  problems={[
    {
      kind: "choice",
      prompt: "Почему алгоритм противоположных концов два-сумма требует отсортированный массив?",
      choices: [
        "Потому что компьютеры могут искать только в отсортированных массивах",
        "Потому что правило 'двигай left, если сумма мала' предполагает, что двигание left увеличивает сумму, что гарантировано только в отсортированном массиве",
        "Потому что сортировка быстрее, чем сам алгоритм",
        "Потому что неотсортированные массивы нельзя скользить с двух концов"
      ],
      answer: 1,
      hint: "В отсортированном массиве все значения слева от left меньше, поэтому двигание left увеличивает сумму. Неотсортированный: нет гарантии."
    },
    {
      kind: "choice",
      prompt: "В алгоритме одного направления (быстрый/медленный) убрать дубликаты, почему быстрый бежит вперёд, пока медленный отмечает уникальные позиции?",
      choices: [
        "Быстрый всегда быстрее, чем медленный",
        "Быстрый разведывает новые уникальные значения; медленный их записывает на месте, позволяя менять на месте",
        "Потому что медленный не может двигаться больше, чем один раз за итерацию цикла",
        "Чтобы запутать читателя"
      ],
      answer: 1,
      hint: "fast = 1, 2, 3, ... скользит по массиву. slow остаётся позади, двигается только, когда fast что-то новое нашёл. slow отмечает, куда писать."
    },
    {
      kind: "numeric",
      prompt: "Проверь палиндром '[1, 2, 3, 2, 1]'. Что вернёт isPalindrome? (1 = истина, 0 = ложь)",
      answer: 1,
      hint: "Сравни концы: 1 === 1 (да), 2 === 2 (да), середина 3 (одна середина, больше пар нет). Палиндром!"
    },
    {
      kind: "numeric",
      prompt: "После вызова removeDuplicates на [5, 5, 5, 7, 7], сколько уникальных элементов?",
      answer: 2,
      hint: "Уникальные элементы: 5 и 7. Функция возвращает новую длину."
    },
    {
      kind: "choice",
      prompt: "В формате противоположных концов, когда left и right встретятся (left >= right), что это значит?",
      choices: [
        "Ты нашёл цель",
        "Ты проверил все пары с противоположных концов и условие не выполнилось",
        "Цикл кончился указатели",
        "Нужно перезапустить с новым правилом"
      ],
      answer: 1,
      hint: "Цикл останавливается, когда left >= right. Если пара не удовлетворила условие к тому времени, пары нет."
    }
  ]}
/>

<Inset kind="mistake" lang="ru">
**Применять противоположные концы два-сумма к неотсортированному массиву.** Если массив [3, 1, 4, 2, 5] и ищешь сумму 6, используя противоположные концы, начни с 3 и 5 (сумма 8, велико). Двигай right влево на 2. Теперь 3 + 2 = 5 (мало). Двигай left на 1. Теперь 1 + 2 = 3 (всё ещё мало). Двигай left на 4. Теперь 4 + 2 = 6 (нашли!). Это сработало, но случайно. Правило "двигай left, если мало" не гарантирует корректность без сортировки. Алгоритм неправильный на неотсортированных данных.
</Inset>

<Inset kind="edgecase" lang="ru">
**Пустые или одноэлементные массивы.** Если массив имеет 0 или 1 элемент, цикл противоположных концов `while (left < right)` никогда не входит (потому что 0 < 0 ложно, или 0 < 1 истинно только раз, но потом left++, right-- сразу выходят). Для одного направления цикл `for (let fast = 1; ...)` начинает fast на 1; если массив один элемент (индекс 0), fast = 1 вне границ и цикл не запускается. Оба формата корректно обрабатывают пустые/одноэлементные массивы: они возвращают истину (палиндром) или вход неизменённым.
</Inset>

<Check lang="ru">
  <Quiz
    id="quiz-two-pointers-1"
    pieceSlug="03-two-pointers"
    lang="ru"
    question="Почему техника двух указателей превращает O(n²) проблему вложенных циклов в O(n)?"
    choices={[
      {
        label: "Потому что два указателя могут скользить по массиву дважды, что быстрее, чем вложенные циклы",
        misconception: "Два прохода всё ещё O(n), но не это причина. Причина — один проход, не полторы."
      },
      {
        label: "Потому что правило, управляющее движением указателей, гарантирует, что каждую позицию посещают максимум один раз или дважды, не n раз за позицию",
        correct: true
      },
      {
        label: "Потому что компьютеры быстрее выполняют два указателя, чем вложенные циклы",
        misconception: "Скорость зависит от алгоритма, не от аппаратуры. Два указателя быстрее, потому что делают меньше работы (один проход, не n² работы)."
      },
      {
        label: "Потому что два указателя не могут посещать одну пару дважды",
        misconception: "Верное наблюдение, но не основная причина. Основная причина — движение указателей O(n), не что пары уникальны."
      }
    ]}
  />
</Check>

<Recap lang="ru">
Техника **двух указателей** использует два индекса и правило, чтобы решить проблемы в однопроходном обходе вместо вложенных циклов.

Ключевые факты:

1. **Формат противоположных концов:** Начни с обоих концов, двигайся к середине. Применяй, когда проверяешь симметрию (палиндром) или ищешь пары в отсортированном массиве.

2. **Формат одного направления:** Оба начинаются с начала; один разведывает вперёд, другой записывает. Применяй, когда меняешь массив на месте или когда ритм медленный/быстрый естественен.

3. **Почему O(n), не O(n²)?** Правило движения указателей гарантирует, что каждую позицию посещают максимум один раз за указатель, не один раз за внешний цикл. Нет вложенной итерации.

4. **Противоположные концы два-сумма требует отсортированный вход.** Без сортировки правило "двигай left, если сумма мала" неправильно.

5. **Одно направление позволяет менять на месте.** Быстрый находит; медленный пишет назад, перезаписывая дубликаты или старые значения.

Дальше ты будешь видеть два указателя, применённые к связным спискам (где обход — единственный способ вперёд) и скользящие окна (где два указателя отслеживают диапазон элементов под другим правилом).
</Recap>
