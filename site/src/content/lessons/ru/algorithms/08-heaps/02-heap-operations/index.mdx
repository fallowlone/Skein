---
concepts:
- heap
- sift_up
- sift_down
- push
- pop
- extract_min
- build_heap
- heapify
- heap_property
- time_complexity
estMin: 30
lang: ru
mathPrereqs: []
order: 2
prereqs:
- 08-heaps/01-the-heap
slug: 02-heap-operations
sources:
- https://en.wikipedia.org/wiki/Binary_heap
- https://www.geeksforgeeks.org/dsa/binary-heap/
- https://www.programiz.com/dsa/heap-data-structure
- https://brilliant.org/wiki/binary-heap/
status: ready
summary: 'Push, pop и построение кучи: как мутировать кучу, сохраняя свойство кучи.
  Просеивание вверх и вниз восстанавливают порядок. Построение кучи превращает любой
  массив в кучу за O(n) времени — не O(n log n).'
title: Операции с кучей
track: algorithms
unit: 08-heaps
---



import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Idea from "~/components/lesson/Idea.astro";
import Code from "~/components/lesson/Code.astro";
import Trace from "~/components/lesson/Trace.astro";
import Complexity from "~/components/lesson/Complexity.astro";
import Check from "~/components/lesson/Check.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import AnnotatedCode from "~/components/algo/AnnotatedCode.astro";
import AlgoTrace from "~/components/algo/AlgoTrace.astro";
import CodeRunner from "~/components/algo/CodeRunner.astro";
import AlgoPractice from "~/components/algo/AlgoPractice.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";
import Term from "~/components/prose/Term.astro";

<Hook>
У вас есть куча. Теперь вы хотите добавить новый элемент или удалить корень. Когда вы это делаете, куча ломается — родитель становится больше его ребёнка, или массив больше не представляет полное дерево. Две операции восстанавливают порядок: <Term slug="sift_up">просеивание вверх</Term> (подбросить нового элемента вверх мимо его родителя) и <Term slug="sift_down">просеивание вниз</Term> (потопить неправильно расположенный элемент вниз мимо его детей). Вместе они сохраняют свойство кучи живым. И есть третье чудо: вы можете превратить любой неупорядоченный массив в кучу путём просеивания вниз каждого не-листового узла, в O(n) времени — быстрее, чем вставлять n элементов один за другим.
</Hook>

<Goal lang="ru">
После этого урока вы можете вставить элемент в кучу с просеиванием вверх и объяснить, почему это занимает O(log n) времени. Вы можете удалить корень, обменявшись с последним элементом и просеяв вниз, и объяснить стоимость O(log n). Вы можете превратить произвольный массив в валидную кучу, вызвав просеивание вниз на каждом не-листовом узле, начиная с последнего, за O(n) общее время — не O(n log n). Вы можете трассировать все три операции на конкретном массиве кучи, шаг за шагом, проверяя свойство кучи после.
</Goal>

<Idea lang="ru">
**Две фундаментальные операции: просеивание вверх и вниз**

<Term slug="heap">Куча</Term> валидна только если <Term slug="heap_property">свойство кучи</Term> выполняется: каждый родитель ≤ его дети (мин-куча) или ≥ (макс-куча). Когда вы добавляете или удаляете элемент, это свойство нарушается. Две операции восстанавливают его.

**Просеивание вверх (всплытие):** Когда новый элемент добавляется в конец массива кучи, он может быть меньше (в мин-куче) его родителя. <Term slug="sift_up">Просеивание вверх</Term> повторно сравнивает элемент со своим родителем и обменивается вверх до тех пор, пока свойство кучи не восстановится или не будет достигнут корень.

**Просеивание вниз (погружение):** Когда корень удаляется и последний элемент перемещается в корень, он может быть больше его детей. <Term slug="sift_down">Просеивание вниз</Term> повторно сравнивает элемент со своим меньшим (или большим) ребёнком и обменивается вниз до тех пор, пока свойство кучи не восстановится или не будет достигнут лист.

**Почему это работает:** Свойство кучи нарушается только в одном узле за раз. Просеивание вверх и вниз исправляют ровно одну пару родитель-ребёнок за каждый обмен, и потому что дерево сбалансировано (высота O(log n)), необходимо максимум O(log n) обменов.

**Push (вставка)**

<Term slug="push">Push</Term> добавляет новый элемент в кучу.

1. Добавьте новое значение в конец массива кучи.
2. Вызовите просеивание вверх на вновь добавленном элементе.
3. Просеивание вверх останавливается, когда новый элемент находится в правильной позиции (свойство кучи восстановлено) или достигнут корень.

Пример: Вставьте 0 в мин-кучу [1, 3, 2, 5, 4].
- Добавьте: [1, 3, 2, 5, 4, 0]
- Просеивание вверх с индекса 5 (значение 0):
  - Родитель на индексе (5-1)/2 = 2 (значение 2).
  - 0 < 2, обмен: [1, 3, 0, 5, 4, 2].
  - Теперь на индексе 2 (значение 0), родитель на индексе (2-1)/2 = 0 (значение 1).
  - 0 < 1, обмен: [0, 3, 1, 5, 4, 2].
  - Теперь на индексе 0 (корень), стоп.
- Результат: [0, 3, 1, 5, 4, 2]. Свойство кучи проверено: 0 ≤ (3, 1), 3 ≤ (5, 4), 1 ≤ (2). ✓

**Pop (извлечение-минимум или извлечение-максимум)**

<Term slug="pop">Pop</Term> удаляет и возвращает корень (минимальный или максимальный элемент).

1. Сохраните значение корня (чтобы вернуть его).
2. Переместите последний элемент в массиве в позицию корня.
3. Удалите последний элемент из массива (размер уменьшается на 1).
4. Вызовите просеивание вниз на корне.
5. Просеивание вниз останавливается, когда свойство кучи восстановлено или достигнут лист.

Пример: Pop из мин-кучи [1, 3, 2, 5, 4].
- Сохраните корень 1 (чтобы вернуть).
- Переместите последний элемент (4) в корень: [4, 3, 2, 5].
- Просеивание вниз с индекса 0 (значение 4):
  - Дети на индексах 1 (значение 3) и 2 (значение 2). Меньший ребёнок — 2 на индексе 2.
  - 4 > 2, обмен: [2, 3, 4, 5].
  - Теперь на индексе 2 (значение 4), дети на индексах 5 и 6 (не существуют).
  - Стоп.
- Результат: [2, 3, 4, 5]. Свойство кучи проверено: 2 ≤ (3, 4), 3 ≤ (5). ✓ Возвращаемое значение: 1.

**Построение кучи (heapify)**

Вы можете построить валидную кучу из произвольного массива за O(n) времени — быстрее, чем вызывая push n раз (это было бы O(n log n)).

<Term slug="build_heap">Построение кучи</Term> (иначе <Term slug="heapify">heapify</Term>) работает путём просеивания вниз каждого не-листового узла, начиная с последнего не-листового и работая в обратном направлении к корню.

Последний не-листовый узел находится на индексе `⌊n/2⌋ - 1`. Почему? Потому что куча с n элементами имеет высоту ⌈log₂(n+1)⌉, а последний не-листовый — последний узел, у которого существуют дети.

Для каждого не-листового вызовите просеивание вниз. Хотя просеивание вниз O(log n) на один узел, построение кучи O(n) всего потому, что большинство узлов — листья (что требует O(1) просеивания), а внутренние узлы у дна имеют короткие поддеревья.

Пример: Превратьте [5, 3, 8, 1, 4] в мин-кучу.
- n = 5. Последний индекс не-листового = ⌊5/2⌋ - 1 = 1.
- Просеивание вниз индекса 2 (значение 8): Нет детей (индексы 5, 6 не существуют). Нет обмена.
- Просеивание вниз индекса 1 (значение 3):
  - Дети на индексах 3 (значение 1) и 4 (значение 4). Меньший — 1 на индексе 3.
  - 3 > 1, обмен: [5, 1, 8, 3, 4].
  - Теперь на индексе 3, нет детей. Стоп.
- Просеивание вниз индекса 0 (значение 5):
  - Дети на индексах 1 (значение 1) и 2 (значение 8). Меньший — 1 на индексе 1.
  - 5 > 1, обмен: [1, 5, 8, 3, 4].
  - Теперь на индексе 1, дети на индексах 3 (значение 3) и 4 (значение 4). Меньший — 3 на индексе 3.
  - 5 > 3, обмен: [1, 3, 8, 5, 4].
  - Теперь на индексе 3, нет детей. Стоп.
- Результат: [1, 3, 8, 5, 4]. Свойство кучи проверено: 1 ≤ (3, 8), 3 ≤ (5, 4). ✓

</Idea>

<Code lang="ru">
**Просеивание вверх в коде**

<AnnotatedCode
  code={`function siftUp(heap, index) {
  // Повторно обменивайтесь с родителем, пока свойство кучи не будет восстановлено или не будет достигнут корень
  while (index > 0) {
    const parentIndex = Math.floor((index - 1) / 2);
    // В мин-куче, ребёнок < родитель — нарушение; обмен и продолжение
    if (heap[index] < heap[parentIndex]) {
      [heap[index], heap[parentIndex]] = [heap[parentIndex], heap[index]];
      index = parentIndex;
    } else {
      break; // Свойство кучи восстановлено
    }
  }
}

function push(heap, value) {
  heap.push(value);
  siftUp(heap, heap.length - 1);
}`}
  annotations={[
    { line: 1, text: "Просеивание вверх восстанавливает свойство кучи после вставки." },
    { line: 3, text: "Продолжайте, пока узел не является корнем." },
    { line: 4, text: "Вычислите индекс родителя, используя стандартную формулу." },
    { line: 6, text: "В мин-куче, если ребёнок < родитель, свойство нарушено." },
    { line: 7, text: "Обменяйте ребёнка с родителем и движение вверх." },
    { line: 10, text: "Push добавляет значение в конец и просеивает его вверх." }
  ]}
/>

**Просеивание вниз в коде**

<AnnotatedCode
  code={`function siftDown(heap, index) {
  // Повторно обменивайтесь с меньшим (или большим) ребёнком, пока свойство будет выполнено
  while (true) {
    const leftChild = 2 * index + 1;
    const rightChild = 2 * index + 2;
    let smallest = index;
    
    // Проверьте, существует ли левый ребёнок и меньше ли он текущего наименьшего
    if (leftChild < heap.length && heap[leftChild] < heap[smallest]) {
      smallest = leftChild;
    }
    
    // Проверьте, существует ли правый ребёнок и меньше ли он
    if (rightChild < heap.length && heap[rightChild] < heap[smallest]) {
      smallest = rightChild;
    }
    
    // Если ребёнок меньше, обмен и продолжение; иначе стоп
    if (smallest !== index) {
      [heap[index], heap[smallest]] = [heap[smallest], heap[index]];
      index = smallest;
    } else {
      break; // Свойство кучи восстановлено
    }
  }
}

function pop(heap) {
  const root = heap[0];
  heap[0] = heap[heap.length - 1];
  heap.pop();
  if (heap.length > 0) {
    siftDown(heap, 0);
  }
  return root;
}`}
  annotations={[
    { line: 1, text: "Просеивание вниз восстанавливает свойство кучи после удаления корня." },
    { line: 4, text: "Вычислите индексы левого и правого детей." },
    { line: 5, text: "Отслеживайте индекс наименьшего элемента (для мин-кучи)." },
    { line: 8, text: "Если левый ребёнок существует и меньше, обновите наименьший." },
    { line: 13, text: "Проверьте правый ребёнок так же." },
    { line: 18, text: "Если наименьший не текущий узел, обмен необходим." },
    { line: 19, text: "Обменяйте и продолжайте просеивание с новой позиции." },
    { line: 24, text: "Pop возвращает корень и восстанавливает свойство кучи." }
  ]}
/>

**Построение кучи в коде**

<AnnotatedCode
  code={`function buildHeap(arr) {
  // Начните с последнего не-листового узла и просейте вниз каждый
  const lastNonLeaf = Math.floor(arr.length / 2) - 1;
  for (let i = lastNonLeaf; i >= 0; i--) {
    siftDown(arr, i);
  }
  return arr;
}

// Пример: превратить неупорядоченный массив в мин-кучу
const unordered = [5, 3, 8, 1, 4];
buildHeap(unordered);
console.log("Куча:", unordered); // [1, 3, 8, 5, 4]`}
  annotations={[
    { line: 1, text: "buildHeap преобразует любой массив в валидную кучу." },
    { line: 3, text: "Последний не-листовый узел на индексе floor(n/2) - 1." },
    { line: 4, text: "Повторяйте от последнего не-листового до корня (индекс 0)." },
    { line: 5, text: "Просейте вниз каждый узел, чтобы восстановить свойство кучи в его поддереве." },
    { line: 11, text: "buildHeap — O(n), потому что большинство узлов листья (O(1) просеивание) и внутренние узлы имеют короткие поддеревья." }
  ]}
/>

<CodeRunner
  code={`function siftDown(heap, index) {
  while (true) {
    const leftChild = 2 * index + 1;
    const rightChild = 2 * index + 2;
    let smallest = index;
    
    if (leftChild < heap.length && heap[leftChild] < heap[smallest]) {
      smallest = leftChild;
    }
    if (rightChild < heap.length && heap[rightChild] < heap[smallest]) {
      smallest = rightChild;
    }
    
    if (smallest !== index) {
      [heap[index], heap[smallest]] = [heap[smallest], heap[index]];
      index = smallest;
    } else {
      break;
    }
  }
}

function buildHeap(arr) {
  const lastNonLeaf = Math.floor(arr.length / 2) - 1;
  for (let i = lastNonLeaf; i >= 0; i--) {
    siftDown(arr, i);
  }
  return arr;
}

// Проверьте построение кучи на неупорядоченном массиве
const test = [5, 3, 8, 1, 4];
buildHeap(test);
console.log("Куча:", test.join(", "));

// Проверьте свойство кучи: каждый родитель <= дети
let valid = true;
for (let i = 0; i < test.length; i++) {
  const left = 2 * i + 1;
  const right = 2 * i + 2;
  if (left < test.length && test[i] > test[left]) {
    console.log(\`Нарушение на индексе \${i}\`);
    valid = false;
  }
  if (right < test.length && test[i] > test[right]) {
    console.log(\`Нарушение на индексе \${i}\`);
    valid = false;
  }
}
console.log("Валидная куча:", valid);`}
  expected={`Куча: 1, 3, 8, 5, 4
Валидная куча: true`}
/>

</Code>

<Trace lang="ru">
<AlgoTrace
  id="sift-up-trace"
  lang="ru"
  code={[
    "const minHeap = [1, 3, 2, 5, 4];",
    "// Push 0",
    "minHeap.push(0);  // [1, 3, 2, 5, 4, 0]",
    "// Просеивание вверх с индекса 5"
  ]}
  frames={[
    { caption: "Начните с мин-кучи [1, 3, 2, 5, 4]. Push 0 в конец.", line: 3, cells: [{ value: "1" }, { value: "3" }, { value: "2" }, { value: "5" }, { value: "4" }, { value: "0", mark: "active" }] },
    { caption: "Индекс 5, родитель на floor((5-1)/2) = 2 (значение 2). 0 < 2, обмен.", line: 4, cells: [{ value: "1" }, { value: "3" }, { value: "0", mark: "active" }, { value: "5" }, { value: "4" }, { value: "2" }] },
    { caption: "Индекс 2, родитель на floor((2-1)/2) = 0 (значение 1). 0 < 1, обмен.", line: 4, cells: [{ value: "0", mark: "active" }, { value: "3" }, { value: "1" }, { value: "5" }, { value: "4" }, { value: "2" }] },
    { caption: "Индекс 0 — корень. Свойство кучи восстановлено: 0 ≤ (3, 1), 3 ≤ (5, 4), 1 ≤ (2). Готово.", line: 4, cells: [{ value: "0", mark: "done" }, { value: "3" }, { value: "1", mark: "done" }, { value: "5" }, { value: "4" }, { value: "2", mark: "done" }] }
  ]}
/>

<AlgoTrace
  id="sift-down-trace"
  lang="ru"
  code={[
    "const minHeap = [1, 3, 2, 5, 4];",
    "// Pop корень (1) и просейте вниз"
  ]}
  frames={[
    { caption: "Мин-куча [1, 3, 2, 5, 4]. Pop удаляет корень 1.", line: 2, cells: [{ value: "1", mark: "active" }, { value: "3" }, { value: "2" }, { value: "5" }, { value: "4" }] },
    { caption: "Переместите последний элемент (4) в корень, удалите последний. Массив — [4, 3, 2, 5].", line: 2, cells: [{ value: "4", mark: "active" }, { value: "3" }, { value: "2" }, { value: "5" }] },
    { caption: "Индекс 0, дети на 1 (значение 3) и 2 (значение 2). Меньший — 2. 4 > 2, обмен.", line: 2, cells: [{ value: "2", mark: "active" }, { value: "3" }, { value: "4" }, { value: "5" }] },
    { caption: "Индекс 2, дети на 5, 6 (не существуют). Свойство кучи восстановлено. Готово.", line: 2, cells: [{ value: "2", mark: "done" }, { value: "3" }, { value: "4", mark: "done" }, { value: "5", mark: "done" }] }
  ]}
/>
</Trace>

<Complexity lang="ru">
**Время просеивания вверх и вниз**

<Term slug="sift_up">Просеивание вверх</Term> перемещает элемент вверх на один уровень за обмен. Мин-куча с n элементами имеет высоту ⌈log₂(n+1)⌉, поэтому происходит максимум O(log n) обменов. Каждый обмен O(1) (два сравнения, один доступ к массиву).
- **Время: O(log n)**

<Term slug="sift_down">Просеивание вниз</Term> перемещает элемент вниз на один уровень за обмен, также O(log n) в худшем случае.
- **Время: O(log n)**

**Push и pop**

<Term slug="push">Push</Term> добавляет (O(1)) и вызывает просеивание вверх (O(log n)).
- **Время: O(log n)**

<Term slug="pop">Pop</Term> перемещает последний в корень (O(1)), удаляет последний (O(1)) и просеивает вниз (O(log n)).
- **Время: O(log n)**

**Время построения кучи: почему O(n) а не O(n log n)?**

Наивный подход вызвал бы push n раз: O(n log n). Но <Term slug="build_heap">построение кучи</Term> работает иначе. Оно вызывает просеивание вниз на n/2 узлах. Это похоже на O(n * log n), но не так.

Ключевой инсайт: просеивание вниз с узла на высоте h требует O(h) времени (не O(log n) для всех узлов). Куча имеет:
- 1 узел на высоте ⌈log₂ n⌉ (корень).
- 2 узла на высоте ⌈log₂ n⌉ - 1.
- 4 узла на высоте ⌈log₂ n⌉ - 2.
- ...
- n/2 узлов на высоте 0 (листья).

Общая работа:
```
1*⌈log₂ n⌉ + 2*(⌈log₂ n⌉ - 1) + 4*(⌈log₂ n⌉ - 2) + ... + (n/2)*0
= Σ(2^h * (⌈log₂ n⌉ - h)) для h от 0 до ⌈log₂ n⌉
```

Эта сумма равна n * (постоянная, что зависит от логарифма), которая сходится к постоянной < 2. Поэтому **время O(n)**.

- **Построение кучи: O(n)**

**Пространственная сложность**

Все три операции (push, pop, построение кучи) работают на месте. Куча хранится в одном массиве.
- **Пространство: O(1)** (не считая сам массив)

</Complexity>

<AlgoPractice
  id="practice-heap-operations"
  lessonSlug="02-heap-operations"
  lang="ru"
  problems={[
    {
      kind: "choice",
      prompt: "После вставки нового элемента в мин-кучу, просеивание вверх останавливается, когда элемент находится в позиции, где его значение больше значения своего родителя. Сколько обменов может потребоваться в худшем случае для кучи с 1000 элементами?",
      choices: [
        "Около 1000 обменов",
        "Около 10 обменов (log₂ 1000 ≈ 10)",
        "Ровно 1 обмен",
        "Это зависит от скорости CPU"
      ],
      answer: 1,
      hint: "Худший случай — когда новый элемент минимален и должен всплыть к корню. Куча — полное двоичное дерево высотой максимум log₂(n). Поэтому около 10 обменов для n=1000."
    },
    {
      kind: "numeric",
      prompt: "У вас мин-куча с 16 элементами. Какой индекс последнего не-листового узла при вызове построения кучи?",
      answer: 7,
      hint: "Индекс последнего не-листового = floor(n/2) - 1 = floor(16/2) - 1 = 8 - 1 = 7."
    },
    {
      kind: "choice",
      prompt: "Почему построение кучи O(n) вместо O(n log n)?",
      choices: [
        "Потому что современные компьютеры быстрые",
        "Потому что большинство узлов листья (просеивание 0 уровней); внутренние узлы у дна имеют короткие поддеревья. Общая работа суммируется к O(n).",
        "Потому что алгоритм на самом деле не проверяет все n узлов",
        "Потому что мы только один раз вызываем просеивание вниз на один узел, не log n раз"
      ],
      answer: 1,
      hint: "Половина узлов листья (0 работы). Четверть на один уровень выше (1 работа каждый). Восьмая на два уровня выше (2 работы каждый). Сумма сходится."
    },
    {
      kind: "choice",
      prompt: "Вы извлекаете корень из мин-кучи [1, 4, 2, 7, 5]. Стандартная операция pop перемещает последний элемент в корень, затем просеивает вниз. Какой массив кучи получится в результате?",
      choices: ["[2, 4, 5, 7]", "[4, 5, 2, 7]", "[2, 4, 7, 5]", "[5, 4, 2, 7]"],
      answer: 0,
      hint: "Pop: меняем местами корень 1 и последний элемент 5, удаляем старый корень -> [5, 4, 2, 7]. Просеиваем 5 вниз с индекса 0: его дети — 4 (индекс 1) и 2 (индекс 2); меньший — 2. Меняем местами 5 и 2 -> [2, 4, 5, 7]. Узел 5 теперь на индексе 2, детей нет. Стоп. Результат: [2, 4, 5, 7]."
    },
    {
      kind: "numeric",
      prompt: "После применения построения кучи к [10, 5, 20, 15, 30], какое минимальное значение на корне результирующей мин-кучи?",
      answer: 5,
      hint: "Построение кучи обрабатывает массив [10, 5, 20, 15, 30] и переупорядочивает его так, чтобы свойство кучи выполнялось. Минимальный элемент должен быть в корне. Элементы 10, 5, 20, 15, 30; минимум — 5."
    }
  ]}
/>

<Inset type="why">
**Почему операции с кучей важны.** Push и pop — O(log n), намного быстрее, чем добавление в конец отсортированного списка (O(n), потому что вы должны сдвинуть все большие элементы). Построение кучи за O(n) означает, что вы можете взять любой набор данных и подготовить его для эффективных приоритетных очередей. Алгоритм Дейкстры, планирование задач и поиск медианы — всё полагается на эти операции.
</Inset>

<Inset type="mistake">
**Путаница в направлениях просеивания.** Просеивание вверх — для вставки (начните с листа, движение к корню). Просеивание вниз — для удаления (начните с корня, движение к листьям). Они восстанавливают свойство в разных направлениях, поэтому логика не симметрична. Проверьте ваш код: просеивание вверх идёт вверх (родитель = (i-1)/2), просеивание вниз идёт вниз (дети = 2i+1, 2i+2).
</Inset>

<Inset type="edgecase">
**Куча с одним элементом.** Вставка в пустую кучу или кучу с одним элементом работает нормально: новый элемент добавляется, и просеивание вверх останавливается сразу потому что условие родителя не нарушено. Pop из кучи с одним элементом оставляет пустую кучу; просеивание вниз не должно быть вызвано, если куча пуста.
</Inset>

<Check lang="ru">
  <Quiz
    id="quiz-heap-operations"
    pieceSlug="02-heap-operations"
    lang="ru"
    question="Объясни различие между просеиванием вверх и просеиванием вниз, и когда вы используете каждое."
    choices={[
      {
        label: "Просеивание вверх вызывается после push нового элемента в конец; оно поднимает элемент вверх к корню путём сравнения с родителем и обмена, если свойство кучи нарушено. Просеивание вниз вызывается после удаления корня; оно опускает элемент вниз путём сравнения с детьми. Просеивание вверх восстанавливает свойство движением к корню; просеивание вниз движением к листьям.",
        correct: true
      },
      {
        label: "Просеивание вверх и просеивание вниз — это одна операция; названия взаимозаменяемы. Они оба восстанавливают свойство кучи путём обмена с соседом.",
        misconception: "Просеивание вверх и просеивание вниз принципиально различны. Просеивание вверх сравнивает с родителем (вверх); просеивание вниз сравнивает с детьми (вниз). Они движутся в противоположных направлениях."
      },
      {
        label: "Просеивание вверх быстрее, чем просеивание вниз, потому что он имеет меньше детей для проверки.",
        misconception: "Оба O(log n). Просеивание вверх проверяет одного родителя за уровень; просеивание вниз проверяет до двух детей. Высота одна и та же, поэтому время одно и то же."
      },
      {
        label: "Вы используете просеивание вниз для push и pop, в зависимости от размера кучи.",
        misconception: "Push использует просеивание вверх; pop использует просеивание вниз. Операция зависит от того, где нарушение: после вставки в конец (просеивание вверх) или после перемещения последнего элемента в корень (просеивание вниз)."
      }
    ]}
  />
</Check>

<Recap lang="ru">
Чтобы сохранить <Term slug="heap">кучу</Term> при добавлении или удалении элементов, используйте две операции: **просеивание вверх** (поднять новый элемент путём сравнения и обмена с его родителем, O(log n)) и **просеивание вниз** (опустить неправильный элемент путём сравнения и обмена с его меньшим/большим ребёнком, O(log n)). **Push** добавляет новое значение и просеивает вверх; **pop** извлекает корень, перемещает последний элемент в корень и просеивает вниз — оба O(log n). Чудо-операция — **построение кучи** (также **heapify**): просейте вниз каждый не-листовый узел, начиная с последнего, превращая любой массив в валидную кучу за O(n) общее время. Это быстрее, чем push n элементов один за другим (O(n log n)), потому что большинство узлов листья (O(1) работа) и внутренние узлы имеют неглубокие поддеревья. Общая работа — сумма счёта узлов × глубина, которая сходится к O(n). **Дальше:** Раздел 08, урок 03 использует кучи для построения приоритетных очередей, фундаментальной структуры данных для алгоритма Дейкстры и планирования задач.
</Recap>
