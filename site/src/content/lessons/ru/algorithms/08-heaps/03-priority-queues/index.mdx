---
concepts:
- abstract_data_type
- priority_queue
- heap
- extract_min
- insert
- peek
- heap_implementation
- adt_vs_implementation
- application
estMin: 28
lang: ru
mathPrereqs: []
order: 3
prereqs:
- 08-heaps/01-the-heap
- 08-heaps/02-heap-operations
slug: 03-priority-queues
sources:
- https://en.wikipedia.org/wiki/Priority_queue
- https://www.cs.princeton.edu/courses/archive/spring02/cs226/lectures/pq.4up.pdf
- https://charlesreid1.com/wiki/Priority_Queues/ADT
- https://lucasmagnum.medium.com/sidenotes-priority-queue-abstract-data-type-and-data-structure-52b5fcd7b904
status: ready
summary: 'АТД очередь с приоритетом: всегда извлекай элемент с наивысшим приоритетом.
  Реализация двоичная куча: insert и extractMin O(log n), peek O(1). Применение: планирование,
  Дейкстра, слияние потоков. Ключевое: АТД это контракт, куча это один способ его
  выполнить.'
title: Очереди с приоритетом
track: algorithms
unit: 08-heaps
---



import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Idea from "~/components/lesson/Idea.astro";
import Code from "~/components/lesson/Code.astro";
import Trace from "~/components/lesson/Trace.astro";
import Complexity from "~/components/lesson/Complexity.astro";
import Check from "~/components/lesson/Check.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import AnnotatedCode from "~/components/algo/AnnotatedCode.astro";
import AlgoTrace from "~/components/algo/AlgoTrace.astro";
import CodeRunner from "~/components/algo/CodeRunner.astro";
import AlgoPractice from "~/components/algo/AlgoPractice.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";
import Term from "~/components/prose/Term.astro";
import DiagramFrame from "~/components/diagram/DiagramFrame.astro";

<Hook>
У вас есть куча. Теперь вы обёртываете её новым именем и обещанием: «Ты никогда не ищешь максимум или минимум. Ты всегда получаешь его за одну операцию — самый срочный, наиболее приоритетный, самый дешёвый элемент вперёд». Это обещание — <Term slug="priority_queue">очередь с приоритетом</Term> — не структура данных (это куча внизу), а <Term slug="abstract_data_type">контракт</Term>, что говорит, что можно делать. Куча — один способ держать этот контракт. Алгоритм Дейкстры её использует. Планировщики её используют. Каждый раз, когда нужно всегда обработать самую важную вещь следующей, ты берёшь очередь с приоритетом.
</Hook>

<Goal lang="ru">
После этого урока ты можешь объяснить разницу между абстрактным типом данных и реализацией. Ты можешь назвать интерфейс очереди с приоритетом: insert новый элемент, extractMin (или extractMax) чтобы удалить и вернуть элемент с наивысшим приоритетом, и peek посмотреть его без удаления. Ты понимаешь, что двоичная куча — стандартная реализация и почему (O(log n) insert и extract, O(1) peek). Ты можешь узнать, когда задача требует очередь с приоритетом вместо обычной очереди или массива, и назвать три реальных применения: планирование задач, кратчайший путь Дейкстры и симуляция событий.
</Goal>

<Idea lang="ru">
**Что такое очередь с приоритетом?**

<Term slug="priority_queue">Очередь с приоритетом</Term> — это <Term slug="abstract_data_type">абстрактный тип данных</Term> — контракт, не конкретная структура данных. Описывает, что можно делать, а не как построено.

Контракт говорит: у тебя есть коллекция элементов, каждый с приоритетом (число или правило сравнения). Ты можешь:
1. **Insert** (вставить) новый элемент.
2. **ExtractMin** (или **extractMax**): удалить и вернуть элемент с наименьшим (или наибольшим) приоритетом.
3. **Peek** (подглядеть): посмотреть элемент с наивысшим приоритетом без удаления.

Вот интерфейс. Куча живёт внизу.

**АТД против реализации**

<Term slug="abstract_data_type">Абстрактный тип данных (АТД)</Term> — это спецификация — контракт. Говорит, какие операции поддерживаются и что они делают. НЕ говорит как.

Очередь с приоритетом — это АТД. Её операции: insert, extractMin и peek.

**Реализация** — это код. Двоичная куча — одна частая реализация: использует полное двоичное дерево, хранящееся в массиве, со свойством мин-кучи (каждый родитель ≤ дети).

Один АТД может иметь много реализаций. Очередь с приоритетом может быть:
- Двоичной кучей (быстрый insert и extract, эффективна по памяти).
- Отсортированным массивом (быстрый peek, медленный insert и extract).
- Связным списком (медленно всё).
- Сбалансированным двоичным деревом поиска вроде красно-чёрного дерева (log n все операции, но больше констант).

Когда кодишь, используешь абстракцию АТД. Спрашиваешь: insert, extractMin, peek. Реализация заменяема — поменяй её, клиентский код не изменится.

**Почему двоичная куча?**

Двоичная куча — стандартная реализация, потому что быстра (O(log n) insert и extract, O(1) peek) и памяти эффективна (нет указателей, просто массив). Простая настолько, чтобы написать руками, но боевая.

**Мин-куча против макс-кучи**

В <Term slug="min_heap">мин-куче</Term> extractMin возвращает наименьший элемент. Используется когда хочешь обработать наименьшую стоимость, кратчайшее время или наименее срочную задачу первой.

В <Term slug="max_heap">макс-куче</Term> extractMax возвращает наибольший элемент. Используется когда хочешь наивысший приоритет, наибольший вес или самую срочную задачу первой.

Логика симметрична — если нужна макс-куча и есть только мин-куча библиотека, инвертируй приоритеты и используй мин-кучу.

**Интерфейс очереди с приоритетом в псевдокоде**

```
PriorityQueue:
  insert(element, priority)
    Добавь элемент в очередь.
  
  extractMin()
    Удали и верни элемент с наименьшим приоритетом.
  
  peek()
    Верни (без удаления) элемент с наименьшим приоритетом.
  
  isEmpty()
    Истина если очередь пуста.
  
  size()
    Количество элементов в очереди.
```

**Пример: планирование задач**

У тебя есть очередь задач. Каждая задача имеет приоритет (1 = срочно, 10 = низкий приоритет). Задачи приходят в случайные моменты. Ты хочешь всегда работать над самой срочной задачей следующей.

```
Queue: [print job (приоритет 3), compile (приоритет 5), test (приоритет 1)]

extractMin() → test с приоритетом 1 (хотя добавлена последней)
insert(deploy, приоритет 2)

Queue: [deploy (приоритет 2), print job (приоритет 3), compile (приоритет 5)]

extractMin() → deploy с приоритетом 2
```

Обычная очередь (FIFO) здесь не работает — заставила бы делать print job первой, потом compile, тратя время на низкоприоритетную работу.

</Idea>

<DiagramFrame
  label="Очередь с приоритетом на основе мин-кучи. Четыре элемента входят в произвольном порядке — compile приоритет 5, deploy приоритет 2, test приоритет 3, documentation приоритет 7. Внутри куча держит наименьший приоритет наверху: deploy 2 это корень, с test 3 и compile 5 как его дети и documentation 7 ниже. extractMin каждый раз удаляет корень, поэтому элементы выходят в порядке приоритета: deploy 2, затем test 3, затем compile 5, затем documentation 7 — хотя вставлены в другом порядке."
  hue="var(--d-systems)"
  caption="Вставляй в любом порядке; удаляй по приоритету. Куча переупорядочивает внутри так, что extractMin всегда возвращает элемент с наименьшим приоритетом. Порядок прихода (compile, deploy, test, doc) не совпадает с порядком выхода (deploy, test, compile, doc) — приоритет решает, кто уходит первым."
>
  <svg viewBox="0 0 760 360" width="760" height="360" role="img" xmlns="http://www.w3.org/2000/svg" font-family="var(--font-body, sans-serif)">
    <defs>
      <marker id="pq-arrow" viewBox="0 0 10 10" refX="9" refY="5" markerWidth="7" markerHeight="7" orient="auto-start-reverse">
        <path d="M0 0 L10 5 L0 10 z" fill="var(--d-systems)" />
      </marker>
    </defs>

    <text x="100" y="28" text-anchor="middle" font-size="14" font-weight="700" fill="var(--ink, currentColor)">insert (любой порядок)</text>
    <text x="380" y="28" text-anchor="middle" font-size="14" font-weight="700" fill="var(--ink, currentColor)">мин-куча</text>
    <text x="660" y="28" text-anchor="middle" font-size="14" font-weight="700" fill="var(--ink, currentColor)">extractMin (по приоритету)</text>

    <rect x="30" y="52" width="140" height="34" rx="6" fill="var(--card, transparent)" stroke="var(--hairline-2, currentColor)" stroke-width="1" />
    <text x="100" y="74" text-anchor="middle" font-size="13" fill="var(--ink, currentColor)">compile · п5</text>
    <rect x="30" y="96" width="140" height="34" rx="6" fill="var(--card, transparent)" stroke="var(--hairline-2, currentColor)" stroke-width="1" />
    <text x="100" y="118" text-anchor="middle" font-size="13" fill="var(--ink, currentColor)">deploy · п2</text>
    <rect x="30" y="140" width="140" height="34" rx="6" fill="var(--card, transparent)" stroke="var(--hairline-2, currentColor)" stroke-width="1" />
    <text x="100" y="162" text-anchor="middle" font-size="13" fill="var(--ink, currentColor)">test · п3</text>
    <rect x="30" y="184" width="140" height="34" rx="6" fill="var(--card, transparent)" stroke="var(--hairline-2, currentColor)" stroke-width="1" />
    <text x="100" y="206" text-anchor="middle" font-size="13" fill="var(--ink, currentColor)">documentation · п7</text>
    <text x="100" y="248" text-anchor="middle" font-size="11" fill="var(--muted, currentColor)">порядок прихода</text>

    <line x1="178" y1="135" x2="288" y2="120" stroke="var(--d-systems)" stroke-width="1.5" marker-end="url(#pq-arrow)" />

    <line x1="380" y1="92" x2="332" y2="148" stroke="var(--hairline-2, currentColor)" stroke-width="1.5" />
    <line x1="380" y1="92" x2="428" y2="148" stroke="var(--hairline-2, currentColor)" stroke-width="1.5" />
    <line x1="332" y1="172" x2="332" y2="228" stroke="var(--hairline-2, currentColor)" stroke-width="1.5" />

    <circle cx="380" cy="80" r="26" fill="var(--card, transparent)" stroke="var(--d-systems)" stroke-width="2.5" />
    <text x="380" y="78" text-anchor="middle" font-size="12" fill="var(--ink, currentColor)">deploy</text>
    <text x="380" y="92" text-anchor="middle" font-size="11" font-weight="700" fill="var(--d-systems)">п2</text>
    <text x="380" y="44" text-anchor="middle" font-size="11" fill="var(--muted, currentColor)">корень = мин</text>

    <circle cx="332" cy="160" r="24" fill="var(--card, transparent)" stroke="var(--hairline-2, currentColor)" stroke-width="1.5" />
    <text x="332" y="158" text-anchor="middle" font-size="11" fill="var(--ink, currentColor)">test</text>
    <text x="332" y="171" text-anchor="middle" font-size="11" font-weight="700" fill="var(--ink, currentColor)">п3</text>

    <circle cx="428" cy="160" r="24" fill="var(--card, transparent)" stroke="var(--hairline-2, currentColor)" stroke-width="1.5" />
    <text x="428" y="158" text-anchor="middle" font-size="11" fill="var(--ink, currentColor)">compile</text>
    <text x="428" y="171" text-anchor="middle" font-size="11" font-weight="700" fill="var(--ink, currentColor)">п5</text>

    <circle cx="332" cy="240" r="24" fill="var(--card, transparent)" stroke="var(--hairline-2, currentColor)" stroke-width="1.5" />
    <text x="332" y="238" text-anchor="middle" font-size="10" fill="var(--ink, currentColor)">doc</text>
    <text x="332" y="251" text-anchor="middle" font-size="11" font-weight="700" fill="var(--ink, currentColor)">п7</text>

    <text x="380" y="300" text-anchor="middle" font-size="11" fill="var(--muted, currentColor)">приоритет родителя &lt;= детей</text>

    <line x1="472" y1="100" x2="582" y2="100" stroke="var(--d-systems)" stroke-width="1.5" marker-end="url(#pq-arrow)" />
    <text x="527" y="92" text-anchor="middle" font-size="10" fill="var(--muted, currentColor)">снять корень</text>

    <rect x="590" y="52" width="140" height="34" rx="6" fill="var(--card, transparent)" stroke="var(--d-systems)" stroke-width="1.5" />
    <text x="660" y="74" text-anchor="middle" font-size="13" fill="var(--ink, currentColor)">1. deploy · п2</text>
    <rect x="590" y="96" width="140" height="34" rx="6" fill="var(--card, transparent)" stroke="var(--hairline-2, currentColor)" stroke-width="1" />
    <text x="660" y="118" text-anchor="middle" font-size="13" fill="var(--ink, currentColor)">2. test · п3</text>
    <rect x="590" y="140" width="140" height="34" rx="6" fill="var(--card, transparent)" stroke="var(--hairline-2, currentColor)" stroke-width="1" />
    <text x="660" y="162" text-anchor="middle" font-size="13" fill="var(--ink, currentColor)">3. compile · п5</text>
    <rect x="590" y="184" width="140" height="34" rx="6" fill="var(--card, transparent)" stroke="var(--hairline-2, currentColor)" stroke-width="1" />
    <text x="660" y="206" text-anchor="middle" font-size="13" fill="var(--ink, currentColor)">4. documentation · п7</text>
    <text x="660" y="248" text-anchor="middle" font-size="11" fill="var(--muted, currentColor)">порядок выхода = приоритет</text>
  </svg>
</DiagramFrame>

<Code lang="ru">
**Интерфейс очереди с приоритетом в коде**

<AnnotatedCode
  code={`class MinPriorityQueue {
  constructor() {
    this.heap = [];
  }

  // Вставь элемент со значением (меньше = выше приоритет)
  insert(element, value) {
    this.heap.push({ element, value });
    this.siftUp(this.heap.length - 1);
  }

  // Удали и верни элемент с наименьшим значением
  extractMin() {
    const root = this.heap[0];
    this.heap[0] = this.heap[this.heap.length - 1];
    this.heap.pop();
    if (this.heap.length > 0) {
      this.siftDown(0);
    }
    return root.element;
  }

  // Подглядни элемент с наименьшим значением без удаления
  peek() {
    return this.heap.length > 0 ? this.heap[0].element : null;
  }

  // Проверь если пуста
  isEmpty() {
    return this.heap.length === 0;
  }

  // Количество элементов
  size() {
    return this.heap.length;
  }

  // Просей вверх чтобы восстановить свойство кучи после вставки
  siftUp(index) {
    while (index > 0) {
      const parentIndex = Math.floor((index - 1) / 2);
      if (this.heap[index].value < this.heap[parentIndex].value) {
        [this.heap[index], this.heap[parentIndex]] = [this.heap[parentIndex], this.heap[index]];
        index = parentIndex;
      } else {
        break;
      }
    }
  }

  // Просей вниз чтобы восстановить свойство кучи после удаления
  siftDown(index) {
    while (true) {
      const left = 2 * index + 1;
      const right = 2 * index + 2;
      let smallest = index;

      if (left < this.heap.length && this.heap[left].value < this.heap[smallest].value) {
        smallest = left;
      }
      if (right < this.heap.length && this.heap[right].value < this.heap[smallest].value) {
        smallest = right;
      }

      if (smallest !== index) {
        [this.heap[index], this.heap[smallest]] = [this.heap[smallest], this.heap[index]];
        index = smallest;
      } else {
        break;
      }
    }
  }
}`}
  annotations={[
    { line: 1, text: "MinPriorityQueue обёртывает массив кучи." },
    { line: 5, text: "Вставь элемент с приоритет значением. Меньше значение = выше приоритет." },
    { line: 9, text: "extractMin удаляет и возвращает элемент с наименьшим значением (корень мин-кучи)." },
    { line: 17, text: "peek возвращает элемент с наименьшим значением без удаления." },
    { line: 29, text: "siftUp восстанавливает свойство кучи после вставки, двигая элемент вверх." },
    { line: 39, text: "siftDown восстанавливает свойство кучи после удаления, двигая элемент вниз." }
  ]}
/>

**Использование очереди с приоритетом**

<CodeRunner
  code={`class MinPriorityQueue {
  constructor() {
    this.heap = [];
  }

  insert(element, value) {
    this.heap.push({ element, value });
    this.siftUp(this.heap.length - 1);
  }

  extractMin() {
    const root = this.heap[0];
    this.heap[0] = this.heap[this.heap.length - 1];
    this.heap.pop();
    if (this.heap.length > 0) {
      this.siftDown(0);
    }
    return root.element;
  }

  peek() {
    return this.heap.length > 0 ? this.heap[0].element : null;
  }

  isEmpty() {
    return this.heap.length === 0;
  }

  siftUp(index) {
    while (index > 0) {
      const parentIndex = Math.floor((index - 1) / 2);
      if (this.heap[index].value < this.heap[parentIndex].value) {
        [this.heap[index], this.heap[parentIndex]] = [this.heap[parentIndex], this.heap[index]];
        index = parentIndex;
      } else {
        break;
      }
    }
  }

  siftDown(index) {
    while (true) {
      const left = 2 * index + 1;
      const right = 2 * index + 2;
      let smallest = index;

      if (left < this.heap.length && this.heap[left].value < this.heap[smallest].value) {
        smallest = left;
      }
      if (right < this.heap.length && this.heap[right].value < this.heap[smallest].value) {
        smallest = right;
      }

      if (smallest !== index) {
        [this.heap[index], this.heap[smallest]] = [this.heap[smallest], this.heap[index]];
        index = smallest;
      } else {
        break;
      }
    }
  }
}

// Пример планирования задач
const pq = new MinPriorityQueue();

// Добавь задачи с приоритетами (меньше число = выше приоритет)
pq.insert("compile", 5);
pq.insert("deploy", 2);
pq.insert("test", 3);
pq.insert("documentation", 7);

console.log("Обрабатываем задачи в порядке приоритета:");
while (!pq.isEmpty()) {
  const task = pq.extractMin();
  console.log("Выполняю:", task);
}

// Посмотри что следующее без удаления
const pq2 = new MinPriorityQueue();
pq2.insert("urgent", 1);
pq2.insert("normal", 5);
console.log("Следующая задача:", pq2.peek());`}
  expected={`Обрабатываем задачи в порядке приоритета:
Выполняю: deploy
Выполняю: test
Выполняю: compile
Выполняю: documentation
Следующая задача: urgent`}
/>

</Code>

<Trace lang="ru">
<AlgoTrace
  id="pq-insert-trace"
  lang="ru"
  code={[
    "const pq = new MinPriorityQueue();",
    "pq.insert('task1', 5);",
    "pq.insert('task2', 2);",
    "pq.insert('task3', 1);"
  ]}
  frames={[
    { caption: "Вставь 'task1' с приоритетом 5. Куча: [(task1, 5)].", line: 2, cells: [{ value: "(task1,5)" }] },
    { caption: "Вставь 'task2' с приоритетом 2. Добавь: [(task1,5), (task2,2)]. Просей вверх: 2 < 5, обмен.", line: 3, cells: [{ value: "(task2,2)" }, { value: "(task1,5)" }] },
    { caption: "Вставь 'task3' с приоритетом 1. Добавь: [(task2,2), (task1,5), (task3,1)]. Просей вверх с индекса 2.", line: 4, cells: [{ value: "(task2,2)" }, { value: "(task1,5)", mark: "active" }, { value: "(task3,1)" }] },
    { caption: "Индекс 2, родитель на индексе 0 это (task2,2). 1 < 2, обмен.", line: 4, cells: [{ value: "(task3,1)", mark: "active" }, { value: "(task1,5)" }, { value: "(task2,2)" }] },
    { caption: "Индекс 0 это корень. Свойство кучи восстановлено: (task3,1) ≤ оба ребёнка. Готово.", line: 4, cells: [{ value: "(task3,1)", mark: "done" }, { value: "(task1,5)" }, { value: "(task2,2)", mark: "done" }] }
  ]}
/>

<AlgoTrace
  id="pq-extract-trace"
  lang="ru"
  code={[
    "const pq = new MinPriorityQueue();",
    "// После вставок: куча имеет [(task3,1), (task1,5), (task2,2)]",
    "const extracted = pq.extractMin();"
  ]}
  frames={[
    { caption: "Куча: [(task3,1), (task1,5), (task2,2)]. extractMin возвращает (task3,1).", line: 3, cells: [{ value: "(task3,1)", mark: "active" }, { value: "(task1,5)" }, { value: "(task2,2)" }] },
    { caption: "Перемести последний элемент (task2,2) в корень. Удали последний. Куча: [(task2,2), (task1,5)].", line: 3, cells: [{ value: "(task2,2)", mark: "active" }, { value: "(task1,5)" }] },
    { caption: "Просей вниз с индекса 0. Ребёнок на индексе 1 это (task1,5). 2 < 5, обмен не нужен.", line: 3, cells: [{ value: "(task2,2)", mark: "done" }, { value: "(task1,5)", mark: "done" }] }
  ]}
/>
</Trace>

<Complexity lang="ru">
**Операции очереди с приоритетом с использованием двоичной кучи**

<Term slug="insert">Insert</Term>: Добавляет новый элемент и вызывает просей вверх. Просей вверх двигает элемент вверх один уровень за обмен, и куча с n элементами имеет высоту O(log n).
- **Время: O(log n)**

<Term slug="extract_min">ExtractMin</Term>: Удаляет корень, двигает последний элемент в корень и вызывает просей вниз. Просей вниз тоже берёт O(log n).
- **Время: O(log n)**

<Term slug="peek">Peek</Term>: Возвращает корень за один доступ к массиву.
- **Время: O(1)**

**IsEmpty** и **size**: Проверяют длину массива.
- **Время: O(1)**

**Почему не другие реализации?**

**Отсортированный массив** может дать O(1) peek, но insert и extract требуют сдвига O(n) элементов — O(n) за операцию.

**Неотсортированный массив** дает O(1) insert, но extractMin требует сканирования всех элементов чтобы найти наименьший — O(n) за операцию.

**Связный список** это O(n) за всё.

**Двоичная куча** балансирует все три: O(log n) insert и extract, O(1) peek. Поэтому стандартный выбор для очередей с приоритетом.

**Пространственная сложность**

Куча использует один массив, так что пространство это O(n) для n элементов.

</Complexity>

<AlgoPractice
  id="practice-priority-queues"
  lessonSlug="03-priority-queues"
  lang="ru"
  problems={[
    {
      kind: "choice",
      prompt: "В очереди с приоритетом на основе двоичной мин-кучи, какая операция гарантировано O(1)?",
      choices: [
        "insert",
        "extractMin",
        "peek",
        "decrease-key (понизить приоритет)"
      ],
      answer: 2,
      hint: "peek просто смотрит на корень, что всегда на индексе 0 в массиве. Один доступ к массиву. insert и extractMin оба требуют просеивание — O(log n)."
    },
    {
      kind: "choice",
      prompt: "Ты строишь планировщик задач. Задачи имеют приоритеты (меньше число = срочнее). Ты хочешь всегда выполнить самую срочную задачу следующей. Что использовать?",
      choices: [
        "Обычную очередь (FIFO)",
        "Стек (LIFO)",
        "Очередь с приоритетом с мин-кучей",
        "Отсортированный список что ищешь линейно"
      ],
      answer: 2,
      hint: "Обычная очередь дает FIFO, что игнорирует приоритет. Мин-куча очередь с приоритетом каждый раз извлекает наименьший приоритет (самый срочный) — ровно что нужно."
    },
    {
      kind: "choice",
      prompt: "Какая разница между абстрактным типом данных (АТД) и реализацией?",
      choices: [
        "АТД это код; реализация это дизайн на бумаге.",
        "АТД это контракт (что операции делают); реализация это как код построен. Один АТД может иметь много реализаций.",
        "АТД и реализация это одно и то же.",
        "АТД быстрее чем реализация."
      ],
      answer: 1,
      hint: "АТД говорит 'ты можешь insert, extractMin и peek.' Двоичная куча это один способ это сделать. Отсортированный массив или красно-чёрное дерево это другие способы — все удовлетворяют контракт АТД."
    },
    {
      kind: "choice",
      prompt: "В алгоритме кратчайшего пути Дейкстры, нужно повторно извлекать вершину с наименьшим расстоянием. Какая это структура данных?",
      choices: [
        "Обычная очередь",
        "Стек",
        "Очередь с приоритетом",
        "Неупорядоченный список"
      ],
      answer: 2,
      hint: "Ты всегда хочешь вершину с минимальным расстоянием, не первую добавленную. Это определение очереди с приоритетом."
    },
    {
      kind: "numeric",
      prompt: "Куча с приоритетом мин-кучи имеет 8 элементов. Сколько шагов (сравнений и обменов) extractMin берёт в худшем случае?",
      answer: 3,
      hint: "Куча с 8 элементами имеет высоту ⌊log₂(8)⌋ = 3 (уровни 0, 1, 2). Просеивание вниз проходит от корня до листа — максимум 3 уровня — значит максимум 3 обмена в худшем случае."
    },
    {
      kind: "choice",
      prompt: "Нужно слить 4 отсортированные списки в один отсортированный список. Какой подход использует очередь с приоритетом?",
      choices: [
        "Возьми первый элемент из каждого списка и вставь в очередь с приоритетом. Повторно извлекай минимум.",
        "Конкатенируй все списки и отсортируй — O(n log n).",
        "Используй обычную очередь чтобы отследить элементы из каждого списка.",
        "Вручную найди минимальный элемент каждый раз, ища все списки."
      ],
      answer: 0,
      hint: "Очередь с приоритетом (мин-куча) позволяет всегда извлечь глобально наименьший элемент среди 4 списков. Это k-way merge алгоритм, что работает в O(n log k) времени где n это общее число элементов и k это число списков."
    }
  ]}
/>

<Inset type="why">
**Почему очереди с приоритетом имеют значение.** Любая реальная система с задачами, событиями или запросами имеет приоритеты. Веб-сервер имеет запросы от пользователей; некоторые важнее (платящие клиенты первыми). Робот имеет датчики и исполнительные элементы; срочные события безопасности имеют приоритет. Больница имеет пациентов в очереди; критические случаи видены первыми. Без очереди с приоритетом, ты или обрабатываешь вещи случайно (несправедливо, неэффективно) или вручную ищешь самую важную вещь (медленно, O(n)). Очередь с приоритетом решает оба: всегда дай мне самую важную вещь, быстро.
</Inset>

<Inset type="mistake">
**Смешивание АТД и реализации.** Очередь с приоритетом это НЕ куча. Куча это один способ реализовать очередь с приоритетом. Если кто-то спросит «Является ли очередь с приоритетом кучей?», ответ это «Нет, очередь с приоритетом это АТД; куча это реализация этого АТД.» Это отличие имеет значение, потому что на интервью ты можешь сказать «Я буду использовать очередь с приоритетом, поддержанную кучей» чтобы понятно показать, что ты думаешь обоих уровнях.
</Inset>

<Inset type="edgecase">
**Понижение приоритета.** Некоторые проблемы с очередью с приоритетом (вроде Дейкстры) нужно понизить приоритет элемента после вставки. Двоичная куча это не поддерживает напрямую — пришлось бы перестроить или использовать более сложную реализацию (куча Фибоначчи, или вставить заново с новым приоритетом и пропустить старый). Для этого урока, предполагаем простую очередь с приоритетом что поддерживает только insert, extractMin и peek.
</Inset>

<Check lang="ru">
  <Quiz
    id="quiz-priority-queues"
    pieceSlug="03-priority-queues"
    lang="ru"
    question="Объясни отношение между очередью с приоритетом (АТД) и двоичной кучей (реализация). Какие операции должен поддерживать АТД и почему куча хороший выбор?"
    choices={[
      {
        label: "Очередь с приоритетом это абстрактный тип данных, что задаёт интерфейс: insert, extractMin (или extractMax) и peek. Двоичная куча это конкретная реализация, что удовлетворяет этот интерфейс, сохраняя элементы в полном двоичном дереве (на основе массива), держа свойство кучи. Куча хороший выбор, потому что достигает O(log n) insert и extract, O(1) peek и использует O(n) пространство без указателей.",
        correct: true
      },
      {
        label: "Очередь с приоритетом и двоичная куча это одно и то же. Оба поддерживают insert и extractMin.",
        misconception: "Очередь с приоритетом это АТД (контракт); куча это реализация (конкретный код). Другие реализации существуют: отсортированные массивы, красно-чёрные деревья и другие."
      },
      {
        label: "Очередь с приоритетом полезна только для маленьких наборов данных, потому что требует O(log n) за операцию.",
        misconception: "O(log n) это очень быстро. Для n=1 миллион, log₂(n) ≈ 20. Это 20 сравнений за insert или extract — намного лучше чем O(n)."
      },
      {
        label: "Очередь с приоритетом на двоичной куче может быть использована везде где нужна очередь или стек, потому что поддерживает любой порядок.",
        misconception: "Очередь с приоритетом всегда извлекает по приоритету, не по порядку вставки (как очередь) или последний-вход-первый-выход (как стек). Это решает другую проблему."
      }
    ]}
  />
</Check>

<Recap lang="ru">
**Очередь с приоритетом** это <Term slug="abstract_data_type">абстрактный тип данных</Term> — контракт, что обещает три операции: <Term slug="insert">insert</Term> новый элемент, <Term slug="extract_min">extractMin</Term> чтобы удалить и вернуть элемент с наивысшим приоритетом, и <Term slug="peek">peek</Term> чтобы посмотреть элемент с наивысшим приоритетом без удаления. **Двоичная куча** это стандартная <Term slug="heap">реализация</Term>, дающая O(log n) insert и extract, O(1) peek. Понимай разницу: АТД это интерфейс; реализация это код. Очередь с приоритетом это не FIFO очередь и не LIFO стек — она всегда дает тебе самую важную вещь следующей. Используй очередь с приоритетом когда нужно всегда обрабатывать самый срочный элемент: планирование задач, кратчайший путь Дейкстры, симуляция событий, слияние отсортированных потоков. **Следующее:** Урок 04 применяет очередь с приоритетом к проблемам top-K, и урок 05 показывает k-way merge.
</Recap>
