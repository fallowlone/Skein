---
concepts:
- heap-property
- build-heap-complexity
- priority-queue-adt
- top-k
- k-way-merge
- min-heap-vs-max-heap
estMin: 13
lang: ru
level: middle
order: 7
prereqs: []
slug: quiz-choice
sources:
- https://en.wikipedia.org/wiki/Binary_heap
- https://en.wikipedia.org/wiki/Priority_queue
- https://en.wikipedia.org/wiki/K-way_merge_algorithm
status: ready
summary: "Тест с выбором ответа на синтез по всему юниту: инвариант heap, O(n) build-heap, контракт priority queue и выбор нужного heap для top-k и k-way merge."
title: 'Heap: тест с выбором ответа'
track: algorithms
unit: 08-heaps
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Шесть вопросов, проходящих через весь юнит. Ни один не на заучивание определений — каждый это проектное решение: какой heap, какая сложность, какой инвариант. Выберите ответ, который защитили бы на собеседовании, затем прочитайте, почему каждый отвлекающий вариант ошибочен.
</Hook>

<Goal lang="ru">
Убедитесь, что можете связать инвариант heap, аргумент O(n) для build-heap, контракт priority queue и приём «heap размера k», на котором строятся и top-k, и k-way merge — синтез, к которому вели пять уроков.
</Goal>

<Quiz
  id="08-heaps-choice-1"
  lessonSlug="08-heaps"
  lang="ru"
  question="Интервьюер даёт массив [10, 5, 20, 15, 30] и спрашивает: «это корректный min-heap?» Каков верный ответ и почему?"
  choices={[
    { label: "Нет. Читаем как полное дерево: корень 10 на индексе 0 имеет детей 5 (индекс 1) и 20 (индекс 2). 10 > 5 нарушает «родитель ≤ ребёнок». Свойство heap позиционное, проверяется от родителя к детям через индексную арифметику — а не по тому, отсортирован ли массив.", correct: true },
    { label: "Да, потому что массив не отсортирован, а heap по определению это неотсортированный массив.", misconception: "Неотсортированность не критерий. В heap для каждого узла должно выполняться «родитель ≤ оба ребёнка». Здесь 10 > 5 в корне, значит проверка не пройдена — независимо от сортировки." },
    { label: "Нет, потому что массив min-heap должен идти по возрастанию.", misconception: "Min-heap это НЕ отсортированный массив. [1, 3, 2, 5, 4] корректный min-heap, но не отсортирован. Достаточно лишь отношения «родитель–ребёнок» на каждом индексе." },
    { label: "Да, потому что минимальное значение 5 где-то в массиве присутствует.", misconception: "Присутствия минимума мало; в min-heap минимум должен быть в корне (индекс 0). Здесь корень 10, а не 5." },
  ]}
/>

<Quiz
  id="08-heaps-choice-2"
  lessonSlug="08-heaps"
  lang="ru"
  question="Построение heap из массива n элементов через n вызовов push даёт O(n log n), но стандартный build-heap (heapify) — O(n). Откуда ускорение?"
  choices={[
    { label: "build-heap делает sift-down от последнего нелистового узла к корню, поэтому стоимость зависит от высоты каждого узла, а не от высоты дерева. Половина узлов — листья (высота 0), четверть — высота 1 и т.д.; Σ 2^h·(уровни − h) сходится к ≤ 2n. Sift UP (что делает push) стоит полную глубину для множества нижних узлов — это и есть дорогое направление.", correct: true },
    { label: "build-heap пропускает большинство узлов, обрабатывая лишь константное их число.", misconception: "build-heap делает sift-down для каждого нелистового узла — около n/2 узлов. Узлы не пропускаются; выигрыш в том, что узлы у дна (большинство) имеют крошечную высоту поддерева, и каждый sift-down дёшев." },
    { label: "На самом деле оба O(n log n); граница O(n) — миф.", misconception: "build-heap доказуемо O(n). Сумма Σ (n/2^{h+1})·h сходится к константе, умноженной на n, поэтому heapify линеен — стандартный, хорошо известный результат." },
    { label: "build-heap есть O(n) только на уже отсортированном входе.", misconception: "build-heap есть O(n) на ЛЮБОМ входе, отсортированном или нет. Граница идёт из распределения высот узлов, которое не зависит от значений." },
  ]}
/>

<Quiz
  id="08-heaps-choice-3"
  lessonSlug="08-heaps"
  lang="ru"
  question="Коллега говорит: «priority queue — это просто heap». Почему это неточно и когда различие реально важно?"
  choices={[
    { label: "Priority queue это ADT — контракт (insert, extractMin/Max, peek). Binary heap это одна реализация; отсортированный массив, сбалансированное BST или Fibonacci heap тоже выполняют контракт, но с разными профилями сложности. Различие важно, когда часто нужен decrease-key (Dijkstra) — там Fibonacci heap или индексированный binary heap меняет асимптотику.", correct: true },
    { label: "Они взаимозаменяемы; различие академично и на код не влияет.", misconception: "Оно влияет на реальные решения. У обычного binary heap нет эффективного decrease-key, поэтому для Dijkstra на плотных графах нужна другая реализация PQ. Именно отделение контракта от реализации позволяет её заменить." },
    { label: "Priority queue быстрее, чем heap, потому что она высокоуровневее.", misconception: "У ADT нет собственной скорости — скорость даёт реализация. PQ на отсортированном массиве имеет insert за O(n); на heap — за O(log n). ADT это контракт, а не утверждение о производительности." },
    { label: "Heap умеет peek за O(1), а priority queue — нет.", misconception: "peek за O(1) — как раз свойство, которое реализация heap даёт контракту PQ. ADT PQ задаёт peek; heap делает его O(1)." },
  ]}
/>

<Quiz
  id="08-heaps-choice-4"
  lessonSlug="08-heaps"
  lang="ru"
  question="Нужно держать k наибольших элементов из потока n чисел (k ≪ n). Какой heap и почему именно он, а не другой?"
  choices={[
    { label: "MIN-heap размера k. Его корень — наименьший из текущих top-k, поэтому на каждый новый элемент сравниваем с корнем за O(1) и вытесняем за O(log k), если новичок больше. Итого O(n log k), память O(k). Max-heap держал бы наибольший в корне, скрывая кандидата на вытеснение и заставляя сканировать за O(k) в поисках минимума.", correct: true },
    { label: "MAX-heap размера k, чтобы наибольший из top-k всегда был виден в корне.", misconception: "Для k НАИБОЛЬШИХ нужно вытеснять НАИМЕНЬШЕГО кандидата при переполнении. Max-heap скрывает минимум, поэтому поиск вытесняемого стоит O(k). Min-heap держит вытесняемого в корне — в этом весь приём." },
    { label: "Max-heap размера n, затем pop k раз.", misconception: "Это O(n + k log n) времени и O(n) памяти — вы сохранили всё. Min-heap размера k даёт O(n log k) времени и O(k) памяти, что и нужно при k ≪ n и потоке, который нельзя удержать целиком." },
    { label: "Любой heap работает одинаково; выбор стилистический.", misconception: "Выбор меняет стоимость вытеснения с O(log k) (min-heap, вытесняемый в корне) на O(k) (max-heap, нужно сканировать минимум). Это решение о корректной сложности, а не стиль." },
  ]}
/>

<Quiz
  id="08-heaps-choice-5"
  lessonSlug="08-heaps"
  lang="ru"
  question="В k-way merge из k отсортированных списков с n элементами всего каждая запись в heap хранит (value, listIndex, position), а не только значение. Зачем эта учётная информация?"
  choices={[
    { label: "После извлечения минимума нужно добавить СЛЕДУЮЩИЙ элемент из того же списка, чтобы сохранить инвариант «по одному кандидату от каждого списка в heap». listIndex/position говорит, какой список продвинуть. Без этого нельзя узнать, откуда пришло извлечённое значение, и merge сломается.", correct: true },
    { label: "Это позволяет heap сначала отсортировать списки по длине.", misconception: "Длина списка не важна для merge. Учётная информация нужна, чтобы при удалении головы заменить её следующим элементом именно этого списка — сохраняя ровно по одному кандидату от каждого активного списка." },
    { label: "Это нужно лишь для обнаружения дубликатов значений между списками.", misconception: "Дубликаты сливаются и без дополнительных метаданных. Задача метаданных — продвижение: знать, из какого списка взять преемника после каждого извлечения." },
    { label: "Хранение метаданных и делает алгоритм O(n log k) вместо O(n log n).", misconception: "Граница O(n log k) идёт из того, что heap держит не более k элементов, поэтому каждый pop/push есть O(log k). Метаданные нужны для корректности (продвижения нужного списка), а не для границы сложности." },
  ]}
/>

<Quiz
  id="08-heaps-choice-6"
  lessonSlug="08-heaps"
  lang="ru"
  question="При pop из min-heap последний элемент массива переносится в корень, массив уменьшается на один, затем делается sift-down. Почему поднимают ПОСЛЕДНИЙ элемент, а не продвигают меньшего ребёнка в образовавшуюся дыру?"
  choices={[
    { label: "Продвижение детей вверх оставило бы дыру, которая дойдёт до произвольного листа и может нарушить форму полного дерева (заполнение не слева направо). Перенос последнего элемента в корень держит массив непрерывным — форма остаётся полным деревом — и один sift-down восстанавливает порядок за O(log n).", correct: true },
    { label: "Потому что последний элемент всегда второй по малости и потому ему место в корне.", misconception: "У последнего элемента нет особого ранга — это просто крайний правый лист. Его берут, потому что его удаление сохраняет форму полного дерева, затем sift-down чинит порядок. Обычно он крупный, поэтому и опускается обратно." },
    { label: "Продвижение детей быстрее, но стандартная библиотека это запрещает.", misconception: "Продвижение детей не запрещено библиотекой; его избегают, потому что оно может нарушить инвариант формы полного бинарного дерева, на котором держится индексная арифметика массива." },
    { label: "Любой подход годится; оба сохраняют heap корректным.", misconception: "Только перенос последнего элемента сохраняет форму полного дерева, которая нужна представлению массивом (без внутренних дыр). Продвижение детей может создать дыру не в конце, ломая индексные формулы." },
  ]}
/>

<Recap lang="ru">
Сквозная линия юнита — одна структура, выполняющая много ролей. Свойство heap плюс форма полного дерева дают peek за O(1) и push/pop за O(log n) через sift-up и sift-down; build-heap использует распределение высот и достигает O(n). Оберните это в контракт priority queue — и та же машина становится планировщиком, фильтром top-k (min-heap размера k, вытесняем корень) и k-way merge (по одному кандидату от списка, продвигаем по listIndex). Каждый вопрос здесь был по сути про «какой инвариант или какой heap» — ровно то решение, что вы принимаете на собеседовании или в реальной системе.
</Recap>
