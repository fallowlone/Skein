---
concepts:
- sift-up
- sift-down
- build-heap
- top-k
- heapify-complexity
- last-non-leaf
estMin: 14
lang: ru
level: middle
order: 9
prereqs: []
slug: quiz-code
sources:
- https://en.wikipedia.org/wiki/Binary_heap
- https://en.wikipedia.org/wiki/Selection_algorithm
- https://en.wikipedia.org/wiki/Heapsort
status: ready
summary: "Чтение реального кода heap и предсказание поведения: баг sift-up, граница цикла build-heap, top-k через heap размера k и аргумент сложности heapify."
title: 'Heap: чтение кода'
track: algorithms
unit: 08-heaps
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Баги в heap прячутся в индексной арифметике и границах циклов — перевёрнутый знак, off-by-one в последнем нелистовом узле, путаница min/max. Читайте каждый фрагмент как на ревью: проследите инвариант, затем выберите точный дефект или верное утверждение.
</Hook>

<Goal lang="ru">
Потренируйтесь читать код heap на уровне ревью сеньора: заметить sift-up со сравнением не в ту сторону, обосновать границу цикла build-heap, подтвердить, что heap для top-k правильного размера и ориентации, и защитить, почему heapify есть O(n).
</Goal>

## Фрагмент 1 — sift-up

```js
// Замысел: min-heap. siftUp восстанавливает свойство после push.
function siftUp(heap, i) {
  while (i > 0) {
    const parent = Math.floor((i - 1) / 2);
    if (heap[i] > heap[parent]) {        // <-- сравнение
      [heap[i], heap[parent]] = [heap[parent], heap[i]];
      i = parent;
    } else {
      break;
    }
  }
}
```

<Quiz
  id="08-heaps-code-1"
  lessonSlug="08-heaps"
  lang="ru"
  question="Это должно поддерживать MIN-heap, но после push свойство нарушается. В чём дефект?"
  choices={[
    { label: "Сравнение перевёрнуто. В min-heap ребёнок должен быть МЕНЬШЕ родителя, чтобы всплывать вверх, поэтому условие обмена должно быть heap[i] < heap[parent]. Как написано, обмен идёт, когда ребёнок больше — это правило для max-heap, поэтому строится max-heap, а не min-heap.", correct: true },
    { label: "Индекс родителя должен быть Math.floor(i / 2), а не Math.floor((i - 1) / 2).", misconception: "Для массива с индексацией от 0 родитель i — это ровно floor((i-1)/2). floor(i/2) — формула для индексации от 1 и здесь неверна. Арифметика родителя корректна; баг — направление сравнения." },
    { label: "Цикл должен идти, пока i >= 0, а не i > 0.", misconception: "При i = 0 вы в корне, у которого нет родителя — остановка на i > 0 верна. Идти до i >= 0 значило бы вычислить родителя корня и выйти за границы. Реальный баг — направление сравнения." },
    { label: "Бага нет; min-heap поддерживается корректно.", misconception: "Обмен идёт, когда heap[i] > heap[parent], поднимая бо́льшие значения наверх — это поддерживает MAX-heap. Для min-heap условие должно быть heap[i] < heap[parent]." },
  ]}
/>

## Фрагмент 2 — build-heap

```js
function buildHeap(arr) {           // sift-down каждого нелистового узла снизу вверх
  const start = Math.floor(arr.length / 2) - 1;
  for (let i = start; i >= 0; i--) {
    siftDown(arr, i);
  }
  return arr;
}
```

<Quiz
  id="08-heaps-code-2"
  lessonSlug="08-heaps"
  lang="ru"
  question="Почему цикл стартует с Math.floor(n/2) - 1 и идёт вниз, и что сломается, если стартовать с 0 и идти вверх?"
  choices={[
    { label: "Math.floor(n/2) - 1 — это последний нелистовой узел; все индексы за ним — листья, которые уже тривиально корректный heap и не требуют sift-down. Движение снизу вверх гарантирует, что при sift-down узла i оба его поддерева уже корректные heap — предусловие, на которое опирается sift-down. Старт с 0 сверху вниз делал бы sift-down в ещё не heapify-нутые поддеревья, и результат может нарушить свойство heap.", correct: true },
    { label: "Старт с n/2 - 1 — лишь оптимизация; старт с n - 1 вниз тоже корректен, просто медленнее.", misconception: "Индексы за floor(n/2)-1 — листья без детей, поэтому sift-down на них пустой — включение их лишь тратит константный множитель, но безвредно. Настоящая причина границы в том, что обработка должна идти снизу вверх, чтобы поддеревья уже были корректны; это и гарантирует движение вниз от последнего нелистового." },
    { label: "Должно стартовать с 0 и идти вверх; фрагмент как написан содержит баг.", misconception: "Снизу вверх (от последнего нелистового к корню) — корректный heapify. Сверху вниз с sift-down ломает предусловие, что поддеревья узла уже heap, поэтому массив может остаться не heapify-нутым." },
    { label: "Граница n/2 - 1 неверна; последний нелистовой узел на n/2.", misconception: "Для массивов с индексацией от 0 последний узел — n-1, его родитель — floor((n-1-1)/2) = floor(n/2)-1, это последний узел, имеющий ребёнка. Значит floor(n/2)-1 верно." },
  ]}
/>

## Фрагмент 3 — top-k

```js
function topKLargest(nums, k) {
  const h = new MinHeap();              // min-heap, корень — наименьший
  for (const x of nums) {
    h.push(x);
    if (h.size() > k) h.pop();          // выбрасываем текущий минимум
  }
  return h.toArray();                   // k наибольших (в порядке heap)
}
```

<Quiz
  id="08-heaps-code-3"
  lessonSlug="08-heaps"
  lang="ru"
  question="Для nums = [4, 1, 7, 3, 8, 5] и k = 3 что держит heap в конце и каково время работы?"
  choices={[
    { label: "Heap держит {5, 7, 8} (в некотором порядке heap); его корень 5 — 3-й по величине. Каждый из n элементов делает push и не более одного pop на heap размера ≤ k+1, поэтому работа O(n log k); память O(k).", correct: true },
    { label: "Heap держит {1, 3, 4} — три наименьших — потому что это min-heap.", misconception: "Min-heap используют именно для того, чтобы НАИМЕНЬШИЙ текущий кандидат был в корне и выбрасывался при переполнении сверх k. После всех pop выжившие — k НАИБОЛЬШИХ: {5, 7, 8}." },
    { label: "Держит {4, 7, 8}; сохраняются первые три вставленных.", misconception: "Порядок вставки не решает выживание. Каждое переполнение выбрасывает текущий минимум, поэтому 4 (выброшен, когда доминируют 5,7,8) не выживает. Остаются три наибольших: {5, 7, 8}." },
    { label: "Время работы O(n log n), потому что каждый push идёт в heap размера n.", misconception: "Heap никогда не превышает k+1 элементов (при переполнении возвращается к k), поэтому каждый push/pop есть O(log k), что даёт O(n log k) — лучше O(n log n) при k ≪ n." },
  ]}
/>

## Фрагмент 4 — сложность heapify

```text
Heap, построенный снизу вверх, вызывает sift-down по разу на нелистовой узел.
Число узлов по высоте h (всего n):  ~n/2 при h=0, n/4 при h=1, n/8 при h=2, ...
стоимость sift-down из узла высоты h есть O(h).
Полная работа  T(n) = Σ_h (n / 2^(h+1)) · h
```

<Quiz
  id="08-heaps-code-4"
  lessonSlug="08-heaps"
  lang="ru"
  question="Вычисляя сумму T(n) = Σ_h (n / 2^(h+1)) · h, какова сложность build-heap и почему интуиция «n/2 sift-down × O(log n) каждый = O(n log n)» ошибочна?"
  choices={[
    { label: "T(n) = O(n). Ряд Σ h/2^h сходится к константе (равной 2), поэтому T(n) ≈ n · (константа) = O(n). Интуиция O(n log n) пересчитывает: лишь немногие узлы у корня имеют высоту ≈ log n; основная масса узлов у листьев с высотой 0 или 1, и их sift-down есть O(1).", correct: true },
    { label: "T(n) = O(n log n), потому что внутренних узлов n/2 и каждый sift-down есть O(log n).", misconception: "Это считает каждый внутренний узел высоты log n. Но ~n/2 — листья (высота 0), ~n/4 — высота 1 и т.д.; взвешенная сумма Σ h/2^h сходится, давая O(n), а не O(n log n)." },
    { label: "T(n) = O(log n), потому что высота heap — log n.", misconception: "build-heap затрагивает Θ(n) узлов, поэтому не может быть сублинейным. Итог — O(n) (линейный), а не O(log n)." },
    { label: "T(n) = O(n²), потому что каждый из n узлов может опускаться через n уровней.", misconception: "Heap из n узлов имеет высоту ⌈log₂ n⌉, а не n, поэтому ни один узел не опускается более чем на O(log n) уровней. Взвешенная по высотам сумма сходится к O(n)." },
  ]}
/>

<Recap lang="ru">
Корректность heap живёт в деталях, которые читаешь построчно: сравнение в sift-up должно соответствовать min или max (ребёнок &lt; родитель для min-heap); build-heap должен идти снизу вверх от последнего нелистового на floor(n/2)-1, чтобы поддеревья каждого узла уже были heap; top-k использует MIN-heap размера k и выбрасывает корень при переполнении, оставляя k наибольших за O(n log k); а heapify есть O(n), потому что взвешенная по высотам сумма узлов Σ h/2^h сходится — большинство узлов это дешёвые листья. Проследите инвариант, прежде чем довериться коду.
</Recap>
