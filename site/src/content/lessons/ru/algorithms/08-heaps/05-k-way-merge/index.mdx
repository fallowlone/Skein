---
concepts:
- heap
- priority_queue
- merge
- k_way_merge
- sorted_lists
- streaming
- time_complexity
- space_complexity
- min_heap
estMin: 36
lang: ru
mathPrereqs: []
order: 5
prereqs:
- 08-heaps/01-the-heap
- 08-heaps/02-heap-operations
- 08-heaps/03-priority-queues
- 08-heaps/04-top-k
slug: 05-k-way-merge
sources:
- https://en.wikipedia.org/wiki/K-way_merge_algorithm
- https://emre.me/coding-patterns/k-way-merge/
- https://leetcode.com/problems/merge-k-sorted-lists/
status: ready
summary: 'Объедини k отсортированных списков: мин-куча с головой каждого, вытащи минимум,
  добавь в вывод, добавь следующий из того же списка. O(n log k) время, O(k) память.
  Лучше наивного O(nk).'
title: K-путевое слияние и слияние отсортированных списков
track: algorithms
unit: 08-heaps
---



import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Idea from "~/components/lesson/Idea.astro";
import Code from "~/components/lesson/Code.astro";
import Trace from "~/components/lesson/Trace.astro";
import Complexity from "~/components/lesson/Complexity.astro";
import Check from "~/components/lesson/Check.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import AnnotatedCode from "~/components/algo/AnnotatedCode.astro";
import AlgoTrace from "~/components/algo/AlgoTrace.astro";
import CodeRunner from "~/components/algo/CodeRunner.astro";
import AlgoPractice from "~/components/algo/AlgoPractice.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";
import Term from "~/components/prose/Term.astro";
import DiagramFrame from "~/components/diagram/DiagramFrame.astro";

<Hook>
У тебя есть k отсортированных списков. Задача: объединить их в один отсортированный список. Наивный ответ: сканируй голову всех k списков, найди наименьший, добавь его к выводу, двигайся вперёд. Повторяй до истощения. Но сканирование k голов за каждый шаг это O(nk) — медленно. Ключевое: держи <Term slug="min_heap">мин-кучу размера k</Term> с головой каждого списка. Вытащи наименьшую голову, добавь её, добавь следующий элемент из того же списка. Куча держит k наименьших кандидатов на вершине, так что каждый вытаск/добавь это O(log k). Итоговое время: O(n log k). Память: O(k). Это паттерн k-путевого слияния.
</Hook>

<Goal lang="ru">
После этого урока ты можешь решить проблему слияния k отсортированных списков используя мин-кучу: инициализируй кучу с головой каждого списка (отслеживая какой список каждая голова представляет), вытащи наименьший, добавь к выводу, и добавь следующий элемент из того же списка. Ты понимаешь почему это работает: куча всегда держит одного кандидата из каждого из k списков, и корень всегда это общий минимум в этот момент. Ты можешь объяснить сложность времени O(n log k) и сложность памяти O(k), и почему это бьёт наивный подход O(nk). Ты можешь узнать форму «объедини k отсортированных X» на LeetCode (связные списки, массивы, итераторы), отследить алгоритм шаг за шагом на конкретном вводе, и кодировать его. Ты понимаешь варианты наименьший-диапазон и объединение-отсортированных-массивов. Это твой финальный урок куч; дальше графы.
</Goal>

<Idea lang="ru">
**Проблема: объедини k отсортированных списков**

Дано k отсортированных списков. Задача: объединить их в один отсортированный вывод список, сохраняя порядок.

Пример:
- Списки: [1, 4, 5], [1, 3, 4], [2, 6]
- Объединённо: [1, 1, 2, 3, 4, 4, 5, 6]

**Наивный подход: сканируй все головы за каждый шаг**

За каждый шаг:
1. Сканируй голову всех k списков чтобы найти минимум.
2. Добавь минимум к выводу.
3. Двигай указатель в том списке вперёд.

Время: O(n) шагов, и каждый шаг сканирует k голов в O(k). Итого: O(nk). Это медленно когда k большой.

**Ключевое для кучи: держи кучу размера-k голов**

Вместо сканирования всех k голов, используй <Term slug="min_heap">мин-кучу</Term> их отслеживать. Вот алгоритм:

1. Создай мин-кучу.
2. Для каждого из k списков, добавь его голову (первый элемент) в кучу. Храни с каждой записью кучи индекс списка и текущую позицию в том списке, так ты знаешь какой список вытягивать следующим.
3. Пока куча не пуста:
   - Вытащи минимум из кучи.
   - Добавь его к выводу.
   - Если тот список имеет следующий элемент, добавь его в кучу (тот же индекс списка, следующая позиция).
4. Вывод теперь объединённый и отсортированный.

Почему это работает? На любом шаге, куча держит ровно одного кандидата из каждого из k списков. Корень это общий минимум среди этих кандидатов. Когда ты вытаскиваешь его и добавляешь следующий элемент из того же списка, куча держит инвариант: всегда k наименьших кандидатов видимых до сих пор, один за список. Каждый вытаск/добавь это O(log k), и ты делаешь n итого вытасков, так O(n log k) всего.

**Пример: объедини три списка [1, 4, 5], [1, 3, 4], [2, 6]**

Мин-куча держит (значение, индекс_списка, позиция):

1. Инициализируй: добавь (1, список 0, поз 0), (1, список 1, поз 0), (2, список 2, поз 0). Куча: мин это (1, 0, 0).
2. Вытащи (1, 0, 0), добавь 1 к выводу. Добавь (4, 0, 1). Вывод: [1]. Куча мин: (1, 1, 0).
3. Вытащи (1, 1, 0), добавь 1. Добавь (3, 1, 1). Вывод: [1, 1]. Куча мин: (2, 2, 0).
4. Вытащи (2, 2, 0), добавь 2. Добавь (6, 2, 1). Вывод: [1, 1, 2]. Куча мин: (3, 1, 1).
5. Вытащи (3, 1, 1), добавь 3. Добавь (4, 1, 2). Вывод: [1, 1, 2, 3]. Куча мин: (4, 0, 1).
6. Вытащи (4, 0, 1), добавь 4. Добавь (5, 0, 2). Вывод: [1, 1, 2, 3, 4]. Куча мин: (4, 1, 2).
7. Вытащи (4, 1, 2), добавь 4. Список 1 истощен. Вывод: [1, 1, 2, 3, 4, 4]. Куча мин: (5, 0, 2).
8. Вытащи (5, 0, 2), добавь 5. Список 0 истощен. Вывод: [1, 1, 2, 3, 4, 4, 5]. Куча мин: (6, 2, 1).
9. Вытащи (6, 2, 1), добавь 6. Список 2 истощен. Вывод: [1, 1, 2, 3, 4, 4, 5, 6]. Куча пуста. Готово.

Финально: [1, 1, 2, 3, 4, 4, 5, 6]. ✓

**Почему размер кучи остаётся при k**

Максимум, куча держит один элемент из каждого из k списков. Когда ты вытаскиваешь один и добавляешь другой (из того же списка), размер кучи не растёт. Если список истощен, ты не добавляешь, так что куча уменьшается. Куча никогда не превышает k.

<DiagramFrame
  label="Три отсортированных списка слева подают по одной голове каждый в мин-кучу размера 3 в середине; корень кучи это глобальный минимум, который вытаскивается в объединённый вывод справа, после чего следующий элемент из того же списка добавляется обратно в кучу."
  hue="var(--d-systems)"
  caption="Куча держит ровно одну голову на список, так что её корень это наименьший элемент сразу по всем k спискам. Вытащи корень в вывод, затем подтяни следующий элемент из того же списка в кучу. Здесь голова L0 1 это глобальный минимум: выведи её, затем добавь следующий из L0 (4). Повторяй, пока каждый список не исчерпан."
>
  <svg viewBox="0 0 640 360" font-family="var(--font-body)">
    <text x="16" y="24" font-size="13" fill="var(--ink)">k = 3 отсортированных списка</text>
    <text x="262" y="24" font-size="13" fill="var(--ink)">мин-куча размера k</text>
    <text x="494" y="24" font-size="13" fill="var(--ink)">объединённый вывод</text>

    <text x="16" y="68" font-size="11" font-family="var(--font-mono)" fill="var(--muted)">L0</text>
    <rect x="40" y="52" width="34" height="26" rx="5" fill="var(--card-2)" stroke="var(--accent)" stroke-width="1.6" />
    <text x="57" y="70" font-size="12" font-family="var(--font-mono)" fill="var(--accent)" text-anchor="middle">1</text>
    <rect x="78" y="52" width="34" height="26" rx="5" fill="var(--card)" stroke="var(--hairline-strong)" stroke-width="0.8" />
    <text x="95" y="70" font-size="12" font-family="var(--font-mono)" fill="var(--muted)" text-anchor="middle">4</text>
    <rect x="116" y="52" width="34" height="26" rx="5" fill="var(--card)" stroke="var(--hairline-strong)" stroke-width="0.8" />
    <text x="133" y="70" font-size="12" font-family="var(--font-mono)" fill="var(--muted)" text-anchor="middle">5</text>

    <text x="16" y="138" font-size="11" font-family="var(--font-mono)" fill="var(--muted)">L1</text>
    <rect x="40" y="122" width="34" height="26" rx="5" fill="var(--card-2)" stroke="var(--hairline-strong)" stroke-width="1.2" />
    <text x="57" y="140" font-size="12" font-family="var(--font-mono)" fill="var(--ink)" text-anchor="middle">1</text>
    <rect x="78" y="122" width="34" height="26" rx="5" fill="var(--card)" stroke="var(--hairline-strong)" stroke-width="0.8" />
    <text x="95" y="140" font-size="12" font-family="var(--font-mono)" fill="var(--muted)" text-anchor="middle">3</text>
    <rect x="116" y="122" width="34" height="26" rx="5" fill="var(--card)" stroke="var(--hairline-strong)" stroke-width="0.8" />
    <text x="133" y="140" font-size="12" font-family="var(--font-mono)" fill="var(--muted)" text-anchor="middle">4</text>

    <text x="16" y="208" font-size="11" font-family="var(--font-mono)" fill="var(--muted)">L2</text>
    <rect x="40" y="192" width="34" height="26" rx="5" fill="var(--card-2)" stroke="var(--hairline-strong)" stroke-width="1.2" />
    <text x="57" y="210" font-size="12" font-family="var(--font-mono)" fill="var(--ink)" text-anchor="middle">2</text>
    <rect x="78" y="192" width="34" height="26" rx="5" fill="var(--card)" stroke="var(--hairline-strong)" stroke-width="0.8" />
    <text x="95" y="210" font-size="12" font-family="var(--font-mono)" fill="var(--muted)" text-anchor="middle">6</text>

    <text x="40" y="252" font-size="10" font-family="var(--font-mono)" fill="var(--faint)">головы выделены; одна голова на список входит в кучу</text>

    <line x1="154" y1="65" x2="276" y2="120" stroke="var(--muted)" stroke-width="1.3" marker-end="url(#km-arrow)" />
    <line x1="154" y1="135" x2="246" y2="158" stroke="var(--muted)" stroke-width="1.3" marker-end="url(#km-arrow)" />
    <line x1="154" y1="205" x2="316" y2="158" stroke="var(--muted)" stroke-width="1.3" marker-end="url(#km-arrow)" />

    <line x1="286" y1="138" x2="246" y2="186" stroke="var(--hairline-strong)" stroke-width="1.1" />
    <line x1="286" y1="138" x2="326" y2="186" stroke="var(--hairline-strong)" stroke-width="1.1" />
    <circle cx="286" cy="138" r="22" fill="var(--card-2)" stroke="var(--accent)" stroke-width="1.8" />
    <text x="286" y="135" font-size="13" font-family="var(--font-mono)" fill="var(--accent)" text-anchor="middle">1</text>
    <text x="286" y="148" font-size="8.5" font-family="var(--font-mono)" fill="var(--accent)" text-anchor="middle">L0</text>
    <circle cx="246" cy="200" r="22" fill="var(--card)" stroke="var(--hairline-strong)" stroke-width="1" />
    <text x="246" y="197" font-size="13" font-family="var(--font-mono)" fill="var(--ink)" text-anchor="middle">1</text>
    <text x="246" y="210" font-size="8.5" font-family="var(--font-mono)" fill="var(--muted)" text-anchor="middle">L1</text>
    <circle cx="326" cy="200" r="22" fill="var(--card)" stroke="var(--hairline-strong)" stroke-width="1" />
    <text x="326" y="197" font-size="13" font-family="var(--font-mono)" fill="var(--ink)" text-anchor="middle">2</text>
    <text x="326" y="210" font-size="8.5" font-family="var(--font-mono)" fill="var(--muted)" text-anchor="middle">L2</text>
    <text x="262" y="252" font-size="10" font-family="var(--font-mono)" fill="var(--faint)">корень = глобальный минимум по всем спискам</text>

    <line x1="308" y1="132" x2="486" y2="64" stroke="var(--accent)" stroke-width="1.6" marker-end="url(#km-arrow-a)" />
    <text x="404" y="86" font-size="10" font-family="var(--font-mono)" fill="var(--accent)" text-anchor="middle">{"вытащи мин → выведи"}</text>

    <rect x="486" y="50" width="34" height="28" rx="5" fill="var(--card-2)" stroke="var(--accent)" stroke-width="1.6" />
    <text x="503" y="69" font-size="12" font-family="var(--font-mono)" fill="var(--accent)" text-anchor="middle">1</text>
    <rect x="524" y="50" width="34" height="28" rx="5" fill="var(--card)" stroke="var(--hairline-strong)" stroke-width="0.6" stroke-dasharray="4 3" />
    <text x="541" y="69" font-size="12" font-family="var(--font-mono)" fill="var(--faint)" text-anchor="middle">…</text>
    <text x="486" y="104" font-size="10" font-family="var(--font-mono)" fill="var(--faint)">вывод растёт на один элемент за вытаск</text>

    <path d="M 95 82 C 150 108 210 118 262 124" fill="none" stroke="var(--accent)" stroke-width="1.4" stroke-dasharray="5 3" marker-end="url(#km-arrow-a)" />
    <text x="40" y="300" font-size="10.5" font-family="var(--font-mono)" fill="var(--accent)">после вытаскивания головы 1 из L0 добавь следующий из L0 (4) в кучу</text>

    <defs>
      <marker id="km-arrow" viewBox="0 0 10 10" refX="9" refY="5" markerWidth="7" markerHeight="7" orient="auto-start-reverse">
        <path d="M0 0 L10 5 L0 10 z" fill="var(--muted)" />
      </marker>
      <marker id="km-arrow-a" viewBox="0 0 10 10" refX="9" refY="5" markerWidth="7" markerHeight="7" orient="auto-start-reverse">
        <path d="M0 0 L10 5 L0 10 z" fill="var(--accent)" />
      </marker>
    </defs>
  </svg>
</DiagramFrame>

**Флагманская форма: объедини k отсортированных связных списков**

Частая проблема LeetCode: дано k отсортированных связных списков, объедини их. Алгоритм идентичен. Вместо массивов, ты пересекаешь указатели. Каждая запись кучи хранит узел, не просто значение. Ты вытаскиваешь мин узел, добавляешь его, и добавляешь его следующий узел.

**Вариант: наименьший диапазон**

Дано k массивов, найди наименьший диапазон что включает по крайней мере один элемент из каждого массива. Решение: используй подобную кучу отслеживать текущий элемент из каждого массива. Держи минимум и максимум текущих k элементов. Диапазон [мин, макс] это кандидат ответ. Вытащи мин и добавь следующий из того массива. Отслеживай наименьший диапазон видимый. Время: O(n log k).

**Вариант: объедини отсортированные массивы (разные размеры)**

k массивов разных размеров. Объедини их в O(n log k) время используя одинаковый подход куча. n = всего элементов через все массивы.

</Idea>

<Code lang="ru">
**Объединение k отсортированных массивов используя мин-кучу**

<AnnotatedCode
  code={`function mergeKSortedLists(lists) {
  // Мин-куча держит [значение, индекс_списка, позиция]
  const minHeap = new MinPriorityQueue();
  const result = [];
  
  // Добавь голову каждого списка
  for (let i = 0; i < lists.length; i++) {
    if (lists[i].length > 0) {
      // Храни: значение, индекс списка, позиция в том списке
      minHeap.insert([lists[i][0], i, 0], lists[i][0]);
    }
  }
  
  // Вытащи мин, добавь, добавь следующий из того же списка
  while (!minHeap.isEmpty()) {
    const [value, listIdx, pos] = minHeap.extractMin();
    result.push(value);
    
    // Если есть следующий элемент в том списке, добавь его
    if (pos + 1 < lists[listIdx].length) {
      const nextValue = lists[listIdx][pos + 1];
      minHeap.insert([nextValue, listIdx, pos + 1], nextValue);
    }
  }
  
  return result;
}

// Пример
const lists = [[1, 4, 5], [1, 3, 4], [2, 6]];
const merged = mergeKSortedLists(lists);
console.log(merged); // [1, 1, 2, 3, 4, 4, 5, 6]`}
  annotations={[
    { line: 2, text: "Мин-куча где ключ это значение само. Каждая запись это [значение, индекс_списка, позиция]." },
    { line: 7, text: "Для каждого из k списков, добавь первый элемент (если список не пуст)." },
    { line: 8, text: "Храни значение, список откуда оно пришло, и его позицию в том списке." },
    { line: 15, text: "Пока куча не пуста, вытащи минимум значение (всегда в корне)." },
    { line: 19, text: "Добавь к результату." },
    { line: 22, text: "Если тот же список имеет следующий элемент, добавь его. Если нет, тот список истощен." },
    { line: 28, text: "Вывод теперь объединённый и отсортированный." }
  ]}
/>

**Объедини k отсортированных связных списков**

<CodeRunner
  code={`class Node {
  constructor(val, next = null) {
    this.val = val;
    this.next = next;
  }
}

class MinPriorityQueue {
  constructor() {
    this.heap = [];
  }

  insert(item, priority) {
    this.heap.push(item);
    this.siftUp(this.heap.length - 1);
  }

  extractMin() {
    const root = this.heap[0];
    this.heap[0] = this.heap[this.heap.length - 1];
    this.heap.pop();
    if (this.heap.length > 0) {
      this.siftDown(0);
    }
    return root;
  }

  isEmpty() {
    return this.heap.length === 0;
  }

  siftUp(index) {
    while (index > 0) {
      const parentIdx = Math.floor((index - 1) / 2);
      if (this.heap[index][0] < this.heap[parentIdx][0]) {
        [this.heap[index], this.heap[parentIdx]] = [this.heap[parentIdx], this.heap[index]];
        index = parentIdx;
      } else {
        break;
      }
    }
  }

  siftDown(index) {
    while (true) {
      const left = 2 * index + 1;
      const right = 2 * index + 2;
      let smallest = index;

      if (left < this.heap.length && this.heap[left][0] < this.heap[smallest][0]) {
        smallest = left;
      }
      if (right < this.heap.length && this.heap[right][0] < this.heap[smallest][0]) {
        smallest = right;
      }

      if (smallest !== index) {
        [this.heap[index], this.heap[smallest]] = [this.heap[smallest], this.heap[index]];
        index = smallest;
      } else {
        break;
      }
    }
  }
}

function mergeKLinkedLists(lists) {
  const minHeap = new MinPriorityQueue();
  
  // Добавь голову каждого списка
  for (let i = 0; i < lists.length; i++) {
    if (lists[i]) {
      // Храни: [узел, индекс_списка] с приоритет = узел.val
      minHeap.insert([lists[i], i]);
    }
  }
  
  const dummyHead = new Node(-1);
  let current = dummyHead;
  
  while (!minHeap.isEmpty()) {
    const [node, listIdx] = minHeap.extractMin();
    current.next = node;
    current = current.next;
    
    // Если этот узел имеет следующий, добавь его
    if (node.next) {
      minHeap.insert([node.next, listIdx]);
    }
  }
  
  return dummyHead.next;
}

// Пример: создай три связные списки
const list1 = new Node(1, new Node(4, new Node(5)));
const list2 = new Node(1, new Node(3, new Node(4)));
const list3 = new Node(2, new Node(6));

const merged = mergeKLinkedLists([list1, list2, list3]);

// Преобразуй в массив для отображения
const result = [];
let curr = merged;
while (curr) {
  result.push(curr.val);
  curr = curr.next;
}
console.log(result);`}
  expected={`[1, 1, 2, 3, 4, 4, 5, 6]`}
/>

</Code>

<Trace lang="ru">
<AlgoTrace
  id="kway-merge-trace"
  lang="ru"
  code={[
    "const lists = [[1, 4, 5], [1, 3, 4], [2, 6]];",
    "const minHeap = new MinPriorityQueue();",
    "const result = [];",
    "// Инициализируй: добавь головы всех списков",
    "for (const [val, idx] of [[1,0], [1,1], [2,2]]) minHeap.insert(val, idx);",
    "while (!minHeap.isEmpty()) {",
    "  const [val, idx] = minHeap.extractMin();",
    "  result.push(val);",
    "  if (hasNext(lists[idx])) minHeap.insert(nextVal(lists[idx]), idx);",
    "}"
  ]}
  frames={[
    { caption: "Инициализируй: куча = [(1,L0), (1,L1), (2,L2)]. Куча мин: (1, L0).", line: 4, cells: [{ value: "1", mark: "active" }, { value: "1" }, { value: "2" }] },
    { caption: "Вытащи (1, L0), добавь 1. Добавь (4, L0). Куча: [(1,L1), (2,L2), (4,L0)]. Мин: (1, L1).", line: 7, cells: [{ value: "1", mark: "done" }, { value: "1" }, { value: "2" }] },
    { caption: "Вытащи (1, L1), добавь 1. Добавь (3, L1). Куча: [(2,L2), (4,L0), (3,L1)]. Мин: (2, L2).", line: 7, cells: [{ value: "1", mark: "done" }, { value: "1", mark: "done" }, { value: "2" }] },
    { caption: "Вытащи (2, L2), добавь 2. Добавь (6, L2). Куча: [(3,L1), (4,L0), (6,L2)]. Мин: (3, L1).", line: 7, cells: [{ value: "2", mark: "active" }, { value: "2", mark: "active" }, { value: "2" }] },
    { caption: "Вытащи (3, L1), добавь 3. Добавь (4, L1). Куча: [(4,L0), (4,L1), (6,L2)]. Мин: (4, L0).", line: 7, cells: [{ value: "3", mark: "active" }, { value: "3", mark: "active" }, { value: "6" }] },
    { caption: "Вытащи (4, L0), добавь 4. Добавь (5, L0). Куча: [(4,L1), (6,L2), (5,L0)]. Мин: (4, L1).", line: 7, cells: [{ value: "4", mark: "active" }, { value: "4", mark: "active" }, { value: "6" }] },
    { caption: "Вытащи (4, L1), добавь 4. L1 истощен. Куча: [(5,L0), (6,L2)]. Мин: (5, L0).", line: 7, cells: [{ value: "4", mark: "done" }, { value: "4", mark: "done" }, { value: "6" }] },
    { caption: "Вытащи (5, L0), добавь 5. L0 истощен. Куча: [(6,L2)]. Мин: (6, L2).", line: 7, cells: [{ value: "5", mark: "done" }, { value: "5", mark: "done" }, { value: "6" }] },
    { caption: "Вытащи (6, L2), добавь 6. L2 истощен. Куча пуста. Результат: [1,1,2,3,4,4,5,6].", line: 7, cells: [{ value: "6", mark: "done" }, { value: "6", mark: "done" }, { value: "6", mark: "done" }] }
  ]}
/>
</Trace>

<Complexity lang="ru">
**K-путевое слияние с мин-кучей размера k**

У тебя есть n всего элементов через все k списков.

Для каждого из n элементов:
- <Term slug="extract_min">ExtractMin</Term>: O(log k) (куча держит максимум k элементов).
- <Term slug="insert">Insert</Term> (следующий элемент из того же списка): O(log k).

Итого: n элементов × (вытаск + добавь) = n × O(log k) = **O(n log k) время**.

Память: Куча держит максимум k элементов (одна голова из каждого списка), так **O(k) память**.

**Сравнение с наивным подходом**

Наивный: для каждого из n элементов, сканируй все k голов чтобы найти минимум. Время: O(nk). Память: O(1) (если ты не считаешь вход).

K-путевое слияние с кучей: O(n log k) время, O(k) память.

Когда подход кучи лучше?
- Всегда. O(n log k) строго лучше чем O(nk), потому что log k ≤ n для разумного k (и обычно log k ≪ n).
- Пример: n = 1,000,000 элементов, k = 100 списков. O(n log k) = 1,000,000 × 6.6 ≈ 6.6 миллионов операций. O(nk) = 1,000,000 × 100 = 100 миллионов операций. Куча ~15 раз быстрее.

**Почему не другие подходы?**

- Попарное слияние (объедини два списка, потом объедини результат с третьим, и т.д.): O(n log k) но с большими константами. Куча проще.
- Глобально отсортируй все элементы: O(n log n) время, O(n) память. Хуже чем O(n log k) когда k ≪ n.

</Complexity>

<AlgoPractice
  id="practice-kway-merge"
  lessonSlug="05-k-way-merge"
  lang="ru"
  problems={[
    {
      kind: "choice",
      prompt: "Чтобы объединить k отсортированных списков, какая главная цель хранить индекс списка и позицию в каждой записи кучи?",
      choices: [
        "Отсортируй списки по размеру.",
        "Знай какой список вытягивать следующий элемент из после ты вытаскиваешь текущий минимум.",
        "Балансируй кучу так все списки вносят поровну.",
        "Обнаруживай когда список истощен без сканирования."
      ],
      answer: 1,
      hint: "После ты вытаскиваешь минимум и добавляешь его, ты должен знать какой список он пришёл из так ты можешь добавить следующий элемент из того же списка. Индекс списка и позиция говорят тебе точно то."
    },
    {
      kind: "choice",
      prompt: "В алгоритме k-путевого слияния, что гарантирует инвариант кучи на каждом шаге?",
      choices: [
        "Наименьший элемент всего хранится где-то во входных списках.",
        "Корень кучи это наименьший среди текущих голов k списков.",
        "Все k списков были частично потреблены в равных количествах.",
        "Вывод массив отсортирован после каждой вставки."
      ],
      answer: 1,
      hint: "Куча держит одного кандидата из каждого из k списков (его текущая голова). Корень мин-кучи это наименьший среди этих k кандидатов — общий минимум в этот момент."
    },
    {
      kind: "numeric",
      prompt: "Ты объединяешь k = 5 отсортированных списков с n = 1,000,000 всего элементов. Какая приблизительная сложность времени в операциях, используя log₂(5) ≈ 2.3?",
      answer: 2300000,
      hint: "O(n log k) = 1,000,000 × 2.3 ≈ 2,300,000 операций."
    },
    {
      kind: "choice",
      prompt: "Какая сложность памяти алгоритма k-путевого слияния?",
      choices: [
        "O(n), потому что ты хранишь вывод.",
        "O(k), потому что куча держит максимум один элемент из каждого списка.",
        "O(1), потому что ты только используешь указатели.",
        "O(n log k), одинаково как сложность времени."
      ],
      answer: 1,
      hint: "Куча держит максимум k элементов (текущая голова каждого списка). Если ты считаешь только вспомогательную память (не вход или вывод), это O(k). Если ты считаешь вывод, это O(n)."
    },
    {
      kind: "choice",
      prompt: "Дано три списка [1, 5, 9], [2, 6, 10], [3, 7, 11], что это первый элемент вытащен из мин-кучи?",
      choices: [
        "1",
        "2",
        "3",
        "Это зависит от порядка кучи."
      ],
      answer: 0,
      hint: "Инициализируй: добавь (1, L0), (2, L1), (3, L2). Корень мин-кучи это 1, что это минимум среди текущих голов."
    },
    {
      kind: "choice",
      prompt: "После вытаскивания (1, L0) и добавления (5, L0) в предыдущем примере, что это следующий минимум вытащен?",
      choices: [
        "1",
        "2",
        "3",
        "5"
      ],
      answer: 1,
      hint: "После добавления (5, L0), куча держит [(2, L1), (3, L2), (5, L0)]. Корень (минимум) это (2, L1)."
    }
  ]}
/>

<Inset type="why">
**Почему k-путевое слияние имеет значение.** Реальные системы часто нужно объединять отсортированные данные из множественных источников: объединение k файлов лога отсортированных по временам, объединение результатов из k шардов базы отсортированных по ID, объединение k отсортированных итераторов в конвейер данных. Ты не можешь себе позволить хранить все данные в памяти, так ты должен потоком пройтись через один элемент за раз. K-путевая куча позволяет тебе объединять онлайн в O(n log k) время и O(k) память, обрабатывая каждый элемент ровно раз.
</Inset>

<Inset type="mistake">
**Хранение весь остающийся список вместо просто головы.** Это соблазнительно хранить весь хвост списка в куче. Это тратит память и делает сравнения медленно. Вместо, храни только узел/значение само и его индекс списка/позицию. Когда ты вытаскиваешь его, ты вычисляешь или получаешь следующий элемент по требованию. Куча никогда не держит больше чем k элементов.
</Inset>

<Inset type="edgecase">
**Пустые списки.** Если некоторые входные списки пусты, не добавляй их в кучу. Алгоритм правильно обрабатывает k-m непустых списков: размер кучи растёт к количеству непустых списков, и сложность O(n log (k-m)). Если все списки пусты, вывод пуст. Если k = 1, есть только один список, так просто верни его.
</Inset>

<Inset type="practice">
**Вариант наименьший-диапазон.** Дано k массивов, найди наименьший диапазон [a, b] такой что каждый массив вносит по крайней мере один элемент. Держи текущий мин и макс среди k голов. Диапазон [мин, макс] это кандидат ответ. Вытащи мин, добавь следующий из того массива, обновить диапазон. Время: O(n log k). Память: O(k).
</Inset>

<Check lang="ru">
  <Quiz
    id="quiz-kway-merge"
    pieceSlug="05-k-way-merge"
    lang="ru"
    question="Объясни алгоритм k-путевого слияния и его сложность времени. Почему это O(n log k) и не O(nk)?"
    choices={[
      {
        label: "Используй мин-кучу размера k чтобы держать одну голову из каждого списка. Вытащи минимум голову, добавь её к выводу, и добавь следующий элемент из того же списка. Куча всегда содержит одного кандидата из каждого списка; её корень это общий минимум. Время: n элементов × O(log k) за вытаск/добавь = O(n log k). Наивный подход сканирует все k голов за каждый элемент: O(nk). Куча избегает сканирование используя инвариант кучи чтобы найти мин в O(log k).",
        correct: true
      },
      {
        label: "Объединяй списки попарно (объедини 1 и 2, объедини 3 и 4, и т.д.) до один список остаётся. Это O(n log k) рекурсией.",
        misconception: "Попарное слияние это O(n log k), но вопрос спрашивает почему подход кучи это O(n log k) и не O(nk). Подход кучи прямой и чище чем попарное слияние."
      },
      {
        label: "Отсортируй все n элементов глобально. Время: O(n log n). Это менее эффективно чем O(n log k), но всё ещё правильно.",
        misconception: "Сортировка всех n элементов это O(n log n), что хуже чем O(n log k) когда k ≪ n. Подход кучи лучше потому что это только держит кучу размера k, не n."
      },
      {
        label: "Сложность в действительности O(nk) потому что ты должен добавь и вытащь k элементов за каждый из n шагов.",
        misconception: "Ты вытаскиваешь раз за шаг (после что размер кучи может уменьшиться), и ты добавляешь максимум раз за шаг (только если список имеет следующий элемент). Так это O(n × log k), не O(n × k). Размер кучи это k, не стоимость за операцию."
      }
    ]}
  />
</Check>

<Recap lang="ru">
Чтобы объединить k отсортированных списков, держи <Term slug="min_heap">мин-кучу размера k</Term> с одной головой из каждого списка. Вытащи минимум голову, добавь её к выводу, добавь следующий элемент из того же списка. Инвариант кучи гарантирует корень это всегда минимум среди k текущих голов. Время: **O(n log k)**. Память: **O(k)**. Это намного лучше чем наивный подход O(nk) (сканирование всех k голов за шаг). Одинаковый паттерн решает объединение k отсортированных связных списков, наименьший-диапазон, и объединение k отсортированных массивов. **Следующее:** модуль 09 (графы), где ты будешь исследовать узлы и рёбра, BFS, DFS, и кратчайшие пути. Теперь, когда увидишь k отсортированных потоков, которые нужно объединить, знаешь ход: мин-куча размера k, один слот на поток, вытащи-добавь за каждый элемент — и вывод пишется в правильном порядке сам по себе.
</Recap>
