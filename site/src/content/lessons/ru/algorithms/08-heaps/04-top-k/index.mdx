---
concepts:
- heap
- priority_queue
- top_k
- selection
- min_heap
- kth_largest
- time_complexity
- space_complexity
- streaming_algorithm
estMin: 32
lang: ru
mathPrereqs: []
order: 4
prereqs:
- 08-heaps/01-the-heap
- 08-heaps/02-heap-operations
- 08-heaps/03-priority-queues
slug: 04-top-k
sources:
- https://en.wikipedia.org/wiki/Selection_algorithm
- https://www.algoexpert.io
- https://brilliant.org/wiki/heap-sort/
status: ready
summary: 'Найди k наибольших элементов эффективно: держи мин-кучу размера k. Добавляй
  элемент, выкидывай минимум если превышено k. Результат: O(n log k) время, O(k) память
  против O(n log n) для сортировки. Потоковая обработка без хранения всех данных.'
title: Топ-K и k-й по величине элемент
track: algorithms
unit: 08-heaps
---



import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Idea from "~/components/lesson/Idea.astro";
import Code from "~/components/lesson/Code.astro";
import Trace from "~/components/lesson/Trace.astro";
import Complexity from "~/components/lesson/Complexity.astro";
import Check from "~/components/lesson/Check.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import AnnotatedCode from "~/components/algo/AnnotatedCode.astro";
import AlgoTrace from "~/components/algo/AlgoTrace.astro";
import CodeRunner from "~/components/algo/CodeRunner.astro";
import AlgoPractice from "~/components/algo/AlgoPractice.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";
import Term from "~/components/prose/Term.astro";

<Hook>
У тебя есть поток чисел. Ты хочешь знать k наибольших. Наивный ответ: жди все числа, отсортируй их, возьми последние k. Но это O(n log n) время и O(n) память — ты сохраняешь всё. Ключевое: ты заботишься только о топ k, так что держи <Term slug="min_heap">мин-кучу</Term> размера k. Добавляй каждое число. Когда куча превышает k элементов, вытащи наименьший (минимум). В конце твоя куча держит ровно k наибольших, и её корень это граница — k-й по величине элемент. Время: O(n log k). Память: O(k). Когда k малО, это разбивает сортировку.
</Hook>

<Goal lang="ru">
После этого урока ты можешь решить проблему k-наибольших используя мин-кучу размера k: добавляй каждый элемент и выкидывай минимум когда куча растёт за k. Ты понимаешь почему это работает: куча всегда держит k кандидатов, и корень это k-й по величине. Ты можешь объяснить сложность времени O(n log k) и сложность памяти O(k), и почему это бьёт сортировку O(n log n) когда k ≪ n. Ты можешь узнать паттерны «k-й наибольший» и «топ k по частоте» на LeetCode и других платформах, и применить одинаковый подход на куче. Ты можешь кодировать это в один проход (нет фазы сортировки), и отследить алгоритм на конкретном массиве шаг за шагом.
</Goal>

<Idea lang="ru">
**Проблема: найди k наибольших (или k наименьших)**

Дан массив чисел. Задача: найти k наибольших элементов. Например:
- Массив: [3, 2, 1, 5, 6, 4], k = 2 → ответ: [5, 6]
- Массив: [3, 2, 3, 1, 2, 4, 5, 5, 6], k = 4 → ответ: [5, 5, 6, 4] (или [6, 5, 5, 4], порядок может отличаться)

**Наивный подход: сортировка**

Отсортируй массив и возьми последние k элементов. Время: O(n log n), память: O(1) если на месте или O(n) с внешним хранилищем.

Проблема: ты должен отсортировать весь массив, хотя ты заботишься только о топ k. Когда k малО, это тратит работу впустую.

**Ключевое для кучи: держи кучу размера k**

Держи <Term slug="min_heap">мин-кучу</Term> размера ровно k. Эта куча всегда будет содержать k наибольших элементов, видимых до сих пор.

Алгоритм:
1. Для каждого элемента в массиве:
   - Добавь элемент в мин-кучу.
   - Если размер кучи превышает k, вытащи минимум.
2. После обработки всех элементов, куча содержит ровно k наибольших.
3. Корень мин-кучи это k-й по величине элемент (наименьший из топ k).

Почему мин-куча, а не макс-куча? Потому что ты хочешь выкинуть наименьшего кандидата когда у тебя слишком много. Корень мин-кучи это наименьший, так что ты вытаскиваешь его немедленно.

**Почему это работает?**

В любой момент, куча держит k наибольших элементов *видимых до сих пор*. Когда приходит новый элемент:
- Если он больше чем корень, он принадлежит топ k, так что ты удаляешь текущий минимум (корень) и вставляешь новый элемент.
- Если он меньше чем корень, он не в топ k, так что ты игнорируешь его.

После обработки всех n элементов, куча держит k наибольших глобально. Корень это граница: это наибольший элемент среди топ k что может быть выкинут, то есть k-й по величине.

**Пример: найди 2 наибольших в [3, 2, 1, 5, 6, 4]**

Мин-куча размера k=2:

1. Добавь 3: куча = [3]. Размер = 1 < k, продолжи.
2. Добавь 2: куча = [2, 3]. Размер = 2 = k, продолжи.
3. Добавь 1: куча = [1, 3, 2]. Размер = 3 > k, вытащи минимум (1). куча = [2, 3].
4. Добавь 5: куча = [2, 3, 5]. Размер = 3 > k, вытащи минимум (2). куча = [3, 5].
5. Добавь 6: куча = [3, 5, 6]. Размер = 3 > k, вытащи минимум (3). куча = [5, 6].
6. Добавь 4: куча = [4, 5, 6]. Размер = 3 > k, вытащи минимум (4). куча = [5, 6].

Финальная куча: [5, 6]. k наибольших это 5 и 6. ✓

**Симметричная проблема: найди k наименьших**

Если хочешь k *наименьших* элементов, используй <Term slug="max_heap">макс-кучу</Term> размера k. Логика идентична: когда куча превышает k, вытащи максимум (корень). В конце, куча держит k наименьших, и корень это k-й наименьший.

**Частый паттерн: k-й по величине элемент**

Часто проблема просит только k-й по величине элемент, не все k. После построения кучи, верни `heap.peek()` (корень) — это k-й по величине.

**Частый паттерн: топ k по частоте**

Дан массив, найди k самых частых элементов. Решение:
1. Посчитай частоту каждого элемента (хеш-таблица).
2. Построй мин-кучу размера k на частотах (не элементах).
3. Выкидывай наименее частый когда куча превышает k.
4. Куча содержит k самых частых элементов.

Это прямое применение одинакового паттерна.

</Idea>

<Code lang="ru">
**Топ-k используя мин-кучу**

<AnnotatedCode
  code={`function topK(arr, k) {
  // Мин-куча (очередь с приоритетом с меньшими значениями имеющими выше приоритет)
  const minHeap = new MinPriorityQueue();
  
  // Добавляй каждый элемент и выкидывай минимум если куча превышает k
  for (const num of arr) {
    minHeap.insert(num, num); // insert(value, priority)
    if (minHeap.size() > k) {
      minHeap.extractMin(); // Удали наименьший
    }
  }
  
  // Вытащи все элементы из кучи (они в порядке кучи, не отсортированы)
  const result = [];
  while (!minHeap.isEmpty()) {
    result.push(minHeap.extractMin());
  }
  return result;
}

// Пример
const arr = [3, 2, 1, 5, 6, 4];
const k = 2;
const topTwoLargest = topK(arr, k);
console.log(topTwoLargest); // [5, 6] (порядок может отличаться; оба в куче)`}
  annotations={[
    { line: 2, text: "Мин-куча (очередь с приоритетом с мин в корне). Размер останется ≤ k." },
    { line: 5, text: "Для каждого элемента в массиве." },
    { line: 6, text: "Добавь элемент с приоритет = значение (меньший приоритет = выше приоритет)." },
    { line: 7, text: "Если размер кучи превышает k, наименьший элемент (корень) удалён." },
    { line: 12, text: "Вытащи все оставшиеся элементы. Потому что это куча (не отсортированный массив), порядок может не быть строго возрастающий, но все k наибольших присутствуют." }
  ]}
/>

**Нахождение k-го по величине элемента**

<CodeRunner
  code={`class MinPriorityQueue {
  constructor() {
    this.heap = [];
  }

  insert(value) {
    this.heap.push(value);
    this.siftUp(this.heap.length - 1);
  }

  extractMin() {
    const root = this.heap[0];
    this.heap[0] = this.heap[this.heap.length - 1];
    this.heap.pop();
    if (this.heap.length > 0) {
      this.siftDown(0);
    }
    return root;
  }

  peek() {
    return this.heap[0];
  }

  isEmpty() {
    return this.heap.length === 0;
  }

  size() {
    return this.heap.length;
  }

  siftUp(index) {
    while (index > 0) {
      const parentIndex = Math.floor((index - 1) / 2);
      if (this.heap[index] < this.heap[parentIndex]) {
        [this.heap[index], this.heap[parentIndex]] = [this.heap[parentIndex], this.heap[index]];
        index = parentIndex;
      } else {
        break;
      }
    }
  }

  siftDown(index) {
    while (true) {
      const left = 2 * index + 1;
      const right = 2 * index + 2;
      let smallest = index;

      if (left < this.heap.length && this.heap[left] < this.heap[smallest]) {
        smallest = left;
      }
      if (right < this.heap.length && this.heap[right] < this.heap[smallest]) {
        smallest = right;
      }

      if (smallest !== index) {
        [this.heap[index], this.heap[smallest]] = [this.heap[smallest], this.heap[index]];
        index = smallest;
      } else {
        break;
      }
    }
  }
}

// Найди k-й по величине элемент
function findKthLargest(arr, k) {
  const minHeap = new MinPriorityQueue();
  
  for (const num of arr) {
    minHeap.insert(num);
    if (minHeap.size() > k) {
      minHeap.extractMin();
    }
  }
  
  // Корень это k-й по величине
  return minHeap.peek();
}

// Пример
const arr = [3, 2, 1, 5, 6, 4];
const k = 2;
const kthLargest = findKthLargest(arr, k);
console.log("The " + k + "-th largest element:", kthLargest);

const arr2 = [3, 2, 1, 5, 6, 4];
const k2 = 4;
const kthLargest2 = findKthLargest(arr2, k2);
console.log("The " + k2 + "-th largest element:", kthLargest2);`}
  expected={`The 2-th largest element: 5
The 4-th largest element: 3`}
/>

</Code>

<Trace lang="ru">
<AlgoTrace
  id="topk-insert-trace"
  lang="ru"
  code={[
    "const arr = [3, 2, 1, 5, 6, 4];",
    "const k = 2;",
    "const minHeap = new MinPriorityQueue();",
    "for (const num of arr) {",
    "  minHeap.insert(num);",
    "  if (minHeap.size() > k) minHeap.extractMin();",
    "}"
  ]}
  frames={[
    { caption: "Добавь 3: куча = [3]. Размер = 1 ≤ k.", line: 5, cells: [{ value: "3" }] },
    { caption: "Добавь 2: куча = [2, 3]. Размер = 2 ≤ k, просей вверх: 2 < 3, так 2 это корень.", line: 5, cells: [{ value: "2", mark: "active" }, { value: "3" }] },
    { caption: "Добавь 1: куча = [1, 3, 2]. Размер = 3 > k. Просей вверх: 1 < 2 и 1 < 3, так 1 это корень.", line: 5, cells: [{ value: "1", mark: "active" }, { value: "3" }, { value: "2" }] },
    { caption: "Размер > k, вытащи минимум (1). куча = [2, 3]. Размер = 2 ≤ k.", line: 6, cells: [{ value: "2", mark: "done" }, { value: "3" }] },
    { caption: "Добавь 5: куча = [2, 3, 5]. Размер = 3 > k. Просей вверх: 5 добавлен как ребёнок 2 на индексе 2.", line: 5, cells: [{ value: "2", mark: "active" }, { value: "3" }, { value: "5" }] },
    { caption: "Размер > k, вытащи минимум (2). куча = [3, 5]. Размер = 2 ≤ k.", line: 6, cells: [{ value: "3", mark: "done" }, { value: "5" }] },
    { caption: "Добавь 6: куча = [3, 5, 6]. Размер = 3 > k. Просей вверх: 6 это ребёнок 3 на индексе 2.", line: 5, cells: [{ value: "3", mark: "active" }, { value: "5" }, { value: "6" }] },
    { caption: "Размер > k, вытащи минимум (3). куча = [5, 6]. Размер = 2 ≤ k.", line: 6, cells: [{ value: "5", mark: "done" }, { value: "6" }] },
    { caption: "Добавь 4: куча = [4, 6, 5]. Размер = 3 > k. Просей вверх: 4 < 5, 4 становится корень.", line: 5, cells: [{ value: "4", mark: "active" }, { value: "6" }, { value: "5" }] },
    { caption: "Размер > k, вытащи минимум (4). куча = [5, 6]. Размер = 2 ≤ k. Финал: 2 наибольших это 5 и 6.", line: 6, cells: [{ value: "5", mark: "done" }, { value: "6", mark: "done" }] }
  ]}
/>
</Trace>

<Complexity lang="ru">
**Топ-K используя мин-кучу размера k**

Для каждого из n элементов в массиве:
- <Term slug="insert">Insert</Term>: O(log k) (куча имеет максимум k+1 элементов перед pop).
- <Term slug="extract_min">ExtractMin</Term> (когда куча превышает k): O(log k).

Итого: n итераций × O(log k) за итерацию = **O(n log k) время**.

Память: Куча держит максимум k элементов, так что **O(k) память**.

**Сравнение с сортировкой**

Если отсортируешь весь массив: O(n log n) время, O(1) вспомогательная память (если на месте).

Топ-K с кучей: O(n log k) время, O(k) память.

Когда подход кучи лучше?
- Если k ≪ n (k намного меньше чем n), тогда log k ≪ log n, так O(n log k) ≪ O(n log n).
- Если k близко к n, сортировка может быть проще и быстрее на практике.

Пример:
- n = 1,000,000, k = 10: O(n log k) = O(1,000,000 × 3.3) ≈ 3.3 миллиона операций. O(n log n) = O(1,000,000 × 20) ≈ 20 миллионов операций. Куча быстрее.
- n = 1,000, k = 500: O(n log k) = O(1,000 × 9) ≈ 9,000. O(n log n) = O(1,000 × 10) ≈ 10,000. Похоже; сортировка может быть проще.

**Обмен время-память**

Сортировка использует O(n) память (или O(1) с трюками на месте). Топ-K использует O(k) память. Когда k намного меньше чем n, куча экономит память.

</Complexity>

<AlgoPractice
  id="practice-top-k"
  lessonSlug="04-top-k"
  lang="ru"
  problems={[
    {
      kind: "choice",
      prompt: "Чтобы найти k наибольших элементов, должен ли ты использовать мин-кучу или макс-кучу размера k?",
      choices: [
        "Макс-кучу, так что наибольший элемент всегда в корне.",
        "Мин-кучу, так что наименьший из топ-k в корне и может быть выкинут.",
        "Любая работает одинаково хорошо.",
        "Ни одна — используй quickselect вместо."
      ],
      answer: 1,
      hint: "Ты хочешь выкинуть наименьшего кандидата когда куча превышает размер k. Корень мин-кучи это минимум, так что ты его вытаскиваешь немедленно. Корень макс-кучи это максимум — ты должен был бы найти минимум где-то ещё, что медленно."
    },
    {
      kind: "choice",
      prompt: "Что такое k-й по величине элемент в контексте алгоритма топ-K кучи?",
      choices: [
        "Наибольший элемент в финальной куче.",
        "Корень финальной мин-кучи — наименьший из топ k.",
        "Элемент что был выкинут первым.",
        "Медиана k элементов."
      ],
      answer: 1,
      hint: "Финальная мин-куча содержит k наибольших элементов. Её корень это наименьший среди них — граница, то есть k-й по величине."
    },
    {
      kind: "choice",
      prompt: "Дан arr = [3, 2, 1, 5, 6, 4] и k = 2, что такое k-й по величине элемент?",
      choices: [
        "3",
        "4",
        "5",
        "6"
      ],
      answer: 2,
      hint: "2 наибольших элемента это 5 и 6. 2-й по величине (меньший из двух) это 5."
    },
    {
      kind: "choice",
      prompt: "Какая сложность времени нахождения k наибольших элементов используя мин-кучу?",
      choices: [
        "O(n)",
        "O(n log k)",
        "O(n log n)",
        "O(k log n)"
      ],
      answer: 1,
      hint: "Для каждого из n элементов, ты добавляешь в кучу размера ≤ k (O(log k) за insert). n итераций × O(log k) = O(n log k)."
    },
    {
      kind: "numeric",
      prompt: "Ты хочешь найти 5 наибольших элементов в массиве 1,000,000 чисел. Примерно сколько больше операций берёт сортировка сравнительно с подходом кучи? (Используй log₂(1,000,000) ≈ 20 и log₂(5) ≈ 2.3.)",
      answer: 17700000,
      hint: "Куча: 1,000,000 × 2.3 ≈ 2,300,000. Сортировка: 1,000,000 × 20 ≈ 20,000,000. Разница: 20,000,000 - 2,300,000 ≈ 17,700,000. Куча намного быстрее для малого k."
    },
    {
      kind: "choice",
      prompt: "Проблема «топ k по частоте» просит: дан массив, найди k самых частых элементов. Как бы ты применил стратегию кучи?",
      choices: [
        "Построй макс-кучу всех элементов по частоте.",
        "Посчитай частоты (хеш-таблица), потом построй мин-кучу размера k на частотах, выкидывая наименее частый.",
        "Отсортируй элементы по частоте и возьми первые k.",
        "Используй quickselect на счётах частоты."
      ],
      answer: 1,
      hint: "Ты сначала посчитаешь частоту (хеш-таблица в O(n)). Потом ты применяешь одинаковый паттерн кучи: мин-куча размера k, выкидывая элемент с наименьшей частотой когда куча превышает k. В конце, k самых частых элементов в куче."
    }
  ]}
/>

<Inset type="why">
**Почему топ-K имеет значение.** Реальные системы часто просят: дай мне топ 10 результаты поиска, или топ 100 пользователей по вовлечённости, или топ k редчайшие ошибки. Сортировка всего датасета каждый раз это трата. Топ-K куча позволяет тебе ответить на эти запросы эффективно, даже на большие датасеты и потоки. Стриминг кейс: представь датчик непрерывно выпускает показания. Ты хочешь знать 100 наибольших показаний видимых до сих пор. Ты не можешь хранить все показания (памятью ограниченный). Куча размера-100 позволяет тебе держать топ 100 онлайн, обрабатывая каждое показание в O(log 100) времени.
</Inset>

<Inset type="mistake">
**Используя макс-кучу вместо мин-кучи.** Если держишь макс-кучу размера k чтобы найти k наибольших, наибольший элемент в корне — но ты хочешь выкинуть наименьшего кандидата когда куча превышает k. Макс-куча заставляет тебя искать всю кучу на минимум, что O(k), не O(log k). Мин-куча держит наименьшего кандидата (выкидываемого) в корне, так одно сравнение говорит тебе если новый элемент принадлежит топ k.
</Inset>

<Inset type="edgecase">
**k равен n или k больше чем n.** Если k ≥ n, каждый элемент в топ k, так просто верни весь массив. Если k = 0, верни пустой список. Если k это 1, ты находишь одинарный максимум, что O(n) с кучей (один проход, один insert за элемент, размер никогда не превышает 1) или O(n) с линейным сканированием.
</Inset>

<Check lang="ru">
  <Quiz
    id="quiz-top-k"
    pieceSlug="04-top-k"
    lang="ru"
    question="Объясни алгоритм топ-K кучи. Почему ты используешь мин-кучу размера k вместо макс-кучи или сортировки?"
    choices={[
      {
        label: "Мин-куча размера k держит k наибольших элементов. Когда приходит новый элемент, ты его добавляешь. Если куча превышает размер k, ты вытаскиваешь корень (наименьший из k кандидатов). Это k-й по величине — граница. Время O(n log k): n итераций insert/pop, каждый O(log k). Макс-куча поместила бы наибольший в корень, затрудняя выкидывание наименьшего. Сортировка берёт O(n log n), что хуже чем O(n log k) когда k ≪ n.",
        correct: true
      },
      {
        label: "Макс-куча размера k лучше, потому что наибольший элемент всегда видимый в корне.",
        misconception: "Макс-куча ставит максимум в корень, так ты не можешь видеть минимум (кандидат на выкидывание). Ты должен был бы искать кучу на минимум, что O(k) за вставку — хуже чем O(log k)."
      },
      {
        label: "Сортировка всегда лучший подход, потому что производит отсортированный результат.",
        misconception: "Сортировка O(n log n) и требует O(n) память. Подход кучи O(n log k) и использует O(k) память. Когда k малО, куча выигрывает на обоих времени и памяти."
      },
      {
        label: "Ты должен используй сбалансированное двоичное дерево поиска (BST) вместо кучи, потому что держит отсортированный порядок.",
        misconception: "BST работает для топ-K (O(n log k) время), но куча проще и быстрее на практике, потому что ты нужен только k наибольших, не полностью отсортированный порядок."
      }
    ]}
  />
</Check>

<Recap lang="ru">
Чтобы найти **k наибольших элементов**, держи <Term slug="min_heap">мин-кучу размера k</Term>. Для каждого элемента в массиве, добавь его и выкидывай минимум когда куча превышает k. Финальная куча содержит ровно k наибольших, и её корень это <Term slug="kth_largest">k-й по величине элемент</Term>. Время: **O(n log k)**. Память: **O(k)**. Это бьёт сортировку (O(n log n)) когда k ≪ n. Симметричная логика применяется к k наименьшим (используй макс-кучу). Одинаковый паттерн решает «топ k по частоте»: посчитай частоты, применить стратегию кучи на значения частоты. **Следующее:** урок 05 расширяет это в k-way merge, где ты держишь размера-k кучу потоков, вытягивая следующий минимум из потока чей головой минимум.
</Recap>
