---
concepts:
- heap-invariant
- build-heap
- priority-queue-adt
- top-k
- k-way-merge
- heap-sort
estMin: 13
lang: ru
level: middle
order: 8
prereqs: []
slug: quiz-short
sources:
- https://en.wikipedia.org/wiki/Binary_heap
- https://en.wikipedia.org/wiki/Priority_queue
- https://en.wikipedia.org/wiki/Heapsort
status: ready
summary: "Подсказки на свободное припоминание по всему юниту heap. Сначала ответьте своими словами, затем откройте эталонный ответ и сравните."
title: 'Heap: свободное припоминание'
track: algorithms
unit: 08-heaps
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";

<Hook>
Припоминание сильнее перечитывания. Для каждой подсказки скажите или запишите полный ответ по памяти, прежде чем открыть эталон — именно усилие припоминания закрепляет модель heap.
</Hook>

<Goal lang="ru">
Восстановите стержень юнита, не подглядывая: двусоставный инвариант heap, аргумент O(n) для build-heap, контракт priority queue, приём «heap размера k» для top-k и k-way merge и то, как heap-sort вытекает из pop.
</Goal>

<RetrievalDrawer
  client:load
  id="08-heaps-short-retrieval"
  lang="ru"
  questions={[
    { q: "Назовите два свойства, определяющие binary heap, и объясните, почему они позволяют хранить его как плоский массив без указателей.", a: "Binary heap — это (1) полное бинарное дерево — каждый уровень заполнен, кроме, возможно, последнего, который заполняется слева направо без дыр — и (2) оно удовлетворяет свойству heap: каждый родитель ≤ обоих детей (min-heap) или ≥ (max-heap). Именно полнота включает кодирование массивом: укладка узлов в порядке обхода в ширину не оставляет дыр, поэтому для узла на индексе i родитель на ⌊(i−1)/2⌋, левый ребёнок на 2i+1, правый на 2i+2. Указатели не нужны, потому что позиция в массиве задаёт позицию в дереве. Корень (min или max) всегда на индексе 0, давая peek за O(1)." },
    { q: "Пройдите push и pop и объясните, почему каждая операция есть O(log n).", a: "Push добавляет новое значение в конец массива (следующий лист, сохраняя полноту) и вызывает sift-up: сравнение с родителем и обмен вверх, пока свойство heap нарушено, останов в корне. Pop удаляет корень (ответ), переносит ПОСЛЕДНИЙ элемент в корень, чтобы дерево осталось полным, уменьшает массив на один и вызывает sift-down: обмен с меньшим ребёнком (min-heap), пока свойство нарушено, останов на листе. Обе есть O(log n), потому что высота дерева ⌈log₂ n⌉ и каждая делает не более одного обмена на уровень — sift-up трогает одного родителя на уровень, sift-down не более двух детей на уровень." },
    { q: "Почему build-heap есть O(n), а вставка n элементов по одному — O(n log n)?", a: "build-heap делает sift-down каждого нелистового узла, начиная с последнего нелистового на ⌊n/2⌋−1 и двигаясь к корню, поэтому поддеревья каждого узла уже корректные heap к моменту его обработки. Стоимость sift-down из узла пропорциональна его ВЫСОТЕ, а не высоте дерева. Около n/2 узлов — листья (высота 0, нет работы), n/4 — высота 1, n/8 — высота 2 и т.д.; сумма Σ (n/2^{h+1})·h сходится к ≤ 2n = O(n). Вставка по одному, наоборот, делает sift-UP каждого элемента через его полную ГЛУБИНУ, а большинство элементов живёт у дна, где глубина ≈ log n — поэтому этот путь O(n log n)." },
    { q: "Различите ADT priority queue и binary heap и назовите, когда различие меняет проект.", a: "Priority queue — абстрактный тип данных, контракт операций: insert, extractMin (или extractMax), peek, плюс isEmpty/size. Binary heap — одна РЕАЛИЗАЦИЯ этого контракта; другие: отсортированный массив (peek за O(1), insert за O(n)), сбалансированное BST (O(log n) на всё, бо́льшие константы) и Fibonacci heap (амортизированный decrease-key за O(1)). Различие важно, когда нагрузке нужна операция, которую обычный heap делает плохо. Классический случай — кратчайшие пути Dijkstra, опирающиеся на decrease-key: у binary heap нет эффективного decrease-key, поэтому индексированный binary heap или Fibonacci heap меняет асимптотику. Отдельное именование контракта позволяет менять реализацию, не трогая клиентский код." },
    { q: "Объясните приём «heap размера k» и как одна идея решает и top-k, и k-way merge.", a: "Общая идея: никогда не держать в heap больше k кандидатов, поэтому каждая операция есть O(log k). Для top-k НАИБОЛЬШИХ из n элементов держите MIN-heap размера k; добавляйте каждый элемент и при превышении k извлекайте корень (текущий наименьший из top-k). Выжившие — k наибольших, корень — k-й по величине, итого O(n log k) времени и O(k) памяти — лучше сортировки O(n log n) при k ≪ n. Для k-way merge из k отсортированных списков (n элементов всего) heap держит ровно по одному кандидату от каждого списка (текущую голову); извлеките минимум, добавьте его, затем добавьте СЛЕДУЮЩИЙ элемент из того же списка (отслеживается через listIndex/position). Каждый pop/push есть O(log k), поэтому merge есть O(n log k) против наивного O(nk) сканирования голов." },
    { q: "Как heap-sort вытекает из операций heap и каковы его время и память?", a: "Heap-sort — две фазы из операций, что вы уже знаете. Фаза 1: build-heap превращает массив в (max-)heap за O(n). Фаза 2: повторно меняем корень (максимум) с последним неотсортированным элементом, уменьшаем heap на один и делаем sift-down нового корня, восстанавливая свойство — это каждый раз ставит наибольший элемент в конец, поэтому после n−1 извлечений массив отсортирован по возрастанию на месте. Каждый из n sift-down есть O(log n), поэтому итог O(n) + O(n log n) = O(n log n) в худшем, среднем и лучшем случае. Память O(1) дополнительная, потому что всё в том же массиве. Компромисс против quicksort: у heap-sort гарантированный O(n log n) (нет плохих опорных), но хуже cache locality и константы, поэтому на практике он не всегда самый быстрый." },
  ]}
/>

<Recap lang="ru">
Если вы восстановили каждый ответ по памяти, у вас есть стержень юнита: пара «полнота + свойство heap» даёт массив без указателей и peek за O(1); sift-up/sift-down дают push/pop за O(log n); build-heap достигает O(n), взвешивая стоимость по высоте узла; контракт PQ отделён от heap, его реализующего; heap размера k превращается в top-k (min-heap, вытеснить корень) и k-way merge (по одному кандидату от списка, продвижение по индексу); а heap-sort — это просто build-heap плюс повторный pop, гарантированный O(n log n), память O(1).
</Recap>
