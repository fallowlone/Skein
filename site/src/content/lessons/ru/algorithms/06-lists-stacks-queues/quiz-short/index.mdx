---
concepts:
- linked-list-cost
- pointer-reversal
- fast-slow-pointers
- lifo-fifo
- deque
- monotonic-stack
estMin: 13
lang: ru
level: middle
order: 8
prereqs: []
slug: quiz-short
sources:
- https://walkccc.me/CLRS/Chap10/10.2/
- https://en.wikipedia.org/wiki/Stack_(abstract_data_type)
- https://www.geeksforgeeks.org/dsa/introduction-to-monotonic-stack-2/
status: ready
summary: "Промпты на свободное припоминание по юниту. Сначала ответь своими словами, затем раскрой модельный ответ и сравни — cost list, разворот, tortoise-and-hare, LIFO/FIFO и monotonic stack."
title: 'Списки, стеки, очереди: свободное припоминание'
track: algorithms
unit: 06-lists-stacks-queues
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";

<Hook>
Припоминание сильнее перечитывания. На каждый промпт проговори или запиши полный ответ по памяти, прежде чем открыть модельный — именно усилие припоминания закрепляет юнит.
</Hook>

<Goal lang="ru">
Восстанови хребет юнита — почему linked list меняет индексацию на вставку в голову, как разворот и tortoise-and-hare манипулируют pointer, почему stack и queue — это дисциплины, а не структуры, и почему monotonic stack линеен — не подглядывая в уроки.
</Goal>

<RetrievalDrawer
  client:visible
  id="06-lists-stacks-queues-short-retrieval"
  lang="ru"
  questions={[
    { q: "Почему доступ по индексу — O(n) у linked list, но O(1) у массива, и что linked list получает взамен?", a: "Массив хранит элементы непрерывно, поэтому адрес элемента i равен base + i * elementSize — формула за константу, без обхода. Linked list разбрасывает узлы по памяти; формулы, связывающей позицию с адресом, нет, поэтому чтобы дойти до индекса i, надо стартовать с head и пройти i pointer next, а это O(i), в худшем случае O(n). Взамен linked list получает O(1)-вставку и удаление в голове: выделить узел, направить его на старую голову, передвинуть ссылку head — без сдвига n элементов, которого потребовал бы массив. Лозунг: непрерывность позволяет вычисление, разбросанность требует обхода. Структуру выбираешь по тому, какая операция доминирует в нагрузке." },
    { q: "Пройди in-place разворот singly linked list. Какое одно движение pointer нельзя пропускать и каковы стоимости по времени и памяти?", a: "Держи три pointer: prev (старт null), current (старт head) и next. Каждая итерация: сохрани next = current.next, затем разверни current.next = prev, затем продвинь prev = current и current = next. Когда current станет null, prev — новая голова. Движение, которое нельзя пропускать — сохранение next до строки current.next = prev: как только перезапишешь current.next, ты потерял единственную связь с остатком списка, и обход либо остановится рано, либо зациклится. Стоимость: O(n) времени (один проход, каждый узел тронут раз) и O(1) памяти (только три pointer, без стека рекурсии и без новых узлов). Рекурсивная версия элегантна, но стоит O(n) памяти на стек." },
    { q: "Объясни технику fast-and-slow (tortoise-and-hare). Почему pointer обязаны встретиться внутри цикла и почему ровно два шага у быстрого?", a: "Гони slow на один узел за итерацию и fast на два от head. Если fast достигнет null — список ацикличен. Если есть цикл, fast входит в него и, поскольку он сокращает разрыв со slow ровно на один узел за итерацию, разрыв уменьшается на единицу каждый шаг, пока не станет нулём — значит они обязаны столкнуться внутри петли; снаружи встретиться не могут, потому что как только slow входит в цикл, он его не покидает, а fast уже внутри. Та же техника находит середину: когда fast сваливается с конца, slow прошёл половину шагов и стоит в середине. Fast должен идти по два: шаг в один никогда не догонит, а шаг в три и больше может перепрыгнуть slow и ни разу на него не приземлиться, и сходимость уже не гарантирована. Всё это — O(n) времени и O(1) памяти, выигрыш по памяти над visited-set, который O(n)." },
    { q: "И stack, и queue можно реализовать на массиве или linked list. Так что же на самом деле их различает и как выбирать между ними под задачу?", a: "Это дисциплины, а не структуры данных: каждая ограничивает, где можно вставлять и удалять. Stack — это LIFO: push и pop оба на одном конце (top), так что последний добавленный выходит первым. Queue — это FIFO: enqueue в rear, dequeue из front, так что самый старый выходит первым. Выбираешь по порядку, который нужен задаче. Задачи последний-первым берут stack: сопоставление скобок (внутренняя открытая закрывается первой), undo/redo, парсинг выражений, обход в глубину, call stack. Задачи порядка прибытия / справедливости берут queue: обход в ширину / по уровням, планирование задач, буферы сообщений. Подложка (массив, linked list, circular buffer) — это деталь реализации, выбранная под память и производительность; дисциплина — вот что делает её stack или queue." },
    { q: "Что такое deque и почему dequeue у FIFO queue через наивный shift массива тихо рушит производительность?", a: "Deque (двусторонняя очередь) поддерживает вставку и удаление с обоих концов — pushFront, pushBack, popFront, popBack — каждое за O(1) при подложке из doubly linked list или circular buffer. Обычная queue — частный случай, использующий по одному концу на операцию. Ловушка производительности: реализация dequeue через array.shift() (или удаление индекса 0) заставляет рантайм переиндексировать каждый оставшийся элемент на слот вниз, поэтому каждый dequeue — O(n). По N парам enqueue+dequeue это суммируется в O(N^2) — вывод корректный, латентность под нагрузкой катастрофическая. Фикс — держать pointer/индексы front и rear и никогда не делать shift: linked-list queue (head и tail) или circular buffer, оборачивающий индексы по модулю, чтобы переиспользовать освобождённые слоты. Оба делают каждую операцию O(1)." },
    { q: "Опиши monotonic stack и паттерн next-greater-element. Почему он O(n) несмотря на цикл внутри цикла?", a: "Monotonic stack держит содержимое в отсортированном порядке (возрастающем или убывающем), попая любой элемент, который нарушил бы порядок, до push нового. Для next-greater-element держишь убывающий stack индексов: сканируешь массив, и для каждого элемента попаешь каждый индекс, чьё значение меньше-или-равно (они никогда не станут next-greater для последующих), затем новый top — если есть — это ответ для попнутых индексов, и наконец пушишь текущий индекс. Хранишь индексы, а не значения, чтобы восстанавливать позиции и расстояния. Это O(n) по амортизационному анализу: хотя внутренний while может попнуть много элементов за один внешний шаг, каждый индекс пушится ровно один раз и попается максимум один раз за весь прогон, так что суммарные операции stack ограничены 2n. Тот же паттерн решает next-smaller (возрастающий stack), daily temperatures, stock span и largest rectangle in histogram." },
  ]}
/>

<Recap lang="ru">
Если ты смог восстановить каждый ответ по памяти, ты держишь хребет юнита: linked list меняет O(1)-индексацию на O(1)-вставку в голову, потому что разбросанность требует обхода; разворот и tortoise-and-hare — это дисциплинированные танцы pointer, где ты сохраняешь связи до перезаписи; stack и queue — дисциплины LIFO/FIFO, которые выбираешь по нужному порядку и никогда не делаешь dequeue через shift; а monotonic stack превращает O(n^2)-скан в O(n) по инварианту push-once/pop-once.
</Recap>
