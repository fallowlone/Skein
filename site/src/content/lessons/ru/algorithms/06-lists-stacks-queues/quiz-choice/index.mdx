---
concepts:
- linked-list
- pointer-cost
- dummy-head
- stack-queue-discipline
- deque
- monotonic-stack
estMin: 13
lang: ru
level: middle
order: 7
prereqs: []
slug: quiz-choice
sources:
- https://walkccc.me/CLRS/Chap10/10.2/
- https://en.wikipedia.org/wiki/Stack_(abstract_data_type)
- https://en.wikipedia.org/wiki/Double-ended_queue
- https://www.geeksforgeeks.org/dsa/introduction-to-monotonic-stack-2/
status: ready
summary: "Синтез по юниту в формате выбора: cost model linked list, манипуляция pointer, дисциплина LIFO/FIFO, реализация deque и амортизационный аргумент monotonic stack."
title: 'Списки, стеки, очереди: тест с выбором'
track: algorithms
unit: 06-lists-stacks-queues
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Шесть вопросов через весь юнит. Каждый — это решение, на которое давит интервьюер: не определение для пересказа, а cost model, которую надо защитить, и выбор реализации, который надо обосновать.
</Hook>

<Goal lang="ru">
Убедись, что умеешь связать раскладку linked list в памяти с его cost model, рассуждать о манипуляции pointer не теряя узлов, выбирать правильную дисциплину LIFO/FIFO под задачу и объяснять, почему monotonic stack линеен несмотря на внутренний цикл.
</Goal>

<Quiz
  id="06-lists-stacks-queues-choice-1"
  lessonSlug="06-lists-stacks-queues"
  lang="ru"
  question="Нагрузка делает push в голову 10 000 раз, а затем 100 раз читает по индексу в случайных позициях. Надо выбрать array vs singly linked list. Что быстрее в сумме и почему?"
  choices={[
    { label: "Linked list. Вставка в голову — O(1) (выделить узел, переставить head), тогда как вставка в голову массива — O(n) со сдвигом; 10k вставок доминируют над 100 чтениями по индексу за O(n).", correct: true },
    { label: "Array, потому что массивы всегда быстрее linked list.", misconception: "Массивы выигрывают только на индексации и локальности кэша. Здесь нагрузка — это вставки в голову, а это ровно сила linked list O(1) против O(n)-сдвига массива." },
    { label: "Одинаково, ведь оба поддерживают вставку и доступ по индексу.", misconception: "Оба поддерживают операции, но стоимости различаются на порядок: массив платит O(n) за вставку в голову, linked list — O(1). Доминирует именно 10k вставок в голову." },
    { label: "Linked list, потому что доступ по индексу у linked list тоже O(1).", misconception: "Вывод верный, но причина неверна. Доступ по индексу у linked list — O(n): нет формулы адреса, надо перепрыгивать по pointer. Linked list выигрывает на вставках в голову, а не на индексации." },
  ]}
/>

<Quiz
  id="06-lists-stacks-queues-choice-2"
  lessonSlug="06-lists-stacks-queues"
  lang="ru"
  question="Ты пишешь in-place разворот singly linked list. Коллега пишет внутри цикла current.next = prev; current = current.next;. Что ломается?"
  choices={[
    { label: "Теряется связь с остатком списка. После current.next = prev поле current.next уже не указывает на исходного преемника, поэтому current = current.next уводит назад в prev, а не вперёд. Надо сохранить next = current.next до перезаписи.", correct: true },
    { label: "Ничего — это стандартный цикл разворота.", misconception: "Стандартный цикл сначала сохраняет преемника: const next = current.next; current.next = prev; prev = current; current = next;. Пропуск сохранения портит обход, потому что ты перезаписал нужный pointer." },
    { label: "Утекает память, потому что старые узлы никогда не освобождаются.", misconception: "Утечки нет — GC соберёт недостижимые узлы. Баг логический: обход идёт в неправильную сторону, потому что pointer на преемника уничтожен до сохранения." },
    { label: "Падает только на списках с циклом.", misconception: "Падает на любом нетривиальном списке. Как только ты перезаписал current.next и затем читаешь его, ты идёшь по развёрнутому pointer назад в prev вместо продвижения вперёд." },
  ]}
/>

<Quiz
  id="06-lists-stacks-queues-choice-3"
  lessonSlug="06-lists-stacks-queues"
  lang="ru"
  question="Ты постоянно вставляешь и удаляешь в голове singly linked list и всё время натыкаешься на null-pointer спецслучаи для пустого списка и позиции головы. Какой самый чистый структурный фикс?"
  choices={[
    { label: "Ввести фиктивный (sentinel) узел-голову, который всегда предшествует настоящей голове. Теперь у каждого реального узла есть предшественник, поэтому вставка и удаление идут одним путём без спецслучая для головы.", correct: true },
    { label: "Перейти на doubly linked list, чтобы у каждого узла был back pointer.", misconception: "Back pointer помогает с O(1)-доступом к предшественнику, но не убирает спецслучаи головы/пустоты — ты всё равно ветвишься по тому, null ли head. Sentinel убирает ветку, гарантируя предшественника." },
    { label: "Обернуть каждую операцию в try/catch, чтобы глушить null-pointer ошибки.", misconception: "Глушение ошибки прячет баг, а не чинит логику. Структурный фикс — sentinel-узел, чтобы ссылка на предыдущий узел всегда была валидна." },
    { label: "Держать отдельный счётчик и пропускать операцию, когда список пуст.", misconception: "Счётчик не объединяет пути кода для операций над головой и серединой. Именно sentinel делает 'вставку в голову' идентичной 'вставке в середину'." },
  ]}
/>

<Quiz
  id="06-lists-stacks-queues-choice-4"
  lessonSlug="06-lists-stacks-queues"
  lang="ru"
  question="Ты валидируешь вложенные скобки в парсере конфигов и отдельно делаешь обход дерева в порядке уровней (в ширину). Какие структуры подходят и почему?"
  choices={[
    { label: "Скобкам нужен stack (LIFO): последняя открытая скобка должна закрыться первой, а это ровно требование вложенности. Обходу по уровням нужна queue (FIFO): узлы обрабатываются в порядке прибытия, так что соседи посещаются раньше внуков.", correct: true },
    { label: "Оба должны использовать stack, ведь оба обрабатывают символы или узлы по одному.", misconception: "Stack на дереве даёт обход в глубину, а не по уровням. Обход по уровням требует FIFO, чтобы дети узла ждали позади всех уже добавленных узлов текущего уровня — это queue." },
    { label: "Оба должны использовать queue, потому что порядок важен в обоих.", misconception: "Queue на скобках вытолкнула бы самую старую открытую скобку первой — это обратно вложенности. Сопоставлению скобок нужна дисциплина 'последний-первым' у stack." },
    { label: "Скобкам нужна queue, а обходу дерева — stack.", misconception: "Ровно наоборот. Вложенность — это последний-открыт-первым-закрыт (stack); обход в ширину — порядок прибытия (queue). Перестановка даёт неправильный обход и неправильную валидацию." },
  ]}
/>

<Quiz
  id="06-lists-stacks-queues-choice-5"
  lessonSlug="06-lists-stacks-queues"
  lang="ru"
  question="Ты реализуешь FIFO queue на массиве JavaScript: push для enqueue и shift для dequeue. Под устойчивым потоком из N пар enqueue+dequeue какова суммарная стоимость и фикс?"
  choices={[
    { label: "O(N^2): каждый shift переиндексирует все оставшиеся элементы, поэтому dequeue — O(n). Чини linked-list queue (pointer head/tail) или circular buffer с индексами front/rear — оба дают dequeue за O(1).", correct: true },
    { label: "O(N): shift — O(1) как и push, значит N операций линейны.", misconception: "push амортизированно O(1), но shift — O(n): удаление индекса 0 сдвигает каждый последующий элемент на слот вниз. N dequeue над растущим массивом суммируются в O(N^2)." },
    { label: "O(N log N): операции с массивом логарифмичны.", misconception: "Здесь нет ничего логарифмического. shift линеен по длине массива, а сумма линейных стоимостей по N операциям даёт квадрат, а не N log N." },
    { label: "Всё нормально, ведь deque умеет popFront/popBack за O(1).", misconception: "Правильный deque (doubly linked list или circular buffer) — O(1) с обоих концов, но массив с shift — это не он: shift всё равно переиндексирует. Смысл в том, чтобы перестать использовать shift." },
  ]}
/>

<Quiz
  id="06-lists-stacks-queues-choice-6"
  lessonSlug="06-lists-stacks-queues"
  lang="ru"
  question="Решение next-greater-element на monotonic stack имеет цикл while внутри for, но это O(n), а не O(n^2). В чём точный аргумент?"
  choices={[
    { label: "Амортизационный анализ: каждый индекс пушится ровно один раз и попается не более одного раза за весь прогон, так что суммарное число итераций внутреннего цикла по всем внешним шагам ограничено n. Итого работа = n пушей + не более n попов = O(n).", correct: true },
    { label: "Потому что внутренний while выполняется константное число раз на каждой итерации.", misconception: "Внутренний цикл может попнуть много элементов за один внешний шаг (высокий элемент чистит длинную серию). Это не константа на шаг — граница глобальна: каждый элемент попается максимум один раз за весь прогон." },
    { label: "Потому что входной массив сортируется перед обработкой.", misconception: "Ничего не сортируется; stack поддерживает монотонное подмножество внутри. Линейная граница идёт от того, что каждый элемент входит и выходит из stack один раз, а не от сортировки." },
    { label: "Потому что мы пропускаем половину элементов на каждом проходе, как в бинарном поиске.", misconception: "Никакого деления пополам нет. Посещается каждый элемент; эффективность идёт от инварианта push-once/pop-once, который ограничивает суммарные операции stack величиной 2n." },
  ]}
/>

<Recap lang="ru">
Сквозная линия юнита — одна cost model и три дисциплины. Раскладка в памяти задаёт стоимость: непрерывность покупает O(1)-индексацию, разбросанность — O(1)-вставку в голову; выбираешь по доминирующей операции. Манипуляция pointer — это сохранять связи до перезаписи и использовать sentinel, чтобы стереть спецслучаи головы. Stack (LIFO) и queue (FIFO) — это дисциплины, а не структуры данных: выбирай по тому, нужен ли тебе последний-первым или порядок прибытия, и никогда не делай dequeue через shift. Monotonic stack — это выигрыш: stack плюс инвариант порядка превращают O(n^2)-скан в O(n) по аргументу push-once/pop-once.
</Recap>
