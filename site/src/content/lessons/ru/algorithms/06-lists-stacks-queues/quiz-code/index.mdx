---
concepts:
- pointer-bug
- linked-list-reversal
- queue-dequeue
- monotonic-stack
- sentinel-node
estMin: 14
lang: ru
level: middle
order: 9
prereqs: []
slug: quiz-code
sources:
- https://walkccc.me/CLRS/Chap10/10.2/
- https://labuladong.online/en/algo/data-structure/monotonic-stack/
- https://en.wikipedia.org/wiki/Double-ended_queue
status: ready
summary: "Читай реальные сниппеты на JavaScript — баг pointer в развороте, баг удаления по значению, сломанную array queue и паттерн monotonic stack — и выбирай самый рычажный диагноз или фикс."
title: 'Списки, стеки, очереди: чтение кода'
track: algorithms
unit: 06-lists-stacks-queues
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Баги pointer в коде linked list не падают громко — они тихо портят структуру. Читай каждый сниппет, прокрути pointer в голове и выбери, что senior-инженер пометит первым.
</Hook>

<Goal lang="ru">
Потренируй цикл, который ты гоняешь на каждом разборе списков/стеков/очередей: читай движения pointer, предскажи, где теряется узел или где неверно заявлена сложность, и назови точный фикс.
</Goal>

## Сниппет 1 — разворот, теряющий хвост

```js
function reverse(head) {
  let prev = null;
  let current = head;
  while (current !== null) {
    current.next = prev;   // развернуть pointer
    prev = current;
    current = current.next; // продвинуться
  }
  return prev;
}
```

<Quiz
  id="06-lists-stacks-queues-code-1"
  lessonSlug="06-lists-stacks-queues"
  lang="ru"
  question="Что произойдёт при запуске на [10] -> [20] -> [30]?"
  choices={[
    { label: "Он зациклится на первом узле навсегда (или остановится через один узел). После current.next = prev поле current.next указывает на prev, поэтому current = current.next возвращает current в prev (null на первом шаге), а остаток списка никогда не достигается. Фикс: сохранить const next = current.next до перезаписи и продвигаться через current = next.", correct: true },
    { label: "Он корректно вернёт [30] -> [20] -> [10].", misconception: "Не может: строка current = current.next читает только что перезаписанный pointer. На узле 10 current.next теперь null (prev), поэтому цикл завершается после одного узла, а 20, 30 теряются." },
    { label: "Он вернёт исходный список без изменений.", misconception: "Первый pointer мутирован (10.next становится null), так что список уже испорчен, а не без изменений. Обход также завершается рано, потому что продвижение читает перезаписанный pointer." },
    { label: "Он сразу бросит null-pointer ошибку.", misconception: "Исключение не бросается — current чисто становится null и цикл просто выходит рано. Баг — тихое усечение, что хуже падения." },
  ]}
/>

## Сниппет 2 — удаление по значению без sentinel

```js
function deleteValue(head, target) {
  let current = head;
  while (current !== null) {
    if (current.value === target) {
      current = current.next; // "удалить" его
    }
    current = current.next;
  }
  return head;
}
```

<Quiz
  id="06-lists-stacks-queues-code-2"
  lessonSlug="06-lists-stacks-queues"
  lang="ru"
  question="Удаляет ли это целевой узел из списка?"
  choices={[
    { label: "Нет. Переприсваивание локальной переменной current никогда не меняет поле next ни одного узла, так что список нетронут; вдобавок при совпадении пропускается узел (два продвижения). Настоящее удаление переставляет предшественника: prev.next = current.next. Sentinel-голова делает случай головы единообразным.", correct: true },
    { label: "Да, current = current.next отвязывает узел.", misconception: "current — локальная переменная. Её перемещение не меняет pointer списка. Чтобы отвязать узел, надо изменить поле next предыдущего узла, а не локальную копию ссылки." },
    { label: "Да, но только для узлов в середине, не головы.", misconception: "Он не удаляет ничего нигде — ни голову, ни середину. Ни один pointer предшественника никогда не переписывается, поэтому ни один узел не отвязывается ни в одной позиции." },
    { label: "Он удаляет узел, но утекает его память.", misconception: "Ничего не удаляется, значит утекать нечему. Фикс — отслеживать prev и ставить prev.next = current.next; sentinel-голова убирает спецслучай головы." },
  ]}
/>

## Сниппет 3 — array queue под нагрузкой

```js
class Queue {
  constructor() { this.items = []; }
  enqueue(x) { this.items.push(x); }
  dequeue() { return this.items.shift(); }
}
// Драйвер: поток из 1 000 000 пар enqueue/dequeue
```

<Quiz
  id="06-lists-stacks-queues-code-3"
  lessonSlug="06-lists-stacks-queues"
  lang="ru"
  question="Очередь корректна, но нагрузочный тест показывает, что она еле ползёт. В чём причина и самый рычажный фикс?"
  choices={[
    { label: "shift удаляет индекс 0 и переиндексирует каждый оставшийся элемент, делая dequeue O(n), а весь поток O(N^2). Замени на linked-list queue (pointer head/tail) или circular buffer с индексами front/rear для dequeue за O(1).", correct: true },
    { label: "Узкое место — push; переключись на unshift.", misconception: "push амортизированно O(1) и в порядке. unshift — O(n) как и shift, и сделал бы медленным ещё и enqueue. Дефект — shift на dequeue, а не push." },
    { label: "Массиву нужна большая преднарезанная ёмкость.", misconception: "Ёмкость не чинит стоимость переиндексации на каждый dequeue. Даже огромный массив платит O(n) на каждый shift. Надо перестать переиндексировать — использовать pointer/индексы." },
    { label: "Массивы JavaScript вообще нельзя использовать как очереди.", misconception: "Можно — circular buffer над массивом с индексами front/rear — отличная queue за O(1). Проблема именно в вызове shift, а не в массиве." },
  ]}
/>

## Сниппет 4 — паттерн monotonic stack

```js
function dailyWarmerWait(temps) {
  const result = new Array(temps.length).fill(0);
  const stack = []; // индексы, убывающие температуры
  for (let i = 0; i < temps.length; i++) {
    while (stack.length && temps[i] > temps[stack[stack.length - 1]]) {
      const j = stack.pop();
      result[j] = i - j;   // дней до более тёплой температуры
    }
    stack.push(i);
  }
  return result;
}
```

<Quiz
  id="06-lists-stacks-queues-code-4"
  lessonSlug="06-lists-stacks-queues"
  lang="ru"
  question="Для temps [73, 74, 75, 71, 69, 72, 76, 73] что это возвращает и какова сложность?"
  choices={[
    { label: "[1, 1, 4, 2, 1, 1, 0, 0] за O(n) времени и O(n) памяти. Это monotonic-убывающий stack индексов слева направо: более тёплый день попает каждый более холодный индекс, который он превосходит, и записывает разрыв в днях; неразрешённые индексы остаются 0. Каждый индекс пушится один раз и попается максимум один раз.", correct: true },
    { label: "Он возвращает следующее более тёплое значение температуры в каждой позиции.", misconception: "Он записывает разрыв в днях (i - j), а не значение температуры. Паттерн — next-greater по расстоянию, а не по значению; прочитай, что пишется в result[j]." },
    { label: "Это O(n^2) из-за вложенного while внутри for.", misconception: "Вложенный while не делает его квадратичным. Каждый индекс попается максимум один раз за весь прогон, так что суммарные попы ограничены n: алгоритм O(n) амортизированно." },
    { label: "Он оставляет 0 для позиций, чей ответ должен быть ненулевым.", misconception: "0 корректен ровно для индексов, которые никогда не попаются — дней без более тёплого будущего дня (здесь последние 76 и 73). Каждому попнутому индексу пишется его реальный разрыв." },
  ]}
/>

<Recap lang="ru">
Каждый баг списков/стеков/очередей читается в pointer и cost model. Разворот должен сохранить преемника до перезаписи current.next, иначе теряет хвост. Удаление должно переставить предшественника — перемещение локальной ссылки не меняет ничего — а sentinel-голова стирает спецслучай головы. FIFO queue никогда не делает dequeue через shift; используй pointer head/tail или circular buffer для O(1). А monotonic stack остаётся линейным, потому что каждый индекс пушится один раз и попается максимум один раз, независимо от внутреннего while.
</Recap>
