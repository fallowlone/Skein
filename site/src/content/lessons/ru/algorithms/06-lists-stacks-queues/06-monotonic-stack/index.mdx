---
concepts:
- monotonic-stack
- monotonic-decreasing
- invariant
- next-greater-element
- amortized
- push-pop
estMin: 25
lang: ru
mathPrereqs: []
order: 6
prereqs:
- 01-the-linked-list
- 02-pointer-manipulation
- 04-the-stack
slug: 06-monotonic-stack
sources:
- https://www.geeksforgeeks.org/dsa/introduction-to-monotonic-stack-2/
- https://labuladong.online/en/algo/data-structure/monotonic-stack/
- https://medium.com/@manuchaitanya/monotonic-stack-for-efficient-problem-solving-next-greater-next-smaller-previous-greater-and-6c63d0572644
- https://algo.monster/problems/mono_stack_intro
status: ready
summary: 'Стек, держащийся в отсортированном порядке (возрастающем или убывающем),
  путём удаления элементов, что нарушают инвариант, перед добавлением. Каноническое
  применение: найти следующий больший элемент справа от каждого элемента за O(n) вместо
  O(n²).'
title: Монотонный стек
track: algorithms
unit: 06-lists-stacks-queues
---



import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Idea from "~/components/lesson/Idea.astro";
import Code from "~/components/lesson/Code.astro";
import Trace from "~/components/lesson/Trace.astro";
import Complexity from "~/components/lesson/Complexity.astro";
import Check from "~/components/lesson/Check.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Term from "~/components/prose/Term.astro";
import AlgoTrace from "~/components/algo/AlgoTrace.astro";
import CodeRunner from "~/components/algo/CodeRunner.astro";
import AlgoPractice from "~/components/algo/AlgoPractice.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DiagramFrame from "~/components/diagram/DiagramFrame.astro";

<Hook>
В прошлом уроке ты выучил очереди и деки. Сегодня ты выучишь мощный вариант <Term slug="stack">стека</Term>: <Term slug="monotonic_stack">монотонный стек</Term>. Это стек, что держит специальное свойство: элементы внутри держатся в отсортированном порядке (либо возрастающем, либо убывающем). Когда ты хочешь добавить новый элемент, ты сначала удаляешь из стека любые элементы, что нарушают этот порядок. Ключевой insight: это нарушает классическое правило "последний добавленный — первый удалённый", но каждый элемент всё ещё добавляется и удаляется не более одного раза, давая O(n) итого. Каноническая задача — найти <Term slug="next_greater_element">следующий больший элемент</Term> (NGE) — для каждого элемента в массиве, найти следующий элемент справа что больше. Наивное решение O(n²), но монотонный стек решает за O(n).
</Hook>

<Goal lang="ru">
После этого урока ты можешь объяснить что такое <Term slug="monotonic_stack">монотонный стек</Term> и как push-with-pop <Term slug="invariant">инвариант</Term> держит порядок, реализовать алгоритм монотонного стека чтобы найти <Term slug="next_greater_element">следующий больший элемент</Term> для каждого элемента за O(n) время, пройти через конкретный пример пошагово, понять почему каждый элемент добавляется и удаляется не более одного раза и как это гарантирует O(n) итого время, и узнать другие задачи (следующий меньший, предыдущий больший) что подходят под паттерн монотонного стека.
</Goal>

<Idea lang="ru">
**Что такое монотонный стек?**

<Term slug="monotonic_stack">Монотонный стек</Term> это вариант обычного <Term slug="stack">стека</Term> что держит строгий порядок среди своих элементов. Вместо того чтобы позволить любому элементу быть добавленным, ты применяешь правило: элементы должны быть в возрастающем порядке (или убывающем порядке, зависит от твоего выбора). Когда новый элемент прибывает:

1. Пока стек не пуст и вершина нарушает порядок, удали вершину.
2. Добавь новый элемент.

Результат: после каждого добавления, содержимое стека формирует отсортированную последовательность.

**Возрастающий vs. убывающий:**

- **Монотонный возрастающий:** стек = [1, 3, 5, 7]. Когда ты добавляешь 2, удали 7, 5, 3 сначала. Потом добавь 2 → [1, 2].
- **Монотонный убывающий:** стек = [7, 5, 3, 1]. Когда ты добавляешь 6, удали 5, 3, 1 сначала. Потом добавь 6 → [7, 6].

**Задача следующий-больший-элемент:**

Дан массив, для каждого элемента, найди следующий элемент справа что больше. Если такого элемента нет, верни -1.

Пример: arr = [2, 1, 2, 4, 3]
- Элемент 2 (индекс 0): следующий больший это 4. Ответ: 4.
- Элемент 1 (индекс 1): следующий больший это 2. Ответ: 2.
- Элемент 2 (индекс 2): следующий больший это 4. Ответ: 4.
- Элемент 4 (индекс 3): нет больших элементов справа. Ответ: -1.
- Элемент 3 (индекс 4): нет больших элементов справа. Ответ: -1.

**Наивный подход: O(n²)**

Для каждого элемента, отсканируй все элементы справа чтобы найти следующий больший. Худший случай: n * n = O(n²).

```js
function nextGreaterBrute(arr) {
  const result = new Array(arr.length);
  for (let i = 0; i < arr.length; i++) {
    result[i] = -1;  // По умолчанию: нет больших элементов
    for (let j = i + 1; j < arr.length; j++) {
      if (arr[j] > arr[i]) {
        result[i] = arr[j];
        break;
      }
    }
  }
  return result;
}
```

Это медленно. По мере того как ты читаешь больше элементов, стоимость вложенного цикла взрывается.

**Подход монотонного стека: O(n)**

Insight: обработай массив справа налево. Держи монотонный убывающий стек элементов, которые ты видел до сих пор. Для каждого нового элемента:

1. Удали все элементы из стека что ≤ новому элементу (они не могут быть "следующий больший" ни для чего).
2. Если стек не пуст, вершина это следующий больший элемент для текущего элемента.
3. Добавь текущий элемент в стек.

Почему это работает? Стек действует как "список кандидатов". К тому времени как ты посетишь элемент, стек уже содержит все возможные кандидаты справа, предварительно отфильтрованные и отсортированные.

Почему это O(n)? Каждый элемент добавляется один раз и удаляется не более одного раза. Так что итого операций = O(2n) = O(n).

</Idea>

<DiagramFrame label="Монотонный убывающий стек, сканирующий массив [5, 3, 1, 4]. Стек слева держит значения 5, 3, 1 снизу вверх, в строго убывающем порядке — монотонный инвариант. Входящее значение 4 больше вершины стека, поэтому оно удаляет 1, затем удаляет 3 (каждое удаление даёт ответ следующий-больший, равный 4), останавливаясь на 5, которое больше. Затем 4 добавляется, оставляя стек 5, 4 снизу вверх — всё ещё строго убывающий." hue="var(--d-systems)" caption="Один шаг сканирования. Входящее значение (4) удаляет каждый меньший элемент с убывающего стека — 1 и 3, для каждого 4 является следующим большим элементом — затем добавляется наверх. Оставшаяся 5 больше, поэтому она остаётся. После добавления стек всё ещё строго убывающий: монотонный инвариант держится.">
  <svg viewBox="0 0 480 300" width="480" height="300" style="display:block;margin:0 auto;max-width:100%;height:auto;">
    <text x="240" y="22" text-anchor="middle" font-size="11" font-family="var(--font-body)" fill="var(--muted)">массив, сканируем справа налево — входящее значение = 4</text>
    <rect x="120" y="34" width="44" height="34" rx="4" fill="var(--card-2)" stroke="var(--hairline-strong)" stroke-width="1.2" />
    <text x="142" y="56" text-anchor="middle" font-size="14" font-family="var(--font-body)" fill="var(--ink)">5</text>
    <rect x="164" y="34" width="44" height="34" rx="4" fill="var(--card-2)" stroke="var(--hairline-strong)" stroke-width="1.2" />
    <text x="186" y="56" text-anchor="middle" font-size="14" font-family="var(--font-body)" fill="var(--ink)">3</text>
    <rect x="208" y="34" width="44" height="34" rx="4" fill="var(--card-2)" stroke="var(--hairline-strong)" stroke-width="1.2" />
    <text x="230" y="56" text-anchor="middle" font-size="14" font-family="var(--font-body)" fill="var(--ink)">1</text>
    <rect x="252" y="34" width="44" height="34" rx="4" fill="var(--card-2)" stroke="var(--d-systems)" stroke-width="2" />
    <text x="274" y="56" text-anchor="middle" font-size="14" font-family="var(--font-body)" fill="var(--ink)">4</text>
    <text x="274" y="84" text-anchor="middle" font-size="9" font-family="var(--font-body)" fill="var(--d-systems)">входящее</text>

    <text x="86" y="116" text-anchor="middle" font-size="11" font-family="var(--font-body)" fill="var(--muted)">ДО</text>
    <text x="86" y="276" text-anchor="middle" font-size="9" font-family="var(--font-body)" fill="var(--muted)">убывание ↑</text>
    <rect x="44" y="234" width="84" height="30" rx="3" fill="var(--card-2)" stroke="var(--hairline-strong)" stroke-width="1.4" />
    <text x="86" y="254" text-anchor="middle" font-size="13" font-family="var(--font-body)" fill="var(--ink)">5</text>
    <rect x="44" y="200" width="84" height="30" rx="3" fill="var(--card-2)" stroke="var(--hairline-strong)" stroke-width="1.4" stroke-dasharray="4 3" />
    <text x="86" y="220" text-anchor="middle" font-size="13" font-family="var(--font-body)" fill="var(--muted)">3</text>
    <rect x="44" y="166" width="84" height="30" rx="3" fill="var(--card-2)" stroke="var(--hairline-strong)" stroke-width="1.4" stroke-dasharray="4 3" />
    <text x="86" y="186" text-anchor="middle" font-size="13" font-family="var(--font-body)" fill="var(--muted)">1</text>
    <text x="86" y="158" text-anchor="middle" font-size="9" font-family="var(--font-body)" fill="var(--muted)">вершина</text>

    <path d="M150 200 L210 200" fill="none" stroke="var(--d-systems)" stroke-width="1.6" marker-end="url(#mono-arrow-hue)" />
    <text x="180" y="150" text-anchor="middle" font-size="10" font-family="var(--font-body)" fill="var(--d-systems)">4 &gt; 1 и 4 &gt; 3</text>
    <text x="180" y="166" text-anchor="middle" font-size="10" font-family="var(--font-body)" fill="var(--d-systems)">удалить оба</text>
    <text x="180" y="216" text-anchor="middle" font-size="9" font-family="var(--font-body)" fill="var(--muted)">следующий больший = 4</text>
    <text x="180" y="252" text-anchor="middle" font-size="10" font-family="var(--font-body)" fill="var(--muted)">4 &lt; 5: стоп, оставить 5</text>

    <text x="394" y="116" text-anchor="middle" font-size="11" font-family="var(--font-body)" fill="var(--muted)">ПОСЛЕ push 4</text>
    <text x="394" y="276" text-anchor="middle" font-size="9" font-family="var(--font-body)" fill="var(--muted)">убывание ↑</text>
    <rect x="352" y="234" width="84" height="30" rx="3" fill="var(--card-2)" stroke="var(--hairline-strong)" stroke-width="1.4" />
    <text x="394" y="254" text-anchor="middle" font-size="13" font-family="var(--font-body)" fill="var(--ink)">5</text>
    <rect x="352" y="200" width="84" height="30" rx="3" fill="var(--card-2)" stroke="var(--d-systems)" stroke-width="2" />
    <text x="394" y="220" text-anchor="middle" font-size="13" font-family="var(--font-body)" fill="var(--ink)">4</text>
    <text x="394" y="192" text-anchor="middle" font-size="9" font-family="var(--font-body)" fill="var(--d-systems)">вершина (добавлено)</text>
    <path d="M210 244 L350 244" fill="none" stroke="var(--hairline-strong)" stroke-width="1.4" marker-end="url(#mono-arrow)" />

    <defs>
      <marker id="mono-arrow" viewBox="0 0 10 10" refX="9" refY="5" markerWidth="7" markerHeight="7" orient="auto-start-reverse">
        <path d="M0 0 L10 5 L0 10 z" fill="var(--hairline-strong)" />
      </marker>
      <marker id="mono-arrow-hue" viewBox="0 0 10 10" refX="9" refY="5" markerWidth="7" markerHeight="7" orient="auto-start-reverse">
        <path d="M0 0 L10 5 L0 10 z" fill="var(--d-systems)" />
      </marker>
    </defs>
  </svg>
</DiagramFrame>

<Code lang="ru">
**Решение монотонного стека для следующий больший элемент:**

```js
function nextGreater(arr) {
  const stack = [];  // Стек индексов (не значений)
  const result = new Array(arr.length).fill(-1);
  
  // Обработай справа налево
  for (let i = arr.length - 1; i >= 0; i--) {
    // Удали элементы что <= текущему элементу
    while (stack.length > 0 && arr[stack[stack.length - 1]] <= arr[i]) {
      stack.pop();
    }
    
    // Если стек не пуст, вершина это следующий больший элемент
    if (stack.length > 0) {
      result[i] = arr[stack[stack.length - 1]];
    }
    
    // Добавь текущий индекс
    stack.push(i);
  }
  
  return result;
}
```

**Почему мы храним индексы, не значения:**

Мы добавляем индексы в стек, так что позже, когда нам нужно фактическое значение следующего большего элемента, мы можем индексировать в массив. Этот pattern часто встречается в задачах с монотонным стеком.

Попробуй в интерактивном runner:

<CodeRunner
  id="cr-nge"
  lang="ru"
  code={`function nextGreater(arr) {
  const stack = [];  // Стек индексов
  const result = new Array(arr.length).fill(-1);
  
  for (let i = arr.length - 1; i >= 0; i--) {
    // Удали пока вершина <= текущему
    while (stack.length > 0 && arr[stack[stack.length - 1]] <= arr[i]) {
      stack.pop();
    }
    
    // Вершина это следующий больший (если стек не пуст)
    if (stack.length > 0) {
      result[i] = arr[stack[stack.length - 1]];
    }
    
    // Добавь текущий индекс
    stack.push(i);
  }
  
  return result;
}

// Test 1: Basic case
console.log("Test [2, 1, 2, 4, 3]:");
console.log(nextGreater([2, 1, 2, 4, 3]));  // [4, 2, 4, -1, -1]

// Test 2: Increasing array
console.log("\\nTest [1, 2, 3, 4]:");
console.log(nextGreater([1, 2, 3, 4]));  // [2, 3, 4, -1]

// Test 3: Decreasing array (no next greater)
console.log("\\nTest [4, 3, 2, 1]:");
console.log(nextGreater([4, 3, 2, 1]));  // [-1, -1, -1, -1]

// Test 4: Duplicates
console.log("\\nTest [3, 3, 5, 2]:");
console.log(nextGreater([3, 3, 5, 2]));  // [5, 5, -1, -1]`}
/>
</Code>

<Trace lang="ru">
Давай пройдём через алгоритм монотонного стека на массиве [2, 1, 2, 4, 3].

Мы обрабатываем справа налево (i = 4, 3, 2, 1, 0). На каждом шаге, мы удаляем индексы чьи значения ≤ текущему значению, потом добавляем текущий индекс.

<AlgoTrace
  id="trace-nge"
  lang="ru"
  code={[
    "for (let i = arr.length - 1; i >= 0; i--) {",
    "  while (stack.length > 0 && arr[stack[stack.length - 1]] <= arr[i]) {",
    "    stack.pop();",
    "  }",
    "  if (stack.length > 0) {",
    "    result[i] = arr[stack[stack.length - 1]];",
    "  }",
    "  stack.push(i);",
    "}"
  ]}
  frames={[
    {
      caption: "Старт. Массив: [2, 1, 2, 4, 3]. i = 4 (значение 3). Стек пуст.",
      line: 1,
      cells: [
        { value: "2", mark: "value" },
        { value: "1", mark: "value" },
        { value: "2", mark: "value" },
        { value: "4", mark: "value" },
        { value: "3", mark: "target" }
      ],
      stackState: "Стек: []"
    },
    {
      caption: "i = 4 (значение 3). Нет удалений (стек пуст). Добавь индекс 4. result[4] = -1.",
      line: 8,
      cells: [
        { value: "2", mark: "value" },
        { value: "1", mark: "value" },
        { value: "2", mark: "value" },
        { value: "4", mark: "value" },
        { value: "3", mark: "done" }
      ],
      stackState: "Стек: [4] (значение 3)"
    },
    {
      caption: "i = 3 (значение 4). arr[4] = 3 ≤ 4? Да, удали 4. Стек пуст. Добавь индекс 3. result[3] = -1.",
      line: 2,
      cells: [
        { value: "2", mark: "value" },
        { value: "1", mark: "value" },
        { value: "2", mark: "value" },
        { value: "4", mark: "target" },
        { value: "3", mark: "done" }
      ],
      stackState: "Стек: [3] (значение 4)"
    },
    {
      caption: "i = 2 (значение 2). arr[3] = 4 ≤ 2? Нет. Вершина стека это arr[3] = 4. result[2] = 4. Добавь индекс 2.",
      line: 6,
      cells: [
        { value: "2", mark: "value" },
        { value: "1", mark: "value" },
        { value: "2", mark: "target" },
        { value: "4", mark: "value" },
        { value: "3", mark: "done" }
      ],
      stackState: "Стек: [3, 2] (значения 4, 2)"
    },
    {
      caption: "i = 1 (значение 1). arr[2] = 2 ≤ 1? Нет. Вершина это arr[2] = 2. result[1] = 2. Добавь индекс 1.",
      line: 6,
      cells: [
        { value: "2", mark: "value" },
        { value: "1", mark: "target" },
        { value: "2", mark: "value" },
        { value: "4", mark: "value" },
        { value: "3", mark: "done" }
      ],
      stackState: "Стек: [3, 2, 1] (значения 4, 2, 1)"
    },
    {
      caption: "i = 0 (значение 2). arr[1] = 1 ≤ 2? Да, удали 1. arr[2] = 2 ≤ 2? Да, удали 2. arr[3] = 4 ≤ 2? Нет. Вершина это 4. result[0] = 4. Добавь 0.",
      line: 2,
      cells: [
        { value: "2", mark: "target" },
        { value: "1", mark: "done" },
        { value: "2", mark: "done" },
        { value: "4", mark: "value" },
        { value: "3", mark: "done" }
      ],
      stackState: "Стек: [3, 0] (значения 4, 2)"
    },
    {
      caption: "Готово. result = [4, 2, 4, -1, -1]",
      line: 9,
      cells: [
        { value: "2", mark: "done" },
        { value: "1", mark: "done" },
        { value: "2", mark: "done" },
        { value: "4", mark: "done" },
        { value: "3", mark: "done" }
      ],
      stackState: "Итоговый результат: [4, 2, 4, -1, -1]"
    }
  ]}
/>

</Trace>

<Complexity lang="ru">
**Временная сложность: O(n)**

Каждый элемент добавляется в стек ровно один раз. Каждый элемент удаляется не более одного раза. Итого операций = добавления + удаления ≤ 2n. Так что временная сложность это O(n).

Это драматическое улучшение по сравнению с наивным подходом O(n²).

**Пространственная сложность: O(n)**

В худшем случае, стек хранит все n индексов (например, если массив в возрастающем порядке). Массив результата также использует O(n) пространства.

**Сравнение с наивным подходом:**

| Подход | Время | Пространство |
|---|---|---|
| Вложенный цикл (наивный) | O(n²) | O(n) |
| Монотонный стек | O(n) | O(n) |

Для больших массивов, монотонный стек намного быстрее.

</Complexity>

<AlgoPractice
  id="practice-nge"
  lessonSlug="06-monotonic-stack"
  lang="ru"
  problems={[
    {
      kind: "choice",
      prompt: "В подходе монотонного стека для следующего большего элемента, мы обрабатываем массив в каком направлении?",
      choices: [
        "Слева направо",
        "Справа налево",
        "От центра наружу",
        "В любом порядке, это не важно"
      ],
      answer: 1,
      hint: "Мы начинаем с самого правого элемента и движемся налево. Это позволяет нам строить стек 'кандидатов справа' по ходу."
    },
    {
      kind: "choice",
      prompt: "Когда мы видим новый элемент в монотонном стеке для следующего большего элемента, какие элементы мы удаляем?",
      choices: [
        "Все элементы",
        "Элементы с чётными индексами",
        "Элементы, чьи значения ≤ новому элементу",
        "Только верхний элемент"
      ],
      answer: 2,
      hint: "Мы удаляем элементы, которые ≤ текущему элементу, потому что они не могут быть следующим большим элементом ничего слева."
    },
    {
      kind: "choice",
      prompt: "Что хранится в монотонном стеке: значения массива или индексы массива?",
      choices: [
        "Только значения массива",
        "Индексы массива, потому что нам нужно получить доступ к исходному массиву позже",
        "И значения, и индексы",
        "Ни то, ни другое; мы используем отдельную структуру данных"
      ],
      answer: 1,
      hint: "Мы храним индексы, чтобы когда нам нужно значение следующего большего элемента, мы могли использовать индекс для поиска значения в исходном массиве."
    },
    {
      kind: "choice",
      prompt: "Для массива [1, 2, 3, 4], какой следующий больший элемент для индекса 0?",
      choices: [
        "1",
        "2",
        "4",
        "-1 (нет большего элемента)"
      ],
      answer: 1,
      hint: "На индексе 0, значение это 1. Следующее большее значение справа это 2 (на индексе 1)."
    },
    {
      kind: "choice",
      prompt: "Почему подход монотонного стека O(n) вместо O(n²)?",
      choices: [
        "Потому что массив всегда отсортирован",
        "Потому что каждый элемент добавляется и удаляется не более одного раза",
        "Потому что мы используем хеш-таблицу",
        "Потому что мы пропускаем половину массива"
      ],
      answer: 1,
      hint: "Итого операций = добавления + удаления ≤ 2n = O(n). Каждый элемент переходит в стек и из стека ровно один раз."
    }
  ]}
/>

<Inset kind="edgecase" lang="ru">
**Что если есть равные элементы?** Формулировка задачи обычно говорит "следующий больший," что значит строго больший (не равный). В коде, мы используем `<=` в условии удаления, что гарантирует что равные элементы также удаляются. Если тебе нужен "следующий больший-или-равный," измени `<=` на `<`.
</Inset>

<Inset kind="mistake" lang="ru">
**"Монотонный стек = всегда возрастающий."** Неправильно. Монотонный стек может быть возрастающий или убывающий. Для следующего большего элемента, мы используем убывающий стек (так что вершина это всегда самый маленький кандидат справа, т.е. следующий больший). Для следующего меньшего элемента, мы используем возрастающий стек.
</Inset>

<Inset kind="practice" lang="ru">
**Следующие задачи:**
- **Следующий меньший элемент (NSE):** Используй монотонный возрастающий стек.
- **Предыдущий больший элемент (PGE):** Обработай слева направо с монотонным убывающим стеком, отслеживая индексы слева.
- **Самый большой прямоугольник в гистограмме:** Классическая задача с монотонным стеком где ты удаляешь когда высота уменьшается, вычисляя площади по дороге. Паттерн похож: держи порядок и удаляй чтобы обнаружить когда ограничение нарушается.
</Inset>

<Check lang="ru">
  <Quiz
    id="quiz-nge"
    pieceSlug="06-monotonic-stack"
    lang="ru"
    question="Почему подход монотонного стека работает для поиска следующего большего элемента?"
    choices={[
      {
        label: "Стек держит кандидатов (элементы справа) в убывающем порядке, так что вершина это всегда следующий больший элемент когда мы удаляем меньшие элементы.",
        correct: true
      },
      {
        label: "Это работает потому что мы сначала отсортируем массив.",
        misconception: "Мы никогда не сортируем массив. Стек естественно держит отсортированное (убывающее) подмножество элементов справа."
      },
      {
        label: "Это работает потому что мы используем рекурсию для исследования всех возможностей.",
        misconception: "Здесь нет рекурсии. Стек итеративно строит и держит список кандидатов."
      },
      {
        label: "Это работает потому что массив всегда в специфическом порядке.",
        misconception: "Алгоритм работает для любого порядка массива. Монотонный стек налагает порядок внутренне."
      }
    ]}
  />
</Check>

<Recap lang="ru">
**Монотонный стек: список отсортированных кандидатов**

1. **Что это:** Стек что держит элементы в отсортированном порядке (возрастающем или убывающем). Когда ты добавляешь новый элемент, ты сначала удаляешь элементы что нарушают порядок.

2. **Инвариант:** После каждого добавления, стек отсортирован (например, убывающий для NGE). Это свойство гарантирует что ты держишь только релевантные кандидаты.

3. **Следующий больший элемент:** Обработай массив справа налево. Удали индексы чьи значения ≤ текущему. Вершина это ответ. Добавь текущий индекс. Результат: O(n) вместо O(n²).

4. **Почему O(n):** Каждый элемент добавляется один раз и удаляется один раз. Итого операций = O(2n) = O(n).

5. **Возрастающий vs. убывающий:** Для следующего большего элемента, используй убывающий. Для следующего меньшего элемента, используй возрастающий. Выбор зависит от какого направления тебе нужно.

6. **Реальные приложения:** Размах акций (Stock span), самый большой прямоугольник в гистограмме, вода в ловушке (Trapping rain water), дневные температуры. Любая задача что спрашивает для следующий/предыдущий элемент удовлетворяющий ограничение это кандидат для монотонного стека.

7. **Ключевой insight:** Стек это не контейнер общего назначения; это инструмент для эффективной фильтрации и упорядочения кандидатов. Отсортированный инвариант это что делает магию.

Теперь у тебя есть инструменты чтобы решать задачи что наивно кажутся O(n²) но фактически O(n) с умно держимым стеком.
</Recap>
