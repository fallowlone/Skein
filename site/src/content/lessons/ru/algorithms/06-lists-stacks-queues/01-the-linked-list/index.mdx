---
slug: 01-the-linked-list
lang: ru
track: algorithms
unit: 06-lists-stacks-queues
order: 1
title: "Связный список"
summary: "Связный список — это последовательность узлов. Каждый узел содержит значение и указатель на следующий узел. В отличие от массива (контигуозная память), связный список использует разреженную память: стоимость индексирования O(n), но вставка в начало O(1)."
estMin: 18
status: ready
prereqs: ["01-the-array"]
mathPrereqs: []
concepts: ["linked_list", "node", "pointer", "head", "next"]
sources:
  - https://www.tutorialspoint.com/data_structures_algorithms/linked_list_algorithms.htm
  - https://www.programiz.com/dsa/linked-list
  - https://launchschool.com/books/dsa/read/comparing_arrays_and_linked_lists
  - https://www.happycoders.eu/algorithms/array-vs-linked-list/
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Idea from "~/components/lesson/Idea.astro";
import Code from "~/components/lesson/Code.astro";
import Trace from "~/components/lesson/Trace.astro";
import Complexity from "~/components/lesson/Complexity.astro";
import Check from "~/components/lesson/Check.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import AnnotatedCode from "~/components/algo/AnnotatedCode.astro";
import AlgoTrace from "~/components/algo/AlgoTrace.astro";
import CodeRunner from "~/components/algo/CodeRunner.astro";
import AlgoPractice from "~/components/algo/AlgoPractice.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Ты знаешь, что массив — это ряд смежных ячеек в памяти, и что смежность даёт тебе O(1) индексирование, но O(n) вставку в середину. Теперь представь другую структуру: узлы разбросаны по памяти, каждый держит значение и указывает на следующий узел. Это связный список. Ты теряешь суперсилу O(1) индексирования — нахождение 5-го элемента теперь стоит O(n) — но получаешь суперсилу: вставка в начало за O(1), без сдвигов.
</Hook>

<Goal lang="ru">
После этого урока ты можешь объяснить, что такое узел и указатель, нарисовать связный список в памяти, реализовать push (вставку в начало) и обход, понять, почему индексирование по позиции O(n), а вставка в начало O(1), сравнить временные и пространственные затраты операций связного списка и массива, и узнать, когда связный список — правильный выбор.
</Goal>

<Idea lang="ru">
**Базовая идея: узлы и указатели**

**Массив** — это ряд ячеек, выстроенных в ряд. **Связный список** — это последовательность **узлов**. Каждый узел — маленький контейнер, держащий две вещи:
1. **Значение** (данные, что ты хочешь хранить)
2. **Указатель** (адрес памяти, указывающий на следующий узел)

Связный список начинается со специального узла, называемого **head** (голова). Если ты хочешь посетить каждое значение, начни с head, прочитай значение, проследуй по указателю на следующий узел, и повтори.

Пример: связный список, содержащий [10, 20, 30]:
```
head → [10 | next → ] → [20 | next → ] → [30 | next → null]
```

Каждый `[ value | next → ]` это узел. Указатель следующего узла это `null`, означая «больше нет узлов».

**Почему разреженная память важна:**

Массивы хранят все элементы в смежном блоке. Компьютер может вычислить адрес любого элемента мгновенно: `base_address + (index × element_size)`.

Связные списки иные. Узлы могут храниться где угодно в памяти. Чтобы найти узел на позиции 5, ты не можешь вычислить его адрес. Вместо этого ты должен начать с head, проследить указатель, шаг 1, шаг 2, шаг 3, шаг 4, шаг 5. Это стоит O(n) времени.

**Почему указатели важны:**

Потому что узлы разреженны, каждый узел должен хранить адрес следующего узла. Тот адрес — это **указатель**. В памяти указатель это просто число — обычно 8 байт на 64-битной системе — но оно говорит компьютеру: «Следующий кусок данных находится по этому адресу».

Пример расположения в памяти (воображаемый):
```
Адрес 1000: узел 1 = [10, указатель на 3008]
Адрес 3008: узел 2 = [20, указатель на 5016]
Адрес 5016: узел 3 = [30, указатель на null]
```

Заметь, что адреса не последовательны. Узлы 1, 2, 3 лежат на 1000, 3008, 5016. Но каждый узел указывает на следующий, так что мы можем обойти список.

**Вставка в начало: суперсила связного списка**

В массиве вставка в начало требует сдвига всех n элементов. Дорого.

В связном списке вставка в начало тривиальна:
1. Создай новый узел со своим значением.
2. Установи его указатель на текущий head.
3. Обнови head, чтобы он указывал на новый узел.

Это три операции. Время: **O(1)**.

<AnnotatedCode
  code={`class Node {
  constructor(value, next = null) {
    this.value = value;
    this.next = next;
  }
}

let head = new Node(10);  // Список: [10]

// Вставь 20 в начало
head = new Node(20, head);  // Список: [20, 10]

// Вставь 30 в начало
head = new Node(30, head);  // Список: [30, 20, 10]`}
  annotations={[
    { line: 1, text: "Каждый узел держит значение и указатель на следующий узел" },
    { line: 8, text: "Создай первый узел" },
    { line: 11, text: "Создай новый узел, указывающий на старый head" },
    { line: 14, text: "Обнови head на новый узел. O(1) — без сдвигов!" }
  ]}
/>

**Обход: стоимость связного списка**

Чтобы найти значение или получить доступ на позицию i, ты должен обойти с head:

<AnnotatedCode
  code={`function getAtIndex(head, index) {
  let current = head;
  for (let i = 0; i < index; i++) {
    if (current === null) return null;  // Конец списка
    current = current.next;             // Проследуй указатель
  }
  return current?.value;
}`}
  annotations={[
    { line: 2, text: "Начни с head" },
    { line: 3, text: "Цикл index раз" },
    { line: 5, text: "Проследуй указатель на следующий узел" },
    { line: 8, text: "Верни значение на позиции index, или null если не найдено" }
  ]}
/>

Для списка из n узлов поиск позиции i стоит O(i) времени. В худшем случае (i = n) это O(n).

**Связный список в сравнении с массивом в целом:**

| Операция | Массив | Связный список |
|---|---|---|
| Доступ по индексу | O(1) | O(n) |
| Вставка в начало | O(n) | O(1) |
| Вставка в середину | O(n) | O(n) |
| Вставка в конец | O(1) амортизировано | O(n) |
| Удаление в начале | O(n) | O(1) |
| Удаление в середине | O(n) | O(n) |
| Поиск значения | O(n) | O(n) |

Трейд-офф: массивы быстры для чтения; связные списки быстры для вставки в начало.
</Idea>

<Code lang="ru">
Давай построим простой класс связного списка с push (вставка в начало) и обходом:

```js
class Node {
  constructor(value, next = null) {
    this.value = value;
    this.next = next;
  }
}

class LinkedList {
  constructor() {
    this.head = null;
  }

  // Вставка в начало: O(1)
  push(value) {
    this.head = new Node(value, this.head);
  }

  // Получить значение на индексе: O(n)
  getAtIndex(index) {
    let current = this.head;
    for (let i = 0; i < index; i++) {
      if (current === null) return null;
      current = current.next;
    }
    return current?.value;
  }

  // Получить узел на индексе (полезно для удаления/вставки): O(n)
  getNodeAt(index) {
    let current = this.head;
    for (let i = 0; i < index; i++) {
      if (current === null) return null;
      current = current.next;
    }
    return current;
  }

  // Обход и печать всех значений: O(n)
  traverse() {
    const values = [];
    let current = this.head;
    while (current !== null) {
      values.push(current.value);
      current = current.next;
    }
    return values;
  }
}
```

Попробуй построить и обойти связный список:

<CodeRunner
  id="cr-linkedlist-1"
  lang="ru"
  code={`class Node {
  constructor(value, next = null) {
    this.value = value;
    this.next = next;
  }
}

class LinkedList {
  constructor() {
    this.head = null;
  }

  push(value) {
    this.head = new Node(value, this.head);
  }

  getAtIndex(index) {
    let current = this.head;
    for (let i = 0; i < index; i++) {
      if (current === null) return null;
      current = current.next;
    }
    return current?.value;
  }

  traverse() {
    const values = [];
    let current = this.head;
    while (current !== null) {
      values.push(current.value);
      current = current.next;
    }
    return values;
  }
}

const list = new LinkedList();

// Push значения: O(1) каждое
list.push(30);
list.push(20);
list.push(10);

console.log("Список после pushes:", list.traverse());

// Получить по индексу: O(n)
console.log("Элемент на индексе 0:", list.getAtIndex(0));
console.log("Элемент на индексе 2:", list.getAtIndex(2));

// Попробуй доступ за пределами границ
console.log("Элемент на индексе 5:", list.getAtIndex(5));`}
/>
</Code>

<Trace lang="ru">
Давай проследим операцию push пошагово. Начинаем с пустого списка и вставляем три значения: 10, 20, 30.

<AlgoTrace
  id="trace-push"
  lang="ru"
  code={[
    "const list = new LinkedList();",
    "list.push(10);",
    "list.push(20);",
    "list.push(30);"
  ]}
  frames={[
    {
      caption: "Начало: список пуст, head = null",
      line: 1,
      cells: []
    },
    {
      caption: "После push(10): создай узел [10 | null], установи head на него. Список: [10]",
      line: 2,
      cells: ["10"]
    },
    {
      caption: "После push(20): создай узел [20 | next→10], установи head на него. Список: [20, 10]",
      line: 3,
      cells: ["20", "10"]
    },
    {
      caption: "После push(30): создай узел [30 | next→20], установи head на него. Список: [30, 20, 10]",
      line: 4,
      cells: ["30", "20", "10"]
    }
  ]}
/>

Теперь проследим вызов getAtIndex(2) на списке [30, 20, 10]:

<AlgoTrace
  id="trace-indexing"
  lang="ru"
  code={[
    "function getAtIndex(index) {",
    "  let current = head;",
    "  for (let i = 0; i < index; i++) {",
    "    current = current.next;",
    "  }",
    "  return current?.value;",
    "}"
  ]}
  frames={[
    {
      caption: "Начало: index = 2, current = head (узел 30)",
      line: 2,
      cells: [
        { value: "30", mark: "target" },
        { value: "20", mark: "value" },
        { value: "10", mark: "value" }
      ]
    },
    {
      caption: "Цикл i=0: проследуй указатель, current = узел 20",
      line: 4,
      cells: [
        { value: "30", mark: "done" },
        { value: "20", mark: "target" },
        { value: "10", mark: "value" }
      ]
    },
    {
      caption: "Цикл i=1: проследуй указатель, current = узел 10",
      line: 4,
      cells: [
        { value: "30", mark: "done" },
        { value: "20", mark: "done" },
        { value: "10", mark: "target" }
      ]
    },
    {
      caption: "Цикл кончается (i=2). Верни current.value = 10. Потребовалось 2 прыжка по указателям.",
      line: 6,
      cells: [
        { value: "30", mark: "done" },
        { value: "20", mark: "done" },
        { value: "10", mark: "done" }
      ]
    }
  ]}
/>

Каждый прыжок на следующий узел это один шаг. Чтобы достичь позицию i, ты должен прыгнуть i раз. Время: O(i).
</Trace>

<Complexity lang="ru">
**Операции связного списка:**

| Операция | Время | Пространство |
|---|---|---|
| Доступ к элементу на индексе i | O(i) | O(1) aux |
| Поиск значения | O(n) | O(1) aux |
| Push (вставка в начало) | O(1) | O(1) |
| Вставка после узла на индексе i | O(i) чтобы найти узел, потом O(1) вставить | O(1) |
| Удаление в начале | O(1) | O(1) |
| Удаление на индексе i | O(i) | O(1) |
| Обход всех | O(n) | O(1) aux |

**Почему push это O(1)?** Ты создаёшь новый узел и обновляешь один указатель. Две операции. Без обхода.

**Почему доступ по индексу O(n)?** Ты должен проследить указатели один за другим. Нет формулы, чтобы прыгнуть сразу на позицию i.

**Контраст с массивами:**

Массивы O(1) для индексирования, потому что память смежна. Связные списки O(n), потому что память разреженна и ты должен проследить указатели.

Интуиция: **Смежность даёт расчёт. Разреженность требует обхода.**
</Complexity>

<AlgoPractice
  id="practice-linkedlist-1"
  lessonSlug="01-the-linked-list"
  lang="ru"
  problems={[
    {
      kind: "choice",
      prompt: "В связном списке из 1000 узлов сколько времени требуется, чтобы найти узел на индексе 500?",
      choices: [
        "O(1) — компьютер может прыгнуть на любой узел мгновенно",
        "O(500) — ты должен проследить 500 указателей",
        "O(1000) — ты должен обойти весь список",
        "O(log n) — связные списки это как бинарный поиск"
      ],
      answer: 1,
      hint: "Ты начинаешь с head и проследуешь указатель на следующий узел 500 раз. Линейно в позиции, не в размере списка."
    },
    {
      kind: "choice",
      prompt: "В связном списке почему вставка в начало O(1)?",
      choices: [
        "Потому что ты создаёшь новый узел и сдвигаешь его в начало по формуле",
        "Потому что ты создаёшь новый узел, указываешь его на старый head, и обновляешь указатель head — без обхода",
        "Потому что список держит отсортированный индекс того, куда вставлять",
        "Потому что связные списки всегда используют O(1) вставку"
      ],
      answer: 1,
      hint: "Нет сдвигов, нет поиска. Просто создай узел, укажи его на старый head, и измени одну ссылку."
    },
    {
      kind: "choice",
      prompt: "Массив из 100 элементов даёт O(1) доступ к arr[99]. Связный список из 100 узлов требует O(n) чтобы дошла до узла 99. Почему эта разница?",
      choices: [
        "Связные списки всегда медленнее массивов",
        "Массивы используют смежную память, так что адрес может быть вычислен. Связные списки используют разреженную память, так что ты должен проследить указатели.",
        "Связные списки забыли добавить формулу вычисления адреса",
        "Индексирование в массиве быстро благодаря облаку; связные списки работают на-премесе"
      ],
      answer: 1,
      hint: "Ключ это расположение памяти. Смежность → формула. Разреженность → обход."
    },
    {
      kind: "numeric",
      prompt: "Ты вставляешь значения 10, 20, 30 в начало связного списка (в таком порядке). Какой список после всех трёх pushes?",
      answer: 30,
      hint: "Push 10: [10]. Push 20: [20, 10]. Push 30: [30, 20, 10]. Передний элемент это 30."
    },
    {
      kind: "choice",
      prompt: "В связном списке почему ты не можешь просто вычислить адрес памяти узла на позиции i, как ты делаешь в массиве?",
      choices: [
        "Потому что связный список слишком сложный",
        "Потому что узлы разреженны по памяти, и нет формулы, связывающей позицию i с адресом памяти",
        "Потому что компьютеры не поддерживают арифметику указателей в связных списках",
        "Потому что JavaScript не позволяет адреса памяти"
      ],
      answer: 1,
      hint: "Массивы: address = base + (i × size). Связные списки: нет такой формулы; узлы могут быть где угодно."
    }
  ]}
/>

<Inset kind="edgecase" lang="ru">
**Вставка в середину:** Если у тебя есть ссылка на узел и ты хочешь вставить после него, это O(1) — просто перенаправь указатель. Но если ты хочешь вставить на позицию i (где у тебя ещё нет ссылки), ты должен сначала обойти на позицию i, что стоит O(i). Итак, «вставка на позицию i» в связном списке это O(i), не O(1).
</Inset>

<Inset kind="mistake" lang="ru">
**«Связные списки всегда медленнее.»** Неправда. Связные списки медленнее на индексирование (O(n)), но быстрее на вставку в начало (O(1)). Правильный инструмент зависит от задачи. Если ты часто вставляешь в начало и редко индексируешь, связный список лучше. Если ты часто индексируешь, массив лучше.
</Inset>

<Check lang="ru">
  <Quiz
    id="quiz-linkedlist-1"
    pieceSlug="01-the-linked-list"
    lang="ru"
    question="Почему связный список достигает O(1) вставки в начало, пока массив требует O(n) для вставки в начало?"
    choices={[
      {
        label: "Связный список создаёт новый узел и обновляет один указатель (head), избегая O(n) стоимости сдвига всех элементов. Массив должен сдвинуть каждый элемент, чтобы освободить место.",
        correct: true
      },
      {
        label: "Связные списки всегда быстрее массивов.",
        misconception: "Связные списки быстрее для конкретных операций (как вставка в начало), но медленнее для других (как индексирование). У обоих есть трейд-оффы."
      },
      {
        label: "Вставка в начало в массиве это O(n), потому что массивы не могут вставлять; они могут только заменять. Связные списки были разработаны для вставки.",
        misconception: "Массивы могут вставлять, но стоимость O(n), потому что ты должен сдвинуть элементы. Связные списки избегают этого, изменяя указатели."
      },
      {
        label: "Связные списки не вставляют на самом деле; они добавляют, так что никакой стоимости нет.",
        misconception: "Вставка в начало связного списка создаёт новый узел и обновляет указатель head. Это вставка в начало, и она имеет ясную O(1) стоимость."
      }
    ]}
  />
</Check>

<Recap lang="ru">
**Связный список** — это последовательность **узлов**, каждый держит значение и указатель на следующий узел.

Ключевые факты:
1. **Узел:** Контейнер с двумя полями — значение и next (указатель на следующий узел).
2. **Head:** Первый узел. Обход начинается отсюда.
3. **Указатель:** Адрес памяти, указывающий на следующий узел.
4. **Разреженная память:** Узлы не смежны. Указатели связывают их вместе.
5. **Индексирование это O(n):** Нет формулы для прыжка на позицию i. Должен проследить указатели один за другим.
6. **Push это O(1):** Создай новый узел, укажи его на старый head, обнови head. Без сдвигов.
7. **Трейд-офф:** Теряешь O(1) индексирование; получаешь O(1) вставку в начало.
8. **Используй когда:** Частые вставки в начало, редкий случайный доступ.

Далее ты изучишь **стек** и **очередь** — две другие последовательные структуры данных, построенные на связных списках, каждая со своей дисциплиной и вариантами использования.
</Recap>
