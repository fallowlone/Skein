---
slug: 04-the-stack
lang: ru
track: algorithms
unit: 06-lists-stacks-queues
order: 4
title: "Стек"
summary: "Стек это LIFO контейнер, где ты push-ишь элементы сверху и pop-аешь их сверху. Обе операции выполняются за O(1) постоянное время. Стеки моделируют историю undo/redo, парсинг выражений и любую ситуацию где последнее добавленное должно быть первым удалённым."
estMin: 22
status: ready
prereqs: ["01-the-linked-list", "02-pointer-manipulation"]
mathPrereqs: []
concepts: ["stack", "LIFO", "push", "pop", "peek", "balanced_brackets", "expression_validation"]
sources:
  - https://en.wikipedia.org/wiki/Stack_(abstract_data_type)
  - https://www.geeksforgeeks.org/dsa/introduction-to-stack-data-structure-and-algorithm-tutorials/
  - https://runestone.academy/ns/books/published/pythonds/BasicDS/SimpleBalancedParentheses.html
  - https://www.geeksforgeeks.org/dsa/time-and-space-complexity-analysis-of-stack-operations/
---

import Hook from "../../../../../../components/lesson/Hook.astro";
import Goal from "../../../../../../components/lesson/Goal.astro";
import Idea from "../../../../../../components/lesson/Idea.astro";
import Code from "../../../../../../components/lesson/Code.astro";
import Trace from "../../../../../../components/lesson/Trace.astro";
import Complexity from "../../../../../../components/lesson/Complexity.astro";
import Check from "../../../../../../components/lesson/Check.astro";
import Recap from "../../../../../../components/lesson/Recap.astro";
import Inset from "../../../../../../components/lesson/Inset.astro";
import Term from "../../../../../../components/prose/Term.astro";
import AnnotatedCode from "../../../../../../components/algo/AnnotatedCode.astro";
import AlgoTrace from "../../../../../../components/algo/AlgoTrace.astro";
import CodeRunner from "../../../../../../components/algo/CodeRunner.astro";
import AlgoPractice from "../../../../../../components/algo/AlgoPractice.astro";
import Quiz from "../../../../../../components/pedagogy/Quiz.astro";

<Hook>
В прошлом уроке ты выучил методику двух указателей на связном списке. Сегодня ты выучишь про <Term slug="stack">стек</Term> — простую и мощную структуру данных. Стек это как стопка тарелок: ты добавляешь тарелки сверху и удаляешь тарелки сверху. Последняя добавленная тарелка это первая что снимается. В программировании, ты используешь стеки для истории undo/redo (каждое действие push-ит новое состояние на стек), парсинга выражений (сопоставление скобок и операторов) и везде где самое свежее это самое важное. Стек налагает одно правило: <Term slug="lifo">LIFO</Term> (последний-вошедший-первый-вышедший).
</Hook>

<Goal lang="ru">
После этого урока ты можешь объяснить дисциплину LIFO и три основные операции стека (push, pop, peek), реализовать стек используя массив или связный список, объяснить почему все операции стека выполняются за O(1) время, решать задачи сопоставления скобок и валидации выражений используя стек, и узнать когда стек это правильная структура данных для задачи.
</Goal>

<Idea lang="ru">
**Правило LIFO:**

<Term slug="stack">Стек</Term> это <Term slug="linear_data_structure">линейная структура данных</Term> где все вставки и удаления происходят на одном конце, называемом <Term slug="top">верхом</Term>. Правило это LIFO (последний вошедший, первый вышедший): элемент добавленный совсем недавно это тот что удаляется первым.

Представь стопку обеденных тарелок в шкафу. Когда ты хватаешь тарелку, ты берёшь сверху (последнюю положенную там). Когда ты кладёшь тарелку назад, она идёт сверху. Ты никогда не хватаешь из середины или снизу. Это LIFO.

**Три операции:**

1. **Push:** Добавь элемент на верх стека. Возвращает ничего; обновляет стек на месте.
2. **Pop:** Удали и верни верхний элемент стека. Если стек пуст, или верни null или подними ошибку (зависит от реализации).
3. **Peek (или top):** Верни верхний элемент без его удаления. Позволяет тебе увидеть что там перед тем как ты обязуешься pop-нуть.

```js
const stack = [];  // Пустой стек

// Push: добавь 10, 20, 30
stack.push(10);    // Стек: [10]
stack.push(20);    // Стек: [10, 20]
stack.push(30);    // Стек: [10, 20, 30]

// Peek: увидь верх
console.log(stack[stack.length - 1]);  // 30 (без удаления)

// Pop: удали верх
const top = stack.pop();  // Возвращает 30. Стек: [10, 20]
```

**Реализация с массивом:**

Массив естественно моделирует стек. Конец массива это верх. Push добавляет в конец; pop удаляет из конца. Оба выполняются за O(1) <Term slug="amortized_time">амортизированное</Term> время (в JavaScript язык скрывает resize массива).

```js
class Stack {
  constructor() {
    this.items = [];
  }
  
  push(value) {
    this.items.push(value);
  }
  
  pop() {
    if (this.items.length === 0) return null;
    return this.items.pop();
  }
  
  peek() {
    if (this.items.length === 0) return null;
    return this.items[this.items.length - 1];
  }
  
  isEmpty() {
    return this.items.length === 0;
  }
}
```

**Реализация со связным списком:**

Стек может быть также подкреплён связным списком. Push и pop оба работают на голове (нет traverse нужно). Это также O(1).

```js
class Node {
  constructor(value, next = null) {
    this.value = value;
    this.next = next;
  }
}

class StackLinkedList {
  constructor() {
    this.top = null;  // Head pointer
  }
  
  push(value) {
    const newNode = new Node(value, this.top);
    this.top = newNode;
  }
  
  pop() {
    if (this.top === null) return null;
    const value = this.top.value;
    this.top = this.top.next;
    return value;
  }
  
  peek() {
    if (this.top === null) return null;
    return this.top.value;
  }
  
  isEmpty() {
    return this.top === null;
  }
}
```

**Подробный пример: сбалансированные скобки**

Одно из самых частых использований стеков это проверка сбалансированы ли скобки, круглые скобки и фигурные скобки в выражении.

**Задача:** Дана строка со знаками `(`, `)`, `[`, `]`, `{`, `}`, проверь сбалансирована ли каждая открывающая скобка с соответствующей закрывающей скобкой в правильном порядке.

Пример: `"({[]})"` сбалансирована. `"({[}])"` не сбалансирована (закрывающая `}` закрывает неправильную скобку).

**Алгоритм:** Обработай строку слева направо.
1. Если ты видишь открывающую скобку (`(`, `[`, `{`), push её на стек.
2. Если ты видишь закрывающую скобку (`)`, `]`, `}`), pop стек и проверь совпадает ли. Если не совпадает, возврати false. Если стек пуст, возврати false (нет открывающей скобки для сопоставления).
3. В конце, стек должен быть пуст. Если нет, есть незакрытые открывающие скобки.

Пример trace: `"{[]}"`
- Видишь `{`: push `{`. Стек: `[{]`
- Видишь `[`: push `[`. Стек: `[{, []`
- Видишь `]`: pop `[`. Совпадает ли `[` с `]`? Да. Стек: `[{]`
- Видишь `}`: pop `{`. Совпадает ли `{` с `}`? Да. Стек: `[]`
- Конец: стек пуст. Сбалансирована!

Пример trace: `"{[}]"`
- Видишь `{`: push `{`. Стек: `[{]`
- Видишь `[`: push `[`. Стек: `[{, []`
- Видишь `}`: pop `[`. Совпадает ли `[` с `}`? Нет. Возврати false. Не сбалансирована.

</Idea>

<Code lang="ru">
**Стек подкреплённый массивом:**

```js
class Stack {
  constructor() {
    this.items = [];
  }
  
  push(value) {
    this.items.push(value);
  }
  
  pop() {
    if (this.items.length === 0) return null;
    return this.items.pop();
  }
  
  peek() {
    if (this.items.length === 0) return null;
    return this.items[this.items.length - 1];
  }
  
  isEmpty() {
    return this.items.length === 0;
  }
  
  size() {
    return this.items.length;
  }
}
```

**Проверка сбалансированных скобок:**

```js
function isBalanced(s) {
  const stack = new Stack();
  const pairs = { ')': '(', ']': '[', '}': '{' };
  
  for (let char of s) {
    if (char === '(' || char === '[' || char === '{') {
      stack.push(char);
    } else if (char === ')' || char === ']' || char === '}') {
      const top = stack.pop();
      if (top !== pairs[char]) {
        return false;
      }
    }
  }
  
  return stack.isEmpty();
}
```

Попробуй обе в интерактивном runner:

<CodeRunner
  id="cr-stack"
  lang="ru"
  code={`class Stack {
  constructor() {
    this.items = [];
  }
  
  push(value) {
    this.items.push(value);
  }
  
  pop() {
    if (this.items.length === 0) return null;
    return this.items.pop();
  }
  
  peek() {
    if (this.items.length === 0) return null;
    return this.items[this.items.length - 1];
  }
  
  isEmpty() {
    return this.items.length === 0;
  }
}

function isBalanced(s) {
  const stack = new Stack();
  const pairs = { ')': '(', ']': '[', '}': '{' };
  
  for (let char of s) {
    if (char === '(' || char === '[' || char === '{') {
      stack.push(char);
    } else if (char === ')' || char === ']' || char === '}') {
      const top = stack.pop();
      if (top !== pairs[char]) {
        return false;
      }
    }
  }
  
  return stack.isEmpty();
}

// Тест 1: Сбалансирована
console.log("Тест '({[]}):', " + isBalanced("({[]})"));

// Тест 2: Не сбалансирована (неправильная закрывающая скобка)
console.log("Тест '({[}]):', " + isBalanced("({[}])"));

// Тест 3: Не сбалансирована (незакрытая)
console.log("Тест '({[):', " + isBalanced("({[)"));

// Тест 4: Не сбалансирована (лишняя закрывающая)
console.log("Тест '())', " + isBalanced("())"));

// Тест 5: Пустая
console.log("Тест '':", " + isBalanced(""));`}
/>
</Code>

<Trace lang="ru">
Давай проследим проверку сбалансирована ли `"({[]})"`.

<AlgoTrace
  id="trace-balanced"
  lang="ru"
  code={[
    "for (let char of s) {",
    "  if (char === '(' || char === '[' || char === '{') {",
    "    stack.push(char);",
    "  } else if (char === ')' || char === ']' || char === '}') {",
    "    const top = stack.pop();",
    "    if (top !== pairs[char]) return false;",
    "  }",
    "}",
    "return stack.isEmpty();"
  ]}
  frames={[
    {
      caption: "Начало: пустой стек. Обработай символ '('",
      line: 2,
      cells: [
        { value: "(", mark: "target" },
        { value: "{", mark: "value" },
        { value: "[", mark: "value" },
        { value: "]", mark: "value" },
        { value: "}", mark: "value" },
        { value: ")", mark: "value" }
      ],
      stackState: "[]"
    },
    {
      caption: "Символ '(' это открывающий. Push его. Стек: ['(']",
      line: 3,
      cells: [
        { value: "(", mark: "done" },
        { value: "{", mark: "target" },
        { value: "[", mark: "value" },
        { value: "]", mark: "value" },
        { value: "}", mark: "value" },
        { value: ")", mark: "value" }
      ],
      stackState: "['(']"
    },
    {
      caption: "Символ '{' это открывающий. Push его. Стек: ['(', '{']",
      line: 3,
      cells: [
        { value: "(", mark: "done" },
        { value: "{", mark: "done" },
        { value: "[", mark: "target" },
        { value: "]", mark: "value" },
        { value: "}", mark: "value" },
        { value: ")", mark: "value" }
      ],
      stackState: "['(', '{']"
    },
    {
      caption: "Символ '[' это открывающий. Push его. Стек: ['(', '{', '[']",
      line: 3,
      cells: [
        { value: "(", mark: "done" },
        { value: "{", mark: "done" },
        { value: "[", mark: "done" },
        { value: "]", mark: "target" },
        { value: "}", mark: "value" },
        { value: ")", mark: "value" }
      ],
      stackState: "['(', '{', '[']"
    },
    {
      caption: "Символ ']' это закрывающий. Pop '['. Совпадает ли '[' с ']'? Да. Стек: ['(', '{']",
      line: 6,
      cells: [
        { value: "(", mark: "done" },
        { value: "{", mark: "done" },
        { value: "[", mark: "done" },
        { value: "]", mark: "done" },
        { value: "}", mark: "target" },
        { value: ")", mark: "value" }
      ],
      stackState: "['(', '{']"
    },
    {
      caption: "Символ '}' это закрывающий. Pop '{'. Совпадает ли '{' с '}'? Да. Стек: ['(']",
      line: 6,
      cells: [
        { value: "(", mark: "done" },
        { value: "{", mark: "done" },
        { value: "[", mark: "done" },
        { value: "]", mark: "done" },
        { value: "}", mark: "done" },
        { value: ")", mark: "target" }
      ],
      stackState: "['(']"
    },
    {
      caption: "Символ ')' это закрывающий. Pop '('. Совпадает ли '(' с ')'? Да. Стек: []",
      line: 6,
      cells: [
        { value: "(", mark: "done" },
        { value: "{", mark: "done" },
        { value: "[", mark: "done" },
        { value: "]", mark: "done" },
        { value: "}", mark: "done" },
        { value: ")", mark: "done" }
      ],
      stackState: "[]"
    },
    {
      caption: "Конец строки. Стек пуст. Возврати true. Сбалансирована!",
      line: 9,
      cells: [
        { value: "(", mark: "done" },
        { value: "{", mark: "done" },
        { value: "[", mark: "done" },
        { value: "]", mark: "done" },
        { value: "}", mark: "done" },
        { value: ")", mark: "done" }
      ],
      stackState: "[]"
    }
  ]}
/>

</Trace>

<Complexity lang="ru">
**Операции стека:**

| Операция | Время | Пространство |
|---|---|---|
| Push | O(1) | O(1) |
| Pop | O(1) | O(1) |
| Peek | O(1) | O(1) |
| isEmpty | O(1) | O(1) |

Все операции выполняются за O(1) постоянное время потому что они только трогают верх стека. Нет <Term slug="traversal">traverse</Term> нужно; нет цикла что итерирует над структурой.

**Реализация массивом vs связным списком:**

Обе имеют O(1) push и pop. Разница в раскладке памяти:
- **Массив:** Cache-дружественный, но resize (когда массив растёт) это O(n) и редкий.
- **Связный список:** Нет resize, но каждый <Term slug="node">узел</Term> стоит дополнительную память для <Term slug="pointer">указателя</Term>.

Для большинства задач, массив проще. Используй связный список если память scarce или если resize неприемлемый.

**Проверка сбалансированных скобок:**

- **Время:** O(n), где n это длина строки. Каждый символ обработан один раз.
- **Пространство:** O(n) в худшем случае (все открывающие скобки), но обычно намного меньше.
</Complexity>

<AlgoPractice
  id="practice-stack"
  lessonSlug="04-the-stack"
  lang="ru"
  problems={[
    {
      kind: "choice",
      prompt: "После push 10, 20, и 30 на стек (в том порядке), что возвращает pop?",
      choices: [
        "10",
        "20",
        "30",
        "null"
      ],
      answer: 2,
      hint: "Стек это LIFO. Последний элемент push-ленный (30) это первый что приходит."
    },
    {
      kind: "choice",
      prompt: "Какая операция позволяет тебе проверить что на верху стека без его удаления?",
      choices: [
        "pop",
        "peek (или top)",
        "push",
        "isEmpty"
      ],
      answer: 1,
      hint: "Peek читает верхний элемент без pop. Это O(1) и не модифицирует стек."
    },
    {
      kind: "choice",
      prompt: "Сбалансирована ли строка '([)]'?",
      choices: [
        "Да, потому что она имеет тот же число открывающих и закрывающих скобок",
        "Нет, потому что ')' закрывает перед ']', но '[' пришёл после '('",
        "Да, потому что скобки правильно вложены",
        "Невозможно определить без дополнительной информации"
      ],
      answer: 1,
      hint: "Скобки должны закрываться в обратном порядке что открыты (LIFO). ')' закрывает '(' перед ']', что нарушает порядок вложения."
    },
    {
      kind: "choice",
      prompt: "Для алгоритма проверки сбалансированных скобок, что это временная сложность?",
      choices: [
        "O(1)",
        "O(n), где n это длина строки",
        "O(n²)",
        "O(log n)"
      ],
      answer: 1,
      hint: "Каждый символ в строке обработан один раз (одна итерация). Каждая операция (push или pop) это O(1), так что итого это O(n)."
    },
    {
      kind: "choice",
      prompt: "Какая реализация стека менее память-эффективна потому что каждый узел хранит указатель?",
      choices: [
        "Стек с массивом",
        "Стек со связным списком",
        "Обе равно эффективны",
        "Ни одна — стеки не хранят указатели"
      ],
      answer: 1,
      hint: "Узлы связного списка хранят и значение и указатель на следующий узел. Массивы не нужны дополнительные указатели на элемент."
    }
  ]}
/>

<Inset kind="edgecase" lang="ru">
**Операции на пустом стеке:** Когда стек пуст и ты пытаешься pop или peek, что должно произойти? Разные языки отличаются: некоторые возвращают null, некоторые подымают ошибку. В JavaScript, `Array.pop()` возвращает `undefined`. Когда реализуешь стек, ты должен решить заранее: возврати null/undefined для пустого pop, или подними ошибку? Для задачи сопоставления скобок, мы возвращаем null и проверяем это перед используя значение.
</Inset>

<Inset kind="mistake" lang="ru">
**«Стек это просто массив.»** Нет. Стек это абстрактная дисциплина (LIFO с только верхним доступом), не структура данных. Массив это структура данных (случайный доступ, индексирование). Ты можешь реализовать стек используя массив, но стек скрывает способность массива доступить середину или дно. Не думай что они одно и то же — стек налагает правила.
</Inset>

<Inset kind="practice" lang="ru">
**Последующее:** Реально мировое использование стеков это undo/redo в text editors. Каждое действие push состояние на undo стек. Когда ты нажимаешь Ctrl+Z, pop из undo стека и восстанови предыдущее состояние, пока push текущее состояние на redo стек. Это красивое использование LIFO.
</Inset>

<Check lang="ru">
  <Quiz
    id="quiz-stack"
    pieceSlug="04-the-stack"
    lang="ru"
    question="Почему стек это идеальная структура данных для проверки сбалансированных скобок вместо, скажи, очередь или массив?"
    choices={[
      {
        label: "Стек налагает LIFO: самый недавно открытая скобка должна быть первая что закрывается, что совпадает требованию вложения.",
        correct: true
      },
      {
        label: "Очередь медленнее чем стек потому что она имеет O(n) операции.",
        misconception: "Очереди также имеют O(1) операции. Причина что стеки лучше это LIFO семантика, не скорость."
      },
      {
        label: "Массив не может хранить скобки, так мы должны использовать стек.",
        misconception: "Ты можешь хранить скобки в массиве. Но используя массив прямо потребовал бы проверка внутри его (O(n)), в то время как стек позволяет тебе только смотреть на верх (O(1))."
      },
      {
        label: "Стеки это только структура данных что может быть реализована с массивами.",
        misconception: "Много структур данных могут быть реализованы с массивами: очереди, кучи, итд. Стеки выбраны для сопоставления скобок потому что LIFO, не потому что реализация массива."
      }
    ]}
  />
</Check>

<Recap lang="ru">
**Стек: LIFO и три операции**

1. **LIFO правило:** Последний элемент push-ленный это первый что pop-нут. Думай о стопке тарелок.

2. **Три операции:** push (добавь на верх, O(1)), pop (удали с верха, O(1)), peek (видь верх без удаления, O(1)). Все постоянное время.

3. **Две реализации:** Массив (проще, cache-дружественный, редкий O(n) resize) или связный список (нет resize, дополнительный указатель на узел).

4. **Проверка сбалансированных скобок:** Обработай слева направо. Push открывающие скобки, pop и сопоставь закрывающие скобки. Если стек пуст в конце, все скобки сбалансированы. O(n) время, O(n) пространство.

5. **Реально мировые использования:** Undo/redo, парсинг выражений, стек вызовов функций, traverse поиск в глубину.

6. **Ключевое понимание:** Стек это абстрактная дисциплина, не структура данных. Ты можешь реализовать его с массивом или связным списком — оба дают O(1) операции.

Далее, ты выучишь **очереди** — противоположность стеков. Очередь налагает FIFO (первый вошедший, первый вышедший). Ты enqueue на спине и dequeue на передней. Очереди моделируют очередь ожидания, поиск в ширину, и любую ситуацию где справедливость (первый пришедший, первый обслужен) имеет значение.
</Recap>
