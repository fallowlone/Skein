---
concepts:
- interval-scheduling
- sort-key
- greedy-vs-dp
- jump-game
- gas-station
estMin: 14
lang: ru
level: senior
order: 9
prereqs: []
slug: quiz-code
sources:
- https://leetcode.com/problems/jump-game/
- https://leetcode.com/problems/gas-station/
status: ready
summary: "Чтение реальных greedy-фрагментов: interval-планировщик с неверным ключом сортировки, greedy, проигрывающий DP, и проход Gas Station. Предскажите поведение и выберите фикс с наибольшим рычагом."
title: 'Greedy: чтение кода'
track: algorithms
unit: 11-greedy
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Баги greedy тихие: код выполняется, возвращает что-то правдоподобное и ошибается только на тех входах, которые вы не протестировали. Читайте каждый фрагмент как на ревью — найдите выбор, который не является доказуемо безопасным, и выберите фикс, который senior-инженер делает первым.
</Hook>

<Goal lang="ru">
Отработайте цикл, который вы запускаете на каждом greedy: прочитать локальный выбор, решить, доказуемо ли он оптимален, и потянуться к правильному ключу сортировки, правильному инварианту или к DP — прежде чем доверять результату.
</Goal>

## Фрагмент 1 — interval-планировщик

```js
// Цель: выбрать МАКСИМУМ непересекающихся интервалов.
function maxNonOverlapping(intervals) {
  const sorted = [...intervals].sort((a, b) => a[0] - b[0]); // сортировка по СТАРТУ
  let count = 0, lastEnd = -Infinity;
  for (const [start, end] of sorted) {
    if (start >= lastEnd) { count++; lastEnd = end; }
  }
  return count;
}
// maxNonOverlapping([[1, 10], [2, 3], [4, 5]])  ->  ?
```

<Quiz
  id="11-greedy-code-1"
  lessonSlug="11-greedy"
  lang="ru"
  question="Что вернётся на [[1,10], [2,3], [4,5]] и каков фикс в одну строку?"
  choices={[
    { label: "Вернётся 1. При сортировке по старту [1,10] берётся первым, и его финиш (10) блокирует и [2,3], и [4,5], хотя те двое совместимы (оптимум равен 2). Фикс — сортировать по ВРЕМЕНИ ФИНИША: sort((a, b) => a[1] - b[1]).", correct: true },
    { label: "Вернётся 2, что оптимально — сортировка по старту корректна для этой задачи.", misconception: "Сортировка по старту берёт длинный [1,10] первым и затем блокируется от обоих коротких интервалов, возвращая 1, а не 2. Сортировка по старту — это ключ для MERGING интервалов, а не для максимизации." },
    { label: "Вернётся 3, потому что в итоге засчитываются все три интервала.", misconception: "[1,10] пересекается и с [2,3], и с [4,5], поэтому их нельзя выбрать все. С сортировкой по старту берётся только [1,10], возвращая 1." },
    { label: "Счёт верен, но это O(n^2); фикс — убрать сортировку.", misconception: "Удаление сортировки делает его неверным, а не быстрее — greedy зависит от порядка. Дефект в КЛЮЧЕ (старт против финиша), а алгоритм уже O(n log n)." },
  ]}
/>

## Фрагмент 2 — greedy, проигрывающий DP

```js
// «Минимум монет для amount», всегда беря самую крупную подходящую монету.
function greedyCoins(coins, amount) {
  const sorted = [...coins].sort((a, b) => b - a);
  let left = amount, count = 0;
  for (const c of sorted) {
    while (left >= c) { left -= c; count++; }
  }
  return left === 0 ? count : Infinity;
}
// greedyCoins([1, 5, 6, 9], 11)  ->  ?   (DP-оптимум равен 2: 5 + 6)
```

<Quiz
  id="11-greedy-code-2"
  lessonSlug="11-greedy"
  lang="ru"
  question="greedyCoins([1,5,6,9], 11) возвращает 3 (9+1+1), но DP-оптимум равен 2 (5+6). Какое утверждение корректно диагностирует это?"
  choices={[
    { label: "Набор номиналов {1,5,6,9} лишён greedy choice property: взятие самой крупной монеты (9) не входит ни в одно оптимальное решение для 11, и никакой exchange это не чинит. Корректный алгоритм здесь — DP за O(amount x coins), оптимальный для любых номиналов.", correct: true },
    { label: "Это баг сортировки; сортировка монет по возрастанию вместо убывания чинит его.", misconception: "Возрастающий порядок заставил бы greedy брать множество единиц — ещё хуже. Провал внутренне присущ набору номиналов, а не направлению сортировки. Фикс — DP." },
    { label: "Greedy прав, а DP ошибается; 9+1+1 = 3 монеты действительно оптимальны для 11.", misconception: "5 + 6 = 11 использует ровно 2 монеты, обыгрывая greedy с тремя. DP минимизирует исчерпывающе и корректен; greedy возвращает худший ответ." },
    { label: "Баг в проверке left === 0; её удаление делает greedy оптимальным.", misconception: "Проверка лишь сообщает о невыполнимости; к оптимальности она отношения не имеет. Даже с чистой проверкой greedy всё равно возвращает 3 для этого набора номиналов. Только DP гарантирует минимум." },
  ]}
/>

## Фрагмент 3 — проход Gas Station

```js
function canCompleteCircuit(gas, cost) {
  let total = 0, tank = 0, start = 0;
  for (let i = 0; i < gas.length; i++) {
    const diff = gas[i] - cost[i];
    total += diff;
    tank += diff;
    if (tank < 0) { start = i + 1; tank = 0; }
  }
  return total >= 0 ? start : -1;
}
```

<Quiz
  id="11-greedy-code-3"
  lessonSlug="11-greedy"
  lang="ru"
  question="Ревьюер хочет удалить переменную total и возвращать start напрямую, аргументируя, что сброс и так находит ответ. Почему total должен остаться?"
  choices={[
    { label: "total — это тест выполнимости: валидный маршрут существует тогда и только тогда, когда net fuel по всему кругу >= 0. Без него функция возвращает индекс start даже когда маршрут невозможен (total < 0), где корректный ответ -1. Локальный сброс находит, ГДЕ стартовать; глобальная сумма решает, РАБОТАЕТ ли хоть какой-то старт.", correct: true },
    { label: "total — просто отладочный счётчик; функция корректна и без него.", misconception: "Уберите total — и функция может вернуть фиктивный старт на невыполнимых входах (например, при отрицательном суммарном net fuel), где она обязана вернуть -1. total — несущая проверка выполнимости, а не отладочный вывод." },
    { label: "total нужен для сброса бака; без него tank никогда не обнуляется.", misconception: "Бак сбрасывается строкой tank = 0 внутри if, независимо от total. total не участвует в сбросах — его единственная задача в финальном вердикте о выполнимости." },
    { label: "total должен заменить tank; это один и тот же аккумулятор.", misconception: "Они разные: tank — net fuel с текущего кандидата-старта (решает о сбросах), total — net fuel по всему кругу (решает о выполнимости). Слияние ломает обе роли." },
  ]}
/>

## Фрагмент 4 — граница цикла Jump Game II

```js
function jump(nums) {
  let jumps = 0, curEnd = 0, farthest = 0;
  for (let i = 0; i < nums.length; i++) {   // цикл до n-1
    farthest = Math.max(farthest, i + nums[i]);
    if (i === curEnd) { jumps++; curEnd = farthest; }
  }
  return jumps;
}
// jump([2, 3, 1, 1, 4])  ->  ?   (истинный минимум равен 2)
```

<Quiz
  id="11-greedy-code-4"
  lessonSlug="11-greedy"
  lang="ru"
  question="Это возвращает 3 на [2,3,1,1,4], но минимум равен 2. В чём ошибка на единицу и каков фикс?"
  choices={[
    { label: "Цикл идёт до n-1, поэтому когда i достигает индекса цели (который в конце равен curEnd), тратится один лишний прыжок, чтобы покинуть клетку, которую он никогда не покидает. Фикс — цикл до n-2 (i < nums.length - 1): вы ПРИБЫВАЕТЕ в последний индекс, но никогда не прыгаете ИЗ него.", correct: true },
    { label: "Баг в том, что jumps стартует с 0; должен стартовать с 1.", misconception: "Старт с 1 пересчитал бы с первого прыжка и сломал случай [0] длины 1 (которому нужно 0 прыжков). Настоящий баг — граница цикла, она должна останавливаться на n-2." },
    { label: "Math.max должен быть Math.min; фронтир должен сжиматься.", misconception: "Фронтир (самый дальний достижимый) только растёт — Math.max корректен. Min схлопнул бы его и отрапортовал недостижимость. Дефект в границе цикла." },
    { label: "Бага нет; 3 прыжка корректны для [2,3,1,1,4].", misconception: "0 -> 1 -> 4 достигает конца за 2 прыжка. Цикл до n-1 тратит лишний третий прыжок на сам индекс цели; остановка на n-2 это чинит." },
  ]}
/>

<Recap lang="ru">
Каждый greedy читается одинаково: назовите локальный выбор, затем спросите, доказуемо ли он безопасен. Фрагмент 1 использовал неверный ключ сортировки (старт вместо финиша) для максимизации; фрагмент 2 применил greedy на наборе номиналов без greedy choice property, где корректен только DP; фрагмент 3 показал, что глобальный инвариант выполнимости (total >= 0) несущий и отделён от локального сброса; фрагмент 4 был классической ошибкой на единицу в Jump Game II (цикл до n-2, никогда не прыгать из цели). Диагностируйте небезопасный выбор, затем чините ключ, инвариант или откатывайтесь к DP.
</Recap>
