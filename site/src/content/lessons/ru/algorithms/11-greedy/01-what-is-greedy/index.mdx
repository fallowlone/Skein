---
concepts:
- greedy_algorithm
- local_choice
- greedy_choice_property
- optimal_substructure
- coin_change
- greedy_vs_dp
- canonical_coin_system
- counterexample
- never_reconsider
- n_log_n
estMin: 24
lang: ru
mathPrereqs: []
order: 1
prereqs:
- 10-dynamic-programming/01-what-is-dp
slug: 01-what-is-greedy
sources:
- https://en.wikipedia.org/wiki/Greedy_algorithm
- https://www.geeksforgeeks.org/dsa/greedy-algorithms/
- https://cp-algorithms.com/dynamic_programming/intro-to-dp.html
status: ready
summary: 'Жадный алгоритм делает локально лучший выбор и никогда не пересматривает
  его. Быстрый (обычно O(n log n)), но корректен только при жадном выборе плюс оптимальной
  подструктуре. Размен монетами {1,3,4} для 6 показывает провал: 4+1+1 = 3 монеты
  против оптимума 3+3 = 2.'
title: Что это жадный алгоритм? Локальный выбор против DP
track: algorithms
unit: 11-greedy
---



import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Idea from "~/components/lesson/Idea.astro";
import Code from "~/components/lesson/Code.astro";
import Trace from "~/components/lesson/Trace.astro";
import Complexity from "~/components/lesson/Complexity.astro";
import Check from "~/components/lesson/Check.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import AnnotatedCode from "~/components/algo/AnnotatedCode.astro";
import AlgoTrace from "~/components/algo/AlgoTrace.astro";
import CodeRunner from "~/components/algo/CodeRunner.astro";
import AlgoPractice from "~/components/algo/AlgoPractice.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";
import Term from "~/components/prose/Term.astro";
import DiagramFrame from "~/components/diagram/DiagramFrame.astro";

<Hook>
Тебе нужно набрать 30 центов сдачи монетами США. Почти не думая ты тянешься за четвертаком, потом за пятаком — готово, две монеты. Ты не рисовал дерево решений и не перебирал все комбинации. Ты схватил самую большую монету которая поместилась, потом следующую самую большую, и ни разу не оглянулся. У этого инстинкта есть имя: <Term slug="greedy_algorithm">жадный алгоритм</Term>. Это простейший, самый быстрый способ построить решение — и иногда он ровно неверен. В прошлом разделе динамическое программирование тщательно исследовало *каждую* комбинацию выборов подзадач. Жадный отказывается это делать. Он фиксирует один локальный выбор на каждом шаге и идёт дальше. Весь навык в том чтобы знать когда этот срез пути безопасен.
</Hook>

<Goal lang="ru">
После этого урока ты можешь определить жадный алгоритм: он строит решение, повторно делая локально лучший выбор и никогда его не пересматривая. Ты можешь назвать два условия которые делают жадный *корректным* — свойство жадного выбора (глобально оптимальное решение всегда может начинаться с локально лучшего выбора) и оптимальную подструктуру (оптимальное решение содержит оптимальные решения своих подзадач). Ты можешь сравнить жадный с динамическим программированием: DP исследует все комбинации выборов подзадач и хранит лучшую, тогда как жадный фиксирует один выбор и никогда не откатывается — так что жадный быстрее, но корректен лишь иногда. Ты можешь привести пример где жадный работает (размен в канонической системе США {1,5,10,25}) и конкретный контрпример где он проваливается ({1,3,4} для суммы 6: жадный даёт 4+1+1 = 3 монеты, но оптимум это 3+3 = 2 монеты). Ты понимаешь почему это значит что жадный алгоритм должен быть *доказан* корректным, а не просто предположен — это тема следующего урока. И ты можешь назвать типичную стоимость: когда жадный корректен, он обычно O(n log n), доминирует начальная сортировка.
</Goal>

<Idea lang="ru">
**Что это жадный алгоритм?**

<Term slug="greedy_algorithm">Жадный алгоритм</Term> строит решение по одной части за раз. На каждом шаге он делает <Term slug="local_choice">выбор который выглядит лучшим прямо сейчас</Term> — локально оптимальный ход — и потом **никогда не пересматривает** этот выбор. Никакого отката, никаких попыток не пройденной дороги. Он шагает прямо от начала к концу, хватая лучше всего выглядящий вариант на каждой развилке.

Это вся идея, и именно поэтому жадный быстр и прост. Нет дерева рекурсии чтобы исследовать, нет таблицы подзадач чтобы заполнить. Ты сортируешь или сканируешь вход, делаешь лучший выбор, повторяешь.

**Жадный против динамического программирования**

Динамическое программирование из прошлого раздела и жадный атакуют один вид задачи — построение оптимального решения из выборов подзадач — но в противоположном духе:

- **Динамическое программирование** исследует *все* комбинации выборов подзадач, вычисляет лучшую каждой, и хранит общую лучшую. Оно подстраховывается: оно не фиксирует выбор пока не сравнит альтернативы.
- **Жадный** фиксирует *один* выбор на каждом шаге — локально лучший — и никогда не сравнивает его с альтернативами потом. Он ставит на то что локально лучший выбор это также часть глобально лучшего ответа.

Эта ставка и есть подвох. Когда ставка всегда срабатывает, жадный получает оптимальный ответ намного быстрее чем DP. Когда ставка может провалиться, жадный возвращает неверный (субоптимальный) ответ, тогда как DP всё ещё был бы прав.

**Когда жадный корректен? Два условия**

Жадный алгоритм даёт оптимальный ответ только когда задача обладает обоими этими свойствами:

1. **Свойство жадного выбора.** Всегда существует глобально оптимальное решение которое *начинается с* локально лучшего выбора. Иначе говоря, жадный выбор никогда не загоняет тебя в угол — ты всегда можешь расширить его до оптимума.
2. **Оптимальная подструктура.** После того как ты сделал этот первый выбор, остаток это уменьшенная версия той же задачи, и оптимальное решение целого содержит оптимальное решение этого остатка. (Динамическому программированию это свойство тоже нужно.)

Если любого из свойств нет, жадный может вернуть субоптимальный ответ. Опасность в том что жадный код *всегда работает и всегда что-то возвращает* — он просто тихо возвращает неверное. Так что ты должен **доказать** что жадный корректен для твоей задачи; ты не можешь это предполагать. Это доказательство — следующий урок.

**Разобранный контрпример: размен монет**

Чище всего ощутить разницу на размене наименьшим числом монет. Диаграмма ниже трассирует этот путь решений: на каждом шаге жадный фиксирует самую большую монету которая помещается и никогда не оглядывается, тогда как более короткий оптимальный путь лежит в одном выборе в стороне, недостигнутый.

С канонической системой США **{1, 5, 10, 25}** жадный работает. Чтобы набрать 30, бери самую большую монету которая помещается (25), потом повтори на оставшихся 5 (бери 5). Две монеты — оптимально.

Теперь переключись на странную систему монет **{1, 3, 4}** и набери **6**. Жадный всё так же хватает самую большую монету которая помещается каждый раз:

```
Coins available: {1, 3, 4}   Target: 6

Greedy:
  take 4  ->  remaining 2   (4 is the largest coin <= 6)
  take 1  ->  remaining 1   (no 3 or 4 fits in 2, take 1)
  take 1  ->  remaining 0
  result: 4 + 1 + 1  = 3 coins

Optimal:
  3 + 3                = 2 coins
```

Жадный взял локально лучшую монету (четвёрку) и застрял: четвёрка заблокировала ему использование двух троек. Оптимум **вообще не использует четвёрку**. Это ровно провал свойства жадного выбора — здесь локально лучший первый ход (взять 4) *не* часть никакого оптимального решения. Система {1, 3, 4} просто не обладает этим свойством, так что жадный неверен на ней. Канонические системы как {1, 5, 10, 25} обладают им, так что жадный прав на них. Тот же алгоритм, другие монеты, противоположные исходы — именно поэтому корректность надо проверять, а не предполагать.
</Idea>

<DiagramFrame
  label="Путь решений для размена 6 монетами {1, 3, 4}, нарисованный как две горизонтальные цепочки состояний. Каждое состояние это блок показывающий остаток; каждая стрелка это один выбор, подписанный взятой монетой. Верхняя цепочка жадная: из остатка 6 она фиксирует самую большую монету которая помещается (взять 4) достигая остатка 2, потом взять 1 до остатка 1, потом взять 1 до остатка 0, заканчивая 3 монетами. Каждая жадная стрелка помечена 'локально лучшее — зафиксировано, без отката'. Нижняя цепочка это оптимальный путь который жадный никогда не исследует: из остатка 6 взять 3 до остатка 3, потом взять 3 до остатка 0, заканчивая 2 монетами. Пунктирная серая стрелка указывает от самого первого состояния жадного вниз на оптимальный первый ход (взять 3), подписанная 'дорога которую жадный отвергает — без пересмотра'. Сноска утверждает что жадный оптимален только когда каждый локально лучший выбор складывается в глобально лучшее решение; для {1,3,4} это не так, поэтому 3 монеты жадного проигрывают оптимальным 2."
  hue="var(--d-systems)"
  caption="Жадный строит слева направо, фиксируя самую большую монету которая помещается на каждом шаге и никогда не пересматривая — взять 4, потом 1, потом 1 = 3 монеты. Оптимальный путь (взять 3, взять 3 = 2 монеты) лежит в одном выборе в стороне, но первая фиксация жадного на четвёрке закрывает эту дорогу навсегда. Локально лучшее выигрывает каждый шаг, но проигрывает в целом, потому что здесь локальные выборы не складываются в глобальный оптимум."
>
  <svg viewBox="0 0 640 380" width="640" height="380">
    <text x="20" y="34" font-size="13" font-family="var(--font-body)" fill="var(--ink)">{"Набрать 6 монетами {1, 3, 4} — каждая стрелка это один выбор"}</text>

    <text x="20" y="74" font-size="12" font-family="var(--font-mono)" fill="var(--accent)">{"ЖАДНЫЙ: бери самую большую монету что помещается, не оглядывайся"}</text>

    <line x1="118" y1="110" x2="186" y2="110" stroke="var(--accent)" stroke-width="1.8" marker-end="url(#ghead)" />
    <line x1="263" y1="110" x2="331" y2="110" stroke="var(--accent)" stroke-width="1.8" marker-end="url(#ghead)" />
    <line x1="408" y1="110" x2="476" y2="110" stroke="var(--accent)" stroke-width="1.8" marker-end="url(#ghead)" />
    <text x="152" y="100" text-anchor="middle" font-size="12" font-family="var(--font-mono)" fill="var(--ink)">{"взять 4"}</text>
    <text x="297" y="100" text-anchor="middle" font-size="12" font-family="var(--font-mono)" fill="var(--ink)">{"взять 1"}</text>
    <text x="442" y="100" text-anchor="middle" font-size="12" font-family="var(--font-mono)" fill="var(--ink)">{"взять 1"}</text>

    <rect x="68" y="92" width="50" height="36" rx="6" fill="var(--card-2)" stroke="var(--d-systems)" stroke-width="1.6" />
    <text x="93" y="115" text-anchor="middle" font-size="13" font-family="var(--font-mono)" fill="var(--ink)">{"6"}</text>
    <rect x="213" y="92" width="50" height="36" rx="6" fill="var(--accent)" stroke="var(--accent)" stroke-width="1.6" />
    <text x="238" y="115" text-anchor="middle" font-size="13" font-family="var(--font-mono)" fill="var(--on-accent)">{"2"}</text>
    <rect x="358" y="92" width="50" height="36" rx="6" fill="var(--accent)" stroke="var(--accent)" stroke-width="1.6" />
    <text x="383" y="115" text-anchor="middle" font-size="13" font-family="var(--font-mono)" fill="var(--on-accent)">{"1"}</text>
    <rect x="503" y="92" width="50" height="36" rx="6" fill="var(--accent)" stroke="var(--accent)" stroke-width="1.6" />
    <text x="528" y="115" text-anchor="middle" font-size="13" font-family="var(--font-mono)" fill="var(--on-accent)">{"0"}</text>
    <text x="570" y="115" font-size="13" font-family="var(--font-mono)" fill="var(--accent)">{"3 монеты"}</text>

    <text x="68" y="150" font-size="11" font-family="var(--font-mono)" fill="var(--muted)">{"каждый шаг: локально лучшее, зафиксировано — без отката"}</text>

    <line x1="93" y1="158" x2="93" y2="244" stroke="var(--hairline-strong)" stroke-width="1.4" stroke-dasharray="5 4" marker-end="url(#dhead)" />
    <text x="104" y="206" font-size="11" font-family="var(--font-mono)" fill="var(--muted)">{"дорога которую жадный отвергает — без пересмотра"}</text>

    <text x="20" y="240" font-size="12" font-family="var(--font-mono)" fill="var(--d-systems)">{"ОПТИМУМ: путь который жадный не исследует"}</text>

    <line x1="118" y1="272" x2="258" y2="272" stroke="var(--d-systems)" stroke-width="1.8" marker-end="url(#ohead)" />
    <line x1="335" y1="272" x2="476" y2="272" stroke="var(--d-systems)" stroke-width="1.8" marker-end="url(#ohead)" />
    <text x="188" y="262" text-anchor="middle" font-size="12" font-family="var(--font-mono)" fill="var(--ink)">{"взять 3"}</text>
    <text x="405" y="262" text-anchor="middle" font-size="12" font-family="var(--font-mono)" fill="var(--ink)">{"взять 3"}</text>

    <rect x="68" y="254" width="50" height="36" rx="6" fill="var(--card-2)" stroke="var(--d-systems)" stroke-width="1.6" />
    <text x="93" y="277" text-anchor="middle" font-size="13" font-family="var(--font-mono)" fill="var(--ink)">{"6"}</text>
    <rect x="285" y="254" width="50" height="36" rx="6" fill="var(--card-2)" stroke="var(--d-systems)" stroke-width="1.6" />
    <text x="310" y="277" text-anchor="middle" font-size="13" font-family="var(--font-mono)" fill="var(--ink)">{"3"}</text>
    <rect x="503" y="254" width="50" height="36" rx="6" fill="var(--card-2)" stroke="var(--d-systems)" stroke-width="1.6" />
    <text x="528" y="277" text-anchor="middle" font-size="13" font-family="var(--font-mono)" fill="var(--ink)">{"0"}</text>
    <text x="570" y="277" font-size="13" font-family="var(--font-mono)" fill="var(--d-systems)">{"2 монеты"}</text>

    <text x="20" y="332" font-size="11.5" font-family="var(--font-mono)" fill="var(--ink)">{"жадный оптимален ТОЛЬКО когда каждое локально лучшее складывается в глобально лучшее"}</text>
    <text x="20" y="352" font-size="11.5" font-family="var(--font-mono)" fill="var(--muted)">{"здесь это не так: жадный 3 монеты  >  оптимум 2 монеты"}</text>

    <defs>
      <marker id="ghead" markerWidth="8" markerHeight="8" refX="6" refY="3" orient="auto">
        <path d="M0,0 L6,3 L0,6 Z" fill="var(--accent)" />
      </marker>
      <marker id="ohead" markerWidth="8" markerHeight="8" refX="6" refY="3" orient="auto">
        <path d="M0,0 L6,3 L0,6 Z" fill="var(--d-systems)" />
      </marker>
      <marker id="dhead" markerWidth="8" markerHeight="8" refX="6" refY="3" orient="auto">
        <path d="M0,0 L6,3 L0,6 Z" fill="var(--hairline-strong)" />
      </marker>
    </defs>
  </svg>
</DiagramFrame>

<Code lang="ru">
**Жадный размен монет**

Алгоритм короткий: отсортируй монеты от большей к меньшей, потом для каждой монеты бери столько сколько помещается перед переходом к следующей. Он фиксирует каждую монету и никогда не пересматривает.

<AnnotatedCode
  code={`function greedyChange(coins, amount) {
  // Sort coins largest-first so we always try the biggest that fits
  const sorted = [...coins].sort((a, b) => b - a);
  let remaining = amount;
  const picks = [];
  // Walk coins from largest to smallest, never going back
  for (const coin of sorted) {
    // Take this coin as many times as it fits into the remainder
    while (remaining >= coin) {
      remaining -= coin;
      picks.push(coin);
    }
  }
  return picks; // the coins greedy chose (may not be optimal!)
}`}
  annotations={[
    { line: 3, text: "Сортируй по убыванию чтобы первая монета которую мы пробуем на каждом шаге была локально лучшей (самой большой). Эта сортировка и делает всё O(n log n)." },
    { line: 7, text: "Сканируй монеты от большей к меньшей. Раз оставив монету позади, мы никогда к ней не возвращаемся — это часть 'никогда не пересматривай' жадного." },
    { line: 9, text: "Жадная фиксация: хватай эту монету пока она ещё помещается, даже не взглянув на меньшие монеты." },
    { line: 13, text: "Верни то что жадный выбрал. Для {1,3,4} суммы 6 это 3 монеты — субоптимально. Жадный всегда возвращает ЧТО-ТО; это что-то оптимально только когда задача обладает свойством жадного выбора." }
  ]}
/>

**Запуск: успех на канонических монетах, провал на {1, 3, 4}**

Смотри как та же функция выигрывает на системе США и проигрывает на {1, 3, 4}. Код идентичен; меняется только набор монет.

<CodeRunner
  id="cr-greedy-change"
  lang="ru"
  code={`function greedyChange(coins, amount) {
  const sorted = [...coins].sort((a, b) => b - a);  // largest coin first
  let remaining = amount;
  const picks = [];
  for (const coin of sorted) {
    while (remaining >= coin) {
      remaining -= coin;
      picks.push(coin);
    }
  }
  return picks;
}

const us = greedyChange([1, 5, 10, 25], 63);
console.log("US coins {1,5,10,25}, amount 63:");
console.log("  picks:", us.join(" + "), "=", us.length, "coins  (optimal: 6)");

const bad = greedyChange([1, 3, 4], 6);
console.log("Coins {1,3,4}, amount 6:");
console.log("  greedy picks:", bad.join(" + "), "=", bad.length, "coins");
console.log("  optimal     : 3 + 3 = 2 coins  -> greedy LOSES");`}
  expected={`US coins {1,5,10,25}, amount 63:
  picks: 25 + 25 + 10 + 1 + 1 + 1 = 6 coins  (optimal: 6)
Coins {1,3,4}, amount 6:
  greedy picks: 4 + 1 + 1 = 3 coins
  optimal     : 3 + 3 = 2 coins  -> greedy LOSES`}
/>

На системе США жадный точно попадает в оптимум (63 нужно 6 монет, и жадный находит 6). На {1, 3, 4} та же самая логика возвращает 3 монеты когда хватило бы 2. Ничего не упало, ничего тебя не предупредило — жадный просто уверенно вернул худший ответ. Эта тишина и есть ровно причина почему жадное решение надо *доказывать* корректным для его конкретной области входов.
</Code>

<Trace lang="ru">
Давай протрассируем жадный на провальном случае — монеты `{1, 3, 4}`, сумма `6` — шаг за шагом, чтобы ты мог увидеть ровно где локально лучший выбор уводит его в сторону.

<AlgoTrace
  id="trace-greedy-change"
  lang="ru"
  code={[
    "sorted = {4, 3, 1}   // largest first",
    "remaining = 6, picks = []",
    "coin 4: while remaining >= 4 -> take 4",
    "coin 3: 3 > remaining? skip",
    "coin 1: while remaining >= 1 -> take 1",
    "return picks"
  ]}
  frames={[
    { caption: "Старт. Монеты отсортированы от большей: [4, 3, 1]. remaining = 6, picks = []. Жадный будет брать самую большую монету которая помещается на каждом шаге.", line: 1, cells: [{ value: "remaining=6" }, { value: "picks=[]" }] },
    { caption: "Монета 4: 4 <= 6, так что бери её. remaining = 6 - 4 = 2. picks = [4]. Жадный зафиксировал четвёрку и никогда её не отменит.", line: 3, cells: [{ value: "remaining=2", mark: "active" }, { value: "picks=[4]" }] },
    { caption: "Монета 4 снова: 4 > 2, больше не помещается. Переходим к следующей монете. Четвёрка заблокирована.", line: 3, cells: [{ value: "remaining=2" }, { value: "picks=[4]" }] },
    { caption: "Монета 3: 3 > 2, не помещается. Пропусти её целиком. (Это фатальный шаг: тройка помогла бы, но жадный уже потратил четвёрку которая съела бюджет.)", line: 4, cells: [{ value: "remaining=2", mark: "target" }, { value: "picks=[4]" }] },
    { caption: "Монета 1: 1 <= 2, бери её. remaining = 1. picks = [4, 1].", line: 5, cells: [{ value: "remaining=1", mark: "active" }, { value: "picks=[4,1]" }] },
    { caption: "Монета 1 снова: 1 <= 1, бери её. remaining = 0. picks = [4, 1, 1]. Готово.", line: 5, cells: [{ value: "remaining=0", mark: "active" }, { value: "picks=[4,1,1]" }] },
    { caption: "Верни [4, 1, 1] = 3 монеты. Оптимум был 3 + 3 = 2 монеты. Первый локально лучший выбор жадного (взять 4) не был частью никакого оптимального решения -> свойство жадного выбора проваливается здесь.", line: 6, cells: [{ value: "3 coins", mark: "done" }, { value: "optimal=2" }] }
  ]}
/>
</Trace>

<Complexity lang="ru">
**Время: O(n log n), обычно доминирует сортировка**

Жадный алгоритм делает один проход по выборам, выполняя O(1) (или почти O(1)) работы на шаг, так что сам скан это O(n) для n элементов. Но большинство жадных алгоритмов сначала **сортируют** вход чтобы поставить локально лучший выбор впереди — самые большие монеты первыми, самые короткие работы первыми, самые ранние дедлайны первыми — и эта сортировка стоит O(n log n). Сортировка доминирует, так что заглавная стоимость это:

```
total time = O(n log n)   (sort) + O(n) (greedy scan) = O(n log n)
```

Пример с разменом монет даже дешевле потому что число *номиналов монет* крошечное и фиксированное (4 монеты для системы США), так что "n" которое сортируется это маленькая константа — но общий жадный шаблон это "сортируй, потом подметай", и это O(n log n).

**Почему жадный настолько быстрее чем DP**

Скорость берётся целиком из *неисследования альтернатив*. Жадный фиксирует один выбор на шаг, так что он делает один линейный проход. Динамическое программирование, напротив, вычисляет много комбинаций выборов подзадач и хранит лучшую, так что оно платит за таблицу состояний.

```
                what it does                         typical time
greedy          take locally-best choice, no backtrack   O(n log n)
dynamic prog    explore all subproblem-choice combos     O(states x work)  often O(n*amount)
```

Для размена монет с целевой суммой `amount`, DP который *всегда* корректен заполняет таблицу размера `amount` и это O(coins x amount). Жадный пропускает всё это. Так что когда жадный доказуемо корректен, ты меняешь O(coins x amount) у DP на почти O(n log n) у жадного — большой выигрыш. Цена этого выигрыша в том что жадный корректен только на задачах со свойством жадного выбора; иначе его скорость покупает тебе неверный ответ.

**Вывод о стоимости**

Жадный быстр потому что он неглубок: один проход, без сомнений. Когда бы ты ни тянулся к жадному, реальный вопрос никогда не "достаточно ли он быстр" (почти всегда да), а "*корректен* ли он для этой задачи" — это ровно то чему следующий урок учит тебя доказывать.
</Complexity>

<AlgoPractice
  id="practice-what-is-greedy"
  lessonSlug="01-what-is-greedy"
  lang="ru"
  problems={[
    {
      kind: "choice",
      prompt: "Какое единственное правило определяет жадный алгоритм?",
      choices: [
        "Он исследует каждую комбинацию выборов подзадач и хранит лучшую",
        "Он повторно делает локально лучший выбор и никогда его не пересматривает",
        "Он заполняет таблицу подзадач от базовых случаев вверх",
        "Он случайно выбирает вариант и откатывается если тот провалился"
      ],
      answer: 1,
      hint: "Жадный фиксирует лучше всего выглядящий ход на каждом шаге и никогда не откатывается. Исследование всех комбинаций это динамическое программирование, не жадный."
    },
    {
      kind: "choice",
      prompt: "Чем жадный отличается от динамического программирования на одном виде задачи?",
      choices: [
        "Жадный всегда даёт оптимальный ответ; DP лишь иногда",
        "DP фиксирует один выбор на шаг; жадный исследует все комбинации",
        "DP исследует все комбинации выборов подзадач и хранит лучшую; жадный фиксирует один локальный выбор и никогда не пересматривает",
        "Они идентичны; жадный это просто более медленное имя для DP"
      ],
      answer: 2,
      hint: "DP подстраховывается сравнивая все альтернативы. Жадный ставит на локально лучший выбор и никогда не сравнивает его потом. Это делает жадный быстрее, но корректным лишь иногда."
    },
    {
      kind: "choice",
      prompt: "Какими ДВУМЯ свойствами должна обладать задача чтобы жадный алгоритм был корректен?",
      choices: [
        "Перекрывающимися подзадачами и стеком рекурсии",
        "Свойством жадного выбора и оптимальной подструктурой",
        "Отсортированным входом и хеш-таблицей",
        "Маленьким размером входа и целочисленными ключами"
      ],
      answer: 1,
      hint: "Свойство жадного выбора: оптимум всегда может начинаться с локально лучшего выбора. Оптимальная подструктура: оптимальное решение содержит оптимальные решения своих подзадач."
    },
    {
      kind: "choice",
      prompt: "Для монет {1, 3, 4} и суммы 6 жадный возвращает 4 + 1 + 1 = 3 монеты. Каково оптимальное число монет, и почему жадный его упускает?",
      choices: [
        "3 монеты; жадный тут на самом деле оптимален",
        "2 монеты (3 + 3); первый локально лучший выбор жадного (взять 4) не часть никакого оптимального решения",
        "1 монета; есть одна монета достоинством 6",
        "4 монеты; жадный пересчитывает"
      ],
      answer: 1,
      hint: "Оптимум это 3 + 3 = 2 монеты, вообще без четвёрки. Жадный схватил четвёрку первой и застрял, так что свойство жадного выбора проваливается для {1, 3, 4}."
    },
    {
      kind: "choice",
      prompt: "Какова типичная временная сложность корректного жадного алгоритма, и почему?",
      choices: [
        "O(2^n), потому что он рекурсивен",
        "O(n log n), обычно доминирует начальная сортировка с последующим одним линейным проходом",
        "O(n^2), потому что он сравнивает каждую пару выборов",
        "O(1), потому что он делает всего один выбор"
      ],
      answer: 1,
      hint: "Жадный это 'сортируй, потом подметай'. Сортировка это O(n log n), а скан в один проход это O(n), так что сортировка доминирует: O(n log n)."
    },
    {
      kind: "choice",
      prompt: "Жадный код для задачи БЕЗ свойства жадного выбора будет:",
      choices: [
        "Падать с ошибкой",
        "Зацикливаться навсегда",
        "Работать нормально и возвращать ответ который может быть субоптимальным, без предупреждения",
        "Автоматически откатываться к динамическому программированию"
      ],
      answer: 2,
      hint: "Эта тихая неверность и есть опасность: жадный всегда что-то возвращает. На {1,3,4} сумме 6 он уверенно возвращает 3 монеты когда 2 оптимальны. Именно поэтому жадный надо доказывать корректным."
    },
    {
      kind: "numeric",
      prompt: "Используя жадный на канонической системе США {1, 5, 10, 25}, сколько монет нужно чтобы набрать 30 центов (бери самую большую монету которая помещается, повторяй)?",
      answer: 2,
      hint: "Бери 25 (остаётся 5), потом бери 5 (остаётся 0). Это 25 + 5 = 2 монеты, что также оптимум для канонической системы."
    }
  ]}
/>

<Inset kind="why" lang="ru">
**Зачем учить жадный если он может быть неверен?** Потому что когда он *корректен*, он драматически проще и быстрее чем динамическое программирование — один проход вместо целой таблицы — и многие классические задачи (выбор активностей, кодирование Хаффмана, минимальные остовные деревья, дробный рюкзак, кратчайшие пути Дейкстры) построены на доказуемо корректных жадных выборах. Ценность жадного не в "используй его везде"; она в "распознай редкие задачи где локальный выбор доказуемо глобален, и эксплуатируй их ради огромного ускорения". Подвох — знание *какие* это задачи — и есть то что делает навык доказательства (следующий урок) настоящим содержанием этого раздела.
</Inset>

<Inset kind="mistake" lang="ru">
**Ошибка: предположение что жадный корректен потому что он "ощущается правильным" или проходит пару тестов.** Жадный на {1, 3, 4} работает для многих сумм (от 1 до 5 в порядке) и проваливается лишь на конкретных целях вроде 6. Горстка проходящих примеров ничего не доказывает — жадный алгоритм корректен только если ты можешь доказать что свойство жадного выбора держится для *каждого* входа. Никогда не выпускай жадное решение на силе пары зелёных тестов; неверный ответ который он возвращает тихий и его легко пропустить.
</Inset>

<Inset kind="edgecase" lang="ru">
**Тот же жадный может быть прав на одном наборе входов и неверен на другом.** Размен монет это учебниковый пример: жадный оптимален на канонической системе США {1, 5, 10, 25}, но субоптимален на {1, 3, 4}. Корректность это свойство *экземпляра задачи* (здесь — номиналов монет), а не жадного кода. Так что "корректен ли жадный для размена монет?" не имеет единого ответа — ты должен спросить "корректен для *какой* системы монет?". Является ли система монет "канонической" (безопасной для жадного) — это сам по себе вопрос требующий проверки.
</Inset>

<Inset kind="practice" lang="ru">
**Чек-лист жадного.** Перед использованием жадного алгоритма спроси: (1) Могу ли я сформулировать локально лучший выбор на каждом шаге? (2) Держится ли свойство жадного выбора — может ли каждый оптимум начинаться с этого выбора? (3) Держится ли оптимальная подструктура — является ли остаток меньшим экземпляром той же задачи? (4) Могу ли я *доказать* (2), или хотя бы не найти контрпример после упорных попыток? Если ты не можешь это доказать, либо найди контрпример (как {1,3,4} сумма 6) либо откатись к динамическому программированию, которое корректно без свойства жадного выбора. Скорость это дар жадного; корректность тебе надо заработать.
</Inset>

<Check lang="ru">
  <Quiz
    id="quiz-what-is-greedy"
    pieceSlug="01-what-is-greedy"
    lang="ru"
    question="Объясни что это жадный алгоритм, чем он отличается от динамического программирования, два условия которые ему нужны для корректности, случай где он работает и где проваливается, и его типичную временную сложность."
    choices={[
      {
        label: "Жадный алгоритм строит решение повторно делая локально лучший выбор и никогда его не пересматривая. Он отличается от динамического программирования, которое исследует все комбинации выборов подзадач и хранит лучшую; жадный вместо этого фиксирует один локальный выбор на шаг и никогда не откатывается. Он корректен только когда задача обладает свойством жадного выбора (оптимум всегда может начинаться с локально лучшего выбора) и оптимальной подструктурой (оптимальное решение содержит оптимальные решения своих подзадач). Размен монет показывает обе стороны: жадный оптимален на канонической системе США {1,5,10,25}, но на {1,3,4} для суммы 6 он возвращает 4+1+1 = 3 монеты, тогда как оптимум это 3+3 = 2 монеты. Поскольку жадный может тихо вернуть худший ответ, его надо доказывать корректным. Когда он корректен, он обычно O(n log n), доминирует начальная сортировка.",
        correct: true
      },
      {
        label: "Жадный алгоритм исследует каждую комбинацию выборов, как динамическое программирование, но хранит их в кэше так что он быстрее. Он всегда корректен на каждой задаче потому что он сравнивает все альтернативы перед фиксацией. Размен монет с {1,3,4} для суммы 6 даёт оптимальные 2 монеты. Его временная сложность O(2^n) потому что он рекурсивен.",
        misconception: "Это описание задом наперёд. Жадный НЕ исследует все комбинации и не сравнивает альтернативы — он фиксирует один локальный выбор и никогда не пересматривает (исследование всех комбинаций это DP). Он не всегда корректен: на {1,3,4} сумме 6 жадный возвращает 3 монеты, не оптимальные 2. И корректный жадный обычно O(n log n), не O(2^n)."
      },
      {
        label: "Жадный алгоритм делает локально лучший выбор на каждом шаге, и пока задача обладает перекрывающимися подзадачами и стеком рекурсии, он гарантированно оптимален. Он работает для любой системы монет, включая {1,3,4}, где он даёт 2 монеты для суммы 6. Его стоимость O(n) потому что он никогда не сортирует.",
        misconception: "Корректности жадного нужны свойство жадного выбора и оптимальная подструктура, не перекрывающиеся подзадачи или стек рекурсии. Он НЕ работает для любой системы монет: на {1,3,4} сумме 6 жадный возвращает 3 монеты, не 2. И жадный обычно сначала сортирует, так что он обычно O(n log n), не O(n)."
      },
      {
        label: "Жадный алгоритм это просто динамическое программирование под другим именем; оба исследуют все комбинации выборов подзадач и всегда возвращают оптимальный ответ. Нет условия при котором жадный может быть неверен, так что ему никогда не нужно доказательство корректности. Размен монет работает идентично при любых номиналах.",
        misconception: "Жадный это не переименованный DP. DP исследует все комбинации; жадный фиксирует один локальный выбор и никогда не пересматривает, так что жадный МОЖЕТ быть неверен (например {1,3,4} сумма 6 даёт 3 монеты против оптимальных 2). Именно поэтому жадный надо доказывать корректным, и поэтому он не работает идентично при любых номиналах."
      }
    ]}
  />
</Check>

<Recap lang="ru">
<Term slug="greedy_algorithm">Жадный алгоритм</Term> строит решение повторно делая <Term slug="local_choice">локально лучший выбор</Term> и **никогда не пересматривая** его. Никакого отката, никакого исследования альтернатив — просто один прямой проход, хватая лучший вариант на каждом шаге.

**Жадный против динамического программирования:**
- **DP** исследует *все* комбинации выборов подзадач и хранит лучшую (оно подстраховывается).
- **Жадный** фиксирует *один* локальный выбор на шаг и никогда не сравнивает альтернативы потом (он ставит).

DP корректен шире; жадный быстрее, но корректен только когда его ставка всегда срабатывает.

**Жадный корректен только с двумя свойствами:**
- **Свойство жадного выбора** — глобально оптимальное решение всегда может начинаться с локально лучшего выбора.
- **Оптимальная подструктура** — оптимальное решение содержит оптимальные решения своих подзадач.

Если любое проваливается, жадный всё равно работает и всё равно возвращает *что-то* — просто тихо субоптимальное. Так что жадный надо **доказывать** корректным, никогда не предполагать.

**Демонстрация на размене монет:**
- Каноническая система США {1, 5, 10, 25}: жадный оптимален (30 = 25 + 5 = 2 монеты).
- Система {1, 3, 4}, сумма 6: жадный даёт 4 + 1 + 1 = **3 монеты**, но оптимум это 3 + 3 = **2 монеты**. Свойство жадного выбора проваливается потому что локально лучший первый ход (взять 4) не в каком оптимальном решении.

**Сложность:** когда корректен, жадный обычно **O(n log n)** — доминирует начальная сортировка, потом один проход O(n) — намного дешевле таблицы DP.

Дальше: урок 02 показывает как *доказать* жадный алгоритм корректным (аргумент обмена), чтобы ты мог отличить случаи {1,5,10,25} от ловушек {1,3,4} до того как выпустишь.
</Recap>
