---
concepts:
- greedy-choice-property
- exchange-argument
- optimal-substructure
- interval-scheduling
- greedy-stays-ahead
- gas-station
estMin: 13
lang: ru
level: senior
order: 8
prereqs: []
slug: quiz-short
sources:
- https://jeffe.cs.illinois.edu/teaching/algorithms/book/04-greedy.pdf
- https://en.wikipedia.org/wiki/Greedy_algorithm
status: ready
summary: "Подсказки на свободное припоминание по всему юниту. Сначала отвечайте по памяти, затем раскрывайте модельный ответ и сравнивайте — именно усилие припоминания закрепляет дисциплину доказательства."
title: 'Greedy: свободное припоминание'
track: algorithms
unit: 11-greedy
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";

<Hook>
Припоминание сильнее перечитывания. На каждую подсказку проговорите или запишите полный ответ по памяти, прежде чем открыть модельный. Восстановление exchange argument с нуля и есть то, что превращает его из узнаваемого в применимое.
</Hook>

<Goal lang="ru">
Восстановите стержень юнита — два условия корректности greedy, три хода exchange argument, ключи сортировки для interval-задач и инвариант Gas Station — не заглядывая обратно в уроки.
</Goal>

<RetrievalDrawer
  client:visible
  id="11-greedy-short-retrieval"
  lang="ru"
  questions={[
    { q: "Какими двумя свойствами должна обладать задача, чтобы greedy был оптимален, и что означает каждое?", a: "Greedy choice property и optimal substructure. Greedy choice property говорит, что всегда существует глобально оптимальное решение, начинающееся с локально-лучшего выбора — взятие greedy choice никогда не загоняет в угол, его всегда можно расширить до оптимума. Optimal substructure говорит, что после первого выбора остаток — это меньший экземпляр той же задачи, и оптимальное решение целого содержит оптимальное решение остатка (DP тоже нуждается в этом свойстве). Если хоть одно отсутствует, greedy всё равно выполняется и всё равно что-то возвращает — просто тихо неоптимальное — поэтому greedy надо доказывать, а не предполагать." },
    { q: "Назовите три хода exchange argument и направление рассуждения, которое заставляет его работать.", a: "(1) Предположите произвольное оптимальное решение O, отличающееся от greedy в первом выборе. (2) Обмен: измените O, убрав его первую отличающуюся часть и вставив greedy choice, получив O', сконструированное так, что O' остаётся допустимым и не хуже O. (3) Вывод: поскольку O было оптимальным, а O' не хуже, O' тоже оптимально и теперь согласуется с greedy — значит, существует оптимальное решение, содержащее greedy choice, и первый greedy-выбор безопасен. Ключевое направление: вы не строите ответ greedy с нуля, вы переформировываете известный оптимум к нему одним безобидным swap за раз. Затем optimal substructure плюс индукция распространяют безопасность на всё решение." },
    { q: "Почему в шаге обмена достаточно «не хуже» (а не «строго лучше») и почему это правильная сила утверждения?", a: "Нужно лишь, чтобы O' было не хуже оптимума O, а не строго лучше. Если бы вы всегда могли сделать его строго лучше, то O не было оптимальным — лишнее противоречие. Честное утверждение мягче: greedy choice никогда ничего не стоит. Поскольку O было оптимальным, а O' совпадает по значению, O' тоже оптимально и теперь содержит greedy choice. «Не хуже» достаточно сильно, чтобы доказать безопасность, и достаточно слабо, чтобы реально выполняться — в activity selection swap убирает одну работу и добавляет одну, сохраняя счёт ровно равным, что является успешным доказательством, а не провалом." },
    { q: "Для interval-задач какой ключ сортировки соответствует какой цели и почему каждый корректен?", a: "Для максимизации непересекающихся интервалов (activity selection): сортировка по earliest finish time — интервал с самым ранним финишем оставляет больше всего места после себя, поэтому по exchange argument он всегда безопасный выбор. Для merging пересекающихся интервалов в наименьшее число непересекающихся прогонов: сортировка по времени старта — пройдя интервал, ничто более позднее не дотянется за старт текущего прогона, поэтому корректен один проход слева направо со сравнением с одним прогоном. Для минимума комнат (meeting rooms II): сортируйте старты и финиши отдельно и проходите двухуказательным счётчиком — ответ это пиковое число одновременно активных интервалов. Сортировка по кратчайшей длительности — неверный greedy для максимизации: короткий интервал посередине может блокировать двух совместимых соседей ([0,4],[3,5],[4,8] даёт 1 вместо 2)." },
    { q: "Чем «greedy stays ahead» отличается от exchange argument и когда вы бы его применили?", a: "Оба доказывают одну и ту же корректность, но разной механикой. Exchange argument преобразует известный оптимум к greedy безобидными swap. «Greedy stays ahead» вместо этого отслеживает одну измеримую величину и доказывает, что значение greedy на каждом шаге не хуже значения любого другого решения — для activity selection это что после выбора первых k работ k-я работа greedy финиширует не позже k-й работы любого другого валидного расписания, поэтому greedy никогда не отстаёт и вмещает не меньше работ. Применяйте, что чище: stays-ahead блестит, когда есть естественная монотонная величина для отслеживания; exchange argument — более общий инструмент, когда вы можете сконструировать локальный swap." },
    { q: "В Gas Station какое глобальное условие решает о выполнимости и почему сброс кандидата-старта в i+1 безопасен?", a: "Круговой маршрут возможен тогда и только тогда, когда суммарный net fuel по всему кругу, sum(gas[i] - cost[i]), >= 0 — если по кругу вы сжигаете больше, чем везёте, ни один старт не поможет. Чтобы найти старт, пройдите один раз с running tank с момента кандидата-старта; когда бак уходит в минус на станции i, сбросьте start в i+1 и tank в 0. Сброс безопасен, потому что каждая станция между старым start и i достигалась с неотрицательным running tank, так что стартуя на любой из них, до i доедешь с ещё меньшим топливом и тоже провалишься — пропуская их все разом, вы ничего не теряете. Когда total >= 0, выживший после прохода start корректен. Именно это делает один проход достаточным вместо перебора всех n стартов (O(n^2))." },
  ]}
/>

<Recap lang="ru">
Если вы восстановили каждый ответ по памяти, вы держите стержень юнита: greedy оптимален только при наличии greedy choice property плюс optimal substructure; exchange argument доказывает безопасность, переформировывая оптимум к greedy swap-ами «не хуже» (а отсутствующий swap сам по себе доказательство провала); interval-задачи живут или умирают по ключу сортировки — finish для максимизации, start для merging, sweep по стартам и финишам для комнат; а scan-greedy вроде Gas Station решают о выполнимости по глобальному инварианту, пока самокорректирующийся сброс находит старт за один проход.
</Recap>
