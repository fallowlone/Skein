---
concepts:
- two_dimensional_dp
- 2d_dp
- dp_table
- subproblem_state
- unique_paths
- longest_common_subsequence
- lcs
- edit_distance
- levenshtein_distance
- base_row_column
- fill_order
- rolling_array
- space_optimization
estMin: 30
lang: ru
mathPrereqs: []
order: 5
prereqs:
- 04-one-dimensional-dp
slug: 05-two-dimensional-dp
sources:
- https://en.wikipedia.org/wiki/Longest_common_subsequence
- https://en.wikipedia.org/wiki/Levenshtein_distance
- https://leetcode.com/problems/unique-paths/
- https://www.geeksforgeeks.org/dsa/longest-common-subsequence-dp-4/
- https://www.geeksforgeeks.org/dsa/edit-distance-dp-5/
status: ready
summary: 'Когда подзадаче нужны два индекса, состояние — dp[i][j], а кэш — двумерная
  таблица. Заполняй от базовой строки и столбца; ответ — в правом нижнем углу. Три
  классики: пути в сетке, LCS, расстояние редактирования. O(m*n) время, память ужимается
  до O(min(m,n)).'
title: 'Двумерная ДП: пути в сетке, LCS, расстояние редактирования'
track: algorithms
unit: 10-dynamic-programming
---



import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Idea from "~/components/lesson/Idea.astro";
import Code from "~/components/lesson/Code.astro";
import Trace from "~/components/lesson/Trace.astro";
import Complexity from "~/components/lesson/Complexity.astro";
import Check from "~/components/lesson/Check.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import AnnotatedCode from "~/components/algo/AnnotatedCode.astro";
import AlgoTrace from "~/components/algo/AlgoTrace.astro";
import CodeRunner from "~/components/algo/CodeRunner.astro";
import AlgoPractice from "~/components/algo/AlgoPractice.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";
import Term from "~/components/prose/Term.astro";
import DiagramFrame from "~/components/diagram/DiagramFrame.astro";

<Hook>
Одномерные ДП что ты видел до сих пор имели одну подвижную часть: состояние было одним числом, как `dp[i]`, и ты заполнял одну строку значений. Но огромное семейство задач нуждается в двух индексах сразу. Сколько есть способов пройти из левого верхнего угла сетки в правый нижний? Насколько похожи две строки? Как малым числом правок превратить `kitten` в `sitting`? У каждой из них есть подзадача описанная *двумя* позициями — строкой и столбцом, или индексом в каждую строку. Состояние становится `dp[i][j]`, а кэш становится двумерной таблицей. Механизм идентичен одномерной ДП — определи состояние, напиши рекуррентность, зафиксируй базовые случаи, заполни в порядке, прочитай ответ — только по сетке, а не по линии.
</Hook>

<Goal lang="ru">
После этого урока ты можешь распознать когда задаче нужно двумерное состояние `dp[i][j]` и выстроить соответствующую двумерную таблицу. Ты можешь точно определить состояние (что значит `dp[i][j]`), написать рекуррентность из соседей клетки, зафиксировать базовую строку и базовый столбец, выбрать порядок заполнения так чтобы зависимости каждой клетки были уже вычислены, и прочитать ответ в правом нижнем углу. Ты можешь проработать три классики по памяти: <Term slug="unique_paths">число уникальных путей в сетке</Term> (`dp[i][j] = dp[i-1][j] + dp[i][j-1]`), <Term slug="longest_common_subsequence">наибольшую общую подпоследовательность</Term> (`dp[i-1][j-1]+1` при совпадении, иначе `max(dp[i-1][j], dp[i][j-1])`), и <Term slug="edit_distance">расстояние редактирования</Term> (вставка, удаление или замена). Ты знаешь что сложность это O(m·n) по времени и O(m·n) по памяти, и ты можешь ужать память до O(min(m,n)) скользящим массивом, потому что каждая строка зависит только от строки выше. Следующий блок выходит за пределы сеток к более общим формам ДП.
</Goal>

<Idea lang="ru">
**Когда состоянию нужны два индекса**

В <Term slug="two_dimensional_dp">двумерной ДП</Term> подзадачу нельзя описать одним числом — ей нужны два. <Term slug="subproblem_state">Состояние</Term> это `dp[i][j]`, где `i` и `j` это две независимые позиции: строка и столбец в сетке, или индекс в каждую из двух строк. Поскольку состояние это пара, кэш это двумерная таблица (массив массивов), и ты заполняешь её в двух вложенных циклах вместо одного.

Всё остальное это тот же пятишаговый рецепт что и одномерная ДП:

1. **Определи состояние.** Скажи одним предложением что значит `dp[i][j]`. Это самый трудный и самый важный шаг — сделай его правильно и рекуррентность выпадет сама.
2. **Напиши рекуррентность.** Вырази `dp[i][j]` через клетки которые ты уже заполнил (обычно `dp[i-1][j]`, `dp[i][j-1]`, `dp[i-1][j-1]`).
3. **Установи базовые случаи.** Заполни первую строку и первый столбец напрямую — у них нет "предыдущей" клетки на которую опереться.
4. **Выбери порядок заполнения.** Зацикли так чтобы зависимости каждой клетки были вычислены до неё. Сверху вниз, слева направо работает всякий раз когда клетка зависит только от клеток сверху и слева.
5. **Прочитай ответ.** Это почти всегда правая нижняя клетка, `dp[m][n]` — состояние для *полной* задачи.

**Классика 1: уникальные пути в сетке**

Робот стартует в левом верхнем углу сетки `m × n` и может двигаться только **вправо** или **вниз**. Сколько различных путей достигают правого нижнего угла? Определи `dp[i][j]` = число путей от старта до клетки `(i, j)`. Чтобы достичь `(i, j)` твой последний ход пришёл либо сверху `(i-1, j)`, либо слева `(i, j-1)`, так:

```
dp[i][j] = dp[i-1][j] + dp[i][j-1]
```

База: вся первая строка и первый столбец это `1` (только один прямолинейный способ достичь любую краевую клетку). Ответ это `dp[m-1][n-1]`. Сетка 3 × 3:

```
   c0 c1 c2
r0  1  1  1
r1  1  2  3
r2  1  3  6   <- 6 paths to bottom-right
```

**Классика 2: наибольшая общая подпоследовательность (LCS)**

<Term slug="longest_common_subsequence">Подпоследовательность</Term> сохраняет символы в порядке но может пропускать некоторые (без перестановки). LCS находит самую длинную последовательность появляющуюся в *обеих* строках. Определи `dp[i][j]` = длина LCS первых `i` символов `A` и первых `j` символов `B`. Сравни последние символы каждого префикса:

```
if A[i-1] == B[j-1]:  dp[i][j] = dp[i-1][j-1] + 1     // match: extend the diagonal
else:                 dp[i][j] = max(dp[i-1][j], dp[i][j-1])  // mismatch: drop one char
```

База: строка 0 и столбец 0 это все `0` (LCS с пустой строкой пуста). Ответ это `dp[m][n]`. Заполненная таблица для `A = ABCBDAB`, `B = BDCAB`:

```
        ''  B  D  C  A  B
    ''   0  0  0  0  0  0
    A    0  0  0  0  1  1
    B    0  1  1  1  1  2
    C    0  1  1  2  2  2
    B    0  1  1  2  2  3
    D    0  1  2  2  2  3
    A    0  1  2  2  3  3
    B    0  1  2  2  3  4   <- LCS length 4 (e.g. "BCAB" or "BDAB")
```

Заметь что `+1` читает **диагональ** `dp[i-1][j-1]`, а `max` читает клетку **сверху** и клетку **слева**. Эти три соседа это сердце каждой строковой ДП.

**Классика 3: расстояние редактирования (Левенштейна)**

<Term slug="edit_distance">Расстояние редактирования</Term> это минимальное число односимвольных операций **вставки**, **удаления** или **замены** чтобы превратить строку `A` в строку `B`. Определи `dp[i][j]` = расстояние редактирования между первыми `i` символами `A` и первыми `j` символами `B`. Если последние символы совпадают, новая правка не нужна; иначе возьми самую дешёвую из трёх операций и добавь единицу:

```
if A[i-1] == B[j-1]:  dp[i][j] = dp[i-1][j-1]
else:                 dp[i][j] = 1 + min(
                          dp[i-1][j],     // delete A[i-1]
                          dp[i][j-1],     // insert B[j-1]
                          dp[i-1][j-1] )  // replace A[i-1] with B[j-1]
```

Базовые случаи несут здесь настоящий смысл: `dp[i][0] = i` (удали все `i` символов чтобы достичь пустой строки) и `dp[0][j] = j` (вставь все `j` символов из ничего). Ответ это `dp[m][n]`. `kitten → sitting` это 3 (замени k→s, замени e→i, вставь g).

**Общая форма**

Все три определяют `dp[i][j]`, заполняют базовую строку и базовый столбец, потом заполняют внутренность из уже-вычисленных соседей, и читают ответ в правом нижнем углу. Уникальные пути *складывают* двух соседей; LCS и расстояние редактирования *комбинируют* диагональ плюс двух ортогональных соседей. Как только ты видишь этот паттерн, большинство задач ДП на сетках и строках выглядят одинаково.
</Idea>

<DiagramFrame
  label="Двумерная таблица ДП для строковой задачи (LCS / расстояние редактирования) над строкой A по строкам (эпсилон, A, B, C) и строкой B по столбцам (эпсилон, X, Y, Z). Строка с индексом 0 и столбец с индексом 0 образуют базовую границу пустого префикса. Одна внутренняя клетка, dp[i][j] в строке i = 2 и столбце j = 2, выделена как текущая клетка. Три стрелки ведут в неё из её уже-вычисленных соседей: dp[i-1][j] из клетки прямо сверху, dp[i][j-1] из клетки прямо слева, и dp[i-1][j-1] из диагональной клетки сверху-слева. Большая диагональная стрелка показывает порядок заполнения, идущий от верхнего левого базового угла вниз к правому нижнему. Ответ для полной задачи читается из правой нижней клетки dp[m][n]."
  hue="var(--d-systems)"
  caption="Каждая внутренняя клетка dp[i][j] вычисляется из трёх уже-заполненных соседей: сверху dp[i-1][j], слева dp[i][j-1] и по диагонали dp[i-1][j-1]. Первая строка и первый столбец это базовый случай пустого префикса. Заполнение сверху вниз, слева направо гарантирует что каждый сосед готов прежде чем достигнута клетка; ответ всей задачи это правая нижняя клетка dp[m][n]."
>
  <svg viewBox="0 0 720 360" width="720" height="360" xmlns="http://www.w3.org/2000/svg" font-family="var(--font-body, sans-serif)">
    <defs>
      <marker id="dp2d-arrow-ru" markerWidth="9" markerHeight="9" refX="7" refY="4" orient="auto">
        <path d="M0,0 L8,4 L0,8 Z" fill="var(--d-systems)" />
      </marker>
      <marker id="dp2d-fill-ru" markerWidth="11" markerHeight="11" refX="8" refY="5" orient="auto">
        <path d="M0,0 L10,5 L0,10 Z" fill="var(--muted, #888)" />
      </marker>
    </defs>

    <text x="208" y="34" text-anchor="middle" font-size="13" font-weight="700" fill="var(--ink, currentColor)">Таблица 2-D ДП: dp[i][j]</text>
    <text x="208" y="52" text-anchor="middle" font-size="11" fill="var(--muted, #888)">B (столбцы j) поперёк, A (строки i) вниз</text>

    <line x1="96" y1="42" x2="320" y2="42" stroke="var(--d-systems)" stroke-width="2" marker-end="url(#dp2d-fill-ru)" />
    <text x="340" y="46" font-size="10.5" fill="var(--muted, #888)">j растёт</text>
    <line x1="78" y1="64" x2="78" y2="272" stroke="var(--d-systems)" stroke-width="2" marker-end="url(#dp2d-fill-ru)" />
    <text x="40" y="170" font-size="10.5" fill="var(--muted, #888)">i растёт</text>

    <text x="122" y="58" text-anchor="middle" font-size="11" font-weight="700" fill="var(--muted, #888)">&#949;</text>
    <text x="178" y="58" text-anchor="middle" font-size="11" font-weight="700" fill="var(--muted, #888)">X</text>
    <text x="234" y="58" text-anchor="middle" font-size="11" font-weight="700" fill="var(--muted, #888)">Y</text>
    <text x="290" y="58" text-anchor="middle" font-size="11" font-weight="700" fill="var(--muted, #888)">Z</text>

    <text x="88" y="90" text-anchor="end" font-size="11" font-weight="700" fill="var(--muted, #888)">&#949;</text>
    <text x="88" y="142" text-anchor="end" font-size="11" font-weight="700" fill="var(--muted, #888)">A</text>
    <text x="88" y="194" text-anchor="end" font-size="11" font-weight="700" fill="var(--muted, #888)">B</text>
    <text x="88" y="246" text-anchor="end" font-size="11" font-weight="700" fill="var(--muted, #888)">C</text>

    <rect x="96" y="64" width="224" height="52" fill="var(--card-2, #f4f4f4)" stroke="none" />
    <rect x="96" y="64" width="56" height="208" fill="var(--card-2, #f4f4f4)" stroke="none" />
    <text x="234" y="100" text-anchor="middle" font-size="9.5" fill="var(--muted, #888)">база: граница пустого префикса</text>

    <rect x="96" y="64" width="52" height="44" fill="var(--card, #fff)" stroke="var(--hairline-2, #888)" stroke-width="1" />
    <rect x="152" y="64" width="52" height="44" fill="var(--card, #fff)" stroke="var(--hairline-2, #888)" stroke-width="1" />
    <rect x="208" y="64" width="52" height="44" fill="var(--card, #fff)" stroke="var(--hairline-2, #888)" stroke-width="1" />
    <rect x="264" y="64" width="52" height="44" fill="var(--card, #fff)" stroke="var(--hairline-2, #888)" stroke-width="1" />

    <rect x="96" y="116" width="52" height="44" fill="var(--card, #fff)" stroke="var(--hairline-2, #888)" stroke-width="1" />
    <rect x="152" y="116" width="52" height="44" fill="var(--card, #fff)" stroke="var(--hairline-2, #888)" stroke-width="1" />
    <rect x="208" y="116" width="52" height="44" fill="var(--card, #fff)" stroke="var(--d-systems)" stroke-width="2" />
    <rect x="264" y="116" width="52" height="44" fill="var(--card, #fff)" stroke="var(--hairline-2, #888)" stroke-width="1" />

    <rect x="96" y="168" width="52" height="44" fill="var(--card, #fff)" stroke="var(--hairline-2, #888)" stroke-width="1" />
    <rect x="152" y="168" width="52" height="44" fill="var(--card, #fff)" stroke="var(--d-systems)" stroke-width="2" />
    <rect x="208" y="168" width="52" height="44" fill="var(--card-2, #f4f4f4)" stroke="var(--d-systems)" stroke-width="3" />
    <rect x="264" y="168" width="52" height="44" fill="var(--card, #fff)" stroke="var(--hairline-2, #888)" stroke-width="1" />

    <rect x="96" y="220" width="52" height="44" fill="var(--card, #fff)" stroke="var(--hairline-2, #888)" stroke-width="1" />
    <rect x="152" y="220" width="52" height="44" fill="var(--card, #fff)" stroke="var(--hairline-2, #888)" stroke-width="1" />
    <rect x="208" y="220" width="52" height="44" fill="var(--card, #fff)" stroke="var(--hairline-2, #888)" stroke-width="1" />
    <rect x="264" y="220" width="52" height="44" fill="var(--card, #fff)" stroke="var(--accent, #c70)" stroke-width="2.5" />

    <text x="178" y="142" text-anchor="middle" font-size="10" fill="var(--ink-2, currentColor)" font-family="var(--font-mono, monospace)">[i-1][j-1]</text>
    <text x="234" y="142" text-anchor="middle" font-size="10" fill="var(--ink-2, currentColor)" font-family="var(--font-mono, monospace)">[i-1][j]</text>
    <text x="178" y="194" text-anchor="middle" font-size="10" fill="var(--ink-2, currentColor)" font-family="var(--font-mono, monospace)">[i][j-1]</text>
    <text x="234" y="192" text-anchor="middle" font-size="11" font-weight="700" fill="var(--d-systems)" font-family="var(--font-mono, monospace)">[i][j]</text>
    <text x="290" y="246" text-anchor="middle" font-size="10" font-weight="700" fill="var(--accent, #c70)" font-family="var(--font-mono, monospace)">[m][n]</text>
    <text x="290" y="284" text-anchor="middle" font-size="9.5" fill="var(--accent, #c70)">ответ</text>

    <line x1="234" y1="150" x2="234" y2="176" stroke="var(--d-systems)" stroke-width="2" marker-end="url(#dp2d-arrow-ru)" />
    <line x1="190" y1="190" x2="216" y2="190" stroke="var(--d-systems)" stroke-width="2" marker-end="url(#dp2d-arrow-ru)" />
    <line x1="190" y1="150" x2="216" y2="176" stroke="var(--d-systems)" stroke-width="2" marker-end="url(#dp2d-arrow-ru)" />

    <line x1="116" y1="84" x2="280" y2="248" stroke="var(--muted, #888)" stroke-width="2" stroke-dasharray="6 4" marker-end="url(#dp2d-fill-ru)" />
    <text x="150" y="252" font-size="10" fill="var(--muted, #888)">порядок: сверху-слева вниз-вправо</text>

    <rect x="400" y="74" width="296" height="120" rx="10" fill="var(--card, #fff)" stroke="var(--hairline-2, #888)" stroke-width="1.5" />
    <text x="416" y="98" font-size="12" font-weight="700" fill="var(--ink, currentColor)">Рекуррентность (строковая ДП)</text>
    <text x="416" y="122" font-size="11" fill="var(--ink-2, currentColor)" font-family="var(--font-mono, monospace)">совпало:    dp[i][j] = dp[i-1][j-1] + 1</text>
    <text x="416" y="142" font-size="11" fill="var(--ink-2, currentColor)" font-family="var(--font-mono, monospace)">нет:        max(dp[i-1][j], dp[i][j-1])</text>
    <text x="416" y="166" font-size="10.5" fill="var(--muted, #888)">диагональ при совпадении; сверху + слева при несовпадении.</text>
    <text x="416" y="184" font-size="10.5" fill="var(--muted, #888)">клетка читает только сверху / слева / сверху-слева -&gt; безопасно.</text>

    <text x="400" y="232" font-size="11.5" font-weight="700" fill="var(--d-systems)">три соседа -&gt; одна клетка</text>
    <text x="400" y="252" font-size="10.5" fill="var(--muted, #888)">O(m*n) клеток, O(1) каждая.</text>
    <text x="400" y="270" font-size="10.5" fill="var(--muted, #888)">читай ответ в dp[m][n], правом нижнем углу.</text>
  </svg>
</DiagramFrame>

<Code lang="ru">
**Наибольшая общая подпоследовательность, снизу вверх**

Состояние это пара `(i, j)`, так кэш это таблица `(m+1) × (n+1)`. Лишняя строка и столбец держат базовый случай пустого префикса (все нули), вот почему индексы в строки это `A[i-1]` и `B[j-1]`.

<AnnotatedCode
  code={`function lcs(a, b) {
  const m = a.length, n = b.length;
  // (m+1) x (n+1) table; row 0 and col 0 are the empty-prefix base (all 0)
  const dp = Array.from({ length: m + 1 }, () => new Array(n + 1).fill(0));

  for (let i = 1; i <= m; i++) {
    for (let j = 1; j <= n; j++) {
      if (a[i - 1] === b[j - 1]) {
        dp[i][j] = dp[i - 1][j - 1] + 1;             // match: extend the diagonal
      } else {
        dp[i][j] = Math.max(dp[i - 1][j], dp[i][j - 1]); // mismatch: drop one char
      }
    }
  }
  return dp[m][n];                                    // answer is the bottom-right cell
}`}
  annotations={[
    { line: 2, text: "m и n это длины двух строк." },
    { line: 4, text: "Выдели двумерную таблицу размером (m+1) на (n+1), заполненную нулями." },
    { line: 6, text: "Внешний цикл проходит строки i = 1..m; строка 0 остаётся нулевым базовым случаем." },
    { line: 7, text: "Внутренний цикл проходит столбцы j = 1..n; столбец 0 остаётся нулевым базовым случаем." },
    { line: 8, text: "Сравни последние символы двух префиксов: A[i-1] и B[j-1]." },
    { line: 9, text: "Совпадение: LCS растёт на один за диагональной клеткой dp[i-1][j-1]." },
    { line: 11, text: "Несовпадение: лучшее из отбрасывания последнего символа A (сверху) или последнего символа B (слева)." },
    { line: 15, text: "Состояние полной задачи dp[m][n] это длина LCS." }
  ]}
/>

**Ключевой момент: таблица на один размер больше строк**

У таблицы `m+1` строк и `n+1` столбцов, не `m` и `n`. Индекс 0 каждой оси это пустой префикс, чья LCS равна 0. Эта лишняя граница и есть то что позволяет рекуррентности читать `dp[i-1][j-1]` даже когда `i` или `j` равно 1 не сваливаясь с таблицы.

**Запуск**

<CodeRunner
  id="cr-lcs"
  lang="ru"
  code={`function lcs(a, b) {
  const m = a.length, n = b.length;
  const dp = Array.from({ length: m + 1 }, () => new Array(n + 1).fill(0));
  for (let i = 1; i <= m; i++) {
    for (let j = 1; j <= n; j++) {
      if (a[i - 1] === b[j - 1]) {
        dp[i][j] = dp[i - 1][j - 1] + 1;
      } else {
        dp[i][j] = Math.max(dp[i - 1][j], dp[i][j - 1]);
      }
    }
  }
  return dp[m][n];
}

console.log("lcs(ABCBDAB, BDCAB) =", lcs("ABCBDAB", "BDCAB"));
console.log("lcs(AGGTAB, GXTXAYB) =", lcs("AGGTAB", "GXTXAYB"));
console.log("lcs(abc, abc)        =", lcs("abc", "abc"));
console.log("lcs(abc, def)        =", lcs("abc", "def"));`}
  expected={`lcs(ABCBDAB, BDCAB) = 4
lcs(AGGTAB, GXTXAYB) = 4
lcs(abc, abc)        = 3
lcs(abc, def)        = 0`}
/>

**Расстояние редактирования, та же форма с тремя вариантами**

Расстояние редактирования переиспользует ровно ту же структуру таблицы — меняются только рекуррентность и базовая строка/столбец. Здесь базовые случаи ненулевые: превращение префикса длины `i` в пустую строку стоит `i` удалений, так `dp[i][0] = i`, и симметрично `dp[0][j] = j`.

<CodeRunner
  id="cr-edit"
  lang="ru"
  code={`function editDistance(a, b) {
  const m = a.length, n = b.length;
  const dp = Array.from({ length: m + 1 }, () => new Array(n + 1).fill(0));

  for (let i = 0; i <= m; i++) dp[i][0] = i;   // delete all i chars
  for (let j = 0; j <= n; j++) dp[0][j] = j;   // insert all j chars

  for (let i = 1; i <= m; i++) {
    for (let j = 1; j <= n; j++) {
      if (a[i - 1] === b[j - 1]) {
        dp[i][j] = dp[i - 1][j - 1];                 // chars match: no new edit
      } else {
        dp[i][j] = 1 + Math.min(
          dp[i - 1][j],      // delete A[i-1]
          dp[i][j - 1],      // insert B[j-1]
          dp[i - 1][j - 1]   // replace A[i-1] -> B[j-1]
        );
      }
    }
  }
  return dp[m][n];
}

console.log("editDistance(kitten, sitting) =", editDistance("kitten", "sitting"));
console.log("editDistance(sunday, saturday) =", editDistance("sunday", "saturday"));
console.log("editDistance(horse, ros)      =", editDistance("horse", "ros"));
console.log("editDistance(abc, abc)        =", editDistance("abc", "abc"));`}
  expected={`editDistance(kitten, sitting) = 3
editDistance(sunday, saturday) = 3
editDistance(horse, ros)      = 3
editDistance(abc, abc)        = 0`}
/>
</Code>

<Trace lang="ru">
Давай заполним таблицу LCS для короткой пары `A = "AB"`, `B = "AC"`. Таблица 3 × 3 (одна лишняя строка и столбец для пустого префикса). Строка 0 и столбец 0 начинаются с 0. Мы заполняем строка за строкой, слева направо, наблюдая как каждая внутренняя клетка читает трёх своих соседей (сверху, слева, по диагонали).

<AlgoTrace
  id="trace-lcs-2d"
  lang="ru"
  code={[
    "for i in 1..m:",
    "  for j in 1..n:",
    "    if A[i-1] == B[j-1]:",
    "      dp[i][j] = dp[i-1][j-1] + 1",
    "    else:",
    "      dp[i][j] = max(dp[i-1][j], dp[i][j-1])"
  ]}
  frames={[
    { caption: "Базовый случай: строка 0 и столбец 0 это все 0 (LCS с пустым префиксом равна 0). Порядок чтения: столбцы ''  A  C, строки ''  A  B.", line: 1, cells: [{ value: "0", mark: "done" }, { value: "0", mark: "done" }, { value: "0", mark: "done" }] },
    { caption: "dp[1][1]: сравни A[0]='A' с B[0]='A'. Совпадение! dp[1][1] = dp[0][0] + 1 = 0 + 1 = 1.", line: 4, cells: [{ value: "i=1,j=1" }, { value: "A==A" }, { value: "dp=1", mark: "active" }] },
    { caption: "dp[1][2]: сравни A[0]='A' с B[1]='C'. Несовпадение. dp[1][2] = max(dp[0][2]=0, dp[1][1]=1) = 1.", line: 6, cells: [{ value: "i=1,j=2" }, { value: "A!=C" }, { value: "dp=1", mark: "active" }] },
    { caption: "Строка 1 готова: [0, 1, 1]. Переходи к строке 2 (символ B). dp[2][1]: сравни A[1]='B' с B[0]='A'. Несовпадение. dp[2][1] = max(dp[1][1]=1, dp[2][0]=0) = 1.", line: 6, cells: [{ value: "i=2,j=1" }, { value: "B!=A" }, { value: "dp=1", mark: "active" }] },
    { caption: "dp[2][2]: сравни A[1]='B' с B[1]='C'. Несовпадение. dp[2][2] = max(dp[1][2]=1, dp[2][1]=1) = 1.", line: 6, cells: [{ value: "i=2,j=2" }, { value: "B!=C" }, { value: "dp=1", mark: "active" }] },
    { caption: "Таблица заполнена. Правый нижний угол dp[2][2] = 1: LCS строк 'AB' и 'AC' это 'A', длина 1. Ответ всегда живёт в правой нижней клетке.", line: 4, cells: [{ value: "answer", mark: "target" }, { value: "dp[2][2]", mark: "target" }, { value: "= 1", mark: "done" }] }
  ]}
/>
</Trace>

<Complexity lang="ru">
**Время: O(m·n)**

В таблице `(m+1) × (n+1)` клеток, что это O(m·n). Каждая клетка делает O(1) работы: сравнение и `max` или `min` по фиксированному числу соседей. Так общее время это

```
total time = (number of states) x (work per state) = O(m*n) x O(1) = O(m*n)
```

Для двух строк длины 1000 это около миллиона вычислений клеток — быстро. Это то же правило сложности что и одномерная ДП (различные состояния умножить на работу на состояние); единственное изменение это что число состояний теперь *произведение* двух размерностей, а не одной.

**Память: O(m·n), ужимаемая до O(min(m, n))**

Полная таблица хранит каждую клетку, так прямолинейная версия использует O(m·n) памяти. Но посмотри на рекуррентности: `dp[i][j]` читает только из **строки `i`** и **строки `i-1`**. Она никогда не касается строки `i-2` или раньше. Так тебе нужно держать только две строки за раз — текущую и предыдущую — и перезаписывать по ходу. Это приём <Term slug="rolling_array">скользящего массива</Term>:

```
full table:    O(m*n) space   (keep every row)
two rows:      O(n)   space   (keep current + previous row)
one row:       O(n)   space   (overwrite carefully, saving the diagonal in a temp)
```

Чтобы сделать хранимую строку меньшей размерностью, зацикли с **более короткой** строкой по столбцам; тогда память это O(min(m, n)). Загвоздка: скользящие строки дают тебе только финальное *число*. Если тебе также нужно восстановить саму подпоследовательность или сценарий правок, ты должен держать полную таблицу O(m·n) чтобы пройтись по ней назад.

```
                 time      space        gives the path back?
full 2D table    O(m*n)    O(m*n)       yes
rolling rows     O(m*n)    O(min(m,n))  no (number only)
```

**Скачок от 1D к 2D**

Ментальная модель не меняется от 1D: посчитай различные состояния, умножь на работу на состояние. У одномерной ДП по `n` было `n` состояний и O(n) время. У двумерной ДП по сетке `m × n` есть `m·n` состояний и O(m·n) время. Если будущей задаче понадобились бы три индекса, таблица была бы трёхмерной а время O(m·n·p) — то же правило, больше размерностей.
</Complexity>

<AlgoPractice
  id="practice-two-dimensional-dp"
  lessonSlug="05-two-dimensional-dp"
  lang="ru"
  problems={[
    {
      kind: "choice",
      prompt: "В подсчёте уникальных путей в сетке, какая рекуррентность для dp[i][j] (пути до клетки (i,j)), если ходы могут быть только вправо или вниз?",
      choices: [
        "dp[i][j] = dp[i-1][j-1] + 1",
        "dp[i][j] = dp[i-1][j] + dp[i][j-1]",
        "dp[i][j] = max(dp[i-1][j], dp[i][j-1])",
        "dp[i][j] = dp[i-1][j] * dp[i][j-1]"
      ],
      answer: 1,
      hint: "Твой последний ход в (i,j) пришёл либо из клетки сверху (i-1,j), либо из клетки слева (i,j-1). Общее число путей это сумма путей достигающих эти две клетки."
    },
    {
      kind: "choice",
      prompt: "Для наибольшей общей подпоследовательности, чему равно dp[i][j] когда последние символы совпадают, то есть A[i-1] == B[j-1]?",
      choices: [
        "max(dp[i-1][j], dp[i][j-1])",
        "dp[i-1][j-1] + 1",
        "dp[i][j-1] + 1",
        "1 + min(dp[i-1][j], dp[i][j-1], dp[i-1][j-1])"
      ],
      answer: 1,
      hint: "Совпадение значит что оба префикса заканчиваются одним и тем же символом, так LCS растёт на один за LCS более коротких префиксов — диагональная клетка dp[i-1][j-1], плюс 1."
    },
    {
      kind: "choice",
      prompt: "В расстоянии редактирования, почему базовые случаи dp[i][0] = i и dp[0][j] = j (а не все нули как у LCS)?",
      choices: [
        "Потому что расстояние редактирования работает только на строках равной длины",
        "Потому что превращение строки длины i в пустую строку стоит i удалений, а построение строки длины j из пустой стоит j вставок",
        "Потому что таблица должна быть квадратной",
        "Потому что нули заставили бы min() всегда возвращать 0"
      ],
      answer: 1,
      hint: "Столбец 0 значит вторая строка пуста: ты должен удалить все i символов. Строка 0 значит первая строка пуста: ты должен вставить все j символов. Каждое это одна правка на символ."
    },
    {
      kind: "choice",
      prompt: "После заполнения двумерной таблицы ДП сверху вниз и слева направо, где ты читаешь ответ для полной задачи?",
      choices: [
        "В левой верхней клетке dp[0][0]",
        "В правой нижней клетке dp[m][n]",
        "Наибольшее значение где угодно в таблице",
        "Последняя клетка первой строки"
      ],
      answer: 1,
      hint: "Правая нижняя клетка это состояние для полной задачи — оба индекса на максимуме, что значит вся сетка или обе полные строки. (Для LCS и расстояния редактирования это dp[m][n].)"
    },
    {
      kind: "choice",
      prompt: "Почему память двумерной ДП часто можно сократить с O(m*n) до O(min(m,n))?",
      choices: [
        "Потому что таблица симметрична, так половина её избыточна",
        "Потому что каждая клетка dp[i][j] зависит только от текущей и предыдущей строки, так ты держишь только две строки (используя меньшую размерность как длину строки)",
        "Потому что большинство клеток в итоге нули",
        "Потому что можно пропустить базовую строку и столбец"
      ],
      answer: 1,
      hint: "Рекуррентности читают только dp[i-1][...] и dp[i][...] — никогда строку i-2. Так двух строк достаточно. Помести более короткую строку по хранимой размерности чтобы получить O(min(m,n))."
    },
    {
      kind: "numeric",
      prompt: "Вычисление расстояния редактирования между двумя строками длины 200 и 300 заполняет таблицу (200+1) x (300+1). Сколько это клеток?",
      answer: 60501,
      hint: "(m+1) x (n+1) = 201 x 301 = 60501. Каждая клетка это O(1) работы, так время пропорционально этому числу: O(m*n)."
    }
  ]}
/>

<Inset kind="why" lang="ru">
**Зачем лишняя строка и столбец?** Таблица это `(m+1) × (n+1)`, не `m × n`. Строка и столбец с индексом 0 представляют *пустой префикс* каждой строки. Без них рекуррентность `dp[i-1][j-1]` сваливалась бы с таблицы всякий раз когда `i` или `j` равно 1. Граница кодирует наименьший базовый случай (LCS с пустой = 0; расстояние редактирования с пустой = другая длина) и даёт каждой внутренней клетке валидного соседа для чтения. Вот почему индексы строк появляются как `A[i-1]` и `B[j-1]`: строка таблицы `i` соответствует символу строки `i-1`.
</Inset>

<Inset kind="mistake" lang="ru">
**Неправильный порядок заполнения, или чтение неправильного соседа.** Двумерная рекуррентность корректна только если зависимости каждой клетки уже заполнены когда ты до неё доходишь. `dp[i][j]` зависящая от `dp[i-1][j]`, `dp[i][j-1]` и `dp[i-1][j-1]` безопасна с циклами сверху вниз, слева направо — но если бы рекуррентность когда-нибудь читала `dp[i+1][j]` тебе понадобилось бы противоположное направление. Вторая частая оплошность это путаница трёх соседей: случай совпадения читает **диагональ** `dp[i-1][j-1]`, случай несовпадения читает **сверху** и **слева**. Перестановка их молча производит неправильные ответы которые всё равно "выглядят" правдоподобно на малых входах.
</Inset>

<Inset kind="edgecase" lang="ru">
**Пустые строки и идентичные строки.** Если любая из строк пуста, ответ немедленен из базовых случаев: LCS это 0, а расстояние редактирования это длина непустой строки. Если строки идентичны, LCS равна их длине а расстояние редактирования равно 0. Эти крайности это ровно то что кодируют базовая строка и столбец, так корректная таблица обрабатывает их без особых случаев. Всегда проверяй свой код на пустом входе и на двух равных входах — они ловят большинство ошибок на единицу и ошибок базового случая.
</Inset>

<Inset kind="practice" lang="ru">
**Рецепт двумерной ДП.** (1) Напиши одно предложение определяющее `dp[i][j]`. (2) Из этого определения выведи рекуррентность спрашивая "какие выборы ведут в состояние `(i,j)`?" — обычно клетки сверху, слева и по диагонали. (3) Заполни базовую строку и базовый столбец напрямую из определения. (4) Зацикли сверху вниз, слева направо чтобы зависимости были готовы. (5) Прочитай `dp[m][n]`. Потом оптимизируй: если тебе нужно только финальное число а не восстановленный путь, сверни к скользящему массиву для O(min(m,n)) памяти. Тренируйся заполняя вручную таблицы уникальных путей и расстояния редактирования для крошечных входов прежде чем доверять коду.
</Inset>

<Check lang="ru">
  <Quiz
    id="quiz-two-dimensional-dp"
    pieceSlug="05-two-dimensional-dp"
    lang="ru"
    question="Объясни как работает двумерная ДП: состояние, почему у таблицы лишняя строка и столбец, рекуррентности LCS и расстояния редактирования, где живёт ответ, и сложность по времени и памяти."
    choices={[
      {
        label: "Двумерная ДП использует состояние dp[i][j] с ключом из двух индексов (две координаты сетки, или один индекс в каждую из двух строк), так кэш это двумерная таблица размером (m+1) x (n+1) — лишняя строка и столбец держат базовый случай пустого префикса чтобы рекуррентность могла читать dp[i-1][j-1] на границе. LCS: dp[i-1][j-1]+1 когда последние символы совпадают, иначе max(dp[i-1][j], dp[i][j-1]). Расстояние редактирования: dp[i-1][j-1] при совпадении, иначе 1 + min(удаление dp[i-1][j], вставка dp[i][j-1], замена dp[i-1][j-1]), с базой dp[i][0]=i и dp[0][j]=j. Заполняй сверху вниз, слева направо; читай ответ в правом нижнем углу dp[m][n]. Время это O(m*n), память это O(m*n) но ужимается до O(min(m,n)) скользящим массивом поскольку каждая клетка зависит только от текущей и предыдущей строки.",
        correct: true
      },
      {
        label: "Двумерная ДП просто запускает одномерный алгоритм дважды, раз на строку, и складывает два результата. Таблица ровно m x n без лишней границы, а ответ это наибольшее значение найденное где угодно в таблице. Память никогда нельзя сократить ниже O(m*n) потому что каждая клетка может понадобиться в конце.",
        misconception: "2D ДП это не два отдельных одномерных прохода; она заполняет одну совместную таблицу где dp[i][j] связывает оба индекса. Таблица это (m+1) x (n+1) с базовой границей, а ответ это конкретная правая нижняя клетка dp[m][n], не глобальный максимум. Память сокращается до O(min(m,n)) скользящим массивом, потому что каждая клетка зависит только от текущей и предыдущей строки."
      },
      {
        label: "В LCS, dp[i][j] = dp[i-1][j-1] + 1 совпадают символы или нет, потому что каждая клетка расширяет диагональ на один. Расстояние редактирования использует те же нулевые базовые случаи что и LCS. Ответ читается из левой верхней клетки, поскольку именно там начинается рекуррентность.",
        misconception: "LCS добавляет 1 на диагонали только когда символы действительно совпадают; при несовпадении она берёт max(сверху, слева) и не добавляет ничего. Расстояние редактирования не использует нулевые базовые случаи — dp[i][0]=i и dp[0][j]=j потому что опустошение или построение префикса стоит столько правок. Ответ это правая нижняя клетка dp[m][n], не левая верхняя."
      },
      {
        label: "Двумерная ДП экспоненциальна, O(2^(m+n)), потому что есть два индекса для рекурсии и каждый ветвится в двух направлениях. Ты должен использовать мемоизацию иначе таблица никогда не закончится. Нет способа заполнить её снизу вверх потому что у клеток циклические зависимости.",
        misconception: "2D ДП полиномиальна, не экспоненциальна: есть только (m+1)(n+1) различных состояний и O(1) работы каждое, давая O(m*n) время. Нет циклических зависимостей — каждая клетка зависит только от клеток сверху и слева, что это ровно почему заполнение снизу вверх сверху вниз, слева направо работает без всякой рекурсии."
      }
    ]}
  />
</Check>

<Recap lang="ru">
<Term slug="two_dimensional_dp">Двумерная ДП</Term> для задач чьей <Term slug="subproblem_state">подзадаче</Term> нужны два индекса: состояние это `dp[i][j]` а кэш это двумерная таблица.

**Пятишаговый рецепт (тот же что 1D, по сетке):** определи что значит `dp[i][j]`; напиши рекуррентность из уже-заполненных соседей (сверху `dp[i-1][j]`, слева `dp[i][j-1]`, по диагонали `dp[i-1][j-1]`); заполни базовую строку и базовый столбец; зацикли сверху вниз, слева направо; прочитай ответ в правом нижнем углу `dp[m][n]`.

**Три классики:**
- **<Term slug="unique_paths">Уникальные пути</Term>:** `dp[i][j] = dp[i-1][j] + dp[i][j-1]`, базовая строка и столбец все 1.
- **<Term slug="longest_common_subsequence">LCS</Term>:** `dp[i-1][j-1]+1` при совпадении, иначе `max(dp[i-1][j], dp[i][j-1])`, базовая граница все 0.
- **<Term slug="edit_distance">Расстояние редактирования</Term>:** `dp[i-1][j-1]` при совпадении, иначе `1 + min(удаление, вставка, замена)`, база `dp[i][0]=i`, `dp[0][j]=j`.

**Лишняя строка и столбец** держат базовый случай пустого префикса чтобы у граничной рекуррентности был валидный сосед; символ строки `i` сидит в строке таблицы `i+1`.

**Сложность:** **O(m·n)** по времени (состояния умножить на O(1) работы). Память **O(m·n)**, ужимаемая до **O(min(m,n))** <Term slug="rolling_array">скользящим массивом</Term> потому что каждая клетка зависит только от текущей и предыдущей строки — но держи полную таблицу если тебе нужно восстановить сам путь.

Следующий блок выходит за пределы сеток и строк к более общим формам ДП построенным на той же дисциплине состояние-рекуррентность-база-заполнить-прочитать.
</Recap>
