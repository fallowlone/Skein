---
concepts:
- memoization-bug
- state-design
- transition
- space-optimization
- complexity
estMin: 14
lang: ru
level: middle
order: 9
prereqs: []
slug: quiz-code
sources:
- https://en.wikipedia.org/wiki/Dynamic_programming
- https://cp-algorithms.com/dynamic_programming/intro-to-dp.html
- https://en.wikipedia.org/wiki/Knapsack_problem
status: ready
summary: "Читаем реальные DP-сниппеты: найдите баг кэша, неверный state, небезопасную оптимизацию памяти и настоящую сложность — диагноз, который senior ставит до доверия любой DP."
title: 'Dynamic programming: чтение кода'
track: algorithms
unit: 10-dynamic-programming
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Баги в DP молчаливы — они возвращают уверенно неверное число, а не падение. Прочитайте каждый сниппет, предскажите, что он на самом деле считает, и выберите исправление, которое senior сделает первым.
</Hook>

<Goal lang="ru">
Отработайте цикл, который вы запускаете на каждой DP: проверить, что ключ кэша совпадает с полным state, что transition покрывает каждый выбор, что оптимизация памяти не разрушает ещё нужное значение, и считать настоящую сложность по циклам.
</Goal>

## Сниппет 1 — memo, который не попадает

```python
def lcs(a, b):
    memo = {}
    def go(i, j):
        if i == len(a) or j == len(b):
            return 0
        if a in memo:                 # баг: ключ не тот
            return memo[a]
        if a[i] == b[j]:
            res = 1 + go(i + 1, j + 1)
        else:
            res = max(go(i + 1, j), go(i, j + 1))
        memo[a] = res
        return res
    return go(0, 0)
```

<Quiz
  id="10-dynamic-programming-code-1"
  lessonSlug="10-dynamic-programming"
  lang="ru"
  question="Эта memoization LCS одновременно неверна и медленна. В чём дефект и как исправить?"
  choices={[
    { label: "Кэш ключуется по строке a, а не по state подзадачи (i, j). Поэтому он возвращает первое посчитанное значение для каждого вызова и не отражает реальную позицию. Исправление: ключуйте по (i, j) — memo[(i, j)] — по переменным, от которых subproblem реально зависит.", correct: true },
    { label: "Базовый случай должен возвращать 1, а не 0.", misconception: "Пустая subsequence имеет длину 0; возврат 1 завысил бы счёт. Базовый случай верен — дефект в ключе кэша." },
    { label: "max(go(i+1, j), go(i, j+1)) должно быть суммой, а не max.", misconception: "LCS берёт лучший из двух вариантов пропуска, так что max верен. Сумма задвоила бы пересекающиеся subsequence. Баг — в ключе кэша." },
    { label: "Рекурсия переполнит стек; перепишите итеративно.", misconception: "Глубина тут не больше len(a)+len(b), что нормально для обычных входов. Настоящий баг — ключевание кэша по a вместо (i, j)." },
  ]}
/>

## Сниппет 2 — state в coin-change

```python
def min_coins(coins, amount):
    # одна монета на номинал, без повторов
    dp = [0] + [float('inf')] * amount
    for c in coins:
        for x in range(amount, c - 1, -1):   # ёмкость вниз
            dp[x] = min(dp[x], dp[x - c] + 1)
    return dp[amount] if dp[amount] != float('inf') else -1
```

<Quiz
  id="10-dynamic-programming-code-2"
  lessonSlug="10-dynamic-programming"
  lang="ru"
  question="Автор хочет НЕОГРАНИЧЕННОЕ использование каждой монеты (классический coin change). Делает ли это 1D-код, и что задаёт направление обхода?"
  choices={[
    { label: "Нет — обход ёмкости ВНИЗ это форма 0/1, где каждая монета используется не более раза. Для неограниченного повтора внутренний цикл должен идти ВВЕРХ (range(c, amount+1)), чтобы dp[x-c] уже мог учитывать монету c из этого же прохода.", correct: true },
    { label: "Да, это корректный unbounded coin change — направление в 1D не важно.", misconception: "Направление именно и разделяет два варианта. Вниз (0/1) запрещает повтор монеты в одном проходе; вверх (unbounded) разрешает. Здесь обход вниз даёт неверный ответ «не более раза»." },
    { label: "Нет — баг в базовом случае; dp[0] должно быть inf.", misconception: "dp[0]=0 верно: нулём монет набирают сумму 0. Дефект — обход вниз, который тихо ограничивает каждую монету одним использованием." },
    { label: "Нет — монеты нужно сначала отсортировать.", misconception: "Порядок монет не влияет на корректность этой DP. Дефект — направление цикла, выбирающее семантику 0/1 вместо unbounded." },
  ]}
/>

## Сниппет 3 — оптимизация скользящей строки

```python
def edit_distance(a, b):
    n, m = len(a), len(b)
    prev = list(range(m + 1))          # строка для i = 0
    for i in range(1, n + 1):
        cur = [i] + [0] * m            # cur[0] = i
        for j in range(1, m + 1):
            if a[i - 1] == b[j - 1]:
                cur[j] = prev[j - 1]
            else:
                cur[j] = 1 + min(prev[j], cur[j - 1], prev[j - 1])
        prev = cur
    return prev[m]
```

<Quiz
  id="10-dynamic-programming-code-3"
  lessonSlug="10-dynamic-programming"
  lang="ru"
  question="Этот код edit distance сворачивает 2D-таблицу до двух строк. Корректна ли оптимизация памяти и какая зависимость делает её безопасной?"
  choices={[
    { label: "Корректна. Каждая ячейка dp[i][j] зависит только от dp[i-1][j], dp[i][j-1], dp[i-1][j-1] — сверху, слева и по диагонали. Хранение предыдущей строки (prev) плюс текущей (cur) сохраняет ровно эти три, так что O(n*m) памяти падает до O(m).", correct: true },
    { label: "Неверно — edit distance нельзя сжать ниже полной 2D-таблицы, ведь нужны все строки.", misconception: "Каждая ячейка читает лишь предыдущую строку и левого соседа текущей, так что двух строк хватает. Полная таблица нужна, только если надо восстановить сам сценарий правок." },
    { label: "Неверно — перезапись cur[j-1] разрушает prev[j-1] до чтения.", misconception: "cur и prev — отдельные массивы, запись cur[j-1] не трогает prev[j-1]. Диагональ prev[j-1] цела для следующего столбца. Оптимизация безопасна." },
    { label: "Корректна, но меняет ответ, ведь диагональ теперь приблизительна.", misconception: "Ничего не приближается; три чтения (prev[j], cur[j-1], prev[j-1]) — это ровно те же значения, что в полной таблице. Тот же ответ, меньше памяти." },
  ]}
/>

## Сниппет 4 — циклы interval DP

```python
def interval_cost(n, weight):
    dp = [[0] * n for _ in range(n)]
    for length in range(2, n + 1):                # длина интервала
        for i in range(0, n - length + 1):        # левый конец
            j = i + length - 1                    # правый конец
            best = float('inf')
            for k in range(i, j):                 # точка разбиения
                best = min(best, dp[i][k] + dp[k + 1][j] + weight(i, k, j))
            dp[i][j] = best
    return dp[0][n - 1]
```

<Quiz
  id="10-dynamic-programming-code-4"
  lessonSlug="10-dynamic-programming"
  lang="ru"
  question="Какова временная и пространственная сложность этой interval DP и откуда берётся доминирующая стоимость?"
  choices={[
    { label: "O(n^3) по времени, O(n^2) по памяти. Интервалов (i, j) порядка O(n^2), и каждый перебирает до O(n) точек разбиения k, так что три вложенных цикла дают n^3; таблица dp размером n на n, отсюда n^2 памяти.", correct: true },
    { label: "O(n^2) по времени, O(n^2) по памяти — два цикла по таблице.", misconception: "Вложенных циклов три: длина, левый конец и точка разбиения k. Внутренний перебор разбиений и поднимает до O(n^3), а не O(n^2)." },
    { label: "O(n^3) по времени, O(n^3) по памяти.", misconception: "Время O(n^3), но память — лишь таблица dp размером n на n, то есть O(n^2). Третьего измерения не хранится." },
    { label: "O(2^n) по времени, ведь каждое разбиение пробуется рекурсивно.", misconception: "Разбиения пробуются, но каждый (i, j) считается один раз и кэшируется в dp, так что это полином. Экспонента — это наивная рекурсия без таблицы." },
  ]}
/>

<Recap lang="ru">
Четыре проверки ловят большинство дефектов DP: ключ кэша должен равняться полному state подзадачи (Сниппет 1 ключевал по неверной переменной); transition и направление цикла должны совпадать с задуманной семантикой (Сниппет 2 — вниз это 0/1, вверх это unbounded); сжатие до скользящей строки безопасно, лишь когда ячейка читает только предыдущую строку и соседей текущей (Сниппет 3); а настоящая сложность — произведение границ циклов: O(n^2) state на O(n) работы на state даёт O(n^3) (Сниппет 4). Читайте циклы, а не комментарий.
</Recap>
