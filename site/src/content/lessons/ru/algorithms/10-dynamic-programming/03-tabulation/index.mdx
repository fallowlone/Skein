---
concepts:
- tabulation
- bottom_up_dp
- dp_array
- base_case
- transition
- recurrence
- iteration_order
- rolling_array
- space_optimization
- fibonacci
- climbing_stairs
estMin: 25
lang: ru
mathPrereqs: []
order: 3
prereqs:
- 02-memoization
slug: 03-tabulation
sources:
- https://en.wikipedia.org/wiki/Dynamic_programming
- https://www.geeksforgeeks.org/dsa/tabulation-vs-memoization/
- https://cp-algorithms.com/dynamic_programming/intro-to-dp.html
status: ready
summary: 'Табуляция это ДП снизу вверх: построй массив dp, заполни базовые случаи,
  потом иди циклом от малых состояний к цели так чтобы каждая ячейка читала уже заполненные.
  Без рекурсии, без стека. То же O(n) время что и мемоизация, а скользящее окно роняет
  fib до O(1) памяти.'
title: 'Табуляция: восходящее ДП, таблица'
track: algorithms
unit: 10-dynamic-programming
---



import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Idea from "~/components/lesson/Idea.astro";
import Code from "~/components/lesson/Code.astro";
import Trace from "~/components/lesson/Trace.astro";
import Complexity from "~/components/lesson/Complexity.astro";
import Check from "~/components/lesson/Check.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import AnnotatedCode from "~/components/algo/AnnotatedCode.astro";
import AlgoTrace from "~/components/algo/AlgoTrace.astro";
import CodeRunner from "~/components/algo/CodeRunner.astro";
import AlgoPractice from "~/components/algo/AlgoPractice.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";
import Term from "~/components/prose/Term.astro";
import DiagramFrame from "~/components/diagram/DiagramFrame.astro";

<Hook>
В прошлом уроке мемоизация вычисляла каждую подзадачу Фибоначчи один раз, кэшируя рекурсию. Это работает, но всё ещё рекурсирует: fib(n) спускается вызовами до fib(0) прежде чем хоть что-то вернётся, и эта цепочка незавершённых вызовов это стек который может стать достаточно глубоким чтобы рухнуть. Теперь переверни всё с ног на голову. Вместо того чтобы начать от цели и рекурсировать *вниз*, начни от базовых случаев и строй *вверх*. Разложи ряд коробок — одна на подзадачу. Запиши два ответа которые ты уже знаешь, fib(0) и fib(1). Потом пройди ряд слева направо обычным циклом, заполняя каждую коробку из двух предыдущих. Без рекурсии, без стека, и когда ты дойдёшь до последней коробки у тебя есть ответ. Это <Term slug="tabulation">табуляция</Term>: восходящее ДП, таблица.
</Hook>

<Goal lang="ru">
После этого урока ты можешь реализовать динамическое программирование снизу вверх с табуляцией. Ты можешь назвать четыре части которые нужны каждому табулированному ДП: <Term slug="dp_array">массив dp</Term> и что значит каждая его ячейка, базовые случаи которые его засевают, переход (рекуррента в форме массива), и порядок обхода который гарантирует что каждая ячейка заполнена прежде чем что-либо её читает. Ты можешь преобразовать мемоизированное (нисходящее) решение в табулированное (восходящее) и для Фибоначчи, и для подъёма по лестнице. Ты можешь противопоставить два направления: табуляция не платит за стек рекурсии и работает в предсказуемом порядке, но она вычисляет каждое состояние даже те которые нисходящий запуск пропустил бы; мемоизация вычисляет только достижимые состояния но платит за стек и хеширование. Ты можешь применить оптимизацию памяти: когда ячейка зависит только от последних нескольких ячеек, держи только их в <Term slug="rolling_array">скользящем окне</Term> и выбрось всю таблицу, уменьшив память Фибоначчи с O(n) до O(1). Это закрывает основы ДП; более поздние уроки применяют таблицу к более сложным рекуррентам.
</Goal>

<Idea lang="ru">
**Что это табуляция?**

<Term slug="tabulation">Табуляция</Term> это восходящий способ писать динамическое программирование. Вместо рекурсии что начинает от цели и спускается к базовым случаям, ты создаёшь таблицу — обычно массив называемый `dp` — заполняешь базовые случаи напрямую, потом запускаешь цикл который заполняет остаток таблицы в порядке где каждая ячейка которую ты вычисляешь читает только ячейки которые уже заполнены. Когда цикл заканчивается, ответ сидит в ячейке для цели. Нет рекурсии и нет стека вызовов.

**Четыре части каждого табулированного ДП**

Табулированное решение это всегда те же четыре решения. Сделай их правильно и код пишется сам:

1. **Массив dp и его значение.** Реши что `dp[i]` *обозначает*. Для Фибоначчи `dp[i]` это "i-е число Фибоначчи". Записать это значение одним предложением это самый важный шаг — всё остальное следует из него.
2. **Базовые случаи.** Ячейки которые ты можешь заполнить без вычислений. Для Фибоначчи: `dp[0] = 0` и `dp[1] = 1`.
3. **Переход (рекуррента).** Как вычислить небазовую ячейку из более ранних ячеек. Для Фибоначчи: `dp[i] = dp[i - 1] + dp[i - 2]`. Это ровно та же рекуррента что использовала рекурсия, только читающая массив вместо совершения вызовов.
4. **Порядок обхода.** Порядок заполнения ячеек так чтобы когда ты вычисляешь `dp[i]`, каждая ячейка от которой она зависит уже готова. Для Фибоначчи `dp[i]` нуждается в `dp[i-1]` и `dp[i-2]`, так что заполнение слева направо (i от 2 до n) гарантирует что обе готовы.

**Почему порядок обхода это весь фокус**

Мемоизация никогда не беспокоилась о порядке — рекурсия обнаруживала порядок по запросу. У табуляции нет рекурсии чтобы что-либо обнаруживать, так что *ты* должен выбрать порядок который уважает зависимости. Правило простое: ячейка может зависеть только от ячеек заполненных *раньше* в цикле. Для Фибоначчи зависимость указывает назад (`i` нуждается в `i-1` и `i-2`), так что движение вперёд от базовых случаев её удовлетворяет. Если бы ты ошибся с порядком — скажем попробовал заполнить `dp[i]` прежде чем `dp[i-1]` существовала — ты прочитал бы пустую ячейку и получил мусор.

**Табуляция как та же рекуррента, запущенная вперёд**

Мемоизация и табуляция решают идентичную рекуррену с идентичными базовыми случаями. Единственная разница это направление:

```
memoization (top-down):  start at fib(n), recurse DOWN to fib(0), fill cache on the way back up
tabulation  (bottom-up): start at fib(0), loop UP to fib(n), fill the table left to right
```

**Смотри как таблица заполняется, слева направо**

Для fib `dp` начинается с двух базовых случаев, и каждый шаг добавляет сумму двух ячеек слева от него. "?" отмечает ячейки ещё не вычисленные:

```
seed bases:  dp = [0, 1, ?, ?, ?, ?, ?, ?]
i = 2:       dp[2] = dp[1] + dp[0] = 1     -> [0, 1, 1, ?, ?, ?, ?, ?]
i = 3:       dp[3] = dp[2] + dp[1] = 2     -> [0, 1, 1, 2, ?, ?, ?, ?]
i = 4:       dp[4] = dp[3] + dp[2] = 3     -> [0, 1, 1, 2, 3, ?, ?, ?]
i = 5:       dp[5] = dp[4] + dp[3] = 5     -> [0, 1, 1, 2, 3, 5, ?, ?]
i = 6:       dp[6] = dp[5] + dp[4] = 8     -> [0, 1, 1, 2, 3, 5, 8, ?]
i = 7:       dp[7] = dp[6] + dp[5] = 13    -> [0, 1, 1, 2, 3, 5, 8, 13]
answer:      dp[7] = 13
```

Каждая ячейка вычисляется ровно раз, в одном чистом проходе слева направо. Ни одна ячейка никогда не читается прежде чем записана, потому что порядок цикла совпадает с направлением зависимости.

**Цена движения снизу вверх**

Табуляция вычисляет *каждую* ячейку от базовых случаев до цели, нужна она нисходящему запуску или нет. Для плотной задачи как Фибоначчи это не потеря — каждая ячейка всё равно нужна. Но если бы достижимые состояния были редкими (вообрази рекурсию которая когда-либо касается лишь разбросанной горстки возможных состояний), мемоизация вычислила бы только их, тогда как табуляция заполнила бы весь диапазон. Это компромисс который ты принимаешь в обмен на избавление от стека рекурсии.

</Idea>

<DiagramFrame label="Одномерный массив dp для Фибоначчи, нарисованный как ряд из восьми ячеек с индексами от 0 до 7. Две левые ячейки это базовые случаи, засеянные напрямую без вычислений: dp[0] = 0 и dp[1] = 1. Остальная таблица заполняется слева направо обычным циклом, без рекурсии. Диаграмма замораживает цикл на i = 5: dp[5] это текущая ячейка, и две стрелки указывают в неё из двух ячеек прямо слева от неё, dp[4] = 3 и dp[3] = 2, показывая переход dp[5] = dp[4] + dp[3] = 3 + 2 = 5. Ячейки 0..4 уже заполнены; ячейки 6 и 7 ещё пусты, показаны вопросительными знаками, ждут своей очереди позже в том же проходе слева направо." hue="var(--d-systems)" caption="Восходящая табуляция: засей базовые случаи dp[0] и dp[1], потом заполняй слева направо циклом. Каждая ячейка dp[i] читает две уже заполненные ячейки слева от неё — dp[i-1] + dp[i-2] — так что ответ строится итеративно от базовых случаев, без рекурсии и без стека.">
  <svg viewBox="0 0 720 240" width="720" height="240" style="display:block;margin:0 auto;max-width:100%;height:auto;">
    <text x="360" y="26" text-anchor="middle" font-size="13" font-family="var(--font-body)" fill="var(--ink)">{"dp[i] = dp[i-1] + dp[i-2]"}  —  строй таблицу итеративно от базовых случаев, без рекурсии</text>
    <path d="M372 116 Q400 78 432 116" fill="none" stroke="var(--d-systems)" stroke-width="2" stroke-dasharray="5 4" marker-end="url(#tabArrow)" />
    <path d="M300 116 Q372 60 444 116" fill="none" stroke="var(--d-systems)" stroke-width="2" stroke-dasharray="5 4" marker-end="url(#tabArrow)" />
    <rect x="48" y="118" width="72" height="52" rx="6" fill="var(--card-2)" stroke="var(--hairline-strong)" stroke-width="1.2" />
    <text x="84" y="150" text-anchor="middle" font-size="16" font-family="var(--font-body)" fill="var(--ink)">0</text>
    <text x="84" y="188" text-anchor="middle" font-size="11" font-family="var(--font-body)" fill="var(--muted)">i=0</text>
    <text x="84" y="106" text-anchor="middle" font-size="10" font-family="var(--font-body)" fill="var(--d-systems)">база</text>
    <rect x="120" y="118" width="72" height="52" rx="6" fill="var(--card-2)" stroke="var(--hairline-strong)" stroke-width="1.2" />
    <text x="156" y="150" text-anchor="middle" font-size="16" font-family="var(--font-body)" fill="var(--ink)">1</text>
    <text x="156" y="188" text-anchor="middle" font-size="11" font-family="var(--font-body)" fill="var(--muted)">i=1</text>
    <text x="156" y="106" text-anchor="middle" font-size="10" font-family="var(--font-body)" fill="var(--d-systems)">база</text>
    <rect x="192" y="118" width="72" height="52" rx="6" fill="var(--card-2)" stroke="var(--hairline-strong)" stroke-width="1.2" />
    <text x="228" y="150" text-anchor="middle" font-size="16" font-family="var(--font-body)" fill="var(--ink)">1</text>
    <text x="228" y="188" text-anchor="middle" font-size="11" font-family="var(--font-body)" fill="var(--muted)">i=2</text>
    <rect x="264" y="118" width="72" height="52" rx="6" fill="var(--card-2)" stroke="var(--d-systems)" stroke-width="1.6" />
    <text x="300" y="150" text-anchor="middle" font-size="16" font-family="var(--font-body)" fill="var(--ink)">2</text>
    <text x="300" y="188" text-anchor="middle" font-size="11" font-family="var(--font-body)" fill="var(--muted)">i=3</text>
    <text x="300" y="106" text-anchor="middle" font-size="10" font-family="var(--font-body)" fill="var(--d-systems)">dp[i-2]</text>
    <rect x="336" y="118" width="72" height="52" rx="6" fill="var(--card-2)" stroke="var(--d-systems)" stroke-width="1.6" />
    <text x="372" y="150" text-anchor="middle" font-size="16" font-family="var(--font-body)" fill="var(--ink)">3</text>
    <text x="372" y="188" text-anchor="middle" font-size="11" font-family="var(--font-body)" fill="var(--muted)">i=4</text>
    <text x="372" y="106" text-anchor="middle" font-size="10" font-family="var(--font-body)" fill="var(--d-systems)">dp[i-1]</text>
    <rect x="408" y="114" width="72" height="60" rx="6" fill="var(--accent-ghost)" stroke="var(--d-systems)" stroke-width="2.4" />
    <text x="444" y="150" text-anchor="middle" font-size="16" font-family="var(--font-body)" fill="var(--ink)">5</text>
    <text x="444" y="188" text-anchor="middle" font-size="11" font-family="var(--font-body)" fill="var(--d-systems)">i=5</text>
    <text x="444" y="208" text-anchor="middle" font-size="10" font-family="var(--font-body)" fill="var(--d-systems)">3 + 2 = 5</text>
    <rect x="480" y="118" width="72" height="52" rx="6" fill="var(--card)" stroke="var(--hairline-2)" stroke-width="1" stroke-dasharray="4 4" />
    <text x="516" y="152" text-anchor="middle" font-size="16" font-family="var(--font-body)" fill="var(--muted)">?</text>
    <text x="516" y="188" text-anchor="middle" font-size="11" font-family="var(--font-body)" fill="var(--muted)">i=6</text>
    <rect x="552" y="118" width="72" height="52" rx="6" fill="var(--card)" stroke="var(--hairline-2)" stroke-width="1" stroke-dasharray="4 4" />
    <text x="588" y="152" text-anchor="middle" font-size="16" font-family="var(--font-body)" fill="var(--muted)">?</text>
    <text x="588" y="188" text-anchor="middle" font-size="11" font-family="var(--font-body)" fill="var(--muted)">i=7</text>
    <line x1="48" y1="222" x2="624" y2="222" stroke="var(--hairline-strong)" stroke-width="1.4" marker-end="url(#tabAxis)" />
    <text x="48" y="216" text-anchor="start" font-size="10" font-family="var(--font-body)" fill="var(--muted)">базовые случаи</text>
    <text x="624" y="216" text-anchor="end" font-size="10" font-family="var(--font-body)" fill="var(--muted)">порядок заполнения: слева направо</text>
    <defs>
      <marker id="tabArrow" markerWidth="9" markerHeight="9" refX="6" refY="4" orient="auto"><path d="M1 1 L7 4 L1 7" fill="none" stroke="var(--d-systems)" stroke-width="1.6" /></marker>
      <marker id="tabAxis" markerWidth="9" markerHeight="9" refX="6" refY="4" orient="auto"><path d="M1 1 L7 4 L1 7" fill="none" stroke="var(--hairline-strong)" stroke-width="1.4" /></marker>
    </defs>
  </svg>
</DiagramFrame>

<Code lang="ru">
**Табулированный Фибоначчи (снизу вверх)**

Это та же рекуррента и базовые случаи что и в мемоизированной версии из урока 02, но управляемая циклом по массиву вместо рекурсии по кэшу.

<AnnotatedCode
  code={`function fib(n) {
  // Base cases handled directly
  if (n <= 1) {
    return n;
  }
  // dp[i] means "the i-th Fibonacci number"
  const dp = new Array(n + 1);
  dp[0] = 0;                       // base case
  dp[1] = 1;                       // base case
  // Fill left to right: every cell reads two already-filled cells
  for (let i = 2; i <= n; i++) {
    dp[i] = dp[i - 1] + dp[i - 2]; // the transition (recurrence)
  }
  // The goal is the last cell
  return dp[n];
}`}
  annotations={[
    { line: 3, text: "Охрани крошечные входы так чтобы dp[1] всегда был валидным индексом когда мы строим массив." },
    { line: 7, text: "Массив dp: одна ячейка на подзадачу, индексы 0..n. dp[i] = i-е число Фибоначчи." },
    { line: 8, text: "Базовый случай dp[0] = 0, заполнен без вычислений." },
    { line: 9, text: "Базовый случай dp[1] = 1, заполнен без вычислений." },
    { line: 11, text: "Порядок обхода: i идёт от 2 до n, так что dp[i-1] и dp[i-2] всегда уже заполнены." },
    { line: 12, text: "Переход: каждая ячейка это сумма двух ячеек слева от неё. Та же рекуррента что у рекурсии, читающая массив." },
    { line: 15, text: "После цикла dp[n] держит ответ. Без рекурсии, без стека." }
  ]}
/>

**Ключевой момент: нет стека рекурсии**

Вся функция это один цикл. Ничто не вызывает само себя, так что нет цепочки незавершённых вызовов и нет стека вызовов чтобы расти. Для очень большого `n` мемоизированная версия могла переполнить стек на своём первом глубоком спуске; табулированная версия никогда не может — она просто итерирует.

**Запуск: заполненная таблица и ответы**

<CodeRunner
  id="cr-fib-tab"
  lang="ru"
  code={`// Tabulated Fibonacci (bottom-up)
function fib(n) {
  if (n <= 1) return n;
  const dp = new Array(n + 1);
  dp[0] = 0;                       // base case
  dp[1] = 1;                       // base case
  for (let i = 2; i <= n; i++) {   // fill from base cases up to n
    dp[i] = dp[i - 1] + dp[i - 2]; // transition: each cell from the two below it
  }
  return dp[n];
}

// Print the whole filled table for n = 7
const n = 7;
const dp = [0, 1];
for (let i = 2; i <= n; i++) dp[i] = dp[i - 1] + dp[i - 2];
console.log("dp table for fib(7):");
console.log(dp.join(" "));
console.log("fib(7)  =", fib(7));
console.log("fib(10) =", fib(10));
console.log("fib(20) =", fib(20));`}
  expected={`dp table for fib(7):
0 1 1 2 3 5 8 13
fib(7)  = 13
fib(10) = 55
fib(20) = 6765`}
/>

Напечатанная таблица это ровно тот ряд который мы заполнили вручную. Цикл сделал тот же проход слева направо.

**Оптимизация памяти: скользящее окно**

Посмотри на переход снова: `dp[i] = dp[i - 1] + dp[i - 2]`. Чтобы вычислить любую ячейку, тебе когда-либо нужны только **последние две** ячейки. Как только `dp[i]` вычислена, `dp[i - 2]` больше никогда не читается. Так что хранить весь массив расточительно — ты можешь держать только два последних значения в двух переменных и сдвигать их вперёд. Это <Term slug="rolling_array">скользящее окно</Term> уменьшает память Фибоначчи с O(n) до **O(1)** сохраняя то же O(n) время.

<AnnotatedCode
  code={`function fib(n) {
  if (n <= 1) {
    return n;
  }
  // Keep only the two cells the transition needs
  let prev2 = 0;                 // plays the role of dp[i - 2], starts at fib(0)
  let prev1 = 1;                 // plays the role of dp[i - 1], starts at fib(1)
  for (let i = 2; i <= n; i++) {
    const curr = prev1 + prev2;  // the same transition, on the two kept values
    prev2 = prev1;               // slide the window forward
    prev1 = curr;
  }
  return prev1;                  // prev1 now holds fib(n)
}`}
  annotations={[
    { line: 6, text: "prev2 держит fib(i-2). Он начинается как fib(0) = 0." },
    { line: 7, text: "prev1 держит fib(i-1). Он начинается как fib(1) = 1." },
    { line: 9, text: "Тот же переход что в версии с массивом, но читающий две хранимые переменные." },
    { line: 10, text: "Сдвинь окно: старый prev1 становится новым prev2." },
    { line: 11, text: "А свежевычисленное значение становится новым prev1." },
    { line: 13, text: "После цикла prev1 это fib(n). Мы никогда не хранили всю таблицу: O(1) память." }
  ]}
/>

<CodeRunner
  id="cr-fib-rolling"
  lang="ru"
  code={`// Rolling-array Fibonacci: O(1) space, no full table
function fib(n) {
  if (n <= 1) return n;
  let prev2 = 0;                 // fib(i - 2), starts at fib(0)
  let prev1 = 1;                 // fib(i - 1), starts at fib(1)
  for (let i = 2; i <= n; i++) {
    const curr = prev1 + prev2;  // current cell from the two we kept
    prev2 = prev1;               // slide the window forward
    prev1 = curr;
  }
  return prev1;                  // prev1 now holds fib(n)
}

console.log("fib(10) =", fib(10));
console.log("fib(30) =", fib(30));
console.log("fib(50) =", fib(50));`}
  expected={`fib(10) = 55
fib(30) = 832040
fib(50) = 12586269025`}
/>

Те же ответы, один константный кусок памяти. Оптимизация памяти работает всякий раз когда переход дотягивается назад только на фиксированное число ячеек.

**Второй пример: подъём по лестнице, табулированный**

Рекуррента подъёма по лестнице из урока 02 это `ways(n) = ways(n-1) + ways(n-2)` — той же формы что Фибоначчи, с базовыми случаями `ways(1) = 1` и `ways(2) = 2`. Так что версия с таблицей это та же механика: массив dp, две базовые ячейки, переход, цикл слева направо. И поскольку переход снова дотягивается назад только на две ячейки, оптимизация скользящего окна применяется ровно как для fib.

<CodeRunner
  id="cr-stairs-tab"
  lang="ru"
  code={`// Climbing stairs, tabulated then space-optimized
function climbTab(n) {
  if (n <= 2) return n;
  const dp = new Array(n + 1);
  dp[1] = 1;                       // base: 1 way to climb 1 step
  dp[2] = 2;                       // base: 2 ways to climb 2 steps (1+1, 2)
  for (let i = 3; i <= n; i++) {
    dp[i] = dp[i - 1] + dp[i - 2]; // last move is a 1-step or a 2-step
  }
  return dp[n];
}

function climbRolling(n) {
  if (n <= 2) return n;
  let a = 1, b = 2;                // ways(1), ways(2)
  for (let i = 3; i <= n; i++) {
    const c = a + b;
    a = b;
    b = c;
  }
  return b;
}

console.log("table version :", climbTab(5), climbTab(10), climbTab(20));
console.log("rolling version:", climbRolling(5), climbRolling(10), climbRolling(20));`}
  expected={`table version : 8 89 10946
rolling version: 8 89 10946`}
/>

Обе версии согласны. Как только ты можешь назвать четыре части — массив, базовые случаи, переход, порядок — преобразование любого мемоизированного ДП в таблицу механично, а сжатие памяти это вопрос того насколько далеко назад дотягивается переход.
</Code>

<Trace lang="ru">
Давай протрассируем табулированный fib для n = 5 и посмотрим как ячейки заполняются слева направо. Массив dp начинается только с записанными базовыми случаями; каждый кадр вычисляет ещё одну ячейку из двух слева от неё. "?" отмечает ячейку ещё не заполненную.

<AlgoTrace
  id="trace-fib-tab"
  lang="ru"
  code={[
    "function fib(n) {",
    "  if (n <= 1) return n;",
    "  const dp = new Array(n + 1);",
    "  dp[0] = 0;",
    "  dp[1] = 1;",
    "  for (let i = 2; i <= n; i++) {",
    "    dp[i] = dp[i - 1] + dp[i - 2];",
    "  }",
    "  return dp[n];",
    "}"
  ]}
  frames={[
    { caption: "Засей базовые случаи: dp[0] = 0 и dp[1] = 1. Ячейки 2..5 пусты. Цикл вот-вот стартует с i = 2.", line: 5, cells: [{ value: "0", mark: "done" }, { value: "1", mark: "done" }, { value: "?" }, { value: "?" }, { value: "?" }, { value: "?" }] },
    { caption: "i = 2: dp[2] = dp[1] + dp[0] = 1 + 0 = 1. Оба входа уже были заполнены. Запиши dp[2].", line: 7, cells: [{ value: "0", mark: "done" }, { value: "1", mark: "done" }, { value: "1", mark: "active" }, { value: "?" }, { value: "?" }, { value: "?" }] },
    { caption: "i = 3: dp[3] = dp[2] + dp[1] = 1 + 1 = 2. Запиши dp[3].", line: 7, cells: [{ value: "0", mark: "done" }, { value: "1", mark: "done" }, { value: "1", mark: "done" }, { value: "2", mark: "active" }, { value: "?" }, { value: "?" }] },
    { caption: "i = 4: dp[4] = dp[3] + dp[2] = 2 + 1 = 3. Запиши dp[4].", line: 7, cells: [{ value: "0", mark: "done" }, { value: "1", mark: "done" }, { value: "1", mark: "done" }, { value: "2", mark: "done" }, { value: "3", mark: "active" }, { value: "?" }] },
    { caption: "i = 5: dp[5] = dp[4] + dp[3] = 3 + 2 = 5. Запиши dp[5]. Цикл заканчивается.", line: 7, cells: [{ value: "0", mark: "done" }, { value: "1", mark: "done" }, { value: "1", mark: "done" }, { value: "2", mark: "done" }, { value: "3", mark: "done" }, { value: "5", mark: "active" }] },
    { caption: "Верни dp[5] = 5. Каждая ячейка вычислена раз, в одном проходе слева направо, без рекурсии и без стека.", line: 9, cells: [{ value: "0", mark: "done" }, { value: "1", mark: "done" }, { value: "1", mark: "done" }, { value: "2", mark: "done" }, { value: "3", mark: "done" }, { value: "5", mark: "target" }] }
  ]}
/>
</Trace>

<Complexity lang="ru">
**Время: O(n)**

Цикл запускается раз на ячейку от `i = 2` до `i = n`, и каждая итерация делает O(1) работы (одно сложение, одно присваивание). Это n − 1 итераций константной работы, так что **O(n)**. Это в точности совпадает с мемоизацией, и по той же причине: общее правило ДП это

```
total time = (number of distinct states) x (work per state)
```

Для Фибоначчи это (n+1) состояний x O(1) работы = O(n), достигаешь ли ты состояний рекурсируя вниз или зацикливаясь вверх.

**Память: O(n) с полной таблицей, O(1) со скользящим окном**

Наивная табуляция хранит одну ячейку на состояние в массиве dp: **O(n)**. Но стек рекурсии за который платила мемоизация *исчез* — у цикла нет глубины вызовов — так что память табуляции это просто таблица, без дополнительного O(n) слагаемого за стек.

Ещё лучше: переход `dp[i] = dp[i-1] + dp[i-2]` дотягивается назад только на две ячейки, так что тебе никогда не нужен весь массив сразу. Держи два последних значения в двух переменных и память падает до **O(1)** тогда как время остаётся O(n). Это оптимизация скользящего окна, и она применяется к любому ДП чей переход зависит только от фиксированного числа недавних состояний.

```
                time      space (table)   space (rolling)   stack
memoization     O(n)      O(n) cache      —                 O(n)   <- last lesson
tabulation      O(n)      O(n) table      O(1)              none   <- this lesson
```

**Когда табуляция выигрывает, и когда нет**

Преимущества табуляции это отсутствие стоимости стека рекурсии (так нет риска переполнения стека для огромного n), предсказуемый фиксированный порядок обхода, и лёгкий путь к O(1) памяти когда переход дотягивается назад лишь немного. Её цена в том что она вычисляет *каждую* ячейку от базовых случаев до цели, даже ячейки которые нисходящий запуск пропустил бы. Для плотных задач как fib это ничего не стоит — каждая ячейка нужна. Для редких, разбросанных пространств состояний "вычисляй только то чего достигаешь" мемоизации может сделать строго меньше работы. Выбери направление которое подходит задаче; рекуррента и O(n) время те же в любом случае.
</Complexity>

<AlgoPractice
  id="practice-tabulation"
  lessonSlug="03-tabulation"
  lang="ru"
  problems={[
    {
      kind: "choice",
      prompt: "Что это определяющая черта табуляции по сравнению с мемоизацией?",
      choices: [
        "Она использует Map вместо массива для кэша",
        "Она заполняет таблицу итеративно от базовых случаев до цели, без рекурсии",
        "Она вычисляет только те состояния которых рекурсия реально достигла бы",
        "Она всегда использует больше памяти чем мемоизация"
      ],
      answer: 1,
      hint: "Табуляция это снизу вверх: засей базовые случаи, потом зацикливайся вверх заполняя каждую ячейку из более ранних. Нет рекурсии и нет стека вызовов."
    },
    {
      kind: "choice",
      prompt: "В табулированном ДП, почему порядок обхода имеет значение?",
      choices: [
        "Потому что массивы всегда должны заполняться от индекса 0 вверх",
        "Потому что каждая ячейка должна вычисляться до любой ячейки которая от неё не зависит",
        "Потому что каждая ячейка должна вычисляться только после того как каждая ячейка от которой она зависит уже заполнена",
        "Это не имеет значения; любой порядок даёт тот же результат"
      ],
      answer: 2,
      hint: "Нет рекурсии чтобы обнаружить порядок за тебя. Ты должен зацикливаться так чтобы когда ты вычисляешь dp[i], каждая ячейка которую она читает (как dp[i-1], dp[i-2]) уже заполнена."
    },
    {
      kind: "choice",
      prompt: "Для табулированного Фибоначчи, что это переход который заполняет небазовую ячейку dp[i]?",
      choices: [
        "dp[i] = dp[i - 1]",
        "dp[i] = dp[i - 1] + dp[i - 2]",
        "dp[i] = dp[i + 1] + dp[i + 2]",
        "dp[i] = dp[0] + dp[1]"
      ],
      answer: 1,
      hint: "Это та же рекуррента что использовала рекурсия: каждое число Фибоначчи это сумма двух перед ним. Читая массив, это dp[i] = dp[i-1] + dp[i-2]."
    },
    {
      kind: "choice",
      prompt: "Почему табуляция Фибоначчи может быть оптимизирована с O(n) памяти до O(1) памяти?",
      choices: [
        "Потому что ответ всегда достаточно мал чтобы поместиться в одну переменную",
        "Потому что переход дотягивается назад только на две ячейки, так что надо держать только два последних значения",
        "Потому что табуляция никогда не нуждается ни в какой памяти",
        "Потому что Map сжимает массив автоматически"
      ],
      answer: 1,
      hint: "dp[i] зависит только от dp[i-1] и dp[i-2]. Как только ячейка на два шага позади, она больше никогда не читается, так что две скользящие переменные заменяют весь массив."
    },
    {
      kind: "choice",
      prompt: "Какую стоимость табуляция избегает которую платит нисходящая мемоизация?",
      choices: [
        "Стоимость вычисления базовых случаев",
        "Стек вызовов рекурсии, который может достичь глубины O(n) и переполниться для большого n",
        "Стоимость самого перехода",
        "Необходимость хранить хоть какие-то ответы вообще"
      ],
      answer: 1,
      hint: "Табуляция это цикл, так что нет цепочки незавершённых вызовов. Мемоизация рекурсирует, так что она несёт стек O(n) который может переполниться для очень большого n."
    },
    {
      kind: "choice",
      prompt: "Что это один недостаток табуляции по сравнению с мемоизацией?",
      choices: [
        "Она даёт другой ответ для той же рекуррены",
        "Она вычисляет каждое состояние от базовых случаев до цели, даже те которые нисходящий запуск пропустил бы",
        "Она не может использовать базовые случаи",
        "Она всегда асимптотически медленнее"
      ],
      answer: 1,
      hint: "Цикл заполняет весь диапазон снизу вверх. Для плотных задач это нормально, но для редких пространств состояний 'вычисляй только то чего достигаешь' мемоизации может сделать меньше работы."
    },
    {
      kind: "numeric",
      prompt: "Табулированное ДП имеет 250 различных состояний и делает O(1) работы на состояние. Примерно сколько вычислений ячеек цикл выполняет всего?",
      answer: 250,
      hint: "Время = различные состояния x работа на состояние = 250 x 1 = 250. Цикл заполняет каждую ячейку ровно раз."
    }
  ]}
/>

<Inset kind="why" lang="ru">
**Зачем учить табуляцию если мемоизация уже работает?** Три причины. Первая, нет стека рекурсии: для очень большого `n` нисходящий запуск может переполнить стек вызовов, тогда как цикл никогда не может. Вторая, предсказуемость: фиксированный порядок обхода легко осмыслить и у него нет накладных расходов на вызов функции, так что он обычно немного быстрее на практике. Третья, и самая ценная, таблица делает оптимизацию памяти очевидной — как только ты видишь что переход дотягивается назад лишь на пару ячеек, скользящее окно которое роняет O(n) до O(1) почти пишет себя само. Частый рабочий поток это написать рекурсию, мемоизировать её чтобы получить корректное O(n) решение, потом табулировать когда тебе нужен меньший стек или меньшая память.
</Inset>

<Inset kind="mistake" lang="ru">
**Ошибка: заполнение таблицы в порядке который читает пустые ячейки.** У табуляции нет рекурсии чтобы обнаружить порядок, так что неверное направление цикла читает ячейки которые ещё не записаны. Если бы ты попробовал вычислить `dp[i]` прежде чем `dp[i-1]` существовала, ты прочитал бы `undefined` и произвёл `NaN`. Всегда проверяй направление зависимости сначала: `dp[i]` Фибоначчи зависит от меньших индексов, так что цикл должен идти *вперёд* от базовых случаев. Некоторые ДП зависят от больших индексов и должны зацикливаться назад. Подгони цикл под зависимости.
</Inset>

<Inset kind="edgecase" lang="ru">
**Крошечные входы и базовые случаи.** Охрани малые входы прежде чем строить массив. Для Фибоначчи `if (n <= 1) return n;` обрабатывает n = 0 и n = 1, так что запись `dp[1] = 1` никогда не индекс вне диапазона. Для подъёма по лестнице `if (n <= 2) return n;` покрывает две базовые ячейки. Пропуск этих охран это классический баг на единицу: построение массива длины `n+1` а потом предположение что `dp[1]` существует когда `n` это 0 пишет за пределы массива из одного элемента.
</Inset>

<Inset kind="practice" lang="ru">
**Рецепт для любого табулированного ДП.** (1) Определи `dp[i]` одним предложением — что значит ячейка? (2) Заполни базовые случаи напрямую, без вычислений. (3) Напиши переход: как небазовая ячейка строится из более ранних ячеек (это та же рекуррента что использовала рекурсия). (4) Выбери порядок обхода так чтобы каждая ячейка была заполнена прежде чем она прочитана — вперёд когда зависимости указывают на меньшие индексы. (5) Спроси насколько далеко назад дотягивается переход; если лишь на фиксированную горстку ячеек, замени массив скользящими переменными для O(1) памяти. Попробуй рецепт на подъёме по лестнице выше, потом на подъёме по лестнице с минимальной стоимостью.
</Inset>

<Check lang="ru">
  <Quiz
    id="quiz-tabulation"
    pieceSlug="03-tabulation"
    lang="ru"
    question="Объясни что это табуляция, четыре части которые ей нужны, почему порядок обхода имеет значение, как она сравнивается с мемоизацией, и как работает оптимизация скользящего окна."
    choices={[
      {
        label: "Табуляция это динамическое программирование написанное снизу вверх: построй массив dp где каждая ячейка это одна подзадача, заполни базовые случаи напрямую, потом зацикливайся от малых состояний к цели используя переход (рекуррента в форме массива), в порядке обхода который гарантирует что каждая ячейка заполнена прежде чем что-либо её читает. Нет рекурсии и нет стека вызовов. Она решает ту же рекуррену что мемоизация с тем же O(n) временем, но она не платит за стек (так нет переполнения для большого n) ценой вычисления каждого состояния в диапазоне даже тех которые нисходящий запуск пропустил бы. Когда переход дотягивается назад только на фиксированное число ячеек — две для Фибоначчи — ты можешь держать только их в скользящем окне и выбросить таблицу, уменьшив память с O(n) до O(1).",
        correct: true
      },
      {
        label: "Табуляция это просто мемоизация с кэшем переименованным в dp; она всё ещё рекурсирует от цели вниз к базовым случаям, так что она несёт тот же стек рекурсии O(n). Порядок обхода не имеет значения потому что рекурсия его обнаруживает. Она никогда не может быть оптимизирована по памяти ниже O(n) потому что вся таблица должна храниться.",
        misconception: "Табуляция не рекурсирует вообще — это цикл снизу вверх, так что нет стека. Поскольку нет рекурсии чтобы обнаружить порядок, порядок обхода это ровно то что ты должен сделать правильно. И когда переход дотягивается назад только на фиксированное число ячеек, скользящее окно роняет память до O(1)."
      },
      {
        label: "Табуляция вычисляет только те состояния которые реально достижимы от цели, ровно как мемоизация, и она всегда строго быстрее и использует строго меньше памяти чем нисходящая версия в каждом случае.",
        misconception: "Это мемоизация, не табуляция, вычисляет только достижимые состояния. Табуляция заполняет каждую ячейку от базовых случаев до цели, так что для редких пространств состояний она может сделать больше работы. Её выигрыш это избавление от стека рекурсии и лёгкий путь к O(1) памяти, а не быть строго лучше везде."
      },
      {
        label: "Табуляция заполняет таблицу в любом порядке какой тебе нравится, потому что каждая ячейка независима. Базовые случаи вычисляются циклом как любая другая ячейка, и ответ находится сканированием всего массива на максимальное значение.",
        misconception: "Ячейки не независимы — каждая зависит от более ранних ячеек, так что порядок должен уважать эти зависимости. Базовые случаи засеваются напрямую без вычислений, и ответ это просто ячейка цели (dp[n]), не максимум массива."
      }
    ]}
  />
</Check>

<Recap lang="ru">
<Term slug="tabulation">Табуляция</Term> это восходящий способ писать динамическое программирование: построй <Term slug="dp_array">массив dp</Term>, заполни базовые случаи, потом зацикливайся от малых состояний к цели. Без рекурсии, без стека вызовов.

**Четыре части:**
- **Массив dp и его значение** — реши одним предложением что `dp[i]` обозначает (для fib, "i-е число Фибоначчи").
- **Базовые случаи** — ячейки которые ты заполняешь без вычислений (`dp[0] = 0`, `dp[1] = 1`).
- **Переход (рекуррента)** — как небазовая ячейка строится из более ранних ячеек (`dp[i] = dp[i-1] + dp[i-2]`).
- **Порядок обхода** — заполняй ячейки так чтобы каждая ячейка была вычислена прежде чем что-либо её читает. Вперёд когда зависимости указывают на меньшие индексы.

**Снизу вверх против сверху вниз:** табуляция зацикливается вверх от базовых случаев; мемоизация рекурсирует вниз от цели. Та же рекуррента, то же **O(n)** время. Табуляция не платит за стек рекурсии (нет переполнения для большого n) но вычисляет каждое состояние в диапазоне, даже те которые нисходящий запуск пропустил бы.

**Оптимизация памяти (скользящее окно):** когда переход дотягивается назад только на фиксированное число ячеек, держи только их в переменных и выбрось таблицу. Для Фибоначчи это уменьшает память с **O(n)** до **O(1)** с тем же O(n) временем.

Это закрывает основы ДП: задача, рекуррента, кэш или таблица. Мемоизируй когда достижимо лишь редкое множество состояний или порядок трудно спланировать; табулируй, когда нужны отсутствие стека, предсказуемый порядок или O(1) память.
</Recap>
