---
concepts:
- longest_increasing_subsequence
- lis
- subsequence
- one_dimensional_dp
- tails_array
- patience_sorting
- binary_search
- lower_bound
- strictly_increasing
estMin: 30
lang: ru
mathPrereqs: []
order: 7
prereqs:
- 04-one-dimensional-dp
slug: 07-longest-increasing-subsequence
sources:
- https://en.wikipedia.org/wiki/Longest_increasing_subsequence
- https://en.wikipedia.org/wiki/Patience_sorting
- https://cp-algorithms.com/sequences/longest_increasing_subsequence.html
- https://leetcode.com/problems/longest-increasing-subsequence/
status: ready
summary: 'LIS это наибольшая строго возрастающая подпоследовательность (не подряд).
  ДП за O(n^2): dp[i] = 1 + max(dp[j]) по j<i с nums[j]<nums[i]. Приём за O(n log
  n) держит массив хвостов и бинарно ищет каждое значение; ответ это ДЛИНА массива
  хвостов, а не его содержимое.'
title: 'Наибольшая возрастающая подпоследовательность: ДП за O(n^2) и приём с массивом
  хвостов за O(n log n)'
track: algorithms
unit: 10-dynamic-programming
---



import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Idea from "~/components/lesson/Idea.astro";
import Code from "~/components/lesson/Code.astro";
import Trace from "~/components/lesson/Trace.astro";
import Complexity from "~/components/lesson/Complexity.astro";
import Check from "~/components/lesson/Check.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import AnnotatedCode from "~/components/algo/AnnotatedCode.astro";
import AlgoTrace from "~/components/algo/AlgoTrace.astro";
import CodeRunner from "~/components/algo/CodeRunner.astro";
import AlgoPractice from "~/components/algo/AlgoPractice.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";
import Term from "~/components/prose/Term.astro";
import DiagramFrame from "~/components/diagram/DiagramFrame.astro";

<Hook>
У тебя есть массив чисел и ты хочешь найти длину самой длинной цепочки которая продолжает расти — но числа не обязаны стоять рядом. Из `[10, 9, 2, 5, 3, 7, 101, 18]` ты можешь выбрать `2, 5, 7, 101` (или `2, 3, 7, 18`): четыре числа, каждое больше предыдущего, разбросанные по массиву. Это <Term slug="subsequence">подпоследовательность</Term>, не подряд идущий срез. Это задача о наибольшей возрастающей подпоследовательности (LIS). Естественное <Term slug="one_dimensional_dp">одномерное ДП</Term> решает её за O(n^2). Потом хитрый приём — держи крошечный массив "лучших хвостов" и бинарно ищи в нём — снижает стоимость до O(n log n). Подвох который сбивает всех с толку: этот массив хвостов даёт правильную *длину*, но сам по себе он не является корректной подпоследовательностью.
</Hook>

<Goal lang="ru">
После этого урока ты можешь точно определить LIS: наибольшую строго возрастающую <Term slug="subsequence">подпоследовательность</Term> массива, где элементы сохраняют относительный порядок, но не обязаны быть соседними. Ты можешь написать ДП за O(n^2) — `dp[i]` это длина наибольшей возрастающей подпоследовательности которая *заканчивается на* индексе `i`, вычисляемая как `1 + max(dp[j])` по всем `j < i` с `nums[j] < nums[i]`, а ответ это `max(dp)`. Ты можешь написать метод за O(n log n): держи массив `tails` где `tails[k]` это наименьшее возможное последнее значение возрастающей подпоследовательности длины `k+1`, и для каждого нового значения бинарно ищи первый хвост который `>= x` и перезаписывай его (или добавляй в конец). Ты понимаешь точно почему `tails.length` это длина LIS, тогда как сам `tails` *не* является настоящей подпоследовательностью. Ты можешь сформулировать обе сложности. Следующий урок продолжает арку ДП-на-последовательностях.
</Goal>

<Idea lang="ru">
**Что это наибольшая возрастающая подпоследовательность?**

<Term slug="subsequence">Подпоследовательность</Term> это то что ты получаешь удаляя ноль или больше элементов из массива без переупорядочивания остальных. Так `[2, 7, 18]` это подпоследовательность `[10, 9, 2, 5, 3, 7, 101, 18]`, но `[7, 2]` нет (неправильный порядок). <Term slug="longest_increasing_subsequence">Наибольшая возрастающая подпоследовательность (LIS)</Term> это самая длинная такая подпоследовательность чьи значения <Term slug="strictly_increasing">строго возрастают</Term> (каждый элемент строго больше предыдущего).

```
nums = [10, 9, 2, 5, 3, 7, 101, 18]
one LIS = 2, 5, 7, 101   (length 4)
another = 2, 3, 7, 18    (length 4)
```

Может быть несколько LIS одной длины; обычно нам нужна только длина. Заметь "подпоследовательность" (разбросанная, порядок сохранён) отличается от "подмассива/подстроки" (подряд). LIS **не** требует чтобы выбранные элементы были соседями.

**Подход 1: одномерное ДП за O(n^2)**

Определи состояние `dp[i]` = длина наибольшей возрастающей подпоследовательности которая **заканчивается ровно на индексе `i`**. Каждая возрастающая подпоследовательность где-то заканчивается, так ответ это `max(dp)` по всем `i`.

Как мы заполняем `dp[i]`? Подпоследовательность заканчивающаяся на `i` это некоторая более короткая подпоследовательность заканчивающаяся на более раннем индексе `j < i` с `nums[j] < nums[i]`, плюс приделанный `nums[i]`. Чтобы сделать её как можно длиннее, выбери лучший такой `j`:

```
dp[i] = 1 + max( dp[j] )   over all j < i with nums[j] < nums[i]
dp[i] = 1                  if no such j exists (nums[i] starts fresh)
answer = max(dp[0..n-1])
```

Каждый элемент сам по себе это подпоследовательность длины 1, так мы инициализируем каждый `dp[i] = 1`. Два вложенных цикла (`i` внешний, `j` внутренний) дают O(n^2).

**Подход 2: массив хвостов за O(n log n)**

ДП за O(n^2) перепроверяет все более ранние индексы для каждого `i`. Более быстрый метод заменяет этот перебор бинарным поиском, держа один маленький массив, `tails`:

> `tails[k]` = **наименьшее возможное последнее значение** любой возрастающей подпоследовательности длины `k + 1`.

Почему "наименьшее возможное"? Потому что меньший хвост легче расширить позже — он оставляет больше места будущему элементу быть больше него. Хранение минимального хвоста на каждую длину жадно ровно в нужном смысле. Длина `tails` в конце это длина LIS.

Для каждого значения `x` в `nums`, делай одно из двух:
- **Добавить в конец:** если `x` больше каждого хвоста (больше `tails[last]`), он расширяет самую длинную цепочку на один, так добавь его.
- **Заменить:** иначе, найди **первый** хвост который `>= x` и перезапиши его на `x`. Это говорит "подпоследовательность той длины может теперь заканчиваться меньшим значением `x`", что может только помочь будущим расширениям. Поиск этой позиции это <Term slug="binary_search">бинарный поиск</Term> (<Term slug="lower_bound">нижняя граница</Term>: первый индекс где `tails[idx] >= x`), потому что `tails` всегда отсортирован по возрастанию.

Это идея patience-сортировки (раскладывай карты на стопки, каждая стопка отсортирована; число стопок это длина LIS).

**Смотри как эволюционирует tails**

Прогони `nums = [10, 9, 2, 5, 3, 7, 101, 18]` через правило хвостов. Каждый шаг либо добавляет в конец (найдена новая самая длинная длина), либо заменяет первый хвост `>= x`:

```
x = 10  append            tails = [10]
x = 9   replace tails[0]  tails = [9]
x = 2   replace tails[0]  tails = [2]
x = 5   append            tails = [2, 5]
x = 3   replace tails[1]  tails = [2, 3]
x = 7   append            tails = [2, 3, 7]
x = 101 append            tails = [2, 3, 7, 101]
x = 18  replace tails[3]  tails = [2, 3, 7, 18]
```

Итоговый `tails = [2, 3, 7, 18]`, длина 4. Длина LIS равна **4**. (Здесь итоговый tails оказался настоящей подпоследовательностью, но это везение — смотри Inset о том почему это не гарантировано.)

**Почему `tails` всегда отсортирован?** Подпоследовательности длины `k` могут заканчиваться значением как минимум таким же маленьким как у длины `(k-1)` плюс ещё один шаг вверх, так более длинным цепочкам нужны бо́льшие минимальные хвосты. Инвариант "tails строго возрастает" держится всё время, что ровно и позволяет нам бинарно искать в нём.

</Idea>

<DiagramFrame
  label="ДП за O(n^2) для LIS на nums = 10, 9, 2, 5, 3, 7, 101, 18. Каждый элемент массива несёт значение dp под собой: dp[i] это длина наибольшей строго возрастающей подпоследовательности заканчивающейся на индексе i, и равна 1 плюс максимум dp[j] по каждому более раннему индексу j с nums[j] < nums[i]. Значения dp это 1, 1, 1, 2, 2, 3, 4, 4. Изогнутые стрелки над массивом связывают элемент с лучшим совместимым более ранним окончанием которое он расширяет: 2 расширяется до 5, 5 расширяется до 7, и 7 расширяется до 101, строя цепочку dp 1, 2, 3, 4. Более тонкая пунктирная стрелка от 3 к 7 отмечает совместимого-но-не-лучшего предшественника (3 < 7 даёт тоже только dp 3, так выбран 5). Выделенная цепочка 2, 5, 7, 101 это наибольшая возрастающая подпоследовательность, длина 4, что является максимальным значением dp."
  hue="var(--d-systems)"
  caption="ДП за O(n^2): dp[i] = длина наибольшей возрастающей подпоследовательности заканчивающейся на индексе i = 1 + max(dp[j]) по всем более ранним j с nums[j] < nums[i]. Каждый элемент расширяет лучшее совместимое более раннее окончание (сплошные стрелки выбирают предшественника с наибольшим dp; пунктирная стрелка это совместимая но более короткая альтернатива которая проигрывает в максимуме). Выделенная цепочка 2 -> 5 -> 7 -> 101 имеет dp 1, 2, 3, 4 — это LIS, длина 4 = max(dp)."
>
  <svg viewBox="0 0 760 300" width="760" height="300" xmlns="http://www.w3.org/2000/svg" font-family="var(--font-body, sans-serif)">
    <defs>
      <marker id="lis-arr-ru" markerWidth="9" markerHeight="9" refX="7" refY="4" orient="auto">
        <path d="M0,0 L8,4 L0,8 Z" fill="var(--d-systems)" />
      </marker>
      <marker id="lis-arr-mut-ru" markerWidth="9" markerHeight="9" refX="7" refY="4" orient="auto">
        <path d="M0,0 L8,4 L0,8 Z" fill="var(--muted, #888)" />
      </marker>
    </defs>

    <text x="20" y="116" font-size="12" font-weight="600" fill="var(--muted, #888)">idx</text>
    <text x="20" y="148" font-size="13" font-weight="700" fill="var(--ink, currentColor)">arr</text>
    <text x="20" y="218" font-size="13" font-weight="700" fill="var(--d-systems)">dp</text>

    <text x="72" y="116" text-anchor="middle" font-size="11" fill="var(--muted, #888)">0</text>
    <text x="160" y="116" text-anchor="middle" font-size="11" fill="var(--muted, #888)">1</text>
    <text x="248" y="116" text-anchor="middle" font-size="11" fill="var(--muted, #888)">2</text>
    <text x="336" y="116" text-anchor="middle" font-size="11" fill="var(--muted, #888)">3</text>
    <text x="424" y="116" text-anchor="middle" font-size="11" fill="var(--muted, #888)">4</text>
    <text x="512" y="116" text-anchor="middle" font-size="11" fill="var(--muted, #888)">5</text>
    <text x="600" y="116" text-anchor="middle" font-size="11" fill="var(--muted, #888)">6</text>
    <text x="688" y="116" text-anchor="middle" font-size="11" fill="var(--muted, #888)">7</text>

    <rect x="44" y="124" width="56" height="48" rx="6" fill="var(--card, #fff)" stroke="var(--hairline-2, #888)" stroke-width="1.5" />
    <text x="72" y="155" text-anchor="middle" font-size="18" font-weight="700" fill="var(--muted, #888)">10</text>
    <rect x="132" y="124" width="56" height="48" rx="6" fill="var(--card, #fff)" stroke="var(--hairline-2, #888)" stroke-width="1.5" />
    <text x="160" y="155" text-anchor="middle" font-size="18" font-weight="700" fill="var(--muted, #888)">9</text>
    <rect x="220" y="124" width="56" height="48" rx="6" fill="var(--card, #fff)" stroke="var(--d-systems)" stroke-width="3" />
    <text x="248" y="155" text-anchor="middle" font-size="18" font-weight="700" fill="var(--ink, currentColor)">2</text>
    <rect x="308" y="124" width="56" height="48" rx="6" fill="var(--card, #fff)" stroke="var(--d-systems)" stroke-width="3" />
    <text x="336" y="155" text-anchor="middle" font-size="18" font-weight="700" fill="var(--ink, currentColor)">5</text>
    <rect x="396" y="124" width="56" height="48" rx="6" fill="var(--card, #fff)" stroke="var(--hairline-2, #888)" stroke-width="1.5" />
    <text x="424" y="155" text-anchor="middle" font-size="18" font-weight="700" fill="var(--muted, #888)">3</text>
    <rect x="484" y="124" width="56" height="48" rx="6" fill="var(--card, #fff)" stroke="var(--d-systems)" stroke-width="3" />
    <text x="512" y="155" text-anchor="middle" font-size="18" font-weight="700" fill="var(--ink, currentColor)">7</text>
    <rect x="572" y="124" width="56" height="48" rx="6" fill="var(--card, #fff)" stroke="var(--d-systems)" stroke-width="3" />
    <text x="600" y="155" text-anchor="middle" font-size="17" font-weight="700" fill="var(--ink, currentColor)">101</text>
    <rect x="660" y="124" width="56" height="48" rx="6" fill="var(--card, #fff)" stroke="var(--hairline-2, #888)" stroke-width="1.5" />
    <text x="688" y="155" text-anchor="middle" font-size="18" font-weight="700" fill="var(--muted, #888)">18</text>

    <rect x="52" y="194" width="40" height="34" rx="6" fill="var(--card, #fff)" stroke="var(--hairline-2, #888)" stroke-width="1.5" />
    <text x="72" y="217" text-anchor="middle" font-size="16" font-weight="700" fill="var(--muted, #888)">1</text>
    <rect x="140" y="194" width="40" height="34" rx="6" fill="var(--card, #fff)" stroke="var(--hairline-2, #888)" stroke-width="1.5" />
    <text x="160" y="217" text-anchor="middle" font-size="16" font-weight="700" fill="var(--muted, #888)">1</text>
    <rect x="228" y="194" width="40" height="34" rx="6" fill="var(--card, #fff)" stroke="var(--d-systems)" stroke-width="3" />
    <text x="248" y="217" text-anchor="middle" font-size="16" font-weight="700" fill="var(--ink, currentColor)">1</text>
    <rect x="316" y="194" width="40" height="34" rx="6" fill="var(--card, #fff)" stroke="var(--d-systems)" stroke-width="3" />
    <text x="336" y="217" text-anchor="middle" font-size="16" font-weight="700" fill="var(--ink, currentColor)">2</text>
    <rect x="404" y="194" width="40" height="34" rx="6" fill="var(--card, #fff)" stroke="var(--hairline-2, #888)" stroke-width="1.5" />
    <text x="424" y="217" text-anchor="middle" font-size="16" font-weight="700" fill="var(--muted, #888)">2</text>
    <rect x="492" y="194" width="40" height="34" rx="6" fill="var(--card, #fff)" stroke="var(--d-systems)" stroke-width="3" />
    <text x="512" y="217" text-anchor="middle" font-size="16" font-weight="700" fill="var(--ink, currentColor)">3</text>
    <rect x="580" y="194" width="40" height="34" rx="6" fill="var(--card, #fff)" stroke="var(--d-systems)" stroke-width="3" />
    <text x="600" y="217" text-anchor="middle" font-size="16" font-weight="700" fill="var(--ink, currentColor)">4</text>
    <rect x="668" y="194" width="40" height="34" rx="6" fill="var(--card, #fff)" stroke="var(--hairline-2, #888)" stroke-width="1.5" />
    <text x="688" y="217" text-anchor="middle" font-size="16" font-weight="700" fill="var(--muted, #888)">4</text>

    <path d="M248,122 Q292,82 336,122" fill="none" stroke="var(--d-systems)" stroke-width="3" marker-end="url(#lis-arr-ru)" />
    <path d="M336,122 Q424,58 512,122" fill="none" stroke="var(--d-systems)" stroke-width="3" marker-end="url(#lis-arr-ru)" />
    <path d="M512,122 Q556,82 600,122" fill="none" stroke="var(--d-systems)" stroke-width="3" marker-end="url(#lis-arr-ru)" />

    <path d="M424,122 Q468,92 512,122" fill="none" stroke="var(--muted, #888)" stroke-width="1.5" stroke-dasharray="4 3" marker-end="url(#lis-arr-mut-ru)" />

    <text x="292" y="74" text-anchor="middle" font-size="11" font-weight="600" fill="var(--d-systems)">{"2 < 5"}</text>
    <text x="424" y="50" text-anchor="middle" font-size="11" font-weight="600" fill="var(--d-systems)">{"5 < 7 (лучший)"}</text>
    <text x="556" y="74" text-anchor="middle" font-size="11" font-weight="600" fill="var(--d-systems)">{"7 < 101"}</text>
    <text x="468" y="108" text-anchor="middle" font-size="10" fill="var(--muted, #888)">{"3 < 7, dp равно 3 - не лучше"}</text>

    <text x="380" y="262" text-anchor="middle" font-size="12" font-weight="700" fill="var(--ink, currentColor)">{"выделенная цепочка 2 -> 5 -> 7 -> 101  =  LIS, длина 4 = max(dp)"}</text>
    <text x="380" y="284" text-anchor="middle" font-size="11" fill="var(--muted, #888)">{"dp[i] = 1 + max(dp[j]) по j < i с arr[j] < arr[i]; каждый шаг расширяет лучшее совместимое более раннее окончание"}</text>
  </svg>
</DiagramFrame>

<Code lang="ru">
**ДП за O(n^2) (ясное и прямое)**

Состояние `dp[i]` = длина наибольшей возрастающей подпоследовательности заканчивающейся на `i`.

<AnnotatedCode
  code={`function lisN2(nums) {
  if (nums.length === 0) return 0;
  // dp[i] = length of LIS ending exactly at index i. Every element alone is length 1.
  const dp = new Array(nums.length).fill(1);

  for (let i = 1; i < nums.length; i++) {
    for (let j = 0; j < i; j++) {
      // Can we extend the subsequence ending at j by appending nums[i]?
      if (nums[j] < nums[i]) {
        dp[i] = Math.max(dp[i], dp[j] + 1);
      }
    }
  }
  // The LIS can end at any index, so take the best dp value.
  return Math.max(...dp);
}`}
  annotations={[
    { line: 2, text: "Пустой вход не имеет подпоследовательности: длина 0." },
    { line: 4, text: "Инициализируй каждый dp[i] единицей — один элемент это возрастающая подпоследовательность длины 1." },
    { line: 6, text: "Внешний цикл: вычисляй dp[i] для каждой позиции по очереди." },
    { line: 7, text: "Внутренний цикл: перебери все более ранние индексы j как кандидатов в предшественники." },
    { line: 9, text: "Только строго меньшее более раннее значение может стоять перед nums[i] в возрастающей цепочке." },
    { line: 10, text: "Возьми лучшего предшественника: dp[i] = 1 + max(dp[j]) по валидным j." },
    { line: 15, text: "Ответ это максимум dp[i] — LIS может заканчиваться где угодно." }
  ]}
/>

**Метод с массивом хвостов за O(n log n)**

Держи `tails`, где `tails[k]` это наименьший хвост любой возрастающей подпоследовательности длины `k+1`. Для каждого `x` бинарно ищи первый хвост `>= x` (нижняя граница). Добавь в конец если такого нет, иначе перезапиши тот хвост.

<AnnotatedCode
  code={`function lengthOfLIS(nums) {
  const tails = [];

  for (const x of nums) {
    // Binary search: find the first index where tails[idx] >= x (lower bound).
    let lo = 0, hi = tails.length;
    while (lo < hi) {
      const mid = lo + Math.floor((hi - lo) / 2);
      if (tails[mid] < x) {
        lo = mid + 1;   // tails[mid] too small - x belongs to the right
      } else {
        hi = mid;       // tails[mid] >= x - candidate, but keep looking left
      }
    }

    if (lo === tails.length) {
      tails.push(x);    // x beats every tail: a new longest length
    } else {
      tails[lo] = x;    // overwrite the first tail >= x with the smaller x
    }
  }

  return tails.length; // LENGTH is the LIS length (tails itself is NOT a subsequence)
}`}
  annotations={[
    { line: 2, text: "tails растёт до длины LIS; tails[k] это наименьшее возможное последнее значение цепочки длины (k+1)." },
    { line: 6, text: "Бинарный поиск нижней границы по отсортированному массиву tails — это шаг за O(log n)." },
    { line: 9, text: "tails[mid] < x значит x должен идти дальше вправо: подвинь lo за mid." },
    { line: 11, text: "tails[mid] >= x это кандидат на место замены: сожми hi до mid, никогда ниже ответа." },
    { line: 16, text: "lo == tails.length значит ни один хвост не был >= x, так x расширяет самую длинную цепочку: добавь его." },
    { line: 19, text: "Иначе перезапиши tails[lo] на x: та же длина, но теперь меньший хвост (легче расширить)." },
    { line: 23, text: "Верни только длину. Содержимое tails не гарантированно является настоящей подпоследовательностью." }
  ]}
/>

**Запусти оба и сравни**

Оба метода должны согласоваться по длине. Метод хвостов делает это за O(n log n).

<CodeRunner
  id="cr-lis"
  lang="ru"
  code={`function lengthOfLIS(nums) {
  const tails = [];
  for (const x of nums) {
    let lo = 0, hi = tails.length;
    while (lo < hi) {
      const mid = lo + Math.floor((hi - lo) / 2);
      if (tails[mid] < x) lo = mid + 1;
      else hi = mid;
    }
    if (lo === tails.length) tails.push(x);
    else tails[lo] = x;
  }
  return tails.length;
}

function lisN2(nums) {
  if (nums.length === 0) return 0;
  const dp = new Array(nums.length).fill(1);
  for (let i = 1; i < nums.length; i++) {
    for (let j = 0; j < i; j++) {
      if (nums[j] < nums[i]) dp[i] = Math.max(dp[i], dp[j] + 1);
    }
  }
  return Math.max(...dp);
}

const a = [10, 9, 2, 5, 3, 7, 101, 18];
console.log("nums:", a.join(", "));
console.log("LIS length (O(n log n)):", lengthOfLIS(a));
console.log("LIS length (O(n^2)) :", lisN2(a));

const b = [0, 1, 0, 3, 2, 3];
console.log("nums:", b.join(", "));
console.log("LIS length:", lengthOfLIS(b));

const c = [7, 7, 7, 7, 7];
console.log("nums:", c.join(", "));
console.log("LIS length:", lengthOfLIS(c));`}
  expected={`nums: 10, 9, 2, 5, 3, 7, 101, 18
LIS length (O(n log n)): 4
LIS length (O(n^2)) : 4
nums: 0, 1, 0, 3, 2, 3
LIS length: 4
nums: 7, 7, 7, 7, 7
LIS length: 1`}
/>

Массив из равных `[7, 7, 7, 7, 7]` возвращает 1: "строго возрастающая" отвергает равных соседей, так каждая 7 просто продолжает заменять `tails[0]` и длина никогда не вырастает дальше 1.
</Code>

<Trace lang="ru">
Смотри как массив хвостов заполняется для `nums = [10, 9, 2, 5, 3, 7, 101, 18]`. Каждый шаг это одно значение `x`: либо **добавление в конец** (теперь возможна более длинная цепочка), либо **замена** (первый хвост `>= x`, найденный бинарным поиском, становится меньшим `x`). Клетки показывают текущий `tails`.

<AlgoTrace
  id="trace-lis-tails"
  lang="ru"
  code={[
    "for (const x of nums) {",
    "  // lower bound: first idx with tails[idx] >= x",
    "  if (lo === tails.length) tails.push(x);  // append",
    "  else tails[lo] = x;                      // replace",
    "}"
  ]}
  frames={[
    { caption: "x = 10. tails пуст, ничего не >= 10, lo = 0 = длина. Добавь в конец. tails = [10].", line: 3, cells: [{ value: "10", mark: "active" }] },
    { caption: "x = 9. Первый хвост >= 9 это tails[0] = 10. Замени его на 9. tails = [9]. Та же длина, меньший хвост.", line: 4, cells: [{ value: "9", mark: "target" }] },
    { caption: "x = 2. Первый хвост >= 2 это tails[0] = 9. Замени на 2. tails = [2]. Минимальный хвост продолжает падать.", line: 4, cells: [{ value: "2", mark: "target" }] },
    { caption: "x = 5. Ни один хвост не >= 5 (только 2). lo = 1 = длина. Добавь в конец. tails = [2, 5]. Длина растёт до 2.", line: 3, cells: [{ value: "2" }, { value: "5", mark: "active" }] },
    { caption: "x = 3. Первый хвост >= 3 это tails[1] = 5. Замени на 3. tails = [2, 3]. Цепочка длины 2 может теперь заканчиваться на 3.", line: 4, cells: [{ value: "2" }, { value: "3", mark: "target" }] },
    { caption: "x = 7. Ни один хвост не >= 7. Добавь в конец. tails = [2, 3, 7]. Длина 3.", line: 3, cells: [{ value: "2" }, { value: "3" }, { value: "7", mark: "active" }] },
    { caption: "x = 101. Ни один хвост не >= 101. Добавь в конец. tails = [2, 3, 7, 101]. Длина 4.", line: 3, cells: [{ value: "2" }, { value: "3" }, { value: "7" }, { value: "101", mark: "active" }] },
    { caption: "x = 18. Первый хвост >= 18 это tails[3] = 101. Замени на 18. tails = [2, 3, 7, 18]. Длина остаётся 4.", line: 4, cells: [{ value: "2" }, { value: "3" }, { value: "7" }, { value: "18", mark: "target" }] },
    { caption: "Готово. tails имеет длину 4, так длина LIS равна 4. (Его содержимое [2,3,7,18] не всегда является настоящей подпоследовательностью — здесь так совпало.)", line: 1, cells: [{ value: "2", mark: "done" }, { value: "3", mark: "done" }, { value: "7", mark: "done" }, { value: "18", mark: "done" }] }
  ]}
/>
</Trace>

<Complexity lang="ru">
| Метод | Время | Память |
|---|---|---|
| **ДП за O(n^2)** | O(n^2) — два вложенных цикла по массиву | O(n) — массив `dp` |
| **Хвосты + бинарный поиск** | O(n log n) — n значений, каждое поиск за O(log n) | O(n) — массив `tails` |

**Почему ДП это O(n^2)**

Внешний цикл запускается `n` раз; для каждого `i` внутренний цикл перебирает до `i` более ранних индексов. Это `1 + 2 + ... + (n-1) ≈ n^2 / 2` сравнений = **O(n^2)**. Память это единственный массив `dp` длины `n` = O(n).

**Почему метод хвостов это O(n log n)**

Каждое из `n` значений запускает ровно один бинарный поиск по `tails`, а `tails` никогда не превышает длину `n`, так каждый поиск это **O(log n)**. Добавление/замена это O(1). Итого: `n × O(log n)` = **O(n log n)**. Память это массив `tails`, максимум `n` записей = O(n).

```
                time          space
O(n^2) DP       O(n^2)        O(n)
tails + bsearch O(n log n)    O(n)   <- this lesson's speedup
```

**Когда ускорение имеет значение?** Для `n = 100,000`, `n^2` это десять миллиардов операций; `n log n` это около 1.7 миллиона. Метод хвостов это стандартный ответ когда `n` большое. ДП за O(n^2) проще для рассуждений и его стоит писать первым если тебе также нужно *восстановить* саму подпоследовательность (методу хвостов для этого нужна дополнительная бухгалтерия).
</Complexity>

<AlgoPractice
  id="practice-lis"
  lessonSlug="07-longest-increasing-subsequence"
  lang="ru"
  problems={[
    {
      kind: "numeric",
      prompt: "Какова длина LIS массива [3, 1, 8, 2, 5]?",
      answer: 3,
      hint: "Возрастающая подпоследовательность сохраняет порядок, но не обязана быть подряд. 1, 2, 5 имеет длину 3. Также 3, 8 (длина 2) или 1, 8 (длина 2). Самая длинная это 3."
    },
    {
      kind: "choice",
      prompt: "В ДП за O(n^2), что представляет dp[i]?",
      choices: [
        "Длину LIS всего массива вплоть до индекса i включительно",
        "Длину наибольшей возрастающей подпоследовательности которая заканчивается ровно на индексе i",
        "Само значение nums[i]",
        "Число элементов меньших чем nums[i]"
      ],
      answer: 1,
      hint: "dp[i] привязан к индексу i: лучшая возрастающая цепочка которая заканчивается на nums[i]. Ответ по всему массиву это max(dp) по всем i, потому что LIS может заканчиваться где угодно."
    },
    {
      kind: "choice",
      prompt: "В массиве хвостов, что это tails[k]?",
      choices: [
        "k-й элемент самой настоящей наибольшей возрастающей подпоследовательности",
        "Наименьшее возможное последнее значение любой возрастающей подпоследовательности длины k+1",
        "Наибольшее значение увиденное до сих пор среди первых k+1 элементов",
        "Число возрастающих подпоследовательностей длины k+1"
      ],
      answer: 1,
      hint: "tails[k] это наименьший хвост достижимый для длины k+1. Хранение минимального хвоста на каждую длину жадно: меньший хвост легче расширить позже."
    },
    {
      kind: "choice",
      prompt: "Для каждого значения x, метод хвостов бинарно ищет какую позицию?",
      choices: [
        "Последний хвост который строго меньше x",
        "Первый хвост который >= x (нижняя граница), потом заменяет его; если такого нет, добавляет x в конец",
        "Точную позицию где x уже появляется в tails",
        "Середину массива tails, всегда"
      ],
      answer: 1,
      hint: "Это поиск нижней границы: первый индекс где tails[idx] >= x. Перезапиши тот хвост меньшим x, или добавь x в конец если каждый хвост меньше x."
    },
    {
      kind: "choice",
      prompt: "Почему `tails.length` это длина LIS, хотя `tails` не обязательно является настоящей подпоследовательностью?",
      choices: [
        "Потому что tails всегда это копия самой настоящей LIS",
        "Потому что tails растёт на один ровно когда становится возможной строго более длинная возрастающая цепочка, так его длина отслеживает максимально достижимую длину; замены никогда не меняют длину, только понижают хвост",
        "Потому что tails отсортирован, а отсортированные массивы всегда равны LIS",
        "Это совпадение которое работает только на некоторых входах"
      ],
      answer: 1,
      hint: "Добавления в конец случаются только когда x превышает каждый хвост (возможна новая, более длинная цепочка), так длина = максимальная длина цепочки. Замены сохраняют длину, но уменьшают хвост для лёгкого будущего расширения. Содержимое может смешивать значения из разных цепочек, так оно не обязано образовывать одну настоящую подпоследовательность."
    },
    {
      kind: "numeric",
      prompt: "Какова длина LIS строго возрастающего массива [1, 2, 3, 4, 5]?",
      answer: 5,
      hint: "Уже строго возрастает, так весь массив это LIS. Каждое значение добавляется в конец tails; tails вырастает до длины 5."
    }
  ]}
/>

<Inset kind="mistake" lang="ru">
**Массив хвостов это НЕ сама настоящая подпоследовательность.** Его *длина* это длина LIS, но его содержимое может быть значениями которые никогда не появлялись вместе в одной возрастающей цепочке. Конкретное доказательство: `nums = [2, 5, 3, 7, 11, 8, 10, 13, 6]` заканчивается итоговым `tails = [2, 3, 6, 8, 10, 13]` (длина 6, что является правильной длиной LIS). Но `[2, 3, 6, 8, 10, 13]` **не** является подпоследовательностью `nums`: `6` это самый последний элемент `nums`, однако здесь он сидит в середине tails впереди 8, 10, 13. Последняя замена (`6` перезаписывает `7`) понизила хвост для длины которая уже была финализирована, испортив содержимое, но оставив длину правильной. Чтобы восстановить настоящую подпоследовательность ты должен хранить ссылки на предшественников отдельно; один массив хвостов даёт тебе только длину.
</Inset>

<Inset kind="why" lang="ru">
**Почему хранить наименьший хвост на каждую длину?** Жадная интуиция: если две возрастающие подпоследовательности обе имеют длину k, та что заканчивается меньшим значением строго полезнее, потому что любой будущий элемент который может расширить ту с бо́льшим хвостом может также расширить ту с меньшим хвостом (и возможно больше). Так мы никогда ничего не теряем всегда запоминая минимальный хвост для каждой длины. Это инвариант который держит массив хвостов, и поэтому перезапись первого хвоста `>= x` меньшим `x` всегда безопасна.
</Inset>

<Inset kind="edgecase" lang="ru">
**Строго возрастающая против неубывающей.** Этот урок вычисляет *строго* возрастающую LIS, так равные соседи не считаются: `[7, 7, 7]` имеет длину LIS 1. Если вместо этого ты хочешь *наибольшую неубывающую* подпоследовательность (допускающую равные значения), смени бинарный поиск с нижней границы (первый хвост `>= x`) на верхнюю границу (первый хвост `> x`). Это однострочное изменение переключает строгое на нестрогое. Их путаница это самая частая ошибка в LIS: всегда подтверждай что хочет задача.
</Inset>

<Inset kind="practice" lang="ru">
**Рецепт для применения.** (1) Сформулируй вариант: строго возрастающая или неубывающая? (2) Для первого корректного решения или когда ты должен восстановить саму настоящую подпоследовательность, напиши ДП за O(n^2): `dp[i] = 1 + max(dp[j])` по `j < i` с `nums[j] < nums[i]`, ответ `max(dp)`. (3) Для большого `n`, переключись на метод хвостов: для каждого `x`, бинарный поиск нижней границы, потом добавь в конец или замени, и верни `tails.length`. (4) Помни оговорку: длина массива хвостов это ответ, но его содержимое не является гарантированной подпоследовательностью.
</Inset>

<Check lang="ru">
  <Quiz
    id="quiz-lis"
    pieceSlug="07-longest-increasing-subsequence"
    lang="ru"
    question="Объясни задачу LIS, ДП за O(n^2), метод хвостов за O(n log n), и почему tails.length это ответ хотя tails не является настоящей подпоследовательностью."
    choices={[
      {
        label: "LIS это наибольшая строго возрастающая подпоследовательность (порядок сохранён, элементы не обязаны быть соседними). ДП за O(n^2) задаёт dp[i] = длина LIS заканчивающейся на индексе i = 1 + max(dp[j]) по всем j < i с nums[j] < nums[i], а ответ это max(dp). Метод за O(n log n) держит массив хвостов где tails[k] это наименьшее последнее значение возрастающей подпоследовательности длины k+1; для каждого значения x он бинарно ищет первый хвост >= x (нижняя граница) и либо добавляет x в конец (если каждый хвост меньше), либо перезаписывает тот хвост меньшим x. tails.length это длина LIS потому что добавления в конец случаются ровно когда становится возможной более длинная цепочка, тогда как замены только понижают хвост не меняя длину. Содержимое tails может смешивать значения из разных цепочек, так сам tails не обязательно является настоящей подпоследовательностью.",
        correct: true
      },
      {
        label: "LIS спрашивает самую длинную подряд идущую цепочку возрастающих чисел (подмассив). И ДП за O(n^2), и метод хвостов за O(n log n) возвращают эту цепочку, а итоговый массив хвостов это ровно наибольшая возрастающая подпоследовательность, так ты можешь прочитать ответ прямо из него.",
        misconception: "LIS это подпоследовательность, не подряд идущий подмассив — элементы могут быть разбросаны пока сохранён порядок. И tails не является самой настоящей подпоследовательностью: только его длина это ответ; его содержимое может быть невозможным как одна настоящая цепочка."
      },
      {
        label: "Оба метода это O(n^2). Массив хвостов помогает только сначала отсортировав вход; после сортировки LIS это просто весь массив, так tails.length равно n каждый раз.",
        misconception: "Метод хвостов это O(n log n), не O(n^2), и он никогда не сортирует вход — сортировка разрушила бы исходный порядок который определяет подпоследовательность. tails.length это длина LIS, которая обычно намного меньше n."
      },
      {
        label: "В методе хвостов ты бинарно ищешь первый хвост строго меньше x и заменяешь его. Равные значения считаются возрастанием, так [7, 7, 7] имеет длину LIS 3, а массив хвостов всегда является валидной возрастающей подпоследовательностью входа.",
        misconception: "Поиск это нижняя граница (первый хвост >= x), не 'строго меньше x'. Строгое возрастание отвергает равных соседей, так [7, 7, 7] имеет длину LIS 1 (используй верхнюю границу только для неубывающего варианта). И tails не гарантированно является валидной подпоследовательностью входа."
      }
    ]}
  />
</Check>

<Recap lang="ru">
<Term slug="longest_increasing_subsequence">Наибольшая возрастающая подпоследовательность (LIS)</Term> это наибольшая <Term slug="strictly_increasing">строго возрастающая</Term> <Term slug="subsequence">подпоследовательность</Term> массива — элементы сохраняют свой порядок, но не обязаны быть подряд.

**ДП за O(n^2):** `dp[i]` = длина LIS заканчивающейся на индексе `i` = `1 + max(dp[j])` по всем `j < i` с `nums[j] < nums[i]`; инициализируй каждый `dp[i] = 1`; ответ это `max(dp)`. Время O(n^2), память O(n).

**Метод хвостов за O(n log n):** держи `tails` где `tails[k]` это наименьшее последнее значение любой возрастающей подпоследовательности длины `k+1`. Для каждого `x`, бинарно ищи первый хвост `>= x` (<Term slug="lower_bound">нижняя граница</Term>); добавь `x` в конец если каждый хвост меньше, иначе перезапиши тот хвост меньшим `x`. Ответ это `tails.length`. Время O(n log n), память O(n).

**Ключевая оговорка:** `tails.length` это длина LIS, но сам `tails` **не** является гарантированной подпоследовательностью — его значения могут происходить из разных цепочек (доказательство: `[2, 5, 3, 7, 11, 8, 10, 13, 6]` заканчивается хвостами `[2, 3, 6, 8, 10, 13]`, длина 6 правильная, содержимое невозможно как одна цепочка). Чтобы восстановить настоящую подпоследовательность, отслеживай ссылки на предшественников отдельно.

**Строгое против неубывающего:** нижняя граница (первый хвост `>= x`) даёт строго возрастающее; переключись на верхнюю границу (первый хвост `> x`) для неубывающего варианта.
</Recap>
