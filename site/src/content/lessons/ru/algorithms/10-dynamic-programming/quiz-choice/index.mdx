---
concepts:
- overlapping-subproblems
- optimal-substructure
- memoization-vs-tabulation
- state-design
- knapsack
- interval-dp
estMin: 14
lang: ru
level: middle
order: 7
prereqs: []
slug: quiz-choice
sources:
- https://en.wikipedia.org/wiki/Dynamic_programming
- https://cp-algorithms.com/dynamic_programming/intro-to-dp.html
- https://en.wikipedia.org/wiki/Knapsack_problem
status: ready
summary: "Синтез по всему юниту dynamic programming: когда DP применима, memoization против tabulation, дизайн state, классические рекуррентности и их сложность."
title: 'Dynamic programming: тест с множественным выбором'
track: algorithms
unit: 10-dynamic-programming
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Шесть вопросов через весь юнит. Каждый отражает решение, которое вы принимаете в начале реальной DP-задачи — не определение для зубрёжки, а какое свойство проверить, какой state выбрать и какую сложность это даёт.
</Hook>

<Goal lang="ru">
Убедитесь, что связываете два разрешающих свойства, два стиля реализации, дизайн state и классические рекуррентности — синтез, к которому вели отдельные уроки.
</Goal>

<Quiz
  id="10-dynamic-programming-choice-1"
  lessonSlug="10-dynamic-programming"
  lang="ru"
  question="Задача распадается на подзадачи, и вы подтвердили, что эти подзадачи повторяются. Достаточно ли этого, чтобы оправдать DP-решение?"
  choices={[
    { label: "Нет. Нужна ещё optimal substructure: оптимальный ответ должен собираться из оптимальных ответов его subproblem. Одни лишь overlapping subproblems говорят только, что кэш поможет; без optimal substructure рекуррентность, объединяющая подответы, неверна.", correct: true },
    { label: "Да — повторяющиеся subproblem единственное требование для DP.", misconception: "Overlapping subproblems оправдывают кэш, но DP нужны оба свойства. Без optimal substructure нет корректной рекуррентности для объединения подответов, и memoization просто быстро кэширует неверные ответы." },
    { label: "Нет — нужно ещё, чтобы subproblem были независимыми и не пересекались.", misconception: "Это описывает divide and conquer (например merge sort), а не DP. DP — это именно случай, когда subproblem ПЕРЕСЕКАЮТСЯ, из-за чего кэш и окупается." },
    { label: "Да, если на каждом шаге работает ещё и жадный выбор.", misconception: "Если жадный выбор доказуемо работает, DP часто не нужна вовсе. DP — инструмент для случая, когда локальный жадный выбор проваливается, но optimal substructure всё же есть." },
  ]}
/>

<Quiz
  id="10-dynamic-programming-choice-2"
  lessonSlug="10-dynamic-programming"
  lang="ru"
  question="У вас есть рабочее решение через memoization (сверху вниз). Когда переписывание его в tabulation (снизу вверх) стоит усилий?"
  choices={[
    { label: "Когда глубина рекурсии грозит переполнением стека, когда нужно сжать память до скользящего окна или когда важна константа на рекурсию и хэширование. Tabulation убирает стек вызовов и позволяет хранить лишь несколько последних строк.", correct: true },
    { label: "Всегда — у tabulation строго лучшая асимптотическая сложность, чем у memoization.", misconception: "Обе вычисляют каждый отдельный state один раз, поэтому асимптотика одинакова. Tabulation выигрывает на константах, безопасности стека и оптимизации памяти, но не на big-O." },
    { label: "Никогда — memoization всегда предпочтительнее, потому что считает только достижимые state.", misconception: "Преимущество memoization на разрежённом множестве state реально, но не абсолютно: глубокая рекурсия переполняет стек, а хэш-кэш дороже массива на доступ. Это компромисс, а не правило." },
    { label: "Только когда у задачи нет базового случая.", misconception: "Базовые случаи есть у любой DP; это не связано. Tabulation именно требует порядка обхода, заполняющего ячейки до того, как их прочитают." },
  ]}
/>

<Quiz
  id="10-dynamic-programming-choice-3"
  lessonSlug="10-dynamic-programming"
  lang="ru"
  question="Для House Robber рекуррентность dp[i] = max(dp[i-1], dp[i-2] + nums[i]). Что кодирует каждая ветвь и почему хватает двух индексов истории?"
  choices={[
    { label: "Пропустить дом i (dp[i-1]) или ограбить его и прибавить к лучшему до i-2 (dp[i-2]+nums[i]). Двух ячеек хватает, потому что единственное ограничение — нельзя два соседних дома, так что решение в i зависит только от i-1 и i-2, и работает O(1) скользящая память.", correct: true },
    { label: "dp[i-1] грабит текущий дом, а dp[i-2]+nums[i] пропускает его.", misconception: "Наоборот. dp[i-1] — лучшее, когда дом i НЕ берут; dp[i-2]+nums[i] — лучшее, когда его БЕРУТ, ведь взятие i запрещает i-1." },
    { label: "Нужен весь массив dp, ведь ограбление дома i может зависеть от любого предыдущего дома.", misconception: "Ограничение соседства локально: взятие i запрещает только i-1. Поэтому dp[i] читает лишь dp[i-1] и dp[i-2], и две скользящие переменные заменяют весь массив." },
    { label: "Рекуррентность жадная — грабить каждый второй дом.", misconception: "Грабёж через один проваливается на входах вроде [2,1,1,2]; оптимум берёт дома 0 и 3. Максимум по двум ветвям — именно то, чем DP бьёт жадное чередование." },
  ]}
/>

<Quiz
  id="10-dynamic-programming-choice-4"
  lessonSlug="10-dynamic-programming"
  lang="ru"
  question="0/1 knapsack работает за O(n*W). Почему это называют псевдополиномиальным, а не полиномиальным?"
  choices={[
    { label: "W — числовое значение, и его размер во входе — это число бит, около log W. Значит O(n*W) экспоненциально по длине W в битах, а не полиномиально по размеру входа. Ёмкость 10^9 делает таблицу огромной даже при немногих предметах.", correct: true },
    { label: "Потому что рекурсия делает два вызова на предмет, что экспоненциально.", misconception: "Версия с memoization или tabulation считает каждый state один раз — она не экспоненциальна по числу state. Псевдополиномиальность относится к зависимости от величины W, а не от ветвления." },
    { label: "Потому что n*W — лишь средний случай, а худший O(2^n).", misconception: "O(n*W) — это худший случай самой DP, а не средний. Цифра 2^n — это наивная рекурсия без memoization." },
    { label: "Потому что W должно быть простым числом, чтобы оценка держалась.", misconception: "Простота W неважна. Ярлык псевдополиномиальности целиком про то, что W — значение с логарифмической длиной записи." },
  ]}
/>

<Quiz
  id="10-dynamic-programming-choice-5"
  lessonSlug="10-dynamic-programming"
  lang="ru"
  question="Алгоритм Longest Increasing Subsequence за O(n log n) хранит массив 'tails'. Что является ответом задачи и что на самом деле содержит массив tails?"
  choices={[
    { label: "Ответ — это ДЛИНА массива tails. По индексу k массив хранит наименьшее возможное хвостовое значение любой возрастающей subsequence длины k+1 — то есть это не валидная LIS, а граница по длинам, в которой бинарным поиском размещают каждое значение.", correct: true },
    { label: "Массив tails И ЕСТЬ искомая возрастающая subsequence, так что возвращают его содержимое.", misconception: "Tails — не реальная subsequence: каждая ячейка перезаписывается наименьшим хвостом для своей длины, и хранимые значения могут даже не идти в таком порядке во входе. Значима только его длина." },
    { label: "Ответ — максимальное значение в tails.", misconception: "Максимум — это просто наибольший размещённый элемент, он не связан с длиной LIS. Ответ — число занятых ячеек." },
    { label: "Tails хранит dp[i] для каждого индекса — LIS, заканчивающуюся в i.", misconception: "Это массив O(n^2)-DP, а не tails. Массив tails индексируется по ДЛИНЕ subsequence, а не по позиции во входе, что и позволяет бинарный поиск." },
  ]}
/>

<Quiz
  id="10-dynamic-programming-choice-6"
  lessonSlug="10-dynamic-programming"
  lang="ru"
  question="Interval DP (matrix chain, burst balloons) идёт по возрастанию длины интервала и выбирает точку разбиения k внутри [i..j]. Почему обходить по длине, а не привычно слева направо по i?"
  choices={[
    { label: "dp[i][j] объединяет меньшие подинтервалы dp[i][k] и dp[k+1][j], оба строго короче [i..j]. Обход по возрастанию длины гарантирует, что любой более короткий интервал уже вычислен до того, как его прочитает более длинный — обычный цикл по i прочитал бы ещё не заполненные ячейки.", correct: true },
    { label: "Обход по длине — лишь стилистическая условность; слева направо тоже работает.", misconception: "Это требование корректности, а не стиль. Цикл по i слева направо доходит до dp[i][j] до заполнения dp[i+1][j-1] и читает мусор. Зависимость от более коротких интервалов значит, что длина должна быть внешним циклом." },
    { label: "Потому что у interval DP нет базового случая, с которого начать.", misconception: "Базовый случай есть: интервалы длины 1 (или 0). Это и есть самые короткие интервалы, заполняемые первыми при обходе по длине." },
    { label: "Потому что точка разбиения k всегда должна быть серединой.", misconception: "k пробегает все позиции внутри интервала; берут лучшее по всем разбиениям. Фиксация k серединой сломала бы оптимальность — оптимальное последнее действие редко в середине." },
  ]}
/>

<Recap lang="ru">
Сквозная линия юнита — один чеклист перед написанием любой DP: подтвердить overlapping subproblems И optimal substructure; спроектировать наименьший state, схватывающий решение (один индекс для House Robber, два для knapsack и LCS, интервал для matrix chain); выбрать memoization или tabulation по глубине стека и памяти, а не по big-O; и знать, что реально лежит в таблице — включая массив tails в LIS, ответом служит его длина, а не содержимое. Сложность одинакова: число state на работу на один state.
</Recap>
