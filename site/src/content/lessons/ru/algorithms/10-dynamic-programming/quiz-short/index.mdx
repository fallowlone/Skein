---
concepts:
- overlapping-subproblems
- optimal-substructure
- memoization-vs-tabulation
- state-design
- transition
- space-optimization
estMin: 14
lang: ru
level: middle
order: 8
prereqs: []
slug: quiz-short
sources:
- https://en.wikipedia.org/wiki/Dynamic_programming
- https://cp-algorithms.com/dynamic_programming/intro-to-dp.html
- https://en.wikipedia.org/wiki/Longest_increasing_subsequence
status: ready
summary: "Подсказки на свободное воспроизведение по всему юниту DP. Сначала ответьте своими словами, затем раскройте образцовый ответ и сравните."
title: 'Dynamic programming: тест со свободным воспроизведением'
track: algorithms
unit: 10-dynamic-programming
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";

<Hook>
Воспроизведение бьёт перечитывание. На каждую подсказку проговорите или запишите полный ответ по памяти, прежде чем открыть образец — именно усилие припоминания закрепляет материал.
</Hook>

<Goal lang="ru">
Восстановите хребет юнита — два разрешающих свойства, memoization против tabulation, как спроектировать state и его transition, и как работает оптимизация памяти — не подсматривая в уроки.
</Goal>

<RetrievalDrawer
  client:load
  id="10-dynamic-programming-short-retrieval"
  lang="ru"
  questions={[
    { q: "Какие два свойства должна иметь задача, прежде чем применима dynamic programming, и что даёт каждое?", a: "Overlapping subproblems: одна и та же subproblem решается в наивной рекурсии многократно, поэтому кэширование каждого ответа один раз схлопывает экспоненциальную повторную работу в линейные обращения. Optimal substructure: оптимальное решение целого строится из оптимальных решений его subproblem, и именно это делает рекуррентность корректной — подответы можно объединять, не залезая в их внутренности. Overlapping subproblems оправдывают кэш; optimal substructure оправдывает рекуррентность. Без первого кэш не помогает (это territory divide and conquer); без второго рекуррентность неверна, как её ни храни." },
    { q: "Сравните memoization и tabulation. Когда выбрать каждое?", a: "Memoization — сверху вниз: пишут естественную рекурсию, добавляют cache по аргументам subproblem, проверяют кэш перед вычислением и сохраняют после. Считаются только реально достигнутые state, и это ближе всего к записанной рекуррентности. Tabulation — снизу вверх: заводят массив dp, заполняют базовые случаи, затем идут от малых state к цели в порядке, где каждая ячейка читает уже заполненные — без рекурсии и стека вызовов. Обе считают каждый отдельный state один раз, поэтому асимптотика одинакова. Берите memoization при разрежённом множестве state или когда порядок трудно вывести; берите tabulation, когда глубина рекурсии переполнит стек, нужна оптимизация памяти скользящим окном или важна константа на рекурсию и хэширование." },
    { q: "Как спроектировать state для DP-задачи и что делает state корректным?", a: "State — это минимальный набор переменных, полностью определяющих ответ subproblem: измените любую — и ответ может измениться; всё прочее неважно. Корректный state достаточен (схватывает все ограничения, от которых зависят будущие решения) и минимален (без лишнего измерения, раздувающего таблицу). House Robber нужен один индекс i, ведь единственное ограничение — соседство. Knapsack нужен (индекс предмета, остаток ёмкости), ведь выбор зависит от обоих. LCS и edit distance нужны два индекса (i, j), по одному на строку. Interval DP нужен интервал [i, j], ведь стоимость зависит от того, какой непрерывный диапазон остался. Если две разные ситуации дали бы разные ответы, но отображаются в один state, то state не хватает измерения." },
    { q: "Что такое transition и почему порядок обхода должен его уважать?", a: "Transition — это рекуррентность, выражающая dp[state] через строго меньшие (уже решённые) state: она перечисляет каждый доступный в state выбор и объединяет полученные подответы, обычно через min, max или сумму. Порядок обхода должен гарантировать, что каждый state, читаемый в transition, уже заполнен до вычисления текущего. В 1D это обычно слева направо; в 2D — по зависимостям строк и столбцов; в interval DP — по возрастанию длины интервала, ведь dp[i][j] зависит от более коротких dp[i][k] и dp[k+1][j]. Ошибётесь в порядке — transition прочитает неинициализированные ячейки и вернёт уверенно неверный ответ без падения." },
    { q: "Объясните, как работает оптимизация памяти (скользящий массив) и когда она безопасна.", a: "Оптимизация памяти сжимает таблицу dp до нескольких строк или переменных, когда каждая ячейка зависит лишь от ограниченного недавнего окна других ячеек. Рекуррентности в стиле Fibonacci 1D (dp[i] из dp[i-1] и dp[i-2]) нужны лишь две скользящие переменные — O(1) памяти. 2D-таблица, где dp[i][j] читает только предыдущую строку и левого соседа текущей (LCS, edit distance), требует двух строк — O(m) памяти. Это безопасно ровно тогда, когда вы не перезаписываете значение, которое более поздняя ячейка того же прохода ещё должна прочитать — поэтому 1D-форма 0/1 knapsack идёт по ёмкости вниз, чтобы не читать уже обновлённую ячейку того же предмета. Подвох: скользящие массивы выбрасывают историю, нужную для восстановления самого пути решения, так что храните полную таблицу, если надо вернуть последовательность, а не только её значение." },
    { q: "Возьмите Longest Increasing Subsequence: дайте O(n^2) DP, идею O(n log n) и ловушку быстрой версии.", a: "O(n^2) DP: dp[i] — длина самой длинной возрастающей subsequence, заканчивающейся ровно в индексе i, считается как 1 + max(dp[j]) по всем j < i с nums[j] < nums[i]; ответ — максимум по всем dp[i]. O(n log n): держат массив tails, где tails[k] — наименьшее хвостовое значение любой возрастающей subsequence длины k+1; для каждого значения бинарным поиском находят первый хвост, который оно не может продолжить, и перезаписывают его (или добавляют в конец, если оно продолжает всё). Ответ — ДЛИНА tails. Ловушка: массив tails НЕ является валидной возрастающей subsequence — его значения могут быть смесью, которая никогда не идёт в таком порядке во входе. Значима лишь его длина; восстановление самой subsequence требует дополнительных указателей на родителя." },
  ]}
/>

<Recap lang="ru">
Если вы смогли восстановить каждый ответ по памяти, вы держите хребет юнита: подтвердить overlapping subproblems и optimal substructure, выбрать memoization или tabulation по глубине стека и памяти (а не по big-O), спроектировать минимальный достаточный state, написать transition по каждому выбору и обходить в порядке, уважающем его, и свернуть таблицу в окно лишь когда ни одно ещё нужное значение не перезаписывается. Сложность одинакова — число state на работу на один state.
</Recap>
