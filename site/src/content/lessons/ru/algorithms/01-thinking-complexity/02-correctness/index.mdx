---
concepts:
- correctness
- loop_invariant
- edge_case
- specification
estMin: 18
lang: ru
mathPrereqs: []
order: 2
prereqs:
- 01-what-is-an-algorithm
slug: 02-correctness
sources:
- https://en.wikipedia.org/wiki/Loop_invariant
- https://www.baeldung.com/cs/loop-invariant
status: ready
summary: Корректность означает, что алгоритм выдаёт правильный результат для каждого
  действительного входа, не только для тех, что ты проверил. Используй инварианты
  цикла, чтобы рассуждать о корректности.
title: Правильный ли алгоритм?
track: algorithms
unit: 01-thinking-complexity
---



import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Idea from "~/components/lesson/Idea.astro";
import Code from "~/components/lesson/Code.astro";
import Trace from "~/components/lesson/Trace.astro";
import Complexity from "~/components/lesson/Complexity.astro";
import Check from "~/components/lesson/Check.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import AnnotatedCode from "~/components/algo/AnnotatedCode.astro";
import AlgoTrace from "~/components/algo/AlgoTrace.astro";
import CodeRunner from "~/components/algo/CodeRunner.astro";
import AlgoPractice from "~/components/algo/AlgoPractice.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DiagramFrame from "~/components/diagram/DiagramFrame.astro";

<Hook>
Ты проверяешь функцию на `[3, 9, 2, 7]` и она возвращает 9 — правильно. Проверяешь на `[1, 2, 3]` и возвращает 3 — правильно. Чувствуешь уверенность. А затем на производстве она падает на пустом списке. Или возвращает неправильный ответ для всех отрицательных чисел. Проверка нескольких примеров — это не то же самое, что уверенность в том, что алгоритм правилен для каждого возможного входа.
</Hook>

<Goal lang="ru">
После этого урока ты можешь определить, что значит **корректность** алгоритма, заметить ошибки, рассматривая **граничные случаи**, и использовать **инвариант цикла**, чтобы доказать, что цикл решает задачу правильно.
</Goal>

<Idea lang="ru">
Алгоритм **корректен**, если он выдаёт правильный результат для **каждого** действительного входа, не только для тех, что ты проверил. Это определение требует двух вещей:

1. **Спецификацию** — точное утверждение о том, какой результат ожидается для какого входа. Например: «Дан список чисел, верни наибольшее.»
2. **Доказательство** — логический аргумент, что алгоритм соответствует спецификации для всех возможных входов.

Тестирование — это не доказательство. Тестирование может обнаружить ошибки, но не может доказать корректность, потому что ты не можешь проверить каждый возможный вход. Граничные случаи — необычные или пограничные входы вроде пустых списков, одного элемента или всех равных значений — вот где скрываются ошибки.

Инструмент для доказательства того, что цикл правилен, называется **инвариант цикла**: утверждение о цикле, которое верно до его запуска, остаётся верным после каждой итерации и полезно, когда цикл завершается.

Рассмотри функцию `largestOf` из урока 01:

<AnnotatedCode
  code={`function largestOf(numbers) {
  let largest = numbers[0];
  for (let i = 1; i < numbers.length; i++) {
    if (numbers[i] > largest) {
      largest = numbers[i];
    }
  }
  return largest;
}`}
  annotations={[
    { line: 2, text: "После этой строки largest = numbers[0]" },
    { line: 3, text: "Цикл: мы обновим largest, когда увидим больше чисел" },
    { line: 8, text: "Инвариант: после обработки numbers[0..i], largest хранит максимум" }
  ]}
/>

**Инвариант цикла** такой: «После обработки первых i+1 чисел (с индекса 0 по индекс i), `largest` содержит наибольшее из этих чисел.»

- **До цикла**: Мы устанавливаем `largest = numbers[0]`, поэтому инвариант верен для i=0.
- **Каждая итерация**: Мы сравниваем `numbers[i]` с `largest`. Если оно больше, мы обновляем `largest`. В любом случае, после итерации `largest` остаётся наибольшим из чисел, которые мы видели до сих пор.
- **После цикла**: Цикл выходит, когда `i = numbers.length`. По инварианту, `largest` — наибольшее из всех чисел. Это то, что мы обещали вернуть.

Этот логический аргумент доказывает, что алгоритм правилен — не тестированием, а рассуждением.

<DiagramFrame
  label="Инвариант цикла доказывает корректность в трёх контрольных точках вдоль одного цикла. Инициализация: до первой итерации свойство INV (largest содержит максимум numbers[0..i]) делается верным при i=0. Поддержание: каждая итерация предполагает, что INV верен в начале, и доказывает, что он остаётся верным в конце, поэтому за счёт повторения INV держится на каждом шаге цикла; изогнутая обратная стрелка показывает повторение тела цикла. Завершение: когда цикл выходит при i = n, INV всё ещё верен — и так как весь массив обработан, INV прямо утверждает, что largest — максимум всех чисел, а это и есть спецификация. Цепочка читается: докажи, что свойство держится на каждом шаге, следовательно результат правильный."
  hue="var(--d-systems)"
  caption="Инвариант цикла — это одно свойство INV, доказанное в трёх контрольных точках. ИНИЦИАЛИЗАЦИЯ делает INV верным до цикла; ПОДДЕРЖАНИЕ показывает, что каждая итерация сохраняет INV (верно в начале тела ⇒ верно в конце), поэтому INV держится на каждом шаге; ЗАВЕРШЕНИЕ: на выходе из цикла INV плюс условие выхода дают спецификацию. Свойство верно на каждом шаге ⇒ результат правильный."
>
  <svg viewBox="0 0 760 340" width="760" height="340" xmlns="http://www.w3.org/2000/svg" font-family="var(--font-body, sans-serif)">
    <defs>
      <marker id="inv-arrow-ru" markerWidth="9" markerHeight="9" refX="7" refY="4" orient="auto">
        <path d="M0,0 L8,4 L0,8 Z" fill="var(--d-systems)" />
      </marker>
      <marker id="inv-arrow-mut-ru" markerWidth="9" markerHeight="9" refX="7" refY="4" orient="auto">
        <path d="M0,0 L8,4 L0,8 Z" fill="var(--muted, #888)" />
      </marker>
    </defs>

    <text x="380" y="26" text-anchor="middle" font-size="13" font-weight="700" fill="var(--d-systems)">{"INV: largest = максимум numbers[0..i] — одно свойство, три точки"}</text>

    <line x1="40" y1="120" x2="700" y2="120" stroke="var(--hairline-2, #888)" stroke-width="2" marker-end="url(#inv-arrow-ru)" />
    <text x="370" y="112" text-anchor="middle" font-size="11" fill="var(--muted, #888)">цикл идёт слева направо</text>

    <circle cx="120" cy="120" r="9" fill="var(--d-systems)" />
    <rect x="40" y="150" width="160" height="80" rx="8" fill="var(--card, #fff)" stroke="var(--d-systems)" stroke-width="3" />
    <text x="120" y="172" text-anchor="middle" font-size="13" font-weight="700" fill="var(--d-systems)">1. ИНИЦИАЛИЗАЦИЯ</text>
    <text x="120" y="192" text-anchor="middle" font-size="11" fill="var(--muted, #888)">до цикла, i = 0</text>
    <text x="120" y="210" text-anchor="middle" font-size="11" font-weight="600" fill="var(--ink, currentColor)">largest = numbers[0]</text>
    <text x="120" y="224" text-anchor="middle" font-size="11" font-weight="700" fill="var(--d-systems)">INV верен</text>

    <circle cx="380" cy="120" r="9" fill="var(--d-systems)" />
    <rect x="290" y="150" width="180" height="80" rx="8" fill="var(--card, #fff)" stroke="var(--d-systems)" stroke-width="3" />
    <text x="380" y="172" text-anchor="middle" font-size="13" font-weight="700" fill="var(--d-systems)">2. ПОДДЕРЖАНИЕ</text>
    <text x="380" y="192" text-anchor="middle" font-size="11" fill="var(--muted, #888)">каждая итерация, i = 1..n-1</text>
    <text x="380" y="210" text-anchor="middle" font-size="11" font-weight="600" fill="var(--ink, currentColor)">INV вход ⇒ INV выход</text>
    <text x="380" y="224" text-anchor="middle" font-size="11" font-weight="700" fill="var(--d-systems)">INV остаётся верным</text>

    <path d="M330 150 C 330 96, 430 96, 430 150" fill="none" stroke="var(--d-systems)" stroke-width="2" marker-end="url(#inv-arrow-ru)" />
    <text x="380" y="92" text-anchor="middle" font-size="11" font-weight="600" fill="var(--d-systems)">повтор: тело сохраняет INV</text>

    <circle cx="640" cy="120" r="9" fill="var(--d-systems)" />
    <rect x="560" y="150" width="160" height="80" rx="8" fill="var(--card, #fff)" stroke="var(--d-systems)" stroke-width="3" />
    <text x="640" y="172" text-anchor="middle" font-size="13" font-weight="700" fill="var(--d-systems)">3. ЗАВЕРШЕНИЕ</text>
    <text x="640" y="192" text-anchor="middle" font-size="11" fill="var(--muted, #888)">цикл выходит, i = n</text>
    <text x="640" y="210" text-anchor="middle" font-size="11" font-weight="600" fill="var(--ink, currentColor)">все [0..n-1] просмотрены</text>
    <text x="640" y="224" text-anchor="middle" font-size="11" font-weight="700" fill="var(--d-systems)">INV верен</text>

    <line x1="640" y1="234" x2="640" y2="270" stroke="var(--d-systems)" stroke-width="2" marker-end="url(#inv-arrow-ru)" />
    <rect x="470" y="278" width="270" height="50" rx="8" fill="var(--card, #fff)" stroke="var(--d-systems)" stroke-width="2" />
    <text x="605" y="300" text-anchor="middle" font-size="12" font-weight="700" fill="var(--ink, currentColor)">{"INV при i = n ⇒ largest = максимум всех"}</text>
    <text x="605" y="318" text-anchor="middle" font-size="12" font-weight="700" fill="var(--d-systems)">результат правильный</text>

    <text x="240" y="300" text-anchor="middle" font-size="12" font-weight="600" fill="var(--muted, #888)">свойство верно на каждом шаге</text>
    <line x1="350" y1="296" x2="466" y2="296" stroke="var(--muted, #888)" stroke-width="1.5" stroke-dasharray="4 3" marker-end="url(#inv-arrow-mut-ru)" />
  </svg>
</DiagramFrame>
</Idea>

<Code lang="ru">
Напишем функцию `largestOf` и затем докажем её корректность:

```js
function largestOf(numbers) {
  let largest = numbers[0];
  for (let i = 1; i < numbers.length; i++) {
    if (numbers[i] > largest) {
      largest = numbers[i];
    }
  }
  return largest;
}
```

Попробуй её на нескольких входах:

<CodeRunner
  id="cr-correct"
  lang="ru"
  code={`function largestOf(numbers) {
  let largest = numbers[0];
  for (let i = 1; i < numbers.length; i++) {
    if (numbers[i] > largest) {
      largest = numbers[i];
    }
  }
  return largest;
}

console.log(largestOf([3, 9, 2, 7]));      // 9
console.log(largestOf([1]));               // 1
console.log(largestOf([-5, -1, -10]));     // -1
console.log(largestOf([5, 5, 5, 5]));      // 5`}
/>

Все эти примеры проходят. Но ты можешь попасть в ловушку: думать, что раз я это проверил, то оно правильно. Это заблуждение. Инвариант цикла — вот что даёт тебе уверенность.
</Code>

<Trace lang="ru">
Проследим алгоритм на `[-5, -1, -10]`, чтобы увидеть инвариант цикла в действии:

<AlgoTrace
  id="trace-correct"
  lang="ru"
  code={[
    "function largestOf(numbers) {",
    "  let largest = numbers[0];",
    "  for (let i = 1; i < numbers.length; i++) {",
    "    if (numbers[i] > largest) {",
    "      largest = numbers[i];",
    "    }",
    "  }",
    "  return largest;",
    "}"
  ]}
  frames={[
    {
      caption: "Инициализация: largest = numbers[0] = -5. Инвариант верен для индекса 0.",
      line: 2,
      cells: [{ value: "-5", mark: "active" }, "-1", "-10"]
    },
    {
      caption: "Итерация 1: i=1. Является ли -1 > -5? Да. Обнови largest на -1.",
      line: 4,
      cells: ["-5", { value: "-1", mark: "active" }, "-10"]
    },
    {
      caption: "После обновления largest = -1, максимум из numbers[0..1]. Инвариант верен.",
      line: 6,
      cells: ["-5", { value: "-1", mark: "done" }, "-10"]
    },
    {
      caption: "Итерация 2: i=2. Является ли -10 > -1? Нет. Оставляем largest = -1.",
      line: 4,
      cells: ["-5", "-1", { value: "-10", mark: "active" }]
    },
    {
      caption: "После этой итерации largest = -1 по-прежнему максимум из numbers[0..2]. Инвариант верен.",
      line: 6,
      cells: ["-5", "-1", { value: "-10", mark: "done" }]
    },
    {
      caption: "Цикл выходит. По инварианту, largest = -1 — максимум всех чисел. Возвращаем его.",
      line: 8,
      cells: ["-5", { value: "-1", mark: "done" }, "-10"]
    }
  ]}
/>

Инвариант остаётся верным на каждом шаге. Это даёт нам уверенность не только для этого примера, но для каждого действительного входа.
</Trace>

<Complexity lang="ru">
Доказательство корректности не имеет прямой стоимости в терминах шагов — это логический аргумент, не вычисление. Но понимание структуры алгоритма (инвариант цикла) часто делает код быстрее или яснее. Пока что ключевая идея: правильный алгоритм — это алгоритм, о котором ты можешь рассуждать с инвариантом.
</Complexity>

<AlgoPractice
  id="practice-correct-1"
  lessonSlug="02-correctness"
  lang="ru"
  problems={[
    {
      kind: "choice",
      prompt: "Что такое инвариант цикла?",
      choices: [
        "Условие, которое должно быть истинно до и после каждой итерации",
        "Переменная, которая никогда не меняется внутри цикла",
        "Утверждение, которое ты пишешь, чтобы код работал быстрее",
        "Правило, которое предотвращает вечный цикл"
      ],
      answer: 0,
      hint: "Инвариант цикла верен до цикла и остаётся верным после каждой итерации — он помогает доказать, что цикл правильный."
    },
    {
      kind: "choice",
      prompt: "Почему только тестирование недостаточно для доказательства корректности алгоритма?",
      choices: [
        "Тестирование слишком медленно",
        "Тестирование может проверить конечное число входов; ты не можешь проверить каждый возможный вход",
        "Тестирование требует слишком много кода",
        "Тестирование всегда дает неправильные ответы"
      ],
      answer: 1,
      hint: "Ты можешь протестировать миллион случаев, но может быть один случай, который ты не проверил, где алгоритм падает. Доказательство рассуждением (вроде инвариантов цикла) охватывает все случаи."
    },
    {
      kind: "numeric",
      prompt: "Если цикл обрабатывает элементы с индекса 0 по n-1, и инвариант — «largest содержит максимум элементов [0..i]», каким будет инвариант, когда цикл заканчивается и i = n?",
      answer: 1,
      hint: "Когда i = n, цикл обработал индексы 0 через n-1, это все элементы. Поэтому largest содержит максимум всех элементов [0..n-1]."
    },
    {
      kind: "choice",
      prompt: "Алгоритм падает на отрицательных числах, хотя работает на положительных. Это пример чего?",
      choices: [
        "Нарушения инварианта цикла",
        "Граничного случая — пограничного входа, где алгоритм падает",
        "Проблемы с нотацией большого-O",
        "Бесконечного цикла"
      ],
      answer: 1,
      hint: "Отрицательные числа — это граничный случай — необычный вход, что проверяет пределы корректности алгоритма."
    },
    {
      kind: "order",
      prompt: "Расставь шаги для доказательства корректности цикла с использованием инварианта цикла:",
      items: [
        "Докажи, что инвариант верен до цикла (инициализация)",
        "Докажи, что инвариант остаётся верным после каждой итерации (поддержание)",
        "Покажи, что когда цикл выходит, инвариант говорит нам, что ответ правильный (завершение)",
        "Сформулируй, что такое инвариант цикла"
      ],
      hint: "Сначала сформулируй инвариант чётко. Затем докажи три вещи: он начинается верным, остаётся верным на каждом шаге и приводит к правильному ответу, когда всё готово."
    }
  ]}
/>

<Inset kind="edgecase" lang="ru">
**Граничный случай: пустой список.** Функция падает на пустом списке, потому что `numbers[0]` не существует. Правильная версия сначала проверила бы `if (numbers.length === 0)`. Спецификация должна сказать, что вернуть для пустого входа — может быть, null или выкинуть ошибку. Без такой спецификации ты не можешь сказать, правильна ли функция.
</Inset>

<Inset kind="mistake" lang="ru">
**Багги вариант.** Что если мы инициализируем `largest = 0` вместо `largest = numbers[0]`?

```js
function largestOf_WRONG(numbers) {
  let largest = 0;  // БАГ: должно быть numbers[0]
  for (let i = 0; i < numbers.length; i++) {
    if (numbers[i] > largest) {
      largest = numbers[i];
    }
  }
  return largest;
}
```

Проверка: `largestOf_WRONG([-5, -3, -1])` возвращает 0, а не -1. Это падает на всех отрицательных списках. Инвариант цикла перехватил бы это: если мы инициализируем `largest = 0`, инвариант «largest содержит максимум [0..i]» ложен ещё до начала цикла, потому что максимум [-5] — это -5, не 0. Ложный инвариант означает ложный алгоритм.
</Inset>

<Check lang="ru">
  <Quiz
    id="quiz-correct-1"
    pieceSlug="02-correctness"
    lang="ru"
    question="Друг проверяет алгоритм на 100 случайных входах и он проходит все. Доказывает ли это, что алгоритм правильный?"
    choices={[
      {
        label: "Нет — тестирование некоторых входов не доказывает корректность для всех входов",
        correct: true
      },
      {
        label: "Да — если он проходит 100 тестов, он должен быть правильным",
        misconception: "Тестирование может обнаружить ошибки, но не может доказать корректность. Может быть граничный случай или редкий вход, который ты не проверил."
      },
      {
        label: "Да — корректность означает только то, что оно работает на входах, которые ты проверяешь",
        misconception: "Корректность означает, что оно работает на каждом действительном входе, не только на тех, что ты проверил. Спецификация определяет, что должен дать каждый вход."
      },
      {
        label: "Только если 100 входов включают все граничные случаи",
        misconception: "Даже если ты думаешь, что покрыл все граничные случаи, ты можешь пропустить один. Доказательство рассуждением (вроде инвариантов цикла) охватывает все случаи логически."
      }
    ]}
  />
</Check>

<Recap lang="ru">
Алгоритм **корректен**, если он выдаёт правильный результат для каждого действительного входа. Это определяется **спецификацией** — что должен выдать каждый вход. **Тестирование** может обнаружить ошибки, но не может доказать корректность, потому что ты не можешь проверить каждый вход. **Граничные случаи** — необычные или пограничные входы — вот где скрываются ошибки.

**Инвариант цикла** — это утверждение о цикле, которое верно до его запуска, остаётся верным после каждой итерации и помогает доказать, что цикл правилен. Доказав три вещи (инициализацию, поддержание, завершение), ты можешь быть уверен, что цикл работает для всех входов. Это логическое доказательство, не просто уверенность от тестирования.
</Recap>
