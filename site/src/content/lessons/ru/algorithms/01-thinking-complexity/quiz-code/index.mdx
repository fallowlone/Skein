---
concepts:
- counting-steps
- nested-loops
- big-o
- space-complexity
- correctness-bug
estMin: 14
lang: ru
level: middle
order: 9
prereqs: []
slug: quiz-code
sources:
- https://www.geeksforgeeks.org/dsa/step-count-method-for-time-complexity-analysis/
- https://en.wikipedia.org/wiki/Big_O_notation
- https://en.wikipedia.org/wiki/Space_complexity
status: ready
summary: "Читай реальные фрагменты кода, считай шаги, выводи Big-O по времени и памяти, предсказывай поведение и находи баг сложности или корректности."
title: 'Мышление и сложность: чтение кода'
track: algorithms
unit: 01-thinking-complexity
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Анализ закрепляется, только когда делаешь его на реальном коде. Прочитай каждый фрагмент, посчитай циклы и память и определи сложность — ровно тот ход, который делаешь на ревью пул-реквеста или оценивая решение.
</Hook>

<Goal lang="ru">
Отработай цикл, который запускаешь на любой незнакомой функции: найди доминирующую работу, перемножь вложенные циклы, отдели время от памяти и замечай, когда проблема вовсе не в скорости, а в баге корректности.
</Goal>

## Фрагмент 1 — во что обходится вложенность?

```js
function countCloserPairs(points, threshold) {
  let count = 0;
  for (let i = 0; i < points.length; i++) {
    for (let j = i + 1; j < points.length; j++) {
      if (Math.abs(points[i] - points[j]) < threshold) {
        count++;
      }
    }
  }
  return count;
}
```

<Quiz
  id="01-thinking-complexity-code-1"
  lessonSlug="01-thinking-complexity"
  lang="ru"
  question="Каковы сложности по времени и памяти у countCloserPairs на n точках?"
  choices={[
    { label: "Время O(n²), память O(1). Внутренний цикл, стартующий с i+1, делает в сумме n(n−1)/2 сравнений — всё равно ~n², доминирующий член, — а хранятся только count, i и j.", correct: true },
    { label: "Время O(n), память O(n). Это один проход, и count может расти с n.", misconception: "Тут два вложенных цикла, поэтому время O(n²), а не O(n). А память считает структуры данных, а не значение count — три скалярные переменные это O(1)." },
    { label: "Время O(n²), память O(n²). Пары, которые он проверяет, образуют таблицу n×n.", misconception: "Он проверяет ~n² пар, но никогда их не хранит — лишь инкрементирует счётчик. Память O(1); n² — это время, а не память." },
    { label: "Время O(n log n), память O(1). Старт j с i+1 делает его логарифмическим.", misconception: "Старт j с i+1 уменьшает константу вдвое (n(n−1)/2 вместо n²), но доминирующий член всё равно n². Это O(n²), а не O(n log n)." },
  ]}
/>

## Фрагмент 2 — предскажи число сравнений

```js
function linearSearch(arr, target) {
  for (let i = 0; i < arr.length; i++) {
    if (arr[i] === target) return i;
  }
  return -1;
}
// arr = [0, 1, 2, ..., 999]  (1000 отсортированных элементов)
```

<Quiz
  id="01-thinking-complexity-code-2"
  lessonSlug="01-thinking-complexity"
  lang="ru"
  question="Для arr = [0..999] какое утверждение про linearSearch верно?"
  choices={[
    { label: "Поиск 999 делает ~1000 сравнений, и поиск отсутствующего значения — 1000; худший случай O(n). Бинарный поиск нашёл бы 999 за ~10 сравнений, потому что массив отсортирован.", correct: true },
    { label: "Это O(log n), потому что массив отсортирован.", misconception: "Отсортированность не делает линейный проход логарифмическим — этот код игнорирует порядок и проверяет каждый элемент. Чтобы использовать сортировку и получить O(log n), нужен бинарный поиск." },
    { label: "Худший и лучший случай оба ~500 сравнений (среднее).", misconception: "Лучший случай — 1 (цель на индексе 0); худший — n (цель в конце или отсутствует). Среднее ~n/2, но Big-O сообщает худший случай, O(n)." },
    { label: "Это O(1), потому что ранний возврат делает время константным.", misconception: "Ранний возврат помогает, только когда цель близка к началу. Для отсутствующего значения или значения в конце он сканирует все n элементов — худший случай O(n)." },
  ]}
/>

## Фрагмент 3 — один результат, разная стоимость

```js
// Версия A
function sumToNLoop(n) {
  let sum = 0;
  for (let i = 1; i <= n; i++) sum += i;
  return sum;
}

// Версия B
function sumToNFormula(n) {
  return n * (n + 1) / 2;
}
```

<Quiz
  id="01-thinking-complexity-code-3"
  lessonSlug="01-thinking-complexity"
  lang="ru"
  question="Обе функции возвращают одно значение. Как соотносятся их сложности?"
  choices={[
    { label: "Версия A — O(n) по времени (цикл выполняется n раз); версия B — O(1) по времени (фиксированное арифметическое выражение независимо от n). Обе O(1) по памяти. Один результат, очень разный рост.", correct: true },
    { label: "Обе O(n), потому что обе вычисляют сумму до n.", misconception: "Версия B не вычисляет никакой суммы — она применяет замкнутую формулу n(n+1)/2 за константное число операций. Результат совпадает, но стоимость O(1), а не O(n)." },
    { label: "Версия B быстрее, но всё равно O(n), раз зависит от n.", misconception: "Зависеть от ЗНАЧЕНИЯ n — не то же, что масштабироваться с n. B делает те же несколько арифметических операций, будь n равно 5 или 5 миллиардам — это O(1)." },
    { label: "Версия A — O(1), потому что один цикл константен.", misconception: "Цикл, ограниченный n, выполняется n раз, поэтому это O(n), а не O(1). Лишь цикл с фиксированной границей, независимой от n, — константное время." },
  ]}
/>

## Фрагмент 4 — баг, который тест может пропустить

```js
function average(numbers) {
  let sum = 0;
  for (let i = 0; i < numbers.length; i++) {
    sum += numbers[i];
  }
  return sum / numbers.length;
}
```

<Quiz
  id="01-thinking-complexity-code-4"
  lessonSlug="01-thinking-complexity"
  lang="ru"
  question="Сложность в порядке — время O(n), память O(1). Но какой крайний случай ломает корректность и почему это здесь важно?"
  choices={[
    { label: "Пустой массив: цикл не выполняется ни разу, затем возвращается 0/0 = NaN. Спецификация обязана сказать, что возвращать для пустого входа; тестируя лишь непустые списки, это никогда не вскроешь.", correct: true },
    { label: "Очень большой массив, потому что цикл O(n) уйдёт в таймаут.", misconception: "O(n) осуществим для огромных входов — это не баг корректности. Дефект — случай пустого массива, дающий NaN, независимо от размера." },
    { label: "Отрицательные числа, потому что sum может стать отрицательным.", misconception: "Отрицательное среднее — вполне допустимый результат для отрицательных входов — никакого бага. Настоящий крайний случай — пустой массив с делением на ноль." },
    { label: "Бага нет; функция прошла все тесты, что запускал автор.", misconception: "Прохождение тестов — не доказательство корректности. Автор просто не тестировал пустой массив, где длина 0 делает деление NaN." },
  ]}
/>

<Recap lang="ru">
Снимать сложность с кода — фиксированный приём: найди доминирующую работу и оставь только этот член; перемножай вложенные циклы и складывай последовательные фазы; считай структуры памяти, а не итерации цикла, для памяти; и помни, что замкнутая формула может заменить цикл O(n) на O(1). Затем проверь вторую ось — алгоритм с идеальной Big-O всё ещё может быть неверен на крайнем случае (пустой вход, деление на ноль), которого твои тесты не коснулись.
</Recap>
