---
slug: 01-what-is-an-algorithm
lang: ru
track: algorithms
unit: 01-thinking-complexity
order: 1
title: "Что такое алгоритм?"
summary: "Алгоритм — это конечный список однозначных шагов, который превращает вход в выход."
estMin: 16
status: ready
prereqs: []
mathPrereqs: []
concepts: ["algorithm", "input_output", "step_count"]
sources:
  - https://en.wikipedia.org/wiki/Algorithm
  - https://developer.mozilla.org/en-US/docs/Glossary/Algorithm
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Idea from "~/components/lesson/Idea.astro";
import Code from "~/components/lesson/Code.astro";
import Trace from "~/components/lesson/Trace.astro";
import Complexity from "~/components/lesson/Complexity.astro";
import Check from "~/components/lesson/Check.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import AnnotatedCode from "~/components/algo/AnnotatedCode.astro";
import AlgoTrace from "~/components/algo/AlgoTrace.astro";
import CodeRunner from "~/components/algo/CodeRunner.astro";
import AlgoPractice from "~/components/algo/AlgoPractice.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Ты следуешь рецепту, чтобы испечь торт. Каждый шаг — смешай муку, добавь яйца, пеки при 350°F в течение 30 минут — это однозначная инструкция. Рецепт работает для каждого, потому что не нужно угадывать. Вот что такое алгоритм в реальной жизни.
</Hook>

<Goal lang="ru">
После этого урока ты можешь дать точное определение алгоритма, определить вход и выход простого алгоритма и посчитать, сколько шагов нужно, чтобы решить задачу.
</Goal>

<Idea lang="ru">
**Алгоритм** — это конечный список однозначных шагов, который превращает вход в выход. Слово «однозначный» — здесь самое важное: каждый шаг должен быть настолько ясным, что компьютер (или человек) мог бы его выполнить без всяких недоразумений. Слово «конечный» означает, что шаги должны когда-нибудь закончиться; алгоритм, который зацикливается навеки, — это не алгоритм.

Посмотри на реальный алгоритм: «Найди наибольшее число в списке». Ты начинаешь со входа — списка чисел. Ты кончаешь с выходом — наибольшим из них. Шаги посредине — это алгоритм.

<AnnotatedCode
  code={`function largestOf(numbers) {
  let largest = numbers[0];
  for (let i = 1; i < numbers.length; i++) {
    if (numbers[i] > largest) {
      largest = numbers[i];
    }
  }
  return largest;
}`}
  annotations={[
    { line: 1, text: "Вход: список чисел" },
    { line: 5, text: "Ключевой шаг: сравни каждое число с текущим наибольшим" },
    { line: 8, text: "Выход: вернули найденное наибольшее число" }
  ]}
/>

Каждый алгоритм имеет вход (с чего ты начинаешь) и выход (чем ты кончаешь). Шаги посредине превращают вход в выход. Если шаги однозначны и конечны, это алгоритм.
</Idea>

<Code lang="ru">
Алгоритм «найди наибольшее число в списке» встречается так часто, что мы напишем его на JavaScript. Вот логика пошагово:

1. Предположи, что первое число — наибольшее.
2. Посмотри на каждое оставшееся число.
3. Если число больше текущего наибольшего, запомни его как новое наибольшее.
4. Когда достигнешь конца списка, верни наибольшее.

Вот функция:

```js
function largestOf(numbers) {
  let largest = numbers[0];
  for (let i = 1; i < numbers.length; i++) {
    if (numbers[i] > largest) {
      largest = numbers[i];
    }
  }
  return largest;
}
```

Попробуй запустить её со списком `[3, 9, 2, 7]`. Функция:
1. Начнёт с `largest = 3` (первое число).
2. Увидит `9` — оно больше, поэтому `largest` становится `9`.
3. Увидит `2` — оно меньше `9`, поэтому пропусти его.
4. Увидит `7` — оно меньше `9`, поэтому пропусти его.
5. Вернёт `9`.

<CodeRunner
  id="cr-largest"
  lang="ru"
  code={`function largestOf(numbers) {
  let largest = numbers[0];
  for (let i = 1; i < numbers.length; i++) {
    if (numbers[i] > largest) {
      largest = numbers[i];
    }
  }
  return largest;
}

console.log(largestOf([3, 9, 2, 7]));`}
/>
</Code>

<Trace lang="ru">
Смотри, как алгоритм выполняется пошагово на списке `[3, 9, 2, 7]`:

<AlgoTrace
  id="trace-largest"
  lang="ru"
  code={[
    "function largestOf(numbers) {",
    "  let largest = numbers[0];",
    "  for (let i = 1; i < numbers.length; i++) {",
    "    if (numbers[i] > largest) {",
    "      largest = numbers[i];",
    "    }",
    "  }",
    "  return largest;",
    "}"
  ]}
  frames={[
    {
      caption: "Начало: присвой largest первому числу, 3.",
      line: 2,
      cells: ["3", "9", "2", "7"].map((val, i) => i === 0 ? { value: val, mark: "active" } : val)
    },
    {
      caption: "Первая итерация цикла: сравни 9 с largest (3). 9 больше, поэтому largest становится 9.",
      line: 4,
      cells: ["3", { value: "9", mark: "active" }, "2", "7"]
    },
    {
      caption: "После сравнения largest обновлен на 9.",
      line: 5,
      cells: ["3", { value: "9", mark: "done" }, "2", "7"]
    },
    {
      caption: "Вторая итерация цикла: сравни 2 с largest (9). 2 меньше, поэтому пропусти.",
      line: 4,
      cells: ["3", "9", { value: "2", mark: "active" }, "7"]
    },
    {
      caption: "Третья итерация цикла: сравни 7 с largest (9). 7 меньше, поэтому пропусти.",
      line: 4,
      cells: ["3", "9", "2", { value: "7", mark: "active" }]
    },
    {
      caption: "Готово: вернули наибольшее число, 9.",
      line: 8,
      cells: ["3", { value: "9", mark: "done" }, "2", "7"]
    }
  ]}
/>
</Trace>

<Complexity lang="ru">
Как быстро работает алгоритм? Посчитай шаги: алгоритм посещает каждое из n чисел ровно один раз. Для списка из 4 чисел цикл работает 3 раза (мы пропускаем первое). Для списка из 100 чисел цикл работает 99 раз. Для списка из n чисел цикл работает n − 1 раз.

Мы говорим, что алгоритм работает за **O(n) время**, что означает число шагов растёт пропорционально размеру входа. Если список удвоится в размере, число шагов примерно удвоится.

Другие алгоритмы поиска наибольшего медленнее. Например, если ты сравнишь каждое число со всеми остальными (проверяя, A > B, B > A и так далее), тебе понадобится n × n сравнений — **O(n²) время**. Для больших списков это намного хуже.

Ключевая идея: число шагов — это стоимость алгоритма.
</Complexity>

<AlgoPractice
  id="practice-algo-1"
  lessonSlug="01-what-is-an-algorithm"
  lang="ru"
  problems={[
    {
      kind: "numeric",
      prompt: "Если алгоритм largestOf работает на списке из 10 чисел, сколько сравнений он делает?",
      answer: 9,
      hint: "Алгоритм сравнивает каждое из оставшихся n − 1 чисел с текущим наибольшим."
    },
    {
      kind: "numeric",
      prompt: "Сколько сравнений для списка из 50 чисел?",
      answer: 49,
      hint: "На одно меньше, чем размер списка."
    },
    {
      kind: "choice",
      prompt: "Какой из этих вариантов — это алгоритм?",
      choices: [
        "Рецепт с шагами типа 'добавь немного муки'",
        "Последовательность однозначных шагов, которая заканчивается результатом",
        "Любой код, написанный на JavaScript",
        "Программа, которая может зацикливаться бесконечно"
      ],
      answer: 1,
      hint: "Алгоритм должен иметь однозначные шаги и должен закончиться за конечное время."
    },
    {
      kind: "choice",
      prompt: "Какова временная сложность алгоритма largestOf?",
      choices: [
        "O(1) — константное время",
        "O(n) — линейное время",
        "O(n²) — квадратичное время",
        "O(log n) — логарифмическое время"
      ],
      answer: 1,
      hint: "Число шагов растёт пропорционально размеру списка."
    },
    {
      kind: "order",
      prompt: "Расставь эти шаги в правильном порядке для алгоритма largestOf:",
      items: [
        "Присвой largest первому числу",
        "Для каждого оставшегося числа проверь, больше ли оно текущего largest",
        "Если число больше, обнови largest",
        "Вернули largest"
      ],
      hint: "Начни с установки начального largest, затем пройдись по списку, обновляй при необходимости, затем верни результат."
    }
  ]}
/>

<Inset kind="edgecase" lang="ru">
Что если список пуст? Алгоритм упадёт при попытке доступа к `numbers[0]`. Более надёжная версия сначала проверяет, пуст ли список, и обрабатывает это — например, возвращая null или выкидывая ошибку. Реальные алгоритмы должны обрабатывать граничные случаи вроде этого.
</Inset>

<Check lang="ru">
  <Quiz
    id="quiz-algo-1"
    pieceSlug="01-what-is-an-algorithm"
    lang="ru"
    question="Почему важно, чтобы шаги в алгоритме были однозначными?"
    choices={[
      {
        label: "Чтобы кто угодно или любой компьютер мог следовать шагам без угадывания",
        correct: true
      },
      {
        label: "Чтобы алгоритм всегда нашёл правильный ответ",
        misconception: "Алгоритм может быть однозначным и всё равно дать неправильный ответ, если он плохо спроектирован. Однозначность означает ясность каждого шага, а не корректность алгоритма."
      },
      {
        label: "Чтобы алгоритм работал как можно быстрее",
        misconception: "Однозначность — это вопрос ясности, не скорости. Медленный алгоритм может быть однозначным."
      },
      {
        label: "Чтобы алгоритм можно было написать на любом языке программирования",
        misconception: "Однозначные шаги можно написать на многих языках, но сама однозначность — это вопрос логики, а не выбора языка."
      }
    ]}
  />
</Check>

<Recap lang="ru">
**Алгоритм** — это конечный список однозначных шагов, который превращает вход в выход. Каждый алгоритм имеет стоимость, измеряемую числом шагов, которые он требует. Мы используем **нотацию большого-O** (вроде O(n) или O(n²)), чтобы описать, как число шагов растёт с ростом размера входа. Алгоритм largestOf ищет в списке за O(n) время — посещая каждый элемент один раз.
</Recap>
