---
concepts:
- dominant-term
- big-o
- complexity-classes
- time-space-tradeoff
- reading-constraints
- loop-invariant
estMin: 13
lang: ru
level: middle
order: 7
prereqs: []
slug: quiz-choice
sources:
- https://en.wikipedia.org/wiki/Big_O_notation
- https://www.geeksforgeeks.org/dsa/analysis-algorithms-big-o-analysis/
- https://codeforces.com/blog/entry/21344
status: ready
summary: "Тест с выбором на синтез по всему юниту — доминирующий член, классы Big-O, компромисс время-память и чтение ограничений для выбора подхода."
title: 'Мышление и сложность: тест с выбором ответа'
track: algorithms
unit: 01-thinking-complexity
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Шесть вопросов поперёк всего юнита. Ни один из них — не определение для заучивания: каждый отражает решение, которое ты принимаешь, оценивая алгоритм ещё до того, как его запустишь.
</Hook>

<Goal lang="ru">
Убедись, что связываешь подсчёт шагов, Big-O, классы сложности, компромисс время-память и входные ограничения в одно суждение: вот код и вот лимиты — сколько это стоит и достаточно ли быстро?
</Goal>

<Quiz
  id="01-thinking-complexity-choice-1"
  lessonSlug="01-thinking-complexity"
  lang="ru"
  question="Алгоритм выполняет ровно 5n + n²/2 + 1000 базовых операций на входе размера n. Какова его Big-O и почему?"
  choices={[
    { label: "O(n²) — при больших n доминирует член n²/2; 5n и +1000 растут медленнее (или вовсе не растут) и становятся пренебрежимыми, а константа 1/2 не меняет форму роста.", correct: true },
    { label: "O(n²/2) — оставим константу, ведь она часть подсчёта.", misconception: "Big-O отбрасывает константные множители. 1/2 не меняет, как масштабируется стоимость при удвоении n, поэтому O(n²/2) и O(n²) — один класс; пишем простейшую форму, O(n²)." },
    { label: "O(n² + n) — обе петли дают вклад, поэтому оба члена остаются.", misconception: "Члены младшего порядка отбрасываются, а не суммируются в нотации. При больших n n² подавляет n, поэтому член n пренебрежим, и ответ — просто O(n²)." },
    { label: "O(1000) — константа 1000 — самое большое число при малых n.", misconception: "Big-O — про рост при больших n, а не про значение при малых. +1000 — фиксированная стоимость, которая никогда не растёт, поэтому она исчезает против n² для любого заметного входа." },
  ]}
/>

<Quiz
  id="01-thinking-complexity-choice-2"
  lessonSlug="01-thinking-complexity"
  lang="ru"
  question="В задаче указано n ≤ 1 000 000 при лимите в 1 секунду. Твоя первая идея — вложенный цикл O(n²). Прочитай ограничение — что оно говорит ещё до написания кода?"
  choices={[
    { label: "O(n²) исключается: (10⁶)² = 10¹² операций против бюджета ~10⁸/сек — примерно в 10 000 раз больше. Нужно дойти до O(n log n) или O(n), поэтому переделай (hash set, сортировка + два указателя) до кодирования.", correct: true },
    { label: "Всё равно попробуй O(n²) и оптимизируй константы внутреннего цикла, если будет медленно.", misconception: "Никакая настройка констант не закроет разрыв в 10 000 раз. Когда ограничение исключает класс сложности, меняют алгоритм, а не константы." },
    { label: "Ограничение про память, а не про время, поэтому оно ничего не говорит о том, какую Big-O брать.", misconception: "Граница входа n прежде всего ограничивает бюджет времени: ~10⁸ опер/сек × лимит. n ≤ 10⁶ напрямую отображается в «требуется O(n log n) или лучше»." },
    { label: "n ≤ 10⁶ означает, что любой полиномиальный алгоритм подойдёт, раз он не экспоненциальный.", misconception: "Полиномиальный — не автоматически реализуемый. O(n²) полиномиален и всё равно 10¹² операций здесь — далеко за бюджетом. Граница выбирает конкретный класс, а не просто «полином против экспоненты»." },
  ]}
/>

<Quiz
  id="01-thinking-complexity-choice-3"
  lessonSlug="01-thinking-complexity"
  lang="ru"
  question="Чтобы найти дубликат, можно взять вложенный цикл (время O(n²), память O(1)) или hash set (время O(n), память O(n)). На сервере с запасом памяти и пользователями, ждущими ответ, что выберешь и почему?"
  choices={[
    { label: "Hash set. Потратить O(n) памяти, чтобы снизить время с O(n²) до O(n), — классический компромисс время-память, который окупается, когда память доступна, а задержка важна.", correct: true },
    { label: "Вложенный цикл — он использует меньше памяти, а меньше памяти всегда лучше.", misconception: "Меньше памяти лучше только когда память — связывающее ограничение (embedded, мобильные). Здесь памяти вдоволь, а задержка важна, поэтому обмен памяти на ускорение O(n²)→O(n) — верный выбор." },
    { label: "Любой из двух — у них одинаковая сложность по времени, разницы нет.", misconception: "Неодинаковая. Вложенный цикл — O(n²); hash set — O(n). Для больших n это разница между триллионом и миллионом операций." },
    { label: "Hash set, потому что он использует меньше памяти, чем вложенный цикл.", misconception: "Наоборот: hash set использует БОЛЬШЕ памяти (O(n) против O(1)). Его выбирают несмотря на лишнюю память, потому что он покупает большую экономию времени — это и есть компромисс." },
  ]}
/>

<Quiz
  id="01-thinking-complexity-choice-4"
  lessonSlug="01-thinking-complexity"
  lang="ru"
  question="В функции один цикл, выполняющийся n раз, и внутри него вызывается бинарный поиск (O(log n)) по отсортированному массиву. Какова её сложность по времени?"
  choices={[
    { label: "O(n log n) — цикл выполняется n раз, и каждая итерация делает O(log n) работы, поэтому стоимости перемножаются: n × log n.", correct: true },
    { label: "O(n + log n) — один цикл плюс один поиск, поэтому складываем.", misconception: "Сложности складывают только для последовательных независимых фаз. Здесь поиск ВНУТРИ цикла, поэтому выполняется по разу на итерацию — стоимости перемножаются, давая O(n log n)." },
    { label: "O(n) — бинарный поиск настолько быстр, что считается константой.", misconception: "log n не константа; он растёт с n (медленно). Его нельзя свести к O(1). Работа O(log n) на итерацию через n итераций — это O(n log n)." },
    { label: "O(n²) — цикл с чем угодно внутри квадратичен.", misconception: "Квадратичность даёт цикл, чья внутренняя работа O(n), то есть ещё один полный проход по n. Здесь внутренняя работа O(log n), а не O(n), поэтому это O(n log n), много ниже O(n²)." },
  ]}
/>

<Quiz
  id="01-thinking-complexity-choice-5"
  lessonSlug="01-thinking-complexity"
  lang="ru"
  question="Джуниор пишет largestOf с `let largest = 0` вместо `let largest = numbers[0]`, тестирует на нескольких списках из положительных чисел и выкатывает. Какой более глубокий урок, когда функция возвращает 0 для [-5, -3, -1]?"
  choices={[
    { label: "Прохождение тестов — не доказательство. Инвариант цикла «largest хранит максимум среди уже просмотренных элементов» ложен с самого начала при largest=0, поэтому алгоритм некорректен на полностью отрицательных входах — крайний случай, который выбранные тесты упустили.", correct: true },
    { label: "Это проблема производительности — инициализация нулём замедляет цикл.", misconception: "Инициализация не меняет число шагов; цикл всё равно делает n−1 итераций. Это баг корректности, а не сложности — функция возвращает неверный ответ." },
    { label: "Тесты были в порядке; -5, -3, -1 — просто необычный вход, который не обязан обрабатываться.", misconception: "Полностью отрицательные списки — допустимые входы, которые спецификация обязана покрывать. Аргумент через инвариант цикла ловит баг для каждого класса входов, не перечисляя их как тесты." },
    { label: "Анализ Big-O поймал бы это ещё до запуска тестов.", misconception: "Big-O измеряет рост стоимости, а не корректность. Неверный и верный алгоритм могут иметь одинаковую O(n). Корректность доказывают инвариантами и разбором крайних случаев, а не Big-O." },
  ]}
/>

<Quiz
  id="01-thinking-complexity-choice-6"
  lessonSlug="01-thinking-complexity"
  lang="ru"
  question="Две процедуры сортировки обе «корректны». Merge sort — время O(n log n) / память O(n); bubble sort — время O(n²) / память O(1). Для сортировки 1 000 000 записей на обычной машине как верно это прочитать?"
  choices={[
    { label: "Брать merge sort. При n = 10⁶ O(n log n) — это ~2×10⁷ операций, а O(n²) — ~10¹², примерно в 50 000 раз больше. Дополнительная O(n) память дёшева рядом с таким разрывом времени, поэтому компромисс в пользу merge sort.", correct: true },
    { label: "Брать bubble sort — он использует O(1) памяти, а экономия памяти всегда побеждает.", misconception: "На миллионе записей 10¹² операций bubble sort безнадёжно медленны. Экономия O(1) памяти неважна, когда стоимость времени в 50 000 раз хуже, а память не является ограничением." },
    { label: "Они взаимозаменяемы, ведь оба правильно сортируют массив.", misconception: "Оба корректны, но корректность ничего не говорит о стоимости. Разрыв O(n log n) против O(n²) — это и есть суть: на масштабе только один завершится за разумное время." },
    { label: "Bubble sort, потому что O(n²) нормально для n до миллиона.", misconception: "O(n²) практичен только до нескольких тысяч элементов. При 10⁶ это ~10¹² операций — далеко за пределами осуществимого, поэтому и существуют сортировки O(n log n)." },
  ]}
/>

<Recap lang="ru">
Сквозная линия юнита — одна привычка: до запуска оцени стоимость. Подсчитай шаги, оставь только доминирующий член, назови класс Big-O, взвесь время против памяти под своё реальное ограничение и дай границе входа подсказать, какой класс вообще допустим. И никогда не путай стоимость с корректностью — Big-O измеряет, насколько быстро, а инвариант цикла доказывает, верно ли это вообще.
</Recap>
