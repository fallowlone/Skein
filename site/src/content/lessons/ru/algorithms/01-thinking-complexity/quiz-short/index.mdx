---
concepts:
- dominant-term
- big-o
- complexity-classes
- time-space-tradeoff
- reading-constraints
- correctness
estMin: 14
lang: ru
level: middle
order: 8
prereqs: []
slug: quiz-short
sources:
- https://en.wikipedia.org/wiki/Big_O_notation
- https://www.geeksforgeeks.org/dsa/analysis-algorithms-big-o-analysis/
- https://codeforces.com/blog/entry/21344
status: ready
summary: "Вопросы на свободное припоминание по всему юниту. Сначала ответь своими словами, затем открой образцовый ответ и сравни."
title: 'Мышление и сложность: тест на припоминание'
track: algorithms
unit: 01-thinking-complexity
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";

<Hook>
Припоминание сильнее перечитывания. На каждый вопрос проговори или запиши полный ответ по памяти, прежде чем открыть образцовый — именно усилие извлечения закрепляет идею.
</Hook>

<Goal lang="ru">
Восстанови костяк юнита — зачем считаем шаги, что Big-O сохраняет и отбрасывает, лестницу классов сложности, компромисс время-память и как ограничения выбирают подход — не подсматривая в уроки.
</Goal>

<RetrievalDrawer
  client:visible
  id="01-thinking-complexity-short-retrieval"
  lang="ru"
  questions={[
    { q: "Почему мы измеряем алгоритм его темпом роста (Big-O), а не реальным временем по часам?", a: "Время по часам зависит от машины, языка, компилятора и входа — запусти тот же код на более быстром CPU, и он ускорится, не став лучшим алгоритмом. Big-O измеряет нечто машинно-независимое: как число базовых операций растёт с ростом размера входа n. Именно это свойство решает, выживет ли алгоритм на масштабе. Быстрый компьютер покупает тебе константный множитель; лучший класс сложности покупает порядки величины. Поэтому мы считаем шаги как функцию от n и описываем, как этот счёт растёт, ведь рост доминирует, как только n велико." },
    { q: "Сформулируй правило нахождения Big-O по числу шагов и примени его к 3n² + 100n + 50.", a: "Оставь только быстрее всего растущий (доминирующий) член и отбрось его константный множитель; члены младшего порядка и аддитивные константы исчезают, потому что при больших n они пренебрежимы против доминирующего члена. В 3n² + 100n + 50 доминирующий член — 3n²; отбрось 100n и +50 (младший порядок) и отбрось 3 (константу), остаётся O(n²). Интуиция: при n = 10 000 3n² — это 300 миллионов, тогда как 100n — миллион, а 50 — ничто, поэтому член n² задаёт форму кривой." },
    { q: "Как комбинировать сложности для последовательного кода и для вложенных циклов и почему разница так велика?", a: "Последовательные независимые фазы складываются: проход O(n), за которым следует ещё один проход O(n), — это O(n) + O(n) = O(n) (оставляешь доминирующий член). Вложенные циклы перемножаются: цикл из n итераций с внутренним циклом из n итераций делает n × n = O(n²) работы. Разница огромна, потому что сложение держит тебя в том же классе, а вложенность поднимает на класс выше — два последовательных прохода по миллиону элементов это ~2 миллиона операций, но один проход, вложенный в другой, это ~10¹² операций. Всегда спрашивай, происходит работа после фазы (складывать) или внутри неё (умножать)." },
    { q: "Назови распространённые классы сложности от быстрейшего к медленнейшему и где практический обрыв.", a: "От быстрейшего к медленнейшему: O(1) константная, O(log n) логарифмическая, O(n) линейная, O(n log n) линеарифмическая, O(n²) квадратичная, O(2ⁿ) экспоненциальная, O(n!) факториальная. Практический обрыв лежит между O(n log n) и O(n²): от O(1) до O(n log n) комфортно обрабатывают миллион элементов; O(n²) годится лишь до нескольких тысяч; O(2ⁿ) — лишь до n ≈ 30; O(n!) — лишь до n ≈ 10. Переход от полиномиального (степени n) к суперполиномиальному (2ⁿ, n!) — граница между «решаемо на масштабе» и «только крошечные входы»." },
    { q: "Объясни компромисс время-память на примере поиска дубликата и как решить, какую сторону взять.", a: "Поиск дубликата двумя способами: вложенный цикл сравнивает каждую пару — время O(n²), но память O(1); hash set запоминает всё увиденное — время O(n), но память O(n). Ты тратишь память, чтобы купить скорость, или принимаешь медленное время, чтобы сэкономить память. Решение определяет связывающее ограничение: на сервере с запасом RAM и ждущим пользователем бери hash set (скорость важна, память дёшева); на embedded-устройстве с крошечной RAM бери вложенный цикл (память — стена, медлительность терпима). Универсально «лучшего» выбора нет — есть лишь тот, что подходит под ограничение, с которым ты реально столкнулся." },
    { q: "Как входное ограничение n подсказывает, какую сложность брать, с одним разобранным примером?", a: "Компьютер делает примерно 10⁸ простых операций в секунду, а судьи дают ~1–2 секунды, поэтому твой бюджет — ~10⁸–2×10⁸ операций. Сравни число операций кандидата с этим бюджетом. Пример: в задаче n ≤ 1 000 000. Идея O(n²) была бы (10⁶)² = 10¹² операций — примерно в 10 000 раз за бюджетом, поэтому она уходит в таймаут и исключается ещё до написания. O(n log n) — это ~2×10⁷, а O(n) — ~10⁶, оба комфортно в бюджете. Так ограничение вынуждает спроектировать решение O(n log n)-или-лучше (сортировка + два указателя, либо hash set), превращая «с чего начать?» в суженную цель." },
  ]}
/>

<Recap lang="ru">
Если ты смог восстановить каждый ответ по памяти, ты держишь костяк юнита: мы измеряем рост, а не время по часам; Big-O сохраняет доминирующий член и отбрасывает константы; последовательные фазы складываются, а вложенные циклы перемножаются; лестница классов идёт от O(1) до O(n!) с практическим обрывом на O(n²); время и память торгуются между собой, и решает связывающее ограничение; а граница входа n напрямую отображается в сложность, которую нужно достичь. Оценивай стоимость до того, как что-либо запустишь.
</Recap>
