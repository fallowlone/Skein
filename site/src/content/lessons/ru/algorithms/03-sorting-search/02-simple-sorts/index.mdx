---
concepts:
- selection_sort
- insertion_sort
- bubble_sort
- adaptive_sort
- swap
estMin: 19
lang: ru
mathPrereqs: []
order: 2
prereqs:
- 01-why-sort
slug: 02-simple-sorts
sources:
- https://en.wikipedia.org/wiki/Selection_sort
- https://en.wikipedia.org/wiki/Insertion_sort
- https://en.wikipedia.org/wiki/Bubble_sort
- https://www.geeksforgeeks.org/dsa/adaptive-and-non-adaptive-sorting-algorithms/
status: ready
summary: 'Сортировка выбором, вставкой и пузырьком — всё O(n²). Выбор всегда ищет
  минимум. Вставка адаптивна: O(n) на почти отсортированных данных. Все работают на
  месте, в O(1) памяти. Настоящие системы берут O(n log n).'
title: Простые сортировки
track: algorithms
unit: 03-sorting-search
---



import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Idea from "~/components/lesson/Idea.astro";
import Code from "~/components/lesson/Code.astro";
import Trace from "~/components/lesson/Trace.astro";
import Complexity from "~/components/lesson/Complexity.astro";
import Check from "~/components/lesson/Check.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import AnnotatedCode from "~/components/algo/AnnotatedCode.astro";
import AlgoTrace from "~/components/algo/AlgoTrace.astro";
import CodeRunner from "~/components/algo/CodeRunner.astro";
import AlgoPractice from "~/components/algo/AlgoPractice.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DiagramFrame from "~/components/diagram/DiagramFrame.astro";

<Hook>
Ты хочешь отсортировать массив. Быстрые алгоритмы (сортировка слиянием, быстрая сортировка) сложные: раздели массив, объедини или разбей, рекурсия. Прежде чем их учить, поймёшь простые: сортировка выбором, вставкой и пузырьком. Все работают за O(n²) — вложенный цикл в цикле. Медленные на больших данных, но быстро кодируются и просто понимаются. И вставка имеет тайну: когда массив почти отсортирован, она работает за O(n). Выучи эти, и быстрые станут понятны.
</Hook>

<Goal lang="ru">
После этого урока ты разбираешься в трёх простых алгоритмах сортировки (выбором, вставкой, пузырьком), почему все O(n²) (вложенные проходы), как вставка адаптируется к O(n) на почти отсортированных данных, почему все на месте и O(1) памяти, и когда их безопасно применить (маленькие массивы или предсортированные данные) против O(n log n).
</Goal>

<Idea lang="ru">
**Суть: вложенные циклы = O(n²)**

Все простые сортировки используют вложенную структуру: внешний цикл проходов и внутренний для поиска/постановки одного элемента. n проходов × n сравнений за проход = O(n²).

**Сортировка выбором: повторно ищи минимум**

Сортировка выбором делит массив на две части: отсортированный префикс (слева) и неотсортированный суффикс (справа). На каждом проходе ищешь минимум в суффиксе и переносишь его в конец префикса.

```js
function selectionSort(arr) {
  for (let i = 0; i < arr.length; i++) {
    let minIndex = i;
    for (let j = i + 1; j < arr.length; j++) {
      if (arr[j] < arr[minIndex]) minIndex = j;
    }
    [arr[i], arr[minIndex]] = [arr[minIndex], arr[i]];
  }
}
```

Почему O(n²)? На проходе i ты сканируешь n - i элементов ища минимум. Всего: n + (n-1) + (n-2) + ... + 1 = n(n+1)/2 = O(n²), без разницы на порядок входа.

**Сортировка вставкой: растёшь отсортированный префикс**

Сортировка вставкой строит отсортированный массив слева направо, по одному элементу. Для каждого нового элемента сканируешь префикс справа налево, сдвигаешь большие элементы вправо и вставляешь новый в правильное место.

```js
function insertionSort(arr) {
  for (let i = 1; i < arr.length; i++) {
    const key = arr[i];
    let j = i - 1;
    while (j >= 0 && arr[j] > key) {
      arr[j + 1] = arr[j];
      j--;
    }
    arr[j + 1] = key;
  }
}
```

Почему O(n²) в худшем случае? Когда массив развёрнут (худший случай), каждая вставка требует сдвига всех предыдущих элементов. Но вот магия: **вставка адаптивна**. Если массив уже отсортирован или почти отсортирован, цикл while не запускается (или один раз), так что алгоритм за O(n).

**Сортировка пузырьком: повторно меняешь соседей**

Сортировка пузырьком повторно ходит по массиву и меняет соседние элементы если они не в порядке. Большие элементы "всплывают" в конец. После каждого прохода самый правый неотсортированный на месте.

```js
function bubbleSort(arr) {
  for (let i = 0; i < arr.length; i++) {
    for (let j = 0; j < arr.length - i - 1; j++) {
      if (arr[j] > arr[j + 1]) {
        [arr[j], arr[j + 1]] = [arr[j + 1], arr[j]];
      }
    }
  }
}
```

Тоже O(n²) в худшем. Проход за проходом, каждый сравнивает соседей. Пузырьёк медленнее всех трёх на практике — больше сравнений и обменов чем вставка.

**Почему все O(n²) и на месте**

Все три O(1) памяти — только переменные-индексы, временная для обмена. Все O(n²) в худшем из-за вложенного цикла. Но вставка особая: адаптивна.
</Idea>

<DiagramFrame label="Сортировка вставкой массива 5, 3, 4, 1, нарисованная как четыре строки-снимка. В каждой строке отсортированный префикс слева (подсвечен) и растёт на одну ячейку вниз. Следующий неотсортированный элемент — ключ, обведённый — сравнивается справа налево с префиксом; каждый элемент префикса больше ключа сдвигается на одну ячейку вправо (штриховые стрелки), освобождая слот, затем ключ занимает этот слот. Проход 1 вставляет 3 за 1 сравнение; проход 2 вставляет 4 за 2 сравнения; проход 3 вставляет 1 за 3 сравнения. Число сравнений растёт 1, 2, 3 по мере удлинения префикса — повторное поэлементное сканирование, из-за которого вставка работает за O(n в квадрате) в худшем случае." hue="var(--d-systems)" caption="Простая сортировка в действии. Отсортированный префикс (подсвечен) растёт на один элемент за проход; каждый новый ключ вставляется сдвигом всех больших элементов префикса вправо. Сравнения растут 1, 2, 3 по мере удлинения префикса — повторное сканирование за O(n в квадрате).">
  <svg viewBox="0 0 640 360" width="640" height="360" style="display:block;margin:0 auto;max-width:100%;height:auto;">
    <text x="20" y="28" text-anchor="start" font-size="11" font-family="var(--font-body)" fill="var(--muted)">префикс растёт</text>
    <text x="372" y="28" text-anchor="start" font-size="11" font-family="var(--font-body)" fill="var(--muted)">сравнений за проход</text>

    <text x="20" y="74" text-anchor="start" font-size="11" font-family="var(--font-body)" fill="var(--ink)">старт</text>
    <rect x="96" y="46" width="56" height="48" rx="6" fill="var(--card-2)" stroke="var(--ok)" stroke-width="1.6" />
    <text x="124" y="76" text-anchor="middle" font-size="16" font-family="var(--font-body)" fill="var(--ink)">5</text>
    <rect x="160" y="46" width="56" height="48" rx="6" fill="var(--card)" stroke="var(--hairline-strong)" stroke-width="1.2" />
    <text x="188" y="76" text-anchor="middle" font-size="16" font-family="var(--font-body)" fill="var(--ink)">3</text>
    <rect x="224" y="46" width="56" height="48" rx="6" fill="var(--card)" stroke="var(--hairline-strong)" stroke-width="1.2" />
    <text x="252" y="76" text-anchor="middle" font-size="16" font-family="var(--font-body)" fill="var(--ink)">4</text>
    <rect x="288" y="46" width="56" height="48" rx="6" fill="var(--card)" stroke="var(--hairline-strong)" stroke-width="1.2" />
    <text x="316" y="76" text-anchor="middle" font-size="16" font-family="var(--font-body)" fill="var(--ink)">1</text>
    <text x="372" y="76" text-anchor="start" font-size="11" font-family="var(--font-body)" fill="var(--muted)">префикс = {"{5}"}</text>

    <text x="20" y="154" text-anchor="start" font-size="11" font-family="var(--font-body)" fill="var(--ink)">проход 1</text>
    <text x="20" y="170" text-anchor="start" font-size="10" font-family="var(--font-body)" fill="var(--muted)">ключ = 3</text>
    <rect x="96" y="126" width="56" height="48" rx="6" fill="var(--card-2)" stroke="var(--ok)" stroke-width="1.6" />
    <text x="124" y="156" text-anchor="middle" font-size="16" font-family="var(--font-body)" fill="var(--ink)">5</text>
    <rect x="160" y="126" width="56" height="48" rx="6" fill="var(--card-2)" stroke="var(--d-systems)" stroke-width="1.8" />
    <text x="188" y="156" text-anchor="middle" font-size="16" font-family="var(--font-body)" fill="var(--ink)">3</text>
    <rect x="224" y="126" width="56" height="48" rx="6" fill="var(--card)" stroke="var(--hairline-strong)" stroke-width="1.2" />
    <text x="252" y="156" text-anchor="middle" font-size="16" font-family="var(--font-body)" fill="var(--ink)">4</text>
    <rect x="288" y="126" width="56" height="48" rx="6" fill="var(--card)" stroke="var(--hairline-strong)" stroke-width="1.2" />
    <text x="316" y="156" text-anchor="middle" font-size="16" font-family="var(--font-body)" fill="var(--ink)">1</text>
    <path d="M124 116 Q156 96 188 116" fill="none" stroke="var(--d-systems)" stroke-width="1.8" stroke-dasharray="5 4" marker-end="url(#shiftR)" />
    <text x="372" y="150" text-anchor="start" font-size="11" font-family="var(--font-body)" fill="var(--ink)">5 &gt; 3: сдвинуть 5 вправо</text>
    <text x="372" y="168" text-anchor="start" font-size="11" font-family="var(--font-body)" fill="var(--d-systems)">1 сравнение</text>

    <text x="20" y="234" text-anchor="start" font-size="11" font-family="var(--font-body)" fill="var(--ink)">проход 2</text>
    <text x="20" y="250" text-anchor="start" font-size="10" font-family="var(--font-body)" fill="var(--muted)">ключ = 4</text>
    <rect x="96" y="206" width="56" height="48" rx="6" fill="var(--card-2)" stroke="var(--ok)" stroke-width="1.6" />
    <text x="124" y="236" text-anchor="middle" font-size="16" font-family="var(--font-body)" fill="var(--ink)">3</text>
    <rect x="160" y="206" width="56" height="48" rx="6" fill="var(--card-2)" stroke="var(--d-systems)" stroke-width="1.8" />
    <text x="188" y="236" text-anchor="middle" font-size="16" font-family="var(--font-body)" fill="var(--ink)">4</text>
    <rect x="224" y="206" width="56" height="48" rx="6" fill="var(--card-2)" stroke="var(--ok)" stroke-width="1.6" />
    <text x="252" y="236" text-anchor="middle" font-size="16" font-family="var(--font-body)" fill="var(--ink)">5</text>
    <rect x="288" y="206" width="56" height="48" rx="6" fill="var(--card)" stroke="var(--hairline-strong)" stroke-width="1.2" />
    <text x="316" y="236" text-anchor="middle" font-size="16" font-family="var(--font-body)" fill="var(--ink)">1</text>
    <path d="M252 196 Q284 176 316 196" fill="none" stroke="var(--d-systems)" stroke-width="1.8" stroke-dasharray="5 4" marker-end="url(#shiftR)" />
    <text x="372" y="230" text-anchor="start" font-size="11" font-family="var(--font-body)" fill="var(--ink)">5 &gt; 4 сдвиг; 3 &lt; 4 стоп</text>
    <text x="372" y="248" text-anchor="start" font-size="11" font-family="var(--font-body)" fill="var(--d-systems)">2 сравнения</text>

    <text x="20" y="314" text-anchor="start" font-size="11" font-family="var(--font-body)" fill="var(--ink)">проход 3</text>
    <text x="20" y="330" text-anchor="start" font-size="10" font-family="var(--font-body)" fill="var(--muted)">ключ = 1</text>
    <rect x="96" y="286" width="56" height="48" rx="6" fill="var(--card-2)" stroke="var(--ok)" stroke-width="1.6" />
    <text x="124" y="316" text-anchor="middle" font-size="16" font-family="var(--font-body)" fill="var(--ink)">3</text>
    <rect x="160" y="286" width="56" height="48" rx="6" fill="var(--card-2)" stroke="var(--ok)" stroke-width="1.6" />
    <text x="188" y="316" text-anchor="middle" font-size="16" font-family="var(--font-body)" fill="var(--ink)">4</text>
    <rect x="224" y="286" width="56" height="48" rx="6" fill="var(--card-2)" stroke="var(--ok)" stroke-width="1.6" />
    <text x="252" y="316" text-anchor="middle" font-size="16" font-family="var(--font-body)" fill="var(--ink)">5</text>
    <rect x="288" y="286" width="56" height="48" rx="6" fill="var(--card-2)" stroke="var(--d-systems)" stroke-width="1.8" />
    <text x="316" y="316" text-anchor="middle" font-size="16" font-family="var(--font-body)" fill="var(--ink)">1</text>
    <path d="M124 276 Q156 256 188 276" fill="none" stroke="var(--d-systems)" stroke-width="1.8" stroke-dasharray="5 4" marker-end="url(#shiftR)" />
    <path d="M188 276 Q220 256 252 276" fill="none" stroke="var(--d-systems)" stroke-width="1.8" stroke-dasharray="5 4" marker-end="url(#shiftR)" />
    <path d="M252 276 Q284 256 316 276" fill="none" stroke="var(--d-systems)" stroke-width="1.8" stroke-dasharray="5 4" marker-end="url(#shiftR)" />
    <text x="372" y="310" text-anchor="start" font-size="11" font-family="var(--font-body)" fill="var(--ink)">3, 4, 5 все &gt; 1: сдвиг всех</text>
    <text x="372" y="328" text-anchor="start" font-size="11" font-family="var(--font-body)" fill="var(--d-systems)">3 сравнения</text>

    <defs>
      <marker id="shiftR" markerWidth="9" markerHeight="9" refX="6" refY="4.5" orient="auto"><path d="M1 1 L7 4.5 L1 8" fill="none" stroke="var(--d-systems)" stroke-width="1.6" /></marker>
    </defs>
  </svg>
</DiagramFrame>

<Code lang="ru">
Кодим и трассируем **сортировку выбором** и **вставкой** с полной подробностью.

**Сортировка выбором:**
```js
function selectionSort(arr) {
  for (let i = 0; i < arr.length; i++) {
    // Найди минимум в arr[i..конец]
    let minIndex = i;
    for (let j = i + 1; j < arr.length; j++) {
      if (arr[j] < arr[minIndex]) {
        minIndex = j;
      }
    }
    // Обменяй arr[i] и arr[minIndex]
    [arr[i], arr[minIndex]] = [arr[minIndex], arr[i]];
  }
}
```

**Сортировка вставкой:**
```js
function insertionSort(arr) {
  for (let i = 1; i < arr.length; i++) {
    const key = arr[i];  // Элемент для вставки
    let j = i - 1;
    // Сдвинь элементы больше чем key один вправо
    while (j >= 0 && arr[j] > key) {
      arr[j + 1] = arr[j];
      j--;
    }
    // Вставь key в правильное место
    arr[j + 1] = key;
  }
}
```

Запустим обе на неотсортированном и отсортированном вводе:

<CodeRunner
  id="cr-simple-sorts"
  lang="ru"
  code={`function selectionSort(arr) {
  const arr2 = [...arr];
  let comparisons = 0;
  for (let i = 0; i < arr2.length; i++) {
    let minIndex = i;
    for (let j = i + 1; j < arr2.length; j++) {
      comparisons++;
      if (arr2[j] < arr2[minIndex]) minIndex = j;
    }
    [arr2[i], arr2[minIndex]] = [arr2[minIndex], arr2[i]];
  }
  return { sorted: arr2, comparisons };
}

function insertionSort(arr) {
  const arr2 = [...arr];
  let comparisons = 0;
  for (let i = 1; i < arr2.length; i++) {
    const key = arr2[i];
    let j = i - 1;
    while (j >= 0 && arr2[j] > key) {
      comparisons++;
      arr2[j + 1] = arr2[j];
      j--;
    }
    arr2[j + 1] = key;
  }
  return { sorted: arr2, comparisons };
}

const unsorted = [5, 3, 7, 1, 4];
const sorted = [1, 2, 3, 4, 5];

console.log("Неотсортировано [5, 3, 7, 1, 4]:");
console.log("  Выбор:", selectionSort(unsorted).comparisons, "сравнений");
console.log("  Вставка:", insertionSort(unsorted).comparisons, "сравнений");

console.log("Уже отсортировано [1, 2, 3, 4, 5]:");
console.log("  Выбор:", selectionSort(sorted).comparisons, "сравнений");
console.log("  Вставка:", insertionSort(sorted).comparisons, "сравнений (вставка адаптируется!)");
`}
/>
</Code>

<Trace lang="ru">
Трассируем **сортировку вставкой** на `[5, 3, 2, 1, 4]` шаг за шагом:

<AlgoTrace
  id="trace-insertion-sort"
  lang="ru"
  code={[
    "function insertionSort(arr) {",
    "  for (let i = 1; i < arr.length; i++) {",
    "    const key = arr[i];",
    "    let j = i - 1;",
    "    while (j >= 0 && arr[j] > key) {",
    "      arr[j + 1] = arr[j];",
    "      j--;",
    "    }",
    "    arr[j + 1] = key;",
    "  }",
    "}"
  ]}
  frames={[
    {
      caption: "Начало: arr = [5, 3, 2, 1, 4]. i = 1, key = 3.",
      line: 2,
      cells: [
        { value: "5", mark: "done" },
        { value: "3", mark: "active" },
        "2",
        "1",
        "4"
      ]
    },
    {
      caption: "j = 0: arr[0] = 5 > 3. Сдвинь 5 вправо. arr[1] = 5.",
      line: 6,
      cells: [
        { value: "5", mark: "active" },
        "5",
        "2",
        "1",
        "4"
      ]
    },
    {
      caption: "j = -1. Стоп. Вставь key=3 в arr[0]. arr = [3, 5, 2, 1, 4].",
      line: 9,
      cells: [
        { value: "3", mark: "done" },
        { value: "5", mark: "done" },
        "2",
        "1",
        "4"
      ]
    },
    {
      caption: "i = 2, key = 2. j = 1: arr[1] = 5 > 2. Сдвинь вправо.",
      line: 6,
      cells: [
        "3",
        { value: "5", mark: "active" },
        "5",
        "1",
        "4"
      ]
    },
    {
      caption: "j = 0: arr[0] = 3 > 2. Сдвинь вправо. arr[1] = 3.",
      line: 6,
      cells: [
        "3",
        { value: "3", mark: "active" },
        "5",
        "1",
        "4"
      ]
    },
    {
      caption: "j = -1. Стоп. Вставь key=2 в arr[0]. arr = [2, 3, 5, 1, 4].",
      line: 9,
      cells: [
        { value: "2", mark: "done" },
        { value: "3", mark: "done" },
        { value: "5", mark: "done" },
        "1",
        "4"
      ]
    },
    {
      caption: "i = 3, key = 1. Сдвинь 2, 3, 5 все вправо (3 сдвига).",
      line: 6,
      cells: [
        "2",
        "3",
        "5",
        { value: "5", mark: "active" },
        "4"
      ]
    },
    {
      caption: "Вставь key=1 в arr[0]. arr = [1, 2, 3, 5, 4].",
      line: 9,
      cells: [
        { value: "1", mark: "done" },
        { value: "2", mark: "done" },
        { value: "3", mark: "done" },
        { value: "5", mark: "done" },
        "4"
      ]
    },
    {
      caption: "i = 4, key = 4. j = 3: arr[3] = 5 > 4. Сдвинь вправо.",
      line: 6,
      cells: [
        "1",
        "2",
        "3",
        { value: "5", mark: "active" },
        "5"
      ]
    },
    {
      caption: "j = 2: arr[2] = 3 ≤ 4. Стоп. Вставь key=4 в arr[3]. Итог: [1, 2, 3, 4, 5].",
      line: 9,
      cells: [
        { value: "1", mark: "done" },
        { value: "2", mark: "done" },
        { value: "3", mark: "done" },
        { value: "4", mark: "done" },
        { value: "5", mark: "done" }
      ]
    }
  ]}
/>
</Trace>

<Complexity lang="ru">
| Алгоритм | Худший | Лучший | Адаптивна? | Память |
|---|---|---|---|---|
| Выбор | O(n²) | O(n²) | Нет | O(1) |
| Вставка | O(n²) | O(n) | Да | O(1) |
| Пузырьёк | O(n²) | O(n) | Да (с оптимизацией) | O(1) |

**Выбор в деталях:**
- Всегда O(n²) потому что ищешь минимум неотсортированного суффикса, даже если массив уже отсортирован.
- Количество обменов: O(n).

**Вставка в деталях:**
- Худший O(n²): массив развёрнут. Каждая вставка сдвигает все предыдущие.
- Лучший O(n): массив уже отсортирован. Условие цикла while сразу ложно, только n сравнений.
- Адаптивна: количество сравнений зависит от инверсий (неупорядоченных пар). Почти отсортированный вход = мало инверсий = O(n).

**Пузырьёк в деталях:**
- O(n²) худший: повторные обмены соседей.
- O(n) лучший (с оптимизацией): если обменов нет за проход, отсортировано. Выйди раньше.
- Менее эффективен чем вставка на практике — больше обменов и сравнений.
</Complexity>

<AlgoPractice
  id="practice-simple-sorts-1"
  lessonSlug="02-simple-sorts"
  lang="ru"
  problems={[
    {
      kind: "choice",
      prompt: "Почему в сортировке выбором O(n²) даже когда массив уже отсортирован?",
      choices: [
        "Потому что сортировка слишком медленная",
        "Потому что ищешь минимум неотсортированного суффикса, независимо от порядка",
        "Потому что сортировка выбором создаёт временную копию массива",
        "Потому что сортировка медленнее на больших массивах"
      ],
      answer: 1,
      hint: "Выбор не имеет раннего выхода. Каждый проход сканирует оставшийся неотсортированный суффикс. Исходный порядок не важен."
    },
    {
      kind: "choice",
      prompt: "Вставка за O(n) на почти отсортированном массиве. Почему?",
      choices: [
        "Потому что пропускает большинство элементов",
        "Потому что цикл while редко запускается — элементы уже в порядке",
        "Потому что использует хеш-таблицу внутри",
        "Потому что почти отсортированные имеют меньше данных"
      ],
      answer: 1,
      hint: "Вставка O(n + k) где k инверсии (неупорядоченные пары). Почти отсортированный массив имеет мало инверсий, так что k мало."
    },
    {
      kind: "numeric",
      prompt: "Сколько проходов делает сортировка выбором на массиве из 5 элементов?",
      answer: 5,
      hint: "Выбор делает n проходов (один за позицию элемента). При n=5, это 5 проходов."
    },
    {
      kind: "choice",
      prompt: "Какой алгоритм лучше для большого уже отсортированного массива?",
      choices: [
        "Выбор (всегда O(n²))",
        "Пузырьёк (O(n) лучший с оптимизацией)",
        "Вставка (O(n) лучший, адаптивна)",
        "Все три равны"
      ],
      answer: 2,
      hint: "Вставка адаптируется к отсортированному вводу за O(n). Выбор всегда O(n²). Вставка для предсортированного или почти отсортированного."
    },
    {
      kind: "choice",
      prompt: "Что значит 'на месте' для алгоритмов сортировки?",
      choices: [
        "Массив отсортирован в месте где был создан",
        "Алгоритм O(1) вспомогательной памяти (только переменные, без нового массива)",
        "Алгоритм сортирует слева направо",
        "Алгоритм никогда не модифицирует вход"
      ],
      answer: 1,
      hint: "На месте значит алгоритм переставляет собственные ячейки массива без создания нового. Выбор, вставка, пузырьёк все на месте."
    }
  ]}
/>

<Inset kind="advantage" lang="ru">
**Когда использовать простые сортировки.** Выбор, вставка, пузырьёк O(n²). Когда их применять? Маленькие массивы (n ≤ ~50) или почти отсортированный вход. Вставка особо: простая, на месте, сильно адаптивна. Множество реальных систем используют вставку на маленьких данных внутри оптимизированных O(n log n) алгоритмов (гибридная сортировка).
</Inset>

<Inset kind="mistake" lang="ru">
**Думаешь выбор может выйти рано.** Выбор всегда сканирует весь неотсортированный суффикс, даже если уже отсортирован. Не может "заметить" что отсортировано и выйти рано. Пузырьёк может (с оптимизацией: отслеживай был ли обмен), но выбор нет. Вот почему вставка лучше для предсортированных данных.
</Inset>

<Check lang="ru">
  <Quiz
    id="quiz-simple-sorts-1"
    pieceSlug="02-simple-sorts"
    lang="ru"
    question="Надо отсортировать список 10 элементов. Все три простые (выбор, вставка, пузырьёк) O(n²). Какую выбрать?"
    choices={[
      {
        label: "Выбор, потому что она самая известная",
        misconception: "Известность не определяет эффективность. Вставка проще и адаптивна; лучше для маленьких данных."
      },
      {
        label: "Вставка, потому что простая, адаптивна к отсортированному и эффективна на маленьких массивах",
        correct: true
      },
      {
        label: "Пузырьёк, потому что её проще понять",
        misconception: "Пузырьёк медленнее на практике — больше обменов и сравнений чем вставка. Редко используется вне образования."
      },
      {
        label: "Никакую; все три одинаковы для n=10",
        misconception: "Все O(n²), но вставка адаптируется к отсортированному, делая её быстрее на практике."
      }
    ]}
  />
</Check>

<Recap lang="ru">
**Сортировка выбором:** Повторно ищи минимум неотсортированной части и переноси в конец. O(n²) всегда. Никакой адаптивности.

**Сортировка вставкой:** Растёшь отсортированный префикс вставляя каждый новый элемент в правильное место. O(n²) худший, но O(n) на отсортированном или почти отсортированном вводе. Адаптивна.

**Сортировка пузырьком:** Повторно обменяй неупорядоченные соседи. O(n²) худший, O(n) лучший (с оптимизацией). Медленнее чем вставка на практике.

**Все три на месте и O(1) вспомогательной памяти.** Это важно на очень больших данных или в системах с ограничениями памяти.

**Ключевое:** Все простые O(n²) из-за вложенного цикла. Вставка особая: адаптируется к порядку входа, за O(n) на почти отсортированных данных. На больших неотсортированных, переходи на O(n log n) (сортировка слиянием, быстрая сортировка) что в следующих уроках.

**Почему учить?** Понимание простых учит вложенной структуре. Подготовляет к divide-and-conquer сортировке (слияние) и партиционированию (быстрая).
</Recap>
