---
concepts:
- binary-search-off-by-one
- lomuto-partition
- merge-stability
- quickselect
estMin: 14
lang: ru
level: middle
order: 9
prereqs: []
slug: quiz-code
sources:
- https://en.wikipedia.org/wiki/Binary_search_algorithm
- https://en.wikipedia.org/wiki/Quicksort
- https://en.wikipedia.org/wiki/Quickselect
status: ready
summary: "Читаем реальные сниппеты сортировки и поиска — ошибки на единицу в binary search, partition Lomuto, stable-слияние и quickselect — предсказываем баг или поведение и выбираем фикс."
title: 'Сортировка и поиск: чтение кода'
track: algorithms
unit: 03-sorting-search
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Баги сортировки и поиска прячутся на границах: не то сравнение, половина, отброшенная на индекс дальше нужного, swap, теряющий stable-свойство. Читайте каждый сниппет как на ревью и называйте дефект — или точное поведение — прежде чем выбрать.
</Hook>

<Goal lang="ru">
Отработайте цикл ревьюера для этого юнита: проследите инвариант в голове, найдите ошибку на единицу или потерянную гарантию и выберите минимальный корректный фикс.
</Goal>

## Сниппет 1 — бесконечный цикл

```js
function bsearch(arr, target) {
  let lo = 0, hi = arr.length - 1;
  while (lo <= hi) {
    const mid = Math.floor((lo + hi) / 2);
    if (arr[mid] === target) return mid;
    if (arr[mid] < target) lo = mid;   // отбрасываем левую часть
    else hi = mid - 1;                 // отбрасываем правую часть
  }
  return -1;
}
```

<Quiz
  id="03-sorting-search-code-1"
  lessonSlug="03-sorting-search"
  lang="ru"
  question="На многих входах это зависает навсегда. В чём баг и каков фикс?"
  choices={[
    { label: "lo = mid не сужает диапазон, когда mid == lo (например, диапазон из двух элементов, где arr[mid] < target): lo стоит на месте, mid пересчитывается в тот же индекс, и цикл крутится. Фикс: lo = mid + 1, ведь arr[mid] уже исключён.", correct: true },
    { label: "Условие цикла должно быть lo < hi.", misconception: "lo <= hi корректно для включающего диапазона — именно оно позволяет проверить финальный диапазон из одного элемента. Зависание из-за того, что lo = mid не продвигается, а не из-за условия." },
    { label: "Math.floor неверен; mid должен округляться вверх.", misconception: "Направление округления тут ни при чём. При lo = mid диапазон не сужается при любом округлении. Фикс — lo = mid + 1." },
    { label: "Проверка arr[mid] === target должна быть последней, а не первой.", misconception: "Порядок проверок не вызывает бесконечный цикл. Дефект — обновление границы lo = mid, которое не даёт прогресса." },
  ]}
/>

## Сниппет 2 — переполнение в вычислении середины

```c
int bsearch(int arr[], int n, int target) {
    int lo = 0, hi = n - 1;
    while (lo <= hi) {
        int mid = (lo + hi) / 2;        // <-- здесь
        if (arr[mid] == target) return mid;
        if (arr[mid] < target) lo = mid + 1;
        else hi = mid - 1;
    }
    return -1;
}
```

<Quiz
  id="03-sorting-search-code-2"
  lessonSlug="03-sorting-search"
  lang="ru"
  question="Это проходило все маленькие тесты, но портило результаты на многогигабайтном отсортированном массиве из 32-битных int. Почему и каков канонический фикс?"
  choices={[
    { label: "lo + hi может превысить INT_MAX, когда оба индекса большие, переполниться в отрицательное значение, и mid станет неверным индексом. Фикс — mid = lo + (hi - lo) / 2, который не переполняется, так как hi - lo помещается в диапазон.", correct: true },
    { label: "Такие большие массивы вообще нельзя обрабатывать binary search.", misconception: "Можно; алгоритм в порядке. Единственный дефект — переполнение суммы lo + hi в 32-битном int. Считайте mid = lo + (hi - lo) / 2." },
    { label: "Циклу нужно lo < hi, чтобы не читать за концом.", misconception: "lo <= hi корректно и читает только валидные индексы. Баг — арифметическое переполнение (lo + hi), а не граница цикла." },
    { label: "Целочисленное деление отбрасывает дробь, поэтому mid всегда слишком мал.", misconception: "Усечение в сторону lo — именно то, чего binary search и ожидает, и здесь безвредно. Реальный дефект — переполнение lo + hi на больших индексах." },
  ]}
/>

## Сниппет 3 — partition Lomuto

```js
function partition(arr, low, high) {
  const pivot = arr[high];
  let i = low - 1;
  for (let j = low; j < high; j++) {
    if (arr[j] < pivot) {
      i++;
      [arr[i], arr[j]] = [arr[j], arr[i]];
    }
  }
  [arr[i + 1], arr[high]] = [arr[high], arr[i + 1]];
  return i + 1;
}
```

<Quiz
  id="03-sorting-search-code-3"
  lessonSlug="03-sorting-search"
  lang="ru"
  question="Что гарантирует возвращаемый индекс и почему эта схема делает quicksort не stable?"
  choices={[
    { label: "Он возвращает финальную позицию pivot в отсортированном массиве: всё слева < pivot, всё справа >= pivot, а сам pivot больше не двигается. Не stable потому, что финальный swap может перебросить pivot через равные элементы, переставив равные ключи.", correct: true },
    { label: "Он возвращает индекс медианы массива.", misconception: "Он возвращает место, куда сел выбранный pivot после partition, то есть его отсортированную позицию — а не медиану, если только pivot не оказался медианой. Поиск медианы — это quickselect, другое применение partition." },
    { label: "Он гарантирует, что обе стороны равны по размеру.", misconception: "Partition гарантирует порядок относительно pivot, а не баланс. Экстремальный pivot оставляет одну сторону пустой — это худший случай O(n²). Баланс даёт хороший выбор pivot." },
    { label: "Quicksort не stable, потому что использует рекурсию.", misconception: "Рекурсия не имеет отношения к stable-свойству. Нестабильность из-за swap'ов, перемещающих равные элементы через границу pivot и теряющих исходный относительный порядок." },
  ]}
/>

## Сниппет 4 — quickselect для k-го наименьшего

```js
function quickselect(arr, k, lo = 0, hi = arr.length - 1) {
  const p = partition(arr, lo, hi);    // финальный индекс pivot
  if (p === k) return arr[p];
  if (p < k) return quickselect(arr, k, p + 1, hi);
  return quickselect(arr, k, lo, p - 1);
}
```

<Quiz
  id="03-sorting-search-code-4"
  lessonSlug="03-sorting-search"
  lang="ru"
  question="Quickselect находит k-й наименьший элемент, переиспользуя partition. Какова его сложность и почему это лучше полной сортировки для этой задачи?"
  choices={[
    { label: "O(n) в среднем (n + n/2 + n/4 + … = 2n), потому что он рекурсирует только в ту сторону, где лежит k, а не в обе. Полная сортировка — O(n log n); quickselect пропускает работу по упорядочиванию всего, что не нужно.", correct: true },
    { label: "O(n log n), как и сортировка, так что выгоды нет.", misconception: "Сортировка обрабатывает обе стороны на каждом уровне; quickselect рекурсирует только в сторону с k, давая в среднем O(n). В этом весь смысл выбора вместо сортировки." },
    { label: "O(log n), потому что он делит массив пополам, как binary search.", misconception: "Каждый partition всё равно сканирует O(n) элементов, так что один уровень линеен. Геометрическая сумма односторонней рекурсии — O(n) в среднем, а не O(log n)." },
    { label: "Он не может работать, потому что массив не отсортирован.", misconception: "Quickselect намеренно работает на неотсортированном входе — partition не нужна отсортированность. Худший случай O(n²) на плохих pivot чинится рандомизацией или median-of-medians." },
  ]}
/>

<Recap lang="ru">
Каждый дефект здесь живёт на границе: binary search должен сужать диапазон (lo = mid + 1, никогда lo = mid) и считать mid = lo + (hi - lo) / 2, чтобы обойти переполнение; partition Lomuto возвращает финальную отсортированную позицию pivot и не stable, потому что swap'ы пересекают равные ключи; а quickselect переиспользует этот partition, находя k-й элемент за O(n) в среднем рекурсией только в одну сторону. Сначала читайте инвариант — тогда фикс обычно в один токен.
</Recap>
