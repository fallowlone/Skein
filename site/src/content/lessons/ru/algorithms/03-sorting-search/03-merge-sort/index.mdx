---
concepts:
- merge_sort
- divide_and_conquer
- stable_sort
- merge
- space_complexity
estMin: 21
lang: ru
mathPrereqs:
- math/08-growth/02-logarithms
order: 3
prereqs:
- 02-simple-sorts
slug: 03-merge-sort
sources:
- https://en.wikipedia.org/wiki/Merge_sort
- https://www.geeksforgeeks.org/dsa/merge-sort/
- https://www.w3schools.com/dsa/dsa_algo_mergesort.php
status: ready
summary: 'Сортировка слиянием раздели́т массив, рекурсивно сортирует каждую половину,
  потом объединяет за O(n). Результат: O(n log n) всегда. Стабильна, не на месте,
  O(n) памяти. Первый вкус divide-and-conquer.'
title: Сортировка слиянием
track: algorithms
unit: 03-sorting-search
---



import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Idea from "~/components/lesson/Idea.astro";
import Code from "~/components/lesson/Code.astro";
import Trace from "~/components/lesson/Trace.astro";
import Complexity from "~/components/lesson/Complexity.astro";
import Check from "~/components/lesson/Check.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import AnnotatedCode from "~/components/algo/AnnotatedCode.astro";
import AlgoTrace from "~/components/algo/AlgoTrace.astro";
import CodeRunner from "~/components/algo/CodeRunner.astro";
import AlgoPractice from "~/components/algo/AlgoPractice.astro";
import MathRecall from "~/components/algo/MathRecall.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Ты видел простые сортировки: выбор, вставка, пузырёк. Все O(n²) и медленные на больших данных. Быстрые алгоритмы (сортировка слиянием, быстрая сортировка) используют трюк: раздели́ массив на куски, реши каждый отдельно, потом объедини решения. Это divide and conquer (раздели и властвуй). Сортировка слиянием — чистейший пример. Раздели́т массив пополам, рекурсивно сортирует каждую половину, потом объединяет обе отсортированные половины за линейное время. Результат O(n log n) — всегда, даже в худшем случае.
</Hook>

<Goal lang="ru">
После этого урока ты разбираешься в сортировке слиянием: как объединить две уже отсортированные массивы за O(n) (две указателя), как работает divide-and-conquer, почему слияние всегда O(n log n) (log n уровней раздела, O(n) слияния на каждый уровень), почему оно стабильно (равные элементы остаются в порядке) и не на месте (использует O(n) памяти), и что divide and conquer значит как общий паттерн алгоритма.
</Goal>

<Idea lang="ru">
**Раздели и властвуй: стратегия**

Суть сортировки слиянием: если ты раздели́шь задачу на две независимые подзадачи половинного размера, решишь каждую рекурсивно и объединишь решения, часто получишь намного более быстрый алгоритм.

Сортировка слиянием следует трём шагам:
1. **Раздели** (divide): раздели́ массив пополам (или остановись если размер 1).
2. **Властвуй** (conquer): рекурсивно вызови сортировку слиянием на каждую половину.
3. **Объедини** (combine): объедини две отсортированные половины в один отсортированный массив.

**Шаг слияния: объединение двух отсортированных массивов**

Сердце сортировки слиянием — функция `merge`. Даны две уже отсортированные массивы, объедини их в один за O(n) с двумя указателями.

Представь две отсортированные массивы рядом:
- Левая: `[1, 3, 5]`
- Правая: `[2, 4, 6]`

Начни с двух указателей, один в начале каждой. Сравни элементы у обоих. Возьми меньший, сдвинь указатель того массива и повтори:

```
Левая:   [1, 3, 5]
         ^
Правая:  [2, 4, 6]
         ^
Сравни: 1 < 2. Возьми 1. Указатель левой сдвинется.

Левая:   [1, 3, 5]
            ^
Правая:  [2, 4, 6]
         ^
Сравни: 3 > 2. Возьми 2. Указатель правой сдвинется.

Левая:   [1, 3, 5]
            ^
Правая:  [2, 4, 6]
            ^
Сравни: 3 < 4. Возьми 3. Указатель левой сдвинется.

(продолжаем...)
Результат: [1, 2, 3, 4, 5, 6]
```

Почему O(n)? Ты проходишь каждый указатель через его массив ровно один раз. Всего шагов = длина левой + длина правой = O(n).

**Почему сортировка слиянием O(n log n)**

Сложность по времени идёт из двух: сколько уровней раздела и сколько работы на уровень.

Сколько уровней раздела? Массив делится пополам каждый раз: n → n/2 → n/4 → ... → 1. Это log n уровней.

<MathRecall lang="ru" href="/ru/learn/math/08-growth/02-logarithms" title="Логарифмы" />

Сколько работы на уровень? На каждом уровне ты объединяешь подмассивы. Общая работа по всем подмассивам на одном уровне O(n) — каждый элемент трогаешь один раз.

- Уровень 1 (полный массив): одно объединение, O(n) работы.
- Уровень 2 (две половины): два объединения, O(n/2) + O(n/2) = O(n) работы.
- Уровень 3 (четыре четвёрки): четыре объединения, O(n/4) × 4 = O(n) работы.
- ...
- Уровень log n: много маленьких объединений, всё ещё O(n) работы в целом.

Всего: log n уровней × O(n) работы за уровень = **O(n log n)**.

**Стабильность и память**

Сортировка слиянием стабильна: при объединении если оба указателя показывают на равные элементы, берёшь из левого массива первым. Это сохраняет порядок равных элементов.

Сортировка слиянием не на месте: ты создаёшь новый массив при слиянии чтобы удерживать результат. Это стоит O(n) памяти. Рекурсивная сортировка слиянием тоже использует O(log n) памяти для стека вызовов. Всего: O(n).
</Idea>

<Code lang="ru">
Давайте напишем сортировку слиянием с нуля.

**Шаг 1: Объедини две отсортированные массивы**

```js
function merge(left, right) {
  const result = [];
  let i = 0;  // Указатель в левом
  let j = 0;  // Указатель в правом

  while (i < left.length && j < right.length) {
    if (left[i] <= right[j]) {
      result.push(left[i]);
      i++;
    } else {
      result.push(right[j]);
      j++;
    }
  }

  // Если в левом ещё есть, добавь
  while (i < left.length) {
    result.push(left[i]);
    i++;
  }

  // Если в правом ещё есть, добавь
  while (j < right.length) {
    result.push(right[j]);
    j++;
  }

  return result;
}
```

Обрати внимание: `left[i] <= right[j]` (не `<`) сохраняет стабильность. Если элементы равны, возьми из левого первым.

**Шаг 2: Сортировка слиянием рекурсивно**

```js
function mergeSort(arr) {
  // Базовый случай: массив размера 0 или 1 уже отсортирован
  if (arr.length <= 1) {
    return arr;
  }

  // Раздели: найди середину
  const mid = Math.floor(arr.length / 2);

  // Властвуй: рекурсивно сортируй каждую половину
  const left = mergeSort(arr.slice(0, mid));
  const right = mergeSort(arr.slice(mid));

  // Объедини: слей отсортированные половины
  return merge(left, right);
}
```

Это полный алгоритм. Рекурсия заканчивается когда массив размера 0 или 1 (уже отсортирован).

Давайте запустим:

<CodeRunner
  id="cr-merge-sort"
  lang="ru"
  code={`function merge(left, right) {
  const result = [];
  let i = 0, j = 0;
  while (i < left.length && j < right.length) {
    if (left[i] <= right[j]) {
      result.push(left[i++]);
    } else {
      result.push(right[j++]);
    }
  }
  while (i < left.length) result.push(left[i++]);
  while (j < right.length) result.push(right[j++]);
  return result;
}

function mergeSort(arr) {
  if (arr.length <= 1) return arr;
  const mid = Math.floor(arr.length / 2);
  const left = mergeSort(arr.slice(0, mid));
  const right = mergeSort(arr.slice(mid));
  return merge(left, right);
}

const unsorted = [38, 27, 43, 3, 9, 82, 10];
const sorted = mergeSort(unsorted);
console.log("Исходный:", unsorted);
console.log("Отсорт: ", sorted);

// Проверь стабильность
const items = [
  { val: 3, id: 'a' },
  { val: 1, id: 'b' },
  { val: 3, id: 'c' },
  { val: 2, id: 'd' }
];

function mergeStable(left, right) {
  const result = [];
  let i = 0, j = 0;
  while (i < left.length && j < right.length) {
    if (left[i].val <= right[j].val) {
      result.push(left[i++]);
    } else {
      result.push(right[j++]);
    }
  }
  while (i < left.length) result.push(left[i++]);
  while (j < right.length) result.push(right[j++]);
  return result;
}

function mergeSortStable(arr) {
  if (arr.length <= 1) return arr;
  const mid = Math.floor(arr.length / 2);
  const left = mergeSortStable(arr.slice(0, mid));
  const right = mergeSortStable(arr.slice(mid));
  return mergeStable(left, right);
}

const sorted2 = mergeSortStable(items);
console.log("Стабильная сортировка (две 3 остаются в порядке a, потом c):");
sorted2.forEach(x => console.log(\`  val=\${x.val}, id=\${x.id}\`));
`}
/>
</Code>

<Trace lang="ru">
Давайте трассируем сортировку слиянием на маленьком массиве: `[38, 27, 43, 3]`.

<AlgoTrace
  id="trace-merge-sort"
  lang="ru"
  code={[
    "function mergeSort(arr) {",
    "  if (arr.length <= 1) return arr;",
    "  const mid = Math.floor(arr.length / 2);",
    "  const left = mergeSort(arr.slice(0, mid));",
    "  const right = mergeSort(arr.slice(mid));",
    "  return merge(left, right);",
    "}"
  ]}
  frames={[
    {
      caption: "Начало: mergeSort([38, 27, 43, 3]). mid = 2.",
      line: 2,
      cells: ["38", "27", "43", "3"]
    },
    {
      caption: "Вызови mergeSort([38, 27]) на левую половину.",
      line: 3,
      cells: [{ value: "38", mark: "active" }, { value: "27", mark: "active" }, "43", "3"]
    },
    {
      caption: "  Левая половина: mergeSort([38, 27]). mid = 1.",
      line: 2,
      cells: [{ value: "38", mark: "active" }, { value: "27", mark: "active" }, "43", "3"]
    },
    {
      caption: "  Рекурсия на [38], потом [27]. Оба размер 1, верни как есть.",
      line: 1,
      cells: ["38", "27", "43", "3"]
    },
    {
      caption: "  Объедини [38] и [27] → [27, 38].",
      line: 5,
      cells: [{ value: "27", mark: "done" }, { value: "38", mark: "done" }, "43", "3"]
    },
    {
      caption: "Вернулись в main: left = [27, 38]. Теперь mergeSort([43, 3]) на право.",
      line: 4,
      cells: ["27", "38", { value: "43", mark: "active" }, { value: "3", mark: "active" }]
    },
    {
      caption: "  Правая половина: mergeSort([43, 3]). Рекурсия на [43] и [3].",
      line: 2,
      cells: ["27", "38", { value: "43", mark: "active" }, { value: "3", mark: "active" }]
    },
    {
      caption: "  Объедини [43] и [3] → [3, 43].",
      line: 5,
      cells: ["27", "38", { value: "3", mark: "done" }, { value: "43", mark: "done" }]
    },
    {
      caption: "Теперь объедини левую [27, 38] и правую [3, 43]. Сравни 27 vs 3: возьми 3.",
      line: 5,
      cells: [{ value: "27", mark: "active" }, "38", { value: "3", mark: "active" }, "43"]
    },
    {
      caption: "Сравни 27 vs 43: возьми 27. Результат пока: [3, 27].",
      line: 5,
      cells: ["27", "38", "3", { value: "43", mark: "active" }]
    },
    {
      caption: "Сравни 38 vs 43: возьми 38. Результат пока: [3, 27, 38].",
      line: 5,
      cells: ["27", { value: "38", mark: "active" }, "3", { value: "43", mark: "active" }]
    },
    {
      caption: "Только 43 осталось в правой. Возьми 43. Финально: [3, 27, 38, 43].",
      line: 5,
      cells: [{ value: "3", mark: "done" }, { value: "27", mark: "done" }, { value: "38", mark: "done" }, { value: "43", mark: "done" }]
    }
  ]}
/>
</Trace>

<Complexity lang="ru">
| Аспект | Значение |
|---|---|
| **Время (лучший)** | O(n log n) |
| **Время (средний)** | O(n log n) |
| **Время (худший)** | O(n log n) |
| **Память** | O(n) |
| **Стабильна?** | Да |
| **На месте?** | Нет |

**Почему всегда O(n log n)?**

В отличие от сортировки выбором (всегда O(n²)) или вставкой (O(n) лучше, O(n²) худше), сортировка слиянием не меняется от порядка входа. Она всегда делит массив пополам log n раз, и каждый уровень слияния стоит O(n). Результат всегда O(n log n) — нет лучшего случая, нет худшего, просто O(n log n).

**Стабильность:**

Сортировка слиянием сохраняет порядок равных элементов. При слиянии и оба указателя на равные значения, берёшь из левого массива первым. Это гарантирует стабильность.

**Память:**

Сортировка слиянием использует O(n) доп. памяти чтобы создать временные массивы при слиянии. Если входной массив размером n, ты выделяешь примерно n элементов памяти. Рекурсия добавляет O(log n) на стеке (глубина рекурсии = log n). Всего: O(n).

Это компромисс: сортировка слиянием быстра и предсказуема, но использует доп. память.
</Complexity>

<AlgoPractice
  id="practice-merge-sort-1"
  lessonSlug="03-merge-sort"
  lang="ru"
  problems={[
    {
      kind: "choice",
      prompt: "Почему сортировка слиянием O(n log n) даже когда вход уже отсортирован?",
      choices: [
        "Потому что слияние стоит O(n²) времени",
        "Потому что она всегда делит массив log n раз, и каждый уровень слияния O(n) работы",
        "Потому что сортировка отсортированного всегда O(n log n)",
        "Потому что глубина рекурсии n"
      ],
      answer: 1,
      hint: "Сортировка слиянием не адаптируется к порядку входа. Она всегда делит log n раз. Каждый уровень O(n) слияния. Итак: log n уровней × O(n) работа = O(n log n)."
    },
    {
      kind: "choice",
      prompt: "При слиянии двух отсортированных массивов, почему две указатели вместо поиска каждого элемента?",
      choices: [
        "Потому что указатели быстрее чем двоичный поиск",
        "Потому что оба массивы уже отсортированы, ходишь обоих один раз, сравниваешь, O(n) всего",
        "Потому что поиск был бы O(n²)",
        "Потому что указатели требуют меньше памяти"
      ],
      answer: 1,
      hint: "Оба отсортированы. Ходишь двумя указателями — один в каждом начале — позволяет объединить в одну ёмкость без повтора работы."
    },
    {
      kind: "choice",
      prompt: "Сортировка слиянием использует O(n) доп. памяти. Почему это хуже чем выбор (O(1))?",
      choices: [
        "Потому что в целом медленнее",
        "Потому что O(n) памяти значит для массива 1 GB нужно 1 GB временной памяти",
        "Потому что временные массивы на стеке",
        "Потому что рекурсия всегда больше памяти"
      ],
      answer: 1,
      hint: "Для большого массива O(n) память дорога. Выбор O(1) переставляет массив на месте. Слияние нужен отдельный временный массив."
    },
    {
      kind: "choice",
      prompt: "Является ли сортировка слиянием стабильной? То есть равные элементы сохраняют порядок?",
      choices: [
        "Нет, она переупорядочивает равные элементы",
        "Да, потому что берёшь из левого массива первым если равные",
        "Только на отсортированном входе",
        "Только на маленьких массивах"
      ],
      answer: 1,
      hint: "При слиянии и оба указателя на равные значения, берёшь из левого (left[i] <= right[j]). Это сохраняет относительный порядок равных элементов."
    },
    {
      kind: "numeric",
      prompt: "Сколько уровней раздела случается при сортировке слиянием массива из 16 элементов?",
      answer: 4,
      hint: "Уровни: 16 → 8 → 4 → 2 → 1. Это 4 деления пополам. log₂(16) = 4."
    }
  ]}
/>

<Inset kind="advantage" lang="ru">
**Когда использовать сортировку слиянием.** Сортировка слиянием O(n log n) всегда, делая её предсказуемой и быстрой на больших данных. Она стабильна, так что сохраняет порядок (полезно при сортировке по нескольким ключам). Настоящие базы и системы часто используют сортировку слиянием или варианты. Минус: O(n) доп. памяти. Используй когда скорость критична и память есть. Для огромных данных лучше in-place алгоритмы как quicksort (хотя он не стабильный).
</Inset>

<Inset kind="pattern" lang="ru">
**Divide and conquer как стратегия.** Сортировка слиянием — первый divide-and-conquer алгоритм что ты видишь. Паттерн: раздели задачу на независимые подзадачи, решай каждую рекурсивно, объедини решения. Это работает для многих задач: поиск в сбалансированном дереве, умножение больших чисел, поиск ближайшей пары точек. Ключ: подзадачи должны быть независимы (решение одной не влияет другую), и объединение должно быть эффективно.
</Inset>

<Check lang="ru">
  <Quiz
    id="quiz-merge-sort-1"
    pieceSlug="03-merge-sort"
    lang="ru"
    question="Нужно отсортировать массив в миллион элементов и у тебя 2 GB RAM. Массив 800 MB. Что лучше: сортировка слиянием (нужно 800 MB доп) или выбор (O(1) доп)?"
    choices={[
      {
        label: "Сортировка слиянием, потому что быстрее: O(n log n) vs O(n²)",
        correct: true
      },
      {
        label: "Выбор, потому что O(1) память дешевле",
        misconception: "Выбор менее памяти, но O(n²) время — 14 часов или более. Слияние O(n log n) — секунды. Время важнее на больших данных. И у тебя 2 GB, так что 800 MB подходит."
      },
      {
        label: "Ни то, ни другое; quicksort лучше",
        misconception: "Quicksort может быстрее на практике, но сортировка слиянием тоже отличный выбор. Вопрос про слияние vs выбор."
      },
      {
        label: "Зависит от железа памяти",
        misconception: "Разница памяти важна, но разница времени огромна. O(n log n) так быстрее чем O(n²) что слияние побеждает несмотря на больше памяти."
      }
    ]}
  />
</Check>

<Recap lang="ru">
**Сортировка слиянием** раздели́т массив пополам, рекурсивно сортирует каждую половину, потом объединяет вместе.

**Шаг слияния** берёт два отсортированных массива и объединяет их за O(n) с двумя указателями: ходишь оба, сравниваешь, берёшь меньший.

**Сложность по времени:** O(n log n) всегда (log n уровней раздела, O(n) работа за уровень). Никогда O(n²), даже на плохом входе.

**Память:** O(n) для временных массивов при слиянии.

**Стабильная:** Да. Равные элементы сохраняют порядок.

**На месте:** Нет. Нужна O(n) доп. памяти.

**Раздели и властвуй:** Ключевое озарение — раздели пополам, реши каждую половину независимо, объедини. Быстро и мощно. Этот паттерн работает для многих алгоритмов за пределами сортировки.

**Почему выучить?** Сортировка слиянием учит divide and conquer, один из самых важных дизайна алгоритмов стратегий. Она ещё показывает O(n log n), класс сложности что видишь в многих настоящих системах.
</Recap>
