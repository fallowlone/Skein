---
concepts:
- sorting-as-preprocessing
- adaptive-insertion-sort
- merge-sort-guarantee
- quicksort-worst-case
- binary-search-invariant
- monotonic-predicate
estMin: 13
lang: ru
level: middle
order: 8
prereqs: []
slug: quiz-short
sources:
- https://en.wikipedia.org/wiki/Merge_sort
- https://en.wikipedia.org/wiki/Quicksort
- https://en.wikipedia.org/wiki/Binary_search_algorithm
status: ready
summary: "Свободное припоминание по всему юниту сортировки и поиска. Сначала ответьте на каждый вопрос по памяти, затем откройте эталонный ответ и сравните."
title: 'Сортировка и поиск: свободное припоминание'
track: algorithms
unit: 03-sorting-search
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";

<Hook>
Припоминание сильнее перечитывания. Для каждого вопроса проговорите или запишите полный ответ по памяти, прежде чем открыть эталон — именно усилие припоминания закрепляет материал.
</Hook>

<Goal lang="ru">
Восстановите ключевые идеи юнита — сортировка как препроцессинг, почему merge sort и quicksort ведут себя так по-разному, инвариант binary search и бинарный поиск по ответу — не подглядывая в уроки.
</Goal>

<RetrievalDrawer
  client:load
  id="03-sorting-search-short-retrieval"
  lang="ru"
  questions={[
    { q: "Почему сортировку называют препроцессингом и каково правило, стоит ли сортировать?", a: "Сортировка — это препроцессинг, потому что вы платите O(n log n) один раз, а дальше каждая операция над этими данными дешевле: проверка наличия превращается в binary search за O(log n), поиск дубликатов — в проход по соседям за O(n) вместо O(n²), медиана — в прямой доступ O(1) к индексу n/2, ближайшая пара — в проход по соседям за O(n). Правило: сортируйте, только если переиспользуете порядок — много запросов к одним данным или задача, ответ которой и есть порядок. Для одной операции линейная работа O(n) бьёт O(n log n) сортировки плюс запрос, так что не сортируйте «на всякий случай»." },
    { q: "Все простые сортировки O(n²), но insertion sort всё равно полезна. Почему и чем она отличается от selection sort на почти отсортированном входе?", a: "Insertion sort адаптивна: её работа пропорциональна числу инверсий (пар не по порядку), поэтому на уже или почти отсортированном входе внутренний while почти не выполняется, и она финиширует за O(n). У selection sort нет раннего выхода — каждый проход сканирует весь неотсортированный суффикс в поисках минимума, так что это O(n²) даже на отсортированном входе. Эта адаптивность — причина, почему insertion sort применяют для маленьких или почти отсортированных подмассивов внутри гибридных O(n log n) сортировок, и почему она верный выбор для данных, приходящих почти по порядку." },
    { q: "Почему merge sort O(n log n) на любом входе и чем вы за эту гарантию платите?", a: "Merge sort всегда делит массив пополам — n до n/2 до n/4 вплоть до 1, то есть log n уровней — и на каждом уровне слияния касаются каждого элемента один раз за O(n) работы, так что произведение — O(n log n) независимо от порядка входа. Он никогда не адаптируется и никогда не деградирует. Плата — память: шагу merge нужен O(n) вспомогательный буфер (плюс O(log n) стека вызовов), поэтому он не in-place. Бонус сверх гарантии — stable-свойство: при равенстве указателей слияния взятие сначала из левой половины сохраняет исходный порядок равных элементов." },
    { q: "Quicksort O(n log n) в среднем, но O(n²) в худшем случае. Объясните разрыв и как продакшен-код от него защищается.", a: "Стоимость quicksort задаёт баланс partition. Pivot около медианы делит массив примерно пополам, давая log n уровней по O(n) = O(n log n). Стабильно экстремальный pivot (например, всегда последний на уже отсортированных данных) делит на пустую сторону и сторону n-1, давая n уровней по O(n) = O(n²). Защита выбирает лучшие pivot: рандомизируют pivot или берут median-of-three (первый, средний, последний). Оба делают вырожденное деление астрономически маловероятным, возвращая ожидаемое O(n log n). Quicksort выбирают вопреки этому, потому что он сортирует in-place с O(log n) стека, с малыми константами и хорошей работой с кэшем." },
    { q: "Сформулируйте инвариант binary search и три классических бага, его нарушающих.", a: "Инвариант: цель, если она есть, всегда лежит внутри текущего включающего диапазона [lo, hi]; каждый шаг проверяет mid и по результату сравнения отбрасывает половину, которая заведомо не может её содержать. Три классических бага: (1) условие цикла — lo < hi вместо lo <= hi пропускает финальный диапазон из одного элемента и промахивается по целям в lo == hi; (2) обновление границы — lo = mid (или hi = mid) вместо lo = mid + 1 / hi = mid - 1 не исключает уже проверенный mid, диапазон перестаёт сужаться и цикл крутится вечно; (3) переполнение середины — (lo + hi) / 2 может переполнить целое фиксированной ширины, поэтому считают mid = lo + (hi - lo) / 2." },
    { q: "Что такое «бинарный поиск по ответу» и какое предусловие должно выполняться для его корректности?", a: "Вместо поиска индекса в отсортированном массиве вы ищете в пространстве возможных значений ответа наименьшее (или наибольшее), удовлетворяющее проверке да/нет feasible(x). Вы задаёте lo и hi как минимально и максимально правдоподобные ответы, затем делите диапазон пополам: если feasible(mid) истинно, ответ это mid или меньше (двигаете hi); если ложно — больше (двигаете lo). Предусловие — монотонный предикат: при росте x feasible переключается с false на true не более одного раза и не возвращается обратно. Монотонность гарантирует, что одна сторона от mid целиком неосуществима и её можно отбросить, ровно как отсортированность для binary search по массиву. Классика: минимальная грузоподъёмность для доставки за D дней, минимальная скорость поедания, наименьшая максимальная сумма разбиения." },
  ]}
/>

<Recap lang="ru">
Если вы смогли восстановить каждый ответ по памяти, вы держите стержень юнита: сортируйте только ради переиспользования порядка; insertion sort адаптируется, а selection sort — никогда; merge sort гарантирует O(n log n) и stable-свойство ценой O(n) памяти; quicksort меняет худший случай на in-place скорость и защищает его умным выбором pivot; а binary search — хоть по массиву, хоть по монотонному пространству ответов — живёт или умирает по своему включающему инварианту.
</Recap>
