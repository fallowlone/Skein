---
concepts:
- sorting
- sorted
- preprocessing
- binary_search
- duplicate_detection
estMin: 16
lang: ru
mathPrereqs: []
order: 1
prereqs:
- 02-traversal
slug: 01-why-sort
sources:
- https://en.wikipedia.org/wiki/Sorting_algorithm
- https://www.designgurus.io/answers/detail/is-sorting-important-in-dsa
- https://www.geeksforgeeks.org/dsa/analysis-algorithms-big-o-analysis/
status: ready
summary: 'Сортировка — предварительная обработка: потрать O(n log n) один раз, разблокируй
  O(log n) бинарный поиск, O(n) поиск дубликатов, O(n) нахождение медианы. Узнай,
  когда оно стоит.'
title: Зачем сортировать?
track: algorithms
unit: 03-sorting-search
---



import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Idea from "~/components/lesson/Idea.astro";
import Code from "~/components/lesson/Code.astro";
import Trace from "~/components/lesson/Trace.astro";
import Complexity from "~/components/lesson/Complexity.astro";
import Check from "~/components/lesson/Check.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import AnnotatedCode from "~/components/algo/AnnotatedCode.astro";
import AlgoTrace from "~/components/algo/AlgoTrace.astro";
import CodeRunner from "~/components/algo/CodeRunner.astro";
import AlgoPractice from "~/components/algo/AlgoPractice.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DiagramFrame from "~/components/diagram/DiagramFrame.astro";

<Hook>
У тебя есть несортированные данные и трудная задача: найти дубликаты, поискать значение, найти медиану или две ближайшие цифры. Каждая выглядит сложной и дорогой. Но что если сначала упорядочить данные? Отсортированный массив как отсортированный справочник — внезапно трудные задачи становятся тривиальными. Цена: один sort за O(n log n). Награда: разблокируй O(log n) поиск, мгновенное обнаружение дубликатов, нахождение медианы за линейное время и ещё.
</Hook>

<Goal lang="ru">
После этого урока ты понимаешь, что значит "отсортировано", почему сортировка работает предварительной обработкой, что разблокирует быстрые алгоритмы (бинарный поиск, поиск дубликатов, медиана, ближайшие пары), и когда сортировка стоит первоначальной цены, а когда нет.
</Goal>

<Idea lang="ru">
**Что значит "отсортировано"?**

Массив **отсортирован** (или в отсортированном порядке), когда каждый элемент меньше или равен следующему. Для массива чисел [3, 1, 4, 1, 5, 9, 2, 6]:
- Отсортировано по возрастанию: [1, 1, 2, 3, 4, 5, 6, 9]
- Отсортировано по убыванию: [9, 6, 5, 4, 3, 2, 1, 1]

Порядок зафиксирован, на него можно полагаться.

**Большая идея: сортировка как предварительная обработка**

Сортировка — это шаг предварительной обработки: делаешь один раз, потом получаешь выгоды для всех будущих операций. Думай как организация библиотеки: потратишь время, отсортировав книги по названию (O(n log n)), и потом каждый поиск мгновенен (O(log n)). Сделай один раз, получай выгоду всегда.

**Какие задачи становятся лёгкими после сортировки?**

**1. Бинарный поиск (поиск значения)**

Несортированный массив: Чтобы узнать, есть ли значение, нужно сканировать массив линейно. Худший случай: O(n).

Отсортированный массив: Используй бинарный поиск. Режь пространство поиска пополам каждый шаг. Худший случай: O(log n).

Для массива из 1 миллиона элементов:
- Линейный поиск: ~500 000 сравнений (в среднем)
- Бинарный поиск: ~20 сравнений

Это ускорение в 25 000 раз.

**2. Поиск дубликатов (есть ли повторы в массиве?)**

Несортированный массив: Сравни каждый элемент со всеми остальными. Вложенные циклы. O(n²).

```js
function hasDuplicateUnsorted(arr) {
  for (let i = 0; i < arr.length; i++) {
    for (let j = i + 1; j < arr.length; j++) {
      if (arr[i] === arr[j]) return true;
    }
  }
  return false;  // O(n²)
}
```

Отсортированный массив: Ходи один раз. Если два соседа равны, есть дубликат. O(n).

```js
function hasDuplicateSorted(arr) {
  for (let i = 0; i < arr.length - 1; i++) {
    if (arr[i] === arr[i + 1]) return true;
  }
  return false;  // O(n)
}
```

Цена: O(n log n) сортировка один раз. Выгода: O(n) вместо O(n²).

**3. Нахождение медианы (средний элемент)**

Несортированный массив: Найти n/2-й наименьший элемент. Требует сложный алгоритм (median-of-medians, или сортировка). O(n) в среднем, но трудно.

Отсортированный массив: Медиана на индексе n/2. Прямой доступ. O(1).

**4. Нахождение ближайшей пары (два значения с минимальной разницей)**

Несортированный массив: Сравни каждую пару со всеми другими. O(n²).

Отсортированный массив: Ходи по соседям. Ближайшая пара обязана быть соседями в отсортированном порядке. O(n).

**Решение "сортировать или нет"**

Сортировка всегда стоит O(n log n). Вопрос: стоит ли?

**Сортируй если:**
- Ты будешь делать много запросов (поиски, проверки, медиана) на одних данных. Каждый запрос быстрее, и стоимость сортировки амортизируется.
- Отсортированный порядок сам решает твою задачу (например, найти ближайшую пару, найти медиану).
- Ты в сценарии "читаю много, пишу раз": напиши данные один раз, читай много раз.

**Не сортируй если:**
- Нужна одна операция (один поиск, одна проверка) на данных. O(n log n) сортировка + O(log n) поиск не лучше чем O(n) линейный поиск.
- Данные уже отсортированы (или почти). Используй insertion sort — O(n).
- Сортировка уничтожает информацию, что нужна (например, индексы). Держи несортированную версию.
</Idea>

<DiagramFrame
  label="Поиск значения 9 двумя способами. Сверху: НЕСОРТИРОВАННЫЙ массив из девяти ячеек со значениями 7, 2, 9, 4, 1, 6, 3, 8, 5. Чтобы точно узнать, есть ли 9, линейный скан обязан проверить каждую ячейку слева направо, ведь значение может быть где угодно — затрагивается каждая ячейка, это работа O(n). Снизу: те же девять значений, ОТСОРТИРОВАННЫЕ в 1, 2, 3, 4, 5, 6, 7, 8, 9. Бинарный поиск смотрит только на среднюю ячейку (индекс 4, значение 5), видит, что 9 больше, и прыгает в правую половину (индекс 6, значение 7), всё ещё больше — прыгает вправо снова на индекс 8 и находит 9. Опрошено только три ячейки; каждый шаг делит оставшийся диапазон пополам, это работа O(log n). Сбоку перечислены дополнительные возможности, которые открывает отсортированный массив: проход двумя указателями, поиск дубликатов за один проход и запросы диапазона."
  hue="var(--d-systems)"
  caption="Один sort оплачивает множество быстрых операций. НЕСОРТИРОВАННЫЙ (сверху): чтобы найти значение, нужно сканировать каждую ячейку — O(n) — ведь оно может быть где угодно. ОТСОРТИРОВАННЫЙ (снизу): бинарный поиск проверяет середину, отбрасывает половину массива и повторяет — всего ~log n опросов, чтобы найти 9. Тот же отсортированный порядок открывает проход двумя указателями, поиск дубликатов за один проход и запросы диапазона. Сортировка — это предобработка за O(n log n), которая превращает работу O(n) в O(log n)."
>
  <svg viewBox="0 0 560 340" width="560" height="340">
    <text x="40" y="26" font-size="13" font-family="var(--font-mono)" fill="var(--muted)">{"НЕСОРТ. — найти 9: скан каждой ячейки  → O(n)"}</text>
    <rect x="40" y="44" width="48" height="48" fill="var(--card-2)" stroke="var(--d-systems)" stroke-width="1.4" />
    <rect x="88" y="44" width="48" height="48" fill="var(--card-2)" stroke="var(--d-systems)" stroke-width="1.4" />
    <rect x="136" y="44" width="48" height="48" fill="var(--card-2)" stroke="var(--d-systems)" stroke-width="1.4" />
    <rect x="184" y="44" width="48" height="48" fill="var(--card-2)" stroke="var(--d-systems)" stroke-width="1.4" />
    <rect x="232" y="44" width="48" height="48" fill="var(--card-2)" stroke="var(--d-systems)" stroke-width="1.4" />
    <rect x="280" y="44" width="48" height="48" fill="var(--card-2)" stroke="var(--d-systems)" stroke-width="1.4" />
    <rect x="328" y="44" width="48" height="48" fill="var(--card-2)" stroke="var(--d-systems)" stroke-width="1.4" />
    <rect x="376" y="44" width="48" height="48" fill="var(--card-2)" stroke="var(--d-systems)" stroke-width="1.4" />
    <rect x="424" y="44" width="48" height="48" fill="var(--card-2)" stroke="var(--d-systems)" stroke-width="1.4" />
    <text x="64" y="74" text-anchor="middle" font-size="16" font-family="var(--font-body)" fill="var(--ink)">7</text>
    <text x="112" y="74" text-anchor="middle" font-size="16" font-family="var(--font-body)" fill="var(--ink)">2</text>
    <text x="160" y="74" text-anchor="middle" font-size="16" font-family="var(--font-body)" fill="var(--ink)">9</text>
    <text x="208" y="74" text-anchor="middle" font-size="16" font-family="var(--font-body)" fill="var(--ink)">4</text>
    <text x="256" y="74" text-anchor="middle" font-size="16" font-family="var(--font-body)" fill="var(--ink)">1</text>
    <text x="304" y="74" text-anchor="middle" font-size="16" font-family="var(--font-body)" fill="var(--ink)">6</text>
    <text x="352" y="74" text-anchor="middle" font-size="16" font-family="var(--font-body)" fill="var(--ink)">3</text>
    <text x="400" y="74" text-anchor="middle" font-size="16" font-family="var(--font-body)" fill="var(--ink)">8</text>
    <text x="448" y="74" text-anchor="middle" font-size="16" font-family="var(--font-body)" fill="var(--ink)">5</text>
    <line x1="40" y1="108" x2="472" y2="108" stroke="var(--muted)" stroke-width="1.4" stroke-dasharray="4 3" />
    <path d="M 466 104 L 474 108 L 466 112 Z" fill="var(--muted)" />
    <text x="40" y="126" font-size="11" font-family="var(--font-mono)" fill="var(--muted)">{"шаг 1 → шаг 2 → … → шаг 9 (затронуты все 9 ячеек)"}</text>
    <text x="40" y="170" font-size="13" font-family="var(--font-mono)" fill="var(--muted)">{"СОРТ. — бинарный поиск 9: половинь диапазон → O(log n)"}</text>
    <rect x="40" y="188" width="48" height="48" fill="var(--card-2)" stroke="var(--d-systems)" stroke-width="1.4" />
    <rect x="88" y="188" width="48" height="48" fill="var(--card-2)" stroke="var(--d-systems)" stroke-width="1.4" />
    <rect x="136" y="188" width="48" height="48" fill="var(--card-2)" stroke="var(--d-systems)" stroke-width="1.4" />
    <rect x="184" y="188" width="48" height="48" fill="var(--card-2)" stroke="var(--d-systems)" stroke-width="1.4" />
    <rect x="232" y="188" width="48" height="48" fill="var(--card-2)" stroke="var(--accent)" stroke-width="2.2" />
    <rect x="280" y="188" width="48" height="48" fill="var(--card-2)" stroke="var(--d-systems)" stroke-width="1.4" />
    <rect x="328" y="188" width="48" height="48" fill="var(--card-2)" stroke="var(--accent)" stroke-width="2.2" />
    <rect x="376" y="188" width="48" height="48" fill="var(--card-2)" stroke="var(--d-systems)" stroke-width="1.4" />
    <rect x="424" y="188" width="48" height="48" fill="var(--accent)" stroke="var(--accent)" stroke-width="2.2" />
    <text x="64" y="218" text-anchor="middle" font-size="16" font-family="var(--font-body)" fill="var(--ink)">1</text>
    <text x="112" y="218" text-anchor="middle" font-size="16" font-family="var(--font-body)" fill="var(--ink)">2</text>
    <text x="160" y="218" text-anchor="middle" font-size="16" font-family="var(--font-body)" fill="var(--ink)">3</text>
    <text x="208" y="218" text-anchor="middle" font-size="16" font-family="var(--font-body)" fill="var(--ink)">4</text>
    <text x="256" y="218" text-anchor="middle" font-size="16" font-family="var(--font-body)" fill="var(--ink)">5</text>
    <text x="304" y="218" text-anchor="middle" font-size="16" font-family="var(--font-body)" fill="var(--ink)">6</text>
    <text x="352" y="218" text-anchor="middle" font-size="16" font-family="var(--font-body)" fill="var(--ink)">7</text>
    <text x="400" y="218" text-anchor="middle" font-size="16" font-family="var(--font-body)" fill="var(--ink)">8</text>
    <text x="448" y="218" text-anchor="middle" font-size="16" font-family="var(--font-body)" fill="var(--on-accent)">9</text>
    <text x="256" y="252" text-anchor="middle" font-size="10" font-family="var(--font-mono)" fill="var(--accent)">{"опрос 1"}</text>
    <text x="352" y="252" text-anchor="middle" font-size="10" font-family="var(--font-mono)" fill="var(--accent)">{"опрос 2"}</text>
    <text x="448" y="252" text-anchor="middle" font-size="10" font-family="var(--font-mono)" fill="var(--accent)">{"найдено"}</text>
    <path d="M 256 268 C 290 296, 318 296, 352 270" fill="none" stroke="var(--accent)" stroke-width="1.6" />
    <path d="M 346 268 L 354 270 L 348 276 Z" fill="var(--accent)" />
    <path d="M 352 268 C 386 296, 414 296, 448 270" fill="none" stroke="var(--accent)" stroke-width="1.6" />
    <path d="M 442 268 L 450 270 L 444 276 Z" fill="var(--accent)" />
    <text x="40" y="312" font-size="11" font-family="var(--font-mono)" fill="var(--ink)">{"9 > 5 → вправо    9 > 7 → вправо    = 9 ✓  (3 опроса, не 9)"}</text>
    <text x="40" y="330" font-size="11" font-family="var(--font-mono)" fill="var(--muted)">{"сорт. также открывает: два указателя · дедуп за проход · запросы диапазона"}</text>
  </svg>
</DiagramFrame>

<Code lang="ru">
Запрограммируем пример поиска дубликатов, показав огромную разницу.

**O(n²) без сортировки:**
```js
function hasDuplicateNaive(arr) {
  // Сравни каждый элемент со всеми другими
  for (let i = 0; i < arr.length; i++) {
    for (let j = i + 1; j < arr.length; j++) {
      if (arr[i] === arr[j]) {
        return true;  // Нашли дубликат
      }
    }
  }
  return false;  // Нет дубликатов
}
```

**O(n log n) с сортировкой:**
```js
function hasDuplicateSorted(arr) {
  // Сначала, отсортируй массив
  arr.sort((a, b) => a - b);
  
  // Теперь проверь только соседей
  for (let i = 0; i < arr.length - 1; i++) {
    if (arr[i] === arr[i + 1]) {
      return true;  // Нашли дубликат
    }
  }
  return false;  // Нет дубликатов
}
```

Давайте запустим оба на разных размерах:

<CodeRunner
  id="cr-sort-benefit"
  lang="ru"
  code={`function hasDuplicateNaive(arr) {
  let comparisons = 0;
  for (let i = 0; i < arr.length; i++) {
    for (let j = i + 1; j < arr.length; j++) {
      comparisons++;
      if (arr[i] === arr[j]) {
        return { found: true, comparisons };
      }
    }
  }
  return { found: false, comparisons };
}

function hasDuplicateSorted(arr) {
  let comparisons = 0;
  // Считай сравнения в sort (упрощено: n log n)
  comparisons = Math.ceil(arr.length * Math.log2(arr.length));
  
  arr.sort((a, b) => a - b);
  
  // Считай сравнения соседей
  for (let i = 0; i < arr.length - 1; i++) {
    comparisons++;
    if (arr[i] === arr[i + 1]) {
      return { found: true, comparisons };
    }
  }
  return { found: false, comparisons };
}

const testArray = [3, 7, 2, 9, 1, 5, 8, 4, 6, 10];
const withoutSort = hasDuplicateNaive([...testArray]);
const withSort = hasDuplicateSorted([...testArray]);

console.log("Массив:", testArray);
console.log("Наивный (без sort):", withoutSort.comparisons, "сравнений");
console.log("С sort + проверка соседей:", withSort.comparisons, "сравнений");

// Больший массив
const big = Array.from({length: 1000}, (_, i) => i % 100);
const big1 = hasDuplicateNaive([...big]);
const big2 = hasDuplicateSorted([...big]);
console.log("Для 1000 элементов:");
console.log("  Наивный:", big1.comparisons, "сравнений");
console.log("  Отсортированный:", big2.comparisons, "сравнений");`}
/>
</Code>

<Trace lang="ru">
Давайте трассируем поиск дубликатов рядом. Массив: [5, 2, 5, 8, 1].

**Несортированный (O(n²)):**

<AlgoTrace
  id="trace-unsorted-dupe"
  lang="ru"
  code={[
    "for (let i = 0; i < arr.length; i++) {",
    "  for (let j = i + 1; j < arr.length; j++) {",
    "    if (arr[i] === arr[j]) return true;",
    "  }",
    "}"
  ]}
  frames={[
    {
      caption: "Массив [5, 2, 5, 8, 1]. Сравни arr[0]=5 со всеми позже.",
      line: 2,
      cells: [
        { value: "5", mark: "pivot" },
        "2",
        "5",
        "8",
        "1"
      ]
    },
    {
      caption: "arr[0]=5 vs arr[2]=5 — совпадение! Вернись true.",
      line: 3,
      cells: [
        { value: "5", mark: "pivot" },
        "2",
        { value: "5", mark: "match" },
        "8",
        "1"
      ]
    }
  ]}
/>

Нашли совпадение после 2 сравнений — повезло. Худший случай: нет дубликатов. Сравнили бы все пары: 1 + 2 + 3 + 4 = 10 сравнений для всего 5 элементов. Это O(n²).

**Отсортированный (O(n log n)):**

После сортировки [5, 2, 5, 8, 1] → [1, 2, 5, 5, 8]:

<AlgoTrace
  id="trace-sorted-dupe"
  lang="ru"
  code={[
    "arr.sort(...);",
    "for (let i = 0; i < arr.length - 1; i++) {",
    "  if (arr[i] === arr[i + 1]) return true;",
    "}"
  ]}
  frames={[
    {
      caption: "Отсортированный массив [1, 2, 5, 5, 8]. Ходи по соседям.",
      line: 2,
      cells: [
        "1",
        "2",
        { value: "5", mark: "pivot" },
        { value: "5", mark: "next" },
        "8"
      ]
    },
    {
      caption: "arr[2]=5 vs arr[3]=5 — совпадение! Вернись true.",
      line: 3,
      cells: [
        "1",
        "2",
        { value: "5", mark: "match" },
        { value: "5", mark: "match" },
        "8"
      ]
    }
  ]}
/>

Только 1 сравнение после сортировки (плюс O(n log n) sort). Для большого массива без дубликатов, сканировали бы отсортированный массив один раз: 4 сравнения, а не 10.
</Trace>

<Complexity lang="ru">
Вот сравнение сложности для поиска дубликатов:

| Подход | Полная стоимость | Когда использовать |
|---|---|---|
| Вложенный цикл (без sort) | O(n²) | Только маленькие массивы (n ≤ ~100) |
| Sort + проверка соседей | O(n log n) | Средние+ массивы, особенно если нужен порядок потом |

Для разных размеров массива:

| n | O(n²) | O(n log n) |
|---|---|---|
| 100 | 10 000 ops | ~700 ops |
| 1 000 | 1 000 000 ops | ~10 000 ops |
| 10 000 | 100 000 000 ops | ~130 000 ops |
| 100 000 | 10 000 000 000 ops | ~1 700 000 ops |

**Паттерн держится для всех задач, разблокируемых сортировкой:** Нахождение медианы, нахождение ближайшей пары, слияние отсортированных массивов. Один раз отсортирован, остальное линейно или логарифмично.
</Complexity>

<AlgoPractice
  id="practice-why-sort-1"
  lessonSlug="01-why-sort"
  lang="ru"
  problems={[
    {
      kind: "choice",
      prompt: "У тебя несортированный массив, и нужно узнать, есть ли конкретное значение. Лучше отсортировать первым или сканировать линейно?",
      choices: [
        "Отсортировать первым (O(n log n) + O(log n) бинарный поиск) всегда лучше",
        "Линейный скан (O(n)) лучше, если один поиск, потому что O(n log n) sort добавляет лишнюю стоимость",
        "Зависит от языка, что ты используешь",
        "Сортировка никогда не стоит для одного поиска"
      ],
      answer: 1,
      hint: "Для одной операции O(n log n) + O(log n) > O(n). Не сортируй, пока не переиспользуешь порядок."
    },
    {
      kind: "choice",
      prompt: "Ты отсортировал массив за O(n log n), потом поискал его 100 раз бинарным поиском (O(log n) за раз). Какова полная стоимость?",
      choices: [
        "O(n log n) — поиски незначительны",
        "O(n log n) + 100 × O(log n) = O(n log n) приблизительно",
        "O(100 × n log n) — константа 100 делает её намного хуже",
        "O(n) — сортировка так быстра, что исчезает"
      ],
      answer: 1,
      hint: "Sort O(n log n) один раз. Каждый поиск O(log n). Итого: O(n log n) + O(100 log n) ≈ O(n log n) для больших n."
    },
    {
      kind: "choice",
      prompt: "Какова сложность поиска дубликатов в отсортированном массиве, пройдя соседей?",
      choices: [
        "O(n log n) потому что пришлось сортировать",
        "O(n) — просто один скан соседей",
        "O(n²) потому что сравниваешь пары",
        "O(log n) благодаря бинарному поиску"
      ],
      answer: 1,
      hint: "Проверка соседей O(n). Сортировка — отдельная первоначальная стоимость. Сам шаг поиска дубликатов O(n)."
    },
    {
      kind: "choice",
      prompt: "Когда НЕ стоит сортировать перед решением задачи?",
      choices: [
        "Всегда сортируй, чтобы быть в безопасности",
        "Когда делаешь одну операцию, и линейное время подходит (например, один поиск на маленьком n)",
        "Когда массив очень большой",
        "Когда нужна 100% точность результата"
      ],
      answer: 1,
      hint: "Сортировка фиксированная цена O(n log n). Если делаешь одну операцию, эта цена может превысить прямое линейное решение."
    },
    {
      kind: "choice",
      prompt: "Нужно найти медиану (средний элемент) несортированного массива. После сортировки, какова стоимость нахождения медианы?",
      choices: [
        "O(n log n) потому что отсортировал",
        "O(1) — прямой доступ к индексу n/2",
        "O(n) потому что сканируешь весь массив",
        "O(log n) потому что используешь бинарный поиск"
      ],
      answer: 1,
      hint: "Один раз отсортирован, медиана на фиксированном индексе. Прямой доступ к arr[n/2] это O(1), хотя sort стоил O(n log n) в начале."
    }
  ]}
/>

<Inset kind="edgecase" lang="ru">
**Сортировка на месте vs копирование.** Если отсортируешь исходный массив, потеряешь первоначальный порядок. Иногда нужны оба — отсортированный порядок для запросов и первоначальные индексы. В этом случае создай копию массива для сортировки, или используй массив индексов, чтобы не менять исходный. Это стоит дополнительную память, но сохраняет информацию.
</Inset>

<Inset kind="mistake" lang="ru">
**Переизбыточная сортировка.** Не сортируй "чтобы быть в безопасности" для одной операции. Если поищешь один раз или проверишь дубликаты один раз, линейное время дешевле сортировки. Сортируй только когда переиспользуешь отсортированный порядок много раз, или когда сама задача требует порядка (медиана, ближайшая пара и т. д.).
</Inset>

<Check lang="ru">
  <Quiz
    id="quiz-sort-1"
    pieceSlug="01-why-sort"
    lang="ru"
    question="Нужно обнаружить дубликаты в массиве из 100 000 элементов. Используешь ли ты O(n²) вложенные циклы или O(n log n) sort + скан соседей?"
    choices={[
      {
        label: "O(n log n) sort + O(n) скан соседей. Итого: O(n log n) операций, намного лучше чем O(n²) = 10 миллиардов.",
        correct: true
      },
      {
        label: "O(n²) вложенные циклы потому что сортировка слишком сложна.",
        misconception: "Сортировка проще в коде и намного быстрее на практике. 10 миллиардов операций — это слишком медленно."
      },
      {
        label: "Оба одинаковые; не важно.",
        misconception: "O(n log n) ≈ 1,7 миллиона операций. O(n²) ≈ 10 миллиардов операций. Разница огромная."
      },
      {
        label: "Ответ зависит от конкретных значений в массиве.",
        misconception: "Сложность независима от значений. Зависит только от алгоритма и n."
      }
    ]}
  />
</Check>

<Recap lang="ru">
**Что такое сортировка?** Упорядочение элементов в неубывающем (или невозрастающем) порядке.

**Зачем сортировать?** Сортировка — предварительная обработка: потрать O(n log n) один раз, получи выгоды потом.

**Какие задачи становятся лёгкими?**
1. **Поиск:** O(n log n) sort → O(log n) бинарный поиск. Итого: O(n log n).
2. **Поиск дубликатов:** O(n log n) sort → O(n) скан соседей. Итого: O(n log n).
3. **Медиана:** O(n log n) sort → O(1) прямой доступ. Итого: O(n log n).
4. **Ближайшая пара:** O(n log n) sort → O(n) скан соседей. Итого: O(n log n).

**Когда сортировать:**
- Будешь делать много запросов или операций на данных.
- Задача требует отсортированный порядок (медиана, ближайшая пара, слияние).
- Ты в сценарии "читаю много": напиши один раз, читай много.

**Когда НЕ сортировать:**
- Делаешь одну операцию. O(n) линейный дешевле чем O(n log n) sort + O(log n) поиск.
- Данные уже отсортированы. Используй более быструю сортировку (insertion sort) или пропусти.

**Основное понимание:** Сортировка не магия; стоит O(n log n). Но разблокирует O(log n) поиск, O(n) поиск дубликатов, O(n) нахождение медианы. Вот трейдофф. Плати один раз, получай выгоду много раз.

Далее ты узнаешь конкретные алгоритмы сортировки — простые sorts (bubble, insertion, selection), что медленные, и быстрые sorts (merge sort, quicksort), что эффективные.
</Recap>
