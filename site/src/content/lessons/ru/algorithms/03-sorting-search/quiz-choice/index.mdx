---
concepts:
- sorting-as-preprocessing
- stability
- divide-and-conquer
- pivot-choice
- binary-search-invariant
- monotonic-predicate
estMin: 13
lang: ru
level: middle
order: 7
prereqs: []
slug: quiz-choice
sources:
- https://en.wikipedia.org/wiki/Merge_sort
- https://en.wikipedia.org/wiki/Quicksort
- https://en.wikipedia.org/wiki/Binary_search_algorithm
status: ready
summary: "Синтез по всему юниту в формате выбора: когда сортировка окупается, stable-сортировки, merge sort против quicksort, инвариант binary search и бинарный поиск по ответу."
title: 'Сортировка и поиск: тест с выбором'
track: algorithms
unit: 03-sorting-search
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Шесть вопросов через весь юнит. Каждый — это решение, которое вы принимаете, когда берётесь за сортировку или поиск в реальном коде: не определение для пересказа, а компромисс, который надо взвесить.
</Hook>

<Goal lang="ru">
Убедитесь, что вы связываете стержень юнита: сортировка — это препроцессинг, за который платят один раз; сортировки за O(n log n) меняют stable-свойство на память и худший случай; а binary search обобщается с массивов на любое монотонное пространство ответов.
</Goal>

<Quiz
  id="03-sorting-search-choice-1"
  lessonSlug="03-sorting-search"
  lang="ru"
  question="Вам пришёл неотсортированный массив из 1 000 000 элементов, и нужно ответить ровно на один запрос: есть ли в нём значение X? Какой подход дешевле всего и при этом корректен?"
  choices={[
    { label: "Линейный проход, O(n). Сортировка ради binary search стоит O(n log n) заранее, и это уже перекрывает единственный запрос за O(log n) — сортировка окупается только при многократном переиспользовании порядка.", correct: true },
    { label: "Сначала отсортировать, потом binary search — O(n log n) + O(log n) всегда быстрее.", misconception: "Для одного запроса O(n log n) сортировки плюс O(log n) поиска строго хуже одного прохода за O(n). Сортировка — это препроцессинг: она окупается, только когда амортизируется по многим последующим запросам." },
    { label: "Построить hash set и искать в нём — это бьёт оба варианта.", misconception: "Построение множества — это тоже O(n), тот же порядок, что и проход, плюс лишняя память и никакой выгоды для одного поиска. Смысл в том, что любой препроцессинг впустую для единственного запроса." },
    { label: "Применить binary search прямо к неотсортированному массиву.", misconception: "Binary search требует отсортированного массива. На неотсортированном входе он отбрасывает не ту половину и возвращает неверные ответы — отсортированность это его предусловие." },
  ]}
/>

<Quiz
  id="03-sorting-search-choice-2"
  lessonSlug="03-sorting-search"
  lang="ru"
  question="Вы сортируете список заказов по сумме, где несколько заказов имеют одинаковую сумму, и нужно, чтобы при равенстве сохранялся исходный порядок поступления. Какая сортировка безопасна?"
  choices={[
    { label: "stable-сортировка вроде merge sort: равные элементы сохраняют входной порядок, потому что слияние при равенстве берёт сначала из левой половины (left[i] <= right[j]). Обычный quicksort не stable.", correct: true },
    { label: "Quicksort — он самый быстрый, значит лучше всех сохраняет порядок.", misconception: "Скорость не имеет отношения к stable-свойству. Quicksort с partition по схеме Lomuto может переставлять равные ключи, перекидывая их через границу pivot; он не stable." },
    { label: "Selection sort, потому что он лишь меняет местами минимумы.", misconception: "Selection sort тоже не stable: перенос минимума на место может перепрыгнуть один равный элемент через другой, ломая порядок поступления. Stable-варианты — insertion sort и merge sort." },
    { label: "Любая сортировка за O(n log n), ведь stable-свойство — это свойство сложности.", misconception: "Stable-свойство не зависит от сложности. Merge sort (O(n log n)) — stable; quicksort и heapsort (тоже O(n log n)) — нет. Выбирать надо алгоритм, а не только асимптотику." },
  ]}
/>

<Quiz
  id="03-sorting-search-choice-3"
  lessonSlug="03-sorting-search"
  lang="ru"
  question="Merge sort и quicksort оба O(n log n) в среднем. Для сортировки огромного массива на месте при жёстком ограничении по памяти и без требования stable — почему quicksort часто выбирают в продакшене?"
  choices={[
    { label: "Quicksort делает partition на месте, тратя лишь O(log n) стека, тогда как merge sort выделяет O(n) дополнительной памяти под буфер слияния. При хороших pivot у quicksort к тому же меньше константы и лучше работа с кэшем.", correct: true },
    { label: "Quicksort O(n log n) в худшем случае, а merge sort — нет.", misconception: "Всё наоборот: merge sort всегда O(n log n), а quicksort деградирует до O(n²) на плохих pivot. Quicksort выигрывает по памяти и константам, а не по худшему случаю." },
    { label: "Merge sort не stable и потому переставляет данные непредсказуемо.", misconception: "Stable — это как раз merge sort. Причина предпочесть quicksort — это O(n) буфер merge sort, а не какая-либо проблема со stable-свойством." },
    { label: "Quicksort не использует рекурсию, поэтому избегает роста стека.", misconception: "Quicksort тоже рекурсивен; на сбалансированных partition он тратит O(log n) стека. Преимущество — partition на месте против O(n) вспомогательного массива merge sort." },
  ]}
/>

<Quiz
  id="03-sorting-search-choice-4"
  lessonSlug="03-sorting-search"
  lang="ru"
  question="Quicksort, который всегда берёт последний элемент как pivot, нормально работает на случайных данных, но тратит минуты на уже отсортированных. Почему и как это стандартно чинят?"
  choices={[
    { label: "На отсортированном входе последний элемент всегда наибольший, поэтому каждый partition делит на пустую сторону и сторону из n-1: n уровней по O(n) работы = O(n²). Чинят так: рандомизируют pivot или берут median-of-three, чтобы partition оставался сбалансированным.", correct: true },
    { label: "На отсортированном входе сравнения становятся дороже.", misconception: "Сравнения стоят столько же; проблема в форме partition. Фиксированный последний pivot на отсортированных данных всегда экстремален и даёт максимально несбалансированную рекурсию O(n²)." },
    { label: "Quicksort не может корректно отсортировать уже отсортированные данные.", misconception: "Он сортирует корректно — просто медленно. Результат верный; вырождается рекурсия, превращая O(n log n) в O(n²)." },
    { label: "Рекурсия исчерпывает стек и молча перезапускается.", misconception: "Никакого молчаливого перезапуска нет; глубокая рекурсия может переполнить стек, но суть в работе O(n²) из-за несбалансированных partition. Рандомизированный pivot возвращает ожидаемое O(n log n)." },
  ]}
/>

<Quiz
  id="03-sorting-search-choice-5"
  lessonSlug="03-sorting-search"
  lang="ru"
  question="Binary search использует while (lo < hi) с hi = arr.length - 1 и возвращает -1 после цикла. На большинстве входов он работает, но промахивается, когда цель стоит в позиции, где lo равно hi. В чём дефект?"
  choices={[
    { label: "Условие lo < hi выходит из цикла, пока один кандидат-индекс (lo == hi) ещё не проверен. Для включающего диапазона [lo, hi] условие должно быть lo <= hi, чтобы финальный диапазон из одного элемента был проверен.", correct: true },
    { label: "hi должен начинаться с arr.length, а не arr.length - 1.", misconception: "При включающем диапазоне hi = arr.length - 1 корректно; arr.length дало бы выход за границы. Дефект — строгое < в условии цикла, оставляющее ячейку lo == hi непроверенной." },
    { label: "mid надо считать как (lo + hi + 1) / 2, чтобы избежать промаха.", misconception: "Округление mid здесь ни при чём; цикл просто завершается до проверки индекса lo == hi. Поменяйте условие на lo <= hi." },
    { label: "Binary search в принципе не может найти последний элемент массива.", misconception: "Может, при корректном инварианте. Промах — чисто ошибка на единицу в границе цикла, а не ограничение binary search." },
  ]}
/>

<Quiz
  id="03-sorting-search-choice-6"
  lessonSlug="03-sorting-search"
  lang="ru"
  question="Нужно найти минимальную грузоподъёмность корабля, чтобы доставить все посылки за D дней, и нет отсортированного массива грузоподъёмностей, по которому можно индексироваться. Когда binary search всё равно применим?"
  choices={[
    { label: "Когда feasible(capacity) монотонна: если какой-то грузоподъёмности хватает уложиться в D дней, то любой большей тоже хватает. Вы ведёте binary search по диапазону ответов [max_package, sum_of_packages], ища наименьшее подходящее значение.", correct: true },
    { label: "Никогда — binary search требует явного отсортированного массива для индексации.", misconception: "Binary search требует отсортированного/монотонного пространства поиска, а не буквального массива. Монотонный предикат feasible() позволяет так же делить диапазон ответов пополам." },
    { label: "Только если веса посылок сами отсортированы.", misconception: "Порядок весов не важен; проверка осуществимости работает с ними как есть. Монотонна должна быть feasible(capacity) при росте capacity." },
    { label: "Когда feasible() возвращает true, потом false, потом снова true при росте capacity.", misconception: "Это как раз немонотонный случай, где binary search ломается: переход, который разворачивается обратно, рушит логику отбрасывания половины. Нужен единственный переход false→true." },
  ]}
/>

<Recap lang="ru">
Сквозная мысль юнита — одна привычка: решить, стоит ли покупать порядок. Сортируйте, только если переиспользуете порядок; берите stable-сортировку (merge, insertion), когда равные элементы должны сохранять место; меняйте O(n) буфер merge sort на in-place O(log n) стек quicksort, когда памяти мало, и помните, что плохие pivot грозят O(n²), если не рандомизировать. Когда порядок есть, binary search находит ответы за O(log n) — но только при корректном включающем инварианте, и обобщается на любой монотонный предикат, а не только на массивы.
</Recap>
