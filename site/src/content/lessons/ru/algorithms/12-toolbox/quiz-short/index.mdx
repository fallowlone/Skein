---
concepts:
- technique-selection
- constraint-reading
- complexity-budget
- data-structure-choice
- pattern-recognition
- decision-tree
estMin: 14
lang: ru
level: middle
order: 8
prereqs: []
slug: quiz-short
sources:
- https://en.wikipedia.org/wiki/Time_complexity
- https://en.wikipedia.org/wiki/Dynamic_programming
- https://en.wikipedia.org/wiki/Greedy_algorithm
status: ready
summary: "Свободное припоминание по выбору техники через весь трек — восстанови дерево решений от ограничений и формы задачи к верному инструменту."
title: 'Инструментарий: свободное припоминание'
track: algorithms
unit: 12-toolbox
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";

<Hook>
Распознавание паттернов закрепляется, только когда ты можешь выдать дерево решений по памяти под давлением. На каждый вопрос проговори или запиши всё рассуждение — от подсказки в задаче к технике и её цене — прежде чем открыть модельный ответ.
</Hook>

<Goal lang="ru">
Восстанови логику выбора всего трека, не подглядывая: как читать ограничения в бюджет сложности, какие глаголы задачи отображаются на какое семейство техник и почему соблазнительная альтернатива — ловушка.
</Goal>

<RetrievalDrawer
  client:load
  id="12-toolbox-short-retrieval"
  lang="ru"
  questions={[
    { q: "Как превратить ограничение на размер входа в бюджет сложности до выбора техники?", a: "Прими грубый потолок около 10^8 простых операций в секунду для лимита в одну секунду. Прочти n и иди обратно: n <= 20 допускает 2^n или n! (порядка 10^6 и выше, реально лишь при сильном отсечении) — думай о битмасках или backtracking; n <= 5000 допускает O(n^2); n <= 10^5..10^6 допускает O(n log n) — думай о сортировке, кучах или одном проходе хеша; n <= 10^9 запрещает даже прочитать весь вход — значит O(log n), O(sqrt n) или формула. Ограничение не декорация — это самая сильная подсказка о семействе техники, которую заложил автор, часто красноречивее самого текста." },
    { q: "Сопоставь каждый глагол задачи семейству техник: 'существует ли X / посчитать различные', 'k-й наименьший / следующий по приоритету', 'число способов или оптимизация по перекрывающимся подзадачам', 'лучший локальный выбор, который можно обосновать'.", a: "'Существует ли X', 'посчитать различные' или 'видел ли я это' отображается на хеширование — hash set или map для O(1) проверки принадлежности в среднем, обмен памяти на время. 'k-й наименьший' или 'всегда выдавать следующий по приоритету' отображается на кучу или приоритетную очередь: O(log n) на вставку и извлечение, экстремум всегда в корне. 'Сколько способов' или 'минимизировать или максимизировать, когда подзадачи перекрываются и выборы складываются' отображается на динамическое программирование — найди состояние и рекуррентность с оптимальной подструктурой. 'Многократно брать лучший локальный выбор' отображается на жадность, но только когда можно подкрепить аргументом обмена или матроидом; иначе это ловушка, и ты откатываешься к DP или поиску." },
    { q: "Когда появляется отсортированный вход, какие две техники он открывает и как выбрать между ними?", a: "Отсортированный (или сортируемый) вход открывает бинарный поиск и two pointers. Бинарный поиск — когда ищешь одно значение, границу или делишь пополам монотонный предикат (бинарный поиск по ответу): O(log n) на запрос. Two pointers (или скользящее окно) — когда проходишь структуру в поиске отношения между элементами: пара с суммой, ближайшая пара, длиннейшее допустимое окно — за O(n) и обычно O(1) памяти. Решающий фактор — память и шаблон доступа: two pointers не требует доп. памяти и одного линейного прохода; бинарный поиск блистает на повторных точечных запросах. Если массив неотсортирован и нужна лишь принадлежность или дополнения, hash map часто бьёт саму сортировку." },
    { q: "Различи, когда графовая задача требует BFS, Дейкстры, топологической сортировки или Union-Find.", a: "BFS даёт кратчайшие пути в НЕВЗВЕШЕННОМ графе (меньше всего рёбер) и это обходчик по умолчанию слой за слоем. Дейкстра даёт кратчайшие пути при НЕОТРИЦАТЕЛЬНЫХ весах через min-heap; при отрицательных рёбрах переходишь к Беллману-Форду. Топологическая сортировка упорядочивает ориентированный ацикличный граф по зависимостям и одновременно ловит циклы (если не удаётся упорядочить все вершины — есть цикл) — инструмент порядков сборки и планирования. Union-Find (система непересекающихся множеств) отвечает на связность и поиск цикла в НЕориентированных графах и двигает MST Краскала. Ошибка — тянуться к Дейкстре на невзвешенном графе (BFS проще и быстрее) или к Union-Find на ориентированном графе зависимостей (нужна топосортировка)." },
    { q: "Почему распознавание перекрывающихся подзадач — переломный момент в решении задач, и каковы три реакции на него?", a: "Если одна и та же подзадача повторяется в дереве рекурсии, наивное рекурсивное или backtracking-решение пересчитывает её экспоненциально часто — это самая частая причина, по которой корректная грубая сила упирается в таймаут. Распознавание этого — переход от экспоненты к полиному. Три реакции: (1) мемоизировать рекурсию сверху вниз (кэш по состоянию) — наименьшая правка кода; (2) табулировать снизу вверх (заполнить DP-таблицу в порядке зависимостей) — лучше константы и нет лимита рекурсии; (3) если подзадачи НЕ перекрываются — оставить как divide-and-conquer или backtracking с отсечением. Диагностика — нарисовать два уровня дерева рекурсии и проверить, повторяются ли одинаковые аргументы." },
    { q: "Приведи полный поток решений с вершины дерева, который ты запускаешь на новой задаче.", a: "(1) Прочти ограничения и выведи бюджет сложности — это часто сразу исключает целые семейства. (2) Определи, что задача просит (глагол): существование, подсчёт, оптимум, упорядочивание, путь, k-й. (3) Отметь структурные подсказки: отсортированный вход → бинарный поиск или two pointers; дерево или сетка → DFS или BFS; узлы и рёбра → графовые алгоритмы; малое n с ответом-подмножеством → битмаски или backtracking. (4) Прощупай перекрывающиеся подзадачи → DP; доказуемый жадный выбор → жадность. (5) Выбери структуру данных, делающую доминирующую операцию дешёвой (хеш для принадлежности, куча для приоритета, стек для last-in-first-out, очередь для обхода по слоям). (6) Сверь выбранную сложность с бюджетом из шага 1 до написания кода. Инструментарий — это не зубрёжка алгоритмов, а быстрый и корректный прогон этого потока." },
  ]}
/>

<Recap lang="ru">
Если ты смог восстановить каждый ответ без подсказок, ты держишь хребет трека: ограничения становятся бюджетом сложности, глагол задачи называет семейство техники, отсортированный вход открывает бинарный поиск или two pointers, графовый вариант выбирает BFS / Дейкстру / топосортировку / Union-Find, а замеченные перекрывающиеся подзадачи — переход от экспоненты к полиному. Инструментарий — это один поток решений, прогнанный быстро: бюджет, глагол, структура, проверка подзадач, структура данных, сверка — а не куча заученных алгоритмов.
</Recap>
