---
concepts:
- pattern-recognition
- data-structure-choice
- complexity-tradeoff
- technique-selection
- code-reading
estMin: 14
lang: ru
level: middle
order: 9
prereqs: []
slug: quiz-code
sources:
- https://en.wikipedia.org/wiki/Dynamic_programming
- https://en.wikipedia.org/wiki/Sliding_window_protocol
- https://en.wikipedia.org/wiki/Heap_(data_structure)
status: ready
summary: "Прочти четыре наброска кода, распознай, какую технику каждый реализует (или должен), и выбери верную структуру данных или самую рычажную правку."
title: 'Инструментарий: распознавание паттернов в коде'
track: algorithms
unit: 12-toolbox
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
На собеседовании или код-ревью тебе редко дают помеченную задачу — тебе дают фрагмент и ограничение. Навык в том, чтобы прочесть цикл и назвать технику, которую он реализует, или заметить, что выбранная структура неверна. Прочти каждый набросок, затем ответь.
</Hook>

<Goal lang="ru">
Потренируй распознавательную половину инструментария: по коду определи паттерн, оцени, подходит ли структура данных доминирующей операции, и выбери изменение, которое реально сдвигает сложность.
</Goal>

## Фрагмент 1 — вложенный цикл по отсортированным данным

```python
def has_pair_with_sum(a, target):   # a is sorted ascending
    n = len(a)
    for i in range(n):
        for j in range(i + 1, n):
            if a[i] + a[j] == target:
                return True
    return False
```

<Quiz
  id="12-toolbox-code-1"
  lessonSlug="12-toolbox"
  lang="ru"
  question="Вход отсортирован, но это O(n^2). К какому паттерну зовёт отсортированный вход и какова итоговая сложность?"
  choices={[
    { label: "Two pointers: lo=0, hi=n-1; если a[lo]+a[hi] меньше target — двигай lo вверх, если больше — двигай hi вниз, иначе найдено. Отсортированность позволяет каждый шаг отбрасывать один конец, давая O(n) времени и O(1) памяти.", correct: true },
    { label: "Добавить мемоизацию во внутренний цикл.", misconception: "Здесь нет перекрывающихся подзадач — каждая пара рассматривается один раз. Мемоизация кэширует повторные подрезультаты, которых нет. Правка — использовать отсортированный порядок через two pointers, а не кэшировать." },
    { label: "Это уже оптимально; поиск пары по своей природе O(n^2).", misconception: "Пара-с-суммой на отсортированном входе — это O(n) через two pointers, либо O(n) в среднем через hash set на неотсортированном входе. Грубая сила O(n^2) игнорирует самую сильную подсказку задачи: массив отсортирован." },
    { label: "Перейти на кучу, чтобы ускорить сравнения.", misconception: "Куча даёт экстремальный элемент, а не отношения между парами, и данные уже отсортированы, так что куча ничего не добавляет. Паттерн, на который указывает отсортированность, — проход two pointers." },
  ]}
/>

## Фрагмент 2 — рекурсия, которая пересчитывает

```python
def ways(n, coins):
    if n == 0: return 1
    if n < 0 or not coins: return 0
    # use coins[0] again, or drop it
    return ways(n - coins[0], coins) + ways(n, coins[1:])
```

<Quiz
  id="12-toolbox-code-2"
  lessonSlug="12-toolbox"
  lang="ru"
  question="Это корректно, но экспоненциально при большом n. Каков точный диагноз и стандартная правка?"
  choices={[
    { label: "Одно и то же состояние (n, индекс-монеты) пересчитывается во многих ветвях — перекрывающиеся подзадачи. Мемоизируй по (остаток, индекс) или табулируй 2D DP-таблицу; это сворачивает до O(n * len(coins)).", correct: true },
    { label: "Базовые случаи неверны, что вызывает бесконечную рекурсию.", misconception: "Базовые случаи (n==0 даёт 1, n<0 или пусто даёт 0) корректны, и рекурсия завершается. Цена — не баг, а экспоненциальный повторный обход одинаковых состояний (остаток, индекс) — это и есть сигнал DP." },
    { label: "Заменить на жадный проход 'сначала наибольшая монета'.", misconception: "Жадность считает максимум один способ, а не все различные, и может ошибаться для неканонических номиналов. Подсчёт комбинаций требует суммирования по подзадачам — это DP; жадность отвечает на другой вопрос." },
    { label: "Использовать two pointers по массиву монет.", misconception: "Two pointers решает задачи отношения на отсортированной последовательности, а не подсчёт по рекурсивному пространству выборов. Перекрывающиеся состояния (остаток, индекс) — безошибочный сигнал к мемоизации или табуляции." },
  ]}
/>

## Фрагмент 3 — текущий k-й элемент

```python
import heapq

def stream_kth_largest(stream, k):
    h = []                       # what kind of heap?
    for x in stream:
        heapq.heappush(h, x)     # Python heapq is a MIN-heap
        if len(h) > k:
            heapq.heappop(h)     # pops the smallest
    return h[0]                  # the k-th largest seen so far
```

<Quiz
  id="12-toolbox-code-3"
  lessonSlug="12-toolbox"
  lang="ru"
  question="Этот код держит текущий k-й наибольший через min-heap размера k. Почему именно MIN-heap (а не max-heap) — верная структура здесь?"
  choices={[
    { label: "Чтобы держать k НАИБОЛЬШИХ, нужно дёшево вытеснять наименьший из текущих k при приходе большего — этот наименьший лежит в корне min-heap. Тогда корень и есть ровно k-й наибольший. O(n log k) времени, O(k) памяти.", correct: true },
    { label: "Max-heap был бы корректнее и быстрее.", misconception: "Max-heap кладёт наибольший в корень, так что вытеснение нужного элемента (наименьшего из хранимых k) стоило бы O(k)-скана каждый раз. Min-heap делает вытеснение O(log k). Зеркальная логика даёт max-heap для k наименьших." },
    { label: "Подойдёт любая куча; выбор произволен.", misconception: "Выбор задан тем, какой элемент нужно дёшево вытеснять. Хранить k наибольших значит выбрасывать текущий наименьший, поэтому наименьший должен быть в корне — это min-heap. Ориентация не произвольна." },
    { label: "Отсортированный список был бы здесь столь же эффективен.", misconception: "Поддержка отсортированного списка стоит O(k) на вставку (сдвиг элементов), давая O(n*k) в сумме против O(n log k) у кучи. Для потокового top-k куча — верная структура именно потому, что вставка и вытеснение оба O(log k)." },
  ]}
/>

## Фрагмент 4 — неограниченное окно

```python
def longest_unique_substring(s):
    seen = set()
    best = 0
    for r in range(len(s)):
        while s[r] in seen:
            seen.add(s[r])       # BUG: should shrink from the left
        seen.add(s[r])
        best = max(best, len(seen))
    return best
```

<Quiz
  id="12-toolbox-code-4"
  lessonSlug="12-toolbox"
  lang="ru"
  question="Задумана техника скользящего окна по строке. Окно никогда не сжимается, поэтому цикл сломан. Каков верный механизм?"
  choices={[
    { label: "Держи левый указатель; когда s[r] уже в окне, продвигай left и удаляй s[left] из множества, пока дубликат не уйдёт, затем добавь s[r]. Каждый индекс входит в окно и выходит из него один раз, давая O(n) суммарно.", correct: true },
    { label: "Сначала отсортировать строку, затем посчитать длиннейшую серию.", misconception: "Сортировка разрушает подстрочную (непрерывную) структуру, о которой задача — подстрока обязана быть непрерывным срезом исходного порядка. Скользящее окно с левым указателем сохраняет непрерывность и работает за O(n)." },
    { label: "Пересчитывать множество с нуля для каждого r во вложенном цикле.", misconception: "Это та грубая сила O(n^2), которую окно призвано заменить. Вся ценность окна в том, что левый указатель движется только вперёд, поэтому суммарная работа O(n) — никогда не перезапускай внутренний скан." },
    { label: "Использовать кучу по частоте символов.", misconception: "Куча выдаёт экстремум по приоритету, что не имеет отношения к поддержке непрерывного диапазона без дубликатов. Паттерн, на который указывает 'длиннейшая непрерывная подстрока при условии', — скользящее окно, а не куча." },
  ]}
/>

<Recap lang="ru">
Чтение кода ради паттерна — тот же поток, прогнанный в обратную сторону: вложенный цикл по отсортированным данным должен быть two pointers (O(n)); рекурсия, пересчитывающая одинаковые состояния, кричит о мемоизации или табуляции (DP); текущий k-й элемент задаёт конкретную ориентацию кучи тем, какой элемент нужно дёшево вытеснять; а 'длиннейший непрерывный диапазон при условии' — это скользящее окно, чей левый указатель движется только вперёд. Назови паттерн, сверь структуру данных с доминирующей операцией, затем измени то единственное, что сдвигает сложность.
</Recap>
