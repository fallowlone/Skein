---
concepts:
- technique-selection
- pattern-recognition
- complexity-tradeoff
- data-structure-choice
- problem-decomposition
- constraint-reading
estMin: 14
lang: ru
level: middle
order: 7
prereqs: []
slug: quiz-choice
sources:
- https://en.wikipedia.org/wiki/Dynamic_programming
- https://en.wikipedia.org/wiki/Greedy_algorithm
- https://en.wikipedia.org/wiki/Time_complexity
status: ready
summary: "Кросс-трековый тест: прочти задачу и выбери технику и причину, взвешивая сложность, структуру данных и режим отказа неверного инструмента."
title: 'Инструментарий: выбор техники'
track: algorithms
unit: 12-toolbox
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Весь трек сводится к одному навыку: прочесть задачу и назвать верный инструмент до того, как написана первая строка. Каждый вопрос даёт формулировку задачи и спрашивает не 'каков ответ', а 'какая техника и почему именно она, а не соблазнительный неверный вариант'.
</Hook>

<Goal lang="ru">
Убедись, что умеешь отображать форму задачи — её ограничения, структуру, то, что она просит — на подходящую технику и объяснять, почему очевидная альтернатива не работает или тратит время.
</Goal>

<Quiz
  id="12-toolbox-choice-1"
  lessonSlug="12-toolbox"
  lang="ru"
  question="Дан неотсортированный массив из 10^6 целых чисел; нужно ответить на 10^5 запросов вида 'существует ли значение X?'. Какой подход и почему?"
  choices={[
    { label: "Построить hash set один раз (O(n)), затем отвечать на каждый запрос за O(1) в среднем. Итого ~O(n + q). Меняешь O(n) памяти на проверку принадлежности за константу.", correct: true },
    { label: "Бинарный поиск на запрос после однократной сортировки: сортировка O(n log n), затем O(log n) на запрос.", misconception: "Это работает и годится как запасной вариант, но для чистой проверки принадлежности hash set отвечает за O(1) в среднем против O(log n), и ты избегаешь сортировки за n log n. Бинарный поиск выигрывает, только если нужен ещё порядок или диапазонные запросы." },
    { label: "Линейный проход на каждый запрос — просто и без препроцессинга.", misconception: "O(n) на запрос умножить на 10^5 запросов даёт 10^11 операций. Весь смысл трека — потратить O(n) препроцессинга один раз, чтобы каждый запрос стал дешёвым. Это та грубая сила, ради победы над которой существуют остальные инструменты." },
    { label: "Отсортировать, удалить дубликаты, затем линейный проход на запрос.", misconception: "Сортировка не помогает запросу принадлежности, если потом не делать бинарный поиск; линейный проход после сортировки всё равно O(n) на запрос. Ты заплатил за сортировку и ничего не получил." },
  ]}
/>

<Quiz
  id="12-toolbox-choice-2"
  lessonSlug="12-toolbox"
  lang="ru"
  question="Задача просит число различных способов набрать сумму N заданными номиналами монет. N до 10^4. Какая техника и в чём признак?"
  choices={[
    { label: "Динамическое программирование. Признак: ответ строится из перекрывающихся подзадач (способы для N переиспользуют способы для N минус монета), и ты считаешь по пространству с оптимальной подструктурой. Табулируй по суммам 0..N.", correct: true },
    { label: "Жадность: на каждом шаге брать наибольшую подходящую монету.", misconception: "Жадность даёт корректное решение для канонических систем монет, но НЕ считает все способы, а для произвольных номиналов может даже промахнуться мимо минимума. Подсчёт различных комбинаций требует суммирования по подзадачам — это DP, а не жадность." },
    { label: "Backtracking: перебрать все комбинации и посчитать.", misconception: "Backtracking обходит всё дерево комбинаций — экспонента от N. Подзадачи сильно перекрываются (одна и та же оставшаяся сумма встречается в разных ветвях), а это и есть сигнал к мемоизации или табуляции. Чистый backtracking упирается в таймаут." },
    { label: "Бинарный поиск по ответу.", misconception: "Бинарный поиск по ответу требует монотонного предиката допустимости на числовом диапазоне. Число способов набрать сумму — задача подсчёта без такой монотонной структуры для деления пополам." },
  ]}
/>

<Quiz
  id="12-toolbox-choice-3"
  lessonSlug="12-toolbox"
  lang="ru"
  question="Нужно найти k наименьших элементов потока из n чисел (n заранее неизвестно, k фиксировано и мало). Какая структура данных несёт технику?"
  choices={[
    { label: "Max-heap размера k: вставляй каждый элемент, и когда куча превышает k — извлекай максимум. O(n log k) времени, O(k) памяти — корень всегда текущий k-й наименьший.", correct: true },
    { label: "Отсортировать весь поток и взять первые k.", misconception: "Поток неизвестной длины нельзя отсортировать без буферизации всех n элементов, что стоит O(n) памяти и O(n log n) времени. Куча держит только k элементов и никогда не видит все n сразу." },
    { label: "Min-heap размера k.", misconception: "Min-heap размера k делает дешёвым извлечение наименьшего, но чтобы СОХРАНЯТЬ k наименьших, нужно дёшево вытеснять наибольший из текущих k — это максимум в корне max-heap. Min-heap заставляет сканировать за O(k), чтобы найти элемент на выброс." },
    { label: "Hash set размером k.", misconception: "Hash set даёт принадлежность, а не порядок. Он не подскажет, какой элемент вытеснить, чтобы сохранить k наименьших. Упорядочивание по приоритету — работа кучи." },
  ]}
/>

<Quiz
  id="12-toolbox-choice-4"
  lessonSlug="12-toolbox"
  lang="ru"
  question="Дан отсортированный массив; найди два индекса, значения которых дают в сумме target. Массив может быть огромным, а доп. памяти разрешено O(1). Какая техника?"
  choices={[
    { label: "Two pointers с двух концов: двигай левый указатель внутрь, если сумма мала, правый внутрь, если велика. O(n) времени, O(1) памяти — отсортированность позволяет каждый шаг доказуемо отбрасывать одного кандидата.", correct: true },
    { label: "Hash map дополнений, увиденных ранее.", misconception: "Two-sum через hash map — верный инструмент для НЕотсортированного массива, но стоит O(n) доп. памяти. Здесь массив уже отсортирован и память ограничена — two pointers использует порядок для O(1) памяти. Чтение ограничений выбирает инструмент." },
    { label: "Бинарный поиск target минус a[i] для каждого i.", misconception: "O(n log n) и игнорирует структуру, которую использует проход two pointers. Это работает, но строго медленнее, чем O(n)-проход two pointers, а вопрос просит использовать отсортированность полностью." },
    { label: "Backtracking по всем парам.", misconception: "Перебор всех пар — O(n^2) и полностью выбрасывает отсортированный порядок. Отсортированный вход — самая громкая подсказка для two pointers или бинарного поиска, но никак не для перебора пар грубой силой." },
  ]}
/>

<Quiz
  id="12-toolbox-choice-5"
  lessonSlug="12-toolbox"
  lang="ru"
  question="Нужно определить, есть ли цикл в ориентированном графе зависимостей, и если нет — выдать корректный порядок сборки. Какая техника?"
  choices={[
    { label: "Топологическая сортировка (BFS Кана по входящим степеням или DFS со стеком рекурсии). Успешная топосортировка даёт порядок сборки; если она не может поглотить все вершины — есть цикл. O(V + E).", correct: true },
    { label: "Кратчайший путь Дейкстры из корня.", misconception: "Дейкстра находит кратчайшие взвешенные пути; он не определяет цикл в смысле зависимостей и не выдаёт порядок, уважающий все ограничения предшествования. Упорядочивание по зависимостям — это топологическая сортировка, а не кратчайший путь." },
    { label: "Union-Find для поиска цикла.", misconception: "Union-Find находит циклы в НЕориентированных графах. Графы зависимостей ориентированы, где о цикле сигнализирует обратное ребро, а не общая компонента, и тебе ещё нужен порядок. Топосортировка даёт и то, и другое." },
    { label: "Динамическое программирование по подмножествам вершин.", misconception: "DP по подмножествам — для задач вроде гамильтоновых путей или TSP на малых графах (экспонента от V). Упорядочивание по предшествованию — это линейная топосортировка; DP здесь дико избыточно." },
  ]}
/>

<Quiz
  id="12-toolbox-choice-6"
  lessonSlug="12-toolbox"
  lang="ru"
  question="Задача даёт n <= 20 предметов и просит оптимальное подмножество при ограничении, которое сопротивляется любой жадной или value-DP формулировке. Ограничения кричат об определённом семействе. О каком, и в чём признак n <= 20?"
  choices={[
    { label: "Экспоненциальный поиск — backtracking или перебор битмасок по всем 2^n подмножествам, возможно с bitmask DP при перекрытии подзадач. n <= 20 значит 2^20, то есть около 10^6 подмножеств, что выполнимо; малое n — явное разрешение перебирать пространство подмножеств грубой силой.", correct: true },
    { label: "Жадный sort-and-pick подойдёт при таком малом n.", misconception: "Малое n не делает жадность корректной — корректность про структуру (аргумент обмена или матроид), а не про размер. Признак n <= 20 — это разрешение перебирать экспоненциально, именно потому что полиномиальные жадность или DP не подходят." },
    { label: "Полиномиальное DP по диапазону значений.", misconception: "Value-DP нужен ограниченный числовой измеритель для табуляции; задача сообщает, что сопротивляется такой формулировке. Когда естественное состояние ЕСТЬ подмножество, а n крошечно, перебирают подмножества (2^n), а не значения." },
    { label: "Это должно решаться за полином — n <= 20 не важно.", misconception: "Размеры ограничений — главный сигнал выбора техники. n <= 20 (около 10^6 подмножеств) против n <= 10^5 (нужно O(n log n)) против n <= 10^9 (нужно O(log n) или математика) указывают на разные семейства. Игнорировать n значит отбросить самую громкую подсказку." },
  ]}
/>

<Recap lang="ru">
Выбор техники — это чтение, а не зубрёжка. Размеры ограничений — самый громкий сигнал: n ≤ 20 разрешает перебор 2^n; n ≤ 10^5 требует O(n log n); 10^9 вынуждает O(log n) или математику. Глагол задачи называет семейство — 'существует ли' ведёт к хешированию; 'k наименьших или следующий по приоритету' к куче; 'сколько способов или оптимизация по перекрывающимся подзадачам' к DP; 'лучший локальный выбор, который можно доказать' к жадности; 'упорядочить по зависимости' к топосортировке; 'пара в отсортированных данных при O(1) памяти' к two pointers. Каждый неверный ответ выше — это реальный инструмент, применённый не туда; навык в том, чтобы сопоставить форму инструменту, а затем понимать, почему соблазнительная альтернатива тратит время или ломается.
</Recap>
