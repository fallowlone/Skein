---
concepts:
- hash-table-implementation
- chaining
- load-factor
- amortized-resize
- worst-case-degradation
estMin: 220
lang: ru
level: middle
order: 10
prereqs: []
slug: project
sources:
- https://en.wikipedia.org/wiki/Hash_table
- https://www.cs.cornell.edu/courses/cs3110/2008fa/lectures/lec22_amort.html
status: ready
summary: "Практический проект: с нуля постройте hash map на chaining с resize по load factor, измерьте среднее и худшее поведение и докажите стоимость O(1) amortized."
title: 'Хеширование: соберите hash map с resize'
track: algorithms
unit: 05-hashing
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import ProjectBrief from "~/components/pedagogy/ProjectBrief.astro";

<Hook>
Читать про chaining, load factor и amortized resize — не то же самое, что заставить их работать. Постройте hash map от массива вверх — без встроенного Map под капотом, — затем загоните её в худший случай и посмотрите, как теория проступает в числах.
</Hook>

<Goal lang="ru">
Превратите модель юнита в рабочий код и измеренные доказательства: реализуйте set/get/delete поверх массива цепочек, делайте resize по порогу load factor и докажите экспериментами, что поиски — O(1) в среднем, что resize — O(1) amortized, и что плохой хеш обрушивает её до O(n).
</Goal>

<ProjectBrief
  id="05-hashing-project"
  lessonSlug="05-hashing"
  lang="ru"
  objective="Реализуйте hash map с нуля поверх сырого массива, используя separate chaining и resize по порогу load factor, затем спроектируйте эксперименты, демонстрирующие её среднее, amortized и худшее поведение реальными измерениями — а не голословно."
  requirements={[
    "Реализуйте класс HashMap поверх обычного массива бакетов (каждый бакет — список). Предоставьте set(key, value), get(key), has(key), delete(key) и счётчик size. НЕ делегируйте встроенному Map или объекту как хранилищу.",
    "Напишите собственную hash function для строк (например полиномиальный rolling hash: h = h * 31 + charCode, по модулю длины массива бакетов). Вычисляйте индекс как hash(key) % length.",
    "Обрабатывайте collision через separate chaining: на set просканируйте бакет и обновите существующий ключ на месте, иначе добавьте в конец. На get/has/delete сканируйте цепочку и сопоставляйте по полному ключу, а не только по индексу.",
    "Отслеживайте load factor (элементы / бакеты). Когда он на вставке превышает 0.75, выделите новый массив вдвое большей длины и перехешируйте в него каждую запись. Подтвердите, что get по-прежнему находит каждый ключ после resize.",
    "Инструментируйте map: считайте вызовы хеша, сравнения ключей на операцию, общее число resize и текущий load factor. Откройте их так, чтобы эксперименты могли их читать.",
    "Эксперимент A (средний случай): вставьте N случайных строковых ключей для N от 1k до 100k, затем замерьте M случайных поисков. Покажите, что среднее число сравнений на поиск держится у малой константы (отслеживая load factor) независимо от N.",
    "Эксперимент B (amortized resize): вставляйте N ключей по одному и записывайте работу на вставку (сравнения + стоимость любого rehash). Постройте график или таблицу, показывающую редкие пики O(n) в точках resize, при том что текущее среднее на вставку остаётся плоским — O(1) amortized.",
    "Эксперимент C (худший случай): подставьте намеренно плохую hash function (например, возвращающую 0 для каждого ключа) и перезапустите бенчмарк поиска. Покажите, что число сравнений на поиск растёт линейно с N — деградация O(n).",
  ]}
  acceptance={[
    "Набор тестов корректности: round-trip set/get/has/delete, перезапись существующего ключа возвращает новое значение (а не дубликат), удаление ключа убирает только эту запись из его цепочки, и все ключи переживают resize.",
    "Таблица результатов для эксперимента A, показывающая среднее число сравнений на поиск примерно плоским по N от 1k до 100k, с измеренным load factor рядом.",
    "Таблица или график для эксперимента B, показывающий пики стоимости на вставку в точках resize, при том что накопленное среднее на вставку остаётся ограничено малой константой.",
    "Таблица результатов для эксперимента C с плохим хешем, показывающая рост сравнений на поиск примерно линейно с N, плюс абзац-объяснение, связывающий это с длиной цепочки = load factor и формулой O(1 + alpha).",
  ]}
  stretch={[
    "Добавьте автоматический shrink-on-delete: когда load factor падает ниже 0.25, уменьшайте массив вдвое и перехешируйте. Покажите, что это держит память ограниченной при delete-heavy нагрузке, не ломая поиски.",
    "Добавьте рандомизированный seed в hash function (подмешайте случайное значение на экземпляр в h) и продемонстрируйте, что два экземпляра кладут одни и те же ключи в разные бакеты — защита от атакующего, подбирающего коллидирующие входы.",
    "Реализуйте вариант с open addressing (линейное пробирование) с тем же интерфейсом, затем сравните его сравнения на поиск и память с chained-версией при load factor 0.5, 0.75 и 0.9.",
    "Используйте свою HashMap (не встроенную), чтобы решить two-sum и group-anagrams, и подтвердите по своей инструментации, что оба работают за один проход O(n) по входу.",
  ]}
/>

<Recap lang="ru">
Это цикл за каждой hash map, которой вы когда-либо воспользуетесь: массив плюс hash function дают доступ O(1) в среднем, chaining поглощает неизбежные collision, load factor управляет длиной цепочки, а resize держит её ограниченной за O(1) amortized. Построив её один раз и измерив среднее, amortized и худшее поведение, вы превращаете «hash map — это O(1)» из лозунга в то, что можно доказать — и точно знать, когда это перестаёт быть правдой.
</Recap>
