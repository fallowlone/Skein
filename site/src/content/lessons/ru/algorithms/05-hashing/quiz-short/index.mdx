---
concepts:
- hash-function
- collision
- load-factor
- amortized-resize
- seen-pattern
- grouping
estMin: 13
lang: ru
level: middle
order: 8
prereqs: []
slug: quiz-short
sources:
- https://en.wikipedia.org/wiki/Hash_table
- https://www.cs.cornell.edu/courses/cs3110/2008fa/lectures/lec22_amort.html
status: ready
summary: "Вопросы на свободное припоминание по всему юниту хеширования. Сначала ответьте своими словами, затем откройте образцовый ответ и сравните."
title: 'Хеширование: тест на припоминание'
track: algorithms
unit: 05-hashing
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";

<Hook>
Припоминание сильнее перечитывания. Для каждого вопроса проговорите или запишите полный ответ по памяти, прежде чем открывать образец — именно усилие припоминания закрепляет хеширование, когда оно нужно на собеседовании или в два часа ночи.
</Hook>

<Goal lang="ru">
Восстановите ключевые механизмы юнита — что даёт hash function, как разрешаются collision, почему load factor управляет стоимостью, почему resize — это O(1) amortized, и какие паттерны построены сверху — не заглядывая в уроки.
</Goal>

<RetrievalDrawer
  client:visible
  id="05-hashing-short-retrieval"
  lang="ru"
  questions={[
    { q: "Что на самом деле даёт hash function и какова цена этого O(1) поиска в среднем?", a: "Hash function превращает ключ в индекс массива за константное время, поэтому вы прыгаете прямо к слоту, а не сканируете. Это даёт O(1) в среднем на поиск, вставку и удаление. Цена двойная: память (вы выделяете массив бакетов с запасом, чтобы collision были редкими) и неупорядоченный обход (у hash map нет внутреннего порядка ключей — то, что Map в JavaScript хранит порядок вставки, это отдельное удобство). Вы меняете память и порядок на скорость. «В среднем» условно: оно держится лишь пока ключи распределены примерно равномерно и load factor ограничен." },
    { q: "Что такое collision и как separate chaining её разрешает?", a: "Collision — это когда два разных ключа хешируются в один индекс бакета; неизбежно, как только ключей больше, чем бакетов, по принципу Дирихле. Separate chaining делает каждый бакет маленьким списком. На вставке вы добавляете в конец (или обновляете на месте, если ключ уже есть) список этого бакета; на поиске хешируете в бакет, затем сканируете список, сравнивая полный ключ, ведь индекс сам по себе не различает коллидирующие ключи. Стоимость поиска — O(1 + длина цепочки), а длина цепочки в среднем равна load factor." },
    { q: "Определите load factor и объясните, почему именно он, а не общее число элементов, управляет стоимостью поиска.", a: "Load factor alpha = элементы / бакеты — это среднее число элементов на бакет, что равно средней длине цепочки при chaining. Поиск — O(1 + alpha): один хеш, чтобы найти бакет, затем скан, пропорциональный цепочке. Общее число элементов само по себе ничего не говорит о скорости: миллион элементов в двух миллионах бакетов (alpha = 0.5) быстр, а сотня элементов в двух бакетах (alpha = 50) медленна. Поэтому таблицы делают resize, чтобы держать alpha ограниченным (часто ниже 0.75), а не просто накапливать элементы." },
    { q: "Почему вставка в hash table — это O(1) amortized, хотя одна вставка может быть O(n)?", a: "Когда alpha пересекает порог, таблица выделяет больший массив (обычно вдвое) и перехеширует в него каждый существующий ключ — операция O(n), потому что индекс hash(key) % length меняется при смене length, так что каждый ключ надо переразместить. Но удвоение означает resize лишь O(log n) раз по мере роста таблицы. Суммируя по n вставкам, работа resize — это n + n/2 + n/4 + ... = O(n), что в пересчёте на n вставок даёт O(1) amortized на каждую. Большинство вставок дёшевы; редкую дорогую оплачивают многие дешёвые." },
    { q: "Сравните seen-паттерн и паттерн дополнения и дайте задачу, которую каждый решает за один проход.", a: "Seen-паттерн держит hash set уже встреченных значений; для каждого нового элемента вы проверяете принадлежность за O(1) и затем добавляете — используется для contains-duplicate или поиска первого повторившегося элемента за O(n). Паттерн дополнения держит hash map из значения в индекс увиденного; для каждого x вы ищете нужного партнёра (например target - x для two-sum) до записи x, поэтому элемент не паруется сам с собой. Оба заменяют вложенный цикл O(n в квадрате) одним проходом O(n), меняя память на время." },
    { q: "Когда hash table перестаёт давать O(1) и что с этим делать?", a: "Она деградирует к O(n), когда ключи не распределены равномерно: плохая hash function (например, только по первому символу), патологический вход или атакующий, подбирающий ключи, которые все коллидируют в один бакет — это algorithmic-complexity DoS. Фиксы зависят от причины. Для слабого хеша — хорошо перемешивающая функция (умножение на простое число, перемешивание битов). Для враждебного входа — рандомизированный хеш с seed на процесс, чтобы collision нельзя было предсказать. И всегда держите load factor ограниченным через resize — но resize сам по себе не остановит атакующего, контролирующего входы хеша, ведь они снова коллидируют после resize." },
  ]}
/>

<Recap lang="ru">
Если вы смогли восстановить каждый ответ по памяти, вы держите стержень юнита: hash function даёт доступ O(1) в среднем ценой памяти и порядка; collision неизбежны, и chaining хранит их в списках на бакет; load factor — а не сырое число элементов — задаёт среднюю длину цепочки и тем самым стоимость; resize держит его ограниченным за O(1) amortized; а паттерны seen, дополнение, частотный подсчёт и группировка — это всё та же механика в применении. Гарантия условна, и рандомизированный хеш — то, что защищает её от недоверенного входа.
</Recap>
