---
concepts:
- chaining
- amortized-resize
- complement-pattern
- frequency-map
- hash-map-bug
estMin: 14
lang: ru
level: middle
order: 9
prereqs: []
slug: quiz-code
sources:
- https://en.wikipedia.org/wiki/Hash_table
- https://www.cs.cornell.edu/courses/cs3110/2008fa/lectures/lec22_amort.html
status: ready
summary: "Читаем реальные сниппеты hash table и hash map, предсказываем поведение и выбираем фикс: chaining-поиск, resize удвоением, баги дополнения и частотной map."
title: 'Хеширование: чтение кода'
track: algorithms
unit: 05-hashing
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Баги хеширования прячутся в коде, который выглядит корректным. Прочитайте каждый сниппет, предскажите, что он на самом деле делает под нагрузкой или на крайних случаях, и выберите фикс, который аккуратный инженер сделает первым.
</Hook>

<Goal lang="ru">
Отработайте цикл, который вы прогоняете на каждой задаче с hash map: прочитать код, протрассировать на неудобном входе и взять точный фикс — а не отмахнуться расплывчатым «используй hash map».
</Goal>

## Сниппет 1 — вставка и поиск с chaining

```js
class HashTable {
  constructor(size = 8) {
    this.buckets = Array.from({ length: size }, () => []);
  }
  hash(key) {
    let h = 0;
    for (const ch of key) h = (h * 31 + ch.charCodeAt(0)) % this.buckets.length;
    return h;
  }
  set(key, value) {
    const b = this.buckets[this.hash(key)];
    b.push([key, value]);          // всегда push
  }
  get(key) {
    const b = this.buckets[this.hash(key)];
    for (const [k, v] of b) if (k === key) return v;
    return undefined;
  }
}
```

<Quiz
  id="05-hashing-code-1"
  lessonSlug="05-hashing"
  lang="ru"
  question="Вы вызываете set('a', 1), затем set('a', 2), затем get('a'). Что вернётся и в чём баг?"
  choices={[
    { label: "Вернётся 1. set всегда делает push, поэтому бакет хранит [['a',1],['a',2]], и get возвращает первое совпадение. Фикс: в set просканировать бакет и обновить существующий ключ на месте перед добавлением новой пары.", correct: true },
    { label: "Вернётся 2, потому что побеждает более поздний push.", misconception: "get возвращает ПЕРВЫЙ совпавший ключ, который сканирует, а это устаревшая пара ['a',1]. Более новая пара стоит за ней в цепочке и не достигается." },
    { label: "Вернётся undefined, потому что две записи с одним ключом гасят друг друга.", misconception: "Ничего не гасится — обе пары хранятся в цепочке. get находит первую и возвращает 1; баг с дублирующимся ключом тихий." },
    { label: "Будет исключение, потому что дублирующийся ключ запрещён.", misconception: "Эта реализация вообще не проверяет дубликаты ключей, поэтому исключения быть не может. Она молча хранит обе и возвращает устаревшее значение — что и есть баг." },
  ]}
/>

## Сниппет 2 — resize по load factor

```js
set(key, value) {
  if ((this.count + 1) / this.buckets.length > 0.75) this.resize();
  const b = this.buckets[this.hash(key)];
  b.push([key, value]);
  this.count++;
}
resize() {
  const old = this.buckets;
  this.buckets = Array.from({ length: old.length * 2 }, () => []);
  for (const bucket of old)
    for (const [k, v] of bucket) {
      const i = this.hash(k);       // перехешируем по НОВОЙ длине
      this.buckets[i].push([k, v]);
    }
}
```

<Quiz
  id="05-hashing-code-2"
  lessonSlug="05-hashing"
  lang="ru"
  question="Почему resize обязан перехешировать каждый ключ по новой длине массива, а не копировать бакеты напрямую?"
  choices={[
    { label: "Индекс — это hash(key) % length, поэтому удвоение length меняет, куда относится большинство ключей. Копирование бакета i в новый бакет i разбросало бы поиски: get перехешировал бы по новой длине и искал в бакете, куда ключ не был скопирован. Перехеширование кладёт каждый ключ туда, где его будут искать.", correct: true },
    { label: "Копирование подходит; перехеширование — лишь оптимизация для балансировки цепочек.", misconception: "Это корректность, а не оптимизация. После смены длины hash(key) даёт другие индексы; ключ, скопированный в старый слот, был бы недостижим для get, который использует новую длину." },
    { label: "Перехеширование нужно, потому что удвоение меняет саму hash function.", misconception: "Hash function не меняется — меняется только модуль (длина массива). Одна эта смена модуля перемещает ключи, поэтому каждый ключ надо переразместить." },
    { label: "Перехешируют, чтобы полностью устранить collision после resize.", misconception: "Resize понижает load factor и укорачивает цепочки, но collision всё равно случаются. Перехеширование — про то, чтобы класть ключи в бакет по новому модулю, а не про устранение collision." },
  ]}
/>

## Сниппет 3 — two-sum через паттерн дополнения

```js
function twoSum(nums, target) {
  const seen = new Map();           // значение -> индекс
  for (let i = 0; i < nums.length; i++) {
    const need = target - nums[i];
    if (seen.has(need)) return [seen.get(need), i];
    seen.set(nums[i], i);           // записываем ПОСЛЕ проверки
  }
  return null;
}
```

<Quiz
  id="05-hashing-code-3"
  lessonSlug="05-hashing"
  lang="ru"
  question="Почему проверка need идёт ДО seen.set(nums[i], i), и что сломается, если поменять две строки местами?"
  choices={[
    { label: "Запись после проверки гарантирует, что текущий элемент паруется только с БОЛЕЕ РАННИМ. Если сначала set, то при target 6 и nums[i] = 3 элемент совпал бы сам с собой (need 3 уже в map), вернув [i, i] — ложную пару из одного элемента, использованного дважды.", correct: true },
    { label: "Порядок не важен; map в итоге одинакова в обоих случаях.", misconception: "Итоговое содержимое map одинаково, но пошаговый поиск — нет. Set до проверки даёт элементу найти САМОГО СЕБЯ как своё дополнение, выдавая невалидный ответ [i, i] для случаев вроде 3 + 3 = 6 при единственной тройке." },
    { label: "Сначала делать set быстрее, потому что избегается лишний has-вызов.", misconception: "Дело не в скорости, а в корректности: set сначала допускает self-pairing. Has-вызов всё равно нужен; перестановка меняет ответ, а не объём работы." },
    { label: "Перестановка норм, пока вы также проверяете i !== seen.get(need).", misconception: "Эта дополнительная защита залатала бы баг, но чистый идиоматичный фикс — просто записывать после проверки. Сниппет уже корректен; перестановка вносит баг, который защите потом пришлось бы отменять." },
  ]}
/>

## Сниппет 4 — группировка анаграмм по частотному ключу

```js
function groupAnagrams(words) {
  const groups = new Map();
  for (const w of words) {
    const key = w.split('').sort().join('');   // канонический ключ
    if (!groups.has(key)) groups.set(key, []);
    groups.get(key).push(w);
  }
  return [...groups.values()];
}
```

<Quiz
  id="05-hashing-code-4"
  lessonSlug="05-hashing"
  lang="ru"
  question="Это корректно группирует анаграммы. Для длинных строк сколько стоит ключ, и какой более дешёвый канонический ключ даёт тот же результат группировки?"
  choices={[
    { label: "Сортировка каждого слова длины L стоит O(L log L). Сигнатура по счётчику символов — массив на 26 ячеек, превращённый в строку, — это O(L) и даёт идентичную группировку, ведь анаграммы имеют одинаковые счётчики букв. Меняем сортировку на ключ-счётчик на горячем пути.", correct: true },
    { label: "Сортировка — это O(L), поэтому более дешёвого ключа нет.", misconception: "Сортировка — O(L log L), а не O(L). Линейная сигнатура по счётчику символов асимптотически дешевле и группирует анаграммы идентично, потому что у анаграмм равные частоты букв." },
    { label: "Следует использовать само слово как ключ, чтобы избежать пересчёта.", misconception: "Сырое слово группирует одинаковые строки, а не анаграммы — 'eat' и 'tea' попали бы в разные группы. Ключ должен быть инвариантен по анаграммам, что даёт отсортированная форма (или сигнатура-счётчик)." },
    { label: "Хеширование слова более сильной hash function сводит анаграммы в одну группу.", misconception: "Опираться на collision хеша для группировки анаграмм неверно и небезопасно — не-анаграммы тоже коллидировали бы. Нужен детерминированный канонический ключ (сортировка или счётчик), а не поведение collision." },
  ]}
/>

<Recap lang="ru">
Каждая задача с hash map читается в коде: set с chaining должен обновлять существующий ключ на месте, а не слепо делать push; resize должен перехешировать по новому модулю, иначе поиски уходят не в тот бакет; паттерн дополнения записывает после проверки, чтобы элемент не паровался сам с собой; а группировке нужен канонический ключ, инвариантный по группе — счётчик символов бьёт сортировку на горячем пути. Сначала прочитайте неудобный вход, потом выберите точный фикс.
</Recap>
