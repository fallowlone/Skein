---
concepts:
- hash-function
- collision-resolution
- load-factor
- amortized-resize
- seen-pattern
- worst-case-degradation
estMin: 13
lang: ru
level: middle
order: 7
prereqs: []
slug: quiz-choice
sources:
- https://en.wikipedia.org/wiki/Hash_table
- https://www.cs.cornell.edu/courses/cs3110/2008fa/lectures/lec22_amort.html
status: ready
summary: "Синтез по всему юниту в формате выбора: hash function, collision, chaining, load factor, amortized resize и паттерны поверх hash map и hash set."
title: 'Хеширование: тест с выбором ответа'
track: algorithms
unit: 05-hashing
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Шесть вопросов, которые проходят через весь юнит. Каждый — это решение, которое вы принимаете, когда тянетесь к hash map на собеседовании или в реальном коде: не определение для пересказа, а tradeoff, который нужно защитить.
</Hook>

<Goal lang="ru">
Убедитесь, что вы связываете hash function, обработку collision, load factor и amortized resize с повседневными паттернами — seen, частотный подсчёт, группировка, — к которым вели отдельные уроки.
</Goal>

<Quiz
  id="05-hashing-choice-1"
  lessonSlug="05-hashing"
  lang="ru"
  question="Hash set даёт O(1) поиск в среднем. На каком единственном факте на самом деле держится это «в среднем»?"
  choices={[
    { label: "Hash function распределяет ключи примерно равномерно по бакетам, поэтому load factor остаётся малым, а цепочки — короткими. Средний поиск — O(1 + alpha), и это O(1) лишь пока alpha — малая константа.", correct: true },
    { label: "Set хранит элементы в отсортированном виде, поэтому поиск — это бинарный поиск, округлённый до O(1).", misconception: "Hash set ничего не хранит в отсортированном виде, а обход неупорядочен. O(1) берётся из прямого вычисления индекса, а не из поиска по отсортированным данным." },
    { label: "Хеширование O(1) по определению, поэтому никаких условий выполнять не нужно.", misconception: "O(1) — это СРЕДНЕЕ при равномерном распределении и ограниченном load factor. При плохой hash function или враждебных ключах оно деградирует до O(n): среднее — это утверждение о типичных входах, а не гарантия." },
    { label: "JavaScript оптимизирует Set внутри, поэтому стоимость зависит только от движка, а не от ключей.", misconception: "Движок помогает с константами, но асимптотическая гарантия всё равно держится на равномерном хешировании и управляемом load factor. Патологическое распределение ключей деградирует любую реализацию." },
  ]}
/>

<Quiz
  id="05-hashing-choice-2"
  lessonSlug="05-hashing"
  lang="ru"
  question="Два разных ключа хешируются в один и тот же индекс бакета. При separate chaining что происходит и сколько это стоит?"
  choices={[
    { label: "Обе пары ключ-значение лежат в списке этого бакета; поиск хеширует в бакет, затем сканирует короткий список в поисках совпавшего ключа. Стоимость — O(1 + длина цепочки), а длина цепочки в среднем равна load factor.", correct: true },
    { label: "Второй ключ перезаписывает первый, ведь каждый бакет хранит одну запись.", misconception: "Это молча потеряло бы данные. Chaining хранит оба — бакет это список, а поиск сравнивает полный ключ, а не только индекс, поэтому коллидирующие ключи сосуществуют." },
    { label: "Таблица сразу удваивается, чтобы дать второму ключу собственный бакет.", misconception: "Одна collision не вызывает resize. Resize управляется тем, что общий load factor пересекает порог, а не одной коллизией; collision ожидаемы и обрабатываются на месте цепочкой." },
    { label: "Поиск вернёт неверное значение, потому что индекс общий.", misconception: "Индекс общий, но сохранённый ключ — нет. Поиск сканирует цепочку и сопоставляет по реальному ключу, поэтому возвращает правильное значение несмотря на collision." },
  ]}
/>

<Quiz
  id="05-hashing-choice-3"
  lessonSlug="05-hashing"
  lang="ru"
  question="В таблице 10 элементов на 5 бакетов, и она никогда не делает resize. Если продолжать вставлять без resize, что произойдёт со стоимостью поиска и почему?"
  choices={[
    { label: "Load factor alpha = элементы / бакеты растёт, поэтому средняя длина цепочки растёт вместе с ним. Поиск — O(1 + alpha), и он дрейфует к линейному сканированию. Именно поэтому реальные таблицы делают resize, когда alpha пересекает порог.", correct: true },
    { label: "Ничего не меняется; хеширование всегда O(1) независимо от того, насколько полна таблица.", misconception: "O(1) держится лишь пока alpha ограничен. При фиксированных бакетах и растущем числе элементов alpha растёт без предела, цепочки удлиняются — поиск деградирует к O(n)." },
    { label: "Поиск ускоряется, потому что больше элементов означает больше попаданий в кэш.", misconception: "Больше элементов в фиксированных бакетах означает более длинные цепочки и больше сравнений на поиск, а не меньше. Драйвер — load factor, и он растёт." },
    { label: "Ключи начинают коллидировать реже по мере заполнения таблицы.", misconception: "Collision становятся ЧАЩЕ по мере роста alpha — меньше свободных бакетов на элемент. Более полная таблица означает большую тесноту, а не меньшую." },
  ]}
/>

<Quiz
  id="05-hashing-choice-4"
  lessonSlug="05-hashing"
  lang="ru"
  question="Вставку в hash table называют O(1) amortized, но одна вставка может стоить O(n). Как оба утверждения верны одновременно?"
  choices={[
    { label: "Большинство вставок — O(1), но когда alpha пересекает порог, таблица удваивается и перехеширует каждый элемент — одна вставка стоит O(n). Поскольку таблица удваивается, resize случается лишь O(log n) раз, и суммарная работа resize за n вставок — O(n), то есть в среднем O(1) на вставку.", correct: true },
    { label: "Amortized означает лучший случай, а лучший случай — O(1); O(n)-resize просто игнорируется.", misconception: "Amortized — это не лучший случай, а среднее по последовательности, и оно явно ВКЛЮЧАЕТ O(n)-resize. Они учитываются, а затем размазываются по дешёвым вставкам через геометрию удвоения." },
    { label: "Resize на самом деле O(1), потому что копирование на практике быстрое.", misconception: "Копирование и перехеширование n элементов действительно O(n) — каждый элемент переразмещается. Amortization не делает этот шаг дешёвым; она делает его достаточно редким, чтобы среднее оставалось O(1)." },
    { label: "Вставки O(1) amortized только если вы никогда не удаляете.", misconception: "Удаление не ломает amortized-оценку вставки. Аргумент про удвоение-при-росте самодостаточен; amortized-граница про геометрическое расписание resize, а не про удаления." },
  ]}
/>

<Quiz
  id="05-hashing-choice-5"
  lessonSlug="05-hashing"
  lang="ru"
  question="Нужно определить, повторяется ли хоть одно значение в потоке, и отдельно — найти два числа, дающих в сумме target. Какой паттерн хеширования подходит каждому?"
  choices={[
    { label: "Обнаружение дубликата — это seen-паттерн: держим hash set, проверяем принадлежность за O(1) перед добавлением. Two-sum — паттерн дополнения: для каждого x ищем target - x в hash map уже встреченных значений. Оба — один проход O(n).", correct: true },
    { label: "Обоим нужна частотная map, считающая вхождения каждого значения.", misconception: "Частотная map работает, но избыточна. Принадлежности нужна только presence (set), а two-sum нужна map из значения в индекс уже увиденного — ни тому, ни другому не нужны полные счётчики." },
    { label: "Обоим сначала нужна сортировка, затем проход двумя указателями.", misconception: "Сортировка — O(n log n) и разрушает информацию об индексах, нужную для two-sum. Хеш-подходы — это один проход O(n) и сохраняют исходные индексы." },
    { label: "Обнаружению дубликата нужна hash map; two-sum нужен hash set.", misconception: "Это обратная пара: обнаружению по присутствию нужен set, а поиску дополнения нужна map (значение в индекс), чтобы вернуть пару. Голый set не вернёт совпавший индекс для two-sum." },
  ]}
/>

<Quiz
  id="05-hashing-choice-6"
  lessonSlug="05-hashing"
  lang="ru"
  question="Сервис хеширует строки от пользователя в таблицу, а атакующий заливает входами, специально подобранными так, чтобы все коллидировали в один бакет. В чём отказ и в чём настоящий фикс?"
  choices={[
    { label: "Все ключи попадают в одну цепочку, поэтому каждый поиск деградирует к O(n) и эндпоинт застревает — это algorithmic-complexity DoS. Настоящий фикс — рандомизированная, seeded hash function (случайный seed на процесс), чтобы атакующий не мог предсказать коллидирующие входы.", correct: true },
    { label: "Никакого отказа нет; chaining обрабатывает все collision за O(1).", misconception: "Chaining обрабатывает collision корректно, но не дёшево, когда все попадают в один бакет — цепочка становится длиной n, а поиски — O(n). Корректность выживает; производительность рушится." },
    { label: "Просто понизьте порог load factor, чтобы таблица делала resize чаще.", misconception: "Resize перераспределяет той же детерминированной hash function, поэтому враждебные ключи снова коллидируют после resize. Меньший порог не помогает, когда атакующий контролирует входы хеша; нужен непредсказуемый seed." },
    { label: "Перейдите с chaining на open addressing, чтобы устранить атаку.", misconception: "Open addressing меняет то, как хранятся collision, а не то, коллидируют ли ключи атакующего. При предсказуемом хеше они всё равно скучиваются; защита — рандомизированный хеш, независимо от схемы разрешения." },
  ]}
/>

<Recap lang="ru">
Сквозная линия юнита — одна цепочка рассуждений: hash function превращает ключ в индекс для доступа O(1) в среднем; collision неизбежны, и chaining хранит их в списках на бакет; load factor задаёт среднюю длину цепочки, поэтому таблица делает resize — удвоение и перехеширование, — чтобы держать её ограниченной: O(n) на resize, но O(1) amortized; а сверху лежат повседневные паттерны — seen, дополнение, частотный подсчёт, группировка. Та же механика деградирует к O(n) при плохой или враждебной hash function, поэтому рандомизированный хеш важен для недоверенного входа.
</Recap>
