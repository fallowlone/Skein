---
concepts:
- enumeration
- directory-bruteforce
- api-enumeration
- user-enumeration
- rate-limiting
deepensInto: []
estMin: 15
lang: ru
lessonType: topic
level: middle
order: 4
prereqs: []
slug: 04-enumeration-techniques
sources:
- https://owasp.org/www-project-web-security-testing-guide/
- https://attack.mitre.org/tactics/TA0043/
spiral: []
status: ready
summary: Перечисление превращает неизвестную поверхность атаки в конкретный список целей — скрытые эндпоинты, реальные пользователи, недокументированные объекты API. Ровно это и призваны замедлить и выявить лимиты запросов и мониторинг.
title: Техники перечисления
track: security-offensive
unit: 01-recon-and-enumeration
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
Лабораторный стенд отвечает на `POST /api/login` двумя разными сообщениями: «аккаунта с таким email нет» и «неверный пароль». Безобидная формулировка — вот только скрипт теперь может скормить ему список из десяти тысяч email и за несколько минут оставить только те, что вернули «неверный пароль». Он только что подтвердил, у кого из ваших клиентов есть аккаунт, не угадав ни одного пароля. Это и есть перечисление: не взлом, а превращение неизвестной поверхности в точный, ранжированный список того, что стоит атаковать следующим. У формы входа не было бага в привычном смысле. Она просто сказала правду слишком конкретно — а защитник, понимающий перечисление, как раз и замечает, что ответ, тайминг и частота запросов все вместе утекают.
</Hook>

К концу урока вы будете знать, как атакующие перечисляют эндпоинты, пользователей и объекты API в авторизованной лаборатории и как именно лимиты запросов и мониторинг превращают это дешёвое, тихое прощупывание в медленную, дорогую, заметную операцию, которую защитник может поймать.

<Crux>Перечисление превращает неизвестную поверхность в список целей; лимиты запросов и мониторинг делают его медленным, дорогим и заметным.</Crux>

<Explanation>

## Сначала граница: только лаборатория, и перечисление — это разведка

Прежде всего о механиках: всё ниже рассчитано на систему, которой вы владеете или которую вам явно разрешено тестировать, — CTF-стенд, намеренно уязвимое приложение в вашей собственной VM, согласованный по объёму проект. Перечисление обращается к реальным эндпоинтам и реальным аккаунтам, поэтому без письменной авторизации и определённого scope это неавторизованный доступ, точка. Здесь нет вооружённых скриптов; цель — понять *механизм* достаточно глубоко, чтобы обнаруживать и притуплять его на системах, которые вы защищаете.

В последовательности атакующего перечисление — это **разведка**, первая тактика MITRE ATT&CK (TA0043). Само по себе оно не даёт пробоя. Оно даёт карту: какие пути существуют, какие имена пользователей реальны, какие объекты API можно назвать. Каждая последующая стадия дешевле и тише, потому что перечисление сузило поиск с «всего интернета» до «вот этих сорока вещей». Именно поэтому притупление перечисления имеет несоразмерно высокую защитную ценность — вы повышаете стоимость *фундамента*, на котором строится вся остальная атака.

## Перечисление эндпоинтов: найти то, на что нет ссылок

Первая цель — структура. Карта сайта приложения показывает парадную дверь; перечисление ищет двери, на которые никогда не было ссылок, — `/admin`, `/api/internal`, `/.git/config`, `/backup.zip`, старую `/v1`, которую должны были вывести из эксплуатации. Техника — это **перебор директорий и контента**: берётся список частых путей и имён файлов, каждый запрашивается, и по HTTP-статусу и размеру ответа отличается «это существует» от «этого нет».

Сигнал, который читает атакующий, — это *разница* в ответах. Аккуратное приложение возвращает `404` на всё, чего нет. Дырявое возвращает `403 Forbidden` на `/admin` (подтверждая, что путь существует, но защищён), `200` на `/api/internal/users` (существует и не защищён) и редирект `301`, тихо раскрывающий реальный путь. **Размер** ответа важен не меньше статуса: типовая страница 404 имеет фиксированную длину, поэтому 404 с другой длиной часто оказывается реальной страницей в чужом статус-коде. Вся игра — в чтении этих признаков на тысячах запросов.

<FlowDiagram
  label="Перечисление эндпоинтов читает разницу в ответах сервера на список угаданных путей. 404 означает отсутствие, 403 подтверждает, что защищённый путь существует, 200 означает найденный незащищённый путь, а необычный размер ответа выдаёт реальную страницу за неверным статус-кодом; единообразные ответы плюс лимиты запросов лишают атакующего сигнала."
  perRow={5}
  hue="var(--d-crypto)"
  nodes={[
    { id: "wordlist", label: "Словарь", sub: "/admin /backup" },
    { id: "probe", label: "Запрос пути", sub: "по одному" },
    { id: "s404", label: "404 / нет", sub: "нет сигнала" },
    { id: "s403", label: "403 / 200", sub: "путь есть" },
    { id: "size", label: "Странный размер", sub: "скрытая стр." },
  ]}
  edges={[
    { from: "wordlist", to: "probe" },
    { from: "probe", to: "s404", label: "шум" },
    { from: "probe", to: "s403", label: "сигнал" },
    { from: "probe", to: "size", label: "сигнал" },
  ]}
  caption="Перечисление — игра в извлечение сигнала: атакующий выстреливает словарём и оставляет только ответы, отличные от единообразного «не найдено». Единообразные ошибки, отсутствие утечки по размеру и лимит запросов сворачивают сигнал обратно в шум."
/>

## Перечисление пользователей и API: подтвердить, кто и что существует

Вторая цель — личности и объекты. **Перечисление пользователей** — это история формы входа из вступления: любое место, где приложение ведёт себя по-разному для реального и фальшивого аккаунта, — это оракул. Классические признаки — отдельное сообщение об ошибке («email не найден» против «неверный пароль»), другой HTTP-ответ и — самый тонкий — **разница в тайминге**. Если реальный аккаунт запускает дорогое хеширование пароля (bcrypt, argon2), а несуществующий возвращается мгновенно, потому что хешировать нечего, то одно лишь время ответа отделяет реальное от фальшивого даже при одинаковых сообщениях. Потоки регистрации и сброса пароля утекают так же («этот email уже зарегистрирован»).

**Перечисление API** обобщает это на объекты. REST API по адресу `GET /api/orders/1001` напрашивается на вопрос: а что насчёт `1002`, `1003`, `1000000`? Последовательные целочисленные ID позволяют атакующему пройти всю коллекцию простым счётом. Даже когда контроль доступа корректен и каждый запрос отклоняется, *шаблон отказов* размечает, какие ID существуют (`403` на реальный, но запрещённый заказ, `404` на пробел), а подробные тела ошибок, утёкший документ OpenAPI/Swagger или introspection-запрос GraphQL выдают модель объектов целиком.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Цель</th>
        <th class="border border-border px-3 py-2 text-left">Утечка (оракул)</th>
        <th class="border border-border px-3 py-2 text-left">Что узнаёт атакующий</th>
        <th class="border border-border px-3 py-2 text-left">Защитный фикс</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">Эндпоинты</td>
        <td class="border border-border px-3 py-2">Статус/размер различаются (404 vs 403 vs 200)</td>
        <td class="border border-border px-3 py-2">Какие скрытые пути существуют</td>
        <td class="border border-border px-3 py-2">Единый 404; deny by default; авторизация до маршрутизации</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Пользователи (вход)</td>
        <td class="border border-border px-3 py-2">Отдельное сообщение об ошибке или ответ</td>
        <td class="border border-border px-3 py-2">У каких email есть аккаунты</td>
        <td class="border border-border px-3 py-2">Одинаковое общее сообщение для обоих случаев</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Пользователи (тайминг)</td>
        <td class="border border-border px-3 py-2">Реальный аккаунт хеширует, фальшивый отвечает мгновенно</td>
        <td class="border border-border px-3 py-2">Реальный vs фальшивый по времени ответа</td>
        <td class="border border-border px-3 py-2">Хешировать фиктивный пароль и на ветке промаха</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Объекты API</td>
        <td class="border border-border px-3 py-2">Последовательные ID; раскол 403 vs 404</td>
        <td class="border border-border px-3 py-2">Какие записи существуют и сколько их</td>
        <td class="border border-border px-3 py-2">Случайные UUID; одинаковый статус для запрета и отсутствия</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Форма API</td>
        <td class="border border-border px-3 py-2">Подробные ошибки, Swagger, introspection GraphQL</td>
        <td class="border border-border px-3 py-2">Полную модель объектов</td>
        <td class="border border-border px-3 py-2">Общие ошибки; закрыть доки/introspection в проде</td>
      </tr>
    </tbody>
  </table>
</div>

<Inset kind="why" lang="ru">
Почему возврат одной и той же общей ошибки для «пользователь не найден» и «неверный пароль» реально помогает, если реальный атакующий всё равно может перебирать пароли? Потому что это меняет *экономику*. Без оракула атакующий вынужден разбрасывать учётные данные по неизвестному множеству аккаунтов — большинство попыток тратится на несуществующие email, и каждая впустую — это лишний шум для вашего лимитера и ваших алертов. С оракулом он *сначала* дёшево перечисляет валидные аккаунты, а затем нацеливает сфокусированную, более медленную атаку по паролям только на реальные цели. Закрытие оракула не останавливает атаку целиком; оно убирает дешёвый разведывательный шаг, который делал дорогой шаг эффективным, — а именно в этом и смысл срыва перечисления.
</Inset>

## Что притупляет это: лимиты запросов и мониторинг

Определяющая слабость перечисления — **объём**. Подтверждение скрытых путей, валидных пользователей или объектов API требует от сотен до миллионов запросов, потому что атакующий ищет в пространстве, а не эксплуатирует одну известную брешь. Вот рычаг, за который тянут защитники.

**Лимиты запросов** бьют прямо по стоимости. Ограничьте запросы на IP, на аккаунт и на эндпоинт — и перебор, который должен был занять минуты, теперь занимает дни, достаточно долго, чтобы окно проекта закрылось или атакующий ушёл. Зрелый нюанс — *где* ограничивать: лимиты на IP тривиально обходятся ботнетом или ротацией прокси, поэтому высокоценные оракулы (вход, сброс пароля, эндпоинты токенов) нуждаются ещё и в лимитах на аккаунт и в глобальных потолках, плюс экспоненциальный backoff и CAPTCHA или proof-of-work на подозрительном пути. Лимиты редко делают перечисление невозможным; они делают его достаточно медленным, чтобы оно стало непрактичным, и достаточно громким, чтобы его заметили.

**Мониторинг** бьёт по скрытности. У перечисления есть отпечаток, которого нет у нормального трафика: один источник, генерирующий поток `404`, прогон по последовательным ID, сотни неудачных входов по множеству имён пользователей или частота запросов, которую не выдаёт человек. Алертите на эти паттерны — всплески доли `4xx`, доступ к путям или ID высокой кардинальности от одного клиента, всплески неудачной аутентификации — и вы обнаружите стадию разведки *до* того, как она перерастёт в эксплойт. Это выгода kill chain в миниатюре: перечисление — самая дешёвая, ранняя стадия атакующего, поэтому поймать её здесь — самое дешёвое, раннее место, чтобы разорвать цепочку.

<TradeoffMatrix
  id="04-enumeration-techniques-tradeoff-1"
  lessonSlug="04-enumeration-techniques"
  lang="ru"
  prompt="API входа возвращает «аккаунта с таким email нет» для неизвестных email и «неверный пароль» для реальных. Вы укрепляете его против перечисления пользователей. Выберите лучший фикс."
  options={[
    { name: "Возвращать одно одинаковое общее сообщение («неверный email или пароль») для обоих случаев, И выровнять тайминг, хешируя фиктивный пароль на ветке «такого пользователя нет», И ограничить эндпоинт по IP и по аккаунту", summary: "Закрыть все оракулы.", correct: true, justification: "Перечисление пользователей утекает по нескольким каналам, поэтому полный фикс закрывает их все. Одинаковые сообщения убирают текстовый оракул; хеширование фиктивного пароля на ветке промаха убирает оракул по таймингу (иначе реальный аккаунт медленный, а фальшивый мгновенный, и они различимы даже при одинаковом тексте); а лимиты запросов повышают стоимость высокообъёмного прощупывания, к которому атакующий откатывается. Вместе они превращают дешёвый, надёжный список пользователей в медленную, шумную, малосигнальную атаку." },
    { name: "Просто поменять оба ответа на одинаковое общее сообщение «неверный email или пароль»", summary: "Только формулировка.", justification: "Необходимо, но недостаточно. Одинаковая формулировка закрывает оракул сообщения, но если реальный аккаунт запускает дорогое хеширование пароля, а несуществующий отвечает мгновенно, разница во времени ответа остаётся надёжным оракулом. Атакующие измеряют тайминг точно; один текстовый фикс оставляет этот побочный канал нараспашку." },
    { name: "Сменить пользовательские ID с последовательных целых на случайные UUID", summary: "Скрыть ID.", justification: "Это адресует перечисление объектов/ID, а не пользователей при входе — оно не трогает утечку по сообщению или таймингу на эндпоинте входа, который и есть здесь настоящий оракул. Это полезный контроль на своём месте, но применённый не к той проблеме, он оставляет перечисление пользователей полностью эксплуатируемым." },
    { name: "Добавить правило WAF, блокирующее любой запрос, в теле которого email, которого ещё нет в вашей таблице пользователей", summary: "Блокировать неизвестные email.", justification: "Чтобы решить, есть ли email в таблице, WAF должен сверяться с ней на каждом запросе — а это И ЕСТЬ оракул, просто перенесённый на периметр, и он всё равно отличал бы известные email от неизвестных своим поведением блок/пропуск. Он также ломает легитимную регистрацию, хрупок и дорог. Это привинчивает эвристику к краю вместо устранения утечки информации у источника." },
  ]}
/>

<Quiz
  id="04-enumeration-techniques-quiz-1"
  lessonSlug="04-enumeration-techniques"
  lang="ru"
  question="Атакующий запрашивает тысячи угаданных путей и оставляет только те, что не вернули 404. Какое поведение сервера даёт ему больше всего сигнала?"
  choices={[
    { label: "Различные ответы для разных путей — 403 для защищённых, 200 для незащищённых и страницы 404, чей размер отличается от реальных страниц", correct: true },
    { label: "Единообразный 404 с одинаковым телом для каждого несуществующего пути", misconception: "Это защитный идеал, а не утечка. Единообразные ответы «не найдено» не дают атакующему способа отличить реальный, но скрытый путь от отсутствующего, что лишает перечисление сигнала." },
    { label: "Возврат 500 на любой запрос независимо от пути", misconception: "Сплошной 500 не утекает различий между путями, поэтому не даёт атакующему сигнала о том, какие пути существуют, — и к тому же сломал бы приложение для всех. Сигнал берётся из ответов, различающихся по пути." },
    { label: "Требование HTTPS на всех эндпоинтах", misconception: "Шифрование транспорта не меняет статус-код или размер, которые атакующий читает в ответе; оно защищает данные в пути, а не структурный оракул, который эксплуатирует перечисление." },
  ]}
/>

<Quiz
  id="04-enumeration-techniques-quiz-2"
  lessonSlug="04-enumeration-techniques"
  lang="ru"
  question="Почему лимиты запросов — настолько эффективный контроль именно против перечисления?"
  choices={[
    { label: "Перечисление зависит от высокого объёма запросов, чтобы искать в пространстве, поэтому ограничение частоты делает его слишком медленным для практики и достаточно громким для обнаружения", correct: true },
    { label: "Лимиты запросов шифруют ответы, так что атакующий не может прочитать статус-коды", misconception: "Лимиты запросов управляют тем, сколько запросов разрешено, а не тем, шифруются ли ответы. Они бьют по объёму, который нужен перечислению, а не по читаемости отдельного ответа." },
    { label: "Они чинят саму утечку информации, так что одинаковые ответы больше не нужны", misconception: "Лимиты замедляют прощупывание, но не убирают оракул — терпеливый, распределённый атакующий под лимитом всё равно читает ту же разницу в ответах. Закрывать саму утечку всё равно нужно." },
    { label: "Они важны только для подбора паролей, а не для перечисления эндпоинтов или объектов API", misconception: "Все три — задачи поиска в пространстве с высоким объёмом. Лимиты на IP, на аккаунт и на эндпоинт притупляют перебор путей и прогон по последовательным ID так же, как и атаки по учётным данным." },
  ]}
/>

<DragOrder
  id="04-enumeration-techniques-drag-1"
  lessonSlug="04-enumeration-techniques"
  lang="ru"
  prompt="Упорядочьте авторизованную оценку перечислением от первого шага к последнему:"
  items={[
    { id: "a", label: "Подтвердить письменную авторизацию и scope для цели" },
    { id: "b", label: "Перечислить эндпоинты: перебрать пути, читать разницу статуса/размера" },
    { id: "c", label: "Перечислить пользователей и объекты API через оракулы сообщения/тайминга/ID" },
    { id: "d", label: "Ранжировать подтверждённую поверхность по эксплуатируемости для следующей стадии" },
    { id: "e", label: "Сообщить об оракулах и рекомендовать лимиты запросов + мониторинг" },
  ]}
  correctOrder={["a", "b", "c", "d", "e"]}
/>

</Explanation>

<KeyTakeaway>Перечисление превращает неизвестную поверхность атаки в ранжированный список целей — скрытые эндпоинты (через разницу статуса/размера), валидные пользователи (через оракулы сообщения/тайминга) и объекты API (через последовательные ID). Оно работает на объёме, поэтому защита такова: убрать оракулы (единообразные ответы, выровненный тайминг, случайные ID, закрытые доки) и поднять стоимость (лимиты на IP/аккаунт), мониторя отпечаток — поток 404, прогон по ID и всплески неудачной аутентификации.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="04-enumeration-techniques-retrieval"
  lang="ru"
  questions={[
    { q: "Объясните три главные цели перечисления — эндпоинты, пользователей и объекты API — и конкретный «оракул», который утекает информацию в каждой.", a: "Эндпоинты: атакующий перебирает словарь путей и читает разницу в ответах. Единообразный 404 не утекает ничего, но 403 подтверждает, что защищённый путь существует, 200 находит незащищённый, а необычный размер ответа выдаёт реальную страницу в неверном статус-коде — так что оракул — это вариация статуса и размера по путям. Пользователи: любое место, где приложение ведёт себя по-разному для реального и фальшивого аккаунта, — оракул. Классические признаки — отдельное сообщение об ошибке («email не найден» против «неверный пароль»), другой HTTP-ответ и разница в тайминге — если реальный аккаунт запускает дорогое хеширование пароля, а несуществующий отвечает мгновенно, одно лишь время ответа отделяет их даже при одинаковых сообщениях. Потоки регистрации и сброса пароля утекают так же. Объекты API: последовательные целочисленные ID позволяют пройти всю коллекцию счётом; даже при корректном контроле доступа шаблон отказов (403 на реальный, но запрещённый, 404 на пробел) размечает, какие ID существуют, а подробные ошибки, утёкший документ Swagger/OpenAPI или introspection GraphQL выдают модель объектов целиком." },
    { q: "Почему лимиты запросов и мониторинг — правильная защита против перечисления, и в чём зрелый нюанс о том, где применять лимиты?", a: "Определяющее свойство перечисления — ему нужен высокий объём (от сотен до миллионов запросов), потому что атакующий ищет в пространстве, а не эксплуатирует одну известную брешь. Этот объём — рычаг. Лимиты запросов бьют по стоимости: ограничение запросов на IP, на аккаунт и на эндпоинт превращает минутный перебор в многодневный, достаточно долгий, чтобы окно закрылось или атакующий сдался; они редко делают перечисление невозможным, лишь непрактичным. Мониторинг бьёт по скрытности: у перечисления есть отпечаток, которого нет у нормального трафика, — поток 404 из одного источника, прогон по последовательным ID, неудачные входы по множеству имён, нечеловеческая частота запросов, — поэтому алерты на всплески 4xx, доступ к путям/ID высокой кардинальности и всплески неудачной аутентификации ловят стадию разведки до того, как она станет эксплойтом. Зрелый нюанс — где ограничивать: лимиты на IP тривиально обходятся ботнетами или ротацией прокси, поэтому высокоценные оракулы (вход, сброс пароля, эндпоинты токенов) нуждаются ещё и в лимитах на аккаунт и глобальных потолках, плюс экспоненциальный backoff и CAPTCHA или proof-of-work на подозрительном пути. И лимиты замедляют прощупывание, но не убирают оракул — закрывать саму утечку (единообразные ответы, выровненный тайминг, случайные ID) всё равно нужно, чтобы медленный, распределённый атакующий под лимитом не получал ничего." },
  ]}
/>

<Recap lang="ru">
Перечисление — это шаг разведки, превращающий неизвестную поверхность атаки в конкретный, ранжированный список целей: оно не взламывает, а сообщает атакующему, куда целиться следующим, — поэтому его притупление имеет несоразмерно высокую защитную ценность. У него три главные цели, у каждой свой утекающий оракул: эндпоинты, находимые перебором путей и чтением разницы HTTP-статуса и размера ответа по путям; пользователи, подтверждаемые через отдельные сообщения об ошибке или разницу в тайминге, когда реальный аккаунт запускает дорогое хеширование, а фальшивый отвечает мгновенно; и объекты API, проходимые через последовательные целочисленные ID и выдаваемые целиком подробными ошибками, утёкшими доками Swagger или introspection GraphQL. Всё это работает на объёме, поэтому защита идёт в два хода. Первый — убрать оракулы: возвращать единообразные ответы и общие ошибки, выровнять тайминг хешированием фиктивного пароля на ветке промаха, использовать случайные UUID вместо последовательных ID и закрывать доки API и introspection в проде. Второй — поднять стоимость и видимость: ограничить запросы на IP, на аккаунт и на эндпоинт — помня, что один лишь IP-лимит обходится ботнетами, так что высокоценным эндпоинтам нужны ещё лимиты на аккаунт и глобальные, — и мониторить безошибочный отпечаток перечисления: потоки 404, прогоны по последовательным ID и всплески неудачной аутентификации. Поймайте его на этой стадии — и вы разорвали kill chain на самом дешёвом, раннем звене. Теперь, проектируя эндпоинт, ваш рефлекс — спросить: что каждый ответ раскрывает о том, что существует, и насколько быстро кто-то может задать этот вопрос миллион раз?
</Recap>
