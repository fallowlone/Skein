---
slug: "02-how-postgres-executes-a-query"
lang: ru
track: "sql-postgres"
unit: "00-start-here"
order: 2
title: "Как Postgres выполняет запрос"
summary: "Запрос проходит пять стадий — parse, analyze, rewrite, plan, execute — прежде чем вернётся хоть одна строка. Знание конвейера говорит, куда уходит время и какая проблема живёт на какой стадии."
estMin: 14
status: ready
lessonType: topic
level: junior
concepts:
  - query-pipeline
  - parser
  - planner
  - executor
  - logical-vs-physical-plan
prereqs:
  - 01-why-sql-runs-everything
sources:
  - https://www.postgresql.org/docs/current/query-path.html
  - https://www.postgresql.org/docs/current/overview.html
  - https://www.interdb.jp/pg/pgsql03.html
---

import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import MetaphorComplete from "~/components/pedagogy/MetaphorComplete.astro";
import SpiralCue from "~/components/prose/SpiralCue.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";
import Infographic from "~/components/diagram/Infographic.astro";
import symptomToStage from "./infographics/symptom-to-stage.svg?raw";

<Hook>
Ты набираешь `SELECT * FROM orders WHERE id = 42;` и строка возвращается за миллисекунду. Кажется атомарным — будто база просто *посмотрела значение*. Это вовсе не атомарно: твой текст прошёл пять отдельных стадий, и на каждой живёт свой класс багов или тормозов. Когда запрос медленный, первый сеньорский вопрос всегда: «медленный на какой стадии?»
</Hook>

<Crux>Один запрос проходит пять стадий: parse, analyze, rewrite, plan, execute. У каждой свой режим отказа — и свой фикс.</Crux>

<Explanation>

## Пять стадий

Твоя SQL-строка не выполняется напрямую. Backend Postgres, владеющий твоим соединением, проталкивает её через конвейер:

1. **Parse** — сырой текст проверяется на *синтаксис* и превращается в parse tree (дерево разбора — внутреннее представление запроса в виде узлов). Пропущенная запятая или `SELCT` умирают здесь. Таблицы ещё не трогаются; парсер даже не знает, существует ли `orders`.
2. **Analyze (семантика)** — имена разрешаются по каталогу. Существует ли `orders`? Есть ли колонка `id`? Какие типы участвуют? Ошибка «column does not exist» рождается здесь, а не в парсере.
3. **Rewrite** — применяются rule-based преобразования. Главное: запрос к **представлению (view)** переписывается в запрос к нижележащим таблицам; сюда же внедряются политики row-level security и правила.
4. **Plan / optimize** — **планировщик** берёт проанализированное дерево запроса и строит **физический план**: какой скан (sequential? index?), какой алгоритм join (nested loop? hash? merge?), в каком порядке. Он использует **статистику** таблиц, чтобы оценить число строк, и выбирает самый дешёвый план по внутренней стоимостной модели.
5. **Execute** — исполнитель обходит выбранное дерево плана, протягивая строки сквозь него узел за узлом, и стримит результат тебе.

Вместе эти пять стадий означают, что к моменту, когда строка доходит до тебя, она уже прошла валидацию, разрешение имён, преобразование, оптимизацию и фактическое чтение — и на каждом из этих шагов может возникнуть сбой независимо. Не понимать хотя бы одну стадию — значит гадать, когда что-то идёт не так.

<FlowDiagram
  label="Конвейер запроса: сырой SQL-текст становится parse tree, затем разрешённым деревом запроса, затем деревом плана, затем строками. Синтаксические ошибки умирают на parse; плохие оценки строк бьют на plan; работа происходит на execute."
  perRow={5}
  hue="var(--d-data)"
  nodes={[
    { id: "sql", label: "SQL-текст", sub: "твоя строка" },
    { id: "parse", label: "Parse", sub: "синтаксис → tree" },
    { id: "analyze", label: "Analyze", sub: "разрешить имена" },
    { id: "rewrite", label: "Rewrite", sub: "views, RLS" },
    { id: "plan", label: "Plan", sub: "по стоимости" },
    { id: "exec", label: "Execute", sub: "→ строки" },
  ]}
  edges={[
    { from: "sql", to: "parse" },
    { from: "parse", to: "analyze" },
    { from: "analyze", to: "rewrite" },
    { from: "rewrite", to: "plan" },
    { from: "plan", to: "exec" },
  ]}
  caption="Пять стадий, у каждой своя работа. «Почему медленно?» почти всегда значит «стадия plan выбрала плохо» или «стадия execute делает слишком много I/O» — редко parse или analyze."
/>

## Почему стадии важны для отладки

Когда запрос ведёт себя неправильно, большинство инженеров сразу переписывают SQL — и тратят час, меняя текст, который никогда не был проблемой. Конвейер даёт более быстрый путь: сопоставь симптом со стадией, исправь именно там.

Конвейер — это диагностическая карта. Сопоставь симптом со стадией:

- **Синтаксическая ошибка** → стадия parse. Чисто текстовая проблема; схема не важна.
- **«relation/column does not exist»** → стадия analyze. Текст — валидный SQL, но не совпадает с каталогом.
- **Запрос внезапно медленный после загрузки данных** → стадия plan. Оценки строк у планировщика устарели, и он выбрал плохой план. Фикс обычно — `ANALYZE`, чтобы обновить статистику, а не переписывание запроса.
- **Запрос всегда медленный, план выглядит верно** → стадия execute. План в порядке, но он реально читает слишком много страниц; фикс — индекс или меньше данных, что меняет, какие планы вообще *доступны*.

<Infographic
  svg={symptomToStage}
  label="Список из четырёх диагностических случаев, сопоставляющий каждый симптом запроса со стадией конвейера, которой он принадлежит, и её фиксом: синтаксическая ошибка → parse, 'relation/column does not exist' → analyze, внезапное замедление после массовой загрузки → plan (запусти ANALYZE), хронически медленно при верном плане → execute (добавь индекс)."
  caption="Конвейер — это таблица поиска: сопоставь симптом со стадией и исправь именно там. Бо́льшая часть времени на «медленный запрос» тратится на переписывание SQL, который никогда не был проблемой."
  hue="var(--d-infra)"
/>

Поэтому сеньорский рефлекс — `EXPLAIN`. `EXPLAIN <query>` показывает **план**, выбранный планировщиком, не выполняя запрос; `EXPLAIN ANALYZE <query>` выполняет его и показывает оценочные *против* фактических строк в каждом узле. Зазор между оценкой и фактом — самое полезное число в работе над производительностью Postgres: он говорит, обманули ли планировщик устаревшей статистикой.

```sql
-- Только план (быстро, без выполнения):
EXPLAIN SELECT * FROM orders WHERE id = 42;

-- Выполнить и сравнить оценку с реальностью:
EXPLAIN ANALYZE SELECT * FROM orders WHERE id = 42;
```

<SpiralCue thread="latency" lang="ru" />

Ты пока не прочитаешь план целиком — это отдельный раздел дальше (08-internals-and-tuning), а трек `databases` глубоко разбирает планировщик и внутренности планов выполнения. Сейчас усвой только форму: текст внутрь, пять стадий, строки наружу, а `EXPLAIN` — твоё окно в четвёртую стадию.

<Inset kind="why" lang="ru">
Зачем вообще отделять планирование от выполнения? Потому что планирование *дорого* — для запроса, соединяющего шесть таблиц, число возможных порядков join'ов взрывается, и планировщик делает реальную комбинаторную работу, чтобы найти дешёвый. Postgres платит эту цену один раз и (с prepared statements) может переиспользовать план во многих выполнениях. Это также значит, что тот же распарсенный запрос можно перепланировать позже при лучшей статистике. Слить план и выполнение вместе — потерять оба выигрыша. Глава об execution-plans трека `databases` глубоко копает стоимостную модель, ведущую четвёртой стадией.
</Inset>

<Quiz
  id="00-pipeline-quiz-1"
  lessonSlug="02-how-postgres-executes-a-query"
  lang="ru"
  question={"Ты запускаешь запрос и получаешь 'column \"emial\" does not exist'. Какая стадия породила эту ошибку?"}
  choices={[
    { label: "Analyze (семантика) — текст распарсился нормально, но имя не разрешается по каталогу", correct: true },
    { label: "Parse — это синтаксическая ошибка", misconception: "Текст синтаксически валиден; опечатка в имени колонки ловится при разрешении имён, на стадии analyze." },
    { label: "Execute — сбой при чтении строк", misconception: "Выполнение даже не стартует; запрос отвергается до планирования, потому что колонку нельзя разрешить." },
    { label: "Rewrite — сломалось правило view", misconception: "Rewrite обрабатывает views и RLS, а не разрешение имени колонки базовой таблицы." },
  ]}
/>

<Quiz
  id="00-pipeline-quiz-2"
  lessonSlug="02-how-postgres-executes-a-query"
  lang="ru"
  question="Вчера запрос был быстрым; сегодня, после большого bulk INSERT, он в 50× медленнее при том же тексте. Лучший первый шаг?"
  choices={[
    { label: "Запустить ANALYZE, чтобы обновить статистику таблицы и планировщик снова оценивал строки верно", correct: true },
    { label: "Переписать SQL «эффективнее»", misconception: "Текст не менялся и вчера был в порядке; регрессия — в выборе плана из-за устаревшей статистики, а не в форме запроса." },
    { label: "Добавить RAM на сервер", misconception: "Железо редко объясняет внезапную 50× регрессию после загрузки данных; её объясняет устаревшая статистика, вводящая планировщик в заблуждение." },
    { label: "Удалить и пересоздать таблицу", misconception: "Деструктивно и не нужно; данные в порядке — устарела лишь статистика планировщика." },
  ]}
/>

<MetaphorComplete
  id="00-pipeline-metaphor-1"
  lessonSlug="02-how-postgres-executes-a-query"
  lang="ru"
  setup="Заполни пропуск: EXPLAIN показывает выбранный планировщиком план _______ выполнения запроса, тогда как EXPLAIN ANALYZE выполняет его и сообщает фактические тайминги и число строк."
  accepted={["без", "до", "вместо"]}
  canonical="без"
  explanation="Простой EXPLAIN — это сухой прогон: он спрашивает планировщик «какой план ты бы взял?» и печатает его, не трогая heap. EXPLAIN ANALYZE реально выполняет, так что можно сравнить оценку планировщика с реальностью — ключевой навык раздела 08."
/>

</Explanation>

<KeyTakeaway>Каждый запрос проходит пять стадий: parse (синтаксис), analyze (разрешение имён), rewrite (views/RLS), plan (физический план по стоимости), execute (выдача строк). У каждой стадии свой режим отказа, поэтому «медленно на какой стадии?» — первый диагностический вопрос. `EXPLAIN` раскрывает план; `EXPLAIN ANALYZE` сравнивает оценку с фактом — зазор бесценен.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="00-pipeline-retrieval"
  lang="ru"
  questions={[
    { q: "Перечисли пять стадий, через которые проходит запрос, по порядку, одним словом о каждой.", a: "Parse (синтаксис → tree), Analyze (разрешение имён по каталогу), Rewrite (применение правил view/RLS), Plan (стоимостный выбор сканов/join'ов/порядка → физический план), Execute (обход плана, выдача строк)." },
    { q: "Запрос внезапно медленный после bulk-загрузки, хотя текст не менялся. Какая стадия виновата и в чём фикс?", a: "Стадия plan. Bulk-загрузка сделала статистику числа строк у планировщика устаревшей, и теперь он оценивает плохо и выбирает худший план. Фикс — обновить статистику через ANALYZE (или analyze от autovacuum), а не переписывать запрос — SQL никогда не был проблемой." },
    { q: "В чём разница между EXPLAIN и EXPLAIN ANALYZE и почему важен зазор оценка-против-факта?", a: "EXPLAIN печатает план, который планировщик выбрал бы, не выполняя его. EXPLAIN ANALYZE реально выполняет запрос и аннотирует каждый узел плана оценочными vs фактическими строками и таймингом. Большой зазор между оценочными и фактическими строками значит, что планировщик работал с неверной статистикой — это несоответствие и есть корневая причина большинства плохих планов." },
  ]}
/>

<Recap lang="ru">
Запрос не ищется атомарно — он проходит пятистадийный конвейер: **parse** (синтаксис в дерево), **analyze** (разрешение имён по каталогу), **rewrite** (разворачивание views, внедрение row-level security), **plan** (стоимостный оптимизатор превращает дерево запроса в физический план сканов и join'ов) и **execute** (исполнитель обходит план и стримит строки). Ценность знания конвейера — диагностическая: синтаксическая ошибка — это parse, ошибка отсутствующей колонки — analyze, внезапное замедление после загрузки — стадия plan, лишённая свежей статистики (фикс — `ANALYZE`), а хронически медленный, но корректный план — стадия execute, делающая слишком много I/O (фикс — индекс). `EXPLAIN` показывает выбранный план; `EXPLAIN ANALYZE` выполняет его и обнажает зазор оценка-против-факта. Теперь, когда после bulk-загрузки запрос внезапно просядет в 50 раз, ты не будешь переписывать SQL — ты запустишь `ANALYZE`, откроешь `EXPLAIN ANALYZE` и посмотришь зазор между оценкой и фактом: именно там живёт ответ.
</Recap>
