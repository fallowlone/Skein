---
slug: "01-why-sql-runs-everything"
lang: ru
track: "sql-postgres"
unit: "00-start-here"
order: 1
title: "Почему SQL до сих пор управляет всем"
summary: "SQL декларативен — ты описываешь нужное множество строк, а движок сам решает, как его достать. Именно это разделение позволило ему пережить 50 лет и каждую волну NoSQL."
estMin: 12
status: ready
lessonType: topic
level: junior
concepts:
  - declarative-query
  - relational-model
  - set-based-thinking
  - sql-standard
prereqs: []
sources:
  - https://www.postgresql.org/docs/current/tutorial-sql.html
  - https://www.postgresql.org/docs/current/queries-overview.html
  - https://use-the-index-luke.com/sql/preface
---

import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import MetaphorComplete from "~/components/pedagogy/MetaphorComplete.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
Ты приходишь в компанию. Фронтенд уже на третьем фреймворке, бэкенд переписали с Ruby на Go, очередь сообщений меняли дважды. Нетронутым с 2009 года осталось одно: SQL, который читает таблицу orders. Новые языки умирают за десятилетие; этот `SELECT` переживёт твой срок в компании. Этот урок — о том, почему.
</Hook>

<Crux>SQL декларативен — ты заявляешь результат, движок выбирает как. В этом зазоре причина, почему он пережил всех.</Crux>

<Explanation>

## Декларативный, а не императивный

В большинстве языков ты пишешь *как*: пройди по массиву, сравни каждый элемент, добавь совпадения в новый список. SQL — наоборот. Ты пишешь *что* хочешь и молчишь о том, как:

```sql
SELECT name, email
FROM users
WHERE country = 'JP'
ORDER BY created_at DESC
LIMIT 10;
```

Ты не написал ни одного цикла. Не сказал «используй индекс по `country`», «сортируй quicksort» или «читай диск страницами по 8 КБ». Ты описал *множество строк* — японские пользователи, новейшие сверху, первые десять — и отдал задачу движку. Postgres разбирается с остальным: какой индекс взять, сортировать в памяти или сбросить на диск, сколько CPU-воркеров бросить на запрос.

У этого разделения есть имя: запрос — это **логика** (что), **план** — физика (как). Логический слой твой. Физический — планировщика. Следующий урок вскрывает планировщик; пока важно лишь то, что эта стена существует.

<FlowDiagram
  label="Декларативный зазор: твой SQL заявляет множество строк; планировщик превращает его в физический план; исполнитель выдаёт строки. Ты пишешь только первый блок."
  perRow={5}
  hue="var(--d-data)"
  nodes={[
    { id: "you", label: "Ты пишешь", sub: "ЧТО (SQL)" },
    { id: "parse", label: "Парсер", sub: "→ дерево запроса" },
    { id: "plan", label: "Планировщик", sub: "выбирает КАК" },
    { id: "exec", label: "Исполнитель", sub: "выполняет план" },
    { id: "rows", label: "Результат", sub: "строки" },
  ]}
  edges={[
    { from: "you", to: "parse" },
    { from: "parse", to: "plan" },
    { from: "plan", to: "exec" },
    { from: "exec", to: "rows" },
  ]}
  caption="Тебе принадлежит только самый левый блок. Всё правее — работа движка, и он может менять решение по мере роста данных, без единой правки с твоей стороны."
/>

## Почему это и есть долговечная идея

Поскольку ты не закодировал *как*, движок волен это менять. Тот же `SELECT`, что сканировал таблицу на 10 000 строк в 2009-м, сегодня идёт по B-tree индексу на 500 миллионах строк в 2026-м — тот же текст, совершенно другое физическое выполнение. Добавь индекс сегодня вечером — и завтрашний запрос станет в 600 раз быстрее без единой правки кода. Попробуй это с написанным вручную циклом.

Именно поэтому SQL пережил десятилетие NoSQL. Документные и key-value хранилища выиграли отдельные битвы (масштабирование записи, схемалесс-блобы), но большинство из них отрастили язык запросов, подозрительно похожий на SQL, потому что декларативную модель действительно трудно превзойти в вопросе «дай строки, подходящие под условие, соединённые с теми, сгруппированные и отсортированные». Сам Postgres впитал лучшую идею NoSQL — JSONB-документы — не отказавшись от реляционного ядра (встретишь это в разделе 06).

## Мышление множествами, а не строками

Сдвиг, на котором спотыкается каждый новичок: SQL работает с **множествами**, целиком и сразу, а не построчно. Здесь нет «текущей строки», как в цикле. `WHERE` концептуально применяется ко всему множеству как бы параллельно; `JOIN` — это комбинация двух множеств; `GROUP BY` сворачивает множество в корзины.

Практическое следствие: написать в приложении цикл, который шлёт по одному запросу на строку — паттерн **N+1 запросов** — самая частая ошибка производительности в продакшне. Один запрос на 1000 строк — это один round trip; 1000 запросов по одной строке — это 1000 round trip'ов и часто в 100 раз медленнее. Весь смысл SQL — протолкнуть операцию над множеством *вниз*, в движок, а не тянуть строки наверх и крутить цикл в своём коде.

<Inset kind="why" lang="ru">
Почему «лучший» язык не заменил SQL? Несколько пытались — LINQ, ORM, GraphQL, парад NoSQL-DSL. Каждый полезен, но любой из них, когда запрос становится нетривиальным, либо компилируется *вниз в SQL*, либо плохо переизобретает join'ы и агрегацию. За декларативной реляционной моделью стоит 50 лет исследований оптимизаторов; новому языку мало приятного синтаксиса — ему нужно заново вывести стоимостный планировщик. Этот ров и есть причина, по которой «просто выучи SQL» — всё ещё самый рычажный навык работы с данными в 2026 году.
</Inset>

<Quiz
  id="00-why-sql-quiz-1"
  lessonSlug="01-why-sql-runs-everything"
  lang="ru"
  question="Что значит, что SQL декларативен?"
  choices={[
    { label: "Ты описываешь нужный результат; движок решает, как его вычислить", correct: true },
    { label: "Ты пишешь точный цикл и порядок доступа к диску, которому движок обязан следовать", misconception: "Это императивно. В SQL ты никогда не задаёшь цикл или путь доступа — их выбирает планировщик." },
    { label: "Каждый запрос обязан объявить переменные до использования", misconception: "Декларативность — про описание результата, а не про объявление переменных." },
    { label: "SQL умеет только объявлять таблицы, но не запрашивать их", misconception: "SQL и определяет (DDL), и запрашивает (DML) — «декларативный» относится к модели запроса, а не к ограничению." },
  ]}
/>

<Quiz
  id="00-why-sql-quiz-2"
  lessonSlug="01-why-sql-runs-everything"
  lang="ru"
  question="Приложение грузит 1 заказ, затем шлёт по запросу на каждую позицию, чтобы загрузить их. 80 позиций = 81 запрос. Как это называется и почему медленно?"
  choices={[
    { label: "Паттерн N+1 запросов — много round trip'ов вместо одного запроса над множеством", correct: true },
    { label: "JOIN — правильный и быстрый способ это сделать", misconception: "Это противоположность join'а. Один JOIN достал бы все 80 позиций за один round trip." },
    { label: "Шардирование — разбиение запроса по машинам", misconception: "Шардирование — это горизонтальное разбиение данных, не связано с этим антипаттерном." },
    { label: "Нормализация — и это всегда хорошо", misconception: "Нормализация — про проектирование схемы; она не описывает это поведение запросов." },
  ]}
/>

<MetaphorComplete
  id="00-why-sql-metaphor-1"
  lessonSlug="01-why-sql-runs-everything"
  lang="ru"
  setup="Заполни пропуск: SQL для базы данных — то же, что заказ в ресторане для кухни. Ты говоришь, какое блюдо хочешь; ты не указываешь, какую _______ берёт повар и в каком порядке делает шаги."
  accepted={["сковороду", "сковородку", "сковорода", "плиту", "технику"]}
  canonical="сковороду"
  explanation="Ты заявляешь итог («лосось, средней прожарки»); кухня владеет методом (какая сковорода, какой огонь, какой порядок). SQL — это чек заказа; планировщик — шеф, выбирающий технику. Тот же чек — кухня может переоптимизировать готовку, а ты не переписываешь заказ."
/>

</Explanation>

<KeyTakeaway>SQL декларативен: ты описываешь множество строк, движок владеет выполнением. Эта стена между логикой и физикой — причина, по которой один неизменный запрос продолжает ускоряться по мере роста данных, и почему SQL пережил все замены. Мысли множествами — проталкивая операцию в движок — а не построчными циклами.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="00-why-sql-retrieval"
  lang="ru"
  questions={[
    { q: "В одном предложении: чем декларативный SQL-запрос отличается от императивного кода?", a: "Императивный код задаёт, как вычислить результат шаг за шагом (циклы, сравнения, порядок доступа); декларативный SQL-запрос задаёт только то, какое множество строк нужно, и оставляет выбор стратегии выполнения планировщику движка." },
    { q: "Почему один и тот же текст SQL может стать в 600 раз быстрее спустя годы без правок?", a: "Потому что запрос задаёт лишь логический результат, а не физический план. Когда ты добавляешь индекс или меняется статистика планировщика, движок выбирает новый, более быстрый физический план (например, index scan вместо sequential scan) для того же текста запроса — «как» никогда не зашито в твой SQL." },
    { q: "Что такое паттерн N+1 запросов и в чём фикс через множества?", a: "N+1 — это один запрос, чтобы получить N родительских строк, плюс по одному дополнительному запросу на каждую строку ради связанных данных — итого N+1 round trip'ов. Фикс — один запрос над множеством, который достаёт всё сразу, обычно JOIN или один WHERE ... IN (...), проталкивая работу в движок вместо цикла в коде приложения." },
  ]}
/>

<Recap lang="ru">
SQL декларативен: ты пишешь, *какое* множество строк хочешь, а движок решает, *как* его произвести. Это разделение логического запроса и физического плана — долговечное ядро языка: оно позволяет одному неизменному `SELECT` продолжать ускоряться по мере добавления индексов и роста данных, и именно поэтому SQL пережил эпоху NoSQL (впитав её лучшие идеи, вроде JSONB). Установка, которую стоит принять сейчас: мысли **множествами**, которые движок обрабатывает целиком, а не построчными циклами — построчная привычка порождает антипаттерн N+1, самый частый баг производительности в продакшн-коде. Следующий урок вскрывает правый блок диаграммы: как Postgres на самом деле превращает твой `SELECT` в строки.
</Recap>
