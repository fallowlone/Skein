---
slug: "02-cte-vs-subquery"
lang: ru
track: "sql-postgres"
unit: "05-cte-and-recursion"
order: 2
title: "CTE против подзапроса и представления"
summary: "CTE, инлайн-подзапрос и представление выражают одну логику в разных местах с разным временем жизни — а у CTE когда-то была тихая оптимизационная оговорка, которой не было у подзапроса."
estMin: 14
status: ready
lessonType: topic
level: middle
concepts:
  - derived-table
  - scalar-subquery
  - view
  - cte-scope
  - optimization-fence
prereqs:
  - 01-cte-basics
sources:
  - https://www.postgresql.org/docs/current/queries-with.html
  - https://www.postgresql.org/docs/current/sql-createview.html
  - https://www.postgresql.org/docs/current/queries-table-expressions.html
---

import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import MetaphorComplete from "~/components/pedagogy/MetaphorComplete.astro";
import StackDiagram from "~/components/diagram/StackDiagram.astro";

<Hook>
Двое инженеров спорят на ревью. Один: «тут должен быть CTE, так чище». Другой: «подзапрос в FROM нормально, и он быстрее». Оба порой правы — а до Postgres 12 у довода «быстрее» были настоящие зубы, потому что CTE был не просто подзапросом с именем: он был *оптимизационным барьером*, сквозь который планировщик отказывался смотреть. Понимать, где живёт каждая форма, сколько живёт и как её трактует планировщик — это разница между вкусовщиной и обоснованным мнением.
</Hook>

<Crux>CTE, подзапрос и представление — одна логика с разным временем жизни; а у CTE когда-то была оговорка, которой нет у подзапроса.</Crux>

<Explanation>

## Одна логика, три места, три времени жизни

Один и тот же подзапрос может выступать в трёх структурно разных формах. Возьмём «категории, чей родитель — корень» над `categories(id, parent_id)`:

```sql
-- 1. Инлайн / производная таблица: живёт в FROM одного оператора
SELECT c.id
FROM categories c
JOIN (SELECT id FROM categories WHERE parent_id IS NULL) roots
  ON c.parent_id = roots.id;

-- 2. CTE: именованный, всё ещё ограничен одним оператором
WITH roots AS (SELECT id FROM categories WHERE parent_id IS NULL)
SELECT c.id
FROM categories c
JOIN roots ON c.parent_id = roots.id;

-- 3. Представление: именованное, хранимое в каталоге, переиспользуемое между операторами
CREATE VIEW roots AS SELECT id FROM categories WHERE parent_id IS NULL;
SELECT c.id FROM categories c JOIN roots ON c.parent_id = roots.id;  -- позже, где угодно
```

Ось, которая их различает, — **время жизни и видимость**. У инлайн-подзапроса нет имени, и он существует только в своей позиции в одном операторе. У CTE есть имя, но он всё ещё ограничен одним оператором — когда оператор завершается, имя исчезает. У представления имя *хранится в каталоге*: оно сохраняется, и любой будущий запрос может из него читать. Скалярный подзапрос — четвёртая форма: подзапрос в скобках, возвращающий ровно одну строку и один столбец, годный везде, где ожидается одно значение, как `WHERE created_at = (SELECT max(created_at) FROM orders)`.

<StackDiagram
  label="Четыре формы запросных выражений, упорядоченные по времени жизни и видимости. Скалярный подзапрос и инлайн производная таблица живут анонимно внутри одного оператора. CTE именован, но всё ещё одно-операторный. Представление именовано и сохраняется в каталоге, переиспользуемое любым более поздним запросом."
  hue="var(--d-data)"
  layers={[
    { label: "Представление (view)", note: "именовано • хранится в каталоге • между операторами" },
    { label: "CTE (WITH)", note: "именован • один оператор • цепочки" },
    { label: "Производная таблица (подзапрос в FROM)", note: "анонимна • один оператор" },
    { label: "Скалярный подзапрос", note: "анонимен • одна строка/столбец • инлайн-значение" },
  ]}
  caption="Решающая ось — время жизни: бери самую низкую форму, покрывающую твою потребность в переиспользовании. Между операторами → представление; многошаговость внутри оператора → CTE; одно локальное использование → инлайн-подзапрос."
/>

## Когда обычный подзапрос — более ясный выбор

CTE не лучше автоматически. Подзапрос в `FROM` часто яснее, когда логика используется ровно один раз и мала — обернуть трёхстрочный фильтр в `WITH` добавляет церемонии без ясности. Скалярный подзапрос в `SELECT` или `WHERE` — естественная форма для «сравни каждую строку с одним агрегатным значением»; переписывание его как CTE обычно читается хуже. А **коррелированный** подзапрос — тот, что ссылается на внешнюю строку, как `WHERE EXISTS (SELECT 1 FROM orders o WHERE o.customer_id = c.id)` — не имеет чистого эквивалента-CTE, потому что по замыслу выполняется на каждую внешнюю строку. Используй форму, которая называет то, что заслуживает имени, и инлайнит то, что нет.

## Оговорка, указывающая на урок 03

Вот часть, что превратила спор на ревью из вкусовщины в суть. Бóльшую часть истории Postgres CTE был **безусловным оптимизационным барьером**: планировщик всегда вычислял CTE отдельно и материализовал его результат, отказываясь проталкивать предикаты из внешнего запроса внутрь него. Эквивалентный инлайн-подзапрос барьером *не* был — планировщик мог его расплющить, протолкнуть фильтры внутрь и выбрать индексы соответственно. Так что одна и та же логика могла выполняться очень по-разному в зависимости от выбранной формы, и форма-CTE порой была драматически медленнее.

```sql
-- До PG12: этот CTE материализовался целиком, потом фильтровался — WHERE
-- НЕ мог протолкнуться в CTE, поэтому сканировал куда больше строк, чем нужно.
WITH all_events AS (SELECT * FROM events)        -- миллионы строк материализованы
SELECT * FROM all_events WHERE tenant_id = 7;    -- фильтр применён ПОСЛЕ

-- Эквивалентный подзапрос позволил планировщику протолкнуть tenant_id = 7 в индекс.
SELECT * FROM (SELECT * FROM events) e WHERE tenant_id = 7;
```

Postgres 12 изменил этот дефолт, а ключевые слова `MATERIALIZED` / `NOT MATERIALIZED` позволяют управлять этим явно. Вся эта история — что изменилось, когда материализация всё ещё помогает и как прочитать это в `EXPLAIN` — это урок 03. Пока вывод такой: CTE против подзапроса *не* чисто косметика; исторически это влияло на план, а на старых серверах влияет до сих пор.

<Inset kind="edgecase" lang="ru">
Представление добавляет нюанс, которого нет у других: оно проходит стадию **rewrite** (вспомни конвейер из раздела 00). Когда ты запрашиваешь представление, планировщик подставляет его определение в твой запрос и затем планирует объединённое целое — так что простое представление обычно встраивается и оптимизируется вместе с твоим запросом, во многом как современный не-материализованный CTE. Исключение — `MATERIALIZED VIEW`, хранимый снимок, который ты обновляешь явно: вот он действительно предвычислен и читается как таблица. Обычные представления — это макросы запроса; материализованные представления — это кешированные результаты.
</Inset>

<Quiz
  id="02-cte-vs-subquery-quiz-1"
  lessonSlug="02-cte-vs-subquery"
  lang="ru"
  question="Нужен один и тот же именованный фильтр, переиспользуемый в десятке разных отчётов, запускаемых в разное время. Какая форма подходит?"
  choices={[
    { label: "Представление — оно именовано и хранится в каталоге, так что любой поздний запрос может из него читать", correct: true },
    { label: "CTE — объяви один раз, и каждый отчёт его увидит", misconception: "Имя CTE живёт только внутри одного оператора, что его определяет; его нельзя разделить между отдельными запросами-отчётами." },
    { label: "Инлайн-подзапрос, скопированный в каждый отчёт", misconception: "Это ровно то дублирование, которого избегает общий объект — двенадцать копий со временем расходятся." },
    { label: "Скалярный подзапрос", misconception: "Скалярный подзапрос возвращает одно значение инлайн; это не переиспользуемое именованное отношение между операторами." },
  ]}
/>

<Quiz
  id="02-cte-vs-subquery-quiz-2"
  lessonSlug="02-cte-vs-subquery"
  lang="ru"
  question="На сервере Postgres 11 отчёт, обёрнутый в CTE, заметно медленнее той же логики как подзапроса в FROM. Наиболее вероятная причина?"
  choices={[
    { label: "CTE — оптимизационный барьер: он материализуется целиком, и внешний WHERE не проталкивается внутрь", correct: true },
    { label: "CTE всегда медленнее подзапросов на любой версии Postgres", misconception: "С Postgres 12 не-материализованный CTE встраивается и обычно планируется идентично подзапросу; поведение барьера было привязано к версии." },
    { label: "CTE форсирует sequential scan, чего подзапрос никогда бы не сделал", misconception: "Дело не в типе скана как таковом; барьер блокирует проталкивание предиката, поэтому CTE читает больше строк до фильтрации." },
    { label: "Подзапросы кешируются, а CTE нет", misconception: "Ни то ни другое не кешируется между операторами; разница — в барьере планировщика вокруг до-12 CTE, а не в кешировании." },
  ]}
/>

<MetaphorComplete
  id="02-cte-vs-subquery-metaphor-1"
  lessonSlug="02-cte-vs-subquery"
  lang="ru"
  setup="Заполни пропуск: CTE и представление выражают один запрос, но имя CTE живёт в пределах одного _______, тогда как имя представления хранится в каталоге и сохраняется во многих из них."
  accepted={["оператора", "оператор", "запроса", "одного оператора", "одного запроса"]}
  canonical="оператора"
  explanation="Решающая ось между двумя именованными формами — время жизни. CTE ограничен ровно одним оператором и исчезает с его завершением; представление — это хранимый объект каталога, из которого может читать любой более поздний отдельный оператор. Выбирай форму, чьё время жизни совпадает с твоей потребностью в переиспользовании."
/>

</Explanation>

<KeyTakeaway>CTE, инлайн-подзапрос и представление — одна логика с разным временем жизни: подзапрос (анонимный, одно использование), CTE (именованный, один оператор), представление (именованное, хранимое, между операторами). Бери самую низкую форму, покрывающую потребность в переиспользовании — и помни, что CTE исторически был оптимизационным барьером, которым подзапрос не был, что и есть тема следующего урока.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="02-cte-vs-subquery-retrieval"
  lang="ru"
  questions={[
    { q: "Упорядочь CTE, инлайн-подзапрос и представление по времени жизни и видимости.", a: "Инлайн-подзапрос: анонимен, существует только в своей позиции в одном операторе. CTE: именован, но всё ещё ограничен одним оператором — исчезает с его завершением. Представление: именовано и хранится в каталоге, поэтому сохраняется, и любой поздний отдельный запрос может из него читать. Выбирай самую низкую форму, покрывающую потребность в переиспользовании." },
    { q: "Когда обычный подзапрос яснее, чем CTE?", a: "Когда логика используется ровно один раз и мала (CTE добавляет церемонии), для скалярного подзапроса, возвращающего одно значение в SELECT/WHERE, и для коррелированного подзапроса, ссылающегося на внешнюю строку на каждой итерации — у него нет чистого эквивалента-CTE, потому что по замыслу он выполняется на каждую внешнюю строку." },
    { q: "В чём была историческая оптимизационная разница между CTE и эквивалентным подзапросом?", a: "До Postgres 12 CTE был безусловным оптимизационным барьером: он материализовался целиком, и предикаты внешнего запроса не могли протолкнуться внутрь, поэтому он часто сканировал куда больше строк, чем эквивалентный инлайн-подзапрос, который планировщик мог расплющить и отфильтровать рано. PG12 сделал CTE встраиваемыми по умолчанию; MATERIALIZED / NOT MATERIALIZED теперь этим управляют." },
  ]}
/>

<Recap lang="ru">
CTE, инлайн (производная таблица) подзапрос, скалярный подзапрос и представление — четыре способа написать одну логику, разделённые **временем жизни и видимостью**: скалярный подзапрос и инлайн производная таблица анонимны и живут только в своём месте в одном операторе; CTE именован, но всё ещё одно-операторный; представление именовано, хранится в каталоге и переиспользуемо любым поздним запросом. Тянись к самой низкой форме, покрывающей потребность в переиспользовании — обычный подзапрос яснее для разовой и коррелированной логики, CTE — для многошагового конвейера внутри оператора, представление — для переиспользования между операторами. Довод, поднимающий это над вкусовщиной, — история производительности: CTE был, до Postgres 12, безусловным оптимизационным барьером — материализован целиком, неуязвим для проталкивания предиката — тогда как эквивалентный подзапрос им не был, поэтому эти две формы могли выполняться дико по-разному. Postgres 12 сделал CTE встраиваемыми по умолчанию и добавил `MATERIALIZED` / `NOT MATERIALIZED` для управления. Когда именно материализация помогает, когда вредит и как увидеть это в `EXPLAIN` — это весь следующий урок.
</Recap>
