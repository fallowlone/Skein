---
slug: "04-advisory-locks"
lang: ru
track: "sql-postgres"
unit: "07-transactions-concurrency"
order: 4
title: "Advisory-блокировки"
summary: "Advisory-блокировки — мьютексы уровня приложения по целочисленному ключу, не привязанные к строке. Идеальны для leader election и дедупа cron — а сессионные тихо текут через transaction-pooler."
estMin: 15
status: ready
lessonType: topic
level: senior
concepts:
  - advisory-locks
  - pg-advisory-lock
  - xact-advisory-lock
  - leader-election
  - connection-pooling
prereqs:
  - 03-select-for-update
sources:
  - https://www.postgresql.org/docs/current/explicit-locking.html
  - https://www.postgresql.org/docs/current/functions-admin.html
  - https://www.interdb.jp/pg/pgsql05.html
---

import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import MetaphorComplete from "~/components/pedagogy/MetaphorComplete.astro";
import SequenceDiagram from "~/components/diagram/SequenceDiagram.astro";

<Hook>
Ночной биллинг-cron работал на трёх app-серверах для надёжности. Однажды ночью все три сработали в одну минуту, и каждого клиента списали трижды. Первый фикс команды — `SELECT FOR UPDATE` на «строке-блокировке» — работал, пока они не перешли на PgBouncer в transaction-режиме: тогда блокировка тихо перестала держаться между запросами задачи. Настоящий инструмент они никогда не использовали — **advisory-блокировка** Postgres, мьютекс уровня приложения, вообще не привязанный к таблице. Но у advisory-блокировок есть своя ловушка, которая укусила их следующей.
</Hook>

<Crux>Advisory-блокировки — это мьютексы уровня приложения по числовому ключу, не привязанные к строкам; сессионные текут через пул соединений.</Crux>

<Explanation>

## Мьютекс, который не строка

Что блокировать, когда то, что ты координируешь, — не одна строка, а целая задача, пересборка индекса тенанта или право быть единственным сервером, делающим что-то прямо сейчас? Вот для чего нужны advisory-блокировки.

Каждая блокировка до сих пор защищала *строку*. **Advisory-блокировка** не защищает ничего конкретного — это именованная защёлка, о которой договаривается приложение, по 64-битному целому (или двум 32-битным), смысл которым задаёшь *ты*. Postgres лишь гарантирует не больше одного держателя данного ключа за раз, по всему кластеру. Это распределённый мьютекс, который ты получаешь бесплатно вместе с уже имеющейся базой.

```sql
-- Попытаться стать единственным воркером для "nightly-billing" (ключ 42).
SELECT pg_try_advisory_lock(42);
-- вернёт true  -> ты держишь, делай работу
-- вернёт false -> держит кто-то другой, тихо пропусти
SELECT pg_advisory_unlock(42);   -- освободить по завершении
```

`pg_advisory_lock(key)` **блокируется** до захвата; `pg_try_advisory_lock(key)` возвращается сразу с `true`/`false`. Неблокирующая форма `try_` — то, что нужно для «это должен сделать только один из нас — остальные просто пропускают».

## Сессионная против транзакционной

Именно это различие решает, работает ли твоя блокировка в продакшене.

- **Сессионная** (`pg_advisory_lock`, `pg_try_advisory_lock`) — держится, пока ты явно не вызовешь `pg_advisory_unlock` или пока **сессия** (соединение с БД) не закончится. Она **не** освобождается на `COMMIT`.
- **Транзакционная** (`pg_advisory_xact_lock`, `pg_try_advisory_xact_lock`) — автоматически освобождается в конце транзакции (`COMMIT` или `ROLLBACK`). Нет функции unlock и нет способа её утечь.

```sql
-- Транзакционная: освобождается автоматически на COMMIT/ROLLBACK. Безопасна с пулером.
BEGIN;
SELECT pg_try_advisory_xact_lock(42);
-- ... делай защищённую работу ...
COMMIT;   -- блокировка гарантированно ушла, даже если код забыл unlock
```

<SequenceDiagram
  label="Три инстанса cron каждый вызывают pg_try_advisory_lock на одном ключе; только первый получает true и запускает биллинг, два других получают false и пропускают, так что клиентов списывают один раз."
  hue="var(--d-data)"
  actors={["Cron A", "Postgres", "Cron B/C"]}
  messages={[
    { from: "Cron A", to: "Postgres", label: "pg_try_advisory_lock(42)" },
    { from: "Postgres", to: "Cron A", label: "true -> запуск биллинга" },
    { from: "Cron B/C", to: "Postgres", label: "pg_try_advisory_lock(42)" },
    { from: "Postgres", to: "Cron B/C", label: "false -> пропуск" },
    { from: "Cron A", to: "Postgres", label: "unlock / COMMIT" },
    { from: "Postgres", to: "Cron A", label: "освобождено" },
  ]}
  caption="Один ключ, один держатель. Проигравшие не встают в очередь и не падают — получают false и уходят. Это паттерн дедупа: каждая реплика пробует, ровно одна выигрывает, задача выполняется один раз на весь флот."
/>

## Где advisory-блокировки сияют

- **Leader election / дедуп cron** — N реплик каждая делает `pg_try_advisory_lock(key)`; та, что получит `true`, выполняет singleton-задачу, остальные пропускают. Без ZooKeeper, без Redis.
- **Мьютекс уровня приложения над логическим ресурсом** — сериализовать путь кода по чему-то, что не одна строка: «перестроить поисковый индекс тенанта 99» → `pg_advisory_xact_lock(99)`.
- **Сериализация горячего пути** без блокировки реальных строк данных, избегая bloat и FK-конкуренции.

Против блокировок строк: блокировка строки требует существования строки и защищает именно тот кортеж; advisory-блокировка защищает абстрактный ключ, который может не соответствовать никакой строке, будущей строке или целой логической операции. Используй блокировки строк для данных, которые редактируешь; advisory-блокировки для координации.

<Inset kind="mistake" lang="ru">
Ловушка пулера целиком. Сессионные advisory-блокировки привязаны к **соединению с БД**, а не к логическому запросу приложения. Под transaction-режимом пулера (PgBouncer `pool_mode = transaction`, RDS Proxy, Supabase pooler) приложение одалживает backend-соединение на *транзакцию* и возвращает его в пул после — так что сессионная блокировка, взятая в одной транзакции, может держаться на соединении, которое теперь переиспользует другой запрос, или никогда не освободиться, потому что «твою» сессию передали дальше. Получаешь фантомную конкуренцию и блокировки, переживающие своего владельца. Два правила: (1) предпочитай `pg_advisory_xact_lock`, чтобы блокировка умирала с транзакцией и не зависала на пулинговом соединении; (2) если приходится использовать сессионную блокировку, не запускай этот путь кода за transaction-режимом пулера. Также берегись **коллизий пространства ключей**: две несвязанные фичи, обе хэширующие имя в один 64-битный ключ, будут блокировать друг друга без видимой причины — давай ключам неймспейс (например, форму из двух int с classid на фичу).
</Inset>

<Quiz
  id="04-advisory-locks-quiz-1"
  lessonSlug="04-advisory-locks"
  lang="ru"
  question="Твой сервис работает за PgBouncer в transaction-режиме. Какой вызов advisory-блокировки безопасен и почему?"
  choices={[
    { label: "pg_advisory_xact_lock — он освобождается автоматически на COMMIT/ROLLBACK, так что не может зависнуть на соединении, которое пулер отдаёт другому запросу", correct: true },
    { label: "pg_advisory_lock — сессионные блокировки надёжнее", misconception: "Сессионные блокировки привязаны к физическому соединению, которое transaction-режим пулера переназначает между запросами; блокировка может утечь на соединение другого запроса или никогда не освободиться. Это ровно небезопасный случай." },
    { label: "Любой работает одинаково под пулером", misconception: "Они критически различаются: сессионные блокировки переживают COMMIT и привязаны к соединению, так что transaction-режим пулера их ломает; xact-блокировки ограничены транзакцией, которую пулер сохраняет целой." },
    { label: "Никакой; advisory-блокировки вообще не работают с пулерами", misconception: "Транзакционные advisory-блокировки прекрасно работают за transaction-режимом пулера именно потому, что их время жизни совпадает с границей транзакции, которую пулер сохраняет." },
  ]}
/>

<Quiz
  id="04-advisory-locks-quiz-2"
  lessonSlug="04-advisory-locks"
  lang="ru"
  question="Пять реплик приложения каждая запускают одну и ту же ежечасную задачу очистки. Нужно, чтобы ровно одна выполнила её в час, остальные тихо пропустили. Лучший примитив?"
  choices={[
    { label: "pg_try_advisory_lock(key): каждая реплика пробует один ключ, та что получит true запускает задачу, четыре с false пропускают", correct: true },
    { label: "SELECT FOR UPDATE на выделенной строке-блокировке", misconception: "Может сработать, но привязывает мьютекс к реальной строке (которая должна существовать) и ломается под transaction-режимом пулера, если держится между операторами; advisory-блокировка — специально созданный, без-строчный примитив для этого." },
    { label: "pg_advisory_lock(key) — блокирующая форма", misconception: "Блокирующая форма заставляет остальные четыре реплики ЖДАТЬ блокировку и затем тоже выполнить задачу, как только она освободится — ты запустишь очистку пять раз подряд, а не один. Нужна неблокирующая форма try_, чтобы проигравшие пропускали." },
    { label: "Unique-ограничение на таблице 'job_runs'", misconception: "Unique-вставка может дедупнуть запись, но не даёт удерживаемого мьютекса вокруг самой работы; конкурентные раннеры всё ещё могут выполнить тело до коммита любой вставки. Advisory-блокировка охраняет всю критическую секцию." },
  ]}
/>

<MetaphorComplete
  id="04-advisory-locks-metaphor-1"
  lessonSlug="04-advisory-locks"
  lang="ru"
  setup="Заполни пропуск: сессионная advisory-блокировка привязана к _______ с БД, так что под transaction-режимом пулера она может зависнуть на том, что позже переиспользует другой запрос — поэтому транзакционный вариант безопаснее по умолчанию."
  accepted={["соединению", "соединение", "сессии", "backend-соединению", "connection"]}
  canonical="соединению"
  explanation="Сессионная advisory-блокировка живёт с физическим backend-соединением и переживает COMMIT. Transaction-режим пулера переназначает это соединение между логическими запросами, так что блокировка может утечь на другой запрос или не освободиться. pg_advisory_xact_lock привязывает блокировку к транзакции, которую пулер сохраняет целой, убирая ловушку."
/>

</Explanation>

<KeyTakeaway>Advisory-блокировка — мьютекс уровня приложения по целочисленному ключу, не прикреплённый к строке — идеален для leader election, дедупа cron и сериализации логической операции. Решающий выбор — область: `pg_advisory_xact_lock` освобождается в конце транзакции и безопасен за transaction-режимом пулера, тогда как `pg_advisory_lock` (сессионный) привязан к соединению, переживает `COMMIT` и тихо течёт через пулеры вроде PgBouncer. Предпочитай xact-форму, используй `pg_try_*`, чтобы проигравшие пропускали, и давай ключам неймспейс, чтобы избежать коллизий.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="04-advisory-locks-retrieval"
  lang="ru"
  questions={[
    { q: "Чем advisory-блокировка отличается от блокировки строки вроде SELECT FOR UPDATE?", a: "Блокировка строки защищает конкретный существующий кортеж и берётся чтением этой строки; advisory-блокировка защищает абстрактный 64-битный ключ, которому ты задаёшь смысл и который может не соответствовать никакой строке. Postgres лишь гарантирует одного держателя на ключ по всему кластеру. Используй блокировки строк, когда редактируешь реальные данные; advisory-блокировки — для координации над логическим ресурсом (leader election, дедуп cron, сериализация пути кода), где нет одной строки для блокировки." },
    { q: "В чём разница между pg_advisory_lock и pg_advisory_xact_lock и какая безопасна с пулером?", a: "pg_advisory_lock сессионная: держится, пока ты не вызовешь pg_advisory_unlock или не закончится соединение, и НЕ освобождается на COMMIT. pg_advisory_xact_lock транзакционная: освобождается автоматически на COMMIT или ROLLBACK и не может утечь. Xact-форма безопасна с пулером, потому что её время жизни совпадает с границей транзакции, которую сохраняет transaction-режим пулера; сессионная форма привязана к физическому соединению, переназначаемому пулером между запросами, так что может зависнуть или утечь." },
    { q: "Назови два специфичных для advisory-блокировок режима отказа и как их избежать.", a: "(1) Утечка через пул: сессионная блокировка привязана к backend-соединению, так что под transaction-режимом пулера (PgBouncer transaction mode, RDS Proxy) она может держаться на соединении, переиспользуемом другим запросом, или никогда не освободиться — избегай через pg_advisory_xact_lock либо не запуская этот путь за transaction-режимом пулера. (2) Коллизии пространства ключей: две несвязанные фичи, хэширующие имена в один 64-битный ключ, блокируют друг друга без видимой причины — избегай неймспейсом ключей, например формой из двух int (classid, objid) с разным classid на фичу." },
  ]}
/>

<Recap lang="ru">
Advisory-блокировка — мьютекс, который Postgres держит над определённым тобой целочисленным ключом, отвязанный от любой строки — база становится распределённым менеджером блокировок, которым ты уже управляешь. Блокирующий `pg_advisory_lock` и неблокирующий `pg_try_advisory_lock` покрывают «ждать её» против «пропустить, если занята»; для singleton'ов на весь флот (leader election, дедуп cron) форма `try_` даёт ровно одной реплике выиграть, а остальным уйти. Выбор, решающий продакшн-надёжность, — область: сессионные блокировки переживают `COMMIT` и привязаны к физическому соединению, так что transaction-режим пулера может их зависить или утечь; транзакционная `pg_advisory_xact_lock` освобождается в конце транзакции и безопасна с пулером, что делает её верным умолчанием. Помимо ловушки пулера, давай ключам неймспейс, чтобы две фичи не столкнулись на одном целом, и тянись к advisory-блокировкам только для координации — блокировки строк остаются инструментом для охраны данных, которые ты реально редактируешь. Дальше — отказ, возникающий, когда блокировки любого вида берутся в конфликтующем порядке: deadlock'и. Теперь, когда увидишь cron, отработавший трижды, или сессионную блокировку, непонятно удержавшуюся дольше своей транзакции, — проверь, стоит ли код за transaction-режимом пулера, и переключись на `pg_advisory_xact_lock`.
</Recap>
