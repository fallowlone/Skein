---
slug: "05-deadlocks"
lang: ru
track: "sql-postgres"
unit: "07-transactions-concurrency"
order: 5
title: "Deadlock'и"
summary: "Deadlock — это цикл блокировок: две транзакции держат строку, нужную другой. Postgres обнаруживает цикл, убивает жертву ошибкой 40P01, и ты повторяешь — но консистентный порядок блокировок предотвращает его полностью."
estMin: 15
status: ready
lessonType: topic
level: senior
concepts:
  - deadlocks
  - lock-ordering
  - deadlock-timeout
  - error-40P01
  - retry
prereqs:
  - 04-advisory-locks
sources:
  - https://www.postgresql.org/docs/current/explicit-locking.html
  - https://www.postgresql.org/docs/current/runtime-config-locks.html
  - https://www.interdb.jp/pg/pgsql05.html
---

import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import SequenceDiagram from "~/components/diagram/SequenceDiagram.astro";

<Hook>
Платёжный сервис ловил deadlock примерно сорок раз в час на пике — каждый это транзакция, убитая на лету с `ERROR: deadlock detected`. Переводы по отдельности были корректны; баг был в том, что `transfer(A, B)` блокировал счёт A, затем B, а `transfer(B, A)` блокировал B, затем A. Когда они шли одновременно, каждый хватал по одной строке и затем вечно ждал чужую. Postgres ломал тупик, расстреливая одну транзакцию. Постоянный фикс изменил одну строку: всегда блокировать меньший id счёта первым. С тех пор ноль deadlock'ов.
</Hook>

<Crux>Deadlock — это цикл блокировок: две транзакции держат то, что нужно другой. Postgres убивает одну жертву; ты её повторяешь.</Crux>

<Explanation>

## Анатомия deadlock'а

Когда видишь в продакшн-логах `ERROR: deadlock detected`, инстинкт — подкрутить `deadlock_timeout` или добавить retry и двигаться дальше. Сопротивляйся. Каждый deadlock указывает на инверсию порядка, которую однострочный фикс устраняет навсегда.

Deadlock'у нужен ровно один ингредиент: две транзакции, захватывающие одни блокировки в **противоположном порядке**. Каждая берёт первую блокировку, затем просит вторую — которую уже держит другая — и ни одна не отпустит, пока не получит то, на чём заблокирована. Это цикл в графе ожиданий, и никакое ожидание его не разрешит.

```sql
-- Транзакция 1                            -- Транзакция 2
BEGIN;                                     BEGIN;
UPDATE accounts SET balance = balance - 100
  WHERE id = 1;       -- блокирует строку 1
                                           UPDATE accounts SET balance = balance - 50
                                             WHERE id = 2;     -- блокирует строку 2
UPDATE accounts SET balance = balance + 100
  WHERE id = 2;       -- ЖДЁТ строку 2 (держит Tx2)
                                           UPDATE accounts SET balance = balance + 50
                                             WHERE id = 1;     -- ЖДЁТ строку 1 (держит Tx1)
-- теперь обе ждут друг друга вечно -> deadlock
```

<SequenceDiagram
  label="Транзакция один блокирует строку 1, транзакция два блокирует строку 2, затем каждая просит строку, которую держит другая, образуя цикл, который Postgres обнаруживает и ломает абортом одной жертвы."
  hue="var(--d-data)"
  actors={["Tx 1", "Postgres", "Tx 2"]}
  messages={[
    { from: "Tx 1", to: "Postgres", label: "lock row 1 (ok)" },
    { from: "Tx 2", to: "Postgres", label: "lock row 2 (ok)" },
    { from: "Tx 1", to: "Postgres", label: "want row 2 -> wait" },
    { from: "Tx 2", to: "Postgres", label: "want row 1 -> wait" },
    { from: "Postgres", to: "Tx 2", label: "40P01: deadlock, abort" },
    { from: "Tx 1", to: "Postgres", label: "row 1 свободна -> идёт" },
  ]}
  caption="В графе ожиданий цикл: Tx1 ждёт Tx2, а Tx2 ждёт Tx1. Детектор deadlock'ов Postgres находит его и прерывает одну транзакцию (жертву), чтобы другая завершилась. Ничто не висит вечно."
/>

## Как Postgres его обнаруживает и разрешает

Postgres не проверяет deadlock на каждом ожидании блокировки — это было бы дорого. Вместо этого, когда транзакция блокируется на блокировке, она ждёт `deadlock_timeout` (по умолчанию **1 секунда**) и только затем запускает детектор deadlock'ов, который обходит граф ожиданий в поисках цикла. Найдя его, он выбирает **жертву** и прерывает её:

```
ERROR:  deadlock detected
DETAIL:  Process 1234 waits for ShareLock on transaction 5678; blocked by process 4321.
         Process 4321 waits for ShareLock on transaction 8765; blocked by process 1234.
HINT:    See server log for query details.
SQLSTATE: 40P01
```

Транзакция жертвы откатывается целиком; выживший идёт дальше. `deadlock_timeout` в 1 секунду означает, что реальный deadlock стоит как минимум ~1с задержки до разрешения, так что частые deadlock'и — и запах корректности, и проблема задержки. Снижение timeout ускоряет обнаружение, но добавляет CPU на каждом обычном ожидании блокировки — не крути его, пока не починишь порядок.

## Профилактика лучше обнаружения

Обнаружение — страховочная сетка, а не стратегия. Лекарство — **консистентный порядок блокировок**: если каждая транзакция, трогающая несколько строк, всегда блокирует их в одном глобальном порядке, цикл невозможен.

```sql
-- Перевод без deadlock: блокируем обе строки одним оператором, меньший id первым.
BEGIN;
SELECT id FROM accounts
WHERE id IN (:from_id, :to_id)
ORDER BY id           -- магия: детерминированный порядок, оба направления
FOR UPDATE;
UPDATE accounts SET balance = balance - :amt WHERE id = :from_id;
UPDATE accounts SET balance = balance + :amt WHERE id = :to_id;
COMMIT;
```

Поскольку `transfer(1, 2)` и `transfer(2, 1)` теперь оба блокируют id 1 раньше id 2, один полностью захватывает до старта другого — цикла нет. Остальные рычаги по приоритету: держи транзакции **короткими** (меньше времени с блокировками = меньше окно столкновений), трогай строки в стабильном порядке по всему приложению и лишь затем рассматривай `NOWAIT`, чтобы падать быстро, а не выжидать timeout.

<Inset kind="edgecase" lang="ru">
Deadlock'и — не только твои явные `UPDATE`. Foreign key тоже берут блокировки: вставка дочерней строки блокирует родителя `FOR KEY SHARE`, и две транзакции, вставляющие детей двух родителей и заодно обновляющие этих родителей, могут словить deadlock неочевидно. Конкуренция по индексам и unique-ограничениям тоже может породить циклы, когда конкурентные вставки трогают одни страницы индекса или конфликтуют на одном ключе. Очередь `jobs` из прошлого урока — рабочий пример: воркеры с `FOR UPDATE SKIP LOCKED` никогда не словят deadlock друг на друге, потому что никто не ждёт — они пропускают — ещё одна причина, почему `SKIP LOCKED` верный примитив очереди. Когда deadlock всё же случился, читай `DETAIL` в логе сервера: он называет два процесса и блокировки в цикле, чего обычно достаточно, чтобы заметить инверсию порядка. Всегда заставляй вызывающего жертвы **повторять** всю транзакцию с ограниченным backoff; повторённый перевод заново блокирует в верном порядке и проходит.
</Inset>

<Quiz
  id="05-deadlocks-quiz-1"
  lessonSlug="05-deadlocks"
  lang="ru"
  question="Две транзакции перевода, transfer(1,2) и transfer(2,1), идут одновременно, и каждая блокирует свой исходный счёт первым. Что произойдёт и каков надёжный фикс?"
  choices={[
    { label: "Они словят deadlock (каждая держит одну строку, ждёт другую); Postgres прерывает одну с 40P01. Фикс — консистентный порядок блокировок, всегда блокировать меньший id первым", correct: true },
    { label: "Они кратко блокируются, затем обе проходят; фикс не нужен", misconception: "Нет порядка, в котором обе могут пройти — каждая держит строку, нужную другой, настоящий цикл. Они не проходят обе; одну прерывают с 40P01." },
    { label: "Postgres сериализует их автоматически, так что deadlock'и невозможны с блокировками строк", misconception: "Postgres не переупорядочивает твои захваты блокировок; блокировка одних строк в противоположном порядке и есть то, что создаёт цикл. Ты должен сам навязать консистентный порядок." },
    { label: "Вторая транзакция ждёт вечно, пока ты не убьёшь её вручную", misconception: "Postgres обнаруживает цикл после deadlock_timeout (~1с) и прерывает жертву автоматически; он не висит бесконечно." },
  ]}
/>

<Quiz
  id="05-deadlocks-quiz-2"
  lessonSlug="05-deadlocks"
  lang="ru"
  question="Твоё приложение поймало SQLSTATE 40P01 (deadlock). Какова верная реакция в коде приложения?"
  choices={[
    { label: "Повторить всю транзакцию (с ограниченным экспоненциальным backoff); на повторе она заново захватывает блокировки по порядку и обычно проходит", correct: true },
    { label: "Снизить deadlock_timeout до 0, чтобы deadlock'и никогда не случались", misconception: "deadlock_timeout управляет задержкой обнаружения, а не тем, случаются ли deadlock'и, и 0 невалиден; снижение лишь жжёт CPU на каждом ожидании блокировки. Цикл это не предотвращает." },
    { label: "Игнорировать ошибку; данные в порядке, ведь выживший закоммитил", misconception: "Транзакция жертвы откатана целиком — её работа НЕ произошла. Игнор 40P01 молча теряет эту единицу работы; её надо повторить." },
    { label: "Закоммитить транзакцию жертвы заново с места падения", misconception: "Deadlock прерывает всю транзакцию; нет частичного состояния для продолжения. Надо перезапустить с BEGIN, а не возобновить с середины." },
  ]}
/>

<DragOrder
  id="05-deadlocks-drag-1"
  lessonSlug="05-deadlocks"
  lang="ru"
  prompt="Упорядочи жизненный цикл deadlock'а от образования к разрешению и постоянному фиксу:"
  items={[
    { id: "a", label: "Две транзакции блокируют одни строки в противоположном порядке, каждая держит одну и хочет другую" },
    { id: "b", label: "Заблокированная транзакция выжидает deadlock_timeout (по умолчанию ~1с)" },
    { id: "c", label: "Детектор deadlock'ов находит цикл и прерывает одну жертву с SQLSTATE 40P01" },
    { id: "d", label: "Вызывающий жертвы повторяет всю транзакцию с backoff" },
    { id: "e", label: "Ты меняешь код, чтобы блокировать строки в консистентном порядке (например, ORDER BY id), чтобы цикл больше не мог образоваться" },
  ]}
  correctOrder={["a", "b", "c", "d", "e"]} />

</Explanation>

<KeyTakeaway>Deadlock — цикл в графе ожиданий: две транзакции каждая держат блокировку, нужную другой, так что ни одна не может пройти. Postgres ждёт `deadlock_timeout` (~1с), запускает детектор и прерывает одну жертву с `ERROR: deadlock detected` (SQLSTATE 40P01); выживший завершается. Работа жертвы откатана целиком, так что приложение **обязано** повторить всю транзакцию с ограниченным backoff. Настоящий фикс — профилактика: блокируй несколько строк в консистентном глобальном порядке (например, меньший id первым) и держи транзакции короткими, чтобы цикл никогда не образовался.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="05-deadlocks-retrieval"
  lang="ru"
  questions={[
    { q: "Что именно такое deadlock и какое единственное условие вызывает классический случай двух строк?", a: "Deadlock — цикл в графе ожиданий: транзакция A держит блокировку, нужную B, пока B держит блокировку, нужную A, так что ни одна не двигается. Классический случай двух строк вызван несогласованным порядком блокировок — две транзакции захватывают одни две строки в противоположном порядке (Tx1 блокирует строку 1, затем 2, Tx2 блокирует строку 2, затем 1). Каждая хватает свою первую строку, затем вечно блокируется на второй, образуя цикл." },
    { q: "Как Postgres обнаруживает и разрешает deadlock и какова цена?", a: "Когда транзакция блокируется на блокировке, Postgres ждёт deadlock_timeout (по умолчанию 1 секунда) перед запуском детектора deadlock'ов, который обходит граф ожиданий в поисках цикла. Найдя его, он прерывает транзакцию-жертву с ERROR: deadlock detected (SQLSTATE 40P01) и пускает выжившего дальше. Цена — ~1с задержки, которую несёт каждый реальный deadlock до разрешения, плюс полностью откатанная работа жертвы — поэтому частые deadlock'и это и проблема задержки, и проблема корректности." },
    { q: "Каков надёжный фикс deadlock'ов и что код приложения всё равно обязан делать?", a: "Надёжный фикс — профилактика через консистентный порядок блокировок: всегда блокируй несколько строк в одном глобальном порядке (например, SELECT ... WHERE id IN (a,b) ORDER BY id FOR UPDATE, чтобы оба направления блокировали меньший id первым) и держи транзакции короткими, чтобы сжать окно столкновений. Даже так код приложения обязан ловить SQLSTATE 40P01 и повторять всю транзакцию с ограниченным экспоненциальным backoff, потому что работа жертвы откатана; повтор заново захватывает блокировки по порядку и обычно проходит." },
  ]}
/>

<Recap lang="ru">
Deadlock — единственный отказ конкурентности, который Postgres разрешает *за* тебя — убивая транзакцию. Он образуется, когда две транзакции захватывают одни блокировки в противоположном порядке: каждая держит один ресурс и вечно блокируется на другом, цикл в графе ожиданий. Postgres не опрашивает это на каждом ожидании; заблокированная транзакция сидит `deadlock_timeout` (по умолчанию ~1 секунда), затем запускается детектор, находит цикл и прерывает **жертву** с `ERROR: deadlock detected`, SQLSTATE **40P01**, откатывая её работу целиком, пока выживший коммитится. Поскольку транзакция жертвы исчезла, приложение обязано **повторить** её с ограниченным backoff. Но повтор — пластырь; лекарство — профилактика. Блокируй несколько строк в консистентном глобальном порядке — каноничный трюк `ORDER BY id ... FOR UPDATE`, чтобы каждое направление перевода блокировало меньший id первым — держи транзакции короткими, чтобы минимизировать окно, и помни, что конкуренция по foreign key и индексам тоже может дать deadlock. Очередь `FOR UPDATE SKIP LOCKED` из прошлого урока обходит deadlock'и, никогда не ожидая. Читай `DETAIL` в логе, чтобы найти инвертированный порядок, исправь порядок раз — и deadlock'и прекращаются.
</Recap>
