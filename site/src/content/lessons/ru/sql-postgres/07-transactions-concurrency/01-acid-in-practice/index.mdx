---
slug: "01-acid-in-practice"
lang: ru
track: "sql-postgres"
unit: "07-transactions-concurrency"
order: 1
title: "ACID на практике"
summary: "Транзакция — это забор: BEGIN…COMMIT делает группу записей всё-или-ничего, а SAVEPOINT режет внутри точки отката. Но ACID — это не управление конкурентностью: по умолчанию оно не сериализует."
estMin: 14
status: ready
lessonType: topic
level: middle
concepts:
  - transactions
  - atomicity
  - savepoint
  - rollback
  - durability
prereqs:
  - 00-start-here/02-how-postgres-executes-a-query
sources:
  - https://www.postgresql.org/docs/current/tutorial-transactions.html
  - https://www.postgresql.org/docs/current/sql-savepoint.html
  - https://www.postgresql.org/docs/current/transaction-iso.html
---

import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import SequenceDiagram from "~/components/diagram/SequenceDiagram.astro";

<Hook>
Платёжный сервис списывает с одного счёта и зачисляет на другой двумя `UPDATE`. Однажды ночью процесс падает между ними: с 1 200 клиентов списали, но не зачислили, и деньги просто исчезли из реестра. Фикс — три слова, которые автор забыл: `BEGIN` и `COMMIT`. Транзакция сделала бы так, что обе записи лягут вместе или никак. ACID не академичен; это разница между реестром и слухом.
</Hook>

<Crux>Транзакция делает группу записей всё-или-ничего и долговечной при commit — но сама по себе она не сериализует конкурентные транзакции.</Crux>

<Explanation>

## Что на самом деле даёт каждая буква

ACID — это четыре гарантии, и инженеры путают ту, которой не получают, с теми, что получают.

- **Atomicity (атомарность)** — каждый оператор между `BEGIN` и `COMMIT` ложится вместе, или никакой. Любая ошибка или `ROLLBACK` выбрасывает весь пакет. Полуготовое состояние никогда не видно и не сохраняется.
- **Consistency (согласованность)** — закоммиченная транзакция оставляет базу удовлетворяющей всем ограничениям (foreign key, `CHECK`, unique, `NOT NULL`). Если оператор нарушит ограничение, транзакция падает с ошибкой и нужен откат. Postgres обеспечивает *правила*; *твои* инварианты («баланс не уходит в минус») ты кодируешь как ограничения, иначе они не гарантированы.
- **Isolation (изоляция)** — конкурентные транзакции не видят незакоммиченной работы друг друга. Важно: у изоляции есть *уровни*: по умолчанию `READ COMMITTED` — это слабое обещание, а не полная сериализуемость. Именно эту букву люди считают сильнее, чем она есть.
- **Durability (долговечность)** — как только `COMMIT` вернулся, изменение переживёт крах или потерю питания. Postgres гарантирует это, сбрасывая write-ahead log (WAL) на диск до подтверждения коммита.

```sql
-- Атомарный перевод денег: обе строки двигаются, или ни одна.
BEGIN;
UPDATE accounts SET balance = balance - 100 WHERE id = 1;
UPDATE accounts SET balance = balance + 100 WHERE id = 2;
COMMIT;   -- только теперь перевод долговечен и виден другим
```

Если второй `UPDATE` упадёт (скажем, счёт 2 удалён), транзакция переходит в aborted-состояние; первый `UPDATE` отбрасывается при `ROLLBACK`. Деньги не утекают.

## BEGIN, COMMIT, ROLLBACK и неявная транзакция

Каждый оператор уже *внутри* транзакции. Вне явного `BEGIN` Postgres оборачивает каждый одиночный оператор в собственную транзакцию, которая коммитится автоматически (autocommit). Поэтому один `UPDATE accounts SET balance = balance * 1.05` сам по себе атомарен — все подходящие строки меняются или ни одна, даже без `BEGIN`.

`BEGIN` ты пишешь только когда нужно сделать **несколько** операторов одной атомарной единицей. После ошибки внутри транзакционного блока Postgres отвергает каждый следующий оператор с `current transaction is aborted`, пока ты не сделаешь `ROLLBACK` (или `COMMIT`, который Postgres превратит в откат). Никакого «проигнорировать ошибку и продолжить» — если только не используешь savepoint.

<SequenceDiagram
  label="Клиент открывает транзакцию, делает два списания, ловит ошибку на втором и откатывает всю единицу, так что первое списание тоже отброшено."
  hue="var(--d-data)"
  actors={["Client", "Postgres"]}
  messages={[
    { from: "Client", to: "Postgres", label: "BEGIN" },
    { from: "Client", to: "Postgres", label: "UPDATE accounts (id=1)" },
    { from: "Postgres", to: "Client", label: "ok" },
    { from: "Client", to: "Postgres", label: "UPDATE id=2 -> ERROR" },
    { from: "Postgres", to: "Client", label: "txn aborted" },
    { from: "Client", to: "Postgres", label: "ROLLBACK" },
    { from: "Postgres", to: "Client", label: "всё отброшено" },
  ]}
  caption="Атомарность в движении: как только любой оператор падает, транзакция отравлена. ROLLBACK выбрасывает всё с момента BEGIN — включая списание id=1 — и баланс книг сходится."
/>

## SAVEPOINT: частичный откат внутри одной транзакции

`SAVEPOINT` — именованная метка, к которой можно откатиться, не бросая всю транзакцию. Она позволяет одной транзакции пережить восстановимую ошибку в части своей работы.

```sql
BEGIN;
INSERT INTO jobs (id, status) VALUES (501, 'queued');

SAVEPOINT try_dedupe;
-- это может нарушить unique-индекс, если задача уже есть:
INSERT INTO jobs (id, status) VALUES (501, 'queued');
-- падает; вместо потери всей транзакции откатываем только до savepoint:
ROLLBACK TO SAVEPOINT try_dedupe;

-- первый INSERT всё ещё жив; продолжаем и коммитим его:
COMMIT;
```

Именно это большинство драйверов делают под капотом для «вложенных транзакций» — в Postgres нет настоящих вложенных транзакций, только savepoint'ы. Цена, однако, реальна: каждый savepoint потребляет subtransaction ID, и транзакция, открывающая десятки тысяч их, может вызвать пресловутый **subtransaction (`subxid`) overflow** — как только backend превышает 64 живых подтранзакции, остальным приходится обращаться к `pg_subtrans` на диске, и задержка чтения на нагруженном primary может скакнуть с микросекунд до миллисекунд. Savepoint — скальпель, а не тело цикла.

<Inset kind="mistake" lang="ru">
Самое частое непонимание ACID: «я обернул это в транзакцию, значит конкурентные обновления безопасны». Нет. Два перевода, которые `SELECT` один и тот же баланс на уровне по умолчанию `READ COMMITTED`, могут оба прочитать 500, оба вычесть 100 и оба записать 400 — **lost update (потерянное обновление)** — хотя каждый идеально атомарен и долговечен. Атомарность — про *твои операторы как единицу*; она ничего не говорит про *чередование с другими сессиями*. Сериализовать конкурентный доступ — задача уровней изоляции и явных блокировок, следующих уроков этого раздела.
</Inset>

## Чего транзакция НЕ даёт

Транзакция по умолчанию — **не** мьютекс и **не** сериализуема. На `READ COMMITTED` (умолчание Postgres) каждый оператор видит свежий снимок, взятый в начале оператора, так что две конкурентные последовательности read-modify-write могут гоняться. Атомарность и долговечность ты получаешь бесплатно; защиту от lost update, write skew или фантомных строк ты **не** получаешь, пока не поднимешь уровень изоляции или не возьмёшь явные блокировки строк. Знание этой границы — вся причина, по которой существует остаток раздела.

<Quiz
  id="01-acid-in-practice-quiz-1"
  lessonSlug="01-acid-in-practice"
  lang="ru"
  question="Две конкурентные сессии каждая выполняет на уровне по умолчанию: BEGIN; UPDATE accounts SET balance = balance - 100 WHERE id = 1; COMMIT. Стартовый баланс 500. Что гарантировано?"
  choices={[
    { label: "Обе транзакции атомарны и долговечны, и Postgres сериализует два UPDATE на одной строке через блокировку строки, так что результат — корректные 300", correct: true },
    { label: "Одна транзакция молча перезатирает другую, и баланс заканчивается на 400 — потерянное обновление", misconception: "Слепой UPDATE, читающий и пишущий ту же строку одним оператором, берёт блокировку строки; второй ждёт коммита первого, перечитывает и снова вычитает. Ловушка lost update появляется при отдельном SELECT-затем-UPDATE, а не при одном самоссылающемся UPDATE." },
    { label: "Вторая транзакция падает с serialization failure", misconception: "Serialization failure случается только на REPEATABLE READ или SERIALIZABLE. READ COMMITTED разрешает конфликт блокировкой и перечитыванием, а не абортом." },
    { label: "Ничего не гарантировано; транзакции вообще не влияют на конкурентные записи", misconception: "Атомарность и долговечность гарантированы всегда, а одиночный UPDATE на одной строке сериализуется блокировкой строки. Негарантирована многооператорная read-modify-write, а не этот случай." },
  ]}
/>

<DragOrder
  id="01-acid-in-practice-drag-1"
  lessonSlug="01-acid-in-practice"
  lang="ru"
  prompt="Упорядочи жизненный цикл атомарного перевода в два шага, восстанавливающегося от ошибки дубль-вставки через savepoint:"
  items={[
    { id: "a", label: "BEGIN — открыть явную транзакцию, чтобы следующие операторы были одной единицей" },
    { id: "b", label: "Сделать первую запись (списать со счёта 1)" },
    { id: "c", label: "SAVEPOINT перед записью, которая может упасть" },
    { id: "d", label: "Попытаться рискованную запись; при ошибке ROLLBACK TO SAVEPOINT отменяет только этот шаг" },
    { id: "e", label: "COMMIT — сделать уцелевшие записи долговечными и видимыми" },
  ]}
  correctOrder={["a", "b", "c", "d", "e"]} />

</Explanation>

<KeyTakeaway>Транзакция оборачивает операторы в одну атомарную, долговечную единицу: `BEGIN`/`COMMIT` делают их всё-или-ничего, `ROLLBACK` их отбрасывает, а `SAVEPOINT` даёт частичный откат внутри одной транзакции. Но «I» в ACID — уровневая: умолчание `READ COMMITTED` не сериализует конкурентные транзакции, так что одна атомарность никогда не защищает от lost update. Этот зазор и закрывают блокировки и более высокие уровни изоляции.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="01-acid-in-practice-retrieval"
  lang="ru"
  questions={[
    { q: "Что гарантирует атомарность и к какой наименьшей единице она уже применяется без BEGIN?", a: "Атомарность гарантирует, что каждый оператор между BEGIN и COMMIT ложится вместе или никак; любая ошибка или ROLLBACK отбрасывает весь пакет. Она уже применяется к одиночному оператору: в autocommit каждый одиночный оператор — своя транзакция, поэтому один UPDATE, совпавший с миллионом строк, меняет все или ни одной даже без явного BEGIN. BEGIN пишешь только чтобы сделать несколько операторов одной единицей." },
    { q: "Чем SAVEPOINT отличается от ROLLBACK и какова его продакшн-цена на масштабе?", a: "ROLLBACK прерывает всю транзакцию обратно до BEGIN. ROLLBACK TO SAVEPOINT отменяет только работу с момента именованного savepoint, оставляя транзакцию живой, чтобы продолжить и всё же сделать COMMIT. Цена: каждый savepoint потребляет subtransaction ID; как только backend превышает 64 живых подтранзакции, он переливается в pg_subtrans на диске, и на нагруженном primary это может превратить микросекундные чтения в миллисекундные (subxid overflow). Используй savepoint'ы экономно, не в цикле." },
    { q: "Почему оборачивание read-modify-write в транзакцию НЕ предотвращает lost update по умолчанию?", a: "Потому что уровень по умолчанию READ COMMITTED даёт каждому оператору свежий снимок в начале оператора и не сериализует отдельные транзакции. Две сессии могут SELECT тот же баланс 500, каждая вычесть 100 и каждая UPDATE до 400 — обе атомарны, обе долговечны, но одно обновление потеряно. Атомарность связывает твои собственные операторы; она ничего не говорит про чередование с другими сессиями. Чтобы предотвратить lost update, нужна явная блокировка строки (SELECT FOR UPDATE) или более высокий уровень изоляции." },
  ]}
/>

<Recap lang="ru">
ACID — это четыре отдельных обещания, и сеньоры держат их раздельно. **Atomicity**: `BEGIN`…`COMMIT` делает группу записей всё-или-ничего; любая ошибка отравляет транзакцию до `ROLLBACK`, и даже одиночный autocommit-оператор атомарен. **Consistency**: коммит должен оставить все ограничения удовлетворёнными — Postgres обеспечивает объявленные правила, а бизнес-инварианты ты кодируешь как ограничения. **Isolation**: конкурентные транзакции прячут незакоммиченную работу, но изоляция *уровневая*, и умолчание `READ COMMITTED` — слабое обещание, которое не сериализует. **Durability**: как только `COMMIT` вернулся, WAL на диске и изменение переживёт крах. `SAVEPOINT` добавляет частичный откат внутри одной транзакции, с реальной subxid-ценой на масштабе. Единственное, чего транзакция **не** даёт по умолчанию, — защиты от конкурентных гонок read-modify-write; это требует уровней изоляции и явных блокировок, которые ты встретишь дальше, включая проблему lost update, ради которой существует `SELECT FOR UPDATE`.
</Recap>
