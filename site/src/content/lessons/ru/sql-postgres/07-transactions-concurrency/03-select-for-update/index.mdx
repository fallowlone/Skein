---
slug: "03-select-for-update"
lang: ru
track: "sql-postgres"
unit: "07-transactions-concurrency"
order: 3
title: "SELECT FOR UPDATE и очередь задач"
summary: "Пессимистичные блокировки строк чинят гонку lost update: FOR UPDATE блокирует прочитанные строки, делая следующую запись безопасной. FOR UPDATE SKIP LOCKED превращает таблицу в очередь задач без конкуренции за блокировки."
estMin: 16
status: ready
lessonType: topic
level: senior
concepts:
  - select-for-update
  - row-locks
  - lost-update
  - skip-locked
  - job-queue
prereqs:
  - 02-isolation-levels
sources:
  - https://www.postgresql.org/docs/current/explicit-locking.html
  - https://www.postgresql.org/docs/current/sql-select.html
  - https://www.interdb.jp/pg/pgsql05.html
---

import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import SequenceDiagram from "~/components/diagram/SequenceDiagram.astro";

<Hook>
Команда построила пул воркеров над таблицей `jobs`: каждый воркер выполнял `SELECT id FROM jobs WHERE status='queued' LIMIT 1`, затем помечал её `running`. С одним воркером всё было идеально. На десяти воркерах каждую задачу подхватывали трое-четверо — дубль-письма, тройные списания, всё подряд. Фикс — восемь символов: `FOR UPDATE SKIP LOCKED`. Эти же восемь символов превращают обычную таблицу в продакшн-очередь. Это самая полезная блокирующая клауза в Postgres.
</Hook>

<Crux>SELECT FOR UPDATE блокирует прочитанные строки, делая read-modify-write безопасным; SKIP LOCKED даёт конкурентную очередь задач.</Crux>

<Explanation>

## Гонка lost update и пессимистичный фикс

Умолчание `READ COMMITTED` не сериализует **read-then-write**, сделанный двумя операторами. Две сессии могут обе прочитать баланс, обе вычислить новое значение по устаревшему чтению и обе записать — вторая молча перезатирает первую. Это **lost update**.

```sql
-- Сессия 1 и Сессия 2 обе выполняют это, чередуясь:
SELECT balance FROM accounts WHERE id = 1;   -- обе читают 500
-- приложение вычисляет 500 - 100 = 400
UPDATE accounts SET balance = 400 WHERE id = 1;  -- обе пишут 400; одно списание исчезает
```

`SELECT ... FOR UPDATE` чинит это пессимистично: он берёт **блокировку записи уровня строки** на каждую возвращённую строку. Вторая транзакция, пытающаяся заблокировать ту же строку, **блокируется** до коммита первой, затем продолжает по уже актуальным данным.

```sql
BEGIN;
SELECT balance FROM accounts WHERE id = 1 FOR UPDATE;  -- блокирует строку
-- только эта транзакция держит её; другие ждут здесь
UPDATE accounts SET balance = balance - 100 WHERE id = 1;
COMMIT;  -- блокировка снимается; следующий ожидающий перечитывает свежий баланс
```

Блокировка живёт остаток транзакции и снимается на `COMMIT` или `ROLLBACK`. Цена — сериализация: спорные строки обрабатываются по одной транзакции за раз. В этом и смысл — корректность важнее пропускной способности на горячей строке.

<SequenceDiagram
  label="Две транзакции обе делают SELECT FOR UPDATE одной строки счёта; вторая блокируется до коммита первой, затем перечитывает свежий баланс, так что ни одно списание не теряется."
  hue="var(--d-data)"
  actors={["Tx A", "Postgres", "Tx B"]}
  messages={[
    { from: "Tx A", to: "Postgres", label: "SELECT id=1 FOR UPDATE" },
    { from: "Postgres", to: "Tx A", label: "строка занята: 500" },
    { from: "Tx B", to: "Postgres", label: "SELECT id=1 FOR UPDATE" },
    { from: "Postgres", to: "Tx B", label: "...блокируется..." },
    { from: "Tx A", to: "Postgres", label: "UPDATE -> 400; COMMIT" },
    { from: "Postgres", to: "Tx B", label: "разблок: теперь 400" },
  ]}
  caption="Блокировка загоняет две транзакции в последовательный порядок на этой строке. Tx B никогда не действует по устаревшему 500 — она ждёт, затем читает 400 и вычитает из верной базы. Нет lost update."
/>

## Четыре силы блокировок

В Postgres четыре режима блокировки строк, от сильнейшего к слабейшему. Выбор слабейшего корректного пропускает больше конкурентности.

- **FOR UPDATE** — полная блокировка записи. Блокирует любой другой `FOR UPDATE`/`FOR SHARE` и блокирует `UPDATE`/`DELETE` строки. Используй, когда будешь менять строку.
- **FOR NO KEY UPDATE** — чуть слабее; берётся автоматически обычным `UPDATE`, не трогающим ключевую колонку. Он **не** блокирует `FOR KEY SHARE`, который берут проверки foreign key — так что снижает FK-конкуренцию.
- **FOR SHARE** — разделяемая блокировка чтения. Несколько транзакций могут держать её; она блокирует писателей. Используй, чтобы закрепить читаемую, но не меняемую строку.
- **FOR KEY SHARE** — слабейшая; её берёт проверка существования по foreign key на родительской строке. Блокирует лишь изменения ключа строки.

Практический урок: `INSERT` в дочернюю таблицу берёт `FOR KEY SHARE` на родителя, а `UPDATE` неключевой колонки родителя берёт `FOR NO KEY UPDATE` — эти два сделали совместимыми, чтобы обычный FK-трафик не сериализовался. Тянись к простому `FOR UPDATE` только когда реально намерен обновить заблокированную строку.

## SKIP LOCKED: каноничная конкурентная очередь

`SKIP LOCKED` меняет поведение ожидания: вместо блокировки на уже занятой строке запрос **пропускает её** и возвращает следующую доступную. Это ровно то, что нужно очереди задач — каждый воркер хватает *другую* незанятую задачу.

```sql
-- Атомарный dequeue одного воркера: взять старейшую свободную задачу, пропуская занятые соседом.
BEGIN;
SELECT id FROM jobs
WHERE status = 'queued'
ORDER BY id
FOR UPDATE SKIP LOCKED
LIMIT 1;
-- пометить взятую строку, чтобы её не выбрали снова после коммита:
UPDATE jobs SET status = 'running', locked_at = now() WHERE id = :claimed_id;
COMMIT;
```

Десять воркеров, выполняющих это параллельно, каждый блокирует и берёт отдельную строку; нет дублей, нет блокировок, почти линейное масштабирование. Так работают Sidekiq-Pro, `pg-boss`, Que и большинство современных Postgres-очередей — без отдельного брокера, просто `FOR UPDATE SKIP LOCKED`.

<SequenceDiagram
  label="Два воркера делают dequeue одновременно; воркер A блокирует задачу 1, воркер B пропускает занятую задачу 1 и берёт задачу 2, так что каждый получает отдельную задачу без блокировок."
  hue="var(--d-data)"
  actors={["Worker A", "jobs", "Worker B"]}
  messages={[
    { from: "Worker A", to: "jobs", label: "FOR UPDATE SKIP LOCKED" },
    { from: "jobs", to: "Worker A", label: "берёт job 1" },
    { from: "Worker B", to: "jobs", label: "FOR UPDATE SKIP LOCKED" },
    { from: "jobs", to: "Worker B", label: "пропуск 1, берёт job 2" },
    { from: "Worker A", to: "jobs", label: "set running; COMMIT" },
    { from: "Worker B", to: "jobs", label: "set running; COMMIT" },
  ]}
  caption="SKIP LOCKED никогда не блокируется: он проходит мимо строк, занятых другими воркерами. ORDER BY + LIMIT 1 внутри блокирующего SELECT заставляет каждого воркера брать старейшую свободную задачу, так что пропускная способность растёт с числом воркеров."
/>

<Inset kind="edgecase" lang="ru">
Ещё две ручки. `NOWAIT` — противоположность `SKIP LOCKED`: вместо ожидания или пропуска он мгновенно падает с ошибкой (`55P03`), если любая целевая строка занята — полезно, когда «кто-то другой редактирует это» должно быть мгновенным отказом, а не ожиданием. И **консистентный порядок блокировок** — правило, предотвращающее deadlock (следующий урок): если каждая транзакция, блокирующая несколько строк, всегда блокирует их в одном порядке — например, всегда `ORDER BY id` перед `FOR UPDATE` — две транзакции никогда не смогут каждая держать строку, нужную другой. Перевод денег, блокирующий счета 1 и 2, должен блокировать меньший id первым в *обоих* направлениях перевода, иначе под нагрузкой словишь deadlock.
</Inset>

<Quiz
  id="03-select-for-update-quiz-1"
  lessonSlug="03-select-for-update"
  lang="ru"
  question="Воркер очереди использует SELECT ... FOR UPDATE (без SKIP LOCKED) с LIMIT 1, чтобы взять задачу. На десяти воркерах что произойдёт?"
  choices={[
    { label: "Девять воркеров блокируются в ожидании одной строки, занятой первым воркером, так что пул фактически сериализуется — пропускная способность падает до одной задачи за раз", correct: true },
    { label: "Каждый воркер берёт отдельную задачу, как со SKIP LOCKED", misconception: "Простой FOR UPDATE блокируется на занятой строке; он не пропускает к следующей свободной. Без SKIP LOCKED LIMIT 1 заставляет всех воркеров конкурировать за одну и ту же первую строку." },
    { label: "Postgres поднимает ошибку deadlock", misconception: "Deadlock'а нет — каждый воркер ждёт одного и того же держателя блокировки, это конкуренция/сериализация, а не цикл." },
    { label: "Каждый воркер получает ту же задачу и обрабатывает её десять раз", misconception: "Они блокируются, а не дублируют; ожидающие перечитывают после коммита держателя. Именно SKIP LOCKED даёт отдельные задачи, а не дубли." },
  ]}
/>

<Quiz
  id="03-select-for-update-quiz-2"
  lessonSlug="03-select-for-update"
  lang="ru"
  question="Нужно, чтобы «редактирование записи мгновенно падало, если другая сессия уже её редактирует» — без ожидания, без пропуска. Какая клауза?"
  choices={[
    { label: "FOR UPDATE NOWAIT — он мгновенно падает с 55P03, если любая целевая строка уже занята", correct: true },
    { label: "FOR UPDATE SKIP LOCKED", misconception: "SKIP LOCKED молча убирает занятую строку из результата, так что ты получишь пустой результат, а не ошибку — неверный сигнал для «кто-то редактирует это»." },
    { label: "Простой FOR UPDATE", misconception: "Простой FOR UPDATE ждёт блокировку неограниченно; ты просил мгновенный отказ, а не ожидание." },
    { label: "FOR SHARE", misconception: "FOR SHARE — разделяемая блокировка чтения, пускающая других читателей и блокирующая лишь писателей; он ни падает быстро, ни гарантирует эксклюзивность для редактирования." },
  ]}
/>

<DragOrder
  id="03-select-for-update-drag-1"
  lessonSlug="03-select-for-update"
  lang="ru"
  prompt="Упорядочи атомарный, без-lost-update dequeue одного воркера из таблицы jobs, разделяемой десятью воркерами:"
  items={[
    { id: "a", label: "BEGIN транзакцию, чтобы взятие и обновление статуса были одной единицей" },
    { id: "b", label: "SELECT старейшую queued-задачу с ORDER BY id ... FOR UPDATE SKIP LOCKED LIMIT 1" },
    { id: "c", label: "UPDATE этой строки на status='running', locked_at=now()" },
    { id: "d", label: "COMMIT, чтобы снять блокировку и сделать взятие долговечным" },
  ]}
  correctOrder={["a", "b", "c", "d"]} />

</Explanation>

<KeyTakeaway>`SELECT ... FOR UPDATE` берёт блокировку записи уровня строки на каждую возвращённую строку, так что двухоператорный read-then-write не может потерять обновление — конкурентные блокировщики ждут до твоего коммита. Добавь `SKIP LOCKED`, и запрос шагает мимо занятых строк вместо ожидания — каноничный способ построить конкурентную очередь задач без конкуренции на обычной таблице. Бери слабейшую корректную силу блокировки, блокируй несколько строк в консистентном порядке и выбирай `NOWAIT`, когда «уже занято» должно падать быстро.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="03-select-for-update-retrieval"
  lang="ru"
  questions={[
    { q: "Что такое проблема lost update и как SELECT FOR UPDATE её предотвращает?", a: "Lost update происходит, когда две транзакции каждая читают значение (например, баланс 500), вычисляют новое значение из этого устаревшего чтения и обе пишут — вторая перезатирает первую, и одно изменение теряется. Это проскакивает через READ COMMITTED, потому что отдельный SELECT, затем UPDATE не сериализуются. SELECT ... FOR UPDATE предотвращает это, беря блокировку записи строки во время чтения: вторая транзакция блокируется до коммита первой, затем перечитывает уже актуальное значение перед записью, так что ни одно обновление не теряется." },
    { q: "Как FOR UPDATE SKIP LOCKED строит конкурентную очередь задач и как выглядит запрос dequeue?", a: "SKIP LOCKED заставляет блокирующий SELECT пропускать строки, которые уже держат другие транзакции, вместо блокировки на них, так что каждый воркер берёт отдельную свободную строку. Dequeue: BEGIN; SELECT id FROM jobs WHERE status='queued' ORDER BY id FOR UPDATE SKIP LOCKED LIMIT 1; UPDATE jobs SET status='running', locked_at=now() WHERE id=<взятый>; COMMIT. Десять воркеров выполняют это параллельно, каждый берёт старейшую незанятую задачу, без дублей и с почти линейным масштабированием — основа pg-boss, Que и подобных очередей." },
    { q: "Когда использовать NOWAIT вместо SKIP LOCKED или простой блокировки и чем они различаются на занятой строке?", a: "На строке, которую уже держит другая сессия: простой FOR UPDATE ЖДЁТ освобождения блокировки; SKIP LOCKED молча ПРОПУСКАЕТ эту строку из результата и возвращает другие свободные строки; NOWAIT мгновенно ПАДАЕТ с SQLSTATE 55P03. Используй NOWAIT, когда конкуренция должна быть мгновенным явным отказом — например, интерактивное редактирование, где хочешь сказать пользователю «кто-то другой редактирует это», а не зависнуть или молча вернуть ничего." },
  ]}
/>

<Recap lang="ru">
Уровень изоляции по умолчанию оставляет двухоператорный read-then-write открытым для **lost update**: две сессии читают одно значение, вычисляют по устаревшему чтению и обе пишут, теряя одно изменение. `SELECT ... FOR UPDATE` закрывает это пессимистично, беря блокировку записи строки во время чтения; конкурирующий блокировщик ждёт до твоего коммита, затем работает по свежим данным, так что горячая строка обрабатывается по одной транзакции за раз. Postgres предлагает четыре силы блокировок — `FOR UPDATE`, `FOR NO KEY UPDATE`, `FOR SHARE`, `FOR KEY SHARE` — и ты берёшь слабейшую корректную, чтобы обычный FK-трафик не сериализовался. Звёздный паттерн — `FOR UPDATE SKIP LOCKED`: он шагает мимо строк, занятых другими воркерами, так что обычная таблица `jobs` становится конкурентной очередью без конкуренции, где каждый воркер берёт отдельную строку через `ORDER BY id ... LIMIT 1`. Дополни это `NOWAIT` для быстрого отказа при редактировании и консистентным порядком блокировок, чтобы избежать deadlock'ов, которые изучишь дальше.
</Recap>
