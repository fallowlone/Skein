---
slug: "01-join-as-product"
lang: ru
track: "sql-postgres"
unit: "02-joins-deep"
order: 1
title: "JOIN — это отфильтрованное произведение"
summary: "Любой JOIN начинается как декартово произведение двух таблиц — каждая строка A в паре с каждой строкой B — а предикат ON это просто фильтр на этом произведении. Усвой это, и поведение JOIN перестанет быть магией."
estMin: 14
status: ready
lessonType: topic
level: middle
concepts:
  - cartesian-product
  - cross-join
  - join-predicate
  - row-cardinality
  - set-combination
prereqs:
  - 00-start-here/02-how-postgres-executes-a-query
sources:
  - https://www.postgresql.org/docs/current/queries-table-expressions.html
  - https://www.postgresql.org/docs/current/tutorial-join.html
  - https://use-the-index-luke.com/sql/join
---

import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import MetaphorComplete from "~/components/pedagogy/MetaphorComplete.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
Отчётный запрос, прекрасно работавший в staging, вернул в проде 4,2 миллиарда строк и держал CPU на пределе девять минут, пока его не убили. Причина — одно пропущенное слово в `ON`. Автор думал о JOIN как о «сопоставлении строк». Движок думает о нём куда грубее: спарь *всё* со *всем*, а потом выбрось то, что не совпало. Стоит увидеть JOIN глазами движка — и тот девятиминутный инцидент становится очевиден заранее.
</Hook>

<Crux>Любой JOIN — это отфильтрованное декартово произведение: спарь каждую строку A с каждой строкой B, оставь лишь пары, принятые ON.</Crux>

<Explanation>

## Декартово произведение — это фундамент

На минуту забудь про «сопоставление». Базовейшая комбинация двух таблиц — это **декартово произведение** (`CROSS JOIN`): взять каждую строку `users` и спарить её с каждой строкой `orders`. Если в `users` 3 строки, а в `orders` 4, то в произведении ровно 3 × 4 = 12 строк. Никакого условия, никакого сопоставления — просто все возможные пары.

```sql
-- Сырое произведение: каждый пользователь с каждым заказом
SELECT u.id AS user_id, o.id AS order_id
FROM users u
CROSS JOIN orders o;
-- 3 пользователя × 4 заказа = 12 строк, неважно кто что заказал
```

Число строк произведения — `|A| × |B|`, оно *умножается*. Этот единственный факт и есть причина, по которой JOIN может взорваться. У двух таблиц по 100k строк произведение — 10 миллиардов строк. Движок никогда не материализует их все, если может избежать, но *логический* смысл твоего запроса всё равно — «начать с этого произведения».

<FlowDiagram
  label="Сетка 3 на 4, где каждая строка A спарена с каждой строкой B, давая 12 ячеек; затем предикат ON оставляет только совпавшие ячейки."
  perRow={4}
  hue="var(--d-data)"
  nodes={[
    { id: "a1b1", label: "u1 × o1", sub: "оставить" },
    { id: "a1b2", label: "u1 × o2", sub: "убрать" },
    { id: "a1b3", label: "u1 × o3", sub: "убрать" },
    { id: "a1b4", label: "u1 × o4", sub: "убрать" },
    { id: "a2b1", label: "u2 × o1", sub: "убрать" },
    { id: "a2b2", label: "u2 × o2", sub: "оставить" },
    { id: "a2b3", label: "u2 × o3", sub: "оставить" },
    { id: "a2b4", label: "u2 × o4", sub: "убрать" },
    { id: "a3b1", label: "u3 × o1", sub: "убрать" },
    { id: "a3b2", label: "u3 × o2", sub: "убрать" },
    { id: "a3b3", label: "u3 × o3", sub: "убрать" },
    { id: "a3b4", label: "u3 × o4", sub: "оставить" },
  ]}
  edges={[]}
  caption="Полное произведение 3×4 — это 12 ячеек. Предикат ON (здесь u.id = o.user_id) принимает только ячейки, где ключи совпадают — их 4. JOIN — это ровно эта сетка с применённым фильтром."
/>

## INNER JOIN — это то же произведение, отфильтрованное

`INNER JOIN ... ON <предикат>` *по определению* — это декартово произведение с предикатом, применённым как фильтр. Эти два запроса логически тождественны:

```sql
-- В виде JOIN (так ты будешь писать всегда)
SELECT u.id, o.id, o.total
FROM users u
JOIN orders o ON o.user_id = u.id;

-- В виде произведение + фильтр (вот что это ЗНАЧИТ)
SELECT u.id, o.id, o.total
FROM users u
CROSS JOIN orders o
WHERE o.user_id = u.id;
```

Оба спрашивают: из всех 12 возможных пар оставить те, где `o.user_id = u.id`. Клауза `ON` — не особый синтаксис, а фильтр на произведении. Планировщик никогда не строит буквально сетку из 12 строк (он применяет hash, merge или nested-loop стратегии, чтобы дёшево прийти к тому же ответу — это урок про join-algorithms трека `databases`), но *результат* гарантированно равен «произведение, затем фильтр».

Эта переформулировка окупается сразу. Число строк на выходе INNER JOIN — это **не** `|A|` и **не** `|B|`. Это «сколько пар пережили фильтр», и зависит оно целиком от отношения. Если у каждого заказа ровно один совпадающий пользователь, в результате будет `|orders|` строк. Если ключ join неуникален с обеих сторон, можно получить *больше* строк, чем в любой из таблиц — это fan-out, тема последнего урока этого раздела.

## Почему поведение CROSS JOIN — это фича, а не только грабли

Инцидент из хука был случайным произведением: кто-то написал JOIN, забыл предикат `ON` (или написал `ON true`) и получил `|A| × |B|`. SQL тебя не останавливает, потому что произведение иногда — ровно то, что нужно: календарная сетка, матрица комбинаций «каждый товар × каждый размер», заполнение пропусков. Четвёртый урок разбирает осознанные cross join. Дисциплина: клауза `ON`, которая на самом деле не ограничивает отношение, — это молча произведение, а произведение двух больших таблиц — это авария.

<Inset kind="why" lang="ru">
Зачем думать «произведение, затем фильтр», если планировщик произведение не строит? Потому что это делает кардинальность предсказуемой, а кардинальность управляет стоимостью. Когда ты можешь ответить «сколько строк выдаст этот JOIN?» до запуска, ты замечаешь fan-out, случайные cross join и пропущенные предикаты на глаз. Вся работа планировщика — *избежать* материализации произведения, возвращая ответ «произведение-затем-фильтр», но сам ответ определён произведением, поэтому и твоя модель должна начинаться оттуда. Глава execution-plans трека `databases` показывает, как nested-loop, hash и merge join приходят к этому ответу с разными профилями стоимости.
</Inset>

<Quiz
  id="01-join-as-product-quiz-1"
  lessonSlug="01-join-as-product"
  lang="ru"
  question="В users 5 строк, в orders 8 строк. Ты запускаешь SELECT * FROM users CROSS JOIN orders. Сколько строк вернётся?"
  choices={[
    { label: "40 — декартово произведение это |users| × |orders| = 5 × 8", correct: true },
    { label: "13 — строки складываются", misconception: "Произведение умножает, а не складывает. CROSS JOIN парит каждую левую строку с каждой правой, так что это 5 × 8 = 40, а не 5 + 8." },
    { label: "8 — по одной строке на заказ", misconception: "Это был бы результат join один-к-одному по ключу, а не cross join. Без предиката каждый пользователь парится с каждым заказом." },
    { label: "5 — по одной строке на пользователя", misconception: "Cross join не схлопывается к левой таблице; каждый пользователь спарен со всеми 8 заказами, давая 40 строк." },
  ]}
/>

<Quiz
  id="01-join-as-product-quiz-2"
  lessonSlug="01-join-as-product"
  lang="ru"
  question="Что логически эквивалентно: FROM users u JOIN orders o ON o.user_id = u.id ?"
  choices={[
    { label: "FROM users u CROSS JOIN orders o WHERE o.user_id = u.id — произведение, затем фильтр по предикату", correct: true },
    { label: "FROM users u, orders o без WHERE — это уже ограничивает совпадениями", misconception: "Запятая без WHERE — это голый cross join; он НЕ фильтрует. Ты получишь полное произведение |u| × |o|." },
    { label: "Два отдельных SELECT, чьи результаты склеены", misconception: "Склейка — это UNION, он ставит строки вертикально. JOIN комбинирует колонки горизонтально из отфильтрованного произведения." },
    { label: "Это нельзя выразить без ключевого слова JOIN", misconception: "INNER JOIN — это ровно cross join плюс фильтр WHERE; форма JOIN ... ON это синтаксический сахар для произведения-затем-фильтра." },
  ]}
/>

<MetaphorComplete
  id="01-join-as-product-metaphor-1"
  lessonSlug="01-join-as-product"
  lang="ru"
  setup="Заполни пропуск: INNER JOIN — это декартово _______ двух таблиц с предикатом ON, применённым как фильтр: каждая левая строка спарена с каждой правой, затем несовпавшие пары отброшены."
  accepted={["произведение"]}
  canonical="произведение"
  explanation="Декартово произведение — это множество всех возможных пар (|A| × |B| строк). JOIN оставляет лишь пары, удовлетворяющие ON. Поэтому размер вывода JOIN зависит от отношения, а не от числа строк одной таблицы — и поэтому пропущенный предикат даёт полное произведение."
/>

</Explanation>

<KeyTakeaway>JOIN — это отфильтрованное декартово произведение. Произведение парит каждую строку A с каждой строкой B (|A| × |B| строк, умножение), а предикат ON оставляет лишь совпавшие пары. Число строк на выходе — это «выжившие пары», никогда не просто |A| или |B| — поэтому пропущенный или слишком слабый предикат молча даёт взрывное произведение.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="01-join-as-product-retrieval"
  lang="ru"
  questions={[
    { q: "Определи JOIN через декартово произведение и предикат.", a: "INNER JOIN — это декартово произведение двух таблиц (каждая строка A в паре с каждой строкой B, давая |A| × |B| кандидатных пар) с предикатом ON, применённым как фильтр: оставляются лишь пары, удовлетворяющие предикату. Клауза ON — это просто фильтр на произведении, а не особый синтаксис сопоставления." },
    { q: "В users 3 строки, в orders 4. Сколько строк у CROSS JOIN и что вместо этого вернёт inner join по o.user_id = u.id?", a: "CROSS JOIN вернёт 3 × 4 = 12 строк — полное произведение. Inner join вернёт лишь пары, где o.user_id = u.id; если каждый заказ принадлежит ровно одному из этих пользователей, это по одной строке на совпавший заказ — подмножество тех 12." },
    { q: "Почему «произведение, затем фильтр» — правильная модель, хотя Postgres не материализует полное произведение?", a: "Потому что результат определён через неё: ответ равен «произведение-затем-фильтр» независимо от того, какой физический алгоритм (nested loop, hash, merge) выбрал планировщик. Эта модель позволяет предсказывать кардинальность — замечать fan-out, случайные cross join и пропущенные предикаты на глаз, прежде чем они станут аварией." },
  ]}
/>

<Recap lang="ru">
Фундамент любого JOIN — **декартово произведение**: спарь каждую строку A с каждой строкой B, давая `|A| × |B|` строк — число, которое *умножается*, отчего JOIN и может взорваться. `CROSS JOIN` — это сырое произведение; `INNER JOIN ... ON p` — это ровно произведение с `p` как фильтром, тождественно `CROSS JOIN ... WHERE p`. Из-за этого размер вывода JOIN — это «сколько пар пережили предикат», никогда не просто размер одной из таблиц — поэтому пропущенная или слишком слабая клауза `ON` молча вырождается в полное произведение и аварию. Планировщик не строит буквальную сетку; он применяет hash, merge или nested-loop стратегии (урок join-algorithms трека `databases`), чтобы дёшево прийти к тому же ответу «произведение-затем-фильтр». Держи эту модель — и весь остаток раздела (outer join, semi/anti join, LATERAL и ловушки fan-out) сводится к «какие пары оставляем и что делаем со строками без партнёра».
</Recap>
