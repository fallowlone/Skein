---
slug: "05-lateral-joins"
lang: ru
track: "sql-postgres"
unit: "02-joins-deep"
order: 5
title: "LATERAL join"
summary: "LATERAL позволяет подзапросу в FROM ссылаться на более ранние элементы FROM, поэтому он выполняется по разу на внешнюю строку. Это чистый способ сделать top-N на группу и развернуть set-returning функции — обычно за один проход, обгоняя коррелированные скалярные подзапросы."
estMin: 15
status: ready
lessonType: topic
level: middle
concepts:
  - lateral-join
  - correlated-from-subquery
  - top-n-per-group
  - set-returning-functions
  - per-row-evaluation
prereqs:
  - 04-self-and-cross-joins
sources:
  - https://www.postgresql.org/docs/current/queries-table-expressions.html
  - https://www.postgresql.org/docs/current/sql-select.html
  - https://www.postgresql.org/docs/current/functions-srf.html
---

import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import MetaphorComplete from "~/components/pedagogy/MetaphorComplete.astro";
import SequenceDiagram from "~/components/diagram/SequenceDiagram.astro";

<Hook>
«Показать 3 последних заказа каждого пользователя» звучит тривиально, пока не попробуешь. `GROUP BY` не может вернуть целые строки, только агрегаты. Оконная функция работает, но читает каждый заказ, чтобы их ранжировать. Первая версия команды стреляла одним запросом на пользователя из кода приложения — паттерн N+1 — и эндпоинт занимал 4 секунды на 800 пользователей. Переписывание в один запрос, доведшее его до 90 мс, использовало фичу, к которой большинство инженеров никогда не тянется: `LATERAL`. Это чистейший инструмент для «top-N на группу», и обретя его, ты перестаёшь писать построчные циклы.
</Hook>

<Crux>LATERAL — подзапрос в FROM, видящий более ранние элементы FROM и выполняемый по разу на внешнюю строку — чистый top-N на группу.</Crux>

<Explanation>

## Что LATERAL меняет во FROM

Прежде чем тянуться к N+1 циклу или нагромождать три коррелированных подзапроса, спроси себя: зависит ли каждая строка результата от текущего значения внешней строки? Если да — `LATERAL` сделает из этого один запрос вместо многих. Обычно табличные выражения в клаузе `FROM` независимы: `FROM users u, orders o` не даёт `o` знать ничего об `u` — условие join связывает их потом. Подзапрос во `FROM` такой же; он не видит другие таблицы. `LATERAL` убирает эту стену. `LATERAL`-подзапрос (или функция) во `FROM` **может ссылаться на колонки элементов FROM, идущих перед ним**, и вычисляется **по разу на строку** этих более ранних элементов.

В этом вся идея: коррелированный подзапрос, но во `FROM`, а не в `SELECT` или `WHERE`, поэтому он может вернуть *несколько колонок и несколько строк* на внешнюю строку, а не единственный скаляр.

```sql
-- Для каждого пользователя его 3 последних заказа — целые строки, не агрегаты
SELECT u.id, u.name, recent.id AS order_id, recent.created_at
FROM users u
CROSS JOIN LATERAL (
  SELECT o.id, o.created_at
  FROM orders o
  WHERE o.user_id = u.id        -- ссылается на u — законно лишь благодаря LATERAL
  ORDER BY o.created_at DESC
  LIMIT 3
) AS recent;
```

Внутренний подзапрос ссылается на `u.id`, что разрешено лишь из-за `LATERAL`. Для каждой строки пользователя Postgres выполняет этот маленький запрос «топ-3 заказа для этого пользователя» и приджойнивает (до 3) результаты обратно. Это каноническое решение **top-N на группу**, и нет краткого нелатерального эквивалента, возвращающего целые строки.

<SequenceDiagram
  label="Построчное вычисление LATERAL-подзапроса: для каждой строки пользователя исполнитель выполняет коррелированный внутренний запрос и выдаёт его строки, затем переходит к следующему пользователю."
  hue="var(--d-data)"
  actors={["Внешний (users)", "LATERAL подзапрос"]}
  messages={[
    { from: "Внешний (users)", to: "LATERAL подзапрос", label: "u1 → топ-3 для u1" },
    { from: "LATERAL подзапрос", to: "Внешний (users)", label: "3 заказа" },
    { from: "Внешний (users)", to: "LATERAL подзапрос", label: "u2 → топ-3 для u2" },
    { from: "LATERAL подзапрос", to: "Внешний (users)", label: "3 заказа" },
    { from: "Внешний (users)", to: "LATERAL подзапрос", label: "u3 → топ-3 для u3" },
    { from: "LATERAL подзапрос", to: "Внешний (users)", label: "1 заказ" },
  ]}
  caption="Латеральный подзапрос срабатывает по разу на внешнюю строку, видя её значения. У u3 был лишь один заказ, поэтому он даёт одну строку. CROSS JOIN LATERAL выбрасывает пользователей без заказов; LEFT JOIN LATERAL ... ON true оставляет их, NULL-расширенными."
/>

## CROSS против LEFT JOIN LATERAL и set-returning функции

`CROSS JOIN LATERAL (...)` выбрасывает внешнюю строку, если её подзапрос ничего не вернул — пользователь без заказов исчезает. Чтобы оставить каждую внешнюю строку (NULL-расширенной при пустом подзапросе), используй `LEFT JOIN LATERAL (...) ON true`. `ON true` — обязательный синтаксис: латеральному left join всё равно нужен `ON`, а корреляция уже живёт внутри подзапроса, так что условие join — просто «всегда».

`LATERAL` также сияет с **set-returning функциями** — функциями, возвращающими много строк, как `jsonb_array_elements` или `generate_series`. Чтобы развернуть JSONB-массив-колонку в строки, по одной на элемент на родительскую строку:

```sql
-- По строке на каждый тег в JSONB-массиве tags каждого товара
SELECT p.id, tag.value AS tag
FROM products p
CROSS JOIN LATERAL jsonb_array_elements_text(p.tags) AS tag(value);
```

Вызов функции ссылается на `p.tags`, что неявно латерально, когда функция во `FROM` ссылается на более раннюю таблицу — Postgres даже позволяет опустить ключевое слово для функций, но писать его яснее. Этот паттерн «развернуть на строку» повсюду, как только ты хранишь массивы или JSON (раздел 06 глубоко копает JSONB).

## Почему LATERAL часто бьёт коррелированный скалярный подзапрос

Иногда можно подделать построчные результаты коррелированными скалярными подзапросами в `SELECT` — но каждый возвращает *единственное* значение, и нужен отдельный подзапрос на колонку, каждый пересканирует те же заказы. Хочешь id последнего заказа *и* дату *и* сумму? Это три коррелированных подзапроса, три скана. Один `LATERAL` возвращает все три колонки из **одного** вычисления на строку. Меньше сканов, меньше round trip'ов, чем у N+1 цикла на стороне приложения, и планировщик может выбрать индексный nested loop, где внутренний `LIMIT 3` читает лишь несколько индексных записей на пользователя — превращая 4-секундный N+1 эндпоинт в запрос менее 100 мс.

<Inset kind="why" lang="ru">
Когда LATERAL — неправильный инструмент? Когда тебе действительно нужно ранжирование или скользящее вычисление по всем строкам — оконная функция (раздел 04) часто чище и может быть одним последовательным проходом. Грубое правило: LATERAL хорош для маленького top-N на группу (LIMIT k на внешнюю строку), потому что индексный nested loop читает лишь k записей на группу; оконная функция хороша, когда нужно значение, выведенное из всей партиции (rank, скользящий итог), и иначе пришлось бы пересканировать. Для топ-3-последних на индексированном (user_id, created_at) LATERAL с LIMIT 3 обычно дешевле; для «ранжировать каждый заказ внутри его пользователя» — оконная функция.
</Inset>

<Quiz
  id="05-lateral-joins-quiz-1"
  lessonSlug="05-lateral-joins"
  lang="ru"
  question="Что именно включает LATERAL, чего обычный подзапрос во FROM не может?"
  choices={[
    { label: "Подзапрос может ссылаться на колонки более ранних элементов FROM и вычисляется по разу на внешнюю строку", correct: true },
    { label: "Он заставляет подзапрос выполниться лишь раз, закешировав для всех строк", misconception: "LATERAL — противоположность кеша-однократно: он вычисляется на каждую внешнюю строку как раз чтобы использовать её значения." },
    { label: "Он убирает нужду в клаузе ON в каждом join", misconception: "LEFT JOIN LATERAL всё равно нуждается в ON true; LATERAL про корреляцию во FROM, а не про отказ от ON." },
    { label: "Он превращает запрос в возвращающий лишь агрегаты", misconception: "LATERAL возвращает целые строки на внешнюю строку — противоположность схлопыванию в агрегаты, это работа GROUP BY." },
  ]}
/>

<Quiz
  id="05-lateral-joins-quiz-2"
  lessonSlug="05-lateral-joins"
  lang="ru"
  question="Ты используешь CROSS JOIN LATERAL, чтобы получить топ-3 заказа каждого пользователя, но пользователи без заказов исчезают из результата. Как их оставить?"
  choices={[
    { label: "Использовать LEFT JOIN LATERAL (...) ON true — пустые результаты подзапроса NULL-расширяются вместо выбрасывания пользователя", correct: true },
    { label: "Добавить WHERE order_id IS NOT NULL", misconception: "Это выбросило бы пользователей без заказов ещё жёстче; ты хочешь их ОСТАВИТЬ, для чего нужен outer (LEFT) латеральный join." },
    { label: "Убрать LIMIT 3", misconception: "LIMIT управляет top-N; его удаление не заставит пользователя без заказов появиться — для него всё равно нет строк для возврата." },
    { label: "Переключиться на обычный INNER JOIN", misconception: "Inner join тоже выбрасывает пользователей без совпадающих заказов; фикс — outer-форма LEFT JOIN LATERAL ... ON true." },
  ]}
/>

<MetaphorComplete
  id="05-lateral-joins-metaphor-1"
  lessonSlug="05-lateral-joins"
  lang="ru"
  setup="Заполни пропуск: LATERAL-подзапрос вычисляется по разу на _______ строку более ранних элементов FROM и может ссылаться на колонки этой строки — поэтому он и может вернуть топ-N заказов каждого пользователя."
  accepted={["внешнюю", "левую", "каждую", "ведущую"]}
  canonical="внешнюю"
  explanation="LATERAL превращает подзапрос во FROM в коррелированное вычисление на каждую внешнюю строку: для каждой строки левой/внешней стороны подзапрос выполняется со значениями этой строки в области видимости. Это построчное срабатывание и делает top-N на группу выразимым в одном запросе вместо N+1 цикла."
/>

</Explanation>

<KeyTakeaway>LATERAL позволяет подзапросу во FROM ссылаться на более ранние элементы FROM и выполняет его по разу на внешнюю строку — коррелированный подзапрос, способный вернуть несколько колонок и строк. Это канонический инструмент top-N на группу (CROSS JOIN LATERAL (... ORDER BY ... LIMIT k)) и чистый способ развернуть set-returning функции вроде jsonb_array_elements. Используй LEFT JOIN LATERAL ... ON true, чтобы оставить внешние строки с пустым подзапросом. Один LATERAL бьёт несколько коррелированных скалярных подзапросов (один проход, все колонки) и N+1 цикл на стороне приложения.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="05-lateral-joins-retrieval"
  lang="ru"
  questions={[
    { q: "Одним предложением: что такое LATERAL join и как он вычисляется?", a: "LATERAL join — это подзапрос (или set-returning функция) в клаузе FROM, которому разрешено ссылаться на колонки элементов FROM, идущих перед ним, и Postgres вычисляет его по разу на строку этих более ранних элементов — фактически коррелированный подзапрос во FROM, способный вернуть несколько строк и колонок на внешнюю строку." },
    { q: "Напиши форму запроса топ-3-заказа-на-пользователя через LATERAL и скажи, как оставить пользователей без заказов.", a: "SELECT u.id, r.* FROM users u CROSS JOIN LATERAL (SELECT o.id, o.created_at FROM orders o WHERE o.user_id = u.id ORDER BY o.created_at DESC LIMIT 3) AS r. CROSS JOIN LATERAL выбрасывает пользователей, чей подзапрос ничего не вернул; чтобы оставить пользователей без заказов NULL-расширенными, переключись на LEFT JOIN LATERAL (...) AS r ON true." },
    { q: "Почему один LATERAL часто обгоняет несколько коррелированных скалярных подзапросов или N+1 цикл приложения?", a: "Коррелированный скалярный подзапрос возвращает одно значение, поэтому для нескольких полей нужно несколько подзапросов, каждый пересканирует те же данные; один LATERAL возвращает все колонки из одного вычисления на внешнюю строку. По сравнению с N+1 циклом приложения (один запрос на пользователя, много round trip'ов) LATERAL — это единый запрос, который планировщик может выполнить как индексный nested loop, где внутренний LIMIT k читает лишь k индексных записей на группу — превращая секунды в миллисекунды." },
  ]}
/>

<Recap lang="ru">
`LATERAL` поднимает стену между элементами `FROM`: `LATERAL`-подзапрос или функция может ссылаться на колонки идущих перед ним элементов и вычисляется **по разу на внешнюю строку**, делая его коррелированным подзапросом во `FROM`, способным вернуть много строк и колонок на строку. Его главное применение — **top-N на группу** — `CROSS JOIN LATERAL (SELECT ... WHERE o.user_id = u.id ORDER BY created_at DESC LIMIT 3)` — у которого нет краткого целостроичного эквивалента без него. Используй `CROSS JOIN LATERAL`, чтобы выбросить внешние строки с пустым подзапросом, или `LEFT JOIN LATERAL (...) ON true`, чтобы оставить их NULL-расширенными. `LATERAL` также разворачивает **set-returning функции** вроде `jsonb_array_elements` и `generate_series`, по строке-элементу на родителя. Он выигрывает у коррелированных скалярных подзапросов (одно вычисление возвращает все колонки, а не скан на поле) и у N+1 цикла на стороне приложения (единый запрос, который планировщик исполнит как индексный nested loop, читающий лишь k записей на группу). Граница с оконными функциями: `LATERAL` для маленького top-N на группу по индексу; оконная функция (раздел 04), когда нужно значение, вычисленное по всей партиции. Теперь, когда нужны топ-N строк на группу в одном запросе, потянешься к `CROSS JOIN LATERAL (... ORDER BY ... LIMIT N)` — а не к циклу с одним запросом на группу из кода приложения.
</Recap>
