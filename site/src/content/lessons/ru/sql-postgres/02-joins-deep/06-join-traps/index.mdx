---
slug: "06-join-traps"
lang: ru
track: "sql-postgres"
unit: "02-joins-deep"
order: 6
title: "Ловушки join и взрыв строк"
summary: "Присоединение двух детей один-ко-многим к одному родителю умножает строки и молча удваивает твои SUM и COUNT — баг fan-out. Фикс — сперва агрегировать каждого ребёнка отдельно. Плюс пропущенные условия, неуникальные ключи и порядок join."
estMin: 16
status: ready
lessonType: topic
level: middle
concepts:
  - fan-out
  - row-explosion
  - double-counting
  - pre-aggregation
  - duplicate-join-keys
prereqs:
  - 05-lateral-joins
sources:
  - https://www.postgresql.org/docs/current/queries-table-expressions.html
  - https://www.postgresql.org/docs/current/tutorial-agg.html
  - https://use-the-index-luke.com/sql/join
---

import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
Финансовый дашборд сообщил о $2,4M месячной выручки. Бухгалтерская система сказала $1,2M. Ровно вдвое. Баг был в одном запросе, который соединял `orders` сразу с `order_items` и `payments` в одном выражении и суммировал total заказа. У каждого заказа было ~2 платежа, поэтому каждая строка заказа дублировалась, и `SUM(o.total)` считал сумму каждого заказа по разу на строку платежа. Ни ошибки, ни предупреждения — join молча удвоил число, которому доверяла вся компания. Это и есть fan-out, и это самый дорогой баг join, потому что запрос *работает нормально*.
</Hook>

<Crux>Два ребёнка один-ко-многим к родителю умножают строки и удваивают SUM/COUNT — это fan-out. Фикс: сперва предагрегировать каждого ребёнка.</Crux>

<Explanation>

## Fan-out: два ребёнка, умноженные строки

Вспомни урок 1: join — это произведение, и число строк умножается. Fan-out — это то самое умножение, бьющее там, где ты не ждал. Возьми одного родителя (`orders`) с двумя детьми **один-ко-многим**: `order_items` (позиции) и `payments` (платежи частями). Соедини все три в одном запросе:

```sql
-- НЕВЕРНО: выручка удвоена (или хуже)
SELECT SUM(o.total) AS revenue,
       SUM(oi.quantity) AS units
FROM orders o
JOIN order_items oi ON oi.order_id = o.id
JOIN payments    p  ON p.order_id  = o.id;
```

Если у заказа 3 позиции и 2 платежа, join производит 3 × 2 = 6 строк для этого единственного заказа — каждая позиция спарена с каждым платежом, потому что оба ребёнка джойнятся по `order_id` независимо. Теперь `SUM(o.total)` складывает total этого заказа **6 раз**, а `SUM(oi.quantity)` считает каждую позицию **2 раза** (по разу на платёж). Сумма заказа не изменилась; изменилось число строк, её несущих. Два ребёнка размножились друг против друга — декартово произведение *внутри* группы каждого заказа.

<FlowDiagram
  label="Умножение fan-out: один заказ с 3 позициями и 2 платежами джойнится в 3 на 2 равно 6 строк, поэтому любое значение на заказ суммируется 6 раз вместо одного."
  perRow={3}
  hue="var(--d-data)"
  nodes={[
    { id: "order", label: "1 заказ", sub: "total = 100" },
    { id: "items", label: "3 позиции", sub: "один-ко-многим" },
    { id: "pays", label: "2 платежа", sub: "один-ко-многим" },
    { id: "rows", label: "6 строк", sub: "3 × 2" },
    { id: "sum", label: "SUM(total)", sub: "= 600, не 100" },
  ]}
  edges={[
    { from: "order", to: "items" },
    { from: "order", to: "pays" },
    { from: "items", to: "rows" },
    { from: "pays", to: "rows" },
    { from: "rows", to: "sum" },
  ]}
  caption="Два независимых join один-ко-многим к одному родителю умножаются: 3 позиции × 2 платежа = 6 строк вывода на заказ. Агрегаты по этим строкам пересчитывают любое значение, принадлежащее самому заказу (его total) или другому ребёнку."
/>

## Фикс: предагрегируй каждого ребёнка перед join

Правило прямолинейно: **никогда не соединяй двух независимых детей один-ко-многим с родителем и затем агрегируй.** Схлопни каждого ребёнка до одной строки на родителя *сначала* — в подзапросе или CTE — затем соедини эти предагрегированные результаты, которые теперь один-к-одному с родителем и не могут размножиться:

```sql
-- ВЕРНО: каждый ребёнок агрегирован до одной строки на заказ, затем join
SELECT o.id,
       o.total,
       items.units,
       pays.paid
FROM orders o
LEFT JOIN (
  SELECT order_id, SUM(quantity) AS units
  FROM order_items GROUP BY order_id
) items ON items.order_id = o.id
LEFT JOIN (
  SELECT order_id, SUM(amount) AS paid
  FROM payments GROUP BY order_id
) pays ON pays.order_id = o.id;
```

Каждый подзапрос возвращает не более одной строки на `order_id`, поэтому ни один join не умножает. `SUM(o.total)` по этому теперь верен. Паттерн обобщается на любое число детей: агрегируй каждого в своём CTE/подзапросе, затем соедини сводки одна-строка-на-родителя. (`LATERAL` из прошлого урока — другой способ вычислить эти агрегаты на родителя инлайн.)

Для быстрого *подсчёта* иногда можно залатать fan-out через `COUNT(DISTINCT oi.id)` вместо `COUNT(*)` — distinct-подсчёт игнорирует дублированные строки. Но `SUM(DISTINCT o.total)` — ловушка: два разных заказа могут законно иметь одинаковый total, и `DISTINCT` их схлопнет. `COUNT(DISTINCT)` спасает подсчёты; только предагрегация надёжно спасает суммы.

## Другие ловушки: пропущенные условия, неуникальные ключи, порядок join

Fan-out — тонкая, но три более грубые ловушки делят раздел. Когда ревьюишь запрос с несколькими join, спроси себя: ограничивает ли каждое условие join реальное отношение, уникален ли ключ хотя бы с одной стороны, и не присоединяются ли несколько независимых детей к одному родителю?

- **Пропущенное условие join** → случайный cross join из урока 1. Забытый `ON` или `ON`, который реально не связывает таблицы, даёт `|A| × |B|` строк — мгновенный взрыв, а не тихое удвоение.
- **Неуникальные ключи join** → даже «невинный» join дублирует строки, если ключ неуникален на стороне, которую ты считал уникальной. Соединение `orders` с `users` по `email` вместо `id` сделает fan-out, если два пользователя делят email. Всегда джойнь по ключу, уникальному хотя бы на одной стороне, или точно знай, почему он неуникален.
- **Порядок join и планировщик** → ты пишешь join слева направо, но **планировщик свободно их переупорядочивает** ради минимизации стоимости; *результат* идентичен независимо от написанного порядка (для inner join). Так что не подкручивай порядок join вручную ради корректности или скорости — чини предикаты и статистику и дай планировщику выбрать. (Outer join ограничивают переупорядочивание; глава execution-plans трека `databases` разбирает когда.)

<Inset kind="mistake" lang="ru">
Причина, по которой fan-out так опасен, в том, что он невидим на малых данных. В деве у заказа одна позиция и один платёж, так что 1 × 1 = 1 — запрос выглядит верным и уезжает. В проде у заказов много позиций и платежей частями, умножение включается, и дашборд молча завышен в 2× или 5×. Защита — привычка, а не инструмент: всякий раз, когда запрос соединяет больше одной детской таблицы с родителем и затем агрегирует, остановись и предагрегируй каждого ребёнка. Проверяй здравость, сравнивая число строк после join с числом строк родителя — если join добавил строки, любой агрегат на родителя теперь неверен.
</Inset>

<Quiz
  id="06-join-traps-quiz-1"
  lessonSlug="06-join-traps"
  lang="ru"
  question="У заказа 3 позиции и 2 платежа. Ты делаешь SELECT SUM(o.total), соединяя orders с ОБОИМИ order_items и payments. Истинный total $100. Что сообщит запрос?"
  choices={[
    { label: "$600 — join делает 3 × 2 = 6 строк для заказа, поэтому o.total суммируется шесть раз", correct: true },
    { label: "$100 — join не влияет на агрегаты", misconception: "Ещё как влияет: каждая из 6 размноженных строк несёт o.total = 100, и SUM складывает все шесть, давая 600." },
    { label: "$500 — 3 позиции + 2 платежа = 5 строк", misconception: "Независимые join один-ко-многим умножают, а не складывают: это 3 × 2 = 6 строк, поэтому total суммируется 6 раз = 600." },
    { label: "$300 — размножаются только позиции", misconception: "Оба ребёнка размножаются друг против друга. С 2 платежами тоже это 6 строк, а не 3 — платежи удваивают уже утроенный счёт." },
  ]}
/>

<Quiz
  id="06-join-traps-quiz-2"
  lessonSlug="06-join-traps"
  lang="ru"
  question="Каков надёжный фикс fan-out при суммировании значения родителя по нескольким детским таблицам?"
  choices={[
    { label: "Предагрегировать каждого ребёнка до одной строки на родителя (подзапрос/CTE), затем соединить эти сводки", correct: true },
    { label: "Обернуть каждый SUM в SUM(DISTINCT ...)", misconception: "SUM(DISTINCT) схлопывает законно-равные значения (два заказа с одинаковым total становятся одним), давая другой неверный ответ — это не общий фикс." },
    { label: "Добавить ORDER BY к запросу", misconception: "Сортировка не меняет, какие строки существуют и сколько раз значение суммируется; fan-out остаётся." },
    { label: "Переключить join на LEFT JOIN", misconception: "Inner против outer не решает дублирование; LEFT JOIN двух детей один-ко-многим всё равно умножает совпавшие строки." },
  ]}
/>

<DragOrder
  id="06-join-traps-drag-1"
  lessonSlug="06-join-traps"
  lang="ru"
  prompt="Расставь шаги для безопасного отчёта о выручке и единицах по orders, items и payments:"
  items={[
    { id: "a", label: "Заметить, что запрос соединяет ДВУХ детей один-ко-многим (items, payments) с orders и агрегирует" },
    { id: "b", label: "Распознать fan-out: строки = items × payments на заказ, поэтому суммы пересчитывают" },
    { id: "c", label: "Агрегировать order_items до одной строки на order_id (SUM quantity) в подзапросе/CTE" },
    { id: "d", label: "Агрегировать payments до одной строки на order_id (SUM amount) в отдельном подзапросе/CTE" },
    { id: "e", label: "Соединить две сводки одна-строка-на-заказ с orders (теперь один-к-одному, без fan-out)" },
    { id: "f", label: "Проверить, что число строк после join равно числу строк orders" },
  ]}
  correctOrder={["a", "b", "c", "d", "e", "f"]}
/>

</Explanation>

<KeyTakeaway>Fan-out: соединение двух независимых детей один-ко-многим с родителем умножает строки (items × payments на родителя), поэтому SUM и COUNT пересчитывают любое значение на родителя — молча, без ошибки. Надёжный фикс: предагрегировать каждого ребёнка до одной строки на родителя в подзапросе/CTE, затем соединить эти сводки один-к-одному. COUNT(DISTINCT id) может спасти подсчёт; SUM(DISTINCT) не спасает сумму (равные значения схлопываются). Следи и за грубыми ловушками: пропущенный/слабый ON это cross join, неуникальный ключ join дублирует строки, а порядок join выбирает планировщик, а не ты.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="06-join-traps-retrieval"
  lang="ru"
  questions={[
    { q: "Объясни баг fan-out: почему соединение orders сразу с order_items и payments удваивает выручку?", a: "Оба ребёнка джойнятся к orders по order_id независимо, поэтому внутри группы каждого заказа два join один-ко-многим образуют декартово произведение: заказ с i позициями и p платежами производит i × p строк вывода. Каждая из этих строк несёт один и тот же o.total, поэтому SUM(o.total) складывает сумму заказа i × p раз вместо одного. Данные заказа не изменились; join умножил строки, их несущие, пересчитывая любое значение на заказ." },
    { q: "Каков надёжный фикс и почему SUM(DISTINCT) — не он?", a: "Предагрегировать каждого ребёнка ровно до одной строки на родителя в своём подзапросе или CTE (например SUM(quantity) GROUP BY order_id, SUM(amount) GROUP BY order_id), затем LEFT JOIN этих сводок к orders — теперь каждый join один-к-одному и не может размножиться, так что SUM(o.total) верен. SUM(DISTINCT o.total) проваливается, потому что два по-настоящему разных заказа могут делить один total; DISTINCT их схлопнет, давая другое неверное число. COUNT(DISTINCT id) может починить дублированный подсчёт, но только предагрегация чинит сумму." },
    { q: "Стоит ли вручную упорядочивать join ради производительности и что вызывает дубли строк из «обычного» join?", a: "Нет — для inner join планировщик свободно переупорядочивает ради минимизации стоимости, и результат идентичен независимо от написанного порядка, так что подкручивай предикаты и статистику, а не порядок join (outer join действительно ограничивают переупорядочивание). Дубли строк из обычного join приходят от неуникального ключа join: если джойнить по колонке, неуникальной на стороне, которую ты считал уникальной (например users.email, когда два пользователя делят email), каждое совпадение умножается, делая fan-out ровно как в случае с двумя детьми." },
  ]}
/>

<Recap lang="ru">
**Fan-out** — самый дорогой баг раздела, потому что запрос успешен, а число неверно. Соединение двух независимых детей один-ко-многим с одним родителем образует декартово произведение *внутри* группы каждого родителя: `i` позиций × `p` платежей = `i × p` строк на заказ, поэтому `SUM(o.total)` складывает сумму заказа `i × p` раз, и финансовый дашборд молча удваивается. Надёжный фикс — **предагрегация** (pre-aggregation): схлопни каждого ребёнка до одной строки на родителя в своём подзапросе или CTE, затем соедини эти сводки один-к-одному (или вычисли их через `LATERAL`); `COUNT(DISTINCT id)` может залатать подсчёт, но `SUM(DISTINCT)` не латает сумму, ведь равные значения схлопываются. Другие ловушки раздела грубее: пропущенный или слишком слабый `ON` — это случайный cross join из урока 1; неуникальный ключ join дублирует строки тем же образом (джойнь по ключу, уникальному хотя бы на одной стороне); а *порядок* join — работа планировщика, не твоя — чини предикаты и статистику, а не написанную последовательность. Завершающая привычка: всякий раз, когда запрос соединяет больше одного ребёнка с родителем и агрегирует, сравни число строк после join с числом строк родителя — если join добавил строки, каждый агрегат на родителя уже неверен. Теперь, когда видишь запрос с несколькими дочерними join и суммой, сразу сравниваешь число строк результата с числом строк родителя — потому что тихое удвоение опаснее любой ошибки, которая хотя бы шумит.
</Recap>
