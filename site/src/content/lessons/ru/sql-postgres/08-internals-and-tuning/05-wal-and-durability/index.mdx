---
slug: "05-wal-and-durability"
lang: ru
track: "sql-postgres"
unit: "08-internals-and-tuning"
order: 5
title: "WAL и долговечность"
summary: "Журнал упреждающей записи фиксирует каждое изменение до того, как тронута страница данных. fsync и synchronous_commit — рычаг долговечность-против-пропускной способности; checkpoint'ы ограничивают время восстановления; тот же WAL питает репликацию и PITR."
estMin: 16
status: ready
lessonType: topic
level: senior
concepts:
  - write-ahead-log
  - fsync-synchronous-commit
  - checkpoints
  - crash-recovery
  - wal-replication-pitr
prereqs:
  - 04-vacuum-and-bloat
  - 07-transactions-concurrency/01-acid-in-practice
sources:
  - https://www.postgresql.org/docs/current/wal-intro.html
  - https://www.postgresql.org/docs/current/runtime-config-wal.html
  - https://www.interdb.jp/pg/pgsql09.html
---

import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import MetaphorComplete from "~/components/pedagogy/MetaphorComplete.astro";
import SequenceDiagram from "~/components/diagram/SequenceDiagram.astro";

<Hook>
Платёжный сервис коммитит заказ, возвращает клиенту `200 OK`, и через 80 миллисекунд машина теряет питание. После перезагрузки заказ *на месте*, total цел — хотя сама страница таблицы не успела на диск до краха. Это выживание не везение; это журнал упреждающей записи, делающий ровно одну работу: записывает то, что ты собираешься изменить, *до* изменения. Переключи один флаг конфига — `synchronous_commit = off` — и тот же заказ может исчезнуть при следующем крахе, в обмен на 3× пропускной способности записи. Этот урок — про этот флаг и машинерию за ним.
</Hook>

<Crux>WAL логирует изменение до записи страницы — это долговечность; synchronous_commit меняет её на пропускную способность.</Crux>

<Explanation>

## Упреждающая запись: сначала лог, потом страница

Прежде чем решать, стоит ли переключить `synchronous_commit`, нужно понять, что он контролирует — потому что два флага долговечности делают принципиально разные вещи. Главное правило защиты от краха — **журнал упреждающей записи (write-ahead logging, WAL)**: прежде чем Postgres изменит страницу данных, он сначала дописывает компактную запись об изменении в **WAL** — последовательный, только-на-добавление лог. Только запись WAL должна быть сброшена на долговечное хранилище в момент коммита; сами страницы таблицы и индекса можно записать позже, лениво, фоновым писателем и checkpointer.

Почему это быстрее *и* безопаснее, чем запись самой страницы данных при коммите? Потому что записи WAL **последовательны** (дописывание в конец текущего файла лога), тогда как записи страниц данных **случайны** (разбросаны по heap). Форсировать один маленький последовательный `fsync` на коммит куда дешевле, чем форсировать много случайных записей страниц — и этого достаточно, потому что если машина падает до сброса грязных страниц, **восстановление переигрывает WAL** и реконструирует их. Страницы данных — оптимизация; WAL — источник истины.

```sql
-- Где WAL сейчас и как быстро он генерируется:
SELECT pg_current_wal_lsn();                       -- текущая позиция записи (LSN)
SHOW synchronous_commit;                            -- on | off | remote_apply ...
SHOW wal_level;                                     -- replica (по умолч.) | logical | minimal
SHOW max_wal_size;                                  -- мягкий потолок, запускающий checkpoint'ы
```

<SequenceDiagram
  label="Долговечный коммит: backend дописывает запись WAL об изменении, делает fsync WAL на диск, затем подтверждает COMMIT клиенту; изменённая страница данных сбрасывается в heap позже, а периодический checkpoint помечает, откуда восстановление может безопасно стартовать."
  hue="var(--d-data)"
  actors={["Client", "Backend", "WAL", "Heap"]}
  messages={[
    { from: "Client", to: "Backend", label: "UPDATE orders ..." },
    { from: "Backend", to: "WAL", label: "дописать запись" },
    { from: "Backend", to: "WAL", label: "fsync (долговечно)" },
    { from: "Backend", to: "Client", label: "COMMIT ok" },
    { from: "Backend", to: "Heap", label: "сброс страницы (позже)" },
    { from: "WAL", to: "Heap", label: "checkpoint синхр." },
  ]}
  caption="Долговечность достигается в момент возврата fsync WAL — до записи страницы heap. При крахе переигрывание с последнего checkpoint восстанавливает любую несброшенную страницу."
/>

## Рычаг долговечность/пропускная способность

`synchronous_commit` управляет, *когда* коммит подтверждается относительно сброса WAL:

- **`on` (по умолчанию)** — `COMMIT` ждёт, пока запись WAL не будет `fsync`-нута на долговечное хранилище. Закоммиченная транзакция переживёт крах. Полная долговечность, один fsync латентности на коммит.
- **`off`** — `COMMIT` возвращается *немедленно* после записи WAL в буфер ОС, *до* fsync. Сброс происходит асинхронно в пределах `wal_writer_delay` (≈200 мс). При крахе можно потерять последнюю долю секунды *закоммиченных* транзакций — но **база остаётся консистентной** (без повреждений, без рваного состояния), ты теряешь лишь хвост. В обмен пропускная способность на мелких транзакциях записи может подскочить в 2–3×, потому что коммиты больше не блокируются на диске.

Это самое значимое решение по тюнингу долговечности. Сеньорская формулировка: `synchronous_commit = off` — это *не* «небезопасно» так, как отключение fsync; он никогда не повреждает базу, он лишь расширяет окно *потерянных-но-подтверждённых* коммитов. Для платёжного реестра: никогда. Для аналитического конвейера загрузки, способного переиграть свой источник: часто это бесплатный 3× выигрыш.

Два связанных флага, которые путают: **`fsync = off`** — реально опасный; он позволяет самому WAL пропустить сброс на диск, так что крах может повредить кластер; никогда не используй вне одноразовой тестовой машины. А `synchronous_commit = remote_apply` расширяет долговечность до применения изменения репликой (см. репликацию ниже).

## Checkpoint'ы ограничивают восстановление

Если бы WAL копился вечно, восстановление после краха переигрывало бы с начала времён. **Checkpoint** — периодический акт сброса всех грязных страниц данных в heap и записи: «всё до этой позиции WAL теперь безопасно на файлах данных». Восстановлению нужно переиграть WAL только *после* последнего checkpoint — поэтому частота checkpoint'ов ограничивает время восстановления.

Компромисс: частые checkpoint'ы — короткое восстановление, но больше постоянного I/O записи (частый сброс грязных страниц); редкие checkpoint'ы — меньше ровного I/O, но долгое, скачкообразное восстановление и большой WAL. Postgres запускает checkpoint, когда истекает `checkpoint_timeout` (по умолчанию 5 мин) или WAL достигает `max_wal_size` (по умолчанию 1 ГБ). Прод-тюнинг обычно *повышает* оба, чтобы checkpoint'ы были разреженнее, а пики записи сглажены, принимая более долгое восстановление — `checkpoint_completion_target` (по умолчанию 0.9) растягивает сброс по интервалу, избегая стены I/O.

<Inset kind="why" lang="ru">
Почему тот же WAL — основа и репликации, и восстановления на момент времени? Потому что WAL — полное, упорядоченное описание каждого изменения кластера. **Потоковая репликация** отправляет этот поток WAL на standby, который непрерывно его переигрывает, оставаясь почти живой копией — так работают read-реплики и HA-failover. **Восстановление на момент времени (PITR)** берёт базовый бэкап плюс архивированные сегменты WAL и переигрывает WAL до любой выбранной отметки времени или LSN, так что можно восстановиться до «5 секунд до запуска плохой миграции». Оба — это просто «переиграй WAL где-то ещё», ровно то, что делает восстановление после краха локально. Задай `wal_level = replica` (по умолчанию) или `logical`, чтобы хранить достаточно деталей WAL для этого; `minimal` логирует меньше и лишает их.
</Inset>

<Quiz
  id="05-wal-and-durability-quiz-1"
  lessonSlug="05-wal-and-durability"
  lang="ru"
  question="Почему логирование изменения в WAL до записи страницы данных делает коммиты и быстрыми, и долговечными?"
  choices={[
    { label: "Записи WAL последовательны и при коммите fsync-ится лишь маленькая запись WAL; страницы данных сбрасываются позже и восстанавливаются из WAL после краха", correct: true },
    { label: "WAL полностью пропускает диск, поэтому коммиты не трогают хранилище", misconception: "WAL fsync-ится на долговечное хранилище при коммите (с synchronous_commit=on) — именно это даёт долговечность. Он быстрый, потому что запись последовательна, а не потому что пропускает диск." },
    { label: "Запись страницы данных первой всегда быстрее записи WAL", misconception: "Наоборот: записи страниц данных случайны и разбросаны; один последовательный fsync WAL куда дешевле, чем форсировать много случайных записей страниц на коммит." },
    { label: "WAL означает, что страницы данных вообще не нужно записывать", misconception: "Страницы данных всё равно записываются, просто лениво checkpointer'ом/фоновым писателем; WAL позволяет отложить это без потери долговечности." },
  ]}
/>

<Quiz
  id="05-wal-and-durability-quiz-2"
  lessonSlug="05-wal-and-durability"
  lang="ru"
  question="Что именно происходит при synchronous_commit = off, если сервер падает сразу после возврата COMMIT?"
  choices={[
    { label: "Можно потерять последнюю долю секунды закоммиченных транзакций, но база остаётся консистентной и неповреждённой", correct: true },
    { label: "База может стать повреждённой и невосстановимой", misconception: "Это fsync = off. synchronous_commit = off лишь откладывает сброс WAL; он никогда не повреждает кластер — ты теряешь только недавно подтверждённые коммиты, без рваного состояния." },
    { label: "Ничего никогда не теряется — это идентично synchronous_commit = on", misconception: "Это не идентично: off подтверждает до fsync WAL, поэтому крах может потерять самые свежие закоммиченные транзакции. Выигрыш — 2-3x пропускной способности записи." },
    { label: "Теряются все транзакции с последнего checkpoint", misconception: "Восстановление всё равно переигрывает WAL с последнего checkpoint; под риском лишь маленький хвост, ещё не fsync-нутый (в пределах wal_writer_delay), а не всё с checkpoint." },
  ]}
/>

<MetaphorComplete
  id="05-wal-and-durability-metaphor-1"
  lessonSlug="05-wal-and-durability"
  lang="ru"
  setup="Заполни пропуск: журнал упреждающей записи фиксирует изменение _______ того, как страница данных модифицирована, поэтому крах можно починить, переиграв лог и реконструировав любую ещё не сброшенную страницу."
  accepted={["до", "прежде", "раньше"]}
  canonical="до"
  explanation="Упреждающая запись буквально значит, что лог пишется до данных. Поскольку долговечная запись WAL всегда предшествует (возможно потерянной) записи страницы данных, восстановление может переиграть лог с последнего checkpoint и заново применить каждое изменение, не достигшее heap — превращая крах в детерминированную починку, а не потерю данных."
/>

</Explanation>

<KeyTakeaway>WAL фиксирует каждое изменение *до* того, как тронута страница данных; при коммите fsync-ится лишь маленькая последовательная запись WAL, а страницы данных сбрасываются лениво, поэтому крах чинится переигрыванием WAL. `synchronous_commit = off` подтверждает до fsync — теряя последнюю долю секунды закоммиченных транзакций при крахе, но никогда не повреждая базу, в обмен на 2–3× пропускной способности (в отличие от реально опасного `fsync = off`). Checkpoint'ы сбрасывают грязные страницы и ограничивают время восстановления; тюнь `checkpoint_timeout`/`max_wal_size`, чтобы растянуть I/O. Тот же WAL питает потоковую репликацию и PITR.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="05-wal-and-durability-retrieval"
  lang="ru"
  questions={[
    { q: "Что такое упреждающая запись и почему она быстрее и безопаснее записи страницы данных при коммите?", a: "WAL значит дописывание записи об изменении в последовательный, только-на-добавление лог до модификации самой страницы данных. При коммите fsync-ится лишь эта маленькая запись WAL; страницы heap/индекса сбрасываются позже checkpointer'ом. Это быстрее, потому что записи WAL последовательны, а записи страниц данных случайны и разбросаны, так что один последовательный fsync обгоняет много случайных записей. Это безопасно, потому что если машина падает до сброса страниц, восстановление переигрывает WAL и восстанавливает их — WAL это источник истины." },
    { q: "Что меняет synchronous_commit = off и чем он отличается от fsync = off?", a: "synchronous_commit = off заставляет COMMIT возвращаться после записи WAL в буфер ОС, но до fsync, поэтому при крахе можно потерять последнюю долю секунды закоммиченных транзакций — но база остаётся полностью консистентной и неповреждённой. В ответ пропускная способность мелких записей может вырасти в 2-3x. fsync = off совсем другое и реально опасно: он позволяет WAL пропустить долговечный сброс, так что крах может повредить весь кластер. Никогда не используй fsync = off на проде; synchronous_commit = off — законный компромисс долговечность/пропускная способность." },
    { q: "Что такое checkpoint и как он связан с восстановлением после краха и репликацией?", a: "Checkpoint сбрасывает все грязные страницы данных в heap и записывает, что всё до данной позиции WAL теперь безопасно на файлах данных. Восстановлению нужно переиграть WAL только после последнего checkpoint, поэтому частота checkpoint'ов ограничивает время восстановления (часто = быстрое восстановление, но больше I/O; редко = скачкообразное долгое восстановление, тюнится через checkpoint_timeout, max_wal_size, checkpoint_completion_target). Тот же поток WAL, что восстановление переигрывает локально, отправляется на standby для потоковой репликации и переигрывается до выбранной точки для восстановления на момент времени." },
  ]}
/>

<Recap lang="ru">
Упреждающая запись — механизм долговечности Postgres: каждое изменение дописывается в последовательный **WAL** до модификации страницы данных, и при коммите fsync-ится лишь эта маленькая запись WAL — страницы heap и индекса сбрасываются лениво, потому что крах чинится **переигрыванием WAL** с последнего checkpoint. Поэтому коммиты быстры (один последовательный fsync, а не много случайных записей страниц) и долговечны (WAL — источник истины). Ключевой рычаг — `synchronous_commit`: `on` ждёт fsync WAL (полная долговечность); `off` подтверждает до него, рискуя последней долей секунды *закоммиченных* транзакций при крахе, но сохраняя базу **консистентной и неповреждённой**, ради 2–3× пропускной способности — категорически безопаснее, чем никогда-не-на-проде `fsync = off`. **Checkpoint'ы** сбрасывают грязные страницы и ограничивают, сколько WAL переиграть восстановлению; тюнь `checkpoint_timeout`, `max_wal_size` и `checkpoint_completion_target`, балансируя I/O записи против времени восстановления. Наконец, тот же самый WAL питает **потоковую репликацию** (отправь и переиграй на standby) и **PITR** (переиграй до выбранной отметки) — восстановление, репликация и восстановление во времени это всё просто «переиграй лог». Теперь, когда видишь базу, пережившую крах без потерь, реплику, отстающую на секунду, или восстановление к конкретному моменту — знаешь, какой единственный механизм сделал все три возможными.
</Recap>
