---
slug: "04-vacuum-and-bloat"
lang: ru
track: "sql-postgres"
unit: "08-internals-and-tuning"
order: 4
title: "VACUUM и раздувание таблиц"
summary: "MVCC оставляет мёртвые кортежи после каждого UPDATE и DELETE; autovacuum их убирает. Когда он отстаёт, bloat раздувает каждый скан — а exclusive-блокировка VACUUM FULL это неверное лекарство на проде."
estMin: 16
status: ready
lessonType: topic
level: senior
concepts:
  - dead-tuples
  - autovacuum
  - table-bloat
  - vacuum-full-vs-pg-repack
  - visibility-map
prereqs:
  - 03-statistics-and-estimates
  - databases/04-mvcc-isolation/04-vacuum-and-bloat
sources:
  - https://www.postgresql.org/docs/current/routine-vacuuming.html
  - https://www.postgresql.org/docs/current/runtime-config-autovacuum.html
  - https://www.interdb.jp/pg/pgsql06.html
---

import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import MetaphorComplete from "~/components/pedagogy/MetaphorComplete.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
Таблица-очередь — строки вставляются, обрабатываются, удаляются, тысячи в минуту — держит 4000 живых строк в любой момент. Её размер на диске 9 ГБ. `SELECT count(*)`, который должен быть мгновенным, занимает 6 секунд, потому что таблица на 99.9% из мёртвых кортежей, мимо которых планировщику всё равно надо просканировать. Кто-то в панике в 2 часа ночи запускает `VACUUM FULL` и блокирует таблицу на 11 минут посреди инцидента. И раздувания, и лекарства можно было избежать. Этот урок — почему MVCC создаёт мусор и как его собирать без простоя.
</Hook>

<Crux>MVCC оставляет мёртвые кортежи; autovacuum их убирает. Отстал — bloat тормозит сканы, а VACUUM FULL на проде это не фикс.</Crux>

<Explanation>

## Откуда берутся мёртвые кортежи

Почему таблица вырастает до 9 ГБ, держа всего 4000 живых строк? Механизм встроен в каждую запись. Глава MVCC трека `databases` выводит это глубоко; SQL-автору нужно следствие. При MVCC Postgres никогда не перезаписывает строку на месте. `UPDATE` пишет **новую** версию строки и помечает старую мёртвой; `DELETE` просто помечает строку мёртвой. Старая версия задерживается, потому что снимок какой-то ещё работающей транзакции может захотеть её увидеть. Поэтому каждый `UPDATE` и `DELETE` порождает **мёртвый кортеж** — строку, невидимую новым запросам, но всё ещё физически занимающую страницу на диске.

Пока эти мёртвые кортежи не очищены, они стоят дважды: занимают место, и каждый последовательный или индексный скан всё равно читает страницы, на которых они сидят, и пропускает их. Таблица на 90% из мёртвых кортежей делает примерно 10× I/O за те же живые данные.

```sql
-- Посмотри ущерб на любой таблице:
SELECT relname, n_live_tup, n_dead_tup,
       round(100.0 * n_dead_tup / nullif(n_live_tup + n_dead_tup, 0), 1) AS dead_pct,
       last_autovacuum
FROM pg_stat_user_tables
WHERE relname IN ('orders', 'events', 'line_items')
ORDER BY n_dead_tup DESC;
```

<FlowDiagram
  label="Жизненный цикл мёртвого кортежа: UPDATE создаёт новую живую версию и оставляет старую мёртвой; мёртвые кортежи накапливаются и раздувают таблицу; когда n_dead_tup пересекает порог autovacuum, autovacuum возвращает место под переиспользование, откатывая таблицу к её живому размеру."
  perRow={5}
  hue="var(--d-data)"
  nodes={[
    { id: "upd", label: "UPDATE/DELETE", sub: "помечает строку мёртвой" },
    { id: "dead", label: "Мёртвые кортежи", sub: "всё ещё на диске" },
    { id: "bloat", label: "Bloat растёт", sub: "сканы читают мимо них" },
    { id: "trig", label: "Порог достигнут", sub: "n_dead_tup пересекает" },
    { id: "vac", label: "Autovacuum", sub: "возвращает под переиспольз." },
  ]}
  edges={[
    { from: "upd", to: "dead" },
    { from: "dead", to: "bloat" },
    { from: "bloat", to: "trig" },
    { from: "trig", to: "vac" },
  ]}
  caption="Стабильная нагрузка даёт пилу: мёртвые кортежи накапливаются, autovacuum убирает, повтор. Bloat — это то, что происходит, когда уборка не успевает за нагрузкой."
/>

## Autovacuum: что его запускает

`autovacuum` — фоновый процесс, запускающий `VACUUM` автоматически. Он **не** ужимает файл — он помечает место мёртвых кортежей как **переиспользуемое**, чтобы новые строки заполняли дыры вместо расширения таблицы. Он также запускает `ANALYZE`, держащий статистику свежей (урок 03), и обновляет **visibility map**.

Он срабатывает на таблицу, когда:

```
n_dead_tup  >  autovacuum_vacuum_threshold + autovacuum_vacuum_scale_factor × n_live_tup
```

Дефолты: `threshold = 50`, `scale_factor = 0.2`. Так таблица автовакуумится, когда мёртвые кортежи превышают 20% живых строк. Эти 0.2 нормальны для таблицы в 10k строк (вакуум при 2k мёртвых), но **ужасны** для таблицы в 100M строк — он ждёт 20M мёртвых кортежей, к которым сканы уже раздуты. Прод-правило: снижай `scale_factor` на больших/горячих таблицах, например `ALTER TABLE events SET (autovacuum_vacuum_scale_factor = 0.02);`, чтобы он вакуумил при 2% нагрузки.

## Visibility map и index-only scans

`VACUUM` поддерживает **visibility map**: битмап, помечающий страницы, где *каждый* кортеж видим всем транзакциям. Именно это делает возможным **index-only scan** — если страница вся-видима, Postgres может ответить из одного индекса без обращения к heap для проверки видимости (глава индексов трека `databases` копает глубже). Раздутая, недовакуумленная таблица имеет устаревший visibility map, поэтому «index-only» сканы тихо деградируют в heap-обращения. Вакуум — не просто сборка мусора; это то, что держит твой быстрейший тип скана быстрым.

## VACUUM против VACUUM FULL — и прод-ловушка

- **`VACUUM` (обычный)** — возвращает мёртвое место *на месте* под переиспользование. Берёт лишь `SHARE UPDATE EXCLUSIVE` блокировку; чтения и записи продолжаются. Это то, что запускает autovacuum. Он не возвращает диск ОС, но останавливает рост раздувания.
- **`VACUUM FULL`** — переписывает всю таблицу в новый компактный файл, возвращая место ОС. Но он берёт **`ACCESS EXCLUSIVE` блокировку** на всю перезапись — каждое чтение и запись блокируются до конца. На большой таблице это минуты полного простоя. **Никогда не запускай `VACUUM FULL` на живой прод-таблице.**

Чтобы реально вернуть диск на раздутой прод-таблице, используй **`pg_repack`** (расширение): он перестраивает таблицу в свежую копию *онлайн*, беря exclusive-блокировку только на короткий финальный своп. Та же компактизация, без простоя.

<Inset kind="mistake" lang="ru">
Классический инцидент: долго работающая транзакция (зависший `BEGIN`, застрявший аналитический запрос, забытая `psql`-сессия) удерживает **xmin horizon** — старейший снимок, всё ещё в использовании. Autovacuum не может вернуть ни один кортеж новее этого горизонта, на *любой* таблице, потому что та древняя транзакция может всё ещё захотеть его увидеть. Так одно забытое соединение `idle in transaction` может раздуть bloat по всей БД, пока autovacuum работает постоянно, но не возвращает ничего. Проверь `SELECT * FROM pg_stat_activity WHERE state = 'idle in transaction' ORDER BY xact_start;` и задай `idle_in_transaction_session_timeout`, чтобы убивать их автоматически. Поэтому «autovacuum работает, но bloat растёт» почти всегда проблема долгой транзакции, а не настройки autovacuum.
</Inset>

<Quiz
  id="04-vacuum-and-bloat-quiz-1"
  lessonSlug="04-vacuum-and-bloat"
  lang="ru"
  question="Высоконагруженная таблица-очередь держит 4000 живых строк, но 9 ГБ на диске и медленна в сканировании. Что происходит?"
  choices={[
    { label: "Мёртвые кортежи от постоянных UPDATE/DELETE раздули таблицу; сканы читают мимо 99.9% мёртвого места", correct: true },
    { label: "Таблице нужно больше индексов, чтобы ускорить count", misconception: "Больше индексов не помогут count'у по раздутому heap; проблема — раздувание мёртвыми кортежами, добавляющее I/O. Фикс это вакуум/repack, а не индексация." },
    { label: "Postgres хранит 9 ГБ, потому что строки большие", misconception: "4000 живых строк не могут быть 9 ГБ живых данных; размер — это мёртвые кортежи, оставленные MVCC, которые вакуум не вернул." },
    { label: "Диск отказывает", misconception: "Это нормальное MVCC-раздувание от нагрузки, обгоняющей autovacuum, а не отказ железа." },
  ]}
/>

<Quiz
  id="04-vacuum-and-bloat-quiz-2"
  lessonSlug="04-vacuum-and-bloat"
  lang="ru"
  question="Нужно вернуть диск с сильно раздутой таблицы на живой прод-системе. Лучший инструмент?"
  choices={[
    { label: "pg_repack — перестраивает таблицу онлайн, беря exclusive-блокировку только на короткий финальный своп", correct: true },
    { label: "VACUUM FULL — он компактизирует таблицу", misconception: "VACUUM FULL берёт ACCESS EXCLUSIVE блокировку на всю перезапись, блокируя все чтения и записи на минуты — простой на живой таблице. Используй pg_repack для онлайн-компактизации." },
    { label: "Обычный VACUUM — он возвращает диск ОС", misconception: "Обычный VACUUM возвращает место под переиспользование на месте, но НЕ ужимает файл и не возвращает диск ОС; для этого нужен VACUUM FULL или pg_repack." },
    { label: "Удалить и пересоздать таблицу", misconception: "Деструктивно и не нужно; pg_repack компактизирует онлайн без потери данных и долгого простоя." },
  ]}
/>

<MetaphorComplete
  id="04-vacuum-and-bloat-metaphor-1"
  lessonSlug="04-vacuum-and-bloat"
  lang="ru"
  setup="Заполни пропуск: обычный VACUUM помечает место мёртвых кортежей как _______ для новых строк, но не ужимает файл; VACUUM FULL переписывает файл, чтобы вернуть диск ОС, ценой exclusive-блокировки."
  accepted={["переиспользуемое", "переиспользуемым", "свободное", "доступное"]}
  canonical="переиспользуемое"
  explanation="Обычный VACUUM работает на месте: он помечает мёртвое место переиспользуемым, чтобы будущие INSERT и UPDATE заполняли дыры вместо расширения файла, останавливая рост раздувания без простоя. Только VACUUM FULL (или pg_repack онлайн) реально переписывает таблицу и возвращает физический диск операционной системе."
/>

</Explanation>

<KeyTakeaway>Каждый `UPDATE`/`DELETE` при MVCC оставляет мёртвый кортеж; autovacuum возвращает это место на месте (и обновляет статистику и visibility map, питающий index-only scans). Он срабатывает при `threshold + scale_factor × live_rows` — дефолт 0.2 слишком мягок для больших таблиц, поэтому снижай `autovacuum_vacuum_scale_factor` на горячих таблицах. Bloat тормозит каждый скан. Никогда не используй `VACUUM FULL` на проде (ACCESS EXCLUSIVE блокировка); используй `pg_repack` для онлайн-компактизации. Если bloat растёт несмотря на autovacuum, ищи долгий `idle in transaction`, удерживающий xmin horizon.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="04-vacuum-and-bloat-retrieval"
  lang="ru"
  questions={[
    { q: "Почему каждый UPDATE и DELETE создаёт мёртвый кортеж и чего это стоит?", a: "При MVCC Postgres никогда не перезаписывает на месте: UPDATE пишет новую версию строки и помечает старую мёртвой, DELETE просто помечает строку мёртвой. Старая версия остаётся, потому что снимок ещё работающей транзакции может её захотеть. Пока вакуум их не вернёт, мёртвые кортежи стоят места и вынуждают каждый последовательный и индексный скан читать страницы, которые они занимают, и пропускать их — таблица на 90% мёртвая делает примерно 10x I/O за те же живые данные." },
    { q: "Что запускает autovacuum и почему дефолтный scale_factor — проблема на больших таблицах?", a: "Autovacuum вакуумит таблицу, когда n_dead_tup превышает autovacuum_vacuum_threshold + autovacuum_vacuum_scale_factor x n_live_tup, с дефолтами 50 и 0.2 (то есть при 20% мёртвых). На таблице в 100M строк 0.2 значит ждать 20M мёртвых кортежей до уборки, к которым сканы уже сильно раздуты. Фикс — снизить autovacuum_vacuum_scale_factor на больших/горячих таблицах (например, 0.02), чтобы он вакуумил при нескольких процентах нагрузки." },
    { q: "Чем различаются VACUUM, VACUUM FULL и pg_repack и что безопасно на проде?", a: "Обычный VACUUM возвращает мёртвое место на месте под переиспользование при лёгкой SHARE UPDATE EXCLUSIVE блокировке (чтения/записи идут), но не ужимает файл. VACUUM FULL переписывает таблицу в компактный новый файл и возвращает диск ОС, но держит ACCESS EXCLUSIVE блокировку на всю перезапись, блокируя весь доступ — никогда не запускай на живой таблице. pg_repack делает ту же компактизацию онлайн, беря exclusive-блокировку лишь на короткий финальный своп, поэтому это прод-безопасный способ вернуть диск." },
  ]}
/>

<Recap lang="ru">
MVCC никогда не перезаписывает строку, поэтому каждый `UPDATE` и `DELETE` оставляет **мёртвый кортеж**, задерживающийся на диске, пока вакуум его не вернёт — а до тех пор он раздувает таблицу и вынуждает каждый скан читать мимо него, легко 10× I/O на сильно нагруженной таблице. **Autovacuum** возвращает это место *на месте* (не ужимая файл), обновляет статистику и поддерживает **visibility map**, держащий index-only scans быстрыми; он срабатывает при `threshold + scale_factor × live_rows`, и дефолтный scale factor 0.2 слишком мягок для больших таблиц — снижай его на горячих таблицах. Чтобы вернуть реальный диск, `VACUUM` не вернёт (он лишь освобождает место под переиспользование), а `VACUUM FULL` вернёт, но с `ACCESS EXCLUSIVE` блокировкой, означающей простой — поэтому используй **`pg_repack`** для онлайн-компактизации. А когда autovacuum работает, но bloat растёт, причина почти всегда — долгоживущее соединение `idle in transaction`, прибивающее xmin horizon, так что ничего новее нельзя вернуть по всей БД. Мониторь `n_dead_tup` и `pg_stat_activity`; тюнь до того, как это станет ночным инцидентом.
</Recap>
