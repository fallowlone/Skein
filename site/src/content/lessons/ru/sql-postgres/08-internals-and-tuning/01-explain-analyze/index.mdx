---
slug: "01-explain-analyze"
lang: ru
track: "sql-postgres"
unit: "08-internals-and-tuning"
order: 1
title: "Как читать EXPLAIN ANALYZE"
summary: "EXPLAIN ANALYZE печатает дерево плана с пометками оценка против факта. Зазор между оценочными и фактическими строками — это сигнал, что планировщику соврали."
estMin: 15
status: ready
lessonType: topic
level: middle
concepts:
  - explain-analyze
  - estimate-vs-actual
  - plan-tree
  - loops-per-node
  - buffers
prereqs:
  - 00-start-here/02-how-postgres-executes-a-query
  - databases/03-execution-plans/01-explain-and-plans
sources:
  - https://www.postgresql.org/docs/current/using-explain.html
  - https://use-the-index-luke.com/sql/explain-plan
  - https://www.interdb.jp/pg/pgsql03.html
---

import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import StackDiagram from "~/components/diagram/StackDiagram.astro";
import SpiralCue from "~/components/prose/SpiralCue.astro";

<Hook>
Отчётный запрос упирается в таймаут 30 с на проде. Джуниор трижды переписывает SQL — не помогает. Ты один раз запускаешь `EXPLAIN ANALYZE`, и ответ лежит на третьей строке: узел говорит `rows=1` оценка, `rows=2 300 000` факт. Планировщик думал, что тронет одну строку, и построил весь join вокруг этой лжи. План никто не прочитал. Прочитать его — навык на пять минут, который предотвращает пятичасовые инциденты.
</Hook>

<Crux>EXPLAIN ANALYZE помечает каждый узел плана оценкой против факта; зазор по строкам — первое, что читает сеньор.</Crux>

<Explanation>

## Что на самом деле в выводе

`EXPLAIN ANALYZE <query>` выполняет твой запрос и печатает **дерево плана**, которое его породило, по одному узлу на строку, с отступами по вложенности. Глава об execution-plans трека `databases` выводит, *почему* планировщик строит дерево сканов и join'ов; здесь ты учишься только **читать** его как runbook (пошаговый рабочий справочник, к которому тянешься при инциденте). Каждая строка-узел несёт две группы чисел:

- **Оценки** (с планирования): `cost=0.00..431.00` (startup..total в условных единицах стоимости) и `rows=1000` — что планировщик *угадал* до запуска.
- **Факты** (с выполнения): `actual time=0.012..4.318` (мс) и `rows=998 loops=1` — что *реально произошло*.

```sql
EXPLAIN (ANALYZE, BUFFERS, FORMAT TEXT)
SELECT o.id, o.total
FROM orders o
JOIN line_items li ON li.order_id = o.id
WHERE o.status = 'shipped';
```

Реальный (урезанный) результат:

```
Hash Join  (cost=33.50..812.40 rows=1200 width=20)
            (actual time=1.2..58.7 rows=1180 loops=1)
  Hash Cond: (li.order_id = o.id)
  Buffers: shared hit=512 read=140
  ->  Seq Scan on line_items li
        (cost=0..512 rows=20000 width=12)
        (actual time=0.01..21.3 rows=20000 loops=1)
  ->  Hash  (cost=20..20 rows=300 width=12)
              (actual time=1.1..1.1 rows=305 loops=1)
        ->  Index Scan on orders o
              (cost=0.29..20 rows=300 width=12)
              (actual time=0.03..0.8 rows=305 loops=1)
              Index Cond: (status = 'shipped')
Planning Time: 0.4 ms
Execution Time: 59.4 ms
```

## Читай изнутри наружу, следи за зазором

Дерево выполняется от **самых глубоких, внутренних** узлов наружу — листья первыми, корень последним. Читай так же. Для каждого узла задай один вопрос: **совпадает ли `rows=` оценка с `rows=` факт?**

- Оценка 300, факт 305 → планировщик откалиброван хорошо. Этому поддереву можно доверять.
- Оценка 1, факт 2 300 000 → планировщик **промахнулся в оценке**. Каждое решение выше этого узла (какой алгоритм join, какой порядок join'ов) принималось на неверном числе строк, поэтому весь план над ним под подозрением. Это самый ценный сигнал во всём выводе, и весь урок 03 — про то, как это чинить.

Зазор в 10× — жёлтый флаг; зазор в 1000× — это баг. Фикс почти никогда не «переписать запрос» — это обновить статистику, добавить индекс, которому планировщик поверит, или поправить оценки коррелированных колонок.

<StackDiagram
  label="Узел плана EXPLAIN ANALYZE, разобранный: строка estimate — догадка планировщика; строка actual — реальность; зазор по строкам между ними — диагностический сигнал; loops умножает время одной итерации; buffers показывает страницы из кеша против диска."
  hue="var(--d-data)"
  layers={[
    { label: "Тип узла", note: "Hash Join / Seq Scan / Index Scan" },
    { label: "Оценка", note: "cost=.. rows=1200 (догадка)" },
    { label: "Факт", note: "time=.. rows=1180 loops=1 (реальность)" },
    { label: "Зазор строк", note: "оценка против факта = сигнал" },
    { label: "Buffers", note: "shared hit=кеш  read=диск" },
  ]}
  caption="Читай листья первыми. Для каждого узла сравнивай оценочные строки с фактическими; самый большой зазор — где планировщик ошибся и где живёт фикс."
/>

## Три числа, которые читают неверно

**Loops — `actual time` это на одну итерацию, не итого.** На внутренней стороне nested loop ты увидишь `actual time=0.05..0.06 rows=1 loops=50000`. Этот узел отработал 50 000 раз. Итоговое время на нём — примерно `время на итерацию × loops` ≈ `0.06 мс × 50000` = 3 секунды, а не 0.06 мс. Самая частая ошибка новичка — «этот узел быстрый», когда он тайком и есть всё время выполнения. Так же `rows` на узле в цикле — **среднее на итерацию**; умножай на `loops` для реального итого.

**Cost против time.** `cost` безразмерен и имеет смысл только для *сравнения* кандидатных планов внутри планировщика (урок 02). Это не миллисекунды и не сравнимо между машинами. Для диагностики реального запроса игнорируй `cost` и читай `actual time` и `rows`.

**BUFFERS — куда ушёл I/O.** `BUFFERS` добавляет `shared hit=N` (страницы по 8 КБ из буферного кеша) и `read=N` (страницы с диска/ОС). Узел с `shared read=180000` упёрт в I/O: он вытащил 180k страниц (~1.4 ГБ) с диска. Это, а не CPU, и есть твоя медлительность — фикс это более селективный индекс или меньше сканируемых данных. `BUFFERS` — разница между догадкой «может, это I/O» и доказательством этого.

<Inset kind="why" lang="ru">
Почему `EXPLAIN ANALYZE` реально выполняет запрос (и потому опасен на записях)? Потому что половина «ANALYZE» значит «инструментируй и выполни», а не «только спланируй». Запуск `EXPLAIN ANALYZE` на `UPDATE` или `DELETE` **выполняет запись**. Чтобы безопасно осмотреть изменяющий запрос, оберни его: `BEGIN; EXPLAIN ANALYZE UPDATE ...; ROLLBACK;`. Сама инструментация тоже добавляет накладные расходы (чтение часов на каждом узле), поэтому `Execution Time` здесь слегка завышен относительно голого запроса — используй его для сравнения, а не как SLA-число. `FORMAT TEXT` (по умолчанию) — для чтения глазами; `FORMAT JSON` — когда вывод будет парсить инструмент.
</Inset>

<DragOrder
  id="01-explain-analyze-drag-1"
  lessonSlug="01-explain-analyze"
  lang="ru"
  prompt="Расставь, как сеньор читает план EXPLAIN ANALYZE, чтобы найти проблему:"
  items={[
    { id: "a", label: "Запусти EXPLAIN (ANALYZE, BUFFERS) на медленном запросе" },
    { id: "b", label: "Найди самые внутренние (глубже отступ) узлы — они выполняются первыми" },
    { id: "c", label: "На каждом узле сравни оценочные строки с фактическими" },
    { id: "d", label: "Найди узел с самым большим зазором оценка-против-факта" },
    { id: "e", label: "Умножь время на итерацию на loops, чтобы получить реальную цену узла" },
    { id: "f", label: "Проверь BUFFERS: цена это чтения с диска или попадания в кеш" },
    { id: "g", label: "Выбери фикс: обновить статистику, индекс или переписать — по тому, что показали числа" },
  ]}
/>

<Quiz
  id="01-explain-analyze-quiz-1"
  lessonSlug="01-explain-analyze"
  lang="ru"
  question="Внутренний узел nested loop показывает: actual time=0.04..0.05 rows=1 loops=80000. Сколько реального времени стоит этот узел?"
  choices={[
    { label: "Около 4 секунд — время на итерацию (~0.05 мс) на 80 000 итераций", correct: true },
    { label: "Около 0.05 мс — это и есть напечатанное actual time", misconception: "actual time на узле в цикле — НА ОДНУ ИТЕРАЦИЮ. Надо умножить на loops; 0.05 мс x 80 000 это примерно 4 с — реальная цена." },
    { label: "Ноль — rows=1 значит он почти ничего не сделал", misconception: "rows=1 — среднее на итерацию, не итого. За 80 000 итераций он вернул около 80 000 строк и потратил секунды." },
    { label: "Ровно 80 000 мс, потому что loops=80000", misconception: "loops — это счётчик, не миллисекунды. Умножай loops на actual time на итерацию, а не считай счётчик циклов за мс." },
  ]}
/>

<Quiz
  id="01-explain-analyze-quiz-2"
  lessonSlug="01-explain-analyze"
  lang="ru"
  question="Узел Seq Scan читает rows=1 оценка, но rows=2 300 000 факт. О чём это говорит в первую очередь?"
  choices={[
    { label: "Статистика планировщика здесь неверна; каждый выбор join поверх этой оценки под подозрением", correct: true },
    { label: "Текст запроса плохо написан и его надо переписать", misconception: "Зазор по строкам — проблема статистики/кардинальности, а не синтаксиса. Переписывание редко чинит промах в оценке; обновление статистики или extended statistics — да." },
    { label: "Seq Scan в порядке, ведь Seq Scan всегда быстрый", misconception: "Seq Scan по 2.3M строк, которые планировщик считал за 1, — ровно сбой: он, вероятно, выбрал nested loop, ожидая одну строку, и теперь крутится миллионы раз." },
    { label: "Ничего — оценки всегда приблизительны, игнорируй их", misconception: "Зазор в 2 300 000x — это не обычная погрешность; это корневая причина плохого плана и первое, что надо чинить." },
  ]}
/>

<SpiralCue thread="latency" lang="ru" />

</Explanation>

<KeyTakeaway>EXPLAIN ANALYZE печатает дерево плана с оценкой и фактом бок о бок. Читай листья первыми, ищи самый большой зазор оценка-против-факта (там планировщика обманули), помни, что `actual time` и `rows` — на итерацию, поэтому умножай на `loops`, и используй `BUFFERS`, чтобы доказать упор в диск против кеша. `cost` нужен только для сравнения планов, это не время.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="01-explain-analyze-retrieval"
  lang="ru"
  questions={[
    { q: "Какое самое полезное число читать в выводе EXPLAIN ANALYZE и почему?", a: "Зазор между оценочными и фактическими строками на каждом узле. Если они совпадают, планировщик откалиброван и его выбору плана можно доверять. Большой зазор (10x — флаг, 1000x — баг) значит, что планировщик строил план на неверной кардинальности, поэтому каждый алгоритм join и порядок join'ов, решённые выше этого узла, под подозрением — там и живёт фикс." },
    { q: "Почему 'actual time=0.05 rows=1' на узле в цикле обманчиво и как получить реальную цену?", a: "На узле с loops>1 и actual time, и rows — это средние НА ИТЕРАЦИЮ, не итоги. Узел с actual time=0.05 мс и loops=80000 реально стоил около 0.05 x 80000 = 4 секунды и вернул около 80 000 строк. Умножай время на итерацию на loops, чтобы получить вклад узла в общее время." },
    { q: "Что добавляет BUFFERS и что говорят shared hit против read?", a: "BUFFERS сообщает обращения к страницам по 8 КБ на узел: shared hit=N — страницы из буферного кеша, read=N — страницы с диска/ОС. Высокий read значит, что узел упёрт в I/O (холодный или большой рабочий набор), поэтому фикс это более селективный индекс или меньше данных; в основном hit при высоком времени указывает наоборот на работу CPU/памяти вроде большой сортировки." },
  ]}
/>

<Recap lang="ru">
`EXPLAIN ANALYZE` выполняет запрос и печатает выбранное **дерево плана**, по одному узлу с отступом на строку, и каждый несёт **оценку** планировщика (`cost`, `rows`) рядом с **фактом** исполнителя (`time`, `rows`, `loops`). Читай изнутри наружу — глубочайшие узлы выполняются первыми — и на каждом узле сравнивай оценочные строки с фактическими: самый большой зазор — где планировщика обманули и где живёт реальный фикс (статистика, индекс, оценки коррелированных колонок — уроки 02 и 03), а не переписывание запроса. Следи за двумя ловушками: на узлах в цикле `actual time` и `rows` — *на итерацию*, поэтому умножай на `loops` для реальной цены; а `cost` — безразмерная мерка планирования, не миллисекунды. Добавь `BUFFERS`, чтобы превратить «похоже на I/O» в доказательство — `shared hit` это кеш, `read` это диск. Теперь, когда видишь медленный запрос, твой первый ход — `EXPLAIN (ANALYZE, BUFFERS)`, а первое, что ищешь — зазор оценка-против-факта по строкам на каждом узле, а не новый индекс и не переписывание.
</Recap>
