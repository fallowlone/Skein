---
slug: "06-tuning-workflow"
lang: ru
track: "sql-postgres"
unit: "08-internals-and-tuning"
order: 6
title: "Рабочий процесс тюнинга"
summary: "Систематический цикл: найти медленный запрос через pg_stat_statements, воспроизвести через EXPLAIN (ANALYZE, BUFFERS), диагностировать настоящую причину, применить наименьший фикс, проверить, повторить. Измеряй, не гадай."
estMin: 16
status: ready
lessonType: topic
level: senior
concepts:
  - pg-stat-statements
  - reproduce-with-explain
  - diagnose-root-cause
  - smallest-fix
  - measure-dont-guess
prereqs:
  - 05-wal-and-durability
  - 01-explain-analyze
sources:
  - https://www.postgresql.org/docs/current/pgstatstatements.html
  - https://www.postgresql.org/docs/current/using-explain.html
  - https://use-the-index-luke.com/
---

import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";
import SpiralCue from "~/components/prose/SpiralCue.astro";

<Hook>
Два инженера получают один тикет: «приложение тормозит». Первый открывает кодовую базу и начинает вешать индексы на каждую колонку, которую трогает `WHERE` — через три деплоя не быстрее, а записи теперь медленнее. Второй запускает один запрос к `pg_stat_statements`, находит, что 78% общего времени БД — это один отчётный запрос, делает `EXPLAIN`, видит промах оценки, запускает `ANALYZE` и закрывает тикет за двадцать минут. Потолок навыка тот же, исход противоположный. Разница — это *цикл*, а не трюк. Этот урок — цикл, связывающий весь раздел воедино.
</Hook>

<Crux>Найди дорогой запрос, воспроизведи EXPLAIN, диагностируй причину, примени наименьший фикс, проверь, повтори — измеряй, не гадай.</Crux>

<Explanation>

## Шаг 1 — найди запрос, который реально важен

Нельзя тюнить то, что не измерил, а интуиция про «медленный запрос» почти всегда неверна. Первый ход — **`pg_stat_statements`** — расширение, агрегирующее каждый выполненный запрос (нормализованный, со снятыми литералами) и отслеживающее его суммарную стоимость. Важное число — **`total_exec_time`**, а не `mean_exec_time`: запрос на 5 мс, выполненный 2 миллиона раз, стоит больше общего времени БД, чем 4-секундный отчёт, запущенный дважды, и срезание первого экономит больше.

```sql
-- Запросы, съедающие больше всего общего времени на сервере:
SELECT
  substring(query, 1, 60)        AS query,
  calls,
  round(total_exec_time)         AS total_ms,
  round(mean_exec_time, 2)       AS mean_ms,
  round(100 * total_exec_time / sum(total_exec_time) OVER (), 1) AS pct
FROM pg_stat_statements
ORDER BY total_exec_time DESC
LIMIT 10;
```

Читай колонку `pct`: тюнинг это **Парето**. Если один запрос — 78% общего времени, это весь твой день; остальное — шум, пока не станет важным. Сортируй по `total_exec_time`, бери верхний, игнорируй всё остальное.

## Шаги 2–4 — воспроизведи, диагностируй, почини наименьшее

Когда у тебя есть *тот самый* запрос, цикл механистичен:

- **Воспроизведи** через `EXPLAIN (ANALYZE, BUFFERS)` (урок 01). Это истина в последней инстанции — никогда не тюнь по одному тексту запроса.
- **Диагностируй**, сопоставив улики с одной корневой причиной (весь раздел, сжато):
  - Большой **зазор оценка-против-факта** по строкам? → устаревшая или предполагающая независимость статистика → `ANALYZE` или `CREATE STATISTICS` (урок 03).
  - **Seq Scan** на селективном фильтре, высокие `read=` буферы? → отсутствующий индекс → добавь (`CONCURRENTLY`), по главе индексов трека databases.
  - Живых строк мало, но **буферы/размер огромны**? → bloat → вакуум/`pg_repack` (урок 04).
  - Оценки в порядке, скан верный, всё равно медленно? → данных правда слишком много → перепиши, пагинируй или предагрегируй.
- **Примени наименьший фикс**, адресующий *эту* причину. Одно изменение. Точечный `ANALYZE` лучше нового индекса; новый индекс лучше переписывания; переписывание лучше изменения конфига — в этом порядке локальности, ведь наименьшее изменение имеет наименьший радиус поражения.
- **Проверь**, перезапустив `EXPLAIN (ANALYZE, BUFFERS)` и убедившись, что число сдвинулось. Затем пересмотри `pg_stat_statements` после реального трафика — иногда фикс смещает узкое место на следующий запрос, и цикл повторяется.

<FlowDiagram
  label="Цикл тюнинга как круг: найди самый дорогой запрос в pg_stat_statements, воспроизведи через EXPLAIN ANALYZE BUFFERS, диагностируй единственную корневую причину, примени наименьший фикс, проверь, что число сдвинулось, затем вернись к поиску нового верхнего запроса."
  perRow={5}
  hue="var(--d-data)"
  nodes={[
    { id: "find", label: "Найти", sub: "pg_stat_statements" },
    { id: "repro", label: "Воспроизвести", sub: "EXPLAIN ANALYZE" },
    { id: "diag", label: "Диагностировать", sub: "одна причина" },
    { id: "fix", label: "Наим. фикс", sub: "ANALYZE/индекс/переписать" },
    { id: "verify", label: "Проверить", sub: "число сдвинулось?" },
  ]}
  edges={[
    { from: "find", to: "repro" },
    { from: "repro", to: "diag" },
    { from: "diag", to: "fix" },
    { from: "fix", to: "verify" },
    { from: "verify", to: "find" },
  ]}
  caption="Это круг, а не чек-лист: проверка возвращает к поиску, ведь фикс часто продвигает следующий запрос наверх. Останавливайся, когда верхний запрос приемлем, а не когда кончились идеи."
/>

## Почему «измеряй, не гадай» — это вся дисциплина

Каждый анти-паттерн тюнинга — это догадка, пропустившая шаг измерения:

- **Преждевременная индексация** — добавление индексов «на всякий случай» до того, как хоть один запрос доказал, что нужен. Каждый индекс облагает налогом каждую запись (глава индексов трека databases это количественно оценивает) и может вводить планировщик в заблуждение; неиспользуемый индекс — чистая цена. Добавляй индекс только после того, как `EXPLAIN` покажет Seq Scan, который он уберёт.
- **Карго-культ конфига** — копирование значений `random_page_cost` или `work_mem` из блога без измерения своей нагрузки.
- **Слепое переписывание** — джуниор из хука переписал SQL трижды, потому что *текст* казался медленным, когда реальной причиной была устаревшая оценка, которую переписывание не могло тронуть.

Сеньорский рефлекс обратен: каждому изменению предшествует измерение, его оправдывающее, и следует измерение, его подтверждающее. Это и заставляет цикл сходиться, а не молотить впустую. (Капстоун раздела 09 прогоняет этот самый цикл от начала до конца на реалистичном аналитическом API — этот урок — метод, который он применяет.)

<Inset kind="why" lang="ru">
Почему сортировать по `total_exec_time`, а не `mean_exec_time`? Потому что ты оптимизируешь бюджет времени *сервера*, а общее время — это то, что пользователи коллективно ждут. Запрос со средним 2 мс, но вызываемый 50 миллионов раз в день, жжёт куда больше совокупного времени — и больше конкуренции за блокировки, больше оборота буферов, больше CPU — чем 30-секундный месячный отчёт. Починка запроса с 2 мс до 1 мс экономит больше реального времени по всем пользователям, чем вдвое срезанный отчёт. `mean_exec_time` важен для SLA латентности *одного* пользовательского запроса, но для «где база проводит свою жизнь?» доминирует total. Используй обе линзы: total — найти системную цену, mean — защитить конкретную интерактивную точку.
</Inset>

<DragOrder
  id="06-tuning-workflow-drag-1"
  lessonSlug="06-tuning-workflow"
  lang="ru"
  prompt="Расставь систематический цикл тюнинга для тикета «приложение тормозит»:"
  items={[
    { id: "a", label: "Запроси pg_stat_statements; сортируй по total_exec_time, чтобы найти самый дорогой запрос" },
    { id: "b", label: "Воспроизведи этот запрос через EXPLAIN (ANALYZE, BUFFERS)" },
    { id: "c", label: "Диагностируй единственную корневую причину: промах оценки, отсутствующий индекс, bloat или слишком много данных" },
    { id: "d", label: "Примени наименьший фикс, адресующий эту причину (например, ANALYZE до добавления индекса)" },
    { id: "e", label: "Проверь, перезапустив EXPLAIN и убедившись, что число реально сдвинулось" },
    { id: "f", label: "Пересмотри pg_stat_statements после трафика; вернись к новому верхнему запросу" },
  ]}
/>

<Quiz
  id="06-tuning-workflow-quiz-1"
  lessonSlug="06-tuning-workflow"
  lang="ru"
  question="В pg_stat_statements какая колонка говорит, где база тратит больше всего времени в целом?"
  choices={[
    { label: "total_exec_time — суммарное время по всем вызовам этого запроса", correct: true },
    { label: "mean_exec_time — среднее на вызов", misconception: "mean важен для латентности одного запроса, но быстрый запрос, вызываемый миллионы раз, может доминировать в общем времени сервера даже при крошечном mean. Сортируй по total_exec_time, чтобы найти системную цену." },
    { label: "calls — просто число выполнений", misconception: "Один счётчик вызовов ничего не говорит о стоимости; запрос может вызываться часто, но быть дешёвым. total_exec_time объединяет частоту и цену на вызов." },
    { label: "rows — число возвращённых строк", misconception: "Число возвращённых строк — не метрика времени; запрос может вернуть мало строк, но быть дорогим (например, огромный скан с тугим фильтром)." },
  ]}
/>

<Quiz
  id="06-tuning-workflow-quiz-2"
  lessonSlug="06-tuning-workflow"
  lang="ru"
  question="EXPLAIN ANALYZE показывает большой зазор оценка-против-факта и nested loop. Какой наименьший верный первый фикс?"
  choices={[
    { label: "Запустить ANALYZE (или CREATE STATISTICS для коррелированных колонок), чтобы починить оценку, затем пересмотреть план", correct: true },
    { label: "Добавить индексы на каждую колонку запроса", misconception: "Это преждевременная индексация — облагает записи и не адресует промах оценки. Зазор по строкам указывает на статистику, наименьший и самый локальный фикс; проверь до того, как тянуться к индексам." },
    { label: "Переписать запрос с нуля", misconception: "Переписывание — изменение большого радиуса поражения, обычно неспособное починить промах кардинальности; сначала почини статистику, наименьшее изменение, целящее в реальную причину." },
    { label: "Увеличить CPU сервера", misconception: "Железо не чинит планировщик, накормленный неверным числом строк; он всё равно выберет плохой nested loop. Почини оценку, чтобы планировщик сам выбрал hash join." },
  ]}
/>

<SpiralCue thread="latency" lang="ru" />

</Explanation>

<KeyTakeaway>Цикл тюнинга: (1) найди самый дорогой запрос через `pg_stat_statements`, сортируя по `total_exec_time` (тюнинг — Парето: один запрос часто и есть большая часть времени); (2) воспроизведи через `EXPLAIN (ANALYZE, BUFFERS)`; (3) диагностируй одну корневую причину — промах оценки, отсутствующий индекс, bloat или слишком много данных; (4) примени наименьший фикс (`ANALYZE` до индекса до переписывания до конфига); (5) проверь, что число сдвинулось, затем повтори. Каждое изменение оправдано измерением до и подтверждено измерением после — измеряй, не гадай.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="06-tuning-workflow-retrieval"
  lang="ru"
  questions={[
    { q: "Каков первый шаг цикла тюнинга и по какой колонке pg_stat_statements сортировать и почему?", a: "Сначала найди запрос, который реально стоит больше всего, через pg_stat_statements, сортируя по total_exec_time (суммарное время по всем вызовам) — не по mean_exec_time. Тюнинг — Парето: быстрый запрос, вызванный миллионы раз, жжёт больше общего времени сервера, чем медленный отчёт, запущенный дважды, поэтому total_exec_time указывает на изменение, экономящее больше всего. Часто один запрос — большинство общего времени; почини его и игнорируй остальное, пока оно не станет важным." },
    { q: "Имея EXPLAIN ANALYZE, как сопоставить улики с наименьшим фиксом?", a: "Сопоставь симптом с одной корневой причиной: большой зазор оценка-против-факта значит устаревшую или предполагающую независимость статистику, поэтому ANALYZE или CREATE STATISTICS; Seq Scan на селективном фильтре с высокими read-буферами значит отсутствующий индекс, поэтому добавь его CONCURRENTLY; мало живых строк, но огромный размер/буферы значит bloat, поэтому вакуум или pg_repack; верные оценки и правильный скан, но всё равно медленно — значит данных правда слишком много, поэтому перепиши/пагинируй/предагрегируй. Применяй наименьший, самый локальный фикс первым — ANALYZE до индекса до переписывания до конфига — затем проверь." },
    { q: "Почему «измеряй, не гадай» — ядро дисциплины и какие анти-паттерны это предотвращает?", a: "Потому что у каждого изменения есть цена и риск, и без измерения нельзя знать, помогло ли. Это предотвращает преждевременную индексацию (индексы «на всякий случай», лишь облагающие записи и сбивающие планировщик), карго-культ конфига (копирование work_mem/random_page_cost без проверки своей нагрузки) и слепые переписывания (смена текста SQL, когда реальная причина — устаревшая оценка). Рефлекс: измерение оправдывает каждое изменение, а другое его подтверждает, так что цикл сходится, а не молотит впустую." },
  ]}
/>

<Recap lang="ru">
Тюнинг — это **цикл**, и его прогон отличает сеньорскую работу от барахтанья. **Найди** запрос, который реально важен, через `pg_stat_statements`, сортируя по `total_exec_time` — тюнинг это Парето, и дешёвый запрос, выполненный миллионы раз, перевешивает редкий дорогой. **Воспроизведи** его через `EXPLAIN (ANALYZE, BUFFERS)`; никогда не тюнь по тексту. **Диагностируй** единственную корневую причину, читая улики — большой зазор оценка-против-факта значит статистику (урок 03), Seq Scan на селективном фильтре значит отсутствующий индекс, мало живых строк в огромной таблице значит bloat (урок 04), а чистый план, всё равно медленный, значит данных правда слишком много. **Примени наименьший фикс** для *этой* причины, в порядке локальности — `ANALYZE` до индекса до переписывания до конфига — ведь наименьшее изменение имеет наименьший радиус поражения. **Проверь**, что число сдвинулось, затем вернись в начало, ведь фикс часто продвигает следующий запрос наверх. Связующая нить — *измеряй, не гадай*: каждому изменению предшествует измерение, его оправдывающее, и следует измерение, его подтверждающее. Теперь, когда приходит тикет «приложение тормозит», ты знаешь: первый запрос, который нужно запустить, — не в кодовой базе, а к `pg_stat_statements`.
</Recap>
