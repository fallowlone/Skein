---
slug: "04-arrays-and-enums"
lang: ru
track: "sql-postgres"
unit: "06-types-and-modeling"
order: 4
title: "Массивы и enum: когда каждый бьёт строку"
summary: "Нативные массивы (ANY, unnest, @> с GIN) и enum (упорядоченные, компактные) мощны на верной границе — но боль ALTER у enum и враждебность массивов к джойнам решают, когда побеждает lookup-таблица."
estMin: 15
status: ready
lessonType: topic
level: middle
concepts:
  - postgres-arrays
  - array-containment
  - enum-type
  - enum-vs-lookup-table
  - any-all-unnest
prereqs:
  - 03-jsonb-deep
sources:
  - https://www.postgresql.org/docs/current/arrays.html
  - https://www.postgresql.org/docs/current/datatype-enum.html
  - https://www.postgresql.org/docs/current/functions-array.html
---

import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import StackDiagram from "~/components/diagram/StackDiagram.astro";

<Hook>
Команда задала `status` как enum с тремя значениями, выкатила, а через полгода продукт попросил четвёртое значение — *между* двумя существующими. Добавить в конец — одна строка; вставить в середину, а позже вообще удалить значение — это превратилось в многочасовую миграцию, переписавшую зависимое представление и функцию. Тем временем другая команда взяла `text[]` для «ролей» и обнаружила, что не может поставить внешний ключ на id ролей. Оба типа отличны — на границе, где их компромиссы это фичи, а не сюрпризы. Этот урок о том, как найти эту границу.
</Hook>

<Crux>Массивы и enum бьют строки на верной границе; боль ALTER у enum и враждебность массивов к джойнам говорят, когда побеждает lookup-таблица.</Crux>

<Explanation>

## Нативные массивы: настоящий первоклассный тип

Массивы Postgres — не хак: любой тип может быть массивом (`int[]`, `text[]`, даже `jsonb[]`), с синтаксисом литерала и богатым набором операторов. Они блестят для маленького, упорядоченного, однотабличного списка, к которому ты никогда не джойнишь: теги строки, набор флагов, путь.

```sql
CREATE TABLE products (
  id   bigint PRIMARY KEY,
  tags text[] NOT NULL DEFAULT '{}'        -- литерал массива: пустой массив
);

INSERT INTO products (id, tags) VALUES (1, '{sale,featured,new}');

SELECT * FROM products WHERE 'sale' = ANY(tags);      -- проверка вхождения
SELECT * FROM products WHERE tags @> '{sale,new}';    -- содержит ВСЕ из этих
SELECT id, unnest(tags) AS tag FROM products;         -- развернуть в строки
SELECT array_agg(id) FROM products WHERE 'sale' = ANY(tags);  -- свернуть в массив
```

Ключевые операторы: `ANY(arr)` / `ALL(arr)` для «совпадает с каким-то / каждым элементом», `@>` containment («содержит все из»), `unnest()` развернуть массив в строки и `array_agg()` свернуть строки обратно в массив. И массивы индексируемы — **GIN** (Generalized Inverted Index, обобщённый инвертированный индекс) на `text[]` делает запросы containment `@>` и `= ANY` быстрыми, ровно как у JSONB.

```sql
CREATE INDEX ON products USING gin (tags);   -- теперь @> и = ANY используют индекс
```

Когда ловишь себя на том, что хочешь сохранить список связанных id в массиве, — сделай паузу здесь. Загвоздка, решающая всё: **нельзя поставить внешний ключ на элемент массива.** Поле `tag_id int[]` может держать id, которых нет ни в одной таблице tags, и Postgres не остановит это. Массивы меняют ссылочную целостность и производительность джойнов на локальность и простоту. Отлично для самодостаточного списка; неверно для связи.

## Enum: упорядоченный и компактный

`enum` — кастомный тип с фиксированным, *упорядоченным* набором меток-значений. Он компактен (хранится как 4-байтовый oid, а не полный текст), и порядок осмыслен — сравнения следуют порядку объявления, что идеально для вещей вроде степени серьёзности или стадий workflow.

```sql
CREATE TYPE order_status AS ENUM ('pending', 'paid', 'shipped', 'delivered');

CREATE TABLE orders (
  id     bigint PRIMARY KEY,
  status order_status NOT NULL DEFAULT 'pending'
);

-- упорядочивание следует порядку объявления, не алфавиту:
SELECT * FROM orders WHERE status >= 'shipped';   -- shipped + delivered
```

Это упорядочивание — суперсила enum над `CHECK (status IN (...))` на текстовой колонке: произвольные строки нельзя естественно упорядочить по стадии workflow, а сравнения enum просто работают.

## Боль ALTER у enum

Вот компромисс, кусающий в проде. `ADD VALUE` в enum прост и онлайн в современном Postgres:

```sql
ALTER TYPE order_status ADD VALUE 'returned';            -- в конец, нормально
ALTER TYPE order_status ADD VALUE 'cancelled' BEFORE 'paid';  -- с позицией, тоже нормально
```

Но **нельзя удалить или переименовать-прочь значение** без фактической пересборки типа: создать новый enum, изменить каждую колонку, использующую его, удалить старый тип и пересоздать зависимые представления и функции — настоящая миграция, не однострочник. Enum — храповик в одну сторону: легко добавлять, больно сжимать. Если твой набор значений действительно фиксирован на жизнь системы (уровни серьёзности, группы крови), компактность и порядок enum — чистый выигрыш. Если он churn'ит — категории, которые бизнес постоянно переустраивает, — жёсткость становится повторяющимся налогом.

## Enum против CHECK против lookup-таблицы

Три способа смоделировать «колонку из небольшого набора», на спектре от жёстко-и-быстро до гибко-и-реляционно:

<StackDiagram
  label="Три способа смоделировать небольшой набор значений: enum-тип компактен и упорядочен, но больно сжимать; CHECK на text гибок и легко правится, но без порядка; lookup-таблица с внешним ключом полностью реляционна, позволяет прикрепить метаданные и удалять значения, ценой джойна."
  hue="var(--d-data)"
  layers={[
    { label: "ENUM-тип", note: "компактно + упорядоченно · ADD легко, REMOVE больно" },
    { label: "CHECK (x IN ...)", note: "text + гибко · правишь ограничение, без порядка" },
    { label: "Lookup-таблица + FK", note: "реляционно · метаданные, удаление значений · ценой джойна" },
  ]}
  caption="Выбирай по churn и потребности в метаданных: фиксированный упорядоченный набор → enum; маленький гибкий набор без метаданных → CHECK; развивающийся набор или нужны доп. колонки (метки, порядок, флаг активности) → lookup-таблица с внешним ключом."
/>

- **Enum** — набор значений фиксирован и порядок важен. Компактно, быстро, самодокументируемо; боль ALTER при удалении.
- **CHECK (`status IN ('a','b')`)** на колонке `text` — маленький набор, порядок не нужен, легко править (просто измени ограничение). Без порядка, чуть больше хранения, без метаданных.
- **Lookup-таблица** (`statuses(code text PK, label text, sort_order int, is_active bool)`) с внешним ключом — реляционный ответ, когда набор развивается, нужны метаданные или нужен внешний ключ. Платишь джойном, но можешь добавлять/удалять/переименовывать значения обычными `INSERT`/`UPDATE`/`DELETE` и прикрепить сколько угодно атрибутов.

Сеньорская эвристика: **фиксировано + упорядочено → enum; маленькое + гибкое → CHECK; развивается или нужны метаданные → lookup-таблица.** Дефолт на lookup-таблицу редко неверен; дефолт на enum для churn'ящего набора — это будущая миграция, которую ты сам себе запланировал.

<Inset kind="mistake" lang="ru">
Классическое злоупотребление массивами: хранение `tag_ids int[]` для моделирования товар-к-тегу. Кажется опрятным, но нельзя поставить внешний ключ на id (заводятся сироты), нельзя эффективно ответить «у каких товаров этот тег» без GIN-индекса, да и с ним это неуклюжее, чем джойн, а добавление одного тега переписывает весь массив (и строку). Реляционный ответ — джойн-таблица `product_tags(product_id, tag_id)` — даёт целостность, двусторонние джойны и дешёвые однострочные вставки. Резервируй массивы для списков, которые ты никогда не связываешь с другими таблицами.
</Inset>

<DragOrder
  id="04-arrays-and-enums-drag-1"
  lessonSlug="04-arrays-and-enums"
  lang="ru"
  prompt="Небольшой набор значений развивается и теперь нужны метка для показа и порядок сортировки. Упорядочь шаги перехода с enum на lookup-таблицу:"
  items={[
    { id: "a", label: "Создай lookup-таблицу: statuses(code text PRIMARY KEY, label text, sort_order int)" },
    { id: "b", label: "Засей её текущими значениями enum плюс новые колонки метаданных" },
    { id: "c", label: "Добавь в orders колонку status_code text с FK на statuses(code)" },
    { id: "d", label: "Backfill status_code из существующей enum-колонки" },
    { id: "e", label: "Переключи чтения/записи на status_code, затем удали старую enum-колонку и тип" },
  ]}
  correctOrder={["a", "b", "c", "d", "e"]}
/>

<Quiz
  id="04-arrays-and-enums-quiz-1"
  lessonSlug="04-arrays-and-enums"
  lang="ru"
  question="Почему массив tag_id int[] — плохой способ смоделировать связь многие-ко-многим товар-к-тегу?"
  choices={[
    { label: "На элементы массива нельзя поставить внешний ключ, поэтому id могут стать сиротами, а джойны/правки неуклюжее джойн-таблицы", correct: true },
    { label: "Массивы Postgres вообще нельзя индексировать", misconception: "Массивы можно индексировать GIN для @> и = ANY. Настоящая проблема — нет внешнего ключа на элементах и плохая реляционная джойнируемость, а не индексируемость." },
    { label: "Массивы могут держать только text, не id", misconception: "Массивы держат любой тип, включая int. Проблема в ссылочной целостности и производительности джойнов, а не в типе элемента." },
    { label: "Массивы ограничены 3 элементами", misconception: "Такого крошечного лимита нет. Проблема в моделировании связи без внешних ключей, а не в размере массива." },
  ]}
/>

<Quiz
  id="04-arrays-and-enums-quiz-2"
  lessonSlug="04-arrays-and-enums"
  lang="ru"
  question="Набор статусов постоянно меняется — значения добавляются, переупорядочиваются, иногда удаляются, и продукт хочет метку для показа на значение. Лучшая модель?"
  choices={[
    { label: "Lookup-таблица с внешним ключом — легко добавлять/удалять/переименовывать и может нести метаданные", correct: true },
    { label: "Enum — он самый компактный", misconception: "Компактность не помогает, когда набор churn'ит: удаление значения enum — болезненная пересборка типа, а enum не несёт колонку-метку. Churn'ящему набору с метаданными нужна lookup-таблица." },
    { label: "Массив text[]", misconception: "Массив моделирует список на строку, а не общий развивающийся набор валидных значений с метаданными. Это одиночная колонка из развивающегося набора — lookup-таблица." },
    { label: "Колонка jsonb", misconception: "jsonb не добавляет здесь целостности или порядка и не может поставить внешний ключ на значение. Для развивающегося, помеченного набора значений реляционно подходит lookup-таблица." },
  ]}
/>

</Explanation>

<KeyTakeaway>Нативные массивы (`ANY`/`ALL`, `unnest`, `array_agg`, `@>` с GIN-индексом) хороши для самодостаточного списка на строку, но на элемент нельзя поставить внешний ключ — поэтому они неверны для связей. Enum компактны и упорядочены, идеальны для *фиксированного* набора, но удаление значения — болезненная пересборка типа. Моделируй небольшой набор значений верным инструментом: фиксировано + упорядочено → enum; маленькое + гибкое → `CHECK (... IN ...)`; развивается или нужны метаданные → lookup-таблица с внешним ключом. Многие-ко-многим — всегда джойн-таблица, никогда массив id.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="04-arrays-and-enums-retrieval"
  lang="ru"
  questions={[
    { q: "Каковы ключевые операторы массивов и что массивы не могут, что решает против них для связей?", a: "= ANY(arr) / ALL(arr) проверяют вхождение по какому-то/каждому элементу, @> проверяет containment (содержит все из), unnest() разворачивает массив в строки, array_agg() сворачивает строки в массив; GIN-индекс делает @> и = ANY быстрыми. Решающее ограничение: нельзя поставить внешний ключ на элемент массива, так что массив id может держать сиротские id без ссылочной целостности — поэтому многие-ко-многим принадлежит джойн-таблице, а не массиву." },
    { q: "Что легко и что больно в изменении enum и что это значит для моделирования?", a: "Добавить значение легко и онлайн — ALTER TYPE ... ADD VALUE 'x' (опционально BEFORE/AFTER существующего). Удалить или переименовать-прочь значение больно: нет DROP VALUE, так что ты фактически пересобираешь тип (новый enum, изменить каждую колонку, удалить старый тип, пересоздать зависимые представления/функции). Поэтому enum подходит наборам значений, действительно фиксированным и где важен порядок; churn'ящий набор должен быть lookup-таблицей." },
    { q: "Дай эвристику enum против CHECK против lookup-таблицы.", a: "Фиксированный + упорядоченный набор → enum (компактно, порядок по объявлению, ADD легко / REMOVE больно). Маленький, гибкий набор без потребности в порядке или метаданных → CHECK (status IN ('a','b')) на текстовой колонке (просто измени ограничение). Развивающийся набор или такой, что нуждается в метаданных (метка для показа, порядок сортировки, флаг активности) или должен быть с внешним ключом → lookup-таблица с внешним ключом (стоит джойна, но добавление/удаление/переименование — обычный DML). Дефолт на lookup-таблицу редко неверен." },
  ]}
/>

<Recap lang="ru">
Массивы Postgres — настоящий первоклассный тип — любой тип элемента, с вхождением `= ANY`/`ALL`, containment `@>`, `unnest`, `array_agg` и GIN-индексацией — идеальны для маленького, самодостаточного списка на строку, к которому ты никогда не джойнишь. Их жёсткий предел — элемент массива не может нести внешний ключ, так что массив id тихо допускает сирот и неверен для связи; многие-ко-многим — всегда джойн-таблица. Enum компактны (4-байтовый oid) и *упорядочены* (сравнения следуют порядку объявления, бьют текстовый `CHECK`, когда важен порядок стадий), но это храповик в одну сторону: `ADD VALUE` прост и онлайн, а удаление значения — болезненная пересборка типа, тянущая за собой зависимые представления и функции. Так что моделируй небольшой набор значений осознанно — фиксированное и упорядоченное благоволит enum, маленькое и гибкое — `CHECK`, а развивающийся набор или такой, что нуждается в метаданных, — lookup-таблице с внешним ключом, где добавление/удаление/переименование — обычный DML (Data Manipulation Language: INSERT/UPDATE/DELETE). Теперь, когда увидишь колонку `tag_ids int[]` или enum для набора значений, который продукт продолжает пересматривать — ты будешь знать, какой инструмент несёт скрытую цену и к чему тянуться вместо него.
</Recap>
