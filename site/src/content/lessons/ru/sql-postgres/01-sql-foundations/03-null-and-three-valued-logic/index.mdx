---
slug: "03-null-and-three-valued-logic"
lang: ru
track: "sql-postgres"
unit: "01-sql-foundations"
order: 3
title: "NULL и трёхзначная логика"
summary: "NULL значит «неизвестно», а не «пусто». Это делает логику SQL трёхзначной (TRUE/FALSE/UNKNOWN) и порождает тихий баг NOT IN, возвращающий ноль строк в день, когда появляется один NULL."
estMin: 15
status: ready
lessonType: topic
level: middle
concepts:
  - null-semantics
  - three-valued-logic
  - is-null-vs-equals
  - not-in-null-trap
  - coalesce-nullif
prereqs:
  - 02-filtering-rows
sources:
  - https://www.postgresql.org/docs/current/functions-comparison.html
  - https://www.postgresql.org/docs/current/functions-conditional.html
  - https://www.postgresql.org/docs/current/sql-select.html
---

import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import MetaphorComplete from "~/components/pedagogy/MetaphorComplete.astro";
import StackDiagram from "~/components/diagram/StackDiagram.astro";
import Infographic from "~/components/diagram/Infographic.astro";
import notInFix from "./infographics/not-in-fix.svg?raw";

<Hook>
Фрод-фильтр год работал чисто: `WHERE user_id NOT IN (SELECT user_id FROM blocked_users)`. Однажды ночью он вернул **ноль** помеченных аккаунтов — не несколько, ноль — и дежурный решил, что фрод-кольцо затихло. Оно не затихло. Кто-то вставил одну строку `blocked_users` с `NULL` user_id, и этот единственный `NULL` молча превратил весь `NOT IN` в «не совпадать ни с чем». Ни ошибки, ни предупреждения. Это самый дорогой NULL-баг в SQL, и он прямо следует из одного факта: в SQL `NULL` значит *неизвестно*.
</Hook>

<Crux>NULL значит «неизвестно», так что x = NULL никогда не TRUE — это UNKNOWN. Это третье значение истины и ломает NOT IN, тихо теряя строки.</Crux>

<Explanation>

## NULL — это «неизвестно», а не «пусто» или «ноль»

Единственная идея, на которой держится всё остальное: `NULL` — не значение, а *отсутствие* известного значения, «мы не знаем». Это не ноль, не пустая строка, не false. Два `NULL` не равны друг другу, потому что «неизвестно» нельзя подтвердить равным «неизвестно».

Значит, сравнения с `NULL` не возвращают `TRUE` или `FALSE` — они возвращают третье значение истины, **UNKNOWN**. `5 = NULL` — это UNKNOWN. `NULL = NULL` — UNKNOWN. `NULL <> 3` — UNKNOWN. SQL поэтому — **трёхзначная логика**: TRUE, FALSE и UNKNOWN.

Жёсткое правило, которое из этого следует: `WHERE` оставляет строку, только когда её предикат вычисляется в **TRUE**. UNKNOWN — это *не* TRUE, так что строка, чей предикат UNKNOWN, отбрасывается — ровно как при FALSE. Эта симметрия — источник почти всех NULL-сюрпризов.

```sql
-- users(id, email, deleted_at)  -- deleted_at равен NULL у активных пользователей
SELECT count(*) FROM users WHERE deleted_at = NULL;   -- всегда 0 строк!
SELECT count(*) FROM users WHERE deleted_at IS NULL;  -- корректная проверка
```

`= NULL` — никогда не та проверка, что тебе нужна. Чтобы спросить «это неизвестно?», нужно использовать специальные операторы `IS NULL` / `IS NOT NULL`, возвращающие настоящие булевы значения.

## Трёхзначные таблицы истинности

Каждой связке нужно определить, что она делает, когда операнд UNKNOWN. Ключевые записи:

<StackDiagram
  label="Результаты трёхзначной логики. TRUE OR UNKNOWN это TRUE, потому что OR уже удовлетворён. FALSE AND UNKNOWN это FALSE, потому что AND уже обречён. Но TRUE AND UNKNOWN это UNKNOWN, FALSE OR UNKNOWN это UNKNOWN, а NOT UNKNOWN остаётся UNKNOWN."
  hue="var(--d-data)"
  layers={[
    { label: "TRUE  OR  UNKNOWN", note: "= TRUE" },
    { label: "FALSE AND UNKNOWN", note: "= FALSE" },
    { label: "TRUE  AND UNKNOWN", note: "= UNKNOWN" },
    { label: "FALSE OR  UNKNOWN", note: "= UNKNOWN" },
    { label: "NOT UNKNOWN", note: "= UNKNOWN" },
    { label: "WHERE оставляет строку, если", note: "предикат = TRUE" },
  ]}
  caption="UNKNOWN ведёт себя как «может быть и так, и эдак». OR с TRUE уже TRUE; AND с FALSE уже FALSE; всё остальное с UNKNOWN остаётся UNKNOWN — а UNKNOWN-строки отбрасываются WHERE, как и FALSE."
/>

Две строки короткого замыкания важны на практике: `TRUE OR <что угодно>` — это TRUE, а `FALSE AND <что угодно>` — это FALSE, даже когда «что угодно» — UNKNOWN, потому что результат уже определён. Любая другая комбинация, касающаяся UNKNOWN, даёт UNKNOWN.

## Ловушка NOT IN, расшифрованная

Теперь инцидент из вступления. `x NOT IN (a, b, c)` определяется как `x <> a AND x <> b AND x <> c`. Допустим, список — `(1, 2, NULL)`, а `x = 5`:

```text
5 <> 1  → TRUE
5 <> 2  → TRUE
5 <> NULL → UNKNOWN      -- потому что сравнение с NULL всегда UNKNOWN
TRUE AND TRUE AND UNKNOWN → UNKNOWN
```

Всё выражение схлопывается в UNKNOWN, и строка отбрасывается. Даже с одним `NULL` в списке `NOT IN` **ни одна строка никогда не станет TRUE** — запрос возвращает ноль строк, молча, для любого значения. Именно так один `NULL` в `blocked_users` обнулил фрод-фильтр.

Фиксы, в порядке предпочтения:

- **Используй `NOT EXISTS` вместо `NOT IN`** для подзапросов. `NOT EXISTS` NULL-безопасен (проверяет существование строки, а не равенство значений) и обычно планируется лучше как anti-join:

```sql
SELECT * FROM users u
WHERE NOT EXISTS (
  SELECT 1 FROM blocked_users b WHERE b.user_id = u.id
);
```

- Или исключи NULL из подзапроса: `... NOT IN (SELECT user_id FROM blocked_users WHERE user_id IS NOT NULL)`.
- Ещё лучше — **сделай колонку `NOT NULL`**, чтобы баг был невозможен по построению.

Все три фикса устраняют одну и ту же корневую причину: как только NULL может попасть в сравнение, логика рушится. Ограничение `NOT NULL` — сильнейший вариант, оно делает невозможным само возникновение ситуации; `NOT EXISTS` — самая безопасная замена без изменения схемы.

<Infographic
  svg={notInFix}
  label="Сравнение двух фиксов ловушки NOT IN с NULL: ограничение NOT NULL предотвращает баг на уровне схемы для всех запросов, но требует миграции, а NOT EXISTS — NULL-безопасная замена, проверяющая существование строки и применяемая по одному запросу."
  caption="Два фикса одной корневой причины: запретить NULL на уровне схемы (NOT NULL — сильнейший, но миграция) или обойти равенство значений на уровне запроса (NOT EXISTS — безопасная замена)."
  hue="var(--d-data)"
/>

## IS DISTINCT FROM и NULL-aware инструментарий

Иногда тебе действительно нужно «эти два значения различны, считая NULL обычным сравниваемым значением?». Простой `<>` не может — он уходит в UNKNOWN на NULL. Postgres даёт `IS DISTINCT FROM`: он возвращает настоящий булев, считая два NULL *не* различными (равными), а NULL-против-значения — различными.

```sql
-- обнаружить изменённую колонку, даже если любая сторона может быть NULL:
SELECT * FROM staging s JOIN target t USING (id)
WHERE s.email IS DISTINCT FROM t.email;   -- TRUE/FALSE, никогда UNKNOWN
```

Повседневные помощники:

- **COALESCE(a, b, …)** возвращает первый не-NULL аргумент — твой инструмент значения по умолчанию: `COALESCE(nickname, email, 'anon')`.
- **NULLIF(a, b)** возвращает NULL, когда `a = b`, иначе `a` — удобно обойти деление на ноль: `total / NULLIF(qty, 0)`.

И два факта об агрегатах, на которых спотыкаются: агрегаты **пропускают NULL**. `COUNT(col)` считает только не-NULL значения `col`, тогда как `COUNT(*)` считает строки независимо. `AVG(col)` усредняет только по не-NULL значениям — так что `AVG` может отличаться от `SUM/COUNT(*)`, когда есть NULL.

<Inset kind="edgecase" lang="ru">
`NULL` и `UNIQUE` тоже расходятся с интуицией. Ограничение `UNIQUE` считает `NULL` различными, так что по умолчанию уникальная колонка допускает *много* NULL-строк — они не нарушают уникальность, потому что два NULL не «равны». Если нужен максимум один NULL, Postgres 15+ предлагает `UNIQUE NULLS NOT DISTINCT`, считающий NULL равными для ограничения. Та же корневая причина: `NULL = NULL` — это UNKNOWN, так что проверка уникальности по умолчанию на NULL никогда не срабатывает.
</Inset>

<Quiz
  id="03-null-quiz-1"
  lessonSlug="03-null-and-three-valued-logic"
  lang="ru"
  question="Почему WHERE deleted_at = NULL возвращает ноль строк, даже когда у многих строк deleted_at равен NULL?"
  choices={[
    { label: "Сравнение чего угодно с NULL даёт UNKNOWN, а WHERE оставляет строку, только если предикат TRUE — так что все строки отбрасываются; используй IS NULL", correct: true },
    { label: "NULL равен NULL, поэтому совпадает с каждой строкой, и Postgres отвергает неоднозначный результат", misconception: "NULL НЕ равен NULL — сравнение это UNKNOWN, а не TRUE, так что ни одна строка не подходит." },
    { label: "= NULL это синтаксическая ошибка, возвращающая пустой набор", misconception: "Это синтаксически валидно; оно просто всегда вычисляется в UNKNOWN, который WHERE трактует как не-оставляемый." },
    { label: "deleted_at нужно проиндексировать, чтобы = NULL заработал", misconception: "Индексация ни при чём; проблема в трёхзначной логике — = NULL никогда не TRUE независимо от индексов." },
  ]}
/>

<Quiz
  id="03-null-quiz-2"
  lessonSlug="03-null-and-three-valued-logic"
  lang="ru"
  question="NOT IN (подзапрос) внезапно возвращает ноль строк после деплоя. Наиболее вероятная причина и лучший фикс?"
  choices={[
    { label: "Подзапрос теперь даёт NULL; NOT IN с любым NULL схлопывается в UNKNOWN для каждой строки. Перейди на NOT EXISTS (NULL-безопасный)", correct: true },
    { label: "Подзапрос возвращает слишком много строк, чтобы NOT IN справился", misconception: "Дело не в числе строк; один NULL в списке делает весь NOT IN UNKNOWN. NOT EXISTS или фильтрация NULL чинят это." },
    { label: "NOT IN требует ORDER BY в подзапросе", misconception: "У NOT IN нет требования сортировки; регрессия — в семантике NULL-в-списке, а не в сортировке." },
    { label: "Внешняя таблица потеряла первичный ключ", misconception: "PK ни при чём; причина — трёхзначная логика NULL внутри NOT IN." },
  ]}
/>

<MetaphorComplete
  id="03-null-metaphor-1"
  lessonSlug="03-null-and-three-valued-logic"
  lang="ru"
  setup="Заполни пропуск: в SQL NULL значит, что значение _______ — не ноль и не пусто — поэтому x = NULL вычисляется в UNKNOWN, а не TRUE или FALSE."
  accepted={["неизвестно", "не известно", "неизвестное", "отсутствует"]}
  canonical="неизвестно"
  explanation="NULL моделирует «мы не знаем этого значения», так что его нельзя подтвердить равным чему-либо, даже другому NULL. Это «неизвестно» — третье значение истины (UNKNOWN), и поскольку WHERE оставляет только TRUE-строки, предикаты над NULL тихо теряют строки. Проверяй через IS NULL и используй NOT EXISTS / COALESCE / IS DISTINCT FROM, чтобы оставаться NULL-безопасным."
/>

</Explanation>

<KeyTakeaway>`NULL` значит *неизвестно*, так что любое сравнение с ним — `UNKNOWN`, третье значение истины — а `WHERE` оставляет строку, только когда предикат `TRUE`, поэтому UNKNOWN-строки тихо исчезают. Проверяй nullness через `IS NULL`, никогда `= NULL`. Главный баг: `NOT IN` с одним `NULL` в списке возвращает ноль строк для всего — предпочитай `NOT EXISTS`. Бери `COALESCE`, `NULLIF` и `IS DISTINCT FROM`, и помни, что агрегаты пропускают NULL (`COUNT(col)` против `COUNT(*)`).</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="03-null-retrieval"
  lang="ru"
  questions={[
    { q: "Почему = NULL никогда не верная проверка и какое третье значение истины тут участвует?", a: "Поскольку NULL значит «неизвестно», любое сравнение с ним — включая = NULL — вычисляется в UNKNOWN, третье значение истины рядом с TRUE и FALSE. WHERE оставляет строку, только когда предикат TRUE, так что предикат, равный UNKNOWN, отбрасывает строку, как и FALSE. Корректная проверка nullness — IS NULL / IS NOT NULL, возвращающие настоящие булевы значения." },
    { q: "Объясни точно, почему NOT IN с NULL в списке возвращает ноль строк.", a: "x NOT IN (a, b, NULL) разворачивается в x <> a AND x <> b AND x <> NULL. Последний член x <> NULL всегда UNKNOWN, а TRUE AND TRUE AND UNKNOWN это UNKNOWN, так что весь предикат никогда не станет TRUE ни для какого x — каждая строка отбрасывается. Фикс — NOT EXISTS (проверяет существование строки, NULL-безопасен и планируется как anti-join), или фильтрация NULL из подзапроса, или колонка NOT NULL." },
    { q: "Противопоставь COUNT(col) и COUNT(*) и скажи, для чего IS DISTINCT FROM.", a: "COUNT(*) считает все строки; COUNT(col) считает только строки, где col IS NOT NULL, потому что агрегаты пропускают NULL (AVG, SUM и т.д. тоже игнорируют NULL). IS DISTINCT FROM — это NULL-безопасное неравенство: возвращает настоящий TRUE/FALSE, считая два NULL равными (не различными), а NULL-против-значения — различными — полезно для обнаружения изменений, где простой <> ушёл бы в UNKNOWN." },
  ]}
/>

<Recap lang="ru">
`NULL` — это отсутствие известного значения, «неизвестно», а не ноль и не пусто. Поскольку нельзя подтвердить «неизвестно = чему угодно», сравнения с `NULL` дают третье значение истины, **UNKNOWN**, делая SQL трёхзначной логикой. `WHERE` (и `HAVING`, и условия `JOIN`) оставляют строку, только когда предикат `TRUE`, так что UNKNOWN-строки отбрасываются, как и FALSE — поэтому `= NULL` всегда даёт ничего, и нужно использовать `IS NULL`. Главный режим отказа — `NOT IN` с `NULL` в списке: он схлопывается в UNKNOWN для каждой строки и молча возвращает ноль результатов, так что предпочитай NULL-безопасный `NOT EXISTS` (или отфильтруй NULL, или сделай колонку `NOT NULL`). Дополни инструментарий `COALESCE` для значений по умолчанию, `NULLIF` для обхода деления на ноль и `IS DISTINCT FROM` для NULL-aware неравенства — и помни, что агрегаты пропускают NULL, так что `COUNT(col)` и `COUNT(*)` различаются. Теперь, когда видишь фильтр, который должен находить строки, но возвращает ноль, — сразу задай вопрос: есть ли NULL где-то в цепочке сравнения? Этот единственный вопрос ловит `= NULL`, `NOT IN (подзапрос с NULL)` и `<>` по nullable-колонке — всё одна корневая причина.
</Recap>
