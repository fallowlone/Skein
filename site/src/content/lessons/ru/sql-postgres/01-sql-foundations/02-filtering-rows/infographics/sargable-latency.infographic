infographic chart-column-simple
data
  title Sargable или нет: 2 мс против 1.9 с на 20М строк
  desc Обёртка индексируемой колонки в lower() превращает index scan за 2 мс в полный скан за 1.9 с — регрессия ~900x из урока
  values
    - label email = $1 · index scan
      value 2
    - label lower(email) = $1 · seq scan
      value 1900
theme
  palette #14b8a6 #f43f5e
