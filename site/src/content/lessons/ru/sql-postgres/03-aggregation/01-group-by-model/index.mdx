---
slug: "01-group-by-model"
lang: ru
track: "sql-postgres"
unit: "03-aggregation"
order: 1
title: "Модель GROUP BY: строки в корзины"
summary: "GROUP BY сворачивает множество строк в одну строку на каждый уникальный ключ. Отсюда железное правило: каждое выражение в SELECT должно быть в GROUP BY либо обёрнуто в агрегат."
estMin: 14
status: ready
lessonType: topic
level: middle
concepts:
  - group-by
  - aggregation-buckets
  - functional-dependency
  - select-list-rule
prereqs:
  - 00-start-here/02-how-postgres-executes-a-query
sources:
  - https://www.postgresql.org/docs/current/queries-table-expressions.html
  - https://www.postgresql.org/docs/current/sql-select.html
  - https://use-the-index-luke.com/sql/sorting-grouping
---

import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import MetaphorComplete from "~/components/pedagogy/MetaphorComplete.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";
import Infographic from "~/components/diagram/Infographic.astro";
import aggregateStrategies from "./infographics/aggregate-strategies.svg?raw";

<Hook>
Джун выкатывает `SELECT user_id, status, SUM(total) FROM orders GROUP BY user_id`. Локально возвращает аккуратные суммы по пользователям. На ревью даже не запустится: `ERROR: column "orders.status" must appear in the GROUP BY clause or be used in an aggregate function`. Это не придирка Postgres — база говорит, что запрос задал невозможный вопрос. Пойми, *почему* он невозможен, и больше никогда не будешь воевать с `GROUP BY`.
</Hook>

<Crux>GROUP BY сворачивает строки в корзины; всё в SELECT должно быть на корзину — в GROUP BY или обёрнуто в агрегат.</Crux>

<Explanation>

## Группировка — это раскладка по корзинам

`GROUP BY` берёт строки, пережившие `WHERE`, и раскладывает их по **корзинам** по значению ключа группировки. Все строки с одинаковым ключом попадают в одну корзину. Затем — в этом вся суть — **каждая корзина сворачивается ровно в одну выходную строку**.

Представь пять заказов. Группируем по `status`:

```sql
SELECT status, COUNT(*) AS n, SUM(total) AS revenue
FROM orders
GROUP BY status;
```

Postgres делит строки на одну корзину на каждое уникальное `status` (`paid`, `pending`, `cancelled`, …) и выдаёт по одной строке на корзину. Результат — это уже не «заказы», а «статусы», по строке на каждый.

<FlowDiagram
  label="GROUP BY status берёт множество отдельных строк-заказов и делит их на одну корзину на каждое уникальное значение status, затем сворачивает каждую корзину в одну выходную строку с ключом группировки и агрегатами."
  perRow={3}
  hue="var(--d-data)"
  nodes={[
    { id: "rows", label: "5 строк-заказов", sub: "paid, paid, pending, paid, cancelled" },
    { id: "bucket1", label: "Корзина: paid", sub: "3 строки" },
    { id: "bucket2", label: "Корзина: pending", sub: "1 строка" },
    { id: "bucket3", label: "Корзина: cancelled", sub: "1 строка" },
    { id: "out1", label: "paid, 3, sum", sub: "1 строка вывода" },
    { id: "out2", label: "pending, 1, sum", sub: "1 строка вывода" },
    { id: "out3", label: "cancelled, 1, sum", sub: "1 строка вывода" },
  ]}
  edges={[
    { from: "rows", to: "bucket1" },
    { from: "rows", to: "bucket2" },
    { from: "rows", to: "bucket3" },
    { from: "bucket1", to: "out1" },
    { from: "bucket2", to: "out2" },
    { from: "bucket3", to: "out3" },
  ]}
  caption="Много строк на вход, одна строка на корзину на выход. После свёртки нет больше отдельного «заказа» — только статус и числа, посчитанные по его корзине."
/>

## Железное правило, и почему оно не произвольно

Когда корзина держит три заказа, спроси: какой *тот самый* `id` у этой корзины? Его нет — их три. А *тот самый* `created_at`? Снова три. У корзины есть одно определённое `status` (это ключ), но нет единого значения ни для какой другой сырой колонки. Postgres упирается в противоречие: ты просишь одну выходную строку на корзину, но `SELECT id` требует значения, которого у корзины нет.

Вот правило, и оно прямо вытекает из свёртки:

> **Каждое выражение в списке `SELECT` должно быть либо перечислено в `GROUP BY` (тогда у него одно значение на корзину), либо обёрнуто в агрегат (который вычисляет одно значение *из* корзины).**

`COUNT(*)`, `SUM(total)`, `MAX(created_at)`, `array_agg(id)` — каждый агрегат берёт всю корзину и возвращает один скаляр. Всё, что *не* агрегировано, должно быть константой внутри корзины — а это ровно то, что гарантирует попадание в `GROUP BY`. Ошибка «must appear in GROUP BY or be used in an aggregate» — это отказ Postgres молча взять значение одной произвольной строки. Именно так делал старый дефолт MySQL — и десятилетие молча выдавал неверные отчёты.

```sql
-- НЕВЕРНО: status не сгруппирован и не агрегирован
SELECT user_id, status, SUM(total)
FROM orders
GROUP BY user_id;            -- ERROR: status должен быть в GROUP BY или агрегате

-- ВЕРНО, вариант A: сгруппировать и по нему (одна строка на пару user+status)
SELECT user_id, status, SUM(total)
FROM orders
GROUP BY user_id, status;

-- ВЕРНО, вариант B: убрать его агрегатом (одна строка на пользователя)
SELECT user_id, MAX(status) AS some_status, SUM(total)
FROM orders
GROUP BY user_id;
```

## Исключение по функциональной зависимости

Есть одно место, где Postgres ослабляет правило, и это не лазейка — это корректность. Если ты делаешь `GROUP BY` по **первичному ключу** таблицы, каждая другая колонка этой таблицы *функционально зависит* от ключа: ключ однозначно определяет строку, поэтому каждая корзина держит ровно одну строку этой таблицы, и у каждой колонки есть единственное чётко определённое значение. Postgres знает это из каталога и разрешает выбирать такие зависимые колонки без группировки:

```sql
-- u.id — это PK таблицы users, поэтому u.name и u.email функционально
-- зависят от него — Postgres разрешает их без перечисления в GROUP BY:
SELECT u.id, u.name, u.email, COUNT(o.id) AS order_count
FROM users u
LEFT JOIN orders o ON o.user_id = u.id
GROUP BY u.id;               -- не u.id, u.name, u.email — достаточно PK
```

Это часть стандарта SQL (правило функциональной зависимости из SQL:1999), и Postgres реализует его для первичных ключей и других not-null unique-ограничений. Оно избавляет от набивания `GROUP BY` дюжиной колонок только ради их вывода.

## Как на самом деле выполняется свёртка

Бакетизация не бесплатна — и у планировщика два способа её сделать. На таблице `orders` в 12М строк `GROUP BY user_id` в ~2М корзин в `EXPLAIN ANALYZE` показывается как один из двух узлов. **HashAggregate** строит in-memory хеш-таблицу по ключу `user_id`, одна запись на корзину с накопленным состоянием агрегата — сортировка входа не нужна, но все различные ключи должны разом поместиться в память. **GroupAggregate** требует, чтобы вход был *уже отсортирован* по `user_id` (обычно через предшествующий `Sort` или index scan), затем стримит по одной корзине за раз, выдавая и выбрасывая каждую до следующей — почти постоянная память, но платит за сортировку. Планировщик выбирает HashAggregate, когда оценивает, что хеш-таблица влезет в `work_mem`, и переключается на sort-based GroupAggregate, когда нет.

<Infographic
  svg={aggregateStrategies}
  label="Сравнение двух стратегий выполнения GROUP BY: HashAggregate строит хеш-таблицу в памяти из несортированного входа, но держит все различные ключи разом и выгружается на диск при превышении work_mem, а GroupAggregate требует отсортированный вход, обрабатывает по одному ведру при почти постоянной памяти и заранее платит за сортировку."
  caption="Выбор планировщика: HashAggregate, пока хеш-таблица помещается в work_mem, иначе — сортирующий GroupAggregate. На дефолтных 4 МБ с ~2 млн групп хеш-таблица выгружает ~320 МБ на диск, и узел подскакивает с ~1 с до 4–6 с."
  hue="var(--d-data)"
/>

<Inset kind="why" lang="ru">
Конкретные формы из `EXPLAIN (ANALYZE, BUFFERS)` на той таблице в 12М строк. При `work_mem = 4MB` (по умолчанию) и ~2М различных групп `user_id` хеш-таблице нужно сильно больше 4MB, поэтому PG14+ показывает `HashAggregate ... Disk Usage: 320000 kB` (он сбрасывает хеш-партиции во временный файл) либо планировщик откатывается к `Sort` + `GroupAggregate` с `Sort Method: external merge Disk: 285000 kB` — и то и другое значит, что агрегация ушла на диск, а узел подскочил с ~900мс до ~4–6с. Поставь `SET work_mem = '256MB'` — и тот же запрос отчитывается `HashAggregate ... Batches: 1 Memory Usage: 210000 kB` без строки Disk, падая обратно до ~1.1с. Правило большого пальца: примерно `различных_групп × ~50–100 байт` состояния на группу должно влезть в `work_mem`, иначе спилл. Запрос с группировкой в 50 корзин не спиллит никогда; с группировкой в миллионы — почти всегда на дефолтных 4MB.
</Inset>

<Inset kind="edgecase" lang="ru">
Зависимость должна быть такой, которую Postgres может *доказать* из ограничения. Группировка по `u.id` работает, потому что это объявленный первичный ключ. Группировка по колонке, которая просто *случайно* уникальна в твоих данных, но не имеет ограничения `UNIQUE`/`PRIMARY KEY`, всё равно будет отвергнута — планировщик не доверяет уникальности во время выполнения, только объявленным ограничениям. И зависимость — потаблично: группировка по `users.id` позволяет выбирать другие колонки `users` без группировки, но не сырые колонки `orders`, ведь один пользователь может владеть многими заказами.
</Inset>

<Quiz
  id="01-group-by-model-quiz-1"
  lessonSlug="01-group-by-model"
  lang="ru"
  question="Почему SELECT user_id, status, SUM(total) FROM orders GROUP BY user_id падает?"
  choices={[
    { label: "После группировки по user_id каждая корзина может держать много разных значений status, так что у status нет единого значения на выходную строку", correct: true },
    { label: "SUM вообще нельзя сочетать с неагрегированными колонками", misconception: "Можно — проблема лишь в том, что status не сгруппирован и не агрегирован. Добавить status в GROUP BY или обернуть в агрегат — оба варианта работают." },
    { label: "status всегда должен быть первой колонкой в GROUP BY", misconception: "Порядок колонок в GROUP BY не важен; проблема в том, что status вообще нет в GROUP BY (и не агрегирован)." },
    { label: "GROUP BY разрешает только одну колонку", misconception: "GROUP BY принимает много колонок; группировка по user_id, status как раз сделала бы запрос валидным." },
  ]}
/>

<Quiz
  id="01-group-by-model-quiz-2"
  lessonSlug="01-group-by-model"
  lang="ru"
  question="SELECT u.id, u.name, u.email, COUNT(o.id) FROM users u LEFT JOIN orders o ON o.user_id=u.id GROUP BY u.id принимается. Почему u.name и u.email разрешены без их перечисления?"
  choices={[
    { label: "u.id — первичный ключ users, поэтому name и email функционально зависят от него — каждая корзина держит ровно одну строку users", correct: true },
    { label: "Postgres всегда разрешает выбирать любую колонку из присоединённой таблицы", misconception: "Не разрешает — попробуй выбрать сырую колонку orders здесь, и это будет отвергнуто. Исключение именно для колонок, функционально зависящих от сгруппированного первичного ключа." },
    { label: "Текстовые колонки освобождены от правила GROUP BY", misconception: "Тип данных не важен. Исключение про функциональную зависимость от сгруппированного PK, а не про текст." },
    { label: "Это работает только из-за LEFT JOIN", misconception: "Тип join не при чём. То же исключение действует и в простом однотабличном GROUP BY u.id." },
  ]}
/>

<MetaphorComplete
  id="01-group-by-model-metaphor-1"
  lessonSlug="01-group-by-model"
  lang="ru"
  setup="Заполни пропуск: GROUP BY раскладывает строки по корзинам, затем сворачивает каждую корзину в одну выходную строку — поэтому выбираемая колонка должна быть либо ключом корзины, либо _______ по всей корзине."
  accepted={["агрегирована", "агрегат", "вычислена", "сведена", "посчитана"]}
  canonical="агрегирована"
  explanation="Каждая корзина становится одной строкой. У сырой колонки внутри корзины много значений и нет единственного ответа, поэтому она должна быть либо ключом группировки (одно значение по построению), либо сведена агрегатом вроде SUM/COUNT/MAX, превращающим всю корзину в один скаляр."
/>

</Explanation>

<KeyTakeaway>`GROUP BY` делит пережившие строки на корзины по ключу и сворачивает каждую корзину в одну строку. Поэтому каждое выражение в `SELECT` должно быть в `GROUP BY` или внутри агрегата — иначе у него нет единого значения на корзину. Единственное послабление: группировка по первичному ключу таблицы позволяет выбирать другие её колонки без группировки, ведь они функционально зависят от ключа.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="01-group-by-model-retrieval"
  lang="ru"
  questions={[
    { q: "Сформулируй правило списка SELECT для GROUP BY и объясни, почему оно существует.", a: "Каждое выражение в списке SELECT должно быть в GROUP BY либо обёрнуто в агрегатную функцию. Оно существует, потому что GROUP BY сворачивает каждую корзину строк в одну выходную строку: колонка, не являющаяся ключом группировки, может держать много разных значений внутри корзины, так что у неё нет единого значения для вывода. Помещение в GROUP BY делает её константой на корзину; агрегирование вычисляет из корзины одно значение." },
    { q: "Что такое исключение по функциональной зависимости и когда оно действует?", a: "Если ты делаешь GROUP BY по первичному ключу таблицы (или другому объявленному not-null unique-ограничению), каждая другая колонка этой таблицы функционально зависит от ключа — ключ однозначно определяет строку — поэтому каждая корзина держит ровно одну строку этой таблицы и у каждой колонки одно чётко определённое значение. Тогда Postgres разрешает выбирать такие зависимые колонки без их перечисления в GROUP BY. Работает только для зависимостей, доказуемых из объявленного ограничения, и только для колонок той таблицы, чей ключ сгруппирован." },
    { q: "Дай два валидных способа починить SELECT user_id, status, SUM(total) FROM orders GROUP BY user_id и скажи, чем отличаются результаты.", a: "Вариант A: добавить status в GROUP BY (GROUP BY user_id, status) — одна строка на каждую пару user+status, более тонкая гранулярность, больше строк. Вариант B: обернуть status в агрегат (например MAX(status) или string_agg(DISTINCT status, ',')) — одна строка на пользователя со status, сведённым в одно производное значение. A меняет гранулярность группировки; B сохраняет одну строку на пользователя." },
  ]}
/>

<Recap lang="ru">
`GROUP BY` — это операция раскладки по корзинам: строки, пережившие `WHERE`, делятся по ключу группировки, и каждая корзина сворачивается ровно в одну выходную строку. Эта свёртка — источник железного правила: каждое выражение в `SELECT` должно быть в `GROUP BY` (константа на корзину) или внутри агрегата (одно значение, посчитанное из корзины), потому что у сырой колонки нет единого значения по многострочной корзине. Postgres это требует, а не молча возвращает произвольную строку, — и эта дисциплина держит отчёты корректными. Единственное послабление — исключение по функциональной зависимости: сгруппируй по первичному ключу, и можешь выбирать другие колонки той таблицы без группировки, ведь ключ их определяет. Дальше посмотрим, *когда* происходит фильтрация вокруг этой свёртки — `WHERE` до группировки против `HAVING` после, — что решает и корректность, и производительность.
</Recap>
