---
slug: "02-having-vs-where"
lang: ru
track: "sql-postgres"
unit: "03-aggregation"
order: 2
title: "HAVING против WHERE: фильтр до или после группировки"
summary: "WHERE фильтрует строки до группировки; HAVING фильтрует целые группы после агрегации. В WHERE агрегат нельзя, а проталкивание построчных фильтров в WHERE — реальный рычаг производительности."
estMin: 14
status: ready
lessonType: topic
level: middle
concepts:
  - where-clause
  - having-clause
  - aggregation-pipeline
  - filter-pushdown
prereqs:
  - 01-group-by-model
sources:
  - https://www.postgresql.org/docs/current/queries-table-expressions.html
  - https://www.postgresql.org/docs/current/tutorial-agg.html
  - https://use-the-index-luke.com/sql/where-clause
---

import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
Отчёт по выручке показывает «топ-клиентов с более чем $10k оплаченных заказов». Первый черновик засунул `paid` и `> 10000` в один `HAVING` и работал 9 секунд, сканируя каждый когда-либо сделанный заказ. Перенос одного предиката на одно предложение вверх — `paid` в `WHERE` — уронил время до 200мс, потому что группировке внезапно осталось переваривать в десять раз меньше строк. `WHERE` против `HAVING` — это не стиль; это меняет работу движка.
</Hook>

<Crux>WHERE фильтрует строки до группировки; HAVING фильтрует группы после агрегации. Агрегаты в WHERE недопустимы.</Crux>

<Explanation>

## Два фильтра в два разных момента

У агрегации фиксированный конвейер, и два фильтрующих предложения стоят по разные стороны свёртки:

1. **`FROM` / `JOIN`** — собрать сырые строки.
2. **`WHERE`** — отбросить *строки*, которые не нужны. Работает **до** группировки, поэтому действует на отдельные строки и не видит ни одного агрегата (групп ещё нет).
3. **`GROUP BY`** — свернуть пережившие строки в корзины.
4. **`HAVING`** — отбросить целые *группы*, которые не нужны. Работает **после** агрегации, поэтому может ссылаться на агрегаты вроде `SUM(total)` или `COUNT(*)`.
5. **`SELECT` / `ORDER BY` / `LIMIT`** — оформить вывод.

<FlowDiagram
  label="Конвейер агрегации: FROM собирает строки, WHERE фильтрует отдельные строки до группировки, GROUP BY сворачивает строки в корзины, HAVING фильтрует целые группы после агрегации, затем SELECT и ORDER BY оформляют вывод."
  perRow={5}
  hue="var(--d-data)"
  nodes={[
    { id: "from", label: "FROM/JOIN", sub: "сырые строки" },
    { id: "where", label: "WHERE", sub: "фильтр строк" },
    { id: "group", label: "GROUP BY", sub: "строки → корзины" },
    { id: "having", label: "HAVING", sub: "фильтр групп" },
    { id: "select", label: "SELECT", sub: "оформить вывод" },
  ]}
  edges={[
    { from: "from", to: "where" },
    { from: "where", to: "group" },
    { from: "group", to: "having" },
    { from: "having", to: "select" },
  ]}
  caption="WHERE действует на строки до свёртки; HAVING действует на группы после неё. Всё, что решается построчно, идёт в WHERE; только условия на агрегированное значение требуют HAVING."
/>

Конкретно — правильный отчёт:

```sql
SELECT user_id, SUM(total) AS paid_revenue
FROM orders
WHERE status = 'paid'          -- построчное условие → до группировки
GROUP BY user_id
HAVING SUM(total) > 10000;      -- условие на агрегат → после группировки
```

`status = 'paid'` решается на одной строке, поэтому идёт в `WHERE` и сжимает вход для `GROUP BY`. `SUM(total) > 10000` можно вычислить только когда целая корзина существует, поэтому это обязательно `HAVING`.

## Почему агрегаты в WHERE недопустимы

Попробуй перенести порог в `WHERE` — и Postgres откажет:

```sql
SELECT user_id, SUM(total)
FROM orders
WHERE SUM(total) > 10000        -- ERROR: aggregate functions are not allowed in WHERE
GROUP BY user_id;
```

Это не недостающая фича — это невозможность по времени. `WHERE` работает **до** `GROUP BY`, в точке, где ещё нет ни одной группы, так что `SUM(total)` нечего суммировать. Агрегат в этот момент не определён. `HAVING` существует именно как фильтр, работающий *после* того, как группы (и их агрегаты) материализуются.

## Рычаг производительности: проталкивай фильтры вниз

Вот часть, отделяющая корректный запрос от быстрого. Оба возвращают одни и те же строки:

```sql
-- МЕДЛЕННО: каждый заказ группируется, потом неоплаченные группы... всё равно неверно и дорого
SELECT user_id, SUM(total)
FROM orders
GROUP BY user_id
HAVING bool_and(status = 'paid');   -- вычурно и сначала группирует ВСЕ заказы

-- БЫСТРО: отбросить неоплаченные строки до того, как они дойдут до группировки
SELECT user_id, SUM(total)
FROM orders
WHERE status = 'paid'
GROUP BY user_id;
```

Построчный фильтр место в `WHERE`, чтобы до (дорогого) шага группировки доходило меньше строк. Группировке приходится хешировать или сортировать вход; вдвое меньший вход примерно вдвое дешевле, а если есть частичный индекс по `status`, планировщик может вовсе пропустить несовпадающие строки. **Положить простое построчное условие в `HAVING` — это code smell**: обычно это значит, что фильтр, который должен был отработать до группировки, работает после неё, выполняя всю работу группировки над строками, которые ты собирался выбросить.

<Inset kind="mistake" lang="ru">
Классическая ловушка: `HAVING status = 'paid'`. В Postgres это синтаксически законно, только если `status` функционально зависит от ключа группировки — иначе получишь ту же ошибку «must appear in GROUP BY». Даже когда парсится, это неверный подход: ты сгруппировал каждый заказ, включая те, что отбросишь. Правило большого пальца — если условие не упоминает **ни одного агрегата**, оно почти всегда место в `WHERE`. Резервируй `HAVING` для условий на `SUM`, `COUNT`, `AVG`, `MAX`, `MIN` и т.п.
</Inset>

<Quiz
  id="02-having-vs-where-quiz-1"
  lessonSlug="02-having-vs-where"
  lang="ru"
  question="Почему WHERE SUM(total) > 10000 поднимает ошибку?"
  choices={[
    { label: "WHERE работает до GROUP BY, поэтому группы ещё нет и SUM нечего агрегировать", correct: true },
    { label: "SUM можно использовать только в SELECT, никогда в фильтре", misconception: "SUM нормально в фильтре — этот фильтр и есть HAVING, который работает после группировки. Проблема в моменте WHERE, а не в функции." },
    { label: "Нужно сначала задать алиас SUM, чтобы по нему фильтровать", misconception: "Алиас не меняет момент; агрегат не определён в WHERE независимо от алиаса, ведь WHERE предшествует группировке." },
    { label: "total нужно сначала проиндексировать", misconception: "Индексация не при чём; это ошибка логического порядка, а не вопрос производительности или индекса." },
  ]}
/>

<Quiz
  id="02-having-vs-where-quiz-2"
  lessonSlug="02-having-vs-where"
  lang="ru"
  question="Тебе нужна оплаченная выручка на пользователя свыше $10k. В какое предложение идёт status='paid' и почему?"
  choices={[
    { label: "WHERE — это построчное условие, и фильтрация до группировки значит, что до дорогого GROUP BY доходит меньше строк", correct: true },
    { label: "HAVING — все фильтры для единообразия идут после группировки", misconception: "Только агрегатные условия требуют HAVING. Построчный фильтр в HAVING заставляет группировать строки, которые ты выбросишь, — это медленнее." },
    { label: "Любое — они всегда работают одинаково", misconception: "Они могут вернуть одни и те же строки, но не за одну цену: WHERE сжимает вход группировки, часто на порядок." },
    { label: "ORDER BY — чтобы отсортировать оплаченные", misconception: "ORDER BY сортирует вывод, а не фильтрует. Условие paid — это фильтр, и притом построчный, значит WHERE." },
  ]}
/>

<DragOrder
  id="02-having-vs-where-drag-1"
  lessonSlug="02-having-vs-where"
  lang="ru"
  prompt="Упорядочи предложения по тому, когда Postgres логически их применяет:"
  items={[
    { id: "a", label: "FROM / JOIN — собрать сырые строки" },
    { id: "b", label: "WHERE — фильтр отдельных строк" },
    { id: "c", label: "GROUP BY — свернуть строки в корзины" },
    { id: "d", label: "HAVING — фильтр целых групп через агрегаты" },
    { id: "e", label: "SELECT — спроецировать выходные колонки" },
    { id: "f", label: "ORDER BY / LIMIT — отсортировать и обрезать" },
  ]}
  correctOrder={["a", "b", "c", "d", "e", "f"]}
/>

</Explanation>

<KeyTakeaway>`WHERE` фильтрует отдельные строки **до** группировки; `HAVING` фильтрует целые группы **после** агрегации и является единственным местом, где агрегатное условие законно. Агрегаты не могут быть в `WHERE`, потому что там ещё нет ни одной группы. Всегда проталкивай построчные условия в `WHERE`, чтобы до шага группировки доходило меньше строк — простое неагрегатное условие в `HAVING` это code smell.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="02-having-vs-where-retrieval"
  lang="ru"
  questions={[
    { q: "Одним предложением о каждом: что фильтруют WHERE и HAVING и в каком порядке работают?", a: "WHERE фильтрует отдельные строки до группировки (работает первым, на сырых строках, и не видит агрегатов). HAVING фильтрует целые группы после агрегации (работает после GROUP BY и может ссылаться на агрегаты вроде SUM или COUNT). Порядок конвейера: FROM → WHERE → GROUP BY → HAVING → SELECT → ORDER BY." },
    { q: "Почему WHERE SUM(total) > 10000 это ошибка и в чём фикс?", a: "WHERE работает до GROUP BY, поэтому в этот момент ни одной группы нет и у SUM нет набора строк для агрегации — агрегат там не определён, и Postgres его отвергает. Фикс — перенести условие в HAVING (HAVING SUM(total) > 10000), который работает после группировки, когда SUM каждой группы определён." },
    { q: "Почему положить status = 'paid' в HAVING вместо WHERE — это и smell, и проблема производительности?", a: "status = 'paid' — построчное условие без агрегата, его можно решить до группировки. В HAVING это заставляет Postgres сгруппировать каждый заказ — включая неоплаченные, которые ты собираешься выбросить, — и только потом отбросить группы, тратя работу группировки впустую. В WHERE оно убирает неоплаченные строки до группировки, так что GROUP BY хеширует/сортирует куда меньше строк (часто в 10 раз), и может использоваться частичный индекс по status. Неагрегатные условия место в WHERE." },
  ]}
/>

<Recap lang="ru">
Конвейер агрегации идёт `FROM` → `WHERE` → `GROUP BY` → `HAVING` → `SELECT` → `ORDER BY`, и два фильтра стоят по разные стороны свёртки. `WHERE` фильтрует отдельные строки **до** группировки и поэтому не может ссылаться на агрегат — групп ещё нет, так что `SUM(total)` не определён и Postgres его отвергает. `HAVING` фильтрует целые группы **после** агрегации и именно туда место агрегатным условиям вроде `SUM(total) > 10000`. Сеньорская привычка — проталкивать каждый построчный предикат в `WHERE`, чтобы до дорогого шага группировки доходило меньше строк (вдвое меньший вход примерно вдвое дешевле по hash/sort, и частичные индексы могут отсечь строки), и считать простое неагрегатное условие, застрявшее в `HAVING`, smell'ом. Дальше углубимся в сами агрегатные функции: чем `COUNT(*)` отличается от `COUNT(col)`, предложение `FILTER` и числовые подводные камни `AVG` и `SUM`.
</Recap>
