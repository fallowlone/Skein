---
slug: "04-grouping-sets-rollup-cube"
lang: ru
track: "sql-postgres"
unit: "03-aggregation"
order: 4
title: "GROUPING SETS, ROLLUP, CUBE: много группировок за один проход"
summary: "GROUPING SETS считает несколько группировок одним запросом; ROLLUP добавляет иерархические подытоги, CUBE — все комбинации. GROUPING() отличает NULL подытога от настоящего."
estMin: 15
status: ready
lessonType: topic
level: senior
concepts:
  - grouping-sets
  - rollup
  - cube
  - grouping-function
prereqs:
  - 03-aggregate-functions
sources:
  - https://www.postgresql.org/docs/current/queries-table-expressions.html
  - https://www.postgresql.org/docs/current/functions-aggregate.html
  - https://www.postgresql.org/docs/current/sql-select.html
---

import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import MetaphorComplete from "~/components/pedagogy/MetaphorComplete.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
Финансовой команде нужна была одна таблица: выручка по статусу, выручка по пользователю и общий итог — всё сразу. Наивная сборка — три `SELECT`, склеенные `UNION ALL`, сканирующие `orders` трижды и разъезжающиеся, как только кто-то правил одну ветку. Один `GROUP BY ROLLUP` заменил всё это: один скан, подытоги и общий итог включены, рассинхронизировать невозможно. Это фича, превращающая отчётный запрос из обузы для сопровождения в одну строку.
</Hook>

<Crux>GROUPING SETS считает несколько группировок за один проход; ROLLUP даёт иерархические подытоги, CUBE — все комбинации.</Crux>

<Explanation>

## Один запрос, несколько группировок

Обычно `GROUP BY` выдаёт ровно одну группировку. `GROUPING SETS` позволяет запросить **несколько группировок одним запросом**, складывая их результаты в один вывод:

```sql
SELECT status, user_id, SUM(total) AS revenue
FROM orders
GROUP BY GROUPING SETS (
  (status),        -- выручка по статусу
  (user_id),       -- выручка по пользователю
  ()               -- общий итог (пустое множество = без группировки)
);
```

Каждый список в скобках — одна группировка. Пустое множество `()` значит «не группировать вовсе» — общий итог по всем строкам. Postgres сканирует `orders` один раз и выдаёт объединение всех трёх группировок. Колонки, не входящие в данную группировку, возвращаются как `NULL` в тех строках (`user_id` равен `NULL` в строках по статусу, ведь пользователя нет в этом множестве).

<FlowDiagram
  label="GROUPING SETS по одному скану orders выдаёт три сложенных блока результата: строки, сгруппированные по status, строки по user_id и одна строка общего итога из пустого множества группировки."
  perRow={4}
  hue="var(--d-data)"
  nodes={[
    { id: "scan", label: "Скан orders", sub: "один раз" },
    { id: "byStatus", label: "GROUP (status)", sub: "строки по статусу" },
    { id: "byUser", label: "GROUP (user_id)", sub: "строки по пользователю" },
    { id: "total", label: "GROUP ()", sub: "строка общего итога" },
  ]}
  edges={[
    { from: "scan", to: "byStatus" },
    { from: "scan", to: "byUser" },
    { from: "scan", to: "total" },
  ]}
  caption="Один скан питает три группировки; их выводы складываются. Пустое множество () даёт единственную строку общего итога. Это заменяет три UNION ALL запроса, каждый из которых пересканировал бы таблицу."
/>

## ROLLUP и CUBE: сокращения для частых форм

Два сокращения покрывают шаблоны, которые тебе реально нужны:

- **`ROLLUP (a, b)`** разворачивается в *иерархические* множества группировок `(a, b)`, `(a)`, `()`. Используй, когда колонки образуют иерархию — `(year, month)` → итоги по месяцу, подытог по году, общий итог. Это «drill-down с подытогами».
- **`CUBE (a, b)`** разворачивается во *все* комбинации: `(a, b)`, `(a)`, `(b)`, `()`. Используй для полной кросс-таблицы, где важны все измерения и итоги.

```sql
-- Подытоги по статусу в иерархии rollup, плюс общий итог:
SELECT status, SUM(total) AS revenue
FROM orders
GROUP BY ROLLUP (status);   -- = GROUPING SETS ((status), ())

-- Каждая комбинация status × user_id, плюс все подытоги и общий итог:
SELECT status, user_id, SUM(total)
FROM orders
GROUP BY CUBE (status, user_id);
```

`ROLLUP (a, b)` даёт n+1 множеств группировок; `CUBE` из n колонок даёт 2^n. Тянись за `ROLLUP` для иерархий (частые 90%) и за `CUBE` лишь когда тебе действительно нужна каждая кросс-комбинация — его вывод растёт экспоненциально с числом измерений.

## GROUPING(): этот NULL — подытог или настоящие данные?

Вот ловушка. У строки подытога `NULL` в колонках, по которым она *не* сгруппирована. Но твои данные могут *также* содержать настоящий `NULL` в `status`. Как отличить «все статусы» (подытог) от «заказа, чей status действительно NULL»? Функция `GROUPING()`:

```sql
SELECT
  status,
  SUM(total) AS revenue,
  GROUPING(status) AS is_subtotal   -- 1 = строка подытога, 0 = настоящее значение status
FROM orders
GROUP BY ROLLUP (status);
```

`GROUPING(col)` возвращает `1`, если `col` был свёрнут (агрегирован прочь) для этой строки — то есть это строка подытога/итога, — и `0`, если `col` — настоящее сгруппированное значение (включая настоящий `NULL`). Используй её для разметки строк или чистого coalesce: `CASE WHEN GROUPING(status) = 1 THEN 'All statuses' ELSE status END`. Без `GROUPING()` настоящий `NULL` status и подытог неразличимы, и твой отчёт молча подписывает их одинаково.

<Inset kind="edgecase" lang="ru">
`GROUPING()` может принимать несколько аргументов и возвращает **битовую маску**: `GROUPING(a, b)` даёт 2-битное целое, где каждый бит говорит, был ли свёрнут тот столбец. Для `CUBE(a, b)` `GROUPING(a, b)` возвращает 0 (оба настоящие), 1 (b свёрнут), 2 (a свёрнут) или 3 (общий итог). Эта битовая маска — чистый способ отсортировать или отфильтровать до конкретного уровня группировки в одном результате CUBE — например `HAVING GROUPING(a, b) = 0` оставляет лишь полностью детальные строки. Да, `GROUPING()` можно класть в `HAVING` — это выражение в агрегатном контексте.
</Inset>

<Quiz
  id="04-grouping-sets-rollup-cube-quiz-1"
  lessonSlug="04-grouping-sets-rollup-cube"
  lang="ru"
  question="Что GROUP BY ROLLUP (status) считает такого, чего не делает простой GROUP BY status?"
  choices={[
    { label: "Он добавляет строку общего итога (пустое множество группировки) поверх строк по статусу", correct: true },
    { label: "Он группирует по status дважды ради точности", misconception: "Он не дублирует группировку по status; он добавляет множество () — то есть общий итог — за один проход." },
    { label: "Он убирает NULL-статусы из результата", misconception: "ROLLUP не фильтрует NULL; он, наоборот, может вводить NULL подытогов, ради чего и существует GROUPING()." },
    { label: "Он сортирует вывод по выручке", misconception: "ROLLUP меняет, какие группировки считаются, а не порядок сортировки; для сортировки всё равно нужен ORDER BY." },
  ]}
/>

<Quiz
  id="04-grouping-sets-rollup-cube-quiz-2"
  lessonSlug="04-grouping-sets-rollup-cube"
  lang="ru"
  question="В результате ROLLUP у строки status = NULL. Как отличить строку подытога от настоящего NULL-статуса?"
  choices={[
    { label: "Использовать GROUPING(status): возвращает 1 для свёрнутой строки подытога и 0 для настоящего значения status (даже настоящего NULL)", correct: true },
    { label: "Строки подытогов всегда идут последними, так что подскажет позиция", misconception: "Позиция строк не гарантирована без ORDER BY и не является надёжным сигналом; правильная проверка — GROUPING()." },
    { label: "Проверить, больше ли выручка — подытоги крупнее", misconception: "Подытог может быть меньше отдельной группы при перекосе данных; величина не надёжный индикатор." },
    { label: "Различить их нельзя", misconception: "Можно: GROUPING(status) существует именно чтобы отделить NULL подытога от настоящего значения NULL." },
  ]}
/>

<MetaphorComplete
  id="04-grouping-sets-rollup-cube-metaphor-1"
  lessonSlug="04-grouping-sets-rollup-cube"
  lang="ru"
  setup="Заполни пропуск: ROLLUP (a, b) разворачивается в иерархические множества группировок (a,b), (a), () — тогда как CUBE (a, b) разворачивается во _______ комбинации, включая (b) отдельно."
  accepted={["все", "все возможные", "всевозможные"]}
  canonical="все"
  explanation="ROLLUP иерархичен: он отшелушивает колонки справа, давая n+1 множеств группировок для drill-down с подытогами. CUBE исчерпывающ: каждое подмножество колонок, 2^n множеств группировок, поэтому он также включает (b) отдельно. Используй ROLLUP для иерархий, CUBE — только когда действительно нужна каждая кросс-комбинация."
/>

</Explanation>

<KeyTakeaway>`GROUPING SETS` считает несколько группировок за один скан, складывая их строки, — заменяя многие `UNION ALL` запросы. `ROLLUP (a,b)` — иерархическое сокращение `(a,b),(a),()` для подытогов плюс общего итога; `CUBE` даёт все 2^n комбинаций. Строки подытогов несут `NULL` в свёрнутых колонках, поэтому используй `GROUPING(col)` (1 = подытог, 0 = настоящее значение), чтобы отличить NULL подытога от настоящего `NULL`.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="04-grouping-sets-rollup-cube-retrieval"
  lang="ru"
  questions={[
    { q: "Какую проблему решают GROUPING SETS / ROLLUP / CUBE и что бы ты написал без них?", a: "Они считают несколько группировок (по статусу, по пользователю, общий итог и т.п.) одним запросом и одним сканом таблицы, складывая результаты. Без них ты писал бы один SELECT на каждую группировку, склеивая UNION ALL, что пересканирует таблицу раз на группировку и разъезжается при правке одной ветки. ROLLUP/CUBE — сокращения: ROLLUP для иерархических подытогов, CUBE для всех комбинаций." },
    { q: "Чем ROLLUP (a,b) отличается от CUBE (a,b) по множествам группировок, в которые разворачиваются?", a: "ROLLUP (a,b) иерархичен и отшелушивает колонки справа: даёт (a,b), (a), () — n+1 множеств группировок, для drill-down с подытогами и общим итогом. CUBE (a,b) исчерпывающ: даёт (a,b), (a), (b), () — 2^n множеств, каждое подмножество колонок. Используй ROLLUP для иерархий вроде (year, month); CUBE — только когда нужна каждая кросс-комбинация, ведь его вывод растёт экспоненциально." },
    { q: "Зачем нужен GROUPING() и что он возвращает?", a: "Строки подытога/итога несут NULL в колонках, свёрнутых для этой строки, что неотличимо от настоящего значения NULL. GROUPING(col) возвращает 1, если col был свёрнут (это строка подытога/итога), и 0, если col — настоящее сгруппированное значение (включая настоящий NULL). С несколькими аргументами возвращает битовую маску, по биту на колонку, которую можно использовать в SELECT для разметки строк ('All statuses') или в HAVING для оставления конкретного уровня группировки." },
  ]}
/>

<Recap lang="ru">
`GROUPING SETS` считает несколько группировок одним запросом и одним сканом таблицы, складывая их строки результата, — пустое множество `()` даёт общий итог. Это заменяет старый шаблон `UNION ALL` из одного запроса на группировку, который пересканировал таблицу и разъезжался. `ROLLUP (a, b)` — иерархическое сокращение для `(a,b), (a), ()` — подытоги drill-down плюс общий итог, — а `CUBE` — исчерпывающее сокращение `2^n` для каждой комбинации, его стоит применять умеренно, ведь он растёт экспоненциально. Тонкость — в NULL: строка подытога несёт `NULL` в свёрнутых колонках, неотличимый от настоящего `NULL`, поэтому `GROUPING(col)` (1 для свёрнутой строки/подытога, 0 для настоящего значения, битовая маска для нескольких колонок) — это способ корректно разметить, отсортировать или отфильтровать строки. Дальше применим инструменты условной агрегации, что ты выучил, для построения сводных таблиц — превращая длинные строки в широкие отчётные колонки через `SUM(CASE …)` и `FILTER`.
</Recap>
