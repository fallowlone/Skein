infographic chart-column-simple
data
  title Один проход против трёх сканов на таблице orders в 12М строк
  desc Время в мс — FILTER читает каждую строку один раз; три отдельных запроса сканируют таблицу трижды
  values
    - label FILTER (1 проход)
      value 850
    - label 3 запроса (3 скана)
      value 2400
theme
  palette #14b8a6 #f43f5e
