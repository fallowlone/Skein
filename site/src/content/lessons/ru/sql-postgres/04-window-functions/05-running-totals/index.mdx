---
slug: "05-running-totals"
lang: ru
track: "sql-postgres"
unit: "04-window-functions"
order: 5
title: "Накопительные итоги, скользящие средние, LAG/LEAD"
summary: "Накопительный SUM, скользящие средние через ROWS BETWEEN n PRECEDING и LAG/LEAD для дельт период-к-периоду — плюс классическая ловушка LAST_VALUE, где фрейм по умолчанию кончается на CURRENT ROW, поэтому LAST_VALUE возвращает текущую строку, а не последнюю в партиции."
estMin: 16
status: ready
lessonType: topic
level: senior
concepts:
  - running-total
  - moving-average
  - lag-lead
  - first-last-value
  - last-value-trap
prereqs:
  - 04-ranking-functions
sources:
  - https://www.postgresql.org/docs/current/functions-window.html
  - https://www.postgresql.org/docs/current/tutorial-window.html
---

import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import MetaphorComplete from "~/components/pedagogy/MetaphorComplete.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";
import Infographic from "~/components/diagram/Infographic.astro";
import lastValueTrap from "./infographics/last-value-trap.svg?raw";

<Hook>
Колонка «крупнейшая сделка с начала квартала» на дашборде продаж показывает неверное число на каждой строке, кроме последней. В запросе было `LAST_VALUE(amount) OVER (ORDER BY sale_date)`. Читается как по-английски — «последнее значение» — но возвращает сумму *текущей* строки, а не финальной в квартале, потому что фрейм по умолчанию кончается на `CURRENT ROW`. `LAST_VALUE` — самый частый сообщаемый баг оконных функций, и фикс — одна клауза фрейма. Этот урок — инструментарий накопления: накопительные итоги, скользящие средние, построчные дельты и эта ловушка.
</Hook>

<Crux>Накопительный SUM и скользящие средние идут от фрейма; LAST_VALUE по умолчанию даёт текущую строку — его фрейм кончается на CURRENT ROW.</Crux>

<Explanation>

## Накопительные итоги и скользящие средние

Накопительный итог — это `SUM` по фрейму, который растёт от начала партиции до текущей строки. Из урока о фреймах: всегда пиши `ROWS` для последовательного построчного итога:

```sql
SELECT region, sale_date, amount,
       SUM(amount) OVER (
         PARTITION BY region ORDER BY sale_date
         ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW
       ) AS running_total
FROM sales;
```

**Скользящее среднее** — та же идея с *ограниченным* фреймом — среднее по текущей строке и нескольким перед ней:

```sql
-- скользящее среднее по 3 строкам назад от дневной суммы
SELECT sale_date, amount,
       AVG(amount) OVER (
         ORDER BY sale_date
         ROWS BETWEEN 2 PRECEDING AND CURRENT ROW
       ) AS moving_avg_3
FROM sales;
```

Единственное изменение от накопительного итога — начальная граница: `2 PRECEDING` вместо `UNBOUNDED PRECEDING`. Ближе к началу партиции фрейм просто держит меньше строк (1, затем 2, затем полные 3) — корректное краевое поведение, не баг.

<FlowDiagram
  label="Накопительный итог, накапливающийся по четырём упорядоченным строкам 10, 20, 30, 5. Фрейм каждой строки тянется от начала до себя, поэтому итоги 10, 30, 60, 65 — каждый вбирает прежний итог плюс текущую сумму."
  perRow={4}
  hue="var(--d-data)"
  nodes={[
    { id: "a", label: "10", sub: "итог 10" },
    { id: "b", label: "+20", sub: "итог 30" },
    { id: "c", label: "+30", sub: "итог 60" },
    { id: "d", label: "+5", sub: "итог 65" },
  ]}
  edges={[
    { from: "a", to: "b" },
    { from: "b", to: "c" },
    { from: "c", to: "d" },
  ]}
  caption="Каждая строка несёт накопленную сумму до этого момента. С фреймом ROWS накопительный итог добавляет ровно одну сумму за шаг — фундамент кумулятивных дашбордов."
/>

## LAG и LEAD: построчные дельты

`LAG(col, n)` читает колонку из строки за `n` *до* текущей в порядке окна; `LEAD(col, n)` читает за `n` *после*. Это как считать изменение период-к-периоду без self-join:

```sql
-- изменение выручки месяц-к-месяцу
SELECT month, revenue,
       revenue - LAG(revenue) OVER (ORDER BY month) AS mom_change,
       ROUND(100.0 * (revenue - LAG(revenue) OVER (ORDER BY month))
             / NULLIF(LAG(revenue) OVER (ORDER BY month), 0), 1) AS mom_pct
FROM monthly_revenue;
```

`LAG(revenue)` на первой строке — `NULL` (предыдущей строки нет) — задай дефолт через `LAG(revenue, 1, 0)`, если хочешь 0; и защити деление через `NULLIF`, чтобы нулевое предыдущее значение не дало деление на ноль. `LAG`/`LEAD` полностью игнорируют фрейм; они навигируют по смещению строки внутри упорядоченной партиции.

## FIRST_VALUE, LAST_VALUE и ловушка

`FIRST_VALUE(col)` возвращает колонку из первой строки фрейма, `LAST_VALUE(col)` — из последней. Ловушка: с `ORDER BY` в окне фрейм по умолчанию — `RANGE UNBOUNDED PRECEDING AND CURRENT ROW`, поэтому *последняя* строка фрейма **и есть текущая строка**. `LAST_VALUE` поэтому возвращает значение текущей строки, а не финальное в партиции — чего почти никто не хочет.

```sql
-- НЕВЕРНО: возвращает сумму текущей строки, потому что фрейм кончается на CURRENT ROW
LAST_VALUE(amount) OVER (PARTITION BY region ORDER BY sale_date)

-- ВЕРНО: растяни фрейм до конца партиции
LAST_VALUE(amount) OVER (
  PARTITION BY region ORDER BY sale_date
  ROWS BETWEEN UNBOUNDED PRECEDING AND UNBOUNDED FOLLOWING
)
```

`FIRST_VALUE` обычно выглядит корректно *случайно*: фрейм начинается с `UNBOUNDED PRECEDING`, что действительно первая строка, так что фрейм по умолчанию её уже покрывает. Кусается именно `LAST_VALUE` — его цель живёт на дальнем краю партиции, за потолком `CURRENT ROW` фрейма по умолчанию. Чини растяжкой фрейма до `UNBOUNDED FOLLOWING` или обойди через `MAX`/`MIN` по всей партиции (без `ORDER BY`, так что фрейм — вся партиция), когда хочешь экстремум, а не буквально последнюю-по-порядку строку.

<Infographic
  svg={lastValueTrap}
  label="Сравнение бок о бок LAST_VALUE с двумя фреймами. Фрейм по умолчанию кончается на CURRENT ROW, поэтому последняя строка фрейма — текущая, и LAST_VALUE возвращает текущее значение — неверно на всех строках, кроме последней. Растянутый фрейм, ROWS UNBOUNDED PRECEDING AND UNBOUNDED FOLLOWING, покрывает всю партицию, поэтому LAST_VALUE возвращает истинное финальное значение партиции — верно на каждой строке."
  caption="Имя функции обещает последнюю строку; реальность решает фрейм. С ORDER BY фрейм по умолчанию кончается на CURRENT ROW — растяни его до UNBOUNDED FOLLOWING, и баг уходит одной клаузой."
  hue="var(--d-infra)"
/>

<Inset kind="mistake" lang="ru">
Причина, по которой `LAST_VALUE` путает людей, в том, что имя функции описывает намерение, а *фрейм* решает реальность. Две продакшен-безопасные привычки: (1) всякий раз, когда пишешь `LAST_VALUE` с `ORDER BY`, сразу добавляй `ROWS BETWEEN UNBOUNDED PRECEDING AND UNBOUNDED FOLLOWING`, иначе тихо получишь текущую строку; (2) если тебе реально нужно «значение, связанное с max/min какой-то колонки», предпочти `FIRST_VALUE(col) OVER (... ORDER BY key DESC)` — упорядочь так, чтобы нужная строка была первой, куда фрейм по умолчанию уже дотягивается — или используй форму `DISTINCT ON` / `MAX`. Та же дисциплина «числа не лгут», что и в уроке о фреймах: не верь имени функции, проверяй фрейм.
</Inset>

<Quiz
  id="05-running-totals-quiz-1"
  lessonSlug="05-running-totals"
  lang="ru"
  question="LAST_VALUE(amount) OVER (PARTITION BY region ORDER BY sale_date) возвращает неверное значение на большинстве строк. Почему?"
  choices={[
    { label: "Фрейм по умолчанию кончается на CURRENT ROW, поэтому последняя строка фрейма И ЕСТЬ текущая строка — LAST_VALUE возвращает текущую сумму, а не финальную в партиции", correct: true },
    { label: "LAST_VALUE не работает с PARTITION BY", misconception: "Он отлично работает с PARTITION BY; проблема в верхней границе CURRENT ROW фрейма по умолчанию, а не в партиционировании." },
    { label: "Нужно использовать FIRST_VALUE вместо LAST_VALUE", misconception: "FIRST_VALUE отвечает на другой вопрос (первая строка). Чтобы получить истинную последнюю строку, растяни фрейм LAST_VALUE до UNBOUNDED FOLLOWING." },
    { label: "ORDER BY sale_date разворачивает строки", misconception: "ORDER BY ничего не разворачивает; проблема в том, что конечная граница фрейма по умолчанию — CURRENT ROW, а не в направлении сортировки." },
  ]}
/>

<Quiz
  id="05-running-totals-quiz-2"
  lessonSlug="05-running-totals"
  lang="ru"
  question="Нужно изменение выручки месяц-к-месяцу. Какое выражение верно?"
  choices={[
    { label: "revenue - LAG(revenue) OVER (ORDER BY month), с NULLIF для защиты нулевого/отсутствующего прежнего значения", correct: true },
    { label: "revenue - LEAD(revenue) OVER (ORDER BY month)", misconception: "LEAD смотрит на СЛЕДУЮЩИЙ месяц, давая следующий-к-текущему, а не месяц-к-ПРЕДЫДУЩЕМУ. Для прошлого периода используй LAG." },
    { label: "SUM(revenue) OVER (ORDER BY month) - revenue", misconception: "Это вычитает текущий месяц из накопительного итога, который есть сумма всех предыдущих месяцев, а не значение одного прошлого месяца." },
    { label: "revenue - MIN(revenue) OVER ()", misconception: "Это сравнивает с минимумом за всё время, а не с непосредственно предыдущим месяцем. Период-к-периоду требует LAG." },
  ]}
/>

<MetaphorComplete
  id="05-running-totals-metaphor-1"
  lessonSlug="05-running-totals"
  lang="ru"
  setup="Заполни пропуск: чтобы LAST_VALUE возвращал истинную финальную строку партиции, а не текущую строку, растяни конечную границу фрейма до UNBOUNDED _______."
  accepted={["following", "следующая"]}
  canonical="following"
  explanation="Фрейм по умолчанию кончается на CURRENT ROW, поэтому LAST_VALUE видит текущую строку как «последнюю». Растяжка верхней границы до UNBOUNDED FOLLOWING делает фрейм покрывающим всю партицию, так что LAST_VALUE действительно дотягивается до последней строки партиции по ORDER BY."
/>

</Explanation>

<KeyTakeaway>Накопительные итоги — это `SUM` по `ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW`; скользящие средние просто ограничивают начало (`n PRECEDING`). `LAG`/`LEAD` читают прошлую/следующую строку для дельт период-к-периоду (защита через `NULLIF` и дефолт). Ловушка: `LAST_VALUE` с `ORDER BY` возвращает *текущую* строку, потому что фрейм по умолчанию кончается на `CURRENT ROW` — растяни его до `ROWS BETWEEN UNBOUNDED PRECEDING AND UNBOUNDED FOLLOWING`, чтобы получить реальное последнее значение партиции.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="05-running-totals-retrieval"
  lang="ru"
  questions={[
    { q: "В чём разница во фрейме между накопительным итогом и скользящим средним по 3 строкам?", a: "Накопительный итог использует ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW (от начала до сюда). Скользящее среднее по 3 строкам назад использует ROWS BETWEEN 2 PRECEDING AND CURRENT ROW (только текущая строка и две перед ней). Меняется лишь начальная граница — UNBOUNDED PRECEDING против 2 PRECEDING." },
    { q: "Почему LAST_VALUE часто возвращает неверное значение и как это чинить?", a: "С ORDER BY в окне фрейм по умолчанию — RANGE/ROWS, кончающийся на CURRENT ROW, поэтому последняя строка фрейма — текущая, и LAST_VALUE возвращает текущее значение, а не финальное в партиции. Чини растяжкой фрейма: LAST_VALUE(col) OVER (... ORDER BY ... ROWS BETWEEN UNBOUNDED PRECEDING AND UNBOUNDED FOLLOWING)." },
    { q: "Как считать изменение месяц-к-месяцу и какие краевые случаи защищать?", a: "Используй revenue - LAG(revenue) OVER (ORDER BY month). LAG первой строки — NULL (нет предыдущей строки), так что дельта там NULL — задай LAG(revenue, 1, 0), если предпочитаешь 0. Для процентного изменения оберни предыдущее значение в NULLIF(..., 0), чтобы избежать деления на ноль, когда прошлый месяц был 0." },
  ]}
/>

<Recap lang="ru">
Это инструментарий накопления. Накопительный итог — это `SUM` по `ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW`; скользящее среднее — то же с ограниченным началом вроде `2 PRECEDING`; оба опираются на явный фрейм `ROWS` из прошлого урока, чтобы ничьи их не искажали. `LAG` и `LEAD` шагают назад и вперёд по смещению строки, чтобы считать дельты период-к-периоду без self-join — помни, что первая/последняя строка даёт `NULL`, и защищай процентную математику через `NULLIF`. Фирменная ловушка — `LAST_VALUE`: его имя обещает финальную строку партиции, но с `ORDER BY` фрейм по умолчанию кончается на `CURRENT ROW`, поэтому он возвращает текущую строку; растяни фрейм до `UNBOUNDED FOLLOWING` (или переупорядочь и используй `FIRST_VALUE`, или `MAX`/`MIN` по всей партиции). `FIRST_VALUE` выглядит нормально лишь потому, что фрейм по умолчанию уже начинается с первой строки партиции. Теперь, встречая `LAST_VALUE` в запросе и видя корректные числа на dev, — всё равно добавь `ROWS BETWEEN UNBOUNDED PRECEDING AND UNBOUNDED FOLLOWING`: в продакшене он сломается в первый же день, когда появится tied-timestamp. Следующий урок собирает эти примитивы в реальные паттерны — gaps-and-islands, сессионизацию, дедупликацию — и стоимость производительности при стопке нескольких окон.
</Recap>
