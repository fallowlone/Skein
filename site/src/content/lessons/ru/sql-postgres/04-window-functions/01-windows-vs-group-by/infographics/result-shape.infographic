infographic compare-binary-horizontal-underline-text-vs
data
  title Тот же SUM, обратная форма результата
  desc Выбор — это форма нужного ответа, а не то, какой инструмент лучше
  compares
    - label GROUP BY
      children
        - label GROUP BY
          desc Редуктор
        - label Одна строка на группу
          desc Строки деталей уничтожены
        - label 8 продаж на входе, 4 строки на выходе
          desc По одной на регион
        - label Брать для сводки
          desc Отчёты, счётчики, итоги за день
    - label Окно
      children
        - label OVER()
          desc Построчное окно
        - label Каждая строка сохранена
          desc Агрегат идёт рядом
        - label 8 продаж на входе, 8 строк на выходе
          desc Число строк не меняется
        - label Брать для деталей плюс агрегата
          desc Процент от итога, ранг, нарастающий итог
theme
  palette #f43f5e #14b8a6
