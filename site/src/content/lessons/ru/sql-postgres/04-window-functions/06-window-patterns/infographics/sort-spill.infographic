infographic chart-column-simple
data
  title Время сортировки: в памяти против сброса на диск (50M строк)
  desc Секунды на одну оконную сортировку 50M строк — влезает в work_mem против сброса во временные файлы
  values
    - label В памяти (влезает в work_mem)
      value 2
    - label Сброс на диск
      value 40
theme
  palette #14b8a6 #f43f5e
