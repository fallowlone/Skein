---
concepts:
- deploy-release-decoupling
- flag-types
- percentage-rollout
- kill-switch
- flag-debt
- sticky-bucketing
estMin: 13
lang: ru
level: senior
order: 7
prereqs: []
slug: quiz-choice
sources:
- https://specbranch.com/posts/knight-capital/
- https://docs.getunleash.io/topics/feature-flags/feature-flag-best-practices
- https://launchdarkly.com/docs/guides/flags/technical-debt
status: ready
summary: "Синтез по всему юниту feature flags в формате выбора: разделение deploy и release, четыре типа флагов и их жизненные циклы, sticky-rollout, kill switch и flag debt."
title: 'Feature flags: тест с выбором ответа'
track: engineering-practice
unit: 05-feature-flags
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Шесть вопросов, пронизывающих весь юнит. Каждый отражает решение, которое вы принимаете в реальном релизе: это не определение для заучивания, а компромисс, который нужно взвесить, когда флаг уже включён в проде и на него идёт трафик.
</Hook>

<Goal lang="ru">
Убедитесь, что вы связываете воедино разделение deploy и release, четыре типа флагов с противоположными жизненными циклами, sticky percentage rollout, kill switch и flag debt — синтез, к которому вёл урок.
</Goal>

<Quiz
  id="05-feature-flags-choice-1"
  lessonSlug="05-feature-flags"
  lang="ru"
  question="Команда вливает недоделанную фичу в main за выключенным по умолчанию release-флагом и деплоит её. Коллега возражает, что катить недописанный код в прод безответственно. Какова senior-трактовка?"
  choices={[
    { label: "Deploy и release — разные события. Код задеплоен «тёмным»: он есть в бинарнике, но проверка флага возвращает false для всех, и фича выпускается только при включении флага. Именно это позволяет вливать код непрерывно и маленькими порциями вместо того, чтобы он гнил в долгоживущей ветке.", correct: true },
    { label: "Коллега прав; недописанный код должен жить в feature-ветке до готовности, а потом деплоиться и выпускаться вместе.", misconception: "Долгоживущие ветки расходятся с main, копят конфликты слияния и приводят к big-bang релизу в конце. Разделение deploy и release выключенным флагом — это как раз способ безопасно катить незавершённый код и сохранять trunk-based development." },
    { label: "Это нормально, только если недописанный код в отдельном сервисе, до которого продовый трафик не доходит.", misconception: "Разделение — про флаг, а не про отдельное окружение. Один и тот же продовый бинарник держит код и тёмным, и активным; экспозицию решает проверка флага, а не сетевая изоляция." },
    { label: "Это нормально, потому что следующий deploy автоматически выпустит фичу, как только она будет дописана.", misconception: "Автовыпуск при деплое — это противоположность разделению, он снова сваривает два события. Release должен оставаться отдельным, осознанным переключением флага." },
  ]}
/>

<Quiz
  id="05-feature-flags-choice-2"
  lessonSlug="05-feature-flags"
  lang="ru"
  question="В спринт по чистке инженер находит булев флаг, который месяцами отдаёт всем одно и то же значение, и предлагает удалить его вместе с мёртвой веткой. Что обязательно проверить до мёрджа этого PR?"
  choices={[
    { label: "Тип флага. Release-флаг на 100% месяцами — это долг, его удаляют; ops/kill switch намеренно простаивает до инцидента. Один и тот же «застойный» сигнал, противоположная правильная судьба — решает тип, а не застой.", correct: true },
    { label: "Ничего — любой флаг, отдающий одно значение более 30 дней, по определению устарел и его можно удалять.", misconception: "Эвристика «одно значение 30 дней» помечает кандидатов, но не разрешает удаление. Kill switch и должен отдавать одно значение, пока он не понадобится; удалив его, вы убираете страховочную сетку." },
    { label: "Точно ли процент раскатки равен 100, а не 99 или 95.", misconception: "Решает не точный процент, а назначение и жизненный цикл флага. Постоянный ops-флаг может стоять на любом значении и всё равно не подлежит удалению." },
    { label: "Упоминается ли флаг более чем в одном файле перед удалением.", misconception: "Число ссылок говорит о радиусе чистки, а не о правильности удаления. Kill switch с единственной ссылкой — всё равно kill switch, который нужно сохранить." },
  ]}
/>

<Quiz
  id="05-feature-flags-choice-3"
  lessonSlug="05-feature-flags"
  lang="ru"
  question="Раскатка на 25% показывает чистые error rate и latency, но поддержка сообщает, что у пользователей новый UI мигает обратно на старый между загрузками страниц. В чём дефект?"
  choices={[
    { label: "Rollout не sticky. Каждый запрос заново разыгрывает корзину вместо того, чтобы хешировать стабильный ключ (userId плюс groupId) в фиксированную корзину 0–99, поэтому один и тот же пользователь попадает на разные варианты между запросами — мигающий UI и испорченные данные эксперимента.", correct: true },
    { label: "25% слишком мало; поднять до 50% — и мигание прекратится.", misconception: "Расширение раскатки меняет, сколько пользователей внутри, а не остаётся ли конкретный пользователь внутри. Не-sticky раскатка на 50% мигает так же — лечит детерминированное побучковое распределение per-user, а не больший процент." },
    { label: "SDK считает по устаревшему конфигу, нужен более короткий интервал опроса.", misconception: "Интервал опроса определяет скорость распространения изменения флага, а не консистентность в рамках запроса. Даже при мгновенном распространении не-sticky хеш переразыгрывается на каждом вычислении и мигает." },
    { label: "CDN кэширует старый UI для части запросов.", misconception: "Кэширование объяснило бы устаревший ассет, но симптом — пользователь скачет между вариантами на живых вычислениях, а это сигнатура недетерминированного распределения, которую как раз и чинит стабильный ключ хеширования." },
  ]}
/>

<Quiz
  id="05-feature-flags-choice-4"
  lessonSlug="05-feature-flags"
  lang="ru"
  question="Почему SDK вычисляют флаг как локальный in-memory хеш-lookup с фоновой синхронизацией, а не спрашивают сервис флагов на каждую проверку?"
  choices={[
    { label: "Флаг проверяется много раз на путь запроса. Сетевой round-trip на каждую проверку добавил бы задержку и сделал сервис флагов жёсткой зависимостью — если он лежит, лежит и ваше приложение. Локальное вычисление превращает проверку в lookup по хеш-таблице, а сбой сервиса флагов деградирует до последнего известного конфига.", correct: true },
    { label: "Так просто быстрее писать SDK; по корректности это идентично вызову API на каждую проверку.", misconception: "Это не просто удобство реализации. Локальное вычисление меняет режим отказа: сбой сервиса флагов превращается в плавную деградацию до кэшированного конфига вместо падения вашего собственного сервиса." },
    { label: "Локальное вычисление гарантирует, что у всех серверов одно и то же состояние флага в один и тот же момент.", misconception: "Фоновая синхронизация означает короткое окно распространения, когда серверы могут расходиться. Выгода — в задержке и развязке с сервисом флагов, а не в мгновенной глобальной консистентности." },
    { label: "Это избавляет от необходимости в sticky-bucketing, потому что хеш считается централизованно.", misconception: "Sticky-bucketing достигается локальным хешированием стабильного ключа пользователя; централизация вызова не изменит stickiness и вернёт сетевую зависимость." },
  ]}
/>

<Quiz
  id="05-feature-flags-choice-5"
  lessonSlug="05-feature-flags"
  lang="ru"
  question="В сервисе живут десять независимых булевых флагов. В проде всплывает баг, который QA ни разу не воспроизвёл в staging. Как число флагов связано с этим?"
  choices={[
    { label: "Десять независимых флагов дают 2^10 = 1024 возможных рантайм-конфигураций. Все их прогнать нельзя, поэтому конкретная комбинация, на которую попал пользователь в проде, может быть той, что не тестировалась ни разу. Флаги умножают пространство состояний системы.", correct: true },
    { label: "Флаги не влияют на поверхность багов; каждый флаг независим, так что они не могут взаимодействовать.", misconception: "Независимые тумблеры всё равно комбинируются в мультипликативное пространство конфигураций. Два флага, чьи пути кода трогают одно и то же состояние, могут взаимодействовать в комбинации, не покрытой ни одним одно-флаговым тестом." },
    { label: "Десять флагов добавляют десять дополнительных состояний для тестирования, так что покрытие растёт линейно.", misconception: "Это комбинаторика, а не сложение: N булевых флагов дают 2^N конфигураций. Десять флагов — это более тысячи комбинаций, а не десять." },
    { label: "Решение — добавить в staging тесты на все комбинации флагов перед следующим релизом.", misconception: "Исчерпывающе протестировать 2^N комбинаций нереально для любого реального N. Устойчивый ответ — держать N маленьким, оперативно удаляя флаги, а не пытаться протестировать весь взрыв." },
  ]}
/>

<Quiz
  id="05-feature-flags-choice-6"
  lessonSlug="05-feature-flags"
  lang="ru"
  question="Инцидент Knight Capital в 2012 году стоил ~$440M за 45 минут. Если свести его к уроку о жизненном цикле флага, что на самом деле пошло не так?"
  choices={[
    { label: "Мёртвая тестовая фича 2003 года осталась в кодовой базе за старым флагом Power Peg. Деплой переиспользовал этот флаг, и на одном необновлённом сервере его включение разбудило мёртвый код. Корень — flag debt: застойный флаг, который так и не удалили, а затем переиспользовали.", correct: true },
    { label: "Пайплайн деплоя выкатил код лишь на 7 из 8 серверов, так что чинить нужно только автоматизацию деплоя.", misconception: "Частичный деплой был триггером, но заряженным ружьём был мёртвый годами флаг и его мёртвый код, всё ещё присутствующий и переиспользуемый. Надёжные деплои помогают; удаление застойных флагов и их веток убирает сами патроны." },
    { label: "У них не было kill switch, чтобы остановить торги, который дал бы постоянный ops-флаг.", misconception: "Kill switch — хорошая практика, но урок этого инцидента выше по течению: мёртвый код за застойным переиспользованным флагом вообще не должен был быть живым, чтобы сработать. Типизируйте флаги и удаляйте release-флаги в конце жизни." },
    { label: "Флаг выставили на неправильный процент во время раскатки.", misconception: "Это была не ошибка процента раскатки. Давно мёртвый флаг переиспользовали и он разбудил мёртвый код — провал flag debt и чистки, а не ошибка ramp." },
  ]}
/>

<Recap lang="ru">
Сквозная линия: deploy и release — разные события, а флаг — переключатель между ними: катим тёмным, выпускаем тумблером. Четыре типа (release, ops/kill switch, experiment, permission) имеют противоположные правильные сроки жизни, поэтому тип записывают, а не угадывают. Rollout обязан быть sticky (хешируем стабильный ключ в фиксированную корзину 0–99), иначе варианты мигают и данные эксперимента портятся; вычисление — локальный хеш-lookup, так что сбой сервиса флагов деградирует плавно. И каждый живой флаг — это ветка в проде: N флагов означают 2^N конфигураций, поэтому дисциплина — это жизненный цикл: давайте release-флагам срок, помечайте kill switch как постоянные и удаляйте застойные флаги, пока один не стал следующим Knight Capital.
</Recap>
