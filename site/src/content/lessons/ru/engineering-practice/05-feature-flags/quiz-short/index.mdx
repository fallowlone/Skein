---
concepts:
- deploy-release-decoupling
- flag-types
- flag-lifecycle
- sticky-bucketing
- kill-switch
- flag-debt
estMin: 14
lang: ru
level: senior
order: 8
prereqs: []
slug: quiz-short
sources:
- https://specbranch.com/posts/knight-capital/
- https://docs.getunleash.io/topics/feature-flags/feature-flag-best-practices
- https://launchdarkly.com/docs/guides/flags/technical-debt
status: ready
summary: "Промпты на свободное воспроизведение по всему юниту feature flags. Сначала ответьте своими словами, затем раскройте образцовый ответ и сравните."
title: 'Feature flags: тест на воспроизведение'
track: engineering-practice
unit: 05-feature-flags
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";

<Hook>
Воспроизведение по памяти эффективнее перечитывания. На каждый промпт проговорите или запишите полный ответ из памяти, прежде чем открыть образцовый — именно усилие припоминания закрепляет материал.
</Hook>

<Goal lang="ru">
Восстановите спину юнита — разделение deploy и release, четыре типа флагов и их жизненные циклы, sticky-rollout, kill switch, локальное вычисление и flag debt — не подглядывая в урок.
</Goal>

<RetrievalDrawer
  client:visible
  id="05-feature-flags-short-retrieval"
  lang="ru"
  questions={[
    { q: "Объясните, как feature flags разделяют deploy и release и почему это меняет то, как команда катит код.", a: "Деплой кода и релиз фичи обычно одно событие — мёрдж, попавший в прод, и есть момент, когда пользователи видят изменение, поэтому каждый рискованный запуск — это высокоставочный деплой. Feature flag разделяет их. Вы деплоите код тёмным: он есть в бинарнике, но обёрнут в проверку флага, возвращающую false для всех, а затем выпускаете позже, переключив флаг, без пересборки и редеплоя. SDK подхватывают изменение быстро — Unleash опрашивает на дефолтном интервале ~15 секунд, LaunchDarkly стримит по SSE за доли секунды. Это позволяет вливать код непрерывно и маленькими порциями (trunk-based development вместо долгоживущих веток), держать недописанные фичи безопасно за выключенным флагом и превращает release в осознанное конфиг-решение, а не инженерное событие. Раскатка становится процентным ramp, а откат — выключением флага за секунды." },
    { q: "Назовите четыре типа флагов и объясните, почему запись типа важна для жизненного цикла.", a: "Release-флаги гейтят фичу в работе для постепенной раскатки и живут дни-недели — удаляются после 100%, владелец dev-команда. Ops/kill switch отключают подсистему под нагрузкой или в инциденте и постоянны по замыслу, владелец SRE/on-call. Experiment-флаги отдают A/B-варианты на один цикл и затем удаляются, владелец product/data. Permission-флаги гейтят по плану, роли или праву и долгоживущи, привязаны к продуктовой модели, владелец product. Тип важен, потому что два флага могут выглядеть одинаково — один булев, месяцами отдающий одно значение, — но иметь противоположную правильную судьбу: застойный release-флаг надо удалить, а «прибранный» кем-то kill switch убирает страховочную сетку. Решает тип, а не сигнал застоя, поэтому его надо записывать, а не угадывать." },
    { q: "Как механически работает sticky percentage rollout и что ломается, если он не sticky?", a: "Вместо прыжка с 0% на 100% вы делаете ramp 1% → 5% → 25% → 100%, на каждом шаге наблюдая error rate и latency, и мгновенно выключаете, если шаг ломается. Чтобы назначение оставалось консистентным, SDK хеширует стабильный ключ — userId плюс groupId — в корзину 0–99; раскатка 25% означает, что корзины 0–24 включены. Хеш детерминированный и считается локально, поэтому данный пользователь всегда попадает в ту же корзину, а вычисление — субмиллисекундное без сетевого вызова на проверку. Если rollout не sticky — например, свежее случайное число на каждое вычисление — один и тот же пользователь скачет между вариантами между запросами: UI мигает, а данные эксперимента портятся, потому что экспозиция не стабильна per-user." },
    { q: "Почему SDK вычисляют флаги локально в памяти с фоновой синхронизацией, а не вызывают сервис флагов на каждую проверку?", a: "На горячем пути флаг проверяется тысячи раз на запрос. Сетевой round-trip на каждую проверку добавил бы задержку и, хуже, сделал сервис флагов жёсткой зависимостью: если он лежит, лежит ваше приложение. Поэтому SDK держит весь набор правил в памяти и обновляет его в фоне; проверка флага становится локальным lookup по хеш-таблице и хешем бакетинга, субмиллисекундной и без зависимостей в момент вычисления. Критическое следствие — режим отказа: сбой сервиса флагов деградирует до «последнего известного конфига», а не до падения вашего собственного сервиса. Поэтому же kill switch обязан быть подключён к локальному кэшу с безопасным дефолтом, а не как блокирующий удалённый вызов на критическом пути." },
    { q: "Что такое flag debt и какая дисциплина не даёт ему стать новым Knight Capital?", a: "Flag debt — это накопление флагов, переживших своё назначение, но всё ещё живущих в коде. Каждый флаг — это if/else, обе ветки которого работают в проде, поэтому N независимых булевых дают 2^N возможных конфигураций — комбинаторную поверхность тестирования, которую нельзя полностью покрыть. Флаг на 100% месяцами — мёртвый конфиг, который всё ещё вычисляется, засоряет код и может быть переиспользован. Knight Capital — канонический провал: в 2012 деплой переиспользовал старый флаг Power Peg — мёртвую тестовую фичу 2003 года — и на одном необновлённом сервере его включение разбудило мёртвый код, выстрелив ~4 миллионами ошибочных сделок и потеряв ~$440 миллионов за 45 минут. Дисциплина — это жизненный цикл: записывать тип каждого флага, давать release-флагам срок и тикет на удаление, делать удаление частью definition of done, помечать kill switch постоянными, чтобы никто не удалил сетку, и архивировать застойные флаги — отдающие одно значение ~30+ дней — по расписанию. Инструменты вроде Piranha от Uber автоматизируют PR на чистку." },
    { q: "Почему «каждый флаг — это ветка в проде» и есть senior-цена, уравновешивающая всю скорость, которую дают флаги?", a: "Флаг — не бесплатная абстракция, а if/else, обе ветки которого живут в проде одновременно, поэтому код, что вы выкатили, навсегда сложнее, а пространство состояний растёт мультипликативно. Десять независимых флагов — это 2^10 = 1024 рантайм-конфигурации; все их протестировать нельзя, и комбинация, на которую попал реальный пользователь, может быть той, что не прогонялась ни одним тестом, — так в прод попадают баги, которых staging не воспроизводил. Флаги ещё и гниют: оставленные нетронутыми, они становятся застойным конфигом, который всё ещё вычисляется и может быть переиспользован, чтобы разбудить забытый код. Поэтому, хотя флаги дают непрерывное вливание, безопасные тёмные деплои, постепенную раскатку и мгновенный откат, senior взвешивает это против растущей, недотестированной поверхности конфигураций и считает оперативное удаление флагов — удержание N маленьким — рычагом, удерживающим цену в границах." },
  ]}
/>

<Recap lang="ru">
Если вы смогли восстановить каждый ответ по памяти, вы держите спину юнита: флаги разделяют deploy и release, так что код катится тёмным и выпускается тумблером; четыре типа имеют противоположные правильные сроки жизни, так что тип записывают; rollout обязан быть sticky через стабильный хеш, иначе варианты мигают; вычисление — локальный lookup, так что сервис флагов никогда не жёсткая зависимость; и каждый живой флаг — это ветка в проде, 2^N конфигураций, поэтому дисциплина, предотвращающая следующий Knight Capital, — это жизненный цикл: давать release-флагам срок, помечать kill switch постоянными и удалять застойные флаги, пока один не переиспользовали.
</Recap>
