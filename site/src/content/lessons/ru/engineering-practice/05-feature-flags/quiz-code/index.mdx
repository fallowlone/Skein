---
concepts:
- flag-check
- sticky-bucketing
- kill-switch
- targeting-rule
- flag-debt
estMin: 14
lang: ru
level: senior
order: 9
prereqs: []
slug: quiz-code
sources:
- https://docs.getunleash.io/topics/feature-flags/feature-flag-best-practices
- https://launchdarkly.com/docs/guides/flags/technical-debt
status: ready
summary: "Читаем реальный код вычисления флага и правило targeting, предсказываем поведение или баг и выбираем самую рычажную правку, которую senior делает первой."
title: 'Feature flags: чтение кода и targeting'
track: engineering-practice
unit: 05-feature-flags
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Проверка флага, хеш бакетинга и правило targeting — места, где системы флагов тихо ломаются. Прочитайте сниппет, предскажите, что сделает с ним прод, и выберите правку, которую senior сделает первой.
</Hook>

<Goal lang="ru">
Отработайте цикл, который вы прогоняете на каждом ревью флагов: прочитать путь вычисления, заметить баг консистентности или fail-open и потянуться к структурной правке — sticky-bucketing, безопасный дефолт, тикет на удаление — прежде чем добавлять новые флаги.
</Goal>

## Сниппет 1 — проверка раскатки

```js
function inRollout(flag, user) {
  // раскатка 25%: разыгрываем свежее случайное число на каждый вызов
  return Math.random() * 100 < flag.rolloutPercent;
}

if (inRollout(newCheckout, user)) {
  return renderNewCheckout(user);
}
return renderOldCheckout(user);
```

<Quiz
  id="05-feature-flags-code-1"
  lessonSlug="05-feature-flags"
  lang="ru"
  question="Раскатка на 25% использует эту проверку. Что ломается в проде и в чём правка?"
  choices={[
    { label: "Math.random переразыгрывается на каждом вычислении, поэтому один и тот же пользователь скачет между новым и старым checkout между запросами — мигающий UI и непригодные данные эксперимента. Правка: хешировать стабильный ключ (userId плюс соль) в фиксированную корзину 0–99 и сравнивать bucket < rolloutPercent, чтобы назначение было детерминированным и sticky.", correct: true },
    { label: "Проценты с ошибкой на единицу; должно быть `<=`, а не `<`.", misconception: "Ошибка на единицу на границе сдвигает одну корзину, но не вызывает симптом. Дефект в том, что назначение случайно на каждый вызов, а не детерминированная функция пользователя, поэтому оно не sticky." },
    { label: "Math.random слишком медленный для горячего пути; закэшируйте значение.", misconception: "Random дёшев; дело не в скорости. Дело в корректности: недетерминированный розыгрыш на каждый вызов означает отсутствие per-user stickiness. Правка — стабильный хеш, а не кэширование розыгрыша." },
    { label: "Нужен сетевой вызов к сервису флагов, чтобы назначить корзину консистентно.", misconception: "Сетевой round-trip на каждое вычисление добавил бы задержку и жёсткую зависимость. Stickiness берётся из локального детерминированного хеширования стабильного ключа пользователя — без сетевого round-trip на проверку." },
  ]}
/>

## Сниппет 2 — путь kill switch

```js
async function chargeCard(order) {
  const enabled = await flags.evaluate("payments-enabled", order.user);
  if (!enabled) {
    throw new Error("payments disabled by kill-switch");
  }
  return gateway.charge(order);
}

// flags.evaluate бросает исключение, если сервис флагов недоступен
```

<Quiz
  id="05-feature-flags-code-2"
  lessonSlug="05-feature-flags"
  lang="ru"
  question="Здесь kill switch обёрнут вокруг платежей. В чём продовый риск и какова senior-правка?"
  choices={[
    { label: "evaluate делает блокирующий сетевой вызов, который бросает исключение при недоступности сервиса флагов, поэтому сбой сервиса флагов кладёт платежи — сервис флагов становится жёсткой зависимостью на горячем пути. Правка: вычислять локально по in-memory кэшу с фоновой синхронизацией и безопасным дефолтом, чтобы сбой деградировал до последнего известного конфига, а не ронял списание.", correct: true },
    { label: "Kill switch инвертирован; нужно списывать, когда `enabled` равно false.", misconception: "Полярность верна — disabled означает не списывать. Риск в том, что сетевой сбой в evaluate бросает исключение и блокирует путь, превращая сервис флагов в единую точку отказа." },
    { label: "Бросать Error — неправильный тип; нужно возвращать булево.", misconception: "Намеренный путь отключения вполне законно отклоняет списание. Настоящий дефект — сетевая зависимость на каждый вызов, которая роняет путь, когда сервис флагов лежит." },
    { label: "Нужно перепроверить флаг после списания, чтобы убедиться, что он всё ещё включён.", misconception: "Вторая проверка добавляет ещё одну сетевую зависимость, не починив первую. Структурная правка — локальное вычисление с безопасным дефолтом, чтобы сервис флагов никогда не был на критическом пути." },
  ]}
/>

## Сниппет 3 — правило targeting

```yaml
flag: new-dashboard
rules:
  - if: user.plan == "enterprise"
    serve: on
  - if: user.betaOptIn == true
    serve: on
  - rollout:
      percent: 10
      stickyBy: user.id
default: off
```

<Quiz
  id="05-feature-flags-code-3"
  lessonSlug="05-feature-flags"
  lang="ru"
  question="Enterprise-пользователь, который вдобавок подписался на бету, открывает дашборд. Какое правило решает его вариант и на какое общее свойство опирается этот конфиг?"
  choices={[
    { label: "Побеждает первое совпавшее правило: enterprise-правило отдаёт on, и вычисление останавливается до того, как будут рассмотрены правила беты или rollout. Targeting чувствителен к порядку — правила вычисляются сверху вниз, и первое совпадение замыкает вычисление, поэтому порядок правил кодирует приоритет.", correct: true },
    { label: "Все совпавшие правила комбинируются, так что on они получают, только если каждое правило согласно.", misconception: "Targeting не объединяет все правила по И. Правила вычисляются по порядку, и побеждает первое совпадение; enterprise-пользователь получает on независимо от последующих правил беты и rollout." },
    { label: "Решает раскатка на 10%, потому что процентные правила всегда приоритетнее атрибутных.", misconception: "Неявного приоритета у rollout-правил нет. Приоритет чисто позиционный — enterprise-правило идёт первым и замыкает вычисление до достижения rollout." },
    { label: "Применяется дефолт `off`, потому что пользователь совпадает более чем с одним правилом, что неоднозначно.", misconception: "Несколько совпадений не неоднозначны; побеждает первое совпадение, а дефолт применяется, только когда не совпало ни одно правило. Этот пользователь сначала совпадает с enterprise-правилом." },
  ]}
/>

## Сниппет 4 — застойный флаг в коде

```js
// добавлено 2023-02 для релиза checkout-v2, доведено до 100% в 2023-03
if (flags.isEnabled("checkout-v2")) {
  return renderCheckoutV2(cart);
} else {
  return renderCheckoutV1(cart);   // V1 больше не поддерживается
}
```

<Quiz
  id="05-feature-flags-code-4"
  lessonSlug="05-feature-flags"
  lang="ru"
  question="Прошёл год; checkout-v2 отдаёт on всем с 2023-03, а V1 не поддерживается. Какое действие правильно и почему?"
  choices={[
    { label: "Удалить флаг и мёртвую ветку else целиком, встроив путь V2. Работа release-флага заканчивается на 100%; держать его — значит оставлять мёртвый неподдерживаемый код V1, который всё ещё вычисляется и может быть переиспользован — режим отказа Knight Capital. Удаление — часть definition of done фичи.", correct: true },
    { label: "Оставить; он отдаёт on всем, так что это безвредный мёртвый конфиг.", misconception: "Это не безвредно. Ветка всё ещё вычисляется, засоряет код, а неподдерживаемый путь V1 может быть переиспользован или случайно включён — ровно так застойный флаг разбудил мёртвый код в Knight Capital." },
    { label: "Превратить в постоянный kill switch, чтобы V1 можно было включить в экстренном случае.", misconception: "Включение годовалого неподдерживаемого пути воскрешает мёртвый код против устаревших допущений. Release-флаг не следует молча перепрофилировать в ops; это требует осознанного решения, владельца и поддерживаемого кода с обеих сторон." },
    { label: "Снизить раскатку до 95%, чтобы немного пользователей продолжали гонять V1 и держать его живым.", misconception: "Отправлять трафик на неподдерживаемый путь, чтобы держать его тёплым, институционализирует долг. Правильный ход — удалить V1 и флаг, а не сохранять путь, который никто не поддерживает." },
  ]}
/>

<Recap lang="ru">
Каждое ревью флагов читается одинаково: проверка раскатки обязана хешировать стабильный ключ в фиксированную корзину, иначе она мигает; kill switch обязан вычисляться локально с безопасным дефолтом, иначе сервис флагов становится жёсткой зависимостью, роняющей ваш горячий путь; правила targeting чувствительны к порядку и первое совпадение замыкает вычисление, так что порядок правил — это приоритет; а release-флаг на 100% с неподдерживаемой второй веткой — это flag debt на удаление, а не конфиг на сохранение. Прочитать путь вычисления, починить структуру, затем пере-ревьюить.
</Recap>
