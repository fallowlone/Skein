---
slug: 01-overview
lang: ru
track: engineering-practice
unit: 05-feature-flags
order: 1
level: junior
lessonType: topic
status: ready
title: "Feature flags: расцепить деплой и релиз, не утонув в долге флагов"
summary: "Флаги шипят код тёмным и релизят тумблером, покупая постепенный выкат и мгновенный kill-switch. Цена сеньора: каждый флаг — живая ветка в проде, а забытый забрал у Knight Capital $440M за 45 минут."
prereqs: []
deepensInto: []
spiral: []
concepts: ["deploy-release-decoupling", "flag-types", "percentage-rollout", "kill-switch", "flag-debt", "flag-lifecycle"]
estMin: 16
sources:
  - https://specbranch.com/posts/knight-capital/
  - https://en.wikipedia.org/wiki/Knight_Capital_Group
  - https://docs.getunleash.io/topics/feature-flags/feature-flag-best-practices
  - https://launchdarkly.com/docs/guides/flags/technical-debt
---

import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";

<Hook>
1 августа 2012 года Knight Capital задеплоил новый код на 7 из 8 своих торговых серверов. Новый код переиспользовал старый флаг с именем `Power Peg` — тестовую фичу 2003 года, мёртвую годами. На восьмом, незапатченном сервере переключение этого флага разбудило мёртвый код. За 45 минут он выстрелил 4 миллионами ошибочных сделок по 154 акциям и потерял примерно $440 миллионов — больше рыночной капитализации фирмы. Устаревший флаг, оставленный в кодбазе и переиспользованный, прикончил компанию из 1400 человек.
</Hook>

<Crux>Флаги расцепляют деплой и релиз: шипь код тёмным, релизь тумблером — но каждый живой флаг это ветка в проде, которой ты теперь владеешь.</Crux>

<Explanation>

## Деплой — не релиз

Первый рефрейм сеньора: задеплоить код и зарелизить фичу — два разных события. Без флагов они сварены вместе — мерж, попавший в `main`, и есть момент, когда пользователи видят изменение, так что каждый рискованный запуск становится высокоставочным деплоем в 2 ночи под взглядом всей команды. Feature flag их разделяет. Ты деплоишь код «тёмным» (присутствует в проде, обёрнут в `if (flags.newCheckout) {...}` и возвращает `false` для всех), потом релизишь позже переключением флага — без пересборки, без редеплоя.

Это разделение меняет то, как команды шипят. Код мержится непрерывно и мелко (trunk-based development перестаёт зависеть от долгоживущих веток), незаконченные фичи безопасно сидят за выключенным флагом, а релиз становится изменением конфига, что SDK подхватывает за секунды. Клиентский спек Unleash поллит состояние флага на дефолтном интервале 15 секунд; LaunchDarkly стримит обновления по SSE, так что тумблер распространяется на работающие серверы заметно меньше чем за секунду. Релиз перестаёт быть деплоем и становится решением.

## Четыре типа флагов — и почему их жизненные циклы различны

«Feature flag» прячет четыре разных вида, и ошибка сеньора — обращаться со всеми одинаково. Тип диктует, как долго флаг должен жить и кто им владеет.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Тип</th>
        <th class="border border-border px-3 py-2 text-left">Назначение</th>
        <th class="border border-border px-3 py-2 text-left">Срок жизни</th>
        <th class="border border-border px-3 py-2 text-left">Владелец</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2"><code>release</code></td>
        <td class="border border-border px-3 py-2">Гейтит фичу в процессе; постепенный выкат</td>
        <td class="border border-border px-3 py-2">Дни–недели — удалить после 100%</td>
        <td class="border border-border px-3 py-2">Команда разработки</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2"><code>ops</code> / kill-switch</td>
        <td class="border border-border px-3 py-2">Отключить подсистему под нагрузкой или в аварии</td>
        <td class="border border-border px-3 py-2">Постоянный — держат нарочно</td>
        <td class="border border-border px-3 py-2">SRE / on-call</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2"><code>experiment</code></td>
        <td class="border border-border px-3 py-2">Отдавать A/B-варианты, мерить метрику</td>
        <td class="border border-border px-3 py-2">Один цикл эксперимента — потом удалить</td>
        <td class="border border-border px-3 py-2">Продукт / данные</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2"><code>permission</code></td>
        <td class="border border-border px-3 py-2">Гейтить по плану, роли или праву</td>
        <td class="border border-border px-3 py-2">Долгоживущий — привязан к модели продукта</td>
        <td class="border border-border px-3 py-2">Продукт</td>
      </tr>
    </tbody>
  </table>
</div>

Release-флаг, которого никогда не удалили, тихо стал долгом флагов. Kill-switch, который кто-то «прибрал», потому что он выглядел устаревшим, убрал твою страховочную сетку. Тот же механизм, противоположная правильная судьба — ровно поэтому тип надо записывать, не угадывать.

## Постепенный выкат и мгновенный откат

Суперсила release-флага — процентный выкат. Вместо 0% → 100% ты разгоняешь: 1% → 5% → 25% → 100%, следя за частотой ошибок и латентностью на каждом шаге. Если новый путь ломается на 5%, ты выключаешь флаг, и радиус поражения был 5% трафика, восстановлен за секунды — без отката коммита, без пайплайна хотфикса, без редеплоя. Вот настоящий аргумент за флаги: откат перестаёт быть инженерным событием и становится тумблером конфига.

Механизм важен для корректности. Хороший выкат *липкий*: тот же пользователь должен продолжать получать тот же вариант между запросами, иначе твой UI мерцает, а данные эксперимента — мусор. SDK делают это, хешируя стабильный ключ (`userId` плюс `groupId`) в корзину 0–99; «выкат 25%» значит, что корзины 0–24 включены. Хеш детерминирован и считается локально, так что оценка субмиллисекундна и не требует сетевого вызова на проверку — SDK держит весь набор правил в памяти и обновляет его в фоне.

<Inset kind="why" lang="ru">
Зачем хешировать локально вместо запроса к серверу на каждую оценку? На масштабе флаг проверяется тысячи раз на путь запроса. Сетевой round-trip на проверку добавил бы латентность и жёсткую зависимость: если сервис флагов лежит, твоё приложение лежит. Локальная in-memory оценка с фоновой синхронизацией означает, что проверка флага — это lookup в хешмапе, а падение сервиса флагов деградирует до «последнего известного конфига», а не в твою аварию.
</Inset>

## Каждый флаг — ветка в проде

Вот цена, что сеньор взвешивает против всей этой скорости. Каждый живой флаг — это `if`/`else`, оба пути которого работают в проде одновременно. Десять независимых булевых флагов — это 2^10 = 1024 возможных рантайм-конфигурации — ты не можешь протестировать их все, и комбинация, в которую пользователь реально попадает в проде, может быть той, что не задействовал ни один тест. Флаги умножают пространство состояний твоей системы. Они ещё и гниют: флаг, оставленный на 100% месяцами, — мёртвый конфиг, который всё ещё оценивается, всё ещё захламляет код веткой, которую никто не читает, и — урок Knight Capital — может быть *переиспользован*, разбудив код, о котором все забыли.

Это долг флагов, и он не гипотетический. Собственное руководство LaunchDarkly определяет устаревший флаг как тот, что отдаёт один и тот же вариант всем больше ~30 дней, и рекомендует архивировать по расписанию; инструменты вроде Piranha от Uber существуют ровно для того, чтобы AST-парсить кодбазы и автогенерить pull request, удаляющий флаг и его мёртвую ветку. Дисциплина — это вся игра: release-флаг должен нести срок годности и Jira-тикет на удаление, kill-switch'и должны быть помечены постоянными, чтобы никто их не «прибрал», а удаление — часть definition of done фичи, а не когда-нибудь-может-быть.

<TradeoffMatrix
  id="05-feature-flags-tradeoff-1"
  lessonSlug="01-overview"
  lang="ru"
  prompt="Новый путь checkout собран и протестирован в staging. Хочешь зашипить его в прод сегодня, но снизить риск запуска. Выбери подход к выкату."
  options={[
    { name: "Задеплоить тёмным за release-флагом, разогнать 1% → 5% → 25% → 100% следя за частотой ошибок, с флагом как мгновенным kill-switch", summary: "Постепенный выкат с флагом.", correct: true, justification: "Это учебниковое применение release-флага: код шипится в прод, но остаётся выключенным, потом релизишь маленькому срезу и расширяешь, только пока метрики чисты. Если новый путь ломается на 5%, выключаешь флаг, и урон был 5% трафика, восстановлен за секунды — без отката, без редеплоя. Флаг должен быть липким по userId, чтобы пользователи не скакали между checkout'ами, и нести срок годности, чтобы его удалили на 100%." },
    { name: "Смержить в main и задеплоить на 100% пользователей разом; ты тестировал в staging, так что должно быть нормально", summary: "Big-bang деплой.", justification: "Staging никогда не воспроизводит микс трафика, данные и конкурентность прода. Big-bang на 100% означает, что любой баг бьёт по всем пользователям разом, а откат — это полный цикл revert-и-редеплой, измеряемый минутами-часами, пока пользователи страдают. Это ровно высокоставочный деплой, для устранения которого флаги и существуют." },
    { name: "Добавить флаг, но оставить его включённым навсегда после запуска, чтобы можно было когда-нибудь вернуть старый путь", summary: "Держать флаг вечно.", justification: "Это превращает release-флаг в постоянный долг флагов. Старый путь теперь мёртвый код, который всё ещё оценивается и может быть случайно включён — режим отказа Knight Capital. Работа release-флага кончается на 100%; удали его и мёртвую ветку. Только ops/kill-switch флаги нарочно постоянны." },
    { name: "Использовать долгоживущую feature-ветку, QA-ить её две недели, потом смержить и задеплоить, когда полностью готово", summary: "Задержка на ветке.", justification: "Долгоживущие ветки дрейфуют от main, копят болезненные конфликты мержа и всё равно дают big-bang релиз в конце. Флаги существуют ровно для того, чтобы код мог мержиться непрерывно и мелко, оставаясь тёмным — ты получаешь trunk-based development и контролируемый релиз вместо рискованного дня мержа." },
  ]}
/>

<Quiz
  id="05-feature-flags-quiz-1"
  lessonSlug="01-overview"
  lang="ru"
  question="Что на самом деле значит расцепить деплой и релиз?"
  choices={[
    { label: "Код задеплоен в прод выключенным за флагом, а фича релизится позже переключением флага — без редеплоя", correct: true },
    { label: "Ты деплоишь на отдельный release-сервер, на который пользователи попадают только после запуска", misconception: "Расцепление про флаг, не про отдельное окружение. Тот же прод-бинарь держит код тёмным и активным; флаг — не редеплой и не новый сервер — решает экспозицию." },
    { label: "Релизы происходят автоматически, как только деплой успешен", misconception: "Это противоположность — сцепление деплоя и релиза. Расцепление значит, что деплой беззвучен, а релиз — отдельный, намеренный тумблер." },
    { label: "Только одобренные QA сборки допускаются к деплою в прод", misconception: "Это гейт деплоя, не расцепление деплой/релиз. Расцепление про контроль видимой пользователю экспозиции уже задеплоенного кода через рантайм-тумблер." },
  ]}
/>

<Quiz
  id="05-feature-flags-quiz-2"
  lessonSlug="01-overview"
  lang="ru"
  question="Release-флаг стоит на выкате 100% три месяца, и никто его не трогал. Каков ход сеньора?"
  choices={[
    { label: "Это устаревший долг флагов — удали флаг и его теперь мёртвую ветку else из кода", correct: true },
    { label: "Оставь; он отдаёт 100%, так что безвреден", misconception: "Он всё ещё оценивается, всё ещё захламляет поверхность веток, и — как показал Knight Capital — забытый флаг может быть переиспользован и разбудить мёртвый код. Устаревшие флаги — обуза, не бесплатны." },
    { label: "Переключи его в постоянный kill-switch на всякий случай", misconception: "Не перепрофилируй тихо release-флаг в постоянный без намерения и маркировки. Работа release-флага кончается на 100%; конверсия в ops требует намеренного решения и владельца, не инерции." },
    { label: "Верни его на 50%, чтобы снова A/B-тестить старый путь", misconception: "Включение пути, с которого пользователей мигрировали три месяца назад, воскрешает мёртвый код против устаревших допущений — ровно форма инцидента Knight Capital." },
  ]}
/>

<DragOrder
  id="05-feature-flags-drag-1"
  lessonSlug="01-overview"
  lang="ru"
  prompt="Расставь жизненный цикл release-флага от создания до вывода из эксплуатации:"
  items={[
    { id: "a", label: "Создай флаг, дефолт выключен; задеплой код тёмным в прод" },
    { id: "b", label: "Релизь 1–5% пользователей, липко по userId; следи за частотой ошибок и латентностью" },
    { id: "c", label: "Расширь выкат до 25% → 100%, пока метрики чисты (или kill-switch выключи, если нет)" },
    { id: "d", label: "Стабильно на 100% — убери флаг и удали теперь мёртвую ветку else" },
    { id: "e", label: "Подтверди, что ссылок не осталось в коде или конфиге; закрой тикет на чистку" },
  ]}
  correctOrder={["a", "b", "c", "d", "e"]}
/>

</Explanation>

<KeyTakeaway>Флаги делят деплой и релиз: шипь тёмным, разгоняй процентом, kill-switch за секунды. Цена — живая ветка на флаг: комбинаторная поверхность тестов и долг флагов. Дай release-флагам срок годности; устаревший — заряженное ружьё.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="05-feature-flags-retrieval"
  lang="ru"
  questions={[
    { q: "Объясни, как feature flags расцепляют деплой и релиз, и почему это меняет то, как команда шипит.", a: "Задеплоить код и зарелизить фичу обычно одно событие: мерж, попавший в прод, и есть момент, когда пользователи видят изменение, так что каждый рискованный запуск — высокоставочный деплой. Feature flag разделяет эти два. Ты деплоишь код тёмным — он присутствует в проде, но обёрнут в проверку флага, что возвращает false для всех — и потом релизишь позже переключением флага, без пересборки или редеплоя. SDK подхватывают изменение быстро: Unleash поллит на дефолте 15 секунд, LaunchDarkly стримит по SSE субсекундно. Это позволяет коду мержиться непрерывно и мелко (trunk-based development вместо долгоживущих веток), позволяет незаконченным фичам безопасно сидеть за выключенным флагом и превращает релиз в решение конфига, а не инженерное событие. Выкат становится процентным разгоном (1% → 5% → 25% → 100%), а откат становится выключением флага за секунды вместо отката коммитов и редеплоя." },
    { q: "Что такое долг флагов, почему инцидент Knight Capital — канонический пример, и какая дисциплина его предотвращает?", a: "Долг флагов — накопление флагов, переживших своё назначение, но всё ещё живущих в коде. Каждый флаг — это if/else, оба пути которого работают в проде, так что N флагов создают 2^N возможных конфигураций — комбинаторную поверхность тестов, что не покрыть целиком. Флаг, оставленный на 100% месяцами, — мёртвый конфиг, что всё ещё оценивается, захламляет код и может быть переиспользован. Knight Capital — каноническая катастрофа: в 2012 деплой переиспользовал старый флаг с именем Power Peg — мёртвую тестовую фичу 2003 года — и на одном необновлённом сервере его переключение разбудило мёртвый код, выстрелив 4 миллионами плохих сделок и потеряв ~$440 миллионов за 45 минут, больше стоимости фирмы. Дисциплина: записывай тип каждого флага (release-флаги временны, kill-switch'и постоянны), давай release-флагам срок годности и тикет на удаление, делай удаление частью definition of done и трактуй устаревший флаг — отдающий один и тот же вариант всем ~30+ дней — как обузу для архивации по расписанию. Инструменты вроде Piranha от Uber автоматизируют PR чистки." },
  ]}
/>

<Recap lang="ru">
Feature flags расцепляют деплой и релиз: код шипится в прод тёмным, а рантайм-тумблер — подхваченный SDK за секунды — решает экспозицию, так что релиз становится решением, а не высокоставочным деплоем. Это покупает постепенный процентный выкат (1% → 5% → 25% → 100%, липко по userId, чтобы варианты не мерцали) и мгновенный откат kill-switch, при оценке локально как субмиллисекундный lookup хеша, так что падение сервиса флагов деградирует мягко. Но четыре типа флагов — release, ops/kill-switch, experiment, permission — имеют противоположные правильные сроки жизни, и каждый живой флаг — ветка в проде, так что N флагов значат 2^N конфигураций, что не протестировать целиком. Режим отказа — долг флагов: устаревшие или забытые флаги, что всё ещё оцениваются, захламляют код и могут быть переиспользованы — ровно механизм, стоивший Knight Capital ~$440M за 45 минут в 2012. Дисциплина сеньора — жизненный цикл: типизируй каждый флаг, давай release-флагам срок годности и тикет на удаление, помечай kill-switch'и постоянными, чтобы никто не удалил страховочную сетку, и делай чистку флагов частью «готово».
</Recap>
