---
concepts:
- test-pyramid
- end-to-end-testing
- integration-environment
- flaky-tests
- combinatorial-explosion
- service-boundary
deepensInto:
- 02-consumer-driven-contracts
estMin: 15
lang: ru
lessonType: topic
level: junior
order: 1
prereqs: []
slug: 01-overview
sources:
- https://martinfowler.com/articles/microservice-testing/
- https://www.infoq.com/news/2020/12/end-to-end-testing-microservices/
- https://pactflow.io/blog/contract-testing-vs-integration-testing/
- https://www.tweag.io/blog/2025-01-23-contract-testing/
spiral:
- feedback-loops
- blast-radius
status: ready
summary: 'Поднимать все сервисы вместе работает на двух и рушится на двадцати: e2e
  медленный, флакает на любом хопе и растёт как ~N², поэтому команды глушат его и
  не ловят ничего. Пирамида тестов ломается на границе сервиса, где unit-тесты мокают
  именно то, что и ломается.'
title: Дилемма интеграционного тестирования
track: engineering-practice
unit: 02-contract-testing
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
В 02:14 сервис `orders` начинает отдавать 500-е. Команда провайдера переименовала поле с `total_cents` на `amount_cents` и выкатила это — чистое, хорошо протестированное изменение, зелёное на их собственном наборе тестов. Ничто в их CI не знало, что три нижестоящих консьюмера читают `total_cents`. У команды было «полноценное интеграционное окружение», но оно было красным уже девять дней, и все перестали в него смотреть. Первым реальным сигналом для кого-либо стал пейджер, в проде, в пятницу. Фикс занял десять минут. *Узнать, что оно сломалось*, стоило аварии — потому что единственный тест, который мог это поймать, был тем, которому уже никто не доверял.
</Hook>

<Crux>Поднимать все сервисы вместе ради проверки интеграции работает на двух и рушится на двадцати — e2e медленный, флакает и растёт как ~N².</Crux>

<Explanation>

## Интуитивный ответ не масштабируется

Спросите «будут ли эти два сервиса по-прежнему общаться?» — и очевидный ответ: запусти оба, отправь реальный запрос, проверь ответ. Этот инстинкт верен — для двух сервисов. Беда в том, что происходит по мере роста системы. При N сервисах, вызывающих друг друга, число пар взаимодействий, которые хотелось бы прогнать, растёт примерно как **N²**, а end-to-end-тест любого одного сценария требует, чтобы *каждый* хоп в этом сценарии был здоров в один и тот же момент. Запрос, расходящийся через `gateway → orders → pricing → inventory → payments`, проходит свой e2e-тест только когда все пять сервисов, плюс их базы данных и очереди, одновременно подняты, промигрированы и засеяны правильными данными.

Требование «все здоровы одновременно» — это тихий убийца. У каждого сервиса, скажем, 98% шанс быть зелёным в общем окружении в любой момент. Свяжите пять вместе — и сценарий зелёный лишь ~90% времени; свяжите двенадцать — и вы уже ниже 80%. Окружение сломано не из-за *вашего* изменения — оно сломано, потому что чужая миграция применена наполовину, или провалился seed-скрипт, или зависимость в середине деплоя. Весь этот шум вы наследуете на каждом прогоне.

## Медленный, флакающий — а значит, игнорируемый

Эти две силы — комбинаторная подготовка и общая хрупкость — порождают три симптома, узнаваемых любой командой на большом e2e-наборе. **Медленный:** поднять полдюжины сервисов, базу и брокер сообщений до первого ассерта толкает наборы к 40+ минутам, так что они уходят из внутреннего цикла в ночной джоб, за которым никто не следит. **Флакающий:** поскольку любой хоп может отвалиться по таймауту, большая доля падений — часто называют около **1 из 5 прогонов** — не имеет отношения к проверяемому изменению. **Игнорируемый:** тест, кричащий «волки» четыре раза на каждый реальный баг, глушат, перезапускают-до-зелёного или карантинят. Заглушенный тест не ловит ничего, что строго хуже отсутствия теста, потому что он всё ещё стоит вам времени прогона и даёт ложное спокойствие.

Именно поэтому зрелые платформы переворачивают классический совет. Netflix и Spotify знаменито перекроили «пирамиду» тестов в **соты или ромб** на уровне сервисов: тонкая шапка настоящих end-to-end-сценариев (ориентир — примерно **5-10%** всех тестов), а основная масса уверенности в межсервисной совместимости спущена во что-то более быстрое и изолированное. Вопрос, на который отвечает весь этот юнит: *что это за более быстрая и изолированная штука?*

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Слой тестов</th>
        <th class="border border-border px-3 py-2 text-left">Что поднимает</th>
        <th class="border border-border px-3 py-2 text-left">Скорость / детерминизм</th>
        <th class="border border-border px-3 py-2 text-left">Ловит переименование в 02:14?</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">Unit-тест</td>
        <td class="border border-border px-3 py-2">Ничего — граница замокана</td>
        <td class="border border-border px-3 py-2">Миллисекунды, детерминирован</td>
        <td class="border border-border px-3 py-2">Нет — мок возвращает старую форму</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">End-to-end</td>
        <td class="border border-border px-3 py-2">Все сервисы + БД + очередь вместе</td>
        <td class="border border-border px-3 py-2">Минуты, флакает на любом хопе</td>
        <td class="border border-border px-3 py-2">Да — если окружение зелёное, а оно нет</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Недостающий слой</td>
        <td class="border border-border px-3 py-2">Одна сторона, против записанного соглашения</td>
        <td class="border border-border px-3 py-2">Секунды, детерминирован</td>
        <td class="border border-border px-3 py-2">Да — и прямо за столом автора</td>
      </tr>
    </tbody>
  </table>
</div>

## Граница — именно там, где в пирамиде дыра

Вот тонкий момент. Классическая пирамида тестов гласит: «много unit-тестов, меньше интеграционных, очень мало e2e». Внутри монолита это работает, потому что unit-тест прогоняет реальные внутрипроцессные вызовы между модулями. Через сетевую границу появляется течь: unit-тест консьюмера `orders` **мокает** провайдера `pricing`. Мок возвращает ту форму, которую автор консьюмера *считал*, что отдаёт `pricing`. В день, когда `pricing` переименовывает поле, unit-тесты консьюмера остаются зелёными — они проверяют устаревшее убеждение автора, а не реальность. То, что вероятнее всего сломается (контракт по проводу между сервисами), — это ровно то, что unit-тесты намеренно заглушают.

Так что вас сжимает с двух сторон. Unit-тесты быстры и надёжны, но слепы к границе по построению. End-to-end-тесты могут видеть границу, но слишком медленны и флакающи, чтобы гейтить каждый деплой. Авария с переименованием поля проваливается прямо в эту щель. Нужен тест, который проверяет именно границу — форму и семантику запросов и ответов, которыми обмениваются два сервиса, — **не** поднимая оба сервиса вместе. Такова форма задачи, которую решает остальной юнит.

<Inset kind="why" lang="ru">
«Просто держите интеграционное окружение зелёным» звучит как проблема дисциплины, но это проблема структурная. Аптайм общего окружения — это *произведение* индивидуальных аптаймов всех сервисов, поэтому он деградирует мультипликативно по мере добавления сервисов, а его здоровье принадлежит всем, что означает — никому. Призыв «постарайтесь сильнее» не меняет математику. Единственный долговечный фикс — перестать требовать, чтобы все сервисы были одновременно здоровы, лишь бы узнать, согласны ли два из них.
</Inset>

<TradeoffMatrix
  id="02-contract-testing-tradeoff-1"
  lessonSlug="01-overview"
  lang="ru"
  prompt="На платформе 18 сервисов с плотными HTTP-зависимостями. E2e-набор идёт 45 минут и падает на ~20% прогонов по причинам, не связанным с изменением. Команда хочет надёжную обратную связь по межсервисной совместимости. Какое направление наиболее здравое?"
  options={[
    { name: "Найти тест на каждую пару (per-edge), проверяющий границу без подъёма обоих сервисов, и оставить лишь тонкий e2e-смоук для критичных сценариев", summary: "Спустить совместимость на слой ниже.", correct: true, justification: "Это бьёт по корню: комбинаторному требованию, чтобы все сервисы были здоровы одновременно. Тест на пару, ограниченный двумя сервисами, масштабируется линейно и идёт детерминированно за секунды, поэтому может гейтить каждый деплой; тонкая e2e-шапка (5-10%) всё ещё покрывает реальные сценарии и тайминг, которые тестам на пару не видны. Вы сохраняете покрытие границы и сбрасываете флак." },
    { name: "Вложиться в e2e-набор: больше параллелизма, автоматические ретраи и окружение побольше", summary: "Раскачать e2e.", justification: "Ретраи прячут флак вместо того, чтобы его чинить, а параллелизм не меняет форму N² — вам всё ещё нужно одно общее окружение, здоровое целиком и сразу. Вы тратите деньги, делая медленный, хрупкий слой чуть быстрее, пока он продолжает падать по причинам, не связанным с изменением. Это ловушка, в которую команды попадают прямо перед тем, как ищут что-то лучше." },
    { name: "Сильнее опереться на unit-тесты с хорошими моками каждой зависимости и убрать интеграционное тестирование", summary: "Только unit + моки.", justification: "Unit-тесты мокают границу, так что по построению проверяют убеждение автора о зависимости, а не её реальную форму. Переименование поля провайдером оставляет unit-тесты всех консьюмеров зелёными — это ровно авария в 02:14. Unit-тесты необходимы, но структурно слепы к контракту по проводу." },
    { name: "Принять, что интеграционные баги доходят до прода, и вложиться в быстрое обнаружение и откат", summary: "Наблюдать в проде.", justification: "Обнаружение-и-откат — реальный слой защиты, но как *единственный* гейт для совместимости он делает клиента вашим тестом, а аварию — обратной связью. Вся суть в том, что переименование поля дёшево ловится до деплоя; выбирать ловить его после — значит выбирать самое дорогое место для обучения." },
  ]}
/>

<Quiz
  id="02-contract-testing-quiz-1"
  lessonSlug="01-overview"
  lang="ru"
  question="Почему надёжность end-to-end-набора деградирует по мере добавления сервисов в сценарий?"
  choices={[
    { label: "Сценарий проходит только когда каждый хоп здоров одновременно, так что его вероятность успеха — произведение аптаймов сервисов и падает мультипликативно при добавлении сервисов", correct: true },
    { label: "Каждый добавленный сервис делает код теста длиннее и подверженнее ошибкам при написании", misconception: "Длина теста ни при чём. Хрупкость идёт из требования времени выполнения, чтобы все сервисы были зелёными разом — из математики перемножения аптаймов, — а не из того, сколько кода теста вы написали." },
    { label: "Новые сервисы по своей природе багованнее старых", misconception: "Возраст сервиса не в этом. Даже если каждый сервис железобетонно зелёный на 98%, связка двенадцати всё равно опускает сценарий ниже 80%, потому что вероятности перемножаются." },
    { label: "У CI-сервера кончается память при большем числе сервисов", misconception: "Лимиты ресурсов — отдельная, решаемая забота. Структурная проблема в том, что здоровье общего окружения — это произведение всех его частей, поэтому оно деградирует при добавлении частей независимо от железа." },
  ]}
/>

<Quiz
  id="02-contract-testing-quiz-2"
  lessonSlug="01-overview"
  lang="ru"
  question="Почему unit-тесты не ловят переименование провайдером поля, которое читает консьюмер?"
  choices={[
    { label: "Unit-тесты мокают границу, поэтому проверяют убеждение автора о форме провайдера — мок продолжает возвращать старое поле даже после изменения реального провайдера", correct: true },
    { label: "Unit-тесты запускаются недостаточно часто, чтобы заметить изменение", misconception: "Частота не при чём — они могут идти на каждом коммите и всё равно проходить. Мок кодирует устаревшее допущение о провайдере, так что возвращает старую форму независимо от того, когда запущен." },
    { label: "Unit-тесты проверяют только приватные методы, а не ответы API", misconception: "Unit-тесты рутинно покрывают код, обрабатывающий ответы API. Брешь в том, что они обрабатывают *замоканный* ответ, который по замыслу отражает убеждение автора, а не реальный, возможно изменённый, вывод провайдера." },
    { label: "Переименование — текстовое изменение, которое git бы пометил", misconception: "git понятия не имеет, что другой сервис читает это поле; он видит обычную правку. Поломка — это межсервисное семантическое расхождение, которое не обнаружит никакой diff в репозитории провайдера." },
  ]}
/>

<DragOrder
  id="02-contract-testing-drag-1"
  lessonSlug="01-overview"
  lang="ru"
  prompt="Упорядочьте, как стратегия «только e2e» деградирует до аварии в 02:14:"
  items={[
    { id: "a", label: "Два сервиса интегрируются; ради их проверки поднимают общее e2e-окружение" },
    { id: "b", label: "Присоединяется больше сервисов; сценарию нужны они все здоровы одновременно" },
    { id: "c", label: "Набор замедляется за 40 минут и флакает ~1 из 5 по несвязанным причинам" },
    { id: "d", label: "Команда глушит окружение или перестаёт следить; оно красное днями" },
    { id: "e", label: "Провайдер переименовывает поле; единственный тест, что поймал бы это, заглушен; прод пейджит в 02:14" },
  ]}
  correctOrder={["a", "b", "c", "d", "e"]}
/>

</Explanation>

<FlowDiagram
  label="Куда проваливается переименование поля"
  caption="Unit-тесты мокают границу и остаются зелёными; e2e видит, но заглушено. Переименование проваливается в щель между обоими слоями."
  hue="var(--d-systems)"
  perRow={3}
  nodes={[
    { id: "unit", label: "Unit-тесты", sub: "мокают границу" },
    { id: "gap", label: "Щель", sub: "переименование сюда" },
    { id: "e2e", label: "E2e-тесты", sub: "флакают, заглушены" },
    { id: "rename", label: "Переименование поля", sub: "провайдер выкатил" },
    { id: "prod", label: "Авария в проде", sub: "пейджер в 02:14" },
    { id: "contract", label: "Контрактный слой", sub: "проверяет границу" },
  ]}
  edges={[
    { from: "unit", to: "gap", label: "слепы к проводу" },
    { from: "e2e", to: "gap", label: "проигнорированы" },
    { from: "rename", to: "gap", label: "проскальзывает" },
    { from: "gap", to: "prod", label: "достигает прода" },
    { from: "contract", to: "gap", label: "закрывает щель" },
  ]}
/>

<KeyTakeaway>End-to-end-тестирование интеграции рушится на масштабе: оно растёт как ~N², требует здоровья каждого хопа разом и флакает, пока его не заглушат. Unit-тесты быстры, но мокают границу, поэтому слепы к контракту по проводу. Переименованное поле проваливается в щель — недостающий слой должен проверять согласие двух сервисов без подъёма обоих.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="02-contract-testing-retrieval"
  lang="ru"
  questions={[
    { q: "Коллега говорит: «нашему интеграционному окружению просто нужно больше дисциплины, чтобы оставаться зелёным». Объясните, почему это структурная проблема, а не дисциплинарная.", a: "Общее end-to-end-окружение здорово, только когда каждый сервис в сценарии одновременно поднят, промигрирован и засеян, — так что его аптайм есть *произведение* индивидуальных аптаймов каждого сервиса. Если каждый из двенадцати сервисов зелёный 98% времени, весь сценарий зелёный лишь около 78% времени, и дальше становится хуже по мере добавления сервисов. Деградация мультипликативна и встроена в архитектуру; никакое «постараться сильнее» не меняет математику. Вдобавок здоровье окружения принадлежит всем, что на практике значит — никому, поэтому наполовину применённая миграция или провалившийся seed от другой команды ломает ваш прогон. Эта комбинация и есть причина, почему большие e2e-наборы замедляются за 40 минут, флакают примерно на 1 из 5 прогонов по причинам, не связанным с изменением, и в итоге глушатся, — а заглушенный тест не ловит ничего, при этом всё равно стоит времени прогона и даёт ложное спокойствие. Долговечный фикс — не дисциплина; это перестать требовать здоровья всех сервисов разом лишь ради того, чтобы узнать, согласны ли два из них по-прежнему." },
    { q: "Где именно пирамида тестов «ломается» на границе сервиса и почему это пропускает переименование поля в прод?", a: "Внутри монолита unit-тест прогоняет реальные внутрипроцессные вызовы, так что совет пирамиды «много быстрых unit-тестов» работает. Через сетевую границу появляется течь: unit-тест консьюмера мокает провайдера, и мок возвращает ту форму, которую автор консьюмера считал, что отдаёт провайдер. Это убеждение заморожено на момент написания мока. Когда провайдер переименовывает total_cents в amount_cents, unit-тесты консьюмера остаются зелёными, потому что проверяют устаревший мок, а не реального провайдера — то, что вероятнее всего сломается, контракт по проводу, есть ровно то, что unit-тесты намеренно заглушают. End-to-end-тесты *могут* видеть границу, но слишком медленны и флакающи, чтобы гейтить каждый деплой, поэтому их глушат. Переименование поэтому проваливается прямо в щель между слоем «быстрый-но-слепой» и слоем «видит-но-не-доверяют», и первый сигнал — пейджер в проде. Недостающий слой — тот, что проверяет именно границу — форму и семантику запросов и ответов между двумя сервисами — не поднимая оба вместе." },
  ]}
/>

<Recap lang="ru">
Инстинкт тестировать интеграцию, запуская все сервисы вместе, верен для двух сервисов и неверен для двадцати. End-to-end-наборы растут комбинаторно: при N взаимодействующих сервисах число пар для прогона масштабируется как N², а любой сценарий проходит, только когда каждый хоп здоров в один и тот же миг, так что надёжность общего окружения — произведение его частей и деградирует мультипликативно по мере добавления сервисов. Результат — знакомая троица: медленно (подъём за 40+ минут), флакающе (~1 из 5 падений не связаны с изменением) и потому заглушено, а заглушенный тест не ловит ничего, при этом всё равно стоит времени прогона. Классическая пирамида не спасает, потому что на сетевой границе unit-тесты мокают провайдера и проверяют устаревшее убеждение автора о его форме, оставаясь зелёными в день, когда провайдер переименовывает поле. Так вас сжимает между слоем «быстрый-но-слепой» и слоем «видит-но-не-доверяют», и межсервисное переименование проваливается в щель — в пейджер в 2 ночи. Нужен слой, который проверяет саму границу — форму и семантику, которыми обмениваются два сервиса, — не требуя поднимать оба вместе. Этот слой — contract testing, и его выстраивание — то, чем занимается остальной юнит.
</Recap>
