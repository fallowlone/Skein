---
concepts:
- pact-consumer-test
- matchers
- provider-verification
- provider-states
- breaking-change-detection
estMin: 14
lang: ru
level: senior
order: 9
prereqs: []
slug: quiz-code
sources:
- https://docs.pact.io/getting_started/how_pact_works
- https://docs.pact.io/getting_started/matching
- https://docs.pact.io/getting_started/provider_states
status: ready
summary: "Читай настоящие Pact consumer-тесты, настройку provider verification и diff поломки контракта, предсказывай поведение и выбирай фикс с наибольшим рычагом."
title: 'Контрактное тестирование: чтение кода и контрактов'
track: engineering-practice
unit: 02-contract-testing
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Контракты читают в коде и diff'ах, не в прозе. Прочитай consumer-тест, обвязку provider verification и настоящую поломку контракта, затем выбери фикс, который сениор сделал бы первым.
</Hook>

<Goal lang="ru">
Отработай цикл, который ты проходишь в каждом контрактном инциденте: прочитать тест, предсказать, что утвердит сгенерированный pact, и взяться за фикс, держащий гейт точным — без повторной связки двух команд.
</Goal>

## Сниппет 1 — consumer-тест

```js
// checkout-web consumer test (Pact JS)
const { like, integer, string } = MatchersV3;

provider
  .given("a price with id 42 exists")
  .uponReceiving("a request for price 42")
  .withRequest({ method: "GET", path: "/prices/42" })
  .willRespondWith({
    status: 200,
    body: like({
      amount_cents: integer(1299),
      currency: string("USD"),
    }),
  });

await provider.executeTest(async (mock) => {
  const res = await pricingClient(mock.url).getPrice(42);
  expect(res.amountCents).toBe(1299);   // reads amount_cents
});
```

<Quiz
  id="02-contract-testing-code-1"
  lessonSlug="02-contract-testing"
  lang="ru"
  question="Что pact, сгенерированный этим тестом, реально утверждает про amount_cents и каково следствие?"
  choices={[
    { label: "Он утверждает, что amount_cents — целое (любое значение), потому что integer() — type matcher, а 1299 лишь пример. Провайдер может вернуть любую целую цену и пройти верификацию; ломает только удаление или смена типа поля.", correct: true },
    { label: "Он пиннит amount_cents ровно к 1299, поэтому провайдер падает на любой другой цене.", misconception: "integer(1299) — type matcher; 1299 — пример, не запиненное значение. Пиннинг переспецифицировал бы и падал на безобидных изменениях цены — та хрупкость, ради избегания которой и существуют matcher'ы." },
    { label: "Он записывает amount_cents, только если assert expect() прошёл в рантайме; иначе поле опускается.", misconception: "Pact записывает взаимодействие, которое клиент потребителя реально выполнил против mock'а. expect() охраняет тест, но именно matcher в willRespondWith задаёт записанное типовое ограничение." },
    { label: "Поскольку присутствует given(), pact утверждает, что у price 42 значение 1299 в БД провайдера.", misconception: "given() записывает строку provider-state для настройки во время верификации — он ничего не утверждает про хранимые значения. Body matcher задаёт форму ответа; пример — не утверждение про данные." },
  ]}
/>

## Сниппет 2 — настройка provider verification

```js
// pricing provider verification (Pact JS)
await new Verifier({
  provider: "pricing",
  providerBaseUrl: "http://localhost:8080",
  pactBrokerUrl: process.env.PACT_BROKER_URL,
  publishVerificationResult: true,
  providerVersion: process.env.GIT_SHA,
  // stateHandlers: { ... }   // <-- intentionally not wired
}).verifyProvider();
```

<Quiz
  id="02-contract-testing-code-2"
  lessonSlug="02-contract-testing"
  lang="ru"
  question="Pact потребителя выше использует given('a price with id 42 exists'), но у этого верификатора нет stateHandlers. Что произойдёт и каков фикс?"
  choices={[
    { label: "Верификация падает: без обработчика данные не засеяны, GET /prices/42 возвращает 404, и в ответе нет минимальной ожидаемой формы. Фикс: зарегистрировать stateHandler для этого given, вставляющий price 42 перед проигрыванием взаимодействия.", correct: true },
    { label: "Верификация проходит — верификатор автоматически трактует примеры в pact как seed-данные.", misconception: "Верификатор никогда не пишет в хранилище провайдера из примеров pact; они описывают ожидаемый ответ. Без обработчика данные не засеяны, и повторённый запрос не может вернуть 200." },
    { label: "Верификация пропускает любое взаимодействие с provider state, пока обработчики не добавлены.", misconception: "Взаимодействия с состояниями не пропускаются — они проигрываются, и предусловие просто не выполнено, поэтому они падают. Пропуск тихо снёс бы покрытие; Pact прогоняет их и сообщает о сбое." },
    { label: "Взаимодействие проходит, потому что publishVerificationResult записывает его зелёным независимо от ответа.", misconception: "publishVerificationResult лишь управляет тем, отправляется ли результат pass/fail в брокер — он не делает падающее взаимодействие проходящим. Отсутствующий обработчик всё равно валит сравнение." },
  ]}
/>

## Сниппет 3 — diff поломки контракта

```diff
  // pricing provider response serializer
  {
    "id": price.id,
-   "amount_cents": price.amountCents,
+   "price_minor_units": price.amountCents,
    "currency": price.currency
  }
```

<Quiz
  id="02-contract-testing-code-3"
  lessonSlug="02-contract-testing"
  lang="ru"
  question="Три развёрнутых потребителя читают amount_cents. Это переименование едет в одном PR. Собственные unit-тесты провайдера зелёные. Что делает контрактный гейт и как переименование надо реально выкатывать?"
  choices={[
    { label: "Provider verification падает для всех трёх pact'ов потребителей (потребляемое поле исчезло), поэтому can-i-deploy блокирует. Выкатывай как expand-then-contract: верни оба поля, мигрируй каждого потребителя в своём темпе, затем удали amount_cents, когда матрица покажет, что его никто не читает.", correct: true },
    { label: "Ничего не падает, потому что unit-тесты провайдера зелёные и у поля по-прежнему есть значение под новым именем.", misconception: "Собственные тесты провайдера не знают, что три потребителя читают amount_cents — это исходный пробел e2e. Верификация против pact'ов потребителей падает, потому что старого имени поля больше нет в ответе." },
    { label: "Верификация падает, поэтому правильный ход — заставить трёх потребителей обновиться в lockstep с деплоем провайдера.", misconception: "Lockstep-координация flag-day — ровно та боль, ради устранения которой существуют контракты. expand-then-contract даёт каждой стороне двигаться независимо, держа обе формы живыми в окне миграции." },
    { label: "Гейт падает, поэтому пометь падающие pact'ы как pending, чтобы сборка провайдера позеленела, и выкати переименование.", misconception: "Pending — для новых ожиданий потребителя, которые провайдер ещё не построил, а не для маскировки настоящего breaking change полей, от которых зависят потребители. Пометка реальной поломки как pending воссоздаёт аварию." },
  ]}
/>

## Сниппет 4 — гейт can-i-deploy

```bash
# in the consumer's deploy pipeline, keyed to the commit being shipped
pact-broker can-i-deploy \
  --pacticipant checkout-web --version "$GIT_SHA" \
  --to-environment production
# exit 0 => deploy; non-zero => blocked
# ...then, at the very end of a successful rollout:
pact-broker record-deployment \
  --pacticipant checkout-web --version "$GIT_SHA" \
  --environment production
```

<Quiz
  id="02-contract-testing-code-4"
  lessonSlug="02-contract-testing"
  lang="ru"
  question="Почему record-deployment должен запускаться в самом конце успешного rollout и почему can-i-deploy привязан к git SHA, а не к semver?"
  choices={[
    { label: "Запись брокера должна отражать то, что физически обслуживает трафик, чтобы can-i-deploy следующей команды получил истину; запись до rollout, который потом упадёт, отравляет матрицу. SHA меняется всякий раз, когда мог измениться контракт, поэтому устаревший результат не переиспользуется для другого кода.", correct: true },
    { label: "Порядок для record-deployment не важен; SHA используется лишь потому, что он короче строки semver.", misconception: "Порядок важен: деплой, записанный до упавшего rollout, заставляет брокер верить, что в prod стоит несуществующая версия. А ценность SHA — уникальность на коммит, не длина." },
    { label: "record-deployment должен идти первым, чтобы can-i-deploy его прочитал; SHA сортируется хронологически, что нужно матрице.", misconception: "can-i-deploy читает то, что уже развёрнуто до этого деплоя, поэтому запись идёт после. И SHA вообще не сортируются по времени — их ценность в точном отслеживании контракта." },
    { label: "Оба вызова опциональны, как только верификация зелёная; матрица выводит состояние деплоя из результатов верификации.", misconception: "Матрица не может узнать, что где развёрнуто, из одной верификации — верификация это попарный факт. Именно record-deployment сообщает брокеру, какая версия провайдера реально в каждом окружении." },
  ]}
/>

<Recap lang="ru">
Каждый контрактный инцидент читается в коде: type matcher'ы consumer-теста задают, что pact реально утверждает (тип, не пример); отсутствующий stateHandler валит верификацию из-за предусловия, которое провайдер не засеял, а не из-за сломанного контракта; переименование потребляемого поля в одном PR валит верификацию для каждого потребителя и должно ехать как expand-then-contract; а гейт деплоя работает, только когда версии — git SHA и record-deployment запускается в конце успешного rollout, чтобы матрица отражала реальность. Прочитай тест и diff, предскажи поведение гейта, затем чини на том уровне, что держит гейт и точным, и развязанным.
</Recap>
