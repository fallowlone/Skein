---
concepts:
- can-i-deploy
- verification-matrix
- pacticipant-versioning
- git-sha-versions
- record-deployment
- record-release
- independent-deployability
deepensInto:
- 05-contract-evolution-and-limits
estMin: 17
lang: ru
lessonType: topic
level: senior
order: 4
prereqs:
- 03-provider-verification-broker
slug: 04-can-i-deploy-and-versioning
sources:
- https://docs.pact.io/pact_broker/can_i_deploy
- https://docs.pact.io/getting_started/versioning_in_the_pact_broker
- https://docs.pact.io/pact_broker/recording_deployments_and_releases
- https://docs.pact.io/pact_broker/advanced_topics/matrix_selectors
spiral:
- feedback-loops
- blast-radius
status: ready
summary: 'Проверенный pact доказывает согласие двух версий, а не безопасность консьюмера
  против прода. Версионируй pact-ы по git SHA, записывай, что где задеплоено, и гейти
  деплои на can-i-deploy: он читает matrix твоей версии против провайдера, реально
  стоящего в целевом env.'
title: 'can-i-deploy и версионирование контрактов: гейт безопасности деплоя'
track: engineering-practice
unit: 02-contract-testing
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
`checkout-web` Прии добавляет фичу, которой нужно новое поле `discount_cents`. Она обновляет тест консьюмера, pact перегенерируется, и CI публикует его в брокер. Провайдер `pricing` Сэма ещё не выкатил `discount_cents` — задача верификации против *latest*-сборки `pricing` зелёная для старых взаимодействий, а новое всё ещё помечено pending, поэтому ни у кого сборка не красная. Пайплайн Прии весь зелёный, так что он деплоит `checkout-web` в прод. Через сорок секунд частота ошибок ползёт вверх: инстанс `pricing`, реально работающий в проде, на три версии позади latest у `pricing`, и он не возвращает `discount_cents`. Pact был проверен — просто не против той версии, что *стоит там*. Контрактное тестирование сделало своё дело, а деплой всё равно сломал прод, потому что гейт задавал не тот вопрос.
</Hook>

<Crux>Проверенный pact доказывает согласие двух версий; can-i-deploy — что твоя версия согласуется с тем, что реально задеплоено в целевом env.</Crux>

<Explanation>

## Зелёный pact — это попарный факт, а не факт безопасности деплоя

Урок 03 оставил тебя с брокером, полным результатов верификации. Каждый результат — точное, узкое утверждение: *pact версии X консьюмера был проверен против версии Y провайдера, и он прошёл.* Это действительно полезно, но отвечает на вопрос, который на момент деплоя никто на самом деле не задаёт. Вопрос на момент деплоя такой: *если я выкачу версию X консьюмера в продакшен прямо сейчас, сработает ли она против версии провайдера, которая физически работает в продакшене?* Верификация, что у тебя есть, — для некой версии Y, обычно latest CI-сборки провайдера, которая может быть на несколько деплоев впереди того, что стоит в проде.

Это и есть зазор, в который проваливается Hook. Pact был проверен против новейшей сборки `pricing`, в которой уже было `discount_cents`. Но в проде работал более старый `pricing`, а отдельные результаты верификации брокера ни словом не говорили о том, какая версия провайдера была *где задеплоена*. Куча зелёных галочек говорит тебе, какие пары версий тестировались вместе; она не говорит, является ли конкретная пара, которую ты вот-вот создашь в продакшене, — новый консьюмер, старый провайдер — одной из зелёных. Тебе нужен инструмент, который скрещивает твою версию с реальными обитателями целевого окружения, и этот инструмент — `can-i-deploy`.

## Matrix: каждая протестированная пара и что где задеплоено

Брокер собирает всё это в **matrix** — таблицу каждой версии консьюмера × версии провайдера, которые когда-либо тестировались друг против друга, с результатом pass/fail каждого спаривания. В неё поступают две вещи. Во-первых, **результаты верификации**: когда pact версии X консьюмера проверяется версией Y провайдера, брокер записывает ячейку (X, Y) → pass/fail. Во-вторых, **записи о деплоях**: когда ты реально деплоишь версию в окружение, ты сообщаешь брокеру, так что он знает, что в проде сейчас стоит, скажем, `pricing` v56. Затем `can-i-deploy` делает один запрос: взять версию, которую я хочу задеплоить, найти версии каждого интегрированного приложения, что сейчас стоят в целевом окружении, и проверить, что каждая требуемая ячейка между ними зелёная.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <caption class="text-left text-xs text-bbg-muted mb-2">Matrix для консьюмера <code>checkout-web</code> × провайдера <code>pricing</code>. В проде сейчас работает <code>pricing</code> v56.</caption>
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">версия checkout-web</th>
        <th class="border border-border px-3 py-2 text-left">версия pricing</th>
        <th class="border border-border px-3 py-2 text-left">Проверено?</th>
        <th class="border border-border px-3 py-2 text-left">can-i-deploy в прод (pricing=v56)?</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">v22 (a1b2c3d)</td>
        <td class="border border-border px-3 py-2">v56 (задеплоено в проде)</td>
        <td class="border border-border px-3 py-2">pass</td>
        <td class="border border-border px-3 py-2">Да — пара зелёная, exit 0</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">v23 (e4f5a6b)</td>
        <td class="border border-border px-3 py-2">v56 (задеплоено в проде)</td>
        <td class="border border-border px-3 py-2">pass</td>
        <td class="border border-border px-3 py-2">Да — пара зелёная, exit 0</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">v24 (7c8d9e0) — добавляет discount_cents</td>
        <td class="border border-border px-3 py-2">v56 (задеплоено в проде)</td>
        <td class="border border-border px-3 py-2">нет результата</td>
        <td class="border border-border px-3 py-2">Нет — пара не проверена, exit 1, деплой заблокирован</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">v24 (7c8d9e0) — добавляет discount_cents</td>
        <td class="border border-border px-3 py-2">v59 (latest CI, не задеплоено)</td>
        <td class="border border-border px-3 py-2">pass</td>
        <td class="border border-border px-3 py-2">Неважно — v59 не в проде</td>
      </tr>
    </tbody>
  </table>
</div>

Последние две строки — это весь урок. `checkout-web` v24 *проверена* — против `pricing` v59, сборки, в которой уже есть `discount_cents`. Но v59 не задеплоена; в проде по-прежнему работает v56. `can-i-deploy --pacticipant checkout-web --version 7c8d9e0 --to-environment production` полностью игнорирует результат для v59, не находит зелёной ячейки для (v24, v56), завершается с ненулевым кодом и останавливает пайплайн. Тот самый деплой, что вызвал пейджер Прии в Hook, — это деплой, в котором этот гейт отказывает.

## Версии должны быть git SHA, а деплои должны записываться

Matrix работает, только если правильны две вещи учёта. Во-первых, **номер версии**. Сильная рекомендация Pact: версия pacticipant *есть* git commit SHA или содержит его (напр. `0.0.10+76a39e5`). Причина механическая: версия должна меняться ровно тогда, когда мог измениться pact. Если ты версионируешь по semver из `package.json`, две разные сборки с разными контрактами могут делить версию `2.3.0`, и брокер с радостью переиспользует устаревший результат верификации для не того кода. Git SHA меняется на каждом коммите, известен на момент деплоя и позволяет постить статус верификации обратно как статус коммита. Фичеветки автоматически получают отдельные версии от main. SHA — единственный идентификатор, гарантированно отслеживающий контракт.

Во-вторых, **отслеживание деплоев**. Брокер не может знать, что стоит в проде, если ты не скажешь ему в конце каждого деплоя через `pact-broker record-deployment --pacticipant checkout-web --version 7c8d9e0 --environment production` (pacticipant — термин Pact для любого приложения, участвующего в контракте). Есть две разновидности. `record-deployment` — для того, что деплоится в известный инстанс: сервисы, консьюмеры, API; он автоматически помечает *предыдущую* версию в этом окружении как более не задеплоенную, потому что одновременно работает лишь одна версия. `record-release` — для артефактов, которые ты публикуешь, но не заменяешь: мобильные приложения в сторе, библиотеки в реестре, где много выпущенных версий сосуществуют, так что он *не* снимает с деплоя предыдущую. (Старые конфигурации использовали для той же цели environment-**теги** вроде `prod`; теги всё ещё работают, а брокеры начиная с 2.81.0 автоконвертируют тег с именем окружения в деплой, но deployed/released-версии — это модель на будущее.)

<Inset kind="why" lang="ru">
Почему вызывать `record-deployment` *после* успешного деплоя, а не до? Потому что запись брокера призвана отражать физическую реальность — что реально обслуживает трафик, — чтобы `can-i-deploy` *следующей* команды получил правдивый ответ. Если ты запишешь деплой до того, как раскатка завершится, а раскатка затем провалится, брокер теперь считает, что в проде работает версия, которой там нет, и чей-то гейт даст зелёный свет деплою против провайдера, которого ещё не существует. Записывай это в самом конце, когда нет шанса на провал и старая версия полностью слита. Matrix честна ровно настолько, насколько честны записи о деплоях, которыми ты её питаешь; ложь о том, что задеплоено, побеждает гейт так же верно, как и его пропуск.
</Inset>

## Независимая деплоябельность — весь выигрыш

Отступи на шаг и заметь, что это даёт. Без гейта на «безопасно ли деплоить?» можно ответить, лишь деплоя всё вместе синхронно или поднимая весь мир в общем окружении — ровно та проблема N², что убил первый урок. С гейтом каждый сервис отвечает на вопрос *за себя*, локально, против записанной правды каждого окружения: я могу задеплоить свою новую версию тогда и только тогда, когда у каждого приложения, уже стоящего в целевом env, есть со мной зелёный pact. Это и есть **независимая деплоябельность** — способность выкатывать один сервис по своему графику без координации связки релизов, — и это вся причина, ради которой существует юнит. Контрактные тесты ловят поломку; `can-i-deploy` — то, что позволяет тебе безопасно *действовать* по этому знанию, по одному сервису за раз.

Режим отказа, когда ты это пропускаешь, — ровно Hook: консьюмер, чьи новые ожидания прод-провайдер никогда не удовлетворял, проходит сквозь CI (его pact проверен против *latest*-версии провайдера, а не *задеплоенной*) и ломается в рантайме — заново внося ровно тот продакшен-инцидент, который контрактное тестирование должно было предотвратить. Верификация была реальной; решение о деплое её проигнорировало. Задача гейта — заставить решение о деплое использовать matrix вместо «на ощупь».

<DragOrder
  id="04-can-i-deploy-and-versioning-drag-1"
  lessonSlug="04-can-i-deploy-and-versioning"
  lang="ru"
  prompt="Упорядочьте шаги, которые делают can-i-deploy надёжным гейтом деплоя:"
  items={[
    { id: "a", label: "CI консьюмера версионирует pact по git SHA и публикует его в брокер" },
    { id: "b", label: "CI провайдера верифицирует pact, и брокер записывает ячейку (консьюмер, провайдер) как pass/fail" },
    { id: "c", label: "Перед деплоем пайплайн запускает can-i-deploy --version SHA --to-environment production" },
    { id: "d", label: "Брокер проверяет matrix для этой версии консьюмера против версии провайдера, сейчас стоящей в проде" },
    { id: "e", label: "При exit 0 деплой продолжается; затем пайплайн вызывает record-deployment, чтобы гейт следующей команды видел правду" },
  ]}
  correctOrder={["a", "b", "c", "d", "e"]}
/>

<Quiz
  id="04-can-i-deploy-and-versioning-quiz-1"
  lessonSlug="04-can-i-deploy-and-versioning"
  lang="ru"
  question="Pact консьюмера проверен зелёным против latest CI-сборки провайдера. Почему этого недостаточно, чтобы безопасно задеплоить консьюмер?"
  choices={[
    { label: "Latest CI-сборка может не быть тем, что работает в проде; can-i-deploy должен проверить версию консьюмера против версии провайдера, реально задеплоенной в целевом окружении", correct: true },
    { label: "Результаты верификации истекают через 24 часа и должны перепрогоняться перед каждым деплоем", misconception: "Результаты не истекают по таймеру. Проблема в том, о какой версии провайдера результат — зелёный результат против latest-сборки ничего не говорит о более старой сборке, физически обслуживающей трафик прода." },
    { label: "Одна зелёная верификация никогда не надёжна; нужно как минимум три", misconception: "Одна верификация правильной пары исчерпывающа. Дело не в количестве, а в том, что проверенная пара (новый консьюмер, latest-провайдер) — не та пара, что ты создал бы на момент деплоя (новый консьюмер, задеплоенный провайдер)." },
    { label: "Консьюмер должен быть проверен ещё и против самого себя перед деплоем", misconception: "Само-верификации здесь нет как концепции. Зазор чисто в том, что «проверено против latest» и «проверено против задеплоенного» — разные факты, и лишь второй управляет безопасностью деплоя." },
  ]}
/>

<Quiz
  id="04-can-i-deploy-and-versioning-quiz-2"
  lessonSlug="04-can-i-deploy-and-versioning"
  lang="ru"
  question="Почему Pact рекомендует, чтобы версия pacticipant была (или содержала) git commit SHA, а не semver вроде 2.3.0?"
  choices={[
    { label: "SHA меняется всякий раз, когда мог измениться контракт, и известен на момент деплоя, так что брокер никогда не переиспользует устаревший результат верификации для другого кода под той же версией", correct: true },
    { label: "Git SHA сортируются хронологически, что нужно matrix для упорядочивания деплоев", misconception: "SHA вообще не сортируются по времени — они фактически случайны. Ценность в уникальности на коммит, так что версия точно отслеживает контракт, а не в каком-либо свойстве упорядочивания." },
    { label: "Semver не поддерживается Pact Broker", misconception: "Semver полностью поддерживается; рекомендация — включать SHA как метаданные (напр. 0.0.10+76a39e5). Суть в том, что голый 2.3.0 может пометить две сборки с разными контрактами, позволив переиспользовать устаревший результат." },
    { label: "SHA короче, так что экономит хранилище в брокере", misconception: "Хранилище не при чём. Строка semver сравнима по размеру; вопрос в корректности — версия должна меняться на каждой сборке, затрагивающей контракт, что гарантирует лишь идентификатор коммита." },
  ]}
/>

<TradeoffMatrix
  id="04-can-i-deploy-and-versioning-tradeoff-1"
  lessonSlug="04-can-i-deploy-and-versioning"
  lang="ru"
  prompt="Ваш провайдер верифицирует pact-ы в CI, но никто не запускает can-i-deploy и никто не записывает деплои. Консьюмер добавляет поле, которое прод-провайдер ещё не возвращает. Как закрыть зазор?"
  options={[
    { name: "Записывать деплои в конце каждой раскатки и гейтить каждый деплой на can-i-deploy --to-environment", summary: "Обе записи + гейт.", correct: true, justification: "can-i-deploy может дать правдивый ответ, только если брокер знает, что где задеплоено, так что нужен record-deployment, питающий его; и гейт должен реально запускаться перед деплоем, привязанный к версии (git SHA), которую выкатывают. Вместе они отказывают паре новый-консьюмер-vs-старый-провайдер, и ты сохраняешь независимую деплоябельность. Любой кусок по отдельности недостаточен." },
    { name: "Добавить can-i-deploy в пайплайн, но пропустить запись деплоев", summary: "Гейт без данных о деплоях.", justification: "can-i-deploy читает matrix, но matrix не может знать, какая версия провайдера в проде, если деплои не записаны. Без данных о деплоях инструмент откатывается к 'latest' или возвращает unknown — ровно отказ из Hook, где «проверено против latest» скрыло, что в проде работал более старый провайдер. Гейту нужна базовая правда о каждом окружении." },
    { name: "Положиться на фичу pending-pacts провайдера, чтобы новый контракт никогда не краснил сборку провайдера", summary: "Только pending pact-ы.", justification: "Pending pact-ы — правильный способ дать новому ожиданию консьюмера приземлиться, не ломая сборку провайдера, но это не гейт деплоя — он намеренно держит сборку зелёной, пока взаимодействие не проверено. Деплой консьюмера на зелёном пайплайне, пока его ожидание всё ещё pending против задеплоенного провайдера, — ровно то, как случается инцидент. Тебе всё ещё нужен can-i-deploy, чтобы заблокировать деплой." },
    { name: "Вернуться к общему staging-окружению и прогонять полный e2e-набор перед каждым релизом", summary: "Заново вернуть большой e2e.", justification: "Это отказ от независимой деплоябельности и прямой откат к проблеме N², с которой открылся юнит: каждый релиз ждёт, пока общее окружение станет зелёным всё сразу. can-i-deploy отвечает на тот же вопрос безопасности на каждый сервис, за секунды, из записанных фактов — без поднятия всего мира. Тянуться за e2e здесь — решать решённую проблему дорогим способом." },
  ]}
/>

</Explanation>

<FlowDiagram
  label="can-i-deploy: гейт версий перед каждым деплоем в прод"
  caption="Pact, проверенный против latest-провайдера, недостаточен — can-i-deploy скрещивает SHA консьюмера с версией провайдера, реально стоящей в проде."
  hue="var(--d-systems)"
  perRow={3}
  nodes={[
    { id: "sha", label: "SHA консьюмера", sub: "версия git-коммита" },
    { id: "verify", label: "верификация", sub: "pact × провайдер" },
    { id: "matrix", label: "matrix брокера", sub: "все тестированные пары" },
    { id: "record", label: "record-deployment", sub: "что стоит в проде" },
    { id: "gate", label: "can-i-deploy", sub: "SHA vs задеплоенная версия" },
    { id: "deploy", label: "деплой продолжается", sub: "только exit 0" },
  ]}
  edges={[
    { from: "sha", to: "verify", label: "публикация pact" },
    { from: "verify", to: "matrix", label: "сохранить результат" },
    { from: "record", to: "matrix", label: "состояние окружения" },
    { from: "matrix", to: "gate", label: "запрос" },
    { from: "gate", to: "deploy", label: "пара зелёная" },
  ]}
/>

<KeyTakeaway>Проверенный pact — попарный факт о двух конкретных версиях; это не факт безопасности деплоя. can-i-deploy превращает matrix брокера в гейт деплоя: он скрещивает твою версию (git SHA) с версией провайдера, *реально задеплоенной* в целевом окружении, и завершается с ненулевым кодом, если какая-либо требуемая пара не проверена. Это требует версионирования по SHA и записи деплоев (record-deployment для сервисов, record-release для стор-артефактов), чтобы брокер знал, что где. Выигрыш — независимая деплоябельность; пропуск гейта выкатывает консьюмер, чьи новые ожидания прод-провайдер никогда не верифицировал, воссоздавая ровно тот рантайм-инцидент, который контрактное тестирование должно было предотвратить.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="04-can-i-deploy-and-versioning-retrieval"
  lang="ru"
  questions={[
    { q: "Пройдитесь точно по тому, что проверяет can-i-deploy, и почему зелёная верификация против latest-сборки провайдера недостаточна, чтобы задеплоить консьюмер.", a: "Результат верификации — узкий попарный факт: pact версии X консьюмера был проверен против версии Y провайдера и прошёл. Брокер хранит каждую такую пару в matrix. Но этот результат обычно для Y = latest CI-сборки провайдера, которая может быть на несколько деплоев впереди того, что физически работает в продакшене. Вопрос на момент деплоя другой: если я выкачу консьюмер X в прод сейчас, сработает ли он против версии провайдера, реально обслуживающей там трафик? can-i-deploy отвечает на это, беря версию, которую ты хочешь задеплоить, находя версии каждого интегрированного приложения, сейчас стоящего в целевом окружении (которые брокер знает лишь потому, что деплои были записаны), и проверяя, что каждая требуемая ячейка между ними зелёная. Он завершается с 0, если все требуемые пары проверены, и с 1 иначе, так что пайплайн блокирует деплой. Зелёный результат против 'latest' недостаточен, потому что latest — не то, что задеплоено: в худшем случае новый консьюмер проверен против нового провайдера, у которого есть нужное ему поле, тогда как в проде работает более старый провайдер, у которого его нет — у пары, что ты реально создал бы в проде, нет зелёной ячейки, и лишь can-i-deploy, скрещивающий твою версию с задеплоенной версией, ловит это." },
    { q: "Почему версионировать pact-ы по git SHA, и в чём разница между record-deployment и record-release? Почему вызывать их в конце раскатки?", a: "Версия pacticipant должна меняться ровно тогда, когда мог измениться контракт, и должна быть узнаваема на момент деплоя. Git commit SHA (или semver, встраивающий его, вроде 0.0.10+76a39e5) удовлетворяет обоим: каждый коммит получает отдельную версию, фичеветки автоматически отличаются от main, и ты можешь рапортовать статус верификации обратно как статус коммита. Голый semver вроде 2.3.0 может пометить две сборки с разными контрактами, так что брокер переиспользовал бы устаревший результат верификации для не того кода — тихая дыра в корректности. record-deployment — для того, что деплоится в известный инстанс (сервисы, консьюмеры, API): он помечает ранее задеплоенную версию в этом окружении как более не задеплоенную, потому что одновременно работает лишь одна версия. record-release — для артефактов, которые ты публикуешь, но не заменяешь (мобильные приложения в сторе, библиотеки в реестре), где много версий сосуществуют, так что он не снимает с деплоя предыдущую. Оба должны вызываться в самом конце раскатки, когда нет шанса на провал и старая версия слита, потому что запись брокера должна отражать физическую реальность. Запишешь слишком рано, раскатка провалится, и брокер считает, что в проде работает версия, которой там нет — так что can-i-deploy следующей команды даёт зелёный свет деплою против провайдера, которого не существует. Matrix честна ровно настолько, насколько честны записи о деплоях, которыми ты её питаешь." },
  ]}
/>

<Recap lang="ru">
После верификации провайдером у тебя есть брокер, полный зелёных результатов, но каждый из них — попарный факт: версия X консьюмера согласуется с версией Y провайдера — и это не вопрос, который задаёт момент деплоя. Момент деплоя спрашивает, безопасна ли твоя версия против версии провайдера, *физически работающей* в целевом окружении, которая может быть на несколько деплоев позади latest CI-сборки, против которой был проверен pact. can-i-deploy закрывает этот зазор: он читает matrix брокера (каждую протестированную пару консьюмер×провайдер плюс что где задеплоено), скрещивает версию, которую ты выкатываешь, с версиями, сейчас стоящими в целевом env, и завершается с ненулевым кодом, если какая-либо требуемая пара не проверена, блокируя деплой. Чтобы гейт был надёжным, должны быть правильны две записи: версионируй pact-ы по git SHA, чтобы версия менялась всякий раз, когда мог измениться контракт, и устаревший результат никогда не переиспользовался, и записывай, что задеплоено — record-deployment для сервисов (который снимает с деплоя предыдущую версию) и record-release для стор-артефактов (который нет) — вызываемые в конце каждой раскатки, чтобы брокер отражал реальность. Выигрыш — независимая деплоябельность: каждый сервис отвечает на свой вопрос безопасности деплоя за секунды из записанных фактов, без общего окружения и без синхронного релиза. Пропусти гейт — и ты выкатишь консьюмер, чьи новые ожидания задеплоенный провайдер никогда не верифицировал — зелёный пайплайн, сломанный прод — воссоздавая ровно тот инцидент, который контрактное тестирование было построено предотвратить. Теперь, увидев пайплайн, где есть верификация провайдера, но нет `can-i-deploy`, ты сразу поймёшь: pact проверен, а решение о деплое — нет. И будешь знать, какие две команды закрывают этот зазор.
</Recap>
