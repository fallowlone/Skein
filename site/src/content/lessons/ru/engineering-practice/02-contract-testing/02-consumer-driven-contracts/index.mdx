---
concepts:
- consumer-driven-contracts
- pact-file
- pact-mock-server
- matchers
- provider-states
- pact-broker
deepensInto:
- 03-provider-verification-broker
estMin: 16
lang: ru
lessonType: topic
level: junior
order: 2
prereqs:
- 01-overview
slug: 02-consumer-driven-contracts
sources:
- https://docs.pact.io/
- https://docs.pact.io/getting_started/how_pact_works
- https://martinfowler.com/articles/consumerDrivenContracts.html
- https://docs.pact.io/getting_started/matching
spiral:
- feedback-loops
- source-of-truth
status: ready
summary: Consumer-driven контракт — исполняемая спецификация из проходящего теста
  консьюмера, а не ручной документ. Консьюмер объявляет, что ему нужно, против мока;
  это даёт pact-файл реальных взаимодействий, публикуемый в брокер. Провайдеру говорят,
  что соблюдать, а не дают гадать.
title: 'Consumer-driven контракты: истину объявляет консьюмер'
track: engineering-practice
unit: 02-contract-testing
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import SequenceDiagram from "~/components/diagram/SequenceDiagram.astro";

<Hook>
Прия владеет фронтендом `checkout-web`; Сэм владеет API `pricing`. Сэм держит красивый OpenAPI-документ, но в этом квартале он дважды оказывался неверным — однажды описал поле, которое так и не выкатили, однажды упустил поле `tax_cents`, от которого уже зависели три команды. Документ ничем не *исполняется*, поэтому ничто не заставляет его совпадать с кодом. Команда Прии наконец перестаёт читать документ Сэма и вместо этого пишет крошечный тест: «когда я делаю `GET /prices/42`, я читаю `amount_cents` (число) и `currency` (строку)». Этот тест, запущенный однажды, выдаёт JSON-файл, описывающий ровно это взаимодействие. Теперь есть артефакт, говорящий, что на самом деле нужно `checkout-web`, — и его можно переиграть против реального сервиса Сэма, чтобы доказать, что это всё ещё держится.
</Hook>

<Crux>Consumer-driven контракт порождён проходящим тестом консьюмера и прогоняется против провайдера — исполняемое соглашение, а не документ.</Crux>

<Explanation>

## Кто пишет истину и почему это консьюмер

В прошлом уроке недостающий слой должен был проверять границу без подъёма обоих сервисов. Первое проектное решение: *кто объявляет, какой должна быть граница?* Есть два ответа. **Provider-driven** (spec-first): провайдер публикует OpenAPI-документ, и от консьюмеров ждут соответствия. **Consumer-driven**: каждый консьюмер объявляет, в исполняемой форме, ровно те запросы, что он делает, и те части ответа, что он читает, а провайдер обязан соблюдать объединение всего этого.

Consumer-driven выигрывает по конкретной причине: он фиксирует *фактическое использование*, а не объявленную поверхность. API `pricing` Сэма может возвращать сорок полей; `checkout-web` читает четыре. Consumer-driven контракт записывает эти четыре. Это делает контракт точным утверждением «что на самом деле кого-то сломает» — Сэм волен свободно менять остальные тридцать шесть полей, и только изменение одного из четырёх трогает гейт. Provider-driven спецификация, напротив, помечает каждое изменение одинаково, включая те, до которых ни одному консьюмеру нет дела, что порождает шум и подрывает доверие. Консьюмер — единственная сторона, знающая, от чего он действительно зависит; consumer-driven контракты делают это знание исполняемым.

## Pact-файл — это запись, а не список желаний

Артефакт в центре всего этого — **pact-файл**, в Pact (де-факто открытом стандарте) это JSON-документ. Ключевое свойство — *как он производится*: он не пишется вручную, он **порождается как побочный эффект проходящего теста консьюмера.** Механизм:

1. Тест консьюмера прогоняется против **Pact mock server** — локального HTTP-сервера, который поднимает Pact. Тест его конфигурирует: «ожидай `GET /prices/42`; ответь `200` с этим телом». Затем *реальный* клиентский код консьюмера вызывает этот мок.
2. Если код консьюмера действительно делает заявленный запрос и парсит заявленный ответ, тест проходит — и Pact записывает прогнанное взаимодействие в pact-файл. Если код консьюмера разошёлся (перестал слать `Accept: application/json` или вообще не читает поле), ожидание мока падает и **pact не порождается**, так что нельзя опубликовать контракт, который ты не соблюдаешь.

Это и есть инверсия, делающая контракты надёжными там, где документы нет. Документ — это обещание, написанное отдельно от кода; ничто его не исполняет, поэтому он тихо гниёт. Pact-файл — это *запись кода, который выполнился и прошёл*. Он не может описать взаимодействие, которого консьюмер не совершает, потому что выдаётся лишь когда собственный тест консьюмера его прогоняет.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Аспект</th>
        <th class="border border-border px-3 py-2 text-left">OpenAPI / wiki-документ вручную</th>
        <th class="border border-border px-3 py-2 text-left">Consumer-driven pact-файл</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">Кем написан</td>
        <td class="border border-border px-3 py-2">Провайдером, заявляющим всю поверхность</td>
        <td class="border border-border px-3 py-2">Консьюмером, заявляющим фактическое использование</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Как производится</td>
        <td class="border border-border px-3 py-2">Пишется отдельно от кода</td>
        <td class="border border-border px-3 py-2">Выдаётся проходящим тестом консьюмера</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Риск дрейфа</td>
        <td class="border border-border px-3 py-2">Тихо гниёт; ничто его не исполняет</td>
        <td class="border border-border px-3 py-2">Не может описать то, чего код не делает</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Сигнал об изменении</td>
        <td class="border border-border px-3 py-2">Помечает каждое поле одинаково (шумно)</td>
        <td class="border border-border px-3 py-2">Срабатывает лишь на потребляемых полях</td>
      </tr>
    </tbody>
  </table>
</div>

## Сопоставляй форму, а не точные значения

Наивная запись была бы переуточнённой: если pact фиксирует `amount_cents` ровно на `1299`, то любой ответ с другой ценой провалит верификацию — хотя консьюмеру нет дела до *значения*, лишь до того, что это число. Pact решает это **матчерами**. Вместо «тело равно этому точному JSON» консьюмер объявляет «в теле есть поле `amount_cents`, которое *целое*, поле `currency`, которое *строка по шаблону `[A-Z]{3}`*, и массив `items` с *хотя бы одним* элементом такой формы». Верификация тогда проверяет **тип и структуру**, а не буквальные примеры значений.

Это важно, потому что проводит границу контракта в правильном месте: он утверждает то, на что консьюмер действительно опирается (число присутствует и парсится), и молчит о том, на что нет (какое именно число). Переуточнение точными значениями возвращает хрупкость — контракт падает на безобидных изменениях данных. Недоуточнение (например, проверка лишь наличия поля, а не типа) пропускает реальную поломку. Хорошие контракты используют матчеры (matchers — декларативные правила проверки типа и структуры вместо точного значения) для частей, которые консьюмер парсит, и оставляют всё остальное неограниченным.

<Inset kind="why" lang="ru">
**Provider states** (состояния провайдера) — вторая половина того, почему pact переигрываем. Взаимодействие консьюмера часто предполагает существование данных: «GET /prices/42 возвращает 200» держится, только если цена 42 существует. Pact записывает строку `providerState` вроде `"a price with id 42 exists"`. Во время верификации (следующий урок) провайдер настраивает это предусловие перед переигрыванием запроса, так что тест детерминирован и не зависит от того, что случайно лежит в общей базе. Контракт несёт собственный контракт настройки; именно это позволяет ему прогоняться против реального провайдера в изоляции.
</Inset>

<DragOrder
  id="03-cdc-drag-1"
  lessonSlug="02-consumer-driven-contracts"
  lang="ru"
  prompt="Упорядочьте, как возникает consumer-driven pact-файл:"
  items={[
    { id: "a", label: "Тест консьюмера конфигурирует Pact mock server: «ожидай GET /prices/42, ответь 200 с этой формой»" },
    { id: "b", label: "Реальный клиентский код консьюмера вызывает мок и парсит ответ" },
    { id: "c", label: "Тест ассертит нужные ему поля, используя матчеры типов, а не точные значения" },
    { id: "d", label: "Тест проходит, поэтому Pact записывает прогнанное взаимодействие (с provider states) в pact-файл" },
    { id: "e", label: "Pact-файл публикуется в брокер, помеченный версией и веткой консьюмера" },
  ]}
  correctOrder={["a", "b", "c", "d", "e"]}
/>

<Quiz
  id="03-cdc-quiz-1"
  lessonSlug="02-consumer-driven-contracts"
  lang="ru"
  question="Почему consumer-driven pact-файл надёжнее, чем OpenAPI-документ, поддерживаемый вручную?"
  choices={[
    { label: "Он порождается как побочный эффект проходящего теста консьюмера, поэтому может описывать лишь взаимодействия, которые код консьюмера действительно совершает — он не может тихо разойтись с реальностью", correct: true },
    { label: "JSON — более точный формат, чем YAML у OpenAPI", misconception: "Формат не при чём. Pact мог бы быть любой сериализацией; надёжным его делает то, что он выдаётся выполненным, проходящим кодом, а не пишется вручную и никогда не запускается." },
    { label: "Брокер валидирует документ против строгой схемы при загрузке", misconception: "Валидация схемы при загрузке не заставляет документ совпадать с кодом. Доверие идёт из порождения-через-выполнение: pact отражает то, что тест консьюмера реально сделал, чего рукописный документ никогда не гарантирует." },
    { label: "Консьюмеры пишут аккуратнее провайдеров", misconception: "Дело не в старательности. Даже аккуратный документ провайдера гниёт, потому что ничто его не исполняет; pact консьюмера остаётся честным, потому что производится выполняющимся кодом, а не написанием." },
  ]}
/>

<Quiz
  id="03-cdc-quiz-2"
  lessonSlug="02-consumer-driven-contracts"
  lang="ru"
  question="Консьюмер читает лишь то, что amount_cents — число. Что должен утверждать о поле его pact?"
  choices={[
    { label: "Использовать матчер типа: утверждать, что amount_cents — целое, и оставить конкретное значение неограниченным", correct: true },
    { label: "Зафиксировать amount_cents на точном примере значения (напр. 1299), чтобы верификация была строгой", misconception: "Фиксация точного значения переуточняет — контракт тогда падает всякий раз, когда провайдер законно возвращает другую цену, хотя консьюмер никогда не полагался на то, какое это число. Это возвращает хрупкость." },
    { label: "Утверждать лишь, что поле существует, не проверяя его тип", misconception: "Только-наличие недоуточняет: провайдер мог бы сменить amount_cents на строку, и контракт всё равно прошёл бы, пропустив реальную поломку. Консьюмер парсит его как число, так что тип — ровно то, что надо утверждать." },
    { label: "Опустить его из pact, раз значение меняется между вызовами", misconception: "Опустить поле, которое консьюмер реально читает, полностью убирает его из контракта, так что провайдер может свободно его удалить — ровно та поломка, ради предотвращения которой существуют контракты. Запиши его, но матчи по типу." },
  ]}
/>

<TradeoffMatrix
  id="03-cdc-tradeoff-1"
  lessonSlug="02-consumer-driven-contracts"
  lang="ru"
  prompt="Ваш провайдер возвращает 40 полей; ваш консьюмер читает 4. Как ограничить контракт?"
  options={[
    { name: "Записать лишь 4 потребляемых поля, матченных по типу, плюс provider states, которые они предполагают", summary: "Ограничить фактическим использованием.", correct: true, justification: "Ценность consumer-driven контракта в том, что он фиксирует фактическую зависимость: запись лишь потребляемых полей означает, что провайдер волен свободно эволюционировать остальные 36, и гейт срабатывает только на изменении, которое реально сломало бы этого консьюмера. Матчеры типов уберегают от падения на безобидных изменениях значений, а provider states делают переигрывание детерминированным." },
    { name: "Записать все 40 полей с точными значениями, чтобы перестраховаться", summary: "Переуточнить всё.", justification: "Запись всех 40 превращает контракт в хрупкий снимок: он падает на любом изменении полей, которые консьюмер не читает, и на любом изменении значения, порождая шум, из-за которого контракт заглушают — ровно та эрозия доверия, что убила OpenAPI-документ команды. Контракт должен утверждать зависимость, а не переписывать весь ответ." },
    { name: "Записать 4 поля, но утверждать лишь их наличие, игнорируя типы", summary: "Только наличие.", justification: "Только-наличие позволяет провайдеру сменить тип поля — число на строку, объект на массив — не задев гейт, так что реальная поломка проскальзывает. Консьюмер парсит эти поля с ожидаемым типом; контракт должен утверждать этот тип, чтобы быть настоящей защитой." },
    { name: "Пропустить контракт консьюмера и опереться на OpenAPI провайдера как контракт", summary: "Provider-driven вместо этого.", justification: "Спецификация провайдера помечает каждое изменение одинаково и может разойтись с кодом (он описал поле-призрак и упустил tax_cents). Она также не знает, от какого из 40 полей кто зависит. Это шумная, склонная к гниению модель, которую consumer-driven подход и призван заменить." },
  ]}
/>

</Explanation>

<SequenceDiagram
  label="Тест консьюмера порождает pact-файл и публикует его в broker"
  caption="Pact-файл выдаётся лишь когда реальный клиентский код консьюмера делает заявленный запрос и парсит заявленные поля — он не может разойтись с кодом."
  hue="var(--d-systems)"
  actors={["тест консьюмера", "Pact mock", "pact-файл", "broker"]}
  messages={[
    { from: "тест консьюмера", to: "Pact mock", label: "конфигурация: GET /prices/42 → 200 body" },
    { from: "тест консьюмера", to: "Pact mock", label: "реальный клиентский код вызывает mock" },
    { from: "Pact mock", to: "тест консьюмера", label: "отвечает; проверяет совпадение запроса" },
    { from: "Pact mock", to: "pact-файл", label: "тест прошёл → записать взаимодействие + provider state" },
    { from: "pact-файл", to: "broker", label: "публикация с тегом версии + ветки" },
  ]}
/>

<KeyTakeaway>Consumer-driven контракт порождается проходящим тестом консьюмера против мока, поэтому записывает лишь взаимодействия и поля, которые консьюмер действительно использует — матченные по типу, несущие provider states для переигрывания. В отличие от рукописного документа, он не может разойтись с кодом и срабатывает лишь на изменениях, которые реально сломали бы настоящего консьюмера.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="03-cdc-retrieval"
  lang="ru"
  questions={[
    { q: "Объясните полный механизм, которым производится pact-файл, и почему это делает его надёжнее OpenAPI-документа.", a: "Консьюмер пишет тест, который поднимает Pact mock server и конфигурирует ожидаемое взаимодействие: «ожидай GET /prices/42, ответь 200 с такой формой тела». Затем реальный клиентский код консьюмера вызывает мок и парсит ответ, ассертя конкретные нужные ему поля через матчеры типов, а не точные значения. Только если код консьюмера действительно делает заявленный запрос и читает заявленные поля, тест проходит — и при прохождении Pact записывает это прогнанное взаимодействие, включая любые предполагаемые provider states, в JSON pact-файл. Pact, таким образом, — это запись кода, который выполнился и прошёл, а не написанное отдельно обещание. В этом разница доверия: OpenAPI-документ написан отдельно от кода, и ничто его не исполняет, поэтому он тихо гниёт — он может описывать поле, которое так и не выкатили, или упускать то, от которого зависят три команды. Pact не может описать взаимодействие, которого консьюмер не совершает, потому что выдаётся лишь когда собственный тест консьюмера его прогоняет. Контракт фиксирует фактическое использование, а не объявленную поверхность, так что он и честен, и точно ограничен." },
    { q: "Почему матчить по типу, а не по точным значениям, и какую роль играют provider states?", a: "Сопоставление по точным значениям переуточняет контракт: если pact фиксирует amount_cents на 1299, верификация падает всякий раз, когда провайдер возвращает любую другую цену, хотя консьюмер опирается лишь на то, что это парсимое число, а не на то, какое именно число. Матчеры позволяют консьюмеру утверждать тип и структуру — amount_cents целое, currency трёхбуквенная строка, items имеет хотя бы один элемент заданной формы, — так что контракт защищает ровно то, от чего зависит консьюмер, и молчит о безобидных изменениях данных. Недоуточнение (ассерт лишь наличия, а не типа) — противоположная ошибка: провайдер мог бы сменить число на строку и проскользнуть мимо гейта. Provider states — половина переигрываемости, отвечающая за предусловия: взаимодействие вроде «GET /prices/42 возвращает 200» держится, только если цена 42 существует, поэтому pact записывает строку providerState («a price with id 42 exists»), которую провайдер настраивает перед переигрыванием запроса при верификации. Это позволяет контракту прогоняться детерминированно против реального провайдера в изоляции, не завися от того, какие данные случайно лежат в общем окружении." },
  ]}
/>

<Recap lang="ru">
Недостающему слою границы нужен кто-то, кто объявит, какова эта граница, и консьюмер — правильный автор, потому что лишь он знает, от чего действительно зависит. Consumer-driven контракт записывает фактическое использование — те несколько полей, что консьюмер читает из скольких бы то ни было, что возвращает провайдер, — так что провайдер волен свободно эволюционировать всё остальное, и гейт срабатывает лишь на изменениях, которые реально сломали бы настоящего консьюмера; provider-driven спецификация, напротив, помечает каждое изменение одинаково и может разойтись с кодом. Артефакт — pact-файл, и его определяющее свойство в том, что он порождается как побочный эффект проходящего теста консьюмера против Pact mock server, а не пишется вручную: он может описывать лишь взаимодействия, которые код консьюмера совершает, поэтому не гниёт, как документ. Хорошие контракты матчат по типу и структуре, а не по точным значениям — защищая то, что консьюмер парсит, и молча о том, какое значение он получает, — и несут provider states, позволяющие провайдеру настроить предусловия, чтобы pact переигрывался детерминированно в изоляции. Затем pact публикуется в брокер, помеченный версией и веткой, готовый к верификации провайдером — что и есть следующий урок. Теперь, встретив команду, которая считает OpenAPI-документ провайдера источником истины, ты будешь знать, в чём проблема доверия: документ никто не исполняет, а настоящий контракт живёт в проходящем тесте консьюмера.
</Recap>
