---
concepts:
- consumer-driven-contracts
- provider-verification
- can-i-deploy
- expand-then-contract
- pending-pacts
- contract-testing-limits
estMin: 14
lang: ru
level: senior
order: 7
prereqs: []
slug: quiz-choice
sources:
- https://docs.pact.io/
- https://docs.pact.io/pact_broker/can_i_deploy
- https://martinfowler.com/articles/microservice-testing/
status: ready
summary: "Тест с выбором на синтез по всему юниту — почему рушится e2e, consumer-driven-запись, provider verification, гейт деплоя и жёсткие границы."
title: 'Контрактное тестирование: тест с выбором ответа'
track: engineering-practice
unit: 02-contract-testing
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Шесть вопросов поперёк всего юнита. Каждый отражает решение, которое ты реально принимаешь на платформе сервисов, деплоящихся независимо — не определение для заучивания, а компромисс под настоящей связанностью.
</Hook>

<Goal lang="ru">
Убедись, что связываешь крах e2e на масштабе, consumer-driven-запись, provider verification, гейт can-i-deploy и границы — тот синтез, к которому вели отдельные уроки.
</Goal>

<Quiz
  id="02-contract-testing-choice-1"
  lessonSlug="02-contract-testing"
  lang="ru"
  question="Почему надёжность общего e2e-окружения деградирует по мере добавления сервисов и что из этого следует про решение?"
  choices={[
    { label: "Сценарий проходит, только когда каждый хоп здоров одновременно, поэтому успех — произведение аптаймов сервисов и падает мультипликативно. Решение — тест на каждое ребро, проверяющий согласие двух сервисов без поднятия обоих.", correct: true },
    { label: "Новые сервисы багованнее, поэтому их добавление поднимает частоту отказов; решение — стабилизировать каждый сервис перед интеграцией.", misconception: "Возраст сервиса не причина. Даже при 98% зелёных у каждого склейка двенадцати роняет сценарий ниже 80%, потому что вероятности перемножаются — проблема в структуре, не в числе багов." },
    { label: "У CI-раннеров кончается память на поднятии сервисов; решение — раннеры побольше и параллелизм.", misconception: "Ресурсы — отдельная, чинимая проблема. Параллелизм не меняет форму N² — всё равно нужно одно общее окружение, зелёное целиком и сразу. В эту ловушку попадают прямо перед тем, как начать искать лучшее." },
    { label: "Код тестов растёт длиннее и ошибочнее на каждый сервис; решение — гигиена тестов.", misconception: "Длина тестов не причина. Хрупкость — в рантайм-требовании, чтобы все сервисы были здоровы в один момент (перемножение аптаймов), а не в объёме написанного кода." },
  ]}
/>

<Quiz
  id="02-contract-testing-choice-2"
  lessonSlug="02-contract-testing"
  lang="ru"
  question="Провайдер возвращает 40 полей; потребитель читает 4. Почему запись только этих 4 (по типу) — правильный охват для consumer-driven-контракта?"
  choices={[
    { label: "Контракт должен фиксировать реальную зависимость: запись 4 потребляемых полей по типу даёт провайдеру свободно эволюционировать остальные 36, а гейт срабатывает только на изменение, которое реально сломало бы этого потребителя.", correct: true },
    { label: "Записать все 40 с точными примерными значениями для надёжности — больше покрытия меньше сюрпризов.", misconception: "Запись всех 40 делает контракт хрупким снимком, падающим на безобидных изменениях значений и на полях, которые никто не читает, порождая шум, из-за которого его глушат — та же эрозия доверия, что убила OpenAPI-доку команды." },
    { label: "Записать 4 поля, но утверждать лишь их наличие, не типы — типы меняются слишком часто.", misconception: "Только наличие даёт провайдеру сменить число на строку и проскочить гейт, пропуская реальную поломку. Потребитель парсит их как тип, поэтому именно тип и надо утверждать." },
    { label: "Пропустить consumer-контракт и опереться на OpenAPI-спеку провайдера как источник истины.", misconception: "Спека провайдера флагует каждое изменение одинаково, может разойтись с кодом и не знает, от каких полей кто зависит — шумная, склонная к гниению модель, которую consumer-driven-контракты и заменяют." },
  ]}
/>

<Quiz
  id="02-contract-testing-choice-3"
  lessonSlug="02-contract-testing"
  lang="ru"
  question="Во время provider verification провайдер добавил три новых поля в ответ, не упомянутых в pact потребителя, но и переименовал одно поле, которое потребитель читает. Каков итог верификации?"
  choices={[
    { label: "Верификация падает только из-за переименования: проверка асимметрична — фактический ответ должен содержать хотя бы минимальную ожидаемую форму, поэтому лишние неутверждённые поля норм, а пропавшее потребляемое поле ломает minimal-response-сопоставление.", correct: true },
    { label: "Верификация падает, потому что ответ больше не совпадает с записанным точно, включая новые поля.", misconception: "Provider verification — не точное совпадение. Она проверяет, что фактический ответ содержит хотя бы минимальную форму; лишние поля явно разрешены, что и даёт провайдеру эволюционировать поля, которые никто не читает." },
    { label: "Верификация проходит, потому что добавления и переименование взаимно гасятся — число полей не изменилось.", misconception: "Число полей нерелевантно. Minimal-response-проверка ищет каждое утверждённое потребителем поле; переименованное потребляемое поле просто отсутствует под старым именем, поэтому это взаимодействие падает независимо от добавлений." },
    { label: "Верификация пропускается, потому что форма ответа изменилась и pact теперь устарел.", misconception: "Изменение формы никогда не пропускает верификацию — оно запускает сравнение. Верификация падает именно тогда, когда поле, которое читает потребитель, пропало или сменило тип, что и делает переименование." },
  ]}
/>

<Quiz
  id="02-contract-testing-choice-4"
  lessonSlug="02-contract-testing"
  lang="ru"
  question="У взаимодействия `given` = 'a price with id 42 exists', но команда провайдера не написала обработчик этого состояния. Верификация падает. Как сениор должен это читать?"
  choices={[
    { label: "Контракт в порядке; предусловие не было настроено, поэтому повторённый запрос вернул 404 и минимальной формы нет. Фикс — реализовать state handler, а не менять pact.", correct: true },
    { label: "Контракт потребителя неверен и его надо переписать, чтобы не предполагать существование price 42.", misconception: "Запрос и ожидаемый ответ валидны. Сбой — это отсутствующий на стороне провайдера обработчик состояния для строки `given`; правка pact лишь замаскировала бы реальное полезное покрытие." },
    { label: "Верификация должна пройти, потому что provider states — опциональные метаданные, которые верификатор игнорирует.", misconception: "Provider states — предусловие, от которого зависит взаимодействие. Без обработчика данные не засеяны, поэтому повторённый запрос не даёт ожидаемый ответ — их никогда не игнорируют." },
    { label: "Верификатор должен сам создать price 42 из примерных значений в pact.", misconception: "Верификатор не пишет в хранилище провайдера из примеров pact — они описывают форму ожидаемого ответа, а не seed-данные. Провайдер обязан зарегистрировать обработчик, настраивающий данные." },
  ]}
/>

<Quiz
  id="02-contract-testing-choice-5"
  lessonSlug="02-contract-testing"
  lang="ru"
  question="Новый pact потребителя зелено верифицирован против последней CI-сборки провайдера, пайплайн весь зелёный, поэтому он деплоится — и prod сразу падает. Какой вопрос был неверным и какой задаёт верный?"
  choices={[
    { label: "'Верифицировано против latest' — не 'верифицировано против развёрнутого': в prod старый провайдер без нового поля. can-i-deploy сверяет твою версию (git SHA) с версией провайдера, реально стоящей в целевом окружении, и блокирует деплой.", correct: true },
    { label: "Результат верификации истёк и его надо было перезапустить перед деплоем.", misconception: "Результаты не истекают по таймеру. Проблема в том, о какой версии провайдера результат — зелёное против latest ничего не говорит про более старую сборку, физически обслуживающую prod-трафик." },
    { label: "Одной зелёной верификации никогда не достаточно; нужно минимум три независимых прогона.", misconception: "Одна верификация правильной пары — заключительна. Дело не в числе прогонов, а в числе версий: верифицированная пара (новый потребитель, latest-провайдер) — не та пара, что создаётся при деплое (новый потребитель, развёрнутый провайдер)." },
    { label: "Потребителя надо было версионировать по semver, чтобы брокер мог упорядочить деплои.", misconception: "Версионирование по git SHA важно, потому что версия должна меняться, когда мог измениться контракт — голый semver может пометить два разных контракта. Но реальный пробел здесь — проверка против развёрнутого, а не latest, что делает только can-i-deploy." },
  ]}
/>

<Quiz
  id="02-contract-testing-choice-6"
  lessonSlug="02-contract-testing"
  lang="ru"
  question="Все контракты проходят, но провайдер вернул структурно-идеальный `amount_cents: -500` для возврата, и checkout списал отрицательный баланс. Что это говорит о месте контрактного тестирования в стратегии?"
  choices={[
    { label: "Контракты проверяют форму и взаимодействие, никогда бизнес-семантику — валидно-типизированное неверное значение проходит. Поэтому контракты заменяют бо́льшую часть межсервисного e2e для известных потребителей, но несколько настоящих e2e-smoke-тестов на развёрнутых сервисах незаменимы.", correct: true },
    { label: "Matcher в pact был настроен неверно; более строгий matcher поймал бы отрицательное значение.", misconception: "Ни один matcher не ловит это в общем случае — type matcher утверждает, что amount_cents целое, а -500 — валидное целое. Matcher'ы охраняют структуру, не бизнес-корректность; это категориальная граница, а не баг конфига." },
    { label: "Bi-directional contract testing против OpenAPI-спеки поймал бы это.", misconception: "BDCT сравнивает pact со спекой — всё ещё чистая проверка формы и типов, без семантики значений. Он пропустил бы -500 так же легко. Чтобы поймать, нужен тест, утверждающий бизнес-правила, т.е. e2e или unit-логика." },
    { label: "Это доказывает ненадёжность контрактного тестирования, и командам стоит держать полный межсервисный e2e-набор.", misconception: "Контракты верно отправляют на пенсию бо́льшую часть флаки межсервисного e2e для известных внутренних пар. Урок — про пропорцию (много контрактов, немного e2e-smoke), а не про отказ от контрактов из-за их слепоты к семантике." },
  ]}
/>

<Recap lang="ru">
Сквозная линия юнита — один конвейер решений: e2e рушится, потому что здоровье общего окружения — произведение аптаймов всех сервисов, поэтому совместимость спускают на тест каждого ребра; потребитель записывает реальное использование по типу в pact; провайдер верифицирует, проигрывая каждое взаимодействие против реального сервиса и проверяя минимальную ожидаемую форму с provider states, засевающими данные; can-i-deploy превращает матрицу верификации в гейт деплоя, сверяя твою git-SHA-версию с реально развёрнутым; а жёсткая граница — контракты проверяют форму, не семантику — и есть причина, по которой выживает тонкий слой настоящих e2e-smoke-тестов. Каждый неверный ответ здесь — реальный неверный поворот реальной команды.
</Recap>
