---
concepts:
- provider-verification
- provider-states
- pact-broker
- webhooks
- verification-results-matrix
- can-i-deploy
deepensInto:
- 04-can-i-deploy-and-versioning
estMin: 17
lang: ru
lessonType: topic
level: middle
order: 3
prereqs:
- 02-consumer-driven-contracts
slug: 03-provider-verification-broker
sources:
- https://docs.pact.io/getting_started/how_pact_works
- https://docs.pact.io/getting_started/provider_states
- https://docs.pact.io/pact_broker/webhooks
- https://docs.pact.io/pact_broker/can_i_deploy
- https://martinfowler.com/bliki/IntegrationContractTest.html
spiral:
- feedback-loops
- source-of-truth
status: ready
summary: Консьюмер опубликовал pact; теперь провайдер переигрывает каждое записанное
  взаимодействие и доказывает каждое. Provider states настраивают данные так, чтобы
  взаимодействие было выполнимым. Брокер хранит pact''''ы и результаты и через webhooks
  развязывает два CI-пайплайна.
title: 'Верификация провайдера и брокер: переигрывание ожиданий консьюмера против
  реального сервиса'
track: engineering-practice
unit: 02-contract-testing
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import SequenceDiagram from "~/components/diagram/SequenceDiagram.astro";

<Hook>
Команда Прии `checkout-web` опубликовала pact: «когда я делаю `GET /prices/42`, я читаю `amount_cents` (число) и `currency` (строку)». Контракт теперь существует. Но контракт, который никто не проверяет, — лишь пожелание. Сэм владеет сервисом `pricing`, и в прошлом спринте он переименовал `amount_cents` в `unit_amount` — чистый рефакторинг, все собственные тесты `pricing` зелёные, выкатил в пятницу. В понедельник `checkout-web` рисовал `NaN` на каждой странице товара, потому что поле, которое он парсит, тихо исчезло. Pact описывал ровно ту поломку, что выкатилась, и ничто не переиграло его против реального сервиса Сэма до деплоя. Запись так и не переиграли. Это переигрывание — отправка каждого записанного запроса на *реально работающий провайдер* и проверка того, что ответ всё ещё соблюдает то, что читает консьюмер, — и есть **верификация провайдера** (provider verification), а место, которое хранит pact'ы и результаты верификации, чтобы двум командам не пришлось координироваться вручную, — это **брокер** (broker).
</Hook>

<Crux>Верификация переигрывает каждое записанное взаимодействие против реального провайдера, доказывая, что ответ соблюдает ожидания консьюмера.</Crux>

<Explanation>

## Верификация — это переигрывание, а не перечитывание

Pact-файл — это список конкретных взаимодействий: запрос, ожидаемый ответ и provider state, который каждое из них предполагает. Верификация провайдера механична: верификатор Pact берёт этот список и для каждого взаимодействия **отправляет записанный запрос на реальный, работающий провайдер и сравнивает фактический ответ с ожидаемым.** Со стороны провайдера никакого мока нет — запрос попадает в подлинную маршрутизацию, контроллеры, сериализаторы и (при настроенном состоянии) подлинный слой данных. Если pact `checkout-web` говорит, что `GET /prices/42` должен вернуть тело, содержащее `amount_cents`, верификатор шлёт ровно этот запрос на поднятый сервис `pricing` и осматривает, что вернётся.

Сравнение намеренно асимметрично, и именно это свойство делает гейт стабильным. Верификатор проверяет, что фактический ответ **содержит как минимум** те данные, что описал консьюмер, — *минимальный ожидаемый ответ*. Сэм может добавить десять новых полей в `/prices/42`, и верификация останется зелёной, потому что консьюмер никогда не утверждал их отсутствие. Но если он переименует `amount_cents`, поле, которое читает консьюмер, исчезнет, фактический ответ перестанет содержать минимальную ожидаемую форму, и **верификация упадёт до деплоя.** Контракт срабатывает ровно на том изменении, что сломало бы реального консьюмера, и молчит обо всём остальном — та же точность, что давала consumer-driven запись, теперь обеспеченная со стороны провайдера.

## Provider states — тонкая трудная часть

Взаимодействие вроде «`GET /prices/42` возвращает `200` с телом цены» выполнимо, только если цена 42 действительно существует в провайдере в момент переигрывания запроса. Консьюмер записал это предусловие как **provider state** (состояние провайдера) — строку в клаузе `given`, например `"a price with id 42 exists"`, Pact-эквивалент шага *Given* из Cucumber: привести систему в известное состояние перед запуском взаимодействия. Верификация — момент, когда эту строку приходится превратить в реальные данные.

Поэтому провайдер должен зарегистрировать **обработчики состояний** (state handlers): код, который для каждого именованного состояния настраивает данные, нужные взаимодействию. Перед переигрыванием каждого взаимодействия верификатор вызывает соответствующий обработчик (часто через POST `{ "consumer": "...", "state": "..." }` на тестовый эндпойнт смены состояния), обработчик вставляет цену 42 в хранилище данных провайдера, затем отправляется запрос. Два правила делают это рабочим и легко нарушаются. Во-первых, **каждое взаимодействие верифицируется в изоляции** — никакое состояние не переносится из предыдущего, так что каждый обработчик должен устанавливать своё полное предусловие с нуля и в идеале убирать за собой после. Во-вторых, обработчик настраивает *данные*, а не ответ: он делает взаимодействие выполнимым, но ответ по-прежнему порождает реальный код провайдера, в чём весь смысл. Здесь верификация тихо дорожает — каждая отдельная строка `given`, что написали консьюмеры, становится обработчиком состояния, который команда провайдера обязана реализовать и поддерживать, а отсутствующий или неверный обработчик валит верификацию не потому, что контракт сломан, а потому что предусловие так и не было выполнено.

<Inset kind="why" lang="ru">
Почему бы просто не направить верификацию на общую staging-базу, где цена 42 уже есть? Потому что тогда тест перестаёт быть детерминированным и изолированным: вернёт ли `GET /prices/42` `200`, зависит от того, какие строки случайно существуют в этот день, чужой джоб очистки может удалить цену 42 между прогонами, а взаимодействие, ожидающее `404`, не может сосуществовать с ожидающим `200` против одних и тех же фиксированных данных. Provider states переносят предусловие *в контракт* и в принадлежащий провайдеру код настройки, так что каждое взаимодействие несёт собственный мир и переигрываемо в изоляции на свежезасеянном провайдере. Именно это позволяет верификации прогоняться в собственном CI провайдера, против его собственных эфемерных данных, без координации с окружением консьюмера.
</Inset>

## Брокер — система записи между двумя пайплайнами

Верификации нужны два артефакта, чтобы встретиться: pact консьюмера и результат «прошло/упало» провайдера. Наивный способ их свести — подключить CI консьюмера так, чтобы он напрямую запускал сборку провайдера, — но это снова связывает те два пайплайна, ради развязки которых ты и пришёл к контрактному тестированию. **Pact Broker** (open-source) / **PactFlow** (хостинг) — обменник, разрывающий эту связь. Консьюмер публикует свой pact в брокер, помеченный версией и веткой, и уходит. Независимо CI провайдера тянет нужные pact'ы из брокера, прогоняет верификацию и публикует результаты обратно. Ни один пайплайн не вызывает другой; брокер — общая, асинхронная система записи, держащая каждый pact и каждый результат верификации.

Брокер также может *толкать* (push). **Webhook** (HTTP-коллбэк, срабатывающий при наступлении события) срабатывает на событиях брокера — важнее всего `contract_requiring_verification_published` (который пришёл на смену `contract_content_changed` в брокере 2.82.0) — чтобы запустить сборку верификации провайдера в момент, когда приходит новый или изменённый pact, передавая шаблонные параметры вроде `${pactbroker.pactUrl}` и `${pactbroker.providerVersionNumber}`, так что сборка провайдера точно знает, что верифицировать. Итог всего этого — **матрица результатов верификации** (verification results matrix): сетка того, какие версии консьюмеров были верифицированы против каких версий провайдеров. Эту матрицу запрашивает инструмент `can-i-deploy` перед релизом — «верифицирована ли версия `pricing`, которую я собираюсь выкатить, против версии `checkout-web`, уже работающей в проде?» — так что каждый *pacticipant* (слово Pact для приложения в контракте) может деплоиться по собственному расписанию, под гейтом из записанных результатов, а не из человека, сверяющегося с другой командой.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Аспект</th>
        <th class="border border-border px-3 py-2 text-left">Без брокера: пайплайны связаны напрямую</th>
        <th class="border border-border px-3 py-2 text-left">Брокер как система записи</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">Передача pact</td>
        <td class="border border-border px-3 py-2">Закоммичен в репо провайдера / передан вручную</td>
        <td class="border border-border px-3 py-2">Опубликован в брокер, помечен версией + веткой</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Кто запускает верификацию</td>
        <td class="border border-border px-3 py-2">CI консьюмера вызывает сборку провайдера напрямую</td>
        <td class="border border-border px-3 py-2">Webhook брокера на contract-requiring-verification</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Связанность пайплайнов</td>
        <td class="border border-border px-3 py-2">Тесная — сборка консьюмера блокируется на провайдере</td>
        <td class="border border-border px-3 py-2">Асинхронная — ни один пайплайн не вызывает другой</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Решение о деплое</td>
        <td class="border border-border px-3 py-2">Вручную «зелёная ли сборка другой команды?»</td>
        <td class="border border-border px-3 py-2">`can-i-deploy` запрашивает матрицу результатов</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Независимый деплой</td>
        <td class="border border-border px-3 py-2">Нет — оба должны релизиться вместе</td>
        <td class="border border-border px-3 py-2">Да — каждый pacticipant по своему расписанию</td>
      </tr>
    </tbody>
  </table>
</div>

<DragOrder
  id="03-provider-verification-broker-drag-1"
  lessonSlug="03-provider-verification-broker"
  lang="ru"
  prompt="Упорядочьте сквозной поток от нового pact консьюмера до гейтированного деплоя провайдера:"
  items={[
    { id: "a", label: "CI консьюмера публикует pact в брокер, помеченный его версией и веткой" },
    { id: "b", label: "Брокер шлёт webhook contract_requiring_verification_published в CI провайдера" },
    { id: "c", label: "Провайдер поднимается, и для каждого взаимодействия запускает обработчик состояния, чтобы настроить данные, предполагаемые его `given`" },
    { id: "d", label: "Верификатор переигрывает каждый записанный запрос и проверяет, что ответ содержит минимальную ожидаемую форму" },
    { id: "e", label: "Результаты верификации публикуются обратно в брокер, заполняя матрицу" },
    { id: "f", label: "Перед релизом can-i-deploy запрашивает матрицу, чтобы подтвердить: эта версия провайдера верифицирована против консьюмера из прода" },
  ]}
  correctOrder={["a", "b", "c", "d", "e", "f"]}
/>

<Quiz
  id="03-provider-verification-broker-quiz-1"
  lessonSlug="03-provider-verification-broker"
  lang="ru"
  question="Во время верификации провайдер добавляет в GET /prices/42 три новых поля, которые pact консьюмера никогда не упоминает. Что произойдёт и почему?"
  choices={[
    { label: "Верификация проходит — верификатор проверяет, что фактический ответ содержит как минимум минимальную ожидаемую форму, поэтому лишние поля, которые консьюмер не утверждал, в порядке", correct: true },
    { label: "Верификация падает, потому что фактический ответ больше не совпадает в точности с записанным", misconception: "Верификация провайдера — не проверка точного совпадения. Она утверждает, что фактический ответ содержит как минимум минимальный ожидаемый ответ, описанный консьюмером; неутверждённые лишние поля явно разрешены, и именно это позволяет провайдеру эволюционировать поля, которые ни один консьюмер не читает." },
    { label: "Верификация проходит, только если консьюмер переопубликует pact с новыми полями", misconception: "Консьюмер не должен записывать поля, которые он не читает. Весь смысл проверки минимального ответа в том, что провайдер может свободно добавлять поля, а консьюмер не трогает свой pact; требование переопубликации снова связало бы команды." },
    { label: "Верификация пропускается, потому что форма ответа изменилась", misconception: "Изменение формы никогда не пропускает верификацию — оно запускает сравнение. Здесь сравнение проходит, потому что ничего из утверждённого консьюмером не убрали; верификация падает лишь когда поле, которое читает консьюмер, отсутствует или имеет неверный тип." },
  ]}
/>

<Quiz
  id="03-provider-verification-broker-quiz-2"
  lessonSlug="03-provider-verification-broker"
  lang="ru"
  question="У взаимодействия `given` — 'a price with id 42 exists', но команда провайдера так и не написала обработчик для этого состояния. Каков результат и его правильное прочтение?"
  choices={[
    { label: "Верификация падает, потому что предусловие так и не настроено — цены 42 нет, GET /prices/42 возвращает 404, и причина падения — отсутствующий обработчик состояния, а не сломанный контракт", correct: true },
    { label: "Верификация проходит, потому что provider states — необязательные метаданные, которые верификатор игнорирует", misconception: "Provider states не игнорируются — это предусловие, от которого зависит взаимодействие. Без обработчика данные не засеяны, поэтому переигранный запрос не может породить ожидаемый ответ, и верификация падает." },
    { label: "Верификатор автосоздаёт цену 42 из примеров значений в pact", misconception: "Верификатор не пишет в хранилище данных провайдера из примеров pact. Провайдер должен зарегистрировать обработчик состояния, настраивающий данные; примеры значений описывают форму ожидаемого ответа, а не данные для засева." },
    { label: "Верификация падает, и это доказывает, что контракт консьюмера неверен", misconception: "Контракт в порядке — запрос и ожидаемый ответ валидны. Падение на стороне провайдера: строка `given` без соответствующего обработчика. Исправление — реализовать обработчик, а не менять pact." },
  ]}
/>

<TradeoffMatrix
  id="03-provider-verification-broker-tradeoff-1"
  lessonSlug="03-provider-verification-broker"
  lang="ru"
  prompt="Как провайдер должен подготовить данные, чтобы каждое записанное взаимодействие было выполнимо во время верификации?"
  options={[
    { name: "Зарегистрировать обработчики на каждое состояние, засевающие ровно те данные, что называет каждый `given`, запускаемые перед каждым взаимодействием в изоляции и убирающие за собой после", summary: "Provider states, изолированно.", correct: true, justification: "Каждое взаимодействие объявляет своё предусловие как provider state, и верификатор запускает соответствующий обработчик перед переигрыванием. Настройка данных на каждое состояние и в изоляции означает, что каждое взаимодействие несёт собственный мир: взаимодействие, ожидающее 200, и ожидающее 404 могут оба пройти, прогоны детерминированы, а верификация работает против собственных эфемерных данных CI провайдера без зависимости от общего окружения." },
    { name: "Направить верификацию на общую staging-базу, где обычно есть данные", summary: "Общие staging-данные.", justification: "Общие данные разрушают изоляцию и детерминизм: существует ли цена 42, зависит от того, что оставили другие команды, джоб очистки может удалить её посреди прогона, и нельзя удовлетворить взаимодействие, ожидающее 404, против тех же фиксированных строк, что и ожидающее 200. Предусловие принадлежит контракту и принадлежащей провайдеру настройке, а не тому, что случайно лежит в staging." },
    { name: "Позволить верификатору автоматически вставлять примеры значений pact как данные для засева", summary: "Автозасев из примеров.", justification: "Примеры значений описывают форму ожидаемого ответа для сравнения, а не строки для вставки; верификатор не пишет их в хранилище провайдера. Полагаться на это означает, что данные на самом деле не засеяны, переигранный запрос отдаёт 404, и верификация падает из-за предусловия, которое провайдер так и не настроил." },
    { name: "Удалить provider states из pact, чтобы взаимодействиям не нужна была настройка", summary: "Убрать состояния.", justification: "Удаление `given` не убирает зависимость — `GET /prices/42` по-прежнему нужна цена 42, чтобы вернуть 200. Без состояния взаимодействие выполнимо лишь по везению окружающих данных, что и есть тот самый хрупкий, зависящий от порядка ад интеграционных тестов, ради устранения которого provider states и были созданы." },
  ]}
/>

</Explanation>

<SequenceDiagram
  label="Broker развязывает CI-пайплайны консьюмера и провайдера"
  caption="Ни один пайплайн не вызывает другой. Broker хранит pact-ы и результаты; can-i-deploy запрашивает матрицу перед каждым релизом."
  hue="var(--d-systems)"
  actors={["CI консьюмера", "broker", "CI провайдера", "can-i-deploy"]}
  messages={[
    { from: "CI консьюмера", to: "broker", label: "публикация pact (версия + тег ветки)" },
    { from: "broker", to: "CI провайдера", label: "webhook: contract_requiring_verification_published" },
    { from: "CI провайдера", to: "broker", label: "загрузить pact; запустить обработчики состояний + переиграть взаимодействия" },
    { from: "CI провайдера", to: "broker", label: "опубликовать результат верификации (pass / fail)" },
    { from: "can-i-deploy", to: "broker", label: "запросить матрицу перед релизом" },
    { from: "broker", to: "can-i-deploy", label: "exit 0 (пара зелёная) или exit 1 (блок)" },
  ]}
/>

<KeyTakeaway>Верификация провайдера переигрывает каждое записанное взаимодействие против реального работающего провайдера и проверяет, что ответ содержит как минимум минимальную ожидаемую форму, поэтому срабатывает лишь когда сломано поле, которое читает консьюмер. Provider states — предусловие: обработчики на каждое состояние засевают данные, чтобы каждое взаимодействие было выполнимо в изоляции. Брокер хранит pact'ы и результаты верификации и через webhooks развязывает два CI-пайплайна, так что `can-i-deploy` может запросить матрицу и дать каждой стороне деплоиться независимо.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="03-provider-verification-broker-retrieval"
  lang="ru"
  questions={[
    { q: "Объясните полный механизм верификации провайдера, включая сравнение по минимальному ответу и роль provider states.", a: "Верификация провайдера переигрывает записанный контракт консьюмера против реального, работающего провайдера. Верификатор проходит по списку взаимодействий pact и для каждого отправляет ровно записанный запрос на поднятый провайдер — попадая в подлинную маршрутизацию, контроллеры, сериализаторы и слой данных, без мока со стороны провайдера, — затем сравнивает фактический ответ с ожидаемым. Сравнение асимметрично: оно проверяет, что фактический ответ содержит как минимум минимальный ожидаемый ответ, описанный консьюмером, так что провайдер может добавлять поля, которые ни один консьюмер не читает, и оставаться зелёным, но переименование или удаление поля, которое парсит консьюмер, приводит к тому, что ответ перестаёт содержать минимальную форму, и верификация падает. Provider states делают каждое взаимодействие выполнимым. Взаимодействие вроде 'GET /prices/42 возвращает 200' держится, только если цена 42 существует, поэтому консьюмер записал строку `given` ('a price with id 42 exists'), Pact-эквивалент Given из Cucumber. Провайдер регистрирует обработчики состояний — код, засевающий данные для каждого именованного состояния, часто вызываемый через тестовый эндпойнт смены состояния, получающий имя консьюмера и состояние. Перед переигрыванием каждого взаимодействия верификатор запускает соответствующий обработчик, который вставляет данные; затем реальный код провайдера порождает ответ. Два правила: каждое взаимодействие верифицируется в изоляции без переносимого состояния, и обработчик настраивает данные, а не ответ. Отсутствующий обработчик валит верификацию потому, что предусловие не было выполнено, а не потому, что контракт сломан." },
    { q: "Для чего нужен брокер и как webhooks плюс can-i-deploy позволяют двум командам деплоиться независимо?", a: "Брокер (open-source Pact Broker или хостинг PactFlow) — общая, асинхронная система записи, держащая каждый pact и каждый результат верификации, разрывающая связь между CI-пайплайнами консьюмера и провайдера. Консьюмер публикует свой pact в брокер, помеченный версией и веткой, и уходит; провайдер независимо тянет нужные pact'ы, прогоняет верификацию и публикует результаты обратно. Ни один пайплайн не вызывает другой — альтернатива, подключение CI консьюмера так, чтобы он напрямую запускал сборку провайдера, снова связывает ровно то, ради развязки чего существует контрактное тестирование, и заставляет два приложения релизиться вместе. Брокер также может толкать: webhook срабатывает на событиях брокера, важнее всего contract_requiring_verification_published (который пришёл на смену contract_content_changed в брокере 2.82.0), запуская сборку верификации провайдера в момент прихода изменённого pact и передавая шаблонные параметры вроде URL pact и версии провайдера, чтобы сборка знала, что верифицировать. Все результаты накапливаются в матрицу результатов верификации, сетку того, какие версии консьюмеров верифицированы против каких версий провайдеров. Перед релизом инструмент can-i-deploy запрашивает эту матрицу — 'верифицирована ли версия, которую я собираюсь выкатить, против версии другого приложения, уже работающего в проде?' — так что каждый pacticipant деплоится по собственному расписанию, под гейтом из записанных результатов, а не из человека, спрашивающего другую команду, зелёная ли их сборка." },
  ]}
/>

<Recap lang="ru">
Консьюмер опубликовал pact, но контракт, который никто не переигрывает, — лишь пожелание: переименование, вернувшее NaN, выкатилось, потому что ничто не переиграло запись против реального провайдера. Верификация провайдера — это и есть переигрывание: верификатор шлёт каждый записанный запрос на подлинный работающий провайдер и проверяет, что фактический ответ содержит как минимум минимальную ожидаемую форму, так что гейт остаётся зелёным, когда провайдер добавляет поля, которые никто не читает, и срабатывает лишь когда поле, которое консьюмер действительно парсит, переименовано, удалено или сменило тип. Тонкая трудная часть — provider states: каждое взаимодействие объявляет своё предусловие как строку `given`, и провайдер должен зарегистрировать обработчики на каждое состояние, засевающие ровно те данные — запускаемые перед каждым взаимодействием, в изоляции, настраивающие данные, а не ответ, — потому что направление верификации на общие staging-данные разрушает детерминизм, который контракт призван гарантировать. Связывает всё брокер (Pact Broker или PactFlow), асинхронная система записи, держащая каждый pact и каждый результат верификации, чтобы ни одному CI-пайплайну не пришлось вызывать другой; webhooks вроде contract_requiring_verification_published запускают сборку верификации провайдера, когда pact меняется, результаты накапливаются в матрицу верификации, а can-i-deploy запрашивает эту матрицу, чтобы каждый pacticipant мог деплоиться независимо — под гейтом из записанного доказательства, а не из двух команд, координирующихся вручную. Дальше: как контракты безопасно эволюционируют, пока обе стороны меняются со временем. Теперь, когда увидишь провайдера, переименовывающего поле, первый инстинкт будет: был ли pact переигран против работающего сервиса и обновлена ли матрица в брокере?
</Recap>
