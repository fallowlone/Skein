---
concepts:
- consumer-driven-contracts
- pact-file
- provider-verification
- can-i-deploy
- expand-then-contract
- contract-testing-limits
estMin: 14
lang: ru
level: senior
order: 8
prereqs: []
slug: quiz-short
sources:
- https://docs.pact.io/
- https://docs.pact.io/pact_broker/can_i_deploy
- https://martinfowler.com/articles/consumerDrivenContracts.html
status: ready
summary: "Промпты на свободное припоминание по всему юниту контрактного тестирования. Сначала ответь своими словами, потом раскрой эталон и сравни."
title: 'Контрактное тестирование: тест на припоминание'
track: engineering-practice
unit: 02-contract-testing
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";

<Hook>
Припоминание бьёт перечитывание. На каждый промпт скажи или запиши полный ответ по памяти, прежде чем открыть эталон — именно усилие припоминания закрепляет механизм.
</Hook>

<Goal lang="ru">
Восстанови позвоночник юнита — почему рушится e2e, как рождается pact, что доказывает provider verification, что добавляет can-i-deploy, как эволюционировать контракт и где контракты заканчиваются — не подглядывая в уроки.
</Goal>

<RetrievalDrawer
  client:visible
  id="02-contract-testing-short-retrieval"
  lang="ru"
  questions={[
    { q: "Почему сквозное интеграционное тестирование рушится на масштабе и где именно в тестовой пирамиде дыра, через которую проскальзывает переименование поля?", a: "E2e-сценарий проходит, только когда каждый хоп одновременно поднят, мигрирован и засеян, поэтому надёжность сценария — произведение индивидуальных аптаймов сервисов: двенадцать сервисов по 98% — это под 80% зелёных, и деградирует мультипликативно с добавлением сервисов. Здоровьем окружения к тому же владеют все, значит — никто, поэтому полу-применённая миграция чужой команды валит твой прогон. Это даёт медленные (40+ минут поднятия), флаки (~1 из 5 несвязанных падений) и потому заглушённые наборы — а заглушённый тест не ловит ничего, продолжая жечь время. Дыра пирамиды — на сетевой границе: unit-тест потребителя мокает провайдера, поэтому утверждает против устаревшего представления автора о форме провайдера. В день, когда провайдер переименовывает поле, unit-тесты потребителя остаются зелёными, и переименование доходит до prod. Недостающий слой проверяет согласие двух сервисов без поднятия обоих." },
    { q: "Пройди по тому, как рождается consumer-driven pact-файл, и почему это делает его доверительнее, чем рукописная OpenAPI-дока.", a: "Потребитель пишет тест, поднимающий Pact mock server и настраивающий ожидаемое взаимодействие: ждём GET /prices/42, отвечаем 200 с формой тела. Реальный клиентский код потребителя затем вызывает mock и парсит ответ, утверждая нужные поля type matcher'ами, а не точными значениями. Только если код потребителя реально делает заявленный запрос и читает заявленные поля, тест проходит — и на прохождении Pact пишет это проигранное взаимодействие, включая предполагаемые provider states, в JSON pact-файл. Поэтому pact — это запись кода, который прогнался и прошёл, а не отдельно написанное обещание. OpenAPI-дока написана отдельно от кода, и ничто её не исполняет, поэтому она тихо гниёт — описывает фантомное поле или упускает то, от которого зависят три команды. Pact не может описать взаимодействие, которого потребитель не выполняет, поэтому фиксирует реальное использование и остаётся честным." },
    { q: "Что реально делает provider verification, включая minimal-response-сравнение и роль provider states?", a: "Верификатор обходит взаимодействия pact'а и для каждого шлёт точный записанный запрос реальному поднятому провайдеру — настоящие роутинг, контроллеры, сериализаторы, слой данных, без мока на стороне провайдера — затем сравнивает фактический ответ с ожидаемым. Сравнение асимметрично: фактический ответ должен содержать хотя бы минимальную ожидаемую форму, поэтому провайдер может добавлять поля, которые никто не читает, и оставаться зелёным, но переименование или удаление потребляемого поля валит верификацию до деплоя. Provider states делают каждое взаимодействие выполнимым: взаимодействие вроде GET /prices/42 возвращает 200 держится, только если price 42 существует, поэтому pact записывает строку given, а провайдер регистрирует state handler, засевающий эти данные перед каждым взаимодействием. Каждое взаимодействие верифицируется изолированно без переноса состояния, и обработчик настраивает данные, а не ответ — отсутствующий обработчик валит верификацию из-за невыполненного предусловия, а не сломанного контракта." },
    { q: "Почему зелёной верификации недостаточно для деплоя и что вместо этого проверяет can-i-deploy? Почему версионировать по git SHA и записывать деплои в конце rollout?", a: "Результат верификации — узкий попарный факт: версия потребителя X согласилась с версией провайдера Y — обычно Y это последняя CI-сборка провайдера, которая может быть на несколько деплоев впереди того, что в prod. Вопрос деплоя другой: безопасна ли твоя версия против версии провайдера, физически работающей в целевом окружении. can-i-deploy отвечает, сверяя версию, которую ты хочешь задеплоить, с версиями всех интегрированных приложений, сейчас стоящих в этом окружении (что брокер знает лишь потому, что деплои записаны), и выходя ненулевым, если хоть одна нужная пара не верифицирована. Версионируй по git SHA, потому что версия должна меняться ровно тогда, когда мог измениться контракт, а голый semver может пометить два разных контракта, давая брокеру переиспользовать устаревший результат для другого кода. Зови record-deployment в самом конце успешного rollout, чтобы брокер отражал физическую реальность — запишешь рано, а rollout упадёт, и гейт следующей команды отравлен. Выигрыш — независимая деплоябельность." },
    { q: "Как выкатить breaking-изменение провайдера, не ломая потребителей, и как pending/WIP-pact'ы обрабатывают зеркальный, consumer-led случай?", a: "Используй expand-then-contract (parallel change), превращая одно breaking-изменение в три зелёных деплоя. Expand: задеплой провайдера, возвращающего и старое, и новое поле с одним значением — каждый существующий pact всё ещё читает старое поле, поэтому can-i-deploy проходит и провайдер едет с нулём изменений у потребителей. Migrate: каждый потребитель в своём темпе без lockstep переключает тест на новое поле, регенерируя pact, который верифицируется сразу, ведь провайдер уже возвращает поле. Contract: когда can-i-deploy через матрицу подтвердит, что ни один prod-pact больше не ссылается на старое поле, задеплой провайдера, который его удаляет — теперь не-breaking. Никогда не удаляй на том же шаге, где добавил. Зеркальный случай — потребитель добавляет ожидание до того, как провайдер его построил: pending-pact'ы дают провайдеру верифицировать и сообщать этот контент, но pending-сбой не меняет код выхода сборки, поэтому она остаётся зелёной, пока pact не верифицирован хоть раз; WIP-pact'ы расширяют это, авто-подтягивая любой новый неверифицированный pact с даты с жёстко включённым pending." },
    { q: "Назови жёсткие границы контрактного тестирования и сениорское суждение про контракты против сквозных тестов.", a: "Контракты проверяют форму и взаимодействие, никогда бизнес-логику или семантику — структурно-валидное неверное значение вроде amount_cents: -500 проходит каждый контракт, из-за чего возврат списал отрицательный баланс. Им нужны известные потребители, поэтому они не подходят публичным API с неизвестными внешними клиентами (где лучше spec-first или BDCT, хотя и они не знают, какой клиент какое поле читает). Они добавляют реальную инфраструктуру — брокер, который надо держать и защищать, publish/verify, вшитые в каждый пайплайн, гейты can-i-deploy, запись деплоев — оправданную между многими быстро меняющимися внутренними сервисами, но избыточную для двух сервисов на стабильном интерфейсе. И они дают ложную уверенность, когда фикстуры provider-state чище production-данных, заверяя формы, которые реальный сервис никогда не возвращает. Поэтому суждение — не контракты-или-e2e, а оба-в-пропорции: контракты заменяют бо́льшую часть межсервисного e2e для известных внутренних потребителей, но держи несколько настоящих e2e-smoke-тестов на развёрнутых сервисах с почти-реальными данными ради корректности значений сквозь хопы, расхождения данных с фикстурами и сквозного auth/config." },
  ]}
/>

<Recap lang="ru">
Если ты смог восстановить каждый ответ по памяти, ты держишь позвоночник юнита: e2e рушится, потому что здоровье общего окружения — произведение аптаймов всех сервисов, поэтому совместимость спускают на слой каждого ребра; pact — запись прошедшего consumer-кода по типу, доверительная там, где дока гниёт; provider verification проигрывает каждое взаимодействие против реального сервиса и проверяет минимальную форму с state handler'ами, засевающими данные; can-i-deploy сверяет твою git-SHA-версию с реально развёрнутым; expand-then-contract плюс pending/WIP-pact'ы эволюционируют контракты без lockstep; а границы — форма, не семантика, только известные потребители, реальные накладные, расхождение фикстур и production — и есть причина, по которой всегда выживает тонкий слой настоящих e2e-smoke-тестов.
</Recap>
