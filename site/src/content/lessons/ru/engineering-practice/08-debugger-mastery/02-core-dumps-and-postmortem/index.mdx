---
concepts:
- ep-debugger-coredumps
deepensInto: []
estMin: 17
lang: ru
lessonType: topic
level: senior
order: 2
prereqs: []
slug: "02-core-dumps-and-postmortem"
sources:
- https://man7.org/linux/man-pages/man5/core.5.html
- https://www.freedesktop.org/software/systemd/man/latest/coredumpctl.html
- https://sourceware.org/gdb/current/onlinedocs/gdb/Core-Files.html
spiral: []
status: ready
summary: 'Core dump замораживает упавший процесс для офлайн-вскрытия — но лишь если ulimit -c и core_pattern разрешают. Дампы достигают гигабайтов. gdb сопоставляет дамп с символами по build-id; несовпавшая сборка даёт мусорный стек — классическая ловушка works-in-dev, cores-in-prod.'
title: 'Core dump-ы и post-mortem: ulimit, core_pattern, сопоставление по build-id и разматывание SIGSEGV постфактум'
track: "engineering-practice"
unit: "08-debugger-mastery"
---

import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
Платёжный сервис падал раз в неделю с `SIGSEGV`, который никто не мог воспроизвести, и два месяца у команды была лишь однострочная запись в логе. Первый фикс был не в коде — а в обнаружении, что `ulimit -c` в контейнере был `0`, поэтому ядро выбрасывало каждый core dump. Его выставили, и следующее падение оставило core на 6 ГБ. gdb открыл его и напечатал стек, что был чистым мусором: кадры `??`, адреса без символов, backtrace, указывающий в никуда. Причина — вторая ловушка: бинарь в работающем контейнере был пересобран и передеплоен с момента локальной копии инженера, поэтому build-id не совпадали, и gdb разматывал дамп против неверной таблицы символов. Как только из реестра вытащили точный бинарь по его build-id, те же 6 ГБ замороженной памяти размотались в чистый стек, кончающийся use-after-free в пути ретраев — баг, что был невидим два месяца, всё это время сидел в дампе.
</Hook>

<Crux>Core dump — замороженный образ процесса: лишь если ulimit и core_pattern разрешают, читаем только через символы, совпавшие по build-id.</Crux>

<Explanation>

## Генерация core: ulimit, core_pattern, coredumpctl

Core dump — это запись ядром образа памяти падающего процесса (адресное пространство, регистры, стек каждого потока) в файл, чтобы вскрыть его после исчезновения процесса. Но три гейта решают, получишь ли ты его. Первый — попроцессный лимит ресурсов `ulimit -c`: если он `0` (дефолт во многих базовых образах контейнеров и юнитах systemd), ядро тихо отбрасывает дамп. Второй — `/proc/sys/kernel/core_pattern` решает, куда он идёт: буквальный шаблон пути вроде `core.%p.%e` или, если начинается с `|`, пайп к программе-обработчику. На современных systemd-хостах `core_pattern` пайпит в `systemd-coredump`, и дампы достаёшь через `coredumpctl list` и `coredumpctl gdb <pid>` вместо охоты за файлом. Третий — в контейнерах лимит и шаблон живут в namespace ядра *хоста*, поэтому core_pattern, что пайпит к обработчику, отсутствующему в mount namespace контейнера, проваливается — частая причина «я выставил ulimit и всё равно ничего».

Размер не мал. Core содержит всю записываемую отображённую память, поэтому сервис, держащий несколько гигабайтов кучи, рождает многогигабайтный core — 6 ГБ из Hook обычны. Это имеет операционный вес: дампы могут забить диск и занимают секунды записи, пока процесс мёртв, поэтому прод-настройки их ограничивают, направляют на выделенное хранилище и иногда срезают ненужные отображения. Дамп нужен, но планируй его размер.

<Quiz
  id="02-core-dumps-and-postmortem-quiz-1"
  lessonSlug="02-core-dumps-and-postmortem"
  lang="ru"
  question="Контейнеризованный сервис падает с SIGSEGV, но core-файл так и не появляется, даже после добавления разработчиком 'ulimit -c unlimited' в entrypoint. Какая наиболее вероятная оставшаяся причина?"
  choices={[
    { label: "Хостовый /proc/sys/kernel/core_pattern пайпит к обработчику (напр. systemd-coredump), недостижимому из namespace контейнера, поэтому дамп генерируется, но не захватывается там, где ждали", correct: true },
    { label: "SIGSEGV никогда не рождает core dump; только SIGABRT", misconception: "SIGSEGV — один из сигналов, чьё дефолтное действие включает сброс core, рядом с SIGABRT, SIGBUS и другими. Отсутствие дампа — про гейты ulimit/core_pattern и раскол контейнер/хост, а не про тип сигнала." },
    { label: "Core dump-ы требуют сборки бинаря с -g, и стрипнутый бинарь их подавляет", misconception: "Debug-символы влияют на то, можешь ли прочесть дамп, а не на то, пишет ли ядро его. Стрипнутый бинарь всё равно делает core; нужны лишь совпадающие символы для размотки. Сбой захвата — про окружение." },
    { label: "ulimit -c вступает в силу только после перезагрузки", misconception: "ulimit — попроцессный лимит, применяемый сразу к шеллу и его детям, без перезагрузки. Реальный разрыв — core_pattern, направляющий дамп через хостовый обработчик, который контейнер не видит." },
  ]}
/>

<Inset kind="why" lang="ru">
Почему обработчик core_pattern живёт на хосте даже для падения в контейнере? Потому что сброс core — действие ядра, а `core_pattern` — единственная хост-глобальная настройка ядра: попроцессного core_pattern не бывает. Когда процесс в контейнере получает segfault, ядро хоста вычисляет хостовый core_pattern и запускает хостовый обработчик. Поэтому идеально корректный `ulimit -c unlimited` внутри контейнера всё равно направляет дамп через то, что говорит core_pattern ноды, а на ноде Kubernetes это часто путь или обработчик, что контейнер никогда не видит. Урок: настраивай захват core на уровне ноды, а не только образа.
</Inset>

## Символы, build-id и расхождение dev-vs-prod

Core dump — это сырая память и значения регистров, он понятия не имеет, что за функция `0x4a3f10`. Чтобы превратить адреса в имена функций и номера строк, нужны совпадающие debug-символы, и «совпадающие» точно: каждый ELF-бинарь несёт **build-id**, хеш, впечённый на линковке, а дамп записывает build-id бинаря и разделяемых библиотек, что были загружены. gdb (или `debuginfod`) использует этот build-id, чтобы достать точный `.debug`-файл. Если навести gdb на бинарь, что был перекомпилирован — пусть из того же исходника с другим timestamp или флагом компилятора, — build-id различаются, таблица символов больше не ложится на адреса в дампе, и получаешь мусорный стек из Hook: кадры `??` и бессмысленные номера строк. Это ловушка «works in dev, cores in prod»: твоя локальная пересборка — *не* прод-бинарь, даже если исходник идентичен.

Профессиональная настройка разделяет заботы: шипи стрипнутый бинарь в прод (мелкий, без `.debug`), храни полную debug-инфу как отдельный `.debug`-файл, совпадающий по build-id, и дай `debuginfod` или твоему символ-серверу отдавать её по требованию. Тогда core из прода размотается против точных символов, хоть работающий бинарь не нёс ни одного. Проверяй совпадение явно — `readelf -n core` и `readelf -n binary` показывают build-id; если различаются, стоп, ведь каждый кадр, что печатает gdb, — ложь.

## Разматывание SIGSEGV постфактум

С совпавшим core и символами вскрытие механично. `bt` идёт по сохранённым кадрам стека от точки падения внутрь, используя DWARF CFI (call-frame information), чтобы найти кадр каждого вызывающего даже при кастомной раскладке кадра. Верхний кадр — где ударил fault; `info registers` показывает адрес сбоя (на x86 затронутый адрес в регистре, что записывает ядро), и `SIGSEGV` по адресу `0x0` кричит о null-разыменовании, а дикий высокий адрес намекает на испорченный указатель или use-after-free. Можно `frame N` в любой вызывающий, `print` локалы (с оговорками про оптимизацию из прошлого урока) и прочесть код `_exit`: core от `SIGABRT` обычно значит ассерт или обнаруженную libc порчу кучи (`free(): invalid pointer`), указывая на собственный диагноз аллокатора, а не на сырой fault. Дамп — единственный замороженный миг, без реплея и шагов, поэтому реконструируешь историю из замороженного стека, регистров и кучи — ровно поэтому use-after-free, что падает лишь иногда, становится находимым: одного захваченного мига достаточно.

<Quiz
  id="02-core-dumps-and-postmortem-quiz-2"
  lessonSlug="02-core-dumps-and-postmortem"
  lang="ru"
  question="gdb открывает 6-ГБ прод-core, но backtrace — сплошь кадры '??' без имён символов, тогда как тот же gdb показывает чистый стек на core с твоего ноутбука. Наиболее вероятная причина?"
  choices={[
    { label: "У бинаря, что ты дал gdb, build-id отличается от того, что породил прод-core, поэтому таблица символов не ложится на адреса дампа", correct: true },
    { label: "6-ГБ core слишком велик для размотки gdb, поэтому он сдаётся и печатает ??", misconception: "gdb разматывает большие core нормально; размер — забота I/O и диска, не причина кадров ??. ?? значат отсутствующие или несовпавшие символы, тут — расхождение build-id между твоим бинарём и тем, что упал." },
    { label: "Прод-core никогда не содержат данных стека, только кучу", misconception: "Core включает стек каждого потока вместе с регистрами и записываемой памятью — именно это и делает размотку возможной. Пустой backtrace — сбой сопоставления символов, а не отсутствие данных стека." },
    { label: "Core записан SIGSEGV, что опускает символы, которые включил бы SIGABRT", misconception: "Сигнал сбоя не меняет, какие символы есть у core — core не несут символов вовсе; символы из совпавшего бинаря/.debug. Фикс — достать бинарь, совпавший по build-id, а не сменить сигнал." },
  ]}
/>

</Explanation>

<FlowDiagram
  label="От падения к читаемому стеку: гейты и стыковка по build-id"
  hue="var(--d-backend)"
  perRow={3}
  nodes={[
    { id: "crash", label: "SIGSEGV", sub: "процесс падает" },
    { id: "gate", label: "ulimit + core_pattern", sub: "гейты уровня хоста" },
    { id: "core", label: "Core dump", sub: "ГБ, замороженная память" },
    { id: "match", label: "совпадение build-id", sub: "бинарь + .debug" },
    { id: "unwind", label: "Размотка", sub: "bt через DWARF CFI" },
    { id: "garbage", label: "Кадры ??", sub: "несовпавшие символы" },
  ]}
  edges={[
    { from: "crash", to: "gate", label: "ядро" },
    { from: "gate", to: "core", label: "разрешено" },
    { from: "core", to: "match", label: "открыть в gdb" },
    { from: "match", to: "unwind", label: "id сходятся" },
    { from: "match", to: "garbage", label: "id различны" },
  ]}
  caption="ulimit и core_pattern — хост-глобальные гейты; пройди их и получишь многогигабайтный замороженный образ. build-id — ключ стыковки дампа и его символов: не совпал — и каждый кадр, что печатает gdb, выдумка."
/>

<KeyTakeaway>Core dump — это замороженная память и регистры упавшего процесса, записываемые лишь если ulimit -c ненулевой и хостовый core_pattern направляет дамп в достижимое место — гейт, что живёт на ноде, а не в образе контейнера. Дампы доходят до гигабайтов, ведь держат всю кучу. Чтобы прочесть, нужны символы, совпавшие по build-id: наведи gdb на перекомпилированный бинарь — и стек размотается в мусорные кадры ??, это ловушка works-in-dev, cores-in-prod. Шипи стрипнутым, отдавай .debug, совпавший по build-id, через debuginfod, проверяй id до доверия любому кадру, затем разматывай SIGSEGV из замороженного мига.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="02-core-dumps-and-postmortem-retrieval"
  lang="ru"
  questions={[
    { q: "Какие три гейта решают, родит ли падающий контейнеризованный процесс пригодный core dump?", a: "Первый — ulimit -c (RLIMIT_CORE): если ноль, ядро тихо отбрасывает дамп — а ноль это дефолт во многих образах контейнеров и юнитах systemd. Второй — /proc/sys/kernel/core_pattern: шаблон пути вроде core.%p.%e или пайп (ведущий |) к обработчику вроде systemd-coredump, достаётся через coredumpctl. Третий — раскол контейнер/хост: core_pattern — единственная хост-глобальная настройка ядра без попроцессного переопределения, поэтому дамп из контейнера направляется core_pattern-ом ноды через обработчик ноды — идеально корректный ulimit -c unlimited внутри контейнера всё равно провалится, если хостовый обработчик или путь недостижим из namespace контейнера. Настраивай захват на уровне ноды. И планируй размер: core держит всю записываемую память, поэтому многогигабайтные дампы нормальны и могут забить диски." },
    { q: "Почему прод-core иногда разматывается в кадры ?? и как build-id это чинит?", a: "Core — это сырая память и регистры без собственных символов; адреса становятся именами лишь против совпадающей debug-инфы. Каждый ELF-бинарь несёт хеш build-id, впечённый на линковке, а дамп записывает build-id загруженного бинаря и библиотек. gdb (или debuginfod) использует build-id, чтобы достать точный .debug. Если навести gdb на бинарь, перекомпилированный пусть из идентичного исходника, build-id различаются, таблица символов больше не ложится на адреса дампа, и bt печатает кадры ?? и мусорные номера строк — ловушка works-in-dev, cores-in-prod, ведь локальная пересборка — не прод-бинарь. Фикс: шипи стрипнутые бинари, храни полный .debug, совпавший по build-id, отдавай через debuginfod или символ-сервер и проверяй через readelf -n, что build-id core и бинаря сходятся, до доверия любому кадру." },
  ]}
/>

<Recap lang="ru">
Core dump — это снимок ядром умирающего процесса: его адресное пространство, стек каждого потока и регистры, записанный, чтобы провести вскрытие после исчезновения процесса, превращая невоспроизводимое падение в замороженный миг, что осматриваешь не спеша. Но захват гейтован. ulimit -c должен быть ненулевым, а он по дефолту ноль во многих образах контейнеров и юнитах systemd, поэтому самое первое падение часто рождает ничего. /proc/sys/kernel/core_pattern решает назначение: шаблон пути или пайп к обработчику вроде systemd-coredump, достаётся через coredumpctl. Принципиально, core_pattern — единственная хост-глобальная настройка ядра без попроцессного переопределения, поэтому падение в контейнере направляется шаблоном ноды через обработчик ноды — вот почему корректный ulimit внутри контейнера всё равно даёт ничего, если захват не настроен на ноде. Планируй и размер: core держит всю записываемую память, поэтому многогигабайтные дампы вроде 6 ГБ из Hook обычны и могут забить диски. Чтение core требует символов, совпавших по build-id, хешу с линковки, записанному для бинаря и каждой загруженной библиотеки. Наведи gdb на перекомпилированный бинарь — пусть из идентичного исходника — и build-id разойдутся, таблица символов больше не ляжет на адреса дампа, и bt размотается в мусор ??. Это ловушка works-in-dev, cores-in-prod: твоя локальная пересборка — не прод-бинарь. Профессиональный путь шипит стрипнутые бинари, хранит полный .debug, совпавший по build-id, отдаёт его через debuginfod и проверяет id через readelf -n до доверия кадру. С совпавшим core вскрытие механично: bt идёт по кадрам через DWARF CFI, info registers даёт адрес сбоя (0x0 значит null-разыменование, дикий адрес намекает на use-after-free или порчу), frame и print читают цепочку вызывающих, а core от SIGABRT указывает на ассерт или собственный диагноз порчи кучи от libc — всё из одного замороженного мига, без реплея, чего ровно достаточно, чтобы найти баг, что падает лишь иногда.
</Recap>
