---
concepts:
- ep-debugger-prod
deepensInto: []
estMin: 17
lang: ru
lessonType: topic
level: senior
order: 3
prereqs: []
slug: "03-production-debugging"
sources:
- https://www.brendangregg.com/perf.html
- https://github.com/iovisor/bpftrace/blob/master/README.md
- https://github.com/async-profiler/async-profiler
spiral: []
status: ready
summary: 'У зависшего прод-процесса нет repro, и его нельзя остановить. Сэмплирующие инструменты — perf, eBPF/bpftrace, async-profiler — наблюдают при 1-5% оверхеда без остановки; ptrace attach останавливает процесс. USDT-пробы и flame graph находят горячий путь на живом сервисе.'
title: 'Прод-отладка без repro: perf, eBPF, flame graph и почему ptrace останавливает мир'
track: "engineering-practice"
unit: "08-debugger-mastery"
---

import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
Один под был пригвождён к 100% CPU, обслуживал запросы, но медленно, и так уже час. Инстинкт on-call инженера — приаттачить gdb и снять стек — и сеньор остановил его руку. «Этот `ptrace` attach остановит процесс. Он принимает трафик; ты заморозишь живой сервер, и балансировщик навалит тысячу запросов на его соседей, пока ты читаешь один стек». Вместо этого они запустили `perf record -F 99 -p <pid>` на десять секунд — сэмплируя работающий процесс 99 раз в секунду при примерно 2% оверхеда, ни разу не останавливая, — и свернули результат в flame graph. Широкое плато наверху было безошибочным: 80% CPU в компиляции регэкспа, вызываемой на каждый запрос, потому что ключ кэша считался неверно и скомпилированный паттерн ни разу не переиспользовался. Они ни разу не остановили процесс, ни разу не потеряли запрос и получили ответ меньше чем за минуту. Урок, что унёс джун: в проде первый вопрос не «что за баг», а «могу ли я это наблюдать, не останавливая мир».
</Hook>

<Crux>Зависший прод-процесс можно сэмплировать при 1-5% оверхеда, но ptrace attach останавливает его намертво — в проде смотришь, не останавливая.</Crux>

<Explanation>

## Прод-ограничение: нельзя остановить мир

Каждая техника отладчика из прошлых уроков предполагает, что можно остановить процесс. В проде это предположение переворачивается. Классический интерактивный отладчик аттачится через `ptrace`, а `ptrace` attach (`PTRACE_ATTACH`, что делают gdb и Delve) **останавливает каждый поток цели**, пока не отцепишься. На живом сервере, принимающем трафик, это катастрофа: запросы встают в очередь или таймаутят, health-чеки падают, балансировщик выбрасывает инстанс и наваливает на соседей, а автоскейлер может убить «нездоровый» под, который ты отлаживал — уничтожив живое состояние, ради чтения которого ты приаттачился. Поэтому прод-тулкит построен на другом принципе: *наблюдать, не останавливая*. Ты сэмплируешь или трассируешь процесс на ходу, платя несколько процентов оверхеда, и реконструируешь, что он делает, из этих сэмплов, а не замораживаешь его для инспекции.

Есть и вторая причина, почему `ptrace` — неверный дефолт в проде: он одноцелевой и интрузивный, тогда как вопросы обычно статистические — «куда уходит CPU», «какой syscall медленный», «какой путь аллоцирует» — на которые куда лучше отвечает агрегация тысяч дешёвых сэмплов, чем один замороженный снимок.

## Сэмплирование vs инструментирование: perf, eBPF, async-profiler

Низкооверхедный тулкит делится на *сэмплирование* против *инструментирования*. **Сэмплирование** прерывает процесс на фиксированной частоте — `perf record -F 99` берёт 99 сэмплов стека в секунду на CPU — и выводит, куда уходит время, из распределения; оверхед примерно 1-5%, ведь большинство циклов идут нетронутыми и стоит лишь периодическое прерывание. **Инструментирование** цепляется к конкретным событиям — входу функции, syscall, tracepoint — и работает на каждом возникновении; дёшево на событие, но дорого, если событие горячее. `perf`, сэмплер ядра, профилирует CPU аппаратными счётчиками производительности. **eBPF** (управляемый `bpftrace`) запускает верифицированную, песочную программу в ядре на выбранных событиях, агрегируя в ядре, так что даже трассировка на событие остаётся дешёвой — `bpftrace -e 'tracepoint:syscalls:sys_enter_read { @[comm] = count(); }'` считает чтения по процессам с пренебрежимой ценой, без остановки процесса, без модуля ядра. Для управляемых рантаймов **async-profiler** сэмплирует JVM (или похожее) сигнальным или perf-events механизмом, который, в отличие от наивного обхода стека JVMTI, не страдает safepoint-смещением — он сэмплирует потоки в произвольных точках, а не только там, где рантайм их паркует, поэтому flame graph отражает реальные горячие пути, а не локации safepoint.

<Quiz
  id="03-production-debugging-quiz-1"
  lessonSlug="03-production-debugging"
  lang="ru"
  question="Под пригвождён к 100% CPU и обслуживает живой трафик. On-call хочет приаттачить gdb, чтобы снять стек. Почему это неверный первый ход и что лучше?"
  choices={[
    { label: "gdb аттачится через ptrace, что останавливает каждый поток цели — заморозка живого сервера теряет трафик; вместо этого сэмплируй perf-ом при ~1-5% оверхеда без остановки", correct: true },
    { label: "gdb не может приаттачиться к процессу уже на 100% CPU, поэтому всё равно провалится", misconception: "gdb аттачится нормально независимо от загрузки CPU; проблема не в провале аттача, а в том, что ptrace останавливает весь процесс. Уровень CPU не связан с успехом аттача." },
    { label: "perf тоже останавливает процесс, но даёт более красивый flame graph", misconception: "perf сэмплирует работающий процесс на фиксированной частоте без остановки — эта неинтрузивность и есть весь смысл. Только ptrace-основанные интерактивные отладчики останавливают цель." },
    { label: "Аттач в порядке; реальная проблема в том, что gdb нужно дерево исходников в проде", misconception: "Опасность никак не связана с доступностью исходников — она в том, что ptrace attach останавливает сервер, принимающий трафик, вызывая таймауты и выброс. Наличие исходников не сделало бы заморозку живого процесса безопасной." },
  ]}
/>

<Inset kind="why" lang="ru">
Почему сэмплирование на 99 Гц так надёжно выявляет горячий путь, глядя на процесс лишь ~99 раз в секунду? Потому что горячесть статистична. Если 80% времени CPU тратится в одной функции, то по тысячам сэмплов примерно 80% поймают стек внутри этой функции — широкое плато на flame graph. Ты не пытаешься увидеть каждую инструкцию; ты оцениваешь распределение, а распределение сходится быстро с дешёвыми сэмплами. Нечётное 99 (не 100) избегает синхронизации с таймерами, тикающими на круглых частотах, что систематически промахивались бы или перечитывали периодическую работу. Дешёвое, слегка-иррациональное сэмплирование бьёт дорогую исчерпывающую трассировку для вопроса «куда уходит время».
</Inset>

## USDT-пробы и flame graph: находим путь на живом

Два куска превращают сэмплы в ответ. **Flame graph** агрегируют тысячи сэмплов стека в одну картину: каждый прямоугольник — функция, ширина — доля сэмплов (время), сложены по глубине вызова. Широкий прямоугольник у вершины — туда CPU реально уходит — плато компиляции регэкспа из Hook считывается одним взглядом, без шагов. **USDT-пробы** (userland statically-defined tracing) — нулевого-оверхеда-когда-выключены маркеры, вкомпилированные в бинарь (базы данных, рантаймы языков, твой сервис), к которым eBPF/`perf` цепляются в именованных, семантически осмысленных точках — «commit транзакции», «старт запроса» — давая стабильное инструментирование без изменения или остановки процесса. Вместе они дают ответить «что этот зависший процесс делает прямо сейчас» на живом, нетронутом сервисе: сэмплируй его, сверни в flame graph, прочти горячий путь и подтверди USDT или kernel-tracepoint, что подозреваемый путь — тот, что срабатывает. Всё расследование стоит несколько процентов CPU и ноль даунтайма — единственный вид отладки, что разрешает прод-SLA.

<Quiz
  id="03-production-debugging-quiz-2"
  lessonSlug="03-production-debugging"
  lang="ru"
  question="Ты хочешь посчитать, как часто срабатывает конкретный syscall на всех процессах загруженной прод-ноды, с минимальным оверхедом и без остановок процессов. Какой подход подходит и почему?"
  choices={[
    { label: "eBPF/bpftrace-программа на tracepoint syscall, что агрегирует счётчики в ядре — на событие, но дёшево, ведь суммирует в ядре и не останавливает ни один процесс", correct: true },
    { label: "Приаттачить gdb к каждому процессу и поставить брейкпоинт на syscall", misconception: "Брейкпоинты gdb останавливают каждую цель на каждом срабатывании через ptrace, что на загруженной ноде остановило бы процессы и раздавило throughput. eBPF агрегирует в ядре без остановки чего-либо — ровно прод-безопасный путь." },
    { label: "Сэмплирование perf record -F 99, потому что сэмплирование всегда считает события точно", misconception: "Сэмплирование оценивает распределение, оно не считает каждое возникновение — идеально для 'куда уходит время', но не для точного счёта событий. Для счёта конкретного syscall верный инструмент — агрегация eBPF в ядре на tracepoint." },
    { label: "Core dump, затем посчитать возникновения syscall в замороженном образе", misconception: "Core — единственный замороженный миг без истории событий — он не может посчитать, как часто syscall срабатывал во времени. Живой счёт событий нужна трассировка в ядре вроде eBPF, не статический снимок." },
  ]}
/>

</Explanation>

<FlowDiagram
  label="Отладка зависшего живого процесса без его остановки"
  hue="var(--d-backend)"
  perRow={3}
  nodes={[
    { id: "wedge", label: "Зависший под", sub: "100% CPU, принимает трафик" },
    { id: "ptrace", label: "ptrace attach", sub: "ОСТАНАВЛИВАЕТ мир" },
    { id: "sample", label: "perf -F 99", sub: "~2% оверхеда, без остановки" },
    { id: "ebpf", label: "eBPF / USDT", sub: "в ядре, дёшево на событие" },
    { id: "flame", label: "Flame graph", sub: "ширина = доля времени" },
    { id: "hot", label: "Горячий путь", sub: "широкое плато наверху" },
  ]}
  edges={[
    { from: "wedge", to: "ptrace", label: "НЕ НАДО" },
    { from: "wedge", to: "sample", label: "НАДО" },
    { from: "sample", to: "flame", label: "свернуть стеки" },
    { from: "ebpf", to: "flame", label: "подтвердить путь" },
    { from: "flame", to: "hot", label: "прочесть плато" },
  ]}
  caption="ptrace attach останавливает живой сервер и триггерит выброс; сэмплирование и eBPF в ядре наблюдают при нескольких процентах оверхеда без остановки. Сверни сэмплы в flame graph, и горячий путь — широкое плато."
/>

<KeyTakeaway>В проде обычно нельзя остановить мир: ptrace attach (gdb/Delve) останавливает каждый поток живого сервера, вызывая таймауты, выброс и убийство автоскейлером того самого пода, что ты хотел прочесть. Прод-тулкит наблюдает без остановки — perf сэмплирует на 99 Гц при ~1-5% оверхеда, eBPF/bpftrace агрегирует на событие в ядре, а async-profiler сэмплирует управляемые рантаймы без safepoint-смещения. Сэмплирование оценивает, куда уходит время; инструментирование считает конкретные события. Сверни сэмплы стека в flame graph, и горячий путь — самое широкое плато наверху; USDT-пробы метят стабильные точки для подтверждения — всё на живом, нетронутом сервисе при нуле даунтайма.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="03-production-debugging-retrieval"
  lang="ru"
  questions={[
    { q: "Почему ptrace/gdb attach — неверный дефолт в проде и что его заменяет?", a: "PTRACE_ATTACH — что используют gdb и Delve — останавливает каждый поток цели, пока не отцепишься. На живом сервере, принимающем трафик, это катастрофа: запросы встают в очередь или таймаутят, health-чеки падают, балансировщик выбрасывает инстанс и наваливает на соседей, а автоскейлер может убить нездоровый под, уничтожив живое состояние, ради чтения которого ты приаттачился. Он также одноцелевой и интрузивный, когда реальные вопросы статистические. Замена — наблюдать-без-остановки: сэмплируй или трассируй работающий процесс при нескольких процентах оверхеда и реконструируй поведение из сэмплов. perf сэмплирует CPU на фиксированной частоте (напр. -F 99) при ~1-5% оверхеда; eBPF через bpftrace запускает верифицированные песочные программы на событиях ядра, агрегируя в ядре, так что даже трассировка на событие остаётся дешёвой и ничего не останавливает; async-profiler сэмплирует JVM без safepoint-смещения." },
    { q: "Различи сэмплирование и инструментирование и объясни, как flame graph и USDT-пробы находят горячий путь на живом.", a: "Сэмплирование прерывает процесс на фиксированной частоте и выводит распределение времени из того, куда падают стеки — оверхед ~1-5%, ведь большинство циклов идут нетронутыми и стоит лишь периодическое прерывание; идеально для 'куда уходит CPU'. Инструментирование цепляется к конкретным событиям (вход функции, syscall, tracepoint) и работает на каждом — дёшево на событие, дорого если событие горячее, и точно для счёта конкретного события, чего сэмплирование не может. Flame graph сворачивает тысячи сэмплов стека в одну картину: каждый прямоугольник — функция, ширина — доля сэмплов (время), сложены по глубине, поэтому самый широкий прямоугольник наверху — туда CPU реально уходит — плато компиляции регэкспа, прочитанное одним взглядом. USDT-пробы — нулевой-цены-когда-выключены маркеры, вкомпилированные в бинари в семантически осмысленных точках (commit транзакции, старт запроса), к которым цепляются eBPF/perf, давая стабильное инструментирование для подтверждения подозреваемого пути — всё без изменения или остановки живого процесса." },
  ]}
/>

<Recap lang="ru">
Прод-отладка переворачивает ключевое предположение интерактивного отладчика: нельзя остановить мир. ptrace attach — PTRACE_ATTACH, что делают gdb и Delve — останавливает каждый поток цели до отцепления, и на живом сервере, принимающем трафик, это значит запросы в очереди, упавшие health-чеки, балансировщик, выбрасывающий инстанс и наваливающий на соседей, и автоскейлер, потенциально убивающий тот самый под, чьё живое состояние ты приаттачился прочесть. ptrace также одноцелевой и интрузивный, когда реальные прод-вопросы статистические: куда уходит CPU, какой syscall медленный, какой путь аллоцирует. Поэтому прод-тулкит наблюдает без остановки. Сэмплирование прерывает процесс на фиксированной частоте — perf record -F 99 берёт 99 сэмплов стека в секунду — при примерно 1-5% оверхеда, ведь большинство циклов идут нетронутыми и стоит лишь периодическое прерывание; оно оценивает распределение, что сходится быстро, и нечётное 99 избегает синхронизации с таймерами круглых частот. Инструментирование цепляется к конкретным событиям и работает на каждом, точно для счёта, но дорого на горячих событиях; eBPF, управляемый bpftrace, запускает верифицированные песочные программы на событиях ядра и агрегирует в ядре, так что даже трассировка на событие остаётся дешёвой, ничего не останавливает и не нужен модуль ядра. async-profiler сэмплирует управляемые рантаймы вроде JVM без safepoint-смещения, поэтому flame graph отражают реальные горячие пути, а не локации safepoint. Два куска превращают сэмплы в ответ: flame graph сворачивают тысячи стеков в одну картину, где ширина прямоугольника — доля времени, поэтому самое широкое плато наверху — туда CPU уходит — компиляция регэкспа на каждый запрос из Hook, прочитанная взглядом; и USDT-пробы, нулевой-цены-когда-выключены маркеры, вкомпилированные в осмысленных точках, дают eBPF/perf подтвердить подозреваемый путь на живом, нетронутом сервисе. Всё расследование стоит несколько процентов CPU и ноль даунтайма — единственная отладка, что разрешает прод-SLA, и причина, почему первый прод-вопрос не что за баг, а могу ли я это наблюдать, не останавливая мир.
</Recap>
