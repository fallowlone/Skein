---
concepts:
- ep-debugger-breakpoints
deepensInto: []
estMin: 16
lang: ru
lessonType: topic
level: senior
order: 1
prereqs: []
slug: "01-breakpoints-and-watchpoints"
sources:
- https://sourceware.org/gdb/current/onlinedocs/gdb/Breakpoints.html
- https://lldb.llvm.org/use/map.html
- https://github.com/go-delve/delve/tree/master/Documentation/cli
spiral: []
status: ready
summary: 'Брейкпоинт — это байт 0xCC, вшитый в код, или один из четырёх аппаратных debug-регистров. Условный брейкпоинт вычисляется на каждом срабатывании и бывает в 1000 раз медленнее. Watchpoint ловит запись по адресу. Оптимизированная сборка ломает пошаговую отладку.'
title: 'Брейкпоинты и watchpoint-ы: как на самом деле работают INT3, четыре debug-регистра и условный eval'
track: "engineering-practice"
unit: "08-debugger-mastery"
---

import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
Баг проявлялся только на клиенте #4 815 162 ночной сверки и только иногда. Джун поставил брейкпоинт внутри цикла по строкам и запустил под отладчиком; задача, что обычно заканчивалась за 90 секунд, шла уже 40 минут — потому что отладчик два миллиона раз в секунду проваливался в ядро, чтобы проверить условие, которое инженер вписал в брейкпоинт. Сеньор поменял одну вещь: вместо условного брейкпоинта на цикле она поставила аппаратный watchpoint на единственное поле баланса счёта, сказала отладчику продолжить — и процесс шёл почти на полной скорости до той самой инструкции, что записала отрицательный баланс, а затем замер с живым стеком виновника. Условие, которое не удавалось воспроизвести чтением кода, стало одним trap-ом по адресу памяти. Разница между 40 минутами и 3 секундами — в знании, что один из этих брейкпоинтов это программный trap в отладчике, а другой — кремний, следящий за шиной.
</Hook>

<Crux>Брейкпоинт — патч 0xCC или один из четырёх debug-регистров; условный eval идёт на каждом срабатывании, до 1000x медленнее watchpoint-а.</Crux>

<Explanation>

## Программные брейкпоинты: вшивание байта 0xCC

Обычный брейкпоинт — не магия: отладчик переписывает первый байт целевой инструкции на `0xCC`, опкод `INT3` в x86, и запоминает исходный байт. Когда исполнение доходит до этого адреса, CPU поднимает breakpoint-trap, ядро доставляет `SIGTRAP`, и отладчик (он подключён через `ptrace`) просыпается, восстанавливает исходный байт, даёт осмотреть состояние, а чтобы продолжить — делает один шаг по восстановленной инструкции и снова вшивает `0xCC`. Поэтому программных брейкпоинтов неограниченно много — каждый стоит один пропатченный байт памяти, — но они работают только на записываемых страницах кода: нельзя вшить `0xCC` в read-only флеш или страницу, на запись в которую нет прав, и ровно тогда приходишь к аппаратным брейкпоинтам.

У модели патча есть резкое следствие: брейкпоинт на адресе, который цель позже перезаписывает или перекомпилирует (JIT, самомодифицирующийся код, hot-patch функции), тихо перестаёт срабатывать — твой `0xCC` затёрли. Delve и gdb это прячут, но механизм проступает, как только отлаживаешь JIT-рантайм.

## Аппаратные брейкпоинты и watchpoint-ы: четыре регистра, без патча

x86-64 выставляет четыре debug-регистра, `DR0`–`DR3`, каждый хранит адрес, плюс `DR7` задаёт режим каждого: брейк на исполнение, на запись или на чтение/запись, длиной 1, 2, 4 или 8 байт. Поскольку CPU сравнивает эти адреса на шине аппаратно, аппаратный брейкпоинт ничего не патчит и работает на read-only страницах — но их всего четыре, на поток, и точка. *Watchpoint* — тот же механизм, наведённый на данные: ставишь `DR0` следить за 8-байтным полем на запись, и CPU делает trap в тот же миг, как любая инструкция в него пишет, неважно каким путём. Это и есть приём сеньора из Hook: вместо «какая из тысячи строк портит это поле?» дать кремнию ответить, следя за самим полем.

Числа, что решают стратегию: аппаратный watchpoint стоит почти ноль рантайм-оверхеда, ведь сравнение в конвейере бесплатно, тогда как *программный* watchpoint — то, на что отладчик откатывается, когда все четыре регистра заняты, — делает по шагу через всю программу и сравнивает значение после каждой инструкции, замедляя цель в 100–1000 раз. gdb молча переключится с аппаратного на программный watchpoint, если попросить пятый; `info watchpoints` и сообщение `Hardware watchpoint` против `Watchpoint` — то, как узнаёшь, что получил.

<Quiz
  id="01-breakpoints-and-watchpoints-quiz-1"
  lessonSlug="01-breakpoints-and-watchpoints"
  lang="ru"
  question="Ты просишь gdb следить за 5-й областью памяти, уже поставив четыре аппаратных watchpoint-а. Программа, прежде быстрая под отладчиком, теперь еле ползёт. Что произошло?"
  choices={[
    { label: "Все четыре debug-регистра заняты, поэтому gdb откатился к программному watchpoint-у, который идёт по шагу через каждую инструкцию и каждый раз сравнивает значение", correct: true },
    { label: "5-й watchpoint вшил байт 0xCC в данные, испортив их и вызвав медленные перечитывания", misconception: "Watchpoint-ы на данные не реализуются патчем 0xCC — так работают программные брейкпоинты на код. Замедление от того, что gdb идёт по шагу через всю программу, эмулируя слежение в ПО, когда аппаратные регистры кончились." },
    { label: "gdb отклонил 5-й watchpoint, а замедление не связано", misconception: "gdb его не отклоняет — он молча деградирует до программного watchpoint-а, и эта деградация и есть причина замедления. info watchpoints различает Hardware watchpoint и обычный Watchpoint." },
    { label: "Watchpoint-ы всегда идут по шагу; первые четыре были такими же медленными", misconception: "Аппаратные watchpoint-ы почти без оверхеда, ведь CPU сравнивает адрес на шине бесплатно. По шагу идёт только программный откат — поэтому именно пятый еле ползёт." },
  ]}
/>

<Inset kind="why" lang="ru">
Почему условный брейкпоинт стоит куда дороже watchpoint-а на той же логике? Условный брейкпоинт всё равно срабатывает аппаратно или через 0xCC каждый раз, как достигнут адрес, а затем отладчик вычисляет выражение условия в своём интерпретаторе и решает, останавливаться ли. В плотном цикле это полный цикл trap-в-ядро, переключение-на-отладчик, вычисление, продолжение на каждой итерации — легко в 1000 раз дороже тела цикла. Аппаратный watchpoint на данных переворачивает вопрос: CPU остаётся в горячем цикле на полной скорости и делает trap только на той одной записи, что нужна. Подгоняй trap под редкое событие, а не под частую строку.
</Inset>

## Почему оптимизированная сборка ломает пошаговую отладку

Поставь брейкпоинт на строку в `-O2`-сборке — и можешь встать на строку, что «уже отработала», или шагнуть в строку, исполняющуюся трижды на один исходный оператор, или услышать, что локальная переменная `<optimized out>`. Это не баг отладчика. Оптимизатор переставляет инструкции через границы строк, инлайнит вызовы (нет кадра, в который шагать), держит переменные лишь в регистрах, что переиспользуются, и сливает одинаковые хвосты ветвей — так что взаимно-однозначного отображения исходной строки в диапазон инструкций, на которое опирается пошаговая отладка, больше нет. DWARF (стандарт отладочной информации, встроенной компилятором в бинарь) записывает best-effort таблицу строк, но для инлайненной, переставленной, размещённой по регистрам программы эта таблица lossy по построению. Сеньорский поток: воспроизвести под `-O0 -g`, когда можешь себе позволить, а когда нет (баг лезет только в оптимизации — частый Heisenbug) — спуститься к пошаговой отладке по инструкциям и читать регистры напрямую, а не верить номерам строк и локалам.

<Quiz
  id="01-breakpoints-and-watchpoints-quiz-2"
  lessonSlug="01-breakpoints-and-watchpoints"
  lang="ru"
  question="Отлаживая -O2-сборку, ты ставишь брейкпоинт на строку 40, но он встаёт, локал показывает <optimized out>, а шаги пропускают строки. Тот же код на -O0 шагает чисто. Лучшее прочтение?"
  choices={[
    { label: "Оптимизатор переставил и заинлайнил инструкции и держит переменную в переиспользуемом регистре, поэтому отображение строки в инструкции lossy — переключись на шаги по инструкциям или пересобери под -O0, если там воспроизводится", correct: true },
    { label: "Debug-символы повреждены и их надо перегенерировать", misconception: "Символы в порядке; таблица строк просто lossy, ведь оптимизатор сломал взаимно-однозначное отображение строки в инструкции. Перегенерация тех же -O2-символов ничего не меняет." },
    { label: "Регистр аппаратного брейкпоинта переполнился и пропустил строки", misconception: "Исчерпание регистров деградирует watchpoint-ы до программного шага; оно не вызывает артефактов оптимизированной отладки. Пропуски и <optimized out> — прямые эффекты -O2: перестановка, инлайн, размещение по регистрам." },
    { label: "Брейкпоинт был программным 0xCC-патчем, который оптимизатор затёр", misconception: "Оптимизатор работает на компиляции, задолго до того, как 0xCC вшивается в рантайме, поэтому затереть байт брейкпоинта он не может. Артефакты — от lossy отображения строк оптимизированного кода, а не от затёртого байта." },
  ]}
/>

</Explanation>

<FlowDiagram
  label="Выбор брейкпоинта: подгоняй механизм trap-а под задаваемый вопрос"
  hue="var(--d-backend)"
  perRow={3}
  nodes={[
    { id: "q", label: "Что ловить?", sub: "строка кода vs запись данных" },
    { id: "soft", label: "Программный bp", sub: "0xCC-патч, без лимита" },
    { id: "hw", label: "Аппаратный bp", sub: "DR0-DR3, только 4" },
    { id: "watch", label: "Watchpoint", sub: "trap на запись по адресу" },
    { id: "cond", label: "Условный", sub: "eval на каждом, медленно" },
    { id: "swfall", label: "Программный watch", sub: "по шагу 100-1000x" },
  ]}
  edges={[
    { from: "q", to: "soft", label: "записываемый код" },
    { from: "q", to: "hw", label: "read-only страница" },
    { from: "q", to: "watch", label: "порча данных" },
    { from: "watch", to: "swfall", label: "5-й, регистры заняты" },
    { from: "soft", to: "cond", label: "добавить условие" },
  ]}
  caption="Четыре аппаратных debug-регистра обслуживают и брейкпоинты на исполнение, и watchpoint-ы на данные; пятое слежение или условие в горячем цикле форсируют медленную программную эмуляцию. Выбирай механизм, делающий trap на редком событии."
/>

<KeyTakeaway>Программный брейкпоинт — байт 0xCC, вшитый в записываемый код; аппаратный брейкпоинт или watchpoint — один из четырёх регистров DR0-DR3, сравнивающих адреса на шине бесплатно. Условные брейкпоинты переоцениваются на каждом срабатывании и бывают в 1000 раз медленнее аппаратного watchpoint-а, делающего trap лишь на той редкой записи, что нужна. Оптимизированные -O2-сборки переставляют, инлайнят и размещают по регистрам, делая таблицу строк lossy — шаги пропускают, локалы читаются optimized out, и откатываешься к шагам по инструкциям.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="01-breakpoints-and-watchpoints-retrieval"
  lang="ru"
  questions={[
    { q: "Сравни программные брейкпоинты, аппаратные брейкпоинты и watchpoint-ы по механизму и лимитам.", a: "Программный брейкпоинт переписывает первый байт целевой инструкции на 0xCC (INT3); на срабатывании CPU делает trap, ядро шлёт SIGTRAP, отладчик восстанавливает исходный байт, даёт осмотреть, затем делает один шаг и снова вшивает 0xCC. Их неограниченно много, но работают только на записываемых страницах кода и затираются JIT-ом или самомодифицирующимся кодом. Аппаратный брейкпоинт использует один из четырёх debug-регистров DR0-DR3 (под управлением DR7), сравнивая адрес на шине аппаратно — ничего не патчит, работает на read-only страницах, но их всего четыре. Watchpoint — тот же регистровый механизм на данных: делает trap, когда любая инструкция пишет (или читает) отслеживаемый адрес, так находишь, какой код портит поле, не зная пути. Кончились регистры — отладчик эмулирует слежение в ПО, идя по шагу через всю программу, в 100-1000 раз медленнее." },
    { q: "Почему условные брейкпоинты в горячих циклах убивают производительность и почему -O2 ломает пошаговую отладку?", a: "Условный брейкпоинт всё равно делает trap на каждом срабатывании, затем отладчик вычисляет условие в своём интерпретаторе перед решением остановиться. В плотном цикле это trap-в-ядро плюс переключение контекста плюс вычисление плюс продолжение на каждой итерации, легко в 1000 раз дороже тела цикла. Замена на аппаратный watchpoint на данных даёт циклу идти на полной скорости и делать trap лишь на той редкой записи. Про -O2: оптимизатор переставляет инструкции через строки, инлайнит вызовы, держит переменные в переиспользуемых регистрах и сливает хвосты ветвей, поэтому взаимно-однозначного отображения строки в инструкции больше нет. DWARF пишет best-effort, но lossy таблицу строк, давая пропущенные строки и <optimized out>-локалы; воспроизводи под -O0 -g, когда можешь, иначе спускайся к шагам по инструкциям и читай регистры." },
  ]}
/>

<Recap lang="ru">
Брейкпоинт — одна из двух физических вещей. Программный брейкпоинт вшивает байт 0xCC INT3 поверх первого байта инструкции; на срабатывании CPU делает trap, ядро доставляет SIGTRAP ptrace-подключённому отладчику, тот восстанавливает байт, даёт посмотреть, затем шагает и снова вшивает — их неограниченно, но только на записываемом коде, и они тихо побеждаются JIT-ом, что перезаписывает пропатченную страницу. Аппаратный брейкпоинт использует один из всего четырёх debug-регистров DR0-DR3, под управлением DR7 для режимов исполнение/запись/чтение и длин 1-8 байт, сравнивая адреса на шине бесплатно и работая на read-only страницах. Watchpoint наводит тот же регистровый механизм на данные и делает trap на любой записи по адресу, превращая «какая из тысячи строк портит это поле?» в один trap — разница между 40-минутным циклом с условным брейкпоинтом и 3-секундным ответом. Условные брейкпоинты дороги, ведь они делают trap и переоценку на каждом срабатывании, до 1000x тела горячего цикла, тогда как аппаратный watchpoint остаётся вне цикла и срабатывает лишь на редком событии; попроси пятое слежение — и отладчик деградирует до программной эмуляции по шагу в 100-1000 раз медленнее. Оптимизированные -O2-сборки переставляют, инлайнят, размещают по регистрам и сливают хвосты, поэтому таблица строк lossy по построению — шаги пропускают, строки идут не по порядку, локалы читаются &lt;optimized out&gt;. Воспроизводи под -O0 -g, когда можешь, а когда баг лезет только в оптимизации — шагай по инструкциям и доверяй регистрам, а не номерам строк. Теперь, когда встретишь условный брейкпоинт, ползущий по плотному циклу, или watchpoint, тихо промахивающийся по записям, — ты знаешь, какой механизм поменять и почему это ничего не стоит.
</Recap>
