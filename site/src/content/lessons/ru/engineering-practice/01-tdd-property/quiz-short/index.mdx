---
concepts:
- design-pressure
- boundary-rule
- property-shapes
- spec-stability
- mutation-score
estMin: 14
lang: ru
level: senior
order: 8
prereqs: []
slug: quiz-short
sources:
- https://martinfowler.com/articles/mocksArentStubs.html
- https://hypothesis.works/articles/quickcheck-in-every-language/
- https://stryker-mutator.io/docs/mutation-testing-elements/mutant-states-and-metrics/
status: ready
summary: "Промпты на свободное припоминание по юниту TDD. Сначала ответь своими словами, затем раскрой модельный ответ и сравни."
title: 'TDD: свободное припоминание'
track: engineering-practice
unit: 01-tdd-property
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";

<Hook>
Припоминание сильнее перечитывания. На каждый промпт проговори или запиши полный ответ по памяти, прежде чем открыть модельный — именно усилие припоминания закрепляет материал.
</Hook>

<Goal lang="ru">
Восстанови ключевые аргументы юнита — тест-сначала как давление на дизайн, правило границ для дублёров, четыре формы property, компромисс по стабильности спеки и почему mutation score честная метрика — не подглядывая в уроки.
</Goal>

<RetrievalDrawer
  client:load
  id="01-tdd-property-short-retrieval"
  lang="ru"
  questions={[
    { q: "Коллега говорит: «TDD это просто про поднятие покрытия — напишу тесты после, тот же результат». Каков ответ senior?", a: "Покрытие ты получишь любым способом; отличительное, что даёт тест-сначала — обратная связь по дизайну, и только до того, как код существует. Написание теста первым делает тебя первым потребителем своего API — ты выбираешь имя, аргументы, форму возврата и режим отказа, оставаясь снаружи. Плохой интерфейс (объект, который не сконструировать без четырёх коллабораторов, метод, который не вызвать без живой БД, возврат, который не assert'ить без лазания во внутренности) валит тест сразу, до того как появятся пятнадцать мест вызова, которые все пришлось бы менять. Напишешь тест после — дизайн уже задан; тест лишь ратифицирует ту форму, что ты построил. Сравнительный case study нашёл, что эффект на дизайн был устойчивым сигналом по командам, тогда как эффекты на число багов были шумными. Так что тест-после даёт то же покрытие и нисколько давления на дизайн — что и есть весь смысл делать это сначала." },
    { q: "Сформулируй правило границ для тестовых дублёров и решающий вопрос за ним.", a: "Мокай то, что не можешь прогнать; используй реальные объекты для своего кода. Дублируй то, что медленно, недетерминированно или с необратимыми сайд-эффектами — сеть, часы, платёж, email, сторонние API — стабом или fake, а для быстрых чистых коллабораторов, которыми владеешь, используй реальный объект (или fake), проверяя финальное состояние, а не вызовы. Это разрешает London (mockist) против Detroit (classicist): по сути это позиция classicist с дисциплиной London на границах системы. Решающий вопрос для любого дублёра: если я рефакторю внутренности, не меняя поведение, должен ли этот тест упасть? Если нет — ты используешь mock там, где место стабу или реальному объекту. Over-mocking — это когда ответ «да» для кода, которым ты владеешь — отчего сохраняющий поведение рефакторинг делает 600 тестов красными без единого бага, пока реальная ошибка в цене уезжает зелёной за застабленными значениями." },
    { q: "Назови четыре формы property и ситуацию, к которой подходит каждая.", a: "Round-trip: decode(encode(x)) === x — любая пара serialize/parse, compress/decompress, save/load, и форма, что ловит денежный баг на .005. Invariant: факт, всегда истинный после операции — sort(xs).length === xs.length, выход упорядочен, сумма неотрицательна. Oracle: доверенный эталон согласуется с новым кодом — переписывание против старой функции, быстрый путь против медленного; форма с наибольшим рычагом для любого переписывания, так как обкатанный код задаёт корректность. Metamorphic: отношение между двумя связанными прогонами — результаты search(q) есть надмножество search(q AND x). Слабое property вроде «результат это число» выглядит тщательно, но тестирует почти ничего; сформулировать полезный инвариант — сам по себе акт дизайна. Фреймворк генерирует сотни входов, чтобы атаковать property, затем шринкует любое падение до минимального контрпримера и печатает seed, так что оно воспроизводится детерминированно." },
    { q: "Почему property-тесты находят баги, которые example-набор с 94% покрытия не может, и что делает генерируемые тесты надёжными в CI?", a: "Покрытие измеряет, какие строки прогнались, а не какие входы ты перепробовал, поэтому example-набор гоняет только воображённые человеком случаи — его слепые зоны это ровно твои слепые зоны. Можно прогнать каждую строку на круглых числах и ни разу не попасть в ломающее значение, как денежный форматтер оставался зелёным восемь месяцев, потому что расхождение half-even/half-up появлялось лишь на значениях на .005, которые никто не набирал. Property проверяет инвариант над сгенерированным входом, так что фреймворк швыряет в него граничные значения вроде 0.005 и 2.675 за горстку прогонов. Риск — флакость: property, тайно зависящее от часов, незасеянного random или внешнего состояния, может пройти 999 прогонов и упасть на 1000-м. Дисциплина, что обезвреживает это, двояка — держи property чистым и дай фреймворку владеть всей генерацией, и закрепи напечатанный seed в момент появления флака, чтобы падение было воспроизводимым, а не подбрасыванием монеты. Property-тест, который нельзя воспроизвести по требованию — не тест, а слух." },
    { q: "Какие две переменные определяют, окупается ли TDD, и как это отличает давление на дизайн от test-induced design damage?", a: "Стабильность спеки — можешь ли сформулировать верное поведение до написания кода — и время жизни кода — проживёт ли он месяцы или будет удалён на следующей неделе. Высоко по обоим (биллинг, авторизация, долгоживущее доменное ядро) — пиковая отдача: обратная связь по дизайну сейчас, регрессионная сеть навсегда. Низко по обоим (исследовательский spike, проба вендора, одноразовый скрипт) — и front-loaded стоимость в 15–22% покупает уверенность, которой у тебя нет, на одноразовом коде — поэтому сначала делаешь spike, чтобы превратить неизвестное в познаваемую спеку, выбрасываешь spike, потом перестраиваешь keeper с TDD. Та же сила рождает давление или damage. Давление ценно, когда трудный для написания тест вскрывает реальную проблему связности (god-объект из урока 01). Damage — когда стремление сделать код unit-тестируемым добавляет косвенность, служащую только тесту: лишний слой, интерфейс и mock, разрезание связной вещи ради изоляции. Признак: делает ли исправление того, на что жалуется тест, код лучше для читателя, который тест никогда не видит? Если да — это давление; если изменение существует лишь чтобы удовлетворить тест — это damage." },
    { q: "Как у модуля может быть 100% покрытия строк и 67% mutation score, и как senior внедряет mutation testing с учётом его издержек?", a: "Покрытие спрашивает «прогнал ли тест эту строку?»; mutation score спрашивает «упадёт ли тест, если эта строка неверна?». Они расходятся постоянно — инструмент мутирует 'amount > limit' в 'amount >= limit', строка всё равно прогоняется в каждом тесте, но ни один тест не подал точное граничное значение, где операторы различаются, так что мутант выживает. 67% — честное измерение того, сколько покрытого кода набор не защитил бы; 100% измеряло не то. Внедрение: считай продуктом список выживших мутантов, а не score — каждый выживший это именованный недостающий assertion ('>' → '>=' значит добавь граничный тест, '&&' → '||' значит условие недотестировано, удалённый statement значит сайд-эффект не проверяется). Управляй двумя издержками осознанно: время выполнения, так как набор перезапускается раз на мутант и полные прогоны занимают часы, поэтому ограничь изменёнными файлами в CI (Stryker и PIT оба делают diff-прогоны) или гоняй ночью на критичных модулях; и эквивалентные мутанты, поведенчески идентичные мутации, что не убить ни одним тестом, делающие идеальный score недостижимым и требующие ручного триажа. Цель — убивать значимых выживших на критичном коде — деньги, права, переходы состояний — а не безупречное число везде." },
  ]}
/>

<Recap lang="ru">
Если ты смог восстановить каждый ответ по памяти, ты держишь спину юнита: тест-сначала — это давление на дизайн, потому что ты первым потребляешь свой API; правило границ (мокай то, что не можешь прогнать, реальные объекты для своего кода, проверяй состояние) укрощает дублёров и ловушку over-mocking; четыре формы property — round-trip, invariant, oracle, metamorphic — находят входы, что пропустили твои примеры, а shrinking и seed делают падения отлаживаемыми; налог TDD в 15–22% окупается пропорционально стабильности спеки и времени жизни кода, поэтому делай spike по неизвестному и отличай давление от damage; а mutation score честная метрика, потому что, в отличие от покрытия, его нельзя удовлетворить исполнением без assertion.
</Recap>
