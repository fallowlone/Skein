---
concepts:
- failing-test-first
- fragile-mock-test
- property-based-testing
- mutation-survivor
estMin: 14
lang: ru
level: senior
order: 9
prereqs: []
slug: quiz-code
sources:
- https://martinfowler.com/articles/mocksArentStubs.html
- https://fast-check.dev/docs/introduction/why-property-based/
- https://stryker-mutator.io/docs/mutation-testing-elements/mutant-states-and-metrics/
status: ready
summary: "Читай реальный тестовый код — первый падающий тест, хрупкий mock-тяжёлый тест, property и выжившего мутанта — и выбирай фикс или диагноз, который senior делает первым."
title: 'TDD: чтение кода и тестов'
track: engineering-practice
unit: 01-tdd-property
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Качество тестов оценивается в файле теста, а не в отчёте о покрытии. Читай каждый сниппет как PR коллеги, затем выбери изменение, которое senior сделал бы первым.
</Hook>

<Goal lang="ru">
Натренируй взгляд, с которым приходишь на реальное ревью: замечать тест, что проверяет структуру вместо поведения, писать падающий тест, который ведёт дизайн, формулировать property, что бьёт примеры, и читать, о чём говорит выживший мутант.
</Goal>

## Сниппет 1 — первый падающий тест

```js
// Ведём ещё не написанное правило возврата, тест-сначала.
test("refund for a fully-shipped order is rejected", () => {
  const order = anOrder({ status: "SHIPPED" });
  const result = refundPolicy.evaluate(order);
  expect(result.allowed).toBe(false);
  expect(result.reason).toBe("ALREADY_SHIPPED");
});
```

<Quiz
  id="01-tdd-property-code-1"
  lessonSlug="01-tdd-property"
  lang="ru"
  question="До того как refundPolicy существует, что даёт написание этого теста первым и почему он хорошо сформирован?"
  choices={[
    { label: "Он делает тебя первым потребителем API: ты выбираешь имя evaluate(order), вход (доменный Order) и форму возврата ({allowed, reason}), оставаясь снаружи кода. Он проверяет наблюдаемый исход, поэтому переживает любой рефакторинг, сохраняющий правило верным.", correct: true },
    { label: "Ничего полезного, пока нет реализации — падающий тест это просто ошибка компиляции.", misconception: "Шаг red — несущий. Вызов evaluate(order) до его существования форсирует решения по интерфейсу — имя, аргументы, форму возврата, режим отказа — в самый дешёвый момент, до того как появятся зависящие места вызова." },
    { label: "Лучше проверять, что evaluate() вызывает order.getStatus() и ledger.skip() по порядку.", misconception: "Это привязывает тест к внутренней структуре вызовов — антипаттерн хрупкого теста. Он сломается на безобидном рефакторинге и может остаться зелёным, пока исход возврата неверен. Проверяй исход, а не танец." },
    { label: "Он доказывает, что фича возврата без багов, как только станет зелёной.", misconception: "Один проходящий пример доказывает один случай, а не отсутствие багов. Продукт шага red — обратная связь по дизайну; широкий поиск багов добавят property и mutation testing позже." },
  ]}
/>

## Сниппет 2 — хрупкий тест

```js
test("checkout charges the customer", () => {
  const tax = mock(TaxRule);          // чистый, наш собственный
  const gateway = mock(PaymentGateway); // сторонняя сеть
  when(tax.apply(100)).thenReturn(108);
  checkout.pay(cart, tax, gateway);
  verify(tax.apply(100)).once();      // проверяем, что вызов был
  verify(gateway.charge(108)).once();
});
```

<Quiz
  id="01-tdd-property-code-2"
  lessonSlug="01-tdd-property"
  lang="ru"
  question="Сохраняющий поведение рефакторинг checkout делает этот тест красным, но реальный баг вычисления суммы однажды уехал с ним зелёным. В чём корневая причина?"
  choices={[
    { label: "Это over-mocking: TaxRule — чистый код, которым ты владеешь, поэтому его мок пришпиливает тест к структуре вызовов (падает на рефакторинге), а стаб apply()→108 значит, что реальная налоговая математика не проверяется (баг уезжает зелёным). Используй реальный TaxRule, дублируй только gateway, проверяй итоговую списанную сумму.", correct: true },
    { label: "Библиотека мокинга неверно настроена — почини матчеры verify().", misconception: "Матчеры в порядке; дефект — в дизайне теста. Проверять взаимодействия на собственном чистом коде — это ловушка over-mocking независимо от того, как написаны матчеры." },
    { label: "Конкурентный доступ к моку gateway не потокобезопасен.", misconception: "Конкурентность тут ни при чём. Тест падает на рефакторинге, потому что проверяет, какие вызовы случились, и пропускает баги, потому что застабил результат, который должен был вычислить." },
    { label: "Тесту нужно больше покрытия, чтобы поймать баг суммы.", misconception: "Покрытие тут неважно — строка прогналась. Баг проскочил, потому что проверяемая сумма была застабленной константой (108), а не реальным вычислением. Проверяй вычисленное состояние с реальным правилом." },
  ]}
/>

## Сниппет 3 — property

```js
// fast-check: переписываем обкатанный CSV-парсер, oldParse — доверенный эталон.
fc.assert(fc.property(fc.string(), (raw) => {
  expect(newParse(raw)).toEqual(oldParse(raw)); // oracle property
}), { numRuns: 1000 });
```

<Quiz
  id="01-tdd-property-code-3"
  lessonSlug="01-tdd-property"
  lang="ru"
  question="Какое утверждение об этом property верно?"
  choices={[
    { label: "Это oracle-property: доверенный старый парсер задаёт корректность, поэтому любой сгенерированный вход, где новый расходится, валит прогон и шринкуется до наименьшей расходящейся строки — тебе не надо самому перечислять граничные случаи.", correct: true },
    { label: "Оно лишь доказывает, что newParse и oldParse самосогласованы, как round-trip.", misconception: "Round-trip проверяет parse∘serialize против тождества и может пройти, пока оба ошибаются одинаково. Oracle сравнивает с независимым доверенным эталоном, поэтому ловит расхождения, которые round-trip не может." },
    { label: "1000 прогонов делают его детерминированным, так что падение нельзя воспроизвести.", misconception: "Воспроизводимость даёт напечатанный seed, а не число прогонов. Перезапусти с seed — и повторишь точный падающий случай; numRuns лишь задаёт, насколько жёстко атакует генератор." },
    { label: "fc.string() слишком узок — он не может сгенерировать CSV-кейсы вроде запятых в кавычках.", misconception: "fc.string() генерирует произвольные строки, включая кавычки, запятые и переводы строк — именно те ужасы с запятой-в-кавычках и встроенным переводом строки, что ломают реальные CSV-парсеры. Эта широта и есть смысл." },
  ]}
/>

## Сниппет 4 — выживший мутант

```js
// Продакшн-код под mutation testing:
function withinBudget(amount, limit) {
  return amount <= limit;   // Stryker мутировал в: amount < limit
}
// Полный набор всё равно прошёл после мутации. Мутант ВЫЖИЛ.
```

<Quiz
  id="01-tdd-property-code-4"
  lessonSlug="01-tdd-property"
  lang="ru"
  question={"Мутант '<=' → '<' выжил при 100% покрытия строк. О чём это говорит и какой фикс?"}
  choices={[
    { label: "Ни один тест не подал amount === limit — единственное значение, где '<=' и '<' различаются. Выживший — это именованный недостающий assertion: добавь граничный тест, проверяющий withinBudget(limit, limit) === true. Это самый частый класс реальных багов — off-by-one на границе.", correct: true },
    { label: "Мутант эквивалентен — два оператора поведенчески идентичны, так что игнорируй.", misconception: "Они различаются ровно при amount === limit. У эквивалентного мутанта нет входа, который его отличает; у этого есть, так что это реальный пробел, а не шум для отбрасывания." },
    { label: "Строка не была покрыта, потому ничего и не упало.", misconception: "Предпосылка — 100% покрытия строк, строка прогонялась каждый раз. Он выжил, потому что ни один assertion не различил граничный случай, что покрытие как раз и не может измерить." },
    { label: "Поднять целевое покрытие, чтобы форсировать граничный тест.", misconception: "Покрытие уже максимально и всё равно это пропустило. Фикс — конкретный assertion при amount === limit; гонка за числом покрытия, что уже 100%, ничего не меняет." },
  ]}
/>

<Recap lang="ru">
Каждый тест в этом юните читается одинаково. Первый падающий тест — это обратная связь по дизайну: он фиксирует интерфейс и проверяет наблюдаемый исход, поэтому переживает рефакторинги. Хрупкий тест over-mock'ает собственный чистый код: падает на рефакторинге и отгружает баги зелёным за застабленными результатами — используй реальные объекты и проверяй состояние. Oracle-property даёт доверенному эталону задавать корректность над сгенерированным входом и шринкует до минимального контрпримера. А выживший мутант — это именованный недостающий assertion, чаще всего граница, которую набор никогда не подал. Диагностируй по тесту, чини assertion, перезапускай для подтверждения, что мутант убит.
</Recap>
