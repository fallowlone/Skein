---
concepts:
- mutation-testing
- mutation-score
- surviving-mutants
- coverage-is-not-quality
- stryker-pit
- equivalent-mutants
deepensInto: []
estMin: 16
lang: ru
lessonType: topic
level: senior
order: 5
prereqs:
- 04-when-tdd-hurts
slug: 05-mutation-testing
sources:
- https://stryker-mutator.io/docs/mutation-testing-elements/mutant-states-and-metrics/
- https://stryker-mutator.io/docs/
- https://pitest.org/
- https://learn.microsoft.com/en-us/dotnet/core/testing/mutation-testing
spiral:
- edge-cases
- feedback-loops
status: ready
summary: Покрытие строк говорит, что строка выполнилась, а не что тест заметит её
  поломку. Мутационное тестирование внедряет баги и проверяет, убивает ли их набор
  — можно достичь 100% покрытия с мутационным баллом 67%. Выжившие мутанты — это проверки,
  которых вам не хватает.
title: Мутационное тестирование — честная метрика качества тестов
track: engineering-practice
unit: 01-tdd-property
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
Команда требует 100% покрытия строк и добивается его. Руководство успокоено. Затем кто-то запускает Stryker на том же модуле, и мутационный балл возвращается равным **67%** — треть внедрённых багов выжила. Худший выживший: Stryker сменил граничную проверку с `amount > limit` на `amount >= limit`, прогнал весь набор, и каждый тест всё равно прошёл. Набор выполнял эту строку в каждом прогоне — покрытие 100% — но ни один тест ни разу не подал ровно то граничное значение, на котором `>` и `>=` различаются. Число покрытия измеряло, что строка *выполнилась*, но никогда — что тест *заметит её поломку*. Это не одно и то же, и зазор составил треть кода.
</Hook>

<Crux>Покрытие доказывает, что строка выполнилась; мутационное — что тест упал бы, будь она неверной. 100% покрытия могут скрыть балл 67%.</Crux>

<Explanation>

## Покрытие измеряет выполнение, а не обнаружение

Покрытие строк отвечает на слабый вопрос: вызвал ли тест выполнение этой строки? Оно ничего не говорит о том, *упадёт* ли хоть один тест, будь строка неверной. Этот зазор не академический — это разница между набором, который вас защищает, и набором, который лишь навещает ваш код. Можно вызвать функцию в тесте, выполнить в ней каждую строку и не проверить ничего осмысленного о результате; покрытие засчитает это как покрытое, и реальный баг на этой строке уедет в продакшен зелёным. Высокое покрытие почти необходимо, но радикально недостаточно: это пол «тест коснулся этого», а не потолок «тест стережёт это».

**Мутационное тестирование** измеряет то, что покрытие не может. Инструмент берёт ваш проходящий набор, вносит маленькое изменение в продакшен-код — **мутанта** — и перезапускает тесты. Меняет `>` на `>=`, `+` на `-`, `&&` на `||`, `return x` на `return null`, удаляет инструкцию. Если теперь тест падает, мутант **убит** — ваш набор обнаружил внедрённый баг. Если все тесты всё равно проходят, мутант **выжил** — ваш набор не заметил бы ровно эту поломку. **Мутационный балл** — это убитые ÷ (всего валидных мутантов), и, в отличие от покрытия, это прямое измерение силы обнаружения. Stryker (JS/TS, C#, Scala) и PIT (JVM) — стандартные инструменты; документация Stryker точно определяет состояния убит/выжил/таймаут/без-покрытия.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Метрика</th>
        <th class="border border-border px-3 py-2 text-left">На какой вопрос отвечает</th>
        <th class="border border-border px-3 py-2 text-left">Что упускает</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">Покрытие строк</td>
        <td class="border border-border px-3 py-2">Прогнал ли тест эту строку?</td>
        <td class="border border-border px-3 py-2">Проверяет ли хоть один тест поведение этой строки</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Покрытие ветвей</td>
        <td class="border border-border px-3 py-2">Выполнилась ли каждая ветвь?</td>
        <td class="border border-border px-3 py-2">Тестировалось ли граничное значение, различающее ветви</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Мутационный балл</td>
        <td class="border border-border px-3 py-2">Упал бы тест, будь этот код неверным?</td>
        <td class="border border-border px-3 py-2">Мало что — но он медленный и шумит эквивалентными мутантами</td>
      </tr>
    </tbody>
  </table>
</div>

## Выжившие мутанты — это ваши недостающие проверки, названные поимённо

Важен не балл, а *список выживших мутантов*. Когда вы читаете этот список, каждый выживший — точное, действенное утверждение: «я изменил вот это, а твоим тестам всё равно». Граничный выживший — `>` стал `>=`, и ничего не упало — прямо говорит вам, что не хватает теста на граничном значении, самого частого класса реальных багов. Выживший `&&`→`||` говорит, что логическое условие недотестировано. Выжившее удаление инструкции говорит, что побочный эффект никогда не проверяется. Это и есть настоящий продукт мутационного тестирования: оно превращает «ваши тесты где-то слабы» в «добавь проверку здесь, на этой строке, для этого случая». Это честный ревьюер, который читает ваши проверки, а не отчёт о покрытии.

Окупается оно ровно там, где покрытие лжёт сильнее всего: критическая логика с границами, деньгами, правами, переходами состояний. Его запуск всплывает тесты, которые вы бы написали, додумайся вы до случая — это та же проблема слепых пятен, которую property-тестирование атакует со стороны входов. Эти два подхода дополняют друг друга: property-тесты генерируют входы, которые вы не перечислили; мутационное тестирование находит выходы, которые вы не проверили. Оба существуют потому, что покрытие строк измеряет не то.

## Цена: время CPU и эквивалентные мутанты

Мутационное тестирование не бесплатно, и из-за цены команды его дозируют, а не гоняют на всём. Главная цена — **время**: инструмент перезапускает (подмножество) вашего набора по разу на мутанта, поэтому модуль с сотнями мутантов прогоняет ваши тесты сотни раз. Прогон мутаций по всей кодовой базе может занять часы, что неприемлемо на каждом коммите. Практический шаблон — дозировать: гонять на изменённых файлах в CI (Stryker и PIT оба поддерживают инкрементальные/diff-прогоны) или прогонять полный проход ночью на критических модулях, а не весь репозиторий на каждый push.

Вторая цена — проблема **эквивалентного мутанта**. Некоторые мутации дают код, поведенчески идентичный оригиналу — например, мутация `i < n` в `i != n` в цикле, который всегда инкрементирует на единицу, где оба завершаются одинаково. Ни один тест не может убить эквивалентного мутанта, потому что обнаруживать нечего, но он засчитывается против вашего балла и требует человеческого суждения, чтобы его отбросить. Обнаружение эквивалентных мутантов в общем случае неразрешимо, поэтому вы разбираете их вручную. Это и есть честная сеньорская оговорка: мутационный балл ниже 100% ожидаем, выживших надо читать, а не слепо гнаться за ними, и цель — убить *значимых* выживших на *критическом* коде, а не идеальное число на всём.

<Inset kind="why" lang="ru">
Мутационное тестирование честная метрика потому, что оно тестирует тесты той же валютой, что и реальный баг: неверный оператор, перевёрнутое условие, удалённый побочный эффект. Инструмент покрытия удовлетворяется одним лишь выполнением, поэтому его можно обмануть — можно гнаться за 100%, вызывая код без проверок, и число растёт, тогда как набор сильнее не становится. Мутационный балл так не обмануть, потому что единственный способ убить мутанта — иметь проверку, которая реально отличает корректное поведение от внедрённого неверного. Вот почему выживший мутант ценнее процента покрытия: это не статистика о вашем коде, это конкретный баг, который ваш набор только что доказательно пропустил бы.
</Inset>

<TradeoffMatrix
  id="05-mutation-testing-tradeoff-1"
  lessonSlug="05-mutation-testing"
  lang="ru"
  prompt="У вашего биллинг-модуля 100% покрытия строк, но Stryker сообщает мутационный балл 67%, с выжившим граничным мутантом '>' → '>='. Какой ответ правильный?"
  options={[
    { name: "Добавить тест на точное граничное значение, затем приоритетно убивать значимых выживших на критическом коде", summary: "Читайте выживших.", correct: true, justification: "Выживший мутант '>' → '>=' — это названная недостающая проверка: ни один тест не подал граничное значение, где два оператора различаются, а это самый частый класс реальных багов в денежной логике. Добавьте этот тест, затем пройдитесь по списку выживших, убивая тех, кто на критических путях. Продукт мутационного тестирования — этот действенный список, а не балл — он точно говорит, какую проверку добавить и где." },
    { name: "Добавлять тесты, пока покрытие строк снова не покажет 100% — оно уже показывает, значит вы закончили", summary: "Доверять покрытию.", justification: "Покрытие уже 100%, а набор всё равно пропускает треть внедрённых багов; в этом весь смысл урока. Покрытие измеряет выполнение, а не обнаружение, поэтому оно не видит недостающую граничную проверку. Гнаться за числом, которое уже на максимуме, ничего не чинит." },
    { name: "Поднять мутационный балл до 100% по всему репозиторию перед релизом", summary: "Гнаться за идеальным баллом.", justification: "Идеальный балл — неверная цель. Эквивалентные мутанты — поведенчески идентичные мутации, которых не убить ни одним тестом — делают балл ниже 100% ожидаемым, а прогоны по всему репозиторию занимают часы. Сеньорская цель — убить значимых выживших на критических модулях и разобрать эквивалентов вручную, а не безупречное число везде." },
    { name: "Отключить мутационное тестирование — 100% покрытия и есть согласованный гейт качества", summary: "Игнорировать сигнал.", justification: "Так вы выбросите единственную метрику, которая только что доказала, что ваш гейт пустой. Балл 67% против 100% покрытия — ровно то доказательство, что гейт по покрытию не измеряет обнаружение. Выключить честную метрику, чтобы сберечь обманчивую — ровно наоборот, чем надо." },
  ]}
/>

<Quiz
  id="05-mutation-testing-quiz-1"
  lessonSlug="05-mutation-testing"
  lang="ru"
  question="Как возможно иметь 100% покрытия строк и мутационный балл 67% на одном и том же коде?"
  choices={[
    { label: "Покрытие лишь доказывает, что строки выполнились; мутант вроде '>' → '>=' выживает, если ни один тест не подал граничное значение, их различающее, даже хотя строка выполнялась в каждом прогоне", correct: true },
    { label: "Инструмент покрытия наверняка считает неправильно — эти числа противоречивы", misconception: "Они не противоречивы; они измеряют разное. Покрытие измеряет выполнение, мутационный балл — обнаружение. Строка может прогоняться в каждом тесте, при этом ни один тест не проверяет поведение, которое сломала бы мутация." },
    { label: "Мутационный балл 67% значит, что 33% кода недостижимы", misconception: "Выжившие мутанты достижимы и выполнены — поэтому зазор и тревожен. Недостижимый код всплывает как мутанты 'без покрытия', отдельная категория от 'выживших'." },
    { label: "Мутационное тестирование — просто более шумная версия покрытия ветвей", misconception: "Оно категорически иное: оно внедряет неверное поведение и проверяет, ловят ли его ваши проверки. Покрытие ветвей всё равно лишь измеряет, выполнились ли ветви, а не упал бы ли тест на неверном результате." },
  ]}
/>

<Quiz
  id="05-mutation-testing-quiz-2"
  lessonSlug="05-mutation-testing"
  lang="ru"
  question="Почему сеньору не стоит гнаться за мутационным баллом 100% по всему репозиторию?"
  choices={[
    { label: "Эквивалентных мутантов не убить (нет поведенческого различия), а полные прогоны занимают часы — поэтому цель в том, чтобы убивать значимых выживших на критическом коде, дозируя через diff/ночные прогоны", correct: true },
    { label: "Потому что мутационное тестирование ненадёжно и его баллы случайны", misconception: "Мутационный балл детерминирован при том же наборе и мутантах; он не случаен. Причина не гнаться за 100% — эквивалентные мутанты и стоимость прогона, а не ненадёжность." },
    { label: "Потому что мутационный балл 100% гарантирует, что в коде нет багов, делая лишние тесты бессмысленными", misconception: "Ни одна метрика не гарантирует отсутствия багов. Мутационное тестирование находит недостающие проверки для мутаций, которые оно пробует; оно не доказывает корректность, и идеальный балл всё равно недостижим из-за эквивалентных мутантов." },
    { label: "Потому что выжившие мутанты всегда эквивалентны и их безопасно игнорировать", misconception: "Большинство выживших — реальные пробелы, которые надо чинить — граничный мутант это подлинно недостающий тест. Лишь часть эквивалентна; вы разбираете, чтобы отделить реальных выживших от эквивалентных." },
  ]}
/>

<DragOrder
  id="05-mutation-testing-drag-1"
  lessonSlug="05-mutation-testing"
  lang="ru"
  prompt="Упорядочьте, как мутационное тестирование вскрывает и закрывает пробел в тестах:"
  items={[
    { id: "a", label: "Ваш набор полностью зелёный с 100% покрытия строк" },
    { id: "b", label: "Инструмент внедряет мутанта: меняет '>' на '>=' в граничной проверке" },
    { id: "c", label: "Он перезапускает набор; каждый тест всё равно проходит — мутант выживает" },
    { id: "d", label: "Выживший называет пробел: ни один тест не подаёт граничное значение" },
    { id: "e", label: "Добавляете граничную проверку; перезапуск — и мутант теперь убит" },
  ]}
  correctOrder={["a", "b", "c", "d", "e"]}
/>

</Explanation>

<FlowDiagram
  label="Мутационное тестирование: внедрить мутанта, прогнать набор, убит или выжил, улучшить тесты"
  hue="var(--d-systems)"
  perRow={3}
  nodes={[
    {id:"suite",label:"зелёный набор",sub:"покрытие 100%"},
    {id:"mutant",label:"внедрить мутанта",sub:"> → >=, + → -"},
    {id:"run",label:"прогнать набор",sub:"тесты vs мутант"},
    {id:"killed",label:"убит",sub:"тест упал — хорошо"},
    {id:"survived",label:"выжил",sub:"ни один не заметил"},
    {id:"improve",label:"добавить проверку",sub:"закрыть пробел"}
  ]}
  edges={[
    {from:"suite",to:"mutant"},
    {from:"mutant",to:"run"},
    {from:"run",to:"killed",label:"тест падает"},
    {from:"run",to:"survived",label:"все прошли"},
    {from:"survived",to:"improve",label:"назван пробел"},
    {from:"improve",to:"suite",label:"набор сильнее"}
  ]}
  caption="Инструмент внедряет мутанта (> в >=, + в - и т.д.) и перезапускает набор. Падение теста убивает мутанта — хорошо. Если все тесты проходят, мутант выживает, называя точно недостающую проверку. Добавление её закрывает пробел, и цикл повторяется."
/>

<KeyTakeaway>Покрытие строк доказывает, что строка выполнилась; мутационное тестирование доказывает, что тест упал бы, будь эта строка неверной — отсюда 100% покрытия при мутационном балле 67%. Каждый выживший мутант — это названная недостающая проверка. Дозируйте его (diff/ночью на критическом коде), потому что прогоны медленные, а эквивалентные мутанты делают идеальный балл невозможным.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="05-mutation-testing-retrieval"
  lang="ru"
  questions={[
    { q: "Руководство доверяет гейту 100% покрытия. Объясните, почему мутационное тестирование, сообщающее 67% на том же коде, не противоречие.", a: "Покрытие и мутационный балл отвечают на разные вопросы. Покрытие строк спрашивает: вызвал ли тест выполнение этой строки? Мутационный балл спрашивает: упал бы тест, будь эта строка неверной? Эти вещи постоянно расходятся. Канонический случай — граничная проверка: инструмент мутирует 'amount > limit' в 'amount >= limit', перезапускает весь набор, и каждый тест проходит — при этом строка выполнялась в каждом прогоне, так что покрытие 100%. Мутант выжил, потому что ни один тест ни разу не подал ровно то граничное значение, где '>' и '>=' различаются, а это самый частый класс реальных багов. Так что 100% покрытия при мутационном балле 67% — не противоречие; это точное измерение того, какую долю покрытого кода ваш набор не сумел бы защитить. Покрытие можно обмануть одним выполнением — можно вызывать код без проверок, и число растёт — а мутационный балл может вырасти только при наличии проверок, отличающих корректное поведение от неверного. 67% — честное число; 100% измеряло не то." },
    { q: "Как сеньору на деле внедрять мутационное тестирование с учётом его цены?", a: "Считайте продуктом список выживших мутантов, а не балл. Каждый выживший — названная, действенная недостающая проверка: выживший '>' → '>=' значит добавь сюда граничный тест; выживший '&&' → '||' значит условие недотестировано; выжившее удаление инструкции значит побочный эффект никогда не проверяется. Приоритетно убивайте значимых выживших на критическом коде — деньги, права, переходы состояний — потому что там покрытие лжёт сильнее всего. Управляйте двумя ценами осознанно: временем прогона, ведь инструмент перезапускает набор по разу на мутанта, а проходы по всему репозиторию занимают часы, поэтому дозируйте до изменённых файлов в CI (Stryker и PIT оба делают diff-прогоны) или прогоняйте полный проход ночью только на критических модулях; и эквивалентными мутантами — поведенчески идентичными мутациями, которых не убить ни одним тестом, что делает идеальный балл недостижимым и требует ручного разбора. Цель — не 100% везде, а убить выживших, представляющих реальные пробелы, на коде, который важен, используя мутационное тестирование как дополнение к property-тестам: свойства генерируют входы, которые вы не перечислили, мутационное тестирование находит выходы, которые вы не проверили." },
  ]}
/>

<Recap lang="ru">
Покрытие строк отвечает на слабый вопрос — прогнал ли тест эту строку — и ничего не говорит о том, упал бы ли хоть один тест, будь строка неверной, поэтому набор может достичь 100% покрытия и всё равно пропустить треть внедрённых багов. Мутационное тестирование измеряет обнаружение напрямую: оно внедряет маленькие изменения (мутант — сменить '>' на '>=', '+' на '-', '&&' на '||', удалить инструкцию), перезапускает набор и сообщает мутанта убитым, если тест падает, или выжившим, если все проходят; мутационный балл — это убитые от всего числа валидных мутантов, а Stryker и PIT — стандартные инструменты. Реальный выход — список выживших, каждый из которых названная недостающая проверка — граничный выживший '>' → '>=' точно говорит, где добавить тест, для самого частого класса реальных багов. Окупается оно ровно там, где покрытие лжёт сильнее всего: границы, деньги, права, переходы состояний, и оно дополняет property-тестирование, которое атакует то же слепое пятно со стороны входов. Цена настоящая: время прогона, ведь набор перезапускается по разу на мутанта, а полные проходы занимают часы, поэтому дозируйте его до diff'ов в CI или ночью на критических модулях; и эквивалентные мутанты — поведенчески идентичные мутации, которых не убить ни одним тестом, что делает идеальный балл невозможным и требует ручного разбора. Сеньорская цель — убить значимых выживших на критическом коде, а не безупречное число на всём — мутационный балл честная метрика потому, что, в отличие от покрытия, его не удовлетворить выполнением без проверки. Теперь, когда вы видите высокое покрытие на биллинговом или разрешительном модуле, следующий вопрос — что показывает мутационный балл, и какие выжившие называют проверки, которых вам всё ещё не хватает?
</Recap>
