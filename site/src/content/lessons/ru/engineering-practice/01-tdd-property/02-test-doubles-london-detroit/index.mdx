---
concepts:
- test-doubles
- mockist-vs-classicist
- london-detroit-schools
- over-mocking
- interaction-vs-state
- test-boundaries
deepensInto:
- 03-property-based-testing
estMin: 16
lang: ru
lessonType: topic
level: middle
order: 2
prereqs:
- 01-rgr-design-loop
slug: 02-test-doubles-london-detroit
sources:
- https://martinfowler.com/articles/mocksArentStubs.html
- https://medium.com/@adrianbooth/test-driven-development-wars-detroit-vs-london-classicist-vs-mockist-9956c78ae95f
- http://xunitpatterns.com/Test%20Double.html
- https://martinfowler.com/bliki/UnitTest.html
spiral:
- feedback-loops
- design-pressure
status: ready
summary: 'Mockist-тесты (London) проверяют взаимодействия; classicist-тесты (Detroit)
  — состояние на настоящих коллабораторах. Мокайте границы — сеть, часы, оплату —
  внутри берите настоящие объекты. Чрезмерный мок привязывает тесты к структуре: ломаются
  на рефакторинге, зелены на багах.'
title: 'Тестовые дублёры: London против Detroit и ловушка чрезмерного мокинга'
track: engineering-practice
unit: 01-tdd-property
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
Команда мокает всё. Каждый коллаборатор в каждом юнит-тесте — это мок с `expect(...).toHaveBeenCalledWith(...)`. Их набор — это 2400 тестов, все зелёные, 91% покрытия. Затем рефакторинг, разбивающий один сервис на два — чисто внутренняя реорганизация, поведение идентично, — за один день красит **600 тестов в красный**. Ни один из них не поймал баг; они поймали лишь факт, что внутренние вызовы переместились. Хуже того, месяцем ранее реальный баг в ценообразовании уехал в продакшен с полностью зелёным набором, потому что моки возвращали заготовленные значения и никто не проверял реально вычисленную сумму. Всё это время набор измерял не то.
</Hook>

<Crux>Мокайте то, что нельзя запустить — сеть, часы, оплату; берите настоящие объекты. Мок своих коллабораторов привязывает тесты к структуре.</Crux>

<Explanation>

## Пять видов дублёра и тот, что кусается

«Мок» используют как универсальный ярлык, но xUnit Test Patterns называет пять различных **тестовых дублёров**, и это различие решает, будет ли тест надёжным или хрупким. **Dummy** (пустышка) — это заглушка, которую передают, но никогда не используют. **Stub** (стаб) возвращает заготовленные ответы на вызовы — он подаёт состояние внутрь. **Fake** (фейк) — это рабочая облегчённая реализация (репозиторий в памяти вместо Postgres). **Spy** (шпион) записывает, как его вызывали, чтобы вы могли проинспектировать это после. **Mock** (мок) запрограммирован *ожиданиями*: он утверждает, что конкретные вызовы случились, конкретным образом, и валит тест, если их не было.

Первые четыре подают входы или наблюдают выходы; **мок** — единственный, кто утверждает *взаимодействия*, и именно отсюда берётся хрупкость. Мок вшивает структуру вызовов продакшен-кода в условие прохождения/падения теста. В тот миг, когда вы меняете, *как* код достигает результата — даже при идентичном наблюдаемом поведении, — ожидания мока больше не совпадают, и тест падает. Стабы и фейки так не делают; они просто подают данные и дают вам проверить конечное состояние. Хвататься за мок там, где хватило бы стаба, — это самый частый способ, которым команды производят хрупкие тесты.

## London (mockist) против Detroit (classicist)

Эти две школы — реальное, поимённо названное разногласие. Школа **London / mockist** работает снаружи-внутрь: юнит — это один класс, вы мокаете всех его коллабораторов и тестируете, утверждая *взаимодействия* между объектами — что нужные сообщения были посланы в нужном порядке. Школа **Detroit / classicist** (также Chicago) работает изнутри-наружу: юнит — это поведение, которое может охватывать несколько настоящих объектов, вы используете настоящих коллабораторов везде, где можете, мокаете лишь то, что обязаны, и утверждаете *конечное состояние*, а не вызовы.

Практическое следствие — это то, что переживает рефакторинг. Classicist-тесты утверждают результаты, поэтому они позволяют беспощадный рефакторинг — вы можете свободно реструктурировать внутренности, и тест остаётся зелёным, пока результат верен. Mockist-тесты утверждают структуру вызовов, поэтому они фиксируют реализацию: они точно ловят замысел дизайна, но ломаются всякий раз, когда меняется внутреннее взаимодействие. Сила London — быстрая обратная связь по дизайну снаружи-внутрь и крошечные изолированные юниты; её режим отказа — это ровно тот день с 600 красными тестами: набор, настолько привязанный к структуре, что рефакторинг становится непомерно дорогим.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Аспект</th>
        <th class="border border-border px-3 py-2 text-left">London / mockist</th>
        <th class="border border-border px-3 py-2 text-left">Detroit / classicist</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">Юнит = </td>
        <td class="border border-border px-3 py-2">Один класс</td>
        <td class="border border-border px-3 py-2">Поведение через настоящих коллабораторов</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Коллабораторы</td>
        <td class="border border-border px-3 py-2">Замоканы</td>
        <td class="border border-border px-3 py-2">Настоящие везде, где возможно</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Проверяет</td>
        <td class="border border-border px-3 py-2">Взаимодействия (какие вызовы случились)</td>
        <td class="border border-border px-3 py-2">Конечное состояние / результат</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Переживает рефакторинг?</td>
        <td class="border border-border px-3 py-2">Часто ломается — привязан к структуре вызовов</td>
        <td class="border border-border px-3 py-2">Да — ломается только при смене поведения</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Режим отказа</td>
        <td class="border border-border px-3 py-2">Хрупкий набор, дорогие рефакторинги</td>
        <td class="border border-border px-3 py-2">Труднее локализовать падение</td>
      </tr>
    </tbody>
  </table>
</div>

## Правило границ разрешает спор

Синтез сеньора — это не «выберите школу». Это правило границ: **мокайте то, что не можете запустить, используйте настоящие объекты для того, что можете.** Мокайте вещи медленные, недетерминированные или с побочными эффектами, которые нельзя откатить — сеть, часы, платёжный процессор, отправитель писем, сторонний API. Для коллабораторов, которыми вы *владеете* и которые работают быстро и чисто, используйте настоящий объект или фейк; проверка конечного состояния поверх настоящих внутренних объектов даёт вам тест, который ломается только тогда, когда ломается поведение. Это в основном позиция classicist с дисциплиной London, применённой на краях системы, и это растворяет ловушку чрезмерного мокинга: вы никогда не мокаете объект-значение или чистый хелпер, потому что не от чего изолировать.

Решающий вопрос для любого дублёра: *если я рефакторю внутренности, не меняя поведения, должен ли этот тест упасть?* Если ответ «нет», вы используете мок там, где место стабу или настоящему объекту. Чрезмерный мокинг — это когда ответ «да» для тестов кода, которым вы управляете, — и это ровно то, что превращает зелёный набор в 600 красных тестов, не поймавших ни одного бага, пока реальная ошибка в ценообразовании просочилась, потому что моки возвращали заготовленные числа, которые никто никогда не сверял с реальным вычислением.

<Inset kind="why" lang="ru">
Чрезмерный мокинг ощущается продуктивным, потому что делает каждый тест быстрым, изолированным и тривиально детерминированным — вы управляете каждым входом. Но этот контроль и есть ловушка: тест, где вы застабили каждого коллаборатора, в пределе — это тест того, что код вызывает методы, которые вы велели ему вызвать. Он не может поймать интеграционный баг между двумя настоящими объектами и не может поймать неверный результат, если вы застабили результат. Моки превращают тест в зеркало реализации, поэтому он отражает каждое структурное изменение как падение и не отражает ничего из поведенческой правды. Настоящие коллабораторы медленнее и труднее в настройке, но они — единственный способ, которым тест может разойтись с кодом.
</Inset>

<TradeoffMatrix
  id="02-test-doubles-london-detroit-tradeoff-1"
  lessonSlug="02-test-doubles-london-detroit"
  lang="ru"
  prompt="Вы тестируете PriceCalculator, который использует объект TaxRule (чистый, ваш собственный) и CurrencyApi (сторонний HTTP). Как продублировать каждый?"
  options={[
    { name: "Использовать настоящий TaxRule, застабить/зафейкать CurrencyApi, проверить итоговую вычисленную цену", summary: "Правило границ.", correct: true, justification: "TaxRule — это чистый код, которым вы владеете, и он работает быстро — используйте его по-настоящему, чтобы тест реально прогонял налоговую математику и переживал рефакторинги. CurrencyApi — это сетевая граница, медленная и недетерминированная — застабьте её фиксированным курсом. Затем проверяйте итоговую вычисленную цену (состояние), а не то, какие методы сработали. Тест падает только когда цена неверна, а это ровно то поведение, которое вас волнует." },
    { name: "Замокать и TaxRule, и CurrencyApi, проверить, что каждый вызван с правильными аргументами", summary: "Замокать всё.", justification: "Мокать TaxRule — это чрезмерный мокинг: это чистый код, которым вы владеете, так что не от чего изолировать, а проверка его вызовов привязывает тест к внутренней структуре PriceCalculator. Отрефакторите, как калькулятор собирает налог, и тест покраснеет без всякого бага. Хуже того, застабив результат налога, вы никогда не проверяете реальную математику — реальный баг ценообразования может уехать зелёным." },
    { name: "Использовать настоящие объекты для обоих, включая живой вызов CurrencyApi", summary: "Без дублёров.", justification: "Настоящий TaxRule — это верно, но живой CurrencyApi делает тест медленным, флакающим и зависящим от доступности третьей стороны и от курса, который меняется — тест может падать по причинам, не связанным с вашим кодом. Сетевые границы — это ровно то, что вы обязаны дублировать; здесь вы теряете детерминизм без всякой пользы." },
    { name: "Застабить TaxRule, чтобы возвращал фиксированный налог, использовать настоящий CurrencyApi", summary: "Наоборот.", justification: "Это правило границ, перевёрнутое наизнанку. Стабая чистое налоговое правило, вы выбрасываете математику, которую больше всего хотите протестировать, а живой сетевой вызов возвращает флакающесть и медлительность, которые вы должны были устранить. Вы продублировали то, чем владеете, и доверились тому, чем не владеете." },
  ]}
/>

<Quiz
  id="02-test-doubles-london-detroit-quiz-1"
  lessonSlug="02-test-doubles-london-detroit"
  lang="ru"
  question="Чем мок отличается от стаба и почему это важно для хрупкости?"
  choices={[
    { label: "Мок утверждает, что конкретные взаимодействия случились (и падает, если нет); стаб лишь возвращает заготовленные данные — поэтому моки привязывают тест к структуре вызовов кода", correct: true },
    { label: "Мок быстрее стаба, потому что предкомпилирован", misconception: "Скорость — не различие. Разница поведенческая: стаб подаёт входы, мок утверждает, что вызовы случились определённым образом — и эта проверка вызовов и есть то, что привязывает тест к реализации." },
    { label: "Это взаимозаменяемые термины для одного и того же", misconception: "xUnit Test Patterns называет пять различных дублёров. Смешивание мока и стаба — это ровно то, как команды хватаются за проверки взаимодействий там, где проверки состояния были бы надёжны." },
    { label: "Стаб валит тест на неожиданных вызовах; мок их игнорирует", misconception: "Всё наоборот. У мока есть предзапрограммированные ожидания, и он падает на неверных вызовах; стаб просто возвращает свой заготовленный ответ, как бы его ни вызывали." },
  ]}
/>

<Quiz
  id="02-test-doubles-london-detroit-quiz-2"
  lessonSlug="02-test-doubles-london-detroit"
  lang="ru"
  question="Чисто рефакторинговое разбиение одного сервиса на два красит 600 мок-нагруженных тестов в красный, ни один не ловит баг. В чём корневая причина?"
  choices={[
    { label: "Тесты проверяют взаимодействия, поэтому привязаны к структуре вызовов и ломаются при смене внутреннего взаимодействия, хотя поведение не менялось", correct: true },
    { label: "Рефакторинг наверняка внёс смену поведения, которую тесты верно поймали", misconception: "По условию поведение идентично и ни один не поймал баг. Краснота структурная — моки ожидали вызовы, которые теперь после разбиения происходят иначе, а не реальную регрессию." },
    { label: "У тестов было слишком мало покрытия, чтобы быть надёжными", misconception: "Покрытие было высоким (91%); дело не в этом. Проблема в виде проверки: моки на взаимодействиях ломаются на рефакторинге независимо от того, сколько кода они покрывают." },
    { label: "Моки всегда неправильны и никогда не должны использоваться", misconception: "Моки верны на настоящих границах (сеть, часы, оплата). Провал здесь — чрезмерный мокинг коллабораторов, которыми команда владеет, а не мокинг как таковой." },
  ]}
/>

<DragOrder
  id="02-test-doubles-london-detroit-drag-1"
  lessonSlug="02-test-doubles-london-detroit"
  lang="ru"
  prompt="Упорядочьте, как деградирует зелёный, но бесполезный чрезмерно замоканный набор:"
  items={[
    { id: "a", label: "Каждый коллаборатор замокан, включая чистые объекты, которыми вы владеете" },
    { id: "b", label: "Тесты проверяют, какие вызовы случились, а не итоговый результат" },
    { id: "c", label: "Застабленные возвращаемые значения означают, что реальное вычисление никогда не проверяется" },
    { id: "d", label: "Реальный баг уезжает в прод с полностью зелёным набором" },
    { id: "e", label: "Сохраняющий поведение рефакторинг красит сотни тестов в красный без всякого бага" },
  ]}
  correctOrder={["a", "b", "c", "d", "e"]}
/>

</Explanation>

<FlowDiagram
  label="London мокает взаимодействия; Detroit использует настоящих коллабораторов и проверяет состояние"
  hue="var(--d-systems)"
  perRow={3}
  nodes={[
    {id:"sut",label:"SUT",sub:"тестируемый код"},
    {id:"mock",label:"mock",sub:"London: взаимодействия"},
    {id:"real",label:"настоящий",sub:"Detroit: состояние"},
    {id:"interact",label:"проверить вызовы",sub:"привязан к структуре"},
    {id:"state",label:"проверить итог",sub:"переживает refactor"},
    {id:"boundary",label:"правило границ",sub:"мок = нельзя запустить"}
  ]}
  edges={[
    {from:"sut",to:"mock",label:"London"},
    {from:"sut",to:"real",label:"Detroit"},
    {from:"mock",to:"interact"},
    {from:"real",to:"state"},
    {from:"interact",to:"boundary"},
    {from:"state",to:"boundary"}
  ]}
  caption="London (mockist) мокает всех коллабораторов и проверяет взаимодействия — привязан к структуре вызовов. Detroit (classicist) использует настоящих коллабораторов и проверяет конечное состояние — переживает рефакторинги. Правило границ синтезирует оба: мокать сеть/часы/оплату, настоящие объекты — для кода, которым владеете."
/>

<KeyTakeaway>Моки проверяют взаимодействия; стабы и фейки подают состояние. Правило границ разрешает спор London против Detroit: мокайте то, что не можете запустить (сеть, часы, оплату), используйте настоящие объекты для кода, которым владеете, и проверяйте конечное состояние. Чрезмерный мокинг привязывает тесты к структуре вызовов — они ломаются на рефакторинге и остаются зелёными на реальных багах.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="02-test-doubles-london-detroit-retrieval"
  lang="ru"
  questions={[
    { q: "Объясните London против Detroit в TDD и как правило границ разрешает это разногласие.", a: "London (mockist) работает снаружи-внутрь: юнит — это один класс, вы мокаете всех его коллабораторов и проверяете взаимодействия — что нужные сообщения были посланы. Detroit/Chicago (classicist) работает изнутри-наружу: юнит — это поведение, которое может охватывать несколько настоящих объектов, вы используете настоящих коллабораторов везде, где можете, мокаете лишь то, что обязаны, и проверяете конечное состояние. Практическая разница — выживание при рефакторинге: classicist-тесты проверяют результаты, поэтому позволяют беспощадный рефакторинг, а mockist-тесты проверяют структуру вызовов, поэтому фиксируют реализацию и ломаются при смене внутреннего взаимодействия. Синтез сеньора — не выбор школы, а правило границ: мокайте то, что не можете запустить (медленное, недетерминированное, необратимые побочные эффекты — сеть, часы, оплата, почта, сторонние API), и используйте настоящие объекты или фейки для быстрого, чистого кода, которым владеете. Решающий тест для любого дублёра: если я рефакторю внутренности, не меняя поведения, должен ли этот тест упасть? Если нет, вы используете мок там, где место стабу или настоящему объекту. Это в основном classicist с дисциплиной London, применённой на краях системы, и это растворяет чрезмерный мокинг." },
    { q: "Как чрезмерно замоканный набор может быть зелёным на 91% и всё же пропустить реальный баг, ломаясь на ничего не меняющем рефакторинге?", a: "Потому что моки делают тест зеркалом реализации, а не проверкой поведения. Когда каждый коллаборатор застаблен, тест лишь проверяет, что код вызвал методы, которые вы велели ему вызвать — он не может поймать неверный результат, если вы застабили результат, и не может поймать интеграционный баг между двумя настоящими объектами, потому что настоящих объектов нет. Так баг ценообразования уезжает зелёным: моки возвращали заготовленные суммы, и никто не сверял реально вычисленное значение с реальным расчётом. Одновременно моки вшивают структуру вызовов в условие прохождения, поэтому сохраняющий поведение рефакторинг — разбиение одного сервиса на два — меняет, какие вызовы происходят, и красит сотни тестов в красный без единого бага среди них. Лекарство — правило границ: используйте настоящие объекты для кода, которым владеете, и проверяйте конечное состояние, чтобы набор мог реально разойтись с кодом и ломался только тогда, когда ломается поведение." },
  ]}
/>

<Recap lang="ru">
«Мок» — это универсальный ярлык, но пять тестовых дублёров различаются способами, которые решают надёжность: dummy, стабы, фейки и шпионы подают входы или наблюдают выходы, тогда как мок утверждает, что конкретные взаимодействия случились — и эта проверка вызовов и есть источник хрупкости. Школа London (mockist) трактует юнит как один класс, мокает каждого коллаборатора и проверяет взаимодействия; школа Detroit/Chicago (classicist) трактует юнит как поведение через настоящие объекты, мокает лишь то, что обязана, и проверяет конечное состояние. Classicist-тесты переживают рефакторинги, потому что проверяют результаты; mockist-тесты фиксируют реализацию и ломаются при смене взаимодействия, и так сохраняющий поведение рефакторинг красит 600 тестов в красный без единого бага среди них, пока реальная ошибка ценообразования уезжает зелёной за заготовленными значениями стабов. Решение сеньора — правило границ: мокайте то, что не можете запустить — сеть, часы, оплату, почту, сторонние API, — и используйте настоящие объекты или фейки для быстрого, чистого кода, которым владеете, проверяя конечное состояние. Решающий вопрос для любого дублёра — должен ли сохраняющий поведение рефакторинг уронить этот тест; если не должен, вы схватились за мок там, где место стабу или настоящему объекту.
</Recap>
