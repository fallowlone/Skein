---
concepts:
- red-green-refactor
- test-first-design
- behavior-vs-implementation
- refactor-step
- fragile-tests
- arrange-act-assert
deepensInto:
- 02-test-doubles-london-detroit
estMin: 15
lang: ru
lessonType: topic
level: junior
order: 1
prereqs: []
slug: 01-rgr-design-loop
sources:
- https://martinfowler.com/bliki/TestDrivenDevelopment.html
- http://xunitpatterns.com/Fragile%20Test.html
- https://enterprisecraftsmanship.com/posts/tdd-not-tdd/
- https://arxiv.org/pdf/1711.05082
spiral:
- feedback-loops
- design-pressure
status: ready
summary: Тест-сначала — это давление на дизайн, а не гигиена покрытия. Вы вызываете
  собственный API до того, как он существует, и плохой интерфейс всплывает за секунды,
  а не после пятнадцати мест вызова. Вся выгода живёт в шаге refactor, который команды
  режут под дедлайн.
title: Red-green-refactor — это цикл проектирования, а не ритуал тестирования
track: engineering-practice
unit: 01-tdd-property
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";

<Hook>
Прию просят добавить фичу «отмена заказа». Она пишет тест первым и сразу упирается в стену: чтобы проверить результат, ей надо собрать `OrderService`, которому нужны соединение с БД, платёжный шлюз, почтовый клиент и сервис фича-флагов — четыре коллаборатора, только чтобы протестировать одно булево правило. Тест невозможно написать чисто. Её коллега заметил бы это лишь после того, как `cancel()` уже вшит в контроллер, две джобы и админ-скрипт. Ненаписуемый тест — это и есть ревью дизайна: `OrderService` делает слишком много, и TDD сказал об этом до единой строки продакшен-кода.
</Hook>

<Crux>Тест-сначала делает вас первым потребителем своего API — плохой интерфейс валит тест ещё до того, как у него появятся места вызова.</Crux>

<Explanation>

## Первый шаг — это давление на дизайн, а не покрытие

Цикл намеренно крошечный: написать наименьший падающий тест (**red**), написать наименьший код, делающий его зелёным (**green**), затем улучшить код, не меняя поведения (**refactor**). Ценность, которую большинство упускает, целиком живёт в шаге *red*. Написание теста до реализации заставляет вас вызывать собственный код снаружи до того, как он существует — вы выбираете имя функции, её аргументы, форму возврата и режим отказа, пока вы всё ещё *потребитель*, а не автор. Код, который неудобно вызывать, неудобно тестировать первым, поэтому плохой интерфейс всплывает за секунды, а не после того, как его вшили в пятнадцать мест вызова, которые теперь все надо менять.

Это переосмысляет всю практику. Люди спорят о TDD так, будто вопрос в том, «находит ли написание тестов первыми больше багов?». Это мимо. Механизм — это задержка обратной связи о *дизайне*: труднособираемый объект, метод, которому нужно шесть моков, функция, чей возврат нельзя проверить, не залезая во внутренности — это запахи дизайна, и тест-сначала вскрывает их в самый дешёвый момент, до того как дизайн прорастёт в кодовую базу. Сравнительный кейс-стади на arXiv показал, что эффект на дизайн был устойчивым сигналом во всех командах; эффект на число багов был шумнее.

## Пропуск refactor — вот как цикл гниёт

Самый частый провал — остановиться на green. Под дедлайном ритуал схлопывается до «тест прошёл, выкатываем», и третий шаг — refactor — тихо исчезает. А это шаг, где убирается дублирование и чинятся запутанные имена *пока тесты всё ещё стерегут поведение*. Пропускайте его квартал — и у вас зелёный CI поверх кодовой базы, которую никто не хочет трогать. Зелёный — не здоровье. Прохождение тестов говорит, что поведение сохранено; оно ничего не говорит о том, осталась ли форма кода такой, которую человек может менять.

Здесь есть реальная цена, и притворяться иначе нечестно. Индустриальные данные оценивают тест-сначала примерно в **15–22% больше времени на входе**, чем тест-потом для той же фичи. Размен — это смещённая вперёд обратная связь по дизайну и регрессионная сеть против последующих переделок — поэтому отдача TDD выше всего на долгоживущем, часто меняемом коде и слабее всего на одноразовых скриптах. Полностью разрешим это противоречие в уроке 04; пока суть в том, что шаг refactor — это где приземляется большая часть долгосрочной выгоды, и это первое, что команды срезают.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Шаг цикла</th>
        <th class="border border-border px-3 py-2 text-left">Что вы реально делаете</th>
        <th class="border border-border px-3 py-2 text-left">Что он даёт</th>
        <th class="border border-border px-3 py-2 text-left">Цена пропуска</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">Red</td>
        <td class="border border-border px-3 py-2">Пишете наименьший падающий тест, вызывая API снаружи</td>
        <td class="border border-border px-3 py-2">Обратную связь по дизайну до существования кода; плохой API валится здесь</td>
        <td class="border border-border px-3 py-2">Находите плохой API после того, как 15 мест вызова от него зависят</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Green</td>
        <td class="border border-border px-3 py-2">Пишете наименьший код, который проходит — пусть даже грубый</td>
        <td class="border border-border px-3 py-2">Заставляет столкнуться с реальным требованием, а не с догадкой</td>
        <td class="border border-border px-3 py-2">Переусложнение под случаи, которых не требует ни один тест</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Refactor</td>
        <td class="border border-border px-3 py-2">Чистите имена и дублирование, пока тесты зелёные</td>
        <td class="border border-border px-3 py-2">Бо́льшую часть долгосрочной выгоды; безопасную реструктуризацию</td>
        <td class="border border-border px-3 py-2">Зелёный CI поверх гниющего кода, который никто не тронет</td>
      </tr>
    </tbody>
  </table>
</div>

## Привязывайтесь к поведению, или набор станет обузой

Тихий убийца — это *что* вы проверяете. Тест, привязанный к **поведению** — «возврат за полностью отгруженный заказ отклоняется» — переживает любой рефакторинг, сохраняющий результат, поэтому он защищает вас, пока вы улучшаете код. Тест, привязанный к **реализации** — «`RefundService` вызывает `inventoryClient.check()` ровно раз, затем `ledger.post()`» — ломается в тот же миг, когда вы реорганизуете внутренности, даже если для клиента ничего не изменилось. Команда усваивает, что red обычно шум, и начинает его игнорировать. Именно так набор сползает из актива в обузу: это анти-паттерн хрупкого теста, описанный в xUnit Test Patterns.

Зеркальный провал так же опасен: тест, привязанный к реализации, может оставаться *зелёным*, пока реальное поведение сломано, потому что он лишь проверяет, что случился некий внутренний танец, а не что произведён правильный результат. Проверяйте через публичную поверхность, проверяйте наблюдаемые результаты и мокайте только настоящие внешние границы. Набор, который ломается ровно тогда, когда ломается поведение — и только тогда — это тот, которому разработчики доверяют достаточно, чтобы действовать.

<Inset kind="why" lang="ru">
«Тест первым» звучит как дисциплина о корректности, но её настоящий продукт — это *пригодный интерфейс*. Когда вас заставляют потреблять собственный API до его написания, вы чувствуете трение, которое почувствуют ваши вызывающие — четыре зависимости в конструкторе, метод, который нельзя вызвать без живой БД, возврат, который нельзя проинспектировать. Эти трения — дефекты дизайна, и тест — первое место, где они становятся конкретными и дешёвыми для починки. Вы пишете не тест; вы проводите ревью дизайна, где вы сами — самый строгий пользователь.
</Inset>

<TradeoffMatrix
  id="01-rgr-design-loop-tradeoff-1"
  lessonSlug="01-rgr-design-loop"
  lang="ru"
  prompt="Вы пишете тест первым и обнаруживаете, что нужно собрать четыре коллаборатора (БД, оплата, почта, флаги), только чтобы прогнать одно правило отмены заказа. О чём говорит этот тест?"
  options={[
    { name: "У объекта слишком много обязанностей — выделите правило отмены, чтобы его можно было тестировать изолированно", summary: "Слушайте запах дизайна.", correct: true, justification: "Объект, которому нужны четыре коллаборатора для одного правила, делает четыре вещи. Труднописуемый тест — это ревью дизайна, ради которого существует TDD: он вскрывает чрезмерную обязанность до того, как у cancel() появятся места вызова. Выделение правила в сфокусированный юнит упрощает и тест, и продакшен-код — это трение и есть сигнал, а не препятствие, которое надо обойти." },
    { name: "Подключить четыре мока, чтобы тест компилировался, и двигаться дальше", summary: "Замокать запах.", justification: "Так тест можно сделать зелёным, но вы заглушили ровно тот сигнал, который дал TDD. Тест, которому нужны четыре мока, тестирует god-объект; моки замазывают проблему дизайна и фиксируют плохую форму. Трение было сутью — замокав его, вы выбросили обратную связь." },
    { name: "Пропустить тест для этого случая — он явно слишком сложен для тестирования", summary: "Бросить тест-сначала.", justification: "Трудно тестируемое почти всегда значит трудно изменяемое. Объявив код нетестируемым и двинувшись дальше, вы выкатываете god-объект нетронутым и незащищённым. Сложность — это информация о дизайне, а не повод бросить дисциплину, которая её вскрыла." },
    { name: "Написать сквозной (E2E) тест, гоняющий весь стек вместо этого", summary: "Эскалация до E2E.", justification: "E2E-тест может подтвердить, что отмена работает через весь стек, но он медленный, флакающий и не даёт никакой обратной связи по дизайну юнита. Он также не чинит проблему обязанностей — вы просто отнесли проверку далеко от правила и усложнили локализацию падений." },
  ]}
/>

<Quiz
  id="01-rgr-design-loop-quiz-1"
  lessonSlug="01-rgr-design-loop"
  lang="ru"
  question="Что в первую очередь даёт сеньору шаг «red» цикла red-green-refactor?"
  choices={[
    { label: "Обратную связь по дизайну — вы потребляете собственный API до его существования, поэтому плохой интерфейс валит тест до того, как обретёт места вызова", correct: true },
    { label: "Более высокое покрытие строк, что и есть главная цель тест-сначала", misconception: "Покрытие — побочный эффект, а не цель. Отличительная ценность тест-сначала в том, что вы используете интерфейс как потребитель до его написания, что вскрывает проблемы дизайна — покрытие можно получить и тестами потом." },
    { label: "Доказательство, что у фичи нет багов, как только тест позеленел", misconception: "Один проходящий пример доказывает один случай, а не отсутствие багов. Ценность шага red — обратная связь по дизайну; поиск багов ограничен, и именно поэтому дальше в юните есть свойства и мутационное тестирование." },
    { label: "Гарантию, что код не понадобится рефакторить позже", misconception: "Он гарантирует обратное ожидание — refactor является явным третьим шагом. Тест-сначала не убирает нужду в реструктуризации; он делает её безопасной, оставляя сеть из поведения." },
  ]}
/>

<Quiz
  id="01-rgr-design-loop-quiz-2"
  lessonSlug="01-rgr-design-loop"
  lang="ru"
  question="Тест проверяет, что RefundService вызывает inventoryClient.check() раз, затем ledger.post() раз. Почему это анти-паттерн хрупкого теста?"
  choices={[
    { label: "Он привязан к деталям реализации — ломается на безобидных рефакторингах и может оставаться зелёным, пока результат возврата на деле неправильный", correct: true },
    { label: "Это идеальный тест поведения, ведь он проверяет точные внутренние шаги", misconception: "Порядок внутренних вызовов — это реализация, а не поведение. Клиенту важно, что возврат за полностью отгруженный заказ отклоняется, а не какие методы и в какой последовательности сработали — проверка последовательности привязывает вас к внутренностям." },
    { label: "Всё нормально, пока ожидания моков настроены правильно", misconception: "Правильные моки не чинят корневую проблему: тест проверяет, как код работает внутри, поэтому ломается на любом рефакторинге и никогда не подтверждает реальный видимый клиенту результат." },
    { label: "Он не находит баги только в статически типизированных языках", misconception: "Хрупкость не зависит от типизации. Тест, привязанный к реализации, может ломаться на рефакторинге и оставаться зелёным на реальной поломке в любом языке; типы не меняют того, что тест проверяет." },
  ]}
/>

<DragOrder
  id="01-rgr-design-loop-drag-1"
  lessonSlug="01-rgr-design-loop"
  lang="ru"
  prompt="Упорядочьте цикл red-green-refactor так, как его проводит сеньор для нового правила:"
  items={[
    { id: "a", label: "Red: пишете наименьший падающий тест, вызывая ещё не написанный API снаружи" },
    { id: "b", label: "Чувствуете трение — если объект трудно собрать, чините дизайн до продолжения" },
    { id: "c", label: "Green: пишете наименьший код, который делает тест зелёным" },
    { id: "d", label: "Refactor: чистите имена и убираете дублирование, пока тесты зелёные" },
    { id: "e", label: "Проверяете поведение, а не внутренности, чтобы тест пережил следующий рефакторинг" },
  ]}
  correctOrder={["a", "b", "c", "d", "e"]}
/>

</Explanation>

<KeyTakeaway>Тест-сначала — это давление на дизайн: вы потребляете собственный API до его существования, поэтому плохой интерфейс валит тест до того, как у него появятся места вызова для защиты. Выгода — в шаге refactor, который команды режут под дедлайн, и только если вы проверяете поведение, а не внутренности.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="01-rgr-design-loop-retrieval"
  lang="ru"
  questions={[
    { q: "Коллега говорит: «TDD — это просто про поднятие покрытия, напишу тесты потом, результат тот же». Каков ответ сеньора?", a: "Покрытие можно получить любым способом; отличительное, что даёт тест-сначала, — это обратная связь по дизайну, и получаете вы её только до существования кода. Когда вы пишете тест первым, вы становитесь первым потребителем собственного API: вы выбираете имя, аргументы, форму возврата и режим отказа, пока вы ещё снаружи. Плохой интерфейс — объект, который нельзя собрать без четырёх коллабораторов, метод, который нельзя вызвать без живой БД, возврат, который нельзя проверить, не залезая во внутренности, — валит тест немедленно, до того как у него появятся пятнадцать мест вызова, которые все пришлось бы менять. Напишете тест потом — дизайн уже зафиксирован; тест лишь утверждает ту форму, которую вы случайно построили. Сравнительный кейс-стади на arXiv нашёл, что эффект на дизайн был устойчивым сигналом во всех командах, тогда как эффекты на число багов были шумными. Так что ответ: тест-потом даёт то же покрытие и ноль давления на дизайн, что и есть весь смысл делать это первым." },
    { q: "Почему «проверяй поведение, а не реализацию» решает, будет ли набор активом или обузой?", a: "Тест, привязанный к поведению — «возврат за полностью отгруженный заказ отклоняется» — продолжает проходить через любой рефакторинг, сохраняющий результат, поэтому защищает вас, пока вы улучшаете код. Тест, привязанный к реализации — «RefundService вызывает inventoryClient.check(), затем ledger.post()» — падает каждый раз, когда вы реорганизуете внутренности, хотя для клиента ничего не изменилось, поэтому команда усваивает, что red обычно шум, и начинает игнорировать падения. Так набор становится обузой: он кричит «волк» на безобидные изменения, что и есть анти-паттерн хрупкого теста из xUnit Test Patterns. Зеркальный провал так же плох — тест на реализацию может оставаться зелёным, пока реальное поведение сломано, ведь он лишь проверяет, что случился некий внутренний танец, а не что произведён правильный результат. Лекарство — тестировать через публичную поверхность, проверять наблюдаемые результаты и мокать лишь настоящие внешние границы, чтобы набор ломался ровно тогда, когда ломается поведение, и только тогда." },
  ]}
/>

<Recap lang="ru">
Red-green-refactor — это цикл проектирования, а не ритуал покрытия. Несущий шаг — red: написание теста первым делает вас первым потребителем собственного API, поэтому плохой интерфейс — объект, которому нужны четыре коллаборатора, метод, который не запустить без БД — валит тест за секунды, а не после того, как от него зависят пятнадцать мест вызова. Это обратная связь по дизайну в самый дешёвый момент, и сравнительный кейс-стади нашёл её самым устойчивым эффектом TDD. Шаг green заставляет столкнуться с реальным требованием; шаг refactor — где приземляется большая часть долгосрочной выгоды, ведь он убирает дублирование и чинит имена, пока тесты всё ещё стерегут поведение — и это первый шаг, который команды режут под дедлайн, оставляя зелёный CI поверх кода, который никто не тронет. Тест-сначала стоит примерно на 15–22% больше времени на входе, поэтому его отдача выше всего на долгоживущем коде и слабее всего на одноразовой работе. Главное — привязывайте тесты к поведению, никогда к реализации: тесты на реализацию ломаются на безобидных рефакторингах и могут оставаться зелёными, пока реальный результат сломан, приучая команду игнорировать red. Проверяйте наблюдаемые результаты через публичную поверхность — и набор сломается ровно тогда, когда сломается поведение.
</Recap>
