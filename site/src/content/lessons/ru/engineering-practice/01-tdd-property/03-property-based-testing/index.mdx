---
concepts:
- property-based-testing
- invariants
- generators
- shrinking
- round-trip-property
- oracle-property
- metamorphic-property
deepensInto:
- 04-when-tdd-hurts
estMin: 17
lang: ru
lessonType: topic
level: middle
order: 3
prereqs:
- 02-test-doubles-london-detroit
slug: 03-property-based-testing
sources:
- https://hypothesis.readthedocs.io/en/latest/
- https://fast-check.dev/docs/introduction/why-property-based/
- https://hypothesis.works/articles/quickcheck-in-every-language/
- https://github.com/dubzzz/fast-check
spiral:
- feedback-loops
- edge-cases
status: ready
summary: Тесты-примеры проверяют лишь случаи, о которых вы подумали, поэтому слепые
  зоны набора — это ваши слепые зоны. Property-тест утверждает инвариант над сгенерированным
  входом — round-trip, оракул, метаморфический — и сжимает падение до минимального
  контрпримера.
title: 'Property-based-тестирование: инварианты вместо примеров, со сжатием контрпримера'
track: engineering-practice
unit: 01-tdd-property
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
У платёжного сервиса 94% покрытия строк и восемь месяцев зелёного CI. И вот клиента в Турции списывают дважды. Баг: форматтер денег округлял половину-к-чётному на одном пути и половину-вверх на другом, и эти два варианта расходились только на значениях, оканчивающихся на `.005`. Каждый юнит-тест использовал круглые числа — `19.99`, `100.00` — поэтому каждый тест проходил. Никто не написал `0.005`, `2.675` или случайно сгенерированную дробь, потому что никто об этом не подумал. Набор был зелёным, и поведение было сломано одновременно, восемь месяцев, потому что примеры и слепые зоны были написаны одним и тем же человеком.
</Hook>

<Crux>Тесты-примеры покрывают лишь воображённый вход; свойство утверждает инвариант над входом и сжимает падение до минимального контрпримера.</Crux>

<Explanation>

## Ваши примеры — это ровно ваши слепые зоны

Тест на примере — это один фиксированный вход, отображённый в один ожидаемый выход: `reverse([1,2,3]) === [3,2,1]`. Он документирует единственный случай, и вы пишете те случаи, которые можете вообразить — а значит, слепые зоны набора — это в точности ваши слепые зоны. Денежный баг — каноническая форма этого: каждый пример был «красивым» числом, поэтому расхождение половины-к-чётному и половины-вверх на значениях `.005` жило нетронутым в зазоре между примерами. Никакое количество дополнительных примеров с круглыми числами его не закроет, потому что зазор задан тем, что вы не догадались набрать.

**Property-based-тест** переворачивает отношение. Вместо того чтобы задавать пару вход-выход, вы задаёте нечто, что должно быть истинно для *всех* допустимых входов — инвариант — и фреймворк генерирует сотни входов, чтобы его атаковать. «Двойной разворот списка возвращает исходный список.» «Разбор с последующей сериализацией даёт исходное значение.» Hypothesis (Python), fast-check (JS/TS) и QuickCheck (Haskell, прародитель семейства) кидают в свойство сгенерированные значения: пустые, из одного элемента, огромные, с обилием дубликатов и, что критично, граничные значения, которые сидят там, где живут баги. fast-check по умолчанию делает `numRuns: 100` на свойство, настраивается до 1000+ в CI. Вы больше не ограничены входами, которые произвело ваше воображение.

## Четыре формы свойств, которые реально ловят баги

Сформулировать полезный инвариант — это акт проектирования, а слабое свойство (`результат — это число`) тестирует почти ничего, выглядя при этом основательно. Есть четыре формы, которые стоит запомнить, потому что большинство реальных свойств — это экземпляр одной из них. **Round-trip:** `decode(encode(x)) === x` — любая пара сериализация/разбор, сжатие/распаковка, сохранение/загрузка. Это самая ходовая форма, и она поймала бы денежный баг за несколько прогонов. **Инвариант:** факт, который всегда держится после операции — `sort(xs).length === xs.length`, выход отсортирован, сумма неотрицательна. **Оракул:** доверенный эталон согласуется с новым кодом — переписанная версия против старой функции, быстрый путь против медленного. **Метаморфический:** отношение между двумя связанными прогонами — результаты `search(q)` ⊇ `search(q + " AND x")`.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Форма свойства</th>
        <th class="border border-border px-3 py-2 text-left">Форма утверждения</th>
        <th class="border border-border px-3 py-2 text-left">Где подходит</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">Round-trip</td>
        <td class="border border-border px-3 py-2"><code>decode(encode(x)) === x</code></td>
        <td class="border border-border px-3 py-2">JSON сериализация/разбор, сжатие, сохранение/загрузка, формат денег</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Инвариант</td>
        <td class="border border-border px-3 py-2">факт, который всегда держится после операции</td>
        <td class="border border-border px-3 py-2"><code>sort(xs).length === xs.length</code>; выход отсортирован</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Оракул</td>
        <td class="border border-border px-3 py-2">новая реализация согласуется с доверенным эталоном</td>
        <td class="border border-border px-3 py-2">переписанная vs старая функция; быстрый путь vs медленный</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Метаморфический</td>
        <td class="border border-border px-3 py-2">отношение между двумя связанными прогонами</td>
        <td class="border border-border px-3 py-2"><code>search(q)</code> ⊇ <code>search(q + " AND x")</code></td>
      </tr>
    </tbody>
  </table>
</div>

## Сжатие контрпримера — вот почему его можно отлаживать

Причина, по которой property-тестирование пригодно, а не просто шум, — это **сжатие контрпримера** (shrinking). Когда фреймворк находит падающий вход, он не отдаёт вам гигантское случайное значение, которое случайно сгенерировал; он автоматически ищет *наименьший* вход, который всё ещё падает, обычно через редукцию в стиле бинарного поиска. fast-check может сгенерировать падающий список из 40 случайных целых, а затем сжать его до `[23, 22]` — минимальной пары, которая всё ещё нарушает свойство. Hypothesis делает то же и затем прогоняет минимальный пример ещё раз, чтобы убедиться, что падение не флакает, прежде чем сообщить о нём.

Это и есть разница между пригодным баг-репортом и непригодным. Без сжатия вы получаете «упало на `[483, -29, 0, 17, -6, 92, ...]`» и режете руками; со сжатием вы получаете «упало на `[0, -1]`», и корневая причина часто очевидна с первого взгляда. Оба фреймворка ещё печатают **seed**: перезапустите с этим seed — и вы воспроизведёте точно тот падающий случай на своём ноутбуке, что делает в остальном пугающие «случайные тесты» детерминированными для отладки. Генератор находит баг; шринкер делает его дешёвым для понимания; seed делает его воспроизводимым.

<Inset kind="why" lang="ru">
Сеньорское возражение против property-тестов реально: сгенерированная случайность — это риск флакания. Свойство, тайно зависящее от часов, незасеянного `Math.random`, реального времени или внешнего состояния, может пройти 999 прогонов и упасть на 1000-м, покрасив CI в красный на несвязанном коммите. Дисциплина, которая это обезвреживает, двойная — сделайте свойство чистым и отдайте всю генерацию входа фреймворку, и зафиксируйте seed в тот момент, когда появился флак, чтобы падение было воспроизводимым, а не подбрасыванием монеты. Property-тест, который нельзя воспроизвести по требованию, — это не тест; это слух. Воспроизводимость, а не сырая изобретательность, — вот что делает сгенерированное тестирование заслуживающим доверия в CI.
</Inset>

<TradeoffMatrix
  id="03-property-based-testing-tradeoff-1"
  lessonSlug="03-property-based-testing"
  lang="ru"
  prompt="Вы переписываете проверенный боем, но медленный CSV-парсер, который используется по всей компании. Как протестировать переписанную версию с наибольшим рычагом?"
  options={[
    { name: "Свойство-оракул: подать сгенерированный вход и в старый, и в новый парсер, утверждать равенство их выходов", summary: "Дифференциальный тест.", correct: true, justification: "Старый парсер — ваш доверенный эталон. Свойство-оракул генерирует тысячи строк CSV-формы — запятые в кавычках, встроенные переводы строк, пустые поля, юникод — и падает в тот же миг, когда переписанная версия расходится с проверенной реализацией, сжимаясь до наименьшего входа, на котором они расходятся. Вам никогда не приходится перечислять крайние случаи самим; старый код задаёт корректность. Это форма свойства с наибольшим рычагом для любого переписывания." },
    { name: "Round-trip-свойство: утверждать parse(serialize(rows)) === rows на сгенерированных строках", summary: "Round-trip.", justification: "По-настоящему полезно и стоит иметь, но это лишь проверяет, что пара разбор/сериализация самосогласованна — переписанная версия может быть самосогласованно неправильной (обе одинаково плохо обрабатывают запятые в кавычках), и round-trip всё равно пройдёт. Сочетайте его с оракулом; не полагайтесь на него одного при переписывании." },
    { name: "Скопировать тесты-примеры старого парсера и прогнать их на новом", summary: "Переиспользовать примеры.", justification: "Полезный быстрый регрессионный пол, но он лишь перепроверяет случаи, которые кто-то уже вообразил — ровно те входы, что меньше всего способны вскрыть тонкое расхождение. Он не сгенерирует ужас «запятая в кавычках внутри перевода строки», который ломает реальные CSV-парсеры. Оставьте их, но их недостаточно." },
    { name: "Утверждать, что новый парсер вызывает те же внутренние методы токенизатора, что и эталон", summary: "Мокать внутренности.", justification: "Это привязывает тест к детали реализации — а весь смысл переписывания в том, чтобы менять внутренности. Он ломается на каждом рефакторинге и может проходить, пока выход неправильный, ведь он никогда не проверяет разобранные строки. Это анти-паттерн хрупкого теста из предыдущего урока." },
  ]}
/>

<Quiz
  id="03-property-based-testing-quiz-1"
  lessonSlug="03-property-based-testing"
  lang="ru"
  question="Коллега говорит: «94% покрытия — с чего бы property-тестам найти что-то новое?» Каков точный ответ?"
  choices={[
    { label: "Покрытие измеряет, какие строки выполнились, а не какие входы вы пробовали; набор примеров прогоняет только случаи, которые вообразил человек, поэтому его слепые зоны — это ваши", correct: true },
    { label: "Property-тесты прогоняют те же строки, но больше раз, поэтому они ещё повышают покрытие", misconception: "Прогон тех же строк большее число раз не меняет покрытие. Суть — в пространстве входов: свойства генерируют значения, которые вы бы никогда не перечислили (вроде дробей класса .005), находя баги в зазорах между вашими примерами." },
    { label: "94% покрытия значит, что все баги — в оставшихся 6%", misconception: "Денежный баг жил в покрытых строках — каждая строка выполнялась с круглыми числами. Баги прячутся в неопробованных входах через полностью покрытый код, а это ровно то, чего покрытие увидеть не может." },
    { label: "Property-тесты полностью заменяют покрытие как метрику", misconception: "Они дополняют друг друга: покрытие говорит, какие строки выполнились, свойства исследуют пространство входов, которое не достаёт ваше воображение. Ни одно не поглощает другое." },
  ]}
/>

<Quiz
  id="03-property-based-testing-quiz-2"
  lessonSlug="03-property-based-testing"
  lang="ru"
  question="Свойство падает на случайно сгенерированном входе. Почему сжатие контрпримера важно и чем оно отличается от напечатанного seed?"
  choices={[
    { label: "Сжатие редуцирует падающий вход до минимального контрпримера, чтобы причина была очевидна; seed отдельно позволяет детерминированно переиграть точный прогон", correct: true },
    { label: "Сжатие ускоряет работу свойства за счёт использования меньших входов", misconception: "Сжатие запускается после того, как найдено падение, и добавляет раунды редукции. Оно о выдаче отлаживаемого контрпримера, а не об общей скорости." },
    { label: "Сжатие убирает случайность, поэтому тест становится детерминированным", misconception: "Детерминизм для переигрывания идёт от напечатанного seed, а не от сжатия. Сжатие минимизирует одно известное падение; оно не делает генерацию детерминированной." },
    { label: "Сжатие повышает покрытие, пробуя больше вариаций", misconception: "Это работа генерации. Сжатие — противоположное направление: оно упрощает одно известное падение до наименьшей формы." },
  ]}
/>

<DragOrder
  id="03-property-based-testing-drag-1"
  lessonSlug="03-property-based-testing"
  lang="ru"
  prompt="Упорядочьте жизненный цикл property-теста, который ловит денежный баг:"
  items={[
    { id: "a", label: "Сформулировать инвариант: parse(format(x)) === x для любой дроби x" },
    { id: "b", label: "Фреймворк генерирует сотни дробей, включая граничные значения" },
    { id: "c", label: "Сгенерированное значение, оканчивающееся на .005, нарушает свойство" },
    { id: "d", label: "Сжатие редуцирует его до минимального падающего значения, например 0.005" },
    { id: "e", label: "Зафиксировать напечатанный seed, детерминированно переиграть и починить округление" },
  ]}
  correctOrder={["a", "b", "c", "d", "e"]}
/>

</Explanation>

<FlowDiagram
  label="Property-based-тестирование: генерация входов, проверка инварианта, сжатие до минимального контрпримера"
  hue="var(--d-systems)"
  perRow={3}
  nodes={[
    {id:"state",label:"инвариант",sub:"напр. round-trip"},
    {id:"generate",label:"генерировать",sub:"100+ входов"},
    {id:"run",label:"прогнать",sub:"проверить инвариант"},
    {id:"pass",label:"прошло",sub:"следующий вход"},
    {id:"fail",label:"нарушено",sub:"найдено падение"},
    {id:"shrink",label:"shrink",sub:"минимизировать вход"},
    {id:"report",label:"контрпример",sub:"+ seed для replay"}
  ]}
  edges={[
    {from:"state",to:"generate"},
    {from:"generate",to:"run"},
    {from:"run",to:"pass",label:"держится"},
    {from:"pass",to:"generate",label:"ещё прогоны"},
    {from:"run",to:"fail",label:"нарушено"},
    {from:"fail",to:"shrink"},
    {from:"shrink",to:"report"}
  ]}
  caption="Фреймворк генерирует сотни входов и проверяет инвариант на каждом. Если держится — берётся следующий вход. При нарушении шринкер находит минимальный контрпример; печатается seed, чтобы точное падение детерминированно переигрывалось."
/>

<KeyTakeaway>Тесты-примеры покрывают только тот вход, который вы вообразили, поэтому слепые зоны набора — это ваши. Свойство утверждает инвариант — round-trip, инвариант, оракул, метаморфический — над сотнями сгенерированных входов, находит зазор, который ваши примеры пропустили, затем сжимает падение до минимального контрпримера и печатает seed, чтобы оно детерминированно переигрывалось.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="03-property-based-testing-retrieval"
  lang="ru"
  questions={[
    { q: "У нас 94% покрытия. Обоснуйте добавление property-тестов всё равно.", a: "Покрытие измеряет, какие строки выполнились, а не какие входы вы пробовали, и набор на примерах прогоняет только случаи, которые вообразил человек — поэтому его слепые зоны — это в точности ваши слепые зоны. Можно прогнать каждую строку круглыми числами и ни разу не попасть в значение, которое ломает. Денежный баг — учебный случай: 94% покрытия, восемь месяцев зелёного и расхождение половины-к-чётному против половины-вверх, появлявшееся только на значениях, оканчивающихся на .005, которые никто не набрал в тесте. Property-тест утверждает инвариант над сгенерированным входом — для форматтера денег parse(format(x)) === x — и фреймворк кидает в него сотни сгенерированных дробей, включая граничные значения вроде 0.005 и 2.675, в первые же несколько прогонов. fast-check по умолчанию делает 100 случаев на свойство, настраивается до 1000+. Он находит зазор между вашими примерами, затем сжимает падение до минимального запускающего значения. Свойства не заменяют покрытие; они покрывают пространство входов, которое не достаёт ваше воображение, а именно там живут покрытые-но-неопробованные баги." },
    { q: "Пройдите по тому, как property-тест полезно сообщает о падении, от генерации до воспроизводимой починки.", a: "Сначала находит генератор: из заявленного инварианта фреймворк производит сотни входов — пустые, огромные, с обилием дубликатов и граничные значения — и один из них нарушает свойство. Но сырой падающий вход часто гигантское случайное значение, бесполезное для отладки. Вот тут вступает сжатие: фреймворк ищет наименьший вход, который всё ещё падает, обычно через редукцию в стиле бинарного поиска — fast-check сжимает массив из 40 элементов до [23, 22], минимальной пары, которая всё ещё его ломает. Hypothesis делает то же, затем прогоняет минимальный пример ещё раз, чтобы убедиться, что он не флакает, прежде чем сообщить. Наконец фреймворк печатает seed; перезапустите с этим seed — и вы воспроизведёте точно тот падающий случай на своём ноутбуке, превращая «случайные тесты» в детерминированное, переигрываемое падение. Так что разделение труда такое: генератор находит баг, шринкер делает его дешёвым для понимания, seed делает его воспроизводимым. Дисциплина, которая держит это заслуживающим доверия, — чистота: отдайте всю генерацию фреймворку и зафиксируйте seed, когда появился флак." },
  ]}
/>

<Recap lang="ru">
Тесты на примерах отображают один фиксированный вход в один ожидаемый выход, поэтому вы тестируете только случаи, которые можете вообразить — а значит, слепые зоны набора — это в точности ваши собственные, как форматтер денег оставался зелёным восемь месяцев, потому что каждый пример был круглым числом, а расхождение на .005 жило в зазоре между ними. Property-based-тестирование переворачивает это: вы задаёте инвариант, истинный для всех допустимых входов, и фреймворк генерирует их сотни — пустые, огромные, с обилием дубликатов, граничные — чтобы его атаковать, при fast-check на 100 прогонах по умолчанию и Hypothesis и QuickCheck, делающих то же в разных языках. Четыре формы, которые стоит знать, — это round-trip (decode∘encode — это тождество, самая ходовая и та, что ловит денежный баг), инвариант (факт, всегда истинный после операции), оракул (доверенный эталон согласуется, идеален для переписываний) и метаморфический (отношение между связанными прогонами). Сжатие контрпримера — вот что делает падение отлаживаемым: фреймворк редуцирует падающий массив из 40 элементов до [23, 22], а напечатанный seed переигрывает точный прогон. Цена реальна — свойства труднее писать, они медленнее и флакают, если просочилась нечистота — поэтому дисциплина это чистота, генерация под управлением фреймворка и фиксация seed, когда появился флак.
</Recap>
