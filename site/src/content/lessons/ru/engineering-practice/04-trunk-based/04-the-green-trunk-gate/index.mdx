---
concepts:
- ci-gate
- green-trunk
- merge-queue
- batch-testing
- flaky-tests
- build-speed
- git-bisect
deepensInto:
- 05-flag-debt-and-rollout-discipline
estMin: 17
lang: ru
lessonType: topic
level: senior
order: 4
prereqs:
- 03-feature-flags-dark-launch
slug: 04-the-green-trunk-gate
sources:
- https://dora.dev/capabilities/continuous-integration/
- https://github.blog/engineering/engineering-principles/how-github-uses-merge-queue-to-ship-hundreds-of-changes-every-day/
- https://graphite.com/guides/merge-queue-comparison-github-gitlab-graphite
- https://martinfowler.com/articles/continuousIntegration.html
spiral:
- blast-radius
- feedback-loops
status: ready
summary: 'Общий trunk: одна поломка останавливает всех, поэтому trunk-based неотделим
  от быстрого блокирующего CI-гейта. На масштабе наивный pre-merge гейт гонится сам
  с собой — применяют merge queue с проверкой против проецируемого trunk; FIFO-очередь
  упирается после ~20-30 инженеров.'
title: Гейт зелёного trunk — это вся сделка
track: engineering-practice
unit: 04-trunk-based
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
Команда из 40 инженеров переходит на trunk-based и за месяц объявляет это провалом: trunk красный половину дня, а когда он красный, никто не может вытянуть чистую базу или слиться на зелёном, так что вся команда стопорится. Диагноз не «trunk-based не масштабируется» — а в том, что они приняли *риск* (все на одной ветке) без *смягчения* (гейт, держащий эту ветку зелёной). Trunk-based без быстрого блокирующего CI-гейта — не trunk-based; это общая ветка без тормозов.
</Hook>

<Crux>Все на одной ветке — сломанный trunk стопорит всех, поэтому trunk-based неотделим от быстрого блокирующего CI-гейта: no green, no merge.</Crux>

<Explanation>

## Общий trunk концентрирует риск

Сила trunk-based development — все интегрируются в одну ветку — одновременно его единственная точка отказа. С долгоживущими ветками сломанная ветка одного человека — проблема одного человека. С общим trunk сломанный trunk — проблема *всех*: никто не может вытянуть чистую базу, чтобы начать работу, никто не может слиться, потому что его PR строится на красном, а «быстрый фикс» приземляется поверх поломки, делая отказ труднее в диагностике. Практика намеренно меняет изолированную отложенную боль на общий немедленный риск — и эта сделка окупается, только если trunk держат зелёным.

Так что гейт — не дополнение; это вторая половина сделки. Правило — **нет зелёного, нет слияния**: каждое изменение прогоняет полную сборку и набор тестов *до* приземления на trunk, а красный результат блокирует слияние. Вот почему trunk-based и непрерывная интеграция — одна и та же практика, описанная с двух сторон: «мёржить в trunk ежедневно» безопасно лишь потому, что одновременно держится «автоматический гейт держит trunk всегда релизуемым». Данные DORA показывают, что сила trunk-based идёт *через* CI; примите модель ветвления без гейта — и вы получите режим отказа красного trunk вместо выгоды.

## Гейт должен быть быстрым, или его будут обходить

Эффективность гейта затухает с его задержкой. Если сборка занимает 45 минут, разработчики начинают батчить изменения, чтобы амортизировать ожидание — что удлиняет ветки, что вновь впитывает разрыв из урока 1. Они срезают углы, пропускают локальный прогон тестов или давят слить на красном «только в этот раз». Медленный гейт тихо разрушает то самое поведение, ради обеспечения которого существует. Цель — минуты: сборка достаточно быстрая, чтобы интегрироваться мало и часто было путём наименьшего сопротивления. Скорость идёт от параллелизации тестов, ярусности (быстрые юнит-тесты блокируют слияние; медленные end-to-end наборы бегут post-merge или по ночам), кэширования и беспощадной вырезки самых медленных нарушителей в наборе.

Когда trunk всё же краснеет вопреки гейту — проскочил флейки-тест, отказ только в окружении — сеньорский рефлекс — **остановить линию**: починка или откат trunk важнее новой работы над фичами, потому что каждая минута красного облагает налогом всю команду. `git bisect` делает поиск виновного коммита дешёвым через бинарный поиск по истории, а малая, часто интегрируемая история — ровно то, что делает bisect быстрым, а откат чистым.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Дизайн гейта</th>
        <th class="border border-border px-3 py-2 text-left">Когда бегут тесты</th>
        <th class="border border-border px-3 py-2 text-left">Может ли trunk сломаться?</th>
        <th class="border border-border px-3 py-2 text-left">Предел масштабирования</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">Только post-merge CI</td>
        <td class="border border-border px-3 py-2">После приземления на trunk</td>
        <td class="border border-border px-3 py-2">Да — обнаружено поздно</td>
        <td class="border border-border px-3 py-2">Ломается постоянно после пары разработчиков</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Pre-merge (тест PR-ветки)</td>
        <td class="border border-border px-3 py-2">До слияния, против устаревшей базы</td>
        <td class="border border-border px-3 py-2">Да — два зелёных PR могут конфликтовать семантически</td>
        <td class="border border-border px-3 py-2">Гоняется сам с собой при высокой частоте слияний</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Merge queue (тест против проецируемого trunk)</td>
        <td class="border border-border px-3 py-2">Против trunk + PR впереди в очереди</td>
        <td class="border border-border px-3 py-2">Нет — приземляется только зелёное против финального состояния</td>
        <td class="border border-border px-3 py-2">FIFO-бутылочное горло ~20–30 разработчиков; флейки-тесты её стопорят</td>
      </tr>
    </tbody>
  </table>
</div>

## В масштабе гейт гоняется сам с собой — на сцену выходят merge queue

У наивного pre-merge гейта есть тонкая дыра. Вы тестируете PR-A против trunk: зелёный. Я тестирую PR-B против *того же* trunk: зелёный. Мы оба мёржим. Но A и B никогда не тестировались *вместе* — они могут конфликтовать семантически и сломать trunk, хотя каждый прошёл. При паре слияний в день это редко; при десятках — постоянно. Лекарство — **merge queue**: вместо прямого слияния PR входят в очередь, и CI тестирует каждый против *проецируемого* состояния trunk — trunk плюс все PR впереди него в линии. PR приземляется, только если он зелёный против ровно того состояния, которое создаст. Чтобы держать пропускную способность, очереди **батчат**: тестируют несколько очередных PR вместе как один комбинированный кандидат и мёржат весь батч, если зелёный, откатываясь к bisect батча, если он падает.

Так большие команды держат единственный trunk зелёным при высоком объёме — собственная merge queue GitHub отмасштабировала монорепозиторий с примерно тысячи слияний в месяц в 2016-м до более чем тридцати тысяч к 2023-му, с сотнями инженеров, мёржащих тысячи PR в месяц, и примерно на треть ниже среднее время деплоя. Но у очереди есть пределы, которые сеньор должен планировать. Единственная FIFO-очередь становится бутылочным горлом после примерно **20–30 активных разработчиков**: несвязанные изменения конкурируют за одну полосу, и медленный CI стопорит всё за ним. Хуже того, единственный **флейки-тест** катастрофичен в очереди — при 50+ PR в день он может застопорить линию на часы, потому что недетерминированный отказ отвергает батчи, которые на самом деле были в порядке. Вот почему карантин флейков и параллелизм очереди (независимые полосы для независимого кода) становятся первоочередными инфраструктурными заботами в масштабе, а не приятными мелочами.

<Inset kind="why" lang="ru">
Заметьте: требование к качеству гейта симметрично дисциплине флагов из прошлого урока. Флейки-тест в merge queue — это версия устаревшего флага у гейта: часть системы, которая должна давать ясный сигнал, но вместо этого излучает шум, и шум нарастает в масштабе, пока не остановит всех. Trunk-based в масштабе — меньше про модель ветвления и больше про то, чтобы держать каждый сигнал в системе — гейт, очередь, флаги — заслуживающим доверия. Сигнал, которому нельзя доверять, хуже отсутствия сигнала, потому что люди его обходят.
</Inset>

<TradeoffMatrix
  id="04-green-trunk-gate-tradeoff-1"
  lessonSlug="04-the-green-trunk-gate"
  lang="ru"
  prompt="При 50 PR/день на одном trunk он ломается пару раз в неделю, хотя каждый PR был зелёным до слияния. Какое правильное лекарство?"
  options={[
    { name: "Взять merge queue, тестирующую каждый PR против проецируемого trunk (trunk + PR впереди), с батчингом для пропускной способности", correct: true, justification: "Два PR, каждый зелёный против одной и той же устаревшей базы, всё равно могут сломать trunk при объединении — наивный pre-merge гейт гоняется сам с собой при таком объёме. Merge queue тестирует против ровно того состояния, которое создаст каждый PR, так что приземляется только зелёное против финального состояния. Батчинг держит пропускную способность высокой; bisect упавшего батча изолирует виновника." },
    { name: "Сделать CI строже и требовать два аппрува на PR", summary: "Больше ревью, та же гонка.", justification: "Ревью и строгие проверки не адресуют гонку: каждый PR всё равно валидируется против устаревшей базы, так что два независимо-зелёных PR всё равно могут вместе сломать trunk. Проблема в том, против какого состояния trunk вы тестируете, а не в том, как тщательно ревьюите." },
    { name: "Перейти на post-merge CI и быстро откатывать, когда trunk ломается", summary: "Обнаружить-и-откатить.", justification: "Post-merge тестирование обнаруживает поломку только после того, как она уже блокирует команду, а при 50 PR/день churn откатов постоянен. Смысл гейта — держать trunk зелёным, а не научиться хорошо чистить его после того, как он красный." },
    { name: "Разбить на долгоживущие командные ветки, интегрирующиеся еженедельно", summary: "Вернуть разрыв.", justification: "Это бросает trunk-based ради ухода от решаемой инфраструктурной проблемы, меняя бутылочное горло очереди на еженедельные big-bang слияния и впитанный разрыв. Правильный ход — масштабировать гейт (очередь + параллельные полосы + контроль флейков), а не ветки." },
  ]}
/>

<Quiz
  id="04-green-trunk-gate-quiz-1"
  lessonSlug="04-the-green-trunk-gate"
  lang="ru"
  question="Почему trunk-based development неотделим от быстрого блокирующего CI-гейта?"
  choices={[
    { label: "Все делят одну ветку, так что сломанный trunk блокирует всю команду от чистой базы и от слияния — гейт это смягчение для этого сконцентрированного риска", correct: true },
    { label: "Потому что CI-серверы нужны для создания веток", misconception: "CI не имеет отношения к созданию веток. Суть в том, что общий trunk концентрирует риск — одна поломка останавливает всех — так что гейт, держащий trunk зелёным, и есть необходимая вторая половина сделки." },
    { label: "Потому что гейт заменяет нужду в код-ревью", misconception: "Гейт и ревью раздельны; гейт бегает тесты, ревью проверяет дизайн и намерение. Гейт нужен, потому что общий trunk делает одну поломку проблемой всех, а не потому, что заменяет ревью." },
    { label: "Потому что trunk-based запрещает прогонять тесты локально", misconception: "Trunk-based поощряет локальное тестирование; гейт — авторитетная проверка перед приземлением. Он нужен из-за сконцентрированного риска на общей ветке, а не из-за запрета на локальные тесты." },
  ]}
/>

<Quiz
  id="04-green-trunk-gate-quiz-2"
  lessonSlug="04-the-green-trunk-gate"
  lang="ru"
  question="Два PR каждый проходят CI против текущего trunk, затем оба мёржатся — и trunk ломается. Что случилось, и что это предотвращает?"
  choices={[
    { label: "Их тестировали против одной базы, но никогда вместе; merge queue, тестирующая каждый PR против проецируемого trunk (trunk + PR впереди), это предотвращает", correct: true },
    { label: "Один разработчик пропустил тесты; более строгое принуждение это предотвращает", misconception: "Оба PR действительно прошли — отказ в том, что ни один не тестировался против изменений другого. Принуждение не помогает; нужно тестировать против проецируемого комбинированного состояния, что и делает merge queue." },
    { label: "Кэш CI был устаревшим; его очистка это предотвращает", misconception: "Это не проблема кэширования. Каждый PR был корректно зелёным против базы, которую видел; пробел в том, что комбинированное состояние никогда не валидировали. Merge queue закрывает этот пробел." },
    { label: "Git слил их в неверном порядке; rebase это предотвращает", misconception: "Порядок — не корневая причина: даже при верном порядке два PR, валидированные только против устаревшей базы, могут конфликтовать семантически. Лекарство — тестировать против проецируемого trunk, а не переупорядочивать." },
  ]}
/>

<DragOrder
  id="04-green-trunk-gate-drag-1"
  lessonSlug="04-the-green-trunk-gate"
  lang="ru"
  prompt="Упорядочьте, как merge queue безопасно приземляет изменение в масштабе:"
  items={[
    { id: "a", label: "PR проходит свой собственный CI и аппрувится" },
    { id: "b", label: "PR входит в merge queue, а не мёржится напрямую" },
    { id: "c", label: "CI тестирует его (часто батчем) против trunk + PR впереди в линии" },
    { id: "d", label: "Зелёный против проецируемого финального состояния → батч мёржится в trunk" },
    { id: "e", label: "При отказе — bisect батча, чтобы выкинуть виновный PR и перезапустить" },
  ]}
  correctOrder={["a", "b", "c", "d", "e"]}
/>

</Explanation>

<FlowDiagram
  label="CI-гейт: коммит должен быть зелёным для попадания в trunk"
  hue="var(--d-systems)"
  perRow={4}
  nodes={[
    {id:"commit",label:"Коммит / PR",sub:"войти в очередь"},
    {id:"ci",label:"CI-гейт",sub:"сборка + тесты"},
    {id:"green",label:"Зелёный",sub:"слияние в trunk"},
    {id:"red",label:"Красный",sub:"блок — сначала чини"}
  ]}
  edges={[
    {from:"commit",to:"ci",label:"запустить"},
    {from:"ci",to:"green",label:"пройти"},
    {from:"ci",to:"red",label:"упасть"},
    {from:"red",to:"commit",label:"починить и повторить"}
  ]}
  caption="Нет зелёного — нет слияния. Красный блокирует trunk до починки; гейт неотделим от общей ветки."
/>

<KeyTakeaway>Общий trunk концентрирует риск — одна поломка останавливает всех — так что trunk-based неотделим от быстрого блокирующего CI-гейта: нет зелёного, нет слияния. Гейт должен быть быстрым (минуты), иначе люди батчат и впитывают разрыв. В масштабе наивный pre-merge гейт гоняется сам с собой (два PR, зелёные против одной базы, всё равно могут сломать trunk вместе), так что merge queue тестируют против проецируемого trunk и батчат ради пропускной способности — но единственная FIFO-очередь становится бутылочным горлом после ~20–30 разработчиков, и один флейки-тест может застопорить её на часы.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="04-green-trunk-gate-retrieval"
  lang="ru"
  questions={[
    { q: "Почему команда, которая «перешла на trunk-based», заканчивает с постоянно красным trunk, и какая часть отсутствует?", a: "Они приняли риск без смягчения. Trunk-based концентрирует всех на одной ветке, что его сила для интеграции, но и единственная точка отказа: когда trunk красный, никто не может вытянуть чистую базу или слиться на зелёном, так что одна поломка останавливает всю команду — а «быстрый фикс» поверх поломки делает диагностику хуже. Отсутствует быстрый блокирующий CI-гейт: каждое изменение прогоняет полную сборку и тесты до приземления, а красное блокирует слияние — нет зелёного, нет слияния. Вот почему trunk-based и непрерывная интеграция — одна практика с двух сторон: «мёржить в trunk ежедневно» безопасно лишь потому, что одновременно держится «автоматический гейт держит trunk всегда релизуемым». Гейт также должен быть быстрым — минуты, не десятки минут, — иначе разработчики батчат изменения, чтобы амортизировать ожидание, и вновь впитывают разрыв, ради избежания которого существует практика. Примите модель ветвления без гейта — и унаследуете режим отказа красного trunk вместо выгоды элитной доставки." },
    { q: "Объясните, почему наивный pre-merge гейт гоняется сам с собой в масштабе и как merge queue это чинят, включая пределы очередей.", a: "Pre-merge гейт тестирует каждый PR против текущего trunk. PR-A зелёный против trunk; PR-B зелёный против того же trunk; оба мёржатся — но их никогда не тестировали вместе, так что они могут конфликтовать семантически и сломать trunk, хотя каждый прошёл. При паре слияний в день это редко; при десятках — постоянно. Merge queue чинит гонку: PR входят в очередь, и CI тестирует каждый против проецируемого состояния trunk — trunk плюс все PR впереди в линии — так что изменение приземляется, только если оно зелёное против ровно того состояния, которое создаст. Чтобы держать пропускную способность, очереди батчат несколько PR как один комбинированный кандидат и мёржат батч, если зелёный, делая bisect батча при отказе, чтобы выкинуть виновника. Так GitHub держал один монорепозиторий зелёным с ~1000 слияний/месяц в 2016-м до 30 000+ к 2023-му. Но у очередей есть пределы: единственная FIFO-очередь становится бутылочным горлом после примерно 20–30 разработчиков, потому что несвязанные изменения конкурируют за одну полосу и медленный CI стопорит всё за ним, а единственный флейки-тест катастрофичен — при 50+ PR/день он может застопорить линию на часы, отвергая батчи, которые на самом деле были в порядке. Так что в масштабе карантин флейков и параллельные независимые полосы становятся первоочередной инфраструктурой, а не приятными мелочами." },
  ]}
/>

<Recap lang="ru">
Общий trunk — это сила trunk-based development и его единственная точка отказа: когда все интегрируются в одну ветку, сломанный trunk блокирует всю команду от чистой базы и от слияния, так что практика неотделима от быстрого блокирующего CI-гейта — нет зелёного, нет слияния, — который держит trunk всегда релизуемым. Гейт должен бежать за минуты, потому что медленный гейт толкает разработчиков батчить изменения и вновь впитывать разрыв, ради устранения которого существует trunk-based, а когда trunk всё же краснеет, сеньорский рефлекс — остановить линию и починить или откатить до новой работы, используя git bisect на малой частой истории, чтобы быстро найти виновника. В масштабе наивный pre-merge гейт гоняется сам с собой — два PR, каждый зелёный против одной базы, всё равно могут сломать trunk при объединении — так что merge queue тестируют каждый PR против проецируемого trunk и батчат ради пропускной способности, техника, что позволила GitHub держать один монорепозиторий зелёным с тысячи до тридцати тысяч слияний в месяц. Но очереди не бесплатны: единственная FIFO-очередь становится бутылочным горлом после примерно 20–30 разработчиков, а один флейки-тест может застопорить её на часы, делая контроль флейков и параллельные полосы ключевой инфраструктурой. Сквозная мысль: trunk-based в масштабе — это дисциплина держать каждый сигнал — гейт, очередь, флаги — заслуживающим доверия, потому что сигнал, которому люди не могут доверять, обходят. Теперь, когда trunk краснеет несколько раз в неделю, хотя каждый PR был зелёным до слияния, знаешь, с чего начать расследование: не тестировались ли два PR против одной и той же устаревшей базы?
</Recap>