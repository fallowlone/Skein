---
slug: 01-integration-frequency
lang: ru
track: engineering-practice
unit: 04-trunk-based
order: 1
level: middle
lessonType: topic
status: ready
title: "Частота интеграции — вот рычаг"
summary: "Цена слияния задаётся тем, насколько уехал trunk с момента ответвления, а разрыв растёт лавинообразно. Ежедневная интеграция не убирает конфликты — она держит каждый малым, решаемым за минуты с полным контекстом, пока он не стал big-bang, который никто не проверит."
prereqs: []
deepensInto: ["02-short-lived-branches-vs-gitflow"]
spiral: ["feedback-loops", "cost-of-delay"]
concepts: ["integration-frequency", "merge-conflict-cost", "branch-drift", "semantic-conflicts", "continuous-integration", "batch-size"]
estMin: 15
sources:
  - https://dora.dev/capabilities/trunk-based-development/
  - https://www.atlassian.com/continuous-delivery/continuous-integration/trunk-based-development
  - https://martinfowler.com/articles/branching-patterns.html
  - https://trunkbaseddevelopment.com/
---

import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";

<Hook>
Двое инженеров в понедельник ответвляются от одного и того же `OrderService`. Алиса в понедельник днём выкатывает однострочный фикс: её ветка примиряет полдня чужих коммитов, мёржится за тридцать секунд, готово. Боб три недели держит свою «переделку checkout» на ветке. В день слияния trunk уехал на 600 коммитов под ним — вызываемый им хелпер валидации переименовали, денежный тип сменился с центов-как-int на класс `Money`, миграция перекроила таблицу. Его фича была закончена ещё на второй неделе. Остальные полторы недели — это налог на интеграцию, и он платит его весь сразу.
</Hook>

<Crux>Цена слияния растёт с тем, как далеко уехал trunk за время жизни ветки, а разрыв растёт лавинообразно — поэтому интегрируйся ежедневно.</Crux>

<Explanation>

## Цена слияния — функция разрыва, а не размера диффа

Интуитивная модель — «большую фичу тяжело мёржить, потому что она большая» — неверна, причём важным образом. Цена слияния ветки задаётся **расхождением**: тем, насколько далеко trunk ушёл от точки, в которой вы ответвились. Фича на 2000 строк, слитая в тот же час, когда вы начали, почти ничего не стоит при слиянии, потому что trunk не сдвинулся. А фикс на 50 строк, слитый через три недели, может стать мучением, потому что эти 50 строк теперь лежат поверх фундамента, перестроенного под ними.

Расхождение растёт лавинообразно, потому что интеграция — это *общая* система. Пока ваша ветка жива, каждый другой инженер тоже мёржит в trunk. Ветка, живущая один день, примиряет один день чужой работы. Ветка, живущая три недели, примиряет три недели — и хуже того, вы потеряли мысленный контекст для кода, который менялся, пока вы не смотрели. Кривая цены загибается вверх с возрастом ветки, а не линейно. Именно поэтому внутреннее исследование Microsoft показало, что команды, не интегрировавшиеся более трёх дней, тратили примерно **в 12× больше времени** на разрешение конфликтов, чем команды с ежедневным слиянием, и почему работа над merge-конфликтами съедает, по оценкам, **10–25%** времени разработчиков в командах с обилием веток.

## Текстовые конфликты — дешёвые

Слияние git помечает *текстовый* конфликт, когда две ветки правят одни и те же строки — они шумные, и инструмент вас останавливает. Дорогие провалы — это **семантические конфликты**: обе стороны накладываются чисто, git доволен, а код теперь неправильный. Алиса переименовывает `validate(order)` в `validateOrder(order)` и обновляет все 40 мест вызова в trunk. Ветка Боба добавила 41-е место вызова к старому имени. Git мёржит оба без единого маркера конфликта; сборка падает, или, хуже, она компилируется из-за перегрузки и тихо делает не то.

Семантические конфликты масштабируются с той же переменной разрыва. Чем сильнее изменился trunk, пока вас не было, тем больше невидимых допущений, на которых построена ваша ветка, тихо стали ложными. Частая интеграция — единственная дешёвая защита: ветка возрастом в один день могла унаследовать лишь один день сломанных допущений, и ваш CI-гейт ловит их, пока вызвавшее их изменение ещё свежо у кого-то в памяти.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Возраст ветки при слиянии</th>
        <th class="border border-border px-3 py-2 text-left">Разрыв trunk для примирения</th>
        <th class="border border-border px-3 py-2 text-left">Характер конфликта</th>
        <th class="border border-border px-3 py-2 text-left">У кого ещё есть контекст</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">Часы</td>
        <td class="border border-border px-3 py-2">Несколько коммитов</td>
        <td class="border border-border px-3 py-2">Крошечный, в основном текстовый</td>
        <td class="border border-border px-3 py-2">У всех — это только что случилось</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">3+ дня</td>
        <td class="border border-border px-3 py-2">Десятки коммитов</td>
        <td class="border border-border px-3 py-2">~12× времени на разрешение</td>
        <td class="border border-border px-3 py-2">Угасает</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Недели</td>
        <td class="border border-border px-3 py-2">Сотни коммитов</td>
        <td class="border border-border px-3 py-2">Семантические + текстовые, big-bang</td>
        <td class="border border-border px-3 py-2">Ни у кого — код менялся под вами</td>
      </tr>
    </tbody>
  </table>
</div>

## Частота — рычаг; инструменты — погрешность округления

Команды в боли от слияний первым делом тянутся к лучшему merge-инструменту. Это не та ручка. Более умный трёхсторонний дифф снижает цену разрешения *данного* конфликта, но не может снизить его *размер* — он зафиксирован тем, насколько уехал trunk, а это зафиксировано тем, сколько вы ждали. Три недели расхождения — это три недели расхождения, разрешаете вы их в vim или в навороченном GUI. Рычаг, который реально двигает цену, — тот, что управляет разрывом: **интегрируйся чаще.**

Это переосмысляет «непрерывную интеграцию» — не как «у нас есть CI-сервер». CI — это *практика*: каждый разработчик мёржит малые изменения в общий trunk хотя бы ежедневно — а сервер лишь гейт, делающий практику безопасной. Принцип малых партий из lean проявляется здесь точь-в-точь: малые партии вскрывают проблемы рано и дёшево; большие прячут их, пока они не сдетонируют вместе. Ежедневное слияние — малая партия. Трёхнедельная ветка — одна огромная партия, которую нельзя «расвыкатить».

<Inset kind="why" lang="ru">
«Интегрируйся ежедневно» звучит как занудство про продуктивность, но на самом деле это утверждение о задержке обратной связи. Каждый час, что ваша ветка расходится с trunk, — это час решений, принимаемых на устаревшей информации: вы вызываете функции, которых больше нет, опираетесь на инварианты, которые только что сломали. Сокращение времени жизни ветки сокращает окно, в котором вы можете ошибаться, не зная об этом. В этом вся игра.
</Inset>

<TradeoffMatrix
  id="01-integration-frequency-tradeoff-1"
  lessonSlug="01-integration-frequency"
  lang="ru"
  prompt="Слияния в вашей команде стабильно болезненны. Трёхнедельная ветка только что заняла у двух инженеров целый день, чтобы её приземлить. Какое изменение даёт наибольший рычаг?"
  options={[
    { name: "Сократить время жизни ветки — разбить работу на изменения, интегрирующиеся в trunk хотя бы ежедневно", summary: "Бить по разрыву у источника.", correct: true, justification: "Цена слияния — функция того, насколько уехал trunk за время жизни ветки, а разрыв растёт лавинообразно, потому что все продолжают коммитить. Единственный рычаг, снижающий цену, а не усилие на каждый конфликт, — сократить время жизни: интегрируйся ежедневно, и каждый конфликт — это разрыв в один день, малый и разрешимый с полным контекстом." },
    { name: "Внедрить более умный merge/diff-инструмент с лучшим разрешением конфликтов", summary: "Инструмент вместо частоты.", justification: "Лучший инструмент снижает усилие на разрешение данного конфликта, но не его размер, который задан разрывом. Три недели расхождения остаются тремя неделями независимо от инструмента — это лечит симптом, а не причину." },
    { name: "Добавить сеньора-ревьюера, который хорошо распутывает большие слияния", summary: "Героизм вместо частоты.", justification: "Прогон всех болезненных слияний через одного эксперта создаёт бутылочное горло и bus factor и всё равно не уменьшает диффы. Боль повторяется на каждой ветке; вы спрятали её за человеком, а не устранили." },
    { name: "Назначить еженедельный «день интеграции», когда все мёржат вместе", summary: "Партии из партий.", justification: "Еженедельная синхронизация — это всё ещё недельная партия, вы просто синхронизировали big-bang. Это снижает неожиданность по времени, но не разрыв; ежедневная интеграция означает, что каждая ветка примиряет лишь день работы." },
  ]}
/>

<Quiz
  id="01-integration-frequency-quiz-1"
  lessonSlug="01-integration-frequency"
  lang="ru"
  question="Почему крошечное изменение на 50 строк может оказаться тяжелее для слияния, чем большое изменение, закоммиченное в тот же час, когда вы ответвились?"
  choices={[
    { label: "Цена слияния задаётся тем, насколько уехал trunk с момента ответвления, а не размером диффа — малое изменение поверх недель разрыва примиряет недели чужой работы", correct: true },
    { label: "Git берёт больше за слияние старых веток независимо от содержимого", misconception: "Алгоритм git не штрафует за возраст напрямую. С возрастом растёт объём расходящихся коммитов, которые надо примирить, и потерянный человеческий контекст — а не временна́я цена в инструменте." },
    { label: "Малые изменения рискованнее, потому что их недостаточно ревьюят", misconception: "Глубина ревью здесь ни при чём. Суть в том, что тот же малый дифф дёшев для слияния, когда trunk не сдвинулся, и дорог, когда trunk недели расходился под ним." },
    { label: "Большие слияния в тот же час тайно откладывают свою цену на потом", misconception: "Нет отложенной цены, когда trunk не сдвинулся — расхождение почти нулевое, так что слияние почти бесплатно. Цена появляется только по мере того, как trunk уезжает от точки ответвления." },
  ]}
/>

<Quiz
  id="01-integration-frequency-quiz-2"
  lessonSlug="01-integration-frequency"
  lang="ru"
  question="Что делает семантический конфликт опаснее текстового?"
  choices={[
    { label: "Обе стороны мёржатся чисто, без маркеров конфликта, так что git и merge-инструмент молчат, пока код уже тонко неправильный", correct: true },
    { label: "Семантические конфликты порождают больше маркеров конфликта для разрешения", misconception: "Наоборот — семантические конфликты порождают ноль маркеров. Слияние выглядит успешным; поломка проявляется только на сборке или в рантайме, что и делает её опасной." },
    { label: "Git отказывается мёржить, когда обнаруживает семантический разрыв", misconception: "Git не понимает смысла. Он примиряет текст. Переименование плюс новое место вызова к старому имени могут слиться чисто и сломаться — git не видит противоречия." },
    { label: "Они случаются только в динамически типизированных языках", misconception: "Они случаются везде. Система типов ловит часть на компиляции, но изменённые инварианты, поменявшаяся семантика функции или поведенческие допущения проскальзывают независимо от типизации." },
  ]}
/>

<DragOrder
  id="01-integration-frequency-drag-1"
  lessonSlug="01-integration-frequency"
  lang="ru"
  prompt="Упорядочьте, как цена слияния нарастает на долгоживущей ветке:"
  items={[
    { id: "a", label: "Вы ответвляетесь от trunk; расхождение нулевое, слияние было бы бесплатным" },
    { id: "b", label: "Проходят дни; коллеги мёржат десятки коммитов в trunk" },
    { id: "c", label: "Код, от которого вы зависите, переименовали или поменяли его инварианты" },
    { id: "d", label: "Ваша ветка теперь несёт устаревшие допущения, которых вы не видите" },
    { id: "e", label: "День слияния: текстовые + семантические конфликты детонируют вместе, контекста нет ни у кого" },
  ]}
  correctOrder={["a", "b", "c", "d", "e"]}
/>

</Explanation>

<KeyTakeaway>Цена слияния — функция разрыва, того, насколько уехал trunk с момента ответвления, и разрыв растёт лавинообразно, потому что все продолжают коммитить. Ежедневная интеграция не предотвращает конфликты; она ограничивает каждый разрывом в один день, малым и разрешимым с полным контекстом. Частота — рычаг; лучшие инструменты почти не двигают цену.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="01-integration-frequency-retrieval"
  lang="ru"
  questions={[
    { q: "Коллега говорит: «большие фичи тяжело мёржить, потому что они большие». Поправьте модель и объясните, что на самом деле определяет цену слияния.", a: "Размер диффа — не двигатель, двигатель — расхождение. Цена слияния задаётся тем, насколько trunk ушёл от точки, где вы ответвились, потому что каждый конфликт, переименование и изменённый инвариант, которые надо примирить, идут из разрыва trunk, а не из ваших строк. Фича на 2000 строк, слитая в тот же час, когда вы ответвились, почти ничего не стоит, потому что trunk не сдвинулся; фикс на 50 строк, слитый три недели спустя, может стать мучением, потому что лежит на фундаменте, перестроенном под ним. И разрыв растёт лавинообразно, потому что интеграция общая: пока живёт ваша ветка, все остальные продолжают коммитить, так что ветка в один день примиряет один день чужой работы, а трёхнедельная ветка — три недели плюс потерянный контекст. Microsoft измерила ~12× более долгое разрешение конфликтов после трёх дней без интеграции. Лекарство — сократить время жизни ветки, интегрироваться ежедневно, чтобы каждое слияние примиряло лишь малый, свежий срез разрыва." },
    { q: "Почему «интегрируйся ежедневно» — это на самом деле утверждение о задержке обратной связи, и как сюда вписываются семантические конфликты?", a: "Каждый час, что ваша ветка расходится с trunk, — это час, проведённый за принятием решений на устаревшей информации: вы вызываете функции, которые могли переименовать, опираетесь на инварианты, которые могли только что сломать. Опасность — не только текстовые конфликты, которые git помечает шумно и останавливает вас, а семантические конфликты, где обе стороны мёржатся чисто без маркеров, но код теперь неправильный (кто-то переименовал функцию, а вы добавили вызов к старому имени; сборка падает, или перегрузка тихо ведёт себя не так). Семантические конфликты масштабируются с той же переменной разрыва: чем сильнее изменился trunk, пока вас не было, тем больше скрытых допущений вашей ветки тихо стали ложными. Ежедневная интеграция ограничивает эту экспозицию одним днём — ваш CI-гейт ловит сломанное допущение, пока вызвавшее его изменение ещё свежо у кого-то в памяти, вместо того чтобы дать неделям невидимого разрыва сдетонировать разом. Сокращение времени жизни ветки сокращает окно, в котором вы можете ошибаться, не зная об этом." },
  ]}
/>

<Recap lang="ru">
Цена слияния ветки управляется разрывом — тем, насколько уехал trunk с момента ответвления, — а не размером вашего собственного изменения. Разрыв растёт лавинообразно, потому что интеграция общая: каждый другой инженер продолжает коммитить, пока живёт ваша ветка, так что ветка в один день примиряет день работы, а трёхнедельная — три недели плюс потерянный контекст, и именно поэтому время разрешения конфликтов растёт примерно в 12× после трёх дней и может съедать четверть времени разработчиков. Текстовые конфликты — дешёвые и шумные; дорогие провалы — это семантические конфликты, которые мёржатся чисто и ломаются тихо, и они масштабируются с тем же разрывом. Лучший merge-инструмент снижает усилие на конфликт, но никогда — его размер, поэтому единственный реальный рычаг — частота: интегрируйся в trunk хотя бы ежедневно, держи ветки живущими часы, а не недели, и относись к малым партиям как к механизму, который вскрывает проблемы рано, а не детонирует их вместе. Непрерывная интеграция — это и есть та практика, а сервер лишь гейт, делающий её безопасной.
</Recap>
