---
concepts:
- flag-debt
- combinatorial-explosion
- flag-lifecycle
- progressive-delivery
- dora-metrics
- cleanup-discipline
deepensInto: []
estMin: 17
lang: ru
lessonType: topic
level: senior
order: 5
prereqs:
- 04-the-green-trunk-gate
slug: 05-flag-debt-and-rollout-discipline
sources:
- https://martinfowler.com/articles/feature-toggles.html
- https://www.getunleash.io/blog/feature-flag-management-technical-debt
- https://launchdarkly.com/blog/what-is-progressive-delivery-all-about/
- https://dora.dev/research/2024/
spiral:
- blast-radius
- incremental-change
- feedback-loops
status: ready
summary: 'Trunk-based меняет долг веток на долг флагов: release-флаг, оставленный
  после раскатки, — это долгоживущая ветка в if, а N забытых флагов дают 2^N непротестированных
  конфигураций. Окупает практику дисциплина уборки: удаляй ветку при merge, флаг —
  при полной раскатке.'
title: Долг флагов и дисциплина выкатки
track: engineering-practice
unit: 04-trunk-based
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";

<Hook>
Команда, перешедшая на trunk-based два года назад, открывает свой дашборд флагов: 400 флагов, и никто не может сказать, какие ещё важны. Флаг «new checkout» уже год на 100% — но старый путь кода всё ещё там, за `else`, непротестированный, изредка задеваемый опечаткой в конфиге. Инцидент трассируется к двум флагам, чью комбинацию никто никогда не пробовал. Они сбежали из ада слияний и зашли прямиком в другую тюрьму: флаги, которые они так и не удалили. Trunk-based не убрал долг. Он сменил его форму.
</Hook>

<Crux>Trunk-based меняет долг веток на долг флагов; флаг после выкатки — долгоживущая ветка в if, и зачистка делает практику окупаемой.</Crux>

<Explanation>

## Долг не исчез — он сменил форму

Прошлые уроки сделали флаги героем: они отвязывают деплой от релиза и делают ежедневную интеграцию возможной. Сеньорская оговорка в том, что каждый флаг — это ещё и **ветка в вашем рантайме**. Долгоживущая git-ветка расщепляет вашу кодовую базу на расходящиеся истории; долгоживущий флаг расщепляет вашу *работающую систему* на расходящиеся пути исполнения. Вы переместили развилку из системы контроля версий в оператор `if`, который труднее увидеть и труднее удалить, потому что он живёт в проде и кто-то может на него опираться.

Так что release-флаг, переживший свою фичу, — не нейтральный остаток, это долгоживущая ветка в маскировке, с той же проблемой разрыва, всплывающей как непротестированные пути кода. Старый путь за `else` гниёт: никто его не запускает, никто не обновляет, но он всё ещё достижим. Trunk-based приносит свою выгоду, только если вы удаляете флаги так же агрессивно, как удаляете ветки: **удали ветку при слиянии, удали флаг при полной выкатке.** Зачистка — не гигиена, которую делают, когда есть время; это вторая половина сделки, которая делает всю практику net-positive.

## Почему устаревшие флаги взрываются комбинаторно

Цена долга флагов не линейна по числу флагов — она экспоненциальна. Каждый независимый булев флаг удваивает число возможных рантайм-конфигураций вашей системы. С 3 флагами у вас 2³ = 8 конфигураций; с 10 флагами — 1024; с 20 — более миллиона. Вы тестируете горстку этих путей и выкатываете; остальные живы, но непроверены. Большинство инцидентов от долга флагов — это **баги взаимодействия**: флаг A в порядке, флаг B в порядке, но состояние, где оба включены (или один включён, а другой полу-разогнан), никогда не прогонялось и делает что-то не так. Это та же проблема 2^N непротестированных конфигураций, которую вызвали бы устаревшие ветки, если бы вы держали их все живыми — она просто прячется в условиях вместо `git branch -a`.

Вот почему таксономия времени жизни из урока 3 несущая. Release-флаги должны быть короткоживущими именно потому, что это они тихо становятся постоянными. Смягчение — дисциплина жизненного цикла: дайте каждому release-флагу владельца и срок годности, отслеживайте возраст флага так же, как возраст ветки, валите сборку или алертите, когда release-флаг переживает свою ожидаемую жизнь, и сделайте «убрать флаг и мёртвый путь» обязательным завершающим шагом каждой выкатки — а не опциональным follow-up тикетом, который никогда не приоритизируется.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Активные release-флаги</th>
        <th class="border border-border px-3 py-2 text-left">Возможные рантайм-конфиги (2^N)</th>
        <th class="border border-border px-3 py-2 text-left">Реалистично протестировано</th>
        <th class="border border-border px-3 py-2 text-left">Поверхность багов взаимодействия</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">3</td>
        <td class="border border-border px-3 py-2">8</td>
        <td class="border border-border px-3 py-2">Несколько</td>
        <td class="border border-border px-3 py-2">Малая, управляемая</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">10</td>
        <td class="border border-border px-3 py-2">1024</td>
        <td class="border border-border px-3 py-2">Всё ещё несколько</td>
        <td class="border border-border px-3 py-2">Большинство конфигов никогда не бегут</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">20</td>
        <td class="border border-border px-3 py-2">&gt; 1 000 000</td>
        <td class="border border-border px-3 py-2">Исчезающая доля</td>
        <td class="border border-border px-3 py-2">Баги взаимодействия неизбежны</td>
      </tr>
    </tbody>
  </table>
</div>

## Progressive delivery и честное прочтение данных

Сделанный правильно, жизненный цикл флага — это ещё и техника доставки: **progressive delivery** — это дисциплина релиза через контролируемые, измеряемые стадии — внутренние → 1% → canary-когорта → 100% — с автоматическими ограждениями, которые останавливают или откатывают рампу, если частота ошибок, задержка или бизнес-метрики регрессируют. Флаг — это исполнительный механизм; метрики — контроллер. Это та операционная зрелость, к которой строит trunk-based: релиз как замкнутый контур обратной связи, а не односторонний пуш.

Стоит также быть точным в том, что доказывают знаменитые числа. Исследование DORA стабильно ставит trunk-based development среди сильнейших *коррелятов* элитной доставки — отчёт 2024 года (39 000+ респондентов) нашёл, что лишь ~19% команд достигают элиты, а элитные исполнители деплоят порядка в 182× чаще, с в 127× более быстрым lead time и куда более низкой частотой отказов изменений, и заметно чаще практикуют trunk-based. Но корреляция идёт через предпосылки: trunk-based приносит результат, только когда быстрый зелёный гейт, флаги *и* дисциплина зачистки — все присутствуют. Примите только модель ветвления — и получите режимы отказа из этих пяти уроков: разрыв без слияний, если ветки залёживаются, красный trunk, если гейт отсутствует, или комбинаторное болото флагов, если зачистку пропустили. Рычаг — не модель веток; это вся дисциплинированная система вокруг неё.

<Inset kind="why" lang="ru">
Чистая симметрия этого юнита: долгоживущая ветка и устаревший флаг — это один и тот же баг в двух местах. Оба — развилки, которые должны были быть временными, оба накапливают непротестированное расхождение, и оба налагают цену 2^N, если дать им накопиться — ветки в вашей истории, флаги в вашем рантайме. Trunk-based development в корне — это единая дисциплина, применяемая к обоим: держи развилки короткоживущими и удаляй их в тот момент, когда они отслужили своё. Получи эту дисциплину — и числа DORA последуют; пропусти её — и ты просто переместил бардак.
</Inset>

<TradeoffMatrix
  id="05-flag-debt-tradeoff-1"
  lessonSlug="05-flag-debt-and-rollout-discipline"
  lang="ru"
  prompt="Фича на 100% выкатки и стабильна две недели. Release-флаг всё ещё в коде. Что должно произойти?"
  options={[
    { name: "Удалить флаг и теперь-мёртвый старый путь кода как завершающий шаг выкатки", correct: true, justification: "Release-флаг — временные леса; оставленный, он долгоживущая ветка в if — старый путь гниёт непротестированным и добавляет к поверхности багов взаимодействия 2^N. Удаление флага и мёртвого пути — обязательный финальный шаг, делающий trunk-based net-positive, рантайм-эквивалент удаления ветки после слияния." },
    { name: "Оставить на месте на случай, если придётся откатиться позже", summary: "Страховочный флаг.", justification: "После двух стабильных недель на 100% «на всякий случай» — это как флаги становятся постоянными. Старый путь уже гниёт; если вам действительно нужна обратимость, это решение деплой/revert, а не причина держать непротестированную ветку живой в проде вечно." },
    { name: "Завести тикет на зачистку на следующий квартал и идти дальше", summary: "Отложенная зачистка.", justification: "Отложенная зачистка флагов — механизм, которым команды доходят до 400 устаревших флагов. Тикеты, не являющиеся завершающим шагом выкатки, депроиоритизируются бесконечно; зачистка должна быть частью «готово», а не follow-up-ом." },
    { name: "Превратить его в постоянный kill switch ради безопасности", summary: "Подмена типа.", justification: "Release-флаг — не ops-флаг; переклеивание ярлыка на постоянный kill switch держит мёртвый старый путь живым под новым оправданием. Если подсистеме действительно нужен kill switch, спроектируйте его осознанно; не отмывайте остаточные release-леса в постоянство." },
  ]}
/>

<Quiz
  id="05-flag-debt-quiz-1"
  lessonSlug="05-flag-debt-and-rollout-discipline"
  lang="ru"
  question="Почему release-флаг, оставленный включённым после полной выкатки, описывают как «долгоживущую ветку, спрятанную в if»?"
  choices={[
    { label: "Это развилка, которая должна была быть временной — она расщепляет работающую систему на расходящиеся пути, старый гниёт непротестированным, ровно как устаревшая ветка расщепляет историю", correct: true },
    { label: "Потому что флаги буквально реализованы как git-ветки", misconception: "Флаги — рантайм-условия, а не VCS-ветки. Аналогия структурная: оба — временные развилки, оставленные живыми, накапливающие непротестированное расхождение — одна в истории, одна в работающей системе." },
    { label: "Потому что флаг делает ветку невозможной для удаления", misconception: "Флаг не блокирует ветку. Суть в том, что сам флаг ведёт себя как долгоживущая ветка — развилка в рантайме, чья неиспользуемая сторона гниёт, с той же ценой разрыва." },
    { label: "Потому что он замедляет CI, как долгоживущая ветка", misconception: "Проблема не в скорости CI — а в том, что устаревший флаг держит расходящийся непротестированный путь кода живым в проде, так же как долгоживущая ветка держит расходящуюся непротестированную историю." },
  ]}
/>

<Quiz
  id="05-flag-debt-quiz-2"
  lessonSlug="05-flag-debt-and-rollout-discipline"
  lang="ru"
  question="Почему долг флагов описывают как экспоненциальный, а не линейный по числу флагов?"
  choices={[
    { label: "Каждый независимый булев флаг удваивает возможные рантайм-конфигурации (2^N), так что большинство комбинаций остаются непротестированными и баги взаимодействия становятся неизбежными", correct: true },
    { label: "Каждый флаг добавляет фиксированный объём кода, суммирующийся линейно", misconception: "Если бы это были просто суммируемые строки, было бы линейно. Экспоненциальная цена идёт от конфигураций: N независимых булевых дают 2^N состояний, а вы можете протестировать лишь горстку — непротестированные комбинации это где живут баги взаимодействия." },
    { label: "Флаги замедляют систему мультипликативно в рантайме", misconception: "Цена не в производительности рантайма — а в комбинаторном пространстве непротестированных конфигураций. 20 флагов — это более миллиона возможных состояний; взрыв в том, что вы не можете проверить, а не в задержке." },
    { label: "Каждый флаг требует тестирования каждого другого флага попарно (N²)", misconception: "Это хуже, чем попарно — реальное пространство это все комбинации, 2^N, а не только пары. Попарное тестирование — одно смягчение, но базовый взрыв экспоненциален." },
  ]}
/>

<DragOrder
  id="05-flag-debt-drag-1"
  lessonSlug="05-flag-debt-and-rollout-discipline"
  lang="ru"
  prompt="Упорядочьте полный жизненный цикл дисциплинированного release-флага:"
  items={[
    { id: "a", label: "Создать release-флаг с владельцем и датой истечения" },
    { id: "b", label: "Выкатить работу на trunk тёмной за выключенным флагом, интегрируясь ежедневно" },
    { id: "c", label: "Постепенно разогнать 1% → 100% с метрик-ограждениями, способными авто-остановить" },
    { id: "d", label: "Подержать на стабильных 100% коротко, чтобы подтвердить отсутствие регрессии" },
    { id: "e", label: "Удалить флаг и мёртвый старый путь как завершающий шаг выкатки" },
  ]}
  correctOrder={["a", "b", "c", "d", "e"]}
/>

</Explanation>

<KeyTakeaway>Trunk-based меняет долг веток на долг флагов: release-флаг, оставленный включённым после выкатки, — это долгоживущая ветка в if, со старым путём, гниющим непротестированным, а N устаревших флагов создают 2^N непротестированных рантайм-конфигураций, где баги взаимодействия неизбежны. Дисциплина, делающая всю практику net-positive, — это зачистка: удали ветку при слиянии, удали флаг при выкатке — выполняемая как завершающий шаг прогрессивно-доставленной, метрик-защищённой рампы. Элитные числа DORA коррелируют с trunk-based, только когда гейт, флаги и зачистка — все присутствуют.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="05-flag-debt-retrieval"
  lang="ru"
  questions={[
    { q: "Объясните утверждение, что trunk-based «меняет долг веток на долг флагов», и почему эта сделка стоит того только с дисциплиной зачистки.", a: "Флаги — герой trunk-based: они отвязывают деплой от релиза, так что ежедневная интеграция многонедельной работы возможна — но каждый флаг ещё и ветка в вашем рантайме. Долгоживущая git-ветка расщепляет кодовую базу на расходящиеся истории; долгоживущий флаг расщепляет работающую систему на расходящиеся пути исполнения. Так release-флаг, переживший свою фичу, — это долгоживущая ветка в if: старый путь за else гниёт непротестированным, но остаётся достижимым, и та же проблема разрыва всплывает как живой непроверенный код. Цена экспоненциальна, не линейна — каждый независимый булев флаг удваивает рантайм-конфигурации (3 флага = 8, 10 = 1024, 20 = более миллиона), вы тестируете горстку и выкатываете, а непротестированные комбинации — где живут баги взаимодействия (A в порядке, B в порядке, оба-включены никогда не пробовали). Это та же проблема 2^N непротестированных конфигураций, которую вызвали бы устаревшие ветки, перемещённая в условия. Так что trunk-based net-positive, только если вы удаляете флаги так же агрессивно, как ветки: удали ветку при слиянии, удали флаг при полной выкатке. Зачистка — вторая половина сделки, не опциональная гигиена — и она должна быть завершающим шагом выкатки, потому что отложенные тикеты на зачистку — как команды доходят до 400 устаревших флагов." },
    { q: "Что данные DORA на самом деле доказывают про trunk-based, и какова честная причинная история?", a: "DORA стабильно ставит trunk-based development среди сильнейших коррелятов элитной доставки — отчёт 2024 года (39 000+ респондентов) нашёл, что лишь около 19% команд достигают элиты, при этом элитные исполнители деплоят порядка в 182× чаще, с в 127× более быстрым lead time и куда более низкой частотой отказов изменений, и заметно чаще практикуют trunk-based. Но это корреляция, и причинность идёт через предпосылки, а не через модель ветвления саму по себе. Trunk-based приносит результат, только когда присутствуют все три вещи: быстрый зелёный CI-гейт, держащий общий trunk релизуемым, флаги, отвязывающие деплой от релиза, и дисциплина зачистки, держащая короткоживущими и ветки, и флаги. Примите только модель ветвления — и получите режимы отказа вместо этого: разрыв и big-bang слияния, если ветки залёживаются, постоянно красный trunk, если гейт отсутствует или медленный, или комбинаторное болото флагов, если зачистку пропустили. Рычаг — вся дисциплинированная система; «мы используем trunk-based» без гейта, флагов и зачистки предсказывает режимы отказа, а не элитные числа." },
  ]}
/>

<Recap lang="ru">
Trunk-based development не устраняет долг — он меняет его форму с долга веток на долг флагов, потому что каждый флаг — это ветка в вашем рантайме: release-флаг, оставленный включённым после полной выкатки, — это долгоживущая ветка в if, со старым путём, гниющим непротестированным, но всё ещё достижимым. Цена экспоненциальна, поскольку каждый независимый булев флаг удваивает рантайм-конфигурации — 20 флагов это более миллиона состояний, из которых вы тестируете горстку — так что баги взаимодействия (A в порядке, B в порядке, оба-включены никогда не пробовали) становятся неизбежными, та же проблема 2^N непротестированных конфигураций, которую вызвали бы устаревшие ветки, перемещённая в условия. Дисциплина, делающая всю практику net-positive, — это зачистка, выполняемая как завершающий шаг выкатки: дайте release-флагам владельцев и сроки годности, отслеживайте возраст флага как возраст ветки и удаляйте флаг и мёртвый путь в тот момент, когда фича стабильна на 100% — в идеале как конец рампы progressive delivery, где метрики защищают каждый шаг и могут авто-остановить. А числа DORA — ~19% элиты, 182× частота деплоя, 127× более быстрый lead time — коррелируют с trunk-based, только когда быстрый зелёный гейт, флаги и дисциплина зачистки — все присутствуют; рычагом никогда не была модель ветвления сама по себе, а дисциплинированная система вокруг неё, держащая каждую развилку, в истории или в рантайме, короткоживущей.
</Recap>