---
concepts:
- feature-flags
- branch-by-abstraction
- ci-gate
- merge-queue
- dark-launch
estMin: 14
lang: ru
level: senior
order: 9
prereqs: []
slug: quiz-code
sources:
- https://martinfowler.com/bliki/BranchByAbstraction.html
- https://martinfowler.com/articles/feature-toggles.html
- https://github.blog/engineering/engineering-principles/how-github-uses-merge-queue-to-ship-hundreds-of-changes-every-day/
status: ready
summary: "Читайте реальные диффы и CI-конфиг, предсказывайте поведение по trunk-based и выбирайте фикс с наибольшим рычагом — обёртка флагом, branch by abstraction и зелёный гейт."
title: 'Trunk-based: чтение кода и конфигов'
track: engineering-practice
unit: 04-trunk-based
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Дисциплина trunk-based видна в диффе и CI-конфиге, а не в манифесте. Читайте каждый сниппет, предсказывайте, как он поведёт себя на общем trunk, и выбирайте фикс, который senior-инженер сделает первым.
</Hook>

<Goal lang="ru">
Потренируйтесь читать артефакты, которые решают, действительно ли команда trunk-based: как незавершённая работа обёрнута, чтобы ехать dark, как большое изменение едет на trunk через абстракцию и как настроен гейт, держащий trunk зелёным.
</Goal>

## Сниппет 1 — отправка незавершённой работы в trunk

```ts
// Новый checkout-флоу не готов, но должен влиться в trunk сегодня.
export async function handleCheckout(cart: Cart, user: User) {
  if (flags.isEnabled("checkout-v2", { userId: user.id })) {
    return newCheckout(cart, user);   // недостроен, ещё нет расчёта налога
  }
  return legacyCheckout(cart, user);  // текущий продакшен-путь
}
```

<Quiz
  id="04-trunk-based-code-1"
  lessonSlug="04-trunk-based"
  lang="ru"
  question="Флаг 'checkout-v2' по умолчанию выключен в продакшене. Что верно, если этот код вливается в trunk ежедневно?"
  choices={[
    { label: "Незавершённый newCheckout едет в продакшен dark — интегрирован, собран и протестирован CI, но недостижим пользователями, — так что работа вливается ежедневно с нулевым дрейфом и без долгоживущей ветки.", correct: true },
    { label: "Вливать это небезопасно, ведь недостроенный код теперь в продакшене.", misconception: "Deployed — не released. Флаг выключен, поэтому пользователи попадают в legacyCheckout; новый путь присутствует, но dark. Именно так trunk-based безопасно отправляет незавершённую работу." },
    { label: "Это сработает, только если newCheckout полностью готов до merge.", misconception: "Весь смысл флага в том, что newCheckout может быть незавершён. Он вливается ежедневно за выключенным флагом и включается только когда готов — release отделён от deploy." },
    { label: "Проверку флага лучше держать на долгоживущей feature-ветке.", misconception: "Флаг заменяет долгоживущую ветку. Размещение условия на trunk и позволяет работе вливаться ежедневно, не накапливая дрейф." },
  ]}
/>

## Сниппет 2 — шестинедельная замена ORM на trunk

```ts
interface UserStore {                 // абстракция
  find(id: string): Promise<User>;
  save(u: User): Promise<void>;
}

class LegacyOrmStore implements UserStore { /* текущий, в работе */ }
class NewOrmStore    implements UserStore { /* строится инкрементально, dark */ }

// связано один раз, переключается на границе, когда NewOrmStore готов
export const userStore: UserStore =
  flags.isEnabled("orm-v2") ? new NewOrmStore() : new LegacyOrmStore();
```

<Quiz
  id="04-trunk-based-code-2"
  lessonSlug="04-trunk-based"
  lang="ru"
  question="Почему такая форма branch by abstraction — это trunk-based ответ на миграцию, которую нельзя выпустить за один день?"
  choices={[
    { label: "Абстракция позволяет строить новую реализацию за ней маленькими ежедневно вливаемыми коммитами, пока старая в работе; вы переключаете на границе, проверяете, затем удаляете старый путь — шестинедельное изменение живёт на trunk как маленькие зелёные шаги.", correct: true },
    { label: "Это сработает, только если сначала завести долгоживущую ветку orm-v2 под NewOrmStore.", misconception: "Суть обратная: NewOrmStore строится на trunk за абстракцией, а не на долгоживущей ветке. Абстракция и убирает потребность в ветке." },
    { label: "Абстракция — чистый оверхед; прямое переписывание было бы проще.", misconception: "Прямое переписывание вынуждает либо долгоживущую ветку, либо сломанный trunk. Абстракция держит trunk релизопригодным всё время и позволяет переключить или откатить на одной границе." },
    { label: "Слой абстракции нужно держать навсегда после завершения замены.", misconception: "После проверки нового пути вы удаляете старую реализацию, а затем ставшую лишней абстракцию — оставить её значит создать свою же оставленную обвязку." },
  ]}
/>

## Сниппет 3 — конфиг CI-гейта

```yaml
# .ci/trunk-gate.yml — запускается на каждый PR
on: pull_request
jobs:
  gate:
    steps:
      - run: make unit-tests          # ~2 мин, блокирует merge
      - run: make lint typecheck      # ~1 мин, блокирует merge
  e2e:
    steps:
      - run: make e2e-suite           # ~40 мин
        if: github.event_name == 'schedule'   # только ночью, никогда не блокирует
```

<Quiz
  id="04-trunk-based-code-3"
  lessonSlug="04-trunk-based"
  lang="ru"
  question="Читая этот гейт, какая senior-оценка верна?"
  choices={[
    { label: "Грамотная многоуровневость: быстрые unit + lint/typecheck (~3 мин) блокируют merge, чтобы гейт оставался достаточно быстрым и интегрировать мелко и часто было путём наименьшего сопротивления, а 40-минутный e2e-набор идёт ночью вне критического пути.", correct: true },
    { label: "Сломано — e2e-набор должен блокировать каждый merge, иначе trunk сломается.", misconception: "Блокирующий 40-минутный набор подтолкнул бы инженеров батчить изменения, удлиняя ветки и возвращая дрейф. Многоуровневость — быстрые проверки до merge, медленные после/ночью — стандартный способ держать гейт быстрым." },
    { label: "Нормально, пока инженеры помнят запускать e2e локально перед merge.", misconception: "Опора на память — не гейт. Выбор намеренный: блокировать на быстрых проверках, медленные гонять ночью, а редкий ночной сбой лечить через stop-the-line плюс bisect." },
    { label: "Гейт слишком строгий, потому что lint и typecheck блокируют merge.", misconception: "Быстрые статические проверки, блокирующие merge, — ровно то, что дёшево держит trunk зелёным. Управлять надо медлительностью, а не строгостью — а эти проверки идут секунды-минуты." },
  ]}
/>

## Сниппет 4 — оставленный устаревший флаг

```ts
// выпущен 14 месяцев назад, "search-v2" всё это время был на 100%
function rankResults(q: Query, results: Result[]) {
  if (flags.isEnabled("search-v2")) {
    return rankV2(q, results);        // живой путь
  }
  return rankV1(q, results);          // не запускается, не обновлялся с запуска
}
```

<Quiz
  id="04-trunk-based-code-4"
  lessonSlug="04-trunk-based"
  lang="ru"
  question="search-v2 был на 100% уже 14 месяцев. Какое действие имеет наибольший рычаг и почему?"
  choices={[
    { label: "Удалить флаг и мёртвый путь rankV1. Release flag, оставленный после полной раскатки, — это долгоживущая ветка, спрятанная в if: rankV1 гниёт непротестированным, но достижим через опечатку в конфиге, и добавляет к 2^N поверхности interaction-багов.", correct: true },
    { label: "Оставить как постоянный kill switch на случай регресса search-v2.", misconception: "Это type laundering — переименование оставленной release-обвязки в постоянный ops-флаг держит гниющий старый путь живым. Если kill switch правда нужен, спроектируйте его осознанно." },
    { label: "Завести тикет на очистку в следующем квартале и идти дальше.", misconception: "Отложенная очистка флагов — ровно то, как команды доходят до 400 устаревших флагов. Удаление должно быть закрывающим шагом раскатки, частью «готово», а не follow-up, который деприоритизируют." },
    { label: "Ничего — флаг на стабильном 100% это безвредный мёртвый код.", misconception: "Он не безвреден: rankV1 всё ещё достижим (опечатка в конфиге может направить туда), не поддерживается и это ещё один булев флаг в комбинаторном пространстве конфигов. Release flag на 100% нужно удалять." },
  ]}
/>

<Recap lang="ru">
Дисциплина trunk-based читаема в артефактах. Условие с флагом на trunk позволяет незавершённой работе ехать dark и вливаться ежедневно без дрейфа; интерфейс-абстракция позволяет шестинедельной миграции ехать на trunk как маленькие зелёные коммиты с одним flip-and-delete; многоуровневый CI-гейт остаётся быстрым, блокируя на unit/lint/typecheck и гоняя медленный e2e вне критического пути; а release flag, всё ещё включённый на стабильном 100%, — это долгоживущая ветка, спрятанная в if, которую вы удаляете (флаг и мёртвый путь вместе) как закрывающий шаг раскатки.
</Recap>
