---
concepts:
- short-lived-branches
- gitflow
- github-flow
- branch-by-abstraction
- release-from-trunk
- three-branch-rule
deepensInto:
- 03-feature-flags-dark-launch
estMin: 16
lang: ru
lessonType: topic
level: middle
order: 2
prereqs:
- 01-integration-frequency
slug: 02-short-lived-branches-vs-gitflow
sources:
- https://dora.dev/capabilities/trunk-based-development/
- https://trunkbaseddevelopment.com/branch-for-release/
- https://martinfowler.com/bliki/BranchByAbstraction.html
- https://nvie.com/posts/a-successful-git-branching-model/
spiral:
- cost-of-delay
- incremental-change
status: ready
summary: Trunk-based — это короткоживущие ветки (три или меньше, живут меньше дня,
  без фазы интеграции), а не отсутствие веток. Gitflow хорош для плановых релизов,
  но насаждает разрыв при непрерывной доставке. Крупные рефакторинги живут в trunk
  через branch by abstraction.
title: Короткоживущие ветки против gitflow
track: engineering-practice
unit: 04-trunk-based
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
Команда читает «trunk-based development» и слышит «коммить прямо в main, без веток, без PR». Они отключают защиту веток. За неделю trunk краснеет дважды в день, незапрошенное изменение утекает с кредами, и они заключают, что trunk-based «не масштабируется». Они выбросили не то. Trunk-based никогда не был про удаление веток — он про то, чтобы держать их настолько короткоживущими, чтобы они не накапливали разрыв. Ветка живёт час, за зелёным гейтом и ревью, а потом её нет.
</Hook>

<Crux>Trunk-based означает короткоживущие ветки — три или меньше, исчезающих за день, без фазы интеграции — а не отсутствие веток.</Crux>

<Explanation>

## Что на самом деле задаёт «короткоживущая»

Определение trunk-based от DORA конкретно, а не «по ощущениям». Держите **три или меньше активных веток** в репозитории в любой момент. Ветки должны жить **не более одного дня** до слияния в trunk — часто всего несколько часов. **Нет заморозок кода и нет фаз интеграции.** Каждая ветка — это одно малое, узкое изменение, которое открывает PR, проходит быстрый CI-гейт, получает быстрое ревью и мёржится в тот же день. Ветка — это зона ожидания для ревью и гейтинга, а не место, где работа *живёт*.

Иногда это запускают как чистый «коммит в trunk» (вообще без веток, частое в малых co-located командах с парным программированием), а чаще как «короткоживущие feature-ветки» (версия, которую использует большинство, потому что она сохраняет код-ревью и PR-гейт). Обе — trunk-based. Разделительная черта не «ветка или не ветка» — это **время жизни**. Ветка, живущая часы, — trunk-based; ветка `feature/*`, живущая две недели, — это долгоживущая ветка, ради устранения которой существует практика, как бы вы ни называли свой workflow.

## Почему gitflow оптимизирован под противоположный мир

Gitflow даёт вам постоянные ветки `main` и `develop`, плюс ветки `feature/*`, `release/*` и `hotfix/*` с формальными фазами: фичи интегрируются в `develop`, ветка `release` стабилизируется, затем мёржится в `main` и тегается. Это связная модель — для мира, под который она была спроектирована: **«коробочный», версионированный софт с плановыми релизами**, несколько поддерживаемых в поле версий и без непрерывного развёртывания. В том мире явная фаза стабилизации и параллельные релизные линии оправдывают себя.

Для непрерывно развёртываемого веб-сервиса gitflow институционализирует ровно тот разрыв, о котором предупреждал прошлый урок. Долгоживущие ветки `feature/*` накапливают расхождение; продвижение `develop`-в-`release`-в-`main` — это встроенная фаза интеграции; «готовые» фичи лежат в `develop` и гниют, пока ждут следующего релизного поезда. Вы наследуете big-bang слияния и заморозки кода по дизайну. Исследование DORA явно отмечает фазы интеграции и заморозки кода как анти-паттерны для производительности доставки — gitflow делает их несущими.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Измерение</th>
        <th class="border border-border px-3 py-2 text-left">Gitflow</th>
        <th class="border border-border px-3 py-2 text-left">GitHub flow</th>
        <th class="border border-border px-3 py-2 text-left">Trunk-based</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">Долгоживущие ветки</td>
        <td class="border border-border px-3 py-2">main + develop (+ release)</td>
        <td class="border border-border px-3 py-2">только main</td>
        <td class="border border-border px-3 py-2">только trunk</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Жизнь feature-ветки</td>
        <td class="border border-border px-3 py-2">Дни–недели</td>
        <td class="border border-border px-3 py-2">До слияния PR (варьируется)</td>
        <td class="border border-border px-3 py-2">Часы, &lt; 1 дня</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Фаза интеграции</td>
        <td class="border border-border px-3 py-2">Явная (release-ветка)</td>
        <td class="border border-border px-3 py-2">Нет</td>
        <td class="border border-border px-3 py-2">Нет</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Под что построен</td>
        <td class="border border-border px-3 py-2">Версионированные плановые релизы</td>
        <td class="border border-border px-3 py-2">Веб-приложения, непрерывный деплой</td>
        <td class="border border-border px-3 py-2">Непрерывный деплой в масштабе</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Релиз</td>
        <td class="border border-border px-3 py-2">Продвинуть develop → release → main</td>
        <td class="border border-border px-3 py-2">Развернуть слитый main</td>
        <td class="border border-border px-3 py-2">Тег/ветка от trunk в точке релиза</td>
      </tr>
    </tbody>
  </table>
</div>

## Релизить без долгоживущих веток

Возражение: «если есть только trunk, откуда берутся релизы, и как мне пропатчить старую версию?» У trunk-based есть конкретный ответ — **ветка для релиза, а не для фичи**. Когда вы нарезаете релиз, вы создаёте короткоживущую ветку `release/x.y` *от* trunk на этом коммите. Повседневная разработка на ней никогда не идёт. Если нужен критический фикс, вы чините его сначала на trunk (чтобы следующий релиз тоже его имел) и **cherry-pick**-аете единственный коммит на release-ветку. Release-ветка — это read-mostly снимок, а не параллельная линия разработки, так что она никогда не накапливает разрыв. Команды на непрерывном деплое часто пропускают даже это и просто тегают trunk.

Для больших изменений, которые действительно нельзя выкатить за день — замена ORM, смена платёжного провайдера — инструмент trunk-based — это **branch by abstraction**, а не долгоживущая ветка. Вы вводите слой абстракции над тем, что заменяете, инкрементально строите новую реализацию за ним на trunk (каждый шаг мёржится ежедневно, тёмный, пока не готов), переключаете абстракцию на новую реализацию, затем удаляете старую и саму абстракцию. Большой рефакторинг всё это время живёт на trunk как последовательность малых зелёных коммитов — никогда как трёхнедельная ветка.

<Inset kind="why" lang="ru">
Ловушка именования реальна: ветка `feature/*` в gitflow-репозитории и короткоживущая ветка в trunk-based репозитории могут выглядеть в git идентично. Разница невидима в ветке и видна только в её времени жизни и дисциплине вокруг. «Мы используем feature-ветки» не говорит вам ничего о том, trunk-based вы или нет — «наши ветки мёржатся или удаляются за день» говорит всё.
</Inset>

<TradeoffMatrix
  id="02-short-lived-branches-tradeoff-1"
  lessonSlug="02-short-lived-branches-vs-gitflow"
  lang="ru"
  prompt="Вы заменяете ORM в непрерывно развёртываемом сервисе. Миграция займёт шесть недель. Как сделать это по trunk-based?"
  options={[
    { name: "Branch by abstraction: ввести слой над доступом к данным, строить новый ORM за ним малыми ежедневно-сливаемыми шагами, переключить абстракцию, затем удалить старый путь", correct: true, justification: "Это держит шестинедельное изменение на trunk всё время как последовательность малых зелёных коммитов, каждый интегрируется ежедневно, тёмный до переключения. Вы никогда не накапливаете разрыв, команда продолжает строить на релизуемом trunk, и вы можете приостановиться или откатиться на границе абстракции. Это ответ trunk-based на «слишком большое, чтобы выкатить за день»." },
    { name: "Нарезать долгоживущую ветку migration/orm и слить её, когда миграция готова", summary: "Долгоживущая ветка.", justification: "Шесть недель расхождения в непрерывно развёртываемом сервисе — это ровно тот big-bang merge и детонация семантических конфликтов, ради избежания которых существует trunk-based. Остальная команда строит на trunk, о котором ваша ветка ничего не знает." },
    { name: "Перевести репозиторий на gitflow с выделенной release-веткой под миграцию", summary: "Неверная модель для мира.", justification: "Фазы стабилизации gitflow подходят плановым «коробочным» релизам, а не непрерывно развёртываемому сервису. Это институционализирует фазу интеграции и долгоживущую ветку, которых вы пытаетесь избежать." },
    { name: "Заморозить остальную работу над затронутыми модулями, пока миграция не приземлится", summary: "Заморозка кода.", justification: "Заморозки — анти-паттерн DORA: они стопорят команду и просто переносят боль интеграции на день разморозки. Branch by abstraction позволяет обычной работе продолжаться на trunk параллельно миграции." },
  ]}
/>

<Quiz
  id="02-short-lived-branches-quiz-1"
  lessonSlug="02-short-lived-branches-vs-gitflow"
  lang="ru"
  question="Где проходит настоящая разделительная черта между trunk-based и workflow с долгоживущими ветками?"
  choices={[
    { label: "Время жизни ветки — ветки trunk-based мёржатся или удаляются за день; модель про разрыв, а не про то, существуют ли ветки", correct: true },
    { label: "Используете ли вы ветки вообще — trunk-based их запрещает", misconception: "Большинство trunk-based команд используют короткоживущие feature-ветки, чтобы сохранить код-ревью и PR-гейт. Практика запрещает долгоживущие ветки, а не ветки как таковые." },
    { label: "Используете ли вы pull request-ы", misconception: "PR-ы ортогональны. Можно быть trunk-based с PR (частый случай) или с прямыми коммитами в trunk (парные команды). Отличающая переменная — время жизни, а не механизм PR." },
    { label: "Есть ли в репозитории ветка develop", misconception: "Ветка develop — артефакт gitflow, и её отсутствие наводит на мысль, но настоящий тест — время жизни: одна feature-ветка, оставленная живой на две недели, не trunk-based, существует develop или нет." },
  ]}
/>

<Quiz
  id="02-short-lived-branches-quiz-2"
  lessonSlug="02-short-lived-branches-vs-gitflow"
  lang="ru"
  question="Как в trunk-based development обрабатывается критический фикс к уже выпущенной версии?"
  choices={[
    { label: "Починить его сначала на trunk, затем cherry-pick этого единственного коммита на короткоживущую release-ветку, нарезанную от trunk", correct: true },
    { label: "Разработать фикс на release-ветке, затем слить release-ветку обратно в trunk", misconception: "Это превращает release-ветку в параллельную линию разработки, накапливающую разрыв — то, чего trunk-based избегает. Чините на trunk первым, чтобы следующий релиз его унаследовал, затем cherry-pick вниз." },
    { label: "Создать долгоживущую hotfix-ветку как в gitflow", misconception: "Это модель gitflow. Ветки для релиза в trunk-based — короткоживущие read-mostly снимки; фиксы текут с trunk вниз через cherry-pick, а не из долгоживущей hotfix-линии." },
    { label: "Откатить trunk к выпущенному коммиту и переприменить последующую работу", misconception: "Откат trunk ради патча старого релиза отбросил бы всю последующую работу. Release-ветка существует именно чтобы можно было патчить старую точку, не трогая trunk." },
  ]}
/>

<DragOrder
  id="02-short-lived-branches-drag-1"
  lessonSlug="02-short-lived-branches-vs-gitflow"
  lang="ru"
  prompt="Упорядочьте миграцию branch by abstraction на trunk:"
  items={[
    { id: "a", label: "Ввести слой абстракции над компонентом, который заменяете" },
    { id: "b", label: "Строить новую реализацию за абстракцией малыми ежедневно-сливаемыми коммитами, тёмной" },
    { id: "c", label: "Переключить абстракцию на новую реализацию" },
    { id: "d", label: "Проверить в проде, затем удалить старую реализацию" },
    { id: "e", label: "Убрать ставший лишним слой абстракции" },
  ]}
  correctOrder={["a", "b", "c", "d", "e"]}
/>

</Explanation>

<FlowDiagram
  label="Короткоживущие ветки против Gitflow"
  hue="var(--d-systems)"
  perRow={4}
  nodes={[
    {id:"trunk",label:"trunk",sub:"всегда релизуем"},
    {id:"sb",label:"Короткая ветка",sub:"часы, < 1 дня"},
    {id:"merge",label:"Слияние в trunk",sub:"CI-гейт проходит"},
    {id:"develop",label:"ветка develop",sub:"дни–недели разрыва"},
    {id:"release",label:"ветка release",sub:"фаза интеграции"},
    {id:"main",label:"main",sub:"плановый релиз"}
  ]}
  edges={[
    {from:"trunk",to:"sb",label:"ответвить"},
    {from:"sb",to:"merge",label:"PR + CI"},
    {from:"merge",to:"trunk",label:"приземляет"},
    {from:"develop",to:"release",label:"стабилизировать"},
    {from:"release",to:"main",label:"выкатить"}
  ]}
  caption="Trunk-based: ветка живёт часы, сливается в тот же день. Gitflow: фичи накапливаются в develop, интеграция — фаза."
/>

<KeyTakeaway>Trunk-based запрещает долгоживущие ветки, а не ветки: держите три или меньше активных, исчезающих за день, без фазы интеграции. Структура gitflow develop/release/hotfix верна для планового «коробочного» софта, но институционализирует разрыв для непрерывно развёртываемых сервисов. Релизьте, ответвляясь от trunk и cherry-pick-ая вниз; большие рефакторинги обрабатывайте через branch by abstraction, чтобы они жили на trunk как малые зелёные коммиты.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="02-short-lived-branches-retrieval"
  lang="ru"
  questions={[
    { q: "Команда отключила всю защиту веток, потому что прочла, что trunk-based означает «без веток». Что они недопоняли, и что trunk-based на самом деле задаёт?", a: "Они спутали «без долгоживущих веток» с «без веток». Trunk-based про время жизни ветки и разрыв, а не про существование веток. Определение DORA конкретно: три или меньше активных веток, каждая живёт не более дня (часто часы), без заморозок кода и фаз интеграции. Большинство команд запускают это как короткоживущие feature-ветки именно чтобы сохранить код-ревью и PR/CI-гейт — отключение их выбрасывает механизмы безопасности, не оставляя ничего из выгоды. Чистый коммит-в-trunk тоже существует (малые парные команды), но всё равно опирается на быстрый зелёный гейт. Разделительная черта между trunk-based и workflow с долгоживущими ветками — время жизни: ветка, мёржащаяся за день, — trunk-based; feature-ветка, живая две недели, — долгоживущая ветка, ради устранения которой существует практика, как бы вы её ни называли." },
    { q: "Если есть только trunk, как вы нарезаете релизы и делаете шестинедельный рефакторинг без долгоживущей ветки?", a: "Два разных инструмента. Для релизов: ветка для релиза, а не для фичи. Нарежьте короткоживущую ветку release/x.y от trunk на релизном коммите; никогда не разрабатывайте на ней. Критический фикс идёт сначала на trunk (чтобы следующий релиз его унаследовал) и cherry-pick-ается вниз на release-ветку — release-ветка остаётся read-mostly снимком, который никогда не накапливает разрыв, а команды на непрерывном деплое часто просто тегают trunk. Для шестинедельного рефакторинга вроде замены ORM: branch by abstraction, а не долгоживущая ветка. Поставьте слой абстракции над тем, что заменяете, стройте новую реализацию за ним малыми коммитами, сливаемыми в trunk ежедневно и держащимися тёмными, переключите абстракцию на новую реализацию, проверьте, затем удалите старый путь и абстракцию. Большое изменение всё время живёт на trunk как последовательность малых зелёных коммитов, так что команда продолжает строить на релизуемом trunk и вы никогда не платите за big-bang merge." },
  ]}
/>

<Recap lang="ru">
Trunk-based development запрещает долгоживущие ветки, а не сами ветки — DORA задаёт три или меньше активных веток, время жизни менее дня и отсутствие фаз интеграции или заморозок кода. Коммитите вы прямо в trunk или используете короткоживущие feature-ветки с PR — отличающая переменная это время жизни, а не наличие ветки. Структура gitflow main/develop/release/hotfix связна для «коробочного», планового, мульти-версионного софта, но для непрерывно развёртываемого сервиса институционализирует тот самый разрыв, big-bang слияния и фазы интеграции, ради устранения которых существует trunk-based. Релизы приходят из короткоживущих веток, нарезанных от trunk, с фиксами, сделанными на trunk первыми и cherry-pick-ну­тыми вниз, чтобы release-ветка никогда не становилась параллельной линией разработки. А изменения, слишком большие для выката за день, используют branch by abstraction — слой абстракции, новую реализацию за ним малыми ежедневными коммитами, переключение, затем зачистку — так что даже шестинедельный рефакторинг живёт на trunk как малые зелёные шаги вместо долгоживущей ветки.
</Recap>