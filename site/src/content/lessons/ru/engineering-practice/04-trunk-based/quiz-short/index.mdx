---
concepts:
- integration-frequency
- short-lived-branches
- feature-flags
- ci-gate
- branch-by-abstraction
- flag-debt
estMin: 14
lang: ru
level: senior
order: 8
prereqs: []
slug: quiz-short
sources:
- https://dora.dev/capabilities/trunk-based-development/
- https://martinfowler.com/articles/feature-toggles.html
- https://martinfowler.com/bliki/BranchByAbstraction.html
status: ready
summary: "Промпты на свободное припоминание по trunk-based юниту. Сначала ответьте своими словами, затем раскройте модельный ответ и сравните."
title: 'Trunk-based: свободное припоминание'
track: engineering-practice
unit: 04-trunk-based
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";

<Hook>
Припоминание сильнее перечитывания. На каждый промпт проговорите или запишите полный ответ по памяти, прежде чем открыть модельный — именно усилие припоминания закрепляет материал.
</Hook>

<Goal lang="ru">
Восстановите стержень юнита — почему дрейф задаёт стоимость merge, что конкретно означает short-lived, как флаги отделяют deploy от release, почему зелёный гейт обязателен, branch by abstraction и очистку flag debt — не подглядывая в уроки.
</Goal>

<RetrievalDrawer
  client:load
  id="04-trunk-based-short-retrieval"
  lang="ru"
  questions={[
    { q: "Почему стоимость merge — функция дрейфа, а не размера диффа, и какой единственный рычаг её снижает?", a: "Стоимость merge задаётся дивергенцией — тем, как далеко trunk ушёл от точки вашего форка, — а не размером вашего изменения. Фича на 2000 строк, влитая в тот же час, что и форк, почти бесплатна, ведь trunk не двигался; правка на 50 строк, влитая три недели спустя, может быть мучением, ведь она лежит на фундаменте, перестроенном под ней. Дрейф накапливается, потому что интеграция общая: пока ваша ветка жива, все остальные продолжают коммитить, так что однодневная ветка примиряет день чужой работы, а трёхнедельная — три недели плюс потерянный человеческий контекст. Microsoft измерила примерно 12× более долгое разрешение конфликтов после трёх дней без интеграции. Инструмент merge снижает лишь усилие на конфликт, но не его размер — поэтому единственный рычаг, реально снижающий стоимость, это сокращение времени жизни ветки: вливайтесь в trunk хотя бы ежедневно, чтобы каждый merge примирял лишь маленький свежий срез дрейфа." },
    { q: "Что конкретно задаёт «short-lived branch» и где граница между trunk-based и процессом с долгоживущими ветками?", a: "Определение DORA конкретно, а не на ощущениях: держите три или меньше активных веток одновременно, каждая живёт не больше дня (часто лишь часы), без code freeze и фаз интеграции. Каждая ветка — одно маленькое узкое изменение, которое открывает PR, проходит быстрый гейт, получает короткое ревью и вливается в тот же день; ветка — это staging для ревью и гейтинга, а не место, где живёт работа. Trunk-based идёт либо как чистый commit-to-trunk (маленькие парные команды), либо чаще как short-lived feature branch с PR ради ревью и гейта. Граница — не существование веток и не использование PR (и то, и другое ортогонально), а время жизни: ветка, влитая за день, — trunk-based; ветка feature/*, живущая две недели, — та долгоживущая ветка, которую практика и устраняет, как её ни называй." },
    { q: "Почему отделение deploy от release — именно то, что заставляет trunk-based работать для многонедельных фич, и что даёт флаг помимо сокрытия работы?", a: "Слияние deploy и release в один акт и вынуждает долгоживущие ветки: если отгрузить код равно показать фичу, незавершённая работа должна оставаться вне trunk до готовности. Feature flag рвёт связку — deploy это перенос кода в продакшен, release это сделать его достижимым, а флаг — рантайм-переключатель между ними. Так месячная фича вливается в trunk каждый день маленькими зелёными коммитами, каждый скрыт за выключенным флагом: задеплоен, интегрирован, протестирован, но dark. Это убирает единственную честную причину держать работу вне trunk. Помимо сокрытия флаг даёт три операционные возможности: dark launch (гнать новый путь в проде на реальном трафике, сравнивать и отбрасывать вывод, валидируя с нулевым риском), прогрессивное раскрытие (внутренние, затем 1%, 5%, 25%, 100%, наращивая blast radius лишь по мере уверенности) и kill switch (выключить флаг — и все за секунды вернутся на старый путь, без rollback-деплоя)." },
    { q: "Почему trunk-based неотделим от быстрого блокирующего CI-гейта и что ломается, если гейт медленный?", a: "Общий trunk концентрирует риск: с долгоживущими ветками одна сломанная ветка — проблема одного человека, но с общим trunk сломанный trunk останавливает всех — никто не может взять чистую базу или влиться на зелёном, а «быстрый фикс» поверх поломки усложняет диагностику. Поэтому гейт — вторая половина сделки: нет зелёного — нет merge — каждое изменение прогоняет полную сборку и тесты до приземления, а красный блокирует merge. Поэтому trunk-based и continuous integration — одна практика с двух сторон: «вливаться ежедневно» безопасно только потому, что одновременно держится «автоматический гейт держит trunk релизопригодным». Гейт ещё должен быть быстрым, минуты а не десятки минут: медленный гейт толкает инженеров батчить изменения ради амортизации ожидания, что удлиняет ветки и возвращает дрейф. Когда trunk всё же краснеет, senior-рефлекс — stop the line: чинить или откатывать до новой работы, используя git bisect по маленькой частой истории, чтобы быстро найти виновника." },
    { q: "Как merge queue устраняет гонку, которую имеет наивный pre-merge гейт на масштабе, и каковы его пределы?", a: "Pre-merge гейт тестирует каждый PR против текущего trunk. PR-A зелёный против trunk; PR-B зелёный против того же trunk; оба вливаются — но их не тестировали вместе, так что они могут конфликтовать семантически и сломать trunk, хотя каждый прошёл. Редко при паре merge в день, постоянно при десятках. Merge queue это чинит: PR входят в очередь, и CI тестирует каждый против проецируемого состояния trunk — trunk плюс все PR впереди в очереди, — так что изменение приземляется, только если зелёное против точного состояния, которое создаст. Ради пропускной способности очереди батчат несколько PR как один кандидат и вливают батч, если зелёный, делая bisect батча при сбое, чтобы выкинуть виновника. Так GitHub держал один монорепозиторий зелёным с примерно 1000 merge/месяц в 2016-м до 30000+ к 2023-му. Пределы: одна FIFO-очередь становится bottleneck после примерно 20-30 активных разработчиков, ведь несвязанные изменения конкурируют за одну полосу, а единственный flaky-тест катастрофичен — при 50+ PR/день он может стопорить линию часами, отвергая батчи, которые на деле были в порядке. Поэтому карантин flaky-тестов и параллельные независимые полосы становятся инфраструктурой первого порядка." },
    { q: "Объясните «trunk-based меняет branch debt на flag debt» и почему сделка выгодна только с дисциплиной очистки.", a: "Каждый флаг — это тоже ветка в вашем рантайме: долгоживущая git-ветка расщепляет кодовую базу на разошедшиеся истории, а долгоживущий флаг расщепляет работающую систему на разошедшиеся пути исполнения. Так release flag, переживший свою фичу, — это долгоживущая ветка, спрятанная в if: старый путь за else гниёт непротестированным, но остаётся достижимым — та же проблема дрейфа, перенесённая в условие. Стоимость экспоненциальна, а не линейна: каждый независимый булев флаг удваивает рантайм-конфигурации (3 флага = 8, 10 = 1024, 20 = больше миллиона), вы тестируете горстку и отгружаете, а непротестированные комбинации — там, где живут interaction-баги (A в порядке, B в порядке, оба включены не пробовали никогда). Это та же проблема 2^N непротестированных конфигураций, что дали бы устаревшие ветки. Поэтому trunk-based выгоден, только если удалять флаги так же агрессивно, как ветки: удалить ветку при merge, удалить флаг и мёртвый путь при полной раскатке — как закрывающий шаг раскатки, а не follow-up тикет, ведь отложенная очистка — то, как команды доходят до 400 устаревших флагов. Элитные цифры DORA коррелируют с trunk-based, только когда быстрый гейт, флаги и очистка присутствуют все вместе." },
  ]}
/>

<Recap lang="ru">
Если вы смогли восстановить каждый ответ по памяти, вы держите стержень юнита: дрейф задаёт стоимость merge, поэтому ежедневная интеграция её ограничивает; short-lived означает три или меньше веток, исчезающих за день, и граница — это время жизни, а не существование веток или PR; флаги отделяют deploy от release, так что незавершённая работа едет на trunk dark, а вы получаете dark launch, прогрессивное раскрытие и kill switch; быстрый блокирующий зелёный гейт делает общий trunk безопасным, а merge queue держит его зелёным на масштабе в пределах своих ограничений; и очистка — удалить ветку при merge, флаг при раскатке — это дисциплина, не дающая branch debt вернуться как 2^N flag debt.
</Recap>
