---
slug: 03-feature-flags-dark-launch
lang: ru
track: engineering-practice
unit: 04-trunk-based
order: 3
level: middle
lessonType: topic
status: ready
title: "Feature-флаги отвязывают деплой от релиза"
summary: "Ежедневная интеграция долгих фич требует разделить деплой и релиз. Флаг выкатывает незавершённое в прод тёмным — интегрированным, но недостижимым, — и релиз становится runtime-рампой с откатом за секунды. Типы флагов различаются сроком жизни."
prereqs: ["02-short-lived-branches-vs-gitflow"]
deepensInto: ["04-the-green-trunk-gate"]
spiral: ["incremental-change", "blast-radius"]
concepts: ["feature-flags", "dark-launch", "decouple-deploy-from-release", "canary-release", "kill-switch", "flag-taxonomy"]
estMin: 16
sources:
  - https://martinfowler.com/articles/feature-toggles.html
  - https://launchdarkly.com/blog/why-decouple-deployments-from-releases/
  - https://www.getunleash.io/blog/using-feature-flags-to-enable-trunk-based-development
  - https://trunkbaseddevelopment.com/feature-flags/
---

import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";

<Hook>
«Я не могу мёржить ежедневно — моя фича не закончена. Я не могу положить половину flow checkout-а на trunk.» Это возражение топит большинство внедрений trunk-based, и оно держится на скрытом допущении: что положить код в прод значит *показать его пользователям*. Разделите эти два — и возражение растворяется. Полузаконченный flow checkout-а уезжает в прод сегодня, прогоняется через CI, лежит там полностью интегрированный — и ни один пользователь не может до него добраться, потому что он за выключенным флагом. Вы развернули код. Вы не выпустили фичу.
</Hook>

<Crux>Feature-флаг разделяет деплой кода и релиз фичи: незавершённая работа едет на trunk тёмной, а релиз становится рантайм-решением.</Crux>

<Explanation>

## Деплой и релиз — разные события

Связывание «деплоя» и «релиза» в один акт — вот что вынуждает к долгоживущим веткам: если выкатить код — то же, что показать фичу, то незавершённая работа должна оставаться вне trunk, пока не готова. Feature-флаги разбивают эту связку. **Развёртывание** — это перенос кода на прод-серверы. **Релиз** — это сделать фичу достижимой пользователями. Флаг — рантайм-переключатель между ними: оберните новый путь в условие, читающее флаг, выкатите его выключенным — и код развёрнут, интегрирован и протестирован, но **тёмный**: присутствует и недостижим.

Именно это делает ежедневную интеграцию совместимой с многонедельными фичами. Работа мёржится в trunk каждый день как малые зелёные коммиты, каждый спрятан за выключенным флагом, так что вы никогда не накапливаете разрыв (урок 1) и никогда не нуждаетесь в долгоживущей ветке (урок 2). Когда фича завершена, вы ничего не развёртываете — вы переключаете флаг. Релиз перестаёт быть рискованным большим пушем и становится изменением конфигурации, которое можно сделать в 10 утра во вторник, пока автор смотрит на дашборды.

## Dark launch и постепенная экспозиция

Когда релиз стал рантайм-решением, вы получаете возможности, которых деплой дать не может. **Dark launching** означает прогонять новый путь кода в проде *до* показа его результатов пользователям — например, слать реальный трафик на новый сервис рекомендаций, сравнивать его вывод со старым и измерять задержку, но выбрасывать новый вывод. Вы нагрузочно тестируете и валидируете против реальности прода с нулевым видимым для пользователя риском. Затем вы **постепенно экспонируете**: включаете фичу для внутренних пользователей, потом 1%, потом 5%, 25%, 100%, наблюдая за частотой ошибок и задержкой на каждом шаге. Это canary через флаг — blast radius растёт только по мере роста уверенности.

Тот же переключатель — ваш самый быстрый инструмент восстановления. Если разогнанная фича ведёт себя плохо на 5%, вы выключаете флаг — **kill switch** — и каждый пользователь возвращается на старый путь за секунды, без отката-деплоя, без revert-PR, без ожидания CI. Отвязывание деплоя от релиза превращает «мы выкатили баг, начинаем инцидент» в «мы его выключили, теперь спокойно дебажим».

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Тип флага</th>
        <th class="border border-border px-3 py-2 text-left">Назначение</th>
        <th class="border border-border px-3 py-2 text-left">Ожидаемое время жизни</th>
        <th class="border border-border px-3 py-2 text-left">Кто его переключает</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">Release</td>
        <td class="border border-border px-3 py-2">Спрятать незавершённую работу; разогнать выкатку</td>
        <td class="border border-border px-3 py-2">Дни–недели — удалить после 100%</td>
        <td class="border border-border px-3 py-2">Разработчик / владелец релиза</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Ops / kill switch</td>
        <td class="border border-border px-3 py-2">Отключить фичу под нагрузкой/в инциденте</td>
        <td class="border border-border px-3 py-2">Долгоживущий по дизайну</td>
        <td class="border border-border px-3 py-2">On-call / ops</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Experiment</td>
        <td class="border border-border px-3 py-2">A/B-тест, проверить гипотезу</td>
        <td class="border border-border px-3 py-2">Один цикл эксперимента</td>
        <td class="border border-border px-3 py-2">Продукт / данные</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Permission / entitlement</td>
        <td class="border border-border px-3 py-2">Гейтить фичи по плану/роли</td>
        <td class="border border-border px-3 py-2">Постоянный (это продуктовое правило)</td>
        <td class="border border-border px-3 py-2">Продукт / рантайм</td>
      </tr>
    </tbody>
  </table>
</div>

## Не каждый флаг — один и тот же зверь

Одно слово «флаг» прячет четыре очень разные вещи, и их смешение — это как гниют системы флагов. **Release-флаг** — временные леса: он существует, чтобы прятать незавершённую работу и разгонять выкатку, и должен быть удалён, как только фича на 100%. **Ops-флаг** (kill switch) призван жить вечно, чтобы вы могли сбрасывать нагрузку или отключать сбоящую подсистему во время инцидента. **Experiment-флаг** живёт один A/B-цикл. **Permission-флаг** постоянен, потому что кодирует продуктовое правило (эта фича — для планов Pro). Опасная категория — release-флаг, потому что он *выглядит* постоянным, если о нём забыть — а забытый release-флаг — это проблема flag debt из следующего урока.

<Inset kind="why" lang="ru">
Причина, по которой «отвяжи деплой от релиза» — это ключ к trunk-based, не в самих флагах — а в том, что это убирает единственную честную причину держать работу вне trunk. Без флагов «моя фича не готова» — реальный аргумент за долгоживущую ветку. С флагами незавершённая работа имеет безопасный дом в проде, так что больше нет оправдания давать ветке накапливать разрыв. Флаг — это то, что делает «интегрируйся ежедневно» достижимым для работы, занимающей месяц.
</Inset>

<TradeoffMatrix
  id="03-feature-flags-tradeoff-1"
  lessonSlug="03-feature-flags-dark-launch"
  lang="ru"
  prompt="Вы выкатываете переписанное ранжирование поиска, затрагивающее каждый запрос. Как выпустить его с наименьшим blast radius?"
  options={[
    { name: "Сначала dark launch (прогнать новое ранжирование в проде, сравнить со старым, выбросить его вывод), затем разогнать флаг 1% → 25% → 100%, наблюдая за метриками, с готовым kill switch", correct: true, justification: "Dark launch валидирует новый путь против реального прод-трафика с нулевым влиянием на пользователей; постепенная рампа растит blast radius только по мере роста уверенности; а kill switch делает восстановление переключением флага за секунды вместо отката-деплоя. Это полный набор инструментов «отвяжи деплой от релиза», применённый к высокорисковому изменению." },
    { name: "Развернуть на 100% пользователей разом и пристально смотреть на частоту ошибок", summary: "Big-bang релиз.", justification: "Пристальное наблюдение не уменьшает blast radius — регрессия ранжирования бьёт по каждому пользователю немедленно, а восстановление означает откат-деплой. Смысл флагов — сделать экспозицию постепенной, а восстановление мгновенным." },
    { name: "Держать на ветке, пока полностью не уверены, затем слить и развернуть", summary: "Уверенность на ветке.", justification: "Нельзя набрать прод-уверенность на ветке — новое ранжирование доказывает себя только против реального трафика, что и даёт dark launch. К тому же ветка накапливает разрыв, пока вы ждёте." },
    { name: "Выкатить за флагом, но пропустить dark launch и разогнать сразу до 100%", summary: "Флаг без рампы.", justification: "Флаг, который вы переключаете сразу на 100%, даёт быстрый kill switch, но выбрасывает выгоду постепенной экспозиции. Для изменения, затрагивающего каждый запрос, валидируйте тёмным и разгоняйте — не экспонируйте всех одним переключением." },
  ]}
/>

<Quiz
  id="03-feature-flags-quiz-1"
  lessonSlug="03-feature-flags-dark-launch"
  lang="ru"
  question="Какое скрытое допущение делает «я не могу мёржить ежедневно, моя фича не готова» ощущаемым как истина — и как флаг его растворяет?"
  choices={[
    { label: "Допущение, что развернуть код равно показать его пользователям; флаг их разделяет, так что незавершённый код едет тёмным и недостижимым до релиза", correct: true },
    { label: "Допущение, что CI слишком медленный; флаг ускоряет сборку", misconception: "Флаги не меняют скорость сборки. Растворяемое допущение — что положить код в прод значит, что пользователи могут до него добраться; флаги рвут эту связь, давая незавершённой работе уехать спрятанной." },
    { label: "Допущение, что требуется код-ревью; флаг обходит ревью", misconception: "Флаги не обходят ревью — флагнутый код всё равно проходит гейт. Они меняют экспозицию: развёрнут, но тёмен, так что «не готово» больше не причина держать его вне trunk." },
    { label: "Допущение, что вам нужны ветки; флаг — это разновидность ветки", misconception: "Флаг — это рантайм-условие, а не VCS-ветка, и смысл противоположный — флаги убирают нужду в долгоживущих ветках, давая незавершённой работе дом в проде." },
  ]}
/>

<Quiz
  id="03-feature-flags-quiz-2"
  lessonSlug="03-feature-flags-dark-launch"
  lang="ru"
  question="Какой тип флага — это временные леса, которые должны быть удалены после того, как фича достигла 100%?"
  choices={[
    { label: "Release-флаг — он существует только чтобы прятать незавершённую работу и разгонять выкатку и становится flag debt, если его оставить", correct: true },
    { label: "Ops/kill-switch-флаг", misconception: "Kill switch-и призваны жить вечно, чтобы on-call мог отключить подсистему во время инцидента. Временная, обязательная-к-удалению категория — это release-флаг." },
    { label: "Permission/entitlement-флаг", misconception: "Permission-флаги постоянны — они кодируют продуктовое правило вроде «только план Pro». Временный, обязательный к удалению на 100% — это release-флаг." },
    { label: "Experiment-флаг", misconception: "Experiment-флаги живут один A/B-цикл и зачищаются после, но тип флага, конкретно связанный с прятанием незавершённой работы по выкатке, — это release-флаг." },
  ]}
/>

<DragOrder
  id="03-feature-flags-drag-1"
  lessonSlug="03-feature-flags-dark-launch"
  lang="ru"
  prompt="Упорядочьте безопасный релиз рискованной фичи с помощью флагов:"
  items={[
    { id: "a", label: "Мёржить работу в trunk ежедневно за выключенным release-флагом — развёрнуто, но тёмно" },
    { id: "b", label: "Dark launch: прогнать новый путь в проде, сравнить вывод, выбросить его" },
    { id: "c", label: "Включить для внутренних пользователей, затем разогнать 1% → 25% → 100%, наблюдая за метриками" },
    { id: "d", label: "Держать наготове kill switch для выключения за секунды, если метрики регрессируют" },
    { id: "e", label: "На стабильных 100% удалить release-флаг и мёртвый старый путь" },
  ]}
  correctOrder={["a", "b", "c", "d", "e"]}
/>

</Explanation>

<KeyTakeaway>Развёртывание кода и выпуск фичи — разные события; флаг — рантайм-переключатель между ними. Это позволяет незавершённой работе ехать на trunk тёмной, делает релиз постепенной рампой, которую можно dark-launch-ить и откатить за секунды через kill switch, и убирает единственную честную причину держать работу вне trunk. Но «флаг» прячет четырёх зверей — release, ops, experiment, permission — с разным временем жизни; только release-флаги — временные леса, которые должны быть удалены.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="03-feature-flags-retrieval"
  lang="ru"
  questions={[
    { q: "Объясните, почему отвязывание деплоя от релиза — это та конкретная вещь, что заставляет trunk-based работать для многонедельных фич.", a: "Связывание деплоя и релиза в один акт — вот что вынуждает к долгоживущим веткам: если выкатить код — то же, что показать фичу, незавершённая работа должна оставаться вне trunk, пока не готова. Feature-флаг разбивает связку — развёртывание это перенос кода в прод, релиз это сделать его достижимым пользователями, а флаг — рантайм-переключатель между ними. Так месячная фича мёржится в trunk каждый день как малые зелёные коммиты, каждый спрятан за выключенным флагом: развёрнут, интегрирован и протестирован, но тёмен. Это и убирает единственную честную причину держать работу вне trunk — «оно не готово» больше не требует ветки, потому что незавершённая работа имеет безопасный дом в проде. Вы никогда не накапливаете разрыв и никогда не нуждаетесь в долгоживущей ветке. Релиз затем перестаёт быть рискованным пушем и становится изменением конфигурации: переключите флаг, когда фича завершена." },
    { q: "Помимо прятания незавершённой работы, какие операционные возможности даёт флаг, и почему таксономия флагов важна?", a: "Когда релиз стал рантайм-решением, вы получаете три вещи, которых деплой дать не может. Dark launching: прогнать новый путь кода в проде до показа результатов пользователям — слать реальный трафик на новый сервис, сравнивать вывод и измерять задержку, выбросить новый вывод — так вы валидируете против реальности прода с нулевым риском для пользователей. Постепенная экспозиция: включить для внутренних пользователей, потом 1%, 5%, 25%, 100%, наблюдая за частотой ошибок и задержкой, так что blast radius растёт только с уверенностью. И kill switch: если рампа ведёт себя плохо, выключите флаг и все вернутся на старый путь за секунды — без отката-деплоя. Таксономия важна, потому что «флаг» прячет четыре разных времени жизни: release-флаги — временные леса, удаляемые на 100%; ops/kill-switch-флаги живут вечно для реагирования на инциденты; experiment-флаги живут один A/B-цикл; permission-флаги — постоянные продуктовые правила. Их смешение — как гниют системы флагов; опасный случай — оставленный release-флаг, который выглядит постоянным и становится flag debt." },
  ]}
/>

<Recap lang="ru">
Ежедневная интеграция многонедельной работы возможна только потому, что развёртывание кода и выпуск фичи — разделимые события, а feature-флаг — рантайм-переключатель между ними: оберните новый путь в выключенное условие, и код уезжает в прод тёмным — интегрированным и протестированным, но недостижимым. Это даёт незавершённой работе безопасный дом на trunk, так что вам никогда не нужна долгоживущая ветка, и превращает релиз из рискованного деплоя в изменение конфигурации. Это также открывает операционную мощь: dark launching валидирует новый код против реального трафика без влияния на пользователей, постепенная экспозиция разгоняет blast radius с уверенностью, а kill switch делает восстановление переключением за секунды вместо отката. Но «флаг» смешивает четырёх зверей с разным временем жизни — временные release-флаги, постоянные ops kill switch-и, одноцикловые experiment-флаги и постоянные permission-флаги — и release-флаг — тот, что должен быть удалён на 100%, иначе он тихо становится flag debt из следующего урока.
</Recap>