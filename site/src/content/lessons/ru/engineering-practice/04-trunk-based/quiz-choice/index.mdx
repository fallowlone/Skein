---
concepts:
- integration-frequency
- short-lived-branches
- feature-flags
- ci-gate
- merge-queue
- flag-debt
estMin: 13
lang: ru
level: senior
order: 7
prereqs: []
slug: quiz-choice
sources:
- https://dora.dev/capabilities/trunk-based-development/
- https://martinfowler.com/articles/branching-patterns.html
- https://github.blog/engineering/engineering-principles/how-github-uses-merge-queue-to-ship-hundreds-of-changes-every-day/
status: ready
summary: "Синтез юнита в формате множественного выбора — дрейф, short-lived branch, feature flag, зелёный CI-гейт, merge queue на масштабе и flag debt."
title: 'Trunk-based: тест с множественным выбором'
track: engineering-practice
unit: 04-trunk-based
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Шесть вопросов сквозь весь юнит. Каждый отражает решение, которое вы принимаете в реальном споре о процессе, — это не определение для пересказа, а компромисс, который нужно защитить, когда кто-то говорит «trunk-based не масштабируется».
</Hook>

<Goal lang="ru">
Убедитесь, что связываете стержень юнита: дрейф задаёт стоимость merge, short-lived branch её ограничивают, флаги отделяют deploy от release, зелёный CI-гейт делает общий trunk безопасным, merge queue держит его на масштабе, а очистка — то, что делает всю сделку выгодной.
</Goal>

<Quiz
  id="04-trunk-based-choice-1"
  lessonSlug="04-trunk-based"
  lang="ru"
  question="Один инженер вливает фичу на 2000 строк в тот же час, когда форкнулся; другой вливает правку на 50 строк из трёхнедельной ветки. Какой merge дороже и почему?"
  choices={[
    { label: "Правка на 50 строк. Стоимость merge задаётся тем, как далеко trunk сдрейфовал с момента форка, а не размером диффа — маленькое изменение лежит поверх трёх недель чужих коммитов, переименований и изменившихся инвариантов, которые надо примирить.", correct: true },
    { label: "Фича на 2000 строк, потому что большие диффы всегда дают больше конфликтов.", misconception: "Размер диффа не задаёт стоимость merge — её задаёт дивергенция. Большое изменение, влитое в тот же час, примиряет почти нулевой дрейф, ведь trunk не двигался, — это почти бесплатно." },
    { label: "Одинаково, ведь git разрешает оба одним и тем же трёхсторонним merge-алгоритмом.", misconception: "Алгоритм один, входные данные разные. Стоимость — это объём разошедшихся коммитов и потерянного контекста, и задаётся он временем жизни ветки, а не инструментом." },
    { label: "Правка на 50 строк, потому что маленькие изменения хуже ревьюят и они рискованнее.", misconception: "Глубина ревью тут ни при чём. Те же 50 строк дёшевы на свежем trunk и дороги поверх трёх недель дрейфа — переменная это время жизни, а не ревью." },
  ]}
/>

<Quiz
  id="04-trunk-based-choice-2"
  lessonSlug="04-trunk-based"
  lang="ru"
  question="Команда прочитала «trunk-based development», отключила branch protection и коммитит прямо в main. Через неделю trunk красный дважды в день, а ещё утёк секрет. В чём была настоящая ошибка?"
  choices={[
    { label: "Они спутали «нет долгоживущих веток» с «нет веток». Trunk-based — про время жизни ветки и зелёный гейт; большинство команд держат short-lived branch с PR и CI именно ради ревью и гейтинга.", correct: true },
    { label: "Ни в чём — коммитить прямо в main и есть единственный истинный trunk-based.", misconception: "Чистый commit-to-trunk существует для маленьких парных команд, но всё равно опирается на быстрый зелёный гейт. Модель запрещает долгоживущие ветки, а не механизмы безопасности PR/CI." },
    { label: "Trunk-based действительно не масштабируется дальше пары инженеров.", misconception: "Провал был в отказе от гейта и ревью, а не в модели веток. Данные DORA показывают: trunk-based коррелирует с элитной поставкой, когда гейт и дисциплина на месте." },
    { label: "Им нужна была ветка develop из gitflow, чтобы безопасно стейджить изменения.", misconception: "Добавление develop возвращает фазу интеграции и дрейф, которые trunk-based убирает. Решение — быстрый блокирующий гейт плюс short-lived branch, а не долгоживущая staging-ветка." },
  ]}
/>

<Quiz
  id="04-trunk-based-choice-3"
  lessonSlug="04-trunk-based"
  lang="ru"
  question="Инженер настаивает, что не может вливаться ежедневно: переписывание checkout не готово, «нельзя положить половину флоу в trunk». Что снимает возражение?"
  choices={[
    { label: "Deploy кода и release фичи — разные события. Feature flag — рантайм-переключатель между ними, так что недоделанный флоу едет в продакшен dark — интегрирован и протестирован, но недостижим — пока вы не включите флаг.", correct: true },
    { label: "Нужно держать это на долгоживущей ветке до готовности, а потом влить разом.", misconception: "Это ровно та накапливающая дрейф долгоживущая ветка, которую юнит и устраняет; флаг даёт недоделанной работе безопасный дом в trunk." },
    { label: "Нужно ускорить CI, чтобы недоделанный код проходил сборку.", misconception: "Скорость сборки тут не блокер. Скрытое допущение — что deploy равен показу пользователям; флаг рвёт эту связь, и незавершённый код едет скрыто." },
    { label: "Ревью можно пропустить для незавершённой работы за флагом.", misconception: "Код за флагом всё равно проходит гейт и ревью. Флаг меняет видимость, а не требование пройти ревью и CI." },
  ]}
/>

<Quiz
  id="04-trunk-based-choice-4"
  lessonSlug="04-trunk-based"
  lang="ru"
  question="Нужно заменить ORM в непрерывно деплоящемся сервисе; миграция займёт шесть недель. Как сделать это по trunk-based?"
  choices={[
    { label: "Branch by abstraction: положить слой над доступом к данным, строить новый ORM за ним маленькими ежедневно вливаемыми коммитами dark, переключить абстракцию, затем удалить старый путь. Изменение живёт в trunk как маленькие зелёные шаги.", correct: true },
    { label: "Завести долгоживущую ветку миграции и влить её, когда миграция готова.", misconception: "Шесть недель дивергенции в непрерывно деплоящемся сервисе — это big-bang merge и детонация семантических конфликтов, которых trunk-based избегает. Команда строит на trunk, о котором та ветка ничего не знает." },
    { label: "Перевести репозиторий на gitflow с отдельной release-веткой под миграцию.", misconception: "Фазы стабилизации gitflow подходят для запланированных коробочных релизов, а не для continuous deploy. Это институционализирует фазу интеграции и долгоживущую ветку, которых вы избегаете." },
    { label: "Заморозить остальную работу по затронутым модулям, пока миграция не приземлится.", misconception: "Code freeze — это анти-паттерн DORA: он стопорит команду и переносит боль интеграции на день разморозки. Branch by abstraction позволяет обычной работе продолжаться в trunk." },
  ]}
/>

<Quiz
  id="04-trunk-based-choice-5"
  lessonSlug="04-trunk-based"
  lang="ru"
  question="При 50 PR/день на одном trunk каждый PR зелёный до merge, но trunk всё равно ломается несколько раз в неделю. Какое решение верное?"
  choices={[
    { label: "Merge queue, тестирующий каждый PR против проецируемого trunk — trunk плюс PR, стоящие впереди в очереди, — с батчингом ради пропускной способности, чтобы приземлялись только изменения, зелёные против итогового состояния.", correct: true },
    { label: "Требовать два аппрува и сделать CI-проверки строже.", misconception: "Каждый PR всё равно валидируется против устаревшей базы, так что два независимо зелёных PR могут вместе сломать trunk. Проблема — против какого состояния тестируем, а не глубина ревью." },
    { label: "Перейти на post-merge CI и просто быстро откатывать, когда trunk краснеет.", misconception: "Post-merge тестирование ловит поломку только когда она уже блокирует команду, а на таком объёме churn откатов постоянный. Гейт нужен, чтобы держать trunk зелёным, а не чистить его после." },
    { label: "Разбиться на долгоживущие командные ветки с еженедельной интеграцией.", misconception: "Это отказ от trunk-based ради обхода решаемой инфра-проблемы: вы меняете bottleneck очереди на еженедельные big-bang merge и возвращённый дрейф. Масштабируйте гейт, а не ветки." },
  ]}
/>

<Quiz
  id="04-trunk-based-choice-6"
  lessonSlug="04-trunk-based"
  lang="ru"
  question="Через два года у trunk-based команды 400 флагов, флаг «new checkout» на 100% уже год со старым путём в else, и инцидент от двух флагов, чью комбинацию никто не пробовал. В чём корень провала?"
  choices={[
    { label: "Пропущенная очистка. Release flag, оставленный после раскатки, — это долгоживущая ветка, спрятанная в if; N устаревших флагов дают 2^N непротестированных рантайм-конфигураций, поэтому interaction-баги неизбежны. Решение — удалять флаги так же агрессивно, как ветки.", correct: true },
    { label: "Trunk-based просто меняет боль веток на неизбежную боль флагов.", misconception: "Flag debt предотвратим дисциплиной жизненного цикла — владелец, срок и удаление флага и мёртвого пути как закрывающий шаг раскатки. Долг копится только когда очистку пропускают." },
    { label: "Надо было использовать меньше флагов, оставляя работу на ветках.", misconception: "Ветки возвращают дрейф и big-bang merge. Флаги — правильный инструмент; дисциплина в том, чтобы удалять release flag на 100%, а не избегать флагов." },
    { label: "Система флагов слишком медленная в рантайме с 400 флагами.", misconception: "Стоимость не в задержке рантайма — она в комбинаторном пространстве непротестированных конфигураций и гниющих старых путях за устаревшими release flag." },
  ]}
/>

<Recap lang="ru">
Стержень юнита — единая дисциплинированная система, а не трюк с ветками. Дрейф задаёт стоимость merge, поэтому short-lived branch (три или меньше, исчезают за день) её ограничивают; feature flag отделяют deploy от release, так что незавершённая работа едет в trunk dark, а большие рефакторинги используют branch by abstraction; быстрый блокирующий зелёный гейт делает общий trunk безопасным, а merge queue держит его зелёным на масштабе; и очистка — удалить ветку при merge, флаг при полной раскатке — не даёт branch debt вернуться как 2^N flag debt. Внедрите одну модель веток — получите режимы отказа; внедрите всю систему — и цифры DORA приходят следом.
</Recap>
