---
concepts:
- spot-the-bug
- idempotency
- review-comment-quality
- pr-decomposition
- automate-the-mechanical
estMin: 14
lang: ru
level: senior
order: 9
prereqs: []
slug: quiz-code
sources:
- https://google.github.io/eng-practices/review/reviewer/standard.html
- https://conventionalcomments.org/
- https://google.github.io/eng-practices/review/developer/small-cls.html
status: ready
summary: "Читай реальные диффы, описание PR и комментарии ревью — поймай дизайн-баг, который обязан увидеть ревьюер, оцени читаемость PR и перепиши комментарий так, чтобы он называл важность и фикс."
title: 'Code review: чтение диффов и комментариев'
track: engineering-practice
unit: 03-code-review
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Ревью — это чтение, а не пересказ. Вот артефакты, с которыми ты реально сталкиваешься в PR — дифф, описание, тред комментариев. Для каждого делай то, что делает senior-ревьюер: найди дефект, который тулинг не поймает, оцени, читаем ли вообще дифф, и преврати туманный комментарий в действенный.
</Hook>

<Goal lang="ru">
Отработай цикл ревьюера на реальном материале: отдели неразрешимый дизайн-изъян от механического шума, читай форму PR на читаемость и пиши фидбэк, отвечающий на «насколько важно» и «что делать».
</Goal>

## Сниппет 1 — путь ретрая, который ревьюер обязан поймать

```python
# PR: "Добавить ретрай billing при транзиентных ошибках шлюза"
def charge_customer(order_id, amount):
    record = db.insert_charge(order_id=order_id, amount=amount, status="pending")
    for attempt in range(3):
        resp = gateway.charge(amount)          # может истечь ПОСЛЕ списания
        if resp.ok:
            db.update(record, status="settled")
            return resp
        time.sleep(2 ** attempt)               # пауза и повтор
    db.update(record, status="failed")
    raise ChargeError(order_id)
```

<Quiz
  id="03-code-review-code-1"
  lessonSlug="03-code-review"
  lang="ru"
  question="CI зелёный — формат, lint, типы и юнит-тесты проходят. Какой дефект ревью существует, чтобы поймать именно здесь?"
  choices={[
    { label: "Ретрай не идемпотентен. Таймаут шлюза может прийти ПОСЛЕ успешного списания; цикл тогда повторяет и списывает с клиента дважды. Фикс — idempotency key, передаваемый шлюзу, чтобы повторный запрос дедуплицировался, а не стал вторым списанием.", correct: true },
    { label: "Sleep использует экспоненциальный backoff, который слишком надолго заблокирует поток запроса.", misconception: "Тайминг backoff — деталь тюнинга, а не дефект. Баг корректности в том, что ретрай после таймаута-уже-после-списания списывает с клиента дважды — ровно тот неразрешимый изъян денежного пути, ради которого нужно человеческое ревью." },
    { label: "Ничего — зелёный CI плюс проходящие тесты означают, что изменение корректно.", misconception: "Тесты проверяют пути, которые автор вообразил; они зелёные именно потому, что никто не протестировал кейс таймаута-после-успеха. Поэтому ревью целится в замысел и денежный путь, а не только в то, проходит ли набор тестов." },
    { label: "status='pending' должен быть enum, а не строкой.", misconception: "Это style/typing-nit, которым должен владеть linter или система типов — разрешимо и дёшево. Потратить ревью на него, пока двойное списание уезжает в прод, — тот сбой инвертированных приоритетов, о котором предупреждает юнит." },
  ]}
/>

## Сниппет 2 — описание PR

```
Title:  Refactor checkout + add coupons + migrate orders table
Files changed: 47   +1,812  −940
Description: "Cleaned up checkout, added coupon support, and migrated the
orders table to the new schema. All in one PR so it deploys atomically."
```

<Quiz
  id="03-code-review-code-2"
  lessonSlug="03-code-review"
  lang="ru"
  question="Как назначенный ревьюер, какой senior-ответ на этот PR ещё до прочтения хотя бы строчки диффа?"
  choices={[
    { label: "Попросить автора разбить: behavior-preserving рефакторинг, затем купоны, затем миграция — в идеале stacked. На 1812 строках, объединяющих три концерна, это за пределами полосы 200–400 LOC, поэтому его проскользят и зарубят-штампом, а рискованная миграция не получит реального внимания.", correct: true },
    { label: "Прочесть внимательно от начала до конца; тщательный ревьюер осилит 1800 строк, если не спешить.", misconception: "Внимание — фиксированный бюджет, не масштабируемый размером диффа: за пределами нескольких сотен строк ты скользишь независимо от усилий, а реальный риск миграции прячется в непрочитанной массе. Фикс — декомпозиция, а не больше силы воли." },
    { label: "Заапрувить, потому что объединение делает деплой атомарным и избегает частичных состояний.", misconception: "Атомарный деплой — реальный концерн, но он решается порядком/feature-флагами stacked PR, а не одним нечитаемым диффом. «Деплоится атомарно» — рационализация, которая порождает rubber stamp на самом рискованном изменении." },
    { label: "Добавить двух ревьюеров, чтобы 1800 строк были покрыты.", misconception: "Каждый добавленный ревьюер всё равно видит 1800 строк за пределами своего когнитивного лимита — больше скольжений и больше ожидания подхвата, а не больше обнаружения. Лимит внимания на человека — ограничение; размер диффа — рычаг." },
  ]}
/>

## Сниппет 3 — комментарий ревью

```
Reviewer comment on a 40-line PR, left as one line on the file:
  "this is confusing, why did you even do it this way"
```

<Quiz
  id="03-code-review-code-3"
  lessonSlug="03-code-review"
  lang="ru"
  question="Почему этот комментарий породит трение и какова минимальная переписка, которая его чинит?"
  choices={[
    { label: "Он не называет ни важность, ни фикс и целится в суждение автора, а не в код, поэтому автор должен гадать, блокирует ли он и что менять. Перепиши с лейблом и действенным «почему»: 'question: мне было трудно проследить ранний return здесь — не сделает ли вынос validateOrder happy path яснее? (non-blocking)'.", correct: true },
    { label: "Он слишком короткий; ревьюеру стоит написать несколько абзацев с полным обоснованием.", misconception: "Длина — не проблема, проблема — ясность. Короткий комментарий, называющий важность и конкретное действие, бьёт длинный, который всё равно оставляет автора гадать. Фикс — лейбл плюс действенный запрос, а не больше слов." },
    { label: "Всё нормально как есть; прямые комментарии делают ревью быстрее.", misconception: "Прямота, атакующая автора и опускающая важность, создаёт round-trips и подтачивает психологическую безопасность, которая держит людей в готовности искать ранний фидбэк. В ожидании это медленнее, а не быстрее." },
    { label: "Ревьюеру стоило просто заапрувить 40-строчный PR без комментария.", misconception: "Если есть настоящий концерн по читаемости, поднять его правильно — ревью и про maintainability тоже. Дефект в том, КАК сформулировано, а не в том, что подняли; пометь его и сделай действенным." },
  ]}
/>

## Сниппет 4 — дифф, который никогда не должен доходить до человека

```diff
- const  userName=req.body.userName
+ const userName = req.body.userName;
- function getUser( id ){return db.find(id)}
+ function getUser(id) { return db.find(id); }
```

<Quiz
  id="03-code-review-code-4"
  lessonSlug="03-code-review"
  lang="ru"
  question="Это единственные изменения в PR, и ревьюер потратил десять минут на ручную проверку пробелов и точек с запятой. Что это говорит о пайплайне?"
  choices={[
    { label: "Каждое изменение здесь — разрешимое форматирование, которое гейт Prettier/gofmt + lint должен чинить и enforce-ить автоматически до ревью. Человек, вручную проверяющий пробелы, значит, что механического гейта нет — это senior-время украдено у дизайна и корректности, на каждом PR, пока не автоматизируешь один раз.", correct: true },
    { label: "Ревьюер поступил правильно — ручная проверка стиля гарантирует консистентность.", misconception: "Ручная проверка стиля непоследовательна и это повторяющийся налог на каждый PR; formatter применяет те же правила одинаково и мгновенно. Гонять разрешимые проверки через человека — медленная, ошибкоёмкая версия инструмента." },
    { label: "Эти изменения слишком тривиальны, чтобы вообще быть в PR, и должны быть запрещены.", misconception: "Изменения форматирования нормально мержить — суть в том, что их должен производить и проверять formatter в CI, а не человек вручную. Запрещай ручное ревью их, а не сами изменения." },
    { label: "Это показывает, что автор не умеет писать чистый код, что является проблемой производительности.", misconception: "Это показывает, что у команды нет гейта форматирования, а не что автор неполноценен. С автоформатом при сохранении и блоком в CI ни один человек никогда не видит и не судит этот класс изменений." },
  ]}
/>

<Recap lang="ru">
Чтение — это и есть работа: зелёный CI и проходящие тесты не сертифицируют денежный путь, поэтому ревьюер охотится за неразрешимым дефектом — здесь это неидемпотентный ретрай, списывающий дважды. Форму PR читают до диффа: изменение в 1812 строк на три концерна делят или стекают, потому что внимание до него не дотянется. Комментарии судят по тому, называют ли они важность и фикс и целятся ли в код, а не в автора. А дифф, полный правок пробелов, проверяемый человеком вручную, — это отсутствующий гейт форматирования: разрешимый класс никогда не должен доходить до ревью. Диагностируй по артефакту, чини на правильном слое, держи человеческое внимание на том, что может решить только человек.
</Recap>
