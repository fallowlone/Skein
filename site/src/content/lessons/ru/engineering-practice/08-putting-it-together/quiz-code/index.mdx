---
concepts:
- test-as-design-pressure
- feature-flag-lifecycle
- ci-quality-gate
- postmortem-action-item
- consumer-driven-contract
estMin: 14
lang: ru
level: senior
order: 9
prereqs: []
slug: quiz-code
sources:
- https://getdx.com/blog/2024-dora-report/
- https://docs.getunleash.io/guides/trunk-based-development
- https://docs.devcycle.com/best-practices/tech-debt/
status: ready
summary: "Чтение реальных артефактов петли доставки — теста, feature flag, CI-гейта и action item постмортема — и выбор изменения, которое сеньор делает первым."
title: 'Собираем вместе: чтение кода и конфигов'
track: engineering-practice
unit: 08-putting-it-together
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Петля — не абстракция: она проявляется как тест, флаг, CI-гейт и action item постмортема. Прочитайте каждый артефакт, найдите, какое звено петли доставки он усиливает или ломает, и выберите фикс, который сеньор сделает первым.
</Hook>

<Goal lang="ru">
Потренировать чтение конкретных артефактов, которые порождает каждая практика — теста, feature flag, гейта пайплайна, action item постмортема — и оценку того, реально ли каждый из них замыкает петлю или тихо оставляет стык открытым.
</Goal>

## Сниппет 1 — проверка флага на горячем пути

```ts
// поставлен 14 месяцев назад под раскатку "new-checkout", так и не удалён
export function priceCart(cart: Cart): Money {
  if (flags.isEnabled("new-checkout-pricing")) {
    return newPricing(cart);   // раскатка "completed" много месяцев назад
  }
  return legacyPricing(cart);  // всё ещё здесь, всё ещё достижим
}
```

<Quiz
  id="08-putting-it-together-code-1"
  lessonSlug="08-putting-it-together"
  lang="ru"
  question="Раскатка new-checkout завершилась больше года назад. Какое сеньорное действие с этим флагом и почему оставить его дороже, чем кажется?"
  choices={[
    { label: "Удалить флаг и ветку legacyPricing: раскатка завершена, так что toggle теперь — постоянный долг. Каждый живой флаг — это ветка в проде (N флагов дают до 2^N достижимых состояний), а забытый release toggle — ровно то, что переключают на пике и что выстреливает мёртвым путём.", correct: true },
    { label: "Оставить — рабочий флаг безвреден и может понадобиться для будущего отката.", misconception: "Завершённый флаг — не инструмент отката, а живая ветка, которую никто не тестирует. Жизненный цикл (initial → live → completed → archived) существует потому, что пропуск удаления — это как release toggle становится следующим инцидентом в духе Knight Capital." },
    { label: "Превратить его в постоянную конфиг-настройку, чтобы ops могли переключать цены.", misconception: "Тихое перепрофилирование release toggle в permission toggle — классическая ловушка flag debt: 'временный' переключатель становится несущим навсегда, без владельца и без тестов на legacy-ветке." },
    { label: "Добавить unit-тест на legacy-ветку, чтобы она оставалась покрытой.", misconception: "Тестировать ветку, которую вы намерены удалить — потраченная работа, легитимизирующая мёртвый код. Фикс — удаление, которое заодно снимает бремя теста, а не добавление покрытия к долгу." },
  ]}
/>

## Сниппет 2 — тест, написанный первым

```ts
// написан до того, как появилась реализация
test("refund splits across original tender lines", () => {
  const order = orderWith([cash(30), card(70)]);
  const refund = refundService.refund(order, money(50));
  expect(refund.allocations).toEqual([cash(15), card(35)]);  // пропорционально
});
```

<Quiz
  id="08-putting-it-together-code-2"
  lessonSlug="08-putting-it-together"
  lang="ru"
  question="Команда пишет этот тест до того, как существует refundService. В чём главная ценность написать его первым — в терминах петли?"
  choices={[
    { label: "Это design pressure: вызов refund() и проверка формы allocation заставляют решить интерфейс до того, как от него зависят пятнадцать мест вызова, и он даёт быстрый, надёжный сигнал, который позже гейтит мержи в trunk. Покрытие — побочный продукт, а не цель.", correct: true },
    { label: "Это гарантирует более высокое покрытие кода, что и есть главная причина писать тесты первыми.", misconception: "Покрытие — побочный продукт. Выигрыш test-first — обратная связь по дизайну (плохой интерфейс виден за секунды) плюс надёжный гейт; погоня за покрытием как целью рождает хрупкие тесты, проверяющие реализацию, а не поведение." },
    { label: "Это позволяет пропустить код-ревью, ведь тест доказывает корректность.", misconception: "Тесты ловят регрессии против известной спеки; ревью ловит дизайн и намерение, которые автор теста не подумал проверить. Это разные звенья — зелёный тест не заменяет ревьюера, читающего изменение." },
    { label: "Писать первым важно только для end-to-end тестов, не для unit-тестов.", misconception: "Выигрыш design pressure сильнее всего на уровне unit/API, где вы формируете интерфейс. Петле нужен быстрый unit/контрактный сигнал как гейт мержа; e2e остаётся тонким слоем smoke." },
  ]}
/>

## Сниппет 3 — гейт CI-пайплайна

```yaml
# .ci/merge-gate.yml — обязателен для мержа в trunk
on: pull_request
jobs:
  gate:
    steps:
      - run: make unit            # ~90с, детерминированно
      - run: make contract        # верификация consumer + provider
      - run: make e2e-smoke       # только 1 критичный путь
      - run: make e2e-full || true # полный набор, разрешено падать
```

<Quiz
  id="08-putting-it-together-code-3"
  lessonSlug="08-putting-it-together"
  lang="ru"
  question="Этот гейт обязателен для мержа. Какой фикс конфига даёт наибольший рычаг и почему?"
  choices={[
    { label: "Убрать `|| true` у e2e-full или вынести его из гейта мержа целиком. Шаг, которому 'разрешено падать' — не гейт: он приучает команду игнорировать красное, тихо ломая CI-сигнал, делающий trunk-based мерж безопасным. Держите быстрые детерминированные шаги (unit, contract, smoke) настоящим гейтом.", correct: true },
    { label: "Добавить больше e2e-покрытия в гейт мержа, чтобы ничего не проскользнуло.", misconception: "Навешивание медленных флакающих e2e на гейт мержа — ровно то, что делает сигнал медленным и недоверенным, так что его заглушают — отказ, который гейт уже показывает. Петле нужен быстрый детерминированный гейт; e2e-full — для ночного или асинхронного потока." },
    { label: "Сделать каждый шаг `|| true`, чтобы флакающие тесты никогда не блокировали мерж.", misconception: "Это удаляет гейт целиком: сломанная работа свободно садится в trunk, и вы теряете предусловие безопасного частого мержа. Фикс — меньше детерминированных блокирующих проверок, а не превращение всего в неблокирующее." },
    { label: "Ничего — `|| true` на медленном наборе разумно держит мержи быстрыми.", misconception: "Держать в обязательном гейте шаг, который никогда не может упасть — это театр: похоже на покрытие, но не гейтит ничего, и инженеры узнают, что красное у набора бессмысленно. Либо он блокирует, либо уходит из гейта." },
  ]}
/>

## Сниппет 4 — action item постмортема

```md
## Action items
- [ ] Улучшить коммуникацию во время инцидентов          owner: ?        due: ?
- [x] Добавить контрактный тест: `cart.total` неотрицателен   owner: @ana   due: 2026-06-02   ticket: PAY-1183
- [ ] Быть аккуратнее при изменении конфига цен     owner: team     due: ongoing
```

<Quiz
  id="08-putting-it-together-code-4"
  lessonSlug="08-putting-it-together"
  lang="ru"
  question="Читая эти три action item из blameless-постмортема, какой реально замыкает петлю и почему два других проваливаются?"
  choices={[
    { label: "Петлю замыкает только пункт с контрактным тестом: у него есть владелец, дата, тикет, и он возвращает конкретное утверждение в CI, так что тот же дефект не повторится. Два других — без владельца, без даты, непроверяемы; 'быть аккуратнее' — не action item, а пожелание, дающее инциденту повториться.", correct: true },
    { label: "Все три валидны; мягкие пункты вроде 'быть аккуратнее' задают правильную культуру.", misconception: "Action item должен быть конкретным, owned и отслеживаемым, иначе это театр. 'Быть аккуратнее' не назначает работу и не меняет систему, так что сбой повторяется — ровно тот стык, что ломает петлю." },
    { label: "Пункт про коммуникацию важнее всего, ведь инциденты — в основном проблема коммуникации.", misconception: "Может, и важен, но без владельца и даты он ничего не выкатывает. Пункт без владельца неотличим от отсутствия пункта; петля замыкается только когда работа owned и отслеживается." },
    { label: "Ни один не важен — ценность постмортема в таймлайне, а не в action items.", misconception: "Таймлайн — для понимания; owned action items — то, что превращает инцидент в систему, которую вы починили. Без них постмортем — документ, по которому никто не действует, и тот же инцидент возвращается через квартал." },
  ]}
/>

<Recap lang="ru">
Каждая практика оставляет конкретный артефакт, и его чтение говорит, цела ли петля: завершённый флаг всё ещё на горячем пути — это долг к удалению, не инструмент отката; тест, написанный первым — это design pressure и сигнал мержа, с покрытием как побочным продуктом; CI-шаг, которому 'разрешено падать' — не гейт и тихо приучает команду игнорировать красное; а пункт постмортема замыкает петлю, только когда он owned, датирован, отслеживается и возвращает конкретное утверждение в CI. Читайте артефакт, находите звено, чините стык — не любуйтесь церемонией. Теперь, когда откроешь пулл-реквест и увидишь флаг, выкаченный год назад, `|| true` в конфиге гейта или постмортем без владельца, — ты знаешь единственный вопрос: какое звено это ломает?
</Recap>
