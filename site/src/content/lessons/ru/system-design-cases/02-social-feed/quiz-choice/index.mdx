---
slug: "quiz-choice"
lang: ru
track: "system-design-cases"
unit: "02-social-feed"
order: 5
title: "02-social-feed: обзор с выбором ответа"
summary: "Синтез кейсов соцленты в формате с выбором ответа: fan-out уведомлений и шторм ретраев, компромисс fan-out ленты и гибрид знаменитостей, server-push и порядок в диалоге у чата, правило офлайн-предвычисления у автодополнения."
estMin: 13
status: ready
lessonType: topic
level: senior
concepts:
  - fan-out
  - retry-dlq
  - hybrid-fanout
  - message-ordering
  - top-k-per-prefix
  - latency-budget
prereqs:
  - system-design-cases/02-social-feed/01-notification-system
  - system-design-cases/02-social-feed/02-news-feed
  - system-design-cases/02-social-feed/03-chat-system
  - system-design-cases/02-social-feed/04-search-autocomplete
sources:
  - https://firebase.google.com/docs/cloud-messaging/concept-options
  - https://www.rfc-editor.org/rfc/rfc6455
  - https://github.com/donnemartin/system-design-primer
---
import Hook from "~/components/lesson/Hook.astro";
import Crux from "~/components/prose/Crux.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Четыре решения по четырём кейсам юнита — каждое из тех, что принимаешь в дизайн-ревью, а не определение для пересказа. Нить, связывающая их, та же, что в любой read-heavy соцсистеме: сделай дорогую работу раз, на стороне записи или сборки, а путь к пользователю держи дешёвым лукапом.
</Hook>

<Crux>Четыре решения ревью: шторм ретраев, гибрид ленты, порядок в чате и правило предвычисления автодополнения.</Crux>

<Explanation>

Подтверди, что назовёшь недостающий страж дедупа в шторме ретраев, проведёшь знаменитость через гибрид ленты, наложишь консистентный порядок чата без глобальной координации и отвергнешь автодополнение, ранжирующее вживую на нажатие.

<Quiz
  id="02-social-feed-choice-1"
  lessonSlug="quiz-choice"
  lang="ru"
  question="Провайдер уведомлений восстанавливается после сбоя, бэклог хлынул заново; пользователи получают один push 5–9 раз. Ретраи с backoff уже на месте. Какой единственной части не хватает?"
  choices={[
    { label: "Ключа идемпотентности (событие+получатель+канал), проверяемого в быстром хранилище перед каждой отправкой, чтобы любое переотправленное или повторно хлынувшее сообщение схлопывалось в одну доставку.", correct: true },
    { label: "Exactly-once доставки, согласованной с провайдером.", misconception: "Exactly-once через провайдера, который ты не контролируешь, недостижим — он может доставить и провалить ack. At-least-once плюс страж идемпотентности дают пользователю effectively-once; этот страж и есть недостающая часть." },
    { label: "Убрать ретраи, чтобы каждое сообщение слалось раз.", misconception: "Это тихо роняет сообщения при любой временной икоте — обратный отказ. Сохрани ретраи (ограниченные, с backoff+джиттером) и добавь проверку идемпотентности, чтобы ретраи не дублировали." },
    { label: "Больше воркеров канала, чтобы бэклог сливался быстрее.", misconception: "Быстрее слить — просто быстрее доставить дубли. Дубли идут от переотправки без стража дедупа; пропускная способность это не решает." },
  ]}
/>

<Quiz
  id="02-social-feed-choice-2"
  lessonSlug="quiz-choice"
  lang="ru"
  question="Твоя лента использует fan-out-on-write, но пост знаменитости на 30M подписчиков вызывает шторм записей, стопорящий ленты всех. В чём стандартная починка?"
  choices={[
    { label: "Гибрид: сохранить push для обычных авторов (дешёвые чтения), но тянуть знаменитостей (хранить раз, сливать их недавние посты в ленту на чтении), с порогом по числу подписчиков, маршрутизирующим каждого автора.", correct: true },
    { label: "Перейти целиком на fan-out-on-read.", misconception: "Чистый pull делает каждую загрузку ленты scatter-gather по всем аккаунтам из подписок на каждом обновлении, без амортизации на ~100:1 чтениях — гибельно для read-heavy ленты. Сохрани push для частого случая; тяни лишь немногих знаменитостей." },
    { label: "Добавить fan-out воркеров и большее хранилище лент.", misconception: "Больше воркеров — всё равно 30M записей на пост знаменитости, масштабирование не той оси. Проблема в стратегии на хвосте степенного закона, а не в пропускной способности; тяни эти аккаунты." },
    { label: "Ограничить, сколько подписчиков может иметь знаменитость.", misconception: "Ты не можешь диктовать соцграф; степенной хвост врождён. Дизайн обязан тянуть аккаунты на 30M подписчиков, и способ — тянуть их на чтении." },
  ]}
/>

<Quiz
  id="02-social-feed-choice-3"
  lessonSlug="quiz-choice"
  lang="ru"
  question="Два пользователя чата на разных гейтвеях быстро переписываются; сообщения рендерятся не по порядку, и одно дублируется после переподключения. Как получить консистентный порядок и без дублей?"
  choices={[
    { label: "Маршрутизировать каждый диалог на один шард, назначающий монотонный seq в диалоге на персисте; клиенты рендерят по seq и дедупят по клиентскому id сообщения.", correct: true },
    { label: "Назначить единый глобальный seq по всем диалогам.", misconception: "Глобальный seq — ненужная координация и бутылочное горло, и бессмысленный, ведь порядок важен лишь внутри диалога. Seq на диалог на его шарде даёт тотальный порядок там, где он нужен, без глобальной сериализации." },
    { label: "Сортировать по локальной метке времени каждого клиента.", misconception: "Часы клиентов разъезжаются и не синхронизированы, так что сортировка по метке переупорядочивает при дрифте. Порядок должен идти от seq на диалог, назначенного на персисте; дедуп всё равно нужен id сообщения." },
    { label: "Подтверждать быстрее, чтобы сообщения не приходили не по порядку.", misconception: "Скорость не накладывает порядок — сетевое переупорядочивание и ретраи происходят независимо. Нужен seq для порядка и id сообщения для дедупа." },
  ]}
/>

<Quiz
  id="02-social-feed-choice-4"
  lessonSlug="quiz-choice"
  lang="ru"
  question="Автодополнение запрашивает логи поиска на каждое нажатие ради ранжирования совпадений; тормозит на 600 мс и перегружает кластер. В чём архитектурная починка?"
  choices={[
    { label: "Разбить ранжирование и отдачу: офлайн-пайплайн периодически ранжирует запросы в trie с предвычисленным top-k на узле; путь запроса идёт к префиксу и возвращает закешированный top-k (O(1), без доступа к логам), за edge-кешем для популярных префиксов.", correct: true },
    { label: "Добавить read-реплики, чтобы агрегации на нажатие разошлись.", misconception: "Больше реплик всё равно гоняют тяжёлую агрегацию на нажатие — масштабирование не того. Задержка — ранжирование на пути чтения по большому датасету; предвычисли офлайн и отдавай O(1) лукап." },
    { label: "Кешировать результаты каждого полного запроса.", misconception: "Автодополнение про префиксы, не полные запросы; кеширование полного запроса ничего не даёт частичным префиксам на нажатие. Нужен trie префиксов с предвычисленным top-k на узле, построенный офлайн." },
    { label: "Дебаунсить жёстче на клиенте.", misconception: "Дебаунс срезает нагрузку чуть-чуть, но даже один запрос триггерит живую агрегацию, взрывающую бюджет в десятки мс. Надо убрать ранжирование с пути чтения вовсе." },
  ]}
/>

</Explanation>

<KeyTakeaway>По четырём кейсам сеньорный ход один: предвычисляй или буферизуй на стороне записи/сборки — ключи дедупа, гибрид ленты, упорядоченные на персисте сообщения, офлайн-trie — чтобы путь чтения был дешёвым и безопасным.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="02-social-feed-choice-retrieval"
  lang="ru"
  questions={[
    { q: "Почему ретраям уведомлений нужен ключ идемпотентности и почему ленте нужен гибрид?", a: "Ретраи at-least-once, так что без ключа идемпотентности (событие+получатель+канал), проверяемого перед отправкой, любое переотправленное или повторно хлынувшее сообщение дублируется на устройстве — шторм ретраев. Ленте нужен гибрид, потому что степенное распределение подписчиков ломает единую стратегию: push дёшев для обычных авторов, но шторм записей для знаменитостей, так что push для обычных и pull для знаменитостей, слитый на чтении." },
    { q: "Как порядок чата и отдача автодополнения избегают дорогой работы на пути пользователя?", a: "Чат назначает монотонный seq в диалоге на персисте на шарде диалога, так что порядок не нуждается в глобальной координации, и клиенты просто рендерят по seq (дедуп по id сообщения). Автодополнение предвычисляет top-k на узле trie в офлайн-пайплайне, так что отдача — O(1) закешированное чтение после прохода к префиксу, никогда не живое ранжирование по логам." },
  ]}
/>

<Recap lang="ru">
Четыре кейса рифмуются. **Система уведомлений** ретраит at-least-once, потому ей нужен **ключ идемпотентности** ради избегания шторма ретраев — ретраи без дедупа дублируют. **Лента новостей** read-heavy, потому толкает (fan-out-on-write) обычных авторов, но **тянет знаменитостей** в гибриде, ведь степенной хвост ломает единую стратегию. **Чат** упорядочивает сообщения **seq в диалоге**, назначенным на персисте — тотальный порядок там, где он важен, без глобальной координации — и дедупит по id сообщения. **Автодополнение** никогда не ранжирует вживую на нажатие; оно отдаёт **предвычисленный top-k** из trie, построенного офлайн, O(1) чтение за edge-кешем. Объединяющий принцип всех четырёх: сделай дорогую работу раз, на стороне записи или сборки, а путь к пользователю держи дешёвым, безопасным лукапом.
</Recap>
