---
slug: "quiz-short"
lang: ru
track: "system-design-cases"
unit: "02-social-feed"
order: 6
title: "02-social-feed: обзор со свободным припоминанием"
summary: "Промпты на свободное припоминание по кейсам соцленты. Реконструируй каждый дизайн по памяти — пайплайн уведомлений, гибрид fan-out ленты, push/порядок чата, предвычисление автодополнения — до того, как откроешь образец."
estMin: 14
status: ready
lessonType: topic
level: senior
concepts:
  - fan-out
  - hybrid-fanout
  - message-ordering
  - delivery-receipts
  - top-k-per-prefix
  - latency-budget
prereqs:
  - system-design-cases/02-social-feed/01-notification-system
  - system-design-cases/02-social-feed/02-news-feed
  - system-design-cases/02-social-feed/03-chat-system
  - system-design-cases/02-social-feed/04-search-autocomplete
sources:
  - https://developer.apple.com/documentation/usernotifications/setting_up_a_remote_notification_server
  - https://engineering.fb.com/2014/06/12/core-infra/tao-the-power-of-the-graph/
  - https://github.com/donnemartin/system-design-primer
---
import Hook from "~/components/lesson/Hook.astro";
import Crux from "~/components/prose/Crux.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";

<Hook>
Припоминание бьёт перечитывание. На каждый промпт реконструируй полный дизайн по памяти — пайплайн, компромисс, режим отказа — до того, как откроешь образец. Усилие пересборки архитектуры и есть то, что закрепляет эти четыре кейса.
</Hook>

<Crux>Реконструируй четыре кейса: пайплайн уведомлений, гибрид ленты, push/порядок чата, предвычисление автодополнения.</Crux>

<Explanation>

Пересобери каждый дизайн без подглядывания: пайплайн fan-out уведомлений и что предотвращает шторм ретраев; компромисс fan-out ленты и гибрид знаменитостей; транспорт push, порядок и галочки доставки чата; и почему автодополнение предвычисляет всё офлайн.

<RetrievalDrawer
  client:load
  id="02-social-feed-short-retrieval"
  lang="ru"
  questions={[
    { q: "Реконструируй пайплайн уведомлений и три куска, предотвращающих шторм ретраев.", a: "API события (валидация, персист, в очередь, сразу возврат), затем очередь событий, затем fan-out воркер (разрешить получателей, пагинировать с чекпоинтингом, topics провайдера для широковещания знаменитостей), затем фильтр настроек + дедуп (ключ идемпотентности), затем очереди каналов + воркеры (каждый владеет одним провайдером со своими rate limit, ретраем, DLQ). Шторм ретраев — дубли и повторное удушение, когда бэклог восстановившегося провайдера хлынет — предотвращают три вещи вместе: экспоненциальный backoff с джиттером, ограниченное число попыток, питающее DLQ, и ключ идемпотентности, проверяемый перед отправкой. Пропуск любой переоткрывает дыру." },
    { q: "Назови компромисс fan-out ленты, отношение чтение:запись, что его решает, и гибрид знаменитостей.", a: "Fan-out-on-write (push) предвычисляет ленту каждого подписчика, так что чтения — O(1) лукап кеша, но записи усиливаются числом подписчиков; fan-out-on-read (pull) хранит каждый пост раз и собирает на чтении, так что записи дёшевы, но каждое чтение — неамортизированный scatter-gather. Лента ~100:1 read-heavy, потому push — дефолт. Ломатель — степенное распределение подписчиков: пост знаменитости под push — шторм на десятки миллионов записей, так что тянешь знаменитостей (хранить раз, слить на чтении) и толкаешь остальных — гибрид, с порогом по числу подписчиков. Работает, потому что знаменитости делают push дорогим, но редки на читателя, так что тянуть их стоит каждому читателю лишь нескольких лишних лукапов." },
    { q: "Объясни транспорт чата, правило персист-до-ack, порядок и три галочки.", a: "Транспорт — постоянные WebSocket к stateful tier гейтвеев соединений (масштабируется по одновременным соединениям), с реестром соединений, отображающим юзера/устройство на гейтвей, чтобы stateless сервис маршрутизировал. Доставка at-least-once с дедупом по клиентскому id сообщения, и правило — персист до ack — галочка 'отправлено' значит долговечно сохранено, а не 'сервер видел'. Порядок в диалоге через монотонный seq на персисте на шарде диалога (он же курсор офлайн-синхронизации). Три галочки: отправлено (персистировано), доставлено (устройство получателя приняло и подтвердило), прочитано (получатель открыл чат) — каждая подтверждение, текущее обратно отправителю." },
    { q: "Почему автодополнение обязано предвычислять и как trie с top-k на узле отдаёт за O(1)?", a: "Бюджет ~100 мс (десятки мс серверу) не уместит агрегацию по миллиардам строк лога, и каждое нажатие — запрос, так что живое ранжирование и слишком медленно, и шторм нажатий. Потому офлайн-пайплайн считает частоту запросов за скользящее окно с затуханием, фильтрует редкие/забаненные и строит trie. Trie хранит строки по символам от корня, так что префикс — один узел, а его дополнения — поддерево под ним; вместо обхода этого поддерева сборка хранит top-k дополнений прямо на каждом узле. Отдача тогда — O(длина префикса) на спуск плюс O(1) на чтение закешированного top-k, публикуемого как неизменяемый снапшот и за edge-кешем для популярных префиксов." },
  ]}
/>

</Explanation>

<KeyTakeaway>Реконструируй каждый кейс по памяти — пайплайн уведомлений, гибрид ленты, push/порядок чата, предвычисление автодополнения — и держишь спину юнита: read-heavy соцсистемы толкают стоимость на сторону записи/сборки.</KeyTakeaway>

<Recap lang="ru">
Если можешь пересобрать каждый дизайн по памяти, ты держишь спину юнита. **Система уведомлений** — асинхронный пайплайн fan-out, чей шторм ретраев предотвращают backoff+джиттер, **DLQ** с ограниченными ретраями и **ключ идемпотентности**. **Лента новостей** дефолтит в **push** (read-heavy, ~100:1), но **тянет знаменитостей** в гибриде, ведь степенной хвост ломает единую стратегию. **Чат** — **server-push** по WebSocket, маршрутизируемый через реестр соединений, **персистит до ack** и упорядочивает сообщения **seq в диалоге**. **Автодополнение** предвычисляет **top-k на узле trie** офлайн и отдаёт O(1) чтение за edge-кешем, никогда не ранжируя вживую на нажатие. Сквозная нить: на этой глубине каждая read-heavy соцсистема двигает дорогую работу на сторону записи или сборки, чтобы путь к пользователю был дешёвым, безопасным лукапом.
</Recap>
