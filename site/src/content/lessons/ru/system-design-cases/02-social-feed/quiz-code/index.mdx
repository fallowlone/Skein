---
slug: "quiz-code"
lang: ru
track: "system-design-cases"
unit: "02-social-feed"
order: 7
title: "02-social-feed: чтение конфигов и кода"
summary: "Читай реальный конфиг и код четырёх кейсов, затем выбирай сеньорную починку: цикл ретраев без дедупа, лента на offset, чат-хендлер с ack до персиста и автодополнение, ранжирующее на пути чтения."
estMin: 14
status: ready
lessonType: topic
level: senior
concepts:
  - idempotency-dedup
  - cursor-pagination
  - message-ordering
  - latency-budget
  - retry-dlq
  - top-k-per-prefix
prereqs:
  - system-design-cases/02-social-feed/01-notification-system
  - system-design-cases/02-social-feed/02-news-feed
  - system-design-cases/02-social-feed/03-chat-system
  - system-design-cases/02-social-feed/04-search-autocomplete
sources:
  - https://aws.amazon.com/builders-library/timeouts-retries-and-backoff-with-jitter/
  - https://redis.io/docs/latest/develop/data-types/sorted-sets/
  - https://github.com/donnemartin/system-design-primer
---
import Hook from "~/components/lesson/Hook.astro";
import Crux from "~/components/prose/Crux.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Баги соцсистем прячутся в нескольких строках: ретрай без проверки дедупа, OFFSET в запросе ленты, ack до записи, вызов базы на пути чтения. Читай каждый сниппет, прослеживай, что происходит на масштабе, и выбирай изменение, которое закоммитит сеньор.
</Hook>

<Crux>Читай сниппет, найди недостающий шов — дедуп, курсор, персист-до-ack, предвычисление — и выбирай починку, которой требует режим отказа.</Crux>

<Explanation>

Отрабатывай цикл дизайн-ревью: находи несущую строку, рассуждай, что она делает на миллионах событий, и выбирай починку, что адресует режим отказа, а не замазывает его.

## Сниппет 1 — цикл ретраев уведомлений

```python
def deliver(msg):
    for attempt in range(5):
        try:
            provider.send(msg)          # может таймаутить / 429
            return
        except Retryable:
            sleep(2 ** attempt)         # экспоненциальный backoff
    dead_letter.put(msg)
```

<Quiz
  id="02-social-feed-code-1"
  lessonSlug="quiz-code"
  lang="ru"
  question="Здесь есть ограниченные ретраи, backoff и DLQ. После восстановления провайдера пользователи всё ещё получают дубли push. Чего не хватает?"
  choices={[
    { label: "Стража дедупа: проверять ключ идемпотентности (событие+получатель+канал) в быстром хранилище перед provider.send и пропускать, если виден. Ретраи at-least-once, так что без него повторно хлынувший бэклог дублируется на устройстве.", correct: true },
    { label: "Добавить джиттер в backoff — и дубли прекратятся.", misconception: "Джиттер растекает ретраи ради избегания thundering herd на восстановившемся провайдере — стоит добавить — но не останавливает дубли. Переотправленное или повторно хлынувшее сообщение всё равно шлётся дважды без проверки идемпотентности перед отправкой." },
    { label: "Поднять потолок ретраев выше 5.", misconception: "Больше ретраев дают БОЛЬШЕ дублей, не меньше. Дубль идёт от переотправки без стража дедупа; потолок управляет тем, когда сдаться в DLQ, а не тем, дублирует ли переотправка." },
    { label: "Ловить все исключения, не только Retryable.", misconception: "Ретрай постоянных ошибок (невалидный токен, hard bounce) тратит отправки и спотыкается об abuse-лимиты — надо ретраить МЕНЬШЕ типов ошибок, не больше. И это всё равно не адресует дубли, которым нужен ключ идемпотентности." },
  ]}
/>

## Сниппет 2 — запрос ленты

```sql
SELECT post_id FROM feed
WHERE user_id = :uid
ORDER BY created_at DESC
LIMIT 20 OFFSET :n;     -- n растёт по мере прокрутки
```

<Quiz
  id="02-social-feed-code-2"
  lessonSlug="quiz-code"
  lang="ru"
  question="Глубокие прокрутки медленны, и пользователи сообщают, что посты появляются дважды или пропускаются. В чём причина и починка?"
  choices={[
    { label: "Offset-пагинация сканирует и отбрасывает n строк (медленнее, чем глубже) и сдвигается, когда новые посты приходят между запросами, вызывая пропуски/дубли. Используй курсорную (keyset) пагинацию: передавай скор/id последнего элемента и возвращай элементы после него — O(1) по глубине, стабильно при вставках.", correct: true },
    { label: "Добавить индекс на created_at.", misconception: "Индекс помогает сортировке, но OFFSET n всё равно сканирует и выбрасывает n строк, так что медленнее с глубиной независимо, и всё ещё сдвигается при конкурентных вставках. Структурная починка — курсор, а не индекс." },
    { label: "Кешировать каждую страницу по её offset.", misconception: "Нижележащий список сдвигается при вставке постов, так что закешированные страницы всё равно пропускают/дублируют, а персонализированная лента молотит кеш. Курсорная пагинация чинит и стоимость, и корректность." },
    { label: "Уменьшить LIMIT до 10, чтобы OFFSET оставался малым.", misconception: "OFFSET n всё равно пропускает n строк как ни режь, а глубокая прокрутка копит большой n; он всё ещё пропускает/дублирует при вставках. Привяжись к последнему виденному элементу курсором." },
  ]}
/>

## Сниппет 3 — чат-хендлер отправки

```python
def on_message(ws, msg):
    ack(ws, msg.client_id)              # сказать отправителю: доставлено ✓
    seq = persist(msg)                  # назначить seq, сохранить
    route_to_recipient(msg, seq)
```

<Quiz
  id="02-social-feed-code-3"
  lessonSlug="quiz-code"
  lang="ru"
  question="В чём баг корректности и как переупорядочить хендлер?"
  choices={[
    { label: "Он подтверждает до персиста: крах между ack и persist теряет сообщение, пока UI отправителя показывает уверенную галочку. Сначала персист (и назначь seq), затем ack — галочка 'отправлено' должна значить долговечно сохранено.", correct: true },
    { label: "Надо подтверждать после возврата подтверждения доставки от route_to_recipient.", misconception: "Галочка 'отправлено' значит, что сервер долговечно держит сообщение, а не что получатель принял (это отдельная галочка 'доставлено'). Починка — персист-затем-ack; привязка ack к доставке получателю смешивает две разные галочки." },
    { label: "Ничего не сломано; ack первым делает UI быстрее.", misconception: "Эта скорость — ровно ловушка: если процесс падает после оптимистичного ack и до персиста, сообщение тихо потеряно — единственное, что чат никогда не должен делать. Персист до ack." },
    { label: "Нужен лок вокруг persist ради предотвращения гонок.", misconception: "Баг — в порядке, не в конкурентности: ack происходит до долговечной записи. Персист первым чинит это; лок не адресует окно потери сообщения при крахе между ack и persist." },
  ]}
/>

## Сниппет 4 — эндпойнт автодополнения

```python
def suggest(prefix):
    rows = db.query(
      "SELECT q FROM search_log WHERE q LIKE %s GROUP BY q ORDER BY count(*) DESC LIMIT 10",
      prefix + "%")            # бежит на каждое нажатие
    return [r.q for r in rows]
```

<Quiz
  id="02-social-feed-code-4"
  lessonSlug="quiz-code"
  lang="ru"
  question="Это ранжирует совпадающие запросы вживую на каждое нажатие. Тормозит на 600 мс и перегружает кластер. В чём починка?"
  choices={[
    { label: "Вынести ранжирование офлайн: периодический пайплайн агрегирует лог в trie с предвычисленным top-k на узле; suggest() тогда идёт к префиксу и возвращает закешированный top-k (O(1), без доступа к логам), за edge-кешем для популярных префиксов.", correct: true },
    { label: "Добавить индекс на колонку q.", misconception: "Индекс ускоряет скан LIKE, но ты всё равно гоняешь GROUP BY / ORDER BY агрегацию на каждое нажатие по огромному логу — слишком медленно для десятков мс, сотни тысяч раз в секунду. Ранжирование не должно быть на пути чтения вовсе." },
    { label: "Кешировать результат на полную строку префикса в Redis.", misconception: "Наивный кеш на префикс всё равно постоянно промахивается на длинном хвосте и устаревает без пайплайна обновления; структурный ответ — предвычисленный trie с top-k на узле, обновляемый офлайн-сборкой." },
    { label: "Ограничить запрос последними сутками логов.", misconception: "Меньшее окно — всё равно живая агрегация на каждое нажатие — быстрее, но всё ещё на пути чтения и всё ещё шторм нажатий. Предвычисли ранжирование офлайн и отдавай O(1) лукап trie." },
  ]}
/>

</Explanation>

<KeyTakeaway>Каждый баг — недостающий шов: дедуп перед отправкой, курсор вместо OFFSET, персист до ack и предвычисление-офлайн вместо ранжирования на пути чтения. Читай несущую строку и чини режим отказа, а не симптом.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="02-social-feed-code-retrieval"
  lang="ru"
  questions={[
    { q: "Почему цикл ретраев с backoff и DLQ всё равно дублирует и почему OFFSET неверен для ленты?", a: "Ретраи at-least-once по дизайну, так что повторно хлынувшее или переотправленное сообщение шлётся дважды, если ключ идемпотентности (событие+получатель+канал) не проверен в быстром хранилище перед отправкой — backoff и DLQ управляют тем, когда и как ретраить, а не тем, дублирует ли переотправка. OFFSET n сканирует и отбрасывает n строк, так что медленнее по мере прокрутки, а список сдвигается, когда новые посты приходят между запросами, вызывая пропущенные или дублированные элементы; курсорная (keyset) пагинация передаёт скор/id последнего виденного и возвращает элементы после него, что O(1) на любой глубине и стабильно при вставках." },
    { q: "Почему чат-хендлер обязан персистить до ack и почему автодополнение не может ранжировать на пути чтения?", a: "Если хендлер подтверждает 'отправлено' до долговечного персиста, крах в этом окне теряет сообщение, пока отправитель видит уверенную галочку — тихая потеря, чего чат никогда не должен делать; сначала персист (и назначь seq в диалоге), затем ack, чтобы галочка значила долговечно сохранено. Автодополнение не может ранжировать вживую, потому что бюджет ~100 мс (десятки мс серверу) не уместит агрегацию по миллиардам строк лога, и каждое нажатие — запрос, так что живое ранжирование — шторм нажатий; ранжирование предвычисляется офлайн в trie с top-k на узле и отдаётся как O(1) чтение." },
  ]}
/>

<Recap lang="ru">
Четыре сниппета прячут по однострочному отказу. Цикл ретраев **уведомлений** имеет backoff и DLQ, но нет **ключа идемпотентности**, так что повторно хлынувший бэклог дублирует — ретраи at-least-once, дедуп обязан сторожить отправку. Запрос **ленты** использует **OFFSET**, что сканирует n строк (медленно на глубине) и сдвигается при вставках (пропуски/дубли); починка — **курсорная пагинация**. Хендлер **чата** **подтверждает до персиста**, так что крах теряет сообщение под уверенной галочкой; сначала персист (и назначь seq), затем ack. Эндпойнт **автодополнения** ранжирует **вживую на нажатие** по логу, что не уместит бюджет или нагрузку; предвычисли **top-k на узле trie** офлайн и отдавай O(1) чтение. Сеньорная привычка: найди несущую строку, рассуди о ней на масштабе и приложи починку, которой требует режим отказа.
</Recap>
