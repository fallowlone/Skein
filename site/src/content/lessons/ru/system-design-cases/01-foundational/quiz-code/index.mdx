---
slug: "quiz-code"
lang: ru
track: "system-design-cases"
unit: "01-foundational"
order: 7
title: "Фундаментальные кейсы: чтение чисел и конфига"
summary: "Читай реальную математику ёмкости и конфиг из четырёх кейсов и считай: проверка пересечения кворума, оценка QPS/keyspace сокращателя, размер bloom filter краулера и расчёт числа партиций Kafka."
estMin: 14
status: ready
lessonType: topic
level: senior
concepts:
  - quorum
  - capacity-estimation
  - bloom-filter-dedup
  - partitioned-log
  - base62-encoding
  - read-heavy-caching
prereqs:
  - 01-foundational/01-key-value-store
  - 01-foundational/02-url-shortener
  - 01-foundational/03-web-crawler
  - 01-foundational/04-distributed-message-queue
sources:
  - https://www.allthingsdistributed.com/files/amazon-dynamo-sosdp2007.pdf
  - https://kafka.apache.org/documentation/
  - https://github.com/donnemartin/system-design-primer
---
import Hook from "~/components/lesson/Hook.astro";
import Crux from "~/components/prose/Crux.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Баги дизайна прячутся в числах, не в прозе: кворум, что не пересекается, QPS, ошибающийся на 10^5, bloom filter, размеренный под неверную долю ошибок, число партиций, ограничивающее пропускную способность. Читай каждый сниппет, считай в уме и выбери ответ, под которым подпишется senior-инженер.
</Hook>

<Crux>Читай конфиг кейса, найди кворум, QPS, размер bloom filter или число партиций, посчитай и выбери починку, что поддерживает математика.</Crux>

<Explanation>

Практикуй цикл, что крутишь на ревью дизайна: найди числа в конфиге, примени правило (пересечение кворума, daily-в-QPS, биты bloom filter, пропускная способность партиции) и выбери изменение, которое арифметика реально поддерживает.

## Сниппет 1 — конфиг кворума

```yaml
# распределённое KV-хранилище
replication_factor_N: 3
write_quorum_W: 1
read_quorum_R: 1
# "настроено под низкую задержку"
```

<Quiz
  id="01-foundational-code-1"
  lessonSlug="quiz-code"
  lang="ru"
  question="С этим конфигом может ли клиент всегда прочитать обратно только что записанное значение на строгом кворуме, и какая починка, если нет?"
  choices={[
    { label: "Нет: R + W = 2 ≤ N = 3, так что множества чтения и записи могут не пересекаться, и чтение может промахнуться мимо нового значения. Чини, сделав R + W > N — например W=2, R=2 — чтобы множества пересекались.", correct: true },
    { label: "Да — одна запись и одно чтение всегда согласованы.", misconception: "Лишь если R + W > N. Здесь R + W = 2 ≤ 3, так что одно чтение может попасть на два узла, не получивших запись W=1. Нужно R + W > N для гарантированного пересечения." },
    { label: "Нет, и починка — поднять N до 5.", misconception: "Поднятие N без поднятия R и W держит R + W ≤ N и делает пересечение менее вероятным, не более. Починка — неравенство R + W > N, например W=2,R=2 при N=3." },
    { label: "Да, потому что записи всегда идут на все N реплик.", misconception: "Запись для возврата нуждается лишь в W подтверждениях; при W=1 гарантированно имеет её лишь одна реплика. Чтение R=1 может попасть на другую. R + W > N — то, что вынуждает пересечение." },
  ]}
/>

## Сниппет 2 — оценка сокращателя

```python
def shortener_capacity(new_urls_per_month: float, read_write_ratio: float = 100):
    writes_per_sec = new_urls_per_month / (30 * 86_400)
    reads_per_sec = writes_per_sec * read_write_ratio
    return round(writes_per_sec), round(reads_per_sec)

# 100 миллионов новых URL/месяц
print(shortener_capacity(100_000_000))
```

<Quiz
  id="01-foundational-code-2"
  lessonSlug="quiz-code"
  lang="ru"
  question="Примерно что вернёт shortener_capacity(100_000_000) и какое число диктует архитектуру?"
  choices={[
    { label: "~40 записей/с и ~4 000 чтений/с. Число чтений диктует всё — при ~100:1 путь чтения редиректа (cache-aside над KV-хранилищем) и есть система; ~40 записей/с — мелочь.", correct: true },
    { label: "~40 записей/с и ~40 чтений/с — чтения и записи равны.", misconception: "Отношение 100:1, так что чтения = записи × 100 ≈ 4 000/с, не 40. Весь смысл сокращателя в том, что чтения массово доминируют, поэтому ты кешируешь путь редиректа." },
    { label: "~100 000 000 записей/с — это месячная цифра.", misconception: "Это в МЕСЯЦ. Деление на ~2,6M секунд/месяц даёт ~40 записей/с. Всегда переводи месячный итог в в-секунду до рассуждений о нагрузке." },
    { label: "~4 000 записей/с и ~40 чтений/с.", misconception: "Ты поменял их местами. Записи — малое число (~40/с); чтения — записи × 100 ≈ 4 000/с. Чтения доминируют, так что путь редиректа/кеш — куда идёт дизайн." },
  ]}
/>

## Сниппет 3 — размер bloom filter

```python
# множество виденных URL краулера
exact_set_urls = 10_000_000_000       # 10 миллиардов виденных URL
bytes_per_url_exact = 100             # ~100 байт/URL точно
bits_per_url_bloom = 10               # цель ~1% ложно-положительных

exact_ram_bytes = exact_set_urls * bytes_per_url_exact
bloom_ram_bytes = exact_set_urls * bits_per_url_bloom / 8
```

<Quiz
  id="01-foundational-code-3"
  lessonSlug="quiz-code"
  lang="ru"
  question="Примерно сколько RAM нужно каждому подходу и чего стоит bloom filter?"
  choices={[
    { label: "Точное множество ≈ 1 ТБ (10^10 × 100 Б); bloom filter ≈ 12,5 ГБ (10^10 × 10 / 8). Bloom filter ~80x меньше, ценой изредка пометить новый URL виденным и пропустить — нормально на этом масштабе.", correct: true },
    { label: "Оба ≈ 1 ТБ — bloom filter не экономит память.", misconception: "Bloom filter использует ~10 бит на URL против 100 байт (800 бит) точно — примерно в 80x меньше, ~12,5 ГБ против ~1 ТБ. Экономия этой памяти — вся причина использовать его для дедупа веб-масштаба." },
    { label: "Bloom filter ≈ 1 ТБ; точное множество ≈ 12,5 ГБ.", misconception: "Наоборот. Точное множество по 100 байт/URL — это ~1 ТБ; bloom filter по ~10 бит/URL — ~12,5 ГБ. Bloom filter — малое — в этом смысл." },
    { label: "Bloom filter ≈ 12,5 ГБ без минусов.", misconception: "Математика памяти верна, но он не бесплатен: у bloom filter настраиваемая доля ложно-положительных, так что он может изредка пометить реально новый URL виденным и пропустить. Приемлемо здесь, но реальная цена." },
  ]}
/>

## Сниппет 4 — число партиций Kafka

```python
# размер Kafka-подобного топика
target_throughput_mb_s = 1000     # ~1 ГБ/с на пике
per_partition_mb_s = 10           # потолок упорядоченной реплиц. записи на партицию
consumers_in_group = 8

partitions_needed = target_throughput_mb_s / per_partition_mb_s
```

<Quiz
  id="01-foundational-code-4"
  lessonSlug="quiz-code"
  lang="ru"
  question="Сколько партиций нужно этому топику и каково последствие для группы из 8 консьюмеров?"
  choices={[
    { label: "~100 партиций (1000 / 10). При ~100 партициях и лишь 8 консьюмерах в группе каждый читает ~12 партиций — нормально; группа могла бы вырасти до 100 консьюмеров максимум (одна партиция на консьюмера) до того, как добавление перестанет что-либо давать.", correct: true },
    { label: "8 партиций — по одной на консьюмера.", misconception: "8 партиций ограничивают пропускную способность до 8 × 10 = 80 МБ/с, далеко ниже цели 1 ГБ/с. Нужно ~100 партиций ради пропускной способности; число консьюмеров — отдельный лимит (максимум консьюмеров = партиций)." },
    { label: "~100 партиций, но лишь 8 могут использоваться, ведь консьюмеров 8.", misconception: "Все ~100 партиций пишутся и читаются; при 8 консьюмерах каждый просто обрабатывает ~12 партиций. Консьюмеры ≤ партиций — потолок параллелизма, не потолок того, сколько партиций существует или используется." },
    { label: "~10 партиций (1 ГБ / 10 МБ, считая единицы равными).", misconception: "Следи за единицами: 1000 МБ/с ÷ 10 МБ/с = ~100 партиций, не 10. Смешение ГБ и МБ роняет множитель ~10 — ровно такой промах единиц недопровижинит топик." },
  ]}
/>

</Explanation>

<KeyTakeaway>Каждое решение кейса — арифметика в конфиге: R+W>N для пересечения кворума, daily ÷ секунды × отношение для QPS, биты × число для bloom filter, цель ÷ на-партицию для числа партиций. Следи за единицами.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="01-foundational-code-retrieval"
  lang="ru"
  questions={[
    { q: "Как проверить, что конфиг кворума даёт read-your-write, и в чём ловушка?", a: "Проверь R + W > N. Если сумма ≤ N (например W=1, R=1 при N=3 → сумма 2), множества чтения и записи могут не пересекаться, так что чтение может промахнуться мимо только что записанного значения. Починка — любая настройка с R + W > N (например W=2, R=2). Ловушка — думать «я записал, так что чтение видит» — это держится лишь когда кворумы вынуждены пересекаться неравенством." },
    { q: "Как размерить bloom filter краулера и сколько партиций Kafka под цель пропускной способности?", a: "Bloom filter: ~10 бит/URL под долю ложно-положительных ~1%, так что 10 миллиардов URL ≈ 10^10 × 10 / 8 ≈ 12,5 ГБ — против ~1 ТБ для точного множества по 100 байт/URL (~80x меньше), ценой изредка пропустить новый URL. Партиции: цель пропускной способности ÷ потолок на партицию, например 1000 МБ/с ÷ 10 МБ/с ≈ 100 партиций — и максимум консьюмеров в группе = число партиций, так что ~100 партиций поддерживают до 100 параллельных консьюмеров. Следи за единицами: смешение ГБ и МБ роняет множитель 10." },
  ]}
/>

<Recap lang="ru">
Каждое решение дизайна в этих кейсах сводится к арифметике, что читаешь прямо с конфига. Проверка **кворума** — `R + W > N`: конфиг W=1, R=1 при N=3 суммируется в 2 ≤ 3, так что чтения могут промахиваться мимо записей — чини W=2, R=2. **Оценка сокращателя** переводит 100M URL/месяц в ~40 записей/с и, при 100:1, ~4 000 чтений/с — и число чтений диктует архитектуру кеш-и-редирект. **Размер bloom filter** показывает, что ~10 бит/URL дают ~12,5 ГБ для 10 миллиардов URL против ~1 ТБ точно (~80x меньше), ценой малой доли ложно-положительных. **Число партиций** — цель пропускной способности ÷ потолок на партицию (1000 ÷ 10 ≈ 100 партиций), что также ограничивает параллелизм консьюмеров числом партиций. Senior-привычка одна и та же для всех четырёх: найди числа, посчитай, следи за единицами и выбери починку, что поддерживает арифметика — а не ту, что её прячет.
</Recap>
