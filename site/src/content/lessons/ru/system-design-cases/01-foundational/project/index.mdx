---
slug: "project"
lang: ru
track: "system-design-cases"
unit: "01-foundational"
order: 8
title: "Фундаментальные кейсы: напишите полный дизайн-документ"
summary: "Практический проект: возьми один из четырёх фундаментальных кейсов и сделай полный дизайн-документ — требования, оценка на салфетке, диаграмма высокоуровневой архитектуры, два глубоких погружения и раздел узких мест и компромиссов."
estMin: 240
status: ready
lessonType: topic
level: senior
concepts:
  - design-document
  - capacity-estimation
  - high-level-design
  - deep-dive
  - bottlenecks-and-tradeoffs
  - delivery-semantics
prereqs:
  - 01-foundational/01-key-value-store
  - 01-foundational/02-url-shortener
  - 01-foundational/03-web-crawler
  - 01-foundational/04-distributed-message-queue
sources:
  - https://github.com/donnemartin/system-design-primer
  - https://www.allthingsdistributed.com/files/amazon-dynamo-sosdp2007.pdf
  - https://kafka.apache.org/documentation/
---
import Hook from "~/components/lesson/Hook.astro";
import Crux from "~/components/prose/Crux.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import ProjectBrief from "~/components/pedagogy/ProjectBrief.astro";

<Hook>
Прочитать четыре разобранных кейса — не то же, что сделать один самому под ограничениями, которые навязывает реальное ревью. Выбери одну из четырёх систем, напиши полный дизайн-документ от начала до конца — требования, оценку, выбирающую форму, архитектуру, два сложных компонента и режимы отказа — и защити каждый порог числом, которое staff-инженер сможет проверить.
</Hook>

<Crux>Преврати один кейс в дизайн-документ: требования, оценка, выбирающая форму, архитектура, два глубоких погружения и узкие места.</Crux>

<Explanation>

Этот проект делает юнит операционным: ты прогоняешь полный цикл дизайна для одной системы, обосновываешь каждое решение арифметикой из кейсов и вскрываешь компромиссы, отделяющие дизайн, что работает, от того, что выживает. Результат — документ, не код; навык — рассуждение, не печатание.

<ProjectBrief
  id="01-foundational-project"
  lessonSlug="project"
  lang="ru"
  objective="Сделай полный senior-уровня дизайн-документ для ОДНОЙ из четырёх фундаментальных систем (распределённое key-value хранилище, сокращатель URL, веб-краулер или распределённая очередь сообщений), следуя той же структуре, что использовали кейсы: требования → оценка → высокоуровневый дизайн → глубокое погружение → узкие места и компромиссы. Каждый числовой порог в документе обоснован оценкой на салфетке, а каждый выбор компонента — явным компромиссом."
  requirements={[
    "Сформулируй функциональные и нефункциональные требования отдельно и определи ОДНО нефункциональное требование, что диктует остальной дизайн (например «всегда доступно для записи при разделении» для KV-хранилища, «~100:1 чтения/записи» для сокращателя, «никогда не перегружать хост» для краулера, «1 ГБ/с надёжно» для очереди).",
    "Сделай оценку на салфетке: выведи QPS (или сообщений/с), хранение за окно удержания и одну цифру памяти/пропускной способности, каждую округлённую до одной значащей цифры, и скажи, какой ресурс ломается первым.",
    "Нарисуй диаграмму высокоуровневой архитектуры (блоки и стрелки), показывающую путь запроса/данных от начала до конца, и напиши абзац-проводку одного запроса сквозь неё.",
    "Глубоко погрузись в ДВА сложных компонента твоей системы — например кворум + версионирование (KV), генерация кодов + путь редиректа/кеш (сокращатель), frontier + слои дедупа (краулер), партиции/ISR + семантика доставки (очередь) — объясняя механизм и отвергнутую альтернативу.",
    "Напиши раздел узких мест и компромиссов, называющий хотя бы четыре режима отказа или жёстких лимита (горячие ключи/партиции, проблема перехеширования, cache stampede, unclean leader election, дубли at-least-once, ловушки и т.д.) и как ты митигируешь каждый.",
    "Для каждого решения дизайна напиши явный компромисс одним предложением («302 над 301, потому что клики — продукт, принимая, что обслуживаем каждый редирект»).",
    "Включи абзац «что бы я построил первым»: минимальную версию, отвечающую диктующему требованию, и первый порог (из твоей оценки), на котором добавил бы следующий кусок.",
  ]}
  acceptance={[
    "Дизайн-документ со всеми пятью разделами (требования, оценка, HLD с диаграммой, два глубоких погружения, узкие места), читаемый примерно за десять минут другим инженером.",
    "Каждый числовой порог подкреплён показанным расчётом (без голых чисел), и документ явно называет, какой ресурс ломается первым.",
    "Каждый из двух компонентов глубокого погружения называет механизм И отвергнутую альтернативу, с причиной — а не просто описание выбранного дизайна.",
    "Раздел узких мест называет хотя бы четыре реальных режима отказа, специфичных для выбранной системы, каждый с митигацией, а не общие ответы «добавить серверов».",
    "Ревьюер может не согласиться с выбором и увидеть ровно, какое допущение поменять — каждое решение прослеживается до заявленного требования или числа.",
  ]}
  stretch={[
    "Напиши документ для ВТОРОЙ системы и извлеки общий скелет: какие ходы (партиционировать, реплицировать, дедупить, кешировать, упорядочить) повторяются и что меняет форму между двумя.",
    "Добавь раздел роста 10x: пересчитай оценку при 10x нагрузке и определи первый порог, что пересекаешь (новый шард, слой кеша, больше партиций) и что в дизайне должно поменяться.",
    "Добавь явную позицию согласованность/доступность: скажи, где твоя система сидит на CAP/PACELC, и проведи ровно, что сетевое разделение делает с чтениями и записями.",
    "Раскритикуй один из четырёх разобранных кейсов: найди требование, что он недообслужил, или компромисс, что он замял, и предложи изменение с числом, что его обосновывает.",
  ]}
/>

</Explanation>

<KeyTakeaway>Дизайн-документ делает юнит операционным: требования (найди диктующее), оценка, выбирающая форму, архитектура, два глубоких погружения с отвергнутой альтернативой и режимы отказа, названные наперёд.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="01-foundational-project-retrieval"
  lang="ru"
  questions={[
    { q: "Каковы пять разделов дизайн-документа и что даёт каждый?", a: "Требования (функциональные + нефункциональные, и одно нефункциональное, диктующее дизайн), Оценка (QPS, хранение, память/пропускная способность до одной значащей цифры — какой ресурс ломается первым), Высокоуровневый дизайн (путь запроса/данных как диаграмма плюс проводка), Глубокое погружение (два сложных компонента, каждый с механизмом и отвергнутой альтернативой) и Узкие места и компромиссы (режимы отказа и жёсткие лимиты, названные до постройки). Оценка выбирает форму; глубокие погружения доказывают, что ты понимаешь сложные части; узкие места доказывают, что знаешь, где ломается." },
    { q: "Почему обосновывать каждый порог числом и называть отвергнутую альтернативу?", a: "Голое число («нужно 100 партиций») неопровержимо; показанный расчёт (1000 МБ/с ÷ 10 МБ/с на партицию) даёт ревьюеру не согласиться, поменяв допущение, а не твой вывод — это и делает документ ревьюабельным. Назвать отвергнутую альтернативу (счётчик против KGS, векторные часы против LWW, 301 против 302) доказывает, что выбор был решением, а не дефолтом, и фиксирует компромисс, чтобы будущий читатель знал, что меняется при сдвиге требования. Дизайны судят по качеству компромиссов, а не по хитрости блоков." },
    { q: "Что делает «диктующее требование» и почему найти его первым?", a: "У каждой системы одно нефункциональное требование, определяющее почти всё ниже: «всегда доступно для записи при разделении» гонит KV-хранилище к без-мастера + итоговой согласованности; «~100:1 чтения/записи» гонит сокращатель к кеш-и-редирект; «никогда не перегружать хост» гонит двухступенчатый frontier краулера; «1 ГБ/с надёжно» гонит очередь к партиционированному реплицированному логу на последовательном диске. Найти его первым значит, что остальной дизайн следует из одного принципа, а не кучи независимых выборов — и это первое, что ищет сильный ревьюер." },
  ]}
/>

<Recap lang="ru">
Этот проект превращает четыре кейса в повторяемый рабочий процесс, применимый к любому запросу «спроектируй X». Выбери одну систему и напиши **пятиразделный дизайн-документ**: **требования** (и единственное нефункциональное, что диктует остальное — всегда-доступно-для-записи, read-heavy, никогда-не-перегружать-хост или надёжный-шланг); **оценку**, что выводит QPS, хранение и цифру памяти/пропускной способности до одной значащей цифры и называет, какой ресурс ломается первым; **высокоуровневый дизайн** с диаграммой и проводкой запроса; **два глубоких погружения**, каждое с механизмом и отвергнутой альтернативой с причиной; и раздел **узких мест и компромиссов**, называющий хотя бы четыре реальных режима отказа с митигациями. Обоснуй каждый порог показанным расчётом, чтобы ревьюер мог не согласиться, поменяв допущение, и зафиксируй компромисс за каждым выбором. Инженер, написавший один такой от начала до конца, перестаёт обращаться с системным дизайном как с заученной диаграммой и начинает обращаться с ним как с рассуждением с числами — ровно тем, чему учит весь трек.
</Recap>
