---
slug: "quiz-short"
lang: ru
track: "system-design-cases"
unit: "03-media-storage"
order: 6
title: "Медиа и хранилище: разбор по памяти"
summary: "Промпты на свободное воспроизведение по разборам медиа-хранилища. Сначала восстанови каждый дизайн своими словами — раскол байты/метаданные, пайплайн транскодинга, чанкинг и разрешение конфликтов, долговечность через erasure coding и почта на масштабе — затем открой и сверься."
estMin: 14
status: ready
lessonType: topic
level: senior
concepts:
  - control-vs-data-path
  - transcoding
  - chunking-dedup
  - erasure-coding
  - per-user-partitioning
  - incremental-sync
prereqs:
  - system-design-cases/03-media-storage/01-youtube
  - system-design-cases/03-media-storage/02-google-drive
  - system-design-cases/03-media-storage/04-distributed-email
sources:
  - https://netflixtechblog.com/high-quality-video-encoding-at-scale-d159db052746
  - https://docs.aws.amazon.com/AmazonS3/latest/userguide/DataDurability.html
  - https://github.com/donnemartin/system-design-primer
---
import Hook from "~/components/lesson/Hook.astro";
import Crux from "~/components/prose/Crux.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";

<Hook>
Воспроизведение бьёт перечитывание. На каждый промпт восстанови полный дизайн по памяти — поток данных, раскол, отказ, что он избегает — прежде чем открыть образец. Усилие пересборки архитектуры и заставляет её закрепиться.
</Hook>

<Crux>Восстанови спину: раскол байты/метаданные, транскодинг для adaptive bitrate, чанкинг с конфликтами, erasure-coded долговечность, почта/юзер.</Crux>

<Explanation>

Восстанови каждый дизайн, не подглядывая: как раскалываются пути записи и чтения YouTube, что производит транскодинг и почему, как Drive двигает наименьшие байты и разрешает конфликты, как объектное хранилище дёшево достигает одиннадцати девяток и как почта масштабируется партиционированием всего на пользователя.

<RetrievalDrawer
  client:visible
  id="03-media-short-retrieval"
  lang="ru"
  questions={[
    { q: "Восстанови два пути YouTube и правило, что правит обоими.", a: "Путь записи (тяжёлый, async): клиент грузит байты напрямую в объектное хранилище через presigned URL, API пишет малую строку метаданных и кладёт задачу транскода, а флот воркеров транскодирует исходник — разворачивая его в лестницу рендиций (240p…4K), нарезанную на короткие сегменты с HLS/DASH манифестом, делая это параллельным транскодом чанков с последующей сшивкой. Путь чтения (быстрый): зритель тянет манифест и сегменты с CDN рядом, который origin-pull-ит из объектного хранилища лишь при промахе (hit rate близок к 100% для популярного контента). Правило обоих: API остаётся на пути управления, никогда на пути данных — байты не касаются ни сервера приложения, ни базы. Счётчики просмотров eventually consistent (логируются и агрегируются), а стоимость диктует egress, не хранилище." },
    { q: "Почему транскодинг производит лестницу сегментированных рендиций плюс манифест?", a: "Потому что канал зрителя не в твоей власти и меняется посреди видео. Транскодинг производит лестницу рендиций (несколько разрешений/битрейтов, часто несколько кодеков), каждая нарезана на короткие сегменты по 2–10 с, с манифестом (HLS .m3u8 / DASH .mpd), перечисляющим URL сегментов каждой рендиции. Это включает adaptive bitrate streaming: плеер на каждый сегмент выбирает наивысшую рендицию, какую вытянет его текущий измеренный канал, переключаясь бесшовно, ведь все рендиции делят границы сегментов — избегая и буферизации (файл велик для канала), и каши (файл мал для экрана). Делается это параллельным fan-out (разбить → транскодировать чанки по воркерам → сшить), так что долгая загрузка завершается за минуты." },
    { q: "Как Drive двигает наименьшие байты и как разрешает конфликтные правки без потери данных?", a: "Адресация по содержимому: режь каждый файл на чанки с ключом по контент-хешу, так что синк изменения перечанкивает, сравнивает хеши и грузит лишь чанки с новыми хешами (delta-синк — правка абзаца двигает ~4 МБ, не 200 МБ), а дедуп (одинаковые чанки хранятся однажды между версиями/файлами/пользователями) и целостность выпадают бесплатно. Файл раскалывается на метаданные (версионированный список чанков, транзакционная БД) и чанки (неизменяемые байты, блоб-хранилище); грузи чанки первыми, затем коммить версию. Конфликты: каждая версия записывает родителя, так что расходящийся синк (родитель больше не голова) обнаруживается и разрешается хранением конфликтной копии (или автослиянием совместных документов через OT/CRDT) — никогда молчаливой перезаписью last-writer-wins." },
    { q: "Как хранилище как S3 дёшево достигает ~11 девяток долговечности и где живёт согласованность?", a: "Размажь избыточность по независимым доменам отказа и используй erasure coding для крупных холодных данных: разбей на k data + m parity шардов (напр. 10+4), реконструируй из любых k, терпи m потерь — ~1.4× накладных за 4-потери vs 3× репликация за лишь 2 — решает на масштабе эксабайт; всё же реплицируй малые горячие объекты, чей fan-out реконструкции бил бы больно. Интерфейс — плоское пространство ключ→байты (нет реальных директорий; LIST по префиксу — range-скан). Согласованность живёт в сервисе метаданных: его коммит — точка линеаризации, так что объект полностью проиндексирован или невидим, а сильная read-after-write идёт от не-подтверждения PUT, пока метаданные не долговечны. Долговечность — гонка: фоновый ремонт должен перестраивать отказавшие шарды быстрее, чем накапливаются новые отказы. Multipart upload делает огромные объекты надёжными через независимые, параллельные, ретраемые части с атомарным complete." },
    { q: "Как масштабируется почта Gmail и назови два решения по тяжёлым байтам и структурированному состоянию.", a: "Партиционируй всё по пользователю — хранилище сообщений, инвертированный индекс поиска на пользователя и журнал изменений все шардируются по ID пользователя, ведь ящики независимы без кросс-юзер-джойнов на горячем пути. Спам (~90% входящего) отвергается на SMTP-приёме до хранения (дешёвые IP/auth-проверки, затем классификация контента), так что ты никогда не платишь долговечным хранилищем за выброшенную почту. Тяжёлые байты: вложения отделяются и хранятся адресуемо по содержимому и дедуплицированно в объектном хранилище (PDF рассылки 50M юзерам — один ref-counted блоб с 50M указателей, не 50M копий). Структурированное состояние: поиск — инвертированный индекс на пользователя (не скан, sub-100 мс), метки — many-to-many оверлей (не папки, так реорганизация не двигает байты), а клиенты синкаются инкрементально через монотонный журнал изменений ящика (HISTORYID/MODSEQ/JMAP state) — O(изменений), не O(ящик)." },
  ]}
/>

</Explanation>

<KeyTakeaway>Восстанови каждый дизайн по памяти — два пути YouTube, лестницу транскодинга, чанкинг Drive, erasure-coded долговечность, почту на пользователя — и держишь спину: отделяй тяжёлые байты от структурированного состояния.</KeyTakeaway>

<Recap lang="ru">
Если ты смог восстановить каждый дизайн по памяти, держишь спину юнита. **YouTube** раскалывает async путь записи (загрузка прямо в объектное хранилище, транскод в сегментированную лестницу рендиций + манифест ради **adaptive bitrate**) и быстрый путь чтения (зритель→CDN), держа API на пути управления и делая счётчики просмотров eventually consistent. **Drive** двигает наименьшие байты через **адресацию по содержимому** (delta-синк + дедуп) и никогда не перезаписывает — цепочка версий ловит конфликты, разрешаемые конфликтными копиями. **Объектное хранилище** дёшево достигает ~11 девяток с **erasure coding** (10+4 ≈ 1.4× vs 3× репликация) для холодных данных, держит согласованность в сервисе метаданных и использует multipart upload для огромных объектов. **Почта** партиционирует всё по пользователю, отвергает спам на приёме, дедуплицирует вложения в объектном хранилище, обслуживает поиск из инвертированного индекса на пользователя, моделирует метки как many-to-many оверлей и синкает инкрементально через журнал изменений. Единая нить: отделяй тяжёлые байты (объектное хранилище, дедуплицировано, отдача с CDN) от структурированного состояния (метаданные, индексы, версии, журналы изменений) и трать бюджет согласованности лишь там, где человек заметит.
</Recap>
