---
slug: "quiz-code"
lang: ru
track: "system-design-cases"
unit: "03-media-storage"
order: 7
title: "Медиа и хранилище: чтение кода и конфигов"
summary: "Читай реальные обработчики загрузки, конфиги хранилища, логику синка и счётчики, затем выбирай сеньорскую починку: загрузку прямо в объектное хранилище, расчёт накладных erasure coding, сравнение чанков delta-синка и eventually-consistent счётчик просмотров."
estMin: 14
status: ready
lessonType: topic
level: senior
concepts:
  - control-vs-data-path
  - erasure-coding
  - delta-sync
  - eventual-consistency
  - chunking-dedup
  - object-storage
prereqs:
  - system-design-cases/03-media-storage/01-youtube
  - system-design-cases/03-media-storage/02-google-drive
  - system-design-cases/03-media-storage/03-object-storage
sources:
  - https://docs.aws.amazon.com/AmazonS3/latest/userguide/DataDurability.html
  - https://dropbox.tech/infrastructure/streaming-file-synchronization
  - https://github.com/donnemartin/system-design-primer
---
import Hook from "~/components/lesson/Hook.astro";
import Crux from "~/components/prose/Crux.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Баги медиа прячутся в обработчике и конфиге: запрос, что буферизует гигабайты, фактор репликации, сравнение чанков, синхронный счётчик. Читай каждый сниппет, найди изъян дизайна и выбери изменение, что закоммитит сеньор.
</Hook>

<Crux>Читай обработчик/конфиг, замечай изъян пути данных или согласованности и выбирай починку, что уважает структуру: байты вне пути приложения.</Crux>

<Explanation>

Практикуй цикл дизайн-ревью: находи, где байты касаются пути приложения, где избыточность выбрана неверно, где синк двигает слишком много и где счётчик опасно синхронен.

## Сниппет 1 — обработчик загрузки

```python
@app.post("/upload")
def upload(req):
    data = req.body.read()          # всё видео, в памяти
    db.execute("INSERT INTO videos (id, blob) VALUES (?, ?)", req.id, data)
    return {"ok": True}
```

<Quiz
  id="03-media-code-1"
  lessonSlug="quiz-code"
  lang="ru"
  question="Что не так с этим обработчиком загрузки и в чём починка?"
  choices={[
    { label: "Он буферизует всё (многогигабайтное) видео в памяти приложения и пишет как блоб в базу — байты на пути данных и в базе. Починка: верни presigned URL, чтобы клиент грузил напрямую в объектное хранилище; храни лишь строку метаданных со ссылкой на ключ объекта.", correct: true },
    { label: "Не хватает валидации входа; добавь проверку схемы и всё нормально.", misconception: "Валидация не чинит ядро изъяна: многогигабайтное видео буферизуется в памяти и хранится блобом в базе. Байты должны идти прямо в объектное хранилище через presigned URL; база держит лишь малую ссылку-метаданные." },
    { label: "Надо использовать async-фреймворк, чтобы чтение не блокировало.", misconception: "Async I/O не меняет того, что гигабайты проходят через приложение и в базу. Структурная починка — загрузка прямо в объектное хранилище (presigned URL) с DB-строкой лишь метаданных — держи байты вне пути приложения целиком." },
    { label: "Нужна транзакция вокруг INSERT.", misconception: "Транзакция не решает хранение многогигабайтного блоба в базе или его буферизацию в памяти. Двинь байты в объектное хранилище через presigned URL и храни лишь ключ в базе." },
  ]}
/>

## Сниппет 2 — конфиг хранилища

```yaml
# конфиг долговечности объектного хранилища
redundancy: replication
replication_factor: 3      # 3 полные копии каждого объекта
# нагрузка: эксабайты холодного архивного видео, редко читается
```

<Quiz
  id="03-media-code-2"
  lessonSlug="quiz-code"
  lang="ru"
  question="Для эксабайт холодного, редко читаемого архивного видео — сколько стоит этот конфиг и какая схема лучше?"
  choices={[
    { label: "3× репликация — +200% накладных по хранению — разорительно на масштабе эксабайт. Переведи холодные данные на erasure coding (напр. 10+4): ~1.4× накладных, терпит 4 одновременные потери, всё ещё бьёт ~11 девяток. Стоимость реконструкции нормальна, ведь данные редко читаются.", correct: true },
    { label: "Это оптимально — 3 копии стандарт для долговечности, оставь.", misconception: "Для холодных данных 3× (+200% накладных) расточительно. Erasure coding (10+4) даёт ~40% накладных И лучшую отказоустойчивость (4 потери vs 2). Стоимость репликации — проблема на масштабе эксабайт; переведи холодные данные на erasure coding." },
    { label: "Снизь replication_factor до 2 ради экономии.", misconception: "RF=2 терпит лишь 1 потерю и всё ещё стоит +100% накладных — хуже долговечность за всё ещё высокую цену. Erasure coding (10+4) — верный ответ: ~40% накладных и 4-потери, идеален для редко читаемых холодных данных." },
    { label: "Добавь чексуммы и держи одну копию.", misconception: "Одна копия не переживёт отказ диска (чексуммы лишь обнаруживают порчу). Нужна избыточность по доменам отказа; для холодных данных это erasure coding (10+4) при ~1.4× накладных, не одна копия." },
  ]}
/>

## Сниппет 3 — дифф синка

```python
def sync_changes(local_file, remote_version):
    local_chunks  = [sha256(c) for c in chunk(local_file)]
    remote_chunks = remote_version.chunk_hashes
    to_upload = [h for h in local_chunks if h not in set(remote_chunks)]
    upload(to_upload)            # затем коммить новую версию со списком local_chunks
```

<Quiz
  id="03-media-code-3"
  lessonSlug="quiz-code"
  lang="ru"
  question="Пользователь дописывает абзац в файл на 200 МБ (~50 чанков). Примерно что это грузит и что демонстрирует?"
  choices={[
    { label: "Лишь 1–2 чанка, чей контент-хеш нов (~4–8 МБ), не весь файл — delta-синк. Одинаковые чанки между версиями/файлами пропускаются (их хеш уже существует), что и есть то, как кросс-версионный дедуп выпадает из того же сравнения.", correct: true },
    { label: "Все ~50 чанков, ведь дописывание сдвигает файл и меняет каждый хеш.", misconception: "Дописывание в КОНЕЦ меняет лишь последний чанк(и); ранние чанки сохраняют хеши и пропускаются. (Вставка в НАЧАЛО сдвинула бы фиксированные границы — потому существует content-defined chunking.) Дописывание грузит ~1–2 чанка, демонстрируя delta-синк." },
    { label: "Ничего, ведь файл уже существует удалённо.", misconception: "Файл изменился, так что его новый чанк(и) имеет новые хеши, которых нет в remote_chunks, и должен грузиться. Суть в том, что грузятся ЛИШЬ эти чанки с новыми хешами (~4–8 МБ), не все 200 МБ — delta-синк." },
    { label: "Все 200 МБ, ведь надо перезагрузить, чтобы закоммитить новую версию.", misconception: "Коммит новой версии лишь записывает список хешей чанков; загружаемые байты — это лишь чанки с новыми хешами (~4–8 МБ). Неизменные чанки уже существуют удалённо и пропускаются — это и есть выигрыш delta-синка." },
  ]}
/>

## Сниппет 4 — счётчик просмотров

```python
def record_view(video_id):
    db.execute(
      "UPDATE videos SET views = views + 1 WHERE id = ?", video_id
    )                            # один синхронный UPDATE на просмотр
# вирусное видео тянет ~200 000 просмотров/секунду
```

<Quiz
  id="03-media-code-4"
  lessonSlug="quiz-code"
  lang="ru"
  question="При ~200 000 просмотров/секунду на вирусном видео — что делает этот счётчик и каков верный дизайн?"
  choices={[
    { label: "Каждый просмотр борется за одну горячую строку, сериализуясь на её блокировке — пропускная записи рушится, а воспроизведение встаёт за ожиданием блокировки. Сделай eventually consistent: логируй события в стрим, агрегируй асинхронно, обновляй счёт периодически.", correct: true },
    { label: "Нормально, если у таблицы videos есть индекс по id.", misconception: "Индекс ускоряет поиски, но не помогает одной горячей строке — каждый UPDATE сериализуется на блокировке той строки независимо. 200K/сек в одну строку — катастрофа борьбы; починка — async агрегация, не индекс." },
    { label: "Оберни в транзакцию с изоляцией SERIALIZABLE ради корректности.", misconception: "Сильнее изоляция делает борьбу хуже, не лучше — добавляет больше блокировок на уже горячую строку. Счёт — ровно там, где eventual consistency безопасна: логируй события и агрегируй асинхронно." },
    { label: "Добавь read-реплику, чтобы показ счёта не блокировал записи.", misconception: "Проблема — борьба за запись в одну строку, не нагрузка чтения; реплика обслуживает чтения, но шторм записи в горячую строку остаётся. Развяжи стримом событий и периодической агрегацией." },
  ]}
/>

</Explanation>

<KeyTakeaway>Решения медиа видны в коде: байты покидают путь приложения (presigned URL, не блоб в базе), избыточность совпадает с профилем (кодируй холодное), синк двигает лишь чанки с новыми хешами, счётчик eventually consistent.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="03-media-code-retrieval"
  lang="ru"
  questions={[
    { q: "Как заметить баг байтов-на-пути-данных в обработчике загрузки и в чём починка?", a: "Ищи обработчик, читающий всё тело запроса в память и/или пишущий его в базу (колонка BLOB). Оба кладут многогигабайтные байты на путь данных приложения и в хранилище, что не должно их держать. Починка — держать API на пути управления: дай клиенту presigned URL, чтобы он грузил байты напрямую в объектное хранилище (multipart для огромных объектов), и храни лишь малую строку метаданных со ссылкой на ключ объекта. База и память приложения никогда не касаются байт медиа." },
    { q: "Дан конфиг 3× репликации для холодных данных и синхронный счётчик на просмотр — что меняешь и почему?", a: "Репликация 3× — +200% накладных по хранению — разорительно для эксабайт холодных, редко читаемых данных; переведи на erasure coding (напр. 10+4) ради ~1.4× накладных, 4-потери и ~11 девяток, ведь высокая стоимость реконструкции нормальна, когда чтения редки (но держи репликацию для малых горячих объектов). Синхронный UPDATE на просмотр создаёт одну горячую строку, сериализующуюся на блокировке — при 200K просмотров/сек пропускная записи рушится; сделай eventually consistent, логируя события просмотра в стрим и агрегируя асинхронно, обновляя счёт периодически. Согласуй избыточность с профилем данных и никогда синхронно не инкрементируй вирусный счётчик." },
  ]}
/>

<Recap lang="ru">
Каждое решение медиа в этом юните проявляется в коде, что можно прочесть. **Обработчик загрузки**, читающий всё тело и пишущий блоб в базу, — баг байтов-на-пути-данных — чини его **presigned URL**, чтобы клиент грузил напрямую в объектное хранилище, а база держала лишь ссылку-метаданные. **Конфиг 3× репликации** для холодных архивных данных тратит +200% накладных — переведи холодные данные на **erasure coding** (10+4 ≈ 1.4×, 4-потери), держа репликацию для малых горячих объектов. **Дифф синка**, сравнивающий хеши чанков, грузит лишь чанки с новыми хешами (~4–8 МБ для дописывания абзаца), суть **delta-синка** и где кросс-версионный **дедуп** выпадает бесплатно. А **синхронный UPDATE на просмотр** — горячая строка, что рушится под вирусной нагрузкой — сделай счёт **eventually consistent**, логируя события и агрегируя асинхронно. Сеньорская привычка одна во всех четырёх: найди, где байты или согласованность обрабатываются неверно, и выбери починку, что уважает структуру, а не маскирует её.
</Recap>
