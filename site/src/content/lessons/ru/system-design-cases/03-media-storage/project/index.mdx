---
slug: "project"
lang: ru
track: "system-design-cases"
unit: "03-media-storage"
order: 8
title: "Медиа и хранилище: построй сервис загрузки-синка с чанками и дедупом"
summary: "Практический проект: построй малый файловый сервис, что грузит байты прямо в объектное хранилище, режет файлы на адресуемые чанки ради delta-синка и дедупа, хранит метаданные отдельно и разрешает конфликты через цепочку версий — спина каждого дизайна юнита."
estMin: 300
status: ready
lessonType: topic
level: senior
concepts:
  - control-vs-data-path
  - chunking-dedup
  - delta-sync
  - conflict-resolution
  - object-storage
  - metadata-vs-blob
prereqs:
  - system-design-cases/03-media-storage/01-youtube
  - system-design-cases/03-media-storage/02-google-drive
  - system-design-cases/03-media-storage/03-object-storage
sources:
  - https://dropbox.tech/infrastructure/streaming-file-synchronization
  - https://docs.aws.amazon.com/AmazonS3/latest/userguide/DataDurability.html
  - https://github.com/donnemartin/system-design-primer
---
import Hook from "~/components/lesson/Hook.astro";
import Crux from "~/components/prose/Crux.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import ProjectBrief from "~/components/pedagogy/ProjectBrief.astro";

<Hook>
Прочитать, что «байты идут в объектное хранилище, а ты синкаешь дельты через адресуемые чанки», — не то же, что построить сервис, который это реально делает. Построй малый, но настоящий сервис загрузки-синка: байты никогда не касаются пути данных твоего сервера приложения, файлы хранятся дедуплицированными чанками, правка синкает лишь изменённые чанки, а две конфликтные правки обе выживают. Закончив, ты реализуешь в миниатюре спину, общую для YouTube, Drive и S3.
</Hook>

<Crux>Построй сервис синка: байты прямо в хранилище, адресуемые чанки ради delta-синка и дедупа, отдельные метаданные, конфликты цепочкой версий.</Crux>

<Explanation>

Этот проект делает юнит операционным. Ты отделишь путь управления от пути данных, реализуешь адресацию по содержимому и измеришь выигрыши delta-синка и дедупа, сохранишь метаданные отдельно от байт и докажешь, что цепочка версий предотвращает отказ молчаливой перезаписи, о котором предупреждает урок Drive.

<ProjectBrief
  id="03-media-storage-project"
  lessonSlug="project"
  lang="ru"
  objective="Построй малый сервис загрузки-синка файлов, что хранит файлы адресуемыми по содержимому чанками в объектном хранилище (реальный S3/MinIO или локальная блоб-директория), держит метаданные в отдельной базе, синкает правки, передавая лишь изменённые чанки, дедуплицирует одинаковые чанки и разрешает конфликтные правки через цепочку версий — затем измерь выигрыши канала и хранилища, что даёт твой дизайн."
  requirements={[
    "Подними объектное хранилище (MinIO, S3 или локальная директория как блоб-хранилище) и отдельную БД метаданных (SQLite/Postgres). Чётко определи раскол: чанки (неизменяемые байты, ключ — контент-хеш) живут в объектном хранилище; метаданные файла (имя, версия, упорядоченный список хешей чанков) живут в БД.",
    "Реализуй загрузку так, чтобы байты шли прямо в блоб-хранилище, не через буфер в памяти или колонку БД: режь файл на фиксированные чанки (например, 1–4 МБ), хешируй каждый (SHA-256), грузи лишь чанки, чей хеш ещё не сохранён, затем коммить новую версию файла со списком хешей чанков — чанки первыми, метаданные вторыми.",
    "Реализуй скачивание/чтение, ища список чанков версии в метаданных и стримя чанки из блоб-хранилища по порядку; поставь малый кеш (в памяти или Redis) перед поисками метаданных (не байт чанков).",
    "Реализуй delta-синк: для изменённого файла перечанкуй его, сдиффай хеши его чанков против прошлой версии, грузи лишь чанки с новыми хешами и коммить новую версию — залогируй, сколько байт ты загрузил vs размер файла.",
    "Реализуй дедупликацию и докажи её: загрузи тот же файл (или общий чанк) под двумя разными именами/пользователями и покажи, что хранится лишь одна физическая копия; держи счётчик ссылок, чтобы чанк удалялся лишь когда уходит последняя ссылающаяся версия.",
    "Реализуй разрешение конфликтов: пусть каждая версия записывает родителя. Когда приходит синк, чей родитель больше не текущая голова (симулируй двух клиентов, правящих от той же базы), обнаружь расхождение и храни ОБЕ как конфликтные копии вместо перезаписи — никогда не теряй правку.",
    "Добавь крошечную multipart-стиль загрузку для одного крупного файла: разбей на части, грузи части независимо с одной намеренно отваливающейся и ретраемой отдельно, затем «complete» для сборки — покажи, что объект становится виден лишь после завершения.",
    "Измерь и отчитайся: для представительной правки (например, смена одного чанка файла на 100 МБ) отчитайся о байтах, передвинутых delta-синком, vs наивная полная перезагрузка, и о хранилище, сэкономленном дедупом по дублирующим загрузкам — до одной значащей цифры.",
  ]}
  acceptance={[
    "Рабочий сервис, где загрузка хранит чанки в объектном хранилище и строку метаданных в БД, БЕЗ единого байта файла, хранимого в колонке БД или буферизованного целиком в памяти приложения — продемонстрируй это файлом в несколько сотен МБ.",
    "Демонстрация delta-синка: правка одного чанка крупного файла грузит лишь объём ~одного чанка байт, с залогированным счётом байт до/после, показывающим экономию на порядок vs полная перезагрузка.",
    "Демонстрация дедупа: тот же контент, сохранённый дважды, занимает один физический чанк (показано через счёт объектов блоб-хранилища и счётчик ссылок), а удаление одной ссылающейся версии не удаляет всё ещё ссылаемый чанк.",
    "Демонстрация конфликта: две правки от той же родительской версии обе выживают как конфликтные копии, с показанной цепочкой версий (указатели родителей) — перезапись last-writer-wins НЕ должна произойти.",
    "Краткий отчёт: раскол метаданные-vs-блоб, что ты выбрал, и почему, измеренные выигрыши delta-синка и дедупа до одной значащей цифры, и абзац о том, что изменилось бы для достижения S3-подобной долговечности (erasure coding по доменам отказа) на реальном масштабе.",
  ]}
  stretch={[
    "Замени фиксированный чанкинг на content-defined chunking (граница по rolling-hash) и продемонстрируй, что вставка байт в НАЧАЛО файла теперь ресинкает лишь затронутый чанк вместо каждого последующего — квантифицируй разницу.",
    "Добавь push-стиль уведомление об изменении: вместо опроса клиентами пусть сервер уведомляет подключённого клиента (WebSocket/long-poll), когда файл меняется, и клиент затем тянет лишь дифф метаданных и недостающие чанки.",
    "Добавь eventually-consistent счётчик (например, счёт скачиваний), реализованный логированием событий и асинхронной агрегацией, и контрастируй его пропускную с синхронным UPDATE на событие под нагрузочным тестом.",
    "Симулируй erasure coding: разбей чанк на k data + m parity фрагментов, храни их в отдельных «узлах» (директориях), удали до m фрагментов и реконструируй чанк из выживших — измеряя накладные по хранению vs 3-копийная репликация того же чанка.",
  ]}
/>

</Explanation>

<KeyTakeaway>Построение сервиса делает юнит реальным: держи байты вне пути приложения, храни адресуемые чанки отдельно от метаданных, синкай лишь изменённые чанки, дедуплицируй одинаковые, разрешай конфликты цепочкой версий.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="03-media-storage-project-retrieval"
  lang="ru"
  questions={[
    { q: "Какой ядровый структурный раскол проект заставляет тебя построить и почему?", a: "Раскол метаданные-vs-блоб: неизменяемые, адресуемые по содержимому чанки (ключ — контент-хеш) живут в объектном/блоб-хранилище, а метаданные файла (имя, версия, упорядоченный список хешей чанков, указатель родителя) живут в отдельной транзакционной базе. У них противоположные нужды — байты велики, неизменяемы, write-once-read-many (дешёвое долговечное объёмное хранилище), а метаданные малы, запрашиваются и транзакционно обновляются (индексы + ACID). Построение обоих доказывает, почему каждый дизайн юнита держит байты вне пути данных приложения: грузи чанки напрямую в блоб-хранилище, затем коммить версию метаданных, ссылающуюся на них (чанки первыми, метаданные вторыми)." },
    { q: "Как продемонстрировать delta-синк, дедуп и безопасность конфликтов и что масштабировало бы их до S3-подобной долговечности?", a: "Delta-синк: правь один чанк крупного файла, сдиффай хеши чанков против прошлой версии, грузи лишь чанк с новым хешем и логируй байты-передвинуто vs размер файла (выигрыш на порядок). Дедуп: храни одинаковый контент дважды и покажи один физический чанк плюс счётчик ссылок, так что удаление одной версии сохраняет всё ещё ссылаемый чанк. Безопасность конфликтов: каждая версия записывает родителя, так что синк, чей родитель не текущая голова, обнаруживается как ветвление, и ОБЕ правки выживают как конфликтные копии — никогда last-writer-wins. Чтобы достичь S3-подобной долговечности на реальном масштабе, ты заменил бы одно-копийное хранение чанков на erasure coding по независимым доменам отказа (k+m шардов, реконструкция из любых k) для холодных данных и репликацию для горячих, с фоновым ремонтом, обгоняющим отказы." },
  ]}
/>

<Recap lang="ru">
Этот проект превращает юнит медиа-хранилища в то, что ты построил. Ты поднимаешь **объектное хранилище** для байт и **отдельную БД метаданных**, затем реализуешь спину, общую для YouTube, Drive и S3: **грузи байты напрямую в блоб-хранилище** (никогда через буфер в памяти приложения или колонку БД), режь файлы на **адресуемые по содержимому чанки** и коммить **версию** метаданных со списком хешей чанков — чанки первыми, метаданные вторыми. Из этого одного механизма ты получаешь **delta-синк** (правь чанк, грузи лишь этот чанк и измерь экономию канала на порядок), **дедупликацию** (одинаковые чанки хранятся однажды со счётчиком ссылок) и **целостность** бесплатно. Ты делаешь систему безопасной **цепочкой версий** — каждая версия записывает родителя, так что две правки от той же базы обе выживают как **конфликтные копии**, а не молчаливая перезапись — и добавляешь **multipart-стиль** загрузку, чтобы передача крупного файла была надёжной. Поставка — не просто код, а **измерение**: выигрыши delta-синка и дедупа до одной значащей цифры и абзац о том, что потребовало бы достижение реальной **S3-подобной долговечности** (erasure coding по доменам отказа). Построй это однажды — и дизайны юнита перестают быть диаграммами и становятся мышечной памятью.
</Recap>
