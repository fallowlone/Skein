---
slug: "quiz-choice"
lang: ru
track: "system-design-cases"
unit: "05-data-money"
order: 7
title: "Данные и деньги: обзор с выбором ответа"
summary: "Обзор с выбором ответа по кейсам данных-и-денег: кардинальность, exactly-once против опоздания, идемпотентность и двойная запись, потерянное обновление, детерминизм биржи и гонка двойного бронирования."
estMin: 13
status: ready
lessonType: topic
level: senior
concepts:
  - cardinality
  - exactly-once
  - idempotency
  - lost-update
  - determinism
  - double-booking
prereqs:
  - system-design-cases/05-data-money/01-metrics-monitoring
  - system-design-cases/05-data-money/03-payment-system
  - system-design-cases/05-data-money/04-digital-wallet
  - system-design-cases/05-data-money/05-stock-exchange
sources:
  - https://prometheus.io/docs/practices/instrumentation/
  - https://docs.stripe.com/api/idempotent_requests
  - https://martinfowler.com/articles/lmax.html
---
import Hook from "~/components/lesson/Hook.astro";
import Crux from "~/components/prose/Crux.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Шесть вопросов, что режут поперёк юнита. Каждый — решение, что ты принимаешь в дизайн-ревью, где выход — деньги или критичные к корректности данные — не определение для зубрёжки, а рассуждение, что предотвращает двойное списание, OOM от кардинальности или двойное бронирование.
</Hook>

<Crux>Шесть решений по кейсам: кардинальность, exactly-once, идемпотентность, потерянное обновление, детерминизм биржи и двойное бронирование.</Crux>

<Explanation>

Подтверди, что умеешь засечь бомбу кардинальности, отделить exactly-once от поздних событий, применить ключ идемпотентности, побить потерянное обновление, обосновать однопоточное ядро биржи и предотвратить двойное бронирование.

<Quiz
  id="05-data-money-choice-1"
  lessonSlug="quiz-choice"
  lang="ru"
  question="Команда метрик добавляет лейбл `user_id` (миллионы значений) к высокотрафиковой метрике. Что ломается и почему?"
  choices={[
    { label: "Кардинальность активных рядов взрывается: число рядов — произведение кардинальностей лейблов, так что один неограниченный лейбл умножает ряды в миллиарды и исчерпывает память/индекс TSDB — темп сэмплов почти не меняется. Значения высокой кардинальности — в логи/трейсы, не в лейблы.", correct: true },
    { label: "Пропускная способность приёма удваивается; добавь более быстрый путь записи.", misconception: "Темп СЭМПЛОВ почти не двигается; взрывается число АКТИВНЫХ РЯДОВ (произведение кардинальностей лейблов), что задаёт память/индекс. Быстрый путь записи не спасёт от взрыва кардинальности." },
    { label: "Ничего — лейблы это просто доп. колонки ценой пары байт каждая.", misconception: "Лейбл — не колонка; он форкает целый новый временной ряд, что БД отслеживает с оверхедом индекса + памяти. Уникальный-на-пользователя лейбл — миллионы рядов, классический OOM, не «пара байт»." },
    { label: "Диск заполняется, но память в порядке.", misconception: "Связывающее ограничение — ПАМЯТЬ на активные ряды (индекс + открытые чанки), не диск. Неограниченная кардинальность сперва выносит in-memory индекс; это и убивает узел." },
  ]}
/>

<Quiz
  id="05-data-money-choice-2"
  lessonSlug="quiz-choice"
  lang="ru"
  question="В агрегации кликов клик приходит на 30 минут позже; его event-time окно закрылось давно. Exactly-once включён. Посчитан ли он, и как всё-таки выставить в счёт?"
  choices={[
    { label: "Exactly-once не помогает — он предотвращает двойное ПРИМЕНЕНИЕ событий, что движок видит, не поздние события, что окно исключило. Потоковый слой отбрасывает его; пакетный слой сверки перечитывает все сырые события без давления времени и включает его в авторитетный итог.", correct: true },
    { label: "Exactly-once гарантирует подсчёт в его окне.", misconception: "Exactly-once о том, чтобы не применить дважды; опоздание — забота окон/watermark. Уже закрытое окно его не примет — пакетный слой его восстанавливает, не механизм exactly-once." },
    { label: "Выстави отставание watermark в 30+ минут, чтобы ничто не опаздывало.", misconception: "Это облагает КАЖДЫЙ результат 30 минутами устаревания ради спасения редкого опоздавшего. Размерь watermark под обычный случай и дай пакетному слою (без давления времени) поймать хвост." },
    { label: "Он падает в текущее окно, посчитан в неверной минуте — для биллинга норм.", misconception: "Агрегация по EVENT time именно чтобы он не падал в неверную минуту. Его реальное окно закрылось; поток его отбрасывает, а пакетная сверка восстанавливает." },
  ]}
/>

<Quiz
  id="05-data-money-choice-3"
  lessonSlug="quiz-choice"
  lang="ru"
  question="Клиент списывает с карты, PSP успешен, но ответ теряется; клиент повторяет. Что предотвращает двойное списание, и откуда должен прийти ключ?"
  choices={[
    { label: "Клиентом сгенерированный ключ идемпотентности, отправленный с обеими попытками: атомарный check-and-store сервера находит ключ на ретрае и возвращает исходный результат вместо повторного списания. Клиент должен отчеканить его до первой попытки, ведь лишь заранее существующий ключ переживает потерянный ответ.", correct: true },
    { label: "Сервером сгенерированный ID транзакции, переиспользуемый на ретрае.", misconception: "Если ответ потерян, клиент не получил серверный ID, так что ретрай не может его переиспользовать — он выглядит свежим платежом. Ключ должен прийти от клиента до первой попытки." },
    { label: "Обернуть списание в транзакцию БД, чтобы оно было атомарным.", misconception: "Локальная транзакция не охватит внешний вызов PSP и не помогает, когда ОТВЕТ потерян после того, как PSP уже списал. Нужен клиентский ключ идемпотентности, чтобы распознать ретрай." },
    { label: "Не повторять при таймауте.", misconception: "Неповтор рискует реально пропущенным списанием (таймаут двусмыслен). Верный ход — повторять БЕЗОПАСНО с ключом идемпотентности, чтобы дубликаты распознавались, а пропущенные списания завершались." },
  ]}
/>

<Quiz
  id="05-data-money-choice-4"
  lessonSlug="quiz-choice"
  lang="ru"
  question="Два конкурентных дебета ($70, $60) оба читают баланс кошелька $100, оба проходят проверку, оба пишут обратно, и кошелёк уходит в минус. Что это и чистейшая одношардовая починка?"
  choices={[
    { label: "Проблема потерянного обновления: зазор чтение-запись даёт обоим считать от устаревшего $100. Чинится атомарной условной записью — UPDATE … SET balance = balance - 70 WHERE balance >= 70 — делая check-and-decrement одной неделимой операцией; вторая видит 30 и затрагивает ноль строк.", correct: true },
    { label: "Дедлок; добавь цикл ретраев.", misconception: "Ничто не в дедлоке — обе успешны, в этом баг. Это потерянное обновление от гонки read-modify-write. Цикл ретраев не закрывает зазор чтения-записи; атомарная условная запись (или блокировка) — да." },
    { label: "Лаг репликации; читай с primary.", misconception: "Это происходит на одном primary — две конкурентные транзакции читают ту же строку до того, как любая запишет. Починка — контроль конкуренции (атомарная условная запись / SELECT FOR UPDATE), не откуда читаешь." },
    { label: "Eventual consistency; используй более сильное хранилище.", misconception: "Даже строго консистентное хранилище теряет обновление, если приложение читает-решает-пишет с зазором. Сделай решение-и-запись атомарными в БД или держи блокировку строки — а не просто смени модель консистентности." },
  ]}
/>

<Quiz
  id="05-data-money-choice-5"
  lessonSlug="quiz-choice"
  lang="ru"
  question="Ядро матчинга биржи однопоточно над секвенсированным потоком. Инженер предлагает несколько потоков матчинга «чтобы быстрее». Почему это неверно?"
  choices={[
    { label: "Честность требует одного тотального порядка; конкурентный матчинг против той же книги создаёт гонку, где «кто первым» зависит от планирования, реплики расходятся, а реплей не воспроизводит сделки. Один поток здесь ещё и быстрее (нет блокировок, книга в кэше). Масштабируй партиционированием по инструментам, не одной книгой.", correct: true },
    { label: "Норм — больше потоков всегда помогает пропускной способности.", misconception: "Для книги матчинга конкурентность разрушает единый тотальный порядок, что требуют честность и аудит, и добавляет конкуренцию за блокировки/кэш. Ядро намеренно однопоточно; параллелишь всё ВОКРУГ него." },
    { label: "Потоки норм, если добавить блокировки вокруг книги.", misconception: "Блокировки возвращают зависящий от планирования порядок (недетерминизм) и конкуренцию, сводя смысл на нет. Один поток над секвенсированным потоком не нуждается в блокировках и остаётся детерминированным." },
    { label: "Лишь лог долговечности должен быть однопоточным.", misconception: "Именно ЯДРО матчинга должно быть однопоточным ради детерминированного тотального порядка; лог добавляется в том же порядке. Параллельный матчинг ломает детерминизм независимо от лога." },
  ]}
/>

<Quiz
  id="05-data-money-choice-6"
  lessonSlug="quiz-choice"
  lang="ru"
  question="Два гостя бронируют последний номер на ту же ночь в пределах секунды; оба видели «1 доступен» и оба получили подтверждения. Что это и верная починка для многоночного проживания?"
  choices={[
    { label: "Проблема двойного бронирования — гонка потерянного обновления над инвентарём. Чинится тем, чтобы сделать check-and-reserve одной атомарной транзакцией всё-или-ничего, что условно декрементит счётчик на КАЖДУЮ ночь в диапазоне; вторая бронь матчит ноль строк и отклоняется.", correct: true },
    { label: "Баг кэша; отключи кэш доступности.", misconception: "Даже идеально свежее чтение даёт двум запросам увидеть «1» до того, как любой запишет. Гарантия принадлежит атомарному check-and-reserve; поиск может быть чуть устаревшим и рекомендательным." },
    { label: "Декременть каждую ночь независимыми обновлениями, без транзакции.", misconception: "Поночёвые обновления должны быть ВСЁ-ИЛИ-НИЧЕГО в одной транзакции, иначе 3-ночное проживание бронирует 2 ночи, затем валится на 3-й. И без атомарности на ночь гонка остаётся." },
    { label: "Сделай поиск доступности строго консистентным.", misconception: "Поиск — это ~100× трафика; форсирование его строго консистентным убивает масштабируемость и ВСЁ РАВНО не гарантирует номер (кто-то бронирует между поиском и кликом). Гарантия — атомарный резерв." },
  ]}
/>

</Explanation>

<KeyTakeaway>Корректность данных-и-денег — чеклист именованных угроз: бомба кардинальности, exactly-once, ключ идемпотентности, потерянное обновление, детерминизм биржи и двойное бронирование — у каждой точная починка.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="05-data-money-choice-retrieval"
  lang="ru"
  questions={[
    { q: "Почему exactly-once — не ответ на позднее событие?", a: "Exactly-once предотвращает двойное ПРИМЕНЕНИЕ событий, что движок обрабатывает (через at-least-once + идемпотентное/транзакционное применение); позднее событие — то, что временное окно уже исключило. Опоздание — забота watermark/окон — размеряешь watermark под обычный случай и восстанавливаешь настоящих опоздавших в пакетном слое сверки, что перечитывает все сырые события без давления времени для авторитетного итога." },
    { q: "Что общего между потерянным обновлением и двойным бронированием?", a: "Оба — та же гонка read-modify-write: два запроса читают значение (баланс, «1 доступен») до того, как любой запишет, так что оба продолжают и один перезаписывает другой — деньги созданы или номер продан дважды. Починка идентична по принципу: сделать check-and-act одной атомарной операцией (атомарная условная запись / одна ACID-транзакция / блокировка строки), чтобы не было зазора устаревшее-чтение-затем-запись. Кошелёк делает это на баланс; отель — на ночь через диапазон, всё-или-ничего." },
  ]}
/>

<Recap lang="ru">
Угрозы юнита — именованный чеклист, что прогоняешь в любом дизайн-ревью, где выход — деньги или критичные к корректности данные. **Бомба кардинальности**: число рядов — произведение кардинальностей лейблов, так что неограниченный лейбл OOM'ит TSDB — держи лейблы ограниченными. **Exactly-once против опоздания**: exactly-once останавливает двойное применение, не поздние события, под которые размеряешь watermark, а восстанавливает пакетная сверка. **Ключ идемпотентности**: клиентом отчеканенный, атомарный check-and-store, так что ретрай после потерянного ответа возвращает исходный результат вместо двойного списания. **Потерянное обновление** и **двойное бронирование** — та же гонка read-modify-write — чинятся тем, чтобы сделать check-and-act одной атомарной операцией (на баланс или на ночь всё-или-ничего). И **детерминизм биржи**: единый секвенсированный тотальный порядок через однопоточное ядро, так что реплики и реплей согласны — никогда не параллель одну книгу. У каждой угрозы точная атомарная починка, и распознавание, с какой ты столкнулся, — senior-навык, что строит этот юнит.
</Recap>
