---
slug: "project"
lang: ru
track: "system-design-cases"
unit: "05-data-money"
order: 10
title: "Данные и деньги: собери корректный двигатель денег"
summary: "Практический проект: собери небольшой сервис кошелька/ledger, что переживает ретраи и конкуренцию — двойная запись, клиентские ключи идемпотентности, атомарный перевод против потерянного обновления и проход сверки — доказанный адверсариальными тестами."
estMin: 240
status: ready
lessonType: topic
level: senior
concepts:
  - idempotency-keys
  - double-entry-ledger
  - lost-update
  - concurrency-control
  - reconciliation
  - event-sourcing
prereqs:
  - system-design-cases/05-data-money/03-payment-system
  - system-design-cases/05-data-money/04-digital-wallet
sources:
  - https://docs.stripe.com/api/idempotent_requests
  - https://www.postgresql.org/docs/current/transaction-iso.html
  - https://martinfowler.com/eaaDev/EventSourcing.html
---
import Hook from "~/components/lesson/Hook.astro";
import Crux from "~/components/prose/Crux.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import ProjectBrief from "~/components/pedagogy/ProjectBrief.astro";

<Hook>
Прочитать, что ключи идемпотентности и двойная запись предотвращают двойные списания и потерянные обновления, — не то же, что собрать сервис, что переживает шторм ретраев и тысячу конкурентных переводов, не потеряв ни цента. Собери небольшой двигатель денег, затем докажи его корректность единственным способом, что считается: адверсариальными тестами, что кидают в него дублирующие запросы и конкурентные дебеты и проверяют, что книги всё ещё сходятся.
</Hook>

<Crux>Собери кошелёк/ledger против ретраев и конкуренции: двойная запись, ключи идемпотентности, атомарный перевод, сверка — с атакующими тестами.</Crux>

<Explanation>

Этот проект делает юнит операционным: ты реализуешь два несущих паттерна — идемпотентность и двойную запись — побьёшь потерянное обновление атомарным переводом, а затем атакуешь свою же систему дубликатами и конкуренцией, чтобы доказать инвариант, что книги всегда сходятся.

<ProjectBrief
  id="05-data-money-project"
  lessonSlug="project"
  lang="ru"
  objective="Собери небольшой сервис кошелька/ledger (любой язык + реальная транзакционная БД), что двигает деньги между счетами корректно под ретраями и конкуренцией. Он должен реализовать append-only ledger двойной записи, принимать клиентские ключи идемпотентности, выполнять атомарные переводы, что не могут списать дважды или потерять обновление, и включать проход сверки, что проверяет схождение книг — затем доказать всё это адверсариальными тестами."
  requirements={[
    "Смоделируй append-only ledger двойной записи: перевод пишет две записи (дебет + кредит) в сумме ноль, записи неизменяемы (никогда не обновляются и не удаляются), а баланс счёта выводится суммированием его записей — не хранится как изменяемое поле. Засей пару счетов записями открывающего баланса.",
    "Реализуй операцию transfer(from, to, amount, idempotency_key) в одной транзакции БД: проверь достаточность средств, добавь записи дебета и кредита и закоммить атомарно — обе ноги или ни одной.",
    "Добавь идемпотентность: персисти ключ идемпотентности с результатом перевода под уникальным ограничением, и на повторный ключ верни исходный результат без повторного движения денег. Отклони переиспользованный ключ, чьи параметры отличаются.",
    "Побей потерянное обновление: убедись, что два конкурентных перевода с того же счёта не могут оба пройти за баланс (используй атомарную условную проверку, SELECT … FOR UPDATE или serializable-транзакцию) — и задокументируй, какой механизм выбрал и почему.",
    "Напиши проход сверки: независимо пересчитай каждый баланс счёта суммированием его записей и утверди глобальный инвариант, что сумма дебетов равна сумме кредитов (деньги сохраняются); сделай его запускаемым по требованию и репортящим любой дисбаланс.",
    "Адверсариальный тест 1 — шторм ретраев: запусти ОДИН И ТОТ ЖЕ запрос перевода N раз конкурентно (тот же ключ идемпотентности) и утверди, что создан ровно один набор записей ledger и баланс двинулся один раз.",
    "Адверсариальный тест 2 — конкурентные дебеты: запусти много конкурентных переводов с одного счёта, чей итог превышает его баланс, и утверди, что счёт никогда не уходит в минус и книги всё ещё сходятся (нет потерянного обновления, нет созданных денег).",
    "Напиши короткую заметку дизайна: укажи свой уровень изоляции / выбор блокировки, почему баланс выводится, а не хранится, и один режим отказа, что сверка поймала бы, а один лишь горячий путь — нет.",
  ]}
  acceptance={[
    "Append-only ledger, где балансы вычисляются суммированием неизменяемых записей, со скриптом сидинга и запросом, что печатает баланс любого счёта и его историю записей.",
    "Перевод, что атомарен (обе записи или ни одной) и идемпотентен (повторный ключ возвращает исходный результат без второго движения; конфликтующий ключ отклоняется).",
    "Проходящий тест шторма ретраев: N конкурентных идентичных запросов производят записи ровно на один перевод и единственную смену баланса.",
    "Проходящий тест конкурентных дебетов: много переоверкоммиченных конкурентных переводов оставляют счёт неотрицательным и глобальный инвариант дебеты = кредиты целым.",
    "Команда сверки, что пересчитывает балансы из записей и репортит книги как сбалансированные (и пометила бы внесённый дисбаланс), плюс заметка дизайна, объясняющая выбор изоляции/блокировки и один отказ, что сверка ловит.",
  ]}
  stretch={[
    "Добавь межшардовый перевод как saga: раздели счета на две БД, реализуй дебет→кредит с компенсирующим действием, смоделируй in-flight pending-состояние и покажи, как сверка исцеляет перевод, что ты намеренно прервал между двумя ногами.",
    "Добавь снимок/материализованный баланс и обслуживай чтения из него (снимок + свёртка хвоста свежих записей), затем докажи, что снимок всегда равен полной пересвёртке — оптимизация чтения event-sourcing без потери проверяемости.",
    "Добавь внешний 'PSP'-заглушку, чей ответ можешь дропнуть/задержать, поставь списание за ключом идемпотентности и покажи, что потерянный ответ + ретрай клиента не списывает дважды, пока дневная сверка с отчётом PSP всё же ловит расхождение.",
    "Нагрузь горячий счёт: сделай один счёт целью большинства переводов и измерь, как твой выбор блокировки его сериализует; сравни пессимистичную блокировку против атомарной условной записи против оптимистичной/CAS при высокой конкуренции и отрепорти пропускную способность и частоту ретраев.",
  ]}
/>

</Explanation>

<KeyTakeaway>Сборка двигателя денег делает юнит реальным: ledger двойной записи, ключи идемпотентности, атомарный перевод против потерянного обновления, сверка — докажи тестами ретраев и конкурентных дебетов, что книги сходятся.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="05-data-money-project-retrieval"
  lang="ru"
  questions={[
    { q: "Почему выводить балансы суммированием неизменяемых записей, а не хранить изменяемое поле баланса?", a: "Изменяемый баланс — это то, что ВКЛЮЧАЕТ потерянное обновление (гонка read-modify-write) и стирает историю. Append-only ledger двойной записи делает каждое движение двумя неизменяемыми записями в сумме ноль (дебет = кредит), так что деньги сохраняются по построению, баланс — проверяемая сумма по истории (пересвернуть и сравнить для обнаружения порчи), а полный аудит-трейл цел (коррекция — новая компенсирующая запись, никогда правка). Проход сверки проекта опирается на это: он независимо пересуммирует записи и утверждает, что сумма дебетов равна сумме кредитов — невозможно осмысленно сделать против изменяемого поля." },
    { q: "Как два адверсариальных теста доказывают два ядровых инварианта?", a: "Шторм ретраев (идемпотентность): запусти ОДИН И ТОТ ЖЕ запрос с одним ключом идемпотентности N раз конкурентно; корректное поведение — ровно один набор записей ledger и одна смена баланса, потому что атомарный check-and-store под уникальным ключом впускает первый и возвращает сохранённый результат остальным — доказывая, что ретраи не списывают дважды. Конкурентные дебеты (потерянное обновление): запусти много конкурентных переводов с одного счёта, чей итог превышает баланс; корректное поведение — счёт никогда не уходит в минус и сумма дебетов всё ещё равна сумме кредитов, потому что атомарная условная проверка / блокировка / serializable-транзакция закрывает зазор чтения-записи — доказывая, что конкуренция не создаёт денег и не уводит в минус." },
    { q: "Что проход сверки ловит, чего один горячий путь не может?", a: "Горячий путь обеспечивает корректность на операцию, но двусмысленность и внешние системы оставляют остаток: потерянный ответ PSP (списано, но не записано, или записано как отказ, когда удалось), прерванная межшардовая saga, оставленная in-flight, или тонкий баг, что сдвинул баланс. Сверка — независимый аудит: она пере-выводит каждый баланс из неизменяемых записей и утверждает глобальный инвариант дебеты = кредиты, и (с PSP-заглушкой) сравнивает ledger с отчётом процессора о расчётах — ловя расхождения, что путь на операцию не видит, затем помечая их на компенсирующую запись. Поэтому денежная система может быть доступной, но доказуемо корректной: медленный исчерпывающий проход — финальный арбитр." },
  ]}
/>

<Recap lang="ru">
Этот проект превращает два несущих паттерна юнита в рабочий код. Ты собираешь **append-only ledger двойной записи**, где каждый перевод — две неизменяемые записи в сумме ноль, а баланс — **выводимая сумма**, никогда изменяемое поле — так что деньги сохраняются по построению, а книги проверяемы. **Перевод** — это одна атомарная транзакция (обе ноги или ни одной), охраняемая **клиентским ключом идемпотентности** (атомарный check-and-store под уникальным ограничением), а **потерянное обновление** побеждается атомарной условной проверкой, блокировкой строки или serializable-транзакцией. Затем ты атакуешь свою же систему: **шторм ретраев** (N идентичных конкурентных запросов должны двинуть деньги ровно раз) доказывает идемпотентность, а **конкурентные переоверкоммиченные дебеты** (счёт никогда не должен уйти в минус, дебеты должны всё ещё равняться кредитам) доказывают, что потерянное обновление закрыто. Наконец **проход сверки** независимо пересуммирует записи и утверждает глобальный инвариант — слой, что ловит потерянные ответы, прерванные saga и дрейф, что горячий путь не видит. Инженер, что собрал это однажды, перестаёт доверять одному ответу как истине о деньгах и начинает доверять сверенному append-only ledger — и знает, с тестами в доказательство, что ретраи и конкуренция не заставят книги лгать.
</Recap>
