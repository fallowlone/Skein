---
slug: "quiz-code"
lang: ru
track: "system-design-cases"
unit: "04-location-realtime"
order: 7
title: "Локация и реальное время: читаем код и конфиг"
summary: "Читаем реальный гео-код, конфиг маршрутизации pub/sub, настройку движка маршрутизации и команды отсортированного множества Redis, затем рассуждаем: баг одной ячейки, взрыв вещания, ловушка пересжатия и запрос ранга O(n)."
estMin: 14
status: ready
lessonType: topic
level: senior
concepts:
  - spatial-index
  - cell-boundary
  - pub-sub-by-region
  - contraction-hierarchies
  - redis-zset
  - rank-query
prereqs:
  - 04-location-realtime/01-proximity-service
  - 04-location-realtime/02-nearby-friends
  - 04-location-realtime/03-google-maps
  - 04-location-realtime/04-gaming-leaderboard
sources:
  - https://redis.io/docs/latest/commands/zrevrank/
  - https://redis.io/docs/latest/develop/interact/pubsub/
  - https://h3geo.org/docs/highlights/indexing/
---
import Hook from "~/components/lesson/Hook.astro";
import Crux from "~/components/prose/Crux.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Баги этих систем живут в коде, не в прозе: запрос, читающий одну ячейку, публикация, идущая всем, настройка маршрутизации, пере-предобрабатывающая на каждый тик трафика, запрос ранга, который сканирует. Читай каждый сниппет, прогоняй в голове и выбирай ответ, на который подписался бы senior-инженер.
</Hook>

<Crux>Читай сниппет, найди структурную ошибку — одна ячейка, вещание, пересжатие, ранг O(n) — и выбери фикс, уважающий паттерн доступа.</Crux>

<Explanation>

Практикуй петлю дизайн-ревью: найди паттерн доступа в коде, сопоставь его с верной структурой и выбери изменение, которое арифметика/архитектура реально поддерживают.

## Сниппет 1 — запрос близости

```python
def nearby(lat, lng, radius_m):
    cell = h3.geo_to_h3(lat, lng, res=9)         # собственная ячейка пользователя
    candidates = store.get_by_cell(cell)         # лишь эта одна ячейка
    return [p for p in candidates
            if haversine(lat, lng, p.lat, p.lng) <= radius_m]
```

<Quiz
  id="04-location-realtime-code-1"
  lessonSlug="quiz-code"
  lang="ru"
  question="В чём баг nearby() и как его починить?"
  choices={[
    { label: "Читается лишь собственная ячейка пользователя, так что место прямо за границей пропускается. Берите ячейку плюс её kRing соседей (под radius_m), объедините кандидатов, затем примените фильтр haversine.", correct: true },
    { label: "Фильтр haversine не нужен; ячейка уже гарантирует радиус.", misconception: "Ячейка — грубая корзина, не круг — она содержит места вне радиуса и исключает те, что прямо за краем. Нужны И запрос соседей (чтобы ничего близкого не пропустить), И фильтр точного расстояния." },
    { label: "Разрешение 9 слишком мелкое; понизьте до res=5, чтобы захватить больше мест.", misconception: "Огрубление ячейки грубо хватает далёкие места и всё равно асимметрично пропускает случай границы. Верный фикс — запрашивать соседние ячейки (kRing) на разрешении под радиус, затем фильтровать по точному расстоянию." },
    { label: "haversine надо заменить на SQL ORDER BY distance.", misconception: "Математика расстояния в порядке; структурный баг — чтение одной ячейки. ORDER BY distance по всем строкам — это скан O(n), которого индекс ячейки и существует, чтобы избежать." },
  ]}
/>

## Сниппет 2 — публикатор локаций

```python
def on_ping(user_id, lat, lng):
    store.set(user_id, (lat, lng), ttl=30)
    for conn in all_connections:          # каждый подключённый клиент
        conn.send({"user": user_id, "lat": lat, "lng": lng})
```

<Quiz
  id="04-location-realtime-code-2"
  lessonSlug="quiz-code"
  lang="ru"
  question="При 2 млн пингов/с этот цикл плавит кластер. Структурный фикс?"
  choices={[
    { label: "Прекратить вещать всем соединениям (квадратичный взрыв). Публиковать пинг в pub/sub-канал его региональной ячейки; шлюзы подписаны лишь на ячейки со своими пользователями, а край-подписчик фильтрует до друзей в радиусе.", correct: true },
    { label: "Перенести цикл в фоновый поток, чтобы не блокировать приём.", misconception: "Фоновый поток всё равно шлёт каждому соединению — общая работа тот же квадратичный взрыв, лишь вне потока приёма. Фикс — НЕ слать всем: маршрутизировать по каналу региона и фильтровать на краю." },
    { label: "Батчить отправки и флашить каждые 100 мс.", misconception: "Батчинг снижает syscalls, но всё равно доставляет каждый пинг каждому соединению — проблема в множестве получателей, не в каденции флаша. Публикуйте по региональной ячейке, чтобы получали лишь близкие подписчики." },
    { label: "Увеличить TTL, чтобы записей было меньше.", misconception: "TTL управляет истечением, не тем, кто получает пинг. Плавление — фанаут на все соединения; чините его pub/sub по региону плюс фильтр дружбы/радиуса на краю." },
  ]}
/>

## Сниппет 3 — настройка движка маршрутизации

```python
def update_traffic(edge_id, new_travel_time):
    graph.set_weight(edge_id, new_travel_time)
    graph.build_contraction_hierarchy()   # бежит на каждое обновление трафика
    return graph
# вызывается ~тысячи раз в секунду из потока зондов
```

<Quiz
  id="04-location-realtime-code-3"
  lessonSlug="quiz-code"
  lang="ru"
  question="Почему это катастрофа и какая архитектура верна?"
  choices={[
    { label: "Перестройка contraction hierarchy отнимает часы на континентальном графе, так что делать её на каждое обновление трафика невозможно. Разделите медленно меняющуюся структуру (предобрабатывать редко) и быстро меняющиеся веса (накладывать живьём как оверлей), чтобы трафик перевзвешивал рёбра без пересжатия.", correct: true },
    { label: "Просто закэшировать построенную иерархию, чтобы перестройка была быстрой во второй раз.", misconception: "Кэш не помогает, когда веса меняются на каждом вызове — каждое обновление инвалидирует кэш иерархии. Надо развязать структуру и веса, чтобы живой трафик накладывался без перестройки." },
    { label: "Отказаться от contraction hierarchies и использовать наивный Dijkstra, чтобы веса были свежими.", misconception: "Это жертвует скоростью, нужной на планетарном QPS. Держите предобработку, но партиционируйте по скорости изменения: структура предобрабатывается редко, веса накладываются живьём." },
    { label: "Запускать перестройку на большей машине, чтобы успевала.", misconception: "Ни одна машина не сжимает континентальный граф за миллисекунды между тиками трафика. Архитектурный фикс — разделение структуры и весов, не более быстрое железо." },
  ]}
/>

## Сниппет 4 — запрос ранга

```sql
-- ранг игрока на лидерборде
SELECT COUNT(*) + 1 AS rank
FROM scores
WHERE score > (SELECT score FROM scores WHERE player_id = :id);
-- вызывается на игрока, миллионы опрашивают свой ранг
```

<Quiz
  id="04-location-realtime-code-4"
  lessonSlug="quiz-code"
  lang="ru"
  question="Почему этот запрос плохо масштабируется и что его заменяет?"
  choices={[
    { label: "COUNT(*) WHERE score > mine — это счёт O(n) на вызов, так что миллионы опрашивающих ранг сканируют таблицу снова и снова. Используйте Redis-отсортированное множество: ZREVRANK player даёт ранг за O(log n), потому что ранг держится в span'ах skip list.", correct: true },
    { label: "Добавить индекс по score, и запрос станет O(1).", misconception: "Индекс помогает найти строки, но COUNT(*) по всем высшим очкам всё равно O(n) на запрос. ZREVRANK у ZSET честно O(log n) — суммирует предвычисленные span'ы вместо счёта строк." },
    { label: "Обернуть в материализованное представление с еженочным обновлением.", misconception: "Еженочное представление делает ранг устаревшим (очки меняются вживую) и O(n) на обновление для всех игроков, большинство из которых не смотрят. ZREVRANK считает любой ранг свежим за O(log n) на чтение." },
    { label: "Денормализовать колонку 'rank', обновляемую на каждой записи.", misconception: "Обновление хранимого ранга на смене очков может сдвинуть ранг всех ниже — O(n) на запись. Отсортированное множество держит ранг неявно в структуре, так что чтения O(log n) и записи O(log n)." },
  ]}
/>

</Explanation>

<KeyTakeaway>Ошибки структурны и видны в коде: чтение одной ячейки (запрашивай соседей), вещание всем соединениям (публикуй по региону), пересжатие на каждый тик (раздели структуру и веса), счёт ранга O(n) (множество).</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="04-location-realtime-code-retrieval"
  lang="ru"
  questions={[
    { q: "Два кодовых запаха, сигналящих о баге близости или фанаута, и их фиксы?", a: "(1) Гео-запрос, читающий лишь собственную ячейку пользователя — он пропускает места прямо за границей; фикс — брать ячейку плюс кольцо соседей (kRing/3×3) под радиус, затем фильтровать по точному расстоянию. (2) Обработчик локаций, шлющий каждый пинг всем соединениям — квадратичное вещание, плавящее на масштабе; фикс — публиковать в канал региональной ячейки пинга, чтобы получали лишь подписчики этой ячейки, с фильтром друг-и-радиус на краю-подписчике." },
    { q: "Почему пере-сжатие на каждое обновление трафика неверно, и почему SQL-запрос ранга медлен — что заменяет каждое?", a: "Сжатие континентального графа отнимает часы, так что перестройки на обновление невозможны — разделите медленную структуру (предобрабатывать редко) и быстрые веса (живой оверлей), позволяя трафику перевзвешивать рёбра без пересжатия. SQL-ранг — это COUNT(*) WHERE score > mine, O(n) на вызов, давит под миллионами опрашивающих — заменяйте Redis-отсортированным множеством, чей ZREVRANK O(log n), суммирующий span'ы skip list, а не считающий строки. Оба — один урок: сопоставь структуру с паттерном доступа вместо пересчёта на горячем пути." },
  ]}
/>

<Recap lang="ru">
Каждый сбой этого юнита **виден в коде и структурен**, не деталь тюнинга. Запрос близости, читающий **одну ячейку**, пропускает место за границей — запрашивай **кольцо соседей** и фильтруй по точному расстоянию. Обработчик локаций, шлющий каждый пинг **всем соединениям** — квадратичное вещание — **публикуй по региональной ячейке** и фильтруй на краю-подписчике. Движок маршрутизации, **перестраивающий contraction hierarchy** на каждый тик трафика — невозможно на континентальном масштабе — **раздели структуру и веса**, чтобы трафик накладывался живьём. А SQL-запрос ранга (`COUNT(*) WHERE score > mine`) — это **скан O(n)** на опрашивающего — заменяй **отсортированным множеством**, чей `ZREVRANK` — `O(log n)`. Senior-привычка одна каждый раз: читай паттерн доступа из кода, затем выбирай структуру данных или маршрутизацию под него — никогда версию, что прячет `O(n)` или пересчёт на горячем пути.
</Recap>
