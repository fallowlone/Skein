---
slug: "quiz-short"
lang: ru
track: "system-design-cases"
unit: "04-location-realtime"
order: 6
title: "Локация и реальное время: обзор со свободным припоминанием"
summary: "Промпты на свободное припоминание по кейсам локации и реального времени. Отвечай по памяти первым — выбор индекса, фикс границы, слой фанаута, предобработка маршрутизации, структура ранга — затем открой образец и сравни."
estMin: 14
status: ready
lessonType: topic
level: senior
concepts:
  - spatial-index
  - cell-boundary
  - pub-sub-by-region
  - contraction-hierarchies
  - redis-zset
  - score-sharding
prereqs:
  - 04-location-realtime/01-proximity-service
  - 04-location-realtime/02-nearby-friends
  - 04-location-realtime/03-google-maps
  - 04-location-realtime/04-gaming-leaderboard
sources:
  - https://h3geo.org/docs/highlights/indexing/
  - https://redis.io/docs/latest/develop/data-types/sorted-sets/
  - https://datatracker.ietf.org/doc/html/rfc6455
---
import Hook from "~/components/lesson/Hook.astro";
import Crux from "~/components/prose/Crux.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";

<Hook>
Припоминание бьёт перечитывание. Для каждого промпта восстанови полный ответ по памяти — механизм, не только название — прежде чем открыть образец. Усилие пересборки пространственного индекса, фанаута по региону и отсортированного множества — то, что заставляет их закрепиться.
</Hook>

<Crux>Восстанови спину юнита: индексирование и границы, write-heavy фанаут, предобработка маршрутизации, отсортированное множество.</Crux>

<Explanation>

Восстанови каждый кейс по памяти, не подглядывая: как индексировать пространство и увернуться от ловушки границы, почему «друзья рядом» write-heavy и как pub/sub по региону ограничивает фанаут, почему наивный Dijkstra проваливается и что чинит, и структура, делающая обновление, top-k и ранг дешёвыми разом.

<RetrievalDrawer
  client:load
  id="04-location-realtime-short-retrieval"
  lang="ru"
  questions={[
    { q: "Сравните geohash, quadtree и S2/H3, и объясните проблему границы ячейки и её фикс.", a: "Geohash чередует биты широты/долготы в строку, так что близкие точки обычно делят префикс (ищется B-деревом), но свойство префикса ломается на границах, а ячейки неравномерны по площади. Quadtree рекурсивно делит на четыре, разбивая лишь плотные ячейки — адаптивен к плотности, но это stateful-дерево. S2 (куб + кривая Гильберта, 64-битные ID) и H3 (шестиугольники) дают близкие к равноплощадным, чисто вложенные ячейки; шесть равноудалённых соседей H3 делают расширение кольца чистым — продакшен-выбор. Проблема границы: истинный ближайший сосед точки может лежать прямо за линией, так что запрос одной ячейки его пропускает. Фикс: запросить ячейку И соседей (блок 3×3 или kRing под радиус) как грубый фильтр, затем ранжировать по точному ортодромическому расстоянию — и совместить разрешение с радиусом («покрытия» S2/H3)." },
    { q: "Почему «друзья рядом» write-heavy, и как WebSocket и pub/sub по региону заставляют это работать?", a: "Каждый активный делящийся телефон пингует каждые несколько секунд — ≈2 млн записей/с для 10 млн делящихся — но данные эфемерны и бесполезны через секунды, так что нужно in-memory истекающее по TTL хранилище, не долговечная БД. Клиенты держат постоянные WebSocket-соединения, чтобы сервер толкал в момент новости (опрос платит полный запрос/ответ за проверку и фиксирует задержку на интервал). Pub/sub по региону ограничивает фанаут: делим мир на ячейки, публикуем каждый пинг ровно в канал его ячейки, шлюзы подписаны лишь на свои ячейки — так берлинский пинг не доходит до Токио. Дешёвый фильтр «друг и в радиусе» бежит на краю-подписчике, никогда как join по графу+гео на потоке приёма (фанаут-на-запись). Приватность структурна: истечение TTL, согласие по-отношению, фильтр дружбы на краю." },
    { q: "Почему наивный Dijkstra проваливается на маршрутизации Maps, и как contraction hierarchies и живой трафик сочетаются?", a: "Dijkstra расходится во все стороны и раскрывает миллионы узлов на запрос; A* помогает, но исследует широкий конус — ни один не по карману на планетарном QPS. Contraction hierarchies предобрабатывают граф офлайн: ранжируют узлы по важности, сжимают их, добавляют сохраняющие расстояния рёбра-сокращения и бегут двунаправленным запросом, движущимся только ВВЕРХ — едущим на горстке высокоуровневых сокращений, так что трогает тысячи узлов, не миллионы, всё ещё возвращая точный кратчайший путь. Так как чистое сжатие запекает веса в сокращения, живой трафик (непрерывный поток GPS-зондов, агрегированный по сегменту дороги для ETA) вынудил бы пересжатие — поэтому продакшен разделяет медленную СТРУКТУРУ (предобрабатывается редко) и быстрые ВЕСА (накладываются живьём). Тайлы при этом — отдельная задача CDN." },
    { q: "Почему отсортированное множество — верная структура для лидерборда, и как работают top-k, мой-ранг, ничьи и шардинг?", a: "Доске нужны дешёвое обновление, top-k И ранг-одного вместе; SQL-таблица делает «мой ранг» счётом O(n) COUNT(*) WHERE score > mine, фатально при опросе. Redis ZSET (skip list со span'ами узлов + хеш) даёт ZADD, ZREVRANGE и ZREVRANK все за O(log n) — ранг суммированием предвычисленных ширин-прыжков, без счёта. Top-k разделяем (считай один раз, кэшируй для всех); мой-ранг персонален (считается вживую на запрос, дёшево через ZREVRANK); окно вокруг игрока переиспользует ранг. Ничьи (равные очки упорядочиваются произвольно по имени) чинятся кодированием тайбрейкера в составной score (очки плюс обратная временная метка). Когда один узел мал/медлен, шардь по диапазону очков: top-k читает высший диапазон, а глобальный ранг = внутришардовый ранг + суммированные размеры более высоких шардов из дешёвых счётчиков (O(шарды)). ZSET — быстрый in-RAM индекс поверх долговечного источника истины, перестраивается при рестарте." },
    { q: "Какое единое озарение связывает все четыре кейса, и где каждый расходится?", a: "Все четыре превращают «сравни со всем» в «трогай лишь осколок», через предвычисление или ключ маршрутизации. Расходятся в паттерне доступа: близость read-heavy и статична, так что строишь тяжёлый пространственный индекс и кэшируешь по ячейке; «друзья рядом» write-heavy и эфемерны, так что бросаешь долговечность, толкаешь по WebSocket и маршрутизируешь через pub/sub по региону; Maps определяется предвычислением (пирамида тайлов + contraction hierarchies) со структурой, отделённой от живых весов трафика; лидерборду нужны три операции дёшево разом, что даёт ранг O(log n) отсортированного множества. Повторяется инстинкт границы: запрашивай соседние ячейки (близость), переподписывайся при пересечении (друзья), партиционируй дорожный граф вдоль малотрафиковых разрезов (Maps) и шардь по диапазону очков с кросс-шардовым рангом (лидерборд)." },
  ]}
/>

</Explanation>

<KeyTakeaway>Если восстанавливаешь каждый кейс по памяти — индекс и фикс границы, write-heavy фанаут, CH и ранг множества — держишь спину юнита: схема выпадает из паттерна доступа, превращая «скан всего» в «трогай осколок».</KeyTakeaway>

<Recap lang="ru">
Если смог пересобрать каждый ответ по памяти, держишь спину юнита. **Близость** индексирует пространство (geohash/quadtree/S2-H3) и запрашивает ячейку **плюс соседей** как грубый фильтр, ранжируя уцелевших по точному расстоянию и кэшируя по ячейке. **«Друзья рядом»** инвертируют это в **поток записи** эфемерных пингов, толкаемых по **WebSocket** из TTL-хранилища и маршрутизируемых через **pub/sub по региону** с фильтром друг-и-радиус на краю-подписчике. **Maps** определяется **предвычислением** — пирамида тайлов на CDN и маршрутизация через **contraction hierarchies** — с живым трафиком как отдельным оверлеем весов, чтобы он не вынуждал пересжатие. **Лидерборд** использует **отсортированное множество**, делая обновление, top-k и ранг-одного все `O(log n)`, где SQL делает ранг счётом `O(n)`, и шардит по диапазону очков с глобальным рангом из по-шардовых счётчиков. Объединяющая идея: каждая схема **следует паттерну доступа** и превращает «сравни со всем» в «трогай лишь осколок».
</Recap>
