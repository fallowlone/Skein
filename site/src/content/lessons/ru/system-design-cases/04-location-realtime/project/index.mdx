---
slug: "project"
lang: ru
track: "system-design-cases"
unit: "04-location-realtime"
order: 8
title: "Локация и реальное время: соберите живой сервис близости + лидерборд"
summary: "Практический проект: соберите поиск рядом по пространственному индексу (запрос соседних ячеек), канал push в реальном времени по региону и backend лидерборда на отсортированном множестве — затем докажите корректность ранга и близости тестами."
estMin: 240
status: ready
lessonType: topic
level: senior
concepts:
  - spatial-index
  - cell-boundary
  - pub-sub-by-region
  - websockets
  - redis-zset
  - rank-query
prereqs:
  - 04-location-realtime/01-proximity-service
  - 04-location-realtime/02-nearby-friends
  - 04-location-realtime/03-google-maps
  - 04-location-realtime/04-gaming-leaderboard
sources:
  - https://h3geo.org/docs/highlights/indexing/
  - https://redis.io/docs/latest/develop/data-types/sorted-sets/
  - https://redis.io/docs/latest/develop/interact/pubsub/
---
import Hook from "~/components/lesson/Hook.astro";
import Crux from "~/components/prose/Crux.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import ProjectBrief from "~/components/pedagogy/ProjectBrief.astro";

<Hook>
Прочитать, что нужно запрашивать соседние ячейки и держать ранг в отсортированном множестве — не то же, что собрать сервис, переживающий движущуюся нагрузку. Возьмите три примитива юнита — пространственный индекс, канал push по региону и отсортированное множество — и соберите небольшой, но реальный сервис локации и реального времени, затем докажите два свойства корректности, что вероятнее всего укусят: что близость находит соседа за границей и что ранг игрока верен среди миллионов.
</Hook>

<Crux>Соберите поиск рядом по индексу, push по региону и лидерборд на отсортированном множестве — затем докажите корректность близости и ранга.</Crux>

<Explanation>

Этот проект делает юнит операционным: вы реализуете запрос близости грубо-потом-точно, фанаут по региону, избегающий вещания, и движок ранга `O(log n)`, затем пишете тесты, ловящие канонические баги — пропущенного соседа за границей и неверный глобальный ранг.

<ProjectBrief
  id="04-location-realtime-project"
  lessonSlug="project"
  lang="ru"
  objective="Соберите рабочий сервис локации и реального времени из трёх частей — запрос поиска рядом по пространственному индексу, канал push в реальном времени для движущихся сущностей по региону и лидерборд на отсортированном множестве — и докажите тестовым харнессом, что близость возвращает соседей за границей и что ранги (включая глобальный ранг при шардинге) корректны."
  requirements={[
    "Выберите пространственный индекс (geohash, библиотека H3/S2 или собственный quadtree) и обоснуйте выбор паттерном доступа; загрузите датасет статичных точек (например, 100 тыс. синтетических мест) и индексируйте каждую по ячейке.",
    "Реализуйте find-nearby(lat, lng, radius): переведите точку в ячейку, достаньте ячейку И её кольцо соседей под радиус, затем фильтруйте объединение по точному ортодромическому (haversine) расстоянию — грубый фильтр, затем тонкий.",
    "Добавьте кэш по ключу (cell, фильтры) для пути чтения и покажите измеренное улучшение hit-rate на перекошенной нагрузке (большинство запросов в несколько горячих ячеек).",
    "Соберите канал реального времени: смоделируйте N движущихся сущностей, эмитящих гео-пинги, храните каждую с коротким TTL по ключу региональной ячейки и публикуйте в pub/sub-топик на ячейку; подписчик получает лишь пинги своей ячейки и переподписывается, когда отслеживаемая сущность пересекает границу.",
    "Толкайте обновления клиенту по постоянному соединению (WebSocket или SSE) и проверяйте фильтр дружбы/видимости на краю-подписчике, чтобы локация не-друга никогда не доставлялась.",
    "Сделайте backend лидерборда на отсортированном множестве (Redis ZSET или собственный skip list): поддержите обновление (ZADD), top-k (ZREVRANGE), мой-ранг (ZREVRANK) и окно вокруг игрока; закодируйте детерминированный тайбрейкер в составной score.",
    "Шардируйте лидерборд по диапазону очков хотя бы на две партиции и реализуйте глобальный ранг как внутришардовый ранг плюс суммированные размеры более высоких по очкам шардов (держите по-шардовые счётчики кардинальности).",
    "Напишите тестовый харнесс, утверждающий два свойства корректности: брутфорс-скан O(n) совпадает с find-nearby на рандомизированных запросах (включая точки намеренно у границ ячеек), и брутфорс-сортировка совпадает с глобальным рангом отсортированного/шардированного множества для рандомизированных очков.",
  ]}
  acceptance={[
    "Эндпоинт find-nearby, который на рандомизированных входах, включая точки у границ, возвращает ровно то же множество, что брутфорс-скан по расстоянию — демонстрируя, что запрос кольца соседей чинит проблему границы ячейки.",
    "Измеренный hit-rate кэша на перекошенной нагрузке чтения с однострочной заметкой, почему кэширование по ячейке окупается на read-heavy, медленно меняющихся данных о местах.",
    "Рабочее демо реального времени, где пинги движущейся сущности доходят лишь до подписчиков её текущей региональной ячейки (показано логом или счётчиком), с переключением подписки при пересечении границы и недоставкой локации не-другу.",
    "Лидерборд, чьи операции обновления, top-k, мой-ранг и окно бегут без полного скана, с ничьями, решёнными детерминированно составным score.",
    "Тест, доказывающий, что глобальный ранг при шардинге по диапазону очков равен брутфорс-отсортированному рангу для рандомизированных очков, плюс краткая заметка, как по-шардовые счётчики делают это O(шарды), а не кросс-шардовым сканом.",
  ]}
  stretch={[
    "Добавьте адаптацию к плотности: переключите горячую переполняющуюся ячейку на более мелкое разрешение (или дробление quadtree) и покажите падение числа кандидатов на запрос, удерживая задержку плотной зоны ограниченной.",
    "Добавьте крошечный компонент маршрутизации: постройте малый дорожный граф, сожмите его в иерархию и покажите, что двунаправленный поиск вверх возвращает тот же кратчайший путь, что наивный Dijkstra, раскрывая куда меньше узлов.",
    "Добавьте адаптивную частоту пинга в канал реального времени (медленнее при покое сущности, быстрее при движении) и измерьте снижение записей против фиксированного интервала при равной воспринимаемой свежести.",
    "Добавьте временны́е лидерборды (дневной / недельный / всё-время) как отдельные отсортированные множества и обсудите, как это уменьшает каждую доску и размазывает нагрузку записи.",
  ]}
/>

</Explanation>

<KeyTakeaway>Рабочая сборка делает юнит реальным: близость грубо-потом-точно, push по региону, лидерборд на отсортированном множестве O(log n). Докажи два — find-nearby совпадает с брутфорс-сканом на границе, ранг — с сортировкой.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="04-location-realtime-project-retrieval"
  lang="ru"
  questions={[
    { q: "Какие три примитива собирает этот проект, и что демонстрирует каждый?", a: "(1) Запрос find-nearby по пространственному индексу, достающий ячейку плюс кольцо соседей и фильтрующий по точному расстоянию — демонстрирует паттерн грубо-потом-точно и фикс границы ячейки, с кэшированием по ячейке на read-heavy нагрузке. (2) Канал push реального времени, хранящий движущиеся сущности с TTL по ключу региона и публикующий по ячейке по постоянному соединению, с фильтром дружбы на краю-подписчике — демонстрирует фанаут по региону вместо вещания. (3) Лидерборд на отсортированном множестве, поддерживающий обновление, top-k, мой-ранг и окно за O(log n), шардированный по диапазону очков с глобальным рангом из по-шардовых счётчиков — демонстрирует, что ранг живёт в структуре." },
    { q: "Как доказать два свойства корректности, и почему именно эти два?", a: "Близость: гоните рандомизированные запросы find-nearby — намеренно включая точки у границ ячеек — и утверждайте, что результат равен брутфорс-скану O(n) по расстоянию; это ловит канонический баг одной ячейки, ведь сервис, запрашивающий лишь одну ячейку, разойдётся ровно на точках границы. Ранг: генерируйте рандомизированные очки, считайте глобальный ранг через отсортированное множество при шардинге по диапазону очков (внутришардовый ранг + суммированные размеры более высоких шардов) и утверждайте совпадение с брутфорс-полной сортировкой; это ловит ошибки на единицу и в кросс-шардовом суммировании. Они выбраны, потому что вероятнее всего тихо неверны: оба выглядят нормально в небрежном тесте и падают лишь на крайних случаях." },
    { q: "Почему собирать все три вместе, а не по отдельности?", a: "Потому что урок юнита — верная схема следует паттерну доступа, и три рядом делают контраст конкретным: та же идея региональной ячейки — это поисковый индекс для статичных точек (близость), ключ маршрутизации для движущихся (реальное время), а инстинкт обработки границ всплывает как шардинг по диапазону очков в лидерборде. Сборка вместе вынуждает переиспользовать абстракцию ячейки на read-heavy и write-heavy нагрузках и почувствовать, почему грубо-потом-точно, фильтрация на краю и ранг, держимый в структуре — одна дисциплина, приложенная к разным формам нагрузки." },
  ]}
/>

<Recap lang="ru">
Этот проект превращает юнит локации и реального времени в рабочую систему из **трёх примитивов**. Часть **близости** реализует поиск грубо-потом-точно по пространственному индексу — достать ячейку **плюс кольцо соседей**, фильтровать по точному расстоянию и кэшировать по ячейке на read-heavy нагрузке — и доказывается корректной против брутфорс-скана **включая точки у границ**, тест, вскрывающий баг одной ячейки. Часть **реального времени** хранит движущиеся сущности с **TTL по ключу региональной ячейки**, публикует по ячейке по постоянному соединению, переподписывается при пересечении границ и проверяет фильтр дружбы на **краю-подписчике**, а не вещает. Часть **лидерборда** использует **отсортированное множество** для `O(log n)` обновления, top-k, мой-ранг и окна, с тайбрейкером в составном score, и шардит по диапазону очков, так что **глобальный ранг** — внутришардовый ранг плюс счёты более высоких шардов — доказывается против брутфорс-сортировки. Сборка всех трёх вместе делает объединяющий урок осязаемым: та же абстракция региональной ячейки — это поисковый индекс, ключ маршрутизации и ось партиционирования, а верная структура всегда следует **паттерну доступа**.
</Recap>
