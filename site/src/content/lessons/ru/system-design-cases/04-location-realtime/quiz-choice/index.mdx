---
slug: "quiz-choice"
lang: ru
track: "system-design-cases"
unit: "04-location-realtime"
order: 5
title: "Локация и реальное время: обзор с выбором ответа"
summary: "Синтез с выбором ответа по кейсам локации и реального времени: пространственное индексирование и ловушка границы ячейки, write-heavy фанаут локаций, contraction hierarchies для маршрутизации и отсортированное множество за лидербордом."
estMin: 13
status: ready
lessonType: topic
level: senior
concepts:
  - spatial-index
  - cell-boundary
  - pub-sub-by-region
  - contraction-hierarchies
  - redis-zset
  - rank-query
prereqs:
  - 04-location-realtime/01-proximity-service
  - 04-location-realtime/02-nearby-friends
  - 04-location-realtime/03-google-maps
  - 04-location-realtime/04-gaming-leaderboard
sources:
  - https://s2geometry.io/
  - https://redis.io/docs/latest/develop/data-types/sorted-sets/
  - https://www.microsoft.com/en-us/research/publication/customizable-route-planning/
---
import Hook from "~/components/lesson/Hook.astro";
import Crux from "~/components/prose/Crux.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Шесть решений, проходящих сквозь четыре кейса. Каждое — выбор у доски: как индексировать пространство, где запускать фанаут, какой алгоритм переживёт континентальный граф, какая структура отвечает «мой ранг среди миллионов» — не определение для зубрёжки.
</Hook>

<Crux>Шесть межкейсовых решений: пространственный индекс, фикс границы, фанаут по региону, предобработка маршрутизации и запрос ранга лидерборда.</Crux>

<Explanation>

Подтвердите, что умеете выбрать пространственный индекс под паттерн доступа, починить ловушку границы ячейки, разместить фанаут локаций на верном слое, отвергнуть наивный Dijkstra на континентальном масштабе и выбрать структуру, делающую ранг-одного дешёвым.

<Quiz
  id="04-location-realtime-choice-1"
  lessonSlug="quiz-choice"
  lang="ru"
  question="Сервис поиска рядом запрашивает лишь собственную ячейку geohash пользователя и ранжирует места в ней. Пользователи сообщают, что явно близкий магазин иногда пропадает. Фикс?"
  choices={[
    { label: "Запрашивать ячейку И её соседей (блок 3×3 или kRing в H3), считая индекс ячейки грубым фильтром, затем ранжировать по точному расстоянию — ближайшее место может лежать прямо за границей в соседней ячейке.", correct: true },
    { label: "Повысить точность geohash, чтобы ячейки были мельче и точнее.", misconception: "Мельче ячейки делают проблему границы ХУЖЕ — больше краёв, через которые упасть. Фикс — запрашивать соседние ячейки, не уменьшать их; разрешение должно совпадать с радиусом, но соседи всё равно нужны." },
    { label: "Перестраивать пространственный индекс еженочно ради исправления устаревших назначений.", misconception: "Назначение не устарело; магазин корректно в своей ячейке. Запрос одной ячейки структурно не видит места прямо за линией — надо запрашивать окружающие." },
    { label: "Добавить реплику чтения, чтобы запрос видел больше строк.", misconception: "Реплика возвращает тот же результат одной ячейки, лишь с другого узла. Пропавший магазин в соседней ячейке, которую не запрашивали — берите соседей." },
  ]}
/>

<Quiz
  id="04-location-realtime-choice-2"
  lessonSlug="quiz-choice"
  lang="ru"
  question="Квадратная сетка фиксированного разрешения переполняется на Таймс-сквер (одна ячейка держит десятки тысяч мест), а океанские ячейки пусты, и задержка плотных запросов ужасна. Какое структурное изменение помогает больше всего?"
  choices={[
    { label: "Индекс с адаптацией к плотности — quadtree или переменное разрешение S2/H3 — чтобы плотные зоны дробились на мелкие ячейки, а разреженные оставались грубыми, ограничивая число кандидатов на запрос.", correct: true },
    { label: "Кэшировать каждую ячейку, чтобы БД не трогать.", misconception: "Кэш режет нагрузку чтения, но в плотной ячейке всё равно десятки тысяч кандидатов на ранжирование на запрос. Нужна адаптивная разрешающая способность, чтобы ограничить число кандидатов, а кэш сверху." },
    { label: "Использовать грубее ячейки везде, чтобы уменьшить число ячеек.", misconception: "Грубее ячейки делают переполнение плотной зоны хуже — каждая держит ещё больше. Нужны МЕЛЬЧЕ там, где плотно, что и дают адаптивные индексы (quadtree, S2/H3)." },
    { label: "Перейти с geohash на одну глобальную ячейку.", misconception: "Одна ячейка означает сканирование всего — противоположность индексированию. Фикс — адаптивное разрешение: мельче где плотно, грубее где разреженно." },
  ]}
/>

<Quiz
  id="04-location-realtime-choice-3"
  lessonSlug="quiz-choice"
  lang="ru"
  question="Система «друзья рядом» принимает 2 млн гео-пингов/с. Инженер предлагает на каждый пинг загружать друзей публикатора, доставать их локации и толкать близким. Почему предпочесть pub/sub по региону?"
  choices={[
    { label: "Фанаут-на-запись делает дорогой join по графу + гео на самом горячем пути на каждый пинг. Публикация в канал региона развязывает приём с подбором; дешёвый фильтр «друг и в радиусе» бежит на краю-подписчике на потоке, уже суженном до одного региона.", correct: true },
    { label: "Pub/sub гарантирует доставку ровно один раз, чего прямой push не может.", misconception: "Ни один не гарантирует exactly-once, и это неважно — потерянный пинг заменяется через секунды. Причина — развязать поток с подбором друзей, а не семантика доставки." },
    { label: "Прямой push не может использовать WebSocket, а pub/sub может.", misconception: "Оба толкают по WebSocket на краю. Различие — pub/sub по региону публикует один раз на ячейку и дёшево фильтрует на подписчике, вместо полного join по графу+гео на пинг при приёме." },
    { label: "Pub/sub хранит историю локаций бесплатно.", misconception: "Живая локация эфемерна и намеренно не хранится на этом пути; история, если нужна — отдельное пакетное хранилище. Причина pub/sub — убрать подбор с горячего пути приёма." },
  ]}
/>

<Quiz
  id="04-location-realtime-choice-4"
  lessonSlug="quiz-choice"
  lang="ru"
  question="Команда строит континентальную маршрутизацию наивным Dijkstra; корректно, но секунды на запрос с раскрытием миллионов узлов. Какая предобработка заставит запросы трогать лишь тысячи узлов, всё ещё возвращая точный кратчайший путь?"
  choices={[
    { label: "Contraction hierarchies: ранжировать узлы по важности и сжать их, добавляя сохраняющие расстояния рёбра-сокращения, чтобы двунаправленный запрос двигался только вверх и ехал на горстке высокоуровневых сокращений вместо проползания по каждой улице.", correct: true },
    { label: "Предвычислить и хранить кратчайший путь для каждой пары исток–цель.", misconception: "Пар ~N² на десятках миллионов узлов — невозможно вычислить или хранить. Contraction hierarchies предобрабатывают ГРАФ (сокращения), делая любой запрос быстрым без невозможного кэша." },
    { label: "Выбросить второстепенные дороги, чтобы уменьшить граф.", misconception: "Выброс дорог меняет ответ (валидные маршруты исчезают) и всё равно оставляет большой граф. CH держат корректность через сохраняющие расстояния сокращения, а не удаляя рёбра." },
    { label: "Запустить Dijkstra на большем числе CPU параллельно.", misconception: "Двунаправленный/параллельный Dijkstra всё равно раскрывает огромный фронт. Выигрыш на порядок — предобработка графа в иерархию с сокращениями, а не сырое вычисление." },
  ]}
/>

<Quiz
  id="04-location-realtime-choice-5"
  lessonSlug="quiz-choice"
  lang="ru"
  question="Лидерборд хранит очки в SQL-таблице с индексом по score. Top-10 быстр, но показ каждому из 12 млн игроков его живого ранга давит базу. Корень и фикс?"
  choices={[
    { label: "«Мой ранг» — это SELECT COUNT(*) WHERE score > mine — счёт O(n) на игрока на запрос. Используйте Redis-отсортированное множество: ZREVRANK возвращает ранг за O(log n), потому что ранг живёт в span'ах skip list, так что он никогда не сканирует.", correct: true },
    { label: "Индекс фрагментирован; перестройте его и добавьте реплик.", misconception: "Индекс делает top-k быстрым, но не делает COUNT(*) WHERE score > mine дешёвым — это O(n) на запрос ранга, а реплики лишь размазывают дорогой запрос. Фикс — ZSET с O(log n) ZREVRANK." },
    { label: "Предвычислить и кэшировать ранг каждого игрока.", misconception: "Поддержка рангов для всех 12 млн — O(n) на каждую смену очков и в основном впустую (большинство не смотрят). ZSET считает любой ранг за O(log n) на чтение — глобальный кэш рангов не нужен." },
    { label: "Перейти на документную базу ради быстрых записей.", misconception: "Документное хранилище всё равно сканирует, чтобы сосчитать высшие очки. Верный инструмент — упорядоченная, осознающая ранг структура: отсортированное множество с O(log n) ZREVRANK." },
  ]}
/>

<Quiz
  id="04-location-realtime-choice-6"
  lessonSlug="quiz-choice"
  lang="ru"
  question="Вы шардите лидерборд по диапазону очков по узлам, потому что один узел не держит его. Как всё ещё отвечать на ГЛОБАЛЬНЫЙ ранг игрока без скана?"
  choices={[
    { label: "Глобальный ранг = внутришардовый ранг игрока (ZREVRANK, O(log n)) ПЛЮС суммированные размеры всех более высоких по очкам шардов, читаемые из дешёвых по-шардовых счётчиков кардинальности — сложение O(шарды).", correct: true },
    { label: "Опросить каждый шард на всех игроков выше очков и сосчитать их.", misconception: "Это скан O(n) по шардам — то, чего шардинг должен избегать. Используйте внутришардовый ранг плюс по-шардовые счётчики размеров более высоких шардов: O(шарды)." },
    { label: "Глобальный ранг невозможен после шардинга; работает только top-k.", misconception: "Решаемо: внутришардовый ранг плюс суммированные размеры более высоких шардов из счётчиков. Шардинг по диапазону очков сохраняет и top-k, и глобальный ранг." },
    { label: "Реплицировать всю доску на один узел ради вычисления ранга.", misconception: "Это воссоздаёт одноузловое узкое место памяти и пропускной способности, от которого вы шардировали. Используйте внутришардовый ранг плюс счёты более высоких шардов из поддерживаемых счётчиков." },
  ]}
/>

</Explanation>

<KeyTakeaway>Ответы идут из паттерна доступа: запрашивай соседей, не одну ячейку; фанаут на краю по региону; предобрабатывай граф в сокращения, не перебирай Dijkstra; держи ранг в множестве, чтобы «мой ранг» был O(log n), не O(n).</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="04-location-realtime-choice-retrieval"
  lang="ru"
  questions={[
    { q: "Почему запрос близости должен брать соседей ячейки, не одну ячейку?", a: "Истинный ближайший сосед точки может лежать прямо за линией ячейки в соседней. Запрос одной ячейки структурно его не видит. Считайте индекс ячейки грубым фильтром: берите ячейку И её окружающие (блок 3×3 или kRing в H3 под радиус), затем ранжируйте по точному расстоянию. Грубо-потом-точно, с кольцом соседей под радиус поиска." },
    { q: "Почему ранг-одного дёшев в ZSET, но дорог в SQL?", a: "В SQL «мой ранг» — это SELECT COUNT(*) WHERE score > mine — счёт O(n) на запрос, фатально при опросе миллионами. Redis ZSET — это skip list, чьи узлы хранят span'ы (сколько элементов перепрыгивает каждый указатель), так что ZREVRANK суммирует горстку предвычисленных ширин-прыжков вдоль пути O(log n) — ранг без счёта. Обновление, top-k и ранг — все O(log n) разом." },
  ]}
/>

<Recap lang="ru">
Сквозная мысль юнита — верная схема выпадает из **паттерна доступа**. **Близость** read-heavy и медленно меняется, так что строишь пространственный индекс и запрашиваешь ячейку **плюс её соседей** как грубый фильтр, ранжируя уцелевших по точному расстоянию; перекос плотности чинится адаптивным разрешением (quadtree, S2/H3). **«Друзья рядом»** инвертируют это в поток записи, так что толкаешь по WebSocket из эфемерного TTL-хранилища и маршрутизируешь через **pub/sub по региону**, гоняя фильтр друг-и-радиус на краю-подписчике, а не как join по графу+гео на приёме. **Maps** определяется **предвычислением**: тайлы на CDN, маршрутизация через **contraction hierarchies**, чтобы запрос ехал на горстке сокращений вместо раскрытия миллионов узлов, с живым трафиком как отдельным оверлеем весов. **Лидерборду** нужны обновление, top-k и ранг-одного вместе, что даёт **отсортированное множество** за `O(log n)`, где SQL `ORDER BY` делает ранг счётом `O(n)` — и оно шардится по диапазону очков с глобальным рангом из по-шардовых счётчиков.
</Recap>
