---
concepts:
- why-nest
- modular-structure
- dependency-injection
- ioc-container
- constructor-injection
- testability
deepensInto: []
estMin: 15
lang: ru
lessonType: topic
level: junior
order: 1
prereqs:
- Уверенно владеешь Node.js и TypeScript (классы, декораторы, async/await)
slug: 01-why-nest
sources:
- https://docs.nestjs.com/
- https://docs.nestjs.com/fundamentals/custom-providers
spiral: []
status: ready
summary: 'NestJS добавляет структуру к голому Node-бэкенду: модули, контроллеры,
  провайдеры — и dependency injection. Ты объявляешь, что нужно классу, в
  конструкторе; IoC container строит и внедряет это, поэтому проводка
  централизована, а код остаётся тестируемым.'
title: 'Зачем NestJS и ментальная модель DI'
track: nest
unit: 00-start-here
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";
import Infographic from "~/components/diagram/Infographic.astro";
import diPayoffs from "./infographics/di-payoffs.svg?raw";

<Hook>
Через три месяца жизни голого Express-приложения в папке `routes/` лежит 40 файлов, и каждый обработчик где-то наверху тянется к глобальному `db` и к `new MailService()`. Чтобы протестировать один контроллер, приходится поднимать настоящую базу — потому что контроллер жёстко зашил то, как он получает свои зависимости. Добавить кэширование значит править двенадцать файлов, в каждом из которых через `new` создавался один и тот же сервис. Формально всё не так уж плохо — просто нет структуры и нет шва, чтобы что-то подменить. NestJS существует, чтобы дать такому приложению хребет.
</Hook>

<Crux>NestJS структурирует Node-бэкенд через модули, контроллеры и провайдеры, а его IoC container внедряет зависимости каждого класса.</Crux>

<Explanation>

## Что такое Nest и какую проблему он решает

Express и Fastify намеренно минималистичны: они маршрутизируют HTTP-запросы к функциям — и на этом всё. Для сервиса на 200 строк это идеально, для сервиса на 20 000 строк — мучительно. Без соглашения каждая команда изобретает своё — где живёт бизнес-логика, как обработчик получает клиент к базе, как сгруппированы фичи — и ответы расходятся от файла к файлу. Получается тот самый клубок из хука: логика приклеена к глобальным объектам, зависимости создаются через `new` где придётся, и нет чистого места, чтобы подсунуть фейк в тесте.

NestJS — это opinionated-фреймворк поверх Node (по умолчанию он использует Express, но может работать и на Fastify). Он не заменяет обработку HTTP; он её организует. Он даёт три основных строительных блока и один механизм, связывающий их:

- **Модули (modules)** — контейнеры в рамках фичи (`@Module({...})`), которые группируют связанный код и объявляют, что они отдают наружу и что им нужно.
- **Контроллеры (controllers)** — классы, чьи методы обрабатывают входящие запросы и формируют ответ. В них маршрутизация, а не бизнес-логика.
- **Провайдеры (providers)** — всё, что можно внедрить: сервисы, репозитории, фабрики, клиенты. Здесь живёт бизнес-логика, помеченная `@Injectable()`.

Вместе эти три блока дают каждой фиче постоянный адрес: модуль ею владеет, контроллер открывает её по HTTP, а провайдер делает работу. Когда ты добавляешь новую фичу — ты знаешь, куда кладёт каждый кусок, и следующий инженер, открывший проект, тоже это знает.

Механизм — это **dependency injection (DI)**, и именно эту часть стоит усвоить в первый же день: всё остальное в Nest построено на ней.

## Ментальная модель DI: объявляй, что нужно, не создавай это сам

Вот вся идея в одном предложении: *класс перечисляет то, что ему нужно, в конструкторе, а предоставлять это — задача чего-то другого.* Ты перестаёшь писать `new` для своих зависимостей.

Без DI контроллер сам тянется наружу и создаёт своих соавторов:

```ts
// Ручная проводка — контроллер сам решает, КАК он получает зависимости
class UsersController {
  private service: UsersService;
  constructor() {
    const db = new Database(process.env.DB_URL);
    this.service = new UsersService(db); // жёстко зашито, нельзя подменить
  }
}
```

Теперь контроллер приварен к конкретному `UsersService` и к настоящему `Database`. Чтобы протестировать его, придётся дать настоящую базу; чтобы изменить то, как строится `UsersService`, придётся править каждое место, где его создавали.

С DI в Nest контроллер просто *объявляет* зависимость как параметр конструктора. Он никогда не говорит, как сервис создаётся:

```ts
import { Injectable, Controller, Get, Module } from "@nestjs/common";

@Injectable()
class UsersService {
  findAll() {
    return [{ id: 1, name: "Ada" }];
  }
}

@Controller("users")
class UsersController {
  // Объявлено, а не создано. Экземпляр поставляет Nest.
  constructor(private readonly users: UsersService) {}

  @Get()
  list() {
    return this.users.findAll();
  }
}

@Module({
  controllers: [UsersController],
  providers: [UsersService], // регистрируем то, что можно внедрять
})
export class UsersModule {}
```

Когда приложение стартует, **IoC container** (Inversion of Control container, контейнер инверсии управления) читает сигнатуры конструкторов, вычисляет граф зависимостей, создаёт каждый provider один раз и передаёт экземпляры внутрь. «Инверсия управления» — это и есть название для такого переворота: *созданием управляет фреймворк, а не твой код.* Ты объявил `UsersService` в `providers` модуля, поэтому, когда контейнер строит `UsersController`, он видит, что конструктору нужен `UsersService`, создаёт (или переиспользует) его и внедряет.

<Inset kind="why" lang="ru">
Откуда Nest знает, какой класс внедрить из `private readonly users: UsersService`? В TypeScript при включённом `emitDecoratorMetadata` компилятор записывает *типы* параметров конструктора как метаданные, которые Nest читает в рантайме через `reflect-metadata`. Поэтому аннотация типа здесь — не просто документация, а injection token. Для зависимостей, которые не являются классом (значение конфига, интерфейс, сторонний клиент), используют **custom providers** с явным токеном и `@Inject()` — это подробно разобрано в документации по fundamentals.
</Inset>

## Почему это важно: тестирование, проводка, связанность, lifecycle

Выгода от «объявляй, не создавай» проявляется в четырёх конкретных вещах.

<Infographic
  svg={diPayoffs}
  label="Дерево-таксономия с корнем «DI: объявляй, что нужно» и четырьмя ветвями-выгодами — тестируемость (переопредели provider моком), единый источник проводки (создание описано один раз в метаданных модуля), слабая связанность (зависишь от того, что provider делает, а не как он собран) и управление lifecycle (контейнер владеет временем жизни синглтона и его хуками)."
  caption="Один сдвиг — объявить зависимости в конструкторе, чтобы их внедрил IoC container — покупает все четыре сразу: тестируемость, централизованную проводку, слабую связанность и управляемый lifecycle."
  hue="var(--d-infra)"
/>

**Тестируемость.** Поскольку контроллер знает лишь, что ему нужно *нечто формы* `UsersService`, тест может подсунуть мок. Тестовый модуль Nest позволяет переопределить provider, чтобы прогнать потребителя с фейком — без настоящей базы, без сети. Тот шов, которого не хватало в хуке, теперь встроен.

```ts
const moduleRef = await Test.createTestingModule({
  controllers: [UsersController],
  providers: [UsersService],
})
  .overrideProvider(UsersService)
  .useValue({ findAll: () => [{ id: 1, name: "Mock" }] }) // подставляем фейк
  .compile();
```

**Единый источник проводки.** Создание описано один раз, в метаданных модуля, а не разбросано по каждому потребителю. Добавить зависимость в `UsersService` — это правка одной строки в его конструкторе; никто из тех, кто *использует* сервис, не меняется.

**Слабая связанность (loose coupling).** Потребители зависят от того, что provider делает, а не от того, как он собран. Это делает сервисы композируемыми и заменяемыми — кэширование, логирование, другая реализация за тем же токеном — без правки мест вызова.

**Управление lifecycle.** Контейнер владеет временем жизни каждого provider'а. По умолчанию provider — синглтон: создаётся один раз и разделяется, что и нужно для stateless-сервиса или пула соединений. Nest также вызывает lifecycle-хуки (`onModuleInit`, `onApplicationShutdown`), чтобы ресурсы запускались и останавливались в порядке зависимостей.

<FlowDiagram
  label="IoC container Nest читает зависимости конструктора, разрешает граф и внедряет экземпляры в контроллер при старте"
  hue="var(--p-rose)"
  perRow={4}
  nodes={[
    { id: "ctrl", label: "Controller", sub: "needs Service" },
    { id: "ioc", label: "IoC container", sub: "@nestjs" },
    { id: "svc", label: "Service", sub: "@Injectable" },
    { id: "dep", label: "Database", sub: "provider" },
  ]}
  edges={[
    { from: "ctrl", to: "ioc", label: "объявляет" },
    { from: "ioc", to: "svc", label: "строит" },
    { from: "svc", to: "dep", label: "нужен" },
    { from: "ioc", to: "ctrl", label: "внедряет" },
  ]}
  caption="Ты объявляешь потребность в конструкторе; контейнер разрешает весь граф и внедряет готовые экземпляры. Твой код никогда не вызывает new для зависимости."
/>

## Когда брать Nest — и когда это перебор

Структура Nest — это рычаг на большой, долгоживущей кодовой базе и чистый оверхед на крошечной. Решение — про размер, срок жизни и команду, а не про моду.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Аспект</th>
        <th class="border border-border px-3 py-2 text-left">Голый Express / Fastify</th>
        <th class="border border-border px-3 py-2 text-left">NestJS</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">Структура</td>
        <td class="border border-border px-3 py-2">Изобретаешь сам; расходится по командам</td>
        <td class="border border-border px-3 py-2">Модули/контроллеры/провайдеры, навязаны</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Dependency injection</td>
        <td class="border border-border px-3 py-2">Ручной <code>new</code> или самописный контейнер</td>
        <td class="border border-border px-3 py-2">Встроенный IoC container</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Шов для тестов</td>
        <td class="border border-border px-3 py-2">Самописные моки, настоящие зависимости лезут внутрь</td>
        <td class="border border-border px-3 py-2"><code>overrideProvider</code> подменяет фейки</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Boilerplate</td>
        <td class="border border-border px-3 py-2">Минимум на старте</td>
        <td class="border border-border px-3 py-2">Больше заранее (декораторы, модули)</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Кривая обучения</td>
        <td class="border border-border px-3 py-2">Низкая</td>
        <td class="border border-border px-3 py-2">Выше (DI, декораторы, соглашения)</td>
      </tr>
    </tbody>
  </table>
</div>

Структурированный TypeScript-бэкенд, команда, которой нужны общие соглашения, или сервис, который ты будешь поддерживать годами — всё это многократно окупает первоначальную церемонию. Однофайловый скрипт, одна serverless-функция или одноразовый прототип — нет: декораторы и проводка модулей там чистый налог, а голый Express-обработчик или обычная функция выкатятся быстрее. Перед выбором спроси себя: будет ли эта кодовая база понятна другому инженеру через два года? Если да — соглашения Nest отобьют затраты с лихвой.

<TradeoffMatrix
  id="nest-why-tradeoff-1"
  lessonSlug="01-why-nest"
  lang="ru"
  prompt="Команда начинает долгоживущий внутренний API на TypeScript: много эндпоинтов, несколько общих сервисов (auth, db, mailer), 5 инженеров, проект на годы. Выбери основу."
  options={[
    { name: "NestJS с модулями, контроллерами и провайдерами через его DI-контейнер", summary: "Структура + DI встроены.", correct: true, justification: "Это ровно та форма, под которую сделан Nest: большой, долгоживущий, мульти-инженерный TypeScript-бэкенд с общими сервисами. Навязанная структура module/controller/provider даёт всем одну карту кодовой базы, а IoC container делает общие сервисы (auth, db, mailer) внедряемыми и мокабельными, так что тесты не тянут настоящую инфраструктуру. Первоначальная церемония декораторов и проводки модулей окупается многократно за жизнь сервиса." },
    { name: "Голый Express, где каждый обработчик вызывает new для нужных ему сервисов", summary: "Минимум, без DI.", justification: "Годится для маленького или короткоживущего сервиса, но на таком размере воспроизводит клубок из хука: сервисы создаются через new по всей кодовой базе, нет единого места сменить проводку и нет шва подсунуть фейк в тестах без поднятия настоящих зависимостей. Пятеро инженеров каждый изобретут свою структуру. Низкая стартовая цена возвращается с процентами в поддержке." },
    { name: "Голый Express плюс самописный DI-контейнер, который команда поддерживает сама", summary: "DIY IoC.", justification: "Ты верно определил, что не хватает именно DI — но переписывать IoC container, управление lifecycle и механизм переопределения для тестов значит изобретать ровно то колесо, что Nest уже поставляет, закалённое и задокументированное. Для пары эндпоинтов ещё можно защитить; для многолетнего общего API это поддержка, на которую ты подписался вместо использования фреймворка вокруг неё." },
    { name: "Без фреймворка: один файл со всеми маршрутами и глобальным объектом сервисов", summary: "Один большой файл.", justification: "Это верный выбор для одноразового прототипа или крошечного скрипта, но не для мульти-инженерного API на годы. Глобальный объект сервисов — это анти-паттерн ручной проводки в масштабе: всё связано с глобалами, ничто не тестируется независимо, а файл становится ненавигируемым. Оставь это для случаев, где Nest действительно перебор." },
  ]}
/>

<Quiz
  id="nest-why-quiz-1"
  lessonSlug="01-why-nest"
  lang="ru"
  question="В Nest у контроллера есть constructor(private readonly users: UsersService), и UsersService указан в providers модуля. Откуда берётся экземпляр UsersService?"
  choices={[
    { label: "IoC container Nest читает тип в конструкторе, создаёт (или переиспользует) UsersService и внедряет его, когда строит контроллер", correct: true },
    { label: "Контроллер вызывает new UsersService() в теле своего конструктора", misconception: "Это была бы ручная проводка — то, что DI как раз убирает. С DI контроллер лишь объявляет зависимость как параметр; он никогда её не создаёт. Экземпляр поставляет контейнер." },
    { label: "Nest по умолчанию создаёт свежий UsersService на каждый запрос", misconception: "По умолчанию провайдеры — синглтоны: создаются один раз и разделяются на всё приложение. Экземпляры на запрос (request scope) существуют, но включаются явно, а не по умолчанию." },
    { label: "Нужно вызвать глобальный реестр вроде container.get(UsersService) внутри метода", misconception: "Это паттерн service locator, которого DI в Nest намеренно избегает. Ты объявляешь зависимость в конструкторе, и контейнер её внедряет; ты не тянешь её из глобала в момент вызова." },
  ]}
/>

<Quiz
  id="nest-why-quiz-2"
  lessonSlug="01-why-nest"
  lang="ru"
  question="В чём главная выгода для тестируемости от объявления зависимости в конструкторе вместо new внутри класса?"
  choices={[
    { label: "Тест может внедрить мок-реализацию вместо настоящей, и потребитель работает без настоящих баз и сети", correct: true },
    { label: "Constructor injection делает код быстрее в рантайме", misconception: "DI — про структуру и проводку, а не про скорость; разрешение графа происходит на старте. Выгода — в шве, который он создаёт, а не в производительности." },
    { label: "Это снимает необходимость писать тесты для сервиса вообще", misconception: "DI облегчает написание тестов, а не делает их ненужными. Сам сервис ты по-прежнему тестируешь; DI — то, что позволяет тестировать его потребителей изолированно, подменяя фейк." },
    { label: "Это позволяет классу надёжнее создавать собственные зависимости", misconception: "Весь смысл в том, что класс больше вообще не создаёт свои зависимости — это делает что-то другое. Именно вынесенное наружу создание и делает возможной подмену (а значит, и мокинг)." },
  ]}
/>

</Explanation>

<KeyTakeaway>NestJS даёт Node-бэкенду хребет: модули, контроллеры, провайдеры и DI. Объяви, что нужно классу, в конструкторе; IoC container внедрит это — поэтому проводка централизована, код слабо связан, а тесты подменяют моки.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="nest-why-retrieval"
  lang="ru"
  questions={[
    { q: "Объясни ментальную модель DI в Nest и почему она лучше, чем new зависимостей внутри класса.", a: "Ментальная модель такая: класс объявляет то, что ему нужно, как параметры конструктора и никогда не создаёт это сам — это предоставляет что-то другое. В Nest это что-то — IoC container (контейнер инверсии управления). На старте приложения контейнер читает типы конструкторов каждого класса, вычисляет граф зависимостей, создаёт каждый provider (по умолчанию синглтон) и внедряет экземпляры. «Инверсия управления» называет этот переворот: созданием владеет фреймворк, а не твой код. По сравнению с new зависимостей внутри класса это выигрывает по четырём фронтам. Тестируемость: поскольку потребитель знает лишь, что ему нужно нечто формы UsersService, тест может переопределить provider моком и работать без настоящей базы и сети. Единый источник проводки: то, как всё строится, описано один раз в метаданных модуля, а не разбросано по каждому потребителю, поэтому добавление зависимости — правка одной строки конструктора. Слабая связанность: потребители зависят от того, что provider делает, а не как он собран, так что можно менять реализации за тем же токеном. Lifecycle: контейнер владеет временем жизни каждого provider'а и его хуками. Жёстко зашитый new приваривает класс к конкретному соавтору и не оставляет шва, чтобы что-то подменить." },
    { q: "Когда NestJS — верный выбор, а когда перебор?", a: "Структура Nest — это рычаг, масштабирующийся с размером кодовой базы, сроком жизни и командой. Он окупается на большом, долгоживущем TypeScript-бэкенде с множеством эндпоинтов и общими сервисами (auth, db, mailer) или на любом сервисе, который команда поддерживает годами: навязанная раскладка module/controller/provider даёт всем одну карту, а встроенный IoC container делает общие сервисы внедряемыми и мокабельными. Цена — первоначальная церемония: декораторы, модули, больше boilerplate и более крутая кривая обучения (DI, декораторы, соглашения), чем у голого Express или Fastify. На крошечном или короткоживущем проекте эта цена — чистый налог: однофайловый скрипт, одна serverless-функция или одноразовый прототип выкатятся быстрее как обычный Express-обработчик или функция. Решение — про масштаб и срок жизни, а не про моду: бери Nest, когда структура и тестируемость нужны надолго; пропускай, когда нет." },
  ]}
/>

<Recap lang="ru">
Голые Express и Fastify маршрутизируют запросы — и на этом всё, что оставляет растущий бэкенд без общей структуры и без шва для тестирования: бизнес-логика приклеена к глобалам, а зависимости создаются через new где придётся. NestJS встаёт поверх Node (по умолчанию Express, опционально Fastify) и добавляет эту структуру через модули, контроллеры и провайдеры, связанные dependency injection. Ментальная модель DI — её сердце: класс объявляет, что ему нужно, в конструкторе и никогда это не создаёт; IoC container читает типы конструктора, разрешает граф зависимостей, создаёт каждый provider один раз и внедряет экземпляры — инверсия управления означает, что созданием владеет фреймворк, а не твой код. Этот один сдвиг покупает тестируемость (переопредели provider моком, без настоящей базы), единое центральное место для проводки, слабую связанность между потребителями и реализациями и управляемый контейнером lifecycle. Бери Nest на больших, долгоживущих, командных TypeScript-бэкендах, где такая структура окупает свою первоначальную церемонию, и пропускай для крошечных скриптов и одноразовых функций, где обычный обработчик быстрее. Теперь, когда ты встретишь конструктор вида `constructor(private readonly users: UsersService)`, ты сразу увидишь суть: класс объявляет потребность, IoC container её удовлетворяет — никакого `new`, никаких глобалов, никакой проводки, разбросанной по всей кодовой базе.
</Recap>
