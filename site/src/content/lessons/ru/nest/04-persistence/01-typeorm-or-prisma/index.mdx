---
concepts:
- typeorm-module
- prisma-service
- repository-injection
- entities
- datasource
deepensInto: []
estMin: 16
lang: ru
lessonType: topic
level: middle
order: 1
prereqs: []
slug: "01-typeorm-or-prisma"
sources:
- https://docs.nestjs.com/techniques/database
- https://docs.nestjs.com/recipes/prisma
spiral: []
status: ready
summary: 'TypeORM и Prisma оба подключают ORM в Nest, но источник истины для типобезопасности разный: классы @Entity с декораторами, регистрируемые через TypeOrmModule, либо schema.prisma, генерирующая клиент, который ты оборачиваешь в PrismaService.'
title: 'TypeORM или Prisma: интеграция ORM'
track: nest
unit: 04-persistence
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
Новому сервису нужна база, и первый PR в репозитории решает за следующие два года. Один ревьюер хочет классы `@Entity()` — «модель и есть TypeScript, ей там и место». Другой хочет `schema.prisma` — «схема и есть источник истины, клиент генерируется, дрифта не будет». Оба варианта будут работать. Но это не спор о вкусах: два пути кладут источник истины для типобезопасности в разные места, отдают миграции разным инструментам и интегрируются с dependency injection Nest принципиально по-разному. Один даёт тебе DI-нативные repository; другой — один сгенерированный клиент, который ты должен обернуть сам. Выбирай по ограничению, а не по настроению.
</Hook>

<Crux>TypeORM: истина — entity-классы, repository инжектятся. Prisma: истина — schema.prisma, ты оборачиваешь сгенерированный клиент.</Crux>

<Explanation>

## TypeORM: истина — это entities, repository инжектятся

В пути TypeORM ты один раз настраиваешь `DataSource` в корне, затем регистрируешь entities по фиче-модулям. Подключение асинхронно, потому что его значения приходят из `ConfigService`, — поэтому берёшь `TypeOrmModule.forRootAsync` с `useFactory`, а не статический `forRoot`:

```ts
import { Module } from '@nestjs/common';
import { TypeOrmModule } from '@nestjs/typeorm';
import { ConfigModule, ConfigService } from '@nestjs/config';
import { User } from './user.entity';

@Module({
  imports: [
    TypeOrmModule.forRootAsync({
      imports: [ConfigModule],
      inject: [ConfigService],
      useFactory: (config: ConfigService) => ({
        type: 'postgres',
        host: config.get('DB_HOST'),
        port: config.get<number>('DB_PORT'),
        username: config.get('DB_USER'),
        password: config.get('DB_PASS'),
        database: config.get('DB_NAME'),
        entities: [User],
        synchronize: false, // НИКОГДА не true в production
      }),
    }),
  ],
})
export class AppModule {}
```

Entity — это обычный класс, помеченный `@Entity()`, и его колонки — это схема:

```ts
import { Entity, PrimaryGeneratedColumn, Column } from 'typeorm';

@Entity()
export class User {
  @PrimaryGeneratedColumn()
  id: number;

  @Column({ unique: true })
  email: string;

  @Column({ default: true })
  isActive: boolean;
}
```

Метаданные этого декоратора *и есть* источник истины для типобезопасности — TypeScript-класс задаёт и рантайм-маппинг колонок, и генератор миграций. Чтобы использовать entity в фиче-модуле, ты регистрируешь его через `forFeature`, который провайдит для него repository в DI-контейнер, и инжектишь этот repository через `@InjectRepository`:

```ts
import { Module } from '@nestjs/common';
import { TypeOrmModule } from '@nestjs/typeorm';
import { User } from './user.entity';
import { UsersService } from './users.service';

@Module({
  imports: [TypeOrmModule.forFeature([User])], // провайдит Repository<User>
  providers: [UsersService],
})
export class UsersModule {}
```

```ts
import { Injectable } from '@nestjs/common';
import { InjectRepository } from '@nestjs/typeorm';
import { Repository } from 'typeorm';
import { User } from './user.entity';

@Injectable()
export class UsersService {
  constructor(
    @InjectRepository(User)
    private readonly users: Repository<User>,
  ) {}

  findActive(): Promise<User[]> {
    return this.users.find({ where: { isActive: true } });
  }
}
```

`Repository&lt;User>` — это граница персистентности: твой сервис говорит с ней, а не с сырым SQL или глобальным клиентом. Это DI-нативно — repository является обычным провайдером Nest, который мокается в тестах подменой провайдера `getRepositoryToken(User)`.

## Prisma: истина — это схема, клиент ты оборачиваешь сам

Prisma переворачивает источник истины. Один файл `schema.prisma` объявляет модели; `prisma generate` читает его и выдаёт полностью типизированный клиент. Ты не пишешь entity-классы — схема *генерирует* типы:

```prisma
// schema.prisma
generator client {
  provider = "prisma-client-js"
}

model User {
  id       Int     @id @default(autoincrement())
  email    String  @unique
  isActive Boolean @default(true)
}
```

У Nest нет первоклассного Prisma-провайдера для DI, поэтому ты предоставляешь его сам: injectable `PrismaService`, который наследует сгенерированный `PrismaClient` и открывает подключение в `onModuleInit`. Это та интеграционная обвязка, которую ты пишешь руками:

```ts
import { Injectable, OnModuleInit } from '@nestjs/common';
import { PrismaClient } from '@prisma/client';

@Injectable()
export class PrismaService extends PrismaClient implements OnModuleInit {
  async onModuleInit() {
    await this.$connect();
  }
}
```

Предоставь его один раз, затем инжекти единственный сервис — нет per-entity `@InjectRepository`, только типизированные аксессоры моделей на клиенте:

```ts
import { Injectable } from '@nestjs/common';
import { PrismaService } from './prisma.service';
import { User } from '@prisma/client';

@Injectable()
export class UsersService {
  constructor(private readonly prisma: PrismaService) {}

  findActive(): Promise<User[]> {
    return this.prisma.user.findMany({ where: { isActive: true } });
  }
}
```

Обрати внимание, чего нет: ни `forFeature`, ни токена repository, ни декоратора на модели. Вся поверхность API — это `this.prisma.&lt;model>.&lt;operation>`, а тип `User` приходит из `@prisma/client`, регенерируясь каждый раз при изменении схемы.

<FlowDiagram
  label="Как запрос доходит до Postgres в каждом пути ORM: фиче-модуль (UsersModule) зависит от интеграционного модуля ORM, который владеет объектом подключения, который говорит с Postgres. TypeORM: UsersModule регистрирует entity через TypeOrmModule.forFeature, корневой TypeOrmModule владеет DataSource, а DataSource подключается к Postgres. Prisma: UsersModule инжектит PrismaService, PrismaService наследует PrismaClient и владеет подключением, а PrismaClient подключается к Postgres."
  hue="var(--d-systems)"
  perRow={4}
  nodes={[
    { id: "fm", label: "UsersModule", sub: "feature" },
    { id: "om", label: "ORM module", sub: "forFeature / provide" },
    { id: "conn", label: "DataSource", sub: "/ PrismaClient" },
    { id: "pg", label: "Postgres", sub: "wire protocol" },
  ]}
  edges={[
    { from: "fm", to: "om", label: "imports / injects" },
    { from: "om", to: "conn", label: "owns connection" },
    { from: "conn", to: "pg", label: "SQL" },
  ]}
  caption="Форма одна, объект подключения разный: DataSource у TypeORM даёт тебе инжектируемые repository; PrismaClient у Prisma — один сгенерированный клиент, который ты оборачиваешь в PrismaService."
/>

## Настоящее решение: где живёт истина

Когда оцениваешь эти варианты в реальном PR, аргументы о вкусе быстро перестают иметь значение — ограничения не перестают. Ни один не верен универсально — они различаются по трём осям, которые сеньор взвешивает против заявленного ограничения. Источник истины для типобезопасности — первая: entity-классы с декораторами TypeScript (TypeORM) против `.prisma`-схемы, которая генерирует клиент (Prisma). Миграции вытекают из этого: TypeORM генерирует и прогоняет файлы миграций, выведенные из твоих entities; Prisma Migrate сравнивает схему и выдаёт SQL-миграции. И эргономика DI расходится: TypeORM даёт per-entity repository как первоклассные провайдеры Nest; Prisma даёт один клиент, который ты оборачиваешь и инжектишь повсюду.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Ось</th>
        <th class="border border-border px-3 py-2 text-left">TypeORM</th>
        <th class="border border-border px-3 py-2 text-left">Prisma</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2"><strong>Источник истины</strong></td>
        <td class="border border-border px-3 py-2">TS-классы с декоратором <code>@Entity()</code></td>
        <td class="border border-border px-3 py-2"><code>schema.prisma</code> генерирует клиент</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2"><strong>Миграции</strong></td>
        <td class="border border-border px-3 py-2">Генерация + прогон файлов миграций из entities</td>
        <td class="border border-border px-3 py-2">Prisma Migrate сравнивает схему &rarr; SQL</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2"><strong>Эргономика DI</strong></td>
        <td class="border border-border px-3 py-2">Per-entity <code>Repository&lt;T&gt;</code>, инжектится</td>
        <td class="border border-border px-3 py-2">Один <code>PrismaService</code>, ты пишешь + инжектишь</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2"><strong>Интеграция с Nest</strong></td>
        <td class="border border-border px-3 py-2">Первоклассный модуль <code>@nestjs/typeorm</code></td>
        <td class="border border-border px-3 py-2">Нет первоклассного DI &mdash; провайдишь сам</td>
      </tr>
    </tbody>
  </table>
</div>

<Inset kind="why" lang="ru">
Почему `synchronize: true` — тот единственный переключатель, который нельзя трогать в production? Он велит TypeORM менять живую схему при каждом старте под твои entities — молча, без файла миграции и без ревью. Переименуй свойство — и старая колонка дропается вместе с её данными; изменение уезжает в момент рестарта процесса, без записи о том, что произошло. Это удобная игрушка для одноразового локального наброска и инцидент с потерей данных, ждущий в staging или prod. Дисциплинированный путь — `synchronize: false` плюс сгенерированные файлы миграций, которые ты ревьюишь и прогоняешь осознанно.
</Inset>

<TradeoffMatrix
  id="nest-04-persistence-01-typeorm-or-prisma-tradeoff-1"
  lessonSlug="01-typeorm-or-prisma"
  lang="ru"
  prompt="Твоя команда хочет, чтобы сама схема базы была единственным источником истины, с полностью сгенерированными TypeScript-типами, которые не могут разойтись со схемой, и готова написать одну обёртку-сервис. Какой слой персистентности подходит под это ограничение?"
  options={[
    { name: "Prisma: schema.prisma — источник истины, prisma generate выдаёт типизированный клиент, обёрнутый в injectable PrismaService", summary: "Схема-как-истина -> Prisma.", correct: true, justification: "Заявленное ограничение — схема как источник истины плюс сгенерированные, не дрейфующие типы — это ровно модель Prisma. Одна schema.prisma объявляет каждую модель; prisma generate выдаёт полностью типизированный клиент, так что типы не могут разойтись со схемой, а Prisma Migrate выводит SQL-миграции из того же файла. Цена, которую команда уже приняла, — написать один PrismaService, наследующий PrismaClient и вызывающий $connect в onModuleInit, — единственная нужная обвязка, ведь у Nest нет первоклассного Prisma DI. При ДАННОМ ограничении подходит Prisma. Это не был бы автоматический ответ, если бы команда вместо этого хотела DI-нативные per-entity repository." },
    { name: "TypeORM с классами @Entity() и инжектируемыми repository", summary: "Здесь истина — entities.", justification: "TypeORM — сильный выбор, но он переворачивает заявленное ограничение: TypeScript-классы @Entity() являются источником истины, а схема выводится из них, а не наоборот. Команда явно хотела, чтобы схема была канонической со сгенерированными типами — это форма Prisma. TypeORM блистает, когда нужны DI-нативные провайдеры Repository<T> на каждую entity — другое ограничение, чем данное." },
    { name: "Сырой query builder / драйвер pg с рукописным SQL", summary: "Нет сгенерированных типов.", justification: "Спуск к query builder или сырому pg даёт максимум контроля и ноль ORM-оверхеда, но полностью отбрасывает ограничение: нет единого источника истины-схемы и нет сгенерированных типов — типы поддерживаешь руками, и они дрейфуют в момент изменения колонки. Разумно для горячего пути или крошечного сервиса, неверно для команды, которая явно попросила схема-driven сгенерированную безопасность." },
    { name: "TypeORM с synchronize: true, чтобы схема следовала за entities автоматически", summary: "Никогда в production.", justification: "Это «автоматизирует» синхронизацию схемы, позволяя TypeORM менять живую базу при каждом старте под entities — молча и деструктивно, дропая колонки и их данные без миграции и ревью. Это ждущий инцидент потери данных и явно production-антипаттерн этого урока, а не стратегия источника истины." },
  ]}
/>

<Quiz
  id="nest-04-persistence-01-typeorm-or-prisma-quiz-1"
  lessonSlug="01-typeorm-or-prisma"
  lang="ru"
  question="В пути TypeORM как сервис фиче-модуля получает Repository<User>, чтобы делать запросы?"
  choices={[
    { label: "Модуль импортирует TypeOrmModule.forFeature([User]), чтобы провайдить repository, а сервис инжектит его через @InjectRepository(User) repo: Repository<User>", correct: true },
    { label: "Сервис напрямую наследует Repository<User>, и Nest подключает его на старте", misconception: "Repository не наследуют — TypeORM провайдит Repository<User> в DI-контейнер, когда ты регистрируешь entity через forFeature, и ты инжектишь его через @InjectRepository. Наследование клиента — это паттерн Prisma (PrismaService extends PrismaClient), а не TypeORM." },
    { label: "Корневой TypeOrmModule.forRoot делает каждый repository глобально инжектируемым, регистрация на модуль не нужна", misconception: "forRoot/forRootAsync настраивает DataSource (подключение), но каждый фиче-модуль всё равно должен вызвать forFeature([Entity]), чтобы repository этой entity был провайжен в его область. Без forFeature у @InjectRepository нечего резолвить." },
    { label: "Ты вызываешь prisma.user.findMany() на инжектированном клиенте", misconception: "Это путь Prisma — один сгенерированный клиент с аксессорами моделей. Граница TypeORM — это инжектированный Repository<User>, регистрируемый на модуль через forFeature, а не глобальный клиент." },
  ]}
/>

<Quiz
  id="nest-04-persistence-01-typeorm-or-prisma-quiz-2"
  lessonSlug="01-typeorm-or-prisma"
  lang="ru"
  question="Почему PrismaService должен реализовывать OnModuleInit и вызывать await this.$connect()?"
  choices={[
    { label: "У Nest нет первоклассного Prisma DI, поэтому ты сам оборачиваешь PrismaClient; onModuleInit — это lifecycle-хук, где ты открываешь подключение, когда модуль готов", correct: true },
    { label: "Потому что @InjectRepository нужен токен подключения, чтобы зарезолвить Prisma-repository", misconception: "У Prisma нет @InjectRepository и нет per-entity repository — это TypeORM. PrismaService оборачивает один сгенерированный клиент; $connect в onModuleInit открывает подключение этого клиента и никак не связан с токеном repository." },
    { label: "Потому что Prisma не может сгенерировать типы, пока $connect не выполнится на старте", misconception: "Генерация типов — это шаг времени сборки (prisma generate), который выполняется до старта приложения и независим от $connect, который является рантайм-вызовом, открывающим подключение к базе." },
    { label: "Это не нужно — Nest сам провайдит PrismaClient и подключает его за тебя", misconception: "У Nest нет первоклассной поддержки Prisma, поэтому он не провайдит и не подключает клиент. Ты пишешь PrismaService и открываешь подключение в onModuleInit сам; в этом и весь смысл существования обёртки." },
  ]}
/>

</Explanation>

<KeyTakeaway>TypeORM: классы @Entity — истина; forRootAsync DataSource, forFeature на модуль, инжекти Repository. Prisma: schema.prisma генерирует клиент, который ты оборачиваешь в PrismaService. Никогда synchronize: true в prod.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="nest-01-typeorm-or-prisma-retrieval"
  lang="ru"
  questions={[
    { q: "Пройди всю обвязку TypeORM от подключения до запроса: что настраивает DataSource, что регистрирует entity на модуль и как сервис получает свой repository?", a: "Подключение — это DataSource, настраиваемый один раз в корне через TypeOrmModule.forRootAsync({ imports, inject, useFactory }) — асинхронно, потому что значения приходят из ConfigService. Фабрика возвращает type драйвера, host/port/креды, массив entities и synchronize: false. Каждая entity — это обычный класс с декоратором @Entity(), чьи метаданные @Column/@PrimaryGeneratedColumn — это схема и источник истины для типобезопасности. Чтобы использовать entity в фиче-модуле, ты импортируешь TypeOrmModule.forFeature([User]), что провайдит Repository<User> в DI-контейнер этого модуля. Сервис затем инжектит его через @InjectRepository(User) private repo: Repository<User> и вызывает repo.find(...) — repository является границей персистентности, обычным провайдером Nest, который можно подменить в тестах через getRepositoryToken(User)." },
    { q: "Пройди всю обвязку Prisma: откуда берутся типы, почему ты должен написать PrismaService и как сервис делает запрос?", a: "Источник истины — schema.prisma; запуск prisma generate читает его и выдаёт полностью типизированный @prisma/client, так что TypeScript-типы генерируются из схемы и не могут с ней разойтись. У Nest нет первоклассного Prisma-провайдера для DI, поэтому ты пишешь его сам: @Injectable() PrismaService, который extends PrismaClient implements OnModuleInit, и в onModuleInit вызывает await this.$connect(), чтобы открыть подключение, когда модуль готов. Ты предоставляешь его один раз и инжектишь единственный PrismaService — нет @InjectRepository и нет forFeature. Сервис делает запрос через аксессоры моделей клиента, например this.prisma.user.findMany({ where: { isActive: true } }), и возвращаемый тип User тоже приходит из @prisma/client. Миграциями занимается Prisma Migrate, который сравнивает схему и выдаёт SQL." },
  ]}
/>

<Recap lang="ru">
И TypeORM, и Prisma интегрируют ORM в Nest, но кладут источник истины для типобезопасности в разные дома, что каскадом влияет на миграции и DI. В пути TypeORM истина — классы @Entity() с декораторами: ты настраиваешь DataSource один раз через TypeOrmModule.forRootAsync с useFactory, питаемой ConfigService (с synchronize: false), регистрируешь каждую entity на фиче-модуль через TypeOrmModule.forFeature([User]), чтобы провайдить её repository, и инжектишь этот repository через @InjectRepository(User) repo: Repository&lt;User> — repository является DI-нативной границей персистентности, а файлы миграций генерируются из entities. В пути Prisma истина — schema.prisma: prisma generate выдаёт полностью типизированный клиент, и поскольку у Nest нет первоклассного Prisma DI, ты сам оборачиваешь его в PrismaService, который extends PrismaClient implements OnModuleInit и вызывает await this.$connect() в onModuleInit; ты инжектишь этот один сервис и вызываешь prisma.user.findMany(), а изменения схемы держит Prisma Migrate — нигде нет @InjectRepository. Решение driven ограничением: схема-как-источник-истины со сгенерированными, не дрейфующими типами указывает на Prisma; DI-нативные per-entity repository указывают на TypeORM; сырой SQL меняет и то, и другое на контроль. И в обоих случаях synchronize: true — production-антипаттерн: он молча переписывает живую схему и дропает колонки, — поэтому ты всегда используешь отревьюенные миграции вместо него. Теперь, когда увидишь в PR synchronize: true «только для staging», знай ровно, на что возражать и почему.
</Recap>
