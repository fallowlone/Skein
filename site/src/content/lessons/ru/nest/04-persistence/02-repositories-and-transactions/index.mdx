---
concepts:
- repository-pattern
- transactions
- unit-of-work
- query-runner
- isolation-levels
deepensInto: []
estMin: 18
lang: ru
lessonType: topic
level: senior
order: 2
prereqs: []
slug: "02-repositories-and-transactions"
sources:
- https://typeorm.io/transactions
- https://docs.nestjs.com/techniques/database
- https://typeorm.io/data-source-api
- https://typeorm.io/working-with-entity-manager
spiral: []
status: ready
summary: 'Repository привязан к дефолтному manager — вызов инжектированного repo внутри dataSource.transaction НЕ входит в транзакцию. Гони каждую запись через ОДИН транзакционный EntityManager (manager.getRepository), иначе частичный коммит оставит осиротевшие строки.'
title: 'Репозитории, транзакции и unit of work'
track: nest
unit: "04-persistence"
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import SequenceDiagram from "~/components/diagram/SequenceDiagram.astro";
import Infographic from "~/components/diagram/Infographic.astro";
import managerBinding from "./infographics/manager-binding.svg?raw";

<Hook>
Канал инцидентов вспыхивает в два часа ночи: клиент перевёл $400 между своими счетами, счёт-источник списан, а счёт-получатель так и не пополнен. Деньги пропали — не украдены, просто растворились в недописанной записи. Сервис `transferFunds` обёрнут в `dataSource.transaction(async (manager) => { ... })`, так что автор клялся, что это атомарно. Оно компилируется, прошло ревью, месяцами работало без проблем. Баг невидим в каждом тесте, потому что тесты никогда не выбрасывают между двумя записями. Сервис инжектит `@InjectRepository(Account)` и вызывает `this.accountRepo.save(...)` внутри колбэка — а этот repo привязан к *дефолтному* manager, а не к транзакции. Списание закоммитилось на своём соединении; пополнение выбросило; rollback откатил ничего из того, что трогало списание. Это самая дорогая ошибка из пяти строк во всём слое персистентности.
</Hook>

<Crux>Инжектированный repo использует дефолтный manager — он не входит в dataSource.transaction. Гони каждую запись через manager из колбэка.</Crux>

<Explanation>

## Repository — это твоя граница персистентности

Repository — это шов между доменной логикой и базой. В Nest с TypeORM ты обычно получаешь его инжекцией — `@InjectRepository(Account)` отдаёт тебе `Repository&lt;Account>`, провязанный регистрацией `TypeOrmModule.forFeature([Account])`. Этот repository несёт методы вроде `find`, `save` и `remove`, и, что критично, он привязан к конкретному `EntityManager`: к **дефолтному** manager у `DataSource`, который выполняет каждый вызов на своём авто-коммитящем соединении.

```ts
import { Injectable } from '@nestjs/common';
import { InjectRepository } from '@nestjs/typeorm';
import { Repository } from 'typeorm';
import { Account } from './account.entity';

@Injectable()
export class AccountsService {
  constructor(
    @InjectRepository(Account)
    private readonly accountRepo: Repository<Account>,
  ) {}

  // Каждый вызов здесь — СВОЯ крошечная транзакция — норм для одной записи.
  async getBalance(id: string): Promise<number> {
    const acc = await this.accountRepo.findOneByOrFail({ id });
    return acc.balance;
  }
}
```

Для одного чтения или одной записи это поведение «авто-коммит на вызов» — ровно то, что нужно. Беда начинается в тот момент, когда одной *бизнес*-операции нужно, чтобы **две или больше записи прошли или упали вместе** — перевод, заказ с позициями, регистрация, которая пишет пользователя и строку аудита. Это транзакция, и именно в транзакции дефолтная привязка repository превращается в ловушку.

## Два способа выполнить транзакцию — и оба разделяют один manager

TypeORM даёт два API транзакций. Первый — **форма-колбэк**, `dataSource.transaction`: она открывает транзакцию, отдаёт тебе транзакционный `EntityManager`, коммитит, если колбэк зарезолвился, и откатывает, если он выбросил. Ты никогда не пишешь `commit` или `rollback` руками.

```ts
// ФОРМА-КОЛБЭК — авто-commit при resolve, авто-rollback при throw.
await this.dataSource.transaction(async (manager) => {
  await manager.getRepository(Account).decrement({ id: fromId }, 'balance', amount);
  await manager.getRepository(Account).increment({ id: toId }, 'balance', amount);
  // если любая строка выбросит, откатится ВСЁ
});
```

Второй — **ручной `QueryRunner`**, когда нужен явный контроль — savepoint'ы, выбранный уровень изоляции или переплетённая логика. Жизненным циклом владеешь ты, и `release()` ОБЯЗАН жить в `finally`, иначе ты утечёшь соединение из пула:

```ts
// РУЧНАЯ ФОРМА — commit/rollback/release на тебе.
const queryRunner = this.dataSource.createQueryRunner();
await queryRunner.connect();
await queryRunner.startTransaction('SERIALIZABLE'); // опциональный уровень изоляции
try {
  const repo = queryRunner.manager.getRepository(Account);
  await repo.decrement({ id: fromId }, 'balance', amount);
  await repo.increment({ id: toId }, 'balance', amount);
  await queryRunner.commitTransaction();
} catch (err) {
  await queryRunner.rollbackTransaction();
  throw err;
} finally {
  await queryRunner.release(); // ВСЕГДА — даже при успехе
}
```

Заметь, что общего у обеих форм: каждая запись идёт через repository, полученный **из собственного manager транзакции** — `manager.getRepository(...)` в колбэке, `queryRunner.manager.getRepository(...)` в ручной форме. Этот общий manager и *есть* unit of work.

<SequenceDiagram
  label="Транзакция unit of work в TypeORM: сервис просит DataSource начать транзакцию, которая отдаёт один транзакционный EntityManager; запись A и запись B обе идут через этот же manager; если обе успешны, транзакция коммитится как один атомарный блок, но если запись B выбросит, транзакция откатит и запись A тоже, так что база никогда не остаётся наполовину обновлённой."
  hue="var(--d-systems)"
  actors={["Service", "Manager", "DB"]}
  messages={[
    { from: "Service", to: "Manager", label: "1. begin tx (one manager)" },
    { from: "Manager", to: "DB", label: "2. write A (debit)" },
    { from: "Manager", to: "DB", label: "3. write B (credit)" },
    { from: "Manager", to: "DB", label: "4a. commit (both ok)" },
    { from: "DB", to: "Service", label: "4b. rollback A on B's throw" },
  ]}
  caption="Одна транзакция = один manager = один атомарный блок. Записи A и B разделяют manager, так что throw на B откатывает A. Инжектированный repo обошёл бы manager целиком и закоммитил A на своём соединении — тихий баг частичного коммита."
/>

## Unit of work (единица работы) и ловушка, которая его ломает

Если запомнишь из этого урока одну фразу, пусть это будет: обернуть код в колбэк транзакции — не то же самое, что поместить записи *внутрь* этой транзакции. **Unit of work** — единица работы — это идея, что одна атомарная бизнес-операция отображается ровно в одну транзакцию, и *все* записи внутри неё идут через *один и тот же* транзакционный `EntityManager`. Несколько репозиториев, один manager. Нарушь это правило — и атомарность тихо исчезнет.

Вот ловушка, тот самый `transferFunds` из инцидента — сделанный **НЕВЕРНО**:

```ts
// НЕВЕРНО — инжектированный repo игнорирует manager транзакции.
async transferFunds(fromId: string, toId: string, amount: number) {
  await this.dataSource.transaction(async (manager) => {
    // this.accountRepo привязан к ДЕФОЛТНОМУ manager, НЕ к `manager`.
    await this.accountRepo.decrement({ id: fromId }, 'balance', amount); // авто-коммит!
    if (amount > 1000) throw new Error('needs approval');                // слишком поздно
    await this.accountRepo.increment({ id: toId }, 'balance', amount);
  });
}
```

`decrement` выполняется на собственном соединении инжектированного repo и коммитится немедленно. Когда следующая строка выбрасывает, `dataSource.transaction` откатывает *свою* транзакцию — которая никогда не содержала списание. Счёт-источник списан, получатель нетронут, а rollback был no-op над пустой транзакцией. Теперь та же операция, сделанная **ВЕРНО**:

```ts
// ВЕРНО — каждая запись идёт через собственный manager транзакции.
async transferFunds(fromId: string, toId: string, amount: number) {
  await this.dataSource.transaction(async (manager) => {
    const accounts = manager.getRepository(Account);            // привязан к ЭТОЙ tx
    await accounts.decrement({ id: fromId }, 'balance', amount);
    if (amount > 1000) throw new Error('needs approval');       // теперь откатит списание
    await accounts.increment({ id: toId }, 'balance', amount);
  }); // коммитит, только если ОБЕ записи прошли
}
```

Если у тебя уже есть инжектированный repository и ты хочешь переиспользовать его транзакционно, `manager.withRepository(this.accountRepo)` перепривязывает именно этот repository (с кастомными методами) к транзакционному manager. Альтернатива для команд, которым противно протаскивать `manager` через каждый метод, — библиотека транзакционного контекста: `typeorm-transactional`, построенная на `AsyncLocalStorage`, где декоратор `@Transactional()` хранит активный manager в async-контексте, и инжектированные репозитории прозрачно его подхватывают. Это держит код сервиса чистым ценой ещё одной зависимости и пропатченного `DataSource`.

<Infographic
  svg={managerBinding}
  label="Сравнение бок о бок: инжектированный repository (this.accountRepo) привязан к дефолтному manager — каждая запись авто-коммитится на своём соединении, не атомарна на нескольких записях, и throw посреди операции оставляет тихий частичный коммит; manager.getRepository(Account) привязан к manager транзакции, коммитит только когда колбэк зарезолвился, атомарен на всех записях, и throw откатывает каждую запись."
  caption="Тот же колбэк, противоположный исход: атомарность решает привязка. Инжектированный repo коммитит списание в одиночку; manager.getRepository держит обе записи в одном unit of work."
  hue="var(--d-infra)"
/>

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Подход</th>
        <th class="border border-border px-3 py-2 text-left">Кто владеет commit/rollback</th>
        <th class="border border-border px-3 py-2 text-left">Атомарность на нескольких записях?</th>
        <th class="border border-border px-3 py-2 text-left">Контроль уровня изоляции</th>
        <th class="border border-border px-3 py-2 text-left">Главный риск</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2"><strong>Инжектированный repo (без tx)</strong></td>
        <td class="border border-border px-3 py-2">Каждый вызов авто-коммитит</td>
        <td class="border border-border px-3 py-2">Нет — каждая запись своя tx</td>
        <td class="border border-border px-3 py-2">Никакого</td>
        <td class="border border-border px-3 py-2">Тихие частичные коммиты</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2"><strong>dataSource.transaction + manager.getRepository</strong></td>
        <td class="border border-border px-3 py-2">TypeORM (авто при resolve/throw)</td>
        <td class="border border-border px-3 py-2">Да — общий manager</td>
        <td class="border border-border px-3 py-2">Опционально первым аргументом</td>
        <td class="border border-border px-3 py-2">Забыть использовать manager</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2"><strong>Ручной QueryRunner</strong></td>
        <td class="border border-border px-3 py-2">Ты — commit/rollback/release</td>
        <td class="border border-border px-3 py-2">Да — queryRunner.manager</td>
        <td class="border border-border px-3 py-2">Полный (startTransaction, savepoint'ы)</td>
        <td class="border border-border px-3 py-2">Утечка соединения (нет release)</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2"><strong>Библиотека @Transactional()</strong></td>
        <td class="border border-border px-3 py-2">Декоратор (AsyncLocalStorage)</td>
        <td class="border border-border px-3 py-2">Да — инжектированные repo авто-входят</td>
        <td class="border border-border px-3 py-2">Опция декоратора</td>
        <td class="border border-border px-3 py-2">Лишняя зависимость + патч DataSource</td>
      </tr>
    </tbody>
  </table>
</div>

## Уровни изоляции, ретраи и миграции

Дефолтный уровень изоляции в Postgres и MySQL — **READ COMMITTED**: каждый стейтмент видит строки, закоммиченные до его выполнения, что нормально для большинства записей, но допускает non-repeatable reads. Когда операция должна обеспечить *инвариант* по строкам, которые она читает, а затем пишет (нет овердрафта, нет двойного бронирования), поднимай его до **SERIALIZABLE** (или используй явные блокировки строк). Более строгая изоляция жертвует пропускной способностью и может прерывать транзакции, поэтому SERIALIZABLE-транзакция обязана быть **повторяемой**: при serialization failure (Postgres `40001`) или deadlock (`40P01`) лови, делай backoff и переигрывай весь unit of work.

```ts
async transferWithRetry(fromId: string, toId: string, amount: number) {
  for (let attempt = 0; attempt < 3; attempt++) {
    try {
      return await this.dataSource.transaction('SERIALIZABLE', async (manager) => {
        const accounts = manager.getRepository(Account);
        await accounts.decrement({ id: fromId }, 'balance', amount);
        await accounts.increment({ id: toId }, 'balance', amount);
      });
    } catch (err: any) {
      if (err.code === '40001' || err.code === '40P01') continue; // serialization / deadlock -> ретрай
      throw err;
    }
  }
  throw new Error('transfer failed after retries');
}
```

Последняя дисциплина: схема, в которую пишут эти репозитории, принадлежит **миграциям** под контролем версий, сгенерированным через `typeorm migration:generate` и прогоняемым на деплое. Никогда не выкатывай с `synchronize: true` — он диффает сущности против живой схемы на старте и может тихо дропнуть столбцы. Миграции ревьюабельны, упорядочены и обратимы; runtime-sync — это продакшен-инцидент, ждущий неудачного запуска.

<Inset kind="why" lang="ru">
Почему ловушка частичного коммита невидима до продакшена? Потому что баг проявляется только когда что-то выбрасывает *между* двумя записями — а твои тесты почти никогда этого не делают. Тест счастливого пути переводит валидную сумму, обе записи «успешны» (каждая авто-коммитится на своём соединении), балансы выглядят верно, зелёная галочка. Только реальный сбой посреди операции — нарушение ограничения, таймаут, правило одобрения — разрывает две записи и обнажает, что они никогда не были в одной транзакции. Код читается как атомарный, тайпчекер доволен, набор тестов молчит. Вот почему «оно обёрнуто в транзакцию» — не доказательство атомарности; доказательство — «каждая запись идёт через один и тот же manager».
</Inset>

<TradeoffMatrix
  id="nest-04-persistence-02-repositories-and-transactions-tradeoff-1"
  lessonSlug="02-repositories-and-transactions"
  lang="ru"
  prompt="Нужно списать с одного счёта и зачислить на другой так, чтобы пара была атомарной — обе прошли или ни одна. Как ты выполнишь две записи?"
  options={[
    { name: "Открыть dataSource.transaction(async (manager) => ...) и сделать обе записи через manager.getRepository(Account)", summary: "Общий manager = атомарно.", correct: true, justification: "Это unit of work, сделанный правильно. dataSource.transaction открывает одну транзакцию, отдаёт один транзакционный EntityManager, коммитит, если колбэк зарезолвился, и откатывает, если выбросил. Поскольку и decrement, и increment идут через manager.getRepository(Account), они разделяют этот единственный manager и, значит, единственную транзакцию — throw на второй записи откатывает первую. (manager.withRepository(repo) или библиотека контекста @Transactional() дают ту же гарантию общего manager; все верны, потому что все держат записи на одном manager.)" },
    { name: "Инжектить @InjectRepository(Account) и вызвать this.accountRepo.save дважды внутри колбэка dataSource.transaction", summary: "Тихий частичный коммит.", justification: "Это ровно тот инцидент. Инжектированный repository привязан к ДЕФОЛТНОМУ manager у DataSource, а не к транзакционному из колбэка. Каждый save авто-коммитится на своём соединении, так что первая запись уже постоянна до того, как выполнится вторая; если вторая выбросит, dataSource.transaction откатит свою пустую транзакцию, а первая запись останется. Код выглядит транзакционным и проходит каждый тест счастливого пути, а потом списывает без зачисления в первый же раз, когда что-то падает посреди операции." },
    { name: "Сделать две записи в двух отдельных HTTP-запросах, чтобы каждый был мал и независим", summary: "Никакой атомарности.", justification: "Разбиение атомарной операции на два запроса убирает всякий шанс на атомарность — нет транзакции, охватывающей оба вообще. Падение, обрыв сети или клиент, который никогда не пошлёт второй запрос, оставят списание закоммиченным, а зачисление пропавшим. Атомарные операции принадлежат одной транзакции на сервере, а не двум round-trip'ам, координируемым клиентом." },
    { name: "Обернуть каждую запись в собственный dataSource.transaction, чтобы обе были «транзакционными»", summary: "Две транзакции, не одна.", justification: "Две транзакции — это два unit of work, а не один. Первая коммитится независимо; если транзакция второй упадёт, первая уже долговечна и нетронута. Обернуть каждую запись отдельно — значит получить *слово* транзакция дважды, но никогда — нужную *атомарность*: пара должна жить в одной транзакции, разделяя один manager." },
  ]}
/>

<Quiz
  id="nest-04-persistence-02-repositories-and-transactions-quiz-1"
  lessonSlug="02-repositories-and-transactions"
  lang="ru"
  question="Внутри dataSource.transaction(async (manager) => { ... }) ты вызываешь save() у инжектированного @InjectRepository(User) repo, и следующая строка выбрасывает. Что произойдёт?"
  choices={[
    { label: "Инжектированный save уже авто-закоммитился на соединении дефолтного manager, так что он НЕ откатывается — откатились бы только записи, сделанные через `manager`", correct: true },
    { label: "Откатывается всё, потому что любой код внутри колбэка — часть транзакции", misconception: "Быть лексически внутри колбэка не записывает операцию в транзакцию — это делает привязка. Инжектированный repo привязан к дефолтному manager и выполняется на своём соединении, так что он закоммитился независимо, и rollback его никогда не трогал." },
    { label: "save отложен и сбрасывается только на коммите, так что throw его отменяет", misconception: "Репозитории TypeORM — это не отложенный unit of work, который сбрасывается на коммите; save() выдаёт свой стейтмент немедленно. На дефолтном manager этот стейтмент авто-коммитится на месте, до позднего throw, так что rollback его отменить не может." },
    { label: "Nest обнаруживает несоответствие и выбрасывает на старте, так что это никогда не выполнится", misconception: "Никакой проверки на старте для этого нет; код компилируется и выполняется чисто. Несоответствие невидимо, пока реальный throw не приземлится между двумя записями в продакшене — именно поэтому ловушка так опасна." },
  ]}
/>

<Quiz
  id="nest-04-persistence-02-repositories-and-transactions-quiz-2"
  lessonSlug="02-repositories-and-transactions"
  lang="ru"
  question="Ты выполняешь многошаговую запись через ручной QueryRunner и SERIALIZABLE-транзакцию. Какие две вещи обязательно сделать правильно?"
  choices={[
    { label: "Вызвать queryRunner.release() в блоке finally и ретраить весь unit of work при serialization failure (40001) или deadlock", correct: true },
    { label: "Вызывать release() только при ошибке и никогда не ретраить — SERIALIZABLE гарантирует, что транзакция не упадёт", misconception: "release() обязан выполняться и при успехе, иначе ты утечёшь соединение из пула на каждом успешном вызове. И SERIALIZABLE не гарантирует успех — он может прерваться с serialization failure именно для того, чтобы обеспечить инвариант; эти прерывания ожидаемы и должны ретраиться." },
    { label: "Пропустить connect(), так как createQueryRunner() достаточно, и понизить изоляцию до READ UNCOMMITTED ради скорости", misconception: "Нужно вызвать connect() перед startTransaction() на ручном QueryRunner, а READ UNCOMMITTED перечеркнул бы причину, по которой ты выбрал SERIALIZABLE — обеспечение инварианта. Понижение изоляции возвращает ровно те аномалии, от которых ты защищался." },
    { label: "Коммитить каждый стейтмент отдельно, чтобы частичный прогресс сохранялся, и release только при выходе процесса", misconception: "Коммит каждого стейтмента отдельно разрушает атомарность — весь смысл транзакции. А release только при выходе процесса утекает соединение на всю жизнь процесса; release должен происходить в finally на каждую операцию." },
  ]}
/>

</Explanation>

<KeyTakeaway>Одна атомарная операция = одна транзакция = один общий EntityManager. Гони каждую запись через manager.getRepository из колбэка (или библиотеку @Transactional()). Инжектированный repo тихо коммитит частичные записи.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="nest-04-persistence-02-repositories-and-transactions-retrieval"
  lang="ru"
  questions={[
    { q: "Объясни ловушку частичного коммита: почему вызов инжектированного @InjectRepository repo внутри dataSource.transaction не атомарен и как это починить?", a: "Инжектированный repository привязан к ДЕФОЛТНОМУ EntityManager у DataSource, который выполняет каждый вызов на своём авто-коммитящем соединении. Он НЕ наследует транзакционный manager, который создаёт dataSource.transaction, даже когда ты вызываешь его лексически внутри колбэка — нахождение внутри колбэка нерелевантно; важна привязка. Так что первая запись коммитится немедленно на дефолтном соединении; если позже строка выбросит, dataSource.transaction откатит свою транзакцию, которая никогда не содержала эту первую запись. Результат — частичный коммит, например счёт списан, но не пополнен, и он невидим в тестах, потому что тесты редко выбрасывают между двумя записями. Фикс: гони каждую запись через СОБСТВЕННЫЙ manager транзакции — manager.getRepository(Entity) внутри колбэка, или manager.withRepository(existingRepo), чтобы перепривязать кастомный repo, или используй библиотеку транзакционного контекста (typeorm-transactional / AsyncLocalStorage + @Transactional()), чтобы инжектированные repo прозрачно подхватывали активный manager. Правило: одна атомарная операция, одна транзакция, один общий manager, все записи через него." },
    { q: "Сопоставь форму-колбэк и форму QueryRunner для транзакций и скажи, когда поднимать изоляцию и как обрабатывать последствия.", a: "Две формы: (1) dataSource.transaction(async (manager) => {...}) — форма-колбэк: она открывает транзакцию, даёт транзакционный EntityManager, авто-коммитит, если колбэк зарезолвился, и авто-откатывает, если выбросил; ты никогда не пишешь commit/rollback. (2) Ручная форма QueryRunner — createQueryRunner(), connect(), startTransaction([изоляция]), затем commitTransaction()/rollbackTransaction(), с release() в finally (всегда, даже при успехе, иначе утечёшь соединение из пула) — когда нужен явный контроль вроде savepoint'ов или переплетённой логики. Обе обязаны гнать каждую запись через manager транзакции (manager.getRepository или queryRunner.manager.getRepository). Изоляция: дефолт — READ COMMITTED; поднимай до SERIALIZABLE, когда операция должна обеспечить инвариант по строкам, которые читает, а затем пишет (нет овердрафта, нет двойного бронирования). Более строгая изоляция может прервать транзакцию с serialization failure (Postgres 40001) или deadlock (40P01), поэтому SERIALIZABLE unit of work обязан быть повторяемым — лови эти коды, делай backoff и переигрывай всю транзакцию ограниченное число раз. И держи схему в миграциях под контролем версий, никогда synchronize: true в runtime." },
  ]}
/>

<Recap lang="ru">
Repository — это граница персистентности между твоим доменом и базой, и инжектированный @InjectRepository repo привязан к дефолтному EntityManager у DataSource — каждый вызов авто-коммитится на своём соединении, что верно для одного чтения или записи. Unit of work — это одна атомарная бизнес-операция, отображённая ровно в одну транзакцию, где ВСЕ записи идут через ОДИН И ТОТ ЖЕ транзакционный manager. TypeORM предлагает две формы транзакций: колбэк dataSource.transaction(async (manager) => ...), который авто-коммитит при resolve и авто-откатывает при throw, и ручной QueryRunner (connect, startTransaction, commit/rollbackTransaction, release в finally) для явного контроля и изоляции. Сеньорская ловушка: вызов инжектированного repo внутри dataSource.transaction НЕ входит в эту транзакцию — запись авто-коммитится на дефолтном manager, так что throw посреди операции оставляет частичный коммит (списано, но не зачислено), невидимый до продакшена, потому что тесты редко выбрасывают между записями. Фиксы — гнать записи через manager.getRepository / manager.withRepository внутри колбэка или использовать библиотеку контекста @Transactional() на AsyncLocalStorage. Поднимай изоляцию до SERIALIZABLE для обеспечения инвариантов, ретрай при serialization failure и deadlock, и держи схему в миграциях под контролем версий, а не в runtime-synchronize. Теперь, когда читаешь сервис и видишь инжектированный @InjectRepository, используемый внутри колбэка dataSource.transaction, — ты знаешь точно, какой тихий риск несёт этот код и как его починить.
</Recap>
