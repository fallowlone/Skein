---
concepts:
- stateful-sessions
- stateless-jwt
- revocation
- token-denylist
- token-version
deepensInto: []
estMin: 18
lang: ru
lessonType: topic
level: senior
order: 4
prereqs: []
slug: "04-sessions-vs-jwt-and-logout"
sources:
- https://docs.nestjs.com/recipes/passport
- https://docs.nestjs.com/techniques/session
spiral: []
status: ready
summary: 'Сессии хранят авторизацию на сервере и отзывают удалением строки; JWT несёт свои claims и не может быть отозван до exp. Поэтому logout с JWT сложен: очистка cookie ничего не делает с украденным токеном. Митигируй коротким TTL плюс denylist или per-user tokenVersion.'
title: 'Сессии против JWT, и почему logout — сложно'
track: nest
unit: 05-auth
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
В тикете было написано: «пользователь травит людей в чате — забань его, сейчас». Дежурный инженер нажал эндпоинт бана, увидел, как строка переключилась в `banned: true`, и ответил «готово». Через сорок минут абьюз всё ещё шёл. Аккаунт был забанен в базе — и продолжал постить. Никто ничего не отозвал, потому что отзывать было нечего: приложение авторизовало каждый запрос из stateless-JWT, а токен абьюзера был выпущен за двадцать минут до бана с часовым сроком жизни. Флаг бана был настоящим; токену было всё равно. Он продолжал верифицироваться, продолжал проходить guard, продолжал пропускать записи — пока не истёк по своему расписанию, а не по нашему. Logout, как выясняется, — это не действие в UI. С неправильной моделью авторизации это то, что ты сделать не можешь.
</Hook>

<Crux>Сессии хранят авторизацию на сервере и отзывают удалением строки; JWT несёт свои claims и валиден до exp — отозвать его нельзя.</Crux>

<Explanation>

## Две модели: где живёт состояние авторизации

Прежде чем выбирать модель авторизации или отлаживать logout, который «не сработал», нужно понять одно: где сервер хранит истину о том, кто ты? Ответ на этот вопрос определяет всё остальное — что стоит отзыв, сколько round-trip тратит запрос и возможно ли «забань сейчас» одной строкой. Каждая схема авторизации отвечает на один вопрос на каждом запросе — «этот вызывающий действительно тот, за кого себя выдаёт?» — и две доминирующие модели отвечают на него из противоположных мест.

**Stateful серверные сессии** держат истину на сервере. Логин создаёт **строку** сессии в хранилище (Redis или таблица БД); клиент получает назад только непрозрачный, ничего не значащий id сессии в `httpOnly` cookie. Каждый последующий запрос несёт этот id, и сервер **ищет сессию**, чтобы узнать, кто ты.

```ts
// Stateful: cookie держит непрозрачный id; сервер держит истину.
// Логин пишет строку; каждый запрос читает её обратно.
await store.set(`sess:${sid}`, { userId, roles }, { ttlSec: 3600 });
res.cookie('sid', sid, { httpOnly: true, secure: true, sameSite: 'lax' });

// На каждом запросе guard резолвит личность через ПОИСК:
const session = await store.get(`sess:${req.cookies.sid}`); // 1 чтение store
if (!session) throw new UnauthorizedException();             // отозван → мгновенно вышел
```

**Stateless JWT** держит истину в токене. Токен **и есть** claims (`sub`, `roles`, `exp`), подписанные сервером. Верификация локальна: проверь подпись и `exp`, доверяй payload. Никакого хранилища, никакого поиска.

```ts
// Stateless: токен несёт claims, подписанные. Verify локален — ноль поисков.
const token = jwt.sign({ sub: userId, roles }, SECRET, { expiresIn: '15m' });

// На каждом запросе: проверь подпись + exp, затем доверяй payload. Никакого I/O.
const claims = jwt.verify(req.headers.authorization?.slice(7), SECRET);
```

Эта разница — весь урок. Сессии стоят **одного чтения store на запрос** и заставляют делить это хранилище между всеми инстансами приложения (sticky-сессии или центральный Redis — общее состояние и есть цена). JWT стоит **ноля поисков** и масштабируется горизонтально без чего-либо общего — и именно поэтому его нельзя отозвать: нет строки, которую можно удалить.

## Проблема logout: тривиальна с сессиями, сложна с JWT

«Разлогинь этого пользователя везде» / «забань этот аккаунт СЕЙЧАС» — это одна и та же операция, и она брутально обнажает асимметрию.

С **сессиями** это одна строка: удали строку. Поиск следующего запроса промахнётся, guard выбросит, и пользователь вышел — на каждом устройстве, мгновенно, глобально. Сервер держал состояние, значит сервер может его уничтожить.

```ts
// Сессии: отзыв ЕСТЬ удаление. Мгновенно, глобально, готово.
await store.del(`sess:${sid}`);        // одно устройство
await store.del(`user-sessions:${userId}`); // или каждое устройство — следующий поиск промахнётся
```

С **JWT** такой строки нет. Токен само-валидируется: любой держатель байтов авторизован до `exp`, точка. Ты можешь очистить cookie — но это затронет только **кооперирующего** клиента. Токен, скопированный вором или переигранный скриптом абьюзера, нетронут. Нет серверной рукоятки, за которую можно потянуть.

Поэтому ты симулируешь отзыв, и каждый вариант — это компромисс:

```ts
// (1) КОРОТКИЙ TTL access-токена — сожми неотзываемое окно до минут.
//     Ради этого существуют refresh-токены (разбирается отдельно): отзывает сам exp.
jwt.sign({ sub: userId }, SECRET, { expiresIn: '15m' }); // окно ≤ 15 минут

// (2) DENYLIST отозванных jti до их exp — но это снова поиск на каждом запросе,
//     возвращающий общее состояние, ради ухода от которого ты взял JWT. Гибрид.
if (await denylist.has(claims.jti)) throw new UnauthorizedException(); // +1 чтение

// (3) PER-USER tokenVersion (он же minIssuedAt) — бампай его на logout-all /
//     смене пароля; отвергай любой токен, выпущенный раньше. Один индексированный поиск,
//     инвалидирует ВСЕ токены пользователя разом. Прагматичная золотая середина.
const u = await users.findById(claims.sub);               // 1 индексированное чтение
if (claims.tokenVersion !== u.tokenVersion) throw new UnauthorizedException();
```

Вариант (1) ограничивает окно урона, но никогда не закрывает его по требованию. Вариант (2) даёт точный отзыв на каждый токен, но возвращает чтение на запрос, ради ухода от которого ты ушёл в stateless. Вариант (3) — тот, на котором оседает большинство команд: единственная индексированная колонка, бампнутая один раз, убивает каждый токен пользователя — правильный инструмент для «забань сейчас» и «разлогинь все устройства».

<Inset kind="why" lang="ru">
Почему «просто удали cookie при logout» на самом деле не разлогинивает JWT-пользователя? Потому что JWT само-валидируется — он несёт свою подпись, и сервер доверяет ему без поиска. Любая сторона, держащая сырые байты, авторизована до `exp`: легитимный браузер, конечно, но также вор, скопировавший токен, или скрипт, переигрывающий его. Очистка cookie убирает лишь копию в **кооперирующем** браузере; она не может дотянуться до украденной или переигранной копии, потому что сервер не хранит записи о выпущенных токенах, которую можно было бы инвалидировать. Очистка cookie — это любезность на стороне клиента, а не отзыв. Настоящий отзыв требует серверного состояния, которое сервер может изменить — записи в denylist или бампнутого `tokenVersion`, — так чтобы следующий verify смог отвергнуть токен, который подпись иначе бы приняла.
</Inset>

## Где какая модель уместна — и почему реальные системы гибридны

Тянись к **сессиям**, когда мгновенный глобальный отзыв — жёсткое требование, а общее хранилище приемлемо: классические server-rendered веб-приложения, банкинг, админ-консоли — везде, где «убей этот доступ прямо сейчас» должно стать правдой в следующую миллисекунду. Тянись к **JWT**, когда центральное хранилище сессий — это узкое место: stateless-микросервисы, мобильные/API-клиенты и межсервисная авторизация, где каждый хоп, перепроверяющий один Redis, сериализовал бы твой fan-out.

Большинство зрелых систем отказываются от бинарности и идут в **гибрид**: короткоживущий **stateless access-JWT** (ноль поисков на горячем пути) в паре со **stateful refresh-токеном, хранимым в БД** (отзываемым). Ты уже держишь серверное состояние на границе refresh, так что отзыв refresh-токена останавливает выпуск новых access-токенов — а короткий access-TTL осушает те, что уже выданы. Это и есть архитектура, дающая масштаб JWT и отзываемость сессий, с неотзываемым окном, ограниченным access-TTL.

Несколько JWT-граблей едут с тобой в любом случае. **Размер токена**: JWT в заголовке или cookie куда крупнее непрозрачного id сессии и едет на *каждом* запросе — держи claims минимальными, никогда не запихивай весь профиль пользователя. **Расхождение часов**: `exp` сравнивается с часами каждого сервера, так что допусти небольшой leeway (несколько секунд), иначе нода с убежавшими часами отвергнет ещё валидные токены. **Флаги cookie** важны для обеих моделей: `httpOnly` не даёт JS читать токен, `Secure` держит его вне открытого текста, `SameSite` притупляет CSRF.

<FlowDiagram
  label="Как запрос авторизуется в каждой модели и где отзыв возможен, а где нет. Путь сессий: запрос несёт непрозрачный id сессии в httpOnly cookie; guard делает один поиск в store, чтобы резолвить строку сессии; поскольку сервер держит эту строку, она отзываема — удаление строки заставляет следующий поиск промахнуться, и пользователь мгновенно вышел везде. Путь JWT: запрос несёт подписанный токен; guard верифицирует подпись и exp локально без поиска, что быстро и не делит состояния, но токен поэтому неотзываем до exp — очистка cookie не может отозвать украденную копию; чтобы отозвать до exp, ты должен вернуть серверное состояние, denylist по jti или per-user tokenVersion, проверяемый на каждом запросе, что возвращает одно чтение."
  hue="var(--d-systems)"
  perRow={3}
  nodes={[
    { id: "req", label: "запрос", sub: "cookie / header" },
    { id: "look", label: "поиск в store", sub: "1 чтение" },
    { id: "rev", label: "отзываем", sub: "удали строку → вышел" },
    { id: "verify", label: "verify подпись + exp", sub: "0 поисков" },
    { id: "unrev", label: "неотзываем", sub: "валиден до exp" },
    { id: "state", label: "denylist / tokenVersion", sub: "+1 чтение для отзыва" },
  ]}
  edges={[
    { from: "req", to: "look", label: "сессии" },
    { from: "look", to: "rev", label: "сервер держит состояние" },
    { from: "req", to: "verify", label: "JWT" },
    { from: "verify", to: "unrev", label: "нет состояния" },
    { from: "unrev", to: "state", label: "чтобы отозвать" },
  ]}
  caption="Сессии (сверху): поиск на запрос — это цена, но именно владение состоянием делает удаление мгновенным глобальным logout. JWT (снизу): отсутствие поиска — выигрыш, но отсутствие состояния и есть причина, почему токен не отозвать — отзыв возвращает чтение denylist или tokenVersion обратно в путь."
/>

<TradeoffMatrix
  id="nest-05-auth-04-sessions-vs-jwt-and-logout-tradeoff-1"
  lessonSlug="04-sessions-vs-jwt-and-logout"
  lang="ru"
  prompt="Админ должен мгновенно забанить сейчас залогиненного пользователя на всех его устройствах. Приложение авторизует каждый запрос stateless access-JWT (TTL 15 минут). Какой подход лучший?"
  options={[
    { name: "Добавь per-user tokenVersion (minIssuedAt), проверяемый на каждом запросе; бампай его при бане, чтобы каждый существующий токен был отвергнут, и сохрани короткий access-TTL", summary: "Бамп tokenVersion → все токены мертвы.", correct: true, justification: "Это единственный вариант, который реально отзывает СЕЙЧАС и глобально без переархитектуры. Токен уже несёт id пользователя; на каждом запросе ты делаешь один индексированный поиск tokenVersion этого пользователя и отвергаешь любой токен, чья встроенная версия устарела. Бамп колонки один раз инвалидирует каждый токен пользователя, на каждом устройстве, на следующем запросе — это и есть «бан на всех устройствах». Короткий access-TTL — комплементарный кап: даже токены в полёте исчезают в пределах TTL. Да, это добавляет одно чтение на горячем пути — это намеренная, ограниченная цена превращения stateless-JWT в отзываемый, и она куда дешевле полного хранилища сессий, ведь это единственный поиск по ключу, который можно кэшировать." },
    { name: "Очисти auth-cookie пользователя на сервере / скажи клиенту удалить её", summary: "Останавливает лишь кооперирующего клиента.", justification: "Очистка cookie убирает токен из единственного кооперирующего браузера, но JWT само-валидируется: любой другой держатель байтов — скрипт абьюзера, скопированный токен, второе устройство — продолжает авторизоваться до exp. Очистка cookie — любезность на стороне клиента, а не отзыв; она буквально ничего не делает с токеном, который ты не контролируешь. Это в точности ошибка из истории: флаг бана переключился, cookie очистили, а абьюз продолжился." },
    { name: "Ничего не делай — жди, пока 15-минутный токен истечёт", summary: "Оставляет живое окно абьюза.", justification: "Короткий TTL в конце концов закрывает окно, и это реальная митигация для ограничения урона. Но «забань СЕЙЧАС» значит сейчас, а не через до пятнадцати минут, в течение которых забаненный пользователь продолжает писать. Для бана при активном абьюзе или инциденте безопасности четверть часа продолжающегося доступа — это провал, а не приемлемый SLA. TTL ограничивает худший случай; это не отзыв по требованию." },
    { name: "Мигрируй всё приложение с JWT на серверные сессии сегодня ночью", summary: "Верная способность, неверный (огромный) рычаг.", justification: "Сессии дали бы мгновенный отзыв — но переписывать модель авторизации под активным инцидентом это массивное, рискованное изменение, вводящее общее хранилище сессий, инфраструктуру sticky-сессий или центрального store и миграцию для каждого клиента, ничего из чего ты не выкатишь за минуты, которых требует бан. tokenVersion покупает тот же исход мгновенного глобального отзыва одной колонкой и одним чтением. Прибереги миграцию на сессии для планового решения, а не для реакции на инцидент." },
  ]}
/>

<Quiz
  id="nest-05-auth-04-sessions-vs-jwt-and-logout-quiz-1"
  lessonSlug="04-sessions-vs-jwt-and-logout"
  lang="ru"
  question="Какая модель авторизации даёт мгновенный глобальный отзыв залогиненного пользователя и почему?"
  choices={[
    { label: "Stateful-сессии — сервер держит строку сессии, так что её удаление заставляет поиск следующего запроса промахнуться, и пользователь мгновенно вышел везде", correct: true },
    { label: "Stateless-JWT — ты просто очищаешь токен, и сервер перестаёт его принимать", misconception: "Stateless-JWT — это противоположность: сервер НЕ хранит записи о нём, так что удалять нечего. Очистка токена затрагивает лишь кооперирующего клиента; любой другой держатель продолжает авторизоваться до exp. JWT — это ровно та модель, где мгновенный отзыв сложен, в чём и весь смысл урока." },
    { label: "Обе одинаково — exp у JWT истекает так же быстро, как удаление строки сессии", misconception: "exp — это фиксированная будущая отметка времени, а не действие по требованию: 15-минутный токен остаётся валидным ещё до 15 минут, что бы ты ни делал. Удаление строки сессии срабатывает на самом следующем запросе. Они не эквивалентны — одно мгновенно и по требованию, другое — таймер, который ты не сдвинешь." },
    { label: "Stateless-JWT — поскольку верификация локальна, сервер может отвергнуть его без какого-либо I/O", misconception: "Локальная верификация — это ровно та причина, почему JWT НЕЛЬЗЯ отозвать: без I/O и без хранимой записи у сервера нет способа узнать, что ты хотел убрать именно этот токен. Локальный stateless-verify — это и выигрыш масштаба, и проклятие отзыва одновременно." },
  ]}
/>

<Quiz
  id="nest-05-auth-04-sessions-vs-jwt-and-logout-quiz-2"
  lessonSlug="04-sessions-vs-jwt-and-logout"
  lang="ru"
  question="Твой API использует stateless access-JWT. Ты должен принудительно разлогинить пользователя до exp его токена. Какой подход работает и почему наивный проваливается?"
  choices={[
    { label: "Короткий access-TTL плюс серверная проверка — denylist отозванных jti или per-user tokenVersion — отвергаемая на каждом запросе; одной очистки cookie мало, потому что JWT само-валидируется, и любой другой держатель остаётся авторизованным до exp", correct: true },
    { label: "Просто очисти auth-cookie при logout — это удаляет токен и завершает сессию", misconception: "Очистка cookie убирает лишь копию кооперирующего браузера. JWT само-валидируется, так что украденная или переигранная копия продолжает проходить guard до exp; сервер не хранил записи, чтобы её инвалидировать. Настоящий force-logout требует серверного состояния (denylist или tokenVersion), которое шаг verify может проверить." },
    { label: "Ротируй секрет подписи сервера, чтобы подпись старого токена больше не верифицировалась", misconception: "Ротация секрета инвалидирует КАЖДЫЙ непогашенный токен КАЖДОГО пользователя, разлогинивая всю твою базу пользователей разом и ломая валидные сессии — тупой инструмент, а не per-user force-logout. tokenVersion целит ровно в токены одного пользователя; ротация секрета — для компрометации ключа, а не для одного logout." },
    { label: "Понизь exp токена задним числом в базе данных", misconception: "exp — это подписанный claim, впечённый в уже выпущенный токен; нет его копии в базе, которую можно понизить, и изменение чего-либо на сервере не может изменить байты, которые клиент уже держит. Ты не можешь дотянуться обратно в выпущенный JWT — можешь только отказать ему на этапе verify через проверку denylist или tokenVersion." },
  ]}
/>

</Explanation>

<KeyTakeaway>Сессии отзывают удалением строки — мгновенно, глобально. JWT валиден до exp; очистка cookie не отзовёт украденную копию. Чтобы force-logout JWT: короткий TTL плюс denylist или per-user tokenVersion (одно чтение обратно).</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="nest-05-auth-04-sessions-vs-jwt-and-logout-retrieval"
  lang="ru"
  questions={[
    { q: "Сопоставь stateful-сессии и stateless-JWT по тому, где живёт состояние авторизации, по цене на запрос и по отзыву.", a: "Stateful-сессии держат истину на сервере: логин пишет строку сессии в хранилище (Redis/БД), а клиент держит лишь непрозрачный id сессии в httpOnly cookie. Каждый запрос стоит одного чтения store, чтобы найти сессию, и это хранилище должно быть общим для всех инстансов приложения (sticky-сессии или центральный Redis) — общее состояние и есть цена. Выигрыш — тривиальный отзыв: удали строку, и следующий поиск промахнётся, так что пользователь вышел мгновенно, глобально, на каждом устройстве. Stateless-JWT держит истину в токене: токен ЕСТЬ подписанные claims (sub, roles, exp), так что верификация локальна — проверь подпись и exp, доверяй payload, ноль поисков, ничего общего, масштабируется горизонтально. Цена — зеркальное отражение: поскольку нет серверной записи, ты не можешь отозвать JWT до его exp — нет строки, которую удалить. Так что сессии меняют поиск на запрос и общее состояние на мгновенную отзываемость; JWT меняет отзываемость на масштаб с нулём поисков и без общего состояния." },
    { q: "Почему очистка cookie не разлогинивает JWT-пользователя, и каковы три реальные митигации с их компромиссами?", a: "Очистка cookie проваливается, потому что JWT само-валидируется: сервер доверяет подписанным байтам без поиска, так что любой держатель этих байтов — кооперирующий браузер, но также вор, скопировавший токен, или скрипт, переигрывающий его — остаётся авторизованным до exp. Очистка cookie убирает лишь копию кооперирующего браузера; она не дотянется до украденной или переигранной копии, потому что сервер не хранит записи о выпущенных токенах для инвалидации. Настоящий force-logout требует серверного состояния, которое шаг verify может проверить. Три митигации, каждая компромисс: (1) КОРОТКИЙ TTL access-токена (~5–15 мин) сжимает неотзываемое окно до минут — ради этого существуют refresh-токены — но никогда не закрывает окно по требованию. (2) Серверный DENYLIST отозванных jti до их exp даёт точный отзыв на каждый токен, но возвращает поиск на запрос — ровно то общее состояние, ради ухода от которого ты взял JWT, — гибрид. (3) Per-user колонка tokenVersion / minIssuedAt, бампнутая на logout-all или смене пароля и проверяемая на каждом запросе, стоит одного индексированного чтения, но инвалидирует ВСЕ токены пользователя разом — прагматичная золотая середина для «забань сейчас» и «разлогинь все устройства». Многие системы гибридны: stateless access-JWT плюс stateful, отзываемый refresh-токен в БД, так что ты отзываешь на границе refresh, а короткий access-TTL осушает то, что уже выдано." },
  ]}
/>

<Recap lang="ru">
Каждая модель авторизации отвечает на «этот вызывающий действительно тот, за кого себя выдаёт?» из одного из двух мест, и этот выбор решает, возможен ли вообще logout. Stateful-сессии держат истину на сервере: логин пишет строку сессии в Redis или БД, клиент держит лишь непрозрачный id в httpOnly cookie, и каждый запрос стоит одного чтения store для резолва личности — плюс общее хранилище между инстансами. Награда в том, что отзыв есть удаление: дропни строку, и следующий поиск промахнётся, так что пользователь вышел мгновенно и глобально. Stateless-JWT держит истину в токене — самих подписанных claims, — так что верификация локальна с нулём поисков и без общего состояния, что масштабируется горизонтально, но означает, что нет строки для удаления: JWT валиден до своего exp что бы ни случилось, а очистка cookie убирает лишь копию кооперирующего клиента, никогда — украденную или переигранную. Поэтому забаненный пользователь продолжал постить. Чтобы force-logout JWT, ты возвращаешь серверное состояние с компромиссом: короткий access-TTL (~5–15 мин) ограничивает окно, denylist отозванных jti даёт точность на каждый токен ценой чтения на запрос, а per-user tokenVersion (колонка, хранящая версию токенов пользователя), бампнутый на logout-all, убивает каждый токен пользователя одним индексированным поиском. Сессии подходят server-rendered приложениям, банкингу и админке, где мгновенный отзыв не обсуждается; JWT подходит stateless-микросервисам, мобайлу/API и межсервисной авторизации, где центральное хранилище — узкое место; а большинство зрелых систем гибридны — stateless access-JWT в паре со stateful, отзываемым refresh-токеном в БД, отзывая на границе refresh, пока короткий access-TTL осушает остальное. Держи claims минимальными (размер едет на каждом запросе), допускай небольшой clock-skew leeway против exp и ставь httpOnly + Secure + SameSite в обеих моделях. Теперь, когда кто-то скажет «почему забаненный пользователь всё ещё постит?», первый вопрос будет: какой TTL access-токена, есть ли колонка tokenVersion и кто отозвал refresh? Это и есть верная ментальная модель — не «мы же очистили cookie?»
</Recap>
