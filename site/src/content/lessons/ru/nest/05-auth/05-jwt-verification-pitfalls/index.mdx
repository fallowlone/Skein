---
concepts:
- alg-allowlist
- key-confusion
- registered-claims
- jwks-by-kid
- audience-issuer
deepensInto: []
estMin: 18
lang: ru
lessonType: topic
level: senior
order: 5
prereqs: []
slug: "05-jwt-verification-pitfalls"
sources:
- https://docs.nestjs.com/security/authentication
- https://datatracker.ietf.org/doc/html/rfc8725
spiral: []
status: ready
summary: 'Подпись доказывает целостность, а не то, что токен предназначен тебе. Заголовок JWT контролируется атакующим, поэтому верификатор, доверяющий его alg/kid, открывает alg:none и путаницу ключей HS256/RS256. Пин allowlist алгоритмов, проверка aud/iss/exp, JWKS по kid.'
title: 'Ловушки верификации JWT: алгоритмы, claims и ключи'
track: nest
unit: 05-auth
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
В отчёте пентеста была одна строка, угробившая весь спринт: «обход аутентификации, доступ админа, учётные данные не нужны». Приложение выпускало RS256-токены из своего IdP и верифицировало их публичным ключом IdP — по учебнику. Но `JwtStrategy` была настроена только с secret-or-key и больше ничем: ни `algorithms`, ни `audience`, ни `issuer`. Тестировщик не крал ключ. Он взял обычный токен, поменял в заголовке `alg` на симметричный и переподписал его, используя *публичный* ключ сервера как HMAC-секрет — ключ, напечатанный в discovery-документе IdP на всеобщее обозрение. Верификатор, которому не сказали, какой алгоритм ожидать, радостно пошёл по симметричному пути и подтвердил подделку. Валидная подпись была проверена. Просто это была не та подпись, для которой кому-то нужны секреты. Этот урок — про разрыв между «подпись проверилась» и «этому токену можно доверять».
</Hook>

<Crux>Заголовок JWT контролируется атакующим. Пин allowlist алгоритмов, проверка aud/iss/exp, JWKS по kid — одна подпись ничего не доказывает.</Crux>

<Explanation>

## Заголовок — это недоверенный ввод

JWT состоит из трёх частей: заголовок, payload, подпись. Инстинкт — обращаться со всем токеном как с запечатанным конвертом, где надо лишь проверить печать. Но заголовок — включая `alg` (каким алгоритмом подписано) и `kid` (каким ключом) — приходит *до* того, как ты что-либо проверил. Это контролируемый атакующим ввод. Верификатор, который читает `alg` из заголовка и затем проверяет «так, как говорит токен», отдал атакующему руль: он выбирает алгоритм, ты подчиняешься. Два классических обхода живут ровно в этом разрыве.

**`alg: none`.** Спецификация JWS определяет небезопасный алгоритм, буквально названный `none`, для случаев, где целостность гарантирована другими средствами. Токен с `alg: none` несёт пустую подпись. Наивный верификатор, который уважает выбор заголовка, увидит «алгоритм: none, подпись: (пусто)» и заключит, что токен валидно подписан — потому что для `none` пустая подпись *и есть* валидная. Атакующий переписывает payload на `role: admin`, ставит `alg: none`, не шлёт подпись и становится кем угодно.

**Путаница ключей HS256/RS256.** Это та самая из вступления, и она тоньше. RS256 асимметричен: IdP подписывает *приватным* ключом, ты верифицируешь парным *публичным*. HS256 симметричен: один и тот же секрет и подписывает, и верифицирует. Теперь представь, что твоему verify-коду передан публичный ключ RS256, но алгоритм он выбирает из заголовка. Атакующий шлёт `alg: HS256`. Библиотека, доверяющая заголовку, идёт по HMAC-пути — и использует переданные тобой байты (публичный ключ RS256) *как HMAC-секрет*. Публичный ключ публичен. У атакующего он есть. Поэтому он вычисляет валидный HS256-MAC над своим поддельным payload, и верификатор это подтверждает.

```ts
// THE VULNERABILITY: the verifier lets the token choose the algorithm.
// No `algorithms` allowlist, no audience, no issuer.
@Injectable()
export class JwtStrategy extends PassportStrategy(Strategy) {
  constructor() {
    super({
      jwtFromRequest: ExtractJwt.fromAuthHeaderAsBearerToken(),
      secretOrKey: PUBLIC_KEY, // RS256 public key — but nothing pins RS256
      // BUG: omitting `algorithms` means an alg:none or HS256 token is accepted
      // BUG: omitting `audience`/`issuer` means any signed token is trusted
    });
  }
  validate(payload: any) { return { userId: payload.sub, role: payload.role }; }
}
```

Фикс для обоих — одно и то же движение: **перестань давать токену выбирать.** Передай в verify явный allowlist допустимых алгоритмов. Если ожидаешь RS256, скажи `algorithms: ['RS256']`, и верификатор откажет `none` и откажет HS256 *до* того, как вообще посмотрит на подпись — симметричный путь и небезопасный путь для этого верификатора просто не существуют.

```ts
// THE FIX: pin the algorithm, the audience, and the issuer.
@Injectable()
export class JwtStrategy extends PassportStrategy(Strategy) {
  constructor(config: ConfigService) {
    super({
      jwtFromRequest: ExtractJwt.fromAuthHeaderAsBearerToken(),
      // for an external IdP, fetch keys from its JWKS endpoint, selected by `kid`:
      secretOrKeyProvider: passportJwtSecret({
        cache: true,
        rateLimit: true,
        jwksUri: config.get('IDP_JWKS_URI'),
      }),
      algorithms: ['RS256'],                  // <- the token can no longer choose
      audience: config.get('API_AUDIENCE'),   // <- this token was minted for US
      issuer: config.get('IDP_ISSUER'),       // <- by OUR trusted IdP
      // exp/nbf are validated by default unless you disable them — leave them on
    });
  }
  validate(payload: JwtPayload) { return { userId: payload.sub, role: payload.role }; }
}
```

<Inset kind="why" lang="ru">
Почему верификация публичным ключом RS256 при приёме HS256-токена позволяет атакующему подделывать токены? Потому что верификатор переиспользует любые переданные ему байты ключа как вход для того алгоритма, что назвал заголовок — а для HS256 эти байты становятся *HMAC-секретом*. Вся безопасность HMAC держится на том, что секрет секретен. Но *публичный* ключ RS256 по определению опубликован — он в JWKS-документе IdP, отдаётся кому угодно. Так что «использовать публичный ключ как HMAC-секрет» означает «использовать значение, которое все уже знают, как то, что должно быть неизвестным». Любой может затем вычислить подходящий MAC над любым payload. Пин `algorithms: ['RS256']` убирает HMAC-путь целиком: верификатор никогда не трактует эти байты ключа как симметричный секрет, и путаница невозможна.
</Inset>

## Валидной подписи недостаточно

Допустим, ты запинил алгоритм и подпись честно проверяется. Доверять claims токена всё равно нельзя, потому что подпись доказывает ровно одно: байты не были подменены кем-то без ключа. Она ничего не говорит о том, *когда* токен валиден и *для кого* он был выпущен.

**`exp` и `nbf` — свежесть.** `exp` (истечение) и `nbf` (не ранее) ограничивают окно валидности токена. Большинство библиотек проверяют их автоматически, *если ты их не отключишь* (`ignoreExpiration: true` в passport-jwt — это грабли). Подпись остаётся валидной вечно; только `exp` делает так, что утёкший или вышедший из системы токен перестаёт работать. Проверяй их.

**`aud` — audience.** `aud` называет сервис, *для* которого выпущен токен. Это claim, который команды забывают, и он вызывает отказ типа confused-deputy. Представь внутренний сервис и публичный API, которые ради удобства делят один ключ подписи (или один IdP, выпускающий для обоих). Токен, выпущенный для внутреннего сервиса, — совершенно валидный, корректно подписанный токен. Без проверки `aud` публичный API его принимает — предъявитель теперь владеет токеном, никогда не предназначавшимся этой поверхности. Проверка `audience` говорит «я принимаю только токены, чей `aud` — это *я*», и межсервисный replay умирает.

**`iss` — issuer.** `iss` пинит, *какому* IdP ты доверяешь. Если ты его пропустишь и твой набор доверенных ключей пересекается с другим issuer, токен от не той инстанции может проскочить. Проверяй, что `iss` — это твой IdP.

И не доверяй прикладным claims вроде `roles` или `sub` сверх того, что покрывает подпись и что заново перепроверяет твой слой авторизации — подпись гарантирует, что issuer *положил* эти claims туда, а не что они всё ещё отражают текущее состояние пользователя.

<FlowDiagram
  label="Конвейер верификации как последовательность контрольных точек безопасности между входящим токеном и доверенными claims. Входящий токен приходит с заголовком, чьи поля alg и kid контролируются атакующим и потому недоверенны. Первая контрольная точка пинит allowlist алгоритмов, отклоняя alg:none и любой симметричный алгоритм, когда ожидается асимметричный, так что токен больше не может выбирать, как его проверяют. С зафиксированным алгоритмом верификатор берёт публичный ключ из JWKS-эндпоинта IdP, выбранный по kid токена, с кэшированием и ротацией. Затем он проверяет криптографическую подпись против этого ключа. Только после прохождения подписи он валидирует зарегистрированные claims — exp и nbf для свежести, aud, что токен выпущен для этого API, iss, что он пришёл от доверенного issuer. Только токены, прошедшие каждую точку, дают доверенные claims."
  hue="var(--d-systems)"
  perRow={3}
  nodes={[
    { id: "token", label: "Входящий токен", sub: "заголовок alg/kid = недоверен" },
    { id: "pin", label: "ПИН алгоритмов", sub: "отказ none / HS256" },
    { id: "kid", label: "JWKS по kid", sub: "взять + кэш ключа" },
    { id: "sig", label: "Проверка подписи", sub: "только целостность" },
    { id: "claims", label: "Проверка exp/nbf/aud/iss", sub: "свежесть + audience" },
    { id: "trust", label: "Доверенные claims", sub: "теперь годны" },
  ]}
  edges={[
    { from: "token", to: "pin", label: "недоверен alg" },
    { from: "pin", to: "kid", label: "alg зафиксирован" },
    { from: "kid", to: "sig", label: "верный ключ" },
    { from: "sig", to: "claims", label: "подписан != доверен" },
    { from: "claims", to: "trust", label: "все шлюзы прошли" },
  ]}
  caption="Каждая стрелка — контрольная точка, которая может отклонить. Пин алгоритмов закрывает alg:none и путаницу ключей ещё до чтения подписи; шлюзы claims (exp/nbf/aud/iss) закрывают разрыв между «подписан» и «достоин доверия»."
/>

## Ключи, которые умеют ротироваться

Последняя ловушка — управление ключами. Для асимметричной верификации против внешнего IdP (Auth0, Cognito, Keycloak) не хардкодь публичный ключ. IdP ротируют ключи подписи, и захардкоженный ключ превращает рутинную ротацию в инцидент — или хуже, давит на тебя отключить верификацию, чтобы «починить». Вместо этого бери публичные ключи из **JWKS**-эндпоинта IdP и выбирай нужный по `kid` из заголовка токена, с кэшированием и путём обновления, чтобы новые ключи подхватывались. В NestJS это `secretOrKeyProvider`, подключённый к `passportJwtSecret` из `jwks-rsa`, с `cache: true` и `rateLimit: true`, чтобы и не долбить эндпоинт, и не закэшировать снятый ключ навсегда.

Для симметричного случая (HS256 с общим секретом) секрет несёт всю модель доверия: любой, у кого он есть, может подделать любой токен. Поэтому он должен быть высокоэнтропийным (не словарное слово, не буквальная строка `"secret"`) и пер-окруженческим, чтобы утёкший staging-секрет не мог подделать production-токены. Слабый или утёкший HS256-секрет — это полная компрометация: нет разделения публичный/приватный, на которое можно опереться.

<TradeoffMatrix
  id="nest-05-auth-05-jwt-verification-pitfalls-tradeoff-1"
  lessonSlug="05-jwt-verification-pitfalls"
  lang="ru"
  prompt="Тебе надо укрепить NestJS JwtStrategy, которая верифицирует access-токены, выпущенные внешним IdP (Auth0/Cognito/Keycloak). Как должна быть настроена верификация?"
  options={[
    { name: "Запинить allowlist алгоритмов (напр. ['RS256']), проверять audience и issuer и резолвить ключ из JWKS-эндпоинта IdP по kid с кэшированием", summary: "Полное укрепление.", correct: true, justification: "Это закрывает все ловушки разом. Allowlist алгоритмов убирает и путь alg:none, и путь путаницы ключей HS256/RS256 до чтения подписи, потому что верификатор будет пытаться только RS256. Проверка audience не даёт токену, выпущенному для соседнего сервиса (confused deputy), быть переигранным против этого API; проверка issuer пинит, какому IdP ты доверяешь. Резолв ключа из JWKS по kid позволяет IdP ротировать ключи подписи без инцидента и без захардкоженного ключа. exp/nbf остаются включёнными по умолчанию для свежести. Это ровно то, что предписывает RFC 8725 (JWT BCP), и каждый фикс — одна явная опция на стратегии." },
    { name: "Доверять заголовку alg токена и верифицировать тем алгоритмом, что он называет", summary: "Отдаёт атакующему руль.", justification: "Это И ЕСТЬ уязвимость. Чтение алгоритма из контролируемого атакующим ввода позволяет токену выбрать alg:none (пустая подпись принята как валидная) или сменить RS256 на HS256, чтобы верификатор использовал опубликованный публичный ключ как HMAC-секрет — подделка, которую любой может произвести. Верификатор должен диктовать алгоритм через allowlist; он никогда не должен давать токену выбирать." },
    { name: "Проверять только криптографическую подпись — если подписан, значит доверенный", summary: "Подписан != для-тебя.", justification: "Валидная подпись доказывает целостность (байты не изменены без ключа) и больше ничего. Она не доказывает, что токен не истёк (нет проверки exp), что он выпущен для ЭТОГО API (нет проверки aud — replay confused-deputy) или что он пришёл от твоего IdP (нет проверки iss). Проверка подписи необходима, но недостаточна; шлюзы зарегистрированных claims — это то, что делает подписанный токен достойным доверия." },
    { name: "Проверять только exp, чтобы истёкшие токены отклонялись", summary: "Ловит несвежее, пропускает поддельное.", justification: "Проверка exp корректна и необходима, но адресует только свежесть. Она ничего не делает с подделкой alg:none или путаницей ключей (которая несёт совершенно свежий exp, выбранный атакующим), ничего с токеном, переигранным от соседнего сервиса (aud), и ничего с не тем issuer (iss). exp — один из четырёх шлюзов, и самая дешёвая подделка проходит прямо сквозь него." },
  ]}
/>

<Quiz
  id="nest-05-auth-05-jwt-verification-pitfalls-quiz-1"
  lessonSlug="05-jwt-verification-pitfalls"
  lang="ru"
  question="Приходит токен с заголовком alg:none и пустой подписью, его payload отредактирован на role:admin. Неправильно настроенный сервер принимает его как валидный. Почему — и каков однострочный фикс?"
  choices={[
    { label: "Верификатор уважил alg:none из заголовка, для которого пустая подпись ВАЛИДНА; фикс — запинить allowlist алгоритмов (напр. algorithms: ['RS256']), чтобы 'none' никогда не был принятым алгоритмом", correct: true },
    { label: "Подпись подделали приватным ключом сервера; фикс — сротировать приватный ключ", misconception: "Никакая подпись не подделана — alg:none означает, что подписи НЕТ, чтобы её подделывать. Сервер принял пустую подпись, потому что доверился утверждению заголовка, что алгоритм — 'none', для которого пустая валидна. Ротация ключей ничего не даёт; токен ключ не использовал. Фикс — отказаться уважать 'none', запинив allowlist алгоритмов." },
    { label: "Отсутствовал claim exp, поэтому токен не истекал; фикс — требовать exp", misconception: "exp не связан с этим обходом. Даже токен с валидным будущим exp принимается, если уважается alg:none, потому что пустая подпись проходит. Требование exp не останавливает подделку — атакующий просто включает валидный exp. Фикс — пин алгоритма, чтобы 'none' отклонялся." },
    { label: "Не совпал audience; фикс — проверять aud", misconception: "Проверка aud важна, но это другая защита. Токен alg:none отклоняется задолго до валидации claims, если ты пинишь algorithms, потому что 'none' нет в allowlist. Без allowlist даже токен с правильным aud был бы принят через пустую подпись. Конкретный фикс для alg:none — это allowlist алгоритмов." },
  ]}
/>

<Quiz
  id="nest-05-auth-05-jwt-verification-pitfalls-quiz-2"
  lessonSlug="05-jwt-verification-pitfalls"
  lang="ru"
  question="Ты запинил algorithms: ['RS256'] и подпись корректно проверяется. Почему этого всё равно недостаточно, чтобы доверять токену?"
  choices={[
    { label: "Подпись доказывает только целостность (байты не изменены) — надо ещё проверять exp/nbf (свежесть), aud (токен выпущен для ЭТОГО API) и iss (от твоего IdP)", correct: true },
    { label: "Этого достаточно — корректно проверенная RS256-подпись означает, что токену полностью доверяют", misconception: "Проверенная подпись доказывает лишь, что payload не подменён кем-то без ключа. Она ничего не говорит о том, истёк ли токен, выпущен ли он для твоего API, пришёл ли от твоего issuer. Токен, корректно подписанный для соседнего сервиса, — это валидная подпись и межсервисный replay против тебя. Надо ещё проверять exp/nbf, aud и iss." },
    { label: "Надо дополнительно перепроверить подпись с HS256 как запасным вариантом", misconception: "Это задом наперёд и опасно — верификация HS256 как запасным вариантом заново открывает ровно ту дыру путаницы ключей, что ты закрыл, запинив RS256. Второй симметричный путь не нужен никогда. Недостающие проверки — это зарегистрированные claims (exp/nbf/aud/iss), а не ещё один алгоритм." },
    { label: "Надо расшифровать payload, потому что RS256 подписывает, но не скрывает claims", misconception: "Верно, что подписанный JWT не зашифрован, но конфиденциальность — не тот разрыв доверия здесь. Причина, почему проверенной подписи недостаточно, в том, что она доказывает целостность, а не audience или свежесть — нужны ещё проверки exp/nbf/aud/iss. Шифрование (JWE) — отдельная забота от верификации того, что claims подписанного токена можно доверять." },
  ]}
/>

</Explanation>

<KeyTakeaway>Не давай токену выбирать: пин алгоритмов (убивает alg:none и путаницу ключей HS256/RS256) и проверяй aud/iss/exp — подпись доказывает целостность, а не что токен для тебя или свеж. Для внешних IdP — JWKS по kid.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="nest-05-auth-05-jwt-verification-pitfalls-retrieval"
  lang="ru"
  questions={[
    { q: "Объясни два обхода JWT через заголовок (alg:none и путаница ключей HS256/RS256) и единственное движение конфигурации, что закрывает оба.", a: "Оба эксплуатируют тот факт, что заголовок JWT — включая поле alg — это контролируемый атакующим ввод, приходящий до того, как что-либо проверено. В обходе alg:none спецификация JWS определяет небезопасный алгоритм 'none', чья подпись пуста; верификатор, уважающий заголовок, видит alg:none с пустой подписью и заключает, что он валидно подписан, так что атакующий переписывает payload (напр. role:admin), ставит alg:none, не шлёт подпись и получает доверие. В путанице ключей HS256/RS256 у сервера есть публичный ключ RS256, но он даёт заголовку выбрать алгоритм; атакующий шлёт alg:HS256, библиотека идёт по HMAC-пути и использует байты публичного ключа RS256 как HMAC-секрет — но публичный ключ опубликован в JWKS IdP, так что любой может вычислить валидный MAC над поддельным payload. Единственный фикс для обоих — запинить явный allowlist алгоритмов на вызове verify (напр. algorithms: ['RS256']): верификатор диктует алгоритм вместо токена, так что 'none' отклоняется и симметричный путь никогда не существует, оба отклонены ещё до изучения подписи." },
    { q: "Почему валидной подписи недостаточно, чтобы доверять токену, и что должен дополнительно проверять укреплённый NestJS-верификатор — включая управление ключами для внешнего IdP?", a: "Подпись доказывает только целостность — что payload не изменён кем-то без ключа. Она не доказывает, что токен ещё валиден во времени или предназначался тебе. Поэтому укреплённый верификатор должен ещё валидировать зарегистрированные claims: exp и nbf (свежесть — большинство библиотек проверяют их, если не отключить, так что никогда не ставь ignoreExpiration), aud (audience — что токен выпущен для ЭТОГО API; пропуск даёт токену, выпущенному для соседнего сервиса, делящего ключ подписи, быть переигранным против тебя — отказ confused-deputy) и iss (issuer — что он пришёл от твоего доверенного IdP). Прикладным claims вроде roles/sub нельзя доверять сверх того, что покрывает подпись и что перепроверяет твой слой авторизации. По ключам, с внешним IdP (Auth0/Cognito/Keycloak) ты резолвишь публичный ключ из JWKS-эндпоинта IdP по kid токена с кэшированием и ротацией (в Nest secretOrKeyProvider, подключённый к passportJwtSecret из jwks-rsa с cache и rateLimit), вместо хардкода ключа, который не умеет ротироваться; для HS256 общий секрет должен быть высокоэнтропийным и пер-окруженческим, ведь утёкший или слабый секрет — это полная подделка. Это RFC 8725 (JWT BCP) на практике, и каждая защита — одна явная опция на JwtStrategy." },
  ]}
/>

<Recap lang="ru">
Заголовок JWT — его поля alg и kid — это контролируемый атакующим ввод, приходящий до верификации, так что верификатор, доверяющий ему, отдаёт атакующему руль. Отсюда два обхода через заголовок: alg:none, где небезопасный алгоритм спецификации делает пустую подпись «валидной», так что атакующий переписывает payload и не шлёт подпись; и путаница ключей HS256/RS256, где верификатор, держащий публичный ключ RS256, но дающий заголовку выбрать алгоритм, идёт по HMAC-пути и использует этот опубликованный публичный ключ как HMAC-секрет, позволяя любому подделать валидный MAC. Единственный фикс для обоих — запинить явный allowlist алгоритмов (algorithms: ['RS256']), чтобы верификатор диктовал алгоритм и отклонял 'none' и любой симметричный алгоритм до чтения подписи. Но валидная подпись доказывает только целостность, а не свежесть или audience, так что укреплённый верификатор должен ещё валидировать exp/nbf (оставленные включёнными по умолчанию — никогда ignoreExpiration), aud (этот токен выпущен для ЭТОГО API, закрывая межсервисный replay confused-deputy под общим ключом) и iss (от твоего доверенного IdP). Наконец, управляй ключами с прицелом на ротацию: для внешнего IdP резолвь публичный ключ из его JWKS-эндпоинта по kid с кэшированием, а не захардкоженный ключ, превращающий ротацию в инцидент; для HS256 держи общий секрет высокоэнтропийным и пер-окруженческим. Это RFC 8725, JWT BCP, и в NestJS каждая защита — одна явная опция на JwtStrategy: algorithms, audience, issuer и secretOrKeyProvider на базе jwks-rsa.
</Recap>
