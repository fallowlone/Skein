---
concepts:
- passport
- jwt
- auth-guard
- local-strategy
- jwt-strategy
deepensInto: []
estMin: 17
lang: ru
lessonType: topic
level: middle
order: 1
prereqs: []
slug: "01-passport-and-jwt"
sources:
- https://docs.nestjs.com/security/authentication
- https://docs.nestjs.com/recipes/passport
spiral: []
status: ready
summary: 'Passport даёт Nest две стратегии — LocalStrategy проверяет учётные данные на /login, JwtStrategy верифицирует Bearer-токен на каждом защищённом маршруте. Глобальный JwtAuthGuard fail-closed; @Public() выводит маршруты из-под него. JWT подписан, а не зашифрован.'
title: 'Стратегии Passport и JWT-аутентификация'
track: nest
unit: 05-auth
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import SequenceDiagram from "~/components/diagram/SequenceDiagram.astro";

<Hook>
Отчёт пентеста приходит с одной находкой, помеченной как critical: токен бывшего подрядчика всё ещё работает — спустя три недели после отзыва доступа. Ты проверяешь — строки в таблице users нет, место в SSO освобождено, а `GET /admin/billing` всё равно отдаёт 200 на токен, который они выпустили в марте. Потом ты читаешь собственный `JwtModule.register`: `signOptions: { expiresIn: '90d' }`. Нет ни logout, ни отзыва, ни denylist — JWT, однажды подписанный, валиден до истечения, а ты сказал ему жить девяносто дней. Фикс — это не патч; это понимание того, что подписанный токен есть на самом деле и как Passport его выдаёт и проверяет обратно.
</Hook>

<Crux>LocalStrategy проверяет учётные данные раз на /login; JwtStrategy верифицирует подписанный Bearer-токен на каждом защищённом маршруте.</Crux>

<Explanation>

## Две стратегии, два момента

Аутентификация в Nest — это два разных момента, и Passport моделирует каждый отдельной *стратегией*. Первый момент — **login**: клиент шлёт логин и пароль ровно один раз, ты сверяешь их с базой, и если совпало — выдаёшь токен. Это `LocalStrategy`. Второй момент — **каждый запрос после**: клиент предъявляет токен, ты верифицируешь подпись и срок жизни и доверяешь личности внутри, не трогая базу. Это `JwtStrategy`. Стратегия — это класс, расширяющий `PassportStrategy(...)`, чей метод `validate()` возвращает принципала, — и то, что вернёт `validate()`, Passport кладёт в `req.user`.

`LocalStrategy` читает учётные данные, сравнивает пароль с сохранённым хэшем и возвращает пользователя (без хэша). Парный `LocalAuthGuard` — это однострочник `AuthGuard('local')`, и его вешают на `POST /login`, чтобы стратегия отработала до хендлера:

```ts
import { Strategy } from 'passport-local';
import { PassportStrategy } from '@nestjs/passport';
import { Injectable, UnauthorizedException } from '@nestjs/common';
import * as bcrypt from 'bcrypt';

@Injectable()
export class LocalStrategy extends PassportStrategy(Strategy) {
  constructor(private users: UsersService) {
    super(); // по умолчанию поля называются 'username' и 'password'
  }

  async validate(username: string, password: string) {
    const user = await this.users.findByName(username);
    if (!user || !(await bcrypt.compare(password, user.passwordHash))) {
      throw new UnauthorizedException();
    }
    const { passwordHash, ...safe } = user;
    return safe; // -> становится req.user
  }
}
```

Хендлер `/login` затем чеканит токен. К моменту запуска хендлера guard уже наполнил `req.user`, так что хендлеру остаётся лишь подписать payload — и никогда не клади в него пароль или что-либо секретное:

```ts
@UseGuards(LocalAuthGuard)
@Post('login')
async login(@Request() req) {
  const payload = { sub: req.user.id, username: req.user.username };
  return { access_token: this.jwt.sign(payload) };
}
```

## Проводка модуля: secret из конфига, а не из исходников

`JwtModule` нужны секрет для подписи и время жизни токена, и оба места — конфигурация, а не строковый литерал в репозитории. `registerAsync` позволяет внедрить `ConfigService` через `useFactory`, так что секрет приходит из окружения и ротируется без правки кода. `PassportModule` регистрирует инфраструктуру стратегий; свои классы-стратегии ты отдаёшь как обычные провайдеры:

```ts
@Module({
  imports: [
    PassportModule,
    JwtModule.registerAsync({
      inject: [ConfigService],
      useFactory: (config: ConfigService) => ({
        secret: config.getOrThrow<string>('JWT_SECRET'),
        signOptions: { expiresIn: '15m' }, // короткоживущий access-токен
      }),
    }),
  ],
  providers: [AuthService, LocalStrategy, JwtStrategy],
})
export class AuthModule {}
```

`JwtStrategy` — это верификатор. `ExtractJwt.fromAuthHeaderAsBearerToken()` велит Passport достать токен из `Authorization: Bearer &lt;token>`; `secretOrKey` обязан быть *тем же* секретом, которым подписывали, иначе каждая верификация падает; а оставленный по умолчанию `false` у `ignoreExpiration` — это то, что и enforce-ит TTL. Passport верифицирует подпись и срок жизни *до* того, как запустится твой `validate()`, так что к моменту, когда ты видишь payload, он уже доказанно подлинный — тебе остаётся лишь сформировать принципала:

```ts
import { ExtractJwt, Strategy } from 'passport-jwt';
import { PassportStrategy } from '@nestjs/passport';
import { Injectable } from '@nestjs/common';
import { ConfigService } from '@nestjs/config';

@Injectable()
export class JwtStrategy extends PassportStrategy(Strategy) {
  constructor(config: ConfigService) {
    super({
      jwtFromRequest: ExtractJwt.fromAuthHeaderAsBearerToken(),
      ignoreExpiration: false, // отклонять просроченные токены (дефолт — оставь)
      secretOrKey: config.getOrThrow<string>('JWT_SECRET'),
    });
  }

  async validate(payload: { sub: string; username: string }) {
    return { userId: payload.sub, username: payload.username }; // -> req.user
  }
}
```

## Fail closed: глобальный guard плюс @Public()

Опасный дефолт — защита по принципу opt-in: рассыпать `@UseGuards(JwtAuthGuard)` по каждому маршруту и надеяться, что никто ни один не забудет. Переверни это: зарегистрируй JWT-guard глобально через `APP_GUARD`, чтобы *каждый* маршрут был защищён по умолчанию, а затем вырежи явные дыры декоратором `@Public()`, который guard проверяет через `Reflector`. Забыть декоратор теперь означает оставить маршрут запертым, а не открытым — безопасный режим отказа.

```ts
// public.decorator.ts
export const IS_PUBLIC_KEY = 'isPublic';
export const Public = () => SetMetadata(IS_PUBLIC_KEY, true);
```

```ts
// jwt-auth.guard.ts
@Injectable()
export class JwtAuthGuard extends AuthGuard('jwt') {
  constructor(private reflector: Reflector) {
    super();
  }

  canActivate(context: ExecutionContext) {
    const isPublic = this.reflector.getAllAndOverride<boolean>(IS_PUBLIC_KEY, [
      context.getHandler(),
      context.getClass(),
    ]);
    if (isPublic) return true; // @Public()-маршрут -> пропускаем JWT-верификацию
    return super.canActivate(context); // иначе запускаем AuthGuard('jwt')
  }
}
```

```ts
// app.module.ts — привязываем guard глобально
providers: [{ provide: APP_GUARD, useClass: JwtAuthGuard }],
```

Теперь `POST /login` и `GET /health` несут `@Public()`; всё остальное требует валидный Bearer-токен. Поток от начала до конца:

<SequenceDiagram
  label="Поток JWT-аутентификации Passport: клиент постит учётные данные на login, LocalStrategy сверяет их с хэшированным паролем, сервер подписывает и возвращает JWT-access-токен, клиент хранит его и шлёт как Bearer-заголовок на защищённый маршрут, JwtAuthGuard запускает JwtStrategy, которая верифицирует подпись и срок жизни, и только тогда запускается защищённый хендлер с наполненным req.user."
  hue="var(--d-systems)"
  actors={["Client", "Guard+Strategy", "Handler"]}
  messages={[
    { from: "Client", to: "Guard+Strategy", label: "1. POST /login (user, pass)" },
    { from: "Guard+Strategy", to: "Guard+Strategy", label: "2. LocalStrategy.validate -> user" },
    { from: "Guard+Strategy", to: "Handler", label: "3. handler: jwt.sign(payload)" },
    { from: "Handler", to: "Client", label: "4. { access_token } stored" },
    { from: "Client", to: "Guard+Strategy", label: "5. GET /me (Bearer token)" },
    { from: "Guard+Strategy", to: "Guard+Strategy", label: "6. JwtStrategy: verify sig + exp" },
    { from: "Guard+Strategy", to: "Handler", label: "7. req.user set -> handler runs" },
  ]}
  caption="LocalStrategy работает один раз на /login и выдаёт токен; JwtStrategy работает на каждом защищённом запросе, чтобы его проверить. Маршрут с @Public() пропускает шаг 6 целиком. Guard fail-closed: нет валидного токена — нет хендлера."
/>

## Что лежит в req.user после каждого guard

Два guard'а наполняют `req.user` из разных источников, и путать их — частый баг. После `LocalAuthGuard` `req.user` — это то, что вернул `LocalStrategy.validate()`, то есть свежезагруженный пользователь из базы. После `JwtAuthGuard` `req.user` — это то, что вернул `JwtStrategy.validate(payload)`, выведенное из claim'ов токена, а не из чтения базы. Если роли пользователя поменялись после выпуска токена, JWT-аутентифицированный запрос всё равно видит старые claim'ы, пока токен не истечёт.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Аспект</th>
        <th class="border border-border px-3 py-2 text-left">LocalStrategy</th>
        <th class="border border-border px-3 py-2 text-left">JwtStrategy</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2"><strong>Работает на</strong></td>
        <td class="border border-border px-3 py-2">Только POST /login</td>
        <td class="border border-border px-3 py-2">Каждый защищённый маршрут</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2"><strong>Вход validate()</strong></td>
        <td class="border border-border px-3 py-2">username, password</td>
        <td class="border border-border px-3 py-2">декодированный, проверенный payload</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2"><strong>Ходит в БД?</strong></td>
        <td class="border border-border px-3 py-2">Да — грузит юзера, сверяет хэш</td>
        <td class="border border-border px-3 py-2">Нет — доверяет claim'ам токена</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2"><strong>req.user становится</strong></td>
        <td class="border border-border px-3 py-2">загруженным юзером (без хэша)</td>
        <td class="border border-border px-3 py-2">тем, что вернёт validate(payload)</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2"><strong>Guard</strong></td>
        <td class="border border-border px-3 py-2">AuthGuard('local')</td>
        <td class="border border-border px-3 py-2">AuthGuard('jwt'), часто глобальный</td>
      </tr>
    </tbody>
  </table>
</div>

<Inset kind="why" lang="ru">
Почему JWT подписан, но не зашифрован? Токен — это три base64url-части (header, payload, signature), соединённые точками. Base64url — это кодирование, а не шифрование: любой, у кого есть токен, может декодировать payload и прочитать каждый claim открытым текстом (вставь любой в jwt.io и посмотри). Подпись не прячет payload; она лишь доказывает, что payload не подменили и что его выпустил держатель секрета. Так что JWT гарантирует целостность и подлинность, но никогда конфиденциальность. Никогда не клади в claim'ы пароль, секрет, PII или что-либо, что ты не написал бы на открытке, — клади непрозрачный id пользователя, а остальное сервер пусть подтянет сам.
</Inset>

<TradeoffMatrix
  id="nest-05-auth-01-passport-and-jwt-tradeoff-1"
  lessonSlug="01-passport-and-jwt"
  lang="ru"
  prompt="Ограничение: SaaS-дашборд, где админ должен иметь возможность УБИТЬ скомпрометированную сессию за секунды, трафик высокий, и ты гоняешь несколько stateless API-инстансов за балансировщиком. Какая модель auth подходит?"
  options={[
    { name: "Короткоживущий JWT-access-токен (~15m) + более долгий ротируемый refresh-токен, refresh в httpOnly-cookie и сверяемый с серверным хранилищем", summary: "Лучшее попадание.", correct: true, justification: "Это ровно та модель, которую просит ограничение. Access-токен stateless, поэтому масштабируется по инстансам без общего session-хранилища на горячем пути, а его короткий TTL ограничивает окно ущерба минутами. Отзыв живёт на refresh-токене: он хранится на сервере (строка или Redis-ключ), ротируется при каждом использовании и может быть удалён мгновенно, чтобы убить сессию, — следующий refresh падает, и access-токен умирает за минуты. Ты получаешь statelessness на чтениях И почти мгновенный отзыв, чего не даёт по отдельности ни один из чистых подходов." },
    { name: "Серверная сессия с cookie session-id, строка сессии в Redis, сверяемая на каждом запросе", summary: "Мгновенный отзыв ценой запроса.", justification: "Отзыв тривиален — удали строку сессии, и самый следующий запрос отклонён. Но каждый запрос теперь делает round-trip в Redis — это и есть per-request stateful-ность, которой ограничение пытается избежать на высоком трафике, и она связывает каждый API-инстанс с session-хранилищем. Это валидная модель, просто не та stateless-масштабируемая, которую просят; разделение access+refresh держит чтения stateless, при этом всё равно позволяя мгновенно убить." },
    { name: "Один долгоживущий JWT (например, 30–90 дней), без refresh-токена, без серверного состояния", summary: "Баг из хука.", justification: "Это ровно находка пентеста из хука. Подписанный JWT нельзя un-issue — здесь нет ни logout, ни denylist — так что скомпрометированный токен или токен бывшего сотрудника остаётся валидным неделями, что бы ты ни удалил из базы. Масштабируется прекрасно, отзывается никогда. Длинный TTL — это и есть вся уязвимость; требование «убить за секунды» делает этот вариант дисквалифицирующим." },
  ]}
/>

<Quiz
  id="nest-05-auth-01-passport-and-jwt-quiz-1"
  lessonSlug="01-passport-and-jwt"
  lang="ru"
  question="После того как запрос прошёл глобальный JwtAuthGuard, откуда берётся значение в req.user?"
  choices={[
    { label: "Из того, что вернёт JwtStrategy.validate(payload) — выведено из проверенных claim'ов токена, без чтения базы", correct: true },
    { label: "Из свежего обращения к базе, которое guard делает на каждом запросе", misconception: "JwtStrategy не ходит в базу — в этом и смысл stateless-JWT. Она верифицирует подпись и срок жизни, затем validate(payload) формирует req.user чисто из claim'ов токена. Если нужны свежие данные из БД, обращение надо добавить самому внутри validate()." },
    { label: "Из LocalStrategy.validate(), которая отработала на login", misconception: "LocalStrategy работает только на POST /login, чтобы выдать токен; на защищённых маршрутах её в цепочке нет. Там req.user наполняет именно JwtStrategy.validate(payload)." },
    { label: "Он пуст — JWT-guard лишь пропускает или отклоняет и никогда не ставит req.user", misconception: "Guard делает и то, и другое: он пропускает запрос И прикрепляет принципала. Passport ставит req.user равным возвращаемому значению validate() стратегии, так что JwtStrategy.validate(payload) — это ровно то, что ты читаешь." },
  ]}
/>

<Quiz
  id="nest-05-auth-01-passport-and-jwt-quiz-2"
  lessonSlug="01-passport-and-jwt"
  lang="ru"
  question="Ты регистрируешь JwtAuthGuard глобально через APP_GUARD. Что должен нести POST /login и почему?"
  choices={[
    { label: "@Public(), чтобы Reflector-проверка guard'а закоротила цепочку и пропустила JWT-верификацию — иначе ты бы вообще не смог залогиниться и получить токен", correct: true },
    { label: "Ничего — глобальные guard'ы автоматически исключают маршруты с именем /login", misconception: "В Nest нет исключения по имени; глобальный guard применяется к каждому маршруту единообразно. /login потребовал бы валидный JWT раньше, чем он у тебя есть, — тупик «курица и яйцо». Маршрут выводят из-под guard явно через @Public()." },
    { label: "Второй @UseGuards(JwtAuthGuard), чтобы перекрыть глобальный", misconception: "Дублирование того же guard'а его не ослабляет — он всё равно требует токен. Чтобы пропустить JWT-верификацию, маршрут помечают метаданными public, которые guard читает через Reflector, и затем возвращают для него true." },
    { label: "@UseGuards(LocalAuthGuard) вместо этого, что снимает глобальный JWT-guard на этом маршруте", misconception: "LocalAuthGuard запускает LocalStrategy, но глобальный JwtAuthGuard всё равно тоже отработает, пока маршрут не помечен public, — применятся оба. /login нужны и LocalAuthGuard, И @Public(), чтобы JWT-guard его пропустил, пока LocalStrategy проверяет учётные данные." },
  ]}
/>

</Explanation>

<KeyTakeaway>LocalStrategy проверяет данные на /login; JwtStrategy верифицирует Bearer-токен на защищённых маршрутах. Глобальный JWT-guard (APP_GUARD) fail-closed; @Public() выводит маршруты. JWT подписан, а не зашифрован.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="nest-05-auth-01-passport-and-jwt-retrieval"
  lang="ru"
  questions={[
    { q: "Пройди полный поток Passport + JWT от login до защищённого запроса, называя каждую стратегию, что делает её validate() и что оказывается в req.user.", a: "Login: клиент POST-ит логин и пароль на /login, который за LocalAuthGuard (AuthGuard('local')). Тот запускает LocalStrategy.validate(username, password): грузит пользователя, bcrypt-сравнивает пароль с сохранённым хэшем и возвращает пользователя без хэша — Passport кладёт это в req.user. Хендлер /login затем вызывает jwtService.sign(payload) с непрозрачным payload вроде { sub: userId, username } и возвращает access_token; он никогда не кладёт в payload пароль или секрет. Защищённый запрос: клиент шлёт Authorization: Bearer <token>. Маршрут за JwtAuthGuard (AuthGuard('jwt')), обычно привязанным глобально через APP_GUARD. Тот запускает JwtStrategy, настроенную с jwtFromRequest: ExtractJwt.fromAuthHeaderAsBearerToken(), secretOrKey равным тому же JWT_SECRET, которым подписывали, и ignoreExpiration: false. Passport сначала верифицирует подпись и срок жизни; только потом запускается validate(payload), возвращая, например, { userId: payload.sub, username } — и это становится req.user, выведенным из claim'ов токена без чтения базы. Маршрут с @Public() guard пропускает по Reflector-проверке." },
    { q: "Почему JWT — это не сессия, из которой можно выйти, и как access + refresh токены это решают?", a: "JWT подписан, а не сохранён: сервер не держит о нём записи, поэтому не может его un-issue. Верификация проверяет лишь подпись и claim exp — никто не сверяется с серверным списком — так что валидный токен остаётся валидным до истечения, что бы ты ни удалил из базы. Это и есть проблема отзыва; длинный TTL (например, 90 дней) превращает утёкший токен в недели доступа без kill-switch. Решение — два токена. Access-токен — короткоживущий JWT (~15m), используемый на каждом запросе, хранимый в памяти; его короткий TTL ограничивает окно ущерба. Refresh-токен — более долгоживущий, ротируется при каждом использовании и хранится и на клиенте в httpOnly-cookie, и на сервере (строка или Redis-ключ), так что его МОЖНО отозвать: удаление сохранённого refresh-токена означает, что следующий refresh падает, и доступ умирает в пределах TTL access-токена. Можно также держать короткий denylist id access-токенов для экстренного отзыва. Итог: stateless, масштабируемые чтения по access-токену плюс почти мгновенный отзыв через refresh-токен." },
  ]}
/>

<Recap lang="ru">
Аутентификация в Nest — это два момента, каждый моделируется стратегией Passport, чьё возвращаемое из validate() значение становится req.user. LocalStrategy работает один раз за LocalAuthGuard на POST /login: грузит пользователя, сравнивает пароль с bcrypt-хэшем и возвращает пользователя; хендлер затем подписывает JWT через jwtService.sign(payload), кладя в него только непрозрачный id и несекретные claim'ы. AuthModule импортирует PassportModule и JwtModule.registerAsync, подтягивая секрет и signOptions.expiresIn из ConfigService, так что ключ подписи живёт в окружении. JwtStrategy верифицирует каждый защищённый запрос: ExtractJwt.fromAuthHeaderAsBearerToken() достаёт Bearer-токен, secretOrKey обязан совпадать с секретом подписи, ignoreExpiration остаётся false, чтобы enforce-ить TTL, а validate(payload) формирует req.user из проверенных claim'ов без чтения базы. Привяжи JwtAuthGuard глобально через APP_GUARD, чтобы каждый маршрут был защищён по умолчанию — fail closed — и выводи маршруты из-под него декоратором @Public(), который guard читает через Reflector. Сеньорская осторожность: JWT подписан, а не зашифрован, так что любой может base64-декодировать payload — никогда не клади секреты или PII в claim'ы — и подписанный токен нельзя un-issue, так что отзыв идёт от короткоживущих access-токенов плюс ротируемого, отслеживаемого на сервере refresh-токена, а не от долгоживущего JWT, который живёт месяцами без kill-switch. Теперь, когда ты увидишь в отчёте пентеста «токен всё ещё валиден после отзыва доступа», ты знаешь, где искать: expiresIn слишком длинный, нет ротации refresh, нет инверсии @Public() — и знаешь фикс ещё до начала постмортема.
</Recap>
