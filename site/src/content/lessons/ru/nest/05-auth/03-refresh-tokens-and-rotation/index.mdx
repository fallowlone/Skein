---
concepts:
- access-vs-refresh
- rotation
- reuse-detection
- token-family
- hashed-storage
deepensInto: []
estMin: 18
lang: ru
lessonType: topic
level: senior
order: 3
prereqs: []
slug: "03-refresh-tokens-and-rotation"
sources:
- https://docs.nestjs.com/security/authentication
- https://auth0.com/docs/secure/tokens/refresh-tokens/refresh-token-rotation
spiral: []
status: ready
summary: 'JWT access-токен нельзя отозвать до exp, поэтому держи его коротким (~15 мин) и сочетай с долгим refresh-токеном, который ротируется одноразово. Повтор израсходованного refresh-токена значит, что им владеют двое — отзови всю семью и заставь переавторизацию.'
title: 'Refresh-токены, ротация и обнаружение повторного использования'
track: nest
unit: 05-auth
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";
import Infographic from "~/components/diagram/Infographic.astro";
import twoTokenDesign from "./infographics/two-token-design.svg?raw";

<Hook>
В постмортеме инцидента была одна строка, от которой комната замерла: «у атакующего была валидная сессия 26 дней». После первого часа никого больше не фишили. Произошло проще и хуже — refresh-токен утёк через лог-сток, захватывавший тела запросов, а время жизни токена было 30 дней. Каждый раз, когда access-токен атакующего истекал, тот тихо POST-ил тот же самый refresh-токен на `/auth/refresh` и получал назад свежий 15-минутный access-токен. Ни одна тревога не сработала, потому что в этом запросе ничего не выглядело неправильно: валидный токен обменивался ровно так, как задумано. Refresh-токен был 30-дневной отмычкой, и у системы не было способа заметить, что им пользуются двое сразу. Этот урок — про жизненный цикл токенов, который закрывает эту дыру: почему ты носишь два токена, почему долгоживущий обязан ротироваться при каждом использовании и как ротация превращает украденный токен в растяжку вместо отмычки.
</Hook>

<Crux>JWT нельзя отозвать до exp — держи access-токены короткими. Добавь одноразовый ротируемый refresh-токен; повтор израсходованного — кража.</Crux>

<Explanation>

## Почему два токена: проблема отзыва

JWT access-токен **самодостаточен**: сервер проверяет его подпись и доверяет клеймам внутри без обращения к базе. Эта без-состоятельность и есть весь смысл — она масштабируется, и любой сервис с публичным ключом может его валидировать. Но у неё есть жёсткое следствие: ты **не можешь отозвать JWT до его `exp`**. Нет серверной записи, которую можно удалить; токен валиден до истечения, и точка. Так что если он утёк, твой единственный рычаг — насколько коротким ты сделал `exp`.

Это диктует дизайн. Держи access-токен **коротким — ~15 минут** — чтобы украденный был 15-минутной проблемой, а не вечной. Но нельзя заставлять пользователей логиниться каждые 15 минут, поэтому ты сочетаешь его с **refresh-токеном** — долгоживущим (~7–30 дней) — чья единственная работа в том, чтобы быть обменянным на `/auth/refresh` на новый access-токен.

```ts
import { Injectable } from '@nestjs/common';
import { JwtService } from '@nestjs/jwt';

@Injectable()
export class TokenService {
  constructor(private readonly jwt: JwtService) {}

  // two tokens, two secrets, two very different TTLs
  async issuePair(userId: string, familyId: string) {
    const accessToken = await this.jwt.signAsync(
      { sub: userId },
      { secret: process.env.ACCESS_SECRET, expiresIn: '15m' },   // short: caps theft blast radius
    );
    const refreshToken = await this.jwt.signAsync(
      { sub: userId, familyId },
      { secret: process.env.REFRESH_SECRET, expiresIn: '30d' },  // long: avoids constant re-login
    );
    return { accessToken, refreshToken };
  }
}
```

Refresh-токен теперь — чувствительная учётка: он живёт неделями и чеканит access-токены по требованию. Поэтому он обязан быть **серверно-отслеживаемым** (в отличие от без-состоятельного access-токена) и никогда не должен касаться доступного из JS хранилища. Он путешествует как **httpOnly, Secure, SameSite cookie**: httpOnly держит его вне `document.cookie`, чтобы XSS не мог его прочитать, Secure прикрепляет его к HTTPS, SameSite притупляет CSRF. Access-токен может лежать в памяти; refresh-токен сидит в закалённой cookie.

<Infographic
  svg={twoTokenDesign}
  label="Сравнение двух токенов бок о бок. Access-токен короткоживущий (TTL ~15 минут), самодостаточный stateless JWT, проверяемый по подписи без запроса в БД, его нельзя отозвать до exp, и он живёт в памяти. Refresh-токен долгоживущий (TTL 7-30 дней), отслеживается на сервере через запись семьи, ротируется одноразово и отзываем, путешествует в httpOnly Secure SameSite cookie, а база хранит только его SHA-256-хеш."
  caption="Две учётки с намеренно противоположными свойствами: короткий, без состояния, неотзываемый access-токен и долгий, отслеживаемый, ротируемый refresh-токен в закалённой cookie, хранимый хешем."
  hue="var(--d-infra)"
/>

## Ротация: сделай каждый refresh одноразовым

Наивный дизайн возвращает **тот же** refresh-токен на всю его 30-дневную жизнь. Это ровно та отмычка из инцидента: одна утечка покупает 30 дней тихого доступа. **Ротация** чинит проблему времени жизни: при каждом вызове `/auth/refresh` ты **выпускаешь совершенно новый refresh-токен и инвалидируешь старый**. Каждый refresh-токен одноразовый.

```ts
async refresh(presentedToken: string) {
  const record = await this.findFamilyRecord(presentedToken); // looks up by hash (below)
  if (!record || record.revoked) throw new UnauthorizedException();

  // ROTATION: the old token is now spent; mint a fresh pair on the same family
  await this.markConsumed(record.id);
  return this.issuePair(record.userId, record.familyId);
}
```

Ротация делает две вещи. Она **сжимает окно валидности** — утёкший refresh-токен годен только до следующего refresh легитимного клиента, часто минуты, а не недели. И, что критично, она даёт тебе **сигнал обнаружения**: раз токен ротирован, предъявление его снова аномально, а на аномалии ты можешь реагировать.

## Обнаружение повторного использования: израсходованный токен — это растяжка

Вот основной механизм. Ротированный refresh-токен одноразовый, так что его никогда не должны увидеть снова. Если **израсходованный** refresh-токен *всё же* предъявлен второй раз, есть только одно объяснение: **им владеют двое** — легитимный клиент и вор. Утечка уже случилась; повторное предъявление — это система, говорящая тебе об этом.

Сложность в том, что ты **не можешь сказать, кто из них кто**. Запрос от атакующего и запрос от жертвы выглядят одинаково — оба несут когда-то валидный токен. Так что ты делаешь единственное безопасное допущение: **компрометация**. Ты **отзываешь всю семью токенов** — каждый refresh-токен, происходящий от исходного логина, — что заставляет полную переаутентификацию.

Моделируй это как семью. Каждый логин начинает **семью** (`familyId`); каждая ротация прицепляет следующий токен к той же семье. Запись семьи отслеживает, какой токен сейчас жив и отозвана ли семья.

```ts
// При refresh: если предъявленный токен УЖЕ был использован — это повторное использование → убить семью
async refresh(presentedToken: string) {
  const record = await this.findByHash(this.hash(presentedToken));
  if (!record) throw new UnauthorizedException();

  if (record.consumed) {
    // REUSE DETECTED: a single-use token came back. Two holders exist.
    await this.revokeFamily(record.familyId);  // nuke every descendant of this login
    throw new UnauthorizedException('token reuse detected');
  }

  await this.markConsumed(record.id);
  const familyId = record.familyId;
  return this.issuePairAndStore(record.userId, familyId);
}
```

Теперь переиграй инцидент с ротацией на месте. Атакующий крадёт refresh-токен и использует его, чтобы отчеканить access-токен — ладно, он ротировал его, теперь держит токен *v2*. Позже **настоящий пользователь**, его клиент, делает refresh с токеном, который *он* всё ещё держит (тот, что атакующий уже израсходовал, *v1*). Сервер видит израсходованный токен, предъявленный снова → **повтор обнаружен** → он **отзывает всю семью**. И *v2* атакующего, и любой будущий refresh умирают. Пользователя выбрасывает на экран логина, он переаутентифицируется и начинает свежую семью; атакующий заблокирован. Окно в 26 дней схлопывается до «до следующего refresh пользователя».

<FlowDiagram
  label="Жизненный цикл семьи refresh-токенов с обнаружением повторного использования. Логин создаёт семью токенов и выпускает access-токен (15 минут) плюс одноразовый refresh-токен. Каждый вызов слэш auth слэш refresh расходует текущий refresh-токен и ротирует новый на ту же семью. Если токен, который уже был израсходован, предъявлен снова — значит им владеют двое — сервер обнаруживает повтор и отзывает всю семью, заставляя каждого держателя переаутентифицироваться со свежего логина."
  hue="var(--d-systems)"
  perRow={3}
  nodes={[
    { id: "login", label: "login", sub: "create family" },
    { id: "issue", label: "access 15m + refresh", sub: "single-use" },
    { id: "refresh", label: "/auth/refresh", sub: "rotate" },
    { id: "consume", label: "old token consumed", sub: "new minted" },
    { id: "reuse", label: "consumed token reused", sub: "two holders" },
    { id: "revoke", label: "revoke family", sub: "force re-auth" },
  ]}
  edges={[
    { from: "login", to: "issue", label: "issue pair" },
    { from: "issue", to: "refresh", label: "exp ~15m" },
    { from: "refresh", to: "consume", label: "rotate" },
    { from: "consume", to: "refresh", label: "loop" },
    { from: "consume", to: "reuse", label: "leak" },
    { from: "reuse", to: "revoke", label: "detected" },
  ]}
  caption="Логин открывает семью; каждый refresh ротирует одноразовый токен на неё. Повторное появление израсходованного токена значит, что им владеют двое — сервер отзывает всю семью и заставляет переаутентификацию, превращая утёкший токен в растяжку."
/>

<Inset kind="why" lang="ru">
Почему отзывать ВСЮ семью при повторе, а не только токен, который переиграли? Потому что раз одноразовый токен появился снова, у тебя есть доказательство, что утечка произошла, но нет способа отличить запрос атакующего от запроса жертвы — оба предъявляют когда-то валидный токен, и нет сигнала, который их различал бы. Если отозвать только предъявленный токен, ты можешь отозвать копию жертвы и оставить атакующего с только что ротированным — ты заблокируешь легитимного пользователя и оставишь вора залогиненным, ровно наоборот тому, что ты хочешь. Единственное безопасное допущение — что вся цепочка, происходящая от того логина, скомпрометирована, так что ты отзываешь каждый токен в семье и заставляешь чистую переаутентификацию. Это намеренный размен: один утёкший токен стоит пользователю единственного релогина, а взамен атакующий гарантированно выкинут.
</Inset>

## Хранение: хешируй refresh-токен, никогда не храни сырым

Запись семьи живёт в твоей базе — но ты **не** должен хранить в ней сырой refresh-токен. Если бы хранил, утечка базы (дамп, бэкап, оставленный в бакете, read-only SQLi) вручила бы атакующему каждый живой refresh-токен в открытом виде: мгновенный угон аккаунтов в масштабе, без подделки подписи. Вместо этого **храни только хеш** refresh-токена — argon2/bcrypt или простой SHA-256, раз токен уже высокоэнтропийный — ровно как хранил бы пароль. При refresh хешируй предъявленный токен и сравнивай с сохранённым значением.

```ts
import { createHash } from 'node:crypto';

// the token is high-entropy random already, so a fast SHA-256 is fine here
private hash(token: string): string {
  return createHash('sha256').update(token).digest('hex');
}

private async issuePairAndStore(userId: string, familyId: string) {
  const { accessToken, refreshToken } = await this.tokens.issuePair(userId, familyId);
  await this.familyRepo.save({
    familyId,
    userId,
    tokenHash: this.hash(refreshToken), // store the HASH, never the raw token
    consumed: false,
    revoked: false,
  });
  return { accessToken, refreshToken };
}
```

Обвязка в Nest зеркалит access-путь, но остаётся отдельной: **`JwtRefreshStrategy`** (отдельная от access-овой `JwtStrategy`) читает refresh-токен из cookie и валидирует его подпись refresh-секретом; эндпоинт `/auth/refresh` ограждён ею; а сервис делает stateful-часть — ищет семью по хешу, проверяет `consumed`/`revoked`, ротирует, сохраняет новый хеш, возвращает новую пару. `@nestjs/jwt` подписывает оба токена, но **разными секретами и разными TTL**, так что access-токен никогда не может быть переигран как refresh-токен и наоборот.

<TradeoffMatrix
  id="nest-05-auth-03-refresh-tokens-and-rotation-tradeoff-1"
  lessonSlug="03-refresh-tokens-and-rotation"
  lang="ru"
  prompt="Refresh-токен неизбежно однажды утечёт — через лог-сток, XSS-баг или украденное устройство. Как ограничить ущерб от украденного refresh-токена?"
  options={[
    { name: "Ротируй одноразово при каждом refresh, обнаруживай повтор израсходованного токена и отзывай всю семью; неси refresh-токен в httpOnly Secure SameSite cookie; храни в БД только его хеш", summary: "Ротация + обнаружение повтора.", correct: true, justification: "Это многослойный ответ. Ротация сжимает валидность утёкшего токена до «до следующего легитимного refresh» вместо полных 7–30 дней. Обнаружение повтора превращает утечку в растяжку: когда израсходованный токен появляется снова (двое держателей), отзыв всей семьи выкидывает атакующего и заставляет пользователя переаутентифицироваться. httpOnly/Secure/SameSite cookie держит токен вне доступного из JS хранилища (XSS) и вне не-HTTPS. Хранение только хеша значит, что утечка БД не даёт пригодных токенов. Каждый слой покрывает свой путь утечки, а вместе они превращают украденный refresh-токен из отмычки в самоотзывающийся инцидент." },
    { name: "Используй один долгоживущий не-ротирующийся JWT как refresh-токен", summary: "Отмычка.", justification: "Это и есть инцидент. Не-ротирующийся refresh-токен валиден всю свою 7–30-дневную жизнь, а раз JWT нельзя отозвать до exp, утечка покупает атакующему недели тихого доступа — он просто продолжает обменивать тот же токен на свежие access-токены, и ничто не выглядит аномально, потому что токен по-настоящему валиден. Без ротации нет сигнала повтора, так что система никогда не сможет заметить, что им владеют двое. Это ровно та отмычка, ради устранения которой этот урок существует." },
    { name: "Пропусти refresh-токены вовсе; выдавай только access-токен с TTL 24 часа", summary: "Долгий радиус поражения, без выигрыша в UX.", justification: "Это худшее из обоих миров. 24-часовой access-токен тоже нельзя отозвать до exp, так что украденный — это однодневная отмычка, гораздо хуже 15-минутного радиуса, который покупает дизайн с двумя токенами. И ты ничего не получаешь взамен: пользователей всё равно выбросит через 24 часа, так что ты даже не купил бесшовную долгую сессию, ради которой refresh-токены существуют. Короткий access + ротирующийся refresh доминирует над этим и по безопасности, и по UX." },
    { name: "Храни refresh-токен в localStorage, чтобы SPA мог прикреплять его к запросам", summary: "Читаемо из XSS.", justification: "localStorage читается любым JavaScript на странице, так что один XSS-баг — или вредоносная зависимость — эксфильтрирует долгоживущий refresh-токен напрямую, а это самая ценная учётка, что у тебя есть. httpOnly cookie существует именно чтобы держать refresh-токен вне досягаемости JS. Этот выбор максимизирует поверхность утечки для единственного токена, который тебе нужнее всего защитить; это противоположность закалённому хранению, которое использует верный ответ." },
  ]}
/>

<Quiz
  id="nest-05-auth-03-refresh-tokens-and-rotation-quiz-1"
  lessonSlug="03-refresh-tokens-and-rotation"
  lang="ru"
  question="Почему access-токен держат коротким (~15 мин) вместо того, чтобы просто выдать один долгоживущий JWT?"
  choices={[
    { label: "Потому что JWT нельзя отозвать до его exp, так что короткий TTL — единственный рычаг, ограничивающий радиус поражения украденного access-токена", correct: true },
    { label: "Потому что короткие токены меньше на проводе и снижают размер запроса", misconception: "TTL не имеет отношения к размеру токена — expiresIn это клейм, а не объём payload. Причина короткого TTL — отзыв: самодостаточный JWT валиден до exp без серверной записи для удаления, так что украденный нельзя отозвать раньше. Сделать exp коротким (~15 мин) — единственный способ ограничить, как долго работает утёкший access-токен." },
    { label: "Потому что сервер ре-валидирует короткий токен против базы на каждом запросе", misconception: "Весь смысл JWT access-токена в том, что он самодостаточен и проверяется подписью БЕЗ обращения к базе — эта без-состоятельность и есть причина, почему он масштабируется. Нет ни одной per-запросной проверки в базу, против которой можно отозвать; именно поэтому ты не можешь отозвать его до exp и вместо этого должен держать TTL коротким." },
    { label: "Потому что короткий access-токен можно мгновенно отозвать, когда пользователь выходит", misconception: "JWT access-токен как раз НЕЛЬЗЯ отозвать до его exp — нет серверной записи, которую можно инвалидировать. Logout отзывает refresh-токен (он серверно-отслеживаемый), но любой уже выпущенный access-токен остаётся валиден до истечения. Короткий TTL — это то, что ограничивает эту остаточную валидность." },
  ]}
/>

<Quiz
  id="nest-05-auth-03-refresh-tokens-and-rotation-quiz-2"
  lessonSlug="03-refresh-tokens-and-rotation"
  lang="ru"
  question="Refresh-токен, который уже был ротирован (израсходован), предъявлен на /auth/refresh второй раз. Что должен сделать сервер?"
  choices={[
    { label: "Трактовать это как повтор: отозвать всю семью токенов, происходящую от того логина, и заставить переаутентификацию", correct: true },
    { label: "Всё равно выпустить свежую пару — токен был валиден однажды, так что уважь его", misconception: "Ротированный токен одноразовый; увидеть его снова значит, что им владеют ДВОЕ (легитимный клиент и вор), а это доказательство утечки. Уважить его — значит отчеканить токены атакующему. Верный ответ — допустить компрометацию и отозвать всю семью, заставив переаутентификацию, — это и есть весь смысл обнаружения повтора." },
    { label: "Отозвать только переигранный токен и дать остальным токенам семьи продолжать работать", misconception: "Ты не можешь отличить запрос атакующего от запроса жертвы — оба предъявляют когда-то валидный токен. Отзыв только предъявленного токена рискует убить копию жертвы, оставив атакующего с только что ротированным. Единственный безопасный ход — отозвать всю семью, чтобы ни один держатель не мог продолжить, затем заставить чистый релогин." },
    { label: "Молча проигнорировать запрос и вернуть ранее выпущенный access-токен", misconception: "Нет никакого «ранее выпущенного access-токена», который можно вернуть, а молчаливое игнорирование события повтора выбрасывает единственный сигнал, что утечка произошла. Повторное появление израсходованного токена — индикатор активной компрометации; сервер должен реагировать на это отзывом семьи, а не проглатывать." },
  ]}
/>

</Explanation>

<KeyTakeaway>Держи access-токены ~15 мин (JWT нельзя отозвать до exp); сочетай с 7–30-дневным refresh-токеном: одноразовая ротация, httpOnly cookie, хранение хешем. Повтор израсходованного = двое держателей = отзови всю семью.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="nest-05-auth-03-refresh-tokens-and-rotation-retrieval"
  lang="ru"
  questions={[
    { q: "Почему ты носишь два токена и каковы верные время жизни, хранение и свойства каждого?", a: "Ты носишь два токена, потому что JWT access-токен самодостаточен и проверяется подписью без обращения к базе, а значит его НЕЛЬЗЯ отозвать до exp — нет серверной записи для удаления. Единственный рычаг против украденного access-токена — короткий TTL, так что ты держишь его коротким (~15 мин), чтобы ограничить радиус поражения, и он может жить в памяти. Но нельзя заставлять пользователей логиниться каждые 15 минут, поэтому ты сочетаешь его с долгоживущим refresh-токеном (~7–30 дней), чья единственная работа — быть обменянным на /auth/refresh на новый access-токен. Refresh-токен теперь чувствительная долгоживущая учётка, так что он обязан быть серверно-отслеживаемым (в отличие от без-состоятельного access-токена) и никогда не должен касаться доступного из JS хранилища: он путешествует как httpOnly (XSS не прочитает), Secure (только HTTPS), SameSite (защита от CSRF) cookie. В БД ты хранишь только ХЕШ refresh-токена (argon2/bcrypt или SHA-256), никогда сырой токен, так что утечка базы не даёт пригодных токенов. @nestjs/jwt подписывает оба разными секретами и TTL, так что ни один нельзя переиграть как другой." },
    { q: "Пройди ротацию и обнаружение повтора: что делает ротация, что значит переигранный израсходованный токен и почему ты отзываешь всю семью.", a: "Ротация значит, что каждый вызов /auth/refresh выпускает совершенно новый refresh-токен и инвалидирует старый, так что каждый refresh-токен одноразовый. Это сжимает окно, в котором утёкший refresh-токен валиден — только до следующего refresh легитимного клиента, часто минуты вместо недель — и даёт тебе сигнал обнаружения, потому что ротированный токен никогда не должны увидеть снова. Обнаружение повтора — это механизм: если ИЗРАСХОДОВАННЫЙ refresh-токен предъявлен второй раз, им владеют двое (легитимный клиент и вор), а это доказательство утечки. Ты не можешь сказать, какой запрос атакующего, а какой жертвы — оба несут когда-то валидный токен — так что ты делаешь единственное безопасное допущение, компрометацию, и отзываешь ВСЮ семью токенов (каждый refresh-токен, происходящий от исходного логина, сцепленный по familyId), заставляя переаутентификацию. Отзыв только переигранного токена рискует убить копию жертвы и оставить атакующему только что ротированный. Переигрыш военной истории: атакующий крадёт refresh-токен и ротирует его; настоящий пользователь позже делает refresh теперь-израсходованным токеном → повтор обнаружен → семья отозвана → оба разлогинены, атакующий заблокирован, пользователь переаутентифицируется. Без ротации + обнаружения повтора украденный refresh-токен — это многонедельная отмычка." },
  ]}
/>

<Recap lang="ru">
Жизненный цикл токенов существует, чтобы решить один жёсткий факт: самодостаточный JWT access-токен нельзя отозвать до его exp, потому что нет серверной записи для удаления. Так что ты держишь access-токен коротким (~15 мин), чтобы ограничить радиус поражения кражи, и сочетаешь его с долгоживущим refresh-токеном (~7–30 дней), который обменивается на /auth/refresh на новые access-токены — refresh-токен при этом чувствительная учётка, носимая в httpOnly, Secure, SameSite cookie и хранимая в БД только как хеш, так что утечка не даёт ничего пригодного. Ротация делает каждый refresh одноразовым: каждый вызов выпускает новый refresh-токен и инвалидирует старый, сжимая валидность утёкшего токена до минут и создавая сигнал обнаружения. Обнаружение повтора — основной механизм: израсходованный одноразовый токен, предъявленный снова, значит, что им владеют двое, а раз ты не можешь отличить атакующего от жертвы, ты допускаешь компрометацию и отзываешь всю семью токенов (всех потомков того логина, сцепленных по familyId), заставляя переаутентификацию. В NestJS это отдельная JwtRefreshStrategy, читающая cookie, ограждённый эндпоинт /auth/refresh и сервис, который валидирует хеш предъявленного токена против записи семьи, ротирует и возвращает новую пару — с @nestjs/jwt, подписывающим оба токена разными секретами и TTL. Итоговый эффект: ротация плюс обнаружение повтора превращают украденный refresh-токен из многонедельной отмычки в самоотзывающуюся растяжку.
</Recap>
