---
concepts:
- error-tracking
- correlation
- alert-fatigue
- pii-scrubbing
- unhandled-rejection
deepensInto: []
estMin: 18
lang: ru
lessonType: topic
level: senior
order: 5
prereqs: []
slug: "05-error-tracking-and-correlation"
sources:
- https://docs.sentry.io/platforms/javascript/guides/nestjs/
- https://docs.nestjs.com/
spiral: []
status: ready
summary: 'Логи, метрики и трейсы — три стога сена, пока их не сошьёт общий traceId. Трекер ошибок группирует по fingerprint и дедуплицирует; вычищай PII перед отправкой; алертить надо по СКОРОСТИ ошибок, а не по счётчику, иначе канал замьютят и реальный сбой утонет в шуме.'
title: 'Трекинг ошибок, корреляция и усталость от алертов'
track: nest
unit: 07-errors-and-observability
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
Сбой платежей длился час, прежде чем кто-то заглянул. Не потому что не сработал алерт — алерты срабатывали постоянно. Этот канал `#alerts` орал неделями: флапающий таймаут стороннего сервиса, который ретраился и проходил, обработанный 404 от сканера, долбящего мёртвые роуты, порог по счётчику, который кто-то выставил во время инцидента и так и не подкрутил. Сотни сообщений в день, каждое технически реальная ошибка, каждое безопасно проигнорировать. Так что команда сделала рациональное: замьютила канал. Потом чекаут начал реально падать, алерт прилетел в тот же замьюченный канал и выглядел ровно как шум, который все уже приучили себя не замечать. У тебя крутилось три системы наблюдаемости — логи, метрики, трейсы, всё из предыдущих четырёх уроков — и трекер ошибок поверх. Ничего не помогло, потому что сигнал был погребён под шумом на основе счётчика, и ничто не связывало куски воедино. Этот урок — про сшивку: как коррелировать три столпа по id, трекать ошибки так, чтобы они группировались, а не тонули, и алертить по скорости, которая будит человека только когда его стоит будить.
</Hook>

<Crux>Сшивай логи/трейсы/ошибки общим traceId; группируй ошибки по fingerprint; алертить по скорости/burn, а не по счётчику, иначе канал замьютят.</Crux>

<Explanation>

## Три столпа, один инцидент: корреляция по id

Логи говорят, *что случилось* (строка на событие), метрики говорят, *сколько / как быстро* (скорости и перцентили из L04), трейсы говорят, *где в дереве вызовов* живут время и сбои (спаны из L03). Каждое необходимо; ни одно само по себе не достаточно. То, что превращает три несвязанных стога сена в один поисковый инцидент, — это **общий ключ соединения**, проставленный на всех них: `traceId`, протянутый через запрос, плюс `requestId`, записанный в каждую строку лога, прикреплённый к каждому отчёту об ошибке и несомый трейсом.

```ts
// Корреляционный middleware/interceptor проставляет ключ соединения один раз, рано.
// (Конверт лога — L01; пропагация трейса — L03 — здесь мы лишь протягиваем ID.)
@Injectable()
export class CorrelationInterceptor implements NestInterceptor {
  intercept(ctx: ExecutionContext, next: CallHandler) {
    const req = ctx.switchToHttp().getRequest();
    // переиспользуй входящий трейс, если трейсер его задал, иначе сминть requestId
    const traceId = trace.getActiveSpan()?.spanContext().traceId;
    const requestId = req.headers['x-request-id'] ?? randomUUID();
    // сделай оба доступными логгеру, репортеру ошибок и нижестоящим вызовам
    return runWithContext({ traceId, requestId }, () => next.handle());
  }
}
```

С ключом на месте алерт становится **пивотом**, а не расследованием: ошибка в трекере → читаешь её `traceId` → открываешь ровно этот трейс → фильтруешь логи по этому одному `requestId`. Ты переходишь от «что-то не так» к «*этот* запрос, *этот* спан, *эти* строки лога» в три клика. Без ключа соединения у тебя поиск по логам, поиск по трейсам и список ошибок, которые не делят общего словаря, — три инструмента, три догадки про корреляцию по времени и куда более длинный час.

## Трекинг ошибок: группируй по fingerprint, не тони

Строка лога на ошибку топит. Сотое появление того же `NullPointerException` — это сотая идентичная строка, мимо которой ты проскролливаешь. **Трекер ошибок** (в стиле Sentry) переворачивает это: он вычисляет **fingerprint** из типа ошибки и кадров стека, затем *группирует* каждое появление с тем же fingerprint в одну **issue**, считает появления и захватывает контекст — release, пользователь, запрос, breadcrumbs — один раз на группу. Десять тысяч идентичных сбоев становятся одной issue со счётчиком 10 000, а не 10 000 строк.

В Nest глобальный exception filter, который ты построил в L01, — естественная точка репортинга: он уже ловит каждый throw внутри пайплайна запроса, так что это место, где ты отдаёшь ошибку трекеру с прикреплённым корреляционным контекстом.

```ts
@Catch()
export class SentryExceptionFilter implements ExceptionFilter {
  catch(exception: unknown, host: ArgumentsHost) {
    const { traceId, requestId } = getContext();
    Sentry.withScope((scope) => {
      scope.setTag('traceId', traceId);       // <- связывает эту issue с её трейсом
      scope.setTag('requestId', requestId);   // <- и с её точными строками лога
      scope.setUser({ id: getUserId() });     // кто наткнулся (только id, не PII)
      Sentry.captureException(exception);      // fingerprint + группировка Sentry
    });
    // ... всё ещё транслируй в HTTP-ответ, как в L01
  }
}
```

Два прикрепления отрабатывают своё место. **`release`** — SHA деплоя, заданный один раз на старте (`Sentry.init({ release })`) — делает так, что всплеск маппится на конкретный деплой: новая issue, появившаяся в ту минуту, когда уехал `a3f9c1`, — это регрессия, которую можно откатить. **`traceId`/`requestId`** делают каждую issue прыжком в один клик в трейс и логи — это корреляция из предыдущего раздела, сделанная конкретной.

## Ловушка необработанного rejection

У глобального filter жёсткая граница: **он ловит только throw'ы внутри пайплайна запроса.** Ошибка, брошенная out-of-band — необработанный promise rejection в `setInterval`-джобе, throw внутри обработчика EventEmitter, отклонённый promise, который никто не `await`нул — никогда не входит в путь обработки исключений Nest, так что никогда не доходит до filter и никогда не доходит до трекера. Она исчезает. На современном Node `unhandledRejection` может ещё и завершить процесс напрочь.

Боевая история: ночная джоба сверки делала `void doReconcile()` — fire-and-forget — и одной ночью `doReconcile` отклонилась на кривой строке. Глобальный filter её не увидел (нет запроса), Sentry её не увидел (нет filter), и на Node 18 необработанный rejection положил worker-под. Под перезапустился, переретраил ту же плохую строку, снова отклонился, снова упал — тихий crash-loop, который крутился часами с нулём отчётов об ошибках, потому что единственное место, где собирались ошибки, было местом, до которого эта ошибка не могла дойти.

```ts
// Out-of-band ошибкам нужны обработчики уровня ПРОЦЕССА — filter их не видит.
process.on('unhandledRejection', (reason) => {
  Sentry.captureException(reason);           // зарепорти, а не потеряй
});
process.on('uncaughtException', (err) => {
  Sentry.captureException(err);
  Sentry.flush(2000).finally(() => process.exit(1)); // зарепорти, слей, потом выйди
});
```

Правило: глобальный filter покрывает пайплайн запроса; process-обработчики покрывают всё остальное. Тебе нужны *оба*, иначе целые классы сбоев невидимы.

<Inset kind="why" lang="ru">
Почему алертинг по сырому **счётчику** ошибок заставляет людей пропускать реальные сбои? Потому что счётчик смешивает ожидаемый, обработанный шум с настоящим сбоем, и у него нет базовой линии. Ровный ручеёк ретраенных таймаутов и сканерных 404 пейджит постоянно — канал наполняется ошибками, которые все «реальны» и все безопасны. Люди делают единственное, что можно с пожарным шлангом безопасных алертов: мьютят его. Потом реальный сбой приходит в тот же замьюченный канал, выглядя идентично шуму, который все уже приучили себя игнорировать. Алертинг по **скорости относительно SLO/бюджета ошибок** (из L04) чинит рамку: он отделяет «нормальный фоновый ручеёк» от «мы жжём бюджет быстрее, чем позволяет SLO», а это единственное, что реально стоит того, чтобы будить человека. Счётчик всегда был высоким; *скорость, пересекающая бюджет* — это новая информация.
</Inset>

## Усталость от алертов: алертить по сигналу, не по шуму

Режим отказа — алертить на *каждую* ошибку или на сырой *счётчик* ошибок. Оба генерируют шум: кратковременный всплеск, обработанный 404, ретраенный-затем-прошедший таймаут — каждый пейджит человека. Когда пейджит всё, люди мьютят, и мьют навсегда. У дисциплины четыре хода:

- **Алертить по скорости / burn SLO, не по счётчику** (L04). Burn-rate алерт срабатывает, когда ты потребляешь бюджет ошибок быстрее, чем терпит SLO, — он игнорирует ровный безопасный ручеёк и ловит реальный обрыв.
- **Отделяй «залогируй» от «дёрни человека».** Обработанные, ожидаемые условия (валидационный 400, известный ретраебельный таймаут) логируются и считаются, но никогда не пейджат. Пейджат только неожиданные, бьющие по пользователю сбои.
- **Задай severity.** Пейджи на SEV1/SEV2; всё остальное роуть на дашборд или в дайджест, а не на телефон человека в 3 ночи.
- **Используй группировку трекера.** Один *новый* fingerprint — это одно уведомление, а не 10 000. «Появилась новая issue» — сигнал; «существующая issue тикнула на единицу» обычно нет.

Вместе эти четыре хода превращают орущий канал в заслуживающий доверия: burn-rate отсекает безопасный ручеёк, дедуп убирает повторяющиеся краши — но без разделения severity первые три всё равно будут генерировать шум, который замьютят.

## PII и секреты: вычищай перед отправкой

Прежде чем подключать Sentry или любой другой трекер ошибок, спроси себя: что обычно лежит в теле запроса или заголовке Authorization в твоём приложении? Если там есть токены, email или любые пользовательские данные — а они почти всегда там есть — то отправить сырой запрос третьей стороне значит самому залогировать утечку. Контекст ошибки — это утечка, ждущая случиться. Breadcrumbs и снапшоты запроса могут захватить тела запросов, заголовки (включая `Authorization`!), email и токены — а трекер ошибок это **третья сторона**. Отправить ему сырой запрос может означать отправить живой bearer-токен или PII клиента твоему вендору ошибок. Это breach, залогированный тобой, нарочно. Ты обязан вычистить перед тем, как что-либо покинет процесс: хук `beforeSend`, который редактирует известно-чувствительные поля, deny-list для заголовков и срезание паттернов токенов/секретов.

```ts
Sentry.init({
  release: process.env.GIT_SHA,
  beforeSend(event) {
    // deny-list чувствительных заголовков ДО того, как событие покинет процесс
    const headers = event.request?.headers;
    if (headers) { delete headers['authorization']; delete headers['cookie']; }
    // отредактируй очевидные PII / секреты из тела
    if (event.request?.data) event.request.data = redactSecrets(event.request.data);
    return event;
  },
});
```

<TradeoffMatrix
  id="nest-07-errors-and-observability-05-error-tracking-and-correlation-tradeoff-1"
  lessonSlug="05-error-tracking-and-correlation"
  lang="ru"
  prompt="Твой канал #alerts настолько шумный, что команда его мьютит, и реальный сбой пропустили. Ты должен снизить шум, НЕ пропустив следующий реальный инцидент. Что меняешь?"
  options={[
    { name: "Алертить по burn rate бюджета ошибок SLO, группировать по fingerprint и разделить лог-vs-пейдж по severity", summary: "Алертить по сигналу.", correct: true, justification: "Это бьёт по всем трём причинам мьюта. Burn-rate алертинг (L04) игнорирует ровный безопасный ручеёк и срабатывает только когда ты потребляешь бюджет ошибок быстрее, чем позволяет SLO, — реальный обрыв. Группировка по fingerprint означает, что одна новая issue — это одно уведомление, а не 10 000 пейджей за один и тот же сбой. Разделение по severity держит обработанные/ожидаемые условия на дашборде или в дайджесте и пейджит только на бьющие по пользователю SEV1/SEV2. Канал затихает на шуме и громок на том, ради чего стоит просыпаться, так что следующий реальный сбой выделяется, а не сливается." },
    { name: "Алертить на каждое захваченное исключение, чтобы ничего не пропустить", summary: "Это И ЕСТЬ болезнь.", justification: "Пейджить на каждое исключение — это ровно то, из-за чего канал замьютили. Большинство исключений обработаны, ожидаемы или транзиентны — валидационный 400, сканерный 404, ретраенный-затем-прошедший таймаут. Пейджинг на все приучает людей игнорировать канал, а замьюченный канал пропускает всё, включая реальный сбой. «Никогда не пропущено» на бумаге становится «всегда проигнорировано» на практике." },
    { name: "Поднять порог сырого счётчика ошибок выше, чтобы он срабатывал реже", summary: "Не та ось.", justification: "Более высокий порог счётчика — это всё ещё счётчик: у него нет базовой линии, и он смешивает безопасный шум с реальным сбоем. Ты либо ставишь его низко и остаёшься шумным, либо ставишь высоко, и медленно-горящий сбой, который никогда не всплескивает абсолютный счётчик, проскальзывает под ним. Проблема не в значении порога, а в том, что алертишь по счётчику вообще, а не по скорости/burn относительно SLO." },
    { name: "Выключить алерты и поручить кому-то вручную смотреть дашборд", summary: "Бросает сигнал.", justification: "Отключить алерты и полагаться на человека, пялящегося в дашборд, означает, что ничто не пейджит в 3 ночи, когда сбой реально случается, и покрытие зависит от того, смотрит ли кто-то. Цель — меньше, но качественнее пейджей, а не ноль пейджей — ты хочешь, чтобы burn-rate алерт будил человека ровно когда бюджет горит, чего ручной просмотр гарантировать не может." },
  ]}
/>

<Quiz
  id="nest-07-errors-and-observability-05-error-tracking-and-correlation-quiz-1"
  lessonSlug="05-error-tracking-and-correlation"
  lang="ru"
  question="Ночная джоба делает `void doReconcile()`, и одной ночью promise отклоняется. У приложения есть глобальный exception filter, репортящий в Sentry. Почему этот rejection никогда не доходит до Sentry?"
  choices={[
    { label: "Глобальный filter оборачивает только пайплайн запроса; out-of-band rejection требует обработчика уровня процесса (process.on('unhandledRejection')), чтобы быть зарепорченным", correct: true },
    { label: "Sentry дедуплицирует его, потому что тот же fingerprint уже существует", misconception: "Дедупликация группирует появления в одну issue со счётчиком — она никогда не заставляет ошибку исчезнуть целиком. Rejection не подавлен группировкой; он вообще не доходит до Sentry, потому что глобальный exception filter ловит только throw'ы внутри пайплайна запроса, а fire-and-forget джоба бежит вне него. Нужен process.on('unhandledRejection')." },
    { label: "Filter его ловит, но beforeSend дропает его как PII", misconception: "beforeSend вычищает поля из события; он не удаляет молча целые ошибки и запускается только на событиях, которые реально доходят до Sentry. Эта ошибка вообще не доходит до filter или beforeSend — она брошена out-of-band, вне пайплайна запроса, который оборачивает filter, так что обработчики уровня процесса — единственное, что может её захватить." },
    { label: "Nest автоматически ретраит джобу, так что первый rejection проглатывается", misconception: "Nest не авто-ретраит fire-and-forget `void doReconcile()` — вокруг него нет жизненного цикла запроса, чтобы ретраить. Rejection просто необработан: глобальный filter его не видит (нет запроса), и без обработчика process.on('unhandledRejection') он теряется (а на современном Node может уронить процесс)." },
  ]}
/>

<Quiz
  id="nest-07-errors-and-observability-05-error-tracking-and-correlation-quiz-2"
  lessonSlug="05-error-tracking-and-correlation"
  lang="ru"
  question="Канал #alerts был замьючен неделями безопасных-но-реальных ошибок, и сбой платежей оставался незамеченным час. Какое изменение алертинга наиболее прямо это чинит?"
  choices={[
    { label: "Алертить по СКОРОСТИ ошибок / burn rate SLO, дедуплицировать по fingerprint и разделить severity, чтобы пейджили только бьющие по пользователю сбои", correct: true },
    { label: "Понизить порог счётчика ошибок, чтобы сбой пересёк его раньше", misconception: "Понижение порога счётчика делает канал ЕЩЁ шумнее, а это и вызвало мьют изначально. Проблема в том, что алертишь по счётчику вообще: он смешивает безопасные ретраенные таймауты с реальным сбоем. Burn-rate алерт относительно SLO плюс дедуп по fingerprint и разделение severity — вот что отделяет сбой от ручейка." },
    { label: "Перенести алерты во второй канал, чтобы шумный можно было оставить замьюченным", misconception: "Перемещение того же шума на основе счётчика просто создаёт второй канал, который тоже замьютят. Фикс не в том, куда падают алерты, а в том, ЧТО их триггерит: скорость/burn вместо счётчика, сгруппированные по fingerprint, с severity, чтобы пейджили человека только бьющие по пользователю сбои." },
    { label: "Пейджить на каждое новое исключение, чтобы сбой не проскользнул", misconception: "Пейджить на каждое исключение — первородный грех, замьютивший канал, ведь большинство исключений обработаны или транзиентны. Ты вернёшься к пожарному шлангу безопасных алертов, которые люди игнорируют. Цель — меньше, но сигнальнее пейджей: burn-rate алертинг плюс группировка по fingerprint плюс разделение severity." },
  ]}
/>

<FlowDiagram
  label="Пайплайн трекинга ошибок и корреляции, от начала до конца. Ошибка брошена; её ловит либо глобальный exception filter Nest (если случилось внутри пайплайна запроса), либо обработчик уровня процесса (process.on unhandledRejection / uncaughtException, для out-of-band throw'ов из джобы или таймера). Любой путь прогоняет ошибку через вычистку PII (beforeSend deny-листит заголовки Authorization и Cookie и редактирует секреты) перед тем, как что-либо покинет процесс. Вычищенное событие идёт в трекер ошибок, который группирует его по fingerprint и прикрепляет traceId и SHA релиза. Этот traceId позволяет коррелировать issue с её точным трейсом и строками лога. Наконец, алертинг следит за СКОРОСТЬЮ ошибок и burn SLO, а не за сырым счётчиком, так что только реальное прожигание бюджета пейджит человека."
  hue="var(--d-systems)"
  perRow={3}
  nodes={[
    { id: "throw", label: "ошибка брошена", sub: "в запросе или out-of-band" },
    { id: "catch", label: "filter + process-обработчики", sub: "покрывают оба пути" },
    { id: "scrub", label: "вычистка PII", sub: "beforeSend, deny-list" },
    { id: "track", label: "трекер ошибок", sub: "fingerprint + traceId + release" },
    { id: "correlate", label: "корреляция", sub: "трейс + логи по traceId" },
    { id: "alert", label: "алерт по скорости/burn", sub: "не сырой счётчик" },
  ]}
  edges={[
    { from: "throw", to: "catch", label: "лови оба" },
    { from: "catch", to: "scrub", label: "до отправки" },
    { from: "scrub", to: "track", label: "группа + тег" },
    { from: "track", to: "correlate", label: "ключ соединения" },
    { from: "correlate", to: "alert", label: "сигнал" },
  ]}
  caption="Лови и в-запросные, и out-of-band ошибки, вычищай PII перед тем, как они покинут процесс, группируй по fingerprint с ключом соединения traceId, коррелируй с трейсом и логами, затем алертить по скорости/burn, а не по счётчику, чтобы канал оставался достойным доверия."
/>

</Explanation>

<KeyTakeaway>Общий traceId сшивает логи/трейсы/ошибки. Группируй по fingerprint (дедуп); вычищай Authorization/PII перед отправкой; лови out-of-band throw'ы process-обработчиками; алертить по скорости/burn, не по счётчику.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="nest-07-errors-and-observability-05-error-tracking-and-correlation-retrieval"
  lang="ru"
  questions={[
    { q: "Объясни, как коррелировать три столпа наблюдаемости во время инцидента и почему группировка трекера по fingerprint лучше строки лога на ошибку.", a: "Логи говорят что случилось, метрики — сколько/как быстро, трейсы — где в дереве вызовов; три необходимых, но по отдельности недостаточных вида. Они становятся одним поисковым инцидентом только когда сшиты общим ключом: traceId, протянутым через запрос, плюс requestId, проставленным в каждую строку лога, прикреплённым к каждому отчёту об ошибке и несомым трейсом. С ключом алерт — это пивот, а не расследование: ошибка в трекере → читаешь её traceId → открываешь ровно этот трейс → фильтруешь логи по этому одному requestId, переходя от «что-то не так» к «этот запрос, этот спан, эти строки лога» в несколько кликов. Без него у тебя поиск по логам, поиск по трейсам и список ошибок без общего словаря. Трекер ошибок лучше строки-на-ошибку, потому что строка на ошибку топит: 10 000-й идентичный NullPointerException — ещё одна строка, мимо которой ты скроллишь. Трекер вычисляет fingerprint из типа ошибки и кадров стека, группирует каждое появление с этим fingerprint в одну issue со счётчиком и захватывает контекст (release, пользователь, запрос, breadcrumbs) один раз на группу — так что 10 000 сбоев это одна issue со счётчиком 10 000, а не 10 000 строк, и один НОВЫЙ fingerprint — это одно уведомление." },
    { q: "Почему out-of-band ошибки обходят глобальный filter Nest и почему алертинг по сырому счётчику заставляет команды пропускать реальные сбои? Дай фикс для каждого.", a: "Глобальный exception filter ловит только throw'ы ВНУТРИ пайплайна запроса. Необработанный promise rejection в setInterval-джобе, throw в обработчике EventEmitter или fire-and-forget отклонённый promise брошен out-of-band, никогда не входит в путь исключений Nest, никогда не доходит до filter и до трекера — он исчезает (а на современном Node unhandledRejection может уронить процесс, например fire-and-forget джоба сверки, crash-loop'ящая worker-под часами с нулём отчётов). Фикс: добавь обработчики уровня процесса — process.on('unhandledRejection') и process.on('uncaughtException') — которые делают captureException (а на uncaughtException — flush, потом exit). Нужны ОБА: filter (пайплайн запроса) и process-обработчики (всё остальное). Алертинг по сырому счётчику заставляет команды пропускать сбои, потому что счётчик смешивает ожидаемый обработанный шум (ретраенные таймауты, сканерные 404) с реальным сбоем и не имеет базовой линии: канал наполняется безопасными-но-реальными ошибками, люди мьютят его, а реальный сбой приходит в тот же замьюченный канал, выглядя идентично шуму. Фикс: алертить по СКОРОСТИ ошибок / burn rate SLO (L04), чтобы отделить нормальный ручеёк от прожигания бюджета, дедуплицировать по fingerprint, чтобы одна новая issue была одним пейджем, разделить лог-vs-пейдж по severity и всегда вычищать PII/секреты (Authorization, cookies, токены) через beforeSend перед отправкой, так как трекер — третья сторона." },
  ]}
/>

<Recap lang="ru">
Логи (что случилось), метрики (сколько/как быстро, L04) и трейсы (где в дереве вызовов, L03) — три несвязанных стога сена, пока общий ключ соединения — traceId плюс requestId, проставленный в каждую строку лога, каждый отчёт об ошибке и трейс — не превратит алерт в пивот: ошибка → её traceId → этот трейс → эти точные строки лога. Трекер ошибок лучше строки лога на ошибку тем, что вычисляет fingerprint из типа ошибки и стека и группирует каждое появление в одну issue со счётчиком, захватывая release/пользователя/запрос/breadcrumbs один раз; прикрепляй SHA релиза, чтобы всплеск маппился на деплой, и traceId, чтобы каждая issue линковалась к своему трейсу и логам. Глобальный exception filter из L01 ловит только throw'ы внутри пайплайна запроса, так что out-of-band ошибки — необработанный rejection в таймере или обработчике события, fire-and-forget отклонённый promise — обходят его целиком и исчезают (а на современном Node могут crash-loop'нуть worker), поэтому нужны ещё и обработчики process.on(&#123; unhandledRejection, uncaughtException &#125;), которые репортят. Поскольку трекер — третья сторона, ты обязан вычистить PII и секреты — deny-листить заголовки Authorization и Cookie, отредактировать токены — в хуке beforeSend перед тем, как что-либо покинет процесс, иначе ты сам логируешь breach. Наконец, алертить по сигналу, а не по шуму: алертинг на каждую ошибку или сырой счётчик пейджит на безопасный ручеёк, пока команда не замьютит канал и реальный сбой в нём не спрячется, так что алертить по скорости ошибок / burn rate SLO, группировать по fingerprint, чтобы одна новая issue была одним пейджем, и разделять «залогируй» от «дёрни человека» по severity. Теперь, когда канал #alerts долго молчит и потом присылает один пейдж — ты знаешь, что нужно отреагировать: тишина плюс одно уведомление — это ровно то, как должна выглядеть заслуживающая доверия система observability.
</Recap>
