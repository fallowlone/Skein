---
concepts:
- distributed-tracing
- context-propagation
- async-local-storage
- sampling
- trace-log-correlation
deepensInto: []
estMin: 18
lang: ru
lessonType: topic
level: senior
order: 3
prereqs: []
slug: "03-distributed-tracing"
sources:
- https://opentelemetry.io/docs/languages/js/
- https://docs.nestjs.com/
spiral: []
status: ready
summary: 'Один запрос разветвляется по N сервисам; логи по сервису не скажут, какой хоп тормозит и где сломалось. Трейс сшивает запрос в одно дерево span через общий traceId — если ты пробрасываешь контекст через HTTP и async-границы и сэмплируешь разумно.'
title: 'Распределённый трейсинг с OpenTelemetry'
track: nest
unit: 07-errors-and-observability
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";
import Infographic from "~/components/diagram/Infographic.astro";
import headVsTail from "./infographics/head-vs-tail.svg?raw";

<Hook>
Checkout затормозил. Не сломался — затормозил: p99 подполз к 3 секундам, а очередь поддержки заполнилась «спиннер просто висит». Поток пересекал четыре сервиса: `api` принимал запрос, звал `orders`, тот звал `payments`, тот звал `inventory`. Мы открыли все дашборды, что у нас были. p99 у `api`: норм. p99 у `orders`: норм. p99 у `payments`: норм. p99 у `inventory`: норм. Каждый сервис божился, что здоров, а запрос всё равно занимал три секунды. Логи тоже не помогли — четыре отдельных потока логов, у каждого свой request id, ни один не знает о других. Никто не мог ответить на единственный важный вопрос: *какой хоп съел время?* На этот вопрос нельзя ответить по логам отдельных сервисов. Нужен единственный объект, который следует за запросом через все четыре сервиса и записывает, куда ушла каждая миллисекунда. Этот объект — трейс, и этот урок о том, как OpenTelemetry его строит, как теряется контекст, который держит его вместе, и как за это платить, не разорившись.
</Hook>

<Crux>Трейс сшивает один запрос через N сервисов в дерево span общим traceId — если контекст пробрасывается через каждый HTTP- и async-хоп.</Crux>

<Explanation>

## Что такое трейс: span, traceId и цепочка родителей

**Трейс** — это всё путешествие одного запроса, представленное как дерево. Каждый узел — это **span**: одна операция со временем — начало, конец и набор атрибутов (HTTP-маршрут, SQL-выражение, статус). Span связаны двумя id. `traceId` общий для каждого span запроса — это ключ соединения для всего дерева. `spanId` идентифицирует один span, и каждый span записывает свой **родительский** `spanId`, что и придаёт дереву форму: span у `api` — корень, span у `orders` — его ребёнок, span у `payments` — ребёнок того, и так далее.

```ts
// Один span = одна операция. Трейсер создаёт его; ты ставишь атрибуты и заканчиваешь.
const tracer = trace.getTracer('orders');

await tracer.startActiveSpan('chargeCustomer', async (span) => {
  span.setAttribute('order.id', orderId);
  span.setAttribute('payment.amount', amount);
  try {
    return await this.payments.charge(orderId, amount);
  } catch (err) {
    span.recordException(err);
    span.setStatus({ code: SpanStatusCode.ERROR });
    throw err;
  } finally {
    span.end(); // start→end is THIS span's duration on the trace
  }
});
```

С этим деревом инцидент checkout отвечает на себя сам в один экран: открываешь трейс, видишь четыре span, разложенные на таймлайне, и span у `payments` видимо шириной 2.6с, а остальные три — тонкие щепки. Углубляешься в него — и находишь, что `payments` зовёт `inventory` **синхронно в цикле**, один round trip на позицию заказа. Ни один дашборд не мог это показать, потому что каждый отдельный вызов `inventory` был быстрым — медленным был *последовательный fan-out*, сумма, а сумма существует только в трейсе.

## Проброс контекста: механизм, делающий один трейс из четырёх сервисов

Сложная часть — не создавать span, это делает авто-инструментация. Сложная часть — держать каждый span в *одном и том же* трейсе, пока запрос пересекает границы. Для этого **trace context** — `traceId`, текущий `spanId` и флаг сэмплинга — должен путешествовать через два разных вида границ.

**Между сервисами**, по сети, контекст едет в W3C HTTP-заголовке `traceparent` (стандартизированный заголовок трассировки: версия-traceId-spanId-флаги). Вызывающая сторона **инжектит** его в исходящие заголовки запроса; вызываемая сторона **извлекает** его и продолжает трейс, а не начинает новый.

```ts
// traceparent: version-traceId-parentSpanId-flags
// 00-4bf92f3577b34da6a3ce929d0e0e4736-00f067aa0ba902b7-01
//    └─ 32 hex traceId ──────────────┘ └─ 16 hex spanId ┘ └ sampled
// Вызывающая сторона инжектит этот заголовок; вызываемая извлекает его, и span orders
// становится ДОЧЕРНИМ к span api — тот же traceId, новый spanId, parent = api.
```

**Внутри процесса**, через `await` и колбэки, нет заголовка, который понесёт контекст, — поэтому Node-менеджер контекста OTel хранит «текущий активный span» в `AsyncLocalStorage`. ALS — это механизм V8, который держит значение прикреплённым к одной логической async-цепочке вызовов: span, начатый в guard, всё ещё текущий span внутри handler и внутри вызова `pg`, который делает handler, потому что все они выполняются внутри одной отслеживаемой ALS-цепочки. Вот почему запрос к БД авто-вкладывается под span запроса, а ты ничего не передаёшь руками.

Режим отказа симметричен: **потеряй контекст на любой из границ — и трейс фрагментируется.** Пропусти `traceparent` на исходящем вызове — и вызываемая сторона начнёт свежий корневой трейс. Вырвись из ALS-цепочки — fire-and-forget неотавайченный промис, голый `setTimeout`, хоп через очередь без проброса — и продолжение выполнится без активного span, так что OTel не видит родителя и открывает новый корень. В любом случае запрос, который должен быть одним деревом, становится несколькими несвязанными фрагментами, и ты снова таращишься на несвязанные трейсы, не в силах проследить запрос из конца в конец.

<Inset kind="why" lang="ru">
Почему fire-and-forget или неотавайченный async-вызов ломает трейс? Потому что «текущий span» живёт не в переменной, которую ты передаёшь, — он живёт в `AsyncLocalStorage`, привязанный к отавайченной цепочке вызовов, которая начала span. Когда ты делаешь `await`, V8 держит тебя внутри этого хранилища, так что продолжение всё ещё видит span как текущий. Когда ты *не* делаешь await — зовёшь функцию и роняешь промис, или планируешь работу голым `setTimeout`, или пушишь джобу в очередь, а её подхватывает другой процесс — это продолжение выполняется **вне** исходного хранилища. OTel ищет текущий span, не находит ничего и начинает свежий корневой span с совершенно новым `traceId`. Дочерняя работа теперь — свой собственный трейс. Исходное дерево никогда не узнаёт, что работа случилась, а у нового корня нет родителя, так что в просмотре трейсов один запрос выглядит как несколько несвязанных трейсов, которые ты не можешь сшить обратно.
</Inset>

## Подключение OTel к Nest: bootstrap до приложения

Единственное правило, которое кусает всех: инструментация работает, monkey-patch'ая модули, которые трейсит (`http`, `express`, `pg`, ядро Nest), так что SDK должен стартовать **до** того, как твоё приложение импортирует эти модули. На практике ты кладёшь `NodeSDK` в отдельный файл и грузишь его первым — `node --require ./tracing.js dist/main.js` или `import './tracing'` самой первой строкой `main.ts`.

```ts
// tracing.ts — MUST be loaded before the Nest app so it patches http/pg/nest first
import { NodeSDK } from '@opentelemetry/sdk-node';
import { getNodeAutoInstrumentations } from '@opentelemetry/auto-instrumentations-node';
import { TraceIdRatioBasedSampler } from '@opentelemetry/sdk-trace-base';

const sdk = new NodeSDK({
  // auto-instruments http, express, @nestjs/core, pg — spans appear with no app code
  instrumentations: [getNodeAutoInstrumentations()],
  // head sampling: keep 10% of traces (see the sampling section)
  sampler: new TraceIdRatioBasedSampler(0.1),
});
sdk.start();
```

Как только это загружено, каждый HTTP-запрос и каждый запрос `pg` производят span автоматически. Ты добавляешь **ручные** span (`tracer.startActiveSpan`) только вокруг бизнес-операций, которые авто-инструментация не может назвать, — «зарезервировать остаток», «оценить риск». И ты замыкаешь круг структурным логированием из L01: инжектишь активный `traceId` в каждую строку лога. Это единственное поле — **ключ корреляции**: найди медленный трейс, скопируй его `traceId` и подними каждый лог из каждого сервиса для ровно этого запроса. Логи отвечают *что случилось*; трейс отвечает *куда ушло время*; `traceId` — это соединение между ними.

```ts
// L01's logger, now trace-aware: every line carries the active traceId
const span = trace.getActiveSpan();
const ctx = span?.spanContext();
this.logger.log({ msg: 'charge failed', orderId, traceId: ctx?.traceId, spanId: ctx?.spanId });
// now one traceId pulls every log from every service for this exact request
```

## Сэмплинг: ты не можешь позволить себе хранить каждый трейс

Прежде чем добавлять трейсинг в продакшен-сервис, спроси себя: при 5 000 RPS сколько данных span ты реально готов хранить и оплачивать? Ответ определяет, к какой из двух стратегий ниже ты потянешься. Трейсить каждый запрос дорого — хранение span и пропускная способность экспорта доминируют, а неограниченные span или атрибуты на запрос добавляют реальную цену. Поэтому ты **сэмплируешь**: часть трейсов оставляешь, остальное роняешь. Есть две стратегии, и компромисс между ними — это сеньорское решение.

**Head sampling** решает на корне, до выполнения запроса, обычно по доле: `TraceIdRatioBasedSampler(0.1)` оставляет 10%. Дёшево и не требует доп. инфраструктуры — но решение слепое. Оно не может знать, что запрос, который ты уронил, был тем самым, что занял 3 секунды или бросил 500, так что при низкой доле ты систематически пропускаешь редкие медленные и падающие запросы — ровно те, что тебе и нужны.

**Tail sampling** решает *после* того, как запрос завершился, когда исход известен: оставь **все** ошибки и все медленные трейсы, сэмплируй малую долю скучных быстрых. Оно ловит редкий плохой запрос, который head sampling роняет. Цена в том, что его нельзя решить в процессе — каждый span трейса должен где-то буферизоваться, пока трейс не завершится и не вынесется вердикт оставить/уронить, а значит ты должен гонять **OpenTelemetry Collector** как буферизующий ярус между твоими сервисами и бэкендом.

<Infographic
  svg={headVsTail}
  label="Сравнение head- и tail-сэмплинга бок о бок. Head-сэмплинг решает на корне до выполнения запроса, оставляет 10% по доле через TraceIdRatioBasedSampler(0.1) — дёшево и без инфраструктуры — но слеп и теряет редкие медленные и падающие запросы. Tail-сэмплинг решает после завершения запроса, когда исход известен, оставляет все ошибки и все медленные трейсы и лишь долю быстрых успехов, поэтому ловит редкий плохой запрос, ценой того, что нужен OpenTelemetry Collector, буферизующий каждый span до завершения трейса."
  caption="Весь компромисс — в том, где принимается решение оставить-или-уронить: head решает вслепую на корне (дёшево, теряет редкие плохие запросы); tail решает по известному исходу (ловит их, но нужен Collector для буферизации span)."
  hue="var(--d-infra)"
/>

<TradeoffMatrix
  id="nest-07-errors-and-observability-03-distributed-tracing-tradeoff-1"
  lessonSlug="03-distributed-tracing"
  lang="ru"
  prompt="Ты должен трейсить высоконагруженный checkout-поток через четыре сервиса. Объём делает 100%-трейсинг слишком дорогим, но вся причина добавлять трейсинг — поймать редкие медленные и падающие checkout. Какой подход к сэмплингу подходит?"
  options={[
    { name: "Tail sampling: оставить ВСЕ ошибки и медленные трейсы, сэмплировать малую долю быстрых успехов, буферизуя через OTel Collector", summary: "Оставляет по исходу.", correct: true, justification: "Tail sampling решает после завершения запроса, когда исход известен, так что оставляет ровно те трейсы, ради которых ты добавлял трейсинг, — каждую ошибку и каждый медленный checkout, — роняя большинство скучных быстрых ради контроля цены. Цена реальна и признана: вердикт нельзя вынести в процессе, так что ты должен гонять OpenTelemetry Collector как буферизующий ярус, который держит span трейса, пока тот не завершится, и затем решает. Для высоконагруженного потока, ценность которого — ловить редкий плохой запрос, платить за Collector — верный компромисс." },
    { name: "Head sampling с низкой долей (например 10%) через TraceIdRatioBasedSampler — без collector", summary: "Дёшево, но слепо.", justification: "Head sampling дёшев и без инфраструктуры, а доля 10% — хороший дефолт для общей видимости. Но он решает на корне до того, как исход известен, поэтому слеп: доля 10% оставляет 1 из 10 запросов наугад и систематически пропустит большинство редких медленных и падающих checkout — ровно те запросы, ради которых этот поток существует. Приемлемо как базовая линия; неверно, когда заявленная цель — ловить редкий плохой запрос." },
    { name: "Трейсить 100% запросов — оставлять всё, не сэмплировать ничего", summary: "Ровно та цена, что ты не можешь платить.", justification: "Хранение каждого трейса убирает слепое пятно, но возвращает цену, которую промпт исключает: при высокой нагрузке хранение span и пропускная способность экспорта взрываются, а объём span/атрибутов на запрос усугубляет это. Трейсить каждый запрос — это ровно то, ради избегания чего существует сэмплинг; это проваливает ограничение по цене." },
    { name: "Не трейсить ничего — полагаться на логи по сервисам, что у тебя уже есть", summary: "Назад к исходному слепому пятну.", justification: "Это состояние, в котором урок открывается: четыре здоровых на вид сервиса и запрос в 3с, который никто не может объяснить, потому что логи по сервисам не показывают последовательный fan-out через хопы. Отказ от трейсинга возвращает тебя к ровно тому инциденту, который его мотивировал." },
  ]}
/>

<Quiz
  id="nest-07-errors-and-observability-03-distributed-tracing-quiz-1"
  lessonSlug="03-distributed-tracing"
  lang="ru"
  question="Сервис api зовёт сервис orders по HTTP, и ты хочешь, чтобы span у orders был ребёнком span у api в одном трейсе. Что несёт trace context через эту границу сервиса?"
  choices={[
    { label: "W3C HTTP-заголовок traceparent — api инжектит его в исходящий запрос, orders извлекает и продолжает тот же трейс", correct: true },
    { label: "AsyncLocalStorage — он несёт текущий span из api в orders автоматически", misconception: "AsyncLocalStorage пробрасывает контекст только ВНУТРИ одного процесса, через await и колбэки на одной async-цепочке вызовов. Он не может пересечь сетевую границу — у другого процесса своё ALS-хранилище. Между сервисами контекст должен быть сериализован в заголовок W3C traceparent вызывающей стороной и извлечён вызываемой." },
    { label: "Общая колонка traceId, записанная в базу, которую читают оба сервиса", misconception: "Нет общего хранилища, из которого читает вызываемая сторона. Контекст путешествует in-band вместе с самим запросом, в заголовке traceparent, который инжектит вызывающая сторона; вызываемая извлекает его из входящих заголовков. Ничего не пишется в базу и не читается из неё для проброса трейса." },
    { label: "Ничего — orders авто-определяет, что он часть трейса api, потому что вызовы близки по времени", misconception: "Span никогда не коррелируются по времени. Без заголовка traceparent orders не имеет понятия, что он downstream от api, и начинает совершенно новый корневой трейс со своим traceId. Проброс явный: инжект на выходе, извлечение на входе." },
  ]}
/>

<Quiz
  id="nest-07-errors-and-observability-03-distributed-tracing-quiz-2"
  lessonSlug="03-distributed-tracing"
  lang="ru"
  question="Запрос ставит в очередь BullMQ-джобу. В твоём UI трейсинга span джобы показываются как отдельные корневые трейсы, несвязанные с запросом, который её поставил. Почему и каков фикс?"
  choices={[
    { label: "Trace context не был проброшен через очередь, так что processor выполняется вне исходной ALS-цепочки и начинает новый корень — фикс: инжектить traceparent в payload джобы при постановке, извлекать и восстанавливать контекст в processor", correct: true },
    { label: "BullMQ-джобы всегда создают новые трейсы по дизайну; их нельзя связать с поставившим запросом", misconception: "Ничто не заставляет джобу быть новым трейсом — она новая только потому, что контекст не был перенесён. Очередь — это граница проброса, как и HTTP-вызов: сериализуй traceparent в данные джобы, когда добавляешь её, затем извлеки и восстанови его в processor, чтобы span джобы вложились под исходный трейс." },
    { label: "Доля сэмплинга слишком низкая, так что span запроса были уронены и выжили только span джобы", misconception: "Сэмплинг роняет целые трейсы, он не разбивает один трейс на два. Осиротевшие трейсы джоб — это отказ проброса контекста, а не артефакт сэмплинга: processor выполнился вне AsyncLocalStorage-цепочки поставщика, не нашёл активного span и открыл свежий корень." },
    { label: "Processor выполняется в worker-потоке, а AsyncLocalStorage не пересекает потоки — в Node фикса нет", misconception: "Worker это, отдельный процесс или более поздний тик — причина одна: контекст не был перенесён через хоп. И фикс точно есть — инжектить traceparent в payload джобы при постановке и восстанавливать его в processor. То, что ALS не пересекает границу, — это причина, по которой ты обязан пробрасывать вручную, а не тупик." },
  ]}
/>

</Explanation>

<FlowDiagram
  label="Как один checkout-запрос становится единым деревом трейса через четыре сервиса посредством проброса контекста. Сервис api принимает запрос и открывает корневой span, который чеканит traceId на весь запрос. Вызов orders по HTTP инжектит W3C-заголовок traceparent, несущий этот traceId и текущий spanId; orders извлекает его и открывает дочерний span под тем же traceId. Handler у orders выполняет pg-запрос, который авто-вкладывается как дочерний span, потому что вызов базы выполняется внутри той же внутрипроцессной AsyncLocalStorage-цепочки. Затем orders зовёт payments по HTTP, снова пробрасывая traceparent, так что span у payments присоединяется к тому же дереву. Результат — один трейс: единственный traceId, четыре связанных span, рёбра родитель-к-ребёнку следуют пути вызовов."
  hue="var(--d-systems)"
  perRow={3}
  nodes={[
    { id: "api", label: "корневой span api", sub: "чеканит traceId" },
    { id: "orders", label: "span orders", sub: "ребёнок через traceparent" },
    { id: "pg", label: "span pg-запроса", sub: "ребёнок через ALS" },
    { id: "payments", label: "span payments", sub: "ребёнок через traceparent" },
    { id: "tree", label: "одно дерево трейса", sub: "общий traceId" },
  ]}
  edges={[
    { from: "api", to: "orders", label: "заголовок traceparent" },
    { from: "orders", to: "pg", label: "AsyncLocalStorage" },
    { from: "orders", to: "payments", label: "заголовок traceparent" },
    { from: "payments", to: "tree", label: "тот же traceId" },
  ]}
  caption="Проброс — это набор рёбер: заголовок traceparent несёт контекст через каждый HTTP-хоп, AsyncLocalStorage несёт его через внутрипроцессный вызов БД. Урони любое ребро — и это поддерево отколется в собственный корневой трейс."
/>

<KeyTakeaway>Трейс = запрос как дерево span с общим traceId. Пробрасывай контекст через traceparent между сервисами и AsyncLocalStorage в процессе; потеряй — фрагментируется. Head-сэмплируй 1-10% или tail errors+slow; traceId в логи.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="nest-07-errors-and-observability-03-distributed-tracing-retrieval"
  lang="ru"
  questions={[
    { q: "Из чего состоит трейс и как проброс контекста держит один запрос в одном трейсе, когда он пересекает границы сервисов и async?", a: "Трейс — это всё путешествие одного запроса, смоделированное как дерево span. Span — это одна операция со временем (начало, конец, атрибуты вроде HTTP-маршрута или SQL-выражения). Span связаны id: каждый span запроса делит один traceId (ключ соединения для дерева), у каждого span свой spanId, и каждый записывает свой родительский spanId — что придаёт дереву форму (span у api — корень, orders — его ребёнок, payments — ребёнок того). То, что должно путешествовать, чтобы все эти span делили traceId, — это trace context: traceId, текущий spanId и флаг сэмплинга. Он пересекает два вида границ. МЕЖДУ сервисами по сети он едет в W3C HTTP-заголовке traceparent — вызывающая сторона инжектит его в исходящие заголовки, вызываемая извлекает и продолжает тот же трейс как ребёнок, а не начинает новый корень. ВНУТРИ процесса через await и колбэки заголовка нет, так что Node-менеджер контекста OTel хранит текущий активный span в AsyncLocalStorage, который держит значение привязанным к одной логической async-цепочке — так что span, начатый в guard, всё ещё текущий внутри handler и внутри вызова pg, который тот делает. Потеряй контекст на любой границе — пропусти traceparent на исходящем вызове или порви ALS-цепочку fire-and-forget/неотавайченным промисом, голым setTimeout или хопом очереди без проброса — и продолжение выполнится без активного span, OTel не видит родителя и открывает свежий корневой трейс, так что один запрос фрагментируется на несколько несвязанных трейсов." },
    { q: "Как подключить OTel к Nest-приложению и как выбирать между head- и tail-сэмплингом?", a: "Авто-инструментация работает, monkey-patch'ая модули, которые трейсит (http, express, pg, @nestjs/core), так что NodeSDK должен стартовать ДО того, как приложение импортирует эти модули — положи его в отдельный tracing-файл, загружаемый первым через node --require ./tracing.js или import './tracing' первой строкой main.ts. С запущенным getNodeAutoInstrumentations() каждый HTTP-запрос и pg-запрос получают span автоматически; ты добавляешь ручные span через tracer.startActiveSpan только вокруг бизнес-операций, которые авто-инструментация не может назвать. Затем замыкаешь круг структурным логированием, инжектя активный traceId (из trace.getActiveSpan()?.spanContext()) в каждую строку лога — этот traceId и есть ключ корреляции, соединяющий логи (что случилось) с трейсами (куда ушло время). О сэмплинге: ты не можешь позволить каждый трейс, потому что хранение span и пропускная способность экспорта доминируют. Head sampling решает на корне до выполнения запроса, обычно по доле (TraceIdRatioBasedSampler(0.1) оставляет 10%) — дёшево и без инфраструктуры, но слепо: при низкой доле систематически пропускает редкие медленные/падающие запросы, до которых тебе и есть дело. Tail sampling решает после завершения запроса, когда исход известен — оставь все ошибки и медленные трейсы, сэмплируй долю быстрых успехов — так что ловит редкий плохой запрос, но вердикт нельзя вынести в процессе, так что ты должен гонять OpenTelemetry Collector, чтобы буферизовать span трейса, пока тот не завершится. Head 1-10% — хороший дефолт для общей видимости; tail sampling — компромисс, когда вся суть в том, чтобы ловить редкие ошибки и медленные трейсы при высоком объёме." },
  ]}
/>

<Recap lang="ru">
Распределённый трейс превращает один запрос, который разветвляется по N сервисам, в единое дерево, которое — единственный артефакт, способный ответить «какой хоп съел время»: логи по сервисам и дашборды видят каждый по одному срезу и упускают последовательный fan-out, который сделал весь запрос медленным. Трейс — это дерево span (каждый — операция со временем и атрибутами); каждый span делит один traceId (ключ соединения), у каждого есть spanId, и каждый записывает родительский spanId, чтобы придать дереву форму. Механизм, который держит все эти span в одном трейсе, — проброс trace context (traceId + текущий spanId + флаг сэмплинга): МЕЖДУ сервисами он едет в W3C HTTP-заголовке `traceparent` — вызывающая инжектит, вызываемая извлекает — а ВНУТРИ процесса живёт в `AsyncLocalStorage`, менеджере контекста OTel, так что span, начатый в guard, всё ещё текущий внутри handler и его вызова БД. Потеряй контекст — неотавайченный fire-and-forget, голый `setTimeout` или хоп очереди без проброса — и продолжение выполнится без активного span, так что OTel открывает свежий корень и запрос фрагментируется на несвязанные трейсы; фикс для BullMQ — инжектить `traceparent` в payload джобы и восстанавливать его в processor. Подключай OTel, загружая `NodeSDK` до приложения, чтобы авто-инструментация смогла пропатчить http/pg/nest первой, добавляй ручные span для бизнес-операций и инжекти активный `traceId` в каждую строку лога как ключ корреляции, связывающий логи с трейсами. Поскольку хранить каждый трейс слишком дорого, ты сэмплируешь: head sampling (например `TraceIdRatioBasedSampler(0.1)`, 1-10%) дёшев, но слеп к редкому медленному/падающему запросу; tail sampling оставляет все ошибки и медленные трейсы, но требует OpenTelemetry Collector (промежуточный буферизующий сервис), чтобы накапливать span, пока исход не станет известен. Теперь, когда видишь медленный запрос, а дашборды каждого сервиса говорят «всё в порядке», ты знаешь, куда смотреть: открываешь трейс, находишь широкий span и читаешь последовательный fan-out, который ни одна отдельная метрика показать не могла.
</Recap>
