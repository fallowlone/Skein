---
concepts:
- metric-types
- red-method
- percentiles
- cardinality
- slo-error-budget
deepensInto: []
estMin: 18
lang: ru
lessonType: topic
level: senior
order: 4
prereqs: []
slug: "04-metrics-and-slos"
sources:
- https://prometheus.io/docs/concepts/metric_types/
- https://sre.google/sre-book/service-level-objectives/
spiral: []
status: ready
summary: 'Метрики — не логи и не трейсы: это дешёвые агрегаты, по которым ты алертишь. Возьми правильный тип (counter/gauge/histogram), меряй хвост через p99, а не лгущее среднее, держи метки низкокардинальными, иначе Prometheus уйдёт в OOM, и алерть на прожиг бюджета SLO.'
title: 'Метрики, перцентили и SLO'
track: nest
unit: 07-errors-and-observability
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
Дашборд был зелёным. Среднее время ответа на маршруте оформления заказа держалось на плоских 90ms — та же линия, что рисовалась месяцами, — дежурный глянул на неё и вернулся к обеду. А тем временем копились тикеты поддержки: «страница висит», «нажал оплатить дважды, списали один раз», «крутится бесконечно». Среднее не двигалось, потому что и не могло: среднее заякорено плотной серединой распределения, и только самый медленный один запрос из ста разнесло до четырёх секунд. Этот один-из-ста — это реальные пользователи, упирающиеся в клиентский таймаут в 3 секунды, делающие ретрай и удваивающие ту самую нагрузку, что и вызывала медленность. Фикс начался не с изменения кода — с изменения *измерения*: перестать смотреть на среднее, начать смотреть на p99 и повесить на него SLO. Этот урок про то, как делать метрики правильно: типы, перцентили, метка, которая кладёт Prometheus, и бюджет, по которому ты на самом деле алертишь.
</Hook>

<Crux>Метрики — дешёвые агрегаты для алертов: правильный тип, хвост (p99), а не среднее, ограниченные метки, алерть на прожиг бюджета SLO.</Crux>

<Explanation>

## Четыре типа метрик: бери тот, что отвечает на твой вопрос

Метрика — это не строка лога и не span. Это число, семплируемое во времени и агрегируемое на стороне сервера: дёшево хранить, дёшево запрашивать, и именно по нему ты строишь алерты. Prometheus даёт четыре типа, и неверный выбор — первая ошибка.

```ts
import { Counter, Gauge, Histogram } from 'prom-client';

// COUNTER: монотонен, только растёт (или сбрасывается в 0 при рестарте).
// Сырое значение не читают — запрашивают rate() по нему.
const httpRequests = new Counter({
  name: 'http_requests_total',
  help: 'Total HTTP requests',
  labelNames: ['method', 'route', 'status'] as const, // НИЗКАЯ кардинальность — см. ниже
});

// GAUGE: значение, которое растёт И падает — снимок «прямо сейчас».
const inFlight = new Gauge({
  name: 'http_requests_in_flight',
  help: 'Requests currently being served',
});

// HISTOGRAM: бакетизирует наблюдение (здесь — длительность) в заранее заданные бакеты,
// чтобы вычислять квантили НА СТОРОНЕ СЕРВЕРА по всем инстансам.
const httpDuration = new Histogram({
  name: 'http_request_duration_seconds',
  help: 'Request duration in seconds',
  labelNames: ['method', 'route', 'status'] as const,
  buckets: [0.01, 0.05, 0.1, 0.3, 0.5, 1, 2, 5], // boundaries decide p99 accuracy
});
```

**Counter** монотонен — суммы запросов, суммы ошибок. Ты никогда не рисуешь сырое число; ты рисуешь `rate(http_requests_total[5m])`, чтобы получить запросы-в-секунду. **Gauge** ходит в обе стороны — in-flight запросы, использование памяти, глубина очереди, размер пула соединений: снимок, который ты читаешь напрямую. **Histogram** кладёт каждое наблюдение в бакет; поскольку бакеты живут на сервере, ты можешь посчитать `histogram_quantile(0.99, …)` и, что критично, *сначала сложить бакеты по всем своим инстансам*, а потом взять квантиль. Четвёртый тип — **summary** — считает квантили на стороне клиента в каждом процессе, а значит, их нельзя агрегировать: p99 трёх инстансов — это не среднее их трёх p99. В любом мультиинстансном сервисе предпочитай гистограммы.

В Nest ты подключаешь это через `@willsoto/nestjs-prometheus` (или сырой `prom-client`), выставляешь эндпоинт `/metrics` для скрейпа Prometheus и записываешь длительности в интерсепторе, оборачивающем каждый запрос: запускаешь таймер в `intercept`, останавливаешь его в `tap`/`finalize` observable, размечая по методу, шаблону маршрута и статусу.

## RED и golden signals: что на самом деле мерить

Ты не меряешь всё; ты меряешь горстку сигналов, которые говорят, здоров ли сервис. Для request-driven сервиса спина — это **метод RED**: **R**ate (запросов в секунду, из counter), **E**rrors (упавших запросов в секунду, тот же counter, отфильтрованный по 5xx), **D**uration (распределение задержки, из histogram). **Четыре golden signals** от Google добавляют **saturation** (насколько полон самый загруженный ресурс) к latency, traffic и errors. **USE** (Utilization, Saturation, Errors) — это зеркало со стороны ресурсов: применяй к CPU, памяти, диску, пулу соединений. RED для пути запроса, USE для ресурсов, на которые он опирается.

## Перцентили: среднее лжёт, меряй хвост

Вот ключевой инсайт, на котором держится инцидент из начала. **Среднее время ответа прячет аварию, которую обнажает p99.** Среднее в 80ms может сидеть поверх p99 в две секунды, потому что среднее доминируется массой быстрых запросов, и небольшой медленный хвост едва его подталкивает. Но этот хвост — не шум, это реальные пользователи, и это ровно та когорта, что упирается в таймауты, запускает ретраи и усиливает нагрузку. SLO на среднее — это SLO на неверное число.

```text
# PromQL: p99 latency на маршрут, агрегированная по ВСЕМ инстансам.
# sum by (...) — ПЕРЕД histogram_quantile — именно поэтому histogram, а не summary:
# сначала складываем бакеты, потом берём квантиль.
histogram_quantile(
  0.99,
  sum by (le, route) (rate(http_request_duration_seconds_bucket[5m]))
)
```

Поэтому ты отслеживаешь p50 (типичный пользователь), p95 и p99 (хвост, который болит) и p999, когда трафик достаточно велик, чтобы один-из-тысячи был ощутимым числом людей. Одна оговорка: квантиль гистограммы Prometheus хорош ровно настолько, насколько хороши границы твоих бакетов. `histogram_quantile` линейно интерполирует *внутри* того бакета, в который попадает квантиль, так что если твой p99 ложится между бакетами `1s` и `5s`, отчётное значение — это догадка где-то в этом промежутке, возможно сильно неверная. Ставь границы бакетов там, где тебе реально важно (вокруг порога твоего SLO), а не на круглых числах.

<Inset kind="why" lang="ru">
Почему среднее прячет аварию, которую обнажает p99? Потому что среднее — это центр масс: его тянет плотная середина распределения, где сидит большинство запросов. Если 99 запросов занимают 50ms, а один — 4 секунды, среднее около 90ms — один медленный запрос едва его сдвигает. Но этот один медленный запрос — не погрешность округления: при тысяче запросов в секунду это десять пользователей в секунду, упирающихся в глухую стену, а при клиентском таймауте в 3s они делают ретрай, что добавляет нагрузку и ухудшает хвост. Боль системы живёт в её хвосте, поэтому хвост надо мерить напрямую — p99, p999, — а не число, математически устроенное так, чтобы его стереть. SLA или SLO, заданный на среднем, — это SLO на неверном числе, и он будет светиться зелёным сквозь реальную, видимую пользователю аварию.
</Inset>

## Кардинальность: метка, которая кладёт Prometheus

Это тот режим отказа, что убивает саму систему мониторинга. В Prometheus **каждая отдельная комбинация значений меток — это свой временной ряд**, хранимый и индексируемый отдельно. `http_requests_total{method="GET", route="/users/:id", status="200"}` — это один ряд; измени любое значение метки — получишь другой. Это нормально, пока метки *ограничены*: горстка методов, пара десятков маршрутов, несколько классов статусов, так что произведение — может, несколько сотен рядов.

Это становится катастрофой в момент, когда ты вешаешь на метрику **высококардинальную** метку: `userId`, `requestId`, `email` или сырой `path`, в который вшиты id. Метка `userId` означает один ряд *на пользователя* — миллион пользователей — это миллион рядов на метрику, у каждого своя память и своя запись в индексе. Prometheus держит head-ряды в RAM; взрыв кардинальности уводит сервер в OOM, раздувает время скрейпа и хранилище и кладёт мониторинг ровно тогда, когда он тебе нужен.

```ts
// БОМБА КАРДИНАЛЬНОСТИ — сырой путь со вшитыми id = один ряд НА каждый id.
// /users/1, /users/2, … /users/9999999 → миллионы различных значений метки.
httpRequests.inc({ method: 'GET', route: req.path, status: '200' });
// напр. route="/users/8f3a-...-91/orders/4412"  ← неограничен, уникален на запрос

// БЕЗОПАСНО — сворачивай до ШАБЛОНА маршрута; id — в значении, а не в метке.
httpRequests.inc({ method: 'GET', route: '/users/:id/orders/:orderId', status: '200' });
```

Правило: метки должны быть **низкокардинальными и ограниченными** — метод, *шаблон* маршрута (а не сырой путь), класс статуса. То, что тебя тянет положить в метку (id пользователя, id запроса), принадлежит *трейсу* или *строке лога*, а не измерению метрики. Военная история банальна и распространена: кто-то разметил `http_requests` по полному URL, включая литеральные id `/users/{id}`, число рядов взорвалось, Prometheus съел всю свою RAM и упал — а дашборд погас посреди инцидента.

## SLO и бюджет ошибок: то, по чему ты алертишь

**SLO** — это цель: «99.9% запросов оформления завершаются быстрее 300ms, измеряется за 30 дней». Его зеркальное отражение — **бюджет ошибок**, те 0.1%, что тебе *разрешено* промахнуть. Этот бюджет — не отказ, которого надо избегать; это ресурс, который надо тратить. Прожигаешь медленно — у тебя есть простор катить фичи и рисковать; прожигаешь быстро — замораживаешь деплои и чинишь надёжность. И ты алертишь не на сырое число ошибок — горстка ошибок в 3 ночи — это шум. Ты алертишь на **скорость прожига**: насколько быстро ты потребляешь бюджет. Скорость прожига, которая исчерпала бы 30-дневный бюджет за час, — это пейдж; та, что заняла бы неделю, — тикет. Это разница между «разбудить человека» и «не будить».

<TradeoffMatrix
  id="nest-07-errors-and-observability-04-metrics-and-slos-tradeoff-1"
  lessonSlug="04-metrics-and-slos"
  lang="ru"
  prompt="Нужно отслеживать задержку HTTP-запросов для SLO (99.9% быстрее 300ms) на сервисе, работающем на множестве инстансов, и считать p99 на дашбордах. Какая метрика и метки?"
  options={[
    { name: "Histogram с ограниченными метками (method, шаблон маршрута, status), p99 через histogram_quantile по сложенным бакетам", summary: "Histogram, ограниченные метки.", correct: true, justification: "Histogram бакетизирует каждую длительность на стороне сервера, так что ты можешь СНАЧАЛА сложить бакеты по всем инстансам, а потом взять histogram_quantile(0.99, …) — корректный p99 по всему флоту, что ровно и нужно SLO. Разметка по методу, шаблону маршрута и статусу держит кардинальность ограниченной (несколько сотен рядов), так что Prometheus здоров. Поставь границы бакетов рядом с порогом SLO в 300ms, чтобы квантиль не интерполировался через грубый разрыв. Это сеньорский дефолт для latency-SLO в мультиинстансном сервисе." },
    { name: "Summary с поинстансными квантилями", summary: "Нельзя агрегировать.", justification: "Summary считает квантили на стороне клиента внутри каждого процесса, а значит, их нельзя объединить: p99 флота — это НЕ среднее p99 каждого инстанса. На множестве инстансов ты получишь один p99 на процесс и никакого корректного способа свернуть их в число уровня сервиса — бесполезно для SLO, определённого по всему сервису. Summary приемлемы лишь для одноинстансного процесса или квантиля, который ты никогда не агрегируешь." },
    { name: "Gauge, хранящий задержку последнего запроса", summary: "Теряет распределение.", justification: "Gauge — это единственное мгновенное значение, так что он всегда хранит лишь длительность самого свежего запроса — он выбрасывает всё распределение. Ты не восстановишь p50, p95 или p99 из «последнего значения»; ты увидишь шумную линию, пропускающую каждый всплеск хвоста между скрейпами. Gauge верен для счётчика in-flight или глубины очереди, но никогда для распределения задержки, из которого нужны перцентили." },
    { name: "Histogram, размеченный по полному пути и userId", summary: "Бомба кардинальности.", justification: "Тип histogram верен, но разметка по полному пути (со вшитыми id) и userId делает каждый запрос почти уникальной комбинацией меток — миллионы рядов, по одному на пользователя на путь. Это классический взрыв кардинальности: Prometheus уходит в OOM, скрейпы тормозят, хранилище раздувается, а мониторинг умирает посреди того инцидента, ради которого он был нужен. Тот же тип, фатальные метки — сверни до шаблона маршрута и убери userId." },
  ]}
/>

<Quiz
  id="nest-07-errors-and-observability-04-metrics-and-slos-quiz-1"
  lessonSlug="04-metrics-and-slos"
  lang="ru"
  question="Нужно отслеживать задержку HTTP-запросов и показывать корректный p99 по сервису на 12 инстансах. Какой тип метрики и почему?"
  choices={[
    { label: "Histogram — бакеты живут на стороне сервера, так что ты складываешь бакеты по всем инстансам, а потом считаешь histogram_quantile; агрегируется в p99 по всему флоту", correct: true },
    { label: "Summary — он сам считает p99 внутри каждого процесса, что тебе ровно и нужно", misconception: "Summary считает квантили на стороне клиента поинстансно, и их НЕЛЬЗЯ агрегировать: p99 сервиса — не среднее 12 поинстансных p99. У тебя не будет корректного числа по всему флоту. Histogram отдаёт сырые бакеты, которые ты сначала складываешь, потом берёшь квантиль — потому он и выбор для мультиинстансного SLO." },
    { label: "Gauge — выставляй его в длительность последнего запроса и читай p99 с графика", misconception: "Gauge хранит единственное мгновенное значение, так что он помнит лишь задержку последнего запроса и полностью выбрасывает распределение. Читать там p99 неоткуда — из одного числа перцентили не восстановить. Распределениям задержки нужна гистограмма." },
    { label: "Counter — увеличивай его на длительность и дели на число запросов", misconception: "Деление counter-суммы длительностей на counter-число запросов даёт тебе СРЕДНЮЮ задержку — ровно то число, про которое урок говорит, что оно лжёт. Оно не может выдать p99; хвост для него невидим. Чтобы получить перцентили, нужна гистограмма." },
  ]}
/>

<Quiz
  id="nest-07-errors-and-observability-04-metrics-and-slos-quiz-2"
  lessonSlug="04-metrics-and-slos"
  lang="ru"
  question="Инженер добавляет метку `userId` на counter http_requests_total, чтобы разбить трафик по пользователям. У сервиса ~2 миллиона пользователей. Что станет с Prometheus?"
  choices={[
    { label: "Взрыв кардинальности — один ряд на комбинацию меток, так что userId создаёт ~2M рядов на метрику; Prometheus уходит в OOM и падает", correct: true },
    { label: "Ничего страшного — Prometheus хранит метки эффективно, и пара миллионов значений — это нормально", misconception: "Каждая отдельная комбинация значений меток — это отдельный временной ряд со своей стоимостью в RAM и индексе. userId неограничен — миллионы значений означают миллионы рядов НА метрику. Prometheus держит head-ряды в памяти, так что это уводит сервер в OOM. Высококардинальные идентификаторы никогда не должны быть метками метрик." },
    { label: "Prometheus отвергнет метрику при скрейпе, потому что userId — невалидное имя метки", misconception: "userId — совершенно валидное ИМЯ метки; проблема в кардинальности её ЗНАЧЕНИЙ, а не в имени. Prometheus с радостью её примет, а потом захлебнётся, когда число рядов взорвётся. Никакого ограждения нет — потому это и опасно. id принадлежит трейсу или логу, а не метке." },
    { label: "Всё нормально, если использовать histogram вместо counter", misconception: "Тип метрики не имеет отношения к кардинальности — histogram, размеченный по userId, даже хуже, ведь каждый ряд несёт ещё и полный набор бакетов. Кардинальность — про комбинации значений меток, а userId неограничен независимо от типа. Убери метку userId целиком." },
  ]}
/>

<FlowDiagram
  label="Как запрос становится алертом SLO. Запрос входит, и интерсептор записывает две метрики: инкрементирует counter (чья rate даёт запросы-в-секунду и долю ошибок) и наблюдает длительность запроса в бакеты гистограммы. Prometheus скрейпит эндпоинт /metrics, складывая бакеты каждого инстанса. Запрос дашборда выполняет histogram_quantile(0.99, …) по сложенным бакетам, получая p99 задержки по всему флоту, который сравнивается в SLO с целью 300ms; когда скорость прожига бюджета ошибок слишком высока, срабатывает алерт."
  hue="var(--d-systems)"
  perRow={3}
  nodes={[
    { id: "req", label: "запрос", sub: "интерсептор" },
    { id: "counter", label: "counter.inc", sub: "rate() = req/s, ошибки" },
    { id: "hist", label: "histogram.observe", sub: "бакеты длительности" },
    { id: "scrape", label: "скрейп /metrics", sub: "сложить бакеты" },
    { id: "p99", label: "histogram_quantile", sub: "p99 флота" },
    { id: "slo", label: "SLO / бюджет", sub: "алерт по прожигу" },
  ]}
  edges={[
    { from: "req", to: "counter", label: "запись" },
    { from: "req", to: "hist", label: "таймер" },
    { from: "counter", to: "scrape", label: "скрейп" },
    { from: "hist", to: "scrape", label: "скрейп" },
    { from: "scrape", to: "p99", label: "квантиль" },
    { from: "p99", to: "slo", label: "сравнить" },
  ]}
  caption="Интерсептор питает counter (rate, ошибки) и histogram (длительность); Prometheus складывает бакеты по инстансам; histogram_quantile даёт p99 флота, который проверяет SLO, алертя на прожиг бюджета — а не на сырое число ошибок."
/>

</Explanation>

<KeyTakeaway>Counter (rate), gauge (снимок), histogram (агрегируемые квантили); summary не агрегируется. Меряй p99, а не лгущее среднее. Метки ограниченные (шаблон, не userId), иначе Prometheus в OOM. Алерть на прожиг бюджета.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="nest-07-errors-and-observability-04-metrics-and-slos-retrieval"
  lang="ru"
  questions={[
    { q: "Назови четыре типа метрик Prometheus, для чего каждый, и почему для задержки запросов в мультиинстансном сервисе ты предпочитаешь histogram, а не summary.", a: "COUNTER монотонен — он только растёт (или сбрасывается в 0 при рестарте), и ты никогда не читаешь его сырое значение; ты запрашиваешь rate() по нему для сумм запросов и долей ошибок. GAUGE ходит вверх и вниз — мгновенный снимок для in-flight запросов, памяти, глубины очереди, размера пула. HISTOGRAM кладёт каждое наблюдение в заранее заданные бакеты, чтобы квантили считались на стороне сервера; для задержки это именно то, что нужно. SUMMARY считает квантили на стороне клиента внутри каждого процесса. Причина предпочесть histogram, а не summary, в мультиинстансном сервисе — агрегация: бакеты гистограммы можно СНАЧАЛА сложить по всем инстансам, а потом взять histogram_quantile, получив корректный p99 по всему флоту. Квантили summary посчитаны поинстансно заранее и не объединяются — p99 сервиса не равен среднему p99 каждого инстанса, — так что корректного числа уровня сервиса они не дают. Одна оговорка про гистограммы: histogram_quantile интерполирует внутри того бакета, куда попадает квантиль, так что p99 точен ровно настолько, насколько хороши границы бакетов; ставь границы рядом с порогом SLO." },
    { q: "Объясни, почему среднее время ответа прячет аварии, что такое взрыв кардинальности и как его избежать, и что такое бюджет ошибок SLO и на что ты алертишь.", a: "Среднее время ответа прячет аварии, потому что оно — центр масс, доминируемый плотной серединой распределения: 99 запросов по 50ms и один по 4s дают в среднем около 90ms, так что медленный запрос едва сдвигает линию — а при высоком трафике этот 1% — это много реальных пользователей, упирающихся в таймауты, делающих ретрай и усиливающих нагрузку. Боль живёт в хвосте, поэтому хвост ты меряешь напрямую через p50/p95/p99/p999, а не среднее; SLO на среднем — это SLO на неверном числе. Взрыв кардинальности происходит потому, что каждая комбинация значений меток — это свой временной ряд: высококардинальная метка вроде userId, requestId, email или сырого пути со вшитыми id создаёт миллионы рядов (по одному на пользователя), что уводит Prometheus в OOM, раздувает время скрейпа и хранилище и кладёт мониторинг посреди инцидента. Избегай этого, держа метки низкокардинальными и ограниченными — метод, ШАБЛОН маршрута, а не сырой путь, класс статуса — и кладя id в трейс или лог. SLO — это цель (например, 99.9% запросов быстрее 300ms за 30 дней); бюджет ошибок — разрешённые 0.1% — примерно 43 минуты в месяц для 99.9%, — который ты тратишь на катку, когда он здоров, и замораживаешь, когда он прожигается. Ты алертишь на СКОРОСТЬ ПРОЖИГА бюджета, а не на сырое число ошибок: скорость, исчерпывающая бюджет за час, — пейдж, та, что заняла бы неделю, — тикет." },
  ]}
/>

<Recap lang="ru">
Метрики — это дешёвые серверные агрегаты, по которым ты строишь алерты, отличные от логов и трейсов. В Prometheus четыре типа: counter монотонен (суммы запросов и ошибок — ты запрашиваешь rate() по нему), gauge ходит в обе стороны (in-flight, память, глубина очереди), histogram бакетизирует наблюдения, чтобы считать квантили на сервере, а summary считает квантили на клиенте и потому не агрегируется — предпочитай гистограммы в любом мультиинстансном сервисе. Меряй сигналы RED для пути запроса (Rate, Errors, Duration) и USE для ресурсов. Ключевой инсайт: среднее время ответа лжёт — среднее тянет плотная середина, так что p99 в секунды может прятаться под плоским средним в 90ms, пока реальные пользователи упираются в таймауты и делают ретрай; отслеживай p50/p95/p99/p999 и вешай SLO на хвост, помня, что histogram_quantile интерполирует ровно настолько хорошо, насколько позволяют границы бакетов. Убийственный режим отказа — кардинальность: каждая комбинация значений меток — отдельный ряд, так что метка userId, requestId или сырого пути создаёт миллионы рядов и уводит Prometheus в OOM — метки должны быть ограниченными (метод, шаблон маршрута, статус), а id идёт в трейс, а не в метку. Наконец, SLO — это цель (99.9% быстрее 300ms за 30 дней &rarr; бюджет ошибок 0.1% &asymp; 43 мин/месяц), и ты алертишь на скорость прожига бюджета, а не на сырое число ошибок, так что быстрый прожиг — пейдж, а медленный — тикет.
</Recap>
