---
concepts:
- integration-testing
- testcontainers
- per-test-isolation
- real-migrations
- transaction-rollback
deepensInto: []
estMin: 18
lang: ru
lessonType: topic
level: senior
order: 4
prereqs: []
slug: "04-integration-testing-with-db"
sources:
- https://docs.nestjs.com/fundamentals/testing
- https://node.testcontainers.org/
spiral: []
status: ready
summary: 'Моки проверяют твои допущения; настоящая база проверяет реальность. Интеграционные тесты гоняют код против одноразового Postgres (Testcontainers), ловя ограничения, каскады и дрифт миграций, которые мок структурно не видит, — ценой пер-тестового rollback и секунд времени.'
title: 'Интеграционное тестирование с реальной базой'
track: nest
unit: 06-testing
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
Юнит-сьют был сплошной стеной зелёного. Каждый repository замокан, каждый тест сервиса бежал за единицы миллисекунд, покрытие — 94%. А потом продовый `INSERT` рванул на ограничении `UNIQUE`, которое мок никогда не проверял: мок радостно возвращал «сохранённого» пользователя для уже существующего email, потому что мок возвращает ровно то, что ты ему велел. Хуже: `ON DELETE CASCADE` на внешнем ключе молча снёс дерево дочерних строк, когда админ удалил одного родителя, — ни один юнит-тест этого не проверял, ведь у мока нет понятия внешних ключей. Оба бага жили в зазоре между *тем, что код предполагает о базе*, и *тем, что база делает на самом деле*, — а этот зазор моку невидим по построению. Потом команда перегнула в другую сторону: интеграционный сьют без изоляции, где все тесты делят одну изменяемую базу, стал зависеть от порядка и расфлакался так, что блокировал CI два дня. Этот урок — про средний путь: тестировать против настоящего Postgres, изолировать каждый тест и держать всё достаточно быстрым, чтобы жить в CI.
</Hook>

<Crux>Моки проверяют допущения; настоящий Postgres — реальность, ловя ограничения, каскады и дрифт миграций, которых мок не видит.</Crux>

<Explanation>

## Где гнётся пирамида: что ловит только настоящая база

Спроси себя: что на самом деле тестирует твой сьют с 94% покрытием на моках? Он тестирует, что твой код согласован с твоими допущениями о базе — не то, что база ведёт себя именно так. Вот конкретный класс багов, который живёт в этом зазоре.

Юнит-тесты (замоканные зависимости, ~1мс каждый, их много) проверяют *твою логику в изоляции*. Интеграционные проверяют, что твой код плюс **настоящий коллаборатор — база** — действительно работают вместе. В этом различии вся суть: мок возвращает то, что ты в него запрограммировал, поэтому он способен подтвердить лишь те допущения, которые ты уже держишь в голове. А баги, доходящие до прода из слоя данных, — ровно те, что твои допущения упустили:

- **Нарушения ограничений** — `UNIQUE`, `NOT NULL`, `CHECK`, внешние ключи. Замоканный repo будет «сохранять» дубликат email сколько угодно; Postgres поднимет `23505`.
- **Правила каскадов** — `ON DELETE CASCADE` / `SET NULL` переформовывают строки, которых ты явно не касался. Ни один мок не моделирует ссылочное действие.
- **Дрифт миграций** — миграция, которая не прогналась, колонка, всё ещё `varchar(50)`, отсутствующий индекс. У мока нет схемы, с которой можно дрифтовать.
- **Реальный маппинг ORM** — колонки `JSONB`, enum-типы, точность `numeric`, последовательности, snake_case ↔ camelCase. Настоящая генерация SQL у ORM, а не твоя мысленная модель её.
- **Поведение транзакций и изоляции** — что rollback действительно откатывает, что блокировка действительно блокирует.

Цена реальна: интеграционным тестам нужен настоящий Postgres, и они бегут за ~10–100мс+ (реальный IO) вместо ~1мс, а старт контейнера стоит ~1–3с один раз на сьют. Поэтому их пишут **сильно меньше**, чем юнитов, — пирамида остаётся пирамидой. Ты не заменяешь юнит-тесты; ты добавляешь тонкий высокоценный слой, утверждающий то, чего моки не могут.

## Почему не замоканный repo и почему не SQLite

Два соблазнительных срезанных угла проваливаются одинаково — дают зелёные тесты, которые врут:

```ts
// Мок утверждает ТВОЁ ДОПУЩЕНИЕ, а не реальность.
const repo = { save: jest.fn().mockResolvedValue({ id: 1, email: 'a@b.com' }) };
// Это "проходит", даже если email a@b.com уже есть в проде и дал бы 23505.
// Мок не может обеспечить UNIQUE, не может каскадить, не может прогнать миграцию.
```

Замоканный repository не поймает нарушение уникальности, не запустит FK-каскад, не вскроет N+1 и не заметит миграцию, которая не прогналась, — потому что он *и есть* твоё допущение, закодированное. **SQLite-как-фейк** — ловушка тоньше: это настоящая база, поэтому она кажется честной, но это *другой движок*, не тот, что в проде. Его аффинность типов, обработка `JSONB`, семантика последовательностей и поведение изоляции — всё расходится с Postgres. Ты получаешь тесты, зелёные на SQLite и красные в проде, — худший возможный исход, ведь ты им доверял. Сеньорская позиция однозначна: **интеграционные тесты бегут на том же движке и мажорной версии, что и прод.** Если прод — Postgres 16, контейнер теста — Postgres 16.

## Testcontainers: одноразовый Postgres на прогон тестов

`@testcontainers/postgresql` (библиотека, которая поднимает Docker-контейнеры из кода теста и сносит их после) поднимает настоящий одноразовый Postgres в Docker на время прогона и отдаёт тебе *динамический* URL подключения. Ты скармливаешь этот URL в `TypeOrmModule.forRoot` в тестовом модуле, прогоняешь против него **настоящие миграции** (никогда `synchronize: true` — это пропускает те самые миграции, которые ты должен протестировать), выполняешь сьют и сносишь контейнер. Один `beforeAll` собирает всё:

```ts
import { Test } from '@nestjs/testing';
import { TypeOrmModule, getDataSourceToken } from '@nestjs/typeorm';
import { DataSource } from 'typeorm';
import { PostgreSqlContainer, StartedPostgreSqlContainer } from '@testcontainers/postgresql';
import { UsersModule } from '../src/users/users.module';

let container: StartedPostgreSqlContainer;
let dataSource: DataSource;

beforeAll(async () => {
  // ~1–3с один раз: настоящий Postgres ТОЙ ЖЕ мажорной версии, что и прод
  container = await new PostgreSqlContainer('postgres:16').start();

  const moduleRef = await Test.createTestingModule({
    imports: [
      TypeOrmModule.forRoot({
        type: 'postgres',
        url: container.getConnectionUri(), // динамические host/port — никогда не хардкодь 5432
        entities: [/* … */],
        synchronize: false,                // тестируем НАСТОЯЩИЕ миграции, не авто-синк
        migrationsRun: false,              // мы прогоняем их явно ниже
      }),
      UsersModule,
    ],
  }).compile();

  dataSource = moduleRef.get<DataSource>(getDataSourceToken());
  await dataSource.runMigrations();        // прогоняем тот же SQL, что прогонит прод
}, 60_000);                                // щедрый таймаут: тянуть образ медленно на холодном кэше

afterAll(async () => {
  await dataSource?.destroy();
  await container?.stop();                  // утилизируем одноразовую БД
});
```

Вызов `runMigrations()` вместо `synchronize: true` — несущая деталь: значит, миграция, которая дропает колонку, добавляет ограничение или бэкфилит данные, реально выполняется в тесте, поэтому дрифт миграций всплывает здесь, а не в проде.

## Пер-тестовая изоляция: техника, решающая, можно ли доверять сьюту

Тесты не должны видеть данные друг друга. Флакость из Hook пришла из общего изменяемого состояния — один тест вставил строку, другой посчитал строки и получил число, зависящее от *порядка* тестов. Три стратегии в порядке предпочтения:

```ts
// (a) ПРЕДПОЧТИТЕЛЬНО — оборачиваем каждый тест в транзакцию и ROLLBACK после.
//     Быстрее всего: ничего не коммитится, значит и чистить нечего.
let qr;
beforeEach(async () => {
  qr = dataSource.createQueryRunner();
  await qr.connect();
  await qr.startTransaction();             // BEGIN
  // отдаём qr.manager тестируемому коду, чтобы его записи жили в ЭТОЙ tx
});
afterEach(async () => {
  await qr.rollbackTransaction();          // ROLLBACK — каждая запись исчезает
  await qr.release();
});
```

```ts
// (b) НАДЁЖНО — TRUNCATE всех таблиц между тестами. Переживает код, который
//     коммитит свои транзакции, но медленнее (реальные DELETE + перетряска индексов).
afterEach(async () => {
  const tables = dataSource.entityMetadatas.map((m) => `"${m.tableName}"`).join(', ');
  await dataSource.query(`TRUNCATE ${tables} RESTART IDENTITY CASCADE;`);
});

// (c) ПАРАЛЛЕЛЬНО — свежая схема (или база) на каждый ФАЙЛ тестов, чтобы воркеры
//     не делили состояние. Максимум изоляции, максимум настройки; для параллелизма.
```

Стратегия (a) быстрее всего, потому что откатанная транзакция ничего не коммитит — чистить нечего. Но у неё острый край: **она ломается в момент, когда тестируемый код управляет своей транзакцией и коммитит.** Если твой сервис открывает свой `dataSource.transaction(...)` и коммитит внутри, этот коммит сбегает из твоей внешней тестовой транзакции (или вложенный savepoint освобождается), и твой `ROLLBACK` его больше не отменяет. Для такого кода откатывайся к (b) TRUNCATE. Ловушка, которую надо усвоить: **общее изменяемое состояние БД + параллельный раннер = зависящая от порядка флакость.** Изолируй на каждый тест или на каждого воркера — никогда не давай двум тестам трогать одни закоммиченные строки.

<Inset kind="why" lang="ru">
Почему интеграционный тест с реальной БД стоит своей медлительности по сравнению с замоканным repo? Потому что баги, доходящие до прода из слоя данных, — нарушения ограничений, каскадные удаления, дрифт миграций, ошибки ORM-маппинга, сюрпризы изоляции и блокировок — это ровно тот класс, который мок не может смоделировать. Мок возвращает то, что ты ему велел, поэтому он утверждает *твоё допущение*; настоящая база утверждает *реальность*, включая те части реальности, где ты ошибся. Нарушение уникальности, каскад, снёсший дочерние строки, миграция, которая не прогналась, — ни одно из них невыразимо против `jest.fn()`. Ты платишь секундами времени и Docker-контейнером, чтобы купить покрытие единственного класса багов, к которому моки структурно слепы. Это не терпимость к медленным тестам; это единственное место, где такие дефекты можно поймать до прода.
</Inset>

<TradeoffMatrix
  id="nest-06-testing-04-integration-testing-with-db-tradeoff-1"
  lessonSlug="04-integration-testing-with-db"
  lang="ru"
  prompt="Тебе нужно тестировать repository и query-код с настоящей уверенностью — ловя нарушения ограничений, каскады и дрифт миграций до прода. Как настроить тестовую базу?"
  options={[
    { name: "Testcontainers настоящий Postgres (та же мажорная версия) + прогон настоящих миграций + оборачивание каждого теста в транзакцию и ROLLBACK", summary: "Настоящий движок, изолировано, быстро.", correct: true, justification: "Это сеньорский дефолт. Одноразовый Postgres той же мажорной версии, что и прод, означает, что UNIQUE/FK/CHECK-ограничения, ON DELETE CASCADE, маппинг JSONB и enum ведут себя ровно так, как будут в проде. Прогон настоящих миграций (runMigrations, не synchronize) заставляет дрифт миграций падать здесь, а не на деплое. Пер-тестовый transaction rollback даёт изоляцию с почти нулевой ценой очистки — ничего не коммитится, значит чистить нечего, — держа сьют достаточно быстрым для CI. Ты платишь ~1–3с старта контейнера один раз и ~10–100мс на тест за настоящее утверждение реальности." },
    { name: "Замокать repository (jest.fn) и проверять против мока", summary: "Утверждает твоё допущение.", justification: "Именно так баги ограничения и каскада дошли до прода. Мок возвращает то, что ты запрограммировал, поэтому подтверждает твои допущения и больше ничего: не может обеспечить UNIQUE, не может запустить FK-каскад, не может прогнать миграцию, не может вскрыть N+1. Это правильный инструмент для юнит-тестирования чистой логики с заглушенной БД — но он структурно слеп к классу багов слоя данных, который этот слой как раз и должен ловить." },
    { name: "SQLite in-memory как тестовая база", summary: "Другой движок, зелёные тесты, которые врут.", justification: "SQLite — настоящая база, поэтому кажется честной, но это другой движок, не Postgres: аффинность типов, JSONB, последовательности и изоляция расходятся. Ты получаешь тесты, зелёные на SQLite и красные в проде, — хуже, чем без теста, ведь ты им доверял. Интеграционные тесты должны бежать на том же движке и мажорной версии, что и прод, — в этом вся причина тянуться к контейнеру Postgres, а не к in-memory фейку." },
    { name: "Пропустить локальные БД-тесты; гонять только E2E против staging", summary: "Медленно, общее, без изоляции.", justification: "Staging — это одна общая изменяемая база без пер-тестовой изоляции, поэтому сьюты становятся зависимыми от порядка и флакими, а падающий тест может испортить состояние для всех. Это ещё и медленно (сеть + полный деплой) и далеко от кода, так что баг ограничения всплывает поздно и трудно локализуется. E2E против staging уместны для smoke-теста развёрнутой системы, но не могут заменить быстрое, изолированное, пер-тестовое интеграционное покрытие твоего query-кода." },
  ]}
/>

<Quiz
  id="nest-06-testing-04-integration-testing-with-db-quiz-1"
  lessonSlug="04-integration-testing-with-db"
  lang="ru"
  question="Твой сервис полностью покрыт юнит-тестами с замоканным repository и покрытием 94%, и всё же продовый INSERT падает на ограничении UNIQUE, а админское удаление молча каскадит дочерние строки. Какой класс багов замоканные юнит-тесты структурно не поймали?"
  choices={[
    { label: "Баги реальности БД: нарушения ограничений (UNIQUE/FK/CHECK), правила каскадов, дрифт миграций и реальный ORM/SQL-маппинг — то, для чего мок возвращает допущения вместо того, чтобы обеспечивать", correct: true },
    { label: "Чисто бизнес-логические баги в методах сервиса, которые моки не могут прогнать", misconception: "Моки ХОРОШИ в прогоне бизнес-логики — это и есть то, что юнит-тесты делают лучше всего, с заглушенной БД. Слепое пятно противоположно: всё, что зависит от реального поведения базы (ограничения, каскады, миграции, ORM-маппинг). Мок возвращает то, что ты ему велел, поэтому подтверждает твои допущения; он не может обеспечить ограничение UNIQUE или запустить каскад. Только интеграционный тест с реальной БД ловит это." },
    { label: "Сетевые и таймаут-сбои между приложением и базой", misconception: "Временные сетевые/таймаут-сбои — отдельная забота (ретраи, пулы соединений, отказоустойчивость), и это не то же, что нарушение UNIQUE или каскад. Это детерминированные поведения целостности данных схемы базы — UNIQUE, FK, ON DELETE CASCADE, — которые мок не может смоделировать, а интеграционный тест на настоящем Postgres может. Класс багов здесь — реальность базы, а не флакающий IO." },
    { label: "Ошибки типов TypeScript, которые должен был поймать компилятор", misconception: "Нарушение UNIQUE и каскадное удаление — это рантайм-поведения базы, а не ошибки типов времени компиляции — код типизируется нормально. Ограничения схемы и ссылочные действия живут в базе, невидимые для системы типов и для мока. Чтобы поймать их, нужно выполнить против настоящего Postgres с применёнными настоящими миграциями." },
  ]}
/>

<Quiz
  id="nest-06-testing-04-integration-testing-with-db-quiz-2"
  lessonSlug="04-integration-testing-with-db"
  lang="ru"
  question="Для пер-тестовой изоляции ты оборачиваешь каждый тест в транзакцию и делаешь ROLLBACK в afterEach. Это самый быстрый вариант — но когда эта стратегия молча ломается?"
  choices={[
    { label: "Когда тестируемый код управляет и КОММИТИТ свою собственную транзакцию — этот коммит сбегает из твоей внешней тестовой транзакции, так что rollback больше не отменяет его записи", correct: true },
    { label: "Когда тесты бегут последовательно, а не параллельно — rollback работает только с параллельным раннером", misconception: "Изоляция через rollback не зависит от параллелизма; она прекрасно работает последовательно. (Параллелизм — это случай для свежей схемы/БД на воркера, стратегия c.) Transaction-rollback быстрее всего именно потому, что ничего не коммитится, значит чистить нечего, — но ломается, когда тестируемая система коммитит СВОЮ транзакцию, которая затем сбегает из твоей объемлющей тестовой транзакции." },
    { label: "Когда тест только читает и никогда не пишет — откатывать нечего", misconception: "Тест только на чтение — лёгкий случай: rollback — безвредный no-op, и изоляция тривиально сохраняется. Стратегия ломается в противоположной ситуации — когда тестируемый код открывает и коммитит свою транзакцию, так что его записи коммитятся по-настоящему, и твой внешний ROLLBACK не может их отменить. Для такого кода переключаешься на TRUNCATE между тестами." },
    { label: "Когда у базы есть ограничения внешних ключей — rollback не может отменить каскадные удаления", misconception: "ROLLBACK отменяет каскадное удаление так же, как любую другую запись в той же транзакции — FK-каскады полностью транзакционны в Postgres. Настоящий режим отказа — коммит внутри тестируемой системы, сбегающий из границы тестовой транзакции; раз закоммичено, никакой внешний rollback до него не дотянется, поэтому откатываешься к TRUNCATE." },
  ]}
/>

</Explanation>

<FlowDiagram
  label="Жизненный цикл интеграционного сьюта на Testcontainers. Один раз на сьют beforeAll стартует настоящий контейнер Postgres (той же мажорной версии, что и прод), затем прогоняет против него настоящие миграции, затем собирает тестовый модуль Nest, нацеленный на динамический URL подключения контейнера. Затем на каждый тест сьют начинает транзакцию, прогоняет тело теста против настоящей базы и откатывает транзакцию, чтобы следующий тест видел чистую схему без закоммиченных данных. Наконец afterAll уничтожает DataSource и сносит контейнер."
  hue="var(--d-systems)"
  perRow={3}
  nodes={[
    { id: "start", label: "Старт Postgres", sub: "beforeAll, ~1–3с" },
    { id: "migrate", label: "Прогон миграций", sub: "не synchronize" },
    { id: "module", label: "Сборка тест-модуля", sub: "нацелить на URL" },
    { id: "begin", label: "BEGIN", sub: "на тест" },
    { id: "test", label: "Прогон теста", sub: "реальная БД, реальный SQL" },
    { id: "rollback", label: "ROLLBACK", sub: "изолируем след. тест" },
    { id: "teardown", label: "Снос", sub: "afterAll, стоп контейнер" },
  ]}
  edges={[
    { from: "start", to: "migrate", label: "схема готова" },
    { from: "migrate", to: "module", label: "DataSource поднят" },
    { from: "module", to: "begin", label: "сьют готов" },
    { from: "begin", to: "test", label: "в tx" },
    { from: "test", to: "rollback", label: "afterEach" },
    { from: "rollback", to: "begin", label: "след. тест" },
    { from: "rollback", to: "teardown", label: "последний тест" },
  ]}
  caption="Настройка — раз на сьют (start → migrate → сборка модуля); изоляция — раз на тест (BEGIN → test → ROLLBACK замыкается назад); снос утилизирует одноразовую БД. Петля rollback и делает сьют независимым от порядка и быстрым."
/>

<KeyTakeaway>Моки утверждают допущения; настоящий Postgres — реальность. Бери Testcontainers на версии прода, прогоняй настоящие миграции (не synchronize), изолируй на тест через rollback — TRUNCATE, когда код сам коммитит.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="nest-06-testing-04-integration-testing-with-db-retrieval"
  lang="ru"
  questions={[
    { q: "Какой класс багов интеграционный тест против реальной базы ловит, а замоканный юнит-тест структурно не может, и почему?", a: "Он ловит баги реальности базы: нарушения ограничений (UNIQUE, NOT NULL, CHECK, внешние ключи), ссылочные действия вроде ON DELETE CASCADE / SET NULL, дрифт миграций (миграция, что не прогналась, неверная колонка или индекс), реальный маппинг ORM-в-SQL (JSONB, enum, точность numeric, последовательности, snake_case↔camelCase) и настоящее поведение транзакций/изоляции. Мок структурно не может поймать это, потому что мок возвращает то, что ты в него запрограммировал, — он кодирует твои допущения, поэтому может лишь их подтвердить. У него нет схемы, поэтому он не может обеспечить ограничение UNIQUE, запустить FK-каскад, прогнать миграцию, вскрыть N+1. Настоящая база утверждает реальность, включая те её части, где твои допущения ошиблись. Поэтому канонические отказы — продовый INSERT, падающий на UNIQUE, который мок всё равно «сохранил», или каскад, молча сносящий дочерние строки, которых не проверял ни один юнит-тест, — проходят сквозь сьют с 94% покрытия на моках, и только интеграционный тест на настоящем Postgres поймал бы их. Цена — ~1–3с старта контейнера один раз и ~10–100мс на тест против ~1мс на моке, поэтому их пишут сильно меньше — пирамида остаётся пирамидой." },
    { q: "Пройди настройку интеграции на Testcontainers с пер-тестовой изоляцией: контейнер, миграции, тестовый модуль и стратегию изоляции — включая, когда быстрая ломается.", a: "Один раз на сьют в beforeAll: стартуешь одноразовый Postgres через @testcontainers/postgresql на ТОЙ ЖЕ мажорной версии, что и прод (new PostgreSqlContainer('postgres:16').start(), ~1–3с), берёшь его динамический URI подключения (никогда не хардкодь 5432), скармливаешь этот URL в TypeOrmModule.forRoot с synchronize:false, собираешь тестовый модуль Nest и прогоняешь НАСТОЯЩИЕ миграции против контейнера (dataSource.runMigrations(), не synchronize:true), чтобы дрифт миграций всплыл в тесте, а не на деплое. В afterAll уничтожаешь DataSource и останавливаешь контейнер. Для пер-тестовой изоляции три стратегии в порядке предпочтения: (a) оборачиваешь каждый тест в транзакцию и ROLLBACK в afterEach — быстрее всего, потому что ничего не коммитится, значит чистить нечего; (b) TRUNCATE всех таблиц между тестами — надёжно, но медленнее (реальные DELETE + перетряска индексов); (c) свежая схема или база на каждый файл тестов для параллелизма — максимум изоляции, максимум настройки. Стратегия (a) молча ломается, когда тестируемый код управляет и КОММИТИТ свою транзакцию: этот коммит сбегает из внешней тестовой транзакции, так что ROLLBACK больше не отменяет его записи; для такого кода откатывайся к (b) TRUNCATE. Управляющее правило: общее изменяемое состояние БД плюс параллельный раннер равно зависящей от порядка флакости, поэтому изолируй на тест или на воркера." },
  ]}
/>

<Recap lang="ru">
Интеграционные тесты занимают изгиб пирамиды тестов: юнит-тесты моками утверждают твою логику в изоляции (~1мс, много), а интеграционные гоняют код против настоящего коллаборатора — базы — чтобы утверждать реальность (~10–100мс, меньше). Мок возвращает то, что ты в него запрограммировал, поэтому подтверждает твои допущения и структурно слеп к классу багов слоя данных: нарушениям ограничений UNIQUE/FK/CHECK, правилам ON DELETE CASCADE, дрифту миграций, реальному ORM/SQL-маппингу (JSONB, enum, последовательности) и поведению транзакций/изоляции. Эта слепота — то, как сьют с 94% покрытия на моках пропустил в прод нарушение UNIQUE и молчаливое каскадное удаление. Не чини это SQLite — другой движок даёт зелёные тесты, которые врут; гоняй на том же движке и мажорной версии, что и прод. Testcontainers (@testcontainers/postgresql) стартует одноразовый Postgres в Docker один раз на сьют (~1–3с), отдаёт тебе динамический URL подключения, на который ты нацеливаешь TypeOrmModule.forRoot, и ты прогоняешь настоящие миграции (runMigrations, никогда synchronize:true), чтобы дрифт падал в тесте. Изолируй каждый тест, иначе сьют станет зависимым от порядка и флакающим: предпочитай оборачивать каждый тест в транзакцию и откатывать в afterEach (быстрее всего — ничего не коммитится, чистить нечего), откатывайся к TRUNCATE между тестами, когда тестируемый код коммитит свою транзакцию (которая сбегает из внешнего rollback), и используй свежую схему на файл при параллелизме. Дисциплина: настоящий движок, настоящие миграции, пер-тестовая изоляция — оплачено секундами, возвращено как тот класс багов, который моки никогда не увидят. Теперь, когда в проде выскочит нарушение UNIQUE на маршруте со «100% покрытием», ты знаешь, куда добавить интеграционный тест — и какую стратегию изоляции выбрать.
</Recap>
