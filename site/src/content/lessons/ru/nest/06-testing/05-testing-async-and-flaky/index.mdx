---
concepts:
- async-tests
- fake-timers
- queue-event-testing
- flaky-tests
- determinism
deepensInto: []
estMin: 18
lang: ru
lessonType: topic
level: senior
order: 5
prereqs: []
slug: "05-testing-async-and-flaky"
sources:
- https://docs.nestjs.com/fundamentals/testing
- https://jestjs.io/docs/timer-mocks
spiral: []
status: ready
summary: 'Асинхронные тесты падают, когда проверка обгоняет работу, которую проверяет. Всегда await; подмени часы fake-таймерами (и слей микротаски); жди обработчик, а не setTimeout(50). Flaky-тест — это баг-репорт о недетерминизме: чини гонку, а не глуши её retryTimes(3).'
title: 'Тестирование асинхронного кода и борьба с flaky-тестами'
track: nest
unit: 06-testing
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";
import Infographic from "~/components/diagram/Infographic.astro";
import flakeCompounding from "./infographics/flake-compounding.svg?raw";

<Hook>
Тест проходил тысячу раз. `it('notifies on signup')` эмитил событие `user.created` и проверял, что почтовый сервис вызвали — зелёный на каждом ноутбуке, зелёный в CI. И вот во вторник он стал красным. Никто его не трогал. Перезапуск снова сделал его зелёным, поэтому кто-то вписал `jest.retryTimes(3)` в setup-файл, и сьют притих. То, что заглушил этот retry, было настоящей гонкой: обработчик события выполнялся на следующем тике, проверка выполнялась *сейчас*, и на нагруженной CI-машине обработчик иногда проигрывал забег. Та же гонка жила в продакшен-коде — два слушателя мутировали общий агрегат. Через шесть недель она под нагрузкой испортила балансы. Flaky-тест был не шумом. Он был баг-репортом, поданным заранее, который мы заретраили в тишину. Этот урок — о том, как тестировать асинхронный код так, чтобы он не мог гоняться, и как относиться к flakiness как к дефекту, которым она и является.
</Hook>

<Crux>Async-тест, который не ждёт работу, гоняется с ней. Чини недетерминизм fake-таймерами, awaited-обработчиками — никогда retryTimes.</Crux>

<Explanation>

## Почему async-тесты гоняются: непрожданная проверка

Корень провала прост: если тест не делает `await` асинхронной работы, которую проверяет, проверка выполняется *до* того, как работа завершится. Ожидание сверяется со старым состоянием — поэтому оно либо проходит по неправильной причине, либо падает время от времени, либо болтающийся промис утекает в *следующий* тест и портит его. Вернуть или дождаться промиса — это контракт, который говорит раннеру «жди вот это».

```ts
// WRONG — fire-and-forget: the assertion runs before saveUser resolves
it('creates a user', () => {
  service.createUser(dto);          // promise dropped on the floor
  expect(repo.save).toHaveBeenCalled(); // races the await — passes/fails by luck
});

// RIGHT — await the work, then assert against settled state
it('creates a user', async () => {
  await service.createUser(dto);    // runner waits for this promise
  expect(repo.save).toHaveBeenCalledWith(dto);
});
```

Используй форму `async`/`await`, а не легаси-колбэк `done`, если только тебе по-настоящему не нужен `done` для события, которое не превратить в промис — и даже тогда вызывай `done(err)` при ошибке, иначе брошенная проверка висит до таймаута. Правило абсолютно: никогда не делай fire-and-forget асинхронного вызова в тесте. Каждый промис, который ты создаёшь, ты ждёшь или возвращаешь.

## Fake-таймеры: превращаем 30 секунд в ноль

Реальный код использует `setTimeout`, `setInterval`, debounce и retry-with-backoff. Тест, который пускает это на реальных часах, либо ждёт реальные секунды, либо, хуже, проверяет до того, как таймер сработал. `jest.useFakeTimers()` подменяет глобальные часы теми, которыми управляешь *ты*. `jest.advanceTimersByTimeAsync(ms)` и `jest.runAllTimersAsync()` детерминированно перематывают время вперёд — 30-секундный backoff прогоняется заметно меньше чем за миллисекунду.

```ts
// Тестируемый сервис: ретрай с экспоненциальным backoff, максимум 3 попытки
async function callWithBackoff(fn: () => Promise<string>): Promise<string> {
  for (let attempt = 0; ; attempt++) {
    try {
      return await fn();
    } catch (err) {
      if (attempt >= 2) throw err;
      await new Promise((r) => setTimeout(r, 1000 * 2 ** attempt)); // 1s, 2s
    }
  }
}

it('retries twice with backoff then succeeds', async () => {
  jest.useFakeTimers();
  const fn = jest
    .fn<Promise<string>, []>()
    .mockRejectedValueOnce(new Error('flap'))
    .mockRejectedValueOnce(new Error('flap'))
    .mockResolvedValueOnce('ok');

  const promise = callWithBackoff(fn);     // kicks off attempt 0, hits the 1s wait

  await jest.advanceTimersByTimeAsync(1000); // fast-forward the 1s backoff
  await jest.advanceTimersByTimeAsync(2000); // fast-forward the 2s backoff

  await expect(promise).resolves.toBe('ok');
  expect(fn).toHaveBeenCalledTimes(3);
});
```

Ловушка — смешивать fake-таймеры с реальными промисами. `setTimeout` подделан, но `await` между попытками — это реальный микротаск: перемотка таймера запускает колбэк, но прожданное продолжение выполнится, только когда очередь микротасков сольётся. Варианты таймерных API с суффиксом `*Async` сливают за тебя микротаски между перемотками; синхронный `advanceTimersByTime` — нет, поэтому продолжение никогда не выполняется и тест либо висит, либо проверяет недоделанное состояние. Используй асинхронные таймерные API всегда, когда между твоими таймерами стоят реальные промисы.

<Inset kind="why" lang="ru">
Почему `jest.retryTimes(3)` — неправильное лекарство для flaky-теста? Потому что flakiness *и есть* недетерминизм, а retry маскирует его, не убирая гонку — та же гонка по-прежнему живёт в продакшене, где никакого retry нет. Тест, который проходит только с ретраем, говорит тебе нечто конкретное: в коде или тесте есть настоящий баг порядка или таймингов. Заглуши его — и ты отгружаешь баг, теперь невидимый. Retry меняет красный CI сегодня на скрытую порчу данных потом и активно уничтожает сигнал — единственное место, где гонку было дёшево поймать. Лекарство — детерминизм: подделай таймеры, изолируй и сбрасывай общее состояние, правильно жди работу, засей случайность. Сделай тест воспроизводимым, и он не сможет flake-ать, потому что гоняться будет нечему.
</Inset>

## Тестирование очередей и событий: жди обработчик, не спи на нём

`@nestjs/event-emitter` и BullMQ (библиотека очередей задач для Node.js на основе Redis) оба по дизайну запускают обработчики *асинхронно*. Тест, который эмитит событие и сразу проверяет, гоняется со слушателем — ровно тот инцидент с регистрацией. Есть соблазнительный, сломанный фикс: `await new Promise(r => setTimeout(r, 50))` перед проверкой, в надежде, что обработчик закончил. На быстрой машине — закончил; на нагруженной CI-машине — нет. Этот `setTimeout(50)` *и есть* flakiness.

```ts
// FLAKY — emit then assert immediately races the async listener
emitter.emit('user.created', user);
expect(mailer.send).toHaveBeenCalled();   // listener may not have run yet

// FLAKY — the "fix" that just lowers the failure rate, never to zero
emitter.emit('user.created', user);
await new Promise((r) => setTimeout(r, 50)); // hope, not guarantee
expect(mailer.send).toHaveBeenCalled();

// DETERMINISTIC — call the consumer directly and await it
await listener.handleUserCreated(user);   // no event bus, no race
expect(mailer.send).toHaveBeenCalledWith(user.email);

// DETERMINISTIC — or await a signal the handler resolves
const done = new Promise((res) => emitter.once('email.sent', res));
emitter.emit('user.created', user);
await done;                                 // resolves only after the work
expect(mailer.send).toHaveBeenCalled();
```

Для BullMQ раздели задачу: в юнит-тесте проверь, что джоба была *поставлена в очередь* (`expect(queue.add).toHaveBeenCalledWith('send-email', payload)`), и отдельно юнит-тестируй метод `process` процессора напрямую с фейковым `Job`. Если нужен end-to-end путь — запусти настоящий воркер в тесте и дождись его события `completed`, никогда не опрашивай таймаутом. Принцип один: замени «подожди и понадейся» детерминированным сигналом, который ты реально ждёшь.

## Flaky-тест как баг-репорт

Flaky-тест проходит ~95% запусков. Воспринимай это число как уровень серьёзности дефекта, а не как допуск. Корневых причин — короткий список:

1. **Тайминг** — `setTimeout`-ожидания, реальные часы, реальная сетевая задержка. Фикс: fake-таймеры, awaited-сигналы.
2. **Общее изменяемое состояние** — статик на уровне модуля, реальная строка в БД, синглтон — в сочетании с зависимостью от порядка тестов или параллельными воркерами, наступающими друг другу на ноги. Фикс: сбрасывай состояние в `beforeEach`, изолируй данные на тест, не дели записываемый синглтон.
3. **Реальное IO и случайность** — wall-clock время, `Math.random()`, сеть. Фикс: инжекти и стабь часы/RNG, засевай случайность, мокай границу.
4. **Непрожданный async** — промис из теста A, оседающий во время теста B. Фикс: жди всё; роняй сьют на необработанных rejection.

Все четыре корневых причины имеют одну структуру: что-то гоняется с чем-то ещё. Диагностируй, какая пара гоняется, примени нужный фикс — и flakiness исчезнет не потому, что ты дал больше времени, а потому что убрал недетерминизм полностью.

<TradeoffMatrix
  id="nest-06-testing-05-testing-async-and-flaky-tradeoff-1"
  lessonSlug="05-testing-async-and-flaky"
  lang="ru"
  prompt="Тест для обработчика события проходит примерно в 90% случаев и падает в остальных. CI красный с перебоями, и команда начинает его игнорировать. Что ты делаешь?"
  options={[
    { name: "Найти недетерминизм — дождаться обработчик напрямую или ждать детерминированный сигнал, использовать fake-таймеры, сбросить общее состояние — и сделать так, чтобы тест проходил 100% времени", summary: "Чини гонку.", correct: true, justification: "Проходимость 90% — это гонка с именем на ней. Обработчик выполняется асинхронно; проверка гоняется с ним; лекарство — убрать гонку: вызвать метод консьюмера и дождаться его, или дождаться промиса, который резолвит обработчик, плюс fake-таймеры для любого backoff/debounce и сброс в beforeEach для общего состояния. Теперь тест детерминирован — он не может flake-ать, потому что гоняться нечему, и, что важно, ТА ЖЕ гонка, что флакала тест, кусает продакшен. Починив тест, чинишь код." },
    { name: "Добавить jest.retryTimes(3), чтобы переходный провал заретраился", summary: "Прячет гонку.", justification: "Это ловушка. Retry маскирует недетерминизм, не убирая его — гонка по-прежнему есть в продакшене, где retry нет. Тест, который проходит только с ретраем, говорит, что в коде или тесте есть настоящий баг порядка; заглушив его, ты отгружаешь этот баг, теперь невидимый. Ты сменил красный CI сегодня на скрытую порчу потом и уничтожил единственный дешёвый сигнал о том, что гонка существует." },
    { name: "Добавить await new Promise(r => setTimeout(r, 100)) перед проверкой, чтобы дать обработчику время", summary: "Снижает, но не обнуляет частоту провалов.", justification: "Sleep лишь снижает частоту флака, расширяя окно — она никогда не достигает нуля, потому что на нагруженной CI-машине 100мс всё равно не всегда хватает, а на быстрой ты сделал каждый прогон на 100мс медленнее впустую. Это тот же тайминговый недетерминизм, переодетый. Замени надежду детерминированным awaited-сигналом, а не более длинной догадкой." },
    { name: "Удалить тест — от него больше хлопот, чем пользы", summary: "Удаляет баг-репорт.", justification: "Удаление flaky-теста убирает симптом и сохраняет болезнь: гонка в продакшене теперь полностью не покрыта тестами. Тест был (плохо сформулированным) баг-репортом о настоящем недетерминизме. Выбросить его — самый дорогой вариант: ты теряешь покрытие И сохраняешь скрытый баг. Вместо этого почини детерминизм." },
  ]}
/>

Антипаттерн, который связывает их вместе, — потянуться за `jest.retryTimes(3)`, чтобы «стабилизировать» сьют. Он работает в узком смысле — CI зеленеет — и проваливается во всех смыслах, которые важны: гонка по-прежнему в проде, сигнал потерян, а следующий инженер доверяет сьюту, который лжёт. Сеньорская позиция — flaky-тест есть отчёт о дефекте недетерминизма, в тесте или в коде, и единственный верный ответ — восстановить детерминизм. Цифры делают ставки конкретными. Один тест с проходимостью 95% в сьюте из 1000 тестов заставляет *весь сьют* падать постоянно — независимые провалы складываются, поэтому зелёная доля сьюта обваливается заметно ниже доли любого отдельного теста. И человеческая цена хуже цены CI: стоит сьюту начать флакать, инженеры учатся перезапускать красное, и в день, когда *настоящая* регрессия становится красной, они перезапускают и её. Flakiness не просто тратит минуты; она тренирует команду игнорировать тревогу.

<Infographic
  svg={flakeCompounding}
  label="Линейный график зелёной доли прогонов сьюта по мере роста числа независимых flaky-тестов, когда каждый flaky-тест проходит в 95 процентах случаев. При 1 flaky-тесте сьют зелёный в 95 процентах прогонов; 5 flaky-тестов снижают до 77 процентов; 10 — до 60 процентов; 20 — до 36 процентов; 50 — примерно до 8 процентов: зелёная доля убывает как 0,95 в степени N."
  caption="Независимые провалы складываются: каждый тест с проходимостью 95% сам по себе оставляет весь сьют зелёным лишь в 0,95^N прогонов. Горстки флаков хватает, чтобы CI краснел почти всегда."
  hue="var(--d-systems)"
/>

<FlowDiagram
  label="Поток решения для flaky-теста, рассматриваемого как баг-репорт, а не как кандидат на retry. Старт у flaky-теста. Сначала диагностируй корневую причину в одну из четырёх корзин: тайминг (реальные часы, setTimeout-ожидания), общее состояние (статик, строка в БД, синглтон, мутируемый между тестами), реальное IO (сеть, wall-clock, случайность) или непрожданный async (промис, утекающий между тестами). Каждая корзина ведёт к своему фиксу детерминизма: тайминг — к fake-таймерам с advanceTimersByTimeAsync, общее состояние — к изоляции и сбросу в beforeEach, реальное IO — к стабу границы и засеву случайности, непрожданный async — к await или возврату промиса. Все четыре фикса сходятся на одном исходе: стабильный, детерминированный тест, который не может гоняться. Шорткат retryTimes намеренно отсутствует на этой диаграмме, потому что он пропускает диагностику и оставляет гонку в продакшене."
  hue="var(--d-systems)"
  perRow={3}
  nodes={[
    { id: "flaky", label: "flaky-тест", sub: "~95% pass" },
    { id: "diagnose", label: "диагноз причины", sub: "какая корзина?" },
    { id: "timing", label: "тайминг", sub: "→ fake-таймеры" },
    { id: "state", label: "общее состояние", sub: "→ изоляция/сброс" },
    { id: "io", label: "реальное IO", sub: "→ стаб + засев" },
    { id: "await", label: "непрожданный", sub: "→ дождись" },
    { id: "stable", label: "детерминирован", sub: "не гоняется" },
  ]}
  edges={[
    { from: "flaky", to: "diagnose", label: "это баг-репорт" },
    { from: "diagnose", to: "timing", label: "часы/setTimeout" },
    { from: "diagnose", to: "state", label: "статик/БД/синглтон" },
    { from: "diagnose", to: "io", label: "сеть/random" },
    { from: "diagnose", to: "await", label: "утекший промис" },
    { from: "timing", to: "stable", label: "фикс" },
    { from: "state", to: "stable", label: "фикс" },
    { from: "io", to: "stable", label: "фикс" },
    { from: "await", to: "stable", label: "фикс" },
  ]}
  caption="Flaky-тест диагностируется в одну из четырёх корзин недетерминизма, у каждой свой фикс детерминизма, сходящийся на стабильном тесте. retryTimes отсутствует намеренно: он пропускает диагностику и отгружает гонку."
/>

<Quiz
  id="nest-06-testing-05-testing-async-and-flaky-quiz-1"
  lessonSlug="05-testing-async-and-flaky"
  lang="ru"
  question="Тест эмитит событие `user.created` на @nestjs/event-emitter, а на следующей строке проверяет, что почтовик вызвали. Локально проходит, но в CI падает примерно в 10% случаев. Почему?"
  choices={[
    { label: "Обработчик события выполняется асинхронно — проверка на следующей строке гоняется со слушателем, и на нагруженной CI-машине слушатель иногда ещё не отработал", correct: true },
    { label: "В CI медленнее сеть, поэтому письмо иногда реально не отправляется", misconception: "В юнит-тесте ничего реального не отправляется — mailer.send это мок. Провал не сетевой; он про порядок. Эмиттер диспатчит обработчик асинхронно, поэтому синхронная проверка на следующей строке может выполниться раньше обработчика. Лекарство — дождаться обработчик (вызвать напрямую) или дождаться сигнала, который он резолвит, а не трогать сеть." },
    { label: "Событие эмитнулось дважды, поэтому счётчик вызовов мока сбит", misconception: "Двойная эмиссия роняла бы тест стабильно, а не в ~10% случаев. Перебойность — это подсказка: это гонка между асинхронным обработчиком и немедленной проверкой. Иногда обработчик выигрывает забег и тест зелёный; под нагрузкой CI он проигрывает. Проблема в детерминизме, а не в счётчике вызовов." },
    { label: "jest.useFakeTimers() утекает из предыдущего теста", misconception: "Утекшие fake-таймеры скорее заморозили бы время и роняли тест детерминированно, а не давали бы ~10% перебойный провал, привязанный к нагрузке. Настоящая причина — непрожданный async-обработчик, гоняющийся с проверкой. (Утекшее состояние — РЕАЛЬНЫЙ источник флака, но здесь конкретный симптом указывает на непрожданный emit, чинимый ожиданием обработчика или сигнала резолва.)" },
  ]}
/>

<Quiz
  id="nest-06-testing-05-testing-async-and-flaky-quiz-2"
  lessonSlug="05-testing-async-and-flaky"
  lang="ru"
  question="Нужно протестировать путь ретрая с 30с экспоненциального backoff так, чтобы тест не занимал 30 секунд. Что делаешь и какая единственная засада?"
  choices={[
    { label: "jest.useFakeTimers() и перемотка через jest.advanceTimersByTimeAsync(ms) — и ты обязан слить микротаски (используй *Async таймерные API) между перемотками, иначе прожданное продолжение никогда не выполнится", correct: true },
    { label: "jest.useFakeTimers() и синхронный jest.advanceTimersByTime(ms) — fake-таймеры обрабатывают прожданные промисы автоматически", misconception: "Fake-таймеры подменяют часы, но НЕ сливают очередь микротасков. setTimeout подделан, но await между ретраями — реальный микротаск; синхронный advanceTimersByTime запускает колбэк таймера, не сливая микротаски, поэтому прожданное продолжение никогда не выполняется и тест висит или проверяет недоделанное состояние. Нужен advanceTimersByTimeAsync (или runAllTimersAsync), который сливает микротаски между перемотками." },
    { label: "Оставить реальные таймеры, но снизить backoff до 30мс в тесте через оверрайд конфига", misconception: "Уменьшение задержки всё равно оставляет тайминг реальных часов в тесте — быстрее, но всё ещё недетерминированно, и ты теперь тестируешь другой backoff, чем гоняет продакшен. Fake-таймеры делают это детерминированным И позволяют проверять реальное расписание 30с (продакшен-значения) за ноль wall-clock времени. Не меняй корректность на меньшую реальную задержку." },
    { label: "Обернуть проверку в await new Promise(r => setTimeout(r, 30000)), чтобы реальный backoff завершился", misconception: "Это заставляет тест реально ждать 30 секунд — ровно та проблема, которой ты избегаешь — и он всё ещё гоняется с работой. Fake-таймеры существуют именно чтобы 30с-расписание прогонялось за ~0мс детерминированно; перематывай их асинхронным API и проверяй сразу после." },
  ]}
/>

</Explanation>

<KeyTakeaway>Жди async-работу, иначе проверка гоняется с ней. Бери fake-таймеры + advanceTimersByTimeAsync (слей микротаски) для backoff; жди обработчики, не setTimeout(50). Flaky-тест сигналит недетерминизм: чини, не ретрай.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="nest-06-testing-05-testing-async-and-flaky-retrieval"
  lang="ru"
  questions={[
    { q: "Почему async-тесты гоняются и как детерминированно протестировать (а) 30с backoff-ретрай и (б) обработчик события/очереди?", a: "Async-тесты гоняются, потому что если не дождаться async-работу, которую проверяешь, проверка выполняется до того, как работа завершится — она проходит по неправильной причине, падает время от времени или болтающийся промис утекает в следующий тест. Контракт — возвращать или ждать каждый промис, чтобы раннер ждал; используй async/await, а не fire-and-forget, и не легаси-done, если событие по-настоящему не превратить в промис (тогда вызывай done(err) при ошибке). (а) Для 30с экспоненциального backoff подмени часы через jest.useFakeTimers(), запусти вызов, затем перематывай через jest.advanceTimersByTimeAsync(1000) / advanceTimersByTimeAsync(2000) — перематывая 30с в заметно меньше миллисекунды, проверяя реальное продакшен-расписание. Засада: fake-таймеры не сливают очередь микротасков, поэтому между перемотками ты обязан слить микротаски — ровно для этого существуют *Async таймерные API (advanceTimersByTimeAsync, runAllTimersAsync); синхронный advanceTimersByTime оставляет прожданное продолжение невыполненным и тест висит. (б) Для обработчика @nestjs/event-emitter или BullMQ никогда не делай emit-then-assert (он гоняется с асинхронным слушателем) и никогда не await setTimeout(50) (надежда, не гарантия). Вместо этого вызови метод консьюмера напрямую и дождись его (нет шины, нет гонки) или дождись промиса, который резолвит обработчик (emitter.once(...)). Для BullMQ проверь, что джоба поставлена в очередь, и отдельно юнит-тестируй метод process процессора с фейковым Job; настоящий воркер запускай только для end-to-end и жди его событие completed." },
    { q: "Что такое flaky-тест на самом деле, каковы его четыре корневые причины и почему jest.retryTimes(3) — неправильное лекарство?", a: "Flaky-тест проходит ~95% запусков, и это число — уровень серьёзности дефекта, а не допуск: это баг-репорт, поданный заранее, о недетерминизме в тесте или коде. Четыре корневые причины: (1) тайминг — setTimeout-ожидания, реальные часы, реальная сеть — чинится fake-таймерами и awaited-сигналами; (2) общее изменяемое состояние — статик модуля, реальная строка в БД, синглтон — в сочетании с зависимостью от порядка тестов или параллельными воркерами, чинится сбросом состояния в beforeEach и изоляцией данных на тест; (3) реальное IO и случайность — wall-clock, Math.random, сеть — чинится инжектом/стабом часов и RNG, засевом случайности, моком границы; (4) непрожданный async — промис из теста A, оседающий во время теста B — чинится ожиданием всего и падением на необработанных rejection. jest.retryTimes(3) — неправильное лекарство, потому что flakiness ЕСТЬ недетерминизм; retry маскирует его, не убирая гонку, и та же гонка по-прежнему есть в продакшене, где retry нет. Тест, проходящий только с ретраем, сигналит о настоящем баге порядка/тайминга; заглушив его, ты отгружаешь этот баг невидимым. Цифры усугубляют ставки: один тест с проходимостью 95% в сьюте из 1000 тестов роняет весь сьют постоянно (независимые провалы складываются), и хуже того, стоит сьюту начать флакать, команда учится перезапускать красное — поэтому в день, когда настоящая регрессия краснеет, перезапускают и её. Восстанавливай детерминизм; никогда не заретраивай тревогу в тишину." },
  ]}
/>

<Recap lang="ru">
Async-тесты гоняются, когда проверка выполняется до того, как завершится async-работа, которую она проверяет: fire-and-forget вызов оставляет ожидание сверяющимся со старым состоянием, поэтому оно проходит по неправильной причине, падает время от времени или утекает болтающимся промисом в следующий тест — всегда возвращай или жди промис, используй async/await вместо легаси-done. Чтобы тестировать код с setTimeout/setInterval/debounce/backoff без ожидания реальных секунд, подмени часы через jest.useFakeTimers() и перематывай через jest.advanceTimersByTimeAsync(ms) или runAllTimersAsync(), превращая 30с backoff в заметно меньше миллисекунды на реальном продакшен-расписании; ловушка в том, что fake-таймеры не сливают микротаски, поэтому ты обязан использовать *Async API, чтобы слить их между перемотками, иначе прожданное продолжение никогда не выполнится. Обработчики @nestjs/event-emitter и BullMQ выполняются асинхронно, поэтому emit-then-assert гоняется со слушателем, а await setTimeout(50) лишь снижает частоту флака — вместо этого вызови метод консьюмера напрямую и дождись его или дождись сигнала, который резолвит обработчик; для Bull проверь, что джоба поставлена в очередь, и отдельно юнит-тестируй процессор, запуская настоящий воркер только для end-to-end и дожидаясь completed. Flaky-тест (~95% pass) — это баг-репорт о недетерминизме с четырьмя корневыми причинами — тайминг, общее изменяемое состояние, реальное IO/случайность, непрожданный async — у каждой свой фикс детерминизма (fake-таймеры, изоляция/сброс, стаб+засев, await). Антипаттерн — jest.retryTimes(3): он зеленит CI, пряча гонку, которая всё равно отгружается в продакшен, где retry нет, и уничтожает единственный дешёвый сигнал о том, что гонка существует. Один тест с проходимостью 95% в сьюте из 1000 тестов флакает весь сьют постоянно и тренирует команду игнорировать красное. Теперь, когда увидишь jest.retryTimes(3) в setup-файле или комментарий «sleep, чтобы дождаться обработчик», — ты знаешь: это не стабильность, это заглушённый баг-репорт, ждущий своего часа в продакшене. Чини детерминизм.
</Recap>
