---
concepts:
- testing-module
- createtestingmodule
- overrideprovider
- mocking
- repository-token
deepensInto: []
estMin: 15
lang: ru
lessonType: topic
level: middle
order: 1
prereqs: []
slug: "01-unit-testing-providers"
sources:
- https://docs.nestjs.com/fundamentals/testing
- https://jestjs.io/docs/mock-functions
- https://docs.nestjs.com/techniques/database
spiral: []
status: ready
summary: 'Unit-тестируй провайдер Nest, собрав изолированный DI-контейнер через Test.createTestingModule, замокав каждую зависимость на её границе (repository, HTTP-клиент) через useValue или overrideProvider, и проверяя поведение через jest.fn().'
title: 'Unit-тестирование провайдеров через testing module'
track: nest
unit: 06-testing
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";
import Infographic from "~/components/diagram/Infographic.astro";
import realVsMockCost from "./infographics/real-vs-mock-cost.svg?raw";

<Hook>
CI для `UsersService` месяцами был зелёным, а потом стал занимать девять минут и падать один прогон из пяти. Открываешь «unit»-тест — и видишь, что он поднимает весь `AppModule`, открывает реальное соединение с Postgres и стучится в staging-почту, чтобы «проверить» welcome-письмо. Флаки — это таймаут пула соединений; девять минут — это то, что у всех «unit»-сьют делает то же самое. И ничто из этого не тестирует то, что тебе важно, — *логику* `UsersService.register`: хэширует ли он пароль, отклоняет ли дублирующий email и вызывает ли `save` ровно один раз? Чтобы ответить на это, база данных не нужна. Нужен testing module с замоканным на границе repository.
</Hook>

<Crux>Собери контейнер через createTestingModule, замокай каждую зависимость на границе и проверяй, что провайдер сделал, а не как.</Crux>

<Explanation>

## Testing module — это одноразовый DI-контейнер

Когда видишь девятиминутный «unit»-сьют, спроси себя: зачем ему база данных? Почти всегда — незачем. Testing module возвращает это время обратно.

<Infographic
  svg={realVsMockCost}
  label="Сравнение одного и того же теста UsersService.register, прогнанного двумя способами. Сьют с реальной БД занимает около 9 минут на прогон CI, падает 1 раз из 5 на таймауте пула соединений, поднимает AppModule, Postgres и mail API и тестирует Postgres и TypeORM, а не логику. Мок-юнит-тест выполняется за миллисекунды без флаки-прогонов, без БД и без сети и прогоняет логику register()."
  caption="Та же логика, разная цена: мок каждой зависимости на границе превращает девятиминутный сьют с флаки 1 из 5 в миллисекундный и детерминированный."
  hue="var(--d-backend)"
/>

`Test.createTestingModule({ providers: [...] })` собирает *свежий, изолированный* DI-контейнер Nest, в котором есть только то, что ты перечислил. Вызов `.compile()` резолвит граф и отдаёт `TestingModule`; `module.get(Token)` достаёт из него singleton-экземпляр — твой юнит под тестом, уже связанный. Поскольку массивом `providers` управляешь ты, ты решаешь, какие зависимости реальные, а какие — дублёры. Сервис под тестом перечисляй реальным классом, а каждую зависимость, которую он инжектит, подставляй как мок.

Самый чистый способ подставить мок — кастомный провайдер с `useValue`: тот же injection-токен, фейковая реализация. `jest.fn()` на каждый метод даёт шпиона, которого можно запрограммировать и по которому можно проверять.

```ts
import { Test, TestingModule } from '@nestjs/testing';
import { getRepositoryToken } from '@nestjs/typeorm';
import { Repository } from 'typeorm';
import { UsersService } from './users.service';
import { User } from './user.entity';

describe('UsersService', () => {
  let service: UsersService;
  let repo: jest.Mocked<Repository<User>>;

  beforeEach(async () => {
    const module: TestingModule = await Test.createTestingModule({
      providers: [
        UsersService, // реальный — это юнит под тестом
        {
          provide: getRepositoryToken(User), // токен, который Nest инжектит для @InjectRepository(User)
          useValue: {
            findOne: jest.fn(),
            create: jest.fn((dto) => dto),
            save: jest.fn(),
          },
        },
      ],
    }).compile();

    service = module.get(UsersService);
    repo = module.get(getRepositoryToken(User));
  });

  it('отклоняет дублирующий email без сохранения', async () => {
    repo.findOne.mockResolvedValue({ id: 1, email: 'a@b.io' } as User);

    await expect(service.register({ email: 'a@b.io', password: 'pw' }))
      .rejects.toThrow('email already in use');

    expect(repo.save).not.toHaveBeenCalled(); // поведение, а не деталь реализации
  });

  it('сохраняет нового пользователя ровно один раз', async () => {
    repo.findOne.mockResolvedValue(null);
    repo.save.mockResolvedValue({ id: 7, email: 'new@b.io' } as User);

    const result = await service.register({ email: 'new@b.io', password: 'pw' });

    expect(repo.save).toHaveBeenCalledTimes(1);
    expect(repo.save).toHaveBeenCalledWith(
      expect.objectContaining({ email: 'new@b.io' }),
    );
    expect(result.id).toBe(7);
  });
});
```

Две детали оправдывают себя. Токен для TypeORM-repository — это **не** класс сущности, а `getRepositoryToken(User)`, синтетический токен, в который резолвится `@InjectRepository(User)`. Подставь значение под *этот* токен — и сервис получит твой мок. И проверки сделаны по **поведению**: «не сохранил при дубле», «сохранил один раз с этим email». Они никогда не лезут в приватные поля сервиса. Тесты, завязанные на поведение, переживают рефакторинг; тесты, завязанные на реализацию, ломаются в тот момент, когда ты переименовал переменную.

## Мокай на границе — и только там

Тот же паттерн покрывает любого внешнего коллаборатора. `JwtService`, HTTP-клиент, оборачивающий платёжный API, mailer — каждый это шов между твоей логикой и внешним миром, и каждый получает `useValue`-дублёра по своему токену. Что ты **не** мокаешь — это сам юнит под тестом (тогда ты не тестируешь ничего) и чистые внутренние хелперы вроде value object или функции оценки сложности пароля — мок таких лишь замораживает реализацию и тестирует твой мок вместо твоего кода. Мокай I/O-границу; дай логике выполниться.

Для сервиса, который общается и с repository, и с внешним клиентом, это два дублёра:

```ts
const module = await Test.createTestingModule({
  providers: [
    OrdersService,
    { provide: getRepositoryToken(Order), useValue: { findOne: jest.fn(), save: jest.fn() } },
    { provide: PaymentClient, useValue: { charge: jest.fn().mockResolvedValue({ ok: true }) } },
  ],
}).compile();
```

Теперь `OrdersService.checkout` можно прогнать по каждой ветке — отказ платежа, конфликт в repository, счастливый путь — за миллисекунды, детерминированно, без сети и без БД.

## overrideProvider, когда модуль реальный

`useValue` работает, когда провайдеры собираешь ты сам. Иногда удобнее импортировать **реальный** feature-модуль — чтобы его связка оставалась честной — и подменить только листовые зависимости. Это `overrideProvider(TOKEN)`:

```ts
const module = await Test.createTestingModule({
  imports: [UsersModule], // реальный модуль, реальная связка
})
  .overrideProvider(getRepositoryToken(User))
  .useValue({ findOne: jest.fn(), save: jest.fn() })
  .compile();
```

После `.useValue(...)` можно вместо этого зацепить `.useClass(FakeRepo)` (класс-заглушку, которую Nest инстанцирует со своими зависимостями) или `.useFactory({ factory: () => mock })` (когда дублёру нужна логика конструирования). А если не хочешь называть каждого дублёра, `.useMocker((token) => ...)` авто-заглушит *всё, что не подставлено явно*, — удобно для сервиса с длинным списком зависимостей, где тебе важны один-два коллаборатора, а остальные пусть будут инертными no-op-моками.

<FlowDiagram
  label="Как идёт unit-тест провайдера Nest: Test.createTestingModule объявляет провайдеры, ты либо подставляешь useValue-мок, либо делаешь overrideProvider на реальном модуле, .compile() собирает изолированный DI-контейнер, module.get резолвит сервис под тестом с замоканными зависимостями, ты вызываешь метод и проверяешь, что jest.fn()-шпионы зафиксировали ожидаемое поведение."
  hue="var(--d-backend)"
  perRow={3}
  nodes={[
    { id: "create", label: "createTestingModule", sub: "providers: [...]" },
    { id: "mock", label: "Подставь дублёров", sub: "useValue / override" },
    { id: "compile", label: "compile()", sub: "изолир. контейнер" },
    { id: "get", label: "module.get(Svc)", sub: "юнит под тестом" },
    { id: "call", label: "вызов метода", sub: "service.register()" },
    { id: "assert", label: "проверка", sub: "toHaveBeenCalledWith" },
  ]}
  edges={[
    { from: "create", to: "mock", label: "ключ по токену" },
    { from: "mock", to: "compile", label: "резолв графа" },
    { from: "compile", to: "get", label: "достать singleton" },
    { from: "get", to: "call", label: "прогнать ветку" },
    { from: "call", to: "assert", label: "проверить шпионов" },
  ]}
  caption="Testing module — одноразовый DI-контейнер: объяви провайдеры, инжекти моки на каждой границе, скомпилируй, достань сервис, вызови его и проверяй jest.fn()-шпионов. Без БД, без сети — быстро и детерминированно."
/>

## Два способа подставить зависимость, бок о бок

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Способ подставить dep</th>
        <th class="border border-border px-3 py-2 text-left">Как</th>
        <th class="border border-border px-3 py-2 text-left">Трогает БД / сеть?</th>
        <th class="border border-border px-3 py-2 text-left">Когда использовать</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2"><strong>Реальная реализация</strong></td>
        <td class="border border-border px-3 py-2">Перечисли реальный провайдер; Nest свяжет</td>
        <td class="border border-border px-3 py-2">Да — в этом и проблема</td>
        <td class="border border-border px-3 py-2">Integration/e2e-тест, не unit-тест</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2"><strong>useValue-мок</strong></td>
        <td class="border border-border px-3 py-2"><code>&#123; provide: TOKEN, useValue: &#123; m: jest.fn() &#125; &#125;</code></td>
        <td class="border border-border px-3 py-2">Нет</td>
        <td class="border border-border px-3 py-2">Массив providers собираешь сам</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2"><strong>overrideProvider</strong></td>
        <td class="border border-border px-3 py-2"><code>.overrideProvider(TOKEN).useValue/useClass/useFactory</code></td>
        <td class="border border-border px-3 py-2">Нет (для подменённого dep)</td>
        <td class="border border-border px-3 py-2">Импортируешь реальный модуль и меняешь один лист</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2"><strong>useMocker</strong></td>
        <td class="border border-border px-3 py-2"><code>.useMocker((token) =&gt; mock)</code></td>
        <td class="border border-border px-3 py-2">Нет</td>
        <td class="border border-border px-3 py-2">Авто-заглушка длинного списка; важны один-два</td>
      </tr>
    </tbody>
  </table>
</div>

<Inset kind="why" lang="ru">
Почему токен — это `getRepositoryToken(User)`, а не `Repository` или `User`? `@InjectRepository(User)` инжектит не «какой-то Repository» — этот класс делят много repository, по одному на сущность, — поэтому Nest нужен токен на сущность, чтобы их различать. `getRepositoryToken(User)` возвращает ровно такой синтетический токен (строку вроде `UserRepository`). Модуль `@nestjs/typeorm` регистрирует под ним реальный repository в рантайме; в тесте ты регистрируешь под тем же токеном свой мок. Подставь `useValue` по ключу `Repository` — и его никто не заинжектит: сервис всё равно получит реальный repo (или ошибку missing-dependency). Всегда ключуй дублёра по токену, в который резолвится декоратор.
</Inset>

<TradeoffMatrix
  id="nest-06-testing-01-unit-testing-providers-tradeoff-1"
  lessonSlug="01-unit-testing-providers"
  lang="ru"
  prompt="Нужно unit-тестировать логику UsersService.register — захэшировать пароль, отклонить дубль, сохранить один раз — без базы данных, быстро и детерминированно. Как подставить зависимость UserRepository?"
  options={[
    { name: "Подставить useValue-мок по ключу getRepositoryToken(User), с findOne/save как jest.fn(), в массиве providers у createTestingModule", summary: "Мокай на границе.", correct: true, justification: "Это учебниковый unit-тест. Ты объявляешь UsersService реальным провайдером и подставляешь repository дублёром по точному токену, в который резолвится @InjectRepository(User). jest.fn() позволяет запрограммировать findOne (вернуть дубль или null) и проверить, что save вызван один раз с нужной формой. Соединения с БД нет, поэтому тест выполняется за миллисекунды и никогда не флакает на таймауте пула, и он проверяет логику — единственное, что ты на самом деле хочешь тестировать." },
    { name: "Перечислить реальный TypeOrmModule и реальный UserRepository, чтобы тест был «реалистичным»", summary: "Это не unit-тест.", justification: "Связка реального repository открывает соединение с базой — ровно та медлительность и флаки, с которых начинается урок. Плюс это тестирует Postgres и TypeORM, а не твою логику register. Реальной связке место в integration- или e2e-тесте с выделенной тестовой базой — не в быстром детерминированном unit-сьюте." },
    { name: "Подставить useValue-мок по ключу класса Repository вместо getRepositoryToken(User)", summary: "Не тот токен.", justification: "@InjectRepository(User) резолвится в синтетический токен на сущность getRepositoryToken(User), а не в класс Repository — этот класс делят несколько сущностей. Ключ по Repository значит, что дублёра никто не заинжектит; сервис получит реальный repo или Nest бросит ошибку missing-dependency. Мок обязан совпадать с injection-токеном точь-в-точь." },
    { name: "Замокать приватный хелпер хэширования пароля сервиса, чтобы контролировать его вывод", summary: "Мок не того шва.", justification: "Хелпер хэширования — внутренняя логика, а не I/O-граница; мок такого замораживает реализацию и тестирует твой мок вместо кода. Мокай на границе (repository), дай чистой логике выполниться и проверяй итоговое поведение. Единственное, что не мокают никогда, — это юнит под тестом и его чистые внутренности." },
  ]}
/>

<Quiz
  id="nest-06-testing-01-unit-testing-providers-quiz-1"
  lessonSlug="01-unit-testing-providers"
  lang="ru"
  question="В unit-тесте UsersService под каким токеном должен быть подставлен мок repository?"
  choices={[
    { label: "getRepositoryToken(User) — синтетический токен на сущность, в который резолвится @InjectRepository(User)", correct: true },
    { label: "Класс Repository, ведь это тип, который инжектит сервис", misconception: "Repository — это тип, но injection-токен — это getRepositoryToken(User) на сущность: класс Repository делят много сущностей, поэтому Nest нужен отдельный токен на каждую. Ключ по Repository значит, что мок никто не заинжектит." },
    { label: "Класс сущности User, ведь это то, что оборачивает repository", misconception: "Класс сущности — это аргумент getRepositoryToken(User), а не сам токен. @InjectRepository(User) резолвится в синтетический токен, например 'UserRepository' — подставляй мок под getRepositoryToken(User)." },
    { label: "Строковый литерал 'UserRepository', написанный руками", misconception: "Это и правда то, что выдаёт getRepositoryToken(User), но хардкод строки хрупок и зависит от версии. Всегда вызывай getRepositoryToken(User), чтобы токен оставался корректным." },
  ]}
/>

<Quiz
  id="nest-06-testing-01-unit-testing-providers-quiz-2"
  lessonSlug="01-unit-testing-providers"
  lang="ru"
  question="Ты импортируешь реальный UsersModule в тест, но хочешь, чтобы repository был моком, а не ходил в Postgres. Какой подход подходит?"
  choices={[
    { label: "Зацепить .overrideProvider(getRepositoryToken(User)).useValue({ findOne: jest.fn(), save: jest.fn() }) перед .compile()", correct: true },
    { label: "Добавить второй { provide: getRepositoryToken(User), useValue: mock } в массив providers рядом с import", misconception: "Когда ты импортируешь реальный модуль, repository уже зарегистрирован внутри него; добавление дублирующего провайдера на уровне теста неоднозначно и не заменит надёжно привязку модуля. overrideProvider — это API, спроектированный, чтобы подменить привязку из импортированного модуля." },
    { label: "Удалить TypeOrmModule из UsersModule для тестовой сборки", misconception: "Нельзя (и не нужно) править продовую связку модуля ради теста. Testing module даёт тебе overrideProvider как раз для того, чтобы оставить реальный модуль нетронутым и подменить одну листовую зависимость дублёром." },
    { label: "Ничего — импорт реального модуля всегда использует реальные зависимости, так что просто наведи его на тестовую базу", misconception: "Наведение на тестовую базу делает это integration-тестом, а не unit-тестом — он медленнее и может флакать. overrideProvider позволяет сохранить связку реального модуля, заменив repository in-memory-моком." },
  ]}
/>

</Explanation>

<KeyTakeaway>Перечисли провайдер реальным в createTestingModule и замокай зависимости на границе — useValue по ключу getRepositoryToken(User) или overrideProvider на реальном модуле. Проверяй поведение через jest.fn(); без БД и сети.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="nest-06-testing-01-unit-testing-providers-retrieval"
  lang="ru"
  questions={[
    { q: "Пройди по unit-тесту UsersService против замоканного TypeORM-repository: что делает каждое из createTestingModule, массива providers, getRepositoryToken, compile и module.get?", a: "Test.createTestingModule({ providers: [...] }) собирает свежий, изолированный DI-контейнер, в котором есть только перечисленное. UsersService кладёшь туда реальным провайдером (это юнит под тестом), а его зависимость подставляешь кастомным провайдером { provide: getRepositoryToken(User), useValue: { findOne: jest.fn(), save: jest.fn() } } — getRepositoryToken(User) это синтетический токен на сущность, в который резолвится @InjectRepository(User), так что мок встаёт ровно туда, где был бы реальный repository. .compile() резолвит граф и возвращает TestingModule. module.get(UsersService) достаёт связанный сервис (singleton), а module.get(getRepositoryToken(User)) достаёт тот же мок, чтобы его программировать. Дальше прогоняешь метод: repo.findOne.mockResolvedValue(...) задаёт состояние, вызываешь service.register(...) и проверяешь поведение — expect(repo.save).toHaveBeenCalledWith(...) / not.toHaveBeenCalled() — не залезая в приватные поля. Соединение с базой не открывается, поэтому быстро и детерминированно." },
    { q: "Какие есть способы подставить зависимость в unit-тесте Nest и каково правило — что мокать, а что нет?", a: "Четыре способа: (1) перечислить реальный провайдер — только для integration/e2e, ведь он трогает БД/сеть; (2) useValue-мок { provide: TOKEN, useValue: { m: jest.fn() } }, когда массив providers собираешь сам; (3) .overrideProvider(TOKEN).useValue / .useClass / .useFactory, когда импортируешь реальный модуль и хочешь подменить лишь одну листовую зависимость, сохранив честную связку; (4) .useMocker((token) => mock) для авто-заглушки всего, что не подставлено явно, — удобно при длинном списке зависимостей. Правило: мокай на I/O-границе — repository, JwtService, HTTP/платёжный клиент — и только там. Никогда не мокай сам юнит под тестом (тогда не тестируешь ничего) и чистые внутренние хелперы вроде value object или функции хэширования (это лишь замораживает реализацию и тестирует мок). Мокай швы; дай логике выполниться." },
  ]}
/>

<Recap lang="ru">
Unit-тест провайдера Nest собирает одноразовый DI-контейнер через Test.createTestingModule(&#123; providers: [...] &#125;) и .compile(), а затем module.get(Service) резолвит сервис под тестом, уже связанный с дублёрами, которыми управляешь ты. Сервис перечисляй реальным провайдером, а каждую зависимость, которую он инжектит, подставляй моком — самый чистый вариант это кастомный провайдер &#123; provide: TOKEN, useValue: &#123; method: jest.fn() &#125; &#125;. Для TypeORM-repository токен — это getRepositoryToken(User), синтетический токен на сущность, в который резолвится @InjectRepository(User), — не класс Repository и не сущность. Тот же паттерн покрывает JwtService или HTTP/платёжный клиент: каждый это I/O-граница, которая получает useValue-дублёра. Если удобнее импортировать реальный модуль, чтобы его связка осталась честной, .overrideProvider(TOKEN) меняет один лист через .useValue / .useClass / .useFactory, а .useMocker авто-заглушит всё, что не подставлено явно. Когда каждая зависимость замокана, нет ни БД, ни сети, поэтому тест быстрый и детерминированный — и проверяешь поведение (toHaveBeenCalledWith, not.toHaveBeenCalled, mockResolvedValue для задания состояния), а не деталь реализации, так что тест переживает рефакторинг. Что не мокают никогда — это сам юнит под тестом и его чистые внутренние хелперы; мокай границу, дай логике выполниться. Теперь, когда встретишь флакающий девятиминутный «unit»-сьют, ты знаешь, что проверить первым: есть ли там реальная БД — и если да, потянуться за createTestingModule с замоканными зависимостями на границе.
</Recap>
