infographic compare-binary-horizontal-underline-text-vs
data
  title Сьют с реальной БД vs мок-юнит-тест
  desc Та же логика UsersService.register — testing module возвращает время и детерминизм
  compares
    - label Сьют с реальной БД
      children
        - label ~9 мин на прогон CI
        - label 1 из 5 прогонов падает
          desc таймаут пула соединений
        - label Поднимает AppModule, Postgres, mail API
        - label Тестит Postgres/TypeORM, не логику
    - label Мок-юнит-тест
      children
        - label Выполняется за миллисекунды
        - label 0 флаки-прогонов, детерминизм
        - label Без БД, без сети
        - label Прогоняет логику register()
theme
  palette #f43f5e #14b8a6
