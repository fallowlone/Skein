infographic hierarchy-tree-curved-line-rounded-rect-node
data
  root
    label Тест-дублёры: 5 видов
    children
      - label Проверка состояния — assert на результат
        children
          - label Стаб: возвращает заготовку
          - label Фейк: рабочая реализация, чтит контракт
      - label Проверка поведения — assert на вызов
        children
          - label Спай: записывает, что вызов случился
          - label Мок: ожидания запечены, проваливает тест
      - label Заглушка
        children
          - label Dummy: заполняет сигнатуру, не используется
theme
  palette #6366f1 #14b8a6 #f59e0b
