---
concepts:
- graphql
- code-first
- resolvers
- dataloader
- query-complexity
deepensInto: []
estMin: 18
lang: ru
lessonType: topic
level: senior
order: 2
prereqs: []
slug: "02-graphql-module"
sources:
- https://docs.nestjs.com/graphql/quick-start
- https://docs.nestjs.com/graphql/resolvers
- https://docs.nestjs.com/graphql/complexity
- https://docs.nestjs.com/graphql/guards-interceptors
spiral: []
status: ready
summary: 'Code-first GraphQL генерирует SDL из классов @ObjectType/@Resolver. Field resolver порождает N+1; DataLoader на запрос батчит ключи в один вызов БД. Прод закаляем лимитами глубины/сложности.'
title: 'Code-first GraphQL, резолверы и DataLoader'
track: nest
unit: "08-microservices-and-graphql"
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import SequenceDiagram from "~/components/diagram/SequenceDiagram.astro";

<Hook>
GraphQL-эндпоинт уехал чистым, и демо-запрос был мгновенным: один автор с его постами. Потом команда дашборда написала `authors { id posts { title } }` по всему списку — и Postgres вспыхнул: 1 запрос на 200 авторов, а потом ещё 200, по одному на автора, чтобы достать посты каждого. p99 прыгнул с 40ms до 6 секунд, и пул соединений начал копить очередь. В резолвере не было ничего «неправильного»; field resolver для `posts` просто выполнился по разу на каждого родителя, а 200 родителей — это 200 round-trip'ов. Это и есть GraphQL N+1, и это самый частый способ, которым типизированный graph API расплавляет базу. Фикс — это не join, а батчинг ключей, собранных за один тик, в единственный вызов.
</Hook>

<Crux>Code-first строит схему из TS-классов; field resolver выполняется на родителя (N+1), а DataLoader на запрос батчит это в один вызов.</Crux>

<Explanation>

## Code-first: схема генерируется из твоих классов

В подходе code-first ты никогда не пишешь SDL руками. Ты навешиваешь на TypeScript-классы `@ObjectType()` и `@Field()`, на методы резолвера — `@Query()` / `@Mutation()` / `@ResolveField()`, и Nest **генерирует схему** из этой информации о типах при старте. `autoSchemaFile` пишет SDL на диск (или держит в памяти), чтобы тулинг мог её читать, — но источник истины это твой код. Альтернатива schema-first обратна: ты пишешь SDL руками и генерируешь из него TypeScript-типы. Code-first выигрывает, когда команда живёт в TypeScript и хочет, чтобы типы и схема никогда не расходились.

```ts
import { ApolloDriver, ApolloDriverConfig } from '@nestjs/apollo';
import { GraphQLModule } from '@nestjs/graphql';
import { Module } from '@nestjs/common';

@Module({
  imports: [
    GraphQLModule.forRoot<ApolloDriverConfig>({
      driver: ApolloDriver,
      autoSchemaFile: 'schema.gql',        // ГЕНЕРИРУЕМ SDL из TS-классов
      playground: false,                    // выключен в проде
      introspection: process.env.NODE_ENV !== 'production', // прячем схему в проде
    }),
  ],
})
export class AppModule {}
```

Объектные типы — обычные классы; `@Field(() => Int)` фиксирует GraphQL-скаляр там, где рефлексия TS не может его вывести (числа, массивы, nullability):

```ts
import { Field, Int, ObjectType } from '@nestjs/graphql';

@ObjectType()
export class Post {
  @Field(() => Int) id: number;
  @Field() title: string;
}

@ObjectType()
export class Author {
  @Field(() => Int) id: number;
  @Field({ nullable: true }) firstName?: string;
  @Field(() => [Post]) posts: Post[];   // связь -> резолвится лениво field resolver'ом
}
```

## Резолверы и field resolver — где рождается N+1

Класс `@Resolver(() => Author)` хостит корневые операции (`@Query`, `@Mutation`) и, что критично, **field resolver'ы** (`@ResolveField`), которые вычисляют поле лениво, *на каждый родительский объект*. Поле `posts` у `Author` не загружается вместе с автором — оно резолвится, только если запрос его попросил, вызовом field resolver по разу на каждого автора в выборке. Это исполнение на родителя и есть N+1: один запрос на N авторов, потом N отдельных запросов на их посты.

```ts
import { Resolver, Query, ResolveField, Parent, Args, Int } from '@nestjs/graphql';

@Resolver(() => Author)
export class AuthorsResolver {
  constructor(
    private readonly authors: AuthorsService,
    private readonly posts: PostsService,
  ) {}

  @Query(() => [Author])
  authors() {
    return this.authors.findAll();          // 1 запрос -> N авторов
  }

  @ResolveField(() => [Post])
  async postsFor(@Parent() author: Author) {
    return this.posts.findByAuthorId(author.id); // выполняется ПО РАЗУ НА АВТОРА -> N+1
  }
}
```

## DataLoader: батчим ключи, собранные за один тик

[DataLoader](https://github.com/graphql/dataloader) сидит между field resolver'ом и базой. Вместо немедленного запроса каждый вызов `load(id)` ставит ключ в очередь; в конце текущего тика event-loop'а DataLoader отдаёт **весь батч ключей** в один `findByAuthorIds([...])` и раздаёт результат обратно каждому вызывающему. Он также мемоизирует внутри батча, так что два вызова `load(5)` бьют по БД один раз. Коллапс драматичен: 1 + N запросов превращаются в **1 + 1**.

Loader обязан быть **на запрос** — его кэш держит данные запроса и не должен утекать между пользователями, — поэтому подключаем его как request-scoped provider и резолвим в GraphQL-context:

```ts
import * as DataLoader from 'dataloader';
import { Injectable, Scope } from '@nestjs/common';

@Injectable({ scope: Scope.REQUEST })   // свежий loader на каждый запрос -> нет кросс-запросного кэша
export class PostsLoader {
  constructor(private readonly posts: PostsService) {}

  readonly byAuthorId = new DataLoader<number, Post[]>(async (authorIds) => {
    // ОДИН round-trip на все ключи, собранные в этом тике:
    const rows = await this.posts.findByAuthorIds(authorIds as number[]);
    const byId = new Map<number, Post[]>(authorIds.map((id) => [id, []]));
    for (const p of rows) byId.get(p.authorId)!.push(p);
    return authorIds.map((id) => byId.get(id)!); // ОБЯЗАН вернуть результаты в порядке ключей
  });
}
```

```ts
@ResolveField(() => [Post])
postsFor(@Parent() author: Author) {
  return this.loader.byAuthorId.load(author.id); // ставит ключ в очередь; батчится, а не на вызов
}
```

Контракт batch-функции строгий: она обязана вернуть массив **той же длины и в том же порядке**, что и входные ключи, отображая отсутствующий ключ в `[]` (или `null`), никогда молча не выбрасывая его, — перепутаешь порядок, и авторы получат чужие посты.

<SequenceDiagram
  label="GraphQL-запрос на список авторов с их постами: корневой query возвращает N авторов одним обращением к базе, потом field resolver для posts выполняется по разу на автора, и каждый вызов ставит id этого автора в очередь DataLoader на запрос вместо немедленного запроса; в конце тика event-loop'а DataLoader батчит все собранные id в единственный вызов findByAuthorIds, так что весь запрос стоит один плюс один round-trip к базе вместо один плюс N."
  hue="var(--d-systems)"
  actors={["Client", "Resolver", "DataLoader", "Database"]}
  messages={[
    { from: "Client", to: "Resolver", label: "authors { posts }" },
    { from: "Resolver", to: "Database", label: "1. findAll() -> N authors" },
    { from: "Resolver", to: "DataLoader", label: "2. load(id) x N (per parent)" },
    { from: "DataLoader", to: "DataLoader", label: "3. collect keys in one tick" },
    { from: "DataLoader", to: "Database", label: "4. findByAuthorIds([...]) x1" },
    { from: "Database", to: "DataLoader", label: "5. rows -> map by key order" },
    { from: "Resolver", to: "Client", label: "6. response (1 + 1 queries)" },
  ]}
  caption="Без DataLoader field resolver для posts выполняется N раз -> 1 + N запросов. С ним N вызовов load() в одном тике коллапсируют в один батч-запрос -> 1 + 1. Батч обязан вернуть результаты в порядке ключей."
/>

## Context, guards и закалка для прода

Guards, interceptors и filters по-прежнему работают в GraphQL — но форма аргументов резолвера другая (root, args, context, info), так что общий `ExecutionContext` ты адаптируешь через `GqlExecutionContext.create(context)`, чтобы прочитать args или request:

```ts
import { GqlExecutionContext } from '@nestjs/graphql';
import { CanActivate, ExecutionContext, Injectable } from '@nestjs/common';

@Injectable()
export class GqlAuthGuard implements CanActivate {
  canActivate(context: ExecutionContext): boolean {
    const ctx = GqlExecutionContext.create(context);
    const { req } = ctx.getContext();   // request лежит на GraphQL-context
    return Boolean(req?.user);
  }
}
```

Единственный эндпоинт, принимающий произвольные запросы, — это поверхность для denial-of-service: глубоко вложенный `posts { author { posts { author ... } } }` может взорваться в миллионы field-резолвов. Продакшен-графы ограничивают это лимитом **глубины запроса (query depth)** и бюджетом **сложности (complexity)** — назначь каждому полю стоимость и отклоняй любую операцию выше потолка ещё до выполнения:

```ts
import { Plugin } from '@nestjs/apollo';
import { GraphQLSchemaHost } from '@nestjs/graphql';
import { GraphQLError } from 'graphql';
import { fieldExtensionsEstimator, getComplexity, simpleEstimator } from 'graphql-query-complexity';

@Plugin()
export class ComplexityPlugin {
  constructor(private readonly schemaHost: GraphQLSchemaHost) {}
  async requestDidStart() {
    const max = 20, { schema } = this.schemaHost;
    return {
      didResolveOperation: async ({ request, document }) => {
        const complexity = getComplexity({
          schema, query: document, variables: request.variables,
          operationName: request.operationName,
          estimators: [fieldExtensionsEstimator(), simpleEstimator({ defaultComplexity: 1 })],
        });
        if (complexity > max) throw new GraphQLError(`Query too complex: ${complexity} > ${max}`);
      },
    };
  }
}
```

Добавь **persisted queries** (клиенты шлют хеш заранее зарегистрированного запроса, а не произвольный текст) и **выключи introspection в проде**, чтобы атакующий не мог тривиально замапить весь граф.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Задача</th>
        <th class="border border-border px-3 py-2 text-left">Инструмент</th>
        <th class="border border-border px-3 py-2 text-left">Механизм</th>
        <th class="border border-border px-3 py-2 text-left">Какой сбой предотвращает</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2"><strong>Схема</strong></td>
        <td class="border border-border px-3 py-2">code-first <code>autoSchemaFile</code></td>
        <td class="border border-border px-3 py-2">SDL генерится из <code>@ObjectType</code>-классов</td>
        <td class="border border-border px-3 py-2">расхождение типов и схемы</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2"><strong>Связь</strong></td>
        <td class="border border-border px-3 py-2"><code>@ResolveField</code></td>
        <td class="border border-border px-3 py-2">резолвит поле лениво, на родителя</td>
        <td class="border border-border px-3 py-2">over-fetch незапрошенных связей</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2"><strong>N+1</strong></td>
        <td class="border border-border px-3 py-2">DataLoader на запрос</td>
        <td class="border border-border px-3 py-2">батч ключей за тик -> 1 вызов</td>
        <td class="border border-border px-3 py-2">1 + N round-trip'ов, истощение пула</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2"><strong>Сквозное</strong></td>
        <td class="border border-border px-3 py-2"><code>GqlExecutionContext</code></td>
        <td class="border border-border px-3 py-2">адаптирует ctx для guards/interceptors</td>
        <td class="border border-border px-3 py-2">guard не может читать args/req</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2"><strong>DoS</strong></td>
        <td class="border border-border px-3 py-2">лимиты глубины + сложности</td>
        <td class="border border-border px-3 py-2">отклонить дорогой запрос до выполнения</td>
        <td class="border border-border px-3 py-2">вложенный запрос плавит БД</td>
      </tr>
    </tbody>
  </table>
</div>

<Inset kind="why" lang="ru">
Почему DataLoader живёт на запрос, а не синглтоном? Его внутрибатчевый кэш и есть то, что делает батчинг рабочим, но этот кэш держит только что добытые строки — посты автора 5, профиль пользователя 12. Loader-синглтон отдал бы запросу B строки, закэшированные для запроса A, утекая данные одного пользователя в ответ другому и никогда не отражая запись, случившуюся между запросами. Свежий loader на запрос сохраняет выигрыш батчинга, но скоупит кэш одной аутентифицированной единицей работы. Поэтому ты регистрируешь его как `Scope.REQUEST` (или строишь в фабрике GraphQL-context) — один loader, один запрос, выбрасывается в конце.
</Inset>

<TradeoffMatrix
  id="nest-08-microservices-and-graphql-02-graphql-module-tradeoff-1"
  lessonSlug="02-graphql-module"
  lang="ru"
  prompt="Запрос возвращает N авторов, каждый резолвит поле `posts`. Наивный резолвер выполняет один запрос к БД на автора (1 + N). Как резолвить связь в graph API?"
  options={[
    { name: "DataLoader на запрос: field resolver вызывает load(id), ключи собранные за один тик батчатся в единственный findByAuthorIds([...]) и кэшируются в пределах запроса", summary: "Батчит N+1.", correct: true, justification: "Это канонический фикс GraphQL N+1. Field resolver всё ещё выполняется на родителя, но вместо запроса ставит ключ в очередь; DataLoader батчит каждый ключ, собранный за тик, в один round-trip и мемоизирует в пределах запроса, превращая 1 + N в 1 + 1. Он на запрос, так что кэш никогда не утекает между пользователями и отражает записи между запросами. Он также композируется с любыми формами запроса — один join не может, потому что клиент решает в рантайме, какие связи раскрывать." },
    { name: "Оставить наивный field resolver, выполняющий один запрос на родителя — это самый простой код, а GraphQL и задуман резолвить поля лениво", summary: "Сам N+1.", justification: "Ленивая резолюция поля верна в принципе, но отдельный вызов БД на каждого родителя — это ровно тот N+1, который плавит пул на масштабе списка: 200 авторов -> 201 запрос, p99 в секундах. Дело не в корректности, а в throughput. Ленивый резолвер правилен — ему просто нужен DataLoader под ним, чтобы батчить." },
    { name: "Всегда выдавать один большой SQL JOIN, тянущий авторов и все их посты вместе, игнорируя, какие поля выбрал запрос", summary: "Слепой over-fetch.", justification: "Join может быть валидной оптимизацией на уровне резолвера, но навязывать один join каждому запросу authors — это over-fetch, когда клиент не просил посты, и это не обобщается — GraphQL даёт клиенту выбирать связи в рантайме, так что понадобился бы свой join на каждую комбинацию полей. DataLoader адаптируется к тому, что выбрал запрос; захардкоженный join — нет." },
    { name: "Кэшировать посты глобально в синглтоне, чтобы повторяющиеся авторы делили результаты между всеми запросами", summary: "Утечка между пользователями.", justification: "Глобальный на процесс кэш ничего не батчит в пределах одного запроса и, хуже, отдаёт одному запросу строки, закэшированные для другого, — утекая данные между пользователями и устаревая на записях. Единица батчинга — запрос, а не процесс. Именно поэтому DataLoader request-scoped." },
  ]}
/>

<Quiz
  id="nest-08-microservices-and-graphql-02-graphql-module-quiz-1"
  lessonSlug="02-graphql-module"
  lang="ru"
  question="Запрос просит 50 авторов и посты каждого через @ResolveField. С корректно подключённым DataLoader на запрос — сколько запросов к БД выполнится?"
  choices={[
    { label: "2 — один на 50 авторов, потом один батч findByAuthorIds([...]) на все их посты", correct: true },
    { label: "51 — один на авторов и по одному на каждого автора на его посты", misconception: "Это счёт БЕЗ DataLoader (1 + N). Весь смысл loader'а в том, что 50 вызовов load() в одном тике коллапсируют в один батч-запрос, давая 1 + 1 = 2, а не 1 + N = 51." },
    { label: "1 — DataLoader переписывает резолверы в единственный SQL JOIN", misconception: "DataLoader не генерирует SQL или join'ы; он батчит ключи в один вызов того, что делает твой сервис. Корневой запрос authors и батч-запрос posts всё ещё два отдельных round-trip'а." },
    { label: "50 — DataLoader убирает начальный запрос authors, но грузит посты на каждого автора", misconception: "Корневой запрос на список авторов всегда выполняется один раз, а вызовы load() на родителя батчатся в ещё один — так что 2. DataLoader батчит field-резолвы; он не убирает корневой запрос." },
  ]}
/>

<Quiz
  id="nest-08-microservices-and-graphql-02-graphql-module-quiz-2"
  lessonSlug="02-graphql-module"
  lang="ru"
  question="В code-first NestJS GraphQL откуда берётся схема (SDL) и как guards читают GraphQL-аргументы?"
  choices={[
    { label: "SDL генерится из @ObjectType/@Resolver-классов через autoSchemaFile; guards вызывают GqlExecutionContext.create(context), чтобы добраться до args и request", correct: true },
    { label: "Ты пишешь SDL-файл руками, и Nest генерирует TS-классы из него; guards читают context.switchToHttp() как обычно", misconception: "Это описывает подход schema-FIRST (SDL руками -> типы генерятся). Code-first обратен: классы генерируют SDL. И в GraphQL args не на HTTP-context — нужно адаптировать его через GqlExecutionContext.create()." },
    { label: "Схема выводится в момент запроса из каждого query; guards работают без изменений, потому что GraphQL переиспользует HTTP execution context напрямую", misconception: "Схема строится один раз при старте из твоих декорированных классов, а не на запрос. И форма аргументов резолвера GraphQL (root/args/context/info) отличается от HTTP, именно поэтому существует GqlExecutionContext.create()." },
    { label: "autoSchemaFile скачивает схему из реестра Apollo; guards в GraphQL-резолверах не поддерживаются", misconception: "autoSchemaFile пишет локально сгенерированный SDL на диск — ничего не скачивается. Guards, interceptors и filters все работают в GraphQL; ты просто адаптируешь context через GqlExecutionContext.create()." },
  ]}
/>

</Explanation>

<KeyTakeaway>Code-first генерирует SDL из классов @ObjectType/@Resolver. @ResolveField выполняется на родителя — это N+1. Фикс — DataLoader на запрос, батчащий ключи в один вызов. Прод закаляем лимитами глубины/сложности.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="nest-02-graphql-module-retrieval"
  lang="ru"
  questions={[
    { q: "Объясни проблему GraphQL N+1 в резолвере Nest и точно как DataLoader её чинит, включая почему loader обязан быть request-scoped.", a: "N+1 возникает потому, что @ResolveField выполняется по разу на родительский объект: запрос, возвращающий N авторов, каждый из которых резолвит своё поле posts, выполняет 1 запрос на авторов, а потом ещё N — по одному findByAuthorId на автора, — итого 1 + N round-trip'ов, истощающих пул соединений на масштабе списка. DataLoader сидит между field resolver'ом и источником данных: вместо запроса резолвер вызывает load(id), который ставит ключ в очередь; в конце текущего тика event-loop'а DataLoader отдаёт весь батч собранных ключей в единственный findByAuthorIds([...]) и раздаёт результат каждому вызывающему, мемоизируя дубли ключей в пределах батча. Это превращает 1 + N в 1 + 1. Batch-функция обязана вернуть массив той же длины и порядка, что и входные ключи (отсутствующий ключ -> [] или null), иначе авторы получат чужие посты. Loader обязан быть request-scoped — Scope.REQUEST или построенный в фабрике GraphQL-context, — потому что его внутрибатчевый кэш держит только что добытые строки; синглтон отдал бы одному запросу данные, закэшированные для другого, утекая данные одного пользователя в ответ другому и устаревая на записях. Один loader, один запрос, выбрасывается в конце." },
    { q: "Сопоставь code-first и schema-first GraphQL в Nest и назови закалку для прода, которую добавляешь к публичному graph-эндпоинту.", a: "Code-first: ты навешиваешь на TypeScript-классы @ObjectType/@Field, а на методы резолвера @Query/@Mutation/@ResolveField, и Nest генерирует SDL из этой информации о типах при старте (autoSchemaFile пишет его на диск или держит в памяти). Код — источник истины, так что типы и схема не расходятся. Schema-first обратен: ты пишешь SDL руками и генерируешь из него TypeScript-типы. Code-first подходит TypeScript-тяжёлым командам; schema-first — контракту, ведомому схемой и независимому от языка. Для закалки прода на единственном эндпоинте с произвольными запросами: ограничь ГЛУБИНУ запроса (отклоняй глубоко вложенные рекурсивные запросы) и навяжи бюджет СЛОЖНОСТИ — назначь каждому полю стоимость через complexity-плагин (getComplexity с fieldExtensionsEstimator + simpleEstimator) и отклоняй любую операцию выше потолка в didResolveOperation, до её выполнения, — чтобы вредоносный вложенный запрос не мог устроить базе DoS. Добавь persisted queries (клиенты шлют хеш заранее зарегистрированного запроса, а не произвольный текст) и выключи introspection (и playground) в проде, чтобы схема не была тривиально мапаема. Guards/interceptors/filters по-прежнему применяются, адаптированные через GqlExecutionContext.create(context)." },
  ]}
/>

<Recap lang="ru">
Code-first GraphQL в Nest генерирует схему из твоего TypeScript: классы @ObjectType/@Field и методы @Resolver (@Query, @Mutation, @ResolveField) становятся SDL через autoSchemaFile, так что типы и схема не расходятся — обратное к schema-first, где ты пишешь SDL руками и генерируешь типы. Сеньорская ловушка — это field resolver: @ResolveField резолвит связь лениво, по разу на родителя, так что запрос по N авторам, каждый из которых резолвит свои posts, выполняет 1 + N запросов к базе — GraphQL N+1, истощающий пул соединений на масштабе списка. Фикс — это DataLoader: field resolver вызывает load(id), ключи собранные за один тик event-loop'а батчатся в единственный вызов findByAuthorIds([...]) и мемоизируются в пределах запроса, схлопывая 1 + N в 1 + 1; batch-функция обязана вернуть результаты в том же порядке, что и ключи. Loader регистрируется как Scope.REQUEST (или строится в GraphQL-context), так что его кэш скоупится одним запросом и никогда не утекает между пользователями. Guards, interceptors и filters по-прежнему работают — context ты адаптируешь через GqlExecutionContext.create(context), чтобы добраться до args и request. Наконец, поскольку один эндпоинт принимает произвольные запросы, прод закаляем лимитами глубины и сложности, отклоняющими дорогую операцию до её выполнения, persisted queries и выключенным introspection.
</Recap>
