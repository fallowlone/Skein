---
concepts:
- query-depth-limit
- query-complexity
- persisted-queries
- apollo-federation
- subgraph-key
deepensInto: []
estMin: 19
lang: ru
lessonType: topic
level: senior
order: 5
prereqs: []
slug: "05-graphql-hardening-and-federation"
sources:
- https://docs.nestjs.com/graphql/complexity
- https://www.apollographql.com/docs/federation/
spiral: []
status: ready
summary: 'Публичный GraphQL-эндпоинт даёт клиенту составлять запрос, так что один запрос разворачивается в миллионы вызовов резолверов и БД — DoS-вектор, которого у REST нет. Защищайся лимитом глубины, анализом стоимости и persisted-запросами; масштабируй через Apollo Federation.'
title: 'Защита GraphQL: стоимость запроса, глубина и федерация'
track: nest
unit: 08-microservices-and-graphql
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
GraphQL-эндпоинт был публичным восемь месяцев. Для графа на чтение авторизация не требовалась — он питал партнёрскую интеграцию, и команда гордилась тем, какой он гибкий. В 02:14 зазвонил телефон дежурного: p99 API ушёл вертикально вверх, соединения Postgres упёрлись в максимум, а машина свопила. Всплеска числа запросов не было. Был *один* запрос. Кто-то — скрапер, любопытный бот, это так и не выяснили — отправил единственный запрос: `user { friends { friends { friends { posts { author { friends } } } } } }`. Схема радостно его резолвила, и каждый уровень умножал fan-out: сотня друзей, у каждого по сотне друзей, у каждого посты, каждый пост заново резолвит автора. Один HTTP-запрос превратился в несколько миллионов вызовов резолверов и сопоставимую гору чтений из базы. Сервер его не отклонил — отклонять было нечем, ничего не настроено. Этот урок — про контроли, которые там должны были быть: лимит глубины, анализ стоимости запроса, persisted-запросы, а затем федерацию, когда узким местом становится сам единый граф.
</Hook>

<Crux>Запрос составляет клиент, поэтому развернётся без границ. Отклоняй по глубине и стоимости до выполнения; persisted-запросы; федерируй.</Crux>

<Explanation>

## Почему GraphQL нужна защита, которой не нужно REST

В REST форму каждого ответа владеет сервер. `GET /users/42/friends` возвращает ограниченную страницу; стоимость этого эндпоинта фиксирована и известна в момент написания. Клиент не может попросить `/users/42/friends/friends/friends` — такого маршрута нет, и чтобы получить вложенные данные, клиент обязан сделать больше отдельно лимитируемых запросов.

GraphQL переворачивает это. **Запрос составляет клиент** — он выбирает поля, глубину и ширину в одном запросе. Эта выразительность — главная фича, и она же — поверхность атаки: клиент может попросить произвольно глубокие, произвольно широкие, рекурсивные данные одним HTTP-вызовом, и сервер послушно их зарезолвит. Самоссылочный тип вроде `User.friends: [User]` позволяет запросу вкладываться сам в себя, пока ответ не станет комбинаторным взрывом. Один запрос, без авторизации, превращается в миллионы вызовов резолверов и обращений к БД. Это DoS-вектор, уникальный для выставленного наружу GraphQL-эндпоинта, и потому публичному GraphQL API нужны защиты выполнения, которые REST API получает бесплатно от своих фиксированных маршрутов.

Защита многослойная, и слои работают **до выполнения** — ты отклоняешь вредоносный запрос на этапе валидации, а не переживаешь его.

## Контроль 1 — лимит глубины

Первая защита ограничивает, насколько глубоко может вкладываться запрос. Правило валидации обходит AST запроса и отклоняет любую операцию, чьё множество выборки вложено глубже максимума — обычно **7–10** уровней для реального приложения. Именно это останавливает бомбу рекурсивных друзей: `friends { friends { friends { ... } } }` превышает лимит глубины и отклоняется на валидации, до того как запустится хоть один резолвер.

```ts
// main.ts — wire a depth-limit validation rule into Apollo's validationRules
import depthLimit from 'graphql-depth-limit';

GraphQLModule.forRoot<ApolloDriverConfig>({
  driver: ApolloDriver,
  // any operation nested deeper than 7 selection sets is rejected at validation
  validationRules: [depthLimit(7)],
});
```

Лимит глубины дёшев, статичен и ловит самую очевидную рекурсию. Но у него есть слепое пятно, и это слепое пятно — следующий контроль.

## Контроль 2 — сложность / анализ стоимости запроса

Запрос может быть неглубоким и всё равно разрушительным. `users(first: 10000) { posts(first: 1000) { comments(first: 1000) } }` — это всего **три уровня в глубину**, он проскальзывает мимо любого лимита глубины, — но умножается примерно до десяти миллиардов узлов. Лимит глубины считает вложенность; он слеп к **ширине** и к **множителям пагинации**.

Анализ стоимости это чинит. Ты назначаешь числовую **сложность** каждому полю — скалярные поля дёшевы, поля-списки дороги, а стоимость поля-списка умножается на его аргумент пагинации, — затем суммируешь стоимость всего запроса *до выполнения* и отклоняешь всё, что превышает бюджет (обычно **1000**). NestJS поддерживает это через плагин сложности, считающий оценку по per-field эстиматорам или значению `complexity` на поле.

```ts
// Annotate the expensive field, and multiply by the pagination arg
@Field(() => [Post], { complexity: 5 })
posts: Post[];

// A plugin sums the query's complexity at validation time and rejects over budget
import { ComplexityPlugin } from './complexity.plugin'; // computes score, throws over maxComplexity (e.g. 1000)
```

Эстиматор для пагинированного списка умножает базовую стоимость поля на `first`/`limit`, так что `users(first: 10000)` набирает 10000× стоимости на пользователя и сам по себе пробивает бюджет. Это контроль, который ловит **широкую-но-неглубокую** бомбу, которую лимит глубины пропускает. Нужны оба: глубина против рекурсии, стоимость против fan-out.

<Inset kind="why" lang="ru">
Почему лимита глубины в одиночку недостаточно и что добавляет анализ сложности? Неглубокий запрос всё равно может быть разрушительно дорогим. `users(first: 10000) { posts(first: 1000) { comments(first: 1000) } }` — это всего три уровня в глубину, так что лимит глубины 7 пропускает его насквозь, — однако аргументы пагинации умножаются примерно до десяти миллиардов узлов и плавят базу. Лимит глубины считает, насколько *глубоко* ты вкладываешься; он совершенно слеп к тому, насколько *широк* каждый уровень, и к эффекту множителя `first`/`limit`. Анализ сложности назначает оценку, которая умножает размеры списков на их аргументы пагинации, так что тот же широкий-но-неглубокий запрос набирает далеко за бюджет и отклоняется на валидации. Два контроля покрывают ортогональные оси — глубина останавливает рекурсию, стоимость останавливает fan-out, — и защищённый эндпоинт запускает оба, плюс жёсткие ограничения на сами аргументы пагинации, таймауты запроса и rate limiting.
</Inset>

## Контроль 3 — persisted-запросы (сильнейший рычаг)

Предыдущие два контроля ограничивают *произвольный* запрос. Persisted-запросы меняют игру, убирая произвольность. Вместо приёма любой строки запроса клиент шлёт **хеш** запроса, зарегистрированного заранее; сервер ищет хеш и выполняет только ту операцию, которую уже знает. Automatic Persisted Queries (APQ) делают это в первую очередь чтобы уменьшить полезную нагрузку запроса — клиент шлёт короткий хеш вместо длинной строки запроса.

Выигрыш в безопасности приходит от запуска persisted-запросов в режиме **allow-list**: сервер выполняет *только* операции из проверенного, заранее зарегистрированного набора и отклоняет любой хеш, которого не видел. В этом режиме DoS произвольным запросом устраняется напрочь — атакующий не может прислать бомбу рекурсивных друзей, потому что сервер не запустит запрос, не выданный ему на этапе сборки. Цена — гибкость: публичный или сторонний API, чьи клиенты законно составляют новые запросы, не может занести их в allow-list. Для **first-party**-приложения — твоих собственных веб- и мобильных клиентов, поставляющих фиксированный известный набор операций, — allow-list persisted-запросы — единственный сильнейший рычаг защиты, и они складываются с лимитами глубины и стоимости как defense in depth.

## Рантайм-родственник: N+1 и DataLoader

Анализ стоимости ограничивает то, что клиент может **попросить**; он ничего не говорит о том, что твои резолверы на самом деле **делают**, отвечая. Fan-out резолверов заново вносит проблему N+1 — резолвинг списка из N родителей с одним дочерним запросом на родителя, — которую (как разбиралось ранее в этом юните) DataLoader решает, батчируя N чтений в одно и кешируя на запрос. Думай о них как о паре: лимит стоимости — статический бюджет на форму запроса, DataLoader — рантайм-бюджет на работу резолверов. Защищённому API нужны оба, потому что запрос, прошедший ворота стоимости, всё равно может выдать N+1 запросов, если его резолверы наивны.

## Федерация — когда единый граф становится узким местом

Контроли выше защищают один граф. Федерация отвечает на другую задачу: по мере роста графа единая монолитная схема и набор резолверов становятся узким местом деплоя и беспорядком владения — каждая команда правит один файл, каждое изменение — полный передеплой. **Apollo Federation** (в NestJS — `ApolloFederationDriver`) разбивает граф между сервисами. Каждый **сабграф** владеет своими типами, помечает сущности, которыми владеет, через `@key(fields: "id")` и предоставляет резолвер `__resolveReference`, чтобы другие сабграфы могли расширять его сущности по ссылке. **Шлюз** (gateway) композирует сабграфы в один **суперграф** и планирует кросс-сервисные запросы, доставая поля каждой сущности из сабграфа-владельца.

```graphql
# users subgraph — owns the User entity, identified by id
type User @key(fields: "id") {
  id: ID!
  name: String!
}
```

```graphql
# reviews subgraph — extends User by reference, adds the fields it owns
type User @key(fields: "id") {
  id: ID! @external
  reviews: [Review!]!   # resolved here via __resolveReference(id)
}
```

Это современный декларативный преемник более старого **schema stitching**, где шлюз вручную сливал схемы, а кросс-сервисную обвязку ты писал руками. Федерация делает связи декларативными — `@key` и `__resolveReference` — и позволяет шлюзу планировать запрос. Критически: защита не переезжает на шлюз и не останавливается там — лимиты глубины, анализ стоимости и persisted-запросы по-прежнему живут на краю, потому что суперграф ровно так же композируем — а значит ровно так же уязвим, — как единый граф, который он заменил.

<FlowDiagram
  label="Две половины защищённого, федеративного GraphQL-сетапа. Конвейер защиты: входящий клиентский запрос сначала валидируется — правило лимита глубины отклоняет запросы, вложенные глубже максимума, затем оценка сложности суммирует стоимости полей против бюджета; если любые ворота не пройдены, запрос отклоняется до запуска резолверов, и только прошедший запрос идёт к выполнению, где работают резолверы и DataLoader батчит чтения связей. Топология федерации: шлюз композирует сабграфы в один суперграф — сабграф A владеет сущностью User, помеченной @key, а сабграф B расширяет эту сущность и резолвит её по ссылке через __resolveReference. Ворота валидации стоят перед выполнением, и те же ворота охраняют федеративный суперграф."
  hue="var(--d-systems)"
  perRow={3}
  nodes={[
    { id: "query", label: "client query", sub: "client-composed" },
    { id: "depth", label: "depth limit", sub: "reject > max" },
    { id: "cost", label: "cost vs budget", sub: "reject > 1000" },
    { id: "exec", label: "execute", sub: "resolvers + DataLoader" },
    { id: "gateway", label: "gateway", sub: "supergraph" },
    { id: "subA", label: "subgraph A", sub: "@key(id)" },
    { id: "subB", label: "subgraph B", sub: "__resolveReference" },
  ]}
  edges={[
    { from: "query", to: "depth", label: "validate" },
    { from: "depth", to: "cost", label: "pass" },
    { from: "cost", to: "exec", label: "under budget" },
    { from: "gateway", to: "subA", label: "owns entity" },
    { from: "gateway", to: "subB", label: "extends by ref" },
  ]}
  caption="Ворота валидации (сначала глубина, затем стоимость) стоят перед выполнением: запрос отклоняется на валидации, а не переживается. Те же ворота охраняют федеративный суперграф, который шлюз композирует из сабграфов, владеющих сущностями через @key и резолвящих ссылки."
/>

<TradeoffMatrix
  id="nest-08-microservices-and-graphql-05-graphql-hardening-and-federation-tradeoff-1"
  lessonSlug="05-graphql-hardening-and-federation"
  lang="ru"
  prompt="Тебе нужно защитить публичный GraphQL API от DoS по стоимости запроса, где один составленный клиентом запрос может развернуться в миллионы вызовов резолверов и БД. Какая наилучшая многослойная защита?"
  options={[
    { name: "Сложи лимит глубины + бюджет сложности/стоимости + ограничения пагинации и добавь allow-list persisted-запросы для first-party-клиентов", summary: "Defense in depth.", correct: true, justification: "Эти контроли покрывают ортогональные оси атаки. Лимит глубины (макс ~7-10) отклоняет рекурсивную бомбу, вкладывающуюся в самоссылочный тип. Анализ сложности/стоимости суммирует стоимость поля, умноженную на аргументы пагинации, и отклоняет за бюджетом (~1000), ловя широкий-но-неглубокий запрос, который лимит глубины пропускает. Ограничения пагинации лимитируют first/limit, чтобы один список не запросил 10000 строк. А для first-party-клиентов allow-list persisted-запросы целиком устраняют DoS произвольным запросом, выполняя только проверенные заранее зарегистрированные операции. Все они валидируют ДО выполнения. Это слоение и есть сеньорный ответ — ни один контроль не покрывает глубину, ширину и произвольность сразу." },
    { name: "Добавь лимит глубины (макс 7) и считай дело сделанным", summary: "Пропускает ширину.", justification: "Лимит глубины необходим, но недостаточен. Он отклоняет бомбу рекурсивных друзей, но слеп к ширине и множителям пагинации: users(first: 10000) { posts(first: 1000) { comments(first: 1000) } } — всего три уровня в глубину и проскальзывает прямо сквозь лимит глубины, умножаясь до ~10 миллиардов узлов. Тебе ещё нужен анализ сложности/стоимости, чтобы ограничить fan-out. Глубина в одиночку — частичная защита, оставляющая широкий-но-неглубокий вектор нараспашку." },
    { name: "Положись на rate limiter на краю", summary: "Один запрос уже убивает.", justification: "Rate limiting ограничивает ЧИСЛО запросов, но GraphQL-DoS — это ОДИН запрос, разворачивающийся в миллионы вызовов резолверов. Лимит даже в один запрос в секунду на IP ничего не делает против одного запроса, пиннящего Postgres на минуты. Rate limiting — полезный внешний слой, но он не ограничивает стоимость отдельного запроса — а это ровно то, что делают анализ глубины и сложности. Он не может быть основной защитой." },
    { name: "Доверяй клиентам и просто отключи интроспекцию в production", summary: "Театр безопасности.", justification: "Отключение интроспекции прячет схему от случайного обнаружения, но не останавливает того, кто уже знает или угадывает имена полей — а бомбе рекурсивных друзей интроспекция не нужна, чтобы её написать. Доверие клиентам — исходная ошибка, вызвавшая инцидент. Ни одна мера вообще не ограничивает стоимость запроса; они оставляют сам DoS-вектор полностью открытым. Это самый слабый вариант." },
  ]}
/>

<Quiz
  id="nest-08-microservices-and-graphql-05-graphql-hardening-and-federation-quiz-1"
  lessonSlug="05-graphql-hardening-and-federation"
  lang="ru"
  question="Почему один GraphQL-запрос может быть DoS-вектором, тогда как один REST-эндпоинт обычно нет?"
  choices={[
    { label: "Клиент составляет GraphQL-запрос — выбирая произвольную глубину и ширину — так что один запрос может развернуться в неограниченную работу резолверов и БД, тогда как REST-эндпоинт имеет фиксированную, принадлежащую серверу, ограниченную форму ответа", correct: true },
    { label: "GraphQL всегда медленнее REST, потому что парсит строку запроса", misconception: "Оверхед парсинга ничтожен и не в этом дело. DoS приходит от стоимости ВЫПОЛНЕНИЯ запроса, а не его парсинга: поскольку запрос составляет клиент, один запрос может потребовать произвольно глубокие, широкие, рекурсивные данные, разворачивающиеся в миллионы вызовов резолверов. REST-эндпоинт нельзя об этом попросить, потому что форму его ответа фиксирует сервер." },
    { label: "GraphQL запускает каждый резолвер на главном потоке, блокируя event loop", misconception: "Вектор не про потоки — Node обрабатывает I/O асинхронно в любом случае. Проблема в ОБЪЁМЕ работы, которую может запросить один составленный клиентом запрос: неограниченные глубина и ширина превращают один запрос в миллионы вызовов резолверов/БД. Та же асинхронная модель под REST безопасна, потому что стоимость каждого эндпоинта ограничена сервером." },
    { label: "REST-эндпоинты нельзя атаковать, так что весь риск DoS переезжает на GraphQL", misconception: "REST безусловно можно атаковать (например объёмом запросов), для чего и существует rate limiting. Суть уже: ОДИН GraphQL-запрос может быть DoS, потому что клиент контролирует глубину и ширину запроса, тогда как один REST-запрос попадает в одну фиксированную, ограниченную, заданную сервером форму ответа." },
  ]}
/>

<Quiz
  id="nest-08-microservices-and-graphql-05-graphql-hardening-and-federation-quiz-2"
  lessonSlug="05-graphql-hardening-and-federation"
  lang="ru"
  question="Запрос всего в три уровня глубины всё равно плавит базу. Почему лимит глубины его пропускает и какой контроль его ловит?"
  choices={[
    { label: "Лимит глубины считает только вложенность, поэтому слеп к ширине и множителям пагинации; анализ сложности/стоимости ловит его, оценивая стоимость поля, умноженную на размер списка/пагинации, и отклоняя за бюджетом", correct: true },
    { label: "Лимит глубины был просто задан слишком высоким; понижение до 2 поймало бы запрос", misconception: "Понижение лимита глубины до 2 сломало бы легитимные запросы и всё равно упускает суть: этот запрос правда всего ~3 уровня в глубину, но разрушителен из-за ШИРИНЫ — users(first: 10000) { posts(first: 1000) ... }. Лимит глубины вообще не видит множителей пагинации. Анализ сложности/стоимости умножает размеры списков и отклоняет запрос за бюджетом независимо от того, насколько он неглубок." },
    { label: "Лимиты глубины применяются только к мутациям, так что этот запрос на чтение их обошёл", misconception: "Лимит глубины применяется к любой операции, чтения включая — пробел не в этом. Пробел в том, что глубина считает только вложенность и игнорирует ширину и аргументы пагинации, так что неглубокий-но-широкий запрос проходит. Контроль, ограничивающий fan-out, — это анализ сложности/стоимости, оценивающий размер списка на множитель пагинации против бюджета." },
    { label: "DataLoader должен был это предотвратить, батчируя чтения", misconception: "DataLoader ограничивает то, что делают твои РЕЗОЛВЕРЫ (батчинг N+1 чтений), а не то, что клиент может ПОПРОСИТЬ. Он не может отклонить запрос за дороговизну — он послушно сбатчит запрос на миллиарды узлов. Контроль, отклоняющий широкий-но-неглубокий запрос до выполнения, — это анализ сложности/стоимости." },
  ]}
/>

</Explanation>

<KeyTakeaway>Один запрос разворачивается в миллионы вызовов. Отклоняй до выполнения: лимит глубины против рекурсии, бюджет сложности против fan-out, allow-list persisted-запросы против DoS. Федерируй через @key; держи ворота на краю.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="nest-08-microservices-and-graphql-05-graphql-hardening-and-federation-retrieval"
  lang="ru"
  questions={[
    { q: "Объясни, почему публичный GraphQL-эндпоинт — DoS-вектор, которым не является REST, и три контроля, которые его защищают — точно про то, что каждый ловит и упускает.", a: "В REST форму каждого ответа владеет сервер, так что стоимость эндпоинта фиксирована и ограничена в момент написания; клиент не может попросить более глубокие вложенные данные, не делая больше отдельно лимитируемых запросов. GraphQL это переворачивает: клиент составляет запрос, выбирая поля, глубину и ширину в одном HTTP-вызове, так что самоссылочный тип вроде User.friends позволяет одному запросу вкладываться сам в себя — user { friends { friends { ... } } } — и развернуться в миллионы вызовов резолверов и обращений к БД без авторизации. Контроли, все работающие ДО выполнения: (1) Лимит глубины ограничивает вложенность (макс ~7-10) и отклоняет рекурсивную бомбу на валидации, но считает только глубину и слеп к ширине и множителям пагинации. (2) Анализ сложности/стоимости назначает числовую стоимость на поле, умножает поля-списки на их аргумент пагинации (first/limit), суммирует запрос и отклоняет за бюджетом (~1000) — это ловит широкий-но-неглубокий запрос вроде users(first: 10000) { posts(first: 1000) { comments(first: 1000) } }, который всего три уровня в глубину, но умножается до ~10 миллиардов узлов. Нужны оба: глубина против рекурсии, стоимость против fan-out. (3) Persisted-запросы в режиме allow-list шлют хеш заранее зарегистрированного запроса и выполняют только проверенные операции, целиком устраняя DoS произвольным запросом — сильнейший рычаг для first-party-клиентов, хотя слишком жёсткий для публичных/сторонних API, составляющих новые запросы. Плюс ограничения пагинации, таймауты и rate limiting как внешние слои, и DataLoader, чтобы ограничить N+1 на стороне резолверов, которым анализ стоимости не занимается." },
    { q: "Объясни Apollo Federation: задачу, которую она решает, как сабграфы и шлюз композируют граф, роль @key и __resolveReference, чем она отличается от schema stitching и где живёт защита.", a: "По мере роста графа единая монолитная схема и набор резолверов становятся узким местом деплоя и беспорядком владения — каждая команда правит один файл, каждое изменение — полный передеплой. Apollo Federation (ApolloFederationDriver в NestJS) разбивает граф между сервисами. Каждый сабграф владеет своими типами и помечает сущности, которыми владеет, через @key(fields: \"id\"), что даёт сущности стабильную идентичность через граф, и предоставляет резолвер __resolveReference, чтобы другие сабграфы могли расширять эту сущность по ссылке (сабграф reviews добавляет reviews к User, которым не владеет, резолвя ссылку по id). Шлюз композирует сабграфы в один суперграф и планирует кросс-сервисные запросы, доставая каждое поле из сабграфа-владельца. Это современный декларативный преемник schema stitching, где шлюз вручную сливал схемы, а кросс-сервисную обвязку ты писал руками; федерация делает связи декларативными через @key и __resolveReference и позволяет шлюзу планировать запрос. Критически, защита не переезжает на шлюз и не исчезает: лимиты глубины, анализ сложности/стоимости и persisted-запросы по-прежнему живут на краю, потому что суперграф ровно так же составляем клиентом — а значит ровно так же уязвим, — как единый граф, который он заменил." },
  ]}
/>

<Recap lang="ru">
Публичный GraphQL-эндпоинт — DoS-вектор, которым REST не является, потому что запрос составляет клиент и может потребовать произвольно глубокие, широкие, рекурсивные данные одним запросом — самоссылочный тип позволяет `user &#123; friends &#123; friends &#123; ... &#125; &#125; &#125;` развернуться в миллионы вызовов резолверов и БД без авторизации. Защита многослойна и работает до выполнения. Лимит глубины ограничивает вложенность ~7-10 и отклоняет рекурсивную бомбу, но слеп к ширине и множителям пагинации. Анализ сложности/стоимости назначает стоимость на поле, умножает поля-списки на их аргумент пагинации, суммирует запрос и отклоняет за бюджетом (~1000) — ловя широкий-но-неглубокий запрос вроде `users(first: 10000)`, который всего три уровня в глубину, но умножается до миллиардов узлов; нужны оба контроля, потому что они покрывают ортогональные оси. Persisted-запросы в режиме allow-list шлют хеш заранее зарегистрированного запроса и выполняют только проверенные операции, целиком устраняя DoS произвольным запросом — сильнейший рычаг для first-party-клиентов, слишком жёсткий для публичных. Ограничения пагинации, таймауты и rate limiting — внешние слои, а DataLoader ограничивает рантайм-N+1, которым анализ стоимости не занимается. Когда сам единый граф становится узким местом деплоя и владения, Apollo Federation разбивает его на сабграфы, владеющие сущностями через @key и резолвящие ссылки через __resolveReference, а шлюз композирует их в один суперграф — современный преемник ручного schema stitching. Ворота защиты остаются на краю, потому что суперграф ровно так же уязвим, как единый граф, который он заменил.
</Recap>
