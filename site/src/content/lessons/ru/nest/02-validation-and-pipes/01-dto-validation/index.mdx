---
concepts:
- dto-classes
- class-validator
- validation-pipe
- whitelist-forbid
- transform-coercion
- built-in-pipes
deepensInto: []
estMin: 16
lang: ru
lessonType: topic
level: middle
order: 1
prereqs: []
slug: 01-dto-validation
sources:
- https://docs.nestjs.com/techniques/validation
- https://docs.nestjs.com/pipes
spiral: []
status: ready
summary: 'DTO — это runtime-класс, чьи декораторы class-validator описывают валидный
  запрос; ValidationPipe читает эти метаданные до хендлера, отклоняет плохой ввод с
  400, срезает неизвестные поля и приводит типы — так хендлер доверяет своим аргументам.'
title: 'DTO, class-validator и pipes'
track: nest
unit: 02-validation-and-pipes
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import SequenceDiagram from "~/components/diagram/SequenceDiagram.astro";
import Infographic from "~/components/diagram/Infographic.astro";
import pipeOptions from "./infographics/pipe-options.svg?raw";

<Hook>
POST на `/users` приходит с `{ "email": 42, "isAdmin": true }`. Валидации нет, поэтому контроллер берёт тело как есть, сервис разворачивает его в Prisma `create`, и вот у тебя пользователь, чей email — число 42 и который тихо выдал себе админку — поле, которое твой API вообще не собирался показывать. Хендлер доверился проводу. Этого не должно было случиться: контракт полагалось проверить у двери, до того как отработает хоть строка бизнес-логики.
</Hook>

<Crux>DTO — это runtime-класс; декораторы class-validator описывают валидный запрос, а ValidationPipe проверяет контракт до запуска хендлера.</Crux>

<Explanation>

## DTO — это класс, а не интерфейс, потому что метаданные должны дожить до runtime

DTO (Data Transfer Object) описывает форму тела запроса, query или param. В Nest ты пишешь его как **класс**, никогда не как TypeScript `interface`. Причина конкретна: интерфейсы стираются при компиляции — в runtime их не существует, — а валидация Nest работает через чтение *метаданных* декораторов с объекта в runtime. Декоратору `@IsEmail()` на свойстве интерфейса не к чему привязаться после того, как типы стёрты. Класс переживает транспиляцию как настоящее значение, поэтому декораторы (и эмитнутые метаданные типов) на месте, когда pipe их спрашивает.

Каждое поле ты аннотируешь декораторами [class-validator](https://github.com/typestack/class-validator), которые объявляют его ограничения: `@IsString`, `@IsEmail`, `@IsInt`, `@Min`, `@IsOptional`, `@ValidateNested` и десятки других. DTO становится единым декларативным источником истины «как выглядит валидный запрос» — читаемым, переиспользуемым и тестируемым в изоляции.

```ts
import { IsEmail, IsString, IsInt, Min, IsOptional } from 'class-validator';

export class CreateUserDto {
  @IsEmail()
  email: string;

  @IsString()
  @Min(2) // длина строк — через @MinLength в реальном коде; @Min показан для чисел ниже
  name: string;

  @IsInt()
  @Min(18)
  age: number;

  @IsOptional()
  @IsString()
  bio?: string;
}
```

## Pipe бежит до хендлера — и решает, что хендлер вообще увидит

**Pipe** — это класс с методом `transform()`, который Nest запускает на аргументе маршрута *до* того, как хендлер его получит. Pipes делают две работы: **трансформацию** (изменить форму или привести значение) и **валидацию** (проверить, бросить исключение при провале). Встроенный `ValidationPipe` — тот, что связывает DTO воедино: он читает метаданные class-validator с твоего DTO, валидирует входящий payload против них и при провале бросает `BadRequestException` — автоматический **400** со списком того, что не так. Хендлер запускается только на вводе, который уже прошёл.

Включаешь его один раз, глобально, в `main.ts`:

```ts
import { ValidationPipe } from '@nestjs/common';
import { NestFactory } from '@nestjs/core';
import { AppModule } from './app.module';

async function bootstrap() {
  const app = await NestFactory.create(AppModule);
  app.useGlobalPipes(
    new ValidationPipe({
      whitelist: true,            // срезать свойства, которых нет в DTO
      forbidNonWhitelisted: true, // ...или отклонить запрос, если они появились
      transform: true,            // привести plain-объект к экземпляру DTO + привести примитивы
    }),
  );
  await app.listen(3000);
}
bootstrap();
```

Контроллер тогда просто объявляет DTO как тип аргумента `@Body()`. Кода валидации в контроллере нет — в этом и смысл:

```ts
import { Controller, Post, Body, Get, Param, ParseIntPipe } from '@nestjs/common';
import { CreateUserDto } from './dto/create-user.dto';

@Controller('users')
export class UsersController {
  @Post()
  create(@Body() dto: CreateUserDto) {
    // dto уже провалидирован И является экземпляром CreateUserDto.
    // Неизвестные поля вроде `isAdmin` срезаны (или запрос отклонён).
    return this.users.create(dto);
  }

  @Get(':id')
  findOne(@Param('id', ParseIntPipe) id: number) {
    // ParseIntPipe — встроенный pipe — приводит строковый param к числу
    // и отдаёт 400, если это не валидное целое. Здесь id — настоящее число.
    return this.users.findOne(id);
  }
}
```

## Три опции, которые важны: whitelist, forbidNonWhitelisted, transform

Эти опции не косметика — две из них настоящий контроль безопасности.

- **`whitelist: true`** срезает любое свойство payload'а, у которого в DTO нет декоратора валидации. Лишний `isAdmin: true` из хука просто никогда не доходит до твоего сервиса.
- **`forbidNonWhitelisted: true`** (используй *вместе* с `whitelist`) идёт дальше: вместо тихого срезания неизвестных полей он отклоняет весь запрос с 400, перечисляя их. Это более жёсткая позиция — она вскрывает клиентов, шлющих поля, которые ты не принимаешь, а не тихо их роняет.
- **`transform: true`** делает две вещи. Он создаёт экземпляр **класса** DTO из plain JSON-объекта (через class-transformer), так что значение, которое получает хендлер, — настоящий экземпляр `CreateUserDto`, а не голый объект; методы и `instanceof` работают. Он также приводит примитивные типы: query-param `?age=18`, пришедший строкой `"18"`, становится числом `18`, когда поле DTO типизировано как `number`.

Вместе `whitelist` + `forbidNonWhitelisted` — твоя защита от **mass-assignment / over-posting** — класса багов, где клиент задаёт поле (role, balance, `isAdmin`), которое сервер никогда не предполагал делать задаваемым с клиента. Валидация на границе — это то, что позволяет остальному приложению *доверять своим вводам*: как только pipe отработал, каждый слой ниже может считать DTO заведомо корректным.

<Infographic
  svg={pipeOptions}
  label="Таксономия трёх опций ValidationPipe, сгруппированных по их двум задачам: whitelist и forbidNonWhitelisted обе останавливают mass-assignment, а transform делает две вещи — превращает plain JSON в настоящий экземпляр DTO и приводит строку вроде 18 к числу 18."
  caption="Три опции делятся на две задачи: whitelist + forbidNonWhitelisted — контроль безопасности против mass-assignment; transform — контроль типизации, отдающий хендлеру настоящий приведённый экземпляр DTO."
  hue="var(--d-infra)"
/>

<Inset kind="why" lang="ru">
Почему *глобальный* pipe, а не по-маршрутно? Регистрация `ValidationPipe` один раз в `main.ts` делает валидацию дефолтом для каждого эндпоинта, поэтому новый контроллер безопасен по умолчанию — ты не можешь забыть её добавить. Цена в том, что `whitelist`/`forbid` теперь применяются везде, — а именно этого ты и хочешь для публичного API. Для редкого эндпоинта с другими правилами переопределяешь локально через `@UsePipes(new ValidationPipe({ ... }))` на этом хендлере. Берегись одной ловушки: `transform: true` надёжно приводит примитивы только при наличии метаданных типов, поэтому в `tsconfig` должны быть включены `emitDecoratorMetadata` и `experimentalDecorators`.
</Inset>

Схема ниже — то, через что теперь проходит каждый запрос: pipe стоит между проводом и твоим хендлером.

<SequenceDiagram
  label="HTTP-запрос проходит через ValidationPipe, который валидирует и трансформирует тело против DTO до запуска хендлера контроллера."
  hue="var(--d-systems)"
  actors={["Client", "ValidationPipe", "Handler"]}
  messages={[
    { from: "Client", to: "ValidationPipe", label: "POST /users { email, isAdmin }" },
    { from: "ValidationPipe", to: "ValidationPipe", label: "read DTO class-validator metadata" },
    { from: "ValidationPipe", to: "ValidationPipe", label: "whitelist: strip isAdmin" },
    { from: "ValidationPipe", to: "Client", label: "invalid -> throw 400 BadRequest" },
    { from: "ValidationPipe", to: "Handler", label: "valid -> CreateUserDto instance" },
    { from: "Handler", to: "Client", label: "201 Created (trusted input)" },
  ]}
  caption="Время течёт сверху вниз. Pipe бежит до хендлера: невалидный ввод никогда не доходит до бизнес-логики, а то, что доходит, — чистый типизированный экземпляр DTO."
/>

Таблица делает разницу наглядной — тот же шумный payload, с pipe и без него:

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Аспект</th>
        <th class="border border-border px-3 py-2 text-left">Без ValidationPipe</th>
        <th class="border border-border px-3 py-2 text-left">ValidationPipe (whitelist + forbid + transform)</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">Что доходит до хендлера</td>
        <td class="border border-border px-3 py-2">Сырой распарсенный JSON, как есть</td>
        <td class="border border-border px-3 py-2">Провалидированный экземпляр <code>CreateUserDto</code></td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Неизвестное поле <code>isAdmin</code></td>
        <td class="border border-border px-3 py-2">Проходит прямо в сервис</td>
        <td class="border border-border px-3 py-2">Срезано (whitelist) или запрос отклонён (forbid)</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Приведение типа <code>?age=18</code></td>
        <td class="border border-border px-3 py-2">Остаётся строкой <code>"18"</code></td>
        <td class="border border-border px-3 py-2">Приведено к числу <code>18</code></td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Невалидное тело (<code>email: 42</code>)</td>
        <td class="border border-border px-3 py-2">Сохранено как есть; битые данные ниже по стеку</td>
        <td class="border border-border px-3 py-2">Автоматический <code>400</code> со списком ошибок по полям</td>
      </tr>
    </tbody>
  </table>
</div>

Помимо `ValidationPipe`, Nest поставляет маленькие **встроенные pipes** для одиночных скалярных аргументов — `ParseIntPipe`, `ParseUUIDPipe`, `ParseBoolPipe`, `ParseArrayPipe`, — и ты можешь написать **кастомный pipe**, реализовав `transform(value, metadata)` из `PipeTransform`. Но для тел запросов правило держится: клади ограничения в DTO, а не в контроллер, и пусть один глобальный `ValidationPipe` их обеспечивает.

<Quiz
  id="01-dto-validation-quiz-1"
  lessonSlug="01-dto-validation"
  lang="ru"
  question="Почему Nest-DTO должен быть классом, а не TypeScript-интерфейсом?"
  choices={[
    { label: "Интерфейсы стираются при компиляции, поэтому декораторов class-validator и метаданных типов, которые pipe читает в runtime, просто не существовало бы", correct: true },
    { label: "Классы создаются быстрее, чем интерфейсы", misconception: "Интерфейс — конструкция системы типов без runtime-стоимости и без экземпляра вообще — создавать нечего. Дело не в скорости, а в существовании в runtime." },
    { label: "Nest запрещает интерфейсы в DTO правилом линтера", misconception: "Это не стилевое правило. У интерфейса просто нет runtime-представления, поэтому нет объекта, к которому привязаться декораторам, и нечего инспектировать ValidationPipe." },
    { label: "Интерфейсы не могут описывать опциональные поля", misconception: "Интерфейсы прекрасно справляются с опциональными полями (foo?: string). Блокер в том, что декораторам и метаданным нужен runtime-класс, на котором жить." },
  ]}
/>

<Quiz
  id="01-dto-validation-quiz-2"
  lessonSlug="01-dto-validation"
  lang="ru"
  question="Клиент шлёт POST { email, name, isAdmin: true }, но в DTO нет поля isAdmin. ValidationPipe работает с whitelist: true. Что произойдёт?"
  choices={[
    { label: "isAdmin срезается из payload, поэтому хендлер получает только декорированные поля и никогда не видит isAdmin", correct: true },
    { label: "Весь запрос отклоняется с 400", misconception: "Это forbidNonWhitelisted: true. Чистый whitelist тихо срезает неизвестные поля; чтобы отклонить запрос и перечислить их, нужен forbidNonWhitelisted." },
    { label: "isAdmin проходит, но ставится в null", misconception: "whitelist удаляет свойство целиком, не зануляет его. В DTO хендлера ключа isAdmin нет вообще." },
    { label: "Ничего — whitelist влияет только на приведение типов", misconception: "Приведение типов — это опция transform. whitelist именно про срезание свойств без декоратора валидации в DTO." },
  ]}
/>

<TradeoffMatrix
  id="01-dto-validation-tradeoff-1"
  lessonSlug="01-dto-validation"
  lang="ru"
  prompt="Публичный POST-эндпоинт не должен давать клиенту задать поле, которое сервер не предполагал (mass-assignment), а плохие тела должны падать громко. Выбери конфигурацию ValidationPipe."
  options={[
    { name: "whitelist: true, forbidNonWhitelisted: true, transform: true", summary: "Срезать + отклонить + привести.", correct: true, justification: "Это закалённый дефолт для публичного API. whitelist срезает недекорированные поля, forbidNonWhitelisted превращает неожиданное поле в громкий 400 вместо тихого роняния (так клиенты, шлющие то, что ты не принимаешь, всплывают), а transform даёт хендлеру настоящий экземпляр DTO с приведёнными примитивами. Вместе whitelist+forbid закрывают дыру mass-assignment — ровно то, что требуется." },
    { name: "только whitelist: true", summary: "Срезать тихо.", justification: "Безопасно от mass-assignment, потому что неизвестные поля срезаются, но падает тихо — клиент, шлющий isAdmin, получает 201 и никогда не узнаёт, что поле проигнорировали, что прячет баги интеграции. Бриф просит, чтобы плохие тела падали громко, так что добавь forbidNonWhitelisted." },
    { name: "только transform: true (без whitelist)", summary: "Приводить, не срезать.", justification: "Ты получаешь экземпляры DTO и приведение примитивов, но без whitelist недекорированные свойства проходят прямо в сервис — дыра mass-assignment настежь открыта. transform решает типизацию, а не over-posting." },
    { name: "Без глобального pipe; валидировать руками в каждом контроллере", summary: "Ручные проверки.", justification: "Переизобретает то, что делает ValidationPipe, размазывает валидацию по контроллерам и есть тот самый баг из хука, ждущий своего часа — один забытый эндпоинт принимает что угодно. Валидация декларативно живёт в DTO, обеспечивается одним глобальным pipe, а не пишется руками на каждом маршруте." },
  ]}
/>

</Explanation>

<KeyTakeaway>DTO — классы, чтобы метаданные декораторов дожили до runtime. Глобальный `ValidationPipe` с `whitelist` + `forbidNonWhitelisted` убивает mass-assignment, а `transform` даёт типизированные экземпляры. Держи ограничения в DTO.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="01-dto-validation-retrieval"
  lang="ru"
  questions={[
    { q: "Пройди по тому, что ValidationPipe делает с телом запроса, и где он стоит относительно хендлера.", a: "ValidationPipe — это pipe, поэтому Nest запускает его на аргументе маршрута до того, как вообще вызван хендлер контроллера. Он читает метаданные декораторов class-validator с класса DTO, которым ты типизировал @Body(), затем валидирует входящий plain-объект против этих ограничений. Если что-то проваливается, он бросает BadRequestException, который Nest превращает в автоматический ответ 400 со списком ошибок по полям — хендлер не запускается. Если прошло, опции формируют то, что получает хендлер: whitelist срезает любое свойство без декоратора валидации, forbidNonWhitelisted вместо этого отклоняет весь запрос при наличии неизвестных полей, а transform создаёт экземпляр класса DTO из plain-объекта (так ты получаешь настоящий CreateUserDto) и приводит примитивные типы, например строку \"18\" к числу 18. Так pipe стоит ровно между проводом и твоей бизнес-логикой, и хендлер всегда видит лишь чистый, типизированный, заведомо корректный ввод." },
    { q: "Почему whitelist + forbidNonWhitelisted важны для безопасности и почему DTO должен быть классом?", a: "Вместе они защищают от mass-assignment (over-posting): бага, где клиент шлёт поле, которое сервер никогда не предполагал делать задаваемым с клиента — role, balance, isAdmin, — и оно слепо сохраняется. whitelist срезает любое свойство, которое DTO не объявляет с декоратором валидации, так что вброшенный isAdmin никогда не доходит до сервиса; forbidNonWhitelisted — более жёсткая позиция, отклоняющая запрос с 400 вместо тихого роняния поля, что вскрывает клиентов, шлющих то, что ты не принимаешь. Это работает лишь потому, что DTO — класс: TypeScript-интерфейсы стираются при компиляции и не имеют runtime-существования, поэтому не было бы объекта, на котором жить декораторам class-validator или эмитнутым метаданным типов, и pipe нечего было бы читать. Класс переживает транспиляцию как настоящее значение, неся свои метаданные декораторов в runtime, где их инспектирует ValidationPipe." },
  ]}
/>

<Recap lang="ru">
Валидируй ввод на границе, чтобы остальное приложение могло ему доверять. DTO (Data Transfer Object — объект передачи данных) — это runtime-класс, никогда не интерфейс, потому что декораторы class-validator и метаданные типов, которые читает валидатор, должны пережить компиляцию, а интерфейсы стираются. Каждое поле ты аннотируешь (`@IsEmail`, `@IsInt`, `@Min`, `@IsOptional`, `@ValidateNested`), чтобы декларативно объявить валидный запрос. Pipe (конвейерный преобразователь аргументов) бежит до хендлера, чтобы трансформировать и валидировать аргумент; встроенный `ValidationPipe`, включённый один раз через `app.useGlobalPipes(new ValidationPipe({ ... }))`, читает метаданные DTO, отклоняет плохой ввод автоматическим 400 со списком ошибок по полям, а иначе отдаёт хендлеру чистый ввод. Его три несущие опции: `whitelist` срезает недекорированные свойства, `forbidNonWhitelisted` отклоняет запросы, которые их несут (вместе закрывая дыру mass-assignment), а `transform` создаёт экземпляр класса DTO и приводит примитивы вроде `"18"` к `18`. Помимо него, встроенные pipes (`ParseIntPipe` и друзья) и кастомные классы `PipeTransform` обрабатывают скалярные аргументы. Дисциплина, что связывает это воедино: держи ограничения в DTO, а не в контроллере, и пусть один глобальный pipe обеспечивает их везде. Теперь, когда ты видишь метод контроллера, который открывается ручными проверками типов или доверяет сырому `req.body`, — ты знаешь, что взять вместо этого.
</Recap>
