infographic hierarchy-tree-curved-line-rounded-rect-node
data
  root
    label ValidationPipe — 3 опции, 2 задачи
    children
      - label Защита от mass-assignment (безопасность)
        children
          - label whitelist — убирает поля без декоратора в DTO
          - label forbidNonWhitelisted — отклоняет запрос, 400 со списком лишних
      - label Отдать хендлеру типизированный вход
        children
          - label transform — обычный JSON в реальный экземпляр DTO
          - label transform — приводит строку 18 к числу 18
theme
  palette #6366f1 #f43f5e #14b8a6
