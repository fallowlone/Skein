infographic hierarchy-tree-curved-line-rounded-rect-node
data
  root
    label Один слот на каждую сквозную задачу
    children
      - label Guard (CanActivate)
        children
          - label Аутентификация / авторизация — ворота да/нет
          - label Читает @Roles через Reflector, до пайпов
      - label Pipe (ValidationPipe)
        children
          - label Валидация + трансформация входа
          - label Бросает на невалидном входе, прямо перед хендлером
      - label Interceptor (RxJS)
        children
          - label Логирование, кэш, трансформация, таймаут
          - label Единственный этап, что работает и после хендлера
      - label Filter (@Catch)
        children
          - label Формирует ответ-ошибку, один раз
          - label Единый сток для любого throw в цепочке
theme
  palette #6366f1 #14b8a6 #f59e0b #f43f5e
