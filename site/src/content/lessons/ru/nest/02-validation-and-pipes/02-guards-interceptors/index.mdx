---
concepts:
- request-lifecycle
- guards-canactivate
- interceptors-rxjs
- exception-filters
- reflector-metadata
- binding-scope
deepensInto: []
estMin: 17
lang: ru
lessonType: topic
level: senior
order: 2
prereqs: []
slug: 02-guards-interceptors
sources:
- https://docs.nestjs.com/guards
- https://docs.nestjs.com/interceptors
- https://docs.nestjs.com/exception-filters
- https://docs.nestjs.com/faq/request-lifecycle
spiral: []
status: ready
summary: Request lifecycle в Nest фиксирован — middleware, guard, interceptor (pre),
  pipe, handler, interceptor (post), filter. Авторизацию — в guard, валидацию — в
  pipe, логирование/трансформацию — в interceptor, форму ошибки — в filter.
title: 'Guards, interceptors и exception filters'
track: nest
unit: 02-validation-and-pipes
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import SequenceDiagram from "~/components/diagram/SequenceDiagram.astro";

<Hook>
Ревьюер задаёт один вопрос в твоём PR: «почему проверка роли внутри контроллера?» Хендлер начинается с `if (req.user.role !== 'admin') throw new ForbiddenException()`, потом валидирует тело руками, потом логирует длительность через `Date.now()` сверху и снизу, потом форматирует каждую ошибку в один и тот же JSON. Это работает. И это четыре сквозные задачи, размазанные по бизнес-логике и скопированные в девятнадцать хендлеров, — а в день, когда кто-то забудет строку с ролью в хендлере двадцать, это уедет в прод. У Nest уже есть слот для каждой из них. Ревьюер не просит тебя перенести код; он спрашивает, знаешь ли ты request lifecycle.
</Hook>

<Crux>Lifecycle в Nest фиксирован: middleware → guard → interceptor → pipe → handler → interceptor → filter. У задачи один верный слот.</Crux>

<Explanation>

## Lifecycle — это весь урок

Каждый HTTP-запрос в Nest проходит одну и ту же упорядоченную цепочку до и после твоего хендлера. Знание порядка — не мелочь: оно говорит, где каждая задача *может* стоять и, что не менее важно, что она *видит*, когда выполняется.

Последовательность: **middleware → guards → interceptors (pre) → pipes → handler → interceptors (post) → exception filters** (при выбросе). Guard выполняется после middleware, но до любого interceptor или pipe. Pipes выполняются после того, как guards уже решили, что запрос может пройти, — так что к моменту, когда pipe трансформирует тело, авторизация уже улажена. Interceptors — единственный этап, который выполняется *дважды*: на входе (до хендлера) и на выходе (оборачивая ответ). А exception filters стоят сбоку, ловя всё, что выброшено где угодно ниже.

Две тонкости отделяют сеньоров от тех, кто пролистал доки. Первая: **interceptors на выходе разворачиваются по принципу first-in-last-out** — на входе они идут global → controller → route, но ответ разматывается route → controller → global, как возвраты вложенных функций. Вторая: **filter — это catch-all сток** — pipe, который выбросил, guard, который выбросил, хендлер, который выбросил, и даже стрим interceptor, который зафейлился, — всё приземляется в один и тот же filter. Поэтому *форматирование* ошибки живёт там и нигде больше.

<SequenceDiagram
  label="HTTP request lifecycle в Nest по порядку: входящий запрос проходит через middleware, затем guard решает, можно ли продолжить, затем выполняется pre-логика interceptor, затем pipe валидирует и трансформирует вход, затем выполняется хендлер маршрута, затем post-логика interceptor формирует ответ на обратном пути; если что-то выброшено, управление перескакивает в exception filter, который форматирует ответ-ошибку."
  hue="var(--d-systems)"
  actors={["Request", "Pipeline", "Handler"]}
  messages={[
    { from: "Request", to: "Pipeline", label: "1. middleware (req/res, next)" },
    { from: "Pipeline", to: "Pipeline", label: "2. guard: canActivate -> bool" },
    { from: "Pipeline", to: "Pipeline", label: "3. interceptor PRE (before)" },
    { from: "Pipeline", to: "Pipeline", label: "4. pipe: validate + transform" },
    { from: "Pipeline", to: "Handler", label: "5. handler runs" },
    { from: "Handler", to: "Pipeline", label: "6. interceptor POST (map/tap)" },
    { from: "Pipeline", to: "Request", label: "7. response (or filter on throw)" },
  ]}
  caption="Guards решают go/no-go до того, как вообще запустятся pipes; interceptors оборачивают хендлер с обеих сторон; любой выброс на любом этапе прыгает в exception filter. Обратный путь interceptor разматывается route -> controller -> global."
/>

## Guards: решение да/нет, прочитанное из метаданных

Guard реализует `CanActivate`. Он возвращает boolean (или Promise/Observable от него) — `true` пропускает запрос, `false` (или выброшенное исключение) останавливает. Это дом для **аутентификации и авторизации**. Guard получает `ExecutionContext` — надмножество `ArgumentsHost`, которое к тому же знает, *какой хендлер и контроллер* вот-вот выполнятся, — а это ровно то, что нужно, чтобы читать метаданные уровня маршрута.

Канонический паттерн — декоратор `@Roles()`, который штампует метаданные на хендлер, плюс `RolesGuard`, который читает их назад через `Reflector`:

```ts
import { Injectable, CanActivate, ExecutionContext } from '@nestjs/common';
import { Reflector } from '@nestjs/core';
import { ROLES_KEY } from './roles.decorator';

@Injectable()
export class RolesGuard implements CanActivate {
  constructor(private reflector: Reflector) {}

  canActivate(context: ExecutionContext): boolean {
    const required = this.reflector.getAllAndOverride<string[]>(ROLES_KEY, [
      context.getHandler(),
      context.getClass(),
    ]);
    if (!required) return true; // нет @Roles() на маршруте -> открыт
    const { user } = context.switchToHttp().getRequest();
    return required.some((role) => user?.roles?.includes(role));
  }
}
```

`getAllAndOverride` читает метаданные сначала с хендлера, затем откатывается на контроллер, так что `@Roles('admin')` на уровне маршрута перекрывает дефолт контроллера. Выбрасывай `ForbiddenException` вместо возврата `false`, если хочешь 403 с сообщением, а не голый отказ.

## Interceptors: оборачивают хендлер с обеих сторон

`intercept(context, next)` interceptor выполняет код **до** хендлера, затем вызывает `next.handle()` — который возвращает **RxJS `Observable`** с будущим ответом — и может выполнить код **после**, навесив операторы на этот стрим. Эта двойная позиция делает interceptors верным инструментом для всего, что обрамляет вызов: логирования и тайминга, кэширования, формирования ответа и таймаутов.

```ts
import { Injectable, NestInterceptor, ExecutionContext, CallHandler } from '@nestjs/common';
import { Observable } from 'rxjs';
import { map, tap } from 'rxjs/operators';

@Injectable()
export class TransformInterceptor implements NestInterceptor {
  intercept(ctx: ExecutionContext, next: CallHandler): Observable<unknown> {
    const start = Date.now();
    return next.handle().pipe(
      tap(() => console.log(`took ${Date.now() - start}ms`)), // side-effect, данные не меняются
      map((data) => ({ data })),                              // переформировать каждый ответ
    );
  }
}
```

`tap` — для side-effect'ов, которые не трогают payload (логирование, метрики); `map` трансформирует тело ответа — здесь оборачивает возврат каждого хендлера в `{ data: ... }`, чтобы весь API говорил одним конвертом. Поскольку `next.handle()` ленивый, pre-код выполняется синхронно в момент вызова `intercept`, но ничто после `next.handle()` не выполнится, пока хендлер не зарезолвится. Interceptor также может закоротить цепочку (вернуть свой Observable, не вызывая `next.handle()`, например отдав попадание в кэш) или ловить ошибки ниже оператором `catchError`.

## Exception filters: формируй ошибку, один раз

Когда что-то в цепочке выбрасывает, встроенный слой исключений Nest ловит это и формирует ответ — `HttpException` и его наследники мапятся на свой статус-код, всё остальное становится 500. Кастомный `@Catch()`-filter позволяет владеть этой формой ответа глобально, вместо форматирования ошибок руками в каждом хендлере.

```ts
import { ExceptionFilter, Catch, ArgumentsHost, HttpException } from '@nestjs/common';

@Catch(HttpException)
export class HttpErrorFilter implements ExceptionFilter {
  catch(exception: HttpException, host: ArgumentsHost) {
    const res = host.switchToHttp().getResponse();
    const status = exception.getStatus();
    res.status(status).json({
      statusCode: status,
      message: exception.getResponse(),
      timestamp: new Date().toISOString(),
    });
  }
}
```

`@Catch(HttpException)` сужает filter до типа; `@Catch()` без аргумента ловит всё. Поскольку filter — единственный сток для каждого выброса в pipeline, форматирование ошибок живёт здесь и только здесь — guard, pipe или хендлер просто выбрасывают нужное исключение и доверяют filter его отрисовать.

## Куда идёт каждая задача?

Это то сеньорское суждение, к которому ведёт весь урок. Ошибка в хуке была не в плохом коде — она была в четырёх задачах, живущих не в своём слоте. Сопоставь задачу с этапом lifecycle:

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Этап</th>
        <th class="border border-border px-3 py-2 text-left">Задача</th>
        <th class="border border-border px-3 py-2 text-left">Когда выполняется</th>
        <th class="border border-border px-3 py-2 text-left">Может блокировать запрос?</th>
        <th class="border border-border px-3 py-2 text-left">Видит ответ?</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2"><strong>Guard</strong></td>
        <td class="border border-border px-3 py-2">Authn / authz (да/нет)</td>
        <td class="border border-border px-3 py-2">После middleware, до interceptor + pipe</td>
        <td class="border border-border px-3 py-2">Да — false / throw останавливает</td>
        <td class="border border-border px-3 py-2">Нет</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2"><strong>Pipe</strong></td>
        <td class="border border-border px-3 py-2">Валидация + трансформация входа</td>
        <td class="border border-border px-3 py-2">После guard, прямо перед хендлером</td>
        <td class="border border-border px-3 py-2">Да — выбрасывает на невалидном входе</td>
        <td class="border border-border px-3 py-2">Нет</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2"><strong>Interceptor</strong></td>
        <td class="border border-border px-3 py-2">Логирование, кэш, трансформация, таймаут</td>
        <td class="border border-border px-3 py-2">До И после хендлера</td>
        <td class="border border-border px-3 py-2">Да — может пропустить next.handle()</td>
        <td class="border border-border px-3 py-2">Да — мапит стрим</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2"><strong>Filter</strong></td>
        <td class="border border-border px-3 py-2">Формирует ответ-ошибку</td>
        <td class="border border-border px-3 py-2">Только когда что-то выброшено</td>
        <td class="border border-border px-3 py-2">N/A — обрабатывает выброс</td>
        <td class="border border-border px-3 py-2">Видит ошибку, не успешное тело</td>
      </tr>
    </tbody>
  </table>
</div>

Каждый из четырёх можно привязать на трёх уровнях: **метод** (`@UseGuards()` на хендлере), **контроллер** (тот же декоратор на классе) или **глобально** (`app.useGlobalGuards()` в bootstrap, или токен-провайдер для DI-дружелюбных глобалов). Правило: привязывай широко и перекрывай узко — глобальный filter для дефолтной формы ошибки, глобальный `ValidationPipe`, auth-guard на уровне контроллера и `@Roles()`-guard плюс метаданные на маршрут там, где живёт гранулярное решение.

<Inset kind="why" lang="ru">
Почему для авторизации guard, а не middleware? Middleware выполняется первым и является фреймворк-агностичным кодом Express/Fastify — у него нет доступа к `ExecutionContext` Nest, поэтому он не видит, какой хендлер или контроллер — цель, и не может читать метаданные маршрута вроде `@Roles()`. Guard выполняется на шаг позже, с полным execution context, — именно поэтому авторизация, зависящая от маршрута, принадлежит guard. Middleware — для задач, которым не нужно знать пункт назначения: разбор сырого тела, request ID, CORS.
</Inset>

<TradeoffMatrix
  id="nest-02-guards-interceptors-tradeoff-1"
  lessonSlug="02-guards-interceptors"
  lang="ru"
  prompt="Нужно обеспечить, чтобы эндпоинт могли вызывать только пользователи с ролью 'admin', и иначе вернуть чистый 403. Какой компонент lifecycle владеет этим?"
  options={[
    { name: "Guard (CanActivate), читающий метаданные @Roles() через Reflector и возвращающий true / выбрасывающий ForbiddenException", summary: "Authz — это guard.", correct: true, justification: "Авторизация — это решение да/нет о том, можно ли запросу продолжить, что в точности контракт CanActivate. Guard выполняется после middleware и до pipes/interceptors, так что проверка роли улаживается ещё до того, как вход вообще валидируется, и читает метаданные маршрута через ExecutionContext + Reflector — канонический паттерн @Roles() + RolesGuard. Выброс ForbiddenException даёт чистый 403 (отрисованный твоим filter). Это учебниковый сеньорский ответ." },
    { name: "Interceptor, проверяющий роль до вызова next.handle()", summary: "Не тот инструмент.", justification: "Interceptors технически могут осмотреть запрос и закоротить цепочку, но их назначение — оборачивать хендлер для логирования, кэширования и формирования ответа, а не принимать решения о доступе. Использовать его для authz — значит спрятать гейт безопасности в инструмент, который никто не читает ради безопасности, и он всё равно выполняется после guards. У authz есть выделенный этап; используй его." },
    { name: "Pipe, валидирующий роль пользователя как если бы это был вход", summary: "Pipes валидируют данные.", justification: "Pipes валидируют и трансформируют входящий payload, а не личность вызывающего. Роль — это не вход запроса, это факт авторизации о req.user. Втискивать authz в pipe — злоупотребление его контрактом, и он выполняется после этапа guard, который существует именно для этого." },
    { name: "if-проверка в начале хендлера контроллера, выбрасывающая ForbiddenException", summary: "Баг из хука.", justification: "Это ровно тот запах, с которого начинается урок: сквозная задача, размазанная по бизнес-логике, скопированная по хендлерам, в одной забытой строке от дыры в безопасности. Её также нельзя переиспользовать или привязать глобально. Подними её в guard." },
  ]}
/>

<Quiz
  id="nest-02-guards-interceptors-quiz-1"
  lessonSlug="02-guards-interceptors"
  lang="ru"
  question="В request lifecycle Nest когда guard выполняется относительно pipes и interceptors?"
  choices={[
    { label: "После middleware, но до обоих — interceptors и pipes — так что авторизация улажена ещё до валидации любого входа", correct: true },
    { label: "После pipes, чтобы можно было валидировать разобранное тело до решения о доступе", misconception: "Pipes выполняются после guards, а не до. Guard сначала решает go/no-go; только потом pipe трансформирует вход. Guard никогда не видит валидированное тело — и это by design, потому что authz не должна зависеть от валидности payload." },
    { label: "После хендлера, на пути ответа", misconception: "Это post-позиция interceptor, а не guard. Guards — это входной гейт: они выполняются один раз, до хендлера, и никогда не трогают ответ." },
    { label: "Внутри exception filter, только когда что-то выброшено", misconception: "Filters выполняются только при выбросе и являются отдельным этапом. Guard выполняется на каждый запрос как часть нормальной входной цепочки, до interceptors и pipes." },
  ]}
/>

<Quiz
  id="nest-02-guards-interceptors-quiz-2"
  lessonSlug="02-guards-interceptors"
  lang="ru"
  question="Ты хочешь оборачивать каждый ответ в { data: ... } и логировать, сколько занял каждый хендлер. Какой компонент и как?"
  choices={[
    { label: "Interceptor: прогнать next.handle() через tap (для side-effect тайминга) и map (чтобы переформировать payload)", correct: true },
    { label: "Exception filter, потому что он видит каждый ответ", misconception: "Filters выполняются только при выбросе — они никогда не видят успешное тело ответа. Формирование ответа на счастливом пути принадлежит оператору map в interceptor." },
    { label: "Pipe, мапящий возвращаемое значение хендлера", misconception: "Pipes работают над входом до хендлера и не могут видеть или трансформировать вывод хендлера. Исходящая трансформация — задача interceptor через map на возвращённом Observable." },
    { label: "Guard, возвращающий обёрнутый объект", misconception: "Guard возвращает boolean, чтобы разрешить или отклонить, — он не может трансформировать ответ. Используй interceptor, единственный этап, который выполняется после хендлера и может мапить стрим ответа." },
  ]}
/>

</Explanation>

<KeyTakeaway>Один слот на задачу: authz → guard (CanActivate + Reflector), валидация → pipe, логирование/трансформация → interceptor (RxJS вокруг next.handle()), форма ошибки → filter (@Catch). Привязывай широко, перекрывай узко.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="nest-02-guards-interceptors-retrieval"
  lang="ru"
  questions={[
    { q: "Назови порядок request lifecycle в Nest и объясни, что особенного в том, где сидят interceptors и exception filters.", a: "Порядок такой: middleware -> guards -> interceptors (pre-handler) -> pipes -> handler -> interceptors (post-handler) -> exception filters при выбросе. Guard выполняется после middleware, но до любого interceptor или pipe, так что авторизация решается до того, как вход вообще валидируется. Pipes выполняются последними перед хендлером, трансформируя и валидируя вход. Interceptors особенны тем, что это единственный этап, который выполняется дважды — раз до хендлера и раз после, навешивая операторы на Observable, который возвращает next.handle(); и их исходящий путь разматывается first-in-last-out (route -> controller -> global), в обратном порядке к входному. Exception filters особенны тем, что это catch-all сток сбоку: всё выброшенное на любом этапе — guard, pipe, хендлер или даже зафейлившийся стрим interceptor — приземляется в один и тот же filter, и именно поэтому форматирование ошибок принадлежит туда и никуда больше." },
    { q: "Сопоставь каждую из аутентификации/авторизации, валидации входа, логирования запроса и форматирования ошибок с её верным компонентом lifecycle и назови уровни привязки.", a: "Authn/authz -> guard, реализующий CanActivate: он возвращает boolean (или выбрасывает), читает метаданные маршрута вроде @Roles() через ExecutionContext и Reflector и выполняется до pipes, так что решение о доступе улажено первым. Валидация входа -> pipe (например, ValidationPipe), который трансформирует и валидирует payload прямо перед хендлером. Логирование запроса (а также кэш, трансформация ответа, таймаут) -> interceptor, который обрамляет next.handle() через tap для side-effect'ов и map для переформирования ответа. Форматирование ошибок -> exception filter с @Catch(), единственный сток, который отрисовывает каждое выброшенное исключение в единую форму. Все четыре привязываются на трёх уровнях: метод (декоратор на хендлере), контроллер (декоратор на классе) или глобально (useGlobalX в bootstrap, или токен-провайдер для DI-осведомлённого глобала). Паттерн — привязывай широко и перекрывай узко." },
  ]}
/>

<Recap lang="ru">
Guards, interceptors, pipes и exception filters — это слой сквозных задач Nest, и request lifecycle прикалывает каждый к фиксированному слоту: middleware, затем guard, затем pre-код interceptor, затем pipe, затем хендлер, затем post-код interceptor, с exception filter, ловящим любой выброс по пути. Guard реализует CanActivate, возвращает boolean или выбрасывает и читает метаданные маршрута через ExecutionContext и Reflector — что делает его домом аутентификации и авторизации, улаженной до валидации любого входа. Interceptor оборачивает хендлер с обеих сторон: он прогоняет Observable из next.handle() через RxJS-операторы вроде tap (side-effect'ы вроде логирования или тайминга) и map (формирование ответа) и может закоротить цепочку или ловить ошибки ниже. Pipe валидирует и трансформирует вход прямо перед хендлером. Exception filter, объявленный через @Catch(), — единственный сток для каждого выброса, так что форматирование ошибок живёт там одно. Сеньорское суждение — это размещение: authz — это guard, валидация — это pipe, логирование/трансформация/таймаут — это interceptor, форма ошибки — это filter, — каждый привязываемый на уровне метода, контроллера или глобально, где ты привязываешь широко и перекрываешь узко.
</Recap>
