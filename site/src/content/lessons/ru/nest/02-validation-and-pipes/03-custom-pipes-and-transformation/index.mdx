---
concepts:
- pipe-transform
- argument-metadata
- transform-true
- implicit-conversion
- custom-pipe
deepensInto: []
estMin: 17
lang: ru
lessonType: topic
level: senior
order: 3
prereqs: []
slug: "03-custom-pipes-and-transformation"
sources:
- https://docs.nestjs.com/pipes
- https://docs.nestjs.com/techniques/validation#transform-payload-objects
spiral: []
status: ready
summary: 'Pipe работает на один аргумент прямо перед хендлером и делает две работы: приводит и валидирует. Стадия трансформации — это где «42» становится 42, а сырое тело становится настоящим DTO, и где enableImplicitConversion молча превращает ?active=false в true.'
title: 'Кастомные pipe и стадия трансформации'
track: nest
unit: 02-validation-and-pipes
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
В тикете было написано «отчёт по деактивированным пользователям показывает активных». Запрос был `GET /users?active=false`. Хендлер читал `query.active` — настоящий `boolean`, как обещал DTO, — и фильтровал `where: { isActive: query.active }`. Читаешь код — всё верно. Фильтр говорит `false`, отчёт должен показывать деактивированные аккаунты. А он показывал активные. Полгода назад кто-то включил `enableImplicitConversion: true`, чтобы «перестать писать `@Type` повсюду», и этот один флаг заставил class-transformer привести строку `"false"` к `boolean` по-джаваскриптовому: `Boolean("false")` — непустая строка — это `true`. Поле DTO было типа `boolean`, валидатор прошёл, а значение было ложью. Этот урок — про стадию трансформации: про то, где фреймворк молча меняет твои значения ещё до того, как их увидит хендлер, и где один флаг удобства выворачивает фильтр наизнанку.
</Hook>

<Crux>Pipe работает на один аргумент перед хендлером: приводит и валидирует. transform:true даёт настоящий DTO; implicit conversion врёт.</Crux>

<Explanation>

## Что такое pipe на самом деле

Pipe — это класс, реализующий `PipeTransform<T, R>`: один метод, `transform(value: T, metadata: ArgumentMetadata): R`. Nest запускает его на **одном аргументе хендлера**, прямо перед тем, как хендлер выполнится, — после guards и pre-фазы интерсепторов, но это последнее, что трогает аргумент на входе. У pipe ровно две работы: **трансформация** (вернуть приведённое значение, например `"42"` → `42`) и **валидация** (выбросить, чтобы отклонить запрос, что Nest превратит в `400`). Он либо возвращает (возможно, преобразованное) значение, либо выбрасывает; третьего исхода нет.

```ts
import { PipeTransform, Injectable, ArgumentMetadata, BadRequestException } from '@nestjs/common';

// A hand-written equivalent of ParseIntPipe: this is all a built-in pipe is.
@Injectable()
export class ParseIntPipeManual implements PipeTransform<string, number> {
  transform(value: string, _metadata: ArgumentMetadata): number {
    const n = Number(value);
    if (!Number.isInteger(n)) {
      throw new BadRequestException(`Validation failed: "${value}" is not an integer`);
    }
    return n; // the handler now receives a number, not a string
  }
}
```

Второй аргумент, `metadata: ArgumentMetadata`, — это то, что делает pipe больше, чем тупым конвертером. Он несёт `{ type, metatype, data }`:

- `type` — откуда пришёл этот аргумент: `'body'`, `'query'`, `'param'` или `'custom'`.
- `metatype` — **объявленный класс** аргумента (`CreateUserDto`, `Number`, `String`…). Именно так `ValidationPipe` узнаёт, *против какого DTO* валидировать — он читает metatype с аннотации типа параметра через `reflect-metadata`.
- `data` — строка, переданная в декоратор, например `@Param('id')` → `data === 'id'`.

Встроенные — `ParseIntPipe`, `ParseBoolPipe`, `ParseUUIDPipe`, `ParseArrayPipe`, `DefaultValuePipe` — не более чем реализации `PipeTransform`, которые парсят и выбрасывают `BadRequestException` при провале. Ты привязываешь их прямо у параметра:

```ts
@Get(':id')
findOne(@Param('id', ParseIntPipe) id: number) {
  // id is a number here, or the request already 400'd before we got called
  return this.users.findOne(id);
}
```

## Стадия трансформации: transform:true делает настоящий DTO

По умолчанию `ValidationPipe` валидирует входящий объект и оставляет его **обычным объектом**. Параметр *типизирован* как `CreateUserDto`, но в рантайме это просто `{}` со строковыми полями — `body instanceof CreateUserDto` равно `false`, любые геттеры или методы DTO не существуют, а поле, объявленное `Date`, всё ещё `string`. Аннотация типа — это вымысел времени компиляции, который рантайм никогда не уважал.

Установка `{ transform: true }` это меняет. Pipe запускает `plainToInstance(metatype, value)` из class-transformer и отдаёт хендлеру **настоящий экземпляр** класса DTO:

```ts
import { ValidationPipe } from '@nestjs/common';

app.useGlobalPipes(
  new ValidationPipe({
    transform: true, // plainToInstance(CreateUserDto, body) -> a real DTO instance
    whitelist: true,
  }),
);
```

```ts
export class CreateOrderDto {
  @Type(() => Number) // tells class-transformer to coerce this field to a number
  quantity: number;

  @Type(() => ShippingDto) // nested conversion: the child is built as a ShippingDto too
  shipping: ShippingDto;

  get total(): number {
    return this.quantity * UNIT_PRICE; // only callable because it's a REAL instance
  }
}
```

Только под `transform: true` работают две вещи: `instanceof` теперь истинно (так что значение — подлинный `CreateOrderDto`, а его геттеры/методы вроде `total` вызываемы), и декораторы `@Type()` отрабатывают — что и приводит `quantity` к `number` и пересобирает вложенный `shipping` как настоящий `ShippingDto`. Без `transform: true` `@Type()` бездействует, и твои вложенные объекты остаются обычными.

## Footgun enableImplicitConversion — скрытая ловушка удобства

Писать `@Type(() => Number)` на каждом числовом поле утомительно, поэтому class-transformer предлагает срезку: `transformOptions: { enableImplicitConversion: true }`. С ней class-transformer приводит примитивы по **объявленному TypeScript-типу** каждого поля — `@Type()` не нужен. Поле типа `number` получает `Number(value)`; поле типа `boolean` прогоняется через приведение к булеву; и так далее, по всему приложению, из одного флага.

```ts
new ValidationPipe({
  transform: true,
  transformOptions: { enableImplicitConversion: true }, // one flag, changes coercion everywhere
});

export class UserFilterDto {
  active: boolean; // no @Type needed now... but watch what "false" becomes
  page: number;
}
```

Вот ловушка, в трёх конкретных случаях:

- `?active=false` на поле `boolean` → class-transformer приводит строку `"false"`, а **непустая строка истинна**, так что ты получаешь `true`. Твой фильтр «показать деактивированных пользователей» теперь показывает активных. Это и есть тот production-инцидент дословно.
- `?page=` (пустая строка) на поле `number` → `Number("")` это `0`, а `?page=` с мусором вроде `12abc` → `Number("12abc")` это `NaN`, тихо переданный в твой запрос.
- `?ids=` на поле-массив → приведение угадывает, а «угадай» — ровно то, чего ты не хочешь на границе валидации.

<Inset kind="why" lang="ru">
Почему `?active=false` становится `true`? Implicit conversion не *парсит значение* строки — он приводит по объявленному типу поля, используя собственные правила JavaScript. Для цели `boolean` на непустой строке это истинность, а `"false"` — непустая строка, так что `Boolean("false")` это `true`. Тот же флаг, который услужливо превращает `"42"` в `42` (потому что `Number("42")` случайно верен), превращает `"false"` в `true` (потому что приведение к булеву не имеет ничего общего со словом «false»). Удобство и баг — это один и тот же механизм: приведение-по-типу, без разбора значения. Вот почему сеньорский ход — это `@Transform(({ value }) => value === 'true')` или `@IsBoolean()` после приведения — что-то, что реально смотрит на значение.
</Inset>

Сеньорская позиция: предпочитай **явное, по-полевое** приведение — `@Type(() => Number)` для числовых, `@Transform(...)` для случаев вроде булевых, где важно значение, — а не сплошной `enableImplicitConversion`. Если ты всё же включаешь implicit conversion, сопровождай каждое приводимое поле валидатором (`@IsBoolean()`, `@IsInt()`), чтобы *результат* приведения проверялся, а не принимался на веру. Флаг меняет несколько декораторов на поверхность тихого приведения по всему приложению; это плохой обмен на границе валидации.

## Кастомный pipe: альтернатива на Zod

Кастомный pipe позволяет заменить всю машинерию class-validator. Здесь Zod-схема — единственный источник истины: она выводит TypeScript-тип, *и* валидирует, *и* трансформирует, в одном `safeParse`:

```ts
import { PipeTransform, ArgumentMetadata, BadRequestException } from '@nestjs/common';
import { ZodSchema } from 'zod';

export class ZodValidationPipe implements PipeTransform {
  constructor(private readonly schema: ZodSchema) {}

  transform(value: unknown, _metadata: ArgumentMetadata) {
    const result = this.schema.safeParse(value);
    if (!result.success) {
      throw new BadRequestException(result.error.format()); // reject -> 400
    }
    return result.data; // parsed AND coerced: one source of truth, no @Type, no reflect-metadata
  }
}

// usage: @Body(new ZodValidationPipe(createUserSchema)) — the schema infers the body type
```

Компромисс реален и идёт в обе стороны. Class-validator DI-нативен, идиоматичен для Nest, а его декораторы заодно служат источником для API-доков `@nestjs/swagger` — один набор декораторов питает и валидацию, *и* OpenAPI-схему. Zod даёт выведенные типы (нет дублирования декораторов/`reflect-metadata`) и куда лучшую композируемость (`.refine`, `.transform`, юнионы), но pipe ты проводишь сам и теряешь декораторную интеграцию со Swagger. Выбирай по ограничению: Swagger-first и тяжёлый DI → class-validator; вывод-типов-first и композируемость → Zod-pipe.

<TradeoffMatrix
  id="nest-02-validation-and-pipes-03-custom-pipes-and-transformation-tradeoff-1"
  lessonSlug="03-custom-pipes-and-transformation"
  lang="ru"
  prompt="Тебе нужно валидировать И приводить сложное вложенное тело запроса (числа, булевы, вложенный объект адреса) на одном эндпоинте. Какой подход устоит?"
  options={[
    { name: "DTO на class-validator + ValidationPipe({ transform: true }) с явным @Type(() => Number) / @Transform на каждом поле", summary: "Явное приведение, осознающее значение.", correct: true, justification: "transform: true запускает plainToInstance, так что хендлер получает настоящий экземпляр DTO — instanceof работает, геттеры работают, и декораторы @Type отрабатывают, приводя числа и пересобирая вложенный адрес как настоящий экземпляр. Приведение по-полевое (@Type для чисел, @Transform(({value}) => value === 'true') для булевых) оставляет его осознающим значение и видимым на каждом поле, так что ?active=false парсится верно, а не становится истинным-true. Это сеньорский дефолт: явно, провалидировано, дружит со Swagger." },
    { name: "transform: true с transformOptions.enableImplicitConversion: true и без по-полевых декораторов", summary: "Тихое неверное приведение по всему приложению.", justification: "Это footgun. enableImplicitConversion приводит по объявленному типу правилами JS, так что булево поле читает ?active=false как Boolean('false') === true, пустой числовой query становится 0/NaN, и делает это по всему приложению из одного флага без разбора значения. Удобно, пока фильтр молча не инвертируется. Если использовать вообще — обязательно сопровождать @IsBoolean()/@IsInt() для проверки результата приведения, и тогда декораторы ты всё равно написал." },
    { name: "Кастомный ZodValidationPipe, оборачивающий Zod-схему", summary: "Сильно, но меняет Swagger + DI.", justification: "По-настоящему хорошо: одна схема — единственный источник истины, она выводит TS-тип и парсит+приводит в одном safeParse без @Type и reflect-metadata, а .refine/.transform у Zod чисто закрывают вложенные/булевы случаи. Цена — pipe проводишь сам и теряешь декораторные доки @nestjs/swagger и DI-нативное ощущение. Верно, когда вывод типов и композируемость перевешивают Swagger; не дефолт, когда проект Swagger- и декоратор-центричен." },
    { name: "Парсить и приводить поля руками внутри контроллера", summary: "Нет границы, разбросано + без типов.", justification: "Затащить Number(body.quantity) и body.active === 'true' в хендлер работает для одного поля, но выбрасывает весь смысл стадии pipe: приведение теперь разбросано по хендлерам, без типов, без тестов и продублировано везде, где появляется та же форма. Нет единой границы валидации, нет настоящего экземпляра DTO, нет Swagger. Это ровно то, что pipe существуют, чтобы удалить." },
  ]}
/>

<Quiz
  id="nest-02-validation-and-pipes-03-custom-pipes-and-transformation-quiz-1"
  lessonSlug="03-custom-pipes-and-transformation"
  lang="ru"
  question="Где в жизненном цикле запроса работает pipe и что несёт его ArgumentMetadata?"
  choices={[
    { label: "На одном аргументе хендлера прямо перед хендлером (после guards); metadata несёт type ('body'|'query'|'param'|'custom'), metatype (объявленный класс аргумента) и data (строку декоратора)", correct: true },
    { label: "Глобально на весь запрос до guards; metadata несёт полные объекты request и response", misconception: "Pipe работают ПОСЛЕ guards (и pre-фазы интерсепторов) и оперируют по-аргументно, а не на весь запрос как единое целое. ArgumentMetadata мала и привязана к аргументу: { type, metatype, data } — а не req/res. metatype — это ровно то, как ValidationPipe узнаёт, против какого DTO валидировать." },
    { label: "После возврата хендлера, чтобы трансформировать ответ; metadata несёт тип возвращаемого значения", misconception: "Трансформация ИСХОДЯЩЕГО ответа — это работа интерсептора, не pipe. Pipe работает на входе, на аргументе хендлера, прямо перед выполнением хендлера — он формирует то, что хендлер получает, а не то, что возвращает." },
    { label: "Один раз на контроллер при старте; metadata несёт таблицу маршрутов контроллера", misconception: "Pipe работают на запрос, на аргумент — не один раз при старте. ArgumentMetadata описывает один аргумент одного входящего запроса ({ type, metatype, data }), а не маршруты контроллера." },
  ]}
/>

<Quiz
  id="nest-02-validation-and-pipes-03-custom-pipes-and-transformation-quiz-2"
  lessonSlug="03-custom-pipes-and-transformation"
  lang="ru"
  question="С ValidationPipe({ transform: true, transformOptions: { enableImplicitConversion: true } }) и полем DTO `active: boolean`, что даст запрос `?active=false`?"
  choices={[
    { label: "active === true — implicit conversion приводит непустую строку 'false' по типу boolean, а Boolean('false') это true", correct: true },
    { label: "active === false — class-transformer парсит буквальное слово 'false' в булево false", misconception: "Implicit conversion НЕ парсит слово; он приводит по объявленному типу правилами JS. Для цели boolean непустая строка истинна, так что 'false' становится true. Это ровно баг инвертированного фильтра. Чтобы реально распарсить слово, нужен @Transform(({ value }) => value === 'true')." },
    { label: "400 BadRequest — строка 'false' проваливает булеву валидацию до приведения", misconception: "Здесь нет провала валидации: enableImplicitConversion приводит молча и успешно (к true), и без явного @IsBoolean() ничто это не отклоняет. Опасность именно в том, что 400 НЕ происходит — возвращается неверное значение, которое проходит." },
    { label: "active === undefined — булевы нельзя вывести из query-строк", misconception: "Query-строка точно даёт значение; implicit conversion отдаёт его как булево через приведение (здесь true), а не undefined. Поле заполнено — неверно. undefined было бы хоть видимо неверным; true молча инвертирует фильтр." },
  ]}
/>

</Explanation>

<FlowDiagram
  label="Стадия трансформации одного аргумента хендлера под ValidationPipe с transform: true. Запрос приходит, неся обычный объект (сырые строки query/body). Pipe читает metatype из ArgumentMetadata — объявленный класс DTO аргумента — и использует его, чтобы запустить plainToInstance из class-transformer, порождая экземпляр этого класса. Затем идут валидация и приведение: декораторы @Type приводят числа и пересобирают вложенные объекты, @Transform обрабатывает случаи, осознающие значение, вроде булевых, а валидаторы либо проходят, либо выбрасывают BadRequestException (400, завершающий запрос). Если всё прошло, хендлеру отдаётся настоящий типизированный экземпляр DTO — instanceof истинно, геттеры вызываемы. Срезка enableImplicitConversion приводила бы по объявленному типу внутри этой стадии без по-полевых декораторов, и вот где ?active=false молча становится true."
  hue="var(--d-systems)"
  perRow={3}
  nodes={[
    { id: "plain", label: "обычный объект", sub: "сырые строки" },
    { id: "meta", label: "поиск metatype", sub: "ArgumentMetadata" },
    { id: "instance", label: "plainToInstance", sub: "сборка DTO" },
    { id: "coerce", label: "валидация + приведение", sub: "@Type / @Transform" },
    { id: "dto", label: "типизированный DTO", sub: "instanceof true" },
    { id: "handler", label: "хендлер", sub: "получает DTO" },
  ]}
  edges={[
    { from: "plain", to: "meta", label: "читает класс" },
    { from: "meta", to: "instance", label: "plainToInstance" },
    { from: "instance", to: "coerce", label: "@Type отрабатывает" },
    { from: "coerce", to: "dto", label: "прошло" },
    { from: "dto", to: "handler", label: "инжект аргумента" },
  ]}
  caption="Стадия трансформации превращает мешок строк в настоящий экземпляр DTO: поиск metatype -> plainToInstance -> по-полевое приведение/валидация -> типизированный экземпляр хендлеру. enableImplicitConversion приводит по типу внутри этой стадии, и вот где ?active=false становится true."
/>

<KeyTakeaway>Pipe работает на один аргумент перед хендлером: привести или 400. transform:true даёт настоящий экземпляр DTO. enableImplicitConversion приводит по типу, и ?active=false становится true — бери явные @Type/@Transform.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="nest-02-validation-and-pipes-03-custom-pipes-and-transformation-retrieval"
  lang="ru"
  questions={[
    { q: "Объясни, что такое pipe, где он работает, его две работы и что даёт ArgumentMetadata.", a: "Pipe — это класс, реализующий PipeTransform<T, R> с единственным методом transform(value, metadata: ArgumentMetadata): R. Nest запускает его на одном аргументе хендлера, прямо перед выполнением хендлера — после guards и pre-фазы интерсепторов — так что это последнее, что трогает аргумент на входе. У него ровно две работы: трансформация (вернуть приведённое значение, например '42' -> 42) и валидация (выбросить, чтобы отклонить, что Nest превращает в 400). Он возвращает значение или выбрасывает; третьего исхода нет. Встроенные (ParseIntPipe, ParseBoolPipe, ParseUUIDPipe, ParseArrayPipe, DefaultValuePipe) — это просто реализации PipeTransform, которые парсят и выбрасывают BadRequestException при провале. ArgumentMetadata несёт { type, metatype, data }: type — откуда пришёл аргумент ('body'|'query'|'param'|'custom'); metatype — объявленный класс аргумента, через который ValidationPipe узнаёт, какой DTO валидировать (читает его через reflect-metadata); data — строка, переданная в декоратор, например @Param('id') даёт data === 'id'." },
    { q: "Сопоставь transform:true с enableImplicitConversion и скажи, почему ?active=false становится true — затем дай сеньорский фикс.", a: "transform: true заставляет ValidationPipe запустить plainToInstance(metatype, value), так что хендлер получает НАСТОЯЩИЙ экземпляр DTO: instanceof истинно, геттеры/методы вызываемы, и декораторы @Type() отрабатывают — приводя числовые поля и пересобирая вложенные объекты как настоящие экземпляры. Без него «DTO» — обычный объект, чей класс — ложь, а чьи поля Date/number всё ещё строки. enableImplicitConversion — это transformOptions: { enableImplicitConversion: true } — один флаг на всё приложение, который заставляет class-transformer приводить примитивы по ОБЪЯВЛЕННОМУ типу поля правилами JS, без нужды в @Type. Это footgun: он не парсит значение, он приводит по типу. Для булева поля непустая строка 'false' истинна, так что Boolean('false') это true — запрос ?active=false молча даёт active === true и инвертирует фильтр. Тот же флаг превращает ?page= в 0, а 12abc в NaN. Сеньорский фикс — явное по-полевое приведение: @Type(() => Number) для числовых и @Transform(({ value }) => value === 'true') для булевых (осознающее значение), а не сплошной implicit conversion; и если ты его включаешь, сопровождай каждое поле @IsBoolean()/@IsInt(), чтобы результат приведения валидировался, а не принимался на веру." },
  ]}
/>

<Recap lang="ru">
Pipe — это класс, реализующий PipeTransform&lt;T, R> — один метод transform(value, metadata), который Nest запускает на одном аргументе хендлера прямо перед хендлером (после guards), с двумя работами: привести значение или выбросить, чтобы отклонить (400). Встроенные Parse*-pipe — это ровно оно. ArgumentMetadata несёт type ('body'|'query'|'param'|'custom'), metatype (объявленный класс аргумента, через который ValidationPipe выбирает DTO) и data (строку декоратора). По умолчанию ValidationPipe оставляет тело обычным объектом, чей объявленный класс — вымысел: instanceof равно false, а поля Date/number остаются строками; установка transform: true запускает plainToInstance, так что хендлер получает настоящий экземпляр DTO с работающими геттерами и приведением @Type(), включая вложенные объекты. enableImplicitConversion — опасная срезка: один флаг заставляет class-transformer приводить примитивы по объявленному типу правилами JS без @Type, так что ?active=false на булевом становится Boolean('false') === true и инвертирует фильтр, ?page= становится 0/NaN, всё молча и по всему приложению. Сеньорская позиция — явное по-полевое приведение (@Type для чисел, @Transform для осознающих значение булевых) либо валидация результата приведения через @IsBoolean()/@IsInt(), если implicit включён. Кастомный pipe вроде ZodValidationPipe — альтернатива всему стеку class-validator: Zod-схема выводит TS-тип и парсит+приводит в одном safeParse, меняя декораторный Swagger и DI-нативные repo на выведенные типы и композируемость — выбирай по ограничению. Теперь, когда булевой query-параметр ведёт себя противоположно ожидаемому, первый твой вопрос — включён ли enableImplicitConversion и есть ли @Transform, распознающий буквальное слово «false».
</Recap>
