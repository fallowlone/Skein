---
concepts:
- dynamic-modules
- forroot
- forrootasync
- forfeature
- configurable-module-builder
deepensInto: []
estMin: 17
lang: ru
lessonType: topic
level: senior
order: 2
prereqs: []
slug: "02-dynamic-modules"
sources:
- https://docs.nestjs.com/fundamentals/dynamic-modules
- https://docs.nestjs.com/fundamentals/async-providers
spiral: []
status: ready
summary: 'DynamicModule возвращает своё определение в рантайме. forRoot конфигурирует один раз, forRootAsync берёт опции из DI (ConfigService), forFeature регистрирует на каждого потребителя. Свой модуль строится через options-токен; ConfigurableModuleBuilder генерирует бойлерплейт.'
title: 'Динамические модули: forRoot, forRootAsync, forFeature'
track: nest
unit: "03-config-and-modules"
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import SequenceDiagram from "~/components/diagram/SequenceDiagram.astro";
import Infographic from "~/components/diagram/Infographic.astro";
import optionsSource from "./infographics/options-source.svg?raw";

<Hook>
Ты выносишь код загрузки в S3 в общий `StorageModule`, чтобы три сервиса могли его переиспользовать. Первый потребитель хардкодит бакет и регион, выкатывается, работает. Второму нужен разный бакет на окружение — поэтому его значения лежат в `ConfigService`, который наполняется из `.env` на старте. И тут ты застрял: модулю нужно имя бакета, чтобы сконструировать provider с `S3Client`, но имя бакета живёт в provider, которого ещё не существует, пока модули не связаны. Нельзя сделать `@Inject(ConfigService)` в декоратор `@Module()` — это аннотация времени компиляции, а конфиг — значение рантайма. Эта проблема курицы и яйца ровно та, ради которой существует `forRootAsync`, и `TypeOrmModule.forRootAsync`, `JwtModule.registerAsync` и `ConfigModule.forRoot` — это одни и те же три строки под капотом.
</Hook>

<Crux>Динамический модуль возвращает своё определение в рантайме, поэтому forRootAsync строит providers из опций, вытянутых из DI.</Crux>

<Explanation>

## DynamicModule — это модуль, вычисленный в рантайме

Когда ты импортируешь `TypeOrmModule.forRoot(dbConfig)` или `JwtModule.register({ secret })`, ты вызываешь не магию NestJS — ты вызываешь статический метод, который возвращает простой объект. Понимание этого объекта — то, что позволяет тебе строить переиспользуемые модули вместо копирования бойлерплейта.

Обычный `@Module({...})` статичен: его список providers зафиксирован в момент написания. **Динамический модуль** — это модуль, чьё определение *возвращается вызовом метода* в рантайме. Контракт — простой объект, интерфейс `DynamicModule` (буквально «описание модуля, собранное в рантайме»), с такими полями:

```ts
import { DynamicModule } from '@nestjs/common';

interface DynamicModule {
  module: Type<any>;        // обязательно: класс-хост
  imports?: any[];          // модули, нужные этому динамическому модулю
  providers?: Provider[];   // providers для создания, возможно из опций
  exports?: any[];          // что потребители могут инжектить
  global?: boolean;         // зарегистрировать один раз, доступно везде
  controllers?: Type<any>[];
}
```

Этот один рантайм-объект — весь механизм за каждой конфигурируемой библиотекой, которую ты импортируешь. `TypeOrmModule.forRoot(dbConfig)`, `JwtModule.register({ secret })`, `ConfigModule.forRoot()` — каждый из них статический метод, возвращающий эту форму и запекающий твои опции в массив `providers` ещё до того, как Nest их инстанцирует. Соглашение — это *имя* метода: `forRoot` для глобальной конфигурации один-раз-на-приложение, `register` для конфигурации на каждый импорт, `forFeature` для расширений на каждого потребителя.

Базовый паттерн, чтобы построить такой модуль самому: определить **options-токен**, отдать *значение* опций из статического метода, а внутренним providers сделать `@Inject` этого токена. Токен — это шов: он позволяет provider зависеть от опций, которых не существует, пока потребитель не вызовет `forRoot`.

```ts
// storage.constants.ts
export const STORAGE_OPTIONS = 'STORAGE_OPTIONS';

export interface StorageOptions {
  bucket: string;
  region: string;
}
```

```ts
// storage.service.ts
import { Injectable, Inject } from '@nestjs/common';
import { STORAGE_OPTIONS, StorageOptions } from './storage.constants';

@Injectable()
export class StorageService {
  constructor(@Inject(STORAGE_OPTIONS) private readonly opts: StorageOptions) {}
  // this.opts.bucket / this.opts.region теперь доступны
}
```

## forRoot vs forRootAsync: когда опции приходят из DI

`forRoot(options)` **синхронный**: вызывающий передаёт литеральный объект опций, статический метод оборачивает его в provider с `useValue`, готово. Используй его для конфигурируемых-один-раз синглтонов на всё приложение — соединение с БД, HTTP-клиент, секрет авторизации — обычно с `global: true`, чтобы любой модуль мог инжектить exports без повторного импорта.

```ts
// storage.module.ts — синхронный forRoot
import { Module, DynamicModule } from '@nestjs/common';
import { STORAGE_OPTIONS, StorageOptions } from './storage.constants';
import { StorageService } from './storage.service';

@Module({})
export class StorageModule {
  static forRoot(options: StorageOptions): DynamicModule {
    return {
      module: StorageModule,
      global: true,
      providers: [
        { provide: STORAGE_OPTIONS, useValue: options }, // опции запечены как значение
        StorageService,
      ],
      exports: [StorageService],
    };
  }
}
```

`forRootAsync({ imports, useFactory, inject })` — для случая, когда **сами опции должны быть произведены другим provider**. Нельзя передать литеральный объект, потому что значения живут в `ConfigService`, менеджере секретов или другом асинхронном источнике. Вместо `useValue` options-токен отдаётся **фабрикой**, которую Nest разрешает через DI, — так и разрывается курица-и-яйцо: фабрика выполняется *после* того, как её зависимости из `inject` сконструированы.

<Infographic
  svg={optionsSource}
  label="Сравнение forRoot и forRootAsync лицом к лицу. forRoot: опции — литеральный объект, привязка через useValue, разрешается синхронно, для статических констант, известных заранее. forRootAsync: опции приходят из другого provider, привязка через useFactory плюс inject, разрешается асинхронно и отложенно, для значений из ConfigService и .env."
  caption="Вся разница в том, откуда берутся опции: forRoot запекает литеральное значение, forRootAsync откладывает на фабрику, чтобы опции можно было вытянуть из provider вроде ConfigService."
  hue="var(--d-infra)"
/>

```ts
// storage.module.ts — асинхронный forRootAsync
import { Module, DynamicModule, Provider } from '@nestjs/common';
import { STORAGE_OPTIONS, StorageOptions } from './storage.constants';
import { StorageService } from './storage.service';

interface StorageAsyncOptions {
  imports?: any[];
  inject?: any[];
  useFactory: (...args: any[]) => Promise<StorageOptions> | StorageOptions;
}

@Module({})
export class StorageModule {
  static forRootAsync(options: StorageAsyncOptions): DynamicModule {
    const optionsProvider: Provider = {
      provide: STORAGE_OPTIONS,
      useFactory: options.useFactory, // выполнится ПОСЛЕ готовности inject-зависимостей
      inject: options.inject || [],
    };
    return {
      module: StorageModule,
      global: true,
      imports: options.imports || [],   // например ConfigModule, чтобы ConfigService был инжектируем
      providers: [optionsProvider, StorageService],
      exports: [StorageService],
    };
  }
}
```

```ts
// app.module.ts — связываем с ConfigService
@Module({
  imports: [
    ConfigModule.forRoot(),
    StorageModule.forRootAsync({
      imports: [ConfigModule],
      inject: [ConfigService],
      useFactory: (config: ConfigService) => ({
        bucket: config.getOrThrow('S3_BUCKET'),
        region: config.getOrThrow('AWS_REGION'),
      }),
    }),
  ],
})
export class AppModule {}
```

<SequenceDiagram
  label="Как StorageModule.forRootAsync разрешается на старте: AppModule импортирует StorageModule.forRootAsync с imports ConfigModule, inject ConfigService и useFactory; Nest сначала конструирует ConfigService из импортированного ConfigModule, затем вызывает useFactory с этим ConfigService, чтобы получить значение StorageOptions, привязывает это значение к токену STORAGE_OPTIONS, затем конструирует StorageService, который инжектит STORAGE_OPTIONS, и наконец экспортирует StorageService для потребителей."
  hue="var(--d-systems)"
  actors={["AppModule", "StorageModule", "DI Container"]}
  messages={[
    { from: "AppModule", to: "StorageModule", label: "1. forRootAsync({ imports, inject, useFactory })" },
    { from: "StorageModule", to: "DI Container", label: "2. returns DynamicModule (token provider via factory)" },
    { from: "DI Container", to: "DI Container", label: "3. construct ConfigService (from imports)" },
    { from: "DI Container", to: "DI Container", label: "4. useFactory(ConfigService) -> StorageOptions" },
    { from: "DI Container", to: "DI Container", label: "5. bind value to STORAGE_OPTIONS token" },
    { from: "DI Container", to: "AppModule", label: "6. build StorageService(@Inject token), export it" },
  ]}
  caption="Фабрика — это шов: она выполняется только после того, как её inject-зависимости существуют, поэтому опции могут прийти из ConfigService, хотя конфиг — рантайм-значение, которое декоратор @Module() никогда не увидит."
/>

## forFeature: регистрация на каждого потребителя

`forRoot`/`forRootAsync` конфигурируют модуль *один раз* на всё приложение — соединение, синглтоны, глобальное состояние. `forFeature(...)` — второй ярус: он регистрирует нечто, **ограниченное только импортирующим модулем**, поверх существующей корневой конфигурации. Учебниковый пример — TypeORM: `TypeOrmModule.forRoot(dbConfig)` открывает одно соединение в корневом модуле; `TypeOrmModule.forFeature([User])` затем регистрирует repository `User` *для injector именно этого feature-модуля*, так что `UsersModule` может инжектить `Repository&lt;User>`, а `OrdersModule` — `Repository&lt;Order>`, и ни один не видит репозитории другого.

```ts
@Module({
  imports: [TypeOrmModule.forFeature([User])], // repo User, ограничен UsersModule
  providers: [UsersService],
})
export class UsersModule {}
```

Разделение — это сеньорское суждение: **конфигурировать-один-раз-глобально → forRoot; регистрировать-на-фичу → forFeature.** Корневой вызов, который ты случайно сделал дважды, создаёт дублирующиеся синглтоны (два пула соединений, два клиента) — конфигурация соединения должна импортироваться через `forRoot` ровно один раз. Вызов `forFeature`, наоборот, *должен* появляться во многих модулях, каждый регистрирует свой кусочек.

## ConfigurableModuleBuilder: перестань писать бойлерплейт

Писать руками и `forRoot`, и `forRootAsync` (плюс варианты `useClass`/`useExisting`/`useFactory` для async) — повторяемо и легко ошибиться по мелочи. Начиная с Nest 9, `ConfigurableModuleBuilder` генерирует это всё. Ты даёшь ему интерфейс опций; он возвращает `ConfigurableModuleClass` для наследования и `MODULE_OPTIONS_TOKEN` для инжекта.

```ts
// storage.module-definition.ts
import { ConfigurableModuleBuilder } from '@nestjs/common';
import { StorageOptions } from './storage.constants';

export const { ConfigurableModuleClass, MODULE_OPTIONS_TOKEN } =
  new ConfigurableModuleBuilder<StorageOptions>()
    .setClassMethodName('forRoot') // -> генерирует forRoot И forRootAsync
    .build();
```

```ts
// storage.module.ts — теперь тривиально маленький
import { Module } from '@nestjs/common';
import { ConfigurableModuleClass, MODULE_OPTIONS_TOKEN } from './storage.module-definition';
import { StorageService } from './storage.service';

@Module({
  providers: [StorageService],
  exports: [StorageService],
})
export class StorageModule extends ConfigurableModuleClass {}
```

`setClassMethodName('forRoot')` делает сгенерированные методы `forRoot`/`forRootAsync` (по умолчанию — `register`/`registerAsync`); `.setExtras()` позволяет добавить поля вроде `isGlobal`, не входящие в объект опций. `MODULE_OPTIONS_TOKEN` — тот же самый options-токен из ручного паттерна: твой `StorageService` инжектит его через `@Inject(MODULE_OPTIONS_TOKEN)`, а builder связывает с ним и синхронный, и асинхронный пути.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Метод</th>
        <th class="border border-border px-3 py-2 text-left">Источник опций</th>
        <th class="border border-border px-3 py-2 text-left">Sync / async</th>
        <th class="border border-border px-3 py-2 text-left">Область</th>
        <th class="border border-border px-3 py-2 text-left">Типичное применение</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2"><strong>forRoot</strong></td>
        <td class="border border-border px-3 py-2">Литеральный объект от вызывающего</td>
        <td class="border border-border px-3 py-2">Синхронно (useValue)</td>
        <td class="border border-border px-3 py-2">Один раз, на всё приложение (часто global)</td>
        <td class="border border-border px-3 py-2">Синглтон соединения / клиента / секрета</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2"><strong>forRootAsync</strong></td>
        <td class="border border-border px-3 py-2">Provider через useFactory + inject</td>
        <td class="border border-border px-3 py-2">Асинхронно (фабрика)</td>
        <td class="border border-border px-3 py-2">Один раз, на всё приложение (часто global)</td>
        <td class="border border-border px-3 py-2">Опции из ConfigService / async</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2"><strong>forFeature</strong></td>
        <td class="border border-border px-3 py-2">Аргументы на импорт (например entities)</td>
        <td class="border border-border px-3 py-2">Синхронно</td>
        <td class="border border-border px-3 py-2">На каждый импортирующий модуль</td>
        <td class="border border-border px-3 py-2">Репозитории / providers области фичи</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2"><strong>обычный @Module</strong></td>
        <td class="border border-border px-3 py-2">Нет — зафиксирован при написании</td>
        <td class="border border-border px-3 py-2">N/A</td>
        <td class="border border-border px-3 py-2">Везде, где импортирован</td>
        <td class="border border-border px-3 py-2">Внешняя конфигурация не нужна</td>
      </tr>
    </tbody>
  </table>
</div>

<Inset kind="why" lang="ru">
Почему нельзя просто инжектить ConfigService в декоратор @Module() и пропустить forRootAsync? Потому что метаданные декоратора вычисляются во время определения класса — когда файл впервые загружается — задолго до того, как DI-контейнер что-либо сконструирует. `ConfigService` — рантайм-инстанс, который существует только после того, как `ConfigModule` инициализируется из `.env`. Фабрика в forRootAsync — это механизм отложенности: это функция, а не значение, поэтому Nest может придержать её, пока не построены зависимости из `inject`, и затем вызвать. Этот один слой косвенности — значение становится фабрикой — и есть вся причина, по которой существует асинхронная регистрация.
</Inset>

<TradeoffMatrix
  id="nest-03-config-and-modules-02-dynamic-modules-tradeoff-1"
  lessonSlug="02-dynamic-modules"
  lang="ru"
  prompt="Ты строишь переиспользуемый StorageModule. Бакет и регион различаются по окружениям и читаются из .env через ConfigService на старте. Как потребители должны его конфигурировать?"
  options={[
    { name: "forRootAsync({ imports: [ConfigModule], inject: [ConfigService], useFactory }), возвращающий опции", summary: "Опции из DI.", correct: true, justification: "Опции неизвестны во время написания — они живут в ConfigService, provider, который существует только после инициализации ConfigModule из .env. forRootAsync отдаёт options-токен через useFactory, который Nest разрешает через DI, так что фабрика выполняется после конструирования ConfigService и может прочитать реальные значения на окружение. Это каноническое решение проблемы курицы и яйца, когда конфиг нужен до построения модуля, и именно так работают TypeOrmModule.forRootAsync и JwtModule.registerAsync." },
    { name: "forRoot({ bucket, region }) с литеральными значениями, захардкоженными на месте вызова", summary: "Неверно для env-конфига.", justification: "forRoot синхронный: он оборачивает литеральный объект в provider с useValue. Это правильно, когда опции — статические константы, но здесь значения должны прийти из ConfigService в рантайме. Хардкод их на месте вызова дублирует конфиг, ломает деплои на окружение и сводит на нет смысл ConfigService. Используй forRootAsync, когда опции приходят из другого provider." },
    { name: "Обычный @Module с бакетом, прочитанным из process.env внутри provider", summary: "Обходит слой конфига.", justification: "Лезть напрямую в process.env внутри provider — значит пропустить валидацию, типизацию и единый источник истины ConfigService, и сделать модуль невозможным конфигурировать по-разному на разных потребителей. Переиспользуемая библиотека обязана принимать опции через метод регистрации, а не протаскивать их из глобального окружения." },
    { name: "Инжектить ConfigService прямо в метаданные декоратора @Module()", summary: "Невозможно.", justification: "Декоратор @Module() вычисляется во время определения класса, до того как DI-контейнер сконструировал хоть один provider. ConfigService — рантайм-инстанс, его ещё не существует. Нельзя инжектить рантайм-значение в метаданные времени компиляции; отложенность, которую даёт фабрика forRootAsync, — это ровно то, что заставляет это работать." },
  ]}
/>

<Quiz
  id="nest-03-config-and-modules-02-dynamic-modules-quiz-1"
  lessonSlug="02-dynamic-modules"
  lang="ru"
  question="Почему forRootAsync использует useFactory с массивом inject, а не простой объект опций?"
  choices={[
    { label: "Потому что опции должны быть произведены другим provider (например ConfigService), который существует только в рантайме — фабрика откладывает создание опций до конструирования inject-зависимостей", correct: true },
    { label: "Потому что асинхронные фабрики заставляют Nest стартовать приложение быстрее", misconception: "Дело не в производительности. forRootAsync существует по причине порядка зависимостей: опции зависят от provider (вроде ConfigService), которого не существует, пока не отработает DI. Фабрика — механизм отложенности, а не оптимизация." },
    { label: "Потому что forRoot не может вернуть DynamicModule, а forRootAsync может", misconception: "Оба возвращают DynamicModule — это общий контракт. Разница только в том, как отдаётся options-токен: forRoot использует useValue с литеральным объектом, forRootAsync — useFactory + inject, чтобы опции можно было вытянуть из DI." },
    { label: "Потому что массив inject полностью заменяет нужду в options-токене", misconception: "Options-токен всё ещё на месте — forRootAsync отдаёт тот же токен через useFactory вместо useValue. Массив inject перечисляет зависимости фабрики, он не убирает токен, который инжектят внутренние providers." },
  ]}
/>

<Quiz
  id="nest-03-config-and-modules-02-dynamic-modules-quiz-2"
  lessonSlug="02-dynamic-modules"
  lang="ru"
  question="TypeOrmModule.forRoot(dbConfig) стоит в твоём корневом модуле. Куда принадлежит TypeOrmModule.forFeature([User]) и почему?"
  choices={[
    { label: "В каждый feature-модуль, которому нужен repository этой сущности — forFeature регистрирует repository в области injector импортирующего модуля", correct: true },
    { label: "Тоже в корневой модуль, один раз, рядом с forRoot", misconception: "forFeature по дизайну на каждого потребителя: он регистрирует repository в injector импортирующего модуля, чтобы тот мог инжектить Repository<User>. Класть все сущности в корень — сводит на нет ограничение области и заставляет несвязанные модули делить репозитории. forRoot — вызов один-раз-на-приложение; forFeature повторяется на каждую фичу." },
    { label: "Никуда — forRoot уже регистрирует все репозитории глобально", misconception: "forRoot открывает соединение и конфигурирует data source один раз, но не регистрирует репозитории на сущность. forFeature — это вызов, который делает Repository<User> инжектируемым в данном модуле; без него этот модуль не может инжектить repo." },
    { label: "Только один раз где угодно, потому что вызов дважды создаёт дублирующиеся соединения", misconception: "Это forRoot (конфиг соединения) должен выполниться ровно один раз — вызов его дважды создал бы дублирующиеся пулы. forFeature не создаёт соединения; он только регистрирует репозитории, поэтому он и должен появляться во многих feature-модулях." },
  ]}
/>

</Explanation>

<KeyTakeaway>forRoot запекает литеральные опции; forRootAsync отдаёт их через useFactory+inject, чтобы они пришли из ConfigService; forFeature регистрирует на импортёра. ConfigurableModuleBuilder генерирует бойлерплейт.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="nest-02-dynamic-modules-retrieval"
  lang="ru"
  questions={[
    { q: "Что такое DynamicModule, какие поля он возвращает и как паттерн options-токена позволяет внутреннему provider зависеть от конфигурации, переданной вызывающим?", a: "DynamicModule — это модуль, чьё определение возвращается вызовом статического метода в рантайме, а не зафиксировано в декораторе @Module(). Возвращаемый объект имеет: module (обязательно, класс-хост) и опционально imports, providers, exports, global и controllers. Механизм за конфигурируемыми библиотеками — паттерн options-токена: ты определяешь токен (например STORAGE_OPTIONS или сгенерированный MODULE_OPTIONS_TOKEN), статический метод отдаёт значение опций, привязанное к этому токену, а внутренние providers делают @Inject(token), чтобы его прочитать. Токен — это шов между конфигурацией потребителя и providers модуля — он позволяет provider вроде StorageService зависеть от опций, которых не существует, пока потребитель не вызовет forRoot/forRootAsync. Именно так работают TypeOrmModule.forRoot, JwtModule.register и ConfigModule.forRoot: каждый возвращает форму DynamicModule с опциями, запечёнными в массив providers." },
    { q: "Различи forRoot, forRootAsync и forFeature — когда каждый верен и когда async обязателен, а не опционален?", a: "forRoot(options) синхронный: вызывающий передаёт литеральный объект опций, который метод оборачивает в provider с useValue. Используй для конфигурируемых-один-раз синглтонов на всё приложение (соединение, клиент, секрет), обычно с global: true. forRootAsync({ imports, useFactory, inject }) отдаёт тот же options-токен через useFactory, который Nest разрешает через DI — он обязателен (а не опционален) всякий раз, когда опции должны быть произведены другим provider, например ConfigService, читающим .env, потому что нельзя инжектить рантайм-значение в метаданные времени компиляции декоратора @Module(); фабрика откладывает создание опций до конструирования inject-зависимостей, решая курицу-и-яйцо. forFeature(...) — на каждого потребителя: регистрирует нечто, ограниченное только импортирующим модулем — например TypeOrmModule.forFeature([User]) регистрирует repository User для injector этого feature-модуля. Сеньорское суждение: конфигурировать-один-раз-глобально — это forRoot/forRootAsync (и вызов корневого метода дважды создаёт дублирующиеся синглтоны), регистрировать-на-фичу — это forFeature (должен повторяться по модулям). ConfigurableModuleBuilder (Nest 9+) генерирует методы forRoot/forRootAsync и MODULE_OPTIONS_TOKEN через setClassMethodName('forRoot').build()." },
  ]}
/>

<Recap lang="ru">
Динамический модуль — это модуль, вычисленный в рантайме: статический метод возвращает объект DynamicModule — module, imports, providers, exports и опционально global — и этот один объект есть механизм за каждой конфигурируемой библиотекой, которую ты импортируешь (TypeOrmModule, JwtModule, ConfigModule). Свой строится через паттерн options-токена: определи токен, пусть статический метод отдаст опции, привязанные к нему, а внутренние providers сделают @Inject этого токена. forRoot(options) синхронный и запекает литеральный объект в provider с useValue — верно для конфигурируемых-один-раз синглтонов на всё приложение, часто global: true. forRootAsync(&#123; imports, useFactory, inject &#125;) отдаёт тот же токен через фабрику, которую Nest разрешает через DI, — это единственный способ подать опции, приходящие из другого provider вроде ConfigService — он откладывает создание опций, пока inject-зависимости не существуют, решая курицу-и-яйцо, когда конфиг нужен до конструирования модуля. forFeature(...) — ярус на каждого потребителя, регистрирующий нечто, ограниченное импортирующим модулем (TypeOrmModule.forFeature([User]) даёт этому модулю его repository). Сеньорское разделение — конфигурировать-один-раз-глобально (forRoot, выполнить ровно один раз, иначе получишь дублирующиеся синглтоны) против регистрировать-на-фичу (forFeature, повторяется по модулям). ConfigurableModuleBuilder генерирует весь бойлерплейт forRoot/forRootAsync/MODULE_OPTIONS_TOKEN, так что ты наследуешь ConfigurableModuleClass и инжектишь токен. Теперь, когда встретишь `TypeOrmModule.forRootAsync` или `JwtModule.registerAsync` в кодовой базе и нужно будет подключить туда ConfigService, ты будешь точно знать, что происходит под капотом — и как построить тот же паттерн самому.
</Recap>
