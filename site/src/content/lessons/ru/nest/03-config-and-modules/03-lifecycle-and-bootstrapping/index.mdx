---
concepts:
- lifecycle-hooks
- onmoduleinit
- onapplicationbootstrap
- onapplicationshutdown
- graceful-shutdown
deepensInto: []
estMin: 15
lang: ru
lessonType: topic
level: middle
order: 3
prereqs: []
slug: "03-lifecycle-and-bootstrapping"
sources:
- https://docs.nestjs.com/fundamentals/lifecycle-events
- https://docs.nestjs.com/faq/keep-alive-connections
spiral: []
status: ready
summary: 'Nest запускает init-хуки снизу вверх (onModuleInit, затем onApplicationBootstrap)
  и shutdown-хуки в обратном порядке (onModuleDestroy, beforeApplicationShutdown,
  onApplicationShutdown) — но только если вызвать enableShutdownHooks(), чтобы он слушал SIGTERM.'
title: 'Lifecycle-хуки и graceful shutdown'
track: nest
unit: 03-config-and-modules
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import SequenceDiagram from "~/components/diagram/SequenceDiagram.astro";

<Hook>
Деплой уходит в 14:02. Kubernetes перекатывает поды, и на девяносто секунд дашборд ошибок загорается: `ECONNRESET`, наполовину записанные строки, всплеск 502 от балансировщика. Новые поды здоровы; плохо ведут себя именно *старые* на выходе. Открываешь сервис и находишь `PrismaService`, у которого `$connect()` живёт в конструкторе, а `$disconnect()` — нигде, и `main.ts`, который никогда не вызывает `enableShutdownHooks()`. Так что когда k8s шлёт `SIGTERM`, Node просто умирает: запросы в полёте обрываются посреди записи, а пул соединений никогда не сливается. У Nest был хук на каждый из этих шагов. Никто их не подключил.
</Hook>

<Crux>Nest запускает init-хуки снизу вверх, а shutdown-хуки в обратном порядке — но слушает SIGTERM, только если вызвать enableShutdownHooks().</Crux>

<Explanation>

## У lifecycle две фазы, у каждой фиксированный порядок

У Nest-приложения есть рождение и смерть, и оба — упорядоченные события, в которые можно встроиться. Знание порядка — это вся суть: оно говорит, *когда* твои внедрённые зависимости гарантированно готовы и *когда* их безопасно закрывать.

**Init идёт снизу вверх.** Когда ты вызываешь `app.listen()` (или `app.init()`), Nest резолвит граф зависимостей и затем запускает, на провайдер/модуль:

1. `onModuleInit()` — вызывается для модуля **после того, как разрешены собственные зависимости этого модуля**. Это первый момент, когда твои внедрённые провайдеры гарантированно сконструированы и связаны.
2. `onApplicationBootstrap()` — вызывается **после того, как каждый модуль закончил инициализацию**, прямо перед тем, как сервер начнёт слушать. Это первый момент, когда готово *всё приложение*, включая провайдеры из других модулей.

**Teardown идёт в обратном порядке.** На `app.close()` или сигнал завершения Nest разматывает в обратном порядке зависимостей:

1. `onModuleDestroy()` — пришёл сигнал завершения; начинаем разбирать.
2. `beforeApplicationShutdown(signal)` — выполняется после всех обработчиков `onModuleDestroy()`, **до закрытия соединений**; получает сигнал (`"SIGTERM"`, `"SIGINT"`, …).
3. `onApplicationShutdown(signal)` — выполняется **после закрытия соединений**; последний вздох, где ты освобождаешь пулы, consumer'ы очередей и клиентов.

Обратный порядок важен: модуль, зависящий от базы данных, разбирается *до* модуля базы — так что он никогда не пытается использовать соединение, которого уже нет. Каждый интерфейс вносит один метод — `OnModuleInit`, `OnApplicationBootstrap`, `OnModuleDestroy`, `BeforeApplicationShutdown`, `OnApplicationShutdown` — и ты реализуешь только нужные. Асинхронные хуки ожидаются: возврат `Promise` приостанавливает последовательность, пока он не зарезолвится.

<SequenceDiagram
  label="Жизненный цикл Nest-приложения по порядку: на старте Nest запускает onModuleInit на модуль, как только разрешены зависимости этого модуля, затем onApplicationBootstrap, как только проинициализированы все модули, затем HTTP-сервер начинает слушать; на сигнале завершения SIGTERM Nest запускает onModuleDestroy, затем beforeApplicationShutdown с сигналом, затем закрывает соединения, затем onApplicationShutdown с сигналом, выполняя эти teardown-хуки в обратном порядке зависимостей относительно init-порядка."
  hue="var(--d-systems)"
  actors={["Signal", "Nest", "Provider"]}
  messages={[
    { from: "Nest", to: "Provider", label: "INIT 1. onModuleInit (deps ready)" },
    { from: "Nest", to: "Provider", label: "INIT 2. onApplicationBootstrap (all modules up)" },
    { from: "Nest", to: "Nest", label: "-> app.listen(): now serving" },
    { from: "Signal", to: "Nest", label: "SIGTERM (needs enableShutdownHooks)" },
    { from: "Nest", to: "Provider", label: "DOWN 1. onModuleDestroy" },
    { from: "Nest", to: "Provider", label: "DOWN 2. beforeApplicationShutdown(signal)" },
    { from: "Nest", to: "Provider", label: "DOWN 3. onApplicationShutdown(signal)" },
  ]}
  caption="Init идёт снизу вверх (onModuleInit, затем onApplicationBootstrap); teardown — обратный, запускается лишь когда enableShutdownHooks() зарегистрировал слушателей SIGTERM/SIGINT. Async-хуки приостанавливают последовательность, пока не зарезолвятся."
/>

## onModuleInit против конструктора

**Конструктор** провайдера выполняется при инстанцировании, пока Nest ещё связывает граф. В этот момент твои внедрённые зависимости *существуют* (они сконструированы первыми), но любая нужная им асинхронная настройка — TCP-соединение, прогретый кэш, подтянутый конфиг — ещё не выполнилась. Делать настоящий I/O в конструкторе — ловушка: конструкторы не могут быть `async`, так что ты либо блокируешь синхронно, либо стреляешь и забываешь висящий промис, и у тебя нет чистого места обработать сбой.

`onModuleInit()` — это ответ. Когда нужно открыть соединение, прогреть кэш или убедиться, что внешний сервис доступен до прихода трафика, — именно сюда принадлежит этот код. Он выполняется один раз, сразу после разрешения зависимостей модуля, он может быть `async`, и Nest ожидает его перед тем, как двигаться дальше. Это делает его верным домом для прогрева, которому нужны готовые зависимости:

```ts
import { Injectable, OnModuleInit, OnApplicationShutdown } from '@nestjs/common';
import { Pool } from 'pg';

@Injectable()
export class DatabaseService implements OnModuleInit, OnApplicationShutdown {
  private pool!: Pool;

  constructor(private readonly config: ConfigService) {
    // конструктор: зависимости есть, но НИКАКОГО async I/O здесь
    this.pool = new Pool({ connectionString: this.config.get('DB_URL') });
  }

  async onModuleInit(): Promise<void> {
    // прогрев: зависимости готовы, await учитывается
    await this.pool.query('SELECT 1'); // докажем, что пул коннектится, до приёма трафика
  }

  async onApplicationShutdown(signal: string): Promise<void> {
    // слив: соединения закрываются последними, после завершения работы в полёте
    console.log(`shutting down on ${signal}`);
    await this.pool.end(); // закрыть каждый клиент в пуле
  }
}
```

Разделение намеренное: конструируй дёшево, коннекться в `onModuleInit`, освобождай в `onApplicationShutdown`. Если запрос прогрева выбросит, Nest прервёт старт с понятной ошибкой, вместо того чтобы поднять сервис, который не может достучаться до базы. Используй `onApplicationBootstrap` вместо `onModuleInit` только когда прогрев зависит от того, что полностью поднят *другой* модуль — например, прогрев кэша из сервиса, объявленного в другом модуле, который сначала должен закончить свой `onModuleInit`.

## Graceful shutdown, сделанный правильно

Shutdown-хуки не делают ничего, пока ты не подпишешься. `app.enableShutdownHooks()` регистрирует слушателей процесса на `SIGTERM`/`SIGINT`, чтобы Nest мог запустить teardown-последовательность; это **выключено по умолчанию, потому что навешивание слушателей сигналов имеет цену**, и Nest не станет навязывать её молча.

```ts
import { NestFactory } from '@nestjs/core';
import { AppModule } from './app.module';

async function bootstrap() {
  const app = await NestFactory.create(AppModule, {
    // также принудительно закрыть простаивающие Keep-Alive сокеты, чтобы процесс реально вышел
    forceCloseConnections: true,
  });
  app.enableShutdownHooks(); // ОБЯЗАТЕЛЬНО: иначе SIGTERM просто убьёт процесс
  await app.listen(process.env.PORT ?? 3000);
}
bootstrap();
```

Теперь фактическая последовательность на `SIGTERM`: **перестать принимать новую работу, слить запросы в полёте, затем закрыть ресурсы.** HTTP-адаптер Nest останавливает сервер и ждёт завершения открытых ответов, прежде чем зарезолвить закрытие, — но долгоживущие сокеты `Connection: Keep-Alive` могут держать процесс в заложниках, и поэтому существует `forceCloseConnections`. Как только запросы слиты, твой `onApplicationShutdown` закрывает пул базы, consumer'ы очередей и Kafka-клиентов — в обратном порядке зависимостей, так что ничто не закрывает ресурс, который ещё нужен другому провайдеру.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Хук</th>
        <th class="border border-border px-3 py-2 text-left">Фаза</th>
        <th class="border border-border px-3 py-2 text-left">Срабатывает когда</th>
        <th class="border border-border px-3 py-2 text-left">Для чего</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2"><strong>onModuleInit</strong></td>
        <td class="border border-border px-3 py-2">Init (1-й)</td>
        <td class="border border-border px-3 py-2">После разрешения зависимостей этого модуля</td>
        <td class="border border-border px-3 py-2">Коннект, прогрев кэша, warm-up, которому нужны внедрённые зависимости</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2"><strong>onApplicationBootstrap</strong></td>
        <td class="border border-border px-3 py-2">Init (2-й)</td>
        <td class="border border-border px-3 py-2">После инициализации ВСЕХ модулей, до listen</td>
        <td class="border border-border px-3 py-2">Кросс-модульный прогрев, которому нужно всё приложение</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2"><strong>onModuleDestroy</strong></td>
        <td class="border border-border px-3 py-2">Down (1-й)</td>
        <td class="border border-border px-3 py-2">Сигнал пришёл; teardown начинается</td>
        <td class="border border-border px-3 py-2">Остановить consumer'ы, сбросить буферы</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2"><strong>beforeApplicationShutdown</strong></td>
        <td class="border border-border px-3 py-2">Down (2-й)</td>
        <td class="border border-border px-3 py-2">После onModuleDestroy, до закрытия соединений</td>
        <td class="border border-border px-3 py-2">Работа последнего шанса с живыми соединениями; читает сигнал</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2"><strong>onApplicationShutdown</strong></td>
        <td class="border border-border px-3 py-2">Down (3-й)</td>
        <td class="border border-border px-3 py-2">После закрытия соединений</td>
        <td class="border border-border px-3 py-2">Освободить пулы, клиентов, сокеты; читает сигнал</td>
      </tr>
    </tbody>
  </table>
</div>

<Inset kind="why" lang="ru">
Почему Kubernetes делает это срочным? На rolling-обновлении k8s шлёт `SIGTERM` и ждёт `terminationGracePeriodSeconds` (по умолчанию 30с), прежде чем послать неостановимый `SIGKILL`. Если ты никогда не включал shutdown-хуки, ничего не сливается, и `SIGKILL` обрывает каждое соединение. Но есть и более тонкая гонка: k8s убирает под из endpoints сервиса *и* шлёт `SIGTERM` примерно параллельно, так что на мгновение балансировщик может всё ещё направлять новые запросы в завершающийся под. Сеньорский фикс — сначала переключить **readiness-пробу** в провал: под выходит из ротации балансировщика, трафик перестаёт приходить, и только потом ты сливаешь и закрываешь. Поэтому graceful shutdown — это вопрос сети не меньше, чем вопрос lifecycle.
</Inset>

<TradeoffMatrix
  id="nest-03-config-and-modules-03-lifecycle-and-bootstrapping-tradeoff-1"
  lessonSlug="03-lifecycle-and-bootstrapping"
  lang="ru"
  prompt="Провайдеру нужно открыть соединение с базой на старте. Где должен жить вызов connect(), чтобы соединение было готово до прихода трафика, а сбои чисто прерывали загрузку?"
  options={[
    { name: "В onModuleInit (или onApplicationBootstrap, если зависит от полной поднятости другого модуля)", summary: "Слот lifecycle для прогрева.", correct: true, justification: "onModuleInit выполняется один раз, сразу после разрешения внедрённых зависимостей модуля; он может быть async, и Nest его ожидает, так что соединение установлено и проверено до того, как app.listen() начнёт обслуживать, а сбой чисто выбрасывает и прерывает старт. Если прогреву нужно, чтобы провайдер из другого модуля был сначала полностью проинициализирован, верный выбор — onApplicationBootstrap, потому что он срабатывает после завершения onModuleInit каждого модуля. Именно для этого эти хуки и существуют." },
    { name: "В конструкторе, вызывая connect() напрямую", summary: "Баг из хука.", justification: "Конструкторы выполняются во время связывания графа и не могут быть async. Ты либо блокируешь event loop синхронно, либо запускаешь висящий промис, который Nest никогда не ожидает, — так что listen() может стартовать до готовности соединения, а у сбоя соединения нет чистого места прервать загрузку. Конструируй дёшево; коннекться в onModuleInit." },
    { name: "Лениво, на первом запросе, которому нужно соединение", summary: "Откладывает сбой.", justification: "Ленивый коннект переносит стартовый сбой во время обработки запроса, так что первый пользователь ловит латентность холодного старта, а любая проблема с auth/DNS/учётками всплывает как 500 в проде, а не как упавшая загрузка. Readiness-проверки тоже не поймают это. Прогрев принадлежит onModuleInit, чтобы под помечался ready только когда реально может достучаться до зависимостей." },
    { name: "В main.ts после app.listen(), через app.get(Service)", summary: "Слишком поздно и в обход.", justification: "К моменту, когда listen() зарезолвится, сервер уже принимает трафик, так что коннект после гонится с реальными запросами; это также разбрасывает логику связывания вне провайдера, ломая DI. Провайдер должен владеть собственным прогревом в onModuleInit, а не bootstrap должен лезть и делать это за него." },
  ]}
/>

<Quiz
  id="nest-03-config-and-modules-03-lifecycle-and-bootstrapping-quiz-1"
  lessonSlug="03-lifecycle-and-bootstrapping"
  lang="ru"
  question="В каком порядке срабатывают init- и shutdown-хуки Nest?"
  choices={[
    { label: "Init: onModuleInit, затем onApplicationBootstrap; shutdown (обратный): onModuleDestroy, beforeApplicationShutdown, onApplicationShutdown", correct: true },
    { label: "Init: onApplicationBootstrap, затем onModuleInit; shutdown: onApplicationShutdown, затем onModuleDestroy", misconception: "Порядок init обратный: onModuleInit срабатывает на модуль по мере разрешения его зависимостей, а onApplicationBootstrap срабатывает один раз после инициализации каждого модуля. Shutdown затем идёт в обратном порядке зависимостей, начиная с onModuleDestroy." },
    { label: "Сначала срабатывают все четыре init-хука, потом четыре shutdown-хука, в одном и том же порядке сверху вниз оба раза", misconception: "Teardown — не тот же порядок, что init: он идёт в обратном порядке зависимостей, так что модуль, зависящий от другого, разрушается до своей зависимости. Этот разворот и не даёт провайдеру трогать ресурс, который уже закрыт." },
    { label: "onApplicationShutdown срабатывает первым на shutdown, до закрытия соединений", misconception: "onApplicationShutdown — ПОСЛЕДНИЙ teardown-хук и выполняется ПОСЛЕ закрытия соединений. До закрытия соединений выполняется beforeApplicationShutdown; самым первым идёт onModuleDestroy." },
  ]}
/>

<Quiz
  id="nest-03-config-and-modules-03-lifecycle-and-bootstrapping-quiz-2"
  lessonSlug="03-lifecycle-and-bootstrapping"
  lang="ru"
  question="Твой onApplicationShutdown никогда не срабатывает, когда Kubernetes шлёт SIGTERM. Какова самая вероятная причина?"
  choices={[
    { label: "Ты никогда не вызвал app.enableShutdownHooks(), так что Nest не слушает SIGTERM, и процесс просто выходит", correct: true },
    { label: "Shutdown-хуки включены по умолчанию; сам хук, видимо, молча выбрасывает", misconception: "Shutdown-хуки ВЫКЛЮЧЕНЫ по умолчанию — Nest не навешивает слушателей SIGTERM/SIGINT, пока ты не вызовешь enableShutdownHooks(), именно потому что слушатели процесса несут цену. Без этого вызова сигнал убивает процесс, и ни один хук не выполняется." },
    { label: "onApplicationShutdown выполняется только на app.close(), никогда на сигналах ОС", misconception: "Он выполняется и на app.close(), И на сигналах завершения ОС — но только когда enableShutdownHooks() зарегистрировал слушателей сигналов. Путь через сигнал — это ровно случай Kubernetes." },
    { label: "SIGTERM игнорируется Node; нужно перехватывать SIGKILL вместо него", misconception: "Node умеет обрабатывать SIGTERM, а SIGKILL по определению неперехватываем — на SIGKILL нельзя выполнить код никогда. Фикс — включить shutdown-хуки, чтобы Nest обрабатывал SIGTERM и сливал до конца grace-периода." },
  ]}
/>

</Explanation>

<KeyTakeaway>Init снизу вверх (onModuleInit → onApplicationBootstrap); teardown обратный и срабатывает, лишь если enableShutdownHooks() навесил слушателей SIGTERM. Коннект в onModuleInit, слив в onApplicationShutdown.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="nest-03-lifecycle-and-bootstrapping-retrieval"
  lang="ru"
  questions={[
    { q: "Назови полный порядок lifecycle Nest для init и shutdown и объясни, почему teardown идёт в обратном порядке.", a: "Init идёт снизу вверх: onModuleInit срабатывает для модуля сразу после разрешения собственных зависимостей этого модуля (первый момент, когда внедрённые провайдеры связаны), затем onApplicationBootstrap срабатывает один раз после того, как каждый модуль закончил инициализацию, прямо перед тем, как app.listen() начнёт обслуживать. Teardown идёт в обратном порядке зависимостей: сначала onModuleDestroy (сигнал пришёл, начинаем teardown), затем beforeApplicationShutdown(signal), который выполняется до закрытия соединений, затем onApplicationShutdown(signal), который выполняется после закрытия соединений. Обратный порядок важен, потому что модуль, зависящий от другого — скажем, сервис, использующий модуль базы, — должен разбираться до того, от чего зависит, чтобы никогда не тянуться к соединению, которое уже освобождено. Async-хуки ожидаются, так что возврат Promise приостанавливает последовательность, пока он не зарезолвится." },
    { q: "Почему нужен enableShutdownHooks() и как graceful shutdown связан с Kubernetes?", a: "Shutdown-хуки выключены по умолчанию, потому что навешивание слушателей сигналов процесса на SIGTERM/SIGINT несёт цену, поэтому Nest делает это опциональным: app.enableShutdownHooks() регистрирует этих слушателей, чтобы teardown-последовательность (onModuleDestroy → beforeApplicationShutdown → onApplicationShutdown) реально срабатывала на сигнал. Без этого SIGTERM просто убивает процесс, обрывая запросы в полёте и теряя соединения. В Kubernetes это критично: на rolling-обновлении k8s шлёт SIGTERM и ждёт terminationGracePeriodSeconds (по умолчанию 30с) до неперехватываемого SIGKILL, так что слить нужно в этом окне — перестать принимать новую работу, дать запросам в полёте завершиться (forceCloseConnections помогает, когда долгоживущие Keep-Alive сокеты иначе держали бы процесс живым), затем закрыть пулы и consumer'ы в onApplicationShutdown. Поскольку под убирается из endpoints сервиса примерно параллельно с сигналом, readiness-проба должна сначала переключиться в провал, чтобы балансировщик вывел под из ротации до того, как ты начнёшь сливать." },
  ]}
/>

<Recap lang="ru">
У Nest-приложения есть упорядоченные рождение и смерть, в которые можно встроиться. Init идёт снизу вверх: onModuleInit срабатывает для каждого модуля в момент разрешения его собственных зависимостей — верный, осведомлённый об async дом для прогрева вроде открытия соединения или прогрева кэша, — а onApplicationBootstrap срабатывает один раз после того, как поднят каждый модуль, для кросс-модульного прогрева, которому нужно всё приложение. Конструктор — неверное место для этой работы, потому что он не может быть async и выполняется, пока граф ещё связывается. Teardown идёт в обратном порядке зависимостей: onModuleDestroy, затем beforeApplicationShutdown(signal) до закрытия соединений, затем onApplicationShutdown(signal) после их закрытия — слот для освобождения пулов, consumer'ов очередей и клиентов. Ни один shutdown-хук не сработает, пока ты не вызовешь app.enableShutdownHooks(), который опционален, потому что навешивает слушателей SIGTERM/SIGINT; цена — причина, по которой Nest не делает это молча. Graceful shutdown, сделанный правильно, — это: перестать принимать новую работу, слить запросы в полёте (forceCloseConnections для упрямых Keep-Alive сокетов), затем закрыть ресурсы — а в Kubernetes ты сначала переключаешь readiness-пробу в провал, чтобы балансировщик вывел под до слива, всё в пределах terminationGracePeriodSeconds до SIGKILL. Классический баг — коннект в конструкторе и закрытие нигде, со shutdown-хуками, которые никогда не включены, так что деплой обрывает запросы и теряет соединения. Теперь, когда увидишь всплеск ошибок на rolling-обновлении, ты знаешь, где искать: нет `enableShutdownHooks()`, а `connect()` живёт в конструкторе — и знаешь, как это починить.
</Recap>
