---
concepts:
- observability-stack
- ci-gates
- continuous-profiling
- rum
- slo-budget
deepensInto:
- performance/08-putting-it-together/04-incident-to-enforcement
estMin: 14
lang: ru
lessonType: topic
level: middle
order: 3
prereqs:
- performance/08-putting-it-together/02-classify-and-fix-families
- performance/01-profile-first/08-continuous-profiling-at-scale
slug: 03-observability-stack-and-gates
sources:
- https://opentelemetry.io/docs/specs/otel/profiles/
- https://github.com/grafana/pyroscope
- https://web.dev/articles/vitals
- https://sre.google/sre-book/eliminating-toil/
spiral:
- latency
- statefulness
status: ready
summary: Пять интегрированных потоков данных — метрики, логи, трейсы, профили, RUM
  — плюс pre-merge CI gates, которые ловят регрессии до достижения пользователей.
  Самое дешёвое исправление — то, которое не выходит в production.
title: 'Observability-стек и CI gates: ловить регрессии до выпуска'
track: performance
unit: 08-putting-it-together-perf
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import NumbersCard from "~/components/layout/NumbersCard.astro";
import Misconception from "~/components/layout/Misconception.astro";
import StackDiagram from "~/components/diagram/StackDiagram.astro";

<Hook>
Дежурный инженер получает алерт о SLO burn p99 в 2 часа ночи. У неё есть непрерывное профилирование, распределённые трейсы и RUM. Она находит первопричину за 4 минуты. Её коллега в другой компании получает тот же алерт, но имеет только серверные логи. Она всё ещё разбирается в 6 утра. Разница не в инциденте — в стеке observability, который у неё был до инцидента.
</Hook>

<Crux>Пять сигналов — метрики, логи, трейсы, профили, RUM — плюс CI gates: ловить регрессии до пользователей, исправлять проскочившие за минуты.</Crux>

<Explanation>

## Пять production-сигналов

Если тебе хоть раз приходилось разбирать спайк p99, имея на руках только серверные логи, ты уже знаешь, зачем нужен каждый из этих сигналов. Production-grade работа с производительностью требует пяти интегрированных потоков данных. Каждый отвечает на разный вопрос; вместе они дают полную картину.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Сигнал</th>
        <th class="border border-border px-3 py-2 text-left">Примеры инструментов</th>
        <th class="border border-border px-3 py-2 text-left">Вопрос, на который отвечает</th>
        <th class="border border-border px-3 py-2 text-left">Обновление / хранение</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">Метрики</td>
        <td class="border border-border px-3 py-2">Prometheus + Grafana</td>
        <td class="border border-border px-3 py-2">Здоров ли сервис? p50/p99 latency, RPS, error rate</td>
        <td class="border border-border px-3 py-2">15–60 с / 13 месяцев</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Логи</td>
        <td class="border border-border px-3 py-2">Loki / ELK / Datadog Logs</td>
        <td class="border border-border px-3 py-2">Что произошло для этого запроса?</td>
        <td class="border border-border px-3 py-2">По запросу / 7–90 дней</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Трейсы</td>
        <td class="border border-border px-3 py-2">Tempo / Jaeger / Honeycomb</td>
        <td class="border border-border px-3 py-2">Какой span владеет задержкой?</td>
        <td class="border border-border px-3 py-2">На запрос / 7–30 дней</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Профили</td>
        <td class="border border-border px-3 py-2">Pyroscope / Parca / Polar Signals</td>
        <td class="border border-border px-3 py-2">Какая функция съела CPU / аллокации?</td>
        <td class="border border-border px-3 py-2">Непрерывно / 7–30 дней</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">RUM</td>
        <td class="border border-border px-3 py-2">Sentry / Datadog RUM / Vercel Speed Insights</td>
        <td class="border border-border px-3 py-2">Что испытали реальные пользователи? Core Web Vitals</td>
        <td class="border border-border px-3 py-2">На сессию / 30–90 дней</td>
      </tr>
    </tbody>
  </table>
</div>

## Связь сигналов: единый дашборд

Мощь не в каждом сигнале по отдельности — в связях между ними. Алерт SLO burn ведёт к RED-дашборду. RED-дашборд ведёт к трейс-вью, отфильтрованному по burn-окну. Трейс ведёт к профилю, отфильтрованному по trace-id. Профиль ведёт к исходному коду.

Хорошо инструментированный сервис позволяет пройти эту цепочку менее чем за пять минут:

1. Алерт SLO (Prometheus → alertmanager).
2. Открыть RED-дашборд: что изменилось — p99, error-rate или RPS?
3. Перейти к трейсам, отфильтрованным по последним 10 минутам: какой span медленный?
4. Нажать "профилировать этот трейс" в Pyroscope: какая функция горячая?
5. Нажать "git blame" на функцию: какой коммит и деплой?

Без связей (кросс-сигнальный join по trace-id) каждый шаг требует ручной корреляции — копировать timestamp, искать в другом инструменте, угадывать окно деплоя. Медианный MTTR без связи сигналов — 30–90 минут; с ней — 3–10 минут.

OpenTelemetry стандартизирует wire-формат (OTLP) и SDK по всем пяти сигналам. Команды, инструментирующие через OTel, могут менять бэкенды без повторной инструментации. Сигнал профиля (стандартизирован в OTel 2024–2026) присоединяется к метрикам, логам и трейсам в одном Collector-пайплайне.

## Pre-merge CI gates: ловить до выпуска

После постмортема вопрос всегда один: что остановило бы это до выпуска? Ответ почти всегда — gate. Самое дешёвое исправление — то, которое никогда не попадает в production. Четыре gate запускаются на каждом PR, занимают 1–5 минут и ловят 90%+ потенциальных регрессий:

**Bundle-size gate** — не пропускает PR, если JS-бандл любого маршрута превышает per-route бюджет. Установить бюджет на post-fix уровне после оптимизации бандла; любой PR, выходящий за него, требует явного согласования. Инструменты: bundlewatch, Lighthouse CI, Next.js bundle analyzer в CI.

**Query-count gate** — не пропускает PR, если любой endpoint вводит N+1-запросы. Реализация: middleware в тест-сьюте, считающий DB-запросы на запрос и проверяющий, что счётчик ниже порога. Ловит самый частый класс backend-регрессий до слияния.

**Allocation-rate diff** — не пропускает PR, если allocation rate в бенчмарке критического пути регрессирует выше порога. Бенчмарк-хarnest (go test -bench, criterion для Rust, JMH для Java) запускается на горячем пути и проверяет, что alloc/op в пределах бюджета.

**Load-test diff** — запускает краткий load-сценарий против PR и не пропускает, если p99 на критическом пути регрессирует выше X%. Тяжелее остальных (5–15 минут), но ловит архитектурные регрессии, которые microbenchmarks пропускают.

<Inset kind="why" lang="ru">
Ментальная модель gate: каждый gate кодирует урок из одного прошлого инцидента. Bundle gate = урок из момента, когда сторонний скрипт раздул LCP до 4 с. Query gate = урок из N+1, который перевёл /orders с 30 мс до 1,5 с. Allocation gate = урок из аллокации логгера, давшей спайк GC. Каждый ретроспективный разбор инцидента должен завершаться вопросом "какой gate это бы поймал?" — и добавлением этого gate. Набор gate никогда не бывает завершён; он растёт вместе с историей отказов системы.
</Inset>

<Misconception id="mc-gates-done">
  <p><strong>"У нас все CI gates — готово."</strong> Gates ловят известные классы отказов. Новые архитектуры и внешние сервисы вводят режимы, не покрытые ими. После каждого инцидента добавляйте gate, который его бы поймал. Набор gate эволюционирует вместе с системой.</p>
</Misconception>

<NumbersCard
  id="03-observability-numbers"
  title="Числа по observability-инвестициям"
  stats={[
    { label: "MTTR с полным стеком и связью сигналов", value: "3–10 минут" },
    { label: "MTTR без непрерывного профилирования", value: "30–90 минут" },
    { label: "Процент ловли регрессий pre-merge gates (известные классы)", value: "90%+" },
    { label: "Стоимость Pyroscope OSS (self-hosted) в месяц", value: "~$500" },
    { label: "Стоимость Sentry RUM в год (небольшая команда)", value: "~$30k" },
    { label: "Инженерное время на performance firefighting без дисциплины", value: "20–40%" },
    { label: "Инженерное время с дисциплиной (стабильное состояние)", value: "5–10%" },
  ]}
  data-lesson-visual
/>

<Quiz
  id="03-gates-quiz-1"
  lessonSlug="03-observability-stack-and-gates"
  lang="ru"
  question="Команда имеет CI gates для бандла, query count и allocation rate. Они всё равно получают production p99 регрессию раз в квартал. Наиболее вероятный пробел?"
  choices={[
    { label: "Существующие gates ловят известные классы регрессий. Новые режимы отказов не покрыты. Решение: добавлять gate после каждого инцидента и не считать текущий набор завершённым.", correct: true },
    { label: "Gates неправильно настроены", misconception: "Gates, ловящие известные проблемы, настроены верно; пробел — в покрытии, а не в корректности." },
    { label: "Production-трафик невозможно измерить в CI", misconception: "CI gates используют синтетическую нагрузку; RUM и SLO алерты ловят проскользнувшее. Пробел — отсутствующий gate для нового режима отказа." },
    { label: "Нужно больше CI-вычислений", misconception: "Больше вычислений ускоряет gates; это не расширяет покрытие на новые классы отказов." },
  ]}
/>

<Quiz
  id="03-gates-quiz-2"
  lessonSlug="03-observability-stack-and-gates"
  lang="ru"
  question="Почему trace-id является ключевым ключом объединения между сигналами в unified observability-стеке?"
  choices={[
    { label: "Trace-id распространяется через каждый сервис и слой для одного запроса. Фильтруя метрики, логи и профили по одному trace-id, можно точно увидеть, что испытал один медленный запрос.", correct: true },
    { label: "Это требуется спецификацией HTTP", misconception: "Trace-id опционален и зависит от вендора. Его ценность — в коррелированном кросс-сигнальном вью, а не в соответствии стандарту." },
    { label: "Это делает логи легче читаемыми", misconception: "Trace-id — для корреляции между системами, а не для удобочитаемости отдельных логов." },
    { label: "Он снижает alert noise", misconception: "Фильтрация алертов использует пороги метрик; trace-id — для drill-down по кросс-сигналам, а не для объёма алертов." },
  ]}
/>

<DragOrder
  id="03-gates-drag-1"
  lessonSlug="03-observability-stack-and-gates"
  lang="ru"
  prompt="Упорядочите пять production-сигналов от самого широкого (вид всего сервиса) до самого узкого (одна функция в одном запросе):"
  items={[
    { id: "a", label: "Метрики — p99 latency, RPS, error rate по всем запросам" },
    { id: "b", label: "RUM — Core Web Vitals с реальных устройств пользователей по маршруту" },
    { id: "c", label: "Трейсы — per-request waterfall, показывающий какой span медленный" },
    { id: "d", label: "Логи — структурированная детализация событий для конкретного запроса" },
    { id: "e", label: "Профили — какая функция внутри span потребила CPU или аллокации" },
  ]}
  correctOrder={["a", "b", "c", "d", "e"]}
/>

</Explanation>

<StackDiagram
  label="Пять production-сигналов от самого широкого взгляда на сервис до одной функции, плюс pre-merge CI gates, ловящие регрессии до выпуска"
  hue="var(--d-hardware)"
  layers={[
    { label: "Metrics", note: "здоров ли сервис? p99 / RPS" },
    { label: "RUM", note: "что почувствовали реальные юзеры? CWV" },
    { label: "Traces", note: "какой span владеет задержкой?" },
    { label: "Logs", note: "что произошло для этого запроса?" },
    { label: "Profiles", note: "какая функция съела CPU / аллокации?" },
    { label: "CI gates", note: "стоп регрессиям до merge" },
  ]}
  caption="Сигналы связаны через trace-id (от широкого к узкому); CI gates — базовый слой: самое дешёвое исправление — то, что не выпущено."
/>

<KeyTakeaway>Пять сигналов — метрики, логи, трейсы, профили, RUM — связанные через trace-id, снижают MTTR с часов до минут. Pre-merge CI gates кодируют уроки из прошлых инцидентов и ловят 90% регрессий до выпуска. Строить нужно оба; ни одного в одиночку недостаточно.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="03-observability-retrieval"
  lang="ru"
  questions={[
    { q: "Какова роль trace-id в связи пяти observability-сигналов?", a: "Каждый запрос получает trace-id, который распространяется через каждый сервис и слой. Метрики агрегируются по всем запросам; когда p99 поднимается, вы фильтруете трейсы по burn-окну, чтобы найти медленные trace-id. Трейс ведёт к образцам профиля, захваченным при том же trace-id. Профиль называет функцию. Без join по trace-id пришлось бы вручную сопоставлять timestamps по трём разным инструментам и угадывать, какие образцы соответствуют медленному запросу. Trace-id делает это детерминированным запросом: покажи все образцы профиля для трейса abc123." },
    { q: "Назовите четыре типа CI gate, что каждый ловит и как реализован.", a: "Bundle-size gate: ловит client-side bloat регрессии. Реализация: bundlewatch или Lighthouse CI проверяет размер JS каждого маршрута против per-route бюджета, установленного на post-оптимизационном уровне; PR, превышающие бюджет, не проходят. Query-count gate: ловит N+1 регрессии в backend endpoint. Реализация: middleware тест-сьюта считает DB-запросы на запрос и проверяет, что счётчик ниже per-endpoint порога. Allocation-rate diff: ловит GC-регрессии на горячих путях. Реализация: бенчмарк-хarnest (go test -bench, criterion, JMH) запускается на горячем пути и проверяет, что alloc/op в пределах бюджета относительно базового уровня. Load-test diff: ловит архитектурные регрессии, которые microbenchmarks пропускают. Реализация: краткий синтетический load-сценарий против сервиса PR, проверяющий, что p99 на критическом пути не регрессировал выше процентного порога." },
    { q: "Почему CI gates и observability дополняют друг друга, а не заменяют?", a: "CI gates работают на синтетических нагрузках в контролируемой среде до слияния; они ловят изменения, вводящие известный класс регрессий. Но они не могут ловить регрессии, введённые изменениями внешних сервисов, необычными паттернами production-трафика или режимами отказов без соответствующего gate. Observability ловит регрессии, проскользнувшие через CI и достигшие production — непрерывное профилирование и RUM дают раннее предупреждение до сгорания SLO. Вместе они дают defence in depth: gates останавливают 90% регрессий на этапе слияния, observability ловит оставшиеся 10% в течение минут после деплоя. Ни одного в одиночку недостаточно." },
  ]}
/>

<Recap lang="ru">
Production-grade работа с производительностью работает на пяти интегрированных потоках данных: метрики (агрегированное здоровье), логи (per-request события), трейсы (per-request waterfall span), профили (per-function CPU и аллокации) и RUM (Real User Monitoring — замер Core Web Vitals на реальных устройствах пользователей). Trace-id — ключ объединения, связывающий их: SLO алерт ведёт к трейсу, трейс ведёт к профилю, профиль называет функцию. С полным стеком и связью сигналов MTTR падает с 30–90 минут до 3–10. До выпуска четыре CI gates ловят 90% регрессий: bundle-size, query-count, allocation-rate diff и load-test diff. Каждый gate кодирует урок из прошлого инцидента. Набор gate никогда не завершён — он растёт после каждой production-регрессии через ретроспективные разборы, отвечающие на вопрос "какой gate это бы поймал?" Теперь, когда ты проводишь следующий постмортем, у тебя есть чеклист: какого из пяти сигналов не хватало — и какой gate поймал бы это первым.
</Recap>
