---
concepts:
- performance-loop
- discipline-vs-project
- regression-prevention
- ci-gates
deepensInto:
- performance/08-putting-it-together/02-classify-and-fix-families
estMin: 10
lang: ru
lessonType: topic
level: junior
order: 1
prereqs:
- performance/01-profile-first/01-why-profile-first
- performance/02-hot-paths/01-what-makes-a-hot-path
slug: 01-the-performance-loop
sources:
- https://sre.google/sre-book/eliminating-toil/
- https://www.brendangregg.com/methodology.html
- https://web.dev/articles/vitals
spiral:
- latency
- statefulness
status: ready
summary: Performance деградирует по умолчанию. Восьмишаговый цикл — observe, profile,
  classify, predict, fix, verify, enforce, repeat — это дисциплина, удерживающая сервис
  быстрым год за годом.
title: 'Цикл performance: дисциплина, а не проект'
track: performance
unit: 08-putting-it-together-perf
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import MetaphorComplete from "~/components/pedagogy/MetaphorComplete.astro";

<Hook>
Команда исправила p99 с 1,2 с до 200 мс. Они объявили задачу завершённой, выпустили релиз и занялись другим. Через шесть месяцев p99 снова 900 мс. Не одна регрессия — просто новые фичи, новые библиотеки, больший JSON-ответ от upstream-сервиса. Исправление было правильным. Не хватало дисциплины.
</Hook>

<Crux>Performance — это цикл, а не проект. Profile, classify, fix, verify, enforce — затем повторить на следующем bottleneck.</Crux>

<Explanation>

## Почему performance деградирует по умолчанию

Каждая новая фича добавляет байты, запросы или аллокации. Каждое обновление зависимостей привносит новые code path. Каждое изменение схемы может превратить быстрый запрос в медленный. Без механизма для обнаружения этих добавлений performance непрерывно деградирует.

Разовая оптимизация имеет эффективный период полураспада три-шесть месяцев. После этого окна накопленные изменения от новых фич отменяют улучшения. Команды, которые воспринимают performance как проект, добиваются результата — и постепенно откатываются назад. Команды, которые воспринимают его как дисциплину, удерживают результат.

Разница — в одном механизме: **цикле**.

## Восьмишаговый цикл performance

Каждое расследование производительности, независимо от слоя, следует одной структуре:

1. **Observe** — появляется симптом: SLO burn, RUM-регрессия, жалоба пользователя, алерт на дашборде. Это говорит о том, что что-то не так, но не что именно.
2. **Profile** — захватить данные, подходящие к симптому. Flame graph по CPU для CPU-спайков, allocation profile для роста памяти, network waterfall для медленных загрузок страниц, bundle analyzer для client-side раздутости.
3. **Classify** — назвать bottleneck по family: CPU-algorithmic, allocation-bound, cache-bound, lock-bound, I/O-bound (N+1), syscall-bound (batching), JIT-deopt, bundle-bound. Каждая family имеет известный набор исправлений.
4. **Predict** — использовать закон Амдала для оценки улучшения headline-метрики при устранении этого hotspot. Если прогноз ниже целевого SLO, это не тот hotspot — вернитесь к шагу 2.
5. **Fix** — из playbook family выбрать технику, соответствующую конкретной форме hotspot. Применить только предсказанное изменение; никакого scope creep.
6. **Verify** — повторно профилировать под той же нагрузкой. Подтвердить, что и локальный hotspot уменьшился, И headline-метрика улучшилась.
7. **Enforce** — добавить CI gate, алерт или запись в runbook, предотвращающую возврат именно этой регрессии.
8. **Move on** — найти следующий bottleneck. Цикл никогда не заканчивается; он перемещается между слоями.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Шаг</th>
        <th class="border border-border px-3 py-2 text-left">Действие</th>
        <th class="border border-border px-3 py-2 text-left">Результат служит входом для</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">1. Observe</td>
        <td class="border border-border px-3 py-2">Заметить симптом</td>
        <td class="border border-border px-3 py-2">Какой сервис / метрику профилировать</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">2. Profile</td>
        <td class="border border-border px-3 py-2">Захватить правильный поток данных</td>
        <td class="border border-border px-3 py-2">Имя горячей функции / span</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">3. Classify</td>
        <td class="border border-border px-3 py-2">Назвать family bottleneck</td>
        <td class="border border-border px-3 py-2">Playbook для исправлений</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">4. Predict</td>
        <td class="border border-border px-3 py-2">Оценка по Амдалу</td>
        <td class="border border-border px-3 py-2">Решение работать с этим hotspot или нет</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">5. Fix</td>
        <td class="border border-border px-3 py-2">Применить подходящую технику</td>
        <td class="border border-border px-3 py-2">Изменённый код / конфиг</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">6. Verify</td>
        <td class="border border-border px-3 py-2">Повторно профилировать под той же нагрузкой</td>
        <td class="border border-border px-3 py-2">Подтверждение или откат</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">7. Enforce</td>
        <td class="border border-border px-3 py-2">CI gate / алерт / runbook</td>
        <td class="border border-border px-3 py-2">Деплой, защищённый от регрессий</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">8. Move on</td>
        <td class="border border-border px-3 py-2">Найти следующий bottleneck</td>
        <td class="border border-border px-3 py-2">Следующая итерация шага 1</td>
      </tr>
    </tbody>
  </table>
</div>

## Метафора с кухней

Performance похожа на уборку кухни, а не на покраску комнаты. Покрасить один раз — достаточно. Кухня, убранная однажды, снова загрязняется по мере приготовления еды; её убирают непрерывно.

Каждый из семи фрагментов этой главы — инструмент: профилировщик, классификатор горячих путей, средство исправления GC, детектор N+1, бэтчер, анализатор бандла. Ни один не удержит кухню чистой сам по себе; это делает цикл.

<Inset kind="why" lang="ru">
Команды без цикла получают совещания "почему сайт снова медленный?" каждые шесть месяцев, каждое из которых потребляет 5–20 инженерных дней. Команды с циклом поддерживают стабильные метрики год за годом. Разница в общих инженерных затратах невелика — дисциплина просто переносит инвестиции с пожаротушения на CI gates и observability.
</Inset>

## Квартал Беа и Свена

Беа приходит в команду, где год назад сервис был быстрым. Теперь p99 — 1,2 с вместо 200 мс. Свен объясняет ей цикл: профиль показывает GC pressure на 18%, N+1 в /orders добавляет 50 запросов на запрос, бандл /dashboard вырос на 800 КБ за шесть месяцев. Никакого единственного кризиса — три отдельных медленных накопления.

Они запускают цикл для каждого bottleneck поочерёдно: исправление аллокации логгера (неделя 1), дедупликация запросов (недели 2–3), code-split бандла (неделя 4). Через месяц p99 — 280 мс. CI gates удерживают работу живой в следующем квартале при поставке фич.

<Quiz
  id="01-the-performance-loop-quiz-1"
  lessonSlug="01-the-performance-loop"
  lang="ru"
  question="Команда применила исправление производительности и выпустила релиз. Через шесть месяцев производительность хуже, чем до исправления. Наиболее вероятная причина?"
  choices={[
    { label: "Нет CI gate, который бы защищал результат. Новые фичи добавили байты, запросы и аллокации, незаметно отменив исправление. Регрессия — это состояние по умолчанию без enforcement.", correct: true },
    { label: "Исправление было неверным", misconception: "Возможно, но маловероятно — исправление изначально работало. Регрессия пришла от нового кода, а не от сломавшегося старого." },
    { label: "Изменился трафик в production", misconception: "Некоторый эффект возможен, но типичная причина — внутренний дрейф от новых фич без enforcement." },
    { label: "Оборудование устарело", misconception: "Железо не замедляется за месяцы; ПО растёт при отсутствии gates." },
  ]}
/>

<DragOrder
  id="01-the-performance-loop-drag-1"
  lessonSlug="01-the-performance-loop"
  lang="ru"
  prompt="Упорядочите восемь шагов цикла performance, которые senior-инженер выполняет каждый раз:"
  items={[
    { id: "a", label: "Заметить симптом — SLO burn, RUM-регрессия, алерт профиля" },
    { id: "b", label: "Открыть профиль — определить горячий путь с конкретными числами" },
    { id: "c", label: "Классифицировать hotspot: CPU, аллокация, кеш, lock, I/O, syscall, JIT, бандл" },
    { id: "d", label: "Спрогнозировать влияние на headline-метрику по закону Амдала" },
    { id: "e", label: "Применить только предсказанное изменение; никакого scope creep" },
    { id: "f", label: "Повторно профилировать под той же нагрузкой; подтвердить улучшение и локального фрейма, и headline-метрики" },
    { id: "g", label: "Добавить CI gate или алерт, чтобы регрессия не могла вернуться незаметно" },
    { id: "h", label: "Задокументировать и перейти к следующему bottleneck" },
  ]}
  correctOrder={["a", "b", "c", "d", "e", "f", "g", "h"]}
/>

<MetaphorComplete
  id="01-the-performance-loop-metaphor-1"
  lessonSlug="01-the-performance-loop"
  lang="ru"
  setup="Заполните пропуск: performance — это _______ кодовой базы, измеряемая непрерывно, применяемая при каждом коммите, принадлежащая каждому инженеру."
  accepted={["health", "здоровье", "fitness", "quality", "discipline", "дисциплина", "responsibility", "ответственность"]}
  canonical="здоровье / health (свойство, требующее постоянного ухода, а не разового достижения)"
  explanation="Как физическое здоровье, performance-здоровье деградирует незаметно без постоянного внимания. Семь фрагментов — это диагностика и методы лечения; цикл — это режим. Команды без режима получают кризис раз в шесть месяцев; команды с ним — стабильные метрики год за годом."
/>

<Quiz
  id="01-the-performance-loop-quiz-2"
  lessonSlug="01-the-performance-loop"
  lang="ru"
  question="Что значит воспринимать performance как 'цикл', а не как 'проект'?"
  choices={[
    { label: "Работа с производительностью непрерывна: измерять, исправлять, верифицировать, применять enforcement, затем повторять при следующей регрессии. Каждый проход использует один и тот же toolkit; дисциплина — продолжать его запускать.", correct: true },
    { label: "Всегда оптимизировать всё подряд", misconception: "Преждевременная оптимизация — противоположность цикла. Цикл начинается с измерения; оптимизировать следует только то, что действительно медленно." },
    { label: "Нанять отдельную команду по производительности", misconception: "Выделенная команда помогает, но цикл — методология, которую любая команда применяет с нужными инструментами." },
    { label: "Переписывать сервис каждый год", misconception: "Переписывание редко является правильным ответом. Цикл применяет инкрементальные, измеренные исправления." },
  ]}
/>

</Explanation>

<KeyTakeaway>Performance деградирует по умолчанию. Восьмишаговый цикл — observe, profile, classify, predict, fix, verify, enforce, move on — превращает разовое исправление в устойчивое свойство. Enforcement — это шаг, отделяющий дисциплину от проекта.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="01-the-performance-loop-retrieval"
  lang="ru"
  questions={[
    { q: "Почему разовое исправление производительности имеет период полураспада три-шесть месяцев?", a: "Каждая новая фича, обновление зависимости и изменение схемы добавляют байты, запросы или аллокации. Без механизма обнаружения этих добавлений — CI gates, RUM-алертов, порогов allocation-rate — каждое из них остаётся незамеченным. Совокупность десятков мелких добавлений отменяет исходное исправление. Период полураспада оптимизации — это время, за которое накопленные неохраняемые изменения возвращают производительность к уровню до исправления. Добавление CI gates после исправления продлевает период полураспада до бесконечности: любой PR, который вернёт тот же класс регрессии, не пройдёт до слияния." },
    { q: "Какова роль шага 'enforce', и почему он самый важный из восьми?", a: "Enforcement превращает локальное исправление в системную защиту. Без него команда исправит тот же класс ошибки снова через три-шесть месяцев, когда кто-то другой сделает похожее изменение. С ним каждый будущий PR проверяется на основе извлечённого урока. Исправление — разовая работа; gate — постоянный. Это то, что делает performance дисциплиной, а не серией повторяющихся проектов. Конкретные формы: проверка размера бандла в CI, middleware gate для подсчёта запросов, бенчмарк allocation-rate, RUM-алерт, привязанный к SLO budget. Любой из них, запущенный на каждом PR, ловит конкретный класс регрессии до выпуска." },
    { q: "В сценарии Беа и Свена три отдельных bottleneck накапливались шесть месяцев. Что помешало команде заметить каждый из них по мере появления?", a: "Нет observability и нет CI gates. GC pressure постепенно росло по мере добавления аллокаций логгером; алерт на allocation-rate не срабатывал. N+1 появился в новом endpoint и никогда не был отмечен в CI, потому что не было query-count gate. Бандл вырос на 800 КБ по мере добавления новых зависимостей с фичами; на PR не выполнялась проверка per-route bundle budget. Каждое отдельное изменение было достаточно мало, чтобы проскользнуть через code review. Непрерывное профилирование, RUM-алерты и CI gates выявили бы каждую регрессию в течение дней после коммита — вместо того чтобы позволить им накапливаться шесть месяцев." },
  ]}
/>

<Recap lang="ru">
Performance деградирует по умолчанию. Каждая новая фича, зависимость и деплой добавляют байты, запросы или аллокации незаметно. Разовая оптимизация имеет период полураспада три-шесть месяцев до того, как накопленные изменения отменят улучшения. Цикл performance — observe, profile, classify, predict, fix, verify, enforce, repeat — превращает разовое исправление в устойчивое свойство. Критический шаг — enforcement: CI gates, которые не пропускают PR, вводящие тот же класс регрессии. Команды без цикла получают кризис производительности каждые шесть–восемнадцать месяцев и перестраивают всё с нуля; команды с ним поддерживают стабильные метрики год за годом при затратах пяти–десяти процентов инженерного времени против двадцати–сорока процентов в кризисном режиме.
</Recap>
