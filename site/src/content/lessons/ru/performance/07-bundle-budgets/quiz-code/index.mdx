---
concepts:
- bundle-analyzer
- dynamic-import
- size-limit
- tree-shaking-pitfalls
- http3-priorities
estMin: 14
lang: ru
level: senior
order: 9
prereqs: []
slug: quiz-code
sources:
- https://web.dev/articles/granular-chunking-nextgen
- https://nextjs.org/docs/app/building-your-application/optimizing/bundle-analyzer
status: ready
summary: Читай реальный конфиг бандлера, паттерны import, сниппет size-limit в CI и заголовок Priority, предсказывай эффект на поставляемые байты и выбирай фикс с наибольшим рычагом.
title: 'Bundle budgets: чтение конфига и вывода'
track: performance
unit: 07-bundle-budgets
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Проблемы bundle диагностируются в конфигах, строках import, treemap анализатора и логе CI. Читай каждый сниппет как сеньор, затем выбирай фикс, который реально двигает поставляемые байты — а не тот, что двигает только провод.
</Hook>

<Goal lang="ru">
Отработай цикл, который ты гоняешь на каждом bundle-ревью: читай import или конфиг, предсказывай, тащит ли он мёртвый вес, и берись за фикс с наибольшим рычагом — режь или shake байты раньше, чем тянуться к compression или к ручке.
</Goal>

## Сниппет 1 — barrel re-export

```js
// components/index.ts
export * from './Button';
export * from './Modal';
export * from './RichTextEditor';   // тянет 180 KB editor-либу

// потребитель
import { Button } from '@/components';   // здесь используется только Button
```

<Quiz
  id="07-bundle-budgets-code-1"
  lessonSlug="07-bundle-budgets"
  lang="ru"
  question="Роут использует только Button, но анализатор показывает 180 KB editor в этом чанке. Что происходит и каков фикс?"
  choices={[
    { label: "`export *` — это barrel, реэкспортирующий всё; если любой потребитель импортирует из него, бандлер обычно держит весь граф модулей, которого он касается — включая editor-либу RichTextEditor. Фикс: явные именованные реэкспорты или импорт прямо из './Button'.", correct: true },
    { label: "Button транзитивно импортирует editor.", misconception: "Button не импортирует editor. Именно `export *` в barrel тащит editor в граф для любого потребителя barrel." },
    { label: "Всё в порядке — editor будет tree-shaken, раз он не используется.", misconception: "Barrel с `export *` часто побеждает tree-shaking: бандлер держит реэкспортированные модули, когда потреблён даже один экспорт barrel. Editor поставляется." },
    { label: "Выставить sideEffects: false, чтобы выкинуть editor.", misconception: "sideEffects: false помогает с side effects на уровне модуля, а не с barrel, статически реэкспортирующим соседей используемого модуля. Чини реэкспорт." },
  ]}
/>

## Сниппет 2 — dynamic import

```js
// грузит chart-панель, которой нет на первой отрисовке
function openPanel(name) {
  return import(`./panels/${name}`);   // переменная в специфайере
}

// в другом месте — настоящий lazy-boundary
const Editor = React.lazy(() => import('./Editor'));
```

<Quiz
  id="07-bundle-budgets-code-2"
  lessonSlug="07-bundle-budgets"
  lang="ru"
  question="Обе строки выглядят как code-splitting. Какая реально даёт чистый, по отдельности shakeable чанк и почему?"
  choices={[
    { label: "Только `import('./Editor')` со строковым литералом. Переменный специфайер в `import('./panels/' + name)` заставляет бандлер собрать все файлы под ./panels в один catch-all чанк — он не знает на этапе сборки, какой модуль запросят.", correct: true },
    { label: "Обе эквивалентны — dynamic import всегда чисто сплитит.", misconception: "Переменный специфайер делает цель неизвестной на этапе сборки, поэтому бандлер должен включить всю совпавшую директорию в один чанк. Чисто сплитит только строковый литерал." },
    { label: "Только переменная форма, потому что она гибче.", misconception: "Гибкость здесь и есть цена: переменная форма пакует всю папку ./panels. Строковый литерал `import('./Editor')` — тот, что даёт точный чанк." },
    { label: "Ни одна — React.lazy отключает code-splitting.", misconception: "React.lazy построен на dynamic import() и ровно так ты и сплитишь компонент. Чистый — импорт со строковым литералом." },
  ]}
/>

## Сниппет 3 — CI-gate size-limit

```json
// package.json
"size-limit": [
  { "name": "Homepage JS", "path": ".next/static/**/_app-*.js", "limit": "100 KB" },
  { "name": "All pages total", "path": ".next/static/**/*.js", "limit": "500 KB" }
]
```

```
$ npx size-limit
  Homepage JS:     132 KB  (limit 100 KB)   FAIL  +32 KB
  All pages total: 471 KB  (limit 500 KB)   PASS
```

<Quiz
  id="07-bundle-budgets-code-3"
  lessonSlug="07-bundle-budgets"
  lang="ru"
  question="PR триггерит этот вывод. Автор просит смержить и 'починить в следующем спринте'. Какова верная сеньорская реакция?"
  choices={[
    { label: "Блокировать merge. Homepage-gate существует именно чтобы остановить регрессию у источника. Автор либо lazy-load'ит/сплитит добавку обратно под 100 KB, либо открывает budget-raise PR с sign-off — лимит никогда не обходится молча.", correct: true },
    { label: "Смержить — 'All pages total' gate всё ещё проходит, значит budget соблюдён.", misconception: "Каждый именованный лимит независим; homepage-лимит провален. Проходящий агрегат не оправдывает регрессию по конкретному роуту." },
    { label: "Поднять homepage-лимит до 132 KB в этом же PR, чтобы CI стал зелёным.", misconception: "Самостоятельное одобрение raise в провинившемся PR убивает gate. Raise требует протокола budget-raise: документированное обоснование и sign-off от perf-лида." },
    { label: "Добавить флаг пропуска, чтобы обойти size-limit для этого PR.", misconception: "Обход gate уничтожает его ценность — бюджеты эродируют по одному обходу за раз. Смысл в том, что каждое изменение видимо и явно." },
  ]}
/>

## Сниппет 4 — HTTP/3 priority

```
# Запрос браузера на критический app-чанк
GET /_next/static/app.abc123.js
Priority: u=1

# Запрос браузера на большой отложенный vendor-чанк
GET /_next/static/vendor.def456.js
Priority: u=5

# Конфиг CDN (misconfigured): игнорит клиентский Priority, отдаёт FIFO
```

<Quiz
  id="07-bundle-budgets-code-4"
  lessonSlug="07-bundle-budgets"
  lang="ru"
  question="Приложение корректно code-split, и браузер шлёт вменяемые приоритеты RFC 9218, но CDN их игнорит и отдаёт first-come-first-served. Каков вероятный эффект и где это проверить?"
  choices={[
    { label: "Большой u=5 vendor-чанк конкурирует за пропускную способность наравне с u=1 критическим чанком, так что критический чанк может прийти поздно — стирая до 20–30% выигрыша от splitting. Проверь в колонке 'Priority' Network-панели Chrome и в поддержке приоритетов CDN.", correct: true },
    { label: "Никакого эффекта — HTTP/3 быстрее HTTP/2 независимо от приоритетов.", misconception: "Транспортный выигрыш HTTP/3 не помогает, если подсказки приоритета игнорятся: пропускная способность делится поровну и критический чанк теряет фору." },
    { label: "Vendor-чанк грузится последним, потому что он больше.", misconception: "Без учтённых приоритетов порядок недетерминирован / FIFO — размер не толкает его в конец. Именно корректные приоритеты поставили бы малый критический чанк первым." },
    { label: "Слить чанки обратно в один bundle, чтобы избежать конкуренции.", misconception: "Un-split возвращает полную стоимость parse на критический путь. Фикс — заставить CDN учитывать приоритеты, а не отказываться от splitting." },
  ]}
/>

<Recap lang="ru">
Каждая регрессия bundle читается в конфиге и выводе: barrel с `export *` и dynamic import с переменным специфайером тихо побеждают splitting и tree-shaking; провал size-limit — это жёсткий gate, а не предложение отложить; и даже идеально сплитнутый bundle недодаёт, если CDN игнорит приоритеты HTTP/3. Диагностируй по анализатору и логу CI, чини убирая байты с критического пути первым делом, затем подтверждай порядок доставки — перемеряй, а не предполагай.
</Recap>
