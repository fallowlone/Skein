---
concepts:
- tree-shaking
- dead-code-elimination
- brotli
- gzip
- esm
- side-effects
deepensInto:
- 07-bundle-budgets/05-third-party-scripts
estMin: 14
lang: ru
lessonType: topic
level: middle
order: 4
prereqs:
- 07-bundle-budgets/03-code-splitting
slug: 04-tree-shaking-and-compression
sources:
- https://web.dev/articles/granular-chunking-nextgen
- https://v8.dev/blog/cost-of-javascript-2019
spiral:
- latency
status: ready
summary: Tree shaking убирает unused exports при сборке — но четыре распространённые
  ловушки незаметно его побеждают. Brotli и gzip сокращают wire-байты без влияния
  на parse-стоимость.
title: 'Tree shaking и compression: удаляем то, что не используем'
track: performance
unit: 07-bundle-budgets
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";
import Infographic from "~/components/diagram/Infographic.astro";
import compressionBytes from "./infographics/compression-bytes.svg?raw";
import staticVsDynamicCompression from "./infographics/static-vs-dynamic-compression.svg?raw";

<Hook>
Разработчик импортирует одну функцию из lodash: `import { debounce } from 'lodash'`. Bundle analyzer показывает 70 КБ lodash в выводе. Ожидалось 2 КБ. Импорт выглядел правильно — но формат модуля был неверным.
</Hook>

<Crux>Tree shaking режет unused exports: требует ESM, нет side effects, правильный конфиг. Compression — 60-80% wire-байтов, parse не трогает.</Crux>

<Explanation>

## Как работает tree shaking

Tree shaking — способность bundler'а обнаруживать и удалять экспорты, которые никогда не импортируются. Если модуль экспортирует 200 функций, а app импортирует только одну, остальные 199 — «мёртвый код» — их не должно быть в bundle.

Это работает, потому что ES module синтаксис (`import` / `export`) поддаётся статическому анализу: bundler может трассировать import-цепочки при сборке без выполнения кода. CommonJS (`require()`) вычисляет экспорты во время выполнения; bundler не может статически знать, какие используются, и включает всё.

```js
// Tree-shakeable (ESM) — bundler видит при сборке, какие экспорты используются
import { debounce } from 'lodash-es';  // Только debounce в bundle

// НЕ tree-shakeable (CJS) — bundler включает все экспорты
import { debounce } from 'lodash';      // Все 70 КБ lodash в bundle
```

Для включения tree shaking: используй ESM-сборку библиотеки (часто указана как `module` в `package.json`), настрой bundler на её использование (`resolve.mainFields: ['module', 'main']` в Webpack, автоматически в Vite), и убедись, что твой код использует named exports.

## Четыре ловушки, побеждающие tree shaking

**Ловушка 1 — side effects на уровне модуля.** Модуль, устанавливающий `window.foo = bar` или регистрирующий глобал при импорте, не может быть tree-shaken — bundler должен сохранить side effect, даже если ни один экспорт не используется. Fix: пометь пакет как `"sideEffects": false` в `package.json`, если у него нет module-scope side effects, или используй `/* #__PURE__ */` аннотации на конкретных вызовах.

**Ловушка 2 — CJS re-export.** Даже если твой код ESM, библиотека, компилирующаяся в CJS, не может быть tree-shaken. Bundler оборачивает весь модуль. Проверь `package.json` библиотеки: если нет поля `"module"` или `"exports"`, указывающего на ESM-сборку, — весь модуль едет. Переключись на ESM-first альтернативу или используй cherry-picked imports (`lodash/debounce` вместо `lodash`).

**Ловушка 3 — `export *` re-exports.** `export * from './module'` ре-экспортирует всё. Если даже один потребитель импортирует один экспорт из barrel-файла, весь модуль сохраняется. Fix: явные named re-exports в barrel-файлах.

**Ловушка 4 — dynamic import с переменной.** `await import('./module')` — tree-shakeable; `await import(name)`, где `name` — runtime-переменная, — нет: bundler не знает, какой модуль будет запрошен при сборке.

Все четыре ловушки объединяет одно: bundler не может на этапе сборки узнать, что будет использоваться во время выполнения, и включает всё. Без ESM, без pure-аннотаций, без явных re-exports и без строковых литералов в import() tree shaking незаметно выключается — и ты не узнаешь об этом, пока не откроешь analyzer.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Ловушка</th>
        <th class="border border-border px-3 py-2 text-left">Почему побеждает tree shaking</th>
        <th class="border border-border px-3 py-2 text-left">Fix</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">Side effects на уровне модуля</td>
        <td class="border border-border px-3 py-2">Bundler должен сохранить global mutation</td>
        <td class="border border-border px-3 py-2">sideEffects: false или #__PURE__ аннотация</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">CJS-библиотека</td>
        <td class="border border-border px-3 py-2">Экспорты вычисляются во время выполнения</td>
        <td class="border border-border px-3 py-2">ESM-сборка или cherry-pick imports</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">export * barrel files</td>
        <td class="border border-border px-3 py-2">Один потребитель держит весь модуль</td>
        <td class="border border-border px-3 py-2">Явные named re-exports</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Dynamic import с переменной</td>
        <td class="border border-border px-3 py-2">Модуль неизвестен при сборке</td>
        <td class="border border-border px-3 py-2">String literals в import()</td>
      </tr>
    </tbody>
  </table>
</div>

## Compression: gzip vs brotli

Compression уменьшает байты, передаваемые по сети. Text-based ассеты (JS, CSS, JSON, SVG) сжимаются на 60-80% с gzip и 70-90% с brotli. Те же 500 КБ uncompressed JS становятся примерно 200 КБ gzip'd или 160 КБ brotli'd.

**Важное ограничение**: compression уменьшает только download-стоимость. Parse, compile и execute стоимость определяется **несжатым** размером кода. Переход с gzip на brotli экономит ~10-25% сверх на wire — стоит делать, но не адресует CPU-узкое место.

<Infographic
  svg={compressionBytes}
  label="Столбчатая диаграмма одного и того же несжатого JS-файла на 500 КБ: gzip передаёт около 200 КБ, brotli около 160 КБ, но браузер всё равно парсит все 500 КБ в несжатом виде."
  caption="Compression обрушивает wire-байты (500 КБ до ~160 КБ с brotli), но движок сначала распаковывает, поэтому parse-стоимость остаётся привязанной к столбцу несжатых 500 КБ."
  hue="var(--d-infra)"
/>

**Static vs dynamic compression**: Static compression генерирует сжатый файл при сборке и отдаёт его напрямую с `Content-Encoding: br`. Стоимость при request time: ноль. Dynamic compression сжимает on the fly per request: ~5-30 мс для brotli, ~1-5 мс для gzip. Senior pattern: static brotli для всех статических ассетов (JS, CSS); dynamic gzip для HTML responses, varying per user.

<Infographic
  svg={staticVsDynamicCompression}
  label="Сравнение в две колонки static и dynamic compression: static сжимает один раз при сборке за 0 мс на запрос для неизменяемых JS и CSS, предпочитая сильный brotli; dynamic сжимает per request за brotli 5-30 мс или gzip 1-5 мс для per-user HTML, предпочитая более дешёвый gzip."
  caption="Метод под ассет: неизменяемые JS и CSS сжимай один раз (static brotli, 0 мс на запрос), а per-user HTML — на лету более дешёвым gzip (1-5 мс) вместо brotli (5-30 мс)."
  hue="var(--d-infra)"
/>

```bash
# Проверить, что brotli отдаётся
curl -sI -H "Accept-Encoding: br" https://your-app.com/app.js | grep content-encoding
# Ожидается: content-encoding: br
```

<Inset kind="why" lang="ru">
Почему несжатый размер определяет parse-стоимость? Браузер распаковывает файл перед парсингом. JS-движок получает и парсит исходные байты, не сжатую форму. Gzip и brotli — transport-layer оптимизации — экономят сетевые байты, но CPU-работа та же.
</Inset>

<Quiz
  id="04-quiz-1"
  lessonSlug="04-tree-shaking-and-compression"
  lang="ru"
  question="Библиотека поставляет только CJS-сборку. Команда импортирует одну utility-функцию. Что происходит в bundle?"
  choices={[
    { label: "В bundle едет вся библиотека — CJS экспорты вычисляются во время выполнения, bundler не может устранить unused exports.", correct: true },
    { label: "Едет только импортированная функция — bundler достаточно умён для CJS.", misconception: "Современные bundler'ы делают partial tree shaking на некоторых CJS паттернах, но это ненадёжно. ESM нужен для надёжного dead-code elimination." },
    { label: "Библиотека исключена полностью — это peer dep.", misconception: "Peer deps всё равно bundled если явно не externalized; и даже тогда должны загружаться runtime." },
    { label: "Библиотека минифицируется до только импортированной функции.", misconception: "Минификация убирает пробелы и переименовывает переменные; unused exports из CJS-модуля она не удаляет." },
  ]}
/>

<Quiz
  id="04-quiz-2"
  lessonSlug="04-tree-shaking-and-compression"
  lang="ru"
  question="Команда переключается с gzip на static brotli для JS-ассетов. Transfer size падает на 15%. LCP улучшается на 40 мс на mobile. Затронута ли parse-стоимость?"
  choices={[
    { label: "Нет. Parse-стоимость определяется несжатым размером. Brotli уменьшает wire-байты, но браузер распаковывает перед парсингом — CPU-стоимость не изменилась.", correct: true },
    { label: "Да — меньше transferred байтов значит быстрее parse.", misconception: "Compression экономит сетевые байты, не CPU-байты. Движок парсит распакованный код." },
    { label: "Да — brotli использует словарь, ускоряющий V8 parse.", misconception: "Brotli — transport encoding; V8 получает и парсит сырые распакованные байты независимо от того, как они были переданы." },
    { label: "Нет — parse-стоимость определяется gzip-размером.", misconception: "Parse-стоимость определяется несжатым (исходным) размером, не какой-либо сжатой формой." },
  ]}
/>

<Quiz
  id="04-quiz-3"
  lessonSlug="04-tree-shaking-and-compression"
  lang="ru"
  question="Модуль имеет `window.analytics = createAnalytics()` на уровне модуля и помечен sideEffects: true. App импортирует только одну функцию из него. Что происходит?"
  choices={[
    { label: "Весь модуль включается. Side-effect код на уровне модуля должен выполниться, поэтому bundler сохраняет всё для обеспечения корректности.", correct: true },
    { label: "Side-effect код удаляется, едет только импортированная функция.", misconception: "Bundler'ы сохраняют side effects для обеспечения корректности. Удаление global assignment может сломать код, зависящий от него." },
    { label: "Модуль автоматически разбивается на side-effect часть и export часть.", misconception: "Bundler'ы не делают такой split CJS или side-effectful модулей — это ответственность разработчика по рефакторингу." },
    { label: "sideEffects: true помечает модуль как безопасный для shaking.", misconception: "sideEffects: true означает, что модуль ИМЕЕТ side effects и НЕ должен быть shaken. sideEffects: false сигнализирует, что безопасно удалять если не используется." },
  ]}
/>

</Explanation>

<FlowDiagram
  label="Build-пайплайн: tree-shake unused ESM exports, затем сжатие остатка"
  caption="Tree shaking (статический анализ ESM) выбрасывает мёртвые exports и минифицирует; brotli затем сжимает wire-байты. Сжатие уменьшает только transfer — parse-стоимость задаётся несжатым минифицированным размером."
  hue="var(--d-hardware)"
  perRow={5}
  nodes={[
    { id: "esm", label: "ESM imports", sub: "статический граф" },
    { id: "mark", label: "Пометить used", sub: "трассировка exports" },
    { id: "drop", label: "Выбросить dead code", sub: "tree-shake" },
    { id: "min", label: "Minify", sub: "несжатый размер" },
    { id: "br", label: "Brotli", sub: "wire-байты" },
  ]}
  edges={[
    { from: "esm", to: "mark" },
    { from: "mark", to: "drop" },
    { from: "drop", to: "min" },
    { from: "min", to: "br" },
  ]}
/>

<KeyTakeaway>Tree shaking удаляет unused ESM exports при сборке — побеждается CJS-импортами, module-scope side effects, barrel-file export* и dynamic variable imports. Brotli экономит 10-25% wire-байтов сверх gzip, но не уменьшает parse-стоимость. Оба полезны; ни один не заменяет code splitting.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="04-retrieval"
  lang="ru"
  questions={[
    { q: "Почему импорт из CJS-библиотеки шлёт всю библиотеку, даже если используешь только одну функцию?", a: "CJS экспорты вычисляются во время выполнения через require(). Bundler не может статически определить, какие экспорты используются при сборке, и должен включить весь модуль для гарантии корректности. ESM статические импорты позволяют bundler'у точно трассировать, какие экспорты потребляются." },
    { q: "Библиотека помечена sideEffects: false, но всё равно полностью появляется в bundle. Назови две вероятных причины.", a: "1) Библиотека поставляет только CJS-сборку — sideEffects: false помогает только ESM-модулям, tree-shakeable bundler'ом. 2) Barrel-файл ре-экспортирует всё через 'export *' и один из экспортов используется — весь модуль сохраняется. Fix: явные named re-exports в barrel-файлах, и убедиться, что у библиотеки есть ESM-сборка." },
    { q: "В чём разница эффекта gzip и code splitting на parse-стоимость?", a: "Gzip уменьшает transfer-байты, но не parse-стоимость — браузер сначала распаковывает, потом парсит исходный размер. Code splitting уменьшает реальный код, отправляемый на route, уменьшая и transfer и parse-стоимость. Code splitting адресует CPU-узкое место; compression адресует только network-узкое место." },
  ]}
/>

<Recap lang="ru">
Tree shaking опирается на ESM статический анализ для удаления unused exports. Четыре ловушки — CJS-модули, module-scope side effects, barrel export*, dynamic variable imports — незаметно его побеждают и стоят проверки при каждой сборке. Compression (brotli предпочтительнее gzip) уменьшает wire-байты, но не трогает parse-стоимость. Вместе tree shaking и compression дополняют code splitting: splitting уменьшает то, что route скачивает; tree shaking убирает мёртвый код внутри каждого chunk; compression сжимает transfer. Следующий урок рассматривает third-party scripts — категорию, чаще всего взрывающую тщательно выставленные бюджеты. Теперь, когда импортируешь одну функцию, а bundle растёт на 70 КБ, знаешь, какую из четырёх ловушек проверять первой.
</Recap>
