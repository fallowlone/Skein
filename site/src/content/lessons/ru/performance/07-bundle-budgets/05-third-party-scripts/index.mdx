---
concepts:
- third-party-scripts
- partytown
- interaction-gating
- async-defer
- csp
deepensInto:
- 07-bundle-budgets/06-ci-enforcement
estMin: 14
lang: ru
lessonType: topic
level: middle
order: 5
prereqs:
- 07-bundle-budgets/04-tree-shaking-and-compression
slug: 05-third-party-scripts
sources:
- https://web.dev/articles/vitals
- https://web.dev/articles/preload-critical-assets
spiral:
- latency
status: ready
summary: Analytics, реклама, chat-виджеты и A/B testing-инструменты регулярно добавляют
  500 КБ-1 МБ на страницу — вне твоего контроля, но внутри цены юзера. Три паттерна
  их нейтрализуют.
title: 'Third-party scripts: тихий убийца бюджета'
track: performance
unit: 07-bundle-budgets
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";
import Infographic from "~/components/diagram/Infographic.astro";
import thirdPartyTax from "./infographics/third-party-tax.svg?raw";
import budgetErasure from "./infographics/budget-erasure.svg?raw";

<Hook>
Команда тратит спринт, срезая app bundle с 800 КБ до 350 КБ. Lighthouse улучшается на 12 баллов. Через неделю data-команда добавляет Segment, Hotjar и новый Intercom widget — суммарно 450 КБ. Работа спринта стёрта. Никто не заметил, потому что third-party scripts не в репозитории приложения.
</Hook>

<Infographic
  svg={budgetErasure}
  label="Столбчатая диаграмма общего JavaScript страницы за спринт оптимизации: 800 КБ до спринта, 350 КБ после среза app bundle и снова 800 КБ через неделю, когда data-команда добавляет три third-party скрипта суммарно на 450 КБ."
  caption="450 КБ, которые срезал спринт, стираются 450 КБ новых third-party скриптов — общий JS страницы возвращается к 800 КБ. Этот круг и делает third-party тихим убийцей бюджета."
  hue="var(--d-infra)"
/>

<Crux>Third-party scripts: вне репо, внутри цены юзера. Interaction gating, Worker изоляция, server-side — нейтрализуют.</Crux>

<Explanation>

## Third-party налог

Большинство production-веб приложений грузят 5-15 third-party scripts per page. Популярные и их примерные размеры:

- Segment analytics: ~150 КБ
- Hotjar recording: ~200 КБ
- Intercom chat widget: ~200 КБ
- Google Tag Manager (с несколькими tags): ~500 КБ+
- Full Optimizely A/B testing: ~400 КБ
- Один плохо оптимизированный ad tag: ~300 КБ

Десять средних скриптов × 100 КБ = 1 МБ third-party JS до твоего кода. Ничего из этого не появляется в bundle analyzer — файлы грузятся с внешних доменов во время выполнения. Ничего из этого нельзя tree-shaken — ты не контролируешь источник. И большинство работает на main thread, блокируя paint и взаимодействие.

<Infographic
  svg={thirdPartyTax}
  label="Горизонтальная столбчатая диаграмма примерного веса JavaScript для популярных third-party скриптов: Google Tag Manager 500 КБ, full Optimizely A/B testing 400 КБ, неоптимизированный ad tag 300 КБ, Hotjar recording 200 КБ, Intercom chat widget 200 КБ, Segment analytics 150 КБ."
  caption="Без tree shaking, без видимости в bundle analyzer, в основном на main thread — горстка популярных vendor'ов уже достигает ~1 МБ до твоего собственного кода."
  hue="var(--d-infra)"
/>

Проблема усугубляется: бизнес-команды часто добавляют скрипты через Google Tag Manager без инженерного review. Каждое добавление через tag manager обходит code review полностью.

## Паттерн 1: Interaction gating

Грузи скрипт только когда юзер делает действие, требующее его. Chat widget грузится при клике «Help». Analytics SDK грузится после первого значимого взаимодействия. Newsletter modal скрипт грузится когда юзер проскроллил до 80% страницы.

```html
<!-- Defer до клика -->
<button id="chat-btn">Chat with us</button>
<script>
  document.getElementById('chat-btn').addEventListener('click', () => {
    const script = document.createElement('script');
    script.src = 'https://widget.intercom.io/widget/APP_ID';
    document.body.appendChild(script);
  });
</script>
```

Tradeoff: первое взаимодействие, триггерящее загрузку, имеет видимую задержку пока chunk скачивается и парсится. Смягчи через `prefetch` когда trigger-элемент становится видимым (`IntersectionObserver` на кнопке) или на `mouseenter`.

## Паттерн 2: Web Worker изоляция через PartyTown

PartyTown — open-source библиотека (Builder.io), запускающая third-party скрипты в Web Worker через service-worker прокси. DOM-доступ из Worker bridged через synchronous postMessage. Main thread освобождается от analytics, ad и tracking скриптов полностью.

```html
<script>
  partytown = { forward: ['dataLayer.push', 'fbq', 'gtag'] };
</script>
<script src="/~partytown/partytown.js"></script>
<script type="text/partytown" src="https://www.googletagmanager.com/gtm.js?id=GTM-XXXX"></script>
```

Tradeoff: скрипты, зависящие от синхронного DOM-доступа, несовместимы (редко для analytics). postMessage round-trips добавляют ~5-15 мс per call. Некоторые скрипты ломаются при проксировании. Тестируй каждого vendor перед деплоем.

## Паттерн 3: Server-side measurement

Перенеси analytics events на сервер. Вместо того чтобы шить 150 КБ Segment-клиент каждому юзеру, firing Segment events из твоего API-сервера на нужных действиях. Юзеры получают tracking; браузеры не получают ничего.

Похожие варианты: Google Analytics Measurement Protocol, Sentry server-side error capture для SSR ошибок, кастомный event pipeline через лёгкий beacon endpoint.

Tradeoff: теряешь browser-only контекст — user agent details, real-time client errors, click heatmaps. Подходит для core funnel analytics; комбинируй с лёгким client SDK для browser-специфических сигналов.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Паттерн</th>
        <th class="border border-border px-3 py-2 text-left">Main-thread стоимость</th>
        <th class="border border-border px-3 py-2 text-left">Tradeoff</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">Interaction gating</td>
        <td class="border border-border px-3 py-2">Ноль до триггера</td>
        <td class="border border-border px-3 py-2">First-use latency; смягчить prefetch</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">PartyTown Web Worker</td>
        <td class="border border-border px-3 py-2">Почти ноль (проксировано в Worker)</td>
        <td class="border border-border px-3 py-2">DOM-sync несовместимые скрипты; postMessage overhead</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Server-side measurement</td>
        <td class="border border-border px-3 py-2">Ноль (ничего на клиенте)</td>
        <td class="border border-border px-3 py-2">Теряется browser-специфический контекст</td>
      </tr>
    </tbody>
  </table>
</div>

## Governance: отдельный third-party бюджет

Senior-команды ведут отдельный third-party бюджет наряду с app bundle бюджетом. Конкретно: файл `third-party-budget.json` в репо, перечисляющий каждый разрешённый third-party домен, его approved размер и команду-owner. Любой новый скрипт через GTM или `<script src>`, которого нет в файле, fail'ит CI.

Дополнительный enforcement: CSP (Content Security Policy) заголовок `script-src` перечисляет разрешённые origins. Скрипты с не перечисленных origins блокируются браузером. Ежеквартальный аудит просматривает каждую запись, удаляет unused vendors и проверяет track records.

<Inset kind="why" lang="ru">
Почему third-party scripts непропорционально вредят? (1) Vendor'ы ставят features выше байтов — chat SDK поддерживает каждый use case, даже если app использует 5%. (2) Нет tree shaking — нельзя убрать unused vendor код. (3) Кумулятивный эффект — 10 скриптов по 100 КБ = 1 МБ до твоего кода. (4) Main-thread блокировка — большинство запускается синхронно при загрузке страницы если явно не деferred.
</Inset>

<Quiz
  id="05-quiz-1"
  lessonSlug="05-third-party-scripts"
  lang="ru"
  question="Маркетинг хочет 200 КБ chat widget на каждой странице и говорит: должен грузиться немедленно. Senior-компромисс?"
  choices={[
    { label: "Грузить widget при первом клике кнопки chat. Widget 'на каждой странице' через кнопку — но стоит ноль байтов при initial load. Prefetch скрипт при hover кнопки.", correct: true },
    { label: "Удалить widget.", misconception: "Если бизнес требует, удаление — неверный tradeoff. Deferral достигает бизнес-цель без performance-стоимости." },
    { label: "Загружать widget последним после interactive страницы.", misconception: "'Грузить последним' всё равно блокирует main thread при запуске. Interaction gating полностью избегает стоимости до триггера." },
    { label: "Добавить async к script tag.", misconception: "async откладывает скрипт до окончания HTML parse, но всё равно выполняется на main thread и добавляет ~200 КБ parse+execute при загрузке страницы." },
  ]}
/>

<Quiz
  id="05-quiz-2"
  lessonSlug="05-third-party-scripts"
  lang="ru"
  question="Команда запускает пять analytics скриптов через GTM суммарно 600 КБ. Они мигрируют funnel events на server-side Measurement Protocol. Какой ожидаемый клиентский эффект?"
  choices={[
    { label: "Пять скриптов больше не шлются в браузер вообще — 600 КБ удалено с каждой загрузки страницы. LCP и INP улучшаются пропорционально.", correct: true },
    { label: "Events потеряются — server-side ненадёжен.", misconception: "Server-side Measurement Protocol используется в масштабе крупными компаниями и надёжен. Events firing с твоего сервера при нужных API actions." },
    { label: "Частичное улучшение — только server-initiated events удалены.", misconception: "Все пять скриптов заменяются server calls. Полные 600 КБ удалены с клиента." },
    { label: "Нет эффекта — GTM грузит скрипты lazy в любом случае.", misconception: "GTM грузит свой container синхронно при загрузке. Tags внутри него тоже выполняются на main thread." },
  ]}
/>

<Quiz
  id="05-quiz-3"
  lessonSlug="05-third-party-scripts"
  lang="ru"
  question="Почему CSP заголовок script-src релевантен для third-party budget governance?"
  choices={[
    { label: "CSP блокирует скрипты с доменов не в allow-list. Добавление нового vendor без инженерного approval fail'ит в runtime — браузер отказывается грузить скрипт.", correct: true },
    { label: "CSP автоматически сжимает third-party скрипты.", misconception: "CSP — security и governance механизм, не compression инструмент." },
    { label: "CSP запрещает GTM грузить любые скрипты.", misconception: "GTM сам допускается в CSP если перечислен. CSP ограничивает, что GTM потом может грузить с не перечисленных доменов." },
    { label: "CSP нерелевантен для performance — это только security.", misconception: "CSP удваивается как governance инструмент: enforce'ит third-party allow-list на уровне браузера, предотвращая попадание не проверенных добавлений в live." },
  ]}
/>

</Explanation>

<FlowDiagram
  label="Third-party script блокирует main thread, если его не сдержит один из трёх паттернов"
  caption="По умолчанию third-party script работает синхронно на main thread. Interaction gating, изоляция в PartyTown Web Worker и server-side measurement каждый убирает часть или всю эту стоимость."
  hue="var(--d-hardware)"
  nodes={[
    { id: "script", label: "Third-party script", sub: "байты вне бюджета", col: 0, row: 1 },
    { id: "block", label: "Блокирует main thread", sub: "по умолч.: sync", col: 1, row: 0 },
    { id: "gate", label: "Interaction gating", sub: "грузить по триггеру", col: 1, row: 1 },
    { id: "party", label: "PartyTown Worker", sub: "вне main thread", col: 1, row: 2 },
    { id: "server", label: "Server-side measure", sub: "ничего на клиенте", col: 1, row: 3 },
    { id: "free", label: "Main thread свободен", sub: "LCP / INP растут", col: 2, row: 2 },
  ]}
  edges={[
    { from: "script", to: "block", label: "по умолч." },
    { from: "script", to: "gate" },
    { from: "script", to: "party" },
    { from: "script", to: "server" },
    { from: "gate", to: "free" },
    { from: "party", to: "free" },
    { from: "server", to: "free" },
  ]}
/>

<KeyTakeaway>Third-party scripts — самый частый путь взорвать bundle budget — они вне репо, вне code review и работают на main thread. Interaction gating, PartyTown и server-side measurement каждый убирает часть или всю их стоимость. Governance layer (approved-vendor list + CSP) предотвращает непроверенные добавления.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="05-retrieval"
  lang="ru"
  questions={[
    { q: "Почему third-party scripts непропорционально раздувают bundle-стоимость по сравнению с first-party кодом?", a: "Vendor'ы поставляют полный feature set независимо от того, какие features использует app — tree shaking невозможен. Кумулятивный эффект: 10 скриптов по 100 КБ = 1 МБ. Они работают на main thread если не deferred. Добавляются через GTM без инженерного code review. First-party код проходит PR, bundle analysis и может быть split или shaken." },
    { q: "Опиши паттерн interaction gating и его tradeoff.", a: "Грузи third-party скрипт только когда юзер выполняет действие, требующее его (клик, scroll trigger, focus формы). Стоимость при initial load: ноль. Tradeoff: первое использование имеет download+parse latency. Смягчи prefetch-ем скрипта на hover или intersection, чтобы он был в кеше до клика." },
  ]}
/>

<Recap lang="ru">
Third-party scripts регулярно добавляют 500 КБ-1 МБ на каждую страницу, вне app репо и вне code review. Три структурных countermeasure: interaction gating (грузить при триггере, ноль initial-стоимости), PartyTown Web Worker изоляция (main thread освобождён), server-side measurement (ничего на клиенте). Governance через third-party budget file и CSP enforce'ит allow-list в CI и на уровне браузера. Следующий урок рассматривает CI enforcement: как сделать бюджет жёстким constraint, а не руководством. Теперь, когда data-команда просит добавить новый analytics-инструмент, у тебя есть процесс — и ответ никогда не бывает «конечно, просто добавь script tag».
</Recap>
