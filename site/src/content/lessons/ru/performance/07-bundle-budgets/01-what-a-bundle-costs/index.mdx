---
concepts:
- bundle-budget
- js-parse-cost
- mobile-cpu-gap
- time-to-interactive
deepensInto:
- 07-bundle-budgets/02-core-web-vitals
estMin: 12
lang: ru
lessonType: topic
level: junior
order: 1
prereqs:
- performance/01-profile-first/01-why-profile-first
- performance/02-hot-paths/01-what-makes-a-hot-path
slug: 01-what-a-bundle-costs
sources:
- https://v8.dev/blog/cost-of-javascript-2019
- https://web.dev/articles/vitals
- https://developer.chrome.com/docs/lighthouse/performance/
spiral:
- latency
status: ready
summary: 1 МБ JS-bundle — это не 1 МБ цены. Это четыре последовательных CPU-фазы,
  которые на mid-range mobile в 4-8 раз тяжелее, чем на ноутбуке разработчика.
title: 'Что на самом деле стоит bundle: download, parse, compile, execute'
track: performance
unit: 07-bundle-budgets
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
Разработчик выпускает новую фичу в пятницу. На MacBook страница грузится за 800 мс — нормально. К понедельнику bounce rate вырос на 20%. Коллега тестирует на mid-range Android и 8 секунд смотрит в белый экран. Фича была в порядке. Bundle — нет.
</Hook>

<Crux>JS байты = parse + compile + execute на CPU юзера. Mobile в 4-8x медленнее ноутбука. Бюджет или bounce rate.</Crux>

<Explanation>

## Четыре фазы, одна цена

Когда браузер скачивает JavaScript-файл, он не просто «грузит» код. До первого возможного взаимодействия выполняются четыре последовательные фазы.

**Download** — байты идут по сети. На 4G 200 КБ gzip'd занимает 150-500 мс в зависимости от качества сигнала.

**Parse** — JS-движок проходит по каждому байту и строит абстрактное синтаксическое дерево (AST). V8 парсит примерно 1-2 МБ/сек на быстром laptop-CPU и лишь 200-400 КБ/сек на среднем Android. Bundle 500 КБ uncompressed занимает 1,5-2 с parse на mid-range mobile.

**Compile** — движок преобразует AST в байткод, затем в оптимизированный машинный код. V8 использует многоуровневый пайплайн (Ignition → Sparkplug → TurboFan). Стоимость компиляции примерно линейно зависит от количества кода, реально запущенного при старте.

**Execute** — top-level код bundle'а выполняется синхронно: инициализация фреймворка, создание store, регистрация роутов. Для типового React + Redux приложения это блокирует main thread на 100-500 мс.

Полная цепочка на mid-range mobile для bundle 500 КБ uncompressed: download 800 мс + parse 1,5 с + compile 300 мс + execute 400 мс = примерно 3 секунды до первого возможного клика. Каждая фаза — отдельный рычаг; ни одна не поможет без прямого адресования.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Фаза</th>
        <th class="border border-border px-3 py-2 text-left">Узкое место</th>
        <th class="border border-border px-3 py-2 text-left">Цена на mid-range mobile (500 КБ uncompressed)</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">Download</td>
        <td class="border border-border px-3 py-2">Network bandwidth</td>
        <td class="border border-border px-3 py-2">~800 мс на 4G</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Parse</td>
        <td class="border border-border px-3 py-2">CPU — V8 проходит каждый байт</td>
        <td class="border border-border px-3 py-2">~1,5-2 с</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Compile</td>
        <td class="border border-border px-3 py-2">CPU — JIT-уровни</td>
        <td class="border border-border px-3 py-2">~300 мс</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Execute</td>
        <td class="border border-border px-3 py-2">CPU — single-threaded старт</td>
        <td class="border border-border px-3 py-2">~400 мс</td>
      </tr>
    </tbody>
  </table>
</div>

## Почему «работает на моём ноутбуке» — ложный тест

MacBook M-series имеет single-thread CPU в 4-8 раз быстрее среднего Android (Snapdragon 6 или 7-серия). 5G или Wi-Fi помогут с download — но parse, compile и execute выполняются целиком на CPU юзера. Более быстрая сеть не двигает эти фазы ни на миллисекунду.

Большинство пользователей в глобальных рынках — на mid-range или старом железе. Команды, тестирующие только на флагманах или dev-машинах, систематически недооценивают реальный опыт юзера. Chrome DevTools имеет режим CPU throttling 4x, приближающий mid-range Android; его использование при каждой performance-проверке ловит разрыв до продакшна.

## Дисциплина bundle budget

**Bundle budget** — максимальное количество байтов (обычно gzip'd JS, CSS, images раздельно), которое route имеет право доставлять, enforce'ится в CI. Без бюджета размер bundle растёт по accretion: каждый инженер добавляет библиотеку, каждая добавляет несколько КБ, ни один PR не виноват, но через полгода bundle удвоился. С бюджетом каждый PR, превышающий cap, fail'ит CI — команда либо оправдывает добавление, либо находит меньшую альтернативу.

Бюджет конвертирует performance из намерения в constraint. Он делает для клиентских байтов то же, что SLO делает для серверной latency: делает её измеримой, enforced и owned.

<Inset kind="why" lang="ru">
Почему 1 МБ JS стоит дороже 1 МБ image? Image скачивается, декодируется GPU и рисуется. JS скачивается, затем парсится, компилируется и выполняется на CPU — три дополнительных фазы, медленных, последовательных, блокирующих main thread. За байт JS примерно в 5-10 раз дороже image на mid-range mobile.
</Inset>

<Quiz
  id="01-quiz-1"
  lessonSlug="01-what-a-bundle-costs"
  lang="ru"
  question="Bundle 500 КБ uncompressed грузится нормально за 400 мс на dev-ноутбуке. Оцени вероятный Time to Interactive на mid-range Android на 4G."
  choices={[
    { label: "2-4 секунды — parse и compile добавляют 4-8x ноутбучной цены поверх медленного download.", correct: true },
    { label: "Примерно столько же — мобильные CPU догнали.", misconception: "Single-thread скорость mid-range mobile по-прежнему в 4-8 раз ниже M-series ноутбука по состоянию на 2024." },
    { label: "Дольше только из-за медленной сети.", misconception: "Сеть влияет только на download; parse, compile и execute — CPU-bound и в 4-8 раз медленнее на mobile." },
    { label: "Максимум в 2 раза медленнее.", misconception: "Разрыв больше — один parse на mid-range mobile для 500 КБ uncompressed занимает 1,5-2 с." },
  ]}
/>

<DragOrder
  id="01-drag-1"
  lessonSlug="01-what-a-bundle-costs"
  lang="ru"
  prompt="Расставь lifecycle JS bundle'а от сетевого запроса до первого взаимодействия:"
  items={[
    "Браузер запрашивает JS-файл с сервера",
    "Сервер возвращает файл (часто сжатый gzip или brotli)",
    "Браузер распаковывает и получает байты",
    "JS-движок парсит байты в abstract syntax tree (AST)",
    "JS-движок компилирует AST в байткод и затем в оптимизированный машинный код",
    "Браузер выполняет top-level код: imports, side effects, инициализация фреймворка",
    "App достигает interactive состояния — юзер может кликнуть",
  ]}
/>

<Quiz
  id="01-quiz-2"
  lessonSlug="01-what-a-bundle-costs"
  lang="ru"
  question="Команда переходит с gzip на brotli, bundle с 180 КБ до 150 КБ transferred. LCP улучшается на 50 мс. Решена ли проблема bundle?"
  choices={[
    { label: "Нет. Brotli уменьшает только download. Цена parse, compile и execute определяется несжатым размером — он не изменился. Нужно уменьшить реально отправляемый код.", correct: true },
    { label: "Да — меньше download значит быстрее страница.", misconception: "Download — только одна из четырёх фаз. Несжатый размер на диске определяет стоимость parse и compile." },
    { label: "Да — brotli лучшая доступная оптимизация.", misconception: "Brotli полезен, но даёт 10-25% поверх gzip. Code splitting экономит 50-90% того, что route скачивает." },
    { label: "Частично — 50 мс улучшение это всё возможное.", misconception: "50 мс — выигрыш на download. Экономия на parse+compile+execute приходит от уменьшения отправляемых байтов, не их сжатия." },
  ]}
/>

</Explanation>

<FlowDiagram
  label="Четыре последовательные фазы, которые JS bundle стоит до interactive страницы"
  caption="Одна сетевая фаза, затем три CPU-фазы на устройстве юзера. Только download зависит от сети; parse, compile и execute выполняются на main thread."
  hue="var(--d-hardware)"
  perRow={4}
  nodes={[
    { id: "dl", label: "Download", sub: "сеть ~800 мс" },
    { id: "parse", label: "Parse", sub: "CPU ~1,5-2 с" },
    { id: "compile", label: "Compile", sub: "CPU ~300 мс" },
    { id: "exec", label: "Execute", sub: "CPU ~400 мс" },
  ]}
  edges={[
    { from: "dl", to: "parse" },
    { from: "parse", to: "compile" },
    { from: "compile", to: "exec", label: "interactive" },
  ]}
/>

<KeyTakeaway>JS bundle стоит четыре CPU-фазы на устройстве юзера. Mid-range mobile в 4-8 раз медленнее dev-ноутбука — один parse 500 КБ занимает 1,5 с. Bundle budgets enforce'ят per-route byte cap в CI, предотвращая незаметный рост размера.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="01-retrieval"
  lang="ru"
  questions={[
    { q: "Назови четыре фазы, которые JS bundle проходит до interactive страницы.", a: "Download (сеть), parse (V8 строит AST из каждого байта), compile (JIT преобразует AST в байткод/машинный код), execute (top-level код выполняется на main thread)." },
    { q: "Почему апгрейд до 4G не исправляет parse и compile время?", a: "Parse и compile происходят на CPU юзера после прибытия байтов. Скорость сети влияет только на download; parse и compile — CPU-bound и в 4-8 раз медленнее на mid-range mobile чем на dev-ноутбуке независимо от сети." },
    { q: "Что такое bundle budget и что он предотвращает?", a: "Максимальный byte cap per route, enforce'ится в CI. Предотвращает рост размера bundle по accretion — каждый PR, превышающий cap, fail'ит, вынуждая команду split/defer или явно обосновать цену." },
  ]}
/>

<Recap lang="ru">
JavaScript bundle запускает четыре последовательных CPU-фазы: download, parse, compile, execute. Первая — network-bound; остальные три — CPU-bound и в 4-8 раз медленнее на среднем Android, чем на dev-ноутбуке. Bundle 500 КБ uncompressed может стоить 3 секунды заблокированного main thread на mid-range mobile — невидимо в разработке. Bundle budgets делают цену per-route constraint, enforce'ируемым в CI, ловя дрейф до юзеров. Следующий урок связывает эти цены с Google Core Web Vitals и конкретными порогами, определяющими «хорошо» vs «плохо» для LCP, INP и CLS.
</Recap>
