---
concepts:
- four-phase-cost
- core-web-vitals
- code-splitting
- tree-shaking
- third-party-scripts
- ci-enforcement
estMin: 13
lang: ru
level: senior
order: 7
prereqs: []
slug: quiz-choice
sources:
- https://v8.dev/blog/cost-of-javascript-2019
- https://web.dev/articles/vitals
- https://web.dev/articles/granular-chunking-nextgen
status: ready
summary: Синтез с множественным выбором по юниту bundle budgets — четырёхфазная модель стоимости, рычаги Core Web Vitals, splitting vs compression, налог third-party и enforcement в CI.
title: 'Bundle budgets: тест с множественным выбором'
track: performance
unit: 07-bundle-budgets
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Шесть вопросов сквозь весь юнит. Каждый отражает решение, которое ты принимаешь на реальном perf-ревью — не определение для пересказа, а вопрос о том, какой рычаг реально двигает число под нагрузкой.
</Hook>

<Goal lang="ru">
Подтверди, что умеешь связывать четырёхфазную модель стоимости, пороги Core Web Vitals, иерархию splitting/tree-shaking/compression, налог third-party и enforcement в CI — тот синтез, к которому вели отдельные уроки.
</Goal>

<Quiz
  id="07-bundle-budgets-choice-1"
  lessonSlug="07-bundle-budgets"
  lang="ru"
  question="Команда A переходит с gzip на brotli и режет transfer с 180 KB до 150 KB. Команда B применяет code-splitting к тому же роуту с 600 KB до 200 KB несжатого. Обе отчитываются о выигрыше в LCP. Кто реально снизил стоимость CPU и почему?"
  choices={[
    { label: "Команда B. Parse, compile и execute масштабируются по несжатому размеру; браузер распаковывает до парсинга, поэтому brotli ужимает только фазу download. Splitting убирает байты, которые CPU вообще не придётся парсить.", correct: true },
    { label: "Обе одинаково — меньше байт на проводе всегда значит меньше работы CPU.", misconception: "Compression — выигрыш только на транспортном уровне. Движок парсит распакованные байты, поэтому стоимость CPU задаётся несжатым размером и brotli её не меняет." },
    { label: "Команда A — словарь brotli ускоряет парсер V8.", misconception: "Brotli — это encoding; V8 получает сырые распакованные байты и парсит их одинаково независимо от способа доставки. Никакого ускорения парсинга." },
    { label: "Никто — LCP задаётся сетью, а не CPU.", misconception: "При быстром TTFB LCP свыше 2.5 с обычно означает, что JS parse/execute блокирует отрисовку. CPU — ровно то узкое место, на которое нацелен юнит." },
  ]}
/>

<Quiz
  id="07-bundle-budgets-choice-2"
  lessonSlug="07-bundle-budgets"
  lang="ru"
  question="Страница грузится за 800 мс на M-ноутбуке разработчика, но висит пустой ~8 с на mid-range Android. TTFB 70 мс на обоих. Что доминирующая причина?"
  choices={[
    { label: "Parse + compile + execute на CPU юзера. Single-thread mid-range mobile в 4–8× медленнее ноутбука, а эти три фазы полностью CPU-bound — более быстрая сеть их не сдвинет.", correct: true },
    { label: "Android-устройство на более медленной сети.", misconception: "TTFB 70 мс на обоих, то есть сеть быстро доставила первый байт. Разрыв — это CPU-bound parse/compile/execute, не download." },
    { label: "Сервер медленнее отвечает на мобильные user agent.", misconception: "TTFB идентичен — 70 мс. Сервер не переменная; переменная — CPU клиента." },
    { label: "Bundle не gzip'нут для мобильных.", misconception: "Compression влияет на байты download. Пустой экран 8 с при быстром TTFB — это CPU, парсящий/исполняющий большой несжатый bundle." },
  ]}
/>

<Quiz
  id="07-bundle-budgets-choice-3"
  lessonSlug="07-bundle-budgets"
  lang="ru"
  question="У команды P75 LCP — Poor, а P75 CLS — Good. Они режут bundle роута с 700 KB до 200 KB. LCP и INP улучшаются; CLS почти не двигается. Почему это ожидаемый исход?"
  choices={[
    { label: "LCP и INP упираются в JS parse/execute на main thread, поэтому срез байт помогает обоим. CLS — от images без размеров, поздних шрифтов и динамической вставки над сгибом — проблема layout с отдельной причиной и отдельным фиксом.", correct: true },
    { label: "CLS должен был улучшиться пропорционально — он отслеживает размер bundle как LCP.", misconception: "CLS измеряет визуальную стабильность (доля viewport × дистанция сдвига), а не стоимость CPU. Срез байт не резервирует место под layout." },
    { label: "Измерение CLS сломано, раз оно не изменилось.", misconception: "Неизменный CLS — это корректно: фикс CLS — это images с размерами, зарезервированное место и font-display: swap — ничего из этого срез bundle не трогает." },
    { label: "CLS улучшается только от server-side rendering.", misconception: "CLS — это вопрос дисциплины layout на клиенте; один SSR не чинит media без размеров или поздние шрифты." },
  ]}
/>

<Quiz
  id="07-bundle-budgets-choice-4"
  lessonSlug="07-bundle-budgets"
  lang="ru"
  question="Разработчик пишет `import { debounce } from 'lodash'`, и анализатор показывает 70 KB, а не 2 KB. На своём пакете он уже выставил `sideEffects: false`. В чём реальная причина и фикс?"
  choices={[
    { label: "`lodash` (а не `lodash-es`) поставляет CommonJS-сборку; его экспорты вычисляются в рантайме, поэтому бандлер не может выполнить dead-code-elimination. Фикс: импорт из `lodash-es` или cherry-pick `lodash/debounce`.", correct: true },
    { label: "Его собственный `sideEffects: false` неверен и затягивает всю библиотеку.", misconception: "`sideEffects: false` на твоём пакете лишь помечает твой код shakeable; он не делает чужой CJS-модуль статически анализируемым." },
    { label: "Минификация провалилась — минификация выкинула бы неиспользуемые 68 KB.", misconception: "Минификация переименовывает и срезает пробелы; она не удаляет неиспользуемые экспорты CJS. Tree-shaking требует статически анализируемого ESM." },
    { label: "Brotli ужмёт неиспользуемый код.", misconception: "Compression ужимает transfer этих байт, но они всё равно поставляются, парсятся и исполняются. Фикс — не включать их." },
  ]}
/>

<Quiz
  id="07-bundle-budgets-choice-5"
  lessonSlug="07-bundle-budgets"
  lang="ru"
  question="Команда спринт режет первопартийный bundle с 800 KB до 350 KB; через месяц data-команда добавляет через GTM Segment, Hotjar и Intercom (~450 KB), и выигрыш стёрт. Какой контроль поймал бы это до релиза?"
  choices={[
    { label: "Отдельный third-party budget (список одобренных вендоров) плюс CSP script-src allow-list — новые origin валят CI / отклоняются браузером, потому что их не ревьюили. Первопартийный size-limit gate не видит скрипты, подгруженные GTM.", correct: true },
    { label: "Более строгий первопартийный size-limit budget заблокировал бы их.", misconception: "size-limit меряет чанки в выводе твоей сборки. Скрипты, внедрённые GTM, грузятся с внешних доменов в рантайме и не попадают в твой bundle, так что gate их не видит." },
    { label: "Tree-shaking удалил бы неиспользуемый код вендоров.", misconception: "Ты не контролируешь исходники вендоров и не можешь их shake; вендоры поставляют полный набор фич. Tree-shaking — это первопартийный механизм только для ESM." },
    { label: "Brotli на third-party скриптах нейтрализовал бы стоимость.", misconception: "Compression режет transfer, а не parse/execute на main thread, который навязывают скрипты вендоров. Нужен контроль governance, а не compression." },
  ]}
/>

<Quiz
  id="07-bundle-budgets-choice-6"
  lessonSlug="07-bundle-budgets"
  lang="ru"
  question="Lighthouse CI на каждом PR показывает P75 LCP 2.1 с; production RUM показывает 4.8 с P75 на мобильных для того же роута. Как это читает сеньор?"
  choices={[
    { label: "Ожидаемое расхождение. Lighthouse — один синтетический прогон с фиксированным throttling; RUM — реальное распределение по классам устройств, сетям и географиям. Держи CI-gate для ловли регрессий и доверяй RUM как реальному числу — одно не заменяет другое.", correct: true },
    { label: "RUM настроен неправильно — разрыв в 2× значит плохие данные.", misconception: "Разрыв синтетика-vs-поле в 2×+ нормален и отражает реальное разнообразие устройств/сетей, а не ошибку конфигурации." },
    { label: "Убрать Lighthouse CI, раз он спорит с реальностью.", misconception: "Lighthouse дёшево ловит регрессии от кода до merge; RUM ловит то, что синтетика упускает после деплоя. Они покрывают разные классы отказов." },
    { label: "Поднять budget — реальное число это то, что видят юзеры, так что 4.8 с — новый baseline.", misconception: "То, что RUM вскрыл худшее реальное число — повод резать байты, а не принимать регрессию за baseline." },
  ]}
/>

<Recap lang="ru">
Сквозная линия юнита — одно дерево решений: байты равны четырём фазам CPU (download — это сеть, parse/compile/execute — CPU юзера), поэтому реальные рычаги сокращают поставляемые байты — сначала splitting, затем tree-shaking, с compression как финишёром только для транспорта. Core Web Vitals количественно меряют стоимость (LCP/INP двигаются с JS, CLS — нет). Third-party скрипты — тихий убийца бюджета, которого первопартийные gate не видят, потому им нужен свой budget плюс CSP. И ничто не держится без enforcement: size-limit в CI, RUM в production, храповик budget-raise против дрейфа.
</Recap>
