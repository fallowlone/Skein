---
concepts:
- LCP
- INP
- CLS
- core-web-vitals
- rum
- lighthouse
deepensInto:
- 07-bundle-budgets/03-code-splitting
estMin: 14
lang: ru
lessonType: topic
level: middle
order: 2
prereqs:
- 07-bundle-budgets/01-what-a-bundle-costs
slug: 02-core-web-vitals
sources:
- https://web.dev/articles/vitals
- https://web.dev/articles/inp
- https://web.dev/articles/optimize-vitals-lighthouse
- https://developer.chrome.com/docs/lighthouse/performance/
spiral:
- latency
status: ready
summary: Три Google-метрики от реальных пользователей. Размер bundle — главный рычаг
  LCP и INP. Этот урок отображает байты на пороговые значения.
title: 'Core Web Vitals: LCP, INP и CLS'
track: performance
unit: 07-bundle-budgets
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import StackDiagram from "~/components/diagram/StackDiagram.astro";
import Infographic from "~/components/diagram/Infographic.astro";
import interactiveBudget from "./infographics/interactive-budget.svg?raw";

<Hook>
Команда тратит два спринта на оптимизацию API. Server P99 падает с 400 мс до 90 мс. Google Search Console показывает: LCP «Poor» у 40% мобильных пользователей. API не был узким местом. 800 КБ JS-bundle, блокирующий main thread, — был.
</Hook>

<Crux>LCP, INP и CLS — три Google-оценки восприятия юзера. Тяжёлый JS прямо валит LCP и INP. Bundle budgets — рычаг, который их двигает.</Crux>

<Explanation>

## Три метрики и что они меряют

Прежде чем чинить performance-проблему, нужна цифра, говорящая, что она есть — и что фикс сработал. Core Web Vitals — это и есть такие цифры: field-метрики, собираемые от реальных Chrome-пользователей через датасет CrUX, — измеряют разные аспекты пользовательского опыта.

**LCP (Largest Contentful Paint)** — время от начала навигации до рендера самого большого видимого элемента above the fold. Обычно hero image, H1 или фоновый блок. Target: менее 2,5 с — «Good»; 2,5-4,0 с — «Needs Improvement»; более 4,0 с — «Poor». Тяжёлый JS, блокирующий main thread, задерживает момент рисования — LCP страдает напрямую.

**INP (Interaction to Next Paint)** — введён в марте 2024, заменил FID. INP измеряет worst-case latency от любого пользовательского ввода (клик, нажатие, тап) до следующего visual update за всё время жизни страницы. Target: менее 200 мс — «Good»; 200-500 мс — «Needs Improvement»; более 500 мс — «Poor». Долгие JS-задачи на main thread — включая parse и execute большого bundle — поднимают INP.

**CLS (Cumulative Layout Shift)** — насколько видимый контент непредвиденно сдвигается при загрузке. Target: менее 0,1 — «Good»; 0,1-0,25 — «Needs Improvement»; более 0,25 — «Poor». Размер bundle не вызывает CLS напрямую; layout shifts возникают из-за картинок без размеров, поздней загрузки шрифтов и JS, вставляющего above-fold контент после initial paint.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Метрика</th>
        <th class="border border-border px-3 py-2 text-left">Good</th>
        <th class="border border-border px-3 py-2 text-left">Needs Improvement</th>
        <th class="border border-border px-3 py-2 text-left">Poor</th>
        <th class="border border-border px-3 py-2 text-left">Bundle lever?</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">LCP</td>
        <td class="border border-border px-3 py-2">&lt; 2,5 с</td>
        <td class="border border-border px-3 py-2">2,5 – 4,0 с</td>
        <td class="border border-border px-3 py-2">&gt; 4,0 с</td>
        <td class="border border-border px-3 py-2">Сильный — JS parse/execute блокирует paint</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">INP</td>
        <td class="border border-border px-3 py-2">&lt; 200 мс</td>
        <td class="border border-border px-3 py-2">200 – 500 мс</td>
        <td class="border border-border px-3 py-2">&gt; 500 мс</td>
        <td class="border border-border px-3 py-2">Сильный — long tasks от большого bundle</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">CLS</td>
        <td class="border border-border px-3 py-2">&lt; 0,1</td>
        <td class="border border-border px-3 py-2">0,1 – 0,25</td>
        <td class="border border-border px-3 py-2">&gt; 0,25</td>
        <td class="border border-border px-3 py-2">Косвенный — JS, вставляющий above-fold контент</td>
      </tr>
    </tbody>
  </table>
</div>

## Правило 100-170 КБ

Рекомендации Google Web Fundamentals: держи initial JS под 100-170 КБ gzip'd для fast first interactive на mid-range mobile. Математика: 170 КБ gzip'd разжимается примерно в 500 КБ. V8 парсит 200-400 КБ/сек на mid-range Android, значит parse занимает 1,5 с. Добавь compile и execute — итого 2-2,5 с CPU. Добавь 4G download ~500 мс — получается около 3 с до interactive. Это верхняя граница, удерживающая LCP под 2,5 с. Каждые дополнительные 100 КБ сверх неё добавляют примерно 300-500 мс к LCP на median устройстве.

<Infographic
  svg={interactiveBudget}
  label="Столбчатая диаграмма бюджета времени до interactive для 170 КБ gzip'd JS-bundle на mid-range mobile: 4G download 0,5 с, parse 1,5 с, compile и execute 0,75 с — CPU-стадии доминируют."
  caption="CPU-стоимость parse и execute JS (~2,25 с) перевешивает 0,5 с download — поэтому размер bundle, а не сеть, задаёт нижнюю планку LCP."
  hue="var(--d-infra)"
/>

Разные routes могут нести разные бюджеты: marketing homepage — жёстко (50-100 КБ), dashboard — средне (150-250 КБ), admin tools — мягче (300-500 КБ). Homepage несёт самый жёсткий cap, потому что это точка входа для конверсии.

## Три инструмента: Lighthouse, PageSpeed Insights, RUM

Эти инструменты измеряют одни метрики, но с разных точек зрения.

**Lighthouse** запускает синтетический тест — DevTools или CLI — с CPU throttling. Надёжен для поимки регрессий в PR. Не field-данные; один синтетический запуск.

**PageSpeed Insights** — Google-managed Lighthouse плюс field-данные из CrUX-датасета (реальные агрегированные метрики от Chrome-пользователей на твоём домене). Полезен для public-facing анализа. Обновляется раз в день.

**Real User Monitoring (RUM)** — собственная выборка реальных юзеров через Web Vitals JS library, Sentry, Datadog RUM и подобные. Самый точный, но требует setup и бюджет. Senior pattern: Lighthouse в CI для поимки регрессий перед мержем; RUM в production для регрессий, которые CI пропустил; PageSpeed Insights ежеквартально для cross-reference.

<Inset kind="why" lang="ru">
INP заменил FID (First Input Delay) в марте 2024. FID измерял только первое взаимодействие; INP — worst-case за всю сессию. Страница, где первый клик быстрый, а пятый — медленный, получала «Good» по FID и «Poor» по INP. Изменение делает метрику сложнее для манипуляции за счёт быстрого обработчика только для первого события.
</Inset>

<Quiz
  id="02-quiz-1"
  lessonSlug="02-core-web-vitals"
  lang="ru"
  question="P75 LCP команды — 3,2 с на mobile. Server TTFB — 80 мс. Наиболее вероятная причина?"
  choices={[
    { label: "Client-side JS parse и execute блокируют main thread — TTFB быстрый, значит проблема на клиенте. Уменьшение JS bundle — основной рычаг.", correct: true },
    { label: "Медленный ответ сервера.", misconception: "TTFB 80 мс — быстро. Сервер не является узким местом." },
    { label: "Изображения слишком большие.", misconception: "Images влияют на LCP, если самый большой элемент — картинка. Но самая частая причина LCP выше 2,5 с при быстром TTFB — JS, блокирующий paint." },
    { label: "Неверно настроен CDN.", misconception: "TTFB 80 мс говорит, что доставка CDN работает. Узкое место — цепочка обработки на клиенте." },
  ]}
/>

<Quiz
  id="02-quiz-2"
  lessonSlug="02-core-web-vitals"
  lang="ru"
  question="INP route — 380 мс. Bundle — 900 КБ uncompressed. После code splitting bundle до 200 КБ для этого route INP падает до 150 мс. Что улучшилось и почему?"
  choices={[
    { label: "Сократилось время выполнения JS на main thread. Меньше байтов для parse и compile = более короткие long tasks — каждая задача под 50 мс держит main thread отзывчивым к вводу.", correct: true },
    { label: "Сервер стал отвечать быстрее.", misconception: "INP полностью клиентский — измеряет ввод до следующего paint, не server round-trips." },
    { label: "Images стали меньше.", misconception: "INP про JS execution на main thread, не про размер картинок." },
    { label: "Brotli compression уменьшил parse time.", misconception: "Сжатие уменьшает download bytes; parse time определяется несжатым размером. Code splitting уменьшил реальные байты при загрузке route." },
  ]}
/>

<Quiz
  id="02-quiz-3"
  lessonSlug="02-core-web-vitals"
  lang="ru"
  question="Почему CLS (Cumulative Layout Shift) почти не улучшается от уменьшения JS bundle?"
  choices={[
    { label: "CLS вызывается картинками без размеров, поздней загрузкой шрифтов и динамическим контентом выше fold — не временем parse JS. Резервируй место для картинок, используй font-display: swap, избегай above-fold JS-вставок.", correct: true },
    { label: "CLS и размер JS bundle напрямую коррелируют.", misconception: "CLS про layout shifts, не CPU parse cost. Разная root cause, разный fix." },
    { label: "CLS можно исправить только server-side.", misconception: "CLS исправляется client-side дисциплиной — sized images, зарезервированное место, font-display." },
    { label: "CLS измеряет время paint, не shift.", misconception: "CLS измеряет величину неожиданных layout shifts, оценённых как доля viewport, умноженная на расстояние перемещения." },
  ]}
/>

</Explanation>

<StackDiagram
  label="Три Core Web Vitals и их пороги «Good»"
  caption="LCP и INP двигаются размером JS bundle; у CLS отдельные причины из layout."
  hue="var(--d-hardware)"
  layers={[
    { label: "LCP — largest contentful paint", note: "Good < 2,5 с" },
    { label: "INP — interaction to next paint", note: "Good < 200 мс" },
    { label: "CLS — cumulative layout shift", note: "Good < 0,1" },
  ]}
/>

<KeyTakeaway>LCP ниже 2,5 с и INP ниже 200 мс — пороги «Good». Каждые 100 КБ JS на route добавляют ~300-500 мс к LCP на mid-range mobile. CLS — проблема layout, не bundle. Lighthouse ловит регрессии в CI; RUM ловит то, что CI пропускает, в production.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="02-retrieval"
  lang="ru"
  questions={[
    { q: "Что измеряют LCP, INP и CLS? Назови порог «Good» для каждого.", a: "LCP: время рендера самого большого above-fold элемента. Good: менее 2,5 с. INP: worst-case latency от пользовательского ввода до следующего paint. Good: менее 200 мс. CLS: кумулятивная оценка неожиданных layout shifts. Good: менее 0,1." },
    { q: "Почему быстрый TTFB сервера (80 мс) не гарантирует хороший LCP?", a: "TTFB охватывает обработку на сервере и доставку первого байта. LCP измеряется до рендера самого большого элемента. Тяжёлый JS, блокирующий main thread после прибытия байтов, может задержать paint на секунды, независимо от TTFB." },
    { q: "Чем отличаются Lighthouse и RUM при поиске CWV-регрессий?", a: "Lighthouse — синтетический тест, запускаемый локально с throttling — хорош для PR-проверок, но не реальные пользователи. RUM выборочно собирает данные от реальных Chrome-пользователей на реальных устройствах и сетях. Lighthouse может пропустить регрессии, проявляющиеся только на определённых device-классах или географиях; RUM ловит их после деплоя." },
  ]}
/>

<Recap lang="ru">
Core Web Vitals — field-метрики от реальных Chrome-пользователей. LCP и INP оба улучшаются при уменьшении JS bundle — время parse и execute задают нижнюю планку скорости paint и реакции. Правило 100-170 КБ gzip'd опирается на математику: каждые 100 КБ сверх неё добавляют 300-500 мс к LCP на mid-range mobile. У CLS отдельные root causes (картинки без размеров, шрифты, динамические вставки). Lighthouse ловит регрессии перед мержем; RUM ловит дрейф в production. Следующий урок посвящён code splitting — основной технике уменьшения per-route bundle. Теперь, когда LCP застрял в зоне «Poor» несмотря на быстрый TTFB, ты знаешь, куда смотреть — и какая метрика подтвердит, что фикс сработал.
</Recap>
