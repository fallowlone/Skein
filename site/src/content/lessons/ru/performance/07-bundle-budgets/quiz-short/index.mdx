---
concepts:
- four-phase-cost
- splitting-strategies
- tree-shaking
- third-party-mitigation
- ci-and-rum
estMin: 14
lang: ru
level: senior
order: 8
prereqs: []
slug: quiz-short
sources:
- https://v8.dev/blog/cost-of-javascript-2019
- https://web.dev/articles/vitals
- https://web.dev/articles/granular-chunking-nextgen
status: ready
summary: Промпты на свободное воспроизведение по юниту bundle budgets. Сначала ответь своими словами, затем открой модельный ответ и сравни.
title: 'Bundle budgets: тест на свободное воспроизведение'
track: performance
unit: 07-bundle-budgets
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";

<Hook>
Воспроизведение бьёт перечитывание. На каждый промпт скажи или напиши полный ответ по памяти, прежде чем открыть модельный — именно усилие припоминания закрепляет материал.
</Hook>

<Goal lang="ru">
Восстанови ключевые механизмы юнита — четырёхфазную модель стоимости, иерархию splitting, ловушки tree-shaking, митигации third-party и петлю enforcement из CI плюс RUM — не заглядывая в уроки.
</Goal>

<RetrievalDrawer
  client:load
  id="07-bundle-budgets-short-retrieval"
  lang="ru"
  questions={[
    { q: "Назови четыре фазы, которые проходит JS bundle до интерактивности, и скажи, какие CPU-bound. Почему 'работает на моём ноутбуке' проваливается?", a: "Download (сеть), parse (движок проходит каждый байт в AST), compile (JIT превращает AST в байткод, затем в машинный код) и execute (код верхнего уровня исполняется на main thread). Parse, compile и execute — CPU-bound; только download — network-bound. Dev M-ноутбук имеет 4–8× single-thread скорости mid-range Android, поэтому три фазы CPU, доминирующие в большом bundle, невидимы в разработке. 500 KB несжатого bundle может парситься ~1.5 с и стоить ~3 с до интерактивности на mid-range mobile — норм на ноутбуке, пустой экран 8 с для юзера. Всегда тестируй с 4× CPU throttling." },
    { q: "Дай пороги 'Good' для LCP, INP и CLS и скажи, для какого из них bundle — рычаг.", a: "LCP (наибольшая отрисовка над сгибом) Good < 2.5 с; INP (худший input-to-next-paint за сессию, заменил FID в марте 2024) Good < 200 мс; CLS (накопленный неожиданный сдвиг layout) Good < 0.1. Размер bundle — сильный рычаг для LCP и INP — оба упираются в JS parse/execute на main thread, и каждые лишние 100 KB добавляют примерно 300–500 мс к LCP на mid-range mobile. Это НЕ рычаг для CLS: сдвиг layout идёт от images без размеров, поздних шрифтов и динамической вставки над сгибом, чинится зарезервированным местом, размерами media и font-display: swap." },
    { q: "Объясни route-level, component-level и vendor splitting и когда каждый — верный выбор.", a: "Route-level: каждый роут получает свой чанк, чтобы переход на /settings не качал /editor — самое рычажное изменение, верно когда роуты делят мало кода. Component-level: dynamic import() (React.lazy, defineAsyncComponent) для крупного, чего нет на критическом первом пейнте — модалки, charts, editor за кликом; правило большого пальца — lazy-load всё > 30 KB, не рендерящееся на первом пейнте, и prefetch на hover, чтобы скрыть задержку первого использования. Vendor splitting: стабильные third-party либы в отдельный content-hashed чанк, чтобы деплой, меняющий только app-код, оставлял кэшированный vendor-чанк юзера нетронутым — верно для часто деплоящихся приложений. Все три стекуются." },
    { q: "Почему импорт одной функции из CommonJS-библиотеки тащит всю библиотеку, и какие четыре ловушки побеждают tree-shaking?", a: "Экспорты CJS вычисляются в рантайме через require(), поэтому бандлер не может статически определить, какие используются, и должен включить весь модуль ради корректности; статический import/export ESM позволяет ему проследить ровно то, что потреблено. Четыре ловушки: (1) side effects на уровне модуля (например, window.x = ...), которые бандлер обязан сохранить — фикс через sideEffects: false или /* #__PURE__ */; (2) библиотека только в CJS — используй её ESM-сборку или cherry-pick (lodash-es, или lodash/debounce); (3) barrel-файлы с `export *`, где один используемый экспорт держит весь модуль — используй явные именованные реэкспорты; (4) dynamic import с переменным специфайером, который бандлер не может разрешить на этапе сборки — используй строковые литералы." },
    { q: "Почему third-party скрипты — самый частый способ пробить bundle budget, и каковы три структурные митигации?", a: "Они вне репо и вне код-ревью (часто добавляются через GTM не-инженерными командами), не tree-shake'аются, потому что ты не контролируешь исходники (вендоры шлют полный набор фич), быстро накапливаются (10 скриптов × ~100 KB = 1 MB) и по умолчанию исполняются на main thread — и первопартийный size-limit gate их не видит, потому что они грузятся с внешних доменов в рантайме. Три митигации: interaction gating (грузить по триггеру клика/скролла, ноль стоимости на старте, prefetch на hover чтобы скрыть задержку первого использования); изоляция в Web Worker через PartyTown (гоняет их вне main thread через service-worker proxy); и server-side measurement (фаер событий с API, в браузер ничего). Управляй через отдельный third-party budget файл плюс CSP script-src allow-list." },
    { q: "Почему bundle budget без CI-gate — лишь намерение, и каков полный стек enforcement PR-merge-production? Включи механизм против дрейфа.", a: "Без gate budget эродирует по одному разумно выглядящему PR за раз и через месяцы возвращается к старому размеру без единого виноватого PR. Стек: на PR size-limit (или bundlewatch) гонится после сборки и валит PR, если превышен любой per-route gzip'нутый лимит, постя комментарий с дельтой; на merge в main Lighthouse CI гонит полный синтетический CWV-аудит; в production RUM трекает P75 LCP/INP по роуту по классу устройства и алертит на регрессию > 20% — ловя классы устройств и медленное накопление, которые синтетика упускает. Против дрейфа: протокол budget-raise (документированное обоснование + sign-off perf-лида, записано в perf-budgets.json) плюс квартальный храповик, ужимающий budget на ~10%, выдавливая старый код когда приходит новый." },
  ]}
/>

<Recap lang="ru">
Если ты смог восстановить каждый ответ по памяти — у тебя есть хребет юнита: байты — это четыре фазы CPU, бьющие по mobile в 4–8× сильнее, чем по ноутбуку; LCP и INP двигаются с поставляемым JS, а у CLS свои причины; иерархия сокращения — split, затем shake, с compression как финишёром только для транспорта; third-party скрипты — тихий убийца, которому нужны свой budget и CSP; и ничто не выживает без CI-gate, RUM в production и храповика budget-raise против дрейфа.
</Recap>
