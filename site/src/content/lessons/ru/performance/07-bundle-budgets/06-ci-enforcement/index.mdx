---
concepts:
- bundle-budget-ci
- size-limit
- lighthouse-ci
- rum
- real-user-monitoring
deepensInto:
- 07-bundle-budgets/07-v8-pipeline-and-priorities
estMin: 15
lang: ru
lessonType: topic
level: middle
order: 6
prereqs:
- 07-bundle-budgets/05-third-party-scripts
slug: 06-ci-enforcement
sources:
- https://web.dev/articles/optimize-vitals-lighthouse
- https://nextjs.org/docs/app/building-your-application/optimizing/bundle-analyzer
- https://developer.chrome.com/docs/lighthouse/performance/
spiral:
- latency
- statefulness
status: ready
summary: Без CI-gate bundle budget — пожелание. С size-limit в CI и RUM в production
  каждый PR, взрывающий бюджет, fail''''ит, а каждая production-регрессия пейджит
  команду-owner.
title: 'CI enforcement и RUM: делаем бюджеты рабочими'
track: performance
unit: 07-bundle-budgets
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
Команда тратит неделю, срезая homepage bundle с 900 КБ до 180 КБ. Три месяца спустя он снова 820 КБ. Никто не нарушил никакой политики. Каждый инженер делал разумно выглядящий импорт. Без CI-gate, fail'ящего PR, бюджет — аспирация — он размывается по одному PR за раз.
</Hook>

<Crux>Бюджет без CI-gate — пожелание. size-limit fail'ит over-budget PR; RUM ловит production-дрейф до удара по метрикам.</Crux>

<Explanation>

## CI-gate: size-limit

`size-limit` — open-source инструмент, читающий per-route bundle targets из `package.json` или config-файла и fail'ящий build если любой лимит превышен. Запускается после сборки, читает gzip'd chunk sizes и завершается с не-нулевым кодом при превышении бюджета.

```json
// package.json
"size-limit": [
  { "name": "Homepage JS", "path": ".next/static/**/_app-*.js", "limit": "100 KB" },
  { "name": "Dashboard JS", "path": ".next/static/**/dashboard-*.js", "limit": "250 KB" },
  { "name": "All pages total", "path": ".next/static/**/*.js", "limit": "500 KB" }
]
```

```yaml
# .github/workflows/perf.yml
- name: Build
  run: bun run build
- name: Check bundle size
  run: npx size-limit
  # Fail'ит PR если любой лимит превышен
```

PR comment интеграция (`size-limit-action`) постит таблицу текущего vs бюджетного размера на каждый PR, делая эффект видимым перед мержем. Инженеры, добавляющие библиотеку, сразу видят дельту; либо обосновывают добавление (и команда явно поднимает бюджет), либо находят меньшую альтернативу.

## Альтернативы: bundlewatch, Lighthouse CI

`bundlewatch` схож с size-limit с другим форматом конфига и GitHub check интеграцией. Lighthouse CI запускает полный Lighthouse audit на каждый PR — более комплексно, медленнее. Все три инструмента отвечают на один вопрос: «взрывает ли этот PR performance budget?»

Senior-команды используют size-limit для быстрой per-PR обратной связи (запускается за 5-10 с) и Lighthouse CI на merge в main для полного CWV snapshot.

## Workflow с bundle analyzer

До того как CI может enforce бюджет, команда должна понять текущий bundle. Стандартный workflow:

1. Включить bundle analyzer: `ANALYZE=true bun run build` (Next.js: `@next/bundle-analyzer`; Vite: `rollup-plugin-visualizer`; Webpack: `webpack-bundle-analyzer`).
2. Отсортировать по размеру. Найти top 5-10 contributors.
3. Для каждого: нужен ли он на critical path этого route?
4. Типичные находки: `moment.js` (220 КБ, заменить на date-fns); полный `lodash` (80 КБ, cherry-pick); admin panel, импортированный глобально (170 КБ, lazy-load); все вариации иконок (110 КБ, cherry-pick).
5. Поставить initial budget at current size + 20% (место для роста, но не неограниченно).

Вместе эти шаги превращают неизвестный bundle в измеренный с именованным owner'ом. Без шага 4 — аудита типичных находок — команды выставляют произвольные лимиты и сразу обнаруживают, что уже сверх бюджета, не понимая почему.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Инструмент</th>
        <th class="border border-border px-3 py-2 text-left">Цель</th>
        <th class="border border-border px-3 py-2 text-left">Когда использовать</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">size-limit</td>
        <td class="border border-border px-3 py-2">Fail'ит PR если bundle &gt; бюджета</td>
        <td class="border border-border px-3 py-2">Каждый PR</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Bundle analyzer</td>
        <td class="border border-border px-3 py-2">Visual treemap модулей</td>
        <td class="border border-border px-3 py-2">До и после оптимизаций</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Lighthouse CI</td>
        <td class="border border-border px-3 py-2">Полный синтетический CWV audit</td>
        <td class="border border-border px-3 py-2">При merge в main</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">RUM</td>
        <td class="border border-border px-3 py-2">Real-user LCP/INP/CLS метрики</td>
        <td class="border border-border px-3 py-2">Непрерывный production monitoring</td>
      </tr>
    </tbody>
  </table>
</div>

## RUM в production

CI ловит регрессии перед мержем. RUM ловит то, что CI пропустил в production: device-классы, работающие медленнее throttled теста, географии с медленными сетями, и медленные регрессии, накапливающиеся за многие маленькие PR, каждый в рамках бюджета.

Implementations: Web Vitals JS library (Google, бесплатно), Sentry Performance, Datadog RUM, New Relic Browser, Vercel Speed Insights, Cloudflare Web Analytics.

Senior RUM dashboard: P75 LCP per route, per device class (mobile vs desktop), per geography. Alert при P75 LCP для любого route деградирует более 20% за 24 часа.

Связывая RUM с bundle size: некоторые команды шлют gzip'd bundle size per route как custom RUM метрику рядом с LCP, так что при деградации LCP первый dashboard для проверки показывает bundle size дельту для того же деплоя. Делает корреляцию видимой без inference.

## Протокол повышения бюджета

Когда PR легитимно нуждается в превышении бюджета: инженер открывает budget-raise PR. Требует sign-off от performance-owning team lead, документирует обоснование (новая фича, vendor requirement, ценность для юзера) и записывает старые и новые лимиты в `perf-budgets.json`. Без этого протокола бюджеты дрейфуют вверх по одному неявному approval за раз.

<Inset kind="why" lang="ru">
Почему initial budget включает +20% буфер? Установка бюджета на текущем измеренном размере означает, что следующее добавление фичи немедленно fail'ит CI — это деморализует и контрпродуктивно. 20% буфер признаёт, что фичи увеличивают bundle, и вынуждает явное обоснование только при превышении буфера. Затягивать бюджет на 10% в квартал как ratchet — непрерывное улучшение без cliff.
</Inset>

<Quiz
  id="06-quiz-1"
  lessonSlug="06-ci-enforcement"
  lang="ru"
  question="PR добавляет 60 КБ chart-библиотеку на homepage. CI-gate fail'ит. Инженер говорит, что chart важен. Корректный процесс?"
  choices={[
    { label: "Открыть budget-raise PR с задокументированным обоснованием. Performance lead одобряет или предлагает альтернативу (lazy-load chart, более лёгкая библиотека). Бюджет явно повышается при одобрении.", correct: true },
    { label: "Обойти CI-gate флагом.", misconception: "Обход gate уничтожает его ценность. Смысл в том, что каждое изменение бюджета видимо и явно." },
    { label: "Замержить PR; исправить performance позже.", misconception: "Позже никогда не наступает. Смысл gate — остановить регрессию у источника." },
    { label: "Удалить chart из фичи.", misconception: "Удаление — один валидный вариант, но lazy loading часто лучше — доставляет фичу без initial bundle-стоимости." },
  ]}
/>

<Quiz
  id="06-quiz-2"
  lessonSlug="06-ci-enforcement"
  lang="ru"
  question="Lighthouse CI запускается на каждый PR и сообщает P75 LCP 2,1 с. Production RUM показывает P75 LCP 4,8 с на mobile. Почему расхождение?"
  choices={[
    { label: "Lighthouse использует синтетический throttling, не реальные устройства. Реальные пользователи на mid-range железе с переменными сетевыми условиями значительно медленнее. RUM показывает реальное распределение.", correct: true },
    { label: "RUM неправильно настроен и сообщает неверные данные.", misconception: "Расхождение 2x+ между synthetic и field данными — нормально и ожидаемо — отражает разнообразие реальных устройств, не мисконфигурацию." },
    { label: "Lighthouse включает third-party scripts; RUM нет.", misconception: "Оба меряют одну страницу; Lighthouse synthetic тесты часто пропускают third-party scripts, грузящиеся только в production-окружениях." },
    { label: "Lighthouse запускается на desktop; RUM только mobile.", misconception: "Lighthouse настраивается на mobile emulation. Gap — в разнообразии устройств и сетей, не desktop vs mobile." },
  ]}
/>

<Quiz
  id="06-quiz-3"
  lessonSlug="06-ci-enforcement"
  lang="ru"
  question="Команда ставит size-limit бюджет 100 КБ для homepage JS. Через месяц бюджет повышается до 150 КБ, потом 180 КБ, потом 200 КБ — каждый раз ради 'критичной' фичи. Какая структурная проблема и как исправить?"
  choices={[
    { label: "Budget inflation — каждое повышение кажется обоснованным в отдельности, но total неограничен. Fix: ежеквартальный ratchet, затягивающий бюджет на 10% в квартал, вынуждающий команду удалять старый код при добавлении нового.", correct: true },
    { label: "Бюджет был поставлен слишком низко изначально.", misconception: "Даже хорошо поставленный бюджет будет дрейфовать без downward pressure. Протокол бюджет-raise должен включать периодическое затягивание." },
    { label: "size-limit не тот инструмент — используй Lighthouse CI.", misconception: "Инструмент не проблема; любой инструмент можно обойти постоянным повышением лимита. Нужна корректура процесса governance." },
    { label: "Фичи не должны требовать bundle увеличений.", misconception: "Фичи легитимно требуют байты. Проблема в том, что ratchet никогда не затягивается — старые фичи никогда не чистятся." },
  ]}
/>

</Explanation>

<FlowDiagram
  label="CI size-budget gate: измерить bundle каждого PR и завалить его при превышении"
  caption="size-limit запускается после сборки, читает размеры gzip'd-чанков и сравнивает с per-route бюджетом. Под бюджетом — merge; сверх — PR fail, пока не split, не обоснован или бюджет явно не поднят."
  hue="var(--d-hardware)"
  nodes={[
    { id: "build", label: "Build", sub: "PR pipeline", col: 0, row: 1 },
    { id: "measure", label: "Измерить размер", sub: "gzip'd chunks", col: 1, row: 1 },
    { id: "compare", label: "Сравнить с бюджетом", sub: "size-limit", col: 2, row: 1 },
    { id: "pass", label: "Pass → merge", sub: "под бюджетом", col: 3, row: 0 },
    { id: "fail", label: "Fail PR", sub: "сверх бюджета", col: 3, row: 2 },
  ]}
  edges={[
    { from: "build", to: "measure" },
    { from: "measure", to: "compare" },
    { from: "compare", to: "pass", label: "≤ limit" },
    { from: "compare", to: "fail", label: "> limit" },
  ]}
/>

<KeyTakeaway>size-limit в CI делает каждый over-budget PR fail'ящим — конвертирует бюджет из аспирационного в enforced. Bundle analyzer показывает, что внутри каждого chunk. RUM алертит при production-дрейфе, который CI пропускает. Протокол бюджет-raise делает каждое изменение лимита явным и проверенным.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="06-retrieval"
  lang="ru"
  questions={[
    { q: "Опиши полный bundle-budget enforcement стек: PR time, merge и production.", a: "PR time: size-limit запускается после build, fail'ит PR если любой per-route лимит превышен. Bundle analyzer доступен для расследования. Merge: Lighthouse CI запускает полный синтетический CWV audit на main. Production: RUM трекает P75 LCP/INP per route, per device class; алерт при регрессии > 20% за 24 часа. Вместе: CI ловит регрессии перед мержем; RUM ловит то, что synthetic пропускает в реальном production-трафике." },
    { q: "Почему Lighthouse CI иногда показывает Good LCP, пока production RUM показывает Poor LCP для того же route?", a: "Lighthouse запускает синтетический тест с фиксированным throttling на тест-машине. Production-пользователи охватывают много device-классов (low-end, mid-range, high-end), типов сетей (3G, 4G, 5G, WiFi) и географий. Реальный P75 LCP отражает всё распределение; synthetic показывает одну точку. Lighthouse ловит регрессии, вносимые code changes; RUM ловит полный real-user impact." },
  ]}
/>

<Recap lang="ru">
Bundle budget без CI-gate — предложение. size-limit, bundlewatch или Lighthouse CI делают каждый over-budget PR fail'ящим автоматически. Bundle analyzer обеспечивает видимость понять, что внутри каждого chunk и где резать. RUM замыкает цикл в production, ловя медленные регрессии и device-class проблемы, которые synthetic тесты пропускают. Протокол бюджет-raise гарантирует явность и проверку каждого изменения лимита. Следующий и последний урок рассматривает senior-уровень: JIT-пайплайн V8 и как HTTP/3 resource priorities взаимодействуют с bundle delivery. Теперь, когда видишь, что размер bundle незаметно растёт неделю за неделей, у тебя есть полный стек: CI до мержа, RUM после деплоя, и протокол raise, делающий каждое увеличение осознанным.
</Recap>
