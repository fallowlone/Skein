---
concepts:
- bundle-analysis
- code-splitting
- third-party-mitigation
- ci-budget-gate
- rum
estMin: 240
lang: ru
level: senior
order: 10
prereqs: []
slug: project
sources:
- https://web.dev/articles/granular-chunking-nextgen
- https://nextjs.org/docs/app/building-your-application/optimizing/bundle-analyzer
status: ready
summary: Практический проект — проанализируй, сплитни и заговерни раздутый bundle роута под per-route budget, затем зафиксируй выигрыш CI-gate'ом и RUM-дашбордом, доказывая каждый шаг числами до/после.
title: 'Bundle budgets: затащи роут под budget и удержи его'
track: performance
unit: 07-bundle-budgets
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import ProjectBrief from "~/components/pedagogy/ProjectBrief.astro";

<Hook>
Читать про bundle budgets — не то же самое, что затащить реальный роут под бюджет. Возьми раздутое приложение, спрофилируй то, что юзер реально качает, применяй иерархию сокращения, пока роут не влезет в свой budget, затем заставь budget держаться CI-gate'ом и RUM-панелью — с доказательством на каждом шаге.
</Hook>

<Goal lang="ru">
Преврати ментальную модель юнита в воспроизводимый инженерный цикл: проанализируй bundle, режь байты с критического пути в порядке приоритета, нейтрализуй налог third-party, форсируй budget в CI и подтверди реальный выигрыш числами Core Web Vitals до/после.
</Goal>

<ProjectBrief
  id="07-bundle-budgets-project"
  lessonSlug="07-bundle-budgets"
  lang="ru"
  objective="Возьми роут, поставляющий негабаритный JS bundle (своё приложение или стартер ниже), и затащи его под обоснованный per-route budget — доказывая каждое сокращение анализатором и репрезентативными для поля числами LCP/INP, затем загейти его в CI, чтобы он не мог регрессировать."
  requirements={[
    "Выбери или построй реальный роут, поставляющий ≥ 500 KB несжатого JS — например, дашборд, импортирующий chart-либу, date picker, rich-text editor и одну полную utility-библиотеку (lodash, moment) на критическом пути.",
    "Прогони анализатор bundle (@next/bundle-analyzer, rollup-plugin-visualizer или webpack-bundle-analyzer). Зафиксируй baseline: gzip'нутый итог, топ-5 модулей по размеру и какой код чужих роутов утекает в чанк этого роута.",
    "Сними baseline-измерение при 4× CPU throttling на профиле mid-range mobile: LCP, INP и transfer + несжатый размер JS роута. Не меряй на ноутбуке без throttling.",
    "Применяй иерархию сокращения по порядку и документируй рычаг на каждый фикс: (1) route-split, чтобы код чужих роутов ушёл из этого чанка; (2) lazy-load (React.lazy / dynamic import со строковым литералом) каждый компонент > 30 KB не на первом пейнте, с prefetch на hover; (3) tree-shake — замени CJS-импорты на ESM/cherry-picked (lodash-es или lodash/x, date-fns вместо moment), почини любой export* barrel; (4) vendor-split стабильные либы в content-hashed чанк; (5) только затем включи static brotli.",
    "Проаудируй third-party скрипты: перечисли каждый внешний скрипт и его размер; убери хотя бы один с критического пути через interaction gating, PartyTown или server-side measurement; зафиксируй отвоёванное время main thread.",
    "Добавь CI-gate size-limit (или bundlewatch) с per-route лимитом, выставленным на новый размер, подключённый к падению PR и постингу комментария с дельтой. Продемонстрируй его падение на нарочно превышающем budget PR.",
    "Подними RUM-сигнал (beacon библиотеки web-vitals или платформа вроде Sentry/Datadog/Vercel), репортящий P75 LCP/INP для роута, в идеале с gzip'нутым размером bundle как кастомной метрикой рядом.",
    "Перемеряй под идентичным throttled-профилем и запиши числа 'после' рядом с 'до'; несжатый JS роута должен упасть ≥ 50%, а LCP/INP — войти в 'Good' или двинуться к нему.",
  ]}
  acceptance={[
    "Таблица до/после: gzip'нутый transfer, несжатый JS, LCP и INP — всё измерено под одним и тем же профилем 4× CPU-throttled mid-range mobile, не оценено и не с ноутбука без throttling.",
    "Treemap анализатора чётко показывает уменьшение чанка роута и пропавшие модули чужих роутов / мёртвые (перепроанализировано, а не предположено).",
    "Gate size-limit закоммичен и наглядно валит нарочно превышающий budget PR, затем проходит после того, как добавку сплитнули или budget формально подняли.",
    "Абзац-разбор: какой рычаг применён для каждого топового модуля и почему splitting/shaking встали выше compression и тюнинга.",
    "Хотя бы один third-party скрипт показан убранным с main thread, с временем main thread до/после, которое он занимал.",
  ]}
  stretch={[
    "Добавь док протокола budget-raise и квартальный храповик на 10%, чтобы budget ужимался со временем, а не дрейфовал вверх; покажи один проработанный raise-PR с обоснованием и sign-off.",
    "Подключи 103 Early Hints (или modulepreload) для критического чанка и шрифта и замерь дельту LCP на P95; проверь в Network-панели, что ресурс стартует до завершения HTML.",
    "Добавь SRI integrity-хэши на все third-party script-теги и CSP script-src allow-list, затем покажи скрипт с неперечисленного origin, отклонённый браузером.",
    "Проверь, что приоритеты HTTP/3 RFC 9218 учитываются end-to-end: осмотри колонку Priority Network-панели, и если CDN их игнорит, задокументируй misconfiguration и его замеренную стоимость для прихода критического чанка.",
  ]}
/>

<Recap lang="ru">
Это цикл, который ты будешь гонять на каждом раздутом роуте: сначала анализ, режь байты с критического пути в порядке приоритета (split, затем lazy-load, затем shake, затем vendor-split, затем compress), нейтрализуй налог third-party, форсируй budget CI-gate'ом и подтверждай числами Core Web Vitals до/после на throttled mobile-профиле — затем держи RUM, следящий за дрейфом, который синтетика упускает. Сделав это раз на реальном роуте, ты доводишь production-версию до уровня мышечной памяти.
</Recap>
