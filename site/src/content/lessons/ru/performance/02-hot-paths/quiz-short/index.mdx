---
concepts:
- symptom-vs-cause
- five-shapes
- parent-child-chain
- hardware-counters
- fix-verify-loop
- tail-latency
estMin: 14
lang: ru
level: senior
order: 8
prereqs: []
slug: quiz-short
sources:
- https://www.brendangregg.com/usemethod.html
- https://easyperf.net/blog/2019/02/09/Top-Down-performance-analysis-methodology
- https://github.com/google/pprof/blob/main/doc/README.md
status: ready
summary: Промпты на свободное припоминание по всему юниту горячих путей. Сначала ответь своими словами, затем раскрой модельный ответ и сравни.
title: 'Горячие пути: тест на свободное припоминание'
track: performance
unit: 02-hot-paths
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";

<Hook>
Припоминание лучше перечитывания. Для каждого промпта проговори или запиши полный ответ по памяти прежде, чем открыть модельный ответ — именно усилие припоминания закрепляет диагностический цикл.
</Hook>

<Goal lang="ru">
Реконструируй спину юнита, не подглядывая: почему широкий лист — лишь симптом, пять форм и их сигнатуры, как цепочка parent/child находит место фикса, когда решают аппаратные счётчики, цикл fix-and-verify и где на самом деле прячутся регрессии.
</Goal>

<RetrievalDrawer
  client:load
  id="02-hot-paths-short-retrieval"
  lang="ru"
  questions={[
    { q: "Почему назвать широкий лист недостаточно и что ещё надо прочитать прежде, чем выбирать фикс?", a: "Широкий лист называет, где накапливается время (симптом), а не почему (причина). Тот же широкий фрейм может означать пять разных проблем: функция делает реальную CPU-работу (фикс алгоритма), аллоцирует и триггерит GC (фикс аллокаций), трогает некешированную память и стопорит (фикс раскладки данных), ждёт лок (фикс конкуррентности) или делает дорогие syscall'ы (фикс батчинга/IO). У каждой — своё и взаимно бесполезное семейство фиксов: переписывание алгоритма ничего не даёт проблеме аллокаций, перестройка данных ничего не даёт локу. Поэтому прежде чем выбирать, прочитай parent chain (откуда зовётся — баг вызывающего или общий лист?), прочитай детей (настоящая стоимость на уровень ниже в callee?) и сними правильный вторичный профиль (alloc, off-CPU или аппаратные счётчики), чтобы классифицировать форму. Широкий фрейм открывает расследование; классификация его закрывает." },
    { q: "Перечисли пять форм hotspot'а, каждую с одной сигнатурой в профиле и её семейством фиксов.", a: "(1) CPU-bound алгоритмический: широкий self-time, IPC 2–4, узкие дети — чини лучшим алгоритмом, SIMD/векторизацией, специализацией горячего пути. (2) Allocation-bound: GC-фреймы (mallocgc, scanobject, malloc) широки у листа — чини пулингом/переиспользованием буферов, in-place мутацией, struct-of-arrays, преаллоцированными контейнерами. (3) Cache-bound (memory-bound): низкий IPC (<1) с высоким cache-miss rate несмотря на широкий CPU-фрейм — чини сменой раскладки данных (непрерывные массивы, SoA), порядком обхода, prefetch. (4) Lock-bound: широк в off-CPU/mutex-профиле, узок on-CPU — чини lock-free структурами, более гранулярными или шардированными локами, RW-локами, устранением общего состояния. (5) Syscall-bound: kernel-фреймы (read, write, recv, futex) на flame graph, off-CPU доминирует — чини батчингом syscall'ов, бо́льшими буферами, io_uring, mmap или удалением syscall'а. JIT-деопт — шестая форма на managed-рантаймах: фреймы интерпретатора/baseline там, где должен быть фрейм оптимизирующего компилятора — чини стабилизацией типов." },
    { q: "Как parent chain и child chain каждая находит, где применить фикс?", a: "Parent chain отвечает на 'откуда зовётся этот лист?'. Концентрированный fan-in (один доминирующий родитель) означает, что лист молотят из одного места — чини вызывающего (звать реже, батчить, кешировать, убрать вызов); классический случай — json.Marshal широк под одним путём логгера — чини логгер, а не библиотеку. Распределённый fan-in (двадцать тонких родителей) означает, что лист — общая инфраструктура — чини сам лист, потому что оптимизация одного из двадцати вызывающих оставляет остальных неизменными, а маленький выигрыш на листе усиливается по всем путям. Child chain отвечает на 'функция делает работу или делегирует?'. Высокий self-time с узкими детьми означает, что работа здесь — чини внутри функции. Тонкий self-time с широким cum-time означает диспетчер — стоимость на уровень ниже в callee. 'Граница осмысленной работы' — это уровень, где cum-time резко падает при подъёме вверх; чини внутри этой границы, никогда в диспетчеризации над ней." },
    { q: "Когда тянуться к аппаратным счётчикам вместо одного чтения flame graph, и что они раскрывают?", a: "Тянись к счётчикам всякий раз, когда широкий лист может быть либо compute-bound, либо memory-bound — flame graph показывает, что функция широка (доля CPU-времени), но не может сказать, ретайрили ли эти циклы инструкции или стопорили на памяти. perf stat (или VTune / AMD uProf / toplev) даёт IPC и cache-miss rate, делая различие механическим: IPC 2–4 с низким miss rate — compute-bound (алгоритм/SIMD); IPC <1 с высоким L3/DRAM miss rate — memory-bound (раскладка данных). TMA от Intel уточняет это в четыре корзины — Retiring (реальная работа), Bad Speculation (branch mispredict → устранение ветвей), Front-End Bound (стопоры fetch/decode → сокращение размера кода) и Back-End Bound (Memory-Bound → раскладка данных, Core-Bound → SIMD / разрыв цепочек зависимостей). Счётчики также вскрывают формы, видимые только через железо: false sharing показывает IPC 0.3–0.6 с экстремальным miss rate и XSNP_HITM, ухудшаясь с числом потоков. Без счётчиков команды переписывают нормальные алгоритмы, которые на деле стопорили на DRAM." },
    { q: "Сформулируй цикл fix-and-verify и что означает каждый из двух его исходов-провалов.", a: "Пять шагов: (1) назови hotspot и классифицируй в форму; (2) выбери подходящее категориальное семейство фиксов; (3) напиши только предсказанное изменение — без scope creep; (4) сними свежий профиль под той же нагрузкой и сделай diff против baseline; (5) проверь ОБА: что локальный фрейм сузился И что headline-метрика (p99, пропускная способность, CPU%) сдвинулась. Проверка только локального фрейма доказывает, что функция изменилась, но не что система выиграла; проверка только headline доказывает, что что-то улучшилось, но не что и почему. Два режима провала: если фрейм сузился, а headline не сдвинулся — второе узкое место было замаскировано первым — сними новый профиль и атакуй то, что теперь шире всего (это следующая итерация, а не провал). Если headline сдвинулся, а фрейм не сузился — фикс сработал через непредсказанный побочный эффект — разберись в реальном механизме прежде, чем катить. Выигрыши микробенчмарка — предсказания; diff production-профиля — вердикт." },
    { q: "Почему регрессии горячих путей прячутся в хвостовой задержке, а не в среднем CPU%, и как их ловить?", a: "Многие патологии горячих путей срабатывают периодически, а не равномерно: паузы GC, всплески lock contention и циклы JIT-деопта бьют по малой доле запросов. Функция, добавляющая 200 мс к медленнейшим 0.1% запросов, но лишь 0.2 мс к среднему, невидима на дашборде CPU%, который есть среднее — он остаётся плоским, пока хвост раздувается. Ловят это пер-функциональные гистограммы задержки, нарезанные по перцентилям (p99/p99.9), а не общий CPU%: инструменты вроде Pyroscope, Datadog Continuous Profiling и Honeycomb позволяют фильтровать flame graph до медленнейшего 1% запросов. Фрейм, чья ширина p99.9 выросла в 3 раза, пока медиана осталась плоской, — это регресс, хотя ни одна headline-метрика не сдвинулась. Это связано с saturation в методе USE — растущий хвост на стабильном среднем — опережающий индикатор, срабатывающий за недели до SLO-алертов." },
  ]}
/>

<Recap lang="ru">
Если ты реконструировал каждый ответ по памяти, ты держишь диагностический цикл юнита: широкий лист — это симптом, а не причина; классифицируй его в одну из пяти форм (плюс JIT-деопт и случаи, видимые только через счётчики — false sharing, native bridge); parent chain решает caller-fix против leaf-fix, а child chain решает этот-уровень против на-уровень-ниже; аппаратные счётчики и TMA разрешают неоднозначность compute-vs-memory; цикл fix-and-verify требует доказательства local-и-headline; а регрессии прячутся в хвосте, а не в среднем. Сначала диагноз, потом фикс одной вещи, докажи его.
</Recap>
