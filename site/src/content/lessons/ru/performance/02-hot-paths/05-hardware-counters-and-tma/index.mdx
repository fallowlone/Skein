---
slug: 05-hardware-counters-and-tma
lang: ru
track: performance
unit: 02-hot-paths
order: 5
title: "Аппаратные счётчики и Intel TMA: диагностика подкатегорий"
summary: "Аппаратные счётчики производительности отличают compute-bound от memory-bound, когда оба выглядят одинаково широкими. Фреймворк Intel TMA привязывает каждый такт CPU к конкретному микроархитектурному ресурсу."
estMin: 20
status: ready
lessonType: topic
level: senior
prereqs: ["performance/02-hot-paths/02-five-shapes-of-hotspot"]
deepensInto: []
spiral: ["latency"]
concepts: ["IPC", "hardware-counters", "TMA", "cache-miss", "branch-mispredict", "front-end-bound", "back-end-bound", "perf-stat", "vtune"]
sources:
  - https://www.intel.com/content/www/us/en/docs/vtune-profiler/user-guide/2024-2/cpu-microarchitecture-analysis.html
  - https://easyperf.net/blog/2019/02/09/Top-Down-performance-analysis-methodology
  - https://www.brendangregg.com/usemethod.html
---

import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DebugLog from "~/components/pedagogy/DebugLog.astro";
import RFCQuiz from "~/components/pedagogy/RFCQuiz.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";

<Hook>
Flame graph называет горячую функцию. Два инженера спорят: один говорит «перепишем алгоритм», другой — «исправим раскладку памяти». На уровне flame graph оба фрейма выглядят одинаково. Запуск `perf stat -e instructions,cycles,cache-misses` против функции решает спор за 30 секунд: IPC 0.4, cache-miss rate 18%. Побеждает раскладка памяти. Изменение алгоритма потратило бы спринт впустую.
</Hook>

<Crux>IPC ниже 1 — memory-bound; выше 2 — compute-bound. Intel TMA привязывает каждый такт к конкретному микроархитектурному ресурсу.</Crux>

<Explanation>

## Аппаратные счётчики: второй проход

Flame graph называет функцию. Аппаратные счётчики производительности показывают, что функция делала внутри CPU. `perf stat -e cycles,instructions,cache-misses,branch-misses` на том же горячем листе даёт IPC, miss rate и типы stall.

- **Широкий фрейм с IPC 3.0:** compute-bound. CPU выполняет алгоритм. Семейство исправлений: алгоритм, SIMD, специализация.
- **Широкий фрейм с IPC 0.4 и cache-miss rate 15%:** memory-bound. CPU ждёт RAM. Семейство исправлений: изменение раскладки данных.

На flame graph одинаковая ширина — противоположные исправления. Аппаратные счётчики — это второй диагностический проход, предотвращающий оптимизацию не тем инструментом на нетривиальных горячих путях.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Показания счётчиков</th>
        <th class="border border-border px-3 py-2 text-left">Категория</th>
        <th class="border border-border px-3 py-2 text-left">Семейство исправлений</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">IPC 2–4, низкий cache-miss rate</td>
        <td class="border border-border px-3 py-2">Compute-bound (CPU-bound)</td>
        <td class="border border-border px-3 py-2">Лучший алгоритм, векторизация (SIMD)</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">IPC &lt;1, высокий cache-miss rate</td>
        <td class="border border-border px-3 py-2">Memory-bound (cache-bound)</td>
        <td class="border border-border px-3 py-2">Раскладка данных (SoA, непрерывная), порядок обхода</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Высокий branch-miss rate</td>
        <td class="border border-border px-3 py-2">Bad speculation</td>
        <td class="border border-border px-3 py-2">Устранение ветвлений, branchless-код, сортировка входных данных</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Высокий stall cycles, низкий instructions</td>
        <td class="border border-border px-3 py-2">Front-end bound (выборка/декодирование инструкций)</td>
        <td class="border border-border px-3 py-2">Уменьшение размера кода, оптимизация instruction cache</td>
      </tr>
    </tbody>
  </table>
</div>

## Intel TMA: строгая таксономия

Модель пяти форм — рабочее приближение. Строгая версия — Intel Top-Down Microarchitecture Analysis (TMA), формализованная в Intel Optimization Manual и доступная через VTune, Linux perf (через `toplev.py`) и аналог AMD uProf.

TMA классифицирует каждый такт CPU в четыре верхнеуровневых корзины:

- **Retiring** (~25–50% на оптимизированном коде): реальная работа — CPU выполнял полезные инструкции.
- **Bad Speculation** (~5–15%): промах предсказателя ветвлений — конвейер сброшен, инструкции отброшены.
- **Front-End Bound** (~5–15%): stall выборки или декодирования инструкций — CPU не успевает заполнять конвейер новыми инструкциями.
- **Back-End Bound** (~30–60% на типичных нагрузках): stall памяти или вычислительных ресурсов.

Back-End Bound делится дальше:
- **Memory Bound** → L1 Bound, L2 Bound, L3 Bound, DRAM Bound, Store Bound
- **Core Bound** (вычислительные порты, цепочки зависимостей, делители с большой латентностью)

Каскад точно указывает, на каком ресурсе CPU голодает на горячем пути:
- DRAM-bound → исправление раскладки данных
- Bad Speculation → устранение ветвлений
- Front-End Bound → уменьшение размера кода
- Core Bound → настоящее алгоритмическое переосмысление или SIMD

Для senior performance-работы на критических сервисах TMA — диагностика с наивысшим разрешением. Команды, поставляющие latency-sensitive инфраструктуру (HFT, database engines, kernel hot paths), считают его стандартом.

<Inset kind="why" lang="ru">
Скрипт Linux `toplev.py` реализует TMA с помощью perf-событий на любом современном процессоре Intel. Он автоматически обходит дерево TMA и выводит, какая корзина доминирует. Типичный вызов: `toplev.py --core S0-C0 -l2 sleep 5`. Вывод напрямую соответствует четырём корзинам и подкорзинам и называет, какой аппаратный ресурс является узким местом.
</Inset>

<DebugLog
  id="sr-debug-hw"
  pieceSlug="05-hardware-counters-and-tma"
  lang="ru"
  title="Читать вывод аппаратных счётчиков для диагностики memory-bound пути"
  output={`# perf stat -e cycles,instructions,cache-misses,LLC-load-misses ./service --bench feed-rank

   8,400,000,000  cycles
   3,360,000,000  instructions          #  0.40 insns per cycle (IPC)
     900,000,000  cache-misses          # 10.7% of all memory refs
     700,000,000  LLC-load-misses       # 78% of cache misses miss L3 too

 # Hot function from flame graph: score_embeddings()
 # Self-time: 42% CPU
 # IPC: 0.40   ← CPU stalled 60% of the time
 # L3 miss rate: very high — going to DRAM on most accesses`}
  outputLang="log"
  question="IPC 0.40 и 78% промахов кеша достигают DRAM. Какая это корзина TMA и на какое семейство исправлений она указывает?"
  hint="IPC значительно ниже 1, L3 miss доминирует. Back-End Bound или Memory Bound?"
  answer="TMA: Back-End Bound → Memory Bound → DRAM Bound. CPU ждёт данные из основной памяти почти на каждом обращении. Алгоритм не важен — узким местом является латентность памяти, а не пропускная способность вычислений. Семейство исправлений: реструктурировать раскладку данных эмбеддингов. Если эмбеддинги хранятся как array-of-structs (одна запись с множеством полей), а цикл оценки читает только 2–3 поля на элемент — переключиться на struct-of-arrays: хранить каждое используемое поле в непрерывном массиве, чтобы prefetcher мог их стримить. Ожидаемый результат: L3 miss rate снижается с 78% до менее 5%, IPC растёт до 2–3, доля функции в CPU падает с 42% до менее 15%. LinkedIn в 2024 году провёл именно такое преобразование в feed-ranking сервисе: реструктурировал раскладку эмбеддингов с AoS на SoA, латентность снизилась на 35%. Алгоритмическая переработка этой функции дала бы около нуля — CPU не выполнял алгоритмическую работу, он ждал данные."
/>

<TradeoffMatrix
  id="sr-tradeoff-1"
  pieceSlug="05-hardware-counters-and-tma"
  lang="ru"
  prompt="Горячий leaf — JSON-сериализация при 28% CPU. У команды четыре варианта. Выберите senior-выбор."
  options={[
    { name: "Сначала диагностировать (allocation profile + parent chain), затем выбрать подходящее исправление", summary: "Категорийная атака.", correct: true, justification: "JSON-сериализация может быть CPU-bound (парсинг структуры), allocation-bound (построение выходных строк) или call-pattern-bound (вызывается повторно, когда достаточно одного вызова). У каждой категории своё семейство исправлений. Диагностика занимает 5 минут; выбрать правильное исправление; проверить diff. Это senior-рефлекс." },
    { name: "Переключиться на simdjson / orjson / jsonb — известные быстрые библиотеки", summary: "Взять заведомо быстрый инструмент.", justification: "Библиотеки помогают, когда узким местом является CPU-парсинг; они не помогают, если стоимость — это аллокация или частота вызовов. Иногда правильный ответ, но только ПОСЛЕ подтверждения категории диагностикой. Слепой выбор рискует получить меньший выигрыш, чем ожидалось." },
    { name: "Предкодировать payload на этапе записи, кэшировать байты", summary: "Перенести работу в более дешёвое время.", justification: "Отлично, когда один и тот же payload кодируется много раз; бесполезно, когда payload меняется для каждого запроса. Использовать, когда диагностика показывает повторное кодирование стабильных данных; пропускать для высоковариативных payload." },
    { name: "Убрать поля из схемы ответа", summary: "Уменьшить сам объём работы.", justification: "Всегда стоит рассмотреть — самая дешёвая сериализация та, которую не делаешь. Лучше всего, когда продукт может принять меньший payload; плохо, когда стоимость платится за поля, которые клиент действительно использует. Должно быть в диагностическом чеклисте, но не является ПЕРВЫМ шагом." },
  ]}
/>

<RFCQuiz
  id="sr-rfc-1"
  pieceSlug="05-hardware-counters-and-tma"
  lang="ru"
  question="Где формализован строгий фреймворк Top-Down Microarchitecture Analysis (TMA) — Retiring / Bad Speculation / Front-End Bound / Back-End Bound — и какой инструмент предоставляет его напрямую?"
  choices={[
    { rfc: "Intel Optimization Reference Manual + Intel VTune Profiler", title: "TMA формализован в Intel Optimization Reference Manual (глава об анализе микроархитектуры) и предоставляется напрямую через VTune как тип анализа 'Microarchitecture Exploration'. Linux perf через скрипт toplev.py также реализует TMA, как и AMD uProf с аналогичным каскадом.", correct: true },
    { rfc: "Спецификация POSIX threads", title: "POSIX threads — это API для переносимой работы с потоками, а не анализ микроархитектуры." },
    { rfc: "Только Linux perf manual", title: "Linux perf предоставляет сырые счётчики, но сам TMA берётся из методологии Intel; perf может применять TMA через обёртки toplev." },
    { rfc: "Статья ACM Queue 2016 о flame graphs", title: "Та статья формализует flame graphs (визуализацию), а не TMA (методологию классификации тактов)." },
  ]}
/>

<Quiz
  id="05-hardware-counters-and-tma-quiz-1"
  lessonSlug="05-hardware-counters-and-tma"
  lang="ru"
  question="Горячий путь показал функцию X при 25% CPU. После исправления она упала до 5%. Общий CPU% остался прежним. Каково наиболее вероятное системное объяснение?"
  choices={[
    { label: "CPU, освобождённый исправлением функции X, был поглощён другой функцией, стоимость которой ограничивалась предыдущим узким местом — типично, когда планировщик был насыщен. Общий CPU остаётся прежним, потому что система работает настолько, насколько может; вместо этого выросла пропускная способность.", correct: true },
    { label: "Исправление не было задеплоено", misconception: "Локальный сдвиг профиля подтверждает, что исправление применено; общий CPU оставшийся прежним — это системный эффект, а не ошибка измерения." },
    { label: "Что-то не так с профилировщиком", misconception: "Профилировщик точно показывает новое распределение; прежний общий CPU — это системный феномен, а не ошибка измерения." },
    { label: "Исправление вызвало утечку памяти", misconception: "Утечки памяти вызывают рост RSS, а не прежний CPU% после локального CPU-выигрыша." },
  ]}
/>

</Explanation>

<KeyTakeaway>Аппаратные счётчики разграничивают compute-bound и memory-bound, когда flame graph не может. IPC ниже 1 при высоком cache-miss rate означает раскладку данных, а не алгоритм. Четырёхкорзинный каскад Intel TMA — Retiring, Bad Speculation, Front-End Bound, Back-End Bound — даёт диагностику с наивысшим разрешением для критических сервисов.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="05-hardware-counters-and-tma-retrieval"
  lang="ru"
  questions={[
    { q: "Когда нужно обращаться к аппаратным счётчикам вместо простого чтения flame graph, и что они показывают того, чего flame graph не может?", a: "Обращайтесь к аппаратным счётчикам, когда широкий leaf явно не соответствует одной из очевидных пяти категорий — особенно когда без дополнительной информации неясно, compute-bound или memory-bound. Flame graph показывает, какая функция широка (доля CPU-времени), но не может различить, выполнял ли CPU инструкции или ждал памяти в эти сэмплы. Аппаратные счётчики дают IPC (инструкции на такт) и cache-miss rate, что делает это различие механическим: IPC 2–4 с низким cache-miss — compute-bound; IPC менее 1 с высоким L3 miss rate — memory-bound. Без счётчиков команда могла бы переписывать вполне нормальный алгоритм, когда реальная проблема в том, что каждое обращение промахивается мимо L3 и идёт в DRAM. Производственные инструменты (Intel VTune, AMD uProf, Linux perf с TMA через toplev.py) предоставляют это на разных уровнях детализации. Senior-инженеры рефлекторно обращаются к счётчикам на любом неоднозначно выглядящем горячем листе — счётчики являются вторым диагностическим проходом, предотвращающим месяцы работы не тем инструментом на нетривиальных горячих путях." },
    { q: "Опишите четырёхкорзинный каскад TMA и исправление, на которое указывает каждая корзина.", a: "Retiring (25–50% на оптимизированном коде): реальная полезная работа. Высокий процент Retiring означает, что алгоритм эффективен и CPU его выполняет. Структурное исправление не нужно; SIMD или незначительная алгоритмическая настройка может выжать ещё несколько процентов. Bad Speculation (5–15%): промах предсказателя ветвлений вызвал сброс конвейера. Исправление: устранить ветвления (branchless-код, lookup tables, сортировка входных данных для предсказуемости ветвлений) или перевести условия на data-driven выборки. Front-End Bound (5–15%): выборка или декодирование инструкций стопорят конвейер — код слишком велик для instruction cache или имеет сложные паттерны декодирования. Исправление: уменьшить размер кода (избегать инлайнинга больших функций в горячий путь, использовать __cold для редко выполняемых путей), снизить давление на instruction cache. Back-End Bound (30–60%): подкорзина Memory Bound означает DRAM или cache stalls — исправлять раскладкой данных (SoA, непрерывные массивы, prefetch hints). Подкорзина Core Bound означает, что вычислительный ресурс является узким местом (execution ports, цепочки зависимостей, целочисленные делители с большой латентностью) — исправлять SIMD, разрывом цепочек зависимостей, заменой деления на умножение на обратное." },
  ]}
/>

<Recap lang="ru">
Аппаратные счётчики производительности — это второй диагностический проход, отличающий compute-bound от memory-bound горячих путей, когда оба выглядят одинаково на flame graph. IPC ниже 1 с высоким L3 miss rate указывает на исправления раскладки данных; IPC 2–4 с низким miss rate — на алгоритмические исправления. Фреймворк Intel TMA каскадируется от четырёх верхнеуровневых корзин до конкретных подресурсов (L1-bound, DRAM-bound, core-bound), давая наиболее точную диагностику из доступных. Для latency-sensitive production-сервисов запуск perf stat или VTune на неоднозначных горячих листах является стандартной практикой перед тем, как тратить инженерное время на исправление.
</Recap>
