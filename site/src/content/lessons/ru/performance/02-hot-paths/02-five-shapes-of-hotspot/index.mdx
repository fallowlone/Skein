---
concepts:
- cpu-bound
- allocation-bound
- cache-bound
- lock-bound
- syscall-bound
- IPC
- fix-family
deepensInto: []
estMin: 18
lang: ru
lessonType: topic
level: middle
order: 2
prereqs:
- performance/02-hot-paths/01-what-makes-a-hot-path
slug: 02-five-shapes-of-hotspot
sources:
- https://www.brendangregg.com/usemethod.html
- https://easyperf.net/blog/2019/02/09/Top-Down-performance-analysis-methodology
- https://www.intel.com/content/www/us/en/docs/vtune-profiler/user-guide/2024-2/cpu-microarchitecture-analysis.html
spiral:
- latency
status: ready
summary: У каждой из пяти категорий hotspot''''а есть характерная сигнатура в профиле
  и соответствующее семейство правок. Выбрать неверное семейство — значит потратить
  усилия впустую.
title: 'Пять форм hotspot''''''''а: CPU, аллокации, кэш, лок, syscall'
track: performance
unit: 02-hot-paths
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import NumbersCard from "~/components/layout/NumbersCard.astro";
import Misconception from "~/components/layout/Misconception.astro";
import FadedExample from "~/components/pedagogy/FadedExample.tsx";

<Hook>
Два широких leaf'а во flame graph на первый взгляд выглядят одинаково. Один требует лучшего алгоритма. Второй требует переиспользования буферов. Применить лучший алгоритм к allocation-bound-пути — значит получить 1,05x вместо предсказанных 3x. Два часа работы ради пяти процентов. Категория определяет toolbox.
</Hook>

<Crux>Пять форм — CPU, аллокации, кэш, лок, syscall — у каждой своя сигнатура в профиле и своё семейство правок. Диагностируй форму первым делом.</Crux>

<Explanation>

## Пять категорий

Широкий leaf подходит под одну из пяти категорий. Прочтение второго слоя профиля — не только «какая функция», но «что функция делает» — даёт классификацию.

### 1. CPU-bound алгоритмический

Функция исполняет много инструкций. CPU исполняет алгоритм.

**Сигнатура:** большой self-time, узкие children, высокий instructions-per-second, IPC в диапазоне 2–4. В CPU flame graph leaf занимает настоящую ширину без GC или kernel-фреймов рядом.

**Семейство правок:** лучший алгоритм, векторизация (SIMD), inline-pragma, специализация hot-path'а для частого случая.

### 2. Allocation-bound

Функция (или её caller) так много аллоцирует, что сборщик мусора доминирует над wall-time.

**Сигнатура:** `runtime.scanobject`, `gc`, `mallocgc` или `malloc` появляются широкими рядом с горячим leaf'ом. CPU-профиль винит GC-machinery, не application-логику. Переключись на allocation-профиль, чтобы найти аллокатор на стороне приложения.

**Семейство правок:** пулинг объектов, переиспользование буферов (`sync.Pool`), in-place мутация, struct-of-arrays, предварительное выделение контейнеров нужного размера.

### 3. Cache-bound

Функция трогает память по паттерну, который аппаратный prefetcher не может предсказать. CPU стопорится, ожидая данные из RAM вместо L1/L2.

**Сигнатура:** низкий IPC (&lt;1), высокий cache-miss rate (15%+), низкий instructions-per-second несмотря на широкий CPU-фрейм. Аппаратные счётчики подтверждают тип stall (L3 miss, DRAM stall).

**Семейство правок:** смена data-layout'а (contiguous arrays вместо pointer-chased linked lists, struct-of-arrays вместо array-of-structs), смена порядка итерации для улучшения spatial locality, prefetch-подсказки.

### 4. Lock-bound

Функция тратит время в ожидании mutex'а или канала.

**Сигнатура:** широкая в mutex/block/off-CPU профиле, узкая в CPU-профиле. Функция off-CPU, не работает. Wall-clock time высокий; CPU time низкий.

**Семейство правок:** lock-free структуры данных, более гранулярные локи, шардированное состояние, read-write локи для read-heavy-путей, eventual consistency для устранения shared state.

### 5. Syscall-bound

Функция тратит время в ядре — чтение, запись, сетевой I/O или ожидание на `futex`.

**Сигнатура:** kernel-фреймы (`read`, `write`, `recv`, `futex`) видны во flame graph с поддержкой kernel-символов. Доминирует off-CPU время. Может проявляться как частые узкие kernel-entry вместо одного широкого leaf'а.

**Семейство правок:** батчинг syscall'ов (один `writev` вместо десяти `write`), большие I/O-буферы, `io_uring` для async kernel I/O, memory-mapped I/O, полное устранение syscall'а там, где данные могут оставаться в user space.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Категория</th>
        <th class="border border-border px-3 py-2 text-left">Сигнатура в профиле</th>
        <th class="border border-border px-3 py-2 text-left">Семейство правок</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">CPU-bound</td>
        <td class="border border-border px-3 py-2">Высокий self-time, IPC 2–4</td>
        <td class="border border-border px-3 py-2">Лучший алгоритм, SIMD, специализация</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Allocation-bound</td>
        <td class="border border-border px-3 py-2">Широкие GC-фреймы (mallocgc, scanobject)</td>
        <td class="border border-border px-3 py-2">Пулинг, переиспользование буферов, SoA</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Cache-bound</td>
        <td class="border border-border px-3 py-2">IPC &lt;1, высокий cache-miss rate</td>
        <td class="border border-border px-3 py-2">Смена data layout, contiguous arrays</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Lock-bound</td>
        <td class="border border-border px-3 py-2">Широкий off-CPU, узкий on-CPU</td>
        <td class="border border-border px-3 py-2">Lock-free, шардирование, мелкие локи</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Syscall-bound</td>
        <td class="border border-border px-3 py-2">Kernel-фреймы во flame graph</td>
        <td class="border border-border px-3 py-2">Батчинг syscall'ов, io_uring, буферы</td>
      </tr>
    </tbody>
  </table>
</div>

<NumbersCard
  id="card-hot-numbers"
  title="Числа диагностики hot path'ов"
  stats={[
    { label: "Типовой IPC compute-bound кода", value: "2–4 инстр./цикл" },
    { label: "Типовой IPC memory-bound кода", value: "0,3–0,8 инстр./цикл" },
    { label: "Штраф L1 cache miss", value: "~5 циклов" },
    { label: "Штраф L3 cache miss → DRAM", value: "~150–300 циклов" },
    { label: "Штраф branch mispredict", value: "~15–25 циклов" },
    { label: "Цена одного deopt + recompile (V8)", value: "~10–100 мкс" },
    { label: "Round-trip syscall", value: "~1–5 мкс" },
    { label: "futex lock contention wakeup", value: "~5–50 мкс" },
  ]}
/>

<Misconception id="mc-leaf-blame">
  <p><strong>«Широкий leaf И ЕСТЬ bottleneck — переписать его».</strong> Широкий leaf — симптом; причина может быть в caller'е (вызывают слишком часто), в callee (реальная цена ниже) или в паттерне работы (аллокации, лок, syscall). Прочти окрестности до того, как тянуться к правке.</p>
</Misconception>

## Классификация hotspot'а на практике

<FadedExample
  client:load
  id="fe-hot-1"
  pieceSlug="02-five-shapes-of-hotspot"
  lang="ru"
  title="Классифицируй hotspot до выбора правки"
  steps={{
    solved: (
      <div class="space-y-3 text-sm">
        <p><strong>Ситуация.</strong> CPU-профиль показывает функцию <code>renderTemplate</code> на 28% self-time. Bea хочет сменить шаблонизатор. Sven сначала смотрит.</p>
        <pre class="text-xs bg-zinc-100 p-2 rounded overflow-x-auto"><code>{`# 1. Прочти parent chain — pprof: top + tree
(pprof) top 5 --cum
    flat  flat%   cum%   функция
   0.8s   3.5%  28.0%   handleRequest    # cum: 28% через renderTemplate
   6.2s  28.0%  28.0%   renderTemplate   # лидер по self-time

# Один широкий caller (handleRequest) — концентрированный call-site.
# Иди ВНИЗ: цена в самой renderTemplate или в callee?

(pprof) list renderTemplate
  renderTemplate:
    line 42: for k, v := range data {
    line 43:    out = out + fmt.Sprintf("%s=%v;", k, v)  # 5.4s self-time
    line 44: }

# Строка 43 горячая. Профиль говорит self-time, но работа —
# конкатенация строк + Sprintf — это АЛЛОКАЦИИ, не CPU-bound.
# Переключись на allocation-профиль для подтверждения.

# 2. Allocation profile (go tool pprof -alloc_space)
(pprof) top 5
   flat  flat%   функция
   180MB  62%   fmt.Sprintf       # hotspot аллокаций подтверждён
    60MB  21%   strings.<concat>

# Диагноз: КАТЕГОРИЯ = allocation-bound, не CPU-bound алгоритмический.
# Семейство правок: pre-allocate буфер, использовать strings.Builder.
# Смена шаблонизатора поможет куда меньше, чем убрать Sprintf+concat.`}</code></pre>
        <p><strong>Правка:</strong> переписать строку 43 с <code>strings.Builder</code>, предварительно выделив размер под среднюю длину вывода. После пере-профилирования self-time <code>renderTemplate</code> падает до 6%, <code>fmt.Sprintf</code> исчезает из allocation-профиля, ширина GC-фрейма падает на 60%, p99 эндпоинта падает с 380 до 220 мс. Смена шаблонизатора (первый инстинкт) в лучшем случае сдвинула бы 28% в 2x (Amdahl ≤1,16x); реальная правка сдвинула в 4,7x (Amdahl 1,30x) и сжала GC.</p>
      </div>
    ),
    semi: {
      prompt: "Если горячая функция широкая в off-CPU профиле (не в CPU профиле), к какой категории она, скорее всего, относится?",
      blanks: [
        { id: "cat", expected: "lock", placeholder: "категория (одно слово)" },
      ],
    },
    blank: {
      prompt: "Почему диагностика категории до выбора правки экономит реальное инженерное время?",
      reveal: (
        <div class="space-y-2 text-sm">
          <p>У каждой категории своё семейство правок, и неверное семейство тратит всю работу впустую. Команда, переписывающая алгоритм при allocation-bound-bottleneck, получает маленький выигрыш и code-review-конфликт; команда, меняющая data layout при lock-bound, не получает ничего. Диагностика стоит минуты (ещё один capture профиля, прочитать нужный view); отсутствие диагностики стоит дней (неверная правка приземляется, не двигает метрику, команда начинает заново). Карта категорий превращает hot-path-работу из угадывания в дерево решений.</p>
        </div>
      ),
    },
  }}
  misconceptions={{
    "cat:cpu": "Если показывает в OFF-CPU профиле, функция ждёт, а не работает на CPU. Ожидание на mutex/канале = lock-bound; на диске/сети = syscall-bound.",
    "cat:io": "Ближе — но конкретная категория в пятикатегорийной таксономии: либо 'lock' (mutex/channel wait), либо 'syscall' (kernel-side I/O wait). 'io' — более широкое ведро.",
  }}
/>

<Quiz
  id="02-five-shapes-of-hotspot-quiz-1"
  lessonSlug="02-five-shapes-of-hotspot"
  lang="ru"
  question="Функция показывает IPC 0,4 и cache-miss rate 15%. Какова категория и какое семейство правок она предполагает?"
  choices={[
    { label: "Memory-bound (cache-bound). Семейство правок: смена data-layout (contiguous arrays, SoA), смена порядка итерации, prefetch-подсказки. Алгоритмическое переписывание само по себе не поможет, если новый алгоритм по-прежнему обращается к той же разрозненной памяти.", correct: true },
    { label: "CPU-bound. Правка: лучший алгоритм.", misconception: "Низкий IPC означает, что CPU стопорится в ожидании памяти, а не исполняет инструкции — алгоритмические изменения, не исправляющие паттерн обращений к памяти, всё равно будут стопориться." },
    { label: "Lock-bound. Правка: lock-free структуры.", misconception: "Lock wait не проявлялся бы как низкий IPC на самой функции; он был бы виден в off-CPU/block-профиле." },
    { label: "Allocation-bound. Правка: пулинг буферов.", misconception: "Аллокации проявляются как широкие GC-фреймы, а не как низкий IPC на самой функции." },
  ]}
/>

<Quiz
  id="02-five-shapes-of-hotspot-quiz-2"
  lessonSlug="02-five-shapes-of-hotspot"
  lang="ru"
  question="После правки локальный hotspot сжался на 60%, но p99 сервиса не изменился. Что скорее всего это означает?"
  choices={[
    { label: "Локальный выигрыш обнажил (или всегда сочетался с) вторую bottleneck, которую маскировала первая. Сними новый профиль и посмотри, что теперь самое широкое.", correct: true },
    { label: "Правка в действительности не задеплоилась", misconception: "Возможно, но профиль уже показывает, что правка локально приземлилась; вопрос — почему headline не сдвинулся." },
    { label: "Производительность случайна при большом масштабе", misconception: "Производительность детерминирована в масштабе; когда одно изменение профиля не двигает headline, у этого есть конкретная системная причина." },
    { label: "Изменился production-трафик", misconception: "Возможно, но исключено, если снимать под той же нагрузкой, что и baseline." },
  ]}
/>

</Explanation>

<KeyTakeaway>Пять категорий — CPU-bound, allocation-bound, cache-bound, lock-bound, syscall-bound — у каждой чёткая сигнатура в профиле и соответствующее семейство правок. Диагноз за пять минут предотвращает дни потраченных усилий на неверный toolbox.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="02-five-shapes-of-hotspot-retrieval"
  lang="ru"
  questions={[
    { q: "Разбери пять категорий hot path'ов с одним характерным признаком каждой в профиле и соответствующим семейством правок.", a: "CPU-bound алгоритмический: широкий self-time leaf, высокий IPC (2–4 инструкции/цикл), узкие children. Семейство правок: лучший алгоритм, векторизация, inline-pragma, специализация hot-path'а. Allocation-bound: GC-фреймы (mallocgc, scanobject, malloc, free) широкие рядом с горячим leaf'ом. Семейство правок: пулинг буферов, in-place мутация, struct-of-arrays, предварительный размер контейнеров. Cache-bound: низкий IPC (<1), высокий cache-miss rate, низкий instructions-per-second несмотря на широкий CPU-фрейм. Семейство правок: смена data-layout (contiguous, SoA), смена порядка итерации, prefetch-подсказки. Lock-bound: широкий в mutex/block/off-CPU профиле, узкий в CPU-профиле; функция ждёт, не работает. Семейство правок: lock-free структуры, более гранулярные локи, шардированное состояние, eventual consistency. Syscall-bound: kernel-фреймы видны во flame graph (read, write, recv, futex), часто доминируют в off-CPU. Семейство правок: батчинг syscall'ов, большие буферы, io_uring, mmap'd I/O, устранить syscall." },
    { q: "Go API показывает runtime.mallocgc на 18% и runtime.scanobject на 14% в CPU-профиле. Какова категория и каков следующий диагностический шаг?", a: "Allocation-bound. Application-код сам не представлен в этих GC-фреймах — GC платит за аллокации, которые делает application-код. Следующий шаг — переключиться на allocation-профиль (/debug/pprof/allocs), чтобы найти, какие application-функции ответственны за эти аллокации. CPU-профиль называет симптом (GC потребляет 32% CPU); allocation-профиль называет причину (кто аллоцирует). Без allocation-профиля можно оптимизировать не ту функцию." },
  ]}
/>

<Recap lang="ru">
Пять категорий hotspot'ов — CPU, аллокации, кэш, лок, syscall — у каждой чёткая сигнатура в профиле: IPC и self-time для CPU-bound, GC-фреймы для allocation-bound, низкий IPC с высоким cache-miss для cache-bound, широкий off-CPU но узкий on-CPU для lock-bound, kernel-фреймы для syscall-bound. Диагноз занимает минуты (снять нужный вторичный профиль, прочитать IPC или miss-rate); семейство правок следует механически. Выбрать неверное семейство — значит потратить всю работу впустую. Следующий урок разбирает, как читать parent и child chains, чтобы локализовать правку на верном уровне дерева вызовов.
</Recap>
