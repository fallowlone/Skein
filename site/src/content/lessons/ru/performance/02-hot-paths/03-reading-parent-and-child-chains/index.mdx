---
slug: 03-reading-parent-and-child-chains
lang: ru
track: performance
unit: 02-hot-paths
order: 3
title: "Чтение parent и child chains: где применять правку"
summary: "Широкий leaf с одним parent'ом — правь caller'а. Широкий leaf с двадцатью parent'ами — правь leaf. Граница self-time vs cum-time указывает, где работа, а где диспетчеризация."
estMin: 16
status: ready
lessonType: topic
level: middle
prereqs: ["performance/02-hot-paths/02-five-shapes-of-hotspot"]
deepensInto: []
spiral: ["latency"]
concepts: ["parent-chain", "fan-in", "fan-out", "self-time", "cum-time", "fix-layer", "bottom-up", "top-down"]
sources:
  - https://github.com/google/pprof/blob/main/doc/README.md
  - https://www.brendangregg.com/Articles/Netflix_Tech_Blog_Performance.pdf
  - https://github.com/grafana/pyroscope
---

import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import TraceScenario from "~/components/pedagogy/TraceScenario.astro";

<Hook>
Сеньор открывает pprof и видит json.Marshal на 28% CPU. Он не тянется за более быстрой JSON-библиотекой. Он проверяет parent chain. Один широкий parent: логгер запросов. Правка не в библиотеке — в логгере, который вызывает Marshal на каждом запросе независимо от уровня логирования.
</Hook>

<Crux>Parent chain: править leaf или caller'ов? Child chain: править callee? Оба прочтения обязательны до выбора места правки.</Crux>

<Explanation>

## Чтение parent chain

Parent chain отвечает: «откуда вызывается этот широкий leaf?»

**Концентрированный fan-in (один широкий parent-путь):** leaf вызывается многократно из одного места. Правь caller'а — вызывать реже, кэшировать результаты, батчить или устранить вызов.

**Распределённый fan-in (много узких parents):** leaf — это shared-инфраструктура, используемая многими caller'ами. Правь сам leaf, потому что оптимизация любого одного caller'а оставляет остальных девятнадцать неизменными.

Классический пример: `json.Marshal` широкий с одним широким путём от логгера — значит, проблема в логгере; не меняй JSON-библиотеку, чини логгер. Тот же `json.Marshal` широкий и вызываемый из двадцати handler'ов — значит, JSON и есть bottleneck; меняй библиотеку или pre-encode.

Production-grade pprof и Pyroscope позволяют переключаться между leaf-up и caller-down view одним кликом; использовать оба — сеньорная привычка.

## Чтение child chain (self-time vs cum-time)

Child chain отвечает: «эта функция делает работу сама или делегирует callee?»

**Leaf с большим self-time и узкими детьми:** функция делает работу сама. Правка внутри функции.

**Parent с thin self-time и широким cum-time:** это диспетчер. Цена — на уровень ниже в callee. Правь callee, не этот слой.

Правило чтения: иди от leaf'а с высоким self-time вниз по parent chain. Найди уровень, где cum-time резко падает при движении вверх. Этот уровень — «граница содержательной работы» — выше диспетчеризация, ниже исполнение. Правку применяй внутри этой границы.

Неверное определение этой границы — причина переписывания middleware, оборачивающего медленный DB-вызов, или оптимизации HTTP-роутеров, когда цена в теле handler'а.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Сигнал</th>
        <th class="border border-border px-3 py-2 text-left">Что означает</th>
        <th class="border border-border px-3 py-2 text-left">Уровень правки</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">Широкий leaf, один доминирующий parent</td>
        <td class="border border-border px-3 py-2">Концентрированный call-site</td>
        <td class="border border-border px-3 py-2">Caller (реже вызывать / батчить / кэшировать)</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Широкий leaf, много тонких parents</td>
        <td class="border border-border px-3 py-2">Shared-инфраструктура</td>
        <td class="border border-border px-3 py-2">Сам leaf</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Высокий self-time, узкие children</td>
        <td class="border border-border px-3 py-2">Функция делает работу сама</td>
        <td class="border border-border px-3 py-2">Внутри функции</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Thin self-time, широкий cum-time</td>
        <td class="border border-border px-3 py-2">Диспетчер</td>
        <td class="border border-border px-3 py-2">Внутри callee (уровнем ниже)</td>
      </tr>
    </tbody>
  </table>
</div>

## Амортизация fan-in

Когда одна функция вызывается из многих caller'ов, оптимизация leaf'а складывается по каждому caller'у. 30% локальное улучшение в shared-функции, используемой двадцатью путями, даёт 30% по каждому из путей.

Обратный случай — fan-out: один caller, бьющий по многим callees. Оптимизация каждого callee — локальное сокращение одного пути.

Сеньорный приём: смотри fan-in/fan-out shape до выбора leaf vs caller fix. Высокий fan-in (shared infra) — leaf fix амплифицируется. Высокий fan-out (один caller, много callees) — caller fix (батчинг, кэширование, устранение вызова) амплифицируется. Эта математика часто перевешивает выбор семейства правок: даже маленький fix на высоко-fan-in leaf'е может перебить большую правку на single-caller-пути.

## Bottom-up vs top-down views

pprof, Pyroscope и Intel VTune поддерживают два дополняющих друг друга view.

**Bottom-up:** ранжирование функций по self-time, leaf-first. Лучше для поиска одной функции, съедающей больше всего CPU.

**Top-down:** ранжирование функций по cum-time от корня, parent-first. Лучше для поиска подсистемы, ответственной за бо́льшую часть времени.

Сеньорная привычка чтения: сначала bottom-up — назвать горячий leaf, затем top-down — увидеть, какой entry-point его держит. Leaf, горячий под одним entry-point'ом — локализованная правка; leaf, горячий под многими — shared-инфраструктура.

<Inset kind="why" lang="ru">
Intel Top-Down Microarchitecture Analysis (TMA) расширяет это дальше, классифицируя каждый горячий фрейм в front-end bound, back-end bound, retiring или bad speculation. TMA разобран в уроке об аппаратных счётчиках. Чтение parent/child chain здесь — пресреквизит: TMA добавляет CPU-уровень поверх.
</Inset>

<TraceScenario
  id="mid-trace-1"
  pieceSlug="03-reading-parent-and-child-chains"
  lang="ru"
  scenario="Go API имеет p99 600 мс. Профиль показывает runtime.mallocgc на 18% и runtime.scanobject на 14%. Трассируй диагноз и правку."
  steps={[
    { prompt: "Шаг 1: runtime.mallocgc + runtime.scanobject = 32% CPU в GC-machinery. Какова категория?", reveal: "Allocation-bound. Application-код сам не представлен в этих фреймах — GC платит за аллокации, которые делает application. Переключись на allocation-профиль, чтобы найти, какие application-функции ответственны." },
    { prompt: "Шаг 2: переключись на /debug/pprof/allocs. Широкий leaf: json.Encode вызываемый из middleware/RequestLog. Что это говорит?", reveal: "Каждый запрос JSON-кодируется дважды — один раз для ответа, второй раз логгером запросов для записи в лог. Логгер — непреднамеренный аллокатор. Один payload, аллоцируется дважды на запрос." },
    { prompt: "Шаг 3: кандидаты на правку?", reveal: "(а) Убрать JSON-закодированное тело запроса из лог-строки целиком — логировать хэш или резюме. (б) Переиспользовать вывод response-энкодера (кодировать один раз, логировать тот же буфер). (в) Перейти на логгер, откладывающий сериализацию до прохождения level-filter (zerolog, zap с lazy fields). Выбирай (а), если тело в логах не нужно; (в), если нужно, но только для фильтруемых уровней." },
    { prompt: "Шаг 4: применена правка (а). Предсказанный эффект?", reveal: "Доля json.Encode в allocation-профиле должна упасть ~50% (один из двух вызовов устранён). GC-фреймы (mallocgc + scanobject) должны упасть с 32% до ~16%. CPU сервиса улучшится по Amdahl ≈ 1 / (1 − 0,16) ≈ 1,19x throughput. p99 должен упасть, потому что GC-паузы станут тоньше." },
    { prompt: "Шаг 5: профиль после правки показывает GC на 18%, а не предсказанные 16%. p99 упал с 600 до 480 мс. Диагноз?", reveal: "Правка приземлилась (GC упал с 32% до 18%, близко к предсказанию). Оставшиеся 18% — от других аллокаторов: возможно, самого response-энкодера или middleware-буферов. Выигрыш реальный; следующая итерация атакует следующий по ширине аллокатор. Цикл закрыт: диагностировали, починили, верифицировали, нашли следующее." },
  ]}
/>

<TraceScenario
  id="mid-trace-2"
  pieceSlug="03-reading-parent-and-child-chains"
  lang="ru"
  scenario="Node-сервис имеет широкий leaf JSON.parse на 24% CPU. Трассируй, почему наивная смена парсера может не помочь."
  steps={[
    { prompt: "Шаг 1: JSON.parse широкий. Первая проверка?", reveal: "Насколько большие парсируемые payload'ы и сколько раз parse вызывается на запрос? Маленький parse, вызываемый много раз, и гигантский parse, вызываемый один раз, требуют разных правок. Прочти parent chain и посчитай call-site'ы." },
    { prompt: "Шаг 2: parent chain показывает parse вызываемый из одного middleware: requestValidator, один раз на запрос. Средний payload ~50 KB. Что это означает?", reveal: "Каждый запрос платит за один большой parse. Цена доминируется обходом payload'а, а не накладными расходами на вызов parse. Более быстрый парсер может помочь, но доминирующая цена — сама работа парсинга 50 KB, ограниченная пропускной способностью памяти." },
    { prompt: "Шаг 3: какие ещё варианты до смены библиотеки?", reveal: "(а) Не парсить поля, которые validator не использует (lazy или streaming parse, извлекающий только нужные поля). (б) Перенести валидацию на уровень, который может использовать schema и пропускать нерелевантные данные. (в) Перейти на бинарный формат (Protobuf, MessagePack), чтобы цена parse была в 10 раз дешевле. (г) Предварительно валидировать на edge с более быстрым фильтром до handler'а. Смена JSON-библиотеки — вариант (д), worth measuring, но ниже по upside, чем (а)-(г)." },
    { prompt: "Шаг 4: команда выбирает (а) — переключиться на lazy field extraction. Предсказанный эффект?", reveal: "Lazy extraction пропускает поля, которые validator не использует. Предсказанная доля CPU: с 24% до 4–6%. Предсказанное улучшение p99: 1 / (1 − 0,20) ≈ 1,25x throughput. Предсказанное wall-clock на запрос: -18-20 мс." },
    { prompt: "Шаг 5: верификация?", reveal: "Пере-профилировать. Фрейм JSON.parse должен быть заменён фреймом нового парсера значительно меньшей ширины. Headline p99 должен упасть на предсказанную величину. Если нет — ищи новые горячие фреймы: предыдущий hotspot мог маскировать другой." },
  ]}
/>

<DragOrder
  id="03-reading-parent-and-child-chains-drag-1"
  lessonSlug="03-reading-parent-and-child-chains"
  lang="ru"
  prompt="Расставь шаги цикла атаки на hot path по порядку, как их выполняет сеньор-инженер:"
  items={[
    { id: "a", label: "Открой профиль и найди самый широкий leaf по self-time" },
    { id: "b", label: "Прочти parent chain (один caller или много?) и children (реальная цена уровнем ниже?)" },
    { id: "c", label: "Классифицируй hotspot: CPU, аллокации, кэш, лок, syscall или JIT deopt" },
    { id: "d", label: "Выбери категориальное семейство правок, соответствующее классификации" },
    { id: "e", label: "Напиши только предсказанное изменение, без scope creep" },
    { id: "f", label: "Сними свежий профиль под той же нагрузкой" },
    { id: "g", label: "Diff: локальный фрейм сжался И headline-метрика улучшилась? Оба условия обязательны для релиза" },
    { id: "h", label: "Если headline не сдвинулся, найди новый топ hotspot — он был маскирован первым" },
  ]}
  correctOrder={["a", "b", "c", "d", "e", "f", "g", "h"]}
/>

<Quiz
  id="03-reading-parent-and-child-chains-quiz-1"
  lessonSlug="03-reading-parent-and-child-chains"
  lang="ru"
  question="Широкая leaf-функция вызывается из двадцати разных parent-путей, каждый вносит тонкий вклад. Где применять правку?"
  choices={[
    { label: "Правь сам leaf — это shared-инфраструктура, используемая многими caller'ами. Даже маленькие улучшения там складываются по всем двадцати путям.", correct: true },
    { label: "Выбери одного caller'а и оптимизируй только его путь", misconception: "Оптимизация одного caller'а оставляет остальных девятнадцать неизменными; общая цена leaf'а доминирует." },
    { label: "Заинлайни leaf в каждый caller", misconception: "Инлайнинг в таком масштабе дублирует код и редко помогает исходной цене; цена в работе функции, не в накладных расходах вызова." },
    { label: "Добавь кэширование в одном caller'е", misconception: "Та же проблема — частичная правка в одном из двадцати путей." },
  ]}
/>

</Explanation>

<KeyTakeaway>Parent chain: один доминирующий parent — правь caller'а; много тонких parents — правь leaf. Child chain: высокий self-time — работа здесь; thin self-time с широким cum-time — работа уровнем ниже. Оба прочтения вместе локализуют правку на верном уровне.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="03-reading-parent-and-child-chains-retrieval"
  lang="ru"
  questions={[
    { q: "Почему важно читать и parent chain, И children при диагностике широкого leaf'а, и что каждое направление говорит?", a: "Parent chain говорит о концентрации call-site. Широкий leaf, достигаемый через один доминирующий parent-путь — leaf вызывается многократно из одного места; правь caller'а (вызывать реже, батчить, кэшировать, устранить вызов). Тот же широкий leaf через двадцать узких parent-путей — leaf является shared-инфраструктурой, используемой многими; правь сам leaf, потому что оптимизация любого одного caller'а оставляет остальных девятнадцать неизменными. Children chain говорит, делает ли функция свою работу или делегирует. Если self-time большой, а cum-time children маленький — функция делает работу; правка внутри функции. Если self-time маленький, а cum-time callee большой — реальная цена уровнем ниже; правь callee, не leaf. Вместе parent и child прочтения формируют диагноз: откуда приходят вызовы (caller fix vs leaf fix) и где реально накапливается цена (эта функция или её callee)." },
    { q: "Что означает «граница содержательной работы» в pprof и как её найти?", a: "Граница содержательной работы — это уровень в дереве вызовов, где cum-time резко падает при движении вверх от горячего leaf'а. Ниже этого уровня — исполнение (функции, делающие настоящую работу: compute, I/O, обращение к памяти). Выше — диспетчеризация (функции, маршрутизирующие, выбирающие или оборачивающие без значительной собственной работы). Как найти: начни с leaf'а с высоким self-time и иди вверх по parent chain. На каждом уровне сравни self-time этого parent'а с его cum-time. Когда self-time падает почти до нуля, а cum-time по-прежнему равен leaf'у — ты в territory диспетчеров. Последний уровень со значительным self-time — верхняя граница зоны работы. Правка должна попадать внутрь этой зоны. Править выше границы (переписывать dispatch-код) — напрасная трата времени; ниже самого широкого leaf'а — проблема обычно уже в callee, идентифицированном профилем." },
  ]}
/>

<Recap lang="ru">
Parent chain и child chain — две линзы на один и тот же широкий leaf. Parent chain раскрывает форму fan-in: один caller — правь call-site, двадцать caller'ов — правь leaf. Child chain раскрывает, несёт ли функция цену сама или является pass-through к callee. Граница между диспетчеризацией и исполнением указывает, где находится эффективное место правки. Bottom-up и top-down view профилей делают оба прочтения механическими: bottom-up называет горячий leaf, top-down показывает, какая подсистема его держит. Вместе эти два прочтения устраняют самую частую ошибку выбора уровня в performance-работе.
</Recap>
