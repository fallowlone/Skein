---
concepts:
- hot-path
- flame-graph
- symptom-vs-cause
- self-time
- parent-chain
deepensInto: []
estMin: 12
lang: ru
lessonType: topic
level: junior
order: 1
prereqs:
- performance/01-profile-first/04-reading-flame-graphs
slug: 01-what-makes-a-hot-path
sources:
- https://www.brendangregg.com/usemethod.html
- https://www.brendangregg.com/Articles/Netflix_Tech_Blog_Performance.pdf
- https://pkg.go.dev/runtime/pprof
spiral:
- latency
status: ready
summary: Широкий фрейм во flame graph показывает, где накапливается время, но не почему.
  Один и тот же leaf может скрывать пять разных узких мест — каждое требует своей
  правки.
title: 'Что делает путь горячим: симптом против причины'
track: performance
unit: 02-hot-paths
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import MetaphorComplete from "~/components/pedagogy/MetaphorComplete.astro";
import PersonaTag from "~/components/pedagogy/PersonaTag.astro";
import DiagramFrame from "~/components/diagram/DiagramFrame.astro";

<Hook>
Профиль готов. Один фрейм во flame graph широкий. Двое инженеров хотят сменить шаблонизатор. Третий спрашивает: «Широкий от чего? CPU-работа, аллокации, lock contention или syscall?» Только один из четырёх вариантов имеет «сменить шаблонизатор» как правильный fix.
</Hook>

<Crux>Широкий фрейм = горячо. Но горячо от чего? CPU, аллокации, кэш, лок, syscall — разные формы, разные правки. Сначала диагноз.</Crux>

<Explanation>

## Что такое hot path

Hot path — это цепочка вызовов, в которой программа проводит большую часть времени. Профиль показывает её как стек широких фреймов, поднимающихся от leaf'а к top-level entry. Leaf называет функцию; вопрос — почему эта функция дорогая.

Современное железо превращает ту же «1 секунду CPU» в очень разные проблемы в зависимости от того, что CPU реально делал: исполнял инструкции, ждал памяти, ждал лока, ждал возврата syscall. Диагноз решает, какое семейство правок применимо.

Применение неверной правки к верному hotspot'у — вторая по частоте трата в performance-работе, после оптимизации не того hotspot'а вообще (разобрана в unit'е profile-first).

## Метафора приёмной

Приёмная у врача переполнена. Это говорит, что в приёмной много людей — не говорит, почему. Пациенты ждут врача, лабораторию, бумаги, парковку? У каждого варианта своя правка: больше врачей, более быстрая лаборатория, меньше форм, больше мест для парковки.

Широкий фрейм во flame graph — то же самое: приёмная полная; спроси, чего люди ждут внутри.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Широкий фрейм показывает</th>
        <th class="border border-border px-3 py-2 text-left">Что это реально означает</th>
        <th class="border border-border px-3 py-2 text-left">Куда смотреть дальше</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">Высокий self-time в пользовательской функции</td>
        <td class="border border-border px-3 py-2">Функция делает настоящую CPU-работу</td>
        <td class="border border-border px-3 py-2">Изучи алгоритм или data layout</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Широкие GC-фреймы рядом с leaf'ом</td>
        <td class="border border-border px-3 py-2">Caller много аллоцирует</td>
        <td class="border border-border px-3 py-2">Переключись на allocation profile</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Широкий в wall-clock, узкий в CPU</td>
        <td class="border border-border px-3 py-2">Функция ждёт — лок или syscall</td>
        <td class="border border-border px-3 py-2">Сними off-CPU или mutex profile</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Фрейм интерпретатора там, где должен быть JIT</td>
        <td class="border border-border px-3 py-2">JIT деоптимизировался — откат к baseline</td>
        <td class="border border-border px-3 py-2">Стабилизируй формы объектов / типы</td>
      </tr>
    </tbody>
  </table>
</div>

## Bea и Sven: один фрейм, два прочтения

<PersonaTag id="bea" lang="ru" /> находит `processOrder` на 35% CPU и хочет переписать цикл. <PersonaTag id="sven" lang="ru" /> смотрит внимательнее: большая часть этих 35% — в `runtime.scanobject` (GC), вызываемом изнутри цикла. Цикл много аллоцирует. Правка — `sync.Pool`, не новый алгоритм.

Flame graph показал симптом. Причина — на уровень глубже.

## Сценарий: regex на каждый запрос

Search-эндпоинт показывает `regex.test` как широкий leaf. Двое инженеров хотят сменить regex-движок. Третий смотрит на parent: regex компилируется на каждом запросе, потому что паттерн строится из шаблонной строки. Правка — компилировать один раз на старте. Leaf указал на правильную область; баг был в паттерне caller'а, не в самом leaf'е.

<Inset kind="why" lang="ru">
Leaf — это лампочка на dashboard'е: «здесь что-то не так». Правка может быть внутри функции (переписать алгоритм), в caller'е (не вызывать так часто), в callee (реальная цена уровнем ниже) или в окружающем контексте (меньше аллоцировать, реже брать лок, меньше syscall'ов). Сеньоры читают весь район, не только leaf.
</Inset>

<Quiz
  id="01-what-makes-a-hot-path-quiz-1"
  lessonSlug="01-what-makes-a-hot-path"
  lang="ru"
  question="Flame graph показывает широкий leaf-фрейм. Что спросить ПЕРВЫМ?"
  choices={[
    { label: "Почему эта функция дорогая — делает CPU-работу, аллоцирует, ждёт лок или syscall? Форма решает правку.", correct: true },
    { label: "Могу ли я удалить эту функцию?", misconception: "Удаление редко вариант; функция, скорее всего, существует не зря." },
    { label: "Можно ли переписать её на более быстром языке?", misconception: "Преждевременно; вы ещё не знаете, являются ли CPU-инструкции bottleneck'ом вообще." },
    { label: "Можно ли закэшировать её результат?", misconception: "Иногда правильный ответ, но только после диагноза. Кэширование функции, чья цена — GC pressure, не поможет." },
  ]}
/>

<Quiz
  id="01-what-makes-a-hot-path-quiz-2"
  lessonSlug="01-what-makes-a-hot-path"
  lang="ru"
  question="Почему «широкий фрейм = bottleneck» — неполное прочтение flame graph?"
  choices={[
    { label: "Широкий фрейм называет СИМПТОМ (где накапливается время); ПРИЧИНА может быть в callee, caller'е или в том, что делает функция (compute, аллокации, ожидание). Нужно читать контекст.", correct: true },
    { label: "Flame graph ненадёжный инструмент", misconception: "Flame graph точен; проблема — интерпретация, не инструмент." },
    { label: "Широкие фреймы обычно шум в production-данных", misconception: "Широкие фреймы широкие потому, что сэмплы туда попадали — это настоящий сигнал." },
    { label: "Flame graph работает только для тестовых программ", misconception: "Production-профайлеры показывают production-данные; flame graph — стандартный формат." },
  ]}
/>

<DragOrder
  id="01-what-makes-a-hot-path-drag-1"
  lessonSlug="01-what-makes-a-hot-path"
  lang="ru"
  prompt="Расставь шаги сеньорной атаки на hot path по порядку:"
  items={[
    { id: "a", label: "Открой профиль и найди самый широкий leaf-фрейм по self-time" },
    { id: "b", label: "Прочти parent chain — leaf вызывается из одного пути или из многих?" },
    { id: "c", label: "Классифицируй работу: CPU-инструкции, аллокации, cache miss, lock wait, syscall или JIT deopt" },
    { id: "d", label: "Сформулируй одну гипотезу правки, соответствующей классификации" },
    { id: "e", label: "Применить ТОЛЬКО это изменение изолированно" },
    { id: "f", label: "Сними новый профиль под той же нагрузкой и сравни с baseline" },
    { id: "g", label: "Убедись, что и локальный hotspot сжался, И headline-метрика улучшилась" },
  ]}
  correctOrder={["a", "b", "c", "d", "e", "f", "g"]}
/>

<MetaphorComplete
  id="01-what-makes-a-hot-path-metaphor-1"
  pieceSlug="01-what-makes-a-hot-path"
  lang="ru"
  setup="Вставь пропуск: широкий фрейм во flame graph называет _______; причина может сидеть уровнем выше (в caller'е), уровнем ниже (в callee) или в том, что функция реально делает."
  accepted={["симптом", "место", "где", "точку", "symptom"]}
  canonical="симптом (место, где накапливается время, не обязательно причина, которая его производит)"
  explanation="Широкий фрейм — лампочка на dashboard'е: «здесь что-то не так». Правка может быть внутри функции (переписать алгоритм), в caller'е (не вызывать так часто), в callee (реальная цена ниже) или в окружающем контексте (меньше аллоцировать, реже брать лок, меньше syscall'ов). Сеньоры читают весь район, не только leaf."
/>

</Explanation>

<DiagramFrame
  label="Hot path — это малая доля кода, потребляющая большую часть времени выполнения"
  hue="var(--d-hardware)"
  caption="Hot path = несколько функций, исполняемых очень часто × цена за вызов — малая доля кода доминирует над суммарным временем выполнения."
>
  <svg viewBox="0 0 460 150" width="460" height="150" role="img" aria-hidden="true" style="display:block;margin:0 auto;max-width:100%;height:auto;">
    <text x="0" y="20" font-size="12" font-family="var(--font-body)" fill="var(--muted)">Доля кода</text>
    <rect x="0" y="28" width="46" height="28" rx="4" fill="var(--d-hardware)" stroke="var(--hairline-strong)" stroke-width="0.8" />
    <rect x="46" y="28" width="414" height="28" rx="4" fill="var(--card-2)" stroke="var(--hairline-strong)" stroke-width="0.8" />
    <text x="23" y="46" text-anchor="middle" font-size="10.5" font-family="var(--font-mono)" fill="var(--ink)">~10%</text>
    <text x="253" y="46" text-anchor="middle" font-size="10.5" font-family="var(--font-mono)" fill="var(--muted)">холодный код ~90%</text>

    <text x="0" y="90" font-size="12" font-family="var(--font-body)" fill="var(--muted)">Доля времени</text>
    <rect x="0" y="98" width="368" height="28" rx="4" fill="var(--d-hardware)" stroke="var(--hairline-strong)" stroke-width="0.8" />
    <rect x="368" y="98" width="92" height="28" rx="4" fill="var(--card-2)" stroke="var(--hairline-strong)" stroke-width="0.8" />
    <text x="184" y="116" text-anchor="middle" font-size="10.5" font-family="var(--font-mono)" fill="var(--ink)">hot path ~80% времени</text>
    <text x="414" y="116" text-anchor="middle" font-size="10.5" font-family="var(--font-mono)" fill="var(--muted)">~20%</text>
  </svg>
</DiagramFrame>

<KeyTakeaway>Широкий leaf называет симптом — где накапливается время. Причина может быть в caller'е (слишком частые вызовы), в callee (цена уровнем ниже) или в паттерне работы (аллокации, лок, syscall). Читай окрестности до выбора правки.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="01-what-makes-a-hot-path-retrieval"
  lang="ru"
  questions={[
    { q: "В одном абзаце: почему назвать горячую функцию недостаточно — что ещё нужно прочесть из профиля, прежде чем её чинить?", a: "Имя горячей функции говорит, где накапливается время; не говорит, какая работа его потребляет. Один и тот же широкий фрейм может означать пять разных проблем: функция делает настоящую CPU-работу и нуждается в лучшем алгоритме; функция много аллоцирует и триггерит GC; функция трогает не-кэшированную память и стопорится на cache miss'ах; функция ждёт лок; функция делает дорогие syscall'ы. У каждой проблемы своя правка — переписывание алгоритма помогает первой, но вредит второй; уменьшение аллокаций помогает второй, но не делает ничего для третьей; реструктуризация данных помогает третьей, но бесполезна для четвёртой и пятой. Чтобы выбрать правильную правку, прочти parent chain (откуда вызывают?), прочти детей (реальная ли цена в callee?) и используй второй профиль нужного типа. Широкий фрейм открывает расследование; остальной диагноз его закрывает." },
    { q: "Приведи два конкретных примера, когда правка находится в caller'е, а не в широком leaf'е самом по себе.", a: "1) Search-эндпоинт показывает regex.test как широкий leaf. Смена regex-движка кажется логичной, но parent показывает: regex компилируется на каждом запросе, потому что паттерн строится из шаблонной строки. Правка — компилировать один раз на старте. Leaf указал на правильную область, но баг был в caller'е. 2) json.Marshal показывает широкий фрейм с одним доминирующим parent-путём от логгера запросов. Смена JSON-библиотеки выглядит как fix, но логгер вызывает Marshal на каждом запросе, чтобы записать тело запроса в лог. Логируй тело только на debug-уровне или убери его — leaf сожмётся на 50% без касания библиотеки." },
  ]}
/>

<Recap lang="ru">
Hot path — это цепочка вызовов, где программа проводит большую часть времени. Широкий leaf во flame graph называет функцию, но причина может быть в caller'е (слишком частые вызовы), в callee (реальная цена уровнем ниже) или в том, что функция делает (CPU-работа vs аллокации vs ожидание). Диагностический вопрос — какая из пяти форм этот hotspot — должен предшествовать выбору правки. Следующие уроки разбирают каждую из пяти форм и соответствующие семейства правок.
</Recap>
