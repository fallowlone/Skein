infographic hierarchy-tree-curved-line-rounded-rect-node
data
  root
    label Широкий фрейм = 1 симптом
    children
      - label CPU-работа (высокий self-time)
        children
          - label Правка: переписать алгоритм
      - label Аллокации (GC pressure)
        children
          - label Правка: меньше аллокаций / пул
      - label Cache miss (столл памяти)
        children
          - label Правка: реструктурировать data layout
      - label Lock wait (off-CPU)
        children
          - label Правка: сжать критическую секцию
      - label Syscall (ожидание ядра)
        children
          - label Правка: батчить / меньше syscall-вызовов
theme
  palette #6366f1 #14b8a6 #f59e0b #f43f5e
