---
concepts:
- five-shapes
- symptom-vs-cause
- parent-chain
- ipc-diagnosis
- false-sharing
- constant-time
estMin: 13
lang: ru
level: senior
order: 7
prereqs: []
slug: quiz-choice
sources:
- https://www.brendangregg.com/usemethod.html
- https://easyperf.net/blog/2019/02/09/Top-Down-performance-analysis-methodology
- https://www.intel.com/content/www/us/en/docs/vtune-profiler/user-guide/2024-2/cpu-microarchitecture-analysis.html
status: ready
summary: Тест с выбором на синтез по всему юниту — пять форм, чтение symptom-vs-cause, цепочки parent/child, IPC и TMA, false sharing и security-гейт.
title: 'Горячие пути: тест с выбором ответа'
track: performance
unit: 02-hot-paths
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Шесть вопросов поперёк всего юнита. Каждый отражает решение, которое ты принимаешь посреди инцидента с открытым профилем — не определение для заучивания, а диагноз, на котором надо остановиться прежде, чем потратить спринт на неверный фикс.
</Hook>

<Goal lang="ru">
Убедись, что связываешь юнит воедино: читаешь широкий лист как симптом, классифицируешь его в одну из форм, находишь место фикса по цепочкам parent/child и учитываешь ограничения, которые добавляет production — счётчики, хвостовую задержку и security-гейт.
</Goal>

<Quiz
  id="02-hot-paths-choice-1"
  lessonSlug="02-hot-paths"
  lang="ru"
  question="Flame graph показывает renderTemplate с 28% self-time. Профиль аллокаций затем показывает внутри fmt.Sprintf на 62% байт аллокаций. Два инженера хотят сменить шаблонизатор. Как читать правильно?"
  choices={[
    { label: "Allocation-bound, а не CPU-bound. Self-time уходит в Sprintf + конкатенацию строк на каждой итерации, что подтверждает alloc-профиль. Чини аллокацию (strings.Builder, преаллоцированный буфер); смена движка сдвинет метрику куда слабее.", correct: true },
    { label: "CPU-bound — 28% self-time означает, что функция исполняет инструкции, так что переписывай алгоритм.", misconception: "Self-time лишь говорит, что сэмплы упали сюда; он не говорит, исполнял ли CPU инструкции или кормил аллокатор. Alloc-профиль переклассифицирует это как allocation-bound, где переписывание алгоритма почти не помогает." },
    { label: "Узкое место — движок: оба инженера правы, просто замерь, какой быстрее.", misconception: "Стоимость — это Sprintf + конкатенация в цикле вызывающего, а не внутренности движка. Более быстрый движок всё равно платит за те же аллокации, которые создаёт цикл." },
    { label: "Lock-bound — высокий self-time под нагрузкой подразумевает contention.", misconception: "Lock contention показывается широко в off-CPU/mutex-профиле и узко on-CPU. Этот фрейм широк on-CPU с явной аллокационной сигнатурой." },
  ]}
/>

<Quiz
  id="02-hot-paths-choice-2"
  lessonSlug="02-hot-paths"
  lang="ru"
  question="Два широких листа выглядят одинаково на flame graph. perf stat показывает лист A с IPC 3.0 и cache-miss rate 1%, а лист B с IPC 0.4 и cache-miss rate 18%. Чем различаются фиксы?"
  choices={[
    { label: "A — compute-bound: лучший алгоритм, SIMD, специализация. B — memory-bound: смена раскладки данных (SoA, непрерывные массивы, порядок обхода). Та же ширина, противоположные инструменты; решают счётчики, а не flame graph.", correct: true },
    { label: "Обоим нужен лучший алгоритм, раз оба широки на CPU.", misconception: "IPC 0.4 у B означает, что CPU простаивает ~60% времени, ожидая RAM. Новый алгоритм, который трогает ту же разбросанную память, всё равно стопорит — это memory-bound, не compute-bound." },
    { label: "B — бо́льшая проблема, потому что у него выше cache-miss rate, так что чини B первым.", misconception: "Miss rate классифицирует форму; он не ранжирует impact. Приоритет даёт доля каждого листа в общем времени (Amdahl), а не его miss rate." },
    { label: "Оба allocation-bound — высокий cache-miss rate означает давление GC.", misconception: "Allocation-bound показывается как широкие GC-фреймы (mallocgc, scanobject), а не как низкий IPC на самом листе. Cache miss — это паттерн доступа к памяти, а не сигнал GC." },
  ]}
/>

<Quiz
  id="02-hot-paths-choice-3"
  lessonSlug="02-hot-paths"
  lang="ru"
  question="json.Marshal на 28% CPU. В одном сервисе у него один доминирующий родитель (логгер запросов); в другом — двадцать тонких родителей (хендлеры). Куда относится фикс в каждом случае?"
  choices={[
    { label: "Концентрированный fan-in → чини вызывающего: логгер маршалит тело каждого запроса независимо от уровня логирования; перестань это делать. Распределённый fan-in → чини лист: это общая инфраструктура, так что меняй библиотеку или преэнкодь, потому что оптимизация одного из двадцати вызывающих почти ничего не меняет.", correct: true },
    { label: "Оба случая: переходи на более быструю JSON-библиотеку — это и значат 28% на Marshal.", misconception: "Случай с одним родителем — это баг вызывающего (энкодинг на каждый запрос ради логов); смена библиотеки там оставляет лишний энкод на месте. Читай parent chain прежде, чем тянуться к библиотеке." },
    { label: "Оба случая: чини лист, потому что стоимость всегда внутри широкой функции.", misconception: "С одним доминирующим родителем самый дешёвый выигрыш — в вызывающем (звать реже / вообще не звать). Форму fan-in, а не ширину листа, решает выбор leaf-fix против caller-fix." },
    { label: "Оба случая: заинлайнь Marshal в каждого вызывающего, чтобы убрать накладные на вызов.", misconception: "Стоимость — это работа маршалинга, а не диспетчеризация вызова. Инлайн дублирует код и ничего не даёт фактической стоимости сериализации." },
  ]}
/>

<Quiz
  id="02-hot-paths-choice-4"
  lessonSlug="02-hot-paths"
  lang="ru"
  question="Lock-free массив счётчиков использует atomic add с пер-тред индексами, но под нагрузкой IPC падает до 0.42, cache-miss rate достигает 76%, а пропускная способность ухудшается с ростом числа потоков. Диагноз и фикс?"
  choices={[
    { label: "False sharing. Восемь uint64-счётчиков делят одну 64-байтную линию, поэтому atomic-запись каждого потока инвалидирует линию в кеше каждого другого ядра через MESI — железо сериализует их, как будто стоит лок. Фикс: дополни каждый счётчик до собственной кеш-линии (например value + [56]byte).", correct: true },
    { label: "Lock contention — атомики фактически спинлок, так что переходи на настоящий mutex.", misconception: "Атомики — не локи, и contention показался бы как off-CPU ожидание, а не как обвал IPC on-CPU. XSNP_HITM подтверждает баунсинг кеш-линий, а не блокировку планировщика." },
    { label: "У алгоритма инкремента плохая сложность — переписывай.", misconception: "Atomic add — это O(1). Стоимость — это трафик когерентности памяти, который не починит никакое алгоритмическое изменение одноинструкционного add." },
    { label: "Массив превышает ёмкость L1 — уменьши его.", misconception: "16 uint64 — это 128 байт, легко влезает в L1. Проблема — множество ядер пишет в одну 64-байтную линию, а не ёмкость." },
  ]}
/>

<Quiz
  id="02-hot-paths-choice-5"
  lessonSlug="02-hot-paths"
  lang="ru"
  question="Node flame graph показывает доминирующие фреймы InterpreterCallStub в функции, которая должна быть горячей, и всплески задержки, не коррелирующие с трафиком. После переписывания на 'лучший алгоритм' ничего не улучшается. Почему и каков настоящий фикс?"
  choices={[
    { label: "JIT-деопт: V8 оптимизировал функцию, предположение о типе сломалось, и он откатился в интерпретатор/baseline-уровень — теперь каждый вызов платит цену интерпретатора. Переписывание алгоритма впустую, пока типы не стабилизированы (≤4 hidden classes, без позднего добавления свойств в горячем цикле).", correct: true },
    { label: "Это CPU-bound, и переписывание просто было недостаточно агрессивным.", misconception: "CPU-bound код показывает оптимизированные фреймы TurboFan, а не интерпретатора. Фрейм интерпретатора сам по себе сигнал деоптa; никакой алгоритм не починит рантайм, который отказывается оптимизировать код." },
    { label: "Это allocation-bound — пуль объекты.", misconception: "Allocation-bound показывает широкие GC-фреймы, а не интерпретатор. Пулинг не возвращает TurboFan; стабилизация типов — возвращает." },
    { label: "Профилировщик сэмплит именно эту функцию слишком агрессивно.", misconception: "Sample rate влияет на все функции одинаково; он не может избирательно изготовить фреймы интерпретатора для одной функции. Деопт реален." },
  ]}
/>

<Quiz
  id="02-hot-paths-choice-6"
  lessonSlug="02-hot-paths"
  lang="ru"
  question="Инженер ускоряет горячий путь сравнения токена в 3 раза, добавив early-exit ветку на первом несовпавшем байте. Фрейм сузился, p50 улучшился. Почему это всё равно неправильно?"
  choices={[
    { label: "Это ломает constant-time инвариант: задержка ответа теперь зависит от длины совпавшего префикса, превращая поиск 32-байтного секрета из ~2^256 в ~32×256 попыток для атакующего, замеряющего ответы. Constant-time, security-чувствительные пути требуют security-гейта перед любым изменением скорости.", correct: true },
    { label: "Ничего не сломано — более быстрая валидация токена строго лучше.", misconception: "Timing side channel — реальный класс атак; constant-time, ветвь-свободная форма существует именно чтобы лишить атакующего этого сигнала. Локальное ускорение, утекающее секрет, — это регресс." },
    { label: "Early exit вносит гонку данных на буфере токена.", misconception: "Сломано свойство времени, а не конкуррентности. Даже полностью сериализованный, зависящий от данных момент выхода утекает длину совпавшего префикса." },
    { label: "Это важно, только если сдвинулась медиана; раз p50 улучшился, изменение безопасно.", misconception: "Улучшение p50 нерелевантно безопасности. Утечка — в связи между входом и временем, а не в каком-либо перцентиле задержки." },
  ]}
/>

<Recap lang="ru">
Юнит — это одно дерево решений: широкий лист называет симптом, а не причину; классифицируй его в форму (CPU, аллокация, кеш, лок, syscall, JIT-деопт или случаи, видимые только через железо — false sharing, native-bridge); находи место фикса по parent chain (один вызывающий против многих) и child chain (работа здесь против на уровень ниже); тянись к аппаратным счётчикам и TMA, когда CPU-bound и memory-bound выглядят одинаково; и проводи любое изменение constant-time или security-чувствительных путей через ревью. Сначала диагноз, потом фикс одной вещи, докажи его diff'ом профиля — и следи за хвостовой задержкой, а не только за средним.
</Recap>
