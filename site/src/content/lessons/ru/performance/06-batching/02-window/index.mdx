---
concepts:
- batch size
- max wait
- two-dimensional window
- break-even
- speedup
deepensInto: []
estMin: 10
lang: ru
lessonType: topic
level: middle
order: 2
prereqs:
- performance/06-batching/01-basics
slug: 02-window
sources:
- https://kafka.apache.org/documentation/#producerconfigs_linger.ms
- https://man.archlinux.org/man/io_uring.7.en
- https://lwn.net/Articles/776703/
- https://www.postgresql.org/docs/current/populate.html
- https://redis.io/docs/manual/pipelining/
spiral:
- latency
- statefulness
status: ready
summary: 'У каждой системы батчинга две ручки: max-size (байты или записи) и max-wait
  (время). Что сработает первым — то и шлёт батч; и какая ручка сработала, говорит,
  упёрлись вы в throughput или в латентность.'
title: 'Окно батчинга: размер и время ожидания'
track: performance
unit: 06-batching
---


import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
Вы поднимаете `linger.ms` у Kafka-продюсера с 0 до 10 — и throughput взлетает в 10 раз ценой всего 10мс лишней латентности. Apache сделал тот же выбор за всех: в Kafka 4.0 (март 2025) дефолт `linger.ms` сменили с 0 на 5мс после многих лет на нуле. Аргумент команды был жёстким — погоня за немедленностью на стороне отправителя не даёт глобально низкую латентность. Крошечная искусственная задержка покупает эффективность батчинга, которая часто *снижает* сквозную латентность. Чтобы понять, почему задержка в 5мс делает систему быстрее, нужно разобрать двумерное окно под капотом.
</Hook>

<Crux>Два триггера — max-size и max-wait — что сработает первым, то и шлёт батч. Какой сработал — упёрлись вы в throughput или в латентность.</Crux>

<Explanation>

## Два триггера, а не одна ручка

У любой системы батчинга, которую вы будете тюнить, ровно два лимита: **max-size** (байты или число записей) и **max-wait** (таймер). Элементы копятся в буфере. Батч сбрасывается в тот миг, когда сработал *любой* из лимитов — буфер дорос до max-size или таймер дошёл до max-wait. Это OR, а не AND. В Kafka это `batch.size` и `linger.ms`; в Redis-пайплайнинге — буфер клиента и то, сколько вы даёте ему наполняться; в bulk-insert БД — строк на запрос и интервал сброса. Везде одна и та же форма.

Наивный инстинкт — выдать одну ручку («просто задай размер батча»), и каждый сеньор видел, как этот инстинкт поднимал кого-то в 3 ночи. Нужны обе, потому что у каждой в одиночку есть провал, который закрывает другая.

## Почему один размер застревает, а одно время переполняет

Убери max-wait, оставь только max-size: теперь **медленный продюсер берёт батч в заложники**. Трафик проседает в 2 ночи, элементы капают, батч никогда не дорастает до `batch.size` и потому не сбрасывается. Первое сообщение полупустого батча может лежать секундами — неограниченная латентность, которая растёт обратно пропорционально нагрузке. Это классический head-of-line stall: лекарство — таймер, который говорит «шли что есть».

Убери max-size, оставь только max-wait: теперь **быстрый продюсер собирает монстра**. Прилетает Чёрная пятница, элементы хлынули, и за окно в 50мс буфер раздувается до того, что downstream не проглотит — запрос больше брокерского `message.max.bytes`, пакет фрагментируется, транзакция разносит WAL, массив роняет консьюмер по OOM. Лекарство — лимит размера, который говорит «шли, пока не разросся». Нужны обе, потому что они ломаются в противоположные стороны: размер защищает throughput, время защищает латентность, и убрав любую, вы возвращаете тот баг, который другая и закрывала.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Режим нагрузки</th>
        <th class="border border-border px-3 py-2 text-left">Что срабатывает первым</th>
        <th class="border border-border px-3 py-2 text-left">Что это значит</th>
        <th class="border border-border px-3 py-2 text-left">Рычаг тюнинга</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">Высокая нагрузка</td>
        <td class="border border-border px-3 py-2"><strong>max-size</strong> (буфер наполняется первым)</td>
        <td class="border border-border px-3 py-2">Упёрлись в throughput; таймер не успевает</td>
        <td class="border border-border px-3 py-2">Поднять <code>batch.size</code> — меньше, но толще сбросы</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Низкая нагрузка</td>
        <td class="border border-border px-3 py-2"><strong>max-wait</strong> (таймер срабатывает первым)</td>
        <td class="border border-border px-3 py-2">Упёрлись в латентность; батчи мелкие, доминирует таймер</td>
        <td class="border border-border px-3 py-2">Подстроить <code>linger.ms</code> под SLO латентности</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Ровно на безубыточности</td>
        <td class="border border-border px-3 py-2">Любой, примерно вместе</td>
        <td class="border border-border px-3 py-2">Окно хорошо подогнано под текущий трафик</td>
        <td class="border border-border px-3 py-2">Оставить; перепроверить при смене формы трафика</td>
      </tr>
    </tbody>
  </table>
</div>

## Математика ускорения, по шагам

Смоделируем любую операцию на элемент как **фиксированную стоимость** `F` (накладные на вызов — syscall, сетевой round-trip, begin/commit транзакции) плюс **переменную стоимость** `V*n` (работа пропорционально payload размера `n`). `N` элементов по отдельности стоят:

`N * (F + V*n)`

Те же элементы одним батчем платят фиксированную стоимость **один раз** и ту же переменную работу:

`F + V*(N*n)`

Ускорение — это отношение:

`speedup = (N*F + N*V*n) / (F + V*N*n)`

Смотрим на две крайности. Когда доминирует фиксированная стоимость — `F > V*n`, то есть маленькие payload, где накладные на вызов и есть вся история — член `N*F` забивает всё и **speedup → N**. Сбатчили 100 элементов — стали быстрее в ~100 раз. Когда доминирует переменная — `F < V*n`, большие payload, где вы и так платите в основном за байты — член `V*N*n` забивает всё и **speedup → 1**: батчинг не даёт ничего, потому что не было фиксированной стоимости для амортизации. Перелом, **точка безубыточности — это `F = V*n`**: когда переменная стоимость одного элемента равна фиксированным накладным, батчинг начинает окупаться.

## Конкретное число

Возьмём сетевую операцию: пакеты по 1КБ, round-trip 50мкс на вызов как фиксированная стоимость и батч из 100 элементов. По одному round-trip'ы стоят `100 * 50мкс = 5мс`. Батчем вы платите round-trip **один раз** плюс байты — пусть ~150мкс всего. Это `5мс` до `150мкс`, **~33x** ускорение, потому что здесь `F` (RTT 50мкс) сильно перевешивает `V*n` (время передачи на КБ). Ровно в этом режиме живёт Redis-пайплайнинг: опубликованный бенчмарк шлёт 10 000 `PING` за `1.185с` без пайплайна и за `0.250с` с пайплайном — ~5x — и разрыв растёт по мере роста RTT относительно работы на команду. Версия с syscall-ами та же по духу: syscall стоит ~1–5мкс, поэтому схлопывание 300k syscall-ов в ~4k через большие буферизованные записи делает накладные на вызов фактически нулевыми.

<Inset kind="why" lang="ru">
Заметьте: безубыточность — на *элемент*, а не на батч. Если payload одного элемента уже стоит больше фиксированных накладных (`V*n > F`), никакой размер батча вас не спасёт — вы прочно в режиме `speedup → 1`, и батчинг только добавляет латентность. Вот почему батчить крошечные сообщения (логи, метрики, лукапы по ключу) — огромный выигрыш, а батчить уже большие блобы (видео-чанки, большие загрузки файлов) — почти бессмысленно.
</Inset>

## Нагрузка решает, какой триггер главный

Поведение окна не статично — оно смещается с трафиком. При **высокой нагрузке** элементы хлынули, и буфер достигает max-size задолго до таймера; размер — доминирующий триггер, вы упёрлись в throughput. При **низкой нагрузке** ручеёк не наполняет буфер, поэтому таймер срабатывает первым; доминирует время, вы упёрлись в латентность. Практическая диагностика: смотрите средний размер батча против заданного max-size. Если батчи стабильно сбрасываются у max-size — побеждает размер, поднимайте его. Если сбрасываются мелкими и по таймеру — побеждает время, и поднимать `linger.ms` помогает только пока батч не начнёт наполняться раньше истечения таймера.

Разбор живой системы: max-size `10000` байт, max-wait `5мс`, и вы видите батчи в среднем 8000 байт каждые 4мс. Таймер срабатывает первым (`4ms < 5ms`), но буфер не полон (`8000 < 10000`) — умеренная нагрузка, доминирует время. Поднятие max-wait увеличит батчи и throughput, но только до точки, где буфер наполняется раньше таймера.

<DragOrder
  id="02-window-drag-1"
  lessonSlug="02-window"
  lang="ru"
  prompt="Расставьте по порядку, что происходит с одним элементом в окне батчинга «размер + время»:"
  items={[
    { id: "a", label: "Элемент приходит и дописывается в буфер в памяти" },
    { id: "b", label: "Система проверяет: довёл ли он буфер до max-size?" },
    { id: "c", label: "Если не полон — таймер max-wait продолжает идти для самого старого элемента буфера" },
    { id: "d", label: "Что достигнуто первым — полный буфер ИЛИ истёкший таймер — запускает сброс" },
    { id: "e", label: "Весь батч уходит одной операцией, платя фиксированную стоимость один раз" },
  ]}
  correctOrder={["a", "b", "c", "d", "e"]}
/>

## Разумные дефолты и как реально тюнить

Разумные стартовые точки — max-wait `10–100мс` и max-size `64КБ–1МБ`; high-throughput-рецепт Kafka — `batch.size` 64КБ–256КБ с `linger.ms` 20–100мс, а сбалансированный продакшен-конфиг вроде `batch.size=32768, linger.ms=10, compression.type=lz4, acks=1` даёт ~25k msg/s при латентности под 20мс. Но дефолты — стартовая линия, а не ответ. Сеньорский ход — **выводить ожидание из SLO латентности, а не из жажды максимального throughput**: берите наибольший `linger.ms`, который терпит ваш бюджет p99, затем подбирайте буфер так, чтобы он наполнялся у этого таймера на *пиковой* ожидаемой нагрузке. Дальше — проверяйте так, как нельзя на доске: **проигрывайте реальный продакшен-трафик в staging**, прогоняйте обе ручки и читайте фактическое распределение размеров батча и хвостовую латентность. Синтетическая равномерная нагрузка врёт; продакшен бёрстовый, и только реплей покажет, какой триггер доминирует на вашей реальной форме трафика.

<Quiz
  id="02-window-quiz-1"
  lessonSlug="02-window"
  lang="ru"
  question="Ваша система батчинга использует только max-size (без таймера). Ночью трафик падает. Какой провал?"
  choices={[
    { label: "Первый элемент полупустого батча может ждать бесконечно, потому что при низкой нагрузке буфер не дорастает до max-size — неограниченная латентность", correct: true },
    { label: "Батчи разрастаются и переполняют ёмкость downstream", misconception: "Это провал «только max-wait» при ВЫСОКОЙ нагрузке, а не «только max-size» при низкой. С одним лимитом размера батч не может переполниться — он может только не сброситься." },
    { label: "Throughput рушится, потому что каждый элемент уходит отдельно", misconception: "Батчинг по размеру всё ещё батчит; элементы копятся, пока буфер не наполнится. Проблема — латентность ждущих элементов, а не потеря батчинга." },
  ]}
/>

<Quiz
  id="02-window-quiz-2"
  lessonSlug="02-window"
  lang="ru"
  question="Для payload по 4КБ, где фиксированная стоимость вызова ~50мкс, а передача на КБ ~40мкс, даст ли большой батч сильное ускорение?"
  choices={[
    { label: "Нет — переменная стоимость (`4 * 40µs = 160µs`) уже превышает фиксированную (50мкс), значит `F < V*n` и speedup стремится к 1", correct: true },
    { label: "Да — батчинг всегда умножает throughput на размер батча N", misconception: "speedup → N только когда доминирует фиксированная стоимость (`F > V*n`). Здесь доминирует переменная, поэтому амортизировать почти нечего." },
    { label: "Да — большие payload делают батчинг эффективнее", misconception: "Наоборот: большие payload толкают вас в режим `speedup → 1`, потому что фиксированная стоимость становится меньшей долей общей работы." },
  ]}
/>

<TradeoffMatrix
  id="02-window-tradeoff-1"
  lessonSlug="02-window"
  lang="ru"
  prompt="У сервиса подтверждения платежей строгий SLO p99 латентности 25мс, но на пике нужен и высокий throughput. Как настроить окно батчинга?"
  options={[
    { name: "Взять max-wait ниже SLO (например, linger 10мс), подобрать буфер так, чтобы он наполнялся у этого таймера на пике, проверить реплеем прод-трафика в staging", summary: "Окно от SLO.", correct: true, justification: "Время ожидания — жёсткий пол хвостовой латентности, поэтому оно должно сидеть безопасно ниже бюджета p99 в 25мс — никогда выше. Подбор буфера под наполнение у этого таймера на пике значит, что высокую нагрузку поглощает триггер размера (throughput), а низкую ограничивает таймер (латентность). Реплей реального бёрстового трафика — единственный способ убедиться, что распределение размеров батча и p99 реально лягут туда, куда предсказывает математика." },
    { name: "Максимизировать throughput: linger 100мс и буфер 1МБ", summary: "От throughput.", justification: "Таймер в 100мс сам по себе может пробить SLO p99 в 25мс при низкой нагрузке, когда батчи сбрасываются по таймеру с первым элементом, которому уже 100мс. Тюнинг под максимальный throughput в обход потолка латентности — ровно та ошибка, ради которой SLO и существует." },
    { name: "Отключить таймер; полагаться только на max-size, чтобы батчи были всегда полными и эффективными", summary: "Только размер.", justification: "Только размер застревает при низкой или бёрстовой нагрузке: когда трафик проседает, буфер не наполняется, и самый старый элемент ждёт неограниченно, нарушая SLO p99 именно в те тихие периоды, которые вы сочли бы безопасными." },
  ]}
/>

</Explanation>

<FlowDiagram
  label="Элементы копятся в буфере; что сработает первым — max-size ИЛИ max-wait — сбрасывает батч"
  hue="var(--d-hardware)"
  nodes={[
    { id: "size", label: "max-size достигнут", sub: "буфер полон", col: 2, row: 0 },
    { id: "arrive", label: "элементы идут", col: 0, row: 1 },
    { id: "buffer", label: "буфер", sub: "накопление", col: 1, row: 1 },
    { id: "flush", label: "сброс", sub: "первый побеждает", col: 2, row: 1 },
    { id: "ship", label: "шлём батч", sub: "F один раз", col: 3, row: 1 },
    { id: "timer", label: "таймер max-wait", sub: "linger.ms", col: 2, row: 2 },
  ]}
  edges={[
    { from: "arrive", to: "buffer" },
    { from: "buffer", to: "flush", label: "ИЛИ" },
    { from: "size", to: "flush" },
    { from: "timer", to: "flush" },
    { from: "flush", to: "ship" },
  ]}
  caption="Высокая нагрузка наполняет max-size до таймера (упор в throughput). Низкая — таймер срабатывает первым на полупустом буфере (упор в латентность)."
/>

<KeyTakeaway>У окна два триггера — max-size и max-wait — что сработает первым, то и сбрасывает. Размер защищает throughput, время — латентность; нужны оба. Тюньте ожидание под SLO латентности.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="02-window-retrieval"
  lang="ru"
  questions={[
    {
      q: "Почему системе батчинга нужны одновременно лимит размера и лимит времени, и что ломается при удалении каждого?",
      a: "Они закрывают противоположные провалы. Убери max-wait, оставь только max-size: медленный или тихий продюсер не наполняет буфер, батч не сбрасывается, и его самый старый элемент ждёт неограниченно — латентность, растущая обратно нагрузке (head-of-line stall). Убери max-size, оставь только max-wait: быстрый продюсер заливает буфер внутри окна и собирает батч слишком большой для downstream — больше брокерского лимита сообщения или достаточно большой, чтобы уронить консьюмер по OOM либо разнести транзакцию. Размер защищает throughput, время — латентность. Вместе они адаптируются к нагрузке: при высокой буфер достигает max-size раньше таймера (доминирует размер, упёрлись в throughput); при низкой таймер срабатывает раньше наполнения (доминирует время, упёрлись в латентность)."
    },
    {
      q: "Выведите формулу ускорения и объясните, где она идёт к N, а где к 1.",
      a: "Смоделируйте операцию на элемент как фиксированную стоимость F (накладные на вызов: syscall, round-trip, commit транзакции) плюс переменную V*n (работа пропорционально payload размера n). N элементов по отдельности стоят N*(F + V*n). Батчем вы платите F один раз плюс ту же переменную работу: F + V*(N*n). Ускорение — отношение (N*F + N*V*n) / (F + V*N*n). Когда доминирует фиксированная — `F > V*n`, типично для маленьких payload — член N*F доминирует и speedup стремится к N (сбатчили 100 — ~100x). Когда доминирует переменная — `F < V*n`, большие payload — член V*N*n доминирует и speedup стремится к 1 (амортизировать нечего). Безубыточность на элемент при `F = V*n`. Конкретно: пакеты 1КБ, фиксированная стоимость RTT 50мкс, батч из 100 — 100 round-trip'ов по 50мкс = 5мс, в один round-trip плюс байты ~150мкс, выигрыш ~33x."
    },
    {
      q: "max-size=10000 байт, max-wait=5мс, наблюдаемые батчи в среднем 8000 байт каждые 4мс. Что это говорит и что менять?",
      a: "Таймер срабатывает первым (`4ms < 5ms`), а буфер не полон (`8000 < 10000`), значит это умеренная, доминируемая временем нагрузка — вы упёрлись в латентность, а не в throughput. Поднятие max-wait дало бы батчам расти и подняло throughput, но только до точки, где буфер наполняется раньше истечения таймера; дальше триггером становится размер, и лишнее ожидание только добавляет латентность. Прежде чем трогать, спросите, не близки ли 5мс к вашему SLO латентности — если да, большие ожидания вам вовсе не нужны. Честный тюнинг — проиграть реальный продакшен-трафик в staging и смотреть распределение размеров батча и p99 по обеим ручкам, потому что продакшен-бёрсты сместят, какой триггер доминирует."
    }
  ]}
/>

<Recap lang="ru">
Двумерное окно — max-size плюс max-wait — ядро любой системы батчинга, и два лимита это OR: что сработает первым, то и шлёт батч. Нужны оба, потому что они ломаются в противоположные стороны: только размер застревает на медленном продюсере (батч не наполняется, латентность не ограничена), а только время даёт быстрому продюсеру собрать батч слишком большой для downstream. Какой триггер сработал, ещё и диагностирует режим — доминирует размер значит упёрлись в throughput (высокая нагрузка), доминирует время значит упёрлись в латентность (низкая). Математика ускорения — `(N*F + N*V*n) / (F + V*N*n)`: идёт к N, когда доминирует фиксированная стоимость (`F > V*n`, маленькие payload), и к 1, когда доминирует переменная (`F < V*n`, большие payload), с безубыточностью на элемент при `F = V*n` — пакеты 1КБ через RTT 50мкс батчем ~33x. Тюньте, выводя max-wait из SLO латентности, подбирая буфер под наполнение у этого таймера на пике, затем проверяйте реплеем продакшен-трафика в staging — а не погоней за максимальным throughput на доске.
</Recap>
