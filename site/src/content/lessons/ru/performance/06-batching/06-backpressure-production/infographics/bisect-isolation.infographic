infographic chart-column-simple
data
  title Изоляция одной отравленной записи в батче из 500
  desc Split-and-retry делит батч пополам — O(log N) ≈ 9 повторов, а не 500
  values
    - label Проверять каждую запись (линейно)
      value 500
    - label Split-and-retry (бисекция)
      value 9
theme
  palette #f43f5e #14b8a6
