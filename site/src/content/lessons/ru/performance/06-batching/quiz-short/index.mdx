---
concepts:
- fixed-vs-variable-cost
- two-trigger-window
- speedup-math
- compression-needs-batching
- backpressure-policies
- batch-boundary-security
estMin: 14
lang: ru
level: senior
order: 8
prereqs: []
slug: quiz-short
sources:
- https://kafka.apache.org/documentation/#producerconfigs_linger.ms
- https://www.postgresql.org/docs/current/populate.html
- https://www.rfc-editor.org/rfc/rfc896
status: ready
summary: Промпты на свободное воспроизведение по всему юниту. Сначала отвечай своими словами, затем открой модельный ответ и сравни.
title: 'Batching: тест на воспроизведение'
track: performance
unit: 06-batching
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";

<Hook>
Воспроизведение бьёт перечитывание. На каждый промпт скажи или запиши полный ответ по памяти, прежде чем открыть модельный — усилие припоминания и закрепляет материал.
</Hook>

<Goal lang="ru">
Восстанови костяк юнита — модель фиксированной цены, окно с двумя триггерами, математику speedup, почему компрессии нужен batching, политики переполнения и attack surface границы батча — не подглядывая в уроки.
</Goal>

<RetrievalDrawer
  client:visible
  id="06-batching-short-retrieval"
  lang="ru"
  questions={[
    { q: "Зачем нужен batching, какова модель цены и где он НЕ окупается?", a: "У каждой операции есть фиксированная цена F (платится за вызов независимо от payload: переход в syscall, network round-trip плюс ACK, commit транзакции, fsync) и переменная цена V на элемент (сами данные). N элементов по отдельности стоят N·(F + V); batching платит F один раз: F + N·V. Batching превращает доминирующую повторяющуюся фиксированную цену в одну амортизированную — поэтому медленный ingest может выглядеть полностью idle (idle CPU, диск, сеть), но быть медленным: bottleneck — это latency, потоки запаркованы в ожидании round-trip'ов, а не утилизация, которую видит flame graph. Окупается, когда (1) фиксированная цена доминирует над переменной, (2) rate достаточно высок, чтобы создать глубину очереди для амортизации, и (3) есть запас latency. НЕ окупается для редких операций (нет глубины очереди — батч из одного медленнее, чем без батча), при жёстком sub-millisecond SLO (любое ожидание его ломает), при причинно-зависимых операциях (N+1 нуждается в подтверждённом результате N) и при failure model, который не терпит потери частичного батча или batch-granular rollback." },
    { q: "Выведи формулу speedup и объясни, где она идёт в N, а где в 1.", a: "Моделируй per-item-операцию как фиксированную цену F плюс переменную V·n (работа пропорциональна размеру payload n). N элементов по отдельности стоят N·(F + V·n); в батче — F + V·(N·n), платя F один раз. Speedup = (N·F + N·V·n) / (F + V·N·n). Когда доминирует фиксированная цена — F > V·n, типично для малых payload'ов — член N·F доминирует и speedup → N (батч 100, ~100×). Когда доминирует переменная — F < V·n, большие payload'ы — член V·N·n доминирует и speedup → 1: амортизировать нечего, batching лишь добавляет latency. Break-even — на элемент при F = V·n. Конкретно: пакеты 1 КБ, RTT 50 µs как фиксированная цена, батч 100 — 100 round-trip'ов по 50 µs = 5 мс схлопываются в один round-trip плюс байты (~150 µs), выигрыш ~33×. Поэтому батчить крошечные сообщения (логи, метрики, lookup'ы ключей) — огромный выигрыш, а большие блобы (видео-чанки, крупные загрузки) — бессмысленно." },
    { q: "Зачем окну батчинга нужны и max-size, и max-wait, и что диагностирует, какой триггер сработал?", a: "Они покрывают противоположные failure modes, и батч сбрасывается, когда срабатывает ЛЮБОЙ (это OR, не AND). Size-only: медленный или тихий продьюсер никогда не заполняет буфер, батч не сбрасывается, и самый старый элемент ждёт неограниченно — head-of-line stall, у которого latency растёт обратно пропорционально нагрузке. Time-only: быстрый продьюсер затопляет буфер внутри окна и строит батч слишком большой для downstream — больше max message size брокера или достаточный, чтобы OOM-нуть консьюмера либо разорвать транзакцию. Size защищает throughput, time защищает latency; нужны оба. Какой триггер сработал — диагностирует режим: при высокой нагрузке буфер бьёт max-size раньше таймера (size доминирует, throughput-bound); при низкой первым срабатывает таймер (time доминирует, latency-bound). Честный тюнинг — вывести max-wait из latency-SLO, размерить буфер так, чтобы он наполнялся около таймера на пике, затем валидировать, реплеив реальный bursty production-трафик в staging." },
    { q: "Пройди ступени ingestion в Postgres и объясни, почему компрессии сначала нужен batching.", a: "Три ступени амортизации. Per-row INSERT с autocommit платит parse, plan, execute и коммит с WAL-flush на каждую строку — ~1–5k строк/с. Multi-row INSERT ... VALUES амортизирует parse/plan/commit по строкам одного statement — ~5–50k строк/с, но ограничен лимитами параметров и размера statement, так что ты режешь на чанки. COPY стримит по выделенному wire-протоколу, который целиком обходит per-row SQL-парсер и планировщик и коммитит один раз — ~50–500k строк/с. Senior-ход — распознать, на какой ступени тихо сидит код: ORM-цикл, зовущий save() на объект, — это per-row INSERT в красивой обёртке. Компрессии нужен batching, потому что кодеки (lz4, zstd) находят повторы только внутри своего input window: одно маленькое сообщение почти не содержит внутренних повторов и едва сжимается, а батч 128 КБ из похожих записей делит имена полей и структуру и сжимается в 2–4×. Kafka гоняет кодек per batch, так что compression.type почти ничего не даёт при linger.ms=0 и много — при толстых батчах. Сначала батч, потом компрессия." },
    { q: "Продьюсер обгоняет консьюмера и ограниченная очередь полна. Сравни block, drop и spill-to-disk и скажи, как выбирать.", a: "Block: продьюсер ждёт, latency растёт, throughput оседает на скорости слива консьюмера — это backpressure Reactive Streams request(n) и правильный дефолт, когда потеря данных дороже добавленной latency (заказы, платежи). Drop: отбрасывай новый (или самый старый) элемент — лоссово по дизайну, верно, когда свежесть важнее полноты (StatsD по UDP, где брошенный sample невидим, но застрявший продьюсер ломает саму observability). Spill-to-disk: переполнение пишется в локальный write-ahead log, сливаемый за in-memory очередью — durable через всплески продьюсера ценой disk IOPS и latency (Vector, Fluentd, Filebeat); но spill — это рычаг ёмкости, а не сама политика переполнения: даже дисковый буфер конечен и всё равно нуждается в правиле when_full (Vector по умолчанию block). Никогда не оставляй очередь неограниченной: она растёт до OOM и теряет всё в полёте. Выбор — продуктовое решение: оцени один потерянный элемент против одной секунды добавленной latency, ответь один раз на пайплайн и закодируй в конфиге, а не наследуй дефолт библиотеки." },
    { q: "Почему per-batch idempotency небезопасна и как CVE-2023-34455 обобщает правило безопасности границы батча?", a: "Per-batch idempotency-ключ присваивает один nonce всему батчу, так что атакующий шлёт один батч с одним и тем же payload 100 раз, и dedup-проверка на границе видит один уникальный ключ и обрабатывает все 100 копий. Idempotency должна быть per-item — per-item-nonce, проверяемый по dedup-store по мере применения каждой записи. CVE-2023-34455 обобщает это: snappy-java читала объявленное поле длины чанка и аллоцировала столько байт без проверки верхней границы, так что крошечный сжатый фрейм, объявляющий ~2 ГБ, OOM-килил брокер Kafka (0.8.0–3.5.0, фикс в 1.1.10.1). Объединяющее правило: сжатый батч — непрозрачный конверт, и каждое свойство (общий размер, число элементов, idempotency-ключ, порядок) надо валидировать post-decompression и per-item, никогда не доверяя wire-границе или per-batch. Batching множит любой сбой, и атакующий тянется ровно к этому множителю." },
  ]}
/>

<Recap lang="ru">
Если ты смог восстановить каждый ответ по памяти — ты держишь костяк юнита: batching превращает N·(F + V) в F + N·V и окупается только при доминирующей фиксированной цене, глубине очереди и запасе latency; окно с двумя триггерами (size для throughput, time для latency) размеряется под SLO и валидируется на реплее трафика; speedup идёт в N, когда доминирует F, и в 1, когда V, break-even на элемент при F = V·n; Postgres лезет INSERT → multi-row → COPY, и компрессия едет поверх толстых батчей; ограниченные очереди выбирают block, drop или spill как продуктовое решение; а граница батча — attack surface, валидируемый post-decompression и per-item.
</Recap>
