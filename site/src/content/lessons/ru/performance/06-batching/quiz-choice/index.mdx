---
concepts:
- fixed-vs-variable-cost
- batching-window
- break-even
- backpressure
- poison-message
- decompression-bomb
estMin: 13
lang: ru
level: senior
order: 7
prereqs: []
slug: quiz-choice
sources:
- https://kafka.apache.org/documentation/#producerconfigs_linger.ms
- https://www.postgresql.org/docs/current/populate.html
- https://nvd.nist.gov/vuln/detail/CVE-2023-34455
status: ready
summary: Тест с выбором на синтез по всему юниту — модель фиксированной цены, окно size/wait, математика break-even, политики backpressure и production-сбои.
title: 'Batching: тест с выбором ответа'
track: performance
unit: 06-batching
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Шесть вопросов поперёк всего юнита. Ни один — не определение для заучивания: каждый отражает решение под нагрузкой — когда батчить, насколько широким делать окно и что ломается, когда happy path заканчивается.
</Hook>

<Goal lang="ru">
Убедись, что связываешь модель фиксированной цены, окно с двумя триггерами, математику break-even и production-сбои — тот синтез, к которому вели отдельные уроки.
</Goal>

<Quiz
  id="06-batching-choice-1"
  lessonSlug="06-batching"
  lang="ru"
  question="Платёжный сервис пишет одну durable-строку ledger на транзакцию при ~30 записях/с; SLA — 'durable до того, как мы вернём success'. Коллега предлагает COPY-батчи по 50 мс, чтобы снизить нагрузку на БД. Что решает senior и почему?"
  choices={[
    { label: "Отклонить batching здесь. При 30 записях/с почти нет глубины очереди для амортизации, так что выигрыш от фиксированной цены мизерный, зато каждая транзакция теперь ждёт до 50 мс, а крах до flush теряет подтверждённые платежи — durability-SLA несовместим с потерей частичного батча.", correct: true },
    { label: "Батчить в окна COPY по 50 мс — меньше коммитов всегда дешевле для БД.", misconception: "Оптимизирует нагрузку, которую никто не платит: 30 записей/с — не проблема throughput. Покупает запас, который не нужен, и ломает SLA, ведь забуференные строки не durable и исчезают при крахе." },
    { label: "Батчить, но подтверждать пользователю сразу, до flush, чтобы скрыть добавленную latency.", misconception: "Хуже: ты сказал пользователю, что платёж прошёл, пока он лежит в волатильном буфере. Это превращает проблему latency в потерю данных плюс ложь — ровно то, что durability-SLA запрещает." },
    { label: "Батчить с окном 5 мс, чтобы штраф по latency был незаметным.", misconception: "Меньшее окно ужимает latency-налог, но не чинит две реальные проблемы: при 30 записях/с по-прежнему нет глубины очереди для амортизации, а любое забуференное-но-не-сброшенное окно всё равно может потерять подтверждённый платёж при крахе." },
  ]}
/>

<Quiz
  id="06-batching-choice-2"
  lessonSlug="06-batching"
  lang="ru"
  question="Система батчинга настроена только на max-size (без таймера max-wait). Ночью трафик падает. Каков failure mode и что чинит добавление таймера?"
  choices={[
    { label: "Первый элемент полупустого батча ждёт неограниченно, потому что при низкой нагрузке буфер никогда не достигает max-size — head-of-line stall, у которого latency растёт обратно пропорционально трафику. Таймер max-wait говорит 'отправь, что есть' и ограничивает эту latency.", correct: true },
    { label: "Батчи разрастаются слишком большими и переполняют downstream-ёмкость; таймер ограничивает их размер.", misconception: "Это failure mode max-wait-only под ВЫСОКОЙ нагрузкой. Size-only-батчер не может переполниться — он может только не сброситься. Таймер защищает latency, а не size." },
    { label: "Throughput обваливается, потому что каждый элемент по сути уходит сам по себе.", misconception: "Size-only-батчинг всё равно батчит; элементы копятся, пока буфер не заполнится. Дефект — неограниченная latency у ждущих элементов, а не потеря батчинга." },
    { label: "Ничего — при низкой нагрузке нечего оптимизировать, так что size-only-окно норм.", misconception: "Низкая нагрузка — именно когда size-only опасен: буфер никогда не наполняется, и самый старый элемент может пролежать секунды. Это баг latency, и он хуже всего ровно тогда, когда система выглядит idle." },
  ]}
/>

<Quiz
  id="06-batching-choice-3"
  lessonSlug="06-batching"
  lang="ru"
  question="Для payload'ов 4 КБ, где фиксированная цена вызова ~50 µs, а передача ~40 µs на КБ — даст ли большой батч сильный speedup?"
  choices={[
    { label: "Нет. Переменная цена (4 × 40 µs = 160 µs) уже превышает фиксированную (50 µs), значит F < V·n: ты за break-even, в режиме speedup→1, и batching в основном лишь добавляет latency.", correct: true },
    { label: "Да — batching всегда умножает throughput на размер батча N.", misconception: "speedup → N только когда доминирует фиксированная цена (F > V·n). Здесь доминирует переменная, так что амортизировать почти нечего." },
    { label: "Да — большие payload'ы делают batching эффективнее.", misconception: "Наоборот: большие payload'ы толкают к speedup → 1, потому что фиксированная цена становится меньшей долей общей работы. Батчить крошечные сообщения — вот большой выигрыш." },
    { label: "Да — при 4 КБ ты ещё далеко под типичным batch.size, так что амортизация идёт свободно.", misconception: "Break-even — на элемент, не на батч. Как только переменная цена одного элемента превышает фиксированную, никакой размер батча не спасает — то, что ты влезаешь под batch.size, нерелевантно." },
  ]}
/>

<Quiz
  id="06-batching-choice-4"
  lessonSlug="06-batching"
  lang="ru"
  question="Брокеры Kafka сидят под 20% по диску и сети, но продьюсеры не могут выдать больше доли ёмкости. Продьюсеры на дефолтах Kafka 3.x. Какой первый фикс?"
  choices={[
    { label: "Поднять linger.ms до 5–100 мс и batch.size до 64–512 КБ. Дефолт 3.x linger.ms=0 отправляет крошечные батчи под ровной нагрузкой, ограничивая throughput далеко ниже железа; осознанное ожидание делает запросы реже и толще.", correct: true },
    { label: "Добавить партиций и инстансов продьюсера, чтобы поднять параллелизм.", misconception: "Больше клиентов, каждый шлёт крошечные батчи, не помогает — ты множишь мелкие запросы, а не делаешь каждый крупнее. Фикс — толстые батчи, а не их количество." },
    { label: "Включить compression.type=zstd и оставить linger.ms=0.", misconception: "Компрессия идёт per batch; при linger.ms=0 батчи остаются крошечными, и кодеку нечего эксплуатировать. Компрессия усиливает batching — но не заменяет его." },
    { label: "Перевести брокеры на более быстрые диски.", misconception: "Брокеры скучают под 20% — диск не bottleneck. Им является продьюсер, который отказывается ждать, так что быстрые диски брокера ничего не меняют." },
  ]}
/>

<Quiz
  id="06-batching-choice-5"
  lessonSlug="06-batching"
  lang="ru"
  question="Консьюмер Kafka берёт poll'ом 500 записей; запись 47 бросает на десериализации. Пайплайн ретраит весь батч на любую ошибку. Лаг консьюмера растёт до миллионов и поднимается, но консьюмер выглядит здоровым. Что происходит и каков фикс?"
  choices={[
    { label: "Poison-message stall: запись 47 бросает на каждом ретрае, offset никогда не продвигается, и партиция замораживается за одной плохой записью. Фикс — split-and-retry: бисекцией изолируй poison-элемент за O(log N) запросов, отправь в dead-letter, закоммить остальные.", correct: true },
    { label: "Консьюмер недопровижен; добавь инстансов консьюмера, чтобы разгрести лаг.", misconception: "Больше консьюмеров упрутся в ту же poison-запись и встанут одинаково. Лаг растёт, потому что offset не может пройти запись 47, а не из-за throughput." },
    { label: "Скипнуть весь батч на ошибке, чтобы консьюмер прошёл плохую запись.", misconception: "Это выбрасывает 499 хороших записей ради сброса одной — потеря данных, что для заказов или платежей может быть compliance-инцидентом. Split-and-retry изолирует один плохой элемент." },
    { label: "Увеличить poll timeout, чтобы у десериализатора было больше времени на успех.", misconception: "Запись 47 структурно повреждена — больше времени не сделает её парсящейся. Проблема — failure isolation, а не тайминг." },
  ]}
/>

<Quiz
  id="06-batching-choice-6"
  lessonSlug="06-batching"
  lang="ru"
  question="Брокер форсит wire-уровневый max message size, но сжатый батч в 1 МБ OOM-килит его (класс CVE-2023-34455). Какова правильная защита?"
  choices={[
    { label: "Форсить лимит на post-decompression-размер, проверяемый по мере разворачивания фрейма. Эксплойт — коэффициент расширения: крошечный сжатый фрейм может объявить чанк ~2 ГБ, так что wire-лимит ничего не говорит о том, во что он развернётся.", correct: true },
    { label: "Опустить wire-уровневый max message size ещё ниже.", misconception: "Атака целиком ниже wire-лимита: 1 МБ сжатого уже прошёл его. Уменьшение wire-cap не ограничивает разжатый размер, так что защиты нет." },
    { label: "Полностью отключить компрессию на брокере.", misconception: "Это выбрасывает выигрыш по throughput и хранению, и не нужно — post-decompression-граница (фикс snappy-java 1.1.10.1) закрывает дыру, сохраняя компрессию." },
    { label: "Добавить per-item idempotency-ключи, чтобы отклонить вредоносные записи.", misconception: "Idempotency останавливает replay — другую атаку. Decompression bomb не нуждается в дубликате payload — она OOM-килит брокер при разворачивании, до любой per-item-логики." },
  ]}
/>

<Recap lang="ru">
Через весь юнит проходит одна последовательность решений: подтверди, что фиксированная цена доминирует над переменной, подтверди, что есть глубина очереди и запас latency, затем размерь окно — max-size для throughput, max-wait для latency, что сработает первым — до наибольшего значения под твоим SLO. Break-even — на элемент при F = V·n. Production затем превращает каждое свойство эффективности в множитель сбоя: poison-элемент убивает N (split-and-retry плюс DLQ), быстрый продьюсер затопляет очередь (ограничь её, выбери block/drop/spill осознанно), а сжатый батч — непрозрачный конверт, который надо валидировать post-decompression и per-item. Повторяющаяся ловушка senior — оптимизировать throughput, который никто не платит, ломая контракт latency или durability.
</Recap>
