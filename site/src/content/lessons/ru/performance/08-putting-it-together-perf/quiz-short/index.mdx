---
concepts:
- profile-first
- amdahl-cap
- cost-shapes
- right-layer-fix
- verify-and-enforce
- batching-window
estMin: 14
lang: ru
level: senior
order: 8
prereqs: []
slug: quiz-short
sources:
- https://web.dev/articles/vitals
- https://go.dev/doc/gc-guide
status: ready
summary: Промпты на свободное воспроизведение по всему треку — измерительный цикл, потолок Амдала, пять форм цены, фикс на правильном слое и verify-then-enforce.
title: 'Performance capstone: свободное воспроизведение'
track: performance
unit: 08-putting-it-together-perf
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";

<Hook>
Воспроизведение бьёт перечитывание. Для каждого промпта собери полный ответ по памяти сквозь весь трек, прежде чем открыть модельный ответ — усилие свести связи воедино и есть то, что превращает семь юнитов в одну дисциплину.
</Hook>

<Goal lang="ru">
Восстанови хребет трека, не подсматривая: измерительный цикл, почему Амдал ограничивает любое ускорение, формы цены, по которым ты классифицируешь, как чинить на правильном слое и почему verify-then-enforce делает performance устойчивым.
</Goal>

<RetrievalDrawer
  client:visible
  id="08-putting-it-together-perf-short-retrieval"
  lang="ru"
  questions={[
    { q: "Назови пятишаговый цикл, к которому сводится весь трек, и что каждый шаг мешает сделать неправильно.", a: "Profile → classify → fix → verify → enforce. Profile first не даёт действовать по интуиции, которая ошибается чаще половины случаев. Classify заставляет назвать форму доминирующей цены (CPU, аллокация, cache, lock/round-trip, байты), чтобы тянуться к подходящему фиксу, а не к привычному. Fix на правильном слое не даёт латать симптом на уровень в стороне от причины. Verify под репрезентативной нагрузкой не даёт доверять цифре с ноутбука, упускающей продовую конкурентность и объём данных. Enforce — CI-бюджет, benchmark или алерт — не даёт выигрышу молча деградировать с приходом следующей фичи. Именно цикл, а не отдельный фикс, делает performance устойчивым свойством команды." },
    { q: "Почему закон Амдала делает «profile first» необсуждаемым, с конкретным числом?", a: "Закон Амдала ограничивает ускорение всей системы величиной 1 / ((1 − p) + p/s), где p — доля времени в части, которую ускоряешь в s раз. Если компонент — 5% wall time, то даже делая его бесконечно быстрым (s → ∞), получаешь максимум 1/0.95 ≈ 5.3% улучшения. Значит оптимизация чего угодно, что не доминирующая цена, имеет жёсткий потолок, заданный долей времени, которую она реально потребляет — а узнать эту долю нельзя без профилирования. Именно поэтому переписывание названного подозреваемого — ловушка: сериализатор на 3% ограничивает страницу выигрышем 3%, как бы умно его ни переписать." },
    { q: "Назови «формы» цены, которые может принять hot path, и первый ход под каждую.", a: "Hot path, который профилировщик находит снова и снова, обычно попадает в одну из форм: CPU-bound (функция делает тяжёлую математику/работу сама — снизь работу, лучший алгоритм, SIMD); allocation-bound (время в alloc + GC — устрани или pre-size аллокации, pool только то, что пометил профиль); cache-bound (стопоры на доступе к памяти при малом числе инструкций — улучши locality, contiguous layout, меньший working set); lock/round-trip-bound (ожидание contention или последовательного I/O — сократи критическую секцию, батчи или убери N+1 round-trip'ы); syscall/IO-bound (переходы в ядро или сеть — батчи, буферизуй, амортизируй фиксированную цену). Аппаратные счётчики (TMA) разрешают неоднозначные случаи. Форма, прочитанная из профиля, диктует слой, на котором чинишь." },
    { q: "Профиль показывает время в GC. Почему фикс обычно не в коллекторе и как это обобщается на весь трек?", a: "Пауза GC — симптом; rate аллокаций — причина. Тюнинг коллектора (GOGC, pause target) или его замена (G1 → ZGC) лишь оптимизируют цену работы по аллокации, которая всё равно происходит — низ лестницы фиксов. Устойчивый фикс — на аллокации: устрани её, pre-size её или pool её. Это обобщается на весь трек: всегда чини на слое, где цена создаётся, а не на уровень ниже по течению. Фикс N+1 — в паттерне доступа, а не в большей БД; фикс bundle — меньше байтов, а не более быстрый CDN; фикс проблемы per-op-оверхеда — batching, а не больше воркеров. Слой симптома трогай последним." },
    { q: "Объясни ключевой tradeoff batching'а и как то же рассуждение про per-op цену проявляется в других местах трека.", a: "Batching амортизирует фиксированную per-operation цену (установка соединения, auth, syscall, round-trip) по многим элементам, поэтому throughput растёт по мере деления фиксированной цены на размер батча. Плата — tail latency: элемент ждёт, пока окно батча наполнится или сработает таймер max-wait, поэтому размер батча и max-wait настраивают против latency SLO. То же рассуждение про «фиксированную цену на операцию» движет фиксами N+1 (один батч-запрос заменяет N round-trip'ов, каждый с фиксированной round-trip ценой) и инверсно проблеме bundle (там per-load фиксированная цена — байты, отгружаемые каждому пользователю, амортизируемые отгрузкой меньшего). Распознать фиксированную per-op цену — это объединяющая линза." },
    { q: "Что значит verify и enforce фикса performance, и почему зелёный локальный benchmark — недостаточно?", a: "Verify — это переизмерение под той же репрезентативной нагрузкой, что и baseline — идентичный объём данных, конкурентность, состояние кешей и окружение — и сравнение before/after, а не оценка. Benchmark на ноутбуке упускает продовую конкурентность, contention, объём данных и холодные кеши, поэтому локальный выигрыш может исчезнуть или даже развернуться в проде. Enforce — это закрепить выигрыш: per-route bundle budget, валящий CI, benchmark в пайплайне, помечающий регрессии, или алерт на SLI. Без enforce выигрыш деградирует в момент, когда заползёт новая зависимость, N+1 или аллокация — невидимо, пока не почувствуют пользователи. Verify доказывает фикс; enforce его удерживает." },
  ]}
/>

<Recap lang="ru">
Если ты смог восстановить каждый ответ по памяти, ты держишь хребет трека: один цикл — profile, classify, fix, verify, enforce — подкреплённый потолком Амдала на оптимизацию не того, маленьким словарём форм цены, отображающим симптом в фикс, дисциплиной чинить на собственном слое цены, а не на ручке ниже по течению, и привычкой доказывать выигрыш под реальной нагрузкой и фиксировать его в CI. Performance — это цикл, а не отдельный фикс. Теперь, столкнувшись с медленной системой, ты поймаешь себя на том, что тянешься к профилировщику раньше, чем к ближайшему фиксу — этот рефлекс и есть весь трек в одном движении.
</Recap>
