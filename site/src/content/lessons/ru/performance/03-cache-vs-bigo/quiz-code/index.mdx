---
concepts:
- row-major-access
- aos-vs-soa
- false-sharing-padding
- pointer-chasing-mlp
estMin: 14
lang: ru
level: senior
order: 9
prereqs: []
slug: quiz-code
sources:
- https://en.wikipedia.org/wiki/CPU_cache
- https://en.algorithmica.org/hpc/pipelining/branching/
status: ready
summary: Читаешь реальные сниппеты — strided-цикл, AoS-структуру, padded-счётчик и pointer chain, предсказываешь поведение кэша и pipeline, выбираешь фикс с наибольшим рычагом.
title: 'Cache vs big-O: чтение кода и раскладки'
track: performance
unit: 03-cache-vs-bigo
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Проблемы кэша диагностируют в коде и профиле, а не в big-O. Прочитай каждый сниппет, предскажи, куда реально уходит wall-clock time, и выбери фикс, к которому senior-инженер тянется первым.
</Hook>

<Goal lang="ru">
Отработай петлю, которую ты гоняешь в каждом инциденте производительности: прочитал горячий путь, нашёл дефект раскладки или паттерна доступа, который подтвердит профайлер, и выбрал фикс с наибольшим рычагом до SIMD-интринсиков и флагов компилятора.
</Goal>

## Сниппет 1 — strided-сумма

```c
double sum = 0;
// матрица row-major: matrix[i][j] по смещению (i*N + j)*8 байт
for (int j = 0; j < N; j++)        // внешний: индекс столбца
    for (int i = 0; i < N; i++)    // внутренний: индекс строки
        sum += matrix[i][j];
```

<Quiz
  id="03-cache-vs-bigo-code-1"
  lessonSlug="03-cache-vs-bigo"
  lang="ru"
  question="На матрице 10 000×10 000 double, почему этот цикл в ~9 раз медленнее, чем при перестановке двух циклов, и какой фикс?"
  choices={[
    { label: "Внутренний цикл меняет i, поэтому соседние доступы прыгают на N×8 = 80 000 байт — свежая, не-префетченная cache line на каждом шаге, платя полную latency RAM. Поменяй циклы местами, чтобы внутренний индекс был j (шаги по 8 байт): тогда 7 из 8 чтений попадут в L1.", correct: true },
    { label: "Узкое место — сложение `sum +=`; используй SIMD.", misconception: "Floating-point add почти бесплатен; CPU простаивает в ожидании памяти. SIMD всё равно не загрузит strided-столбец эффективно — сначала исправь порядок доступа." },
    { label: "Ничего не так — оба порядка цикла обходят те же элементы.", misconception: "Они обходят те же элементы, но в другом порядке памяти. Column-порядок на row-major хранении промахивается мимо кэша практически на каждом доступе; разрыв по wall-clock ~9x." },
    { label: "Увеличь выравнивание выделения матрицы.", misconception: "Выравнивание не меняет шаг в 80 000 байт. Дефект — итерация поперёк раскладки хранения; переупорядочь циклы или сделай tiling матрицы." },
  ]}
/>

## Сниппет 2 — раскладка структуры

```c
struct Particle { float x, y, z, vx, vy, vz; char name[40]; };  // 64 байта
Particle parts[1_000_000];

float total_x = 0;
for (int i = 0; i < 1_000_000; i++)
    total_x += parts[i].x;          // читается только x
```

<Quiz
  id="03-cache-vs-bigo-code-2"
  lessonSlug="03-cache-vs-bigo"
  lang="ru"
  question="Цикл читает только `x`, но профиль показывает высокий L3 miss rate. В чём дефект раскладки и фикс?"
  choices={[
    { label: "AoS: каждая 64-байтная Particle заполняет целую cache line, поэтому загрузка одного `x` тянет 60 байт неиспользуемых полей — в линии один полезный float, а не 16. Раздели на SoA (плоский `float xs[N]`); теперь 16 значений x помещаются в линию, а SIMD грузит 8 за раз.", correct: true },
    { label: "Массив слишком велик для L3, поэтому ничто не поможет.", misconception: "Корень не в размере, а в потраченной bandwidth. SoA сжимает объём затронутых байт ~16x для этого цикла, поэтому гораздо больше данных остаётся резидентным в кэше, и prefetcher их стримит." },
    { label: "Добавь `__builtin_prefetch` на parts[i+8].", misconception: "Prefetch чуть помогает, но ты всё равно тянул бы 64 байта на элемент, чтобы использовать 4. Структурный фикс — SoA, который убирает потраченные 60 байт на линию." },
    { label: "Пометь структуру `packed`, чтобы убрать паддинг.", misconception: "Она уже 64 байта без зазоров; packing ничего не меняет. Потери — это холодные поля (`y…name`), соседствующие с горячим, что чинит только struct-splitting/SoA." },
  ]}
/>

## Сниппет 3 — padded-счётчик

```go
type paddedCounter struct {
    value uint64
    _     [56]byte   // паддинг до полной 64-байтной cache line
}
var counters [16]paddedCounter

func worker(idx int) {                     // одна горутина на idx
    atomic.AddUint64(&counters[idx].value, 1)
}
```

<Quiz
  id="03-cache-vs-bigo-code-3"
  lessonSlug="03-cache-vs-bigo"
  lang="ru"
  question="Что покупает паддинг `[56]byte` и какой сбой он предотвращает?"
  choices={[
    { label: "Он вынуждает каждый счётчик на свою 64-байтную cache line, поэтому атомарная запись горутины больше не инвалидирует линию соседа. Без него счётчики делят линии, и MESI пинг-понгует их между ядрами — false sharing, делающий lock-free путь медленнее мьютекса.", correct: true },
    { label: "Он ускоряет сам атомарный add.", misconception: "Атомарная инструкция не меняется. Паддинг убирает межъядерный трафик когерентности (инвалидации линий), а не стоимость отдельной инструкции." },
    { label: "Он предотвращает data race на значении счётчика.", misconception: "atomic.AddUint64 уже делает инкремент гонко-безопасным. Паддинг адресует проблему производительности (false sharing), а не корректности." },
    { label: "Он уменьшает суммарную память, упаковывая счётчики плотно.", misconception: "Он делает обратное — раздувает каждый счётчик с 8 до 64 байт. Размен — память в обмен на устранение cache-line contention." },
  ]}
/>

## Сниппет 4 — pointer chain

```c
// idx[] хранит следующий индекс для посещения; следование за ним data-dependent
int i = start;
for (int step = 0; step < N; step++) {
    process(data[i]);
    i = idx[i];          // следующий адрес зависит от результата этой загрузки
}
```

<Quiz
  id="03-cache-vs-bigo-code-4"
  lessonSlug="03-cache-vs-bigo"
  lang="ru"
  question="Даже когда `data` и `idx` влезают в L2, это работает намного медленнее последовательного сканирования тех же массивов. Почему и какой структурный фикс?"
  choices={[
    { label: "Каждый `i = idx[i]` — это dependent load: CPU не может выдать следующий доступ, пока этот не вернётся, поэтому промахи сериализуются и memory-level parallelism (8–16 в полёте) теряется. Prefetcher тоже не угадает data-dependent адрес. Фикс: предвычислить порядок обхода в contiguous массив индексов, чтобы загрузки были независимыми и префетчабельными.", correct: true },
    { label: "Массивы слишком малы, чтобы выиграть от кэширования.", misconception: "Они влезают в L2 — с ёмкостью всё хорошо. Стоимость — цепочка зависимостей, сериализующая промахи; это проблема паттерна доступа, а не ёмкости." },
    { label: "Узкое место — process(), а не доступ к памяти.", misconception: "Простои — на dependent-загрузках `idx[i]`. Даже тривиальный process() оставляет цикл memory-latency-bound, потому что каждый шаг ждёт предыдущей загрузки." },
    { label: "Переход на рекурсию позволил бы компилятору распараллелить это.", misconception: "Рекурсия не разрывает зависимость по данным. Нужно материализовать порядок в независимые, последовательные доступы, чтобы MLP и prefetcher включились." },
  ]}
/>

<Recap lang="ru">
Каждый сниппет здесь — дефект в константном множителе, которого big-O не видит: column-порядок цикла на row-major хранении промахивается на каждом шаге (переупорядочь или tiling); AoS-структура тянет холодные поля в линию для цикла по одному полю (раздели на SoA); непадженные счётчики на поток пинг-понгуют cache lines (паддинг до 64 байт); а dependent pointer chain сериализует промахи и ломает и MLP, и prefetcher (предвычисли contiguous порядок). Читай паттерн доступа, назови причину, исправь раскладку — затем перепрофилируй для подтверждения.
</Recap>
