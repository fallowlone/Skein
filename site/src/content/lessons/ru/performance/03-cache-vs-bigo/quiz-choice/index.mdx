---
concepts:
- memory-hierarchy
- cache-line
- false-sharing
- branch-prediction
- soa-vs-aos
- perf-stat-diagnosis
estMin: 13
lang: ru
level: senior
order: 7
prereqs: []
slug: quiz-choice
sources:
- https://en.wikipedia.org/wiki/CPU_cache
- https://en.algorithmica.org/hpc/pipelining/branching/
- https://www.cs.cornell.edu/courses/cs3110/2012sp/lectures/lec25-locality/lec25.html
status: ready
summary: Тест с выбором на синтез по всему юниту — memory hierarchy, cache lines, false sharing, branch prediction, раскладка данных и чтение perf-профиля.
title: 'Cache vs big-O: тест с выбором ответа'
track: performance
unit: 03-cache-vs-bigo
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Шесть вопросов поперёк всего юнита. Ни один — не определение для заучивания: каждый отражает решение, которое ты принимаешь, когда «быстрый» алгоритм проигрывает «медленному», а big-O об этом молчит.
</Hook>

<Goal lang="ru">
Убедись, что связываешь memory hierarchy, поведение cache line, false sharing, branch prediction и раскладку данных в один диагностический рефлекс: прочитал профиль, назвал причину в константном множителе, выбрал подходящий фикс.
</Goal>

<Quiz
  id="03-cache-vs-bigo-choice-1"
  lessonSlug="03-cache-vs-bigo"
  lang="ru"
  question="Связный список из 1M узлов итерируется в ~17 раз медленнее массива из 1M int, хотя оба O(N). Какое утверждение лучше объясняет разрыв?"
  choices={[
    { label: "Элементы массива contiguous, поэтому prefetcher заполняет cache lines заранее и большинство чтений попадают в L1 (~1 нс). Каждый узел списка лежит по случайному heap-адресу, поэтому каждый разыменованный `.next` — холодный промах в RAM (~70–100 нс). Big-O скрывает этот 100x-множитель.", correct: true },
    { label: "Big-O просто неверен для связных структур.", misconception: "Big-O асимптотичен и верен — оба O(N). Он опускает константу на операцию, которую задаёт memory hierarchy: ~1 нс при попадании в кэш против ~100 нс при промахе в RAM." },
    { label: "Связный список выполняет больше инструкций на узел.", misconception: "Число инструкций на узел сопоставимо; CPU простаивает в ожидании памяти, а не на лишней арифметике. Стоимость — это latency промаха, а не количество операций." },
    { label: "Массив влезает в кэш, а список нет — чисто вопрос ёмкости.", misconception: "Даже когда оба рабочих набора превышают кэш, массив всё равно выигрывает: последовательный доступ префетчится, а pointer-chasing data-dependent и непредсказуем. Дело в паттерне доступа, а не только в ёмкости." },
  ]}
/>

<Quiz
  id="03-cache-vs-bigo-choice-2"
  lessonSlug="03-cache-vs-bigo"
  lang="ru"
  question="Команда меняет порядок внутреннего цикла matrix multiply с row-major на column-major на матрице 10 000×10 000 double и видит ~9x замедление. Арифметика идентична. Корневая причина?"
  choices={[
    { label: "Шаг в column-major продвигается на 10 000×8 = 80 000 байт за доступ — далеко за пределы только что загруженной 64-байтной cache line. Почти каждое чтение попадает в свежую, не-префетченную линию и платит полную latency RAM. Те же загрузки, ~100x стоимость каждой.", correct: true },
    { label: "Big-O-сложность стала хуже, чем O(N²).", misconception: "Оба порядка цикла — O(N²) с одинаковым числом операций. Регрессия целиком в константном множителе (cache-miss rate), а не в асимптотическом классе." },
    { label: "Column-доступ требует другого флага компилятора для векторизации.", misconception: "Никакой флаг не исправит шаг в 80 000 байт. Сам паттерн доступа ломает prefetcher и SIMD; фикс — переупорядочить циклы или cache-oblivious tiling, а не флаг." },
    { label: "Столбцы длиннее строк в высокой матрице.", misconception: "Это квадратная матрица 10k×10k — строки и столбцы одинаковой длины. Разрыв задаёт раскладка в памяти (row-major хранение), а не размерность." },
  ]}
/>

<Quiz
  id="03-cache-vs-bigo-choice-3"
  lessonSlug="03-cache-vs-bigo"
  lang="ru"
  question="Массив счётчиков сделали lock-free через атомики, и он стал медленнее версии с локом по мере роста числа потоков. perf показывает IPC 0.4 и 72% cache-miss rate. Диагноз?"
  choices={[
    { label: "False sharing. Несколько счётчиков на поток делят одну 64-байтную линию; каждая атомарная запись вызывает MESI-инвалидацию этой линии во всех других ядрах, и линия пинг-понгует на latency L3. Фикс: дополнить каждый счётчик до своей cache line паддингом.", correct: true },
    { label: "Lock contention — атомики по сути спинлок.", misconception: "Атомики — не локи. Lock contention проявляется как off-CPU wait time; эта сигнатура — высокая загрузка CPU, обвал IPC, высокий cache-miss rate — это memory-bound трафик когерентности, то есть false sharing." },
    { label: "Массив счётчиков переполнил ёмкость L3.", misconception: "Горстка uint64-счётчиков — это ~128 байт, тривиально резидентна в L1. Дефект — несколько ядер пишут в одну 64-байтную линию, а не ёмкость." },
    { label: "Branch mispredictions в атомарном инкременте.", misconception: "У атомарного add нет data-dependent ветки. Промахи веток поднимают branch-miss counter, а не cache-miss counter; цифра 72% указывает прямо на память." },
  ]}
/>

<Quiz
  id="03-cache-vs-bigo-choice-4"
  lessonSlug="03-cache-vs-bigo"
  lang="ru"
  question="Сортировка массива перед суммированием элементов выше порога даёт 2–3x ускорение, несмотря на лишнюю O(N log N) сортировку. Почему это окупается?"
  choices={[
    { label: "Сортировка группирует все под-пороговые элементы вместе, затем все над-пороговые. Data-dependent ветка превращается в длинную серию 'not-taken', затем 'taken', поэтому предиктор достигает ~99% точности и избегает 10–30-цикловых pipeline flush, которые платил на ~50% итераций.", correct: true },
    { label: "Сортировка уменьшает число сравниваемых элементов.", misconception: "Каждый элемент после сортировки всё равно сравнивается с порогом — число сравнений не меняется. Выигрыш — в предсказуемости ветки, а не в меньшем числе сравнений." },
    { label: "Отсортированные данные удобнее для кэша при сканировании.", misconception: "Линейное сканирование массива уже cache-friendly, отсортирован он или нет. Доминирующий эффект здесь — branch prediction, а не локальность." },
    { label: "Компилятор авто-векторизует только отсортированные циклы.", misconception: "Авто-векторизация не зависит от порядка сортировки. Документированное, не зависящее от архитектуры ускорение — от превращения непредсказуемой ветки в предсказуемую." },
  ]}
/>

<Quiz
  id="03-cache-vs-bigo-choice-5"
  lessonSlug="03-cache-vs-bigo"
  lang="ru"
  question="Горячий ML-цикл работает в 5 раз медленнее эталонного C++-порта; perf показывает IPC 0.8 и 25% L3 miss rate. Кто-то предлагает добавить SIMD-интринсики. Какой первый шаг правильный?"
  choices={[
    { label: "Сначала исправить раскладку: медленная версия хранит активации как AoS, где поля чередуются, поэтому SIMD потребовал бы gather/scatter в 10x медленнее. Перевести в SoA (по одному плоскому массиву на поле); IPC растёт к ~3.0, L3-промахи падают, и только тогда SIMD помогает.", correct: true },
    { label: "Добавить AVX-интринсики прямо в существующий AoS-цикл.", misconception: "SIMD на AoS-данных вынуждает gather/scatter (~10x от contiguous load) и ничего не даёт memory-bound циклу. IPC 0.8 + 25% L3 miss — проблема раскладки; SIMD на плохой раскладке не покупает ничего." },
    { label: "Добавить -O3 -march=native и остановиться.", misconception: "Флаги стоит проверить, но они не превратят чередующуюся AoS-раскладку в contiguous SIMD-дружественные данные. Сигнатура — memory-bound, а не недо-оптимизированные вычисления." },
    { label: "Переписать алгоритм — это наверняка скрытая O(N²).", misconception: "Обе версии используют один алгоритм; 5x разрыв при высоком L3 miss rate — это константная стоимость раскладки, а не асимптотическая разница." },
  ]}
/>

<Quiz
  id="03-cache-vs-bigo-choice-6"
  lessonSlug="03-cache-vs-bigo"
  lang="ru"
  question="perf stat на горячем цикле показывает IPC 0.40, branch-miss 25% и LLC-miss 47%. Можно сначала исправить одно. Что и почему?"
  choices={[
    { label: "47% LLC miss — почти половина обращений к L3 уходит в RAM, крупнейший источник простоя. Перестроить данные (SoA, упаковать горячие поля, тайлинг под L3) ради наибольшего единичного выигрыша; затем взяться за 25% branch miss. IPC — следствие обоих.", correct: true },
    { label: "Branch miss — pipeline flush всегда доминируют.", misconception: "10–30-цикловый flush реален, но промах в RAM — ~300 циклов, и 47% обращений к L3 промахиваются. Memory stall здесь — больший рычаг; сначала фикс раскладки, затем ветки." },
    { label: "Поднять IPC напрямую, выдавая больше инструкций.", misconception: "IPC — не ручка, а результат простоев. Он равен 0.40, потому что CPU ждёт память и мисспредикты веток. Убери эти причины — и IPC поднимется сам." },
    { label: "Переключиться на алгоритм с меньшим big-O.", misconception: "Ничто в профиле не указывает на асимптотический класс; он указывает на константные множители — раскладку и поведение веток. 'Лучший' big-O с худшей локальностью легко может быть медленнее." },
  ]}
/>

<Recap lang="ru">
Сквозная линия юнита — одно дерево решений: big-O оценивает рост стоимости с N, но memory hierarchy задаёт константу, и она колеблется ~100x в зависимости от раскладки. Contiguous бьёт pointer-chasing; порядок цикла должен совпадать с порядком хранения; записи на поток нуждаются в своей cache line; непредсказуемым веткам нужна сортировка или branchless; работа по одному полю хочет SoA. Когда перед тобой профиль — сначала читай miss rate и IPC, назови причину в константном множителе и применяй подходящий фикс: измеряй, не угадывай.
</Recap>
