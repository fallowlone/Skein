---
concepts:
- hardware-prefetcher
- tlb
- huge-pages
- mlp
- non-temporal-stores
- cache-associativity
- software-prefetch
deepensInto: []
estMin: 16
lang: ru
lessonType: topic
level: senior
order: 6
prereqs:
- performance/03-cache-vs-bigo/03-cache-lines-and-false-sharing
- performance/03-cache-vs-bigo/05-simd-and-data-layout
slug: 06-hardware-prefetcher-and-tlb
sources:
- https://en.wikipedia.org/wiki/CPU_cache
- https://www.cs.cornell.edu/courses/cs3110/2012sp/lectures/lec25-locality/lec25.html
- https://learn.arm.com/learning-paths/servers-and-cloud-computing/memory-subsystem/cache-hierarchy/
- https://ivoroshilin.wordpress.com/2013/02/06/know-your-locality-of-reference-some-techniques-for-keeping-data-in-the-cpu-cache/
spiral:
- latency
status: ready
summary: Prefetcher подгружает до 16 cache lines вперёд на последовательных паттернах
  — и молчит на pointer chaining. TLB miss стоит 5–50 циклов за page walk. Huge pages
  сокращают TLB pressure в 512x. MLP позволяет 8–16 miss-ов параллельно — если loads
  независимы.
title: Hardware prefetcher, TLB и memory-level parallelism
track: performance
unit: 03-cache-vs-bigo
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import NumbersCard from "~/components/layout/NumbersCard.astro";
import Misconception from "~/components/layout/Misconception.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";
import Infographic from "~/components/diagram/Infographic.astro";
import mlpSerialVsParallel from "./infographics/mlp-serial-vs-parallel.svg?raw";

<Hook>
Graph traversal. Graph влезает в L2 — 4 MB, L2 4 MB. Каждый узел в cache. Но loop всё равно медленный. Hardware prefetcher знает, что cache line загружена — и всё равно не помогает. Почему?
</Hook>

<Crux>Prefetcher: до 16 lines вперёд — только предсказуемые паттерны. Pointer chaining defeats MLP. TLB: ещё один уровень latency вне L1/L2/L3.</Crux>

<Explanation>

## Hardware prefetcher: типы и поведение

Когда видишь loop с высоким L3 miss rate, хотя весь граф точно влезает в cache, — причина почти всегда в паттерне, который prefetcher не умеет предсказывать. Разбор его возможностей даст точный ответ, какой именно фикс нужен.

Современные CPU имеют несколько hardware prefetchers:

1. **Sequential prefetcher** — детектирует forward strides. Кикается после 2–3 consecutive cache misses в одном направлении; раз активен, prefetches до **16 lines вперёд**. Defeat: random access, слишком длинный stride.

2. **Stride detector** — ловит паттерны «каждый N-й байт». Работает для `arr[i*4]` — stride 4. Defeat: irregular stride.

3. **Content-based prefetcher** (на некоторых chips) — следует pointer-like значениям. Ограниченный; pointer chaining всё равно defeat-ит его.

**Помогать prefetcher-у:**
- Hot loop stride ≤ 256 байт, consistent.
- Избегай conditional access patterns в inner loop.

**Defeating prefetcher:**
- Pointer chaining: `A.next.next.next` — каждый следующий адрес зависит от предыдущего result.
- Random array access (hash probing, graph traversal).

## Software prefetch

Когда hardware не может предсказать паттерн, но разработчик может:

```c
// prefetch данные за N итераций вперёд
__builtin_prefetch(&data[i + 8], 0, 1);  // GCC/Clang
```

Полезно для graph traversals, где следующий узел data-dependent. Hint CPU загрузить следующий узел, пока обрабатываешь текущий.

## TLB и page-level locality

За пределами L1/L2/L3 существует ещё один уровень: **TLB (Translation Lookaside Buffer, буфер быстрого преобразования адресов)** — кеш virtual-to-physical address translations, который CPU обращает до любой проверки L1.

CPU работает с virtual addresses. Для доступа к памяти нужно перевести virtual address в physical. Этот перевод — **page walk** через page tables в RAM. TLB кеширует результаты последних переводов.

**TLB miss** → page walk → 5–50 циклов задержки.

По умолчанию pages 4 KB. L1 TLB обычно: 32–64 entries (covers ~256 KB). L2 TLB: 512–4096 entries.

Workload, трогающий random pages (каждая ~4 KB), blow out TLB даже если данные влезают в L3.

## Huge pages

**Huge pages** снижают TLB pressure резко:

- Standard page: **4 KB**. TLB entry covers 4 KB.
- Huge page (x86): **2 MB** = 512 × standard. Один TLB entry covers в 512x больше.
- 1 GB pages: 262 144 × standard. Один entry covers 1 GB.

Для high-perf кода, трогающего гигабайты — databases, ML inference, image processing — huge pages могут дать **10–20% speedup** независимо от cache locality.

Linux: `madvise(MADV_HUGEPAGE)` или `/sys/kernel/mm/transparent_hugepage/enabled = always`. jemalloc и mimalloc поддерживают huge pages автоматически.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Access path</th>
        <th class="border border-border px-3 py-2 text-left">Latency</th>
        <th class="border border-border px-3 py-2 text-left">Циклы</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">L1 hit</td>
        <td class="border border-border px-3 py-2">~1 нс</td>
        <td class="border border-border px-3 py-2">3–5</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">L2 hit</td>
        <td class="border border-border px-3 py-2">~3 нс</td>
        <td class="border border-border px-3 py-2">10–15</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">L3 hit</td>
        <td class="border border-border px-3 py-2">~10 нс</td>
        <td class="border border-border px-3 py-2">30–50</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">RAM (TLB hit)</td>
        <td class="border border-border px-3 py-2">~70–100 нс</td>
        <td class="border border-border px-3 py-2">200–300</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">RAM + TLB miss</td>
        <td class="border border-border px-3 py-2">~120–150 нс</td>
        <td class="border border-border px-3 py-2">350–500</td>
      </tr>
    </tbody>
  </table>
</div>

## Memory-level parallelism (MLP)

Современные CPU могут иметь **8–16 cache misses outstanding одновременно**. Это out-of-order execution: CPU issue-ит несколько loads не дожидаясь предыдущих.

**Independent loads** получают full MLP:
```cpp
// CPU может issue все 4 loads параллельно
float a = arr[idx_a];  // miss → outstanding
float b = arr[idx_b];  // miss → outstanding  
float c = arr[idx_c];  // miss → outstanding
float d = arr[idx_d];  // miss → outstanding
// Latency: 1 miss ≈ 100 нс (не 4 × 100 нс)
```

**Dependent-load chains** полностью defeat MLP:
```cpp
// Каждый load ждёт result предыдущего
Node *p = head;          // miss 1 → 100 нс
p = p->next;             // miss 2 → нужен result miss 1
p = p->next;             // miss 3 → нужен result miss 2
// Latency: N × 100 нс
```

Linked list traversal — worst case: одна pointer chase = одна serialized miss = 100 нс. 1 млн узлов → 100 мс, независимо от cache size.

<Infographic
  svg={mlpSerialVsParallel}
  label="Сравнение бок о бок: зависимые загрузки с обходом указателей сериализуются в N латентностей промаха при одном промахе в полёте, а независимые загрузки по массиву индексов совмещаются примерно в одну латентность при 8–16 промахах в полёте."
  caption="То же число промахов, тот же CPU: параллельность определяет зависимость данных, а не размер кеша."
  hue="var(--d-infra)"
/>

## Non-temporal stores

Streaming через гигабайты памяти (memcpy-like workloads) не benefit-ит от кеширования — данные read once и discarded. **Non-temporal store** инструкции bypass cache и пишут directly в memory:

```c
// x86: non-temporal store, обходит кеш
_mm_stream_ps(&dst[i], val);     // SSE
_mm256_stream_ps(&dst[i], val);  // AVX2

// GCC/Clang:
__builtin_nontemporal_store(val, &dst[i]);
```

Для bandwidth-bound workloads (DB scans, log processing) non-temporal stores улучшают sustained bandwidth на **20–30%** — L3 не засоряется one-shot data.

## Cache associativity и conflict misses

Caches не fully associative — каждая cache line может быть placed только в нескольких specific **ways** (обычно 8–16 way для L1). Если много hot addresses случайно map в тот же set, они evict друг друга — conflict misses.

Классический пример: stride pattern, совпадающий с cache size modulo.

Митигация: pad data structures на extra cache line для разрыва alignment patterns. `cachegrind` reports D1mr/D1mw (L1 miss rate) per source line, экспонируя conflict misses.

<NumbersCard
  id="06-numbers"
  title="Hardware prefetcher, TLB и MLP числа"
  stats={[
    { label: "Prefetcher: max prefetch ahead", value: "16 lines" },
    { label: "TLB miss: page walk cost", value: "5–50 циклов" },
    { label: "Huge page (x86): 2 MB", value: "512x меньше TLB pressure" },
    { label: "MLP: outstanding misses", value: "8–16 типично" },
    { label: "Non-temporal store bandwidth gain", value: "20–30% streaming" },
    { label: "L1 cache associativity", value: "типично 8-way" },
  ]}
/>

<Inset kind="why" lang="ru">
Allocator awareness дополняет huge pages: jemalloc и mimalloc batch related allocations в одну page region. Для high-perf кода это значит, что связанные данные не только в одном NUMA region, но и в одном physical page — TLB entry кеширует translation для всей группы сразу. PostgreSQL memory contexts, Go arena allocator, Rust bumpalo — все используют этот принцип.
</Inset>

<Misconception id="06-mc-prefetch">
  <p><strong>«Данные в cache — prefetcher поможет.»</strong> Нет: prefetcher видит только предсказуемые паттерны. Pointer chase defeats его полностью, даже если граф в L2. Размер cache не решает проблему pointer chaining — dependent loads сериализованы.</p>
</Misconception>

<Quiz
  id="06-quiz-1"
  lessonSlug="06-hardware-prefetcher-and-tlb"
  lang="ru"
  question="Почему hardware prefetcher fails на graph traversals, даже когда граф влезает в L2?"
  choices={[
    { label: "Каждое посещение узла имеет data-dependent next address — prefetcher не может предсказать следующий address без выполнения load сам, поэтому не помогает", correct: true },
    { label: "Prefetcher работает только с sequential reads", misconception: "Современные prefetchers handle strides; pure sequential не required. Проблема — unpredictability, не sequential requirement." },
    { label: "Графы слишком большие для prefetcher", misconception: "Вопрос states, что граф влезает; размер — не проблема." },
    { label: "Компиляторы disable prefetch на graph коде", misconception: "Компиляторы не контролируют hardware prefetch; hardware решает." },
  ]}
/>

<Quiz
  id="06-quiz-2"
  lessonSlug="06-hardware-prefetcher-and-tlb"
  lang="ru"
  question="10 независимых cache misses в одном hot loop на CPU с 10 outstanding misses. Суммарная latency?"
  choices={[
    { label: "~100–150 нс (≈ одна miss latency): CPU issue-ит все 10 loads параллельно через MLP. Latency = max(individual latencies), не сумма.", correct: true },
    { label: "~1000 нс (10 × 100 нс): misses последовательны", misconception: "Только для dependent chains; независимые loads могут overlap." },
    { label: "~10 нс: все hits из cache", misconception: "Если misses, то по определению не hits." },
    { label: "Зависит от OS scheduler", misconception: "MLP — hardware-уровень; OS scheduler здесь не фактор." },
  ]}
/>

<DragOrder
  id="06-drag-1"
  lessonSlug="06-hardware-prefetcher-and-tlb"
  lang="ru"
  prompt="Поставь шаги virtual memory access с TLB miss:"
  items={[
    { id: "a", label: "CPU нужна data по virtual address X" },
    { id: "b", label: "Проверить TLB: translation для страницы X кеширована?" },
    { id: "c", label: "TLB miss: запустить page walk через page tables в памяти" },
    { id: "d", label: "Page walk стоит 5–50 циклов; найдён physical address Y" },
    { id: "e", label: "TLB обновлён записью {virtual page → physical frame}" },
    { id: "f", label: "Обращение к cache/RAM по physical address Y" },
  ]}
  correctOrder={["a", "b", "c", "d", "e", "f"]}
/>

</Explanation>

<FlowDiagram
  label="Трансляция адреса + путь доступа"
  hue="var(--d-hardware)"
  perRow={4}
  nodes={[
    { id: "vaddr", label: "Виртуальный адрес", sub: "load-инструкция" },
    { id: "tlb", label: "Поиск в TLB", sub: "virtual → physical" },
    { id: "walk", label: "Page walk", sub: "miss: 5–50 циклов" },
    { id: "cache", label: "Поиск в cache", sub: "L1 → L2 → L3 → RAM" },
    { id: "prefetch", label: "Prefetcher", sub: "только sequential / stride" },
  ]}
  edges={[
    { from: "vaddr", to: "tlb" },
    { from: "tlb", to: "cache", label: "hit: 0–1 цикл" },
    { from: "tlb", to: "walk", label: "miss" },
    { from: "walk", to: "cache" },
    { from: "prefetch", to: "cache", label: "подгружает lines" },
  ]}
  caption="Каждый доступ платит TLB-трансляцию до любой проверки cache. Prefetcher подгружает cache lines заранее для stride-паттернов — но не может вывести pointer-chasing."
/>

<KeyTakeaway>Hardware prefetcher подгружает до 16 lines вперёд для последовательных/stride паттернов — и полностью бессилен при pointer chaining. TLB кеширует virtual-to-physical translations: miss стоит 5–50 циклов; huge pages (2 MB) снижают pressure в 512x. MLP позволяет 8–16 независимых miss-ов параллельно — но dependent load chains (linked list) сериализуют каждый. Non-temporal stores bypass cache для streaming workloads: +20–30% bandwidth.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="06-retrieval"
  lang="ru"
  questions={[
    { q: "Почему dependent pointer chain (A.next.next.next) медленнее, чем N независимых loads, даже при том же числе cache misses?", a: "MLP (Memory-Level Parallelism): современные CPU могут иметь 8–16 cache misses outstanding одновременно через out-of-order execution. Для независимых loads (N indep. array accesses) — CPU issue-ит все N loads параллельно; итоговая latency ≈ max(latencies), не сумма. Для dependent chain (A.next.next.next) — каждый load нуждается в result предыдущего (address следующего узла = value текущего load). MLP не может помочь: следующий load не может быть issued до завершения предыдущего. N misses × 100 нс = N × 100 нс total. Linked list из 1М узлов = 1М × 100 нс = 100 мс, независимо от размера cache." },
    { q: "Когда huge pages дают speedup и почему?", a: "Huge pages (2 MB) снижают TLB pressure в 512x по сравнению со standard pages (4 KB). TLB кеширует virtual-to-physical translations; L1 TLB обычно 32–64 entries, покрывая 128–256 KB. Workload, трогающий random pages (каждая 4 KB), быстро exhausts TLB — частые TLB miss-ы требуют page walk в RAM (5–50 cycles). С huge pages один TLB entry покрывает 2 MB — тот же workload требует в 512x меньше TLB entries. Speedup 10–20% типичен для: databases (Postgres, MySQL), ML inference (large weight matrices), image processing. Не помогает для: cache-bound workloads с маленьким working set (данные уже в L1/L2, TLB не bottleneck)." },
  ]}
/>

<Recap lang="ru">
Hardware prefetcher: sequential и stride детекторы active после 2–3 consecutive misses; prefetches до 16 lines вперёд. Defeating: random access, pointer chaining (следующий адрес data-dependent). Software prefetch (`__builtin_prefetch`) — manual escape hatch для graph traversals. TLB кеширует virtual-to-physical translations; miss = page walk = 5–50 циклов. Huge pages (2 MB): один TLB entry покрывает 512× больше, снижают TLB pressure кардинально. MLP (Memory-Level Parallelism): 8–16 outstanding miss-ов параллельно для независимых loads; dependent chains полностью serialize. Non-temporal stores: bypass cache для streaming (write-once) данных, +20–30% bandwidth. Cache associativity (8–16 way): conflict misses на stride patterns, соответствующих cache size. Теперь, когда встретишь traversal по графу или дереву, который тормозит даже при подтверждённом попадании данных в cache, — помни: prefetcher тут не поможет, фикс — реструктурировать порядок доступа или добавить software prefetch на несколько шагов вперёд.
</Recap>
