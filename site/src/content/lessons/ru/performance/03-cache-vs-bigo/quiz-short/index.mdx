---
concepts:
- memory-hierarchy
- false-sharing
- branch-prediction
- soa-vs-aos
- tlb-and-prefetcher
- perf-stat-loop
estMin: 14
lang: ru
level: senior
order: 8
prereqs: []
slug: quiz-short
sources:
- https://en.wikipedia.org/wiki/CPU_cache
- https://en.algorithmica.org/hpc/pipelining/branching/
- https://www.cs.cornell.edu/courses/cs3110/2012sp/lectures/lec25-locality/lec25.html
status: ready
summary: Промпты на свободное воспроизведение по всему юниту. Сначала ответь по памяти, затем раскрой модельный ответ и сравни.
title: 'Cache vs big-O: тест на свободное воспроизведение'
track: performance
unit: 03-cache-vs-bigo
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";

<Hook>
Воспроизведение по памяти бьёт перечитывание. На каждый промпт скажи или запиши полный ответ из памяти до того, как откроешь модельный — именно усилие припоминания чинит ментальную модель, а не узнавание правильно выглядящего варианта.
</Hook>

<Goal lang="ru">
Восстанови костяк юнита, не подглядывая: почему паттерн доступа бьёт число операций, как cache line порождает false sharing, когда сортировка бьёт branchless, почему SoA включает SIMD и какая petlя perf-stat связывает всё вместе.
</Goal>

<RetrievalDrawer
  client:visible
  id="03-cache-vs-bigo-short-retrieval"
  lang="ru"
  questions={[
    { q: "Почему O(N) сканирование contiguous массива может бить O(log N) обход дерева в production, и где это перестаёт быть верным?", a: "Big-O считает операции, но молчит о константе перед N, которую задаёт memory hierarchy: попадание в L1 — ~1 нс, промах в RAM — ~70–100 нс, разрыв 100x. Сканирование массива последовательно, поэтому hardware prefetcher грузит cache lines заранее и большинство чтений попадают в L1; сбалансированное дерево разбрасывает узлы по heap, поэтому каждый уровень — разыменование указателя в, скорее всего, холодную линию, стоящее полного round-trip в RAM. При ~17 промахах на lookup против плотно префетченного сканирования O(N)-массив может выигрывать при N вплоть до миллионов. Это перестаёт быть верным, когда массив больше не влезает в кэш И N достаточно велик, чтобы экономия log N перевесила константу на шаг — так что точка перелома зависит от нагрузки и размера. Правило: измеряй на реальном N на реальном железе до того, как доверять асимптотическому ранжированию." },
    { q: "Объясни false sharing: как протокол MESI превращает независимые записи на поток в баг производительности, и как его обнаружить и исправить.", a: "Cache lines — это 64-байтные единицы владения. По MESI, когда одно ядро пишет любой байт линии, оно берёт линию в Modified и помечает копии всех других ядер как Invalid; те должны перечитать перед следующим доступом. False sharing — когда два потока пишут в разные переменные, случайно делящие одну линию: логически независимо, но каждая запись инвалидирует линию другого потока, поэтому она пинг-понгует между ядрами по ~30–50 циклов за отскок. Lock-free код может оказаться медленнее версии с локом. Обнаружь через `perf c2c` (cache-line contention) или по сигнатуре: низкий IPC (0.3–0.6) плюс высокий cache-miss rate, ухудшающийся с ростом числа потоков — в отличие от lock contention, который проявляется как off-CPU wait. Фикс — дополнить каждое поле на поток до своей 64-байтной линии (Go: 56 байт паддинга после 8-байтного значения; C++ `alignas(64)`; Java `@Contended`; Rust `CachePadded`)." },
    { q: "Когда сортировка (O(N log N)) бьёт branchless-переписывание для data-dependent ветки, а когда branchless — лучший инструмент?", a: "Ветка 50/50 data-dependent мисспредиктит ~половину времени, каждый промах флашит pipeline на 10–30 циклов. Сортировка группирует исходы, поэтому предиктор видит длинную серию not-taken, затем длинную taken, достигая ~99% точности — 2–3x ускорение, окупающее сортировку при большом N или при переиспользовании данных через много проходов. Сортировка — правильный инструмент, когда можешь отсортировать один раз и сканировать/запрашивать много, или когда упорядоченность ценна сама по себе. Branchless (arithmetic select, CMOV) лучше, когда нельзя переупорядочить данные (они приходят в произвольном порядке, один проход), ветка действительно непредсказуема, и обе ветви дёшевы — branchless всегда вычисляет обе ветви, поэтому проигрывает при предсказуемой ветке (95%+) или дорогой ветви. Петля: читай `branch-misses / branches` в perf stat; выше ~5% на горячей ветке — выбирай сортировку или branchless по тому, можно ли переупорядочить данные." },
    { q: "Почему SoA включает SIMD там, где AoS вынуждает gather/scatter, и в чём размен?", a: "SIMD грузит векторный регистр из contiguous блока и оперирует 4–16 лейнами разом. В AoS (struct Point {x,y,z}; points[N]) значения x отстоят на 12 байт, поэтому одна загрузка возвращает смешанные поля — нужно gather, чтобы собрать только лейны x, ~10x медленнее contiguous load, и попутно ты тянешь y и z в кэш зря. В SoA (float xs[N], ys[N], zs[N]) значения x contiguous, поэтому одна SIMD-загрузка заполняет регистр 8 значениями x, а y/z вообще не трогаются, экономя две трети bandwidth для цикла только по x. Размен: AoS лучше, когда обращаешься ко всем полям одного элемента вместе (инкапсуляция, API на один объект); SoA лучше для bulk-операций по одному полю за раз (ML inference, game ECS, колоночные БД). Частый паттерн: держать AoS на поверхности API, конвертировать в SoA внутри для горячего цикла." },
    { q: "Помимо cache hit rate, какие два ограничения лимитируют memory-bound цикл, и как отличить их от cache miss?", a: "Memory bandwidth и NUMA-топология. (1) Bandwidth: цикл, стримящий десятки ГБ, упирается в потолок bandwidth RAM (~50–100 ГБ/с на DDR5) независимо от поведения кэша — больший L3 не даёт ускорения, потому что данные читаются раз и выбрасываются. Признак: высокий L3 miss rate И огромный объём данных; удвоение L3 не помогает. Фиксы: уменьшить данные (меньшие типы, проекция, сжатие), non-temporal stores для обхода кэша при write-once. (2) NUMA: на многосокетном сервере поток, обращающийся к памяти на удалённом сокете, платит ~120–150 нс против ~70 нс локально — штраф ~1.7x, который perf часто ошибочно показывает как L3 misses. Признак: высокий кажущийся miss rate на маленьком рабочем наборе (влезает в L3) на многосокетном железе; подтверди через numastat. Фикс: пин потоков и аллокация на локальном узле (numactl, taskset). Оба — отдельные оси от настоящего capacity/locality cache miss, который ты подтверждаешь, проверив объём данных и топологию сокетов." },
    { q: "Сформулируй петлю инженерии производительности для этого юнита и какая метрика указывает на какой класс фикса.", a: "Измерь → определи → подходящий фикс → перемеряй. Запусти `perf stat -e cache-misses,cache-references,branch-misses,branches,cycles,instructions`. Читай: IPC ниже ~1.0 означает, что CPU простаивает (память или ветки); LLC miss rate выше ~20% означает проблему раскладки/локальности (рабочий набор превышает L3, AoS или pointer-chasing) → фикс через SoA, struct splitting, contiguous arena или cache-oblivious tiling; branch-miss rate выше ~5% на горячей ветке означает непредсказуемую data-dependent ветку → фикс через сортировку или branchless; высокий LLC miss при огромном объёме данных означает bandwidth-bound → уменьши данные или non-temporal stores; здоровые miss rate, но низкий IPC из-за выдачи инструкций означает возможность SIMD → SoA + авто-векторизация. Затем перемеряй, чтобы подтвердить фикс и вскрыть следующее узкое место. Дисциплина: начинай с измерения, а не с выбора алгоритма — big-O это модель масштабируемости, профиль это модель производительности." },
  ]}
/>

<Recap lang="ru">
Если ты восстановил каждый ответ по памяти, ты держишь костяк юнита: memory hierarchy задаёт ~100x константу, которую big-O игнорирует; cache lines — это 64-байтные единицы владения, превращающие соседние записи в false sharing; сортировка и branchless — два инструмента для одной проблемы непредсказуемой ветки; SoA разблокирует SIMD и bandwidth, которые AoS блокирует; а петля perf-stat сопоставляет каждую метрику её фиксу. Сначала измеряй, назови причину в константном множителе, исправь раскладку или ветки, перемеряй.
</Recap>
