---
concepts:
- row-major
- column-major
- cache-oblivious-blocking
- spatial-locality
- access-pattern
deepensInto: []
estMin: 12
lang: ru
lessonType: topic
level: junior
order: 2
prereqs:
- performance/03-cache-vs-bigo/01-memory-hierarchy
slug: 02-row-major-vs-column-major
sources:
- https://en.wikipedia.org/wiki/CPU_cache
- https://www.cs.cornell.edu/courses/cs3110/2012sp/lectures/lec25-locality/lec25.html
- https://ivoroshilin.wordpress.com/2013/02/06/know-your-locality-of-reference-some-techniques-for-keeping-data-in-the-cpu-cache/
spiral:
- latency
status: ready
summary: Смена порядка двух циклов на одной матрице даёт разницу в 9x по wall-clock
  — потому что шаг от строки к столбцу превращает каждый доступ из cache hit в cache
  miss.
title: 'Row-major vs column-major: порядок доступа и разрыв в 9x'
track: performance
unit: 03-cache-vs-bigo
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import DiagramFrame from "~/components/diagram/DiagramFrame.astro";
import Infographic from "~/components/diagram/Infographic.astro";
import wallClockGap from "./infographics/wall-clock-gap.svg?raw";

<Hook>
Два цикла matrix-multiply. Тот же алгоритм. Та же матрица 10 000 × 10 000. Одинаковое число сложений и умножений. Один заканчивается за 300 мс. Другой — за 2 800 мс. Единственная разница: порядок двух `for`-циклов.
</Hook>

<Crux>Row-major шагает по contiguous памяти; column-major прыгает 80 000 байт. Тот же O(N²) — 9x разрыв по wall-clock: только из cache miss rate.</Crux>

<Explanation>

## Как 2D-массивы лежат в памяти

Прежде чем оптимизировать inner loop, стоит разобраться, как язык кладёт матрицу в память — именно это определяет, будет ли каждый шаг L1-хитом на 1 нс или походом в RAM на 100 нс.

В C, Java, Rust, Python (NumPy default) и большинстве других языков 2D-массив `arr[rows][cols]` хранится в **row-major** (построчном) порядке: все элементы строки 0 — contiguous, потом вся строка 1, и т.д.

Для матрицы 10 000 × 10 000 из 8-байтных doubles:

```
arr[0][0], arr[0][1], ..., arr[0][9999],   ← строка 0: байты 0–79999
arr[1][0], arr[1][1], ..., arr[1][9999],   ← строка 1: байты 80000–159999
...
```

## Что происходит внутри каждого порядка цикла

**Row-major итерация** (`for i: for j: arr[i][j]`):
- Обращается к `arr[i][0]`, `arr[i][1]`, `arr[i][2]`, ...
- Каждый шаг — 8 байт вперёд в памяти.
- Cache line 64 байта (8 doubles). Первый read грузит 8 элементов сразу; следующие 7 — бесплатные L1-хиты.
- Prefetcher детектирует sequential pattern и pre-грузит следующую line вперёд.
- Стоимость per element: ~1 нс.

**Column-major итерация** (`for j: for i: arr[i][j]`):
- Обращается к `arr[0][j]`, `arr[1][j]`, `arr[2][j]`, ...
- Каждый шаг — 10 000 × 8 = **80 000 байт** вперёд — далеко за пределы любой загруженной cache line.
- Каждый доступ — свежая cache line, не prefetch-нутая.
- Стоимость per element: ~70–100 нс (full RAM latency).

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Порядок цикла</th>
        <th class="border border-border px-3 py-2 text-left">Шаг в памяти</th>
        <th class="border border-border px-3 py-2 text-left">Поведение cache</th>
        <th class="border border-border px-3 py-2 text-left">Время (10k×10k doubles)</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2"><code>for i: for j: arr[i][j]</code></td>
        <td class="border border-border px-3 py-2">8 байт (sequential)</td>
        <td class="border border-border px-3 py-2">7 из 8 accesses — L1 hit</td>
        <td class="border border-border px-3 py-2">~300 мс</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2"><code>for j: for i: arr[i][j]</code></td>
        <td class="border border-border px-3 py-2">80 000 байт (random)</td>
        <td class="border border-border px-3 py-2">Почти каждый доступ — miss</td>
        <td class="border border-border px-3 py-2">~2 800 мс</td>
      </tr>
    </tbody>
  </table>
</div>

Instruction count идентичен. Арифметика идентична. Разница в 9x — полностью из-за cache-miss latency.

<Infographic
  svg={wallClockGap}
  label="Столбчатая диаграмма сравнивает wall-clock время на один проход по матрице 10 000 × 10 000 из doubles: row-major итерация — около 300 мс, column-major — около 2800 мс, разрыв примерно в 9x без изменения алгоритма."
  caption="Тот же O(N²), та же арифметика — высота столбца это чистая cache-miss latency. Шаг column-major в 80 000 байт превращает почти каждый доступ в поход в RAM."
  hue="var(--d-hardware)"
/>

## Исключение Fortran и языковые конвенции

Fortran хранит массивы в **column-major** порядке — все элементы столбца 0 сначала, потом столбец 1. NumPy имеет Fortran-order опцию (`order='F'`). Если твой код или библиотека использует Fortran layout, «очевидно правильный» цикл в C — worst case в Fortran. Всегда проверяй memory layout языка перед оптимизацией inner loops.

## Cache-oblivious blocking

Иногда оба порядка необходимы — например, в matrix transposition или некоторых linear algebra routines. Фикс — **cache-oblivious blocking** (тайлинг): делай матрицу маленькими тайлами (~64 строки × 64 столбца, чтобы каждый тайл влезал в L2), обрабатывай каждый тайл перед переходом к следующему. Внутри тайла доступы по порядку; весь тайл влезает в cache при обработке, и row, и column dimensions видят mostly cache hits.

<Inset kind="why" lang="ru">
Пример 9x использует матрицу 10 000 × 10 000, потому что это гарантирует, что working set намного превышает L3 cache. На маленьких матрицах (100 × 100) вся матрица влезает в L1, и оба порядка быстры. Performance cliff появляется только когда матрица вырастает за cache — это типичный production сценарий для реальных data processing workloads.
</Inset>

<Quiz
  id="02-quiz-1"
  lessonSlug="02-row-major-vs-column-major"
  lang="ru"
  question="Матрица 10 000 × 10 000 doubles хранится row-major. Inner loop итерирует по столбцам (arr[i][j], j от 0 до N). Почему это быстро?"
  choices={[
    { label: "Consecutive j values обращаются к consecutive addresses. Одна 64-байтная cache line держит 8 doubles. Prefetcher грузит следующую line вперёд. Большинство accesses — L1 hits на 1 нс.", correct: true },
    { label: "Столбцы меньше строк", misconception: "Для square matrix строки и столбцы одинаковой длины. Layout — не размер — определяет производительность." },
    { label: "CPU cores делят L3, который покрывает матрицу", misconception: "Для матрицы 10k×10k из doubles это 800 МБ — далеко за пределами любого L3. Locality, не размер L3, — причина." },
    { label: "Column iteration автоматически использует SIMD", misconception: "SIMD помогает, но требует contiguous data независимо. Преимущество row-major — contiguous access, что позволяет SIMD — но вопрос о cache, не SIMD." },
  ]}
/>

<Quiz
  id="02-quiz-2"
  lessonSlug="02-row-major-vs-column-major"
  lang="ru"
  question="Команда переключила matrix multiply с row-major на column-major порядок цикла и наблюдает замедление 9x без изменения алгоритма. Корневая причина:"
  choices={[
    { label: "Column-major итерация прыгает на 80 000 байт за шаг на 10k-column матрице. Каждый шаг — новая cache line. Каждый доступ платит full RAM latency (~100 нс). То же число loads, но каждый стоит в 100x больше.", correct: true },
    { label: "CPU выполняет меньше инструкций в секунду для column access", misconception: "Instruction count тот же; IPC падает, потому что CPU ждёт память, а не потому что меньше инструкций." },
    { label: "Изменилась big-O сложность алгоритма", misconception: "Оба порядка цикла имеют идентичный O(N²) complexity — алгоритм не изменился. Cost — в constant factor, не в asymptotic class." },
    { label: "Column-major требует другого compiler optimization flag", misconception: "Compiler flags помогают с vectorization, но fundamental проблема — memory access stride, которую никакой flag не исправит." },
  ]}
/>

<DragOrder
  id="02-drag-1"
  lessonSlug="02-row-major-vs-column-major"
  lang="ru"
  prompt="Проследи, что происходит, когда row-major код обращается к arr[i][0] через arr[i][7] (8 consecutive doubles, 64 байта total):"
  items={[
    { id: "a", label: "CPU запрашивает arr[i][0] (address X)" },
    { id: "b", label: "Cache miss: загружает 64-байтную line в X в L1 (стоит ~100 нс один раз)" },
    { id: "c", label: "arr[i][0] через arr[i][7] — все в загруженной line" },
    { id: "d", label: "Доступы к arr[i][1] через arr[i][7] каждый hit L1 (~1 цикл каждый)" },
    { id: "e", label: "Prefetcher срабатывает и грузит следующую 64-байтную line до того, как она нужна" },
    { id: "f", label: "arr[i][8] через arr[i][15] приходят с нулевым ожиданием" },
  ]}
  correctOrder={["a", "b", "c", "d", "e", "f"]}
/>

</Explanation>

<DiagramFrame
  label="Та же row-major сетка, два порядка обхода"
  hue="var(--d-hardware)"
  caption="Row-major идёт по одной cache line (8 байт/шаг); column-major прыгает на целую строку (80 000 байт/шаг) и миссит каждый раз."
>
  <svg viewBox="0 0 460 230" width="460" height="230" role="img" aria-label="Обход row-major против column-major на row-major сетке" style="display:block;margin:0 auto;max-width:100%;height:auto;font-family:var(--font-mono);">
    {/* Row-major: идём вдоль строки */}
    <text x="100" y="18" text-anchor="middle" font-size="12" font-family="var(--font-body)" fill="var(--ink)">Row-major: for i: for j</text>
    {[0,1,2,3].map((r) => [0,1,2,3].map((c) => (
      <rect x={26 + c*38} y={30 + r*38} width="34" height="34" rx="4"
        fill={r===0 ? "var(--accent)" : "var(--card-2)"} opacity={r===0 ? "0.35" : "1"}
        stroke="var(--hairline-strong)" stroke-width="0.8" />
    )))}
    <line x1="43" y1="47" x2="195" y2="47" stroke="var(--accent)" stroke-width="2" />
    <text x="100" y="200" text-anchor="middle" font-size="10.5" fill="var(--muted)">+8 Б/шаг — 7 из 8 в L1</text>

    {/* Column-major: идём вниз по столбцу */}
    <text x="358" y="18" text-anchor="middle" font-size="12" font-family="var(--font-body)" fill="var(--ink)">Column-major: for j: for i</text>
    {[0,1,2,3].map((r) => [0,1,2,3].map((c) => (
      <rect x={284 + c*38} y={30 + r*38} width="34" height="34" rx="4"
        fill={c===0 ? "var(--d-hardware, var(--accent))" : "var(--card-2)"} opacity={c===0 ? "0.35" : "1"}
        stroke="var(--hairline-strong)" stroke-width="0.8" />
    )))}
    <line x1="301" y1="47" x2="301" y2="199" stroke="var(--ink)" stroke-width="2" />
    <text x="358" y="218" text-anchor="middle" font-size="10.5" fill="var(--muted)">+80 000 Б/шаг — мисс каждый шаг</text>
  </svg>
</DiagramFrame>

<KeyTakeaway>Row-major итерация на row-major матрице шагает по 8 байт — 7 из 8 reads — L1 hits. Column-major итерация шагает по 80 000 байт — каждый read миссит cache. Тот же O(N²), разница 9x по wall-clock. Знай memory layout своего языка и ориентируй циклы под него.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="02-retrieval"
  lang="ru"
  questions={[
    { q: "Почему row-major matrix iteration в 9 раз быстрее column-major на матрице 10 000×10 000, когда обе выполняют то же число element accesses?", a: "Из-за cache line behaviour. Матрица хранится row by row в памяти: вся строка 0, потом вся строка 1, и т.д. Cache line — 64 байта (8 doubles). Row-major итерация трогает arr[i][0], arr[i][1], ..., arr[i][N-1] consecutively — каждые 8 reads — один cache line transfer; prefetcher грузит следующую line заранее, hits доминируют. Column-major итерация трогает arr[0][j], arr[1][j], ..., arr[N-1][j] — каждый access прыгает N×8 = 80 000 байт, в свежую cache line, которая не загружена. Каждый read — miss; каждый стоит 30–100 циклов. Total число arithmetic ops идентично; wall-clock time различается в 9x, потому что column-major платит full RAM-latency tax по сути на каждой load. Фикс: change loop order, или cache-oblivious tiling, чтобы каждый тайл влезал в L2." },
    { q: "Что такое cache-oblivious blocking и когда его использовать?", a: "Cache-oblivious blocking (tiling) делит матрицу на маленькие тайлы — обычно sized так, чтобы каждый тайл влезал в L2 cache (~256 KB до 1 MB) — и полностью обрабатывает каждый тайл перед переходом к следующему. Это означает, что внутри тайла все accesses видят mostly L2 hits независимо от того, итерируешь ли по строкам или столбцам. Алгоритм не нуждается в знании конкретных cache sizes; естественная рекурсия обеспечивает, что на каком-то уровне рекурсии sub-problem влезает в какой-то cache level. Используй, когда алгоритм нуждается в доступе к матрице по двум измерениям (matrix transposition, matrix multiplication, FFT), особенно когда полная матрица превышает L3 cache." },
  ]}
/>

<Recap lang="ru">
В row-major языке (C, Java, Rust, Python/NumPy) элементы одной строки занимают последовательные адреса. 64-байтная cache line держит 8 doubles; sequential (row-major) итерация читает 8 элементов per cache miss. Column-major итерация на том же массиве шагает на 80 000 байт per element на 10 000-column матрице, попадая в свежую cache line каждый раз и платя full RAM latency на каждой load. Результат — разница 9x по wall-clock без изменения алгоритма. Всегда знай memory layout языка и выравнивай порядок циклов под него; используй cache-oblivious blocking, когда оба направления доступа неизбежны. Теперь, когда встретишь подозрительно медленную функцию обработки матриц, первым делом проверь порядок циклов — это правка в одну строку и одна точка измерения.
</Recap>
