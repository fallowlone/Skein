---
concepts:
- branch-prediction
- pipeline-flush
- branchless-code
- cmov
- pgo
- tage-predictor
deepensInto: []
estMin: 13
lang: ru
lessonType: topic
level: middle
order: 4
prereqs:
- performance/03-cache-vs-bigo/01-memory-hierarchy
slug: 04-branch-prediction
sources:
- https://en.algorithmica.org/hpc/pipelining/branching/
- https://blog.cloudflare.com/branch-predictor/
- https://danluu.com/branch-prediction/
- https://en.wikipedia.org/wiki/CPU_cache
spiral:
- latency
status: ready
summary: Modern branch predictors угадывают правильно в 95–99% случаев. Один неправильный
  guess на случайных данных — flush pipeline и 10–30 циклов простоя. Sorted-array
  trick, branchless код и PGO — три инструмента фикса.
title: 'Branch prediction: 10–30 циклов штрафа за неожиданный if'
track: performance
unit: 03-cache-vs-bigo
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import NumbersCard from "~/components/layout/NumbersCard.astro";
import Misconception from "~/components/layout/Misconception.astro";

<Hook>
Тот же цикл. Те же данные. Одно сортирование массива перед циклом — и код становится в 2–3 раза быстрее. Алгоритм не изменился. Изменился только порядок данных — и CPU больше не ошибается в предсказаниях.
</Hook>

<Crux>Каждый if — branch, который CPU предсказывает заранее. Предсказуемые данные 95%+ бесплатны. 50/50 flush-ат pipeline: 10–30 циклов per miss.</Crux>

<Explanation>

## Как branch prediction работает

Modern CPU pipeline глубокий: 15–20+ стадий. CPU fetch-ит инструкции задолго до их исполнения. Когда встречается branch (`if`, `for`, `while`, virtual call), CPU не знает заранее, какой путь будет taken — но он должен что-то поставить в pipeline.

**Branch predictor** угадывает: taken или not-taken. CPU начинает исполнять предсказанный путь спекулятивно. Если угадал правильно — win, pipeline не останавливался. Если ошибся — **pipeline flush**: все спекулятивно исполненные инструкции выбрасываются, нужно начать с правильного пути. Цена: глубина pipeline × missed slots.

На современном x86 с pipeline depth ~19 стадий: **10–30 wasted cycles per misprediction**.

## TAGE predictor

Modern branch predictors умные. Intel и AMD используют **TAGE (TAgged GEometric)** predictor, комбинирующий несколько history length. Он запоминает не просто last-taken/not-taken, а complex patterns up to N branches назад.

Типичная accuracy: **95–99% на реальном коде**.

Hard cases для любого predictor:
- Data-dependent branches (if на random input value).
- Virtual function calls с высокой polymorphism (непредсказуемый target).
- Очень длинные pipelines (Apple M1/M2/M3: 10+ стадий) — ошибка дороже.

## Sorted-array trick

**Проблема:** tight loop суммирует элементы array, превышающие threshold.

```cpp
int sum = 0;
for (int i = 0; i < N; i++) {
    if (arr[i] > threshold) sum += arr[i];  // branch 50/50 на random data
}
```

На random unsorted data: ~50% miss rate → ~15 wasted cycles per iteration.

**Фикс: сортировка перед циклом.**

```cpp
std::sort(arr, arr + N);  // O(N log N) один раз
for (int i = 0; i < N; i++) {
    if (arr[i] > threshold) sum += arr[i];
}
```

Теперь первая половина цикла всегда not-taken (`arr[i] <= threshold`), вторая всегда taken. Predictor учит pattern быстро: один transition point, 99%+ accuracy после него.

Несмотря на O(N log N) sort cost, общее время часто меньше — потому что inner loop перестаёт платить mispredict penalty на каждой итерации.

## Branchless код

Когда branch непредсказуем и нельзя re-sort данные — убирай branch совсем.

```cpp
// branched (mispredict-friendly если arr[i] > 0 random):
if (arr[i] > 0) y = a; else y = b;

// branchless:
y = (arr[i] > 0) * a + (arr[i] <= 0) * b;
```

Или CMOV (conditional move на x86) — компилятор часто emit-ит автоматически при `-O2`:
```cpp
y = arr[i] > 0 ? a : b;  // компилятор может генерировать cmov
```

**Tradeoff:**
- Branchless версия **всегда** вычисляет обе стороны.
- Branched версия пропускает одну сторону но платит mispredict cost когда ошибается.

**Tipping point:** если branch предсказуем 95%+, branched быстрее. Если 50/50 (random), branchless выигрывает в **2–5x**.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Предсказуемость branch</th>
        <th class="border border-border px-3 py-2 text-left">Рекомендация</th>
        <th class="border border-border px-3 py-2 text-left">Почему</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">95%+ предсказуем</td>
        <td class="border border-border px-3 py-2">Оставь branched</td>
        <td class="border border-border px-3 py-2">Predictor угадывает правильно, branch почти бесплатен</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">~80% предсказуем</td>
        <td class="border border-border px-3 py-2">Рассмотри branchless</td>
        <td class="border border-border px-3 py-2">20% miss rate × 15 циклов = значительно</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">~50/50 (random)</td>
        <td class="border border-border px-3 py-2">Branchless / re-sort</td>
        <td class="border border-border px-3 py-2">50% miss rate + flush pipeline каждые 2 итерации</td>
      </tr>
    </tbody>
  </table>
</div>

## PGO для branches

**Profile-guided optimisation (PGO):** build с instrumentation, run representative workload, recompile с profile данными.

PGO для branches:
- Predicted-taken branches размещаются **последовательно** в памяти — hot path contiguous в instruction cache.
- Basic blocks переупорядочиваются, держа hot path вместе.
- Компилятор инлайнит callees, которые hot, и не инлайнит cold.

Real speedup: **10–30%** на большинстве production workloads. Компилятор делает автоматически то, что разработчик иначе делает через `__builtin_expect` и ручное аннотирование.

<NumbersCard
  id="04-numbers"
  title="Branch prediction числа"
  stats={[
    { label: "Pipeline depth (x86)", value: "~19 стадий" },
    { label: "Pipeline depth (Apple M1/M2)", value: "10+ стадий" },
    { label: "Misprediction penalty", value: "10–30 циклов" },
    { label: "Zen 2 pipeline depth", value: "19 стадий" },
    { label: "TAGE predictor accuracy", value: "95–99% real code" },
    { label: "50/50 miss rate speedup от branchless", value: "2–5x" },
    { label: "PGO speedup", value: "10–30%" },
  ]}
/>

<Inset kind="why" lang="ru">
Compiler annotations (`__builtin_expect`, `[[likely]]`, `[[unlikely]]`) — лёгкий способ подсказать predictor без PGO. Но они на best-guess разработчика; PGO опирается на реальный runtime data. Для production-critical inner loops PGO точнее. Для startup code или rarely-executed paths — compiler hints достаточно.
</Inset>

<Misconception id="04-mc-predict">
  <p><strong>«Branch prediction — мелочь; оптимизируй после профайлинга.»</strong> Для большинства кода верно. Но для hot loops с random data branch miss может составлять 5–25% циклов — это dominant bottleneck. perf stat branch-misses покажет это.</p>
</Misconception>

<Quiz
  id="04-quiz-1"
  lessonSlug="04-branch-prediction"
  lang="ru"
  question="Loop имеет if-branch, идущий true 50% времени, непредсказуемо. Какое типичное perf-влияние?"
  choices={[
    { label: "10–30 циклов wasted per iteration из-за pipeline flush от misprediction (около 50% iterations миссят)", correct: true },
    { label: "Незначительное", misconception: "50/50 непредсказуемые branches — worst case для predictor; cost реальный и доминирующий." },
    { label: "Компилятор убирает branch", misconception: "Компилятор не может убрать data-dependent branch без hint или PGO." },
    { label: "Быстрее предсказуемых branches", misconception: "Непредсказуемые branches универсально медленнее; предсказуемые почти бесплатны." },
  ]}
/>

<Quiz
  id="04-quiz-2"
  lessonSlug="04-branch-prediction"
  lang="ru"
  question="Команда использует sorted-array trick на inner loop с if-branch на random data. Почему это помогает, несмотря на O(N log N) sort cost?"
  choices={[
    { label: "Сортировка превращает 50/50 непредсказуемый branch в предсказуемый pattern (всё not-taken, потом transition, потом всё taken). Predictor угадывает 99%+. Один sort amortizes across всего N iterations.", correct: true },
    { label: "Sorted data позволяет early exit", misconception: "Early exit возможен, но не причина. Причина — predictor accuracy." },
    { label: "Компилятор оптимизирует sorted loops лучше", misconception: "Компилятор не знает, что данные sorted; причина — hardware branch predictor accuracy." },
    { label: "Sorted arrays меньше по памяти", misconception: "Размер не меняется; меняется predictor accuracy." },
  ]}
/>

<DragOrder
  id="04-drag-1"
  lessonSlug="04-branch-prediction"
  lang="ru"
  prompt="Поставь события pipeline flush при misprediction:"
  items={[
    { id: "a", label: "CPU fetch-ит branch инструкцию в pipeline" },
    { id: "b", label: "Branch predictor угадывает: 'not-taken'" },
    { id: "c", label: "CPU спекулятивно fetch-ит и начинает execute 'not-taken' путь" },
    { id: "d", label: "Execution достигает branch: реальный результат 'taken'" },
    { id: "e", label: "Misprediction detected: все спекулятивные инструкции выбрасываются (pipeline flush)" },
    { id: "f", label: "CPU начинает заново с 'taken' пути — 10–30 циклов потеряно" },
  ]}
  correctOrder={["a", "b", "c", "d", "e", "f"]}
/>

</Explanation>

<KeyTakeaway>Branch predictor угадывает 95–99% на реальном коде. На random data (50/50) — flush pipeline каждые 2 итерации, 10–30 циклов потеряно per miss. Три фикса: (1) re-sort данные перед inner loop — predictor выучит pattern; (2) branchless код (CMOV) — убери branch полностью; (3) PGO — компилятор bake-ит реальные runtime frequencies в code layout.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="04-retrieval"
  lang="ru"
  questions={[
    { q: "Тот же loop с branch на random data — 50% miss rate. Как sorted-array trick фиксит без изменения алгоритма?", a: "Sorted array превращает 50/50 random distribution в monotone pattern. Для loop, проверяющего arr[i] > threshold: на unsorted data — true и false чередуются случайно, predictor угадывает ~50% (случайно). После сортировки — сначала все элементы ниже threshold (not-taken), один transition, потом все выше (taken). Predictor выучит этот pattern за несколько итераций и угадывает 99%+ после. Несмотря на O(N log N) sort cost, total time меньше, потому что inner loop перестаёт платить 10–30 циклов per miss на каждой итерации." },
    { q: "Когда branchless код быстрее, чем branched с данными-зависимым if?", a: "Branchless быстрее когда branch miss rate высокий — typically 50/50 random data. Tipping point около 20%+ miss rate. Branchless версия вычисляет обе стороны всегда (no skip), но избегает pipeline flush. Для miss rate 50%: branched платит 15 циклов × 50% = 7.5 циклов per iter average overhead; branchless платит cost двух вычислений (usually 1–3 cycles). Branchless выигрывает 2–5x. Если branch предсказуем (95%+): branched быстрее, потому что skipает одну сторону вычисления и почти никогда не flush-ит." },
  ]}
/>

<Recap lang="ru">
Modern CPU pipeline спекулятивно fetch-ит инструкции по предсказанному пути branch. При misprediction — pipeline flush, 10–30 циклов wasted. TAGE predictor достигает 95–99% accuracy на предсказуемых данных. На random data (sort, filter, packet classification) — 50% miss rate, dominant bottleneck. Три fix-паттерна: (1) sorted-array trick — превращает random в monotone pattern, predictor выучивает за несколько итераций; (2) branchless код — CMOV или arithmetic select, всегда вычисляет обе стороны, но избегает flush; (3) PGO — bake-ит runtime frequencies в code layout, hot branches sequential в memory. Диагностика через `branch-misses` counter в perf stat.
</Recap>
