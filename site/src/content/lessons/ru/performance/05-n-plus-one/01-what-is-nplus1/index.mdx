---
concepts:
- nplus1
- round-trip
- lazy-loading
- orm
- batch-loading
deepensInto: []
estMin: 10
lang: ru
lessonType: topic
level: junior
order: 1
prereqs:
- performance/02-hot-paths/01-what-makes-a-hot-path
slug: 01-what-is-nplus1
sources:
- https://www.prisma.io/docs/orm/prisma-client/queries/relation-queries
- https://guides.rubyonrails.org/active_record_querying.html
- https://docs.djangoproject.com/en/5.0/ref/models/querysets/
spiral:
- latency
status: ready
summary: Один экран рендерится в 50 запросов к БД, потому что ORM загружает каждую
  связанную строку по требованию. Фикс — не быстрее запрос, а меньше запросов.
title: 'N+1: одна логическая операция, много round-trip''''''''ов'
track: performance
unit: 05-n-plus-one
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import MetaphorComplete from "~/components/pedagogy/MetaphorComplete.astro";
import SequenceDiagram from "~/components/diagram/SequenceDiagram.astro";
import Infographic from "~/components/diagram/Infographic.astro";
import queryExplosion from "./infographics/query-explosion.svg?raw";

<Hook>
Страница медленная — 800 мс. CPU flame graph в порядке, широких функций нет. Свен открывает DB query log и считает 51 запрос на один page load. Профайлер не врал; bottleneck не в одной функции. Он распределён по 51 маленькому round-trip'у. К концу урока ты будешь точно знать, как это происходит и какое одно слово это исправляет.
</Hook>

<Crux>Один trip = быстро. N+1 trip'ов = медленно. Каждый round-trip платит RTT; один запрос на 100 строк бьёт 100 запросов по одной.</Crux>

<Explanation>

## Что такое N+1

Пользователь просит «покажи список заказов с именами клиентов». ORM выполняет один запрос на orders, потом по одному на каждый order для fetch клиента — 1 + N запросов. На 50 заказах — 51 DB round-trip на один рендер.

Каждый round-trip платит сетевой latency (1–5 мс intra-DC, 10–50 мс cross-region) плюс query-overhead. Страница, которая должна делаться за 20 мс, делается за 500 мс — и инженер не найдёт проблему в CPU-профиле, потому что цена распределена по многим быстрым вызовам. Ни одна функция не медленная.

## Почему ORM производит N+1 по умолчанию

Object-relational mapper'ы выставляют модель, где каждый объект имеет relationships, доступные как обычные свойства. `order.customer` читается как обычный атрибут; под капотом ORM триггерит запрос, если customer ещё не загружен. Это называется **lazy loading** — удобно, не нужно объявлять заранее, что нужно, — но это источник N+1.

Цикл по 50 заказам, чтение `order.customer.name` на каждом — ORM стреляет 50 запросами без того, чтобы ты написал 50 запросов.

## Метафора кладовой

Готовишь пасту, нужны чеснок, масло, соль и базилик:

- **Вариант A (batch):** пойти в кладовую один раз, взять все четыре, вернуться. Один trip.
- **Вариант B (N+1):** пойти за чесноком, вернуться; пойти за маслом, вернуться; пойти за солью, вернуться; пойти за базиликом, вернуться. Четыре trip'а.

Та же работа — четыре trip'а против одного. Паттерн N+1 — вариант B. Фикс — вариант A: сократить прогулки.

Единица цены — round-trip: время от момента, когда код просит данные, до момента, когда данные в руках. RTT, query parse, planner overhead, lock acquisition, result encoding — всё платится за каждый round-trip. Один запрос на 100 строк платит раз; 100 запросов платят 100 раз.

## Конкретный пример

```
// Код ORM, который выглядит безобидно:
const orders = await Order.findAll({ where: { userId } });
for (const order of orders) {
  const customer = await order.getCustomer(); // один запрос на каждый order
  render(order, customer);
}

// Лог запросов:
// SELECT * FROM orders WHERE user_id = 42 LIMIT 50      — 1 запрос
// SELECT * FROM customers WHERE id = 1                   — 1 запрос
// SELECT * FROM customers WHERE id = 2                   — 1 запрос
// ...
// SELECT * FROM customers WHERE id = 50                  — 1 запрос
// Итого: 51 запрос × ~15 мс RTT = 765 мс
```

Фикс в Prisma и большинстве ORM — одно слово, `include`:

```
const orders = await prisma.order.findMany({
  where: { userId },
  include: { customer: true },
});
// Теперь: 2 запроса — один на orders, один на всех customers через IN (...)
// Итого: ~20 мс
```

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Подход</th>
        <th class="border border-border px-3 py-2 text-left">Запросы</th>
        <th class="border border-border px-3 py-2 text-left">Время (intra-DC, 15 мс RTT)</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">Lazy (N+1)</td>
        <td class="border border-border px-3 py-2">51</td>
        <td class="border border-border px-3 py-2">~765 мс</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Eager / include</td>
        <td class="border border-border px-3 py-2">2</td>
        <td class="border border-border px-3 py-2">~30 мс</td>
      </tr>
    </tbody>
  </table>
</div>

## Вложенный N+1: проблема множится

Дашборд рендерит команды → проекты → участников. Наивный ORM:

- 1 запрос на команды
- N запросов на проекты (один на команду)
- N×M запросов на участников (один на проект)

С 10 командами × 5 проектов × 8 участников: 1 + 10 + 50 = 61 запрос. Фикс nested eager loading: 3 запроса. Рендер падает с 1,5 с до 80 мс.

<Infographic
  svg={queryExplosion}
  label="Столбчатая диаграмма запросов к БД на рендер страницы. Простая страница падает с 51 lazy-запроса до 2 с eager loading; вложенный дашборд — с 61 до 3, столбцы eager схлопываются почти в ноль."
  caption="Тот же рендер, lazy против eager: фикс — не быстрее запрос, а их радикально меньше. И простой случай (51→2), и вложенный (61→3) схлопываются в один-два батч-round-trip'а."
  hue="var(--d-infra)"
/>

<Inset kind="why" lang="ru">
Имя N+1 отражает worst case: 1 запрос на parent-коллекцию плюс 1 на parent = 1 + N. Термин вошёл в мейнстрим около 2003–2005 с подъёмом ORM вроде Active Record (Rails 2004) и Hibernate (Java). Форма повторяется в каждом новом data-access framework'е: Prisma добавил `include` в 2020, каждый современный ORM поставляет его.
</Inset>

<Quiz
  id="01-what-is-nplus1-quiz-1"
  lessonSlug="01-what-is-nplus1"
  lang="ru"
  question="Страница медленная, но CPU-профиль не показывает широких функций. DB query log показывает сотни маленьких запросов. Самая вероятная причина?"
  choices={[
    { label: "Паттерн N+1 — одна логическая операция делает много маленьких round-trip'ов. Цена в round-trip'ах, не в compute, поэтому CPU-профиль её не показывает.", correct: true },
    { label: "БД медленная", misconception: "Каждый отдельный запрос быстрый; проблема в КОЛИЧЕСТВЕ запросов, не в скорости." },
    { label: "Memory leak", misconception: "Memory leak'и растят RSS, а не генерят сотни быстрых запросов на запрос." },
    { label: "Профайлер сломан", misconception: "Профайлер корректно показывает отсутствие CPU hotspot. Bottleneck — round-trip latency, которую CPU-профиль не меряет." },
  ]}
/>

<Quiz
  id="01-what-is-nplus1-quiz-2"
  lessonSlug="01-what-is-nplus1"
  lang="ru"
  question="Почему получение 100 строк ОДНИМ запросом бьёт получение 100 строк 100 отдельными запросами?"
  choices={[
    { label: "У каждого round-trip фиксированная цена: сетевой RTT плюс query parse плюс planner overhead. Один запрос платит её раз; 100 запросов — 100 раз, даже если каждый сам по себе быстрый.", correct: true },
    { label: "Один запрос использует меньше памяти", misconception: "Память — не первичный фактор. Число round-trip'ов — первичный." },
    { label: "БД не поддерживает 100 запросов", misconception: "БД обрабатывают много запросов. Проблема — per-round-trip overhead, умноженный на количество." },
    { label: "ORM запрещают несколько запросов", misconception: "ORM разрешают N запросов — это и есть N+1." },
  ]}
/>

<DragOrder
  id="01-what-is-nplus1-drag-1"
  lessonSlug="01-what-is-nplus1"
  lang="ru"
  prompt="Расставь четыре частых паттерна фикса N+1 от простейшего к самому гибкому:"
  items={[
    { id: "a", label: "JOIN — забрать parent и child одним запросом через SQL join" },
    { id: "b", label: "IN (...) — первый запрос даёт parent ID, второй фетчит всех children через WHERE child_id IN (parents)" },
    { id: "c", label: "Eager loading / preload — директива ORM, превращающая lazy-доступ в один batch-запрос" },
    { id: "d", label: "Batch loader (DataLoader) — собрать все child ID по всему запросу, выстрелить один батч, когда event loop yield'ит" },
  ]}
  correctOrder={["a", "b", "c", "d"]}
/>

<MetaphorComplete
  id="01-what-is-nplus1-metaphor-1"
  pieceSlug="01-what-is-nplus1"
  lang="ru"
  setup="Вставь пропуск: паттерн N+1 — это N+1 _______ — каждый сам по себе быстрый, но их сумма и делает страницу медленной."
  accepted={["round-trip", "round-trip'ов", "поход", "вызов", "запрос", "поездка", "trip"]}
  canonical="round-trip (каждый платит сетевую latency + query parse + planner overhead)"
  explanation="Единица цены здесь — round-trip: время от того, как код просит данные, до того, как данные в руках. RTT, query parse, planner, lock acquisition, result encoding — всё платится за каждый запрос. Один запрос на 100 строк платит раз; 100 запросов — 100 раз. Оптимизация — уменьшить trips, не делать отдельные trip'ы быстрее."
/>

</Explanation>

<SequenceDiagram
  label="N+1: один запрос списка, затем по запросу на строку"
  caption="50 заказов → 1 запрос на список + 50 lazy-запросов клиентов = 1 + N = 51 round-trip, каждый платит RTT."
  hue="var(--d-hardware)"
  actors={["App", "DB"]}
  messages={[
    { from: "App", to: "DB", label: "SELECT orders → 50 строк" },
    { from: "App", to: "DB", label: "SELECT customer id=1" },
    { from: "App", to: "DB", label: "SELECT customer id=2" },
    { from: "App", to: "DB", label: "... (ещё 47)" },
    { from: "App", to: "DB", label: "SELECT customer id=50" },
  ]}
/>

<KeyTakeaway>N+1 возникает, когда одна логическая операция делает много маленьких round-trip'ов вместо одного батча. Lazy loading в любом ORM вызывает это по умолчанию. Фикс всегда структурный: объяви, что нужно заранее, чтобы ORM сфетчил за один-два запроса.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="01-what-is-nplus1-retrieval"
  lang="ru"
  questions={[
    { q: "В одном абзаце: объясни, почему N+1 трудно найти CPU-профилем и куда смотреть вместо него.", a: "CPU-профиль меряет, где программа тратит compute. N+1 не жжёт CPU нигде — каждый запрос диспатчится быстро, программа ждёт БД off-CPU, ответ приходит быстро, следующий запрос идёт. Wait — off-CPU; CPU-профиль видит быстрые блики и ничего широкого. Чтобы найти N+1, смотри: DB query log (Postgres log_min_duration_statement, MySQL slow query log, ORM-логгер), application request-trace (Tempo, Jaeger, Datadog APM с полным водопадом запросов) или wall-clock / off-CPU профиль, который ловит wait-время. Сигнатура однозначна: десятки-сотни похожих запросов на request, часто один и тот же SELECT, отличающийся только WHERE id = ? параметром." },
    { q: "Что такое lazy loading и почему он производит N+1 по умолчанию?", a: "Lazy loading — поведение ORM, при котором связанный объект фетчится из БД при первом обращении к его свойству. Удобно — не нужно объявлять relations заранее. Но когда цикл по коллекции обращается к связанному свойству на каждом элементе, ORM стреляет одним запросом на элемент — N запросов на N элементов плюс исходный запрос на коллекцию. Это 1 + N запросов: паттерн N+1. Фикс — eager loading: скажи ORM заранее, какие relations нужны, и он сфетчит их за один-два запроса вместо N+1." },
  ]}
/>

<Recap lang="ru">
Проблема N+1 превращает одну логическую операцию в 1 + N DB round-trip'ов, загружая каждую связанную строку лениво. На 50 элементах — 51 запрос; при 15 мс RTT каждый — 765 мс вместо 30 мс. Корень — lazy loading, дефолт ORM, триггерящий запрос при каждом обращении к связанному свойству. Фикс структурный: использовать eager-load директиву (Rails `.includes`, Django `.select_related`, SQLAlchemy `selectinload`, Prisma `include`), чтобы сказать ORM, что нужно заранее. Теперь, когда встретишь медленную страницу с плоским CPU-профилем, открой query log первым делом — если насчитаешь больше 5–10 похожих SELECT'ов на запрос, N+1 найден.
</Recap>
