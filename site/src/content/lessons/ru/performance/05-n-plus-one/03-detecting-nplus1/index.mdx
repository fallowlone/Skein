---
concepts:
- query-log
- apm-tracing
- off-cpu-profiling
- ci-gate
- strict-loading
deepensInto: []
estMin: 13
lang: ru
lessonType: topic
level: middle
order: 3
prereqs:
- performance/05-n-plus-one/02-fix-families
slug: 03-detecting-nplus1
sources:
- https://docs.djangoproject.com/en/5.0/ref/models/querysets/
- https://guides.rubyonrails.org/active_record_querying.html
- https://hibernate.org/orm/documentation/
spiral:
- latency
status: ready
summary: N+1 скрывается от CPU-профилировщиков, поскольку стоимость — это off-CPU
  wait time, а не вычисления. Query logs, APM waterfalls и счётчики запросов на запрос
  раскрывают его — а CI gate предотвращает регрессии.
title: 'Обнаружение N+1: query logs, APM traces и CI gates'
track: performance
unit: 05-n-plus-one
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";

<Hook>
Страница занимает 1.2 секунды. CPU flame graph — плоский. Коллега настаивает, что профилировщик, наверное, ошибается. Профилировщик не ошибается — вы смотрите не в тот инструмент. N+1 живёт в off-CPU wait time между запросами, а не в какой-то функции.
</Hook>

<Crux>N+1 невидим для CPU-профилировщиков. Правильные инструменты — query logs, APM waterfall traces и счётчики запросов на запрос.</Crux>

<Explanation>

## Почему CPU-профили пропускают N+1

CPU-профилировщик сэмплирует стек-трейсы работающих потоков. Проблема N+1 не нагружает CPU в каком-то одном месте. Каждый запрос отправляется за микросекунды; программа затем ждёт базу данных (off-CPU); ответ приходит быстро; отправляется следующий запрос. Ожидание — это узкое место, но оно происходит off-CPU.

Профилировщик видит быстрые всплески без ничего широкого. Совокупная стоимость распределена по многим быстрым точкам вызова — не сконцентрирована в одной медленной функции. CPU flame graph будет выглядеть здоровым, пока страница занимает 1.2 секунды.

## Правильные инструменты

### 1. Database query log

Наиболее прямой инструмент. Каждая крупная СУБД может логировать каждый запрос с его длительностью и меткой времени.

```
# Postgres — логировать всё в разработке
log_min_duration_statement = 0

# MySQL slow query log
slow_query_log = 1
long_query_time = 0

# ORM-level logging
# Rails: config.log_level = :debug (ActiveRecord logger выводит каждый запрос)
# Django: LOGGING = { 'loggers': { 'django.db.backends': { 'level': 'DEBUG' } } }
# SQLAlchemy: echo=True
```

Сигнатура N+1 безошибочна: от десятков до сотен похожих запросов для одного запроса, часто один и тот же SELECT, отличающийся только параметром `id = ?` в WHERE.

### 2. APM tracing — waterfall view

Инструменты мониторинга производительности приложений (Datadog APM, Grafana Tempo, New Relic, Honeycomb с автоинструментированием) захватывают database span'ы в одном ряду с span'ами выполнения кода.

N+1 waterfall выглядит как высокая колонка коротких параллельных полосок — 50 или 100 коротких database span'ов в одном trace запроса. Один взгляд на trace view делает паттерн очевидным так, как логи сами по себе не делают.

Предупреждайте, когда количество span'ов p95 на endpoint растёт более чем на 30% за неделю.

### 3. Счётчик запросов на запрос

Пользовательский middleware, считающий запросы на request и логирующий или предупреждающий при превышении:

```ruby
# Rails — middleware считает запросы на request
ActiveSupport::Notifications.subscribe('sql.active_record') do
  RequestStore[:query_count] ||= 0
  RequestStore[:query_count] += 1
end

after_action do
  if RequestStore[:query_count] > 20
    Rails.logger.warn "QUERY_COUNT #{params[:controller]}##{params[:action]}: #{RequestStore[:query_count]}"
  end
end
```

Публикуйте как Prometheus histogram по endpoint. Предупреждайте, когда p99 endpoint превышает 20 или растёт более чем на 50% за неделю.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Инструмент</th>
        <th class="border border-border px-3 py-2 text-left">Лучше для</th>
        <th class="border border-border px-3 py-2 text-left">Сигнатура</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">CPU профилировщик</td>
        <td class="border border-border px-3 py-2">Compute hotspots</td>
        <td class="border border-border px-3 py-2">Неправильный инструмент — пропускает N+1</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Query log</td>
        <td class="border border-border px-3 py-2">Подтвердить N+1, найти место вызова</td>
        <td class="border border-border px-3 py-2">Много похожих SELECT'ов на запрос</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">APM trace</td>
        <td class="border border-border px-3 py-2">Визуальный waterfall, продакшн</td>
        <td class="border border-border px-3 py-2">Высокая колонка коротких DB span'ов</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Счётчик запросов</td>
        <td class="border border-border px-3 py-2">Алертинг, тренды по endpoint'у</td>
        <td class="border border-border px-3 py-2">queries_per_request p99 &gt; 20</td>
      </tr>
    </tbody>
  </table>
</div>

## CI gate — поймать на этапе PR

Наиболее эффективный механизм против N+1 — поймать регрессию на этапе PR, а не в продакшне. Команды, использующие этот gate, сообщают о снижении инцидентов N+1 в продакшне на 70–90%. Первоначальная настройка: 1–2 недели. Текущее обслуживание: минимальное — gate запускается в CI.

**Rails:** гем `bullet` вызывает исключение при любом lazy-loaded relation, обращение к которому происходит при рендеринге запроса. Тест контроллера, вызывающий N+1, не проходит.

```ruby
# spec/spec_helper.rb
Bullet.enable = true
Bullet.raise = true  # превращает N+1 в провал теста
```

**Django:** `assertNumQueries(N)` в тестовом фреймворке считает точные запросы на view:

```python
def test_orders_page(self):
    with self.assertNumQueries(2):  # проваливается если N+1 проскочил
        response = self.client.get('/orders/')
```

**SQLAlchemy / Hibernate:** включить статистику количества запросов и проверять на тест:

```python
# SQLAlchemy — проверить количество запросов через events
# Hibernate — проверить через Statistics.getPrepareStatementCount()
```

Паттерн во всех них: PR запускает тесты, тесты выполняют запросы, счётчик запросов превышает порог по endpoint'у, тест проваливается, PR не может влиться.

<Inset kind="why" lang="ru">
Off-CPU профилирование (eBPF off-CPU profiles, Java async-profiler в режиме wall-clock, JFR socket-wait events) — четвёртый вариант. Оно показывает, что приложение тратит большую часть wall-time в ожидании socket базы данных, а не выполняя код. Off-CPU профилирование — окончательный инструмент для класса проблем "программа медленная, но CPU profile здоровый" — что и является сигнатурой N+1.
</Inset>

## Чтение реального query log

```
# tail -f log/development.log | grep -E "SELECT|Loading"

Started GET "/dashboard"
User Load (0.4ms)    SELECT "users".* FROM "users" WHERE "users"."id" = 42
Project Load (0.6ms) SELECT "projects".* FROM "projects" WHERE "projects"."user_id" = 42 LIMIT 50
Task Load (0.3ms)    SELECT "tasks".* FROM "tasks" WHERE "tasks"."project_id" = 1
Task Load (0.3ms)    SELECT "tasks".* FROM "tasks" WHERE "tasks"."project_id" = 2
Task Load (0.4ms)    SELECT "tasks".* FROM "tasks" WHERE "tasks"."project_id" = 3
... (ещё 47 строк Task Load)
Comment Load (0.2ms) SELECT "comments".* FROM "comments" WHERE "comments"."task_id" = 1
Comment Load (0.2ms) SELECT "comments".* FROM "comments" WHERE "comments"."task_id" = 2
... (ещё 243 строки Comment Load)

Completed 200 OK in 980ms (Views: 250.3ms | ActiveRecord: 689.4ms)
```

Читаем: 980 мс итого, 689 мс из которых — ActiveRecord на 300+ запросов. Два уровня N+1:
- Уровень 2 (projects → tasks): 50 отдельных Task Load запросов, по одному на project.
- Уровень 3 (tasks → comments): 200–300 отдельных Comment Load запросов, по одному на task.

Фикс: `projects.includes(tasks: :comments).limit(50)` в Rails (или эквивалент в вашем ORM). С 300 запросов до 4 — один для user, один для projects, один для tasks (IN), один для comments (IN). Время ActiveRecord: 689 мс → ~15 мс.

<Quiz
  id="03-detecting-nplus1-quiz-1"
  lessonSlug="03-detecting-nplus1"
  lang="ru"
  question="APM trace показывает 142 database span'а на запрос на endpoint /dashboard, при p99 1.8 с. Span'ы короткие (1–3 мс каждый). Что является диагнозом?"
  choices={[
    { label: "Классический N+1 — много коротких запросов складываются в большое серийное wall-clock время. 142 span'а по 2 мс каждый — это минимум 284 мс серийно; остальное — overhead и обработка результатов.", correct: true },
    { label: "В базе данных не хватает индексов", misconception: "Нехватка индексов замедляет отдельные запросы. Эти span'ы уже короткие (1–3 мс). Проблема — в количестве, а не в скорости отдельного запроса." },
    { label: "Пул соединений слишком мал", misconception: "Маленький пул вызывает ожидание в очереди, а не множество быстрых запросов. Симптом здесь — количество запросов, а не исчерпание пула." },
    { label: "APM-инструмент дублирует подсчёт запросов", misconception: "APM автоинструментирование корректно захватывает один span на запрос. 142 span'а означают 142 запроса." },
  ]}
/>

<Quiz
  id="03-detecting-nplus1-quiz-2"
  lessonSlug="03-detecting-nplus1"
  lang="ru"
  question="PR добавляет новый сериализатор, обращающийся к order.customer.name для каждого заказа в списке. Тесты проходят. Какой CI-механизм поймал бы этот N+1 до выкатки?"
  choices={[
    { label: "Гем Rails bullet, настроенный на raise при lazy-loaded relations в тестовом режиме, или Django assertNumQueries в тесте сериализатора.", correct: true },
    { label: "Линтер, проверяющий плохие имена переменных", misconception: "Линтер не понимает поведение запросов в runtime." },
    { label: "Memory profiler", misconception: "Memory profiler'ы ловят рост аллокаций, а не рост количества запросов." },
    { label: "Code review с замечанием про цикл", misconception: "Code review помогает, но зависит от того, заметит ли ревьюер паттерн. Автоматический gate ловит это каждый раз." },
  ]}
/>

<DragOrder
  id="03-detecting-nplus1-drag-1"
  lessonSlug="03-detecting-nplus1"
  lang="ru"
  prompt="Упорядочите уровни обнаружения N+1 от самого дешёвого до самого труднодоступного:"
  items={[
    { id: "a", label: "Development query log — включить ORM logger, считать строки на запрос" },
    { id: "b", label: "CI gate — bullet/assertNumQueries провалит PR до выкатки" },
    { id: "c", label: "APM trace на staging — waterfall показывает колонки DB span'ов" },
    { id: "d", label: "Алерт в продакшне — queries_per_request p99 скачет на живом endpoint'е" },
  ]}
  correctOrder={["b", "a", "c", "d"]}
/>

</Explanation>

<KeyTakeaway>N+1 скрывается от CPU-профилировщиков, потому что стоимость — это off-CPU wait time. Диагностируйте с query logs (ищите повторяющиеся похожие SELECT'ы), APM waterfall traces (высокая колонка коротких DB span'ов) и счётчиками запросов на запрос. Предотвращайте с CI gate — Rails bullet или Django assertNumQueries — который проваливает PR'ы с lazy-load регрессиями до попадания в продакшн.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="03-detecting-nplus1-retrieval"
  lang="ru"
  questions={[
    { q: "Почему CPU-профиль пропускает N+1, и какие три инструмента правильно его обнаруживают?", a: "CPU-профилировщик сэмплирует стек-трейсы работающих потоков. Проблема N+1 не нагружает CPU в каком-то одном месте. Каждый запрос отправляется за микросекунды; программа ждёт базу данных off-CPU; ответ приходит быстро; отправляется следующий запрос. Ожидание — узкое место, но оно off-CPU; профилировщик видит быстрые всплески без ничего широкого. Три инструмента, правильно обнаруживающих N+1: (1) Database query log — Postgres log_min_duration_statement=0, MySQL slow query log, ORM logging. Сигнатура: десятки похожих SELECT'ов, отличающихся только параметром id = ? в WHERE для одного запроса. (2) APM tracing — Datadog, Tempo, Honeycomb, New Relic с автоинструментированием. Waterfall trace показывает высокую колонку коротких DB span'ов для запроса. (3) Счётчик запросов на запрос — middleware, считающий запросы и публикующий как histogram по endpoint. Алертить когда p99 превышает 20." },
    { q: "Опишите паттерн CI gate для предотвращения регрессий N+1 от попадания в продакшн.", a: "CI gate запускается во время тестов на каждом PR. В Rails гем bullet настроен на raise при любом lazy-loaded relation, обращение к которому происходит при рендеринге запроса: тест контроллера, вызывающий N+1, проваливает PR. В Django assertNumQueries(N) оборачивает вызов view и проваливается если количество запросов превышает объявленный бюджет. В SQLAlchemy или Hibernate статистику количества запросов можно проверять на тест. Паттерн: тесты выполняют запросы, счётчик запросов или strict-mode ORM обнаруживает lazy load, тест проваливается, PR не может влиться. Команды, использующие этот gate, сообщают о снижении инцидентов N+1 в продакшне на 70–90%. Первоначальная настройка — 1–2 недели для инструментирования и baseline cleanup; текущее обслуживание минимально, поскольку gate запускается автоматически в CI." },
  ]}
/>

<Recap lang="ru">
N+1 — off-CPU: программа ждёт между запросами, а не внутри какой-то функции. Именно поэтому CPU-профили выглядят здоровыми, пока страница медленная. Правильные инструменты — query log (повторяющиеся похожие SELECT'ы), APM traces (высокая колонка коротких DB span'ов) и счётчики запросов на запрос (алерт при p99 &gt; 20). Наиболее эффективное предотвращение — CI gate (Rails bullet или Django assertNumQueries), проваливающий PR'ы с lazy-load регрессиями до попадания в продакшн. Количество запросов — метрика, которая важна; p99 latency — её результат.
</Recap>
