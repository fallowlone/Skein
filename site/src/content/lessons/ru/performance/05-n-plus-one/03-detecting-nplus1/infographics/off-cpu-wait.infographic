infographic chart-column-simple
data
  title Куда уходят медленные 980 мс запроса
  desc 73% — off-CPU ожидание базы данных на 300+ запросах N+1, и CPU-профилировщик их не видит
  values
    - label ActiveRecord (ожидание БД, off-CPU)
      value 689
    - label Views (рендеринг, on-CPU)
      value 250
theme
  palette #f43f5e #6366f1
