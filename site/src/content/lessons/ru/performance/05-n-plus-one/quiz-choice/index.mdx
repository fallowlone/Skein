---
concepts:
- round-trip-count
- cardinality-fix-choice
- dataloader
- off-cpu-detection
- pool-exhaustion
- query-amplification-dos
estMin: 13
lang: ru
level: senior
order: 7
prereqs: []
slug: quiz-choice
sources:
- https://www.prisma.io/docs/orm/prisma-client/queries/relation-queries
- https://github.com/graphql/dataloader
- https://docs.sqlalchemy.org/en/20/orm/queryguide/relationships.html
status: ready
summary: Синтез всего юнита N+1 в формате выбора — стоимость round-trip, выбор семейства фикса по cardinality, DataLoader, off-CPU детекция, исчерпание пула и DoS-амплификация.
title: 'N+1: тест с множественным выбором'
track: performance
unit: 05-n-plus-one
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Шесть вопросов через весь юнит. Каждый отражает решение, которое ты принимаешь в реальном инциденте — не определение для пересказа, а фикс, который надо выбрать, когда query log перед тобой, а страница тормозит.
</Hook>

<Goal lang="ru">
Подтвердить, что ты связываешь модель стоимости round-trip, четыре семейства фиксов, инструменты детекции и вторичные сбои — синтез, к которому вели отдельные уроки.
</Goal>

<Quiz
  id="05-n-plus-one-choice-1"
  lessonSlug="05-n-plus-one"
  lang="ru"
  question="Страница-список делает 51 запрос по 0.4 мс каждый, но wall-clock — 765 мс. DBA настаивает, что каждый запрос быстрый и индексы в порядке. Как это примирить?"
  choices={[
    { label: "Оба правы. Каждый запрос быстрый на сервере, но клиент платит полный round-trip latency (~15 мс intra-DC) на запрос — RTT плюс parse плюс planner — последовательно. 51 round-trip доминирует; цена в количестве trip'ов, а не в работе на запрос.", correct: true },
    { label: "DBA неправ — медленные запросы прячутся за индексом.", misconception: "Запросы действительно быстрые на стороне сервера. 765 мс — это round-trip latency × 51, а не медленное выполнение. Ускорение каждого запроса тут почти ничего не даёт." },
    { label: "Добавить индекс на дочернюю таблицу, чтобы убрать 765 мс.", misconception: "Индекс снижает время выполнения запроса, уже равное 0.4 мс. Bottleneck — latency 51 round-trip'а, которую индекс не трогает. Нужно меньше trip'ов, а не быстрее." },
    { label: "Сервер приложения CPU-bound между запросами.", misconception: "Время тратится off-CPU в ожидании сокета БД между trip'ами, а не на CPU. CPU-профиль был бы плоским — классическая сигнатура N+1." },
  ]}
/>

<Quiz
  id="05-n-plus-one-choice-2"
  lessonSlug="05-n-plus-one"
  lang="ru"
  question="Страница грузит 50 заказов, у каждого ~20 line item'ов. Ты фиксишь N+1 через JOIN, количество запросов падает 51 → 2, но p99 ухудшается на 30%. Что произошло и какой фикс?"
  choices={[
    { label: "JOIN дублирует 50 родительских колонок заказа на каждый line item — ~1000 строк, где 950 повторяют данные заказа. Лишний payload и парсинг ORM перевешивают сэкономленный round-trip. Перейди на IN-based batching (selectinload / два запроса), где каждая строка возвращается один раз.", correct: true },
    { label: "Eager loading по своей природе медленнее lazy loading.", misconception: "Eager loading почти всегда выигрывает, схлопывая round-trip'ы. Регрессия тут — стратегия JOIN для тяжёлого one-to-many fan-out, а не сам eager loading." },
    { label: "IN-список переполнил planner.", misconception: "Это JOIN, а не IN-запрос — IN-списка нет. Цена — дублирование строк от JOIN, которого IN-based loading избежал бы." },
    { label: "Откатить к N+1 — 2 запроса явно не бьют 51.", misconception: "Два запроса бьют 51 round-trip по latency; регрессия — раздувание payload от дублирования JOIN. Фикс — IN-based loading, а не возврат к N+1." },
  ]}
/>

<Quiz
  id="05-n-plus-one-choice-3"
  lessonSlug="05-n-plus-one"
  lang="ru"
  question="GraphQL-запрос me { posts { author { name } } } на 50 постов делает 1 + 50 author-lookup'ов. Почему ORM eager loading (.includes / select_related) это не фиксит, и что фиксит?"
  choices={[
    { label: "Нет единого места конструирования запроса, где объявить relationships — 50 независимых author-резолверов каждый тянет одну запись. DataLoader фиксит это, ставя ID в очередь на уровне request и запуская один SELECT ... WHERE id IN (...) на тип, когда event loop тикает.", correct: true },
    { label: "Eager loading работает нормально; просто добавь .includes(:author) в резолвер.", misconception: "Каждый резолвер выполняется независимо на родительский объект, без общего места запроса. Некуда прикрепить .includes, чтобы батчить через 50 отдельных author-резолверов — ровно для этого существует DataLoader." },
    { label: "GraphQL запрещает JOIN'ы, так что eager loading невозможен.", misconception: "GraphQL не имеет мнения о SQL. Блокер — разбросанная, по-резолверная природа lookup'ов, а не запрет JOIN'ов." },
    { label: "Ограничить posts до 10, чтобы снизить число lookup'ов.", misconception: "Это обход, уменьшающий симптом; структурный паттерн 1+N остаётся. DataLoader убирает паттерн независимо от размера fan-out." },
  ]}
/>

<Quiz
  id="05-n-plus-one-choice-4"
  lessonSlug="05-n-plus-one"
  lang="ru"
  question="REST-агрегатор вызывает 8 downstream-сервисов последовательно по 30 мс каждый (240 мс p99). SQL N+1 на странице-списке тоже стоит ~240 мс. Это одна проблема, и у них общий фикс?"
  choices={[
    { label: "Одна форма, разная ось фикса. Оба платят N последовательных round-trip'ов. SQL N+1 батчится в один запрос (JOIN/IN/preload/DataLoader). У независимых вызовов сервисов нет общего backend для батчинга, поэтому ты параллелишь — Promise.all / errgroup / asyncio.gather — превращая sum(latencies) в max(latencies), ~35 мс.", correct: true },
    { label: "Несвязанные проблемы — одна про базу, другая про сеть.", misconception: "Обе — одна форма N round-trip'ов на уровень протокола выше. Вся суть юнита: N+1 протокол-агностичен; различается только механизм батчинга." },
    { label: "Параллелить SQL-запросы через Promise.all тоже.", misconception: "Параллельный SQL всё равно платит N round-trip'ов и может насытить пул соединений. SQL к одной БД надо батчить в один запрос, а не разворачивать параллельно." },
    { label: "Обоим нужен более быстрый RPC-протокол.", misconception: "Скорость протокола даёт инкрементальный выигрыш. Структурный фикс — схлопнуть trip'ы: батчить SQL, параллелить независимые вызовы сервисов." },
  ]}
/>

<Quiz
  id="05-n-plus-one-choice-5"
  lessonSlug="05-n-plus-one"
  lang="ru"
  question="Сервис с пулом на 25 соединений начинает отдавать 503 под нагрузкой. Latency отдельных запросов стабильна, БД здорова, но насыщение пула приклеено к ~100%, а очередь запросов растёт. Корневая причина и первый фикс?"
  choices={[
    { label: "N+1-эндпоинт держит соединение через ~50 последовательных запросов (~100 мс на запрос), поэтому пропускная способность пула упирается в pool_size / hold_time ≈ 250 req/s. Фикс N+1 сократит hold-time до ~4 мс; тот же пул тогда держит ~10× трафика.", correct: true },
    { label: "Пул слишком мал — просто подними до 200.", misconception: "Увеличение пула отодвигает стену, но переносит контеншн на базу и маскирует причину. Реальный рычаг — короче hold-time соединения, достигаемый убийством N+1." },
    { label: "Соединения утекают и не освобождаются.", misconception: "Утечка заставляет active count расти и не восстанавливаться. Здесь соединения освобождаются — просто медленно, потому что каждый запрос гонит ~50 последовательных запросов." },
    { label: "База перегружена параллельными соединениями.", misconception: "Latency отдельных запросов стабильна, значит БД не bottleneck. Приложение держит соединения слишком долго через последовательные запросы." },
  ]}
/>

<Quiz
  id="05-n-plus-one-choice-6"
  lessonSlug="05-n-plus-one"
  lang="ru"
  question="Публичный GraphQL-эндпоинт даёт клиентам управлять глубиной вложенности и размером страницы. Один сконструированный запрос разгоняет БД до тысяч запросов. Как сениор должен это сформулировать, и какой устойчивый guard?"
  choices={[
    { label: "Это query amplification — управляемый пользователем параметр, умножающий число запросов, это DoS-вектор. Guard: depth limits, анализ query-complexity и per-request query budget — плюс CI-gate, чтобы будущее изменение не вернуло неограниченный fan-out.", correct: true },
    { label: "Это просто баг производительности; фикси N+1 и двигайся дальше.", misconception: "Фикс одного пути помогает, но на публичном эндпоинте с управляемым пользователем fan-out это вектор security/availability. Нужны границы depth/complexity, а не только фикс одного пути." },
    { label: "Rate-limit клиента — и решено.", misconception: "Rate limiting глушит объём, но один дорогой запрос всё равно может амплифицировать тысячи запросов. Надо ограничивать per-request стоимость через depth и complexity limits." },
    { label: "Добавить read-реплик, чтобы поглотить всплеск.", misconception: "Реплики поднимают ёмкость, но атакующий масштабирует fan-out вместе с запросом; ты будешь платить за обслуживание атаки. Ограничивай per-request стоимость запроса." },
  ]}
/>

<Recap lang="ru">
Сквозная линия юнита — одно дерево решений: сначала считай round-trip'ы (цена в количестве trip'ов, а не в скорости запроса), затем выбирай фикс по cardinality — JOIN для one-to-one, IN/selectinload для тяжёлого one-to-many, preload когда форма известна на месте запроса, DataLoader когда lookup'ы разбросаны по резолверам, и параллельный dispatch когда независимые сервисы не делят backend. Детектируй off-CPU через query log и APM-waterfall, гейти в CI и помни вторичные сбои: раздувание payload от JOIN, исчерпание пула соединений, переключения query plan и DoS-амплификацию.
</Recap>
