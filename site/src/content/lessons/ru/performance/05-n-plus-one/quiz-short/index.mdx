---
concepts:
- round-trip-cost
- fix-families
- dataloader
- off-cpu-detection
- pool-exhaustion
- denormalisation
estMin: 14
lang: ru
level: senior
order: 8
prereqs: []
slug: quiz-short
sources:
- https://www.prisma.io/docs/orm/prisma-client/queries/relation-queries
- https://github.com/graphql/dataloader
- https://docs.sqlalchemy.org/en/20/orm/queryguide/relationships.html
status: ready
summary: Промпты на свободное воспроизведение через весь юнит N+1. Сначала ответь своими словами, потом раскрой модельный ответ и сравни.
title: 'N+1: тест с краткими ответами'
track: performance
unit: 05-n-plus-one
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";

<Hook>
Воспроизведение бьёт перечитывание. На каждый промпт скажи или напиши полный ответ по памяти, прежде чем открыть модельный — усилие припоминания и закрепляет материал.
</Hook>

<Goal lang="ru">
Восстановить хребет юнита — почему число trip'ов задаёт стоимость, четыре семейства фиксов и как выбирать, механизм DataLoader, off-CPU детекцию и вторичные сбои на масштабе — не подглядывая в уроки.
</Goal>

<RetrievalDrawer
  client:load
  id="05-n-plus-one-short-retrieval"
  lang="ru"
  questions={[
    { q: "Почему забор 100 строк одним запросом бьёт 100 запросов по одной строке, даже когда каждый запрос быстрый?", a: "Каждый round-trip несёт фиксированную стоимость, независимую от скорости выполнения запроса: сетевой RTT (1–5 мс intra-DC, 10–50 мс cross-region), query parse, planner overhead и кодирование результата. Один запрос на 100 строк платит эту фиксированную стоимость один раз; 100 запросов платят её 100 раз. Поэтому страница-список, выпускающая 51 запрос по 0.4 мс выполнения каждый, всё равно может занять ~765 мс wall-clock — доминирующий член это round-trip latency × число trip'ов, а не серверное выполнение. Рычаг оптимизации — снизить число trip'ов, а не ускорить каждый. Поэтому же CPU-профиль плоский — время тратится off-CPU в ожидании сокета между trip'ами." },
    { q: "Назови четыре семейства фиксов N+1 и форму данных, под которую подходит каждое.", a: "(1) JOIN — забрать родителя и ребёнка одним SQL-запросом; один round-trip; лучше для one-to-one или малого one-to-many, потому что для тяжёлого one-to-many он дублирует родительские колонки на каждой дочерней строке. (2) IN-based batching (selectinload / prefetch) — два запроса, без дублирования; лучше для тяжёлого one-to-many fan-out. (3) ORM preload (.includes / select_related / include) — объявляет relationship на месте запроса и даёт ORM выбрать JOIN или IN; лучше, когда форма известна в одной точке конструирования. (4) DataLoader — request-scoped батчинг, ставящий ID в очередь по многим путям кода и запускающий один запрос на тип за event-loop tick; лучше для GraphQL-резолверов и multi-source fan-out, где нет единого места запроса. Cardinality и место происхождения lookup'ов задают выбор." },
    { q: "Объясни, как работает DataLoader и почему ORM eager loading не заменяет его для GraphQL.", a: "DataLoader создаётся один раз на request. Вызов loader.load(id) возвращает Promise и ставит id в очередь внутри, не запуская запрос. Когда текущая синхронная работа завершается и event loop тикает, loader запускает один батч-запрос — SELECT ... WHERE id IN (все очередные ids) — и раздаёт результаты каждому ждущему Promise, позиционно сопоставленные с входными ids. Он также кэширует на request, поэтому повторные load одного id ничего не стоят. ORM eager loading объявляет relationships в одном месте конструирования запроса; в GraphQL такого места нет — каждый из N author-резолверов выполняется независимо, без общего места для .includes. DataLoader переносит батчинг из query scope в request scope — ровно то, что нужно разбросанному дереву резолверов GraphQL. Ему нужен async/event-loop runtime, и он добавляет субмиллисекундную batch-window latency и сложность request-scope кэша." },
    { q: "Почему CPU-профили пропускают N+1, и какие три инструмента его вскрывают?", a: "CPU-профайлер сэмплит стеки работающих потоков. N+1 не жжёт CPU ни в одной функции: каждый запрос диспетчерится за микросекунды, затем программа ждёт off-CPU на сокете БД, затем диспетчерится следующий. Профайлер видит быстрые блипы и ничего широкого. Три инструмента вскрывают это: (1) DB query log (Postgres log_min_duration_statement=0, MySQL slow log, ORM logging) — сигнатура это десятки похожих SELECT, отличающихся только в WHERE id = ?; (2) APM-waterfall трейсы (Datadog, Tempo, Honeycomb) — высокая колонна коротких DB-спанов в одном request; (3) per-request query counter, выставленный гистограммой по эндпоинту, с алертом при p99 выше ~20. Самая рычажная профилактика — CI-gate (Rails bullet или Django assertNumQueries), который роняет PR с lazy-load регрессиями до релиза." },
    { q: "Как N+1 каскадирует в исчерпание пула соединений, и какие сигналы его выдают?", a: "N+1-эндпоинт держит одно соединение БД через всю последовательность последовательных запросов. С пулом на 25 и 50 запросами на request по ~2 мс каждый, каждый request пинит соединение на ~100 мс, поэтому пропускная способность пула упирается в pool_size / hold_time ≈ 250 req/s. За пределом этого request'ы становятся в очередь за соединением; время ожидания раздувает latency; хвосты спиралят; появляются 503, а health-check'и могут выкинуть pod'ы из ротации, концентрируя нагрузку на выживших. Признак: насыщение пула (active/max) у 100% устойчиво, рост connection wait time (pgbouncer cl_waiting), высокий query count на эндпоинт, при этом latency отдельных запросов стабильна и БД здорова. Фикс — это фикс N+1: срезать hold-time с ~100 мс до ~4 мс, и тот же пул держит примерно 10× трафика." },
    { q: "Когда денормализация (counter cache, materialised view, CQRS read model) — правильный ответ для N+1, и когда она преждевременна?", a: "Правильна только когда измерение показывает, что даже 2–3 хорошо забатченных запроса не укладываются в экстремальный latency SLO — read p99 ниже ~50 мс на тяжёлых join'ах И read-нагрузка примерно 1000× write-нагрузки. Counter cache подходит для скалярных агрегаций (counts, sums); materialised view — для сложных join/агрегаций, читаемых много чаще, чем пишут; CQRS read model — для случаев, где формы read и write резко различаются. Каждый вариант добавляет сложность на write-стороне, staleness (мс–секунды), дублирование хранения и постоянную поддержку по мере эволюции схемы. Преждевременна, когда SLO 100+ мс (eager loading укладывается), когда read и write нагрузки сопоставимы, или когда никто ещё измерением не подтвердил, что eager loading и есть bottleneck. Частая ошибка — тянуться к CQRS при соотношении read/write 10×, когда одна директива .includes решила бы за 30 минут." },
  ]}
/>

<Recap lang="ru">
Если ты смог восстановить каждый ответ по памяти, ты держишь хребет юнита: число trip'ов — а не скорость запроса — задаёт стоимость; четыре семейства фиксов ложатся на cardinality и происхождение lookup'ов; DataLoader батчит на уровне request, куда ORM preload не дотягивается; N+1 прячется off-CPU и находится в query log, APM-waterfall и per-request счётчиках, затем гейтится в CI; а на масштабе он каскадирует в исчерпание пула, переключения plan'а и DoS, причём денормализация резервируется на случай, когда измерение доказывает, что одного батчинга мало для SLO.
</Recap>
