---
concepts:
- garbage-collection
- allocation-rate
- stop-the-world
- concurrent-gc
- gc-cycle
deepensInto: []
estMin: 12
lang: ru
lessonType: topic
level: junior
order: 1
prereqs:
- performance/02-hot-paths/01-what-makes-a-hot-path
slug: 01-gc-basics
sources:
- https://go.dev/blog/ismmkeynote
- https://v8.dev/blog/concurrent-marking
- https://shipilev.net/jvm/anatomy-quarks/
spiral:
- latency
status: ready
summary: Сборщик мусора автоматически освобождает память в куче — но работа проявляется
  как паузы и CPU. Именно rate аллокаций, а не размер кучи, определяет хвостовую latency.
title: 'Основы GC: за что рантайм берёт налог'
track: performance
unit: 04-gc
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import MetaphorComplete from "~/components/pedagogy/MetaphorComplete.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
p99 latency сервиса взлетает до 800 мс каждые несколько секунд. CPU в норме. Медленных запросов нет. Открываем GC-лог — паузы 600-700 мс каждые 4 секунды. Сервис аллоцирует 1 ГБ/сек, и рантайм останавливает весь мир для уборки.
</Hook>

<Crux>GC возвращает недостижимую память. Время паузы, throughput и размер кучи — 3-way tradeoff. Давление аллокаций — то, чем управляешь ты.</Crux>

<Explanation>

## Что делает сборщик мусора

Сборщик мусора обменивает CPU на memory safety: обходит кучу, маркирует всё достижимое от корней (регистры, стеки потоков, глобальные переменные) и возвращает остальное. Вам не нужно вызывать `free()` — рантайм делает это за вас. Цена: рантайм тратит CPU на учёт, и иногда останавливает приложение для этого.

Три рычага всегда в напряжении:

- **Время паузы** — как долго приложение остановлено, пока GC работает.
- **Throughput** — сколько CPU коллектор забирает у вашего кода.
- **Размер кучи** — сколько overhead-памяти нужно коллектору для эффективной работы.

Минимизировать все три одновременно невозможно.

## Метафора кухни

Представьте кухню ресторана. Повара (ваш код) производят грязную посуду (аллокации). Посудомойщик (GC) её чистит. Если повара генерят посуду быстрее, чем мойщик успевает, посуда копится — кухне приходится останавливаться, чтобы разгрузить раковину. Современный concurrent-посудомойщик моет, пока повара работают; старый stop-the-world-посудомойщик заставляет всех ждать. Даже лучший посудомойщик стоит воды и электричества (CPU). Быстрая кухня — не от магического посудомойщика: от поваров, переиспользующих тарелки.

## Почему rate аллокаций важнее размера кучи

Размер кучи показывает, сколько памяти программа держит в данный момент. Rate аллокаций показывает, как часто запрашивается новая память. Циклы GC запускаются пропорционально скорости накопления мусора — то есть rate аллокаций, а не размер кучи.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Сценарий</th>
        <th class="border border-border px-3 py-2 text-left">Размер кучи</th>
        <th class="border border-border px-3 py-2 text-left">Rate аллокаций</th>
        <th class="border border-border px-3 py-2 text-left">Частота пауз GC</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">Большой стабильный кэш</td>
        <td class="border border-border px-3 py-2">4 ГБ</td>
        <td class="border border-border px-3 py-2">50 МБ/с</td>
        <td class="border border-border px-3 py-2">Раз в ~80 с, короткие паузы</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">High-throughput API</td>
        <td class="border border-border px-3 py-2">100 МБ</td>
        <td class="border border-border px-3 py-2">1 ГБ/с</td>
        <td class="border border-border px-3 py-2">Каждые ~0,1 с, частые спайки</td>
      </tr>
    </tbody>
  </table>
</div>

Сервис с 4 ГБ кучи, но медленными аллокациями, почти не замечает пауз. Сервис со 100 МБ кучи и rate 1 ГБ/с — в постоянном GC. Рычаг для хвостовой latency — rate аллокаций, а не размер кучи.

## Цикл mark-sweep

Любой tracing GC следует одной схеме:

1. **Сканирование корней** — кратко останавливаем, определяем корни (регистры, стеки, глобалы).
2. **Маркировка** — обходим граф ссылок от корней; маркируем каждый достижимый объект.
3. **Очистка** — возвращаем память каждого немаркированного объекта.
4. **Опционально компактизация** — перемещаем живые объекты, устраняя фрагментацию.
5. **Обновление ссылок** — исправляем указатели на перемещённые объекты.
6. **Возобновление** — приложение работает на полной скорости до следующего цикла.

Наивная версия паузит приложение на шагах 1–5. Для кучи 32 ГБ это могут быть секунды — неприемлемо для latency-sensitive сервиса. Современные коллекторы сокращают или устраняют большинство STW-фаз.

## Concurrent vs stop-the-world GC

**Stop-the-world (STW)** коллектор паузит все потоки приложения, пока работает. Простой в реализации; паузы растут с размером кучи.

**Concurrent** коллектор выполняет большую часть работы параллельно с потоками приложения, поэтому видимые пользователю паузы коротки (субмиллисекундные) вместо длинных (десятки-сотни мс). Ему нужны **write barriers** — небольшие фрагменты кода, выполняемые при каждой записи ссылки, чтобы держать коллектор в курсе. Стоимость барьера ~2-10% CPU; выгода — короткие паузы.

<Inset kind="why" lang="ru">
Все современные concurrent GC всё ещё имеют некоторые stop-the-world фазы — сканирование корней, обработка weak-ref, remap. Concurrent коллекторы минимизируют STW, но не устраняют его. Всегда смотрите GC-логи на реальное распределение пауз, а не на заголовки от вендоров.
</Inset>

<Quiz
  id="01-gc-basics-quiz-1"
  lessonSlug="01-gc-basics"
  lang="ru"
  question="p99 latency сервиса коррелирует с паузами GC. Что проверить ПЕРВЫМ?"
  choices={[
    { label: "Профиль аллокаций — какой код производит больше всего мусора. Сокращение аллокаций сокращает работу GC.", correct: true },
    { label: "Немедленно переключиться на другой алгоритм GC", misconception: "Смена коллектора помогает меньше, чем сокращение аллокаций: те же аллокации всё равно надо собирать любым коллектором." },
    { label: "Добавить серверу оперативной памяти", misconception: "Больше кучи немного помогает, но не устраняет давление аллокаций; паузы зависят от мусора, а не только от размера кучи." },
    { label: "Отключить GC", misconception: "Нельзя — рантайму он нужен. Даже в нативных языках есть затраты аллокатора." },
  ]}
/>

<Quiz
  id="01-gc-basics-quiz-2"
  lessonSlug="01-gc-basics"
  lang="ru"
  question="Почему concurrent GC предпочтительнее stop-the-world GC для production-сервисов?"
  choices={[
    { label: "Concurrent коллекторы выполняют большую часть работы параллельно с кодом, поэтому видимые паузы субмиллисекундные, а не десятки-сотни мс.", correct: true },
    { label: "Concurrent GC использует меньше памяти", misconception: "Concurrent GC обычно используют БОЛЬШЕ памяти (write barriers, снимки); они меняют память на время паузы." },
    { label: "Concurrent GC никогда не паузит", misconception: "Все современные concurrent GC всё ещё имеют короткие STW-фазы; они минимизированы, но не устранены." },
    { label: "Concurrent GC проще реализовать", misconception: "Они сложнее — аргумент простоты работает в обратную сторону." },
  ]}
/>

<DragOrder
  id="01-gc-basics-drag-1"
  lessonSlug="01-gc-basics"
  lang="ru"
  prompt="Расставьте концептуальные стадии типичного цикла сборки мусора по порядку:"
  items={[
    { id: "a", label: "Определить корни — регистры, стеки потоков, глобальные переменные" },
    { id: "b", label: "Маркировка — обход от корней, пометка каждого достижимого объекта" },
    { id: "c", label: "Очистка — возврат памяти немаркированных объектов" },
    { id: "d", label: "Опционально компактизация — перемещение живых объектов для устранения фрагментации" },
    { id: "e", label: "Обновление ссылок на перемещённые объекты" },
    { id: "f", label: "Возобновление приложения на полной скорости до следующего цикла" },
  ]}
  correctOrder={["a", "b", "c", "d", "e", "f"]}
/>

<MetaphorComplete
  id="01-gc-basics-metaphor-1"
  pieceSlug="01-gc-basics"
  lang="ru"
  setup="Заполните пропуск: сборка мусора обменивает CPU на memory _______ — вам не нужно отслеживать каждую аллокацию вручную, но рантайм платит за учёт циклами, которые могли бы достаться вашему коду."
  accepted={["safety", "безопасность", "management", "correctness", "reclamation"]}
  canonical="safety (безопасность) — use-after-free и double-free становятся структурно невозможны"
  explanation="GC устраняет целые классы ошибок (use-after-free, double-free, висячие указатели), убирая ручное управление памятью. Цена — время CPU и сложность рантайма. Ручное управление памятью даёт контроль над обоими, вместе с ошибками в комплекте."
/>

</Explanation>

<FlowDiagram
  label="Цикл mark-sweep"
  perRow={6}
  hue="var(--d-hardware)"
  nodes={[{ id: "roots", label: "Сканирование корней", sub: "стеки, глобалы" }, { id: "mark", label: "Маркировка", sub: "достижимое" }, { id: "sweep", label: "Очистка", sub: "освободить мусор" }, { id: "compact", label: "Компактизация", sub: "опционально" }, { id: "update", label: "Обновление ссылок" }, { id: "resume", label: "Возобновление" }]}
  edges={[{ from: "roots", to: "mark" }, { from: "mark", to: "sweep" }, { from: "sweep", to: "compact" }, { from: "compact", to: "update" }, { from: "update", to: "resume" }]}
  caption="Tracing GC сканирует корни, маркирует достижимые объекты, возвращает остальное, опционально компактизирует, затем возобновляет приложение до следующего цикла."
/>

<KeyTakeaway>GC возвращает недостижимую память кучи, чтобы вам не приходилось это делать. Rate аллокаций определяет частоту GC; время паузы — это симптом. Сокращайте аллокации в первую очередь — каждый байт, который вы не аллоцируете, — работа GC, которую рантайму не придётся делать.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="01-gc-basics-retrieval"
  lang="ru"
  questions={[
    { q: "В одном абзаце: почему rate аллокаций важнее общего размера кучи для хвостовой latency, определяемой GC?", a: "Размер кучи показывает, сколько памяти программа держит в данный момент; rate аллокаций — как часто запрашивается новая память. GC запускает циклы пропорционально скорости накопления мусора — а это rate аллокаций, а не размер кучи. Сервис с кучей 4 ГБ и rate 50 МБ/с может запускать цикл раз в минуту с крошечными паузами. Тот же сервис с кучей 100 МБ, но rate 1 ГБ/с крутит GC непрерывно — паузы каждую секунду, работа mark доминирует, p99 страдает. Рычаг для latency — rate аллокаций; для footprint — размер кучи. Сокращение аллокаций (пул буферов, переиспользование slice, struct-of-arrays, мутация на месте) — долгосрочное решение для хвостовой latency от GC." },
    { q: "Назовите 3-way tradeoff любого GC и приведите пример коллектора, оптимизирующего каждый из крайних значений.", a: "Три оси: (1) время паузы — как долго приложение остановлено; (2) throughput — сколько CPU берёт коллектор; (3) размер кучи — сколько overhead-памяти нужно рантайму. JVM ParallelGC оптимизирует throughput ценой длинных пауз. JVM ZGC оптимизирует время паузы ценой ~10-15% throughput и большей памяти. Concurrent GC Go находится посередине: субмиллисекундные паузы и умеренный расход throughput. Минимизировать все три нельзя; выбираешь подходящий для workload." },
  ]}
/>

<Recap lang="ru">
Сборщик мусора маркирует достижимые объекты и возвращает остальное, обменивая циклы CPU на memory safety. Три рычага — время паузы, throughput и размер кучи — нельзя минимизировать одновременно. Rate аллокаций определяет частоту циклов GC сильнее, чем размер кучи: небольшой сервис с rate 1 ГБ/с испытывает куда большее давление GC, чем большой кэш с rate 50 МБ/с. Современные concurrent GC делают большую часть работы параллельно с потоками приложения, удерживая паузы ниже 1 мс, но всё ещё имеют короткие STW-фазы для сканирования корней. Первый рычаг для хвостовой latency от GC — всегда профиль аллокаций, а не выбор коллектора.
</Recap>
