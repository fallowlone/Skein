infographic compare-binary-horizontal-underline-text-vs
data
  title G1 → ZGC: паузы в обмен на throughput и память
  desc Трёхсторонний tradeoff GC — ни один коллектор не выигрывает по всем трём
  compares
    - label G1 (до)
      children
        - label Пауза ~60 мс на куче 16 ГБ
        - label Throughput базовый
        - label Память / RSS базовая
    - label ZGC (после)
      children
        - label Пауза субмиллисекундная (<1 мс)
        - label Throughput падает на 12%
        - label Память / RSS растёт на 18%
theme
  palette #6366f1 #f43f5e
