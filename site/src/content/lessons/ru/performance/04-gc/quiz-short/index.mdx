---
concepts:
- live-set
- generational-hypothesis
- gomemlimit
- tri-color-marking
- allocation-pressure
estMin: 14
lang: ru
level: senior
order: 8
prereqs: []
slug: quiz-short
sources:
- https://go.dev/doc/gc-guide
- https://go.dev/blog/ismmkeynote
- https://openjdk.org/jeps/377
status: ready
summary: Промпты на свободное припоминание по юниту. Сначала ответь своими словами, затем раскрой модельный ответ и сравни.
title: 'GC: свободное припоминание'
track: performance
unit: 04-gc
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";

<Hook>
Припоминание сильнее перечитывания. На каждый промпт проговори или запиши полный ответ по памяти, прежде чем открыть модельный — именно усилие припоминания закрепляет материал.
</Hook>

<Goal lang="ru">
Восстанови ключевые механизмы юнита — живой набор vs RSS, генерационную гипотезу, GOMEMLIMIT, трёхцветную маркировку и лестницу приоритетов фиксов — не подглядывая в уроки.
</Goal>

<RetrievalDrawer
  client:load
  id="04-gc-short-retrieval"
  lang="ru"
  questions={[
    { q: "Почему allocation rate задаёт GC-хвостовую задержку сильнее, чем общий размер кучи?", a: "Размер кучи — это сколько памяти программа держит в каждый момент; allocation rate — как быстро запрашивается новая память. Трассирующий GC запускает цикл каждый раз, когда накапливается достаточно мусора, а это определяется allocation rate, а не абсолютным размером кучи. Кэш 4 ГБ при 50 МБ/с может циклиться раз в минуту с крошечными паузами; сервис 100 МБ при 1 ГБ/с гоняет GC постоянно, паузя каждую долю секунды. Значит рычаг для хвостовой задержки — профиль аллокаций; размер кучи — рычаг для footprint. Каждый невыделенный байт — это работа GC, которой никогда не придётся случиться." },
    { q: "Сформулируй генерационную гипотезу и приведи один коллектор-контрпример.", a: "Генерационная гипотеза — эмпирическое наблюдение, что большинство объектов умирают молодыми: обработчики запросов аллоцируют много короткоживущих временных объектов и мало долгоживущих. Генерационные коллекторы используют это, разделяя кучу на малое, часто собираемое молодое поколение и большое, редко собираемое старое; выжившие промотируются. Если 95% объектов умирают молодыми, один дешёвый проход по young-gen освобождает 95% байт. Контрпример: коллектор Go негенерационный — единая куча с конкурентной трёхцветной маркировкой. Команда Go поставила на то, что escape-анализ и простота рантайма достаточно снижают давление на кучу, чтобы обойтись без генерационной сложности, и достигает sub-ms пауз на большинстве нагрузок. JVM, V8 и .NET — все генерационные." },
    { q: "В чём разница между RSS и живым набором и почему это важно для тюнинга?", a: "RSS (resident set size) — вся физическая память процесса, включая мусор в ожидании следующего цикла, фрагментацию кучи и зарезервированные рантаймом арены. Живой набор — только реально достижимые байты после цикла GC. Время маркировки масштабируется с живым набором; частота циклов — с allocation rate; RSS — ни с тем, ни с другим. Канонический ориентир тюнинга — размер кучи ~1.5–2× от живого набора: ниже 1.5× коллектор работает почти непрерывно и пропускная способность рушится; выше ~3× память тратится впустую и каждый цикл может занимать дольше. Живой набор читают после GC — HeapLive в Go, used старого поколения после full GC в JVM — а не RSS." },
    { q: "Объясни трёхцветный инвариант и что делает барьер записи для его сохранения.", a: "Трёхцветная маркировка метит каждый объект белым (не посещён), серым (посещён, дети в ожидании) или чёрным (полностью просканирован). Инвариант: чёрный объект не должен держать единственную ссылку на белый. Во время конкурентной маркировки мутатор может перекоммутировать ссылки; если он сохранит ссылку на белый объект в уже просканированный чёрный, белый станет невидим коллектору и был бы ошибочно освобождён — тихое повреждение. Барьер записи перехватывает каждую запись ссылки, чтобы это предотвратить. SATB метит старую (перезаписываемую) ссылку (Go, G1, ZGC, Shenandoah); incremental-update метит новую (CMS, классический V8). Цена — ~2–10% CPU на записях ссылок, плата за отказ от stop-the-world маркировки." },
    { q: "Что делает GOMEMLIMIT и почему это первая ручка для контейнеризированного Go-сервиса?", a: "GOMEMLIMIT (Go 1.19+) — мягкий потолок на общую память рантайма, интегрированный в управляющий контур pacer'а. По мере подхода к лимиту pacer приближает циклы GC — принимая более высокий GC CPU — чтобы удержать RSS под потолком; когда использование сильно ниже лимита, он отступает к обычной цели от GOGC. Это первая ручка для контейнеров, потому что pacer по умолчанию не знает о cgroup-лимите, так что под всплеском трафика куча может вырасти за память контейнера и под будет убит по OOM. GOMEMLIMIT ~90% от лимита контейнера защищает границу без правок кода; за GOGC тянешься только после этого." },
    { q: "Перечисли рычаги фикса GC-давления в порядке приоритета, от наибольшего к наименьшему.", a: "(1) Устранить аллокацию — мутация на месте, struct-of-arrays, примитивы вместо боксированных типов. (2) Пулинг/переиспользование — sync.Pool, object pool, reset bytes.Buffer, но только для горячих, дорогих, короткоживущих объектов. (3) Дать escape-анализу разместить на стеке — держи значения scope-локальными, чтобы ни одна ссылка не убегала из фрейма. (4) Уменьшить аллокацию — меньший struct, преразмеренный контейнер, меньший буфер. (5) Убрать с горячего пути — вычисли один раз и закэшируй. (6) Тюнить коллектор — GOMEMLIMIT/GOGC, MaxGCPauseMillis, max-old-space-size. (7) Сменить алгоритм коллектора — ParallelGC → G1 → ZGC. Верх лестницы устраняет работу; низ лишь оптимизирует стоимость работы, которая всё равно случается, поэтому за ним тянешься в последнюю очередь." },
  ]}
/>

<Recap lang="ru">
Если ты смог восстановить каждый ответ по памяти, ты держишь хребет юнита: allocation rate задаёт частоту, живой набор задаёт стоимость, генерационная гипотеза объясняет большинство коллекторов (и сознательное исключение Go), трёхцветный инвариант плюс барьеры записи делают конкурентную маркировку корректной, GOMEMLIMIT защищает границу контейнера, а лестница фиксов всегда начинается с устранения аллокаций — а не с тюнинга.
</Recap>
