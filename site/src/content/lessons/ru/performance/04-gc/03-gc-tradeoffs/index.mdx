---
concepts:
- gc-tradeoff
- allocation-pressure
- object-pooling
- gc-tuning
- throughput-vs-latency
deepensInto: []
estMin: 14
lang: ru
lessonType: topic
level: middle
order: 3
prereqs:
- performance/04-gc/02-gc-algorithms
slug: 03-gc-tradeoffs
sources:
- https://go.dev/doc/gc-guide
- https://openjdk.org/jeps/377
- https://v8.dev/blog/free-garbage-collection
- https://docs.oracle.com/en/java/javase/21/gctuning/
spiral:
- latency
status: ready
summary: Каждый коллектор выбирает две из трёх осей. Давление аллокаций — причина,
  пауза — симптом. Исправить первопричину всегда выгоднее, чем настраивать следствие.
title: 'GC tradeoffs: пауза, throughput, память и давление аллокаций'
track: performance
unit: 04-gc
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import DiagramFrame from "~/components/diagram/DiagramFrame.astro";

<Hook>
Две команды. Одна переходит с G1 на ZGC и снижает паузы с 60 мс до &lt;1 мс — но throughput падает на 12%, а память растёт на 18%. Другая сокращает аллокации на 60% и снижает паузы с 200 мс до 15 мс — без смены коллектора. Какая правка долгосрочнее?
</Hook>

<Crux>Пауза, throughput, память — выбирай два из трёх. Снижение давления аллокаций — единственный рычаг, улучшающий все три одновременно.</Crux>

<Explanation>

## 3-way tradeoff

Каждый GC выбирает две из трёх:

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Коллектор</th>
        <th class="border border-border px-3 py-2 text-left">Пауза</th>
        <th class="border border-border px-3 py-2 text-left">Throughput</th>
        <th class="border border-border px-3 py-2 text-left">Память</th>
        <th class="border border-border px-3 py-2 text-left">Для кого</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">JVM ParallelGC</td>
        <td class="border border-border px-3 py-2">Высокая (сотни мс)</td>
        <td class="border border-border px-3 py-2">Отличный</td>
        <td class="border border-border px-3 py-2">Низкая</td>
        <td class="border border-border px-3 py-2">Batch-задачи без latency-требований</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">JVM G1</td>
        <td class="border border-border px-3 py-2">Средняя (10–50 мс)</td>
        <td class="border border-border px-3 py-2">Хороший</td>
        <td class="border border-border px-3 py-2">Средняя</td>
        <td class="border border-border px-3 py-2">Большинство production-сервисов</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">JVM ZGC</td>
        <td class="border border-border px-3 py-2">&lt;1 мс</td>
        <td class="border border-border px-3 py-2">-10–15%</td>
        <td class="border border-border px-3 py-2">+10–20%</td>
        <td class="border border-border px-3 py-2">Latency-sensitive, кучи &gt;8 ГБ</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Go concurrent</td>
        <td class="border border-border px-3 py-2">&lt;1 мс</td>
        <td class="border border-border px-3 py-2">-5–10%</td>
        <td class="border border-border px-3 py-2">Умеренная</td>
        <td class="border border-border px-3 py-2">Go-сервисы со смешанным workload'ом</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">V8 Orinoco</td>
        <td class="border border-border px-3 py-2">5–15 мс</td>
        <td class="border border-border px-3 py-2">Хороший</td>
        <td class="border border-border px-3 py-2">Умеренная</td>
        <td class="border border-border px-3 py-2">Node.js web workloads</td>
      </tr>
    </tbody>
  </table>
</div>

Выбор коллектора — это вопрос workload'а. Batch-сервисам без требований к latency подходит throughput → ParallelGC. Latency-sensitive интерактивным сервисам нужны короткие паузы → ZGC, Shenandoah, Go. Неправильный выбор может стоить 30–50% throughput или 10x p99 latency.

## Справочные числа GC

- Go concurrent GC, типичный STW: **~100 мкс – 1 мс**
- JVM G1 по умолчанию: **200 мс** (цель)
- JVM ZGC пауза: **&lt;1 мс** на кучах несколько ТБ
- V8 Orinoco типичная пауза: **5–15 мс**
- Накладные расходы write barrier: **~2–10% CPU**
- Rate аллокаций, при котором GC начинает доминировать: **≥500 МБ/с/ядро**
- GOGC по умолчанию: **100** (куча удваивается между циклами)
- Доля GC в здоровом сервисе: **&lt;5% CPU** типично

## Давление аллокаций как первопричина

Если сервис аллоцирует 1 ГБ/с, GC будет занят вне зависимости от коллектора. **Давление аллокаций — причина; время паузы — симптом.**

Сокращение давления аллокаций имеет неограниченный потенциал; настройка GC — ограниченный (оптимизирует стоимость работы, но не устраняет её). Паттерн среднего+ разработчика: когда GC широко представлен в профиле, смотрим на профиль аллокаций. Широкий листовой узел в профиле аллокаций — цель.

**Приоритет правок (от большего рычага к меньшему):**

1. Устранить аллокацию целиком (мутация на месте, struct-of-arrays, примитивы).
2. Пул/переиспользование аллокации (`sync.Pool` в Go, пулы объектов в Java/JS).
3. Escape analysis — стековая аллокация вместо кучи (работает в Go, .NET, частично в JVM с C2).
4. Уменьшить аллокацию (меньший struct, меньший буфер, предварительно sized контейнер).
5. Убрать аллокацию с hot path (вычислить один раз, переиспользовать).
6. Настроить коллектор (GOGC, MaxGCPauseMillis, max-old-space-size).
7. Сменить алгоритм GC (ParallelGC → G1 → ZGC).

<Inset kind="why" lang="ru">
Почему сокращение аллокаций улучшает и p99, и throughput одновременно, хотя их обычно представляют как tradeoff? Аллокации стоят дважды: один раз при аллокации (малая стоимость), второй при сборке GC (пропорционально аллокациям). Сокращение аллокаций снижает обе. p99 улучшается, потому что GC запускается реже и паузы короче. Throughput улучшается, потому что CPU, ранее тратившийся на GC (mark, scan, write barriers), теперь доступен для кода. Tradeoff «throughput vs latency» применим к настройке GC (длинные паузы → выше throughput); к сокращению аллокаций он не применим.
</Inset>

## Object pooling: когда работает, когда нет

`sync.Pool` в Go, Apache Commons Pool в Java, пулы объектов в .NET — паттерны переиспользования аллокаций для снижения давления на GC.

**Работает хорошо:** объекты дорогие в создании и кратко используются на hot path — `bytes.Buffer`, JSON-энкодеры, regexp-объекты, scratch slices.

**Не работает или вредит:**
- Объекты дёшевы в создании (экономия меньше накладных расходов пула).
- Объекты долгоживущие (пул держит память без освобождения).
- Координационные расходы потоков превышают экономию от аллокации.

`sync.Pool` в Go имеет преимущество: GC может опустошить пул между циклами — память не теряется навсегда. Пулы JVM/.NET держат память до явного освобождения.

Правило: **пулить только то, что профиль отмечает как горячий узел аллокации**. Не пулить превентивно.

<Quiz
  id="03-gc-tradeoffs-quiz-1"
  lessonSlug="03-gc-tradeoffs"
  lang="ru"
  question="JVM-сервис видит паузы G1 периодически 200 мс, несмотря на -XX:MaxGCPauseMillis=50. Самый вероятный рычаг с наибольшим влиянием:"
  choices={[
    { label: "Сокращение rate аллокаций через профилирование и правки кода. С меньшим объёмом работы на каждый цикл паузы уменьшаются, а throughput растёт — оба показателя улучшаются одновременно.", correct: true },
    { label: "Переход G1 → ZGC", misconception: "ZGC даст субмиллисекундные паузы, но ценой ~10-15% throughput и ~10% памяти. Правильный шаг, только если аллокации уже минимизированы. Как первый шаг — маскирует, не лечит." },
    { label: "Увеличить кучу до 32 ГБ", misconception: "Удвоение кучи примерно вдвое сокращает частоту циклов GC, но каждый цикл длится дольше из-за бо́льшего объёма живых данных. Полезно как временный буфер, пока не устранена первопричина." },
    { label: "Установить -XX:MaxGCPauseMillis=20", misconception: "G1 будет стараться соблюдать цель 20 мс, собирая чаще меньшими порциями. Throughput упадёт. Иногда правильно после исчерпания возможностей сокращения аллокаций; обычно преждевременно." },
  ]}
/>

<Quiz
  id="03-gc-tradeoffs-quiz-2"
  lessonSlug="03-gc-tradeoffs"
  lang="ru"
  question="V8-сервис в Node.js имеет память, растущую до 1,4 ГБ, после чего падает. Наиболее вероятная причина:"
  choices={[
    { label: "Достигнут лимит old-space V8 (~1,5 ГБ в Node по умолчанию). Либо поднять его через --max-old-space-size, либо найти удерживаемые объекты через heap snapshot и устранить утечку.", correct: true },
    { label: "GC перестал работать", misconception: "GC не перестаёт работать; достигнут жёсткий лимит, установленный V8 на max-old-space-size." },
    { label: "Node не имеет GC", misconception: "Node использует полный GC V8; лимит настраиваемый, а не отсутствующий." },
    { label: "Утечки памяти всегда вызывают рестарты", misconception: "Только когда достигают лимита; не все утечки крашат, некоторые просто деградируют." },
  ]}
/>

<DragOrder
  id="03-gc-tradeoffs-drag-1"
  lessonSlug="03-gc-tradeoffs"
  lang="ru"
  prompt="Расставьте рычаги снижения давления GC по приоритету — от наибольшего к наименьшему:"
  items={[
    { id: "a", label: "Устранить аллокацию целиком (мутация на месте, примитивы)" },
    { id: "b", label: "Пул/переиспользование аллокации (sync.Pool, ObjectPool)" },
    { id: "c", label: "Позволить escape analysis выполнить стековую аллокацию" },
    { id: "d", label: "Уменьшить аллокацию (меньший struct, предварительно sized)" },
    { id: "e", label: "Убрать аллокацию с hot path (вычислить один раз, переиспользовать)" },
    { id: "f", label: "Настроить коллектор (GOGC, MaxGCPauseMillis)" },
    { id: "g", label: "Сменить алгоритм GC (ParallelGC → G1 → ZGC)" },
  ]}
  correctOrder={["a", "b", "c", "d", "e", "f", "g"]}
/>

</Explanation>

<DiagramFrame
  label="Треугольник tradeoff GC: выбирай два"
  hue="var(--d-hardware)"
  caption="Короткие паузы, высокий throughput, низкая память: любой коллектор берёт два угла из трёх, но не все три. Снижение давления аллокаций — единственный рычаг, двигающий все три внутрь."
>
  <svg viewBox="0 0 320 230" width="320" height="230" style="display:block;margin:0 auto;max-width:100%;height:auto;font-family:var(--font-body);">
    <polygon points="160,30 30,200 290,200" fill="none" stroke="var(--d-hardware)" stroke-width="1.6" opacity="0.85" />
    <circle cx="160" cy="30" r="4" fill="var(--d-hardware)" />
    <circle cx="30" cy="200" r="4" fill="var(--d-hardware)" />
    <circle cx="290" cy="200" r="4" fill="var(--d-hardware)" />
    <text x="160" y="20" text-anchor="middle" font-size="13" fill="var(--ink)">Короткие паузы</text>
    <text x="30" y="220" text-anchor="middle" font-size="13" fill="var(--ink)">Высокий throughput</text>
    <text x="290" y="220" text-anchor="middle" font-size="13" fill="var(--ink)">Низкая память</text>
    <text x="160" y="138" text-anchor="middle" font-size="12" font-family="var(--font-mono)" fill="var(--muted)">выбирай два</text>
  </svg>
</DiagramFrame>

<KeyTakeaway>Каждый коллектор жертвует одной из трёх осей: паузой, throughput или памятью. Давление аллокаций — причина, пауза — симптом. Сокращение аллокаций улучшает и p99, и throughput одновременно — без ограничений. Настройка коллектора — второй шаг; смена алгоритма — последний.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="03-gc-tradeoffs-retrieval"
  lang="ru"
  questions={[
    { q: "Опишите 3-way tradeoff GC и объясните, почему сокращение аллокаций лучше настройки параметров коллектора.", a: "Три оси: время паузы (как долго приложение остановлено), throughput (сколько CPU коллектор берёт у кода) и размер кучи (overhead-память для работы коллектора). Минимизировать все три нельзя: короткие паузы требуют concurrent работы и write barriers, которые стоят CPU и памяти; throughput-ориентированные коллекторы объединяют всё в одну STW-фазу. Настройка параметров коллектора оптимизирует стоимость работы, которую GC должен сделать; сокращение аллокаций устраняет саму работу. Настройка ограничена — паузу 200 мс можно сократить до 50 мс, но не до 0 при том же rate аллокаций. Сокращение аллокаций принципиально неограничено: каждый байт, который не аллоцируется, — байт, которым GC не надо управлять. Кроме того, сокращение аллокаций улучшает и p99, и throughput одновременно — tradeoff отсутствует. Настройка GC — tradeoff: меньше пауз означает больше накладных расходов на concurrent работу." },
    { q: "Когда object pooling снижает давление GC, а когда вредит?", a: "Пулинг амортизирует стоимость аллокации за счёт переиспользования объектов. Помогает, когда объект дорог в создании (например, bytes.Buffer, выросший до 64 КБ, regexp.Regexp, скомпилированный из паттерна) и кратко используется на hot path. Вредит в трёх случаях: (1) дешёвые объекты — mutex или атомарные операции пула стоят дороже, чем свежая аллокация; (2) долгоживущие объекты — пул держит большие буферы в памяти даже при низком трафике; (3) неправильная гигиена пула — объекты возвращаются с устаревшим состоянием (например, bytes.Buffer без Reset). sync.Pool Go имеет преимущество: GC может опустошить пул между циклами. Пулы JVM/.NET держат память до явного maxIdle/maxTotal. Диагностика: профилируйте аллокации первым делом; пулите только топ-N горячих мест; верифицируйте падение rate аллокаций перед тем, как объявить победу." },
  ]}
/>

<Recap lang="ru">
Каждый коллектор выбирает две из трёх осей: пауза, throughput, память. JVM ParallelGC оптимизирует throughput ценой длинных пауз; ZGC покупает субмиллисекундные паузы за счёт throughput и памяти; G1 балансирует между всеми тремя. Go concurrent GC даёт субмиллисекундные паузы при умеренной стоимости. Давление аллокаций — причина высоких пауз; пауза — симптом. Сокращение аллокаций улучшает и p99, и throughput одновременно без ограничений, поскольку устраняет сам объём работы GC. Правильный приоритет: устранить аллокацию → пул → escape analysis → уменьшить → вынести с hot path → настроить → сменить коллектор. Object pooling снижает давление на горячих путях, но вредит для дешёвых или долгоживущих объектов.
</Recap>
