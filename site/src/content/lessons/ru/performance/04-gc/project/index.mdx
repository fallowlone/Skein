---
concepts:
- allocation-profiling
- gc-observability
- gomemlimit
- allocation-reduction
estMin: 240
lang: ru
level: senior
order: 10
prereqs: []
slug: project
sources:
- https://go.dev/doc/gc-guide
- https://go.dev/blog/pprof
status: ready
summary: Практический проект — заинструментируй, диагностируй и укроти GC death-spiral в небольшом аллокационно-тяжёлом сервисе, затем докажи фикс числами до/после.
title: 'GC: укроти death-spiral'
track: performance
unit: 04-gc
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import ProjectBrief from "~/components/pedagogy/ProjectBrief.astro";

<Hook>
Читать про death-spiral — не то же самое, что вытащить сервис из него. Построй небольшой аллокационно-тяжёлый сервер, загони его в GC-беду и применяй лестницу фиксов юнита, пока числа не вернутся — с доказательством на каждом шаге.
</Hook>

<Goal lang="ru">
Преврати ментальную модель юнита в воспроизводимый инженерный цикл: заинструментируй аллокации и GC, диагностируй горячую точку по профилю, сократи аллокации, защити границу памяти и подтверди фикс метриками до/после.
</Goal>

<ProjectBrief
  id="04-gc-project"
  lessonSlug="04-gc"
  lang="ru"
  objective="Возьми намеренно аллокационно-тяжёлый HTTP-сервис (свой или стартовый ниже) и снизь его долю GC CPU ниже 5%, а p99 — до целевого, не меняя коллектор, доказывая каждый шаг измерениями."
  requirements={[
    "Построй или возьми небольшой HTTP-сервис на Go (или JVM/Node) хотя бы с одним аллокационно-тяжёлым эндпоинтом: пер-запросный fmt.Sprintf/сборка JSON в буфер нулевой ёмкости или хендлер, возвращающий неограниченные результаты запроса.",
    "Подключи наблюдаемость аллокаций и GC: allocation rate (байт/с), распределение пауз GC (p50/p99/max), долю GC CPU и размер кучи vs живой набор — четыре панели или эквивалент, залогированный из runtime/metrics + GODEBUG=gctrace=1.",
    "Нагрузи эндпоинт достаточно сильно, чтобы поднять GC CPU выше ~15% (или заметно нарастить время конкурентной маркировки). Зафиксируй базовые числа и сигнатуру death-spiral в gctrace.",
    "Сними профиль аллокаций (/debug/pprof/allocs или эквивалент) и определи топ-3 горячих точек по суммарным байтам.",
    "Применяй фиксы с верха лестницы: устрани или преаллоцируй буфер, убери Sprintf с горячего пути, пуль только то, что отмечает профиль. Задокументируй, какой рычаг использован для каждой точки.",
    "Выстави GOMEMLIMIT (Go) / max-heap (JVM) / --max-old-space-size (Node) на разумную границу (~90% лимита контейнера) и покажи, что рантайм защищает её под той же нагрузкой.",
    "Перезапусти идентичный нагрузочный тест и запиши числа «после» рядом с «до»; allocation rate должен упасть ≥50%, а GC CPU — ниже 5%.",
  ]}
  acceptance={[
    "Таблица до/после: allocation rate, GC CPU %, p99 пауза и p99 задержка запроса — измеренные под одинаковой нагрузкой, а не оценённые.",
    "Профиль аллокаций чётко показывает уменьшение топовых точек после фикса (перепрофилировано, а не предположено).",
    "Доля GC CPU держится ниже ~5%, и сигнатура death-spiral исчезла из gctrace при устойчивой нагрузке.",
    "Абзац-разбор: какой рычаг применён для каждой горячей точки и почему он встал выше тюнинга коллектора.",
  ]}
  stretch={[
    "Добавь одностраничный on-call runbook: быстрый triage по четырём панелям, типичные причины аллокаций для твоего рантайма, лестница приоритетов фиксов и чек-лист верификации.",
    "Добавь защиту от allocation-DoS — лимит размера тела запроса и потолок размера результата — и покажи, что сервис остаётся ограниченным под флудом негабаритных payload'ов.",
    "Добавь CI-гейт, который нагружает canary, диффит профиль аллокаций против main и валит сборку, если доля аллокаций любой функции выросла больше чем на 20%.",
    "Повтори эксперимент на втором рантайме (например, добавь версию на JVM или Node) и сравни, как один и тот же паттерн аллокаций проявляется под другим коллектором.",
  ]}
/>

<Recap lang="ru">
Это цикл, который ты будешь запускать в каждом реальном GC-инциденте: сначала инструментируй, диагностируй по профилю, чини с верха лестницы (устранить раньше, чем пулить, раньше, чем тюнить, раньше, чем менять), защити границу памяти через GOMEMLIMIT и подтверди числами до/после под идентичной нагрузкой. Сделав это раз на игрушечном сервисе, ты доводишь production-версию до уровня мышечной памяти.
</Recap>
