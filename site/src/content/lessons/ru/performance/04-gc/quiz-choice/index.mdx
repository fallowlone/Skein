---
concepts:
- allocation-rate
- gomemlimit
- zgc-tradeoff
- write-barrier
- finalizer-storm
- allocation-dos
estMin: 12
lang: ru
level: senior
order: 7
prereqs: []
slug: quiz-choice
sources:
- https://go.dev/doc/gc-guide
- https://openjdk.org/jeps/377
- https://v8.dev/blog/concurrent-marking
status: ready
summary: Тест с выбором на синтез по всему юниту — давление аллокаций, pacing, компромиссы коллекторов, барьеры и production-сбои.
title: 'GC: тест с выбором ответа'
track: performance
unit: 04-gc
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Шесть вопросов поперёк всего юнита. Каждый отражает решение, которое ты принимаешь в реальном инциденте — не определение для заучивания, а компромисс, который надо взвесить под нагрузкой.
</Hook>

<Goal lang="ru">
Убедись, что связываешь давление аллокаций, выбор коллектора, ручки тюнинга и production-сбои — тот синтез, к которому вели отдельные уроки.
</Goal>

<Quiz
  id="04-gc-choice-1"
  lessonSlug="04-gc"
  lang="ru"
  question="Сервис A держит кэш 4 ГБ и аллоцирует 50 МБ/с. Сервис B держит 100 МБ живых данных и аллоцирует 1 ГБ/с. Коллектор одинаковый. У кого больше GC-хвостовая задержка и почему?"
  choices={[
    { label: "Сервис B — частота циклов GC определяется allocation rate, а не размером кучи. B производит мусор в 20× быстрее, поэтому паузит гораздо чаще, несмотря на меньшую кучу.", correct: true },
    { label: "Сервис A — бо́льшая куча означает, что каждая фаза маркировки сканирует больше памяти.", misconception: "Стоимость маркировки масштабируется с живым набором, а стабильный кэш — это в основном устойчивые живые данные, собираемые редко. Здесь доминирует частота циклов, которую задаёт allocation rate." },
    { label: "У обоих одинаково — коллектор тот же.", misconception: "Коллектор тот же, но профили аллокаций совсем разные. Алгоритм задаёт форму паузы; allocation rate — как часто ты за неё платишь." },
    { label: "Ни у кого — хвост задаёт обработка запросов, а не GC.", misconception: "Паузы GC — главный источник хвостовой задержки в аллокационно-тяжёлых сервисах; на этом построен весь юнит." },
  ]}
/>

<Quiz
  id="04-gc-choice-2"
  lessonSlug="04-gc"
  lang="ru"
  question="Go-сервис в контейнере 4 ГБ спорадически убивается по OOM на пиках трафика; GOGC по умолчанию 100. Какая первая ручка и что она реально делает?"
  choices={[
    { label: "Выставить GOMEMLIMIT ~90% от лимита контейнера. Pacer приближает циклы GC по мере подхода к границе, тратя больше GC CPU, лишь бы RSS не превысил cgroup-лимит.", correct: true },
    { label: "Выставить GOGC=50, чтобы куча была меньше.", misconception: "GOGC=50 триггерит GC при меньшем коэффициенте роста, но не задаёт потолок — достаточно большой всплеск всё равно нарастит кучу за лимит контейнера и вызовет OOM." },
    { label: "Выставить GOGC=off и положиться на лимит контейнера.", misconception: "GOGC=off полностью отключает пропорциональный триггер; куча только растёт. Это безопасно лишь для коротких batch-задач, которые завершаются, но не для долгоживущего сервиса." },
    { label: "Перейти на ZGC.", misconception: "ZGC — это коллектор JVM; здесь Go. Корневая причина — отсутствие мягкого лимита памяти, а не алгоритм коллектора." },
  ]}
/>

<Quiz
  id="04-gc-choice-3"
  lessonSlug="04-gc"
  lang="ru"
  question="После миграции с G1 на (негенерационный) ZGC паузы упали с 60 мс до <1 мс, но пропускная способность снизилась ~12%, а RSS вырос ~18%. Как это читать?"
  choices={[
    { label: "Ожидаемо. ZGC покупает время паузы ценой load-барьеров на каждое чтение из кучи (~5–15% CPU) и multi-mapped памяти. Трёхсторонний компромисс фундаментален; генерационный ZGC (JDK 21+) возвращает бо́льшую часть пропускной способности.", correct: true },
    { label: "Миграция настроена неверно — ZGC должен выигрывать по всем трём осям.", misconception: "Ни один коллектор не минимизирует паузу, пропускную способность и память одновременно. Эти числа — в пределах нормальных накладных негенерационного ZGC." },
    { label: "Пропускная способность и память не связаны с выбором коллектора.", misconception: "Накладные барьеров напрямую облагают CPU-пропускную способность, а multi-mapping цветных указателей раздувает память. Оба определяются коллектором." },
    { label: "Откатить — регрессия доказывает, что ZGC здесь неуместен.", misconception: "Если нагрузка чувствительна к задержке на большой куче, обмен 12% пропускной способности на паузы в 60× меньше — часто и есть цель. Суди по SLO, а не по одному числу пропускной способности." },
  ]}
/>

<Quiz
  id="04-gc-choice-4"
  lessonSlug="04-gc"
  lang="ru"
  question="Зачем конкурентному коллектору барьер записи и что ломается без него?"
  choices={[
    { label: "Он поддерживает трёхцветный инвариант: чёрный (просканированный) объект не должен держать единственную ссылку на белый (непросканированный). Без барьера коллектор может завершить маркировку и освободить живой объект — тихое повреждение кучи.", correct: true },
    { label: "Он ускоряет маркировку, распараллеливая скан.", misconception: "Барьер добавляет накладные на каждую запись (2–10% CPU); он не распараллеливает маркировку. Его задача — корректность при конкурентной мутации, а не скорость." },
    { label: "Он уплотняет кучу, убирая фрагментацию.", misconception: "Уплотнение — отдельная фаза. Барьер записи — про то, чтобы не потерять живые объекты, когда мутатор перекоммутирует ссылки во время маркировки." },
    { label: "Он нужен только генерационным коллекторам.", misconception: "Он нужен любому конкурентному коллектору, генерационному или нет — Go негенерационный и всё равно использует барьер записи." },
  ]}
/>

<Quiz
  id="04-gc-choice-5"
  lessonSlug="04-gc"
  lang="ru"
  question="Java-сервис регистрирует Object.finalize() на обёртках файловых дескрипторов; под нагрузкой число открытых дескрипторов непредсказуемо скачет. Корневая причина?"
  choices={[
    { label: "Финализаторы выполняются на GC-управляемом потоке без гарантии времени. Под churn финализируемые объекты накапливаются быстрее, чем очередь финализаторов их обрабатывает, удерживая дескрипторы открытыми. Фикс: явный close() / try-with-resources / Cleaner.", correct: true },
    { label: "Утечка файловых дескрипторов в ОС.", misconception: "ОС выдаёт дескрипторы корректно; непосредственная причина — недетерминированное освобождение через тайминг финализатора." },
    { label: "Куча слишком мала, поэтому финализаторы запускаются поздно.", misconception: "Размер кучи не управляет планированием потока финализаторов. Финализаторы — просто неверный механизм для детерминированного освобождения ресурсов." },
    { label: "GC отключён для этих объектов.", misconception: "Они всё ещё собираются — но только после запуска их финализатора, и именно эта задержка удерживает дескрипторы." },
  ]}
/>

<Quiz
  id="04-gc-choice-6"
  lessonSlug="04-gc"
  lang="ru"
  question="gctrace показывает рост GC CPU 1% → 39% за час, при этом время конкурентной маркировки растёт с 1.3 мс до ~350 мс. Что происходит и какой первый фикс?"
  choices={[
    { label: "Death-spiral: pacer не успевает завершить цикл до следующей волны аллокаций, поэтому планирует GC агрессивнее, отбирая CPU и усугубляя отставание. Первый фикс: найти горячую точку аллокаций через alloc-профиль и сократить allocation rate.", correct: true },
    { label: "Нормальный прогрев — GC% всегда растёт по мере заполнения кэшей.", misconception: "Здоровый сервис держит GC CPU под ~5%. Монотонный рост до 39% с растущим временем маркировки — патология, а не прогрев." },
    { label: "Поднять GOGC до 400, чтобы остановить циклы.", misconception: "Откладывание GC меняет память на меньшее число циклов и может загнать в OOM во время спирали; это маскирует причину. Сначала сократи аллокации, потом думай про тюнинг." },
    { label: "Добавить CPU — коллектору просто нужно больше ядер.", misconception: "Больше ядер отсрочат OOM, но спираль возобновится, так как давление аллокаций растёт с нагрузкой. Устойчивый фикс — меньше аллокаций." },
  ]}
/>

<Recap lang="ru">
Сквозная линия юнита — одно дерево решений: allocation rate задаёт частоту GC, живой набор задаёт стоимость цикла, коллектор задаёт форму паузы, а ручки (сначала GOMEMLIMIT, потом GOGC/цель по паузе, в последнюю очередь алгоритм) защищают границы. Все production-сбои — OOM на пиках, компромиссы ZGC, штормы финализаторов, death-spiral, allocation-DoS — сводятся к одному рычагу: сократи аллокации, прежде чем что-либо тюнить.
</Recap>
