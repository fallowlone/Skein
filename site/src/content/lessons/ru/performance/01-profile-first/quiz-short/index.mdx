---
concepts:
- measurement-loop
- amdahl-law
- self-vs-cum-time
- observer-effect
- statistical-baseline
- hardware-counters
estMin: 14
lang: ru
level: senior
order: 8
prereqs: []
slug: quiz-short
sources:
- https://dl.acm.org/doi/10.1145/356635.356640
- https://www.brendangregg.com/methodology.html
- https://www.brendangregg.com/flamegraphs.html
status: ready
summary: Промпты на свободное воспроизведение по всему юниту profile-first. Сначала ответь своими словами, затем открой модельный ответ и сравни.
title: 'Сначала профиль: тест на воспроизведение'
track: performance
unit: 01-profile-first
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";

<Hook>
Воспроизведение бьёт перечитывание. Для каждого промпта проговори или напиши полный ответ по памяти прежде, чем открыть модельный — именно усилие припоминания закрепляет материал.
</Hook>

<Goal lang="ru">
Восстанови ключевые механизмы юнита — измерительный цикл, потолок Amdahl, self vs cum-time, выбор scope, observer effect, статистические baseline и hardware counters — не подглядывая в уроки.
</Goal>

<RetrievalDrawer
  client:load
  id="01-profile-first-short-retrieval"
  lang="ru"
  questions={[
    { q: "Перечисли семь шагов измерительного цикла и объясни, почему пропуск baseline или гипотезы превращает его обратно в угадывание.", a: "Воспроизведи медленный сценарий под реалистичной нагрузкой; сними baseline-профиль достаточной длительности для статистической валидности (≈30 с на 100 Гц ≈ 3000 сэмплов); прочитай профиль и назови горячую точку конкретными числами (X% CPU, self-time на запрос); сформируй одну гипотезу о фиксе и предскажи speedup через Amdahl; примени это одно изменение в изоляции; сними новый профиль под той же нагрузкой и диффай против baseline; отгрузи и смотри production-метрики. Пропуск baseline означает, что нет «до», чтобы доказать, что фикс сработал. Пропуск гипотезы означает, что нельзя сказать, был ли фикс причинным или повезло. Каждый шаг — контроль в эксперименте; убери контроль — и ты снова в интуиции." },
    { q: "Сформулируй закон Amdahl и объясни, почему выигрыш микробенчмарка 10x может дать speedup приложения 1.05x.", a: "total_speedup = 1 / ((1 − p) + p/s), где p — доля общего времени в секции, s — локальный speedup. Потолок (s → ∞) равен 1 / (1 − p). Микробенчмарк измеряет s в изоляции, но ничего не говорит о p. Если оптимизированная функция — лишь ~5% общего времени, то даже s = 10 даёт 1 / (0.95 + 0.05/10) ≈ 1.05x — приложение почти не двигается, потому что функция была малой долей. Урок: профиль даёт p, бенчмарк даёт s, и нужны оба, чтобы предсказать реальный выигрыш. Оптимизация функции с малой долей — потраченный инженерный труд, как бы велик ни был локальный speedup." },
    { q: "В чём разница между self-time и cum-time, и какое действие подсказывает каждое чтение?", a: "Self-time — время в собственных инструкциях функции, исключая вызываемые ею функции. Cum-time (cumulative) включает всё поддерево вызовов ниже. Если self-time велик, функция сама делает дорогую работу — чини там. Если cum-time велик, а self-time крошечный, функция — тонкий routing/dispatch-слой (HTTP-хендлер, middleware), и реальная стоимость в вызываемом — спускайся по flame graph к широкому листу с большим self-time. Путаница ведёт к рерайту обёртки, пока стоимость остаётся в вызываемом, или наоборот." },
    { q: "Почему надо проверять, что overhead профайлера в пределах ~5% от baseline headline-метрики, прежде чем верить профилю (observer effect)?", a: "Каждый профайлер возмущает систему, которую измеряет. Sampling-профайлер на 100 Гц добавляет 0.5–2% — приемлемо. Instrumentation-профайлер, тайминящий каждый entry/exit, добавляет 20–100% в JIT-рантаймах, потому что отключает инлайнинг оптимизатора, поэтому измеряет более медленную, иначе сформированную программу, чем работает в production. Если задержка или throughput с включённым профайлером отличаются от baseline без профайлера более чем на ~5%, горячие точки профиля могут быть артефактами измерения, а не реальной production-стоимостью. Поэтому: сними headline-метрику без профайлера сначала, затем подтверди, что прогон с профайлером в пределах 5%, прежде чем верить, куда он указывает. Используй sampling для production; instrumentation резервируй для плотных микробенчмарков, где важен точный счёт вызовов." },
    { q: "Почему один прогон бенчмарка никогда не измерение и что сообщать вместо этого?", a: "Два прогона идентичного кода обычно различаются на 5–10% из источников, не связанных с изменением: тайминг GC (окна stop-the-world сдвигаются), решения планировщика ОС, прогрев кэша на старте, транзиентный disk/network contention и частотный скейлинг CPU на ноутбуках. «На 15% быстрее» по одному прогону неотличимо от этого пола шума. Сообщай вместо этого: median, p95 и p99 минимум по 5 прогонам для макробенча (30+ для короткого микробенча) с явным разбросом (стандартное отклонение или 95% CI). Mean — неверная центральная статистика, потому что доминируется выбросами и прячет хвост; median робастен, а p99 ловит ощущаемый пользователем хвост. Выигрыш реален, только если улучшился median И не регрессировали p95/p99." },
    { q: "Что hardware performance counters добавляют поверх flame graph и как IPC меняет выбираемый фикс?", a: "Flame graph называет, какая функция на CPU; он не говорит, что CPU делает внутри неё. Counters раскрывают стоимость на уровне кремния. IPC (instructions per cycle) — ключевое чтение: IPC ниже ~1.0 с высоким cache-miss rate означает, что функция memory-stalled — CPU простаивает в ожидании RAM, поэтому рычаг — data layout (меньшие структуры, struct-of-arrays, cache-friendly обход, prefetch), а не алгоритм. IPC выше ~2.5 с низкими cache-miss означает compute-bound — рычаг векторизация/SIMD или более умный алгоритм. Flame graph показывает memory-bound и compute-bound функцию идентичными широкими листьями; только counters раскрывают, какой фикс реально её сдвинет. Классическая ловушка — поставить более быстрый парсер для memory-stalled функции и не увидеть улучшения." },
  ]}
/>

<Recap lang="ru">
Если ты смог восстановить каждый ответ по памяти, ты держишь хребет юнита: измерительный цикл — это каркас (воспроизведи → baseline → прочитай → гипотеза → фикс → дифф → отгрузи); потолок Amdahl решает, стоит ли фикс (доля бьёт локальный speedup); self vs cum-time говорит, куда смотреть; observer effect говорит, какому профайлеру верить; статистические baseline говорят, реален ли выигрыш; а hardware counters говорят, почему функция горячая и какой фикс брать. Измерение над интуицией, каждый раз.
</Recap>
