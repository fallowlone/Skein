---
concepts:
- amdahl-law
- self-vs-cum-time
- measurement-scopes
- observer-effect
- statistical-baseline
- flame-graph-shapes
estMin: 13
lang: ru
level: senior
order: 7
prereqs: []
slug: quiz-choice
sources:
- https://dl.acm.org/doi/10.1145/356635.356640
- https://www.brendangregg.com/methodology.html
- https://en.wikipedia.org/wiki/Amdahl%27s_law
status: ready
summary: Тест с выбором на синтез по всему юниту — потолки Amdahl, self vs cum-time, scope измерений, observer effect, статистические baseline и чтение форм flame graph.
title: 'Сначала профиль: тест с выбором ответа'
track: performance
unit: 01-profile-first
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Шесть вопросов поперёк всего юнита. Каждый отражает решение, которое ты принимаешь в реальном расследовании «сервис тормозит» — не определение для заучивания, а суждение, которое надо защитить числами.
</Hook>

<Goal lang="ru">
Убедись, что связываешь измерительный цикл, потолок Amdahl, выбор scope, observer effect, статистическую отчётность и чтение flame graph — тот синтез, к которому вели отдельные уроки.
</Goal>

<Quiz
  id="01-profile-first-choice-1"
  lessonSlug="01-profile-first"
  lang="ru"
  question="Профиль показывает топ-функцию на 12% общего CPU. Коллега предлагает героический рерайт, который сделает её в 8x быстрее. Прежде чем одобрить — какое число решающее и что оно говорит?"
  choices={[
    { label: "Потолок Amdahl: 1 / ((1 − 0.12) + 0.12/8) ≈ 1.12x. Даже локальный выигрыш 8x ограничивает общий speedup примерно 12% — скорее всего ниже планки для рискованного рерайта. Решает ДОЛЯ функции, а не её локальный speedup.", correct: true },
    { label: "Локальный 8x — выигрыш 8x на самой горячей функции всегда стоит отгрузить.", misconception: "Локальный speedup бессмыслен без доли. Amdahl ограничивает общее улучшение через p; 8x на 12%-функции даёт ~12% суммарно, а не 8x." },
    { label: "Пока ничего измеримого — надо отгрузить и смотреть production p99, чтобы узнать.", misconception: "Amdahl позволяет предсказать потолок до единой строки кода. Отгрузить рискованный рерайт, чтобы обнаружить потолок 1.12x, который можно было посчитать — ровно та трата, которую предотвращает profile-first." },
    { label: "Self-time функции — если self-time высокий, рерайт оправдан независимо от доли.", misconception: "Self-time говорит ГДЕ работа, а не стоит ли выигрыш. Функция может быть на 100% self-time и при этом всего 12% от общего — Amdahl всё равно ограничивает выигрыш ~12%." },
  ]}
/>

<Quiz
  id="01-profile-first-choice-2"
  lessonSlug="01-profile-first"
  lang="ru"
  question="Flame graph показывает одну функцию с cum-time 80%, но self-time 2%, над единственным широким листом. Что это и где живёт фикс?"
  choices={[
    { label: "Это routing/dispatch-фрейм — его собственный код стоит почти ничего (2% self), а 80% принадлежат вызываемому ниже. Спускайся к широкому листу с большим self-time; фикс живёт там, а не в диспетчере.", correct: true },
    { label: "Это и есть боттлнек — 80% cum-time означает, что рерайт этой функции — фикс с наибольшим рычагом.", misconception: "Cum-time включает вызываемых. С self-time 2% собственные инструкции функции почти ничего не стоят; рерайт сэкономит максимум 2%. Реальная стоимость — в том, что она вызывает." },
    { label: "Профайлер обсчитался — для настоящей горячей точки cum и self должны быть близки.", misconception: "Огромный cum-time с крошечным self-time — нормальная, ожидаемая сигнатура HTTP-хендлеров, middleware и dispatch-слоёв. Это инструкция к чтению, а не ошибка." },
    { label: "Горизонтальная ширина означает, что она выполнилась первой — чини самый ранний код.", misconception: "X-позиция в flame graph — алфавитная группировка, а не порядок во времени. Слева-справа ничего не говорит о последовательности выполнения; ширина — это число сэмплов." },
  ]}
/>

<Quiz
  id="01-profile-first-choice-3"
  lessonSlug="01-profile-first"
  lang="ru"
  question="Микробенчмарк говорит, что новый хеш в 10x быстрее. Ты хочешь узнать, станет ли быстрее твой API. Какое измерение честно отвечает на это и почему?"
  choices={[
    { label: "Production CPU-профиль, чтобы измерить ДОЛЮ (p) хеширования в общем времени. Микробенч даёт локальный speedup s; только профиль даёт p, а Amdahl нужны оба, чтобы предсказать выигрыш API до отгрузки.", correct: true },
    { label: "Прогони микробенчмарк 1000x и сообщи mean — высокое число итераций делает его репрезентативным для production.", misconception: "Итерации чинят дисперсию, а не scope. Микробенч измеряет функцию в изоляции и никогда не скажет её долю в wall-clock API — для этого нужен production-профиль." },
    { label: "Доверься 10x и отгружай — выигрыш 10x на типичном боттлнеке всегда двигает API.", misconception: "Если хеширование — 3% времени API, Amdahl ограничивает выигрыш ~1.03x — невидимо пользователям. Рассуждение по категории («хеширование — типичный боттлнек») вместо измерения доли — первородный грех, который предотвращает profile-first." },
    { label: "Staging-макробенчмарк сам по себе — честный ответ.", misconception: "Макробенч валидирует end-to-end, но зависит от того, совпадает ли синтетическая нагрузка с реальным трафиком; это финальная проверка, а не измерение доли. Честный источник p — production-профиль." },
  ]}
/>

<Quiz
  id="01-profile-first-choice-4"
  lessonSlug="01-profile-first"
  lang="ru"
  question="Instrumentation-профайлер сообщает горячий Java-метод как 40 мс/вызов; sampling-профайлер — 8 мс/вызов для того же метода. Какой отражает production и что вызывает разрыв?"
  choices={[
    { label: "Sampling-профайлер (~8 мс). Instrumentation оборачивает каждый entry/exit (20–100% overhead в JIT-рантаймах) и блокирует инлайнинг, который применил бы production JIT, поэтому раздувает и искажает тайминг. Sampling возмущает меньше 2% и остаётся репрезентативным.", correct: true },
    { label: "Instrumentation-профайлер — он точно измеряет каждый вызов, значит, это ground truth.", misconception: "Точный счёт вызовов помогает, только когда стоимость измерения пренебрежима. В JIT-рантайме instrumentation отключает инлайнинг и добавляет 20–100% overhead, поэтому измеряет другую (более медленную) программу, чем работает в production." },
    { label: "Оба одинаково валидны — просто сообщают разные единицы.", misconception: "Тот же метод, те же единицы (мс/вызов), дико разные числа из-за разного уровня возмущения. Observer effect заставляет их измерять разные системы; репрезентативен для production sampling." },
    { label: "Ни тот, ни другой — для корректного тайминга метода нужны hardware counters.", misconception: "Hardware counters объясняют ПОЧЕМУ функция горячая (memory- vs compute-bound); они не разрешают разрыв из-за instrumentation-overhead. Это делает sampling." },
  ]}
/>

<Quiz
  id="01-profile-first-choice-5"
  lessonSlug="01-profile-first"
  lang="ru"
  question="PR заявляет «на 15% быстрее» по одному staging-прогону. Второй инженер перезапускает тот же неизменённый код и видит разброс 7%. Как читать исходное заявление?"
  choices={[
    { label: "Неотличимо от шума. Естественный пол шума от прогона к прогону — ~5–10% (тайминг GC, планировщик, прогрев кэша, contention). 15% по одному прогону — не измерение; требуй median + p95 + p99 минимум по 5 прогонам с доверительным интервалом.", correct: true },
    { label: "15% реальны — staging это контролируемая среда, поэтому её одиночный прогон надёжен.", misconception: "Контролируемая не значит без шума. В staging всё равно есть паузы GC, решения планировщика и вариация состояния кэша, дающие разброс 5–10% между идентичными прогонами, как только что показал перезапуск." },
    { label: "Сообщи mean по двум прогонам (≈11%) и отгружай.", misconception: "Два прогона не задают распределение, а mean — неверная статистика: он доминируется выбросами и прячет хвост. Нужны median, p95/p99 и CI минимум по 5 прогонам." },
    { label: "Разброс 7% при перезапуске доказывает, что изменение — регрессия.", misconception: "Разброс доказывает, что измерение шумное, а не что изменение плохое. Ни один прогон не надёжен в одиночку; нельзя судить о знаке или величине без распределения." },
  ]}
/>

<Quiz
  id="01-profile-first-choice-6"
  lessonSlug="01-profile-first"
  lang="ru"
  question="Flame graph показывает тонкие пики, разбросанные по всей ширине, без доминирующего листа, но p99 ужасный. Что говорит форма и что снимать дальше?"
  choices={[
    { label: "CPU-хотспота нет — задержка off-CPU (ожидание I/O, локов, GC или сети). CPU-профиль это не объяснит; снимай off-CPU / block / wait профиль (например, eBPF off-CPU или block-профиль в Go) и диффай его.", correct: true },
    { label: "Профайлер сломан — настоящий боттлнек всегда даёт широкий лист.", misconception: "Тонкие разбросанные пики — узнаваемая, валидная форма, означающая, что время не на CPU. Это диагноз, а не поломка." },
    { label: "Сервис уже оптимален — плоский CPU значит, чинить нечего.", misconception: "Плоский CPU значит, что боттлнек off-CPU, а не отсутствует. С плохим p99 время куда-то уходит — в ожидание, к которому CPU-профиль слеп." },
    { label: "Подними частоту сэмплинга, пока хотспот не появится.", misconception: "Бо́льшая частота отрисует ту же разбросанную форму плотнее; она не консолидирует off-CPU время в CPU-хотспот, потому что это время никогда не было на CPU." },
  ]}
/>

<Recap lang="ru">
Сквозная линия юнита — одно расследование: квантифицируй жалобу, воспроизведи под реалистичной нагрузкой, сними baseline, читай ФОРМУ прежде имён и дай числам — не интуиции — назвать горячую точку. Потолок Amdahl решает, стоит ли фикс (доля важнее локального speedup); self vs cum-time решает, куда смотреть; выбор scope и observer effect решают, какому измерению верить; статистические baseline решают, реален ли «выигрыш»; а разбросанный flame graph говорит, что боттлнек off-CPU. Каждый неверный ответ выше — реальный способ, которым инженеры обманывают себя, оптимизируя не тот код.
</Recap>
