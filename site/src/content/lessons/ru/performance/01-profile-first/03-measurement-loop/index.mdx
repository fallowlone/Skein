---
concepts:
- microbenchmark
- macrobenchmark
- production-profile
- observer-effect
- baseline
- statistical-validity
deepensInto: []
estMin: 18
lang: ru
lessonType: topic
level: middle
order: 3
prereqs:
- performance/01-profile-first/02-amdahl-and-self-time
slug: 03-measurement-loop
sources:
- https://www.brendangregg.com/methodology.html
- https://www.brendangregg.com/systems-performance-2nd-edition-book.html
- https://pkg.go.dev/runtime/pprof
spiral:
- latency
status: ready
summary: 'Три области измерения лгут при неправильном применении. Эффект наблюдателя:
  профайлер возмущает систему — знай насколько.'
title: 'Измерительный цикл: микробенч, макробенч, prod-профиль, эффект наблюдателя'
track: performance
unit: 01-profile-first
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import TraceScenario from "~/components/pedagogy/TraceScenario.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
Инженер переписывает горячую функцию на ассемблере. Микробенчмарк показывает 8x ускорение. Задеплоенный сервис стал быстрее на 3%. Production-профиль предсказал бы этот результат за 60 секунд — до переписывания.
</Hook>

<Crux>Микробенч, макробенч, prod-профиль отвечают на разные вопросы и лгут при неправильном применении. Выбирай область под вопрос.</Crux>

<Explanation>

## Три области измерения

**Микробенчмарк** — одна функция в изоляции, без нагрузки, без I/O, повторяется миллионы раз.
Инструменты: `testing.B` в Go, JMH для Java, Criterion для Rust, Benchmark.js.
Лучший для: сравнения двух реализаций одного примитива (какая хэш-функция быстрее?).
Лжёт когда: используется для предсказания задержки на уровне приложения — функция может занимать 4% production-времени вне зависимости от её изолированной скорости.

**Макробенчмарк** — полная система под синтетической нагрузкой, имитирующей продакшен.
Инструменты: k6, JMeter, locust, vegeta.
Лучший для: валидации end-to-end поведения перед выкаткой.
Лжёт когда: синтетическая нагрузка не отражает реальный трафик (микс запросов, размеры тел, состояние кэша).

**Production-профиль** — непрерывное профилирование с накладными расходами 2-5%, захватывающее реальный трафик, реальное состояние кэша, реальный параллелизм.
Лучший для: честного поиска следующего узкого места.
Ограничение: можно запускать только в продакшене, поэтому он не ответит «поможет ли это изменение» до выкатки.

Рабочий процесс сеньора использует все три: микробенч для сравнения альтернатив, макробенч для end-to-end валидации, prod-профиль для поиска следующей цели.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Область</th>
        <th class="border border-border px-3 py-2 text-left">Правильный вопрос</th>
        <th class="border border-border px-3 py-2 text-left">Неверный вопрос</th>
        <th class="border border-border px-3 py-2 text-left">Ключая ловушка</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">Микробенч</td>
        <td class="border border-border px-3 py-2">Реализация A быстрее реализации B?</td>
        <td class="border border-border px-3 py-2">Ускорит ли это наш API?</td>
        <td class="border border-border px-3 py-2">JIT warmup, dead-code elimination, кэш слишком тёплый</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Макробенч</td>
        <td class="border border-border px-3 py-2">Система выдерживает SLO под нагрузкой?</td>
        <td class="border border-border px-3 py-2">Что реально происходит в продакшене?</td>
        <td class="border border-border px-3 py-2">Синтетическая нагрузка может не совпадать с реальной формой трафика</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Prod-профиль</td>
        <td class="border border-border px-3 py-2">Каково следующее узкое место?</td>
        <td class="border border-border px-3 py-2">Поможет ли кандидатный фикс?</td>
        <td class="border border-border px-3 py-2">Нельзя предсказать до выкатки; показывает только текущее состояние</td>
      </tr>
    </tbody>
  </table>
</div>

## Эффект наблюдателя

Каждое измерение возмущает систему.

Сэмплирующий профайлер на 100 Гц добавляет 0.5-2% накладных расходов от обхода стеков. На 1000 Гц — до 10%. Инструментирующие профайлеры (таймит каждый вход/выход из функции) добавляют 20-100% в JIT-рантаймах, потому что оптимизатор больше не может инлайнить теперь обёрнутые функции. Debug-сборка с хуками профайлера ведёт себя совсем не как release-сборка.

Следствие: выбирай измерение, которое минимально возмущает систему для данного вопроса. Используй сэмплирующие профайлеры для продакшена. Используй инструментирующие профайлеры для плотных микробенчмарков, где важны точные счётчики вызовов.

Всегда измеряй дважды: один раз без профайлера, один — с ним, и проверь, что основная метрика (throughput, latency p99) отличается не более чем на 5%. Если отличается больше — профайлер наблюдает другую систему, чем та, что работает в продакшене.

## Полный измерительный цикл детально

«Сначала профиль» — не разовое действие, а повторяющийся цикл.

1. **Воспроизведи** медленный сценарий под реалистичной нагрузкой — replay production-трафика, нагрузочный тест на staging или живая канарейка.
2. **Сними baseline-профиль** достаточной длительности для статистической значимости (30 секунд при 100 Гц дают 3000 сэмплов — типичный минимум).
3. **Прочитай профиль** — назови hotspot конкретными цифрами: «функция X потребляет 38% CPU; вызывается по пути Y; средний self-time 220 мс на запрос».
4. **Сформулируй одну гипотезу** о фиксе и предскажи ожидаемое ускорение по закону Амдала.
5. **Примени фикс изолированно** — только то изменение, которое предсказал.
6. **Сними новый профиль** под той же нагрузкой и сравни с baseline; убедись, что hotspot уменьшился на предсказанную величину.
7. **Выкати и наблюдай** продакшен. Пропуск любого шага возвращает цикл в режим угадывания.

<Inset kind="why" lang="ru">
«Профилировать продакшен слишком дорого» — это отговорка, которая обходится дороже, чем само профилирование. Сэмплирующий профайлер на 100 Гц добавляет менее 2% нагрузки на CPU. Узкое место, которое он найдёт за первые пять минут production-трафика, стоит на порядки больше, чем те 2%, которые ушли на его поиск.
</Inset>

<TraceScenario
  id="mid-trace-1"
  pieceSlug="03-measurement-loop"
  lang="ru"
  scenario="Команде сообщили, что checkout API медленный. Прокрути profile-first ответ от начала до конца."
  steps={[
    { prompt: "Шаг 1: «медленный» — не измерение. Что делаешь первым?", reveal: "Определяешь метрику и цель. «p99 checkout-complete сейчас 1.8с; SLO требует 500мс». Без цифр не с чем сравнивать потом, а «теперь кажется быстрее» не задеплоишь." },
    { prompt: "Шаг 2: цифры есть — что дальше?", reveal: "Воспроизводишь медленный случай под реалистичной нагрузкой — replay production-трафика на staging или снимаешь живой профиль с канарейки. Микробенчмарки подозреваемых функций здесь преждевременны: ты ещё не знаешь, какие функции подозревать." },
    { prompt: "Шаг 3: профиль показывает 70% CPU в JSON-сериализации внутри кастомного audit-логгера. Интуиция команды — база. Что теперь?", reveal: "Доверяешь профилю. Гипотеза о базе мертва — измерение опровергло интуицию. Формируешь новую гипотезу: замена сериализатора логгера или перевод его в async должна сэкономить ~70% CPU на запрос. Предсказываешь ожидаемое падение p99 по Амдалу: 1 / (1 - 0.7) = 3.3x потолок, реально ~2.5x." },
    { prompt: "Шаг 4: изменение применено, новый профиль снят. Что проверяешь?", reveal: "Сравниваешь с baseline. Фрейм audit-логгера должен занимать ≤10% от исходной ширины. Если нет — изменение не приземлилось туда, куда ты думал; проверь деплой. Если да — p99 должно быть близко к предсказанному значению. Если p99 не изменился несмотря на изменение профиля — что-то другое занимает освободившееся место: исследуй новый топовый фрейм." },
    { prompt: "Шаг 5: rollout в продакшен. За чем наблюдаешь?", reveal: "Метрики продакшена: p99, p95, p50 checkout-complete плюс error rate. Изменение, снизившее задержку ценой поломки audit-лога — это регрессия, а не победа. Наблюдай минимум два дня. Сравни с SLO-целью." },
  ]}
/>

<Quiz
  id="03-measurement-loop-quiz-1"
  lessonSlug="03-measurement-loop"
  lang="ru"
  question="Команда профилирует только на staging (синтетическая нагрузка), никогда в продакшене. После деплоя perf-улучшения p99 в продакшене стал хуже. Наиболее вероятное системное объяснение?"
  choices={[
    { label: "Нагрузка на staging не соответствует форме production-трафика (микс запросов, размеры тел, hit rate кэша, конкурентность). Оптимизация помогла staging-нагрузке и навредила production.", correct: true },
    { label: "В продакшене есть баг, которого нет на staging", misconception: "Возможно, но конкретно; системный ответ — несоответствие нагрузки, а не конкретный баг." },
    { label: "Деплой был сломан", misconception: "Не объясняет паттерн «на staging всё хорошо, в продакшене хуже». Несоответствие нагрузки объясняет." },
    { label: "Профайлеры ненадёжны", misconception: "Профайлеры измеряют то, что перед ними. Проблема в том, что staging — не продакшен." },
  ]}
/>

<Quiz
  id="03-measurement-loop-quiz-2"
  lessonSlug="03-measurement-loop"
  lang="ru"
  question="Инструментирующий профайлер (таймит каждый вход/выход функции) показывает, что функция занимает 40 мс. Сэмплирующий профайлер показывает 8 мс. Какой с большей вероятностью отражает production-поведение?"
  choices={[
    { label: "Сэмплирующий — накладные расходы инструментирования (20-100% в JIT-рантаймах) раздувают тайминги и отключают инлайнинг, который применял бы production JIT.", correct: true },
    { label: "Инструментирующий — он измеряет каждый вызов точно.", misconception: "Точный счётчик вызовов полезен только когда цена измерения пренебрежимо мала; в JIT-рантаймах это не так." },
    { label: "Оба одинаково валидны", misconception: "Они измеряют разные вещи при разных уровнях возмущения; для production-репрезентативной цены побеждает сэмплирующий." },
    { label: "Ни тот, ни другой; нужны hardware counters", misconception: "Hardware counters полезны для микроархитектурного анализа, но не решают проблему накладных расходов инструментирования." },
  ]}
/>

<DragOrder
  id="03-measurement-loop-drag-1"
  lessonSlug="03-measurement-loop"
  lang="ru"
  prompt="Расставь шаги расследования жалобы «сервис медленный» по порядку — от первого до последнего:"
  items={[
    { id: "a", label: "Квантифицируй жалобу: выбери метрику (p99 задержки, throughput, error rate) и целевое значение" },
    { id: "b", label: "Воспроизведи под реалистичной нагрузкой при видимой метрике (живая канарейка, staging replay)" },
    { id: "c", label: "Сними baseline-профиль достаточной длительности для статистической значимости" },
    { id: "d", label: "Прочитай профиль: назови топовую функцию по self-time и доле CPU" },
    { id: "e", label: "Вычисли потолок Амдала для фикса этой функции — реши, оправдана ли победа" },
    { id: "f", label: "Сформулируй одну гипотезу фикса, предскажи суммарное ускорение, примени только это изменение" },
    { id: "g", label: "Сними новый профиль под той же нагрузкой и сравни с baseline" },
    { id: "h", label: "Выкати на канарейку, потом в продакшен, наблюдая за метрикой на устойчивое улучшение" },
  ]}
  correctOrder={["a", "b", "c", "d", "e", "f", "g", "h"]}
/>

</Explanation>

<FlowDiagram
  label="Дисциплинированный измерительный цикл: сними baseline, измени одну вещь, измерь под той же нагрузкой, сравни, потом оставь или откати"
  hue="var(--d-hardware)"
  perRow={4}
  nodes={[
    { id: "baseline", label: "Baseline", sub: "измерь первым" },
    { id: "change", label: "Измени одно", sub: "изолируй" },
    { id: "measure", label: "Измерь", sub: "та же нагрузка" },
    { id: "compare", label: "Сравни", sub: "diff baseline" },
    { id: "keep", label: "Оставь / откати", sub: "реши" },
  ]}
  edges={[
    { from: "baseline", to: "change" },
    { from: "change", to: "measure" },
    { from: "measure", to: "compare" },
    { from: "compare", to: "keep" },
    { from: "keep", to: "baseline", label: "новый baseline" },
  ]}
  caption="Меняй ровно одну вещь за проход, чтобы diff был атрибутируемым; оставленный результат становится следующим baseline."
/>

<KeyTakeaway>Микробенч сравнивает реализации; макробенч валидирует end-to-end; prod-профиль находит следующую цель. Эффект наблюдателя: накладные расходы профайлера меняют то, что ты измеряешь — убедись, что основная метрика отличается не более чем на 5% с профайлером и без.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="03-measurement-loop-retrieval"
  lang="ru"
  questions={[
    { q: "Инженер переписывает горячую функцию на ассемблере. Микробенч показывает 8x. Задеплоенный сервис: 3% быстрее. Проведи диагностику.", a: "Проверь, какую долю общего времени запроса занимала эта функция в production-профиле. Если 4% общего времени, Амдал ограничивает ускорение приложения примерно до 1 / (1 - 0.04 × (1 - 1/8)) ≈ 1.04x — именно то, что наблюдается. Микробенч честно измерил функцию в изоляции; функция занимала малую долю общей работы. Корректирующий рабочий процесс: повторно профилируй продакшен, найди функции с долей ≥10%, оцени, у какой больше пространства для ускорения, сформулируй гипотезу с предсказанием по Амдалу, потом оптимизируй." },
    { q: "Почему нужно всегда проверять, что накладные расходы профайлера укладываются в 5% от базовой основной метрики, прежде чем доверять профилю?", a: "Профайлер возмущает систему, которую измеряет. При 100 Гц сэмплирующий профайлер добавляет 0.5-2% накладных расходов — допустимо. Инструментирующий профайлер может добавить 20-100% в JIT-рантаймах, потому что оборачивает каждый вход/выход функции, лишая оптимизатор возможности инлайнинга. Если задержка или throughput с профайлером отличаются от без него более чем на 5%, профайлер наблюдает другую систему, чем та, что работает в продакшене. Hotspots в профиле могут быть артефактами измерения, а не реальными production-ценами. Всегда снимай baseline-измерение без профайлера, потом подтверждай, что запуск с профайлером достаточно близок для доверия." },
  ]}
/>

<Recap lang="ru">
Три области измерения существуют потому, что каждая честно отвечает на разный вопрос и по-разному лжёт. Микробенч изолирует две реализации, но не может предсказать production-долю. Макробенч валидирует end-to-end, но зависит от того, насколько синтетическая нагрузка совпадает с реальным трафиком. Production-профиль — единственное по-настоящему честное измерение того, что медленно прямо сейчас, но не может предсказать влияние не выкаченного изменения. Рабочий процесс сеньора сцепляет все три. Эффект наблюдателя: каждый профайлер возмущает систему: сэмплирующие добавляют менее 2%, инструментирующие — 20-100% в JIT-рантаймах. Всегда проверяй, что основная метрика остаётся в пределах 5% при работающем профайлере.
</Recap>
