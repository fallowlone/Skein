---
slug: 05-statistical-baselines
lang: ru
track: performance
unit: 01-profile-first
order: 5
title: "Статистические baseline'ы: почему один запуск — не измерение"
summary: "Один прогон неотличим от шума. Репортируй медиану, p95, p99 по ≥5 прогонам с ДИ — иначе догадка в числовой одежде."
estMin: 12
status: ready
lessonType: topic
level: middle
prereqs: ["performance/01-profile-first/03-measurement-loop"]
deepensInto: []
spiral: ["latency"]
concepts: ["statistical-baseline", "noise", "median", "p99", "confidence-interval", "performance-reporting"]
sources:
  - https://www.brendangregg.com/methodology.html
  - https://www.brendangregg.com/systems-performance-2nd-edition-book.html
  - https://en.wikipedia.org/wiki/Amdahl%27s_law
---

import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
«Этот PR делает сервис на 15% быстрее» — основано на одном прогоне на staging, добавлено к ревью. Это реально? Уровень шума в большинстве систем — 5-10% между идентичными прогонами. Без большего числа прогонов и перцентилей не понять.
</Hook>

<Crux>Один прогон не отличает сигнал от шума. Репортируй медиану + p95 + p99 по ≥5 прогонам — всё меньше — фикция.</Crux>

<Explanation>

## Почему один прогон ненадёжен

Один и тот же сценарий, запущенный дважды, может отличаться на 5-10% из-за причин, не связанных с кодом:

- Временны́е отличия GC (окна stop-the-world смещаются между прогонами)
- Решения OS-планировщика (какое CPU-ядро, когда вытеснен)
- Состояние кэша на старте прогона (L3 холодный vs тёплый)
- Временные конкурентные обращения к сети или диску
- Масштабирование частоты CPU на ноутбуках (троттлинг при тепловой нагрузке)

«15%-ное ускорение» из одного прогона не отличить от этого шума. Это может быть реальная победа, может быть совпадение — не скажешь.

## Что репортировать вместо этого

- **Минимальное число прогонов**: 5-10 для макробенча, 30+ для короткого микробенча.
- **Репортируй**: медиану, p95 и p99 — не среднее. Среднее захвачено выбросами; медиана устойчива к ним.
- **Укажи разброс**: стандартное отклонение или доверительный интервал.
- **Как выглядит защищаемый результат**: «p99 задержки упал с 480 мс до 320 мс в пяти 5-минутных прогонах, p50 с 60 мс до 55 мс, с 95% ДИ ±15 мс на каждое измерение».

«10%-ное ускорение» реально только если медиана улучшилась на ≥10% И p95/p99 не деградировали.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Как репортируют</th>
        <th class="border border-border px-3 py-2 text-left">Проблема</th>
        <th class="border border-border px-3 py-2 text-left">Лучшая альтернатива</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">Один прогон, одно число</td>
        <td class="border border-border px-3 py-2">Неотличимо от 5-10% естественного шума</td>
        <td class="border border-border px-3 py-2">5+ прогонов, репортировать медиану + p99 + ДИ</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Средняя задержка</td>
        <td class="border border-border px-3 py-2">Один выброс искажает среднее</td>
        <td class="border border-border px-3 py-2">Медиана + перцентили p95/p99</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Только измерения на staging</td>
        <td class="border border-border px-3 py-2">Синтетическая нагрузка может не совпадать с production</td>
        <td class="border border-border px-3 py-2">Staging + подтверждение production-канарейкой</td>
      </tr>
    </tbody>
  </table>
</div>

## Почему среднее — неверная метрика

Распределения задержек имеют длинный хвост. Сервис, обрабатывающий 99% запросов за 50 мс, а 1% — за 2000 мс, имеет среднее около 70 мс — которое скрывает хвост в 2000 мс, испытываемый пользователями. Среднее захвачено выбросами в любую сторону. Медиана (p50) устойчива к выбросам; p99 описывает пользовательский опыт на хвосте.

Дисциплина: репортируй числа как учёный — с распределениями и неопределённостью, а не как маркетолог — с единственным процентным показателем.

<Inset kind="why" lang="ru">
Дисциплина оправдывается больше всего, когда заявленная «победа» оказывается шумом. Команда, поймавшая шумовую победу до мерджа, экономит: churn от реверта «регрессии» позже, когда продакшен не показывает улучшения, время на постмортем деплоя, который ничего не сделал, и утрату доверия, когда команда узнаёт, что perf-цифрам без методологии нельзя верить.
</Inset>

<Quiz
  id="05-statistical-baselines-quiz-1"
  lessonSlug="05-statistical-baselines"
  lang="ru"
  question="Команда репортирует «это изменение сделало сервис на 15% быстрее» на основе одного прогона на staging. Какой самый важный уточняющий вопрос?"
  choices={[
    { label: "По скольким прогонам и на каком перцентиле? Один прогон не может отличить реальную победу на 15% от естественного шума (обычно 5-10% между прогонами).", correct: true },
    { label: "Вы уже задеплоили?", misconception: "Операционно важно, но не валидирует само утверждение." },
    { label: "Вы написали тесты?", misconception: "Корректность — отдельная ось; вопрос здесь в том, статистически ли обосновано perf-утверждение." },
    { label: "Вы задокументировали изменение?", misconception: "Документация не зависит от статистической валидности." },
  ]}
/>

<Quiz
  id="05-statistical-baselines-quiz-2"
  lessonSlug="05-statistical-baselines"
  lang="ru"
  question="Почему средняя задержка — неверная метрика для репортирования производительности сервиса?"
  choices={[
    { label: "Среднее захвачено выбросами — один запрос на 2000 мс может сильно сдвинуть среднее от типичного опыта, тогда как медиана и p99 точно описывают распределение.", correct: true },
    { label: "Среднее слишком долго вычислять", misconception: "Среднее вычислительно тривиально; проблема статистическая, не вычислительная." },
    { label: "Перцентили требуются спецификацией SLO", misconception: "Причина использовать перцентили — статистическая валидность, а не соответствие спецификации." },
    { label: "Среднее работает только для нормально распределённых данных", misconception: "Технически верно, но не инженерная причина; более глубокая проблема в том, что хвосты невидимы в среднем." },
  ]}
/>

<Quiz
  id="05-statistical-baselines-quiz-3"
  lessonSlug="05-statistical-baselines"
  lang="ru"
  question="Ты запускаешь микробенчмарк 1000 раз и вычисляешь среднее. Оно показывает улучшение на 12%. Какая дополнительная информация нужна, чтобы утверждать, что улучшение реально?"
  choices={[
    { label: "Стандартное отклонение или доверительный интервал, а также не деградировал ли p99 — улучшение среднего на 12% при высокой дисперсии и регрессии p99 может быть чистым минусом.", correct: true },
    { label: "Больше итераций, пока среднее не сойдётся", misconception: "Больше итераций снижает дисперсию, но форма распределения важна так же, как сходимость." },
    { label: "Улучшение в абсолютном времени, а не в процентах", misconception: "Абсолютное vs относительное — выбор представления; это не решает вопрос статистической валидности." },
    { label: "Ничего; 1000 итераций всегда достаточно", misconception: "Только количество сэмплов не говорит о разбросе; плотный ДИ требует достаточного числа сэмплов И низкой дисперсии." },
  ]}
/>

</Explanation>

<KeyTakeaway>Никогда не репортируй одно число производительности. Репортируй медиану + p95 + p99 по ≥5 прогонам с доверительными интервалами. Один прогон неотличим от уровня естественного шума 5-10%; всё меньше распределения — догадка в числовой одежде.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="05-statistical-baselines-retrieval"
  lang="ru"
  questions={[
    { q: "Почему репортирование единственного «X% ускорения» из одного прогона бенчмарка — красный флаг, и что следует репортировать вместо этого?", a: "Метрики производительности по природе зашумлены. Два последовательных прогона одной нагрузки обычно отличаются на 5-10% из-за причин, не связанных с кодом: временны́е отличия GC, решения OS-планировщика, состояние кэша на старте, временные конкурентные обращения к сети или диску. «15%-ное ускорение» из одного прогона не отличить от этого шума. Что следует репортировать: медиану, p95 и p99 по нескольким прогонам (5-10 для макробенча, 30+ для короткого микробенча) с явно указанным разбросом (стандартным отклонением или доверительным интервалом). Защищаемый результат выглядит так: «медианная задержка упала с 240 мс до 195 мс в восьми 5-минутных прогонах, p99 с 820 мс до 640 мс, с 95% ДИ ±10 мс на каждое измерение»." },
    { q: "Назови четыре основных источника шума в измерениях производительности и какие из них контролируемы.", a: "GC-таймирование: сборщик мусора может делать паузы в разное время между прогонами; контролируется достаточным числом сэмплов, чтобы паузы GC усреднились, или принудительным запуском GC перед каждым прогоном. OS-планировщик: таймирование вытеснения потоков варьируется; частично контролируется привязкой к конкретным CPU и использованием высокоприоритетных процессов. Состояние кэша: тепло L3-кэша на старте прогона варьируется; контролируется прогревочным проходом перед измерением. Временные конкурентные обращения: сетевая или дисковая активность от других процессов; частично контролируется изолированным железом, но production-измерения не могут полностью исключить это. Статистический подход: набирать достаточно прогонов, чтобы эти эффекты усреднились и вошли в доверительный интервал." },
  ]}
/>

<Recap lang="ru">
Измерения производительности — распределения, а не отдельные числа. Уровень естественного шума — 5-10% между идентичными прогонами одного кода, из-за GC-таймирования, OS-планировщика, состояния кэша и временных конкурентных обращений. «15%-ное ускорение» из одного прогона статистически неотличимо от шума. Защищаемые репорты требуют медианы, p95 и p99 минимум по пяти прогонам с явными доверительными интервалами. Средняя задержка — неверная метрика, потому что она захвачена выбросами; медиана и хвостовые перцентили правильно описывают пользовательский опыт. Дисциплина репортирования диапазонов вместо отдельных значений выявляет шумовые победы до мерджа и строит доверие команды к perf-утверждениям.
</Recap>
