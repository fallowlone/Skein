---
concepts:
- amdahl-law
- self-time
- cumulative-time
- wall-clock-time
- cpu-time
- bottleneck
deepensInto: []
estMin: 16
lang: ru
lessonType: topic
level: middle
order: 2
prereqs:
- performance/01-profile-first/01-why-profile-first
slug: 02-amdahl-and-self-time
sources:
- https://dl.acm.org/doi/10.1145/356635.356640
- https://en.wikipedia.org/wiki/Amdahl%27s_law
- https://www.brendangregg.com/systems-performance-2nd-edition-book.html
spiral:
- latency
status: ready
summary: 'Закон Амдала даёт потолок любого ускорения. Self-time vs cum-time говорит:
  чинить саму функцию или спускаться к её callees.'
title: 'Закон Амдала и self-time: потолок любого ускорения, которое ты можешь выпустить'
track: performance
unit: 01-profile-first
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import FadedExample from "~/components/pedagogy/FadedExample.tsx";
import NumbersCard from "~/components/layout/NumbersCard.astro";
import Misconception from "~/components/layout/Misconception.astro";

<Hook>
Команда тратит три недели на переписывание хэш-функции с локальным ускорением в 10x. Задеплоенный сервис стал быстрее на 1.05x. Закон Амдала дал бы ответ до начала работы — если бы сначала проверили профиль.
</Hook>

<Crux>Амдал задаёт потолок любого ускорения. Профиль даёт p — горячую долю. Без p уравнение нерешаемо.</Crux>

<Explanation>

## Закон Амдала: потолок любого ускорения

Если часть кода занимает долю **p** от общего времени исполнения, и ты ускоряешь её в **s** раз, суммарное ускорение приложения:

```
суммарное_ускорение = 1 / ((1 − p) + p/s)
```

Предельный случай — сделать секцию бесконечно быстрой — даёт:

```
макс_ускорение = 1 / (1 − p)
```

Функция, занимающая 10% общего времени, никогда не даст больше **1.11x** суммарного ускорения, даже если заменить её на заглушку. Функция на 80% даёт **5x**, если ускорить её вдвое.

Этот закон превращает «стоит ли оптимизировать это?» в количественный вопрос. Снял профиль, увидел, что функция X занимает 12% времени исполнения — и сразу понимаешь: даже идеальное переписывание ограничено потолком в 13.6%. Если потолок ниже твоей планки — не начинай. Если функция Y занимает 70% — двукратное ускорение Y даёт 1.54x суммарно, инженерное время оправдано.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Доля общего времени (p)</th>
        <th class="border border-border px-3 py-2 text-left">Макс. суммарное ускорение (p → 0)</th>
        <th class="border border-border px-3 py-2 text-left">Ускорение при 2x на секции</th>
      </tr>
    </thead>
    <tbody>
      <tr><td class="border border-border px-3 py-2">10%</td><td class="border border-border px-3 py-2">1.11x</td><td class="border border-border px-3 py-2">1.05x</td></tr>
      <tr class="bg-surface-2"><td class="border border-border px-3 py-2">50%</td><td class="border border-border px-3 py-2">2.00x</td><td class="border border-border px-3 py-2">1.33x</td></tr>
      <tr><td class="border border-border px-3 py-2">80%</td><td class="border border-border px-3 py-2">5.00x</td><td class="border border-border px-3 py-2">1.67x</td></tr>
      <tr class="bg-surface-2"><td class="border border-border px-3 py-2">90%</td><td class="border border-border px-3 py-2">10.0x</td><td class="border border-border px-3 py-2">1.82x</td></tr>
    </tbody>
  </table>
</div>

## Время на стене, CPU-время и self-time

Профиль сообщает несколько разных видов измерения времени, и путаница между ними — самая частая ошибка при чтении.

- **Wall-clock time** — то, что чувствует пользователь: сколько времени занял запрос от начала до конца. Запрос на 500 мс wall-clock, использовавший 50 мс CPU, на 90% состоит из ожидания (диск, сеть, локи, GC).
- **CPU time** — сколько из wall-clock было потрачено на реальные вычисления.
- **Self-time** — время, потраченное в собственном коде функции, без учёта времени в вызванных ею функциях.
- **Cumulative time (cum-time)** — включает время в вызовах.

Чтение профиля: если **cum-time большой, а self-time маленький** — функция является роутинговым/диспетчерским слоем: медленная работа происходит в том, что она вызывает; спускайся вниз. Если **self-time большой** — функция сама делает работу; там и живёт фикс.

Путаница между ними приводит к оптимизации обёртки, пока реальная цена сидит в callee — или наоборот.

<Inset kind="why" lang="ru">
«Оптимизировать 10% кода до нуля» — классический пример ловушки Амдала. Даже полное устранение этого кода даёт лишь 1.11x — ниже порога большинства команд для ломающего изменения. Профиль определяет, какие доли достаточно велики, чтобы их атаковать. Без него рискуешь потратить неделю на ускорения в 1.05x.
</Inset>

<NumbersCard
  id="card-amdahl-numbers"
  title="Закон Амдала и экономика profile-first"
  stats={[
    { label: "Макс. ускорение по Амдалу", value: "1 / (1 − p), где p = доля ускоренного кода" },
    { label: "Оптимизация 10% кода до нуля", value: "1.11x суммарного максимума" },
    { label: "Оптимизация 50% кода до нуля", value: "2.0x суммарного максимума" },
    { label: "Оптимизация 90% кода до нуля", value: "10.0x суммарного максимума" },
    { label: "Типичные накладные расходы сэмплирующего профайлера", value: "0.5-5% CPU" },
  ]}
/>

<FadedExample
  client:load
  id="fe-amdahl-1"
  pieceSlug="02-amdahl-and-self-time"
  lang="ru"
  title="Применяем закон Амдала, чтобы решить, какая оптимизация стоит усилий"
  steps={{
    solved: (
      <div class="space-y-3 text-sm">
        <p><strong>Условие.</strong> Запрос занимает 200 мс. Профиль показывает три горячих участка: функция A занимает 100 мс (50%), B — 40 мс (20%), C — 20 мс (10%). Команда может переписать A с ускорением в 2x (сэкономить 50 мс) или переписать B и C вместе с ускорением в 4x (сэкономить 45 мс). Что даёт больше?</p>
        <pre class="text-xs bg-zinc-100 p-2 rounded overflow-x-auto"><code>{`# Вариант 1: Переписать A (50% времени) с ускорением 2x
#   Новое время A = 100 мс / 2 = 50 мс
#   Новое общее  = 50 + 40 + 20 + 40 (остальное) = 150 мс
#   Ускорение    = 200 / 150 = 1.33x
#   Сэкономлено  = 50 мс (25%)

# Вариант 2: Переписать B и C (30% времени) с ускорением 4x
#   Новое время B = 40 мс / 4 = 10 мс
#   Новое время C = 20 мс / 4 = 5 мс
#   Новое общее  = 100 + 10 + 5 + 40 = 155 мс
#   Ускорение    = 200 / 155 = 1.29x
#   Сэкономлено  = 45 мс (22.5%)

# Победитель по Амдалу: Вариант 1 — умеренное ускорение большей доли
# побивает большое ускорение меньшей доли.`}</code></pre>
        <p><strong>Паттерн.</strong> Всегда оценивай «долю общего времени» до «величины локального ускорения». 2x на 50% &gt; 4x на 30%. Профиль даёт тебе доли; без них уравнение нерешаемо.</p>
      </div>
    ),
    semi: {
      prompt: "Функция X занимает 40% времени исполнения. Ты можешь сделать её в 10x быстрее. Каково максимальное суммарное ускорение по закону Амдала?",
      blanks: [
        { id: "speedup", expected: "1.61", placeholder: "коэффициент ускорения" },
      ],
    },
    blank: {
      prompt: "Почему закон Амдала называют «потолком», а не «прогнозом»?",
      reveal: (
        <div class="space-y-2 text-sm">
          <p>Закон даёт максимально возможное ускорение, если оптимизируемая секция становится бесконечно быстрой — что недостижимо. Реальное ускорение всегда ниже, потому что любое переписывание всё равно стоит какого-то времени. Закон — это потолок: если потолок слишком низок, чтобы оправдать работу, не начинай.</p>
        </div>
      ),
    },
  }}
  misconceptions={{
    "speedup:1.5": "Почти — но точная формула Амдала даёт 1 / (0.6 + 0.04) ≈ 1.56. Округли до 1.6 или 1.61.",
    "speedup:10": "Это локальное ускорение секции, а не суммарное. Суммарное ограничено Амдалом: 1 / (1 - 0.4) = 1.67 даже если секция станет бесконечно быстрой.",
  }}
/>

<Misconception id="mc-guess-default">
  <p><strong>«Моя интуиция о том, где медленно, достаточно надёжна.»</strong> Ошибается чаще, чем угадывает — слишком много слоёв (фреймворк, рантайм, OS, кэши, библиотеки), чтобы моделировать в голове. Относись к интуиции как к гипотезе; профиль для проверки. Она редко выживает при контакте с измерением.</p>
</Misconception>

<Quiz
  id="02-amdahl-and-self-time-quiz-1"
  lessonSlug="02-amdahl-and-self-time"
  lang="ru"
  question="Профиль показывает: cum-time функции X — 80%, self-time — 2%. Что это означает и где искать фикс?"
  choices={[
    { label: "X — роутинговый/диспетчерский слой, вызывающий медленных потомков. Фикс не в X, а в том, что она вызывает — спускайся по флейм-графу к широким листовым фреймам.", correct: true },
    { label: "X — узкое место; переписать X.", misconception: "Self-time 2% означает, что собственный код X практически ничего не стоит. 80% cum-time принадлежат её callees." },
    { label: "Профайлер ошибается.", misconception: "Этот паттерн нормален: HTTP-хэндлеры, middleware, функции диспетчеризации — все имеют огромный cum-time и крошечный self-time." },
    { label: "Профиль избыточно сэмплирован.", misconception: "Частота сэмплирования не меняет разницу между self и cum; речь о том, где в дереве вызовов живёт работа." },
  ]}
/>

<Quiz
  id="02-amdahl-and-self-time-quiz-2"
  lessonSlug="02-amdahl-and-self-time"
  lang="ru"
  question="Микробенчмарк показывает, что новая хэш-функция в 10x быстрее старой. После интеграции приложение стало быстрее лишь на 1.05x. Наиболее вероятное объяснение?"
  choices={[
    { label: "Хэширование занимало малую долю общего времени — закон Амдала ограничивает ускорение приложения долей хэш-функции, сколь угодно быстрой та бы ни стала.", correct: true },
    { label: "Микробенчмарк принципиально сломан", misconception: "Он может быть корректен в изоляции; проблема в том, что локальная победа не доминирует в общем времени." },
    { label: "Новый хэш медленнее на реальных нагрузках", misconception: "Возможно, но реже, чем объяснение Амдала; профилируй для проверки, прежде чем предполагать." },
    { label: "Железо не поддерживает его", misconception: "Поддержка железа бинарна — работает или нет. Это не даёт частичного улучшения на 1.05x." },
  ]}
/>

</Explanation>

<KeyTakeaway>Закон Амдала: суммарное_ускорение = 1 / ((1 − p) + p/s). Профиль даёт p; без него уравнение нерешаемо. Большой self-time — чини функцию; большой cum-time при малом self-time — спускайся к callee, там живёт реальная цена.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="02-amdahl-and-self-time-retrieval"
  lang="ru"
  questions={[
    { q: "Пройди через закон Амдала с примером: функция X занимает 30% времени исполнения. Ты ускоряешь X в 5 раз. Каково суммарное ускорение и какую долю времени теперь занимает X?", a: "суммарное_ускорение = 1 / ((1 − p) + p/s) = 1 / (0.7 + 0.3/5) = 1 / (0.7 + 0.06) = 1 / 0.76 ≈ 1.32x. После переписывания X занимает (0.3/5) / (1 − 0.3 + 0.3/5) = 0.06 / 0.76 ≈ 7.9% нового времени исполнения. X перешла с 30% на ~8%. Следующая наибольшая функция теперь доминирует, и именно она — следующая цель профилирования. Этот итеративный паттерн повторяется: каждая успешная оптимизация смещает hotspot, и нужно повторно профилировать, чтобы его найти." },
    { q: "В чём разница между self-time и cum-time, и какое действие подсказывает каждый показатель?", a: "Self-time — время, потраченное в собственных инструкциях функции, без учёта времени в вызванных функциях. Cum-time (накопленный) включает всё время, приписанное функции и всему её поддереву вызовов. Если self-time большой — функция сама выполняет дорогостоящую работу; чини её. Если cum-time большой, а self-time маленький — функция является тонким роутинговым или диспетчерским слоем; реальная работа — в одном из её callees. Спускайся по дереву вызовов, чтобы найти callee с большим self-time." },
  ]}
/>

<Recap lang="ru">
Закон Амдала задаёт потолок: суммарное_ускорение = 1 / ((1 − p) + p/s). Профиль даёт p — долю общего времени, которую занимает целевая функция. Без этого числа нельзя решить, оправдана ли оптимизация инженерным временем. Функция на 10% общего времени никогда не даст больше 1.11x ускорения, даже если сделать её бесплатной; на 80% — до 5x. Self-time vs cum-time определяет, где искать: большой self-time — чини функцию напрямую; большой cum-time при крошечном self-time — спускайся к callee, где живёт реальная цена.
</Recap>
