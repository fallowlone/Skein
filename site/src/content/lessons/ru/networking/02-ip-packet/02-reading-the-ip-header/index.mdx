---
concepts:
- ipv4-header
- ttl
- dscp
- ecn
- protocol-field
- checksum
- ipv6-header
- next-header
deepensInto:
- 03-routing-and-forwarding
estMin: 12
lang: ru
lessonType: topic
level: middle
order: 2
prereqs:
- 01-the-ip-envelope
slug: 02-reading-the-ip-header
sources:
- https://datatracker.ietf.org/doc/html/rfc791
- https://datatracker.ietf.org/doc/html/rfc8200
- https://datatracker.ietf.org/doc/html/rfc9330
spiral:
- encapsulation
status: ready
summary: 'Каждое поле в заголовках IPv4 и IPv6 имеет назначение: TTL предотвращает
  петли, DSCP маркирует приоритет, поле протокола передаёт payload нужному обработчику
  L4.'
title: Читаем IP-заголовок
track: networking
unit: 02-ip-packet
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import NumbersCard from "~/components/layout/NumbersCard.astro";
import StackDiagram from "~/components/diagram/StackDiagram.astro";

<Hook>
Каждый раз когда роутер форвардит пакет — он читает и модифицирует IP-заголовок. Декрементирует TTL на 1, пересчитывает checksum и принимает forwarding-решение по адресу назначения. Знание какое поле что делает позволяет диагностировать сетевые проблемы и понять почему IPv6 убрал некоторые поля совсем.
</Hook>

<Crux>Поля IP-заголовка: TTL ограничивает хопы, DSCP/ECN маркируют приоритет и перегрузку, поле Protocol передаёт payload нужному обработчику L4.</Crux>

<Explanation>

## IPv4-заголовок (минимум 20 байт)

IPv4-пакеты начинаются с 20-байтного фиксированного заголовка (больше если присутствуют опции):

| Поле | Размер | Назначение |
|------|--------|-----------|
| Version | 4 бита | Всегда 4 для IPv4. |
| IHL (длина заголовка) | 4 бита | Количество 32-битных слов в заголовке; минимум 5 (= 20 байт). |
| ToS / DSCP + ECN | 8 бит | Изначально поле приоритета; переопределено как DSCP (6 бит) для QoS + ECN (2 бита). |
| Total length | 16 бит | Заголовок + payload в байтах; максимум 65 535. |
| Identification | 16 бит | Тегирует фрагменты одного исходного пакета. |
| Flags + Fragment offset | 16 бит | Флаг «Don't fragment», флаг «more fragments» и смещение внутри исходного пакета. |
| TTL | 8 бит | Hop limit; каждый роутер декрементирует на 1; дроп при 0. |
| Protocol | 8 бит | Протокол следующего слоя: 6 = TCP, 17 = UDP, 1 = ICMP, 58 = ICMPv6. |
| Header checksum | 16 бит | CRC только над заголовком (не payload); пересчитывается на каждом хопе потому что TTL меняется. |
| Source IP | 32 бита | Адрес отправителя. |
| Destination IP | 32 бита | Адрес получателя. |

**Почему header checksum только над заголовком.** Payload защищён checksum L4 (TCP/UDP). Вычислять checksum над полным пакетом на каждом роутере было бы запретительно дорого при line rate; нужен только header checksum потому что TTL меняется.

## TTL — time to live

TTL — 8-битный счётчик хопов, а не измерение времени (вопреки названию). Каждый роутер декрементирует на 1. Когда достигает 0 роутер дропает пакет и шлёт ICMP «time exceeded» обратно к источнику. Это предотвращает зацикливание пакетов при проблемах маршрутизации.

`traceroute` эксплуатирует TTL: шлёт пробы с TTL = 1, 2, 3, … Каждый роутер который дропает пробу (TTL достигло нуля) отвечает «time exceeded», раскрывая свой адрес и RTT — путь трасируется хоп за хопом.

## DSCP и ECN — рычаги качества

Зачем это знать? Потому что неправильно настроенный DSCP (Differentiated Services Code Point — метка приоритета трафика) — это причина почему голосовой звонок деградирует когда на том же линке стартует bulk-загрузка. А ECN (Explicit Congestion Notification — явное уведомление о перегрузке) — это почему современный TCP умеет сбавлять скорость не дропая пакеты.

Первые 8 бит третьего байта несут два ортогональных сигнала качества:

- **DSCP (6 бит).** Differentiated Services Code Point. Роутеры используют его для приоритизации трафика в очередях. Общие значения: EF (Expedited Forwarding — голос/реальное время), AF (Assured Forwarding — видео), CS (Class Selector — legacy-совместимость). End-to-end DSCP редко выживает в публичном Интернете потому что каждый AS переписывает на ingress; на queueing влияет надёжно только внутри одной сети.
- **ECN (2 бита).** Explicit Congestion Notification. Перегруженный роутер маркирует пакет CE (Congestion Experienced) вместо его дропа. Принимающий хост эхоирует сигнал обратно к отправителю через TCP, который уменьшает congestion window — меньше задержка без потерь. ECN включён по умолчанию на современном Linux. L4S (RFC 9330–9332) расширяет ECN для достижения ultra-low latency через dual-queue traffic shaping.

<NumbersCard
  id="card-header-fields"
  title="Поля IPv4-заголовка с первого взгляда"
  data-lesson-visual
  stats={[
    { label: "Минимальный размер заголовка", value: "20 байт" },
    { label: "TTL начальное значение (Linux)", value: "64" },
    { label: "Protocol: TCP", value: "6" },
    { label: "Protocol: UDP", value: "17" },
    { label: "Protocol: ICMP", value: "1" },
    { label: "Макс. размер пакета (поле total length)", value: "65 535 байт" },
  ]}
/>

## IPv6-заголовок — упрощён намеренно

IPv6 имеет фиксированный 40-байтный заголовок без checksum и без полей фрагментации:

- **Version (4 бита):** Всегда 6.
- **Traffic Class (8 бит):** Та же семантика DSCP + ECN что и в IPv4.
- **Flow Label (20 бит):** Идентифицирует flow (например TCP-соединение) для consistent ECMP без перехеширования каждого пакета.
- **Payload Length (16 бит):** Размер только payload (не заголовка).
- **Next Header (8 бит):** Та же роль что Protocol в IPv4 — идентифицирует extension header или L4-протокол следующий за ним.
- **Hop Limit (8 бит):** Аналог TTL в IPv4.
- **Source / Destination (по 128 бит):** 16-байтные адреса.

**Почему нет checksum.** IPv6 полагается на L2 (Ethernet FCS) и L4 (checksum TCP/UDP) для целостности. Устранение per-hop пересчёта checksum уменьшает работу каждого роутера и соответствует возможностям современного оборудования.

**Extension headers** цепятся после фиксированного заголовка через поле Next Header — аналог IPv4 options но чище. Распространённые расширения: Hop-by-Hop Options, Routing (используется Segment Routing v6), Fragment, Destination Options.

<Inset kind="why" lang="ru">
**Почему IPv6-роутеры никогда не фрагментируют.** IPv4 позволял роутерам разбивать oversized пакет на меньшие фрагменты — полезно, но добавляло CPU-работу на каждом хопе и создавало дыры в безопасности (некоторые firewalls инспектировали только первый фрагмент, позволяя атакующим прятать вредоносный payload). IPv6 запрещает фрагментацию роутерами. Если пакет слишком большой, роутер шлёт ICMP «Packet Too Big» обратно к отправителю, который должен уменьшить размер и переотправить. Сложность переносится на отправителя, роутер остаётся простым.
</Inset>

<Quiz
  id="mid-quiz-1"
  pieceSlug="02-reading-the-ip-header"
  lang="ru"
  question="От чего защищает поле TTL?"
  choices={[
    { label: "Повреждения пакета", misconception: "Это работа checksum." },
    { label: "Вечного зацикливания пакетов в неправильно маршрутизированных сетях — TTL ограничивает максимальное число хопов", correct: true },
    { label: "Прослушивания", misconception: "TTL не связан с конфиденциальностью." },
    { label: "Ошибок reassembly", misconception: "Reassembly использует поля ID + fragment offset, не TTL." },
  ]}
/>

<Quiz
  id="mid-quiz-2"
  pieceSlug="02-reading-the-ip-header"
  lang="ru"
  question="Почему IPv6 убирает IP-level header checksum?"
  choices={[
    { label: "IPv6 всегда зашифрован", misconception: "Ни IPv4 ни IPv6 не зашифрованы по умолчанию." },
    { label: "IPv6 полагается на L2 (Ethernet FCS) и L4 (TCP/UDP) для целостности; устранение per-hop пересчёта уменьшает работу роутера", correct: true },
    { label: "IPv6 checksums вычисляются в аппаратуре", misconception: "Выбор архитектурный, а не вопрос реализации в hardware." },
    { label: "IPv6-пакеты никогда не меняются в пути", misconception: "Hop Limit (аналог TTL) всё равно меняется; суть в том что checksum был только над заголовком." },
  ]}
/>

<DragOrder
  id="mid-drag-1"
  pieceSlug="02-reading-the-ip-header"
  lang="ru"
  prompt="Расставьте поля IPv4-заголовка (после Version + IHL):"
  items={[
    "ToS / DSCP + ECN (QoS и маркировка перегрузки)",
    "Total length (заголовок + payload)",
    "Identification + Flags + Fragment offset",
    "TTL (hop limit)",
    "Protocol (TCP=6, UDP=17, ICMP=1)",
    "Header checksum",
    "Source IP, Destination IP",
  ]}
/>

</Explanation>

<StackDiagram
  label="20-байтный IPv4-заголовок поле за полем в порядке на проводе, сверху вниз: Version и IHL, ToS с DSCP и ECN, Total length, Identification с флагами и fragment offset, TTL, Protocol, Header checksum, Source IP, Destination IP."
  hue="var(--d-network)"
  layers={[
    { label: "Version + IHL", note: "4 = IPv4, длина заголовка" },
    { label: "ToS / DSCP + ECN", note: "приоритет + перегрузка" },
    { label: "Total length", note: "байты заголовка + payload" },
    { label: "Identification + Flags + Frag offset", note: "reassembly фрагментов" },
    { label: "TTL", note: "hop limit, −1 на роутер" },
    { label: "Protocol", note: "TCP=6 UDP=17 ICMP=1" },
    { label: "Header checksum", note: "только над заголовком" },
    { label: "Source IP", note: "32 бита" },
    { label: "Destination IP", note: "32 бита" },
  ]}
  caption="Каждый IPv4-пакет несёт эти поля в этом порядке. TTL декрементируется на каждом хопе, поэтому заголовок меняется — вот почему checksum (только над заголовком) пересчитывается на каждом роутере, и именно эту per-hop работу IPv6 убрал."
/>

<KeyTakeaway>IPv4-заголовок — 20 байт сетевых элементов управления: TTL ограничивает хопы, DSCP/ECN сигнализируют приоритет и перегрузку, поле Protocol передаёт payload нужному обработчику L4. IPv6 упрощает до фиксированного 40-байтного заголовка без checksum и без фрагментации роутерами.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="02-reading-the-ip-header-retrieval-ru"
  lang="ru"
  questions={[
    { q: "Почему роутер пересчитывает IPv4 header checksum на каждом хопе?", a: "Поле TTL декрементируется на 1 на каждом хопе, поэтому заголовок меняется. Checksum покрывает заголовок, значит должен быть пересчитан. Именно поэтому IPv6 убрал IP-level checksum — устранив эту per-hop работу ценой зависимости от целостности L2/L4." },
    { q: "Что делает поле DSCP и почему оно редко работает end-to-end в публичном Интернете?", a: "DSCP (6 бит в байте ToS) маркирует пакеты классом трафика чтобы роутеры приоритизировали их в очередях. Редко выживает в публичном Интернете потому что каждый автономный systems переписывает (перезаписывает) DSCP на ingress — операторы не доверяют маркировкам из внешних сетей. DSCP надёжно влияет на queueing только внутри одной сети (enterprise, datacenter)." },
    { q: "Как traceroute раскрывает путь используя TTL?", a: "traceroute шлёт пробы с TTL=1, 2, 3, … Каждый роутер получающий пробу с TTL=1 декрементирует до 0, дропает пакет и шлёт ICMP 'time exceeded' обратно. Этот ICMP-ответ содержит IP-адрес роутера. Последовательно увеличивая TTL на 1, traceroute собирает адрес каждого хопа от источника до назначения." },
  ]}
/>

<Recap lang="ru">
IPv4-заголовок упаковывает 20 байт сетевого управления в каждый пакет. TTL (hop limit) предотвращает routing loops декрементируясь на каждом роутере и вызывая дроп при нуле. DSCP маркирует QoS-класс; ECN сигнализирует перегрузку без дропа. Поле Protocol направляет payload к TCP, UDP или ICMP. Header checksum покрывает только заголовок и должен пересчитываться на каждом хопе из-за изменений TTL — именно поэтому IPv6 его убрал, полагаясь на L2 и L4 для целостности. IPv6 также зафиксировал заголовок на 40 байтах, запретил фрагментацию роутерами и добавил 20-битный Flow Label для ECMP-консистентности. Теперь, когда вы открываете packet capture и видите TTL=1 — вы знаете что следующий хоп дропнет пакет; когда видите DSCP=EF на медиапотоке — можете проверить применяется ли ваша QoS-политика на самом деле.
</Recap>
