---
concepts:
- best-effort-delivery
- longest-prefix-match
- ttl-traceroute
- pmtud-black-hole
- nat-end-to-end
- rpki-bgp
estMin: 13
lang: ru
level: senior
order: 8
prereqs: []
slug: quiz-short
sources:
- https://datatracker.ietf.org/doc/html/rfc791
- https://datatracker.ietf.org/doc/html/rfc8200
- https://datatracker.ietf.org/doc/html/rfc4271
- https://datatracker.ietf.org/doc/html/rfc8900
status: ready
summary: "Подсказки на свободное припоминание по всему юниту IP-packet. Сначала ответь по памяти, затем открой модельный ответ и сравни."
title: 'IP-пакеты: тест на припоминание'
track: networking
unit: 02-ip-packet
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";

<Hook>
Припоминание сильнее перечитывания. На каждую подсказку проговори или запиши полный ответ по памяти, прежде чем открыть модельный — усилие припоминания и есть то, что закрепляет механику.
</Hook>

<Goal lang="ru">
Восстанови костяк юнита — best-effort доставку, longest-prefix forwarding, TTL и traceroute, PMTUD black hole, что ломает NAT и как работает защита BGP/spoofing — не подглядывая в уроки.
</Goal>

<RetrievalDrawer
  client:visible
  id="02-ip-packet-short-retrieval"
  lang="ru"
  questions={[
    { q: "Почему IP намеренно best-effort и connectionless, и что эта схема перекладывает на уровни выше?", a: "IP ничего не гарантирует: нет подтверждений, нет retransmission, нет упорядочивания, а перегруженный роутер тихо отбрасывает packet. RFC 791 выбрал это, чтобы сетевой уровень оставался достаточно простым для масштабирования на миллиарды устройств — каждый роутер лишь принимает локальное решение forwarding, не отслеживая состояние соединений. Следствие: вся надёжность (подтверждение, retransmission, упорядочивание, congestion control) живёт на endpoints в TCP или QUIC, где это дешевле, чем держать состояние в каждом роутере на каждом пути. Поэтому TCP, TLS и QUIC сидят над IP, а не внутри него." },
    { q: "Объясни longest-prefix match и почему routing-таблица отделена от FIB.", a: "Когда destination совпадает с несколькими CIDR-prefix, longest-prefix match выбирает самый специфичный — prefix с наибольшим числом совпадающих ведущих бит, — так /24 перекрывает покрывающий /8. Переменная длина prefix в CIDR делает перекрытие нормой и включает агрегацию (одно объявление /8 покрывает 16 миллионов адресов, удерживая глобальную таблицу BGP около миллиона записей вместо взрыва). Разделение: routing-таблица — структура control plane, строимая BGP/OSPF/IS-IS, хранит все известные prefix, атрибуты и альтернативы; FIB — урезанный вид next-hop по destination, скомпилированный в железо TCAM для line-rate lookup за наносекунды на data plane. Планы эволюционируют в разных временных масштабах — обновления протоколов за миллисекунды, forwarding packet за наносекунды." },
    { q: "Что такое TTL, что происходит при достижении нуля и как traceroute это эксплуатирует?", a: "TTL (IPv4) / Hop Limit (IPv6) — это 8-битный счётчик hop, а не время; каждый роутер уменьшает его на единицу. При нуле роутер отбрасывает packet и шлёт ICMP 'time exceeded' источнику; это не даёт неверно маршрутизированным packet вечно ходить по кругу. traceroute использует это: шлёт probe с TTL = 1, 2, 3, …; каждый роутер, у которого decrement дал ноль, отвечает 'time exceeded', и этот ответ раскрывает его адрес и RTT. Увеличивая TTL по одному, traceroute трассирует путь hop за hop. Hop, скрывающий или rate-limit ICMP, показывается звёздочками, продолжая пересылать данные." },
    { q: "Что такое PMTUD black hole, почему он даёт симптом 'мелкие запросы работают, большие зависают' и какой надёжный фикс?", a: "Path MTU Discovery опирается на то, что роутер отбрасывает слишком большой packet с битом DF и возвращает ICMP 'packet too big', чтобы sender уменьшил размер. Black hole возникает, когда firewall или middlebox тихо отбрасывает этот ICMP — sender никогда не узнаёт, продолжает слать packet полного размера, и их продолжают отбрасывать. Мелкие запросы влезают в path MTU и проходят; большие payload его превышают и зависают, часто с циклами retransmit ~1с. Надёжный фикс — TCP MSS clamping: gateway переписывает опцию MSS в SYN до безопасного значения, чтобы endpoints не строили слишком большой packet, — или PLPMTUD (RFC 4821), который probe-ит TCP-segment; оба работают без опоры на выживание ICMP. (Разблокировка ICMP помогает лишь там, где ты контролируешь каждый middlebox.)" },
    { q: "Что ломает NAT и почему, и почему 'NAT — это firewall' — опасное заблуждение?", a: "NAT переписывает приватный source IP:port на публичный IP:port роутера на исходящих packet и разворачивает обратно на ответах, позволяя многим host делить один публичный IP — что облегчило исчерпание IPv4, но сломало end-to-end адресацию: внутренний host не может принять неинициированное входящее соединение без явного port forwarding, поэтому peer-to-peer протоколам (WebRTC, VoIP, BitTorrent) нужны STUN/TURN/ICE для traversal. CGNAT добавляет второй слой NAT, усугубляя это и делая per-IP rate-limiting бессмысленным. NAT — не безопасность: он не инспектирует трафик, не применяет политику и не мешает скомпрометированному внутреннему host выйти наружу. Stateful firewall всё равно нужен независимо от NAT; смешение двух понятий оставляет реальные дыры." },
    { q: "IP не аутентифицирует ни source, ни маршрут. Назови защиту от spoofing и hijack и почему каждой нужна кооперация операторов.", a: "Подмена source (любой host может подделать source IP, включая reflection/amplification DDoS) защищается BCP 38 egress filtering — ISP отбрасывает исходящие packet, чей source вне выделенного ему пространства, — подкреплённым uRPF, проверкой data plane, что source маршрутизировался бы обратно через интерфейс прибытия. Угон маршрута (любая AS может объявить чужой prefix) защищается RPKI: владелец prefix публикует подписанный ROA, связывающий prefix с его origin AS, а роутеры с ROV отбрасывают объявления, чей origin 'invalid'. Всем нужна кооперация, потому что они работают лишь там, где развёрнуты — BCP 38 стоит у ISP атакующего (~60% внедрения), а полный эффект RPKI требует и регистрации ROA владельцем prefix, и работы ROV у удалённых ISP. Частичное внедрение помогает, но не закрывает разрыв полностью, поэтому spoofing и hijack всё ещё случаются." },
  ]}
/>

<Recap lang="ru">
Если ты смог восстановить каждый ответ по памяти, ты держишь костяк юнита: IP best-effort по замыслу и перекладывает надёжность на TCP/QUIC; forwarding — это longest-prefix match, разделённый между routing-таблицей control plane и железным FIB; TTL ограничивает hop и питает traceroute; заблокированный ICMP превращает несовпадение path-MTU в тихий black hole, который чинит MSS clamping; NAT меняет облегчение адресов на сломанную end-to-end достижимость и не является firewall; а поскольку IP не аутентифицирует ни source, ни маршрут, защитой служат BCP 38/uRPF и RPKI — эффективные лишь там, где операторы кооперируются.
</Recap>
