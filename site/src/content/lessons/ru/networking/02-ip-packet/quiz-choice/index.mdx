---
concepts:
- longest-prefix-match
- ttl
- pmtud-black-hole
- nat-traversal
- bgp-hijack
- ip-spoofing
estMin: 13
lang: ru
level: senior
order: 7
prereqs: []
slug: quiz-choice
sources:
- https://datatracker.ietf.org/doc/html/rfc791
- https://datatracker.ietf.org/doc/html/rfc8200
- https://datatracker.ietf.org/doc/html/rfc4271
- https://datatracker.ietf.org/doc/html/rfc2827
status: ready
summary: "Тест с выбором на синтез по всему юниту — семантика header, longest-prefix routing, MTU black hole, NAT traversal и защита от BGP-угона и spoofing."
title: 'IP-пакеты: тест с выбором ответа'
track: networking
unit: 02-ip-packet
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Шесть вопросов поперёк всего юнита. Каждый отражает решение, которое ты принимаешь в реальном инциденте — packet, который где-то гибнет; соединение, которое зависает только на больших payload; prefix, который темнеет с одной AS — не поле для пересказа, а поведение, которое надо объяснить.
</Hook>

<Goal lang="ru">
Убедись, что связываешь семантику header, longest-prefix forwarding, MTU и PMTUD, NAT traversal и модель безопасности на уровне IP — тот синтез, к которому вели отдельные уроки.
</Goal>

<Quiz
  id="02-ip-packet-choice-1"
  lessonSlug="02-ip-packet"
  lang="ru"
  question="Мониторинг сообщает, что IPv4-роутер пересчитывает header checksum на каждом пересылаемом packet, а аналогичный IPv6-роутер — нет. Почему разница и на что вместо этого опирается IPv6?"
  choices={[
    { label: "IPv4 обязан пересчитывать, потому что TTL уменьшается на каждом hop, меняя header, который покрывает checksum. IPv6 полностью убрал IP-уровневый checksum и опирается на контроль кадра L2 (Ethernet FCS) и checksum L4 (TCP/UDP).", correct: true },
    { label: "IPv6 считает checksum в железе, поэтому инструмент его просто не видит.", misconception: "Разница архитектурная, а не деталь реализации. В IPv6 вообще нет поля header checksum — пересчитывать нечего." },
    { label: "IPv4 пересчитывает, потому что payload меняется в пути; payload IPv6 неизменен.", misconception: "Header checksum в IPv4 покрывает только header, никогда payload. Он меняется потому, что меняется TTL, а не из-за payload." },
    { label: "IPv6 зашифрован, поэтому открытый checksum бессмыслен.", misconception: "Ни IPv4, ни IPv6 не шифруются по умолчанию. IPv6 просто делегирует целостность на L2 и L4, чтобы сократить работу роутера на каждом hop." },
  ]}
/>

<Quiz
  id="02-ip-packet-choice-2"
  lessonSlug="02-ip-packet"
  lang="ru"
  question="В FIB роутера есть и 10.0.0.0/8 через eth0, и 10.4.0.0/16 через eth1. Приходит packet для 10.4.2.9. Какой путь побеждает и какой принцип это решает?"
  choices={[
    { label: "eth1 через /16. Longest-prefix match выбирает самый специфичный маршрут — тот, у кого больше совпадающих ведущих бит, — поэтому /16 перекрывает более широкий агрегат /8.", correct: true },
    { label: "eth0 через /8, потому что он установлен первым и покрывает больший диапазон.", misconception: "Ни возраст маршрута, ни размер диапазона не решают forwarding. При longest-prefix match всегда побеждает более специфичный prefix, независимо от порядка." },
    { label: "Роутер балансирует по обоим, раз оба совпадают.", misconception: "ECMP делит только пути равной стоимости к одному prefix. Здесь prefix разной длины, поэтому однозначно выбирается более специфичный — без балансировки." },
    { label: "Packet отбрасывается как неоднозначный, раз совпали две записи.", misconception: "Перекрывающиеся prefix — норма при CIDR и никогда не неоднозначны: longest-prefix match детерминированно выбирает самую специфичную запись." },
  ]}
/>

<Quiz
  id="02-ip-packet-choice-3"
  lessonSlug="02-ip-packet"
  lang="ru"
  question="Сервис за WireGuard VPN работает на мелких запросах, но загрузка файлов зависает навсегда без ошибки. tcpdump показывает, что клиент переотправляет один и тот же большой segment примерно раз в секунду. Корневая причина и самый надёжный фикс?"
  choices={[
    { label: "PMTUD black hole: MTU туннеля ниже 1500, слишком большие packet отбрасываются, а ICMP 'packet too big' фильтруется, поэтому sender никогда не уменьшает размер. Самый надёжный фикс: clamp TCP MSS на VPN-gateway, чтобы endpoints вообще не строили слишком большие packet — это не зависит от ICMP.", correct: true },
    { label: "Повреждение packet в туннеле; поднять retransmit timeout и переждать.", misconception: "Повреждения нет — segment отбрасываются за слишком большой размер, а больший timeout лишь дольше держит зависание. Фикс — перестать слать слишком большие packet, а не ждать." },
    { label: "VPN слишком медленный для больших передач; сменить шифр на более быстрый.", misconception: "Дело не в throughput — мелкие запросы проходят мгновенно. Packet превышают path MTU и тихо отбрасываются; скорость шифра ни при чём." },
    { label: "Просто разблокировать ICMP и положиться на PMTUD.", misconception: "Разблокировка ICMP помогает, только если ты контролируешь каждый middlebox на пути. MSS clamping надёжнее, потому что не зависит от того, переживёт ли ICMP весь путь." },
  ]}
/>

<Quiz
  id="02-ip-packet-choice-4"
  lessonSlug="02-ip-packet"
  lang="ru"
  question="WebRTC-функция падает у части пользователей мобильного оператора и сваливается на relay. Оператор использует CGNAT. Почему CGNAT затрудняет прямое соединение peer-to-peer и какой принципиальный долгосрочный фикс?"
  choices={[
    { label: "CGNAT добавляет второй слой NAT поверх NAT домашнего роутера, поэтому ни у одного peer нет стабильного достижимого публичного IP:port для неинициированного соединения — STUN часто не срабатывает, и трафик сваливается на TURN relay. Принципиальный фикс — IPv6: он даёт каждому устройству глобально маршрутизируемый адрес и убирает NAT целиком.", correct: true },
    { label: "CGNAT шифрует packet, поэтому DTLS в WebRTC не может договориться.", misconception: "CGNAT переписывает адреса и порты, но не шифрует. Сбой — в недостижимости внутреннего host, а не в конфликте шифрования." },
    { label: "Фикс — rate-limit нарушителей по их публичному IP.", misconception: "При CGNAT сотни несвязанных пользователей делят один публичный IP, поэтому per-IP rate limiting бессмыслен — и он никак не помогает установить прямое соединение." },
    { label: "CGNAT блокирует UDP, поэтому WebRTC должен использовать TCP.", misconception: "CGNAT нормально пропускает UDP; STUN/TURN работают по UDP. Проблема — двойная трансляция адресов, ломающая входящую достижимость, а не транспортный протокол." },
  ]}
/>

<Quiz
  id="02-ip-packet-choice-5"
  lessonSlug="02-ip-packet"
  lang="ru"
  question="Твой сайт доступен из офиса, но клиент на одном ISP получает полный timeout. Запрос к looking-glass внутри этого ISP показывает твой prefix как RPKI 'invalid'. Что вероятнее всего случилось и что делать первым?"
  choices={[
    { label: "Скорее всего, истёк твой ROA (или объявляющая origin AS перестала совпадать с ним), поэтому роутеры с ROV на этом ISP отбрасывают маршрут как invalid. Первое действие: продлить/исправить ROA в твоём RIR; валидация восстановится за часы по мере обновления кэшей. Затем мониторить срок ROA как сертификат.", correct: true },
    { label: "ISP клиента цензурирует тебя; эскалируй к регулятору.", misconception: "Состояние RPKI 'invalid' — это сбой route-origin validation, почти всегда проблема с ROA на твоей стороне, а не цензура. Сначала исправь ROA." },
    { label: "Твой сервер упал; перезапусти его.", misconception: "Сервер работает — ты достаёшь его из офиса. Сбой в inter-domain routing для одной AS, на что и указывает состояние RPKI 'invalid', а не падение host." },
    { label: "Переключи DNS на другой IP, чтобы обойти проблему.", misconception: "Новый IP без совпадающего валидного ROA будет отфильтрован так же, а смена адресов не устраняет несоответствие origin-validation. Исправляй ROA." },
  ]}
/>

<Quiz
  id="02-ip-packet-choice-6"
  lessonSlug="02-ip-packet"
  lang="ru"
  question="Атакующий шлёт мелкие spoofed UDP-запросы к открытым resolvers с IP твоего сервера в source, и твой канал тонет под многогигабитными ответами, которые ты не запрашивал. Какая одна защита, применённая в сети атакующего, останавливает атаку в истоке?"
  choices={[
    { label: "BCP 38 egress filtering на ISP атакующего: проверка, что уходящие packet несут source IP из адресного пространства, на которое сеть авторизована, — тогда запрос с подменённым source не выйдет наружу. Это крупнейший односторонний фикс, но мировое внедрение около 60%.", correct: true },
    { label: "Включить RPKI ROV на своих роутерах.", misconception: "RPKI валидирует origin маршрутов BGP, останавливая угон prefix — другая атака. Против spoofed-source UDP-amplification он ничего не даёт." },
    { label: "Заблокировать весь ICMP на своём firewall.", misconception: "Это reflection/amplification-флуд по UDP, а не по ICMP — и блокировка ICMP лишь создаст PMTUD black hole. Spoofed-трафик это не остановит." },
    { label: "Понизить TTL сервера, чтобы ответы истекали раньше.", misconception: "TTL ограничивает число hop на packet, которые шлёшь ты; на объём входящего флуда он не влияет. Фикс — source-address validation в сети, откуда идёт spoofing." },
  ]}
/>

<Recap lang="ru">
Сквозная линия юнита — жизнь одного packet: header несёт TTL, protocol и адреса (IPv6 убирает per-hop checksum); роутеры пересылают по longest-prefix match в железном FIB, который BGP держит глобально согласованным; несовпадение MTU требует fragmentation или уменьшения, а заблокированный ICMP превращает это в тихий PMTUD black hole; NAT и CGNAT меняют облегчение исчерпания адресов на сломанную end-to-end достижимость, которую обходят STUN/TURN/ICE и в итоге IPv6; а поскольку IP не аутентифицирует ни source, ни маршрут, защитой служат BCP 38, uRPF и RPKI — все они требуют кооперации операторов. Каждый сбой сводится к этой же механике.
</Recap>
