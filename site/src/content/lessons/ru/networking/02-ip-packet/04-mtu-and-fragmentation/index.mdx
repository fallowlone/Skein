---
concepts:
- mtu
- fragmentation
- pmtud
- pmtud-black-hole
- mss-clamping
- plpmtud
- df-bit
deepensInto:
- 05-nat-and-addressing
estMin: 12
lang: ru
lessonType: topic
level: middle
order: 4
prereqs:
- 03-routing-and-forwarding
slug: 04-mtu-and-fragmentation
sources:
- https://datatracker.ietf.org/doc/html/rfc8200
- https://datatracker.ietf.org/doc/html/rfc8900
- https://datatracker.ietf.org/doc/html/rfc4821
spiral: []
status: ready
summary: Пакет превышающий MTU линка должен быть уменьшен или разбит. IPv4-роутеры
  могут фрагментировать; IPv6 запрещает это. PMTUD black holes возникают когда ICMP
  блокируется.
title: MTU и фрагментация
track: networking
unit: 02-ip-packet
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import TraceScenario from "~/components/pedagogy/TraceScenario.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";
import NumbersCard from "~/components/layout/NumbersCard.astro";
import Misconception from "~/components/layout/Misconception.astro";
import SequenceDiagram from "~/components/diagram/SequenceDiagram.astro";
import Infographic from "~/components/diagram/Infographic.astro";
import mssOverhead from "./infographics/mss-overhead.svg?raw";

<Hook>
Разработчик развёртывает сервис за VPN. Маленькие запросы работают нормально. Большие — загрузка файла, большой JSON-payload — тихо зависают. Никакого сообщения об ошибке. Сервер не получает ничего. Проблема — несоответствие размера пакета максимуму который может нести линк, плюс firewall блокирует сообщение об ошибке которое исправило бы это.
</Hook>

<Crux>Пакет больше MTU: IPv4 фрагментирует, IPv6 требует уменьшить у отправителя. ICMP заблокирован — отправитель не узнаёт, соединения зависают.</Crux>

<Explanation>

## Что такое MTU

Почему это важно для вас как инженера? Потому что каждый VPN который вы деплоите, каждая облачная сеть и каждое PPPoE-соединение у пользователей — потенциальное несоответствие MTU (Maximum Transmission Unit — максимальный размер передаваемого блока данных). И сбой выглядит как нестабильный сервис, а не как ошибка конфигурации.

**MTU** — наибольший IP-пакет (заголовок + payload) который линк может нести без фрагментации. Стандартный MTU Ethernet — 1500 байт. Разные технологии линков имеют разные MTU:

<NumbersCard
  id="card-mtu"
  title="Типичные значения MTU"
  data-lesson-visual
  stats={[
    { label: "Ethernet (стандарт)", value: "1500 байт" },
    { label: "PPPoE (DSL)", value: "1492 байта" },
    { label: "Wi-Fi (802.11)", value: "~1500 байт" },
    { label: "Jumbo frames (10G/40G)", value: "9000 байт" },
    { label: "Минимальный обязательный MTU IPv6", value: "1280 байт" },
    { label: "VPN-туннели (WireGuard, IPSec)", value: "1400–1450 байт" },
  ]}
/>

**Path MTU** — наименьший MTU на всём пути от источника до назначения. Пакет должен вписываться в Path MTU, иначе он будет дропнут где-то на пути.

## Фрагментация IPv4

Когда IPv4-пакет приходит на роутер у которого исходящий линк имеет меньший MTU, роутер разбивает пакет на **фрагменты**:

1. Роутер копирует поле identification во все фрагменты.
2. Устанавливает флаг «more fragments» на всех кроме последнего.
3. Каждый фрагмент несёт смещение внутри исходного пакета.
4. Назначение переcобирает все фрагменты с одним ID + source IP.

Фрагментация дорогостоящая: потеря любого фрагмента вынуждает повторно передавать всю исходную датаграмму. Каждый фрагмент маршрутизируется независимо. Фрагментация на роутерах добавляет задержку и CPU-работу.

**Бит DF (Don't Fragment).** Когда установлен в поле Flags IPv4-заголовка, роутер не может фрагментировать пакет. Если пакет не вписывается в MTU исходящего линка, роутер дропает его и шлёт ICMP «fragmentation needed» обратно к источнику. Это обеспечивает PMTUD.

## IPv6 — без фрагментации роутерами

IPv6-роутеры **никогда не фрагментируют**. Если пакет слишком большой, роутер дропает его и шлёт ICMPv6 «Packet Too Big» обратно к отправителю. Отправитель должен уменьшить размер пакета и повторно передать. Это переносит сложность на отправителя и упрощает обработку роутером.

Отправители IPv6 обязаны поддерживать минимальный MTU 1280 байт (они всегда могут отправлять 1280-байтные пакеты к любому назначению). Path MTU Discovery обязателен для IPv6.

## Path MTU Discovery (PMTUD)

PMTUD обнаруживает наименьший MTU на пути. Отправитель устанавливает DF=1 (IPv4) и шлёт полноразмерные пакеты. Когда роутер дропает один потому что он слишком большой, он шлёт ICMP «packet too big» (или «fragmentation needed») обратно. Отправитель уменьшает размер пакета и повторяет попытку.

**PMTUD black holes** возникают когда firewall или middlebox тихо дропает ICMP. Отправитель никогда не получает сигнал «packet too big». Он продолжает передавать полноразмерные пакеты, которые продолжают дропаться. Соединение **зависает** — маленькие запросы работают (они вписываются), большие зависают навсегда.

**Симптомы PMTUD black hole:**
- Маленькие HTTP-запросы успешны; большие payload'ы или файлы зависают.
- `ping -M do -s 1450 destination` завершается без ответа.
- `traceroute` показывает путь нормально, но TCP-сессии зависают при больших размерах.

<Misconception id="r-blackhole" title="PMTUD black holes">
Роутер дропает пакет и шлёт ICMP «packet too big», но middlebox тихо выбрасывает ICMP. Отправитель никогда не узнаёт что нужно уменьшить пакеты, повторно передаёт на 1500 байт вечно, попадая в ~1-секундные таймауты. TCP MSS clamping и PLPMTUD смягчают это на практике.
</Misconception>

## Решения для PMTUD black holes

**TCP MSS clamping:** MSS (Maximum Segment Size) согласовывается в TCP handshake. Роутер на пути (VPN-шлюз, PPPoE-терминатор) перезаписывает опцию MSS в SYN-пакетах до безопасного значения (например 1452 = 1500 - 20 IP - 20 TCP - 8 PPPoE). Эндпойнты никогда не согласовывают размер сегмента который создал бы oversized пакет. Это прагматичное исправление для большинства VPN и PPPoE развёртываний.

<Infographic
  svg={mssOverhead}
  label="Горизонтальная столбчатая диаграмма раскладывающая бюджет 1500 байт Ethernet на пути PPPoE+TCP: 1452 байта полезного payload MSS, 20 байт заголовка IP, 20 байт заголовка TCP и 8 байт overhead PPPoE."
  caption="48 байт заголовков — причина почему безопасный clamped MSS равен 1452, а не 1500: зажмите до этого значения и пакеты никогда не превысят MTU линка, поэтому нет oversized пакета для дропа и нет ICMP-сигнала который можно потерять."
  hue="var(--d-infra)"
/>

**PLPMTUD (RFC 4821):** Packetisation-Layer PMTUD. Отправитель зондирует всё большими TCP-сегментами; если зонд не получает ACK — отступает. Вообще не полагается на ICMP — устойчив к ICMP-фильтрации. Современные стеки по умолчанию используют PLPMTUD.

**Открытые ICMP-политики:** разрешить ICMP type 3 (destination unreachable) и ICMPv6 type 2 (packet too big) через firewall'ы. Фильтрация всего ICMP — распространённая но неверная «политика безопасности» которая ломает PMTUD и traceroute.

<TraceScenario
  id="mid-trace-1"
  pieceSlug="04-mtu-and-fragmentation"
  lang="ru"
  scenario="Диагноз: пакеты тихо дропаются когда их размер превышает 1400 байт."
  steps={[
    { prompt: "Каков симптом с точки зрения пользователя?", reveal: "Маленькие запросы работают. Большие запросы (загрузка файла, большой JSON) зависают или падают. Браузер может показывать 'connection reset' или просто таймаут." },
    { prompt: "Что происходит на сетевом уровне?", reveal: "Path MTU — 1400 байт (overhead VPN или PPPoE). Пакеты больше этого превышают MTU линка. Роутер шлёт ICMP 'packet too big' обратно. Отправитель должен скорректировать MSS." },
    { prompt: "Почему это 'тихо' падает в некоторых случаях?", reveal: "Если firewall блокирует ICMP, сообщение 'packet too big' никогда не достигает отправителя. Отправитель продолжает повторно передавать полноразмерные пакеты которые продолжают дропаться. PMTUD black hole — соединения зависают." },
    { prompt: "Диагностическая команда?", reveal: "ping -M do -s 1400 destination — шлёт 1400-байтный пакет с установленным DF (don't fragment). Если завершается без ICMP-ответа, Path MTU меньше. Бисектируйте чтобы найти реальный MTU." },
    { prompt: "Исправление?", reveal: "Разрешить ICMP packet-too-big через firewall'ы. Или использовать TCP MSS clamping на шлюзе VPN/PPPoE: перезаписать опцию TCP MSS до безопасного значения чтобы эндпойнты никогда не отправляли oversized пакеты изначально." },
  ]}
/>

<Quiz
  id="mid-quiz-1"
  pieceSlug="04-mtu-and-fragmentation"
  lang="ru"
  question="Почему IPv6 запрещает фрагментацию на уровне роутера?"
  choices={[
    { label: "Современные роутеры слишком медленны для фрагментации", misconception: "Современные роутеры быстрые; выбор дизайна касается простоты и безопасности." },
    { label: "Фрагментация ухудшает производительность, усложняет роутеры и создаёт дыры в безопасности; IPv6 перекладывает нагрузку на отправителей через Path MTU Discovery", correct: true },
    { label: "IPv6-пакеты всегда достаточно маленькие", misconception: "IPv6 тоже поддерживает jumbo-пакеты; правило — архитектурное решение, а не гарантия размера." },
    { label: "Фрагментация зашифрована в IPv6", misconception: "В IPv6 нет встроенного шифрования заголовков." },
  ]}
/>

<Quiz
  id="mid-quiz-2"
  pieceSlug="04-mtu-and-fragmentation"
  lang="ru"
  question="Что такое PMTUD black hole?"
  choices={[
    { label: "Роутер который дропает все пакеты больше 1280 байт", misconception: "PMTUD black hole специфически связан с блокировкой ICMP, а не с порогом размера." },
    { label: "Когда ICMP-сообщения 'packet too big' блокируются firewall'ом, отправитель никогда не узнаёт что нужно уменьшить пакеты и соединения тихо зависают", correct: true },
    { label: "Routing loop поглощающий пакеты", misconception: "Routing loops другие — TTL в конечном счёте их прерывает. Black hole здесь означает тихий дроп." },
    { label: "Ошибка фрагментации IPv6", misconception: "IPv6 запрещает фрагментацию роутерами; black hole специфически касается ICMP-фильтрации препятствующей PMTUD." },
  ]}
/>

</Explanation>

<SequenceDiagram
  label="PMTUD black hole: отправитель передаёт полноразмерный пакет, роутер дропает его и шлёт ICMP 'packet too big' обратно, но firewall тихо выбрасывает этот ICMP, поэтому отправитель не уменьшает размер и продолжает повторно передавать тот же oversized пакет."
  hue="var(--d-network)"
  actors={["Отправитель", "Firewall", "Маршрутизатор"]}
  messages={[
    { from: "Отправитель", to: "Маршрутизатор", label: "1500-байт пакет, DF=1" },
    { from: "Маршрутизатор", to: "Firewall", label: "ICMP packet-too-big" },
    { from: "Firewall", to: "Отправитель", label: "ICMP дропнут (заблокирован)" },
    { from: "Отправитель", to: "Маршрутизатор", label: "повтор 1500-байт (снова!)" },
  ]}
  caption="Роутер дропает oversized пакет и сигнализирует правильный MTU через ICMP, но firewall фильтрующий ICMP проглатывает сигнал. Отправитель не узнаёт что нужно уменьшить — маленькие запросы вписываются и работают, большие зависают навсегда. Исправления не зависящие от ICMP: TCP MSS clamping и PLPMTUD."
/>

<KeyTakeaway>MTU — лимит размера на уровне линка. IPv4-роутеры могут фрагментировать; IPv6-роутеры не могут. Path MTU Discovery узнаёт MTU узкого места на пути через ICMP — но firewall'ы блокирующие ICMP создают тихие PMTUD black holes. Прагматичное исправление — TCP MSS clamping или PLPMTUD.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="04-mtu-and-fragmentation-retrieval-ru"
  lang="ru"
  questions={[
    { q: "Вы запускаете tcpdump и видите TCP-повторные передачи каждые ~1 секунду, потерь не обнаружено, соединение проходит через VPN-туннель. Какова вероятная PMTUD-связанная причина?", a: "VPN-туннели добавляют overhead (например IPSec добавляет ~20+ байт). Path MTU — 1500 байт (Ethernet), но инкапсуляция туннеля заставляет пользовательские пакеты превышать это. Роутер дропает oversized фрейм и шлёт ICMP 'packet too big', но если firewall фильтрует ICMP, отправитель никогда не узнаёт. Он повторно передаёт на 1500 байт вечно, попадая в 1-секундный RTO каждый раз. Исправление: clamp TCP MSS на VPN-интерфейсе или вручную уменьшить MTU VPN-интерфейса." },
    { q: "Почему IPv6 требует чтобы отправитель выполнял PMTUD, тогда как IPv4-роутеры могут фрагментировать на лету?", a: "IPv4 позволял роутерам фрагментировать чтобы вписаться в меньшие MTU, но фрагментация добавляет CPU и сложность на каждом хопе. IPv6 убрал это: если пакет слишком большой, роутер шлёт ICMPv6 'Packet Too Big' отправителю, который уменьшает и повторно передаёт. Это обменивает сложность отправителя на простоту роутера — роутеры форвардят миллионы пакетов в секунду; перекладывание нагрузки на отправителя дешевле. Но это работает только если ICMPv6 не фильтруется." },
    { q: "Что такое TCP MSS clamping и почему его используют вместо опоры на PMTUD?", a: "MSS (Maximum Segment Size) — максимальный TCP-payload который отправитель соглашается использовать, согласовываемый через опцию MSS в SYN/SYN-ACK пакетах. MSS clamping — техника при которой роутер на пути перезаписывает опцию MSS в TCP SYN'ах до безопасного значения учитывающего MTU-overhead линка (например 1452 для PPPoE). Эндпойнты тогда никогда не пытаются отправлять сегменты которые создали бы oversized IP-пакеты. Это устраняет зависимость от ICMP, делая его устойчивым к ICMP-фильтрующим firewall'ам которые вызывают PMTUD black holes." },
  ]}
/>

<Recap lang="ru">
MTU — максимальный размер фрейма на линке. Стандарт Ethernet — 1500 байт; VPN-туннели уменьшают его добавляя overhead заголовков. IPv4-роутеры могут разбивать oversized пакет на меньшие фрагменты, но потеря любого фрагмента вынуждает повторно передавать всю датаграмму. IPv6 полностью запрещает фрагментацию роутерами: если пакет слишком большой, роутер шлёт ICMPv6 «Packet Too Big» и отправитель должен уменьшить. Path MTU Discovery полагается на эти ICMP-сигналы — когда firewall тихо дропает ICMP, отправитель никогда не узнаёт что нужно уменьшить, и большие соединения зависают (PMTUD black hole). Прагматичные исправления: TCP MSS clamping (перезаписывает опцию MSS в SYN'ах до безопасного значения, устойчив к ICMP-фильтрации) и PLPMTUD (RFC 4821, зондирует через TCP-сегменты). Теперь когда вы видите большие загрузки зависающие через VPN при рабочих маленьких запросах — у вас PMTUD black hole, и вы знаете с чего начать диагностику.
</Recap>
