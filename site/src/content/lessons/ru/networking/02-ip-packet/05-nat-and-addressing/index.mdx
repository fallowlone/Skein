---
concepts:
- nat
- cgnat
- rfc1918
- ipv6-slaac
- anycast
- dual-stack
- happy-eyeballs
- stun-turn-ice
deepensInto:
- 06-ip-security-and-operations
estMin: 14
lang: ru
lessonType: topic
level: middle
order: 5
prereqs:
- 04-mtu-and-fragmentation
slug: 05-nat-and-addressing
sources:
- https://datatracker.ietf.org/doc/html/rfc1918
- https://datatracker.ietf.org/doc/html/rfc8200
- https://datatracker.ietf.org/doc/html/rfc8305
- https://datatracker.ietf.org/doc/html/rfc4862
spiral: []
status: ready
summary: NAT расширил IPv4-интернет за пределы 4 миллиардов адресов, но сломал сквозное
  подключение. IPv6, SLAAC, anycast и dual-stack — архитектурно правильный путь вперёд.
title: NAT, адресация и развёртывание IPv6
track: networking
unit: 02-ip-packet
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import NumbersCard from "~/components/layout/NumbersCard.astro";
import SequenceDiagram from "~/components/diagram/SequenceDiagram.astro";
import Infographic from "~/components/diagram/Infographic.astro";
import natTraversalLadder from "./infographics/nat-traversal-ladder.svg?raw";

<Hook>
WebRTC видеозвонки требуют прямого соединения обеих сторон — но типичный пользователь находится за двумя слоями NAT. STUN-сервер должен сообщить каждой стороне её публичный IP и порт. TURN-сервер ретранслирует звонок если прямое соединение не удаётся. Понимание почему существует NAT, как он работает и что он ломает — обязательно для любого фулстек-инженера создающего peer-to-peer или real-time функциональность.
</Hook>

<Crux>NAT делит один публичный IP между многими хостами, продлевая IPv4 — но ломает end-to-end. IPv6 — архитектурное исправление.</Crux>

<Explanation>

## Приватная адресация RFC 1918

32-битное адресное пространство IPv4 вмещает ~4,3 млрд адресов. Они закончились. RFC 1918 зарезервировал три диапазона для приватного использования:

| Диапазон | CIDR | Размер |
|----------|------|--------|
| 10.0.0.0/8 | Class A private | 16,7 миллиона |
| 172.16.0.0/12 | Class B private | 1 миллион |
| 192.168.0.0/16 | Class C private | 65 536 |

Хосты на приватных адресах не могут быть напрямую достигнуты из публичного Интернета. Они могут только инициировать исходящие соединения (после чего NAT-таблица хранит маппинг для ответов).

## Network Address Translation (NAT)

**NAT** (RFC 2663) перезаписывает пакеты на границе сети:

1. **Исходящий пакет:** source IP (приватный, например `192.168.1.10`) + source port (например `45231`) → перезаписывается на публичный IP роутера + уникальный source port (например `203.0.113.5:12345`). Роутер сохраняет этот маппинг.
2. **Входящий ответ:** destination IP + port (`203.0.113.5:12345`) → роутер ищет в NAT-таблице → перезаписывает обратно на `192.168.1.10:45231` и форвардит к внутреннему хосту.

NAT позволил IPv4-интернету вырасти за пределы лимита адресов, но фундаментально сломал **end-to-end адресацию**: публичный Интернет видит один IP для многих внутренних хостов, и внутренние хосты не могут принимать незапрошенные входящие соединения.

**Что ломает NAT:**
- Peer-to-peer протоколы (BitTorrent, VoIP, WebRTC): каждая сторона должна иметь возможность инициировать — NAT разрешает только исходящие соединения.
- Port forwarding: нужно настраивать вручную для каждого сервиса который должен быть доступен снаружи.
- Отслеживание злоупотреблений: единственный публичный IP может скрывать сотни несвязанных пользователей.

Вместе эти три проблемы превращают Интернет из симметричной peer-to-peer-сети в сеть где настоящий «слушатель» — только сторона с публичным IP. Именно поэтому любая real-time функция которую вы создаёте рано или поздно потребует STUN или TURN (протоколы обхода NAT).

## Carrier-Grade NAT (CGNAT)

Мобильные операторы и ISP развёртывают CGNAT чтобы делить один публичный IPv4-адрес между сотнями или тысячами клиентов:

1. Домашний роутер клиента делает NAT в `100.64.x.x` (RFC 6598 shared address space — «пространство адресов NAT64 оператора»).
2. CGNAT оператора делает ещё один NAT на публичный IP.

Двойной NAT усугубляет P2P-проблему. Пользователи CGNAT не могут хостить никакие сервисы на своём IP, и P2P NAT traversal сложнее. Rate-limiting по IP бессмысленен (сотни несвязанных пользователей делят один IP).

## Обход NAT: STUN/TURN/ICE

WebRTC и современные P2P-протоколы используют трёхуровневый подход:

- **STUN** (Session Traversal Utilities for NAT): STUN-сервер сообщает каждому пиру его публичный IP и порт как они видны снаружи NAT. Пиры пытаются соединиться напрямую используя эту информацию.
- **TURN** (Traversal Using Relays around NAT): если прямое соединение невозможно (например симметричный NAT, CGNAT), TURN-сервер ретранслирует весь трафик. Штраф задержки; только fallback.
- **ICE** (Interactive Connectivity Establishment): фреймворк который итерирует через STUN-кандидатов, прямые адреса и TURN пока не найдёт работающий путь.

<Infographic
  svg={natTraversalLadder}
  label="Восходящая лестница из трёх ступеней обхода NAT: STUN узнаёт публичный маппинг и пробует прямое соединение, ICE перебирает кандидатов пока один путь не сработает, а TURN ретранслирует весь трафик как запасной вариант последней надежды."
  caption="Каждая ступень меняет задержку на более высокий шанс соединиться: STUN даёт дешёвый прямой путь, ICE выбирает, а TURN работает всегда, но добавляет задержку релея и стоимость сервера — поэтому пользователи CGNAT так часто скатываются к TURN."
  hue="var(--d-infra)"
/>

<NumbersCard
  id="card-nat"
  title="NAT и IPv6 в числах"
  data-lesson-visual
  stats={[
    { label: "Пул публичных адресов IPv4", value: "~3,7 млрд используемых" },
    { label: "Клиентов CGNAT на публичный IP", value: "100–10 000" },
    { label: "Глобальный трафик IPv6 (2026)", value: "~45–50%" },
    { label: "Веха Google IPv6", value: "50,1% на 2026-03-28" },
    { label: "IPv6 /48 на сайт", value: "281 трлн подсетей /64" },
    { label: "Interface ID SLAAC (расширения приватности)", value: "64 бита, рандомизируется per site" },
  ]}
/>

## IPv6-адреса: структура и SLAAC

IPv6-адреса — 128 бит, записываются как восемь разделённых двоеточиями hex-групп. Сокращения: ведущие нули опускаются, самый длинный пробег нулей заменяется `::`. Пример: `2001:db8::1`.

Типичное выделение: префикс `/48` для сайта → `/64` на подсеть внутри сайта. SLAAC (Stateless Address Autoconfiguration, RFC 4862) позволяет хосту самостоятельно формировать свой адрес:

1. Роутер широковещательно анонсирует `/64`-префикс в Router Advertisement сообщениях.
2. Хост генерирует нижние 64 бита (interface ID) — изначально EUI-64 из MAC-адреса, теперь случайный для приватности (расширения приватности RFC 8981).
3. Хост проверяет дубликаты (Duplicate Address Detection), затем начинает использовать адрес.
4. DHCP не требуется для базового подключения.

**Расширения приватности** (RFC 8981) рандомизируют interface ID per site для предотвращения кросс-сайтового отслеживания через IPv6-адреса. Современные ОС включают это по умолчанию.

## Anycast: один IP, много локаций

**Anycast** анонсирует один и тот же IP-префикс из нескольких географических локаций. BGP маршрутизирует каждого пользователя в POP с наикратчайшим AS-path. Применения: DNS-резолверы (1.1.1.1, 8.8.8.8), CDN edge'ы, корневые серверы имён, сервисы защищённые от DDoS.

Anycast хорошо работает для **stateless-сервисов** (UDP DNS) — каждый пакет может идти в другой POP. Для **stateful-сервисов** (TCP, TLS) короткие флоу нормальны (BGP-маршрутизация стабильна в течение соединения), но длинные флоу требуют либо стабильного BGP, либо репликации состояния сессии между POP'ами (Cloudflare поэтому глобально реплицирует TLS-сессионные тикеты).

## Dual-stack и Happy Eyeballs

Большинство текущих развёртываний работают на **dual-stack**: одновременно включены IPv4 и IPv6. IPv6 в приоритете для доставки контента, IPv4 как fallback.

**Happy Eyeballs v2** (RFC 8305): когда dual-stack хост резолвит имя хоста, он параллельно запускает попытки подключения по IPv4 и IPv6 с небольшим форой для IPv6. Если IPv6 успевает первым — использовать его; если IPv4 побеждает или IPv6 зависает — использовать IPv4, незаметно, без видимой пользователю задержки. Это делает принятие IPv6 безопасным даже когда пути IPv6 имеют периодические проблемы.

<Inset kind="why" lang="ru">
**Почему NAT не обеспечивает безопасность.** NAT скрывает внутренние IP-адреса от публичного Интернета, что некоторые операторы интерпретируют как преимущество безопасности. Но NAT — не firewall: он не инспектирует трафик, не применяет политику и не мешает скомпрометированному внутреннему хосту устанавливать исходящие соединения. Реальный stateful firewall нужен независимо от наличия NAT. Смешение NAT с безопасностью — опасное заблуждение на практике.
</Inset>

<Quiz
  id="mid-quiz-1"
  pieceSlug="05-nat-and-addressing"
  lang="ru"
  question="Почему NAT ломает истинные peer-to-peer протоколы?"
  choices={[
    { label: "NAT слишком медленный для P2P-трафика", misconception: "NAT добавляет микросекунды — пренебрежимо мало для P2P." },
    { label: "NAT разрешает только соединения инициированные изнутри; у публичного Интернета нет способа достичь конкретный внутренний хост если маппинг NAT не был установлен исходящим соединением", correct: true },
    { label: "NAT шифрует трафик которого P2P-протоколы не умеют обрабатывать", misconception: "NAT не шифрует; он перезаписывает адреса." },
    { label: "P2P-протоколы запрещены RFC 1918", misconception: "RFC 1918 определяет приватные диапазоны адресов, ничего о протоколах." },
  ]}
/>

<Quiz
  id="mid-quiz-2"
  pieceSlug="05-nat-and-addressing"
  lang="ru"
  question="Что SLAAC позволяет делать IPv6-хосту?"
  choices={[
    { label: "Получить временный IPv4-адрес от оператора", misconception: "SLAAC только для IPv6 и не требует DHCP." },
    { label: "Сформировать собственный IPv6-адрес из анонсированного роутером /64-префикса плюс самостоятельно сгенерированный interface identifier, без необходимости в DHCP", correct: true },
    { label: "Делить IPv6-адрес с другими хостами в подсети", misconception: "Каждый хост генерирует уникальный interface ID — адреса не делятся." },
    { label: "Заменить STUN для NAT traversal", misconception: "SLAAC касается назначения адресов, а не NAT traversal." },
  ]}
/>

<TradeoffMatrix
  id="sr-tradeoff-1"
  pieceSlug="05-nat-and-addressing"
  lang="ru"
  prompt="Стартап запускает сервисы в 3 облачных регионах и хочет надёжную глобальную адресацию. Выберите стратегию адресации."
  options={[
    { name: "Единственный IPv4 anycast-префикс анонсируемый из всех регионов", summary: "Один IP, BGP выбирает регион.", correct: true, justification: "Один IP везде, BGP маршрутизирует пользователей в ближайший регион, автоматический failover если один регион отзовёт анонс. Отраслевой стандарт для глобальных сервисов. Стоимость: требует собственного публичного ASN + IP-блока + BGP-пирингов — нетривиальная настройка но зрелая операционно." },
    { name: "Регион-специфичные IPv4-адреса, GeoDNS маршрутизирует пользователей", summary: "Маршрутизация на уровне DNS.", justification: "Работает, но DNS-кэш влияет: TTL DNS на стороне пользователя задерживает failover. Плохо на мобильных где IP часто меняется. Дешевле anycast (не нужен BGP) но менее отзывчив к сбоям." },
    { name: "Только IPv6 с anycast", summary: "Современный + anycast.", justification: "Лучший теоретически но ~50% пользователей всё ещё только IPv4 — они не смогут достучаться. Dual-stack обязателен, то есть нужно делать оба." },
    { name: "Регион-специфичный IPv4 с ручными обновлениями DNS", summary: "Всё вручную.", justification: "Максимальный контроль но минимальная надёжность. Сбои требуют человеческого вмешательства. Не деплоить это для чего угодно что должно работать в 3 ночи." },
  ]}
/>

</Explanation>

<SequenceDiagram
  label="NAT перезаписывает адреса на границе: у исходящего пакета внутреннего хоста приватный source перезаписывается NAT-маршрутизатором на публичный IP и порт, а ответ сервера перезаписывается обратно к приватному хосту по сохранённому маппингу."
  hue="var(--d-network)"
  actors={["Хост (приватный)", "NAT-маршрутизатор", "Сервер"]}
  messages={[
    { from: "Хост (приватный)", to: "NAT-маршрутизатор", label: "src 192.168.1.10:45231" },
    { from: "NAT-маршрутизатор", to: "Сервер", label: "src 203.0.113.5:12345 (mapped)" },
    { from: "Сервер", to: "NAT-маршрутизатор", label: "dst 203.0.113.5:12345" },
    { from: "NAT-маршрутизатор", to: "Хост (приватный)", label: "dst 192.168.1.10:45231 (restored)" },
  ]}
  caption="NAT-таблица сохраняет маппинг приватный↔публичный когда исходящий пакет уходит, затем использует его чтобы восстановить исходный приватный адрес в ответе. Поскольку маппинг существует только после исходящего пакета, публичный Интернет не может инициировать входящие соединения — именно это и ломает peer-to-peer."
/>

<KeyTakeaway>NAT прячет множество хостов за одним публичным IP — IPv4 получает передышку, но end-to-end достижимость ломается. P2P требует STUN/TURN/ICE для обхода. IPv6 + SLAAC + Happy Eyeballs — архитектурное исправление.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="05-nat-and-addressing-retrieval-ru"
  lang="ru"
  questions={[
    { q: "Инженер утверждает что нельзя исправить проблемы CGNAT со стороны приложения. Где изъян в этом аргументе?", a: "Убрать sharing адресов нельзя — это развёртывание оператора — но можно смягчить его эффекты. Используйте UDP hole-punching протоколы (STUN/TURN/ICE для WebRTC) чтобы P2P работал через CGNAT. Не rate-limit по IP для обнаружения злоупотреблений: сотни несвязанных пользователей делят один IP, поэтому rate-limit по user ID или device ID. Обеспечьте IPv6-подключение — CGNAT влияет только на IPv4; IPv6 имеет достаточное адресное пространство, давая каждому пользователю уникальный маршрутизируемый адрес." },
    { q: "Почему Happy Eyeballs v2 даёт IPv6 фору но всё равно откатывается на IPv4?", a: "Принятие IPv6 ещё не повсеместно — некоторые пути имеют настроенный но сломанный IPv6 (неправильно настроенные роутеры, фильтрация). Если бы IPv6 всегда выигрывал гонку, пользователи на сломанных IPv6-путях испытывали бы задержки или сбои. Фора (250 мс в RFC 8305) отдаёт предпочтение IPv6 когда он работает, позволяя Интернету мигрировать к IPv6 без ущерба для пользователей на несовершенных путях." },
    { q: "Что такое anycast и какие сервисы его используют?", a: "Anycast анонсирует один и тот же IP-префикс из нескольких географических локаций одновременно. BGP маршрутизирует каждого пользователя в POP с наикратчайшим AS-path. Сервисы: DNS-резолверы (1.1.1.1, 8.8.8.8), CDN edge-ноды, центры DDoS-очистки, корневые серверы имён. Естественно работает для stateless UDP-сервисов (DNS). Для stateful TCP-сервисов короткие флоу нормальны (BGP стабилен в течение соединения); долгоживущие сессии требуют осторожности потому что BGP-реконвергенция может перенаправить mid-flow." },
  ]}
/>

<Recap lang="ru">
Приватные диапазоны RFC 1918 (10/8, 172.16/12, 192.168/16) позволяют организациям повторно использовать адреса внутри; NAT на границе перезаписывает source IP+port чтобы многие внутренние хосты делили один публичный IP. Это продлило жизнь IPv4 но сломало end-to-end подключение: входящие соединения падают без явного port-forwarding, и P2P-протоколы требуют обходных путей STUN/TURN/ICE. CGNAT (NAT операторского масштаба) у мобильных операторов добавляет второй слой NAT. 128-битное адресное пространство IPv6 устраняет необходимость в NAT: каждое устройство получает маршрутизируемый публичный адрес, SLAAC автоконфигурирует без DHCP, и расширения приватности рандомизируют interface ID per site. Anycast использует BGP чтобы маршрутизировать пользователей к географически ближайшей копии сервиса. Dual-stack + Happy Eyeballs v2 позволяют приложениям параллельно гоняться IPv4 и IPv6, мигрируя прозрачно по мере зрелости развёртывания IPv6. Теперь когда WebRTC-звонок падает только у части пользователей — сначала проверьте стоят ли они за CGNAT: этот факт сразу говорит будут ли прямые ICE-кандидаты вообще работать.
</Recap>
