---
concepts:
- ip-packet
- hop-by-hop-routing
- best-effort
- ipv4
- ipv6
- ttl
deepensInto:
- 02-reading-the-ip-header
estMin: 10
lang: ru
lessonType: topic
level: junior
order: 1
prereqs:
- 01-physical-link/01-bits-on-the-wire
slug: 01-the-ip-envelope
sources:
- https://datatracker.ietf.org/doc/html/rfc791
- https://datatracker.ietf.org/doc/html/rfc8200
- https://hpbn.co/building-blocks-of-tcp/
spiral:
- encapsulation
status: ready
summary: IP — адресованный конверт внутри которого путешествует каждый кусок интернет-трафика.
  Ни один роутер не знает полного маршрута, но миллионы работают вместе чтобы доставить
  пакет.
title: Конверт IP
track: networking
unit: 02-ip-packet
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import MetaphorComplete from "~/components/pedagogy/MetaphorComplete.astro";
import NumbersCard from "~/components/layout/NumbersCard.astro";

<Hook>
В момент когда вы кликаете ссылку, ваш браузер пакует запрос в IP-пакет — конверт с вашим адресом и адресом сервера. Радикальная часть: ни один роутер между вами и сервером не знает полного маршрута. Каждый просто читает адрес назначения, смотрит в свою локальную карту и форвардит пакет на следующий хоп.
</Hook>

<Crux>Как пакет находит сервер когда ни один роутер не знает полного маршрута?</Crux>

<Explanation>

## Что делает IP одним предложением

IP — адресованный конверт внутри которого путешествует каждый кусок интернет-трафика. Source-адрес, destination-адрес, payload и маленький заголовок — это IP.

Каждый протокол который вы используете — TCP, UDP, ICMP, каждый видео-кусок, каждая веб-страница — едет внутри IP-пакетов. Пакет — единица маршрутизации для всего Интернета.

## Почтовая метафора

Представьте почтовую систему. Каждое письмо имеет from-адрес и to-адрес. Местные почтовые офисы не знают где Токио, но каждый знает «всё для Азии идёт через этот грузовик». Письма прыгают от локального офиса к региональному к национальному пока не достигнут локального офиса получателя. Ни один офис не держит карту всего мира — только следующий хоп.

IP работает так же. Роутеры держат частичные карты и принимают локальные решения. На каждом хопе роутер читает адрес назначения, проверяет routing table и форвардит пакет наилучшему следующему соседу. После 10–15 таких хопов пакет доходит.

## Один сценарий end-to-end

Ваш браузер уже завершил физический канал (умеет класть биты в провод). Теперь строит IP-пакет для каждого сегмента. Каждый пакет путешествует независимо. Слои выше — TCP, TLS, HTTP — все передают байты IP, который добавляет конверт.

<NumbersCard
  id="card-ip-basics"
  title="Основы IP"
  data-lesson-visual
  stats={[
    { label: "Размер IPv4-адреса", value: "32 бита (4 байта)" },
    { label: "Размер IPv6-адреса", value: "128 бит (16 байт)" },
    { label: "Адресное пространство IPv4", value: "~4,3 млрд" },
    { label: "Типичное количество хопов", value: "10–15 роутеров" },
    { label: "Минимальный IPv4-заголовок", value: "20 байт" },
    { label: "IPv6-заголовок (фиксированный)", value: "40 байт" },
  ]}
/>

## IPv4 vs IPv6

IPv4 использует 32-битные адреса в виде четырёх десятичных октетов (например `93.184.216.34`). IPv6 — 128-битные адреса в виде восьми 16-битных hex-групп (например `2001:db8::1`). Практические различия:

- **Адресное пространство.** IPv4 имеет ~4,3 млрд адресов — исчерпано. IPv6 имеет 340 унциллионов — практически неограниченно.
- **Заголовок.** IPv4 имеет переменный заголовок с checksum пересчитываемым на каждом хопе. IPv6 имеет фиксированный 40-байтный заголовок без checksum (делегировано L2 и L4) и без фрагментации роутерами.
- **Внедрение.** Большинство новых развёртываний работают на dual-stack. IPv6 достиг ~45–50% глобального интернет-трафика к 2026 году.

<Inset kind="why" lang="ru">
**Почему IP connectionless.** IP — best-effort: нет подтверждений, нет пересылок, нет упорядочивания. Роутер заканчивающий место в буфере дропает пакеты молча. Решение было намеренным (RFC 791, 1981): держать network layer простым чтобы он масштабировался на миллиарды устройств, и толкать надёжность вверх к TCP. Именно поэтому все механизмы надёжности живут в TCP или QUIC, а не в самом IP.
</Inset>

<Quiz
  id="jr-quiz-1"
  pieceSlug="01-the-ip-envelope"
  lang="ru"
  question="Что несёт IP-пакет в своём заголовке?"
  choices={[
    { label: "Только адрес назначения", misconception: "Заголовок несёт и source и destination плюс другие поля." },
    { label: "Source-адрес, destination-адрес, TTL, тип протокола, checksum, длину", correct: true },
    { label: "Полный текст HTTP-запроса", misconception: "Это в payload, не в заголовке." },
    { label: "User-agent браузера", misconception: "Application-данные живут намного выше в стеке." },
  ]}
/>

<Quiz
  id="jr-quiz-2"
  pieceSlug="01-the-ip-envelope"
  lang="ru"
  question="Практическая разница между IPv4 и IPv6?"
  choices={[
    { label: "Разные скорости", misconception: "Скорость зависит от сети, а не версии IP." },
    { label: "IPv6 имеет гораздо большее адресное пространство (128-бит vs 32-бит) поэтому может адресовать намного больше устройств, плюс упрощённый заголовок", correct: true },
    { label: "IPv6 зашифрован", misconception: "Ни одна версия не зашифрована по умолчанию; TLS делает это." },
    { label: "IPv6 заменяет TCP", misconception: "IP и TCP — разные слои; обе версии IP несут TCP." },
  ]}
/>

<DragOrder
  id="jr-drag-1"
  pieceSlug="01-the-ip-envelope"
  lang="ru"
  prompt="Расставьте что происходит с пакетом на одном хопе роутера:"
  items={[
    "Пакет приходит на входной интерфейс",
    "Роутер декрементирует TTL на 1; если 0 — пакет дропается",
    "Роутер ищет адрес назначения в routing table",
    "Роутер выбирает следующий хоп (longest prefix match)",
    "Роутер оборачивает пакет в новый link-layer кадр",
    "Роутер отправляет кадр через выходной интерфейс",
  ]}
/>

<MetaphorComplete
  id="jr-metaphor-1"
  pieceSlug="01-the-ip-envelope"
  lang="ru"
  setup="IP-пакет похож на _______ — имеет from-адрес, to-адрес и содержимое внутри."
  accepted={["конверт", "письмо", "посылка", "пакет"]}
  canonical="конверт (или письмо / посылка)"
  explanation="Почтовая аналогия идеально подходит к IP: адресованная обёртка вокруг содержимого, маршрутизируемая хоп за хопом посредниками которые знают только следующий шаг."
/>

</Explanation>

<KeyTakeaway>IP — адресованный конверт Интернета: source IP, destination IP и payload. Ни один роутер не знает полного пути — каждый принимает локальное жадное решение, форвардя к destination хоп за хопом.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="01-the-ip-envelope-retrieval-ru"
  lang="ru"
  questions={[
    { q: "Одним предложением: почему удивительно что Интернет работает учитывая что ни один роутер не знает полного маршрута?", a: "Потому что каждый роутер принимает локальное жадное решение основанное на своём частичном view, а routing-протоколы (BGP, OSPF) держат эти views достаточно согласованными чтобы пакет всегда двигался к назначению — миллионы независимых роутеров кооперируются без центрального координатора." },
    { q: "Почему дизайнеры IP выбрали best-effort доставку вместо надёжной?", a: "Чтобы держать network layer простым и масштабировать на миллиарды устройств. Надёжность делегирована TCP на endpoints где это дешевле и проще чем в каждом роутере на каждом пути." },
    { q: "Что происходит с пакетом у которого TTL достигает нуля?", a: "Роутер дропает пакет и шлёт ICMP 'time exceeded' обратно к источнику. Это предотвращает loops от бесконечного хождения пакетов по неправильно маршрутизированным сетям." },
  ]}
/>

<Recap lang="ru">
IP — универсальный адресованный конверт: каждый кусок интернет-трафика — TCP-сегмент, UDP-датаграмма, ICMP-сообщение — путешествует внутри IP-пакета несущего source и destination адрес. Ни один роутер не знает полного пути; каждый принимает локальное longest-prefix-match решение и передаёт пакет следующему хопу. После 10–15 хопов пакет доходит. IPv6 расширяет эту модель 128-битными адресами (решая кризис адресного пространства) и более простым фиксированным заголовком. Намеренный выбор IP в пользу best-effort connectionless доставки — вот почему TCP, TLS и QUIC все живут выше.
</Recap>
