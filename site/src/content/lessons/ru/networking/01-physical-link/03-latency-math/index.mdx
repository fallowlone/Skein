---
concepts:
- propagation-delay
- rtt
- latency-floor
- submarine-cables
- dwdm
- link-debugging
deepensInto:
- 04-bufferbloat-and-congestion
estMin: 12
lang: ru
lessonType: topic
level: middle
order: 3
prereqs:
- 02-modulation-and-shannon
slug: 03-latency-math
sources:
- https://hpbn.co/primer-on-latency-and-bandwidth/
- https://www.submarinecablemap.com/
- https://en.wikipedia.org/wiki/Propagation_delay
- https://en.wikipedia.org/wiki/Submarine_communications_cable
spiral:
- latency
- rtt
status: ready
summary: География, среда и хопы — каждый добавляет измеримую задержку. Знание чисел
  позволяет отличить баги от физики и проектировать вокруг предела, который нельзя
  снизить.
title: Математика задержки
track: networking
unit: 01-physical-link
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import FadedExample from "~/components/pedagogy/FadedExample.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import NumbersCard from "~/components/layout/NumbersCard.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";
import Infographic from "~/components/diagram/Infographic.astro";
import rttByTechnology from "./infographics/rtt-by-technology.svg?raw";

<Hook>
В тикете написано: «задержка выросла с 30 мс до 500 мс». Это база данных? Сеть? CDN? Прежде чем открыть хоть один лог — нужны предельные числа: задержки, которые физика навязывает каждому маршруту. Зная их, аномалии становятся очевидны.
</Hook>

<Crux>Задержка распространения = расстояние ÷ скорость сигнала. Всё выше этого предела — инженерия: очереди, хопы, сериализация.</Crux>

<Explanation>

## Формула задержки

Односторонняя задержка распространения = расстояние / скорость сигнала.

Свет в вакууме: 300 000 км/с. Свет в стеклянном волокне: ~200 000 км/с (стекло замедляет примерно на 33%). Эти числа дают нижние пределы задержки, которые никакое программное обеспечение не может побить.

<NumbersCard
  id="card-latency"
  title="Нижние пределы задержки по маршрутам"
  data-lesson-visual
  stats={[
    { label: "Нью-Йорк → Лондон (5 500 км)", value: "28 мс мин → 70–90 мс реальный RTT" },
    { label: "Нью-Йорк → Сидней (16 000 км)", value: "80 мс мин → 200–220 мс реальный RTT" },
    { label: "Один континент (2 000 км)", value: "10 мс мин → 20–30 мс RTT" },
    { label: "LAN (100 м, Cat6)", value: "<0.5 мкс распространения" },
    { label: "LEO-спутник (высота 550 км)", value: "~20–50 мс RTT итого" },
    { label: "GEO-спутник (высота 36 000 км)", value: "~600 мс RTT итого" },
  ]}
/>

## Задержка по технологиям

| Технология | Типичный RTT | Пропускная способность | Примечания |
|---|---|---|---|
| GEO-спутник | ~600 мс | 25–100 Мбит/с | Физика: 36 000 км высота |
| LEO-спутник (Starlink) | 20–50 мс | 50–300 Мбит/с | Намного ближе орбита |
| DOCSIS (нагрузка) | 50–200 мс | 100–500 Мбит/с | Bufferbloat при насыщении |
| 4G LTE | 30–60 мс | 10–100 Мбит/с | Планировщик добавляет к распространению |
| 5G sub-6 ГГц | 15–30 мс | 100 Мбит/с – 1 Гбит/с | Лучший планировщик |
| FTTH оптоволокно | 2–10 мс | 1 Гбит/с симметрично | До узла провайдера |
| Гигабитная LAN | &lt;1 мс | 1 Гбит/с | Внутри здания |

Реальный RTT всегда выше предела распространения — маршрутизация увеличивает расстояние, каждый роутер добавляет небольшую задержку обработки (~1 мкс для современного оборудования), а очередизация добавляет миллисекунды под нагрузкой.

<Infographic
  svg={rttByTechnology}
  label="Горизонтальная столбчатая диаграмма типичного реального RTT по технологиям связи: GEO-спутник ~600 мс, DOCSIS под нагрузкой ~125 мс, 4G LTE ~45 мс, LEO Starlink ~35 мс, 5G sub-6 ГГц ~22 мс, FTTH оптоволокно ~6 мс, гигабитная LAN ~1 мс."
  caption="Разброс в 600 раз задаётся высотой орбиты и средой, а не полосой: 36 000 км GEO превосходят всё, и обновление до 100 Гбит/с не сдвинет ни одной полосы — только более короткий путь."
  hue="var(--d-infra)"
/>

## Почему реальный RTT превышает предел

Возьмём Нью-Йорк → Лондон (теоретический предел: 55 мс RTT при 200 000 км/с). Реальный RTT — 70–90 мс, то есть на 30–60% выше предела. Избыток объясняется:

1. **Накладные расходы маршрутизации**: кабельные маршруты — не прямые линии; реальный путь кабеля длиннее ортодромического расстояния.
2. **Обработка в роутере**: каждый промежуточный роутер читает IP-заголовок и делает поиск в таблице маршрутизации (~микросекунды каждый, десятки хопов).
3. **Задержка сериализации**: время для передачи полного пакета (1500 байт) на канал при данной скорости. При 1 Гбит/с: 1500 × 8 / 10⁹ = 12 мкс. Пренебрежимо для высокополосных каналов, значительно на 10 Мбит/с.
4. **Задержка очередизации**: на любом узком месте пакеты ждут за другими. Под нагрузкой это может добавить десятки миллисекунд — рассматривается в уроке 04.

Эти четыре составляющие складываются: пункты 1–3 примерно постоянны для данного маршрута, а пункт 4 — переменный и находится под твоим контролем. Маршрут, выглядящий нормально при малой нагрузке, может показать RTT в 10 раз выше при насыщении — и этот разрыв почти всегда очередь, а не физика.

## Подводный магистральный кабель Интернета

~500 подводных кабелей соединяют континенты. Ключевые факты:

- Каждый кабель несёт 10–30 Тбит/с через **DWDM** (плотное мультиплексирование по длине волны): десятки длин волн на одной паре волокон, каждая длина волны ~100–400 Гбит/с.
- **EDFA** (усилители на основе эрбиевого волокна) регенерируют оптический сигнал каждые ~80 км без конвертации в электрический.
- Повреждения кабелей (якоря кораблей, подводные оползни, намеренный саботаж) происходят ежемесячно; избыточность и перемаршрутизация BGP поддерживают трафик.
- Гиперскейлеры (Google, Meta, Microsoft) владеют частными кабелями (MAREA, Dunant, Curie) для гарантии ёмкости.

<Quiz
  id="quiz-floor"
  pieceSlug="03-latency-math"
  lang="ru"
  question="Разработчик говорит: «Мы только что обновили канал Нью-Йорк → Сидней до 100 Гбит/с, а задержка не улучшилась». Почему?"
  choices={[
    { label: "Не перезапустили TCP-соединения", misconception: "TCP-соединения не нужно перезапускать при обновлении полосы." },
    { label: "Полоса пропускания и задержка распространения независимы. Обновление увеличило throughput, но физический предел 16 000 км (~200 мс RTT) не изменился — только более короткий кабель или более близкий сервер снизит задержку", correct: true },
    { label: "Каналы 100 Гбит/с всегда имеют большую задержку", misconception: "Более высокополосные каналы не добавляют задержку сами по себе." },
    { label: "Роутеры не могут пересылать 100 Гбит/с", misconception: "Современные роутеры обрабатывают 400G+ на порт в производстве." },
  ]}
/>

## Практическая диагностика на канальном уровне

Прежде чем браться за захват пакетов или логи сервисов, потрать две минуты на физический уровень — один плохой кабель или NIC, автоматически переключившийся на 100 Мбит/с, выглядят как медленное приложение и крадут часы отладки.

Когда сетевой путь ведёт себя неожиданно, эти инструменты помогут найти уровень:

```bash
# Linux: статистика интерфейса — ошибки, отброшенные, переполнения
ip -s link show eth0

# Настройки NIC: скорость, дуплекс, автосогласование
ethtool eth0

# Счётчики вендора NIC: ошибки CRC, перезапуски канала
ethtool -S eth0 | grep -E "rx_crc|rx_error|tx_error"

# Сигнал Wi-Fi и скорость
iw dev wlan0 link

# Traceroute с временными метками ICMP (RTT на хоп)
traceroute -I 8.8.8.8
mtr --report 8.8.8.8
```

Интерпретация:
- **rx_crc_errors > 0**: кадры прибывают повреждёнными — плохой кабель, загрязнённый SFP или слабый сигнал. Сначала замените кабель или трансивер.
- **Автосогласование на 100 Мбит/с** вместо ожидаемого 1 Гбит/с: проблема кабеля или порта вынудила откат. Замените кабель.
- **Прыжок RTT в traceroute на хопе N**: задержка добавилась на этом роутере или канале между N-1 и N. Но это не обязательно вина роутера — ограничение скорости ICMP может сделать его похожим на медленный.
- **Wi-Fi «подключено на 54 Мбит/с»**: клиент использует legacy-скорости 802.11g — очень далеко от точки доступа, или старое устройство.

<DragOrder
  id="drag-latency"
  pieceSlug="03-latency-math"
  lang="ru"
  prompt="Упорядочите технологии соединений от наибольшей к наименьшей типичной реальной задержке:"
  items={[
    "GEO-спутник (~600 мс RTT — орбита 36 000 км)",
    "DOCSIS-модем под нагрузкой с bufferbloat (~100–200 мс RTT)",
    "LEO-спутник Starlink (~20–50 мс RTT — орбита всего 550 км)",
    "4G LTE (~30–60 мс RTT)",
    "FTTH оптоволокно домашнее (~5–10 мс RTT до узла провайдера)",
    "Гигабитный LAN Ethernet (<1 мс RTT в здании)",
  ]}
/>

<FadedExample
  client:load
  id="fe-latency"
  pieceSlug="03-latency-math"
  lang="ru"
  title="Расчёт задержки распространения"
  steps={{
    solved: (
      <p>Нью-Йорк → Лондон: 5 500 км, свет в стекле ~200 000 км/с. Односторонняя задержка = 5500 / 200000 = <strong>0.0275 с = 27.5 мс</strong>. Предел RTT = 55 мс. Реальный RTT ~70–90 мс из-за накладных расходов маршрутизации и очередизации.</p>
    ),
    semi: {
      prompt: <p>Нью-Йорк → Токио: примерно 10 800 км подводного кабеля. Односторонняя задержка распространения = 10800 / 200000 = _____ мс. Предел RTT = _____ мс.</p>,
      blanks: [
        { id: "oneway", expected: "54", placeholder: "? мс в одну сторону" },
        { id: "rtt", expected: "108", placeholder: "? мс предел RTT" },
      ],
    },
    blank: {
      prompt: <p>LEO-спутник Starlink на высоте 550 км. Сигнал идёт вверх к спутнику (~550 км) + обратно к наземной станции (~550 км) + наземная сеть (~10 мс). Рассчитайте минимальный RTT.</p>,
      reveal: <p>1100 км / 300 000 км/с (сигнал в воздухе ≈ скорость в вакууме) = 3.7 мс в одну сторону до/от спутника. Туда-обратно только до спутника = ~7.3 мс. Добавить ~10 мс наземной сети → <strong>~18 мс минимальный RTT</strong>. Реальный Starlink — 20–50 мс из-за топологии маршрутизации спутников и очередизации — намного лучше 600 мс GEO.</p>,
    },
  }}
/>

<Inset kind="why" lang="ru">
**Почему traceroute лжёт.** Многие роутеры ограничивают скорость ICMP-пакетов, используемых traceroute — вы можете видеть хопы `* * *` или аномально высокий RTT на хопе, хотя путь за ним в порядке. Используйте `mtr` (Matt's Traceroute) для живого просмотра с агрегированными пробами, или `traceroute -T` (режим TCP), который ACL роутеров реже блокируют. Никогда не делайте вывод «проблема на хопе N» только потому, что traceroute показывает там высокий RTT — только если всё за ним тоже сломано.
</Inset>

</Explanation>

<FlowDiagram
  label="Реальный RTT — это предел распространения плюс четыре добавочные задержки: геометрия маршрутизации, обработка в роутере, сериализация и очередизация"
  hue="var(--d-hardware)"
  perRow={5}
  nodes={[
    { id: "prop", label: "Распространение", sub: "путь ÷ скор." },
    { id: "route", label: "Маршрутизация", sub: "длиннее путь" },
    { id: "proc", label: "Обработка", sub: "~1 мкс/хоп" },
    { id: "ser", label: "Сериализация", sub: "пакет ÷ скор." },
    { id: "queue", label: "Очередизация", sub: "мс под нагр." },
  ]}
  edges={[
    { from: "prop", to: "route", label: "+" },
    { from: "route", to: "proc", label: "+" },
    { from: "proc", to: "ser", label: "+" },
    { from: "ser", to: "queue", label: "= реальный RTT" },
  ]}
  caption="Распространение (расстояние ÷ ~200 000 км/с) — несжимаемый предел; четыре задержки справа — это инженерия, которую можно измерить и часто снизить. Нью-Йорк→Лондон: предел 55 мс становится реальным RTT 70–90 мс."
/>

<KeyTakeaway>Запомните нижние пределы: 28 мс в одну сторону через Атлантику, 80 мс через Тихий океан, ~3.7 мс до LEO-спутника, ~120 мс до GEO. Каждая миллисекунда выше этих пределов — инженерия, которую можно исправить. Сами пределы — нет.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="03-latency-math-retrieval-ru"
  lang="ru"
  questions={[
    { q: "Свет в стекле движется ~200 000 км/с. Нью-Йорк → Сидней ~16 000 км. Какова теоретическая односторонняя задержка и почему реальный RTT 200–220 мс, а не 160 мс?", a: "Теоретическая односторонняя задержка: 16000/200000 = 80 мс. Предел RTT: 160 мс. Реальный RTT 200–220 мс потому что: (1) реальные маршруты кабелей длиннее ортодромических расстояний; (2) каждый роутер добавляет задержку обработки; (3) очередизация на перегруженных каналах добавляет переменную задержку. Превышение в 25–35% выше физического предела — нормально для длинных межконтинентальных путей." },
    { q: "Как DWDM умножает ёмкость волокна без дополнительных волокон?", a: "DWDM (плотное мультиплексирование по длине волны) кодирует множество разных длин волн (цветов) света на одном волокне одновременно. Каждая длина волны несёт независимый оптический канал 100–400 Гбит/с. Одна пара волокон может нести 60–80 длин волн, давая 6–30 Тбит/с агрегированно. Усилители EDFA усиливают все длины волн вместе в каждой точке усиления. DWDM — причина, по которой подводные кабели могут нести 10–30 Тбит/с при небольшом физическом диаметре." },
    { q: "rx_crc_errors не равно нулю на NIC 10G. Что это значит и что делать?", a: "rx_crc_errors считает кадры, которые прибыли с неверной CRC — последовательность проверки кадра (FCS) не совпала. Это значит, что биты были повреждены при передаче: наиболее частые причины — повреждённый кабель, грязный или неисправный SFP-трансивер, или недостаточная оптическая мощность. Первое действие: заменить кабель и/или SFP. Если ошибки CRC прекратились — это была неисправность. Если сохраняются после замены кабеля — проверьте оптическую мощность: если RX ниже порога чувствительности трансивера, проблема в волоконном тракте. Никогда не игнорируйте ненулевые ошибки CRC — они означают молчаливое повреждение данных на линейной скорости." },
  ]}
/>

<Recap lang="ru">
Задержка распространения = расстояние ÷ скорость сигнала (~200 000 км/с в стекле). Нижние пределы: 28 мс в одну сторону через Атлантику, 80 мс через Тихий океан, ~4 мс до LEO-спутника, ~120 мс до GEO. Реальный RTT превышает предел на 25–60% из-за геометрии маршрутизации, обработки в роутерах и очередизации. Магистраль Интернета — ~500 подводных кабелей с DWDM (плотное мультиплексирование по длине волны: десятки длин волн на волокно) и усилители EDFA каждые 80 км. Основные инструменты диагностики: `ip -s link show`, `ethtool -S` (ошибки CRC, состояние канала), `mtr` (traceroute с агрегированными пробами). Когда traceroute показывает высокий RTT (задержка «туда-обратно») на одном хопе, но путь за ним здоров — обычно это ограничение скорости ICMP, а не реальное узкое место. Теперь, когда видишь скачок задержки, сначала сверься с нижними предельными числами: если RTT близок к физическому полу — нужен более короткий маршрут; если RTT далеко выше предела — запускай `mtr` и `ethtool -S` прежде, чем трогать приложение.
</Recap>
