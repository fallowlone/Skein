---
concepts:
- bufferbloat
- active-queue-management
- fq-codel
- cake
- congestion-control
- bbr
- cubic
deepensInto:
- 05-the-datacentre-fabric
estMin: 12
lang: ru
lessonType: topic
level: senior
order: 4
prereqs:
- 03-latency-math
slug: 04-bufferbloat-and-congestion
sources:
- https://www.bufferbloat.net/projects/bloat/wiki/Introduction/
- https://datatracker.ietf.org/doc/html/rfc8290
- https://datatracker.ietf.org/doc/html/rfc8033
- https://research.google/pubs/bbr-congestion-based-congestion-control/
spiral:
- latency
status: ready
summary: Огромные буферы на границе сети заменяют потери задержкой — насыщенный канал
  удерживает 100-500 мс в очереди, ломая интерактивные приложения, пока throughput
  остаётся зелёным.
title: Bufferbloat и перегрузка
track: networking
unit: 01-physical-link
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import FadedExample from "~/components/pedagogy/FadedExample.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import TraceScenario from "~/components/pedagogy/TraceScenario.astro";
import NumbersCard from "~/components/layout/NumbersCard.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";
import Infographic from "~/components/diagram/Infographic.astro";
import latencyUnderLoad from "./infographics/latency-under-load.svg?raw";

<Hook>
Кто-то в команде на видеозвонке, и голос постоянно прерывается — но speed test показывает 300 Мбит/с, зелёный. В это время в фоне идёт резервное копирование в облако. Пропускная способность в норме. Задержка — нет. У этого есть название.
</Hook>

<Crux>Bufferbloat: буферы поглощают пакеты вместо отбрасывания — насыщенный канал создаёт 100–500 мс задержки в очереди.</Crux>

<Explanation>

## Буфер, который съел вашу задержку

Сетевой буфер существует для сглаживания всплесков: когда пакеты приходят быстрее, чем канал их отправляет, они ждут в очереди, а не отбрасываются. В умеренных количествах — это нормально. Bufferbloat — это сбой от избытка.

Граница интернета — кабельные модемы DOCSIS, LTE-модемы, домашние роутеры — это место, где быстрая сторона (гигабитная LAN) встречается с медленной (тарифный аплинк). Это узкое место, и вендоры перестарались с буферами именно там. Модем может держать **100–500 мс** буферизованных данных. При насыщении каждый пакет, стоящий за полной очередью резервной копии, ждёт полную глубину этого буфера.

Симптом узнаётся мгновенно, как только знаешь о нём: idle ping — 20 мс, кто-то начинает загрузку, и ping растёт до 300–500 мс. Throughput выглядит отлично — канал полностью загружен — но каждый интерактивный пакет (DNS-запрос, SYN, видеокадр) стоит за bulk-передачей. Звонки прерываются, игры лагают, страницы зависают — и всё это пока speed test зелёный.

## Почему TCP нуждается в отбрасывании пакетов

Глубинная причина — несоответствие между буферами и управлением перегрузкой. Потерь-ориентированный TCP — CUBIC, стандарт Linux годами — не имеет другого способа узнать, что канал заполнен. Он продолжает увеличивать окно передачи до тех пор, пока пакет не **отбрасывается**, воспринимает потерю как «труба полна» и откатывается.

Правильно настроенный буфер отбрасывает пакет рано, когда очередь ещё мелкая, так что TCP получает сигнал перегрузки при ещё низкой задержке. Огромный буфер **поглощает** пакет вместо этого. TCP не видит потери, предполагает, что есть место, и отправляет ещё данных — которые гигантский буфер тоже поглощает. Окно раздувается, пока буфер не заполняется полностью, на сотни миллисекунд. Вендоры думали, что «никогда не терять пакеты» — безопасный выбор. Это прямо противоположно истине: скрывая сигнал перегрузки, избыточная буферизация гарантирует глубокую очередь.

<NumbersCard
  id="card-bufferbloat"
  title="Bufferbloat: быстрый справочник"
  data-lesson-visual
  stats={[
    { label: "Глубина буфера (DOCSIS / LTE)", value: "100–500 мс данных в очереди" },
    { label: "Ping idle vs насыщение (без AQM)", value: "20 мс → 300–500 мс" },
    { label: "Ping насыщение с fq_codel / CAKE", value: "держится ниже ~30 мс" },
    { label: "CUBIC через GEO (~600 мс RTT)", value: "голодает — окна отправлены до возврата потери" },
    { label: "Восстановление throughput BBR на GEO", value: "большая часть ёмкости канала, независимо от потерь" },
    { label: "RTT LEO (Starlink, орбита ~550 км)", value: "~50–60 мс — CUBIC работает как наземный" },
  ]}
/>

## Почему это продолжается

Bufferbloat известен с 1980-х годов, но он везде до сих пор. Три причины:

1. **Неверный стимул.** Вендоры воспринимали отбрасывание пакетов как дефект и перестарались с буферами, чтобы «избегать потерь» — неверное исправление, потому что потеря *и есть* сигнал.
2. **Невидимая метрика.** Домашние пользователи измеряют throughput, не задержку под нагрузкой. Speed test работает на иначе-простаивающем канале и никогда не видит bloat. Проблема проявляется только когда интерактивное приложение борется с bulk-передачей.
3. **Задержка развёртывания.** Исправление поставляется в прошивке. Нужен роутер с Smart Queue Management (SQM), а много операторского оборудования никогда не получает это обновление.

Само исправление несложное.

## Active Queue Management

Active Queue Management (AQM) — в маркетинге Smart Queue Management на домашних роутерах — отбрасывает или ECN-маркирует пакеты **рано и честно**, до того как очередь вырастает, восстанавливая сигнал перегрузки, который нужен TCP.

- **fq_codel** (RFC 8290) — flow-queued CoDel. CoDel («Controlled Delay») отслеживает время *ожидания* пакетов в очереди, не их количество; как только время ожидания превышает цель (~5 мс) слишком долго, начинается отбрасывание. Flow-queue разделяет потоки на отдельные подочереди, чтобы один bulk-upload не заглушал latency-sensitive поток.
- **PIE** (RFC 8033) — Proportional Integral controller Enhanced. Оценивает задержку очереди и отбрасывает с вероятностью, настроенной на удержание задержки около цели. PIE — это AQM, обязательный для DOCSIS 3.1, поэтому он в современных кабельных модемах.
- **CAKE** — Common Applications Kept Enhanced. fq_codel плюс встроенное формирование полосы, честность per-host и компенсация framing DOCSIS/ATM. Его обычно выбирают в домашних роутер-проектах (OpenWrt).

Общая идея: ограничивать *задержку очереди*, не длину очереди. При полном насыщении канал с CAKE или fq_codel держит ping ниже ~30 мс вместо раздувания до 500 мс.

<Infographic
  svg={latencyUnderLoad}
  label="Столбчатая диаграмма пинга под нагрузкой: холостой канал — 20 мс, тот же канал при насыщении через раздутый буфер взлетает до 500 мс, а при насыщении с fq_codel или CAKE остаётся 30 мс."
  caption="Само насыщение не повышает задержку — её повышает раздутый буфер. AQM удерживает задержку очереди около холостой (~30 мс) на том же насыщенном канале, который без него раздувается до 500 мс."
  hue="var(--d-infra)"
/>

<Inset kind="why" lang="ru">
**Почему SQM нужно формировать ниже линейной скорости.** AQM может управлять только очередью, которой владеет. Если узкое место находится внутри модема ISP, очередь вашего роутера никогда не заполняется — пакеты проходят через него и накапливаются в другом месте, где у вас нет контроля. Поэтому SQM намеренно формирует egress до ~90–95% реальной скорости аплинка. Это перемещает узкое место обратно в роутер, где fq_codel или CAKE им управляет. Вы теряете крупицу пиковой пропускной способности в обмен на очередь, которую реально можете дисциплинировать — почти всегда правильный компромисс для интерактивного домохозяйства.
</Inset>

## Когда проблема не в буфере: BBR vs CUBIC

Управление перегрузкой также ломается на путях с большим RTT, и там ответ — другой алгоритм, а не другая очередь.

LEO-спутник (Starlink, орбита ~550 км, ~50–60 мс RTT) ведёт себя как наземный канал — потерь-ориентированный CUBIC работает нормально. GEO-спутник (~36 000 км орбита, ~600 мс RTT) — нет. При петле обратной связи 600 мс к тому времени, как сигнал о потере возвращается к отправителю, **несколько полных окон уже переданы**. Потерь-ориентированное управление реагирует слишком поздно; CUBIC разгоняется медленно и никогда не заполняет канал — он голодает.

**BBR** (Bottleneck Bandwidth and Round-trip propagation time) полностью обходит потери. Он активно зондирует пропускную способность и минимальный RTT пути, строит модель узкого места и **ведёт** отправки по этой модели. Поскольку он не ждёт потери, задержка обратной связи в 600 мс больше не калечит его, и BBR восстанавливает большую часть ёмкости GEO-канала. Та же независимость от потерь делает BBR сильным на любом потерявшем пути — сотовая связь, перегруженный Wi-Fi.

Эти два аспекта встречаются у сотовой вышки: мобильные операторы развёртывают AQM в стиле CAKE на радиоячейке, чтобы буфер для пользователей, делящих одну ячейку, оставался дисциплинированным. Паттерн для запоминания: **BBR + AQM + маленькие буферы** выигрывает на high-latency или lossy путях; **CUBIC + разумно настроенный буфер** нормален на наземных проводных каналах.

<Quiz
  id="quiz-bufferbloat"
  pieceSlug="04-bufferbloat-and-congestion"
  lang="ru"
  question="Модем буферизует 300 мс данных и 'никогда не отбрасывает пакеты.' Почему это делает задержку хуже, а не лучше?"
  choices={[
    { label: "Отбрасывание пакетов всегда дефект, поэтому модем без потерь строго лучше", misconception: "Потеря — это сигнал перегрузки, от которого зависит потерь-ориентированный TCP. Его подавление и есть причина bufferbloat, а не особенность." },
    { label: "Потерь-ориентированный TCP узнаёт, что канал заполнен, только когда пакет отброшен. Огромный буфер поглощает пакеты вместо их отбрасывания, поэтому TCP раздувает окно до тех пор, пока буфер не оказывается на сотни мс глубины", correct: true },
    { label: "Больше буфера означает медленнее память, что добавляет задержку", misconception: "Обращение к памяти буфера — наносекунды, незначимо. Задержка — это queueing delay: пакеты, ожидающие своей очереди за другими пакетами." },
    { label: "CPU модема слишком медленный, чтобы перенаправить 300 мс пакетов", misconception: "Скорость перенаправления не проблема; пакеты буферизованы намеренно, ожидая своей очереди за полным буфером." },
  ]}
/>

<TradeoffMatrix
  id="tradeoff-bufferbloat"
  pieceSlug="04-bufferbloat-and-congestion"
  lang="ru"
  prompt="В домашней сети видеозвонки прерываются при каждой загрузке резервной копии. Выберите исправление."
  options={[
    { name: "Перейти на более быстрый тарифный план", summary: "Купить больше полосы.", justification: "Больше полосы поднимает потолок насыщения, но не убирает огромный буфер — резервная копия всё равно насытит новый канал, и ping снова раздуется. Throughput никогда не был проблемой." },
    { name: "Включить SQM (CAKE / fq_codel) на роутере, ограничив ниже линейной скорости", summary: "Active Queue Management.", correct: true, justification: "Перемещает узкое место в очередь, которой управляет роутер, отбрасывает/маркирует рано, держит queueing delay ниже ~30 мс даже при насыщении. Прямо атакует bufferbloat." },
    { name: "Запланировать резервные копии на ночь", summary: "Избегать конкуренции.", justification: "Workaround, не исправление. Скрывает симптом и ломается при любой другой bulk-передаче (обновление ОС, синхронизация фото) во время звонка. Буфер остаётся раздутым." },
    { name: "Перевести все устройства на проводное подключение", summary: "Убрать Wi-Fi.", justification: "Помогает при Wi-Fi-специфичной конкуренции, но edge-узкое место и его огромный буфер находятся upstream от LAN полностью — проводной или беспроводной, очередь модема всё равно заполнится при насыщении." },
  ]}
/>

<TraceScenario
  id="trace-bufferbloat"
  pieceSlug="04-bufferbloat-and-congestion"
  lang="ru"
  scenario="Удалённый сотрудник жалуется, что видеозвонки зависают каждый день после обеда. Speed test идеален. Пройди диагностику."
  steps={[
    { prompt: "Шаг 1: speed test показывает 300 Мбит/с вниз / 40 Мбит/с вверх, зелёный. Что он упускает?", reveal: "Стандартный speed test работает на иначе-простаивающем канале — измеряет throughput, никогда задержку под нагрузкой. Bufferbloat появляется только когда bulk-передача и интерактивный поток конкурируют. Правильный тест запускает ping во время насыщения канала (тест bufferbloat на Waveform или DSLReports, или ручной ping во время загрузки)." },
    { prompt: "Шаг 2: idle ping до 8.8.8.8 — 18 мс. Начинаем большую загрузку и пингуем снова. Ping растёт до 420 мс. Что происходит?", reveal: "Классический bufferbloat. Загрузка насыщает аплинк; огромный буфер модема ставит в очередь сотни мс данных. Каждый интерактивный пакет — включая аудиофреймы видеозвонка — стоит за этой очередью. Throughput в порядке; queueing delay — убийца. Зависания совпадают с послеполуденными backup или sync задачами." },
    { prompt: "Шаг 3: роутер поддерживает SQM. Как настроить?", reveal: "Включить SQM (предпочтительно CAKE, fq_codel как запасной вариант) и установить shaped egress rate ~90–95% от реального аплинка — например, 36 Мбит/с для канала 40 Мбит/с. Формирование ниже линейной скорости перемещает узкое место очереди в роутер, где CAKE ею управляет. Установить link-layer adaptation в DOCSIS для кабельного подключения." },
    { prompt: "Шаг 4: после включения CAKE снова запустить тест ping под насыщением. Что должно измениться и чем пришлось пожертвовать?", reveal: "Ping под насыщением должен теперь держаться ниже ~30 мс вместо 420 мс — звонки перестанут зависать. Компромисс: пиковый throughput загрузки падает на ~5–10% от урезания скорости. Для интерактивного домохозяйства это правильный компромисс. Если модем — единственное узкое место и его нельзя обойти (нет SQM-совместимого роутера в bridge mode), исправление требует прошивки с PIE/AQM на стороне модема." },
  ]}
/>

<FadedExample
  client:load
  id="fe-bbr-04"
  pieceSlug="04-bufferbloat-and-congestion"
  lang="ru"
  title="Throughput BBR на GEO-спутнике"
  steps={{
    solved: (
      <p>GEO-спутник: канал 25 Мбит/с, RTT = 600 мс. BDP = 25×10⁶ × 0.6 / 8 = <strong>1,875 МБ</strong>. Окно CUBIC должно вырасти до 1,875 МБ неподтверждённых данных перед каким-либо сигналом о потере. С ростом по умолчанию это занимает много round trip — канал остаётся недозагруженным. BBR зондирует пропускную способность напрямую и заполняет канал без ожидания потери.</p>
    ),
    semi: {
      prompt: <p>LEO-спутник (Starlink): канал 150 Мбит/с, RTT = 50 мс. BDP = 150×10⁶ × 0.05 / 8 = _____ КБ. Практичен ли CUBIC здесь?</p>,
      blanks: [
        { id: "bdp", expected: /^937\.?5?$/, placeholder: "? КБ" },
      ],
    },
    blank: {
      prompt: <p>Межконтинентальная оптика: канал 10 Гбит/с, RTT = 160 мс (NY→Сидней). BDP = ? МБ. Объясни, почему receive window по умолчанию в 64 КБ катастрофически мал здесь.</p>,
      reveal: <p>BDP = 10×10⁹ × 0.16 / 8 = <strong>200 МБ</strong>. Receive window 64 КБ — это 200 МБ / 64 КБ = в 3 125 раз меньше нужного. TCP останавливается после отправки 64 КБ, затем ждёт 160 мс ACK перед отправкой следующих 64 КБ — эффективный throughput: 64 КБ / 0.16 с = ~3,2 Мбит/с на канале 10 Гбит/с. Современные ядра авто-настраивают буферы; BBR явно моделирует BDP. Window scaling (RFC 1323) поднимает cap до 1 ГБ.</p>,
    },
  }}
/>

</Explanation>

<FlowDiagram
  label="Петля обратной связи bufferbloat: огромный буфер поглощает пакет вместо отбрасывания, потерь-ориентированный TCP не видит сигнала перегрузки, раздувает окно, и очередь растёт глубже, что замыкается обратно на буфер"
  hue="var(--d-hardware)"
  perRow={4}
  nodes={[
    { id: "absorb", label: "Буфер поглощает", sub: "нет потери" },
    { id: "nosignal", label: "TCP видит", sub: "нет потери" },
    { id: "inflate", label: "Окно", sub: "раздувается" },
    { id: "deeper", label: "Очередь", sub: "сотни мс" },
  ]}
  edges={[
    { from: "absorb", to: "nosignal" },
    { from: "nosignal", to: "inflate" },
    { from: "inflate", to: "deeper" },
    { from: "deeper", to: "absorb", label: "замыкается" },
  ]}
  caption="Поскольку потеря — это сигнал перегрузки, её подавление разгоняет петлю: потерь-ориентированный CUBIC раздувает окно, а буфер поглощает, пока queueing delay не достигнет сотен мс. AQM (CAKE, fq_codel, PIE) разрывает петлю, отбрасывая рано, до того как очередь вырастает."
/>

<KeyTakeaway>Bufferbloat заменяет потери задержкой: огромные буферы на границе сети скрывают сигнал перегрузки, нужный потерь-ориентированному TCP, поэтому насыщенный канал выстраивает 100–500 мс очереди. AQM (CAKE, fq_codel, PIE) отбрасывает рано и ограничивает queueing delay.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="04-bufferbloat-and-congestion-retrieval"
  lang="ru"
  questions={[
    { q: "Объясни bufferbloat и почему он сохраняется, несмотря на то что известен с 1980-х.", a: "Bufferbloat: избыточная буферизация на сетевой границе — кабельные модемы DOCSIS, LTE-модемы, домашние роутеры держат 100–500 мс данных в очереди. Потерь-ориентированный TCP вроде CUBIC полагается на отбрасывание пакетов как сигнал перегрузки; огромные буферы откладывают этот сигнал, позволяя TCP раздувать окно и закачивать всё больше данных в очередь, так что задержка раздувается с 20 мс до 300–500 мс, пока throughput выглядит зелёным. Сохраняется потому что вендоры гнались за «без потерь» (неверное исправление — потеря это сигнал); домашние пользователи измеряют throughput, не задержку, и проблема невидима пока интерактивные приложения не страдают; исправление требует прошивки роутера с SQM, которую операторское оборудование часто никогда не получает." },
    { q: "Что делает Active Queue Management иначе, чем plain FIFO-буфер? Назови три AQM-алгоритма.", a: "Plain FIFO отбрасывает только когда полностью заполнен (tail drop), что на огромном буфере означает сотни мс задержки до того, как TCP получит хоть один сигнал. AQM отбрасывает или ECN-маркирует пакеты рано и честно, до того как очередь вырастает, так что сигнал перегрузки достигает отправителя при ещё низкой задержке. Ключевая идея — ограничивать queueing delay, а не длину очереди. Три алгоритма: fq_codel (RFC 8290 — flow-queued Controlled Delay, отбрасывает при превышении дождевого времени пакета ~5 мс, изолирует потоки в подочереди); PIE (RFC 8033 — Proportional Integral controller Enhanced, отбрасывает с вероятностью, настроенной на удержание задержки около цели, обязателен в DOCSIS 3.1); CAKE (fq_codel плюс формирование полосы и честность per-host, популярен на роутерах OpenWrt)." },
    { q: "Почему потерь-ориентированный CUBIC голодает на GEO-спутниковом канале, а BBR восстанавливает большую часть throughput?", a: "GEO-спутник имеет ~600 мс RTT (орбита 36 000 км). Потерь-ориентированный CUBIC узнаёт о перегрузке только при потере пакета, но при петле обратной связи 600 мс несколько полных окон передаются прежде чем сигнал о потере вернётся к отправителю — алгоритм реагирует слишком поздно и никогда не заполняет канал, голодая. BBR (Bottleneck Bandwidth and Round-trip propagation time) не зависит от потерь: он активно зондирует пропускную способность и минимальный RTT пути, строит модель узкого места и ведёт отправки по этой модели. Не дожидаясь потери, большая задержка обратной связи больше не калечит его, и BBR восстанавливает большую часть ёмкости GEO-канала. Та же независимость от потерь делает BBR сильным на lossy-путях — сотовая связь, перегруженный Wi-Fi. LEO (~50–60 мс RTT) ведёт себя как наземный, CUBIC там работает нормально." },
  ]}
/>

<Recap lang="ru">
Bufferbloat — это избыточная буферизация на сетевой границе: DOCSIS-модемы, LTE-модемы, домашние роутеры держат 100–500 мс данных в очереди. Потерям-ориентированный TCP вроде CUBIC нуждается в раннем отбрасывании пакетов для обнаружения перегрузки; огромный буфер поглощает потерю, поэтому TCP раздувает окно пока ping не раздуется с 20 мс до 300–500 мс при зелёном throughput. Сохраняется потому что вендоры гнались за «без потерь», пользователи измеряют throughput а не задержку, и исправление живёт в прошивке. AQM — Active Queue Management (управление активной очередью) — fq_codel (RFC 8290), PIE (RFC 8033), CAKE — отбрасывает или маркирует рано и честно, удерживая queueing delay ниже ~30 мс даже при насыщении; SQM формирует ниже линейной скорости чтобы владеть узкой очередью. На путях с большим RTT ответ — другой алгоритм: BBR зондирует пропускную способность и RTT вместо ожидания потери, восстанавливая throughput на GEO-спутнике где CUBIC голодает. Теперь, когда speed test зелёный, а видеозвонки зависают — сразу запускай тест задержки под нагрузкой. Скачок ping до 400 мс при активной загрузке — это bufferbloat, и лечится настройкой роутера, а не правкой кода.
</Recap>
