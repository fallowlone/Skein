---
concepts:
- clos-topology
- spine-leaf
- ecmp
- roce
- lossless-ethernet
- smartnic-dpu
- datacentre-cooling
deepensInto:
- 06-the-physical-frontier
estMin: 13
lang: ru
lessonType: topic
level: senior
order: 5
prereqs:
- 04-bufferbloat-and-congestion
slug: 05-the-datacentre-fabric
sources:
- https://www.ieee802.org/3/df/
- https://www.juniper.net/documentation/us/en/software/junos/cli-reference/topics/topic-map/clos-ip-fabric.html
- https://docs.nvidia.com/networking/display/mlnxofedv461000/rdma+over+converged+ethernet+(roce)
- https://aws.amazon.com/ec2/nitro/
spiral:
- bandwidth
status: ready
summary: Топология Clos spine-leaf, RoCE для GPU-кластеров, оптика 800G, NIC с обходом
  ядра и потолок по питанию и охлаждению — физический уровень в гиперскейле.
title: Фабрика дата-центра
track: networking
unit: 01-physical-link
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import FadedExample from "~/components/pedagogy/FadedExample.tsx";
import NumbersCard from "~/components/layout/NumbersCard.astro";
import TraceScenario from "~/components/pedagogy/TraceScenario.astro";
import DebugLog from "~/components/pedagogy/DebugLog.astro";
import DesignPrompt from "~/components/pedagogy/DesignPrompt.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";
import Infographic from "~/components/diagram/Infographic.astro";
import coolingCeiling from "./infographics/cooling-ceiling.svg?raw";

<Hook>
Задача GPU-тренировки, которая должна занимать 6 часов, идёт 18. GPU простаивают 90% времени, ожидая. С моделью или кодом всё в порядке — узкое место в фабрике дата-центра, физической сети коммутаторов и оптики, которая переносит градиенты между машинами.
</Hook>

<Crux>Гиперскейловая фабрика — это Clos spine-leaf mesh: достаточно полосы для любого сервера, чтобы говорить с любым другим на полной скорости.</Crux>

<Explanation>

## Топология Clos: spine и leaf

Классическая двух- или трёхуровневая сеть Clos заменяет один большой коммутатор — единую точку отказа и кошмар hardware-инженерии — сеткой commodity-коммутаторов. В дизайне **spine-leaf** каждый top-of-rack (ToR) **leaf**-коммутатор имеет один аплинк к каждому **spine**-коммутатору. Два leaf-коммутатора не соединены напрямую; весь трафик проходит через spine.

Это даёт три вещи сразу. **Несжимаемая bisection bandwidth**: если N leaf и M spine, и каждый leaf имеет один M-скоростной аплинк к каждому spine, то при делении фабрики пополам каждая сторона имеет N/2 × M аплинков — любой сервер может насытить свой NIC к любому другому одновременно. **Никаких spanning-tree петель**: ECMP (Equal-Cost Multi-Path) хеширует 5-tuple каждого потока по доступным spine-аплинкам, все пути активны одновременно. **Горизонтальное масштабирование**: добавьте spine для расширения bisection bandwidth; добавьте leaf для расширения серверных портов.

Современные гиперскейловые фабрики используют **400G Ethernet** между leaf и spine, а в передовых сборках уже **800G** (IEEE 802.3df, утверждён в феврале 2024). Аплинки — Direct Attach Copper (DAC) внутри стойки и заранее терминированные **MTP/MPO** многоволоконные магистральные кабели между стойками и рядами.

<NumbersCard
  id="card-fabric"
  title="Быстрый справочник по гиперскейловой фабрике"
  data-lesson-visual
  stats={[
    { label: "Скорость spine↔leaf (2024)", value: "400G Ethernet; 800G в передовых сборках" },
    { label: "Серверный NIC (GPU-кластер)", value: "100–400 Гбит/с RDMA NIC" },
    { label: "Цель oversubscription (AI-обучение)", value: "1:1 (полная bisection bandwidth)" },
    { label: "Риск PFC pause storm (RoCE)", value: "замораживает регион фабрики при неверной настройке" },
    { label: "Потребление GPU-стойки", value: "50–100 кВт (потолок воздушного охлаждения ~15 кВт)" },
    { label: "Стандарт 800G Ethernet", value: "IEEE 802.3df, утверждён февраль 2024" },
  ]}
/>

## RoCE и lossless Ethernet

Задумывался, почему задача ML-обучения на GPU-кластере в облаке работает втрое медленнее, чем на том же железе в приватном кластере? Ответ чаще всего здесь: облако запускает коллективную коммуникацию через TCP и ядро, приватный кластер — через RDMA (Remote Direct Memory Access, прямой доступ к удалённой памяти без участия CPU) напрямую по безпотерной фабрике. Разница не в GPU; разница в сетевом контракте.

Обучение GPU требует коллективной коммуникации — каждый узел должен обмениваться обновлениями градиентов с каждым другим в операциях вроде AllReduce. При обычном TCP ядро копирует данные: GPU → CPU → NIC → сеть → NIC → CPU → GPU. Двойное копирование сжигает циклы CPU и добавляет задержку, сериализуя коллектив и вынуждая GPU простаивать.

**RDMA (Remote Direct Memory Access)** позволяет NIC записывать напрямую в память удалённой машины без участия CPU и без копирования. Над InfiniBand это был стандарт; над стандартным Ethernet это стало **RoCE (RDMA over Converged Ethernet)**. Проблема: RDMA не имеет встроенной повторной передачи. Один потерянный пакет стопорит или прерывает операцию RDMA, поэтому фабрика должна быть **без потерь**.

Два механизма делают Ethernet без потерь:

- **PFC (Priority Flow Control)** — когда буфер коммутатора заполняется сверх порога, он отправляет PAUSE-кадр upstream на основе traffic-class, применяя backpressure до самого отправителя. Пакет не теряется; порт отправителя блокируется.
- **ECN (Explicit Congestion Notification)** — при росте очереди коммутатор маркирует пакеты битом перегрузки (вместо отбрасывания). Получатель отражает метку обратно к отправителю (DCQCN в RoCE v2), который снижает скорость до того, как PFC вступает в действие.

Lossless Ethernet не автоматический — требует точной настройки per-port, per-priority flow-control параметров. Агрессивный PFC создаёт **pause storms** (backpressure распространяется по фабрике, замораживая несвязанные потоки) или **deadlock** (циклическая зависимость заблокированных портов). Поэтому GPU-кластеры обычно на физически изолированной lossless-фабрике, отдельной от общей IP-сети.

<Inset kind="why" lang="ru">
**Почему существуют SmartNIC и DPU.** При 400 Гбит/с хост-CPU больше не справляется с обработкой пакетов для виртуализированных тенантов — NIC один может генерировать больше прерываний, чем CPU обработает. Решение — полностью вынести работу с хоста. **SmartNIC** (например, NVIDIA ConnectX-7) обрабатывает SR-IOV, VXLAN encap/decap и traffic policing в кремнии NIC. **DPU** (Data Processing Unit, например, AWS Nitro, NVIDIA BlueField) идёт дальше: запускает полноценную ОС на ARM-ядре внутри NIC, берёт на себя весь стек VM-сети — security groups, VPC-маршрутизацию, зашифрованный overlay — с нулём CPU-циклов от тенанта. AWS Nitro выносит весь I/O сети и хранилища с хост-CPU, отдавая тенантам 100% оплаченного CPU.
</Inset>

## Питание и охлаждение: реальный потолок

Стандартная 42U-стойка с 1U-серверами потребляет 5–15 кВт. Стойка с восемью GPU H100 потребляет 50–100 кВт. **Воздушное охлаждение** обрабатывает примерно 15 кВт на стойку (конверт ASHRAE Class A2). Стойка 100 кВт требует **жидкостного охлаждения**.

<Infographic
  svg={coolingCeiling}
  label="Столбчатая диаграмма сравнивает потребление стойки с потолком воздушного охлаждения: обычная стойка 1U-серверов потребляет до 15 кВт, потолок воздушного охлаждения около 15 кВт, а GPU-стойка из восьми H100 потребляет до 100 кВт"
  caption="GPU-стойка превышает потолок воздушного охлаждения примерно в 7 раз, поэтому жидкостное охлаждение перестаёт быть опциональным."
  hue="var(--d-infra)"
/>

Доминируют два подхода. **Direct-to-chip (DtC)** проводит хладагент через холодные пластины, прикреплённые напрямую к кристаллу GPU и регуляторам напряжения. Воздух всё ещё обрабатывает оставшиеся ~20% тепла стойки (накопители, NIC, вентиляторы). **Полное погружение** погружает серверные платы в диэлектрическую жидкость (инженерное масло или фторуглерод). Погружение обрабатывает 100% тепла стойки без вентиляторов.

Жёсткое ограничение для новых GPU-развёртываний — часто не порты коммутаторов или оптика, а **мощность питания**. Зал дата-центра, рассчитанный на 10 кВт/стойку, уже оплачен в HVAC и PDU; дооснащение под 100 кВт/стойку означает замену питания, контуров охлаждения и, возможно, трансформатора здания. Поэтому гиперскейлеры теперь покупают или строят объекты специально для GPU-плотности с нуля.

<TraceScenario
  id="trace-fabric"
  pieceSlug="05-the-datacentre-fabric"
  lang="ru"
  scenario="Распределённая задача обучения на 64 GPU (8 серверов × 8 GPU) выполняется в 3× медленнее, чем ожидается. Диагностируй узкое место фабрики."
  steps={[
    { prompt: "Шаг 1: utilisation GPU — 35%. Сеть — узкое место?", reveal: "Utilisation GPU ниже ~80% во время обучения почти всегда означает, что GPU ждут данные — либо data loader, либо сетевой коллектив. AllReduce во время backprop — all-to-all, поэтому сетевой стол сериализует весь обучающий шаг. Проверь профайлер обучающего фреймворка на время ожидания 'NCCL' — если > 50% времени шага, сеть — проблема." },
    { prompt: "Шаг 2: время ожидания NCCL — 65% каждого обучающего шага. Видишь PFC PAUSE-кадры на каждом leaf-аплинке. Что это означает?", reveal: "PFC PAUSE-кадры указывают на backpressure в фабрике — RoCE-трафик заполняет буферы коммутаторов. Либо фабрика oversubscribed (больше GPU NIC, чем поддерживает uplink-полоса), либо ограничение скорости DCQCN настроено слишком агрессивно. Проверь: leaf имеет 400G аплинки, серверы имеют 200 Гбит/с NIC: 8 серверов × 200 Гбит/с = 1,6 Тбит/с вниз, нужно минимум 4 × 400G аплинка на leaf для 1:1." },
    { prompt: "Шаг 3: leaf имеет 2 × 400G аплинка, но 8 × 200G серверных NIC. Каков коэффициент oversubscription и что исправить?", reveal: "Oversubscription = всего downlink / всего uplink = (8 × 200G) / (2 × 400G) = 1,6 Тбит/с / 800 Гбит/с = 2:1. Для AllReduce на 64 GPU oversubscription 2:1 означает, что одновременно может передаваться только половина трафика градиентов — 3× медленнее, чем non-blocking фабрика. Решение: добавить ещё 2 400G аплинка к каждому leaf (достичь 1:1), или направить задачи обучения в pod фабрики с bisection bandwidth 1:1." },
    { prompt: "Шаг 4: oversubscription исправлен. Pause-кадры прекратились, но обучение всё ещё в 1,5× медленнее. Что дальше?", reveal: "С правильным oversubscription проверь пороги ECN-маркировки DCQCN. Если ECN-метки срабатывают слишком агрессивно, отправители RDMA снижают скорость до реальной перегрузки очереди, ограничивая throughput. Также убедись, что priority-классы PFC правильно разделены: RoCE-трафик должен быть на выделенном lossless priority-классе. Неверный priority map отправляет RoCE в тот же класс, что TCP-трафик, и PFC начинает влиять на общий трафик вместо только RDMA." },
  ]}
/>

<DebugLog
  id="debug-fabric"
  pieceSlug="05-the-datacentre-fabric"
  lang="ru"
  title="Здоровье фабрики: диагностика leaf-аплинка"
  logs={[
    { line: "$ ethtool -S eth0 | grep -E 'pfc_pause|ecn_marked|fec_corrected'", kind: "cmd" },
    { line: "     pfc_pause_tx_class3: 145823", kind: "warn", note: "Высокий PFC pause TX — этот порт создаёт backpressure upstream. Проверь, не слишком ли низкий порог буфера или не превышает ли подключённый NIC." },
    { line: "     ecn_marked_tc3: 2187", kind: "info", note: "Некоторая ECN-маркировка нормальна — отправители уведомляются до срабатывания PFC." },
    { line: "     fec_corrected_blocks: 0", kind: "ok", note: "Ноль FEC-коррекций — оптический сигнал чистый. Ненулевое значение указывало бы на слабое волокно или трансивер." },
    { line: "$ mlxlink -d /dev/mst/mt4125_pciconf0 --show_fec", kind: "cmd" },
    { line: "  Effective FEC: RS-FEC (528, 514)", kind: "ok", note: "Reed-Solomon FEC активен на этом 400G-порте — правильно для внутрифабричных каналов." },
    { line: "$ show int ethernet 1/1/49 counters | grep 'input error'", kind: "cmd" },
    { line: "  input errors: 0", kind: "ok", note: "Нет ошибок ввода. Ненулевое значение на 400G канале подозрительно на загрязнённый MTP/MPO коннектор или нарушение радиуса изгиба волокна." },
  ]}
/>

<DesignPrompt
  id="design-fabric"
  pieceSlug="05-the-datacentre-fabric"
  lang="ru"
  prompt="Спроектируй non-blocking фабрику на 512 GPU для AI-кластера. Серверы имеют 400 Гбит/с RoCE NIC."
  constraints={[]}
  keyPoints={[
    "Clos spine-leaf с ECMP для non-blocking bisection bandwidth.",
    "Lossless Ethernet (PFC + ECN) обязателен для RoCE.",
    "Жидкостное охлаждение обязательно выше потолка ~15 кВт/стойка для воздуха.",
    "Заранее терминированные MTP/MPO кабели для скорости и удобства обслуживания.",
    "Питание рассчитано под потребление GPU во избежание stranded capacity.",
    "Per-port мониторинг оптической мощности и BER выявляет деградацию раньше аварии.",
  ]}
  canonical="Clos spine-leaf фабрика: 800G оптика между spine и leaf, 400G от leaf до каждого сервера. ECMP по всем leaf-аплинкам для non-blocking bisection bandwidth. Lossless Ethernet — PFC плюс ECN — на всех RoCE-путях, с PFC-порогами, настроенными на избежание pause storms и deadlock. Жидкостное охлаждение на стойку (direct-to-chip или полное погружение); воздушное не уберёт 50–100 кВт. Кабельная система: заранее терминированные MTP/MPO волоконные магистрали для spine-leaf, структурированная оптика/Cat6a для management-сети. Питание: двойное резервированное PDU на стойку, размеры под потребление GPU с запасом. Наблюдаемость: per-port счётчики оптической мощности и BER, мониторинг ошибок FEC, трафик по priority-классу — поймай деградацию канала раньше аварии."
/>

<FadedExample
  client:load
  title="Коэффициент oversubscription фабрики"
  steps={[
    {
      label: "Решённый — 1:1 (non-blocking) leaf",
      code: `Leaf имеет 8 × 25 Гбит/с downlink к серверам и 4 × 100 Гбит/с uplink к spine.
Всего downlink : 8 × 25 = 200 Гбит/с
Всего uplink   : 4 × 100 = 400 Гбит/с
Коэффициент oversubscription = downlink / uplink = 200 / 400 = 0,5 → non-blocking

Для GPU-кластеров цель 1:1 (полная bisection bandwidth) — ни один AllReduce-поток не стопорится.
Коэффициент 4:1 (200 Гбит/с вниз, 50 Гбит/с вверх) задушит 75% трафика в
all-to-all коллективе — именно blow-up с 6 до 18 часов из Hook.`,
    },
    {
      label: "Полурешённый — 4 spine, 400G uplink на leaf, 8 × 200G серверов",
      code: `Leaf имеет 8 × 200 Гбит/с downlink к серверам и 4 × 400 Гбит/с uplink к spine.
Всего downlink : ??? Гбит/с
Всего uplink   : ??? Гбит/с
Oversubscription = downlink / uplink = ???

// Заполни: non-blocking ли эта фабрика для GPU-обучения?`,
    },
    {
      label: "Пустой — спроектируй non-blocking GPU-уровень фабрики",
      code: `// GPU-стойка имеет 8 серверов, каждый с 400 Гбит/с RDMA NIC.
// Есть 800 Гбит/с leaf uplink.
// Сколько uplink нужно leaf для non-blocking?
// Что происходит с задержкой AllReduce при oversubscription 2:1?`,
    },
  ]}
/>

</Explanation>

<FlowDiagram
  label="Фабрика Clos spine-leaf: каждый leaf-коммутатор имеет один аплинк к каждому spine-коммутатору, и два leaf не соединены напрямую, поэтому весь трафик идёт от leaf вверх к spine и обратно вниз к другому leaf"
  hue="var(--d-hardware)"
  perRow={5}
  nodes={[
    { id: "spineA", label: "Spine A", sub: "800G", col: 1, row: 0 },
    { id: "spineB", label: "Spine B", sub: "800G", col: 3, row: 0 },
    { id: "leaf1", label: "Leaf 1", sub: "ToR", col: 0, row: 1 },
    { id: "leaf2", label: "Leaf 2", sub: "ToR", col: 2, row: 1 },
    { id: "leaf3", label: "Leaf 3", sub: "ToR", col: 4, row: 1 },
  ]}
  edges={[
    { from: "leaf1", to: "spineA" },
    { from: "leaf1", to: "spineB" },
    { from: "leaf2", to: "spineA" },
    { from: "leaf2", to: "spineB" },
    { from: "leaf3", to: "spineA" },
    { from: "leaf3", to: "spineB" },
  ]}
  caption="Каждый leaf уплинкует к каждому spine; leaf никогда не соединены друг с другом. ECMP хеширует каждый поток по равноценным аплинкам, поэтому все пути активны сразу — non-blocking bisection bandwidth. Добавьте spine для роста bisection bandwidth, добавьте leaf для роста серверных портов."
/>

<KeyTakeaway>Clos spine-leaf фабрика даёт any-to-any полосу через ECMP; RoCE убирает ядро для GPU-кластеров, но требует lossless Ethernet; и 50–100 кВт GPU-стойки делают жидкостное охлаждение и мощность питания реальным потолком.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="05-the-datacentre-fabric-retrieval"
  lang="ru"
  questions={[
    { q: "Что такое non-blocking bisection bandwidth в Clos-фабрике, и как ECMP его обеспечивает?", a: "Clos spine-leaf фабрика non-blocking когда суммарная uplink-ёмкость из leaf равна суммарной ёмкости вниз к его серверам — разрежь фабрику пополам, и две половины всё равно обмениваются трафиком на полной скорости без oversubscription. ECMP (Equal-Cost Multi-Path) обеспечивает это хешированием 5-tuple каждого потока и привязкой к одному из равноценных uplink leaf, по одному на каждый spine. Разные потоки попадают на разные spine, поэтому агрегатная полоса — сумма всех uplink, а каждый отдельный поток остаётся на одном пути и сохраняет порядок пакетов." },
    { q: "Почему RoCE требует lossless Ethernet и какие два механизма его обеспечивают?", a: "RoCE (RDMA over Converged Ethernet) позволяет NIC записывать напрямую в память удалённой машины без ядра и без CPU-копирования — но RDMA не имеет встроенной повторной передачи, поэтому один потерянный пакет стопорит или ломает поток. Фабрика не должна никогда терять пакеты. Два механизма делают Ethernet без потерь: PFC (Priority Flow Control) отправляет PAUSE-кадры upstream при заполнении буфера коммутатора, применяя backpressure по traffic-class вместо отбрасывания; ECN (Explicit Congestion Notification) маркирует пакеты при росте очередей, чтобы отправители снизили скорость до срабатывания PFC. При неверной настройке — PFC deadlock или pause storms, замораживающие регион фабрики." },
    { q: "Почему мощность питания и охлаждение теперь ограничивающий фактор для GPU-развёртываний в дата-центре?", a: "Традиционная стойка потребляет 5–15 кВт и воздушное охлаждение справляется; стойка для GPU-обучения потребляет 50–100 кВт, далеко за потолком ~15 кВт воздушного охлаждения, поэтому жидкостное охлаждение (direct-to-chip или погружение) становится обязательным. Жёсткое ограничение — это stranded power: старый дата-центр со свободным местом на полу и свободными портами коммутаторов всё равно не может принять GPU-стойки, если питание и охлаждение рассчитаны под 10 кВт/стойку — дооснащение означает замену питания, HVAC и, возможно, трансформатора здания, многолетний капитальный проект." },
  ]}
/>

<Recap lang="ru">
Гиперскейловая фабрика дата-центра — это Clos spine-leaf mesh: каждый top-of-rack leaf имеет по одному аплинку к каждому spine, и ECMP (Equal-Cost Multi-Path, балансировка по нескольким равноценным маршрутам) хеширует потоки по этим аплинкам для non-blocking bisection bandwidth — 400G/800G оптика в spine, 100–400G к серверам. GPU-кластеры используют RoCE (RDMA over Converged Ethernet) для записи напрямую в удалённую память без копирования через ядро, что требует lossless Ethernet через PFC и ECN; неверная настройка flow control создаёт pause storm, замораживающий фабрику. Плоскость данных всё активнее уходит с хост-CPU в SmartNIC и DPU — AWS Nitro полностью выносит VM-сеть. 800G Ethernet (IEEE 802.3df) доступен сегодня, но обязывающее ограничение теперь — питание: 50–100 кВт GPU-стойки требуют жидкостного охлаждения и достаточно выделенной мощности, чтобы ёмкость не была stranded. Теперь, когда GPU-задача обучения работает с 30% утилизацией GPU, первая проверка — фабрика: измерь коэффициент oversubscription, найди PFC PAUSE счётчики на leaf-аплинках и убедись, что RoCE-трафик на выделенном lossless priority class.
</Recap>
