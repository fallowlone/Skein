---
concepts:
- bandwidth-delay-product
- crc-errors
- sqm-shaping
- oversubscription
estMin: 14
lang: ru
level: senior
order: 9
prereqs: []
slug: quiz-code
sources:
- https://hpbn.co/primer-on-latency-and-bandwidth/
- https://datatracker.ietf.org/doc/html/rfc8290
- https://www.juniper.net/documentation/us/en/software/junos/cli-reference/topics/topic-map/clos-ip-fabric.html
status: ready
summary: "Прочитай расчёт BDP, дамп счётчиков ethtool, конфиг SQM-шейпинга и сумму переподписки leaf — затем выбери прочтение, которое сделал бы senior."
title: 'Физический канал: чтение кода и трасс'
track: networking
unit: 01-physical-link
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Проблемы физического канала диагностируются в счётчиках, конфигах и прикидках на салфетке — не в прозе. Прочитай каждый артефакт, предскажи поведение и выбери ход, который senior делает первым.
</Hook>

<Goal lang="ru">
Потренируй цикл, который запускаешь на каждом инциденте линка: прочитай число или конфиг, реши, борешься ли ты с физикой или с неверной конфигурацией, и возьми верный рычаг.
</Goal>

## Сниппет 1 — приёмное окно на толстой трубе

```python
# Межконтинентальное волокно: канал 10 Гбит/с, RTT 160 мс (NYC -> Sydney)
bandwidth_bps = 10_000_000_000
rtt_s         = 0.160
bdp_bytes     = bandwidth_bps * rtt_s / 8       # байт в полёте при насыщении

rcv_window    = 64 * 1024                        # legacy-дефолт, без window scaling
print(bdp_bytes)                                 # 200_000_000  -> 200 МБ
print(bdp_bytes / rcv_window)                    # ~3125
```

<Quiz
  id="01-physical-link-code-1"
  lessonSlug="01-physical-link"
  lang="ru"
  question="При BDP = 200 МБ и приёмном окне 64 КБ какой throughput реально получит это соединение на канале 10 Гбит/с и почему?"
  choices={[
    { label: "Около 3.2 Мбит/с. Отправитель может держать лишь 64 КБ неподтверждённых, затем стопит на целый RTT 160 мс в ожидании ACK: 64 КБ / 0.16 с ≈ 3.2 Мбит/с — труба недонаполнена в 3125×. Фикс: включить window scaling (RFC 1323), чтобы окно раскрылось до BDP ~200 МБ.", correct: true },
    { label: "Почти 10 Гбит/с — современные NIC пейсят отправку до line rate независимо от окна.", misconception: "Приёмное окно — жёсткий потолок на байты в полёте, независимо от скорости NIC. С 64 КБ нельзя иметь больше 64 КБ неподтверждённых, поэтому линк простаивает бо́льшую часть каждого RTT." },
    { label: "Throughput в порядке; окно 64 КБ влияет только на задержку, не на полосу.", misconception: "На пути с высоким BDP окно прямо ограничивает throughput: байты-в-полёте ÷ RTT. Окно много ниже BDP — классический баг недоиспользования: он стоит throughput, а не задержки." },
    { label: "Это проблема распространения — throughput поднимет только более короткий кабель.", misconception: "Распространение задаёт RTT, но здесь throughput ограничен маленьким окном, а не полом. Window scaling чинит это без изменения пути." },
  ]}
/>

## Сниппет 2 — счётчики интерфейса

```
$ ethtool -S eth0 | grep -E 'rx_crc|rx_errors|link'
     rx_crc_errors: 48213
     rx_errors:     48213
$ ethtool eth0 | grep -E 'Speed|Duplex'
     Speed: 1000Mb/s
     Duplex: Full
```

<Quiz
  id="01-physical-link-code-2"
  lessonSlug="01-physical-link"
  lang="ru"
  question="Линк согласовал 1 Гбит/с full-duplex, но rx_crc_errors стабильно растёт. Что это значит и какое первое действие?"
  choices={[
    { label: "Фреймы приходят, но не проходят проверку CRC/FCS — биты портятся на проводе из-за плохого кабеля, грязного/умирающего SFP или маргинальной оптической мощности. Первое действие: заменить кабель и/или трансивер; если ошибки остаются — измерить оптическую RX-мощность против спецификации чувствительности трансивера.", correct: true },
    { label: "NIC перегружен — CRC-ошибки означают, что CPU не успевает обрабатывать пакеты.", misconception: "CRC-ошибки про целостность сигнала на среде, а не про загрузку CPU. Занятый CPU показывает drops/overruns, а не невалидные frame check sequences." },
    { label: "Это софтверный баг в драйвере; перезагрузи модуль ядра.", misconception: "Провал CRC означает, что биты физически изменились в пути до того, как драйвер их увидел. Перезагрузка драйвера не починит испорченный сигнал на кабеле." },
    { label: "Безвредно — CRC-ошибки ожидаемы на гигабитных скоростях и их можно игнорировать.", misconception: "Ненулевые CRC-ошибки означают, что данные тихо портятся на line rate и должны ретранслироваться; они никогда не нормальны на здоровом линке и должны быть доведены до корневой причины." },
  ]}
/>

## Сниппет 3 — конфиг SQM-шейпинга

```
# tc / CAKE на WAN-egress домашнего роутера с аплинком 40 Мбит/с
tc qdisc replace dev wan root cake bandwidth 36Mbit docsis
#                                            ^^^^^^^ зашейплено НИЖЕ line rate 40 Мбит/с
```

<Quiz
  id="01-physical-link-code-3"
  lessonSlug="01-physical-link"
  lang="ru"
  question="Почему шейпер CAKE выставлен на 36 Мбит при аплинке 40 Мбит/с, а не на полные 40, и что делает ключевое слово `docsis`?"
  choices={[
    { label: "Шейпинг до ~90% line rate переносит bottleneck-очередь из модема ISP (который ты не контролируешь) в роутер, где CAKE дропает/маркирует рано и ограничивает queueing delay. `docsis` включает компенсацию DOCSIS-фрейминга link-layer, чтобы учёт пер-пакетных накладных был точным. Ты меняешь ~10% пикового throughput на дисциплинированную очередь.", correct: true },
    { label: "36 Мбит — опечатка; CAKE всегда надо ставить ровно на line rate, чтобы не терять полосу.", misconception: "Установка на line rate оставляет bottleneck в негабаритном буфере модема, где у CAKE нет контроля — и bufferbloat возвращается. Шейп ниже line rate — весь смысл SQM." },
    { label: "Запас 4 Мбит зарезервирован под аудио звонка; `docsis` приоритизирует голос.", misconception: "CAKE не резервирует статически слот под голос, а `docsis` — это компенсация фрейминга, не класс приоритета. Запас существует, чтобы держать bottleneck внутри роутера." },
    { label: "Шейпинг ниже line rate помогает только загрузке; этот конфиг ничего не делает для случая насыщения аплинка.", misconception: "Это egress (upload) qdisc — ровно то направление, что насыщает бэкап. Шейпинг egress ниже line rate и укрощает upload-bufferbloat." },
  ]}
/>

## Сниппет 4 — сумма переподписки leaf

```
# Один leaf-свитч в GPU-pod
downlinks_to_servers = 8 * 200e9      # 8 серверов x 200 Гбит/с RoCE NIC
uplinks_to_spines    = 2 * 400e9      # 2 x 400 Гбит/с до spine

oversub = downlinks_to_servers / uplinks_to_spines
# = 1.6 Тбит/с / 0.8 Тбит/с = 2.0   ->  2:1
```

<Quiz
  id="01-physical-link-code-4"
  lessonSlug="01-physical-link"
  lang="ru"
  question="Leaf переподписан 2:1. Для задачи AllReduce на 64 GPU что это предсказывает и как достичь non-blocking?"
  choices={[
    { label: "AllReduce — all-to-all, поэтому при 2:1 лишь половина градиентного трафика может покинуть leaf разом — примерно вдвое режется throughput коллектива и стопятся GPU, что согласуется с замедлением в 2–3×. Достичь 1:1, добавив ещё два 400G-аплинка на leaf (или планируя задачу на pod с полной bisection bandwidth).", correct: true },
    { label: "2:1 нормально для тренировки; переподписка важна только для north-south интернет-трафика.", misconception: "GPU-коллективы — east-west и all-to-all — худший случай для переподписки. 2:1 прямо вдвое режет полосу для AllReduce, поэтому задача и стопится." },
    { label: "Добавь больше GPU в задачу, чтобы амортизировать переподписку.", misconception: "Больше GPU увеличивают all-to-all-трафик, усугубляя bottleneck. Фикс — больше uplink-полосы на leaf, а не больше endpoint'ов за теми же аплинками." },
    { label: "Переключи NIC с 200G на 100G, чтобы совпасть с аплинками.", misconception: "Уполовинивание скорости NIC снижает спрос, но и вдвое режет полосу на GPU — ты прячешь коэффициент, делая всё медленнее. Non-blocking значит поднять uplink-ёмкость под downlink'и, а не калечить серверы." },
  ]}
/>

<Recap lang="ru">
Каждый артефакт указывал на одно и то же разделение: окно 64 КБ на пути с BDP 200 МБ — это баг throughput (подними окно, а не кабель); растущий rx_crc_errors — испорченный сигнал (замени кабель/SFP, никогда не игнорируй); CAKE, зашейпленный ниже line rate, переносит bottleneck-очередь туда, где ты можешь её дисциплинировать; а leaf 2:1 режет all-to-all-коллектив вдвое. Прочитай число, реши физика-против-конфига, затем возьми соответствующий рычаг.
</Recap>
