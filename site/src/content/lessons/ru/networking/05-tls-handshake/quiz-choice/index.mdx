---
concepts:
- one-rtt-handshake
- forward-secrecy
- session-resumption
- 0-rtt-replay
- cipher-suite
- ech-sni
estMin: 13
lang: ru
level: senior
order: 7
prereqs: []
slug: quiz-choice
sources:
- https://datatracker.ietf.org/doc/html/rfc8446
- https://datatracker.ietf.org/doc/html/rfc8470
- https://blog.cloudflare.com/rfc-8446-aka-tls-1-3/
status: ready
summary: "Синтез-вопросы с выбором по юниту TLS 1.3: handshake за 1 RTT, forward secrecy, PSK-возобновление, replay 0-RTT, разделение cipher suite и ECH."
title: 'TLS 1.3: обзор с выбором ответа'
track: networking
unit: 05-tls-handshake
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Шесть вопросов сквозь весь юнит. Каждый отражает решение, которое ты принимаешь, проектируя или отлаживая TLS-развёртывание, — не определение для заучивания, а компромисс между скоростью, секретностью и защитой от replay.
</Hook>

<Goal lang="ru">
Убедись, что умеешь связать handshake за 1 RTT, forward secrecy, возобновление сессии, риск replay в 0-RTT, разделение cipher suite и приватность SNI — тот синтез, к которому вели отдельные уроки.
</Goal>

<Quiz
  id="05-tls-handshake-choice-1"
  lessonSlug="05-tls-handshake"
  lang="ru"
  question="TLS 1.2 требовал двух round-trip для холодного handshake; TLS 1.3 — одного. Какое единственное изменение дизайна свернуло RTT и почему Perfect Forward Secrecy появляется бесплатно?"
  choices={[
    { label: "Клиент кладёт эфемерный ECDHE key_share уже в первое сообщение (ClientHello), поэтому сервер выводит общий секрет и отвечает зашифрованно за один проход. Так как обе стороны используют свежие эфемерные ключи, выбрасываемые после соединения, более поздняя кража долгоживущего ключа сервера не расшифрует прошлые сессии — это и есть PFS.", correct: true },
    { label: "TLS 1.3 убрал проверку сертификата, удалив целый round-trip.", misconception: "Проверка сертификата по-прежнему происходит в 1-RTT — она зашифрована под handshake-ключом в том же проходе. RTT сэкономлен переносом key_share в ClientHello, а не отказом от аутентификации." },
    { label: "TLS 1.3 использует RSA key transport, который на одно сообщение короче.", misconception: "Наоборот: TLS 1.3 убрал RSA key transport. Именно RSA transport разрушает forward secrecy, потому что украденный долгоживущий ключ расшифровывает все записанные прошлые сессии." },
    { label: "Он сжимает сертификат, чтобы всё влезло в один пакет.", misconception: "Размер сертификата не связан с числом RTT. Экономия round-trip достигается ранним размещением ECDHE key_share, что позволяет серверу сразу вывести секрет." },
  ]}
/>

<Quiz
  id="05-tls-handshake-choice-2"
  lessonSlug="05-tls-handshake"
  lang="ru"
  question="Тёплый handshake с возобновлением по PSK полностью пропускает проверку сертификата. Как клиент всё ещё знает, что говорит с легитимным сервером, а не с самозванцем?"
  choices={[
    { label: "PSK был установлен в предыдущем полностью аутентифицированном handshake; использовать PSK может только сервер, завершивший тот handshake и владеющий STEK для расшифровки тикета, поэтому переиспользование PSK неявно аутентифицирует сервер.", correct: true },
    { label: "Никак — PSK-возобновление не аутентифицировано, поэтому применяется только для статики.", misconception: "Возобновление аутентифицировано: сам PSK является удостоверением. Использовать его может только сервер, владеющий ключом шифрования session ticket (STEK)." },
    { label: "Клиент тихо заново скачивает и проверяет сертификат на возобновлённом соединении.", misconception: "При возобновлении нет повторной загрузки сертификата — в этом весь смысл пропуска затрат PKI. Доказательством личности служит PSK." },
    { label: "DNSSEC проверяет личность сервера перед возобновлением.", misconception: "DNSSEC не связан с возобновлением TLS. Аутентификация переносится из исходного handshake через PSK." },
  ]}
/>

<Quiz
  id="05-tls-handshake-choice-3"
  lessonSlug="05-tls-handshake"
  lang="ru"
  question="Инженер включил 0-RTT глобально ради задержки. Через неделю финансы сообщают о периодических дублирующихся переводах. Какова первопричина и верное исправление?"
  choices={[
    { label: "Early data в 0-RTT поддаётся replay: атакующий на пути (или повторяющая сеть) может переслать захваченный 0-RTT ClientHello, и POST /transfer выполнится дважды. Ограничь 0-RTT идемпотентными маршрутами и возвращай 425 Too Early для любого изменяющего состояние обработчика, пришедшего в early data.", correct: true },
    { label: "0-RTT использует более слабый шифр, который повредил тело запроса и продублировал его.", misconception: "0-RTT использует те же AEAD-шифры, что и данные 1-RTT. Дефект — это replay, а не стойкость шифра: один и тот же валидный ciphertext можно доставить более одного раза." },
    { label: "Session ticket TLS истёк на лету, и клиент повторил передачу.", misconception: "Истечение тикета вызывает свежий handshake, а не дублированную мутацию. Дубликат — это replay early data, обработанной приложением дважды." },
    { label: "Отключи PSK-возобновление — оно несовместимо с POST-запросами.", misconception: "PSK-возобновление подходит для любого метода; именно early data в 0-RTT поддаётся replay. Исправление — политика идемпотентности на уровне маршрута плюс 425 Too Early, а не отключение возобновления." },
  ]}
/>

<Quiz
  id="05-tls-handshake-choice-4"
  lessonSlug="05-tls-handshake"
  lang="ru"
  question="В TLS 1.3 cipher suite TLS_AES_128_GCM_SHA256 больше не называет обмен ключами или алгоритм подписи — в отличие от TLS 1.2. Что дало это разделение?"
  choices={[
    { label: "Обмен ключами (named group), аутентификация (signature algorithm) и AEAD-плюс-хеш согласуются по трём независимым осям, сворачивая сотни комбинаторных suite TLS 1.2 до пяти и убирая целые классы слабых или несовместимых сочетаний.", correct: true },
    { label: "Это позволило клиенту менять cipher suite в середине соединения без renegotiation.", misconception: "Смена в середине соединения — это KeyUpdate, который переиспользует тот же suite. Разделение касается осей согласования, а не смены suite во время сессии." },
    { label: "Оно сделало RSA key transport выбираемым независимо от шифра.", misconception: "RSA key transport полностью убран в TLS 1.3. Разделённые оси — named group, signature algorithm и AEAD+хеш, и все они дают forward-secret обмен ключами." },
    { label: "Оно позволило сочетать не-AEAD шифры вроде CBC с более стойкими хешами.", misconception: "TLS 1.3 допускает только AEAD-шифры; CBC-режим и комбинации MAC-then-encrypt убраны. Разделение их не возвращает." },
  ]}
/>

<Quiz
  id="05-tls-handshake-choice-5"
  lessonSlug="05-tls-handshake"
  lang="ru"
  question="Middlebox переписывает список ALPN в ClientHello, убирая 'h2', чтобы навязать HTTP/1.1. Соединение обрывается до передачи данных. Почему?"
  choices={[
    { label: "Каждый байт handshake попадает в текущий transcript hash, а сообщение Finished — это HMAC над этим transcript. Изменение ALPN меняет transcript, поэтому HMAC в Finished не сходится с обеих сторон и соединение обрывается — тот же механизм ловит попытки понижения версии.", correct: true },
    { label: "ALPN зашифрован, поэтому middlebox повредил ciphertext.", misconception: "ALPN находится в открытом ClientHello, так что middlebox может его читать и менять — но он всё равно входит в transcript hash, поэтому правка обнаруживается по MAC в Finished." },
    { label: "Сервер обнаружил неверный IP источника и отказал.", misconception: "IP источника ни при чём. Обрыв — это несовпадение HMAC в Finished из-за изменённого transcript, что и делает handshake TLS 1.3 устойчивым к подделке." },
    { label: "HTTP/1.1 не поддерживается в TLS 1.3, поэтому понижение отклонено.", misconception: "HTTP/1.1 прекрасно работает поверх TLS 1.3. Отказ — это проверка целостности transcript, а не ограничение версии протокола." },
  ]}
/>

<Quiz
  id="05-tls-handshake-choice-6"
  lessonSlug="05-tls-handshake"
  lang="ru"
  question="Security review отмечает, что корпоративный прокси всё ещё может логировать, какие хосты посещают сотрудники, даже на TLS 1.3. Какой механизм это раскрывает и что закрывает брешь?"
  choices={[
    { label: "SNI отправляется открытым текстом в ClientHello, чтобы сервер выбрал верный сертификат для общего IP, раскрывая имя хоста любому наблюдателю на пути. Encrypted ClientHello (ECH) шифрует внутренний ClientHello через HPKE под опубликованным в DNS ключом origin, и наблюдатель видит только обобщённое имя front-end.", correct: true },
    { label: "Subject Alternative Name сертификата вещается открытым текстом после handshake.", misconception: "В TLS 1.3 сообщение Certificate зашифровано под handshake-ключом — SAN не виден. Открытая утечка — это SNI в ClientHello, что и решает ECH." },
    { label: "Запросы OCSP раскрывают имя хоста; включение OCSP stapling полностью устраняет утечку приватности.", misconception: "OCSP stapling убирает утечку приватности живого OCSP, но имя хоста всё ещё раскрыто через открытый SNI. Закрыть эту брешь может ECH, а не stapling." },
    { label: "Ничего не утекает — TLS 1.3 по умолчанию шифрует весь ClientHello.", misconception: "TLS 1.3 не шифрует ClientHello по умолчанию; SNI, ALPN и supported groups видны. ECH — это опциональное расширение, шифрующее чувствительный внутренний hello." },
  ]}
/>

<Recap lang="ru">
Сквозная линия юнита — одна цепочка компромиссов: ECDHE key_share в ClientHello даёт handshake за 1 RTT и forward secrecy вместе; PSK-возобновление меняет свежую аутентификацию на скорость, но остаётся аутентифицированным через тикет; 0-RTT меняет round-trip на уязвимость к replay, сдерживаемую только идемпотентностью плюс 425 Too Early; разделение cipher suite сжимает поверхность атаки до пяти suite; а transcript hash делает каждый согласованный параметр устойчивым к подделке. SNI всё ещё раскрывает имя хоста, пока его не зашифрует ECH. Скорость, секретность и защита от replay — три оси, которые ты балансируешь в каждом решении по TLS.
</Recap>
