---
concepts:
- clienthello
- serverhello
- ecdhe
- key-share
- pfs
- certificate-chain
- transcript-hash
- finished
- certificate-verify
deepensInto:
- 03-session-resumption-and-0rtt
- 04-key-schedule-and-extensions
estMin: 14
lang: ru
lessonType: topic
level: middle
order: 2
prereqs:
- 01-what-tls-does
slug: 02-the-1rtt-handshake
sources:
- https://datatracker.ietf.org/doc/html/rfc8446
- https://hpbn.co/transport-layer-security-tls/
- https://blog.cloudflare.com/rfc-8446-aka-tls-1-3/
spiral:
- statefulness
status: ready
summary: Как key share в ClientHello, эфемерные ключи ECDHE и хеш транскрипта сворачивают
  двухшаговое рукопожатие TLS 1.2 в одно — и почему Perfect Forward Secrecy следует
  из этого автоматически.
title: 'Рукопожатие за 1 RTT: key share и ECDHE'
track: networking
unit: 05-tls-handshake
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import TraceScenario from "~/components/pedagogy/TraceScenario.astro";
import NumbersCard from "~/components/layout/NumbersCard.astro";
import Misconception from "~/components/layout/Misconception.astro";
import SequenceDiagram from "~/components/diagram/SequenceDiagram.astro";
import DiagramFrame from "~/components/diagram/DiagramFrame.astro";
import PacketDot from "~/components/diagram/PacketDot.astro";

<Hook lang="ru">
Браузер открывает TCP-соединение и немедленно отправляет ClientHello. Это единственное сообщение содержит и список шифров, и половину ключа шифрования — сервер может вычислить общий секрет прямо здесь и ответить зашифрованными данными за один проход. Именно так TLS 1.3 превращает два RTT в один.
</Hook>

<Crux>TLS 1.3 встраивает key share ECDHE в ClientHello; сервер немедленно выводит общий секрет и отвечает зашифрованно — один RTT.</Crux>

<Explanation>

## Рукопожатие за 1 RTT: шаг за шагом

**Шаг 1 — ClientHello.** Клиент отправляет: список версий TLS (через расширение `supported_versions`), поддерживаемые наборы шифров, поддерживаемые именованные группы (кривые), поддерживаемые алгоритмы подписи и расширение `key_share` с как минимум одним публичным ключом ECDHE — обычно `x25519`.

**Шаг 2 — ServerHello.** Сервер выбирает набор шифров (например `TLS_AES_128_GCM_SHA256`), выбирает именованную группу из key_share клиента, генерирует собственную эфемерную пару ключей и отвечает своим публичным ключом. Теперь обе стороны знают публичные ключи друг друга и могут запустить ECDHE для вывода общего секрета.

**Шаг 3 — Зашифрованное рукопожатие.** Сервер немедленно выводит ключ трафика рукопожатия из общего секрета и отправляет `EncryptedExtensions`, `Certificate`, `CertificateVerify` и серверный `Finished` — все зашифрованные — в одном сетевом проходе.

**Шаг 4 — Клиент завершает.** Клиент выводит тот же ключ трафика, расшифровывает сообщения сервера, проверяет цепочку сертификатов, проверяет подпись `CertificateVerify` над транскриптом, проверяет HMAC серверного `Finished`, отправляет клиентский `Finished` и начинает передавать данные приложения.

Итого round-trip: **один.** ClientHello уходит, ServerHello + зашифрованное рукопожатие приходит, клиентский Finished + первые данные приложения уходят — всё в одном сетевом обмене.

<NumbersCard
  id="card-handshake-wire"
  title="Стоимость холодного рукопожатия TLS 1.3 на проводе"
  data-lesson-visual
  stats={[
    { label: "ClientHello", value: "~512 байт" },
    { label: "ServerHello", value: "~150 байт" },
    { label: "Certificate (цепочка ECDSA P-256)", value: "1–3 КБ" },
    { label: "CertificateVerify (ECDSA)", value: "~70 байт" },
    { label: "Server Finished", value: "~50 байт" },
    { label: "Client Finished", value: "~50 байт" },
    { label: "Итого RTT", value: "1" },
  ]}
/>

<DiagramFrame label="Один пакет идёт от клиента к серверу с ClientHello, затем второй пакет возвращается от сервера к клиенту с ServerHello и зашифрованным рукопожатием — тот самый один round trip, в который обходится 1 RTT" hue="var(--d-network)" caption="Всё рукопожатие умещается в один round trip: ClientHello уходит, ServerHello плюс зашифрованное рукопожатие приходит. После этой задержки в один round trip начинают идти данные приложения.">
  <svg viewBox="0 0 360 120" width="360" height="120" style="display:block;margin:0 auto;max-width:100%;height:auto;">
    <rect x="6" y="46" width="80" height="28" rx="6" fill="var(--card-2)" stroke="var(--hairline-strong)" stroke-width="0.8" />
    <text x="46" y="64" text-anchor="middle" font-size="11" font-family="var(--font-body)" fill="var(--ink)">Клиент</text>
    <rect x="274" y="46" width="80" height="28" rx="6" fill="var(--card-2)" stroke="var(--hairline-strong)" stroke-width="0.8" />
    <text x="314" y="64" text-anchor="middle" font-size="11" font-family="var(--font-body)" fill="var(--ink)">Сервер</text>
    <text x="180" y="30" text-anchor="middle" font-size="10" font-family="var(--font-body)" fill="var(--muted)">ClientHello (key_share)</text>
    <text x="180" y="104" text-anchor="middle" font-size="10" font-family="var(--font-body)" fill="var(--muted)">ServerHello + сертификат/параметры</text>
    <path data-path d="M86 60 L274 60" fill="none" stroke="var(--hairline-2)" stroke-width="1.4" stroke-dasharray="4 4" />
    <PacketDot color="var(--d-network)" durationMs={1500} size={9} />
    <PacketDot color="var(--d-network)" durationMs={1500} delayMs={1700} size={9} />
  </svg>
</DiagramFrame>

## ECDHE: обмен ключами за key share

Обе стороны договариваются о кривой (обычно `x25519` для производительности или `P-256` для FIPS). Каждая генерирует приватный скаляр (случайное 256-битное целое) и вычисляет публичную точку: `public = private · G`, где `G` — базовый генератор кривой. Антон отправляет свою публичную точку в key_share, Дима — свою в ответ. Каждый вычисляет общий секрет, умножая публичную точку другого на свой приватный скаляр:

- Антон: `shared = ant_private · dim_public`
- Дима: `shared = dim_private · ant_public`

Оба получают одну и ту же точку на кривой. Подслушивающий, который видел только публичные точки, не может её восстановить без решения задачи дискретного логарифма на эллиптической кривой — неразрешимой для правильно выбранных кривых на классическом оборудовании.

## Perfect Forward Secrecy (PFS)

Оба клиент и сервер используют **эфемерные** пары ключей, сгенерированные заново для каждого соединения и уничтожаемые после него. Если злоумышленник позже украдёт долгосрочный приватный ключ Димы с диска, **прошлые сессии всё равно не расшифруются** — они использовали эфемерные ключи, которых уже нет. Это и есть Perfect Forward Secrecy.

TLS 1.2 позволял RSA-транспорт ключей: клиент шифровал секрет сессии долгосрочным публичным ключом сервера. Кража приватного ключа означала расшифровку всех записанных прошлых сессий. TLS 1.3 **требует** эфемерный обмен ключами, делая PFS настройкой по умолчанию.

## Проверка цепочки сертификатов

Когда вы отлаживаете TLS-ошибки в продакшне — почти всегда дело именно здесь: неправильно настроенный сервер, истёкший промежуточный сертификат или отсутствующая запись в хранилище доверия проявляются именно как непонятные алерты. Зная каждый шаг, вы превратите диагностику `ERR_CERT_AUTHORITY_INVALID` из получасовой охоты в пятисекундную процедуру.

Получив `Certificate`, клиент видит цепочку: листовой сертификат для хоста, затем один или несколько промежуточных. Он обходит цепочку снизу вверх:

1. Проверить, что листовой подписан промежуточным.
2. Проверить, что промежуточный подписан корневым.
3. Убедиться, что корень находится в локальном хранилище доверия.
4. Проверить, что SAN (Subject Alternative Names) листового включают хост подключения.
5. Проверить срок действия (`notBefore <= now <= notAfter`).
6. Проверить отзыв (CRLite, stapled OCSP или политика короткоживущих сертификатов).

Если хоть один шаг провалился — соединение завершается с fatal alert (например `unknown_ca`). Все шесть должны пройти: сервер, отправляющий только листовой сертификат без промежуточного, провалит шаг 2 — даже если сам лист абсолютно валиден.

## Хеш транскрипта и Finished

Каждый байт, отправленный в ходе рукопожатия, поступает в накапливаемый SHA-256-дайджест — **хеш транскрипта**. Сообщение `Finished` — это `HMAC(finished_key, transcript_hash)`. Если один бит на проводе был перевёрнут или подменён — HMAC не совпадёт и соединение прервётся. Именно так TLS 1.3 обнаруживает атаки понижения версии: транскрипт записывает первоначально предложенную клиентом версию, поэтому попытка middlebox'а принудить к TLS 1.2 сломает проверку Finished.

<TraceScenario
  id="mid-trace-1"
  pieceSlug="02-the-1rtt-handshake"
  lang="ru"
  scenario="Проследите успешное холодное рукопожатие TLS 1.3 от ClientHello до первого зашифрованного байта данных."
  steps={[
    { prompt: "Клиент отправляет ClientHello. Какие поля важнее всего?", reveal: "client random, поддерживаемые наборы шифров, поддерживаемые именованные группы, поддерживаемые алгоритмы подписи и key_share с как минимум одним публичным ключом ECDHE (обычно x25519)." },
    { prompt: "Сервер выбирает набор шифров и именованную группу. Что он отправляет?", reveal: "ServerHello с server random, выбранным набором шифров и key_share сервера. Теперь обе стороны знают публичные ключи друг друга и могут вывести общий секрет ECDHE." },
    { prompt: "Какие сообщения рукопожатия отправляются зашифрованными под серверным ключом трафика?", reveal: "EncryptedExtensions, Certificate, CertificateVerify и серверный Finished — один за другим в том же RTT." },
    { prompt: "Клиент проверяет и отвечает. Что отправляет клиент?", reveal: "Клиентский Finished (HMAC над всем транскриптом) плюс первая запись зашифрованных данных приложения." },
    { prompt: "Итоговая стоимость в RTT?", reveal: "Ровно один RTT. ClientHello уходит, ServerHello + зашифрованное рукопожатие приходит, клиентский Finished + данные приложения уходят — всё в одном обмене." },
  ]}
/>

<Misconception
  id="pfs-misconception"
  title="'TLS 1.3 использует эфемерные ключи, значит возобновление сессии тоже имеет PFS'"
>
PSK-only возобновление выводит ключи из кэшированного PSK без свежего ECDHE. Если STEK утечёт — прошлые сессии расшифруются. Для PFS ClientHello возобновления должен включать fresh key_share (режим PSK-DHE).
</Misconception>

<Quiz
  id="mid-quiz-1"
  pieceSlug="02-the-1rtt-handshake"
  lang="ru"
  question="Почему TLS 1.3 требует эфемерный обмен ключами (ECDHE) и убирает RSA-транспорт ключей?"
  choices={[
    { label: "ECDHE быстрее RSA-транспорта ключей", misconception: "Производительность — побочный эффект. Главная причина — Perfect Forward Secrecy." },
    { label: "Для обеспечения Perfect Forward Secrecy — прошлые сессии остаются безопасными, даже если долгосрочный ключ утечёт позже", correct: true },
    { label: "RSA-ключи больше не доверяются браузерами", misconception: "RSA всё ещё используется для подписей сертификатов; он просто не используется для транспорта ключей в TLS 1.3." },
    { label: "ECDHE поддерживает постквантовую криптографию", misconception: "Классический ECDHE не является постквантовым. Гибридный PQ-обмен — отдельное дополнение." },
  ]}
/>

<Quiz
  id="mid-quiz-2"
  pieceSlug="02-the-1rtt-handshake"
  lang="ru"
  question="Какую гарантию целостности даёт хеш транскрипта TLS 1.3?"
  choices={[
    { label: "Он шифрует сообщения рукопожатия", misconception: "Шифрование — отдельная операция. Хеш транскрипта отвечает за целостность, не конфиденциальность." },
    { label: "Он обнаруживает любое изменение байтов рукопожатия — MAC Finished не пройдёт проверку", correct: true },
    { label: "Он ускоряет рукопожатие за счёт пропуска сертификата", misconception: "Проверка сертификата всё равно происходит. Хеш транскрипта не связан со скоростью." },
    { label: "Он используется только при возобновлении сессии", misconception: "Хеш транскрипта вычисляется при каждом рукопожатии — свежем или возобновлённом." },
  ]}
/>

<DragOrder
  id="mid-drag-1"
  pieceSlug="02-the-1rtt-handshake"
  lang="ru"
  prompt="Упорядочьте шаги проверки цепочки сертификатов:"
  items={[
    "Получить Certificate с листовым + промежуточными сертификатами",
    "Убедиться, что SAN листового совпадает с именем хоста подключения",
    "Обойти цепочку: проверить подпись каждого сертификата публичным ключом его издателя",
    "Найти корневой в локальном хранилище доверия",
    "Проверить срок действия (notBefore <= now <= notAfter)",
    "Проверить статус отзыва (CRLite, stapled OCSP или политика короткоживущих сертификатов)",
  ]}
/>

<Inset kind="why" lang="ru">
**RSA vs ECDSA сертификаты.** Алгоритм подписи серверного сертификата не зависит от ECDHE. Сертификаты ECDSA P-256 примерно в 5 раз быстрее верифицировать, чем RSA-2048, и подписи в четыре раза меньше. Edge-сервер, обрабатывающий миллион рукопожатий в секунду, платит в пять раз больше CPU при RSA. Провайдеры облаков по умолчанию используют цепочки ECDSA; RSA предлагается только для совместимости с устаревшими клиентами.
</Inset>

</Explanation>

<SequenceDiagram
  label="Рукопожатие TLS 1.3 за 1 RTT: ClientHello несёт key_share, сервер отвечает ServerHello плюс зашифрованные EncryptedExtensions, Certificate, CertificateVerify и серверный Finished, затем клиент отправляет Finished и первые данные приложения"
  hue="var(--d-crypto)"
  actors={["Клиент", "Сервер"]}
  messages={[
    { from: "Клиент", to: "Сервер", label: "ClientHello (key_share, шифры, группы)" },
    { from: "Сервер", to: "Клиент", label: "ServerHello (key_share)" },
    { from: "Сервер", to: "Клиент", label: "{EncryptedExtensions, Certificate, CertificateVerify, Finished}" },
    { from: "Клиент", to: "Сервер", label: "Finished + данные приложения" },
  ]}
  caption="ClientHello несёт key_share ECDHE, поэтому сервер выводит общий секрет при получении и отправляет остальную часть рукопожатия — зашифрованную под ключом трафика рукопожатия — в том же проходе. Один round-trip: ClientHello уходит, ServerHello плюс зашифрованное рукопожатие приходит, затем клиентский Finished плюс первые данные приложения уходят."
/>

<KeyTakeaway>Рукопожатие TLS 1.3 за 1 RTT помещает публичный ключ ECDHE клиента в ClientHello, чтобы сервер немедленно вывел общий секрет; эфемерные пары ключей гарантируют Perfect Forward Secrecy; а HMAC транскрипта в Finished обнаруживает любое изменение данных при передаче.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="02-the-1rtt-handshake-retrieval"
  lang="ru"
  questions={[
    { q: "Почему сервер отправляет HelloRetryRequest и что клиент делает в ответ?", a: "Сервер отправляет HelloRetryRequest, когда расширение key_share клиента не содержит именованную группу, которую сервер готов использовать. Сообщение HRR содержит cookie и желаемую группу. Клиент отправляет новый ClientHello с key_share для этой группы плюс cookie. Это добавляет один RTT, но происходит редко, поскольку современные клиенты предлагают x25519 и P-256 одновременно." },
    { q: "Что происходит при провале проверки цепочки сертификатов (неизвестный CA)?", a: "Клиент отправляет fatal Alert с кодом unknown_ca (код 48) и закрывает соединение. Пользователь видит ERR_CERT_AUTHORITY_INVALID. Никакие данные приложения не передаются. Распространённые причины: самоподписанный сертификат, внутренний CA не в хранилище доверия, или истёкший промежуточный (например, истечение DST Root CA X3 у Let's Encrypt в 2021)." },
    { q: "Объясните ECDHE в одном абзаце. Почему обе стороны приходят к одному секрету, не передавая его?", a: "Каждая сторона генерирует приватный скаляр и публичную точку (private * G на кривой). Они обмениваются публичными точками. Затем каждая умножает публичную точку другого на свой приватный скаляр: Антон получает ant_private * dim_public, Дима — dim_private * ant_public. По математике ECDH оба результата равны ant_private * dim_private * G — одна и та же точка. Подслушивающий, видящий только публичные точки, не может обратить умножение (задача дискретного логарифма на эллиптической кривой)." },
  ]}
/>

<Recap lang="ru">
TLS 1.3 сжимает рукопожатие до одного RTT, встраивая публичный ключ ECDHE клиента в первое сообщение. Сервер немедленно запускает алгоритм Диффи-Хеллмана на эллиптических кривых для вычисления общего секрета, шифрует остальную часть рукопожатия под выведенным ключом трафика и отправляет сертификат, CertificateVerify и Finished в одном проходе. Поскольку обе стороны используют свежие эфемерные ключи, компрометация долгосрочного ключа подписи сервера позднее не позволит расшифровать прошлые сессии — это и есть Perfect Forward Secrecy. Хеш транскрипта в каждом сообщении Finished обнаруживает любые изменения байтов рукопожатия, включая попытки понижения версии middlebox'ами. Теперь, когда вы встретите TLS-алерт вроде `unknown_ca` или сбой Finished в логах, вы будете точно знать, какой именно шаг в этой цепочке сломался и с чего начать исправление.
</Recap>
