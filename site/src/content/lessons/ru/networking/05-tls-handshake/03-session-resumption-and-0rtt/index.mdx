---
concepts:
- psk
- new-session-ticket
- session-resumption
- 0-rtt
- early-data
- replay-attack
- 425-too-early
- idempotent
deepensInto:
- 05-0rtt-defenses-and-modern-tls
estMin: 12
lang: ru
lessonType: topic
level: middle
order: 3
prereqs:
- 02-the-1rtt-handshake
slug: 03-session-resumption-and-0rtt
sources:
- https://datatracker.ietf.org/doc/html/rfc8446
- https://datatracker.ietf.org/doc/html/rfc8470
- https://blog.cloudflare.com/tls-session-resumption-full-speed-and-secure/
spiral:
- statefulness
status: ready
summary: PSK-тикеты позволяют тёплым соединениям пропустить валидацию сертификата;
  ранние данные 0-RTT убирают RTT рукопожатия для запросов только для чтения — но
  атаки повтора ограничивают то, что может безопасно попасть в ранние данные.
title: Возобновление сессии и 0-RTT
track: networking
unit: 05-tls-handshake
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import TraceScenario from "~/components/pedagogy/TraceScenario.astro";
import Misconception from "~/components/layout/Misconception.astro";
import NumbersCard from "~/components/layout/NumbersCard.astro";
import SequenceDiagram from "~/components/diagram/SequenceDiagram.astro";

<Hook lang="ru">
Первое TLS-соединение с сервером стоит один полный RTT. Но когда вы перезагружаете ту же страницу пять минут спустя, браузер уже знает этот сервер. TLS 1.3 позволяет ему полностью пропустить валидацию сертификата — а в правильных условиях приложить первый запрос прямо к рукопожатию, не платя ни одного лишнего RTT.
</Hook>

<Crux>PSK-возобновление пропускает валидацию сертификата; 0-RTT ранние данные убирают RTT рукопожатия — оба несут компромиссы безопасности.</Crux>

<Explanation>

## NewSessionTicket и возобновление по PSK

После успешного рукопожатия сервер отправляет сообщение `NewSessionTicket` — зашифрованный блоб, содержащий **предварительно согласованный ключ (PSK)** и метаданные (возраст тикета, время жизни). Клиент кэширует его по имени хоста.

При следующем подключении к тому же серверу клиент включает расширение `pre_shared_key` в свой ClientHello вместе с тикетом. Сервер расшифровывает тикет, распознаёт PSK и полностью пропускает валидацию сертификата. Он выводит сессионные ключи из PSK вместо ECDHE.

**Стоимость:** по-прежнему один полный RTT (ClientHello всё равно должен дойти до сервера), но никаких дорогих операций PKI. Разбор сертификата и проверка цепочки исключаются. Когда вы профилируете установку соединения и заметите, что второй коннект заметно быстрее первого, — это именно PSK-возобновление в действии.

## Ранние данные 0-RTT

Если браузер имеет кэшированный PSK, он может зашифровать первый запрос с помощью ключа PSK и приложить его к самому ClientHello — до завершения рукопожатия. Сервер расшифровывает и обрабатывает запрос немедленно, а затем отправляет и ответ рукопожатия, и ответ приложения. Стоимость запроса: **ноль дополнительных RTT**.

<NumbersCard
  id="card-rtt-comparison"
  title="Сравнение RTT-стоимости"
  data-lesson-visual
  stats={[
    { label: "TLS 1.2 холодное", value: "2 RTT" },
    { label: "TLS 1.3 холодное", value: "1 RTT" },
    { label: "TLS 1.3 тёплое (возобновление PSK)", value: "1 RTT (без сертификата)" },
    { label: "TLS 1.3 0-RTT", value: "0 RTT для запроса" },
    { label: "При 100 мс RTT: выигрыш 0-RTT", value: "100 мс против холодного TLS 1.3" },
    { label: "Типичное время жизни тикета", value: "до нескольких часов (зависит от реализации)" },
  ]}
/>

## Проблема повтора

У 0-RTT есть фундаментальная уязвимость безопасности: **он не защищён от атак повтора**. Злоумышленник в сети может перехватить ClientHello с 0-RTT и переотправить его серверу. Сервер, увидев валидный PSK-тикет и валидные ранние данные, может обработать запрос снова.

- Для `GET /news`: повтор безвреден — то же чтение, тот же результат.
- Для `POST /transfer`: повтор катастрофичен — строка в базе данных создаётся дважды, деньги переводятся дважды.

Простейшее правило: **0-RTT безопасен только для идемпотентных запросов**, где повтор не производит дополнительных побочных эффектов. `GET`, `HEAD` и `OPTIONS` обычно безопасны. `POST`, `PUT`, `DELETE`, изменяющие состояние, — небезопасны.

## RFC 8470 и 425 Too Early

RFC 8470 определяет код HTTP-ответа **425 Too Early**: сервер возвращает его, когда получает 0-RTT-запрос для маршрута, который небезопасно повторять. Клиент должен повторить попытку после завершения рукопожатия. Фреймворки (Spring, Express, Caddy) предоставляют промежуточное ПО, которое проверяет заголовок запроса `Early-Data: 1` и возвращает `425 Too Early` для любого обработчика, изменяющего состояние.

<Misconception
  id="0rtt-safe-misconception"
  title="«0-RTT — это бесплатный выигрыш в производительности, просто включите его»"
>
0-RTT безопасен только для идемпотентных путей. GET, изменяющий корзину — семантически мутация: повторы удваивают корзину. Помечайте маршруты явно как допустимые или недопустимые для 0-RTT.
</Misconception>

<TraceScenario
  id="mid-trace-1"
  pieceSlug="03-session-resumption-and-0rtt"
  lang="ru"
  scenario="POST /transfer приходит с TLS early_data и ticket_age = 5 секунд. Проследите путь принятия решений сервером."
  steps={[
    { prompt: "Шаг 1: Находится ли ticket_age внутри окна повтора (по умолчанию 10 с)?", reveal: "5 с — внутри окна 10 с. Тикет не устарел тривиально. Переходим к следующей проверке." },
    { prompt: "Шаг 2: Встречался ли ранее этот nonce тикета (кэш одноразового использования)?", reveal: "Сервер проверяет (ticket_nonce, age_bucket) в своём LRU анти-повтора. Если уже встречался — ранние данные отклоняются." },
    { prompt: "Шаг 3: Тикет свежий и не встречался. Какова третья проверка?", reveal: "Политика на уровне приложения: POST /transfer не идемпотентен. Фреймворк возвращает 425 Too Early. Клиент должен повторить попытку после Finished." },
    { prompt: "Шаг 4: GET /catalog/42 приходит в том же корректном 0-RTT-контексте. Что происходит?", reveal: "Все три проверки пройдены: в окне, не встречался, идемпотентен. Сервер обрабатывает GET в 0-RTT и отвечает, экономя один RTT." },
  ]}
/>

<Quiz
  id="mid-quiz-1"
  pieceSlug="03-session-resumption-and-0rtt"
  lang="ru"
  question="Почему злоумышленник может повторить 0-RTT ранние данные, но не обычные данные TLS 1.3?"
  choices={[
    { label: "Данные 0-RTT используют более слабый шифр", misconception: "0-RTT использует те же AEAD-шифры. Проблема — в защите от повтора, а не в стойкости шифра." },
    { label: "Данные 0-RTT выводятся из статического PSK без nonce для каждого соединения, покрывающего early_data, поэтому тот же шифртекст валиден при повторе", correct: true },
    { label: "Обычный TLS 1.3 шифрует дважды", misconception: "Обычные данные используют один проход AEAD. Разница — в nonce, выводимом при рукопожатии, который предотвращает повтор после Finished." },
    { label: "Повтор возможен только в HTTP/2", misconception: "Окно повтора — проблема уровня TLS, не зависящая от версии HTTP выше." },
  ]}
/>

<Quiz
  id="mid-quiz-2"
  pieceSlug="03-session-resumption-and-0rtt"
  lang="ru"
  question="Возобновление PSK пропускает валидацию сертификата. Как клиент всё ещё знает, что разговаривает с нужным сервером?"
  choices={[
    { label: "Никак — возобновление PSK не имеет аутентификации", misconception: "PSK сам по себе и является аутентификацией: только сервер, завершивший исходное рукопожатие (и владеющий STEK для расшифровки тикета), может использовать PSK." },
    { label: "PSK был установлен в ходе предыдущего аутентифицированного рукопожатия — его повторное использование неявно аутентифицирует сервер", correct: true },
    { label: "Клиент повторно получает сертификат при каждом запросе", misconception: "При возобновлении нет дополнительного получения сертификата. Учётные данные PSK — это и есть доказательство." },
    { label: "Клиент проверяет DNSSEC перед возобновлением", misconception: "DNSSEC не участвует в возобновлении TLS." },
  ]}
/>

<Inset kind="why" lang="ru">
**Обфускация NewSessionTicket.** Каждый тикет включает `ticket_age_add` — случайное значение, которое клиент прибавляет к реальному возрасту тикета перед отправкой `obfuscated_ticket_age`. Это не позволяет наблюдателям на пути коррелировать тикеты между соединениями по видимому возрасту. Это также делает проверки окна повтора на основе времени более надёжными: сервер вычитает `ticket_age_add`, чтобы восстановить реальный возраст и сравнить его с ожидаемым RTT-окном.
</Inset>

</Explanation>

<SequenceDiagram
  label="Ранние данные 0-RTT на тёплом соединении: предыдущее рукопожатие выдало NewSessionTicket с PSK; следующий ClientHello несёт PSK плюс зашифрованный первый запрос как early_data, и сервер обрабатывает его ещё до завершения рукопожатия"
  hue="var(--d-crypto)"
  actors={["Клиент", "Сервер"]}
  messages={[
    { from: "Сервер", to: "Клиент", label: "NewSessionTicket (PSK) — прошлое соединение" },
    { from: "Клиент", to: "Сервер", label: "ClientHello + pre_shared_key + early_data (GET)" },
    { from: "Сервер", to: "Клиент", label: "ServerHello + Finished + ответ приложения" },
    { from: "Клиент", to: "Сервер", label: "Finished" },
  ]}
  caption="При наличии кэшированного PSK клиент шифрует первый запрос и прикладывает его к ClientHello как early_data — ноль дополнительных RTT для запроса. Но early_data не несёт nonce для конкретного соединения, поэтому перехваченный ClientHello можно повторить: безопасно только для идемпотентных запросов, иначе сервер возвращает 425 Too Early."
/>

<KeyTakeaway>Возобновление PSK избегает затрат на PKI-сертификат при тёплых соединениях (по-прежнему 1 RTT); 0-RTT убирает RTT рукопожатия для идемпотентных запросов, но принципиально уязвим к повторам — ограничивайте его GET/HEAD/OPTIONS и применяйте 425 Too Early для мутирующих конечных точек.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="03-session-resumption-and-0rtt-retrieval"
  lang="ru"
  questions={[
    { q: "Что сервер отправляет после рукопожатия, чтобы включить будущее возобновление?", a: "Одно или несколько сообщений NewSessionTicket — каждое является зашифрованным блобом, содержащим предварительно согласованный ключ, nonce тикета, время жизни и обфускатор ticket_age_add. Клиент кэширует их по имени хоста и предоставляет в расширении pre_shared_key будущих ClientHello." },
    { q: "Почему 0-RTT ограничен идемпотентными HTTP-методами?", a: "Ранние данные 0-RTT могут быть перехвачены и воспроизведены злоумышленником. Повтор идемпотентного GET даёт тот же результат без побочных эффектов. Повтор POST, создающего строку, PUT, обновляющего состояние, или DELETE, удаляющего данные, приводит к двойному выполнению действия — дублирующим платежам, двойным строкам и т.д. Спецификация HTTP определяет идемпотентные методы, но логика приложения тоже должна быть проверена: GET, мутирующий корзину, идемпотентен по HTTP, но опасен для 0-RTT." },
    { q: "Какой код статуса должен возвращать сервер для 0-RTT POST к мутирующей конечной точке, и что должен сделать клиент?", a: "425 Too Early (RFC 8470). Сервер отклоняет early_data, буферизует или отбрасывает тело запроса, и клиент должен повторно передать запрос после завершения обмена Finished рукопожатия. Заголовок запроса Early-Data: 1 сигнализирует прикладному уровню, что запрос прибыл в ранних данных." },
  ]}
/>

<Recap lang="ru">
После рукопожатия TLS 1.3 сервер выдаёт NewSessionTicket с предварительно согласованным ключом. При следующем подключении клиент предъявляет этот тикет, чтобы сервер мог пропустить валидацию сертификата — по-прежнему один RTT, но дешевле. При наличии кэшированного PSK клиент может также зашифровать первый запрос прямо внутри ClientHello как ранние данные 0-RTT: сервер обрабатывает его до завершения рукопожатия, экономя полный RTT. Подвох — уязвимость к повтору: тот же ClientHello с 0-RTT может быть перехвачен и переотправлен злоумышленником. Безопасное использование ограничено идемпотентными запросами (GET, HEAD, OPTIONS), а серверы должны возвращать 425 Too Early для любой мутирующей конечной точки, получающей ранние данные. Теперь, когда вы потянетесь включить 0-RTT ради снижения задержки, вы сначала проверите каждый маршрут на идемпотентность — и будете рассматривать любой `POST` с заголовком `Early-Data: 1` как потенциальный повтор, пока не доказано обратное.
</Recap>
