---
concepts:
- one-rtt-handshake
- forward-secrecy
- transcript-hash
- 0-rtt-replay
- hkdf-key-schedule
- ech-sni
estMin: 13
lang: ru
level: senior
order: 8
prereqs: []
slug: quiz-short
sources:
- https://datatracker.ietf.org/doc/html/rfc8446
- https://datatracker.ietf.org/doc/html/rfc8470
- https://blog.cloudflare.com/rfc-8446-aka-tls-1-3/
status: ready
summary: Вопросы на свободное припоминание по юниту TLS 1.3. Сначала ответь своими словами, затем раскрой модельный ответ и сравни.
title: 'TLS 1.3: обзор на припоминание'
track: networking
unit: 05-tls-handshake
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";

<Hook>
Припоминание сильнее перечитывания. На каждый вопрос проговори или запиши полный ответ по памяти, прежде чем открыть модельный, — именно усилие припоминания закрепляет механизм.
</Hook>

<Goal lang="ru">
Восстанови ключевые механизмы юнита — handshake за 1 RTT, forward secrecy, transcript hash, защиту от replay в 0-RTT, key schedule на HKDF и приватность SNI — не подглядывая в уроки.
</Goal>

<RetrievalDrawer
  client:visible
  id="05-tls-handshake-short-retrieval"
  lang="ru"
  questions={[
    { q: "Пройди холодный handshake TLS 1.3 от ClientHello до первого зашифрованного байта и объясни, почему он стоит ровно одного RTT.", a: "ClientHello несёт список шифров, supported groups, signature algorithms и key_share хотя бы с одним эфемерным ECDHE-публичным ключом (обычно x25519). Сервер выбирает suite и группу, шлёт свой key_share в ServerHello, затем сразу выводит handshake traffic key и отправляет EncryptedExtensions, Certificate, CertificateVerify и server Finished — всё зашифровано — в том же проходе. Клиент выводит тот же ключ, проверяет цепочку и CertificateVerify, проверяет server Finished, затем шлёт client Finished плюс первые данные приложения. ClientHello туда, поток сервера обратно, client Finished с данными туда: один round trip. TLS 1.2 требовал двух, потому что согласование параметров и обмен ключами были разными фазами." },
    { q: "Почему размещение эфемерного ECDHE key_share в ClientHello автоматически даёт Perfect Forward Secrecy?", a: "Обе стороны генерируют свежие эфемерные пары ключей для этого соединения и выбрасывают их по его завершении; общий секрет выводится из этих эфемерных ключей через ECDHE, а не из какого-либо долгоживущего ключа. Поэтому, если атакующий позже украдёт долгоживущий подписывающий ключ сервера, восстанавливать ключи прошлой сессии нечем — эфемеры уже исчезли. Опциональный RSA key transport в TLS 1.2 ломал это: клиент шифровал секрет сессии под долгоживущим публичным ключом сервера, и украденный приватный ключ расшифровывал каждую записанную прошлую сессию. TLS 1.3 предписывает эфемерный обмен, делая PFS поведением по умолчанию без настройки." },
    { q: "Что такое transcript hash и как он делает handshake устойчивым к подделке, включая попытки понижения?", a: "Каждый байт handshake попадает в текущий дайджест SHA-256/384 — transcript hash. Сообщение Finished — это HMAC над этим transcript на выведенном finished-ключе. Если хоть один байт handshake был перевёрнут, заменён или переставлен — багом или man-in-the-middle — пересчитанный HMAC не сойдётся и соединение оборвётся до передачи данных приложения. Это ловит атаки понижения, потому что исходно предложенные версия и параметры записаны в transcript: middlebox, навязывающий TLS 1.2 или вырезающий ALPN h2, меняет transcript и ломает Finished с обеих сторон." },
    { q: "PSK-возобновление и 0-RTT оба переиспользуют кешированный секрет. В чём разница в безопасности между ними?", a: "PSK-возобновление всё ещё стоит одного RTT: клиент предъявляет тикет в ClientHello, сервер узнаёт PSK и выводит из него ключи, пропуская проверку сертификата — но round-trip от ClientHello до ответа всё равно происходит, поэтому данные не более подвержены replay, чем обычные данные 1-RTT. 0-RTT идёт дальше: клиент шифрует первый запрос под PSK и прикрепляет его к ClientHello, до завершения handshake. У этой early data нет per-connection handshake-nonce, защищающего её, поэтому атакующий может захватить и переиграть её. Отсюда 0-RTT ограничен идемпотентными запросами, а серверы обязаны возвращать 425 Too Early для изменяющих маршрутов." },
    { q: "Перечисли три ортогональные защиты от replay в 0-RTT, которые продакшен-развёртывания слоят вместе, и что ловит каждая.", a: "(1) Проверка возраста тикета: сервер восстанавливает реальный возраст из obfuscated_ticket_age, учитывает ожидаемый RTT и отклоняет early data вне узкого окна (например, 10 с), так что replay часами позже отбрасывается. (2) Кеш одноразового тикета: ключ (ticket_nonce, age_bucket), отклоняет любую вторую попытку расшифровки в окне — Cloudflare держит per-PoP кеши плюс cross-PoP gossip. (3) Идемпотентность на уровне приложения: только безопасные методы попадают в early data, а фреймворки возвращают 425 Too Early для любого обработчика, меняющего состояние, так что даже replay, проскользнувший мимо первых двух слоёв, не вызовет вредной мутации." },
    { q: "Почему TLS 1.3 пропускает ECDHE-секрет через key schedule на HKDF с domain-separated метками, а не использует его напрямую, и почему SNI всё ещё нуждается в ECH?", a: "Общий секрет питает HKDF-Extract/Expand-Label, порождая дерево ключей — early, handshake, master — каждая ветвь помечена (c hs traffic, s ap traffic, res master). Разделение доменов означает, что компрометация одного выведенного ключа не помогает атакующему вывести другую ветвь, поэтому ключи handshake, приложения и возобновления криптографически изолированы. SNI — отдельная проблема: он шлётся открытым текстом в ClientHello, чтобы сервер на общем IP выбрал верный сертификат прежде, чем сможет что-либо расшифровать, что раскрывает посещаемое имя хоста наблюдателям на пути. Encrypted ClientHello (ECH) шифрует внутренний ClientHello через HPKE под опубликованным в DNS ключом origin, и наблюдатель видит только обобщённый front-end." },
  ]}
/>

<Recap lang="ru">
Если ты смог восстановить каждый ответ по памяти, у тебя есть стержень юнита: ECDHE key_share в ClientHello даёт 1-RTT и PFS вместе; transcript hash делает каждый согласованный параметр устойчивым к подделке; PSK-возобновление меняет свежую аутентификацию на скорость, оставаясь безопасным к replay, но 0-RTT меняет round-trip на уязвимость к replay, которую должны сдерживать три слоя защиты; key schedule на HKDF изолирует каждую ветвь ключей; а SNI всё ещё раскрывает имя хоста, пока его не зашифрует ECH.
</Recap>
