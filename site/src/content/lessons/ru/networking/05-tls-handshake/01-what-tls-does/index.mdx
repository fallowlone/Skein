---
slug: 01-what-tls-does
lang: ru
track: networking
unit: 05-tls-handshake
order: 1
title: "Что делает TLS и зачем он нужен"
summary: "TLS строит запечатанный канал поверх ненадёжной сети — до TLS 1.3 это требовало двух round-trip, понимание причин открывает путь ко всему остальному."
estMin: 8
status: ready
lessonType: topic
level: junior
prereqs: ["04-dns-resolution/05-encrypted-dns"]
deepensInto: ["02-the-1rtt-handshake"]
spiral: ["encapsulation"]
concepts: ["tls", "https", "sealed-channel", "certificate-authority", "pki", "confidentiality", "integrity"]
sources:
  - https://datatracker.ietf.org/doc/html/rfc8446
  - https://hpbn.co/transport-layer-security-tls/
  - https://developer.mozilla.org/ru/docs/Web/Security/Transport_Layer_Security
---

import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import MetaphorComplete from "~/components/pedagogy/MetaphorComplete.astro";
import NumbersCard from "~/components/layout/NumbersCard.astro";

<Hook lang="ru">
TCP согласовал номера последовательности, DNS разрешил имя в IP. Теперь браузеру и серверу нужно договориться о шифровании прежде, чем передавать что-либо ценное. Это согласование и есть TLS-рукопожатие — разница между открыткой и запечатанным конвертом.
</Hook>

<Crux>TLS строит запечатанный канал: его может перехватить любой, но прочитать содержимое — только отправитель и получатель.</Crux>

<Explanation>

## Что делает TLS одним предложением

TLS (Transport Layer Security) оборачивает каждый байт данных приложения в симметрично зашифрованный конверт, который никто в сети не может прочитать. Конверт согласовывается с помощью асимметричной криптографии в ходе рукопожатия; после согласования симметричный ключ обрабатывает все данные на полной скорости.

Простой HTTP передаёт каждое слово, которое вы вводите, в открытом виде. Ваш пароль, баланс банка, читаемая статья — всё это видно любому в той же кафе-сети wifi, вашему провайдеру или корпоративному прокси. TLS превращает тот же провод в закрытый туннель.

## Метафора: запечатанный конверт

Антон хочет отправить Диме сообщение, но боится, что почтальон его прочитает. Он кладёт сообщение в коробку, запирает печатью и отправляет. Только у Димы есть подходящий ключ. Перед отправкой обе стороны договариваются о печати и ключе: Антон предлагает замки, которые знает, Дима выбирает один и присылает свою половину ключа плюс ID-карту от Кары — доверенного издателя. Они объединяют половины в общую печать. TLS 1.3 сделал встречу короче, позволив Антону прислать свою половину в первом сообщении.

## Три гарантии TLS

1. **Конфиденциальность** — никто в сети не может прочитать данные.
2. **Целостность** — никто не может незаметно изменить данные.
3. **Аутентификация** — сервер доказывает, что владеет доменом (через сертификат от доверенного CA).

<NumbersCard
  id="card-tls-overview"
  title="TLS 1.3: кратко"
  data-lesson-visual
  stats={[
    { label: "TLS 1.2 холодное рукопожатие", value: "2 RTT" },
    { label: "TLS 1.3 холодное рукопожатие", value: "1 RTT" },
    { label: "TLS 1.3 возобновление (PSK)", value: "1 RTT (без проверки сертификата)" },
    { label: "TLS 1.3 0-RTT", value: "0 RTT для запроса" },
    { label: "Доля HTTPS в веб-трафике (2025)", value: "≥95%" },
    { label: "Браузер показывает замок когда", value: "TLS-рукопожатие успешно" },
  ]}
/>

## Один сценарий от начала до конца

Вы набираете `https://`. Браузер уже завершил DNS (знает IP) и TCP (соединение открыто). Теперь он строит TLS-печать. После её построения каждый HTTP-запрос и ответ едет внутри неё. Браузер показывает замок.

Замок означает: **никто в сети не может прочитать то, что проходит между вами и этим хостом.** Это не означает, что сайт заслуживает доверия — мошеннический сайт может иметь валидный замок. Это не означает, что устройство в безопасности. Гарантируется только канал.

<Quiz
  id="jr-quiz-1"
  pieceSlug="01-what-tls-does"
  lang="ru"
  question="Что на самом деле защищает TLS?"
  choices={[
    { label: "Только поле пароля, которое я ввожу", misconception: "TLS защищает весь канал, не одно поле. Всё, что вы отправляете и получаете, запечатано." },
    { label: "Всё соединение между вами и сайтом", correct: true },
    { label: "Содержимое сайта от изменений хакерами в будущем", misconception: "TLS защищает данные при передаче, а не данные на сервере." },
    { label: "Ваше устройство от взлома", misconception: "TLS работает на уровне канала. Скомпрометированное устройство — отдельная проблема." },
  ]}
/>

<Quiz
  id="jr-quiz-2"
  pieceSlug="01-what-tls-does"
  lang="ru"
  question="Иконка замка в браузере означает..."
  choices={[
    { label: "Этому сайту можно доверять что угодно", misconception: "Замок означает только, что канал запечатан. Мошеннический сайт может иметь валидный замок." },
    { label: "Никто в сети не может прочитать то, что проходит между вами и этим хостом", correct: true },
    { label: "Ваш пароль не может быть украден нигде", misconception: "Гарантия только канала. Скомпрометированное устройство или сервер всё равно утечёт." },
    { label: "Сайт проверен удостоверяющим центром", misconception: "CA проверяют контроль над доменом, а не поведение сайта." },
  ]}
/>

<DragOrder
  id="jr-drag-1"
  pieceSlug="01-what-tls-does"
  lang="ru"
  prompt="Расставьте шаги TLS-рукопожатия по порядку (упрощённо):"
  items={[
    "Антон отправляет список поддерживаемых шифров и свою половину ключа",
    "Дима выбирает шифр, отправляет свою половину ключа и сертификат",
    "Антон проверяет сертификат против доверенных CA",
    "Обе стороны объединяют половины в общий секрет",
    "Каждое сообщение после этого момента зашифровано",
  ]}
/>

<MetaphorComplete
  id="jr-metaphor-1"
  pieceSlug="01-what-tls-does"
  lang="ru"
  setup="Заполните пропуск: TLS превращает ваше соединение в запечатанный _______, который может открыть только сайт."
  accepted={["конверт", "туннель", "канал", "контейнер", "ящик"]}
  canonical="конверт (или туннель, канал)"
  explanation="Метафора запечатанного конверта: все видят, что он прошёл, но прочитать содержимое может только адресат."
/>

<Inset kind="why" lang="ru">
**Почему TLS 1.3 сократил рукопожатие до одного RTT.** TLS 1.2 требовал двух round-trip: один для согласования параметров шифра, второй для обмена ключевым материалом. TLS 1.3 поместил ключевой материал клиента в самое первое сообщение — ClientHello — чтобы сервер мог вычислить общий секрет и начать шифрование в том же проходе. На межконтинентальных линках это экономит полный RTT (80–300 мс) при каждом холодном соединении.
</Inset>

</Explanation>

<KeyTakeaway>TLS строит запечатанный канал с тремя гарантиями: конфиденциальность, целостность и аутентификация сервера — рукопожатие согласовывает ключ однажды, затем симметричное шифрование защищает все данные, и TLS 1.3 делает это за один RTT вместо двух.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="01-what-tls-does-retrieval"
  lang="ru"
  questions={[
    { q: "Одним предложением: что произошло бы, если бы вы посетили банковский сайт без TLS?", a: "Любой в той же сети (кафе wifi, провайдер, корпоративный прокси) мог бы прочитать ваш логин, пароль, баланс и каждое действие в открытом виде — TLS защищает данные при передаче, не только пароли." },
    { q: "Какие три гарантии даёт TLS, и какую из них обеспечивает сертификат?", a: "Конфиденциальность (никто не читает данные), целостность (никто незаметно не изменяет) и аутентификация (сервер доказывает, что владеет доменом). Сертификат обеспечивает аутентификацию: он связывает публичный ключ сервера с именем домена, подписанным доверенным CA." },
    { q: "Почему TLS 1.2 требовал двух RTT, а TLS 1.3 — только одного?", a: "TLS 1.2 разделял согласование параметров (первый RTT) и обмен ключами (второй RTT). TLS 1.3 поместил эфемерный публичный ключ клиента в первое сообщение, поэтому сервер может немедленно вывести общий секрет и ответить зашифрованными данными в том же проходе." },
  ]}
/>

<Recap lang="ru">
TLS оборачивает каждый байт данных в зашифрованный конверт, который все в сети видят проходящим, но только отправитель и получатель могут прочитать. Он даёт три гарантии: конфиденциальность (шифрование), целостность (MAC/хеш транскрипта) и аутентификацию (сертификат сервера подтверждает владение доменом перед доверенным CA). До TLS 1.3 построение этого канала требовало двух round-trip; TLS 1.3 сократил это до одного, поместив ключевой материал клиента в открывающее сообщение. Замок означает только, что канал запечатан — не то, что сайт заслуживает доверия или устройство в безопасности.
</Recap>
