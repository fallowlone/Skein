---
concepts:
- openssl-s-client
- certificate-chain
- early-data
- key-share
- hybrid-pq
estMin: 14
lang: ru
level: senior
order: 9
prereqs: []
slug: quiz-code
sources:
- https://datatracker.ietf.org/doc/html/rfc8446
- https://datatracker.ietf.org/doc/html/rfc8470
- https://blog.cloudflare.com/rfc-8446-aka-tls-1-3/
status: ready
summary: Читай реальный вывод openssl s_client, конфиг ticket-ключа Nginx, обработчик Early-Data и трассу key_share, затем выбирай диагноз и самое рычажное исправление.
title: 'TLS 1.3: чтение трасс и конфигов'
track: networking
unit: 05-tls-handshake
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Проблемы TLS диагностируются в трассах handshake, конфигах сервера и заголовках запросов — а не в учебных схемах. Прочитай каждый артефакт, предскажи поведение и выбери исправление, которое senior-инженер сделает первым.
</Hook>

<Goal lang="ru">
Отработай цикл, который ты запускаешь в каждом инциденте TLS: прочитай вывод openssl или конфиг, найди нарушенный инвариант handshake и потянись за самым рычажным исправлением.
</Goal>

## Сниппет 1 — проверка цепочки в openssl s_client

```
% openssl s_client -connect api.example.com:443 -tls1_3
depth=0 CN = api.example.com
verify error:num=20:unable to get local issuer certificate
verify error:num=21:unable to verify the first certificate
---
Certificate chain
 0 s:CN = api.example.com
   i:CN = R3 Issuing CA
---
New, TLSv1.3, Cipher is TLS_AES_256_GCM_SHA384
Verify return code: 21 (unable to verify the first certificate)
```

<Quiz
  id="05-tls-handshake-code-1"
  lessonSlug="05-tls-handshake"
  lang="ru"
  question="Handshake завершается и cipher согласован, но verify return code равен 21. Что сконфигурировано неверно?"
  choices={[
    { label: "Сервер отправляет только leaf-сертификат (длина цепочки 1). 'R3 Issuing CA' — это intermediate, а не доверенный корень, поэтому клиент не может построить путь до своего trust store. Исправление: настрой сервер слать leaf плюс intermediate(ы) — например, конкатенируй intermediate в файл ssl_certificate Nginx.", correct: true },
    { label: "Leaf-сертификат истёк, поэтому проверка падает на depth 0.", misconception: "Истечение даёт ошибку 10 (certificate has expired), а не 20/21. Ошибка 20/21 означает неполную цепочку issuer — intermediate отсутствует в том, что прислал сервер." },
    { label: "TLS_AES_256_GCM_SHA384 — невалидный suite TLS 1.3, что ломает проверку.", misconception: "Это стандартный suite TLS 1.3, и handshake успешно его согласовал. Ключи вывелись нормально; сбой — чисто построение цепочки, отдельный шаг от обмена ключами." },
    { label: "У клиента нет SNI, поэтому сервер вернул не тот сертификат.", misconception: "openssl s_client шлёт SNI по умолчанию, и возвращённый CN совпадает с хостом. Цепочка для правильного хоста — ей просто не хватает intermediate, нужного клиенту для достижения корня." },
  ]}
/>

## Сниппет 2 — конфиг ticket-ключа сессии в Nginx

```nginx
server {
    listen 443 ssl;
    ssl_protocols TLSv1.3;
    ssl_session_ticket_key /etc/nginx/ticket.key;   # generated once at install
    ssl_early_data on;
    # ...
}
```

<Quiz
  id="05-tls-handshake-code-2"
  lessonSlug="05-tls-handshake"
  lang="ru"
  question="Этот конфиг работает без изменений год. Каков самый серьёзный дефект безопасности, учитывая, что ssl_early_data включён?"
  choices={[
    { label: "Ключ шифрования session ticket (STEK) статичен и сохранён на диск, никогда не ротируется. Один утёкший файл ключа даёт атакующему расшифровать каждую возобновлённую сессию и все early data 0-RTT, что он защищал — у возобновления нет forward secrecy. Ротируй STEK минимум ежечасно, держи ключи в памяти и распространяй по аутентифицированным каналам.", correct: true },
    { label: "Сам ssl_early_data on и есть уязвимость, его надо отключить.", misconception: "Early data 0-RTT приемлема для идемпотентных маршрутов с защитой от replay. Глубже здесь дефект — никогда не ротируемый, лежащий на диске STEK, который подрывает forward secrecy возобновления независимо от early data." },
    { label: "TLSv1.3 only слишком строг и сломает старых клиентов — вот главная проблема.", misconception: "Ограничение до TLS 1.3 — это выбор в пользу безопасности, а не дефект. Проблема безопасности — статичный, лежащий на диске ticket-ключ." },
    { label: "Путь к ключу абсолютный, что раскрывает структуру файловой системы в сообщениях об ошибках.", misconception: "Абсолютность пути несущественна для безопасности TLS. Настоящая проблема в том, что ключ статичен и на диске — STEK должны ротироваться и оставаться в памяти." },
  ]}
/>

## Сниппет 3 — обработчик запроса Early-Data

```js
app.post("/transfer", (req, res) => {
  // money movement
  if (req.headers["early-data"] === "1") {
    // request arrived in TLS 0-RTT early data
  }
  doTransfer(req.body);
  res.send("ok");
});
```

<Quiz
  id="05-tls-handshake-code-3"
  lessonSlug="05-tls-handshake"
  lang="ru"
  question="Заголовок Early-Data читается, но обработчик всё равно вызывает doTransfer в любом случае. Что пойдёт не так при replay и каково верное исправление?"
  choices={[
    { label: "Атакующий может захватить и переслать 0-RTT-запрос, поэтому doTransfer выполнится дважды — дублированное движение денег. Обработчик должен при Early-Data равном 1 на изменяющем маршруте возвращать 425 Too Early, чтобы клиент повторил передачу после завершения handshake; только тогда вызывать doTransfer.", correct: true },
    { label: "Ничего — чтение заголовка Early-Data автоматически делает запрос безопасным к replay.", misconception: "Чтение заголовка само по себе ничего не меняет; код всё ещё выполняет мутацию. Нужно отклонять с 425 Too Early на изменяющих маршрутах при Early-Data равном 1, заставляя повтор после handshake." },
    { label: "Тело будет undefined в early data, поэтому перевод тихо ничего не сделает.", misconception: "Early data несёт полный запрос, включая тело; он обрабатывается нормально. Опасность именно в том, что он обрабатывается — и может быть переигран, чтобы обработаться дважды." },
    { label: "TLS отклонит POST на уровне записей до того, как он дойдёт до обработчика.", misconception: "TLS доставляет early data приложению; он не знает, что твой маршрут не идемпотентен. Обеспечение идемпотентности — задача уровня приложения через 425 Too Early." },
  ]}
/>

## Сниппет 4 — трасса клиентского key_share

```
ClientHello key_share: x25519 (0x001d), X25519MLKEM768 (0x11ec)
ServerHello: HelloRetryRequest, selected_group = secp256r1 (0x0017)
```

<Quiz
  id="05-tls-handshake-code-4"
  lessonSlug="05-tls-handshake"
  lang="ru"
  question="Клиент предложил x25519 и гибридный X25519MLKEM768, но сервер прислал HelloRetryRequest с запросом secp256r1. Что произошло и какова цена?"
  choices={[
    { label: "Сервер не поддерживает ни одну из предложенных групп, поэтому прислал HelloRetryRequest с именем secp256r1 (P-256). Клиент должен переслать свежий ClientHello с key_share для P-256 плюс cookie из HRR, что стоит одного лишнего RTT. Предложение P-256 сразу рядом с x25519 предотвратило бы повтор.", correct: true },
    { label: "HelloRetryRequest означает, что сертификат не прошёл проверку, поэтому клиент должен переаутентифицироваться.", misconception: "HRR касается чисто согласования группы key_share, а не аутентификации. Он шлётся, когда ни одна из предложенных клиентом групп не совпадает с тем, что использует сервер." },
    { label: "Гибридная группа X25519MLKEM768 невалидна, поэтому весь handshake обрывается.", misconception: "X25519MLKEM768 — валидная гибридная named group; сервер просто её не поддерживает. Handshake продолжается через HRR с группой, принимаемой обеими сторонами, стоит одного RTT, а не обрывается." },
    { label: "HelloRetryRequest понижает соединение до TLS 1.2.", misconception: "HRR остаётся внутри TLS 1.3 и лишь пересогласует группу key_share. Версию протокола он не меняет; попытки понижения сломали бы проверку transcript в Finished." },
  ]}
/>

<Recap lang="ru">
Каждый инцидент TLS читается в артефактах: код verify 20/21 в openssl означает неполную цепочку — отправь intermediate; статичный лежащий на диске ssl_session_ticket_key разрушает forward secrecy возобновления — ротируй STEK ежечасно в памяти; обработчик Early-Data, который мутирует без возврата 425 Too Early, поддаётся replay — гейти изменяющие маршруты по заголовку Early-Data; а HelloRetryRequest с именем другой группы означает, что предложенные key_share не попали — предлагай x25519 плюс P-256 сразу, чтобы избежать лишнего RTT. Прочитай артефакт, найди нарушенный инвариант, исправь причину.
</Recap>
