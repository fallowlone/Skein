---
concepts:
- attack-economics
- amplification
- syn-cookies
- waf-vs-firewall
- dnssec-rpki
- defense-in-depth
estMin: 14
lang: ru
level: senior
order: 8
prereqs: []
slug: quiz-short
sources:
- https://blog.cloudflare.com/ddos-threat-report-2025-q4/
- https://datatracker.ietf.org/doc/html/rfc6797
- https://isbgpsafeyet.com/
status: ready
summary: "Промпты на свободное припоминание по всему юниту. Сначала ответьте своими словами, затем раскройте модельный ответ и сравните."
title: 'Сетевая безопасность: тест на припоминание'
track: networking
unit: 11-network-security
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";

<Hook>
Припоминание сильнее перечитывания. Для каждого промпта проговорите или запишите полный ответ по памяти, прежде чем открыть модельный — именно усилие припоминания закрепляет юнит.
</Hook>

<Goal lang="ru">
Восстановите спину юнита, не подглядывая: почему экономика атак благоволит одинокому защитнику, как работают amplification и истощение состояния, где кончается firewall и начинается WAF, что защищают DNSSEC и RPKI, и почему defense in depth обязательна.
</Goal>

<RetrievalDrawer
  client:visible
  id="11-network-security-short-retrieval"
  lang="ru"
  questions={[
    { q: "Почему экономика DDoS благоволит атакующему только если вы защищаетесь в одиночку, и как CDN это переворачивает?", a: "Стоимость асимметрична: ботнет на 10–100 Гбит/с арендуется за ~$50–500/месяц, тогда как одинокому origin пришлось бы купить столько аплинка, чтобы поглотить это — экономически невозможно. CDN с сотнями anycast PoP имеет суммарную ёмкость в десятки Тбит/с и амортизирует её на миллионы клиентов, поэтому стоимость защиты на клиента ничтожна. Ботнет в одиночку не переиграть; общая инфраструктура делает атаку проваленной и потому экономически непривлекательной. Рычаг — не покупка большего аплинка, а распределение поглощения по edge, за который вы платите не в одиночку." },
    { q: "Объясните amplification/reflection и почему это выбор атакующего-экономиста.", a: "Атакующий шлёт маленький UDP-запрос с подделанным source IP жертвы на открытый stateless сторонний сервер (DNS ANY, NTP monlist, memcached). Сервер отвечает жертве куда большим ответом — коэффициенты усиления от ~42x для DNS до ~1024x для memcached. Атакующий тратит почти ничего, а жертву бьёт легитимно выглядящая инфраструктура, которую нельзя занести в блоклист, не сломав реальные сервисы. Эта асимметрия — крошечная цена атаки, огромный урон жертве, ни одного IP для блокировки — и есть причина, почему открытые резолверы и неверно настроенный memcached атакуют постоянно. Структурный фикс — закрытие открытых сервисов и поглощение объёма на edge." },
    { q: "Как SYN cookies побеждают SYN-flood и в чём компромисс?", a: "SYN-flood шлёт миллионы спуфнутых SYN; каждый обычно выделяет полуоткрытый слот в backlog, и при заполнении сервер отбрасывает легитимные SYN. SYN cookies не хранят состояние: при переполнении backlog сервер кодирует параметры соединения в ISN как хэш source/dest/port/timestamp и шлёт SYN-ACK без выделения слота. Легитимный клиент возвращает верный ACK, из которого сервер восстанавливает состояние; спуфнутый flood не даёт валидных ACK и не тратит память. Компромисс — память на CPU: вычисление хэша на каждое принятое соединение вместо хранимой записи backlog." },
    { q: "Где firewall перестаёт быть достаточным и начинается WAF, и почему ни один не достаточен в одиночку?", a: "Firewall инспектирует L3/L4 — source IP, порт, TCP-флаги — поэтому останавливает сканы портов, спуфинг и нарушения протокола, но слеп к зашифрованным HTTPS-нагрузкам и к валидно выглядящим HTTP-атакам. WAF инспектирует L7 после терминации TLS — заголовки, query-параметры, тела — сопоставляя SQLi, XSS, path traversal и паттерны ботов, но никогда не видит floods L3/L4 вроде SYN-атак. Они покрывают непересекающиеся слои: firewall пропустит валидный HTTP SQLi насквозь, а WAF не тронет SYN-flood. Продакшну нужны оба плюс rate limiting и adaptive concurrency, потому что у каждого инструмента есть слепое пятно, которое закрывает следующий." },
    { q: "Что защищает DNSSEC против RPKI/ROV, и почему публикация ROA без применения ROV — лишь половина защиты?", a: "DNSSEC защищает целостность DNS-ответов: операторы зон подписывают каждый RRset, а резолверы отвергают подделанные ответы (например, отравление кэша Kaminsky) без валидной подписи. RPKI/ROV защищает BGP-маршрутизацию: держатели ресурсов публикуют ROA, объявляя, какая AS может анонсировать какой префикс, а маршрутизаторы с ROV отвергают анонсы от неавторизованных AS (захваты/утечки маршрутов). Они охраняют разные слои — DNS-ответ против origin маршрута. ROA лишь объявляет легитимный origin; он ничего не останавливает, пока маршрутизаторы реально не отбрасывают невалидные маршруты, а на 2026 только ~25% сетей применяют ROV. Так что захват всё равно распространяется через ~75%, которые не валидируют. Реальная защита нужна и в публикации ROA, и в широком применении ROV — обе половины." },
    { q: "Почему ни один слой не достаточен против DDoS, и как выглядит полный стек defense in depth по порядку?", a: "У каждого слоя есть слепое пятно, куда переходит атакующий: anycast edge поглощает объёмные floods, но пропускает малообъёмное L7-злоупотребление; stateless L3/L4-фильтры отбрасывают amplification и SYN floods, но пропускают валидный HTTP; WAF ловит известные сигнатуры, но пропускает zero-day, обфускацию и злоупотребление бизнес-логикой; per-IP rate limits останавливают очевидные ботнеты, но пропускают распределённые floods через резидентные прокси; mTLS останавливает латеральное движение, но не внешние атаки. Стек от edge к origin: anycast edge scrubbing, затем stateless L3/L4 rate limits, затем WAF на сбалансированном уровне паранойи (PL2-3), затем token-bucket лимиты на IP и на пользователя, затем adaptive concurrency limiting на origin, с наблюдаемостью и эскалацией к людям, когда автоматика отказывает. Поскольку атакующий адаптируется, вы ставите реактивные (по нагрузке) защиты за проактивными (по контенту)." },
  ]}
/>

<Recap lang="ru">
Если вы смогли восстановить каждый ответ по памяти, вы держите спину юнита: экономика атак благоволит одинокому защитнику, поэтому вы делите инфраструктуру; amplification и SYN floods превращают дешёвого атакующего во flood, который вы поглощаете, а не блокируете; firewall и WAF покрывают непересекающиеся слои; DNSSEC и RPKI/ROV защищают DNS-ответы и BGP-маршруты соответственно — каждый бесполезен как полумера; и ни один слой не останавливает все векторы, поэтому defense in depth стекает anycast edge, L3/L4-фильтры, WAF, rate limits, mTLS и adaptive concurrency, с людьми на дежурстве на остальное.
</Recap>
