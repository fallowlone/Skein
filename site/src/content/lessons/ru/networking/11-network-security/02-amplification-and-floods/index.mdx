---
concepts:
- amplification-attack
- reflection
- dns-amplification
- ntp-amplification
- memcached-amplification
- syn-flood
- syn-cookies
- http2-rapid-reset
deepensInto:
- 06-defense-in-depth
estMin: 12
lang: ru
lessonType: topic
level: middle
order: 2
prereqs:
- 01-ddos-what-and-why
- 03-tcp-handshake/02-state-machine-and-teardown
- 04-dns-resolution/01-resolution-chain
slug: 02-amplification-and-floods
sources:
- https://blog.cloudflare.com/ddos-threat-report-2025-q4/
- https://www.cisa.gov/news-events/alerts/2023/10/10/http2-rapid-reset-vulnerability-cve-2023-44487
- https://datatracker.ietf.org/doc/html/rfc4987
spiral:
- statefulness
- latency
status: ready
summary: Атаки усиления используют открытые сторонние серверы для умножения полосы
  атакующего в 50–1280 раз; атаки истощения состояния заполняют таблицы backlog TCP,
  отбрасывая легитимные соединения.
title: Атаки усиления и истощение состояния
track: networking
unit: 11-network-security
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import TraceScenario from "~/components/pedagogy/TraceScenario.astro";
import NumbersCard from "~/components/layout/NumbersCard.astro";
import Misconception from "~/components/layout/Misconception.astro";

<Hook>
Ваш CDN поглощает атаку, но DDoS-трафик идёт от легитимных DNS-резолверов — не от IP атакующего. Блокировка резолверов блокирует весь DNS. Атакующий отправил один маленький запрос на каждый резолвер; каждый резолвер ответил жертве данными в 50 раз больше. Добро пожаловать в атаку усиления.
</Hook>

<Crux>Усиление умножает полосу в 50–1280 раз через сторонние серверы; SYN-наводнения заполняют таблицы — IP для блокировки нет.</Crux>

<Explanation>

**Усиление и отражение: механизм.** Атака усиления эксплуатирует протокол без состояния на основе UDP на стороннем сервере. Атакующий отправляет маленький запрос с поддельным исходным IP (IP жертвы). Сторонний сервер отвечает на исходный IP — которым является жертва — намного большим ответом. Атакующему не нужно ничего получать; всю работу делает третья сторона. «Коэффициент усиления» — это соотношение размера ответа к размеру запроса.

| Протокол | Размер запроса | Размер ответа | Усиление |
|----------|---------------|---------------|----------|
| DNS (ANY-запись) | 40 байт | ~1 700 байт | 42x |
| NTP (monlist) | 40 байт | 468 байт | 12x (до 556x) |
| Memcached (UDP) | 50 байт | 51 200 байт | 1 024x |

<div data-lesson-visual class="my-4 p-4 border rounded bg-gray-50">
  <p class="text-sm font-semibold mb-2">Поток DNS-атаки усиления</p>
  <ol class="text-sm list-decimal list-inside space-y-1">
    <li>Атакующий отправляет DNS-запрос, исходный IP = IP жертвы, цель = открытый резолвер</li>
    <li>Открытый резолвер получает его и отвечает на исходный IP (жертве) большим ответом</li>
    <li>Жертва получает незапрошенные большие ответы от тысяч резолверов</li>
    <li>Каждый запрос атакующего стоит 40 байт; каждое попадание по жертве — ~1 700 байт</li>
    <li>Результат: 1 Гбит/с полосы атакующего → 42 Гбит/с атаки на жертву</li>
  </ol>
</div>

<Misconception id="amplification-cost" title="Усиление — экономически оптимальный выбор атакующих">
Ботнеты стоят ~$50–500/мес. Атаки усиления ничего не стоят: отправь один запрос, получи ответ в 50 раз больше. Открытые DNS-резолверы, NTP-серверы и неправильно сконфигурированный memcached — бесплатное оружие.
</Misconception>

**HTTP/2 Rapid Reset (CVE-2023-44487).** HTTP/2 позволяет несколько параллельных потоков. Атака Rapid Reset отправляет поток и немедленно его отменяет — до того, как сервер отправил ответ. Сервер всё равно выделяет память и CPU для запроса. Отправка и отмена тысяч потоков в секунду исчерпывает сервер на уровне CPU, не полосы. Маленький ботнет (20 000 машин) генерировал 398 миллионов запросов в секунду против Google с помощью этой техники в 2023 году. Исправление: HTTP/2-серверы должны ограничивать скорость создания потоков и считать клиентов с высоким темпом сброса атакующими.

<NumbersCard
  id="card-attack-numbers"
  title="Статистика атак из поля"
  stats={[
    { label: "Запросов/сек (HTTP/2 Rapid Reset)", value: "398M" },
    { label: "Крупнейший DDoS (дек 2025)", value: "31,4 Тбит/с" },
    { label: "Коэффициент усиления Memcached", value: "1 024x" },
    { label: "Машины ботнета (атака Rapid Reset)", value: "~20K" },
  ]}
/>

<TraceScenario
  id="mid-trace-1"
  lessonSlug="02-amplification-and-floods"
  lang="ru"
  scenario="Проследите DNS-атаку усиления от атакующего к жертве."
  steps={[
    { prompt: "Шаг 1: что отправляет атакующий?", reveal: "UDP-пакет с исходным IP = IP жертвы и целевым IP = публичный DNS-резолвер. Запрос запрашивает большую DNS-запись (например, ANY-запись для домена с DNSSEC-подписями)." },
    { prompt: "Шаг 2: что делает резолвер?", reveal: "Получает запрос и отправляет ответ на исходный IP в пакете — которым является IP жертвы. Ответ намного больше запроса (42x усиление для DNS)." },
    { prompt: "Шаг 3: какова роль атакующего после отправки запроса?", reveal: "Никакой. Атакующий отправляет один маленький запрос, и резолвер отправляет больший ответ прямо жертве. Атакующий усиливает свою атаку с минимальной полосой." },
    { prompt: "Шаг 4: почему это сложно защищать?", reveal: "Ответы приходят от легитимных DNS-резолверов, не от атакующего. Блокировка IP резолвера блокирует весь DNS-трафик. Решение: ограничение исходящих DNS-ответов и закрытие открытых резолверов (прекратить ответы на запросы из публичного Интернета)." },
  ]}
/>

**Истощение состояния: SYN-наводнения.** TCP-рукопожатие требует, чтобы сервер хранил состояние для каждого соединения в процессе. SYN-наводнение отправляет миллионы SYN-пакетов с поддельными исходными IP. Для каждого SYN сервер выделяет слот в SYN backlog и ждёт ACK, который никогда не придёт (потому что исходный IP поддельный). Когда backlog заполняется, сервер отбрасывает новые SYN-пакеты — включая те, что от легитимных клиентов. Linux SYN backlog по умолчанию: 128–512 слотов; под атакой заполняется за миллисекунды.

**SYN cookies: обмен памяти на CPU.** SYN cookies Linux (включены по умолчанию с ядра 2.6) позволяют избежать хранения состояния backlog. Когда backlog переполнился бы, сервер кодирует параметры соединения (ISN) как cookie, выведенный из исходного IP, порта и timestamp. Слот не выделяется. Если клиент отвечает правильным ACK, сервер восстанавливает состояние из cookie и завершает рукопожатие. Стоимость: немного выше CPU на новое соединение; выгода: нет исчерпания памяти в условиях наводнения.

<Quiz
  id="mid-quiz-1"
  lessonSlug="02-amplification-and-floods"
  lang="ru"
  question="В DNS-атаке усиления, почему атакующий использует поддельный исходный IP?"
  choices={[
    { label: "Чтобы скрыть свой IP от DNS-резолвера", misconception: "Резолвер логирует IP, отправивший запрос; подделка источника изменяет, кто получает ответ." },
    { label: "Чтобы заставить DNS-ответ пойти на IP жертвы вместо назад атакующему", correct: true },
    { label: "Чтобы ускорить запрос", misconception: "Исходный IP не влияет на скорость запроса." },
    { label: "Чтобы обойти аутентификацию", misconception: "DNS-запросы не аутентифицированы по умолчанию; поддельный источник — о перенаправлении ответа." },
  ]}
/>

<Quiz
  id="mid-quiz-2"
  lessonSlug="02-amplification-and-floods"
  lang="ru"
  question="Почему SYN cookie защита обменивает память на CPU?"
  choices={[
    { label: "Она выделяет больше памяти на соединение, чтобы избежать CPU-поиска", misconception: "SYN cookies снижают использование памяти — сервер ничего не хранит для входящих SYN." },
    { label: "Она кодирует состояние соединения в ISN cookie, а не сохраняет его, требуя хэш-вычисления для каждого ACK", correct: true },
    { label: "Она использует общий пул памяти для всех CPU", misconception: "SYN cookies работают per-core; общего пула нет." },
    { label: "Она откладывает TCP-рукопожатие на отдельный рабочий процесс", misconception: "SYN cookies работают в сетевом стеке ядра, а не в отдельном процессе." },
  ]}
/>

<Inset kind="edgecase" lang="ru">
ACK-наводнения — менее известный аналог SYN-наводнений. Атакующий отправляет миллионы ACK-пакетов на порт без активного соединения. Ядро должно ответить RST-пакетами (нет соединения для подтверждения). При сильном ACK-наводнении темп RST может потреблять значительный CPU. Защита: ограничение TCP RST (лимит RST в секунду по исходному IP) и подавление на уровне брандмауэра — отбрасывать ACK, не совпадающие ни с каким существующим соединением в таблице conntrack.
</Inset>

</Explanation>

<KeyTakeaway>Атаки усиления используют сторонние серверы для умножения полосы атакующего в 42–1024 раза путём подделки исходного IP в UDP-запросах. Атаки истощения состояния заполняют таблицы TCP backlog с помощью поддельных SYN — SYN cookies устраняют хранение состояния в памяти ценой хэш-вычисления на каждый ACK.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="02-amplification-and-floods-retrieval"
  lang="ru"
  questions={[
    { q: "Почему усиление намного экономичнее прямого ботнет-наводнения?", a: "При усилении атакующий отправляет крошечные запросы (40 байт), и третья сторона отправляет ответы в 42–1024 раза больше напрямую жертве. Ботнет из 10 000 машин, отправляющих по 100 Мбит/с каждая = 1 Тбит/с; с 1024x усилением memcached та же полоса генерирует эквивалент 1 Пбит/с атаки. Узкое место — ёмкость ответов открытого резолвера, а не полоса атакующего." },
    { q: "Что такое уязвимость HTTP/2 Rapid Reset и почему она обходит обычное ограничение скорости?", a: "Rapid Reset (CVE-2023-44487) отправляет и немедленно отменяет HTTP/2-потоки. Сервер выделяет CPU/память для каждого потока даже при отмене. Многие ограничители скорости считают завершённые запросы или ответы — не события создания потоков — поэтому атакующий остаётся под лимитом, исчерпывая ресурсы сервера. Исправление: ограничивать скорость создания потоков независимо от их завершённости." },
    { q: "Как SYN cookie защита устраняет исчерпание SYN backlog?", a: "Вместо выделения слота backlog для каждого SYN, сервер кодирует параметры соединения в ISN как хэш. Память не потребляется. Когда клиент отвечает ACK, сервер пересчитывает хэш для верификации и восстановления состояния. Flood-трафик просто производит хэш-вычисления без совпадающего ACK — память не исчерпывается." },
  ]}
/>

<Recap lang="ru">
Атаки усиления эксплуатируют открытые UDP-сервисы (DNS, NTP, Memcached), которые отвечают на поддельные запросы — атакующий отправляет 40 байт, жертва получает 1 700+ байт на запрос, умноженные на тысячи резолверов. HTTP/2 Rapid Reset — эквивалент прикладного уровня: 20 000 машин ботнета генерировали 398M запросов/сек, используя отмену потоков. SYN-наводнения исчерпывают таблицы соединений сервера с помощью миллионов полуоткрытых соединений через поддельные IP; SYN cookies защищают, кодируя состояние в TCP sequence number вместо хранения в памяти. Все три атаки разделяют одно свойство: атакующий тратит мало, защитник платит много.
</Recap>
