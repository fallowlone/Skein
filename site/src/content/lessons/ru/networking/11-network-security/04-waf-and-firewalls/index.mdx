---
concepts:
- waf
- firewall
- owasp-crs
- paranoia-level
- anomaly-scoring
- mtls
- spiffe
- hsts
- hsts-preload
- ssl-stripping
deepensInto:
- 06-defense-in-depth
estMin: 14
lang: ru
lessonType: topic
level: middle
order: 4
prereqs:
- 01-ddos-what-and-why
- 05-tls-handshake/01-tls-what-and-why
- 03-rate-limiting-algorithms
slug: 04-waf-and-firewalls
sources:
- https://owasp.org/www-project-modsecurity-core-rule-set/
- https://www.cloudflare.com/learning/ddos/ddos-attack-tools/slowloris/
- https://datatracker.ietf.org/doc/html/rfc6797
spiral:
- statefulness
- latency
status: ready
summary: Межсетевые экраны блокируют L3/L4-мусор; WAF проверяет HTTP-содержимое на
  паттерны атак; mTLS аутентифицирует сервисы; HSTS preload предотвращает атаки на
  понижение TLS.
title: WAF, межсетевые экраны, mTLS и HSTS
track: networking
unit: 11-network-security
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import NumbersCard from "~/components/layout/NumbersCard.astro";
import TraceScenario from "~/components/pedagogy/TraceScenario.astro";
import StackDiagram from "~/components/diagram/StackDiagram.astro";
import Infographic from "~/components/diagram/Infographic.astro";
import paranoiaTradeoff from "./infographics/paranoia-tradeoff.svg?raw";

<Hook>
Ваш межсетевой экран блокирует сканирование портов и явный мусор. Но злоумышленник отправляет валидные HTTP-запросы на порт 443. Межсетевой экран пропускает их все — он не видит внутрь зашифрованного TLS-пэйлоада. Для атак на прикладном уровне нужен другой инструмент, понимающий HTTP: WAF.
</Hook>

<Crux>МСЭ блокирует L3/L4; WAF блокирует HTTP-атаки. Ни один не достаточен. mTLS идентифицирует сервисы; HSTS preload устраняет downgrade.</Crux>

<Explanation>

**Межсетевые экраны против WAF: фундаментальное различие.** Межсетевой экран (L3/L4) проверяет IP- и TCP-заголовки: IP-адрес источника, порт назначения, TCP-флаги. Он блокирует сканирование портов, подделку IP и нарушения протокола. Он не видит внутрь зашифрованных HTTPS-пэйлоадов. Web Application Firewall (WAF, L7) проверяет содержимое HTTP-запроса/ответа после завершения TLS: заголовки, параметры запросов, тела запросов — сопоставляя с известными паттернами атак (SQL-инъекция, XSS, обход пути). WAF должен видеть открытый текст для инспекции, поэтому он завершает TLS на edge или располагается на origin за TLS-терминацией. Оба необходимы: межсетевой экран останавливает сетевой мусор, WAF — атаки на прикладном уровне.

<div data-lesson-visual class="overflow-x-auto my-4">
  <table class="text-sm w-full border-collapse">
    <thead>
      <tr class="bg-gray-100">
        <th class="border p-2 text-left">Инструмент</th>
        <th class="border p-2 text-left">Уровень</th>
        <th class="border p-2 text-left">Что проверяет</th>
        <th class="border p-2 text-left">Что останавливает</th>
        <th class="border p-2 text-left">Что пропускает</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border p-2">Межсетевой экран</td>
        <td class="border p-2">L3/L4</td>
        <td class="border p-2">IP src/dst, TCP-флаги, порт</td>
        <td class="border p-2">Сканирование портов, подделка IP, нарушения протокола</td>
        <td class="border p-2">Зашифрованные пэйлоады, валидные HTTPS-атаки</td>
      </tr>
      <tr>
        <td class="border p-2">WAF</td>
        <td class="border p-2">L7</td>
        <td class="border p-2">HTTP-заголовки, параметры запроса, тело</td>
        <td class="border p-2">SQLi, XSS, обход пути, паттерны ботов</td>
        <td class="border p-2">L3/L4-наводнения, SYN-наводнения, IP-атаки</td>
      </tr>
    </tbody>
  </table>
</div>

**Режимы обнаружения WAF.** Как WAF (Web Application Firewall — экран прикладного уровня) отличает попытку SQL-инъекции от обычного поискового запроса, содержащего слово «union»? Ответ зависит от режима обнаружения. Сигнатурный: сопоставление с базой известных паттернов атак. OWASP ModSecurity Core Rule Set (CRS) содержит паттерны вроде `union select` (SQL-инъекция), `<script>` (XSS) и `../../../etc/passwd` (обход пути). Быстрый, с низким числом ложных срабатываний на известных паттернах, но пропускает zero-day и обфусцированные варианты (например, `un/**/ion select`).

Аномальный/ML-based: моделирует нормальный трафик, помечает статистические выбросы. Обнаруживает новые атаки, но требует настройки — слишком строгий блокирует легитимных пользователей, слишком мягкий пропускает атаки.

**Аномальное скорирование OWASP CRS.** Каждое правило в OWASP CRS назначает оценку (1–8 баллов). Запрос блокируется при суммарной оценке >= 5. Уровни паранойи (PL1–PL4) контролируют количество активных правил: PL1 — разрешительный (меньше ложных срабатываний, пропускает часть атак); PL4 — строгий (блокирует больше, но и больше легитимных пользователей).

<NumbersCard
  id="false-positive-tradeoff"
  title="Компромисс при настройке WAF"
  stats={[
    { label: "Заблокированные легитимные запросы (PL1)", value: "~0.1% ложных срабатываний" },
    { label: "Заблокированные легитимные запросы (PL2)", value: "~0.5%" },
    { label: "Заблокированные легитимные запросы (PL3)", value: "~2%" },
    { label: "Заблокированные легитимные запросы (PL4)", value: "~5%" },
    { label: "Заблокированные атаки (PL1)", value: "~70% покрытие" },
    { label: "Заблокированные атаки (PL4)", value: "~95% покрытие" },
  ]}
/>

**Настройка WAF в production.** Начните с PL1 в режиме обнаружения (логировать и оповещать, не блокировать) на 1–2 недели. Соберите базовые метрики: распределение аномальных оценок на реальном трафике, попадания по категориям правил. Затем повышайте уровень паранойи и наблюдайте за частотой ложных срабатываний. Если ложные срабатывания превысят допустимое значение, настройте правила: добавьте в белый список известные ложные срабатывания, скорректируйте веса или исключите низкоточные правила. Цель: блокировать 95%+ атак, затрагивая менее 0.1% реальных пользователей.

<Infographic
  svg={paranoiaTradeoff}
  label="PL1 против PL4 по двум метрикам: PL1 даёт около 70% покрытия атак при около 0.1% заблокированных легитимных запросов, тогда как PL4 даёт около 95% покрытия при около 5% заблокированных — рост ложных срабатываний в 50 раз ради 25 дополнительных пунктов покрытия."
  caption="Рост уровня паранойи покупает покрытие атак ценой ложных срабатываний — поэтому начинают с PL1 в режиме обнаружения и настраивают к 95% покрытия при менее 0.1% блокировок."
  hue="var(--d-infra)"
/>

<Quiz
  id="mid-quiz-1"
  lessonSlug="04-waf-and-firewalls"
  lang="ru"
  question="Почему WAF неэффективен против SYN-наводнений?"
  choices={[
    { label: "Потому что WAF проверяет только L7 (прикладной уровень), а SYN-наводнения происходят на L4 (транспортном)", correct: true },
    { label: "Потому что SYN-наводнения шифруют атакующий трафик", misconception: "SYN-наводнения используют TCP-флаги, а не шифрование пэйлоада." },
    { label: "Потому что WAF не может отличить легитимный SYN от атакующего", misconception: "Это отчасти верно, но принципиальная причина — различие L4 и L7." },
    { label: "Потому что межсетевые экраны нельзя комбинировать с WAF", misconception: "Оба уровня необходимы и должны использоваться совместно." },
  ]}
/>

**mTLS для идентификации сервис-к-сервису.** Mutual TLS: и клиент, и сервер предъявляют X.509-сертификаты друг другу, а не только сервер клиенту. В service mesh (Istio, Linkerd) каждый sidecar-прокси использует mTLS для общения с соседними sidecar'ами, обеспечивая zero-trust: запрос от Pod A к Pod B зашифрован и аутентифицирован. SPIFFE (Secure Production Identity Framework for Everyone) выпускает краткосрочные сертификаты (часто ~1 час) через SDS (Service Discovery Service), который sidecar загружает и автоматически ротирует. Ключ sidecar никогда не покидает локальный контейнер; control plane обновляет сертификат вне диапазона.

Стоимость: каждый новый TLS-хендшейк добавляет 20–50 мс на старом оборудовании; ротация сертификатов увеличивает операционную сложность. Преимущество: боковое перемещение предотвращается, даже если сетевая структура контейнера скомпрометирована.

**TLS-стриппинг и HSTS.** SSL-стриппинг (Мокси Марлинспайк, 2009): MITM держит пользователя на HTTP, пока сам общается по HTTPS с реальным сервером, расшифровывая оба направления. Пользователь не знает, что находится на открытом тексте. Защита: HTTP Strict Transport Security (HSTS) — заголовок `Strict-Transport-Security: max-age=31536000` сообщает браузеру «используй только HTTPS для этого origin следующий год». Но заголовок доставляется по HTTP в первом ответе — если первый запрос уже был стриппирован, заголовок тоже стриппируется.

**HSTS preload.** Список доменов, встроенный в браузер, которые должны использовать только HTTPS — применяется до первого посещения домена. Google, Firefox, Chrome и Safari поставляют preload-списки. Вы запрашиваете включение своего домена; оно входит в следующий релиз браузера. Начиная с этого момента, HTTPS применяется даже для первых посетителей. Минимальный max-age для preload: 31 536 000 секунд (1 год). Требование в 1 год обусловлено тем, что preload поставляется с релизами браузера (циклы в 4 недели у Chrome) — более короткий max-age может истечь до того, как пользователи установят следующее обновление.

<TraceScenario
  id="mid-trace-1"
  lessonSlug="04-waf-and-firewalls"
  lang="ru"
  scenario="Проследите эшелонированную защиту от DDoS-атаки."
  steps={[
    { prompt: "Шаг 1: на ваш origin IP приходит объёмная атака 100 Гбит/с. CDN нет. Что происходит?", reveal: "Канал origin насыщается. Легитимные запросы не достигают сервера. Сервис недоступен." },
    { prompt: "Шаг 2: вы подключаете Cloudflare (CDN с DDoS-скруббингом). Что меняется?", reveal: "Трафик маршрутизируется сначала на ближайший PoP Cloudflare (центр очистки). Cloudflare поглощает атаку по своей инфраструктуре и проксирует на ваш origin только чистый трафик." },
    { prompt: "Шаг 3: атакующий переходит на HTTP-наводнение (меньше пакетов, выглядит легитимнее). Что делает Cloudflare?", reveal: "Ограничитель скорости на edge отклоняет запросы с IP, делающих более N запросов в секунду. Легитимные пользователи в среднем делают 10–20 req/s; атакующие пробуют тысячи в секунду." },
    { prompt: "Шаг 4: атакующий использует 10 тысяч IP одновременно, каждый ниже лимита на IP. Что следующее?", reveal: "WAF обнаруживает, что запросы имеют идентичные пэйлоады, заголовки или паттерны — невозможно для реальных разных пользователей. Он блокирует паттерн с помощью сигнатурных или аномально-скоринговых правил." },
  ]}
/>

<Quiz
  id="mid-quiz-2"
  lessonSlug="04-waf-and-firewalls"
  lang="ru"
  question="Почему HSTS preload требует минимального max-age в 1 год, а не 1 месяц?"
  choices={[
    { label: "Потому что браузеры проверяют HSTS раз в год", misconception: "Браузеры проверяют HSTS при каждом запросе; max-age контролирует, как долго они его запоминают." },
    { label: "Потому что релизы браузера выходят каждые 4 недели, и max-age в 1 месяц может истечь до установки следующего обновления", correct: true },
    { label: "Потому что заголовок HSTS кэшируется CDN на 1 год", misconception: "HSTS — это политика браузера, а не директива кэширования CDN." },
    { label: "Потому что 1 год требуется для получения валидации DNSSEC", misconception: "HSTS и DNSSEC — независимые механизмы." },
  ]}
/>

<Inset kind="why" lang="ru">
Почему mTLS — правильный ответ для zero-trust в микросервисах, а не IP-разрешение на сетевом уровне? IP-разрешение доказывает только «этот пакет пришёл из пода в подсети X» — но не какой именно сервис его отправил. Если любой под в разрешённой подсети скомпрометирован, он может имитировать любой другой сервис. mTLS доказывает «этот запрос пришёл от пода, имеющего валидный сертификат для сервиса Y, выданный нашим control plane в момент T». Краткосрочные сертификаты (1 час) означают, что даже при краже сертификата он быстро истекает. Сетевые контроли — это запасной вариант; криптографическая идентичность — правильный примитив.
</Inset>

</Explanation>

<StackDiagram
  label="Входящий трафик проходит сверху вниз: межсетевой экран проверяет L3/L4-заголовки, TLS завершается, WAF проверяет L7 HTTP-содержимое, затем чистые запросы достигают origin"
  hue="var(--d-network)"
  layers={[
    { label: "Межсетевой экран (L3/L4)", note: "IP, порт, TCP-флаги" },
    { label: "Терминация TLS", note: "расшифровка в открытый текст" },
    { label: "WAF (L7)", note: "тело HTTP, параметры, заголовки" },
    { label: "Origin", note: "только чистые запросы" },
  ]}
  caption="Каждый запрос проходит оба уровня по порядку. Межсетевой экран отбрасывает сканирование портов, подделку IP и нарушения протокола, но не видит внутрь зашифрованных пэйлоадов; после терминации TLS WAF проверяет открытый HTTP на паттерны SQLi, XSS и обхода пути. Ни один уровень не достаточен в одиночку — межсетевой экран пропускает HTTP-атаки на порт 443, WAF пропускает L3/L4-наводнения."
/>

<KeyTakeaway>Межсетевые экраны блокируют L3/L4-атаки; WAF блокирует L7-атаки, проверяя HTTP-содержимое. Ни один не достаточен в одиночку. mTLS обеспечивает криптографическую идентичность сервисов. HSTS preload предотвращает понижение TLS до первого посещения. Начните WAF с PL1 в режиме обнаружения, затем настройте до менее 0.1% ложных срабатываний перед блокировкой.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="04-waf-and-firewalls-retrieval"
  lang="ru"
  questions={[
    { q: "Объясните разницу между RPKI и DNSSEC. Почему они защищают разные уровни?", a: "RPKI (Resource Public Key Infrastructure) защищает BGP (L3): авторизует, какая AS может анонсировать какой IP-префикс, предотвращая перехват маршрутов. DNSSEC (DNS Security Extensions) защищает DNS-ответы (прикладной уровень L4): криптографически подписывает DNS RRset, предотвращая отравление кэша. BGP-перехват перенаправляет трафик на уровне маршрутизации; DNSSEC останавливает поддельные DNS-ответы. Они защищают разные проблемы — RPKI это целостность маршрутизации, DNSSEC это целостность DNS-ответов. Нужны оба." },
    { q: "Что такое SSL-стриппинг и почему HSTS preload решает проблему, с которой не справляется обычный заголовок HSTS?", a: "SSL-стриппинг понижает HTTPS-соединение пользователя до HTTP, перехватывая первый HTTP-запрос и удерживая пользователя на HTTP, пока атакующий общается по HTTPS с сервером. Обычный заголовок HSTS отправляется в HTTP-ответе — если первый запрос уже стриппирован, заголовок никогда не достигает браузера. HSTS preload встраивает список HTTPS-only доменов в сам бинарник браузера, применяя HTTPS до первого запроса — это делает SSL-стриппинг невозможным для preload-доменов." },
    { q: "Ваш WAF на PL2, атаки проходят. Вы поднимаете до PL4. Ложные срабатывания вырастают до 5%. Что лучше крайностей?", a: "Комбинируйте PL3 с ограничением скорости и адаптивным сбросом нагрузки. PL3 блокирует больше атак чем PL2 (~90% против ~80%) с меньшим числом ложных срабатываний чем PL4 (~2% против ~5%). Добавьте token-bucket ограничение скорости на edge для объёмных атак до WAF. Добавьте адаптивное ограничение конкурентности на origin для отклонения перегрузки без идеальной точности WAF. Многоуровневый подход снижает зависимость от идеальной настройки одного инструмента." },
  ]}
/>

<Recap lang="ru">
Межсетевые экраны и WAF работают на разных уровнях — межсетевые экраны блокируют IP/TCP-мусор, WAF блокирует HTTP-атаки (SQLi, XSS, обход пути) с аномальным скорингом OWASP CRS (набор правил ModSecurity). Уровни паранойи WAF — компромисс между частотой ложных срабатываний и покрытием атак: PL1 — 0.1% ложных срабатываний но 70% покрытие; PL4 — 95% покрытие но 5% ложных срабатываний. Начинайте в режиме обнаружения и настройте перед блокировкой. mTLS (mutual TLS — взаимная аутентификация сертификатами) обеспечивает криптографическую идентичность сервисов в микросервисах, предотвращая боковое перемещение даже при компрометации сети; SPIFFE автоматизирует выпуск и ротацию краткосрочных сертификатов. HSTS preload (список доменов в браузере, принудительно использующих HTTPS) применяет HTTPS до первого посещения, полностью устраняя SSL-стриппинг — но требует max-age в 1 год и явной регистрации. Теперь, получив в наследство WAF на PL4 и поток тикетов о заблокированных легитимных запросах, вы знаете с чего начать: переключиться в режим обнаружения, собрать две недели базовых данных и постепенно снижать уровень паранойи.
</Recap>
