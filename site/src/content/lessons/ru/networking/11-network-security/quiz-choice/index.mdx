---
concepts:
- ddos-vectors
- amplification
- rate-limiting
- waf-tuning
- mtls
- rpki-rov
estMin: 13
lang: ru
level: senior
order: 7
prereqs: []
slug: quiz-choice
sources:
- https://blog.cloudflare.com/ddos-threat-report-2025-q4/
- https://owasp.org/www-project-modsecurity-core-rule-set/
- https://isbgpsafeyet.com/
status: ready
summary: "Синтез с множественным выбором по всему юниту: векторы атак, экономика amplification, компромиссы rate limiting, тюнинг WAF, идентичность mTLS и RPKI/ROV."
title: 'Сетевая безопасность: тест с множественным выбором'
track: networking
unit: 11-network-security
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Шесть вопросов, проходящих через весь юнит. Каждый отражает решение, которое вы принимаете прямо в ходе инцидента — какой слой отказывает, какая ручка реально меняет стоимость атаки для атакующего — а не определение для зазубривания.
</Hook>

<Goal lang="ru">
Убедитесь, что вы связываете вектор атаки со слоем защиты: какой инструмент останавливает какую атаку, почему amplification и Rapid Reset обходят наивные лимиты и почему некоторые защиты (RPKI без ROV, HSTS без preload) сами по себе косметические.
</Goal>

<Quiz
  id="11-network-security-choice-1"
  lessonSlug="11-network-security"
  lang="ru"
  question="Атакующий арендует ботнет на 1 Гбит/с прямого трафика, затем переходит на memcached-отражение с коэффициентом усиления 1024x при тех же 1 Гбит/с аплинка. Что меняется и почему это сложнее остановить?"
  choices={[
    { label: "Жертва теперь видит до ~1 Тбит/с, и поток приходит от легитимных memcached-серверов с подделанным source IP жертвы — блокировка этих источников ломает легитимную инфраструктуру, поэтому нужно поглощать на anycast edge и заставлять операторов закрывать открытые серверы.", correct: true },
    { label: "Ничего не меняется — аплинк атакующего в 1 Гбит/с ограничивает атаку 1 Гбит/с независимо от усиления.", misconception: "Весь смысл amplification в том, что объём задаёт пропускная способность ответа стороннего сервера, а не атакующего. 1 Гбит/с при усилении 1024x приближается к 1 Тбит/с у жертвы." },
    { label: "Это легче остановить, потому что IP отражающих серверов известны и их можно занести в блоклист на firewall.", misconception: "Это реальные DNS/NTP/memcached серверы; их блокировка ломает легитимный трафик. Решение — поглощать объём на anycast edge и закрывать открытые сервисы, а не блокировать отражатели." },
    { label: "WAF поймает это, потому что нагрузка совпадает с известными сигнатурами атак.", misconception: "Amplification — это объёмный flood на L3/L4, насыщающий канал ещё до уровня HTTP; WAF инспектирует контент L7 и просто не успевает увидеть эти пакеты." },
  ]}
/>

<Quiz
  id="11-network-security-choice-2"
  lessonSlug="11-network-security"
  lang="ru"
  question="Публичное API использует строгий fixed window лимит 100 req/min на IP. Скрапер стабильно вытягивает ~200 запросов за несколько секунд и его ни разу не блокируют. В чём механизм и какой алгоритм чинит это дёшево?"
  choices={[
    { label: "Проблема границы fixed window: скрапер шлёт ~100 в конце одного окна и ~100 в начале следующего, так что два окна попадают в короткий промежуток. Sliding window counter (два счётчика, O(1) памяти) оценивает через границу и закрывает дыру.", correct: true },
    { label: "Лимитер протекает, потому что fixed window использует O(n) памяти и теряет счётчики под нагрузкой.", misconception: "Fixed window — это O(1), один счётчик на окно. Дефект в точности на границе, а не в памяти." },
    { label: "Token bucket — единственное решение, потому что fixed window нельзя сделать точным.", misconception: "Sliding window counter чинит границу при O(1). Token bucket тоже подходит, но он намеренно допускает всплески до ёмкости — это не 'единственное' решение." },
    { label: "Перейти на sliding window log для точного подсчёта; это правильный дефолт при любом масштабе.", misconception: "Sliding window log точен, но O(n) на клиента — при тысячах req/sec он хранит метку времени на каждый запрос и непрактичен в масштабе. Counter даёт почти точность при O(1)." },
  ]}
/>

<Quiz
  id="11-network-security-choice-3"
  lessonSlug="11-network-security"
  lang="ru"
  question="Чёрная пятница через три дня. Ваш WAF на PL2 блокирует ~70% текущей L7-атаки; подъём до PL4 даёт ~95% покрытия, но ложные срабатывания по легитимным клиентам растут до ~5%. Что сделает сеньор?"
  choices={[
    { label: "Оставить WAF умеренным (PL2-3) и добавить adaptive concurrency limiting на origin, который отбрасывает запросы только когда система уже перегружена — так ни один платящий клиент не блокируется превентивно, а атака проваливается при попытке насытить ёмкость.", correct: true },
    { label: "Перейти на PL4 и принять 5% ложных срабатываний как цену безопасности.", misconception: "5% заблокированных клиентов в Чёрную пятницу — это прямая потеря выручки и отток. Ложное срабатывание, блокирующее покупателя, здесь хуже, чем пропуск ограниченного объёма атаки." },
    { label: "Остаться на PL2 и принять, что 30% атаки проходит.", misconception: "Принять обход — значит, что деградация продолжается; вы ничего не решили. Нужен ещё один слой, а не смирение." },
    { label: "Развернуть WAF второго вендора перед первым для двойного покрытия.", misconception: "Стек из двух WAF с высокими ложными срабатываниями умножает ложные срабатывания, а не уменьшает их. Нужен слой, реагирующий на нагрузку, а не второй слой сопоставления контента." },
  ]}
/>

<Quiz
  id="11-network-security-choice-4"
  lessonSlug="11-network-security"
  lang="ru"
  question="Платформенная команда предлагает запереть трафик между сервисами IP-allowlist (только поды подсети 10.2.0.0/16 могут звать payments). Почему сеньор настаивает на mTLS?"
  choices={[
    { label: "IP-allowlist лишь доказывает, что пакет пришёл откуда-то из подсети — если любой под в ней скомпрометирован, он может выдать себя за любой сервис. mTLS доказывает, что вызывающий держит валидный короткоживущий сертификат конкретной идентичности, так что взломанный под не сможет двигаться латерально под видом другого сервиса.", correct: true },
    { label: "mTLS быстрее IP-фильтрации, потому что пропускает TCP handshake.", misconception: "mTLS добавляет стоимость handshake (десятки мс на новое соединение на старом железе); его ценность — криптографическая идентичность и шифрование, а не скорость." },
    { label: "IP-allowlist нельзя выразить в Kubernetes, поэтому mTLS — единственный вариант.", misconception: "NetworkPolicy прекрасно выражают IP/label-allowlist. Суть в том, что сетевое расположение — это не идентичность; mTLS привязывает идентичность криптографически." },
    { label: "mTLS убирает необходимость в любой сетевой политике, потому что шифрует трафик.", misconception: "Шифрование — это не авторизация; mTLS аутентифицирует идентичность, а сетевую политику вы всё равно накладываете как defense-in-depth. Они решают разные задачи." },
  ]}
/>

<Quiz
  id="11-network-security-choice-5"
  lessonSlug="11-network-security"
  lang="ru"
  question="У всех ваших префиксов опубликованы валидные RPKI ROA, но захваченный анонс от неавторизованной AS всё равно на час уводит часть вашего трафика. Как это возможно?"
  choices={[
    { label: "Публикация ROA лишь объявляет легитимный origin — она ничего не делает, пока маршрутизаторы не применяют ROV. Только ~25% сетей отбрасывают RPKI-невалидные маршруты, поэтому захват всё равно распространяется через ~75%, которые не валидируют.", correct: true },
    { label: "ROA, должно быть, истёк; валидный ROA делает захваты технически невозможными.", misconception: "Даже идеально валидный текущий ROA не остановит захват в сетях, не применяющих ROV. Публикация ROA и применение ROV — две отдельные половины." },
    { label: "Не был включён DNSSEC, поэтому захват удался на уровне DNS.", misconception: "Это BGP/маршрутный захват, а не DNS. DNSSEC защищает целостность DNS-ответов; на распространение маршрутов он не влияет." },
    { label: "BGPsec предотвратил бы это и развёрнут повсеместно, так что ROA был лишним.", misconception: "Внедрение BGPsec минимально из-за накладных расходов CPU/подписи пути — он не развёрнут повсеместно. Реалистичная защита сегодня — публикация ROA плюс широкое применение ROV." },
  ]}
/>

<Quiz
  id="11-network-security-choice-6"
  lessonSlug="11-network-security"
  lang="ru"
  question="Вы прикрыли всё CDN, rate limits и WAF. Атакующий переходит на GET-запросы с уникальным ?x=random на каждый запрос к самому дорогому запросу; cache-hit rate падает с 95% до 5%, и БД origin плавится. Какой слой реально вас спасает?"
  choices={[
    { label: "Adaptive concurrency limiting на origin: когда число запросов в полёте превышает порог ёмкости, он сбрасывает нагрузку быстрыми 503, и БД остаётся отзывчивой — он реагирует на реальную перегрузку, а не пытается вычислить per-IP rate, который ни один IP не превышает.", correct: true },
    { label: "Более строгий per-IP rate limit, потому что flood наверняка идёт с нескольких горячих IP.", misconception: "По условию это распределённый flood, где ни один IP не превышает лимит; ужесточение per-IP порогов блокирует реальных пользователей раньше, чем затрагивает атаку." },
    { label: "Anycast edge поглощение, ведь оно уже обрабатывает объёмные floods.", misconception: "Edge поглощает объём в Гбит/с, но это малообъёмный L7-трафик с обходом кэша, выглядящий легитимно и проходящий прямо на origin." },
    { label: "DNSSEC, чтобы остановить отравление кэша, обрушившее cache-hit rate.", misconception: "Падение cache-hit — это уникальные query-параметры атакующего для обхода кэша CDN, а не отравление DNS. DNSSEC не связан с поведением HTTP-кэша." },
  ]}
/>

<Recap lang="ru">
Сквозная линия — одна карта: вектор атаки к слою защиты. Amplification превращает мелкого атакующего в объёмный flood, который надо поглощать на anycast edge, а не блокировать. Выбор лимитера — компромисс: fixed window протекает на границе, sliding window counter чинит это при O(1). Паранойя WAF меняет покрытие на ложные срабатывания, поэтому умеренный WAF паруют с adaptive concurrency, реагирующим на нагрузку, а не гонятся за идеальным сопоставлением контента. mTLS даёт идентичность, которую IP-allowlist дать не может. А RPKI без ROV — как HSTS без preload — лишь половина контроля. Ни один слой не останавливает все векторы; в этом весь смысл defense in depth.
</Recap>
