---
concepts:
- defense-in-depth
- anycast-scrubbing
- adaptive-concurrency-limiting
- load-shedding
- circuit-breaker
- observability-under-attack
- attack-economics
- mTLS-service-mesh
- spiffe
- rfc-5961
deepensInto: []
estMin: 16
lang: ru
lessonType: topic
level: senior
order: 6
prereqs:
- 02-amplification-and-floods
- 03-rate-limiting-algorithms
- 04-waf-and-firewalls
- 05-dns-bgp-security
slug: 06-defense-in-depth
sources:
- https://blog.cloudflare.com/ddos-threat-report-2025-q4/
- https://www.cisa.gov/news-events/alerts/2023/10/10/http2-rapid-reset-vulnerability-cve-2023-44487
- https://datatracker.ietf.org/doc/html/rfc5961
spiral:
- statefulness
- latency
status: ready
summary: Ни одна защита не останавливает все DDoS-векторы — anycast поглощение на
  edge, ограничение скорости, WAF, mTLS и адаптивный сброс нагрузки образуют слои;
  экономика атак благоприятствует атакующему только при защите в одиночку.
title: Эшелонированная защита и экономика атак
track: networking
unit: 11-network-security
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import TraceScenario from "~/components/pedagogy/TraceScenario.astro";
import DebugLog from "~/components/pedagogy/DebugLog.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import DesignPrompt from "~/components/pedagogy/DesignPrompt.astro";
import NumbersCard from "~/components/layout/NumbersCard.astro";

<Hook>
Вы развернули CDN, ограничение скорости и WAF. Затем злоумышленник переключается на HTTP-наводнения, вызывающие промахи кэша и нацеленные на самый дорогой запрос к базе данных, распределённый по 10 000 IP, каждый ниже вашего лимита на IP. Ни одна защита не перехватывает это. Нужно понять, как взаимодействуют слои и когда эскалировать к людям.
</Hook>

<Crux>Ни одна защита не останавливает все векторы. Стекуйте anycast, лимиты, WAF, mTLS, адаптивную конкурентность и CDN.</Crux>

<Explanation>

**Архитектура эшелонированной защиты: полный стек.** Ни одна защита не останавливает все атаки. Многоуровневый подход:

1. **Anycast-скруббинг на edge** — каждый PoP CDN является активным центром очистки. Атаки поглощаются на ближайшем PoP, а не концентрируются на origin. Суммарная ёмкость 330+ PoP Cloudflare превышает 37 Тбит/с. Атака 10 Гбит/с становится незначительной, распределённой по многим узлам.
2. **Stateless L3/L4 ограничения скорости** — лимиты на ASN, на префикс отбрасывают очевидные источники усиления и SYN-наводнения до создания TCP-состояния.
3. **WAF на edge** — обнаруживает паттерны прикладного уровня (SQLi, XSS, отпечатки ботов). Работает на PL2 (баланс ложных срабатываний/покрытия), не PL4 (параноидальный) для публичного API.
4. **Token bucket на IP + на пользователя** — останавливает очевидные ботнеты и злоупотребления аутентифицированными пользователями.
5. **Адаптивное ограничение конкурентности на origin** — когда количество in-flight запросов превышает пороговое значение, отклонять новые запросы с быстрым 503. Сервис остаётся стабильным; пользователи получают ошибки вместо таймаутов.
6. **Наблюдаемость и эскалация к людям** — когда автоматические защиты не справляются, дежурные инженеры добавляют пользовательские правила.

<div data-lesson-visual class="overflow-x-auto my-4">
  <table class="text-sm w-full border-collapse">
    <thead>
      <tr class="bg-gray-100">
        <th class="border p-2 text-left">Слой</th>
        <th class="border p-2 text-left">Что останавливает</th>
        <th class="border p-2 text-left">Что пропускает</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border p-2">Anycast edge</td>
        <td class="border p-2">Объёмные наводнения (масштаб Гбит/с)</td>
        <td class="border p-2">Умные низкоскоростные атаки на IP</td>
      </tr>
      <tr>
        <td class="border p-2">Stateless L3/L4 лимиты</td>
        <td class="border p-2">Усиление, SYN-наводнения</td>
        <td class="border p-2">HTTP-атаки на валидные порты</td>
      </tr>
      <tr>
        <td class="border p-2">WAF (PL2)</td>
        <td class="border p-2">Известные сигнатуры атак, паттерны ботов</td>
        <td class="border p-2">Zero-day, обфусцированные пэйлоады, злоупотребления бизнес-логикой</td>
      </tr>
      <tr>
        <td class="border p-2">Лимит (на IP/пользователя)</td>
        <td class="border p-2">Очевидные ботнеты, злоупотребление авторизацией</td>
        <td class="border p-2">Распределённые ботнеты с резидентными прокси</td>
      </tr>
      <tr>
        <td class="border p-2">Адаптивная конкурентность</td>
        <td class="border p-2">Распределённая перегрузка, наводнения на промахи кэша</td>
        <td class="border p-2">Атаки ниже порога перегрузки</td>
      </tr>
      <tr>
        <td class="border p-2">mTLS</td>
        <td class="border p-2">Боковое перемещение внутри сети</td>
        <td class="border p-2">Внешние векторы атак</td>
      </tr>
    </tbody>
  </table>
</div>

**mTLS в service mesh со SPIFFE.** Istio или Linkerd развёртывают sidecar-прокси на каждом поде. Control plane (Istiod) запускает SPIFFE-совместимый центр сертификации. При запуске каждый sidecar получает краткосрочный сертификат (1–24 часа). Сертификаты ротируются через SDS (Service Workload API) push — перезапуск sidecar не нужен. Каждый вызов сервис-к-сервису: (1) mTLS-хендшейк (накладные расходы 20–50 мс на новое соединение на старом оборудовании), (2) зашифрованный пэйлоад, (3) обе стороны проверяют сертификаты. Предотвращает боковое перемещение при компрометации pod-сети. Стоимость: ротация сертификатов добавляет операционную сложность и нагрузку мониторинга (истёкший сертификат = инцидент инфраструктуры, а не баг).

**Истощение протокола/состояния в глубину.** SYN-наводнения: каждый SYN выделяет слот полуоткрытого соединения в очереди backlog сервера. При переполнении backlog сервер сбрасывает новые SYN-пакеты легитимных клиентов. SYN cookies кодируют состояние соединения как криптографический cookie в ISN — память не выделяется; легитимные клиенты отвечают валидным ACK, декодирующим cookie. ACK-наводнения: ограничение скорости RST (лимит RST в секунду) и подавление несопоставленных ACK брандмауэром. Инъекция TCP RST (MITM на пути): RFC 5961 challenge-ACK вынуждает злоумышленника знать точный номер последовательности, а не просто быть в окне.

**Внутренности ограничения скорости: сложность распределённых систем.** Token bucket с Redis: `T = min(C, T + R * delta_time)`. Распределённый: каждый запрос атомарно `INCR key; EXPIRE key window`. При 100k req/sec, Redis добавляет 0.5–1 мс на запрос = 50–100 мс суммарной задержки. Оптимизация: локальный счётчик на сервер + периодическая синхронизация с Redis (допускает небольшую погрешность, сокращает до микросекундных решений). HyperLogLog для приближённого ограничения скорости: ~1.6 кБ на эскиз, ~2% погрешность, подходит для лимитов на уровне ASN или IP-диапазона.

**Адаптивная конкурентность: формула сброса нагрузки.** Отслеживайте in-flight запросы `Q`. Установите порог max_queue. Для новых запросов вероятность принятия = `max(0, 1 - Q / max_queue)`. Принимайте запрос с этой вероятностью. Это создаёт плавную деградацию: при 50% перегрузке отклоняется 50% новых запросов; при 100% — все. Пользователи получают быстрые 503 вместо таймаутов очереди (которые могут быть 30+ секунд). Автоматические выключатели отклоняют запросы к бэкендам с недавним превышением порога частоты отказов — предотвращая каскадные сбои по всему графу сервисов.

<TraceScenario
  id="sr-trace-1"
  lessonSlug="06-defense-in-depth"
  lang="ru"
  scenario="Проследите изощрённую атаку с использованием усиления и тактик прикладного уровня."
  steps={[
    { prompt: "Шаг 1: злоумышленник начинает атаку усиления memcached 10 Гбит/с. Что происходит без CDN?", reveal: "Трафик попадает в канал origin; запросы истекают; сервис недоступен. Без CDN вы платите за полосу и не имеете защиты от объёма." },
    { prompt: "Шаг 2: вы переходите на CDN (Cloudflare). 10 Гбит/с поглощается на edge. Какова стоимость атаки для злоумышленника?", reveal: "Обнаружение усиления CDN (аномалия соотношения ответ/запрос) отбрасывает memcached-трафик. Злоумышленнику нужен легитимный HTTP-трафик для обхода этого слоя." },
    { prompt: "Шаг 3: злоумышленник переходит на HTTP-наводнения — легитимно выглядящие GET-запросы, 100 000 req/sec с ботнета. Что делает ограничитель скорости?", reveal: "Ограничитель скорости на edge CDN отклоняет IP, делающие >10 000 req/sec. Каждый IP ботнета троттлируется, но при 1 000 IP ботнета по 100 req/sec каждый, атака всё равно достигает цели. Легитимные пользователи в среднем делают 10 req/s; 100 req/s — пограничный случай." },
    { prompt: "Шаг 4: злоумышленник добавляет ?x=random к каждому запросу (обход кэша), нацеливаясь на дорогие эндпоинты БД. Cache-hit rate падает с 95% до 5%. В чём новая проблема?", reveal: "Origin перегружен запросами-промахами кэша, бьющими в БД. Ограничение скорости на IP недостаточно — ни один IP не делает 100 000 req/sec. Адаптивное ограничение конкурентности отклоняет при in-flight > порога; WAF обнаруживает паттерн ?x=random." },
    { prompt: "Шаг 5: злоумышленник адаптирует случайный параметр с реалистичными User-Agent и распределяет дальше. Сервис всё ещё медленный. Каков операционный ответ?", reveal: "Эскалируйте к дежурному инженеру. Это больше не бот-атака, с которой может справиться автоматизация. Настройка оператора: повысить уровень паранойи WAF, добавить пользовательское правило на отпечаток атаки, или принять деградацию пока злоумышленник не потеряет интерес (атаки дорого поддерживать)." },
  ]}
/>

<DebugLog
  id="sr-debug-1"
  lessonSlug="06-defense-in-depth"
  lang="ru"
  title="Аномальное скорирование WAF во время атаки"
  output={`2026-05-15 14:23:00 | requests=45000/sec | score-p50=0.5 | score-p95=3.2 | score-p99=8.1
2026-05-15 14:24:00 | requests=120000/sec | score-p50=6.1 | score-p95=14.2 | score-p99=28.5
2026-05-15 14:25:00 | requests=450000/sec | score-p50=12.3 | score-p95=19.8 | score-p99=32.1
2026-05-15 14:26:00 | blocked=380000 | allowed=70000 | score-threshold=5`}
  outputLang="log"
  question="WAF скорирует трафик и блокирует при threshold=5. Что происходит в этой хронологии и что должен делать оператор?"
  hint="Посмотрите на рост скорости запросов (45k → 120k → 450k) и распределение аномальных оценок (p99 растёт с 8 до 28+). Что означает решение о блокировке в 14:26?"
  answer="Это атака прикладного уровня. Запросы вырастают в 10 раз за 3 минуты; аномальные оценки скачут (p99=28.5 на пике). WAF блокирует при threshold=5, пропуская 70k/sec — порог слишком мягкий при p99=28. Оператор должен: (1) снизить порог до 8–10; (2) включить PL2–3 для более строгих правил; (3) если слишком много ложных срабатываний, сосредоточиться вместо этого на ограничении скорости; (4) идентифицировать паттерн атаки (идентичные заголовки? необычные User-Agent? отсутствующие Referer?) и добавить правило сигнатуры нацеленное на него."
/>

**Экономика атак.** Стоимость атакующего: ~$50–500/мес за сервис ботнета, способный на 10 Гбит/с устойчиво. Стоимость защиты origin: ~$500/мес счёт CDN за 10 Гбит/с устойчивого трафика. Если злоумышленник генерирует 100 Гбит/с, origin не может соответствовать. Но CDN с глобальными PoP поглощает сотни Тбит/с и распределяет стоимость среди миллионов клиентов — стоимость на клиента ничтожна. Экономика благоприятствует атакующему только при защите в одиночку. Общая инфраструктура (CDN) — ответ: нельзя превзойти ботнет в масштабах; можно сделать атаку экономически невыгодной, заставив её провалиться.

**Наблюдаемость во время атаки.** Ключевые сигналы: (1) скорость запросов в секунду — 10-кратный рост от нормы подозрителен; (2) географическое распределение источников — всё с одной ASN подозрительно; (3) аномальная оценка p99 — нормальные пользователи набирают &lt;1, атакующий трафик &gt;10; (4) cache-hit rate — атакующий трафик на уникальные параметры показывает резкий спад. Пороги оповещения: скорость запросов 10x базовой, спад аномальных оценок p99, снижение энтропии IP-источников (100 IP вместо 100 000), или cache-hit rate ниже 80% во время всплеска трафика. Эскалация к людям, если атака длится >5 минут или превышает 50 Гбит/с.

<TradeoffMatrix
  id="sr-tradeoff-1"
  lessonSlug="06-defense-in-depth"
  lang="ru"
  prompt="Сервис e-commerce под устойчивой атакой прикладного уровня. Выберите стратегию защиты."
  options={[
    { name: "WAF на PL4 (уровень паранойи 4)", summary: "Строгая инспекция, много ложных срабатываний.", justification: "Блокирует 95% атак, но и 5% легитимных клиентов. Через 3 дня Чёрная пятница — нельзя позволить заблокировать 5% продаж." },
    { name: "Ограничение скорости (10 req/sec на IP) + token bucket", summary: "Дёшево, мало ложных срабатываний.", justification: "Блокирует только очевидные ботнеты. Умные злоумышленники с резидентными прокси (10 разных IP на атакующего) обходят это — каждый IP остаётся ниже 10 req/sec." },
    { name: "Адаптивное ограничение конкурентности + сброс нагрузки", summary: "Отклонять при in-flight > 5 000 запросов.", correct: true, justification: "Когда origin перегружен (очередь накапливается), отклонять новые запросы с быстрым 503. Легитимные пользователи проходят; атака проваливается из-за каскада отклонений. Ложные срабатывания только когда сервис уже сломан — что плохо в любом случае." },
    { name: "Увеличить ёмкость origin в 10 раз", summary: "Масштабировать вертикально или горизонтально.", justification: "Грубый подход. Работает если атака меньше новой ёмкости, но при масштабировании атаки в соответствие, только откладывает проблему и тратит деньги." },
  ]}
/>

<DesignPrompt
  id="sr-design-1"
  lessonSlug="06-defense-in-depth"
  lang="ru"
  prompt="Разработайте архитектуру DDoS-защиты для видео-CDN ёмкостью 100 Гбит/с, обслуживающего глобальных пользователей. CDN работает в 50 PoP в 30 странах."
  constraints={[
    "Поглощать атаки 100+ Гбит/с на edge без превышения 10% ёмкости любого PoP.",
    "Защищаться от объёмных (L3/L4), протокольных (SYN-наводнения) и прикладного уровня (HTTP-наводнения) атак.",
    "Поддерживать p50 задержку &lt; 50 мс и p99 &lt; 200 мс для легитимных пользователей во время атаки.",
    "Обнаруживать и блокировать новые паттерны атак в течение 60 секунд.",
  ]}
  canonical="Anycast поглощение на edge: все 50 PoP активные центры очистки. Атакующий трафик поглощается на ближайшем PoP — не перенаправляется в централизованный центр. Каждый PoP может поглотить ~2 Гбит/с без превышения 10% ёмкости; суммарно = 100 Гбит/с. Объёмные атаки: stateless L3/L4 лимиты скорости на ASN, на префикс. Отбрасывать ответы усиления с подозрительными соотношениями ответ/запрос. Протокольные: обнаружение SYN-наводнений (если скорость SYN от одной ASN > 100k/sec, ограничить скорость); ограничение RST (если скорость RST > 10k/sec от одного IP, ограничить). Прикладной уровень: token bucket 10 000 req/sec на IP глобально, 1 000 на PoP локально. WAF аномальное скорирование на PL2. Отслеживание cache-hit rate: при падении ниже 80% во время всплеска, включить адаптивное ограничение. Обнаружение: агрегация данных потоков на PoP (NetFlow экспорт в центральную аналитику). Оповещение при скорости запросов >10x базовой, всплеске аномальных оценок p99, снижении энтропии IP источников, или географической аномалии. Ответ: автоматическая эскалация ограничения скорости каждые 30 секунд; ручная эскалация к дежурному при атаке >5 минут или превышении 50 Гбит/с на любом PoP."
  keyPoints={[
    "Anycast поглощение распределяет нагрузку атаки; нет единой точки отказа.",
    "Stateless L3/L4 фильтрация перехватывает 80% объёмных атак с минимальной нагрузкой CPU.",
    "WAF на PL2 балансирует обнаружение vs ложные срабатывания для публичного API.",
    "Адаптивное ограничение отклоняет при перегрузке, не по превентивной блокировке IP.",
    "Эскалация к людям через 5 минут перехватывает новые паттерны атак.",
    "Постоянно измерять частоту ложных срабатываний и нагрузку origin.",
  ]}
/>

<Inset kind="why" lang="ru">
Почему адаптивное ограничение конкурентности предпочтительнее WAF PL4 для высоконагруженного production сервиса под атакой? WAF PL4 принимает превентивные решения на уровне запроса на основе паттернов содержимого, вызывая 5% ложных срабатываний — 1 из 20 легитимных пользователей заблокирован. Адаптивное ограничение конкурентности реагирует на реальную нагрузку системы, отклоняя запросы только когда сервер уже перегружен. Ни один легитимный пользователь не блокируется превентивно; стоимость — что часть атакующего трафика проходит до достижения перегрузки, но быстрые 503 защищают origin от каскадного сбоя. Для публичного сервиса ложные срабатывания (блокировка реальных пользователей) хуже ложноотрицательных (пропуск части атакующего трафика) когда атака ограничена лимитами конкурентности.
</Inset>

<Quiz
  id="sr-quiz-1"
  lessonSlug="06-defense-in-depth"
  lang="ru"
  question="Ваш WAF на уровне паранойи 2 и атаки проходят (заблокировано только 70%). Вы повышаете до PL4. Легитимные клиенты начинают жаловаться (5% ложных срабатываний). Что лучше?"
  choices={[
    { label: "Принять 5% ложных срабатываний как плату за безопасность", misconception: "Ложные срабатывания имеют скрытые издержки: отток клиентов, тикеты в поддержку, потери дохода. Неприемлемо для production сервиса." },
    { label: "Оставить PL2 и принять 30% пропуск атак", misconception: "Принятие пропуска означает продолжение деградации сервиса и ничего не решает." },
    { label: "Комбинировать PL3 с ограничением скорости и адаптивным сбросом нагрузки", correct: true },
    { label: "Развернуть второй WAF от другого вендора рядом с текущим", misconception: "Два WAF с высоким числом ложных срабатываний усугубляют проблему." },
  ]}
/>

</Explanation>

<KeyTakeaway>Эшелонированная защита объединяет anycast edge, stateless L3/L4 фильтры, WAF на PL2, лимиты на IP/пользователя и адаптивное ограничение конкурентности на origin. Ни один слой не останавливает все атаки. Экономика атак благоприятствует защитникам при использовании общей CDN-инфраструктуры — стартап не может самостоятельно поглотить 100 Гбит/с; CDN амортизирует это по миллионам клиентов.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="06-defense-in-depth-retrieval"
  lang="ru"
  questions={[
    { q: "Почему адаптивное ограничение конкурентности предпочтительнее WAF PL4 для высоконагруженного production сервиса под атакой?", a: "WAF PL4 принимает превентивные решения на основе сопоставления паттернов содержимого, вызывая 5% ложных срабатываний — 1 из 20 легитимных пользователей блокирован. Адаптивное ограничение конкурентности реагирует на реальную нагрузку системы, отклоняя запросы только при перегрузке сервера. Ни один легитимный пользователь не блокируется превентивно; стоимость — что часть атакующего трафика проходит до перегрузки, но быстрые 503 защищают origin от каскадного сбоя. Для публичного сервиса ложные срабатывания хуже ложноотрицательных при атаке, ограниченной лимитами конкурентности." },
    { q: "Во время атаки Rapid Reset (CVE-2023-44487) почему атака обходит ограничение скорости только для HTTP/1.1?", a: "Ограничение скорости HTTP/1.1 обычно считает завершённые запросы (один запрос = один ответ). HTTP/2 Rapid Reset отправляет поток и сбрасывает его до отправки ответа. Сервер всё равно выделяет CPU/память для потока. Ограничитель, считающий только ответы, никогда не видит отменённые потоки. Решение: ограничить скорость по скорости создания потоков (учитываются и завершённые, и сброшенные) или по счётчику in-flight запросов. Google, nghttp2 и OpenSSL выпустили патчи в октябре 2023 с добавлением ограничения скорости сброса потоков." },
    { q: "Какие метрики должен мониторить дежурный инженер во время DDoS-атаки и какие пороги сигнализируют об эскалации?", a: "Ключевые метрики: (1) скорость запросов в секунду — 10x от нормы подозрительно; (2) аномальная оценка p99 — должна быть <1 для нормальных пользователей, >10 для атакующего трафика; (3) энтропия IP-источников — весь трафик с 100 IP вместо 100 000 говорит о сконцентрированном ботнете; (4) cache-hit rate — резкий спад во время всплеска трафика указывает на атаку обхода кэша; (5) глубина очереди origin — растущая очередь означает что атака достигает origin. Пороги эскалации: скорость запросов 10x базовой, всплеск аномальных оценок p99, снижение энтропии IP источников, cache-hit rate ниже 80%, или атака длится >5 минут." },
  ]}
/>

<Recap lang="ru">
Эшелонированная защита от DDoS требует нескольких слоёв, потому что ни одна защита не останавливает все векторы. Anycast поглощение на edge распределяет объёмные атаки по 330+ глобальным PoP; stateless L3/L4 фильтры отбрасывают усиление и SYN-наводнения до потребления состояния соединения; WAF на PL2 обнаруживает известные прикладные паттерны с допустимыми ложными срабатываниями; адаптивное ограничение конкурентности на origin отклоняет при перегрузке, а не превентивной блокировке IP. Экономика атак благоприятствует защитникам только при использовании общей CDN-инфраструктуры — ботнет, генерирующий 100 Гбит/с за $500/мес, побеждается CDN, который амортизирует ёмкость защиты по миллионам клиентов. Когда автоматические защиты не справляются, наблюдаемость (скорость запросов, аномальные оценки, cache-hit rate, энтропия IP источников) даёт дежурным инженерам сигнал для добавления пользовательских правил в 60-секундном окне эскалации.
</Recap>
