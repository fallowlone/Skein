---
concepts:
- syn-cookies
- token-bucket-redis
- http2-rapid-reset
- waf-anomaly-scoring
estMin: 14
lang: ru
level: senior
order: 9
prereqs: []
slug: quiz-code
sources:
- https://datatracker.ietf.org/doc/html/rfc4987
- https://www.cisa.gov/news-events/alerts/2023/10/10/http2-rapid-reset-vulnerability-cve-2023-44487
- https://owasp.org/www-project-modsecurity-core-rule-set/
status: ready
summary: "Читайте реальный конфиг, сниппет распределённого rate-лимитера, настройку HTTP/2-сервера и строку лога WAF — предскажите поведение под атакой и выберите фикс с наибольшим рычагом."
title: 'Сетевая безопасность: чтение конфигов и логов'
track: networking
unit: 11-network-security
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Инциденты безопасности диагностируются в конфигах, коде лимитеров и строках логов — а не в слайдах. Читайте каждый артефакт, предсказывайте его поведение под атакой и выбирайте фикс, который сеньор сделает первым.
</Hook>

<Goal lang="ru">
Отработайте цикл, который вы проходите в каждом инциденте: прочитать артефакт, найти разрыв между тем, что он делает, и тем, что предполагал оператор, и взяться за изменение с наибольшим рычагом.
</Goal>

## Сниппет 1 — sysctl против SYN-flood

```ini
# /etc/sysctl.d/hardening.conf
net.ipv4.tcp_max_syn_backlog = 4096
net.core.somaxconn          = 4096
net.ipv4.tcp_synack_retries = 5
net.ipv4.tcp_syncookies     = 0   # отключено
```

<Quiz
  id="11-network-security-code-1"
  lessonSlug="11-network-security"
  lang="ru"
  question="Инженер «захардил» хост, увеличив SYN backlog до 4096, и оставил tcp_syncookies=0. Что происходит под спуфнутым SYN-flood и какой фикс имеет наибольший рычаг?"
  choices={[
    { label: "Больший backlog даёт лишь несколько миллисекунд — flood заполняет 4096 полуоткрытых слотов так же быстро, и с выключенными syncookies ядро отбрасывает легитимные SYN при заполнении. Поставьте tcp_syncookies=1, чтобы ядро кодировало состояние соединения в ISN и не выделяло слот при переполнении.", correct: true },
    { label: "4096 слотов более чем достаточно; хост полностью защищён и менять ничего не нужно.", misconception: "Размер backlog — линейный запас; flood в миллионы SYN/sec насыщает любой фиксированный backlog за миллисекунды. Структурный фикс — SYN cookies, а не больший стол." },
    { label: "Поднять tcp_synack_retries выше, чтобы полуоткрытые соединения держались дольше и в итоге завершились.", misconception: "Больше ретраев держит спуфнутые полуоткрытые записи дольше, усугубляя истощение. Их надо убирать быстрее, а cookies убирают слот совсем." },
    { label: "Отключение syncookies верно, потому что cookies ломают легитимных клиентов за NAT.", misconception: "SYN cookies — это резерв, включающийся только при переполнении backlog, и совместимый с обычными клиентами; реальная цена — небольшое вычисление хэша на ACK, а не сломанные клиенты за NAT." },
  ]}
/>

## Сниппет 2 — распределённый token bucket

```lua
-- лимитер на запрос, вызывается на каждом хите API
local count = redis.call("INCR", key)
if count == 1 then
  redis.call("EXPIRE", key, window_seconds)
end
if count > limit then
  return DENY
end
return ALLOW
```

<Quiz
  id="11-network-security-code-2"
  lessonSlug="11-network-security"
  lang="ru"
  question="Этот лимитер на Redis вызывается на каждом запросе по флоту app-серверов. Под атакой 100k req/sec в чём операционная проблема и какова стандартная митигация?"
  choices={[
    { label: "Каждое решение — синхронный round-trip к Redis (~0.5–1 мс), так что при 100k req/sec лимитер добавляет латентность и делает Redis горячей единой зависимостью. Митигация: локальный счётчик на сервер для быстрого пути с периодической синхронизацией в Redis, меняя лёгкую неточность на решения за микросекунды.", correct: true },
    { label: "INCR не атомарен, поэтому конкурентные запросы портят счётчик и лимит теряет смысл.", misconception: "INCR в Redis атомарен — именно поэтому его здесь используют. Проблема в латентности сети на запрос и в том, что Redis становится узким местом, а не в атомарности." },
    { label: "EXPIRE срабатывает только при count==1, так что ключ никогда не истекает и память течёт вечно.", misconception: "Установка EXPIRE при первом инкременте — корректная идиома fixed window; TTL окна задаётся один раз на окно. Утечки нет." },
    { label: "Всё нормально; счётчики на Redis бесплатны и всегда верный дефолт.", misconception: "Они корректны, но не бесплатны — каждое решение стоит round-trip. При высоких req/sec митигируют локальными счётчиками плюс периодическая глобальная синхронизация." },
  ]}
/>

## Сниппет 3 — конфиг HTTP/2-сервера

```nginx
http2_max_concurrent_streams 128;
# нет лимита на rate создания / сброса стримов
keepalive_requests 100000;
```

<Quiz
  id="11-network-security-code-3"
  lessonSlug="11-network-security"
  lang="ru"
  question="Этот конфиг ограничивает конкурентные стримы 128, но никогда не лимитирует rate создания стримов. Почему он остаётся уязвим к HTTP/2 Rapid Reset (CVE-2023-44487)?"
  choices={[
    { label: "Rapid Reset открывает стрим и сразу шлёт RST_STREAM до любого ответа, так что он никогда не держит 128 конкурентных стримов — он гоняет create-then-cancel тысячи раз в секунду, а сервер всё равно выделяет CPU/память на каждый стрим. Фикс — лимитировать rate создания стримов (считая и сброшенные), а не конкурентность.", correct: true },
    { label: "Лимит конкурентности 128 уже останавливает Rapid Reset, потому что атакующий не может превысить 128 стримов.", misconception: "Атака намеренно держится ниже лимита конкурентности, мгновенно отменяя каждый стрим; цена — в churn create/reset, который сервер всё равно платит и который лимит не видит." },
    { label: "keepalive_requests на 100000 — это уязвимость; его понижение останавливает Rapid Reset.", misconception: "Счётчик keepalive соединения не связан; Rapid Reset злоупотребляет жизненным циклом стримов внутри соединения. Фикс — ограничение rate создания/сброса стримов." },
    { label: "Rapid Reset — объёмная атака L3/L4, поэтому настройка HTTP-сервера на неё не влияет.", misconception: "Rapid Reset — атака истощения ресурсов прикладного уровня (L7) на машину состояний стримов HTTP/2; серверное ограничение rate стримов — именно та митигация, что вендоры выпустили в 2023." },
  ]}
/>

## Сниппет 4 — лог anomaly-score WAF

```
14:23 | req=45000/s  | score-p50=0.5 | score-p99=8.1  | threshold=5
14:24 | req=120000/s | score-p50=6.1 | score-p99=28.5 | threshold=5
14:25 | req=450000/s | score-p50=12.3| score-p99=32.1 | threshold=5
14:26 | blocked=380000 allowed=70000 | threshold=5
```

<Quiz
  id="11-network-security-code-4"
  lessonSlug="11-network-security"
  lang="ru"
  question="Читая эту хронологию anomaly-scoring WAF (блок при score &ge; threshold=5), какое прочтение верно?"
  choices={[
    { label: "Это L7-атака: rate запросов растёт в 10 раз за три минуты, и даже медианный anomaly-score пересекает порог к 14:24. 70k req/sec всё ещё проходят, потому что порог мягкий относительно p99=32 — ужесточите порог и спаруйте его с rate limiting, а не полагайтесь только на WAF.", correct: true },
    { label: "Трафик здоров, потому что p50 score держится в единицах.", misconception: "К 14:24 p50 равен 6.1 — выше порога 5 — а p99 равен 28.5. Всё распределение сдвинулось в зону атаки; это не здоровый трафик." },
    { label: "WAF сломан, потому что заблокировал 380k запросов; столько он блокировать не должен.", misconception: "Блокировка 380k из flood 450k/sec — это работа WAF. Беспокоят 70k, что всё ещё проходят при таком высоком p99 — это зазор тюнинга, а не поломка." },
    { label: "threshold=5 означает, что блокируется 5% запросов.", misconception: "Порог — это отсечка anomaly-score (блок при сумме score правил &ge; 5), а не процент трафика." },
  ]}
/>

<Recap lang="ru">
Любая защита читается в конфигах и логах. Больший SYN backlog — линейный запас; SYN cookies — структурный фикс. Лимитер на Redis корректен, но каждое решение — round-trip; митигируйте локальным быстрым путём плюс периодическая синхронизация. Лимиты конкурентности не останавливают Rapid Reset; нужно ограничивать rate создания стримов, включая сбросы. А лог anomaly WAF говорит сразу, сдвинулось ли распределение score в зону атаки и пропускает ли ваш порог трафик. Прочитайте артефакт, найдите разрыв между замыслом и поведением, почините самый рычажный первым.
</Recap>
