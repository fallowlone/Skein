---
concepts:
- power-of-two-choices
- health-checks-draining
- retry-storm-circuit-breaker
- session-externalization
- lb-observability
estMin: 240
lang: ru
level: senior
order: 10
prereqs: []
slug: project
sources:
- https://www.envoyproxy.io/docs/envoy/latest/intro/arch_overview/upstream/load_balancing/load_balancers.html
- https://www.envoyproxy.io/docs/envoy/latest/intro/arch_overview/upstream/circuit_breaking
- https://learn.microsoft.com/en-us/azure/architecture/antipatterns/retry-storm/
status: ready
summary: "Практический проект — поставьте перед пулом backends реальный load balancer, загоните его в retry storm, затем примените лестницу отказоустойчивости юнита, пока кластер не переживёт падающий узел, доказав это метриками до/после."
title: 'Proxy и load balancing: пережить падающий backend'
track: networking
unit: 09-proxy-load-balancing
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import ProjectBrief from "~/components/pedagogy/ProjectBrief.astro";

<Hook>
Читать про retry storm — не то же самое, что удержать кластер живым, пока один узел заваливается. Поднимите небольшой пул backends за реальным proxy, внесите сбой под нагрузкой, посмотрите, как блип на 500 мс пытается стать отказом, и применяйте меры отказоустойчивости юнита, пока кластер не переживёт его — с доказательствами на каждом шаге.
</Hook>

<Goal lang="ru">
Превратите ментальную модель юнита в воспроизводимый цикл: балансируйте load-aware алгоритмом, обнаруживайте сбой active и passive health check, дренируйте аккуратно, ограничивайте каскад circuit breaker и retry budget, вынесите состояние сессии наружу и проверяйте каждую меру метриками до/после под одинаковой нагрузкой.
</Goal>

<ProjectBrief
  id="09-proxy-load-balancing-project"
  lessonSlug="09-proxy-load-balancing"
  lang="ru"
  objective="Поставьте перед пулом из 3–5 backend-инстансов реальный load balancer (Envoy, nginx или HAProxy), внесите отказ одного узла под нагрузкой и докажите, что кластер остаётся доступным — удерживая error rate ниже 0.1% и p99 в пределах цели — применяя лестницу отказоустойчивости юнита с измерениями на каждом шаге."
  requirements={[
    "Поднимите 3–5 идентичных backend-инстансов (любой язык) за одним load balancer. Выставьте эндпоинт с переменной стоимостью — смесь быстрых чтений и намеренно медленного обработчика (например, sleep или тяжёлый запрос), чтобы стоимость запросов была неоднородной.",
    "Настройте балансировку load-aware алгоритмом (Envoy least_request / power-of-two-choices или nginx least_conn), а не default round-robin. Отметьте, какую директиву использовали и почему.",
    "Подключите наблюдаемость LB: per-backend request rate, error rate, латентность p50/p95/p99, активные соединения, успех/провал health check, retry rate и события открытия/закрытия circuit breaker — дашборд или эквивалент со stats-эндпоинта proxy.",
    "Настройте active health check (HTTP GET на /healthz, который задействует реальную зависимость, каждые 10–30 с, выброс после 2–3 сбоев) И passive outlier detection (выброс после подряд идущих 5xx). Покажите в метриках, что каждый ловит отказ, который пропускает другой.",
    "Внесите сбой: заставьте один backend возвращать 500 на каждый запрос, продолжая принимать TCP (например, уронив его зависимость). Подайте нагрузку генератором и снимите baseline — error rate, p99 и retry rate, взлетающий выше 0.1%.",
    "Примените меры против каскада: circuit breaker / лимит соединений-и-pending-запросов, чтобы перегруженный backend получал быстрые 503; ограниченную политику retry с budget (~10% от RPS) только на самом внешнем слое; и exponential backoff с jitter на клиенте.",
    "Добавьте connection draining: при остановке backend перестать принимать новые соединения, дослужить in-flight запросы в пределах drain timeout, рассчитанного на самое долгое соединение, затем выйти — и покажите отсутствие видимых клиенту reset во время rolling restart.",
    "Вынесите состояние сессии в общее хранилище (Redis), чтобы LB маршрутизировал свободно без sticky session, и продемонстрируйте, что запрос обслужен корректно после того, как его первый backend убит в середине сессии.",
  ]}
  acceptance={[
    "Таблица до/после под одинаковой нагрузкой: error rate, латентность p99, retry rate и время обнаружения упавшего узла — измеренные, не оценочные — показывающая error rate снова ниже 0.1% и retry rate снова ниже 0.1% после применения мер.",
    "Доказательство, что active и passive health check каждый обнаружил свой режим отказа (жёсткий краш против узла, принимающего TCP, но отдающего 500).",
    "Rolling restart с connection draining даёт ноль видимых клиенту reset соединений в access-логах, тогда как резкое убийство (draining отключён) их даёт — оба захвачены.",
    "Краткий разбор, называющий в порядке приоритета, какая мера остановила каскад и почему circuit breaker и retry budget стоят выше простого добавления ёмкости.",
  ]}
  stretch={[
    "Добавьте второй load balancer и поставьте пару за общий VIP (keepalived/VRRP локально или anycast в лабе), чтобы убийство одного LB не роняло весь трафик; измерьте время failover.",
    "Разделите proxy на L4-уровень перед L7-уровнем и маршрутизируйте /api/ против /static/ в разные пулы на L7; сохраните реальный IP клиента сквозь через X-Forwarded-For или PROXY protocol и проверьте, что backend логирует настоящего клиента.",
    "Добавьте slow start: когда восстановившийся backend возвращается в пул, наращивайте его трафик 10% → 100% за несколько минут и покажите, что его p99 остаётся ограниченным против немедленного возврата под полную нагрузку.",
    "Соберите одностраничный on-call runbook: триаж по дашборду, сигнатура retry storm, лестница отказоустойчивости (алгоритм → health → drain → breaker → budget → вынос состояния) и чек-лист верификации.",
  ]}
/>

<Recap lang="ru">
Это цикл, который вы запустите на каждом реальном инциденте с LB: балансировать load-aware алгоритмом, обнаруживать сбой И active, И passive проверками, дренировать in-flight запросы перед удалением узла, ограничивать каскад circuit breaker и retry budget прежде чем хвататься за ёмкость, выносить состояние сессии наружу, чтобы любой backend мог продолжить любой запрос, и проверять каждую меру числами до/после под одинаковой нагрузкой. Сделав это один раз на игрушечном пуле, вы превращаете продакшен-версию в мышечную память.
</Recap>
