---
concepts:
- active-health-check
- passive-health-check
- outlier-detection
- connection-draining
- slow-start
- health-check-flapping
deepensInto:
- 06-retry-storms-and-circuit-breakers
estMin: 12
lang: ru
lessonType: topic
level: middle
order: 4
prereqs:
- 09-proxy-load-balancing/01-what-reverse-proxies-do
- 09-proxy-load-balancing/02-balancing-algorithms
slug: 04-health-checks-and-draining
sources:
- https://www.envoyproxy.io/docs/envoy/latest/intro/arch_overview/upstream/health_checking
- https://www.haproxy.com/blog/layer-4-vs-layer-7-load-balancing/
- https://docs.aws.amazon.com/elasticloadbalancing/latest/application/load-balancer-target-groups.html
spiral:
- latency
- statefulness
status: ready
summary: Активные пробы обнаруживают полные отказы; пассивное обнаружение выбросов
  улавливает тихую деградацию с 5xx в реальном времени; connection draining позволяет
  завершить запросы в полёте до удаления backend; slow start защищает холодный backend
  от немедленной полной нагрузки.
title: Health checks, connection draining и slow start
track: networking
unit: 09-proxy-load-balancing
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import TraceScenario from "~/components/pedagogy/TraceScenario.astro";
import NumbersCard from "~/components/layout/NumbersCard.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
У backend заполняется диск. Он продолжает принимать TCP-соединения, но каждый HTTP-запрос возвращает 500. Health check L4 (TCP SYN-проба) говорит, что backend здоров. Запросы продолжают маршрутизироваться к нему и завершаться ошибками. Это именно тот пробел, который закрывает пассивный health check.
</Hook>

<Crux>Активные пробы обнаруживают крахи за 10–30 с; пассивное обнаружение улавливает деградацию с 5xx — используйте оба для защиты в глубину.</Crux>

<Explanation>

## Активные health checks

LB отправляет **пробные запросы** к каждому backend с фиксированным интервалом, независимо от реального клиентского трафика.

**Типы проб:**
- **HTTP GET `/healthz`** — наиболее распространённый; ожидает 200 OK. Обнаруживает отказы на уровне приложения (заполнен диск, база данных недоступна).
- **TCP SYN** — лёгкий; просто пытается установить TCP. Обнаруживает полный сетевой или процессный отказ, но пропускает ошибки приложения.
- **gRPC health check** — стандарт для gRPC-сервисов (`grpc.health.v1.Health/Check`).
- **Кастомные UDP-пробы** — для не-TCP-сервисов.

**Пороги:**
- Помечать **unhealthy** после 2–3 последовательных отказов.
- Помечать **healthy** после 2–3 последовательных успехов.
- Интервал: 10–30 секунд.

**Пробел тихого отказа.** Backend, упавший жёстко (процесс убит), обнаруживается TCP SYN. Backend, зависший (thread pool исчерпан, возвращает 500), принимает TCP, но HTTP-запросы завершаются ошибкой — TCP SYN-проба это полностью пропускает. HTTP GET-пробы обнаруживают это, но только если URL пробы упражняет реальную логику приложения, а не тривиальную заглушку, всегда возвращающую 200.

## Пассивные health checks и обнаружение выбросов

Вместо отправки проб LB **наблюдает реальный трафик**. Если backend возвращает повторные 5xx-ошибки или повторные таймауты, он является выбросом.

**Обнаружение выбросов Envoy (конфиг по умолчанию):**
- Исключить backend после **5 последовательных 5xx-ответов**.
- Продолжительность исключения начинается с **30 секунд**, удваивается при каждом повторном исключении: 30 с → 60 с → 120 с → до ~300 с.
- LB прекращает маршрутизировать новые запросы к исключённому backend на этот период.

**Пассивный vs активный — что каждый обнаруживает:**
- Активный обнаруживает: упавший процесс, сетевой раздел, неправильно маршрутизированный порт.
- Пассивный обнаруживает: зависания на уровне приложения, исчерпание пула БД, 500-бури из-за GC.
- Ни один отдельно не достаточен. Используйте оба для эшелонированной защиты.

## Флаппинг health checks

Сетевой сбой вызывает 2–3 последовательных отказа активной проверки → backend исключён. Сеть восстанавливается → 2–3 успеха → backend добавлен обратно. Снова сбой → исключён. Это циклирование — **флаппинг** — вызывает быстрые колебания трафика, когда backend переключается между healthy и unhealthy.

**Решение:** Увеличить пороги отказа/успеха (требовать 5 последовательных отказов перед исключением). Добавить **джиттер** к времени проверок, чтобы проверки от нескольких реплик LB не синхронизировались. Экспоненциальный откат исключения Envoy (30 с → 60 с → 120 с) естественно стабилизирует флаппирующий backend: окно исключения расширяется, пока не станет длиннее продолжительности флапа.

<NumbersCard
  id="card-health-check-numbers"
  title="Числа health check и draining"
  data-lesson-visual
  stats={[
    { label: "Интервал активного health check", value: "10–30 с" },
    { label: "Последовательных отказов до исключения", value: "2–3" },
    { label: "Последовательных успехов до добавления", value: "2–3" },
    { label: "Начальная продолжительность исключения Envoy", value: "30 с" },
    { label: "Максимальная продолжительность исключения Envoy", value: "~300 с" },
    { label: "Таймаут drain AWS ALB (по умолчанию)", value: "300 с" },
    { label: "Drain для коротких HTTP", value: "5–30 с" },
    { label: "Drain для WebSocket/SSE", value: "300+ с" },
    { label: "Продолжительность медленного старта (типичная)", value: "1–5 мин" },
  ]}
/>

## Connection draining

При удалении backend (деплой, масштабирование вниз, обслуживание) нельзя резко обрывать активные соединения. **Connection draining:**

1. Прекратить маршрутизировать **новые** запросы на backend.
2. Позволить **запросам в полёте** завершиться в пределах таймаута drain.
3. После таймаута принудительно закрыть оставшиеся соединения.

**Таймауты drain:**
- Короткие HTTP-запросы: 5–30 с.
- Долгоживущие WebSocket / SSE-соединения: 300+ с.
- AWS ALB по умолчанию: 300 с (настраивается 0–3 600 с).
- GCP: настраивается 0–3 600 с.

**Сторона приложения.** При SIGTERM backend должен:
1. Вызвать `close(listening_socket)` — прекратить принимать новые соединения.
2. Завершить все запросы в полёте.
3. Выйти чисто.

Без draining LB удаляет backend посреди запроса: клиент получает connection reset и вынужден делать retry. С draining запрос завершается нормально и backend тихо выходит.

## Slow start / прогрев

Когда backend **возвращается** в пул после восстановления или первого деплоя, он холодный:
- Кэши в процессе пусты.
- Пулы соединений к БД нужно прогреть.
- Кэши TLS-сессий холодные.

Немедленная отправка 100% трафика вызывает отставание backend под нагрузкой всплеска, что потенциально запускает каскадный отказ.

**Решение:** Постепенно наращивать трафик — 10% → 50% → 100% в течение 1–5 минут. Envoy поддерживает это через `slow_start_window` и `slow_start_duration`. AWS ALB поддерживает через взвешенную target group: новый backend начинает с весом 1 и инкрементируется со временем.

<TraceScenario
  id="lb-mid-trace-health"
  pieceSlug="04-health-checks-and-draining"
  lang="ru"
  scenario="Проследите падение backend, обнаружение health check и плавное возвращение."
  steps={[
    { prompt: "Backend B2 упал. Активный health check LB зондирует B2 через HTTP GET /healthz каждые 30 с. Что видит LB на первой пробе после падения?", reveal: "TCP-соединение к B2 отклонено (ECONNREFUSED) или по таймауту. LB фиксирует один отказ. Продолжает маршрутизировать запросы к B2, так как порог отказов ещё не достигнут." },
    { prompt: "После 2–3 последовательных отказов health check LB помечает B2 unhealthy. Что происходит с новыми запросами vs запросами в полёте?", reveal: "Новые запросы больше не маршрутизируются к B2 — они идут к B1, B3, B4 по алгоритму балансировки. Запросы в полёте на существующих соединениях к B2 разрешается завершить (или упасть по собственному TCP-таймауту)." },
    { prompt: "B2 перезапустился и прошёл 3 последовательных health check. Как трафик нарастает обратно?", reveal: "Slow start: LB добавляет B2 обратно на 10% от его нормальной доли на 1 минуту, затем 50% на 1 минуту, затем 100%. Это позволяет пулу соединений к БД B2 прогреться и внутрипроцессному кэшу заполниться до прихода полной нагрузки." },
    { prompt: "B3 всё ещё работает, но его пул БД исчерпан — он возвращает 500 на все запросы. Активный health check (TCP SYN) всё ещё успешен. Что обнаружит проблему?", reveal: "Пассивное обнаружение выбросов: Envoy наблюдает, что B3 вернул 5 последовательных 5xx-ответов на реальном трафике. Он исключает B3 на 30 с. Если B3 восстановится и пройдёт проверки, он возвращается в пул с медленным стартом." },
  ]}
/>

<Inset kind="edgecase" lang="ru">
**Дизайн endpoint health check.** Тривиальный `/healthz`, всегда возвращающий 200, опасен — он проходит активную пробу даже когда приложение сломано. Хороший health check endpoint проверяет минимальные критические зависимости: можем ли мы подключиться к БД? Можем ли подключиться к кэшу? Возвращает 200 только если backend реально может обслуживать запросы. Но не добавляйте все зависимости: если некритичный downstream-сервис упал, возврат 500 из `/healthz` излишне исключит backend.
</Inset>

<Quiz
  id="lb-mid-quiz-health"
  pieceSlug="04-health-checks-and-draining"
  lang="ru"
  question="Backend принимает TCP-соединения, но его thread pool исчерпан и на каждый HTTP-запрос возвращает 500. Какой тип health check это обнаружит, а какой пропустит?"
  choices={[
    { label: "TCP SYN обнаружит; HTTP GET пропустит", misconception: "TCP SYN только проверяет, что TCP подключается — если процесс принимает соединение, но HTTP затем не работает, TCP SYN видит успех." },
    { label: "HTTP GET /healthz обнаружит, если endpoint упражняет реальную логику приложения; TCP SYN пропустит, потому что TCP-соединение успешно, даже пока приложение падает", correct: true },
    { label: "Оба обнаруживают одновременно", misconception: "TCP SYN не может обнаружить отказы на HTTP-уровне. Только HTTP-уровневые пробы могут это сделать." },
    { label: "Ни один не обнаружит — только пассивное обнаружение выбросов может", misconception: "HTTP GET health check, упражняющий реальную логику приложения (не тривиальную заглушку), обнаружит это через код ответа 500." },
  ]}
/>

<Quiz
  id="lb-mid-quiz-drain"
  pieceSlug="04-health-checks-and-draining"
  lang="ru"
  question="Почему connection draining необходим при удалении backend из пула балансировщика?"
  choices={[
    { label: "Чтобы LB мог сбросить таблицу маршрутизации перед удалением backend", misconception: "Обновление таблицы маршрутизации LB происходит быстро. Проблема в завершении запросов в полёте, а не в состоянии маршрутизации LB." },
    { label: "Потому что немедленное удаление backend обрывает активные TCP-соединения посреди запроса, вызывая потерю данных и видимые пользователю ошибки", correct: true },
    { label: "Чтобы предотвратить выполнение health checks против удалённого backend", misconception: "Health checks автоматически прекращаются при удалении backend. Draining касается завершения запросов в полёте." },
    { label: "Чтобы дождаться завершения текущего цикла health check у backend", misconception: "Draining касается завершения клиентских запросов, а не циклов health check." },
  ]}
/>

</Explanation>

<FlowDiagram
  label="Жизненный цикл бэкенда в пуле: Healthy обслуживает трафик; после 2–3 подряд неудачных проб он помечается Unhealthy и новые запросы прекращаются; запросы в полёте завершаются через Draining; после 2–3 подряд успехов он возвращается через Slow start, наращивая трафик с 10 до 100 процентов, прежде чем вернуться в Healthy."
  hue="var(--d-network)"
  perRow="4"
  nodes={[
    { id: "healthy", label: "Healthy", sub: "обслуживает" },
    { id: "unhealthy", label: "Unhealthy", sub: "нет новых" },
    { id: "drain", label: "Draining", sub: "в полёте завершить" },
    { id: "slowstart", label: "Slow start", sub: "рост 10→100%" },
  ]}
  edges={[
    { from: "healthy", to: "unhealthy", label: "2–3 неудачных пробы" },
    { from: "unhealthy", to: "drain", label: "убрать из пула" },
    { from: "drain", to: "slowstart", label: "2–3 успеха" },
    { from: "slowstart", to: "healthy", label: "рост завершён" },
  ]}
  caption="Удаление плавное (запросы в полёте дренируются до завершения, а не обрываются на полпути), а возврат постепенный (slow start, чтобы холодный бэкенд с пустыми кэшами не утонул под полной нагрузкой в первую же секунду после возврата)."
/>

<KeyTakeaway>Активные пробы обнаруживают падения за 10–30 с; пассивное обнаружение выбросов улавливает тихую деградацию с 5xx в реальном времени. Connection draining позволяет запросам в полёте завершиться (5–30 с для HTTP, 300+ с для WebSocket) перед удалением backend. Slow start нарастает трафик нового backend 10%→100% за несколько минут, чтобы холодные кэши успели прогреться.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="04-health-checks-and-draining-retrieval"
  lang="ru"
  questions={[
    { q: "Почему нужно использовать и активные, и пассивные health checks, а не только один?", a: "Активные проверки обнаруживают полные отказы (упавший процесс, сетевой раздел), но пропускают деградацию на уровне приложения (исчерпан thread pool, база данных недоступна при TCP-подключении). Пассивные проверки обнаруживают 5xx-бури на реальном трафике, но не работают в периоды низкого трафика. Вместе они обеспечивают эшелонированную защиту: активные — страховочная сеть, пассивные — обнаружение частых боевых отказов." },
    { q: "Что такое флаппинг health check и как экспоненциальный откат исключения Envoy его смягчает?", a: "Флаппинг — когда backend колеблется между healthy и unhealthy из-за нестабильной сети: исключён → сеть восстановилась → добавлен → сбой → исключён снова. Экспоненциальный откат Envoy удваивает продолжительность исключения при каждом повторном исключении (30 с → 60 с → 120 с → до ~300 с), поэтому окно исключения флаппирующего backend расширяется до тех пор, пока не переживёт нестабильный отказ, естественно стабилизируясь." },
    { q: "Что должен делать backend при SIGTERM для сотрудничества с connection draining?", a: "Закрыть listening socket (прекратить принимать новые соединения), завершить все запросы в полёте, затем выйти. Это предотвращает ненужное ожидание LB полного таймаута drain: как только backend закрывает listening socket, LB знает, что новых соединений не будет, и может завершить drain сразу, как только все существующие соединения закроются." },
  ]}
/>

<Recap lang="ru">
Две стратегии health check дополняют друг друга. Активные проверки отправляют пробы (HTTP GET, TCP SYN, gRPC) каждые 10–30 секунд и исключают backend после 2–3 отказов — быстро, но слепы к деградации на уровне приложения, когда TCP всё ещё принимает соединения. Пассивное обнаружение выбросов наблюдает реальный трафик и исключает после повторных 5xx-ответов с экспоненциальным откатом (30 с → 300 с), гасящим флаппинг. Connection draining закрывает разрыв при удалении: новые запросы прекращаются немедленно, запросы в полёте получают 5–30 с (HTTP) или 300+ с (WebSocket) на завершение. Slow start защищает возвращающиеся backends, нарастая трафик с 10% до 100% за 1–5 минут, чтобы холодные кэши и пулы соединений успели прогреться до полной нагрузки.
</Recap>
