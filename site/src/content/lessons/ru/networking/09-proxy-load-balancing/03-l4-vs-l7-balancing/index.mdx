---
slug: 03-l4-vs-l7-balancing
lang: ru
track: networking
unit: 09-proxy-load-balancing
order: 3
title: "L4 vs L7 балансировка и сохранение IP клиента"
summary: "L4 маршрутизирует по IP:порту за 1–5 мкс с пропускной способностью 10–40 Гбит/с; L7 парсит HTTP за 100–500 мкс для умной маршрутизации — а X-Forwarded-For, RFC 7239 и PROXY protocol каждый по-своему сохраняют IP клиента."
estMin: 11
status: ready
lessonType: topic
level: middle
prereqs:
  - "09-proxy-load-balancing/01-what-reverse-proxies-do"
  - "05-tls-handshake/01-what-tls-does"
  - "06-http-versions/01-http-fundamentals"
deepensInto:
  - "07-resilient-lb-architecture"
spiral: ["latency"]
concepts: ["l4-load-balancer", "l7-load-balancer", "x-forwarded-for", "rfc-7239-forwarded", "proxy-protocol", "tls-termination", "content-routing"]
sources:
  - https://www.haproxy.com/blog/layer-4-vs-layer-7-load-balancing/
  - https://www.rfc-editor.org/rfc/rfc7239.html
  - https://www.envoyproxy.io/docs/envoy/latest/intro/arch_overview/upstream/load_balancing/load_balancers.html
---

import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import NumbersCard from "~/components/layout/NumbersCard.astro";
import Misconception from "~/components/layout/Misconception.astro";
import TraceScenario from "~/components/pedagogy/TraceScenario.astro";
import RFCQuiz from "~/components/pedagogy/RFCQuiz.astro";

<Hook>
Логи сервиса показывают, что все запросы приходят с одного IP — IP балансировщика. Нельзя применить rate limiting по клиенту, geo-блокировку, нельзя провести аудит. Это цена невнимательного выбора уровня, на котором работает балансировщик.
</Hook>

<Crux>L4 маршрутизирует по IP:порту на скорости провода; L7 парсит HTTP за 100× дороже — оба скрывают IP без XFF, RFC 7239, PROXY protocol.</Crux>

<Explanation>

## Layer 4: маршрутизация по IP и порту

L4 балансировщик видит только **TCP или UDP заголовок**. Маршрутизирует по 4-tuple: IP источника, порт источника, IP назначения, порт назначения. HTTP-payload он никогда не читает.

**Механика:**
- Видит TCP SYN от клиента.
- Выбирает backend по настроенному алгоритму.
- Либо перезаписывает IP назначения (режим NAT), либо открывает новое TCP-соединение к backend (режим прокси).
- Передаёт сырые TCP-сегменты в обоих направлениях.

**Стоимость:** ~1–5 мкс на пакет, &lt;1% CPU. Пропускная способность: 10–40 Гбит/с на сервер.

**Что L4 умеет:**
- Маршрутизировать любой TCP/UDP-протокол (PostgreSQL, Redis, MQTT, кастомный бинарный).
- Обрабатывать миллионы соединений в секунду.
- Завершать TCP независимо от HTTP.

**Что L4 не умеет:**
- Инспектировать или модифицировать HTTP-заголовки, путь или host.
- Делать retry на HTTP 5xx (он не понимает HTTP).
- Завершать TLS без понимания формата TLS-записей.
- Маршрутизировать `/api/v1/` и `/api/v2/` на разные backends.

## Layer 7: маршрутизация по содержимому HTTP

L7 балансировщик завершает **HTTP (и TLS)** соединение, парсит запрос и применяет правила маршрутизации перед пересылкой на backend.

**Механика:**
1. Завершает TLS клиента — видит открытый текст.
2. Читает HTTP-метод, путь, заголовок `Host`, кастомные заголовки.
3. Применяет правила маршрутизации (например, `/api/` → пул A, `/static/` → пул B).
4. Открывает новое соединение (или переиспользует пул) к выбранному backend.
5. Пересылает запрос (добавляя proxy-заголовки, такие как `X-Forwarded-For`).

**Стоимость:** ~100–500 мкс на запрос, 5–15% CPU. Пропускная способность: 1–10 Гбит/с на сервер.

**Что L7 добавляет к L4:**
- Маршрутизацию по содержимому: путь, hostname, заголовок, cookie.
- Retry на 5xx (понимает HTTP-семантику).
- Метрики на endpoint, rate limiting, проверки аутентификации.
- Завершение TLS один раз на edge LB вместо каждого backend.
- Разделение трафика (10% → canary, 90% → stable).

<Misconception id="lb-l7-always-better">
«L7 всегда лучше». Неверно. L4 в 10–100 раз быстрее и поддерживает UDP и не-HTTP протоколы (базы данных, игровые серверы). L7 добавляет задержку, но открывает контент-маршрутизацию. L4 — для сырой пропускной способности, L7 — для микросервисов.
</Misconception>

<NumbersCard
  id="card-l4-l7-compare"
  title="L4 vs L7 балансировка"
  data-lesson-visual
  stats={[
    { label: "Задержка хопа L4", value: "1–5 мкс" },
    { label: "Задержка хопа L7", value: "100–500 мкс" },
    { label: "Пропускная способность L4 на сервер", value: "10–40 Гбит/с" },
    { label: "Пропускная способность L7 на сервер", value: "1–10 Гбит/с" },
    { label: "CPU L4 на пакет", value: "<1%" },
    { label: "CPU L7 на запрос", value: "5–15%" },
    { label: "Протоколы L4", value: "любой TCP/UDP" },
    { label: "Протоколы L7", value: "HTTP, gRPC, WebSocket" },
  ]}
/>

## Сохранение IP клиента

Когда LB пересылает запрос на backend, backend видит **IP LB** в качестве TCP-источника — не реальный IP клиента. Существуют три стандарта для восстановления этой информации:

### X-Forwarded-For (XFF)

```
X-Forwarded-For: 203.0.113.195
```

LB добавляет IP клиента в этот заголовок. Только для HTTP. Тривиально подделывается: вредоносный клиент может отправить `X-Forwarded-For: 1.2.3.4`, и непроверенный прокси добавит его, а не перезапишет. Надёжен только **крайний правый IP, добавленный доверенным прокси**.

### Заголовок RFC 7239 Forwarded

```
Forwarded: for=203.0.113.195;proto=https;host=example.com
```

Стандартизированный (RFC 7239), структурированный формат, сложнее некорректно распарсить. Включает протокол и host вместе с IP. Только для HTTP, но менее подвержен случайному неверному разбору.

### PROXY protocol (HAProxy)

Текстовый или бинарный заголовок, **добавляемый перед TLS/HTTP-payload**:

```
PROXY TCP4 203.0.113.195 93.184.216.34 35646 80\r\n
```

Работает для **любого TCP/UDP-протокола** — HTTP не нужен. TCP-стек backend читает заголовок до любого парсинга на прикладном уровне. Сложнее подделать, чем HTTP-заголовок, поскольку вставляется на транспортном уровне доверенным прокси. Nginx, HAProxy и Envoy поддерживают PROXY protocol.

**Что выбрать:** HTTP-сервисы за доверенными прокси → XFF или RFC 7239. Не-HTTP-сервисы (PostgreSQL, MQTT, кастомный бинарный) или там, где важна устойчивость к подделке → PROXY protocol.

<TraceScenario
  id="lb-mid-trace-1"
  pieceSlug="03-l4-vs-l7-balancing"
  lang="ru"
  scenario="Проследите запрос, переходящий с L4 на L7 в двухуровневой архитектуре LB."
  steps={[
    { prompt: "Клиент подключается к L4 балансировщику на порту 443. Что видит L4 LB и по чему может маршрутизировать?", reveal: "L4 LB видит 4-tuple (IP клиента, порт клиента, IP LB, 443). Может маршрутизировать только по IP и порту — TLS SNI или HTTP-путь он не читает. Выбирает backend из пула по hash 5-tuple или round-robin и передаёт сырой TCP-поток." },
    { prompt: "L4 LB пересылает на L7 LB (например, Envoy). L7 LB завершает TLS. Что он видит теперь?", reveal: "После завершения TLS L7 LB читает HTTP-запрос в открытом тексте: метод, путь, заголовок Host, кастомные заголовки. Теперь может маршрутизировать: /api/ → пул backend A, /admin/ → пул backend B. Также видит реальный IP клиента и добавляет X-Forwarded-For." },
    { prompt: "L7 LB добавляет X-Forwarded-For и пересылает на пул backend A. Backend читает XFF. Какое допущение о доверии он должен делать?", reveal: "Backend должен доверять только X-Forwarded-For от LB. Если backend доступен напрямую (не только через LB), клиент может подделать XFF. Backend должен доверять XFF только от известных IP LB и проверять крайний правый (последний добавленный) IP в заголовке." },
    { prompt: "Backend нужен реальный IP клиента для rate limiting, но это сырой TCP-сервис (не HTTP). Как получить IP клиента?", reveal: "Используйте PROXY protocol: L4 LB добавляет 'PROXY TCP4 <IP клиента> <IP backend> <sport> <dport>\\r\\n' перед TCP-payload. Приложение backend читает эту строку первой. Работает независимо от следующего протокола (HTTP, PostgreSQL, MQTT и т.д.)." },
  ]}
/>

<RFCQuiz
  id="lb-mid-rfc-1"
  pieceSlug="03-l4-vs-l7-balancing"
  lang="ru"
  question="Какой RFC стандартизирует заголовок Forwarded для сохранения IP клиента через прокси?"
  choices={[
    { rfc: "RFC 7239", title: "Forwarded HTTP Extension", correct: true },
    { rfc: "RFC 9110", title: "HTTP Semantics" },
    { rfc: "RFC 3986", title: "URI Generic Syntax" },
    { rfc: "RFC 1918", title: "Address Allocation for Private Internets" },
  ]}
/>

<Inset kind="why" lang="ru">
**Зачем L4 + L7 последовательно?** Многие production-архитектуры используют L4 LB на сетевом edge (BGP anycast, поглощает флуды, обрабатывает миллионы соединений) и L7 LB за ним (завершает TLS, применяет политики маршрутизации, выдаёт метрики на endpoint). L4-уровень дёшево справляется с пакетным потоком; L7-уровень добавляет интеллект без необходимости масштабироваться до пакетной пропускной способности. AWS использует этот двухуровневый паттерн: Network Load Balancer (L4) → Application Load Balancer (L7).
</Inset>

<Quiz
  id="lb-mid-quiz-l4-l7"
  pieceSlug="03-l4-vs-l7-balancing"
  lang="ru"
  question="Нужно маршрутизировать /api/payments/ на PCI-DSS-совместимый пул backend, а /api/catalog/ на обычный пул. Какой уровень балансировщика может это сделать и почему?"
  choices={[
    { label: "L4, потому что он обрабатывает весь TCP-трафик", misconception: "L4 видит только IP и порт, но не URL-путь. Он не может различить /payments/ от /catalog/." },
    { label: "L7, потому что он парсит HTTP-запрос и может маршрутизировать по пути, host и заголовкам", correct: true },
    { label: "Любой уровень, потому что оба читают HTTP-заголовки", misconception: "L4 не читает HTTP-заголовки. Только L7 парсит HTTP." },
    { label: "Ни один — для маршрутизации по пути нужен DNS", misconception: "DNS работает на уровне hostname, а не URL-пути. Маршрутизация по пути требует L7." },
  ]}
/>

</Explanation>

<KeyTakeaway>L4 маршрутизирует по IP:порту на скорости провода (1–5 мкс, 10–40 Гбит/с) для любого протокола; L7 парсит HTTP для маршрутизации по содержимому за 100–500 мкс на запрос. В обоих случаях backend видит IP LB — восстановите IP клиента через X-Forwarded-For (HTTP), RFC 7239 Forwarded (структурированный HTTP) или PROXY protocol (любой TCP/UDP).</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="03-l4-vs-l7-balancing-retrieval"
  lang="ru"
  questions={[
    { q: "Нужно маршрутизировать по HTTP-пути и делать retry на 5xx. Какой уровень LB требуется и какова его задержка?", a: "L7 балансировка. Завершает HTTP, парсит запрос и понимает HTTP-семантику (5xx означает ошибку приложения → допустимо retry). Стоимость задержки: 100–500 мкс на запрос, 5–15% CPU, пропускная способность 1–10 Гбит/с — против 1–5 мкс у L4 при 10–40 Гбит/с." },
    { q: "Почему X-Forwarded-For поддаётся подделке, и как PROXY protocol улучшает ситуацию?", a: "X-Forwarded-For — HTTP-заголовок, который любой клиент может выставить до того, как прокси добавит своё значение. Без строгой проверки IP доверенных прокси backend не может знать, какой IP в списке подлинный. PROXY protocol — бинарный/текстовый заголовок, добавляемый прокси на TCP-уровне: клиент, отправляющий сырые TCP-байты, не может легко подделать PROXY-заголовок, который придёт раньше заголовка самого прокси, поэтому подделать его значительно сложнее." },
    { q: "Когда выбирают L4 вместо L7 даже для HTTP-трафика?", a: "Когда сырая пропускная способность важнее маршрутизации по содержимому: CDN edge-точки под DDoS-флудами, игровые серверы (UDP), балансировка протоколов баз данных (PostgreSQL, Redis) или любой не-HTTP бинарный протокол. L4 стоит в 10–100 раз меньше CPU на пакет и обрабатывает в 10 раз больше трафика." },
  ]}
/>

<Recap lang="ru">
L4 балансировщики маршрутизируют по TCP/UDP 4-tuple — они быстрые (1–5 мкс, 10–40 Гбит/с) и не зависят от протокола, но не могут инспектировать HTTP. L7 балансировщики завершают TLS, парсят HTTP и обеспечивают маршрутизацию по пути/host/заголовку и retry на 5xx — ценой задержки 100–500 мкс и 5–15% CPU. Оба типа скрывают IP клиента от backend; три стандарта для его восстановления: X-Forwarded-For (де-факто, но поддаётся подделке), RFC 7239 Forwarded (структурированный, только HTTP) и PROXY protocol (бинарный, любой TCP/UDP, сложнее всего подделать). Production-архитектуры часто используют L4 на edge и L7 за ним: сырой пакетный поток дёшево на L4, контент-интеллект на L7.
</Recap>
