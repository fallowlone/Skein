---
slug: 01-what-reverse-proxies-do
lang: ru
track: networking
unit: 09-proxy-load-balancing
order: 1
title: "Что делают обратные прокси"
summary: "Обратный прокси незаметно стоит между клиентами и backends — он завершает клиентское соединение, выбирает backend и пересылает запрос, отделяя масштабирование от клиентской видимости."
estMin: 8
status: ready
lessonType: topic
level: junior
prereqs:
  - "03-tcp-handshake/01-the-three-way-handshake"
  - "06-http-versions/01-http-fundamentals"
  - "07-cdn-edge/01-cdn-what-and-why"
deepensInto:
  - "02-balancing-algorithms"
  - "03-l4-vs-l7-balancing"
spiral: ["latency", "statefulness"]
concepts: ["reverse-proxy", "load-balancer", "backend-pool", "health-check", "two-connections"]
sources:
  - https://www.haproxy.com/blog/layer-4-vs-layer-7-load-balancing/
  - https://www.nginx.com/resources/glossary/reverse-proxy-server/
  - https://www.envoyproxy.io/docs/envoy/latest/intro/what_is_envoy
---

import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import MetaphorComplete from "~/components/pedagogy/MetaphorComplete.astro";
import NumbersCard from "~/components/layout/NumbersCard.astro";

<Hook>
Вы кликаете ссылку и подключаетесь к тому, что кажется единым сервером — но за этим адресом стоят десятки машин, готовых обработать ваш запрос. Что-то невидимое решило, какая из них вас обслужит, и вы никогда не узнаете, какая именно.
</Hook>

<Crux>Обратный прокси перехватывает соединение клиента, выбирает backend и пересылает запрос — клиент никогда не знает о реальных серверах.</Crux>

<Explanation>

## Невидимый посредник

**Обратный прокси** стоит между клиентами и пулом backend-серверов. Клиенты подключаются к прокси, считая его реальным сервером. Прокси:

1. Завершает TCP (и TLS) рукопожатие с клиентом.
2. Выбирает backend из своего пула.
3. Открывает **отдельное** TCP-соединение к этому backend.
4. Пересылает запрос клиента на backend.
5. Ждёт ответ backend и передаёт его клиенту.

Клиент никогда не видит IP-адрес backend и не знает, сколько backends существует. Это ключевое свойство: **клиент говорит с одним адресом; прокси говорит со многими.**

## Почему два соединения, а не пересылка пакетов

Простой пересыльщик пакетов (layer 3 NAT) передаёт пакеты без изменений — он не может инспектировать или модифицировать полезную нагрузку. Обратный прокси, который маршрутизирует по HTTP, должен **распарсить запрос**, чтобы решить, куда его отправить. Парсинг требует владения обеими сторонами: клиентским TCP-соединением (для чтения запроса) и backend-TCP-соединением (для его отправки).

Именно эта двухсоединительная модель делает прокси мощным и видимым для каждой стороны. Backend видит IP прокси, а не IP клиента — вопрос, рассматриваемый в следующем уроке.

## Метафора ресторана

Ресторан с одним **хостом** (load balancer) и несколькими **шеф-поварами** (backends). Когда гость (клиент) приходит, хост решает, кто из поваров наименее занят, передаёт заказ и возвращает блюдо гостю. Гости никогда не говорят с поварами. Если повар заболел, хост прекращает отправлять к нему заказы — незаметно.

<NumbersCard
  id="card-lb-basics"
  title="Основы обратного прокси"
  data-lesson-visual
  stats={[
    { label: "Соединения, видимые клиенту", value: "1 (к IP прокси)" },
    { label: "TCP-соединений на запрос (L7 прокси)", value: "2 (клиент→прокси + прокси→backend)" },
    { label: "Backends, видимых клиенту", value: "0 (прокси скрывает все)" },
    { label: "Интервал health check (типичный)", value: "10–30 с" },
    { label: "Backends, которые может обслуживать один nginx", value: "сотни" },
    { label: "Zero-downtime деплой: drain + swap", value: "да, через health check + draining" },
  ]}
/>

## Что происходит при падении backend

Цикл health check прокси зондирует каждый backend с фиксированным интервалом (10–30 с). После 2–3 последовательных отказов backend помечается **unhealthy** и новые запросы к нему больше не маршрутизируются. Когда backend восстанавливается, он возвращается в пул после 2–3 последовательных успехов.

Это означает:
- Клиенты маршрутизируются только к **здоровым** backends.
- Падение backend невидимо для новых клиентов в пределах одного интервала health check.
- Активные соединения к упавшему backend всё равно падают — прокси не может воскресить умершее TCP-соединение.

<Inset kind="why" lang="ru">
**Почему forward proxy отличается.** Forward proxy (например, корпоративный веб-фильтр) стоит на *исходящем* пути: клиент знает о нём и подключается к нему умышленно. Reverse proxy стоит на *входящем* пути: клиент о нём не знает и считает, что говорит напрямую с origin-сервером. Forward proxies реализуют клиентскую политику; reverse proxies обеспечивают серверное масштабирование и отказоустойчивость.
</Inset>

<Quiz
  id="lb-jr-quiz-1"
  pieceSlug="01-what-reverse-proxies-do"
  lang="ru"
  question="Почему обратный прокси открывает ДВА отдельных TCP-соединения вместо того, чтобы пересылать пакеты напрямую на backend?"
  choices={[
    { label: "Чтобы дважды зашифровать трафик", misconception: "TLS — отдельный слой; архитектура прокси не связана с двойным шифрованием." },
    { label: "Потому что он должен распарсить HTTP-запрос, чтобы решить, куда маршрутизировать, — это требует владения обоими соединениями", correct: true },
    { label: "Чтобы сделать трафик труднее перехватить", misconception: "Это задача TLS, а не архитектуры прокси." },
    { label: "Обратный прокси не открывает два соединения — он пересылает пакеты на уровне L3", misconception: "L3-пересылка не может инспектировать HTTP. HTTP-aware прокси требует двух TCP-соединений." },
  ]}
/>

<Quiz
  id="lb-jr-quiz-2"
  pieceSlug="01-what-reverse-proxies-do"
  lang="ru"
  question="Backend падает в середине запроса. Что load balancer делает со СЛЕДУЮЩИМ клиентским запросом?"
  choices={[
    { label: "Маршрутизирует на упавший backend в надежде на восстановление", misconception: "Health checks фиксируют падение и удаляют backend из пула." },
    { label: "Маршрутизирует на здоровый backend из пула, поскольку health checks уже (или скоро) удалили упавший backend", correct: true },
    { label: "Возвращает 200 OK с кэшированным ответом из памяти", misconception: "LB не кэш — он маршрутизирует запросы к backends." },
    { label: "Немедленно разрывает все соединения ко всем backends", misconception: "Затрагивается только соединение с упавшим backend." },
  ]}
/>

<DragOrder
  id="lb-jr-drag-1"
  pieceSlug="01-what-reverse-proxies-do"
  lang="ru"
  prompt="Расставьте последовательность, когда клиент делает запрос через load balancer:"
  items={[
    "Клиент устанавливает TCP-соединение к load balancer",
    "Клиент отправляет HTTP-запрос к load balancer",
    "Load balancer выбирает здоровый backend",
    "Load balancer открывает TCP-соединение к этому backend",
    "Load balancer пересылает запрос клиента на backend",
    "Backend обрабатывает запрос и отправляет ответ",
    "Load balancer пересылает ответ обратно клиенту",
  ]}
/>

<MetaphorComplete
  id="lb-jr-metaphor-1"
  pieceSlug="01-what-reverse-proxies-do"
  lang="ru"
  setup="Load balancer похож на ресторанного _______, который усаживает гостей и направляет заказы на кухню."
  accepted={["хоста", "хост", "хостесс", "сомелье", "рецепциониста", "менеджера"]}
  canonical="хост (любая роль, направляющая трафик)"
  explanation="Load balancer, как ресторанный хост, получает все входящие запросы и маршрутизирует их на доступные backends исходя из текущей нагрузки, полностью скрывая кухню от гостей."
/>

</Explanation>

<KeyTakeaway>Обратный прокси открывает два TCP-соединения на запрос — одно к клиенту, одно к backend — давая ему возможность маршрутизировать, проверять здоровье и скрывать пул backends. Клиент видит один IP; прокси берёт остальное на себя.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="01-what-reverse-proxies-do-retrieval"
  lang="ru"
  questions={[
    { q: "Почему load balancer должен определять состояние backend?", a: "Если backend падает или становится перегруженным, load balancer должен прекратить маршрутизировать новые запросы к нему и отправлять их на более здоровые backends. Без определения состояния LB отправлял бы запросы на мёртвые backends, и клиенты видели бы ошибки." },
    { q: "В чём ключевое отличие forward proxy от reverse proxy?", a: "Forward proxy — на стороне клиента: клиент знает о нём и использует намеренно для управления исходящим доступом или конфиденциальности. Reverse proxy — на стороне сервера: клиент о нём не знает и думает, что говорит напрямую с origin-сервером." },
    { q: "Почему reverse proxy открывает два TCP-соединения на запрос, а не пересылает пакеты на уровне L3?", a: "Чтобы маршрутизировать по HTTP path, hostname или header, прокси должен распарсить запрос — это требует владения клиентским соединением (для чтения) и backend-соединением (для пересылки). L3-пересылка передаёт сырые пакеты без разбора HTTP." },
  ]}
/>

<Recap lang="ru">
Обратный прокси является публичным лицом всего пула backends: клиенты подключаются к одному IP, прокси выбирает здоровый backend, открывает собственное соединение к нему и передаёт трафик в обе стороны. Два TCP-соединения на запрос — плата за возможность парсить и маршрутизировать HTTP. Health checks работают в фоне каждые 10–30 секунд; после 2–3 отказов backend удаляется из пула и трафик немедленно перераспределяется. Это разделение — клиенты говорят с одним адресом, backends меняются свободно — делает zero-downtime деплои и масштабирование невидимыми для пользователей.
</Recap>
