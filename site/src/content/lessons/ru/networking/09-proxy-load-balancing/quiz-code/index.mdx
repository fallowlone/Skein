---
concepts:
- nginx-upstream-config
- power-of-two-choices
- proxy-protocol
- connection-draining
- circuit-breaker-config
estMin: 14
lang: ru
level: senior
order: 9
prereqs: []
slug: quiz-code
sources:
- https://www.envoyproxy.io/docs/envoy/latest/intro/arch_overview/upstream/circuit_breaking
- https://www.haproxy.com/blog/layer-4-vs-layer-7-load-balancing/
- https://www.rfc-editor.org/rfc/rfc7239.html
status: ready
summary: "Читайте реальные конфиги LB, сниппет балансировки, заголовок PROXY protocol и обработчик graceful shutdown — затем выбирайте поведение или исправление с наибольшим рычагом."
title: 'Proxy и load balancing: чтение конфигов и кода'
track: networking
unit: 09-proxy-load-balancing
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Поведение LB решается в конфигах и обработчиках завершения, а не на диаграммах. Прочитайте каждый сниппет, предскажите его поведение под нагрузкой и выберите исправление, которое senior-инженер сделает первым.
</Hook>

<Goal lang="ru">
Отработайте цикл, который вы запускаете на каждом инциденте с LB: прочитать конфиг upstream или обработчик, предсказать режим отказа и взяться за изменение, которое действительно его чинит.
</Goal>

## Сниппет 1 — блок upstream в nginx

```nginx
upstream api {
    server 10.0.0.1:8080;
    server 10.0.0.2:8080;
    server 10.0.0.3:8080;
}

server {
    location / {
        proxy_pass http://api;
    }
}
```

<Quiz
  id="09-proxy-load-balancing-code-1"
  lessonSlug="09-proxy-load-balancing"
  lang="ru"
  question="Стоимость запросов сильно разнится, и один backend периодически уходит в паузу GC. С этим default-конфигом что произойдёт и какое однострочное изменение даёт наибольший рычаг?"
  choices={[
    { label: "Default upstream в nginx — это round-robin: он продолжает слать паузящему backend его справедливую долю вне зависимости от нагрузки, и тот отстаёт всё сильнее. Добавьте `least_conn;` (load-aware политика nginx), чтобы новые запросы шли на менее загруженные backends.", correct: true },
    { label: "Ничего не меняется — nginx по умолчанию использует least-connections и уже обходит медленный узел.", misconception: "Default в nginx — round-robin, а не least_conn. Без директивы он слеп к нагрузке и продолжает кормить паузящий backend." },
    { label: "Конфиг невалиден, потому что не объявлен алгоритм балансировки.", misconception: "Блок валиден; отсутствие директивы просто выбирает default round-robin. Проблема в том, что default слеп к нагрузке, а не в синтаксической ошибке." },
    { label: "Добавьте четвёртый backend — больше ёмкости это единственное реальное исправление для медленного узла.", misconception: "Добавление ёмкости не мешает round-robin маршрутизировать в паузящий узел. Исправление — это load-aware политика, а не сырая ёмкость." },
  ]}
/>

## Сниппет 2 — power-of-two-choices вручную

```python
import random

def pick_backend(backends):
    a, b = random.sample(backends, 2)        # два разных, равномерно случайно
    return a if a.active_conns <= b.active_conns else b
```

<Quiz
  id="09-proxy-load-balancing-code-2"
  lessonSlug="09-proxy-load-balancing"
  lang="ru"
  question="Это ядро power-of-two-choices. Почему выборка ровно двух — а не скан всех N ради истинного минимума — важна в продакшене?"
  choices={[
    { label: "Это O(1) вместо O(N), и случайность на каждый запрос означает, что разные клиенты сэмплируют разные пары — поэтому при восстановлении backend они не сбегаются на него все сразу (нет синхронного стада).", correct: true },
    { label: "Это просто least-connections со случайным разрешением ничьих; поведение идентично.", misconception: "Настоящий least-connections сканирует все N ради глобального минимума, что O(N) и синхронизирует всех клиентов на один восстановившийся узел. Выборка двух ломает и то и другое." },
    { label: "Выборка двух — только оптимизация CPU; распределение нагрузки хуже, чем при скане всех N.", misconception: "Выборка двух не просто дешевле — она даёт дисбаланс ~12% против ~100% у чистого random и рассинхронизирует стадо, чего скан всех N не делает." },
    { label: "Нужно использовать random.sample с возвращением, чтобы backend мог сравниваться сам с собой.", misconception: "Выбираются два РАЗНЫХ backend без возвращения; сравнение backend с самим собой было бы пустой операцией и сводило бы на нет шаг сравнения." },
  ]}
/>

## Сниппет 3 — заголовок PROXY protocol

```
PROXY TCP4 203.0.113.195 198.51.100.7 56324 443\r\n
GET /orders HTTP/1.1
Host: shop.example.com
```

<Quiz
  id="09-proxy-load-balancing-code-3"
  lessonSlug="09-proxy-load-balancing"
  lang="ru"
  question="Backend на чистом TCP (не HTTP) нуждается в реальном IP клиента для rate limiting. LB добавляет строку выше. Какое утверждение верно?"
  choices={[
    { label: "Строка PROXY вставляется на транспортном уровне до любых байт приложения, поэтому работает для любого TCP/UDP-протокола и её труднее подделать, чем X-Forwarded-For — клиент не может легко добиться, чтобы его поддельная строка пришла раньше строки доверенного proxy.", correct: true },
    { label: "Это то же самое, что X-Forwarded-For; оба — HTTP-заголовки, которые backend парсит одинаково.", misconception: "X-Forwarded-For — это HTTP-заголовок (уровень приложения); PROXY protocol — это преамбула транспортного уровня, работающая даже для не-HTTP протоколов вроде PostgreSQL или MQTT." },
    { label: "203.0.113.195 — это IP балансировщика, а 198.51.100.7 — клиента.", misconception: "Формат: `PROXY <proto> <src> <dst> <sport> <dport>`: 203.0.113.195 — это источник (клиент), а 198.51.100.7 — назначение, на которое подключился proxy." },
    { label: "Backend может доверять этому даже на напрямую доступном порту, ведь заголовок криптографически подписан.", misconception: "Заголовок PROXY не подписан. Backend должен принимать его только с известных IP доверенных proxy; напрямую доступному backend всё ещё можно скормить поддельную строку." },
  ]}
/>

## Сниппет 4 — graceful shutdown по SIGTERM

```go
func main() {
    srv := &http.Server{Addr: ":8080", Handler: mux}
    go srv.ListenAndServe()

    <-sigterm                                  // LB пометил нас как draining
    ctx, cancel := context.WithTimeout(context.Background(), 25*time.Second)
    defer cancel()
    srv.Shutdown(ctx)                          // перестать принимать, дослужить in-flight
}
```

<Quiz
  id="09-proxy-load-balancing-code-4"
  lessonSlug="09-proxy-load-balancing"
  lang="ru"
  question="Этот обработчик кооперируется с connection draining. Что делает srv.Shutdown и какой один продакшен-пробел тут важно отслеживать?"
  choices={[
    { label: "Он закрывает слушающий сокет (новых соединений нет) и даёт in-flight запросам дослужиться в пределах таймаута 25 с. Пробел: долгоживущие WebSocket/SSE-потоки могут превысить 25 с, поэтому и drain timeout у LB, И этот context должны быть рассчитаны на самое долгое соединение.", correct: true },
    { label: "Он немедленно принудительно закрывает все соединения, так что клиенты получают reset — для draining нужна отдельная библиотека.", misconception: "Shutdown работает аккуратно: перестаёт принимать и ждёт in-flight запросов до таймаута. Принудительное закрытие — это то, чего вы ИЗБЕГАЕТЕ, вызывая его." },
    { label: "Таймаут 25 с не важен, потому что временем draining полностью управляет LB.", misconception: "Важны оба. Если приложение выйдет раньше, чем LB перестанет маршрутизировать, клиенты получат reset; если drain timeout у LB короче этого context, LB всё равно принудительно закроет соединение в середине запроса. Их нужно согласовать." },
    { label: "Shutdown блокируется навсегда, пока не закроется каждое соединение, поэтому таймаут не нужен.", misconception: "Shutdown уважает context: на 25 с он возвращается, а оставшиеся соединения принудительно закрываются. Без таймаута зависший поток подвесил бы деплой бесконечно." },
  ]}
/>

<Recap lang="ru">
Проблемы LB живут в конфигах и обработчиках. Default в nginx — round-robin, поэтому load-aware политика вроде least_conn (или P2C в upstream) — это исправление для слепого к нагрузке пула. Power-of-two-choices — это две случайные выборки плюс одно сравнение: O(1) и рассинхронизирует стадо, а не просто более дешёвый least-connections. Преамбула PROXY protocol восстанавливает IP клиента для любого протокола транспортного уровня, но ей нужно доверять только с известных IP proxy. А graceful shutdown (закрыть слушатель, дослужить in-flight, таймаут) работает лишь когда окно draining приложения и drain timeout у LB рассчитаны вместе на самое долгоживущее соединение.
</Recap>
