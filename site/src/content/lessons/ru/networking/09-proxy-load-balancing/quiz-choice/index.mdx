---
concepts:
- power-of-two-choices
- l4-vs-l7
- health-checks
- session-affinity
- retry-storm
- anycast-ecmp
estMin: 14
lang: ru
level: senior
order: 7
prereqs: []
slug: quiz-choice
sources:
- https://www.haproxy.com/blog/layer-4-vs-layer-7-load-balancing/
- https://www.envoyproxy.io/docs/envoy/latest/intro/arch_overview/upstream/load_balancing/load_balancers.html
- https://learn.microsoft.com/en-us/azure/architecture/antipatterns/retry-storm/
status: ready
summary: "Синтез по всему юниту proxy и load balancing в формате выбора — компромиссы алгоритмов, L4 против L7, обнаружение здоровья, анти-паттерн sticky session, retry storm и отказоустойчивая топология."
title: 'Proxy и load balancing: тест с выбором ответа'
track: networking
unit: 09-proxy-load-balancing
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Шесть вопросов, проходящих через весь юнит. Каждый отражает решение, которое вы принимаете на реальной платформе — не определение для пересказа, а компромисс между скоростью, справедливостью и состоянием под нагрузкой.
</Hook>

<Goal lang="ru">
Убедитесь, что можете связать выбор алгоритма, L4 против L7, обнаружение здоровья, стратегию сессий и контроль каскада — тот синтез, к которому вели отдельные уроки.
</Goal>

<Quiz
  id="09-proxy-load-balancing-choice-1"
  lessonSlug="09-proxy-load-balancing"
  lang="ru"
  question="Пул из 50 backends обслуживает запросы с резко разной стоимостью (вызовы API на 1 мс вперемешку с загрузками на 200 мс). Какой алгоритм балансировки — правильный default и почему?"
  choices={[
    { label: "Power-of-two-choices: выбрать два случайных backend на запрос и направить на менее загруженный — O(1), дисбаланс ~12%, а случайность рассинхронизирует стадо при восстановлении медленного backend.", correct: true },
    { label: "Round-robin: он распределяет запросы идеально равномерно по всем 50 backends.", misconception: "Round-robin распределяет КОЛИЧЕСТВО запросов равномерно, но не нагрузку. Загрузка на 200 мс и вызов на 1 мс занимают по одному ходу, поэтому backend с загрузками отстаёт несмотря на свою справедливую долю." },
    { label: "Чистый least-connections: всегда направлять на backend с наименьшим числом активных соединений, тогда нагрузка идеально ровная.", misconception: "Least-connections адаптируется к нагрузке, но устраивает стадо: когда счётчик восстановившегося backend падает, все клиенты видят один минимум одновременно и сбегаются на него. Плюс ему нужен O(log N) скан на запрос." },
    { label: "IP-hash: хеширование IP клиента закрепляет каждого клиента за одним backend, распределяя нагрузку детерминированно.", misconception: "IP-hash — это инструмент session affinity, а не балансировки. Он плохо кластеризуется за NAT и полностью игнорирует реальную нагрузку." },
  ]}
/>

<Quiz
  id="09-proxy-load-balancing-choice-2"
  lessonSlug="09-proxy-load-balancing"
  lang="ru"
  question="Нужно направлять /api/payments/ в PCI-совместимый пул, а /api/catalog/ — в общий, делать retry на HTTP 5xx и собирать метрики по эндпоинтам. Какой слой нужен и сколько это стоит?"
  choices={[
    { label: "L7: он терминирует TLS и парсит HTTP-запрос, поэтому может маршрутизировать по path/host/header и понимает семантику 5xx — за ~100–500 мкс на запрос и 5–15% CPU против 1–5 мкс у L4.", correct: true },
    { label: "L4, потому что он обрабатывает весь TCP-трафик на скорости провода.", misconception: "L4 видит только 4-кортеж IP:port. Он не может прочитать path URL или отличить /payments/ от /catalog/ и не понимает HTTP 5xx, чтобы делать retry." },
    { label: "Любой слой — оба читают HTTP-заголовки, выберите более быстрый.", misconception: "L4 никогда не парсит HTTP-заголовки; это делает только L7. Content routing и retry на 5xx — возможности исключительно L7." },
    { label: "Ни тот ни другой — маршрутизация по path делается в DNS.", misconception: "DNS резолвит hostname, а не path URL. Маршрутизация по path требует L7-прокси, который парсит строку запроса." },
  ]}
/>

<Quiz
  id="09-proxy-load-balancing-choice-3"
  lessonSlug="09-proxy-load-balancing"
  lang="ru"
  question="У backend заполнился диск; он по-прежнему принимает TCP-соединения, но возвращает 500 на каждый HTTP-запрос. Ваш активный health check — это TCP SYN-проба, и она сообщает, что backend здоров. Какое исправление правильное?"
  choices={[
    { label: "Использовать HTTP GET /healthz, который задействует реальные зависимости, И включить passive outlier detection, чтобы всплески 5xx на живом трафике выбрасывали узел — вместе они покрывают и краши, и тихую деградацию.", correct: true },
    { label: "Сократить интервал TCP SYN-пробы до 1 секунды, чтобы поймать сбой быстрее.", misconception: "Более частая TCP SYN-проба всё равно проверяет лишь то, что TCP соединяется. Процесс ПРИНИМАЕТ соединения, поэтому проба сообщает «здоров» как бы часто вы ни пробовали." },
    { label: "Отключить health check и положиться на алгоритм балансировки, чтобы обходить медленный backend.", misconception: "Маршрутизация по наименьшей загрузке снижает трафик на медленный узел, но никогда не удаляет узел, отдающий мгновенные 500: его соединения закрываются быстро, поэтому он выглядит простаивающим и притягивает БОЛЬШЕ трафика." },
    { label: "Добавить тривиальный /healthz, всегда возвращающий 200, чтобы проба перестала флапать.", misconception: "Заглушка, всегда возвращающая 200, — это опасная противоположность исправлению: она маскирует сломанное приложение и продолжает направлять трафик в шторм 500-ок." },
  ]}
/>

<Quiz
  id="09-proxy-load-balancing-choice-4"
  lessonSlug="09-proxy-load-balancing"
  lang="ru"
  question="Команда включает sticky session, чтобы пользователи не разлогинивались, когда запросы попадают на разные backends. После деплоя дисбаланс нагрузки ухудшается, а упавший узел разлогинивает тысячи пользователей. Как это читать?"
  choices={[
    { label: "Sticky session — это обходной приём, закрепляющий клиента за одним backend: дисбаланс 1.5–2.5× и SPOF на уровне сессии. Настоящее исправление — вынести состояние сессии в Redis, чтобы любой backend мог обслужить любой запрос.", correct: true },
    { label: "TTL cookie-стикости слишком короткий; увеличьте его и дисбаланс исчезнет.", misconception: "Более длинный TTL cookie закрепляет клиентов ещё сильнее, ухудшая дисбаланс и делая потерю сессии при краше более вероятной, а не менее. Само закрепление и есть проблема." },
    { label: "Переключиться с cookie-based на IP-hash affinity для более сильного закрепления.", misconception: "IP-hash — это всё та же affinity: она сохраняет SPOF и добавляет кластеризацию за NAT и поломку при смене IP. Смена разновидности affinity не убирает дефект stateful-backend дизайна." },
    { label: "Sticky session сделали приложение отказоустойчивым; дисбаланс — несвязанная проблема ёмкости.", misconception: "Дисбаланс и массовый разлогин — прямые следствия закрепления. Sticky session снижают отказоустойчивость, создавая единую точку отказа на уровне сессии." },
  ]}
/>

<Quiz
  id="09-proxy-load-balancing-choice-5"
  lessonSlug="09-proxy-load-balancing"
  lang="ru"
  question="У backend пауза GC на 500 мс. Клиенты и три промежуточных слоя сервисов делают по одному retry при таймауте. Блип на 500 мс превращается в отказ всего кластера. Каков минимальный набор мер?"
  choices={[
    { label: "Circuit breaker, чтобы быстро отдавать 503 после N сбоев; глобальный retry budget (~10% от RPS), чтобы усиление оставалось ограниченным; и exponential backoff с jitter, чтобы рассинхронизировать всплеск retry.", correct: true },
    { label: "Добавить больше retry с более короткими таймаутами, чтобы запросы восстанавливались быстрее.", misconception: "Больше retry ещё сильнее усиливают перегрузку — по K слоям retry компаундируются к нагрузке 2^K×. Агрессивнее ретраить в тонущий backend — это именно то, что вызывает шторм." },
    { label: "Масштабировать пул backend в 4 раза, чтобы он поглотил нагрузку от retry.", misconception: "Ёмкость отсрочивает коллапс, но усиление retry масштабируется вместе с нагрузкой; новые узлы втягиваются в тот же шторм. Нужно ограничивать retry, а не просто добавлять ёмкость." },
    { label: "Убрать jitter, чтобы все клиенты ретраили по фиксированному расписанию и нагрузка была предсказуемой.", misconception: "Убрав jitter, вы синхронизируете каждого затаймаутившего клиента на retry в один момент — thundering herd. Jitter существует именно для размазывания всплеска." },
  ]}
/>

<Quiz
  id="09-proxy-load-balancing-choice-6"
  lessonSlug="09-proxy-load-balancing"
  lang="ru"
  question="Ваш единственный L7 load balancer сам является единой точкой отказа: когда он падает, весь трафик пропадает. Какая топология убирает SPOF и какое ограничение она накладывает?"
  choices={[
    { label: "Несколько LB, анонсирующих один anycast VIP через BGP ECMP, дают переключение менее чем за секунду, но LB должны быть фактически stateless (или использовать consistent hashing 5-кортежа в стиле Maglev), чтобы перехешированный поток мог попасть на любой выживший LB.", correct: true },
    { label: "Active-passive горячий резерв — он переключается мгновенно и без потери соединений.", misconception: "Переключение active-passive занимает 10–30 с на обнаружение и продвижение резерва, достаточно долго, чтобы пользователи заметили. Anycast + ECMP сходятся меньше чем за секунду на активном кластере." },
    { label: "DNS round-robin по IP-адресам LB — клиенты автоматически выберут живой.", misconception: "TTL DNS в 60–300 с означают, что клиенты продолжают бить в мёртвый LB, пока кэш не истечёт; у DNS нет осведомлённости о здоровье на уровне запроса. DNS делает гео-маршрутизацию, а не переключение LB." },
    { label: "Просто дать LB больше CPU и памяти, чтобы он никогда не падал.", misconception: "Вертикальное масштабирование не убирает единую точку отказа — машина, NIC, стойка или питание всё равно могут отказать. Резервирование на уровне нескольких машин — единственное исправление." },
  ]}
/>

<Recap lang="ru">
Сквозная линия юнита — один треугольник компромиссов: скорость, справедливость и состояние, разрешаемый на каждом слое. Power-of-two-choices — справедливый O(1)-default; L7 покупает content routing и retry на 5xx ценой латентности в 100× выше L4; активные плюс пассивные health check покрывают и краши, и тихие 5xx; sticky session — обход stateful-backend, который заменяется Redis; retry budget, circuit breaker и jitter ограничивают каскад; а anycast + BGP ECMP убирают собственную единую точку отказа LB ценой statelessness.
</Recap>
