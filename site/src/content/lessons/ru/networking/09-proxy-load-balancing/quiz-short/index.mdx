---
concepts:
- two-connection-model
- load-imbalance
- outlier-detection
- consistent-hashing
- retry-budget
- stateless-lb
estMin: 14
lang: ru
level: senior
order: 8
prereqs: []
slug: quiz-short
sources:
- https://www.nginx.com/blog/nginx-power-of-two-choices-load-balancing-algorithm/
- https://www.envoyproxy.io/docs/envoy/latest/intro/arch_overview/upstream/health_checking
- https://en.wikipedia.org/wiki/Consistent_hashing
status: ready
summary: "Промпты на свободное воспроизведение по всему юниту proxy и load balancing. Сначала ответьте своими словами, затем раскройте модельный ответ и сравните."
title: 'Proxy и load balancing: тест на воспроизведение'
track: networking
unit: 09-proxy-load-balancing
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";

<Hook>
Воспроизведение по памяти сильнее перечитывания. Для каждого промпта произнесите или запишите полный ответ из памяти, прежде чем открыть модельный — именно усилие припоминания закрепляет материал.
</Hook>

<Goal lang="ru">
Восстановите ключевые механизмы юнита — модель двух соединений, компромиссы алгоритмов, active против passive health, consistent hashing, retry budget и требование stateless LB — не подглядывая в уроки.
</Goal>

<RetrievalDrawer
  client:load
  id="09-proxy-load-balancing-short-retrieval"
  lang="ru"
  questions={[
    { q: "Почему L7 reverse proxy открывает два TCP-соединения на запрос вместо пересылки пакетов на третьем уровне?", a: "Чтобы маршрутизировать по HTTP — path, host, header, cookie — proxy должен распарсить запрос, а парсинг требует владения обеими сторонами разговора: клиентским соединением, чтобы прочитать запрос, и отдельным backend-соединением, чтобы его записать. NAT третьего уровня пересылает сырые пакеты без изменений и не может инспектировать или менять payload, поэтому маршрутизирует только по IP и порту. Модель двух соединений — это цена HTTP-aware маршрутизации, и именно поэтому backend видит IP proxy, а не клиента — что и восстанавливают X-Forwarded-For, RFC 7239 и PROXY protocol." },
    { q: "Сравните round-robin, least-connections и power-of-two-choices по стоимости, справедливости и стаду.", a: "Round-robin — O(1), но слеп к нагрузке: распределяет количество запросов, а не нагрузку, поэтому медленный backend тонет в своей справедливой доле. Least-connections маршрутизирует на наименьшее число активных соединений (адаптируется к нагрузке), но требует O(log N) скан и устраивает стадо: когда счётчик восстановившегося backend падает, все клиенты видят один минимум одновременно и сбегаются на него. Power-of-two-choices сэмплирует два случайных backend и маршрутизирует на менее загруженный — O(1), дисбаланс ~12% (Adler 1996) против ~100% у чистого random, а случайность на запрос рассинхронизирует стадо. Это продакшен-default в nginx, Envoy и AWS ALB." },
    { q: "Зачем нужны и active, и passive health check, и что каждый пропускает в одиночку?", a: "Active-проверки шлют пробы (HTTP GET /healthz, TCP SYN, gRPC) каждые 10–30 с и выбрасывают узел после 2–3 сбоев — они ловят полные отказы вроде упавшего процесса или сетевого раздела, но TCP SYN-проба пропускает приложение, которое принимает TCP, но возвращает 500 на каждый запрос, а тривиальный /healthz, всегда отдающий 200, маскирует сломанное приложение. Passive outlier detection наблюдает за реальным трафиком и выбрасывает узел после повторяющихся 5xx (Envoy: 5 подряд, выброс на 30 с с удвоением до ~300 с) — он ловит тихую деградацию вроде исчерпанного пула БД, но не покрывает периоды низкого трафика. Используйте оба: active — страховочная сеть, passive ловит частые режимы живых отказов." },
    { q: "Почему sticky session — анти-паттерн и какое правильное исправление? Сопоставьте с consistent hashing.", a: "Sticky session закрепляют каждого клиента за одним backend (через cookie или IP-hash), чтобы скрыть состояние сессии, хранящееся в памяти этого backend. Это даёт дисбаланс нагрузки 1.5–2.5×, потому что LB больше не может ребалансировать, и создаёт SPOF на уровне сессии: если закреплённый backend падает, сессия теряется без failover. Правильное исправление — вынести состояние сессии во внешнее хранилище (Redis) по ключу session ID, чтобы любой backend мог обслужить любой запрос, а LB свободно маршрутизировал через power-of-two-choices. Consistent hashing — другой инструмент, для локальности КЭША, а не пользовательского состояния: он отображает ключ на тот же backend с ремаппингом лишь ~1/N ключей при изменении состава (используя 150–300 виртуальных узлов на backend для выравнивания кольца), максимизируя cache hit по шардам." },
    { q: "Объясните усиление retry по слоям микросервисов и меры, которые его ограничивают.", a: "Когда backend перегружен, retry добавляют ту самую нагрузку, что вызвала перегрузку, поэтому он отстаёт ещё сильнее. При N клиентах × 1 retry это 2× нагрузки; по K слоям сервисов, каждый из которых ретраит раз, усиление в худшем случае приближается к 2^K (≈1024× при K=10). Меры: circuit breaker отдаёт 503 немедленно после N сбоев, чтобы backend не получал новой нагрузки и мог дренировать (состояния: closed → open → half-open); глобальный retry budget ограничивает суммарные retry на уровне ~10% от RPS, чтобы усиление оставалось ограниченным независимо от числа ретраящих клиентов; exponential backoff с jitter (delay = min(base·2^attempt + rand(0,base), max)) рассинхронизирует всплеск retry, избегая thundering herd; а load shedding отбрасывает новые запросы быстрым 503, когда глубина очереди превышает порог. Алертите на retry rate выше 0.1% как на самое раннее предупреждение о шторме." },
    { q: "Почему load balancer за anycast + BGP ECMP должен быть фактически stateless и как Maglev смягчает это?", a: "Anycast анонсирует один VIP со многих LB; ECMP хеширует 5-кортеж каждого потока на один из них, и когда LB падает, BGP отзывает его маршрут менее чем за секунду, а ECMP перехеширует затронутые потоки на выживших. Если LB хранил per-flow состояние (сессию TLS, состояние стрима HTTP/2) локально, перехешированный поток попадает на LB без памяти о нём, и клиенту приходится переподключаться. Поэтому LB должны быть stateless — каждое соединение самодостаточно — либо разделять состояние сессий в распределённом кэше. Maglev от Google смягчает это, используя consistent hashing 5-кортежа, чтобы тот же поток отображался на тот же LB даже при приходе и уходе машин: при удалении одного LB ремаппится лишь ~1/N потоков, остальные продолжаются без прерывания." },
  ]}
/>

<Recap lang="ru">
Если вы смогли восстановить каждый ответ по памяти, вы держите хребет юнита: модель двух соединений — это то, что делает возможными HTTP-маршрутизацию и health check; power-of-two-choices — справедливый O(1)-default, побеждающий и round-robin, и least-connections; active плюс passive health check вместе покрывают краши и тихие 5xx; sticky session — обход stateful-backend, который заменяется Redis (consistent hashing — для локальности кэша, не для сессий); retry budget, circuit breaker и jitter ограничивают каскад до 2× вместо 2^K; а statelessness — это то, что позволяет anycast + ECMP превратить сам LB из SPOF в отказоустойчивый кластер.
</Recap>
