---
concepts:
- OpenTelemetry
- traceparent
- span
- trace-propagation
- head-based-sampling
- tail-based-sampling
- USE-method
- RED-method
- error-budget
- SLO
- RUM
deepensInto:
- 07-resilience-and-cascades
estMin: 15
lang: ru
lessonType: topic
level: senior
order: 6
prereqs:
- 02-dns-tcp-tls-sequence
- 05-quic-websocket-paths
slug: 06-observability-and-traces
sources:
- https://opentelemetry.io/
- https://www.w3.org/TR/trace-context/
- https://web.dev/articles/vitals/
spiral:
- latency
- statefulness
status: ready
summary: Распространение W3C Trace Context через OpenTelemetry показывает, какой хоп
  «съел» 500 мс; методы USE и RED дисциплинируют инструментирование; head-based и
  tail-based семплирование управляют стоимостью захвата трейсов при 1 млн запросов/с.
title: 'Наблюдаемость: распределённые трейсы, USE/RED и семплирование'
track: networking
unit: 12-putting-it-together
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import TraceScenario from "~/components/pedagogy/TraceScenario.astro";
import DebugLog from "~/components/pedagogy/DebugLog.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
p95 задержки утроилась за ночь с 80 мс до 240 мс. У вас есть логи балансировщика нагрузки, приложения и базы данных. Три лога, три часовых пояса, три grep-сессии. Без распределённых трейсов выяснить «куда делись 160 мс» занимает часы. С OpenTelemetry — 30 секунд: найдите span, который вырос, и у вас есть подозреваемый.
</Hook>

<Crux>Распределённая трассировка распространяет единый trace ID через каждый хоп; span, который вырос, — это узкое место, без grep и догадок.</Crux>

<Explanation>

## OpenTelemetry и W3C Trace Context

До появления распределённой трассировки диагностика медленного запроса означала сопоставление временных меток из разных лог-файлов — процесс, подверженный ошибкам и плохо масштабируемый при десяти сервисах. Современная наблюдаемость инструментирует каждый сетевой хоп с помощью общего идентификатора. Стандарт **W3C Trace Context** определяет два HTTP-заголовка:

- **`traceparent`** (заголовок-родитель трейса, содержащий сквозной идентификатор запроса): `00-<16-байтовый trace-id>-<8-байтовый span-id>-<флаги>` — глобально уникальный trace ID и ID текущего span.
- **`tracestate`**: специфичные для вендора метаданные (Datadog trace ID, флаги Jaeger и т.д.).

Каждый компонент на пути запроса — браузер, CDN edge (через Cloudflare Workers), балансировщик нагрузки, сервер приложений, вызов к базе данных, вызов внешнего API — читает входящий `traceparent`, создаёт дочерний span с новым span-id, выполняет работу, отправляет данные о времени и передаёт тот же trace-id в заголовках исходящих запросов.

**Результат:** backend наблюдаемости (Tempo, Jaeger, Honeycomb, Datadog APM) может отобразить весь запрос как **каскад span** — каждый хоп, каждая зависимость, каждый запрос к БД, каждый вызов внешнего API — с точным временем начала и длительностью. Поиск «куда делись 500 мс» сводится к просмотру самого длинного span.

<DebugLog
  id="sr-debug-1"
  lessonSlug="06-observability-and-traces"
  lang="ru"
  title="Трейс OpenTelemetry для одного медленного запроса — найдите узкое место"
  output={`Трейс 7a8f3c... длительность 587мс

span: HTTP request                              0–587мс (587мс)
  span: DNS lookup                              0–4мс    (4мс)
  span: TCP connect                             4–32мс   (28мс)
  span: TLS handshake                           32–67мс  (35мс)
  span: HTTP request send                       67–69мс  (2мс)
  span: Server processing                       69–512мс (443мс)
    span: Auth middleware                       69–73мс  (4мс)
    span: Database query SELECT users           73–78мс  (5мс)
    span: Database query SELECT user_settings   78–82мс  (4мс)
    span: External API call third-party.com     82–489мс (407мс)
    span: Response serialisation                489–512мс (23мс)
  span: HTTP response receive                   512–587мс (75мс)`}
  outputLang="log"
  question="Весь запрос занимает 587 мс. Какой span является узким местом и что делать?"
  hint="Найдите единственный самый длинный span и подумайте, можно ли его сократить или перенести за пределы критического пути."
  answer="'External API call to third-party.com' занимает 407 мс — 69% всего запроса. Действия: (1) проверить, нужен ли вызов третьей стороны на этом пути кода — можно ли перенести его за пределы критического пути (async, фоновая задача, кэшированный результат)? (2) если обязателен, можно ли кэшировать ответ локально с коротким TTL? (3) если нужен свежий, можно ли распараллелить с другой независимой работой? Сетевые round-trips (DNS+TCP+TLS 67 мс, получение ответа 75 мс) суммарно 142 мс — не отлично, но не главная проблема. База данных быстрая. Auth middleware быстрый. Решение: кэшировать результат API третьей стороны на настраиваемый TTL или перенести его за пределы синхронного пути запроса. p95 падает с 587 мс до менее 200 мс."
/>

## Распространение через CDN edge

CDN edge-узлы (Cloudflare Workers, Fastly Compute, AWS CloudFront Functions) теперь поддерживают распространение трейсов. Когда запрос приходит на edge:
1. Edge читает `traceparent` из входящего запроса.
2. Создаёт edge-span со своим span-id.
3. Записывает время до первого байта от origin.
4. Передаёт `traceparent` (с оригинальным trace-id, span-id edge) на origin.

Результат: трейс показывает задержку CDN edge как отдельный span. Если edge span равен 5 мс, а origin span — 400 мс, понятно, что оптимизировать нужно origin, а не CDN. Без распространения трейсов видно только одно суммарное значение 405 мс и нужно угадывать разбивку.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="text-sm w-full border-collapse">
    <thead>
      <tr class="bg-slate-100">
        <th class="border px-3 py-2 text-left">Компонент</th>
        <th class="border px-3 py-2 text-left">Действие с заголовком трейса</th>
        <th class="border px-3 py-2 text-left">Что записывает</th>
      </tr>
    </thead>
    <tbody>
      <tr><td class="border px-3 py-1">Браузер</td><td class="border px-3 py-1">Генерирует корневой trace-id + span-id</td><td class="border px-3 py-1">Navigation timing (LCP, DNS, TCP, TLS)</td></tr>
      <tr><td class="border px-3 py-1">CDN edge worker</td><td class="border px-3 py-1">Читает traceparent, создаёт дочерний span</td><td class="border px-3 py-1">Попадание/промах кэша edge, RTT до origin</td></tr>
      <tr><td class="border px-3 py-1">Балансировщик нагрузки</td><td class="border px-3 py-1">Передаёт traceparent, записывает маршрутизацию</td><td class="border px-3 py-1">Выбор backend, время в очереди</td></tr>
      <tr><td class="border px-3 py-1">Сервер приложений</td><td class="border px-3 py-1">Читает, создаёт дочерний span на обработчик</td><td class="border px-3 py-1">Auth, бизнес-логика, запросы к БД</td></tr>
      <tr><td class="border px-3 py-1">Драйвер базы данных</td><td class="border px-3 py-1">Записывает запрос + план выполнения</td><td class="border px-3 py-1">Текст запроса, просмотренные строки, попадание в индекс</td></tr>
      <tr><td class="border px-3 py-1">Клиент внешнего API</td><td class="border px-3 py-1">Передаёт traceparent исходящим</td><td class="border px-3 py-1">Задержка зависимости, частота ошибок</td></tr>
    </tbody>
  </table>
</div>

## Операционные фреймворки USE и RED

Когда инструментируешь новый сервис, велик соблазн собирать всё. Два дисциплинированных фреймворка предотвращают «расползание метрик» — сбор 500 метрик без понимания, какие из них важны.

**Метод USE** (Utilization — утилизация, Saturation — насыщение, Errors — ошибки; Brendan Gregg) для ресурсов (CPU, память, диск, сетевые интерфейсы, пулы соединений):
- **U**tilization (Утилизация) — какая доля ресурса используется? (CPU 80%, пул соединений 95%)
- **S**aturation (Насыщение) — стоит ли работа в очереди из-за заполненности ресурса? (глубина очереди запросов, длина очереди run queue)
- **E**rrors (Ошибки) — ресурс даёт сбои? (TCP-ошибки, ошибки диска, OOM kills)

**Метод RED** (Rate — частота, Errors — ошибки, Duration — длительность; Tom Wilkie) для сервисов (API, микросервисы):
- **R**ate (Частота) — сколько запросов в секунду?
- **E**rrors (Ошибки) — какая доля возвращает ошибки?
- **D**uration (Длительность) — каково распределение задержки (p50, p95, p99)?

**Использование обоих вместе.** RED говорит *что* сломано (частота ошибок сервиса выросла). USE говорит *почему* (насыщение CPU вызвано CPU-bound обработчиком, или насыщение пула соединений из-за медленной БД). Вместе они сокращают MTTR с часов до минут.

## Стратегии семплирования

Трассировка каждого запроса при 1 млн req/s производит терабайты данных трейсов в день — это запретительно дорого. Две стратегии балансируют полноту и стоимость:

**Head-based семплирование.** Решение о трассировке принимается при входе запроса — фиксированный процент (например, 1% запросов). Дёшево и детерминировано: trace-id несёт решение о семплировании, распространяемое на все нижестоящие компоненты. Недостаток: большинство ошибок и медленных запросов случается в 99%, которые вы не трассировали. Для ваших самых серьёзных инцидентов у вас нет трейсов.

**Tail-based семплирование.** Буферизировать все span в памяти, принимать решение после получения результата запроса:
- 100% запросов с ошибочным статусом (4xx, 5xx)
- 100% запросов с длительностью > порога (p99 cutoff)
- 0,1% быстрых успешных запросов (базовый уровень)

Реализуется **OpenTelemetry Collector** с процессором `tail_sampling`. Недостаток: требует буферизации всех span на время окна принятия решения (обычно 30–60 с), используя память пропорционально запросам в полёте. При 1 млн req/s с окном 30 с — 30 млн span в памяти, управляемо при правильном шардировании.

**Адаптивное семплирование.** Динамически регулирует частоту семплирования в зависимости от нагрузки или времени суток. Во время инцидентов повышает до 100% для трейсов ошибок; в тихие периоды снижает до 0,01%.

**Правильный паттерн:** head-based для базовых данных устойчивого состояния (дёшево); tail-based для ошибок + медленных запросов (гарантирует трейсы там, где важно); адаптивное для управления стоимостью при переменной нагрузке.

<Inset kind="why" lang="ru">
Чисто head-based семплирование пропускает критические инциденты: 1% выборки вряд ли захватит редкий запрос к БД на 500 мс из 99%. Чисто tail-based запретительно дорого по памяти при высоком трафике без правильного шардирования. Комбинация — head для основной массы, tail для ошибок — обеспечивает покрытие значимых событий при приемлемой стоимости.
</Inset>

<TraceScenario
  id="sr-trace-tls"
  lessonSlug="06-observability-and-traces"
  lang="ru"
  scenario="Старший SRE получил пейджер: p95 задержки утроилась за ночь. Проследите диагностику с помощью распределённой трассировки."
  steps={[
    { prompt: "Шаг 1: какой дашборд вы открываете первым?", reveal: "Дашборд распределённых трейсов. Откройте медленный запрос за последний час и посмотрите на разбивку по span — какой span вырос по сравнению с вчерашним базовым уровнем?" },
    { prompt: "Шаг 2: трейс показывает 160 мс в span 'tls.handshake' на edge. Edge был нездоровым?", reveal: "Метрики edge (CPU, память, частота ошибок) в норме. Span TLS handshake вырос, потому что кэш возобновления сессий CDN был фактически очищен — деплой, скорее всего, ротировал STEK (Session Ticket Encryption Key) без сохранения предыдущего ключа в окне перекрытия." },
    { prompt: "Шаг 3: подтвердить метриками ALPN + resumption. tls_resumption_rate упал с 80% до 5%.", reveal: "Гипотеза подтверждена. Каждый запрос теперь выполняет полное TLS 1.3 рукопожатие вместо возобновления. 160 мс × объём трафика = наблюдаемое p95." },
    { prompt: "Шаг 4: немедленное смягчение?", reveal: "Переразвернуть с предыдущим STEK в наборе ротации, чтобы тёплые соединения могли возобновляться. Частота возобновления растёт по мере того, как старые session ticket становятся снова действительными. p95 должен упасть в течение 10–30 минут при прогреве кэшей." },
    { prompt: "Шаг 5: исправление в post-mortem?", reveal: "Задокументировать runbook ротации STEK: всегда держать минимум предыдущие 24 ключа в режиме только-дешифрования для непрерывности ticket. Добавить алерт мониторинга на tls_resumption_rate с порогом (например, падение ниже 60% на протяжении 10 мин вызывает PagerDuty)." },
  ]}
/>

<Quiz
  id="sr-q1"
  lessonSlug="06-observability-and-traces"
  lang="ru"
  question="Почему чисто head-based семплирование пропускает именно те инциденты, для которых трейсы нужны больше всего?"
  choices={[
    { label: "Head-based семплирование не распространяет trace ID на нижестоящие сервисы", misconception: "Trace ID распространяется; проблема в избирательности выборки, а не в распространении." },
    { label: "Ошибки и медленные запросы редки; 1% выборка вряд ли захватит их, поэтому при инциденте у вас нет трейса", correct: true },
    { label: "Head-based семплирование работает только для HTTP/1.1, но не HTTP/3", misconception: "Стратегия семплирования не зависит от транспортного протокола." },
    { label: "Tail-based семплирование захватывает 100% трафика, поэтому оно всегда строго лучше", misconception: "Tail-based имеет высокую стоимость по памяти; правильный паттерн — комбинация обоих." },
  ]}
/>

<Quiz
  id="sr-q2"
  lessonSlug="06-observability-and-traces"
  lang="ru"
  question="Метод USE применяется к ресурсам. Какой из вариантов правильно применяет USE к пулу соединений базы данных?"
  choices={[
    { label: "Rate = 500 запросов/с, Errors = 2%, Duration = p95 80 мс", misconception: "Это RED (для сервисов), а не USE (для ресурсов)." },
    { label: "Utilization = 95% соединений используются, Saturation = 200 запросов в очереди ожидания соединения, Errors = 0 таймаутов соединений/мин", correct: true },
    { label: "Throughput = 500 запросов/с, Latency = 80 мс, Availability = 99,9%", misconception: "Это метрики близкие к RED; USE конкретно: Utilization + Saturation + Errors." },
    { label: "CPU = 60%, Memory = 4 ГБ, Disk I/O = 200 IOPS", misconception: "Это отдельные метрики USE для ресурсов, а не USE применительно к пулу соединений." },
  ]}
/>

</Explanation>

<FlowDiagram
  label="Один и тот же trace-id из W3C traceparent распространяется вперёд через каждый хоп пути запроса: Браузер → CDN edge → балансировщик нагрузки → сервер приложений → база данных → внешний API. Каждый хоп читает входящий traceparent, создаёт дочерний span с новым span-id и передаёт оригинальный trace-id дальше"
  hue="var(--d-network)"
  perRow={6}
  nodes={[
    { id: "browser", label: "Браузер", sub: "корневой trace-id" },
    { id: "edge", label: "CDN edge", sub: "дочерний span" },
    { id: "lb", label: "Балансировщик", sub: "дочерний span" },
    { id: "app", label: "Сервер приложений", sub: "дочерний span" },
    { id: "db", label: "База данных", sub: "дочерний span" },
    { id: "ext", label: "Внешний API", sub: "дочерний span" },
  ]}
  edges={[
    { from: "browser", to: "edge", label: "traceparent" },
    { from: "edge", to: "lb", label: "тот же trace-id" },
    { from: "lb", to: "app", label: "тот же trace-id" },
    { from: "app", to: "db", label: "тот же trace-id" },
    { from: "app", to: "ext", label: "тот же trace-id" },
  ]}
  caption="Один trace-id сшивает каждый хоп в единый каскад span, поэтому поиск «куда делись 500 мс» сводится к нахождению самого длинного span — без grep по трём логам. Здесь узкое место — дочерний span внешнего API."
/>

<KeyTakeaway>W3C traceparent распространяет один trace ID через каждый хоп; самый длинный span всегда является узким местом; tail-based семплирование захватывает 100% ошибок без стоимости трассировки каждого запроса.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="06-observability-and-traces-retrieval"
  lang="ru"
  questions={[
    { q: "Объясните, почему распределённая трассировка требует в продакшне как head-based, так и tail-based семплирования.", a: "Head-based семплирование решает при входе запроса — дёшево и детерминировано, мало памяти. Но пропускает ошибки и медленные запросы в несемплированном большинстве. Tail-based семплирует 100% ошибок + 100% медленных запросов + малую долю быстрых успешных — но требует буферизации всех span на окно принятия решения (много памяти). Комбинация: head для базовых данных устойчивого состояния при низкой стоимости; tail для наиболее важных запросов (ошибки, медленные хвосты). OTel Collector реализует tail-based семплирование с настраиваемыми политиками." },
    { q: "Что определяет W3C Trace Context и как он распространяется через CDN edge?", a: "W3C Trace Context определяет HTTP-заголовок traceparent: 00-<16B trace-id>-<8B span-id>-<флаги>. CDN edge (например, Cloudflare Worker) читает входящий traceparent, создаёт дочерний span с новым span-id, записывает задержку edge, затем передаёт оригинальный trace-id со своим span-id как родителем в исходящем запросе к origin. Результат: один каскад трейса от браузера до origin и базы данных, с каждым хопом как отдельным span." },
    { q: "Чем метод USE отличается от RED и когда использовать каждый?", a: "USE (Utilization, Saturation, Errors) применяется к ресурсам — CPU, память, диск, пулы соединений. Отвечает на вопрос: является ли этот ресурс узким местом? RED (Rate, Errors, Duration) применяется к сервисам — API, микросервисам. Отвечает: работает ли этот сервис корректно и с какой задержкой? Используйте RED первым для обнаружения проблемы (частота ошибок выросла). Используйте USE для диагностики причины (насыщение CPU, исчерпан пул соединений). Вместе они сокращают MTTR." },
  ]}
/>

<Recap lang="ru">
OpenTelemetry и стандарт W3C Trace Context распространяют единый trace ID через каждый хоп в запросе — браузер, CDN edge, балансировщик нагрузки, приложение, базу данных — предоставляя каскад span, который делает вопрос «куда делись 500 мс» задачей на 30 секунд вместо многочасовой археологии логов. CDN edge-узлы (Cloudflare Workers, Fastly Compute) теперь участвуют в распространении трейсов, делая задержку edge измеримой как отдельный span. Метод USE инструментирует ресурсы; RED инструментирует сервисы — вместе они дисциплинируют сбор метрик, которые ведут к действиям. Семплирование при 1 млн req/s требует комбинирования head-based с tail-based семплированием, потому что чисто head-based пропускает именно те инциденты, для которых трейсы нужны больше всего. Теперь, когда видишь скачок p95, первое движение — открыть waterfall трейса, а не grep по логам: самый длинный span точно укажет, куда смотреть.
</Recap>
