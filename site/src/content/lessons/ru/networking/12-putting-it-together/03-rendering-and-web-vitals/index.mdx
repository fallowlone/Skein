---
concepts:
- critical-render-path
- LCP
- INP
- CLS
- preload-scanner
- render-blocking-CSS
- resource-hints
- fetchpriority
deepensInto:
- 06-observability-and-traces
estMin: 14
lang: ru
lessonType: topic
level: middle
order: 3
prereqs:
- 02-dns-tcp-tls-sequence
- 06-http-versions/04-headers-caching-cors
slug: 03-rendering-and-web-vitals
sources:
- https://web.dev/articles/vitals/
- https://web.dev/articles/lcp
- https://web.dev/articles/inp
- https://hpbn.co/
spiral:
- latency
- multiplexing
status: ready
summary: Как браузер превращает байты в пиксели — слияние DOM и CSSOM, CSS-блокировка
  рендеринга, preload scanner, откладывание JavaScript — и как LCP, INP и CLS измеряют
  пользовательский опыт этого конвейера.
title: Критический путь рендеринга и Core Web Vitals
track: networking
unit: 12-putting-it-together
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import TraceScenario from "~/components/pedagogy/TraceScenario.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
Сетевая часть загрузки страницы занимает 150 мс. LCP срабатывает через 3,2 секунды. Разрыв — это конвейер рендеринга браузера, и большая его часть вызвана вашими собственными решениями: какой CSS загружать, где разместить скрипты, есть ли у изображений размеры, какой JavaScript выполняется в главном потоке до отрисовки. Понимание этого конвейера — вторая половина оптимизации загрузки страницы.
</Hook>

<Crux>CSS блокирует отрисовку; JS блокирует разбор без defer; preload scanner спасает от обоих — если URL не скрыты за JavaScript.</Crux>

<Explanation>

## Критический путь рендеринга

После получения HTML-байтов рендеринг происходит в несколько перекрывающихся этапов:

1. **Разбор HTML → DOM.** HTML-парсер строит Document Object Model по мере поступления байтов. Разбор начинается до получения полного тела — потоковый разбор.
2. **Разбор CSS → CSSOM.** Когда браузер встречает тег `<link rel="stylesheet">`, он загружает CSS-файл и строит CSS Object Model. **Ничего не отрисовывается до готовности CSSOM** — CSS блокирует рендеринг. Почему? Показать страницу без стилей (FOUC — вспышка нестилизованного контента) и затем перерисовать её вызывает нестабильность макета, которая снижает воспринимаемую производительность. Это намеренно, а не ошибка.
3. **DOM + CSSOM → дерево рендеринга.** Браузер объединяет DOM и CSSOM в дерево рендеринга, содержащее только видимые элементы с вычисленными стилями.
4. **Компоновка (Layout).** Браузер вычисляет точное положение и размер каждого элемента.
5. **Отрисовка (Paint).** Браузер растеризует дерево рендеринга в пиксели.
6. **Композитинг (Compositing).** GPU-слои объединяются в финальный кадр.

JavaScript усложняет процесс: тег `<script>` без `async` или `defer` блокирует разбор HTML. Парсер останавливается, скрипт загружается и выполняется, затем разбор продолжается. Именно поэтому классические скрипты в `<head>` резко замедляют первую отрисовку.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="text-sm w-full border-collapse">
    <thead>
      <tr class="bg-slate-100">
        <th class="border px-3 py-2 text-left">Тип ресурса</th>
        <th class="border px-3 py-2 text-left">Блокирует разбор HTML?</th>
        <th class="border px-3 py-2 text-left">Блокирует рендеринг?</th>
        <th class="border px-3 py-2 text-left">Решение</th>
      </tr>
    </thead>
    <tbody>
      <tr><td class="border px-3 py-1">CSS (stylesheet)</td><td class="border px-3 py-1">Нет</td><td class="border px-3 py-1">Да</td><td class="border px-3 py-1">встроить критический CSS; preload</td></tr>
      <tr><td class="border px-3 py-1">JS (classic, в &lt;head&gt;)</td><td class="border px-3 py-1">Да</td><td class="border px-3 py-1">Да</td><td class="border px-3 py-1">добавить defer или async</td></tr>
      <tr><td class="border px-3 py-1">JS (async)</td><td class="border px-3 py-1">Частично</td><td class="border px-3 py-1">Частично</td><td class="border px-3 py-1">выполняется по готовности, не по порядку</td></tr>
      <tr><td class="border px-3 py-1">JS (defer)</td><td class="border px-3 py-1">Нет</td><td class="border px-3 py-1">Нет</td><td class="border px-3 py-1">выполняется после разбора HTML, по порядку</td></tr>
      <tr><td class="border px-3 py-1">Изображения (без размеров)</td><td class="border px-3 py-1">Нет</td><td class="border px-3 py-1">Вызывает CLS</td><td class="border px-3 py-1">добавить атрибуты width/height</td></tr>
      <tr><td class="border px-3 py-1">Шрифты (WOFF2)</td><td class="border px-3 py-1">Нет</td><td class="border px-3 py-1">Вызывает FOIT/FOUT</td><td class="border px-3 py-1">preload + font-display:swap</td></tr>
    </tbody>
  </table>
</div>

## Preload scanner

Браузер запускает лёгкий HTML-токенизатор, называемый **preload scanner**, опережая основной парсер. Он ищет URL в `<link>`, `<script>` и `<img>` и отправляет запросы до того, как основной парсер обработает эти теги. Без него обнаружение подресурсов было бы последовательным — каждый ресурс загружается только после того, как основной парсер достигает соответствующего тега.

Preload scanner выводится из строя:
- JavaScript, записывающим `<script src="...">` через `document.write`.
- Сложной клиентской маршрутизацией, которая формирует URL после инициализации фреймворка.
- Схемами ленивой загрузки, скрывающими URL за атрибутами `data-src`.

Современные фреймворки (Next.js, Astro, SvelteKit) автоматически генерируют `<link rel="preload">` для компенсации.

## Resource hints

| Подсказка | Эффект | Стоимость |
|-----------|--------|-----------|
| `<link rel="dns-prefetch" href="//cdn.example.com">` | Разрешает DNS в фоне | Минимальная |
| `<link rel="preconnect" href="//cdn.example.com" crossorigin>` | DNS + TCP + TLS в фоне | Один простаивающий сокет на источник |
| `<link rel="preload" as="font" crossorigin>` | Загружает шрифт до достижения тега парсером | Пропускная способность |
| `<link rel="modulepreload">` | Загружает ES-модуль + его импорты | Пропускная способность |
| `<link rel="prefetch">` | Загружает ресурс следующей навигации (низкий приоритет) | Пропускная способность в простое |

**Атрибут fetchpriority.** Можно переопределить приоритет ресурсов по умолчанию:
- `<img src="hero.jpg" fetchpriority="high">` — hero-изображение загружается раньше прочих.
- `<script src="analytics.js" fetchpriority="low">` — некритический JS откладывается ниже CSS и шрифтов.

## Core Web Vitals

Core Web Vitals от Google количественно оценивают воспринимаемую производительность. **Ранжирование в поиске использует полевые данные RUM** (измерения реальных пользователей через Chrome UX Report), а не синтетические оценки Lighthouse.

**LCP — Largest Contentful Paint.** Когда самый большой видимый элемент завершает рендеринг. Обычно это hero-изображение или основной заголовок. Хорошо: менее 2,5 с. Плохо: более 4 с. Основные причины плохого LCP: медленный TTFB сервера, ресурсы, блокирующие рендеринг и задерживающие главное изображение, большие неоптимизированные файлы изображений.

**INP — Interaction to Next Paint** (заменил FID в марте 2024). Наибольшая задержка между любым вводом пользователя (клик, касание, нажатие клавиши) и результирующим визуальным обновлением на протяжении всего жизненного цикла страницы. Хорошо: менее 200 мс. Плохо: более 500 мс. Основная причина: долгие задачи в главном потоке, блокирующие UI.

**CLS — Cumulative Layout Shift.** Неожиданное смещение видимых элементов во время загрузки страницы. Оценка: сумма (доля воздействия × доля расстояния) для каждого неожиданного смещения. Хорошо: менее 0,1. Основные причины: изображения без объявленных размеров, реклама, вставляющая контент, перетекание текста из-за веб-шрифтов.

<Inset kind="why" lang="ru">
Google использует **полевые данные RUM** (измерения реальных пользователей из Chrome UX Report) для ранжирования, а не Lighthouse. Страница с оценкой 95 в Lighthouse на вашем ноутбуке может получить 55 в CrUX, если реальные пользователи работают на медленных Android-телефонах в слабой сети. Оптимизируйте для поля, а не лаборатории.
</Inset>

<Quiz
  id="mid-q1"
  lessonSlug="03-rendering-and-web-vitals"
  lang="ru"
  question="Почему CSS по умолчанию блокирует рендеринг?"
  choices={[
    { label: "Браузеры не могут разбирать CSS параллельно с HTML", misconception: "Современные браузеры разбирают CSS очень быстро; блокировка — это решение о корректности, а не ограничение производительности." },
    { label: "Показ страницы без стилей (FOUC) и последующая перерисовка вызывают нестабильность макета, ухудшающую воспринимаемую производительность", correct: true },
    { label: "Это ошибка в старых браузерах, которую так и не исправили", misconception: "Это намеренное поведение во всех основных движках." },
    { label: "CSS должен загрузиться до начала разбора HTML", misconception: "Разбор HTML начинается немедленно; CSS блокирует только рендеринг, а не разбор." },
  ]}
/>

<TraceScenario
  id="mid-trace-render"
  lessonSlug="03-rendering-and-web-vitals"
  lang="ru"
  scenario="Проследите, что делает браузер во время потоковой передачи HTML — от первого байта до LCP."
  steps={[
    { prompt: "Прибывают первые байты HTML. Что браузер делает немедленно?", reveal: "Запускает HTML-парсер И preload scanner параллельно. Парсер постепенно строит DOM; preload scanner заглядывает вперёд в поисках URL в <link>, <script>, <img>, чтобы отправить запросы до их достижения парсером." },
    { prompt: "Парсер встречает <link rel=stylesheet href=main.css>. Разбор HTML продолжается. А рендеринг?", reveal: "Браузер загружает main.css. Разбор HTML продолжается (CSS не блокирует разбор). Рендеринг заблокирован — ничего не отрисовывается до готовности CSSOM. Если main.css занимает 200 мс, LCP задерживается на 200 мс." },
    { prompt: "Парсер встречает <script defer src=app.js>. Что происходит?", reveal: "Браузер загружает app.js (preload scanner мог уже начать это). Атрибут defer означает: не блокировать разбор; выполнить скрипт после полного разбора HTML. Главный поток не заблокирован." },
    { prompt: "CSS загружен. CSSOM построен. Дерево рендеринга создано. Браузер отрисовывает первый кадр. Какая метрика срабатывает?", reveal: "First Contentful Paint (FCP) срабатывает, когда отрисовывается любой текст или изображение. LCP срабатывает, когда отрисовывается самый большой видимый элемент (hero-изображение, основной заголовок) — это может быть тот же кадр или позже, если hero-изображение прибывает после первой отрисовки." },
    { prompt: "Hero-изображение загружается через 400 мс после HTML. Срабатывает LCP. Что определило, был ли LCP хорошим или плохим?", reveal: "LCP = TTFB + время загрузки hero-изображения + время рендеринга. Если TTFB был 150 мс, hero-изображение 400 мс (сеть) + 20 мс (декодирование/компоновка), LCP ≈ 570 мс — хорошо. Если hero-изображение не было предзагружено и preload scanner его пропустил (потому что оно динамически вставлено JavaScript), LCP может превысить 2 секунды — плохо." },
  ]}
/>

<DragOrder
  id="mid-drag-1"
  lessonSlug="03-rendering-and-web-vitals"
  lang="ru"
  prompt="Упорядочьте эти вмешательства по влиянию на LCP (от наибольшего к наименьшему, при условии что LCP — это hero-изображение):"
  items={[
    "Кэшировать HTML на CDN edge, чтобы TTFB снизился с 300 мс до 20 мс",
    "Добавить <link rel=preload as=image> для hero-изображения, чтобы оно загружалось параллельно с CSS",
    "Конвертировать hero-изображение из JPEG в WebP (файл на 30% меньше)",
    "Добавить fetchpriority=high к тегу hero <img>",
    "Убрать блокирующий рендеринг скрипт аналитики из <head> (перенести в defer)",
  ]}
/>

<Quiz
  id="mid-q2"
  lessonSlug="03-rendering-and-web-vitals"
  lang="ru"
  question="Что измеряет INP, чего не делал FID?"
  choices={[
    { label: "Время от загрузки страницы до первого взаимодействия пользователя", misconception: "Это ближе к TTI (Time to Interactive)." },
    { label: "Наибольшую задержку между любым вводом пользователя и результирующим визуальным откликом на протяжении всего жизненного цикла страницы", correct: true },
    { label: "Количество смещений макета при загрузке", misconception: "Это CLS." },
    { label: "Время блокировки главного потока", misconception: "Total Blocking Time (TBT) измеряет блокировку главного потока; INP — это воспринимаемая пользователем задержка ввода." },
  ]}
/>

</Explanation>

<FlowDiagram
  label="Критический путь рендеринга превращает HTML-байты в пиксели в шесть этапов: разбор HTML строит DOM, разбор CSS строит CSSOM, оба сливаются в дерево рендеринга, затем layout позиционирует элементы, paint их растеризует, композитинг собирает кадр, а LCP срабатывает при отрисовке самого большого элемента"
  hue="var(--d-network)"
  perRow={7}
  nodes={[
    { id: "html", label: "Разбор HTML", sub: "→ DOM" },
    { id: "css", label: "Разбор CSS", sub: "→ CSSOM" },
    { id: "tree", label: "Дерево рендеринга", sub: "DOM + CSSOM" },
    { id: "layout", label: "Layout", sub: "позиция + размер" },
    { id: "paint", label: "Paint", sub: "растеризация" },
    { id: "composite", label: "Композитинг", sub: "GPU-слои" },
    { id: "lcp", label: "LCP", sub: "наибольший отрисован" },
  ]}
  edges={[
    { from: "html", to: "css" },
    { from: "css", to: "tree" },
    { from: "tree", to: "layout" },
    { from: "layout", to: "paint" },
    { from: "paint", to: "composite" },
    { from: "composite", to: "lcp" },
  ]}
  caption="CSS блокирует рендеринг: дерево рендеринга не строится до готовности CSSOM, поэтому ничего не отрисовывается до прибытия CSS. Блокирующий рендеринг JavaScript останавливает этап разбора HTML. LCP — момент завершения отрисовки самого большого видимого элемента."
/>

<KeyTakeaway>CSS блокирует отрисовку до загрузки; откладывайте весь некритический JS; предзагружайте hero-изображение; добавляйте размеры всем изображениям. Эти четыре правила предотвращают четыре наиболее распространённых сбоя LCP и CLS.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="03-rendering-and-web-vitals-retrieval"
  lang="ru"
  questions={[
    { q: "Почему preload scanner критичен для быстрой загрузки страниц и что выводит его из строя?", a: "Preload scanner — это лёгкий HTML-токенизатор, работающий опережая основной парсер, для обнаружения URL в <link>, <script> и <img> и отправки запросов до завершения разбора. Без него запросы подресурсов начинались бы только после достижения каждого тега основным парсером — последовательное обнаружение сети. Preload scanner выводится из строя JavaScript, который формирует URL ресурсов во время выполнения (например, document.write src скрипта), сложной клиентской маршрутизацией и схемами ленивой загрузки, скрывающими URL за data-атрибутами." },
    { q: "Что вызывает CLS и как это исправить?", a: "CLS вызывается контентом, смещающимся после отрисовки: изображения без объявленных width/height (браузер не резервирует пространство, перекомпоновывает при загрузке), реклама, вставляющая новый контент выше существующего текста, перетекание текста из-за веб-шрифтов. Решение: добавить явные width+height всем изображениям, резервировать место для рекламы через контейнеры фиксированной высоты, использовать font-display:swap и предзагружать шрифты для уменьшения FOIT/FOUT." },
    { q: "Google ранжирует страницы по полевым данным RUM, а не Lighthouse. Какое практическое значение это имеет?", a: "Ваша оценка Lighthouse на быстром ноутбуке может не отражать реальный опыт пользователей на медленных Android-телефонах с 3G. CrUX Google (Chrome UX Report) агрегирует полевые измерения от реальных пользователей Chrome. Страница может получить 95 в Lighthouse и 55 в CrUX. Для улучшения ранжирования измеряйте LCP/INP/CLS в поле с помощью RUM-инструментов (SpeedCurve, Sentry, Vercel Analytics), а не только синтетическими прогонами Lighthouse." },
  ]}
/>

<Recap lang="ru">
Критический путь рендеринга браузера преобразует байты в пиксели через шесть этапов: разбор HTML, разбор CSS, построение дерева рендеринга, компоновка, отрисовка и композитинг. CSS блокирует рендеринг — ничего не отрисовывается до готовности CSSOM — делая его ресурсом с наивысшим приоритетом доставки. JavaScript без defer или async блокирует разбор HTML, задерживая всё. Preload scanner спасает, отправляя запросы подресурсов опережая основной парсер, но выводится из строя динамически внедрёнными URL. Core Web Vitals (LCP, INP, CLS) измеряют восприятие пользователем этого конвейера; Google использует полевые данные RUM (реальные пользователи Chrome), а не оценки Lighthouse для ранжирования. Четыре наиболее эффективных улучшения: CDN-кэш HTML (снизить TTFB), предзагрузить hero-изображение, отложить некритический JS и добавить размеры изображениям.
</Recap>
