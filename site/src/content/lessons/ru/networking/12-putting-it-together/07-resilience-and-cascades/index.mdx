---
concepts:
- retry-storm
- cascading-failure
- circuit-breaker
- exponential-backoff
- jitter
- bulkhead
- load-shedding
- error-budget
- SLO
- cell-based-architecture
- capacity-planning
deepensInto: []
estMin: 16
lang: ru
lessonType: topic
level: senior
order: 7
prereqs:
- 04-proxy-security-gates
- 05-quic-websocket-paths
- 06-observability-and-traces
- 09-proxy-load-balancing/06-retry-storms-and-circuit-breakers
slug: 07-resilience-and-cascades
sources:
- https://datatracker.ietf.org/doc/html/rfc9110
- https://opentelemetry.io/
- https://www.cloudflare.com/en-gb/learning/ddos/
spiral:
- statefulness
- latency
- encapsulation
status: ready
summary: Нескоординированные повторы на всех двенадцати уровнях превращают медленный
  сервис в общесайтовый отказ. Circuit breakers, exponential backoff с jitter, bulkheads
  и error budget — многоуровневая защита от каскадов.
title: 'Устойчивость: каскадные повторы, circuit breakers и error budget'
track: networking
unit: 12-putting-it-together
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import TraceScenario from "~/components/pedagogy/TraceScenario.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import DesignPrompt from "~/components/pedagogy/DesignPrompt.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";
import Infographic from "~/components/diagram/Infographic.astro";
import retryAmplification from "./infographics/retry-amplification.svg?raw";

<Hook>
Сторонний платёжный API замедляется до 5 секунд на ответ. Ваш сервер ждёт, получает таймаут, возвращает 503. Клиентская библиотека повторяет через 100 мс, 200 мс, 400 мс. Так делают ещё 1 000 пользователей. Ваш origin-сервер, обычно обрабатывающий 500 req/s, теперь получает 3 000 req/s — и всё ещё ждёт медленный платёжный API. Через 30 секунд весь сайт недоступен. Платёжный API был медленным, а не сломанным. Но вы превратили «медленно» в «катастрофу», повторяя запросы без координации.
</Hook>

<Crux>Нескоординированные повторы превращают один медленный сервис в общий отказ; circuit breakers и jitter разрывают петлю усиления.</Crux>

<Explanation>

## Анатомия каскада

Когда добавляешь retry-политику, делаешь ставку: downstream восстановится быстрее, чем клиенты усилят нагрузку. Если ставка проигрывает — получаешь retry storm. **Retry storm** — это положительная обратная связь: один таймаут → много повторов → больше нагрузки → больше таймаутов → больше повторов.

**Математика усиления.** Origin-сервер с 99,9% доступностью (1 ошибка на 1 000 запросов) в норме справляется. Во время замедления запросы скапливаются в очереди. 1 000 клиентов каждый повторяет 3 раза с наивным backoff. Трафик к origin: 3× нормы. Origin замедляется больше. Больше повторов. Через 30 секунд origin получает 10× нормального трафика. Сервер с 99,9% доступностью теперь узкое место для 100% трафика. **Один деградировавший зависимый сервис превращает 0,1% ошибок в 100% отказ.**

<Infographic
  svg={retryAmplification}
  label="Столбчатая диаграмма нагрузки на origin как кратности нормы: норма — 1×, первая нескоординированная волна повторов — 3×, а пик каскада через тридцать секунд — 10×, что превышает мощность сервера с 99,9%."
  caption="Повторы не просто добавляют нагрузку — они её умножают: 1× нормы вырастает до 3× на первой волне и 10× на пике каскада за тридцать секунд, поэтому сервер с 99,9% в итоге возвращает 100% ошибок."
  hue="var(--d-infra)"
/>

**Проблема двенадцати уровней.** Каждый уровень в цепочке запросов имеет свою логику повторов:
- DNS-клиент повторяет при SERVFAIL (3 попытки, каждые 2 с).
- TCP SYN повторяет при таймауте (экспоненциальный backoff, до 3 попыток).
- TLS handshake повторяет при таймауте (1–2 попытки).
- HTTP-клиентская библиотека повторяет при 5xx (3 попытки, экспоненциальный backoff по умолчанию).
- Повтор на уровне приложения (бизнес-логика повторяет неудачные вызовы сервисов).

**Все двенадцать уровней повторяют независимо.** Единственный сбой на уровне 6 (обработка origin) вызывает повторы на уровнях 3–12 одновременно. В совокупности один неудачный запрос становится 30+ сетевыми операциями — все они усугубляют нагрузку на и без того перегруженный origin.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="text-sm w-full border-collapse">
    <thead>
      <tr class="bg-slate-100">
        <th class="border px-3 py-2 text-left">Защита</th>
        <th class="border px-3 py-2 text-left">Что предотвращает</th>
        <th class="border px-3 py-2 text-left">Стоимость</th>
      </tr>
    </thead>
    <tbody>
      <tr><td class="border px-3 py-1">Exponential backoff + jitter</td><td class="border px-3 py-1">Синхронизированный thundering herd при повторах</td><td class="border px-3 py-1">Медленнее восстановление для отдельных клиентов</td></tr>
      <tr><td class="border px-3 py-1">Circuit breaker</td><td class="border px-3 py-1">Повторы к отказывающему зависимому сервису</td><td class="border px-3 py-1">Быстрые ошибки при открытом circuit; нужна настройка</td></tr>
      <tr><td class="border px-3 py-1">Bulkhead</td><td class="border px-3 py-1">Голодание всех потоков из-за одного медленного сервиса</td><td class="border px-3 py-1">Пул потоков на зависимость; сложность</td></tr>
      <tr><td class="border px-3 py-1">Load shedding</td><td class="border px-3 py-1">Таймауты из-за глубины очереди</td><td class="border px-3 py-1">Быстрые ошибки 503 для избыточного трафика</td></tr>
      <tr><td class="border px-3 py-1">Graceful degradation</td><td class="border px-3 py-1">Полный отказ при недоступности origin</td><td class="border px-3 py-1">Устаревшие данные; сложный дизайн кэша</td></tr>
    </tbody>
  </table>
</div>

## Защита 1: Exponential backoff с jitter

**Наивный повтор.** Клиент видит 503, повторяет немедленно. Сервер всё ещё сломан. Клиент видит 503, повторяет снова. Сервер никогда не восстанавливается.

**Экспоненциальный backoff.** Первый повтор через 100 мс, второй через 200 мс, третий через 400 мс, четвёртый через 800 мс (удвоение каждый раз, ограниченное максимумом). Origin получает время для восстановления между повторами.

**Почему jitter необходим.** Если 1 000 клиентов все достигают предела и все используют одни параметры backoff, они все повторяют в t=100 мс, t=200 мс и т.д. одновременно. Origin получает всплеск каждые 100 мс — **thundering herd**. Jitter: добавить ±25% случайной вариации к каждой задержке повтора. 1 000 клиентов распределяют повторы по окну 200 мс. Нагрузка на origin постоянная, а не импульсная.

**Формула полного jitter (Jeff Atwood / Amazon):** `sleep = random_between(0, min(cap, base * 2^attempt))`. Это даёт до cap мс сна на последней попытке, полностью рандомизированный — наиболее эффективный против thundering herd.

## Защита 2: Circuit breaker

Паттерн из электротехники: переключатель, который размыкается при превышении тока порога, предотвращая каскадный ущерб.

**Три состояния:**
1. **Closed (замкнутый)** (норма). Запросы проходят. Мониторится частота ошибок и задержка.
2. **Open (разомкнутый)** (сработавший). Частота ошибок или задержка превысила порог для последних N запросов. Новые запросы немедленно отказывают (быстрая ошибка, без сетевого вызова). Сервер не опрашивается.
3. **Half-open (полуоткрытый)** (зонд). После окна остывания (например, 30 с) один запрос пропускается. Если успешен, circuit закрывается (норма). Если неуспешен, circuit снова размыкается.

Эти три состояния вместе означают: origin получает передышку в момент обнаружения сбоя, а не после того, как каждый клиент исчерпает свой retry-бюджет. Без состояния Open origin продолжают нагружать до самовосстановления; без Half-open у него нет шанса доказать, что он восстановился.

**Почему быстрый отказ помогает.** Если circuit открыт и возвращает 503 за &lt;1 мс, клиент завершается быстро. С exponential backoff + jitter клиент ждёт и повторяет. Origin получает облегчение. Когда origin восстанавливается, полуоткрытый зонд успешен и трафик возобновляется. Без circuit breaker клиент продолжает повторять секундами — добавляя нагрузку на и без того перегруженный origin.

**Реализация.** Библиотеки: Netflix Hystrix (Java, deprecated), Resilience4j (Java), Polly (.NET), opossum (Node.js). Конфигурация: порог отказов (например, 50% ошибок за последние 10 запросов), остывание half-open (30 с), размер скользящего окна.

## Защита 3: Bulkhead

Назван по аналогии с переборками корабля, изолирующими затопление в одной секции. В ПО: давать каждому зависимому сервису **отдельный пул потоков** (или async семафор). Если платёжный API медленный, он заполняет пул потоков платежей. Пулы потоков user API, product API и CDN-fetch остаются доступными. Медленный зависимый сервис не голодит все потоки.

**Без bulkheads.** Общий пул потоков из 100. Платёжный API замедляется → 100 запросов в очереди → все 100 потоков заняты → каждый API endpoint (user, product, search) заблокирован. Сайт недоступен для всех функций, а не только для платежей.

**С bulkheads.** Платежи: 20 потоков. Пользователи: 30 потоков. Продукты: 30 потоков. Прочее: 20 потоков. Платёжный API замедляется → 20 потоков платежей заняты → платёжный endpoint медленный → все остальные endpoints не затронуты. Graceful частичная деградация.

## Защита 4: Load shedding

Когда очередь запросов превышает мощность сервера для обработки, **сбрасывайте излишек** с быстрым 503 вместо того, чтобы ждать таймаутов.

**Почему быстрый 503 лучше таймаута.** Запрос, в итоге завершившийся таймаутом через 30 с, удерживает поток, соединение с БД и память 30 с. Запрос, получивший 503 за &lt;1 мс, немедленно освобождает все ресурсы. Клиент может повторить с backoff. Сервер остаётся стабильным.

**Реализация.** Nginx: `limit_req_zone` + `limit_req` (на основе скорости, возвращает 503 при заполнении очереди). В приложении: проверять глубину очереди запросов; если > порога, возвращать 503 немедленно. Сочетать с **stale-while-revalidate** на CDN, чтобы кэшированный контент всё ещё обслуживался с edge при перегрузке origin.

## Error budget и SLO-ориентированный release cadence

**SLO (Service Level Objective — целевой показатель уровня обслуживания).** SLO на 99,9% доступности за 30-дневное окно означает, что сервис может иметь 0,1% ошибок — около 43,2 минут простоя. SLO 99,95% допускает ~21,6 минут.

**Error budget** (бюджет ошибок — допустимое время отказа за период). Допустимое время отказов. Когда бюджет сжигается:
- При 50% использовании: замедлить рискованные изменения (релизы функций, изменения конфигурации).
- При 100% использовании: заморозить все несущественные релизы до улучшения надёжности.

Это формализует компромисс задержка/скорость выпуска без политики. Инженеры могут выпускать быстро, пока есть бюджет; эксплуатация может применять заморозку релизов, когда бюджет исчерпан.

**Performance budgets.** Распространить ту же идею на загрузку страницы: установить явные цели (LCP &lt; 2,5 с, бандл JS &lt; 200 КБ). Применять в CI инструментами типа bundlewatch, size-limit, Lighthouse CI. Сборка, превышающая бюджет, падает в pipeline. Заставляет делать осознанные компромиссы при PR вместо обнаружения раздутости в продакшне.

<Inset kind="why" lang="ru">
Error budget разрешает классическое противоречие надёжность-скорость. Вместо споров инженерной команды и продукта «можно ли выпустить это рискованное изменение», error budget отвечает математически: если у вас осталось 30 минут бюджета в этом месяце, изменение с 20% вероятностью инцидента на 5 минут стоит риска (ожидаемая стоимость: 1 минута); изменение с 20% вероятностью инцидента на 45 минут — нет (ожидаемая стоимость: 9 минут > бюджет 30 минут). Количественный риск заменяет мнение.
</Inset>

<TraceScenario
  id="sr-trace-cascade"
  lessonSlug="07-resilience-and-cascades"
  lang="ru"
  scenario="Каскадный retry storm от DDoS-атаки с усилением: как единственный отказ на уровне 11 каскадирует через все двенадцать уровней."
  steps={[
    { prompt: "Уровень 11 (Network Security): Ботнет запускает объёмный DDoS 1 Гбит/с (UDP flood) на origin. Как реагирует rate limiter?", reveal: "Rate limiter на edge (token bucket) сбрасывает пакеты с IP атакующего. Для легитимных клиентов — без влияния. Но UDP-пакеты атакующего потребляют входящую полосу. Если не очищены выше, DDoS-трафик достигает сетевого интерфейса origin, потребляя backhaul-мощность." },
    { prompt: "Уровень 10 (QUIC): Атака переключается на усиление с поддельными IP. Ограничение QUIC против усиления?", reveal: "У QUIC есть ограничения против усиления: сервер не может отправить больше байт, чем получил в первом RTT. Если атакующий подделывает свой IP, QUIC-сервер не ответит усиленными ответами. Но легитимные клиенты видят ретрансмиты пакетов, которых не просили. Срабатывает логика TCP retransmit timeout → клиенты повторяют тот же запрос." },
    { prompt: "Уровень 9 (Proxy/Load Balancing): Таймаут у легитимных клиентов. Проверки здоровья балансировщика нагрузки не проходят. Что происходит?", reveal: "Балансировщик нагрузки удаляет origin из ротации. Повторы клиентов (backoff 100 мс, 200 мс, 400 мс) попадают к балансировщику. При одном недоступном origin трафик переходит к резервным. Если резервные тоже замедляются (под DDoS), логика повторов усиливает: 1 000 клиентов × 3 повтора = 3 000 запросов = пик 3×." },
    { prompt: "Уровни 3 до 1 (TCP до Physical): Перегрузка распространяется назад. Сигнатура отказа?", reveal: "Каждый уровень повторяет независимо: DNS-клиенты повторяют SERVFAIL. TCP-клиенты повторяют SYN. TLS-клиенты повторяют handshake. HTTP-клиенты повторяют GET. Все повторы используют экспоненциальный backoff, но с независимыми таймерами. В совокупности трафик к origin вырастает с 100 req/s до 1 000+ req/s за 30 секунд. Origin полностью насыщен, затем перегружен." },
    { prompt: "Многоуровневый ответ: circuit breaker + load shedding + jitter + graceful degradation.", reveal: "(1) Circuit breaker на слое прокси: прекратить маршрутизацию к отказывающим origin после 50% частоты ошибок за 30 с. (2) Load shedding на origin: когда запросы в полёте > 500, возвращать быстрый 503. (3) Exponential backoff + jitter в клиентской библиотеке: повторы распределены, не синхронизированы. (4) CDN stale-while-revalidate: edge обслуживает кэшированный контент, пока origin сбрасывает нагрузку. (5) Bulkheads: критический сервис (auth) изолирован от платёжного сервиса. Вместе: circuit закрывается → быстрые ошибки load shedding → клиенты повторяют медленно и с jitter → origin восстанавливается → circuit переходит в half-open → трафик возобновляется." },
  ]}
/>

<TradeoffMatrix
  id="sr-tradeoff-1"
  lessonSlug="07-resilience-and-cascades"
  lang="ru"
  prompt="Продукт для совместной работы в реальном времени требует p95 задержки менее 100 мс глобально для редактирования документов."
  options={[
    { name: "Один origin в us-east-1 с CDN front", summary: "Стандартная архитектура.", justification: "Статика быстрая везде. Мутирующие записи (правки документов) попадают на один origin — RTT 200+ мс для пользователей в Азии/Европе. SLO не выполняется." },
    { name: "Мульти-региональный origin с региональным шардированием", summary: "Пользователи маршрутизируются к ближайшему региону.", justification: "Преимущество по задержке для чтения, но кросс-региональные записи (пользователь в Азии редактирует документ из США) всё равно платят длинный RTT. Eventual consistency усложняет UX." },
    { name: "Edge compute (Cloudflare Workers + Durable Objects)", summary: "Stateful edge.", correct: true, justification: "Durable Object живёт на региональном edge ближайшем к основному пользователю документа. Записи sub-50 мс для основного; репликация в другие регионы async. Весь статический и динамический контент обслуживается с edge. SLO по бюджету задержки выполняется для типичного случая." },
    { name: "Peer-to-peer WebRTC между пользователями", summary: "Обойтись без серверов.", justification: "Теоретически наименьшая задержка, но операционная сложность — NAT traversal, совместимость с мобильными сетями, сохранение данных всё равно требует сервера. Не изобретайте инфраструктуру заново." },
  ]}
/>

<DesignPrompt
  id="sr-design-1"
  lessonSlug="07-resilience-and-cascades"
  lang="ru"
  prompt="Спроектируйте архитектуру пути запросов для глобального SaaS, который должен соответствовать: p95 загрузка страницы 1 с, p95 API-вызов 200 мс, доступность 99,99%."
  constraints={[
    "Глобальная пользовательская база, преимущественно Северная Америка + Европа + Азия.",
    "Преимущественно read-трафик; часть write-трафика для аккаунтов пользователей и правок документов.",
    "Требования compliance: данные пользователей ЕС должны оставаться в ЕС.",
  ]}
  canonical="Edge: Cloudflare (HTTP/3, h2, h1 fallback). DNS через Cloudflare anycast. Статика: кэш 1 год (имена файлов с content-hash), сброс при деплое через cache-tags. API gateway на edge: auth, rate limit, маршрутизация. Compute: региональные Kubernetes-кластеры в us-east-1, eu-west-1, ap-southeast-1 с CDN-маршрутизацией через origin shield. Данные: PostgreSQL с репликами для чтения по регионам; данные пользователей ЕС привязаны к eu-west-1 через row-level policy. Кэширующий слой: Redis по регионам. Наблюдаемость: OpenTelemetry с W3C Trace Context, head-based 5% + tail-based 100% ошибок. Multi-CDN failover через Cedexis RUM. Circuit breakers (Resilience4j) на каждый сторонний зависимый сервис. Performance budgets в CI."
  keyPoints={[
    "Кэшировать статику на edge; маршрутизировать динамику через edge gateway к региональным кластерам.",
    "Региональное хранение данных через row-level policy + региональные реплики.",
    "Распределённая трассировка OpenTelemetry end-to-end с tail-based семплированием ошибок.",
    "Circuit breakers на каждый сторонний зависимый сервис.",
    "Performance budgets, применяемые в CI — регрессии видны при PR.",
    "Error budget (99,99% = 4,38 мин/месяц) управляет release cadence — заморозка при исчерпании бюджета.",
  ]}
/>

<Quiz
  id="sr-q1"
  lessonSlug="07-resilience-and-cascades"
  lang="ru"
  question="Каков error budget для SLO доступности 99,95% за 30-дневное окно?"
  choices={[
    { label: "Около 4,5 часов простоя", misconception: "Это примерно 99,4% — значительно более слабый SLO." },
    { label: "Около 21,6 минут простоя", correct: true },
    { label: "Около 43 минут простоя", misconception: "Это бюджет SLO 99,9%." },
    { label: "Около 5 минут простоя", misconception: "Это примерно 99,99% — значительно более сильный SLO." },
  ]}
/>

<Quiz
  id="sr-q2"
  lessonSlug="07-resilience-and-cascades"
  lang="ru"
  question="Circuit breaker находится в состоянии OPEN. Приходит новый запрос. Что должно произойти?"
  choices={[
    { label: "Запрос ставится в очередь до закрытия circuit", misconception: "Очередь сводит на нет цель — она заполняется и вызывает ту же перегрузку, которую должен предотвращать breaker." },
    { label: "Запрос немедленно возвращает быструю ошибку (503) без обращения к нижестоящему сервису", correct: true },
    { label: "Запрос автоматически перенаправляется к резервному сервису", misconception: "Fallback к резервному сервису — отдельный паттерн (bulkhead/fallback); сам circuit breaker просто возвращает быструю ошибку." },
    { label: "Circuit breaker сбрасывается в CLOSED для повтора", misconception: "После OPEN circuit переходит в HALF-OPEN после окна остывания, затем один запрос-зонд тестирует восстановление." },
  ]}
/>

</Explanation>

<FlowDiagram
  label="Retry storm — это положительная обратная связь: медленный зависимый сервис вызывает таймауты, таймауты запускают нескоординированные повторы на каждом уровне, повторы добавляют нагрузку, больше нагрузки вызывает больше таймаутов, и за тридцать секунд origin достигает полного отказа. Ребро петли от больше нагрузки обратно к таймауту показывает усиление"
  hue="var(--d-network)"
  perRow={5}
  nodes={[
    { id: "slow", label: "Медленный сервис", sub: "5 с / ответ" },
    { id: "timeout", label: "Таймаут", sub: "возврат 503" },
    { id: "retry", label: "Повторы", sub: "каждый уровень" },
    { id: "load", label: "Больше нагрузки", sub: "3× → 10×" },
    { id: "outage", label: "Отказ", sub: "100% ошибок" },
  ]}
  edges={[
    { from: "slow", to: "timeout" },
    { from: "timeout", to: "retry" },
    { from: "retry", to: "load" },
    { from: "load", to: "outage", label: "30 с" },
    { from: "load", to: "timeout", label: "усиливает" },
  ]}
  caption="Один медленный сервис превращает 0,1% ошибок в 100% отказ, потому что все двенадцать уровней повторяют независимо. Circuit breaker разрывает ребро повторы→нагрузка, exponential backoff с jitter распределяет повторы, а load shedding возвращает быстрые 503 вместо удержания потоков — каждая защита разрывает петлю в своей точке."
/>

<KeyTakeaway>Нескоординированные повторы на каждом из двенадцати независимых уровней усиливают один медленный зависимый сервис в общесайтовый отказ; circuit breakers останавливают усиление, jitter распределяет восстановление, bulkheads сдерживают радиус поражения, а error budget делает компромисс скорость/надёжность измеримым.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="07-resilience-and-cascades-retrieval"
  lang="ru"
  questions={[
    { q: "Объясните математику усиления: как сервер с 99,9% доступностью полностью падает при retry storm?", a: "Сервер с 99,9% в норме обслуживает 1 ошибку на 1 000 запросов. Во время замедления запросы скапливаются. 1 000 конкурентных клиентов каждый повторяет 3× с наивным backoff через одинаковые интервалы. Origin получает 3 000 req/s вместо 1 000 req/s. При нагрузке 3× origin замедляется больше, вызывая больше таймаутов, больше повторов. Через 30 с нагрузка 10× нормы. Сервер с 99,9% теперь возвращает 100% ошибок — не потому что сломался, а потому что повторы усилили нагрузку сверх его мощности." },
    { q: "Почему добавление jitter к exponential backoff предотвращает thundering herd и какова формула полного jitter?", a: "Без jitter все клиенты с одинаковыми параметрами backoff повторяют в t=100 мс, t=200 мс одновременно — синхронизированный всплеск. Jitter добавляет случайную вариацию (например, ±25%) к каждой задержке повтора, распределяя 1 000 повторов по окну 200 мс. Формула полного jitter: sleep = random_between(0, min(cap, base * 2^attempt)). Это даёт максимальную рандомизацию — каждая попытка полностью случайна до максимума — и наиболее эффективна против thundering herd." },
    { q: "Старший архитектор утверждает, что edge compute всегда лучше региональных origin. Где ошибка в этом аргументе?", a: "Edge compute побеждает для чувствительных к задержке read-путей, где вычисления дёшевы и данные помещаются на edge (auth-токены, A/B решения, маршрутизация запросов, KV-поиски). Для сложных stateful рабочих нагрузок — join по большим базам данных, ML-inference, платёжная обработка, данные с требованиями compliance — edge compute непрактично: cold start важнее, ограничения памяти жёстче, отладка сложнее, хранение данных неоднозначно. Правильный паттерн: edge для тонкой gateway-логики + региональные кластеры для сложной рабочей нагрузки. «Всегда edge» чрезмерно упрощает." },
  ]}
/>

<Recap lang="ru">
Каскадные retry storm возникают потому, что каждый уровень в двенадцатифазной цепочке запросов имеет независимую логику повторов — когда зависимый сервис замедляется, все уровни повторяют одновременно, усиливая трафик с 1× до 10× менее чем за минуту. Стек защиты: exponential backoff с полным jitter (распределяет повторы), circuit breakers (быстрая ошибка при отказе, half-open зонд для восстановления), bulkheads (пул потоков на зависимость для сдерживания радиуса поражения) и load shedding (быстрый 503 при превышении глубины очереди). Error budget формализует компромисс скорость/надёжность: SLO 99,95% допускает 21,6 минут простоя за 30-дневное окно; исчерпание бюджета вызывает заморозку релизов, а не спор. Полная архитектура для глобального SaaS сочетает CDN edge для статики, региональные Kubernetes-кластеры с региональным хранением данных, OpenTelemetry с tail-based семплированием ошибок и CI-применяемые performance budgets — рассматривая каждый уровень как потенциальный домен отказа с явными runbook. Теперь, когда зависимый сервис замедляется в продакшне, чеклист автоматичен: сработал ли circuit breaker? Активен ли load shedding? Добавлен ли jitter к повторам? Каждый пропущенный пункт — путь усиления, превращающий «медленно» в «недоступно».
</Recap>
