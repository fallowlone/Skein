---
concepts:
- reverse-proxy
- health-check
- token-bucket
- WAF
- mTLS
- SPIFFE
- rate-limit-jitter
- connection-draining
deepensInto:
- 07-resilience-and-cascades
estMin: 13
lang: ru
lessonType: topic
level: middle
order: 4
prereqs:
- 01-the-twelve-layers
- 09-proxy-load-balancing/01-what-reverse-proxies-do
- 11-network-security/03-rate-limiting-algorithms
- 11-network-security/04-waf-and-firewalls
slug: 04-proxy-security-gates
sources:
- https://datatracker.ietf.org/doc/html/rfc9110
- https://spiffe.io/
- https://www.cloudflare.com/en-gb/learning/ddos/
spiral:
- statefulness
- latency
status: ready
summary: 'Каждый запрос проходит четыре шлюза до origin: кэш CDN, per-IP rate limiter
  (token bucket), WAF (сопоставление сигнатур) и mTLS в service mesh. Каждый добавляет
  менее 5 мс, но устраняет целый класс атак.'
title: 'Перехват прокси и шлюзы безопасности: rate limiter, WAF, mTLS'
track: networking
unit: 12-putting-it-together
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import TraceScenario from "~/components/pedagogy/TraceScenario.astro";
import NumbersCard from "~/components/layout/NumbersCard.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";
import Infographic from "~/components/diagram/Infographic.astro";
import gateLatencyBudget from "./infographics/gate-latency-budget.svg?raw";

<Hook>
Ботнет отправляет 10 000 запросов в секунду с 10 000 разных IP. Ваш per-IP rate limiter видит 1 запрос с IP — хорошо ниже лимита. Ваш WAF видит легитимные браузерные fingerprints. Сервер origin вот-вот будет перегружен. Шлюзы безопасности работают, но работают в одиночку, а не в связке. Этот урок рассматривает каждый шлюз, его стоимость и почему ни один из них не работает без остальных.
</Hook>

<Crux>Каждый запрос проходит четыре шлюза до origin; каждый добавляет менее 5 мс, устраняет класс атак — и ни один не работает в одиночку.</Crux>

<Explanation>

## Шлюз 1: Маршрутизация обратного прокси по состоянию health

До того как SYN-пакет Бэа достигает origin, он может прийти к Патти — обратному прокси или CDN edge. Патти принимает решение о маршрутизации:

**Путь при попадании в кэш.** Патти проверяет кэш: «Есть ли у меня ответ для этого URL с совпадающими заголовками Vary?» Если да, она отвечает немедленно из кэша edge. TCP + TLS были согласованы с edge-сервером Патти (~20 мс RTT); origin никогда не видит запрос.

**Путь при промахе кэша.** Патти проверяет состояние origin. Health checks выполняются вне основного потока каждые несколько секунд — периодический GET /health или TCP-зонд к каждому серверу origin. Если origin здоров, Патти перенаправляет. Если нездоров (неудачная проверка, процесс упал, высокая частота 5xx), Патти маршрутизирует к резервному серверу в пуле. Это происходит на лету — SYN, приходящий во время failover, может быть направлен к другому серверу.

**Connection draining.** При удалении сервера из пула (деплой, уменьшение масштаба) Патти прекращает отправлять новые соединения на него, но позволяет существующим завершить ответ. Окно draining (обычно 30–60 с) гарантирует чистое завершение запросов в полёте до отключения сервера.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="text-sm w-full border-collapse">
    <thead>
      <tr class="bg-slate-100">
        <th class="border px-3 py-2 text-left">Шлюз</th>
        <th class="border px-3 py-2 text-left">Задержка</th>
        <th class="border px-3 py-2 text-left">Что блокирует</th>
        <th class="border px-3 py-2 text-left">Что пропускает</th>
      </tr>
    </thead>
    <tbody>
      <tr><td class="border px-3 py-1">Проверка кэша прокси</td><td class="border px-3 py-1">&lt;1 мс</td><td class="border px-3 py-1">Кэшируемый трафик — обслуживает с edge</td><td class="border px-3 py-1">Некэшируемые, мутирующие endpoints</td></tr>
      <tr><td class="border px-3 py-1">Per-IP rate limiter</td><td class="border px-3 py-1">&lt;1 мс</td><td class="border px-3 py-1">Flood с одного IP, простые scrapers</td><td class="border px-3 py-1">Распределённые ботнеты (1 req/IP/s)</td></tr>
      <tr><td class="border px-3 py-1">WAF сопоставление сигнатур</td><td class="border px-3 py-1">1–5 мс</td><td class="border px-3 py-1">SQLi, XSS, CVE-сигнатуры</td><td class="border px-3 py-1">Новые атаки, зашифрованные полезные нагрузки</td></tr>
      <tr><td class="border px-3 py-1">Проверка сертификата mTLS</td><td class="border px-3 py-1">20–40 мс</td><td class="border px-3 py-1">Неаутентифицированные сервисы в service mesh</td><td class="border px-3 py-1">Компрометированный, но действительный владелец сертификата</td></tr>
    </tbody>
  </table>
</div>

## Шлюз 2: Rate limiter (token bucket — алгоритм «токенного ведра»)

Когда проектируешь API, вопрос не в том, ограничивать ли запросы, а в том, какой алгоритм выбрать и какие у него слепые зоны. Rate limiter устанавливает квоты: «Этот IP может отправить N запросов в секунду.» Реализация: **алгоритм token bucket**. У каждого IP есть корзина с ёмкостью C. Каждую секунду добавляется T токенов (до C). Каждый запрос потребляет один токен. Когда токены исчерпаны, новые запросы отбрасываются или возвращается 429 Too Many Requests.

**Token bucket vs leaky bucket.** Token bucket допускает всплески до ёмкости C. Leaky bucket вытекает с постоянной скоростью, предотвращая всплески. Большинство API rate limiter используют token bucket, потому что он допускает пакетных, но добросовестных клиентов (браузер, загружающий 30 подресурсов одновременно) без их наказания.

**Проблема распределённого ботнета.** Ботнет из 10 000 IP, каждый отправляет 0,9 req/s, отправляет суммарно 9 000 req/s. Лимит 2 req/s на IP пропускает их всех. Защита: добавить **глобальный лимит одновременных соединений** или **адаптивное ограничение конкурентности** на origin — когда количество запросов в полёте превышает мощность сервера, новые отклоняются быстрым 503 независимо от per-IP квоты.

**Jitter при сбросе лимита.** Если все клиенты одновременно достигают лимита и все токены сбрасываются в секунду 0, толпа бьёт по origin в t=1 с. Решение: рандомизировать смещения пополнения токенов на клиента (добавить ±10% jitter к окну пополнения). Сбросы лимита распределяются по клиентам, сглаживая нагрузку на origin.

## Шлюз 3: WAF — Web Application Firewall

WAF (межсетевой экран уровня приложения) проверяет контент на уровне приложения. Два режима:

**На основе сигнатур (rule mode).** OWASP ModSecurity Core Rule Set (CRS) определяет паттерны: SQL-инъекции (`union select`), XSS (`<script>`), path traversal (`../`), полезные нагрузки для конкретных CVE. Каждый запрос сопоставляется с сотнями правил. Работа на PL1–PL2 (Paranoia Level 1-2) балансирует ложные срабатывания и покрытие. PL4 (параноидальный) блокирует легитимный трафик, случайно совпадающий с агрессивными паттернами.

**Аномальный.** Базируется на нормальной форме запроса (скорость, user-agent, структура заголовков, энтропия нагрузки). Отклонения оцениваются. IP, отправляющий 100 req/min с fingerprint headless Chrome, который внезапно отправляет 3 000 req/min с идентичными заголовками, помечается как бот. Аномальное обнаружение нельзя обойти, зная правила.

**Стоимость WAF.** 1–5 мс на запрос для rule-based WAF на edge. Обоснование: устраняет широкие классы атак на уровне приложения, которые иначе потребовали бы дорогостоящих путей кода на уровне приложения или защиты запросов к БД.

## Шлюз 4: mTLS — взаимная TLS для авторизации сервис-к-сервису

Обычный TLS аутентифицирует только сервер (Бэа доверяет Свену через сертификат Кары). **mTLS** (mutual TLS — взаимное TLS-рукопожатие с сертификатами с обеих сторон) аутентифицирует обе стороны. Во время TLS-рукопожатия Свен отправляет сообщение `CertificateRequest`. Бэа отправляет свой клиентский сертификат. Свен проверяет его против доверенного CA.

**Где mTLS важен.** Внутренние микросервисы в zero-trust сети. Если у вас есть payment service и user service в одном кластере Kubernetes, mTLS гарантирует, что user service не может имперсонировать payment service — даже если злоумышленник получает сетевой доступ внутри кластера.

**SPIFFE** (Secure Production Identity Framework for Everyone — стандарт идентификации рабочих нагрузок в production). Фреймворк SPIFFE/SPIRE автоматизирует выдачу сертификатов для рабочих нагрузок: каждый сервис получает краткосрочный сертификат (SVID) от SPIFFE-сервера. Сертификаты ротируются автоматически (например, ежечасно). mTLS применяется service mesh (Istio, Linkerd) как sidecar proxy — код приложения не работает с TLS напрямую.

**Стоимость mTLS.** Один дополнительный round trip на новое соединение (запрос клиентского сертификата + отправка). 20–40 мс добавляется к задержке первого соединения. На тёплых соединениях (возобновление сессии) стоимость нулевая. Операционная стоимость: распределение, ротация и отзыв сертификатов должны быть автоматизированы — делать это вручную для сотен сервисов непрактично.

<Infographic
  svg={gateLatencyBudget}
  label="Столбчатая диаграмма стоимости задержки на запрос для четырёх шлюзов безопасности: кэш прокси менее 1 мс, per-IP rate limiter менее 1 мс, сопоставление сигнатур WAF 1–5 мс и проверка клиентского сертификата mTLS 20–40 мс на новом соединении"
  caption="Три шлюза стоят менее 5 мс каждый; mTLS на новом соединении — примерно в 10 раз дороже, поэтому его выгодно пропускать на тёплых соединениях с возобновлением сессии."
  hue="var(--d-infra)"
/>

<Inset kind="edgecase" lang="ru">
mTLS не защищает от скомпрометированного, но действительного владельца сертификата. Если злоумышленник крадёт действительный клиентский сертификат, mTLS им доверяет. Защита: короткий срок жизни сертификата (1 час через SPIFFE) + OCSP stapling для отзыва. Вероятность того, что злоумышленник перехватит и использует украденный сертификат до истечения его срока, резко падает, когда сертификаты живут всего час.
</Inset>

<TraceScenario
  id="mid-trace-sec"
  lessonSlug="04-proxy-security-gates"
  lang="ru"
  scenario="Проследите запрос от обычного пользователя и IP ботнета через все четыре шлюза."
  steps={[
    { prompt: "Обычный пользователь: TCP SYN приходит на edge Патти. Первая проверка?", reveal: "Поиск в кэше. При попадании (например, HTML был кэширован после последнего посетителя), Патти немедленно возвращает 200 из кэша edge. Rate limiter, WAF и origin не задействованы. Задержка: ~20 мс." },
    { prompt: "Промах кэша. Проверка rate limiter для обычного пользователя (лимит 2 req/s на IP)?", reveal: "В корзине токенов пользователя есть токены (они только что перешли сюда впервые за эту минуту). Токен потреблён, запрос проходит. &lt;1 мс." },
    { prompt: "Проверка WAF для обычного пользователя?", reveal: "Заголовки запроса, путь и тело не совпадают ни с одной сигнатурой атаки на PL2. Запрос проходит. 1–3 мс." },
    { prompt: "IP ботнета: 10 000 IP, каждый отправляет 1 req/s. Per-IP rate limiter?", reveal: "Каждый отдельный IP ниже лимита 2 req/s. Все 10 000 req/s проходят через per-IP шлюз. Rate limiter не улавливает распределённый ботнет." },
    { prompt: "Глобальный лимит конкурентности на origin. Запросы в полёте превышают порог. Что происходит?", reveal: "Адаптивный ограничитель конкурентности origin (например, на основе AIMD или явный max_concurrency=500) видит превышение глубины очереди запросов. Новые запросы получают 503 немедленно — быстрый отказ, стабильный origin. Ботнет видит ошибки 503; легитимные пользователи защищены кэшем CDN edge (они всё ещё получают кэшированные ответы)." },
    { prompt: "mTLS для внутреннего вызова API (payment service → user service). Какой дополнительный шаг?", reveal: "User service предъявляет свой SPIFFE SVID (клиентский сертификат) во время TLS-рукопожатия. Payment service проверяет SVID против SPIFFE trust domain. Если действителен, соединение продолжается. Если истёк или неверный trust domain, TLS немедленно завершается. Никакого кода приложения не задействовано." },
  ]}
/>

<Quiz
  id="mid-q1"
  lessonSlug="04-proxy-security-gates"
  lang="ru"
  question="Ботнет отправляет 1 запрос/секунду с 10 000 разных IP. Ваш per-IP лимит — 5 req/s. Как защититься, не блокируя легитимных пользователей?"
  choices={[
    { label: "Снизить per-IP лимит до 0,5 req/s", misconception: "Это заблокирует легитимных пользователей с всплесками (например, браузер, загружающий 30 подресурсов одновременно)." },
    { label: "Добавить глобальный адаптивный лимит конкурентности на origin, чтобы запросы в полёте сверх мощности вызывали быстрый 503 для всего избыточного трафика", correct: true },
    { label: "Заблокировать все IP из ASN ботнета", misconception: "Ботнеты часто используют домашние IP, которые разделяют ASN с легитимными пользователями." },
    { label: "Увеличить per-IP лимит для снижения ложных срабатываний", misconception: "Более высокие лимиты помогают легитимным пользователям, но делают ботнет сложнее остановить." },
  ]}
/>

<Quiz
  id="mid-q2"
  lessonSlug="04-proxy-security-gates"
  lang="ru"
  question="Почему сертификаты mTLS должны быть краткосрочными (например, 1 час) в zero-trust service mesh?"
  choices={[
    { label: "TLS 1.3 поддерживает только краткосрочные сертификаты", misconception: "TLS 1.3 поддерживает сертификаты любой длительности." },
    { label: "Короткий срок жизни сокращает окно, в течение которого украденный сертификат может быть использован до истечения срока", correct: true },
    { label: "Проверка краткосрочных сертификатов быстрее", misconception: "Время проверки зависит от алгоритма подписи сертификата, а не от срока жизни." },
    { label: "SPIFFE требует ежечасной ротации по спецификации", misconception: "SPIFFE рекомендует краткосрочные сертификаты как лучшую практику; интервал ротации настраивается." },
  ]}
/>

</Explanation>

<FlowDiagram
  label="Запрос проходит четыре шлюза безопасности по порядку до достижения origin: проверка кэша обратного прокси, затем per-IP rate limiter, затем сопоставление сигнатур WAF, затем проверка клиентского сертификата mTLS, затем обработка на origin"
  hue="var(--d-network)"
  perRow={6}
  nodes={[
    { id: "req", label: "Запрос", sub: "SYN клиента" },
    { id: "cache", label: "Кэш прокси", sub: "<1 мс · hit отдаёт" },
    { id: "rate", label: "Rate limiter", sub: "<1 мс · token bucket" },
    { id: "waf", label: "WAF", sub: "1–5 мс · сигнатуры" },
    { id: "mtls", label: "Проверка mTLS", sub: "20–40 мс · сертификат" },
    { id: "origin", label: "Origin", sub: "обработка" },
  ]}
  edges={[
    { from: "req", to: "cache" },
    { from: "cache", to: "rate", label: "промах" },
    { from: "rate", to: "waf", label: "проход" },
    { from: "waf", to: "mtls", label: "проход" },
    { from: "mtls", to: "origin", label: "валиден" },
  ]}
  caption="Каждый шлюз добавляет менее 5 мс (mTLS 20–40 мс при первом соединении), но устраняет целый класс атак. Попадание в кэш замыкает все четыре шлюза накоротко; ни один шлюз не останавливает все атаки в одиночку — распределённый ботнет проскальзывает per-IP лимит, поэтому нужен глобальный лимит конкурентности на origin."
/>

<KeyTakeaway>Ни один шлюз безопасности не останавливает все классы атак; CDN-кэширование устраняет нагрузку до того, как её видят rate limiter, per-IP ограничение останавливает очевидные flood, WAF блокирует известные сигнатуры, mTLS предотвращает горизонтальное перемещение — все четыре уровня необходимы.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="04-proxy-security-gates-retrieval"
  lang="ru"
  questions={[
    { q: "Что такое connection draining и зачем он нужен при rolling deploy?", a: "Connection draining означает, что балансировщик нагрузки прекращает отправлять новые соединения на удаляемый сервер, но позволяет существующим завершить ответ. Типичное окно draining: 30–60 с. Без него запросы в полёте резко обрываются при остановке сервера, вызывая ошибки клиентов. Rolling deploy удаляет серверы по одному, поэтому draining обеспечивает нулевой простой для пользователей при каждой замене сервера." },
    { q: "Почему добавление jitter к времени пополнения токенов rate limiter снижает пиковую нагрузку на origin?", a: "Если все клиенты одновременно достигают лимита и все корзины токенов пополняются в секунду 0, синхронизированный всплеск из N клиентов бьёт в t=1 с — толпа. Добавление ±10% случайного jitter к смещению пополнения каждого клиента распределяет сбросы по секундному окну. Трафик сглаживается от одного большого пика до ровной скорости, устраняя перегрузку origin в момент сброса." },
    { q: "Что добавляет SPIFFE по сравнению с ручным управлением сертификатами mTLS?", a: "SPIFFE автоматизирует идентификацию рабочих нагрузок: каждый сервис получает краткосрочный X.509 SVID (SPIFFE Verifiable Identity Document) от SPIFFE-сервера, автоматически обновляемый (например, ежечасно). Service mesh (Istio, Linkerd) инжектирует ротацию сертификатов прозрачно через sidecars — никаких изменений кода приложения. Ручное управление сертификатами mTLS для сотен сервисов непрактично: SPIFFE делает это с операционными затратами O(1) на новый сервис." },
  ]}
/>

<Recap lang="ru">
До того как запрос достигает origin, он проходит четыре шлюза: поиск в кэше CDN (полностью устраняет запрос при кэш-попадании), per-IP rate limiter с token bucket (останавливает flood с одного IP), WAF с сопоставлением сигнатур (блокирует известные паттерны атак) и опциональную проверку клиентского сертификата mTLS (предотвращает неаутентифицированные вызовы сервис-к-сервису в zero-trust mesh). Каждый шлюз добавляет менее 5 мс, кроме mTLS (20–40 мс при первом соединении). Распределённые ботнеты обходят per-IP rate limiter, распределяя нагрузку — защита: глобальный адаптивный лимит конкурентности на origin, отклоняющий избыточные запросы в полёте быстрым 503 независимо от per-IP квоты. SPIFFE автоматизирует выдачу и ротацию сертификатов mTLS, делая краткосрочные ежечасные сертификаты операционально практичными для сотен микросервисов. Теперь, когда проектируешь новую API-поверхность или разбираешь инцидент безопасности, спроси: какой из четырёх шлюзов отсутствовал или был настроен неверно — именно там почти всегда находится точка входа атаки.
</Recap>
