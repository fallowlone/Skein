---
concepts:
- request-lifecycle
- seven-actors
- round-trip
- CDN-edge
- render-pipeline
deepensInto:
- 02-dns-tcp-tls-sequence
- 03-rendering-and-web-vitals
estMin: 10
lang: ru
lessonType: topic
level: junior
order: 1
prereqs:
- 03-tcp-handshake/01-the-three-way-handshake
- 04-dns-resolution/01-dns-what-it-does
- 05-tls-handshake/01-what-tls-does
- 06-http-versions/01-http-fundamentals
slug: 01-the-twelve-layers
sources:
- https://hpbn.co/
- https://web.dev/articles/vitals/
- https://datatracker.ietf.org/doc/html/rfc9110
spiral:
- encapsulation
- latency
status: ready
summary: Когда вы вводите URL, семь действующих лиц — браузер, резолвер, маршрутизатор,
  origin, CA, CDN edge и движок рендеринга — выполняют двенадцать фаз последовательно.
  Этот урок прокладывает маршрут всего пути.
title: 'Двенадцать слоёв: один URL, семь действующих лиц'
track: networking
unit: 12-putting-it-together
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import MetaphorComplete from "~/components/pedagogy/MetaphorComplete.astro";
import NumbersCard from "~/components/layout/NumbersCard.astro";
import StackDiagram from "~/components/diagram/StackDiagram.astro";

<Hook>
Вы вводите URL — страница появляется менее чем за 400 мс. Этот разрыв — не магия: двенадцать фаз, исполняемых семью действующими лицами, которые никогда не встречались и никогда не встретятся. Знать, кто отвечает за какую фазу, — это основа любого навыка в области производительности и диагностики из этой главы.
</Hook>

<Crux>Один URL запускает двенадцать фаз; знание ответственного за каждую — первый шаг к диагностике любой медленной страницы.</Crux>

<Explanation>

## Семь действующих лиц

Каждый HTTPS-запрос проходит через набор персонажей, каждый из которых отвечает ровно за одну задачу:

- **Bea (браузер)** — инициирует запрос, рендерит результат.
- **Rex (рекурсивный DNS-резолвер)** — преобразует hostname в IP-адрес.
- **Rita (сеть маршрутизаторов)** — переносит пакеты от перехода к переходу через интернет.
- **Cara (центр сертификации, CA)** — поручилась за личность сервера ещё до того, как был отправлен запрос; её подпись хранится в сертификате сервера.
- **Patty (CDN/прокси)** — перехватывает соединение на ближайшем edge POP, отдаёт закешированный контент или пересылает запрос к origin.
- **Sven (origin-сервер)** — вычисляет реальный ответ, когда кеш не может ответить.
- **Движок рендеринга Bea** — разбирает HTML/CSS/JS и рисует пиксели.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="text-sm w-full border-collapse">
    <thead>
      <tr class="bg-slate-100">
        <th class="border px-3 py-2 text-left">Фаза</th>
        <th class="border px-3 py-2 text-left">Действующее лицо</th>
        <th class="border px-3 py-2 text-left">Что происходит</th>
        <th class="border px-3 py-2 text-left">Типичная стоимость</th>
      </tr>
    </thead>
    <tbody>
      <tr><td class="border px-3 py-1">1. DNS lookup</td><td class="border px-3 py-1">Rex</td><td class="border px-3 py-1">hostname → IP</td><td class="border px-3 py-1">&lt;1 мс (тёплый) / 30–100 мс (холодный)</td></tr>
      <tr><td class="border px-3 py-1">2. TCP-соединение</td><td class="border px-3 py-1">Rita + Sven/Patty</td><td class="border px-3 py-1">трёхстороннее рукопожатие</td><td class="border px-3 py-1">~10–100 мс (RTT)</td></tr>
      <tr><td class="border px-3 py-1">3. TLS-рукопожатие</td><td class="border px-3 py-1">Bea + Sven + Cara</td><td class="border px-3 py-1">обмен ключами, проверка сертификата</td><td class="border px-3 py-1">1 RTT новое / 0 RTT возобновление</td></tr>
      <tr><td class="border px-3 py-1">4. Прокси / rate-limit</td><td class="border px-3 py-1">Patty</td><td class="border px-3 py-1">health check, поиск в кеше, WAF</td><td class="border px-3 py-1">&lt;1–5 мс</td></tr>
      <tr><td class="border px-3 py-1">5. HTTP-запрос</td><td class="border px-3 py-1">Bea → Sven</td><td class="border px-3 py-1">GET / с заголовками</td><td class="border px-3 py-1">~1 RTT</td></tr>
      <tr><td class="border px-3 py-1">6. Обработка на origin</td><td class="border px-3 py-1">Sven</td><td class="border px-3 py-1">auth, запрос к БД, сериализация</td><td class="border px-3 py-1">10–200 мс</td></tr>
      <tr><td class="border px-3 py-1">7. HTTP-ответ</td><td class="border px-3 py-1">Sven → Bea</td><td class="border px-3 py-1">статус + заголовки + тело</td><td class="border px-3 py-1">~1 RTT + размер тела</td></tr>
      <tr><td class="border px-3 py-1">8. Загрузка субресурсов</td><td class="border px-3 py-1">Bea (параллельно)</td><td class="border px-3 py-1">CSS, JS, шрифты, изображения</td><td class="border px-3 py-1">1–3 RTT (параллельно)</td></tr>
      <tr><td class="border px-3 py-1">9. Разбор DOM + CSSOM</td><td class="border px-3 py-1">Bea</td><td class="border px-3 py-1">построение дерева рендеринга</td><td class="border px-3 py-1">0–50 мс</td></tr>
      <tr><td class="border px-3 py-1">10. Layout + paint</td><td class="border px-3 py-1">Bea</td><td class="border px-3 py-1">позиционирование, растеризация</td><td class="border px-3 py-1">50–200 мс</td></tr>
      <tr><td class="border px-3 py-1">11. Выполнение JavaScript</td><td class="border px-3 py-1">Bea</td><td class="border px-3 py-1">обработчики событий, гидрация</td><td class="border px-3 py-1">0–500 мс</td></tr>
      <tr><td class="border px-3 py-1">12. LCP отрисован</td><td class="border px-3 py-1">Bea</td><td class="border px-3 py-1">наибольший видимый элемент готов</td><td class="border px-3 py-1">итого: 200–4000 мс</td></tr>
    </tbody>
  </table>
</div>

## Почему порядок фиксирован

Когда страница грузится 800 мс и вы хотите найти причину, нужно точно знать, кто отвечает за каждую фазу — иначе вы оптимизируете не то. Каждая фаза зависит от результата предыдущей. DNS должен разрешить hostname прежде, чем Bea узнает, к какому IP подключаться. TCP должен завершиться прежде, чем TLS сможет обменяться ключами. TLS должен завершиться прежде, чем HTTP сможет отправить зашифрованный запрос. Эта строгая последовательность — вот почему **количество round-trip, а не скорость round-trip, является главным рычагом управления задержкой**: можно сократить каждый RTT, переместив сервер ближе, но нельзя пропустить RTT, не перепроектировав протокол.

Единственное исключение: **кеширование на CDN edge**. Если у Patty есть ответ в кеше, фазы 4–7 схлопываются в один CDN-ответ. Bea по-прежнему платит за DNS + TCP + TLS (фазы 1–3), но это происходит с сервером в 20 мс, а не в 200 мс. Это центральный инсайт главы по производительности в одном предложении.

## Метафора доставки

Представьте запрос как доставку посылки:

- Вы делаете заказ (HTTP-запрос).
- Адрес ищется в телефонном справочнике (DNS).
- Фургон отправляется по дорожной сети (TCP + маршрутизация через Rita).
- Посылка запаивается в защитную упаковку (TLS).
- На местном складе товар может уже лежать (кеш CDN).
- Посылка доставляется к двери (ответ).
- Вы распаковываете и используете (рендеринг браузера).

Все эти шаги объясняют, почему замена грузовика на более быстрый (увеличение пропускной способности) редко устраняет ощущаемую медлительность: вы всё равно платите за справочник, дорожную сеть и распаковку. Каждая роль независима; каждую можно измерить и оптимизировать отдельно.

<Inset kind="why" lang="ru">
Строгая зависимость слоёв существует не случайно: каждый слой можно оптимизировать, заменить или вывести из строя независимо. DNS можно сменить, не трогая TLS. TLS можно обновить (1.2 → 1.3), не меняя семантики HTTP. Это принцип **разделения ответственности**, применённый к сетевым коммуникациям в планетарном масштабе.
</Inset>

<Quiz
  id="jr-q1"
  lessonSlug="01-the-twelve-layers"
  lang="ru"
  question="В каком порядке выполняются первые четыре фазы нового HTTPS-запроса?"
  choices={[
    { label: "HTTP, TLS, TCP, DNS", misconception: "Порядок обратный — DNS происходит первым, потому что браузер должен получить IP-адрес, прежде чем сможет открыть сокет." },
    { label: "DNS, TCP, TLS, HTTP", correct: true },
    { label: "TCP, HTTP, TLS, DNS", misconception: "TCP не может запуститься без IP-адреса, который приходит от DNS первым." },
    { label: "Все четыре параллельно", misconception: "Они строго зависят друг от друга; параллелизм помогает только внутри фаз, но не между ними." },
  ]}
/>

<DragOrder
  id="jr-drag-1"
  lessonSlug="01-the-twelve-layers"
  lang="ru"
  prompt="Расставьте фазы в порядке от нажатия клавиши до первой отрисовки:"
  items={[
    "DNS разрешает hostname в IP",
    "Трёхстороннее рукопожатие TCP",
    "Рукопожатие TLS 1.3 и проверка сертификата",
    "Отправка HTTP-запроса",
    "Получение HTTP-ответа",
    "Браузер параллельно загружает субресурсы (CSS, JS, шрифты)",
    "Браузер выполняет layout и рисует первый видимый контент",
  ]}
/>

<MetaphorComplete
  id="jr-metaphor-1"
  lessonSlug="01-the-twelve-layers"
  lang="ru"
  setup="Заполните пропуск: сетевой стек — это _______, где каждый слой оборачивает тот, что ниже."
  accepted={["луковица", "стек", "слоёный пирог", "вложенные конверты", "матрёшка"]}
  canonical="луковица (или стек)"
  explanation="Каждый слой инкапсулирует нижний: HTTP внутри TLS, TLS внутри TCP, TCP внутри IP, IP внутри Ethernet. Тот же байт проходит через каждую оболочку на пути наружу."
/>

</Explanation>

<StackDiagram
  label="Двенадцать фаз загрузки одного URL, выполняемых сверху вниз в фиксированном порядке: DNS lookup, TCP-соединение, TLS-рукопожатие, проверка прокси, HTTP-запрос, обработка на origin, HTTP-ответ, загрузка субресурсов, разбор DOM и CSSOM, layout и paint, выполнение JavaScript, отрисовка LCP"
  hue="var(--d-network)"
  caption="Каждая фаза зависит от результата фазы выше, поэтому они выполняются строго сверху вниз. Количество round-trip, а не их скорость, — главный рычаг задержки, а попадание в кеш CDN edge схлопывает фазы 4–7 в один ближний ответ."
  layers={[
    { label: "1. DNS lookup", note: "Rex: hostname → IP" },
    { label: "2. TCP-соединение", note: "Rita + Sven: 3-стороннее рукопожатие" },
    { label: "3. TLS-рукопожатие", note: "Bea + Sven + Cara: ключи + сертификат" },
    { label: "4. Проверка прокси / rate-limit", note: "Patty: кеш, WAF" },
    { label: "5. HTTP-запрос", note: "Bea → Sven: GET /" },
    { label: "6. Обработка на origin", note: "Sven: auth, БД, сериализация" },
    { label: "7. HTTP-ответ", note: "Sven → Bea: статус + тело" },
    { label: "8. Загрузка субресурсов", note: "Bea: CSS, JS, шрифты" },
    { label: "9. Разбор DOM + CSSOM", note: "Bea: дерево рендеринга" },
    { label: "10. Layout + paint", note: "Bea: позиция + растеризация" },
    { label: "11. Выполнение JavaScript", note: "Bea: обработчики, гидрация" },
    { label: "12. Отрисовка LCP", note: "Bea: наибольший элемент готов" },
  ]}
/>

<KeyTakeaway>Семь действующих лиц выполняют двенадцать фаз в фиксированном порядке; кеширование на CDN edge — наиболее эффективная оптимизация, потому что одновременно сокращает все три рукопожатия.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="01-the-twelve-layers-retrieval"
  lang="ru"
  questions={[
    { q: "Назовите семь действующих лиц HTTPS-запроса и по одной задаче для каждого.", a: "Bea (браузер, рендерит), Rex (DNS, разрешает), Rita (маршрутизатор, переносит пакеты), Cara (CA, подписала сертификат), Patty (CDN/прокси, кеширует и маршрутизирует), Sven (origin, вычисляет), движок рендеринга Bea (рисует пиксели)." },
    { q: "Почему DNS, TCP, TLS и HTTP нельзя выполнять параллельно?", a: "Каждая фаза зависит от результата предыдущей: DNS даёт IP, TCP открывает сокет, TLS согласовывает ключи — только тогда HTTP может отправить зашифрованный запрос. Цепочка зависимостей вынуждает последовательное выполнение." },
    { q: "Что делает кеширование на CDN edge наиболее эффективной оптимизацией задержки?", a: "Попадание в кеш CDN схлопывает обработку на origin и дальние round-trip в один ближний ответ. DNS, TCP и TLS по-прежнему выполняются, но с сервером в 20 мс, а не в 200 мс — одновременно сокращая все три рукопожатия." },
  ]}
/>

<Recap lang="ru">
Один URL запускает двенадцатифазную цепочку, выполняемую семью действующими лицами: Rex разрешает hostname, маршрутизаторы Rita переносят пакеты, Bea согласовывает TLS с Sven используя сертификат Cara, Patty может ответить из кеша, а движок рендеринга Bea рисует результат. Фазы строго упорядочены — каждая зависит от предыдущей — поэтому количество round-trip, а не их скорость, является главным рычагом задержки. Попадание в кеш CDN в 20 мс вместо 200 мс одновременно сокращает все три рукопожатия. Теперь, когда встретишь медленную страницу, первый вопрос — кто из семи действующих лиц отвечает за тормозящую фазу, и в чём причина: количество round-trip или их длина?
</Recap>
