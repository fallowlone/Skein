---
concepts:
- HTTP3-QUIC
- 0-RTT-streams
- HoL-blocking
- WebSocket-upgrade
- connection-migration
- PATH_CHALLENGE
- backpressure
- stream-independence
deepensInto:
- 06-observability-and-traces
- 07-resilience-and-cascades
estMin: 14
lang: ru
lessonType: topic
level: middle
order: 5
prereqs:
- 02-dns-tcp-tls-sequence
- 08-websocket-realtime/01-upgrade-handshake
- 10-quic-internals/02-quic-streams-and-hol
- 10-quic-internals/04-connection-migration
slug: 05-quic-websocket-paths
sources:
- https://datatracker.ietf.org/doc/html/rfc9000
- https://datatracker.ietf.org/doc/html/rfc9001
- https://datatracker.ietf.org/doc/html/rfc6455
spiral:
- multiplexing
- statefulness
- latency
status: ready
summary: HTTP/3 поверх QUIC устраняет HoL-блокировку и выживает при смене IP; WebSocket
  даёт постоянный двунаправленный канал, но отказывает при backpressure. Оба требуют
  явного проектирования отказов.
title: 'Альтернативные пути: QUIC 0-RTT, WebSocket upgrade, миграция соединения'
track: networking
unit: 12-putting-it-together
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import TraceScenario from "~/components/pedagogy/TraceScenario.astro";
import SequenceDiagram from "~/components/diagram/SequenceDiagram.astro";

<Hook>
Мобильный пользователь в поезде сообщает, что сайт перезагружается каждый раз, когда поезд проезжает тоннель. Тем временем дашборд в реальном времени на основе WebSocket тихо теряет сообщения, когда один клиент медленно их потребляет. У обеих проблем одна первопричина: транспорт был разработан для стабильных соединений, но реальность нестабильна. HTTP/3 и WebSocket справляются с этим по-разному — и оба отказывают способами, которые нужно явно проектировать.
</Hook>

<Crux>HTTP/3 потоки переживают потерю пакетов и смену IP; WebSocket даёт постоянные двунаправленные фреймы, но зависает при медленных получателях.</Crux>

<Explanation>

## Путь A: HTTP/3 поверх QUIC

**Почему QUIC вместо TCP.** TCP мультиплексирует потоки через одно соединение, но один потерянный пакет останавливает весь TCP receive window до повторной передачи. У HTTP/2 поверх TCP именно эта проблема: один медленный поток блокирует все остальные потоки того же соединения. QUIC реализует потоки как независимые сущности — потерянный пакет в потоке A не блокирует потоки B, C или D. Эта независимость потоков — главное преимущество в сетях с потерями (мобильные, межконтинентальные линки).

**Объединённое рукопожатие.** TCP требует отдельного трёхстороннего handshake (1 RTT) до начала TLS (ещё минимум 1 RTT). QUIC их объединяет: Initial-пакет QUIC несёт TLS ClientHello. Сервер отвечает одним пакетом, содержащим транспортные параметры QUIC и TLS ServerHello. Итого: **1 RTT для нового соединения** вместо 2.

**0-RTT возобновление.** Для тёплых соединений (пользователь возвращается к тому же origin в пределах срока действия session ticket), клиент отправляет данные приложения внутри Initial-пакета QUIC — до того как сервер ответил. Эффективный RTT для первого байта: 0 дополнительных round-trips. Ограничение: 0-RTT безопасен только для **идемпотентных методов** (GET, HEAD). Запросы POST/PUT должны ждать завершения handshake (сервер отклоняет 0-RTT для неидемпотентных запросов с 425 Too Early).

**Максимальный объём ранних данных 0-RTT.** Серверы ограничивают количество байт ранних данных (обычно: 16–64 КБ). Если HTTP-запрос + заголовки превышают это, остаток отправляется после handshake. Это ограничение предотвращает использование злоумышленниками 0-RTT для внедрения большой нагрузки до завершения аутентификации.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="text-sm w-full border-collapse">
    <thead>
      <tr class="bg-slate-100">
        <th class="border px-3 py-2 text-left">Характеристика</th>
        <th class="border px-3 py-2 text-left">HTTP/2 + TCP</th>
        <th class="border px-3 py-2 text-left">HTTP/3 + QUIC</th>
      </tr>
    </thead>
    <tbody>
      <tr><td class="border px-3 py-1">Стоимость нового соединения</td><td class="border px-3 py-1">2 RTT (TCP + TLS)</td><td class="border px-3 py-1">1 RTT (объединённый)</td></tr>
      <tr><td class="border px-3 py-1">Тёплое соединение (возобновление)</td><td class="border px-3 py-1">1 RTT (TLS PSK)</td><td class="border px-3 py-1">0 RTT (0-RTT early data)</td></tr>
      <tr><td class="border px-3 py-1">Влияние потери пакета</td><td class="border px-3 py-1">Останавливает все потоки (HoL block)</td><td class="border px-3 py-1">Затрагивает только потерянный поток</td></tr>
      <tr><td class="border px-3 py-1">Смена IP</td><td class="border px-3 py-1">Соединение разрывается</td><td class="border px-3 py-1">Мигрирует через Connection ID</td></tr>
      <tr><td class="border px-3 py-1">Управление перегрузкой</td><td class="border px-3 py-1">Ядро (не настраивается по потоку)</td><td class="border px-3 py-1">User-space (настраивается, BBR2, PCC)</td></tr>
      <tr><td class="border px-3 py-1">Совместимость с middlebox</td><td class="border px-3 py-1">Отличная</td><td class="border px-3 py-1">Некоторые firewall блокируют UDP</td></tr>
    </tbody>
  </table>
</div>

## Миграция соединения (мобильные сети)

Когда телефон Бэа переключается с WiFi на мобильный интернет, её IP-адрес меняется. При TCP+HTTP/2 TCP-четвёрка (src IP, src port, dst IP, dst port) является частью идентификатора соединения — смена IP разрывает соединение. Бэа должна заново установить TCP + TLS + HTTP, заплатив полную стоимость handshake.

При QUIC соединения идентифицируются **Destination Connection ID** — непрозрачным токеном, выданным Свеном при handshake, выбранным Свеном, присутствующим в каждом заголовке пакета QUIC. Ядро Бэа отправляет фрейм `PATH_CHALLENGE` с нового IP. Свен отвечает `PATH_RESPONSE` с тем же Connection ID, подтверждая, что смена IP легитимна (а не злоумышленник подделывает исходный IP Бэа). Все потоки в полёте продолжаются по новому пути. Миграция стоит один дополнительный round-trip (~50 мс в хорошей сети), но позволяет избежать повторного установления всего соединения.

<Inset kind="edgecase" lang="ru">
Миграция соединения QUIC работает только если балансировщик нагрузки сервера маршрутизирует пакеты с одним и тем же Connection ID к одному и тому же backend-процессу. Это ограничение при развёртывании: stateless балансировщики нагрузки, маршрутизирующие по source IP, нарушат миграцию QUIC. Решение: маршрутизировать QUIC по Connection ID (Nginx QUIC, Caddy, Cloudflare поддерживают это нативно).
</Inset>

## Путь B: WebSocket upgrade для коммуникации в реальном времени

HTTP работает по схеме запрос-ответ — один запрос, один ответ. Для двунаправленной коммуникации в реальном времени (чат, совместное редактирование, live-дашборды) клиенту нужно получать сообщения от сервера без нового запроса для каждого. WebSocket решает это, обновляя HTTP-соединение до постоянного полнодуплексного канала.

**Upgrade handshake.** Клиент отправляет:
```
GET /ws HTTP/1.1
Upgrade: websocket
Connection: Upgrade
Sec-WebSocket-Key: <base64 random 16 bytes>
```
Сервер отвечает:
```
HTTP/1.1 101 Switching Protocols
Upgrade: websocket
Connection: Upgrade
Sec-WebSocket-Accept: <HMAC of key + magic string>
```
После 101 TCP-соединение перепрофилируется как WebSocket. Обе стороны обмениваются **фреймами** асинхронно — без порядка запрос-ответ, без заголовков на каждое сообщение.

**Структура фрейма.** Каждый WebSocket-фрейм несёт:
- FIN бит (1 = финальный фрагмент, 0 = следуют ещё фрагменты)
- Opcode (1=текст, 2=двоичный, 8=закрытие, 9=ping, 10=pong)
- Маскирование (клиент→сервер обязательно, XOR с 4-байтовым маскирующим ключом — защита от отравления кэша)
- Длина нагрузки + нагрузка

Большие сообщения фрагментируются по нескольким фреймам (FIN=0 для фрагментов, FIN=1 для финального). Получатель буферизирует до FIN=1 перед доставкой приложению.

**Keepalive через ping/pong.** У WebSocket нет встроенного keepalive на уровне TCP. Примерно через 75 с без фрейма многие сетевые устройства таймаутируют простаивающие TCP-соединения. Решение: отправлять `ping`-фреймы каждые 15–30 с. Получатель отвечает `pong`. Если pong не получен в течение таймаута, соединение мертво и должно быть переустановлено.

**Отказ из-за backpressure.** Если приложение Бэа медленно потребляет сообщения (например, тяжёлая JavaScript-обработка каждого сообщения), send buffer Свена заполняется. Когда Свен вызывает `write()` на сокете, ядро возвращает `WOULDBLOCK`. Наивный серверный код, закрученный на `WOULDBLOCK`, повышает CPU; наивный код, теряющий сообщения, теряет данные. Правильный паттерн: Свен прекращает читать от Бэа (приостанавливает входящий поток) до опустошения исходящего буфера. Правильные библиотеки WebSocket реализуют это через async/await и API backpressure потоков.

<TraceScenario
  id="mid-trace-quic"
  lessonSlug="05-quic-websocket-paths"
  lang="ru"
  scenario="Проследите handshake QUIC + HTTP/3 и сравните с TCP + HTTP/2."
  steps={[
    { prompt: "Новое соединение, HTTP/3. Что клиент отправляет первым?", reveal: "QUIC Initial-пакет, содержащий: транспортные параметры QUIC (максимальное число потоков, лимиты flow control) + TLS ClientHello (key share, SNI, cipher suites, 0-RTT session ticket при наличии). Всё в одной UDP-датаграмме." },
    { prompt: "Сервер отвечает. Что содержится в первом пакете сервера?", reveal: "QUIC Initial + Handshake пакеты: TLS ServerHello (выбранный шифр, key share) + сертификат + зашифрованные QUIC Handshake данные. Клиент может получить ключи трафика после этого — прошёл один RTT." },
    { prompt: "Клиент получил ключи. Может ли он теперь отправить HTTP-запрос?", reveal: "Да — клиент отправляет QUIC Handshake ACK и HTTP/3 HEADERS фрейм (запрос) в новом 1-RTT пакете. Для 0-RTT: HTTP HEADERS уже были в Initial-пакете. В любом случае первый байт запроса отправляется после 1 RTT (или 0 RTT для 0-RTT)." },
    { prompt: "Потеря пакета в потоке 3 (CSS-файл). Что происходит с потоками 1 и 2 (HTML + JS)?", reveal: "Потоки 1 и 2 продолжаются без изменений. QUIC повторно передаёт только потерянные пакеты потока 3. Потоки имеют независимые порядковые номера; потеря в одном не блокирует доставку в других. TCP+HTTP/2 остановил бы ВСЕ потоки до повторной передачи потерянного TCP-сегмента." },
    { prompt: "Телефон пользователя переключается с WiFi на LTE посреди загрузки. Что происходит?", reveal: "Ядро Бэа обнаруживает смену IP и отправляет фрейм PATH_CHALLENGE с новым source IP. Свен отвечает PATH_RESPONSE с тем же Connection ID. Все потоки в полёте продолжаются по новому пути после ~1 дополнительного RTT. TCP+HTTP/2 разорвал бы соединение; Бэа пришлось бы перезагружать страницу." },
  ]}
/>

<TraceScenario
  id="mid-trace-ws"
  lessonSlug="05-quic-websocket-paths"
  lang="ru"
  scenario="Проследите отказ из-за WebSocket backpressure и правильное решение."
  steps={[
    { prompt: "Свен рассылает 1 000 сообщений/с 5 000 подключённым WebSocket-клиентам. Один клиент (Бэа) медленный — обработка каждого сообщения занимает 10 мс. Что происходит?", reveal: "Скорость обработки Бэа — 100 сообщ/с; Свен отправляет ей 1 000 сообщ/с. Send buffer Свена для соединения Бэа заполняется (обычно 256 КБ буфер ядра). После заполнения буфера системный вызов write() у Свена возвращает WOULDBLOCK." },
    { prompt: "Наивный сервер: spin loop на WOULDBLOCK. Что происходит?", reveal: "Серверный поток занят в ожидании записи в сокет. CPU растёт. Сообщения другим клиентам ставятся в очередь. Пропускная способность сервера для всех 5 000 клиентов падает, потому что один медленный клиент занимает поток сервера или такт event loop." },
    { prompt: "Второй наивный подход: терять сообщения при заполнении буфера. Что происходит?", reveal: "Приложение Бэа получает неполный поток. Для совместного редактора это потеря данных. Для дашборда — устаревшее или несогласованное состояние. Приложение должно реализовать собственный протокол повтора/ресинхронизации — сложно." },
    { prompt: "Правильный подход: распространение backpressure.", reveal: "Сервер обнаруживает WOULDBLOCK и прекращает читать из входящего потока Бэа (приостанавливает receive-сторону). Receive buffer ядра Бэа заполняется, TCP window сжимается до нуля, приложение Бэа прекращает отправлять. Обе стороны приостанавливаются. Когда исходящий буфер Свена опустошается (Бэа догоняет), Свен возобновляет чтение от Бэа. Полный backpressure-цикл — без потери данных, без CPU-spike." },
    { prompt: "Если Бэа никогда не догоняет (офлайн, всегда медленная). Какая стратегия таймаута?", reveal: "Сервер устанавливает максимальную глубину очереди на клиента. Если глубина превышает порог дольше T секунд, сервер отправляет WebSocket Close-фрейм (opcode 8, код 1001 Going Away) и завершает соединение. Логика переподключения на стороне клиента обнаруживает закрытие и переподключается. Порядковые номера на уровне приложения позволяют клиенту запросить пропущенные сообщения при переподключении." },
  ]}
/>

<Quiz
  id="mid-q1"
  lessonSlug="05-quic-websocket-paths"
  lang="ru"
  question="Независимость потоков — главное преимущество QUIC над HTTP/2+TCP. Какой конкретный отказ она предотвращает?"
  choices={[
    { label: "Head-of-line blocking: потерянный TCP-сегмент останавливает все HTTP/2-потоки соединения до повторной передачи", correct: true },
    { label: "Задержку TLS-рукопожатия на новых соединениях", misconception: "HoL blocking — это о зависании потоков в полёте, а не о стоимости handshake." },
    { label: "Сбои DNS-запросов, влияющие на все потоки", misconception: "DNS разрешается до соединения; независимость потоков не влияет на DNS." },
    { label: "Слишком долгую проверку сертификата", misconception: "Проверка сертификата — это проблема TLS, отдельная от мультиплексирования потоков." },
  ]}
/>

<Quiz
  id="mid-q2"
  lessonSlug="05-quic-websocket-paths"
  lang="ru"
  question="Почему маскирование WebSocket-фреймов клиент→сервер обязательно, а сервер→клиент — нет?"
  choices={[
    { label: "У серверов более быстрые CPU, поэтому маскирование менее затратно", misconception: "Стоимость CPU не является причиной асимметричных правил маскирования." },
    { label: "Маскирование клиент→сервер предотвращает атаки отравления кэша, когда вредоносная страница обманывает промежуточный кэш, чтобы тот сохранил поддельный HTTP-ответ", correct: true },
    { label: "Спецификация WebSocket была написана до того, как симметричное шифрование стало стандартом", misconception: "Причина — конкретно отравление кэша, а не историческая случайность." },
    { label: "Трафик сервер→клиент уже зашифрован TLS", misconception: "TLS шифрует оба направления; маскирование — дополнительный слой для конкретной атаки, а не замена TLS." },
  ]}
/>

</Explanation>

<SequenceDiagram
  label="QUIC объединяет транспортное и TLS-рукопожатие в один round-trip: Клиент отправляет QUIC Initial-пакет, несущий TLS ClientHello, Сервер отвечает транспортными параметрами QUIC плюс TLS ServerHello и сертификатом, затем Клиент отправляет HTTP/3-запрос, и Сервер возвращает ответ — 1 RTT на новое соединение вместо двух у TCP"
  hue="var(--d-network)"
  actors={["Клиент", "Сервер"]}
  messages={[
    { from: "Клиент", to: "Сервер", label: "QUIC Initial + TLS ClientHello" },
    { from: "Сервер", to: "Клиент", label: "QUIC params + ServerHello + Cert" },
    { from: "Клиент", to: "Сервер", label: "QUIC ACK + HTTP/3 GET /" },
    { from: "Сервер", to: "Клиент", label: "HTTP/3 200 ответ" },
  ]}
  caption="TCP требует 3-стороннего рукопожатия (1 RTT) до начала TLS (ещё RTT); QUIC несёт TLS ClientHello внутри первого пакета, поэтому новое соединение стоит 1 RTT вместо 2. На тёплом соединении 0-RTT позволяет Клиенту вложить HTTP-запрос в первый пакет — но только безопасно для идемпотентных методов, ведь перехваченные 0-RTT данные можно воспроизвести."
/>

<KeyTakeaway>HTTP/3 устраняет 1 RTT на новых соединениях и переживает смену IP; WebSocket обеспечивает постоянные двунаправленные фреймы, но требует явной обработки backpressure для предотвращения тихой потери данных или CPU-spike.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="05-quic-websocket-paths-retrieval"
  lang="ru"
  questions={[
    { q: "Миграция соединения QUIC: что такое Destination Connection ID и почему он переживает смену IP?", a: "Destination Connection ID (DCID) — непрозрачный токен, выбранный Свеном при handshake и присутствующий в каждом заголовке пакета QUIC. Он идентифицирует соединение независимо от IP-четвёрки. При смене IP Бэа отправляет PATH_CHALLENGE фрейм с новым IP, но тем же DCID. Свен проверяет с PATH_RESPONSE и продолжает использовать то же состояние соединения — окно перегрузки, потоки, ключи TLS — по новому пути." },
    { q: "Что происходит, если балансировщик нагрузки маршрутизирует QUIC-пакеты по source IP вместо Connection ID при миграции?", a: "После смены IP первый PATH_CHALLENGE пакет от нового IP Бэа будет направлен к другому backend-серверу, не имеющему состояния для этого DCID. Этот сервер отправляет stateless reset или QUIC CONNECTION_REFUSED. Миграция Бэа завершается неудачей; ей нужно заново устанавливать соединение. Решение: настроить QUIC load balancer на маршрутизацию по Destination Connection ID (DCID-based routing, поддерживается Nginx QUIC, Envoy, Cloudflare)." },
    { q: "Объясните, как WebSocket backpressure распространяется с уровня приложения вниз до flow control TCP.", a: "Медленный потребитель приложения → send buffer Свена заполняется → write() возвращает WOULDBLOCK → сервер приостанавливает чтение из входящего потока Бэа → receive buffer ядра Бэа заполняется → рекламируемый TCP receive window сжимается до нуля → ядро Бэа не может отправлять больше данных → приложение Бэа получает backpressure. Вся цепочка распространяется без потери данных или CPU spin." },
  ]}
/>

<Recap lang="ru">
HTTP/3 поверх QUIC решает два ограничения TCP: head-of-line blocking (потерянный пакет больше не останавливает все потоки) и нестабильность при смене IP (Connection ID QUIC переживает переключения WiFi→мобильный через PATH_CHALLENGE/RESPONSE). Объединённый handshake QUIC + TLS стоит 1 RTT для новых соединений и 0 RTT для тёплых возобновлений, но 0-RTT небезопасен для неидемпотентных запросов из-за риска повтора. WebSocket обновляет HTTP-соединение до постоянного полнодуплексного канала с помощью opcode-фреймов и маскирования; его основной отказ — коллапс backpressure при медленном потребителе, заполняющем send buffer — правильное решение — распространение backpressure с уровня приложения до flow control TCP, а не потеря сообщений или spin на WOULDBLOCK.
</Recap>
