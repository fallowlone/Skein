---
concepts:
- packet-capture
- dns-resolution-trace
- tls-handshake-trace
- http3-alt-svc
- protocol-diagnosis
estMin: 14
lang: ru
level: senior
order: 9
prereqs: []
slug: quiz-code
sources:
- https://hpbn.co/
- https://datatracker.ietf.org/doc/html/rfc8446
- https://datatracker.ietf.org/doc/html/rfc9114
status: ready
summary: "Чтение реального вывода инструментов — захват tcpdump, трассировка dig, TLS-рукопожатие curl и согласование HTTP/3 через Alt-Svc — и диагностика того, что байты на самом деле говорят."
title: 'Всё вместе: чтение трасс и захватов'
track: networking
unit: 12-putting-it-together
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Производственная диагностика — это чтение вывода инструментов, а не цитирование RFC. Четыре реальных захвата охватывают весь стек: трасса пакетов, обход резолвера, TLS-рукопожатие и апгрейд до HTTP/3. Прочитай каждый и скажи, что на самом деле произошло на проводе.
</Hook>

<Goal lang="ru">
Отработай senior-приём: открой захват, найди строку, которая рассказывает историю, и переведи сырой вывод протокола в одно предложение о том, что сделала сеть и что делать дальше.
</Goal>

## Захват 1 — tcpdump открытия соединения

```
12:00:00.000000 IP 10.0.0.5.51514 > 93.184.216.34.443: Flags [S], seq 1001, win 64240, length 0
12:00:00.056120 IP 93.184.216.34.443 > 10.0.0.5.51514: Flags [S.], seq 7001, ack 1002, win 65535, length 0
12:00:00.056300 IP 10.0.0.5.51514 > 93.184.216.34.443: Flags [.], ack 7002, win 64240, length 0
12:00:00.056480 IP 10.0.0.5.51514 > 93.184.216.34.443: Flags [P.], seq 1002:1519, ack 7002, length 517
```

<Quiz
  id="12-putting-it-together-code-1"
  lessonSlug="12-putting-it-together"
  lang="ru"
  question="Что показывают эти четыре пакета и какова доминирующая стоимость до сих пор?"
  choices={[
    { label: "TCP three-way handshake (S, S., ack) завершился за ~56 мс — один региональный RTT — а четвёртый пакет (517-байтный PSH) — это TLS ClientHello, открывающий шифрованное рукопожатие. Стоимость пока — один round-trip географии.", correct: true },
    { label: "Это полный HTTPS-запрос и ответ; 517-байтный пакет — тело HTML.", misconception: "Прикладные данные ещё не обменивались. TLS даже не завершён — 517-байтный PSH это ClientHello. Байты рукопожатия пока не несут HTTP." },
    { label: "Шторм ретрансмиссий: четыре пакета значит три потеряны.", misconception: "Это SYN, SYN-ACK, ACK, затем ClientHello — чистое рукопожатие, а не ретрансмиссии. Ретрансмиссии показали бы повторяющиеся seq и бо́льшие паузы." },
    { label: "Разрешение DNS, ведь показан IP назначения.", misconception: "IP уже разрешён до этих пакетов; DNS идёт отдельным запросом (обычно UDP/53) и здесь не появляется. Это чистый TCP плюс начало TLS." },
  ]}
/>

## Захват 2 — dig +trace

```
$ dig +trace www.example.com

.                  518400  IN  NS  a.root-servers.net.
com.               172800  IN  NS  a.gtld-servers.net.
example.com.       172800  IN  NS  a.iana-servers.net.
www.example.com.   60      IN  A   93.184.216.34
;; Received 56 bytes from 199.43.135.53#53(a.iana-servers.net) in 48 ms
```

<Quiz
  id="12-putting-it-together-code-2"
  lessonSlug="12-putting-it-together"
  lang="ru"
  question="У финальной A-записи TTL 60. Сайт стоит за CDN. Во что обходится этот TTL в 60 секунд и правильный ли это дефолт?"
  choices={[
    { label: "TTL 60 с означает, что резолверы переобходят дерево (или хотя бы переспрашивают авторитативный сервер) каждую минуту, поэтому стоимость холодного DNS повторяется чаще, чем нужно. Это даёт быстрый failover, но большинству сайтов лучше TTL ~300 с; короткий TTL держи для активного управления трафиком.", correct: true },
    { label: "TTL неважен, потому что dig +trace всегда обходит кэши.", misconception: "dig +trace обходит кэши, чтобы показать обход делегирования, но реальные клиенты соблюдают TTL 60 с. Низкий TTL действительно увеличивает, как часто пользователи платят за холодный DNS." },
    { label: "TTL 60 ускоряет разрешение, потому что запись меньше.", misconception: "TTL — это длительность кэширования, а не размер. Меньший TTL означает более частые lookup'ы — больше стоимости холодного DNS, а не быстрее разрешение." },
    { label: "TTL 60 принуждает DNSSEC-валидацию на каждом запросе.", misconception: "TTL управляет временем жизни в кэше; он никак не связан с тем, выполняется ли DNSSEC-валидация. Компромисс здесь — скорость failover против повторной задержки lookup'а." },
  ]}
/>

## Захват 3 — TLS-рукопожатие curl -v

```
* Connected to example.com (93.184.216.34) port 443
* ALPN: offers h2,http/1.1
* TLSv1.3 (OUT), TLS handshake, Client hello (1):
* TLSv1.3 (IN), TLS handshake, Server hello (2):
* TLSv1.3 (IN), TLS handshake, Encrypted Extensions (8):
* TLSv1.3 (IN), TLS handshake, Certificate (11):
* TLSv1.3 (IN), TLS handshake, CERT verify (15):
* TLSv1.3 (IN), TLS handshake, Finished (20):
* SSL connection using TLSv1.3 / TLS_AES_256_GCM_SHA384
* ALPN: server accepted h2
```

<Quiz
  id="12-putting-it-together-code-3"
  lessonSlug="12-putting-it-together"
  lang="ru"
  question="Это TLS 1.3-рукопожатие. Какие два факта senior читает с него сразу?"
  choices={[
    { label: "Это полное (не возобновлённое) 1-RTT-рукопожатие — сервер прислал Certificate и CERT verify, которые возобновление пропустило бы — и ALPN согласовал h2, так что по этому соединению пойдёт HTTP/2.", correct: true },
    { label: "Это 0-RTT возобновлённое рукопожатие, потому что TLS 1.3 всегда 0-RTT.", misconception: "Возобновлённое рукопожатие пропускает Certificate и CERT verify и использует PSK. Присутствие этих сообщений означает полное 1-RTT-рукопожатие со свежей проверкой сертификата." },
    { label: "ALPN провалился, поэтому соединение откатывается на HTTP/1.1.", misconception: "Последняя строка показывает, что сервер принял h2 — ALPN успешен и выбрал HTTP/2. Никакого отката тут нет." },
    { label: "Шифр TLS_AES_256_GCM_SHA384 означает, что это TLS 1.2.", misconception: "Это AEAD-шифр TLS 1.3, и каждая строка помечена TLSv1.3. Версия в трассе однозначна." },
  ]}
/>

## Захват 4 — Alt-Svc и апгрейд до HTTP/3

```
$ curl -sI https://example.com
HTTP/2 200
alt-svc: h3=":443"; ma=86400

$ curl --http3 -sI https://example.com
HTTP/3 200
```

<Quiz
  id="12-putting-it-together-code-4"
  lessonSlug="12-putting-it-together"
  lang="ru"
  question="Первый ответ пришёл по HTTP/2, но рекламирует h3. Почему браузер на самом первом визите всё равно доходит до страницы по HTTP/2?"
  choices={[
    { label: "HTTP/3 обнаруживается, а не согласуется in-band: первое соединение идёт по TCP+TLS (h2), а заголовок Alt-Svc говорит клиенту, что h3 доступен на UDP/443 на ma=86400 с. Браузер это кэширует и апгрейдится до QUIC на следующем соединении.", correct: true },
    { label: "Сервер сконфигурирован неправильно; верная настройка отдавала бы HTTP/3 на первом запросе.", misconception: "Это by design. Нельзя узнать, что HTTP/3 доступен, до подключения, поэтому первое соединение использует TCP, а Alt-Svc рекламирует h3 для будущих соединений." },
    { label: "curl не умеет HTTP/3, поэтому тихо откатился.", misconception: "Вторая команда с --http3 доходит до HTTP/3 нормально. Первая использовала дефолт (h2 по TCP), потому что h3 ещё не был обнаружен через Alt-Svc." },
    { label: "ma=86400 означает, что HTTP/3 валиден лишь для 86 400 запросов.", misconception: "ma (max-age) — это длительность в секундах — 24 часа — на которую кэшируется объявление Alt-Svc, а не счётчик запросов." },
  ]}
/>

<Recap lang="ru">
Чтение стека по его собственному выводу: tcpdump показывает SYN/SYN-ACK/ACK плюс ClientHello, так что виден один RTT TCP до любых TLS-данных; dig +trace раскрывает обход делегирования и TTL, решающий, как часто повторяется стоимость холодного DNS; TLS-трасса curl говорит полное-vs-возобновлённое (наличие Certificate) и какой ALPN-протокол победил; а заголовок Alt-Svc раскрывает, что HTTP/3 обнаруживается на предыдущем h2-соединении и апгрейдится до QUIC на следующем. Навык — найти ту единственную строку, что рассказывает историю, и превратить её в действие.
</Recap>
