---
concepts:
- round-trip-budget
- DNS-cold-warm
- TLS-resumption
- 0-RTT
- connection-coalescing
- congestion-window
- IW10
deepensInto:
- 06-observability-and-traces
estMin: 14
lang: ru
lessonType: topic
level: middle
order: 2
prereqs:
- 01-the-twelve-layers
- 03-tcp-handshake/02-sequence-numbers-and-state
- 04-dns-resolution/02-the-resolver-walk
- 05-tls-handshake/02-the-1rtt-handshake
slug: 02-dns-tcp-tls-sequence
sources:
- https://hpbn.co/
- https://datatracker.ietf.org/doc/html/rfc6928
- https://datatracker.ietf.org/doc/html/rfc8446
spiral:
- latency
- statefulness
status: ready
summary: Семь конкретных фаз от холодного DNS до первого зашифрованного байта с числами
  и рычагами оптимизации (возобновление TLS, 0-RTT, объединение соединений), которые
  устраняют round-trip, а не просто сокращают их.
title: 'DNS, TCP, TLS по очереди: куда уходят миллисекунды'
track: networking
unit: 12-putting-it-together
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import TraceScenario from "~/components/pedagogy/TraceScenario.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import NumbersCard from "~/components/layout/NumbersCard.astro";
import SequenceDiagram from "~/components/diagram/SequenceDiagram.astro";

<Hook>
Коллега сообщает, что сайт «ощущается медленным при первом посещении». Вы открываете трейс Lighthouse и видите 320 мс до первого байта. Понять, что именно съело это время — DNS, TCP или TLS — и какая оптимизация устраняет round-trip, а не просто сокращает их, — это разница между реальным исправлением и угадыванием.
</Hook>

<Crux>Первые три рукопожатия (DNS, TCP, TLS) строго последовательны; устранение любого из них экономит больше, чем оптимизация всех трёх вместе.</Crux>

<Explanation>

## Пофазный бюджет времени

Предположим **тёплый сайт с CDN** (пользователь в Лондоне, edge POP в Лондоне, ~25 мс RTT до edge):

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="text-sm w-full border-collapse">
    <thead>
      <tr class="bg-slate-100">
        <th class="border px-3 py-2 text-left">Фаза</th>
        <th class="border px-3 py-2 text-left">Холодный (первое посещение)</th>
        <th class="border px-3 py-2 text-left">Тёплый (повторное)</th>
        <th class="border px-3 py-2 text-left">Рычаг устранения</th>
      </tr>
    </thead>
    <tbody>
      <tr><td class="border px-3 py-1">DNS</td><td class="border px-3 py-1">30–100 мс (обход резолвера)</td><td class="border px-3 py-1">&lt;1 мс (кеш ОС)</td><td class="border px-3 py-1">dns-prefetch / длинный TTL</td></tr>
      <tr><td class="border px-3 py-1">TCP handshake</td><td class="border px-3 py-1">~25 мс (1 RTT региональный)</td><td class="border px-3 py-1">0 мс (пул соединений)</td><td class="border px-3 py-1">keep-alive / preconnect</td></tr>
      <tr><td class="border px-3 py-1">TLS 1.3 новый</td><td class="border px-3 py-1">~25 мс (1 RTT)</td><td class="border px-3 py-1">0 мс (0-RTT возобновление)</td><td class="border px-3 py-1">session ticket / PSK</td></tr>
      <tr><td class="border px-3 py-1">HTTP-запрос</td><td class="border px-3 py-1">~25 мс (1 RTT)</td><td class="border px-3 py-1">~25 мс</td><td class="border px-3 py-1">попадание в кеш CDN edge</td></tr>
      <tr><td class="border px-3 py-1">Итого до TTFB</td><td class="border px-3 py-1">105–175 мс</td><td class="border px-3 py-1">25–30 мс</td><td class="border px-3 py-1">—</td></tr>
    </tbody>
  </table>
</div>

**Ключевой инсайт:** DNS и TCP устраняются кешированием и пулом соединений при повторных посещениях. TLS устраняется возобновлением сессии (PSK). При тёплом повторном посещении TTFB может упасть с 175 мс до менее 30 мс — не потому что сеть стала быстрее, а потому что round-trip были устранены полностью.

## Фаза 1: DNS (~30 мс холодный, &lt;1 мс тёплый)

Браузер спрашивает Rex: «Какой IP у example.com?» Rex проверяет кеш. Кеш тёплый → отвечает за микросекунды. Кеш холодный → Rex обходит иерархию: корневой nameserver → nameserver TLD .com → авторитативный nameserver example.com. Каждый переход — один сетевой round-trip. Холодный DNS-запрос в Северной Америке или Европе занимает 30–100 мс.

**Ловушка TTL.** Если оператор CDN установил TTL=60 с для быстрого failover, большинство пользователей платят цену холодного DNS намного чаще, чем необходимо. TTL=300 с — лучший вариант для большинства случаев: пять минут кеширования в резолвере, при этом failover происходит менее чем за пять минут.

**Оптимизация: `dns-prefetch`.** Добавление `<link rel="dns-prefetch" href="//cdn.example.com">` в HTML просит браузер разрешить ожидаемые hostname во время разбора страницы, до того как основной парсер дойдёт до реальных URL ресурсов. Стоимость: несколько миллисекунд DNS, перемещённых с критического пути.

## Фаза 2: TCP handshake (1 RTT, 10–100 мс по географии)

Bea отправляет SYN → Sven отвечает SYN-ACK → Bea отправляет ACK. Три пакета, один round-trip. Время определяется географией:
- Лондон — Франкфурт: ~10–15 мс.
- Лондон — Нью-Йорк: ~56 мс (по данным *High Performance Browser Networking*, скорость света в оптоволокне).
- Лондон — Калифорния: ~100 мс.

После рукопожатия **начальное congestion window TCP** (IW=10, по RFC 6928) позволяет передать до 10 пакетов (~14,6 КБ) без ожидания ACK. Если первый ответ превышает IW, Bea должна ждать ACK, прежде чем получить больше — одна скрытая причина того, почему важно держать HTML в пределах 14 КБ.

**Оптимизация: `preconnect`.** `<link rel="preconnect" href="//fonts.googleapis.com" crossorigin>` говорит браузеру открыть TCP + TLS к ожидаемым origin до того, как основной парсер до них доберётся. Правило: применять к двум-трём критически важным origin (CDN, шрифты, API), но не ко всем.

## Фаза 3: TLS 1.3 handshake (1 RTT новый, 0 RTT возобновление)

Bea отправляет ClientHello (key share, cipher suites). Sven отвечает ServerHello (выбранный cipher, сертификат, server key share). Bea проверяет сертификат по подписи Cara. Обе стороны выводят общие ключи. Стоимость: один round-trip (~25 мс региональный). Если Bea уже видела Sven раньше, она отправляет **pre-shared key (PSK)** из session ticket, сохранённого при прошлом визите. Sven принимает без повторной отправки сертификата. Стоимость: **ноль дополнительных round-trip** — TLS handshake встраивается в существующее QUIC или TCP соединение.

**Данные 0-RTT.** При TLS 1.3 с 0-RTT возобновлением поверх QUIC (HTTP/3) Bea может отправить HTTP-запрос внутри того же начального QUIC-пакета, что и TLS ClientHello — до того как сервер ответил. Это экономит один RTT на тёплых соединениях. 0-RTT безопасен только для **идемпотентных методов** (GET, HEAD, OPTIONS). Для POST или PUT 0-RTT несёт риск replay-атаки. Серверы должны отклонять 0-RTT для изменяющих состояние методов с кодом `425 Too Early`.

<Inset kind="edgecase" lang="ru">
Объединение соединений (connection coalescing): если два origin имеют один IP и сертификат (например, `api.example.com` и `cdn.example.com` за одним Cloudflare edge), браузер может повторно использовать одно HTTP/2 или /3 соединение для обоих. Это полностью устраняет TCP + TLS handshake для второго origin. Coalescing происходит незаметно; вы получаете выгоду, если размещаете несколько поддоменов за одним CDN.
</Inset>

<TraceScenario
  id="mid-trace-cold"
  lessonSlug="02-dns-tcp-tls-sequence"
  lang="ru"
  scenario="Трассируйте первое посещение страницы с холодного кеша до TTFB."
  steps={[
    { prompt: "Пользователь вводит URL. Что делает браузер первым?", reveal: "Разбирает URL, проверяет собственный кеш HSTS/SVCB, затем посылает DNS-запрос через резолвер ОС или свой DoH-стек." },
    { prompt: "DNS возвращает IP. Что дальше?", reveal: "Браузер открывает TCP-сокет к этому IP и начинает 3-стороннее рукопожатие (SYN → SYN-ACK → ACK). Если HTTP/3 поддерживается, вместо этого может попытаться QUIC Initial packet, который объединяет TCP-эквивалентный транспорт с TLS." },
    { prompt: "TCP готов. Начинается TLS. Что первым отправляет Bea?", reveal: "ClientHello: список поддерживаемых cipher suite, key shares (для TLS 1.3 ECDHE) и поддерживаемые расширения (SNI, ALPN, session ticket для возобновления если есть)." },
    { prompt: "Sven отвечает ServerHello + Certificate. Что проверяет Bea?", reveal: "Bea верифицирует цепочку сертификатов Sven до доверенного корневого CA (Cara). Проверки: сертификат не истёк, домен совпадает с SNI, подпись действительна, не отозван (CRL/OCSP)." },
    { prompt: "TLS готов. Bea отправляет HTTP-запрос. Сервер обрабатывает и возвращает первый байт. Итоговый TTFB?", reveal: "DNS (~50 мс холодный) + TCP (~25 мс региональный) + TLS (~25 мс новый) + HTTP RTT (~25 мс) = ~125 мс TTFB. Тёплое повторное: DNS &lt;1 мс + TCP 0 мс (пул) + TLS 0 мс (0-RTT) + HTTP ~25 мс = ~25 мс." },
  ]}
/>

<TraceScenario
  id="mid-trace-warm"
  lessonSlug="02-dns-tcp-tls-sequence"
  lang="ru"
  scenario="Трассируйте тот же сайт через 30 минут — тёплый кеш."
  steps={[
    { prompt: "DNS кеширован в резолвере ОС. Сколько времени занимает DNS?", reveal: "&lt;1 мс. Браузер тоже может иметь собственный DNS-кеш (Chrome кеширует на 60 секунд), тоже доли миллисекунды." },
    { prompt: "TCP — что изменилось при тёплом соединении?", reveal: "Если HTTP keep-alive или пул соединений HTTP/2+/3 активен, TCP уже установлен. Браузер отправляет HTTP-запрос по существующему соединению — нулевая стоимость установки TCP." },
    { prompt: "TLS — что изменилось?", reveal: "Возобновление сессии TLS 1.3 (PSK из кешированного session ticket) пропускает проверку сертификата. При HTTP/3 0-RTT может отправить запрос до первого TLS-ответа сервера — устраняя тот round-trip." },
    { prompt: "Статические ресурсы кешированы. Что делает браузер для закешированных ресурсов?", reveal: "Читает с дискового кеша браузера (~10 мс каждый, параллельно). Для ресурсов с действующим max-age: никакого сетевого трафика. Для ресурсов с no-cache: отправляет условный GET с If-None-Match. CDN возвращает 304 Not Modified (пустое тело, только заголовки)." },
    { prompt: "Итоговое время до LCP при тёплой загрузке?", reveal: "Часто менее 100 мс. DNS &lt;1 + TCP 0 (пул) + TLS 0 (0-RTT) + HTML CDN hit ~25 мс + рендеринг ~60 мс. Узким местом становится разбор JavaScript и layout, а не сеть." },
  ]}
/>

<Quiz
  id="mid-q1"
  lessonSlug="02-dns-tcp-tls-sequence"
  lang="ru"
  question="0-RTT возобновление экономит round-trip, но небезопасно для POST-запросов. Почему?"
  choices={[
    { label: "POST-тела слишком большие для начального QUIC-пакета", misconception: "Ограничения на размер есть, но настоящий риск — идемпотентность, а не размер." },
    { label: "Злоумышленник, перехвативший зашифрованный 0-RTT пакет, может воспроизвести его позже, заставив сервер дважды обработать тот же POST", correct: true },
    { label: "POST требует нового TLS handshake по спецификации HTTP", misconception: "Спецификация HTTP этого не требует; риск — replay-атака, а не правило протокола." },
    { label: "0-RTT не может зашифровать POST-заголовки", misconception: "0-RTT шифрует всё; проблема — replay, а не шифрование." },
  ]}
/>

<DragOrder
  id="mid-drag-1"
  lessonSlug="02-dns-tcp-tls-sequence"
  lang="ru"
  prompt="Упорядочите оптимизации по количеству устраняемых round-trip (от наибольшего к наименьшему):"
  items={[
    "Попадание в кеш CDN edge (устраняет DNS + TCP + TLS + round-trip к origin)",
    "TLS 0-RTT возобновление (устраняет 1 TLS RTT)",
    "Keep-alive соединение (устраняет TCP handshake для последующих запросов)",
    "dns-prefetch (переносит DNS с критического пути, не устраняет round-trip)",
  ]}
/>

</Explanation>

<SequenceDiagram
  label="Полная последовательность соединения при первом посещении в строгом порядке: Браузер запрашивает у Резолвера DNS, Резолвер возвращает IP, затем Браузер и Сервер выполняют трёхстороннее рукопожатие TCP, затем рукопожатие TLS 1.3, затем Браузер отправляет HTTP-запрос, и Сервер возвращает ответ"
  hue="var(--d-network)"
  actors={["Браузер", "Резолвер", "Сервер"]}
  messages={[
    { from: "Браузер", to: "Резолвер", label: "DNS-запрос: example.com?" },
    { from: "Резолвер", to: "Браузер", label: "DNS-ответ: 93.184.x.x" },
    { from: "Браузер", to: "Сервер", label: "TCP SYN" },
    { from: "Сервер", to: "Браузер", label: "TCP SYN-ACK" },
    { from: "Браузер", to: "Сервер", label: "TCP ACK + TLS ClientHello" },
    { from: "Сервер", to: "Браузер", label: "TLS ServerHello + Certificate" },
    { from: "Браузер", to: "Сервер", label: "TLS Finished + HTTP GET /" },
    { from: "Сервер", to: "Браузер", label: "HTTP 200 ответ" },
  ]}
  caption="DNS разрешается первым, потому что Браузеру нужен IP до открытия любого сокета; TCP должен завершиться до обмена ключами TLS; TLS должен завершиться до шифрования HTTP-запроса. Каждое рукопожатие — один строгий round-trip; устранение любого из них (кеш, пул, возобновление) экономит больше, чем сокращение всех трёх."
/>

<KeyTakeaway>Устранение round-trip (0-RTT, пул соединений, попадание в кеш CDN) даёт больший выигрыш, чем их сокращение; соотношение TTFB тёплый/холодный наглядно объясняет, почему кеширование — наиболее эффективный инструмент.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="02-dns-tcp-tls-sequence-retrieval"
  lang="ru"
  questions={[
    { q: "Объясните, почему 0-RTT TLS 1.3 экономит round-trip, но опасен для неидемпотентных запросов.", a: "0-RTT позволяет клиенту отправить HTTP-запрос внутри того же TLS/QUIC пакета, что и handshake — экономя один RTT на тёплых соединениях. Риск: злоумышленник, перехвативший зашифрованные 0-RTT данные, может воспроизвести их позже, заставив сервер дважды обработать тот же запрос. Для GET (идемпотентный) replay безвреден. Для POST, создающего строку или переводящего деньги, replay катастрофичен. Защита на стороне сервера: отклонять 0-RTT для неидемпотентных методов с кодом 425 Too Early." },
    { q: "Что такое начальное congestion window TCP (IW=10) и почему оно важно для загрузки страниц?", a: "IW=10 (RFC 6928) означает, что отправитель может передать до 10 TCP-сегментов (~14,6 КБ) без ожидания ACK. Если HTML-ответ превышает 14 КБ, браузер должен ждать ACK перед получением остатка — добавляя один RTT. Поддержание критического HTML в пределах 14 КБ поэтому является значимой оптимизацией для новых соединений." },
    { q: "Почему `<link rel=preconnect>` к десяти origin скорее вредит, чем помогает?", a: "Каждый preconnected origin удерживает простаивающий TCP-сокет и требует затрат на DNS + TCP + TLS. Подключение к десяти origin заранее фрагментирует сетевые ресурсы — часть соединений простаивает и истекает неиспользованными, тратя полосу пропускания и блокируя слоты. Правило: preconnect к двум-трём критически важным origin (основной CDN, шрифты, API); всё остальное пусть разогревается по требованию." },
  ]}
/>

<Recap lang="ru">
Первые три фазы — DNS, TCP, TLS — строго последовательны и вместе занимают 100–175 мс при первом посещении. Каждую можно сократить географией (CDN edge) или устранить кешированием (TTL DNS, keep-alive соединений, возобновление TLS-сессии). При тёплом повторном посещении все три могут схлопнуться до менее 1 мс суммарно, повторно используя кеш DNS ОС, пул соединений и TLS PSK. HTTP/3 0-RTT идёт дальше, встраивая HTTP-запрос в TLS ClientHello — но только безопасно для идемпотентных методов. Начальное congestion window TCP в 10 пакетов (~14,6 КБ) — скрытый порог: ответы, превышающие его, платят лишний round-trip, прежде чем сервер сможет отправить больше данных.
</Recap>
