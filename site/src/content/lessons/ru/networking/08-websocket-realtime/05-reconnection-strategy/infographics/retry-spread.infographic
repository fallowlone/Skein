infographic chart-column-simple
data
  title Разброс retry после массового разрыва: jitter против фикс. интервала
  desc То же число клиентов, тот же рестарт — jitter превращает SYN-всплеск в плавный рамп, который сервер успевает принять
  values
    - label Без jitter (фикс. 1с) — thundering herd
      value 1
    - label Jittered backoff (±50%)
      value 10
theme
  palette #f43f5e #14b8a6
