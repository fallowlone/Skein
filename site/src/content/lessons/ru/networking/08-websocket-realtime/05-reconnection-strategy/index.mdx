---
concepts:
- reconnection
- exponential-backoff
- jitter
- thundering-herd
- message-resumption
- message-id
- at-least-once
- deduplication
deepensInto:
- 07-production-architecture
estMin: 12
lang: ru
lessonType: topic
level: senior
order: 5
prereqs:
- 08-websocket-realtime/04-backpressure-and-flow
slug: 05-reconnection-strategy
sources:
- https://datatracker.ietf.org/doc/html/rfc6455
- https://aws.amazon.com/builders-library/timeouts-retries-and-backoff-with-jitter/
- https://www.phoenixframework.org/blog/the-road-to-2-million-websocket-connections
spiral:
- statefulness
- latency
status: ready
summary: Как переподключаться без краша собственного сервера — jittered exponential
  backoff, message ID и гарантия доставки at-least-once с дедупликацией.
title: 'Реконнект: jittered backoff, thundering herd, восстановление сообщений'
track: networking
unit: 08-websocket-realtime
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import TraceScenario from "~/components/pedagogy/TraceScenario.astro";
import NumbersCard from "~/components/layout/NumbersCard.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";
import Infographic from "~/components/diagram/Infographic.astro";
import retrySpread from "./infographics/retry-spread.svg?raw";

<Hook>
Сервер перезапускается для деплоя. Все 10 миллионов подключённых клиентов мгновенно получают close-код 1006. Все повторяют попытку ровно через 1 секунду. Десять миллионов SYN-пакетов одновременно бьют в load balancer. Он рушится. Сервер так и не восстанавливается. WebSocket не имеет встроенного реконнекта — а значит, один наивный цикл retry отделяет вас от уничтожения собственного сервиса во время maintenance-окна.
</Hook>

<Crux>WebSocket не имеет реконнекта; без jittered backoff одновременные retry создают thundering herd, не давая серверу восстановиться.</Crux>

<Explanation>

## Что происходит при разрыве

Когда WebSocket-соединение обрывается — сетевой глюк, перезапуск сервера, idle timeout прокси — клиент получает событие `close` с кодом 1006 (аномальное закрытие — close-фрейм не получен). Приложение должно решить: переподключаться ли, и если да — когда и как.

Сам WebSocket не предоставляет никакой логики реконнекта. Это полностью зона ответственности приложения.

## Проблема thundering herd

Thundering herd (стадо громового буйвола — термин для скачкообразного синхронного шторма запросов) — это самоатака: ваши же клиенты обрушивают ваш восстанавливающийся сервер. Спросите себя: если сервер перезапустится прямо сейчас, что сделают 10 миллионов клиентов в следующую секунду? Если ответ «повторят попытку одновременно» — вы уже написали атаку. Вот как синхронизированные retry уничтожают восстановление.

Если каждый клиент повторяет попытку с одинаковым фиксированным интервалом после разрыва, retry приходят на сервер синхронизированными волнами:

- T=0: сервис падает, 10 миллионов клиентов отключаются.
- T=1с: все 10 миллионов клиентов повторяют попытку одновременно.
- Сервер получает 10 миллионов SYN-пакетов за ~1 секунду.
- SYN backlog заполняется. Новые соединения дропаются.
- T=2с: все 10 миллионов снова повторяют попытку с тем же интервалом.
- Сервер не получает тихого момента для восстановления.

Даже exponential backoff без jitter не помогает: синхронизированное удвоение всё равно создаёт синхронизированные волны.

## Jittered exponential backoff

Решение — добавить **случайность (jitter)** к таймингу retry каждого клиента, чтобы 10 миллионов попыток размазались по окну в несколько секунд вместо одновременного прихода.

Алгоритм:

```
attempt = 0
base_ms = random(100, 500)      // разное для каждого клиента
max_ms  = 60_000

while not connected:
  delay = min(base_ms * 2^attempt, max_ms)
  delay = delay * random(0.5, 1.5)   // ±50% jitter
  sleep(delay)
  attempt += 1
  try_connect()
```

Пример разброса для перезапуска сервера с 100k клиентов:
- Клиент A выбирает base=150 мс, jitter → retry через 186 мс
- Клиент B выбирает base=340 мс, jitter → retry через 412 мс
- Клиент C выбирает base=210 мс, jitter → retry через 288 мс

100k retry размазаны по 10+ секундам. Сервер обрабатывает их небольшими пакетами и восстанавливается за 1–2 retry-окна, а не рушится.

<Infographic
  svg={retrySpread}
  label="Две колонки сравнивают окно разброса retry после массового разрыва: фиксированный интервал 1 секунда без jitter сжимает все retry в менее чем 1 секунду (thundering herd), а jittered exponential backoff с ±50% размазывает те же retry по 10+ секундам."
  caption="Тот же разрыв, то же число клиентов: jitter растягивает окно retry примерно в 10× — сервер получает мелкие поглощаемые пакеты вместо одного SYN-всплеска."
  hue="var(--d-infra)"
/>

<NumbersCard
  id="card-reconnect-numbers"
  title="Параметры стратегии реконнекта"
  data-lesson-visual
  stats={[
    { label: "Рекомендуемый диапазон базовой задержки", value: "100–500 мс (случайно для каждого клиента)" },
    { label: "Коэффициент jitter", value: "±50% от вычисленной задержки" },
    { label: "Потолок максимальной задержки", value: "30–60 секунд" },
    { label: "Разброс retry для 10M клиентов с jitter", value: "10+ секунд" },
    { label: "Разброс retry без jitter (фиксированный 1с интервал)", value: "< 1 секунды (thundering herd)" },
    { label: "Библиотеки с jittered backoff по умолчанию", value: "grpc-go, Phoenix, @tensorflow/tfjs" },
  ]}
/>

## Восстановление сообщений и гарантия at-least-once

Реконнект восстанавливает соединение. Он не восстанавливает потерянные сообщения. Любые сообщения, отправленные сервером пока клиент был отключён — или отправленные клиентом, но не подтверждённые сервером — исчезают, если приложение их не отслеживает.

**Паттерн:**

1. **Каждое сообщение получает ID, сгенерированный клиентом.** Отправитель держит сообщение в очереди retry до получения ACK для этого ID.
2. **Сервер ACK-ирует каждый message ID.** Отправитель убирает подтверждённые сообщения из очереди retry.
3. **При реконнекте клиент переотправляет неподтверждённые сообщения.** Сервер проверяет Redis stream или БД: если message ID уже существует — отправляет ACK клиенту, но не публикует повторно (предотвращает дубликаты для других подписчиков).

Это реализует **at-least-once доставку с идемпотентной дедупликацией** — ни одно сообщение не потеряно, ни одно не доставлено другим клиентам более одного раза.

Для дедупликации серверу нужно долговечное состояние, переживающее перезапуски. Типичное хранилище:
- **Redis stream** (быстрый, постоянный, упорядоченный, с настраиваемым retention).
- **PostgreSQL** (долговечный, с поиском, подходит для истории сообщений с требованиями аудита).

Поток реконнекта клиента:

```
1. Переподключиться с backoff.
2. После установки соединения отправить { type: "resume", lastSeenId: "msg-4291" }.
3. Сервер читает stream с msg-4292 и доставляет пропущенные сообщения.
4. Сервер ACK-ирует доставку.
5. Клиент подтверждает получение и обновляет lastSeenId.
```

<Inset kind="why" lang="ru">
**Почему не session-layer resumption на транспортном уровне?** Некоторые протоколы (QUIC, MPTCP) обрабатывают миграцию соединения на транспортном уровне — приложение не знает о реконнектах. WebSocket сидит на TCP, у которого нет миграции. Собственный session layer (токен + Redis resume state) добавляет 50–100 мс задержки при реконнекте, но работает на любом TCP/TLS WebSocket-деплое. Выбор: простота + универсальность кастомного session layer vs. миграция на транспортном уровне от WebTransport/QUIC (более узкая поддержка браузеров и серверов по состоянию на 2026).
</Inset>

<TraceScenario
  id="ws-sr-trace-1"
  pieceSlug="05-reconnection-strategy"
  lang="ru"
  scenario="Трассируйте каскадный отказ на WebSocket-сервере после кратковременного сетевого раздела — с jitter и без."
  steps={[
    { prompt: "1 Гбит/с канал глючит 5 секунд. Все 100 000 WebSocket-клиентов получают close-код 1006. Что видит сервер?", reveal: "Счётчик активных соединений мгновенно падает со 100k до нуля. ОС закрывает сокеты. Память сервера падает по мере освобождения состояния соединений." },
    { prompt: "Без jitter: все 100k клиентов повторяют попытку с фиксированным интервалом 1 секунда. Что бьёт по серверу?", reveal: "100 000 TCP SYN-пакетов приходят за ~1 секунду. SYN backlog заполняется. ОС начинает дропать SYN-пакеты или включает SYN cookies. Легитимные новые соединения блокируются." },
    { prompt: "Сервер ещё запускается, когда приходит вторая синхронизированная волна (T=2с). Что происходит?", reveal: "Сервер ещё под нагрузкой от первой волны. Вторая снова обрушивает SYN backlog. Сервер не успевает принимать соединения достаточно быстро для восстановления между волнами." },
    { prompt: "С jitter: клиенты выбирают base=100–500 мс, добавляют ±50% jitter, удваивают при каждой попытке. Опишите первые 10 секунд трафика реконнектов.", reveal: "100k клиентов размазывают первую попытку по окну 100–750 мс. Сервер принимает несколько сотен в секунду — легко в пределах мощности. К T=10с 95%+ клиентов переподключились и сервер здоров." },
    { prompt: "При реконнекте клиент отправляет resume-токен с lastSeenId=msg-4291. Redis stream сервера имеет msg-4292 по msg-4300. Что делает сервер?", reveal: "Читает Redis stream с msg-4292 и доставляет 9 пропущенных сообщений клиенту по порядку. ACK-ирует доставку. Клиент обновляет lastSeenId до msg-4300. Ни одно сообщение не потеряно." },
  ]}
/>

<Quiz
  id="ws-sr-quiz-1"
  pieceSlug="05-reconnection-strategy"
  lang="ru"
  question="Вы проектируете load balancer перед WebSocket-кластером. Клиент отправляет сообщение серверу A, затем сеть разделяется и клиент переподключается к серверу B. Почему sticky session routing (клиент всегда идёт к одному серверу) безопаснее балансировки по запросу, даже если у серверов есть pub/sub?"
  choices={[
    { label: "Без sticky session состояние приложения клиента на сервере A теряется при подключении к серверу B", correct: true },
    { label: "Pub/sub автоматически ломается если клиент переключается между серверами", misconception: "Pub/sub — бэкенд-состояние, не per-client; оно работает на любом сервере." },
    { label: "Sticky session снижает задержку, избегая накладных расходов на handoff между серверами", misconception: "Задержка — реальная, но второстепенная проблема; главная — потеря неподтверждённого локального состояния на сервере A." },
    { label: "Без sticky session новый сервер не может вычислить Sec-WebSocket-Accept", misconception: "Accept вычисляется заново при каждом handshake; он не привязан к предыдущему серверу." },
  ]}
/>

<Quiz
  id="ws-sr-quiz-2"
  pieceSlug="05-reconnection-strategy"
  lang="ru"
  question="Мобильный WebSocket-клиент теряет связь, переподключается через 45 секунд и отправляет сообщение из очереди с message ID, на который ещё не получил ACK. Сервер имеет Redis stream с retention 24 часа. Каково правильное действие сервера?"
  choices={[
    { label: "Немедленно ACK-ировать message ID и опубликовать в stream, даже если запись уже существует", misconception: "Это вызовет дублирующую доставку другим подписчикам — at-least-once превращается в at-most-twice." },
    { label: "Проверить наличие message ID в Redis stream; если есть — отправить ACK клиенту, но не публиковать повторно; если нет — опубликовать и ACK-ировать", correct: true },
    { label: "Дропнуть сообщение, так как оно пришло с опозданием 45 секунд и устарело", misconception: "Возраст сообщения не важен при дедупликации по ID. Поздняя доставка корректна; дроп — потеря данных." },
    { label: "Перенаправить на оригинальный сервер A, к которому клиент был подключён ранее", misconception: "Идентичность сервера не важна при репликации состояния. Новый сервер обработает его при наличии доступа к stream." },
  ]}
/>

</Explanation>

<FlowDiagram
  label="Машина состояний реконнекта: подключённый клиент получает close 1006, ждёт задержку jittered exponential backoff, переподключается, затем шлёт resume с last-seen message ID, чтобы переиграть пропущенные сообщения и вернуться в подключённое состояние"
  hue="var(--d-network)"
  perRow={5}
  nodes={[
    { id: "conn", label: "Connected", sub: "фреймы идут" },
    { id: "drop", label: "Disconnect", sub: "close 1006" },
    { id: "backoff", label: "Jittered backoff", sub: "base·2^n ±50%" },
    { id: "reconn", label: "Reconnect", sub: "новое рукопожатие" },
    { id: "resume", label: "Resume", sub: "lastSeenId → replay" },
  ]}
  edges={[
    { from: "conn", to: "drop", label: "сеть / рестарт" },
    { from: "drop", to: "backoff", label: "запланировать retry" },
    { from: "backoff", to: "reconn", label: "после задержки" },
    { from: "reconn", to: "resume", label: "отправить resume-токен" },
    { from: "resume", to: "conn", label: "пропущенные доставлены" },
  ]}
  caption="Jitter размазывает миллионы retry по секундам, избегая thundering herd; resume по lastSeenId переигрывает только пропущенные сообщения — at-least-once без дубликатов."
/>

<KeyTakeaway>WebSocket не имеет встроенного реконнекта — реализуйте jittered exponential backoff (base 100–500 мс, ±50% jitter, потолок 60 с), чтобы размазать retry 10 миллионов клиентов по секундам, а не миллисекундам. Дополните message ID + Redis stream dedup для at-least-once доставки без дубликатов при реконнекте.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="05-reconnection-strategy-retrieval"
  lang="ru"
  questions={[
    { q: "Объясните, почему exponential backoff без jitter всё равно создаёт проблему thundering herd.", a: "Без jitter каждый клиент удваивает задержку на одинаковый коэффициент от одной базы. После первого retry в T=1с второй у каждого клиента — в T=2с, третий — в T=4с — волны синхронизированы, даже хотя и разнесены. Jitter рандомизирует задержку каждого клиента независимо, так что 10M клиентов размазываются по непрерывному окну, а не приходят синхронизированными волнами каждые 2^n секунд." },
    { q: "Какое долговечное хранилище требуется на сервере для восстановления сообщений и почему оно должно переживать перезапуски?", a: "Постоянный лог сообщений — обычно Redis stream (быстрый, упорядоченный, с настраиваемым retention) или таблица в БД. Он должен переживать перезапуски, потому что при реконнекте клиент отправляет lastSeenId; сервер должен прочитать все сообщения после этого ID. Если лог в памяти процесса — перезапуск потеряет всю историю и вынудит переподключившихся клиентов воспроизводить с нуля или терять сообщения." },
    { q: "Опишите полный поток реконнекта с восстановлением сообщений: что отправляет клиент, что возвращает сервер и какая дедуп-проверка предотвращает дублирующую доставку?", a: "1) Клиент переподключается с backoff, затем отправляет { type: resume, lastSeenId: msg-4291 }. 2) Сервер читает stream с msg-4292, доставляет каждое пропущенное сообщение по порядку. 3) Клиент ACK-ирует каждое сообщение и обновляет lastSeenId. 4) Для сообщений, переотправляемых клиентом (не ACK-ированных), сервер проверяет stream: если message ID уже есть — отправляет ACK, но не публикует повторно (дедуп); если нет — публикует и ACK-ирует. Это гарантирует at-least-once доставку клиенту и exactly-once публикацию в stream." },
  ]}
/>

<Recap lang="ru">
WebSocket не имеет встроенного механизма реконнекта — close-код 1006 срабатывает и приложение должно решать что делать. Без jitter (случайного разброса) массовый разрыв вызывает thundering herd: все клиенты повторяют попытку с синхронизированными интервалами, создавая SYN-волны, не дающие серверу восстановиться. Jittered exponential backoff — экспоненциальный откат со случайным разбросом (base 100–500 мс на клиента, ±50% случайный jitter, удвоение при каждой попытке, потолок 30–60 с) — размазывает 10 миллионов retry по секундам. Это стандартный подход во всех крупных WebSocket-библиотеках. Восстановление сообщений совмещает Redis stream (или БД) с client-generated message ID: отправитель держит неподтверждённые сообщения в локальной очереди, переотправляет при реконнекте, и сервер дедуплицирует проверкой stream перед публикацией, достигая at-least-once доставки без дубликатов. Sticky session routing помогает сохранить in-flight состояние на сервере, к которому клиент был подключён ранее, хотя полная долговечность требует вынесенного состояния в Redis или БД. Теперь, когда планируете maintenance-окно для сервиса с миллионами WebSocket-клиентов, jittered backoff — не приятная мелочь, а единственное, что стоит между вашим окном обслуживания и самоучинённой аварией.
</Recap>
