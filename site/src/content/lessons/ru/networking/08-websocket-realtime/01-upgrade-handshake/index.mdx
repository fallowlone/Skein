---
concepts:
- websocket
- http-upgrade
- 101-switching-protocols
- sec-websocket-key
- sec-websocket-accept
- full-duplex
deepensInto:
- 02-frame-format
- 03-transport-alternatives
estMin: 10
lang: ru
lessonType: topic
level: junior
order: 1
prereqs:
- 03-tcp-handshake/01-the-three-way-handshake
- 05-tls-handshake/01-what-tls-does
- 06-http-versions/01-http-fundamentals
slug: 01-upgrade-handshake
sources:
- https://datatracker.ietf.org/doc/html/rfc6455
- https://developer.mozilla.org/en-US/docs/Web/API/WebSockets_API/Writing_WebSocket_servers
- https://hpbn.co/websocket/
spiral:
- statefulness
- latency
status: ready
summary: Как обычный HTTP-запрос превращается в двусторонний канал сообщений за один
  round-trip — и что каждый заголовок апгрейда означает на практике.
title: 'WebSocket: HTTP-апгрейд до постоянного соединения'
track: networking
unit: 08-websocket-realtime
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import MetaphorComplete from "~/components/pedagogy/MetaphorComplete.astro";
import NumbersCard from "~/components/layout/NumbersCard.astro";
import SequenceDiagram from "~/components/diagram/SequenceDiagram.astro";

<Hook>
Вы открываете Discord. Друг отправляет сообщение — оно появляется мгновенно, без перезагрузки страницы. За этим стоит постоянное TCP-соединение, которое HTTP сам по себе обеспечить не может. WebSocket даёт обеим сторонам возможность отправлять данные в любой момент без ожидания запроса.
</Hook>

<Crux>WebSocket апгрейдит HTTP до двустороннего канала фреймов за один round-trip — после 101 обе стороны шлют свободно, без ожидания запроса.</Crux>

<Explanation>

## Проблема, которую HTTP не решает

HTTP — это request-response: клиент спрашивает, сервер отвечает, разговор окончен. Для живого чата, котировок, многопользовательских игр, совместного редактирования — всего, где **сервер должен пушить данные без ожидания нового запроса** — HTTP структурно не подходит. Каждый HTTP-запрос добавляет минимум один полный round-trip задержки прежде, чем данные могут потечь обратно.

WebSocket решает это, превращая существующее HTTP-соединение в постоянный **двусторонний канал**, где любая сторона отправляет в любой момент.

## Метафора

С HTTP вы отправляете письма: пишете, посылаете, ждёте ответа. С WebSocket поднимаете трубку: как только соединились, обе стороны говорят свободно, не ожидая очереди.

## Рукопожатие апгрейда шаг за шагом

Соединение начинается как обычный HTTP/1.1-запрос. Клиент сигнализирует, что хочет переключить протокол:

```
GET /chat HTTP/1.1
Host: example.com
Upgrade: websocket
Connection: Upgrade
Sec-WebSocket-Key: dGhlIHNhbXBsZSBub25jZQ==
Sec-WebSocket-Version: 13
```

Каждый заголовок несёт конкретный смысл:

- **`Upgrade: websocket`** — объявляет целевой протокол.
- **`Connection: Upgrade`** — сообщает промежуточным прокси, что это смена протокола, а не обычный keepalive.
- **`Sec-WebSocket-Key`** — 16 случайных байт в base64. Сервер использует это, чтобы доказать, что намеренно обработал апгрейд (а не отдал кэшированный ответ — см. врезку ниже).
- **`Sec-WebSocket-Version: 13`** — указывает RFC 6455, единственную версию на практике.

Сервер проверяет запрос (метод GET, HTTP/1.1, корректные заголовки) и отвечает:

```
HTTP/1.1 101 Switching Protocols
Upgrade: websocket
Connection: Upgrade
Sec-WebSocket-Accept: s3pPLMBiTxaQ9kYGzzhZRbK+xOo=
```

Значение `Sec-WebSocket-Accept` вычисляется как:

```
base64(SHA-1(Sec-WebSocket-Key + "258EAFA5-E914-47DA-95CA-C5AB0DC85B11"))
```

Фиксированная строка (`258EAFA5-…`) — это magic GUID из RFC. После ответа 101 **обе стороны перестают говорить HTTP**. TCP-соединение теперь является чистым WebSocket-потоком.

<NumbersCard
  id="card-ws-handshake-numbers"
  title="Факты о WebSocket-рукопожатии"
  data-lesson-visual
  stats={[
    { label: "Дополнительные round-trips для апгрейда", value: "0 (использует существующее HTTP-соединение)" },
    { label: "HTTP-код при успешном апгрейде", value: "101 Switching Protocols" },
    { label: "RFC, определяющий WebSocket", value: "RFC 6455 (2011)" },
    { label: "Sec-WebSocket-Version в продакшне", value: "13 (единственная используемая)" },
    { label: "Overhead заголовка фрейма (маленький сервер→клиент)", value: "2 байта" },
    { label: "Задержка сообщения после рукопожатия", value: "0–5 мс" },
  ]}
/>

## Сценарий от начала до конца

Вы открываете Discord в браузере:

1. Браузер делает TCP + TLS к `gateway.discord.gg`.
2. Браузер отправляет HTTP Upgrade-запрос выше.
3. Сервер отвечает 101. Обе стороны прекращают HTTP-парсинг.
4. С этого момента сервер Discord пушит фреймы с сообщениями в браузер в момент, когда друг что-то отправил. Браузер отправляет фреймы (индикаторы набора, сообщения) в другую сторону без открытия нового соединения.
5. Когда вы закрываете вкладку, обмениваются **close-фреймами** и TCP-соединение завершается.

<Inset kind="why" lang="ru">
**Почему вычисление Sec-WebSocket-Accept защищает от cache-poisoning.** В ранние дни веба вредоносный JavaScript на `site-a.com` мог открыть TCP-соединение к промежуточному прокси и отправить байты, похожие на валидный HTTP-ответ. Если прокси кэшировал их, другие пользователи могли получить вредоносный контент. Связка `Sec-WebSocket-Key` + GUID + SHA-1 делает практически невозможным подделку заголовка Accept без знания точного ключа, который сгенерирует сервер — прокси не может закэшировать результат вычисления, которого сам не проводил.
</Inset>

<Quiz
  id="ws-jr-quiz-1"
  pieceSlug="01-upgrade-handshake"
  lang="ru"
  question="Зачем WebSocket требует HTTP Upgrade-запроса, а не просто открывает сырое TCP-соединение на новый порт?"
  choices={[
    { label: "Потому что прокси не понимают сырой TCP", misconception: "Прокси прекрасно понимают TCP. Реальная причина — соединение уже начинается как HTTP и должно формально переключиться." },
    { label: "Потому что соединение уже существует как HTTP, и протокол должен формально согласиться прекратить HTTP и начать говорить WebSocket-фреймы", correct: true },
    { label: "Чтобы зашифровать соединение", misconception: "TLS — отдельный слой, применяется до WebSocket-апгрейда." },
    { label: "Для обратной совместимости со старыми серверами", misconception: "Это forward-looking дизайн для повторного использования существующих HTTP-соединений." },
  ]}
/>

<Quiz
  id="ws-jr-quiz-2"
  pieceSlug="01-upgrade-handshake"
  lang="ru"
  question="После завершения WebSocket-апгрейда с ответом 101 Switching Protocols — для чего используется протокол HTTP?"
  choices={[
    { label: "HTTP по-прежнему используется для каждого фрейма", misconception: "WebSocket-фреймы имеют собственный бинарный формат; HTTP больше не используется." },
    { label: "HTTP-заголовки всё ещё отправляются с каждым фреймом", misconception: "После апгрейда HTTP отбрасывается; WebSocket использует компактный бинарный формат фреймов." },
    { label: "Соединение больше не HTTP — это чистый WebSocket до закрытия", correct: true },
    { label: "HTTP используется только для control-фреймов", misconception: "Все фреймы, и control, и data, используют бинарный формат WebSocket после апгрейда." },
  ]}
/>

<DragOrder
  id="ws-jr-drag-1"
  pieceSlug="01-upgrade-handshake"
  lang="ru"
  prompt="Упорядочите шаги WebSocket-рукопожатия:"
  items={[
    "Клиент отправляет HTTP GET с Upgrade: websocket и Sec-WebSocket-Key",
    "Сервер проверяет запрос и вычисляет Sec-WebSocket-Accept",
    "Сервер отвечает 101 Switching Protocols с заголовком Accept",
    "Обе стороны прекращают HTTP; соединение становится двусторонним WebSocket",
    "Любая сторона теперь может отправлять фреймы в любой момент",
  ]}
/>

<MetaphorComplete
  id="ws-jr-metaphor-1"
  pieceSlug="01-upgrade-handshake"
  lang="ru"
  setup="Заполните пропуск: WebSocket — это как _______, где обе стороны могут говорить свободно, не ждя очереди."
  accepted={["телефонный звонок", "разговор", "диалог", "чат", "беседа"]}
  canonical="телефонный звонок (или живой разговор)"
  explanation="В отличие от HTTP, где клиент делает запрос и ждёт ответ, WebSocket позволяет обеим сторонам отправлять в любой момент — как в настоящем разговоре, где любой может заговорить."
/>

</Explanation>

<SequenceDiagram
  label="Рукопожатие апгрейда WebSocket: клиент отправляет HTTP GET с Upgrade: websocket, сервер отвечает 101 Switching Protocols, затем обе стороны обмениваются WebSocket-фреймами в любом направлении"
  hue="var(--d-network)"
  actors={["Клиент", "Сервер"]}
  messages={[
    { from: "Клиент", to: "Сервер", label: "GET /chat — Upgrade: websocket, Sec-WebSocket-Key" },
    { from: "Сервер", to: "Клиент", label: "101 Switching Protocols — Sec-WebSocket-Accept" },
    { from: "Клиент", to: "Сервер", label: "WebSocket-фрейм" },
    { from: "Сервер", to: "Клиент", label: "WebSocket-фрейм" },
  ]}
  caption="Один HTTP round-trip переключает соединение: после 101 HTTP исчезает, и любая сторона шлёт бинарные фреймы в любой момент."
/>

<KeyTakeaway>WebSocket повторно использует HTTP/1.1-соединение: один GET + ответ 101 переводит обе стороны на бинарный протокол фреймов, где любая сторона отправляет в любой момент. Обмен Sec-WebSocket-Key/Accept доказывает, что сервер намеренно обработал апгрейд, а не отдал кэш.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="01-upgrade-handshake-retrieval"
  lang="ru"
  questions={[
    { q: "В одном предложении: почему сервер не может просто слать повторяющиеся HTTP-запросы, чтобы пушить данные клиенту?", a: "Каждый HTTP-запрос требует, чтобы клиент его инициировал и сервер ответил — полный round-trip задержки каждый раз; WebSocket держит одно постоянное соединение открытым, чтобы сервер мог пушить мгновенно без ожидания нового запроса, полностью убирая RTT-стоимость между каждым пушем." },
    { q: "Что сигнализирует ответ 101 Switching Protocols и что происходит на проводе сразу после?", a: "101 сигнализирует, что обе стороны согласились прекратить HTTP и переключиться на протокол WebSocket-фреймов; сразу после того, как сервер отправил ответ 101, TCP-соединение несёт сырые WebSocket-фреймы в обоих направлениях — HTTP-заголовки больше не обмениваются." },
    { q: "Зачем Sec-WebSocket-Key добавляют к фиксированному GUID перед хешированием, а не хешируют напрямую?", a: "Фиксированный GUID (258EAFA5-…) гарантирует, что только WebSocket-осведомлённый сервер может вычислить правильное значение Accept — обычный HTTP-кэш или прокси, просто эхирующий запрос, не знает GUID и не может подделать корректный Sec-WebSocket-Accept." },
  ]}
/>

<Recap lang="ru">
HTTP — это только pull: каждая доставка данных требует, чтобы клиент сначала спросил. WebSocket снимает это ограничение, апгрейдя обычное HTTP/1.1-соединение до полнодуплексного канала. Апгрейд требует ровно одного дополнительного HTTP round-trip: GET с `Upgrade: websocket` и случайным `Sec-WebSocket-Key`, в ответ — `101 Switching Protocols` и вычисленный через SHA-1 `Sec-WebSocket-Accept`. После ответа 101 HTTP не используется — обе стороны обмениваются компактными бинарными фреймами, причём любая сторона может инициировать. Весь WebSocket-handshake не добавляет ни одного TCP-соединения и ни одной TLS-сессии. Задержка сообщения после рукопожатия составляет 0–5 мс, ограниченная только сетевым RTT.
</Recap>
