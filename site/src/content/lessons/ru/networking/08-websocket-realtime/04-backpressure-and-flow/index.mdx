---
concepts:
- backpressure
- tcp-receive-window
- send-queue
- high-water-mark
- slow-client
- oom
- broadcast-server
deepensInto:
- 05-reconnection-strategy
- 06-scaling-and-limits
estMin: 12
lang: ru
lessonType: topic
level: middle
order: 4
prereqs:
- 08-websocket-realtime/01-upgrade-handshake
- 03-tcp-handshake/03-flow-and-congestion-control
slug: 04-backpressure-and-flow
sources:
- https://datatracker.ietf.org/doc/html/rfc6455
- https://www.phoenixframework.org/blog/the-road-to-2-million-websocket-connections
- https://hpbn.co/websocket/
spiral:
- statefulness
- latency
status: ready
summary: Как медленные клиенты заполняют TCP-окна, раздувают очереди отправки на уровне
  приложения и обрушивают серверы — и паттерн high-water mark, который это предотвращает.
title: 'Backpressure в WebSocket: когда клиенты не успевают'
track: networking
unit: 08-websocket-realtime
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import TraceScenario from "~/components/pedagogy/TraceScenario.astro";
import DebugLog from "~/components/pedagogy/DebugLog.astro";
import NumbersCard from "~/components/layout/NumbersCard.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";
import Infographic from "~/components/diagram/Infographic.astro";
import evictionStrategies from "./infographics/eviction-strategies.svg?raw";

<Hook>
Ваш WebSocket broadcast-сервер на тестах справляется с 10 000 клиентами. В production 100 клиентов на медленном мобильном интернете начинают отставать. Память сервера растёт. GC-паузы удлиняются. Потом срабатывает OOM killer и укладывает весь сервис. Это классический режим отказа WebSocket — и он не имеет никакого отношения к протоколу.
</Hook>

<Crux>Медленные клиенты раздувают очереди отправки; без high-water mark и политики вытеснения сервер исчерпывает RAM и падает.</Crux>

<Explanation>

## TCP-окно приёма с точки зрения приложения

Прежде чем устранять инцидент с backpressure (обратным давлением, когда получатель не успевает за отправителем), нужно понять точно, где давление накапливается — от очереди приложения вниз до буфера ядра. Когда видите рост памяти и удлинение GC-пауз, именно эта цепочка объясняет почему.

TCP даёт каждому сокету **окно приёма** — количество байт, которые получатель готов принять, прежде чем отправитель должен приостановиться. Когда приложение на принимающей стороне читает байты, окно открывается; когда отстаёт — окно сжимается до нуля и отправитель останавливается.

С точки зрения сервера:
1. Сервер вызывает `send(socket, message)`.
2. Ядро копирует сообщение в **буфер отправки** сокета.
3. TCP передаёт из буфера отправки с учётом рекламируемого клиентом окна приёма.
4. Если окно клиента равно нулю (его буфер чтения заполнен), TCP прекращает передачу.
5. Вызов `send()` ядра **блокируется**, если буфер отправки заполнен, или возвращает EAGAIN в неблокирующем режиме.

Для broadcast-сервера с тысячами клиентов это создаёт критическую асимметрию: быстрые клиенты опустошают буферы за миллисекунды; медленные оставляют байты лежать в буферах отправки ядра, и когда те заполняются — сервер застревает.

## Очередь отправки на уровне приложения

Production WebSocket-серверы никогда не блокируются на `send()`. Вместо этого они поддерживают **очередь отправки уровня приложения** на соединение. Когда сервер рассылает сообщение, он ставит его в очередь для каждого соединения и сразу возвращает управление. Фоновый поток записи опустошает каждую очередь в ядро так быстро, как позволяет TCP-окно клиента.

Это решает проблему блокировки — но создаёт новую: **неограниченный рост очереди**.

Рассмотрим сценарий:
- Сервер рассылает сообщение 100 КБ каждые 10 мс.
- 100 из 10 000 клиентов медленные — запросы к БД блокируют их поток чтения.
- Очередь каждого медленного клиента растёт: 100 сообщений/секунду × 100 КБ = 10 МБ/секунду.
- Через 10 секунд: 100 медленных клиентов × 10 МБ × 10 с = **10 ГБ** RAM в очередях.
- Срабатывает OOM killer.

<NumbersCard
  id="card-backpressure-numbers"
  title="Математика отказа от backpressure"
  data-lesson-visual
  stats={[
    { label: "Скорость рассылки (пример)", value: "100 КБ каждые 10 мс = 10 МБ/с на клиента" },
    { label: "Рост RAM на медленного клиента при такой скорости", value: "10 МБ/с" },
    { label: "RAM при 100 медленных клиентах через 10 с", value: "10 ГБ" },
    { label: "Рекомендуемый целевой p95 глубины очереди", value: "< 5 сообщений" },
    { label: "Рекомендуемый порог медленного клиента", value: "< 0,1% соединений" },
    { label: "Рекомендуемый потолок суммарных байт в очереди", value: "< 5% кучи" },
  ]}
/>

## Паттерн high-water mark

Решение — **high-water mark**: ограничение очереди на соединение, триггерирующее вытеснение или backpressure.

Когда очередь соединения превышает high-water mark (например, 100 сообщений или 10 МБ), сервер выбирает:

**Вариант A — вытеснить соединение.** Принудительно закрыть медленного клиента с кодом 1013 ("try again later"). Клиент переподключится когда сеть освободится. Другие клиенты не затронуты. Стандартный выбор для broadcast-сервисов.

**Вариант B — дропать сообщения.** Для идемпотентных данных (биржевые цены, снимки игрового состояния), где последнее значение заменяет предыдущее — дропать старые сообщения и оставлять только N последних на соединение. Медленный клиент получит актуальное состояние при переподключении, а не устаревшую историю.

**Вариант C — приостановить продюсера.** Для peer-to-peer стримов (видеоконференции, передача файлов) — сигнализировать отправляющему приложению приостановиться до опустошения очереди ниже low-water mark. Это полное распространение backpressure.

<Infographic
  svg={evictionStrategies}
  label="Дерево решений, ветвящееся от превышения high-water mark на три стратегии вытеснения: Вариант A вытесняет соединение (broadcast-сервисы, close-код 1013); Вариант B дропает старые сообщения (идемпотентные данные, хранить N последних); Вариант C приостанавливает продюсера (peer-to-peer стримы, слить очередь ниже low-water mark)."
  caption="Три стратегии вытеснения не взаимозаменяемы — выбирайте по семантике данных: broadcast — вытеснять, идемпотентное состояние — дропать, peer-to-peer стримы — приостанавливать."
  hue="var(--d-infra)"
/>

## Режим отказа из-за idle timeout прокси

Есть второй распространённый режим отказа: прокси между клиентом и сервером имеет **60-секундный idle timeout**. Если в течение 60 секунд через TCP-соединение не проходят байты, прокси закрывает его — без отправки WebSocket close-фрейма.

Клиент и сервер об этом не подозревают, пока сервер не попытается отправить следующее сообщение: `send()` вернёт `EPIPE` или `ECONNRESET`. Клиент увидит событие `close` с кодом 1006 (аномальное закрытие). Соединение мертво.

**Решение**: сервер отправляет **ping-фрейм каждые 25–30 секунд**. Ping сбрасывает таймер idle прокси. Клиент должен ответить pong в разумное время (сервер может вытеснять клиентов, не ответивших pong в течение 10 секунд, как очистку устаревших соединений).

<Inset kind="edgecase" lang="ru">
**Сообщение 1 МБ, 10k клиентов, 1% медленных.** Сервер рассылает сообщение 1 МБ. Для 9 900 быстрых клиентов TCP буферизирует и доставляет за миллисекунды. Для 100 медленных сообщение лежит в очереди приложения. Если сервер рассылает 50 таких сообщений до обнаружения медленных клиентов: 100 клиентов × 50 сообщений × 1 МБ = **5 ГБ** раздутой памяти. Production-серверы непрерывно мониторят количество медленных клиентов и вытесняют их когда их доля превышает 0,5% соединений или суммарные байты в очереди превышают 5% кучи — что наступит раньше.
</Inset>

<TraceScenario
  id="ws-mid-trace-1"
  pieceSlug="04-backpressure-and-flow"
  lang="ru"
  scenario="Трассируйте инцидент с backpressure на WebSocket broadcast-сервере."
  steps={[
    { prompt: "Сервер рассылает сообщение 100 КБ 10 000 клиентам. Что делает TCP-стек сервера для быстрых клиентов?", reveal: "Для клиентов, в чьём буфере приёма ядра есть место, ядро копирует сообщение в буфер и немедленно возвращает управление серверу. send() не блокируется." },
    { prompt: "100 из 10 000 клиентов медленные (запрос к БД блокирует их поток чтения). Их буферы приёма ядра заполняются. Что видит сервер при вызове send() для медленного клиента?", reveal: "Если сервер вызывает send() синхронно и буфер отправки ядра заполнен (клиент не читает), send() заблокирует вызывающий поток до момента, когда клиент освободит место — не давая серверу отправлять другим клиентам." },
    { prompt: "Что делают production-серверы вместо блокировки?", reveal: "Ставят сообщение в очередь в буфер приложения (in-memory очередь на соединение). send() возвращает управление сразу. Приложение мониторит размер очереди; при слишком большом росте вытесняет медленного клиента или прекращает ему отправлять." },
    { prompt: "Сервер ставит 100 КБ на сообщение в очередь, рассылает каждые 10 мс, 100 клиентов медленные. Сколько RAM раздувается в секунду?", reveal: "100 клиентов × (1000мс / 10мс) × 100 КБ = 100 клиентов × 100 сообщений × 100 КБ = 1 ГБ в секунду. Через секунды сервер исчерпает RAM и упадёт." },
    { prompt: "Какую метрику должен мониторить сервер для обнаружения проблемы до краша?", reveal: "Суммарные байты в очереди по всем медленным соединениям, или количество соединений, чья глубина очереди превысила порог (например, > 10 ожидающих сообщений). При скачке метрики — вытеснять медленных клиентов или немедленно приостанавливать рассылку." },
  ]}
/>

<DebugLog
  id="ws-mid-debug-1"
  pieceSlug="04-backpressure-and-flow"
  lang="ru"
  title="Метрики приложения WebSocket broadcast-сервиса под backpressure"
  output={`2026-05-15T14:32:00Z broadcast_service[5234]:
  active_connections: 50234
  total_queued_bytes: 8589934592  (8 ГБ)
  slow_client_count: 127
  p95_queue_depth: 156 сообщений
  p99_queue_depth: 341 сообщений
  broadcast_latency_p99: 18432мс
  memory_usage: 15.2 ГБ / 16 ГБ кучи
  gc_pause_time: 1247мс

2026-05-15T14:32:15Z broadcast_service[5234]:
  active_connections: 49891  (-343 за 15с)
  total_queued_bytes: 10737418240  (10 ГБ)
  slow_client_count: 412
  p95_queue_depth: 287 сообщений
  p99_queue_depth: 502 сообщений
  broadcast_latency_p99: 32568мс
  memory_usage: 15.8 ГБ / 16 ГБ кучи
  gc_pause_time: 2100мс

2026-05-15T14:32:30Z broadcast_service[5234]: OOM killer вызван; процесс убит`}
  outputLang="log"
  question="Сервис рассылает 1 МБ в секунду 50k клиентам. Глубина очереди растёт, куча на 15,8 ГБ. Каков режим отказа и немедленное решение?"
  hint="Смотрите динамику количества медленных клиентов и скорость роста байт в очереди. При текущей скорости, когда куча достигнет 16 ГБ?"
  answer="Количество медленных клиентов прыгнуло с 127 до 412 за 15 секунд — клиенты отстают быстрее, чем восстанавливаются. Суммарные байты в очереди выросли с 8 до 10 ГБ за 15 секунд; при такой скорости куча достигнет 16 ГБ за ~30 секунд. Режим отказа — backpressure: 412 медленных клиентов не опустошают буферы сокетов, очереди приложения раздуваются. Немедленное решение: вытеснить 412 самых медленных клиентов (очередь выше high-water mark). Байты в очереди упадут на ~412 МБ; оставшиеся 49,5k здоровых клиентов опустошатся в нормальном темпе. Память сервера стабилизируется. Долгосрочное решение: непрерывно мониторить количество медленных клиентов; вытеснять при превышении 0,5%, или реализовать тиарованную доставку — быстрым клиентам немедленно, медленным — батчить или дропать устаревшие сообщения."
/>

<Quiz
  id="ws-mid-quiz-4"
  pieceSlug="04-backpressure-and-flow"
  lang="ru"
  question="Прокси между клиентом и сервером имеет 60-секундный idle timeout. За 59 секунд не отправлялось ни одного сообщения. На 60-й секунде прокси закрывает TCP-соединение без WebSocket close-фрейма. Какой close-код генерирует WebSocket-реализация клиента?"
  choices={[
    { label: "1000 — нормальное закрытие", misconception: "1000 означает, что обе стороны договорились о закрытии и обменялись close-фреймами. Прокси закрыл TCP-уровень без них." },
    { label: "1001 — going away", misconception: "1001 отправляется сервером при намеренном завершении." },
    { label: "1006 — аномальное закрытие (close-фрейм не получен)", correct: true },
    { label: "1011 — непредвиденное состояние сервера", misconception: "1011 отправляется сервером в его close-фрейме. Прокси не отправил никакого close-фрейма." },
  ]}
/>

</Explanation>

<FlowDiagram
  label="Путь отправки при backpressure: broadcast кладёт сообщение в очередь приложения на соединение, она сливается в kernel send buffer, ограниченный TCP-окном приёма медленного клиента; при превышении high-water mark сервер вытесняет соединение"
  hue="var(--d-network)"
  perRow={5}
  nodes={[
    { id: "bcast", label: "Broadcast", sub: "100 КБ / 10 мс" },
    { id: "queue", label: "Очередь приложения", sub: "на соединение" },
    { id: "sbuf", label: "Kernel send buffer", sub: "заполняется без слива" },
    { id: "win", label: "TCP recv window", sub: "→0 у медленного" },
    { id: "evict", label: "High-water mark", sub: "evict / drop / pause" },
  ]}
  edges={[
    { from: "bcast", to: "queue", label: "enqueue" },
    { from: "queue", to: "sbuf", label: "writer сливает" },
    { from: "sbuf", to: "win", label: "ограничено окном" },
    { from: "queue", to: "evict", label: "очередь растёт → срабатывает" },
  ]}
  caption="Застрявшее TCP-окно создаёт давление вверх — в kernel buffer, затем в очередь приложения; неограниченный рост ведёт к OOM, если high-water mark не вытеснит или не отбросит раньше."
/>

<KeyTakeaway>Медленные клиенты заполняют TCP-окна, вынуждая очереди приложения расти неограниченно. Устанавливайте high-water mark на соединение и вытесняйте или приостанавливайте медленных клиентов при его превышении. Отправляйте ping'и каждые 25–30 с против idle timeout прокси. Мониторьте количество медленных клиентов и суммарные байты в очереди — это ранние предупреждения до OOM.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="04-backpressure-and-flow-retrieval"
  lang="ru"
  questions={[
    { q: "Почему очередь отправки уровня приложения решает проблему блокировки, но создаёт новую — и как high-water mark решает обе?", a: "Очередь приложения позволяет send() возвращать управление немедленно (без блокировки потока) — сервер может поставить в очередь для всех 10k клиентов в тесном цикле. Но без ограничения очереди медленных клиентов растут неограниченно: каждое недоставленное сообщение буферизируется в RAM пока клиент не наверстает или сервер не упадёт. High-water mark ограничивает очередь (например, 100 сообщений или 10 МБ на соединение); при превышении сервер вытесняет соединение или дропает старые сообщения — жертвуя стабильностью соединения ради стабильности сервера." },
    { q: "Каков правильный интервал ping'а против idle timeout прокси и что делать если клиент не ответил pong?", a: "Отправлять ping каждые 25–30 секунд (idle timeout прокси обычно 60 секунд; ping при половине таймаута даёт два шанса в окне). Если клиент не ответил pong в течение ~10 секунд — считать соединение устаревшим и закрыть с кодом 1001 или 1006. Молчащий клиент либо мёртв (прокси закрыл TCP), либо перегружен CPU; в любом случае вытеснение правильно." },
    { q: "Назовите три стратегии вытеснения медленных клиентов и условия применения каждой.", a: "Вариант A — немедленное вытеснение (close 1013): правильно для broadcast-сервисов (биржевые тикеры, live-ленты), где каждый медленный клиент независим и может переподключиться. Вариант B — дроп старых сообщений: правильно для идемпотентных данных (снимки игрового состояния), где новейшее значение заменяет более ранние. Вариант C — приостановить продюсера (распространение backpressure): правильно для peer-to-peer стримов (видео, передача файлов), где важен каждый байт и отправитель должен замедлиться, а не терять данные." },
  ]}
/>

<Recap lang="ru">
Основной режим отказа WebSocket — backpressure: когда клиенты не успевают читать данные со скоростью отправки сервером, TCP-окна приёма сжимаются до нуля, буфер отправки ядра заполняется, и — если сервер использует очереди приложения для избегания блокировки — они растут неограниченно до исчерпания RAM процессом. Решение — high-water mark (верхняя граница очереди) на соединение, триггерирующий вытеснение (код 1013), дроп сообщений или приостановку продюсера в зависимости от семантики данных приложения. Второй режим отказа — idle timeout прокси: прокси с 60-секундным таймером закрывают тихие WebSocket-соединения без close-фрейма, генерируя close-код 1006. Ping-фреймы каждые 25–30 секунд это нейтрализуют. Ключевые метрики для мониторинга: количество медленных клиентов (целевое значение ниже 0,1% соединений), суммарные байты в очереди (целевое значение ниже 5% кучи) и p99 глубины очереди на соединение (целевое значение ниже 5 сообщений для интерактивных приложений). Теперь, когда память на broadcast-сервере начинает расти — первое, что проверяете, это количество медленных клиентов: если оно выше 0,1%, перед вами backpressure-инцидент и вытеснение — немедленное решение.
</Recap>
