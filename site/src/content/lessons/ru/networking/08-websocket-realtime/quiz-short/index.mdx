---
concepts:
- http-upgrade
- frame-masking
- transport-choice
- backpressure
- jittered-backoff
- scaling-ceilings
estMin: 14
lang: ru
level: senior
sources:
- https://datatracker.ietf.org/doc/html/rfc6455
- https://hpbn.co/websocket/
- https://www.phoenixframework.org/blog/the-road-to-2-million-websocket-connections
order: 9
prereqs: []
slug: quiz-short
status: ready
summary: "Промпты на свободное припоминание по всему юниту WebSocket. Сначала ответь своими словами, потом раскрой образцовый ответ и сравни."
title: 'WebSocket: тест на припоминание'
track: networking
unit: 08-websocket-realtime
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";

<Hook>
Припоминание сильнее перечитывания. На каждый промпт скажи или запиши полный ответ по памяти до того, как откроешь образцовый — именно усилие припоминания закрепляет юнит.
</Hook>

<Goal lang="ru">
Восстанови хребет юнита — upgrade, framing и masking, выбор транспорта, backpressure, переподключение с jitter и потолки масштаба — не подглядывая в уроки.
</Goal>

<RetrievalDrawer
  client:visible
  id="08-websocket-realtime-short-retrieval"
  lang="ru"
  questions={[
    { q: "Почему WebSocket делает upgrade существующего HTTP-соединения, а не открывает новый сырой TCP-сокет, и что происходит на проводе сразу после 101?", a: "Соединение уже существует как HTTP поверх TCP (и обычно TLS) на порту 443, так что переиспользование добавляет ноль round-trip и позволяет общей инфраструктуре — proxy, балансировщикам, файрволам — продолжать его маршрутизировать. Клиент шлёт GET с Upgrade: websocket и случайным Sec-WebSocket-Key; сервер отвечает 101 Switching Protocols с производным от SHA-1 Sec-WebSocket-Accept, который доказывает, что сервер намеренно обработал upgrade (а не закэшированный ответ). Сразу после 101 обе стороны перестают говорить на HTTP — то же TCP-соединение теперь несёт сырые бинарные WebSocket-frame в обе стороны, без дальнейших HTTP-заголовков." },
    { q: "Почему frame client-to-server обязаны быть маскированы, а frame server-to-client — нет?", a: "Masking — это защита от cache poisoning, а не шифрование. Вредоносный JavaScript на странице не может прочитать cross-origin ответ сервера (same-origin policy блокирует), но может открыть WebSocket к proxy и послать собранные байты. Masking заставляет клиента XOR-ить каждый байт payload свежим случайным 4-байтовым ключом в frame; поскольку ключ случаен per-frame, атакующий не может заранее собрать байты, которые и декодируются корректно, и выглядят как валидный HTTP-ответ для наивного proxy. Frame server-to-client не нуждаются в mask, потому что same-origin policy и так блокирует чтение этих байтов JavaScript-ом, так что эквивалентной атаки нет." },
    { q: "Назови два случая, где SSE — лучший дефолт, чем WebSocket, и один случай, где подойдёт только WebSocket.", a: "SSE выигрывает, когда (1) поток данных только server-to-client — ленты уведомлений, дашборды, тикеры цен — потому что EventSource браузера переподключается автоматически через Last-Event-ID без кастомного кода, и (2) путь идёт через строгие HTTP-proxy или корпоративные файрволы, ведь SSE — это обычный HTTP/1.1, который проходит любой proxy, тогда как WebSocket требует поддержки Upgrade, которую многие proxy ломают. WebSocket нужен, когда клиент шлёт данные часто и с низкой латентностью — совместные редакторы, мультиплеер, торговые терминалы — где frame в 2-6 байт по одному постоянному соединению лучше открытия нового HTTP-запроса на каждую отправку." },
    { q: "Пройди по сбою backpressure на broadcast-сервере и исправлению через high-water mark.", a: "Чтобы не блокироваться, сервер кладёт каждый broadcast в per-connection application send queue и сразу возвращается; фоновый писатель опустошает каждую очередь так быстро, как позволяет TCP-окно клиента. Быстрые клиенты опустошаются за миллисекунды, но медленные (мобильные каналы, заблокированные read-потоки) никогда не пустеют, так что очередь растёт без границ в RAM — напр. 100 KB каждые 10 мс это 10 MB/s на медленного клиента — пока процесс не уйдёт в OOM. Исправление — high-water mark: per-connection потолок (скажем, 100 сообщений или 10 MB). При превышении сервер либо выселяет клиента с close 1013, либо сбрасывает устаревшие сообщения (для идемпотентного состояния вроде снапшотов цен), либо приостанавливает producer (для peer-to-peer потоков). Мониторь slow-client count (цель ниже 0.1%) и total queued bytes (ниже 5% heap) как раннее предупреждение." },
    { q: "Почему exponential backoff без jitter всё равно вызывает thundering herd и каков правильный рецепт переподключения?", a: "Чистый exponential backoff — это детерминированное удвоение от той же базы, так что массовый разрыв заставляет каждого клиента делать retry в одни и те же моменты — 1 с, 2 с, 4 с — и синхронные SYN-волны продолжают переполнять backlog сервера, так что он не получает тихого момента для восстановления. Исправление — per-client jitter: выбрать случайную базу (100-500 мс), удваивать на каждой попытке, ограничить 30-60 с и умножать каждую задержку на случайный фактор (напр. от 0.5 до 1.5). Это размазывает десять миллионов retry по непрерывному окну, так что сервер принимает несколько сотен в секунду и восстанавливается за одно-два окна. Сочетай это с message ID плюс Redis stream, чтобы unACK'd сообщения можно было переслать и дедуплицировать для at-least-once доставки при переподключении." },
    { q: "Назови три независимых потолка на число соединений на WebSocket-сервер и как горизонтальный масштаб и HTTP/2 extended CONNECT их сдвигают.", a: "Потолки по порядку: (1) RAM — каждое idle-соединение стоит 2-50 KB состояния фреймворка плюс kernel-буферы, а permessage-deflate при дефолтном window_bits добавляет ~300 KB, так что 100k соединений могут сжечь 30 GB только на состоянии сжатия; (2) file descriptor — Linux по умолчанию даёт 1 024 на процесс, поднимается до 100k-1 M через ulimit и тюнинг ядра; (3) пропускная способность NIC — один лишь трафик keepalive ping насыщает 1 Gbps NIC около 1-2 M idle-соединений. HTTP/2 extended CONNECT (RFC 8441) мультиплексирует много WebSocket-соединений по одному TCP-соединению, срезая стоимость TCP-буферов и file descriptor примерно в 100 раз — но требует HTTP/2-осведомлённой цепочки proxy. За потолком одного сервера масштабируешься вширь: backbone pub/sub (Redis Streams) делает fan-out сообщений между серверами, а sticky sessions держат in-flight state каждого клиента на одном сервере." },
  ]}
/>

<Recap lang="ru">
Если ты смог восстановить каждый ответ по памяти, ты держишь хребет юнита: upgrade переиспользует существующее соединение, так что инфраструктура его маршрутизирует; masking защищает провод от cache poisoning; выбор транспорта следует за направлением потока данных; backpressure — каноничный OOM, ограничиваемый high-water mark; переподключение должно быть с jitter, иначе оно становится thundering herd; а масштаб упирается в потолки RAM, file descriptor и NIC, которые мультиплексирование HTTP/2 и fan-out pub/sub отодвигают.
</Recap>
