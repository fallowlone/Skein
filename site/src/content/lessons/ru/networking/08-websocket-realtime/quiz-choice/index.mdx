---
concepts:
- http-upgrade
- frame-masking
- backpressure
- jittered-backoff
- transport-choice
- pubsub-scaling
estMin: 14
lang: ru
level: senior
order: 8
prereqs: []
slug: quiz-choice
sources:
- https://datatracker.ietf.org/doc/html/rfc6455
- https://www.phoenixframework.org/blog/the-road-to-2-million-websocket-connections
- https://hpbn.co/websocket/
status: ready
summary: "Тест с выбором на синтез по всему юниту WebSocket: upgrade, framing, выбор транспорта, backpressure, переподключение, потолки масштаба и production-сбои."
title: 'WebSocket: тест с выбором ответа'
track: networking
unit: 08-websocket-realtime
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Шесть вопросов поперёк всего юнита. Ни один не про заучивание определения — каждый отражает решение, которое senior-инженер принимает, пока real-time-сервис деградирует под нагрузкой.
</Hook>

<Goal lang="ru">
Убедись, что связываешь upgrade, формат frame, выбор транспорта, backpressure, переподключение и потолки масштабирования в единую операционную модель — тот синтез, к которому вели отдельные уроки.
</Goal>

<Quiz
  id="08-websocket-realtime-choice-1"
  lessonSlug="08-websocket-realtime"
  lang="ru"
  question="Коллега предлагает открыть сырой TCP-сокет на отдельном порту, чтобы пропустить HTTP-upgrade и 'сэкономить round-trip'. Почему это неверное рассуждение?"
  choices={[
    { label: "Upgrade не добавляет лишних round-trip — он переиспользует уже существующее HTTP/TCP/TLS-соединение — а 101 как раз и позволяет общей инфраструктуре (proxy, балансировщики, тот же порт 443) маршрутизировать соединение. Сырой сокет на отдельном порту ничего не выигрывает и теряет всё это.", correct: true },
    { label: "Сырой TCP быстрее, потому что полностью пропускает вычисление Sec-WebSocket-Key.", misconception: "Обмен key/accept — это один SHA-1-хеш, микросекунды CPU. Он не добавляет round-trip и не является затратой по латентности — он доказывает, что сервер намеренно обработал upgrade, а не отдал закэшированный ответ." },
    { label: "Браузеры умеют открывать сырые TCP-сокеты, так что это сработает, просто менее портабельно.", misconception: "Браузеры вообще не могут открывать произвольные сырые TCP-сокеты; WebSocket API существует именно потому, что сырой TCP недоступен JavaScript на странице. Предпосылка невозможна, а не просто непортабельна." },
    { label: "Это верно — upgrade действительно стоит лишнего round-trip, который можно избежать.", misconception: "Upgrade идёт по уже существующему соединению; 101 — это ответ на тот же GET. Экономить нечего." },
  ]}
/>

<Quiz
  id="08-websocket-realtime-choice-2"
  lessonSlug="08-websocket-realtime"
  lang="ru"
  question="Кастомный WebSocket-клиент (вне браузера) шлёт твоему серверу немаскированные frame в стиле server-to-client. Должен ли сервер их принимать и зачем вообще нужен masking?"
  choices={[
    { label: "Отклонять: RFC 6455 требует, чтобы frame client-to-server были маскированы, поэтому сервер обязан закрыть соединение с protocol error. Masking — это защита от cache poisoning: случайный per-frame XOR-ключ мешает вредоносному JavaScript на странице собрать байты, которые наивный proxy примет за HTTP-ответ.", correct: true },
    { label: "Принимать — masking это просто шифрование, а доверенному клиенту шифровать не нужно.", misconception: "Masking — не шифрование; ключ передаётся открытым текстом в самом frame. Это структурная защита от cache poisoning, и RFC делает её обязательной для client-frame независимо от доверия." },
    { label: "Принимать — masking важен только для frame server-to-client.", misconception: "Всё наоборот: frame server-to-client никогда не маскируются (same-origin policy и так блокирует их чтение), а frame client-to-server обязаны быть маскированы всегда." },
    { label: "Отклонять, потому что немаскированные frame нельзя декодировать без ключа.", misconception: "Немаскированные frame декодируются тривиально — ключ применять не нужно. Причина отклонять — требование RFC и модель угрозы cache poisoning, а не сбой декодирования." },
  ]}
/>

<Quiz
  id="08-websocket-realtime-choice-3"
  lessonSlug="08-websocket-realtime"
  lang="ru"
  question="Дашборд пушит ~5 обновлений в минуту 100k пользователям, которые ничего не шлют обратно. Инженер тянется за WebSocket. Что лучше по умолчанию и почему?"
  choices={[
    { label: "SSE: поток данных только server-to-client, так что bidirectionality ничего не даёт. EventSource браузера переподключается автоматически через Last-Event-ID, проходит любой HTTP-proxy, а при 5 обновлениях/мин преимущество WebSocket по латентности несущественно.", correct: true },
    { label: "WebSocket всё равно лучше, потому что у него всегда ниже латентность.", misconception: "0-5 мс WebSocket против 3-8 мс SSE реально, но бессмысленно при 5 обновлениях в минуту. Это стоит тебе кастомной логики переподключения и риска совместимости с proxy без всякой выгоды для server-push-only потока." },
    { label: "Long-polling, потому что он работает через любой proxy.", misconception: "Long-polling тоже работает везде, но при 100k пользователей держит 100k открытых запросов и платит ~500 байт заголовков на каждый poll. SSE даёт ту же совместимость с proxy одним эффективным потоком и встроенным переподключением." },
    { label: "WebSocket поверх HTTP/2, чтобы мультиплексировать каналы.", misconception: "Здесь один канал на пользователя; мультиплексирование многих stream на клиента решает проблему, которой у этой фичи нет. Это лишняя сложность по сравнению с обычным SSE." },
  ]}
/>

<Quiz
  id="08-websocket-realtime-choice-4"
  lessonSlug="08-websocket-realtime"
  lang="ru"
  question="Память broadcast-сервера стабильно растёт под нагрузкой, пока не срабатывает OOM killer; 100 из 10 000 клиентов на медленных мобильных каналах. Каков механизм и первое надёжное исправление?"
  choices={[
    { label: "Backpressure: медленные клиенты медленно опустошают свои TCP-окна, поэтому per-connection application send queue растут в RAM без границ. Первое исправление: high-water mark на соединение, который выселяет (close 1013) или сбрасывает сообщения, когда очередь превышает порог.", correct: true },
    { label: "Утечка памяти в коде broadcast; перезапускай процесс по расписанию.", misconception: "'Утечка' — это безграничные send queue для клиентов, которые не успевают: ограниченные, намеренные буферы без политики выселения. Перезапуски маскируют симптом; high-water mark лечит причину." },
    { label: "GC-паузы слишком длинные; переключись на low-pause коллектор.", misconception: "GC-паузы удлиняются, потому что heap раздут очередями сообщений — это симптом, а не причина. Ограничение роста очереди убирает раздувание; выбор коллектора вторичен." },
    { label: "100 медленных клиентов — это слишком много соединений; добавь серверов.", misconception: "Больше серверов размазывает тот же дефект; каждый всё равно раздувается от своих медленных клиентов. Без high-water mark и выселения масштабирование вширь лишь откладывает OOM." },
  ]}
/>

<Quiz
  id="08-websocket-realtime-choice-5"
  lessonSlug="08-websocket-realtime"
  lang="ru"
  question="Ты раскатываешь новый билд; все 10 миллионов подключённых клиентов отваливаются с кодом 1006 и переподключаются. Клиент использует exponential backoff, но без jitter. Почему сервер всё равно с трудом восстанавливается?"
  choices={[
    { label: "Один лишь exponential backoff держит retry синхронными: каждый клиент удваивает от той же базы, так что волны всё равно приходят вместе — на 1 с, 2 с, 4 с. Исправление — per-client случайный jitter, чтобы 10 М retry размазались по непрерывному окну вместо синхронных всплесков.", correct: true },
    { label: "Backoff в порядке; серверу просто нужен больший SYN backlog.", misconception: "Больший backlog поднимает обрыв, но синхронные волны на 10 М всё равно его переполняют. Структурное исправление — десинхронизировать клиентов через jitter, а не поглощать синхронный всплеск." },
    { label: "Код 1006 означает, что клиентам вообще не надо переподключаться.", misconception: "1006 — аварийное закрытие (нет close frame) — именно тот случай, когда клиентам надо переподключаться. Проблема в том, как они переподключаются, а не подключаться ли вообще." },
    { label: "Exponential backoff по определению уже включает jitter.", misconception: "Чистый exponential backoff — это детерминированное удвоение; jitter — отдельная явная рандомизация (например, delay умножается на random(0.5,1.5)), которую надо добавить сверху." },
  ]}
/>

<Quiz
  id="08-websocket-realtime-choice-6"
  lessonSlug="08-websocket-realtime"
  lang="ru"
  question="Два WebSocket-сервера за балансировщиком. User A на Server 1 шлёт сообщение в чат, оно должно дойти до User B на Server 2. Sticky sessions включены. Почему одного sticky-роутинга мало и что закрывает разрыв?"
  choices={[
    { label: "Sticky sessions лишь закрепляют клиента за одним сервером на его собственных переподключениях; они не переносят сообщения между серверами. Backbone pub/sub (Redis Streams) — вот что делает fan-out: Server 1 публикует, Server 2 читает и рассылает своим локальным клиентам.", correct: true },
    { label: "Sticky sessions уже автоматически маршрутизируют сообщение A на Server 2.", misconception: "Stickiness стабильно направляет данного клиента на его собственный сервер; у неё нет механизма доставить сообщение A на другой сервер B. Межсерверный fan-out требует pub/sub." },
    { label: "Надо отказаться от sticky sessions и балансировать каждый frame по-запросно.", misconception: "Балансировка по frame разбросала бы состояние одного клиента по серверам и сломала бы in-flight unACK'd state. Stickiness плюс pub/sub — вот связка: stickiness для локальности per-connection state, pub/sub для fan-out." },
    { label: "Pub/sub не нужен, если оба сервера делят одну базу данных.", misconception: "Общая БД хранит сообщения, но не пушит их; сервер без опроса не получает сигнал, что появилась новая строка. Pub/sub (или streams с consumer-ами) даёт сигнал real-time fan-out." },
  ]}
/>

<Recap lang="ru">
Сквозная линия юнита — одна операционная модель: upgrade переиспользует существующее соединение, поэтому общая инфраструктура его маршрутизирует; framing и обязательный client masking защищают провод; выбор транспорта (WebSocket против SSE против long-polling) следует за направлением потока данных; backpressure — каноничный сбой, на который отвечает high-water mark; переподключение должно быть с jitter, иначе оно становится thundering herd; а горизонтальный масштаб требует pub/sub для fan-out плюс sticky sessions для per-connection state. Каждый production-инцидент в юните сводится к этим рычагам.
</Recap>
