---
concepts:
- server-sent-events
- sse
- long-polling
- eventsource
- subprotocol
- permessage-deflate
- transport-choice
deepensInto:
- 07-production-architecture
estMin: 10
lang: ru
lessonType: topic
level: middle
order: 3
prereqs:
- 08-websocket-realtime/01-upgrade-handshake
- 06-http-versions/01-http-fundamentals
slug: 03-transport-alternatives
sources:
- https://datatracker.ietf.org/doc/html/rfc6455
- https://developer.mozilla.org/en-US/docs/Web/API/Server-sent_events/Using_server-sent_events
- https://hpbn.co/websocket/
spiral:
- latency
status: ready
summary: Сравнение задержки, накладных расходов и совместимости с прокси для WebSocket,
  Server-Sent Events и long-polling — чтобы выбрать инструмент до начала разработки.
title: 'WebSocket vs SSE vs long-polling: выбор правильного транспорта'
track: networking
unit: 08-websocket-realtime
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import NumbersCard from "~/components/layout/NumbersCard.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";
import Infographic from "~/components/diagram/Infographic.astro";
import latencyGap from "./infographics/latency-gap.svg?raw";

<Hook>
Прежде чем тянуться за WebSocket, спросите: действительно ли нужна двусторонняя связь? SSE покрывает 90% сценариев «сервер должен пушить» — без собственной логики реконнекта и с куда более дружелюбными прокси. Неправильный выбор транспорта стоит недель отладки или ненужной сложности.
</Hook>

<Crux>WebSocket — по задержке и двунаправленности; SSE — по простоте и совместимости с прокси; long-polling — HTTP-only запасной вариант.</Crux>

<Explanation>

## Три транспорта, три модели

Каждый неправильный выбор транспорта обходился инженерам в недели: SSE-эндпоинты переписывали в WebSocket, потому что кто-то посчитал двунаправленность обязательной, или WebSocket-кластеры эксплуатировали с 10-кратными затратами там, где хватило бы SSE. Таблица ниже — это матрица решений, к которой обращаются до написания первой строки серверного кода.

| | WebSocket | SSE | Long-polling |
|--|-----------|-----|-------------|
| Направление | **Двунаправленный** | Только сервер→клиент | Сервер→клиент (клиент опрашивает) |
| Протокол | Бинарный фрейм (RFC 6455) | `text/event-stream` по HTTP | Обычный HTTP |
| Накладные расходы | 2–6 байт на фрейм | ~5 байт + метаданные на событие | ~500+ байт заголовков на запрос |
| Задержка | 0–5 мс | 3–8 мс | Интервал опроса + RTT (100–60000 мс) |
| Реконнект | Приложение должно реализовать | Браузерный `EventSource` обрабатывает | Приложение переоткрывает запрос |
| Поддержка прокси | Требует поддержки `Upgrade` | Работает с любым HTTP-прокси | Работает везде |

## WebSocket — full-duplex, минимальные накладные расходы

После upgrade-handshake любая сторона отправляет фреймы в любой момент. Накладные расходы на сообщение: 2–6 байт. Сервер может пушить без запроса клиента; клиент может отправлять без нового HTTP-соединения. Это правильный выбор когда:

- Клиент часто отправляет данные (игровой ввод, совместное редактирование, торговые заявки).
- Бюджет задержки менее 10 мс.
- Вы контролируете прокси/инфраструктуру (или знаете, что она поддерживает `Upgrade`).

Минус: нет встроенного реконнекта. Если соединение обрывается (idle timeout прокси, сетевой глюк), код должен обнаружить close-код 1006 и переподключиться с backoff.

## SSE — server push, браузер обрабатывает всё

SSE использует обычный постоянный HTTP-ответ с `Content-Type: text/event-stream`. Сервер пишет события, разделённые переводом строки:

```
event: price-update
data: {"symbol":"AAPL","price":182.34}
id: 4291

```

Нативный API браузера `EventSource` парсит события и автоматически переподключается с заголовком `Last-Event-ID` при обрыве соединения. Задержка: 3–8 мс. Ноль кода реконнекта писать не нужно. Работает через любой HTTP/1.1-прокси, включая корпоративные файрволы.

SSE неподходящ когда:
- Клиенту нужно отправлять данные (нужен отдельный REST-эндпоинт, что неудобно).
- Нужны бинарные payload (SSE — только текст).

## Long-polling — универсальный запасной вариант

Клиент открывает HTTP-запрос, сервер держит его открытым до появления данных (или до таймаута). Когда сервер отвечает, клиент сразу открывает новый запрос. Это симулирует server push через обычный HTTP.

Задержка = время ожидания сервера + один RTT. В лучшем случае 100 мс для сервера, ответившего сразу. В худшем — 60+ секунд для сервера с длинным таймаутом перед пустым ответом.

Накладные расходы высоки: каждый опрос несёт полные HTTP-заголовки (~500+ байт). При 50 000 клиентов, опрашивающих каждые 100 мс — 500 000 HTTP-запросов/секунду. Ни одно современное приложение не использует long-polling как основной транспорт — это graceful fallback для сред, блокирующих WebSocket.

<NumbersCard
  id="card-transport-comparison"
  title="Сравнение задержки и накладных расходов транспортов"
  data-lesson-visual
  stats={[
    { label: "Заголовок WebSocket-фрейма (сервер→клиент)", value: "2 байта" },
    { label: "Задержка WebSocket-сообщения после handshake", value: "0–5 мс" },
    { label: "Накладные расходы SSE-события (минимум)", value: "~5 байт + метаданные" },
    { label: "Задержка SSE-сообщения", value: "3–8 мс" },
    { label: "HTTP-заголовки long-poll на запрос", value: "~500+ байт" },
    { label: "Эффективная задержка long-poll (лучший случай)", value: "100–500 мс" },
  ]}
/>

<Infographic
  svg={latencyGap}
  label="Столбчатая диаграмма задержки сообщения по транспорту в лучшем случае: WebSocket около 5 мс, SSE около 8 мс, long-polling около 100 мс — на порядок больше."
  caption="WebSocket и SSE доставляют за единицы миллисекунд; long-polling платит примерно в 10–20 раз больше даже в лучшем случае. Подбирайте транспорт под свой бюджет задержки."
  hue="var(--d-infra)"
/>

## Согласование subprotocol и расширений

Клиент может запросить subprotocol прикладного уровня во время upgrade:

```
Sec-WebSocket-Protocol: chat, superchat
```

Сервер выбирает один и возвращает его обратно. Subprotocol'ы позволяют командам версионировать формат сообщений не затрагивая WebSocket-уровень — `chat` использует JSON, `superchat` — компактный бинарный формат. Если сервер не возвращает subprotocol-заголовок, соединение не имеет именованного протокола (это допустимо; приложение неявно определяет формат сообщений).

Расширения работают аналогично через `Sec-WebSocket-Extensions`. Важнейшее расширение на практике — `permessage-deflate` (RFC 7692): DEFLATE-сжатие на сообщение. На текстовых payload (JSON, HTML) типичная степень сжатия 50–90%. На маленьких сообщениях (< 64 байт) сжатие может увеличить payload. На уже сжатых данных (изображения, видео) — никакого эффекта. Затраты памяти значительны: при настройках по умолчанию каждый компрессор потребляет ~256 КБ, каждый декомпрессор ~44 КБ — при 100k idle-соединений это ~25 ГБ RAM только на состояние сжатия. Большинство production-инсталляций отключают `permessage-deflate` по умолчанию.

<Inset kind="why" lang="ru">
**Почему SSE недооценён.** SSE стандартизирован в HTML5 в 2009 году, но в основном игнорировался, потому что WebSocket появился одновременно и казался более интересным. На практике SSE чисто покрывает большинство сценариев уведомлений, лент и дашбордов: сервер пушит события, браузер переподключается автоматически, специальная настройка прокси не нужна. Случаи, действительно требующие WebSocket, — это когда клиент часто отправляет данные: игры, совместные редакторы, торговые терминалы.
</Inset>

<Quiz
  id="ws-mid-quiz-3"
  pieceSlug="03-transport-alternatives"
  lang="ru"
  question="У компании 1 000 WebSocket-соединений. Старый подход открывает 1 000 TCP-соединений к бэкенду, каждое потребляет ~10 КБ памяти в idle. Переход на HTTP/2 extended CONNECT (RFC 8441) мультиплексирует все WebSocket-соединения через одно TCP-соединение, потребляющее 50 КБ суммарно. Какова приблизительная экономия памяти?"
  choices={[
    {
      label: "9,95 МБ (снижение на 99,5% с 10 МБ до 50 КБ)",
      correct: true,
      misconception: "HTTP/2-мультиплексирование концентрирует состояние TCP-сокета в одном соединении, освобождая ~9,95 МБ. Стримы в HTTP/2 — просто целочисленные идентификаторы, их память пренебрежимо мала.",
    },
    {
      label: "1 МБ (экономится только память TLS-сессии)",
      correct: false,
      misconception: "Память TLS-сессии — несколько КБ на соединение. Основная часть памяти idle-соединения — буферы TCP-сокета ядра.",
    },
    {
      label: "Никакой экономии — каждый CONNECT-стрим всё равно выделяет 10 КБ",
      correct: false,
      misconception: "HTTP/2-стримы разделяют буферы одного TCP-соединения. Стрим — это 4-байтный stream ID + 4-байтное окно управления потоком, не полноценный сокет.",
    },
    {
      label: "10 МБ (HTTP/2 устраняет всё состояние соединений)",
      correct: false,
      misconception: "HTTP/2 всё равно поддерживает TCP-состояние (одно соединение) и состояние HTTP/2-фреймов. Экономия ~9,95 МБ, а не 10 МБ.",
    },
  ]}
/>

<TradeoffMatrix
  id="ws-mid-tradeoff-1"
  pieceSlug="03-transport-alternatives"
  lang="ru"
  prompt="Система уведомлений пушит ~5 обновлений в минуту на 100k пользователей. Пользователи не отправляют данные серверу. Какой транспорт?"
  options={[
    { name: "WebSocket", summary: "Full duplex, бинарные фреймы.", justification: "Двунаправленность здесь лишняя — пользователи только получают. WebSocket добавляет сложность upgrade и требует логики реконнекта без каких-либо преимуществ перед SSE для этого сценария." },
    { name: "SSE (Server-Sent Events)", summary: "Server push, автореконнект.", correct: true, justification: "Только server push, браузерный EventSource автоматически обрабатывает реконнект с Last-Event-ID, работает через все HTTP-прокси, формат text/event-stream прост в отладке. При 5 обновлениях в минуту разница в задержке по сравнению с WebSocket несущественна. Правильный выбор." },
    { name: "Long-polling", summary: "HTTP-only запасной вариант.", justification: "При 100k пользователей long-polling генерирует 100k открытых HTTP-запросов одновременно на сервере. Накладные расходы заголовков — 500+ байт на опрос. Работает, но расходует полосу и соединения сервера по сравнению с SSE." },
    { name: "WebSocket через HTTP/2 с мультиплексированием", summary: "Продвинутый мультиплексированный транспорт.", justification: "Излишне. Мультиплексирование HTTP/2 полезно когда нужно много одновременных WebSocket-стримов от одного клиента. Здесь нужен только один канал уведомлений на пользователя." },
  ]}
/>

</Explanation>

<FlowDiagram
  label="Направление потока данных у трёх транспортов: WebSocket двунаправленный между клиентом и сервером, SSE только сервер→клиент, long-polling шлёт запрос клиента, который сервер держит открытым до ответа"
  hue="var(--d-network)"
  perRow={2}
  nodes={[
    { id: "ws-c", label: "Клиент", sub: "WebSocket" },
    { id: "ws-s", label: "Сервер", sub: "WebSocket" },
    { id: "sse-c", label: "Клиент", sub: "SSE" },
    { id: "sse-s", label: "Сервер", sub: "SSE" },
    { id: "lp-c", label: "Клиент", sub: "long-poll" },
    { id: "lp-s", label: "Сервер", sub: "long-poll" },
  ]}
  edges={[
    { from: "ws-c", to: "ws-s", label: "send" },
    { from: "ws-s", to: "ws-c", label: "push" },
    { from: "sse-s", to: "sse-c", label: "push (только сервер→клиент)" },
    { from: "lp-c", to: "lp-s", label: "запрос (держится открытым)" },
    { from: "lp-s", to: "lp-c", label: "ответ, затем повторный poll" },
  ]}
  caption="WebSocket несёт фреймы в обе стороны; SSE только пушит сервер→клиент; long-polling имитирует push, держа каждый запрос клиента открытым до прихода данных."
/>

<KeyTakeaway>WebSocket (0–5 мс, двунаправленный) превосходит SSE (3–8 мс, server push) по задержке и направлению; SSE превосходит WebSocket по дружественности к прокси и встроенному реконнекту. Long-polling — универсальный запасной вариант. Подбирайте транспорт по направлению потока данных до оптимизации задержки.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="03-transport-alternatives-retrieval"
  lang="ru"
  questions={[
    { q: "Назовите два сценария, где SSE лучше WebSocket, и объясните почему.", a: "1) Ленты уведомлений или дашборды: данные идут только сервер→клиент, браузерный EventSource автоматически переподключается с Last-Event-ID, никакого собственного кода реконнекта. 2) Среды со строгими HTTP-прокси (корпоративные сети): SSE — обычный HTTP/1.1 и проходит через любой прокси; WebSocket требует поддержки Upgrade, которую многие корпоративные прокси и файрволы блокируют или искажают." },
    { q: "Почему permessage-deflate потребляет столько памяти в масштабе и как это решается в production?", a: "При window_bits=15 по умолчанию каждый контекст DEFLATE-компрессора использует ~256 КБ, каждый декомпрессор ~44 КБ; при 100k idle-соединений это ~25 ГБ только на состояние сжатия. Решение: отключить permessage-deflate полностью для idle-соединений, или согласовать меньший window_bits (например, 12), что снижает память до ~50 КБ на соединение ценой меньшей степени сжатия." },
    { q: "Браузер отправляет данные серверу каждые 50 мс (например, позиции мыши для совместного редактора). Почему SSE недостаточен и какой правильный транспорт?", a: "SSE — только сервер→клиент; браузер не может отправлять в SSE-поток. Для частых отправок клиент→сервер нужны отдельные HTTP POST-запросы с интервалом 50 мс — 20 запросов/секунду на пользователя, каждый с ~500 байтами заголовков. WebSocket — правильный выбор: браузер отправляет фреймы (2–6 байт накладных расходов, без новых соединений) через постоянное WebSocket-соединение, удерживая задержку ниже 5 мс и минимизируя накладные расходы." },
  ]}
/>

<Recap lang="ru">
Три транспорта решают задачу «серверу нужно пушить» с разными компромиссами. WebSocket предлагает минимальную задержку (0–5 мс) и истинную двунаправленность — правильный выбор когда клиенты часто отправляют данные. SSE (Server-Sent Events, стриминг событий от сервера) стримит `text/event-stream` через постоянный HTTP-ответ, браузерный `EventSource` обрабатывает реконнект через `Last-Event-ID` автоматически — правильный выбор для server-push-only сценариев в средах с прокси. Long-polling симулирует push через обычный HTTP при 500+ байтах накладных расходов заголовков на опрос и ~100–500 мс эффективной задержки — это универсальный запасной вариант, не основной транспорт. WebSocket subprotocol'ы позволяют версионировать схему сообщений; расширение `permessage-deflate` добавляет 50–90% текстового сжатия, но стоит ~256 КБ RAM на контекст компрессора соединения. Теперь, когда кто-то предлагает WebSocket для ленты уведомлений, вы знаете какой вопрос задать первым: клиент вообще когда-либо отправляет данные обратно? Если нет — SSE почти наверняка правильный ответ.
</Recap>
