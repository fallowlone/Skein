---
concepts:
- http2-extended-connect
- rfc-8441
- rfc-9220
- webtransport
- permessage-deflate
- c10k
- c10m
- file-descriptors
- head-of-line-blocking
deepensInto:
- 07-production-architecture
estMin: 14
lang: ru
lessonType: topic
level: senior
order: 6
prereqs:
- 08-websocket-realtime/04-backpressure-and-flow
- 06-http-versions/02-http2-multiplexing
slug: 06-scaling-and-limits
sources:
- https://datatracker.ietf.org/doc/html/rfc8441
- https://datatracker.ietf.org/doc/html/rfc9220
- https://datatracker.ietf.org/doc/html/rfc7692
- https://www.phoenixframework.org/blog/the-road-to-2-million-websocket-connections
spiral:
- latency
- statefulness
status: ready
summary: Три потолка, ограничивающих масштаб WebSocket — RAM на idle-соединение, файловые
  дескрипторы и пропускная способность NIC — и как HTTP/2 extended CONNECT и тюнинг
  сжатия сдвигают эти потолки.
title: 'WebSocket в масштабе: HTTP/2 мультиплексирование, permessage-deflate, C10M'
track: networking
unit: 08-websocket-realtime
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import NumbersCard from "~/components/layout/NumbersCard.astro";
import TraceScenario from "~/components/pedagogy/TraceScenario.astro";
import StackDiagram from "~/components/diagram/StackDiagram.astro";
import Infographic from "~/components/diagram/Infographic.astro";
import deflateMemoryCost from "./infographics/deflate-memory-cost.svg?raw";

<Hook>
Phoenix продемонстрировал 2 миллиона одновременных WebSocket-соединений на одном сервере 64 ГБ. MigratoryData сообщает о 10 миллионах на commodity-железе. Для этого нужно понять три независимых потолка: RAM на idle-соединение, файловые дескрипторы и пропускная способность NIC — и эволюцию протокола, сдвигающую эти потолки.
</Hook>

<Crux>Масштаб WebSocket упирается в RAM, файловые дескрипторы и NIC по порядку. HTTP/2 сдвигает RAM; ulimit — дескрипторы; шардинг — NIC.</Crux>

<Explanation>

## Три потолка по порядку

Когда упираетесь в предел масштаба, решение полностью зависит от того, в какой потолок упёрлись — RAM, файловые дескрипторы (лимит числа открытых файлов/сокетов на процесс) или NIC (сетевой адаптер). Лечить не тот — терять недели. Вот они в порядке встречи.

**Потолок 1 — RAM на idle-соединение.** Idle WebSocket-соединение потребляет буферы сокета ядра (send + receive) плюс состояние соединения в фреймворке. При стандартных настройках Linux:

- Буферы сокета ядра: ~4–8 КБ (rmem/wmem по умолчанию).
- Состояние соединения в фреймворке: зависит от языка/фреймворка (Node.js: ~10–50 КБ из-за накладных расходов V8; Go: ~2–5 КБ; Rust: ~1–2 КБ).

С `permessage-deflate` при window_bits=15 по умолчанию: +256 КБ компрессор + 44 КБ декомпрессор = **+300 КБ на соединение**.

При 10 M соединений × 10 КБ базовых = **100 ГБ RAM** минимум. Именно поэтому 2 M Phoenix на 64 ГБ впечатляет — они урезали накладные расходы ниже 32 КБ на соединение.

**Потолок 2 — Файловые дескрипторы.** Лимит Linux по умолчанию: 1 024 на процесс. Production-серверы поднимают через `ulimit -n` или `LimitNOFILE` в systemd. Практический максимум на процесс — около 1 M на современном Linux. При 10 M соединений на сервер нужно 10 M дескрипторов — требует тюнинга ядра (`fs.nr_open`, `fs.file-max`).

**Потолок 3 — Пропускная способность NIC.** Keepalive ping каждые 25 с с payload 125 байт: 10 M соединений × 4 ping/минуту × 125 байт = ~83 МБ/с только ping-трафика на 1 Гбит/с NIC. Насыщение NIC устанавливает практический потолок idle-соединений примерно в 500k–2 M в зависимости от интервала ping и частоты сообщений.

<NumbersCard
  id="card-ws-scale-numbers"
  title="WebSocket в масштабе — ключевые цифры"
  data-lesson-visual
  stats={[
    { label: "RAM на idle-соединение (зависит от фреймворка)", value: "2–50 КБ" },
    { label: "Накладные расходы permessage-deflate по умолчанию", value: "~300 КБ" },
    { label: "Phoenix: соединений на 64 ГБ, один сервер", value: "2 миллиона" },
    { label: "MigratoryData: соединений на commodity-железе", value: "10 миллионов" },
    { label: "Лимит fd Linux по умолчанию на процесс", value: "1 024 (поднять до 1 М в production)" },
    { label: "Практический потолок на один сервер (RAM или NIC)", value: "500k–2M idle-соединений" },
  ]}
/>

## HTTP/2 extended CONNECT (RFC 8441)

Открытие 100 WebSocket-соединений через HTTP/1.1 Upgrade означает 100 TCP 3-way handshake'ов (~30 мс каждый при RTT 10 мс) плюс 100 TLS-сессий (~20 мс на resume). Каждое TCP-соединение потребляет файловый дескриптор и ~8 КБ буферов ядра.

RFC 8441 определяет **extended CONNECT для WebSocket через HTTP/2**. Вместо `GET + Upgrade` клиент отправляет:

```
HEADERS stream=5
  :method = CONNECT
  :protocol = websocket
  :scheme = https
  :path = /chat
  :authority = example.com
  sec-websocket-protocol = chat
```

Сервер отвечает `200 OK` (не 101). HTTP/2-**стрим 5** становится WebSocket-туннелем. Другие HTTP/2-стримы на том же TCP-соединении несут другой трафик одновременно.

Преимущества:
- 100 WebSocket-соединений разделяют **одно TCP-соединение** — всего один 3-way handshake.
- Ноль дополнительных файловых дескрипторов для новых соединений.
- Тюнинг управления перегрузкой на общем соединении выгоден всем стримам.
- Суммарная задержка handshake для 100 соединений: ~30 мс вместо ~3 000 мс.

Компромисс: требует HTTP/2 end-to-end (клиент, сервер, все прокси). По состоянию на 2026 год крупные облачные load balancer'ы (AWS ALB, GCP Load Balancer) поддерживают это, но большинство корпоративных прокси и файрволов — нет. Adoption ограничен гиперскейл-операторами.

## WebSocket через HTTP/3 (RFC 9220)

HTTP/3 работает поверх QUIC (UDP-based). RFC 9220 определяет WebSocket через HTTP/3 с тем же extended CONNECT и `:protocol websocket`.

Ключевое отличие от HTTP/2: QUIC-стримы **независимы на транспортном уровне** — потерянный пакет на стриме 0 не блокирует стрим 4 (нет head-of-line blocking). HTTP/2 поверх TCP всё ещё страдает от HoL на уровне TCP: потерянный TCP-сегмент блокирует все HTTP/2-стримы на соединении.

По состоянию на начало 2026 года ни один крупный браузер или сервер не имеет production-поддержки WebSocket-over-HTTP/3. **WebTransport** (QUIC-нативный протокол) — предпочтительный выбор для приложений, которым нужна QUIC-семантика: он предлагает двунаправленные стримы плюс ненадёжные датаграммы и имеет ~75% поддержки браузеров (Chrome 120+, Firefox 127+, Safari 17+).

## Тюнинг permessage-deflate

Расширение `permessage-deflate` (RFC 7692) сжимает каждое сообщение DEFLATE. Степень сжатия:
- **50–90%** на текстовых payload (JSON, HTML).
- **Плохо** на маленьких сообщениях (< 64 байт) — может расширить.
- **~0%** на уже сжатых данных (изображения, видео, зашифрованные payload).

Затраты памяти на соединение при window_bits=15 по умолчанию:
- Компрессор: **~256 КБ**
- Декомпрессор: **~44 КБ**

При window_bits=12: ~50 КБ суммарно на соединение. Компромисс — более низкая степень сжатия (история 4 КБ против 32 КБ при window_bits=15). Степень сжатия падает с ~70% до ~50% на типичных JSON payload.

<Infographic
  svg={deflateMemoryCost}
  label="Столбчатая диаграмма памяти permessage-deflate на соединение при трёх настройках: window_bits=15 дефолт ~300 КБ, window_bits=12 ~50 КБ и deflate отключён 0 КБ"
  caption="Дефолтный компрессор deflate стоит ~300 КБ на соединение — при 100k соединений это 30 ГБ чистого состояния сжатия, поэтому снижение window_bits до 12 (~50 КБ) или отключение deflate — первый рычаг потолка по RAM."
  hue="var(--d-infra)"
/>

Production-стратегии:
- **Полностью отключить** для idle-соединений и соединений с маленькими сообщениями или бинарными данными.
- **Установить window_bits=10–12** для деплоёв с ограниченной памятью.
- **Включить context_takeover=false** для сброса состояния сжатия на каждом сообщении — жертвует степенью ради меньших накладных расходов на сообщение.

<Inset kind="why" lang="ru">
**Почему head-of-line blocking важен именно для WebSocket.** WebSocket мультиплексирует все каналы приложения на одно TCP-соединение. Если приложение отправляет независимые каналы данных (игровые объекты A, B, C), TCP-ретрансмит пакета объекта A блокирует всё соединение на ~10–30 мс (типичный таймер ретрансмита). Обновления объектов B и C стопорятся, хотя их пакеты были доставлены. Митигация: использовать несколько WebSocket-соединений (по одному на независимый канал), или перейти на WebTransport/QUIC с независимостью на уровне стримов. Большинство production-приложений терпят этот HoL-риск для интерактивной задержки (< 100 мс) без специальной обработки.
</Inset>

<TraceScenario
  id="ws-sr-trace-2"
  pieceSlug="06-scaling-and-limits"
  lang="ru"
  scenario="Трассируйте экономию памяти и handshake при переводе 1 000 WebSocket-соединений с HTTP/1.1 на HTTP/2 extended CONNECT."
  steps={[
    { prompt: "HTTP/1.1: 1 000 соединений. Каждое idle-соединение использует 10 КБ буферов сокета ядра. Сколько суммарная память ядра?", reveal: "1 000 × 10 КБ = 10 МБ только буферов сокетов ядра, плюс накладные расходы фреймворка на соединение." },
    { prompt: "HTTP/1.1: открываем 1 000 соединений по очереди при RTT 10 мс. Сколько суммарная задержка handshake?", reveal: "1 000 соединений × 30 мс (3-way handshake) = ~30 секунд суммарной задержки handshake. На практике параллельно, но каждое соединение всё равно требует собственного handshake." },
    { prompt: "HTTP/2 extended CONNECT: 1 000 WebSocket-соединений разделяют одно TCP-соединение. Сколько памяти ядра на буферы сокетов?", reveal: "Одно TCP-соединение: ~10–50 КБ буферов сокета ядра. Stream ID и окна flow control HTTP/2: ~8 байт на стрим = ~8 КБ для 1 000 стримов. Итого ~50–60 КБ против 10 МБ — примерно 200× сокращение памяти буферов ядра." },
    { prompt: "HTTP/2: сколько TCP handshake'ов?", reveal: "Один. Одно TCP-соединение требует один 3-way handshake и одну TLS-сессию. Все 1 000 WebSocket-соединений мультиплексируются через него. Суммарная задержка handshake: ~30 мс (плюс TLS ~20 мс) = ~50 мс, независимо от количества последующих WebSocket-соединений." },
    { prompt: "Какое ограничение деплоя сдерживает adoption HTTP/2 extended CONNECT?", reveal: "Все прокси и load balancer'ы на пути должны понимать HTTP/2 и extended CONNECT с заголовком :protocol. Многие корпоративные прокси, некоторые облачные load balancer'ы и большинство API-шлюзов из коробки не поддерживают это по состоянию на 2026 год. Ограничено гиперскейл-операторами, контролирующими весь стек прокси." },
  ]}
/>

<Quiz
  id="ws-sr-quiz-3"
  pieceSlug="06-scaling-and-limits"
  lang="ru"
  question="Сервер имеет 100k idle WebSocket-соединений с включённым permessage-deflate при window_bits=15 по умолчанию. Примерная память только на состояние сжатия?"
  choices={[
    { label: "~30 ГБ (300 КБ на соединение × 100k)", correct: true, misconception: "Каждое соединение использует ~256 КБ компрессор + ~44 КБ декомпрессор = ~300 КБ. При 100k соединений: 30 ГБ." },
    { label: "~3 ГБ (30 КБ на соединение)", correct: false, misconception: "30 КБ — накладные расходы при window_bits=10 (история 1 КБ). window_bits=15 по умолчанию выделяет историю 32 КБ на контекст компрессора, итого ~300 КБ на соединение." },
    { label: "~300 МБ (3 КБ на соединение)", correct: false, misconception: "3 КБ — накладные расходы буферов сокета ядра. Состояние сжатия отдельное и значительно больше при настройках по умолчанию." },
    { label: "~1 ГБ (10 КБ на соединение)", correct: false, misconception: "10 КБ — суммарные накладные расходы idle-соединения ядра без сжатия. permessage-deflate добавляет 300 КБ сверху." },
  ]}
/>

</Explanation>

<StackDiagram
  label="Три потолка масштабирования WebSocket, в которые упираешься по порядку сверху вниз: сначала RAM на idle-соединение, затем файловые дескрипторы, затем пропускная способность NIC, с рычагом протокола или ОС, сдвигающим каждый"
  hue="var(--d-network)"
  layers={[
    { label: "1. RAM на idle-соединение", note: "2-50 КБ +300 КБ deflate → HTTP/2 CONNECT" },
    { label: "2. Файловые дескрипторы", note: "по умолчанию 1024 → поднять ulimit до ~1 M" },
    { label: "3. Пропускная способность NIC", note: "ping-трафик насыщает 1 Гбит/с → шардить серверы" },
  ]}
  caption="Сначала упираешься в RAM, затем кончаются файловые дескрипторы, затем насыщается NIC — у каждого потолка свой рычаг, поэтому определи, в какой ты упёрся на самом деле."
/>

<KeyTakeaway>Масштаб WebSocket упирается в RAM первым (2–300 КБ на idle-соединение в зависимости от сжатия), файловые дескрипторы вторым (поднять ulimit до 1 М), NIC третьим (ping-трафик насыщает 1 Гбит/с при ~2 M idle-соединений). HTTP/2 extended CONNECT сокращает TCP-соединения и задержку handshake в 100× — но требует полностью HTTP/2-совместимого стека прокси.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="06-scaling-and-limits-retrieval"
  lang="ru"
  questions={[
    { q: "Объясните, почему HTTP/2 extended CONNECT эффективнее поддерживает несколько одновременных WebSocket-соединений, чем открытие отдельных TCP-соединений.", a: "HTTP/2 мультиплексирует стримы через одно TCP-соединение; один 3-way handshake устанавливает всё соединение, а каждый дополнительный WebSocket использует новый стрим (stream ID + окно flow control: ~8 байт) вместо нового TCP-сокета (~8 КБ буферов ядра + дескриптор). 100 соединений через HTTP/2 требуют 1 handshake и ~100 КБ памяти; через HTTP/1.1 Upgrade — 100 handshake'ов и ~1 МБ памяти. Компромисс: все стримы разделяют окно перегрузки TCP-соединения — потеря на любом стриме вызывает TCP-ретрансмит, кратко останавливающий все стримы (HoL)." },
    { q: "Какой эффект у server_no_context_takeover при согласовании permessage-deflate и когда его использовать?", a: "server_no_context_takeover сбрасывает контекст DEFLATE-сжатия (скользящее историческое окно) после каждого сообщения вместо его сохранения между сообщениями. Без этого компрессор может ссылаться на байты из предыдущих сообщений, достигая более высоких степеней на повторяющемся контенте (например, JSON-ленты где ключи повторяются). С отключённым context_takeover каждое сообщение сжимается независимо — степень падает на 10–20% на типичном JSON. Использовать когда важна изоляция памяти на сообщение (общее состояние компрессора усложняет многопоточный доступ) или когда сообщения в основном независимы (контекст и так вносит малый вклад)." },
    { q: "Назовите три независимых потолка, ограничивающих WebSocket-соединения на одном сервере, и практический предел каждого.", a: "1) RAM: idle-соединение использует 2–50 КБ состояния фреймворка + буферы ядра; с permessage-deflate до 300 КБ; практический потолок — RAM сервера делённая на footprint соединения (~2 M соединений на 64 ГБ при 30 КБ). 2) Файловые дескрипторы: Linux по умолчанию 1024, в production поднимают до 100k–1 M через ulimit; 10 M соединений требуют 10 M fd и тюнинга ядра. 3) Пропускная способность NIC: ping-трафик при 10 M соединений × 4 ping/мин × 125 байт = ~83 МБ/с; 1 Гбит/с NIC насыщается при ~1–2 M idle-соединений с учётом другого трафика." },
  ]}
/>

<Recap lang="ru">
WebSocket-соединения в масштабе упираются в три независимых потолка. RAM первый: каждое idle-соединение использует 2–50 КБ состояния фреймворка и буферов ядра; permessage-deflate при настройках по умолчанию добавляет ещё 300 КБ на соединение, делая 100k соединений потребителями 30 ГБ только на состояние сжатия — большинство production-деплоёв отключают сжатие или снижают window_bits. Файловые дескрипторы вторые: Linux по умолчанию 1 024 на процесс; production-серверы поднимают до 100k–1 M через `ulimit -n`. Пропускная способность NIC третья: один ping-трафик насыщает 1 Гбит/с NIC при ~1–2 M idle-соединений. HTTP/2 extended CONNECT (RFC 8441) решает потолки TCP и файловых дескрипторов, мультиплексируя 100 WebSocket-соединений через одно TCP — 100× сокращение памяти буферов ядра и задержки handshake — но требует HTTP/2-совместимого стека прокси, пока не распространённого за пределами гиперскейл-деплоёв. WebSocket через HTTP/3 (RFC 9220) добавляет QUIC-независимость стримов (независимость на транспортном уровне: потеря пакета на одном стриме не блокирует другие, в отличие от head-of-line blocking в TCP), но не имеет production-поддержки браузеров по состоянию на 2026; WebTransport — текущая QUIC-нативная альтернатива. Теперь, когда сообщают «не можем добавить больше соединений», первый вопрос — в какой потолок упёрлись: проверьте графики памяти, потом вывод `ulimit -n`, потом пропускную способность NIC — у каждого свой рычаг.
</Recap>
