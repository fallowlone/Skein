---
concepts:
- handshake-rtt
- isn-randomisation
- syn-cookies
- congestion-control
- nagle-delayed-ack
- time-wait-exhaustion
estMin: 13
lang: ru
level: senior
order: 7
prereqs: []
slug: quiz-choice
sources:
- https://datatracker.ietf.org/doc/html/rfc9293
- https://datatracker.ietf.org/doc/html/rfc6528
- https://datatracker.ietf.org/doc/html/rfc7413
status: ready
summary: "Тест с выбором на синтез по всему юниту TCP handshake — цена RTT, рандомизация ISN, SYN cookie, congestion control, стопоры Nagle и исчерпание TIME-WAIT."
title: 'TCP handshake: тест с выбором ответа'
track: networking
unit: 03-tcp-handshake
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Шесть вопросов поперёк всего юнита. Каждый отражает решение, которое ты принимаешь в реальном инциденте — не флаг для называния, а компромисс, который надо взвесить, пока горит задержка или память.
</Hook>

<Goal lang="ru">
Убедись, что связываешь цену RTT в handshake, безопасность ISN, защиту от флуда, congestion control и канонические production-патологии — тот синтез, к которому вели отдельные уроки.
</Goal>

<Quiz
  id="03-tcp-handshake-choice-1"
  lessonSlug="03-tcp-handshake"
  lang="ru"
  question="REST-клиент открывает новое TCP-соединение на каждый запрос к origin с RTT 140 мс, затем накладывает TLS 1.3. Почему каждый вызов медленный, хотя payload крошечный, и какой структурный фикс?"
  choices={[
    { label: "Handshake стоит 1 RTT до любых данных, а TLS добавляет ещё — ~280 мс round-trip'ов зашиты до первого байта. Фикс: пул соединений с keep-alive, чтобы один handshake амортизировался на много запросов.", correct: true },
    { label: "Сервер упирается в CPU на крошечном payload.", misconception: "Крошечный payload — не CPU-проблема. Задержка — это цепочка round-trip'ов (TCP handshake + TLS), оплачиваемая на каждое соединение, что и амортизирует keep-alive." },
    { label: "TCP slow start ограничивает первый ответ, так что подними начальное окно.", misconception: "Крошечный ответ помещается в стандартное IW=10 (~14.6 КБ) за первый RTT; slow start не узкое место. Цена — round-trip'ы handshake на каждое соединение." },
    { label: "Nagle держит запрос — выстави TCP_NODELAY.", misconception: "Nagle стопорит конвейерные мелкие записи в одном соединении, а не cold-start round-trip'ы открытия нового соединения на каждый запрос. Здесь фикс — переиспользование, а не TCP_NODELAY." },
  ]}
/>

<Quiz
  id="03-tcp-handshake-choice-2"
  lessonSlug="03-tcp-handshake"
  lang="ru"
  question="Почему RFC 6528 вычисляет Initial Sequence Number как счётчик по времени плюс keyed-хэш 4-кортежа, а не начинает с нуля?"
  choices={[
    { label: "Предсказуемый ISN позволяет off-path атакующему угадать следующий sequence number и подделать сегмент в установленное соединение. Криптографическая рандомизация на каждое соединение делает слепую инъекцию невозможной без перехвата.", correct: true },
    { label: "Случайные ISN шифруют payload соединения.", misconception: "Рандомизация ISN — не шифрование, TCP несёт открытый текст. Она лишь останавливает предсказание sequence number; конфиденциальность — задача TLS, слоя выше." },
    { label: "Это балансирует соединения по ядрам CPU.", misconception: "Выбор ISN не имеет отношения к распределению по ядрам (это RSS/RPS на NIC). RFC 6528 — чисто защита от инъекции." },
    { label: "Это позволяет обеим сторонам пропустить третий ACK.", misconception: "Любому соединению всё равно нужны все три шага. ACK доказывает живость независимо от способа выбора ISN; рандомизация меняет значение, а не число шагов." },
  ]}
/>

<Quiz
  id="03-tcp-handshake-choice-3"
  lessonSlug="03-tcp-handshake"
  lang="ru"
  question="Во время SYN-флуда твой публичный listener держится с tcp_syncookies=1, но легитимные клиенты на трансконтинентальном пути 200 мс жалуются, что пропускная способность застряла на ручейке. Почему?"
  choices={[
    { label: "SYN cookie кодирует в ISN только индекс MSS; Window Scaling, SACK и Timestamps, предложенные в SYN, тихо отбрасываются для cookie-валидированных соединений. Без scaling окно упирается в 64 КиБ, душа high-BDP пути.", correct: true },
    { label: "SYN cookie аллоцируют столько памяти, что ядро дросселирует bandwidth.", misconception: "Cookie меняют CPU на память — они вообще не аллоцируют request_sock. Удар по пропускной способности — от потери Window Scaling/SACK/Timestamps в cookie, а не от давления памяти." },
    { label: "Флуд насыщает аплинк, поэтому реальным клиентам достаётся остаток bandwidth.", misconception: "SYN-флуд — это крошечные пакеты, переполняющие half-open очередь, а не bandwidth канала. Потолок пропускной способности на соединение — от отброшенной опции scaling в cookie-валидированных handshake." },
    { label: "SYN cookie загоняют каждое соединение на Reno congestion control.", misconception: "Cookie не меняют алгоритм congestion control. Они отбрасывают TCP-опции, несомые в SYN; ограничитель — потолок окна 64 КиБ." },
  ]}
/>

<Quiz
  id="03-tcp-handshake-choice-4"
  lessonSlug="03-tcp-handshake"
  lang="ru"
  question="Сервис 4K-видео по cellular (RTT 150 мс, ~1% случайных потерь) видит, как пропускная способность CUBIC падает до доли ёмкости канала. Переход на BBR восстанавливает почти line-rate. В чём причина?"
  choices={[
    { label: "CUBIC трактует любую потерю как congestion и режет cwnd (~0.7x), поэтому при 1% случайных потерь живёт в recovery и никогда не достигает ёмкости. BBR моделирует bottleneck bandwidth и min-RTT напрямую и игнорирует потерю как неоднозначную, так что случайные дропы не сбивают его темп отправки.", correct: true },
    { label: "BBR шлёт большее начальное окно, поэтому он просто быстрее со старта.", misconception: "Оба могут стартовать с IW=10. Устойчивая разница — loss-агностичная модель bandwidth/RTT у BBR против loss-триггерных срезов окна у CUBIC, а не разовое преимущество начального окна." },
    { label: "BBR отключает ретрансмит, поэтому потерянные пакеты ничего не стоят.", misconception: "BBR всё равно ретрансмитит потерянные данные — надёжность неизменна. Он просто не трактует потерю как сигнал congestion, поэтому не обваливает темп на случайных дропах." },
    { label: "Cellular-канал быстрее под BBR, потому что BBR использует jumbo-фреймы.", misconception: "Congestion control не меняет MSS, а jumbo-фреймы не выживают на cellular-путях. Выигрыш — чисто разница в моделировании loss против congestion." },
  ]}
/>

<Quiz
  id="03-tcp-handshake-choice-5"
  lessonSlug="03-tcp-handshake"
  lang="ru"
  question="Redis-клиент на том же хосте дата-центра показывает стабильный p99 ~40–200 мс на мелких конвейерных командах, без потерь и без давления CPU. Корневая причина?"
  choices={[
    { label: "Nagle держит вторую мелкую запись, пока первая не получит ACK, а delayed-ACK у пира держит этот ACK до 40 мс — каждая сторона ждёт другую. Фикс: TCP_NODELAY на сокете.", correct: true },
    { label: "Соединение попадает на slow start на каждой команде.", misconception: "Установленное соединение прошло slow start, и крошечные команды не нагружают cwnd. Стопор — дедлок Nagle плюс delayed-ACK, лечится через TCP_NODELAY." },
    { label: "Сокеты TIME-WAIT замедляют новые команды.", misconception: "TIME-WAIT влияет на открытие новых соединений (переиспользование портов), а не на задержку команд на уже установленном сокете. Квант 40 мс прямо указывает на delayed-ACK." },
    { label: "Сервер задерживает, потому что ISN не был рандомизирован.", misconception: "Выбор ISN никак не влияет на задержку на команду. Цифра 40 мс — это таймер delayed-ACK во взаимодействии с Nagle." },
  ]}
/>

<Quiz
  id="03-tcp-handshake-choice-6"
  lessonSlug="03-tcp-handshake"
  lang="ru"
  question="Load balancer возвращает EADDRNOTAVAIL на исходящий connect() к одному бэкенду; ss показывает ~30k сокетов TIME-WAIT к этому origin. Коллега предлагает понизить MSL, чтобы TIME-WAIT быстрее сливался. Как читать ситуацию правильно?"
  choices={[
    { label: "Понижение MSL рискует принять устаревший FIN или сегмент как данные нового соединения на переиспользованном 4-кортеже — тихое повреждение. Используй tcp_tw_reuse (безопасно через Timestamps/PAWS), расширь ip_local_port_range и структурно почини пулом keep-alive.", correct: true },
    { label: "Понижение MSL — стандартный фикс и совершенно безопасно.", misconception: "TIME-WAIT существует, чтобы поглощать запоздалые ретрансмиты на том же 4-кортеже; его укорачивание приглашает повреждение устаревшим сегментом. Безопасные рычаги — tcp_tw_reuse плюс пулинг соединений." },
    { label: "Включи tcp_tw_recycle для быстрой переработки.", misconception: "tcp_tw_recycle убрали в Linux 4.12, потому что он дропал легитимные SYN от клиентов за NAT. Никогда не включай его; используй tcp_tw_reuse." },
    { label: "Включи SYN cookie, чтобы освободить порты.", misconception: "SYN cookie защищают входящую half-open SYN-очередь; они ничего не делают для исходящего исчерпания эфемерных портов в TIME-WAIT. Совсем не тот слой." },
  ]}
/>

<Recap lang="ru">
Сквозная линия юнита — одна цепочка решений: handshake стоит round-trip, который ты амортизируешь переиспользованием соединений, рандомизация ISN его защищает, SYN cookie обороняют его под флудом (ценой отброшенных опций), congestion control решает, как быстро он заполнит трубу (CUBIC реагирует на потерю, BBR моделирует bandwidth и RTT), а канонические production-сбои — стопоры Nagle плюс delayed-ACK, утечки CLOSE-WAIT, исчерпание TIME-WAIT — у каждого один верный рычаг и несколько правдоподобных неверных. Знать, какой рычаг и почему остальные провалятся, — и есть синтез.
</Recap>
