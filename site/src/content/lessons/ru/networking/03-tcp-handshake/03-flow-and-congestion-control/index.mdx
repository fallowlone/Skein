---
concepts:
- sliding-window
- flow-control
- mss
- window-scaling
- sack
- slow-start
- congestion-avoidance
- cwnd
- ssthresh
- rto
- srtt
- rack-tlp
- cubic
- reno
deepensInto:
- 06-bbr-and-production-ops
estMin: 16
lang: ru
lessonType: topic
level: middle
order: 3
prereqs:
- 02-sequence-numbers-and-state
slug: 03-flow-and-congestion-control
sources:
- https://datatracker.ietf.org/doc/html/rfc9293
- https://datatracker.ietf.org/doc/html/rfc6298
- https://datatracker.ietf.org/doc/html/rfc8985
- https://datatracker.ietf.org/doc/html/rfc2018
- https://datatracker.ietf.org/doc/html/rfc7323
spiral:
- latency
status: ready
summary: Скользящие окна позволяют получателю управлять темпом отправителя; slow start
  и CUBIC позволяют TCP обнаружить пропускную способность сети, не перегружая её.
title: Управление потоком и перегрузкой
track: networking
unit: 03-tcp-handshake
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import NumbersCard from "~/components/layout/NumbersCard.astro";
import TraceScenario from "~/components/pedagogy/TraceScenario.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";
import Infographic from "~/components/diagram/Infographic.astro";
import windowScalingCeiling from "./infographics/window-scaling-ceiling.svg?raw";

<Hook lang="ru">
Свежее TCP-соединение между Лондоном и Сиднеем не знает, насколько быстра линия. Оно начинает осторожно, удваивая скорость отправки каждый round-trip, пока не достигнет предела — затем отступает и пробует снова. Этот танец между отправителем и сетью позволяет TCP заполнить 10-гигабитный канал или вежливо разделить мегабитный.
</Hook>

<Crux>Скользящие окна дают получателю управление темпом; slow start и congestion avoidance обнаруживают ёмкость сети без перегрузки.</Crux>

<Explanation>

## Скользящее окно и управление потоком

После установки соединения каждый TCP-сегмент несёт **размер окна** — количество байтов, которые получатель готов принять сверх последнего подтверждённого байта. Отправитель поддерживает скользящее окно неподтверждённых байт в пути, ограниченное минимумом из рекламируемого окна получателя и собственного окна перегрузки. По мере прихода ACK окно скользит вперёд, позволяя отправлять новые байты. Если приложение получателя медленно опустошает буфер сокета, окно уменьшается; при полном заполнении буфера получатель рекламирует `window=0` и отправитель делает паузу. Это сквозное управление потоком: получатель диктует темп, отправитель подчиняется.

## MSS, масштабирование окна и SACK

Почему межконтинентальный линк с избыточной физической полосой всё равно отдаёт одному соединению лишь несколько мегабит в секунду? Обычно ответ — одна из трёх опций, которые стек согласовал (или не согласовал) ещё в первом обмене SYN. Три опции, согласуемые при обмене SYN/SYN-ACK, существенно влияют на пропускную способность:

**Maximum Segment Size (MSS):** максимальный TCP-payload, который стек может обработать без IP-фрагментации. Типично: 1460 байт на Ethernet (MTU 1500 минус 20 IP + 20 TCP заголовки), 1220 байт на туннельных сетях.

**Window Scaling (RFC 7323):** 16-битное поле окна умножается на 2^scale, допуская окна до 1 ГиБ. Без этого ограничение 64 КиБ на линке 100 мс RTT ограничивает пропускную способность примерно 5 Мбит/с независимо от реальной скорости линка (bandwidth-delay product = 64 КиБ / 0,1 с ≈ 640 КиБ/с). Необходимо для высокополосных путей с большой задержкой.

**Selective Acknowledgements (SACK, RFC 2018):** получатель перечисляет точные диапазоны байт, полученных за пропуском, чтобы отправитель повторно передал только недостающие части, а не всё после пробела. На потерянном пути с большим RTT SACK может удвоить эффективную пропускную способность.

<NumbersCard
  id="card-window"
  title="Окно и числа пропускной способности"
  data-lesson-visual
  stats={[
    { label: "MSS на Ethernet", value: "1460 байт (MTU 1500)" },
    { label: "Максимум 16-битного окна", value: "64 КиБ (без масштабирования)" },
    { label: "Максимум с масштабированием", value: "1 ГиБ (2^30)" },
    { label: "Начальное окно Linux (IW)", value: "10 MSS (~14,6 КБ)" },
    { label: "Bandwidth × RTT (BDP)", value: "100 Мбит/с × 100 мс = 1,25 МБ" },
    { label: "BDP — минимальное нужное окно", value: "чтобы заполнить путь с большим BDP" },
  ]}
/>

<Infographic
  svg={windowScalingCeiling}
  label="Столбчатая диаграмма, сравнивающая ёмкость линка 1 Гбит/с с ~5 Мбит/с, которые один TCP-поток способен достичь без масштабирования окна, потому что окно 64 КиБ на RTT 100 мс ставит этот предел — разрыв примерно в 200 раз, наложенный протоколом, а не линией."
  caption="Окно 64 КиБ на RTT 100 мс ограничивает один поток ~5 Мбит/с независимо от линка 1 Гбит/с. Window Scaling снимает этот потолок, чтобы окно достигло bandwidth-delay product и реально заполнило канал."
  hue="var(--d-infra)"
/>

## Математика таймера повторной передачи (RFC 6298)

Если сегмент не подтверждён в течение retransmission timeout (RTO), отправитель пересылает его. RFC 6298 задаёт:

```
SRTT ← (1 − 1/8)·SRTT + (1/8)·new_RTT
RTTVAR ← (1 − 1/4)·RTTVAR + (1/4)·|SRTT − new_RTT|
RTO = SRTT + max(G, 4·RTTVAR)
```

SRTT — сглаженный RTT, RTTVAR — дисперсия RTT (экспоненциально взвешенные скользящие средние). Первый RTO по умолчанию 1 с; после таймаута удваивается (экспоненциальный откат) до прихода сегмента или разрыва соединения. Алгоритм Карна запрещает измерять RTT по повторно переданным сегментам, так как ответный ACK неоднозначен.

Современный Linux использует **RACK-TLP (RFC 8985)** для более быстрого обнаружения потерь: RACK объявляет сегмент потерянным, когда позже отправленный сегмент подтверждён и истёк интервал переупорядочивания — без ожидания таймера RTO. TLP (Tail Loss Probe) повторно отправляет последний неподтверждённый сегмент через один RTT после последней отправки, чтобы избежать зависания на последнем пакете отправки.

<Quiz
  id="mid-quiz-1"
  pieceSlug="03-flow-and-congestion-control"
  lang="ru"
  question="Что означает рекламируемое окно получателя в заголовке TCP?"
  choices={[
    { label: "Количество байт, которые получатель уже принял", misconception: "Это значение — номер ACK, а не окно." },
    { label: "Количество дополнительных байт, которые получатель готов принять сверх последнего подтверждённого байта", correct: true },
    { label: "Количество сегментов, которые получатель ожидает за RTT", misconception: "Окна TCP измеряются в байтах, а не в сегментах." },
    { label: "Максимальный размер сегмента", misconception: "MSS — отдельная опция, согласуемая в SYN, а не поле окна." },
  ]}
/>

## Slow start и congestion avoidance

Свежее соединение не знает, насколько быстра сеть. **Slow start** открывает congestion window экспоненциально (1, 2, 4, 8, ... MSS за RTT) до тех пор, пока не произойдёт потеря или не достигнет slow-start threshold (ssthresh). После этого **congestion avoidance** увеличивает окно линейно (~1 MSS за RTT для Reno/CUBIC).

При потере алгоритм зависит от варианта:
- **Reno:** уменьшает окно вдвое, затем линейное увеличение.
- **CUBIC (Linux по умолчанию с 2.6.19):** уменьшает менее агрессивно, затем зондирует с кубической кривой — быстрее восстанавливает пропускную способность на путях с большим BDP.
- **BBR:** полностью игнорирует потери как сигнал перегрузки; использует RTT + измерения доставленных байт (разбирается в уроке о BBR).

Вместе slow start и congestion avoidance создают пилообразный профиль пропускной способности, который вы видите на сетевых графиках: экспоненциальный подъём, сброс при потере, затем линейное восстановление. Без управления перегрузкой один агрессивный отправитель мог бы обрушить любой общий канал; без slow start соединение никогда бы не узнало, на что канал реально способен.

<DragOrder
  id="mid-drag-1"
  pieceSlug="03-flow-and-congestion-control"
  lang="ru"
  prompt="Упорядочьте рост congestion window отправителя в TCP slow start:"
  items={[
    "Начало: cwnd = 10 MSS (IW по умолчанию в Linux)",
    "После 1 RTT и ACK: cwnd удваивается до 20 MSS",
    "После 2 RTT: cwnd удваивается до 40 MSS",
    "Экспоненциальный рост продолжается до ssthresh или потери",
    "При достижении ssthresh: переход в congestion avoidance (линейный +1 MSS/RTT)",
    "При потере пакета: уменьшить cwnd согласно варианту управления перегрузкой",
  ]}
/>

<Quiz
  id="mid-quiz-2"
  pieceSlug="03-flow-and-congestion-control"
  lang="ru"
  question="Почему для повторных передач используется экспоненциальный откат, а не постоянный таймер?"
  choices={[
    { label: "Это упрощает математику в ядре", misconception: "Это о поведении при перегрузке, а не о простоте реализации." },
    { label: "Снизить нагрузку на перегруженную или сломанную сеть — каждый повтор ждёт дольше, чтобы частичный сбой не получал шквал запросов", correct: true },
    { label: "Потому что RFC 793 это постулировал без объяснений", misconception: "RFC 6298 явно объясняет инженерную мотивацию." },
    { label: "Чтобы дать время для ручного вмешательства", misconception: "Механизм автоматизирован и работает на миллисекундных масштабах." },
  ]}
/>

<TraceScenario
  id="mid-trace-1"
  pieceSlug="03-flow-and-congestion-control"
  lang="ru"
  scenario="Проследите эпизод восстановления SACK после потери одного пакета в окне из 10 сегментов."
  steps={[
    { prompt: "Отправлены сегменты 1–10. Сегмент 5 потерян. Сегменты 6–10 пришли. Что сообщает получатель?", reveal: "Получатель отправляет ACK=5 (подтверждая сегменты 1–4) плюс SACK-блоки, указывающие на приход сегментов 6–10. Отправитель теперь точно знает, что отсутствует." },
    { prompt: "Без SACK что пришлось бы повторно передавать отправителю?", reveal: "Без SACK отправитель видит три дублированных ACK (для сегмента 4) и делает fast retransmit сегмента 5, но не знает, пришли ли сегменты 6–10. Возможно, придётся повторно передать всё после пробела. С SACK — только сегмент 5." },
    { prompt: "Почему это важно на пути с RTT 200 мс?", reveal: "На пути с RTT 200 мс каждая лишняя повторная передача стоит 200 мс зря. С SACK один потерянный пакет стоит один дополнительный RTT вне зависимости от размера окна. Без SACK — потеря одного пакета из 10 может стоить времени повторной передачи 9 лишних сегментов." },
  ]}
/>

<Inset kind="why" lang="ru">
**Почему slow start начинается быстро.** Название вводит в заблуждение: Linux 3.0+ (RFC 6928) устанавливает начальное congestion window (IW) равным 10 MSS (~14,6 КБ), а не 1 MSS. Это означает, что первый всплеск данных уже составляет 10 сегментов, выбранных под типичный размер HTTP-ответа на небольшой API-запрос. Slow start выглядит медленным только в сравнении с конечной пропускной способностью; по сравнению с классическим IW=1 это 10-кратное улучшение для первого RTT.
</Inset>

</Explanation>

<FlowDiagram
  label="Фазовый автомат congestion window TCP: slow start растит cwnd экспоненциально до ssthresh, затем congestion avoidance растит линейно; при потере cwnd уменьшается и цикл повторяется"
  hue="var(--d-network)"
  perRow={4}
  nodes={[
    { id: "iw", label: "Начальное окно", sub: "cwnd = 10 MSS" },
    { id: "ss", label: "Slow start", sub: "×2 за RTT" },
    { id: "ca", label: "Congestion avoidance", sub: "+1 MSS за RTT" },
    { id: "loss", label: "Потеря", sub: "уменьшить cwnd" },
  ]}
  edges={[
    { from: "iw", to: "ss", label: "первая отправка" },
    { from: "ss", to: "ca", label: "достигнут ssthresh" },
    { from: "ca", to: "loss", label: "сброс пакета" },
    { from: "loss", to: "ca", label: "CUBIC ×0.7, продолжить" },
    { from: "ss", to: "loss", label: "ранняя потеря" },
  ]}
  caption="Slow start зондирует ёмкость экспоненциально; пересечение ssthresh переключает на линейный congestion avoidance. Потеря режет cwnd (CUBIC ×0,7) и задаёт новый ssthresh, затем рост возобновляется — пилообразный профиль, позволяющий TCP отслеживать меняющийся путь."
/>

<KeyTakeaway>Скользящие окна обеспечивают управление потоком (регулируется получателем); SACK ограничивает масштаб повторных передач; slow start открывается экспоненциально для зондирования мощности; CUBIC растёт кубически после потери. RACK-TLP обнаруживает потери быстрее классического RTO, наблюдая за порядком ACK, а не ожидая срабатывания таймера.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="03-flow-and-congestion-control-retrieval"
  lang="ru"
  questions={[
    { q: "Как Window Scaling (RFC 7323) влияет на достижимую пропускную способность на пути RTT 100 мс, 1 Гбит/с?", a: "Без масштабирования поле окна — 16 бит, максимум 65535 байт. Пропускная способность = окно / RTT = 65535 / 0,1 с ≈ 655 КБ/с, намного ниже мощности линка 1 Гбит/с. С масштабированием окно может вырасти до 1 ГиБ (scale factor 14). Для заполнения канала нужно окно минимум bandwidth × RTT = 1 Гбит/с × 0,1 с = 12,5 МБ — достижимо только с включённым масштабированием. Без него пропускная способность ограничивается протоколом независимо от физической ёмкости." },
    { q: "Что такое RACK-TLP и почему это улучшение относительно классического обнаружения потерь на основе RTO?", a: "RACK (Recent ACKment) объявляет сегмент потерянным, когда отправленный после него сегмент подтверждён и истёк краткий интервал переупорядочивания — использует порядок ACK, а не таймер. TLP (Tail Loss Probe) повторно отправляет последний неподтверждённый сегмент через один RTT, чтобы избежать зависания при потере последнего пакета всплеска. Вместе они обнаруживают потери за 1–2 RTT в большинстве случаев, тогда как классическое обнаружение на основе RTO ждёт SRTT + 4*RTTVAR (минимум 200 мс, часто секунды после откатов). RACK-TLP — стандартное обнаружение потерь в Linux с ядра 5.x." },
    { q: "Почему соединение с высокими случайными потерями работает плохо с CUBIC, но не с BBR?", a: "CUBIC воспринимает каждую потерю пакета как сигнал перегрузки и агрессивно срезает congestion window (0,7×). На пути с 1% случайных потерь CUBIC проводит большую часть времени в recovery, никогда не достигая реальной пропускной способности пути. BBR игнорирует потери как сигнал перегрузки; он оценивает пропускную способность узкого места через скорость доставки байт и минимальный RTT через метки времени, затем темпирует отправки соответственно. Случайные отбрасывания не нарушают оценку BBR, поэтому пропускная способность остаётся близкой к мощности пути." },
  ]}
/>

<Recap lang="ru">
Управление потоком TCP использует скользящее окно: получатель рекламирует, сколько байт он может принять, отправитель никогда не превышает это значение. Три опции из SYN/SYN-ACK резко влияют на пропускную способность: MSS (размер сегмента), Window Scaling (расширяет 64 КиБ до 1 ГиБ окна) и SACK (точно определяет недостающие диапазоны, чтобы повторно передавались только потерянные сегменты). Управление перегрузкой начинает свежие соединения в slow start (экспоненциальный рост окна), затем переходит в congestion avoidance (линейный рост) после достижения ssthresh. CUBIC, стандартный с 2006 года, использует кубическую кривую восстановления после потерь. Таймер повторной передачи вычисляется из сглаженного RTT (RFC 6298) с экспоненциальным откатом. RACK-TLP, стандартный с Linux 5.x, обнаруживает потери быстрее, наблюдая за порядком ACK, а не ожидая срабатывания таймера. Теперь, когда пропускная способность одного TCP-потока намного ниже заявленной скорости линка, первые вопросы: согласован ли Window Scaling, включён ли SACK и каков RTT — эти три числа объясняют большинство случаев ограничения скорости одного потока.
</Recap>
