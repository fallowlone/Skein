---
concepts:
- isn
- sequence-number
- tcp-state-machine
- established
- syn-sent
- syn-recv
- fin
- time-wait
- close-wait
- rtt-cost
deepensInto:
- 03-flow-and-congestion-control
estMin: 14
lang: ru
lessonType: topic
level: middle
order: 2
prereqs:
- 01-the-three-way-handshake
slug: 02-sequence-numbers-and-state
sources:
- https://datatracker.ietf.org/doc/html/rfc9293
- https://datatracker.ietf.org/doc/html/rfc6528
- https://hpbn.co/building-blocks-of-tcp/
spiral:
- statefulness
- latency
status: ready
summary: Как рандомизация ISN защищает TCP от атак инъекций, и полный автомат состояний
  от CLOSED до TIME-WAIT.
title: Номера последовательности и состояние соединения
track: networking
unit: 03-tcp-handshake
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import TraceScenario from "~/components/pedagogy/TraceScenario.astro";
import FadedExample from "~/components/pedagogy/FadedExample.tsx";
import NumbersCard from "~/components/layout/NumbersCard.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook lang="ru">
Вы знаете три шага рукопожатия. Теперь посмотрите, что именно обменивается в этих шагах: случайные 32-битные счётчики, конкретные значения которых — не произвол, а одновременно механизм безопасности, таймерный механизм и контрольная точка автомата состояний.
</Hook>

<Crux>Рандомизация ISN блокирует инъекции TCP; автомат состояний управляет обеими сторонами от CLOSED до TIME-WAIT.</Crux>

<Explanation>

## Трёхшаговый танец в деталях

Антон отправляет `SYN seq=X`. Он переходит в состояние `SYN-SENT`. Светлана получает пакет, выделяет буфер для входящего направления, переходит в состояние `SYN-RECV` и отвечает `SYN-ACK seq=Y ack=X+1`. `ack=X+1` означает: «Я видела твой номер X; следующий байт, который жду от тебя, — X+1.» (Флаг SYN сам по себе потребляет один порядковый номер, даже если данных не передаётся.) Антон отправляет `ACK seq=X+1 ack=Y+1` и переходит в состояние `ESTABLISHED`. Светлана получает ACK, переходит в `ESTABLISHED` — соединение установлено.

<FadedExample
  client:load
  id="seq-numbers"
  pieceSlug="02-sequence-numbers-and-state"
  lang="ru"
  title="Согласование номеров последовательности"
  steps={{
    solved: (
      <div class="space-y-3">
        <p><strong>Рукопожатие в номерах последовательности:</strong></p>
        <ul class="list-disc list-inside space-y-1 text-sm">
          <li>Антон отправляет: <code class="bg-purple-100 px-1">SYN seq=42_000_000</code></li>
          <li>Светлана отвечает: <code class="bg-blue-100 px-1">SYN-ACK seq=99_000_000 ack=42_000_001</code></li>
          <li>Антон подтверждает: <code class="bg-purple-100 px-1">ACK seq=42_000_001 ack=99_000_001</code></li>
        </ul>
        <p class="text-xs text-gray-600 mt-3"><strong>Почему <code>ack = peer_seq + 1</code>?</strong> Флаг SYN сам потребляет один порядковый номер. Даже без передачи данных получатель ожидает следующий байт по адресу seq+1. Та же логика применяется к флагам FIN при закрытии соединения.</p>
        <p class="text-xs text-gray-600"><strong>После рукопожатия:</strong> первый байт данных Антона использует seq=42_000_001. Если он отправляет 200 байт, следующий сегмент начинается с seq=42_000_201. Светлана подтверждает ответом <code>ack=42_000_201</code>, подтверждая приём всех 200 байт.</p>
      </div>
    ),
    semi: {
      prompt: "Антон отправляет SYN seq=15_000_000. Какой ACK включает Светлана в свой SYN-ACK?",
      blanks: [
        {
          id: "ack1",
          expected: "15000001",
          placeholder: "Введите номер ACK (например, 15000001)",
        },
      ],
    },
    blank: {
      prompt: "SYN-ACK Светланы несёт seq=88_000_000. Антон должен подтвердить её номер. Что несёт поле ACK в финальном пакете ACK Антона?",
      reveal: (
        <div class="space-y-2">
          <p><strong>Ответ: ack=88_000_001</strong></p>
          <p class="text-sm">Флаг SYN в SYN-ACK Светланы потребляет один порядковый номер, поэтому Антон подтверждает следующий ожидаемый байт — 88_000_001.</p>
        </div>
      ),
    },
  }}
  misconceptions={{
    "ack1:15000000": "Почти — но флаг SYN сам потребляет один порядковый номер, поэтому ACK на единицу больше.",
    "ack1:15000002": "Ошибка на единицу. SYN потребляет ровно один порядковый номер, не два.",
  }}
/>

## Начальные порядковые номера (ISN) — почему случайные?

ISN (Initial Sequence Number — начальный порядковый номер) — это стартовое значение счётчика байт соединения. Если бы вы могли предсказать, какой ISN использует следующее соединение удалённого сервера, вы могли бы подделать пакет, выглядящий как легитимный, — не перехватывая ни единого настоящего пакета. Именно это делали злоумышленники до RFC 6528. RFC 6528 требует вычислять ISN как:

```
ISN = M + F(saddr, sport, daddr, dport, secret_key)
```

где `M` увеличивается раз в ~4 мкс (трудно угадать извне), а `F` — хеш четырёхкортежа соединения и секретного ключа. Старые реализации использовали простой счётчик: злоумышленник мог предсказать следующий ISN, подделать пакет с правильным порядковым номером и внедрить команды в установленное соединение. Современные ISN криптографически случайны для каждого соединения, что делает слепую инъекцию невозможной без перехвата трафика.

## Стоимость: один RTT

Антон отправляет SYN, ждёт SYN-ACK (1 RTT), отправляет ACK, и только тогда может отправить HTTP-запрос. При RTT 100 мс (через континент) 100 мс задержки заложены в соединении до первого байта данных приложения. Этот RTT важен — он задаёт ритм всего приложения.

<NumbersCard
  id="card-rtt-costs"
  title="Стоимость RTT при рукопожатии"
  data-lesson-visual
  stats={[
    { label: "RTT в локальной сети (холодный)", value: "0,1–1 мс" },
    { label: "Региональный RTT", value: "20–40 мс" },
    { label: "Межконтинентальный RTT", value: "100–300 мс" },
    { label: "TFO — первое соединение", value: "1 RTT + данные" },
    { label: "TFO — повторное соединение", value: "0 RTT (данные в SYN)" },
    { label: "Преимущество HTTP/2", value: "1 соединение, много потоков" },
  ]}
/>

## Закрытие соединения: FIN и TIME-WAIT

Закрытие — зеркало открытия: каждая сторона независимо отправляет FIN, сигнализируя «у меня больше нет данных», а другая сторона подтверждает ACK. Соединения остаются в состоянии TIME-WAIT в течение 2×MSL (~120 с типично), чтобы запоздавшие повторные передачи могли прийти безвредно, а не быть приняты за данные нового соединения. Инициатор закрытия платит цену TIME-WAIT; на занятых серверах это может стать проблемой — подробнее в уроке о SYN cookies и TFO.

## Автомат состояний

При инциденте первый инструмент, за который вы тянетесь, — `ss -tan`. Состояния, которые он показывает, — не декоративные метки: каждое точно сообщает, где именно зависло соединение и какая сторона в этом виновата. Состояния, с которыми вы столкнётесь:

| Состояние | Значение |
|-----------|---------|
| `LISTEN` | Сервер ожидает SYN |
| `SYN-SENT` | Клиент отправил SYN |
| `SYN-RECV` | Сервер отправил SYN-ACK |
| `ESTABLISHED` | Рукопожатие завершено, нормальный трафик |
| `FIN-WAIT-1` | Инициатор закрытия отправил FIN |
| `FIN-WAIT-2` | Получен ACK на собственный FIN |
| `CLOSE-WAIT` | Получен FIN от пира, приложение должно вызвать close() |
| `LAST-ACK` | Отправлен FIN, ожидается финальный ACK |
| `TIME-WAIT` | Остывание после закрытия (2×MSL) |
| `CLOSED` | Соединения нет |

Накопление `CLOSE-WAIT` на сервере обычно означает, что приложение не вызывает `close()` на принятых сокетах — утечка ресурсов.

<TraceScenario
  id="mid-trace-1"
  pieceSlug="02-sequence-numbers-and-state"
  lang="ru"
  scenario="Проследите успешное трёхстороннее рукопожатие TCP и первый байт данных."
  steps={[
    { prompt: "Клиент отправляет SYN. В каком состоянии клиент?", reveal: "SYN-SENT. Клиент выделяет полуоткрытую структуру соединения и запускает таймер повторной передачи SYN." },
    { prompt: "Сервер получает SYN. Что он выделяет и что отвечает?", reveal: "Сервер выделяет request_sock (или генерирует SYN cookie при переполнении очереди), переходит в SYN-RECV и отвечает SYN-ACK со своим ISN и ack = client_isn + 1." },
    { prompt: "Клиент получает SYN-ACK. Что сообщает клиенту номер ACK?", reveal: "ack = client_isn + 1 подтверждает, что сервер получил SYN клиента. Клиент переходит в ESTABLISHED." },
    { prompt: "Клиент отправляет ACK. Что делает сервер?", reveal: "Сервер проверяет ACK, выделяет полный сокет (или проверяет SYN cookie) и переходит в ESTABLISHED. Рукопожатие завершено." },
    { prompt: "Первый байт данных клиента. Какой номер последовательности он несёт?", reveal: "client_isn + 1. SYN потребил один порядковый номер, поэтому следующий байт использует значение, которое сервер уже подтвердил как ожидаемое." },
  ]}
/>

<DragOrder
  id="mid-drag-1"
  pieceSlug="02-sequence-numbers-and-state"
  lang="ru"
  prompt="Упорядочьте переходы состояний TCP для клиента, открывающего и закрывающего соединение:"
  items={[
    "CLOSED",
    "SYN-SENT (после send(SYN))",
    "ESTABLISHED (после receive(SYN-ACK) и send(ACK))",
    "FIN-WAIT-1 (после send(FIN))",
    "FIN-WAIT-2 (после receive(ACK of FIN))",
    "TIME-WAIT (после receive(FIN) и send(ACK))",
    "CLOSED (после истечения 2×MSL)",
  ]}
/>

<Quiz
  id="mid-quiz-1"
  pieceSlug="02-sequence-numbers-and-state"
  lang="ru"
  question="Почему флаг SYN считается потребляющим один порядковый номер, хотя байт данных не передаётся?"
  choices={[
    { label: "TCP увеличивает последовательность на 1 за каждый пакет", misconception: "TCP увеличивает значение на переданные байты, а не на пакеты." },
    { label: "Потому что SYN должен быть индивидуально подтверждаем — без потребления номера повторные передачи были бы неоднозначны", correct: true },
    { label: "Это пережиток старых версий протокола", misconception: "Поведение спроектировано намеренно, а не унаследовано. Оно обеспечивает надёжную повторную передачу рукопожатия." },
    { label: "Для выравнивания размера пакета", misconception: "Выравнивание заголовков TCP обрабатывается отдельно; семантика последовательностей связана с надёжностью." },
  ]}
/>

<Inset kind="why" lang="ru">
**Почему старые реализации ISN были опасны.** До RFC 6528 многие TCP-стеки увеличивали ISN на фиксированное значение в секунду (~250 000 в секунду в BSD-подобных системах). Злоумышленник, видевший время одного соединения, мог с достаточной точностью предсказать следующий ISN и подделать ACK или сегмент данных — классическая атака предсказания порядкового номера TCP. Рандомизированные ISN с криптографическим смешением четырёхкортежа делают эту атаку вычислительно нецелесообразной без возможности перехватить реальный SYN-ACK.
</Inset>

</Explanation>

<FlowDiagram
  label="Автомат состояний TCP для одного соединения: CLOSED в SYN-SENT в ESTABLISHED при открытии, затем FIN-WAIT-1 в FIN-WAIT-2 в TIME-WAIT в CLOSED при закрытии"
  hue="var(--d-network)"
  perRow={7}
  nodes={[
    { id: "closed", label: "CLOSED" },
    { id: "synsent", label: "SYN-SENT" },
    { id: "estab", label: "ESTABLISHED" },
    { id: "fw1", label: "FIN-WAIT-1" },
    { id: "fw2", label: "FIN-WAIT-2" },
    { id: "tw", label: "TIME-WAIT" },
    { id: "closed2", label: "CLOSED" },
  ]}
  edges={[
    { from: "closed", to: "synsent", label: "send SYN" },
    { from: "synsent", to: "estab", label: "recv SYN-ACK / send ACK" },
    { from: "estab", to: "fw1", label: "send FIN" },
    { from: "fw1", to: "fw2", label: "recv ACK" },
    { from: "fw2", to: "tw", label: "recv FIN / send ACK" },
    { from: "tw", to: "closed2", label: "2×MSL истёк" },
  ]}
  caption="Каждый переход инициируется отправкой или получением конкретного флага. Путь активного закрытия проходит через TIME-WAIT, удерживая четырёхкортеж 2×MSL, чтобы запоздавшие повторные передачи приходили безвредно."
/>

<KeyTakeaway>Рукопожатие обменивается криптографически случайными ISN (RFC 6528) для предотвращения атак инъекций; 11-состоятельный автомат отслеживает обе стороны от CLOSED через ESTABLISHED до TIME-WAIT, каждый переход инициируется отправкой или получением конкретного флага.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="02-sequence-numbers-and-state-retrieval"
  lang="ru"
  questions={[
    { q: "Почему начальные порядковые номера (ISN) рандомизируются, а не начинаются с нуля или глобально инкрементируются?", a: "Старые стеки использовали предсказуемые счётчики, что позволяло злоумышленникам угадать следующий ISN и подделать TCP-сегменты (атаки инъекций или RST). RFC 6528 требует ISN = M + F(saddr,sport,daddr,dport,secret_key), где M — счётчик времени, а F — криптографический хеш четырёхкортежа и секретного ключа. Это делает угадывание ISN нецелесообразным без перехвата реального рукопожатия." },
    { q: "Что означает TIME-WAIT, как долго длится и зачем нужен?", a: "TIME-WAIT — состояние, в котором TCP-сторона находится после отправки финального ACK закрытия соединения. Длится 2×MSL (~120 с типично). Нужен, чтобы запоздавшие повторные передачи FIN могли прийти безвредно, а не быть приняты за данные нового соединения с тем же четырёхкортежем. Без TIME-WAIT быстрое повторное использование четырёхкортежа (src-IP, src-port, dst-IP, dst-port) могло бы испортить новое соединение устаревшими данными старого." },
    { q: "На что почти всегда указывает большое количество сокетов в состоянии CLOSE-WAIT на сервере?", a: "Приложение не вызывает close() на принятых сокетах после того, как удалённый пир отправил FIN. Ядро получает FIN пира и переходит в CLOSE-WAIT, ожидая вызова close() от приложения. Если приложение никогда не делает этого — сокет остаётся в CLOSE-WAIT бесконечно, утекая файловые дескрипторы и память ядра. Типичные причины: отсутствие defer conn.Close() в Go, отсутствие try-with-resources в Java, забытый close() на пути ошибки в C." },
  ]}
/>

<Recap lang="ru">
Трёхстороннее рукопожатие обменивается начальными порядковыми номерами (ISN) в обоих направлениях. ISN вычисляются как счётчик времени плюс криптографический хеш четырёхкортежа соединения (RFC 6528), что делает атаки инъекций нецелесообразными. Флаг SYN потребляет один порядковый номер, чтобы обмен был индивидуально подтверждаем. После установки ESTABLISHED обе стороны отслеживают соединение через 11-состоятельный автомат: LISTEN, SYN-SENT, SYN-RECV, ESTABLISHED, FIN-WAIT-1, FIN-WAIT-2, CLOSE-WAIT, CLOSING, LAST-ACK, TIME-WAIT, CLOSED. TIME-WAIT сохраняется 2×MSL (~120 с) для поглощения запоздавших повторных передач. Теперь, когда вы видите шквал CLOSE-WAIT сокетов на проде, вы знаете: приложение не вызывает close() — ищите пропущенный defer или try-with-resources, пока лимит файловых дескрипторов не уложил сервис.
</Recap>
