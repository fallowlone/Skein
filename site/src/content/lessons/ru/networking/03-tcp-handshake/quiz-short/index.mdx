---
concepts:
- three-way-handshake
- isn-randomisation
- congestion-control
- syn-cookies
- time-wait
- quic-relationship
estMin: 13
lang: ru
level: senior
order: 8
prereqs: []
slug: quiz-short
sources:
- https://datatracker.ietf.org/doc/html/rfc9293
- https://datatracker.ietf.org/doc/html/rfc6528
- https://datatracker.ietf.org/doc/html/rfc8985
status: ready
summary: "Вопросы на свободное воспроизведение по юниту TCP handshake. Ответь своими словами, затем открой модельный ответ и сравни."
title: 'TCP handshake: тест на воспроизведение'
track: networking
unit: 03-tcp-handshake
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";

<Hook>
Воспроизведение бьёт перечитывание. На каждый вопрос проговори или запиши полный ответ по памяти, прежде чем открыть модельный — именно усилие припоминания закрепляет механику.
</Hook>

<Goal lang="ru">
Восстанови ключевые механизмы юнита — почему три шага, почему случайные ISN, как ведут себя SYN cookie и TIME-WAIT на масштабе, когда BBR бьёт CUBIC и как TCP связан с QUIC — не подглядывая в уроки.
</Goal>

<RetrievalDrawer
  client:load
  id="03-tcp-handshake-short-retrieval"
  lang="ru"
  questions={[
    { q: "Почему TCP нужны три сообщения, а не два, и что делает каждый шаг?", a: "TCP двунаправлен, поэтому обе стороны должны обменяться и подтвердить Initial Sequence Number друг друга до того, как потекут данные. SYN несёт ISN клиента; SYN-ACK несёт ISN сервера и подтверждает клиентский; третий ACK подтверждает ISN сервера. Двухшаговый handshake оставил бы sequence number сервера неподтверждённым и не доказал бы, что клиент действительно жив — устаревший или спуфнутый SYN заставил бы сервер аллоцировать состояние для пира, который никогда не ответит. Третий ACK замыкает петлю и ограничивает half-open соединения. Цена — один полный round-trip до первого байта приложения." },
    { q: "Почему Initial Sequence Number рандомизируются на каждое соединение и как это делает RFC 6528?", a: "Предсказуемые ISN позволяют off-path атакующему угадать следующий sequence number, подделать сегмент, попадающий в receive window, и вставить данные или RST в установленное соединение — классическая атака предсказания sequence number. RFC 6528 вычисляет ISN = M + F(saddr, sport, daddr, dport, secret_key), где M — счётчик высокого разрешения по времени, а F — keyed криптографический хэш 4-кортежа. Значение неугадываемо без знания секрета или перехвата самого SYN-ACK, поэтому слепая инъекция становится невозможной. Это целостность/анти-спуфинг, а не конфиденциальность — payload TCP остаётся открытым текстом, пока его не обернёт TLS." },
    { q: "Что такое TIME-WAIT, зачем он, и какие production-фиксы при EADDRNOTAVAIL?", a: "TIME-WAIT — состояние, в которое входит закрывающая соединение сторона после финального ACK, длится 2*MSL (~120 с). Он существует, чтобы запоздалый ретрансмит FIN или данных старого соединения приземлился безвредно, а не был принят за новое соединение на том же 4-кортеже. Загруженный клиент, открывающий много короткоживущих соединений к одному origin, может исчерпать эфемерные порты, удерживаемые в TIME-WAIT, и connect() тогда вернёт EADDRNOTAVAIL. Фиксы по порядку: пул соединений с keep-alive, чтобы одно соединение обслуживало много запросов (настоящий фикс); включить net.ipv4.tcp_tw_reuse=1, который безопасно переиспользует записи TIME-WAIT, когда Timestamps доказывают, что новый SYN свежее (RFC 6191/PAWS); расширить ip_local_port_range. Никогда не используй tcp_tw_recycle — убран в Linux 4.12 за поломку NAT — и никогда просто не понижай MSL, что рискует повреждением устаревшим сегментом." },
    { q: "Объясни, как работает SYN cookie, и что теряют легитимные соединения во время флуда.", a: "Когда half-open SYN-очередь превышает tcp_max_syn_backlog, ядро перестаёт аллоцировать request_sock на каждый SYN. Вместо этого оно ставит ISN в SYN-ACK как токен: верхние 5 бит кодируют индекс MSS, следующие 6 бит — минутный счётчик, а младшие 21 бит — HMAC по 4-кортежу плюс ротируемый секрет. Затем соединение забывается. Когда ACK возвращается, ядро пересчитывает и валидирует cookie перед аллокацией полного сокета, поэтому спуфнутый флуд, который никогда не шлёт ACK, стоит лишь CPU, не память. Цена для настоящих клиентов: в cookie выживает только MSS — Window Scaling, SACK и Timestamps, предложенные в SYN, тихо отбрасываются, поэтому на long-RTT high-bandwidth пути окно упирается в 64 КиБ и пропускная способность страдает, пока активен флуд." },
    { q: "Почему BBR держит пропускную способность на пути с ~1% случайных потерь, где CUBIC обваливается?", a: "CUBIC трактует любую потерю пакета как сигнал congestion и режет congestion window (~0.7x), затем пробует обратно по кубической кривой. На пути с ~1% случайных потерь — cellular, спутник, длинный WAN — частота потерь достаточна, чтобы CUBIC проводил бо́льшую часть времени в recovery и никогда не достигал реальной ёмкости канала. BBR отказывается от loss-based сигнализации: он оценивает bottleneck bandwidth по delivered-byte rate и минимальный RTT по таймштампам пакетов, затем темпирует отправку под bandwidth*RTT. Случайные дропы не сбивают эти оценки, поэтому BBR держится у line rate. Рекомендация: оставить CUBIC на стандартных low-loss дата-центровых/региональных путях и переключаться на BBR там, где случайная потеря нормальна." },
    { q: "Как TCP связан с QUIC и почему QUIC переизобрёл механизмы TCP поверх UDP, а не расширил TCP?", a: "QUIC переимплементирует ключевые идеи TCP — sequence number, ACK, надёжную упорядоченную доставку, congestion control, slow start, fast retransmit — в user space поверх UDP. Так сделано, потому что эволюция TCP означает менять ядра по всему миру, что занимает 5–15 лет из-за длинного хвоста необновлённых систем, и потому что middlebox'ы оссифицируют TCP, вырезая или отклоняя незнакомые опции (та же оссификация застопорила TCP Fast Open). UDP проходит через middlebox'ы неизменным, поэтому QUIC поставляется как обновление библиотеки в браузере или сервере и итерируется на скорости приложения. HTTP/3 работает на QUIC; HTTP/1.1 и HTTP/2 работают на TCP. Поскольку механизмы те же, понимание TCP handshake делает QUIC механистически прозрачным." },
  ]}
/>

<Recap lang="ru">
Если ты смог восстановить каждый ответ по памяти, ты держишь хребет юнита: три шага, потому что оба направления должны подтвердить ISN; рандомизированные ISN, чтобы остановить инъекцию; TIME-WAIT, чтобы поглощать запоздалые сегменты (фиксится на масштабе пулингом и tcp_tw_reuse, никогда tw_recycle или коротким MSL); SYN cookie, чтобы пережить флуд ценой отброшенных опций; BBR вместо CUBIC там, где потеря случайна, а не congestive; и QUIC как те же идеи, перенесённые поверх UDP, чтобы уйти от оссификации протокола.
</Recap>
