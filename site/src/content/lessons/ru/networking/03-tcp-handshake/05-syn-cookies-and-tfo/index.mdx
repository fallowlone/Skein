---
concepts:
- syn-cookies
- syn-flood
- tcp-fast-open
- tfo
- time-wait-exhaustion
- tw-reuse
- ip-local-port-range
- tcp-syncookies
- tcp-max-syn-backlog
- eaddrnotavail
deepensInto:
- 06-bbr-and-production-ops
estMin: 16
lang: ru
lessonType: topic
level: senior
order: 5
prereqs:
- 04-tcp-options-and-pathologies
slug: 05-syn-cookies-and-tfo
sources:
- https://datatracker.ietf.org/doc/html/rfc9293
- https://datatracker.ietf.org/doc/html/rfc7413
- https://datatracker.ietf.org/doc/html/rfc6191
- https://blog.cloudflare.com/how-to-stop-running-out-of-ephemeral-ports-and-start-to-love-long-lived-connections/
- https://kernel.org/doc/html/latest/networking/tcp.html
spiral: []
status: ready
summary: Как SYN cookie обменивают память на CPU при flood-атаке, что экономит TCP
  Fast Open и почему его развёртывание застопорилось, и как устранить исчерпание TIME-WAIT
  на нагруженном балансировщике.
title: SYN cookie, TFO и TIME-WAIT при высокой нагрузке
track: networking
unit: 03-tcp-handshake
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import TraceScenario from "~/components/pedagogy/TraceScenario.astro";
import DebugLog from "~/components/pedagogy/DebugLog.astro";
import NumbersCard from "~/components/layout/NumbersCard.astro";

<Hook lang="ru">
Нагруженный балансировщик начинает возвращать EADDRNOTAVAIL на исходящих соединениях к бэкенду. Команда ss -tan показывает 30 000 сокетов в состоянии TIME-WAIT. Одновременно публичный слушатель атакуется SYN flood. Оба явления решаются через sysctl Linux — но неправильный sysctl ухудшает ситуацию.
</Hook>

<Crux>SYN cookie защищают от SYN flood без затрат памяти. Исчерпание TIME-WAIT — tcp_tw_reuse и пул соединений, а не снижение MSL.</Crux>

<Explanation>

## Внутреннее устройство SYN cookie

При SYN flood очередь полуоткрытых соединений на сервере переполняется. Вместо выделения `request_sock` для каждого SYN ядро использует путь в `net/ipv4/tcp_ipv4.c:cookie_v4_init_sequence`:

```
ISN_в_SYN-ACK = MAC от (saddr, sport, daddr, dport, isn, secret)
```

Старшие 5 бит кодируют индекс MSS (8 вариантов), следующие 6 бит — счётчик минут (окно повтора), нижние 21 бит — HMAC. Соединение тут же забывается. Когда приходит ACK, `cookie_v4_check` пересчитывает и проверяет. Если проверка прошла — ядро выделяет полный сокет. Если нет — ACK молча отбрасывается.

**Компромисс:** CPU вместо памяти — хорошо для больших серверов, не для маленьких. **Цена:** в cookie выживает только MSS. Масштаб окна, SACK и метки времени, предложенные в исходном SYN, молча отбрасываются для соединений, прошедших проверку через cookie. На высококачественном пути с большим RTT во время flood пропускная способность легитимных клиентов деградирует — окно ограничено 64 КиБ без масштабирования.

Linux включает SYN cookie, когда очередь полуоткрытых соединений превышает `tcp_max_syn_backlog` (зависит от системы, обычно 4096). Включается через `net.ipv4.tcp_syncookies=1` (по умолчанию с ядра 2.4). Значение `2` форсирует безусловную генерацию cookie для тестирования.

<NumbersCard
  id="card-syn-cookies"
  title="Компромиссы SYN cookie"
  data-lesson-visual
  stats={[
    { label: "Cookie защищает от", value: "SYN flood / переполнения очереди полуоткрытых" },
    { label: "Что выживает в cookie", value: "Только индекс MSS (5 бит)" },
    { label: "Что молча отбрасывается", value: "Масштаб окна, SACK, метки времени" },
    { label: "Окно без масштабирования", value: "Максимум 64 КиБ на соединение" },
    { label: "Дефолт Linux", value: "tcp_syncookies=1 (условный)" },
    { label: "Принудительно для тестов", value: "tcp_syncookies=2" },
  ]}
/>

<DebugLog
  id="sr-debug-1"
  pieceSlug="05-syn-cookies-and-tfo"
  lang="ru"
  title="Трассировка tcpdump SYN flood с включёнными SYN cookie"
  output={`$ sudo tcpdump -i eth0 'tcp[tcpflags] & tcp-syn != 0' -tnn
14:23:45.123456 IP 203.0.113.50.34521 > 198.51.100.10.443: Flags [S], seq 1000000, win 29200, length 0
14:23:45.123489 IP 198.51.100.10.443 > 203.0.113.50.34521: Flags [S.], seq 3851629874, ack 1000001, win 28960, length 0
14:23:45.124000 IP 203.0.113.50.34522 > 198.51.100.10.443: Flags [S], seq 2000000, win 29200, length 0
14:23:45.124050 IP 198.51.100.10.443 > 203.0.113.50.34522: Flags [S.], seq 4025814937, ack 2000001, win 28960, length 0
14:23:45.125000 IP 203.0.113.50.34521 > 198.51.100.10.443: Flags [.], seq 1000001, ack 3851629875, win 29200, length 0
14:23:45.125010 IP 203.0.113.50.34522 > 198.51.100.10.443: Flags [.], seq 2000001, ack 4025814938, win 29200, length 0
14:23:45.200000 IP 203.0.113.100.55000 > 198.51.100.10.443: Flags [S], seq 5000000, win 29200, length 0
14:23:45.200050 IP 198.51.100.10.443 > 203.0.113.100.55000: Flags [S.], seq 2941837465, ack 5000001, win 28960, length 0
(ACK от 203.0.113.100 не пришёл; SYN flood с разных поддельных адресов)`}
  outputLang="log"
  question="Два ACK пришли (легитимные клиенты завершили рукопожатие), но третий источник ACK не отправил. Есть ли риск для сервера?"
  hint="Отправляются ли SYN-ACK на каждый входящий SYN? Что предотвращает исчерпание памяти, когда ACK не приходит?"
  answer="Два легитимных клиента (203.0.113.50) завершают рукопожатие. Третий источник (203.0.113.100) отправляет SYN, но не ACK — подделанный или недоступный. При tcp_syncookies=1 (по умолчанию) сервер не выделяет записи request_sock для каждого SYN; MSS закодирован в ISN, и ядро проверит ACK, когда/если он придёт. Без cookie очередь полуоткрытых растёт к tcp_max_syn_backlog и затем соединения отвергаются. С включёнными cookie риска нет — только CPU на вычисление HMAC для каждого SYN-ACK и проверку ACK, но не исчерпание памяти."
/>

## TCP Fast Open (TFO, RFC 7413)

Стандартное рукопожатие стоит 1 RTT до данных приложения. TFO устраняет это для последующих соединений:

1. **Первое соединение:** сервер отправляет опцию Fast Open Cookie в SYN-ACK; клиент кэширует её для пары (IP-сервера, порт-сервера).
2. **Последующие соединения:** клиент помещает cookie в SYN вместе с данными приложения размером до MSS; сервер проверяет и обрабатывает данные, пока завершает рукопожатие — экономя 1 RTT.

На RTT 280 мс (Лондон → Сидней) TFO экономит 280 мс на каждом тёплом соединении.

**Реальность развёртывания:** проблемы с промежуточными устройствами убили широкое серверное распространение TFO. Middlebox-устройства (файрволы, DPI, некоторые NAT-шлюзы) вырезают опцию TFO из SYN, ломая доставку cookie. Некоторые NAT-шлюзы ломают cookie при смене внешнего IP клиента. Откат обязателен, но добавляет задержку. Серверное развёртывание ограничено; большинство интереса к 0-RTT сместилось к QUIC, который работает поверх UDP и обходит протокольное окостенение сетевой инфраструктуры. Linux поддерживает обе стороны с ядра 3.6 (2012) через битовую маску `net.ipv4.tcp_fastopen` (1=клиент, 2=сервер, 3=оба).

<TraceScenario
  id="sr-trace-1"
  pieceSlug="05-syn-cookies-and-tfo"
  lang="ru"
  scenario="Нагруженный балансировщик возвращает EADDRNOTAVAIL на исходящих соединениях к одному апстриму. Проследите диагностику и исправление."
  steps={[
    { prompt: "Шаг 1: что означает EADDRNOTAVAIL на connect()?", reveal: "Ядро не может выбрать эфемерный порт-источник для нового четырёхкортежа (локальный-IP, локальный-порт, удалённый-IP, удалённый-порт). Каждый порт в эфемерном диапазоне уже используется для этого назначения." },
    { prompt: "Шаг 2: куда уходят порты?", reveal: "Команда ss -tan | grep TIME-WAIT | wc -l возвращает десятки тысяч TIME-WAIT сокетов к апстриму. Балансировщик закрывает соединения быстрее, чем они выходят из TIME-WAIT (~120 с)." },
    { prompt: "Шаг 3: почему бы не уменьшить MSL для более быстрого выхода из TIME-WAIT?", reveal: "Снижение MSL создаёт риск принятия устаревших ретрансмиссий FIN как данных нового соединения на том же четырёхкортеже — тихая порча данных. Совместимые со стандартами серверы держат 2×MSL." },
    { prompt: "Шаг 4: включаем net.ipv4.tcp_tw_reuse=1. Почему это безопасно?", reveal: "tcp_tw_reuse позволяет повторно использовать TIME-WAIT записи для новых исходящих соединений только тогда, когда метки времени доказывают, что новый SYN новее последнего сегмента предыдущего соединения (RFC 6191 / PAWS). Риска порчи данных нет. Никогда не включайте tcp_tw_recycle — он был удалён в Linux 4.12, потому что сбрасывал легитимные SYN от клиентов за NAT." },
    { prompt: "Шаг 5: структурное исправление?", reveal: "Используйте пул соединений с keep-alive, чтобы каждое соединение к апстриму обслуживало множество запросов. Балансировщик перестаёт сжигать четырёхкортеж на каждый запрос, и проблема исчезает полностью. Дополнительно: расширьте net.ipv4.ip_local_port_range до '1024 65535' для ~64к эфемерных портов." },
  ]}
/>

<Quiz
  id="sr-quiz-1"
  pieceSlug="05-syn-cookies-and-tfo"
  lang="ru"
  question="Старший инженер утверждает, что SYN cookie не имеют никакой производительностной цены. В чём ошибка этого утверждения?"
  choices={[
    { label: "Cookie бесплатны — HMAC вычисляется аппаратно", misconception: "HMAC вычисляется программно и добавляет нагрузку на CPU для каждого SYN/ACK." },
    { label: "Cookie кодируют только MSS — масштаб окна, SACK и метки времени молча отбрасываются для соединений, прошедших проверку через cookie, ограничивая пропускную способность на путях с большим RTT во время flood", correct: true },
    { label: "Cookie раскрывают секретный ключ сервера", misconception: "Секретный ключ ротируется периодически (счётчик минут); риск раскрытия низкий." },
    { label: "Cookie работают только на IPv4", misconception: "Linux поддерживает SYN cookie как на IPv4, так и на IPv6." },
  ]}
/>

<Quiz
  id="sr-quiz-2"
  pieceSlug="05-syn-cookies-and-tfo"
  lang="ru"
  question="Что делал tcp_tw_recycle и почему он был удалён из ядра Linux в 4.12?"
  choices={[
    { label: "Это была более быстрая опция переработки TIME-WAIT, которую все должны были использовать", misconception: "Он был удалён именно потому, что вызывал аварии." },
    { label: "Он включал переработку TIME-WAIT на основе меток времени, которая молча отбрасывала легитимные SYN от клиентов за NAT — несовместимо с современным интернетом", correct: true },
    { label: "Он был переименован в tcp_tw_reuse", misconception: "tcp_tw_reuse уже существовал наряду с tw_recycle; у них разная семантика." },
    { label: "Он был объединён с путём SYN cookie", misconception: "SYN cookie работают с очередью полуоткрытых SYN, а не с TIME-WAIT сокетами." },
  ]}
/>

<Inset kind="why" lang="ru">
**Почему интерес к TFO сместился к QUIC.** TFO требует, чтобы клиентская библиотека и серверное приложение явно подключились, чтобы middlebox-устройства не вырезали опцию, и чтобы NAT-шлюзы не меняли адреса в полёте. QUIC обходит всё это, работая поверх UDP — middlebox-устройства, которые его не понимают, просто пересылают, и протокол может эволюционировать без изменений ядра. 0-RTT возобновление QUIC достигает той же экономии задержки, что и TFO, но с гораздо лучшим успехом развёртывания. Это паттерн, повторяющийся в сетях: когда существующий протокольный уровень слишком закостенел для эволюции, инновации перемещаются на уровень выше.
</Inset>

</Explanation>

<KeyTakeaway>SYN cookie защищают от SYN flood, кодируя состояние соединения в ISN (теряя SACK, масштаб окна и метки времени для этого соединения). Исчерпание TIME-WAIT требует tcp_tw_reuse и пула соединений, а не снижения MSL. Никогда не включайте tcp_tw_recycle — удалён в 4.12 за поломку NAT.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="05-syn-cookies-and-tfo-retrieval"
  lang="ru"
  questions={[
    { q: "Объясните механизм SYN cookie: что кодируется в cookie, что теряется и когда ядро активирует cookie?", a: "Когда очередь полуоткрытых SYN превышает tcp_max_syn_backlog, ядро генерирует SYN-ACK, чей порядковый номер является MAC от четырёхкортежа соединения плюс ротируемого секрета. Старшие 5 бит кодируют индекс MSS (8 вариантов), следующие 6 бит — счётчик минут, нижние 21 бит — HMAC. Сервер тут же забывает соединение. Когда приходит ACK, ядро пересчитывает cookie для проверки и затем выделяет состояние. Что теряется: масштаб окна, SACK и метки времени молча отбрасываются для соединений, прошедших проверку через cookie, ограничивая пропускную способность. Путь cookie дёшев по CPU, но ухудшает работу легитимных клиентов с большим RTT во время flood." },
    { q: "Что такое исчерпание TIME-WAIT, что его вызывает и каковы три производственных исправления?", a: "Исчерпание TIME-WAIT: эфемерный диапазон портов Linux по умолчанию 32768-60999 (~28к портов). Активный клиент, открывающий много коротких соединений к одному адресату, накапливает TIME-WAIT записи (живущие 2×MSL = ~120 с). Когда все порты к этому адресату в TIME-WAIT, connect() возвращает EADDRNOTAVAIL. Причины: короткоживущие HTTP/1.1 соединения к одному апстриму без keep-alive. Три исправления: (1) net.ipv4.tcp_tw_reuse=1 — переиспользует TIME-WAIT записи для исходящих соединений, когда метки времени доказывают отсутствие неоднозначности; (2) расширить net.ipv4.ip_local_port_range до 1024-65535 для ~64к портов; (3) использовать пул соединений с keep-alive (настоящее исправление — перестаёт сжигать четырёхкортеж на запрос)." },
    { q: "Почему TCP Fast Open получил ограниченное серверное распространение, несмотря на доступность с Linux 3.6?", a: "Middlebox-устройства (файрволы, DPI-апплайенсы, NAT-шлюзы) часто вырезают опцию TFO из SYN-пакетов, ломая доставку cookie и молча откатываясь к 1 RTT. Некоторые NAT ломают cookie, меняя IP источника в полёте. Операторы серверов не хотят включать TFO без уверенности в сетевых путях пользователей. Большинство интереса к 0-RTT переместилось к QUIC, который работает поверх UDP и полностью обходит вырезание TCP-опций middlebox-устройствами." },
  ]}
/>

<Recap lang="ru">
SYN cookie защищают нагруженные серверы от SYN flood, вычисляя криптографический токен из четырёхкортежа соединения и кодируя его в порядковом номере SYN-ACK, так что память не выделяется до прихода валидного ACK. Цена: в cookie выживает только MSS — SACK, масштаб окна и метки времени отбрасываются, ухудшая пропускную способность для легитимных клиентов с большим RTT во время flood. Включайте через tcp_syncookies=1 (по умолчанию) и убедитесь, что backlog достаточно велик, чтобы cookie не нужны были при стационарной нагрузке. Исчерпание TIME-WAIT — EADDRNOTAVAIL на connect() — устраняется через tcp_tw_reuse=1 (безопасное переиспользование на основе меток времени согласно RFC 6191), расширением ip_local_port_range и использованием пула соединений. Никогда не используйте tcp_tw_recycle (удалён в Linux 4.12, ломает NAT). TCP Fast Open экономит 1 RTT для тёплых соединений, но требует непрерывных middlebox-устройств; QUIC вытеснил большинство интереса к TFO.
</Recap>
