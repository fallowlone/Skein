---
concepts:
- bbr
- cubic
- reno
- bbr-v3
- tcp-congestion
- production-observability
- rst
- mptcp
- ktls
- quic
- ss-command
- nstat
- tcp-congestion-control
deepensInto: []
estMin: 18
lang: ru
lessonType: topic
level: senior
order: 6
prereqs:
- 05-syn-cookies-and-tfo
slug: 06-bbr-and-production-ops
sources:
- https://datatracker.ietf.org/doc/html/rfc9293
- https://datatracker.ietf.org/doc/html/rfc8684
- https://kernel.org/doc/html/latest/networking/tcp.html
- https://man7.org/linux/man-pages/man7/tcp.7.html
spiral:
- latency
status: ready
summary: Модель пропускной способности-RTT в BBR удерживает throughput на путях с
  потерями, где CUBIC рушится. Производственные выводы ss/tcpdump/nstat, семантика
  RST, MPTCP, kTLS и связь TCP с QUIC.
title: BBR, производственная наблюдаемость и за пределами TCP
track: networking
unit: 03-tcp-handshake
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import TraceScenario from "~/components/pedagogy/TraceScenario.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import DesignPrompt from "~/components/pedagogy/DesignPrompt.astro";
import DebugLog from "~/components/pedagogy/DebugLog.astro";
import RFCQuiz from "~/components/pedagogy/RFCQuiz.astro";
import NumbersCard from "~/components/layout/NumbersCard.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";
import Infographic from "~/components/diagram/Infographic.astro";
import lossThroughput from "./infographics/loss-throughput.svg?raw";

<Hook lang="ru">
Сервис потоковой передачи видео раздаёт 4K-сегменты по межконтинентальным мобильным путям (RTT 150 мс, 1% случайных потерь). Пропускная способность CUBIC рушится до доли от реальной ёмкости канала. Переключение на BBR удерживает throughput близко к максимуму на том же пути. Разница не в пропускной способности — в разных ответах на вопрос «что означает потерянный пакет?»
</Hook>

<Crux>BBR моделирует узкое место через полосу и RTT, игнорируя потери; CUBIC реагирует на потери. Выбор — по профилю потерь пути.</Crux>

<Explanation>

## BBR против CUBIC против Reno

Выбор алгоритма управления перегрузкой — одно из самых рычажных решений по настройке, но большинство инженеров никогда его не касаются. Когда видите обвал пропускной способности на мобильной или межконтинентальной линии, первый вопрос: ваш алгоритм считает каждый сброшенный пакет признаком перегрузки, или он измеряет путь напрямую?

**Reno (классический):** делить cwnd пополам при потерях, аддитивное увеличение за RTT. Простой, широко реализован.

**CUBIC (дефолт Linux с 2.6.19):** функция роста в форме кубической кривой — вогнутый зонд ниже предыдущего максимума, выпуклый зонд выше него. Уменьшает ~0,7× при потерях. Восстанавливает пропускную способность на путях с высоким BDP быстрее, чем Reno.

**BBR (Bottleneck Bandwidth and RTT):** полностью отказывается от сигнализации на основе потерь. Напрямую оценивает пропускную способность узкого места пути (через скорость доставленных байт) и минимальный RTT (через метки времени пакетов), затем устанавливает темп отправки в соответствии с ними. Потери трактуются как неоднозначный шум — могут быть перегрузкой или случайными сбросами. На 1% случайных потерь BBR удерживает пропускную способность близко к максимуму; CUBIC оседает на доле от этого значения.

<Infographic
  svg={lossThroughput}
  label="Столбчатая диаграмма достигнутой пропускной способности на одном канале 20 Мбит/с при 1% случайных потерь: CUBIC проседает до менее 2 Мбит/с, а BBR удерживает близко к 20 Мбит/с."
  caption="Тот же канал, те же потери. CUBIC читает 1% случайных сбросов как перегрузку и режет окно в восстановление; BBR игнорирует потери и держит темп по измеренному произведению полосы на задержку — весь аргумент в пользу BBR на путях с потерями и большим RTT в одном разрыве."
  hue="var(--d-infra)"
/>

**BBRv3 (релиз Google 2023 г.):** исправляет баги преждевременного зонда и конвергенции в BBRv2. Развёрнут на google.com, YouTube, Cloudflare и Netflix. По состоянию на начало 2025 г. не влит в mainline Linux; требует кастомного ядра или сторонского бэкпорта. Mainline Linux 6.x поставляется с BBR 1.x.

**Практические рекомендации:** используйте CUBIC для серверов общего назначения на стандартных ядрах, стандартных датацентровых и региональных ISP путях, где потери редки и означают перегрузку. Переходите на BBR для трансконтинентального WAN, мобильных или спутниковых путей, где 0,5–2% случайных потерь — норма и снижение CUBIC рушит throughput.

Установка для сокета: `setsockopt(SOL_TCP, TCP_CONGESTION, "bbr")`. Для всей системы: `net.ipv4.tcp_congestion_control=bbr`.

<TraceScenario
  id="sr-trace-1"
  pieceSlug="06-bbr-and-production-ops"
  lang="ru"
  scenario="Проследите эпизод перегрузки BBR на мобильном пути с потерями и объясните, почему BBR удерживает throughput, а CUBIC рушится."
  steps={[
    { prompt: "Телефон на мобильной связи (1% случайных потерь, RTT 50 мс, узкое место 20 Мбит/с) работает с CUBIC. Что происходит с cwnd?", reveal: "CUBIC трактует каждую потерю как сигнал перегрузки и режет cwnd × 0,7 при каждом событии потери. При 1% потерь CUBIC большую часть времени находится в восстановлении, никогда не достигая ёмкости канала 20 Мбит/с — throughput рушится до менее 2 Мбит/с." },
    { prompt: "Тот же путь переключается на BBR. Чем отличается реакция BBR на потери?", reveal: "BBR не трактует потери как сигнал перегрузки. Он непрерывно оценивает пропускную способность узкого места (по скорости ACK) и минимальный RTT (по меткам времени отправки). Потери пакетов игнорируются как неоднозначные. BBR устанавливает темп отправки приблизительно равным bandwidth × RTT, заполняя канал без переполнения." },
    { prompt: "Телефон теряет 1% пакетов. Меняется ли оценка BBR?", reveal: "Нет. Оценки пропускной способности и RTT у BBR не зависят от потерь. Сброс 1% не изменяет скорость отправки. Throughput остаётся близким к 20 Мбит/с." },
    { prompt: "CUBIC теперь конкурирует с BBR на одном канале. В чём компромисс?", reveal: "На путях с глубокими буферами и потерями CUBIC менее агрессивен, чем BBR, поэтому CUBIC голодает в присутствии BBR. BBR получает большую часть полосы, CUBIC — остаток. На наземных каналах с мелкими буферами CUBIC конкурентоспособен, потому что потери редки и BBR не даёт преимущества." },
    { prompt: "Какая настройка ядра включает BBR для сокета, и в чём оговорка в 2026 г.?", reveal: "setsockopt(SOL_TCP, TCP_CONGESTION, 'bbr') переключает на BBR. BBRv3 не влит в mainline Linux по состоянию на начало 2025 г.; стандартные ядра используют BBR 1.x. Google, Cloudflare и Netflix работают с BBRv3 на кастомных ядрах." },
  ]}
/>

<TradeoffMatrix
  id="sr-tradeoff-1"
  pieceSlug="06-bbr-and-production-ops"
  lang="ru"
  prompt="Сервис потоковой передачи видео раздаёт 4K-сегменты по долгим мобильным путям (RTT часто больше 150 мс, случайные потери 0,5–2%). Выберите алгоритм управления перегрузкой TCP + комбинацию настроек."
  options={[
    { name: "CUBIC + дефолтные настройки Linux", summary: "Стандартный дефолт.", justification: "CUBIC агрессивно режет cwnd при каждой потере; на пути с 0,5% случайных потерь throughput рушится до доли от ёмкости канала. Это именно симптом, который испытывает каждый мобильный стриминг." },
    { name: "BBR + IW=10 + настроенный tcp_notsent_lowat", summary: "Модель bandwidth-RTT + настроенный headroom.", correct: true, justification: "BBR игнорирует случайные потери как сигнал перегрузки и использует RTT + доставленную полосу, удерживая throughput на путях с потерями. tcp_notsent_lowat снижает bloat буфера сокета (лучше задержка p99). Initial Window 10 MSS разгоняет видеосегмент в первый RTT." },
    { name: "Reno + огромные буферы сокетов", summary: "Классика + всё буферизовать.", justification: "Reno ещё чувствительнее к потерям, чем CUBIC. Большие буферы создают bufferbloat — скачки задержки, ухудшающие адаптивное переключение битрейта. Неверное направление." },
    { name: "CUBIC + большой MSS через jumbo frames", summary: "Больше байт на пакет.", justification: "Jumbo frames ломаются на большинстве интернет-путей (нет сквозного MTU 9000 байт между телефонами и CDN-краями). Не решает реальную проблему: чувствительность к потерям." },
  ]}
/>

## Производственная наблюдаемость

`ss -tin` выводит живые значения cwnd, RTT, дисперсию RTT, повторные передачи и состояние откатного таймера на соединение — без накладных расходов на ядро. Запускайте на любом производственном хосте.

```bash
$ ss -tin state established | grep -A1 "dport 443"
# Вывод включает: cwnd:10 ssthresh:2147483647 rtt:42.8/8.5 acked:142 retrans:0/0
```

Ключевые поля:
- `cwnd`: текущее окно перегрузки в MSS
- `rtt/rttvar`: сглаженный RTT и дисперсия в миллисекундах
- `retrans:sent/outstanding`: суммарные повторные передачи
- `ssthresh`: порог медленного старта (2147483647 = бесконечность = в медленном старте)

`ss -s` суммирует количество сокетов по состоянию — следите за скачками CLOSE-WAIT.

`nstat` раскрывает счётчики из `/proc/net/netstat`: `RetransSegs`, `TCPSlowStartRetrans`, `TCPDSACKRecv`. Для долгосрочного мониторинга `tcp_diag` питает Prometheus-экспортёры (`node_exporter` раскрывает большинство метрик); SLO-важные метрики — скорость повторных передач, RTT p95/p99 и соотношение CLOSE-WAIT:ESTABLISHED.

<DebugLog
  id="sr-debug-1"
  pieceSlug="06-bbr-and-production-ops"
  lang="ru"
  title="Вывод ss во время аварии — диагностируйте проблему"
  output={`$ ss -tan state established | wc -l
12384
$ ss -tan state close-wait | wc -l
9821
$ ss -tan state time-wait | wc -l
1247
$ ss -s
Total: 12500
TCP:   23552 (estab 12384, closed 8920, orphaned 2, timewait 1247)
$ ps -p 1234 -o pid,stat,rss,vsz,cmd
  PID STAT  RSS    VSZ CMD
 1234 Ssl 8392000 12000000 /usr/bin/app-server`}
  outputLang="log"
  question="12к ESTABLISHED + 9,8к CLOSE-WAIT сокетов и RSS растёт. В чём баг и как его исправить?"
  hint="CLOSE-WAIT означает, что сосед отправил FIN, но приложение не вызвало close(). Что означают 9,8к таких записей?"
  answer="9,8к CLOSE-WAIT означает 9800 сокетов, где удалённый сосед завершил отправку и прислал FIN, но приложение так и не вызвало close(). Ядро не может освободить их. Это утечка сокетов: отсутствует defer conn.Close() в Go, отсутствует try-with-resources в Java, забытый close() в ветке ошибок на C. Каждый утечённый CLOSE-WAIT удерживает память ядра и файловый дескриптор. RSS подтверждает утечку. Исправление: проверьте путь закрытия соединения, добавьте defer/try-with-resources и следите за возвратом счётчика CLOSE-WAIT к нулю."
/>

## Семантика RST

RST в TCP — это резкое закрытие соединения: без обмена FIN, без TIME-WAIT, получатель немедленно удаляет состояние соединения. Возникает когда:

- Пакет приходит на порт, который никто не слушает.
- Приложение вызывает `close()` на сокете с непрочитанными данными и `SO_LINGER` с lingertime=0.
- Сосед отправляет мусор, нарушающий конечный автомат.
- Stateful файрвол решает, что соединение простаивает.

**RST-атаки:** злоумышленник, способный угадать порядковые номера в пределах окна приёма, может подделать RST и разорвать установленное соединение. RFC 5961 ужесточает допустимое окно RST. Долгоживущие простаивающие соединения (BGP-сессии, SSH) наиболее уязвимы.

## MPTCP (RFC 8684)

Multipath TCP переносит одно логическое соединение по нескольким путям (Wi-Fi + мобильная сеть, сервер с несколькими NIC). Рукопожатие MPTCP добавляет опцию `MP_CAPABLE` в SYN/SYN-ACK/ACK; если оба конца поддерживают её, устанавливается первый подпоток, а дополнительные подпотоки открываются на разных интерфейсах через `MP_JOIN`. iOS использует MPTCP с iOS 7 для Siri. Linux 5.6+ поставляется с RFC 8684. Ограниченное распространение за пределами Apple из-за middlebox-устройств, не понимающих опцию и откатывающихся к обычному TCP.

## kTLS + TCP

kTLS (Linux 4.13+ TX, 4.17+ RX, NIC offload в 6.0+) переносит симметричное шифрование TLS-записей в ядро через `setsockopt(SOL_TLS, ...)`. После завершения TLS-рукопожатия в пространстве пользователя ядро берёт на себя шифрование записей; в сочетании с `sendfile()` файлы перемещаются из page-cache в NIC, не попадая в пространство пользователя. Netflix сообщает об экономии 8–29% CPU при доставке статических ресурсов. kTLS не меняет поведение TCP — управление перегрузкой, повторные передачи, управление окном остаются стандартными.

## Связь TCP с QUIC

TCP — один уровень в стеке; TLS находится непосредственно поверх него; HTTP/1.1 и HTTP/2 работают на TLS. HTTP/3 — исключение: он работает на QUIC, который использует UDP и заново изобретает надёжность и управление перегрузкой в пространстве пользователя. Причина: эволюция TCP в пространстве ядра оказалась слишком медленной. Уроки TCP — порядковые номера, ACK, управление перегрузкой, медленный старт, быстрая повторная передача — все воспроизводятся в QUIC, только на другом уровне. Понимание TCP делает QUIC механистически прозрачным; обратное неверно.

<RFCQuiz
  id="sr-rfc-1"
  pieceSlug="06-bbr-and-production-ops"
  lang="ru"
  question="Какой RFC описывает RACK-TLP — современный алгоритм обнаружения потерь, используемый Linux для избегания ожидания таймера RTO?"
  choices={[
    { rfc: "RFC 6298", title: "Вычисление таймера повторной передачи" },
    { rfc: "RFC 8985", title: "RACK-TLP: алгоритм обнаружения потерь на основе времени", correct: true },
    { rfc: "RFC 7413", title: "TCP Fast Open" },
    { rfc: "RFC 9293", title: "Базовая спецификация TCP" },
  ]}
/>

<DesignPrompt
  id="sr-design-1"
  pieceSlug="06-bbr-and-production-ops"
  lang="ru"
  prompt="Разработайте набор настроек ядра для высоконагруженного API-шлюза, завершающего 200к HTTPS-соединений/сек. Исходящий трафик к ~50 пулам бэкендов, преимущественно короткие HTTP/1.1 запросы с keep-alive."
  constraints={[
    "Никаких внешних зависимостей, кроме sysctl Linux >= 6.0.",
    "Устойчивость к SYN flood на публичном слушателе.",
    "Избегание исчерпания TIME-WAIT на исходящем трафике к пулам бэкендов.",
    "Задержка p99 менее 50 мс при стационарной нагрузке.",
  ]}
  canonical="Публичный слушатель: net.ipv4.tcp_syncookies=1 (дефолт); tcp_max_syn_backlog=65535; somaxconn=65535; tcp_synack_retries=2 (быстрее отказ под атакой); tcp_fastopen=2 (серверная сторона). Исходящие к бэкендам: net.ipv4.ip_local_port_range=1024 65535 (~64к эфемерных портов); net.ipv4.tcp_tw_reuse=1 (безопасное переиспользование эфемерных портов); включить keep-alive в пуле соединений приложения для множественных запросов на соединение. Перегрузка: tcp_congestion_control=bbr (меньше задержки под всплесками); IW=10 по умолчанию. Буферы: tcp_notsent_lowat=131072 для ограничения bloat; автомасштабирование rmem/wmem (rmem_max=16M, wmem_max=16M). Отключить Нейгла через TCP_NODELAY на каждом сокете. Мониторинг ss -tin; алерт при скорости повторных передач >0,1% и RTT p99 >30 мс."
  keyPoints={[
    "tcp_syncookies + поднятый backlog защищают от SYN flood без блокировки легитимного трафика.",
    "tcp_tw_reuse для исходящих, НЕ tcp_tw_recycle (удалён в 4.12, ломает NAT).",
    "Широкий диапазон локальных портов + пул соединений — настоящая защита от исчерпания TIME-WAIT.",
    "Алгоритм перегрузки BBR снижает RTT p99 и лучше справляется со случайными потерями, чем CUBIC.",
    "TCP_NODELAY на уровне сокета устраняет задержки Nagle/delayed-ACK на маленьких запросах.",
    "Измеряйте непрерывно; не настраивайте вслепую.",
  ]}
/>

<Inset kind="why" lang="ru">
**Почему QUIC работает поверх UDP, а не расширяет TCP.** Каждая функция TCP должна быть реализована в ядрах по всему миру — процесс, занимающий десятилетия из-за длинного хвоста необновлённых систем. QUIC работает в пространстве пользователя (или как библиотека), поэтому функции можно добавлять и развёртывать через обновление браузера или сервера, а не через обновление ядра. Цена — заново изобретать всё, что предоставляет TCP (надёжность, порядок, управление перегрузкой) в пространстве пользователя; выгода — возможность эволюционировать в темпе интернета, а не в темпе обновлений ядра. TCP никуда не исчезнет — он несёт подавляющее большинство интернет-трафика и будет делать это десятилетиями — но QUIC представляет признание того, что окостенение протокола TCP в ядре — реальное инженерное ограничение.
</Inset>

</Explanation>

<FlowDiagram
  label="Контур управления BBR: измерить скорость доставленных байт и минимальный RTT, перемножить для bandwidth-delay product, темпировать отправки чтобы заполнить, но не переполнить узкое место; потеря пакетов игнорируется как сигнал перегрузки"
  hue="var(--d-network)"
  perRow={4}
  nodes={[
    { id: "meas", label: "Измерить путь", sub: "скорость + min-RTT" },
    { id: "model", label: "Модель узкого места", sub: "BDP = BW × RTT" },
    { id: "pace", label: "Темп отправки", sub: "заполнить, не перелить" },
    { id: "loss", label: "Потеря пакета", sub: "сигнал игнорируется" },
  ]}
  edges={[
    { from: "meas", to: "model", label: "оценки BW, RTT" },
    { from: "model", to: "pace", label: "задать темп" },
    { from: "pace", to: "meas", label: "наблюдать ACK" },
    { from: "loss", to: "meas", label: "не нарушает оценку" },
  ]}
  caption="Контур BBR управляется измеренной полосой и минимальным RTT, а не потерями. Случайный сброс на мобильном пути не даёт сигнала в оценку, поэтому темп держится близко к максимуму — там, где CUBIC срезал бы cwnd ×0,7 и обрушил throughput."
/>

<KeyTakeaway>BBR напрямую моделирует узкое место (пропускная способность + минимальный RTT) и игнорирует потери, удерживая throughput на путях с потерями, где CUBIC рушится. Производственный мониторинг: ss -tin для живого cwnd/RTT, nstat для счётчиков повторных передач, Prometheus node_exporter для SLO. RST разрывает соединения без TIME-WAIT; QUIC заново изобретает TCP в пространстве пользователя, чтобы избежать окостенения протокола.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="06-bbr-and-production-ops-retrieval"
  lang="ru"
  questions={[
    { q: "Объясните, почему BBR удерживает throughput на пути с 1% случайных потерь, а CUBIC рушится.", a: "CUBIC трактует каждую потерю как сигнал перегрузки и агрессивно режет окно перегрузки (×0,7), затем медленно зондирует обратно. На пути с 1% случайных потерь CUBIC большую часть времени находится в восстановлении — скорость потерь достаточно высока, чтобы CUBIC никогда не достигал реальной ёмкости пути; throughput оседает на малой доле от узкого места. BBR игнорирует потери как сигнал перегрузки. Он оценивает пропускную способность узкого места, измеряя доставленные байты за единицу времени, и минимальный RTT, отслеживая наименьший наблюдаемый RTT в окне, затем устанавливает темп отправки, заполняя, но не переполняя узкое место. Случайные сбросы пакетов не нарушают оценку. Результат: BBR удерживает throughput близко к ёмкости пути там, где CUBIC не может." },
    { q: "Что показывает ss -tin о живом TCP-соединении, чего не показывает netstat?", a: "ss использует интерфейс netlink tcp_diag ядра и раскрывает живое состояние на соединение: cwnd (окно перегрузки в MSS), ssthresh, сглаженный RTT и дисперсию RTT (rtt/rttvar в мс), отправленные/подтверждённые байты, счётчики повторных передач (retrans:sent/outstanding), использование буферов отправки/приёма и память сокета. netstat показывает только кортеж соединения и состояние. ss -tin даёт данные для диагностики обвала перегрузки, bufferbloat или шторма повторных передач без установки дополнительных инструментов." },
    { q: "Какова связь между TCP и QUIC, и почему QUIC не просто расширил TCP?", a: "QUIC заново изобретает механизмы надёжности, упорядочения и управления перегрузкой TCP в пространстве пользователя, работая поверх UDP. Он был разработан именно так, потому что расширение TCP требует изменений в ядрах по всему миру — процесс, занимающий 5–15 лет из-за длинного хвоста необновлённых систем. UDP проходит через практически все middlebox-устройства без изменений, поэтому QUIC можно развернуть как обновление библиотеки в браузере или сервере. Цена — дублирование механизмов TCP; выгода — возможность итерировать в темпе приложений. HTTP/3 работает на QUIC; HTTP/1.1 и HTTP/2 — на TCP. Понимание порядковых номеров TCP, ACK, управления перегрузкой и конечного автомата делает QUIC механистически прозрачным." },
  ]}
/>

<Recap lang="ru">
BBR напрямую оценивает пропускную способность узкого места сети и минимальный RTT, игнорируя потери как сигнал перегрузки. CUBIC режет окно при каждом событии потери — на пути с 1% случайных потерь CUBIC оседает на доле от ёмкости, а BBR удерживает throughput близко к максимуму. BBRv3 развёрнут в Google, Cloudflare и Netflix, но ещё не влит в mainline Linux (начало 2025 г.). Производственный набор инструментов: ss -tin для живого cwnd, RTT и состояния повторных передач на соединение; nstat для счётчиков ядра; node_exporter для Prometheus SLO. RST закрывает соединения немедленно без TIME-WAIT, открывая возможность для инъекционных атак на долгоживущих сессиях. MPTCP распределяет одно соединение по нескольким сетевым путям. kTLS переносит шифрование TLS-записей в ядро для zero-copy доставки статики. QUIC запускает TCP-подобную надёжность в пространстве пользователя поверх UDP, разрывая связь между эволюцией протокола и циклами обновления ядра. Теперь, когда видите потолок throughput на пути с потерями и большим RTT, сначала запустите `ss -tin` — если cwnd крошечный и счётчик повторных передач растёт, вы наблюдаете обвал CUBIC в реальном времени; переключение на BBR — один sysctl.
</Recap>
