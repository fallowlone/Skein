---
concepts:
- tcp-header
- nagle
- delayed-ack
- tcp-nodelay
- ecn
- keepalive
- psh
- close-wait
- tcp-options-field
- paws
- timestamps
deepensInto:
- 05-syn-cookies-and-tfo
estMin: 15
lang: ru
lessonType: topic
level: middle
order: 4
prereqs:
- 03-flow-and-congestion-control
slug: 04-tcp-options-and-pathologies
sources:
- https://datatracker.ietf.org/doc/html/rfc9293
- https://datatracker.ietf.org/doc/html/rfc7323
- https://man7.org/linux/man-pages/man7/tcp.7.html
spiral: []
status: ready
summary: Анатомия заголовка TCP, зависание Nagle+delayed-ACK, ECN, keepalive и ловушка
  утечки сокетов CLOSE-WAIT, с которой рано или поздно сталкивается каждый бэкенд-разработчик.
title: Опции TCP и типичные патологии
track: networking
unit: 03-tcp-handshake
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import TraceScenario from "~/components/pedagogy/TraceScenario.astro";
import NumbersCard from "~/components/layout/NumbersCard.astro";
import DebugLog from "~/components/pedagogy/DebugLog.astro";
import SequenceDiagram from "~/components/diagram/SequenceDiagram.astro";
import Infographic from "~/components/diagram/Infographic.astro";
import nodelayPayoff from "./infographics/nodelay-payoff.svg?raw";

<Hook lang="ru">
Вызов Redis занимает 200 мс — хотя Redis находится на том же хосте в датацентре. Нагрузки нет, таймаутов нет, ошибок нет — только 200 мс задержки на каждом маленьком запросе. Причина — взаимодействие двух дефолтных настроек TCP в худшем из возможных сочетаний, а исправление занимает одну строку.
</Hook>

<Crux>Nagle+delayed-ACK стопорит малые RPC на 40–200 мс; TCP_NODELAY — исправление. CLOSE-WAIT — утечка сокетов. ECN и keepalive — остальное.</Crux>

<Explanation>

## Анатомия заголовка TCP

Прежде чем рассуждать о том, почему соединение ведёт себя именно так — почему зависает, почему медленно работает, почему появляются RST — нужно понять, что реально находится в сегменте, летящем по проводу. Каждый TCP-сегмент содержит обязательный заголовок в 20 байт плюс опциональные поля (ещё до 40 байт):

| Поле | Размер | Назначение |
|------|--------|-----------|
| Порт источника | 16 бит | Часть четырёхкортежа, идентифицирующего соединение |
| Порт назначения | 16 бит | Часть четырёхкортежа |
| Порядковый номер | 32 бита | Позиция первого байта данных в потоке |
| Номер подтверждения | 32 бита | Следующий ожидаемый байт от соседа |
| Смещение данных | 4 бита | Длина заголовка в 32-битных словах (меняется с опциями) |
| Флаги | 8 бит | URG, ACK, PSH, RST, SYN, FIN, CWR, ECE |
| Окно | 16 бит | Байты, которые получатель готов принять |
| Контрольная сумма | 16 бит | Покрывает псевдозаголовок IP и TCP-сегмент |
| Указатель срочности | 16 бит | Почти никогда не используется; наследие старых терминальных протоколов |

Поле опций (до 40 байт) содержит MSS, масштаб окна, SACK, метки времени и cookie TFO. Опции согласовываются только в SYN/SYN-ACK — после установки соединения набор опций фиксируется.

## Поле опций TCP

Помимо MSS, масштаба окна и SACK:

**Метки времени (RFC 7323):** используются как для измерения RTT, так и для PAWS (защита от закольцованных порядковых номеров). PAWS предотвращает ситуацию, когда старые задержанные пакеты принимаются за новые данные при долго живущих высокоскоростных соединениях, где порядковые номера оборачиваются.

**Cookie TFO** также живёт в поле опций. Подробности — в уроке про SYN cookies и TFO.

<NumbersCard
  id="card-header"
  title="Заголовок TCP кратко"
  data-lesson-visual
  stats={[
    { label: "Обязательный заголовок", value: "20 байт" },
    { label: "Максимум опций", value: "40 байт (всего не более 60 байт)" },
    { label: "Байт флагов", value: "URG ACK PSH RST SYN FIN CWR ECE" },
    { label: "Поле окна (сырое)", value: "16 бит, максимум 65535 байт" },
    { label: "С масштабом окна", value: "до 1 ГиБ" },
    { label: "Область контрольной суммы", value: "IP-псевдозаголовок + TCP-сегмент" },
  ]}
/>

## Алгоритм Нейгла и delayed ACK — ловушка на 200 мс

По умолчанию TCP-отправитель группирует маленькие записи с помощью **алгоритма Нейгла**: не отправлять неполный сегмент, если в полёте есть неподтверждённые данные. Независимо от этого получатель **задерживает ACK** до 40 мс (дефолт Linux), надеясь прицепить ACK к исходящим данным приложения.

В сочетании эти два механизма создают опасное взаимодействие на паттернах «маленький запрос — маленький ответ»:

1. Клиент отправляет маленький запрос. Запрос меньше MSS.
2. TCP-стек сервера держит ACK до 40 мс, надеясь прицепить его к исходящему ответу.
3. Клиент не может отправить следующую маленькую запись, потому что Нейгл держит её (ждёт ACK первой записи).
4. Приложение сервера в конце концов отвечает, и delayed ACK срабатывает вместе с ответом.

Результат: **задержка 40–200 мс** на каждом маленьком интерактивном обмене.

Исправление: `TCP_NODELAY=1` на сокете отключает Нейгла. gRPC, клиенты Redis, реализации HTTP/2 и любой RPC-слой, чувствительный к задержкам, устанавливают его по умолчанию. `TCP_QUICKACK=1` говорит Linux немедленно подтвердить следующий сегмент (автосбрасывается после одного пакета, поэтому вызывайте его после каждого `read()` в плотном цикле).

<Infographic
  svg={nodelayPayoff}
  label="Столбчатая диаграмма сравнивает задержку p99 малых RPC: дефолт Nagle плюс delayed-ACK на 200 мс против TCP_NODELAY=1 менее 1 мс"
  caption="Одна опция сокета схлопывает deadlock на 40–200 мс до менее 1 мс — выигрыш по задержке >200x на малых интерактивных RPC."
  hue="var(--d-infra)"
/>

<TraceScenario
  id="mid-trace-1"
  pieceSlug="04-tcp-options-and-pathologies"
  lang="ru"
  scenario="Проследите задержку 200 мс от Nagle + delayed-ACK у клиента Redis без TCP_NODELAY."
  steps={[
    { prompt: "Клиент отправляет 10-байтовую команду PING в Redis. Что делает Нейгл?", reveal: "Нейгл проверяет: есть ли неподтверждённые данные в полёте? Если да — буферизует PING и ждёт. Если нет — отправляет немедленно. Для первой команды соединения нет предыдущих данных, поэтому команда уходит сразу." },
    { prompt: "Redis получает PING. Что ядро делает с ACK?", reveal: "Ядро запускает таймер delayed-ACK на 40 мс, ожидая прицепить ACK к следующему исходящему сегменту данных." },
    { prompt: "Redis обрабатывает команду за 0,1 мс и вызывает write('+PONG\\r\\n'). ACK уходит сейчас?", reveal: "Да — delayed ACK срабатывает, когда уходит ответ, поэтому ACK + PONG путешествуют в одном сегменте. Клиент получает оба одновременно." },
    { prompt: "Теперь клиент отправляет PIPELINE из двух команд быстро подряд (write(CMD1), затем write(CMD2)). Что происходит с Нейглом?", reveal: "CMD1 уходит немедленно (ничего в полёте нет). До прихода ACK для CMD1 Нейгл держит CMD2 в буфере. Redis задерживает ACK для CMD1. Каждая сторона ждёт другую. Задержка — до 40 мс. Это классический deadlock Нейгла и delayed-ACK." },
    { prompt: "Исправление: устанавливаем TCP_NODELAY=1. Что меняется?", reveal: "TCP_NODELAY отключает Нейгла. CMD2 уходит немедленно после CMD1, не дожидаясь ACK. Задержка 40 мс исчезает. Задержка p99 падает с 200 мс до менее 1 мс." },
  ]}
/>

## Флаг PSH

Флаг PSH (push) говорит TCP-стеку получателя немедленно передать буферизованные данные приложению, не дожидаясь новых. Современные стеки передают данные приложению сразу по прибытии, поэтому PSH — это скорее подсказка, чем требование. Он устанавливается на последнем сегменте каждого `write()`, завершающего логическое сообщение, — полезно, когда TCP-стек ядра объединяет несколько записей приложения в один сегмент, гарантируя доставку записи получателю.

## Явное уведомление о перегрузке (ECN)

Вместо сброса пакетов для сигнализации перегрузки **ECN-совместимые маршрутизаторы** помечают их двухбитовым кодом (CE = Congestion Experienced). В заголовке TCP зарезервированы флаги CWR (Congestion Window Reduced) и ECE (ECN-Echo). Согласование ECN происходит при обмене SYN/SYN-ACK: обе стороны объявляют поддержку через флаги ECE+CWR. При получении пакетов с меткой CE получатель устанавливает ECE для уведомления отправителя; отправитель уменьшает cwnd и устанавливает CWR в подтверждение.

ECN включён по умолчанию в Linux и macOS для соединений с надёжными адресатами. Некоторые старые промежуточные устройства сбрасывают пакеты с метками ECN — развёртывания используют обнаружение отказа и отключают ECN для таких адресатов.

## Keepalive

По умолчанию TCP-соединение не отправляет пакетов в режиме простоя, поэтому соединение через NAT или файрвол может быть молча закрыто через 5–60 минут. `SO_KEEPALIVE` отправляет зондирующий пакет каждые `tcp_keepalive_time` секунд (дефолт Linux: 7200 с = 2 часа — слишком долго для использования в service mesh). Для долгоживущих RPC-соединений настройте:

- `tcp_keepalive_time`: 60–120 с
- `tcp_keepalive_intvl`: 10–30 с
- `tcp_keepalive_probes`: 3–5

Это позволяет обнаруживать мёртвых соседей за минуту, а не за два часа.

<DebugLog
  id="mid-debug-1"
  pieceSlug="04-tcp-options-and-pathologies"
  lang="ru"
  title="Вывод ss — диагностика накопления CLOSE-WAIT"
  output={`$ ss -tan state established | wc -l
12384
$ ss -tan state close-wait | wc -l
9821
$ ss -tan state time-wait | wc -l
1247
$ ss -s
Total: 12500
TCP:   23552 (estab 12384, closed 8920, orphaned 2, timewait 1247)
$ ps -p 1234 -o pid,stat,rss,vsz,cmd
  PID STAT  RSS    VSZ CMD
 1234 Ssl 8392000 12000000 /usr/bin/app-server`}
  outputLang="log"
  question="У процесса сервиса 12к ESTABLISHED + 9,8к CLOSE-WAIT сокетов, и RSS растёт. В чём баг и как его исправить?"
  hint="Посмотрите на соотношение CLOSE-WAIT и ESTABLISHED. CLOSE-WAIT означает, что сосед отправил FIN, но локальное приложение не вызвало close(). Что это говорит о жизненном цикле сокетов в приложении?"
  answer="9,8к CLOSE-WAIT означает 9800 сокетов, где удалённый сосед завершил отправку и прислал FIN, но локальное приложение так и не вызвало close(). Ядро не может освободить сокет до вызова close(). Это утечка сокетов: приложение либо отбрасывает файловый дескриптор без закрытия (Go: горутина возвращается без defer conn.Close(); Java: отсутствует try-with-resources; C: забытый close() в ветке ошибок). Каждый утечённый CLOSE-WAIT удерживает память ядра и файловый дескриптор. Рост RSS подтверждает утечку. Исправление: проверьте путь закрытия соединения в приложении, добавьте defer/try-with-resources и следите за тем, как счётчик CLOSE-WAIT вернётся к нулю."
/>

<Quiz
  id="mid-quiz-1"
  pieceSlug="04-tcp-options-and-pathologies"
  lang="ru"
  question="Почему сочетание алгоритма Нейгла и delayed ACK вызывает задержку ~200 мс на маленьком RPC-трафике?"
  choices={[
    { label: "Нейгл сбрасывает маленькие пакеты; delayed ACK замедляет повторную передачу", misconception: "Нейгл не сбрасывает пакеты — он их буферизует. Delayed ACK задерживает подтверждение." },
    { label: "Нейгл удерживает маленькую запись отправителя в ожидании ACK; delayed ACK на получателе задерживает ACK до 40 мс — обе стороны ждут друг друга", correct: true },
    { label: "Оба дефолта неправильно настроены и никогда не должны использоваться", misconception: "У них есть веские причины существовать (Нейгл — для пропускной способности, delayed ACK — для объединения ACK), но они плохо взаимодействуют на интерактивных RPC-паттернах." },
    { label: "Зависание возникает только в сетях с потерями пакетов", misconception: "Зависание происходит на идеальных сетях — это взаимодействие дефолтных поведений, а не проблема потерь." },
  ]}
/>

<Inset kind="why" lang="ru">
**Зачем нужен PAWS.** На очень высокоскоростном долгоживущем соединении 32-битное пространство порядковых номеров TCP (~4 ГиБ) может обернуться за несколько часов. Без PAWS задержанный повторно переданный сегмент из предыдущего прохода через пространство номеров мог бы прийти и выглядеть как новый сегмент. Метки времени RFC 7323 включают PAWS: каждый сегмент несёт метку времени, и получатель отбрасывает сегменты с метками старше недавно виденного значения для этого соединения. Это делает атаки на переполнение и случайную коррупцию при оборачивании практически невозможными.
</Inset>

</Explanation>

<SequenceDiagram
  label="Deadlock Nagle и delayed-ACK: Клиент отправляет CMD1, затем Нейгл держит CMD2 в ожидании ACK, а таймер delayed-ACK на Сервере держит этот ACK до 40 мс"
  hue="var(--d-network)"
  actors={["Клиент", "Сервер"]}
  messages={[
    { from: "Клиент", to: "Сервер", label: "write(CMD1) — отправлен, в полёте пусто" },
    { from: "Сервер", to: "Сервер", label: "таймер delayed-ACK: держит ACK ≤40 мс" },
    { from: "Клиент", to: "Клиент", label: "Нейгл: держит CMD2 до ACK на CMD1" },
    { from: "Сервер", to: "Клиент", label: "ACK после таймаута (≤40 мс)" },
    { from: "Клиент", to: "Сервер", label: "CMD2 наконец отправлен" },
  ]}
  caption="Каждая сторона ждёт другую: Нейгл не отправит следующую маленькую запись, пока CMD1 не подтверждён, а delayed-ACK удерживает этот ACK, надеясь прицепить его. Зависание снимается только при срабатывании таймера 40 мс. TCP_NODELAY отключает Нейгла и убирает deadlock."
/>

<KeyTakeaway>Устанавливайте TCP_NODELAY на каждый чувствительный к задержкам сокет — Nagle+delayed-ACK создаёт deadlock на 40–200 мс при маленьких записях. Накопление CLOSE-WAIT означает утечку сокетов (приложение не вызывает close()). ECN сокращает повторные передачи; настройте SO_KEEPALIVE для обнаружения мёртвых соседей за минуты, а не часы.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="04-tcp-options-and-pathologies-retrieval"
  lang="ru"
  questions={[
    { q: "Объясните зависание Nagle + delayed-ACK: какая сторона вызывает какую задержку и каково стандартное исправление?", a: "Алгоритм Нейгла (сторона отправителя) буферизует маленькие записи и ждёт ACK перед отправкой неполного сегмента, когда данные уже в полёте. Механизм delayed-ACK (сторона получателя) задерживает ACK до 40 мс, надеясь прицепить его к исходящим данным. На интерактивном RPC-паттерне Нейгл ждёт ACK, который получатель задерживает — обе стороны ждут друг друга. Стандартное исправление — TCP_NODELAY=1 на сокете, что отключает Нейгла и позволяет маленьким записям уходить немедленно. gRPC, клиенты Redis и HTTP/2 устанавливают его по умолчанию." },
    { q: "От чего защищает PAWS и какая опция TCP его включает?", a: "PAWS (защита от закольцованных порядковых номеров) предотвращает ситуацию, когда задержанный повторно переданный сегмент из предыдущего прохода принимается за новый сегмент на долгоживущем высокоскоростном соединении. Включается опцией меток времени (RFC 7323): каждый сегмент несёт метку времени, получатель отбрасывает сегменты с метками старше недавнего значения. Это делает атаки на оборачивание пространства номеров нецелесообразными." },
    { q: "Что означает большое количество CLOSE-WAIT на сервере и как найти первопричину?", a: "CLOSE-WAIT означает, что удалённый сосед отправил FIN, но локальное приложение ещё не вызвало close() на сокете. Большое количество CLOSE-WAIT означает утечку файловых дескрипторов. Диагностика: найдите pid командой ss -tpan state close-wait, затем проверьте этот процесс на отсутствующие вызовы close(): defer conn.Close() в Go, try-with-resources в Java, явный close() в ветках ошибок на C. Каждый утечённый сокет потребляет файловый дескриптор и память ядра, поэтому RSS растёт вместе со счётчиком." },
  ]}
/>

<Recap lang="ru">
Заголовок TCP — это 20 обязательных байт плюс до 40 байт опций. Ключевые опции (MSS, масштаб окна, SACK, метки времени, cookie TFO) согласовываются в SYN/SYN-ACK и затем фиксируются. Алгоритм Нейгла и delayed ACK — оба дефолтных поведения, которые катастрофически взаимодействуют на трафике с маленькими записями: отправитель удерживает запись в ожидании ACK, который получатель задерживает на 40 мс. TCP_NODELAY отключает Нейгла и является стандартом для любого RPC-клиента. Накопление CLOSE-WAIT — каноничный симптом утечки сокетов: сосед отправил FIN, но приложение никогда не вызвало close(). ECN помечает перегруженные пакеты вместо их сброса; SO_KEEPALIVE зондирует простаивающие соединения для обнаружения мёртвых соседей до того, как состояние NAT или файрвола молча истечёт. Теперь, когда вызов Redis или gRPC занимает 40–200 мс без нагрузки и без ошибок, первое, что проверяете, — TCP_NODELAY: почти всегда он отключён именно на том сокете, где должен быть включён.
</Recap>
