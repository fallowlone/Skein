---
concepts:
- tcp
- syn
- syn-ack
- ack
- handshake
- reliable-delivery
- connection
deepensInto:
- 02-sequence-numbers-and-state
estMin: 10
lang: ru
lessonType: topic
level: junior
order: 1
prereqs:
- 02-ip-packet/06-ip-security-and-operations
slug: 01-the-three-way-handshake
sources:
- https://datatracker.ietf.org/doc/html/rfc9293
- https://hpbn.co/building-blocks-of-tcp/
spiral:
- statefulness
status: ready
summary: TCP превращает двух незнакомцев в сети в надёжный двусторонний разговор —
  рукопожатие — трёхшаговый ритуал начала этого разговора.
title: Трёхстороннее рукопожатие TCP
track: networking
unit: 03-tcp-handshake
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import MetaphorComplete from "~/components/pedagogy/MetaphorComplete.astro";
import NumbersCard from "~/components/layout/NumbersCard.astro";
import SequenceDiagram from "~/components/diagram/SequenceDiagram.astro";

<Hook lang="ru">
Вы открыли эту страницу, и незнакомец на другом континенте согласился с вами поговорить. До любого HTTP-запроса, до TLS, до DNS — TCP сделал три вещи: поздоровался, согласовал номера последовательности и подтвердил соглашение. Это рукопожатие.
</Hook>

<Crux>TCP превращает двух незнакомцев в сети в надёжный двусторонний разговор — рукопожатие — трёхшаговый ритуал начала этого разговора.</Crux>

<Explanation>

## Что делает TCP одним предложением

TCP превращает двух незнакомцев в сети в надёжный двусторонний разговор: каждый байт приходит по порядку, каждый байт подтверждается, отсутствующие байты пересылаются. Рукопожатие — это начало разговора.

Уровень IP переносил пакеты через сети, но ему было всё равно, приходят ли они, приходят ли по порядку или приходят вообще. TCP добавляет контракт: каждый байт пронумерован, каждый байт подтверждается, а если один отсутствует — он пересылается заново. Этот контракт согласовывается при рукопожатии. До него обе стороны — незнакомцы. После — у обеих есть состояние: буферы, окна и соглашение разговаривать до тех пор, пока кто-то из них не скажет «до свидания».

## Зачем это нужно

Без TCP каждый веб-запрос должен был бы управлять своими повторными передачами и упорядочиванием. TCP даёт каждому протоколу выше (HTTP, IMAP, SSH) чистый надёжный канал бесплатно. Когда вы видите необъяснимую задержку на первом запросе — часто виновато именно рукопожатие: понимая его структуру, вы сразу знаете, куда смотреть.

## Метафора: телефонный звонок

Представьте двух людей на телефонном звонке. Перед тем как сказать что-то важное, они обмениваются: «Алло, слышишь меня? — Да, слышу, а ты меня? — Да.» Этот трёхшаговый ритуал и есть TCP-рукопожатие.

- **SYN** — «Привет, вот мой счётчик»
- **SYN-ACK** — «Слышу тебя, вот мой счётчик, и подтверждаю твой»
- **ACK** — «Подтверждаю твой счётчик, начинаем»

Теперь обе стороны уверены, что говорят по одной линии, и могут начать настоящий разговор.

## Рукопожатие в диалоге

Антон набирает Светлану и говорит: «Привет, мой счётчик начинается с 42 000 000.» Светлана отвечает: «Понял, твой следующий байт — 42 000 001; мой счётчик — 99 000 000.» Антон: «Понял, твой следующий байт — 99 000 001, поехали.» С этого момента каждое слово обоих пронумеровано — никто не потеряет след.

<NumbersCard
  id="card-tcp-intro"
  title="Трёхстороннее рукопожатие: кратко"
  data-lesson-visual
  stats={[
    { label: "Шаг 1 (клиент→сервер)", value: "SYN seq=X" },
    { label: "Шаг 2 (сервер→клиент)", value: "SYN-ACK seq=Y ack=X+1" },
    { label: "Шаг 3 (клиент→сервер)", value: "ACK seq=X+1 ack=Y+1" },
    { label: "Состояние после шага 3", value: "ESTABLISHED у обеих сторон" },
    { label: "Стоимость", value: "1 полный round-trip (RTT)" },
    { label: "RTT в локальной сети", value: "0,1–1 мс" },
  ]}
/>

## Почему три шага, а не два

TCP — двунаправленный протокол, и **обе стороны должны согласовать начальные номера последовательности друг друга.** При двухшаговом рукопожатии (SYN → данные) номер последовательности сервера остался бы неподтверждённым. Третий шаг (ACK) доказывает, что клиент жив и прочитал SYN-ACK сервера. Он также ограничивает количество «полуоткрытых» соединений: если SYN пришёл, но ACK не последовал, сервер истекает по таймеру и освобождает память.

## Один сценарий от начала до конца

Вы кликаете ссылку в браузере. Под капотом браузер уже завершил IP-маршрутизацию (знает адрес сервера) и DNS (разрешил имя). Теперь он выполняет TCP-рукопожатие. После — слой TLS для шифрования, и только затем летит реальный HTTP-запрос.

<Quiz
  id="jr-quiz-1"
  pieceSlug="01-the-three-way-handshake"
  lang="ru"
  question="Почему TCP нужно три сообщения, а не два?"
  choices={[
    { label: "Три — счастливое число в сетях", misconception: "Есть реальная инженерная причина: обе стороны должны независимо подтвердить счётчик друг друга." },
    { label: "Обе стороны должны подтвердить начальный счётчик друг друга — двух сообщений недостаточно, чтобы оба направления были подтверждены", correct: true },
    { label: "Двух сообщений недостаточно для передачи данных", misconception: "Рукопожатие не несёт данных приложения. Причина — симметрия подтверждений." },
    { label: "Старое железо требовало трёх", misconception: "Это выбор дизайна протокола, а не аппаратное ограничение." },
  ]}
/>

<Quiz
  id="jr-quiz-2"
  pieceSlug="01-the-three-way-handshake"
  lang="ru"
  question="Какую гарантию даёт TCP после завершения рукопожатия?"
  choices={[
    { label: "Байты приходят мгновенно, без задержки", misconception: "TCP не обещает нулевую задержку. Он обещает упорядоченную надёжную доставку." },
    { label: "Каждый байт либо доставлен по порядку, либо соединение разрывается — никогда не теряется тихо и не переупорядочивается", correct: true },
    { label: "Шифрование всего, что вы отправляете", misconception: "TCP сам по себе не шифрует. Шифрование добавляет TLS поверх TCP." },
    { label: "Сервер — именно тот, который вы хотели", misconception: "Проверка идентичности — задача TLS. TCP гарантирует только доставку байтов." },
  ]}
/>

<DragOrder
  id="jr-drag-1"
  pieceSlug="01-the-three-way-handshake"
  lang="ru"
  prompt="Упорядочьте три шага TCP-рукопожатия:"
  items={[
    "Клиент отправляет SYN со своим начальным номером последовательности",
    "Сервер отвечает SYN-ACK: свой номер последовательности плюс подтверждение клиентского",
    "Клиент отправляет ACK, подтверждая номер последовательности сервера",
    "Обе стороны переходят в ESTABLISHED и могут обмениваться данными",
  ]}
/>

<MetaphorComplete
  id="jr-metaphor-1"
  pieceSlug="01-the-three-way-handshake"
  lang="ru"
  setup="Заполните пропуск: TCP-рукопожатие похоже на _______, где обе стороны убеждаются, что слышат друг друга, прежде чем сказать что-то важное."
  accepted={["телефонный звонок", "телефон", "рукопожатие", "приветствие", "знакомство", "звонок"]}
  canonical="телефонный звонок (или любое очное приветствие)"
  explanation="Аналогия отражает суть: перед разговором нужно убедиться, что оба слышат друг друга — точь-в-точь как в TCP."
/>

<Inset kind="why" lang="ru">
**Почему TCP добавляет состояние.** IP не хранит состояние: каждый пакет маршрутизируется независимо, и сеть не помнит предыдущих пакетов. TCP добавляет состояние на обоих концах — буферы, размеры окон, счётчики последовательностей. Именно это состояние обеспечивает надёжность. Рукопожатие — момент, когда это состояние создаётся и согласовывается. После него обе стороны больше не незнакомцы.
</Inset>

</Explanation>

<SequenceDiagram
  label="Трёхстороннее рукопожатие TCP: клиент SYN, сервер SYN-ACK, клиент ACK, затем ESTABLISHED"
  hue="var(--d-network)"
  actors={["Клиент", "Сервер"]}
  messages={[
    { from: "Клиент", to: "Сервер", label: "SYN seq=X" },
    { from: "Сервер", to: "Клиент", label: "SYN-ACK seq=Y ack=X+1" },
    { from: "Клиент", to: "Сервер", label: "ACK seq=X+1 ack=Y+1" },
  ]}
  caption="Обе стороны обмениваются начальными номерами последовательности и подтверждают их до передачи данных — один полный round-trip, затем ESTABLISHED с обеих сторон."
/>

<KeyTakeaway>Трёхстороннее рукопожатие TCP создаёт надёжное двунаправленное состояние: обе стороны обмениваются и подтверждают начальные номера последовательности (SYN, SYN-ACK, ACK) до передачи данных, ценой одного полного RTT.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="01-the-three-way-handshake-retrieval"
  lang="ru"
  questions={[
    { q: "Одним предложением: почему каждое новое TCP-соединение занимает минимум один round-trip до передачи данных?", a: "Потому что клиент и сервер должны каждый отправить свой начальный номер последовательности и получить его подтверждение прежде, чем доверять другой стороне — этот обмен (SYN, SYN-ACK, ACK) составляет один полный round-trip." },
    { q: "Что пошло бы не так при двухшаговом рукопожатии (SYN → данные) вместо трёх шагов?", a: "Сервер выделял бы состояние при получении SYN, но клиент начинал бы отправлять данные, не подтвердив номер последовательности сервера. Если SYN — это повтор из старого, упавшего соединения, сервер выделил бы состояние зря, и данные могли бы попасть в неверный поток. Третий шаг (ACK) доказывает, что клиент живёт и видел SYN-ACK сервера." },
    { q: "В чём разница между TCP и IP с точки зрения гарантий доставки?", a: "IP — «лучшие усилия»: маршрутизирует каждый пакет независимо, тихо отбрасывает пакеты при переполнении буферов, не даёт гарантий порядка или повторной передачи. TCP добавляет контракт поверх этого: каждый байт нумеруется, подтверждается, а недостающие передаются повторно. Приложение получает либо все байты по порядку, либо явную ошибку — никогда тихой потери или переупорядочивания." },
  ]}
/>

<Recap lang="ru">
TCP строит надёжный упорядоченный поток байт поверх ненадёжной пакетной доставки IP. Трёхстороннее рукопожатие — это начало соединения: клиент отправляет SYN со своим начальным номером последовательности, сервер отвечает SYN-ACK со своим номером, подтверждая клиентский, затем клиент отправляет ACK, подтверждая серверный. Все три шага необходимы, поскольку TCP двунаправленный — оба направления должны независимо подтвердить начальный счётчик друг друга. Стоимость — один полный round-trip: 80–300 мс на межконтинентальных линках. Теперь, когда вы видите необъяснимую задержку на первом запросе, первым делом измерьте RTT — само рукопожатие может съедать весь бюджет задержки ещё до того, как прошёл хоть один байт данных приложения.
</Recap>
