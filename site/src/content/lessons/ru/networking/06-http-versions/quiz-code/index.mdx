---
concepts:
- alt-svc
- early-hints
- vary-header
- grpc-trailers
- extensible-priorities
- cache-control
estMin: 14
lang: ru
level: senior
order: 9
prereqs: []
slug: quiz-code
sources:
- https://datatracker.ietf.org/doc/html/rfc9110
- https://datatracker.ietf.org/doc/html/rfc9218
- https://datatracker.ietf.org/doc/html/rfc9114
status: ready
summary: "Читай реальные HTTP-заголовки, curl-трейс и gRPC-обмен; предсказывай поведение и выбирай фикс с наибольшим рычагом, который senior-инженер сделает первым."
title: 'Версии HTTP: чтение заголовков и трейсов'
track: networking
unit: 06-http-versions
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Дампы трафика и заголовки ответов — это место, где HTTP-проблемы реально диагностируются. Прочитай обмен, предскажи, что сделает браузер, CDN или QUIC-стек, затем выбери фикс, к которому senior-инженер тянется первым.
</Hook>

<Goal lang="ru">
Отработай цикл, который ты запускаешь в каждом HTTP-инциденте: прочитай заголовки и трейс, предскажи поведение на уровне протокола и потянись за структурным фиксом раньше, чем что-либо тюнить.
</Goal>

## Сниппет 1 — реклама Alt-Svc

```
$ curl -v https://api.example.com/health
* ALPN: server accepted h2
< HTTP/2 200 OK
< alt-svc: h3=":443"; ma=86400
< x-quic-version: draft-29
< content-type: application/json
{"status":"ok"}

$ curl --http3-only https://api.example.com/health
* QUIC handshake fail: protocol violation: invalid initial packet
```

<Quiz
  id="06-http-versions-code-1"
  lessonSlug="06-http-versions"
  lang="ru"
  question="Сервер рекламирует h3 через Alt-Svc, но хендшейк HTTP/3 фейлится. В чём дело и какой первый фикс?"
  choices={[
    { label: "QUIC-стек на старом драфте (draft-29), а не финальной wire-версии RFC 9000. Современные клиенты говорят только на финальной версии и отклоняют draft-29 как protocol violation. Первый фикс: обновить QUIC-библиотеку; до этого убрать Alt-Svc h3, чтобы клиенты не жгли RTT, пробуя h3 и откатываясь.", correct: true },
    { label: "Сертификат не покрывает QUIC-порт, поэтому UDP фейлится.", misconception: "QUIC использует тот же TLS-сертификат, что и TCP-листенер на 443; отдельного QUIC-порта для покрытия нет. Заголовок x-quic-version: draft-29 показывает реальную причину — устаревший драфт QUIC." },
    { label: "ma=86400 слишком длинный, из-за чего хендшейк невалиден.", misconception: "max-age лишь управляет тем, как долго клиент помнит h3-подсказку; он не может вызвать protocol violation в хендшейке. Дефект — реализация на draft-29." },
    { label: "Всё в порядке — curl просто нужен флаг --http2.", misconception: "HTTP/2 уже сработал в первом вызове. Явное 'protocol violation: invalid initial packet' — это реальное несовпадение версий QUIC, а не вопрос флага curl." },
  ]}
/>

## Сниппет 2 — закэшированный профиль пользователя

```
GET /api/user/profile HTTP/2
authorization: Bearer eyJ...userA

HTTP/2 200 OK
content-type: application/json
cache-control: max-age=300
{"id":"userA","email":"a@example.com"}
```

<Quiz
  id="06-http-versions-code-2"
  lessonSlug="06-http-versions"
  lang="ru"
  question="Пользователи время от времени видят чужие профили. Читая только эти заголовки, какой баг и верный фикс?"
  choices={[
    { label: "Ответ пер-пользовательский, но помечен публично кэшируемым (max-age=300, нет private/no-store, нет Vary: Authorization). Общий кэш или CDN хранит тело userA и отдаёт его userB. Фикс: Cache-Control: private (или no-store для чувствительных данных); добавь Vary: Authorization, если общий кэш должен варьироваться по токену.", correct: true },
    { label: "Баг в том, что multiplexing HTTP/2 перекрещивает потоки между пользователями.", misconception: "Потоки HTTP/2 никогда не перекрещивают тела запросов между соединениями или пользователями. Утечка — это баг семантики кэширования: пер-пользовательский ответ помечен публично кэшируемым." },
    { label: "Bearer-токен в заголовке утекает данные; перенеси его в query-строку.", misconception: "Токены принадлежат заголовку Authorization именно потому, что URL утекают через Referer и логи доступа. Баг — отсутствие private/Vary на ответе, а не расположение токена." },
    { label: "max-age=300 слишком короткий и вызывает устаревшие чтения между пользователями.", misconception: "Короткий TTL не смешивает пользователей; любой TTL на публично кэшируемом пер-пользовательском ответе утекает. Фикс — сузить кэш (private/no-store), а не менять max-age." },
  ]}
/>

## Сниппет 3 — ответ gRPC

```
HTTP/2 200 OK
content-type: application/grpc

(DATA-фреймы: сериализованное protobuf-сообщение)

(хвостовой HEADERS-фрейм)
grpc-status: 5
grpc-message: user not found
```

<Quiz
  id="06-http-versions-code-3"
  lessonSlug="06-http-versions"
  lang="ru"
  question="HTTP-статус 200, но grpc-status 5 (NOT_FOUND). Почему результат в трейлере и что ломается, если браузер вызовет это напрямую?"
  choices={[
    { label: "gRPC всегда возвращает HTTP 200 и несёт реальный исход в хвостовом HEADERS-фрейме, потому что статус известен лишь после отправки всего потока сообщений — трейлеры приходят после тела. Браузер не может читать HTTP-трейлеры через fetch/XHR, поэтому нужен gRPC-Web, где прокси переписывает трейлеры в финальный body-фрейм.", correct: true },
    { label: "200 — это баг; gRPC должен маппить grpc-status 5 в HTTP 404.", misconception: "По дизайну gRPC держит HTTP-статус на 200 и сигнализирует исход RPC в трейлерах grpc-status; это корректно, а не баг. Расположение трейлера отражает тайминг конца потока." },
    { label: "Трейлеры используются, чтобы обойти CORS, поэтому браузеры их не читают.", misconception: "Трейлеры — про тайминг метаданных конца потока, а не про CORS. Браузеры просто не отдают HTTP-трейлеры в JS, отсюда gRPC-Web." },
    { label: "grpc-status должен нести body-фрейм; трейлеры только в HTTP/1.1.", misconception: "HTTP/2 поддерживает хвостовые HEADERS-фреймы, и gRPC на них опирается. Тело держит protobuf-сообщение; статус законно попадает в трейлер." },
  ]}
/>

## Сниппет 4 — подсказки приоритета

```
:method = GET
:path = /hero.jpg
priority = u=5

:method = GET
:path = /critical.css
priority = u=1
```

<Quiz
  id="06-http-versions-code-4"
  lessonSlug="06-http-versions"
  lang="ru"
  question="Оба запроса приходят одновременно по HTTP/3. Читая заголовки Priority из RFC 9218, какой сервер отправит первым и каково правило?"
  choices={[
    { label: "critical.css (u=1) планируется впереди hero.jpg (u=5): в RFC 9218 меньшее значение urgency означает большую срочность, поэтому сервер упорядочивает передачу DATA-фреймов в пользу render-blocking CSS перед декоративным изображением. Флаг i (incremental) дал бы большому телу уступать место более срочной работе.", correct: true },
    { label: "hero.jpg первым, потому что u=5 больше, а больше значит выше приоритет.", misconception: "RFC 9218 инвертирует эту интуицию: меньшее u срочнее. u=1 (CSS) обходит u=5 (изображение), поэтому CSS отправляется первым." },
    { label: "Порядок не определён — сервер стримит в порядке прихода независимо от приоритета.", misconception: "Это и был сбойный режим, который починил RFC 9218. С заголовками Priority, уважаемыми современными серверами и CDN, сервер переупорядочивает DATA-фреймы по срочности." },
    { label: "Они отправляются строго одновременно, раз HTTP/3 мультиплексирует поровну.", misconception: "Multiplexing допускает чередование, но приоритеты говорят серверу, как взвесить полосу DATA-фреймов — в пользу потока с меньшим значением urgency (более срочного)." },
  ]}
/>

<Recap lang="ru">
Каждый HTTP-инцидент читается в заголовках и трейсах: подсказка Alt-Svc h3 при QUIC-стеке на драфте жжёт RTT на каждого клиента; пер-пользовательский ответ без private/no-store и Vary утекает через общий кэш; реальный исход gRPC живёт в хвостовом HEADERS-фрейме (200 на уровне HTTP) и требует gRPC-Web для браузеров; а urgency в RFC 9218 инвертирован — меньшее u срочнее. Читай трафик, предсказывай поведение протокола, затем применяй структурный фикс.
</Recap>
