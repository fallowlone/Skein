---
concepts:
- multiplexing
- head-of-line-blocking
- qpack
- connection-migration
- alpn-fallback
- extensible-priorities
estMin: 14
lang: ru
level: senior
order: 8
prereqs: []
slug: quiz-short
sources:
- https://datatracker.ietf.org/doc/html/rfc9113
- https://datatracker.ietf.org/doc/html/rfc9000
- https://datatracker.ietf.org/doc/html/rfc9114
status: ready
summary: "Промпты на свободное припоминание по юниту версий HTTP. Сначала ответь своими словами, затем раскрой модельный ответ и сравни."
title: 'Версии HTTP: свободное припоминание'
track: networking
unit: 06-http-versions
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";

<Hook>
Припоминание сильнее перечитывания. На каждый промпт проговори или запиши полный ответ по памяти, прежде чем открыть модельный — именно усилие припоминания закрепляет механику протокола.
</Hook>

<Goal lang="ru">
Восстанови хребет юнита — зачем существует каждая версия HTTP, где живёт head-of-line blocking на каждом слое, как QUIC и QPACK сохраняют независимость потоков, connection migration и операционные сигналы — не подглядывая в уроки.
</Goal>

<RetrievalDrawer
  client:visible
  id="06-http-versions-short-retrieval"
  lang="ru"
  questions={[
    { q: "Зачем изобрели HTTP/2 и HTTP/3, если HTTP/1.1 уже работал?", a: "HTTP/1.1 несёт один запрос-ответ в полёте на одно TCP-соединение, поэтому, чтобы быстро загрузить современную страницу с десятками под-ресурсов, браузерам приходилось открывать 6–8 параллельных соединений на origin — каждое со своим TCP- и TLS-хендшейком и congestion control с нуля. Даже тогда медленный ответ блокировал дальнейшие запросы на своём соединении. HTTP/2 схлопывает это в одно соединение, мультиплексирующее много потоков, срезая хендшейки и убирая HOL blocking на прикладном слое. HTTP/3 идёт дальше: уходит с TCP на QUIC, поэтому потерянный пакет больше не стопорит каждый поток, и переживает смену IP — закрывая transport-layer HOL blocking, который HTTP/2 всё ещё наследовал." },
    { q: "Объясни два слоя head-of-line blocking и какая версия чинит каждый.", a: "HOL blocking прикладного слоя: в HTTP/1.1 каждое соединение обрабатывает один запрос-ответ за раз, поэтому медленный ответ держит всё, что стоит за ним в очереди на этом соединении. HTTP/2 чинит это, мультиплексируя независимые потоки на одном соединении. HOL blocking транспортного слоя: TCP доставляет единый упорядоченный байтовый поток, поэтому один потерянный сегмент заставляет TCP буферизовать все последующие байты до ретрансмиссии — стопоря каждый поток HTTP/2 на этом соединении. HTTP/2 не может это починить, потому что сидит на TCP. HTTP/3 чинит, работая на QUIC, который отслеживает потери по пакетам и восстанавливает по потокам, так что потерянный пакет стопорит лишь те потоки, чьи данные он нёс." },
    { q: "Почему HTTP/3 использует QPACK вместо HPACK и как QPACK сохраняет независимость потоков?", a: "Динамическая таблица HPACK упорядочена и общая для потоков: HEADERS-фрейм, ссылающийся на запись с индексом N, должен обрабатываться после фрейма, вставившего индекс N. На TCP это невидимо, потому что доставка по порядку. На QUIC потоки доставляют фреймы независимо, поэтому HEADERS-фрейм на одном потоке мог бы ссылаться на запись таблицы, чей фрейм-вставка, несомый другим потоком или пакетом, ещё не пришёл — воссоздавая HOL blocking на слое заголовков. QPACK развязывает это: вставки в динамическую таблицу идут на отдельный encoder control-поток (доставляемый по порядку), decoder-поток подтверждает обработанные записи, а обычные HEADERS-фреймы ссылаются только на подтверждённые записи. Поэтому заголовки одного потока никогда не блокируют другой, сохраняя изоляцию потоков QUIC на слое сжатия. QPACK также держит большую статическую таблицу (99 записей против 61 у HPACK)." },
    { q: "Что такое QUIC connection migration и почему это важно для мобильных?", a: "QUIC-соединение идентифицируется connection ID, выбранным при установке, а не 4-кортежем из source/destination IP и порта. Когда локальный IP устройства меняется — например переключение Wi-Fi на сотовую — клиент шлёт PATH_CHALLENGE с нового адреса, сервер валидирует через PATH_RESPONSE, и существующее соединение продолжается без переустановки. На HTTP/2 поверх TCP смена 4-кортежа разрушает соединение и форсирует новый TCP- плюс TLS-хендшейк (1–2 RTT), видимый как заминка. Для мобильных пользователей, часто пересекающих сети, migration — это реальный выигрыш по задержке и непрерывности." },
    { q: "Почему HTTP/2 Server Push мёртв и что его заменило?", a: "Server Push позволял серверу слать ресурсы, которые браузер не запрашивал, ставя на то, что они понадобятся. Он провалился в продакшне по трём причинам: сервер не видит, что браузер уже закэшировал, поэтому переотправляет свежие закэшированные ассеты и тратит трафик; push помогает только на холодных первых навигациях, не на тёплых загрузках; и middlebox и CDN обрабатывали его неконсистентно, вызывая дубли передач. Chrome удалил его в 2022, а RFC 9113 объявляет PUSH_PROMISE deprecated. Замена — 103 Early Hints плюс link rel=preload: сервер шлёт информационный ответ 103 со списком критичных под-ресурсов до 200, и браузер — который может проверить свой кэш — решает, тянуть ли каждый." },
    { q: "Как HTTP/3 обнаруживается и выбирается и какой production-сигнал говорит, что UDP блокируется?", a: "HTTP/1.1 и HTTP/2 выбираются через ALPN внутри TLS-хендшейка без лишнего round-trip. HTTP/3 так не согласовать, потому что QUIC едет по UDP, который firewall может блокировать. Обнаружение двухэтапное: первое соединение использует TCP-based /1.1 или /2, и сервер рекламирует h3 через заголовок Alt-Svc или DNS-запись HTTPS/SVCB. На следующем визите браузер пробует QUIC на UDP/443; если не вышло — тихо откатывается на HTTP/2 без видимой пользователю ошибки. Операционный сигнал — распределение ALPN и частота фоллбэка h3-to-h2 в разбивке по региону и ASN: всплеск фоллбэка для одного ASN означает, что путь начал дропать UDP/443 (firewall, DDoS-скраббер или смена маршрутизации)." },
  ]}
/>

<Recap lang="ru">
Если ты смог восстановить каждый ответ по памяти, ты держишь хребет юнита: HTTP/1.1 параллелит множеством соединений, HTTP/2 мультиплексирует потоки и убивает HOL blocking прикладного слоя, но наследует транспортный от TCP, а HTTP/3 на QUIC изолирует потери по потокам, использует QPACK ради независимости потоков на слое заголовков и мигрирует через смену IP. Server Push мёртв в пользу 103 Early Hints, а production-сигнал здоровья — распределение ALPN плюс h3-fallback по ASN.
</Recap>
