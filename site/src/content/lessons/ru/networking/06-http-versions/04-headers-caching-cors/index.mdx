---
concepts:
- http-methods
- idempotency
- status-codes
- cache-control
- etag
- conditional-request
- cors
- preflight
- cookies
- samesite
- httponly
- hsts
- csp
- content-negotiation
- range-requests
deepensInto:
- 07-http-design-and-security
estMin: 16
lang: ru
lessonType: topic
level: middle
order: 4
prereqs:
- 02-http2-multiplexing
slug: 04-headers-caching-cors
sources:
- https://datatracker.ietf.org/doc/html/rfc9110
- https://datatracker.ietf.org/doc/html/rfc9112
- https://developer.mozilla.org/en-US/docs/Web/HTTP/Headers
spiral: []
status: ready
summary: Слой заголовков HTTP — методы, статус-коды, Cache-Control, ETag, куки, CORS
  — где живут большинство операционных багов и происходят победы или поражения в безопасности.
title: HTTP-заголовки, кеширование и CORS
track: networking
unit: 06-http-versions
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import TraceScenario from "~/components/pedagogy/TraceScenario.astro";
import Misconception from "~/components/layout/Misconception.astro";
import SequenceDiagram from "~/components/diagram/SequenceDiagram.astro";
import Infographic from "~/components/diagram/Infographic.astro";
import samesiteSpectrum from "./infographics/samesite-spectrum.svg?raw";

<Hook>
HTTP-версия определяет *как* идут байты. Заголовки определяют *что с ними происходит* — кешируются ли они, кто может читать их cross-origin, как долго живут куки, и заставит ли браузер всегда использовать HTTPS. Ошибки в заголовках — самый частый источник production security-багов.
</Hook>

<Crux>Cache-Control, ETag, CORS и куки — где живут большинство операционных багов и где безопасность побеждает или проигрывает.</Crux>

<Explanation>

## Методы и идемпотентность

Прежде чем добавить авто-ретрай в HTTP-клиент, спроси себя: идемпотентен ли этот метод? От ответа зависит, спасёт ли ретрай пользователя или создаст дублирующиеся заказы. HTTP определяет глаголы (методы): `GET` (чтение), `POST` (создание или обобщённый неидемпотентный), `PUT` (идемпотентная замена), `DELETE` (идемпотентное удаление), `PATCH` (частичное обновление), `HEAD` (GET только метаданных), `OPTIONS` (проверка возможностей), `CONNECT` (туннель для прокси).

**Идемпотентность** (RFC 9110 § 9.2.2): метод идемпотентен если многократный вызов даёт тот же результат что однократный. `GET`, `HEAD`, `PUT`, `DELETE` и `OPTIONS` **безопасно** повторять автоматически при таймауте. `POST` и `PATCH` — **нет** — автоматический повтор может создать дублирующиеся заказы, платежи или записи.

Практическое следствие: HTTP-клиенты (браузеры, балансировщики, CDN) могут прозрачно повторять идемпотентные методы при сетевой ошибке. `POST /orders` никогда не должен повторяться без `Idempotency-Key`-заголовка или дедупликации на уровне приложения.

## Статус-коды

Статус-коды по RFC 9110:

- **1xx информационные:** `100 Continue`, `101 Switching Protocols` (апгрейд WebSocket), `103 Early Hints`.
- **2xx успех:** `200 OK`, `201 Created` (POST создал ресурс), `204 No Content` (успешный DELETE), `206 Partial Content` (range-запрос выполнен).
- **3xx редирект:** `301 Moved Permanently` (браузеры кешируют навсегда), `302/307 Temporary Redirect`, `304 Not Modified` (cache validation hit — пустое тело).
- **4xx ошибка клиента:** `400`, `401 Unauthorized` (нужна аутентификация), `403 Forbidden` (аутентифицирован но не разрешено), `404`, `409 Conflict`, `422`, `429 Too Many Requests`.
- **5xx ошибка сервера:** `500`, `502 Bad Gateway`, `503`, `504 Gateway Timeout`.

**Частая ошибка:** возвращать `200 OK` с `{ "error": "not found" }` в JSON-теле. CDN кешируют 200, retry-логика не срабатывает, консоль браузера не показывает ошибку. Используйте правильный статус-код.

## Кеширование: Cache-Control и ETag

Браузер, CDN-edge и reverse proxies кешируют ответы. `Cache-Control`:

- `max-age=3600` — кешировать 3600 секунд; ревалидация не нужна до устаревания.
- `no-cache` — хранить ответ, но **ревалидировать с origin перед отдачей** (отправляет `If-None-Match`). Для часто меняющихся данных.
- `no-store` — **не хранить вообще** — ни в кеше браузера, ни на CDN. Для высокочувствительных данных (банковские выписки, страницы аккаунта).
- `private` — только браузер может кешировать; CDN/прокси — нет. Для пользовательских данных.
- `public` — любой кеш может хранить. CDN нужно это (или отсутствие Cache-Control) для кеширования.
- `stale-while-revalidate=60` — сразу отдать устаревшую копию, ревалидировать в фоне.

**ETag** позволяет условные запросы: сервер отправляет `ETag: "v2"` с ответом. При следующем запросе браузер включает `If-None-Match: "v2"`. Если ETag совпадает — сервер отвечает `304 Not Modified` с пустым телом — экономя трафик.

<DragOrder
  id="mid-drag-cache"
  pieceSlug="04-headers-caching-cors"
  lang="ru"
  prompt="Расставьте cache-связанные события в типичном жизненном цикле:"
  items={[
    "Ответ несёт ETag и Cache-Control: max-age=3600",
    "Браузер кеширует ответ на 3600 секунд",
    "Через 3600с кеш устаревает",
    "Следующий запрос: браузер отправляет If-None-Match с сохранённым ETag",
    "Сервер отвечает 304 Not Modified (пустое тело) если ETag совпадает",
    "Или 200 OK с новым телом + новым ETag если контент изменился",
  ]}
/>

<Misconception
  id="r-no-cache-vs-no-store"
  title="no-cache и no-store — НЕ одно и то же"
>
`no-cache` разрешает хранение но требует ревалидации перед каждой отдачей. `no-store` запрещает хранение полностью. `no-store` — для чувствительных данных; `no-cache` — когда данные должны быть свежими, но кеш приемлем.
</Misconception>

## CORS — Cross-Origin Resource Sharing

Браузеры применяют **same-origin policy**: скрипт на `a.example.com` не может читать ответы с `b.example.com` по умолчанию. CORS открывает доступ:

- Сервер отвечает `Access-Control-Allow-Origin: https://a.example.com` разрешая доступ с этого origin.
- Wildcard `*` разрешает любой origin, но **не может сочетаться с credentials** — браузер отказывается отправлять куки или Authorization-заголовки к wildcard-origin.
- **Preflight-запросы** (OPTIONS): для несimple методов (PUT, DELETE, кастомные заголовки), браузер сначала отправляет OPTIONS проверяя что сервер разрешает cross-origin доступ.
- `Access-Control-Allow-Credentials: true` плюс явный не-wildcard origin позволяет cross-origin запросам нести куки и Auth-заголовки.

**Ошибка в конфигурации CORS:** `Access-Control-Allow-Origin: *` с `Access-Control-Allow-Credentials: true` — дыра в безопасности. Браузер предотвращает эту комбинацию, но некоторые прокси или фреймворки допускают её через баги, открывая атаки типа confused-deputy.

<Misconception
  id="r-cors-security"
  title="CORS применяется браузером, не сервером"
>
CORS-заголовки указывают браузеру разрешить или запретить JavaScript-доступ к ответам cross-origin. curl и серверный код игнорируют их. CORS — не контроль доступа к API, только UI-защита в браузере. Аутентификация требуется на каждом запросе отдельно.
</Misconception>

## Куки: флаги безопасности

Сервер устанавливает куки через:
```
Set-Cookie: session=abc123; HttpOnly; Secure; SameSite=Strict; Max-Age=3600; Path=/
```

- **`HttpOnly`** — блокирует JavaScript-доступ к куке (`document.cookie` не видит её). Основная XSS-защита для session токенов.
- **`Secure`** — отправлять только по HTTPS. Без этого session-кука может уйти по HTTP.
- **`SameSite=Strict`** — никогда не отправлять при cross-site запросах. **`SameSite=Lax`** — отправлять при безопасных cross-site навигациях (переход по ссылке) но не при POST cross-site. **`SameSite=None; Secure`** — отправлять при всех cross-site запросах (для легитимного третьесторонного контента).

Современные браузеры по умолчанию **SameSite=Lax** при отсутствии директивы — это намеренно ломает многие legacy third-party cookie flows.

<Infographic
  svg={samesiteSpectrum}
  label="Трёхступенчатый спектр атрибута cookie SameSite от Strict к Lax и None: каждый шаг отправляет cookie на большее число межсайтовых запросов в обмен на более слабую защиту от CSRF."
  caption="SameSite — это регулятор «CSRF против охвата»: Strict никогда не отправляет межсайтово, Lax (умолчание) разрешает безопасные переходы, None отправляет везде, но не даёт защиты от CSRF."
  hue="var(--d-infra)"
/>

## Заголовки безопасности

Когда разворачиваешь новый сервис, эти четыре заголовка должны быть в чеклисте первого дня — каждый закрывает класс атак, которые на уровне приложения не закрыть. Распространённые заголовки ответа, ужесточающие поведение браузера:

- **`Strict-Transport-Security` (HSTS):** браузеры всегда используют HTTPS для этого домена в течение `max-age` секунд.
- **`Content-Security-Policy` (CSP):** определяет какие источники могут загружать скрипты, стили, фреймы. Мощная XSS-защита.
- **`X-Content-Type-Options: nosniff`** — браузеры не угадывают MIME-тип из содержимого.
- **`Referrer-Policy: strict-origin-when-cross-origin`** — ограничивает утечку URL в Referer-заголовке.

## Контент-переговоры и range-запросы

**Контент-переговоры:** клиенты сообщают предпочтения через `Accept`-заголовки. `Accept: application/json, text/html;q=0.9`. Заголовок `Vary` сообщает кешам какие request-заголовки влияют на ответ — без `Vary: Accept-Encoding` кеш может отдать Brotli-сжатый ответ клиенту не поддерживающему Brotli.

**Range-запросы:** `Range: bytes=500-999` запрашивает диапазон байт; сервер отвечает `206 Partial Content`. Используется для HTTP-видеостриминга и resume-after-disconnect.

<Quiz
  id="mid-quiz-1"
  pieceSlug="04-headers-caching-cors"
  lang="ru"
  question="Какой HTTP-метод требуется быть идемпотентным согласно RFC 9110?"
  choices={[
    { label: "POST", misconception: "POST явно неидемпотентен; двойной вызов создаёт два ресурса." },
    { label: "PUT", correct: true },
    { label: "PATCH", misconception: "PATCH намеренно не обязан быть идемпотентным; зависит от семантики patch-документа." },
    { label: "CONNECT", misconception: "CONNECT открывает туннель; идемпотентность для него не определена." },
  ]}
/>

<Quiz
  id="mid-quiz-2"
  pieceSlug="04-headers-caching-cors"
  lang="ru"
  question="Что означает Cache-Control: no-store для браузера и CDN?"
  choices={[
    { label: "Кешировать 0 секунд — перезапрашивать каждый раз", misconception: "Это `no-cache` (ревалидация) или `max-age=0`. no-store строже." },
    { label: "Не хранить ответ нигде, включая дисковый кеш — подходит для высокочувствительных данных", correct: true },
    { label: "Хранить но никогда не отправлять по сети", misconception: "Это невозможно — кеш должен получить данные прежде чем хранить." },
    { label: "Кешировать только на сервере, не на клиенте", misconception: "Cache-Control применяется ко всем кешам между origin и клиентом." },
  ]}
/>

<TraceScenario
  id="mid-trace-cors"
  pieceSlug="04-headers-caching-cors"
  lang="ru"
  scenario="Трассировка cross-origin API-вызова из браузерного приложения."
  steps={[
    { prompt: "JavaScript на https://app.example.com вызывает fetch('https://api.other.com/data') с credentials. Что браузер делает первым?", reveal: "Проверяет запрос: credentials и кастомный заголовок — несimple-запрос. Браузер отправляет OPTIONS preflight на api.other.com/data перед реальным запросом." },
    { prompt: "OPTIONS preflight приходит на сервер. Какие заголовки должен вернуть сервер для продолжения?", reveal: "Access-Control-Allow-Origin: https://app.example.com (явный, не *), Access-Control-Allow-Credentials: true, Access-Control-Allow-Headers с кастомным заголовком, Access-Control-Allow-Methods: GET." },
    { prompt: "Preflight успешен. Браузер отправляет реальный GET. API возвращает данные с ACAO: https://app.example.com и ACAC: true. Может ли JavaScript прочитать ответ?", reveal: "Да. Браузер видит не-wildcard ACAO совпадающий с request origin, плюс ACAC: true совпадающий с credentials: include. JavaScript может прочитать тело ответа." },
    { prompt: "Разработчик меняет сервер на Access-Control-Allow-Origin: * для упрощения. Что ломается?", reveal: "Cross-origin запросы с credentials теперь отклоняются браузером — он отказывается отправлять credentials к wildcard-origin. fetch()-вызовы начинают падать с CORS-ошибками, хотя сервер отвечает." },
  ]}
/>

</Explanation>

<SequenceDiagram
  label="CORS preflight: обмен запросами"
  hue="var(--d-network)"
  actors={["Браузер", "Сервер"]}
  messages={[
    { from: "Браузер", to: "Сервер", label: "OPTIONS (preflight)" },
    { from: "Сервер", to: "Браузер", label: "200 + Access-Control-Allow-*" },
    { from: "Браузер", to: "Сервер", label: "PUT /resource (реальный запрос)" },
    { from: "Сервер", to: "Браузер", label: "200 + Access-Control-Allow-Origin" },
  ]}
  caption="Для несimple-метода браузер сначала отправляет OPTIONS preflight; реальный запрос идёт только после того, как сервер его разрешил."
/>

<KeyTakeaway>HTTP-заголовки — где живут большинство production-багов: идемпотентность определяет безопасность auto-retry; Cache-Control (no-cache против no-store) — что где кешируется; ETag даёт дешёвую ревалидацию; CORS применяется браузером, не сервером; HttpOnly + SameSite куки и HSTS — базовые security-дефолты.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="04-headers-caching-cors-retrieval"
  lang="ru"
  questions={[
    { q: "Когда ставить Cache-Control: no-store против no-cache?", a: "no-store запрещает любому кешу (браузер, прокси, CDN) хранить ответ — для высокочувствительных данных (банк, аккаунт-страницы) где любая сохранённая копия — утечка. no-cache разрешает хранение но требует ревалидации с origin (через ETag/If-None-Match) перед каждой отдачей — для данных которые должны быть свежими но кеширование допустимо. Без Cache-Control кеши используют эвристики на основе Last-Modified — обычно неправильные, поэтому ставьте явно." },
    { q: "Почему CORS не является защитным барьером для API?", a: "CORS-заголовки читают и применяют только браузеры. curl, Postman, серверный код и скрипты вне браузера полностью игнорируют CORS. Атакующий может напрямую вызывать API не через браузер. CORS предотвращает только доступ cross-origin JavaScript в браузерах к вашим ответам. Настоящий контроль доступа требует аутентификации и авторизации на каждом запросе." },
    { q: "Что делает SameSite=Lax и почему он стал дефолтом браузеров?", a: "SameSite=Lax отправляет куки при безопасных cross-site навигациях (переход по ссылке на страницу) но не при cross-site POST, PUT или других state-changing запросах. Это блокирует большинство CSRF-атак когда вредоносный сайт POST-форму на ваш сайт используя session-куку жертвы. Браузеры выбрали Lax дефолтом (вместо None) для защиты пользователей сайтов не устанавливающих SameSite — более строгий дефолт намеренно сломал некоторые legacy third-party cookie flows как privacy-мера." },
  ]}
/>

<Recap lang="ru">
HTTP-методы определяют намеренную операцию — идемпотентные методы (GET, HEAD, PUT, DELETE) можно автоматически повторять; неидемпотентные (POST, PATCH) — нельзя без idempotency-ключей. Статус-коды — семантические сигналы на которые полагается HTTP-инфраструктура (CDN, retry-логика, browser UI). Cache-Control (max-age, no-cache, no-store, private) контролирует где и как долго хранятся ответы; ETag позволяет дешёвую ревалидацию через 304 Not Modified. CORS применяется браузером, не сервером — не механизм контроля доступа. Куки требуют HttpOnly (блокирует XSS), Secure (только HTTPS) и SameSite=Lax или Strict (CSRF-защита). HSTS и CSP укрепляют security-позицию браузера на уровне заголовков. Теперь, когда смотришь код-ревью нового API-endpoint'а, знаешь, что проверить первым делом: область Cache-Control, список origin в CORS и флаги кук — до того как код пойдёт в прод.
</Recap>
