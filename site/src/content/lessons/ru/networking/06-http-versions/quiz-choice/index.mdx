---
concepts:
- multiplexing
- head-of-line-blocking
- quic-stream-isolation
- hpack-qpack
- server-push
- extensible-priorities
estMin: 13
lang: ru
level: senior
order: 7
prereqs: []
slug: quiz-choice
sources:
- https://datatracker.ietf.org/doc/html/rfc9113
- https://datatracker.ietf.org/doc/html/rfc9114
- https://blog.cloudflare.com/http3-the-past-present-and-future/
status: ready
summary: "Тест с выбором на синтез по юниту версий HTTP — multiplexing, два слоя head-of-line blocking, изоляция потоков QUIC, сжатие заголовков и выбор протокола в продакшне."
title: 'Версии HTTP: тест с выбором ответа'
track: networking
unit: 06-http-versions
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Шесть вопросов поперёк всего юнита. Каждый отражает выбор протокола или диагностику, которую ты делаешь в продакшне — не определение для заучивания, а компромисс, который надо взвесить под реальными сетевыми условиями.
</Hook>

<Goal lang="ru">
Убедись, что связываешь multiplexing, два слоя head-of-line blocking, изоляцию потоков в QUIC, упорядоченность сжатия заголовков и операционные сигналы, которые решают, какая версия HTTP реально выигрывает.
</Goal>

<Quiz
  id="06-http-versions-choice-1"
  lessonSlug="06-http-versions"
  lang="ru"
  question="API делает 50 параллельных под-запросов на страницу. На оптоволокне (потери менее 0.1%) HTTP/2 быстр; на сотовом канале с 1.5% потерь он заметно медленнее, чем HTTP/1.1 с 6 соединениями. Почему?"
  choices={[
    { label: "Все 50 потоков HTTP/2 делят одно TCP-соединение, поэтому один потерянный сегмент стопорит каждый поток до ретрансмиссии. 6 соединений HTTP/1.1 — независимые домены восстановления потерь, так что потеря останавливает лишь ~1/6 работы. Фикс — HTTP/3, а не откат на /1.1.", correct: true },
    { label: "Накладные фрейминга HTTP/2 доминируют на сотовом, а у HTTP/1.1 их нет.", misconception: "9-байтный заголовок фрейма HTTP/2 пренебрежимо мал. Регрессия — это transport-layer HOL blocking: один потерянный пакет стопорит все мультиплексированные потоки на единственном соединении." },
    { label: "HPACK-сжатие ломается под потерями пакетов, портя заголовки.", misconception: "HPACK не портится под потерями; TCP переотдаёт по порядку. Проблема в том, что общий байтовый поток TCP стопорит каждый поток, пока ретранслируется потерянный сегмент." },
    { label: "Сотовые сети троттлят именно HTTP/2.", misconception: "Операторы не троттлят по версии HTTP. Причина структурная: у одного TCP-соединения один домен восстановления потерь, поэтому потеря стопорит все потоки разом." },
  ]}
/>

<Quiz
  id="06-http-versions-choice-2"
  lessonSlug="06-http-versions"
  lang="ru"
  question="HTTP/2 мультиплексирует независимые потоки, но всё равно страдает от head-of-line blocking. На каком слое и почему HTTP/3 от него уходит?"
  choices={[
    { label: "HTTP/2 убирает HOL blocking на прикладном слое, но наследует его на транспортном: TCP доставляет один упорядоченный байтовый поток, поэтому потерянный сегмент буферизует все последующие байты для каждого потока. HTTP/3 работает на QUIC, который отслеживает потери по пакетам и восстанавливает по потокам.", correct: true },
    { label: "У HTTP/2 HOL blocking на прикладном слое, потому что его потоки не по-настоящему независимы.", misconception: "Потоки HTTP/2 независимы на HTTP-слое; чередование бесплатно. Оставшийся HOL blocking чисто транспортный, из упорядоченного байтового потока TCP." },
    { label: "У /2 и /3 идентичный HOL blocking; HTTP/3 просто добавляет больше потоков.", misconception: "Число потоков ни при чём. QUIC даёт каждому потоку своё восстановление потерь, поэтому потерянный пакет стопорит лишь те потоки, чьи данные он нёс — TCP так не умеет." },
    { label: "HTTP/3 уходит от HOL blocking, полностью отключая ретрансмиссию.", misconception: "QUIC всё равно надёжно переотдаёт потерянные данные потоков. Он изолирует потери по потокам, а не отказывается от надёжности." },
  ]}
/>

<Quiz
  id="06-http-versions-choice-3"
  lessonSlug="06-http-versions"
  lang="ru"
  question="Почему HTTP/3 заменил HPACK на QPACK и что было бы, сохрани он HPACK поверх QUIC?"
  choices={[
    { label: "Динамическая таблица HPACK упорядочена и общая, поэтому HEADERS-фрейм, ссылающийся на запись, должен обрабатываться после фрейма, добавившего её. На независимых потоках QUIC потерянный или пришедший не по порядку фрейм-обновление таблицы заблокировал бы декодирование на несвязанных потоках — воссоздавая HOL blocking на слое заголовков. QPACK выносит обновления таблицы на отдельный control-поток и ссылается только на подтверждённые записи.", correct: true },
    { label: "HPACK медленнее QPACK, поэтому HTTP/3 нужен был более быстрый компрессор.", misconception: "Степень и скорость сжатия сопоставимы. Изменение — про упорядоченность: общая упорядоченная таблица HPACK ломает независимость потоков QUIC; QPACK её сохраняет." },
    { label: "HPACK небезопасен (в духе CRIME), а QPACK закрывает утечку.", misconception: "И HPACK, и QPACK уже защищены от compression-oracle атак на заголовки. Причина QPACK — проблема упорядоченности и независимости потоков на QUIC." },
    { label: "QUIC не может нести статическую таблицу HPACK, поэтому потребовался новый формат.", misconception: "QPACK как раз сохраняет статическую таблицу (99 записей). Реальный драйвер — развязать обновления динамической таблицы, чтобы заголовки одного потока никогда не блокировали другой." },
  ]}
/>

<Quiz
  id="06-http-versions-choice-4"
  lessonSlug="06-http-versions"
  lang="ru"
  question="Команда планирует выкатить HTTP/2 Server Push для прелоада критичного CSS. Что должен сказать senior-ревьюер?"
  choices={[
    { label: "Не надо — Server Push deprecated и удалён из Chrome в 2022. Сервер не видит кэш браузера, поэтому переотправляет уже закэшированные ассеты и тратит трафик. Используй 103 Early Hints плюс link rel=preload, которые дают браузеру проверить кэш и решить самому.", correct: true },
    { label: "Хорошая идея — Server Push это стандартный способ убрать round-trip запроса для критичных ассетов.", misconception: "Push хорошо выглядел на бенчмарках с холодным кэшем, но проиграл в продакшне: слепая переотправка закэшированного, отсутствие пользы на тёплых загрузках и поломки на middlebox. Он deprecated." },
    { label: "Нормально, но только поверх HTTP/3, где push надёжнее.", misconception: "HTTP/3 не реабилитирует push; проблема видимости кэша идентична. Современная замена на любой версии — 103 Early Hints плюс preload." },
    { label: "Push нормально, если ещё сжать gzip-ом отправляемые байты.", misconception: "Сжатие ни при чём. Дефект операционный — сервер угадывает, что закэшировал клиент — а не кодировка отправляемых байтов." },
  ]}
/>

<Quiz
  id="06-http-versions-choice-5"
  lessonSlug="06-http-versions"
  lang="ru"
  question="Оператор включает HTTP/3 на edge. За ночь дашборд ALPN показывает обвал доли h3 с 21% до 1% без ошибок в приложении. Наиболее вероятная причина и верный первый шаг?"
  choices={[
    { label: "Сетевой путь начал дропать UDP/443 (правило firewall или DDoS-скраббера, смена маршрутизации ISP). Клиенты пробуют QUIC, фейлятся и тихо откатываются на HTTP/2 — невидимо для пользователей, но видно в распределении ALPN. Первый шаг: разбить частоту фоллбэка по региону и ASN, чтобы локализовать блокирующий hop.", correct: true },
    { label: "Истёк TLS-сертификат, поэтому хендшейки HTTP/3 фейлятся.", misconception: "Истёкший сертификат сломал бы и HTTP/2 поверх TCP и выдал бы громкие ошибки. Тихий обвал только h3 с поглощением трафика h2 указывает на блокировку UDP/443 на пути." },
    { label: "У QPACK кончилось место в динамической таблице, и он сам отключился.", misconception: "Исчерпание таблицы QPACK ухудшает сжатие, а не выбор протокола. Полный обвал ALPN к h2 — это проблема достижимости UDP, а не сжатия заголовков." },
    { label: "Браузеры случайно ротируют версии HTTP, так что это нормальный шум.", misconception: "Браузеры не ротируют версии случайно; они выбирают h3, когда Alt-Svc или SVCB его рекламирует и UDP работает. Падение в 20 раз — сетевое событие, а не шум." },
  ]}
/>

<Quiz
  id="06-http-versions-choice-6"
  lessonSlug="06-http-versions"
  lang="ru"
  question="Real-time multiplayer-бэкенд держит 100k преимущественно сотовых соединений. Обновления позиции бесполезны, если приходят поздно, но пропуск одного безвреден. Какой стек подходит лучше и почему?"
  choices={[
    { label: "HTTP/3 с WebTransport. Изоляция потоков QUIC не даёт потере у одного игрока стопорить остальных; connection migration переживает хэндовер Wi-Fi на сотовую без переустановки; а ненадёжные QUIC DATAGRAM-фреймы несут обновления позиции без ретрансмиссии, которая всё равно пришла бы поздно.", correct: true },
    { label: "HTTP/2 с WebSocket поверх RFC 8441 — мультиплексировать всё на одном соединении.", misconception: "RFC 8441 экономит установку соединения, но едет по TCP: один потерянный пакет на сотовом с потерями стопорит каждый поток game-state. К тому же нет ненадёжных датаграмм для быстрых обновлений позиции." },
    { label: "HTTP/1.1 с классическим WebSocket-апгрейдом — максимальная совместимость.", misconception: "Каждому WebSocket нужно своё TCP-соединение, платящее холодный хендшейк на каждый мобильный реконнект, без изоляции потерь и без ненадёжного канала — худший вариант для real-time с потерями." },
    { label: "Сырой UDP со своим протоколом ради полного контроля.", misconception: "Сырой UDP переизобретает congestion control, шифрование, обход NAT и мобильных firewall, anti-replay — всё, что QUIC уже даёт. Оправдано лишь в крайне нишевых случаях." },
  ]}
/>

<Recap lang="ru">
Сквозная линия юнита — одна эволюция: HTTP/1.1 параллелит множеством соединений, HTTP/2 мультиплексирует потоки на одном TCP-соединении (убирая HOL blocking на прикладном слое, но наследуя его на транспортном), а HTTP/3 переходит на QUIC ради per-stream восстановления потерь, сжатия заголовков QPACK и connection migration. Производственная линза постоянна — подбирай версию под сеть (HTTP/3 на потерях и мобильных, HTTP/2 на надёжном east-west), следи за ALPN и h3-fallback-by-ASN как за сигналом здоровья и предпочитай 103 Early Hints и приоритеты RFC 9218 мёртвому Server Push.
</Recap>
