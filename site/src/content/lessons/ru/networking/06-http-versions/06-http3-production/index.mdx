---
concepts:
- quic-congestion-control
- quic-stream-independence
- hpack-vs-qpack
- alpn-negotiation
- http3-fallback
- alt-svc
- https-svcb
- connection-coalescing
- 0rtt-replay
- quic-userspace
- qlog
- alpn-distribution
- server-push-removal
deepensInto:
- 07-http-design-and-security
estMin: 18
lang: ru
lessonType: topic
level: senior
order: 6
prereqs:
- 03-http3-and-quic
- 05-tls-handshake/05-0rtt-defenses-and-modern-tls
slug: 06-http3-production
sources:
- https://datatracker.ietf.org/doc/html/rfc9000
- https://datatracker.ietf.org/doc/html/rfc9001
- https://datatracker.ietf.org/doc/html/rfc9114
- https://datatracker.ietf.org/doc/html/rfc9204
- https://radar.cloudflare.com/adoption-and-usage
- https://blog.cloudflare.com/http3-the-past-present-and-future/
- https://w3techs.com/technologies/details/ce-http3
spiral:
- multiplexing
status: ready
summary: Запуск HTTP/3 в продакшне требует понимания управления перегрузкой QUIC в
  userspace, механики ALPN-переговоров о версии, тихого fallback и метрик, которые
  показывают, когда UDP блокируется.
title: 'HTTP/3 в продакшне: внутренности QUIC, fallback и наблюдаемость'
track: networking
unit: 06-http-versions
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import TraceScenario from "~/components/pedagogy/TraceScenario.astro";
import DebugLog from "~/components/pedagogy/DebugLog.astro";
import RFCQuiz from "~/components/pedagogy/RFCQuiz.astro";
import DesignPrompt from "~/components/pedagogy/DesignPrompt.astro";
import NumbersCard from "~/components/layout/NumbersCard.astro";
import Misconception from "~/components/layout/Misconception.astro";

<Hook>
Ваш edge-сервис включает HTTP/3. Однажды утром дашборд ALPN показывает: доля h3 упала с 40% до 5%. Ошибок в логах приложения нет. Пользователи не звонят. Что сломалось? Это инцидент HTTP/3 в продакшне — и путь диагностики совсем не похож на TCP-outage.
</Hook>

<Crux>HTTP/3 в продакшне: ALPN-распределение, скорость h3-fallback по ASN и QUIC userspace — ключевые сигналы здоровья сети.</Crux>

<Explanation>

## TCP HOL-блокировка: точный механизм

Антагонист HTTP/2 — контракт доставки TCP. TCP гарантирует **доставку в порядке байтового потока**. Когда сегмент с байтами 50 000–50 099 потерян, TCP буферизует все последующие байты до повторной передачи потерянного сегмента. Буфер приёма TCP в ядре заполняется; ни один байт не движется к приложению. На уровне HTTP/2 поток #1 может использовать байты 50 000–50 099, поток #2 — байты 50 100–50 199: оба встают на ожидание повторной передачи.

Метрика: при 1% потере пакетов и RTT 100 мс ожидается примерно одно событие потери на 100 пакетов, с задержкой ~100 мс на событие. При 50 параллельных потоках эта задержка затрагивает все 50. На каналах с высокими потерями (мобильная сеть 0,5–2%, спутник 2–5%) HTTP/2 ощутимо проигрывает HTTP/1.1 с 6 параллельными TCP-соединениями — потому что параллельные соединения имеют независимые домены восстановления потерь.

## Независимость потоков QUIC: точный механизм

QUIC нумерует **пакеты**, а не байтовые смещения единого потока. Каждый QUIC-пакет подтверждается отдельно. Каждый поток отслеживает, какие пакеты несли его фреймы и какие ещё в полёте.

Когда пакет N потерян:
1. QUIC повторно передаёт **конкретные фреймы потоков A и C**, которые были в пакете N.
2. Потоки B, D и E — чьи фреймы были в других пакетах — продолжают отправлять и получать.
3. Прикладной уровень для потока B не видит прерывания.

Цена: заголовки QUIC-фреймов добавляют накладные расходы (stream ID, смещение, длина на фрейм — ~8–10 байт каждый, против TCP-сегмента без накладных расходов на поток). Состояние на соединение тяжелее. Но на каналах с потерями выигрыш решающий.

## HPACK vs QPACK: зависимость от порядка

HPACK (RFC 7541) поддерживает динамическую таблицу, индексированную по последовательности вставок. Запись под индексом 62 — это 62-я пара (имя, значение), вставленная с начала соединения. HEADERS-фрейм, ссылающийся на индекс 62, *должен* быть обработан после фрейма, который вставил индекс 62.

На TCP: фреймы приходят в порядке — HEADERS-фрейм, добавляющий индекс 62, всегда приходит раньше HEADERS-фреймов, ссылающихся на него. Ограничение порядка невидимо.

На QUIC: потоки доставляют фреймы независимо. HEADERS-фрейм в потоке #3 может прийти раньше HEADERS-фрейма в потоке #1, который добавил бы индекс 62. Если HEADERS потока #3 ссылается на индекс 62, декодирование провалится или заблокируется.

Решение QPACK (RFC 9204):
- **Encoder stream** — выделенный однонаправленный QUIC-поток несёт вставки в динамическую таблицу. Этот поток доставляет обновления таблицы в порядке.
- **Decoder stream** — несёт подтверждение о том, какие записи таблицы были обработаны.
- **HEADERS-фреймы** — ссылаются только на записи, которые энкодер знает как подтверждённые на decoder stream. HEADERS-фрейм в потоке #3 никогда не ссылается на запись, которая может ещё не быть в таблице декодера.

Результат: HEADERS-фреймы одного потока никогда не могут блокироваться HEADERS-фреймами другого потока — независимость потоков QUIC сохраняется на уровне сжатия заголовков.

<NumbersCard
  id="card-h3-ops"
  title="HTTP/3 метрики в продакшне (апрель 2026)"
  data-lesson-visual
  stats={[
    { label: "Глобальная доля HTTP/3-трафика", value: "21.1% (Cloudflare Radar)" },
    { label: "Сайты, рекламирующие HTTP/3", value: "38.8% (W3Techs)" },
    { label: "Google, Meta, Cloudflare, Akamai — доля h3", value: ">50% их трафика" },
    { label: "Типичный процент блокировки UDP/443 корпоративными firewall'ами", value: "5–15% корпоративных путей" },
    { label: "Стоимость fallback HTTP/3 → HTTP/2 в RTT", value: "+1 RTT при первом визите" },
    { label: "Окно риска повтора 0-RTT данных", value: "Срок действия сессионного тикета" },
  ]}
/>

## Переговоры о версии: ALPN и цепочка fallback

**Для HTTP/1.1 и HTTP/2:** ALPN (RFC 7301) — расширение TLS. Во время TLS-хендшейка клиент шлёт список поддерживаемых протоколов (`["h2", "http/1.1"]`). Сервер выбирает один. Без дополнительных round-trip — переговоры о версии бесплатны внутри хендшейка.

**Для HTTP/3:** переговоры двухэтапные, потому что QUIC/UDP — другой транспорт:

1. Первое соединение использует TCP-based HTTP/1.1 или /2.
2. Ответ сервера включает `Alt-Svc: h3=":443"; ma=86400` (или HTTPS/SVCB DNS-запись). Браузер сохраняет эту подсказку.
3. Последующие соединения к этому origin: браузер пробует QUIC/UDP первым, с TLS ALPN, выбирающим `h3`.
4. Если UDP/443 заблокирован (QUIC-хендшейк не прошёл): браузер помечает этот origin как «h3 недоступен» на период cooldown и откатывается к TCP-based HTTP/2.

Fallback **тихий** — пользователи не видят ошибок, но операторы должны мониторить метрики ALPN-распределения. Пик h3→h2 fallback — сигнал изменения сетевого пути, блокирующего UDP/443.

## Коалесценция соединений

Несколько origin'ов, разделяющих один IP edge-сервера и охваченных TLS-сертификатом, могут использовать одно HTTP/2 или HTTP/3 соединение — **коалесценция соединений**. Пример: `cdn.example.com` и `api.example.com` оба резолвятся в один IP, и edge-сертификат покрывает оба имени в SAN (Subject Alternative Names). Chrome и Firefox коалесцируют: одно QUIC-соединение обслуживает запросы к обоим hostname'ам.

Это экономит стоимость хендшейка на страницах, загружающих ресурсы с нескольких origin'ов на одном CDN. Подводный камень: если список SAN сертификата не включает один из коалесцируемых hostname'ов (мискофигурация), браузер открывает отдельное соединение — тихая регрессия задержки. Мониторьте количество соединений на origin; внезапный рост сигнализирует о сбое коалесценции.

## Управление перегрузкой в QUIC userspace

Управление перегрузкой TCP (CUBIC, BBR, Reno) работает в **ядре операционной системы**. QUIC реализует управление перегрузкой в **userspace** — внутри приложения или библиотеки QUIC.

Следствия:
- **Гибкость:** операторы могут настраивать или менять алгоритмы на соединение, на приложение или на класс трафика без изменения ядра.
- **CPU-стоимость:** вычисления управления перегрузкой выполняются в процессе, конкурируя с кодом приложения за CPU. При большом числе соединений это ощутимо.
- **Наблюдаемость:** каждое QUIC-соединение должно экспортировать свои измерения окна перегрузки, скорости потерь и RTT. TCP в ядре экспортирует их через `/proc/net/tcp` и `ss`; QUIC требует хуков телеметрии на соединение.

Формат **qlog** IETF (JSON-лог событий на соединение) — инструмент глубокой отладки для QUIC. Для продакшна экспортёры Prometheus/OTel агрегируют QUIC-метрики по соединениям.

## 0-RTT replay: операционный риск

Возобновление 0-RTT (через TLS-сессионные тикеты) позволяет браузеру отправить HTTP-запрос в самом первом QUIC-пакете, до завершения хендшейка. Это экономит один RTT (~50–300 мс на дальних каналах).

Риск: **0-RTT данные могут быть повторены** сетевым злоумышленником. Атакующий, перехвативший первый QUIC-пакет, может переслать его, заставив сервер обработать запрос второй раз. Для GET-запросов (идемпотентных, без побочных эффектов) это безвредно. Для POST (создание заказа, отправка платежа) — катастрофа.

Требования для продакшна:
- Сервер должен отслеживать и отклонять повторённые 0-RTT данные (через anti-replay токены — RFC 9001 § 9.2).
- Серверы должны отвечать `425 Too Early` для небезопасных методов в 0-RTT.
- Хранилище anti-replay токенов: ограниченное; на крупных распределённых развёртываниях нужен общий кэш (Redis) для обнаружения повторов на разных edge-узлах.

<TraceScenario
  id="sr-trace-h3-incident"
  pieceSlug="06-http3-production"
  lang="ru"
  scenario="Диагностика: доля HTTP/3-трафика сервиса упала с 40% до 5% за ночь."
  steps={[
    { prompt: "Шаг 1: какую метрику проверить первой?", reveal: "ALPN-распределение: счётчик tls_alpn_protocol по метке. Если доля h3 обвалилась и h2 поглотил всё, клиенты пробовали QUIC, но не смогли и откатились. Если доля h3 стабильна, но пропускная способность упала, соединения работают, но что-то ещё вызывает задержку." },
    { prompt: "Шаг 2: ALPN показывает h3 с 40% → 0% и h2 поглотил всё. Что скорее всего произошло?", reveal: "UDP/443 где-то на пути блокируется. Сетевой оператор применил правило firewall'а, ISP изменил маршрутизацию, или middlebox транзитного провайдера начал отбрасывать QUIC-пакеты. Fallback HTTP/3 → /2 прозрачен для пользователей, но ALPN-метрика это ловит." },
    { prompt: "Шаг 3: tcpdump на edge-узле показывает входящие UDP/443-пакеты близко к нулю. Подтвердите гипотезу. Как найти блок?", reveal: "Запустите mtr или paris-traceroute с UDP-зондами на UDP/443 из заведомо затронутой клиентской AS. Ищите хоп AS, где UDP/443 перестаёт пересылаться. Типичные виновники: ошибочно настроенный DDoS-защитный скруббер, добавивший UDP/443 в список сброса, или слишком широкое правило ISP против усиления, захватившее легитимные QUIC Initial-пакеты." },
    { prompt: "Шаг 4: пока сетевая команда координирует исправление, что делать со стороны клиента?", reveal: "Пользователи уже на fallback h2 — никаких действий не нужно. Опционально временно снизить Alt-Svc max-age, чтобы клиенты не тратили RTT на провальную попытку h3. Настройте alert на ALPN-метрику для будущих инцидентов. Задокументируйте номер AS ISP и создайте тикет для peering-команды." },
  ]}
/>

<DebugLog
  id="sr-debug-h3-fallback"
  pieceSlug="06-http3-production"
  lang="ru"
  title="Дашборд наблюдаемости: обнаружение пика HTTP/3-fallback"
  output={`# Prometheus-метрики с CDN edge, апрель 2026

## Распределение ALPN-протоколов (скользящее среднее 5 минут)
tls_alpn_protocol{protocol="h3"} 21.1%     # Нормальный базовый уровень
tls_alpn_protocol{protocol="h2"} 51.3%
tls_alpn_protocol{protocol="http/1.1"} 27.6%

## Скорость fallback (h3 пробовался, но откатился на h2)
http3_to_http2_fallback_rate 0.8%          # Базовый уровень: нормальные потери/блокировка ISP

## В 14:35 UTC 2026-04-15 срабатывает alert: скорость fallback растёт
http3_to_http2_fallback_rate 18.2%         # 23x нормы
tls_alpn_protocol{protocol="h3"} 1.3%      # Упал с 21.1%

## Геолокационная разбивка показывает: пик сконцентрирован
fallback_rate{region="US-Northeast", ISP="AS7922"} 67.4%
fallback_rate{region="US-Midwest"} 2.1%
fallback_rate{region="Europe"} 1.9%

## Сетевая диагностика с edge-узла
$ mtr -P UDP/443 route-to-AS7922-customer
hop 5: (AS7922 transit edge) UDP packets: 0% loss [OK]
hop 6: (AS7922 DDoS scrubber, новое правило: 2026-04-15 14:00 UTC)
       UDP packets: 100% loss [BLOCKED]
hop 7+: unreachable`}
  outputLang="log"
  question="Пик HTTP/3-fallback появился для трафика одного ISP (AS7922, US-Northeast), но нигде больше. MTR показывает, что UDP доходит до хопа 5 без потерь. Где блок и какова вероятная причина?"
  hint="Проверьте, что изменилось в инфраструктуре ISP около 14:00 UTC 2026-04-15 — за 35 минут до пика."
  answer="Блок на хопе 6: DDoS-защитный middlebox ISP добавил новое правило за 35 минут до пика, отбрасывающее UDP/443-пакеты как меру против усиления — вероятно, слишком широкое, захватывающее легитимные QUIC Initial-пакеты наряду с целью. Восстановление: обратитесь к ISP для вайтлистинга RFC 9000 QUIC Initial-пакетов или временно снизьте Alt-Svc max-age. Мониторьте с alertами на http3_to_http2_fallback_rate; порог 3x базового уровня — вызов дежурного."
/>

<DebugLog
  id="sr-debug-h3-version"
  pieceSlug="06-http3-production"
  lang="ru"
  title="Вывод curl -v для ошибочно настроенного HTTP/3-сервера"
  output={`$ curl --http3-only -v https://api.example.com/health
* Trying 203.0.113.42:443...
* QUIC handshake fail: protocol violation: invalid initial packet
* Closing connection
curl: (35) QUIC handshake fail: protocol violation: invalid initial packet

$ curl -v https://api.example.com/health
* Connected to api.example.com (203.0.113.42) port 443
* ALPN: offers h2,http/1.1
* ALPN: server accepted h2
< HTTP/2 200 OK
< alt-svc: h3=":443"; ma=86400
< server: nginx/1.27.0
< x-quic-version: draft-29
< content-type: application/json
{"status":"ok"}`}
  outputLang="log"
  question="HTTP/3 явно падает с 'protocol violation: invalid initial packet', но сервер рекламирует h3 через Alt-Svc. В чём мискофигурация и как исправить?"
  hint="Посмотрите на заголовок x-quic-version. Что означает draft-29?"
  answer="Сервер работает на старом QUIC-черновике (draft-29), а не на финальной версии RFC 9000 (wire version 0x00000001, рекламируемой как 'h3'). Современные клиенты (curl с QUIC, Chrome, Firefox) говорят только на финальных версиях RFC 9000 + RFC 9114 и отвергают draft-29 как нарушение протокола. Исправление: обновите сборку nginx + QUIC-библиотеки до пост-RFC-9000 (середина 2021 и позже). До обновления — уберите Alt-Svc: h3: рекламировать h3 при реализации draft-29 — значит тратить RTT на каждого клиента (попытка h3, сбой, откат на h2)."
/>

<RFCQuiz
  id="sr-rfc-quic"
  pieceSlug="06-http3-production"
  lang="ru"
  question="Какой RFC определяет транспортный протокол QUIC (не сам HTTP/3, не TLS-over-QUIC)?"
  choices={[
    { rfc: "RFC 9114", title: "HTTP/3" },
    { rfc: "RFC 9000", title: "QUIC: A UDP-Based Multiplexed and Secure Transport", correct: true },
    { rfc: "RFC 9001", title: "Using TLS to Secure QUIC" },
    { rfc: "RFC 9204", title: "QPACK: Field Compression for HTTP/3" },
  ]}
/>

<DesignPrompt
  id="sr-design-h3"
  pieceSlug="06-http3-production"
  lang="ru"
  prompt="Вы разворачиваете HTTP/3 для глобального API с 10k req/s из 50+ стран, ожидая, что 5–15% путей блокируют UDP/443. Спроектируйте стратегию rollout HTTP/3."
  constraints={[
    "Часть корпоративных и операторских сетей тихо блокирует UDP/443.",
    "Нельзя сломать существующих HTTP/1.1 и HTTP/2 клиентов.",
    "Мобильные пользователи на сотовой связи — приоритетная цель для изоляции потерь HTTP/3.",
    "SRE-команда должна обнаруживать инциденты блокировки UDP в течение 5 минут.",
  ]}
  canonical="Обслуживать все три версии одновременно: HTTP/3 (QUIC/UDP/443), HTTP/2 (TCP/443), HTTP/1.1 (TCP/443). ALPN согласует h2 vs h1.1 для TCP-соединений. Рекламировать h3 через Alt-Svc: h3=':443'; ma=3600 (сначала короткий) и HTTPS/SVCB DNS-записи. Начать с ma=3600 (1 час срока подсказки) вместо 86400 — короткий позволяет быстрее откатиться во время rollout. Определить alert: http3_to_http2_fallback_rate > 3x от 30-дневного базового по любому региону → PagerDuty. После 30 дней стабильности поднять ma до 86400. 0-RTT: включить только для GET/HEAD; отвечать 425 Too Early для небезопасных методов. Наблюдаемость: ALPN-распределение по региону и ASN, скорость повторных передач QUIC, количество миграций соединений, скорость принятия 0-RTT. Коалесценция соединений: убедиться, что SAN edge-сертификата покрывает все origin'ы, разделяющие один IP."
  keyPoints={[
    "Обслуживать все три версии; ALPN и Alt-Svc управляют выбором.",
    "Начать с короткого Alt-Svc max-age во время rollout, увеличить после стабилизации.",
    "Alert на h3-fallback-rate по региону/ASN — это ваш сигнал здоровья сети.",
    "0-RTT безопасен только для идемпотентных методов; 425 Too Early для POST.",
    "Проверить, что SAN edge-сертификата покрывает все коалесцируемые origin'ы.",
  ]}
/>

</Explanation>

<KeyTakeaway>Эксплуатация HTTP/3 в продакшне строится на трёх сигналах: ALPN-распределение (выбирается ли h3?), скорость h3→h2 fallback по ASN (блокируется ли UDP?) и скорость повторных передач QUIC (реален ли выигрыш от изоляции потерь?). QPACK сохраняет независимость потоков на уровне сжатия заголовков; управление перегрузкой QUIC работает в userspace (гибче, но дороже по CPU); 0-RTT безопасен только для идемпотентных методов.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="06-http3-production-retrieval"
  lang="ru"
  questions={[
    { q: "Объясните, почему QPACK существует отдельно от HPACK.", a: "HPACK использует упорядоченную динамическую таблицу: записи добавляются в порядке вставки, ссылки — по порядковому индексу. Если HEADERS-фрейм, добавляющий запись с индексом 60, потерян или пришёл поздно (возможно на QUIC, где потоки независимы), каждый последующий HEADERS-фрейм, ссылающийся на индекс 60, не может декодироваться до прихода потерянного фрейма. На TCP это невидимо, потому что TCP всё равно доставляет в порядке. На QUIC с независимыми потоками вышедшие из порядка HEADERS-фреймы потока A блокировали бы декодирование HEADERS потока B — разрушая изоляцию потоков QUIC. QPACK (RFC 9204) исправляет это, помещая обновления динамической таблицы в выделенный encoder control stream (доставляется в порядке) и ссылаясь только на записи, которые энкодер знает как подтверждённые декодером. Обычные HEADERS-фреймы никогда не блокируются из-за обновлений таблиц друг друга." },
    { q: "Инженер говорит, что HTTP/3 всегда лучше HTTP/2 из-за потоков QUIC. Где изъян в рассуждении?", a: "Ключевой выигрыш HTTP/3 — изоляция потерь на уровне потока — важен только на каналах с потерями. На проводных каналах с <0.1% потерь пакетов TCP редко повторно передаёт, и HOL-блокировка невидима. У HTTP/3 есть реальные издержки: шифрование QUIC работает в userspace (больше CPU, чем у TCP+TLS с выгрузкой в ядро), стек сложнее, а middlebox'ы, отбрасывающие UDP/443, принудительно переключают на fallback. Для east-west трафика дата-центра на надёжных 10 GbE каналах HTTP/2 over TCP часто быстрее и проще. HTTP/3 выигрывает на мобильных, потеристых, дальних каналах. Правильный ответ: запустить HTTP/3 на пользовательском edge; HTTP/2 для внутреннего east-west трафика." },
    { q: "Что такое коалесценция соединений и что её ломает?", a: "Коалесценция соединений: когда несколько origin'ов разделяют один IP сервера и TLS-сертификат охватывает оба имени (через список SAN), HTTP/2 и HTTP/3 клиенты могут повторно использовать одно соединение для запросов к обоим origin'ам вместо открытия отдельных соединений на hostname. Это экономит стоимость хендшейка на страницах, загружающих ресурсы с нескольких CDN-origin'ов. Что её ломает: TLS-сертификат, чей список SAN не включает один из коалесцируемых hostname'ов. Браузер открывает отдельное соединение для непокрытого hostname — тихая регрессия задержки, видимая только как рост количества соединений на origin в метриках." },
  ]}
/>

<Recap lang="ru">
Эксплуатация HTTP/3 в продакшне фундаментально зависит от надёжности UDP-пути и операционной наблюдаемости. Независимость потоков QUIC означает, что один потерянный пакет останавливает только потоки, чьи данные он нёс — но это требует пакетного отслеживания потерь QUIC (не байтовой модели TCP) и encoder/decoder control streams QPACK (не упорядоченной динамической таблицы HPACK). Управление перегрузкой QUIC работает в userspace — гибче, но CPU-интенсивнее. Переговоры о версии для HTTP/3 используют Alt-Svc или HTTPS/SVCB DNS и тихо откатываются на HTTP/2 при блокировке UDP/443 — ALPN-метрика распределения — сигнал. Коалесценция соединений экономит стоимость хендшейка для origin'ов, разделяющих один IP и SAN сертификата. 0-RTT экономит round-trip, но требует server-side anti-replay и должен быть ограничен идемпотентными методами.
</Recap>
