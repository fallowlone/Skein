infographic chart-column-simple
data
  title Сколько точек head-of-line-блокировки остаётся в каждой версии
  desc HTTP/1.1 блокирует на уровне запроса и наследует упорядоченность TCP; /2 устраняет HTTP-уровень, но HOL на уровне TCP остаётся; /3 (QUIC) устраняет оба
  values
    - label HTTP/1.1
      value 2
    - label HTTP/2
      value 1
    - label HTTP/3
      value 0
theme
  palette #f43f5e #f59e0b #10b981
