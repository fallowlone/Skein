---
concepts:
- http
- request-response
- http1.1
- http2
- http3
- head-of-line-blocking
- parallel-connections
- alpn
deepensInto:
- 02-http2-multiplexing
- 03-http3-and-quic
estMin: 8
lang: ru
lessonType: topic
level: junior
order: 1
prereqs:
- 03-tcp-handshake/01-the-three-way-handshake
- 05-tls-handshake/01-what-tls-does
slug: 01-http-fundamentals
sources:
- https://datatracker.ietf.org/doc/html/rfc9110
- https://datatracker.ietf.org/doc/html/rfc9112
- https://web.dev/articles/performance-http2/
spiral:
- multiplexing
status: ready
summary: HTTP — протокол, которым браузеры и серверы обмениваются страницами, API
  и ресурсами. Три живые версии каждая по-своему борются с блокировкой начала очереди.
title: 'HTTP: язык запрос-ответ в вебе'
track: networking
unit: 06-http-versions
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import MetaphorComplete from "~/components/pedagogy/MetaphorComplete.astro";
import NumbersCard from "~/components/layout/NumbersCard.astro";
import PersonaTag from "~/components/pedagogy/PersonaTag.astro";
import StackDiagram from "~/components/diagram/StackDiagram.astro";

<Hook>
TLS запечатал канал. TCP пронумеровал байты. Теперь протокол сверху: HTTP — язык, которым браузеры и серверы говорят, чтобы запрашивать и получать веб-страницы, API, картинки, шрифты и скрипты. Тридцать лет HTTP/1.1 обрабатывал почти весь трафик. Потом браузеры захотели большего. За следующие несколько минут ты поймёшь, зачем нужна каждая версия и какой именно режим отказа каждая из них исправляет.
</Hook>

<Crux>Зачем каждая HTTP-версия существует, что изменила и почему HOL-блокировка — главный враг.</Crux>

<Explanation>

## Что делает HTTP одним предложением

Ты когда-нибудь замечал, что одна и та же страница грузится быстро на одной сети и тормозит на другой? Нередко виноват HTTP-версия под капотом, а не сервер и не контент. HTTP (HyperText Transfer Protocol) — это **язык запрос-ответ**, которым браузеры и серверы обмениваются веб-страницами, API, картинками — всем что видно в браузере. Клиент спрашивает, сервер отвечает. Но *как именно* они спрашивают и отвечают — вот где версии расходятся.

## Метафора ресторана

Представьте HTTP как официанта принимающего заказы.

**HTTP/1.1:** один официант на стол, один заказ за раз — ждёте каждое блюдо до следующего заказа. Ресторан нанимает шесть официантов на клиента (шесть параллельных соединений). Работает, но расточительно.

**HTTP/2:** один официант берёт десять заказов сразу и приносит блюда в том порядке, в каком кухня успевает. Одно соединение, много запросов в полёте одновременно.

**HTTP/3:** то же что /2, но задержавшееся блюдо больше не задерживает остальные — они выходят независимо. Построен на другом транспорте (QUIC поверх UDP), который изолирует задержки на уровне потока.

<NumbersCard
  id="card-http-versions-overview"
  title="HTTP-версии в цифрах"
  data-lesson-visual
  stats={[
    { label: "HTTP/1.1 параллельных TCP-соединений на origin", value: "6–8" },
    { label: "HTTP/2 потоков на соединение", value: "≥100 (по умолчанию RFC)" },
    { label: "HTTP/3 в трафике (апрель 2026, Cloudflare Radar)", value: "21.1% веб-трафика" },
    { label: "HTTP/2 поддержка сайтов", value: ">95% веба" },
    { label: "Прогрев соединения (HTTP/3 с resumption)", value: "0 RTT для запроса" },
    { label: "Экономия заголовков HPACK (в среднем)", value: "~80–90% после первого запроса" },
  ]}
/>

## Один сценарий от начала до конца

Вы заходите на сайт. Браузер делает [DNS](/ru/networking/04-dns-resolution/), [TCP](/ru/networking/03-tcp-handshake/), [TLS](/ru/networking/05-tls-handshake/), затем первый HTTP-запрос. HTML страницы называет десятки файлов; браузер отправляет запросы используя HTTP-версию, согласованную сервером через **ALPN** (Application Layer Protocol Negotiation — расширение TLS, позволяющее клиенту и серверу выбрать `h2`, `h3` или `http/1.1` в процессе хендшейка). Современные сайты получают HTTP/2 или HTTP/3; старые origin — HTTP/1.1.

<PersonaTag id="bea" lang="ru" /> браузер серверу <PersonaTag id="sven" lang="ru" />: «GET /index.html». Sven отвечает HTML. Bea видит `<script src="app.js">` и сразу говорит: «GET /app.js». В HTTP/1.1 Bea ждёт каждый файл на одном соединении. В HTTP/2 и /3 Bea стреляет все GET сразу, и Sven отдаёт их перемежая потоки.

## Главный враг: HOL-блокировка

**Блокировка начала очереди (HOL, Head-of-Line Blocking)** — паттерн, при котором медленный элемент в начале очереди тормозит всё за ним. В HTTP/1.1 блокировка на уровне запроса: каждое соединение несёт только один запрос-ответ за раз. HTTP/2 устранил HOL на *HTTP-уровне* через мультиплексирование потоков, но TCP снизу по-прежнему имеет HOL на *транспортном уровне*. HTTP/3 исправил оба слоя, используя QUIC с восстановлением потерь на уровне потока.

<Inset kind="why" lang="ru">
**Почему не просто открыть больше соединений?** Обходной путь HTTP/1.1 с шестью параллельными TCP-соединениями стоит шести отдельных хендшейков (каждый 1–2 RTT), шести окон перегрузки с нуля (TCP slow start) и серверных состояний для всех шести. На линии с RTT 100 мс эти шесть холодных стартов обходятся по 100–200 мс каждый. Одно соединение HTTP/2 платит эту цену один раз.
</Inset>

<Quiz
  id="jr-quiz-1"
  pieceSlug="01-http-fundamentals"
  lang="ru"
  question="Главная причина медлительности HTTP/1.1 для современных веб-страниц?"
  choices={[
    { label: "Браузеры плохо его поддерживают", misconception: "Браузеры полностью поддерживают HTTP/1.1. Bottleneck — дизайн протокола." },
    { label: "Каждый запрос ждёт предыдущего ответа на одном соединении — браузеры вынуждены открывать много параллельных соединений", correct: true },
    { label: "Шифрует всё дважды", misconception: "HTTP/1.1 сам не шифрует ничего — шифрование задача TLS." },
    { label: "Работает только на Wi-Fi", misconception: "HTTP работает на любой TCP-сети." },
  ]}
/>

<Quiz
  id="jr-quiz-2"
  pieceSlug="01-http-fundamentals"
  lang="ru"
  question="Главная победа HTTP/3 над HTTP/2?"
  choices={[
    { label: "Другие cipher suites", misconception: "Cipher suites — территория TLS, не HTTP." },
    { label: "На лоссовой сети один потерянный пакет больше не тормозит каждый поток — потоки независимы на уровне восстановления потерь", correct: true },
    { label: "Меньшие URL", misconception: "URL одинаковые во всех версиях HTTP." },
    { label: "Встроенное кеширование", misconception: "Семантика кеширования в основном одинакова во всех версиях." },
  ]}
/>

<DragOrder
  id="jr-drag-1"
  pieceSlug="01-http-fundamentals"
  lang="ru"
  prompt="Расставьте HTTP-версии хронологически:"
  items={[
    "HTTP/1.0 — один запрос на соединение, без keep-alive",
    "HTTP/1.1 — keep-alive, pipelining (редко работал на практике)",
    "HTTP/2 — мультиплексирование потоков на одном TCP + HPACK",
    "HTTP/3 — та же форма но поверх QUIC (UDP) — потеря пакета per-stream",
  ]}
/>

<MetaphorComplete
  id="jr-metaphor-1"
  pieceSlug="01-http-fundamentals"
  lang="ru"
  setup="Заполните пропуск: HTTP — это формат _______, которым браузеры и серверы обмениваются веб-контентом."
  accepted={["запрос-ответ", "запрос ответ", "вопрос-ответ", "request-response"]}
  canonical="запрос-ответ"
  explanation="HTTP фундаментально запрос-ответ: клиент спрашивает, сервер отвечает. Версии меняют то, сколько запросов может быть в полёте одновременно."
/>

</Explanation>

<StackDiagram
  label="Стек протоколов: HTTP/1.1 и HTTP/2 работают поверх TLS поверх TCP; HTTP/3 — поверх QUIC поверх UDP, транспортный уровень снизу"
  hue="var(--d-network)"
  layers={[
    { label: "HTTP/1.1 · HTTP/2 · HTTP/3", note: "запрос-ответ" },
    { label: "TLS 1.2 / 1.3", note: "h2: отдельно · h3: встроен в QUIC" },
    { label: "TCP (HTTP/1.1, HTTP/2)  —или—  QUIC (HTTP/3)", note: "QUIC = восстановление потерь на уровне потока" },
    { label: "TCP (упорядоченные байты)  —или—  UDP (несёт QUIC)", note: "транспорт" },
  ]}
  caption="HTTP/1.1 и HTTP/2 сидят на TCP, поэтому один потерянный пакет тормозит все потоки (HOL-блокировка на уровне TCP). HTTP/3 заменяет TCP на QUIC поверх UDP, перенося восстановление потерь на уровень потока. ALPN выбирает версию внутри TLS-хендшейка."
/>

<KeyTakeaway>HTTP — протокол запрос-ответ для веба; HTTP/1.1 параллелизируется через 6 TCP-соединений, HTTP/2 мультиплексирует потоки на одном TCP (но HOL на уровне TCP остаётся), HTTP/3 переходит на QUIC для изоляции потерь на уровне потока — ALPN выбирает версию во время TLS.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="01-http-fundamentals-retrieval"
  lang="ru"
  questions={[
    { q: "Одним предложением: зачем изобрели HTTP/2 и HTTP/3, если HTTP/1.1 уже работал?", a: "HTTP/1.1 вынуждал браузеры открывать много параллельных TCP-соединений для быстрой загрузки современной страницы, и даже при параллельных соединениях один медленный ответ блокировал дальнейшие запросы на той же линии — /2 и /3 позволяют многим запросам делить одно соединение, а /3 ещё и переживает потерю пакетов без заморозки всех запросов." },
    { q: "Что такое ALPN и когда он работает?", a: "ALPN (Application Layer Protocol Negotiation) — расширение TLS: во время TLS-хендшейка клиент передаёт список поддерживаемых HTTP-версий ([h2, http/1.1]) и сервер выбирает одну. Работает внутри TLS-хендшейка без лишнего round-trip для переговоров о версии." },
    { q: "Почему HTTP/2 по-прежнему страдает от HOL-блокировки, хотя мультиплексирует потоки?", a: "HTTP/2 мультиплексирует на HTTP-уровне, но работает поверх TCP, который доставляет байты строго по порядку. Один потерянный TCP-сегмент заставляет TCP буферизировать все последующие байты до повторной передачи — блокируя все HTTP/2-потоки на этом соединении. HTTP/3 решает это: QUIC обеспечивает восстановление потерь на уровне потока." },
  ]}
/>

<Recap lang="ru">
HTTP — протокол запрос-ответ, которым браузеры и серверы обмениваются страницами, API и ресурсами. HTTP/1.1 параллелизируется через 6–8 TCP-соединений на origin — каждое соединение обрабатывает один запрос за раз. HTTP/2 ввёл мультиплексирование потоков на одном TCP-соединении, устранив HOL-блокировку на уровне приложения, но упорядоченная доставка TCP по-прежнему вызывает HOL на транспортном уровне при потере пакетов. HTTP/3 переходит на QUIC поверх UDP, давая каждому потоку независимое восстановление потерь. Все три версии сосуществуют: ALPN договаривается о выборе во время TLS-хендшейка. Теперь, когда встретишь медленную загрузку на мобильной сети, знаешь, что спросить: какая HTTP-версия работает и не HOL-блокировка ли TCP виновата?
</Recap>
