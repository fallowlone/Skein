---
concepts:
- chunked-transfer
- sse
- server-sent-events
- websocket
- rfc-8441
- grpc
- grpc-web
- trailers
- brotli
- compression
- crime-breach
- rate-limiting
- token-bucket
deepensInto:
- 07-http-design-and-security
estMin: 12
lang: ru
lessonType: topic
level: middle
order: 5
prereqs:
- 02-http2-multiplexing
slug: 05-streaming-and-websockets
sources:
- https://datatracker.ietf.org/doc/html/rfc9110
- https://datatracker.ietf.org/doc/html/rfc6455
- https://datatracker.ietf.org/doc/html/rfc8441
- https://grpc.io/docs/what-is-grpc/introduction/
spiral: []
status: ready
summary: 'HTTP — не только запрос-ответ: chunked transfer, SSE, WebSocket и gRPC каждый
  по-своему пробивают брешь в модели единственного ответа для реального времени и
  двустороннего общения.'
title: HTTP-стриминг, SSE, WebSocket и gRPC
track: networking
unit: 06-http-versions
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import NumbersCard from "~/components/layout/NumbersCard.astro";
import Misconception from "~/components/layout/Misconception.astro";
import SequenceDiagram from "~/components/diagram/SequenceDiagram.astro";
import Infographic from "~/components/diagram/Infographic.astro";
import streamingDecisionSpace from "./infographics/streaming-decision-space.svg?raw";

<Hook>
REST API возвращают полный ответ. Но живые дашборды, чат, цены в реальном времени и потоки токенов AI требуют другого: данные поступают по частям или в двух направлениях. HTTP растягивается до этих случаев — через chunked transfer, SSE, апгрейды WebSocket и gRPC-трейлеры — но каждый выбор несёт свои компромиссы.
</Hook>

<Crux>Chunked, SSE, WebSocket и gRPC-трейлеры расширяют HTTP за запрос-ответ — выбирайте абстракцию под форму трафика.</Crux>

<Explanation>

## Chunked transfer encoding

Что общего у потоковой передачи AI-токенов, слежки за логами в реальном времени и генерируемого HTML? Ни один из них не знает итоговый размер ответа до начала отправки. Именно эту проблему решает chunked transfer (передача по частям). HTTP/1.1 поддерживает `Transfer-Encoding: chunked` для потоковой передачи ответов без заранее известной общей длины. Сервер отправляет **чанки с префиксом размера** до тех пор, пока чанк нулевой длины не закрывает тело:

```
HTTP/1.1 200 OK
Transfer-Encoding: chunked
Content-Type: text/plain

5\r\n
Hello\r\n
6\r\n
 World\r\n
0\r\n
\r\n
```

Это позволяет серверу начать отправку данных до того, как вычислен весь ответ — полезно для генерируемого HTML, потоковых AI-завершений и слежки за логами. HTTP/2 и HTTP/3 не используют chunked encoding: DATA-фреймы нативно обрабатывают тела переменной длины без явного слоя фреймирования.

## Server-Sent Events (SSE)

SSE (`Content-Type: text/event-stream`) — долгоживущий HTTP-ответ, доставляющий читаемые JavaScript'ом события от сервера браузеру **в одном направлении**. Браузер делает один GET-запрос; сервер держит соединение открытым и толкает события:

```
HTTP/1.1 200 OK
Content-Type: text/event-stream
Cache-Control: no-cache

data: {"temp": 72.4}\n\n
data: {"temp": 72.6}\n\n
```

SSE построен на стандартном HTTP, проходит через все прокси без апгрейда протокола, автоматически переподключается при разрыве соединения и работает на HTTP/2 (где едет в одном потоке среди многих, не блокируя другие запросы). Ограничение: **только сервер → клиент** — клиент не может толкать данные обратно через тот же SSE-поток (нужен отдельный запрос).

SSE — правильный выбор для: живых дашбордов, потоков уведомлений, потоковой передачи AI-токенов (`text/event-stream` — то, как ChatGPT-подобные интерфейсы стримят токены). WebSocket — правильный выбор для: двустороннего реального времени (чат, совместное редактирование, состояние мультиплеерной игры).

## WebSocket

WebSocket (RFC 6455) начинается как HTTP/1.1 и апгрейдится до **бинарного двустороннего фрейм-протокола**:

1. Клиент отправляет `GET /ws HTTP/1.1` с `Upgrade: websocket` и `Connection: Upgrade`.
2. Сервер отвечает `101 Switching Protocols`.
3. TCP-соединение теперь — WebSocket-туннель: бинарные фреймы (текстовые или бинарные) текут в обоих направлениях.

WebSocket **несовместим с мультиплексированием HTTP/2** — после переключения соединения на протокол WebSocket оно больше не является HTTP/2-соединением. Решение: **RFC 8441** (WebSocket over HTTP/2) — клиент отправляет расширенный CONNECT-запрос с `:protocol: websocket`, создавая один HTTP/2-поток, ведущий себя как WebSocket-туннель. Это позволяет одному HTTP/2-соединению мультиплексировать несколько WebSocket-соединений рядом с обычными запросами.

<NumbersCard
  id="card-streaming-comparison"
  title="Сравнение вариантов HTTP-стриминга"
  data-lesson-visual
  stats={[
    { label: "SSE — направление", value: "Сервер → Клиент" },
    { label: "SSE — требует апгрейда протокола", value: "Нет — обычный HTTP" },
    { label: "WebSocket — направление", value: "Двустороннее" },
    { label: "WebSocket — поддержка HTTP/2", value: "RFC 8441 extended CONNECT" },
    { label: "gRPC — транспорт", value: "HTTP/2 (всегда)" },
    { label: "gRPC — двусторонний стриминг", value: "Да (4 режима: unary, server-streaming, client-streaming, bidi)" },
  ]}
/>

## gRPC: трейлеры и зависимость от HTTP/2

gRPC — высокопроизводительный RPC-фреймворк, работающий поверх HTTP/2. Ключевая механика:

- gRPC кодирует запросы и ответы как **Protocol Buffers** (бинарно сериализованные структуры) в HTTP/2 DATA-фреймах.
- Каждый gRPC-вызов — один HTTP/2-поток. Множество одновременных RPC мультиплексируются на одном соединении.
- **Трейлеры несут результат:** gRPC-статус и сообщение приходят в trailing HEADERS-фреймах *после* DATA-фреймов (тела). gRPC-статус-код отделён от HTTP-статус-кода — gRPC всегда шлёт `200 OK` на HTTP-уровне; реальный результат (OK, NOT_FOUND и т.д.) приходит в трейлере `grpc-status`.

Вместе эти три механики означают: успех или провал gRPC-вызова невидим для HTTP-уровня мониторинга, если явно не читать трейлеры — без этого каждый gRPC-запрос выглядит как успешный `200 OK`, даже когда RPC завершился ошибкой. gRPC-Web существует потому, что браузеры не могут надёжно читать HTTP-трейлеры. gRPC-Web переписывает trailing-заголовки сервера как финальное **base64-кодированное сообщение в теле ответа**, которое браузеры умеют читать. Прокси (Envoy, gRPC-Web proxy) транслирует между настоящим gRPC (с трейлерами) и gRPC-Web (трейлеры встроены в тело).

<Infographic
  svg={streamingDecisionSpace}
  label="2x2 пространство решений, размещающее четыре стриминговые абстракции по направлению (сервер→клиент или двустороннее) и вписанности в HTTP (обычный прокси-дружелюбный HTTP или переключение протокола / только HTTP/2): SSE — одностороннее по обычному HTTP, chunked transfer — одностороннее потоковое тело, WebSocket — двустороннее через переключение протокола, gRPC — двустороннее, но только HTTP/2."
  caption="Crux наглядно: выбирайте по форме трафика — направление решает SSE/chunked против WebSocket/gRPC, а вписанность в HTTP решает, какая сторона каждой пары."
  hue="var(--d-infra)"
/>

## Сжатие

Серверы сжимают тела ответов через `Content-Encoding: gzip|br|zstd`. **Brotli (br)** сжимает текст примерно на 20% лучше gzip при сопоставимой скорости декомпрессии. Серверы выбирают алгоритм на основе заголовка `Accept-Encoding` клиента.

**Угроза безопасности — атаки CRIME и BREACH:** эти атаки эксплуатировали HTTPS + сжатие для утечки сессионных токенов через наблюдение за изменениями размера ответа, когда атакующий внедрял контролируемый контент. Меры защиты: **отключить сжатие для ответов, включающих и управляемые атакующим данные, и конфиденциальные данные в одном теле ответа**. Сжатие статических ресурсов безопасно (предварительно вычисленное, нет пользовательского ввода). Динамические API-ответы, смешивающие сессионные токены с отражёнными запросом данными, должны либо пропускать сжатие, либо использовать случайное набивание.

## HTTP-ограничение скорости

Серверы возвращают `429 Too Many Requests` с `Retry-After: <секунды>` для регулирования агрессивных клиентов. Распространённые алгоритмы:

- **Token bucket:** корзина пополняется с фиксированной скоростью; всплески разрешены до ёмкости. Лучше всего для рабочих нагрузок с пиками, но в среднем умеренных.
- **Leaky bucket:** запросы вытекают с фиксированной скоростью; излишки ставятся в очередь или сбрасываются. Сглаживает пики.
- **Fixed window:** подсчёт запросов за временное окно (например, 100/минута). Уязвим к пикам на границе окна.
- **Sliding window:** подсчёт запросов за последние N секунд. Точнее, требует больше памяти.

CDN (Cloudflare, Fastly) ограничивают скорость на краю до того, как запросы достигают origin — первая линия обороны. Origin-серверы добавляют второй слой. Клиенты должны уважать `Retry-After` и использовать экспоненциальный откат для предотвращения "стада грома" повторных запросов.

<Quiz
  id="mid-quiz-1"
  pieceSlug="05-streaming-and-websockets"
  lang="ru"
  question="Почему gRPC использует HTTP/2-трейлеры для статуса вместо HTTP-статус-кода?"
  choices={[
    { label: "HTTP-статус-кодов недостаточно", misconception: "HTTP имеет сотни статус-кодов. Причина — в тайминге, не в количестве." },
    { label: "gRPC-статус известен только после отправки всего тела ответа (потока сообщений) — трейлеры приходят после тела, что делает их правильным местом для метаданных конца потока", correct: true },
    { label: "Чтобы избежать ограничений CORS в браузере", misconception: "CORS применяется к любому HTTP-запросу; это не специфично для трейлеров." },
    { label: "Потому что HTTP/1.1 не поддерживает трейлеры", misconception: "HTTP/1.1 поддерживает трейлеры через chunked transfer; причина — семантика потоков, не версия." },
  ]}
/>

<Quiz
  id="mid-quiz-2"
  pieceSlug="05-streaming-and-websockets"
  lang="ru"
  question="Когда выбирать SSE вместо WebSocket?"
  choices={[
    { label: "Когда нужна двусторонняя связь в реальном времени", misconception: "SSE только сервер → клиент. Двусторонность требует WebSocket или HTTP/3 WebTransport." },
    { label: "Когда сервер толкает события, а клиенту нужно только слушать — SSE проще, работает через прокси без апгрейда и автоматически переподключается", correct: true },
    { label: "Когда нужно бинарное фреймирование сообщений", misconception: "SSE отправляет текстовые события. WebSocket нативно поддерживает бинарные фреймы." },
    { label: "Когда HTTP/2 недоступен", misconception: "SSE работает и на HTTP/1.1, и на HTTP/2; выбор зависит от направления трафика, а не версии HTTP." },
  ]}
/>

<DragOrder
  id="mid-drag-ws"
  pieceSlug="05-streaming-and-websockets"
  lang="ru"
  prompt="Упорядочьте шаги WebSocket-хендшейка поверх HTTP/1.1:"
  items={[
    "Клиент отправляет GET /ws HTTP/1.1 с Upgrade: websocket и Sec-WebSocket-Key",
    "Сервер отвечает 101 Switching Protocols с Sec-WebSocket-Accept",
    "HTTP-протокол завершается на этом TCP-соединении",
    "Двусторонний фрейм-протокол WebSocket начинается на том же TCP-соединении",
    "Клиент и сервер теперь могут отправлять фреймы друг другу в любой момент",
  ]}
/>

<Inset kind="why" lang="ru">
**Почему gRPC-Web переписывает трейлеры в тело.** Браузерные API XHR и fetch() не могут читать HTTP-трейлеры — API просто не предоставляет к ним доступ. Трейлеры — заголовки, приходящие *после* тела ответа, и HTTP-клиент браузера их скрывает. gRPC-Web решает это, кодируя трейлеры как финальное фреймированное сообщение в теле, с особым байтом-префиксом для различия данных ответа и трейлеров. JavaScript-библиотека gRPC-Web парсит это финальное сообщение для извлечения grpc-status. На стороне сервера прокси Envoy или nginx-grpc-web транслирует между настоящим gRPC (с трейлерами) и gRPC-Web (трейлеры встроены в тело).
</Inset>

</Explanation>

<SequenceDiagram
  label="Апгрейд WebSocket: HTTP/1.1 GET с заголовком Upgrade получает ответ 101 Switching Protocols, после чего обе стороны шлют бинарные фреймы в любом направлении"
  hue="var(--d-network)"
  actors={["Клиент", "Сервер"]}
  messages={[
    { from: "Клиент", to: "Сервер", label: "GET /ws · Upgrade: websocket" },
    { from: "Сервер", to: "Клиент", label: "101 Switching Protocols" },
    { from: "Клиент", to: "Сервер", label: "WS-фрейм (напр. сообщение чата)" },
    { from: "Сервер", to: "Клиент", label: "WS-фрейм (push в любой момент)" },
  ]}
  caption="После 101 HTTP-протокол на этом TCP-соединении завершается и начинается двусторонний канал WebSocket-фреймов. В отличие от SSE (только сервер→клиент), любая сторона может отправить фрейм в любой момент."
/>

<KeyTakeaway>Chunked transfer стримит тела переменной длины; SSE — правильный выбор для потоков событий сервер→клиент (проще, прокси-дружелюбно, автопереподключение); WebSocket (апгрейд с HTTP) обрабатывает двусторонний трафик; gRPC работает на HTTP/2 и помещает финальный RPC-статус в trailing HEADERS-фреймы — которые gRPC-Web прокси переписывают в фреймы тела для совместимости с браузерами.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="05-streaming-and-websockets-retrieval"
  lang="ru"
  questions={[
    { q: "В чём разница между SSE и WebSocket и когда выбирать каждый?", a: "SSE (Server-Sent Events) только сервер→клиент — сервер держит долгоживущий HTTP-ответ открытым и толкает текстовые события. Работает через все HTTP-прокси без апгрейдов, автопереподключается, работает на HTTP/2. WebSocket двусторонний — после апгрейда 101 Switching Protocols обе стороны могут свободно отправлять фреймы. Выбирайте SSE для живых дашбордов, лент уведомлений и потоков AI-токенов, где только сервер отправляет данные. Выбирайте WebSocket для чата, совместного редактирования и мультиплеерных игр, где клиент тоже отправляет данные в реальном времени." },
    { q: "Почему атаки CRIME и BREACH нацелены на HTTPS+сжатие и как защититься?", a: "CRIME и BREACH эксплуатировали тот факт, что TLS сжимает перед шифрованием (в старых конфигурациях) или сервер сжимает HTTP-ответы перед шифрованием. Атакующий, способный внедрить контролируемый текст в тот же ответ, что и сессионный токен, может наблюдать изменения размера ответа — сжатие уменьшает повторяющиеся паттерны, поэтому угадывание правильного байта токена вызывает уменьшение размера. Защита: отключить сжатие для ответов, смешивающих управляемые атакующим данные с конфиденциальными в одном теле. Статические ресурсы безопасно сжимать; динамические API-ответы, смешивающие сессионные токены с отражёнными строками запроса, должны пропускать сжатие или дополнять случайными данными." },
    { q: "Как RFC 8441 позволяет WebSocket сосуществовать с мультиплексированием HTTP/2?", a: "Обычный WebSocket (RFC 6455) требует HTTP/1.1 — апгрейд 101 Switching Protocols полностью прекращает HTTP-протокол на этом TCP-соединении. RFC 8441 добавляет расширенный метод CONNECT в HTTP/2: клиент отправляет CONNECT с :protocol: websocket для открытия одного HTTP/2-потока, и этот поток становится WebSocket-туннелем. Другие HTTP/2-потоки на том же соединении продолжают работать нормально. Это означает, что одно HTTP/2-соединение может мультиплексировать десятки обычных запросов рядом с несколькими WebSocket-соединениями, вместо выделения отдельного TCP-соединения на каждый WebSocket." },
  ]}
/>

<Recap lang="ru">
HTTP-стриминг выходит за рамки единственного запрос-ответ через chunked transfer encoding (тела переменной длины), SSE (долгоживущие текстовые потоки событий сервер→клиент), WebSocket (двусторонние бинарные фреймы через апгрейд 101, или RFC 8441 extended CONNECT на HTTP/2) и gRPC (бинарные RPC на HTTP/2, с RPC-статусом в trailing HEADERS-фреймах). gRPC-Web прокси переписывают трейлеры в фреймы тела для совместимости с браузерами. Сжатие (Brotli/gzip через Content-Encoding) экономит полосу пропускания, но требует осторожности с ответами, смешивающими управляемые данные с конфиденциальными (CRIME/BREACH). Ограничение скорости через 429 + Retry-After использует алгоритмы token bucket или sliding window; CDN-ограничение на краю — первая линия обороны. Теперь, когда проектируешь real-time фичу, знаешь: SSE — когда сервер толкает, а клиент слушает; WebSocket — только когда нужны двусторонние фреймы.
</Recap>
