---
concepts:
- http2
- stream-multiplexing
- hpack
- headers-frame
- data-frame
- server-push
- rst-stream
- settings-frame
- flow-control
- head-of-line-blocking
deepensInto:
- 03-http3-and-quic
- 06-http3-production
estMin: 14
lang: ru
lessonType: topic
level: middle
order: 2
prereqs:
- 01-http-fundamentals
- 05-tls-handshake/02-the-1rtt-handshake
slug: 02-http2-multiplexing
sources:
- https://datatracker.ietf.org/doc/html/rfc9113
- https://datatracker.ietf.org/doc/html/rfc7541
- https://web.dev/articles/performance-http2/
- https://blog.cloudflare.com/http3-the-past-present-and-future/
spiral:
- multiplexing
status: ready
summary: HTTP/2 заменил обходной путь HTTP/1.1 с параллельными соединениями на мультиплексированные
  потоки на одном TCP — HPACK сжимает заголовки, Server Push оказался тупиком.
title: 'HTTP/2: потоки, фреймы и HPACK'
track: networking
unit: 06-http-versions
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import TraceScenario from "~/components/pedagogy/TraceScenario.astro";
import NumbersCard from "~/components/layout/NumbersCard.astro";
import Misconception from "~/components/layout/Misconception.astro";
import SequenceDiagram from "~/components/diagram/SequenceDiagram.astro";

<Hook>
Браузер загружает страницу с 50 подресурсами: открывает 6 TCP-соединений к одному origin, заполняет их запросами, ставит остальные в очередь. Каждое соединение платит свой хендшейк. HTTP/2 сворачивает всё это в одно соединение с 50 параллельными потоками — но у исправления есть собственный режим отказа.
</Hook>

<Crux>HTTP/2 мультиплексирует потоки на одном TCP-соединении; HPACK сжимает заголовки; HOL-блокировка TCP остаётся нерешённой проблемой.</Crux>

<Explanation>

## HTTP/1.1: pipelining и шесть соединений

Почему современный браузер открывает шесть TCP-соединений к одному серверу? Это не случайность — это обходной путь, зашитый в движки браузеров ради выживания при фундаментальном ограничении HTTP/1.1. HTTP/1.1 (RFC 9112) отправляет один запрос на TCP-соединение и ждёт полного ответа перед следующим. Для параллелизации браузеры открывают **6–8 соединений** к одному origin. При загрузке 10 JS-файлов открывается 6 соединений, по одному запросу на каждое, остальные 4 в очереди. Когда ответ приходит — соединение освобождается и берёт следующий файл.

**Pipelining** (RFC 7230) позволял отправлять несколько запросов не дожидаясь ответов — но редко работал: прокси ломали его, и если запрос #1 ждал медленного сервера, запрос #2 блокировался за ним на том же соединении — HOL-блокировка на стороне ответа. Почти все браузеры отключили pipelining по умолчанию.

## HTTP/2: слой фреймирования

HTTP/2 (RFC 9113) отказывается от модели одного запроса на соединение. **Одно TCP-соединение** несёт много параллельных **потоков** — каждый поток независимая пара запрос-ответ.

**Фреймы** — единица передачи:

- **HEADERS** — открывает поток, несёт метаданные запроса или ответа. Поток создаётся когда клиент отправляет HEADERS с новым stream ID.
- **DATA** — несёт тело потока.
- **RST_STREAM** — прерывает поток без закрытия соединения.
- **SETTINGS** — согласует параметры соединения (максимум потоков, начальный размер окна, размер таблицы заголовков).
- **WINDOW_UPDATE** — управление потоком: сообщает отправителю сколько байт получатель готов буферизировать.

Сервер может **перемежать** ответы разных потоков: 100 байт ответа потока #1, затем 100 байт потока #3, затем снова #1 — всё на одном TCP. Порядок внутри одного потока сохраняется; порядок между потоками — на усмотрение сервера.

<NumbersCard
  id="card-h2-framing"
  title="Факты о фреймировании HTTP/2"
  data-lesson-visual
  stats={[
    { label: "Размер заголовка фрейма HTTP/2", value: "9 байт (тип, флаги, длина, stream ID)" },
    { label: "Пространство stream ID", value: "31 бит (нечётные — клиент, чётные — server push)" },
    { label: "Окно управления потоком по умолчанию", value: "65 535 байт на поток" },
    { label: "Типичный сервер: макс. параллельных потоков", value: "100 (рекомендация RFC 9113)" },
    { label: "SETTINGS фрейм согласуется", value: "при старте соединения, до первого запроса" },
    { label: "Стоимость RST_STREAM", value: "один фрейм — без разрыва TCP" },
  ]}
/>

## HPACK: сжатие заголовков

Если профилируешь загрузку HTTP/1.1-страницы, нередко видишь: только заголовки съедают десятки килобайт повторяющихся данных. У HTTP/2 есть конкретный ответ на это. HTTP/1.1 отправляет полные заголовки в каждом запросе — обычно 400–800 байт даже если меняется только путь. HTTP/2 использует **HPACK** (RFC 7541):

1. **Статическая таблица** — 61 предопределённая пара (имя, значение): `":method": "GET"`, `":status": "200"`, `"content-type": "text/html"` и т.д. Один байт индекса заменяет полный заголовок.
2. **Динамическая таблица** — записи добавляются per-connection по мере появления новых заголовков. Если первый запрос отправляет `user-agent: Mozilla/5.0`, второй может ссылаться на него 1–2 байтами индекса, передавая только дельту.

Результат: после первого запроса к домену, последующие запросы уменьшают накладные расходы на заголовки с ~400 байт до **~20–50 байт**. На странице с 50 подресурсами это экономит ~18 КБ заголовочных данных.

**Ограничение:** динамическая таблица упорядочена и общая для всех потоков. Если HEADERS-фрейм, добавляющий индекс 60, потерян, последующие HEADERS не могут декодировать ссылки на индекс 60 до повторной доставки. На TCP это незаметно. На QUIC это сломает независимость потоков — поэтому HTTP/3 заменил HPACK на **QPACK**.

## Управление потоком HTTP/2

Каждое HTTP/2-соединение имеет **connection-level** и **stream-level** управление потоком через WINDOW_UPDATE — получатель сообщает отправителю сколько байт готов принять. По умолчанию: 65 535 байт на поток, иногда повышается до 16 МиБ на высокопропускных API.

`SETTINGS_MAX_CONCURRENT_STREAMS` ограничивает число параллельных потоков (обычно 100). При достижении лимита клиент ставит запросы в очередь — частично теряя выгоду мультиплексирования.

<TraceScenario
  id="mid-trace-h2"
  pieceSlug="02-http2-multiplexing"
  lang="ru"
  scenario="Трассировка загрузки страницы браузером по HTTP/2 с нуля."
  steps={[
    { prompt: "Браузер завершает DNS, TCP, TLS. Что он анонсирует в ALPN?", reveal: "ALPN-расширение перечисляет [h2, http/1.1]. Сервер выбирает h2." },
    { prompt: "TLS-хендшейк завершён. Что браузер отправляет первым?", reveal: "Фрейм HTTP/2 SETTINGS с параметрами (макс. потоков, размер таблицы заголовков, начальное окно), затем HEADERS для запроса страницы." },
    { prompt: "Сервер отвечает. Как выглядит ответ на уровне фреймирования?", reveal: "HEADERS-фрейм со статусом 200 и заголовками ответа, затем один или несколько DATA-фреймов с HTML-телом, завершённые флагом END_STREAM." },
    { prompt: "Браузер разбирает HTML и обнаруживает 50 подресурсов. Что он делает?", reveal: "Открывает 50 потоков на том же TCP-соединении, отправляя 50 HEADERS-фреймов. Сервер перемежает DATA-фреймы по мере готовности." },
    { prompt: "Один TCP-пакет теряется. Что происходит со всеми 50 потоками?", reveal: "TCP блокирует байтовый поток до повторной передачи потерянного пакета. Все 50 HTTP/2-потоков встают — это HOL-блокировка на уровне TCP, которую HTTP/3 призван исправить." },
  ]}
/>

## Server Push: функция, которая провалилась

HTTP/2 ввёл **Server Push** — сервер мог отправлять ресурсы которые браузер ещё не запрашивал. Получив GET /index.html, сервер мог послать PUSH_PROMISE для /style.css, предполагая что он понадобится.

В теории — экономия одного round-trip. На практике — три причины провала:

1. **Сервер не знает что браузер уже закешировал.** Слепо пушит /style.css даже если у браузера свежая копия, тратя трафик.
2. **Push помогает только при первой навигации** — при тёплых загрузках браузер агрессивно кеширует и пушнутые ресурсы приходят незваными.
3. **Middleboxes и CDN не обрабатывали push корректно**, приводя к дублированным передачам.

Chrome убрал Server Push в 2022. RFC 9113 депрекейтит PUSH_PROMISE. **Современная замена: `103 Early Hints` + `<link rel="preload">`** — сервер отправляет 103 информационный ответ с критическими подресурсами, браузер сам решает что загружать (проверяя кеш первым).

<Misconception
  id="r-h2-push-dead"
  title="Server Push — НЕ выигрыш производительности"
>
Server Push хорошо выглядел в бенчмарках на холодном кеше. В продакшене — с CDN, прокси и тёплыми кешами браузера — серверское предположение о нужных ресурсах ошибается достаточно часто, чтобы Push стал проигрышем. Используйте 103 Early Hints и preload link-заголовки.
</Misconception>

<Quiz
  id="mid-quiz-1"
  pieceSlug="02-http2-multiplexing"
  lang="ru"
  question="Почему Server Push фактически мёртв в 2026?"
  choices={[
    { label: "Серверы не могут шифровать push-фреймы", misconception: "Шифрование работает прекрасно; проблема операционная, не криптографическая." },
    { label: "Сервер не знает что клиент закешировал — трата трафика и дублирование; 103 Early Hints + preload выиграли архитектурную дискуссию", correct: true },
    { label: "Слишком быстро для браузеров", misconception: "Производительность никогда не была ограничением Server Push." },
    { label: "Ломает TLS", misconception: "TLS не зависит от семантики push." },
  ]}
/>

<Quiz
  id="mid-quiz-2"
  pieceSlug="02-http2-multiplexing"
  lang="ru"
  question="HTTP/2 мультиплексирование устраняет HOL-блокировку — верно или нет?"
  choices={[
    { label: "Верно — несколько потоков означает отсутствие ожидания", misconception: "HTTP/2 устраняет HOL на HTTP-уровне, но TCP снизу по-прежнему блокируется: один потерянный пакет тормозит все потоки." },
    { label: "Нет — HTTP/2 устраняет HOL на уровне приложения, но TCP-уровень по-прежнему блокируется при потере пакетов", correct: true },
    { label: "Верно — HPACK убирает блокировку", misconception: "HPACK — сжатие заголовков, не связанное с HOL-блокировкой." },
    { label: "Нет — HTTP/2 хуже HTTP/1.1 по HOL", misconception: "HTTP/2 лучше на HTTP-уровне; оставшаяся проблема на уровне TCP — не введена HTTP/2." },
  ]}
/>

<DragOrder
  id="mid-drag-1"
  pieceSlug="02-http2-multiplexing"
  lang="ru"
  prompt="Расставьте события при загрузке https://example.com по HTTP/2:"
  items={[
    "DNS-резолвинг example.com",
    "TCP-хендшейк",
    "TLS 1.3 хендшейк (ALPN выбирает h2)",
    "Обмен SETTINGS-фреймами HTTP/2",
    "HEADERS-фрейм с запросом страницы",
    "DATA-фреймы с HTML-телом",
    "50 HEADERS-фреймов для подресурсов",
    "Перемежающиеся DATA-фреймы всех 50 потоков",
  ]}
/>

<TraceScenario
  id="mid-trace-hol"
  pieceSlug="02-http2-multiplexing"
  lang="ru"
  scenario="Диагностика: почему HTTP/2 API-сервис имеет большую задержку на мобильных сетях, чем HTTP/1.1?"
  steps={[
    { prompt: "Шаг 1: какая метрика сети сразу скажет есть ли потеря пакетов?", reveal: "Процент потери пакетов — через tcpdump retransmits или метрики оператора. На мобиле 0.5–2% нормально; на оптике <0.1%." },
    { prompt: "Шаг 2: 1.2% потерь на мобильном пути. API делает 50 параллельных запросов на страницу. Что происходит в HTTP/2?", reveal: "Все 50 запросов разделяют одно TCP-соединение. Один потерянный пакет блокирует байтовый поток — все 50 HTTP/2-потоков встают до повторной передачи (~100 мс). Загрузка страницы заметно стопорится." },
    { prompt: "Шаг 3: тот же сценарий по HTTP/1.1 с 6 параллельными соединениями. Что происходит?", reveal: "6 независимых TCP-соединений, каждое со своим восстановлением потерь. Потерянный пакет на соединении #1 тормозит только запросы на нём (~8 запросов). Соединения #2–6 продолжают. Стопорится ~17% работы, не 100%." },
    { prompt: "Шаг 4: стоит ли откатиться к HTTP/1.1 на мобиле?", reveal: "Не обязательно. Лучшее исправление — HTTP/3: независимые QUIC-потоки полностью избегают TCP HOL. Если HTTP/3 недоступен (UDP заблокирован), HTTP/1.1 приемлем для путей с высокими потерями." },
  ]}
/>

</Explanation>

<SequenceDiagram
  label="Мультиплексирование HTTP/2: клиент открывает два потока поверх одного TCP-соединения, и сервер перемежает DATA-фреймы обоих потоков обратно"
  hue="var(--d-network)"
  actors={["Клиент", "Сервер"]}
  messages={[
    { from: "Клиент", to: "Сервер", label: "HEADERS stream 1 (GET /app.js)" },
    { from: "Клиент", to: "Сервер", label: "HEADERS stream 3 (GET /style.css)" },
    { from: "Сервер", to: "Клиент", label: "DATA stream 1 (chunk)" },
    { from: "Сервер", to: "Клиент", label: "DATA stream 3 (chunk)" },
    { from: "Сервер", to: "Клиент", label: "DATA stream 1 (chunk, END_STREAM)" },
  ]}
  caption="Оба запроса уходят не дожидаясь друг друга; сервер перемежает DATA-фреймы потока 1 и потока 3 на одном TCP-соединении. Порядок внутри потока сохраняется; порядок между потоками — выбор сервера."
/>

<KeyTakeaway>HTTP/2 мультиплексирует потоки на одном TCP-соединении, сокращая 6–8 хендшейков до 1 и используя HPACK для ~80–90% уменьшения заголовков; оставшаяся слабость — HOL-блокировка TCP, которую HTTP/3 и QUIC устраняют. Server Push мёртв; вместо него — 103 Early Hints.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="02-http2-multiplexing-retrieval"
  lang="ru"
  questions={[
    { q: "Объясните почему HTTP/2 мультиплексирование НЕ решает полностью HOL-блокировку.", a: "HTTP/2 мультиплексирует потоки поверх одного TCP-соединения. Потоки на уровне приложения независимы, но TCP снизу доставляет байты строго по порядку. Когда теряется один TCP-сегмент, TCP буферизирует все последующие байты до повторной передачи — каждый HTTP/2-поток на этом соединении встаёт. На линии с малыми потерями это незаметно. На лоссовой сети (мобиле, спутнике, 1%+ потерь) это ощутимо. HTTP/3 поверх QUIC исправляет: потеря пакета затрагивает только потоки, чьи данные были в этом пакете." },
    { q: "Что такое HPACK и каково его главное ограничение?", a: "HPACK (RFC 7541) сжимает HEADERS-фреймы HTTP/2 статической таблицей из 61 общего заголовка и динамической таблицей per-connection. После первого запроса к домену накладные расходы на заголовки падают с ~400 байт до ~20–50 байт. Ограничение: динамическая таблица упорядочена и общая для всех потоков. Потеря HEADERS-фрейма добавляющего индекс 60 блокирует все последующие ссылки на индекс 60. На TCP это незаметно; на QUIC-потоках сломало бы независимость — поэтому HTTP/3 использует QPACK." },
    { q: "Что заменило Server Push HTTP/2 и почему это работает лучше?", a: "103 Early Hints + <link rel='preload'>: сервер отправляет 103 информационный ответ с критическими подресурсами до прихода основного ответа 200. Браузер получает подсказку и сам решает загружать ли каждый ресурс — проверяя кеш первым. Это лучше Server Push: браузер имеет выбор пропустить уже кешированные ресурсы; сервер не угадывает вслепую. CDN и прокси корректно обрабатывают 103 Early Hints, в отличие от push-фреймов." },
  ]}
/>

<Recap lang="ru">
HTTP/2 ввёл мультиплексирование потоков на одном TCP-соединении, заменив шесть-соединений HTTP/1.1. Фреймы (HEADERS, DATA, RST_STREAM, SETTINGS, WINDOW_UPDATE) — единица передачи; потоки свободно чередуются на одном соединении. HPACK сокращает размер заголовков на 80–90% через статическую и per-connection динамическую таблицу. Управление потоком через WINDOW_UPDATE предотвращает переполнение буферов. Server Push удалён из Chrome в 2022 — сервер не знает что браузер закешировал; 103 Early Hints заменил его. Оставшаяся открытая проблема: один потерянный TCP-пакет тормозит каждый поток — HOL-блокировка TCP, которую HTTP/3 решает с QUIC. Теперь, когда страница тормозит именно на мобильной сети с потерями, знаешь: проверь, не виновата ли HOL-блокировка HTTP/2 на уровне TCP, — и стоит ли переходить на HTTP/3.
</Recap>
