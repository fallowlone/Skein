---
concepts:
- dns
- recursive-resolver
- root-server
- tld-server
- authoritative-nameserver
- a-record
- ttl
- dns-cache
deepensInto:
- 02-the-resolver-walk
estMin: 8
lang: ru
lessonType: topic
level: junior
order: 1
prereqs:
- 03-tcp-handshake/06-bbr-and-production-ops
slug: 01-dns-what-it-does
sources:
- https://datatracker.ietf.org/doc/html/rfc1034
- https://datatracker.ietf.org/doc/html/rfc1035
- https://hpbn.co/dns-anatomy/
spiral:
- statefulness
status: ready
summary: DNS превращает человекочитаемые имена в IP-адреса через иерархию серверов
  — телефонная книга, от которой зависит весь интернет.
title: 'DNS: что делает и зачем существует'
track: networking
unit: 04-dns-resolution
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import MetaphorComplete from "~/components/pedagogy/MetaphorComplete.astro";
import NumbersCard from "~/components/layout/NumbersCard.astro";
import SequenceDiagram from "~/components/diagram/SequenceDiagram.astro";
import Infographic from "~/components/diagram/Infographic.astro";
import lookupLatency from "./infographics/lookup-latency.svg?raw";

<Hook lang="ru">
Вы вводите `google.com`. Браузер не знает, где находится Google — он знает имена, но не числа. Прежде чем открыть TCP-соединение или отправить HTTP-запрос, браузеру нужен IP-адрес. Этот поиск — DNS, и он происходит молча при каждой загрузке страницы. К концу урока вы будете точно знать, какие серверы отвечают на этот запрос, зачем иерархия состоит из трёх уровней и что ломается, когда любой из них работает медленно.
</Hook>

<Crux>DNS превращает человекочитаемое имя в IP-адрес, опрашивая иерархию серверов — root, TLD, авторитативный — и кешируя ответы для быстроты.</Crux>

<Explanation>

## Что делает DNS одним предложением

DNS (Domain Name System) переводит понятное человеку имя, например `google.com`, в числовой IP-адрес (`142.250.185.46`), необходимый сетевому уровню для маршрутизации пакетов. Без DNS пришлось бы запоминать десятки IP-адресов на каждый сервис и обновлять их каждый раз при переезде сервера.

## Дерево серверов

Когда lookup зависает или тайм-аутится, понимание того, какой уровень виноват — root, TLD или авторитативный — превращает пятиминутное расследование в пятисекундное. DNS — глобальная иерархия. Три уровня обрабатывают каждый запрос:

- **Корневые серверы** — 13 именованных серверов (A–M), каждый реплицирован на сотни точек через anycast. Они не хранят записи доменов; они знают, к какому TLD-серверу обратиться дальше.
- **TLD-серверы** — по одному набору на каждый домен верхнего уровня (`.com`, `.org`, `.uk`, `.io` и т. д.). Они знают, какой авторитативный сервер владеет конкретным доменом.
- **Авторитативные nameserver-ы** — владеют зонными файлами отдельных доменов (`google.com`, `example.co.uk`). Они возвращают финальный ответ.

<NumbersCard
  id="card-dns-tree"
  title="DNS-дерево кратко"
  data-lesson-visual
  stats={[
    { label: "Корневых серверов (имена)", value: "13 (A–M)" },
    { label: "Инстансов root (anycast)", value: "1 000+" },
    { label: "TLD-серверов", value: "по одному набору на TLD (.com, .org, …)" },
    { label: "Авторитативных серверов", value: "по одному на зарегистрированный домен" },
    { label: "DNS-протокол", value: "UDP/TCP, порт 53" },
    { label: "Задержка холодного запроса", value: "30–100 мс" },
    { label: "Задержка тёплого запроса (кеш)", value: "менее 1 мс" },
  ]}
/>

## Метафора справочной

Представьте, что вы хотите позвонить в ресторан, зная только его название. Вы звоните в справочную (рекурсивный резолвер). Оператор не знает номер, но умеет его найти. Она звонит в центральный офис (корневой сервер), затем в региональный справочник (TLD-сервер), а оттуда — на стойку самого ресторана (авторитативный сервер). Стойка даёт номер; оператор передаёт его вам и записывает. Следующий позвонивший с тем же вопросом получает ответ мгновенно из кеша.

## Диалог резолюции

Браузер пользователя отправляет запрос настроенному рекурсивному резолверу. Если у резолвера нет кешированного ответа, он обходит дерево:

1. **Браузер спрашивает резолвер:** «Какой IP у `example.com`?»
2. **Резолвер спрашивает root:** «Кто отвечает за `.com`?» Root: «Спроси TLD-сервер `.com`.»
3. **Резолвер спрашивает TLD `.com`:** «Кто отвечает за `example.com`?» TLD: «Спроси этот авторитативный сервер.»
4. **Резолвер спрашивает авторитативный:** «IP для `example.com`?» Авторитативный: «`203.0.113.10`, TTL=300.»
5. **Резолвер возвращает IP браузеру**, кеширует ответ на 300 секунд.

На следующий запрос в течение этих 300 секунд резолвер пропускает шаги 2–4 и отвечает из кеша. Вместе эти пять шагов означают: один медленный авторитативный сервер добавляет 30–100 мс к каждой холодной загрузке — но стоит записи попасть в кеш, вся эта работа исчезает за субмиллисекундным ответом.

<Infographic
  svg={lookupLatency}
  label="Столбчатая диаграмма сравнивает задержку DNS-запроса: холодный обход root, TLD и авторитативного серверов стоит около 100 мс, а попадание в тёплый кэш возвращает ответ примерно за 1 мс."
  caption="Холодный обход трёх уровней стоит 30–100 мс; попадание в тёплый кэш — менее 1 мс. Этот разрыв в ~100 раз и есть вся причина, по которой DNS кеширует ответы на каждом уровне."
  hue="var(--d-infra)"
/>

<DragOrder
  id="jr-drag-1"
  pieceSlug="01-dns-what-it-does"
  lang="ru"
  prompt="Расставьте шаги холодного lookup для example.com:"
  items={[
    "Браузер спрашивает рекурсивный резолвер за example.com",
    "Резолвер спрашивает root: кто отвечает за .com?",
    "Root отвечает перенаправлением к TLD .com",
    "Резолвер спрашивает TLD .com: кто отвечает за example.com?",
    "TLD отвечает перенаправлением к авторитативному серверу",
    "Резолвер спрашивает авторитативный: IP для example.com?",
    "Авторитативный отвечает A-записью",
    "Резолвер кеширует ответ и возвращает IP браузеру",
  ]}
/>

## Один сценарий от начала до конца

Вы кликаете на ссылку. Под капотом браузер первым делом делает DNS — чтобы узнать IP. Получив IP, он открывает TCP-соединение на этот адрес, затем добавляет TLS поверх него и наконец отправляет HTTP-запрос. Каждая новая вкладка браузера повторяет шаг DNS, если ответ не закеширован. DNS — всегда первый шаг интернет-соединения.

<Quiz
  id="jr-quiz-1"
  pieceSlug="01-dns-what-it-does"
  lang="ru"
  question="Что возвращает DNS при стандартном запросе домена?"
  choices={[
    { label: "HTML-содержимое сайта", misconception: "HTML запрашивает HTTP уже после DNS. DNS возвращает только адрес." },
    { label: "IP-адрес (запись A или AAAA)", correct: true },
    { label: "Зашифрованный сессионный ключ", misconception: "Ключами занимается TLS, а не DNS." },
    { label: "Список роутеров между вами и пунктом назначения", misconception: "Маршрутизация — задача IP-уровня; DNS возвращает конечный адрес." },
  ]}
/>

<Quiz
  id="jr-quiz-2"
  pieceSlug="01-dns-what-it-does"
  lang="ru"
  question="Зачем кеш существует на каждом уровне DNS?"
  choices={[
    { label: "Чтобы экономить дисковое место на авторитативных серверах", misconception: "Кеширование снижает задержку и нагрузку, а не экономит диск." },
    { label: "Большинство имён запрашивается многократно — кеш избегает полного обхода дерева каждый раз", correct: true },
    { label: "Чтобы скрыть ответ от сетевых наблюдателей", misconception: "Кеш возвращает тот же ответ; он ничего не скрывает." },
    { label: "Потому что авторитативные серверы обычно офлайн", misconception: "Они работают круглосуточно. Кеширование — оптимизация, а не обходной путь." },
  ]}
/>

<MetaphorComplete
  id="jr-metaphor-1"
  pieceSlug="01-dns-what-it-does"
  lang="ru"
  setup="DNS — это глобальная _______, переводящая понятные человеку имена в IP-адреса."
  accepted={["телефонная книга", "телефонный справочник", "справочник", "адресная книга", "записная книжка"]}
  canonical="телефонная книга (или справочник)"
  explanation="DNS — телефонная книга интернета: на входе имя, на выходе номер. Резолвер ищет; авторитативный сервер — источник истины."
/>

<Inset kind="why" lang="ru">
**Почему 13 корневых серверов, а не один.** DNS был спроектирован в 1987 году, когда интернет был маленьким. Ограничение в 13 серверов пришло из лимита UDP в 512 байт — именно 13 IPv4-адресов влезают в один DNS-ответ. Сегодня каждый «корневой сервер» — это сотни машин, распределённых по всем континентам через anycast: ваш запрос к root достигает ближайшего инстанса за обычно менее 5 мс, и вы не знаете, какая именно машина ответила.
</Inset>

</Explanation>

<SequenceDiagram
  label="Холодный DNS-запрос для example.com: рекурсивный резолвер по очереди спрашивает Корневой, TLD и Авторитетный серверы, каждый направляет его на шаг глубже, затем резолвер возвращает IP браузеру"
  hue="var(--d-network)"
  actors={["Браузер", "Резолвер", "Корневой", "TLD", "Авторитетный"]}
  messages={[
    { from: "Браузер", to: "Резолвер", label: "IP для example.com?" },
    { from: "Резолвер", to: "Корневой", label: "кто отвечает за .com?" },
    { from: "Корневой", to: "Резолвер", label: "спроси TLD .com" },
    { from: "Резолвер", to: "TLD", label: "кто отвечает за example.com?" },
    { from: "TLD", to: "Резолвер", label: "спроси авторитетный" },
    { from: "Резолвер", to: "Авторитетный", label: "IP для example.com?" },
    { from: "Авторитетный", to: "Резолвер", label: "203.0.113.10 TTL=300" },
    { from: "Резолвер", to: "Браузер", label: "203.0.113.10 (кеш)" },
  ]}
  caption="Резолвер обходит дерево вниз — Корневой → TLD → Авторитетный — собирая перенаправление на каждом шаге, затем кеширует итоговый ответ, чтобы следующий такой же запрос в рамках TTL пропустил весь обход."
/>

<KeyTakeaway>DNS переводит имена в IP через трёхуровневую иерархию — root, TLD, авторитативный — причём рекурсивный резолвер делает обход дерева за вас и кеширует ответы по TTL, чтобы не повторять его при каждом запросе.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="01-dns-what-it-does-retrieval"
  lang="ru"
  questions={[
    { q: "Почему почти все DNS-запросы ощущаются мгновенными после первого?", a: "Потому что каждый уровень кеширует ответы. Как только резолвер узнал IP для имени, он возвращает закешированный ответ следующим запросчикам вместо повторного обхода дерева. Первый запрос платит полную задержку (30–100 мс); последующие запросы в рамках окна TTL практически бесплатны." },
    { q: "В чём роль рекурсивного резолвера и чем он отличается от авторитативного сервера?", a: "Рекурсивный резолвер обходит дерево от имени клиента: спрашивает root, TLD и авторитативный серверы по очереди, кеширует каждый ответ и возвращает итоговый IP. Авторитативный сервер владеет зоной и отвечает только на запросы по именам в этой зоне — он не рекурсирует. Эти роли никогда не смешиваются в продакшене." },
    { q: "Назовите три уровня иерархии DNS по порядку.", a: "Корневые серверы (знают, к какому TLD обратиться), TLD-серверы (знают, к какому авторитативному обратиться), авторитативные серверы (возвращают итоговый IP из зонных файлов)." },
  ]}
/>

<Recap lang="ru">
DNS — глобально распределённая иерархия, которая сопоставляет человекочитаемые имена с IP-адресами. Когда браузеру нужно подключиться к новому домену, он спрашивает рекурсивный резолвер. Резолвер проходит от корневых серверов через TLD-серверы к авторитативному nameserver-у домена, собирая перенаправления на каждом шаге. Итоговый ответ — A-запись с IPv4-адресом и TTL — возвращается браузеру и кешируется на каждом уровне. TTL определяет, как долго кеши хранят ответ перед повторным запросом. Тёплый кеш делает DNS практически мгновенным; холодный обход занимает 30–100 мс через три round-trip. DNS происходит до TCP, до TLS и до HTTP — это первый шаг каждого интернет-соединения. Теперь, когда вы встретите медленную загрузку страницы при быстром TTFB на повторных запросах, вы сразу проверите — не холодный ли обход DNS заплатил штраф за первый запрос, а не сам сервер.
</Recap>
