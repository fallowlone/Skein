---
concepts:
- dns-referral
- authority-section
- additional-section
- glue-record
- a-record
- aaaa-record
- cname
- mx-record
- txt-record
- ns-record
- soa-record
- edns0
- axfr
- ixfr
- rd-bit
- tc-flag
- stub-resolver
- open-resolver
deepensInto:
- 03-ttl-and-caching
estMin: 14
lang: ru
lessonType: topic
level: middle
order: 2
prereqs:
- 01-dns-what-it-does
slug: 02-the-resolver-walk
sources:
- https://datatracker.ietf.org/doc/html/rfc1034
- https://datatracker.ietf.org/doc/html/rfc1035
- https://datatracker.ietf.org/doc/html/rfc6891
- https://datatracker.ietf.org/doc/html/rfc2308
- https://hpbn.co/dns-anatomy/
spiral:
- statefulness
status: ready
summary: Как рекурсивный резолвер следует перенаправлениям от root до авторитативного,
  что содержат секции DNS-сообщения и почему отсутствие glue ломает всё.
title: 'Обход резолвера: перенаправления, типы записей и glue'
track: networking
unit: 04-dns-resolution
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import TraceScenario from "~/components/pedagogy/TraceScenario.astro";
import NumbersCard from "~/components/layout/NumbersCard.astro";
import SequenceDiagram from "~/components/diagram/SequenceDiagram.astro";
import Infographic from "~/components/diagram/Infographic.astro";
import referralVsAnswer from "./infographics/referral-vs-answer.svg?raw";

<Hook lang="ru">
Резолвер, спрашивающий про `cdn.example.co.uk`, не получает ответ от корневого сервера. Он получает перенаправление — «спроси TLD `.uk`». Потом ещё одно — «спроси авторитативный сервер `example.co.uk`». Только на третьем запросе он получает реальный IP-адрес. Понять, что передаётся в этих ответах-перенаправлениях, — значит уметь отлаживать DNS, а не просто запускать `dig` и надеяться.
</Hook>

<Crux>Перенаправление несёт NS в Authority и glue A/AAAA в Additional — вместе они ведут резолвер к следующему шагу без циклической зависимости.</Crux>

<Explanation>

## Итеративные vs рекурсивные запросы

Зачем это важно на практике? Потому что сервер, стирающий эту границу — принимающий рекурсивные запросы от внешних клиентов, — превращается в бесплатный усилитель для DDoS-атак. Запросы резолвера к root, TLD и авторитативным серверам — **итеративные**: бит RD (Recursion Desired) очищен. Эти серверы возвращают только то, что знают, плюс перенаправление — они никогда не обходят дерево за клиента. Только исходный запрос клиента к резолверу имеет RD=1, прося резолвер сделать полный обход.

Это различие важно для безопасности. Авторитативный сервер, принимающий RD=1 от внешних клиентов — **открытый резолвер** — может быть использован как DNS-reflection-усилитель: атакующий отправляет запросы со spoof-адресом источника, и сервер шлёт большие ответы жертве. Продакшен-авторитативы (Route 53, Cloudflare DNS) никогда не рекурсируют для внешних клиентов.

## Секции DNS-сообщения

DNS-сообщение имеет четыре секции:

| Секция | Назначение |
|---|---|
| **Question** | Имя запроса, тип (A, MX, …), класс (IN) |
| **Answer** | Записи, прямо отвечающие на вопрос |
| **Authority** | NS-записи, указывающие на следующий nameserver |
| **Additional** | Заранее собранные A/AAAA для имён из Authority |

Перенаправление не содержит ничего в Answer, зато содержит Authority + Additional. Бит AA (Authoritative Answer) устанавливается только когда отвечающий сервер владеет зоной — перенаправление от TLD-сервера имеет AA=0.

<Infographic
  svg={referralVsAnswer}
  label="Сравнение двух форм DNS-ответа бок о бок. Реферал (от root или TLD) имеет пустую секцию Answer, NS-записи в Authority, glue A/AAAA в Additional и бит AA = 0. Авторитетный ответ имеет заполненную секцию Answer, опциональную Authority, часто пустую Additional и бит AA = 1."
  caption="Один формат DNS-сообщения, две роли: реферал заполняет Authority + Additional с AA=0, указывая дальше; только владелец зоны заполняет Answer с AA=1."
  hue="var(--d-infra)"
/>

## Цепочка перенаправлений для `cdn.example.co.uk`

1. **Резолвер → root**: спрашивает `cdn.example.co.uk`. Root не знает ответа. Возвращает Authority: `ns.nic.uk` (NS-запись для `.uk`) + Additional: `ns.nic.uk A 193.0.0.1` (glue). Секция Answer пуста.
2. **Резолвер → TLD `.uk`**: спрашивает то же QNAME. TLD возвращает Authority: `ns1.example.co.uk` (NS для `example.co.uk`) + Additional: `ns1.example.co.uk A 198.51.100.4` (glue). Answer по-прежнему пуст.
3. **Резолвер → авторитативный**: спрашивает то же QNAME. Авторитативный возвращает Answer: `cdn.example.co.uk A 203.0.113.10 TTL=300`. Бит AA установлен.

Все три прыжка означают: резолвер никогда не угадывает адрес следующего nameserver-а — он всегда получает его явно через glue. Без glue на первом шаге второй шаг — тупик.

<TraceScenario
  id="mid-trace-1"
  pieceSlug="02-the-resolver-walk"
  lang="ru"
  scenario="Трассируйте холодный DNS-lookup для cdn.example.co.uk из чистого кеша."
  steps={[
    { prompt: "Резолвер ничего не кешировал. Первый шаг?", reveal: "Выбрать корневой сервер (резолвер кеширует root NS + A при старте) и отправить итеративный запрос (RD=0) для cdn.example.co.uk." },
    { prompt: "Root отвечает. Что получает резолвер?", reveal: "Перенаправление: NS-записи для .uk в секции Authority, glue A-записи для этих NS в секции Additional. Секция Answer пуста." },
    { prompt: "Резолвер запрашивает TLD .uk. О чём спрашивает?", reveal: "Тот же QNAME cdn.example.co.uk, RD=0. TLD — итеративный, возвращает только то, что знает." },
    { prompt: "TLD отвечает. Что теперь?", reveal: "Ещё одно перенаправление: NS-записи для example.co.uk плюс glue. Резолвер всё ещё не имеет итогового IP." },
    { prompt: "Резолвер запрашивает авторитативный example.co.uk. Что получает?", reveal: "Секция Answer: A-запись cdn.example.co.uk 203.0.113.10 TTL=300. Бит AA установлен. Это финальный ответ." },
    { prompt: "Следующий такой же запрос в рамках TTL?", reveal: "Кеш. Возвращается немедленно, без upstream-запросов." },
  ]}
/>

## Glue-записи и циклическая зависимость

Когда зона делегирует управление nameserver-у **внутри своей же зоны** (NS для `example.com` — это `ns1.example.com`), родительская зона обязана включить A-запись для `ns1.example.com` как **glue** в ответ-перенаправление. Без glue резолвер не может найти nameserver, не разрешив сначала `example.com` — порочный круг.

Отсутствие glue вызывает нерегулярные SERVFAIL или циклы перенаправлений. NS-записи вне зоны (например, `ns1.somednsprovider.org`) не требуют glue — резолвер запросит их IP у другой зоны.

<NumbersCard
  id="card-dns-record-types"
  title="Распространённые типы DNS-записей"
  data-lesson-visual
  stats={[
    { label: "A", value: "IPv4-адрес" },
    { label: "AAAA", value: "IPv6-адрес" },
    { label: "CNAME", value: "псевдоним → другое имя (не IP)" },
    { label: "MX", value: "hostname почтового сервера + приоритет" },
    { label: "TXT", value: "свободный текст — SPF, DKIM, верификация" },
    { label: "NS", value: "делегирование — nameserver зоны" },
    { label: "SOA", value: "авторитет зоны: serial, refresh, мин. TTL" },
    { label: "CAA", value: "кому из CA разрешено выдавать сертификаты" },
    { label: "HTTPS/SVCB", value: "ALPN + ECH-ключи (RFC 9460)" },
  ]}
/>

## CNAME и правило apex

CNAME-запись указывает имя на другое имя: `www CNAME example.com`. Резолвер затем разрешает цель. **CNAME запрещён на вершине зоны** (bare domain, например `example.com`) согласно RFC 1034, потому что SOA и NS тоже должны находиться на вершине, а CNAME заменил бы все другие типы записей для этого имени. CDN-специфичные ALIAS/ANAME и новая запись HTTPS (RFC 9460) обходят это ограничение.

<Quiz
  id="mid-quiz-1"
  pieceSlug="02-the-resolver-walk"
  lang="ru"
  question="Откуда рекурсивный резолвер берёт информацию о следующем шаге при получении перенаправления от TLD-сервера?"
  choices={[
    { label: "Из секции Answer", misconception: "В перенаправлении секция Answer пуста. Данные о следующем шаге находятся в Authority + Additional." },
    { label: "Секция Authority (NS-записи) плюс секция Additional (glue A/AAAA)", correct: true },
    { label: "Только из флагов заголовка", misconception: "Флаги указывают на рекурсию и DNSSEC, а не на цель перенаправления." },
    { label: "Из повторно переданной секции Question", misconception: "Question отражает запрос; она никогда не содержит цели перенаправления." },
  ]}
/>

<Quiz
  id="mid-quiz-2"
  pieceSlug="02-the-resolver-walk"
  lang="ru"
  question="Почему CNAME запрещён на вершине зоны (example.com) согласно RFC 1034?"
  choices={[
    { label: "Современный DNS не рекомендует его, но RFC 1034 не запрещает", misconception: "RFC 1034 явно запрещает CNAME на apex." },
    { label: "SOA и NS обязаны находиться на вершине — CNAME заменил бы все другие типы записей для этого имени", correct: true },
    { label: "У CNAME слишком низкий TTL для apex-записи", misconception: "TTL не имеет отношения к этому правилу. Настоящая причина — конфликт с SOA/NS." },
    { label: "IPv6 требует AAAA, а не CNAME", misconception: "AAAA — адресная запись. Ни AAAA, ни CNAME не являются причиной этого правила." },
  ]}
/>

## UDP, TCP и EDNS0

DNS по умолчанию использует UDP, порт 53. Если ответ превышает размер UDP-буфера, сервер устанавливает флаг **TC (truncation)** и резолвер повторяет запрос через TCP, порт 53. TCP также обязателен для зонных трансферов (AXFR/IXFR). Файервол, блокирующий TCP/53 при разрешённом UDP/53, молча ломает DNSSEC-ответы и зонную репликацию.

EDNS0 (RFC 6891) добавляет псевдозапись OPT для согласования больших UDP-буферов (обычно 1232 или 4096 байт) и передачи расширений: бит DNSSEC OK (DO), ECS (подсеть клиента), EDNS Cookies. Каждый современный резолвер рекламирует EDNS0; без него DNSSEC-ответы будут усечены.

<DragOrder
  id="mid-drag-1"
  pieceSlug="02-the-resolver-walk"
  lang="ru"
  prompt="Расставьте шаги при открытии https://shop.example.com из чистого кеша:"
  items={[
    "Браузер спрашивает OS-резолвер про shop.example.com",
    "OS-резолвер перенаправляет запрос к настроенному upstream (ISP или 1.1.1.1)",
    "Резолвер проходит root → .com → авторитативный example.com",
    "Резолвер возвращает A-запись (IP) браузеру",
    "Браузер выполняет TCP-рукопожатие к этому IP",
    "Браузер выполняет TLS-рукопожатие поверх TCP",
    "Браузер отправляет HTTPS-запрос и получает страницу",
  ]}
/>

<Inset kind="why" lang="ru">
**Stub-резолвер vs полный рекурсивный резолвер.** Операционная система запускает stub-резолвер — тонкий клиент, который перенаправляет запросы к настроенному upstream и кеширует ненадолго. Linux использует `systemd-resolved` или `nscd`; macOS — `mDNSResponder`; Windows — DNS Client service. Stub почти ничего не делает сам. Полный рекурсивный резолвер — тот, что обходит root→TLD→авторитативный — находится upstream (ваш роутер, ISP или публичный резолвер 1.1.1.1). Браузеры часто обходят OS-stub напрямую: Chrome's Async DNS Resolver и Firefox's TRR запрашивают DoH напрямую. Сброс кеша OS-stub (`sudo systemd-resolve --flush-caches`) не сбрасывает кеш upstream-резолвера.
</Inset>

</Explanation>

<SequenceDiagram
  label="Цепочка перенаправлений для cdn.example.co.uk: Корневой и TLD .uk каждый возвращают перенаправление (NS в Authority плюс glue A в Additional), и только Авторитетный сервер возвращает ответ с установленным битом AA"
  hue="var(--d-network)"
  actors={["Резолвер", "Корневой", "TLD", "Авторитетный"]}
  messages={[
    { from: "Резолвер", to: "Корневой", label: "QNAME, RD=0" },
    { from: "Корневой", to: "Резолвер", label: "перенаправление: NS .uk + glue (Answer пуст)" },
    { from: "Резолвер", to: "TLD", label: "QNAME, RD=0" },
    { from: "TLD", to: "Резолвер", label: "перенаправление: NS example.co.uk + glue" },
    { from: "Резолвер", to: "Авторитетный", label: "QNAME, RD=0" },
    { from: "Авторитетный", to: "Резолвер", label: "Answer: A 203.0.113.10, AA=1" },
  ]}
  caption="Каждое перенаправление несёт NS-записи в секции Authority и glue A/AAAA в Additional — направляя резолвер на хоп глубже без циклической зависимости. Только ответ Авторитетного имеет непустую секцию Answer и установленный бит AA."
/>

<KeyTakeaway>DNS-перенаправление несёт NS-записи в Authority и glue A/AAAA в Additional — резолвер следует этой цепочке root→TLD→авторитативный. Отсутствие glue на in-bailiwick NS создаёт циклическую зависимость и вызывает SERVFAIL. CNAME запрещён на apex зоны, потому что там должны сосуществовать SOA и NS.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="02-the-resolver-walk-retrieval"
  lang="ru"
  questions={[
    { q: "В чём операционное различие между авторитативным сервером и рекурсивным резолвером?", a: "Авторитативный сервер владеет зонами и отвечает из локальных зонных файлов; он возвращает перенаправления для имён вне своей авторитарности и никогда не рекурсирует за клиентов. Рекурсивный резолвер ничем не владеет — он обходит дерево от root вниз, следует перенаправлениям, кеширует результаты и возвращает итоговый ответ клиентам. Роли никогда не смешиваются в продакшене: смешанный сервер — открытый резолвер, известный вектор DNS-reflection-амплификации." },
    { q: "Почему glue-записи обязательны для in-bailiwick nameserver-ов?", a: "Когда зона делегирует управление серверу ns1.example.com, а сам ns1.example.com находится внутри example.com, резолвер не может найти IP ns1 без предварительного разрешения example.com — порочный круг. Родительская зона разрывает его, включая A-запись ns1 как glue в ответ-перенаправление. Без glue резолвер зацикливается или возвращает SERVFAIL." },
    { q: "Почему DNS переходит с UDP на TCP для некоторых запросов?", a: "DNS использует UDP порт 53 по умолчанию. Когда ответ превышает буфер EDNS0 (или 512 байт без него), сервер устанавливает флаг TC (truncation). Резолвер повторяет запрос через TCP. Зонные трансферы (AXFR/IXFR) всегда используют TCP. Блокировка TCP/53 молча ломает DNSSEC-ответы и зонную репликацию." },
  ]}
/>

<Recap lang="ru">
Рекурсивный резолвер обходит DNS-дерево итеративно, собирая перенаправления на каждом уровне. Каждое перенаправление несёт NS-записи в секции Authority плюс glue A/AAAA в Additional — вместе они разрывают любую циклическую зависимость для in-bailiwick nameserver-ов. Бит AA (Authoritative Answer) устанавливается только когда отвечающий сервер владеет зоной. Типы DNS-записей выходят далеко за рамки A-записей: CNAME создаёт псевдонимы, MX направляет почту, TXT несёт SPF/DKIM, NS делегирует зоны, SOA определяет авторитет зоны. CNAME запрещён на apex зоны, поскольку там должны сосуществовать SOA и NS. DNS по умолчанию использует UDP; большие ответы (DNSSEC, зонные трансферы) переходят на TCP. EDNS0 расширяет UDP-буферы и передаёт DNSSEC-флаги. Теперь, когда вы увидите SERVFAIL или цикл перенаправлений в выводе dig, проверяйте glue первым делом — отсутствующая A-запись in-bailiwick nameserver-а в секции Additional чаще всего оказывается молчаливым виновником.
</Recap>
