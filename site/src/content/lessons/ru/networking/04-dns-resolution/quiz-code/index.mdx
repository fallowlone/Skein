---
concepts:
- dig-trace
- referral-glue
- dnssec-diagnosis
- ttl-reading
- whoami-anycast
estMin: 14
lang: ru
level: senior
order: 9
prereqs: []
slug: quiz-code
sources:
- https://datatracker.ietf.org/doc/html/rfc1035
- https://datatracker.ietf.org/doc/html/rfc2308
- https://datatracker.ietf.org/doc/html/rfc4034
- https://datatracker.ietf.org/doc/html/rfc8484
status: ready
summary: "Читайте реальный вывод dig, зонный файл и конфиг резолвера, предсказывайте поведение DNS и выбирайте диагноз или фикс, к которому сеньор потянется первым."
title: 'DNS: чтение dig и зонных файлов'
track: networking
unit: 04-dns-resolution
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
DNS-инциденты диагностируются в выводе dig и зонных файлах, а не на слайдах. Прочитайте каждый артефакт, предскажите, о чём он говорит, и выберите решение, которое сеньор примет первым.
</Hook>

<Goal lang="ru">
Отработайте цикл, который вы запускаете в каждом DNS-инциденте: прочитать trace или зонный файл, найти порванное звено и потянуться к диагнозу или фиксу с наибольшим рычагом, не гадая.
</Goal>

## Сниппет 1 — referral в +trace

```
$ dig +trace shop.example.com A

;; received referral from the .com TLD:
example.com.   172800  IN  NS  ns1.example.net.
example.com.   172800  IN  NS  ns2.example.net.
;; (Additional section empty)

;; query to ns1.example.net times out
;; query to ns2.example.net times out
;; SERVFAIL
```

<Quiz
  id="04-dns-resolution-code-1"
  lessonSlug="04-dns-resolution"
  lang="ru"
  question="Referral от .com перечисляет nameserver'ы, но резолв даёт SERVFAIL. Имена NS — ns1/ns2.example.net. Какое первое прочтение самое полезное?"
  choices={[
    { label: "Nameserver'ы out-of-bailiwick (example.NET, не example.com), поэтому glue не нужен и не присылается — пустой Additional нормален. Резолвер должен отдельно зарезолвить ns1.example.net, что и таймаутит. Реальная вина — доступность/резолв nameserver'ов example.net, а не отсутствие glue.", correct: true },
    { label: "Отсутствует glue: пустая секция Additional доказывает, что делегирование сломано.", misconception: "Glue нужен только для in-bailiwick nameserver'ов (ns1.example.com). Эти — в example.net, out-of-bailiwick зоне, которую резолвер резолвит независимо, так что пустой Additional ожидаем, а не баг." },
    { label: "TTL 172800 слишком велик и вызывает таймаут.", misconception: "172800 с (2 дня) — нормальный TTL для NS и к таймауту отношения не имеет. Резолв падает потому, что оба nameserver'а example.net недоступны, а не из-за TTL." },
    { label: "TLD .com должен был вернуть A-запись напрямую.", misconception: "TLD никогда не держит листовые A-записи; он возвращает делегирование (NS) и перенаправляет дальше. Сбой ниже по цепочке, на nameserver'ах example.net." },
  ]}
/>

## Сниппет 2 — правка зонного файла

```dns
$ORIGIN example.com.
@   IN  SOA  ns1.example.com. admin.example.com. (
            2026051300  ; serial  (was 2026051905)
            7200        ; refresh
            3600        ; retry
            1209600     ; expire
            300 )       ; minimum (negative-cache TTL)
@       IN  NS    ns1.example.com.
@       IN  CNAME shop.cdnprovider.net.
www     IN  CNAME shop.cdnprovider.net.
```

<Quiz
  id="04-dns-resolution-code-2"
  lessonSlug="04-dns-resolution"
  lang="ru"
  question="В этой правке зоны ДВА дефекта, которые укусят в продакшене. Какие?"
  choices={[
    { label: "(1) Serial уменьшили (2026051905 → 2026051300), поэтому вторичные считают зону более старой и тихо пропускают обновление — репликация рвётся. (2) CNAME стоит на apex (@) рядом с SOA и NS, что RFC 1034 запрещает, ведь CNAME переопределил бы любой другой тип записи там; используйте ALIAS/ANAME или HTTPS-запись.", correct: true },
    { label: "Minimum (300) слишком низок для negative caching, а у NS-записи нет glue.", misconception: "300 с — нормальный TTL для negative cache, а in-zone NS вроде ns1.example.com получает glue из родительского делегирования .com, а не из этого файла. Реальные дефекты — уменьшенный serial и apex CNAME." },
    { label: "Refresh и retry перепутаны местами; всё остальное валидно.", misconception: "Refresh (7200) больше retry (3600) — нормальный порядок. Реально ломают уменьшенный serial и запрещённый apex CNAME." },
    { label: "www CNAME, указывающий на другой домен, недопустим.", misconception: "CNAME на не-apex метке вроде www, указывающий на другой домен, полностью легален и обычен для CDN. Недопустим именно apex (@) CNAME; второй дефект — уменьшенный serial." },
  ]}
/>

## Сниппет 3 — диагностика DNSSEC

```
$ dig @1.1.1.1 +dnssec api.bank.example A
;; ->>HEADER<<- opcode: QUERY, status: SERVFAIL, id: 41832
;; flags: qr rd ra; QUERY: 1, ANSWER: 0

$ dig @1.1.1.1 +cd api.bank.example A     # +cd = checking disabled
;; ->>HEADER<<- status: NOERROR; flags: qr rd ra cd; ANSWER: 1
api.bank.example.   60   IN   A   203.0.113.42
```

<Quiz
  id="04-dns-resolution-code-3"
  lessonSlug="04-dns-resolution"
  lang="ru"
  question="Валидирующий резолвер возвращает SERVFAIL, но тот же запрос с +cd возвращает чистую A-запись. Что это доказывает и каков эффект для клиентов?"
  choices={[
    { label: "Данные есть и корректны на авторитативном сервере (+cd отключает проверку DNSSEC), поэтому сбой в цепочке DNSSEC — чаще всего DS у родителя, который больше не совпадает с KSK зоны после rollover. Валидирующие резолверы метят BOGUS и дают SERVFAIL; невалидирующие отдают, поэтому примерно 30-40% пользователей лежат, а остальные в порядке. SERVFAIL очистится в пределах TTL кеша DS после починки.", correct: true },
    { label: "Авторитативный сервер лежит; +cd читает из устаревшего кеша.", misconception: "+cd возвращает живой NOERROR-ответ, значит авторитативный работает и отдаёт корректные данные. Сбой в валидации, а не в доступности." },
    { label: "A-запись указывает на неверный IP, и DNSSEC её корректно отвергает.", misconception: "DNSSEC проверяет подписи, а не «правильность» IP. +cd показывает, что данные присутствуют; разрыв в цепочке подписей (DS/KSK), а не в неверном IP." },
    { label: "1.1.1.1 троттлит запрос; повтор с 8.8.8.8 пройдёт.", misconception: "Любой валидирующий резолвер (8.8.8.8, Quad9 в том числе) вернёт тот же SERVFAIL, потому что цепочка порвана выше. Успех +cd исключает rate limiting." },
  ]}
/>

## Сниппет 4 — конфиг резолвера и замер задержки

```
# /etc/unbound/unbound.conf (excerpt)
server:
    serve-expired: yes
    serve-expired-ttl: 86400

$ dig +short @127.0.0.1 whoami.cloudflare TXT CH
"AMS"
$ dig +short @127.0.0.1 example.com A
;; Query time: 70 msec      # every query, never cached lower
```

<Quiz
  id="04-dns-resolution-code-4"
  lessonSlug="04-dns-resolution"
  lang="ru"
  question="serve-expired включён, ближайший к резолверу PoP Cloudflare — AMS, но example.com стоит 70 мс на каждом запросе и никогда не падает до sub-ms. Какие два прочтения верны?"
  choices={[
    { label: "(1) serve-expired (RFC 8767, stale-while-revalidate) срабатывает, только когда upstream недоступен после истечения TTL — он улучшает доступность во время сбоев, а не задержку в стабильном режиме, поэтому постоянные 70 мс им не объясняются. (2) Постоянные 70 мс без быстрого кеш-пути значат, что ответ не кешируется: вероятно TTL=0, очень короткий TTL, истекающий между запросами, или валидация DNSSEC отбрасывает ответ.", correct: true },
    { label: "serve-expired и есть причина 70 мс — его отключение ускорит запросы.", misconception: "serve-expired отдаёт устаревшие данные быстрее во время сбоев; он никогда не добавляет задержку здоровому запросу. Постоянные 70 мс — сбой кеширования (TTL=0 или отбрасывание при валидации), не связанный с этой директивой." },
    { label: "whoami.cloudflare, вернувший AMS, означает, что резолвер смаршрутизирован неверно, и это и есть 70 мс.", misconception: "AMS — просто PoP, который ответил (Амстердам); один anycast-хоп нормален. 70 мс на каждом запросе — про то, что ответ не кешируется, а не про выбор PoP." },
    { label: "70 мс нормальны для тёплого кеша; ничего не сломано.", misconception: "Тёплый кеш возвращает за sub-ms до low-ms. Стабильные 70 мс значат, что каждый запрос делает полный upstream-обход — ответ не попадает в кеш (TTL=0, истечение или отбрасывание DNSSEC)." },
  ]}
/>

<Recap lang="ru">
Каждый DNS-инцидент читается в артефактах: referral в +trace говорит, нужен ли вообще glue (in-bailiwick vs out-of-bailiwick) и где застрял обход; зонный файл с первого взгляда выдаёт уменьшенный serial и запрещённый apex CNAME; SERVFAIL, исчезающий под +cd, — это разрыв цепочки DNSSEC, почти всегда несовпадение DS-vs-KSK после rollover; а постоянное время запроса значит, что ответ не кешируется, тогда как serve-expired помогает только во время сбоев. Диагностируйте по артефакту, назовите порванное звено, затем чините тот один механизм, на который оно указывает.
</Recap>
