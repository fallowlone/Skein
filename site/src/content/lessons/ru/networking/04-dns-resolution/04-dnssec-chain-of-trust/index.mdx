---
concepts:
- dnssec
- zsk
- ksk
- ds-record
- rrsig
- dnskey
- nsec
- nsec3
- bogus
- ad-bit
- trust-anchor
- ksk-rollover
- dnssec-validation
- chain-of-trust
deepensInto:
- 05-encrypted-dns
estMin: 16
lang: ru
lessonType: topic
level: senior
order: 4
prereqs:
- 03-ttl-and-caching
slug: 04-dnssec-chain-of-trust
sources:
- https://datatracker.ietf.org/doc/html/rfc4033
- https://datatracker.ietf.org/doc/html/rfc4034
- https://datatracker.ietf.org/doc/html/rfc5155
- https://datatracker.ietf.org/doc/html/rfc5011
- https://www.swisssign.com/en/blog/ca-browser-forum/dnssec-validation-sc085-smc014.html
spiral: []
status: ready
summary: Как DNSSEC подписывает каждый RRset от корня до листа, что ломается при разрыве
  связи DS-KSK, и почему ротация KSK без обновления DS у родителя молча отключает
  ваш сайт для 30% пользователей.
title: 'DNSSEC: цепочка доверия и сбой валидации'
track: networking
unit: 04-dns-resolution
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import TraceScenario from "~/components/pedagogy/TraceScenario.astro";
import DebugLog from "~/components/pedagogy/DebugLog.astro";
import NumbersCard from "~/components/layout/NumbersCard.astro";
import StackDiagram from "~/components/diagram/StackDiagram.astro";

<Hook lang="ru">
Ваша DNS-запись выглядит корректно во всех авторитативных инструментах. Но 30% пользователей не могут открыть ваш сайт — они видят DNS_PROBE_FINISHED_NXDOMAIN. Остальные 70% работают нормально. Разница не в провайдере; разница в том, валидирует ли их резолвер DNSSEC. На прошлой неделе вы ротировали KSK и забыли обновить DS-запись у родительского регистратора. Цепочка оборвалась.
</Hook>

<Crux>DNSSEC строит цепочку подписей от корня до каждой зоны. Разрыв DS при ротации KSK — и валидирующие резолверы возвращают SERVFAIL.</Crux>

<Explanation>

## Что делает DNSSEC и чего не делает

DNSSEC аутентифицирует DNS-ответы: он доказывает, что A-запись для `bank.example` пришла от легитимного авторитативного сервера и не была подделана в пути. Он **не** шифрует DNS-сообщения — перехватчик по-прежнему видит, какой домен вы запрашивали; этим занимаются DoH и DoT. DNSSEC валидирует данные; шифрование скрывает запросы.

## Роли ключей: ZSK и KSK

Каждая подписанная зона публикует два типа ключей:

- **ZSK (Zone Signing Key)** — подписывает фактические RRset-данные (A-, MX-, TXT-записи и т. д.). Ротируется часто (на практике каждые 1–3 месяца).
- **KSK (Key Signing Key)** — подписывает только DNSKEY RRset, который публикует ZSK. Ротируется редко (каждые 1–3 года). Его хэш публикуется в родительской зоне как запись **DS (Delegation Signer)**.

Разделение существует затем, чтобы ротация ZSK была дешёвой (без обновления у родителя), а ротация KSK — требующая координации с родителем — происходила редко.

## Цепочка доверия

Каждый валидирующий резолвер хранит один жёстко заданный якорь доверия: отпечаток KSK корневой зоны. Валидация идёт сверху вниз:

1. Резолвер держит отпечаток KSK корня как якорь доверия.
2. Корень публикует DNSKEY (KSK корня + ZSK корня). Резолвер проверяет, что KSK корня совпадает с якорем доверия.
3. ZSK корня подписывает DS-запись для `.com`. Резолвер верифицирует DS `.com` с помощью ZSK корня.
4. `.com` публикует DNSKEY (KSK и ZSK `.com`). Резолвер проверяет, что KSK `.com` совпадает с хэшем из DS шага 3.
5. ZSK `.com` подписывает DS-запись для `example.com`. И так далее, вплоть до листовой зоны.
6. ZSK листовой зоны подписывает A-запись. RRSIG идёт с ответом.
7. Если каждый шаг верифицируется → пометить ответ AD (Authentic Data). Если любой шаг падает → пометить BOGUS → вернуть SERVFAIL.

<NumbersCard
  id="card-dnssec-chain"
  title="Цепочка доверия DNSSEC"
  data-lesson-visual
  stats={[
    { label: "Якорь доверия", value: "Отпечаток KSK корня (жёстко задан в каждом валидирующем резолвере)" },
    { label: "Назначение ZSK", value: "Подписывает данные зоны (A, MX, TXT, …)" },
    { label: "Назначение KSK", value: "Подписывает только DNSKEY RRset" },
    { label: "DS-запись", value: "Родительская зона хранит хэш KSK дочерней зоны" },
    { label: "RRSIG", value: "Подпись над одним RRset, прилагается к каждой записи" },
    { label: "Результат BOGUS", value: "Клиенту возвращается SERVFAIL" },
  ]}
/>

## Режим отказа при ротации KSK

Самый распространённый инцидент DNSSEC:

1. Оператор генерирует новый KSK в дочерней зоне.
2. Оператор обновляет DNSKEY в дочерней зоне — новый KSK теперь подписывает DNSKEY RRset.
3. Оператор **забывает** обновить DS-запись у родительского регистратора.
4. Результат: DS у родителя содержит хэш старого KSK; RRSIG в дочерней зоне создан новым KSK.
5. Валидирующие резолверы получают DNSKEY дочерней зоны, верифицируют RRSIG — он валиден с новым KSK — но затем проверяют DS у родителя и обнаруживают, что хэш не совпадает. Цепочка оборвана → BOGUS → SERVFAIL.

Только резолверы, валидирующие DNSSEC (Cloudflare 1.1.1.1, Google 8.8.8.8, Quad9), возвращают SERVFAIL. Невалидирующие резолверы провайдеров отдают неподписанный ответ в обычном режиме. Это разделяет трафик: примерно 30–40% глобальных пользователей (те, кто на валидирующих резолверах) не могут открыть сайт; остальные — могут.

<TraceScenario
  id="sr-trace-1"
  pieceSlug="04-dnssec-chain-of-trust"
  lang="ru"
  scenario="Старшего инженера поднимают по инциденту: bank.example недоступен для ~30% пользователей по всему миру. Пройдите диагностику."
  steps={[
    { prompt: "dig +trace bank.example с машины оператора завершается успешно. Что это говорит?", reveal: "Полная цепочка делегирования исправна с точки зрения резолвера оператора. Проблема не в отсутствующей записи. Затронутые пользователи идут другим путём — конкретно через валидирующие резолверы." },
    { prompt: "drill +dnssec bank.example показывает AD=0 и ошибку валидации RRSIG. Что сломалось?", reveal: "Валидация DNSSEC. В цепочке подписей разрыв: хэш DS у родителя больше не совпадает с KSK дочерней зоны после ротации, либо оператор забыл обновить DS у родителя." },
    { prompt: "Почему только ~30% пользователей?", reveal: "Только DNSSEC-валидирующие резолверы отказываются отдавать BOGUS — Cloudflare 1.1.1.1, Google 8.8.8.8, Quad9. Невалидирующие резолверы (многие провайдеры) отдают неподписанный ответ. 30% соответствует примерной доле валидирующих резолверов в затронутой пользовательской аудитории." },
    { prompt: "Немедленное смягчение?", reveal: "Либо повторно опубликовать правильный DS у родительского регистратора (указывающий на новый KSK), либо временно удалить DS у родителя (небезопасная делегация). Второй вариант восстанавливает доступность в рамках одного TTL DS, но теряет защиту DNSSEC до восстановления цепочки." },
    { prompt: "Исправление post-mortem?", reveal: "Задокументировать SOP ротации KSK: публиковать новый DNSKEY наряду со старым в течение 2× max-TTL, обновить DS у родителя, дождаться ещё 2× max-TTL, затем удалить старый KSK. Добавить автоматический мониторинг согласованности DS и DNSKEY." },
  ]}
/>

<DebugLog
  id="sr-debug-1"
  pieceSlug="04-dnssec-chain-of-trust"
  lang="ru"
  title="Вывод dig во время инцидента DNSSEC — поставьте диагноз"
  output={`$ dig @1.1.1.1 +dnssec api.bank.example
;; ->>HEADER<<- opcode: QUERY, status: SERVFAIL, id: 41832
;; flags: qr rd ra; QUERY: 1, ANSWER: 0

$ dig @8.8.8.8 +dnssec api.bank.example
;; ->>HEADER<<- opcode: QUERY, status: SERVFAIL, id: 8821

$ dig @1.1.1.1 +cd api.bank.example     # +cd = checking disabled
;; ->>HEADER<<- opcode: QUERY, status: NOERROR, id: 1284
;; flags: qr rd ra cd; QUERY: 1, ANSWER: 1
api.bank.example.   60   IN   A   203.0.113.42`}
  outputLang="log"
  question="Оба валидирующих резолвера возвращают SERVFAIL, но +cd возвращает нормальную A-запись. Первопричина и влияние на пользователей?"
  hint="Когда +cd работает, а нормальная валидация падает, данные есть, но DNSSEC-валидация сломана."
  answer="Валидация DNSSEC падает. Авторитативный сервер отдаёт данные в обычном режиме (видно с +cd, который отключает проверку), но цепочка доверия оборвана. Наиболее вероятная причина: оператор ротировал KSK зоны без обновления DS у родителя — DS хранит хэш старого KSK, а дочерняя зона подписывает новым. Валидирующие резолверы отказываются отдавать BOGUS, поэтому клиенты на Cloudflare/Google/Quad9 (~30–40% глобальных пользователей) видят SERVFAIL. Невалидирующие резолверы провайдеров резолвят нормально. Исправление: повторно опубликовать правильный DS у родительского регистратора; SERVFAIL очистится в рамках окна TTL кеша DS."
/>

## NSEC vs NSEC3: отрицание существования

DNSSEC должен также доказывать, что имя не существует — иначе атакующий мог бы удалить запись и заявить «нет A-записи для bank.example». Механизм:

- **NSEC**: цепочка имён зоны в алфавитном порядке. «Между `alpha.example.com` и `zebra.example.com` ничего нет.» Легко верифицировать, но позволяет атакующему обходить все NSEC-записи и перечислять всю зону.
- **NSEC3** (RFC 5155): хеширует имена с солью перед включением в цепочку. Соседние элементы в цепочке — хеш-соседи, а не алфавитные. Предотвращает тривиальное перечисление зоны; атака с таблицами радужных хешей всё ещё возможна, но затратна.
- **NSEC5** (предложен/экспериментален): использует верифицируемые случайные функции; широко не развёрнут.

Операционная рекомендация: используйте NSEC3 для зон, где перечисление критично (корпоративные интрасети, поддомены клиентов); NSEC подходит для публичных зон, содержимое которых и так известно.

<Quiz
  id="sr-quiz-1"
  pieceSlug="04-dnssec-chain-of-trust"
  lang="ru"
  question="Почему ротация KSK без обновления DS у родителя вызывает SERVFAIL только у ~30% пользователей?"
  choices={[
    { label: "Только 30% пользователей резолвят через DNS; остальные используют закешированные IP", misconception: "DNS резолвит имена повсеместно. Разделение — между валидирующими и невалидирующими резолверами." },
    { label: "Валидирующие резолверы (Cloudflare, Google, Quad9) возвращают SERVFAIL; невалидирующие резолверы провайдеров отдают неподписанный ответ", correct: true },
    { label: "Новый KSK публикуется в другом TLD, до которого доходят 30% пользователей", misconception: "Изменения KSK — в той же зоне; TLD здесь не переменная." },
    { label: "DNSSEC применяется только к HTTPS-трафику", misconception: "DNSSEC валидирует DNS-ответы вне зависимости от прикладного протокола, используемого после резолюции." },
  ]}
/>

<DragOrder
  id="sr-drag-1"
  pieceSlug="04-dnssec-chain-of-trust"
  lang="ru"
  prompt="Расставьте шаги валидации DNSSEC для подписанного ответа:"
  items={[
    "Получить RRSIG над ответом; нужен DNSKEY зоны для верификации",
    "Получить DNSKEY RRset зоны; верифицировать с подписью ZSK",
    "Проверить, что ZSK подписан KSK зоны",
    "Сравнить хэш KSK с DS-записью у родительской зоны",
    "Верифицировать RRSIG родительского DS вплоть до корневой зоны",
    "Сравнить KSK корня с локально настроенным якорем доверия",
    "Если все звенья верифицированы — пометить ответ AD (Authentic Data) и вернуть",
  ]}
/>

<Inset kind="why" lang="ru">
**Ротация KSK корня в 2018 году.** Якорь доверия корневой зоны был опубликован в 2010 году и с тех пор ни разу не менялся. В 2018 году ICANN провела первую в истории ротацию KSK корня (KSK-2010 → KSK-2017). Подготовка заняла более трёх лет: любой резолвер, не обновивший якорь доверия до дня ротации, начал бы возвращать SERVFAIL для всех DNSSEC-подписанных имён по всему миру. ICANN мониторила популяции резолверов через специально инструментированные запросы и приостановила ротацию на 11 месяцев в 2017 году, обнаружив слишком много резолверов без поддержки RFC 5011, всё ещё использующих KSK-2010. RFC 5011 определяет автоматическую ротацию ключей: корневая зона публикует новый KSK рядом со старым не менее 30 дней, чтобы совместимые резолверы добавили новый ключ до удаления старого. Следующая запланированная ротация KSK корня ожидается около 2027–2028 годов. Урок: операционная сложность DNSSEC растёт с размером аудитории, зависящей от вашей зоны.
</Inset>

## Требование CA/Browser Forum 2026

Ballot SC-085v2 CA/Browser Forum (вступил в силу 15 марта 2026 года) обязывает все публично доверенные центры сертификации валидировать DNSSEC при его наличии во время Domain Control Validation и проверок CAA. Если у вашего домена неправильно настроена цепочка DNSSEC — просроченные подписи, сломанная делегация, неверный DS — CA откажет в выдаче или продлении TLS-сертификата до исправления цепочки. Это не обязывает активировать DNSSEC; зоны, которые никогда не были подписаны, не затрагиваются. Но домены, подписавшие свои зоны, теперь обязаны поддерживать цепочку в рабочем состоянии, иначе потеряют возможность получать новые сертификаты.

</Explanation>

<StackDiagram
  label="Цепочка доверия DNSSEC сверху вниз: якорь доверия KSK корня защищает корневую зону, DS-запись каждого родителя ручается за KSK дочерней зоны, а ZSK листовой зоны наконец подписывает A-запись — разрыв в любом звене DS делает весь ответ BOGUS"
  hue="var(--d-crypto)"
  layers={[
    { label: "Якорь доверия", note: "отпечаток KSK корня (жёстко задан)" },
    { label: "Корневая зона", note: "KSK подписывает DNSKEY → ZSK подписывает DS .com" },
    { label: "Зона .com", note: "DS совпадает с KSK → ZSK подписывает DS example.com" },
    { label: "Зона example.com", note: "DS совпадает с KSK → ZSK подписывает RRset A" },
    { label: "A-запись + RRSIG", note: "верно → AD; любой разрыв → SERVFAIL" },
  ]}
  caption="Валидация идёт сверху вниз от жёстко заданного KSK корня: DS-запись каждого родителя хранит хэш KSK дочерней зоны. Ротация KSK без обновления родительского DS обрывает цепочку — валидирующие резолверы помечают ответ BOGUS и возвращают SERVFAIL."
/>

<KeyTakeaway>DNSSEC строит криптографическую цепочку от якоря доверия корневой зоны через DS-записи и пары ZSK/KSK до каждого подписанного RRset. Разрыв в любом звене — чаще всего забытое обновление DS после ротации KSK — заставляет валидирующие резолверы возвращать SERVFAIL, тогда как невалидирующие продолжают резолвить, разделяя пользователей.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="04-dnssec-chain-of-trust-retrieval"
  lang="ru"
  questions={[
    { q: "DNSSEC валидирует что именно — данные A-записи, факт существования имени или и то, и другое?", a: "И то, и другое. DNSSEC доказывает, что данные A-записи подписаны ключом легитимной зоны (через цепочку RRSIG + DNSKEY + DS). Он также аутентифицирует отрицание существования через NSEC/NSEC3: если имя не существует, ответ включает подписанное доказательство того, что между соседними именами ничего нет. Без обоих механизмов атакующий мог бы подделать как наличие, так и отсутствие записей." },
    { q: "Каково операционное различие между ZSK и KSK, и зачем нужно это разделение?", a: "ZSK подписывает фактические RRset-данные и ротируется часто (ежемесячно или ежеквартально). KSK подписывает только DNSKEY RRset и ротируется редко. DS-запись у родительской зоны хранит хэш KSK — обновление DS требует координации с родительским регистратором, что имеет задержки распространения. Сохраняя KSK стабильным и ротируя только ZSK, операторы могут часто менять ключи подписи без обращения к родительской зоне." },
    { q: "Что делает флаг +cd в dig и почему он полезен при инциденте DNSSEC?", a: "+cd отключает проверку DNSSEC в резолвере. Запрос с +cd возвращает сырой ответ, даже если валидация должна была упасть. Если +cd возвращает NOERROR с правильной A-записью, а обычные запросы возвращают SERVFAIL, это подтверждает: данные присутствуют на авторитативном сервере, но цепочка DNSSEC где-то сломана между зоной и якорем доверия." },
  ]}
/>

<Recap lang="ru">
DNSSEC аутентифицирует DNS-ответы с помощью цепочки криптографических подписей — от якоря доверия корневой зоны вниз до отдельных записей. Два типа ключей выполняют разные роли: ZSK подписывает RRset-данные и ротируется часто; KSK подписывает DNSKEY RRset и ротируется редко, а его хэш хранится как DS-запись у родительской зоны. Разрыв в цепочке — несовпадение DS после ротации KSK, просроченный RRSIG, неверная делегация — заставляет валидирующие резолверы возвращать SERVFAIL и помечать ответ BOGUS. Невалидирующие резолверы по-прежнему отдают данные, поэтому неправильная конфигурация DNSSEC обычно ломает 30–40% пользователей (тех, кто на Cloudflare, Google, Quad9), не затрагивая остальных. NSEC и NSEC3 аутентифицируют отрицание существования; хеш-цепочка NSEC3 предотвращает тривиальное перечисление зоны. С марта 2026 года CA обязаны валидировать цепочки DNSSEC при выдаче сертификатов — сломанная цепочка теперь также блокирует продление TLS-сертификата.
</Recap>
