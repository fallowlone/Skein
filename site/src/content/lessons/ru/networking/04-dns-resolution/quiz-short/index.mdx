---
concepts:
- resolver-walk
- glue-record
- ttl-propagation
- negative-caching
- dnssec-validation
- doh-vs-dnssec
estMin: 13
lang: ru
level: senior
order: 8
prereqs: []
slug: quiz-short
sources:
- https://datatracker.ietf.org/doc/html/rfc1034
- https://datatracker.ietf.org/doc/html/rfc2308
- https://datatracker.ietf.org/doc/html/rfc4033
- https://datatracker.ietf.org/doc/html/rfc8484
status: ready
summary: "Вопросы на свободное воспроизведение по юниту DNS — обход резолвера, glue, семантика TTL, negative caching, DNSSEC и что добавляет шифрование. Сначала ответьте сами, затем раскройте."
title: 'DNS: тест на свободное воспроизведение'
track: networking
unit: 04-dns-resolution
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";

<Hook>
Воспроизведение по памяти сильнее перечитывания. Для каждого вопроса проговорите или запишите полный ответ по памяти, прежде чем открыть образец — именно усилие припоминания закрепляет механизм, когда вы посреди инцидента.
</Hook>

<Goal lang="ru">
Восстановите ключевые механизмы юнита — итеративный обход резолвера, glue, что на самом деле контролирует TTL, negative caching, цепочку доверия DNSSEC и чем шифрованный транспорт отличается от DNSSEC — не подглядывая в уроки.
</Goal>

<RetrievalDrawer
  client:visible
  id="04-dns-resolution-short-retrieval"
  lang="ru"
  questions={[
    { q: "Пройдите холодный резолв cdn.example.co.uk и объясните, почему запросы резолвера итеративные, а запрос клиента — рекурсивный.", a: "Клиент шлёт один рекурсивный запрос (RD=1) своему резолверу: «дай финальный IP, сделай работу». Резолвер делает итеративные запросы (RD=0) вниз по дереву — к root-серверу, затем к TLD .uk, затем к авторитативному example.co.uk, — и каждый ярус возвращает только то, что знает, плюс referral (NS в Authority, glue в Additional), сам по дереву не идёт. Root перенаправляет на .uk, .uk на example.co.uk, и только авторитативный возвращает A-запись с AA=1. Разделение важно для безопасности: сервер, рекурсирующий за внешних клиентов, — это open resolver и усилитель DNS-reflection." },
    { q: "Что такое glue-запись и что ломается без неё?", a: "Glue — это A/AAAA запись, которую родительская зона кладёт в секцию Additional referral'а, давая IP in-bailiwick nameserver'а. Когда зона делегирует на nameserver внутри самой себя — NS для example.com это ns1.example.com — резолвер не может достичь ns1.example.com, не зарезолвив сначала example.com, который он и пытается зарезолвить: циклическая зависимость. Glue разрывает цикл, отдавая IP ns1 напрямую. Отсутствие glue даёт периодический SERVFAIL или циклы referral'ов. Out-of-bailiwick nameserver'ам (ns1.someprovider.net) glue не нужен, так как резолвер резолвит их через другую зону." },
    { q: "Почему «DNS propagation» — вводящий в заблуждение термин и какова корректная SOP для планового изменения записи?", a: "Ничего не «распространяется»: в DNS нет push. Авторитативный никогда не уведомляет кеши; каждый нижестоящий кеш независимо держит свою копию, пока его TTL не досчитает до нуля, затем вытягивает новое значение. Поэтому изменение появляется в разное время лишь потому, что кеши заполнились в разные моменты и истекают по своим часам — худший случай это старый TTL, а не фиксированные 24-48 часов. SOP: примерно за неделю до изменения понизить TTL (например, до 60 с) и дождаться истечения старого высокого TTL повсюду; внести изменение (макс. устаревание теперь 60 с); после стабилизации поднять TTL обратно, чтобы снизить нагрузку запросами." },
    { q: "Что такое negative caching, какие длительности им управляют и почему это важно под нагрузкой?", a: "Negative caching хранит факт, что имя не существует (NXDOMAIN) или не имеет записи запрошенного типа (NODATA), поэтому повторные запросы того же несуществующего имени отвечаются резолвером, а не бьют по авторитативному. Длительность — min(SOA.MINIMUM, SOA.TTL) по RFC 2308, обычно 1-3 часа. SERVFAIL кешируется лишь ненадолго (примерно от 30 с до 5 мин по RFC 9520) — достаточно коротко, чтобы не усиливать сбой, и достаточно долго, чтобы остановить плотный цикл повторов. Без negative caching шторм опечаток или флуд поддельных поддоменов бил бы по авторитативному на каждом запросе и мог его обрушить." },
    { q: "Опишите цепочку доверия DNSSEC, разделение ZSK/KSK и самую частую ошибку при rollover.", a: "Каждый валидирующий резолвер зашивает один trust anchor: отпечаток корневого KSK. Валидация идёт вниз: корневой KSK подписывает корневой DNSKEY, корневой ZSK подписывает DS для .com, KSK .com (сверенный с этим DS) подписывает DNSKEY .com, ZSK .com подписывает DS для example.com и так до листа, чей ZSK подписывает A-запись через RRSIG. Две роли ключей: ZSK подписывает реальные данные и ротируется часто; KSK подписывает только DNSKEY RRset и ротируется редко, его хеш публикуется как DS у родителя. Разделение делает частую ротацию ZSK дешёвой (без координации с родителем). Классический сбой: прокрутить KSK, но забыть обновить DS у родительского регистратора — родитель всё ещё хеширует старый KSK, цепочка рвётся, валидирующие резолверы возвращают SERVFAIL (BOGUS), а невалидирующие отдают, и примерно 30-40% пользователей ломаются." },
    { q: "Что защищает шифрованный DNS (DoH/DoT/DoQ), что защищает DNSSEC и почему нужны оба?", a: "Они ортогональны. Шифрованный транспорт скрывает запрос от наблюдателей сети на пути между клиентом и резолвером — DoH на порту 443 неотличим от HTTPS и устойчив к блокировке по порту; DoT на 853 тривиально блокируется одним правилом firewall; DoQ добавляет QUIC connection migration и 0-RTT для мобильных. Но шифрование ничего не говорит о подлинности ответа. DNSSEC криптографически аутентифицирует данные записи end-to-end, доказывая подпись легитимной зоной, но шлёт их открыто, так что запрос всё равно виден. DoH без DNSSEC скрывает запрос, но не может проверить ответ; DNSSEC без шифрования проверяет ответ, но раскрывает запрос. Полная защита — конфиденциальность плюс целостность — требует обоих, и именно пара закрывает off-path cache poisoning (Kaminsky, SAD DNS)." },
  ]}
/>

<Recap lang="ru">
Если вы смогли восстановить каждый ответ по памяти, вы держите стержень юнита: резолвер обходит итеративно root → TLD → authoritative по referral'ам, а glue разрывает циклические делегирования; TTL — это разрешение для кеша, поэтому «propagation» — лишь независимое истечение, и плановые изменения начинаются с понижения TTL; negative caching (NXDOMAIN/NODATA через SOA.MINIMUM, SERVFAIL ненадолго) щитит авторитативный от флудов; DNSSEC цепляет подписи от корневого trust anchor через DS/KSK/ZSK, и забытое обновление DS после KSK rollover делит пользователей; а шифрованный транспорт скрывает запросы, тогда как DNSSEC аутентифицирует ответы — ортогонально, и вместе это защита от cache poisoning.
</Recap>
