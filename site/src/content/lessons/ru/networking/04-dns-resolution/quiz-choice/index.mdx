---
concepts:
- recursive-resolver
- referral-glue
- ttl-propagation
- negative-caching
- dnssec-chain
- encrypted-dns
estMin: 13
lang: ru
level: senior
order: 7
prereqs: []
slug: quiz-choice
sources:
- https://datatracker.ietf.org/doc/html/rfc1034
- https://datatracker.ietf.org/doc/html/rfc2308
- https://datatracker.ietf.org/doc/html/rfc4033
- https://datatracker.ietf.org/doc/html/rfc8484
status: ready
summary: "Синтез по всему юниту DNS в формате теста — обход резолвера, glue, TTL и «propagation», negative caching, отказы DNSSEC и шифрованный транспорт."
title: 'DNS: тест с множественным выбором'
track: networking
unit: 04-dns-resolution
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Шесть вопросов, пронизывающих весь юнит. Каждый отражает решение, которое вы принимаете в реальном DNS-инциденте — перенаправление, которое выглядит «сломанным», изменение, которое не хочет «распространяться», SERVFAIL у части пользователей — а не определение для заучивания.
</Hook>

<Goal lang="ru">
Убедитесь, что вы связываете обход резолвера, семантику кеширования и TTL, валидацию DNSSEC и шифрованный транспорт в единую модель — тот синтез, к которому вели отдельные уроки.
</Goal>

<Quiz
  id="04-dns-resolution-choice-1"
  lessonSlug="04-dns-resolution"
  lang="ru"
  question="Резолвер запрашивает у TLD .com имя shop.example.com и получает ответ с пустой секцией Answer, но с NS и A записями. Джуниор называет это поломкой. Почему это на самом деле корректно?"
  choices={[
    { label: "Это referral. TLD-сервер итеративный — он не владеет example.com, поэтому возвращает next-hop nameserver'ы как NS-записи в Authority плюс их glue A/AAAA в Additional, с AA=0 и без Answer. Резолвер идёт по нему к авторитативному серверу.", correct: true },
    { label: "Это поломка — TLD обязан вернуть A-запись для любого имени под ним.", misconception: "TLD владеет только делегированием, а не данными листа. Он знает, какой авторитативный сервер обслуживает example.com, и перенаправляет резолвер туда; саму A-запись shop.example.com он не хранит." },
    { label: "TLD выполнил рекурсию за резолвер, и ответ просто задержался.", misconception: "TLD-серверы никогда не рекурсируют за вызывающего — на этих итеративных запросах бит RD очищен. Они возвращают только то, что знают, плюс referral." },
    { label: "Glue-записи в Additional И ЕСТЬ финальный ответ для shop.example.com.", misconception: "Glue в Additional — это A/AAAA самих next-hop nameserver'ов (чтобы резолвер достиг их без циклического поиска), а не адрес shop.example.com." },
  ]}
/>

<Quiz
  id="04-dns-resolution-choice-2"
  lessonSlug="04-dns-resolution"
  lang="ru"
  question="Вы меняете A-запись. Коллега видит новое значение; у вас ещё 40 минут возвращается старое, потом переключается. Менеджер говорит ждать 24-48 часов «propagation». Что происходит на самом деле?"
  choices={[
    { label: "Ничего не «распространяется» — в DNS нет push. Каждый нижестоящий кеш независимо держит свою копию, пока его TTL не досчитает до нуля, затем перезапрашивает. Ваш резолвер закешировал старое значение с остатком TTL; кеш коллеги уже истёк. Худший случай — старый TTL, а не фиксированные 24-48 ч.", correct: true },
    { label: "Авторитативный сервер рассылает обновление наружу, и оно доходит до резолверов волнами за 24-48 часов.", misconception: "Нет ни push, ни волны. Авторитативные серверы никогда не уведомляют рекурсивные кеши; кеши вытягивают новое значение только при истечении своего TTL." },
    { label: "Ваш ISP тормозит обновление, а другой ISP доставил его быстрее.", misconception: "Это не throttling — просто у вашего резолвера в кеше ещё оставался TTL, а у коллеги уже истёк. Один механизм, разный возраст кеша." },
    { label: "Перезапуск браузера заставил бы новое значение появиться сразу.", misconception: "Устаревшая запись живёт в вышестоящем рекурсивном резолвере, а не в браузере. Кеш браузера отдельный и короткий; перезапуск браузера не сбрасывает резолвер." },
  ]}
/>

<Quiz
  id="04-dns-resolution-choice-3"
  lessonSlug="04-dns-resolution"
  lang="ru"
  question="Шквал запросов бьёт по авторитативному серверу по тысячам несуществующих поддоменов во время атаки. Почему на здоровой конфигурации это почти не повышает нагрузку на авторитативный?"
  choices={[
    { label: "Negative caching (RFC 2308). Ответы NXDOMAIN и NODATA кешируются на min(SOA.MINIMUM, SOA.TTL), поэтому повторные запросы тех же несуществующих имён обслуживаются резолвером, а не авторитативным. SERVFAIL тоже кешируется ненадолго, чтобы не было плотного цикла повторов.", correct: true },
    { label: "Авторитативные серверы игнорируют запросы к несуществующим именам, поэтому они ничего не стоят.", misconception: "Они отвечают — подписанным NXDOMAIN/NODATA. Нагрузка остаётся низкой потому, что резолверы кешируют эти отрицательные ответы, а не потому, что сервер их отбрасывает." },
    { label: "Резолвер отказывается пересылать запросы к именам, которых раньше не видел.", misconception: "Резолверы нормально пересылают неизвестные имена — так и резолвится всё новое. Повторный трафик гасит именно закешированный отрицательный ответ." },
    { label: "TTL применяется только к положительным ответам, поэтому каждый NXDOMAIN всё равно бьёт по авторитативному.", misconception: "TTL применяется и к отрицательным ответам. Длительность кеширования NXDOMAIN/NODATA задаётся SOA.MINIMUM именно для того, чтобы шторм опечаток не заваливал авторитативный." },
  ]}
/>

<Quiz
  id="04-dns-resolution-choice-4"
  lessonSlug="04-dns-resolution"
  lang="ru"
  question="После KSK rollover на прошлой неделе сайт недоступен для ~30% пользователей с SERVFAIL, а остальные 70% работают. dig +cd возвращает корректную A-запись. Какой диагноз?"
  choices={[
    { label: "DS-запись у родительского регистратора не обновили — она всё ещё хеширует старый KSK, тогда как зона теперь подписана новым: цепочка DNSSEC порвана. Валидирующие резолверы (1.1.1.1, 8.8.8.8, Quad9) метят ответ BOGUS и возвращают SERVFAIL; невалидирующие отдают его. Фикс: опубликовать корректный DS или временно убрать DS (insecure delegation).", correct: true },
    { label: "Только 30% пользователей вообще используют DNS; остальные доходят по закешированному IP.", misconception: "Все клиенты резолвят имена. Разделение проходит между валидирующими и невалидирующими резолверами, а не между теми, кто использует DNS, и теми, кто нет." },
    { label: "Сама A-запись неверна; +cd просто скрывает ошибку.", misconception: "+cd отключает проверку DNSSEC и возвращает корректную A-запись — доказывая, что данные на авторитативном сервере в порядке. Сбой в цепочке валидации, а не в записи." },
    { label: "Это проблема шифрования; перевод зоны на DoH её решит.", misconception: "DoH шифрует транспорт между клиентом и резолвером; к порванной цепочке DS-KSK он отношения не имеет. Валидация DNSSEC и шифрованный транспорт ортогональны." },
  ]}
/>

<Quiz
  id="04-dns-resolution-choice-5"
  lessonSlug="04-dns-resolution"
  lang="ru"
  question="Privacy-продукт хочет одновременно скрыть от наблюдателей сети, какие домены резолвят пользователи, И не дать off-path-атакующему подсунуть поддельный ответ. Какая комбинация реально даёт оба свойства?"
  choices={[
    { label: "Шифрованный транспорт (DoH/DoT/DoQ) плюс валидация DNSSEC. Шифрование скрывает запрос от наблюдателей на пути; DNSSEC криптографически подтверждает ответ. Они ортогональны — ни один по отдельности не даёт обоих свойств.", correct: true },
    { label: "Достаточно одного DoH — TLS и скрывает запрос, и доказывает подлинность ответа.", misconception: "DoH скрывает и аутентифицирует канал до резолвера, но сам вышестоящий ответ резолвера может быть поддельным или неверным; только DNSSEC аутентифицирует данные записи end-to-end." },
    { label: "Достаточно одного DNSSEC — подписанный ответ нельзя подсмотреть.", misconception: "DNSSEC подписывает данные, но шлёт их открыто по UDP/53; перехватчик всё равно видит каждый запрашиваемый домен. Это целостность, а не конфиденциальность." },
    { label: "EDNS Client Subnet плюс низкий TTL.", misconception: "ECS раскрывает подсеть клиента вышестоящим серверам (противоположность приватности), а TTL — про свежесть, не про конфиденциальность или целостность. Ни то, ни другое не решает задачу." },
  ]}
/>

<Quiz
  id="04-dns-resolution-choice-6"
  lessonSlug="04-dns-resolution"
  lang="ru"
  question="Резолв имени периодически возвращает SERVFAIL и только для DNSSEC-подписанных или больших ответов; маленькие plain-UDP-ответы работают. Вчера выкатили изменение на firewall. Наиболее вероятная причина?"
  choices={[
    { label: "Firewall блокирует TCP/53 (и/или вырезает EDNS0). Большие ответы — нагруженные RRSIG ответы DNSSEC, зонные трансферы — выставляют флаг TC и должны откатываться на TCP/53, а EDNS0 согласует увеличенный UDP-буфер, нужный DNSSEC. Блокировка любого из них тихо ломает именно большие/подписанные ответы, пока маленькие UDP проходят.", correct: true },
    { label: "У авторитативного сервера закончились RRSIG-подписи, и он может отвечать только на маленькие запросы.", misconception: "RRSIG не расходуются по запросу. Сбой, коррелирующий с размером, указывает на транспорт/MTU — заблокированный TCP/53 или вырезанный EDNS0, — а не на лимит подписей." },
    { label: "DNSSEC по своей сути работает только по TCP, поэтому plain UDP и есть баг.", misconception: "DNSSEC работает по UDP с EDNS0, объявляющим большой буфер; на TCP он откатывается только при усечении ответа. Картина указывает на заблокированный TCP/53 или вырезанную OPT-запись." },
    { label: "Кеш резолвера повреждён и его надо сбросить.", misconception: "Повреждённый кеш не давал бы сбой выборочно только на больших/подписанных ответах сразу после изменения на firewall. Корреляция с размером и тайминг указывают на firewall, а не на кеш." },
  ]}
/>

<Recap lang="ru">
Сквозная линия юнита — одна модель резолва: рекурсивный резолвер обходит root → TLD → authoritative по referral'ам (NS в Authority, glue в Additional); каждый кеш держит ответы — положительные и отрицательные — лишь столько, сколько разрешает TTL, поэтому «propagation» — это просто независимое истечение; DNSSEC накладывает цепочку подписей от корневого trust anchor вниз через DS/KSK/ZSK, и разрыв звена делит пользователей по линии валидирующих/невалидирующих; а шифрованный транспорт (DoH/DoT/DoQ) скрывает запросы, но ортогонален целостности DNSSEC. Большинство продакшен-сбоев — устаревшие записи, NXDOMAIN-флуды, SERVFAIL после rollover, усечение из-за заблокированного TCP — сводятся к одному из этих четырёх механизмов.
</Recap>
