---
concepts:
- doh
- dot
- doq
- ecs
- edns-client-subnet
- kaminsky-attack
- sad-dns
- cache-poisoning
- 0x20-encoding
- anycast-resolver
- rrl
- rfc-8484
- rfc-7858
- rfc-9250
- rfc-7871
deepensInto: []
estMin: 18
lang: ru
lessonType: topic
level: senior
order: 5
prereqs:
- 04-dnssec-chain-of-trust
slug: 05-encrypted-dns
sources:
- https://datatracker.ietf.org/doc/html/rfc8484
- https://datatracker.ietf.org/doc/html/rfc7858
- https://datatracker.ietf.org/doc/html/rfc9250
- https://datatracker.ietf.org/doc/html/rfc7871
- https://blog.cloudflare.com/sad-dns-explained/
- https://netguardia.com/privacy/anonymity/encrypted-dns-in-2026-doh-dot-dnscrypt-and-oblivious-dns-over-https/
- https://packet.guru/blog/DNS-Encryption-in-2026
spiral: []
status: ready
summary: Почему открытый UDP/53 — поверхность для слежки и атак, как DoH/DoT/DoQ решают
  эту проблему, что утекает через EDNS Client Subnet, и как атака Камински и SAD DNS
  сформировали современные средства защиты.
title: 'Зашифрованный DNS: DoH, DoT, DoQ и отравление кеша'
track: networking
unit: 04-dns-resolution
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import TraceScenario from "~/components/pedagogy/TraceScenario.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import NumbersCard from "~/components/layout/NumbersCard.astro";
import SequenceDiagram from "~/components/diagram/SequenceDiagram.astro";
import Infographic from "~/components/diagram/Infographic.astro";
import poisoningDefenceLayers from "./infographics/poisoning-defence-layers.svg?raw";

<Hook lang="ru">
Традиционный DNS работает через UDP порт 53 в открытом виде. Каждый посещаемый вами домен виден вашему провайдеру, каждому роутеру кофейни между вами и резолвером, и любому государственному перехватчику на линии. Компания, строящая продукт с фокусом на приватность, не может позволить DNS-трафику своих пользователей раскрывать их привычки просмотра. И отдельно: атакующий, способный опередить авторитативный сервер, может отравить кеш и перенаправить пользователей на вредоносный IP — риск, предшествующий HTTPS и дважды частично возрождённый после первоначального закрытия. К концу урока вы будете знать, какой зашифрованный DNS выбрать для какой модели угроз, где ECS всё равно вас сдаёт, и что «отравление кеша решено» реально означает в 2026 году.
</Hook>

<Crux>DoH прячет DNS в HTTPS; DoT на 853 легко блокируется; DoQ добавляет QUIC. ECS сливает подсеть. От отравления кеша нужны DNSSEC и шифрование.</Crux>

<Explanation>

## Почему открытый DNS — проблема

Когда вы добавляете вызов внешнего API, вы наверняка думаете об HTTPS для тела запроса — но задумываетесь ли о том, что раскрывает DNS-lookup hostname-а этого API? Традиционный DNS отправляет каждый запрос в открытом виде через UDP порт 53. Перехватчик в сети — ваш провайдер, общий Wi-Fi роутер, государственный тап — видит каждый запрашиваемый домен. Содержимое страницы защищено HTTPS, но сам lookup hostname'а полностью виден. DNS также не аутентифицирован на транспортном уровне: ответ можно подделать, если атакующий способен угадать или наблюдать transaction ID и source port.

## Сравнение DoH, DoT и DoQ

| Протокол | Порт | Транспорт | Устойчивость к блокировке | Примечания |
|---|---|---|---|---|
| **DoH** (RFC 8484) | 443 | TLS поверх TCP/QUIC | Высокая — неотличим от HTTPS | По умолчанию в браузерах; HoL-блокировка HTTP/2 на больших пакетах |
| **DoT** (RFC 7858) | 853 | TLS поверх TCP | Низкая — порт 853 блокируется одним правилом | Чистый протокол; предсказуем для блокировки |
| **DoQ** (RFC 9250) | 853 | QUIC (TLS 1.3) | Средняя | Запросы по-потоково; миграция соединений; 0-RTT |

<NumbersCard
  id="card-encrypted-dns"
  title="Распространение зашифрованного DNS (2026)"
  data-lesson-visual
  stats={[
    { label: "Доля DoH у Firefox (США)", value: "выше 85%" },
    { label: "Активных DoH-резолверов глобально", value: "931+" },
    { label: "Задержка DoH (типичная)", value: "12–18 мс" },
    { label: "Задержка DoT (типичная)", value: "20–25 мс" },
    { label: "Задержка DoQ (APNIC 2026)", value: "12–15 мс (на 40% ниже DoT)" },
    { label: "Топ-100К сайтов с HTTPS-записями", value: "4,2%" },
  ]}
/>

## Почему DoH превосходит DoT в борьбе с цензурой

DoT работает на порту 853. Брандмауэр цензора может добавить одно правило для блокировки всего трафика на порт 853, и все DoT-клиенты молча сломаются. DoH работает на порту 443 вместе с обычным HTTPS-трафиком — с точки зрения сети он неотличим от любого HTTPS-соединения. Блокировка порта 443 останавливает весь HTTPS — экономически неприемлемо. Чтобы заблокировать DoH конкретно, цензор вынужден перечислять и блокировать IP-адреса резолвера — игра в «убей крота», которую пользователь легко обходит, указав другой DoH-эндпоинт.

Та же особенность обходит корпоративную фильтрацию DNS. Корпоративные сети используют DNS-контроль для блокировки вредоносных или нарушающих политику доменов. Если браузеры обходят корпоративный резолвер через DoH, контроль ломается. Предприятия публикуют политику (через MDM или групповую политику), отключающую DoH в браузере и принудительно направляющую трафик через локальный резолвер.

<TraceScenario
  id="sr-trace-1"
  pieceSlug="05-encrypted-dns"
  lang="ru"
  scenario="Проследите соединение клиента через DoH с https://dns.cloudflare.com/dns-query."
  steps={[
    { prompt: "Клиент настроен на DoH. Первый шаг?", reveal: "Клиент выполняет TLS-рукопожатие с dns.cloudflare.com:443, используя SNI для запроса нужного сертификата." },
    { prompt: "TLS-рукопожатие завершено. Что клиент отправляет далее?", reveal: "HTTP POST на /dns-query с DNS-сообщением в теле, Content-Type: application/dns-message (RFC 8484)." },
    { prompt: "Сервер ответил. Что может наблюдать перехватчик в сети?", reveal: "Только то, что клиент подключился к IP-адресу Cloudflare на порту 443 и обменялся HTTPS-трафиком. Фактический DNS-запрос (резолвируемый домен) зашифрован внутри TLS. Провайдер не видит ни запрос, ни ответ." },
    { prompt: "Что произойдёт, если DoH-сервер медленный или недоступен?", reveal: "Клиент ожидает таймаута (~5 с) или переключается на открытый UDP/53, если так настроено. Фоллбэк полностью лишает смысла приватность." },
    { prompt: "Чем DoH отличается от DoT по устойчивости к цензуре?", reveal: "DoT использует выделенный порт 853 — одно правило брандмауэра блокирует его целиком. DoH работает на порту 443 вместе со всем HTTPS; блокировка 443 останавливает весь веб, поэтому цензоры вынуждены атаковать конкретные IP вместо порта." },
  ]}
/>

## Особенности DoQ (RFC 9250)

DNS over QUIC использует QUIC-потоки: каждый запрос — отдельный поток без блокировки начала очереди (проблема DoH поверх HTTP/2 при зависании одного запроса). Миграция QUIC-соединений позволяет одной DoQ-сессии пережить переключение Wi-Fi → LTE без повторного рукопожатия — важно для мобильных клиентов. Возобновление 0-RTT отправляет DNS-запрос внутри ClientHello при наличии кешированного session ticket, экономя один RTT.

RFC 9250 предупреждает: данные 0-RTT воспроизводимы — атакующий, перехвативший 0-RTT DoQ-запрос, мог бы воспроизвести его для временной корреляции пользователя. Смягчение: одноразовые resume-токены, новые QUIC-соединения после смены местоположения. В продакшене: Quad9, AdGuard и NextDNS поставляют DoQ; Cloudflare, Quad9 ставят `dnsdist` или `doqd` перед BIND/Unbound.

## EDNS Client Subnet (ECS): гео-маршрутизация vs приватность

ECS — расширение EDNS, которое добавляет IP-префикс клиента к DNS-запросам авторитативного, чтобы CDN мог ответить ближайшим edge-IP (RFC 7871). ECS позволяет рекурсивному резолверу прикреплять IP-префикс клиента (обычно /24 IPv4 или /56 IPv6) к вышестоящим запросам. Авторитативные серверы CDN используют префикс для возврата ближайшего edge-IP. Без ECS авторитативный видит только IP резолвера — который может находиться за сотни километров от реального пользователя.

Цена за приватность реальна: каждый авторитативный сервер в вышестоящей цепочке теперь узнаёт примерно, из какого района пользователь обращается к домену. Cloudflare 1.1.1.1 отключает ECS по умолчанию именно по этой причине. Google 8.8.8.8 включает его по умолчанию. Использование DoH-резолвера, который затем пересылает ECS вышестоящим, сводит на нет большую часть приватности от шифрования транспорта.

## Отравление кеша: Камински, SAD DNS и средства защиты

**Камински (2008).** Изначально DNS использовал 16-битный transaction ID плюс фиксированный source UDP-порт. Атакующий, инициирующий множество запросов к случайным поддоменам целевой зоны, мог опередить легитимный авторитативный ответ и внедрить поддельную запись — если поддельный ответ приходил первым с правильным 16-битным ID (в худшем случае 65536 попыток, на практике гораздо меньше). Патч: RFC 5452 обязывает рандомизировать source UDP-порт (~65K портов × 65K ID = ~4 млрд комбинаций).

**0x20-кодирование.** Смешивает прописные и строчные буквы в QNAME; нормально работающие авторитативные серверы возвращают точный регистр. Атакующий, не наблюдающий запрос, не знает паттерна регистра — добавляет ~30 бит эффективной энтропии.

**SAD DNS (2020).** Возродил угрозу: глобальный счётчик ограничения частоты ICMP в Linux наблюдаем с другой машины. Внешний атакующий может отправлять поддельные ICMP-пакеты об ошибках для вывода source-порта, используемого резолвером для данного запроса — резко сужая пространство поиска из 4 млрд комбинаций. Linux смягчил это, рандомизировав состояние ограничения ICMP по сокету. Практическая защита: DNSSEC + DoH/DoT — как только канал аутентифицирован и зашифрован, поверхность атаки для классической подмены исчезает.

Все три меры защиты работают слоями: рандомизация source-порта поднимает планку до 4 млрд попыток, 0x20 добавляет энтропию, которую атакующий не может наблюдать, а DNSSEC делает успешно внедрённую запись бесполезной — она не пройдёт аутентификацию. Без всех трёх каждая мера по отдельности обходима.

<Infographic
  svg={poisoningDefenceLayers}
  label="Четыре нарастающих слоя защиты от отравления кеша DNS: изначальный 16-битный TXID с фиксированным портом даёт лишь 65536 попыток подделки; рандомизация source-порта (RFC 5452) поднимает до ~4 млрд комбинаций; 0x20-смешение регистров добавляет ~30 бит, ненаблюдаемых внешним атакующим; DNSSEC и зашифрованный транспорт заставляют любую внедрённую запись провалить аутентификацию и полностью убирают внешние инъекции."
  caption="Каждый слой умножает цену атаки — рандомизация source-порта, энтропия 0x20, затем DNSSEC — и ни один по отдельности не достаточен."
  hue="var(--d-infra)"
/>

<TradeoffMatrix
  id="sr-tradeoff-1"
  pieceSlug="05-encrypted-dns"
  lang="ru"
  prompt="Privacy-ориентированное мобильное приложение выбирает зашифрованный DNS-протокол. Сетевые условия меняются; блокировка портов возможна."
  options={[
    { name: "DoH на порту 443", summary: "Для сети выглядит как HTTPS.", justification: "Лучшая устойчивость к цензуре — неотличим от HTTPS. HoL-блокировка HTTP/2 задерживает запросы на загруженных каналах, но HTTP/3 исправляет это. Наиболее широко поддерживается (Chrome, Firefox, Safari)." },
    { name: "DoT на порту 853", summary: "Выделенный TLS-порт.", justification: "Просто разворачивается на сервере. Порт 853 тривиально блокируется — враждебные провайдеры блокируют его целиком. Плохой выбор для adversarial-сетей." },
    { name: "DoQ на UDP-порту 853", summary: "Современный QUIC-протокол.", correct: true, justification: "Конфиденциальность TLS 1.3 + миграция QUIC-соединений (переживает Wi-Fi→LTE) + возобновление 0-RTT + потоковые запросы без HoL. Mobile-first. Распространение растёт; Cloudflare, Quad9, AdGuard его поставляют. Порт 853 ещё блокируем, но QUIC-транспорт DoQ можно проксировать через 443 при необходимости." },
    { name: "Открытый UDP/53", summary: "Без шифрования.", justification: "Каждый запрос виден наблюдателям в сети. Худший выбор с точки зрения приватности. Допустим только как фоллбэк." },
  ]}
/>

<Quiz
  id="sr-quiz-1"
  pieceSlug="05-encrypted-dns"
  lang="ru"
  question="Почему DoH лучше DoT противостоит сетевой блокировке доменов?"
  choices={[
    { label: "DoH использует более стойкое шифрование, чем DoT", misconception: "Оба используют TLS. Разница в порту, а не в стойкости шифрования." },
    { label: "DoT использует выделенный порт (853), который цензоры могут заблокировать целиком; DoH работает на порту 443 вместе со всем HTTPS-трафиком и не может быть заблокирован без остановки веба", correct: true },
    { label: "Ответы DoH меньше по размеру, поэтому они завершаются до того, как брандмауэр успевает их проинспектировать", misconception: "Размер ответа не связан с устойчивостью к цензуре. Ключ в том, что блокировка порта 443 экономически невозможна." },
    { label: "DoH аутентифицирован DNSSEC, а DoT — нет", misconception: "DNSSEC и DoH/DoT ортогональны. DNSSEC валидирует целостность данных; DoH/DoT шифруют канал. Оба можно использовать независимо." },
  ]}
/>

<Inset kind="why" lang="ru">
**EDNS Cookies (RFC 7873).** Лёгкий механизм аутентификации для каждой пары: клиент и сервер хранят 64-битный секрет для вычисления cookie, отправляемого в каждом запросе и ответе. Cookies ничего не шифруют, но существенно затрудняют подмену произвольных off-path source-IP — атакующий, не наблюдающий обмен, не может вычислить валидный cookie. EDNS Cookies на порядки снижают атаки усиления DNS-рефлексией при включении на уровне резолвера. BIND и Knot Resolver включают их по умолчанию. Некоторые firewall-миддлбоксы удаляют опцию EDNS Cookie и молча ломают защиту.
</Inset>

## Anycast-маршрутизация резолверов

Публичные резолверы (1.1.1.1, 8.8.8.8, 9.9.9.9) анонсируют один IP-префикс из десятков или сотен точек присутствия по всему миру через BGP anycast. BGP выбирает кратчайший AS-путь от каждого провайдера до ближайшей точки. У Cloudflare более 100 PoP, у Google ~40, у Quad9 ~150. Ловушка: если ваш провайдер пирится с оператором только в удалённом городе, ваш трафик к «ближайшей» точке проходит через тот город — типичный штраф 30 мс. Диагностика: `dig +short @1.1.1.1 whoami.cloudflare TXT CH` — покажет, какая точка ответила.

</Explanation>

<SequenceDiagram
  label="DoH прячет DNS-запрос внутри HTTPS: клиент открывает TLS к резолверу на порту 443 и отправляет DNS-сообщение POST'ом в зашифрованном теле, поэтому перехватчик на пути видит только HTTPS к IP резолвера, но не домен"
  hue="var(--d-network)"
  actors={["Клиент", "Перехватчик", "DoH-резолвер"]}
  messages={[
    { from: "Клиент", to: "DoH-резолвер", label: "TLS-рукопожатие :443 (SNI)" },
    { from: "Клиент", to: "DoH-резолвер", label: "POST /dns-query (зашифр. тело)" },
    { from: "DoH-резолвер", to: "Клиент", label: "зашифрованный DNS-ответ" },
    { from: "DoH-резолвер", to: "Перехватчик", label: "видит лишь: HTTPS к IP резолвера:443" },
  ]}
  caption="Поскольку запрос и ответ идут внутри TLS на порту 443, перехватчик не отличит DoH от любого другого HTTPS-соединения — видны лишь IP резолвера и порт. DoH скрывает, какие домены вы запрашиваете; DoT на порту 853 несёт тот же TLS, но на порту, который блокируется одним правилом брандмауэра."
/>

<KeyTakeaway>DoH скрывает DNS на порту 443, делая запросы неотличимыми от HTTPS и побеждая блокировку по портам; DoT на 853 легко блокируется; DoQ добавляет QUIC-миграцию и 0-RTT для мобильных устройств. ECS улучшает гео-маршрутизацию CDN, но раскрывает подсеть клиента каждому вышестоящему серверу. DNSSEC и зашифрованный транспорт вместе побеждают отравление кеша — ни одного по отдельности недостаточно.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="05-encrypted-dns-retrieval"
  lang="ru"
  questions={[
    { q: "Инженер утверждает, что EDNS Client Subnet обязателен для точной гео-маршрутизации CDN. В чём ошибка этого утверждения?", a: "ECS улучшает гео-маршрутизацию CDN, предоставляя IP-префикс клиента авторитативному CDN-серверу, позволяя вернуть ближайший edge. Но без ECS CDN использует IP резолвера — достаточно точный в большинстве случаев, поскольку резолверы провайдеров обычно географически близки к своим клиентам. ECS — оптимизация, а не требование. Цена за приватность реальна: каждый авторитативный сервер в вышестоящей цепочке узнаёт примерное местоположение клиента. Cloudflare отключает ECS по умолчанию именно по этой причине. Честный компромисс: незначительное улучшение задержки в обмен на сообщение вашей подсети каждому посещаемому домену." },
    { q: "Опишите атаку Камински и две защиты, закрывшие её.", a: "Камински (2008) показал: 16-битный transaction ID DNS плюс фиксированный source-порт давали лишь 65536 комбинаций для угадывания. Атакующий, запускающий множество запросов к случайным поддоменам целевой зоны, мог внедрить поддельный ответ с правильным ID раньше легитимного авторитативного, отравив кеш. Защиты: (1) RFC 5452 — рандомизация source-порта добавляет ~65K портов, давая ~4 млрд комбинаций; (2) 0x20-смешение регистров добавляет энтропию регистра, которую атакующий не может предсказать. DNSSEC полностью устраняет поверхность атаки, аутентифицируя ответы криптографически." },
    { q: "В чём разница между тем, что обеспечивают DoH/DoT, и тем, что обеспечивает DNSSEC?", a: "DoH/DoT шифруют DNS-канал — они скрывают, какие домены вы запрашиваете, от наблюдателей в сети между вами и резолвером. DNSSEC аутентифицирует DNS-ответы — он доказывает, что данные A-записи подписаны ключом легитимной зоны и не были подделаны в пути. Они ортогональны: DoH без DNSSEC скрывает запросы, но не может проверить целостность ответа; DNSSEC без DoH аутентифицирует данные, но оставляет запрос видимым для провайдеров и перехватчиков. Полная защита требует обоих." },
  ]}
/>

<Recap lang="ru">
Открытый DNS через UDP порт 53 раскрывает каждый DNS-запрос провайдерам и наблюдателям в сети. DoH (порт 443) скрывает запросы внутри HTTPS, делая их неотличимыми от обычного веб-трафика и устойчивыми к цензуре по портам. DoT (порт 853) обеспечивает то же TLS-шифрование, но на выделенном порту, тривиально блокируемом брандмауэрами. DoQ (QUIC на порту 853) добавляет миграцию соединений и 0-RTT для мобильных сред. EDNS Client Subnet улучшает точность гео-маршрутизации CDN, но рассылает IP-префикс клиента каждому вышестоящему авторитативному — Cloudflare отключает его по умолчанию. Отравление кеша через атаку Камински 2008 года и side-channel SAD DNS 2020 года смягчается рандомизацией source-порта, 0x20-кодированием и DNSSEC; использование зашифрованного транспорта полностью устраняет поверхность атаки для внешних инъекций. Anycast-маршрутизация обеспечивает ответы публичных резолверов из ближайшей точки; диагностика выбора точки — `dig +short @1.1.1.1 whoami.cloudflare TXT CH`. Теперь, выбирая DNS-провайдера для privacy-ориентированного продукта, проверяйте три вещи: поддерживает ли он DoH или DoQ, отключает ли ECS по умолчанию и валидирует ли DNSSEC — «да» по всем трём — минимальная планка.
</Recap>
