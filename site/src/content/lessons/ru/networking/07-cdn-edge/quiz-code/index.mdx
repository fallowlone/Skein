---
concepts:
- cache-control
- vary-header
- stale-while-revalidate
- edge-worker
- cache-status
- wall-time-budget
estMin: 14
lang: ru
level: senior
order: 9
prereqs: []
slug: quiz-code
sources:
- https://datatracker.ietf.org/doc/html/rfc9111
- https://datatracker.ietf.org/doc/html/rfc5861
- https://developers.cloudflare.com/workers/runtime-apis/htmlrewriter/
status: ready
summary: "Читай реальные заголовки ответа, строку Cache-Control, обработчик edge worker и CDN-трейс, затем выбирай поведение или фикс с наибольшим рычагом."
title: 'CDN и edge: чтение заголовков и трейсов'
track: networking
unit: 07-cdn-edge
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Проблемы CDN диагностируются в заголовках ответа, строках Cache-Control и логах edge — не в прозе. Прочитай каждый артефакт, предскажи, что реально делают кеш и worker, затем выбери фикс, к которому senior тянется первым.
</Hook>

<Goal lang="ru">
Отработай цикл каждого инцидента CDN: читай заголовки и трейс, предсказывай кешируемость и hit rate, выбирай изменение с наибольшим рычагом до того, как трогать маршрутизацию или origin.
</Goal>

## Сниппет 1 — заголовки ответа

```
HTTP/2 200
cache-control: public, max-age=3600
vary: User-Agent, Accept-Encoding, Cookie
cf-cache-status: MISS
age: 0
```

<Quiz
  id="07-cdn-edge-code-1"
  lessonSlug="07-cdn-edge"
  lang="ru"
  question="Hit rate на этом кешируемом HTML около 5%. Читая только эти заголовки, какова доминирующая проблема и фикс?"
  choices={[
    { label: "Vary перечисляет User-Agent (тысячи значений) и Cookie (на сессию) — вместе cache key фактически на каждый запрос, поэтому почти ничего не переиспользуется. Сократи Vary до Accept-Encoding; отдавай адаптивный HTML вместо вариантов на агента и помечай cookie-читающие маршруты private.", correct: true },
    { label: "Проблема в max-age=3600; подними до года.", misconception: "Годовой TTL на изменяемом HTML неверен, и TTL не причина обвала hit rate. Cache key фрагментирован через Vary: User-Agent + Cookie — именно это убивает переиспользование." },
    { label: "cf-cache-status: MISS значит, что CDN обходится; включи кеширование.", misconception: "Единственный MISS — лишь этот один холодный запрос; ответ КЕШИРУЕМ (public, max-age). Хронический hit rate 5% — от фрагментации cache key на запрос через Vary, а не от выключенного кеша." },
    { label: "age: 0 показывает, что запись не стареет; укороти TTL.", misconception: "age: 0 просто значит, что это свежий MISS, который заполняется. Укорачивание TTL ещё снизит hit rate. Реальная вина — Vary-заголовок высокой кардинальности." },
  ]}
/>

## Сниппет 2 — строка Cache-Control

```
Cache-Control: public, max-age=60, stale-while-revalidate=600, stale-if-error=86400
```

<Quiz
  id="07-cdn-edge-code-2"
  lessonSlug="07-cdn-edge"
  lang="ru"
  question="Эндпоинт листинга товаров шлёт это. Опиши, что делает общий кеш CDN на 0-й секунде, на 120-й секунде и во время 8-минутного сбоя origin на 300-й секунде."
  choices={[
    { label: "0–60 с: свежо, отдаётся из кеша. На 120 с (после max-age, в пределах SWR): отдаёт устаревшую копию мгновенно каждому запросу и запускает ровно одну фоновую ревалидацию. Во время сбоя после истечения: stale-if-error продолжает отдавать последнюю хорошую копию до 1 дня вместо 503.", correct: true },
    { label: "Ответ некешируем на CDN, потому что max-age всего 60 с.", misconception: "60 с — вполне валидное окно свежести общего кеша; public делает его кешируемым на CDN. SWR и SIE затем продлевают полезную отдачу далеко за него." },
    { label: "На 120 с каждый запрос идёт на origin, потому что max-age истёк.", misconception: "Именно это и предотвращает stale-while-revalidate=600: после max-age он отдаёт stale всем и шлёт одну фоновую ревалидацию, так что стадо не доходит до origin." },
    { label: "stale-if-error заставляет CDN игнорировать 5xx навсегда и никогда не обновляться.", misconception: "stale-if-error отдаёт stale только пока origin сбоит, в пределах своего окна; на следующей успешной ревалидации CDN кеширует свежий 200 и возобновляет нормальное поведение." },
  ]}
/>

## Сниппет 3 — edge worker

```js
export default {
  async fetch(req, env) {
    const page = await fetch(req);                 // кешируемые chrome + body
    const price = await fetch(env.PRICING_URL);    // на пользователя, не кешируется
    return new HTMLRewriter()
      .on("#price", { element(e) { e.setInnerContent(price.statusText); } })
      .transform(page);
  },
};
```

<Quiz
  id="07-cdn-edge-code-3"
  lessonSlug="07-cdn-edge"
  lang="ru"
  question="Этот worker edge-side composition верен по форме, но имеет баг латентности на критическом пути. Какой и каков фикс?"
  choices={[
    { label: "Два fetch идут последовательно (await, затем await), поэтому суммарная латентность = page-fetch + price-fetch. Запусти оба сразу и сделай await Promise.all, чтобы они перекрылись; ответ ограничивает медленный из двух, а не их сумма. Ещё ограничь вызов цены таймаутом и кешируемым fallback, чтобы медленный pricing-сервис не застопорил страницу.", correct: true },
    { label: "HTMLRewriter не умеет стримить, поэтому буферизует всю страницу — вот латентность.", misconception: "HTMLRewriter — потоковый парсер; в этом весь смысл. Латентность — от ожидания fetch страницы и fetch цены одного за другим вместо параллели." },
    { label: "Worker превысит 50 мс CPU wall-time из-за перезаписи.", misconception: "Одна перезапись innerContent — субмиллисекунда CPU, а ожидание I/O не идёт в бюджет wall-time. Дефект — сериализованный I/O, лечится распараллеливанием fetch." },
    { label: "Edge workers не могут вызывать внешние сервисы, поэтому PRICING_URL упадёт.", misconception: "Edge workers рутинно делают fetch к внешним и региональным сервисам; это базовый кейс. Реальная проблема — два fetch последовательно на критическом пути." },
  ]}
/>

## Сниппет 4 — CDN-трейс

```
$ curl -sI https://example.com/article/42 | grep -iE 'cache|age|via'
cf-cache-status: HIT
age: 7200
cache-control: public, max-age=86400
```

<Quiz
  id="07-cdn-edge-code-4"
  lessonSlug="07-cdn-edge"
  lang="ru"
  question="Ты выкатил исправленную версию /article/42 десять минут назад, но пользователи всё ещё видят старый текст. Читая этот трейс — почему и каков самый быстрый верный фикс?"
  choices={[
    { label: "Это HIT с age 7200 с при max-age=86400, поэтому edge будет отдавать эту копию ещё ~22 часа — твой деплой её не инвалидировал. Сделай CDN purge по URL (или его cache-tag); он расходится по всем POP за 1–5 с, и вшей этот purge в пайплайн деплоя, чтобы он происходил автоматически.", correct: true },
    { label: "age: 7200 значит, что запись уже истекла, так что просто подожди пару секунд.", misconception: "age 7200 заметно меньше max-age 86400, поэтому запись ещё свежа и будет отдаваться ещё ~22 часа. Сейчас её снимет только purge (или истечение TTL)." },
    { label: "cf-cache-status: HIT доказывает, что устарел кеш браузера; hard-refresh починит для всех.", misconception: "HIT — это edge-кеш CDN, не браузер, и рефреш одного пользователя не чинит никого другого. Общую edge-копию для всех инвалидирует именно CDN purge." },
    { label: "Снизь max-age до 60 и передеплой; это освободит закешированную копию.", misconception: "Смена max-age влияет только на ответы, закешированные после изменения; уже закешированная запись держит своё исходное окно 86400 с. Чтобы снять её сейчас, нужен purge." },
  ]}
/>

<Recap lang="ru">
Каждый инцидент CDN читается в заголовках и трейсах: заголовок Vary с User-Agent или Cookie фрагментирует cache key на запрос и обваливает hit rate; max-age плюс stale-while-revalidate и stale-if-error задают окно свежести, безопасное к stampede окно stale и fallback на сбой; worker edge-композиции должен распараллелить свои fetch и ограничить волатильный таймаутом и fallback; а HIT с age меньше max-age означает устаревший деплой, который снимет только purge. Диагностируй по артефакту, применяй фикс с наибольшим рычагом, затем перепроверь тот же заголовок.
</Recap>
