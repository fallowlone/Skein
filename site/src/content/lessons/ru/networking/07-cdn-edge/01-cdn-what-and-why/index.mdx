---
concepts:
- cdn
- edge-server
- pop
- cache-hit
- cache-miss
- origin-server
- anycast
- geodns
deepensInto:
- 02-anycast-and-geodns
- 03-tiered-cache-and-cache-control
estMin: 8
lang: ru
lessonType: topic
level: junior
order: 1
prereqs:
- 04-dns-resolution/01-dns-what-it-does
- 03-tcp-handshake/01-the-three-way-handshake
slug: 01-cdn-what-and-why
sources:
- https://blog.cloudflare.com/cloudflare-connected-in-over-300-cities/
- https://almanac.httparchive.org/en/2025/cdn
- https://www.akamai.com/us/en/why-akamai/
spiral:
- latency
status: ready
summary: CDN держит копии контента на сотнях edge-серверов по всему миру, чтобы каждый
  пользователь получал байты с расстояния в 20 мс вместо 200 мс.
title: 'CDN: контент по соседству'
track: networking
unit: 07-cdn-edge
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import MetaphorComplete from "~/components/pedagogy/MetaphorComplete.astro";
import NumbersCard from "~/components/layout/NumbersCard.astro";
import PersonaTag from "~/components/pedagogy/PersonaTag.astro";

<Hook>
Когда <PersonaTag id="bea" lang="ru" /> в Лондоне загружает страницу с сервера в Калифорнии, каждый запрос идёт 200 мс туда-обратно — и никакой код это не исправит. Скорость света конечна. CDN закрывает разрыв, перемещая контент, а не ускоряя сеть.
</Hook>

<Crux>CDN раздаёт копии контента с edge-серверов по всему миру — каждый запрос проходит 20 мс вместо 200 мс, побеждая задержку переносом данных.</Crux>

<Explanation>

## Что делает CDN одним предложением

**CDN (Content Delivery Network)** держит копии вашего сайта на сотнях серверов по всему миру, чтобы браузер пользователя забирал байты с сервера в 20 мс вместо 200 мс.

## Почему задержка — настоящая проблема

Скорость света ~300 000 км/с в вакууме; в оптоволокне медленнее (~200 000 км/с). Лондон — Калифорния ~8 700 км. Минимальное one-way: 8 700 / 200 000 = **~43 мс**. RTT: ~87 мс. Добавьте TCP, TLS, очереди — получается **200–250 мс** на запрос. Никакая оптимизация кода не закрывает этот разрыв. CDN закрывает его, перемещая контент.

## Метафора склада

Представьте, что вы издаёте журнал. Без CDN отправляете каждому читателю копию из одной типографии. С CDN — везёте паллеты в локальные центры дистрибуции каждого города; читатель забирает из ближайшего. Тот же журнал, гораздо короче путь. Каждый **Point of Presence (PoP)** — один из таких складов: датацентр с CDN-серверами.

## Один сценарий от начала до конца

<PersonaTag id="bea" lang="ru" /> в Лондоне заходит на CDN-fronted сайт:

1. Браузер делает [DNS](/ru/networking/04-dns-resolution/) — получает IP **ближайшего CDN edge-сервера** (через Anycast-маршрутизацию или GeoDNS).
2. [TCP](/ru/networking/03-tcp-handshake/) + [TLS](/ru/networking/05-tls-handshake/) + HTTP-запрос — всё против **edge**, не origin.
3. **Cache hit**: у edge страница закешированa → ответ за ~20 мс.
4. **Cache miss**: edge форвардит к origin (или промежуточному shield), кеширует ответ, возвращает пользователю.

<PersonaTag id="patty" lang="ru" /> в Токио запрашивает тот же URL и попадает на токийский edge со своей копией кеша. <PersonaTag id="sven" lang="ru" /> — origin-сервер в Калифорнии — видит один запрос от edge каждого города, почти никакого прямого трафика от пользователей.

<NumbersCard
  id="card-cdn-basic-numbers"
  title="Edge hit vs. origin round-trip"
  data-lesson-visual
  stats={[
    { label: "Origin (Калифорния → Лондон) round-trip", value: "~200–250 мс" },
    { label: "Edge cache hit (Лондонский POP)", value: "~10–20 мс" },
    { label: "Ускорение на cache hit", value: "10–20×" },
    { label: "Edge PoP Cloudflare (2026)", value: "330+ городов, 120+ стран" },
    { label: "Типичный cache hit ratio (статические ассеты)", value: "90–98%" },
    { label: "Поглощение трафика origin shield", value: "90%+ edge-промахов" },
  ]}
/>

## Когда CDN НЕ помогает?

CDN помогает, когда контент **разделяем между пользователями** — статические ассеты (JS, CSS, изображения), HTML без user-specific данных. Не помогает, когда **каждый ответ уникален** (баланс счёта, персонализированная лента, сумма заказа) — нечего кешировать, запрос всё равно идёт к origin. Соединение всё равно терминируется на edge (что экономит TLS-латентность), но ответ должен быть получен свежим каждый раз.

<Inset kind="why" lang="ru">
**Почему три механизма работают вместе.** Anycast IP-маршрутизация (один адрес из многих городов) доставляет запросы к BGP-ближайшему POP без специального DNS. GeoDNS добавляет явный контроль: CDN-оператор возвращает разные A-записи по географии резолвера, так что лондонский резолвер получает IP лондонского edge. Кеш edge затем обслуживает большинство запросов без обращения к origin. Все три механизма вместе: Anycast или GeoDNS находит правильный edge; кеш делает тяжёлую работу.
</Inset>

<Quiz
  id="jr-quiz-1"
  pieceSlug="01-cdn-what-and-why"
  lang="ru"
  question="Главная задача CDN?"
  choices={[
    { label: "Шифровать трафик end-to-end", misconception: "Шифрование — задача TLS, не CDN." },
    { label: "Раздавать копии контента из многих географических локаций так чтобы каждый пользователь получал с ближайшего edge", correct: true },
    { label: "Автоматически блокировать ботов", misconception: "Многие CDN предлагают bot-защиту, но кеширование — основная цель." },
    { label: "Ускорять database-запросы origin-сервера", misconception: "DB origin не связана с CDN edge-кешированием." },
  ]}
/>

<Quiz
  id="jr-quiz-2"
  pieceSlug="01-cdn-what-and-why"
  lang="ru"
  question="Когда CDN НЕ снижает задержку?"
  choices={[
    { label: "Когда страница кешируема и пользователь далеко от origin", misconception: "Именно тогда CDN помогает больше всего." },
    { label: "Когда каждый ответ персонализирован и уникален для пользователя — нечего кешировать", correct: true },
    { label: "Когда у пользователя медленный Wi-Fi", misconception: "CDN не чинит last-mile, но помогает со всем до него." },
    { label: "Когда сайт использует HTTPS", misconception: "CDN отлично работает с HTTPS — терминирует TLS на edge." },
  ]}
/>

<DragOrder
  id="jr-drag-1"
  pieceSlug="01-cdn-what-and-why"
  lang="ru"
  prompt="Что происходит на CDN cache HIT:"
  items={[
    "Браузер DNS-разрешает на CDN edge IP через Anycast или GeoDNS",
    "Браузер открывает TCP + TLS к edge",
    "Браузер отправляет HTTP-запрос",
    "Edge ищет URL в кеше и находит свежую копию",
    "Edge возвращает закешированный ответ браузеру",
  ]}
/>

<MetaphorComplete
  id="jr-metaphor-1"
  pieceSlug="01-cdn-what-and-why"
  lang="ru"
  setup="Заполните: CDN — это цепь _______ распространяющих контент так что каждый пользователь забирает с ближайшего."
  accepted={["складов", "распределительных центров", "депо", "warehouse", "warehouses"]}
  canonical="складов (или распределительных центров)"
  explanation="Каждый edge POP — маленький склад с популярным контентом рядом с пользователями. Origin — центральная фабрика, отгружающая на склады."
/>

</Explanation>

<KeyTakeaway>CDN побеждает задержку, реплицируя контент на edge-PoP по всему миру; Anycast или GeoDNS направляет каждого пользователя к ближайшему edge; cache hit возвращает ответ за ~20 мс вместо ~200 мс. CDN помогает только кешируемому контенту — персонализированные ответы всё равно идут к origin.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="01-cdn-what-and-why-retrieval"
  lang="ru"
  questions={[
    { q: "Одним предложением: какой trade-off CDN требует в обмен на скорость?", a: "Закешированный контент по определению немного устарел — когда origin меняется, пользователи могут видеть старую версию пока кеш не истечёт или не будет очищен; CDN торгует свежесть на задержку, и ваша задача — выбрать подходящие TTL и использовать инструменты invalidation (purge, cache-tags) так чтобы компромисс был приемлемым для каждого типа контента." },
    { q: "Назовите два механизма которые CDN использует для маршрутизации пользователя к ближайшему edge-серверу.", a: "Anycast IP (один IP рекламируется из многих локаций, BGP выбирает ближайший) и GeoDNS (авторитативный DNS возвращает разные A-записи в зависимости от географии резолвера)." },
    { q: "Почему CDN всё равно помогает с задержкой для персонализированных ответов даже если не может их кешировать?", a: "CDN терминирует TCP+TLS соединение на edge (физически близко к пользователю), поэтому пользователь платит только локальный RTT за установку соединения; fetch к origin происходит через оптимизированный backbone CDN, а не через публичный интернет от пользователя до самого origin." },
  ]}
/>

<Recap lang="ru">
CDN (Content Delivery Network) реплицирует контент на сотни edge Point of Presence (PoP) по всему миру, чтобы пользователи получали с сервера в 20 мс вместо 200 мс. Anycast-маршрутизация или GeoDNS направляет каждый запрос к ближайшему POP; локальный кеш edge отвечает без обращения к origin. Cache hit в 10–20 раз быстрее round-trip к origin — выигрыш целиком от близости, не от более мощного железа. CDN помогает больше всего кешируемому контенту (статические ассеты: JS, CSS, изображения) и почти не помогает персонализированным ответам. Origin видит лишь малую долю суммарного трафика — обычно менее 10% для хорошо настроенного статического сайта.
</Recap>
