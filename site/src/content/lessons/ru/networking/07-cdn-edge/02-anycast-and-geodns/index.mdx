---
slug: 02-anycast-and-geodns
lang: ru
track: networking
unit: 07-cdn-edge
order: 2
title: "Anycast и GeoDNS: маршрутизация к ближайшему edge"
summary: "Как один IP-адрес, анонсированный из 300 городов, и геоориентированный DNS вместе отправляют каждого пользователя к ближайшему CDN edge-серверу."
estMin: 10
status: ready
lessonType: topic
level: middle
prereqs:
  - "04-dns-resolution/02-the-resolver-walk"
  - "07-cdn-edge/01-cdn-what-and-why"
deepensInto:
  - "07-cdn-operations-and-observability"
spiral: ["latency"]
concepts: ["anycast", "geodns", "bgp", "pop", "edns-client-subnet", "origin-shield", "dns-resolver"]
sources:
  - https://blog.cloudflare.com/argo-intelligent-routing/
  - https://datatracker.ietf.org/doc/html/rfc7871
  - https://blog.cloudflare.com/cloudflare-connected-in-over-300-cities/
---

import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import TraceScenario from "~/components/pedagogy/TraceScenario.astro";
import NumbersCard from "~/components/layout/NumbersCard.astro";

<Hook>
Вы хотите, чтобы каждый пользователь — из Лондона, Токио или Сан-Паулу — автоматически попадал на ближайший CDN-сервер, без ручной настройки маршрутов или отдельных URL на регион. Два механизма решают это: Anycast и GeoDNS. Они работают по-разному и часто используются вместе.
</Hook>

<Crux>Anycast направляет BGP к ближайшему POP из одного IP; GeoDNS возвращает регион-специфичный IP — proximity-маршрутизация с разными trade-off.</Crux>

<Explanation>

## Anycast IP-маршрутизация

При **Anycast** CDN-оператор анонсирует **один и тот же IP-адрес** (например, `93.184.216.1`) из нескольких географических локаций одновременно через BGP. BGP-маршрутизация выбирает «ближайшую» edge-локацию по длине AS-пути или количеству хопов. Когда пользователь делает запрос, его локальный роутер отправляет его к ближайшему POP, анонсирующему этот IP — никакого специального DNS не нужно, только стандартная BGP-маршрутизация.

**Плюс:** работает автоматически; любое изменение на сетевом уровне (POP добавлен, отозван) вступает в силу за время BGP-конвергенции (секунды-минуты). **Минус:** BGP «ближайший» — это ближайший по хопам, а не по задержке. Два города, одинаково близких по хопам, могут различаться на 50 мс реальной задержки.

## GeoDNS

При **GeoDNS** авторитативный nameserver CDN возвращает **разные A-записи** в зависимости от географического положения резолвера. Запрос от лондонского резолвера получает лондонский edge IP; токийский резолвер — токийский. Это даёт оператору явный контроль над маршрутизацией — можно вручную направлять разные регионы на разные POP.

**Резолвер vs. пользователь.** GeoDNS видит IP **резолвера**, а не браузера. Пользователь в Торонто, использующий резолвер Google 8.8.8.8, получает IP для того региона, из которого видно DNS-сервер Google — часто Mountain View, CA — что может направить его на US POP вместо торонтского. Это неоптимально.

**EDNS Client Subnet (ECS)** частично решает это: резолверы, поддерживающие ECS, отправляют /24-префикс подсети пользователя вместе с DNS-запросом, позволяя авторитативному серверу вернуть географически точный IP. Цена: префикс подсети пользователя путешествует по всей цепочке DNS-делегирования, снижая приватность. Apple iCloud Private Relay и Cloudflare 1.1.1.1 отключают ECS по этой причине, принимая слегка неоптимальную маршрутизацию.

<NumbersCard
  id="card-anycast-geodns"
  title="Сравнение Anycast vs. GeoDNS"
  data-lesson-visual
  stats={[
    { label: "Основа выбора Anycast", value: "Длина BGP AS-пути (количество хопов)" },
    { label: "Основа выбора GeoDNS", value: "Геолокация резолвера" },
    { label: "BGP-конвергенция Anycast при смене POP", value: "Секунды-минуты" },
    { label: "TTL GeoDNS (типичный)", value: "30–60 с для быстрого переключения" },
    { label: "Гранулярность EDNS Client Subnet", value: "/24-префикс (подсеть пользователя)" },
    { label: "Экономия p95 при Cloudflare Argo", value: "30–50% на межконтинентальных путях" },
  ]}
/>

## Как CDN комбинирует оба механизма

Большинство крупных CDN используют Anycast на IP-уровне и GeoDNS поверх него, объединяя их преимущества. Anycast хорошо поглощает DDoS-атаки — объёмные атаки распределяются по всем POP, анонсирующим IP. GeoDNS обеспечивает явный failover: отзываете POP из DNS при сбое — новые соединения перестают приходить в течение одного DNS TTL.

<Inset kind="why" lang="ru">
**Почему Anycast один недостаточен для CDN.** Anycast маршрутизирует по BGP-метрикам, а не по реальной задержке. ISP пользователя может пировать с CDN POP, который географически далёк, но имеет короткий AS-путь. Cloudflare Argo Smart Routing и AWS Global Accelerator добавляют слой измерений: они непрерывно зондируют реальную RTT-задержку со многих POP и направляют трафик через **приватный backbone** (в обход публичного интернета) к POP с наименьшей задержкой. Типичная экономия: 30–50% снижение p95-задержки на межконтинентальных хопах. Цена: повышенная стоимость за ГБ за backbone-транзит. Оправдано для latency-sensitive API; обычно избыточно для доставки статических ассетов.
</Inset>

<Quiz
  id="mid-quiz-1"
  pieceSlug="02-anycast-and-geodns"
  lang="ru"
  question="Пользователь в Торонто использует 8.8.8.8 как DNS-резолвер. Почему он может попасть на CDN POP в западной части США вместо торонтского?"
  choices={[
    { label: "ISP блокирует канадский POP", misconception: "ISP-блокировки не связаны с этой проблемой маршрутизации." },
    { label: "GeoDNS видит IP резолвера Google (из Mountain View, CA) вместо торонтского IP пользователя и возвращает адрес US POP", correct: true },
    { label: "Anycast работает только в США", misconception: "Anycast — глобальная схема маршрутизации." },
    { label: "Торонтские POP не поддерживают HTTPS", misconception: "Поддержка HTTPS не связана с выбором POP." },
  ]}
/>

<Quiz
  id="mid-quiz-2"
  pieceSlug="02-anycast-and-geodns"
  lang="ru"
  question="Какое главное ограничение чистой Anycast-маршрутизации для CDN proximity?"
  choices={[
    { label: "Работает только с IPv6", misconception: "Anycast работает и с IPv4, и с IPv6." },
    { label: "BGP выбирает POP с кратчайшим AS-путём, который может не быть ближайшим по задержке — POP с 10 хопами может иметь худшую задержку, чем с 12", correct: true },
    { label: "Не может работать с TLS-соединениями", misconception: "TLS не зависит от маршрутизации; Anycast отлично обрабатывает TCP/TLS." },
    { label: "Требует смены DNS-записей для каждого пользователя", misconception: "Весь смысл Anycast — в отсутствии per-user DNS-кастомизации." },
  ]}
/>

<TraceScenario
  id="mid-trace-1"
  pieceSlug="02-anycast-and-geodns"
  lang="ru"
  scenario="Трассировка: как лондонский пользователь попадает на правильный CDN edge через GeoDNS."
  steps={[
    { prompt: "Браузер резолвит cdn.example.com. До какого DNS-сервера в итоге доходит запрос?", reveal: "До авторитативного nameserver CDN, после того как рекурсивный резолвер прошёл цепочку DNS-делегирования: root → TLD → NS-записи CDN." },
    { prompt: "Авторитативный сервер видит запрос с IP лондонского резолвера. Что он возвращает?", reveal: "A-запись с IP лондонского (или европейского) CDN edge POP — не IP калифорнийского origin." },
    { prompt: "Браузер подключается к этому лондонскому edge IP через TCP + TLS. В чём преимущество?", reveal: "TCP и TLS хендшейки завершаются за ~5–10 мс (локальный лондонский RTT) вместо ~100–200 мс (Калифорния). Обмен ключами TLS и проверка сертификата происходят на edge, а не на origin." },
    { prompt: "Через 6 месяцев оператор выводит лондонский POP на обслуживание. Как долго новые пользователи будут попадать туда?", reveal: "В течение одного DNS TTL — обычно 30–60 секунд для CDN-записей. Существующие TCP-соединения остаются на старом POP до закрытия; новые DNS-резолюции начинают возвращать альтернативный POP." },
  ]}
/>

<DragOrder
  id="mid-drag-1"
  pieceSlug="02-anycast-and-geodns"
  lang="ru"
  prompt="Упорядочите путь запроса при Anycast-маршрутизации (без GeoDNS):"
  items={[
    "Браузер отправляет DNS-запрос — резолвер идёт к авторитативному NS CDN",
    "Авторитативный NS возвращает единственный Anycast IP (одинаковый для всех регионов)",
    "TCP SYN-пакет браузера выходит в интернет с этим IP назначения",
    "ISP-роутеры применяют BGP-форвардинг: направляют к CDN AS с кратчайшим путём",
    "Пакет прибывает в CDN POP с кратчайшим BGP-путём от этого ISP",
    "TLS хендшейк + HTTP-запрос завершаются на этом POP",
  ]}
/>

</Explanation>

<KeyTakeaway>Anycast маршрутизирует по количеству BGP-хопов (автоматически, устойчиво к DDoS, но не оптимально по задержке); GeoDNS маршрутизирует по географии резолвера (явно, быстро обновляется, но ограничен точностью геолокации резолвера). Умная маршрутизация (Argo, Global Accelerator) добавляет реальные замеры задержки поверх обоих.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="02-anycast-and-geodns-retrieval"
  lang="ru"
  questions={[
    { q: "Объясните двумя предложениями, почему Anycast и GeoDNS решают одну проблему, но ломаются по-разному.", a: "Anycast маршрутизирует по длине BGP AS-пути — это прокси для задержки, но не точный: POP, близкий по хопам, может быть далёким по задержке из-за соглашений о пиринге или перегрузки. GeoDNS маршрутизирует по геолокации резолвера — это прокси для местоположения пользователя, но не точный: пользователь в Торонто с резолвером Google из Mountain View попадает на US POP вместо канадского." },
    { q: "Что делает EDNS Client Subnet и какова цена за приватность?", a: "ECS заставляет рекурсивный резолвер включать /24-префикс подсети пользователя в DNS-запрос, чтобы авторитативный сервер CDN мог вернуть географически точный адрес POP, а не маршрутизировать по местоположению резолвера. Цена: префикс сети пользователя виден каждому DNS-серверу в цепочке делегирования — root, TLD, авторитативному — а не остаётся внутри резолвера." },
    { q: "CDN-оператор обнаруживает высокие потери пакетов в лондонском POP. Как остановить попадание новых пользователей туда и как быстро это сработает?", a: "Отзывают DNS-запись лондонского POP (GeoDNS) или его BGP Anycast-анонс (Anycast-маршрутизация). При GeoDNS новые запросы прекращают приходить в течение одного DNS TTL — обычно 30–60 секунд для CDN-записей. При отзыве BGP конвергенция занимает секунды-несколько минут в зависимости от AS-пропагации. Существующие соединения на POP продолжают работать до естественного закрытия." },
  ]}
/>

<Recap lang="ru">
Anycast и GeoDNS — два механизма proximity-маршрутизации, которые CDN использует для направления каждого пользователя к ближайшему edge-серверу. Anycast анонсирует один IP из всех POP и позволяет BGP выбрать ближайший по хопам — автоматически и устойчиво к DDoS, но не идеально по задержке. GeoDNS возвращает разные A-записи по географии резолвера — явно и быстро меняется, но точен ровно настолько, насколько местоположение резолвера совпадает с местоположением пользователя. Большинство CDN комбинируют оба: Anycast для масштабирования и поглощения DDoS, GeoDNS для управления failover, и опционально умную маршрутизацию (Cloudflare Argo, AWS Global Accelerator) через приватный backbone для коррекции слепого пятна BGP по задержке.
</Recap>
