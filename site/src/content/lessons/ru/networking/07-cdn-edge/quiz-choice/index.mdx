---
concepts:
- cache-key
- vary-header
- origin-shield
- stale-while-revalidate
- anycast
- edge-worker
estMin: 13
lang: ru
level: senior
order: 7
prereqs: []
slug: quiz-choice
sources:
- https://datatracker.ietf.org/doc/html/rfc9111
- https://datatracker.ietf.org/doc/html/rfc5861
- https://developers.cloudflare.com/workers/platform/limits/
status: ready
summary: "Тест с выбором на синтез по всему юниту CDN и edge — proximity-маршрутизация, cache key, многоуровневый кеш, защита от stampede, edge-вычисления и production-сбои."
title: 'CDN и edge: тест с выбором ответа'
track: networking
unit: 07-cdn-edge
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Шесть вопросов поперёк всего юнита. Ни один не про заучивание определений — каждый отражает решение по маршрутизации, кешированию или композиции, которое ты принимаешь, пока дашборд горит.
</Hook>

<Goal lang="ru">
Убедись, что связываешь proximity-маршрутизацию, построение cache key, многоуровневый кеш, защиту от stampede и edge-вычисления — тот синтез, к которому вели отдельные уроки.
</Goal>

<Quiz
  id="07-cdn-edge-choice-1"
  lessonSlug="07-cdn-edge"
  lang="ru"
  question="Сайт ставит Cache-Control: public, max-age=3600, но hit rate CDN держится на 5% с тысячами разных записей на один URL. Самая вероятная единственная причина?"
  choices={[
    { label: "Заголовок Vary: User-Agent (или Vary: Cookie) дробит каждый URL на одну запись на браузер/версию/ОС или на сессию — почти ни одна не переиспользуется. Убери Vary-измерение высокой кардинальности.", correct: true },
    { label: "max-age=3600 слишком короткий, записи истекают до переиспользования.", misconception: "Часа с запасом хватает для переиспользования на нагруженном URL; истечение снизило бы hit rate равномерно, а не взорвало число записей до тысяч на URL. Тысячи записей на URL — сигнатура Vary-footgun." },
    { label: "Origin shield выключен, поэтому edge чаще промахиваются.", misconception: "Отсутствие shield повышает нагрузку на origin на промахах, но не множит число записей на URL. Тысячи записей на один URL прямо указывают на Vary-заголовок высокой кардинальности." },
    { label: "Anycast маршрутизирует пользователей на неправильный POP.", misconception: "Выбор POP влияет на латентность, а не на число слотов кеша у URL. Симптом «тысячи записей на URL» — это фрагментация cache key, то есть Vary." },
  ]}
/>

<Quiz
  id="07-cdn-edge-choice-2"
  lessonSlug="07-cdn-edge"
  lang="ru"
  question="Залогиненный пользователь открывает /account и видит своё имя; через час другой анонимный пользователь идёт по тому же URL и видит имя первого. Логи CDN показывают cache hit у обоих. Корень?"
  choices={[
    { label: "Эндпоинт читает session cookie и возвращает персонализированный HTML, но не помечен Cache-Control: private (и нет Vary: Cookie). Cookie не входят в дефолтный cache key, поэтому первый ответ закешировался по одному URL и реплеится всем.", correct: true },
    { label: "Anycast направил обоих на один POP, и они разделили соединение.", misconception: "Общий POP не означает общие тела ответов — каждый запрос кеширируется независимо. Утечка в том, что cookie нет в cache key, а ответ не помечен private." },
    { label: "Session cookie истёк, и CDN подставил чужую сессию.", misconception: "CDN не подменяет сессии. Персонализированный ответ просто закешировался по cookie-слепому ключу и отдался следующему, кто запросил этот URL." },
    { label: "stale-while-revalidate отдал устаревшую копию чужого пользователя.", misconception: "SWR управляет свежестью во времени для одной записи по ключу; он никогда не пересекает пользователей. Дефект — что cookie-читающий эндпоинт вообще кешируется на общем CDN." },
  ]}
/>

<Quiz
  id="07-cdn-edge-choice-3"
  lessonSlug="07-cdn-edge"
  lang="ru"
  question="Запись популярной статьи на edge истекает на 200 POP региона одновременно; 1000 пользователей запрашивают её в эту секунду, origin начинает отдавать 503, и эти 503 кешируются. Какие два слоя вместе предотвратили бы это?"
  choices={[
    { label: "Origin shield схлопывает все 200 edge-промахов региона в один запрос к origin; stale-while-revalidate отдаёт устаревшую копию каждому, пока идёт одна фоновая ревалидация. Вместе: один запрос к origin, ни одного видимого промаха.", correct: true },
    { label: "Больший max-age плюс Vary: Accept-Encoding.", misconception: "Больший max-age лишь отодвигает тот же синхронный обрыв истечения, а Accept-Encoding не делает ничего против stampede. Фикс — схлопнуть одновременные промахи (shield) и отдавать stale на ревалидации (SWR)." },
    { label: "Переключение с GeoDNS на Anycast-маршрутизацию.", misconception: "Маршрутизация выбирает, какой POP ответит; она не меняет, что происходит при истечении записи. Защита от stampede — забота уровня кеша, а не маршрутизации." },
    { label: "Пометить статью Cache-Control: private.", misconception: "private полностью отключает общий кеш, отправляя каждый запрос на origin — противоположность защите от stampede. От стада origin берегут SWR плюс origin shield." },
  ]}
/>

<Quiz
  id="07-cdn-edge-choice-4"
  lessonSlug="07-cdn-edge"
  lang="ru"
  question="Странице товара нужны стабильный на час chrome, описание со staleness до 10 минут и персональная цена в реальном времени. no-store даёт p95 300 мс; max-age=3600 показывает устаревшие цены. Senior-ход?"
  choices={[
    { label: "Edge-side composition: кешируй chrome и описание на edge с длинными TTL, а edge worker подтягивает фрагмент цены свежим на каждый запрос (~30 мс) и вставляет через HTMLRewriter или ESI. Экономия кеша на статичной массе, точность в реальном времени на единственном волатильном фрагменте.", correct: true },
    { label: "no-store на всю страницу — корректность важнее латентности.", misconception: "no-store выбрасывает те 90%+ байт, что отлично кешируются, ради свежести одного фрагмента. Собирай страницу из фрагментов с разными TTL вместо кеширования на одной гранулярности." },
    { label: "max-age=5, чтобы цена была устаревшей максимум на 5 секунд.", misconception: "TTL 5 с на всю страницу всё равно отдаёт устаревшую цену до 5 с и постоянно перетягивает всё, долбя origin. Реальному времени цены нужен свежий фрагмент, а не короткий TTL на всю страницу." },
    { label: "Положить цену в Vary, чтобы у каждой цены была своя запись.", misconception: "Vary кеширует по заголовкам запроса, а не по данным ответа, и запись на каждую цену стала бы некешируемым шумом. Цену тянет edge worker вживую, а не кодирует в cache key." },
  ]}
/>

<Quiz
  id="07-cdn-edge-choice-5"
  lessonSlug="07-cdn-edge"
  lang="ru"
  question="Чувствительный к латентности API на межконтинентальном пути медленный, хотя Anycast уже шлёт пользователей на BGP-ближайший POP. Почему и каков точечный фикс?"
  choices={[
    { label: "BGP «ближайший» означает кратчайший AS-path, а не наименьшую латентность — близкий по хопам POP может быть далёким по латентности из-за пиринга или конгестии. Слой smart routing (Cloudflare Argo, AWS Global Accelerator) измеряет реальный RTT и маршрутизирует по приватному backbone, срезая p95 на ~30–50% на межконтинентальных хопах.", correct: true },
    { label: "Anycast сломан — переанонсируй префикс из большего числа городов.", misconception: "Больше POP не лечит латентную слепоту BGP; метрика AS-path всё равно игнорирует реальный RTT. Нужен слой маршрутизации на измерениях поверх приватного backbone, а не больше анонсов." },
    { label: "Переключить API на GeoDNS, чтобы регион выбирал резолвер.", misconception: "GeoDNS маршрутизирует по местоположению резолвера (часто провайдера публичного DNS, а не пользователя) и не более латентно-осознан, чем Anycast. Фикс — активное измерение латентности плюс backbone-транзит." },
    { label: "Поднять TTL кеша на ответах API.", misconception: "Больший TTL помогает кешируемым ответам, но ничего не даёт динамическим, чувствительным к латентности вызовам, которые должны дойти до бэкенда. Проблема — качество транзитного пути, его решает smart routing." },
  ]}
/>

<Quiz
  id="07-cdn-edge-choice-6"
  lessonSlug="07-cdn-edge"
  lang="ru"
  question="Ты выкатываешь хотфикс; origin обновлён, но через 20 минут Европа отдаёт старую версию, а Азия — новую. Почему расхождение и каков долговременный фикс?"
  choices={[
    { label: "У каждого edge свой кеш с независимым возрастом TTL — азиатский edge оказался близко к истечению и перетянул, у Европы TTL ещё оставался. Добавь автоматический post-deploy purge (по шаблону URL или cache-tag) в пайплайн, чтобы все регионы инвалидировались в момент обновления origin.", correct: true },
    { label: "Европейский POP на более старой версии ПО CDN.", misconception: "Версия ПО не определяет свежесть кеша; её определяет независимый возраст TTL на каждом edge. Без purge по деплою каждый edge ждёт истечения своего TTL." },
    { label: "Anycast шлёт европейских пользователей на устаревший origin.", misconception: "Origin один и уже обновлён; Anycast выбирает edge, а не origin. Расхождение — состояние кеша на каждом edge, лечится purge по деплою, а не маршрутизацией." },
    { label: "stale-if-error отдаёт старую европейскую копию.", misconception: "stale-if-error срабатывает только когда origin отдаёт 5xx; здесь origin здоров. Расхождение — обычный дрейф TTL по edge, решается автоматическим purge в пайплайне деплоя." },
  ]}
/>

<Recap lang="ru">
Сквозная линия юнита — один конвейер доставки: маршрутизация (Anycast для масштаба и поглощения DDoS, GeoDNS для контроля, smart routing для коррекции латентной слепоты BGP) ставит пользователя на правильный edge; cache key (URL + метод + Vary) решает, что переиспользуемо, и Vary высокой кардинальности или cookie-слепые ключи — классические footgun; многоуровневый кеш плюс origin shield и stale-while-revalidate берегут origin от стада; edge workers и edge-side composition собирают фрагменты с разными TTL ради быстрых страниц со свежими данными; а эксплуатация замыкает цикл cache-tag purge, инвалидацией по деплою и пер-региональной наблюдаемостью.
</Recap>
