---
concepts:
- vary-header
- cache-key
- cache-poisoning
- accept-encoding
- accept-language
- cookie-caching
- faded-example
deepensInto:
- 05-stale-while-revalidate-and-stampede
- 07-cdn-operations-and-observability
estMin: 12
lang: ru
lessonType: topic
level: middle
order: 4
prereqs:
- 06-http-versions/04-headers-caching-cors
- 07-cdn-edge/03-tiered-cache-and-cache-control
slug: 04-vary-header-and-cache-keys
sources:
- https://datatracker.ietf.org/doc/html/rfc7234
- https://datatracker.ietf.org/doc/html/rfc9111
- https://portswigger.net/research/practical-web-cache-poisoning
spiral:
- latency
status: ready
summary: 'Как строятся cache keys в CDN, почему Vary: User-Agent уничтожает hit rate,
  и почему отсутствующий Vary: Cookie может отдать страницу одного пользователя другому.'
title: Заголовок Vary и cache keys
track: networking
unit: 07-cdn-edge
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import FadedExample from "~/components/pedagogy/FadedExample.tsx";
import Misconception from "~/components/layout/Misconception.astro";
import NumbersCard from "~/components/layout/NumbersCard.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";
import Infographic from "~/components/diagram/Infographic.astro";
import varyHitRate from "./infographics/vary-hit-rate.svg?raw";

<Hook>
CDN hit rate 5% несмотря на max-age=3600. Dashboard показывает тысячи уникальных записей кеша на URL. Где-то в заголовках ответа origin есть строка Vary, которая молча фрагментирует каждый URL на тысячи отдельных cache keys — по одному на каждую уникальную комбинацию заголовков браузера. Это самая распространённая мисконфигурация CDN в production.
</Hook>

<Crux>Cache key в CDN: URL + метод + заголовки из Vary; Vary: User-Agent создаёт слот на каждый браузер, обрушивая hit rate к нулю.</Crux>

<Explanation>

## Как строится cache key

CDN-кеш хранит ответы, индексированные по **cache key**. Ключ по умолчанию: `(URL, HTTP method, Host)`. Заголовок ответа `Vary` расширяет ключ дополнительными измерениями запроса:

```
Cache-Control: public, max-age=3600
Vary: Accept-Encoding, Accept-Language
```

Это говорит кешу: хранить **отдельные записи** для каждого уникального значения `Accept-Encoding` и `Accept-Language`. Запрос `/page` с `Accept-Language: en` и другой с `Accept-Language: ru` создают две отдельные записи кеша — обе правильные языковые версии раздаются из кеша.

**RFC 7234 §4.1** формализует это: «Кеш ДОЛЖЕН использовать значение заголовка `Vary` для определения, может ли хранимый ответ использоваться для данного запроса».

## Ловушка Vary

`Vary` работает корректно для `Accept-Encoding` (gzip vs brotli — ~5 уникальных значений) и `Accept-Language` (~10–50 значений на сайт). Но у некоторых заголовков тысячи или миллионы уникальных значений:

| Заголовок Vary | Уникальных значений | Эффект |
|----------------|---------------------|--------|
| `Accept-Encoding` | ~5 (gzip, br, identity...) | Безопасен — небольшая фрагментация |
| `Accept-Language` | ~50 на типичный сайт | Обычно нормально |
| `User-Agent` | Тысячи (каждый браузер × версия × ОС) | **Кеш уничтожен** |
| `Cookie` | На пользователя (миллионы) | **Эффективно некешируемо** |
| `Authorization` | На авторизованного пользователя | **Эффективно некешируемо** |

`Vary: User-Agent` — классическая ловушка: разработчики добавляют его, чтобы отдавать разный HTML для мобильных и десктопных. Результат: одна запись кеша на каждую уникальную строку User-Agent — каждый релиз Chrome, каждая версия iOS, каждая сборка Safari. Кеш заполняется уникальными записями, которые никогда не используются повторно; hit rate падает к нулю. Когда видишь hit rate ниже 20% на «статической» странице — первым делом смотри на Vary: в девяти из десяти случаев туда пробрался высококардинальный заголовок.

<NumbersCard
  id="card-vary-impact"
  title="Влияние Vary на cache hit rate"
  data-lesson-visual
  stats={[
    { label: "Нет Vary (или только Vary: Accept-Encoding)", value: "90–98% hit rate (нормальная работа)" },
    { label: "Добавлен Vary: Accept-Language (10 значений)", value: "80–90% — небольшая фрагментация" },
    { label: "Добавлен Vary: User-Agent", value: "<10% — почти нулевое кеширование" },
    { label: "Добавлен Vary: Cookie", value: "~0% — записи на пользователя никогда не переиспользуются" },
    { label: "Добавлен Vary: Authorization", value: "~0% — записи на токен никогда не переиспользуются" },
  ]}
/>

<Infographic
  svg={varyHitRate}
  label="Столбчатая диаграмма доли попаданий в кэш CDN в зависимости от заголовка Vary: без Vary или только Accept-Encoding держится ~95%, добавление Accept-Language снижает до ~85%, добавление User-Agent обрушивает до ~8%, а добавление Cookie или Authorization падает до ~1%."
  caption="Доля попаданий обратна кардинальности заголовка: низкокардинальные измерения безопасны, но User-Agent и Cookie дробят один URL на почти уникальные ключи, которые никогда не переиспользуются."
  hue="var(--d-infra)"
/>

<FadedExample
  client:load
  id="fade-cache-key"
  pieceSlug="04-vary-header-and-cache-keys"
  lang="ru"
  title="Сюрпризы cache key"
  steps={{
    solved: (
      <div class="space-y-4">
        <p><strong>Сценарий: один URL, разные Accept-Language — Vary выставлен корректно.</strong></p>
        <ul class="list-disc list-inside space-y-2 text-sm mb-4">
          <li><strong>Шаг 1:</strong> Пользователь A (Лондон) запрашивает <code>/api/products</code>. Accept-Language: en. Cache miss в CDN. Origin отвечает: <code>Cache-Control: max-age=3600, Vary: Accept-Language</code>.</li>
          <li><strong>Шаг 2:</strong> CDN сохраняет запись с ключом: [URL=/api/products, Accept-Language=en].</li>
          <li><strong>Шаг 3:</strong> Пользователь B (тоже Лондон) запрашивает <code>/api/products</code>. Accept-Language: ru. Ключ [URL=/api/products, Accept-Language=ru] НЕ существует. Cache miss → запрос к origin → русские продукты закешированы.</li>
          <li><strong>Результат:</strong> Оба пользователя видят правильный язык. Vary обеспечивает отдельные корректные записи кеша.</li>
        </ul>
      </div>
    ),
    semi: {
      prompt: "Тот же сценарий. Origin отвечает Cache-Control: max-age=3600, но ЗАБЫВАЕТ добавить Vary: Accept-Language. Пользователь A запрашивает на en, пользователь B на ru. Что видит пользователь B?",
      blanks: [
        {
          id: "bug",
          expected: "английский",
          placeholder: "Какой язык увидит пользователь B? (английский/русский/ошибка)",
        },
      ],
    },
    blank: {
      prompt: "Авторизованный пользователь открывает /account, видит своё имя и email. CDN показывает cache hit. Через час другой пользователь (не авторизованный) заходит на тот же /account и видит... имя и email первого пользователя. Логи CDN показывают cache hit для обоих. Что забыл инженер?",
      reveal: (
        <div class="space-y-2">
          <p><strong>Ответ: забыл пометить ответ как private или добавить Vary: Cookie.</strong></p>
          <p class="text-sm">Endpoint читает session cookie и возвращает персонализированный контент. Cookies НЕ входят в cache key по умолчанию. Первый запрос кешируется как [URL=/account, method=GET] с персонализированным контентом. Второй запрос (другой пользователь, другой session_id cookie) совпадает с тем же cache key — персонализированный контент раздаётся не тому пользователю.</p>
          <p class="text-xs text-gray-600 mt-2"><strong>Исправление:</strong> Добавить Cache-Control: private (минует CDN, только браузерный кеш) или Vary: Cookie. Современная практика предпочитает private для всех user-specific endpoints.</p>
        </div>
      ),
    },
  }}
  misconceptions={{
    "bug:русский": "Не совсем. CDN считает оба запроса эквивалентными по кешу (один URL, нет заголовка Vary). Первый ответ (английский) закеширован и привязан только к URL. Второй запрос (русский Accept-Language) совпадает с тем же ключом и получает закешированный английский ответ.",
  }}
/>

## Cookies не входят в cache key по умолчанию

Это самый важный с точки зрения безопасности факт про Vary. Cookies не являются частью cache key по умолчанию. Если endpoint читает session cookie и возвращает персонализированный контент:

- **Отсутствие `Cache-Control: private`** → CDN кеширует персонализированный ответ
- **Отсутствие `Vary: Cookie`** → разные пользователи с разными cookies получают один закешированный ответ

Результат: страница аккаунта одного пользователя раздаётся другому — **утечка данных**.

**Правило:** для любого endpoint, читающего cookie, задавайте `Cache-Control: private`. Используйте `Vary: Cookie` только если нужен CDN-кеш для контента, различающегося по cookie (редко, сложно, требует cookie с стабильным и низкокардинальным значением).

<Misconception
  id="r-cookies-not-in-key"
  title="Cookies не в cache key — помечайте user-endpoints как private"
>
CDN кеширует по URL + заголовкам из Vary. Cookies НЕ входят в ключ по умолчанию. Endpoint, читающий session cookie без Cache-Control: private, может отдать данные одного пользователя другому. Проверяйте роут: читает cookie? Ставьте private или no-store.
</Misconception>

<Inset kind="edgecase" lang="ru">
**Cache poisoning через некешируемые заголовки.** Исследование Джеймса Кеттла (PortSwigger, 2018) задокументировало, как CDN можно обмануть, заставив кешировать вредоносный ответ. Если origin читает заголовок запроса (например, `X-Forwarded-Host`) для формирования тела ответа, но этот заголовок НЕ входит в cache key CDN (нет в Vary), атакующий может отправить запрос с `X-Forwarded-Host: evil.com` — origin возвращает страницу со ссылками на evil.com, CDN кеширует её только по URL и раздаёт отравленную страницу всем последующим пользователям. Защита: согласовать CDN и origin, какие заголовки значимы для ключа; удалять нераспознанные forwarding-заголовки на edge CDN; использовать нормализацию cache key для отброса незначимых query-параметров.
</Inset>

<Quiz
  id="mid-quiz-1"
  pieceSlug="04-vary-header-and-cache-keys"
  lang="ru"
  question="Что делает Vary: Accept-Encoding в кеше CDN?"
  choices={[
    { label: "Велит кешу закодировать ответ перед хранением", misconception: "Кодирование определяется заголовком ответа Content-Encoding, а не Vary." },
    { label: "Велит кешу хранить отдельные записи для каждого уникального значения заголовка запроса Accept-Encoding", correct: true },
    { label: "Отключает кеширование для этого ресурса", misconception: "Vary параметризует кеширование по key-измерениям — не отключает его." },
    { label: "Заставляет клиентов отправлять сжатые запросы", misconception: "Клиенты сами выбирают Accept-Encoding; Vary влияет на то, как кеш привязывает ответы к ключам." },
  ]}
/>

<Quiz
  id="mid-quiz-2"
  pieceSlug="04-vary-header-and-cache-keys"
  lang="ru"
  question="Каков практический эффект Vary: Authorization в shared CDN-кеше?"
  choices={[
    { label: "Более быстрые ответы для авторизованных пользователей", misconception: "Vary: Authorization почти устраняет кеширование для авторизованных пользователей, не улучшает его." },
    { label: "Кеш фрагментируется по каждому уникальному значению Authorization — обычно одна запись на пользователя, что уничтожает эффективность кеша", correct: true },
    { label: "Запрещает неавторизованным пользователям читать кеш", misconception: "Vary: Authorization создаёт записи на пользователя; не защищает их от раздачи другим." },
    { label: "Добавляет транспортное шифрование", misconception: "Заголовки не выполняют шифрование — это роль TLS." },
  ]}
/>

<DragOrder
  id="mid-drag-1"
  pieceSlug="04-vary-header-and-cache-keys"
  lang="ru"
  prompt="Упорядочите значения Vary от наиболее безопасного (минимальная фрагментация) к наиболее опасному (максимальная):"
  items={[
    "Vary: Accept-Encoding — ~5 уникальных значений (gzip, brotli, identity, deflate, none)",
    "Vary: Accept-Language — ~10–50 значений на типичный сайт",
    "Vary: Accept — согласование типа контента, ~5–10 значений",
    "Vary: User-Agent — тысячи уникальных значений по браузерам и версиям",
    "Vary: Cookie — потенциально миллионы уникальных значений (по одному на активную сессию)",
  ]}
/>

</Explanation>

<FlowDiagram
  label="Один URL вместе с заголовком Vary распадается на отдельную запись кеша для каждого уникального значения заголовка; низкокардинальные заголовки безопасны, высококардинальные разрушают кеш"
  hue="var(--d-network)"
  perRow={3}
  nodes={[
    { id: "url", label: "GET /page", sub: "+ заголовок Vary" },
    { id: "en", label: "Запись кеша", sub: "Accept-Language: en" },
    { id: "ru", label: "Запись кеша", sub: "Accept-Language: ru" },
    { id: "ua", label: "Тысячи записей", sub: "Vary: User-Agent" },
  ]}
  edges={[
    { from: "url", to: "en", label: "ключ 1" },
    { from: "url", to: "ru", label: "ключ 2" },
    { from: "url", to: "ua", label: "ловушка" },
  ]}
  caption="Cache key = URL + метод + каждый заголовок из Vary. Vary: Accept-Language даёт одну запись на язык (безопасно, ~10–50). Vary: User-Agent даёт запись на каждую строку браузер×версия×ОС — тысячи почти уникальных ключей, которые никогда не переиспользуются, обрушивая hit rate."
/>

<KeyTakeaway>Cache key CDN = URL + метод + заголовки из Vary. Vary: Accept-Encoding и Accept-Language безопасны (малая кардинальность). Vary: User-Agent уничтожает hit rate (тысячи уникальных значений). Отсутствие Vary: Cookie или Cache-Control: private на user-specific endpoints может отдать ответ одного пользователя другому — проверяйте каждый новый роут, читающий cookie или auth-заголовки.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="04-vary-header-and-cache-keys-retrieval"
  lang="ru"
  questions={[
    { q: "Объясните, почему Vary: Accept-Encoding обязателен для endpoint, отдающего gzip-ответы одним клиентам и несжатые — другим.", a: "Без Vary: Accept-Encoding CDN привязывает ответы к ключу только URL + метод. Первый закешированный ответ (например, gzip-версия для клиента с поддержкой gzip) отдаётся всем последующим клиентам, включая тех, кто не объявлял поддержку gzip. Эти клиенты получают gzip-тело и пытаются отрендерить его как обычный текст — страница ломается. Vary: Accept-Encoding говорит кешу хранить отдельные записи для каждого уникального значения заголовка Accept-Encoding, чтобы gzip и identity варианты хранились и раздавались корректно." },
    { q: "Страница статьи новостного сайта имеет Vary: User-Agent для разных макетов мобильных и десктопных. Cache hit rate 3%. В чём корневая причина и как исправить?", a: "User-Agent имеет тысячи уникальных значений (каждый браузер × версия × ОС). Кеш создаёт запись на каждую уникальную строку User-Agent; поскольку большинство комбинаций встречается только один раз, почти ни одна запись не переиспользуется. Исправление: убрать Vary: User-Agent. Вместо этого — детектировать мобильный/десктоп на уровне CDN (например, Cloudflare Workers проверяет CF-Device-Type) и переписывать URL в /mobile/path или /desktop/path, или отдавать единственный responsive HTML с CSS media queries. Дифференциатором становится URL или низкокардинальный заголовок, а не сырой User-Agent." },
    { q: "Почему cookies не входят в cache key CDN по умолчанию и какой риск безопасности это создаёт?", a: "CDN по умолчанию привязывает ключ к URL + метод + заголовки из Vary. Cookies не в Vary по умолчанию, потому что их включение делало бы почти каждый user-specific ответ уникальной записью кеша (hit rate близок к нулю). Риск: endpoint, читающий session cookie и возвращающий персонализированный контент — без Cache-Control: private или Vary: Cookie — закеширует первый ответ по ключу только URL. Любой последующий пользователь, запрашивающий тот же URL, получает закешированный ответ первого пользователя. Это утечка данных. Исправление: помечать все endpoint, читающие cookie, как Cache-Control: private (нет CDN-кеша) или no-store." },
  ]}
/>

<Recap lang="ru">
Cache key CDN строится из URL, HTTP-метода, Host и заголовков запроса, перечисленных в `Vary` ответа. `Vary: Accept-Encoding` безопасен — ~5 возможных значений. `Vary: Accept-Language` обычно безопасен — ~10–50 значений. `Vary: User-Agent` катастрофичен — тысячи уникальных комбинаций браузер×версия×ОС, каждая становится отдельной записью кеша, которая никогда не переиспользуется, обрушивая hit rate к нулю. Cookies НЕ входят в ключ по умолчанию — это выбор в пользу эффективности, создающий риск безопасности: endpoints с персонализированным контентом без `Cache-Control: private` могут отдать ответ одного пользователя другому. Проверяйте каждый роут, читающий cookies или auth-заголовки, и помечайте его `private` или `no-store`. Теперь, когда выпускаешь новый endpoint, сделай аудит Vary частью определения «готово»: только низкокардинальные заголовки, и `private` на всё, что касается сессии.
</Recap>
