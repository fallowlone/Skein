---
concepts:
- anycast
- cache-key
- origin-shield
- stale-while-revalidate
- edge-worker
- cache-tags
estMin: 13
lang: ru
level: senior
order: 8
prereqs: []
slug: quiz-short
sources:
- https://datatracker.ietf.org/doc/html/rfc9111
- https://datatracker.ietf.org/doc/html/rfc7871
- https://blog.cloudflare.com/argo-intelligent-routing/
status: ready
summary: "Промпты на свободное припоминание по всему юниту CDN и edge. Ответь на каждый своими словами, затем открой эталонный ответ и сравни."
title: 'CDN и edge: тест на свободное припоминание'
track: networking
unit: 07-cdn-edge
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";

<Hook>
Припоминание сильнее перечитывания. На каждый промпт скажи или запиши полный ответ по памяти до того, как открыть эталон — именно усилие припоминания закрепляет материал.
</Hook>

<Goal lang="ru">
Восстанови ключевые механизмы юнита — Anycast против GeoDNS, построение cache key, origin shield, stale-while-revalidate, edge-композицию и точечный purge — не подглядывая в уроки.
</Goal>

<RetrievalDrawer
  client:visible
  id="07-cdn-edge-short-retrieval"
  lang="ru"
  questions={[
    { q: "Anycast и GeoDNS оба маршрутизируют пользователя на ближний edge. Объясни, как решает каждый и как они сбоят по-разному.", a: "Anycast анонсирует один IP с каждого POP и даёт BGP направить запрос на POP с кратчайшим AS-path — автоматически, устойчиво к DDoS (атака размазывается по всем POP), но BGP «ближайший» — это ближайший по хопам, а не по латентности, поэтому близкий по хопам POP может быть далёким по реальному RTT. GeoDNS возвращает разный A-record по геолокации резолвящего nameserver — явно и быстро переключается (TTL 30–60 с), но видит местоположение резолвера, а не пользователя: кто-то в Торонто на Google 8.8.8.8 может попасть на POP в США. Большинство CDN запускают оба (Anycast для масштаба, GeoDNS для failover) и добавляют smart routing, чтобы лечить латентную слепоту BGP." },
    { q: "Что такое EDNS Client Subnet, что он чинит и какова цена?", a: "ECS позволяет рекурсивному резолверу приложить /24-префикс подсети пользователя к DNS-запросу, чтобы authoritative-сервер CDN вернул POP по реальному местоположению пользователя, а не резолвера. Он чинит несоответствие резолвер-vs-пользователь в GeoDNS — например, пользователь в Торонто на публичном резолвере США теперь получит канадский POP. Цена — приватность: сетевой префикс пользователя становится виден каждому серверу в цепочке делегирования. Приватность-ориентированные резолверы (Cloudflare 1.1.1.1, Apple iCloud Private Relay) отключают ECS и принимают чуть худшую маршрутизацию." },
    { q: "Как строится cache key CDN и почему заголовок Vary: User-Agent — footgun?", a: "Дефолтный ключ — URL + HTTP-метод + Host; заголовок Vary в ответе расширяет его перечисленными измерениями заголовков запроса, поэтому кеш держит отдельную запись на каждое уникальное значение каждого Vary-заголовка. Vary: Accept-Encoding (~5 значений) и Accept-Language (~10–50) безопасны. У Vary: User-Agent тысячи уникальных значений (каждый браузер × версия × ОС), поэтому кеш заполняется одноразовыми записями, которые не переиспользуются, и hit rate падает почти до нуля. Чтобы отдавать mobile vs desktop, определяй устройство на edge и переписывай URL либо отдавай адаптивный HTML — никогда не кешируй по сырому User-Agent." },
    { q: "Что делает origin shield и сколько запросов к origin он порождает, когда 200 edge региона разом промахиваются на одном холодном URL?", a: "Origin shield — региональный промежуточный кеш между edge POP и origin: все edge региона воронкой проводят свои промахи через один shield-узел, который держит больший, более долгоживущий кеш. Когда 200 edge промахиваются по одному URL одновременно, они все форвардят на shield; shield делает один открытый запрос к origin, остальные 199 ждут, и единственный ответ origin раздаётся всем 199 плюс кешируется. Origin видит один запрос вместо 200 — shield первая линия защиты против thundering herd." },
    { q: "Объясни cache stampede и как stale-while-revalidate его инвертирует.", a: "Stampede случается, когда популярная запись кеша истекает и множество одновременных запросов промахиваются разом, каждый запуская свой fetch к origin; origin перегружен, может вернуть 5xx, и CDN может закешировать эти ошибки. Усиление примерно (одновременные запросы на истечении) × (время ответа origin). stale-while-revalidate=N инвертирует это: после истечения max-age edge мгновенно отдаёт устаревшую копию каждому запросу и шлёт ровно одну фоновую ревалидацию на origin. Все получают быстрый ответ; origin видит один запрос вместо тысяч. Компромисс в том, что контент может быть устаревшим до N секунд, поэтому ставь N по staleness, который терпит тип контента." },
    { q: "Опиши edge-side composition для страницы товара и почему она лучше одного TTL на всю страницу, и как cache-tag purge ложится на сайт из тысячи URL.", a: "Edge-side composition собирает страницу на POP из независимо кешируемых фрагментов через ESI или Worker + HTMLRewriter: chrome кешируется на часы, описание на минуты, а персональная цена тянется свежей edge worker'ом (~30 мс) и вставляется на каждый запрос. Это лучше одного TTL на всю страницу, потому что ни один TTL не подходит всем частям — короткий долбит origin, длинный показывает устаревшие цены. Cache-tag purge решает инвалидацию в масштабе: origin штампует ответы семантическими тегами (Cache-Tag: article-1001, category-tech), и правка делает purge по тегу за 1–5 с по всем POP, превращая O(URL) вызовов purge в O(тегов), обычно от одного до пяти." },
  ]}
/>

<Recap lang="ru">
Если ты смог восстановить каждый ответ по памяти, ты держишь хребет юнита: Anycast и GeoDNS (плюс ECS и smart routing) ставят пользователя на правильный edge с разными режимами сбоя; cache key (URL + метод + Vary) решает переиспользование, а Vary высокой кардинальности его уничтожает; origin shield и stale-while-revalidate берегут origin от стада на истечении; edge-side composition отдаёт быстрые страницы со свежими фрагментами; а cache-tag purge делает инвалидацию управляемой в масштабе.
</Recap>
