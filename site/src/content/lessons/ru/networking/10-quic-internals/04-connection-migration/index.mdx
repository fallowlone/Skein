---
concepts:
- connection-id
- path-validation
- path-challenge
- anti-amplification
- connection-migration
deepensInto:
- 07-deployment-and-cpu
estMin: 10
lang: ru
lessonType: topic
level: middle
order: 4
prereqs:
- 03-tcp-handshake/01-the-three-way-handshake
- 10-quic-internals/01-why-quic
- 10-quic-internals/03-handshake-and-1rtt
slug: 04-connection-migration
sources:
- https://datatracker.ietf.org/doc/html/rfc9000
- https://blog.cloudflare.com/the-road-to-quic/
- https://datatracker.ietf.org/doc/html/rfc9002
spiral:
- statefulness
- latency
status: ready
summary: QUIC идентифицирует соединения непрозрачным Connection ID вместо 4-тупла
  — соединения переживают смену IP-адреса (WiFi на мобильную) без переподключения.
title: Connection ID и миграция сети
track: networking
unit: 10-quic-internals
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import TraceScenario from "~/components/pedagogy/TraceScenario.astro";
import Misconception from "~/components/layout/Misconception.astro";
import SequenceDiagram from "~/components/diagram/SequenceDiagram.astro";
import Infographic from "~/components/diagram/Infographic.astro";
import antiAmplificationBudget from "./infographics/anti-amplification-budget.svg?raw";

<Hook>
Вы выходите из офиса, и телефон переключается с WiFi на LTE. Все TCP-соединения умирают — каждое приложение переподключается, проходит аутентификацию заново, перезагружается. QUIC делает этот переход невидимым для приложения.
</Hook>

<Crux>Connection ID QUIC отделяет идентичность сессии от сетевого адреса — соединения переживают смену IP без переподключения.</Crux>

<Explanation>

## Почему TCP ломается при смене IP

TCP-соединения идентифицируются **4-туплом**: IP клиента, порт клиента, IP сервера, порт сервера. Когда IP клиента меняется — WiFi на сотовую, переназначение NAT — 4-тупл ломается. Сервер видит пакеты из неизвестного источника и игнорирует их. Клиент должен начать новое TCP-рукопожатие, заново согласовать TLS и восстановить состояние приложения. Это занимает секунды и заметно пользователям.

## Connection ID отделяет идентичность от адреса

QUIC вводит **непрозрачный Connection ID** (до 20 байт, обычно 8), выбираемый сервером. Connection ID переносится в заголовке каждого QUIC-пакета. Сервер сопоставляет Connection ID с состоянием соединения, а не с IP-адресами.

При смене IP клиента:

1. Клиент отправляет QUIC-пакет с нового адреса с тем **же Connection ID**.
2. Сервер узнаёт Connection ID, фиксирует новый адрес источника.
3. Сервер отправляет фрейм **PATH_CHALLENGE** — случайный токен 8 байт.
4. Клиент отражает токен в фрейме **PATH_RESPONSE**.
5. Путь проверен — сервер обновляет текущий адрес соединения и разблокирует полную скорость отправки.

**Всё состояние потоков, окна управления потоком и состояние перегрузки переживают миграцию.** Соединение продолжается, как будто ничего не произошло. Вместе эти пять шагов означают: смена сети, которая вынудила бы TCP к полному переподключению, завершается в один PATH_CHALLENGE round trip; без шага 4 злоумышленник мог бы использовать это окно для reflection-атаки.

<div data-lesson-visual class="rounded-lg border border-slate-200 bg-slate-50 p-4 my-4 text-sm space-y-3">
  <div class="font-semibold text-slate-700">Timeline миграции WiFi на сотовую</div>
  <div class="space-y-1 font-mono text-xs">
    <div class="flex gap-2"><span class="text-slate-400 w-8">t=0</span><span>Клиент на WiFi: 192.168.1.100:54321, Connection ID 0x12345678</span></div>
    <div class="flex gap-2"><span class="text-slate-400 w-8">t=1с</span><span class="text-amber-600">Клиент переходит на LTE: 203.0.113.50:55555 (тот же Connection ID)</span></div>
    <div class="flex gap-2"><span class="text-slate-400 w-8">t=1с</span><span>Сервер: видит Connection ID 0x12345678 с нового адреса — отправляет PATH_CHALLENGE</span></div>
    <div class="flex gap-2"><span class="text-slate-400 w-8">t=1.1с</span><span>Клиент: отправляет PATH_RESPONSE — путь проверен</span></div>
    <div class="flex gap-2"><span class="text-slate-400 w-8">t=1.1с</span><span class="text-green-600">Все потоки возобновляются на полной скорости — без переподключения, без повторной аутентификации</span></div>
  </div>
</div>

## Антиамплификационный лимит

До проверки нового адреса клиента сервер может отправить **не более чем 3× байт, которые он получил** с этого адреса. Это предотвращает UDP-атаки амплификации: злоумышленник не может подделать адрес клиента, чтобы сервер отправил большие ответы жертве.

Клиенты должны отправлять Initial-пакеты, дополненные до минимум **1200 байт**, чтобы серверы имели пространство для ~3600-байтового ответа. После проверки PATH_RESPONSE сервер разблокирует полную скорость отправки.

<Infographic
  svg={antiAmplificationBudget}
  label="Столбчатая диаграмма лимита анти-усиления QUIC: принятый Initial клиента в 1200 байт с паддингом ограничивает ответный всплеск сервера до валидации тройным значением — около 3600 байт."
  caption="Правило 3x привязывает бюджет ответа сервера к фактически полученным байтам — поэтому поддельный адрес не выманит больше ~3600 байт в сторону жертвы, пока PATH_RESPONSE не докажет путь."
  hue="var(--d-infra)"
/>

<TraceScenario
  id="04-mid-trace-1"
  lessonSlug="04-connection-migration"
  lang="ru"
  scenario="Трассируйте клиента, переключающегося с WiFi на сотовую в середине соединения."
  steps={[
    { prompt: "Клиент на WiFi отправляет пакеты с Connection ID 0x12345678 с IP 192.168.1.100 порт 54321.", reveal: "Сервер фиксирует, что соединение находится по адресу 192.168.1.100:54321." },
    { prompt: "Клиент выходит на улицу, теряет WiFi, переключается на сотовую с новым IP 203.0.113.50 порт 55555.", reveal: "Новый IP:порт, но Connection ID в QUIC-пакете всё ещё 0x12345678." },
    { prompt: "Сервер получает пакет от 203.0.113.50:55555 с Connection ID 0x12345678. Что он делает?", reveal: "Сервер узнаёт Connection ID (соответствует активному соединению), но 4-тупл новый. Он отправляет фрейм PATH_CHALLENGE на 203.0.113.50:55555 для проверки нового адреса." },
    { prompt: "Клиент отвечает PATH_RESPONSE. Сервер сразу отправляет на полной скорости на новый адрес?", reveal: "Нет. До завершения проверки адреса сервер отправляет не более чем 3× байт, которые он получил (антиамплификация). После верификации PATH_RESPONSE сервер разблокирует полную скорость и обновляет запись текущего адреса соединения." },
    { prompt: "Всё состояние потоков, окна управления потоком и состояние перегрузки переживают миграцию. Почему?", reveal: "Потому что соединение идентифицируется Connection ID, а не 4-туплом. Все метаданные привязаны к соединению, поэтому перемещение соединения на новый путь перемещает все потоки вместе с ним." },
  ]}
/>

<Quiz
  id="04-mid-quiz-1"
  lessonSlug="04-connection-migration"
  lang="ru"
  question="В QUIC что предотвращает антиамплификационный лимит (правило 3×)?"
  choices={[
    { label: "Клиента от отправки данных больше, чем ожидает сервер", misconception: "Антиамплификация защищает от reflection-атак — потенциальный усилитель это сервер, а не клиент." },
    { label: "Злоумышленника на пути от подделки адреса клиента и принуждения сервера отправить большие ответы на IP жертвы", correct: true },
    { label: "Сервера от принятия слишком многих соединений одновременно", misconception: "Лимиты соединений — другая проблема; антиамплификация — про UDP reflection-атаки." },
    { label: "QUIC-пакеты от повторной отправки более 3 раз", misconception: "Антиамплификация не про количество повторов. Это про отношение ответ/полученные данные." },
  ]}
/>

<Quiz
  id="04-mid-quiz-2"
  lessonSlug="04-connection-migration"
  lang="ru"
  question="Почему QUIC может пережить смену IP клиента с WiFi на сотовую, а TCP нет?"
  choices={[
    { label: "Потому что QUIC автоматически восстанавливает TCP-соединение в фоне", misconception: "QUIC не использует TCP. Суть в том, что QUIC идентифицирует соединения по Connection ID, а не 4-туплу." },
    { label: "QUIC-соединения идентифицируются Connection ID, а не 4-туплом — тот же Connection ID с нового адреса это всё то же соединение", correct: true },
    { label: "Потому что UDP-пакеты не требуют фиксированного IP-адреса", misconception: "UDP-пакеты всё равно требуют исходный IP. Трюк в том, что идентификатор соединения QUIC находится в полезной нагрузке, а не на уровне IP." },
    { label: "Потому что QUIC использует keep-alive пакеты для поддержания соединения", misconception: "Keep-alive пакеты поддерживают простаивающие соединения, но не выживают при смене IP." },
  ]}
/>

<Misconception
  id="04-udp-blocking-failure"
  title="Важно: QUIC уязвим к блокировке UDP"
>
3–5% сетей блокируют UDP полностью. При молчаливой блокировке QUIC истекает по таймауту и откатывается на HTTP/2, добавляя задержку. Браузеры гонят QUIC и TCP параллельно, победитель — тот, кто соединится первым. Откат на HTTP/2 должен быть бесшовным.
</Misconception>

<Inset kind="edgecase" lang="ru">
Ротация Connection ID не позволяет наблюдателю связать старый и новый IP клиента через Connection ID. Сервер периодически отправляет фреймы **NEW_CONNECTION_ID**, предлагая новые ID; клиент переключается на новый при миграции. Это обеспечивает конфиденциальность — наблюдатель не может сопоставить WiFi-сессию пользователя с сотовой — не скрывая IP пользователя от сервера.
</Inset>

</Explanation>

<SequenceDiagram
  label="Миграция соединения: после смены IP клиент сохраняет тот же Connection ID, сервер отправляет PATH_CHALLENGE, клиент отражает PATH_RESPONSE, и путь проверяется без нового рукопожатия"
  hue="var(--d-network)"
  actors={["Клиент", "Сервер"]}
  messages={[
    { from: "Клиент", to: "Сервер", label: "пакет с нового IP, тот же CID 0x1234" },
    { from: "Сервер", to: "Клиент", label: "PATH_CHALLENGE (случайный токен 8 байт)" },
    { from: "Клиент", to: "Сервер", label: "PATH_RESPONSE (отражённый токен)" },
    { from: "Сервер", to: "Клиент", label: "путь проверен — полная скорость восстановлена" },
  ]}
  caption="Сессию идентифицирует Connection ID, а не 4-тупл IP, поэтому всё состояние потоков и перегрузки переживает переход. PATH_CHALLENGE/PATH_RESPONSE доказывает достижимость нового адреса до снятия лимита антиамплификации 3×."
/>

<KeyTakeaway>Connection ID QUIC позволяют соединениям переживать смену сети, отделяя идентичность сессии от IP-адреса. Проверка пути (PATH_CHALLENGE/PATH_RESPONSE) предотвращает подмену; лимит антиамплификации 3× предотвращает reflection-атаки во время проверки.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="04-connection-migration-retrieval"
  lang="ru"
  questions={[
    { q: "Объясните, почему QUIC может пережить смену IP клиента с WiFi на сотовую, а TCP нет, и какую роль играет Connection ID.", a: "TCP-соединения идентифицируются 4-туплом (IP клиента, порт клиента, IP сервера, порт сервера). При смене IP клиента 4-тупл ломается, и TCP считает это новым соединением — всё состояние соединения отбрасывается. QUIC-соединения идентифицируются Connection ID, выбираемым сервером. При смене IP клиента Connection ID всё ещё в QUIC-пакете. Сервер узнаёт его, проверяет новый путь через PATH_CHALLENGE/PATH_RESPONSE и перемещает соединение на новый адрес. Всё состояние потоков и перегрузки переживает." },
    { q: "Что такое антиамплификационный лимит в QUIC и зачем он нужен?", a: "До проверки адреса клиента сервер может отправить не более 3× байт, которых он получил с этого адреса. Это предотвращает UDP-атаки амплификации: злоумышленник не может подделать IP жертвы как источник клиента, потому что большой ответ сервера пойдёт жертве. Клиенты дополняют Initial-пакеты до минимум 1200 байт, чтобы дать серверам пространство для 3600 байт ответа." },
    { q: "Какие фреймы использует QUIC для проверки нового адреса клиента после миграции?", a: "Сервер отправляет фрейм PATH_CHALLENGE со случайным токеном 8 байт. Клиент отражает токен в фрейме PATH_RESPONSE. После получения сервер считает новый адрес проверенным и разблокирует полную скорость отправки." },
  ]}
/>

<Recap lang="ru">
Фундаментальное допущение TCP — что соединение равно 4-туплу — ломается при каждой смене сети мобильным клиентом. QUIC обходит это, идентифицируя соединения непрозрачным Connection ID, который живёт в заголовке QUIC-пакета, а не в IP или UDP. При смене IP клиента Connection ID остаётся постоянным, и сервер может обнаружить миграцию: он отправляет PATH_CHALLENGE для проверки доступности нового адреса, ждёт PATH_RESPONSE, а затем мигрирует всё соединение — все потоки, всё состояние управления потоком — на новый путь. Во время проверки лимит антиамплификации 3× предотвращает использование окна миграции как вектора отражения. Ротация Connection ID дополнительно позволяет клиенту менять ID при миграции, чтобы наблюдатели не могли связать две сетевые идентичности. Теперь, когда увидите, что мобильное приложение не прерывается при выходе пользователя из зоны WiFi, вы знаете, что за этим стоит: Connection ID, переживающий смену IP-адреса, и один PATH_CHALLENGE round trip, соединяющий новый путь с существующей сессией.
</Recap>
