---
slug: 05-loss-detection-and-cc
lang: ru
track: networking
unit: 10-quic-internals
order: 5
title: "Обнаружение потерь и управление перегрузкой"
summary: "QUIC использует монотонно возрастающие номера пакетов и подключаемое управление перегрузкой — NewReno, CUBIC, BBR — устраняя неоднозначность алгоритма Карна и позволяя обновлять CC на уровне приложения."
estMin: 12
status: ready
lessonType: topic
level: middle
prereqs:
  - "10-quic-internals/02-quic-streams-and-hol"
  - "10-quic-internals/03-handshake-and-1rtt"
deepensInto:
  - "07-deployment-and-cpu"
spiral: ["latency"]
concepts: ["monotonic-packet-numbers", "ack-ranges", "probe-timeout", "karn-algorithm", "cubic", "bbr", "pluggable-cc"]
sources:
  - https://datatracker.ietf.org/doc/html/rfc9002
  - https://datatracker.ietf.org/doc/html/rfc9000
  - https://blog.cloudflare.com/the-road-to-quic/
---

import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import RFCQuiz from "~/components/pedagogy/RFCQuiz.astro";
import NumbersCard from "~/components/layout/NumbersCard.astro";

<Hook>
TCP повторно отправляет потерянный пакет с тем же порядковым номером. Пришедший ACK неоднозначен — сервер подтверждает оригинальную отправку или повтор? QUIC даёт каждому пакету уникальный номер, и это единственное изменение делает обнаружение потерь однозначным, а управление перегрузкой — подключаемым.
</Hook>

<Crux>Монотонные номера пакетов QUIC убирают неоднозначность алгоритма Карна; user space делает CC подключаемым на уровне соединения.</Crux>

<Explanation>

## Монотонные номера пакетов

Номера пакетов QUIC — **строго возрастающие целые числа** — потерянный пакет при повторе получает **новый** номер. В отличие от TCP, где повторно отправленные пакеты несут тот же порядковый номер (создавая неоднозначность — алгоритм Карна как обходное решение), QUIC никогда не переиспользует номер пакета.

**ACK-диапазоны:** Фреймы ACK QUIC перечисляют диапазоны подтверждённых номеров пакетов:
```
ack_ranges: [1000..1005, 1008..1010]
```
Пропуск в 1006 и 1007 означает, что эти пакеты ещё не получены. Это эквивалент TCP SACK, но нативный для протокола.

**Объявление потери** происходит когда:
1. Более поздний пакет подтверждён, и **окно переупорядочивания** истекло (обычно 1,5× макс. RTT после отправки пакета не по порядку).
2. Или срабатывает **PTO (Probe Timeout)** — аналог RTO TCP, но вычисляемый для каждого пространства номеров пакетов.

Монотонная модель проще модели TCP, но требует повторной сериализации повторно отправленных фреймов с новыми номерами — небольшая стоимость CPU.

<div data-lesson-visual class="rounded-lg border border-slate-200 bg-slate-50 p-4 my-4 text-sm space-y-2">
  <div class="font-semibold text-slate-700 mb-1">Модель номеров пакетов: TCP vs QUIC</div>
  <div class="grid grid-cols-2 gap-4 text-xs font-mono">
    <div class="space-y-1">
      <div class="font-semibold text-slate-500">TCP (неоднозначно)</div>
      <div>Отправка seq=100 → ПОТЕРЯН</div>
      <div>Повтор seq=100 → ACK 101</div>
      <div class="text-amber-600">ACK: оригинал или повтор?</div>
      <div class="text-amber-600">→ Нужен алгоритм Карна</div>
    </div>
    <div class="space-y-1">
      <div class="font-semibold text-slate-500">QUIC (однозначно)</div>
      <div>Отправка pkt#100 → ПОТЕРЯН</div>
      <div>Повтор как pkt#101 → ACK 101</div>
      <div class="text-green-600">ACK#101 = подтверждён повтор</div>
      <div class="text-green-600">→ Нет неоднозначности, нет обходного пути</div>
    </div>
  </div>
</div>

## Подключаемое управление перегрузкой

Поскольку QUIC живёт в пользовательском пространстве, **управление перегрузкой подключаемо**. RFC 9002 определяет NewReno как базовый алгоритм. Основные реализации поставляют:

- **NewReno** — медленный старт, аддитивное увеличение, уполовинивание cwnd при потере.
- **CUBIC** (quiche от Cloudflare, стандарт ядра Linux) — агрессивное зондирование после потери с кубической функцией.
- **BBR** (Google, YouTube, Cloudflare edge) — моделирование пропускной способности узкого места и минимального RTT; игнорирование случайных потерь.
- **HyStart++** — ранний выход из медленного старта при обнаружении очереди, используется с CUBIC.

Приложения могут выбирать алгоритм при открытии соединения. Одна QUIC-библиотека может поддерживать несколько алгоритмов. Такая гибкость невозможна с kernel TCP, где изменение CC требует перекомпиляции или sysctl-изменений на миллионах серверов.

**Компромисс:** Гибкость затрудняет бенчмаркинг — сравнение двух QUIC-стеков с разными алгоритмами CC — это не честное сравнение самого QUIC.

<Quiz
  id="05-mid-quiz-1"
  lessonSlug="05-loss-detection-and-cc"
  lang="ru"
  question="Почему QUIC не нуждается в алгоритме Карна, и какое проектное решение делает это возможным?"
  choices={[
    { label: "QUIC вообще не переотправляет пакеты — он использует FEC", misconception: "QUIC переотправляет потерянные данные, но с новыми номерами пакетов. В базовой спецификации нет FEC." },
    { label: "QUIC использует монотонно возрастающие номера пакетов — повторы получают новые номера, поэтому ACK однозначно идентифицирует, какая отправка получена", correct: true },
    { label: "QUIC использует большие порядковые номера, чем TCP, снижая вероятность переполнения", misconception: "Алгоритм Карна про неоднозначность повторов, а не переполнение. Ключ — уникальные номера пакетов на каждую отправку." },
    { label: "QUIC построен на UDP, который обрабатывает разрешение неоднозначности ACK на уровне ОС", misconception: "UDP вообще не имеет механизма ACK. QUIC реализует свой поверх UDP." },
  ]}
/>

<Quiz
  id="05-mid-quiz-2"
  lessonSlug="05-loss-detection-and-cc"
  lang="ru"
  question="Почему подключаемое управление перегрузкой в QUIC значимо по сравнению с kernel TCP?"
  choices={[
    { label: "Потому что QUIC работает поверх UDP, что по своей природе позволяет любой алгоритм CC", misconception: "UDP — просто транспортный уровень; алгоритм CC — выбор на уровне приложения в QUIC независимо от UDP." },
    { label: "Алгоритм CC kernel TCP требует перекомпиляции ОС или sysctl для смены; библиотеки QUIC могут переключать CC на соединение в пользовательском пространстве без участия ядра", correct: true },
    { label: "Потому что QUIC имеет больше номеров пакетов, давая CC больше данных для работы", misconception: "Количество номеров пакетов — не ключевое отличие. Ключ — возможность деплоя в пользовательском пространстве." },
    { label: "CC QUIC подключаемо, потому что работает быстрее CC TCP", misconception: "Скорость — не причина. Размещение в пользовательском пространстве делает возможной эволюцию CC без обновлений ОС." },
  ]}
/>

<RFCQuiz
  id="05-rfc-1"
  lessonSlug="05-loss-detection-and-cc"
  lang="ru"
  question="Какой RFC определяет алгоритм обнаружения потерь QUIC и базовое управление перегрузкой?"
  choices={[
    { rfc: "RFC 9002", title: "QUIC Loss Detection and Congestion Control", correct: true },
    { rfc: "RFC 9000", title: "QUIC: A UDP-Based Multiplexed and Secure Transport" },
    { rfc: "RFC 9001", title: "QUIC Cryptography" },
    { rfc: "RFC 9114", title: "HTTP/3" },
  ]}
/>

<Inset kind="why" lang="ru">
**Почему не использовать управление перегрузкой TCP в QUIC?** CC TCP реализовано в слое сокетов ядра, где у него прямой доступ к временным меткам пакетов, буферам ядра и аппаратному ускорению NIC. Переимплементация в пользовательском пространстве (как делает QUIC) стоит CPU — каждый пакет должен обрабатываться пользовательским кодом без помощи железа. Выгода: пользовательское пространство может итерировать быстрее, может получать доступ к метаданным уровня приложения (например, приоритет видеопотока) для более умных CC-решений, и может обновляться вместе с приложением без касания ОС.
</Inset>

<NumbersCard
  id="05-cc-numbers"
  title="Алгоритмы управления перегрузкой QUIC"
  stats={[
    { label: "Базовый алгоритм RFC 9002", value: "NewReno (медленный старт + AIMD)" },
    { label: "По умолчанию Cloudflare quiche", value: "CUBIC (агрессивное зондирование)" },
    { label: "Google YouTube / GCP", value: "BBR (модель пропускная способность-RTT)" },
    { label: "Цикл обновления CC в пользовательском пространстве", value: "еженедельно (vs ежемесячно+ для kernel TCP)" },
    { label: "Выбор CC на соединение", value: "при открытии соединения" },
  ]}
/>

</Explanation>

<KeyTakeaway>Монотонные номера пакетов QUIC делают обнаружение потерь однозначным — алгоритм Карна не нужен. Размещение в пользовательском пространстве делает CC подключаемым: NewReno, CUBIC или BBR можно выбирать на соединение и обновлять без изменений ядра.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="05-loss-detection-retrieval"
  lang="ru"
  questions={[
    { q: "Что такое алгоритм Карна и почему QUIC он не нужен?", a: "Алгоритм Карна — обходное решение TCP: когда повторная отправка подтверждена ACK, TCP не может определить, подтверждён ли оригинал или повтор — поэтому игнорирует RTT-сэмпл. QUIC избегает этого, используя монотонно возрастающие номера пакетов. Повтор получает новый номер, поэтому ACK однозначно идентифицирует, какая отправка получена. RTT-сэмплы всегда валидны." },
    { q: "Как работает механизм ACK-диапазонов QUIC и на какую TCP-функцию он похож?", a: "Фреймы ACK QUIC перечисляют диапазоны подтверждённых номеров пакетов (например, ack_ranges: [1000..1005, 1008..1010]). Пропуски представляют неподтверждённые пакеты. Это эквивалент Selective ACK (SACK) TCP, но нативный для QUIC и всегда активный — SACK в TCP опционален и должен согласовываться." },
    { q: "Что такое PTO в QUIC и чем отличается от RTO TCP?", a: "PTO (Probe Timeout) — эквивалент RTO (Retransmission Timeout) TCP в QUIC. Как и RTO, PTO вычисляется из SRTT и RTTVAR и срабатывает, если ACK не приходит. Ключевое отличие: PTO вычисляется для каждого пространства номеров пакетов, поэтому таймаут в пространстве Handshake не сбрасывает окно перегрузки 1-RTT. RTO TCP глобален для соединения." },
  ]}
/>

<Recap lang="ru">
Переиспользование порядкового номера TCP при повторной отправке создаёт неоднозначность алгоритма Карна — нельзя измерить RTT из ACK повторной передачи. QUIC устраняет это, присваивая каждой отправке новый строго возрастающий номер пакета. Фреймы ACK несут диапазоны, поэтому пропуски в подтверждённых номерах точно выявляют потерянные пакеты — эквивалент TCP SACK, но всегда активный. Потеря объявляется через окно переупорядочивания или PTO (вычисляемый для каждого пространства номеров пакетов). Поскольку QUIC работает в пользовательском пространстве, управление перегрузкой подключаемо: RFC 9002 определяет NewReno как базовый, но CUBIC и BBR поставляются в основных библиотеках и могут выбираться на соединение. Приложения на CDN edge могут обновлять алгоритм CC еженедельно без касания ядра.
</Recap>
