---
concepts:
- udp-gso
- nic-offload
- alt-svc
- http3-fallback
- quic-cpu-cost
- quic-observability
- pluggable-cc
deepensInto: []
estMin: 14
lang: ru
lessonType: topic
level: senior
order: 7
prereqs:
- 10-quic-internals/05-loss-detection-and-cc
- 10-quic-internals/06-0rtt-and-encryption
slug: 07-deployment-and-cpu
sources:
- https://datatracker.ietf.org/doc/html/rfc9000
- https://datatracker.ietf.org/doc/html/rfc9114
- https://blog.cloudflare.com/the-road-to-quic/
spiral:
- performance
- latency
status: ready
summary: Обработка пакетов QUIC в пользовательском пространстве обходится на 15–30%
  дороже по CPU на байт, чем ядерный TCP, и снижает пропускную способность до 45%
  на быстрых каналах.
title: Развёртывание и стоимость CPU
track: networking
unit: 10-quic-internals
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import DebugLog from "~/components/pedagogy/DebugLog.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import DesignPrompt from "~/components/pedagogy/DesignPrompt.astro";
import Misconception from "~/components/layout/Misconception.astro";
import NumbersCard from "~/components/layout/NumbersCard.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
CDN выкатывает HTTP/3. Утилизация CPU на граничных серверах вырастает на 25%, пропускная способность на гигабитных каналах падает почти вдвое, а 4% клиентов незаметно откатываются на HTTP/2 — без единой ошибки в логах. QUIC действительно лучше по задержке. И действительно дороже в эксплуатации.
</Hook>

<Crux>QUIC дороже TCP на 15–30% CPU на байт — до 45% потери throughput на быстрых каналах; сети с блокировкой UDP требуют HTTP/2 фолбэка.</Crux>

<Explanation>

## Стоимость CPU: почему транспорт в пользовательском пространстве дорог

QUIC работает в пользовательском пространстве — каждый пакет проходит через код приложения, а не по быстрому пути ядра от NIC до сокета. Составляющие стоимости:

1. **Шифрование AES-GCM на пакет** — ~15–20 тактов на байт на современных CPU. Ядерный TCP выгружает это на NIC (TLS offload, kTLS). QUIC не может — NIC пока не понимает QUIC.
2. **Декодирование целых переменной длины** — номера пакетов, идентификаторы потоков и длины фреймов используют кодировку VarInt из QUIC. Каждое декодирование — условный переход в пользовательском пространстве.
3. **Фреймирование нагрузки** — фреймы STREAM, CRYPTO и ACK сериализуются/десериализуются на пакет в пользовательском пространстве. Повторно передаваемые фреймы должны быть пересериализованы с новыми номерами пакетов.
4. **Накладные расходы системных вызовов** — без батчинга каждый `sendmsg()` по UDP — это системный вызов. На 1 Гбит/с с пакетами по 1500 байт это ~83 тыс. syscall/с на ядро.

**Измеренный эффект:** На гигабитном LAN-канале при полной нагрузке QUIC насыщает ядро CPU раньше, чем канал заполняется. Пропускная способность падает **до ~45% по сравнению с HTTP/2** на TCP. На медленных или нестабильных каналах (типичный мобильный) узким местом является сеть, а не CPU, поэтому накладные расходы незаметны.

<div data-lesson-visual class="rounded-lg border border-slate-200 bg-slate-50 p-4 my-4 text-sm space-y-3">
  <div class="font-semibold text-slate-700 mb-1">Стоимость CPU QUIC: тип канала имеет значение</div>
  <div class="grid grid-cols-3 gap-3 text-xs">
    <div class="border border-slate-200 rounded p-2 space-y-1">
      <div class="font-semibold text-slate-600">Быстрый LAN (1 Гбит/с)</div>
      <div class="text-red-600">CPU — узкое место</div>
      <div>Пропускная способность −45% vs TCP</div>
      <div>Выгода от HoL: незначительна</div>
      <div class="font-semibold text-slate-500 pt-1">Используйте TCP</div>
    </div>
    <div class="border border-slate-200 rounded p-2 space-y-1">
      <div class="font-semibold text-slate-600">WAN / API (RTT 100 мс)</div>
      <div class="text-amber-600">Сеть — узкое место</div>
      <div>Накладные расходы CPU: незаметны</div>
      <div>Экономия рукопожатия: +100 мс</div>
      <div class="font-semibold text-green-700 pt-1">Используйте QUIC</div>
    </div>
    <div class="border border-slate-200 rounded p-2 space-y-1">
      <div class="font-semibold text-slate-600">Мобильный 4G (нестабильный)</div>
      <div class="text-amber-600">Сеть — узкое место</div>
      <div>Накладные расходы CPU: незаметны</div>
      <div>Экономия HoL: 575× при потере 0,5%</div>
      <div class="font-semibold text-green-700 pt-1">Используйте QUIC</div>
    </div>
  </div>
</div>

## Оптимизации: UDP GSO, NIC offload, привязка к ядрам

**UDP Generic Segmentation Offload (UDP GSO):** Вместо одного `sendmsg()` на пакет 1500 байт — батчинг до 64 КБ нагрузки QUIC в одном системном вызове с подсказкой GSO. Ядро разбивает на отдельные UDP-датаграммы перед DMA на NIC. Результат: ~3–4 syscall на 64 КБ вместо 43. Cloudflare сообщает о **~20% снижении CPU только от GSO**.

**NIC QUIC offload (Intel E810 и новее):** Разбор заголовков QUIC long/short в кремнии, маршрутизация пакетов к нужному потоку QUIC без участия демультиплексора в пользовательском пространстве. Снижает накладные расходы на прерывание на пакет. По состоянию на 2026 год всё ещё экспериментально, но доступно на оптимизированных для облака NIC.

**Привязка к ядрам:** Держите процесс QUIC на одном физическом ядре. Состояние QUIC (таблица соединений, окна CC, ключевой материал) помещается в кэш L3. Миграция между ядрами сбрасывает кэш-линии, добавляя ~50 нс на пакет.

С GSO + привязкой к ядрам стоимость CPU на байт снижается с **30% до 15–20%** накладных расходов по сравнению с ядерным TCP.

<Quiz
  id="07-mid-quiz-1"
  lessonSlug="07-deployment-and-cpu"
  lang="ru"
  question="Почему накладные расходы CPU QUIC снижают пропускную способность на 1 Гбит/с LAN, но не на мобильном канале 10 Мбит/с?"
  choices={[
    { label: "LAN-пакеты крупнее, что делает декодирование VarInt дороже на пакет", misconception: "Стоимость VarInt масштабируется с количеством пакетов, а не размером. Ключ — что насыщается первым: CPU или сеть." },
    { label: "На 1 Гбит/с CPU насыщается раньше канала; на мобильном канале 10 Мбит/с сеть — узкое место, и резерв CPU никогда не исчерпывается", correct: true },
    { label: "LAN-серверы не поддерживают UDP GSO, делая обход ядра недоступным", misconception: "UDP GSO — функция ядра, доступная на большинстве современных Linux-серверов независимо от скорости канала." },
    { label: "Мобильные NIC имеют аппаратный QUIC offload, которого нет у LAN NIC", misconception: "NIC QUIC offload — серверная функция и пока экспериментальна; это не мобильная технология." },
  ]}
/>

## Блокировка UDP и обязательный фолбэк на HTTP/2

**~3–5% сетей блокируют UDP** — корпоративные прокси, определённые ISP-шлюзы, некоторые LTE-контексты. Когда QUIC молча дропается (нет ICMP-ошибки, просто потерянные пакеты), единственный сигнал для клиента — таймаут, обычно 1–3 секунды до фолбэка на TCP.

**Гонка браузера:** Современные браузеры запускают и QUIC (UDP 443), и TCP (443) одновременно. Выигрывает то рукопожатие, которое завершится первым. Это ограничивает потери UX от блокировки UDP нулём — TCP побеждает, а QUIC изящно проигрывает гонку. Обратная сторона: при систематической блокировке UDP на пути — лишние усилия на каждом соединении.

**Обнаружение Alt-Svc:** HTTP/3 анонсируется через `Alt-Svc: h3=":443"; ma=3600` в ответе HTTP/2. Браузер кэширует это и пытается использовать QUIC на следующем соединении. Первичные соединения всегда откатываются на TCP, обнаруживают заголовок и переходят на QUIC при следующих запросах.

**Требование RFC 9000:** Реализации ДОЛЖНЫ поддерживать фолбэк на HTTP/2 over TCP. Деплой без этого нарушает спецификацию и сломается в заблокированных сетях.

<Misconception
  id="quic-silent-block"
  title="Внимание: заблокированный UDP выглядит как задержка, а не ошибка"
>
  Молча дропаемый UDP — QUIC таймаут без кода ошибки. Современные браузеры гонят
  QUIC и TCP параллельно, поэтому штраф минимален, но без метрик фолбэков на клиента
  нельзя отличить блокировку UDP от медленного пути.
</Misconception>

<Quiz
  id="07-mid-quiz-2"
  lessonSlug="07-deployment-and-cpu"
  lang="ru"
  question="Пользователь в корпоративной сети сообщает, что загрузка сайта на 2 секунды медленнее обычного. QUIC включён. Что скорее всего происходит и как проверить?"
  choices={[
    { label: "Рукопожатие QUIC медленнее TCP+TLS в корпоративных сетях из-за более длинных RTT", misconception: "QUIC — 1-RTT, быстрее TCP+TLS. Медленное рукопожатие — не реалистичное объяснение." },
    { label: "UDP заблокирован на корпоративном файрволе; клиент ждёт таймаута PTO перед фолбэком на TCP. Проверяйте по частоте фолбэков Alt-Svc в метриках сервера или tcpdump на отсутствие QUIC-ответов", correct: true },
    { label: "Корпоративные сети всегда блокируют TLS 1.3, необходимый QUIC, вызывая сбой рукопожатия", misconception: "TLS 1.3 работает и через TCP, корпоративные сети редко его блокируют. Проблема — UDP, не TLS 1.3." },
    { label: "Накладные расходы CPU QUIC 15–30% выше на корпоративных серверах, добавляя 2 секунды обработки", misconception: "Накладные расходы CPU проявляются как потеря пропускной способности, а не задержка на соединение. 2 секунды — симптом таймаута." },
  ]}
/>

## Реальность развёртывания в 2026 году

**~21% веб-трафика идёт через HTTP/3.** ~35–40% крупных сайтов анонсируют его через Alt-Svc. Принятие бимодально:

- **Мобильные браузеры** включают HTTP/3 по умолчанию — задержка и устранение HoL-блокировки важны в сотовых сетях.
- **Десктоп/LAN** всё ещё устраивает гонку QUIC vs TCP; TCP часто выигрывает, потому что RTT короткие и HoL-блокировка редка на быстрых путях.

Крупные CDN (Cloudflare, Google, Akamai, Fastly) включают HTTP/3 по умолчанию. Браузеры (Chrome, Safari, Firefox) его поддерживают. Кривая принятия будет расти по мере того, как блокировка UDP станет редкостью, аппаратный offload снизит стоимость CPU, а гонка фолбэков станет универсальной.

<DebugLog
  id="07-debug-1"
  lessonSlug="07-deployment-and-cpu"
  lang="ru"
  title="Трассировка пакетов QUIC — диагностика уровня шифрования и потерь"
  output={`$ quictrace capture.pcapng | head -20
timestamp=0.000 dcid=12345678 type=Initial pkt_num=0 frames=[Crypto[0..120], Padding]
timestamp=0.045 dcid=12345678 type=Initial pkt_num=1 frames=[Crypto[120..240], Padding] # Повторная передача (нет ACK вовремя)
timestamp=0.051 scid=87654321 dcid=12345678 type=Initial pkt_num=0 frames=[Crypto[0..200], Ack[0], Padding]
timestamp=0.052 dcid=87654321 type=Handshake pkt_num=0 frames=[Crypto[200..350]]
timestamp=0.100 scid=87654321 dcid=12345678 type=Handshake pkt_num=0 frames=[Crypto[350..400], Finished]
timestamp=0.101 dcid=87654321 type=1RTT pkt_num=0 frames=[Stream(0, fin, 4096 bytes)]
timestamp=0.151 scid=87654321 dcid=12345678 type=1RTT pkt_num=0 frames=[Stream(0, fin, [все байты 0..4095], Ack[0])]`}
  outputLang="log"
  question="Клиент видит одну повторную передачу Initial до прихода Initial от сервера. Handshake далее проходит нормально. Что это означает?"
  hint="Смотрите на временны́е метки. Клиент отправляет pkt_num=0 в t=0, повторно передаёт в t=0.045 (до прихода ACK сервера в t=0.051). Почему клиент повторно передаёт уже подтверждённый пакет?"
  answer="Повторная передача в t=0.045 — это PTO-проба: таймер Probe Timeout срабатывает (~45 мс для пути с RTT 20–30 мс) до прихода ACK сервера. ACK сервера в t=0.051 подтверждает первую отправку (pkt_num=0). Повторная передача была ложной. Это нормальное поведение — множество Initial-ретрансмиссий или потери Handshake-пакетов указывали бы на нестабильную сеть. RTT ~50 мс из трассировки подтверждает путь с RTT 20–25 мс."
/>

## Пробелы в наблюдаемости

Шифрование QUIC предотвращает инспекцию пакетов — `tcpdump` показывает только непрозрачные блобы. Традиционный мониторинг сети (счётчики HTTP-запросов на endpoint, обнаружение медленных клиентов, нарушения на уровне потока) ломается.

**Адаптация стека:**
- Приложения экспортируют трассировки QUIC через JSON (формат qlog по RFC 9312) — жизненный цикл соединения, номера пакетов, события CC.
- Браузеры сообщают `PerformanceResourceTiming.nextHopProtocol = "h3"` для HTTP/3-соединений.
- Облачные провайдеры (AWS, GCP) добавляют QUIC-aware метрики потоков.
- eBPF-пробы на пользовательских QUIC-сокетах могут восстанавливать тайминг пакетов без расшифровки.

Это компромисс по сути: шифрование даёт конфиденциальность и безопасность за счёт операционной непрозрачности.

<TradeoffMatrix
  id="07-tradeoff-1"
  lessonSlug="07-deployment-and-cpu"
  lang="ru"
  prompt="CDN должен выбрать: развернуть QUIC для латентно-чувствительного API (маленький запрос/ответ, межконтинентальный) vs. высокопроизводительного сервиса статических ресурсов (1 Гбит/с, LAN-клиенты)."
  options={[
    { name: "QUIC для API и статических ресурсов", summary: "Единый протокол, единый тулинг.", justification: "Статические ресурсы на быстром LAN теряют ~45% пропускной способности из-за стоимости CPU QUIC. Выгоды от HoL на быстрых путях нет. CPU становится узким местом раньше канала. Неправильный выбор для путей с высоким throughput." },
    { name: "QUIC для API (задержка), TCP для статики (пропускная способность)", summary: "Протокол под задачу.", correct: true, justification: "API выигрывает от рукопожатия QUIC за 1 RTT (экономия 100+ мс межконтинентально) и устранения HoL. Статические ресурсы на быстром LAN используют TCP kernel offload (аппаратное шифрование, GSO, RSS) для максимального throughput. Клиенты договариваются о HTTP/3 для API и HTTP/2 для ресурсов. Именно так делают крупные CDN." },
    { name: "Только TCP; опираться на keep-alive и пулинг соединений", summary: "Пропустить QUIC полностью.", justification: "Для API keep-alive амортизирует рукопожатие TCP, но не для новых сессий. На межконтинентальных маршрутах с RTT 150 мс и холодными сессиями платите 150 мс за рукопожатие — то, что QUIC экономит. Это значит оставить задержку на столе." },
    { name: "QUIC для обоих с UDP GSO и NIC QUIC offload", summary: "Инвестировать в железо.", justification: "С современными NIC и привязкой к ядрам стоимость CPU падает до 15%. Но аппаратный offload доступен не везде (облако, контейнеры), дорог и требует экспертизы. Это вариант будущего, не мейнстрим." },
  ]}
/>

<DesignPrompt
  id="07-design-1"
  lessonSlug="07-deployment-and-cpu"
  lang="ru"
  prompt="Спроектируйте стратегию развёртывания HTTP/3 и QUIC для глобального CDN, обслуживающего мобильных клиентов (90% трафика) и десктоп (10% трафика), при текущей инфраструктуре HTTP/2 на TCP."
  constraints={[
    "Существующее развёртывание HTTP/2 стабильно и хорошо настроено; нельзя ломать TCP-пути.",
    "Мобильные клиенты разнообразны (iOS 14+, Android 5+, разные браузеры); часть сетей блокирует QUIC.",
    "Бюджет CPU на QUIC: не более 20% накладных расходов по сравнению с текущим HTTP/2.",
    "Наблюдаемость: измерять частоту принятия QUIC, частоту фолбэков и улучшение задержки по типу устройств.",
  ]}
  canonical="Фаза 1 (Месяцы 1–3): Включить QUIC на граничных серверах в незаблокированных сетях (датацентры APAC, US, EU). Анонсировать HTTP/3 через Alt-Svc только мобильным браузерам (Alt-Svc: h3=':443'; ma=3600). Метрики: частота обнаружения Alt-Svc, частота QUIC-соединений, частота фолбэков. Ожидать 10–20% принятия после перезапуска браузера. Фаза 2 (Месяцы 4–6): Развернуть UDP GSO на всех граничных серверах; снизить накладные расходы CPU до 15%. Расширить анонс QUIC на всех клиентов, но устраивать гонку QUIC и TCP в браузере (оба инициируют, выигрывает первый). Метрики: частота побед QUIC, улучшение задержки (ожидать 30–55% на мобильных из-за устранения HoL; 5–10% на десктопе из-за экономии рукопожатия). Фаза 3 (Месяцы 7–12): Мониторинг частоты фолбэков из заблокированных QUIC сетей (ожидать 3–5%). Для заблокированных путей добавить явный фолбэк на HTTP/2 после 2-секундного таймаута QUIC. Перенаправлять заблокированных клиентов на HTTP/2-only Alt-Svc для предотвращения повторных сбоев. Фаза 4 (Месяцы 13+): QUIC становится умолчанием; TCP остаётся для фолбэка и устаревших клиентов. Steady-state: ~60–70% QUIC, ~30–40% TCP. Наблюдаемость: отслеживать (1) принятие Alt-Svc, (2) частоту фолбэков QUIC→TCP (алерт если > 8%), (3) p50/p99 задержки по протоколу по типу устройств, (4) стоимость CPU по протоколу."
  keyPoints={[
    "Мобильные выигрывают от задержки QUIC; десктоп — от пропускной способности TCP. Разные уровни получают разные протоколы.",
    "Обнаружение Alt-Svc требует предварительного HTTP/2 запроса; запускайте оба протокола параллельно для новых клиентов, чтобы избежать задержки фолбэка.",
    "Сети с блокировкой QUIC существуют; явный фолбэк после короткого таймаута (2–3с) не подвешивает пользователя.",
    "UDP GSO критичен для контроля стоимости CPU. Без него QUIC слишком дорог для высокопроизводительных CDN.",
    "Непрерывно измеряйте частоту фолбэков. Если > 5%, исследуйте: реальная блокировка UDP или баг развёртывания.",
    "Семантика соединений отличается: HTTP/2 over TCP имеет персистентное TCP-состояние; HTTP/3 over QUIC переносит состояние в QUIC. Убедитесь, что балансировщик нагрузки и стек наблюдаемости понимают оба.",
  ]}
/>

<NumbersCard
  id="07-deployment-numbers"
  title="Реальность QUIC-деплоймента 2026"
  stats={[
    { label: "Накладные расходы CPU vs ядерный TCP", value: "15–30% на байт" },
    { label: "Потеря throughput на быстрых каналах 1 Гбит/с", value: "до ~45% vs HTTP/2" },
    { label: "Снижение CPU от UDP GSO (Cloudflare)", value: "~20% на соединение" },
    { label: "Сети с блокировкой UDP", value: "~3–5%" },
    { label: "Веб-трафик через HTTP/3 (2026)", value: "~21%" },
    { label: "Крупные сайты, анонсирующие HTTP/3", value: "~35–40%" },
    { label: "Таймаут гонки QUIC+TCP в браузере", value: "1–3 с до победы TCP" },
  ]}
/>

<Inset kind="why" lang="ru">
**Почему не добавить QUIC в ядро?** В Linux есть экспериментальные патчи QUIC in-kernel, но сообщество разделено. Ядерный TCP десятилетиями накапливал NIC offload (kTLS, GRO, RSS). Реплицировать это для QUIC займёт годы и привяжет эволюцию QUIC к циклу релизов ядра — противоположность тому, что задумывал RFC 9000. Пользовательский QUIC может выпускать новые алгоритмы CC еженедельно; ядерный QUIC — нет. Стоимость CPU — цена гибкости.
</Inset>

</Explanation>

<FlowDiagram
  label="Путь отправки QUIC: данные приложения проходят через AES-GCM-шифрование в пользовательском пространстве, сериализацию VarInt и фреймов, и syscall sendmsg на каждый пакет до того, как ядро и NIC отправят их — каждый шаг тратит CPU, который путь ядерного TCP выгрузил бы на железо"
  hue="var(--d-network)"
  perRow={6}
  caption="Поскольку QUIC живёт в пользовательском пространстве, каждый пакет платит за AES-GCM, декодирование VarInt, фреймирование и syscall на CPU — работу, которую ядерный TCP отдаёт NIC (kTLS, GSO, RSS). Это и есть накладные расходы 15–30% на байт, и на быстрых каналах CPU насыщается раньше канала."
  nodes={[
    { id: "app", label: "Данные", sub: "HTTP/3" },
    { id: "crypto", label: "AES-GCM", sub: "~15–20 такт/байт" },
    { id: "frame", label: "VarInt+фрейм", sub: "user space" },
    { id: "sys", label: "sendmsg()", sub: "syscall/пакет" },
    { id: "kern", label: "Ядро UDP", sub: "нет QUIC offload" },
    { id: "nic", label: "NIC", sub: "провод" },
  ]}
  edges={[
    { from: "app", to: "crypto" },
    { from: "crypto", to: "frame" },
    { from: "frame", to: "sys" },
    { from: "sys", to: "kern" },
    { from: "kern", to: "nic" },
  ]}
/>

<KeyTakeaway>QUIC обходится на 15–30% дороже по CPU на байт, чем ядерный TCP; на быстрых каналах throughput падает до 45%. UDP GSO восстанавливает ~20%. Используйте QUIC для латентно-чувствительных WAN/мобильных путей; оставляйте TCP для высокопроизводительного LAN. Планируйте фолбэк на HTTP/2 для ~3–5% клиентов с блокировкой UDP.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="07-deployment-retrieval"
  lang="ru"
  questions={[
    { q: "Почему накладные расходы CPU QUIC снижают throughput на 1 Гбит/с LAN, но не на мобильном 10 Мбит/с?", a: "На 1 Гбит/с LAN обработка пакетов QUIC в пользовательском пространстве (AES-GCM на пакет, декодирование VarInt, сериализация фреймов, syscall на пакет) насыщает ядро CPU раньше канала. Throughput падает до ~45% vs HTTP/2. На мобильном 10 Мбит/с канале сеть — узкое место: даже при 30% накладных расходах CPU есть достаточный резерв, чтобы держать канал насыщенным. Накладные расходы CPU видны только когда скорость канала такова, что частота пакетов × стоимость на пакет превышает мощность одного ядра." },
    { q: "Что такое UDP GSO и почему Cloudflare сообщает о снижении CPU на ~20% от него?", a: "UDP Generic Segmentation Offload (GSO) позволяет процессу QUIC батчить до 64 КБ нагрузки в одном syscall sendmsg() с подсказкой размера GSO. Ядро разбивает на отдельные UDP-датаграммы по 1500 байт перед DMA на NIC. Без GSO каждый пакет требует своего syscall — на 1 Гбит/с при пакетах 1500 байт это ~83 тыс. syscall/с на ядро. GSO сокращает до ~3–4 syscall на 64 КБ, резко снижая накладные расходы syscall. Cloudflare сообщает о ~20% снижении CPU только от этого батчинга." },
    { q: "CDN видит 4% QUIC-соединений, тихо проваливающихся без ошибки. Вероятная причина и решение?", a: "~3–5% сетей блокируют UDP. При блокировке пакеты QUIC тихо дропаются — нет ICMP-ошибки, нет RST, только таймаут. PTO клиента срабатывает (1–3 секунды), затем фолбэк на TCP через HTTP/2. Решение: (1) включить гонку QUIC+TCP в браузере, чтобы TCP побеждал сразу при блокировке UDP; (2) после повторных фолбэков от клиента отдавать HTTP/2-only Alt-Svc, чтобы прекратить попытки QUIC; (3) вынести частоту фолбэков QUIC на клиента в наблюдаемость для проактивного обнаружения заблокированных путей." },
  ]}
/>

<Recap lang="ru">
Архитектура QUIC в пользовательском пространстве даёт выигрыши по задержке и HoL ценой реальных затрат CPU: на 15–30% больше на байт, чем у ядерного TCP, вплоть до ~45% потери throughput на быстрых 1 Гбит/с каналах, где CPU — не сеть — является узким местом. UDP GSO батчит системные вызовы и восстанавливает ~20% CPU; NIC offload и привязка к ядрам снижают ещё больше. Около 3–5% сетей молча блокируют UDP, требуя гонки TCP в браузере и обязательного фолбэка на HTTP/2. По состоянию на 2026 год ~21% веб-трафика идёт через HTTP/3 с бимодальным принятием — мобильные выигрывают явно, десктоп часто проигрывает TCP в гонке. Шифрование QUIC ломает традиционную инспекцию пакетов; qlog (RFC 9312), браузерные timing API и eBPF-пробы — замена стека наблюдаемости. Правильная стратегия деплоя: QUIC для латентно-чувствительных WAN и мобильных путей, TCP для высокопроизводительного LAN и статических ресурсов.
</Recap>
