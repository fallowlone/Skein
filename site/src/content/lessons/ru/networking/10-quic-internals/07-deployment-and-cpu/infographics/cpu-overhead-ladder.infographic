infographic chart-column-simple
data
  title Снижаем CPU-налог QUIC с помощью оптимизаций
  desc Накладные расходы CPU на байт против ядерного TCP — UDP GSO возвращает ~20%, привязка к ядру доводит до 15%
  values
    - label Наивный user-space QUIC
      value 30
    - label + UDP GSO (пакетный sendmsg)
      value 20
    - label + GSO + привязка к ядру
      value 15
theme
  palette #f43f5e #f59e0b #14b8a6
