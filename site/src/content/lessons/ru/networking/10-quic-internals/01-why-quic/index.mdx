---
concepts:
- quic
- user-space-transport
- udp-foundation
- middlebox-ossification
- connection-migration
deepensInto:
- 02-quic-streams-and-hol
- 03-handshake-and-1rtt
estMin: 8
lang: ru
lessonType: topic
level: junior
order: 1
prereqs:
- 03-tcp-handshake/01-the-three-way-handshake
- 05-tls-handshake/01-tls-what-and-why
- 06-http-versions/01-http-fundamentals
slug: 01-why-quic
sources:
- https://datatracker.ietf.org/doc/html/rfc9000
- https://blog.cloudflare.com/the-road-to-quic/
- https://www.debugbear.com/blog/http3-vs-http2-performance
spiral:
- latency
- statefulness
status: ready
summary: QUIC переносит логику транспорта из ядра в пользовательское пространство
  поверх UDP — чтобы протокол мог свободно эволюционировать и завершать рукопожатие
  за один RTT.
title: Почему QUIC, а не TCP+TLS
track: networking
unit: 10-quic-internals
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import MetaphorComplete from "~/components/pedagogy/MetaphorComplete.astro";
import NumbersCard from "~/components/layout/NumbersCard.astro";
import StackDiagram from "~/components/diagram/StackDiagram.astro";

<Hook>
Вы знаете TCP. Вы знаете TLS. Вы знаете, как они складываются: браузер делает TCP-рукопожатие (1 RTT), потом TLS-рукопожатие сверху (ещё 1 RTT), и только потом отправляет HTTP-запрос. На межконтинентальной линии с задержкой 100 ms это 200 ms накладных расходов до того, как страница вообще начнёт загружаться. За несколько минут вы поймёте, почему эта двойная пошлина существует и как QUIC схлопывает её в один обмен.
</Hook>

<Crux>QUIC — TCP-транспорт поверх UDP в user space: нет зависимости от ядра, рукопожатие за 1 RTT, потоки без HoL-блокировки.</Crux>

<Explanation>

## Две проблемы TCP

TCP устарел. Добавление новых TCP-опций требует обновления ядра, выпуска патчей ОС через несколько месяцев, а потом нужно надеяться, что middlebox'ы — брандмауэры, NAT, балансировщики — не отбросят опцию молча. Это **окостенение** означало, что TCP почти не мог эволюционировать десятилетиями.

Вторая проблема: потеря одного пакета блокирует все потоки соединения, даже не связанные с этим пакетом. Это состояние называется **head-of-line blocking** (блокировка первым в очереди — когда один потерянный пакет останавливает все данные за ним).

Радикальное решение QUIC: вынести логику транспорта в пользовательское пространство (где её можно обновлять с каждым релизом приложения), запустить поверх UDP (который middlebox'ы не умеют инспектировать) и зашифровать почти весь пакет.

<div data-lesson-visual class="rounded-lg border border-slate-200 bg-slate-50 p-4 my-4 text-sm font-mono space-y-2">
  <div class="font-semibold text-slate-700 mb-2">TCP+TLS против QUIC: задержка рукопожатия</div>
  <div class="space-y-1">
    <div class="flex gap-2 items-center"><span class="w-32 text-slate-500">TCP SYN/ACK</span><span class="bg-blue-200 rounded px-2 py-0.5">1 RTT</span></div>
    <div class="flex gap-2 items-center"><span class="w-32 text-slate-500">TLS ClientHello</span><span class="bg-blue-200 rounded px-2 py-0.5">1 RTT</span></div>
    <div class="flex gap-2 items-center"><span class="w-32 text-slate-500">Первый HTTP байт</span><span class="bg-blue-300 rounded px-2 py-0.5 font-semibold">= 2 RTT итого</span></div>
  </div>
  <div class="border-t border-slate-300 pt-2 mt-2">
    <div class="flex gap-2 items-center"><span class="w-32 text-slate-500">QUIC Initial</span><span class="bg-green-200 rounded px-2 py-0.5">1 RTT</span></div>
    <div class="flex gap-2 items-center"><span class="w-32 text-slate-500">Первый HTTP байт</span><span class="bg-green-300 rounded px-2 py-0.5 font-semibold">= 1 RTT итого</span></div>
  </div>
  <div class="text-xs text-slate-500 mt-1">На межконтинентальной линии 100 ms: TCP+TLS стоит 200 ms; QUIC стоит 100 ms</div>
</div>

## Определение в одно предложение

QUIC — транспортный протокол поверх UDP, который несёт TLS 1.3-рукопожатие, мультиплексирует много независимых потоков данных и переживает миграцию сети — ваш ноутбук переходит с WiFi на сотовую без потери соединения.

**Метафора.** Представьте почтовую доставку: TCP — это отправка конвертов один за другим в строгом порядке. Потеряешь один — все остальные ждут. QUIC — это почтальон с одной сумкой, в которой много отдельных конвертов; потеряешь один, остальные продолжают движение. Кроме того, сумка QUIC зашифрована.

## Один сценарий от начала до конца

Вы открываете мобильное приложение, которое потоком отправляет видео через QUIC, одновременно отправляя периодические beacon-запросы (аналитика) и чат в реальном времени. В TCP каждый использовал бы своё соединение и платил три рукопожатия. В QUIC они используют одно соединение, так что платите одно 1-RTT-рукопожатие, а потом приложение может использовать stream ID 0 для видео, stream ID 2 для аналитики, stream ID 1 для чата. Потеря пакета в потоке 1 не блокирует видеокадры из потока 0. Когда вы переходите с WiFi на 4G, ID соединения остаётся действительным, все потоки остаются живы, и приложение никогда это не заметит.

<Quiz
  id="01-jr-quiz-1"
  lessonSlug="01-why-quic"
  lang="ru"
  question="Почему QUIC в пользовательском пространстве, а не в ядре?"
  choices={[
    { label: "Пользовательское пространство быстрее, чем ядро", misconception: "Пользовательское пространство на самом деле медленнее — компромисс в гибкости и скорости эволюции, не в сырой скорости." },
    { label: "Чтобы приложения контролировали логику перегрузки и протокол мог эволюционировать без перекомпиляции ядра ОС", correct: true },
    { label: "Потому что UDP не работает в ядре", misconception: "UDP отлично работает в ядре — традиционно TCP и UDP оба там работали. Расположение QUIC — выбор дизайна." },
    { label: "Чтобы избежать фильтрации middlebox", misconception: "Размещение в пользовательском пространстве помогает эволюции, а не предотвращению фильтрации. Middlebox'ы видят QUIC как UDP-пакеты независимо от того, где он работает." },
  ]}
/>

<Inset kind="why" lang="ru">
RFC 9000 стандартизировался тринадцать лет (опубликован в мае 2021). Главная сложность — убедить интернет-сообщество доверить новый транспорт пользовательскому пространству, традиционно предназначенному для кода приложений, а не для гарантий доставки. Google выпустил ранний прототип (gQUIC) в Chrome в 2012 году, измерил реальные выигрыши в задержке в масштабе YouTube и передал данные в IETF как доказательство.
</Inset>

<DragOrder
  id="01-jr-drag-1"
  lessonSlug="01-why-quic"
  lang="ru"
  prompt="Расставьте ключевые события в QUIC-соединении от ноутбука к мобильной сети:"
  items={[
    "Ноутбук отправляет QUIC Initial-пакет с Connection ID (например, 0x12345678)",
    "Мобильный получает, открывает соединение, Connection ID остаётся привязан к сессии",
    "Ноутбук переходит на сотовую — IP-адрес меняется, но Connection ID всё ещё 0x12345678",
    "Мобильный узнаёт Connection ID, валидирует новый IP и держит соединение живым",
    "Все потоки и состояние управления потоком пережили переход — без переподключения",
  ]}
/>

<NumbersCard
  id="01-quic-latency-summary"
  title="Ключевые числа QUIC"
  stats={[
    { label: "Стоимость рукопожатия", value: "1 RTT (vs TCP+TLS 2 RTT)" },
    { label: "Стоимость возобновления", value: "0 RTT на кешированном сессионном тикете" },
    { label: "Загрузка страницы на мобильных с потерями", value: "на 30–55% быстрее HTTP/2" },
    { label: "Интернет-трафик на HTTP/3", value: "~21% (2026)" },
  ]}
/>

</Explanation>

<StackDiagram
  label="Два стека протоколов: TCP+TLS держит транспорт и криптографию отдельными слоями ядра, а QUIC объединяет транспорт и TLS 1.3 в один слой пользовательского пространства поверх UDP"
  hue="var(--d-network)"
  caption="QUIC выносит логику надёжного транспорта и TLS из ядра в один слой пользовательского пространства поверх UDP. Middlebox'ы видят только UDP; протокол эволюционирует вместе с приложением, а не с ОС."
  layers={[
    { label: "HTTP/3", note: "приложение" },
    { label: "QUIC", note: "транспорт + TLS 1.3 (user space)" },
    { label: "UDP", note: "ядро — непрозрачно для middlebox'ов" },
    { label: "IP", note: "сеть" },
  ]}
/>

<KeyTakeaway>QUIC обходит окостенение ядра, работая поверх UDP в пользовательском пространстве, сокращает установку соединения с 2 RTT до 1 RTT и позволяет независимым потокам передавать данные одновременно без блокировки друг друга.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="01-why-quic-retrieval"
  lang="ru"
  questions={[
    { q: "Что такое окостенение middlebox и как QUIC его обходит?", a: "Middlebox'ы (брандмауэры, NAT) понимают только существующие TCP-опции и молча отбрасывают новые, замораживая эволюцию TCP. QUIC работает поверх UDP, который middlebox'ы воспринимают как непрозрачный, и шифрует большую часть своего заголовка — middlebox'у нечего интерпретировать или вырезать." },
    { q: "В одном предложении: в чём главное преимущество QUIC по задержке перед TCP+TLS?", a: "QUIC объединяет TCP-рукопожатие и TLS-рукопожатие в один обмен 1-RTT (вместо 1 RTT TCP плюс 1 RTT TLS), экономя один RTT до начала передачи данных приложения." },
    { q: "Как QUIC сохраняет соединение при изменении IP-адреса клиента?", a: "QUIC-соединения идентифицируются Connection ID, а не 4-туплом. При смене IP клиента Connection ID остаётся тем же, сервер валидирует новый путь через PATH_CHALLENGE/PATH_RESPONSE, и все потоки продолжаются без переподключения." },
  ]}
/>

<Recap lang="ru">
Эволюция TCP в ядре была заморожена middlebox-окостенением на десятилетия — любая новая TCP-опция отбрасывается до того, как достигнет пира. QUIC обходит это, работая поверх UDP в пользовательском пространстве, где любое приложение может еженедельно отгружать обновлённое управление перегрузкой. Объединённое рукопожатие транспорт+TLS экономит один полный RTT: 100 ms на межконтинентальной линии, 150 ms на трансокеанской. Независимое мультиплексирование потоков означает, что потерянный пакет блокирует только свой поток, а не всё соединение. Цена — CPU: обработка пакетов в пользовательском пространстве и пошаговое AES-GCM-шифрование добавляют 15–30% накладных расходов на байт по сравнению с kernel TCP. Теперь, когда увидите HTTP/3 в DevTools или QUIC-согласование в серверных логах, вы будете знать, что за этим стоит: один сэкономленный RTT и протокол, способный развиваться без ожидания патча ядра ОС.
</Recap>
