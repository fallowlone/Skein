---
concepts:
- quic-handshake
- crypto-frame
- packet-number-space
- 0-rtt
- 1-rtt
- encryption-levels
deepensInto:
- 06-0rtt-and-encryption
estMin: 12
lang: ru
lessonType: topic
level: middle
order: 3
prereqs:
- 05-tls-handshake/02-tls-record-and-handshake
- 10-quic-internals/01-why-quic
- 10-quic-internals/02-quic-streams-and-hol
slug: 03-handshake-and-1rtt
sources:
- https://datatracker.ietf.org/doc/html/rfc9000
- https://datatracker.ietf.org/doc/rfc9001/
- https://blog.cloudflare.com/even-faster-connection-establishment-with-quic-0-rtt-resumption/
spiral:
- latency
status: ready
summary: QUIC объединяет транспорт и TLS в один обмен — клиент отправляет TLS ClientHello
  в первом пакете, и данные приложения текут уже после одного RTT.
title: Объединённое рукопожатие и 1-RTT
track: networking
unit: 10-quic-internals
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import TraceScenario from "~/components/pedagogy/TraceScenario.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import NumbersCard from "~/components/layout/NumbersCard.astro";
import SequenceDiagram from "~/components/diagram/SequenceDiagram.astro";

<Hook>
TCP завершает своё рукопожатие. Потом TLS начинает своё. Это отдельные протоколы с отдельными RTT. Каждое новое соединение платит двойную пошлину. QUIC отказывается принимать это — он проводит оба рукопожатия в одном обмене пакетами.
</Hook>

<Crux>QUIC объединяет транспорт и TLS в один обмен 1-RTT, встраивая TLS CRYPTO-фреймы прямо в QUIC Initial-пакеты.</Crux>

<Explanation>

## Объединённое рукопожатие

QUIC устраняет отдельный уровень транспортного рукопожатия. Вместо TCP SYN/SYN-ACK (1 RTT), а затем TLS ClientHello/ServerHello (ещё RTT), QUIC переносит TLS-рукопожатие внутри CRYPTO-фреймов, путешествующих в QUIC-пакетах.

Последовательность:

1. **Клиент** отправляет Initial-пакет с CRYPTO-фреймом, содержащим TLS **ClientHello**.
2. **Сервер** отвечает собственным Initial-пакетом с CRYPTO-фреймом **ServerHello + EncryptedExtensions + Certificate + CertificateVerify** — всё в одном пакете.
3. **Клиент** отправляет Handshake-пакет с CRYPTO-фреймом **Finished**.
4. Обе стороны переходят в состояние **1-RTT**. Клиент может отправлять данные потока сразу после завершения шага 1.

**Результат:** 1 RTT от старта клиента до получения ответа сервера, против 2 RTT у TCP+TLS.

<div data-lesson-visual class="rounded-lg border border-slate-200 bg-slate-50 p-4 my-4 text-sm space-y-2">
  <div class="font-semibold text-slate-700 mb-2">Последовательность пакетов рукопожатия QUIC</div>
  <div class="space-y-1 font-mono text-xs">
    <div class="flex gap-3"><span class="text-blue-600 w-16">Клиент</span><span class="text-slate-400">→</span><span class="text-green-700 w-16">Сервер</span><span>Initial [CRYPTO: ClientHello] + Padding</span></div>
    <div class="flex gap-3"><span class="text-blue-600 w-16">Клиент</span><span class="text-slate-400">←</span><span class="text-green-700 w-16">Сервер</span><span>Initial [CRYPTO: ServerHello + EE + Cert + CertVerify]</span></div>
    <div class="flex gap-3"><span class="text-blue-600 w-16">Клиент</span><span class="text-slate-400">←</span><span class="text-green-700 w-16">Сервер</span><span>Handshake [CRYPTO: Finished]</span></div>
    <div class="flex gap-3"><span class="text-blue-600 w-16">Клиент</span><span class="text-slate-400">→</span><span class="text-green-700 w-16">Сервер</span><span>Handshake [CRYPTO: Finished] + 1-RTT [STREAM data]</span></div>
    <div class="text-xs text-slate-500 mt-2">Итого: 1 RTT до того, как клиент может отправить данные потока. TCP+TLS = 2 RTT.</div>
  </div>
</div>

## Четыре уровня шифрования

QUIC определяет четыре отдельных уровня шифрования, каждый со своим ключом:

1. **Initial** — детерминированно выводится из Connection ID назначения. Обе стороны могут вычислить эти ключи до обмена материалом, поэтому Initial-пакет может быть зашифрован немедленно.
2. **0-RTT** — выводится из сессионного тикета, кешированного из предыдущего соединения. Позволяет клиенту отправлять данные приложения в ClientHello без ожидания ответа сервера.
3. **Handshake** — выводится из общих секретов после обмена ClientHello-ServerHello. Используется для сообщений Finished.
4. **1-RTT** — финальные постхендшейковые ключи. Все последующие данные потоков используют их.

## Три пространства номеров пакетов

Четыре уровня шифрования отображаются на три пространства номеров пакетов:

- **Initial/Handshake** — каждое имеет собственную последовательность номеров пакетов (0, 1, 2, ...).
- **Application Data** — 0-RTT и 1-RTT разделяют одно пространство.

Разделение критично: **обнаружение потерь независимо для каждого пространства**. Потерянный Initial-пакет запускает PTO только в пространстве Initial — он не уменьшает окно перегрузки для 1-RTT-данных.

<Quiz
  id="03-mid-quiz-1"
  lessonSlug="03-handshake-and-1rtt"
  lang="ru"
  question="Почему в QUIC разные пространства номеров пакетов для Initial/Handshake и 1-RTT?"
  choices={[
    { label: "Чтобы предотвратить переполнение номеров пакетов", misconception: "Переполнение не проблема при 62-битных номерах. Разделение нужно для обнаружения потерь." },
    { label: "Потому что ключи шифрования разные, поэтому потеря пакета в одном пространстве имеет независимое обнаружение от другого", correct: true },
    { label: "Чтобы сервер мог обрабатывать Initial-пакеты до Handshake-пакетов", misconception: "Оба могут прийти не по порядку; разделение пространств — про ключи шифрования и логику потерь." },
    { label: "Потому что UDP переупорядочивает пакеты больше, чем TCP", misconception: "UDP не переупорядочивает; доставка не по порядку — артефакт пути в сети, а не UDP vs. TCP." },
  ]}
/>

<TraceScenario
  id="03-mid-trace-1"
  lessonSlug="03-handshake-and-1rtt"
  lang="ru"
  scenario="Трассируйте QUIC-соединение от инициации клиента до первых данных потока."
  steps={[
    { prompt: "Клиент отправляет Initial-пакет с ClientHello в CRYPTO-фрейме. Какой ключ шифрования он использует?", reveal: "Ключи, детерминированно выведенные из QUIC Initial Secrets на основе Connection ID назначения. Обе стороны могут вывести эти ключи без обмена материалом, поэтому Initial-пакет может быть зашифрован обеими сторонами немедленно." },
    { prompt: "Сервер получает Initial-пакет. Что он отвечает?", reveal: "Сервер отправляет Initial-пакет с CRYPTO-фреймами ServerHello + EncryptedExtensions + Certificate + CertificateVerify, зашифрованными Handshake-ключами (выведенными из общих секретов теперь, когда у него есть ClientHello)." },
    { prompt: "Клиент получает Initial сервера. В каком состоянии находится клиент?", reveal: "Клиент теперь имеет ключевой материал сервера и может вывести 1-RTT-ключи. Рукопожатие завершено с точки зрения клиента; он может отправлять данные потока в 1-RTT-пакетах." },
    { prompt: "Клиент отправляет первые данные потока в 1-RTT-пакете. Сколько RTT прошло с Initial?", reveal: "Ровно 1 RTT от Initial клиента до получения Initial сервера, без дополнительного ожидания. Сравните TCP+TLS: 1 RTT (TCP трёхстороннее рукопожатие) + 1 RTT (TLS) = 2 RTT." },
  ]}
/>

<DragOrder
  id="03-mid-drag-1"
  lessonSlug="03-handshake-and-1rtt"
  lang="ru"
  prompt="Расставьте типы пакетов в QUIC-рукопожатии от начала до завершения:"
  items={[
    "Клиент отправляет Initial-пакет с ClientHello",
    "Сервер отправляет Initial-пакет с ServerHello + EncryptedExtensions + Certificate + CertificateVerify",
    "Сервер отправляет Handshake-пакет с Finished",
    "Клиент отправляет Handshake-пакет с Finished",
    "Обе стороны переходят в состояние 1-RTT и могут отправлять пакеты Short Header с данными потока",
  ]}
/>

<Inset kind="why" lang="ru">
Initial-пакет QUIC использует **детерминированные ключи, выведенные из Connection ID** — кажущийся парадоксальным выбор. Зачем шифровать, если обе стороны могут вычислить ключ? Цель — не секретность на этом этапе, а целостность: шифрование Initial мешает пассивным наблюдателям тривиально подменить ClientHello. Ключи Handshake и 1-RTT, выведенные из материала DH-обмена, обеспечивают реальную конфиденциальность.
</Inset>

<NumbersCard
  id="03-handshake-numbers"
  title="Задержка рукопожатия QUIC"
  stats={[
    { label: "QUIC новое соединение", value: "1 RTT до первого байта потока" },
    { label: "TCP + TLS 1.3 новое соединение", value: "2 RTT до первого байта потока" },
    { label: "QUIC возобновление 0-RTT", value: "0 RTT (данные в ClientHello)" },
    { label: "Экономия на линии 100 ms", value: "100 ms на новое соединение" },
    { label: "Экономия на линии 150 ms (Азия-США)", value: "150 ms на новое соединение" },
  ]}
/>

</Explanation>

<SequenceDiagram
  label="Объединённое рукопожатие QUIC: клиент отправляет Initial с TLS ClientHello, сервер отвечает ServerHello с сертификатом, затем обе стороны достигают 1-RTT, и данные потока текут после одного round trip"
  hue="var(--d-network)"
  actors={["Клиент", "Сервер"]}
  messages={[
    { from: "Клиент", to: "Сервер", label: "Initial [CRYPTO: ClientHello]" },
    { from: "Сервер", to: "Клиент", label: "Initial [CRYPTO: ServerHello+Cert]" },
    { from: "Сервер", to: "Клиент", label: "Handshake [CRYPTO: Finished]" },
    { from: "Клиент", to: "Сервер", label: "Handshake [Finished] + 1-RTT [STREAM data]" },
  ]}
  caption="Транспорт и TLS завершаются в одном обмене пакетами — один round trip до первого байта потока, против рукопожатия TCP плюс отдельного TLS-рукопожатия (2 RTT)."
/>

<KeyTakeaway>QUIC встраивает TLS CRYPTO-фреймы в Initial-пакеты, завершая транспорт и TLS за один RTT. Четыре уровня шифрования и три пространства номеров пакетов обеспечивают независимое обнаружение потерь на каждой фазе рукопожатия.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="03-handshake-retrieval"
  lang="ru"
  questions={[
    { q: "Почему 1-RTT рукопожатие QUIC экономит задержку по сравнению с TCP+TLS, и как выглядят числа на межконтинентальных линиях?", a: "TCP требует 1 RTT для трёхстороннего рукопожатия (SYN, SYN-ACK, ACK). TLS 1.3 поверх требует ещё RTT. Итого: 2 RTT до первого байта приложения. QUIC объединяет оба: клиент отправляет Initial с ClientHello, сервер отвечает ключевым материалом, и клиент может отправлять данные потока в следующем пакете. На линии 100 ms TCP+TLS стоит 200 ms; QUIC стоит 100 ms." },
    { q: "Каковы четыре уровня шифрования QUIC и для чего каждый используется?", a: "Initial: детерминированный из Connection ID, для обмена ClientHello/ServerHello. 0-RTT: из сессионного тикета, для ранних данных до Finished сервера. Handshake: из DH-обмена, для сообщений Finished. 1-RTT: финальные постхендшейковые ключи, для всех данных потоков." },
    { q: "Почему пространства номеров пакетов разделены по уровням шифрования?", a: "Обнаружение потерь должно быть независимым для каждого уровня шифрования. Потерянный Initial-пакет запускает PTO только в пространстве Initial — он не может уменьшить окно перегрузки для 1-RTT-данных. Без разделения потеря на фазе рукопожатия некорректно сигнализировала бы перегрузку для трафика приложения." },
  ]}
/>

<Recap lang="ru">
Рукопожатие QUIC объединяет то, что TCP и TLS держали раздельно: CRYPTO-фрейм внутри QUIC Initial-пакета несёт TLS ClientHello, поэтому транспорт и TLS завершаются за один RTT вместо двух. Сервер отвечает полной цепочкой Certificate в том же пакете, и клиент может отправлять данные потока немедленно. Четыре уровня шифрования (Initial, 0-RTT, Handshake, 1-RTT) отображаются на три пространства номеров пакетов, чтобы обнаружение потерь на каждой фазе не мешало другим. На линии 100 ms это экономит 100 ms на каждое новое соединение — 150 ms на трансокеанских путях. Возобновление 0-RTT идёт дальше: кешированный сессионный тикет позволяет клиенту прикрепить данные приложения к самому ClientHello, достигая нулевых RTT ценой уязвимости к реплей-атакам для неидемпотентных запросов.
</Recap>
