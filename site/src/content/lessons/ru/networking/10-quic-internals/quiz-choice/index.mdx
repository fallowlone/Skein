---
concepts:
- user-space-transport
- head-of-line-blocking
- connection-migration
- 0-rtt-replay
- packet-encryption
- cpu-cost
estMin: 13
lang: ru
level: senior
order: 7
prereqs: []
slug: quiz-choice
sources:
- https://datatracker.ietf.org/doc/html/rfc9000
- https://datatracker.ietf.org/doc/html/rfc9002
- https://blog.cloudflare.com/the-road-to-quic/
status: ready
summary: "Синтез с множественным выбором по всему юниту QUIC — транспорт в user space, независимость stream'ов, миграция, рукопожатие 1-RTT и 0-RTT, шифрование и CPU-цена развёртывания."
title: 'QUIC внутри: тест с множественным выбором'
track: networking
unit: 10-quic-internals
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Шесть вопросов через весь юнит. Каждый отражает реальное решение — когда выигрывает QUIC, когда TCP и что эта конструкция даёт и чего стоит, — а не определение для зубрёжки.
</Hook>

<Goal lang="ru">
Убедись, что связываешь части: зачем QUIC живёт в user space, что на самом деле дают независимость stream'ов и Connection ID, где экономят рукопожатия 1-RTT и 0-RTT и где 0-RTT кусается, и почему тот же протокол, что выигрывает на мобильном, проигрывает на быстром LAN.
</Goal>

<Quiz
  id="10-quic-internals-choice-1"
  lessonSlug="10-quic-internals"
  lang="ru"
  question="Два сервиса отдают HTTP. Один — межконтинентальный API (мелкие payload'ы, RTT 120 мс, ~1% мобильных потерь); другой стримит статику на 1 Gbps клиентам в LAN. На каждом можно запустить HTTP/3 (QUIC) или HTTP/2 (TCP). Какая раскладка верна и почему?"
  choices={[
    { label: "QUIC для API, TCP для статики в LAN. API ограничен round-trip'ами, поэтому рукопожатие 1-RTT и пер-stream-восстановление окупаются; LAN-сервис CPU-bound на быстром линке, где пер-байтовая user-space-цена QUIC насыщает ядро и goodput падает ~45% без выигрыша от HoL.", correct: true },
    { label: "QUIC для обоих — один протокол упрощает тулинг, а QUIC просто новее.", misconception: "На быстром LAN пер-пакетный AES-GCM и user-space-фрейминг QUIC делают CPU узким местом раньше, чем заполнится линк; goodput падает ~45%, а на почти безпотерьном коротком пути выигрыша от head-of-line нет. Новее не значит быстрее везде." },
    { label: "TCP для обоих — CPU-оверхед QUIC всегда перевешивает выигрыш по задержке.", misconception: "На пути API с RTT 120 мс и потерями узкое место — сеть, а не CPU, поэтому оверхед QUIC невидим, а рукопожатие 1-RTT и независимость stream'ов режут реальную задержку. Оверхед кусается только там, где CPU насыщается первым." },
    { label: "QUIC для статики в LAN (сырой throughput) и TCP для API.", misconception: "Это инверсия компромисса. QUIC помогает там, где доминируют round-trip'ы и потери (API); kernel-offload TCP (kTLS, GSO, RSS) выигрывает там, где гонишь байты быстро по чистому линку (статика в LAN)." },
  ]}
/>

<Quiz
  id="10-quic-internals-choice-2"
  lessonSlug="10-quic-internals"
  lang="ru"
  question="Сервис на HTTP/2-over-TCP и сервис на HTTP/3-over-QUIC мультиплексируют по три параллельных ответа. При 0.5% потерь и RTT 100 мс теряется один пакет с ответом B. Что происходит с ответами A и C и почему?"
  choices={[
    { label: "В HTTP/2 все три ждут ретрансмита пакета B (~230 мс); в HTTP/3 ждёт только stream B (~0.4 мс), а A и C продолжают доставку. TCP навязывает единый упорядоченный поток байт, поэтому потеря блокирует все мультиплексированные stream'ы; QUIC даёт каждому stream'у независимый порядок и восстановление потерь.", correct: true },
    { label: "Оба протокола стопорят все три — мультиплексирование значит общий порядок доставки независимо от транспорта.", misconception: "HTTP/2 мультиплексирует на уровне HTTP, но едет по одному упорядоченному TCP-stream'у, поэтому потеря блокирует всё. QUIC переносит мультиплексирование в транспорт, давая каждому stream'у свой порядок — это и есть исправление HoL, о котором юнит." },
    { label: "В обоих случаях A и C продолжают, потому что HTTP-мультиплексирование уже изолирует stream'ы.", misconception: "Мультиплексирование HTTP/2 не помогает: блокировка идёт на транспорте TCP под ним. Только QUIC, владеющий транспортом, может изолировать stream'ы при потере." },
    { label: "В HTTP/3 всё соединение сбрасывается и все три перезапускаются с контрольной точки.", misconception: "QUIC ретрансмитит только потерянный STREAM-фрейм для B; A и C нетронуты, соединение не сбрасывается. Пер-stream-восстановление — в этом весь смысл." },
  ]}
/>

<Quiz
  id="10-quic-internals-choice-3"
  lessonSlug="10-quic-internals"
  lang="ru"
  question="Пользователь выходит из офиса; телефон переключается с WiFi на LTE посреди скачивания. Соединение QUIC выживает, а TCP бы умер. Что переносит соединение через смену IP и что сервер делает, прежде чем довериться новому адресу?"
  choices={[
    { label: "QUIC идентифицирует соединение по непрозрачному Connection ID в заголовке пакета, а не по 4-tuple, поэтому тот же ID приходит с нового IP. Сервер шлёт токен PATH_CHALLENGE, ждёт совпадающий PATH_RESPONSE и при этом отправляет не больше 3x полученных байт (anti-amplification), пока новый путь не валидирован.", correct: true },
    { label: "QUIC молча переигрывает рукопожатие на новом IP в фоне, поэтому выглядит бесшовно, но это по сути новое соединение.", misconception: "Нового рукопожатия не происходит. Всё состояние stream'ов, flow-control и congestion выживает, потому что соединение идентифицирует Connection ID, а не IP. Повторное рукопожатие потеряло бы именно это состояние." },
    { label: "UDP-пакетам не нужен фиксированный source IP, поэтому соединение просто продолжается без лишних шагов.", misconception: "UDP-пакеты всё равно несут source IP, а слепое переключение открыло бы вектор reflection-amplification. QUIC обязан валидировать новый путь через PATH_CHALLENGE/PATH_RESPONSE и дросселировать до 3x до этого." },
    { label: "Keep-alive-пакеты удерживают 4-tuple открытым через смену сети.", misconception: "Keep-alive лишь не дают простаивающему соединению таймаутиться; они не сохранят соединение, чей 4-tuple сменился. Через миграцию выживает идентификация по Connection ID." },
  ]}
/>

<Quiz
  id="10-quic-internals-choice-4"
  lessonSlug="10-quic-internals"
  lang="ru"
  question="Команда включает 0-RTT-resumption, чтобы срезать round-trip на переподключениях. Через неделю финансы сообщают об изредка дублирующихся переводах средств. В чём механизм и каков корректный фикс?"
  choices={[
    { label: "Данные 0-RTT едут в ClientHello без защиты от replay на уровне TLS-записи, поэтому on-path-атакующий (или переотправленный пакет) может переиграть ciphertext, и сервер выполнит его повторно. Ограничь 0-RTT идемпотентными запросами; на неидемпотентные (POST/DELETE) отвечай 425 Too Early, заставляя повтор после рукопожатия 1-RTT, и изолируй ключи session-ticket по серверам.", correct: true },
    { label: "Данные 0-RTT шлются в открытом виде, поэтому атакующий читает перевод и переотправляет его.", misconception: "0-RTT зашифрован ключом session-ticket; атакующий не может его прочитать. Изъян в том, что тот же ciphertext можно переиграть и обработать повторно — это проблема replay, а не раскрытия." },
    { label: "Сведи время жизни session-ticket к нулю, чтобы 0-RTT вообще не был возможен — replay иначе нерешаем.", misconception: "Полное отключение 0-RTT выбрасывает тот выигрыш по задержке, ради которого его включали. Стандартный фикс — проверка идемпотентности плюс 425 Too Early, что сохраняет 0-RTT для безопасных запросов." },
    { label: "Переключи congestion-контроллер с CUBIC на BBR; дублирование — артефакт ретрансмитов из-за агрессивного зондирования.", misconception: "Congestion control тут ни при чём. Дублирующиеся переводы возникают из переигранных early-data 0-RTT, дважды выполняющих неидемпотентную операцию." },
  ]}
/>

<Quiz
  id="10-quic-internals-choice-5"
  lessonSlug="10-quic-internals"
  lang="ru"
  question="После перехода на HTTP/3 сетевая команда жалуется, что их пакетные дашборды (счётчики запросов по flow, гистограммы RTT, детект медленных клиентов) погасли, и они больше не могут вбросить RST, чтобы убить плохой flow на middlebox. Как это читать?"
  choices={[
    { label: "Ожидаемо. Пакеты QUIC с short-header'ом шифруют packet number и весь payload, поэтому наблюдатель видит лишь Connection ID и UDP 4-tuple — ни sequence-номеров, ни данных stream'а, ни подделываемого RST. Фикс — наблюдаемость на уровне приложения: qlog (RFC 9312), browser timing API и eBPF-пробы на user-space-сокете.", correct: true },
    { label: "Это мисконфигурация — QUIC должен показывать те же пакетные метаданные, что и TCP, как только на мониторе загрузят ключи дешифровки.", misconception: "QUIC намеренно шифрует почти весь пакет end-to-end; у пассивного монитора ключей нет и не должно быть. Непрозрачность — это by design, а не ошибка конфигурации." },
    { label: "QUIC переехал на нестандартный порт, который тулы не видят; направь их на новый порт — и дашборды вернутся.", misconception: "QUIC работает на UDP 443. Метаданные отсутствуют потому, что зашифрованы, а не потому, что порт скрыт — смена порта ничего не меняет." },
    { label: "Возможность RST исчезла, потому что у UDP нет reset, но счётчики запросов всё равно должны быть видны в открытом виде.", misconception: "И то, и другое исчезло по одной причине: packet number и payload зашифрованы, поэтому без ключа невозможны ни учёт запросов, ни поддельный RST. Потеря RST-инъекции — на деле выигрыш в безопасности." },
  ]}
/>

<Quiz
  id="10-quic-internals-choice-6"
  lessonSlug="10-quic-internals"
  lang="ru"
  question="Коллега утверждает, что QUIC строго лучше TCP, потому что «он работает в user space, значит быстрее». Где ошибка в рассуждении и что на самом деле даёт размещение в user space?"
  choices={[
    { label: "User space медленнее на байт, а не быстрее — каждый пакет платит за AES-GCM, декодирование VarInt и фрейминг в коде приложения вместо NIC/kernel-offload, что стоит на 15–30% больше CPU. Зато он даёт эволюционируемость: подключаемый congestion control на соединение (NewReno/CUBIC/BBR), еженедельные обновления вместе с приложением и иммунитет к ossification middlebox'ов — но не сырую скорость.", correct: true },
    { label: "Рассуждение верно — обход ядра убирает оверхед syscall'ов, поэтому транспорт в user space по своей природе быстрее.", misconception: "User-space QUIC добавляет syscall'ы (один sendmsg на датаграмму без GSO) и теряет NIC-offload крипто, поэтому он медленнее на байт. Его преимущество — гибкость и HoL-free stream'ы, а не throughput." },
    { label: "Ошибка в том, что QUIC на самом деле работает в ядре, как TCP; верно лишь утверждение про скорость.", misconception: "QUIC работает в user space by design — отсюда и его CPU-цена, и способность эволюционировать без обновлений ядра. Местоположение названо верно; неверен вывод «быстрее»." },
    { label: "User space быстрее лишь потому, что позволяет QUIC пропустить шифрование, которое обязан делать TCP.", misconception: "QUIC шифрует больше TCP, а не меньше — почти весь пакет, на каждый пакет. CPU-цена выше, а выигрыш — в скорости эволюции и независимости stream'ов." },
  ]}
/>

<Recap lang="ru">
Юнит сводится к одному взвешенному решению: QUIC меняет CPU на задержку и устойчивость. Размещение в user space даёт эволюционируемость (подключаемый CC, отсутствие ossification) ценой 15–30% CPU на байт; независимость stream'ов убивает head-of-line-блокировку при потерях; Connection ID переживают миграцию сети с валидацией пути и защитой 3x anti-amplification; объединённое рукопожатие 1-RTT экономит round-trip, а 0-RTT — ещё один ценой replay (только идемпотентное, 425 Too Early); почти полное шифрование блокирует RST-инъекции и сниффинг, но ослепляет пакетный мониторинг. Бери QUIC на путях, ограниченных round-trip'ами, потерями и мобильностью, — оставляй TCP там, где быстрый чистый линк делает CPU узким местом.
</Recap>
