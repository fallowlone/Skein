---
concepts:
- quic-streams
- head-of-line-blocking
- stream-id
- flow-control
- multiplexing
deepensInto:
- 03-handshake-and-1rtt
- 05-loss-detection-and-cc
estMin: 10
lang: ru
lessonType: topic
level: junior
order: 2
prereqs:
- 10-quic-internals/01-why-quic
slug: 02-quic-streams-and-hol
sources:
- https://datatracker.ietf.org/doc/html/rfc9000
- https://datatracker.ietf.org/doc/html/rfc9114
- https://www.debugbear.com/blog/http3-vs-http2-performance
spiral:
- latency
- multiplexing
status: ready
summary: QUIC мультиплексирует независимые потоки по одному UDP-соединению — потерянный
  пакет блокирует только свой поток, а не всё соединение, давая выигрыш 575x на нестабильных
  линиях.
title: QUIC-потоки и head-of-line blocking
track: networking
unit: 10-quic-internals
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import MetaphorComplete from "~/components/pedagogy/MetaphorComplete.astro";
import NumbersCard from "~/components/layout/NumbersCard.astro";
import FadedExample from "~/components/pedagogy/FadedExample.tsx";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";
import Infographic from "~/components/diagram/Infographic.astro";
import holBlockingLatency from "./infographics/hol-blocking-latency.svg?raw";

<Hook>
В вашем HTTP/2-соединении три запроса в полёте. Один пакет теряется на перегруженном роутере. Теперь все три ответа зависли — хотя у двух из них все байты уже в буфере, готовы к доставке. QUIC создавался, чтобы сделать это невозможным.
</Hook>

<Crux>Независимые потоки QUIC означают: потерянный пакет блокирует только свой поток, не всё соединение.</Crux>

<Explanation>

## Head-of-line blocking в TCP

TCP доставляет все байты в одном упорядоченном потоке. Если пакет N потерян, все байты после него — в каждом логическом сообщении — ждут переотправки пакета N, прежде чем приложение может их обработать.

HTTP/2 мультиплексирует пары запрос-ответ через одно TCP-соединение. Когда TCP теряет пакет, все HTTP/2-потоки зависают — даже те, чьи данные пришли целыми. Это **head-of-line blocking** на уровне соединения.

**Числа:** При 0,5% случайных потерь и RTT 100 ms сообщение, заблокированное в упорядоченной доставке TCP, ждёт ~230 ms. QUIC сокращает это до ~0,4 ms — улучшение в **575 раз**.

<Infographic
  svg={holBlockingLatency}
  label="Столбчатая диаграмма сравнивает задержку head-of-line blocking на одном нестабильном пути: упорядоченная доставка TCP задерживает сообщение примерно на 230 ms, а восстановление QUIC по потокам задерживает только затронутый поток примерно на 0,4 ms."
  caption="Один путь (0,5% потерь, RTT 100 ms). Восстановление после потерь по потокам превращает зависание 230 ms на транспортном уровне в 0,4 ms на уровне потока — разрыв 575x и есть причина уходить с TCP на нестабильных линиях."
  hue="var(--d-infra)"
/>

## Потоки QUIC независимы

QUIC мультиплексирует множество двунаправленных и однонаправленных потоков по одному соединению. Каждый поток имеет:

- Собственное **окно управления потоком** (фрейм `max-stream-data`).
- Собственное **фреймирование на уровне приложения** (STREAM-фреймы, каждый с ID потока и смещением).
- Независимое **обнаружение потерь** — повтор STREAM-фрейма затрагивает только его поток.

Когда STREAM-фрейм (кадр данных конкретного потока, несущий смещение и полезную нагрузку) с данными потока 7 теряется, только получатель потока 7 ждёт переотправки; потоки 1, 3, 5 продолжают получать новые данные.

<div data-lesson-visual class="rounded-lg border border-slate-200 bg-slate-50 p-4 my-4 text-sm space-y-3">
  <div class="font-semibold text-slate-700">Независимость потоков при потере пакета</div>
  <div class="grid grid-cols-4 gap-1 font-mono text-xs">
    <div class="font-semibold text-slate-500">Пакет</div>
    <div class="font-semibold text-slate-500">Поток 0 (видео)</div>
    <div class="font-semibold text-slate-500">Поток 1 (чат)</div>
    <div class="font-semibold text-slate-500">Поток 2 (аналитика)</div>
    <div>пакет #1</div><div class="text-green-600">доставлен</div><div class="text-green-600">доставлен</div><div class="text-green-600">доставлен</div>
    <div>пакет #2</div><div class="text-red-600 font-bold">ПОТЕРЯН</div><div class="text-green-600">доставлен</div><div class="text-green-600">доставлен</div>
    <div>пакет #3</div><div class="text-amber-600">ожидание...</div><div class="text-green-600">доставлен</div><div class="text-green-600">доставлен</div>
    <div>пакет #4 (повтор)</div><div class="text-green-600">восстановлен</div><div class="text-green-600">доставлен</div><div class="text-green-600">доставлен</div>
  </div>
  <div class="text-xs text-slate-500">Только поток 0 (видео) ждёт переотправки. Чат и аналитика продолжают работу.</div>
</div>

## Распределение Stream ID

Stream ID кодирует инициатора и направление в двух младших битах:

- Клиент-инициированный двунаправленный: 0, 4, 8, 12, ... (младшие 2 бита = 00)
- Сервер-инициированный двунаправленный: 1, 5, 9, 13, ... (младшие 2 бита = 01)
- Клиент-инициированный однонаправленный: 2, 6, 10, 14, ... (младшие 2 бита = 10)
- Сервер-инициированный однонаправленный: 3, 7, 11, 15, ... (младшие 2 бита = 11)

В HTTP/3 каждая пара HTTP-запрос/ответ — это один клиент-инициированный двунаправленный поток (IDs 0, 4, 8...). Эта кодировка позволяет обеим сторонам выделять ID независимо, без координации.

<FadedExample
  client:visible
  id="02-stream-id-mapping"
  lessonSlug="02-quic-streams-and-hol"
  lang="ru"
  title="Распределение Stream ID в QUIC"
  steps={{
    solved: (
      <div class="space-y-3">
        <p><strong>Как работают Stream ID:</strong></p>
        <ul class="list-disc list-inside space-y-1 text-sm">
          <li>Клиент-инициированный двунаправленный: 0, 4, 8, 12, ...</li>
          <li>Сервер-инициированный двунаправленный: 1, 5, 9, 13, ...</li>
          <li>Клиент-инициированный однонаправленный: 2, 6, 10, 14, ...</li>
          <li>Сервер-инициированный однонаправленный: 3, 7, 11, 15, ...</li>
        </ul>
        <p class="text-xs text-gray-600 mt-3"><strong>Почему такая схема?</strong> Младшие биты кодируют направление и однонаправленность. Одного монотонного счётчика для каждой категории достаточно — координации между клиентом и сервером не нужно.</p>
      </div>
    ),
    semi: {
      prompt: "Сервер отправляет данные клиенту по однонаправленному потоку. Какие первые три допустимых Stream ID может использовать сервер?",
      blanks: [
        { id: "stream1", expected: "3", placeholder: "Первый ID (например, 3)" },
        { id: "stream2", expected: "7", placeholder: "Второй ID (например, 7)" },
        { id: "stream3", expected: "11", placeholder: "Третий ID (например, 11)" },
      ],
    },
    blank: {
      prompt: "Клиент открывает двунаправленный поток. Его ID должен быть чётным или нечётным?",
      reveal: (
        <div class="space-y-2">
          <p><strong>Ответ: чётным (0, 4, 8, ...)</strong></p>
          <p class="text-sm">Клиент-инициированные потоки всегда чётные; сервер-инициированные — всегда нечётные. Это позволяет каждой стороне независимо выделять Stream ID без координации.</p>
        </div>
      ),
    },
  }}
  misconceptions={{
    "stream1:1": "Сервер-инициированные однонаправленные потоки начинаются с 3, не с 1. (1 — это сервер-инициированный двунаправленный.)",
    "stream1:2": "Клиент-инициированный однонаправленный, не серверный. Сервер использует нечётные Stream ID.",
  }}
/>

<Quiz
  id="02-jr-quiz-1"
  lessonSlug="02-quic-streams-and-hol"
  lang="ru"
  question="В QUIC, если пакет с данными потока 5 потерян, что происходит с потоком 3?"
  choices={[
    { label: "Данные потока 3 тоже ждут переотправки — они в одном соединении", misconception: "Это именно проблема head-of-line blocking, которую решает QUIC. Каждый поток независим." },
    { label: "Поток 3 продолжает приходить нормально — только поток 5 ждёт переотправленные данные", correct: true },
    { label: "Всё соединение сбрасывается и оба потока перезапускаются", misconception: "QUIC сохраняет состояние соединения через потерю пакетов; переотправляются только потерянные данные." },
    { label: "Поток 3 автоматически переходит на другое соединение", misconception: "Потоки остаются на одном соединении. В этом вся суть мультиплексирования." },
  ]}
/>

<MetaphorComplete
  id="02-jr-metaphor-1"
  lessonSlug="02-quic-streams-and-hol"
  lang="ru"
  setup="Исправление head-of-line blocking в QUIC похоже на _______, где у каждого потока своя полоса, поэтому потеря одного пакета в одной полосе не замораживает всё шоссе."
  accepted={["шоссе", "дорогу", "магистраль", "многополосное шоссе", "трассу"]}
  canonical="многополосное шоссе (любая транспортная система с независимыми путями)"
  explanation="TCP доставляет все байты в одном упорядоченном потоке — как однополосная дорога, где одна застрявшая машина блокирует всех. QUIC мультиплексирует независимые потоки — как многополосное шоссе, где каждая полоса (поток) движется независимо и затор в одной полосе не замораживает остальные."
/>

<NumbersCard
  id="02-hol-numbers"
  title="Head-of-line blocking: TCP против QUIC"
  stats={[
    { label: "Блокировка сообщения TCP при 0,5% потерь, RTT 100 ms", value: "~230 ms" },
    { label: "Блокировка потока QUIC (те же условия)", value: "~0,4 ms" },
    { label: "Коэффициент улучшения", value: "575×" },
    { label: "Загрузка страницы на мобильных с потерями (HTTP/3 vs HTTP/2)", value: "на 30–55% быстрее" },
  ]}
/>

</Explanation>

<FlowDiagram
  label="Одно QUIC-соединение разветвляется на три независимых потока; потеря пакета в потоке 0 блокирует только поток 0, пока потоки 1 и 2 продолжают доставку"
  hue="var(--d-network)"
  perRow={4}
  caption="У каждого потока свои смещения и своё восстановление после потерь. Потерянный пакет в потоке 0 (видео) переотправляется независимо — чат и аналитика не ждут. TCP, напротив, это единый упорядоченный поток байт, где эта потеря заблокировала бы все три."
  nodes={[
    { id: "conn", label: "QUIC conn", sub: "1 соединение" },
    { id: "s0", label: "Stream 0", sub: "видео — пакет ПОТЕРЯН" },
    { id: "s1", label: "Stream 1", sub: "чат — идёт" },
    { id: "s2", label: "Stream 2", sub: "аналитика — идёт" },
  ]}
  edges={[
    { from: "conn", to: "s0", label: "блокирует только s0" },
    { from: "conn", to: "s1", label: "не затронут" },
    { from: "conn", to: "s2", label: "не затронут" },
  ]}
/>

<KeyTakeaway>Потоки QUIC несут независимые данные с независимым восстановлением после потерь — потерянный пакет замораживает только свой поток. При 0,5% потерь это выигрыш 575x по сравнению с однопоточной доставкой TCP.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="02-quic-streams-retrieval"
  lang="ru"
  questions={[
    { q: "Объясните проблему head-of-line blocking в TCP и как QUIC её решает с конкретными числами.", a: "В TCP все байты текут в одном упорядоченном потоке. Если пакет N потерян, все последующие байты — даже в несвязанных сообщениях — ждут повтора N. При 0,5% потерях и RTT 100 ms блокировка составляет ~230 ms. QUIC даёт каждому сообщению собственный поток с независимым обнаружением потерь; только этот поток зависает. Те же условия: ~0,4 ms — улучшение в 575 раз." },
    { q: "Что кодируют два младших бита Stream ID в QUIC?", a: "Бит 0 кодирует инициатора (0 = клиент, 1 = сервер). Бит 1 кодирует направленность (0 = двунаправленный, 1 = однонаправленный). Так поток 0 — клиент-инициированный двунаправленный, поток 3 — сервер-инициированный однонаправленный." },
    { q: "Почему HTTP/2 поверх TCP всё ещё страдает от HoL-блокировки, несмотря на мультиплексирование HTTP/2?", a: "HTTP/2 мультиплексирует на уровне HTTP, но работает поверх единого TCP-потока. TCP должен доставлять байты по порядку — потерянный пакет зависает во всех HTTP/2-потоках, независимо от логического запроса. Блокировка на транспортном уровне (TCP), и HTTP/2 не может её исправить." },
  ]}
/>

<Recap lang="ru">
Единый упорядоченный поток байт TCP означает, что один потерянный пакет зависает во всех логических сообщениях, разделяющих соединение — head-of-line blocking на уровне соединения. Мультиплексирование HTTP/2 не помогает, потому что TCP всё ещё снизу. QUIC даёт каждому логическому сообщению собственный поток с собственным окном управления потоком и независимой логикой повтора. Потерянный пакет вызывает переотправку только для своего потока; все остальные продолжают работу. При 0,5% потерях и RTT 100 ms улучшение составляет 575x: ~230 ms блокировки TCP против ~0,4 ms зависания потока QUIC. Stream ID кодируют инициатора и направленность в двух младших битах, так что клиент и сервер выделяют ID без координации. Теперь, когда вы увидите, что видеозвонок или мультиплексированный API не прерывается при нестабильном мобильном соединении, вы узнаете модель независимых потоков в действии: один потерянный пакет стоит одному потоку, а не всей сессии.
</Recap>
