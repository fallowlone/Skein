---
concepts:
- 0-rtt-resumption
- replay-attack
- early-data
- 425-too-early
- long-header
- short-header
- packet-encryption
deepensInto: []
estMin: 14
lang: ru
lessonType: topic
level: senior
order: 6
prereqs:
- 05-tls-handshake/03-forward-secrecy-and-certificates
- 10-quic-internals/03-handshake-and-1rtt
- 10-quic-internals/04-connection-migration
slug: 06-0rtt-and-encryption
sources:
- https://datatracker.ietf.org/doc/rfc9001/
- https://datatracker.ietf.org/doc/html/rfc9000
- https://datatracker.ietf.org/doc/html/rfc8470
spiral:
- latency
- statefulness
status: ready
summary: Возобновление 0-RTT QUIC позволяет клиентам прикрепить данные к первому пакету
  — но без защиты от реплея. Почти полное шифрование пакетов устраняет пассивный сниффинг
  и RST-инъекцию ценой операционной непрозрачности.
title: Возобновление 0-RTT и шифрование пакетов
track: networking
unit: 10-quic-internals
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import TraceScenario from "~/components/pedagogy/TraceScenario.astro";
import RFCQuiz from "~/components/pedagogy/RFCQuiz.astro";
import NumbersCard from "~/components/layout/NumbersCard.astro";

<Hook>
Пользователь повторно посещает сайт, на котором был вчера. В браузере есть сессионный тикет. QUIC отправляет HTTP-запрос в самом первом пакете — ноль RTT — до того, как сервер вообще ответит. Но если злоумышленник перехватит и воспроизведёт этот первый пакет, сервер выполнит запрос дважды.
</Hook>

<Crux>0-RTT отправляет данные до рукопожатия — нет защиты от реплея, значит серверы принимают в 0-RTT только идемпотентные запросы.</Crux>

<Explanation>

## Механика возобновления 0-RTT

При повторных подключениях к тому же серверу клиент может переиспользовать кешированный **сессионный тикет** — зашифрованный блоб TLS-состояния, выданный сервером в конце предыдущего соединения. Клиент включает этот тикет в ClientHello и прикрепляет данные приложения (0-RTT данные) в том же Initial-пакете.

Сервер получает Initial, расшифровывает сессионный тикет (содержащий ключи возобновления) и может немедленно обработать 0-RTT данные — без ожидания полного рукопожатия.

**Задержка:** 0 RTT до обработки сервером первого запроса. На линии 100 ms это экономит 100 ms при переподключении — поверх 100 ms, уже сэкономленных 1-RTT рукопожатием против TCP+TLS.

## Уязвимость реплея

0-RTT данные шифруются симметричным ключом сессионного тикета. **Злоумышленник на пути** захватив Initial-пакет, может позже воспроизвести весь пакет серверу. Поскольку у 0-RTT нет защиты от реплея на уровне TLS-записи, сервер получит и обработает воспроизведённый запрос.

**Конкретная атака:** Алиса отправляет `POST /api/transfer amount=5000 from=alice to=bob` в 0-RTT. Злоумышленник захватывает и воспроизводит пакет. Сервер выполняет перевод дважды: Алиса теряет 10 000 вместо 5 000.

**Защита:**

1. **Ограничить 0-RTT только идемпотентными запросами.** GET, HEAD и безопасные операции — безопасны; воспроизведение даёт тот же результат. POST, DELETE и изменяющие состояние операции должны ждать 1-RTT ключей.
2. **Ответ 425 Too Early.** Когда сервер перенаправляет 0-RTT на origin-backend, он включает заголовок `Early-Data: 1`. Origin может ответить `425 (Too Early)`, принуждая клиента повторить после полного рукопожатия (RFC 8470).
3. **Изоляция сессионных тикетов.** Серверы не должны делиться ключами сессионных тикетов между датацентрами — тикет от Server A должен быть действителен только на Server A. Короткое время жизни тикетов (1–24 часа) ограничивает окно реплея.

<TraceScenario
  id="06-sr-trace-1"
  lessonSlug="06-0rtt-and-encryption"
  lang="ru"
  scenario="Трассируйте атаку реплея 0-RTT QUIC и защиту."
  steps={[
    { prompt: "Клиент подключается к server.example.com и получает сессионный тикет. Позже открывает новое соединение с 0-RTT данными в ClientHello: POST /api/transfer amount=5000 from=alice to=bob.", reveal: "Клиент и сервер согласовывают соединение. Сервер отправляет Finished. 0-RTT POST на проводе, зашифрован, но без защиты от реплея в TLS-записи." },
    { prompt: "Злоумышленник на пути захватывает весь Initial-пакет, содержащий 0-RTT POST. Что он может сделать?", reveal: "Злоумышленник может воспроизвести весь пакет серверу. Поскольку у 0-RTT нет защиты от реплея на уровне TLS-записи, сервер получит и обработает POST-запрос повторно." },
    { prompt: "Сервер выполняет перевод дважды. Как сервер защищается?", reveal: "Полностью отвергая 0-RTT POST, ИЛИ принимая только идемпотентные запросы (GET, HEAD) в 0-RTT. Неидемпотентные запросы в 0-RTT должны ждать 1-RTT ключей и Finished, или получить ответ 425 (Too Early), принуждая к повтору." },
    { prompt: "Сервер перенаправляет 0-RTT на origin-backend. Какой заголовок он должен включить?", reveal: "Early-Data: 1, сигнализируя origin'у, что запрос пришёл из 0-RTT. Origin может ответить 425 (Too Early), принуждая клиента повторить после рукопожатия, или принять, если запрос идемпотентен." },
    { prompt: "Может ли злоумышленник воспроизвести тот же 0-RTT пакет на другом сервере (резервной реплике example.com)?", reveal: "Да, если оба сервера разделяют тот же ключ сессионного тикета. Поэтому серверы НЕ должны делиться ключами сессионных тикетов между датацентрами — ключи должны быть короткоживущими, для каждого сервера и часто ротируемыми." },
  ]}
/>

## Почти полное шифрование пакетов

TCP-заголовки открыты — исходный/целевой IP, порты, порядковые номера видны любому наблюдателю. QUIC шифрует почти всё:

**Длинные заголовки** (рукопожатие и Initial-пакеты):
- Незашифровано: внешние 4 байта (версия + длина Connection ID) и сам Connection ID.
- Зашифровано: номер пакета, полезная нагрузка — соответствующим ключом уровня.

**Короткие заголовки** (постхендшейковые пакеты):
- Несут только Connection ID и зашифрованный номер пакета + полезную нагрузку.
- Почти полностью непрозрачны для наблюдателей.

**Последствия для безопасности:**
- Злоумышленник на пути **не может внедрить RST-пакеты** — без 1-RTT ключа нельзя сконструировать валидный зашифрованный пакет. Уязвимость TCP к RST-инъекции устранена.
- **Пассивный сниффинг** содержимого потоков устранён — все данные потоков шифруются 1-RTT.
- **Анализ трафика** на основе порядковых номеров невозможен без расшифровки.

**Операционное последствие:** `tcpdump` QUIC-трафика показывает только непрозрачные блобы. Сетевые инженеры не могут подсчитать HTTP-запросы на endpoint, идентифицировать медленных клиентов или обнаружить некорректное поведение на уровне пакетов без инструментации на уровне приложения.

<div data-lesson-visual class="rounded-lg border border-slate-200 bg-slate-50 p-4 my-4 text-sm space-y-3">
  <div class="font-semibold text-slate-700 mb-1">QUIC vs TCP: что видит сетевой наблюдатель</div>
  <div class="grid grid-cols-2 gap-4 text-xs">
    <div class="space-y-1">
      <div class="font-semibold text-slate-500">TCP (открыто)</div>
      <div class="text-green-700">Исходный / целевой IP: видно</div>
      <div class="text-green-700">Порты: видно</div>
      <div class="text-green-700">Порядковые номера: видно</div>
      <div class="text-green-700">Флаги SYN/FIN/RST: видно</div>
      <div class="text-amber-600">TLS-полезная нагрузка: зашифровано (если TLS)</div>
    </div>
    <div class="space-y-1">
      <div class="font-semibold text-slate-500">QUIC (постхендшейк)</div>
      <div class="text-green-700">Исходный / целевой IP: видно (IP-заголовок)</div>
      <div class="text-green-700">UDP-порт: видно</div>
      <div class="text-green-700">Connection ID: видно</div>
      <div class="text-red-600">Номер пакета: зашифровано</div>
      <div class="text-red-600">Полезная нагрузка / данные потоков: зашифровано</div>
    </div>
  </div>
</div>

<Quiz
  id="06-sr-quiz-1"
  lessonSlug="06-0rtt-and-encryption"
  lang="ru"
  question="Почему 0-RTT QUIC уязвим к реплею, а TCP Fast Open (TFO) нет?"
  choices={[
    { label: "Потому что 0-RTT QUIC не шифруется, позволяя злоумышленникам читать и изменять его", misconception: "0-RTT зашифровано; уязвимость в том, что одна и та же шифртекст может быть отправлена серверу дважды." },
    { label: "TFO использует валидируемый сервером cookie с TTL; 0-RTT QUIC использует сессионный тикет без встроенной временной защиты от реплея", correct: true },
    { label: "Потому что UDP не имеет встроенной защиты от реплея в отличие от TCP", misconception: "Проблема защиты от реплея на уровне TLS, а не транспортного уровня UDP или TCP." },
    { label: "TFO всегда безопасен к реплею; 0-RTT QUIC всегда небезопасен и должен быть отключён", misconception: "0-RTT QUIC безопасен для идемпотентных запросов, если сервер это обеспечивает и изолирует ключи сессионных тикетов." },
  ]}
/>

<Quiz
  id="06-sr-quiz-2"
  lessonSlug="06-0rtt-and-encryption"
  lang="ru"
  question="Почему почти полное шифрование пакетов QUIC ломает традиционный мониторинг сети?"
  choices={[
    { label: "Потому что QUIC использует неизвестный порт, который инструменты мониторинга не могут обнаружить", misconception: "QUIC обычно использует порт 443 (как и HTTPS). Проблема в непрозрачности полезной нагрузки, не в неизвестных портах." },
    { label: "Зашифрованные номера пакетов и данные потоков делают невозможным подсчёт запросов, измерение RTT или обнаружение некорректного поведения на уровне пакетов без инструментации приложения", correct: true },
    { label: "Потому что QUIC использует формат заголовка, который tcpdump не поддерживает", misconception: "tcpdump может захватывать QUIC-пакеты; проблема в том, что их содержимое зашифровано и не может быть проанализировано без расшифровки." },
    { label: "QUIC-пакеты сжаты и не могут быть разобраны с линейной скоростью", misconception: "QUIC-пакеты по умолчанию не сжимаются. Непрозрачность происходит от шифрования, не от сжатия." },
  ]}
/>

<RFCQuiz
  id="06-rfc-1"
  lessonSlug="06-0rtt-and-encryption"
  lang="ru"
  question="Какой RFC стандартизирует HTTP-код ответа 425 (Too Early) и заголовок Early-Data для защиты от реплея 0-RTT?"
  choices={[
    { rfc: "RFC 8470", title: "Using Early Data in HTTP", correct: true },
    { rfc: "RFC 9001", title: "QUIC Cryptography" },
    { rfc: "RFC 9000", title: "QUIC: A UDP-Based Multiplexed and Secure Transport" },
    { rfc: "RFC 9114", title: "HTTP/3" },
  ]}
/>

<Inset kind="edgecase" lang="ru">
**Сравнение защиты от реплея 0-RTT с TCP Fast Open:** TCP Fast Open хранит cookie, выданный сервером, включающий серверный HMAC и TTL. Сервер проверяет cookie при запросе — если истёк, TFO отклоняется и принудительно выполняется стандартное рукопожатие. Эта встроенная временная проверка защищает TFO от реплея без участия приложения. У 0-RTT QUIC это отсутствует: действительность сессионного тикета контролируется его временем жизни, и в течение этого времени шифртекст может воспроизводиться. Именно поэтому RFC 9001 требует обеспечения идемпотентности на уровне приложения, а не полагается на TLS для защиты от реплея.
</Inset>

<NumbersCard
  id="06-0rtt-numbers"
  title="Компромиссы 0-RTT и шифрования"
  stats={[
    { label: "Экономия задержки 0-RTT при переподключении", value: "100 ms на линии 100 ms" },
    { label: "Риск реплея 0-RTT", value: "любой неидемпотентный запрос" },
    { label: "Типы пакетов с длинными заголовками", value: "Initial, 0-RTT, Handshake, Retry" },
    { label: "Типы пакетов с короткими заголовками", value: "1-RTT (постхендшейк)" },
    { label: "RST-инъекция TCP устранена", value: "да (нет возможности создать валидный RST)" },
  ]}
/>

</Explanation>

<KeyTakeaway>0-RTT отправляет данные приложения в ClientHello — ноль RTT — но злоумышленники могут воспроизвести шифртекст. Серверы должны ограничить 0-RTT идемпотентными запросами и использовать 425 Too Early для принудительных повторов. Короткие заголовки шифруют почти всё постхендшейк, устраняя RST-инъекцию и пассивный сниффинг ценой непрозрачности для сети.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="06-0rtt-retrieval"
  lang="ru"
  questions={[
    { q: "Объясните, почему 0-RTT QUIC уязвим к реплею, а TCP Fast Open нет.", a: "TCP Fast Open хранит cookie в SYN-пакете, вычисленный с серверным HMAC и TTL. Сервер проверяет cookie и отклоняет его, если истёк, обеспечивая встроенную временную защиту от реплея. 0-RTT QUIC зашифровано симметричным ключом сессионного тикета — злоумышленник, захвативший Initial-пакет, может воспроизвести тот же шифртекст позже, и сервер не может отличить реплей от легитимного переподключения на уровне TLS. Защита — на уровне приложения: ограничить 0-RTT идемпотентными запросами, использовать 425 Too Early для неидемпотентных, изолировать ключи сессионных тикетов." },
    { q: "Что такое код ответа 425 Too Early и когда origin-сервер должен его отправлять?", a: "RFC 8470 определяет 425 Too Early именно для этого случая: когда сервер получает HTTP-запрос из ранних 0-RTT данных (сигнализируется заголовком Early-Data: 1), он может ответить 425 для принуждения клиента повторить после полного 1-RTT рукопожатия. Origin должен отправлять 425 для любого неидемпотентного запроса (POST, DELETE, PATCH), пришедшего через 0-RTT. Безопасные/идемпотентные запросы (GET, HEAD) можно обработать немедленно." },
    { q: "Почему почти полное шифрование пакетов QUIC ломает традиционный мониторинг на уровне пакетов?", a: "Постхендшейковые QUIC-пакеты используют короткие заголовки, шифрующие номер пакета и всю полезную нагрузку. В отличие от TCP, где порядковые номера, направление потока и тайминг видны, QUIC раскрывает сетевому наблюдателю только Connection ID и UDP-порт. Инструменты, такие как tcpdump, системы анализа трафика и DPI-файерволы, видят непрозрачные блобы. Подсчёт HTTP-запросов, измерение RTT и обнаружение некорректного поведения требуют метрик на уровне приложения или инструментации с обходом ядра (eBPF-зонды на пользовательском сокете)." },
  ]}
/>

<Recap lang="ru">
Возобновление 0-RTT использует кешированный сессионный тикет для прикрепления данных приложения к ClientHello — ноль RTT для переподключающихся клиентов. Цена — уязвимость к реплею: злоумышленник на пути может захватить и воспроизвести 0-RTT шифртекст, дважды выполнив неидемпотентные операции. Защита трёхсоставная: ограничить 0-RTT безопасными/идемпотентными запросами, использовать механизм 425 Too Early для неидемпотентных случаев, передаваемых прокси, и изолировать ключи сессионных тикетов на сервер, чтобы предотвратить междатацентровый реплей. Почти полное шифрование пакетов QUIC выходит за рамки того, что обеспечивает только TLS: короткие заголовки шифруют номера пакетов и все данные потоков, устраняя RST-инъекцию и делая пассивный сниффинг невозможным. Компромисс — операционная непрозрачность: сетевые инженеры должны инструментировать на уровне приложения, а не на уровне пакетов.
</Recap>
