---
concepts:
- user-space-evolution
- stream-independence
- connection-migration
- 1-rtt-handshake
- 0-rtt-replay
- deployment-tradeoff
estMin: 13
lang: ru
level: senior
order: 8
prereqs: []
slug: quiz-short
sources:
- https://datatracker.ietf.org/doc/html/rfc9000
- https://datatracker.ietf.org/doc/html/rfc9001/
- https://blog.cloudflare.com/the-road-to-quic/
status: ready
summary: "Промпты на свободное припоминание по всему юниту QUIC. Сначала ответь своими словами, затем раскрой эталонный ответ и сравни."
title: 'QUIC внутри: тест на свободное припоминание'
track: networking
unit: 10-quic-internals
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";

<Hook>
Припоминание сильнее перечитывания. Для каждого промпта проговори или запиши полный ответ по памяти, прежде чем открыть эталон, — именно усилие припоминания закрепляет механизм.
</Hook>

<Goal lang="ru">
Восстанови спину юнита — зачем QUIC живёт в user space, как работают независимость stream'ов и Connection ID, что стоят и экономят рукопожатия 1-RTT и 0-RTT, и когда разворачивать QUIC, а когда TCP — не заглядывая в уроки.
</Goal>

<RetrievalDrawer
  client:visible
  id="10-quic-internals-short-retrieval"
  lang="ru"
  questions={[
    { q: "Почему QUIC выполняет транспортную логику в user space поверх UDP и что это даёт ценой чего?", a: "TCP живёт в ядре, поэтому новые опции выходят по циклу релизов ОС, а middlebox'ы (firewall'ы, NAT) молча срезают всё нераспознанное — ossification заморозила эволюцию TCP на десятилетия. QUIC выполняет транспорт в user space поверх UDP, который middlebox'ы считают непрозрачным, и шифрует почти весь пакет, так что срезать нечего. Выигрыш — эволюционируемость: congestion control подключаемый на соединение (NewReno, CUBIC, BBR), стек обновляется еженедельно вместе с приложением, а не с ОС. Цена — CPU: пер-пакетный AES-GCM, декодирование VarInt, сериализация фреймов и пер-датаграммные syscall'ы идут без NIC-offload, добавляя на 15–30% больше CPU на байт, чем ядерный TCP." },
    { q: "Объясни head-of-line-блокировку и как stream-модель QUIC её исправляет, с числами.", a: "TCP доставляет один упорядоченный поток байт, поэтому единственный потерянный пакет стопорит все байты после него — включая несвязанные stream'ы HTTP/2, мультиплексированные в том же соединении, ведь HTTP/2 мультиплексирует поверх одного TCP-stream'а. При 0.5% потерь и RTT 100 мс заблокированное сообщение ждёт ~230 мс. QUIC переносит мультиплексирование в транспорт: у каждого stream'а свой порядок, своё окно flow-control и своё loss detection, поэтому потерянный STREAM-фрейм стопорит только свой stream. При тех же условиях: ~0.4 мс пер-stream-задержки — улучшение в 575 раз. Поэтому HTTP/3 грузится на 30–55% быстрее HTTP/2 на потерянных мобильных путях." },
    { q: "Как соединение QUIC переживает хэндофф WiFi-на-cellular и что охраняет миграцию от злоупотребления?", a: "TCP идентифицирует соединение по 4-tuple (IP+порт клиента, IP+порт сервера), поэтому смена IP его ломает и вынуждает полный реконнект. QUIC идентифицирует соединение по непрозрачному Connection ID в заголовке пакета, отвязанному от IP. Когда IP клиента меняется, тот же Connection ID приходит с нового адреса; сервер обнаруживает новый путь и валидирует его, отправив токен PATH_CHALLENGE и дождавшись совпадающего PATH_RESPONSE. Всё состояние stream'ов, flow-control и congestion выживает. Во время валидации сервер может отправить не больше 3x полученных байт (anti-amplification), поэтому подделанный source-адрес не превратит сервер в reflection-усилитель. Ротация Connection ID (NEW_CONNECTION_ID) позволяет клиенту также менять ID, чтобы наблюдатель не связал две сетевые идентичности." },
    { q: "Почему рукопожатие QUIC занимает 1 RTT вместо 2 у TCP+TLS и как сюда вписываются четыре уровня шифрования и packet-number space'ы?", a: "TCP нужен 1 RTT на трёхстороннее рукопожатие, затем TLS 1.3 нужен ещё RTT сверху — 2 RTT до первого байта приложения. QUIC несёт рукопожатие TLS в CRYPTO-фреймах внутри QUIC-пакетов: клиент шлёт Initial с ClientHello, сервер отвечает ServerHello + Certificate + Finished одним burst'ом, и клиент может слать данные stream'а через один round-trip. Четыре уровня шифрования — Initial (детерминирован из Connection ID, ради целостности, не секретности), 0-RTT (из кэшированного session ticket), Handshake (из DH-обмена) и 1-RTT (финальные ключи) — маппятся на три packet-number space'а (Initial, Handshake, Application). Раздельные space'ы держат loss detection независимым: потерянный Initial запускает PTO только в своём space и не ужимает congestion-окно 1-RTT. На линке 100 мс это экономит 100 мс на новое соединение." },
    { q: "Что экономит 0-RTT-resumption, почему он уязвим к replay и как его защитить?", a: "0-RTT позволяет вернувшемуся клиенту прикрепить данные приложения к ClientHello через кэшированный session ticket, поэтому сервер обрабатывает первый запрос с нулём round-trip'ов — ещё 100 мс экономии на линке 100 мс поверх выигрыша 1-RTT. Данные зашифрованы, но без защиты от replay на уровне TLS-записи, поэтому on-path-атакующий (или переотправленный пакет) может переиграть ciphertext, и сервер выполнит его повторно — фатально для неидемпотентного запроса вроде перевода средств. Защиты: ограничить 0-RTT идемпотентными запросами (GET/HEAD); на неидемпотентные отвечать 425 Too Early (RFC 8470, сигнал через заголовок Early-Data), вынуждая повтор после рукопожатия 1-RTT; и изолировать ключи session ticket по серверам с коротким временем жизни, чтобы ticket нельзя было переиграть между дата-центрами. В отличие от TCP Fast Open, чья валидируемая сервером cookie несёт TTL, QUIC выносит защиту от replay на уровень приложения." },
    { q: "Когда разворачивать QUIC, а когда TCP, и какие операционные пробелы создаёт шифрование QUIC?", a: "Разворачивай QUIC там, где доминируют round-trip'ы и потери: межконтинентальные API (рукопожатие 1-RTT экономит 100+ мс) и мобильные пути (независимость stream'ов и миграция). Оставляй TCP там, где быстрый чистый линк делает CPU узким местом — например, статика на 1 Gbps в LAN, где пер-байтовая user-space-цена QUIC может срезать goodput ~45% без выигрыша от HoL, а kernel-offload TCP (kTLS, GSO, RSS) выигрывает. Митигации сужают разрыв: UDP GSO возвращает ~20% CPU, NIC-offload и core affinity толкают дальше. Учитывай ~3–5% сетей, блокирующих UDP — браузеры гоняют QUIC и TCP наперегонки, поэтому TCP выигрывает бесшовно, а RFC 9000 предписывает fallback на HTTP/2. Шифрование, защищающее QUIC, ослепляет пакетный мониторинг: tcpdump видит непрозрачные blob'ы, поэтому наблюдаемость переезжает на qlog (RFC 9312), browser timing API и eBPF-пробы на user-space-сокете." },
  ]}
/>

<Recap lang="ru">
Если ты восстановил каждый ответ по памяти, ты держишь спину юнита: размещение в user space даёт эволюционируемость ценой CPU; независимость stream'ов убивает head-of-line-блокировку; Connection ID переживают миграцию за валидацией пути и защитой 3x anti-amplification; объединённое рукопожатие 1-RTT экономит round-trip, а 0-RTT — ещё один ценой replay; и решение о развёртывании всегда CPU-против-задержки, а шифрование выталкивает наблюдаемость на уровень приложения.
</Recap>
