---
slug: 02-signals-and-wire-format
lang: ru
track: observability
unit: 03-otel
order: 2
title: "Сигналы OTel, Semantic Conventions и проводной формат OTLP"
summary: "Трассировки, метрики и логи имеют отдельные SDK-пайплайны и форматы OTLP-сообщений, но разделяют общие Resource и trace context — ключи объединения для кросс-сигнальной корреляции без ручного маппинга полей."
estMin: 12
status: ready
lessonType: topic
level: middle
prereqs: ["observability/03-otel/01-what-is-otel"]
deepensInto: []
spiral: ["encapsulation", "latency"]
concepts: ["otel-signals", "otlp-grpc", "otlp-http", "semantic-conventions", "otel-resource", "trace-context"]
sources:
  - https://opentelemetry.io/docs/specs/otlp/
  - https://opentelemetry.io/docs/concepts/semantic-conventions/
  - https://opentelemetry.io/docs/specs/otel/overview/
---

import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";

<Hook>
Ваши метрики идут в vendor-A, логи — в vendor-B, трассировки — в vendor-C. Запрос падает, но ни один бэкенд не согласен с другим по имени поля для сервиса. Кросс-сигнальная корреляция превращается в работу со спредшитом. Semantic Conventions OTel — это решение.
</Hook>

<Crux>Три сигнала OTel разделяют один Resource и один trace context — ключи объединения для кросс-сигнальной корреляции без ручного маппинга.</Crux>

<Explanation>

## Три сигнала, одна модель данных

OTel охватывает три типа сигналов в стабильной форме. Каждый имеет собственный API, SDK-пайплайн и форму OTLP-сообщения, но все три разделяют два концепта: **Resource** (атрибуты эмиттера — `service.name`, `host.name`, `cloud.region`, устанавливаются один раз при старте сервиса) и **Trace Context** (`trace_id`, `span_id`), позволяющий перейти от строки лога к соответствующей трассировке.

**Трассировки** — GA с 2021 года. Основной рабочий инструмент. Каждый бэкенд поддерживает их. Трассировка — это дерево спанов, каждый из которых имеет время начала, длительность, статус и атрибуты. Ключ объединения между сервисами — `trace_id`, передаваемый через заголовок W3C `traceparent`.

**Метрики** — GA с 2023 года. API поддерживает счётчики, gauge, гистограммы и экспоненциальные гистограммы (новый стандарт для высокоточной задержки, заменяющий фиксированные бакеты). Метрики агрегируются внутри SDK перед экспортом; проводной формат содержит resource-metrics батчи.

**Логи** — API стабилен с конца 2023, SDK стабилен с конца 2024. Библиотеки-мосты ("OTel log bridge" для slf4j, structlog, pino) позволяют существующему коду отправлять OTel-логи без переписывания. Проводной формат содержит resource-logs батчи с теми же полями `trace_id`/`span_id`, что и у трассировок.

**Профили** — экспериментальные в конце 2024/2025, GA ожидается в 2026 году как четвёртый сигнал.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Сигнал</th>
        <th class="border border-border px-3 py-2 text-left">GA</th>
        <th class="border border-border px-3 py-2 text-left">OTLP-сообщение</th>
        <th class="border border-border px-3 py-2 text-left">Ключ объединения</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">Трассировки</td>
        <td class="border border-border px-3 py-2">2021</td>
        <td class="border border-border px-3 py-2 font-mono text-xs">ExportTraceServiceRequest</td>
        <td class="border border-border px-3 py-2 font-mono text-xs">trace_id</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Метрики</td>
        <td class="border border-border px-3 py-2">2023</td>
        <td class="border border-border px-3 py-2 font-mono text-xs">ExportMetricsServiceRequest</td>
        <td class="border border-border px-3 py-2 font-mono text-xs">service.name через Resource</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Логи</td>
        <td class="border border-border px-3 py-2">API 2023 / SDK 2024</td>
        <td class="border border-border px-3 py-2 font-mono text-xs">ExportLogsServiceRequest</td>
        <td class="border border-border px-3 py-2 font-mono text-xs">trace_id, span_id</td>
      </tr>
    </tbody>
  </table>
</div>

## Semantic Conventions: общий словарь

Независимо от четырёхкомпонентной архитектуры, OTel публикует **Semantic Conventions** — нормативный набор имён атрибутов для типовых операций.

- **Resource**: `service.name`, `service.version`, `deployment.environment`, `host.name`, `cloud.region`
- **HTTP**: `http.request.method`, `http.route`, `http.response.status_code`, `url.full`
- **Базы данных**: `db.system`, `db.operation`, `db.namespace`
- **Очереди сообщений**: `messaging.system`, `messaging.destination.name`, `messaging.operation`

Это ключи объединения, благодаря которым метрики, логи и трассировки компонуются. Соблюдение Semantic Conventions — наиболее высокоуровневая практика в OTel: каждый авто-дашборд бэкенда и каждый кросс-командный запрос зависят от консистентного заполнения атрибутов. Зрелые платформенные команды создают языково-специфичную обёртку вокруг OTel SDK, которая предварительно конфигурирует извлечение атрибутов так, что о них нельзя забыть.

## OTLP: проводной формат, связывающий всё вместе

OTLP — это определённый через protobuf проводной протокол с двумя транспортами:

- **OTLP/gRPC** — бинарный protobuf поверх HTTP/2. Предпочтительный вариант. Примерно на 50-70% меньше JSON, кодируется в 2-5 раз быстрее. Поддерживает стриминг.
- **OTLP/HTTP** — protobuf или JSON поверх HTTP/1.1. Существует для сред, где gRPC заблокирован (некоторые файрволы), или для браузерной телеметрии.

Три OTLP-сообщения: `ExportTraceServiceRequest`, `ExportMetricsServiceRequest` и `ExportLogsServiceRequest`. Каждый OTel-совместимый бэкенд предоставляет OTLP-эндпоинт. Проводной формат стабилен уже несколько лет и развивается с обратной совместимостью.

**Контракт vendor-нейтральности**: если ваш edge (приложение или его sidecar) отправляет OTLP — вы vendor-нейтральны. Если edge отправляет проприетарный формат — нет, даже если шлюз позже конвертирует.

<Inset kind="why" lang="ru">
Зачем экспоненциальные гистограммы? Фиксированные бакеты гистограмм (0-10 мс, 10-50 мс, 50-100 мс и т.д.) требуют выбора границ в момент инструментирования. Если распределение задержки меняется — например, медленный путь внезапно занимает диапазон 1-5 с — нужен редеплой с новыми бакетами. Экспоненциальные гистограммы (в Prometheus они называются "native histograms") используют логарифмическую шкалу и динамически подстраивают разрешение. Они дают полное распределение задержки без угадывания границ заранее. SDK OTel по умолчанию использует экспоненциальные гистограммы для новых счётчиков.
</Inset>

<Quiz
  id="02-signals-quiz-1"
  lessonSlug="02-signals-and-wire-format"
  lang="ru"
  question="Какой транспорт OTLP предпочтителен для связи сервис-Collector и почему?"
  choices={[
    { label: "OTLP/HTTP — более широко поддерживается, работает везде", misconception: "OTLP/HTTP — резервный вариант для ограниченных сред, а не предпочтительный для связи сервис-Collector." },
    { label: "OTLP/gRPC — бинарный protobuf поверх HTTP/2 на 50-70% меньше JSON и в 2-5 раз быстрее кодируется, поддерживает стриминг", correct: true },
    { label: "OTLP/JSON — удобочитаем и проще в отладке", misconception: "OTLP/JSON поверх HTTP — самый медленный и объёмный вариант; он существует для отладки и браузеров, а не для производственной пропускной способности." },
    { label: "Проприетарный бинарный протокол вендора — минимальные накладные расходы", misconception: "Использование проприетарного протокола возвращает vendor lock-in; OTLP/gRPC имеет почти те же накладные расходы при полной переносимости." },
  ]}
/>

<Quiz
  id="02-signals-quiz-2"
  lessonSlug="02-signals-and-wire-format"
  lang="ru"
  question="Команда использует `http_route` (нижнее подчёркивание) вместо `http.route` (точка) для атрибута HTTP-маршрута. Что сломается?"
  choices={[
    { label: "Экспорт OTLP завершится ошибкой, так как имя поля невалидно", misconception: "OTLP принимает произвольные имена атрибутов; невалидные имена не блокируют экспорт." },
    { label: "Ничего не сломается сразу — но кросс-командные дашборды и авто-обнаружение HTTP-эндпоинтов бэкендом упадут, так как нестандартное имя не распознаётся", correct: true },
    { label: "OTel SDK выдаст ошибку валидации в момент инструментирования", misconception: "SDK не применяет Semantic Conventions при записи." },
    { label: "Только transform processor Collector отклонит нестандартное имя", misconception: "Transform processor Collector может переименовывать поля, но не отклоняет нестандартные имена по умолчанию." },
  ]}
/>

<DragOrder
  id="02-signals-drag-1"
  lessonSlug="02-signals-and-wire-format"
  lang="ru"
  prompt="Упорядочите сигналы OTel от наиболее зрелого к наименее зрелому (по дате GA):"
  items={[
    { id: "a", label: "Трассировки (GA 2021)" },
    { id: "b", label: "Метрики (GA 2023)" },
    { id: "c", label: "Логи API (GA конец 2023), SDK (GA конец 2024)" },
    { id: "d", label: "Профили (экспериментальные, GA ожидается 2026)" },
  ]}
  correctOrder={["a", "b", "c", "d"]}
/>

</Explanation>

<KeyTakeaway>Три сигнала OTel (трассировки GA 2021, метрики GA 2023, логи API/SDK GA 2023/2024) разделяют один Resource и один trace context — ключи объединения для кросс-сигнальной корреляции. Semantic Conventions дают каждому атрибуту нормативное имя, чтобы дашборды компоновались без ручного маппинга. OTLP/gRPC на 50-70% меньше JSON и является контрактом vendor-нейтральности: отправляйте его на edge, и смена бэкенда станет вопросом конфигурации Collector.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="02-signals-retrieval"
  lang="ru"
  questions={[
    { q: "Какие два общих концепта делают кросс-сигнальную корреляцию возможной в OTel?", a: "Resource (атрибуты эмиттера: service.name, host.name, cloud.region, устанавливаются один раз при старте) и Trace Context (trace_id, span_id), позволяющий перейти от строки лога или exemplar метрики к соответствующей трассировке. Без этих общих ключей объединения коррелировать алерт метрики с трассировкой можно лишь угадывая по временным меткам." },
    { q: "Почему Semantic Conventions — наиболее высокоуровневая практика в OTel?", a: "Semantic Conventions — нормативные имена атрибутов (http.route, db.system, service.name и др.), от которых зависит авто-обнаружение каждого бэкенда и каждый кросс-командный запрос. Сервис, использующий http_route вместо http.route, невидим для каждого дашборда и алерта, применяющего стандартное имя. SDK и Collector не применяют их принудительно, поэтому только управление — обычно языково-специфичная обёртка, предварительно конфигурирующая извлечение — гарантирует консистентность в масштабе." },
    { q: "Что такое контракт vendor-нейтральности и где именно он находится?", a: "Контракт: edge приложения отправляет OTLP. Пока SDK в приложении отправляет OTLP (по gRPC или HTTP) на следующий хоп (Collector или бэкенд), приложение переносимо — весь остальной стек можно менять без изменений кода приложения. Если приложение отправляет проприетарный протокол вендора, edge не является OTLP и переносимость теряется у источника, независимо от того, что происходит ниже по стеку." },
  ]}
/>

<Recap lang="ru">
OTel определяет три стабильных сигнала — трассировки (GA 2021), метрики (GA 2023), логи (API стабилен конец 2023, SDK конец 2024) — каждый со своим SDK-пайплайном и типом OTLP-сообщения, но все разделяют концепт Resource (атрибуты эмиттера: service.name) и Trace Context (trace_id, span_id) для кросс-сигнальной корреляции. Semantic Conventions дают каждому атрибуту нормативное имя, чтобы метрики, логи и трассировки компоновались без ручного маппинга полей. Проводной формат — OTLP: protobuf поверх gRPC (предпочтительно, на 50-70% меньше JSON, в 2-5 раз быстрее) или HTTP (для сред с ограничениями или браузеров). Отправляйте OTLP на edge приложения, и каждая смена бэкенда станет изменением конфигурации Collector.
</Recap>
