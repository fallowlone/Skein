---
concepts:
- otel-architecture
- semantic-conventions
- collector-pipeline
- sampling-strategy
- vendor-neutrality
- collector-operations
estMin: 13
lang: ru
level: senior
order: 7
prereqs: []
slug: quiz-choice
sources:
- https://opentelemetry.io/docs/concepts/components/
- https://opentelemetry.io/docs/collector/
- https://opentelemetry.io/docs/concepts/semantic-conventions/
status: ready
summary: "Синтез юнита OTel в формате множественного выбора — архитектура из четырёх частей, сигналы и OTLP, пайплайн Collector, sampling, vendor-нейтральность и эксплуатация."
title: 'OTel: тест с множественным выбором'
track: observability
unit: 03-otel
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Шесть вопросов, прошивающих весь юнит. Каждый отражает реальное архитектурное или инцидентное решение — не определение для заучивания, а место, где вносить изменение, и почему оно ложится именно туда.
</Hook>

<Goal lang="ru">
Убедись, что связываешь четыре части — API, SDK, Collector, OTLP — с решениями, которыми они управляют: где живёт портируемость, что даёт корреляцию сигналов, как sampling и пайплайн Collector складываются вместе и как всё это падает в проде.
</Goal>

<Quiz
  id="03-otel-choice-1"
  lessonSlug="03-otel"
  lang="ru"
  question="Платформенная команда заявляет полную vendor-нейтральность OTel. На ревью всплывают два факта: код приложения напрямую импортирует вендорский SDK dd-trace, а перед каждым сервисом стоит Collector, конвертирующий в OTLP. Нейтральны ли они и почему?"
  choices={[
    { label: "Нет. Нейтральность определяется на границе приложения: SDK в приложении должен эмитить OTLP. Здесь граница — вендорский формат, поэтому стоимость миграции лежит в коде каждого сервиса — конвертация в Collector ниже по потоку этого не отменяет.", correct: true },
    { label: "Да — Collector в пути нормализует всё в OTLP, поэтому backend заменяем.", misconception: "Collector может переформировать данные, но стоимость миграции уже понесена выше по потоку: каждый сервис импортирует вендорский SDK. Чтобы реально уйти, придётся переписать инструментирование во всех сервисах, так что нейтральность задаёт граница, а не Collector." },
    { label: "Да — само наличие любого Collector делает стек vendor-нейтральным по определению.", misconception: "Collector — необходимая инфраструктура, но недостаточная для нейтральности. Если граница приложения не OTLP, lock-in живёт в коде приложения независимо от того, что делает Collector." },
    { label: "Нет — использование вендорского дистрибутива Collector само по себе полный lock-in.", misconception: "Вендорские дистрибутивы, принимающие и эмитящие OTLP, допустимы; тихая эрозия — это вендорские SDK в коде приложения и проприетарные процессоры, существующие только в дистрибутиве, а не сам дистрибутив." },
  ]}
/>

<Quiz
  id="03-otel-choice-2"
  lessonSlug="03-otel"
  lang="ru"
  question="Команде нужен tail sampling, сохраняющий 100% трейсов с ошибками. Они гоняют один agent Collector как DaemonSet (по одному на ноду), экспортирующий прямо в backend. Почему это не работает и каков минимальный фикс?"
  choices={[
    { label: "Tail sampling нужен полный трейс, но каждый DaemonSet-агент видит только спаны со своей ноды — распределённый трейс размазан по нодам, и ни один агент не владеет полным трейсом. Фикс: добавить gateway-ярус и маршрутизировать по trace_id (loadbalancing exporter), чтобы все спаны одного трейса попадали на одну реплику.", correct: true },
    { label: "DaemonSet-агенты вообще не могут запускать процессор tail_sampling; он скомпилирован только в gateway.", misconception: "Бинарь процессора один и тот же; проблема в локальности данных. Агент получает только спаны своей ноды, поэтому у него никогда нет полного контекста трейса, которого требует tail sampling — это не отсутствующая фича." },
    { label: "Head sampling на SDK уже сохраняет ошибки, поэтому tail sampling избыточен.", misconception: "Head sampling решает на старте корневого спана, до того как ошибка произошла — он слеп к ошибкам. Только tail sampling, видя завершённый трейс, может сохранить 100% трейсов с ошибками." },
    { label: "Поднять буфер num_traces агента, чтобы он держал каждый трейс.", misconception: "Размер буфера ни при чём, когда агент вообще не получает спаны трейса с других нод. Проблема в том, что полный трейс не ко-локализован, а не в том, что буфер мал." },
  ]}
/>

<Quiz
  id="03-otel-choice-3"
  lessonSlug="03-otel"
  lang="ru"
  question="Два сервиса эмитят HTTP-телеметрию. Сервис A тегирует маршрут как http.route; сервис B — как http_route. OTLP-экспорт успешен у обоих, ошибки SDK нет. Что на самом деле ломается и где?"
  choices={[
    { label: "На экспорте не ломается ничего — OTLP несёт произвольные имена атрибутов. Но каждый кросс-командный дашборд, алерт и автообнаружение backend, завязанные на имя Semantic Convention http.route, молча пропускают сервис B. Поломка — на этапе запроса, на масштабе всего флота, и незаметна, пока кто-то не заметит, что B отсутствует.", correct: true },
    { label: "OTLP-экспортёр отклоняет http_route как недопустимое имя атрибута и роняет спаны сервиса B.", misconception: "OTLP не накладывает ограничений на имена атрибутов; оба экспортируются нормально. Semantic Conventions — это соглашение об именовании, обеспечиваемое управлением, а не wire-протоколом." },
    { label: "SDK выбрасывает ошибку валидации на этапе инструментирования из-за нестандартного имени.", misconception: "Ни SDK, ни Collector не обеспечивают Semantic Conventions на этапе записи. Именно поэтому дрейф тихий и почему platform-обёртка плюс CI-линт — единственный устойчивый фикс." },
    { label: "Оба сервиса в порядке, потому что transform-процессор Collector автоматически правит имена полей.", misconception: "Процессоры transform/attributes могут переименовывать поля, но только если ты напишешь правило — автоматической коррекции нет. По умолчанию нестандартное имя проходит насквозь." },
  ]}
/>

<Quiz
  id="03-otel-choice-4"
  lessonSlug="03-otel"
  lang="ru"
  question="В продовом trace-пайплайне у тебя memory_limiter, batch и tail_sampling. Почему memory_limiter должен быть первым и каково последствие постановки tail_sampling первым?"
  choices={[
    { label: "memory_limiter дёшево дропает записи под давлением RAM до того, как запустится дорогая обработка. Поставь его после tail_sampling — и Collector тратит CPU и RAM на буферизацию трейсов на окно решения, прежде чем limiter сможет сбросить нагрузку — сводя на нет его смысл и рискуя OOM, который теряет все буферизованные трейсы разом.", correct: true },
    { label: "Порядок не важен — Collector внутренне переупорядочивает процессоры ради эффективности.", misconception: "Collector выполняет процессоры ровно в том порядке, в котором ты их объявил. Порядок — решение о корректности и защите, а не подсказка." },
    { label: "tail_sampling первым — это правильно, потому что он сокращает объём до того, как limiter должен действовать.", misconception: "tail_sampling не сокращает память в полёте — он буферизует каждый спан на 30-60с до решения. Запуск его перед memory_limiter означает, что limiter не может защитить от той самой буферизации, что вызывает OOM." },
    { label: "batch должен быть первым; memory_limiter работает только после того, как спаны сбатчены.", misconception: "memory_limiter проверяет RAM процесса независимо от батчинга и должен идти первым, чтобы отказывать новым записям до того, как более дорогие процессоры потребят больше памяти." },
  ]}
/>

<Quiz
  id="03-otel-choice-5"
  lessonSlug="03-otel"
  lang="ru"
  question="У metrics-backend взрывается кардинальность после флотового раската OTel auto-instrumentation. Новое измерение, разгоняющее её, — атрибут url.full у HTTP-клиента. Какова корневая причина и правильный фикс?"
  choices={[
    { label: "url.full несёт сырой URL, включая ID в пути и query-строки, поэтому каждый отдельный запрос становится новым time series — неограниченная кардинальность. Фикс: использовать шаблонный http.route (например /users/{id}) вместо url.full или срезать query-строки через attributes-процессор на Collector.", correct: true },
    { label: "metrics-backend недоразмерен — добавь хранилище, и проблема решится.", misconception: "Масштабирование хранилища лечит симптом, а стоимость продолжает расти. Причина — атрибут неограниченной кардинальности; устойчивый фикс — шаблонизировать или убрать его, а не поглощать бесконечные series." },
    { label: "Tail sampling сохраняет слишком много трейсов и раздувает metric series.", misconception: "Tail sampling действует на трейсы, а не на кардинальность metric series. Взрывающееся измерение — это пер-запросный атрибут на метриках — url.full — независимо от sampling." },
    { label: "Переключиться с экспоненциальных на фиксированные гистограммы, чтобы ограничить кардинальность.", misconception: "Тип гистограммы управляет разрешением бакетов латентности, а не кардинальностью лейблов. Кардинальность — в измерении лейбла url.full, которое ни один тип гистограммы не меняет." },
  ]}
/>

<Quiz
  id="03-otel-choice-6"
  lessonSlug="03-otel"
  lang="ru"
  question="Твой единственный gateway OTel Collector падает во время инцидента. Дашборды on-call остаются зелёными и алерты не срабатывают, хотя пользовательские ошибки растут. Каков урок и структурный фикс?"
  choices={[
    { label: "Collector — это observability-инфраструктура на критическом пути: когда он падает, видимость падает — а дашборды, зависящие от него, врут умолчанием. Фикс: 3+ реплики gateway, персистентная дисковая очередь, health-пробы и мета-мониторинг (скрейп собственного /metrics Collector, алерты на dropped/refused/failed spans), чтобы отказ Collector сам был наблюдаем.", correct: true },
    { label: "Зелёные дашборды доказывают, что система здорова; пользовательские ошибки не связаны.", misconception: "Дашборды зелёные, потому что их источник данных — Collector — лежит, а не потому что система здорова. Отсутствие телеметрии трактуется как отсутствие проблем." },
    { label: "Добавить больше логирования на стороне приложения, чтобы ошибки всплывали без Collector.", misconception: "Больше логов всё равно течёт через тот же упавший Collector. Фикс — сделать сам Collector высокодоступным и самонаблюдаемым, а не лить больше объёма в сломанную трубу." },
    { label: "SDK должен экспортировать прямо в backend, минуя Collector полностью.", misconception: "Прямой экспорт теряет буферизацию, центральную политику и единообразную обработку и создаёт новую точку отказа на каждый сервис. Ответ — сделать ярус Collector HA и наблюдаемым, а не убирать его." },
  ]}
/>

<Recap lang="ru">
Сквозная линия: портируемость решается на границе приложения (эмить OTLP там или нигде), корреляция решается Semantic Conventions (согласованные имена или невидимые сервисы), а Collector — это место, где живёт политика: порядок процессоров его защищает, sampling складывает дешёвый head и курируемый tail, а gateway должен быть HA и самонаблюдаемым, потому что когда Collector падает, твоя видимость падает молча. Каждый продовый сбой этого юнита — размытая нейтральность, разрезанные трейсы, дрейф именования, OOM, утечки кардинальности, слепые дашборды — сводится к одной из этих четырёх частей.
</Recap>
