---
slug: 01-what-is-propagation
lang: ru
track: observability
unit: 06-trace-propagation
order: 1
title: "Что такое trace propagation и почему сломанная propagation хуже отсутствия трейсов"
summary: "Propagation несёт один идентификатор через каждый сервис. Пропусти один hop — трейс тихо расщепляется на orphan-фрагменты, которые выглядят нормально на дашборде, но скрывают настоящее узкое место."
estMin: 10
status: ready
lessonType: topic
level: junior
prereqs: ["observability/05-slo-budgets/01-sli-slo-budget-basics"]
deepensInto: []
spiral: ["encapsulation"]
concepts: ["trace-propagation", "traceparent", "trace-id", "span-id", "orphan-trace"]
sources:
  - https://www.w3.org/TR/trace-context/
  - https://opentelemetry.io/docs/concepts/context-propagation/
  - https://opentelemetry.io/docs/specs/otel/context/api-propagators/
---

import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import MetaphorComplete from "~/components/pedagogy/MetaphorComplete.astro";

<Hook>
Клиент открывает тикет поддержки: «checkout занял 30 секунд». Tracing-инструмент показывает трейсы для каждого сервиса — но каждый из них один span, не связан ни с чем. Все данные есть, ответов нет.
</Hook>

<Crux>Один заголовок traceparent (55 байт) — N сервисов в один трейс. Нет заголовка — N orphan-трейсов. Observability держится на этой строке.</Crux>

<Explanation>

## Что такое trace propagation

Trace propagation — практика передачи маленького идентификатора от одного сервиса к другому на каждом запросе, чтобы вся работа, сделанная через много сервисов для одного пользовательского действия, сшилась в одну картинку.

Без propagation медленный checkout выглядит как 50 разных историй. С ней — один трейс, проходимый сверху вниз за 30 секунд.

Идентификатор несётся в HTTP-заголовке `traceparent`, определённом спецификацией W3C Trace Context. Каждый сервис, получающий запрос, читает `traceparent`, использует его как parent для своей работы, генерирует новый span-id для себя и пишет новый `traceparent` перед любым исходящим вызовом. Trace-id остаётся постоянным на каждом hop; span-id'ы формируют parent-child-дерево.

## Метафора эстафеты

Представь доставку Amazon с одним tracking number. Посылка покидает склад, прыгает между сортировочными центрами, едет на трёх грузовиках и наконец приезжает к двери. Каждый hop сканирует тот же tracking number, записывая, где она была, когда и куда следующая.

Если какая-то одна остановка забывает просканировать — страница отслеживания замолкает и непонятно, где посылка, даже если она в итоге доедет.

Trace propagation — это сканирование. Каждый сервис обязан:
1. Прочитать входящий `traceparent` (извлечь trace-id и parent span-id).
2. Создать свой span (новый span-id, parent = входящий span-id).
3. Написать новый `traceparent` перед любым исходящим вызовом (тот же trace-id, свой span-id как новый parent-id).

Пропустишь любой из этих шагов — цепочка рвётся.

## Конкретный сценарий с Bea и Sven

On-call-инженер получает тикет поддержки клиента: «checkout занял 30 секунд». Открывает tracing-инструмент, вводит request-id из тикета, поднимает один трейс. Видит: 50 мс на API gateway, 80 мс в auth-сервисе, 28 секунд в inventory-сервисе на ожидании DB-запроса, 200 мс в payment, 100 мс назад пользователю. 28-секундное узкое место точно названо.

Без propagation пришлось бы вручную сопоставлять 50 log-записей через 7 сервисов и угадывать, какие от этого пользователя. С одним трейсом — знаешь за 30 секунд.

## Почему сломанная propagation хуже отсутствия трейсов

Без трейсинга вообще знаешь, что трейсов нет, и идёшь в логи. Со сломанной propagation каждый сервис эмитирует span'ы, но ни один не связан с другим — дашборд утверждает, что наблюдает систему, а каждый трейс покрывает только один сервис.

Думаешь, что отлаживаешь end-to-end, а реально отлаживаешь фрагментами. Пропавший трейс делает медленный сервис невидимым: запрос, быстрый в сервисе A и медленный в B, выглядит как быстрый трейс в A и отдельный медленный трейс в B без причинной связи. Операторы тратят часы, подозревая не тот сервис.

**Типичный сценарий сбоя:** команда добавляет tracing в один сервис, но забывает включить OTel HTTP-client auto-instrumentation. Каждый span стартует свежий трейс; дашборд показывает трейсы, но каждый — глубиной в один span. Клиенты жалуются на медленный journey, команда не может найти, где время — трейс тихо расщеплён на 50 кусков.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Состояние propagation</th>
        <th class="border border-border px-3 py-2 text-left">Что видишь в дашборде</th>
        <th class="border border-border px-3 py-2 text-left">Что можешь отладить</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">Нет трейсинга вообще</td>
        <td class="border border-border px-3 py-2">Ничего</td>
        <td class="border border-border px-3 py-2">Только логи — знаешь, что гадаешь</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Сломанная propagation</td>
        <td class="border border-border px-3 py-2">Трейсы везде, каждый глубиной 1 span</td>
        <td class="border border-border px-3 py-2">Ничего end-to-end — но дашборд утверждает, что можешь</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Правильная propagation</td>
        <td class="border border-border px-3 py-2">Полное дерево: API → auth → inventory → payment</td>
        <td class="border border-border px-3 py-2">Точное узкое место за 30 секунд</td>
      </tr>
    </tbody>
  </table>
</div>

<Quiz
  id="01-what-is-propagation-quiz-1"
  lessonSlug="01-what-is-propagation"
  lang="ru"
  question="Трейс пропагируется между сервисами каким HTTP-заголовком (по W3C-стандарту)?"
  choices={[
    { label: "traceparent (плюс опциональный tracestate для vendor-данных)", correct: true },
    { label: "x-request-id", misconception: "Многие системы ставят x-request-id, но это не W3C-стандарт для trace-context. Стандарт — traceparent." },
    { label: "x-trace-id", misconception: "Vendor-specific header старых систем; W3C-стандарт — traceparent." },
    { label: "Cookie", misconception: "Cookie — это session state, не trace-context." },
  ]}
/>

<Quiz
  id="01-what-is-propagation-quiz-2"
  lessonSlug="01-what-is-propagation"
  lang="ru"
  question="Какой самый частый production-сбой trace propagation?"
  choices={[
    { label: "Заголовок traceparent отсутствует или не читается на каком-то hop, и трейс расщепляется на разрозненные single-span деревья", correct: true },
    { label: "trace-id слишком длинный", misconception: "trace-id фиксированный — 32 hex; длина не runtime-проблема." },
    { label: "HTTPS-шифрование блокирует заголовок", misconception: "Заголовки едут внутри TLS-сессии — всё нормально." },
    { label: "Браузеры отказываются ставить traceparent", misconception: "Браузеры легко ставят произвольные fetch-заголовки; пропажа — обычно баг кодпути backend." },
  ]}
/>

<DragOrder
  id="01-what-is-propagation-drag-1"
  lessonSlug="01-what-is-propagation"
  lang="ru"
  prompt="Расставь, что происходит, когда запрос идёт через три сервиса с правильной propagation:"
  items={[
    { id: "a", label: "Клиент A генерирует новый trace-id и span-id, строит заголовок traceparent" },
    { id: "b", label: "Клиент A отправляет HTTP-запрос в сервис B с заголовком traceparent" },
    { id: "c", label: "Сервис B извлекает trace-id, создаёт свой span (новый span-id, parent = span-id клиента)" },
    { id: "d", label: "Сервис B зовёт сервис C: строит свежий traceparent с тем же trace-id, но со своим span-id как новый parent-id" },
    { id: "e", label: "Сервис C извлекает trace-id, создаёт свой span (parent = span-id B), делает работу" },
    { id: "f", label: "Каждый сервис эмитирует свой span в tracing-backend; backend сшивает по trace-id" },
    { id: "g", label: "Дашборд показывает полное дерево: A → B → C, span'ы делят один trace-id" },
  ]}
  correctOrder={["a", "b", "c", "d", "e", "f", "g"]}
/>

<MetaphorComplete
  id="01-what-is-propagation-metaphor-1"
  pieceSlug="01-what-is-propagation"
  lang="ru"
  setup="Заполни пропуск: стандартный HTTP-заголовок, несущий идентификатор трейса между сервисами, называется _______."
  accepted={["traceparent"]}
  canonical="traceparent (заголовок W3C Trace Context)"
  explanation="Спецификация W3C Trace Context называет заголовок traceparent. Он несёт version, trace-id, parent-id и trace-flags в фиксированном 55-байтном формате. Некоторые legacy-системы используют B3 single или multi headers, но traceparent — текущий стандарт."
/>

</Explanation>

<KeyTakeaway>Trace propagation передаёт один trace-id через каждый сервис-hop по W3C-заголовку traceparent. Пропустишь один hop — трейс расщепляется на orphan-фрагменты, выглядящие нормально на дашборде, но скрывающие настоящее узкое место — делая сломанную propagation хуже отсутствия трейсов.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="01-what-is-propagation-retrieval"
  lang="ru"
  questions={[
    { q: "В одном абзаце: почему сломанная trace propagation хуже отсутствия трейсов вообще?", a: "Без трейсинга знаешь, что трейсов нет, и идёшь в логи. Со сломанной propagation каждый сервис эмитирует span'ы, но ни один не связан с другим — дашборд утверждает, что наблюдает систему, а каждый трейс покрывает только один сервис. Думаешь, что отлаживаешь end-to-end, а реально — фрагментами. Пропавший трейс делает медленный сервис невидимым: запрос, быстрый в A и медленный в B, выглядит как быстрый трейс в A и отдельный медленный трейс в B без причинной связи. Операторы тратят часы, подозревая не тот сервис. Утверждение «у нас есть tracing» истинно, только если trace-id выживает на каждом hop; иначе — театр." },
    { q: "Что три вещи обязан сделать каждый сервис, получив запрос с заголовком traceparent?", a: "1. Извлечь trace-id и parent span-id из входящего traceparent. 2. Создать свой span, используя извлечённый trace-id (принадлежит тому же трейсу), с входящим span-id как своим parent. 3. Перед любым исходящим вызовом написать новый заголовок traceparent с тем же trace-id и своим span-id как новым parent-id, чтобы downstream-сервисы продолжили цепочку." },
    { q: "Назови три состояния трейсинга и что каждое значит для отлаживаемости.", a: "Нет трейсинга вообще: знаешь, что гадаешь, используешь логи. Сломанная propagation: каждый сервис эмитирует span'ы, но каждый глубиной в один, несвязанный — дашборд заявляет о видимости, но не даёт её для end-to-end проблем, и может ввести операторов в заблуждение, что они что-то наблюдают. Правильная propagation: полное parent-child-дерево от точки входа до каждого downstream, навигируемое за секунды до точного узкого места." },
  ]}
/>

<Recap lang="ru">
Trace propagation сшивает всю работу, сделанную для одного пользовательского запроса через каждый сервис, в один навигируемый трейс. W3C Trace Context делает это с 55-байтным HTTP-заголовком `traceparent`, несущим 128-битный trace-id, который остаётся постоянным на каждом hop. Каждый сервис читает входящий заголовок, создаёт дочерний span и пишет новый заголовок перед собственными исходящими вызовами. Пропустишь любой hop — трейс расщепляется на разрозненные single-span orphan'ы — состояние, которое активно хуже отсутствия трейсинга, потому что дашборды сообщают о нормальной видимости, скрывая настоящее узкое место от инженера, которому оно нужно больше всего.
</Recap>
