---
concepts:
- traceparent
- context-bind
- inject-extract
- tail-sampling-config
- orphan-span
estMin: 14
lang: ru
level: senior
order: 9
prereqs: []
slug: quiz-code
sources:
- https://www.w3.org/TR/trace-context/
- https://opentelemetry.io/docs/concepts/context-propagation/
- https://opentelemetry.io/blog/2022/tail-sampling/
status: ready
summary: "Прочитай traceparent-заголовок, сломанный сниппет async-propagation, пару producer/consumer с inject и tail-sampling-конфиг — и выбери поведение и фикс с наибольшим рычагом."
title: 'Trace propagation: чтение кода и заголовков'
track: observability
unit: 06-trace-propagation
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Баги propagation живут в заголовках, коде и конфигах — не в прозе. Читай каждый артефакт так, как читал бы в инциденте, и выбирай фикс, к которому senior-инженер потянется первым.
</Hook>

<Goal lang="ru">
Отработай диагностический цикл любого инцидента propagation: распарси заголовок на проводе, найди, где context тихо теряется, и прочитай конфиг коллектора, решающий, какие трейсы выживут.
</Goal>

## Сниппет 1 — заголовок на проводе

```
traceparent: 00-4bf92f3577b34da6a3ce929d0e0e4736-00f067aa0ba902b7-00
```

<Quiz
  id="06-trace-propagation-code-1"
  lessonSlug="06-trace-propagation"
  lang="ru"
  question="Заголовок корректен. Что говорит хвостовой 00 принимающему сервису делать по умолчанию?"
  choices={[
    { label: "Sampled-флаг равен 0, значит вышестоящий НЕ сэмплировал этот trace. Сервис всё равно создаёт span (тот же trace-id, parent = 00f067aa0ba902b7), но не экспортирует их — consistent sampling означает, что дропается весь trace, а не фрагмент.", correct: true },
    { label: "Версия 00, значит сервис должен трактовать заголовок как старейший формат и понизить парсер.", misconception: "Ведущий 00 — это версия; хвостовой 00 — это trace-flags. Версия 00 — просто текущий формат, никакого понижения. Вопрос про sampled-флаг." },
    { label: "00 означает parent-id из одних нулей, поэтому сервис обязан стартовать свежий trace.", misconception: "parent-id здесь 00f067aa0ba902b7, не ноль. Хвостовой 00 — это байт trace-flags: sampled-бит сброшен." },
    { label: "00 означает, что число span равно нулю, значит это корень; сэмпли на 100%.", misconception: "traceparent не кодирует число span, а сброшенный sampled-флаг означает, что вышестоящий уже решил дропнуть. Переопределение на 100% дало бы частичный trace." },
  ]}
/>

## Сниппет 2 — отложенная работа

```js
const { context, trace } = require('@opentelemetry/api');

app.post('/checkout', async (req, res) => {
  // span запроса активен здесь
  setTimeout(() => {
    writeAuditLog(req.body);   // эмитит span
  }, 200);
  res.status(202).end();
});
```

<Quiz
  id="06-trace-propagation-code-2"
  lessonSlug="06-trace-propagation"
  lang="ru"
  question="Span audit-log появляется как собственный корневой trace, оторванный от /checkout. В чём причина и минимальный фикс?"
  choices={[
    { label: "setTimeout планирует колбэк на свежем async-стеке, где AsyncLocalStorage-context запроса уже исчез, поэтому OTel стартует новый корневой span. Захвати ctx = context.active() и оберни колбэк: setTimeout(context.bind(ctx, () => writeAuditLog(req.body)), 200).", correct: true },
    { label: "writeAuditLog надо сделать синхронным, чтобы он бежал внутри стека хендлера.", misconception: "Граница — это setTimeout, а не sync/async-природа функции. Даже синхронный writeAuditLog внутри таймера бежит на новом стеке без context. Фикс — context.bind, а не убирание async." },
    { label: "Producer'у нужен propagator.inject() в заголовки audit-лога.", misconception: "inject/extract — для межпроцессных носителей. Работа audit бежит в том же процессе; потерянный context восстанавливается через context.bind, а не инъекцию в заголовки." },
    { label: "Перенеси res.status(202).end() до setTimeout, чтобы span не обрезался.", misconception: "Порядок ответа не влияет на context отложенного колбэка. Span осиротел, потому что колбэк таймера потерял активный context, независимо от момента отправки ответа." },
  ]}
/>

## Сниппет 3 — producer и consumer

```js
// producer
const headers = {};
propagator.inject(context.active(), headers);
await producer.send({ topic: 'orders', messages: [{ value: body, headers }] });

// consumer
await consumer.run({
  eachMessage: async ({ message }) => {
    // headers доступны как message.headers
    await handleOrder(message.value);   // эмитит span
  },
});
```

<Quiz
  id="06-trace-propagation-code-3"
  lessonSlug="06-trace-propagation"
  lang="ru"
  question="Producer инъектит корректно, но span консьюмера всё равно orphan'ы. Где баг?"
  choices={[
    { label: "Консьюмер никогда не вызывает propagator.extract(message.headers), чтобы перестроить context, и не запускает handleOrder внутри context.with(extractedCtx, ...). traceparent на проводе, но его не читают обратно, поэтому консьюмер стартует свежий trace.", correct: true },
    { label: "Producer не должен передавать headers в сообщении; Kafka их срезает.", misconception: "Kafka record headers (с 0.11) — стандартный носитель и не срезаются. Сторона producer корректна; консьюмер просто никогда не делает extract." },
    { label: "Консьюмеру нужен context.bind вокруг handleOrder.", misconception: "context.bind оживляет context внутри процесса, но процесс консьюмера стартовал без него — context живёт в заголовках сообщения. Его надо extract'ить, а не bind'ить." },
    { label: "inject и extract должны использовать разные propagator'ы для round-trip.", misconception: "Обе стороны используют один composite-propagator. Round-trip падает из-за отсутствия extract на консьюмере, а не из-за несовпадения propagator'ов." },
  ]}
/>

## Сниппет 4 — конфиг коллектора

```yaml
processors:
  tail_sampling:
    decision_wait: 30s
    policies:
      - name: errors
        type: status_code
        status_code:
          status_codes: [ERROR]
      - name: probabilistic
        type: probabilistic
        probabilistic:
          sampling_percentage: 1
```

<Quiz
  id="06-trace-propagation-code-4"
  lessonSlug="06-trace-propagation"
  lang="ru"
  question="Этот tail-sampling-конфиг OOM-нул коллектор при всплеске трафика. Какая одна самая важная отсутствующая строка?"
  choices={[
    { label: "num_traces — жёсткий потолок на число активных трейсов в полёте. Без него сэмплер буферизует каждый trace в RAM до закрытия decision_wait и растёт неограниченно под всплеском; с ним старейшие незавершённые трейсы вытесняются вместо OOM.", correct: true },
    { label: "Latency-политика для удержания медленных трейсов — это и перегрузило RAM.", misconception: "Latency-политика желательна, но она фильтрует в момент решения; она не ограничивает буферизуемую память. Отсутствующая защита от OOM — это потолок num_traces." },
    { label: "decision_wait слишком короткий на 30s и должен быть поднят до 5m.", misconception: "Поднятие decision_wait держит трейсы дольше и увеличивает RAM — делает OOM хуже. 30s — здравое окно; недостающая деталь — num_traces." },
    { label: "Load-balancing-экспортёр место в блоке processors.", misconception: "Load-balancing-экспортёр — это exporter на вышестоящем ярусе, а не processor здесь, и он решает маршрутизацию span между репликами — а не одноинстансный OOM, который ограничивает num_traces." },
  ]}
/>

<Recap lang="ru">
Любой инцидент propagation читается в артефактах: байт trace-flags говорит, был ли сэмплирован корректный заголовок (а сброшенный флаг означает дроп всего trace, а не экспорт фрагмента); необёрнутый setTimeout осиротит внутрипроцессную работу, пока ты не сделаешь context.bind; Kafka round-trip требует inject на producer и парный extract на consumer; а tail-sampling-конфиг без num_traces буферизует неограниченно до OOM. Распарси заголовок, найди потерянный context, прочитай конфиг — и чини на границе, а не на дашборде.
</Recap>
