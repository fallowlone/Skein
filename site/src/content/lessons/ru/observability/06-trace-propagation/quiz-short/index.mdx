---
concepts:
- traceparent
- consistent-sampling
- tail-sampling-ram
- async-context
- span-links
- propagation-observability
estMin: 14
lang: ru
level: senior
order: 8
prereqs: []
slug: quiz-short
sources:
- https://www.w3.org/TR/trace-context/
- https://opentelemetry.io/docs/concepts/context-propagation/
- https://opentelemetry.io/docs/concepts/sampling/
status: ready
summary: "Промпты на свободное воспроизведение по юниту trace propagation. Сначала ответь своими словами, затем открой модельный ответ и сравни."
title: 'Trace propagation: тест на свободное воспроизведение'
track: observability
unit: 06-trace-propagation
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";

<Hook>
Воспроизведение бьёт перечитывание. Для каждого промпта проговори или запиши полный ответ по памяти, прежде чем открыть модельный, — именно усилие припоминания закрепляет рассуждение о propagation.
</Hook>

<Goal lang="ru">
Восстанови хребет юнита по памяти — W3C-заголовок, fallback при отсутствии заголовка, consistent sampling, RAM-модель tail-сэмплера, фиксы async-границ и метрики здоровья propagation — не подглядывая в уроки.
</Goal>

<RetrievalDrawer
  client:load
  id="06-trace-propagation-short-retrieval"
  lang="ru"
  questions={[
    { q: "Почему сломанная trace propagation хуже, чем полное отсутствие трейсинга, и какая одна метрика её вскрывает?", a: "Без трейсинга ты знаешь, что слеп, и откатываешься на логи. При сломанной propagation каждый сервис всё ещё эмитит span, но они не связаны — каждый trace глубиной в один span. Дашборд рисует трейсы и заявляет end-to-end-видимость, поэтому операторы ему верят и часами гоняют не тот сервис. Медленный нижестоящий невидим, потому что его span сидит в отдельном orphan-трейсе без причинной связи со входной точкой. Сбой тихий: GitHub держал 50% orphan rate целый квартал, пока дашборды выглядели нормально. Вскрывает его только orphan-span rate по service.name — входные точки законно корнят трейсы, но устойчивый orphan rate выше ~5% на внутреннем сервисе — это регрессия propagation. Это должна быть метрика на дашборде и в идеале алерт, потому что OTel по умолчанию её никогда не показывает." },
    { q: "Пройди по четырём полям traceparent и что сервис обязан сделать с невалидным заголовком.", a: "traceparent — ровно 55 байт: version (2 hex, сейчас 00, ff запрещён), trace-id (32 hex / 128 бит, равномерно случайный, нули невалидны), parent-id (16 hex / 64 бита, span-id вызывающего, нули невалидны) и trace-flags (2 hex, определён только бит 0 — sampled-флаг). Фиксированная ширина позволяет sidecar'у распарсить его разбиением по дефисам без regex. Если заголовок невалиден — неизвестная версия, неверная длина поля, нулевой trace-id или parent-id — спека предписывает приёмнику отбросить его и стартовать совершенно новый trace, в идеале залогировав invalid_traceparent_received=true, чтобы операторы нашли сломанного вышестоящего вызывающего. Этот аккуратный fallback не даёт одному кривому вызывающему испортить trace-store бэкенда, который ключует все span по trace-id." },
    { q: "Как consistent sampling позволяет 12 сервисам договориться о keep/drop без координации и почему частичные трейсы недопустимы?", a: "Каждый сервис считает одну и ту же детерминированную функцию: hash(trace-id) mod 100 < sample_percentage. Поскольку trace-id одинаков на каждом hop (пронесён traceparent), а хеш детерминирован, все сервисы независимо приходят к одному булеву вердикту без общения; head-решение к тому же кешируется в sampled-флаге, чтобы нижестоящие могли срезать угол. W3C Trace Context Level 2 добавляет random-trace-id флаг, утверждающий, что id равномерно случаен, на что и опирается распределение хеша. Частичные трейсы недопустимы, потому что trace без span сервиса B выглядит так, будто A вызвал C напрямую — атрибуция задержки неверна, перцентильная математика смещена в сторону коротких span, а on-call-инженеры ставят уверенно неверные диагнозы. Согласованность означает: весь trace оставлен или ни одного span, никогда не фрагмент." },
    { q: "Дай RAM-модель tail-сэмплера и три независимых рычага OOM.", a: "Tail-sampling-коллектор держит все span каждого активного trace в RAM до закрытия окна решения. RAM = active_traces × avg_spans_per_trace × bytes_per_span, где active_traces ≈ in_flight_request_rate × decision_window_seconds. При 10k req/s, окне 30s, 10 span, 1 KB/span это примерно 3 GB на инстанс. Три независимых рычага: (1) active_traces — ограничивается жёстким потолком num_traces, вытесняющим старейший незавершённый trace, плюс масштабированием реплик за load-balancing-экспортёром; (2) spans_per_trace — ограничивается разбиением долгоживущих и batch-воркфлоу на span-linked под-трейсы, чтобы ни один trace не нёс тысячи span; (3) bytes_per_span — ограничивается лимитами размера значений атрибутов. Load-balancing-экспортёр обязан хешировать по trace-id, никогда не round-robin, иначе каждая реплика видит лишь фрагменты и решает неверно." },
    { q: "Назови async-границы, теряющие context, и верный фикс для каждой, включая различие внутрипроцессный vs межпроцессный.", a: "Внутрипроцессные границы теряют субстрат текущего context рантайма (Node AsyncLocalStorage, Python contextvars, Java ThreadLocal+Scope, Go явный ctx). setTimeout, setImmediate, queueMicrotask, вручную порождённые потоки и Loom virtual threads, созданные вне Scope, стартуют без context — фикс: захватить context.active() и обернуть колбэк в context.bind (или context.with), скопировать contextvars между Python-потоками, создавать virtual threads внутри Scope и протаскивать ctx везде в Go. Межпроцессные границы (Kafka, RabbitMQ, SQS) не имеют общей памяти, поэтому context.bind не помогает — producer обязан propagator.inject() traceparent в заголовки сообщения, а consumer — propagator.extract() его обратно. Service mesh прозрачно проносит заголовки только для HTTP и gRPC; он никогда не трогает wire-формат очередей, поэтому propagation очередей всегда уровня приложения." },
    { q: "Когда дерево parent-child ломается и как span-links и проблема 30 минут это решают?", a: "Parent-child предполагает линейную причинность (A вызывает B вызывает C) и заставляет каждый span иметь ровно одного родителя. Оно ломается в трёх случаях: batch-обработка (один консьюмер тянет 1000 сообщений — 1000 входящих контекстов, нет единого осмысленного родителя), fan-in (параллельные под-задачи сходятся в точке join со множеством вкладчиков) и async-продолжения (исходный запрос вернулся пользователю, затем порождает работу через часы под уже закрытым контекстом). span-links решают все три: span объявляет дополнительные SpanContext, с которыми причинно связан, но от которых не происходит, рисуемые пунктиром в бэкенде. Проблема 30 минут — смежная проблема коллектора: batch-задача длиннее окна решения 30s–5min эмитит поздние span, выглядящие как orphan'ы для tail-сэмплера. Senior-фикс — разбить любой trace длиннее ~30s или шире ~100 span на span-linked под-трейсы, каждый влезающий в окно решения; ретрофит — поддержка поздних span в бэкенде (Tempo, Honeycomb, Datadog принимают span до 24ч) с head sampling на 100% для batch." },
  ]}
/>

<Recap lang="ru">
Если ты смог восстановить каждый ответ по памяти, ты держишь хребет юнита: 55-байтный W3C traceparent сшивает сервисы, невалидный заголовок означает «старт заново», а равномерно случайный trace-id заставляет consistent sampling работать без координации. Tail-сэмплер меняет RAM на отбор с учётом исхода — ограничь его через num_traces, реплики и span-linked под-трейсы. Async-границы теряют context (context.bind внутри процесса, inject/extract между процессами), а поскольку propagation падает тихо, orphan-span rate — метрика, доказывающая, что дашборд говорит правду.
</Recap>
