---
concepts:
- traceparent
- tracestate
- trace-id
- span-id
- trace-flags
- propagator-interface
- otel-context
deepensInto: []
estMin: 13
lang: ru
lessonType: topic
level: middle
order: 2
prereqs:
- observability/06-trace-propagation/01-what-is-propagation
slug: 02-traceparent-and-tracestate
sources:
- https://www.w3.org/TR/trace-context/
- https://www.w3.org/TR/trace-context-2/
- https://opentelemetry.io/docs/specs/otel/context/api-propagators/
spiral:
- encapsulation
status: ready
summary: 'traceparent — ровно 55 байт: version, trace-id, parent-id, trace-flags.
  tracestate — escape hatch для vendor-данных. Знание формата на уровне байт позволяет
  валидировать, отлаживать и безопасно игнорировать невалидный ввод.'
title: 'traceparent и tracestate: полный формат W3C-заголовка'
track: observability
unit: 06-trace-propagation
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import NumbersCard from "~/components/layout/NumbersCard.astro";
import DiagramFrame from "~/components/diagram/DiagramFrame.astro";

<Hook>
On-call-инженер видит значение traceparent `99-aaaaaaaa-bbbbbbbb-01` в HTTP-заголовке. Валидно ли оно? Использовать его или сгенерировать свежий трейс? Ответ в спеке, и неправильный выбор тихо ломает trace store backend.
</Hook>

<Crux>traceparent: 4 поля, 55 байт, без regex. tracestate — vendor escape hatch. Один невалидный байт — начинай новый трейс.</Crux>

<Explanation>

## Заголовок traceparent по байтам

Спецификация W3C Trace Context (W3C Recommendation с 2020, Level 2 с 2024) определяет один HTTP-заголовок фиксированной ширины. Значение — ровно 55 байт:

```
00-4bf92f3577b34da6a3ce929d0e0e4736-00f067aa0ba902b7-01
```

Четыре поля через дефис:

<DiagramFrame
  label="55-байтная строка traceparent разбивается по дефисам на четыре поля фиксированной ширины по порядку: version (2 hex), trace-id (32 hex), parent-id (span-id 8 байт, 16 hex) и trace-flags (2 hex). Ниже tracestate несёт упорядоченный список vendor-членов key=value через запятую."
  hue="var(--d-systems)"
  caption="Фиксированная ширина позволяет router'у разбить по дефисам и проверить длины байт — без regex. trace-id — 128-битный (32 hex) ключ склейки; parent-id — 64-битный (16 hex) span-id вызывающего; бит 0 trace-flags = sampled. tracestate — vendor escape hatch."
>
  <svg viewBox="0 0 560 250" font-family="var(--font-body)">
    <text x="10" y="20" font-size="12.5" fill="var(--ink)">traceparent</text>
    <text x="120" y="20" font-size="10.5" fill="var(--muted)" font-family="var(--font-mono)">— 55 байт, фикс. ширина</text>

    <rect x="10" y="34" width="40" height="42" rx="5" fill="var(--card)" stroke="var(--hairline-strong)" stroke-width="0.8" />
    <text x="30" y="52" font-size="9.5" text-anchor="middle" fill="var(--ink)">version</text>
    <text x="30" y="65" font-size="9" text-anchor="middle" fill="var(--muted)" font-family="var(--font-mono)">2 hex</text>
    <text x="55" y="60" font-size="12" text-anchor="middle" fill="var(--muted)" font-family="var(--font-mono)">-</text>

    <rect x="62" y="34" width="232" height="42" rx="5" fill="var(--card)" stroke="var(--accent)" stroke-width="1.2" />
    <text x="178" y="52" font-size="9.5" text-anchor="middle" fill="var(--ink)">trace-id</text>
    <text x="178" y="65" font-size="9" text-anchor="middle" fill="var(--muted)" font-family="var(--font-mono)">32 hex · 128-бит</text>
    <text x="299" y="60" font-size="12" text-anchor="middle" fill="var(--muted)" font-family="var(--font-mono)">-</text>

    <rect x="306" y="34" width="148" height="42" rx="5" fill="var(--card)" stroke="var(--hairline-strong)" stroke-width="0.8" />
    <text x="380" y="52" font-size="9.5" text-anchor="middle" fill="var(--ink)">parent-id</text>
    <text x="380" y="65" font-size="9" text-anchor="middle" fill="var(--muted)" font-family="var(--font-mono)">16 hex · 64-бит</text>
    <text x="459" y="60" font-size="12" text-anchor="middle" fill="var(--muted)" font-family="var(--font-mono)">-</text>

    <rect x="466" y="34" width="40" height="42" rx="5" fill="var(--card)" stroke="var(--hairline-strong)" stroke-width="0.8" />
    <text x="486" y="52" font-size="9.5" text-anchor="middle" fill="var(--ink)">flags</text>
    <text x="486" y="65" font-size="9" text-anchor="middle" fill="var(--muted)" font-family="var(--font-mono)">2 hex</text>

    <text x="30" y="96" font-size="9.5" text-anchor="middle" fill="var(--ink-2)" font-family="var(--font-mono)">00</text>
    <text x="178" y="96" font-size="9.5" text-anchor="middle" fill="var(--ink-2)" font-family="var(--font-mono)">4bf92f3577b34da6a3ce929d0e0e4736</text>
    <text x="380" y="96" font-size="9.5" text-anchor="middle" fill="var(--ink-2)" font-family="var(--font-mono)">00f067aa0ba902b7</text>
    <text x="486" y="96" font-size="9.5" text-anchor="middle" fill="var(--ink-2)" font-family="var(--font-mono)">01</text>

    <text x="30" y="112" font-size="8.5" text-anchor="middle" fill="var(--muted)">ff запрещён</text>
    <text x="178" y="112" font-size="8.5" text-anchor="middle" fill="var(--muted)">random · ключ склейки · не all-zeros</text>
    <text x="380" y="112" font-size="8.5" text-anchor="middle" fill="var(--muted)">span-id вызывающего</text>
    <text x="486" y="112" font-size="8.5" text-anchor="middle" fill="var(--muted)">бит 0 sampled</text>

    <line x1="10" y1="138" x2="506" y2="138" stroke="var(--hairline-2)" stroke-width="0.8" />

    <text x="10" y="162" font-size="12.5" fill="var(--ink)">tracestate</text>
    <text x="120" y="162" font-size="10.5" fill="var(--muted)" font-family="var(--font-mono)">— упорядоченный vendor-список key=value через запятую</text>

    <rect x="10" y="174" width="150" height="30" rx="5" fill="var(--card-2)" stroke="var(--hairline-strong)" stroke-width="0.8" />
    <text x="85" y="193" font-size="9.5" text-anchor="middle" fill="var(--ink)" font-family="var(--font-mono)">congo=t61rcWkgMzE</text>
    <text x="166" y="193" font-size="12" text-anchor="middle" fill="var(--muted)" font-family="var(--font-mono)">,</text>

    <rect x="174" y="174" width="120" height="30" rx="5" fill="var(--card-2)" stroke="var(--hairline-strong)" stroke-width="0.8" />
    <text x="234" y="193" font-size="9.5" text-anchor="middle" fill="var(--ink)" font-family="var(--font-mono)">rojo=00f067aa0b</text>
    <text x="300" y="193" font-size="12" text-anchor="middle" fill="var(--muted)" font-family="var(--font-mono)">,</text>

    <text x="312" y="193" font-size="9.5" fill="var(--muted)">… до 32 членов</text>

    <text x="10" y="226" font-size="9" fill="var(--muted)">add: prepend свой ключ · update: свой ключ в начало · чужие ключи не удалять</text>
  </svg>
</DiagramFrame>

| Поле | Ширина | Текущее значение | Правила |
|---|---|---|---|
| Version | 2 hex (1 байт) | `00` | `ff` зарезервирован и запрещён |
| Trace-id | 32 строчных hex (16 байт) | 128-битный random | All-zeros недопустим |
| Parent-id | 16 строчных hex (8 байт) | 64-битный span-id вызывающего | All-zeros недопустим |
| Trace-flags | 2 hex (1 байт) | `01` = sampled | Определён только бит 0 (sampled) |

Ширина намеренно фиксирована, чтобы router или sidecar мог парсить заголовок без regex-движка — просто разбей по дефисам и проверь длины байт. Компактная форма означает ~0.04 KB на HTTP-запрос — измеримо, но ничтожно относительно типичных payload'ов.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse font-mono text-xs">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Позиция в строке</th>
        <th class="border border-border px-3 py-2 text-left">Поле</th>
        <th class="border border-border px-3 py-2 text-left">Пример</th>
        <th class="border border-border px-3 py-2 text-left font-sans">Смысл</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">0–1</td>
        <td class="border border-border px-3 py-2">version</td>
        <td class="border border-border px-3 py-2">00</td>
        <td class="border border-border px-3 py-2 font-sans">Версия спеки; ff запрещён</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">3–34</td>
        <td class="border border-border px-3 py-2">trace-id</td>
        <td class="border border-border px-3 py-2">4bf92f3577b34da6a3ce929d0e0e4736</td>
        <td class="border border-border px-3 py-2 font-sans">128-битный random; all-zeros недопустим</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">36–51</td>
        <td class="border border-border px-3 py-2">parent-id</td>
        <td class="border border-border px-3 py-2">00f067aa0ba902b7</td>
        <td class="border border-border px-3 py-2 font-sans">span-id вызывающего; all-zeros недопустим</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">53–54</td>
        <td class="border border-border px-3 py-2">trace-flags</td>
        <td class="border border-border px-3 py-2">01</td>
        <td class="border border-border px-3 py-2 font-sans">Бит 0 = sampled; остальные зарезервированы</td>
      </tr>
    </tbody>
  </table>
</div>

## Trace-id обязан быть равномерно случайным

Спека категорична: trace-id должен генерироваться криптостойким случайным 128-битным значением. Он не должен кодировать timestamp, IP или user-id потому что:
- Downstream-инструменты полагаются на равномерное распределение для решений по sampling (consistent hashing опирается на это).
- Встроенная информация утекает операционные данные.

Оба ограничения работают одновременно: trace-id с закодированным timestamp ломает consistent sampling *и* утекает тайминг запросов любому, кто может читать заголовки — два независимых повода для запрета в спеке.

OTel SDK используют secure random платформы: `crypto.getRandomValues` в браузерах, `/dev/urandom` на Linux, `java.security.SecureRandom` на JVM. Генерация одного 128-битного id — sub-microsecond, никогда не измеримая стоимость. При 128-битной случайности вероятность коллизии среди триллиона трейсов порядка 10⁻¹⁵ — практически ноль.

Уникальность trace-id — фундамент: tracing-backend ключит все span'ы по trace-id, склеивает при запросе, и если два независимых запроса делят id — backend тихо смешает их в один бессмысленный трейс.

## Валидация и fallback «нет заголовка»

Когда сервис получает запрос без `traceparent` или с невалидным (неверная version, all-zero trace-id, malformed hex), receiver **обязан сгенерировать совершенно новый trace-id и стартовать свежий трейс**. Спека явно определяет этот fallback, чтобы один сбойный caller не каскадно ломал трейсы.

Сбои валидации должны логироваться как атрибут span'а (`invalid_traceparent_received=true`), чтобы трейс появлялся в backend с маркером, позволяя операторам найти плохого upstream-caller. Production-команды трактуют ненулевую частоту `invalid_traceparent` span'ов как propagation-регресс.

**Пример `99-aaaaaaaa-bbbbbbbb-01`:** версия `99` неизвестна (текущий max — `00`); trace-id и span-id неправильной длины (8 и 8 hex-символов, не 32 и 16). Заголовок невалиден — принимающий сервис стартует свежий трейс.

## Компаньон tracestate

Где `traceparent` — универсальный идентификатор, `tracestate` несёт vendor-специфичный контекст: какой sampler принял решение, какая canary-версия эмитнула, какие дополнительные id backend хочет.

Формат: comma-separated key=value, до 32 членов, ключ и значение до 256 символов каждый. Спека обязывает vendor'ов пропагировать не менее 512 символов кумулятивного `tracestate`, даже если они не понимают ключи — это сохраняет end-to-end tracing через разнородные vendor-инструменты.

Правила мутации:
- Vendor может **добавить** новый ключ (prepend в начало списка).
- Vendor может **обновить** свой ключ (передвинуть в начало).
- Vendor **не должен удалять** ключи других vendor'ов.

Заголовок — escape hatch для того, что не вмещается в жёсткий `traceparent`.

## Интерфейс propagator

OpenTelemetry обобщает логику заголовков двумя операциями над объектом Context:

- `inject(context, carrier)` — пишет нужные заголовки в исходящий запрос.
- `extract(carrier) -> context` — читает их из входящего запроса.

Carrier'ы pluggable (HTTP-заголовки, gRPC metadata, AMQP properties, обычный dict), propagator'ы pluggable (W3C TraceContext, W3C Baggage, B3 single, B3 multi, Jaeger). Дефолтный OTel composite — TraceContext + Baggage. B3 поддерживается для interop с Zipkin-эрой.

Библиотека делает bit-кодирование, валидацию и логику «если входящего нет — сгенерируй», поэтому прикладной код никогда не трогает сырой hex.

<NumbersCard
  id="02-traceparent-numbers"
  title="Ключевые числа traceparent"
  stats={[
    { label: "Размер заголовка traceparent", value: "55 байт (фиксировано)" },
    { label: "Ширина trace-id", value: "128 бит / 32 hex-символа" },
    { label: "Ширина span-id (parent-id)", value: "64 бита / 16 hex-символов" },
    { label: "Определённых бит trace-flags", value: "1 (sampled-флаг, бит 0)" },
    { label: "Макс. членов tracestate", value: "32" },
    { label: "Мин. пропагируемых символов tracestate на vendor", value: "512" },
    { label: "Вероятность коллизии trace-id при триллионе трейсов", value: "~10⁻¹⁵" },
    { label: "Стоимость на запрос", value: "~0.04 KB overhead заголовка" },
  ]}
/>

<Quiz
  id="02-traceparent-quiz-1"
  lessonSlug="02-traceparent-and-tracestate"
  lang="ru"
  question="Значение traceparent `00-4bf92f3577b34da6a3ce929d0e0e4736-00f067aa0ba902b7-01`. Что значит финальный `01`?"
  choices={[
    { label: "Sampled-флаг включён — caller сообщает, что трейс записывается; downstream-сервисы тоже должны писать span'ы", correct: true },
    { label: "Версия трейса 01", misconception: "Версия — первое поле (здесь 00)." },
    { label: "Индекс span'а", misconception: "Span'ы идентифицируются по parent-id (8-байтный hex), не по последовательному индексу." },
    { label: "Число hop'ов", misconception: "Число hop'ов не кодируется в traceparent." },
  ]}
/>

<Quiz
  id="02-traceparent-quiz-2"
  lessonSlug="02-traceparent-and-tracestate"
  lang="ru"
  question="HTTP-запрос пришёл в твой сервис с `traceparent: 99-aaaaaaaa-bbbbbbbb-01`. Что сервис делает по W3C-спеке?"
  choices={[
    { label: "Трактует заголовок как невалидный (версия 99 неизвестна, trace-id и span-id неправильной длины) и стартует свежий трейс", correct: true },
    { label: "Падает с ошибкой", misconception: "Серверы обязаны мягко игнорировать неизвестный traceparent и продолжать." },
    { label: "Отвергает запрос", misconception: "Спека обязывает игнорировать невалидный traceparent, а не отвергать запрос." },
    { label: "Использует заголовок как есть", misconception: "Использование невалидных заголовков ломает backend trace store." },
  ]}
/>

<Quiz
  id="02-traceparent-quiz-3"
  lessonSlug="02-traceparent-and-tracestate"
  lang="ru"
  question="Vendor читает заголовок tracestate с ключами двух других vendor'ов. Какая мутация запрещена спекой?"
  choices={[
    { label: "Удаление ключей других vendor'ов", correct: true },
    { label: "Prepend своего нового ключа", misconception: "Prepend — правильная операция добавления — разрешена." },
    { label: "Перемещение своего существующего ключа в начало (обновление)", misconception: "Перемещение своего ключа в начало — правильная операция обновления — разрешена." },
    { label: "Пропагирование более 512 символов чужих ключей", misconception: "512 символов — минимум для пропагирования, не максимум. Больше — нормально." },
  ]}
/>

</Explanation>

<KeyTakeaway>traceparent упаковывает version + 128-битный trace-id + 64-битный parent-id + 1-битный sampled-флаг в ровно 55 байт. Невалидные заголовки (неверная version, неверная длина, all-zeros id) должны быть отброшены и стартован свежий трейс. tracestate несёт vendor-экстры; vendor'ы обязаны пропагировать чужие ключи, даже не понимая их.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="02-traceparent-retrieval"
  lang="ru"
  questions={[
    { q: "Перечисли четыре поля traceparent по порядку и их назначение.", a: "1. version (2 hex-символа): версия формата; сейчас всегда '00'; receivers должны стартовать свежий трейс при неизвестной версии. 2. trace-id (32 строчных hex-символа = 128 бит): глобально уникальный идентификатор всего путешествия запроса; должен быть равномерно случайным; all-zeros недопустим. 3. parent-id (16 строчных hex-символов = 64 бита): span-id вызывающего — так принимающий сервис знает, чьим дочерним span'ом является. 4. trace-flags (2 hex-символа = 8 бит): битовое поле; определён только бит 0 (значение 01) — sampled-флаг, означающий, что трейс записывается." },
    { q: "Почему trace-id обязан быть равномерно случайным и почему он никогда не должен кодировать timestamp или IP?", a: "Равномерная случайность нужна по двум причинам: (1) алгоритмы consistent head-sampling хешируют trace-id по модулю частоты sampling для решения, какие трейсы хранить — если trace-id кодирует timestamp'ы, свежий трафик смещается к одному хеш-бакету и sampling становится неравномерным; (2) trace-id виден в HTTP-заголовках всем, кто может инспектировать трафик между edge и origin, поэтому кодирование IP или timestamp'ов утекает операционную информацию. Равномерный случайный 128-битный id от secure PRNG не несёт встроенной информации и даёт равномерное хеш-распределение." },
    { q: "Что такое интерфейс propagator inject/extract и какую проблему он решает?", a: "OTel propagator interface предоставляет два метода: inject(context, carrier) пишет trace-context в carrier исходящего запроса (HTTP-заголовки, gRPC metadata, свойства сообщений), и extract(carrier) -> context читает trace-context из carrier входящего запроса. Интерфейс делает carrier pluggable (HTTP, gRPC, Kafka и т.д.) и формат propagator pluggable (W3C, B3, Jaeger). Прикладной код никогда не манипулирует сырым hex — SDK делает кодирование, валидацию и fallback 'нет входящего заголовка → генерируй свежий трейс'. Это значит, что инструментация, написанная однажды, работает через все транспортные протоколы и все vendor-backend'ы." },
  ]}
/>

<Recap lang="ru">
W3C-заголовок `traceparent` несёт четыре поля фиксированной ширины — version, 128-битный trace-id, 64-битный parent-id и trace-flags — в ровно 55 байтах. Фиксированная ширина — намеренный выбор дизайна: любой router или sidecar парсит без regex-движка. Trace-id обязан быть равномерно случайным, никогда не кодировать timestamp'ы или IP, потому что consistent sampler'ы и приватность оба зависят от этого. Невалидный заголовок (неизвестная version, неверная длина поля, all-zero trace-id или parent-id) должен быть отброшен и стартован свежий трейс, залогированный как `invalid_traceparent_received` для отслеживания регрессий. Компаньон `tracestate` несёт vendor-специфику — до 32 пар key=value; vendor'ы обязаны пропагировать чужие ключи, не понимая их. OTel propagator interface (inject/extract) оборачивает всё это, чтобы прикладной код никогда не трогал сырой hex. Теперь, встретив подозрительный traceparent в логе запроса, ты читаешь четыре поля с первого взгляда, сразу понимаешь, валиден ли заголовок, и знаешь, что обязан сделать принимающий сервис — без догадок.
</Recap>
