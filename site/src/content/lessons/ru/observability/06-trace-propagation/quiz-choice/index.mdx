---
concepts:
- traceparent
- consistent-sampling
- tail-sampling
- async-context
- orphan-span-rate
- span-links
estMin: 13
lang: ru
level: senior
order: 7
prereqs: []
slug: quiz-choice
sources:
- https://www.w3.org/TR/trace-context/
- https://opentelemetry.io/docs/concepts/context-propagation/
- https://opentelemetry.io/docs/concepts/sampling/
status: ready
summary: "Синтез юнита trace propagation в формате множественного выбора — W3C-заголовок, consistent sampling, лимиты tail-sampling-коллектора, async-границы и тихие production-сбои."
title: 'Trace propagation: тест с множественным выбором'
track: observability
unit: 06-trace-propagation
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Шесть вопросов, прошивающих весь юнит. Каждый отражает решение, которое ты принимаешь в реальном инциденте, — не определение для пересказа, а выбор по propagation, который надо взвесить, пока дашборд врёт тебе о том, сколько ты на самом деле видишь.
</Hook>

<Goal lang="ru">
Убедись, что можешь связать формат W3C-заголовка, consistent sampling, экономику tail-sampling-коллектора, сбои на async-границах и наблюдаемость самой propagation — синтез, к которому вели отдельные уроки.
</Goal>

<Quiz
  id="06-trace-propagation-choice-1"
  lessonSlug="06-trace-propagation"
  lang="ru"
  question="Сервис получает 'traceparent: 99-aaaaaaaa-bbbbbbbb-01'. Что он обязан сделать по W3C-спеке и почему это правило важно для всего флота?"
  choices={[
    { label: "Отбросить как невалидный (неизвестная версия 99, неверная длина trace-id и parent-id) и начать свежий trace. Аккуратный fallback не даёт одному кривому вызывающему каскадно испортить trace-store у всех нижестоящих.", correct: true },
    { label: "Отклонить запрос с 400, чтобы вызывающий починил заголовок.", misconception: "traceparent — это подсказка, а не контроль доступа. Спека предписывает игнорировать невалидный заголовок и стартовать свежий trace; отклонение запроса сломало бы пользовательский поток из-за телеметрической детали." },
    { label: "Использовать заголовок как есть — любое значение лучше, чем ничего, для сшивки.", misconception: "Использование кривого trace-id молча сливает или портит трейсы в бэкенде, который ключует все span по trace-id. Неверный id хуже свежего валидного." },
    { label: "Дополнить короткие поля нулями до нужной ширины.", misconception: "Спека не определяет поведение «починки»; trace-id или parent-id из одних нулей сам по себе невалиден. Единственно верное действие — отбросить и сгенерировать заново." },
  ]}
/>

<Quiz
  id="06-trace-propagation-choice-2"
  lessonSlug="06-trace-propagation"
  lang="ru"
  question="Команда гоняет вероятностный head sampling на 1% по 12 сервисам без центрального координатора. Как все 12 независимо договариваются оставить или отбросить один и тот же trace?"
  choices={[
    { label: "Каждый считает один и тот же детерминированный hash(trace-id) mod 100 против ставки. Поскольку trace-id одинаков на каждом hop, а хеш детерминирован, все сервисы приходят к одному вердикту keep/drop без всякой координации — consistent sampling, никогда не фрагмент.", correct: true },
    { label: "Коллектор пост-фактум сверяет посервисные решения и дропает несовпавшие span.", misconception: "Head sampling решает до того, как span покидают сервис; пост-фактум сверки нет. Согласованность даёт детерминированное хеширование trace-id в каждом сервисе, а не коллектор." },
    { label: "Каждый сервис сэмплит свой 1% независимо, так что в среднем trace примерно целый.", misconception: "Независимые посервисные «броски кубика» оставят span сервиса A и дропнут span сервиса B для одного trace — ровно тот частичный trace, ради предотвращения которого и существует consistent sampling." },
    { label: "Корневой сервис пишет вердикт, а нижестоящие полностью игнорируют sampled-флаг.", misconception: "Нижестоящие уважают sampled-флаг в trace-flags — но более глубокая гарантия в том, что даже пере-хеширование того же trace-id даёт тот же ответ, поэтому система согласована по построению." },
  ]}
/>

<Quiz
  id="06-trace-propagation-choice-3"
  lessonSlug="06-trace-propagation"
  lang="ru"
  question="Tail-sampling-коллектор OOM-ится каждые несколько часов. Число трейсов ровное, но spans-per-trace растёт. В чём причина и какой «фикс» делает только хуже?"
  choices={[
    { label: "Долгоживущий trace (batch-задача или рекурсивный воркер) накапливает тысячи span, все они держатся в RAM до закрытия окна решения. Удлинение окна решения «на всякий случай» раздувает буфер ещё сильнее и ускоряет OOM.", correct: true },
    { label: "Приходят дублирующиеся span; дедуплицируй на приёме.", misconception: "Дубликаты подняли бы число span по многим трейсам, а не сконцентрировали бы его на нескольких. Сигнатура здесь — несколько трейсов, каждый с тысячами span, это проблема долгоживущего trace." },
    { label: "Слишком мало реплик коллектора; надёжный фикс — просто больше реплик.", misconception: "Реплики за load-balancing-экспортёром делят активные трейсы между инстансами, но один жирный trace всё равно целиком падает на одну реплику. Реальный фикс — разбить работу на span-linked под-трейсы." },
    { label: "Хеш trace-id неравномерный; переключи экспортёр на round-robin.", misconception: "Round-robin разбрасывает span одного trace по репликам, так что ни один инстанс не видит весь trace — это ломает tail sampling напрочь. Хеширование по trace-id обязательно; оно не причина." },
  ]}
/>

<Quiz
  id="06-trace-propagation-choice-4"
  lessonSlug="06-trace-propagation"
  lang="ru"
  question="Node-сервис использует OTel HTTP-автоинструментацию. И работа, отложенная через setTimeout, и сообщения, которые он публикует в Kafka, появляются как orphan-трейсы. Какой правильный двухчастный фикс?"
  choices={[
    { label: "Обернуть колбэк setTimeout в context.bind(ctx, fn) для внутрипроцессной границы и вызвать propagator.inject() в заголовки сообщения при публикации (extract на консьюмере) для межпроцессной границы. Это разные механизмы.", correct: true },
    { label: "context.bind чинит оба — он держит активный context живым через любую границу.", misconception: "context.bind оживляет context только внутри процесса. Kafka пересекает межпроцессную границу без общей памяти, поэтому trace-id надо записать в заголовки сообщения через inject и прочитать обратно через extract." },
    { label: "inject/extract чинит оба — Kafka-инструментация покрывает и таймер.", misconception: "inject/extract — для сериализуемых носителей вроде заголовков сообщений. Колбэк setTimeout исполняется в том же процессе на новом async-стеке; ему нужен context.bind, а не инъекция в заголовки." },
    { label: "Перейти на ручные span везде; автоинструментация не умеет в async.", misconception: "Автоинструментация прекрасно держит HTTP; пробелы именно в необёрнутых async-колбэках и границах очередей. Фикс — точечный bind и inject/extract, а не отказ от автоинструментации." },
  ]}
/>

<Quiz
  id="06-trace-propagation-choice-5"
  lessonSlug="06-trace-propagation"
  lang="ru"
  question="Orphan-span rate внутреннего сервиса держится на 5% целый квартал, а дашборды показывают на вид целые трейсы. Команда добавляет tail sampling в надежде это вычистить. В чём ключевое заблуждение?"
  choices={[
    { label: "Tail sampling только выбирает, какие ЦЕЛЫЕ трейсы оставить — он не может восстановить отсутствующий parent-id. Orphan'ы создаются в момент эмиссии сломанной propagation и чинятся там; sampling только фильтрует, он никогда не чинит lineage.", correct: true },
    { label: "5% orphan'ов нормально для внутренних сервисов, так что всё в порядке.", misconception: "Входные сервисы законно корнят трейсы, но здоровый внутренний сервис должен быть около 0%. Устойчивые 5% на внутреннем сервисе — это регрессия propagation, а не baseline." },
    { label: "Дашборды доказывают, что propagation в норме; метрика считает неправильно.", misconception: "Это ровно ловушка тихого сбоя из юнита: дашборды продолжают рисовать трейсы, пока половина из них разорвана. Метрика orphan-rate — это правда; нарисованные трейсы — иллюзия." },
    { label: "Перевод всего на head sampling на 100% устранил бы orphan'ы.", misconception: "Ставка sampling управляет тем, экспортируются ли span, а не тем, несут ли они корректный parent-id. Экспорт большего числа orphan'ов их не связывает." },
  ]}
/>

<Quiz
  id="06-trace-propagation-choice-6"
  lessonSlug="06-trace-propagation"
  lang="ru"
  question="Консьюмер тянет 1000 Kafka-сообщений и обрабатывает их одним batch. Моделирование этого как один parent span с 1000 детей OOM-ит коллектор. Какой идиоматичный фикс и что он сохраняет?"
  choices={[
    { label: "Использовать span-links: создать один batch-span, который линкуется на все 1000 входящих SpanContext без того, чтобы делать их детьми. Trace остаётся достаточно малым для окна решения, а причинный lineage к каждому исходному сообщению сохраняется.", correct: true },
    { label: "Поднять num_traces, чтобы коллектор смог удержать широкий trace.", misconception: "num_traces ограничивает число активных трейсов, а не spans-per-trace. Trace с 1000 детей всё равно затопляет один буфер; поднятие лимита откладывает OOM, а не убирает fan-in." },
    { label: "Head-сэмплить batch-сервис на 0%, чтобы он перестал эмитить.", misconception: "Сброс всех batch-трейсов сбрасывает и те с ошибками и медленные, которые нужны команде. span-links держат batch наблюдаемым, ограничивая его размер." },
    { label: "Дать каждому сообщению свой независимый trace без связи с batch.", misconception: "Полностью независимые трейсы теряют причинность fan-in — ты больше не видишь, что одна batch-операция обработала именно эти сообщения. span-links существуют ровно ради сохранения этой связи." },
  ]}
/>

<Recap lang="ru">
Сквозная линия юнита — одна цепочка: 55-байтный W3C traceparent сшивает сервисы, невалидный заголовок означает «старт заново», а равномерно случайный trace-id позволяет каждому сервису захешировать один и тот же вердикт keep/drop для consistent sampling. Tail-sampling-коллектор меняет RAM (активные трейсы × span × байты × окно) на отбор с учётом исхода, поэтому долгоживущие трейсы и fan-in надо разбивать через span-links. Async-границы (setTimeout требует context.bind, очереди — inject/extract) — главный источник orphan'ов, а поскольку propagation падает тихо, orphan-span rate — единственная метрика, которая говорит, что дашборд врёт.
</Recap>
