---
concepts:
- async-local-storage
- contextvars
- go-context
- b3-migration
- service-mesh-tracing
- traceparent-privacy
deepensInto: []
estMin: 16
lang: ru
lessonType: topic
level: senior
order: 6
prereqs:
- observability/06-trace-propagation/05-sampling-consistency-and-collector
slug: 06-async-context-and-mesh
sources:
- https://opentelemetry.io/docs/concepts/context-propagation/
- https://github.com/openzipkin/b3-propagation
- https://www.w3.org/TR/trace-context/
spiral:
- encapsulation
status: ready
summary: Каждый runtime имеет субстрат контекста — Node AsyncLocalStorage, Python
  contextvars, Java thread-local, Go context.Context — у каждого свои failure modes.
  Mesh пропагирует заголовки для HTTP, не для очередей. traceparent в браузере — privacy-риск
  без ограничения scope.
title: Async context на разных языках, service mesh, миграция B3 и безопасность
track: observability
unit: 06-trace-propagation
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import Misconception from "~/components/layout/Misconception.astro";
import NumbersCard from "~/components/layout/NumbersCard.astro";
import SequenceDiagram from "~/components/diagram/SequenceDiagram.astro";
import Infographic from "~/components/diagram/Infographic.astro";
import b3MigrationOrder from "./infographics/b3-migration-order.svg?raw";

<Hook>
Java-сервис мигрирует с thread-pool-based request handling на Project Loom virtual threads. После миграции 30% трейсов — orphan'ы. Версия OTel SDK не менялась. Что сломалось?
</Hook>

<Crux>Node ALS, Python contextvars, Java Scope, Go ctx — у каждого своя хрупкость. Пересеки async-границу без контекста — трейс расщепится.</Crux>

<Explanation>

## Async context propagation: язык за языком

Каждый runtime имеет свой механизм для «что такое текущий execution-контекст» — и OTel хукается в этот механизм. Когда пересекаешь примитив, через который runtime не несёт контекст автоматически, контекст тихо теряется.

**Node.js:** `AsyncLocalStorage` (встроена в Node 12+) — субстрат. OTel хукает её для in-process propagation. Подводные камни: `setTimeout`, `setImmediate`, `queueMicrotask` и любые third-party promise wrapping, создающие новый AsyncLocalStorage-контекст, могут потерять trace-контекст. OTel auto-instrumentation патчит распространённые, но custom-библиотеки ломают. Фикс: `context.bind(ctx, fn)` перед передачей callback'а на любую async-границу.

**Python:** `contextvars` из PEP 567 — субстрат; работает автоматически для asyncio, но не для threading без `ContextVar.copy()`. При ручном создании потоков OTel-контекст родительского потока не наследуется. Фикс: передавай текущий контекст дочернему потоку и устанавливай через `context.attach(ctx)` на входе.

**Java:** классический `ThreadLocal` плюс `Span.makeCurrent()` в try-with-resources. Project Loom virtual threads в большинстве случаев прозрачны, но требуют аккуратного вложения `Scope` — если virtual thread создаётся внутри `Scope`, scope обязан пережить virtual thread, иначе контекст закрывается пока поток ещё работает. Фикс: структурируй создание virtual thread внутри scope, не снаружи.

**Go:** явный `context.Context` везде — каждая функция принимает `ctx`, каждый span живёт в `ctx`. Go сделал правильный архитектурный выбор рано; контекст никогда не течёт неявно. Failure mode в Go — не случайная потеря контекста, а забытый `ctx` в цепочке вызовов функций. Фикс: передавай `ctx` везде; используй `go vet` и `staticcheck` для отметки отсутствующих параметров контекста.

**Браузеры:** zone.js (решение Angular для патчинга async-примитивов) или предложение TC39 `AsyncContext`; OTel-JS поддерживает оба через плагины. Service workers и Web Workers требуют явной передачи контекста.

Паттерн одинаков для всех четырёх runtime'ов: найди примитив, на котором runtime перестаёт нести контекст неявно, и вставь явный bind, copy или pass-through в этой точке. Таблица ниже — быстрая шпаргалка: когда видишь orphan-span'ы в новом сервисе, найди его runtime и смотри строку.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Runtime</th>
        <th class="border border-border px-3 py-2 text-left">Субстрат контекста</th>
        <th class="border border-border px-3 py-2 text-left">Типичный failure mode</th>
        <th class="border border-border px-3 py-2 text-left">Фикс</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">Node.js</td>
        <td class="border border-border px-3 py-2">AsyncLocalStorage</td>
        <td class="border border-border px-3 py-2">setTimeout / custom async wrappers</td>
        <td class="border border-border px-3 py-2">context.bind(ctx, fn)</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Python</td>
        <td class="border border-border px-3 py-2">contextvars (PEP 567)</td>
        <td class="border border-border px-3 py-2">Ручное threading обходит asyncio</td>
        <td class="border border-border px-3 py-2">ContextVar.copy() при spawn потока</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Java</td>
        <td class="border border-border px-3 py-2">ThreadLocal + Scope</td>
        <td class="border border-border px-3 py-2">Loom virtual threads переживают Scope</td>
        <td class="border border-border px-3 py-2">Создавать virtual thread внутри Scope; использовать context-aware executor</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Go</td>
        <td class="border border-border px-3 py-2">Явный context.Context</td>
        <td class="border border-border px-3 py-2">ctx не протянут через функцию</td>
        <td class="border border-border px-3 py-2">Передавать ctx везде; vet/staticcheck</td>
      </tr>
    </tbody>
  </table>
</div>

## B3 vs W3C: история миграции и безопасная последовательность

До W3C Trace Context, Twitter's Zipkin/Brave использовали B3 (Binary Batch — формат распределённого трейсинга Zipkin, предшественник W3C-стандарта) с двумя вариантами: B3 multi-header (`X-B3-TraceId`, `X-B3-SpanId`, `X-B3-Sampled` как отдельные заголовки) и B3 single-header (все три в одном). Оригинальный B3 trace-id был 64-битный; расширен до 128 бит для совместимости с W3C.

**Безопасная последовательность миграции:**

1. **Аудит:** найти каждый сервис, всё ещё эмитирующий только B3-заголовки.
2. **Развернуть composite propagator везде:** зарегистрировать W3C TraceContext + B3 multi + B3 single на каждом сервисе. Писать исходящий только W3C; читать входящий из обоих. Это фаза «читать оба, писать W3C».
3. **Верификация:** подтвердить, что orphan-span rate не регрессирует.
4. **Убрать B3 outbound:** после подтверждения, что downstream-сервисы читают W3C, отключить B3 outbound у каждого upstream.
5. **Удалить B3 extractor:** после квартала при нулевом B3 inbound заменить composite propagator на W3C-only.

**Что ломается при пропуске шагов:**
- Пропуск шага 2: upstream отправляет W3C, downstream читает только B3 → трейсы расщепляются.
- Пропуск шага 3: propagation-регрессия тихо длится неделями.
- Пропуск шага 5: двойные байты заголовков на запрос бесконечно.

Шаги 2 и 3 чаще всего пропускают: шаг 2 — потому что деплой composite propagator кажется лишней работой, шаг 3 — потому что две недели мониторинга ощущаются медленными. Пропуск любого из них даёт тихую регрессию, которая обнаруживается как скачок orphan-span rate через дни или недели.

<Infographic
  svg={b3MigrationOrder}
  label="Восходящая лестница из пяти шагов миграции trace-контекста с B3 на W3C: аудит B3-only сервисов, развернуть composite-propagator read-both/write-W3C, верифицировать orphan-span rate, убрать B3 outbound, затем удалить B3 extractor."
  caption="Порядок несущий: read-both должен выйти до write-W3C, а пропуск шага верификации прячет propagation-регрессию на недели."
  hue="var(--d-infra)"
/>

## Trace-контекст через service mesh

Envoy, Linkerd, Istio, Cilium data plane участвуют в tracing двумя способами:
1. **Pass-through (всегда):** sidecar прозрачно перенаправляет заголовки `traceparent`/`tracestate`/`baggage` на каждом HTTP и gRPC запросе.
2. **Эмитировать собственные span'ы (опционально, но рекомендуется):** при включении sidecar создаёт span для сетевого hop'а, показывая sidecar-latency, connection pooling и TLS handshake timing отдельно от application-latency.

**Конфигурация:** mesh-proxy нужен адрес tracing-collector'а и решение по sampling (обычно — наследовать входящий флаг). Sampling-решение mesh'а должно совпадать с приложением; рассинхронизированные частоты дают несогласованные трейсы.

**Ограничение:** service mesh обрабатывает только HTTP и gRPC. Consumer'ы очередей, таймеры и fire-and-forget callback'и всё равно требуют явной application-level propagation. Mesh не замена OTel SDK instrumentation; он добавляет network-hop span, не заменяет application span'ы.

<Inset kind="why" lang="ru">
Когда mesh эмитирует собственный span, получаешь трёхспановый вид одного HTTP-вызова: client app, mesh sidecar, server app. Это позволяет разграничить «приложение было медленным» от «mesh был медленным» — критическое различие при инцидентах с обновлениями sidecar, исчерпанием connection pool или штормами обновления mTLS-сертификатов.
</Inset>

<SequenceDiagram
  label="Sidecar форвардит trace-контекст без изменений"
  hue="var(--d-systems)"
  actors={["App", "Sidecar", "Peer"]}
  messages={[
    { from: "App", to: "Sidecar", label: "запрос + traceparent (app span)" },
    { from: "Sidecar", to: "Peer", label: "форвард traceparent БЕЗ ИЗМЕНЕНИЙ (mesh span)" },
    { from: "Peer", to: "Sidecar", label: "ответ, тот же traceparent" },
    { from: "Sidecar", to: "App", label: "доставка ответа, контекст цел" },
  ]}
  caption="Sidecar оборачивает hop в собственный mesh-span, но форвардит traceparent приложения байт-в-байт, поэтому app-span'ы и mesh-span'ы делят один trace-id и сшиваются в единый трейс."
/>

## Безопасность: trace-id как tracking-идентификатор

Trace-id уникальны на запрос, 128 бит энтропии, пропагируются в HTTP-заголовках и видны всем, кто может инспектировать трафик между клиентом и origin. Это делает их мощными debug-инструментами и столь же мощными потенциальными trackers.

**Риск:** если third party (CDN, marketing pixel, CSP-allowed analytics-сервис) может прочитать заголовок `traceparent` из исходящих запросов пользователя, он может коррелировать user-активность между сайтами, делящими одну tracing-инфру.

**Смягчения:**
- W3C-спека рекомендует, чтобы user-facing-сервисы не пропагировали `traceparent` в response'ах (response — пользователю, не часть upstream-вызова).
- Browser-side OTel SDK должны ограничивать propagation same-origin и явно разрешёнными CORS-origin'ами (список `TraceContextPropagator.allowedOrigins`).
- Production-команды держат allowlist downstream-hostname'ов, получающих `traceparent`, и аудитируют квартально.
- Baggage применяется идентично — всё в baggage наблюдаемо каждым downstream, включая third-party.

<Misconception id="mc-baggage-pii">
  <p><strong>«Baggage — приватный канал между моими сервисами».</strong> Нет. Baggage течёт на каждый исходящий hop, включая third-party API, SaaS-интеграции, CDN, читающие заголовки. Всё в baggage semi-public внутри и за пределами инженерии. Никогда не клади PII или секреты.</p>
</Misconception>

<NumbersCard
  id="06-async-numbers"
  title="Продакшн-числа trace propagation старшего уровня"
  stats={[
    { label: "Дефолтный OTel propagator", value: "TraceContext + Baggage composite" },
    { label: "B3 single-header ширина trace-id (оригинал)", value: "64 бита (позже расширено до 128)" },
    { label: "Overhead tracing service-mesh sidecar", value: "~1–2% extra CPU" },
    { label: "Байт заголовков на запрос (traceparent + tracestate малый)", value: "~80–200 байт" },
    { label: "W3C Trace Context Level 1", value: "Recommendation 2020-02" },
    { label: "W3C Trace Context Level 2", value: "Recommendation 2024" },
  ]}
/>

<Quiz
  id="06-async-quiz-1"
  lessonSlug="06-async-context-and-mesh"
  lang="ru"
  question="Команда мигрирует с B3 на W3C propagation. Они деплоят W3C-write на upstream-сервисах до деплоя W3C-read на downstream. Что происходит?"
  choices={[
    { label: "Upstream-сервисы отправляют W3C traceparent; downstream только извлекают B3-заголовки — не находят trace-id и стартуют свежие трейсы, разрезая каждый трейс на этой границе", correct: true },
    { label: "Downstream-сервисы автоматически обнаруживают и парсят оба формата", misconception: "Если composite propagator (W3C + B3) не настроен, downstream запускают только зарегистрированный extractor. Если зарегистрирован только B3, W3C-заголовки игнорируются." },
    { label: "Новый traceparent-заголовок игнорируется старыми сервисами и B3-заголовок имеет приоритет", misconception: "Если downstream не пишут B3 на входящие запросы (потому что уже мигрировали на W3C-write), и если upstream уже дропнул B3-write, нет B3-заголовка для чтения downstream." },
    { label: "Оба формата сосуществуют безопасно потому что несут тот же trace-id", misconception: "Концептуально несут тот же trace-id, но извлечение зависит от формата. B3-only extractor не распарсит W3C traceparent-заголовок." },
  ]}
/>

<Quiz
  id="06-async-quiz-2"
  lessonSlug="06-async-context-and-mesh"
  lang="ru"
  question="Service mesh (Envoy) настроен пропагировать traceparent и эмитировать собственные mesh-hop span'ы. После включения команда всё равно видит orphan-трейсы для некоторых Kafka consumer'ов. Почему?"
  choices={[
    { label: "Service mesh обрабатывает только HTTP и gRPC. Kafka consumer'ы пересекают process-границу через заголовки сообщений и требуют application-level inject/extract — mesh не трогает Kafka wire-форматы.", correct: true },
    { label: "Envoy не поддерживает W3C traceparent-заголовок", misconception: "Envoy имеет нативную поддержку W3C Trace Context. Проблема в scope: mesh-sidecar'ы перехватывают HTTP/gRPC-трафик, не Kafka и другие queue-протоколы." },
    { label: "Orphan'ы вызваны mesh'ем, эмитирующим дублированные trace-id", misconception: "Envoy переиспользует входящий trace-id и создаёт дочерний span с новым span-id. Дублированные trace-id не эмитируются." },
    { label: "Mesh-hop span'ы требуют отдельную sampling-policy, которая ещё не настроена", misconception: "Sampling влияет на то, хранятся ли span'ы, а не на то, пропагируется ли traceparent. Kafka orphan'ы — propagation-проблема, не sampling-проблема." },
  ]}
/>

</Explanation>

<KeyTakeaway>Субстрат контекста каждого runtime имеет свой failure mode: Node AsyncLocalStorage теряется через setTimeout, Python contextvars требует ручного копирования через потоки, Java thread-local ломается при неправильном использовании Loom scope, Go требует явной передачи ctx. Миграция B3 требует read-both до write-W3C. Service mesh обрабатывает HTTP, но не очереди. traceparent в браузерных fetch-вызовах должен быть ограничен scope для предотвращения cross-site корреляции.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="06-async-retrieval"
  lang="ru"
  questions={[
    { q: "Java-сервис мигрирует на Project Loom virtual threads и orphan-span rate растёт до 30%. Диагностируй и фиксируй.", a: "Project Loom virtual threads поддерживаются carrier threads, и OTel Java SDK использует thread-local storage для текущего Span. Проблема: если virtual thread создаётся вне блока Span.makeCurrent() Scope — или если Scope закрывается (try-with-resources выходит), пока virtual thread ещё работает — ThreadLocal virtual thread'а больше не имеет Span. Virtual thread стартует с пустым контекстом и эмитирует orphan span'ы. Диагноз: проверь где создаются virtual thread'ы относительно Scope-блоков. Фикс: либо (а) создавать virtual thread внутри try-with-resources Scope, чтобы время жизни Scope покрывало работу потока, либо (б) переключиться на CurrentTraceContext-aware executor, пропагирующий OTel-контекст при submit'е потоков. Datadog сообщал о похожей проблеме в 2024, затрагивавшей большую Java-нагрузку, где 80% background-задач были orphan'ами по этой же причине." },
    { q: "Опиши безопасную 5-шаговую последовательность миграции с B3 на W3C TraceContext и что ломается при пропуске шага 2.", a: "Шаг 1: аудит — найти каждый сервис, всё ещё эмитирующий только B3-заголовки. Шаг 2: развернуть composite propagator везде (W3C + B3 multi + B3 single) — писать исходящий только W3C, читать входящий из обоих (фаза read-both, write-W3C). Шаг 3: верифицировать, что orphan-span rate не регрессирует в течение 2 недель. Шаг 4: отключить B3 outbound у каждого сервиса после подтверждения, что его downstream читает W3C. Шаг 5: удалить B3 extractor'ы после квартала при нулевом B3 inbound. Если пропустить шаг 2 и задеплоить W3C-write до деплоя W3C-read downstream: upstream отправляет traceparent; downstream запускает только B3 extractor; не находит B3-заголовок и генерирует свежий trace-id; каждый трейс расщепляется на этой service-границе. Регрессия тихая — дашборды показывают трейсы, просто не те." },
    { q: "Каков traceparent privacy-риск в браузерных приложениях и каковы три смягчения?", a: "Риск: traceparent — уникальный per-request 128-битный идентификатор. Если browser OTel SDK пропагирует его на cross-origin fetch-вызовах (к third-party CDN, analytics-сервисам, ad-пикселям или SaaS API), те third-party могут наблюдать trace-id и коррелировать запросы пользователя с оригинирующим сервисом. В сочетании с другими заголовками это позволяет cross-site tracking. Смягчение 1: настроить OTel-JS fetch instrumentation с явным allowlist origin'ов, которые должны получать traceparent (same-origin + собственные API только). Смягчение 2: W3C-спека рекомендует не пропагировать traceparent в server-to-browser response'ах — response идёт конечному пользователю, не upstream-сервису. Смягчение 3: аудитировать baggage квартально и гарантировать, что никакие user-identifying данные не текут на third-party endpoint'ы — те же CORS и origin-scoping правила применяются к baggage." },
  ]}
/>

<Recap lang="ru">
Propagation контекста в каждом runtime хукается в разный субстрат: `AsyncLocalStorage` в Node, `contextvars` в Python, `ThreadLocal` плюс `Scope` в Java, явный `context.Context` в Go. У каждого свой failure mode, когда пересекаешь примитив, через который runtime не несёт контекст автоматически — фикс всегда явная привязка контекста на этой границе. Миграция B3 на W3C требует read-both до write-W3C, верифицированного мониторингом orphan rate. Service mesh передаёт `traceparent` для HTTP/gRPC прозрачно и может эмитировать mesh-hop span'ы, но не инструментирует queue consumer'ов — они всё равно требуют application-level inject/extract. Заголовок `traceparent` в браузерных запросах — tracking-вектор при пропагировании к cross-origin third-party; ограничивай same-origin и явно разрешёнными CORS-origin'ами. Теперь, когда orphan-span'ы появятся в polyglot-системе, ты сопоставляешь runtime с его известным failure mode и точно знаешь, какой примитив проверять — без догадок и полной переинструментации.
</Recap>
