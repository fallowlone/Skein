---
concepts:
- burn-rate-promql
- traceparent-format
- flame-graph-reading
- log-trace-correlation
- multi-window-alert
estMin: 14
lang: ru
level: senior
order: 9
prereqs: []
slug: quiz-code
sources:
- https://sre.google/workbook/alerting-on-slos/
- https://www.w3.org/TR/trace-context/
- https://www.brendangregg.com/flamegraphs.html
status: ready
summary: "Чтение PromQL burn-rate-выражения, заголовка traceparent, flame graph и коррелированной строки лога — предсказать поведение и выбрать senior-прочтение."
title: 'Observability-капстоун: чтение сигналов и запросов'
track: observability
unit: 08-putting-it-together
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Артефакты трека — это запросы и wire-форматы: строка burn-rate PromQL, строка traceparent, flame graph, запись лога с trace-id. Прочитайте каждый так, как читали бы в 2 ночи, потом выберите вывод senior-инженера.
</Hook>

<Goal lang="ru">
Потренировать чтение четырёх конкретных артефактов главы — SLO-алерт-запроса, заголовка propagation, профиля и коррелированного лога — и превращение каждого в следующий шаг воронки.
</Goal>

## Сниппет 1 — multi-window burn-rate-алерт

```yaml
# SLO: 99.9% availability за 30 дней. Error budget = 0.1%.
- alert: CheckoutFastBurn
  expr: |
    (
      sum(rate(http_requests_total{job="checkout",code=~"5.."}[5m]))
      / sum(rate(http_requests_total{job="checkout"}[5m]))
    ) > (14.4 * 0.001)
    and
    (
      sum(rate(http_requests_total{job="checkout",code=~"5.."}[1h]))
      / sum(rate(http_requests_total{job="checkout"}[1h]))
    ) > (14.4 * 0.001)
  for: 2m
```

<Quiz
  id="08-putting-it-together-code-1"
  lessonSlug="08-putting-it-together"
  lang="ru"
  question="Чего добиваются множитель 14.4 и two-window AND-клауза в этом алерте?"
  choices={[
    { label: "Burn 14.4x означает, что 30-дневный бюджет был бы исчерпан за ~2 дня; короткое (5m) окно даёт сработать быстро, а длинное (1h) тоже должно гореть, чтобы подавить ложные срабатывания от одиночного спайка. Оба окна должны совпасть.", correct: true },
    { label: "14.4 — это процент SLO; два окна усредняют error rate по 5m и 1h.", misconception: "14.4 — это множитель burn-rate (во сколько раз быстрее бюджет-нейтрального), а не SLO. Окна объединяются по AND для подтверждения, а не усредняются." },
    { label: "Окно 1h избыточно — окно 5m само детектит burn.", misconception: "Окно 5m само по себе дёрганое и срабатывает на мгновенных всплесках. Окно 1h — это gate подтверждения; убрать его — обменять MTTD на поток ложных пейджей." },
    { label: "Это алертит только после полного исчерпания бюджета.", misconception: "Burn-rate-алертинг срабатывает, пока бюджет тратится слишком быстро — burn 14.4x исчерпал бы 30 дней бюджета за ~2 дня. Он предиктивный, а не пост-фактум." },
  ]}
/>

## Сниппет 2 — заголовок traceparent

```
traceparent: 00-4bf92f3577b34da6a3ce929d0e0e4736-00f067aa0ba902b7-01
```

<Quiz
  id="08-putting-it-together-code-2"
  lessonSlug="08-putting-it-together"
  lang="ru"
  question="Downstream-сервис получает этот заголовок. Читая четыре поля через дефис, что он должен сделать, чтобы корректно продолжить trace?"
  choices={[
    { label: "Сохранить trace-id (4bf9...4736) неизменным, считать parent-id (00f0...02b7) родительским span'ом, сгенерировать НОВЫЙ span-id для своей работы и пробросить trace-flags (01 = sampled) дальше. Переиспользование того же span-id схлопнуло бы два span'а в один.", correct: true },
    { label: "Сгенерировать свежий trace-id для своей работы и связать назад через tracestate.", misconception: "Новый trace-id ломает trace на два несвязанных дерева — весь смысл propagation в том, что trace-id постоянен на каждом hop. Меняется только span-id." },
    { label: "Переиспользовать все четыре поля дословно, чтобы trace оставался согласованным.", misconception: "Переиспользование входящего span-id как своего сливает ваш span с вызывающим, разрушая parent/child причинный граф. Вы наследуете trace-id и parent-id, но чеканите новый span-id." },
    { label: "Сбросить заголовок, потому что 01 означает, что trace уже завершён.", misconception: "01 в trace-flags — это бит sampled (держать этот trace), а не флаг завершения. Сброс заголовка рвёт propagation и осиротит каждый downstream-span." },
  ]}
/>

## Сниппет 3 — flame graph (текстовая форма)

```
checkout.HandleOrder            100%  (root, 1.50s wall)
  inventory.Lookup               87%  (1.30s)
    json.Marshal                 73%  (1.10s)   <-- широчайший лист
    grpc.Invoke                   9%  (0.13s)
  payment.Charge                  8%  (0.12s)
```

<Quiz
  id="08-putting-it-together-code-3"
  lessonSlug="08-putting-it-together"
  lang="ru"
  question="Этот профиль отфильтрован по медленному trace-id из trace view. Какое прочтение верное и какой следующий шаг воронки?"
  choices={[
    { label: "json.Marshal — широчайший лист на 73% wall time — вот куда ушёл CPU, внутри inventory.Lookup. Следующий шаг: git blame на call site json.Marshal, чтобы найти коммит, внёсший тяжёлую сериализацию.", correct: true },
    { label: "inventory.Lookup на 87% — hotspot для фикса.", misconception: "inventory.Lookup — это фрейм-предок: его 87% в основном унаследованы от ребёнка json.Marshal. Оптимизируют широчайший ЛИСТ (self-time), а не родителя, который лишь вызывает его." },
    { label: "grpc.Invoke на 9% — сетевое узкое место, вызывающее замедление.", misconception: "9% — это мало; 73% в json.Marshal затмевают его. Это проблема CPU-сериализации, а не сетевая." },
    { label: "Flame graph ничего не скажет без сравнения двух профилей.", misconception: "Один профиль уже называет широчайший лист и его долю self-time. Differential-профилирование помогает при регрессиях, но здесь один отфильтрованный профиль указывает прямо на json.Marshal." },
  ]}
/>

## Сниппет 4 — коррелированная строка лога

```json
{"level":"error","ts":"2026-05-29T02:14:07Z","service":"inventory",
 "msg":"marshal failed: schema v3 field overflow","trace_id":"4bf92f3577b34da6a3ce929d0e0e4736",
 "span_id":"00f067aa0ba902b7","http.route":"/inventory/lookup"}
```

<Quiz
  id="08-putting-it-together-code-4"
  lessonSlug="08-putting-it-together"
  lang="ru"
  question="Как эта одна строка лога привязывается к trace и профилю из предыдущих сниппетов и почему это важно?"
  choices={[
    { label: "Она несёт тот же trace_id (4bf9...4736), что и медленный trace и профиль, поэтому джойнится именно к тому запросу — превращая 'что-то где-то упало' в 'этот конкретный запрос словил schema-v3 overflow в inventory.Marshal'. Поле trace_id — это join-ключ через все три сигнала.", correct: true },
    { label: "Она матчится по timestamp; вы находите её поиском логов около 02:14:07.", misconception: "Поиск по timestamp возвращает каждый лог в эту секунду по всем запросам. trace_id — это то, что прицельно привязывает к одному падающему запросу; ровно поэтому структурированные логи его несут." },
    { label: "Она матчится по http.route, так что любой лог /inventory/lookup эквивалентен.", misconception: "http.route матчит каждый запрос к этому эндпоинту, а не один медленный запрос. Только trace_id изолирует этот конкретный падающий запрос из тысяч здоровых." },
    { label: "Она не связывается — логи и трейсы это отдельные силосы.", misconception: "С trace_id, проброшенным в структурированные логи, логи и трейсы явно сджойнены. Это и есть four-signal-модель: один trace-id на логах, exemplar'ах метрик, трейсах и профилях." },
  ]}
/>

<Recap lang="ru">
Четыре артефакта — одна цепочка. Multi-window burn-rate-запрос даёт алерт (короткое окно для скорости, длинное для подтверждения, 14.4x = burn бюджета за 2 дня). Заголовок traceparent держит trace-id постоянным, чеканя новый span-id на каждом hop. Flame graph называет широчайший лист как self-time-hotspot — оптимизируй лист, а не предка. А структурированный лог несёт тот же trace_id, поэтому джойнится точно к тому запросу. Прочитанные последовательно, это воронка, сделанная конкретной.
</Recap>
