---
concepts:
- debugging-funnel
- three-pillars-join
- otel-architecture
- tail-sampling
- burn-rate-alerting
- observability-roi
estMin: 13
lang: ru
level: senior
order: 8
prereqs: []
slug: quiz-short
sources:
- https://opentelemetry.io/docs/
- https://sre.google/workbook/implementing-slos/
- https://www.w3.org/TR/trace-context/
status: ready
summary: "Free-recall-промпты через весь observability-трек — воронка, join трёх pillar'ов, архитектура OTel, sampling, burn-rate-алертинг и ROI."
title: 'Observability-капстоун: free-recall-обзор'
track: observability
unit: 08-putting-it-together
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";

<Hook>
Retrieval бьёт перечитывание. Для каждого промпта восстановите полный ответ по памяти через весь трек, прежде чем открывать модельный ответ — именно усилие припоминания сплавляет отдельные юниты в одну ментальную модель.
</Hook>

<Goal lang="ru">
Восстановить хребет главы, не подглядывая: debugging-воронку, как джойнятся три pillar'а, архитектуру OTel, рычаги sampling и стоимости, burn-rate-алертинг и арифметику ROI.
</Goal>

<RetrievalDrawer
  client:visible
  id="08-putting-it-together-short-retrieval"
  lang="ru"
  questions={[
    { q: "Назовите пять слоёв debugging-воронки по порядку и скажите, что вывод каждого слоя подаёт в следующий.", a: "1. SLO burn-rate-алерт — вывод: какой сервис имеет user-facing impact и как быстро горит бюджет. 2. RED-дашборд этого сервиса — вывод: что из Rate, Errors, Duration сдвинулось, и форма (спайк, дрейф, плато). 3. Trace view, отфильтрованный по burn-окну — вывод: какой сервис и какой span владеет latency или errors; медленный trace-id. 4. Профиль, отфильтрованный по этому trace-id — вывод: широчайший лист, функция и file:line, съевшие время. 5. git blame на этой функции — вывод: коммит, автор, deploy-окно. Вывод каждого слоя — это вход-фильтр для следующего, поэтому следующий клик продиктован, а не выбран. Апгрейды тулинга режут MTTR на 10–20%; дисциплина воронки — на 50–80%, потому что большая часть потерянного времени — навигация, а не анализ." },
    { q: "Как три pillar'а — метрики, логи, трейсы — плюс профили складываются в одну навигируемую поверхность, а не в четыре силоса?", a: "Каждый сигнал отвечает на свой вопрос дешевле всего: метрики (низкая cardinality) для дашбордов и алертов, логи (высокая cardinality) для точечного поиска событий, трейсы (средняя) для per-request причинных графов, профили — какая функция съела CPU. Они становятся одной поверхностью через общий join-ключ — trace-id, пробрасываемый W3C traceparent. Логи эмитят его как структурированное поле, метрики — как histogram exemplar'ы, трейсы ключуются по нему нативно, профили помечают им каждый sample. Запрос 'всё про этот один request' тогда возвращает строки логов, exemplar метрики, полное дерево трейса и sample'ы профиля, сджойненные одной hex-строкой. Без согласованного propagation инженер откатывается к гаданию по timestamp + service-name, что медленно и неоднозначно при конкуренции." },
    { q: "Опишите трёхслойную архитектуру OTel и чего в ней стоит смена backend.", a: "Слой 1, приложение: каждый сервис запускает один OTel SDK (или auto-instrumentation), эмитящий все четыре сигнала по OTLP. Слой 2, collector: локальный агент (DaemonSet на ноду или sidecar) принимает OTLP на localhost:4317, батчит, применяет tail sampling и PII-scrubbing и маршрутизирует в backend'ы; semantic conventions держат ключи атрибутов согласованными между сервисами. Слой 3, backend: любое OTLP-принимающее хранилище — Tempo/Loki/Prometheus/Pyroscope self-hosted или Datadog/Honeycomb/New Relic managed. Поскольку приложение эмитит стандартный OTLP, смена вендора — это смена endpoint-URL'ов exporter'а в конфиге collector'а — config-PR и staging-тест, а не проект переинструментирования. Эта портируемость — ядро обещания OTel: инструментируй однажды, меняй backend без правки кода." },
    { q: "Почему tail sampling нужен в дополнение к head sampling и какова каноническая production-политика?", a: "Head sampling решает в начале трейса, до того как появился error или медленный хвост, поэтому вероятностное правило keep-5% отбрасывает 95% интересных трейсов заранее — а span'ы, которые вы не эмитили, не вернуть. Tail sampling вместо этого эмитит 100% в SDK и буферизует все span'ы трейса в collector'е, пока трейс не закроется (или не истечёт окно решения ~10–30s), затем решает с полной информацией. Каноническая политика: держать 100% трейсов с любым ERROR-span, держать 100% трейсов выше медленного порога (например 2s), держать 5% случайного baseline всего остального. Это сбрасывает объём трейсов на 80–90%, сохраняя каждый трейс, который вы вообще стали бы навигировать; baseline даёт контекст по rate и паттернам. Цена — память collector'а на буферизацию span'ов; поэтому collector — это tier-0-инфраструктура со своим SLO." },
    { q: "Как работает multi-window burn-rate-алерт и почему он лучше статического порога по сырому error rate?", a: "Error budget = 1 − SLO (для 99.9% за 30 дней бюджет — 0.1% запросов). Burn rate — во сколько раз быстрее бюджет-нейтрального вы его тратите; burn 14.4x исчерпывает 30-дневный бюджет примерно за два дня. Multi-window-алерт срабатывает, только когда короткое окно (например 5m, для быстрого детекта) И длинное окно (например 1h, для подтверждения) оба горят выше порога. AND подавляет ложные срабатывания от одиночного спайка, удерживая MTTD низким. Статический порог по сырому error rate либо пейджит на каждый транзиентный всплеск (слишком шумно), либо выставлен так высоко, что пропускает медленные burn'ы, тихо сливающие бюджет. Burn-rate-алертинг привязывает пейдж прямо к потреблению бюджета — к тому, что SLO реально защищает — и доводит MTTD до 1–5 минут." },
    { q: "Сформулируйте ROI-аргумент для observability как арифметику, а не слоган, и из чего измеряется каждый вход.", a: "ROI = избегнутая стоимость outage / расход на observability. Избегнутая стоимость = (инцидентов в год) × (сэкономленных минут MTTR против baseline до инструментирования) × (revenue-экспозиция в минуту). Каждый вход измерим: число инцидентов из incident-лога, MTTR-дельта из записей MTTR до/после, revenue/min из ARR, поделённого вниз ($100M-ARR SaaS — это примерно $190/min сырой выручки, больше с учётом churn и стоимости доверия). Проработанный пример: 30 инцидентов × 25 сэкономленных min × $5k/min — это около $3.75M избегнутого против $150k расхода — примерно 25x. Это арифметика, потому что каждый член взят из реальной истории, а не из оценок. Команды, которые не могут выдать этот расчёт, склонны терять observability-бюджет в следующий downturn и потом смотреть, как MTTR ползёт обратно вверх. Большая картина: observability покупает скорость деплоя — вы можете катить 10 раз в день и падать быстро только потому, что воронка и SLO делают отказ дешёвым для поиска и ограниченным." },
  ]}
/>

<Recap lang="ru">
Если вы смогли восстановить каждый ответ по памяти, вы держите хребет главы: воронка сужает SLO → RED → trace → profile → git blame, каждый шаг продиктован предыдущим; три pillar'а плюс профили складываются через join по trace-id; OTel — это один SDK, один OTLP-формат, взаимозаменяемые backend'ы; tail sampling держит 100% важного и сбрасывает остальное; multi-window burn-rate-алертинг привязывает пейджи к потреблению бюджета; а расход оправдан измеримой арифметикой избегнутого outage. Observability — это не мониторинг; это субстрат, позволяющий деплоить без страха.
</Recap>
