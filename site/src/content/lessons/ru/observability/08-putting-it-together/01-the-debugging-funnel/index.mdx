---
concepts:
- debugging-funnel
- mttr
- funnel-discipline
- slo-burn-alert
deepensInto:
- observability/08-putting-it-together/02-otel-architecture-four-signals
estMin: 10
lang: ru
lessonType: topic
level: junior
order: 1
prereqs:
- observability/04-red-use/01-red-and-use-overview
- observability/05-slo-budgets/01-sli-slo-budget-basics
- observability/06-trace-propagation/01-what-is-propagation
- observability/07-profiling/01-flame-graph-basics
slug: 01-the-debugging-funnel
sources:
- https://opentelemetry.io/docs/concepts/observability-primer/
- https://sre.google/workbook/implementing-slos/
- https://grafana.com/blog/the-red-method-how-to-instrument-your-services/
spiral:
- latency
- encapsulation
status: ready
summary: Фиксированный порядок воронки — SLO burn, затем RED, trace, profile, git
  blame — режет MTTR на 50–80%, устраняя случайную навигацию по дашбордам.
title: 'Debugging-воронка: SLO → RED → trace → profile'
track: observability
unit: 08-putting-it-together
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import MetaphorComplete from "~/components/pedagogy/MetaphorComplete.astro";
import NumbersCard from "~/components/layout/NumbersCard.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";
import Infographic from "~/components/diagram/Infographic.astro";
import mttrReduction from "./infographics/mttr-reduction.svg?raw";

<Hook>
2 ночи. Срабатывает SLO burn alert. Открываешь четыре дашборда наугад, спрашиваешь в Slack кто деплоил последним, и тратишь 40 минут на охоту. Твой коллега, подключившийся к тому же звонку, следует пятишаговому чеклисту и закрывает инцидент за 8 минут. Разница не в инструментах — в порядке.
</Hook>

<Crux>RED — симптом. SLO — бюджет. Traces — span. Profiles — функция. Одна петля, три минуты, каждый инцидент.</Crux>

<Explanation>

## Ментальная модель воронки

К концу этого урока ты будешь знать пятишаговую последовательность, которая ведёт от «что-то горит» до «вот тот коммит» менее чем за три минуты — и почему пропуск любого шага отнимает время, а не экономит его.

Debugging-воронка — это фиксированная сужающаяся последовательность. Вывод каждого слоя — входной фильтр следующего. В воронке пять слоёв:

1. **SLO burn-rate alert** — подтверждает user-facing impact и называет сервис. Без SLO burn команда ещё не знает, реальный ли симптом или flaky-тест.
2. **RED-дашборд** — Rate, Errors, Duration для названного сервиса. Одна из трёх переменных сдвинулась; форма (spike vs drift vs plateau) подсказывает класс причины.
3. **Trace view** — отфильтрованный на burn-окно. Находит какой сервис и какой span владеет latency или ошибками. Сужает с «что-то сломано в checkout» до «inventory.lookup занимает 1.3 с».
4. **Profile** — отфильтрованный по trace-id из шага 3. Flame graph показывает какая функция внутри span'а потребила CPU или I/O. Сужает с «inventory.lookup медленный» до «json.Marshal на новой схеме».
5. **git blame** — на функцию и строку из профиля. Называет коммит, автора и окно деплоя.

Вместе эти пять шагов означают: каждый клик определён предыдущим, и ни один слой не факультативен — пропусти SLO burn, и потратишь двадцать минут на сервис, который вообще не был источником проблемы. Воронка фиксирована. Инструменты меняются каждые два года; воронка не меняется пятнадцать.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Слой</th>
        <th class="border border-border px-3 py-2 text-left">Инструмент</th>
        <th class="border border-border px-3 py-2 text-left">Вывод (сужает до)</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">1. SLO burn alert</td>
        <td class="border border-border px-3 py-2">Alerting + SLO-дашборд</td>
        <td class="border border-border px-3 py-2">Какой сервис имеет user-facing impact</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">2. RED-дашборд</td>
        <td class="border border-border px-3 py-2">Grafana / Datadog</td>
        <td class="border border-border px-3 py-2">Что из Rate / Errors / Duration сдвинулось</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">3. Trace view</td>
        <td class="border border-border px-3 py-2">Jaeger / Tempo / Honeycomb</td>
        <td class="border border-border px-3 py-2">Какой сервис и span владеет временем</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">4. Profile</td>
        <td class="border border-border px-3 py-2">Pyroscope / Parca</td>
        <td class="border border-border px-3 py-2">Какая функция внутри span'а ела CPU</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">5. git blame</td>
        <td class="border border-border px-3 py-2">git</td>
        <td class="border border-border px-3 py-2">Коммит, автор, окно деплоя</td>
      </tr>
    </tbody>
  </table>
</div>

<FlowDiagram
  label="Четыре сигнала как сужающаяся воронка"
  perRow={1}
  hue="var(--d-systems)"
  nodes={[
    { id: "metric", label: "Метрика", sub: "что-то сломано" },
    { id: "trace", label: "Trace", sub: "какой запрос / сервис" },
    { id: "log", label: "Лог", sub: "что именно случилось" },
    { id: "profile", label: "Profile", sub: "какой код / строка" },
  ]}
  edges={[
    { from: "metric", to: "trace", label: "уже" },
    { from: "trace", to: "log", label: "уже" },
    { from: "log", to: "profile", label: "уже" },
  ]}
  caption="Каждый сигнал отвечает на более узкий вопрос, чем предыдущий: алерт метрики говорит, что что-то сломалось, trace выделяет запрос и сервис, лог сообщает, что произошло, а profile называет точный код."
/>

## Конкретный проход

Стартап перенял полный стек главы: RED-дашборды, SLO с multi-window burn-rate алертами, OTel traces, continuous profiling. On-call-инженер получает пейджер: «checkout SLO burn 14x».

Она не открывает все дашборды. Она следует воронке.

- **RED на checkout:** p99 прыгнул с 200 мс до 1.5 с; Rate стабилен; Errors стабильны. Спайк только Duration — не краш, не проблема capacity.
- **Trace view, отфильтрованный на burn-окно:** один span доминирует в каждом медленном trace — `inventory.lookup`, 1.3 с из 1.5 с суммарных.
- **Profile inventory-сервиса, отфильтрованный по тому trace-id:** 1.1 с в `json.Marshal` — новый кодпуть, сериализующий слишком много.
- **git blame на call site `json.Marshal`:** коммит приземлился 30 минут назад, автор — inventory-команда, деплой совпадает со временем начала burn.

Время от пейджера до git blame: 90 секунд. Воронка сделала работу.

За полгода MTTR команды упал с 45 минут до 8. Не быстрее отдельные клики — просто без случайного порядка.

## Почему апгрейд инструментов не режет MTTR

Tooling даёт 10–20% улучшения MTTR. Дисциплина воронки — 50–80%, потому что большая часть потерь — навигация: открытие какого дашборда последним видел on-call, вопросы в Slack, переключение сигналов без направления. Воронка устраняет обдумывание. Каждый клик определён предыдущим. Senior'ы, кажущиеся в 10x быстрее во время инцидентов, обычно не гоняют другие запросы; они гоняют те же в правильном порядке, пока junior'ы — в случайном.

<Infographic
  svg={mttrReduction}
  label="Столбчатая диаграмма снижения MTTR по источнику: только новый tooling даёт лишь 10–20%, а дисциплина воронки — 50–80%, примерно вчетверо больше."
  caption="Лучшие инструменты сокращают любой одиночный шаг; фиксированный порядок воронки сокращает всё путешествие. Поэтому большая часть выигрыша MTTR — от дисциплины, а не от новых инструментов."
  hue="var(--d-infra)"
/>

<Inset kind="why" lang="ru">
Представь приёмный покой больницы. Пациент в боли. Шаг 1: жизненные показатели (RED) — пульс, BP, температура. Шаг 2: известные ограничения (SLO) — стабилен или критичен? Шаг 3: визуализация (traces) — где в теле точно? Шаг 4: биопсия (profile) — из чего ткань состоит? Каждый шаг разные инструменты, но все отвечают на один вопрос: где резать? Production observability — то же самое.
</Inset>

<NumbersCard
  id="card-funnel-numbers"
  title="Дисциплина воронки: что говорят числа"
  stats={[
    { label: "Снижение MTTR от дисциплины воронки", value: "50–80%" },
    { label: "Снижение MTTR только от нового tooling", value: "10–20%" },
    { label: "MTTR в примере (стартап, 6 месяцев)", value: "45 мин → 8 мин" },
    { label: "Типичный MTTD с burn-rate-алертами", value: "1–5 минут" },
    { label: "Типичный MTTR с дисциплиной воронки", value: "3–10 минут" },
  ]}
  data-lesson-visual
/>

<Quiz
  id="01-the-debugging-funnel-quiz-1"
  lessonSlug="01-the-debugging-funnel"
  lang="ru"
  question="В воронке production-debugging какой сигнал смотрит on-call первым?"
  choices={[
    { label: "SLO burn-rate-алерт (или RED-дашборд, на который он указывает) — называет user-facing-симптом; всё остальное — drill-down оттуда", correct: true },
    { label: "Continuous-profile flame graph", misconception: "Profile — последний шаг; требует уже знать, какой сервис и span смотреть." },
    { label: "Сырые логи", misconception: "Логи — последнее средство для конкретных event-lookup'ов; воронка доходит до них, только когда trace'ов недостаточно." },
    { label: "Какой дашборд on-call открыл последним", misconception: "Случайный порядок — то, что делает incident response медленным. Воронка для этого." },
  ]}
/>

<DragOrder
  id="01-the-debugging-funnel-drag-1"
  lessonSlug="01-the-debugging-funnel"
  lang="ru"
  prompt="Расставь production-debugging-воронку от широкого к узкому:"
  items={[
    { id: "a", label: "SLO burn-rate-алерт сработал — user impact идёт, бюджет тратится быстро" },
    { id: "b", label: "RED-дашборд затронутого сервиса — что из Rate / Errors / Duration сдвинулось?" },
    { id: "c", label: "Trace view, отфильтрованный на burn-окно — какой сервис и span владеет latency?" },
    { id: "d", label: "Profile, отфильтрованный по trace-id — какая функция внутри span'а ела время?" },
    { id: "e", label: "git blame на горячей функции — какой коммит и автор её ввели?" },
  ]}
  correctOrder={["a", "b", "c", "d", "e"]}
/>

<MetaphorComplete
  id="01-the-debugging-funnel-metaphor-1"
  lessonSlug="01-the-debugging-funnel"
  lang="ru"
  setup="Заполни пропуск: порядок воронки SLO → RED → trace → _______ → код."
  accepted={["profile", "profiles", "profiling", "профиль", "профилирование"]}
  canonical="profile (или profiles / profiling / профиль)"
  explanation="Profile сидит между trace span (куда ушло время) и кодом (почему). Каждый слой сужает поиск на один уровень."
/>

<Quiz
  id="01-the-debugging-funnel-quiz-2"
  lessonSlug="01-the-debugging-funnel"
  lang="ru"
  question="Что значит обещание OpenTelemetry 'один SDK, один wire-format' для engineering-org?"
  choices={[
    { label: "Переключение observability-backend становится config-изменением, а не re-instrumentation-проектом — SDK остаётся, меняется только collector-exporter", correct: true },
    { label: "Делает мониторинг бесплатным", misconception: "OTel не меняет vendor-цены, только portability." },
    { label: "Убирает нужду в дашбордах", misconception: "Дашборды остаются; OTel меняет то, что их кормит." },
    { label: "Автомагически отлаживает твой код", misconception: "OTel собирает данные; люди отлаживают." },
  ]}
/>

</Explanation>

<KeyTakeaway>Фиксированная воронка — SLO → RED → trace → profile → git blame — режет MTTR на 50–80%, устраняя навигационные потери. Апгрейд инструментов даёт 10–20%; дисциплина воронки — остальное. Дисциплину сложнее установить, чем инструменты, но она окупается навсегда.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="01-the-debugging-funnel-retrieval"
  lang="ru"
  questions={[
    { q: "Почему следование фиксированному порядку воронки режет MTTR больше, чем покупка лучших инструментов?", a: "Лучшие инструменты сокращают любой одиночный шаг; фиксированный порядок воронки сокращает всё путешествие, убирая ненужные поиски. Без воронки инженеры открывают какой дашборд первый вспомнят, охотятся за зацепками, переключают инструменты, отвлекаются. Каждое отвлечение — минуты. С воронкой следующий клик всегда определён текущим: SLO указывает на сервис, RED на метрику, trace на span, profile на функцию. Без размышлений, без случайных кликов. Tool-апгрейды дают 10–20% MTTR; дисциплина воронки — 50–80%, потому что большая часть потерь была навигацией, не анализом." },
    { q: "В сценарии SLO burn checkout что показал RED и какую форму это исключило?", a: "RED на checkout показал: p99 Duration прыгнул с 200 мс до 1.5 с, Rate и Errors стабильны. Спайк только Duration без изменения Errors исключает краш или logic-регресс. Стабильный Rate исключает исчерпание capacity. Форма говорит: медленный downstream или новый кодпуть, потребляющий лишнее время. Это сужает следующий шаг: открой trace view, отфильтрованный на медленные Duration-примеры, чтобы найти span, владеющий превышением на 1.3 с." },
    { q: "Назови пять слоёв воронки в порядке и укажи, как вывод каждого слоя питает следующий.", a: "1. SLO burn alert — вывод: какой сервис имеет user-facing impact. 2. RED-дашборд — вывод: что из Rate / Errors / Duration сдвинулось и форма (spike, drift, plateau). 3. Trace view — вывод: какой сервис и span владеет latency; медленный trace-id. 4. Profile (отфильтрованный по trace-id из шага 3) — вывод: конкретная функция и file:line, потребившие время. 5. git blame (на функции из шага 4) — вывод: хэш коммита, автор, окно деплоя. Вывод каждого слоя — фильтр-вход для следующего." },
  ]}
/>

<Recap lang="ru">
Производственная debugging-воронка — пятислойная сужающаяся последовательность: SLO burn называет сервис, RED — сдвинувшийся сигнал, trace — span, profile — функцию, git blame — коммит. Вывод каждого слоя фильтрует запрос следующего, поэтому никакого обдумывания не требуется. Апгрейды инструментов дают 10–20% улучшения MTTR; дисциплина воронки — 50–80%, потому что большая часть потерь инцидента — навигация, не анализ. Стартап с полным observability-стеком и дисциплиной воронки увидел снижение MTTR с 45 до 8 минут за шесть месяцев — не от более быстрых инструментов, а от устранения случайного порядка. Воронка фиксирована; инструменты меняются каждые два года, воронка не меняется пятнадцать. Теперь, когда придёт пейдж, удержись от соблазна открыть последний просмотренный дашборд — начни с SLO burn и пусть воронка ведёт каждый последующий клик.
</Recap>
