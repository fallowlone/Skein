---
concepts:
- sampling-profiler
- instrumentation-profiler
- sample-rate
- overhead
- pprof
- perf
deepensInto: []
estMin: 13
lang: ru
lessonType: topic
level: middle
order: 2
prereqs:
- observability/07-profiling/01-flame-graph-basics
slug: 02-sampling-vs-instrumenting
sources:
- https://www.brendangregg.com/flamegraphs.html
- https://pkg.go.dev/runtime/pprof
- https://github.com/grafana/pyroscope
spiral:
- latency
status: ready
summary: Instrumentation оборачивает каждый вызов функции — точные данные, но коллапсирует
  под нагрузкой; sampling захватывает один стек каждые 10 мс при ограниченном overhead,
  что делает его единственно жизнеспособным подходом в production.
title: 'Sampling vs instrumentation profiling: почему 99 Гц побеждает в production'
track: observability
unit: 07-profiling
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import NumbersCard from "~/components/layout/NumbersCard.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
На staging-сервере profiling работает без проблем. Тот же profiler в production поднимает latency на 40%. Profiler не менялся — поменялась нагрузка. Понять почему — разница между инструментом, который можно держать включённым always, и тем, который используешь только в экстренных случаях.
</Hook>

<Crux>Instrumentation = точные данные, непосильная цена при масштабе. Sampling = ограниченная цена, достаточная точность для production.</Crux>

<Explanation>

## Два способа профилировать

**Instrumentation profiling** оборачивает каждый вход и выход функции таймирующим кодом. Runtime измеряет точное число вызовов и точную длительность каждой функции. Стоимость пропорциональна частоте вызовов — при 10 миллионах вызовов в секунду платишь за 10 миллионов замеров в секунду. На dev-машине с лёгкой нагрузкой это нормально; под production-трафиком может добавить 20-100% overhead. Instrumentation хороша для маленьких изолированных benchmark'ов; для always-on production profiling не годится.

**Sampling profiling** работает иначе: внешний таймер срабатывает N раз в секунду и захватывает текущий call stack. Стоимость ровно N stack walk'ов в секунду — независимо от того, сколько вызовов функций происходит между ними. При 100 Гц — это 100 stack walk'ов в секунду, каждый стоит ~5-20 микросекунд, итого overhead ~0.5-2% на занятом CPU. Современные profiler'ы (Go pprof, Linux perf, Java async-profiler, py-spy, eBPF-профилеры) — все sampling-based. Стоимость ограничена.

<FlowDiagram
  label="Sampling vs instrumentation: стоимость зависит от разных входов"
  perRow={2}
  hue="var(--d-systems)"
  nodes={[
    { id: "running", label: "Программа", sub: "вызовы N×/с", col: 0, row: 0 },
    { id: "clock", label: "Sampling", sub: "таймер @ 99 Гц", col: 0, row: 1 },
    { id: "instr", label: "Instrumenting", sub: "обёртка входа/выхода", col: 1, row: 1 },
    { id: "snapshot", label: "Снимок стека", sub: "фикс. частота", col: 0, row: 2 },
    { id: "count", label: "Замер каждого", sub: "растёт с вызовами", col: 1, row: 2 },
    { id: "stat", label: "Статистика", sub: "~0.5-2% over", col: 0, row: 3 },
    { id: "exact", label: "Точный счётчик", sub: "20-100% over", col: 1, row: 3 },
  ]}
  edges={[
    { from: "running", to: "clock", label: "sample" },
    { from: "running", to: "instr", label: "instrument" },
    { from: "clock", to: "snapshot" },
    { from: "instr", to: "count" },
    { from: "snapshot", to: "stat", label: "огранич. цена" },
    { from: "count", to: "exact", label: "цена ∝ частоте" },
  ]}
  caption="Стоимость sampling фиксирована sample rate (низкая, статистическая); стоимость instrumentation растёт с частотой вызовов (высокая, точная) — одна программа, два противоположных профиля overhead."
/>

## Ключевое статистическое свойство

При sample rate R Гц функция, потребляющая X% CPU, будет появляться в X% сэмплов — независимо от числа вызовов. Статистика — «доля CPU-времени», не «счётчик вызовов». Это именно та метрика, нужная для поиска узких мест: хочешь знать, какая функция чаще всего на CPU, а не какую чаще всего вызывают.

Следствие: sampling при 100 Гц достаточен для обнаружения функций, потребляющих больше нескольких процентов CPU. Функция, использующая 10% CPU, будет попадать в ~10 сэмплов в секунду. За 30-секундный профиль — 300 сэмплов — вполне достаточно для надёжной оценки.

## Выбор sample rate на практике

Linux `perf` по умолчанию 99 Гц — не 100 Гц. Это намеренно: 100 Гц может случайно синхронизироваться с периодическими kernel-таймерами и давать вводящие в заблуждение результаты. 99 Гц избегает резонанса. Go pprof по умолчанию 100 Гц. eBPF-based continuous profiler'ы обычно используют 19 или 49 Гц для минимизации влияния на очень занятые контейнеры.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Подход</th>
        <th class="border border-border px-3 py-2 text-left">Модель стоимости</th>
        <th class="border border-border px-3 py-2 text-left">Типичный overhead</th>
        <th class="border border-border px-3 py-2 text-left">Применение</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">Instrumentation</td>
        <td class="border border-border px-3 py-2">На вызов функции</td>
        <td class="border border-border px-3 py-2">20-100%</td>
        <td class="border border-border px-3 py-2">Dev benchmark'и, целевые micro-benchmark'и</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Sampling (Go pprof)</td>
        <td class="border border-border px-3 py-2">На сэмпл (100/с)</td>
        <td class="border border-border px-3 py-2">0.5-2%</td>
        <td class="border border-border px-3 py-2">Production on-demand или continuous</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Sampling (eBPF)</td>
        <td class="border border-border px-3 py-2">На сэмпл (19-49/с)</td>
        <td class="border border-border px-3 py-2">1-3%</td>
        <td class="border border-border px-3 py-2">Continuous, polyglot-флоты</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Continuous profiler (полный)</td>
        <td class="border border-border px-3 py-2">Sampling + batching + shipping</td>
        <td class="border border-border px-3 py-2">2-5%</td>
        <td class="border border-border px-3 py-2">Always-on production fleet</td>
      </tr>
    </tbody>
  </table>
</div>

<NumbersCard
  id="card-sample-rates"
  title="Sample rate'ы и overhead"
  stats={[
    { label: "Go pprof дефолтный sample rate", value: "100 Гц" },
    { label: "Linux perf дефолтный rate", value: "99 Гц (избегает синхронизации с таймером)" },
    { label: "eBPF profiler типичный rate", value: "19 или 49 Гц" },
    { label: "Стоимость stack walk на сэмпл", value: "~5-20 мкс" },
    { label: "Overhead при 100 Гц, занятый CPU", value: "~0.5-2%" },
    { label: "Continuous profiler (полный pipeline)", value: "2-5% CPU" },
  ]}
/>

## Когда заявленный 2-5% overhead становится 12%

Continuous profiler заявляет 2% overhead; ты измеряешь 12% в production. Частые причины:

- Sample rate случайно выставлен в 10 раз выше дефолта.
- Stack walking дорог, потому что язык использует JIT и требует symbol resolution при каждом сэмпле (Python, JVM без native hooks).
- Agent делает тяжёлую компрессию или декомпрессию символов в application thread вместо async.
- Средняя глубина стека 120+ frames (глубокие middleware-цепочки) — каждый сэмпл пропорционально дороже.

Прежде чем обвинять инструмент — проверь конфигурацию.

<Quiz
  id="02-sampling-vs-instrumenting-quiz-1"
  lessonSlug="02-sampling-vs-instrumenting"
  lang="ru"
  question="Сервис совершает 5 миллионов вызовов функций в секунду. Instrumentation profiler добавляет 1 мкс на вызов. Sampling profiler работает при 100 Гц и стоит 10 мкс на сэмпл. Какой добавляет меньше overhead?"
  choices={[
    { label: "Sampling profiler: 100 × 10 мкс = 1 мс overhead в секунду против 5 млн × 1 мкс = 5 секунд на секунду для instrumentation — sampling выигрывает в 5000 раз", correct: true },
    { label: "Instrumentation profiler, потому что 1 мкс ничтожен", misconception: "1 мкс ничтожен на один вызов, но умноженный на 5 млн вызовов в секунду он на порядки превышает overhead sampling." },
    { label: "Они эквивалентны", misconception: "Модели стоимости per-call vs per-sample расходятся катастрофически при высоких частотах вызовов." },
    { label: "Ни один не имеет значения на современных CPU", misconception: "Оба добавляют реальный и измеримый overhead; sampling создан именно для его ограничения." },
  ]}
/>

<Quiz
  id="02-sampling-vs-instrumenting-quiz-2"
  lessonSlug="02-sampling-vs-instrumenting"
  lang="ru"
  question="Linux perf по умолчанию 99 Гц, а не 100 Гц. Почему?"
  choices={[
    { label: "Чтобы избежать случайной синхронизации с 100-Гц периодическими kernel-таймерами, которая заставила бы их код появляться в каждом сэмпле независимо от реального CPU-потребления", correct: true },
    { label: "Потому что 99 — простое число", misconception: "Причина техническая — избегание резонанса с таймерами, а не нумерология." },
    { label: "Потому что 100 Гц требует специальных kernel patches", misconception: "100 Гц полностью поддерживается; выбор намеренный для статистической корректности." },
    { label: "Чтобы сэкономить 1% overhead", misconception: "Разница overhead между 99 и 100 Гц пренебрежимо мала; мотивация — избегание резонанса с таймерами." },
  ]}
/>

</Explanation>

<KeyTakeaway>Sampling захватывает один стек каждые 10 мс при ограниченном overhead независимо от частоты вызовов; instrumentation оборачивает каждый вызов и коллапсирует под нагрузкой. Функция, потребляющая X% CPU, появляется в X% сэмплов — статистика именно доля CPU, не счётчик. 2-5% overhead реален и проверяем; скачок выше — мисконфигурация.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="02-sampling-vs-instrumenting-retrieval"
  lang="ru"
  questions={[
    { q: "Почему instrumentation profiling непрактичен для always-on production profiling?", a: "Instrumentation оборачивает каждый вход и выход функции таймирующим кодом, поэтому стоимость масштабируется с частотой вызовов. Сервис, обрабатывающий 5 млн вызовов в секунду при 1 мкс на вызов, добавляет 5 CPU-секунд overhead в секунду — невозможно поддерживать. Sampling ограничивает стоимость N stack walk'ами в секунду, где N — sample rate (обычно 100), давая фиксированный overhead ~1 мс в секунду независимо от частоты вызовов. При production-нагрузке эта разница — граница между жизнеспособным и катастрофическим." },
    { q: "Sampling profiler сообщает, что функция присутствует в 8% сэмплов. Что это значит с точки зрения CPU?", a: "Функция была на CPU в 8% взятых сэмплов. При статистическом разрешении profiler'а это представляет приблизительно 8% CPU-времени, потреблённого функцией за окно профилирования. Точное значение имеет доверительный интервал (шире при коротком capture, уже при длинном), но 8% — достаточно надёжный сигнал для действий. Статистика — доля CPU-времени, не счётчик вызовов: функция, вызванная 10 млн раз по 1 мкс, покажет те же 10%, что и функция, вызванная 100 раз по 100 мкс, если суммарное CPU сопоставимо." },
    { q: "Почему Linux perf использует 99 Гц вместо 100 Гц?", a: "В Linux есть периодические kernel-таймеры, которые срабатывают с кратными базовой настраиваемой частоте. Если sampling profiler срабатывает с той же частотой, что один из этих таймеров, profiler всегда будет видеть код этого таймера на CPU — искусственно раздувая его долю. 99 Гц — простое число относительно стандартных частот таймеров (100, 250, 1000 Гц), что разрывает случайную синхронизацию. Brendan Gregg задокументировал этот паттерн; большинство production profiler'ов ему следуют. Разница overhead между 99 и 100 Гц пренебрежимо мала; разница статистической корректности может быть существенной." },
  ]}
/>

<Recap lang="ru">
Instrumentation profiling оборачивает каждый вызов функции, давая точные данные, но коллапсируя под production-нагрузкой при масштабировании overhead с частотой вызовов. Sampling profiling срабатывает N раз в секунду и захватывает текущий стек — ограниченный overhead независимо от числа вызовов между сэмплами. При 100 Гц и ~10 мкс на stack walk overhead 0.5-2%; полный continuous profiler pipeline (с batching и shipping) — 2-5%. Статистическое свойство, делающее sampling мощным: функция, потребляющая X% CPU, появляется в X% сэмплов — оценки доли CPU без затрагивания каждого вызова. Linux perf по умолчанию 99 Гц для избегания синхронизации с периодическими kernel-таймерами — тонкая деталь корректности, которую должен знать каждый senior-инженер.
</Recap>
