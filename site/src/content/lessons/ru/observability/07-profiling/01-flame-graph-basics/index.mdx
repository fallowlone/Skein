---
concepts:
- profiling
- flame-graph
- call-stack
- sampling-profiler
- cpu-time
deepensInto: []
estMin: 12
lang: ru
lessonType: topic
level: junior
order: 1
prereqs:
- observability/06-trace-propagation/07-production-failures-and-design
slug: 01-flame-graph-basics
sources:
- https://www.brendangregg.com/flamegraphs.html
- https://queue.acm.org/detail.cfm?id=2927301
- https://pkg.go.dev/runtime/pprof
spiral:
- latency
status: ready
summary: Profiler сэмплирует call stack 100 раз в секунду и строит flame graph — самый
  широкий frame на любом уровне — это функция, поедающая CPU, и её видно с первого
  взгляда.
title: 'Flame graph: читаем картинку, которая показывает, куда ушло время'
track: observability
unit: 07-profiling
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import MetaphorComplete from "~/components/pedagogy/MetaphorComplete.astro";
import DiagramFrame from "~/components/diagram/DiagramFrame.astro";
import Infographic from "~/components/diagram/Infographic.astro";
import samplingPipeline from "./infographics/sampling-pipeline.svg?raw";

<Hook>
Trace говорит «1.2 секунды в inventory». Есть логи, метрики, дашборды — но ни один из них не скажет, какая именно функция внутри inventory съела время. Profiling отвечает за 60 секунд, без отладчика и без угадывания. За следующие десять минут ты поймёшь, как именно читать картинку, которая показывает, куда уходит время.
</Hook>

<Crux>Profiling = сэмплы стеков. Широкий frame = горячая функция. Найди узкое место с одного взгляда, без grep-сессий.</Crux>

<Explanation>

## Что делает profiler

Profiler (инструмент сэмплирования производительности) прерывает программу 100 раз в секунду и захватывает текущий **call stack** (стек вызовов) — цепочку функций от `main` до того, что выполняется прямо сейчас. За 30 секунд набирается 3000 снимков. Функция, чаще всего встречающаяся в снимках, — та, что больше всего ест CPU.

<Infographic
  svg={samplingPipeline}
  label="Пятиэтапный конвейер, превращающий периодические прерывания в вердикт о горячем пути: прерывание программы 100 раз в секунду, снимок call stack каждый раз, накопление около 3000 стеков за 30 секунд, ранжирование функций по частоте появления, затем построение flame graph, где самый широкий frame — горячий путь."
  caption="Сэмплирование статистическое, а не исчерпывающее: ~3000 снимков за 30 с достаточно, чтобы самая частая функция доказуемо была пожирателем CPU — поэтому profiler при 2-5% overhead может работать always-on."
  hue="var(--d-infra)"
/>

**Flame graph** визуализирует это: стеки отсортированы alphabetically по x-оси, ширина пропорциональна числу сэмплов, y-ось — глубина от точки входа внизу до leaf-функции наверху. Самый широкий frame на любом уровне — самый горячий путь.

<DiagramFrame
  label="Flame graph: вызывающие под вызываемыми, ширина = доля CPU-времени по сэмплам"
  hue="var(--d-systems)"
  caption="Каждая строка — один уровень call stack (main внизу, on-CPU leaf наверху). Ширина frame'а — доля сэмплов в нём — никогда не меньше суммы его children. json.Marshal — самый широкий leaf, значит это горячий путь; gc — тонкая полоска."
>
  <svg viewBox="0 0 600 200" width="600" height="200" role="img" aria-label="Flame graph: main в основании расширяется вверх до json.Marshal как самого горячего leaf-frame'а" style="max-width:100%;height:auto;font-family:var(--font-mono, monospace);">

    <rect x="20" y="156" width="560" height="28" rx="3" fill="#e25822" />
    <text x="300" y="174" text-anchor="middle" font-size="13" fill="#ffffff">main  (100%)</text>


    <rect x="20" y="120" width="480" height="28" rx="3" fill="#ef7d3a" />
    <text x="260" y="138" text-anchor="middle" font-size="13" fill="#ffffff">handleRequest  (86%)</text>
    <rect x="510" y="120" width="70" height="28" rx="3" fill="#c8c8c8" />
    <text x="545" y="138" text-anchor="middle" font-size="11" fill="#333333">gc</text>


    <rect x="20" y="84" width="440" height="28" rx="3" fill="#f59b4b" />
    <text x="240" y="102" text-anchor="middle" font-size="13" fill="#333333">serializeResponse  (79%)</text>
    <rect x="466" y="84" width="34" height="28" rx="3" fill="#c8c8c8" />
    <text x="483" y="102" text-anchor="middle" font-size="10" fill="#333333">db</text>


    <rect x="20" y="48" width="420" height="28" rx="3" fill="#ffc234" stroke="#e25822" stroke-width="2" />
    <text x="230" y="66" text-anchor="middle" font-size="13" fill="#333333">json.Marshal  (75%)  ← горячий leaf</text>


    <text x="588" y="170" text-anchor="end" font-size="10" fill="#888888">глубина ↑</text>
    <line x1="20" y1="192" x2="580" y2="192" stroke="#bbbbbb" stroke-width="1" />
    <text x="300" y="200" text-anchor="middle" font-size="10" fill="#888888">ширина = CPU-время по сэмплам (не порядок wall-clock)</text>
  </svg>
</DiagramFrame>

## Метафора со стадионом

Представь стадион со 100 000 людьми, занятых разным. Вертолёт пролетает сверху 100 раз в секунду и фотографирует, кто что делает. За минуту — 6000 фото. Посчитай, какая активность встречается чаще всего — туда уходит «CPU-время» толпы.

Flame graph — столбчатая диаграмма этих счётчиков, с вызывающими под вызываемыми. Широкие бары = популярные активности. Вертолёт — profiler; фото — стек-сэмплы; диаграмма — flame graph.

## Чтение flame graph на практике

Bea on-call. Inventory-сервис, p99 = 1.5 с. Она открывает continuous-profile dashboard, фильтрует по trace-id, видит flame graph с одним массивным 1.1-секундным блоком: `json.Marshal` внутри `serializeResponse`. Фикс очевиден: кешировать маршаленный ответ или пре-кодировать при записи. Без profiling команда гадала бы — БД? Кеш? Сеть? С flame graph гадать не нужно.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Ось</th>
        <th class="border border-border px-3 py-2 text-left">Значение</th>
        <th class="border border-border px-3 py-2 text-left">Частая ошибка</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">Ширина (x)</td>
        <td class="border border-border px-3 py-2">Число сэмплов — доля CPU-времени</td>
        <td class="border border-border px-3 py-2">Читают слева-направо как «порядок времени» — это НЕ ТАК</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Позиция (x)</td>
        <td class="border border-border px-3 py-2">Alphabetical-группировка по parent'у</td>
        <td class="border border-border px-3 py-2">Левый frame НЕ выполняется раньше правого</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Высота (y)</td>
        <td class="border border-border px-3 py-2">Глубина стека — main внизу, leaf наверху</td>
        <td class="border border-border px-3 py-2">Более высокий стек = более глубокая вложенность, не медленнее</td>
      </tr>
    </tbody>
  </table>
</div>

## Как захватить CPU-профиль с помощью pprof

Node API — внезапный скачок p99. Tracing находит медленный span. Continuous-profile dashboard, отфильтрованный по trace-id, показывает flame graph с доминирующим regex compile в handler'е. Library upgrade ввёл O(n²) regex; фикс — precompile вне handler'а. Детекция — 60 секунд.

Для Go-сервисов pprof встроен:

```go
// 1. Подключи pprof-handler'ы (регистрирует маршруты /debug/pprof/*)
import _ "net/http/pprof"

// Запусти debug-сервер
go func() {
  http.ListenAndServe("localhost:6060", nil)
}()

// 2. Захвати 30-секундный CPU-профиль под нагрузкой:
//    go tool pprof -http=:9090 \
//      http://localhost:6060/debug/pprof/profile?seconds=30
//
// 3. Flame graph view откроется на :9090.
//    Самый широкий leaf-frame наверху = горячий путь.
```

Профилировать нужно под **репрезентативной нагрузкой** — на простаивающей системе в сэмплах будет почти только idle loop рантайма, бесполезный для поиска горячих путей.

<Quiz
  id="01-flame-graph-basics-quiz-1"
  lessonSlug="01-flame-graph-basics"
  lang="ru"
  question="Что означает ШИРИНА frame'а на flame graph?"
  choices={[
    { label: "Как часто эта функция встречалась в снимках стека — шире значит больше CPU-времени", correct: true },
    { label: "Сколько функция компилировалась", misconception: "Compile-время не связано с шириной flame graph." },
    { label: "Число строк в функции", misconception: "Размер кода не имеет значения; ширина — частота сэмплов." },
    { label: "Wall-clock-время по x-оси", misconception: "x-ось — alphabetical-сортировка стеков, не время. Ширина — счётчик сэмплов." },
  ]}
/>

<Quiz
  id="01-flame-graph-basics-quiz-2"
  lessonSlug="01-flame-graph-basics"
  lang="ru"
  question="Continuous profiler работает в production при 2-5% CPU overhead. Почему не убивает производительность?"
  choices={[
    { label: "Он сэмплирует — не каждый вызов записывается; только ~100 снимков стека в секунду. Стоимость ограничена частотой сэмплирования.", correct: true },
    { label: "Останавливает программу во время работы", misconception: "Не останавливает; сэмплирует без остановки выполнения." },
    { label: "Работает только ночью", misconception: "Continuous profiling работает непрерывно по определению." },
    { label: "Профилирует только ошибки", misconception: "Profiler захватывает всю CPU-активность, не только пути ошибок." },
  ]}
/>

<DragOrder
  id="01-flame-graph-basics-drag-1"
  lessonSlug="01-flame-graph-basics"
  lang="ru"
  prompt="Расставь шаги CPU-профилирования медленной функции с pprof:"
  items={[
    { id: "a", label: "Определить подозрительную нагрузку (медленный span, высокий CPU, медленный endpoint)" },
    { id: "b", label: "Стартовать профилирование (pprof.StartCPUProfile или endpoint /debug/pprof/profile)" },
    { id: "c", label: "Прогнать подозрительную нагрузку 30 секунд под нагрузкой" },
    { id: "d", label: "Остановить профилирование и сохранить файл профиля" },
    { id: "e", label: "Открыть профиль во flame graph viewer (go tool pprof, speedscope, Pyroscope)" },
    { id: "f", label: "Найти самый широкий frame на leaf-уровне — это горячая функция" },
    { id: "g", label: "Подняться по parent'ам — кто вызывает горячий путь, затем применить фикс" },
  ]}
  correctOrder={["a", "b", "c", "d", "e", "f", "g"]}
/>

<MetaphorComplete
  id="01-flame-graph-basics-metaphor-1"
  pieceSlug="01-flame-graph-basics"
  lang="ru"
  setup="Заполни пропуск: вертикальная ось flame graph показывает call _______ — main внизу, функция на CPU наверху."
  accepted={["stack", "стек", "depth", "stack depth"]}
  canonical="stack (call stack — стек вызовов)"
  explanation="y-ось — call stack: функция внизу вызвала ту, что над ней, та вызвала следующую — до самого верха. Верх любой колонки — то, что было на CPU в момент сэмпла."
/>

</Explanation>

<KeyTakeaway>Profiler сэмплирует call stack ~100 раз в секунду. Flame graph превращает тысячи сэмплов в одну картинку: самый широкий frame = самый горячий код. Профилируй под репрезентативной нагрузкой — пустая система показывает только idle loop.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="01-flame-graph-basics-retrieval"
  lang="ru"
  questions={[
    { q: "В одном абзаце: почему flame graph почти всегда быстрее, чем отладчик или print-statement'ы, для поиска медленной части программы?", a: "Отладчик и print'ы требуют угадать, куда смотреть — поставить breakpoint, наблюдать строку, замерять вручную. С функцией на 1000 строк и тремя вложенными цепочками вызовов догадка почти всегда неверна. Flame graph показывает ответ: самый широкий frame — самый горячий путь, без угадывания. Profiler уже инспектировал каждый вызов через тысячи сэмплов; читаешь результат в один взгляд. Единственное требование — медленная функция должна выполняться на CPU достаточно часто, чтобы попасть в сэмплы. Для всего остального (а это почти всё в production) flame graph — самый быстрый путь от «что-то медленно» к «эта функция медленная»." },
    { q: "Какая самая распространённая ошибка при чтении flame graph и что на самом деле означает x-ось?", a: "Самая частая ошибка — читать x-ось как порядок времени: предполагать, что frame слева выполняется раньше frame'а справа. На самом деле x-ось — это alphabetical-группировка: все children одного parent'а отсортированы лексикографически и расположены рядом. Ширина — число сэмплов (доля CPU-времени), не позиция на временной оси. Frame правее другого не значит, что он выполняется позже. Если нужен порядок времени — используй trace view (Gantt-style spans), а не flame graph." },
    { q: "Почему нужно профилировать под репрезентативной нагрузкой, а не на тихой системе?", a: "Sampling profiler видит только то, что запущено. На простаивающей системе в сэмплах почти только idle loop рантайма или GC — бесполезно для поиска горячих путей. Под репрезентативной нагрузкой реальные горячие пути приложения доминируют в сэмплах, и flame graph указывает на настоящее узкое место. Production-grade profiling захватывается при реальном трафике; staging — под синтетической нагрузкой, имитирующей production. Профилировать без нагрузки — профилировать не то." },
  ]}
/>

<Recap lang="ru">
Profiler прерывает программу ~100 раз в секунду, захватывает call stack и после многих сэмплов строит flame graph: ширина = доля CPU. Самый широкий frame на любом уровне — самый горячий путь, без угадывания. x-ось — alphabetical-группировка стеков, не время; читать её как время — самая распространённая ошибка новичка. Профилировать нужно под репрезентативной нагрузкой — пустая система показывает только idle loop. С continuous profiler'ом, работающим always-on при 2-5% overhead, flame graph любого инцидента уже сохранён к моменту, когда срабатывает пейджер. Теперь, когда увидишь широкий фрейм во flame graph, ты знаешь точный вопрос: кто его вызывает и можно ли сократить его работу или закешировать результат?
</Recap>
