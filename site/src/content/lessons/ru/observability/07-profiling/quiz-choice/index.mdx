---
concepts:
- sampling-profiler
- profile-types
- continuous-profiling
- flame-graph-reading
- ebpf-symbolization
- profile-security
estMin: 13
lang: ru
level: senior
order: 7
prereqs: []
slug: quiz-choice
sources:
- https://www.brendangregg.com/flamegraphs.html
- https://pkg.go.dev/runtime/pprof
- https://github.com/grafana/pyroscope
status: ready
summary: "Тест с выбором на синтез по всему юниту profiling — экономика sampling, выбор типа профиля, чтение flame graph, символизация eBPF и production-дисциплина."
title: 'Profiling: тест с выбором ответа'
track: observability
unit: 07-profiling
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Шесть вопросов поперёк всего юнита. Каждый отражает решение, которое ты принимаешь в реальном инциденте по латентности — не определение для заучивания, а то, какой профиль снять, как его прочитать и что его числа реально значат под production-нагрузкой.
</Hook>

<Goal lang="ru">
Убедись, что связываешь экономику sampling, выбор типа профиля, чтение flame graph, символизацию eBPF и production-дисциплину — тот синтез, к которому вели отдельные уроки.
</Goal>

<Quiz
  id="07-profiling-choice-1"
  lessonSlug="07-profiling"
  lang="ru"
  question="Запрос идёт 500 мс по wall-clock, но CPU profile приписывает ему лишь 20 мс. Куда ушли остальные 480 мс и какой профиль их покажет?"
  choices={[
    { label: "Поток был off-CPU — ждал I/O, лок или планировщик — а CPU profile слеп к ожиданию. Off-CPU profile (или block/mutex профиль, или trace-спан) приписывает эти 480 мс месту блокирующего вызова.", correct: true },
    { label: "CPU profile недосэмплирован; подними sample rate до 1000 Hz и недостающие 480 мс появятся.", misconception: "Более высокий rate соберёт больше сэмплов работающего кода, но снятый с CPU поток потребляет ноль CPU и его не на чем сэмплировать при любой частоте. Это время off-CPU по определению." },
    { label: "480 мс — это пауза GC; сними heap profile.", misconception: "Heap profile показывает аллоцированные байты по стеку, а не время ожидания. Общий ответ для запроса с низким CPU и большим wall — off-CPU ожидание, видимое в off-CPU или block профиле." },
    { label: "Профайлер потерял сэмплы; 480 мс невосстановимы.", misconception: "Ничего не потеряно. CPU profile корректно сообщает 20 мс вычислений; остальное — off-CPU время, которое CPU-профайлер и не должен видеть." },
  ]}
/>

<Quiz
  id="07-profiling-choice-2"
  lessonSlug="07-profiling"
  lang="ru"
  question="Сервис делает 5 миллионов вызовов функций в секунду. Нужен always-on профайлинг в production. Почему sampling profiler — единственный жизнеспособный выбор против инструментирования?"
  choices={[
    { label: "Стоимость инструментирования растёт с частотой вызовов (5М измерений/с); стоимость sampling фиксирована — N обходов стека/с независимо от частоты вызовов. На 100 Hz это ~1 мс overhead в секунду против секунд overhead в секунду у инструментирования.", correct: true },
    { label: "Инструментирование не умеет снимать стеки вызовов, только sampling умеет.", misconception: "Инструментирование снимает точные счётчики вызовов и тайминги по функциям — больше данных, а не меньше. Дисквалифицирует его стоимость: она растёт с частотой вызовов, а не объём данных." },
    { label: "Sampling точнее инструментирования.", misconception: "Инструментирование точное; sampling статистический, с доверительным интервалом. Sampling выигрывает на ограниченном overhead, а не на точности — его точности просто достаточно, чтобы найти hotspot." },
    { label: "Sampling-профайлерам не нужна представительная нагрузка.", misconception: "Обоим нужна представительная нагрузка — на простаивающей системе виден только idle-цикл рантайма. Sampling выигрывает в production за счёт ограниченного, не зависящего от частоты вызовов overhead." },
  ]}
/>

<Quiz
  id="07-profiling-choice-3"
  lessonSlug="07-profiling"
  lang="ru"
  question="Инженер указывает на два широких фрейма рядом на одном уровне flame graph и заключает, что левый выполняется раньше правого. Что не так?"
  choices={[
    { label: "Ось x — это алфавитная группировка полных путей стека, а не время. Левее значит лексикографически раньше, и ничего о порядке вызовов. Для порядка выполнения нужен trace-таймлайн (спаны в стиле Gantt), а не flame graph.", correct: true },
    { label: "Ничего — flame graph упорядочен по времени слева направо, так что левый фрейм действительно выполнился первым.", misconception: "Это самое частое неверное прочтение flame graph. Flame graph сортирует стеки алфавитно по корню, чтобы дети одного родителя стояли рядом; ось x никогда не время." },
    { label: "У этих фреймов должен быть общий родитель, поэтому порядок не определён, но они чередуются.", misconception: "У них может быть общий родитель, а может и нет. Соседство возникает из лексикографической сортировки строки полного стека, а ширина — это число сэмплов; ни то ни другое не кодирует, когда и как часто они чередовались." },
    { label: "Вывод верен только если профайлер использовал режим wall-clock.", misconception: "Ни один стандартный flame graph не ставит время на ось x. Wall-clock-вид одного запроса — это flame chart / trace-таймлайн, другая визуализация." },
  ]}
/>

<Quiz
  id="07-profiling-choice-4"
  lessonSlug="07-profiling"
  lang="ru"
  question="eBPF-профайлер показывает чистые стеки для Go и Rust, но множество '[unknown]' фреймов для Python-сервиса на той же ноде. Почему?"
  choices={[
    { label: "Go и Rust встраивают frame pointers / DWARF, так что ядро разматывает стек и символизирует нативно. CPython исполняет код как байткод внутри своего C-цикла интерпретатора, поэтому снятые адреса указывают в этот цикл, а не в исходник Python — символизация требует CPython-aware поддержки.", correct: true },
    { label: "Python слишком медленный для eBPF-сэмплинга, поэтому фреймы истекают по таймауту и показываются как unknown.", misconception: "Скорость к символизации отношения не имеет. Ядро снимает стек нормально; оно просто не может сопоставить внутренние адреса интерпретатора именам функций Python без CPython-специфичной разматки." },
    { label: "eBPF-агент сломан для Python и его надо заменить.", misconception: "[unknown] фреймы для интерпретируемых рантаймов ожидаемы без явной поддержки. Решение — Python-aware профайлер (py-spy) или CPython-поддерживающий eBPF-режим, а не замена агента." },
    { label: "Python использует JIT-компиляцию, которая путает разматчик.", misconception: "CPython — байткод-интерпретатор, не JIT. У JIT-рантаймов (JVM, V8) другая проблема символизации, решаемая через perf maps или AsyncGetCallTrace." },
  ]}
/>

<Quiz
  id="07-profiling-choice-5"
  lessonSlug="07-profiling"
  lang="ru"
  question="Continuous profiler заявлен с overhead 2-5%, но в production ты меряешь 12%. Какую причину проверять первой?"
  choices={[
    { label: "Конфигурацию или окружение, а не инструмент: sample rate выставлен сильно выше дефолта, дорогая символизация на app-потоке, или очень глубокие (120+ фреймов) цепочки middleware, из-за которых каждый обход стека стоит пропорционально дороже.", correct: true },
    { label: "Инструмент врёт; немедленно меняй профайлер.", misconception: "Цифра 2-5% реальна и проверяема на типичных нагрузках. Скачок выше почти всегда означает мисконфиг или стоимость глубоких стеков / JIT-символизации — проверь конфиг до смены инструмента." },
    { label: "Continuous profiling фундаментально слишком дорог для production.", misconception: "Continuous profiling на 2-5% — индустриальный always-on стандарт. Посыл юнита в том, что он достаточно дёшев, чтобы оставлять включённым; 12% — аномалия для диагностики." },
    { label: "Overhead sampling всегда растёт с трафиком, так что 12% под нагрузкой ожидаемы.", misconception: "Стоимость sampling ограничена sample rate, а не частотой вызовов, поэтому она не растёт с трафиком. Рост до 12% указывает на мисконфиг rate, синхронную символизацию или глубокие стеки." },
  ]}
/>

<Quiz
  id="07-profiling-choice-6"
  lessonSlug="07-profiling"
  lang="ru"
  question="На разборе инцидента кто-то предлагает приложить production-профиль к тикету в поддержку вендора. Почему senior-инженер должен возразить?"
  choices={[
    { label: "Профили чувствительны с точки зрения безопасности: имена функций раскрывают приватные API и внутреннюю структуру, allocation-профили могут захватывать значения аргументов, а сами имена выдают проприетарную логику. Относись к ним как к выводу отладчика — RBAC, ограниченная ретенция, без внешнего шаринга без апрува.", correct: true },
    { label: "Возражать не нужно — профили это операционные данные и их безопасно делить широко.", misconception: "Это ровно то заблуждение, о котором предупреждает юнит. Профиль ближе к выводу отладчика, чем к метрике; имена функций и паттерны вызовов позволяют реверс-инжинирить бизнес-логику." },
    { label: "Чувствительны только allocation-профили; CPU profile прикладывать безопасно.", misconception: "CPU profile уже раскрывает имена функций и паттерны вызовов. Allocation-профили добавляют сверху возможные значения аргументов — но и одного CPU profile достаточно, чтобы требовать контроль доступа." },
    { label: "Файл слишком большой для письма.", misconception: "Сжатые профили крошечные (~50-500 КБ). Проблема в раскрытии информации, а не в размере файла." },
  ]}
/>

<Recap lang="ru">
Сквозная линия юнита — одна диагностическая петля: выбери профиль, который отвечает на твой вопрос (CPU для вычислений, off-CPU/block/mutex для ожидания, heap/allocation для памяти), читай flame graph правильно (ширина — доля сэмплов, ось x алфавитная, а не время) и доверяй числам только зная их пределы (sampling ограничен, но слеп к hot-путям короче интервала, eBPF требует language-aware символизации, overhead 2-5% реален, но чувствителен к конфигу). А поскольку профиль раскрывает внутренности кода, относись к нему как к чувствительному артефакту, а не к свободно шарящейся метрике.
</Recap>
