---
concepts:
- flame-graph-algorithm
- differential-profile
- profile-as-data
- slo-burn-drill
- deploy-regression
deepensInto: []
estMin: 15
lang: ru
lessonType: topic
level: middle
order: 5
prereqs:
- observability/07-profiling/04-continuous-profiling
slug: 05-reading-flame-graphs
sources:
- https://www.brendangregg.com/flamegraphs.html
- https://github.com/grafana/pyroscope
- https://github.com/parca-dev/parca
spiral:
- latency
status: ready
summary: Одинаковые стеки схлопываются, алфавитная сортировка группирует родителей
  с детьми, а ширина — это число сэмплов. Зная алгоритм, никогда не перепутаешь x-ось
  с временем. Profiling интегрируется с SLO burn, deploy diff и capacity planning.
title: Как flame graph строится из сэмплов и как использовать его в production
track: observability
unit: 07-profiling
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DiagramFrame from "~/components/diagram/DiagramFrame.astro";

<Hook>
Старший инженер на конференции спрашивает: «Какая функция выполняется раньше этой на flame graph?» Он указывает на два соседних фрейма на одном уровне. Ответ: ни один — x-ось алфавитная. Если этого не знать, потеряешь часы на неверную гипотезу.
</Hook>

<Crux>x-ось flame graph — алфавитная группировка, не время. Ширина = число сэмплов. Знание алгоритма устраняет самую дорогую ошибку прочтения.</Crux>

<Explanation>

## Как flame graph строится из сэмплов

Каждый сэмпл — список имён функций от leaf (выполняется сейчас) до root (точка входа программы). После окна профилирования:

1. Собираются все сэмплы.
2. Одинаковые стеки схлопываются в одну колонку — их счётчики суммируются, делая колонку шире.
3. Для рендеринга все уникальные стеки сортируются **алфавитно по корневой функции** — так один и тот же родитель группирует дочерние элементы рядом.
4. Для каждого уровня прямоугольники рисуются с шириной, пропорциональной счётчику.

Читается сверху вниз: выбери leaf-фрейм (верх), проверь его ширину, спустись вниз, чтобы увидеть, кто его вызывает.

## Самая дорогая ошибка прочтения

Позиция по x-оси — **алфавитный порядок полного пути стека**, не время, не порядок вызовов. Фрейм, стоящий левее другого на том же уровне, не говорит ничего о том, какой из них выполнился первым. Он говорит только о том, что путь его стека раньше по алфавиту.

Это самая распространённая ошибка инженеров при первом чтении flame graph. Если видишь два широких фрейма рядом и думаешь «сначала A, потом B», ты делаешь ложный вывод. Оба могут вызываться одним родителем в разные моменты, из несвязанных code path, из распараллеленного кода.

Если нужен порядок времени — правильный инструмент это trace view (timeline span'ов в стиле Gantt). Flame graph отвечает «что» горячее; traces отвечают «когда» в запросе выполнялся каждый шаг.

Поэтому чтение строго вертикальное: ищи **самое широкое плато**, потому что ширина — это число сэмплов, и широкий фрейм — это место, где CPU реально провёл время; это и есть цель оптимизации. **Высокая, но узкая башня** — это глубокая цепочка вызовов, которая редко бывает на CPU; это red herring, каким бы драматичным ни казался её рост. **Плоская вершина** широкого стека — это leaf, который реально делает работу, ведь над ним нет ничего шире.

<DiagramFrame
  label="Flame graph, читаемый по форме: широкое горячее плато (compress -> deflate), отмеченное как цель оптимизации, и высокая, но тонкая башня (фоновый flush -> syscall), отмеченная как дешёвый red herring."
  hue="var(--d-systems)"
  caption="Читай вертикально, а не слева направо. Ширина — это число сэмплов: широкое плато compress -> deflate — там реально тратится CPU-время (оптимизируй leaf с плоской вершиной). Высокая башня фонового flush глубокая, но тонкая — четыре фрейма в высоту, почти без сэмплов — red herring."
>
  <svg viewBox="0 0 560 220" width="560" height="220">
    <text x="20" y="16" font-size="11" font-family="var(--font-body)" fill="var(--muted)">leaf (верх) -&gt; root (низ). ширина = число сэмплов</text>


    <rect x="20" y="170" width="520" height="24" rx="4" fill="var(--card-2)" stroke="var(--hairline-strong)" stroke-width="0.8" />
    <text x="280" y="186" text-anchor="middle" font-size="11" font-family="var(--font-mono)" fill="var(--ink)">main</text>


    <rect x="20" y="140" width="320" height="24" rx="4" fill="var(--card-2)" stroke="var(--hairline-strong)" stroke-width="0.8" />
    <text x="180" y="156" text-anchor="middle" font-size="11" font-family="var(--font-mono)" fill="var(--ink)">handleRequest</text>
    <rect x="350" y="140" width="70" height="24" rx="4" fill="var(--card-2)" stroke="var(--hairline-strong)" stroke-width="0.8" />
    <text x="385" y="156" text-anchor="middle" font-size="9.5" font-family="var(--font-mono)" fill="var(--muted)">bgFlush</text>


    <rect x="20" y="110" width="120" height="24" rx="4" fill="var(--card-2)" stroke="var(--hairline-strong)" stroke-width="0.8" />
    <text x="80" y="126" text-anchor="middle" font-size="10" font-family="var(--font-mono)" fill="var(--ink)">serialize</text>
    <rect x="150" y="110" width="190" height="24" rx="4" fill="var(--accent)" stroke="var(--accent)" stroke-width="0.8" />
    <text x="245" y="126" text-anchor="middle" font-size="11" font-family="var(--font-mono)" fill="var(--card)">compress</text>
    <rect x="350" y="110" width="70" height="24" rx="4" fill="var(--card-2)" stroke="var(--hairline-strong)" stroke-width="0.8" />
    <text x="385" y="126" text-anchor="middle" font-size="9.5" font-family="var(--font-mono)" fill="var(--muted)">fsync</text>


    <rect x="150" y="80" width="190" height="24" rx="4" fill="var(--accent)" stroke="var(--accent)" stroke-width="0.8" />
    <text x="245" y="96" text-anchor="middle" font-size="11" font-family="var(--font-mono)" fill="var(--card)">deflate (leaf)</text>
    <rect x="350" y="80" width="70" height="24" rx="4" fill="var(--card-2)" stroke="var(--hairline-strong)" stroke-width="0.8" />
    <text x="385" y="96" text-anchor="middle" font-size="9.5" font-family="var(--font-mono)" fill="var(--muted)">writev</text>


    <rect x="350" y="50" width="70" height="24" rx="4" fill="var(--card-2)" stroke="var(--hairline-strong)" stroke-width="0.8" />
    <text x="385" y="66" text-anchor="middle" font-size="9.5" font-family="var(--font-mono)" fill="var(--muted)">syscall</text>


    <text x="245" y="44" text-anchor="middle" font-size="10" font-family="var(--font-body)" fill="var(--accent)">широкое плато = горячо: оптимизируй здесь</text>
    <line x1="245" y1="48" x2="245" y2="78" stroke="var(--accent)" stroke-width="1" stroke-dasharray="3 3" />
    <text x="470" y="62" font-size="10" font-family="var(--font-body)" fill="var(--muted)">высокая + тонкая башня</text>
    <text x="470" y="78" font-size="10" font-family="var(--font-body)" fill="var(--muted)">= глубоко, но дёшево</text>
    <text x="470" y="94" font-size="10" font-family="var(--font-body)" fill="var(--muted)">(red herring)</text>
  </svg>
</DiagramFrame>

## Рабочие процессы с профилями в production

**SLO burn drilldown:**
Срабатывает SLO alert → кликаешь ссылку → временной диапазон предфильтрован по burn-окну → CPU + off-CPU flame graph рядом → определяешь изменившуюся функцию → привязываешь к деплою. Менее 90 секунд от пейджера до git blame для любого инцидента, где баг работал на CPU.

**Обнаружение регрессий деплоя:**
Захватываешь профиль до и после деплоя под сравнимой нагрузкой. Делаешь diff: **дифференциальный flame graph** окрашивает фреймы по относительному изменению — красный для выросших фреймов, синий для сократившихся, белый для неизменных. Новые широкие красные фреймы, которых не было до деплоя — это регрессия. Production-grade continuous-profile бэкенды (Pyroscope, Datadog) включают это из коробки: «compare versions» выбирает два коммита или временных окна и рендерит diff.

**Profile-as-data: запросы за пределами flame graph:**

Профили — это временные ряды сэмплов стека; бэкенды всё чаще позволяют делать к ним запросы как к базе данных:
- «Top-10 функций по self-CPU по всем сервисам за последний час» → capacity planning.
- «Найти все профили, где функция X входит в top-5» → оценка влияния перед удалением медленной библиотеки.
- «Сгруппировать flame graph'ы по Kubernetes-ноде» → найти горячие ноды.
- «Alert при появлении новой функции в top-5 после деплоя» → автоматическое обнаружение регрессии.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Рабочий процесс</th>
        <th class="border border-border px-3 py-2 text-left">Триггер</th>
        <th class="border border-border px-3 py-2 text-left">Действие</th>
        <th class="border border-border px-3 py-2 text-left">Результат</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">SLO burn drill</td>
        <td class="border border-border px-3 py-2">Alert</td>
        <td class="border border-border px-3 py-2">Фильтр профиля по burn-окну</td>
        <td class="border border-border px-3 py-2">Горячая функция за &lt;90 с</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Регрессия деплоя</td>
        <td class="border border-border px-3 py-2">Деплой</td>
        <td class="border border-border px-3 py-2">Diff pre vs post профилей</td>
        <td class="border border-border px-3 py-2">Новый горячий фрейм выделен красным</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Capacity planning</td>
        <td class="border border-border px-3 py-2">Квартально</td>
        <td class="border border-border px-3 py-2">Top-N функций по всему флоту</td>
        <td class="border border-border px-3 py-2">Кандидаты оптимизации в рейтинге</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Trace-id drill</td>
        <td class="border border-border px-3 py-2">Медленный span в trace</td>
        <td class="border border-border px-3 py-2">Фильтр профиля по trace-id</td>
        <td class="border border-border px-3 py-2">Flame graph для этого запроса</td>
      </tr>
    </tbody>
  </table>
</div>

<Inset kind="why" lang="ru">
**Почему дифференциальные профили находят то, что пропускают дашборды.** Стандартный дашборд latency показывает рост p99 после деплоя. Но новый code path на 5% медленнее или на 50%? И какая функция изменилась? Дашборд не скажет. Дифференциальный профиль отвечает на оба вопроса: ширина красных фреймов — это серьёзность проблемы; имя фрейма и его родитель — это местонахождение. Команды, делающие автоматический diff профилей на каждый деплой, ловят регрессии за минуты, а не после жалобы клиента.
</Inset>

<Quiz
  id="05-reading-flame-graphs-quiz-1"
  lessonSlug="05-reading-flame-graphs"
  lang="ru"
  question="Инженер читает flame graph и делает вывод, что функция A выполняется раньше B, потому что A стоит левее B на одном уровне. В чём ошибка?"
  choices={[
    { label: "x-ось flame graph алфавитная (сгруппированная по родителю), не временная. Фрейм левее другого не говорит ничего о порядке вызовов — только о лексикографическом порядке полных путей стека", correct: true },
    { label: "Flame graph перевёрнут неправильно", misconception: "Не перевёрнут; x-ось принципиально не является временной шкалой." },
    { label: "Надо использовать другой profiler для порядка по времени", misconception: "Ни одна реализация flame graph не использует x-ось как время; это trace view / flame chart." },
    { label: "Flame graph'ы сортируются по времени в некоторых реализациях", misconception: "Не по спецификации; flame graph Брендана Грегга всегда алфавитный по стеку." },
  ]}
/>

<Quiz
  id="05-reading-flame-graphs-quiz-2"
  lessonSlug="05-reading-flame-graphs"
  lang="ru"
  question="Только что прошёл деплой. Команда хочет узнать, было ли отклонение в CPU-производительности. Какой рабочий процесс профилирования наиболее прямой?"
  choices={[
    { label: "Дифференциальный flame graph: захвати профили до и после деплоя под сравнимой нагрузкой, сделай diff — красные фреймы указывают на новые CPU hotspot'ы", correct: true },
    { label: "Посмотреть на p99 latency chart", misconception: "Charts latency показывают, что что-то изменилось, но не какая функция и насколько." },
    { label: "Увеличить verbosity логов и поискать ошибки", misconception: "Логи показывают ошибки, не CPU hotspot'ы. Профили — правильный инструмент." },
    { label: "Захватить профиль только для новой версии", misconception: "Один профиль показывает, что горячее, но не что изменилось относительно прошлого. Нужен diff с предыдущей версией." },
  ]}
/>

</Explanation>

<KeyTakeaway>Flame graph строится схлопыванием одинаковых стеков, алфавитной сортировкой по корневой функции и отрисовкой прямоугольников с шириной, пропорциональной числу сэмплов. x-ось — не время, а лексикографическая группировка. Дифференциальные профили ловят регрессии деплоя прежде, чем это заметят клиенты. Профили — это данные: запрашивай их как базу данных для capacity planning и анализа влияния.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="05-reading-flame-graphs-retrieval"
  lang="ru"
  questions={[
    { q: "Объясни, почему x-ось flame graph алфавитная, а не временная, и какой инструмент использовать, если нужен порядок по времени.", a: "Алфавитная сортировка (группировка по родительской функции, затем сортировка по алфавиту среди дочерних) позволяет children одного родителя находиться рядом. Это сохраняет визуальную иерархию вызовов при вертикальном сканировании — видишь все пути через родителя сгруппированными, а не разбросанными. Временной порядок разбросал бы children по всему графу и сделал бы структуру вызовов нечитаемой. Ширина фрейма пропорциональна частоте появления в наборе сэмплов, независимо от того, когда эти сэмплы были сделаны. Для порядка по времени правильная визуализация — timeline trace (span view в стиле Gantt). Flame graph отвечает «что» ест CPU; traces отвечают «когда» выполнялся каждый шаг. Оба нужны; они не взаимозаменяемы." },
    { q: "Что такое дифференциальный flame graph и какую проблему он решает?", a: "Дифференциальный flame graph накладывает два профиля — как правило, до и после деплоя — и окрашивает каждый фрейм по относительному изменению: красный для выросших по числу сэмплов, синий для сократившихся, белый для неизменных. Это напрямую отвечает на вопрос «что изменилось в CPU-использовании после этого деплоя?» вместо «что сейчас горячее?». Ловит регрессии, невидимые в charts latency, потому что они слишком малы для влияния на p99, но достаточно значительны для истощения CPU-бюджета в масштабе. Production profiling бэкенды (Pyroscope compare-versions, Datadog) поддерживают это нативно. CI-интеграция: запускай profile diff как pre-merge проверку, падай, если новая функция появляется в top-5 по self-CPU. Это ловит performance-регрессии до выхода в production." },
    { q: "Назови три способа запрашивать профили как данные (помимо простого просмотра flame graph).", a: "1. Top-N по self-CPU по всем сервисам и временному диапазону — для capacity planning: найти top-10 функций, потребляющих CPU флота, и расставить приоритеты оптимизации. 2. Оценка влияния: «найти все профили, где функция X входит в top-5» — перед удалением или рефакторингом заведомо медленной функции, количественно оценить, какие сервисы затронуты. 3. Alert на регрессию деплоя: «alert при появлении новой функции в top-5 после деплоя» — автоматическое обнаружение регрессии без ожидания жалобы клиента или срабатывания SLO alert. Дополнительно: группировка flame graph'ов по Kubernetes-ноде для поиска горячих нод, или diff профилей из разных временных окон для обнаружения медленно накапливающихся регрессий." },
  ]}
/>

<Recap lang="ru">
Flame graph строится агрегацией одинаковых стеков, их алфавитной сортировкой по корневой функции и отрисовкой прямоугольников с шириной, пропорциональной числу сэмплов. x-ось кодирует алфавитную группировку — никогда не время, — поэтому горизонтальная позиция фрейма не говорит ничего о порядке выполнения. Для порядка по времени используй timeline trace. Дифференциальные flame graph'ы накладывают два профиля (до и после деплоя) и окрашивают фреймы по изменению; это самый прямой способ поймать CPU-регрессии в момент деплоя. Профили — данные временного ряда: современные бэкенды поддерживают запросы по сервисам, diff по версии, группировку по ноде и alert на изменения формы, превращая profiling из инструмента отладки в непрерывный сигнал качества.
</Recap>
