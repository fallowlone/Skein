infographic chart-pie-donut-plain-text
data
  title Запрос 500 мс: что видит CPU-профиль
  desc CPU profiling захватывает только on-CPU время — ожидание невидимо
  values
    - label On-CPU вычисления — видны CPU-профилю (20 мс)
      value 20
    - label Off-CPU ожидание БД — невидимо CPU-профилю (480 мс)
      value 480
theme
  palette #6366f1 #f43f5e
