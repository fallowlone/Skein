---
slug: 03-profile-types
lang: ru
track: observability
unit: 07-profiling
order: 3
title: "Типы профилей: CPU, память, off-CPU, mutex — какой когда брать"
summary: "CPU profiling видит только работающий код; off-CPU, block и mutex профили покрывают 96% запроса, проведённые в ожидании — для полной диагностики нужны все четыре."
estMin: 15
status: ready
lessonType: topic
level: middle
prereqs: ["observability/07-profiling/02-sampling-vs-instrumenting"]
deepensInto: []
spiral: ["latency"]
concepts: ["cpu-profile", "heap-profile", "off-cpu-profile", "block-profile", "mutex-profile", "allocation-profile"]
sources:
  - https://pkg.go.dev/runtime/pprof
  - https://www.brendangregg.com/offcpuanalysis.html
  - https://github.com/grafana/pyroscope
---

import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import Misconception from "~/components/layout/Misconception.astro";

<Hook>
Span занимает 500 мс. Открываешь CPU-профиль — сервис использовал 20 мс CPU. Куда ушли остальные 480 мс? CPU profiling слеп к ожиданию. Если остановиться здесь — ответ не найти никогда.
</Hook>

<Crux>CPU профиль = только работающий код. Off-CPU + block + mutex профили покрывают время ожидания. Вместе — полный жизненный цикл запроса.</Crux>

<Explanation>

## CPU профили: что видят и что упускают

CPU profiling сэмплирует call stack, пока поток **на CPU** — выполняет инструкции. Запрос, проведший 20 мс в вычислениях и 480 мс ожидая DB-запроса, покажет 20 мс в CPU-профиле, а 480 мс останутся невидимыми.

Это важнейшее ограничение profiling: CPU-профили видят только функции, потребляющие процессор. Всё, чего ждёт программа — I/O, сеть, локи, планировщик — находится off-CPU и невидимо для CPU-profiler'а.

<Misconception id="mc-mean-hides">
  <p><strong>«CPU profiling показывает всё, что замедляет мой запрос.»</strong> Нет. CPU profiling видит только фактически выполняющиеся функции. Запрос, ожидающий 400 мс на Postgres, использует ноль CPU во время ожидания — он невидим для CPU-профиля. Для off-CPU-части используй off-CPU профиль, block профиль или trace span'ы.</p>
</Misconception>

## Memory и allocation профили

Heap profiler'ы сэмплируют аллокации, а не CPU. Heap profile в Go сэмплирует одну аллокацию на ~512 КиБ (Poisson) и записывает стек при каждом сэмпле. Результат — flame graph, где ширина это **выделенные байты**, а не CPU-время. Это находит memory-горячки: функция, выделяющая 100 МБ/с, выглядит широкой.

Обнаружение memory leak с heap profile:
1. Взять heap профиль.
2. Подождать 30-60 минут.
3. Взять ещё один heap профиль.
4. Сравнить (`go tool pprof -base baseline.heap current.heap`).
5. Функции, чья аллокация выросла, — источник утечки.

Allocation профили захватывают короткоживущие аллокации, которые GC возвращает; heap профили — снимки живой памяти. JVM-эквиваленты: async-profiler с `-e alloc`, JFR allocation events. Python: `tracemalloc`, `memray`.

## Off-CPU профили

Работа Brendan Gregg по off-CPU analysis (2013) определила пробел: CPU-профили пропускают всё, чего ждёт процесс. eBPF-реализации цепляются за события переключения контекста kernel-планировщика. Когда планировщик снимает поток с CPU (блокировка на I/O, sleep, ожидание лока), kernel захватывает стек потока. Этот стек — место, где началось ожидание. Когда поток возвращается, прошедшее время атрибутируется этому стеку.

Off-CPU flame graph показывает **время ожидания** точно так же, как CPU flame graph показывает время выполнения. Для I/O-bound сервиса off-CPU профиль — единственный, который хоть что-то объясняет: CPU-профиль просто говорит «сервис простаивал».

## Block и mutex профили

**Block profile** (Go: `runtime.SetBlockProfileRate`): время ожидания на sync-примитивах — каналах, condition variable'ах, WaitGroup'ах. Более фокусированный, чем off-CPU, так как нацелен на language-level синхронизацию.

**Mutex profile** (`runtime.SetMutexProfileFraction`): конкуренция за локи конкретно. Сообщает, какой код держал лок, пока другие его ждали, — атрибутировано в момент unlock'а.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Тип профиля</th>
        <th class="border border-border px-3 py-2 text-left">Ширина означает</th>
        <th class="border border-border px-3 py-2 text-left">Когда брать</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">CPU</td>
        <td class="border border-border px-3 py-2">Потреблённое CPU-время</td>
        <td class="border border-border px-3 py-2">Высокое CPU, медленный ответ</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Heap / Allocation</td>
        <td class="border border-border px-3 py-2">Выделенные байты</td>
        <td class="border border-border px-3 py-2">GC pressure, OOM, рост памяти</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Off-CPU</td>
        <td class="border border-border px-3 py-2">Время ожидания (все причины)</td>
        <td class="border border-border px-3 py-2">Медленный запрос, но низкий CPU</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Block</td>
        <td class="border border-border px-3 py-2">Время на sync-примитивах</td>
        <td class="border border-border px-3 py-2">Подозреваемая конкуренция Go goroutine</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Mutex</td>
        <td class="border border-border px-3 py-2">Время конкуренции за лок</td>
        <td class="border border-border px-3 py-2">Подозреваемая высокая конкуренция за лок</td>
      </tr>
    </tbody>
  </table>
</div>

## Выбор профиля по соотношению CPU/wall-time

Диагностический shortcut: смотри CPU-время vs wall-clock-время медленного запроса.

- CPU/wall ≈ 100%: вычислительное узкое место — CPU профиль.
- CPU/wall &lt; 30%: узкое место off-CPU — off-CPU/block профиль или trace span'ы.
- Память растёт стабильно: heap/allocation профиль.
- Потоки конкурируют за лок: mutex профиль.

Java-сервис с GC-thrashing — классический allocation-профиль случай. Симптом: высокая скорость аллокации при частых old-gen GC. Allocation flame graph покажет самый широкий frame как функцию, аллоцирующую с наибольшей скоростью — часто string concatenation в logging-коде без параметризованного форматирования.

<Quiz
  id="03-profile-types-quiz-1"
  lessonSlug="03-profile-types"
  lang="ru"
  question="Запрос проводит 50 мс на CPU и 450 мс ожидая DB-запрос. Какой тип профиля покажет ожидание БД?"
  choices={[
    { label: "Off-CPU профиль (или block профиль, или trace span — всё, что захватывает время ожидания)", correct: true },
    { label: "CPU профиль", misconception: "CPU профиль видит только работающий код; ожидание для него невидимо." },
    { label: "Heap профиль", misconception: "Heap показывает аллокации, а не время ожидания." },
    { label: "Mutex профиль", misconception: "Mutex показывает конкуренцию за лок, а не ожидание DB-запроса." },
  ]}
/>

<Quiz
  id="03-profile-types-quiz-2"
  lessonSlug="03-profile-types"
  lang="ru"
  question="Java-сервис OOM'ится на определённых endpoint'ах. CPU профиль выглядит нормально. Какой тип профиля взять?"
  choices={[
    { label: "Allocation профиль (async-profiler -e alloc, или JFR allocation events) — найти стек, аллоцирующий больше всего байт в секунду", correct: true },
    { label: "Off-CPU профиль", misconception: "Off-CPU показывает время ожидания, а не паттерны аллокации памяти." },
    { label: "CPU профиль с более длинным capture", misconception: "Нормальный CPU профиль означает, что проблема не вычислительная — OOM почти наверняка в аллокации." },
    { label: "Mutex профиль", misconception: "Mutex contention может снизить производительность, но не является типичной причиной OOM." },
  ]}
/>

</Explanation>

<KeyTakeaway>CPU profiling захватывает только выполняющийся код — ожидание невидимо. Для медленного, но CPU-лёгкого запроса берёшь off-CPU, block или mutex профиль. Для memory pressure и OOM — heap или allocation профиль. Соотношение CPU/wall-time говорит, какой тип брать первым.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="03-profile-types-retrieval"
  lang="ru"
  questions={[
    { q: "Почему heap profiler в Go сэмплирует одну аллокацию на ~512 КиБ вместо записи каждой?", a: "Запись каждой аллокации стоила бы CPU пропорционально скорости аллокации — для сервиса, аллоцирующего 100 ГБ/с, это 200 млн stack walk'ов в секунду, совершенно нежизнеспособно. Sampling при 1 на 512 КиБ снижает стоимость примерно в 5 порядков, сохраняя достаточно сэмплов для определения горячих мест аллокации: функция, аллоцирующая 100 МБ/с, попадёт в ~200 сэмплов в секунду — достаточно для статистической оценки. Распределение Poisson означает независимую вероятность для каждого КиБ, поэтому взвешенный по сэмплам агрегат несмещён — умноженный на sample rate, он отражает истинное распределение." },
    { q: "Объясни, почему CPU flame graph недостаточен для диагностики I/O-bound сервиса.", a: "CPU profiling захватывает только то, что выполняется на CPU. I/O-bound сервис проводит большую часть wall-clock-времени в ожидании — disk read, DB-ответов, сетевых пакетов, захватов лока. Во время этого ожидания поток снят с расписания; он потребляет ноль CPU; profiler его не видит. Flame graph такого сервиса показывает ~5% CPU и 95% времени запроса невидимыми. Три ортогональных типа заполняют пробел: off-CPU профиль (захватывает стек в момент, когда поток снимается с CPU, и атрибутирует время ожидания ему), block профиль (время на sync-примитивах), mutex профиль (конкуренция за локи). Паттерн: начинай с трейса — видеть куда ушло wall-clock-время, затем CPU профиль для вычислений, затем off-CPU или block для ожидания." },
    { q: "Опиши процедуру обнаружения memory leak с heap профилями.", a: "Взять baseline heap профиль, подождать 30-60 минут production трафика, взять ещё один heap профиль, сравнить командой go tool pprof -base baseline.heap current.heap. Функции с выросшей cumulative аллокацией указывают, где накапливаются живые объекты. Ключ в том, что heap профили показывают живую память (объекты, не собранные GC), а не просто итоговые аллокации — поэтому рост живой памяти — реальная утечка. Подтверждай повторением diff'а через ещё один час: если те же функции продолжают расти, источник утечки найден. Применяешь фикс, захватываешь свежий heap профиль после — верифицируешь, что рост остановился." },
  ]}
/>

<Recap lang="ru">
Четыре типа профилей покрывают полный жизненный цикл запроса: CPU (что выполняется), heap или allocation (что аллоцируется), off-CPU (что ждёт I/O или планировщика), block или mutex (что ждёт локов). CPU profiling видит только код, активно находящийся на процессоре — запрос, ждущий 480 мс DB-запрос, покажет в CPU профиле только 20 мс вычислений. Соотношение CPU/wall-time — диагностический сигнал: менее 30% означает, что узкое место off-CPU. Heap profiler в Go сэмплирует при 1 на 512 КиБ для affordable always-on memory profiling. Комбинация всех четырёх даёт полную картину; использование только CPU profiling для I/O-bound сервиса гарантирует нахождение не того узкого места.
</Recap>
