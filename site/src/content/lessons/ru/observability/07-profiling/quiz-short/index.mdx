---
concepts:
- sampling-vs-instrumenting
- profile-types
- flame-graph-axis
- continuous-profiling
- ebpf-symbolization
- sampling-blind-spots
estMin: 14
lang: ru
level: senior
order: 8
prereqs: []
slug: quiz-short
sources:
- https://www.brendangregg.com/flamegraphs.html
- https://pkg.go.dev/runtime/pprof
- https://opentelemetry.io/docs/specs/otel/profiles/
status: ready
summary: "Промпты на свободное припоминание по всему юниту profiling. Сначала ответь своими словами, затем открой модельный ответ и сравни."
title: 'Profiling: тест на свободное припоминание'
track: observability
unit: 07-profiling
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";

<Hook>
Припоминание бьёт перечитывание. Для каждого промпта проговори или запиши полный ответ по памяти, прежде чем открыть модельный — именно усилие припоминания закрепляет материал.
</Hook>

<Goal lang="ru">
Восстанови ключевые механизмы юнита — sampling против инструментирования, карту типов профилей, ось flame graph, continuous profiling, символизацию eBPF и слепые зоны sampling — не подглядывая в уроки.
</Goal>

<RetrievalDrawer
  client:visible
  id="07-profiling-short-retrieval"
  lang="ru"
  questions={[
    { q: "Почему sampling profiler жизнеспособен для always-on профайлинга в production, а instrumentation profiler — нет?", a: "Инструментирование оборачивает каждый вход и выход функции кодом тайминга, поэтому его стоимость растёт с частотой вызовов — сервис на 5 миллионах вызовов/с платит 5 миллионов измерений/с, добавляя 20-100% overhead под нагрузкой. Sampling запускает внешний таймер N раз в секунду и обходит текущий стек, поэтому стоимость фиксирована — N обходов стека/с независимо от числа вызовов между сэмплами. На 100 Hz и ~5-20 микросекунд на обход overhead ~0.5-2%, а полный пайплайн continuous profiler (батчинг, отправка) выходит на 2-5%. Статистическое свойство, делающее это рабочим: функция, использующая X% CPU, появляется в X% сэмплов, так что ты восстанавливаешь долю CPU-времени, не трогая каждый вызов. Sampling меняет точность на ограниченный, не зависящий от частоты вызовов overhead — ровно тот размен, которого требует production." },
    { q: "Назови основные типы профилей и один вопрос, на который отвечает каждый.", a: "CPU profile: какие функции на процессоре — отвечает на 'что жжёт CPU'. Heap/allocation profile: какие стеки аллоцируют больше всего байт (heap = живая память, allocation = весь оборот включая собранное GC) — отвечает на 'что создаёт давление GC или OOM'. Off-CPU profile: где поток проводит время снятым с CPU, снимается на sched_switch — отвечает на 'чего ждёт запрос' (I/O, сон, планирование). Block profile: время на примитивах синхронизации (каналы, WaitGroups). Mutex profile: именно конкуренция за лок, приписывается на момент unlock. Диагностический шорткат — отношение CPU/wall-clock: близко к 100% — вычислительное узкое место (CPU profile); ниже ~30% — узкое место off-CPU (off-CPU/block профиль или trace-спаны); стабильный рост памяти — heap/allocation профиль." },
    { q: "Что на самом деле кодирует ось x flame graph и какое самое дорогое неверное прочтение?", a: "Ось x — это алфавитная группировка полных путей стека: одинаковые стеки схлопываются в одну колонку, чья ширина — число сэмплов, а уникальные стеки сортируются алфавитно по корню, так что дети одного родителя стоят рядом. Это не время и не порядок вызовов. Самое дорогое неверное прочтение — трактовать левее-чем как 'выполняется раньше': горизонтальная позиция фрейма говорит лишь о том, что его путь стека сортируется лексикографически раньше, и ничего о порядке выполнения. Два соседних фрейма могут не иметь общего родителя, могут быть несвязанными путями кода или выполняться параллельно. Ширина (доля сэмплов) — единственная величина, что-то значащая о стоимости. Когда реально нужен порядок выполнения, используй trace-таймлайн (спаны в стиле Gantt); flame graph отвечает на 'что' горячо, trace — на 'когда' выполнялся каждый шаг." },
    { q: "Что continuous profiling даёт операционно, чего не может on-demand профайлинг, и сколько это примерно стоит?", a: "On-demand профайлинг требует твоего присутствия, пока проблема происходит: зайти по SSH, воспроизвести под нагрузкой, снять, проанализировать. Многие production-проблемы интермиттентны и саморазрешаются раньше, чем ты подключишься, так что улики исчезли. Continuous profiling запускает агент на каждом хосте, сэмплирующий ~100 раз/с, батчит и отправляет сжатые профили каждые 10-15 секунд в бэкенд, индексированный по сервису, хосту и времени. Когда SLO горит в 2 ночи, flame graph горящих минут уже сохранён — воспроизводить не нужно; on-call фильтрует по окну прожига и читает его сразу. Стоимость ~2-5% CPU и примерно 50-200 МБ/сервис/день (около 1.5-6 ГБ/месяц), ещё меньше за счёт дедупликации символов. Это превращает многочасовое расследование в поиск корневой причины менее чем за 90 секунд." },
    { q: "Почему eBPF-профайлер чисто символизирует Go и Rust, но показывает [unknown] фреймы для Python и частичные для JVM?", a: "eBPF-профайлеры читают стеки со стороны ядра через perf_event_open плюс BPF-проба, снимая сырые адреса. Go и Rust компилируются в нативный код с frame pointers или DWARF-разматкой, так что ядро идёт по цепочке фреймов и сопоставляет адреса именам функций. CPython исполняет код как байткод внутри своего C-цикла интерпретатора (PyEval_EvalFrameDefault); снятые адреса указывают в этот C-цикл, а не в исходник Python, поэтому без CPython-aware разматки (обход связного списка PyFrameObject) они показываются как [unknown]. JIT-рантаймы (JVM, V8) генерируют машинный код в рантайме, чьи адреса не отображены статически; им нужны perf maps (/tmp/perf-PID.map) или нативные хуки вроде AsyncGetCallTrace, а maps могут отставать от рекомпиляций — отсюда частичное покрытие. Фикс для интерпретируемых/JIT рантаймов — language-aware профайлер (py-spy, async-profiler) или runtime-aware eBPF-режим." },
    { q: "Назови две слепые зоны sampling, которые senior-инженер обязан помнить, прежде чем доверять отсутствию пика на flame graph.", a: "Первая — очень короткие горячие функции: функция, каждый вызов которой завершается быстрее интервала сэмплинга (10 мс на 100 Hz), может систематически недосэмплироваться или быть пропущена вовсе, даже при высоком суммарном CPU — функция, вызываемая 1 миллион раз/с по 5 микросекунд, редко ловится в момент исполнения. Митигация: более длинные окна захвата, выше sample rate или точечное инструментирование этого пути. Вторая — статистический шум при малом числе сэмплов: 100 Hz за 10 секунд дают ~1000 сэмплов, так что функция на 1% CPU появляется в ~10 сэмплах со стандартной ошибкой около 30% — достаточно заметить, недостаточно для точных оценок или обнаружения регрессий ниже 1%, для которых нужны захваты 60-300 секунд или differential profile. (Связанное: дисбаланс NUMA/CPU может прятать hotspot на одной ноде за средним по флоту — используй per-CPU виды.) Урок: отсутствие пика не доказывает отсутствие узкого места." },
  ]}
/>

<Recap lang="ru">
Если ты смог восстановить каждый ответ по памяти, ты держишь хребет юнита: sampling покупает ограниченный overhead ценой точности; каждый тип профиля отвечает на один вопрос, а отношение CPU/wall ведёт тебя к нужному; ось x flame graph алфавитная, а не время; continuous profiling делает flame graph инцидента пред-сохранённым на 2-5%; символизация eBPF зависит от рантайма; а слепые зоны sampling (hot-пути короче интервала, шум при малых счётчиках) означают, что пропущенный пик — никогда не доказательство пропущенного узкого места.
</Recap>
