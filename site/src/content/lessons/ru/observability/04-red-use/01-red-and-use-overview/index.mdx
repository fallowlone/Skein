---
slug: 01-red-and-use-overview
lang: ru
track: observability
unit: 04-red-use
order: 1
title: "RED и USE: два чек-листа, одна дисциплина триажа"
summary: "RED (Rate, Errors, Duration) описывает, что почувствовал пользователь. USE (Utilization, Saturation, Errors) описывает, какой ресурс это вызвал. Оба чек-листа по порядку — первый рефлекс senior-инженера, когда срабатывает пейджер."
estMin: 10
status: ready
lessonType: topic
level: junior
prereqs:
  - "observability/03-otel/01-what-is-otel"
deepensInto: []
spiral: ["latency"]
concepts: ["red-method", "use-method", "incident-triage", "golden-signals"]
sources:
  - https://www.brendangregg.com/usemethod.html
  - https://grafana.com/blog/the-red-method-how-to-instrument-your-services/
  - https://sre.google/sre-book/monitoring-distributed-systems/
---

import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import MetaphorComplete from "~/components/pedagogy/MetaphorComplete.astro";

<Hook>
Пейджер сработал. p99-задержка checkout-сервиса подскочила с 80 мс до 1.2 с. У тебя секунды, чтобы найти нужный дашборд, пока incident commander уже спрашивает обновление. Открыть все панели — ошибка. Правильное действие — два коротких чек-листа.
</Hook>

<Crux>RED = Rate, Errors, Duration (сервисы). USE = Utilization, Saturation, Errors (ресурсы). Два чек-листа, без споров.</Crux>

<Explanation>

## Что такое RED

Tom Wilkie представил RED на London Prometheus meetup в 2015. RED описывает сервис с точки зрения **вызывающего**:

- **Rate** — запросов в секунду, поступающих в сервис.
- **Errors** — упавших запросов в секунду (HTTP 5xx, gRPC non-OK, таймауты).
- **Duration** — распределение задержки (p50 / p95 / p99) запросов, которые завершились.

Если RED болеет — болеет пользователь. Rate говорит, нормален ли трафик. Errors говорят, падают ли запросы. Duration говорит, медленны ли выжившие.

## Что такое USE

Brendan Gregg назвал USE в 2012 как чек-лист экстренной диагностики производительности. USE описывает **каждый ресурс** (CPU, память, диск, сеть, локи, thread pool'ы) с трёх сторон:

- **Utilization** — средний процент времени, когда ресурс был занят.
- **Saturation** — объём работы в очереди, которую ресурс ещё не успел обслужить (длина run queue, время ожидания).
- **Errors** — счётчик ошибочных событий на ресурсе (ECC-ошибки, disk EIO, NIC CRC, ENOSPC).

Если USE болеет на боксе — этот ресурс кандидат в причины RED-симптома.

## Послойная ментальная модель

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Метод</th>
        <th class="border border-border px-3 py-2 text-left">Что меряет</th>
        <th class="border border-border px-3 py-2 text-left">Отвечает на вопрос</th>
        <th class="border border-border px-3 py-2 text-left">Автор</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2 font-mono text-xs">RED</td>
        <td class="border border-border px-3 py-2">Сервисы (request-driven)</td>
        <td class="border border-border px-3 py-2">Пользователю плохо? Какой симптом?</td>
        <td class="border border-border px-3 py-2">Tom Wilkie, 2015</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2 font-mono text-xs">USE</td>
        <td class="border border-border px-3 py-2">Ресурсы (CPU, память, диск, …)</td>
        <td class="border border-border px-3 py-2">Какой ресурс это вызвал?</td>
        <td class="border border-border px-3 py-2">Brendan Gregg, 2012</td>
      </tr>
    </tbody>
  </table>
</div>

Ритм чтения в инциденте всегда **RED первым, USE вторым**:

1. Открой RED-дашборд затронутого сервиса.
2. Определи, какой из R / E / D аномален — это назовёт симптом.
3. Переключись на USE-дашборд ресурсов под этим сервисом.
4. Найди ресурс, где utilization или saturation скакнули — это назовёт кандидата в причины.
5. Опускайся в трейсы, логи или профили только после того, как RED и USE сузили область поиска.

## Метафора больницы

Представь больницу. RED — жизненные показатели пациента: пульс, давление, температура — измеряемые снаружи. USE — мониторинг каждой машины интенсивной терапии: расход кислорода, давление насоса, лампочки ошибок — измеряемые на самом оборудовании. Нужны оба. Если падают показатели пациента — действуешь быстро, но чтобы понять _почему_ — идёшь к машинам. Врачи, смотрящие только на одно, пропускают очевидные проблемы.

## Конкретный триаж

On-call-инженер Bea получает пейджер: p99-задержка checkout-сервиса подскочила с 80 мс до 1.2 с. RED-дашборд: Rate стабилен 400 req/s, Errors ниже 0.1%, Duration p99 в 15 раз хуже. Это RED-триаж — запросы приходят и в большинстве успешны, но медленно. Bea переключается на USE по машинам: CPU 92%, run queue прыгнул с 0 до 14. Машины CPU-saturated; треды стоят в очереди за циклами. Решение: scale out. Диагноз занял меньше минуты.

<Inset kind="why" lang="ru">
USE Saturation — самый диагностически ценный из трёх сигналов. Utilization говорит, насколько ресурс был занят в среднем — CPU на 100% нормален, если нет ожидающей работы (он просто успевает). Что важно — длина run queue. Диск на 80% utilization с queue depth 50 хуже, чем диск на 95% с queue depth 1, потому что очередь — это ведущий индикатор задержки: каждая задача в очереди платит queueing delay.
</Inset>

<Quiz
  id="01-red-and-use-overview-quiz-1"
  lessonSlug="01-red-and-use-overview"
  lang="ru"
  question="Мониторинг-дашборд показывает Rate, Errors и Duration сервиса. Что это за методология?"
  choices={[
    { label: "RED — Rate, Errors, Duration; измеряет сервис с точки зрения вызывающего", correct: true },
    { label: "USE — Utilization, Saturation, Errors", misconception: "USE покрывает здоровье ресурсов (CPU, память, диск), а не сервис-уровень." },
    { label: "Четыре golden signals", misconception: "Golden signals — это Latency, Traffic, Errors, Saturation; пересекаются с RED, но не то же самое." },
    { label: "PSI — Pressure Stall Information", misconception: "PSI — фича ядра Linux, сообщающая о давлении на ресурсы, а не сервисная методология." },
  ]}
/>

<Quiz
  id="01-red-and-use-overview-quiz-2"
  lessonSlug="01-red-and-use-overview"
  lang="ru"
  question="Метод USE: для каждого ресурса проверь Utilization, Saturation и Errors. Что такое 'saturation' в USE?"
  choices={[
    { label: "Процент использованной памяти", misconception: "Это utilization, а не saturation." },
    { label: "Количество ошибок за последнюю минуту", misconception: "Это буква E в USE, не S." },
    { label: "Объём дополнительной работы, поставленной в очередь к ресурсу — например, треды в run queue, ждущие CPU", correct: true },
    { label: "Достиг ли сервис 100% CPU", misconception: "100% CPU — это факт utilization; saturation — это очередь работы, ждущей за ним." },
  ]}
/>

<MetaphorComplete
  id="01-red-and-use-overview-metaphor-1"
  pieceSlug="01-red-and-use-overview"
  lang="ru"
  setup="Заполни пропуск: RED — это методология измерения _______, а USE — методология измерения ресурсов."
  accepted={["сервисов", "сервис", "request-driven сервисов", "сервисы"]}
  canonical="сервисов (особенно request-driven: HTTP API, RPC, очереди)"
  explanation="Tom Wilkie ввёл RED именно потому, что метод USE, заточенный под железо, не описывал, как сервис выглядит для своих вызывающих. RED меряет поток запросов; USE меряет пул ресурсов, который его обслуживает."
/>

<DragOrder
  id="01-red-and-use-overview-drag-1"
  lessonSlug="01-red-and-use-overview"
  lang="ru"
  prompt="Расставь шаги типового инцидент-ответа RED+USE по порядку:"
  items={[
    { id: "a", label: "Пейджер сработал — описан симптом (latency, ошибки, capacity)" },
    { id: "b", label: "Открой RED-дашборд затронутого сервиса" },
    { id: "c", label: "Определи, какой из R / E / D аномален — это назовёт симптом" },
    { id: "d", label: "Переключись на USE-дашборд ресурсов под этим сервисом" },
    { id: "e", label: "Найди ресурс, где utilization или saturation скакнули — это назовёт причину" },
    { id: "f", label: "Опускайся в трейсы, логи или профили только после того, как RED и USE сузили область" },
    { id: "g", label: "Применяй фикс (scale up, рестарт, throttle, rollback) и смотри, как RED возвращается к baseline" },
  ]}
  correctOrder={["a", "b", "c", "d", "e", "f", "g"]}
/>

</Explanation>

<KeyTakeaway>RED говорит, какой сервис болен и как (симптом). USE говорит, какой ресурс это вызвал (причина). Запускай RED первым, USE вторым — каждый раз — до того, как потянешься за логами, трейсами или профилями.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="01-red-and-use-overview-retrieval"
  lang="ru"
  questions={[
    { q: "В двух предложениях: почему запускать только RED или только USE обычно недостаточно для инцидент-ответа?", a: "Только RED скажет, что пользователь несчастен (медленно, ошибки, нет трафика), но не скажет почему — 100% CPU на боксе, насыщенный диск, утечка аллокатора памяти невидимы на границе сервиса. Только USE скажет, что ресурс под нагрузкой, но не скажет, замечает ли это пользователь — CPU 95% на batch-воркере норма; на пути запроса — инцидент." },
    { q: "Что означают три буквы USE и что каждая меряет?", a: "U = Utilization: средний процент времени, когда ресурс был занят. S = Saturation: объём работы в очереди, которую ресурс ещё не успел обслужить (длина run queue, время ожидания). E = Errors: счётчик ошибочных событий на ресурсе (disk EIO, NIC CRC, ECC)." },
    { q: "Кто ввёл RED и когда? Кто ввёл USE и когда?", a: "Tom Wilkie представил RED на London Prometheus meetup в 2015, работая в Weaveworks. Brendan Gregg назвал USE в 2012 как чек-лист экстренной диагностики производительности." },
  ]}
/>

<Recap lang="ru">
RED и USE — два коротких чек-листа с разницей в два года, которые вместе покрывают и сервис, к которому обращается пользователь, и ресурсы, на которых этот сервис стоит. RED (Rate, Errors, Duration) меряет поток запросов с точки зрения вызывающего — если RED болеет, болеет пользователь. USE (Utilization, Saturation, Errors) меряет каждый физический и логический ресурс с трёх сторон — если USE болеет на боксе, это кандидат в причины. Рефлекс senior-инженера в инциденте: RED первым (назвать симптом), USE вторым (найти причину), и всё остальное — логи, трейсы, профили — только после того, как эти два чек-листа сузили область. Saturation, а не utilization, — самое диагностически ценное измерение USE: очередь ожидающей работы — ведущий индикатор видимой пользователю задержки даже при умеренном среднем utilization.
</Recap>
