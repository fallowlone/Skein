---
concepts:
- red-method
- use-method
- golden-signals
- cardinality
- saturation
- slo-burn-rate
estMin: 13
lang: ru
level: senior
order: 7
prereqs: []
slug: quiz-choice
sources:
- https://www.brendangregg.com/usemethod.html
- https://grafana.com/blog/the-red-method-how-to-instrument-your-services/
- https://sre.google/sre-book/monitoring-distributed-systems/
status: ready
summary: "Синтез всего юнита в формате выбора ответа — триаж симптом против причины, saturation, агрегация гистограмм, cardinality и дисциплина уровней алертов."
title: 'RED и USE: тест с выбором ответа'
track: observability
unit: 04-red-use
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Шесть вопросов, проходящих сквозь весь юнит. Ни один не про заучивание определения — каждый отражает решение, которое вы принимаете прямо во время инцидента, когда пейджер орёт, а вам нужно выбрать правильный дашборд, правильный сигнал и правильный уровень алерта.
</Hook>

<Goal lang="ru">
Убедитесь, что вы соединяете два чек-листа в единую дисциплину триажа: RED называет симптом со стороны клиента, USE находит причину со стороны ядра, а вспомогательные решения — saturation, агрегация гистограмм, cardinality, уровни алертов — служат этому ритму чтения.
</Goal>

<Quiz
  id="04-red-use-choice-1"
  lessonSlug="04-red-use"
  lang="ru"
  question="Пейджер срабатывает на checkout p99 (80 мс до 1.2 с). RED показывает: Rate стабилен, Errors ниже 0.1%, Duration p99 в 15 раз хуже. Каков правильный следующий шаг и почему?"
  choices={[
    { label: "Переключиться на дашборд USE для ресурсов под сервисом. RED назвал симптом (медленные, но успешные запросы); USE находит причину — ищите ресурс, у которого utilization или saturation подскочили на той же временной шкале.", correct: true },
    { label: "Немедленно начать читать трейсы и логи, чтобы найти медленный запрос.", misconception: "Трейсы и логи — это шаг 5, после того как RED и USE сузили область поиска. Прыжок к поэлементным данным первым делом означает поиск во всём стоге сена вместо ресурса, на который указывает USE." },
    { label: "Откатить последний деплой — 15-кратный скачок латентности всегда плохой релиз.", misconception: "RED не указал на деплой. Rate стабилен, errors плоские, что подходит к нехватке ресурса (CPU saturation, замедление downstream) не хуже регрессии в коде. USE подскажет, что именно, до того как вы действуете." },
    { label: "Позвать команду базы данных — скачки Duration всегда downstream.", misconception: "Вы пока не знаете, что узкое место — база. Дрейф Duration при плоских R и E может быть CPU saturation на хостах сервиса, насыщенным пулом соединений или downstream — именно USE на хостах и прямых зависимостях это различает." },
  ]}
/>

<Quiz
  id="04-red-use-choice-2"
  lessonSlug="04-red-use"
  lang="ru"
  question="Один диск на 80% utilization с глубиной очереди I/O 50; другой на 95% utilization с глубиной очереди 1. Какой сигнал хуже и какое измерение USE это говорит?"
  choices={[
    { label: "Диск 80%/очередь-50 гораздо хуже. Saturation — очередь ожидающей работы — это опережающий индикатор латентности; каждая задача в очереди платит ожидание. Ресурс на 95% utilization с глубиной очереди 1 просто поспевает, без бэклога.", correct: true },
    { label: "Диск с 95% utilization, потому что более высокий utilization всегда означает большую латентность.", misconception: "Utilization — это среднее busy-время, а не ожидающая работа. 95% занятости при очереди 1 значит, диск поспевает; 80% занятости при очереди 50 значит, задачи копятся. Saturation, а не utilization, предсказывает видимую пользователю латентность." },
    { label: "Они одинаково плохи — оба диска сильно нагружены.", misconception: "Они не равны. Глубина очереди отличается в 50 раз. Весь смысл S в USE: два ресурса с похожим utilization могут иметь радикально разную ожидающую работу, и инцидент — у того, где очередь." },
    { label: "Ничего не важно, пока utilization не достигнет 100%.", misconception: "Ждать 100% уже поздно. Saturation растёт раньше, чем насыщается utilization, и является более ранним и диагностичным сигналом — очередь образуется задолго до того, как ресурс выжат." },
  ]}
/>

<Quiz
  id="04-red-use-choice-3"
  lessonSlug="04-red-use"
  lang="ru"
  question="Utilization памяти узла умеренный, MemAvailable всё ещё показывает 500 МБ свободно, но воркер встаёт на минуты. Какой сигнал бы это поймал и почему дашборды free-RAM это пропускают?"
  choices={[
    { label: "PSI memory 'full' стал ненулевым: все не-idle задачи были застопорены в ожидании, пока ядро освободит страницы. Free-RAM показывает то, что доступно прямо сейчас, а не давление reclaim, которое ядро заплатило, чтобы это поддерживать.", correct: true },
    { label: "node_memory_MemAvailable, упавший ниже 10% — стандартный алерт по памяти.", misconception: "MemAvailable всё ещё показывал 500 МБ; порог free-RAM не сработал. Именно этот разрыв закрывает PSI — давление может быть тяжёлым, пока доступная память всё ещё показывает здоровое число." },
    { label: "CPU utilization выше 80%, раз стопор должен быть проблемой CPU.", misconception: "Стопор — это reclaim памяти, а не CPU. PSI cpu был бы не тем ресурсом; PSI memory 'full' — это сигнал, который срабатывает, когда ядро буксует на reclaim." },
    { label: "Ничего — 500 МБ свободно доказывает, что давления памяти не было.", misconception: "Это именно та ошибка, от которой предостерегает юнит. PSI memory 'full' может быть высоким, пока MemAvailable выглядит щедрым; ядро блокирует выделяющие задачи, пока с трудом возвращает страницы." },
  ]}
/>

<Quiz
  id="04-red-use-choice-4"
  lessonSlug="04-red-use"
  lang="ru"
  question="Команда считает p99 по всему флоту, усредняя предвычисленный p99 каждой реплики (Prometheus summaries). Почему это неверно и каков правильный метод?"
  choices={[
    { label: "Перцентили не аддитивны — p99 объединённых распределений не равен среднему из p99 по репликам и может ошибаться в 2 раза и больше. Правильно: эмитить гистограммы, агрегировать счётчики бакетов через sum by (le) (rate(...)), затем запускать histogram_quantile поверх объединённых счётчиков.", correct: true },
    { label: "Summaries просто менее точны, чем гистограммы на реплику, так что и сами по-репличные числа уже неверны.", misconception: "Точность summary на реплику в порядке. Дефект — в агрегации: усреднение перцентилей по репликам математически некорректно, даже если p99 каждой реплики точен." },
    { label: "Это корректно, пока каждая реплика обслуживает примерно равный трафик.", misconception: "Равный трафик не спасает. Если распределения латентности по репликам различаются, среднее их p99 всё равно не равно p99 объединённого распределения." },
    { label: "Summaries нельзя запросить в PromQL, так что запрос ничего не вернёт.", misconception: "Summaries запрашиваемы; проблема — в математике межрепличной агрегации перцентилей, а не в поддержке запросов." },
  ]}
/>

<Quiz
  id="04-red-use-choice-5"
  lessonSlug="04-red-use"
  lang="ru"
  question="Чтобы перейти от скачка p99 в гистограмме к конкретному медленному запросу, инженер хочет добавить trace_id как label метрики. Что сломается и каково спроектированное решение?"
  choices={[
    { label: "trace_id неограничен, поэтому каждое значение умножает число серий и может уронить TSDB или взорвать счёт. Используйте exemplars: сэмплированный trace_id прикрепляется к отдельному наблюдению гистограммы, так что Grafana прыгает к трейсу без добавления умножающего cardinality label.", correct: true },
    { label: "Ничего не сломается — trace_id обычный label, и Prometheus справится.", misconception: "trace_id фактически уникален на запрос, канонический label с неограниченной cardinality. Каждая существующая серия получает новый вариант на трейс; head-блок и счёт растут без границ." },
    { label: "Добавить второй счётчик с label trace_id, чтобы хранить ссылки там.", misconception: "Это просто перемещает ту же неограниченную cardinality на другую метрику — идентичный взрыв TSDB и стоимости. Exemplars существуют именно чтобы нигде не хранить trace_id как label." },
    { label: "Уменьшить число бакетов гистограммы, чтобы запрос корреляции был дешевле.", misconception: "Меньше бакетов снижает точность p99 и ничего не даёт для корреляции с трейсом. Мост между скачком метрики и конкретным трейсом — это exemplar, а не число бакетов." },
  ]}
/>

<Quiz
  id="04-red-use-choice-6"
  lessonSlug="04-red-use"
  lang="ru"
  question="Проектируя алерты для сервиса, как сопоставить сигналы RED и USE с уровнями серьёзности и почему это разделение — сильнейший рычаг против усталости от алертов?"
  choices={[
    { label: "Пейджить на RED (видимые пользователю симптомы: нарушение SLO по p99, скачок error-rate); направлять USE (PSI cpu/memory, запас диска) в Slack или тикет. Когда срабатывает RED, пользователи уже затронуты; USE — это предупреждение о ёмкости, чтобы спланировать до того, как это станет инцидентом RED.", correct: true },
    { label: "Пейджить и на каждый алерт USE, чтобы команда всегда знала, когда CPU переходит 70%.", misconception: "Пейджинг на пороги ресурсов — классическая спираль усталости: большинство колебаний CPU/памяти никогда не доходят до пользователя. USE место — на медленном канале, чтобы on-call будили только реальные пользовательские последствия." },
    { label: "Пейджить на USE и считать RED информационным, раз здоровье ресурсов — корневая причина.", misconception: "Это переворачивает дисциплину. Пользователь чувствует RED, не USE; насыщенный ресурс, который никогда не ухудшает запрос, не инцидент. Пейджите на симптом, расследуйте причину." },
    { label: "Поставить всё на один уровень серьёзности, чтобы ничего не пропустить.", misconception: "Плоский уровень — это как команды тонут. Намеренное разделение RED-page / USE-warning — один из сильнейших рычагов против усталости от алертов именно потому, что отделяет 'пользователю больно сейчас' от 'планируй ёмкость скоро'." },
  ]}
/>

<Recap lang="ru">
Сквозная линия юнита — одно дерево решений: RED называет симптом со стороны клиента (Rate, Errors, Duration), USE находит причину со стороны ядра (Utilization, Saturation, Errors), а saturation — глубина очереди, PSI — самое диагностичное измерение, потому что пользователь чувствует ожидающую работу, а не среднее busy-время. Вокруг этого ядра стоят вспомогательные дисциплины: Duration должен быть гистограммой, агрегированной через sum by (le) (никогда не усреднённым перцентилем); cardinality ограничивается тем, что в label идут только route/method/status_class, а мост к трейсам строится через exemplars; алерты пейджат на RED, предупреждают на USE. Каждый продакшен-сбой в юните сводится к одному пропущенному сигналу в этом дереве.
</Recap>
