infographic compare-binary-horizontal-underline-text-vs
data
  title Сигнал saturation: load average против PSI
  desc Почему PSI (ядро 4.20+) заменяет прокси load average
  compares
    - label Load average
      children
        - label Смешивает состояния задач
          desc Runnable + uninterruptible вперемешку
        - label Зависит от числа ядер
          desc Load 10: норма на 64 ядрах, беда на 2
        - label 1-мин среднее скрывает пики
          desc Кратковременный 5s-кризис почти не виден
        - label Порог — это угадывание
          desc Нет чистого правила для разных хостов
    - label PSI · ядро 4.20+
      children
        - label Wall-clock время stall
          desc Доля времени, что задачи ждали ресурс
        - label Не зависит от числа ядер
          desc Один смысл на 2 и на 64 ядрах
        - label сигнал some против full
          desc full = заблокированы все не-idle задачи
        - label Алертируемо, накладные sub-1%
          desc K8s 1.27+ использует для эвикции
theme
  palette #f43f5e #14b8a6
