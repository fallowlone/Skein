---
concepts:
- slo-production-failures
- slo-self-observability
- slo-security
- error-budget-culture
deepensInto: []
estMin: 18
lang: ru
lessonType: topic
level: senior
order: 8
prereqs:
- observability/05-slo-budgets/07-iceberg-sli-composite-math-sla
slug: 08-production-failures-and-design
sources:
- https://sre.google/workbook/error-budget-policy/
- https://sre.google/workbook/implementing-slos/
- https://sre.google/sre-book/embracing-risk/
spiral:
- statefulness
status: ready
summary: Инциденты Stripe, GitHub, Coinbase и Netflix раскрывают SLO failure mode''''ы.
  Самонаблюдаемость SLO — NaN-знаменатели, дрейф burn rate, пробелы policy — мета-слой.
  Безопасность пересекается с SLI. Фреймворк переживает миграции инструментов потому
  что является контрактом.
title: Продакшн-отказы SLO, самонаблюдаемость, безопасность и общая картина
track: observability
unit: 05-slo-budgets
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import NumbersCard from "~/components/layout/NumbersCard.astro";
import Misconception from "~/components/layout/Misconception.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";
import Infographic from "~/components/diagram/Infographic.astro";
import abandonVsSucceed from "./infographics/abandon-vs-succeed.svg?raw";

<Hook>
Платформенная команда имеет MWMBR-алерты, Sloth-генерированные recording rules и подписанную error budget policy. Половина команд игнорирует SLO-алерты. Проблема не в инструментарии — в том, что SLI не коррелируют с болью пользователя, и policy так и не применялась.
</Hook>

<Crux>Отказы SLO — организационные: неверный SLI, policy без применения, неправильный счёт. SLO-самонаблюдение ловит это раньше.</Crux>

<Explanation>

## Реальные продакшн-отказы

Четыре кейса, раскрывающие failure mode'ы:

**Stripe 2022 — policy сработала:**
Checkout SLO на 99.99% был нарушен внутренне (инженеры заметили через burn rate), но внешнее SLA (99.9%) было выполнено. Предопределённая error budget policy команды автоматически заморозила новые деплои фич на 3 дня пока команда надёжности расследовала — предотвратив второй инцидент на всё ещё деградированном code path. Policy была активирована, применена, и заморозка держалась без исполнительного переопределения. Урок: когда policy подписана и команда считает её применимой, она работает именно так, как задумано. SLO поймал то, что ручной мониторинг мог пропустить.

**GitHub 2023 — SLI был неверным:**
SLO-платформа GitHub неправильно считала фоновые job-отказы как user-facing события на протяжении квартала, поедая reliability-бюджет и запуская разговоры культуры обвинений. Команды наказывались за «инциденты», которых пользователи никогда не испытывали. Postmortem сбросил определение SLI только на journey-уровень (user-facing запуски GitHub Actions, не обработка внутренних очередей). Урок: определение SLI — самое важное решение — неверное отравляет данные целого квартала и может разрушить доверие к SLO-программе до её установления.

**Coinbase 2024 — budget policy остановила рискованное расширение:**
Multi-region деплой нарушил 99.99% trading API SLO на 8 минут из-за неправильно настроенного load balancer. Error budget policy сработала в течение 24 часов, и команда поставила на паузу новые региональные запуски на неделю. Пауза позволила команде надёжности провести аудит инструментария multi-region деплоя и найти два дополнительных паттерна неправильной конфигурации до того, как они вызвали инциденты. Урок: заморозка — не наказание, а forcing function, направляющий инженерные усилия на хрупкость, только что проявившую себя.

**Netflix 2024 — SLO намеренно ослабили:**
Внутренний SLO Netflix для воспроизведения видео был ослаблен с 99.99% до 99.95% после шестимесячного обзора, показавшего что пользователи не могут ощутить разницу при 99.95%, но инженерная стоимость поддержания лишней девятки была значительной. Урок: SLO — живые цели, эволюционирующие с системой и пользовательскими исследованиями. «Жёстче — всегда лучше» — ложь. Ежеквартальный обзор существует для проведения именно этого эксперимента.

**Общий паттерн для всех четырёх:** SLO управляют инженерными решениями, а не наоборот. Компании, получающие ценность от SLO — те где бюджетное число меняет то, что делают команды: замораживать, расследовать, ослаблять, ужесточать — не те, где SLO — метрика на дашборде, на которую никто не реагирует.

<FlowDiagram
  label="Формальный SLI против ориентации на пользователя"
  perRow={3}
  hue="var(--d-systems)"
  nodes={[
    { id: "server", label: "Серверный SLI", sub: "5xx на LB" },
    { id: "green", label: "Дашборд зелёный", sub: "SLO 'выполнен'" },
    { id: "pain", label: "Пользователи страдают", sub: "таймауты, медленно" },
    { id: "edge", label: "Мерить на крае", sub: "user-facing" },
    { id: "burn", label: "Алерт на burn", sub: "MWMBR" },
    { id: "signal", label: "Реальный сигнал", sub: "пейдж срабатывает" },
  ]}
  edges={[
    { from: "server", to: "green", label: "vanity" },
    { from: "green", to: "pain", label: "слепая зона" },
    { from: "pain", to: "edge", label: "фикс: сдвинуть пробу" },
    { from: "edge", to: "burn" },
    { from: "burn", to: "signal" },
  ]}
  caption="Серверный SLI (5xx на load balancer) остаётся зелёным, пока пользователи ловят таймауты и медленные ответы — отказ невидим выше точки измерения. Фикс: мерить на user-facing крае и алертить на burn rate, чтобы пейдж срабатывал на реальную боль."
/>

## Наблюдаемость для самих SLO

Мета-вопрос: как узнать что SLO-платформа работает?

**Сигнал 1 — ratio_total никогда не должен быть NaN:**
Если SLI знаменатель равен нулю (нет трафика, граничный случай малого трафика, сброс счётчика), recording rule производит NaN. NaN burn rate невидим: алерт ни правильно срабатывает, ни правильно очищается. Мониторить `sum(rate(http_request_total[5m])) == 0` и алертить на это отдельно — «у нас нет сигнала трафика» само по себе условие алерта.

**Сигнал 2 — долгооконный burn rate должен быть стационарным:**
Построй 3д burn rate за 90 дней. Он должен осциллировать около 1x в среднем (точно выполняя SLO; некоторые недели выше, некоторые ниже). Устойчивое среднее 1.5x значит SLO-цель слишком жёсткая для текущей системы — постоянный стресс, постоянный риск заморозки. Устойчивое среднее 0.3x значит цель слишком свободная — сверхинжениринг надёжности, которая никому не нужна. Стационарность около 1x значит цель откалибрована.

**Сигнал 3 — исходы policy должны соответствовать истории горения:**
Если история burn rate за 3 месяца показывает три периода ухода бюджета в минус без активированных заморозок, policy переопределяется. Либо policy не имеет реальных полномочий (нужна повторная подпись на уровне директора), либо команды не знают что она применима к ним (пробел коммуникации). SLO-мета-дашборд должен отслеживать: количество активных SLO, количество текущих горящих выше 1x, средний оставшийся бюджет, время последней заморозки на SLO.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Мета-сигнал</th>
        <th class="border border-border px-3 py-2 text-left">Что раскрывает</th>
        <th class="border border-border px-3 py-2 text-left">Действие</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2 font-mono text-xs">ratio_total == 0 / NaN</td>
        <td class="border border-border px-3 py-2">Нет трафика; знаменатель SLI сломан</td>
        <td class="border border-border px-3 py-2">Алерт на NaN; добавить синтетические пробы</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2 font-mono text-xs">3д burn avg &gt; 1.5x устойчиво</td>
        <td class="border border-border px-3 py-2">SLO-цель слишком жёсткая для системы</td>
        <td class="border border-border px-3 py-2">Ежеквартальный обзор: ослабить или починить</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2 font-mono text-xs">3д burn avg &lt; 0.3x устойчиво</td>
        <td class="border border-border px-3 py-2">SLO-цель слишком свободная</td>
        <td class="border border-border px-3 py-2">Ежеквартальный обзор: ужесточить</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2 font-mono text-xs">Бюджет ≤ 0 без активированной заморозки</td>
        <td class="border border-border px-3 py-2">Policy не применяется</td>
        <td class="border border-border px-3 py-2">Повторная подпись policy; расследовать переопределение</td>
      </tr>
    </tbody>
  </table>
</div>

<NumbersCard
  id="08-production-numbers"
  title="SLO продакшн-числа старшего уровня"
  stats={[
    { label: "Бюджет при 99.9% SLO, 1M req/день, 28 дней", value: "28 000 ошибок" },
    { label: "Burn rate 14.4x error rate при 99.9% SLO", value: "1.44% rate отказов запросов" },
    { label: "Составной потолок: 5 сервисов при 99.9%", value: "~99.5%" },
    { label: "Типичный буфер SLA vs SLO", value: "0.05–0.5 процентных пункта" },
    { label: "Триггер postmortem по одному инциденту", value: "≥ 20% 28-дневного бюджета сожжено" },
    { label: "Ослабление SLO Netflix: с 99.99% → 99.95%", value: "Пользователи не замечали разницы; инженерная стоимость снизилась" },
  ]}
/>

## Безопасность и SLO: два пересечения

**Пересечение 1 — бот-трафик перекашивает SLI:**
Успешный но злонамеренный запрос считается «хорошим» в availability SLI — атака credential stuffing (перебор украденных пар логин/пароль с других утечек), возвращающая 200 OK, проходит SLO. Бот-трафик раздувает знаменатель и может маскировать реальные проблемы пользователей: 1000 бот-запросов в секунду могут разбавить 1% error rate на легитимном трафике до измеренного 0.01% error rate. Сениорный паттерн: считать SLO по фильтрованному трафику (отбрасывать известных ботов, rate-limited IP, сканерный трафик из тестирования безопасности). SLI должен отслеживать здоровье реальных пользователей, не всего трафика.

**Пересечение 2 — SLO-горение как сигнал безопасности:**
Падение доступности без причины в инфре — нет деплоев, нет изменений конфигурации, нет деградации upstream — может быть первым симптомом DDoS или бэкенд-эксплойта. Несколько incident-response playbook'ов включают «проверить SLO burn rate» как шаг в чеклист security-инцидента, наряду с проверками аномалий логов и анализом сетевого трафика. Spike burn rate в 3 ночи в субботу без коррелированного infra-события заслуживает security-взгляда даже до нахождения инфраструктурного объяснения.

## Общая картина

SLO — не число на дашборде. Это контракт, превращающий продуктовые решения в инженерную арифметику. Error budget — мост между «хотим шипить» и «хотим быть надёжными». MWMBR-алерт — мост между «бюджет тратится» и «разбудить инженера». Error budget policy — мост между алертом и org chart.

<Inset kind="why" lang="ru">
Причина выживания SLO-фреймворка при каждой смене поколений инструментов мониторинга — он не зависит от инструментов. Он зависит от того, что команда взяла на себя обязательство по одному числу — SLO-цели — с которой согласны все (product, engineering, operations). Prometheus заменяется Datadog; Datadog заменяется чем-то ещё. SLO переживает каждую миграцию потому что является обязательством, а не инфраструктурой.
</Inset>

**Почему команды бросают SLO:**
- SLI не коррелирует с болью пользователя → алерты — шум → команда учится их игнорировать
- SLO-цель слишком жёсткая → постоянные заморозки → давление продукта переопределяет → policy теряет силу
- Policy никогда не подписана на уровне директора → «совещательные» SLO, на которые никто не реагирует
- Нет ежеквартального обзора → SLO дрейфует от реальных нужд пользователей → неверный сигнал два года

**Почему команды преуспевают с SLO:**
- Начинают с одного journey, валидируют SLI против реальных репортов пользователей
- Ставят консервативную начальную цель, ужесточают ежеквартально
- Проводят fire drill перед запуском
- Получают подпись директора до первого freeze-события (не во время него)
- Проводят ежеквартальный обзор с участием продукта

<Infographic
  svg={abandonVsSucceed}
  label="Сравнение бок о бок: команды, бросающие SLO, против команд, преуспевающих с SLO. Каждый failure mode (SLI игнорирует боль пользователя, цель слишком жёсткая, policy не подписана, нет ежеквартального обзора) сопоставлен со средством (валидировать SLI против репортов пользователей, консервативная цель с квартальным ужесточением, директор подписывает до первой заморозки, ежеквартальный обзор с участием продукта)."
  caption="Те же четыре failure mode'а, противоположные исходы. Внедрение SLO проваливается или удаётся на организационном контракте — определении SLI, подписанной policy, ежеквартальном обзоре — а не на инструменте мониторинга."
  hue="var(--d-infra)"
/>

<Misconception id="mc-slo-tools">
  <p><strong>«Лучший инструментарий исправит наш SLO adoption.»</strong> Нет. Команды, переходящие с Sloth на Nobl9 без починки SLI и error budget policy, получают тот же результат: дашборды, на которые никто не реагирует. Узкое место — организационное, не техническое. Сначала почини контракт; потом инструменты.</p>
</Misconception>

<Quiz
  id="08-production-quiz-1"
  lessonSlug="08-production-failures-and-design"
  lang="ru"
  question="Платформенная команда разворачивает SLO на 80 сервисах. Через шесть месяцев половина команд игнорирует SLO-алерты даже при правильно настроенном MWMBR. Какова наиболее вероятная первопричина?"
  choices={[
    { label: "SLI не коррелируют с болью пользователя — алерты срабатывают на технические сигналы, которые пользователи не замечают, поэтому on-call команды учатся что пейдж — шум", correct: true },
    { label: "Пороги burn rate установлены слишком низко — снижение с 14.4x до 10x уменьшит объём алертов", misconception: "Более низкие пороги срабатывают чаще, а не реже. Проблема в определении SLI — если SLI неверен, даже идеально откалиброванные пороги производят бесполезные пейджи." },
    { label: "В конфигурации Sloth/Pyrra есть баги в PromQL", misconception: "Хотя это возможно, это обычно проявляется как вообще не срабатывающие алерты, а не как систематически игнорируемые. Систематическое игнорирование предполагает что алерты технически срабатывают, но не релевантны реальному пользовательскому опыту." },
    { label: "28-дневное окно слишком длинное — сокращение до 7 дней сделает SLO более отзывчивым", misconception: "Длина окна влияет на время детекции и каденцию заморозок. Изменение окна не исправляет алерты, срабатывающие на неверный сигнал." },
  ]}
/>

<Quiz
  id="08-production-quiz-2"
  lessonSlug="08-production-failures-and-design"
  lang="ru"
  question="Recording rule ratio_total для сервиса возвращает NaN в Prometheus. Что это значит для SLO-алертинга?"
  choices={[
    { label: "Знаменатель SLI равен нулю — алерт ни правильно срабатывает, ни правильно очищается, поэтому SLO тихо сломан", correct: true },
    { label: "Сервис при 100% доступности — NaN значит не было ошибок", misconception: "NaN значит что функция rate() получила ноль сэмплов или серия отсутствует — не что ошибок не было. NaN burn rate оставляет алерт в неопределённом состоянии." },
    { label: "Recording rule не смог записать — проверь write path Prometheus", misconception: "Write path может быть нормальным; проблема в том что underlying time series не имеет данных. NaN от rate() значит что серия счётчика отсутствует или имеет нулевой прирост за окно оценки." },
    { label: "Alertmanager автоматически сработает с алертом 'data quality'", misconception: "Alertmanager по умолчанию не мониторит NaN значения recording rule. Команда должна явно алертить на отсутствующие или NaN серии как отдельный мета-сигнал." },
  ]}
/>

</Explanation>

<KeyTakeaway>Продакшн-отказы SLO — организационные: неверное определение SLI (GitHub 2023), policy не применяется, SLO установлен равным SLA. Самонаблюдаемость SLO — NaN знаменатели, устойчивый дрейф burn rate, пробитые бюджеты без заморозок — мета-слой, ловящий сбои платформы. SLO-фреймворк переживает каждую миграцию инструментов потому что является контрактом между продуктом и инженерией, а не конфигурационным файлом.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="08-production-retrieval"
  lang="ru"
  questions={[
    { q: "Каковы три организационных failure mode'а, заставляющих команды бросать SLO после первоначального внедрения?", a: "1. SLI не коррелирует с болью пользователя: алерты срабатывают на технические сигналы (CPU, глубина внутренних очередей, 5xx от health check'ов), которые пользователи не замечают. On-call команды неоднократно поднимают из-за несущественных проблем и учатся воспринимать пейджи как шум. Alert fatigue разрушает SLO-контракт в течение месяцев. Фикс: аудитировать определение SLI против реальных тикетов поддержки — каждый SLI-пейдж без жалобы пользователя — сигнал что SLI неверен. 2. SLO-цель слишком жёсткая для базового уровня системы: сервис постоянно работает при burn rate 2x, что означает постоянные заморозки. Feature-команды так часто переопределяются policy, что давление продукта в конечном итоге вынуждает исполнительное исключение. Как только первое исключение получено, policy теряет силу. Фикс: устанавливать начальную цель консервативно; ужесточать только после подтверждения стабильности baseline. 3. Error budget policy подписана но никогда не применялась: в первый раз когда бюджет достигает нуля и давление продукта всё равно проталкивает деплой, policy теряет доверие. Без подписи на уровне директора и реального применения policy — предложение. Фикс: подпись директора должна предшествовать первому freeze-событию — полномочия должны быть установлены до давления." },
    { q: "Опиши три мета-сигнала, говорящих работает ли сама SLO-платформа правильно.", a: "1. ratio_total никогда не должен быть NaN: если знаменатель SLI падает до нуля (нет трафика), recording rule производит NaN и алерт ни срабатывает, ни очищается правильно. Мониторить на отсутствующие или нулевые rate серии как отдельный алерт: 'если sum(rate(http_requests_total[5m])) == 0 на > 10м, пейджить on-call'. Это ловит граничные случаи малого трафика и баги сброса счётчика до того как они искажают расчёт бюджета. 2. Долгооконный burn rate должен быть стационарным около 1x: строить 3д burn rate за 90 дней. Устойчивое среднее выше 1.5x значит цель слишком жёсткая; ниже 0.3x — слишком свободная. Стационарность около 1x значит SLO откалибровано к реальному поведению системы. Использовать как входные данные для ежеквартального обзора. 3. События пробитого бюджета должны коррелировать с активациями заморозок: если история горения показывает три zero-budget события без активированных заморозок, policy переопределяется. Проверить задокументированы ли переопределения, знал ли директор, была ли исполнительная эскалация. Если переопределения недокументированы, policy существует только на бумаге — нужна повторная подпись и коммуникационное обновление." },
    { q: "Почему SLO-фреймворк переживает миграции инструментов когда конкретные инструменты мониторинга — нет?", a: "SLO-фреймворк — контракт между продуктом и инженерией: обе стороны договорились об одном числе (SLO-цели) и последствии при нарушении (заморозка деплоев). Этот контракт независим от инструментария. Когда Prometheus заменяется Datadog, SLO-цели, документ error budget policy, ритуал ежеквартального обзора и подписанный механизм применения — все остаются нетронутыми. Нужно регенерировать только recording rules и дашборды (с чем справляются SLO-платформы вроде Sloth/Pyrra через YAML-декларации, переносимые на любой бэкенд). Долговечность фреймворка происходит из его организационного слоя: обязательство предшествует инструментам и переживает их. Сравни с алертингом на основе threshold'ов: при миграции с одного инструмента на другой, мигрируешь конфигурации — чисто технически. С SLO мигрируешь конфигурации, но культурный контракт остаётся нетронутым. Команды, воспринимающие SLO как конфигурации дашбордов, теряют их при миграции инструментов. Команды, воспринимающие SLO как обязательства, сохраняют их." },
  ]}
/>

<Recap lang="ru">
Реальные продакшн-отказы SLO раскрывают failure mode'ы: GitHub неправильно считал фоновые jobs как user-facing события и испортил данные квартала; error budget policy Coinbase сработала правильно и предотвратила каскад; Netflix намеренно ослабил цель после пользовательских исследований показавших что лишняя девятка невидима для пользователей. Наблюдение за самой SLO-системой требует трёх мета-сигналов: ratio_total никогда не должен быть NaN (нет трафика → тихий отказ алерта), долгооконный burn rate должен быть стационарным около 1x (дрейф раскрывает неправильно откалиброванные цели), и события пробитого бюджета должны производить активации заморозок (пробел раскрывает policy без силы). Безопасность пересекается с SLO в двух местах: бот-трафик разбавляет знаменатель SLI, а спайки burn rate без infra-причины могут быть инцидентами безопасности. SLO-фреймворк переживает каждое поколение инструментов потому что является контрактом — продукт и инженерия взяли обязательство по одному числу — а не конфигурацией. Инструменты мигрируют; контракты сохраняются. Теперь, когда унаследуешь стек на Prometheus, мигрирующий в Datadog — SLO переживут миграцию: регенерировать нужно только recording rules.
</Recap>
