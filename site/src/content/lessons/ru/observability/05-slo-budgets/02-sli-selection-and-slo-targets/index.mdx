---
concepts:
- sli-selection
- slo-target
- error-budget-arithmetic
- rolling-window
deepensInto: []
estMin: 14
lang: ru
lessonType: topic
level: middle
order: 2
prereqs:
- observability/05-slo-budgets/01-sli-slo-budget-basics
slug: 02-sli-selection-and-slo-targets
sources:
- https://sre.google/workbook/implementing-slos/
- https://sre.google/sre-book/embracing-risk/
- https://prometheus.io/docs/practices/histograms/
spiral:
- latency
status: ready
summary: Хороший SLI — отношение в [0%, 100%], коррелирующее с болью пользователя,
  а не загрузка CPU или длина очереди. Правильная SLO-цель — минимальное число, которое
  терпят пользователи, потому что каждая лишняя девятка умножает инженерные затраты
  в 3–10 раз.
title: 'Выбор SLI и SLO-целей: отношения, не ощущения'
track: observability
unit: 05-slo-budgets
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import NumbersCard from "~/components/layout/NumbersCard.astro";
import Misconception from "~/components/layout/Misconception.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
Команда алертует на загрузку CPU как на основной сигнал надёжности. При автоскейлинге под нормальный трафик CPU взлетает — и пейджер срабатывает. Ни один пользователь не пострадал. SLI был неправильным.
</Hook>

<Crux>SLI — user-facing-отношение в [0%, 100%]. SLO — минимальное число, терпимое пользователями. Лишняя девятка стоит в 3–10 раз больше.</Crux>

<Explanation>

## Что делает SLI хорошим

Задай себе вопрос: если эта метрика выглядит нормально — могут ли пользователи при этом страдать? Если да — это не хороший SLI. Google SRE Workbook категоричен: хороший SLI — это **отношение good events к total events** в [0%, 100%], **коррелирующее с тем, что чувствует пользователь**. Из этого определения следуют четыре свойства.

**Это должно быть отношение, а не счётчик.** «Менее 100 ошибок в день» смешивает объём трафика с надёжностью. При 1,000 req/день это 10% ошибок; при 1,000,000 req/день — 0.01%. Форма отношения делает SLO сопоставимыми при любом объёме и делает error budget напрямую вычисляемым: `budget = (1 − SLO) × total_events`.

**Должно попадать в [0%, 100%].** Это делает дашборды читаемыми (фиксированная ось) и burn rate вычислимым.

**Должно отслеживать боль пользователя, а не боль машины.** Запрещённый антипаттерн — «внутренний SLI»: загрузка CPU, длина очереди, паузы GC, утилизация heap. Сервер при 100% CPU может прекрасно обслуживать каждый запрос. SLI на CPU будет пейджить при здоровом autoscaling-событии, когда пользователи довольны.

**Категории сигналов по типу сервиса:**

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Тип сервиса</th>
        <th class="border border-border px-3 py-2 text-left">Availability SLI</th>
        <th class="border border-border px-3 py-2 text-left">Latency SLI</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">Request-driven API</td>
        <td class="border border-border px-3 py-2 font-mono text-xs">successful_requests / total_requests</td>
        <td class="border border-border px-3 py-2 font-mono text-xs">requests_under_200ms / total_requests</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Data pipeline</td>
        <td class="border border-border px-3 py-2 font-mono text-xs">records_processed / records_arrived</td>
        <td class="border border-border px-3 py-2 font-mono text-xs">records_within_SLA / total_records</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Storage</td>
        <td class="border border-border px-3 py-2 font-mono text-xs">data_intact / data_stored</td>
        <td class="border border-border px-3 py-2 font-mono text-xs">reads_under_threshold / total_reads</td>
      </tr>
    </tbody>
  </table>
</div>

<FlowDiagram
  label="От сырых запросов к SLI: классификация, затем отношение в точке у пользователя"
  perRow={5}
  hue="var(--d-systems)"
  nodes={[
    { id: "stream", label: "Поток запросов", sub: "сырые события" },
    { id: "classify", label: "Классификация", sub: "статус + латентность" },
    { id: "good", label: "Good-события", sub: "2xx и < 200мс" },
    { id: "bad", label: "Bad-события", sub: "5xx или медленные" },
    { id: "sli", label: "SLI", sub: "good / total" },
  ]}
  edges={[
    { from: "stream", to: "classify", label: "мерим на edge" },
    { from: "classify", to: "good", label: "прошёл" },
    { from: "classify", to: "bad", label: "упал" },
    { from: "good", to: "sli", label: "числитель" },
    { from: "bad", to: "sli", label: "в total" },
  ]}
  caption="Меряем в точке, ближайшей к пользователю (load balancer / edge), классифицируем каждый запрос как good или bad по статусу и порогу латентности — тогда SLI = good / total. И good, и bad идут в total — числитель образуют только good-события."
/>

## Latency SLI использует bucket-счётчики, а не percentile

Latency SLO (latency — задержка ответа) типа «99% запросов под 200мс» звучит как percentile, но реализуется как счётчик. Prometheus histogram (гистограмма распределения задержек) на пороге SLO даёт это напрямую:

```
fast_requests = http_request_duration_seconds_bucket{le="0.2"}
latency_sli = sum(rate(fast_requests[1h])) / sum(rate(http_request_duration_seconds_count[1h]))
```

`histogram_quantile` не нужен — и нет погрешности оценки, которая загрязняла бы бюджет. Именно поэтому RED-Duration histogram **обязан** иметь границу bucket'а ровно на пороге SLO: без неё SLO нельзя оценить без аппроксимации.

## Что считается «успехом»: границы хорошего SLI

Определение успеха иногда тоньше, чем кажется. Для HTTP API очевидное — 2xx-ответ, но 2xx с пустым телом или с устаревшими данными уже не success. Для gRPC — статус OK, но client-side timeout без серверного ответа технически не отказ сервера. Зрелые команды описывают определение явно: «success = HTTP 200 AND response body non-empty AND processing latency < 1s» — и инструментируют все три проверки.

## Что не входит в SLI: out-of-scope

SLO считается над определённой совокупностью трафика. Стандартные out-of-scope категории:
- Запросы из load test и pen test (помечены header'ом или IP-диапазоном)
- Запросы от известных ботов и сканеров
- Запросы во время запланированных maintenance window
- Запросы, отброшенные rate-limit'ом на границе

Эти категории явно фиксируются в SLO-документе — иначе каждый postmortem превращается в спор о том, считать ли конкретный инцидент против бюджета. Большинство SLO-платформ поддерживают exclusion-фильтры через label'ы.

## Выбор SLO-цели

SLO-цель — **бизнес-решение**, не инженерное. Вопрос: «какой минимальный уровень надёжности пользователи примут, прежде чем заметят и начнут жаловаться?» Инженерия отвечает: «какая минимальная архитектура это обеспечивает?»

<NumbersCard
  id="02-nines-cost"
  title="SLO-цели и что стоит каждая девятка"
  stats={[
    { label: "99% SLO, 30 дней", value: "7.2 часа допустимого downtime" },
    { label: "99.9% SLO, 30 дней", value: "43.2 минуты" },
    { label: "99.95% SLO, 30 дней", value: "21.6 минут" },
    { label: "99.99% SLO, 30 дней", value: "4.3 минуты" },
    { label: "99.999% SLO, 30 дней", value: "~26 секунд" },
    { label: "Рост инженерных затрат за девятку", value: "3–10x" },
  ]}
/>

Паттерн:
- **99% → 99.9%:** добавить мониторинг и базовый алертинг
- **99.9% → 99.99%:** добавить multi-region с автоматическим failover
- **99.99% → 99.999%:** добавить N+2-избыточность, chaos engineering, 24/7 on-call, просыпающийся за минуты

<Misconception id="mc-more-nines">
  <p><strong>«Больше девяток всегда лучше.»</strong> Нет. Каждая лишняя девятка стоит 3–10x в инженерии, инфре и on-call-нагрузке. 99.999% оправдано для платёжного ядра или телефонии; для маркетингового сайта — это театр, выжигающий команду. Бери самое низкое SLO, которое терпят пользователи.</p>
</Misconception>

## Полная арифметика error budget

После определения SLO бюджет становится конкретным:

`error_budget = (1 − SLO) × total_events_in_window`

Для 99.9% SLO за 28 дней при 1M запросов/день:
- Total events = 28,000,000
- Budget = 0.001 × 28,000,000 = **28,000 упавших запросов**

По мере накопления отказов: `budget_remaining = budget_total − failures_so_far`

Burn rate в любой момент: `burn_rate = current_error_rate / (1 − SLO)`

При 0.1% error rate и 99.9% SLO: burn rate = 0.001 / 0.001 = **1x** (устойчиво).
При 1.44% error rate: burn rate = 0.0144 / 0.001 = **14.4x** (бюджет закончится за 2 дня).

## Почему 28 дней, а не «этот календарный месяц»

Календарные месяцы различаются (28–31 день) и создают cliff effect: плохая неделя в конце февраля по-другому бьёт по бюджету, чем та же неделя в конце марта. **28-дневное rolling window** (четыре полные недели) решает оба: всегда покрывает одинаковую длину, включает полный цикл будни/выходные — паттерны трафика нормализуются.

<Inset kind="why" lang="ru">
Rolling window — также причина, почему SLO-цели нужно начинать свободными и ужесточать ежеквартально, а не ставить жёстко в день первый. Консервативное начальное SLO даёт команде время увидеть реальный базовый error rate, понять паттерн трафика и правильно инструментировать счётчики — до того, как они несут ответственность за число, которое либо недостижимо, либо тривиально лёгкое.
</Inset>

<Quiz
  id="02-sli-selection-quiz-1"
  lessonSlug="02-sli-selection-and-slo-targets"
  lang="ru"
  question="Какой из вариантов является хорошим SLI для request-driven API?"
  choices={[
    { label: "successful_requests / total_requests — отношение в [0%, 100%], отслеживающее боль пользователя", correct: true },
    { label: "Загрузка CPU — растёт когда сервис под нагрузкой", misconception: "CPU — внутренняя машинная метрика. Она не отражает, успешно ли обслуживаются пользователи. CPU при 100% может обслуживать каждый запрос идеально." },
    { label: "Количество ошибок в минуту — фиксирует общее число отказов", misconception: "Абсолютный счётчик смешивает объём трафика с надёжностью. При 10k req/мин 100 ошибок — это 1%; при 100 req/мин — 100%. Форма отношения делает SLO сопоставимыми." },
    { label: "Время паузы GC — длинные паузы значат, что сервис страдает", misconception: "GC-паузы — внутренний JVM-сигнал, не user-facing отношение. Они могут вызывать проблемы с латентностью, но не являются SLI." },
  ]}
/>

<Quiz
  id="02-sli-selection-quiz-2"
  lessonSlug="02-sli-selection-and-slo-targets"
  lang="ru"
  question="99.9% availability SLO за 28 дней, 1M запросов/день. Команда на 14-м дне, 6,000 упавших запросов. Как с бюджетом?"
  choices={[
    { label: "60% потрачено за 50% окна — горят на 1.2x, исчерпают раньше 28 дней", correct: true },
    { label: "В пределах бюджета — 6,000 ниже 10,000 общего", misconception: "Общий бюджет = 0.1% × 28M = 28,000. На день 14 истекло 50% окна → ожидаемый расход 50% бюджета = 14,000. Реальный — 6,000 (только 21%). Команда фактически ниже темпа. Но вопрос указывает на 60% потраченных, значит числа другие — проверь сценарий." },
    { label: "Нельзя сказать без latency SLI", misconception: "Availability и latency SLO независимы; availability-бюджет считается из числа упавших запросов." },
    { label: "Немедленно триггерить freeze", misconception: "Freeze триггерится при достижении нуля, а не при 60% с оставшимися 14 днями и умеренным burn rate." },
  ]}
/>

<Quiz
  id="02-sli-selection-quiz-3"
  lessonSlug="02-sli-selection-and-slo-targets"
  lang="ru"
  question="Почему 28-дневное rolling window предпочтительнее календарного месяца?"
  choices={[
    { label: "Всегда покрывает одинаковую длину и одни и те же циклы будни/выходные — нет cliff effect на границах месяца", correct: true },
    { label: "Календарные месяцы запрещены в SLO-стандартах", misconception: "Никакого запрета нет — проблема в вариации и cliff effect от разных длин месяцев." },
    { label: "Datadog поддерживает только 28-дневные окна", misconception: "Большинство платформ поддерживает произвольные окна; 28 дней — конвенция, не ограничение инструмента." },
    { label: "28 дней короче календарного месяца — более снисходительно", misconception: "28 дней близко к или короче в зависимости от месяца; выигрыш — в консистентности, не длине." },
  ]}
/>

</Explanation>

<KeyTakeaway>Выбирай SLI как отношение, отслеживающее боль пользователя, а не машинные метрики. Ставь SLO на самую низкую цель, которую терпят пользователи: каждая лишняя девятка стоит в 3–10 раз больше. Используй 28-дневное rolling window для нормализации паттернов трафика. Арифметика бюджета следует из SLO-цели; burn rate — производная величина для сопоставимых дашбордов и алертов.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="02-sli-selection-retrieval"
  lang="ru"
  questions={[
    { q: "Почему SLI должен быть выражен как отношение, а не как абсолютный счётчик или машинная метрика?", a: "Отношение (good / total) делает SLO сопоставимыми при любом объёме трафика — сервис на 10 req/с и на 10k req/с оба имеют осмысленный 99.9% SLO. Абсолютный счётчик смешивает объём с надёжностью. Машинная метрика (CPU, GC) не отражает напрямую боль пользователя. Форма отношения делает error budget напрямую вычислимым (budget = (1 − SLO) × total_events) и нормализацию burn rate тривиальной." },
    { q: "Почему latency SLI требует границы bucket'а histogram ровно на пороге SLO?", a: "Latency SLO («99% запросов под 200мс») реализуется как fast_requests / total_requests, где fast_requests = histogram_bucket{le='0.2'}. Если граница не на 0.2s, нужно использовать histogram_quantile — это оценка с погрешностью. Погрешность загрязняет вычисление бюджета: он тратится быстрее или медленнее реального SLI. Production-grade SLO-инструментация определяет границы bucket'ов около порога SLO до того, как что-либо считать." },
    { q: "Как выбрать SLO-цель?", a: "Начни с вопроса: какой минимальный уровень надёжности пользователи примут, прежде чем заметят и начнут жаловаться — это бизнесовый/продуктовый вопрос, не инженерный. Затем спроси: какая минимальная архитектура это обеспечивает. Начинай консервативно (свободнее, чем кажется нужным) и ужесточай ежеквартально на основе реальных отзывов пользователей и фактического baseline error rate. Слишком жёсткое начало — команда тонет в алертах, на которые не может реагировать, и бросает систему до того, как она даст ценность." },
  ]}
/>

<Recap lang="ru">
Хороший SLI — отношение good events к total events, всегда в [0%, 100%], всегда отслеживающее то, что пользователи испытывают, никогда не машинная метрика типа CPU или длины очереди. Latency SLI использует histogram bucket-счётчики на пороге SLO, а не оценки histogram_quantile — чтобы арифметика бюджета была точной. SLO-цель — бизнес-решение: бери самую низкую надёжность, которую терпят пользователи, потому что каждая лишняя девятка умножает инженерные затраты в 3–10 раз. Используй 28-дневное rolling window для нормализации паттернов трафика. Budget = (1 − SLO) × total_events; burn rate = current_error_rate / (1 − SLO). Начинай свободно, ужесточай ежеквартально. Теперь, когда увидишь метрику на дашборде — первый вопрос: «это отношение, оно отслеживает боль пользователя, и оно в [0%, 100%]?»
</Recap>
