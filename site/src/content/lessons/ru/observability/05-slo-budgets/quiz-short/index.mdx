---
concepts:
- error-budget-arithmetic
- burn-rate-derivation
- mwmbr-and-logic
- composite-slo
- error-budget-policy
- low-traffic-slo
estMin: 14
lang: ru
level: senior
order: 8
prereqs: []
slug: quiz-short
sources:
- https://sre.google/workbook/implementing-slos/
- https://sre.google/workbook/alerting-on-slos/
- https://sre.google/workbook/error-budget-policy/
status: ready
summary: "Промпты на свободное воспроизведение по всему юниту. Ответь по памяти, затем раскрой модельный ответ — математика бюджета, вывод burn, MWMBR, composite, policy."
title: 'SLO и error budget: тест на воспроизведение'
track: observability
unit: 05-slo-budgets
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";

<Hook>
Воспроизведение бьёт перечитывание. На каждый промпт скажи или запиши полный ответ по памяти, прежде чем открыть модельный — усилие припоминания и закрепляет арифметику SLO, когда выводишь её вживую на постмортеме.
</Hook>

<Goal lang="ru">
Восстанови костяк юнита — что делает SLI хорошим, арифметику бюджета и burn rate, почему MWMBR использует AND, composite-потолок и организационную политику — не подглядывая в уроки.
</Goal>

<RetrievalDrawer
  client:load
  id="05-slo-budgets-short-retrieval"
  lang="ru"
  questions={[
    { q: "Определи SLI, SLO и error budget и назови формулы бюджета и burn rate с одним посчитанным числом.", a: "SLI — это отношение хороших событий к общему числу, в [0%, 100%], отслеживающее боль пользователя (например, successful_requests / total_requests) — никогда не машинная метрика вроде CPU. SLO — цель для этого SLI, процент, который команда фиксирует (например, 99.9%). Error budget — то, что остаётся ниже цели: budget = (1 − SLO) × total_events. Burn rate нормализует текущий error rate к budget rate: burn_rate = current_error_rate / (1 − SLO). Посчитано: при 99.9% SLO на 28 дней и 1M запросов/день total events = 28M, бюджет = 0.001 × 28M = 28 000 отказов; error rate 1.44% даёт burn rate 0.0144 / 0.001 = 14.4x, что исчерпает 28-дневный бюджет примерно за 2 дня." },
    { q: "Выведи пороги burn rate 14.4x / 6x / 1x из первых принципов.", a: "Каждый порог идёт из одного уравнения: burn_rate = (budget_fraction × period) / window, где budget_fraction — сколько от всего бюджета тир должен потратить при устойчивом burn, period — окно SLO (30 дней = 720h), а window — длинное окно алерта. Page 1h+5m тратит 2% за 1h: (0.02 × 720) / 1 = 14.4x. Page 6h+30m тратит 5% за 6h: (0.05 × 720) / 6 = 6x. Ticket 3d+6h тратит 10% за 3 дня: (0.10 × 720) / 72 = 1x. Короткие окна переиспользуют тот же порог — они лишь добавляют проверку «всё ещё происходит?». Для окна 28 дней (672h) page становится (0.02 × 672) / 1 = 13.44x, обычно округляют до 14x. Это не магические числа; любая команда может пересчитать их под свой period и target." },
    { q: "Почему MWMBR-алертинг использует AND между длинным и коротким окном и что ломается при single-window или OR?", a: "Одно короткое окно слишком шумное — случайные всплески пейджат on-call, и alert fatigue приводит к игнору пейджера. Одно длинное окно слишком медленно сбрасывается — после разрешения инцидента длинное окно всё ещё держит исторические плохие данные и пейджит почти всю длину окна (~55m на 1h-окне), и нельзя понять, сработал ли фикс. OR (Approach 4) срабатывает, когда любое из окон высокое, совмещая шум короткого окна с медленным сбросом длинного. MWMBR использует AND: длинное окно подтверждает, что burn реален и устойчив (устойчивость к шуму), а короткое — что он всё ещё идёт прямо сейчас (быстрый сброс, очищается за ~5 минут после фикса). AND — механизм, ограничивающий и задержку обнаружения, и задержку восстановления." },
    { q: "Объясни composite-потолок SLO и два архитектурных рычага, которые его поднимают.", a: "Пользовательский путь через N независимых сервисов хорош, только если хорош каждый сервис, поэтому потолок — произведение per-service SLO: пять сервисов по 99.9% дают 0.999^5 ≈ 99.5%, а не 99.9%. При коррелированных отказах (общая БД, региональное событие) реальный потолок ещё ниже, поэтому journey-level SLI на API gateway — авторитетное измерение. Рычаги: (1) Idempotent-ретраи с backoff — один ретрай превращает 0.1% отказа на попытку примерно в (0.001)^2 ≈ 0.0001%, но требует idempotency key, чтобы повторное списание не задвоилось; цена — добавленная латентность. (2) Parallel hedging — запусти две реплики, возьми первый успех; оба отказа — это (0.001)^2 = 99.9999%, но цена 2x трафика, и надо отменить или дедупнуть лишний ответ. Оба превращают «добавлять ли ретрай?» в измеримую арифметику бюджета." },
    { q: "Что должна содержать error budget policy, почему важны подписи и чем она отличается от SLA?", a: "Политика — это письменный, подписанный документ, задающий: триггер заморозки деплоев (бюджет упал до 0 → стоп feature-деплои, кроме P0-фиксов и security-патчей); триггер постмортема (один инцидент сжигает ≥20% бюджета); критерий выхода из freeze (возобновить, когда бюджет восстановится выше ~50%); путь эскалации (freeze дольше ~2 недель → VP/CTO); и out-of-scope исключения (load/pen-тесты, известные боты, окна обслуживания, rate-limited edge-запросы). Подписи важны, потому что без подписи на уровне директора политика — это рекомендация, которую on-call под давлением релиза заставляют переопределить; с подписью «политика говорит нет» завершает спор. SLA — другое: это контрактное обязательство перед клиентами с финансовыми штрафами, и оно должно быть слабее внутреннего SLO на 0.05-0.5pp, чтобы у команды был буфер обнаружить и починить до контрактного breach. SLO == SLA — это ловушка новичка." },
    { q: "Почему низкотрафиковые сервисы ломают наивную арифметику SLO и какие четыре стандартных средства?", a: "Когда знаменатель крошечный, один отказ доминирует в error rate: при 100 req/h один отказавший запрос в часовом окне — это 1% error rate, 10x от бюджета 99.9%, хотя один отказ не кризис. Алерт срабатывает, on-call расследует самоустранившийся blip, и доверие к SLO-алертингу рушится. Четыре средства: (1) Synthetic probes (предпочтительно) — проба каждые 10s даёт ~360 стабильных запросов/час, чтобы знаменатель не схлопывался, хотя пробы покрывают доступность, а не полную корректность пути. (2) Агрегация — трактуй несколько логически эквивалентных мелких сервисов как один SLO по объединённому трафику. (3) Более длинные окна оценки — 24h или 7d, чтобы один отказ не доминировал; компромисс — медленнее обнаружение. (4) Гибридный event-based алертинг — держи SLO для отчётности, но алертить по сырым счётчикам («больше 5 отказов за 30 минут») на очень низкотрафиковых поверхностях. Отдельно: всегда алертить, когда знаменатель равен нулю, потому что нулевой знаменатель даёт NaN burn rate, который не срабатывает и не сбрасывается." },
  ]}
/>

<Recap lang="ru">
Если ты смог восстановить каждый ответ по памяти, ты держишь костяк юнита: SLI — это пользовательское отношение good/total, бюджет = (1 − SLO) × events, а burn rate нормализует трату, каждый порог MWMBR выводится из (budget_fraction × period) / window с AND, связывающим длинное и короткое окно, пути перемножаются в потолок 0.999^N, который поднимают ретраи и hedging, error budget policy нужны подписи и исключения, чтобы быть зубастой, SLO обязан быть строже SLA, а низкотрафиковым поверхностям нужны пробы, агрегация, длинные окна или count-based алерты плюс защита от NaN.
</Recap>
