infographic hierarchy-tree-curved-line-rounded-rect-node
data
  root
    label SLI доступности только на 2xx
    children
      - label Видимая верхушка — что считает
        children
          - label HTTP 5xx → считается отказом
          - label HTTP 2xx → считается успехом
      - label Скрытый айсберг — что пропускает
        children
          - label Корректность: списание подтверждено, нет в бэкенде (200 OK)
          - label 4xx-баг: 422 из-за ошибки сериализации
          - label Медленный успех: 200 OK через 30с
          - label Тихая потеря: 0.1% уведомлений не доставлено
theme
  palette #6366f1 #f43f5e
