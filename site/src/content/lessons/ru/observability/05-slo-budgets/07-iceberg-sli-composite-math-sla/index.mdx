---
slug: 07-iceberg-sli-composite-math-sla
lang: ru
track: observability
unit: 05-slo-budgets
order: 7
title: "Iceberg SLI, математика составного SLO и SLA vs SLO"
summary: "SLI только на 2xx пропускает ошибки корректности, 4xx-баги и высоколатентные успехи — айсберг. Математика составного SLO для N сервисов последовательно — произведение; параллельный hedging и идемпотентные retry — архитектурные рычаги. SLA — контракт и жёстче SLO по определению."
estMin: 16
status: ready
lessonType: topic
level: senior
prereqs: ["observability/05-slo-budgets/06-low-traffic-burn-math"]
deepensInto: []
spiral: ["latency"]
concepts: ["iceberg-sli", "composite-slo", "sla-vs-slo", "parallel-hedging", "latency-threshold"]
sources:
  - https://sre.google/workbook/implementing-slos/
  - https://sre.google/sre-book/embracing-risk/
  - https://sre.google/workbook/error-budget-policy/
---

import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import Misconception from "~/components/layout/Misconception.astro";

<Hook>
Checkout-сервис имеет 99.9% availability SLO и он зелёный. Клиенты сообщают о дублированных списаниях. SLO не ошибочен — он измеряет не то.
</Hook>

<Crux>SLI на 2xx rate скрывает айсберг: корректность, 4xx, медленные успехи. SLO в цепочке перемножаются. SLO должен быть жёстче SLA.</Crux>

<Explanation>

## Выбор SLI: failures-айсберг

SLI, ломающий команды, — тот, что не коррелирует с болью пользователя. Классическая ошибка: определить availability SLI только на 5xx-ответах. Что это пропускает:

**4xx из-за внутреннего бага:** 422, выброшенный из-за ошибки сериализации в сервисе, возвращает код ошибки клиента — не считается как серверный отказ, не считается как нарушение SLO. Запрос пользователя упал. SLO зелёный.

**Высоколатентный успех:** Запрос, занявший 30 секунд, но вернувший 200 OK, проходит availability SLI. Пользователь испытал отказ (30 с выше любого разумного порога latency). Если нет latency SLO, SLO-система пропускает это полностью.

**Ошибки корректности:** Списание подтверждено пользователю, но отсутствует в бэкенде. Результат поиска вернул 200 с устаревшими данными трёхчасовой давности. HTTP-статус был нормальным; данные были неверными. SLI это не измерил.

**Потерянные события:** Сервис сообщений, тихо теряющий 0.1% уведомлений — нет ошибки, нет таймаута, просто нет доставки. SLI на эндпойнт отправки показывает 100% успех. Сообщение пользователя никогда не пришло.

Сениорный паттерн: **выбор SLI начинается с «какой плохой исход для пользователя?» и затем выводит измерение.**

Для checkout-сервиса плохие исходы:
- Отказ списания: → availability SLI на payment эндпойнт
- Слишком медленное списание: → latency SLI (99-й перцентиль под 3с)
- Списание подтверждено, но отсутствует в бэкенде: → correctness SLI (confirmed_charges_matched / confirmed_charges_total, из фонового reconciliation)
- Дублированное списание: → uniqueness SLI (distinct charge IDs / total charges attempted)

Несколько SLI на journey — норма. SLO-платформа агрегирует их worst-of соединением: journey SLO нарушается если *любой* из SLI нарушает свою цель.

<Inset kind="why" lang="ru">
Дублированное списание, вернувшее 2xx, проходит availability SLI — сервис был «доступен». Ошибка корректности невидима для мониторинга только на доступность. Это айсберг: доступность — видимые 10%, корректность и свежесть — 90% под ватерлинией. Команды, выпустившие 99.9% availability SLO и всё равно имеющие P0-инцидент корректности каждый квартал, обычно запускают iceberg SLI.
</Inset>

## Математика составного SLO

User journey через N независимых сервисов имеет потолок:

```
journey_slo = ∏(S₁, S₂, ..., Sₙ)
```

При независимых отказах каждый сервис должен успешно завершиться для успеха journey. Пять сервисов по 99.9% каждый: `0.999⁵ ≈ 0.995` — потолок journey 99.5%, не 99.9%.

**При зависимых отказах потолок ниже.** Разделяемая БД, шумный сосед, региональный сетевой инцидент: когда региональная зависимость деградирует, все пять сервисов могут отказывать одновременно. Предположение о независимости нарушается, и `∏Sᵢ` — оптимистичная верхняя граница. Реальная доступность journey в событии коррелированного отказа хуже, чем предсказывает математика.

**Два архитектурных рычага для поднятия потолка:**

**Параллельный hedging:** Отправить два запроса двум репликам, взять первый успешный. Вероятность что *оба* упадут: `(1 - S)²`. При 99.9% на реплику: `0.001² = 0.000001` — доступность journey вырастает до 99.9999%. Цена: двойной трафик, двойная нагрузка на бэкенд, нужно обработать «лишний» ответ (отменить или дедуплицировать). Подходит для read-heavy путей где удвоение трафика приемлемо.

**Идемпотентные retry с backoff:** При транзиентном отказе, retry с экспоненциальным backoff. Один retry превращает большинство транзиентных отказов в успехи. При 99.9% на попытку, один retry даёт: `1 - (0.001)² ≈ 99.9999%`. Цена: latency (retry добавляет минимум один round-trip). Эндпойнт должен быть идемпотентным — retry списания не может создать дублированное списание. Idempotency keys (уникальный ID попытки, генерируемый клиентом) это обеспечивают.

**Превращение архитектуры в SLO-арифметику:** «Добавить retry?» становится измеримым. Если текущий failure rate на payment-вызов — 0.05%, один retry снижает его до `(0.0005)² ≈ 0.000025%` — ниже любого порога. Выигрыш бюджета в сокращении отказов в месяц поддаётся расчёту и сравнению с ценой latency в P99-терминах.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Подход</th>
        <th class="border border-border px-3 py-2 text-left">Per-service SLO</th>
        <th class="border border-border px-3 py-2 text-left">Потолок journey</th>
        <th class="border border-border px-3 py-2 text-left">Цена</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">5 сервисов последовательно, без митигации</td>
        <td class="border border-border px-3 py-2">99.9% каждый</td>
        <td class="border border-border px-3 py-2">~99.5%</td>
        <td class="border border-border px-3 py-2">Нет</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Идемпотентные retry на критическом пути</td>
        <td class="border border-border px-3 py-2">99.9% на попытку</td>
        <td class="border border-border px-3 py-2">&gt;99.99%</td>
        <td class="border border-border px-3 py-2">Добавленная latency на retry-путях</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Параллельный hedging на одном сервисе</td>
        <td class="border border-border px-3 py-2">99.9% на реплику</td>
        <td class="border border-border px-3 py-2">&gt;99.99%</td>
        <td class="border border-border px-3 py-2">2× трафик на этот сервис</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Сокращение journey до 3 сервисов</td>
        <td class="border border-border px-3 py-2">99.9% каждый</td>
        <td class="border border-border px-3 py-2">~99.7%</td>
        <td class="border border-border px-3 py-2">Архитектурный рефакторинг</td>
      </tr>
    </tbody>
  </table>
</div>

## Latency SLO и порог счастья

Latency SLO требует порога — насколько медленно — «достаточно медленно, чтобы считать отказом»?

**Перцептивные границы из UX-исследований:**
- **100мс:** перцептивный предел «мгновенного» — ниже этого UI воспринимается как отзывчивый
- **200мс:** верхняя граница воспринимаемой немедленности для интерактивных элементов
- **1с:** порог выше которого пользователи замечают задержку
- **3с:** порог выше которого значимая доля пользователей бросает веб-запрос
- **10с+:** фактически таймаут для интерактивных use cases

Для request-driven API обслуживающего UI: 200мс — стандартный порог latency SLO. Для backend API-вызовов между сервисами: 1с — распространено. Для batch jobs: минуты-часы в зависимости от бизнес-контекста.

**Мультипороговые SLO** ловят tail-latency, прячущийся за одним перцентилем. «99% под 500мс» может проходить, пока деградированный хвост проваливает «90% под 100мс». Production-grade паттерн: определить оба порога, AND-ить с worst-of логикой. Сервис проходит latency SLO только если оба порога выполнены.

## SLA vs SLO: не путать

<Misconception id="mc-slo-vs-sla">
  <p><strong>«SLO и SLA — одно и то же.»</strong> Нет. SLA (Service Level Agreement) — контрактное обязательство перед клиентами с финансовыми штрафами за нарушение. SLO (Service Level Objective) — внутренняя инженерная цель. SLA по дизайну свободнее SLO.</p>
</Misconception>

Правильное соотношение: `SLO_внутренний > SLA_внешний` (жёстче внутренняя цель).

Типичный паттерн:
- Customer SLA: 99.9% доступности
- Внутренний SLO: 99.95% доступности
- Буфер: 0.05 процентных пункта

Буфер поглощает инциденты: когда внутренний SLO горит, команда расследует и фиксит до порога нарушения SLA. Error budget policy срабатывает на SLO, не на SLA. Клиенты не видят внутренний SLO — они видят выполнено ли SLA, что почти всегда так, потому что команда реагировала на более жёсткую внутреннюю цель.

**Ставить SLO == SLA — ловушка новичка.** Каждый внутренний инцидент становится риском нарушения контракта. Error budget для SLO-enforcement точно совпадает с error budget для правовой ответственности. Нет буфера для поглощения нормального операционного шума (деплои, деградации зависимостей, maintenance).

Размер буфера зависит от типичного времени реагирования сервиса и структуры поддержки. 0.05–0.5 процентных пункта — распространённый диапазон. Для сервиса со средним временем детекции + реагирования 4 часа, буфер 0.5pp даёт команде пространство для нахождения и фикса до нарушения SLA. Для сервиса с 2-минутным автоматическим rollback, буфер 0.05pp может быть достаточным.

<Quiz
  id="07-iceberg-quiz-1"
  lessonSlug="07-iceberg-sli-composite-math-sla"
  lang="ru"
  question="SLI checkout-journey команды считает запрос 'хорошим' если возвращается 2xx. После релиза клиенты сообщают о дублированных списаниях — но никакого SLO-горения нет. Что не так с SLI?"
  choices={[
    { label: "SLI измеряет HTTP-успех, а не корректность — дублированное списание возвращает 2xx и проходит SLI, хотя пользователь пострадал", correct: true },
    { label: "SLO-цель слишком мягкая — ужесточение до 99.99% поймало бы это", misconception: "Цель не имеет значения когда SLI не наблюдает failure mode. 99.999% SLO с 2xx-only SLI всё равно пропустит дублированные списания." },
    { label: "Пороги алертов неверны — нужно 14.4x, а не 6x", misconception: "Пороги алертинга нерелевантны — проблема в самом SLI. Если SLI не считает ошибки корректности как отказы, никакой порог их не поймает." },
    { label: "28-дневное окно слишком длинное для быстрой детекции", misconception: "Длина окна влияет на время детекции, но фундаментальная проблема — определение SLI. Даже 1-минутное окно не поймало бы ошибки корректности если SLI измеряет только HTTP-статус." },
  ]}
/>

<Quiz
  id="07-iceberg-quiz-2"
  lessonSlug="07-iceberg-sli-composite-math-sla"
  lang="ru"
  question="Внутренний SLO установлен на 99.9%, равный customer SLA. Почему это проблема?"
  choices={[
    { label: "Любое нарушение SLO одновременно является нарушением SLA — нет буфера для команды чтобы обнаружить, отреагировать и исправить до контрактного ущерба клиентам", correct: true },
    { label: "SLO всегда должен быть свободнее SLA чтобы у команды было больше гибкости", misconception: "SLO должен быть жёстче SLA, а не свободнее. Внутренняя цель должна быть труднее для нарушения чтобы был буфер перед юридическим порогом." },
    { label: "SLO и SLA используют разные методы измерения поэтому не могут быть равными", misconception: "Они могут использовать одинаковые измерения; проблема не техническая. Когда SLO равен SLA нет запаса прочности между внутренними инженерными целями и внешними контрактными обязательствами." },
    { label: "Это делает применение error budget policy невозможным", misconception: "Policy может применяться, но каждая заморозка, вызванная SLO-горением, также представляет потенциальное нарушение SLA — добавляя юридический и бизнесовый риск поверх операционного реагирования." },
  ]}
/>

</Explanation>

<KeyTakeaway>Дизайн SLI начинается с «какой плохой исход для пользователя?» — только 2xx пропускает корректность, 4xx-баги, медленные-но-успешные. Составные SLO перемножаются: 5 сервисов по 99.9% → потолок ~99.5%; идемпотентные retry и параллельный hedging — рычаги. Устанавливай внутренний SLO жёстче SLA на 0.05–0.5pp — этот буфер — разрыв между инженерным реагированием и юридической ответственностью.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="07-iceberg-retrieval"
  lang="ru"
  questions={[
    { q: "Назови три failure mode'а которые пропускает 2xx-only availability SLI для checkout-сервиса.", a: "1. Ошибки корректности: списание подтверждено пользователю, но не записано в бэкенде, возвращает 200 OK — SLI проходит, деньги пользователя пропали или заказ потерян. 2. 4xx из-за внутреннего бага: 422 Unprocessable Entity, выброшенный из-за ошибки сериализации в сервисе, обрабатывается как клиентская ошибка — не является отказом SLI, хотя запрос упал из-за серверной логики. 3. Высоколатентный успех: запрос, вернувший 200 через 30 секунд, проходит availability SLI, но представляет user-facing отказ (пользователь видел спиннер 30с или его frontend завис). Latency SLO это ловит, но только если определён. Добавить: 4. Тихие потери в downstream-очереди: запрос был подтверждён на HTTP-слое, но downstream-шаг обработки его потерял — 200 OK, никогда не обработан. Паттерн: начинай с плохих исходов для пользователей, а не с HTTP-категорий статусов." },
    { q: "Объясни как идемпотентные retry улучшают математику составного SLO и какое архитектурное условие они требуют.", a: "Улучшение: при 99.9% per-attempt success rate, один retry даёт journey success rate 1 − (failure rate)² = 1 − (0.001)² = 1 − 0.000001 = 99.9999%. Failure rate падает с 0.1% до 0.0001% — фактически устраняя транзиентные отказы из journey SLO. Цена: latency на retry-пути (минимум один дополнительный round-trip) и дополнительная нагрузка на downstream-сервис. Условие: эндпойнт должен быть идемпотентным — retry не должен производить дублированных side effects. Для payment-сервиса: retry попытки списания не должен создавать второго списания. Это требует idempotency key (уникальный ID, генерируемый клиентом для каждой оригинальной попытки, отправляемый с каждым retry). Сервер записывает ключ при первом успехе; последующие запросы с тем же ключом возвращают кэшированный результат вместо повторной обработки. Без idempotency keys retry вызывает именно ошибку корректности из раздела iceberg SLI — дублированные списания." },
    { q: "Каково правильное соотношение между SLA и SLO, и почему SLO должен быть жёстче?", a: "Правильное соотношение: SLO_внутренний > SLA_внешний (выраженное как % доступности, выше — жёстче). Внутренняя инженерная цель должна быть труднее для пропуска, чем внешнее клиентское обязательство. Типичный буфер: 0.05–0.5 процентных пункта. Почему жёстче: буфер даёт команде время обнаружить, отреагировать и исправить деградацию до нарушения порога SLA. Error budget policy срабатывает на SLO; команда расследует; исправляет; клиенты не видят нарушения. Без буфера (SLO == SLA) любое SLO-горение — также контрактный риск: команда одновременно разбирает инцидент и управляет юридической ответственностью. Размер буфера: если среднее время детекции + фикса — 4 часа и бюджет горит при 6x, error budget поглощает ~6% за 4 часа (6x × 4ч / 720ч ≈ 3.3%). Буфер 0.05pp на 30-дневном бюджете — примерно 3.6 часа 6x горения — в самый раз. Для сервисов с более медленным временем реагирования используй больший буфер." },
  ]}
/>

<Recap lang="ru">
Дизайн SLI должен начинаться с user-facing плохих исходов, а не с HTTP-статусов. 2xx-only availability SLI пропускает ошибки корректности, 4xx из-за внутренних багов, высоколатентные успехи и тихие потери — айсберг под ватерлинией. Несколько SLI на journey (доступность + latency + корректность), агрегированных с worst-of логикой — production-паттерн. Математика составного SLO перемножается: N сервисов последовательно по 99.9% даёт потолок 0.999^N. Идемпотентные retry и параллельный hedging — основные архитектурные рычаги для поднятия потолка, каждый с определённой ценой. SLA — контракт с клиентами с финансовыми штрафами; SLO — внутренняя цель. Установка SLO жёстче SLA на 0.05–0.5pp создаёт буфер позволяющий команде реагировать до начала юридической ответственности. SLO == SLA — ловушка новичка.
</Recap>
