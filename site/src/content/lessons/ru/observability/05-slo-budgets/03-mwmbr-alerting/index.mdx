---
concepts:
- mwmbr
- burn-rate-alert
- slo-alerting
- dual-window
deepensInto: []
estMin: 15
lang: ru
lessonType: topic
level: middle
order: 3
prereqs:
- observability/05-slo-budgets/02-sli-selection-and-slo-targets
slug: 03-mwmbr-alerting
sources:
- https://sre.google/workbook/alerting-on-slos/
- https://grafana.com/blog/how-to-implement-multi-window-multi-burn-rate-alerts-with-grafana-cloud/
- https://www.datadoghq.com/blog/burn-rate-is-better-error-rate/
spiral:
- latency
status: ready
summary: Одно-оконные SLO-алерты либо шумные (короткое окно), либо медленно гасятся
  (длинное). Паттерн MWMBR — AND между длинным и коротким окнами — даёт и быструю
  детекцию, и быстрый сброс.
title: 'Multi-window multi-burn-rate-алертинг: почему AND лучше OR'
track: observability
unit: 05-slo-budgets
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import NumbersCard from "~/components/layout/NumbersCard.astro";
import FadedExample from "~/components/pedagogy/FadedExample.tsx";
import DiagramFrame from "~/components/diagram/DiagramFrame.astro";

<Hook>
On-call-команда использует один алерт: «фаер если error rate за последний час выше 1.44%.» Пейджер срабатывает в 2 ночи. Инженер логинится. Проблема ушла сама 55 минут назад — но часовое окно всё ещё содержит плохие данные. Инженер не спит зря.
</Hook>

<Crux>MWMBR: burn rate высокий И всё ещё идёт — длинное окно подтверждает severity, короткое — что огонь живой.</Crux>

<Explanation>

## Почему наивные SLO-алерты ломаются двумя способами

SRE Workbook классифицирует шесть подходов к SLO-алертингу. Пять ломаются предсказуемо.

**Approach 1 (сырой error rate > SLO-порог):** срабатывает сотни раз в день на мелких всплесках. Один плохой батч запросов — пейдж; команды учатся игнорировать пейджер — классическая спираль alert fatigue.

**Approach 5 (одно-оконный burn-rate-порог, например 14.4x за 1ч):** лучше — ловит реальные outage'и — но сброс медленный. После устранения инцидента часовое окно ещё час содержит 55 минут плохих данных. Инженеры не могут понять, помог ли их фикс.

**Approach 6 (MWMBR)** — единственный, балансирующий задержку детекции, устойчивость к шуму и задержку восстановления. Production-default в Google, Datadog, Grafana, Splunk, Honeycomb, Sloth, Pyrra.

## Двухоконный трюк

Ключевая идея: комбинировать **длинное окно** (устойчивость к шуму) AND **короткое окно** (разрешение восстановления):

- Длинное окно (например 1ч): подтверждает, что горение устойчивое, а не транзиентный спайк
- Короткое окно (например 5м): подтверждает, что горение **всё ещё происходит прямо сейчас**

Логика AND:
- Только длинное окно высокое: инцидент, вероятно, завершился; короткое очистилось
- Только короткое окно высокое: короткий спайк, ещё не накопившийся в устойчивое горение
- **Оба** высокие: реальный outage, и серьёзный, и живой — пейджить

Когда инцидент устраняется, короткое окно очищается в течение 5 минут. Алерт гаснет в течение 5 минут, а не 55. Задержка детекции и время сброса оба ограничены.

<DiagramFrame
  label="Две проверки burn-rate-окон ведут к AND-гейту. Быстрое окно (1ч при burn > 14.4x) сработало, и медленное окно (6ч при burn > 6x) сработало; поскольку оба истинны, AND-гейт выдаёт живой PAGE. Если бы любое из окон было ниже своего порога, AND был бы ложным и алерт бы не сработал."
  hue="var(--d-systems)"
  caption="Для срабатывания AND должны сработать оба окна: медленное 6ч-окно подтверждает, что горение серьёзное и устойчивое, быстрое 1ч-окно — что оно всё ещё живое. Любое окно само по себе оставляет AND ложным, подавляя дребезг и шум."
>
  <svg viewBox="0 0 560 260" font-family="var(--font-body)">

    <rect x="24" y="36" width="184" height="78" rx="6" fill="var(--card-2)" stroke="var(--accent)" stroke-width="1.4" />
    <text x="116" y="60" text-anchor="middle" font-size="12.5" fill="var(--ink)">Быстрое окно</text>
    <text x="116" y="80" text-anchor="middle" font-size="11" font-family="var(--font-mono)" fill="var(--muted)">1ч · burn &gt; 14.4x</text>
    <text x="116" y="100" text-anchor="middle" font-size="11" font-family="var(--font-mono)" fill="var(--accent)">СРАБОТАЛО (true)</text>


    <rect x="24" y="146" width="184" height="78" rx="6" fill="var(--card-2)" stroke="var(--accent)" stroke-width="1.4" />
    <text x="116" y="170" text-anchor="middle" font-size="12.5" fill="var(--ink)">Медленное окно</text>
    <text x="116" y="190" text-anchor="middle" font-size="11" font-family="var(--font-mono)" fill="var(--muted)">6ч · burn &gt; 6x</text>
    <text x="116" y="210" text-anchor="middle" font-size="11" font-family="var(--font-mono)" fill="var(--accent)">СРАБОТАЛО (true)</text>


    <path d="M208 75 H300 V112 H336" fill="none" stroke="var(--hairline-strong)" stroke-width="1.4" />
    <path d="M208 185 H300 V148 H336" fill="none" stroke="var(--hairline-strong)" stroke-width="1.4" />


    <path d="M336 96 H372 A32 32 0 0 1 372 164 H336 Z" fill="var(--card)" stroke="var(--ink)" stroke-width="1.6" />
    <text x="356" y="134" text-anchor="middle" font-size="13" fill="var(--ink)">AND</text>


    <path d="M404 130 H452" fill="none" stroke="var(--hairline-strong)" stroke-width="1.4" />


    <rect x="452" y="104" width="92" height="52" rx="6" fill="var(--accent)" />
    <text x="498" y="128" text-anchor="middle" font-size="12.5" fill="var(--card)">PAGE</text>
    <text x="498" y="146" text-anchor="middle" font-size="10" fill="var(--card)">срабатывает</text>

    <text x="356" y="196" text-anchor="middle" font-size="10" fill="var(--muted)">оба true → фаер</text>
  </svg>
</DiagramFrame>

## Каноническая MWMBR-конфигурация (99.9% SLO)

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Severity</th>
        <th class="border border-border px-3 py-2 text-left">Длинное окно</th>
        <th class="border border-border px-3 py-2 text-left">Короткое окно</th>
        <th class="border border-border px-3 py-2 text-left">Burn rate</th>
        <th class="border border-border px-3 py-2 text-left">Бюджет при устойчивом горении</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2 font-semibold text-red-600">Page</td>
        <td class="border border-border px-3 py-2">1ч</td>
        <td class="border border-border px-3 py-2">5м</td>
        <td class="border border-border px-3 py-2 font-mono">&gt;14.4x</td>
        <td class="border border-border px-3 py-2">2% за 1ч — быстрый outage, срочно</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2 font-semibold text-orange-500">Page</td>
        <td class="border border-border px-3 py-2">6ч</td>
        <td class="border border-border px-3 py-2">30м</td>
        <td class="border border-border px-3 py-2 font-mono">&gt;6x</td>
        <td class="border border-border px-3 py-2">5% за 6ч — умеренное устойчивое горение</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2 font-semibold text-yellow-600">Ticket</td>
        <td class="border border-border px-3 py-2">3д</td>
        <td class="border border-border px-3 py-2">6ч</td>
        <td class="border border-border px-3 py-2 font-mono">&gt;1x</td>
        <td class="border border-border px-3 py-2">10% за 3д — медленное горение, требует внимания</td>
      </tr>
    </tbody>
  </table>
</div>

<NumbersCard
  id="03-mwmbr-numbers"
  title="Канонические числа MWMBR на 99.9% SLO"
  stats={[
    { label: "14.4x burn rate = error rate", value: "1.44% (при 99.9% SLO)" },
    { label: "14.4x за 1ч = потрачено бюджета", value: "~2% от 28-дневного бюджета" },
    { label: "6x за 6ч = потрачено бюджета", value: "~5% от 28-дневного бюджета" },
    { label: "1x за 3д = потрачено бюджета", value: "~10% от 28-дневного бюджета" },
    { label: "Время сброса алерта (5м короткое окно)", value: "&lt;5 минут после фикса" },
    { label: "Время сброса алерта (одиночное 1ч окно)", value: "до 55 минут после фикса" },
  ]}
/>

## Реализация на Prometheus

```yaml
# Recording rule: fast-request ratio за 1ч
record: job:slo_latency_fast:ratio_rate1h
expr: |
  sum(rate(http_request_duration_seconds_bucket{le="0.2"}[1h]))
  /
  sum(rate(http_request_duration_seconds_count[1h]))

# Page-алерт: 1ч И 5м оба выше 14.4x burn
alert: SLOLatencyBurnFast
expr: |
  (
    (1 - job:slo_latency_fast:ratio_rate1h) > (14.4 * 0.001)
    and
    (1 - job:slo_latency_fast:ratio_rate5m) > (14.4 * 0.001)
  )
labels:
  severity: page
annotations:
  summary: "Latency SLO горит с >14.4x за 1ч И 5м"
```

`14.4 * 0.001` — это `burn_rate × (1 − SLO)` = `14.4 × 0.001 = 0.0144` — порог error rate, соответствующий 14.4x burn на 99.9% SLO.

<Inset kind="why" lang="ru">
Пороги 14.4, 6 и 1 — не произвольные. Они получаются решением: «какой burn rate потратит X% бюджета за окно W?» При 30-дневном периоде: 2% за 1ч — burn = (0.02 × 720ч) / 1ч = 14.4. 5% за 6ч — burn = (0.05 × 720ч) / 6ч = 6. 10% за 3 дня (72ч) — burn = (0.10 × 720ч) / 72ч = 1. Эти пороги выводятся из первых принципов, а не эмпирической настройки — любая команда может пересчитать их для другой длины SLO-окна.
</Inset>

<FadedExample
  client:load
  id="03-faded-mwmbr"
  pieceSlug="03-mwmbr-alerting"
  lang="ru"
  title="Заполни page-алерт 6ч+30м для того же 99.9% SLO"
  steps={{
    solved: (
      <div class="space-y-3 text-sm">
        <p><strong>Полный 1ч+5м page-алерт (решён):</strong></p>
        <pre class="text-xs bg-zinc-100 p-2 rounded overflow-x-auto"><code>{`alert: SLOBurnFast
expr: |
  (1 - ratio_rate1h) > (14.4 * 0.001)
  and
  (1 - ratio_rate5m) > (14.4 * 0.001)
labels:
  severity: page`}</code></pre>
        <p>Срабатывает когда 1-часовой burn И 5-минутный burn оба выше 14.4x. 5м-окно гасит алерт в течение 5 минут после фикса.</p>
      </div>
    ),
    semi: {
      prompt: "Какой burn-rate-порог для page-алерта 6ч+30м на том же 99.9% SLO?",
      blanks: [
        { id: "br", expected: "6x", placeholder: "burn rate (например 1x, 6x, 14.4x)" },
      ],
    },
    blank: {
      prompt: "Почему алерт 6ч+30м использует более низкий burn rate, чем 1ч+5м?",
      reveal: (
        <div class="space-y-2 text-sm">
          <p>Более низкий burn rate за более длинное окно ловит <em>устойчивые умеренные горения</em>, которые 1ч+5м-алерт пропустит — outage'и, медленно поедающие бюджет на протяжении нескольких часов. Математика: burn_rate × window / period = budget_consumed. Потратить 5% за 6 часов: burn = (0.05 × 720) / 6 = 6x. У 1ч-алерта только 1 час для наблюдения горения — нужно 14.4x чтобы сработать. У 6ч-алерта больше данных — он уверен при меньшем burn rate. Две page-строки дополняют друг друга: первая ловит большие быстрые outage'и, вторая — умеренные устойчивые горения.</p>
        </div>
      ),
    },
  }}
  misconceptions={{
    "br:1x": "1x — это медленно-горящий ticket-порог. Для page на 6ч+30м нужно 6x — потребляющее 5% бюджета за 6 часов.",
    "br:14.4x": "14.4x корректен для 1ч+5м page. 6ч+30м page использует 6x — более низкий порог, ловящий устойчивые умеренные горения, которые 1ч-алерт пропустит.",
  }}
/>

<Quiz
  id="03-mwmbr-quiz-1"
  lessonSlug="03-mwmbr-alerting"
  lang="ru"
  question="Одно-оконный алерт срабатывает на 14.4x burn за 1 час. Инцидент устранён в 12:00. Когда алерт гаснет?"
  choices={[
    { label: "Около 12:55 — часовое окно ещё час содержит 55 минут плохих данных после устранения", correct: true },
    { label: "Немедленно в 12:00 — алерт оценивает текущий burn rate", misconception: "Часовое окно — скользящее среднее. В 12:00 окно содержит данные за 11:00–12:00, большинство из которых плохие. Burn rate остаётся высоким ещё ~55 минут." },
    { label: "В 13:00 — когда запускается следующее полное окно оценки", misconception: "Prometheus оценивает алерты каждые 15–30 секунд. Очистка окна не привязана к интервалам оценки, а зависит от того, сколько плохих данных ещё в окне." },
    { label: "В течение 5 минут — интервал scrape экспортёра быстро подхватывает фикс", misconception: "Интервал scrape нерелевантен. Алерт использует 1-часовой rate — числитель медленно уменьшается по мере того, как плохие данные уходят из окна." },
  ]}
/>

<Quiz
  id="03-mwmbr-quiz-2"
  lessonSlug="03-mwmbr-alerting"
  lang="ru"
  question="MWMBR-алерт срабатывает когда 1ч burn выше 14.4x И 5м burn выше 14.4x. 1ч burn — 15x, 5м burn — 12x (чуть ниже порога). Сработает ли алерт?"
  choices={[
    { label: "Нет — оба условия должны выполняться. 5м burn ниже порога, AND — ложь.", correct: true },
    { label: "Да — 1ч burn на 15x достаточно серьёзный сам по себе", misconception: "MWMBR требует ОБОИХ: длинного и короткого окна выше порога. Проверка короткого окна подтверждает, что горение всё ещё происходит прямо сейчас." },
    { label: "Да — 12x на 5м окне всё равно очень высоко", misconception: "12x высоко, но порог — 14.4x. AND требует обоих выше порога. Это намеренно: если 5м burn ниже 14.4x, ситуация может самоустраняться." },
    { label: "Зависит от настройки 'for: 5m' в Alertmanager", misconception: "Продолжительность 'for' — отдельный механизм. AND-логика между двумя окнами — в самом PromQL-выражении, не в продолжительности алерта." },
  ]}
/>

</Explanation>

<KeyTakeaway>MWMBR-алертинг комбинирует длинное окно (устойчивость к шуму) AND короткое окно (разрешение восстановления) на каждый уровень severity. AND значит: оба окна должны одновременно превысить burn-rate-порог. Результат: быстрая детекция, сброс за &lt;5 минут после фикса, нет шума. Пороги 14.4x/6x/1x получаются из «какой burn rate потребляет 2%/5%/10% бюджета за 1ч/6ч/3д?»</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="03-mwmbr-retrieval"
  lang="ru"
  questions={[
    { q: "Назови два способа, которыми ломается одно-оконный SLO-алертинг.", a: "Короткое окно: слишком шумное — случайные всплески запускают пейджер; alert fatigue приводит к тому, что команды его игнорируют. Длинное окно: слишком медленно гасится — после устранения инцидента окно ещё содержит историческую плохую информацию, поэтому алерт продолжает срабатывать почти весь период окна, что делает невозможным понять, сработал ли фикс." },
    { q: "Почему MWMBR использует AND между окнами, а не OR?", a: "OR срабатывал бы либо на коротком спайке, либо на устойчивом горении — слишком чувствительно. AND требует обоих: длинное окно подтверждает, что горение реальное и устойчивое (устойчивость к шуму); короткое подтверждает, что горение всё ещё происходит прямо сейчас (быстрое восстановление). Пейджеры гаснут в течение 5 минут после фикса, потому что короткое окно — сигнал быстрого восстановления. Только с длинным окном алерт горит почти весь период окна после устранения." },
    { q: "Выведи burn-rate-порог 14.4x для page-алерта 1ч+5м на 99.9% SLO с 30-дневным окном.", a: "Цель: потреблять 2% бюджета за 1 час. Математика: burn_rate × window / period = budget_consumed. Решаем для burn_rate: (0.02 × 720ч) / 1ч = 14.4. 5-минутное подтверждающее окно использует тот же порог 14.4x — оно не меняет burn rate, только добавляет 'всё ещё происходит прямо сейчас?'" },
  ]}
/>

<Recap lang="ru">
Наивные SLO-алерты ломаются одним из двух способов: короткие окна шумные и срабатывают на каждом транзиентном спайке, длинные окна медленно сбрасываются и держат on-call проснувшимся после устранения инцидента. Multi-window multi-burn-rate-алертинг решает оба, комбинируя длинное окно (устойчивость к шуму) с коротким (разрешение восстановления) через AND-логику: страница срабатывает только когда горение и устойчивое, и происходит прямо сейчас. Канонические пороги — 14.4x/6x/1x на 1ч/6ч/3д — выводятся из «какой burn потребляет 2%/5%/10% бюджета за каждое окно?» При 99.9% SLO 14.4x означает 1.44% error rate; 5-минутное короткое окно гасит алерт в течение 5 минут после фикса. Самостоятельно писать MWMBR PromQL на каждый сервис ненадёжно; используй Sloth или Pyrra для декларативной генерации.
</Recap>
