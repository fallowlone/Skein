---
concepts:
- promql-burn-rate
- mwmbr-rule
- latency-bucket-sli
- nan-denominator
- sloth-declaration
estMin: 14
lang: ru
level: senior
order: 9
prereqs: []
slug: quiz-code
sources:
- https://sre.google/workbook/alerting-on-slos/
- https://prometheus.io/docs/practices/histograms/
- https://sloth.dev/
status: ready
summary: "Читай реальные PromQL recording rules, MWMBR-выражения, latency-bucket SLI и Sloth YAML, предсказывай поведение и выбирай фикс с наибольшим рычагом."
title: 'SLO и error budget: чтение PromQL и правил'
track: observability
unit: 05-slo-budgets
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Баги SLO прячутся в PromQL, а не в слайдах. Неверный bucket, захардкоженный budget rate, NaN-знаменатель — каждый тихо портит бюджет и алерт. Прочитай правило и выбери фикс, который senior сделает первым.
</Hook>

<Goal lang="ru">
Потренируй чтение артефактов, из которых реально собран SLO: recording rules, burn-rate-выражения алертов, latency-bucket SLI и YAML, из которого платформа их генерирует — и находи дефект.
</Goal>

## Сниппет 1 — page-правило по burn rate

```yaml
# 99.9% SLO. Задумано: пейджить, когда burn за 1h И за 5m оба превышают 14.4x.
alert: SLOAvailabilityBurnFast
expr: |
  (1 - job:slo_availability:ratio_rate1h) > (14.4 * 0.001)
  or
  (1 - job:slo_availability:ratio_rate5m) > (14.4 * 0.001)
labels:
  severity: page
```

<Quiz
  id="05-slo-budgets-code-1"
  lessonSlug="05-slo-budgets"
  lang="ru"
  question="Автор хотел MWMBR-поведение. В чём баг и что он вызывает в проде?"
  choices={[
    { label: "Оператор `or`, а не `and`. С OR одно 5m-окно пейджит на каждом транзиентном всплеске (шум), а одно 1h-окно держит алерт ~55 минут после фикса — получаешь худшее из двух. Замени `or` на `and`.", correct: true },
    { label: "Budget rate 0.001 неверен для 99.9% SLO.", misconception: "0.001 — это ровно budget rate для 99.9% SLO (1 − 0.999). Дефект — оператор OR: MWMBR требует AND, чтобы длинное окно подтверждало тяжесть, а короткое — что burn всё ещё идёт." },
    { label: "5m-окно должно использовать другой порог burn, чем 1h-окно.", misconception: "В каноническом MWMBR короткое окно использует тот же порог burn, что и парное длинное — это гейт «всё ещё происходит?», а не другая severity. Настоящий баг — OR vs AND." },
    { label: "Ничего — OR это стандартная композиция MWMBR.", misconception: "MWMBR определяется через AND между длинным и коротким окном. OR — это провальный Approach 4 из SRE workbook именно потому, что совмещает шум короткого окна с медленным сбросом длинного." },
  ]}
/>

## Сниппет 2 — latency SLI

```promql
# SLO-цель по латентности: 99% запросов под 200ms.
# Bucket-границы заданы как le: 0.1, 0.25, 0.5, 1, 2.5
latency_sli =
  sum(rate(http_request_duration_seconds_bucket{le="0.25"}[1h]))
  /
  sum(rate(http_request_duration_seconds_count[1h]))
```

<Quiz
  id="05-slo-budgets-code-2"
  lessonSlug="05-slo-budgets"
  lang="ru"
  question="Порог SLO — 200ms, но ближайший bucket — le=0.25. Что не так и какой фикс?"
  choices={[
    { label: "Нет bucket-границы на 0.2s, поэтому считаются запросы под 250ms, а не 200ms — SLI завышает «быстроту» и сливает бюджет слишком медленно. Фикс: добавь bucket le=0.2 ровно на пороге SLO, чтобы счётчик был точным, без оценки через histogram_quantile.", correct: true },
    { label: "Надо использовать histogram_quantile(0.99, ...), чтобы получить настоящий p99.", misconception: "Latency SLO — это отношение счётчиков (fast / total), а не оценка перцентиля. histogram_quantile интерполирует внутри bucket и вносит ошибку, портящую бюджет. Фикс — bucket-граница ровно на пороге, затем прямой подсчёт." },
    { label: "Диапазон [1h] слишком длинный для latency SLI.", misconception: "Диапазон влияет на сглаживание, а не на корректность. Настоящий дефект — отсутствие bucket на 0.2s, что вынуждает SLI использовать границу 0.25 и классифицировать запросы 200-250ms как «быстрые»." },
    { label: "Ничего — le=0.25 достаточно близко к 200ms.", misconception: "«Достаточно близко» тихо смещает бюджет: каждый запрос между 200ms и 250ms засчитывается как хороший, хотя должен жечь бюджет. Порог SLO и bucket-граница гистограммы должны совпадать точно." },
  ]}
/>

## Сниппет 3 — NaN-знаменатель

```promql
# Recording rule для низкотрафикового внутреннего сервиса.
record: job:slo_availability:ratio_rate5m
expr: |
  sum(rate(http_requests_total{status!~"5..",job="reports"}[5m]))
  /
  sum(rate(http_requests_total{job="reports"}[5m]))
# В тихое окно эта серия вычисляется в NaN.
```

<Quiz
  id="05-slo-budgets-code-3"
  lessonSlug="05-slo-budgets"
  lang="ru"
  question="Отношение уходит в NaN в тихие периоды на этом низкотрафиковом сервисе. Что это значит для SLO и какой senior-фикс?"
  choices={[
    { label: "Нулевой трафик делает знаменатель нулём, поэтому отношение NaN — burn-rate-алерт тогда не срабатывает и не сбрасывается, тихо ломая SLO. Фикс: добавь synthetic probes, чтобы знаменатель не был нулём, и отдельно алертить, когда sum(rate(... total ...)) == 0.", correct: true },
    { label: "NaN означает, что сервис достиг 100% доступности без ошибок.", misconception: "NaN — это не 100%, а 0/0 из-за отсутствующего знаменателя. Алерт вычисляется против NaN и остаётся в неопределённом состоянии, что хуже ложного отрицания, потому что никто не замечает." },
    { label: "Фикс — снизить SLO-цель, чтобы отношение оставалось определённым.", misconception: "Цель не связана с нулевым знаменателем. Структурный фикс для низкотрафикового NaN — synthetic probes (стабилизировать знаменатель), агрегация по родственным сервисам или более длинное окно оценки — плюс отдельный no-traffic алерт." },
    { label: "Переключись с rate() на increase() и NaN исчезнет.", misconception: "increase() за окно без трафика всё равно даёт ноль, поэтому отношение всё ещё 0/0. Корневая причина — отсутствие трафика в знаменателе, а не выбор rate vs increase." },
  ]}
/>

## Сниппет 4 — декларация Sloth

```yaml
version: "prometheus/v1"
service: checkout
slos:
  - name: availability
    objective: 99.5
    sli:
      events:
        error_query: sum(rate(http_requests_total{status=~"5..",job="checkout"}[{{.window}}]))
        total_query: sum(rate(http_requests_total{job="checkout"}[{{.window}}]))
    alerting:
      page_alert:   { labels: { severity: page } }
      ticket_alert: { labels: { severity: ticket } }
```

<Quiz
  id="05-slo-budgets-code-4"
  lessonSlug="05-slo-budgets"
  lang="ru"
  question="Коллега говорит, что это рискованно, ведь надо вручную держать шесть recording rules и три MWMBR-алерта на сервис. Почему здесь этот аргумент неверен?"
  choices={[
    { label: "Sloth генерирует все шесть ratio_rate recording rules (по одному на окно) и три MWMBR-алерта с корректной AND-логикой и порогами burn, пересчитанными под цель 99.5% — из этой одной декларации. Ты объявляешь SLI и цель один раз; математика не может разъехаться по сервисам.", correct: true },
    { label: "Всё равно рискованно — Sloth не умеет пересчитывать пороги burn под не-99.9% цель вроде 99.5%.", misconception: "Пересчёт — это ровно то, что делает генератор: он выводит budget rate под цель (здесь 0.005) и эмитит корректные выражения алертов. Именно рукопись путает 0.005; Sloth — нет." },
    { label: "Коллега прав; PromQL надо писать руками ради аудируемости.", misconception: "Рукописный PromQL по многим сервисам — задокументированный источник тихих багов SLO (неверное окно, неверный budget rate). Декларативная генерация даёт аудируемость через YAML плюс консистентный, корректный вывод." },
    { label: "Sloth генерирует только дашборды, не алерты, поэтому правила всё равно ручные.", misconception: "Sloth эмитит Prometheus recording rules И MWMBR alert rules (плюс опционально Grafana-дашборды) из декларации. Генерируется весь стек алертинга, а не только дашборды." },
  ]}
/>

<Recap lang="ru">
Каждый дефект SLO читается в правилах: MWMBR нужен `and` между длинным и коротким окном (OR — это провальный Approach 4); latency SLI нужен bucket гистограммы ровно на пороге SLO, иначе он тихо просчитывается и смещает бюджет; нулевой знаменатель на низкотрафиковых сервисах даёт NaN, который не срабатывает и не сбрасывается, поэтому добавь synthetic probes плюс no-traffic алерт; а платформа вроде Sloth генерирует все шесть recording rules и три MWMBR-алерта — с budget rate, корректно пересчитанным под цель — из одного YAML, и именно поэтому рукописный PromQL — источник багов.
</Recap>
