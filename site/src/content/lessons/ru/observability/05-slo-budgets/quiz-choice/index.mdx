---
concepts:
- error-budget
- burn-rate
- mwmbr
- composite-slo
- error-budget-policy
- sla-vs-slo
estMin: 13
lang: ru
level: senior
order: 7
prereqs: []
slug: quiz-choice
sources:
- https://sre.google/workbook/implementing-slos/
- https://sre.google/workbook/alerting-on-slos/
- https://sre.google/workbook/error-budget-policy/
status: ready
summary: "Тест с выбором на синтез по всему юниту — арифметика error budget, вывод burn rate, AND-логика MWMBR, composite-потолки и организационные сбои."
title: 'SLO и error budget: тест с выбором ответа'
track: observability
unit: 05-slo-budgets
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Шесть вопросов поперёк всего юнита. Каждый — это решение, которое ты принимаешь на реальном reliability-ревью: не определение для заучивания, а число, которое надо посчитать, и компромисс, который надо обосновать.
</Hook>

<Goal lang="ru">
Убедись, что связываешь выбор SLI, арифметику бюджета, вывод burn rate, MWMBR-алертинг, composite-математику пути и организационный слой — тот синтез, к которому вели все восемь уроков.
</Goal>

<Quiz
  id="05-slo-budgets-choice-1"
  lessonSlug="05-slo-budgets"
  lang="ru"
  question="Сервис держит 99.9% SLO по доступности на окне 28 дней при 1M запросов/день. На 14-й день обслужено 14M запросов и накопилось 21 000 отказов. Насколько здоров бюджет?"
  choices={[
    { label: "Бюджет почти исчерпан: error budget на 28 дней = 0.001 × 28M = 28 000 отказов, и 21 000 — это 75% потрачено на середине окна, burn ~1.5x, breach наступит до конца окна.", correct: true },
    { label: "Норм — 21 000 ниже общих 28 000, запас есть.", misconception: "Важна позиция в окне. На 14-й день ожидаемая трата — половина бюджета (14 000). Потрачено 75% бюджета при оставшихся 50% окна — burn ~1.5x, который исчерпывает бюджет раньше срока." },
    { label: "Немедленный freeze — бюджет потрачен больше чем наполовину.", misconception: "Большинство политик замораживают при нулевом остатке, а не при 75% траты. Команде стоит притормозить рискованные деплои и следить за burn, но триггер freeze ещё не достигнут." },
    { label: "Нельзя сказать без значения SLA-контракта.", misconception: "Error budget выводится из SLO и трафика, а не из SLA. Budget = (1 − SLO) × total_events; SLA — отдельное, более слабое обязательство перед клиентом." },
  ]}
/>

<Quiz
  id="05-slo-budgets-choice-2"
  lessonSlug="05-slo-budgets"
  lang="ru"
  question="On-call копирует page-правило 1h+5m с сервиса на 99.9% SLO — expr (1 − ratio_rate1h) &gt; (14.4 × 0.001) — на сервис с SLO 99.5%, не меняя выражение. Что ломается?"
  choices={[
    { label: "Порог теперь неверен: при 99.5% SLO budget rate = 0.005, поэтому burn 14.4x означает error rate 14.4 × 0.005 = 7.2%. Захардкоженный 0.001 заставляет алерт срабатывать на 1.44% — слишком чувствительно, пейджит на нормальной вариации.", correct: true },
    { label: "Ничего — порог burn 14.4x не зависит от SLO-цели.", misconception: "Множитель burn (14.4x) тот же, но error rate, в который он отображается, равен burn × (1 − SLO). Скопированный 0.001 кодирует бюджет 99.9%; для 99.5% он должен стать 0.005, иначе алерт срабатывает ~в 5 раз охотнее." },
    { label: "Алерт вообще не сработает, ведь 99.5% — более слабая цель.", misconception: "Он срабатывает чаще, а не реже. Оставшийся 0.001 ставит точку срабатывания на 1.44% error rate, который сервис на 99.5% SLO превышает рутинно — это противоположность «никогда не сработает»." },
    { label: "Обновить надо только короткое (5m) окно; 1h-окно наследует цель автоматически.", misconception: "Оба окна сравниваются с одним и тем же захардкоженным budget rate. Именно поэтому рукописный PromQL подвержен ошибкам, а платформы вроде Sloth генерируют его из объявленной цели." },
  ]}
/>

<Quiz
  id="05-slo-budgets-choice-3"
  lessonSlug="05-slo-budgets"
  lang="ru"
  question="MWMBR page-правило срабатывает, когда burn за 1h И burn за 5m оба превышают 14.4x. Во время инцидента 1h-burn = 18x, но 5m-burn упал до 9x. Должно ли пейджить и что говорит каждое окно?"
  choices={[
    { label: "Не пейджит: AND ложно. 1h-окно подтверждает, что только что был тяжёлый устойчивый burn, но 5m-окно говорит, что он больше не идёт — инцидент гаснет, будить кого-то бессмысленно.", correct: true },
    { label: "Да — 1h-burn в 18x явно выше порога, и это авторитетный сигнал.", misconception: "MWMBR намеренно требует ОБА окна. Короткое окно — это гейт «всё ещё происходит сейчас?»; при нём ниже порога AND ложно и page подавляется, избегая побудки на уже восстанавливающийся инцидент." },
    { label: "Да — 9x на 5m-окне всё ещё высокий burn.", misconception: "9x высок в абсолюте, но ниже порога page 14.4x. AND требует оба окна за чертой; падающее короткое окно — это recovery-сигнал, который держит page тихим." },
    { label: "Зависит от длительности 'for' в Alertmanager, а не от окон.", misconception: "AND между двумя окнами живёт в самом PromQL-выражении. 'for' — отдельный механизм дебаунса и не реализует dual-window логику." },
  ]}
/>

<Quiz
  id="05-slo-budgets-choice-4"
  lessonSlug="05-slo-budgets"
  lang="ru"
  question="Путь checkout проходит через API gateway, auth, inventory, payment и database, каждый держит независимый 99.9% SLO, все зелёные. Пользовательский success rate checkout — ~99.5%. Как это читать и чинить?"
  choices={[
    { label: "Composite SLO перемножаются: запрос хорош, только если успешны все пять, поэтому потолок 0.999^5 ≈ 99.5%, а не 99.9%. Добавь journey-level SLI на gateway (успешные checkout / попытки) и ранжируй per-service failure rate, чтобы найти доминирующие 1-2 источника.", correct: true },
    { label: "Один из per-service дашбордов врёт — 99.9% по пяти сервисам всё равно ~99.9%.", misconception: "Per-service SLO композируются как произведение, а не среднее или максимум. Пять независимых сервисов по 99.9% дают 0.999^5 ≈ 99.5%; дашборды все верны, просто меряют не тот слой для пользовательского пути." },
    { label: "Поднять каждый сервис до 99.99%, чтобы путь достиг 99.9%.", misconception: "Математически работает (0.9999^5 ≈ 99.95%), но это дорогой рычаг — каждая лишняя девятка стоит 3-10x. Дешевле journey-level SLI плюс работа по Парето-доминирующим источникам или idempotent-ретраи / hedging на худшем хопе." },
    { label: "28-дневное окно прячет отказы; укоротить до 7 дней.", misconception: "Длина окна меняет скорость обнаружения, а не composite-потолок. Путь структурно ограничен ~99.5% при любом окне; поднимают его только journey-level измерение и архитектурные рычаги." },
  ]}
/>

<Quiz
  id="05-slo-budgets-choice-5"
  lessonSlug="05-slo-budgets"
  lang="ru"
  question="SLI доступности checkout считает любой 2xx «хорошим». Он стабильно зелёный, но клиенты сообщают о двойных списаниях и 30-секундных «успешных» checkout. Почему SLO это пропускает и какой senior-фикс?"
  choices={[
    { label: "Это iceberg SLI: и двойное списание, и 30-секундный успех возвращают 2xx, поэтому HTTP-статус прячет ошибки корректности и латентности. Фикс: выводи SLI из плохих пользовательских исходов — добавь latency SLI и uniqueness/correctness SLI из reconciliation — и соединяй worst-of.", correct: true },
    { label: "SLO-цель слишком мягкая; ужесточение до 99.99% выловило бы дубли.", misconception: "Никакая цель не ловит отказ, который SLI не наблюдает. 99.999% SLO с SLI только по 2xx всё равно засчитает двойное списание как «хорошее». Дефект — в индикаторе, а не в цели." },
    { label: "Пороги burn-rate выставлены неверно; 14.4x надо заменить на 6x.", misconception: "Пороги нерелевантны, когда SLI не считает отказ. Если ошибки корректности невидимы индикатору, никакой тюнинг алертов их не выловит." },
    { label: "Composite-математика неверна — пять сервисов надо мерить раздельно.", misconception: "Композиция — другая задача. Здесь один SLI не отслеживает корректность и латентность; фикс — несколько SLI по пользовательским исходам, соединённых worst-of, а не нарезка per-service." },
  ]}
/>

<Quiz
  id="05-slo-budgets-choice-6"
  lessonSlug="05-slo-budgets"
  lang="ru"
  question="Через полгода после раскатки SLO на 80 сервисов с корректно сгенерированными MWMBR-алертами и подписанной error budget policy половина команд игнорирует пейджи, а бюджеты уходят в минус без freeze. Какова наиболее вероятная корневая причина?"
  choices={[
    { label: "Организационная, а не техническая: SLI не коррелируют с пользовательской болью (алерты срабатывают на сигналы, которых пользователь не ощущает, и команды учатся считать page шумом), а политику переопределяют без enforcement — паттерн сбоя GitHub-2023 / неисполняемой политики.", correct: true },
    { label: "Пороги burn-rate слишком низкие; подъём 14.4x до 20x срежет шум.", misconception: "Если SLI неверен, никакой порог это не чинит — идеально откалиброванный burn на бессмысленном индикаторе всё равно пейджит на не-проблемы, а подъём порогов задерживает реальное обнаружение. Вина — в определении SLI и enforcement политики." },
    { label: "Миграция со Sloth на коммерческую платформу вроде Nobl9 починила бы внедрение.", misconception: "Лучший тулинг не чинит организационное бутылочное горло. Команды, меняющие инструмент без починки SLI и политики, получают те же дашборды, на которые никто не реагирует. Сначала контракт, потом инструменты." },
    { label: "28-дневное окно слишком длинное; 7-дневное заставило бы действовать.", misconception: "Укорачивание окна меняет каденс freeze, а не релевантность алертов и не зубастость политики. Отрицательный бюджет без freeze — это разрыв в enforcement, а не проблема длины окна." },
  ]}
/>

<Recap lang="ru">
Сквозная линия юнита — одна цепочка арифметики, ставшей организационной: SLI = good/total (и он обязан отслеживать боль пользователя, а не 2xx), SLO — это цель, error budget = (1 − SLO) × events, а burn rate нормализует трату. Алертинг выводится из burn = (budget_fraction × period) / window, срабатывает по длинному-AND-короткому окну, чтобы быть и устойчивым к шуму, и быстрым на сброс; не забудь пересчитать budget rate при смене SLO-цели. Пути перемножаются (0.999^5 ≈ 99.5%), iceberg SLI прячут корректность за 2xx, SLO обязан быть строже SLA, и фреймворк приносит пользу, только когда подписанная и исполняемая политика превращает число в решение.
</Recap>
