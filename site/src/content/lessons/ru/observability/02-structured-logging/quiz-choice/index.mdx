---
concepts:
- log-schema
- log-levels
- log-sampling
- pii-redaction
- trace-context
- audit-logs
estMin: 13
lang: ru
level: senior
order: 7
prereqs: []
slug: quiz-choice
sources:
- https://opentelemetry.io/docs/specs/otel/logs/data-model/
- https://cwe.mitre.org/data/definitions/117.html
- https://signoz.io/blog/logging-levels/
status: ready
summary: "Тест с выбором на синтез по всему юниту — контракт схемы, маршрутизация по log level, экономика sampling, защита от утечки PII и log injection, корреляция по trace, и разделение audit-логов."
title: 'Структурное логирование: тест с выбором ответа'
track: observability
unit: 02-structured-logging
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Шесть вопросов поперёк всего юнита. Каждый — это решение, которое ты принимаешь, пока сервис горит в 03:00: не определение для заучивания, а компромисс между счётом, алертом, аудитом и расследованием.
</Hook>

<Goal lang="ru">
Убедись, что связываешь контракт схемы, лестницу log level, экономику sampling, защиту от PII и injection, корреляцию по trace и разделение audit-логов — тот синтез, к которому вели отдельные уроки.
</Goal>

<Quiz
  id="02-structured-logging-choice-1"
  lessonSlug="02-structured-logging"
  lang="ru"
  question="Два сервиса одной команды пишут JSON-логи, но один называет поле http.response.status_code, а другой — status. Оба валидный JSON. Почему это всё равно ломает on-call?"
  choices={[
    { label: "Контракт — это не синтаксис JSON, а имена полей. Кросс-сервисный дашборд или алерт по http.response.status_code молча пропускает сервис с полем status, поэтому join возвращает частичный результат, и кому-то под давлением приходится знать соответствие.", correct: true },
    { label: "Один сервис на самом деле не структурный — структурным считается только тот, что с OTel-именами.", misconception: "Оба структурные: стабильные сериализованные записи с адресуемыми полями. Дефект — это schema drift, а не отсутствие структуры. Оба запрашиваемые, просто не по одному и тому же имени." },
    { label: "Фикс — заставить дашборд матчить оба имени поля регуляркой.", misconception: "Сшивать drift регуляркой — значит обнулять смысл структурных логов, и это гниёт с появлением новых вариантов. Фикс — общая схема (OTel Semantic Conventions), которую проверяют до выкатки кода, лучше через общий wrapper-логгер." },
    { label: "Ничего не ломается — backend нормализует имена полей на ingest.", misconception: "Backend индексирует те имена полей, которые получает; он не придумывает каноническое соответствие. status и http.response.status_code — это два разных индексируемых поля." },
  ]}
/>

<Quiz
  id="02-structured-logging-choice-2"
  lessonSlug="02-structured-logging"
  lang="ru"
  question="Хелпер ретраев пишет ERROR на каждой из 5 попыток, включая 3 успешные. Канал on-call стал шумом, а реальный ERROR на прошлой неделе пропустили. Какой правильный фикс и почему?"
  choices={[
    { label: "Успешные или восстановимые ретраи — это WARN; только финальный сбой, оставивший работу несделанной, — ERROR. ERROR маршрутизируется в пейдж, а пейджи на восстановимых событиях вызывают alert fatigue — самый опасный сбой, потому что реальные ошибки тонут в шуме.", correct: true },
    { label: "Понизить логи ретраев до DEBUG, чтобы они перестали пейджить.", misconception: "Исходы ретраев операционно значимы для capacity и надёжности и должны быть видны в проде; DEBUG в проде по умолчанию выключен, так что это прячет их целиком, а не маршрутизирует правильно." },
    { label: "Оставить ERROR, но поднять порог алерта, чтобы 5 ERROR подряд не пейджили.", misconception: "Пороги замазывают баг классификации и проглотят и настоящие всплески ошибок. Log level — это контракт о том, какое действие подразумевается; чинить надо уровень, а не обвязку алерта." },
    { label: "Засемплировать ERROR-строки 1-из-10, чтобы убрать шум.", misconception: "Sampling ERROR молча выбрасывает forensics сбоев; надо держать 100% WARN и ERROR. Проблема в неправильной классификации, а не в объёме." },
  ]}
/>

<Quiz
  id="02-structured-logging-choice-3"
  lessonSlug="02-structured-logging"
  lang="ru"
  question="Один сервис на 1000 req/s с JSON-логами по 1 КБ стоит ~$260/мес на ingest. Какой первый рычаг, чтобы срезать ~90% без потери возможности расследовать сбои?"
  choices={[
    { label: "Success-path sampling: держать 1-из-10 INFO-строк, сохраняя 100% WARN и ERROR, на уровне коллектора. Это срезает объём INFO на 90% и не трогает forensics сбоев.", correct: true },
    { label: "Семплировать все строки 1-из-10 равномерно — так проще рассуждать.", misconception: "Равномерный sampling теряет 90% ERROR-строк и делает расследование инцидента невозможным. Sampling всегда обязан сохранять 100% WARN и ERROR." },
    { label: "Сократить retention горячего слоя до одного дня.", misconception: "Retention tiering управляет тем, как долго ты хранишь данные, а не сколько ингестишь в день. Стоимость ingest — это объём на скорость; sampling режет скорость, retention лишь меняет горизонт хранения." },
    { label: "Зашить логику sampling в каждый сервис, чтобы она ехала с кодом.", misconception: "Per-service sampling фрагментирует политику по всему флоту без центральной видимости и делает tail sampling непрактичным. Sampling живёт на уровне коллектора ради централизованного контроля." },
  ]}
/>

<Quiz
  id="02-structured-logging-choice-4"
  lessonSlug="02-structured-logging"
  lang="ru"
  question="Хендлер интерполирует присланный пользователем текст комментария прямо в строку лог-сообщения. Пользователь шлёт комментарий с переводом строки, за которым идёт поддельный ERROR-объект JSON. Какой это класс сбоя и какой структурный фикс?"
  choices={[
    { label: "Log injection (CWE-117): перевод строки разбивает одну строку на две, и парсеры и SIEM-правила ниже по потоку считают поддельную вторую строку настоящей записью. Чинить структурно — передавать пользовательский ввод типизированным полем, чтобы JSON-сериализатор его экранировал, а не частью строки сообщения.", correct: true },
    { label: "Баг кодировки из-за не-ASCII ввода; нормализовать в UTF-8 перед логированием.", misconception: "Дефект структурный — перевод строки как разделитель записей, — а не вопрос кодировки символов. Нормализация UTF-8 никак не останавливает поддельную лог-строку." },
    { label: "Безвредно — это просто текст в поле сообщения.", misconception: "Поддельные лог-строки приводили к реальным манипуляциям с audit-trail и обходу SIEM-правил. Содержимое лог-сообщения — это поверхность ввода; Log4Shell был крайней версией того же урока." },
    { label: "Вырезать переводы строк из строки сообщения перед записью.", misconception: "Блок-лист символов хрупок и пропускает структурную JSON-инъекцию. Надёжный фикс — никогда не интерполировать ввод в строку сообщения, а передавать его типизированным атрибутом и дать сериализатору его экранировать." },
  ]}
/>

<Quiz
  id="02-structured-logging-choice-5"
  lessonSlug="02-structured-logging"
  lang="ru"
  question="Node-сервис сообщает, что ~5% лог-строк несут trace_id = '00000000000000000000000000000000'. Как это читать?"
  choices={[
    { label: "Часть путей кода пишет логи вне активного контекста запроса — колбэки setTimeout/setImmediate, непривязанные Promise или worker-треды, потерявшие AsyncLocalStorage. Значение из всех нулей — это то, что эмитится, когда на месте вызова нет активного span.", correct: true },
    { label: "OTel SDK глобально неправильно сконфигурирован.", misconception: "Глобальная ошибка конфигурации обнулила бы почти 100% строк, а не 5%. Малая конкретная доля указывает на отдельные async-пути, теряющие контекст исполнения." },
    { label: "Sampling на коллекторе срезал trace_id с этих строк.", misconception: "Sampling выбрасывает целые записи, а не отдельные поля внутри записи. trace_id отсутствует потому, что его так и не заполнили, а не убрали ниже по потоку." },
    { label: "Backend портит trace ID на ingest под нагрузкой.", misconception: "Backend не переписывает trace ID. Все нули — это эмитируемое значение, когда логгер не находит активного контекста: пробел в async-пропагации на стороне кода, а не баг ingest." },
  ]}
/>

<Quiz
  id="02-structured-logging-choice-6"
  lessonSlug="02-structured-logging"
  lang="ru"
  question="Команда хранит операционные логи и audit-логи (логины, выдача ролей, доступ к регулируемым данным) в одном индексе с retention 30 дней. Почему это сбой комплаенса, а не вопрос аккуратности?"
  choices={[
    { label: "Audit-логам нужен retention 1-7 лет (SOC 2 ~1 год; HIPAA/PCI до 7), append-only immutability чтобы подделка детектировалась, и узкий доступ с audit-of-audits. Общий 30-дневный индекс молча удаляет обязательные события и даёт чтение всей команде разработки.", correct: true },
    { label: "Audit-логи должны использовать не-JSON кодировку, поэтому хранение их в JSON — это нарушение.", misconception: "JSON для audit-логов нормален — они разделяют схему и trace_id. Нарушение — это свойства retention, immutability и доступа общего индекса, а не кодировка." },
    { label: "Единственная проблема — производительность запросов из-за смешения высокого и низкого объёма.", misconception: "Производительность вторична. Сбой комплаенса в том, что audit-события удаляются раньше обязательного retention и читаемы слишком широкой аудиторией." },
    { label: "Audit-событиям нужен другой формат trace_id, чтобы быть допустимыми.", misconception: "Audit-логи используют тот же формат trace_id; корреляция работает. Отличаются retention, immutability и слой доступа backend — маршрутизируй через атрибут category=audit в отдельный pipeline." },
  ]}
/>

<Recap lang="ru">
Сквозная линия юнита — это один конвейер решений: стабильная схема (имена полей OTel) делает логи запрашиваемыми и соединяемыми; лестница log level маршрутизирует объём в хранилище, а severity — к людям; sampling держит счёт пропорциональным инцидентам, всегда сохраняя WARN/ERROR; дисциплина PII и защита от CWE-117 injection — это заботы безопасности первого порядка, решаемые у источника и на коллекторе; trace_id делает лог-строку узлом графа observability, а не островом; и audit-логи разделяют JSON-схему, но требуют своего pipeline, retention, immutability и доступа. Каждый ответ сводится к тому, чтобы относиться к лог-строке как к API-контракту, на который полагаются и твой on-call, и твой аудитор.
</Recap>
