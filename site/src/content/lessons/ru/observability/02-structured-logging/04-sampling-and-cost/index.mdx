---
concepts:
- log-sampling
- success-path-sampling
- tail-sampling
- log-cost
- retention-tiering
- log-pipeline
deepensInto: []
estMin: 12
lang: ru
lessonType: topic
level: middle
order: 4
prereqs:
- observability/02-structured-logging/03-levels-and-routing
slug: 04-sampling-and-cost
sources:
- https://opentelemetry.io/docs/concepts/signals/logs/
- https://betterstack.com/community/guides/logging/log-levels-explained/
- https://signoz.io/blog/logging-levels/
spiral:
- latency
status: ready
summary: Success-path, pattern-based и tail-sampling — три рычага, которые держат
  счёт за логи пропорциональным инцидентам, а не объёму трафика.
title: Стратегии sampling и стоимость логов
track: observability
unit: 02-structured-logging
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import NumbersCard from "~/components/layout/NumbersCard.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";
import Infographic from "~/components/diagram/Infographic.astro";
import samplingCost from "./infographics/sampling-cost.svg?raw";

<Hook>
Команда включает структурное логирование. Через шесть недель счёт за логи втрое превышает счёт за compute. Каждый сервис, каждый запрос, каждая INFO-строка — индексируется и биллится. Логирование стоит дороже, чем обслуживание трафика. Сервис в порядке. Политики sampling нет.
</Hook>

<Crux>Sampling держит стоимость логов пропорциональной инцидентам, не трафику. Success-path, pattern-based и tail-sampling режут разные срезы.</Crux>

<Explanation>

## Уравнение стоимости

Цена log-строки платится в трёх местах: на write-time (CPU + RAM на сериализацию), в транспорте (egress + ёмкость collector'а) и на backend'е (ingest-GB + indexed-event-count + retention-байты).

Скромный сервис, эмитящий одну log-строку на запрос, обрабатывает 1 MB/s при 1000 req/s — около 86 GB/день. По ценам хостируемых логов ($0.10/GB ingest плюс indexed-event cost), счёт быстро накапливается на десятках сервисов.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Нагрузка на сервис</th>
        <th class="border border-border px-3 py-2 text-left">Суточный объём</th>
        <th class="border border-border px-3 py-2 text-left">Ежемесячная стоимость ingest</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">1000 req/s, 1 KB/лог</td>
        <td class="border border-border px-3 py-2">&#126;86 GB</td>
        <td class="border border-border px-3 py-2">&#126;$260 (один сервис)</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">То же, с 1-из-10 INFO sampling</td>
        <td class="border border-border px-3 py-2">&#126;10 GB</td>
        <td class="border border-border px-3 py-2">&#126;$30</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">10 сервисов по 1000 req/s</td>
        <td class="border border-border px-3 py-2">&#126;860 GB</td>
        <td class="border border-border px-3 py-2">&#126;$2600/месяц raw</td>
      </tr>
    </tbody>
  </table>
</div>

<Infographic
  svg={samplingCost}
  label="Столбчатая диаграмма сравнивает месячную стоимость приёма логов: один сервис без сэмплинга — около $260, тот же сервис с сэмплингом 1 из 10 INFO — около $30, а десять сервисов без сэмплинга — около $2600 в месяц."
  caption="Один и тот же трафик стоит $260, $30 или $2600 в месяц только в зависимости от политики sampling — один дешёвый рычаг (1 из 10 INFO) срезает счёт на ~90%."
  hue="var(--d-infra)"
/>

## Три стратегии sampling

**Success-path sampling** держит 1-из-N INFO-строк для high-volume успешных событий, сохраняя 100% WARN и ERROR. Типичный N — от 10 до 100. Это первый рычаг: он срезает INFO-объём на 90% без ущерба для forensics по ошибкам.

**Pattern-based sampling** держит каждый уникальный log-pattern на полной скорости и сэмплирует дубликаты. Vector и Fluent Bit поставляются с sampling-фильтрами, хешируемыми по шаблону message — так pattern «retry attempt 1 of 5» держится 1-из-100, а «payment_declined» (редкий) остаётся на 100%.

**Tail sampling для логов** повторяет trace-паттерн: буферизует логи окна запроса, затем решает по outcome — хранит все логи для failed-запросов, сэмплирует успешные. Это самая мощная стратегия, требующая stateful-буфера на tier'е collector'а. Она гарантирует нулевую потерю failure-контекста, избавляясь до 99% success-path-объёма.

Вместе три стратегии дают контроль над стоимостью на разных точках pipeline'а: success-path sampling — дешёвый первый рычаг, pattern-based — ловит болтливые дубликаты, которые первый рычаг пропускает, tail sampling — точный инструмент для нулевой потери на failure. Без хотя бы первого рычага счёт за логи отслеживает объём трафика, а не инциденты.

<FlowDiagram
  label="Гейт sampling: храним failure, сэмплируем success"
  perRow={5}
  hue="var(--d-systems)"
  nodes={[
    { id: "firehose", label: "Firehose", sub: "100% логов+трейсов" },
    { id: "gate", label: "Гейт sampler", sub: "на collector'е" },
    { id: "keep", label: "Храним 100%", sub: "ERROR / редкие" },
    { id: "sample", label: "Сэмплируем 1-из-N", sub: "INFO success" },
    { id: "store", label: "Хранилище", sub: "урезанный объём" },
  ]}
  edges={[
    { from: "firehose", to: "gate", label: "все события" },
    { from: "gate", to: "keep", label: "failures" },
    { from: "gate", to: "sample", label: "successes" },
    { from: "keep", to: "store" },
    { from: "sample", to: "store", label: "~90% отброшено" },
  ]}
  caption="Гейт держит каждый ERROR и редкое событие на 100%, сэмплирует high-volume INFO-успехи до 1-из-N и пропускает в indexed-хранилище только урезанный поток."
/>

<Inset kind="why" lang="ru">
Tier pipeline (collector / agent) — правильное место для sampling, а не приложение. Sampling на collector'е держит application-код простым и позволяет platform-команде управлять политикой централизованно. Антипаттерн — запекать sampling в каждый сервис по отдельности: это фрагментирует политику и затрудняет консистентное изменение по парку.
</Inset>

## Shipping-pipeline

Логи проходят три стадии: emit (приложение пишет JSON в stdout или logger SDK), collect (sidecar-агент или DaemonSet читает stdout, парсит JSON, батчует, применяет sampling), ship (OTLP-HTTP или native-протокол на backend).

Слой collector'а — Fluent Bit, Vector, OTel Collector с filelog-receiver — делает три вещи, которые не нужны в приложении: backpressure (буферизует на диск, если backend медленный), enrichment (прикрепляет resource-атрибуты из pod-метаданных), redaction (убирает PII-паттерны до отправки с хоста).

Продакшн-правило: emit JSON в stdout, остальное пусть делает платформа.

## Retention tiering: горячий, тёплый, холодный

Indexed-log-хранение по $0.10-$1.00/GB-месяц слишком дорого для многомесячного retention в масштабе. Зрелые стеки tier'уют:

- **Hot** (последние 7-15 дней, полный индекс, sub-second-запрос — Datadog Standard, Loki recent)
- **Warm** (30-90 дней, частичный индекс или scan-only — Datadog Flex, Splunk Frozen-Searchable)
- **Cold** (compliance-retention в S3 или эквиваленте по $0.023/GB-месяц, восстанавливается, но не query-ready напрямую)

Инцидент давностью до 7 дней работает против hot-tier с полной query-мощью. Расследование «что случилось 6 месяцев назад» требует warm-tier-запросов, которые могут занять минуты на сканирование и могут не иметь каждой dimension проиндексированной.

<NumbersCard
  id="card-log-cost-shape-lesson-ru"
  title="Структурные логи: цифры стоимости и ёмкости"
  stats={[
    { label: "Throughput pino (Node 24, 1 ядро)", value: "~140k msg/сек" },
    { label: "Throughput Winston (та же нагрузка)", value: "~20k msg/сек" },
    { label: "Типовой размер структурного лога", value: "~0.5-2 KB" },
    { label: "Сервис @ 1000 req/s, 1 лог/запрос", value: "~86 GB / день" },
    { label: "Datadog log ingest", value: "~$0.10 / GB" },
    { label: "Datadog indexed events (standard tier)", value: "~$1.27 / миллион" },
    { label: "Hot tier retention типовой", value: "7-15 дней" },
    { label: "Cold tier (S3) стоимость", value: "~$0.023 / GB-месяц" },
  ]}
/>

<Quiz
  id="04-sampling-and-cost-quiz-1"
  lessonSlug="04-sampling-and-cost"
  lang="ru"
  question="Команда применяет 1-из-10 sampling ко всем log-строкам, включая ERROR. В чём проблема?"
  choices={[
    { label: "Никакой — uniform sampling проще осмыслить", misconception: "Uniform sampling теряет 90% ERROR-строк, делая failure-forensics невозможными. Sampling должен сохранять 100% WARN и ERROR." },
    { label: "Sampling ERROR означает, что 90% failure-событий тихо отбрасываются, что ломает расследование инцидентов", correct: true },
    { label: "Sampling-ratio должен быть 1-из-100, а не 1-из-10", misconception: "Ratio не проблема; применять sampling к ERROR вообще — вот проблема." },
    { label: "Sampling должен применяться только в приложении, а не в collector'е", misconception: "Collector — предпочтительное место для sampling; проблема в sampling ERROR, а не в месте его применения." },
  ]}
/>

<Quiz
  id="04-sampling-and-cost-quiz-2"
  lessonSlug="04-sampling-and-cost"
  lang="ru"
  question="Что такое tail sampling для логов и чем он отличается от success-path sampling?"
  choices={[
    { label: "Tail sampling отбрасывает последние строки в log-файле для экономии хранилища", misconception: "Никакого отношения к позиции в файле. Tail sampling буферизует полные логи запроса и решает по outcome." },
    { label: "Tail sampling буферизует все логи окна запроса, затем сохраняет всё для failed-запросов и сэмплирует success-path — гарантируя нулевую потерю failure-контекста", correct: true },
    { label: "Tail sampling хранит первые N строк в минуту и отбрасывает остальные", misconception: "Это time-window head sampling, не tail sampling. Tail sampling решает per-request на основании outcome." },
    { label: "Tail sampling возможен только в application-коде, а не в collector'е", misconception: "Tail sampling требует stateful-буфера, но работает на tier'е collector'а (например OTel Collector tail-sampling processor)." },
  ]}
/>

<DragOrder
  id="04-sampling-and-cost-drag-1"
  lessonSlug="04-sampling-and-cost"
  lang="ru"
  prompt="Упорядочь рычаги log cost-control от самого дешёвого к самому сложному в реализации:"
  items={[
    { id: "a", label: "Поставить INFO дефолтом в продакшне, выключить DEBUG глобально" },
    { id: "b", label: "Применить success-path sampling (1-из-10 INFO, 100% WARN/ERROR) в collector'е" },
    { id: "c", label: "Добавить pattern-based sampling для схлопывания болтливых дубликатов" },
    { id: "d", label: "Настроить retention tiering: hot 15d, warm 90d, cold S3" },
    { id: "e", label: "Реализовать tail sampling с per-request буферизацией в центральном collector-gateway" },
  ]}
  correctOrder={["a", "b", "c", "d", "e"]}
/>

</Explanation>

<KeyTakeaway>Sampling — инженерный вопрос первого порядка, не afterthought. Сохраняй 100% WARN и ERROR всегда. Сэмплируй INFO 1-из-10 — 1-из-100 на success-path. Используй pattern-based sampling для болтливых дубликатов. Используй tail sampling для нулевой потери на failure. Применяй всё это на tier'е collector'а, а не в application-коде.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="04-sampling-and-cost-retrieval"
  lang="ru"
  questions={[
    { q: "Сервис эмитит 1 KB JSON-логи при 1000 req/s. Каков приблизительный месячный ingest-счёт, и какой стандартный рычаг срезает его на 90% без потери failure-forensics?", a: "Объём: 1000 req/s × 1 KB = 1 MB/s = 86 GB/день = ~2.6 TB/месяц. По $0.10/GB ingest — ~$260/месяц только за этот один сервис. Стандартный рычаг: success-path sampling 1-из-10 для INFO (100% WARN/ERROR сохраняется). Это срезает INFO-объём на 90%, снижая счёт до ~$30-50/месяц в зависимости от error-rate, без потери гарантии, что все failure сохранены. Дополни retention tiering (hot 7d, cold S3), чтобы держать audit-trail без indexed-storage-cost." },
    { q: "Почему sampling принадлежит tier'у collector'а, а не приложению?", a: "Sampling на collector'е держит application-код простым — сервисы эмитят всё, и платформа решает, что хранить. Это также позволяет централизованно управлять политикой: единый конфиг в репо platform-команды контролирует sampling-ratio по всему парку. Application-level sampling фрагментирует политику по десяткам сервисов без центральной видимости, что затрудняет аудит, изменение и консистентное применение. Collector также дает возможность tail sampling (требует буферизации окна запроса), нереального для индивидуальной реализации в каждом приложении." },
    { q: "Что такое retention tiering и почему разделение hot/warm/cold важно для incident response?", a: "Indexed-log-хранение по $0.10-$1.00/GB-месяц слишком дорого для многомесячного retention. Стандартные tier'ы: hot (7-15 дней, полный индекс, sub-second запрос), warm (30-90 дней, частичный индекс или scan-only), cold (S3 или эквивалент по $0.023/GB-месяц, восстанавливается, но не query-ready напрямую). Разделение важно для incident response: инцидент давностью до 7 дней получает полную hot-tier query-мощь с миллисекундным ответом. Расследование событий 3-месячной давности требует warm-tier-запросов, которые могут занять минуты и могут не иметь некоторых indexed dimensions. Команды должны определить per-service, на какие вопросы они должны отвечать в каком возрасте, и настроить retention соответственно." },
  ]}
/>

<Recap lang="ru">
Стоимость логов накапливается, потому что каждая структурная строка индексируется и биллится per-event и per-GB. Сервис на 1000 req/s эмитит ~86 GB/день — и у большинства парков десятки сервисов. Три sampling-рычага: success-path sampling (1-из-10 INFO, 100% WARN/ERROR) срезает объём на 90% без потери failure-контекста; pattern-based sampling схлопывает болтливые дубликаты на collector'е; tail sampling буферизует per-request-логи и хранит всё для failure, сэмплируя только success. Все три принадлежат tier'у collector'а для централизованного управления политикой. Дополни sampling retention tiering — hot (7-15d), warm (30-90d), cold (S3) — чтобы держать audit-trail без полного indexed-storage-счёта. Теперь, когда видишь счёт за логи, повторяющий кривую трафика, а не кривую инцидентов, — знаешь, за какой рычаг тянуть первым.
</Recap>
