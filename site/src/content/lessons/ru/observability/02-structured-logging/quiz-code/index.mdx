---
concepts:
- log-injection
- pii-redaction
- trace-context
- log-sampling
estMin: 14
lang: ru
level: senior
order: 9
prereqs: []
slug: quiz-code
sources:
- https://opentelemetry.io/docs/specs/otel/logs/data-model/
- https://cwe.mitre.org/data/definitions/117.html
- https://signoz.io/blog/logging-levels/
status: ready
summary: "Читай реальные лог-строки и сниппеты логгера, предсказывай поведение или дыру в безопасности и выбирай самый рычажный фикс, который senior делает первым."
title: 'Структурное логирование: чтение кода и логов'
track: observability
unit: 02-structured-logging
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Конфиг логгера и сырая лог-строка — это место, где проблемы структурного логирования реально диагностируются. Читай сниппет, предсказывай, что он эмитит или утекает, и выбирай фикс до того, как тянуться за фичей backend.
</Hook>

<Goal lang="ru">
Отработай цикл, который ты гоняешь на каждом инциденте логирования: читай место эмита или строку, предскажи инъекцию, утечку, промах или дефект sampling — и тянись за структурным фиксом первым.
</Goal>

## Сниппет 1 — интерполированное сообщение

```js
// Express-хендлер логирует присланный пользователем комментарий
logger.info(`comment received: ${req.body.comment}`);
```

<Quiz
  id="02-structured-logging-code-1"
  lessonSlug="02-structured-logging"
  lang="ru"
  question={"Пользователь шлёт комментарий, значение которого — перевод строки, за которым идёт {\"level\":\"error\",\"msg\":\"admin deleted prod db\"}. Что произойдёт и какой фикс?"}
  choices={[
    { label: "Log injection (CWE-117). Перевод строки разбивает вывод на две строки; вторая — поддельная ERROR-запись, которую SIEM-правила и audit-консьюмеры считают настоящей. Фикс: передать значение типизированным полем — logger.info({ comment: req.body.comment }, 'comment received') — чтобы сериализатор его экранировал.", correct: true },
    { label: "Шаблонный литерал кинет ошибку, потому что comment может быть undefined.", misconception: "Значение undefined приводится к строке 'undefined'; исключения нет. Реальный дефект в том, что сырой пользовательский ввод становится частью структуры лог-записи, делая возможной поддельную вторую строку." },
    { label: "Ничего — pino экранирует строку автоматически даже внутри шаблонного литерала.", misconception: "Экранирование происходит, когда значение передаётся полем в сериализатор. Здесь строка уже сконкатенирована в сообщение до того, как pino её увидел, поэтому перевод строки сохраняется буквально." },
    { label: "Только вопрос производительности — длинные комментарии раздувают лог.", misconception: "Размер не проблема. Проблема — целостность: атакующий может подделать синтетические лог-строки, которым доверяет тулинг ниже по потоку." },
  ]}
/>

## Сниппет 2 — конфиг редакции

```js
const logger = pino({
  // намерение: держать auth-токены и email вне логов
  redact: ['req.headers.authorization', 'user.email'],
});

// в другом месте, на ошибке валидации:
logger.error({ err, body: req.body }, 'signup validation failed');
```

<Quiz
  id="02-structured-logging-code-2"
  lessonSlug="02-structured-logging"
  lang="ru"
  question="Аудитор находит сырые пароли и email в индексированных логах несмотря на этот redact-конфиг. Почему редакция их пропустила и какой надёжный фикс?"
  choices={[
    { label: "Deny-list покрывает только req.headers.authorization и user.email, но error-лог дампит весь req.body — поэтому body.password и любой body.email проскакивают под другими путями. Добавь body-пути и, что критично, добавь regex-скраббер на уровне коллектора как defence-in-depth для путей, которые приложение пропускает.", correct: true },
    { label: "pino redact вообще не работает на вложенных путях.", misconception: "pino redact поддерживает вложенные пути с dot-нотацией; именно так матчится user.email. Промах в том, что body.* так и не перечислили, а дамп всего body утекает всё под ним." },
    { label: "redact слишком медленный, поэтому pino молча отключил его под нагрузкой.", misconception: "pino не отключает редакцию молча; накладные расходы около 2 мс на 1000 сообщений для нескольких полей. Утечка — это пробел в покрытии, а не откат по производительности." },
    { label: "Фикс — редактировать на backend (Datadog) вместо этого.", misconception: "Маскирование на backend — это последняя линия, а не первая: к тому моменту данные покинули хост и могут уже быть в холодном хранилище. Защищайся deny-list у источника и скраббером на коллекторе." },
  ]}
/>

## Сниппет 3 — async лог-строка

```js
app.post('/checkout', async (req, res) => {
  res.json({ ok: true });
  setTimeout(() => {
    logger.warn({ orderId }, 'post-checkout reconciliation lag');
  }, 0);
});
```

<Quiz
  id="02-structured-logging-code-3"
  lessonSlug="02-structured-logging"
  lang="ru"
  question="WARN-строка приземляется с trace_id = '00000000000000000000000000000000'. Почему и какой фикс?"
  choices={[
    { label: "Колбэк setTimeout срабатывает вне scope AsyncLocalStorage запроса, поэтому логгер не находит активного span и эмитит trace_id из всех нулей. Фикс: привязать активный контекст в колбэк (context.bind / context.with) до планирования, либо делать работу внутри scope запроса.", correct: true },
    { label: "trace_id корректен; все нули — это просто как pino рендерит валидный корневой trace.", misconception: "Все нули — это сентинел 'нет активного span на момент эмита', а не валидный trace. У настоящего корневого trace случайный ненулевой ID." },
    { label: "Коллектор срезал trace_id во время sampling.", misconception: "Sampling выбрасывает целые записи, а не отдельные поля. Поле из всех нулей, потому что его так и не заполнили — контекст потерян на async-границе." },
    { label: "Фикс — передавать trace_id вручную в каждый вызов лога.", misconception: "Ручная передача ненадёжна на масштабе — забывается в рефакторингах и сторонних вызовах. Структурный фикс — пропагировать контекст исполнения через async-границу, чтобы автоинжекция продолжала работать." },
  ]}
/>

## Сниппет 4 — правило sampling на коллекторе

```yaml
# OTel Collector / намерение pipeline-sampling: срезать объём логов на 90%
processors:
  probabilistic_sampler/logs:
    sampling_percentage: 10   # держать 10% ВСЕХ лог-записей
```

<Quiz
  id="02-structured-logging-code-4"
  lessonSlug="02-structured-logging"
  lang="ru"
  question="Этот конфиг режет счёт, но следующий инцидент невозможно расследовать. Что не так и как выглядит корректная политика?"
  choices={[
    { label: "Он семплирует равномерно по всем severity, поэтому 90% строк WARN и ERROR выброшены, и forensics сбоев нет. Корректная политика: success-path sampling — держать ~10% INFO, но 100% WARN и ERROR — через severity-aware правило, так чтобы прореживался только высокообъёмный success path.", correct: true },
    { label: "10% слишком агрессивно; 50% сохранили бы достаточно ошибок.", misconception: "Любой равномерный процент всё равно отбрасывает ту же долю ERROR-строк. Баг не в коэффициенте — баг в том, что sampling вообще применяется к ERROR." },
    { label: "Sampling должен работать в приложении, а не на коллекторе.", misconception: "Коллектор — правильное место для sampling (центральная политика, возможность tail-sampling). Дефект здесь в том, что правило слепо к severity, а не в том, где оно работает." },
    { label: "probabilistic_sampler нельзя сделать severity-aware, так что это лучшее возможное.", misconception: "Сначала маршрутизируй по severity — держи WARN/ERROR на 100%, шли через 10%-сэмплер только INFO — используя filter/routing-процессор перед probabilistic sampler." },
  ]}
/>

<Recap lang="ru">
Каждый инцидент структурного логирования читается в месте эмита, в конфиге и в сырой строке: интерполяция ввода пользователя в строку сообщения — это log injection, передавай его типизированным полем; deny-list, пропускающий body.*, утекает PII, поэтому перечисли пути и добавь скраббер на коллекторе как defence-in-depth; trace_id из всех нулей значит, что контекст исполнения потерян на async-границе — привяжи его, не передавай trace_id руками; а слепой к severity sampler выбрасывает сбои, которые тебе нужны — держи 100% WARN/ERROR и прореживай только success path. Читай сниппет, найди структурный дефект, чини у источника.
</Recap>
