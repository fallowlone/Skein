infographic sequence-snake-steps-underline-text
data
  title Где оплачивается стоимость строки лога
  desc Структурированные логи не бесплатны — счёт приходит в трёх местах, но окупается на запросе
  sequences
    - label При записи
      desc CPU + RAM на сериализацию события в JSON
    - label В транзите
      desc Сетевой egress на доставку строки в бэкенд
    - label На бэкенде
      desc Ингест в ГБ + число индексируемых событий + байты хранения
theme
  palette #6366f1 #14b8a6 #f59e0b
