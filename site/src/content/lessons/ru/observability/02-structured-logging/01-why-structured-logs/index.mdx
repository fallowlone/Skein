---
slug: 01-why-structured-logs
lang: ru
track: observability
unit: 02-structured-logging
order: 1
title: "Зачем нужны структурные логи: дневник против таблицы"
summary: "Free-text-логи выглядят читаемо и становятся незапрашиваемыми на масштабе. Структурные логи — это таблица, в которой каждая строка — запись с адресуемыми полями."
estMin: 8
status: ready
lessonType: topic
level: junior
prereqs: []
deepensInto: []
spiral: ["latency", "statefulness"]
concepts: ["structured-logging", "json-logs", "log-schema", "free-text-logs"]
sources:
  - https://opentelemetry.io/docs/concepts/signals/logs/
  - https://uptrace.dev/glossary/structured-logging
  - https://blog.sentry.io/javascript-logging-library-definitive-guide/
---

import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import MetaphorComplete from "~/components/pedagogy/MetaphorComplete.astro";
import PersonaTag from "~/components/pedagogy/PersonaTag.astro";

<Hook>
Пейджер срабатывает в 02:00. У тебя 10 миллионов log-строк и пять минут. Если логи free-text — пишешь regex и надеешься. Если логи структурные — пишешь один запрос и знаешь.
</Hook>

<Crux>Структурный лог — JSON-событие со стабильной схемой: каждая строка имеет одинаковую форму с адресуемыми полями, а не вольное предложение.</Crux>

<Explanation>

## Дневник против таблицы

Free-text-логи — это дневник. Структурные логи — таблица. Оба фиксируют, что произошло, но только одна позволяет спросить «покажи каждый payment-fail за прошлую неделю с группировкой по upstream-провайдеру» без чтения каждой записи руками.

Дневник нормален на десяти записях. На десяти миллионах он нечитаем: поля непоследовательны, форматы дрейфуют между сервисами, и каждый новый вопрос требует нового regex.

У таблицы есть правила: у каждой строки одинаковые колонки, у каждой колонки тип, каждое значение в нужной ячейке.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Формат</th>
        <th class="border border-border px-3 py-2 text-left">Пример</th>
        <th class="border border-border px-3 py-2 text-left">Запрос «status 503 от payment»</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">Free-text</td>
        <td class="border border-border px-3 py-2 font-mono text-xs">[ERROR] checkout: gateway timeout (payment, status 503)</td>
        <td class="border border-border px-3 py-2">Regex, substring-match, пропускает варианты</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Структурный JSON</td>
        <td class="border border-border px-3 py-2 font-mono text-xs">&#123;"level":"error","upstream":"payment","status":503&#125;</td>
        <td class="border border-border px-3 py-2">Один индексированный запрос, миллисекунды</td>
      </tr>
    </tbody>
  </table>
</div>

## Почему стоимость накапливается

Цена log-строки платится в трёх местах: на write-time (CPU + RAM на сериализацию), в транспорте (egress), и на backend'е (ingest-GB + indexed-event-count + retention-байты). Структурные логи не бесплатны — JSON-сериализация стоит CPU — но они окупаются на query-time.

Free-text-строка «user 42 failed checkout because gateway returned 503» требует regex- и substring-поиска. Те же данные в JSON `{"level":"error","user_id":42,"route":"/checkout","upstream":"payment-gateway","upstream_status":503,"trace_id":"abc..."}` — это один запрос по индексированному полю с миллисекундным ответом на неделях данных.

Дисциплина: выбрать схему, заполнять её консистентно и трактовать log-строку как API, который твоё будущее on-call-«я» прочитает в 03:00 без контекста.

## Сценарий триажа

<PersonaTag id="bea" lang="ru" /> получает пейджер: error rate на checkout. Запрашивает `service:checkout AND level:error AND @timestamp:[now-15m TO now]` — получает 240 матчевых событий, каждое — JSON-запись. Фасетит по полю `upstream`: 220 из 240 ошибок — от `payment-gateway`. Фасетит по `upstream_status`: все 220 — HTTP 503. Диагноз за 30 секунд.

<PersonaTag id="sven" lang="ru" /> тянет один `trace_id` из упавшей log-строки и открывает трейс, чтобы увидеть, что вызов gateway отваливается по timeout. Будь логи free-text — Bea написала бы regex, упустила бы случаи с другой формулировкой и потеряла бы 10 минут.

<Inset kind="why" lang="ru">
JSON — де-факто кодировка структурных логов в 2026, не XML, не protobuf, не custom binary. JSON — наименьший общий знаменатель: его всё ещё может прочитать человек в терминале, его умеет парсить любой logging-backend из коробки, и стандартная библиотека любого языка сериализует его дёшево. JSON Lines (один JSON-объект на строку, разделённый newline) — каноничный формат: append-friendly, streamable, parseable построчно, удобен для grep + jq.
</Inset>

<Quiz
  id="01-why-structured-logs-quiz-1"
  lessonSlug="01-why-structured-logs"
  lang="ru"
  question="Какая log-строка — структурная?"
  choices={[
    { label: '`{"ts":"2026-04-12T14:02:11Z","level":"error","service":"checkout","msg":"gateway timeout","upstream":"payment","status":503,"trace_id":"abc123"}`', correct: true },
    { label: '`[ERROR] checkout: gateway timeout (payment, status 503, trace abc123)`', misconception: "Читаемо, но free-text — нужен regex, поля не адресуемы backend-индексом." },
    { label: '`2026-04-12 14:02:11 ERROR checkout: gateway timeout on payment status=503`', misconception: "key=value в free-text помогают, но требуют парсинга — структурное значит стабильно сериализованную запись (JSON/protobuf)." },
    { label: '`ERROR 503 payment gateway timeout user 42 trace abc`', misconception: "Все нужные данные, нулевая структура." },
  ]}
/>

<DragOrder
  id="01-why-structured-logs-drag-1"
  lessonSlug="01-why-structured-logs"
  lang="ru"
  prompt="Расставь поля, которые должны быть в каждой продакшн-log-строке, примерно по приоритету:"
  items={[
    { id: "a", label: "timestamp (ISO-8601 UTC, самое базовое)" },
    { id: "b", label: "level (DEBUG / INFO / WARN / ERROR)" },
    { id: "c", label: "service.name (какой сервис эмитировал)" },
    { id: "d", label: "trace_id и span_id (join-ключ к трейсам)" },
    { id: "e", label: "message (читаемое резюме)" },
    { id: "f", label: "event-specific поля (route, status_code, user_segment, ...)" },
    { id: "g", label: "resource-атрибуты (host, region, version — обычно ставятся один раз)" },
  ]}
  correctOrder={["a", "b", "c", "d", "e", "f", "g"]}
/>

<MetaphorComplete
  id="01-why-structured-logs-metaphor-1"
  pieceSlug="01-why-structured-logs"
  lang="ru"
  setup="Заполни пропуск: структурный лог — это _______, где у каждой строки одинаковые колонки, у каждой колонки тип, и каждое значение в нужной ячейке."
  accepted={["таблица", "spreadsheet"]}
  canonical="таблица"
  explanation="Free-text-логи — это дневник: слова, без схемы, не запросить на масштабе. Структурные логи — таблица: JSON-события со стабильной схемой, адресуемыми полями и типами."
/>

</Explanation>

<KeyTakeaway>Структурные логи — JSON-события со стабильной схемой. Free-text-логи выглядят читаемо на малом масштабе и становятся незапрашиваемыми на десяти миллионах строк. Схема — это API, который on-call-«ты» будешь использовать в 03:00 — сделай его предсказуемым.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="01-why-structured-logs-retrieval"
  lang="ru"
  questions={[
    { q: "В двух предложениях — почему JSON — де-факто кодировка структурных логов в 2026?", a: "JSON — наименьший общий знаменатель: его всё ещё может прочитать человек в терминале, его умеет парсить любой logging-backend из коробки, и стандартная библиотека любого языка сериализует его дёшево. Protobuf сжимает лучше, но требует schema registry и декодер на каждом consumer'е — friction, который для логов на практике не окупился, в отличие от трейсов и метрик." },
    { q: "В чём цена бесструктурности и когда она проявляется?", a: "Цена бесструктурности платится каждый раз, когда кто-то задаёт логам вопрос: сервис, логирующий 'user X did Y' через string-интерполяцию, требует regex на каждый запрос, пропускает случаи, где формат изменился в рефакторинге, и не может ответить на кардинальные вопросы вроде 'сколько пользователей делали action=upgrade за 24h' без сканирования каждой строки. На малом масштабе выживаемо; на масштабе парка — нефункционально." },
    { q: "Почему trace_id — обязательное поле log-строки, а не опциональное?", a: "trace_id — join-ключ ко всему остальному observability. Log-строка с trace_id кликабельна в соответствующий трейс, показывающий каждый span каждого сервиса. Без trace_id log-строка — остров: ты знаешь, что что-то случилось, но не видишь окружающий request flow без timestamp-correlation-угадайки." },
  ]}
/>

<Recap lang="ru">
Структурные логи — JSON-события со стабильной схемой: каждая строка — запись одинаковой формы с timestamp, level, service.name, trace_id, message и event-specific полями. Free-text-логи фиксируют те же данные предложениями — читаемо на малом масштабе и незапрашиваемо на миллионах строк, потому что каждый запрос требует regex. Главная отдача — на query-time: индексированные JSON-поля отвечают «покажи все 503 от payment-gateway за последние 15 минут» за один запрос с миллисекундной задержкой. Цена — на write-time (CPU для сериализации) и на backend'е (indexed-события биллятся за миллион). Дисциплина — трактовать log-схему как API-контракт: консистентный между сервисами, заполняемый в каждой строке.
</Recap>
