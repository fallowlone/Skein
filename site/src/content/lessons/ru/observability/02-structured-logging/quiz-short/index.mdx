---
concepts:
- log-schema
- log-levels
- log-sampling
- pii-redaction
- trace-context
- audit-logs
estMin: 14
lang: ru
level: senior
order: 8
prereqs: []
slug: quiz-short
sources:
- https://opentelemetry.io/docs/specs/otel/logs/data-model/
- https://cwe.mitre.org/data/definitions/117.html
- https://signoz.io/blog/logging-levels/
status: ready
summary: "Промпты на свободное воспроизведение по всему юниту. Сначала ответь своими словами, затем открой эталонный ответ и сравни."
title: 'Структурное логирование: тест с краткими ответами'
track: observability
unit: 02-structured-logging
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";

<Hook>
Воспроизведение по памяти бьёт перечитывание. На каждый промпт скажи или запиши полный ответ из памяти до того, как откроешь эталон — именно усилие припоминания закрепляет материал к 03:00, когда он понадобится.
</Hook>

<Goal lang="ru">
Восстанови хребет юнита — что делает лог структурным, контракт level-к-маршрутизации, рычаги sampling и стоимости, защиту PII у источника и на коллекторе, корреляцию по trace и подсистему audit — не подглядывая в уроки.
</Goal>

<RetrievalDrawer
  client:visible
  id="02-structured-logging-short-retrieval"
  lang="ru"
  questions={[
    { q: "Что делает лог-строку структурной, а не свободным текстом, и где проявляется отдача?", a: "Структурный лог — это сериализованная запись (в 2026 — JSON), где каждая строка несёт одни и те же адресуемые типизированные поля (timestamp, level, service.name, trace_id, message плюс event-специфичные атрибуты), а не произвольное предложение. Строки key=value в свободном тексте не считаются: их всё равно надо запрашивать регуляркой, и они промахиваются мимо вариантов при дрейфе формулировок. Цена платится на записи (CPU на сериализацию) и на backend (индексируемые события тарифицируются за миллион). Отдача — на запросе: запрос по индексируемому полю отвечает 'все 503 от payment-gateway за последние 15 минут' за миллисекунды на данных за недели, тогда как свободный текст требовал бы регулярки, промахивающейся мимо случаев. Относись к схеме как к API, который твой on-call читает под давлением." },
    { q: "Назови core-поля production-схемы лога и объясни, почему важны имена полей из OTel Semantic Conventions.", a: "Фиксированный core: timestamp (ISO-8601 UTC, главный ключ сортировки), level/SeverityNumber, service.name (ключ соединения с метриками и трейсами), trace_id и span_id (ключ соединения с трейсами) и message (единственное поле, которое люди пробегают глазами). Дальше event-специфичные атрибуты и resource-атрибуты (host.name, cloud.region, service.version), задаваемые однажды на старте. Конвенциональные имена — http.response.status_code, а не status; http.route, а не path — важны, потому что они общие для сервисов, языков и вендоров: один дашборд или правило алерта работает везде единообразно без per-service трансляции. Когда команды изобретают свои имена, каждый кросс-сервисный запрос и шаг run-book обязан знать соответствие для каждого сервиса, и это вскрывается под давлением во время инцидента." },
    { q: "Что значит каждая ступень лестницы log level, как она задаёт маршрутизацию алертов и почему неправильная классификация опасна?", a: "TRACE/DEBUG выключены в проде (DEBUG только через динамический флаг). INFO — это success path, изменения состояния, позволяющие восстановить нормальное поведение. WARN — восстановимые проблемы (ретрай удался, взят fallback), маршрутизируется в дашборды/Slack. ERROR — невосстановимая несделанная работа (5xx, dead-letter, откат), пейджит on-call. FATAL — близкая смерть процесса, пейджит немедленно. Log level — это контракт о подразумеваемом действии, поэтому маршрутизация следует напрямую. Над-классификация (восстановимая проблема как ERROR) заваливает on-call и вызывает alert fatigue — самый опасный исход, потому что реальные ошибки тонут. Недо-классификация (невосстановимая проблема как WARN) делает сбои невидимыми, пока они не эскалируют." },
    { q: "Опиши три стратегии sampling и почему sampling живёт на коллекторе, а не в приложении.", a: "Success-path sampling держит 1-из-N (N=10-100) INFO-строк, сохраняя 100% WARN/ERROR — первый рычаг, режет INFO ~90% не трогая forensics сбоев. Pattern-based sampling держит каждый отдельный шаблон сообщения на полной ставке и семплирует болтливые дубликаты (Vector/Fluent Bit хешируют шаблон). Tail sampling буферизует логи запроса и решает по исходу — держать всё для упавших запросов, семплировать успешные — гарантируя нулевую потерю сбоев, но требуя stateful-буфер. Всё живёт на уровне коллектора: это держит код приложения простым, даёт платформенной команде одну центральную политику на весь флот, и это единственное место, где tail sampling (требующий буферизации поперёк запроса) практичен. Сочетай с retention tiering (горячий 7-15д, тёплый 30-90д, холодный S3 ~$0.023/ГБ-мес), чтобы держать audit trail без стоимости индексируемого хранилища." },
    { q: "Объясни два слоя защиты PII и структурную профилактику log injection (CWE-117).", a: "Слой 1 — источник: deny-list логгер-SDK (pino redact, processor structlog, handler slog, rewrite log4j2) вырезает известные чувствительные пути вроде req.headers.authorization, body.password, user.email до сериализации. Дёшево и детерминированно. Слой 2 — коллектор: regex-скраббер (OTel Collector, Fluent Bit, Vector) ловит то, что приложение пропустило — сторонние библиотеки, неожиданные дампы стектрейсов, новоподключённые сервисы — матча форматы PII независимо от имени поля. Никогда не полагайся на маскирование backend как единственный слой; к тому моменту данные уже покинули хост. Для инъекции: никогда не интерполируй ввод пользователя в строку сообщения. Передавай его типизированным атрибутом, чтобы JSON-сериализатор его экранировал, делая и newline-инъекцию, и структурную JSON-инъекцию невозможными независимо от ввода. Используй непрозрачный user_id, никогда email/имя, чтобы GDPR-erasure был разрывом соответствия ID, а не переписыванием архивов." },
    { q: "Как trace_id попадает в лог-строку автоматически, какой режим отказа и как его детектить?", a: "OTel SDK хранит активный span в request-scoped контексте исполнения языка — AsyncLocalStorage в Node, context.Context в Go (явный, протягиваемый), contextvars в Python, thread-local MDC в JVM. У логгера есть хук (mixin pino, processor structlog, handler slog), читающий активный span на момент эмита и инжектящий trace_id и span_id, так что ни одно место вызова не передаёт их вручную — а это важно, потому что ручная передача забывается в рефакторингах и сторонних вызовах. Режим отказа: async-границы теряют контекст — setTimeout/setImmediate, непривязанные Promise, горутины без родительского ctx, Python-треды — порождая trace ID из всех нулей. Детекция: мониторь долю строк с trace_id = '000...' по сервису; алерти выше 1%; добавь CI-тест, который шлёт трейснутый запрос, ждёт async-колбэки и проверяет, что каждая строка несёт входящий trace_id." },
  ]}
/>

<Recap lang="ru">
Если ты смог восстановить каждый ответ по памяти, ты держишь хребет юнита: структурный значит адресуемые типизированные поля, дающие отдачу на запросе; схема OTel и конвенциональные имена делают логи соединяемыми между сервисами; лестница log level — это контракт маршрутизации, где неправильная классификация даёт alert fatigue или невидимый сбой; sampling держит стоимость пропорциональной инцидентам, сохраняя все WARN/ERROR на коллекторе; PII защищается у источника и на коллекторе, а injection предотвращается структурно передачей ввода типизированными полями; trace_id автоинжектится из контекста исполнения и ломается на async-границах; а audit-логам нужен свой pipeline, retention, immutability и доступ. Лог-строка — это API-контракт и для твоего on-call, и для твоего аудитора.
</Recap>
