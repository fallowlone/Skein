infographic chart-column-simple
data
  title Объём логов в сутки при 1000 req/s: INFO против DEBUG (ГБ/сут)
  desc Оставленный в проде DEBUG — это ~100x потока INFO: риск по деньгам, а не только шум
  values
    - label INFO по умолчанию ~2 ГБ/сут
      value 2
    - label DEBUG забыли выключить ~200 ГБ/сут
      value 200
theme
  palette #14b8a6 #f43f5e
