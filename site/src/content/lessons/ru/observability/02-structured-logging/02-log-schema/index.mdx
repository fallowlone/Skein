---
concepts:
- log-schema
- otel-logs
- trace-id
- severity
- resource-attributes
- semantic-conventions
deepensInto: []
estMin: 12
lang: ru
lessonType: topic
level: middle
order: 2
prereqs:
- observability/02-structured-logging/01-why-structured-logs
slug: 02-log-schema
sources:
- https://opentelemetry.io/docs/specs/otel/logs/data-model/
- https://github.com/open-telemetry/opentelemetry-specification/blob/main/specification/logs/data-model.md
- https://opentelemetry.io/docs/concepts/signals/logs/
spiral:
- latency
- statefulness
status: ready
summary: Что содержит продакшн-grade структурная log-строка в 2026 — поля OTel Logs
  Data Model, resource-атрибуты, и почему схема — это API, который ожидает on-call-тулинг.
title: 'Схема продакшн-лога: поля, которые несёт каждая строка'
track: observability
unit: 02-structured-logging
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import NumbersCard from "~/components/layout/NumbersCard.astro";
import StackDiagram from "~/components/diagram/StackDiagram.astro";
import Infographic from "~/components/diagram/Infographic.astro";
import severityLadder from "./infographics/severity-ladder.svg?raw";

<Hook>
Два сервиса одной команды называют одно поле "status_code" в одном и "http_status" в другом. Каждый дашборд, который их соединяет, ломается. Фикс — не regex, а общая схема, которую проверяют до того, как код уехал в прод.
</Hook>

<Crux>Продакшн-схема лога — контракт: каждая строка несёт типизированные поля, чтобы любой сервис или on-call-запрос читал её без конфигурации.</Crux>

<Explanation>

## Что несёт каждая продакшн-log-строка

Продакшн-grade структурная log-строка в 2026 содержит как минимум эти поля, примерно в таком приоритете:

1. **`timestamp`** — ISO-8601 UTC с sub-second точностью: `2026-04-12T14:02:11.482Z`. Основной sort-ключ любого backend'а.
2. **`level`** — строковый enum: TRACE / DEBUG / INFO / WARN / ERROR / FATAL. Контролирует, что хранится и кого пейджат.
3. **`service.name`** — OTel-каноничный ключ, идентифицирующий эмиттер. Join-ключ обратно к метрикам и трейсам.
4. **`trace_id`** и **`span_id`** — когда лог эмитится внутри traced-запроса, это join-ключи к tracing-backend'у. Без них pivot log → trace не работает.
5. **`message`** — короткое читаемое резюме. Единственное поле, которое скимят люди; остальное — для машин.
6. **Event-specific поля** — плоское key-value-дерево по OTel Semantic Conventions: `http.route`, `http.response.status_code`, `db.system`, `error.type` и т. д.
7. **Resource-атрибуты** — ставятся один раз на старте: `host.name`, `cloud.region`, `service.version`, `deployment.environment`. Описывают эмиттер, не событие.

Вместе эти группы образуют фиксированный конверт, который никогда не меняет форму между сервисами — именно это делает кросс-сервисные дашборды и runbook'и рабочими без per-service перевода.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Группа полей</th>
        <th class="border border-border px-3 py-2 text-left">Примеры</th>
        <th class="border border-border px-3 py-2 text-left">Кто ставит</th>
        <th class="border border-border px-3 py-2 text-left">Частота</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">Ядро</td>
        <td class="border border-border px-3 py-2 font-mono text-xs">timestamp, level, message</td>
        <td class="border border-border px-3 py-2">Logger SDK</td>
        <td class="border border-border px-3 py-2">Каждая строка</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Идентичность сервиса</td>
        <td class="border border-border px-3 py-2 font-mono text-xs">service.name, service.version</td>
        <td class="border border-border px-3 py-2">Logger SDK + конфиг</td>
        <td class="border border-border px-3 py-2">Каждая строка</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Trace-контекст</td>
        <td class="border border-border px-3 py-2 font-mono text-xs">trace_id, span_id, trace_flags</td>
        <td class="border border-border px-3 py-2">Mixin активного span'а</td>
        <td class="border border-border px-3 py-2">Внутри traced-запросов</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Event-specific</td>
        <td class="border border-border px-3 py-2 font-mono text-xs">http.route, http.response.status_code, error.type</td>
        <td class="border border-border px-3 py-2">Код приложения</td>
        <td class="border border-border px-3 py-2">Зависит от типа события</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Resource</td>
        <td class="border border-border px-3 py-2 font-mono text-xs">host.name, cloud.region, deployment.environment</td>
        <td class="border border-border px-3 py-2">Платформа / конфиг</td>
        <td class="border border-border px-3 py-2">Один раз на старте</td>
      </tr>
    </tbody>
  </table>
</div>

## OTel Logs Data Model

OpenTelemetry Logs specification (стабильный API конец 2023, SDK конец 2024) формализует это разделение. Log-record содержит:

- **Timestamp** — когда событие фактически произошло (ставится приложением).
- **ObservedTimestamp** — когда collector увидел запись. При clock-skew или backpressure они расходятся; senior-команды алертят на расхождение как pipeline-health-метрику.
- **SeverityNumber** — числовая лестница (TRACE=1-4, DEBUG=5-8, INFO=9-12, WARN=13-16, ERROR=17-20, FATAL=21-24), чтобы backend мог сравнивать severity через библиотеки с разными текстовыми label.
- **Body** — человекочитаемое сообщение.
- **Attributes** — flat key-value-карта event-specific-данных.
- **Resource** — ставится один раз на эмиттер (service.name, host.name, cloud.region).
- **TraceId / SpanId / TraceFlags** — W3C traceparent-контекст, наследуемый от активного span'а на emit-time.

Принять эту форму — даже на не-OTel-backend'е — даёт forward compatibility: можно менять backend без переписывания инструментации, и каждый дашборд и on-call-runbook может говорить одним словарём полей.

<Infographic
  svg={severityLadder}
  label="Горизонтальная столбчатая диаграмма лестницы OTel SeverityNumber: TRACE занимает 1-4, DEBUG 5-8, INFO 9-12, WARN 13-16, ERROR 17-20, FATAL 21-24 — шесть текстовых уровней на одной восходящей числовой шкале из 24."
  caption="Каждый текстовый уровень занимает диапазон из четырёх чисел. Их отображение на одну восходящую шкалу позволяет бэкенду запросом severity_number >= 9 означать «INFO и выше» — даже между библиотеками, которые расходятся в текстовой метке."
  hue="var(--d-infra)"
/>

<StackDiagram
  label="Одна log-запись: конверт vs контекст"
  hue="var(--d-systems)"
  layers={[
    { label: "timestamp", note: "конверт · когда" },
    { label: "level", note: "конверт · severity" },
    { label: "service.name", note: "конверт · кто" },
    { label: "trace_id", note: "конверт · корреляция" },
    { label: "span_id", note: "конверт · корреляция" },
    { label: "message", note: "конверт · человек" },
    { label: "context { http.route, status_code, error.type, … }", note: "типизированные поля · событие" },
  ]}
  caption="Каждая строка — фиксированный конверт (одни и те же ключи в каждой записи), оборачивающий один типизированный context-объект (event-specific-поля). На конверт опираются запросы, join'ы и runbook'и; context несёт payload конкретного события."
/>

## OTel Semantic Conventions: одно имя поля для всех сервисов

Semantic Conventions определяют канонические имена для общих полей: `http.route` (не `route`, не `path`, не `url`), `http.response.status_code` (не `status`, не `http_status`), `db.system` (не `db_type`), `error.type`. Это значит:

- Дашборд, запрашивающий `http.response.status_code`, работает одинаково для Node- и Go-сервисов.
- Alert-правило, ссылающееся на `error.type:timeout`, работает без per-service конфигурации.
- Новый сервис, принявший схему, наследует все существующие запросы.

Цена несоблюдения conventions — в incident response: когда checkout называет поле `status`, а payment-gateway — `http_status`, join-запрос ломается, и кто-то должен знать маппинг под давлением в 03:00.

<Inset kind="why" lang="ru">
Схема — не бюрократия, а несущая конструкция. Каждый запрос, каждый дашборд, каждое alert-правило, каждый шаг runbook'а, ссылающийся на имя поля, предполагает, что оно стабильно и консистентно между сервисами. Дрейф схемы ломает их молча: запрос возвращает меньше результатов, алерт срабатывает только на части сервисов, шаг runbook'а требует ручного перевода. Централизация схемы в per-org wrapper-logger (один модуль, импортируемый каждым сервисом) превращает дрейф в build-time-ошибку, а не открытие в 03:00.
</Inset>

<NumbersCard
  id="02-log-schema-numbers"
  title="Цифры log-схемы"
  stats={[
    { label: "Стабильность OTel Logs API (GA)", value: "Конец 2023" },
    { label: "Стабильность OTel Logs SDK (большинство langs)", value: "Конец 2024" },
    { label: "Типовой размер структурной log-строки", value: "~0.5–2 KB" },
    { label: "Диапазон SeverityNumber (TRACE–FATAL)", value: "1–24" },
    { label: "OTLP/Logs gRPC payload, compressed", value: "~30–50% меньше JSON" },
  ]}
/>

<Quiz
  id="02-log-schema-quiz-1"
  lessonSlug="02-log-schema"
  lang="ru"
  question="Log-строка несёт Timestamp = 14:00:00 и ObservedTimestamp = 14:05:00. Что означает этот разрыв?"
  choices={[
    { label: "Clock drift на application-хосте — часы приложения на 5 минут вперёд", misconception: "Если Timestamp позади ObservedTimestamp — лог задержался в pipeline'е. Если бы часы приложения спешили, Timestamp был бы впереди." },
    { label: "5-минутная задержка backpressure или буферизации в pipeline'е сбора — лог эмитирован в 14:00, но collector увидел его в 14:05", correct: true },
    { label: "Log-backend применяет 5-минутный TTL", misconception: "Backend'ы не перезаписывают timestamp'ы; разрыв показывает, когда collector увидел запись против момента создания." },
    { label: "Сервис в другом часовом поясе", misconception: "Оба timestamp'а должны быть UTC; разница часовых поясов не даст такой gap." },
  ]}
/>

<Quiz
  id="02-log-schema-quiz-2"
  lessonSlug="02-log-schema"
  lang="ru"
  question="Зачем OTel Logs Data Model определяет числовой SeverityNumber (1-24) в дополнение к текстовому SeverityText?"
  choices={[
    { label: "Для экономии bandwidth — числа меньше строк", misconception: "Bandwidth — не причина; поле существует для семантической сравнимости." },
    { label: "Чтобы backend мог сравнивать severity между библиотеками с разными текстовыми label (DEBUG vs Debug vs debug), позволяя числовые range-запросы вроде severity_number >= 9 для INFO и выше", correct: true },
    { label: "Для поддержки 24 отдельных log-level вместо 6", misconception: "24 значения сгруппированы в 6 диапазонов по 4, а не 24 независимых level — каждый диапазон имеет 4 номера для внутренней гранулярности." },
    { label: "Потому что поле SeverityText устарело", misconception: "Оба поля сосуществуют; SeverityText — читаемый, SeverityNumber — машинно-сравниваемый." },
  ]}
/>

<DragOrder
  id="02-log-schema-drag-1"
  lessonSlug="02-log-schema"
  lang="ru"
  prompt="Расставь поля OTel Logs Data Model от наиболее фундаментальных к наиболее event-specific:"
  items={[
    { id: "a", label: "Timestamp — когда событие произошло" },
    { id: "b", label: "SeverityNumber — числовая severity (1-24)" },
    { id: "c", label: "Resource — per-emitter атрибуты (service.name, host.name)" },
    { id: "d", label: "TraceId / SpanId — W3C trace-контекст" },
    { id: "e", label: "Body — человекочитаемое сообщение" },
    { id: "f", label: "Attributes — event-specific flat key-value (http.route, error.type, ...)" },
  ]}
  correctOrder={["a", "b", "c", "d", "e", "f"]}
/>

</Explanation>

<KeyTakeaway>Продакшн-схема лога имеет фиксированное ядро (timestamp, level, service.name, trace_id, message), event-specific поля по OTel Semantic Conventions и resource-атрибуты, ставящиеся один раз. Консистентность схемы между сервисами — то, что делает запросы, дашборды и runbook'и рабочими без per-service перевода.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="02-log-schema-retrieval"
  lang="ru"
  questions={[
    { q: "Что такое Timestamp и ObservedTimestamp в OTel Logs Data Model, и почему важны оба?", a: "Timestamp — когда событие фактически произошло, выставляется приложением на emit-time. ObservedTimestamp — когда collector увидел запись. В норме они почти совпадают. Когда расходятся больше нескольких секунд — это сигнал clock-skew на application-хосте или backpressure в pipeline'е сбора: лог был буферизован. Senior-команды алертят, когда (ObservedTimestamp - Timestamp) p99 превышает 60 секунд, потому что time-windowed-запросы ломаются, когда логи попадают в неправильный временной bucket." },
    { q: "Почему нужно использовать имена полей OTel Semantic Conventions (http.route, http.response.status_code) вместо своих?", a: "Convention-имена полей разделяются между сервисами, языками и вендорами. Дашборд или alert-правило, написанное против http.response.status_code, работает одинаково для всех сервисов, следующих конвенциям, без per-service конфигурации. Когда команды изобретают свои имена (status, http_status, statusCode) — каждый запрос и шаг runbook'а должен знать маппинг для каждого сервиса, обнаруживаемый под давлением в 03:00." },
    { q: "Какие поля ставятся один раз при старте, а какие — на каждой log-строке?", a: "Resource-атрибуты (host.name, cloud.region, service.version, deployment.environment) описывают эмиттер и никогда не меняются за время жизни процесса — ставятся один раз при старте и прикрепляются к каждой log-записи SDK'ом. Базовые поля (timestamp, level, message) и trace-контекст (trace_id, span_id) меняются на каждое событие и ставятся logger'ом на каждом emit. Event-specific поля (http.route, error.type) варьируются по типу события и пишутся в коде приложения на каждом call-site." },
  ]}
/>

<Recap lang="ru">
Продакшн-схема лога в 2026 следует OTel Logs Data Model: Timestamp (время события), ObservedTimestamp (время collector'а), SeverityNumber (1-24) плюс SeverityText, Body, Resource (per-emitter: service.name, host.name, cloud.region), Attributes (event-specific: http.route, error.type) и TraceId/SpanId (W3C trace-контекст). Resource-атрибуты ставятся один раз при старте; trace-контекст тянется из активного span'а на emit-time; event-specific атрибуты пишутся в коде приложения. Использование имён полей OTel Semantic Conventions единообразно между сервисами — то, что делает кросс-сервисные запросы, дашборды и runbook'и рабочими без per-service перевода. Разрыв между Timestamp и ObservedTimestamp — сигнал здоровья pipeline'а: алертить на p99 больше 60 секунд. Теперь, когда видишь на дашборде, что (ObservedTimestamp - Timestamp) растёт, ты знаешь: смотреть сначала на collector или на часы приложения — и не списывать всё на «неправильные данные».
</Recap>
