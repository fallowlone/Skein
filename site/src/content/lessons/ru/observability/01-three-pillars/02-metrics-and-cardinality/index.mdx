---
concepts:
- metrics
- time-series
- counter
- gauge
- histogram
- cardinality
- label
- prometheus
- exemplar
deepensInto: []
estMin: 14
lang: ru
lessonType: topic
level: middle
order: 2
prereqs:
- observability/01-three-pillars/01-what-the-three-signals-are
slug: 02-metrics-and-cardinality
sources:
- https://prometheus.io/docs/practices/instrumentation/
- https://grafana.com/blog/how-to-manage-high-cardinality-metrics-in-prometheus-and-kubernetes/
- https://opentelemetry.io/docs/concepts/signals/
spiral:
- latency
status: ready
summary: Как работают метрики в модели Prometheus, почему cardinality — главный драйвер
  стоимости, и что происходит при добавлении одного неограниченного label.
title: 'Метрики и cardinality: cost-модель time-series database'
track: observability
unit: 01-three-pillars
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import NumbersCard from "~/components/layout/NumbersCard.astro";
import Misconception from "~/components/layout/Misconception.astro";
import DiagramFrame from "~/components/diagram/DiagramFrame.astro";
import Infographic from "~/components/diagram/Infographic.astro";
import labelDecision from "./infographics/label-decision.svg?raw";

<Hook>
Команда добавляет label `customer_email` к counter `request_total`, чтобы отслеживать частоту ошибок по клиентам. Через два часа Prometheus исчерпывает память, и 15 минут алертов теряются. Label выглядел безобидно.
</Hook>

<Crux>Cardinality — главный драйвер стоимости метрик. Один неограниченный label умножает число series на миллионы.</Crux>

<Explanation>

## Как хранится метрика

Каноническая модель — Prometheus. Метрика идентифицируется парой `(metric_name, label_set)`, где label_set — небольшой фиксированный набор пар `key=value`. Каждая уникальная комбинация значений label создаёт одну **time series** — поток сэмплов `(timestamp, value)`, обычно собираемых каждые 15 с.

Четыре стандартных типа метрик:

- **Counter** — монотонно возрастающее целое. Осмысленна только скорость изменения (в секунду). Пример: `http_requests_total`.
- **Gauge** — мгновенное значение, может расти и падать. Пример: `memory_used_bytes`, `queue_depth`.
- **Histogram** — распределяет наблюдения по предопределённым bucket'ам для percentile-запросов. Пример: `http_request_duration_seconds` с bucket'ами `[0.01, 0.05, 0.1, 0.5, 1.0, 5.0]`.
- **Summary** — предвычисленный перцентиль на реплике. Редко используется для новой инструментации: нельзя агрегировать между репликами.

Выбрав не тот тип — gauge там, где нужен histogram, или summary вместо histogram — ты теряешь либо способность вычислять перцентили между репликами, либо гибкость в будущих запросах. Тип — это контракт с самим собой на дежурстве.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Тип</th>
        <th class="border border-border px-3 py-2 text-left">Растёт/падает</th>
        <th class="border border-border px-3 py-2 text-left">Использовать для</th>
        <th class="border border-border px-3 py-2 text-left">Пример</th>
      </tr>
    </thead>
    <tbody>
      <tr><td class="border border-border px-3 py-2">Counter</td><td class="border border-border px-3 py-2">Только вверх</td><td class="border border-border px-3 py-2">Скорость событий</td><td class="border border-border px-3 py-2"><code>http_requests_total</code></td></tr>
      <tr class="bg-surface-2"><td class="border border-border px-3 py-2">Gauge</td><td class="border border-border px-3 py-2">Оба</td><td class="border border-border px-3 py-2">Текущее состояние</td><td class="border border-border px-3 py-2"><code>memory_used_bytes</code></td></tr>
      <tr><td class="border border-border px-3 py-2">Histogram</td><td class="border border-border px-3 py-2">Только вверх (bucket'ы)</td><td class="border border-border px-3 py-2">Распределение latency, перцентили</td><td class="border border-border px-3 py-2"><code>http_request_duration_seconds</code></td></tr>
      <tr class="bg-surface-2"><td class="border border-border px-3 py-2">Summary</td><td class="border border-border px-3 py-2">Предвычислен</td><td class="border border-border px-3 py-2">Перцентиль на источнике (не агрегируем)</td><td class="border border-border px-3 py-2">Устарел; предпочтительнее histogram</td></tr>
    </tbody>
  </table>
</div>

## Cardinality: главный драйвер стоимости

Каждая уникальная комбинация значений label создаёт отдельную time series в TSDB. Математика мультипликативная:

- `route` (20 шаблонов) × `method` (5 методов) × `status_class` (4 класса) = **400 series** — нормально.
- Та же метрика + `user_id` (100 к активных пользователей) = 100к × 20 × 5 × 4 = **40 миллионов series** — Prometheus падает с OOM на 16 ГБ.

Prometheus хранит около **3 КБ на активную series** в head block. При 40 М series — 120 ГБ, что далеко за пределами памяти любого рядового сервера.

Правило: **label ставь только на значения из небольшого фиксированного набора** — шаблоны маршрутов, а не полные URL; классы статусов, а не коды статусов; сегменты клиентов, а не ID клиентов. Всё с неограниченными значениями — в логи (cardinality бесплатна) или в трейсы (сэмплированы), не в метрики.

<Infographic
  svg={labelDecision}
  label="Решение о label по cardinality. Ограниченное значение безопасно как label: небольшой фиксированный набор вроде шаблонов route (20), status_class (4) и customer_segment (3) даёт 20 на 5 на 4 равно 400 series, помещается в безопасную зону около 5 миллионов series на 16 ГБ и остаётся запрашиваемым за годы, потому что предагрегировано на источнике. Неограниченное значение нельзя класть в label: high-cardinality идентификатор вроде user_id (100 тысяч) или customer_email доводит ту же метрику до 20 на 5 на 4 на 100 тысяч равно 40 миллионов series, по 3 КБ каждая около 120 ГБ, что вызывает OOM сервера, поэтому оно идёт в логи (cardinality бесплатна) или сэмплированные трейсы, а переход к конкретному запросу делается через exemplar trace_id на bucket'е histogram без стоимости series."
  caption="Каждый новый label отвечает на один вопрос: значение ограничено? Ограничено → безопасно как label (400 series). Неограничено → не клади; направь в логи или трейсы и переходи через exemplar."
  hue="var(--d-infra)"
/>

<DiagramFrame
  label="Число series — произведение числа значений каждого label. http_requests_total с route (20), method (5) и status_class (4) даёт 20 на 5 на 4 равно 400 series. Добавление одного неограниченного label, user_id со 100 тысячами значений, умножает это на 100000 до 40 миллионов series — обрыв с out-of-memory."
  hue="var(--d-systems)"
  caption="Каждый label умножает итог. Ограниченные label (20×5×4) держат 400 series; один неограниченный user_id (100 к) умножает ту же метрику до 40 М — обрыв OOM."
>
  <svg viewBox="0 0 560 250" font-family="var(--font-body)">
    <text x="20" y="24" font-size="13" fill="var(--ink)">http_requests_total</text>

    <rect x="20" y="38" width="92" height="50" rx="6" fill="var(--card-2)" stroke="var(--hairline-strong)" stroke-width="0.8" />
    <text x="66" y="60" text-anchor="middle" font-size="11" font-family="var(--font-mono)" fill="var(--ink)">route</text>
    <text x="66" y="78" text-anchor="middle" font-size="13" font-family="var(--font-mono)" fill="var(--accent)">20</text>

    <text x="124" y="69" text-anchor="middle" font-size="15" font-family="var(--font-mono)" fill="var(--muted)">×</text>

    <rect x="136" y="38" width="92" height="50" rx="6" fill="var(--card-2)" stroke="var(--hairline-strong)" stroke-width="0.8" />
    <text x="182" y="60" text-anchor="middle" font-size="11" font-family="var(--font-mono)" fill="var(--ink)">method</text>
    <text x="182" y="78" text-anchor="middle" font-size="13" font-family="var(--font-mono)" fill="var(--accent)">5</text>

    <text x="240" y="69" text-anchor="middle" font-size="15" font-family="var(--font-mono)" fill="var(--muted)">×</text>

    <rect x="252" y="38" width="92" height="50" rx="6" fill="var(--card-2)" stroke="var(--hairline-strong)" stroke-width="0.8" />
    <text x="298" y="60" text-anchor="middle" font-size="11" font-family="var(--font-mono)" fill="var(--ink)">status_class</text>
    <text x="298" y="78" text-anchor="middle" font-size="13" font-family="var(--font-mono)" fill="var(--accent)">4</text>

    <text x="368" y="69" text-anchor="middle" font-size="15" font-family="var(--font-mono)" fill="var(--muted)">=</text>

    <rect x="392" y="36" width="148" height="54" rx="6" fill="var(--card-2)" stroke="var(--accent)" stroke-width="1.2" />
    <text x="466" y="59" text-anchor="middle" font-size="12" fill="var(--ink)">400 series</text>
    <text x="466" y="78" text-anchor="middle" font-size="10" font-family="var(--font-mono)" fill="var(--muted)">безопасно — ограничено</text>

    <line x1="298" y1="98" x2="298" y2="140" stroke="var(--muted)" stroke-width="1.2" stroke-dasharray="4 4" opacity="0.6" />
    <text x="318" y="124" font-size="10.5" font-family="var(--font-mono)" fill="var(--muted)">добавить один неограниченный label</text>

    <rect x="20" y="150" width="324" height="50" rx="6" fill="var(--card-2)" stroke="var(--hairline-strong)" stroke-width="0.8" />
    <text x="182" y="172" text-anchor="middle" font-size="11" font-family="var(--font-mono)" fill="var(--ink)">route × method × status_class × user_id</text>
    <text x="182" y="190" text-anchor="middle" font-size="11" font-family="var(--font-mono)" fill="var(--ink)">20 × 5 × 4 × <tspan fill="var(--accent)">100000</tspan></text>

    <text x="368" y="181" text-anchor="middle" font-size="15" font-family="var(--font-mono)" fill="var(--muted)">=</text>

    <rect x="392" y="148" width="148" height="54" rx="6" fill="var(--card)" stroke="var(--accent)" stroke-width="1.6" stroke-dasharray="5 4" />
    <text x="466" y="171" text-anchor="middle" font-size="12.5" fill="var(--ink)">40 М series</text>
    <text x="466" y="190" text-anchor="middle" font-size="10" font-family="var(--font-mono)" fill="var(--muted)">обрыв OOM</text>
  </svg>
</DiagramFrame>

## Exemplar: мост метрика → трейс

Когда histogram записывает медленное наблюдение, клиент может прикрепить один **exemplar** — `trace_id` запроса, создавшего это наблюдение. Grafana отображает exemplar'ы как точки на тепловой карте histogram; клик по точке открывает соответствующий трейс.

Exemplar'ы (стандартизированы в Prometheus 2.32, начало 2022) устраняют разрыв: «метрики говорят, что что-то медленно, но нет конкретного запроса для дальнейшего анализа.» Они позволяют не добавлять `user_id` в label метрик — cardinality остаётся управляемой — и при этом навигировать от агрегированного всплеска к одному конкретному запросу через трейс.

<Misconception id="mc-metrics-user-id-ru">
  <p><strong>«Добавить user_id как label для отслеживания ошибок по клиентам.»</strong> Нет. 100 к пользователей × прочие label = миллионы series; Prometheus OOM за часы. Данные по клиентам — в логи или трейсы. Используй ограниченный label вроде <code>customer_segment</code> (3 значения) и exemplar'ы для перехода к запросу.</p>
</Misconception>

<NumbersCard
  id="02-metrics-cardinality-numbers-ru"
  title="Числа Prometheus: cardinality и стоимость"
  stats={[
    { label: "Байт на активную series в head block Prometheus", value: "~3 КБ" },
    { label: "Безопасная зона series на 16 ГБ", value: "~5 М" },
    { label: "Множитель cardinality на один high-card label", value: "100× — 10 000×" },
    { label: "Типичный интервал scrape", value: "15 с" },
    { label: "Series от route × method × status_class (20×5×4)", value: "400" },
    { label: "Дополнительных series при добавлении user_id (100 к)", value: "40 М" },
  ]}
/>

<Quiz
  id="02-metrics-and-cardinality-quiz-1"
  lessonSlug="02-metrics-and-cardinality"
  lang="ru"
  question="Команда добавляет customer_email к counter request_total. Prometheus исчерпывает память за несколько часов. Почему?"
  choices={[
    { label: "Counter'ы потребляют слишком много CPU при инкременте", misconception: "Инкремент counter'а практически бесплатен — атомарный инкремент in-process." },
    { label: "Каждый уникальный customer_email создаёт отдельную time series; при 100k+ активных клиентах число series растёт на порядки и исчерпывает память head block", correct: true },
    { label: "Prometheus не поддерживает строки как значения label", misconception: "Строки полностью поддерживаются; проблема — неограниченная cardinality, а не тип данных." },
    { label: "Нужно увеличить интервал scrape", misconception: "Интервал scrape влияет на разрешение данных, а не на потребление памяти числом series." },
  ]}
/>

<Quiz
  id="02-metrics-and-cardinality-quiz-2"
  lessonSlug="02-metrics-and-cardinality"
  lang="ru"
  question="Что делает exemplar на метрике histogram?"
  choices={[
    { label: "Пример граничного значения bucket", misconception: "Bucket'ы histogram определяются конфигурацией метрики, а не exemplar'ами." },
    { label: "trace_id, прикреплённый к одному сэмплированному наблюдению в bucket'е, чтобы со всплеска метрики можно было перейти к конкретному медленному запросу", correct: true },
    { label: "Наиболее часто встречающееся значение в bucket'е", misconception: "Exemplar'ы — не статистическая мода; это ссылки на конкретные записи трейса." },
    { label: "Канонический пример PromQL-запроса для дашборда", misconception: "Exemplar'ы — это runtime-данные, прикреплённые к сэмплам метрик, а не шаблоны запросов." },
  ]}
/>

<Quiz
  id="02-metrics-and-cardinality-quiz-3"
  lessonSlug="02-metrics-and-cardinality"
  lang="ru"
  question="Какой тип метрики использовать для 'длительности запроса checkout, чтобы можно было запросить p99 по всем репликам'?"
  choices={[
    { label: "Counter — наиболее распространённый тип", misconception: "Counter только считает события; вычислить перцентиль из counter'а невозможно." },
    { label: "Gauge — читать latency в момент scrape", misconception: "Gauge фиксирует мгновенное значение; вычислить распределение из него ретроспективно невозможно." },
    { label: "Histogram — распределяет наблюдения по bucket'ам; histogram_quantile агрегирует по репликам", correct: true },
    { label: "Summary — предвычисляет перцентиль на источнике", misconception: "Summary вычисляет перцентили на каждой реплике отдельно и не может быть точно агрегирован между репликами." },
  ]}
/>

</Explanation>

<KeyTakeaway>Метрики — это предагрегированные time series: быстрые для запросов за годы истории, но слепые к любому измерению, не попавшему в label. Cardinality — обрыв стоимости: каждая уникальная комбинация значений label — отдельная series, поэтому неограниченные label вроде user_id разрушают TSDB. Exemplar'ы соединяют метрики с трейсами без платы за cardinality.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="02-metrics-and-cardinality-retrieval"
  lang="ru"
  questions={[
    { q: "Объясни своими словами, почему метрики дёшевы для запроса, но слепы, а логи мощны, но дороги.", a: "Метрики предагрегируются при записи: TSDB хранит только текущие итоги по (metric_name, label_set, timestamp), поэтому время чтения — это просто обход предвычисленных bucket'ов, быстрый за годы. Цена: можно задавать только вопросы об измерениях, которые ты пометил заранее. Логи — наоборот: каждое событие хранится полностью, поэтому любой вопрос разрешим постфактум, но backend должен сканировать или индексировать каждое поле, платя байтами и задержкой запроса." },
    { q: "Метрика имеет label'ы: route (30 шаблонов), method (5), status_class (4), region (3). Безопасно ли это? Что сделает её опасной?", a: "30 × 5 × 4 × 3 = 1800 series — безопасно. Становится опасным при добавлении неограниченного label вроде user_id (100k+), что умножает число series на 100 000, доводя итог до сотен миллионов и исчерпывая память TSDB." },
    { q: "Почему использовать exemplar вместо добавления user_id как label метрики?", a: "Добавление user_id как label умножает число series на количество активных пользователей, вызывая исчерпание памяти TSDB. Exemplar вместо этого прикрепляет сэмплированный trace_id к bucket'у histogram — можно перейти от всплеска метрики к трейсу одного конкретного запроса, не платя за cardinality каждого запроса." },
  ]}
/>

<Recap lang="ru">
Метрика Prometheus идентифицируется парой (имя, label_set); каждая уникальная комбинация значений label создаёт одну time series, хранящуюся примерно в 3 КБ в head block. Предагрегация при записи делает запросы быстрыми за годы, но означает, что метрики могут отвечать только на вопросы об измерениях, помеченных заранее. Cardinality — произведение числа значений всех label — главный драйвер стоимости: один неограниченный label вроде customer_email на загруженной метрике может умножить число series с тысяч до десятков миллионов, вызвав OOM сервера. Четыре типа метрик: counter (скорость изменения), gauge (мгновенное), histogram (percentile-запрашиваемое распределение), summary (предвычисленное, не агрегируемое). Exemplar'ы (sampled trace_id, прикреплённый к bucket'у histogram) соединяют агрегированную метрику с конкретным трейсом, устраняя необходимость в high-cardinality идентификаторах как label. Теперь, когда ты увидишь PR с новым label метрики, первый вопрос будет «какова cardinality этого значения?» — и ты знаешь, что вместо user-scoped label нужен exemplar.
</Recap>
