---
concepts:
- signal-selection
- cardinality
- log-volume
- trace-sampling
- join-keys
- observability-2.0
estMin: 13
lang: ru
level: senior
order: 7
prereqs: []
slug: quiz-choice
sources:
- https://opentelemetry.io/docs/concepts/signals/
- https://prometheus.io/docs/practices/instrumentation/
- https://www.honeycomb.io/blog/opentelemetry-is-not-three-pillars
status: ready
summary: "Тест с выбором на синтез по всему юниту — выбор сигнала, cardinality, объём логов, sampling, join-ключи и сдвиг стоимости в observability 2.0."
title: 'Три pillarа: тест с выбором ответа'
track: observability
unit: 01-three-pillars
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Шесть вопросов поперёк всего юнита. Каждый отражает решение, которое ты принимаешь в инциденте или при инструментировании — не определение для заучивания, а компромисс по стоимости, который надо взвесить.
</Hook>

<Goal lang="ru">
Убедись, что выбираешь правильный сигнал под вопрос, рассуждаешь о его оси стоимости и связываешь три pillarа через join-ключи — тот синтез, к которому вели отдельные уроки.
</Goal>

<Quiz
  id="01-three-pillars-choice-1"
  lessonSlug="01-three-pillars"
  lang="ru"
  question="Продакт хочет per-customer error rate в разбивке по конкретному customer_id для 200k активных клиентов, с запросом за последние 30 дней. В стеке 1.0 какой сигнал несёт эти данные и почему не остальные?"
  choices={[
    { label: "Logs (или traces) — высокая cardinality там бесплатна на записи; стоимость — байты ingestion и скан при запросе, а не память на число серий. Пивот к конкретному клиенту — через bounded метрику-label customer_segment плюс exemplar.", correct: true },
    { label: "Metrics — просто добавь customer_id как label, чтобы дашборд группировал по нему.", misconception: "customer_id неограничен; 200k значений умножают число серий на 200k и кладут TSDB по OOM. Идентичность клиента живёт в logs или traces, но не в label метрики." },
    { label: "Metrics с типом Summary, который предвычисляет per-customer перцентиль.", misconception: "Summary считает перцентили per-replica и всё равно требует label на клиента, чтобы их разделить — проблема cardinality не меняется, и summary не агрегируется между репликами." },
    { label: "Никакой — разбивка по клиенту невозможна в стеке 1.0.", misconception: "Она полностью возможна в logs и traces, которые сохраняют каждое поле. Только pillar метрик не может позволить себе неограниченные per-customer label." },
  ]}
/>

<Quiz
  id="01-three-pillars-choice-2"
  lessonSlug="01-three-pillars"
  lang="ru"
  question="Два сервиса делят тяжёлый эндпоинт. Сервис A держит стабильный набор label (route × method × status_class = 400 серий), но логирует каждый запрос. Сервис B имеет крошечный объём логов, но добавил user_id в один counter. Кто первым угрожает своему бэкенду и по какой оси?"
  choices={[
    { label: "Сервис B — неограниченный label user_id умножает число серий до миллионов и кладёт TSDB по OOM, тогда как объём логов A лишь линейно растит счёт за ingest и деградирует плавно.", correct: true },
    { label: "Сервис A — высокий объём логов всегда роняет бэкенд быстрее, чем label метрики.", misconception: "Объём логов задаёт линейный счёт за ingest и задержку запросов, а не резкий обрыв по памяти. Это проблема стоимости, а не OOM-за-минуты, как cardinality-бомба." },
    { label: "Оба падают одинаково — cardinality и объём логов — одна и та же стоимость.", misconception: "Это разные оси стоимости: cardinality — память на число серий в TSDB (обрыв); объём логов — байты ingestion и время скана (линейно). Их смешение — главная путаница, которую разбирает юнит." },
    { label: "Ни один — современные бэкенды авто-шардят оба случая.", misconception: "Авто-шардинг помогает ingest логов, но не спасает head-блок Prometheus от миллионов серий; неограниченный label всё равно кладёт TSDB по OOM." },
  ]}
/>

<Quiz
  id="01-three-pillars-choice-3"
  lessonSlug="01-three-pillars"
  lang="ru"
  question="Команда применяет tail-based sampling на коллекторе, оставляя 100% ошибок и 2% успехов. Их счёт за traces почти не падает против 100%-хранения, хотя сохранённый объём резко снизился. Почему?"
  choices={[
    { label: "Tail sampling буферизует каждый span через коллектор до завершения трейса, поэтому CPU, память и входящий трафик коллектора масштабируются с сырым трафиком — сжимается только хранилище ниже по потоку. Экономия видна в storage, а не в стоимости коллектора.", correct: true },
    { label: "Tail sampling сломан; он должен снижать все затраты пропорционально.", misconception: "Он работает как задумано. Объём хранилища падает, но коллектор всё равно обязан принять и забуферизовать каждый span, чтобы принять решение по контексту — эта стоимость в tail sampling неустранима." },
    { label: "Им стоит перейти на 100% head-based sampling, чтобы сэкономить.", misconception: "100% head sampling не экономит ничего; head экономит за счёт отбрасывания в начале трейса. Смысл tail sampling — сохранить редкие ошибки и медленные хвосты, которые head пропустил бы случайно." },
    { label: "Трейсы ошибок — это вся стоимость, поэтому семплирование успехов никогда не помогает.", misconception: "Ошибки обычно малая доля трафика. Доминирующая оставшаяся стоимость здесь — буферизация всех span в коллекторе, а не сохранённые трейсы ошибок." },
  ]}
/>

<Quiz
  id="01-three-pillars-choice-4"
  lessonSlug="01-three-pillars"
  lang="ru"
  question="Метрики используют route='/checkout', логи — http_path='/checkout', трейсы — http.route='/checkout'. Данные корректны и текут. Что на самом деле ломается и какой фикс?"
  choices={[
    { label: "Ломается кросс-сигнальный triage: без общего имени ключа пивот metric→log→trace требует ручного перевода запросов под каждый бэкенд, превращая 30-секундный проход в 20-минутную охоту по вкладкам. Фикс: принять OpenTelemetry Semantic Conventions, чтобы все три эмитили http.route.", correct: true },
    { label: "Ничего — имена полей косметика, пока совпадают значения.", misconception: "Автоматические пивоты ключуются по ИМЕНИ поля, а не только по значению. Расходящиеся имена означают, что каждый дашборд и runbook несёт хрупкий ручной перевод. Согласованность имён — весь тезис join-ключей." },
    { label: "OTel Collector отказывается форвардить данные.", misconception: "Коллектор форвардит независимо от именования атрибутов. Сбой — операционное трение при triage, а не сломанный пайплайн." },
    { label: "Cardinality метрик растёт, потому что имена различаются.", misconception: "Cardinality — про число ЗНАЧЕНИЙ label внутри одной метрики, а не про согласие трёх бэкендов об имени поля. Расхождение имён — проблема join, а не cardinality." },
  ]}
/>

<Quiz
  id="01-three-pillars-choice-5"
  lessonSlug="01-three-pillars"
  lang="ru"
  question="Команда из 40 сервисов тратит ~2 инженеро-недели в квартал на надзор за cardinality-бюджетами и ~3 часа на инцидент на кросс-pillar пивоты. Ещё они держат 5 лет low-cardinality SLO-метрик для комплаенса. Какова senior-рекомендация?"
  choices={[
    { label: "Держать оба: 2.0 wide-event бэкенд для инцидент-респонса и ad-hoc high-cardinality вопросов, и оставить 1.0 TSDB-слой для 5-летних low-cardinality SLO-трендов, где предагрегация дешевле всего. Решение экономическое, а не идеологическое.", correct: true },
    { label: "Мигрировать всё на 2.0 — три pillarа устарели.", misconception: "2.0 выигрывает для инцидент-респонса и high-cardinality запросов, но сырые wide events за 5 лет стоят дороже предагрегированных rollup-метрик. «Всегда лучше» — заблуждение, которое урок 6 явно опровергает." },
    { label: "Остаться полностью на 1.0 — это самая дешёвая инфра.", misconception: "Самая дешёвая инфра игнорирует скрытую инженерную стоимость: 2 недели/квартал на гигиену label плюс 3 часа/инцидент на пивоты — это и есть реальный счёт, толкающий к 2.0." },
    { label: "Построить кастомное in-house wide-event хранилище, чтобы избежать SaaS-локина.", misconception: "Достичь паритета фич с SaaS wide-event бэкендом — 1–2 инженеро-года; это оправдано лишь для очень крупных оргов с выделенной observability-командой, а не для шопа из 40 сервисов." },
  ]}
/>

<Quiz
  id="01-three-pillars-choice-6"
  lessonSlug="01-three-pillars"
  lang="ru"
  question="Во время инцидента on-call обнаруживает, что нужный трейс никогда не семплировался — SDK использует ParentBased(TraceIdRatioBased), но trace_id выводится из хеша пути запроса. Корневая причина и устойчивый фикс?"
  choices={[
    { label: "trace_id из пути коррелирует решение семплирования с эндпоинтом, поэтому целая когорта эндпоинта всегда семплируется или всегда отбрасывается. Фикс: криптографически случайный trace_id плюс tail-based политики на коллекторе, удерживающие 100% ошибок и медленных трейсов.", correct: true },
    { label: "ParentBased игнорирует входящий флаг traceparent, поэтому downstream-сервисы переразрешают случайно.", misconception: "ParentBased явно уважает входящий флаг sampled — это его задача. Дефект — неслучайный seed trace_id, отмеченный в post-mortem Elastic 2024." },
    { label: "Head-based sampling всегда неверен; нужно использовать только tail sampling.", misconception: "Head sampling — дешёвая база и нормален, когда trace_id по-настоящему случаен. Фикс — случайный seed плюс tail-политики для интересных случаев, а не отмена head sampling." },
    { label: "Частота семплирования просто слишком низкая; подними до 100%.", misconception: "100% семплирование убирает экономию, ради которой семплируют, и всё равно не чинит баг корреляции. Случайный seed плюс tail-политики держат стоимость ограниченной и никогда не теряют трейсы ошибок." },
  ]}
/>

<Recap lang="ru">
Сквозная линия юнита — одно решение: вопрос выбирает сигнал по его самой дешёвой оси стоимости. Metrics дёшевы в запросе, но гибнут на cardinality; logs сохраняют всё, но платят байтами ingestion; traces сохраняют причинность, но обязаны семплировать (а tail sampling смещает стоимость со storage на коллектор). Join-ключи — service.name, trace_id, http.route через OpenTelemetry Semantic Conventions — это то, что делает три pillarа единой навигационной поверхностью. Модель wide-event 2.0 схлопывает все три, когда инженерная стоимость cardinality-дисциплины и кросс-pillar пивотов превышает счёт за единый бэкенд — это экономический, а не идеологический выбор.
</Recap>
