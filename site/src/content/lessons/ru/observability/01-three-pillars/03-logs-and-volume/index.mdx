---
concepts:
- logs
- structured-logging
- log-level
- ingestion
- retention
- log-pipeline
- tiered-storage
deepensInto: []
estMin: 12
lang: ru
lessonType: topic
level: middle
order: 3
prereqs:
- observability/01-three-pillars/02-metrics-and-cardinality
slug: 03-logs-and-volume
sources:
- https://www.oreilly.com/library/view/distributed-systems-observability/9781492033431/
- https://opentelemetry.io/docs/concepts/signals/
- https://grafana.com/blog/how-to-manage-high-cardinality-metrics-in-prometheus-and-kubernetes/
spiral:
- latency
status: ready
summary: Как работают структурные логи, почему байты ingestion доминируют в счёте,
  и как дисциплина log level и tiered retention контролируют стоимость.
title: 'Логи и объём: cost-модель структурного логирования'
track: observability
unit: 01-three-pillars
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import NumbersCard from "~/components/layout/NumbersCard.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";
import Infographic from "~/components/diagram/Infographic.astro";
import retryVolumeExplosion from "./infographics/retry-volume-explosion.svg?raw";

<Hook>
Счёт за логирование команды вырос в четыре раза за два месяца при неизменном трафике. Один рефакторинг тихо изменил количество попыток retry с 3 на 20 — и вместе с ним лог-строку INFO, эмитируемую при каждой попытке. 60 ГБ в день стало 600 ГБ в день от одного сервиса.
</Hook>

<Crux>Байты ingestion в день — главный драйвер стоимости логов. Одна болтливая лог-строка в hot path может умножить счёт на 10× за ночь.</Crux>

<Explanation>

## Что такое структурный лог

Лог-строка — это запись события: timestamp плюс payload, описывающий что произошло. Продакшн-наблюдаемость в 2026 — это **структурированное или ничего**: JSON-событие с последовательными ключами — единственная форма, поддерживающая filter-aggregate запросы при масштабе.

Обязательные поля каждой лог-строки:
- `timestamp` (ISO-8601 UTC)
- `level` (`debug` / `info` / `warn` / `error`)
- `service.name`
- `trace_id` (join-ключ к трейсам)
- `span_id`
- `message`
- Плюс поля конкретного события (`user_id`, `order_id`, `status_code` и т.д.)

Join-ключ `trace_id` позволяет перейти от всплеска метрики к лог-строке к трейсу за один клик. Без него — три разрозненных дашборда и никакого нарратива.

## Модель хранения

Pipeline — append-only ingestion с индексированным поиском:

1. Приложение эмитит JSON в stdout или локальный агент (Fluent Bit, Vector, OTel Collector).
2. Агент буферизирует и отправляет в log backend (Loki, ClickHouse, Elasticsearch, Datadog, Splunk).
3. Backend индексирует подмножество полей для быстрой фильтрации; остальное хранит для raw scan.

Логи сохраняют всё записанное — высокая cardinality **бесплатна при записи**, но **дорога при запросе**, поскольку backend должен сканировать или индексировать каждое поле. В отличие от метрик, нет взрыва cardinality от добавления `user_id` в лог-строку; стоимость — в байтах ingestion и времени scan'а запроса, а не в памяти под series.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Log level</th>
        <th class="border border-border px-3 py-2 text-left">Значение</th>
        <th class="border border-border px-3 py-2 text-left">Продакшн по умолчанию</th>
      </tr>
    </thead>
    <tbody>
      <tr><td class="border border-border px-3 py-2">debug</td><td class="border border-border px-3 py-2">Подробная трассировка внутреннего состояния</td><td class="border border-border px-3 py-2">Выключен</td></tr>
      <tr class="bg-surface-2"><td class="border border-border px-3 py-2">info</td><td class="border border-border px-3 py-2">События изменения состояния (запрос завершён, заказ создан)</td><td class="border border-border px-3 py-2">Включён, но high-volume пути сэмплировать на 1%</td></tr>
      <tr><td class="border border-border px-3 py-2">warn</td><td class="border border-border px-3 py-2">Восстановимый сбой (retry удался после N попыток)</td><td class="border border-border px-3 py-2">100%</td></tr>
      <tr class="bg-surface-2"><td class="border border-border px-3 py-2">error</td><td class="border border-border px-3 py-2">Невосстановимый, требующий действия сбой</td><td class="border border-border px-3 py-2">100%</td></tr>
    </tbody>
  </table>
</div>

## Объём ingestion: математика стоимости

Скромный сервис на 1000 запросов/с, эмитирующий 1 КБ структурированного JSON на запрос, производит:
- 1 МБ/с = 86 ГБ/день = 2,6 ТБ/месяц на сервис

Умножить на 50 сервисов в типичном fleet'е — счёт при $0,10/ГБ ingestion плюс стоимость индексированных событий достигает десятков тысяч в месяц до расходов на retention.

Стандартные способы контроля стоимости:

1. **Дисциплина log level.** DEBUG выключен в продакшне. INFO — только для событий изменения состояния. WARN — для восстановимых сбоев. ERROR — для сбоев, требующих действия.
2. **Сэмплирование на success path.** 1 из 100 для high-volume успешных INFO-событий; 100% для errors и warns.
3. **Tiered retention.** 15-дневный hot tier для запросов; архив в дешёвое объектное хранилище для compliance. Большинство запросов логов — в первые 3 дня.
4. **Перенаправление болтливых tier'ов.** Datadog Flex Logs и Splunk tiered indexes позволяют маршрутизировать high-volume/low-value события из дорогого индексированного tier в более дешёвый.

Вместе эти инструменты режут счёт на каждом этапе pipeline до хранилища — не жертвуя WARN и ERROR строками, которые нужны в ночь инцидента. Пропустить хотя бы один — значит оставить нетронутым самый крупный рычаг снижения стоимости.

<Inset kind="why" lang="ru">
Антипаттерн retry-лог — самая частая причина неожиданных счетов: рефакторинг поднимает retry с 3 до 20, каждая попытка эмитит INFO-строку, и сервис, отвечавший за 5 ГБ/день, внезапно производит 60 ГБ/день. Решение: эмитить один структурный лог в конце последовательности retry — `{attempts: 20, final_outcome: "timeout"}` — вместо одной строки на попытку. Метрика-counter `payment_retries_total{outcome}` дёшево фиксирует объём.
</Inset>

<Infographic
  svg={retryVolumeExplosion}
  label="Столбчатая диаграмма объёма логов одного сервиса: 60 ГБ/день до рефакторинга против 600 ГБ/день после того, как количество retry выросло с 3 до 20 с одной INFO-строкой на попытку — скачок в 10 раз при неизменном трафике."
  caption="Одна болтливая лог-строка в hot path умножила счёт на 10× за ночь — без какого-либо изменения трафика запросов. Эта единственная строка и есть весь скачок стоимости."
  hue="var(--d-infra)"
/>

<FlowDiagram
  label="Контроль объёма логов: где байты режутся до дорогого хранилища"
  perRow={5}
  hue="var(--d-systems)"
  nodes={[
    { id: "emit", label: "Приложение", sub: "JSON-событие" },
    { id: "level", label: "Фильтр level", sub: "DEBUG отброшен" },
    { id: "sample", label: "Сэмплирование", sub: "INFO 1%" },
    { id: "ship", label: "Агент шлёт", sub: "Vector / OTel" },
    { id: "store", label: "Хранилище $$", sub: "индекс + retain" },
  ]}
  edges={[
    { from: "emit", to: "level", label: "все строки" },
    { from: "level", to: "sample", label: "WARN/ERROR 100%" },
    { from: "sample", to: "ship", label: "объём снижен" },
    { from: "ship", to: "store", label: "ГБ/день в счёт" },
  ]}
  caption="Объём режется на фильтре level (DEBUG выключен) и сэмплере (INFO на 1%, WARN/ERROR хранятся 100%) до хранилища, где индексация и retention доминируют в счёте. Срезать байты выше по потоку дешевле, чем хранить их."
/>

<DragOrder
  id="03-logs-and-volume-drag-1"
  lessonSlug="03-logs-and-volume"
  lang="ru"
  prompt="Счёт за логирование вырос в четыре раза при неизменном трафике. Расставь шаги диагностики по порядку:"
  items={[
    { id: "a", label: "Открыть дашборд вендора — найти ingest ГБ/день по сервисам" },
    { id: "b", label: "Определить сервис, чей объём вырос (например, с 5 ГБ/день до 60 ГБ/день)" },
    { id: "c", label: "Агрегировать объём логов по log_pattern внутри этого сервиса" },
    { id: "d", label: "Найти болтливую лог-строку (например, одна на попытку retry)" },
    { id: "e", label: "Оценить правильный log level (попытка retry = DEBUG или один WARN при финальном сбое)" },
    { id: "f", label: "Краткосрочное решение: маршрутизировать в сэмплированный tier или повысить level до WARN" },
    { id: "g", label: "Долгосрочное решение: эмитить один итоговый лог в конце retry + добавить metric counter" },
  ]}
  correctOrder={["a", "b", "c", "d", "e", "f", "g"]}
/>

<Quiz
  id="03-logs-and-volume-quiz-1"
  lessonSlug="03-logs-and-volume"
  lang="ru"
  question="Сервис эмитит INFO-лог на каждую попытку retry. После рефакторинга, поднявшего retry с 3 до 20, счёт за логирование вырос в 4 раза за два месяца. Каково долгосрочное решение?"
  choices={[
    { label: "Переключиться на plaintext-логи для экономии байт", misconception: "Plaintext-логи сложнее запрашивать и фильтровать; реальная проблема — объём, а не overhead JSON-кодирования." },
    { label: "Эмитить один структурный лог в конце последовательности retry (attempts_total, final_outcome) вместо одного на попытку, и добавить metric counter для объёма retry", correct: true },
    { label: "Снизить количество retry обратно до 3", misconception: "Снижение retry меняет поведение приложения; реальное решение — дисциплина логов, а не бизнес-логика." },
    { label: "Полностью отключить логирование для сервиса payment", misconception: "Это убирает критически важный диагностический сигнал для сбоев." },
  ]}
/>

<Quiz
  id="03-logs-and-volume-quiz-2"
  lessonSlug="03-logs-and-volume"
  lang="ru"
  question="Почему добавление user_id в лог-строку не вызывает того же взрыва, что добавление его как label метрики?"
  choices={[
    { label: "Лог-строки сжимаются, поэтому дополнительное поле незначительно", misconception: "Сжатие помогает, но не в этом причина; архитектурное различие — write path vs series count." },
    { label: "Log backend хранит каждое событие как raw bytes; нет per-value series, умножающейся в памяти — стоимость в байтах ingestion и времени scan'а запроса, а не в in-memory series count", correct: true },
    { label: "Логи не поддерживают high-cardinality поля", misconception: "Логи поддерживают произвольные поля, включая high-cardinality; в этом как раз их преимущество перед метриками." },
    { label: "Поле user_id автоматически дедуплицируется в log storage", misconception: "Автоматической дедупликации нет; каждое событие хранится как отдельная запись." },
  ]}
/>

<NumbersCard
  id="03-logs-volume-numbers-ru"
  title="Числа стоимости объёма логов"
  stats={[
    { label: "Типичный объём логов на сервис при 1k запросов/с и 1 КБ на запрос", value: "~86 ГБ / день" },
    { label: "Datadog ingestion логов", value: "~$0,10 / ГБ" },
    { label: "Datadog индексированные log events", value: "~$1,27 / миллион" },
    { label: "Hot retention типичный (query tier)", value: "15 дней" },
    { label: "Рекомендуемое сэмплирование success path", value: "1% INFO, 100% WARN+ERROR" },
    { label: "Множитель объёма при retry-антипаттерне (3→20 попыток)", value: "~6-7× рост счёта" },
  ]}
/>

</Explanation>

<KeyTakeaway>Логи сохраняют каждое поле каждого события — это их суперсила и их стоимость. Объём ingestion (ГБ/день на сервис) — драйвер счёта. Дисциплина level (DEBUG выключен, INFO сэмплирован, WARN/ERROR 100%) и tiered retention — стандартные средства контроля. trace_id в каждой лог-строке — join-ключ, делающий три-сигнальные переходы возможными.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="03-logs-and-volume-retrieval"
  lang="ru"
  questions={[
    { q: "Назови обязательные поля продакшн-качества структурной лог-строки и объясни, почему trace_id особенно важен.", a: "Обязательные: timestamp (ISO-8601 UTC), level, service.name, trace_id, span_id, message, плюс поля конкретного события. trace_id — join-ключ, позволяющий перейти из лог-строки прямо к соответствующему distributed trace: клик из результата log grep к полному span waterfall без ручной корреляции временных меток между сервисами." },
    { q: "Сервис производит 86 ГБ/день логов. Назови три способа снизить это, не теряя actionable данные.", a: "1. Дисциплина level: выключить DEBUG в продакшне, сэмплировать INFO на 1% для high-volume success path, хранить 100% WARN и ERROR. 2. Сэмплирование в ingest pipeline: маршрутизировать high-volume/low-value события в более дешёвый scan tier (Datadog Flex Logs, Splunk tiered index). 3. Исправить chatty антипаттерны: эмитить одно summary-событие на логическую операцию вместо одной строки на итерацию или попытку retry." },
    { q: "Почему структурное (JSON) логирование требуется для filter-aggregate запросов при масштабе, а plaintext — нет?", a: "Структурное логирование означает, что у каждого события последовательные имена ключей; backend может индексировать конкретные поля и отвечать на 'покажи все события где user_id=42 и status=error' без парсинга каждого байта свободного текста. Plaintext grep требует full-text scan, масштабирующийся линейно с ingested объёмом и не использующий field-level индексы, делая агрегации вроде 'сколько 5xx ошибок на маршрут' непрактичными при масштабе ТБ." },
  ]}
/>

<Recap lang="ru">
Структурный лог — это JSON-событие с временной меткой и последовательными ключами; только такая форма поддерживает filter-aggregate запросы в продакшн-масштабе. Pipeline backend — append-only ingestion с индексированным поиском, хранящий каждое записанное поле. В отличие от метрик, логи не имеют взрыва cardinality от high-cardinality полей — но платят байтами ingestion: 86 ГБ/день на сервис типично при 1000 запросах/с. Дисциплина level (DEBUG выключен, INFO сэмплирован, WARN/ERROR 100%) и tiered retention контролируют счёт. Обязательное поле `trace_id` — join-ключ, соединяющий лог-строки с трейсами и позволяющий переходить из log search к полному span waterfall в tracing backend. Теперь, когда увидишь скачок счёта за логирование, первым делом откроешь ingest-дашборд вендора и спросишь: какой сервис утроил объём и действительно ли каждая лог-строка на горячем пути должна быть INFO?
</Recap>
