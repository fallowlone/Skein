infographic chart-column-simple
data
  title Одна болтливая лог-строка: скачок счёта в 10 раз
  desc Тот же сервис, трафик неизменен — retry подняли с 3 до 20, одна INFO-строка на попытку
  values
    - label До рефакторинга (ГБ/день)
      value 60
    - label После рефакторинга (ГБ/день)
      value 600
theme
  palette #14b8a6 #f43f5e
