---
concepts:
- signal-cost-model
- cardinality
- sampling-tradeoff
- join-keys
- observability-2.0
- failure-modes
estMin: 14
lang: ru
level: senior
order: 8
prereqs: []
slug: quiz-short
sources:
- https://opentelemetry.io/docs/concepts/signals/
- https://prometheus.io/docs/practices/instrumentation/
- https://www.honeycomb.io/blog/opentelemetry-is-not-three-pillars
status: ready
summary: "Промпты на свободное воспроизведение по всему юниту. Сначала ответь своими словами, затем раскрой модельный ответ и сравни."
title: 'Три pillarа: тест на свободное воспроизведение'
track: observability
unit: 01-three-pillars
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";

<Hook>
Воспроизведение бьёт перечитывание. На каждый промпт скажи или запиши полный ответ по памяти, прежде чем открыть модельный — усилие припоминания и закрепляет модели стоимости.
</Hook>

<Goal lang="ru">
Восстанови хребет юнита — три модели стоимости, cardinality, компромисс семплирования, join-ключи, сдвиг 2.0 и режимы отказа по сигналам — не подглядывая в уроки.
</Goal>

<RetrievalDrawer
  client:load
  id="01-three-pillars-short-retrieval"
  lang="ru"
  questions={[
    { q: "Почему metrics, logs и traces не взаимозаменяемы — что каждый сохраняет, отбрасывает и чем платит?", a: "Каждый — самый дешёвый ответ на свой вопрос. Metrics — предагрегированные числовые time series: дёшево запрашивать за годы, но слепы к любому измерению вне набора label, а их ось стоимости — cardinality (число серий). Logs — записи событий, сохраняющие каждое поле: любой пост-фактум вопрос отвечаем, но стоимость — байты ingestion и время скана при запросе. Traces — причинная цепь одного запроса между сервисами: сохраняют тайминг и причинность по span, но хранить их все нерентабельно, поэтому надо семплировать. Ни одна форма хранения не дёшева одновременно на длинном хранении, высокой cardinality и полной точности запроса — поэтому команда с одним сигналом не сделает triage от края до края." },
    { q: "Объясни, почему cardinality — обрыв стоимости для metrics, но high-cardinality поля почти бесплатны в logs.", a: "Метрика Prometheus идентифицируется парой (name, label_set); каждая уникальная комбинация значений label — отдельная time series по ~3 КБ в head-блоке. Добавление неограниченного label вроде user_id умножает число серий на число различных значений — 100k пользователей могут раздуть метрику из 400 серий до десятков миллионов и положить TSDB по OOM за часы. У logs нет серии на значение: каждое событие хранится как сырые байты, поэтому добавление user_id в log-строку стоит лишь маргинальных байтов этого поля плюс времени скана. Архитектурная разница — память на число серий (резкий обрыв) против объёма ingestion (линейный счёт, деградирующий плавно)." },
    { q: "Сравни head-based и tail-based sampling: где каждый дёшев и какие трейсы каждый рискует потерять?", a: "Head-based решает в начале трейса; если решение «drop», SDK не эмитит span, поэтому он дёшев от края до края, и нагрузка коллектора пропорциональна частоте семплирования. Его слабость — равномерная случайная выборка недопредставляет редкие события: ошибки и медленные хвосты отбрасываются так же часто, как всё прочее. Tail-based буферизует каждый span до завершения трейса (30–60с), затем решает по полному контексту, поэтому держит 100% ошибок и медленных трейсов — но каждый span всё равно проходит через коллектор, так что CPU и память коллектора масштабируются с сырым трафиком, а не с хранимым объёмом. Production-паттерн: head-based на 10–20% базы плюс tail-политики для ошибок и медленных хвостов." },
    { q: "Что такое join-ключи, почему OpenTelemetry Semantic Conventions важны даже на 1.0-бэкендах и как сюда вписываются exemplarы?", a: "Join-ключи — общие ИМЕНА атрибутов, появляющиеся идентично в metrics, logs и traces — service.name, trace_id, http.route, db.system. Именно они превращают три отдельных бэкенда в единую навигационную поверхность: с ними пивот metric → log → trace — один клик; без них каждый дашборд требует ad-hoc перевода запросов, а runbook-и хрупки. OpenTelemetry Semantic Conventions задаёт канонические имена, поэтому он несущий даже на Prometheus + Loki + Jaeger — имена полей ЕСТЬ семантика, независимо от SDK или бэкенда. Exemplarы — мост metric-to-trace: семплированный trace_id, прикреплённый к наблюдению bucket гистограммы, позволяет кликнуть от агрегатного всплеска к одному конкретному медленному запросу, не платя стоимость cardinality." },
    { q: "Сформулируй тезис observability 2.0 и назови одну нагрузку, где 1.0 всё ещё выигрывает.", a: "Три pillarа — не естественная таксономия, а артефакт ограничений хранения конца 2000-х: metrics существовали, потому что нельзя было хранить каждое событие, logs — потому что нельзя было агрегировать на запросе, traces — потому что ни один из них не нёс кросс-сервисную причинность. Колоночное хранение и дешёвое object storage схлопывают все три в wide structured events — одна запись на единицу работы, несущая каждое поле включая high-cardinality, с metrics через GROUP BY, logs через фильтр и traces через join по trace_id на запросе. Cardinality перестаёт быть драйвером стоимости. Но 1.0 всё ещё выигрывает для очень долгого хранения (5+ лет) low-cardinality SLO-метрик, где предагрегация на холодном хранилище дешевле скана сырых wide events. Многие зрелые шопы держат оба; решение экономическое, не идеологическое." },
    { q: "Перечисли характерный режим отказа каждого сигнала и один guardrail, который строят до инцидента.", a: "Metrics — cardinality-бомба: один неограниченный label кладёт TSDB по OOM за минуты; guardrail: CI deny-list lint на имена label (email, user_id, session_id) плюс алерт на rate(prometheus_tsdb_head_series_created_total). Logs — runaway-ingestion: один болтливый сервис умножает дневной счёт; guardrail: бюджет GB/день на сервис и дисциплина уровней. Logs/metrics — тихая утечка PII: данные пользователя в индексируемых полях или label; guardrail: allow-list для label и span-атрибутов плюс redactor в пайплайне (Vector/Fluent Bit). Traces — sampling gap: интересный трейс отброшен; guardrail: tail-based политика, держащая 100% ERROR и задержки выше SLO. Traces — span explosion или orphaned spans: guardrail: ограничить число child span на трейс и пробрасывать W3C traceparent через async-границы." },
  ]}
/>

<Recap lang="ru">
Если ты смог восстановить каждый ответ по памяти, ты держишь хребет юнита: три сигнала — это три модели стоимости (cardinality, байты ingestion, sampling); cardinality — обрыв для metrics, а logs дёшево впитывают высокую cardinality; head и tail sampling меняют стоимость коллектора на гарантию не потерять трейсы ошибок; join-ключи через OpenTelemetry Semantic Conventions плюс exemplarы делают три компонуемыми; observability 2.0 схлопывает их wide events, когда это оправдано экономикой; а у каждого сигнала есть режим отказа, чей guardrail строят до пейджа в 03:00, а не после.
</Recap>
