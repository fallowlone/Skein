---
concepts:
- cardinality-bomb
- chatty-logs
- exemplars
- tail-sampling-config
estMin: 14
lang: ru
level: senior
order: 9
prereqs: []
slug: quiz-code
sources:
- https://prometheus.io/docs/practices/instrumentation/
- https://opentelemetry.io/docs/concepts/sampling/
status: ready
summary: "Читай реальный код инструментирования, log-строку, PromQL-алерт и конфиг tail-sampling; предскажи поведение observability и выбери фикс с наибольшим рычагом."
title: 'Три pillarа: чтение кода и конфигов'
track: observability
unit: 01-three-pillars
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Баги observability написаны в коде инструментирования и конфигах, а не в дашбордах. Читай сниппет, предскажи, что он сделает со стоимостью или корректностью бэкенда, затем выбери фикс, который senior-инженер делает первым.
</Hook>

<Goal lang="ru">
Отработай цикл, который ты прогоняешь в каждом инциденте observability: читай инструментирование, предскажи последствие по cardinality, объёму или семплированию, и тянись к фиксу с наибольшим рычагом, прежде чем кого-то будить.
</Goal>

## Сниппет 1 — label метрики

```go
requestsTotal := prometheus.NewCounterVec(
    prometheus.CounterOpts{Name: "http_requests_total"},
    []string{"route", "method", "status_class", "customer_email"},
)
// в хендлере:
requestsTotal.WithLabelValues(route, method, statusClass, req.CustomerEmail).Inc()
```

<Quiz
  id="01-three-pillars-code-1"
  lessonSlug="01-three-pillars"
  lang="ru"
  question="Этот counter задеплоен в сервис со 100k+ активных клиентов. Что произойдёт и какой фикс сохранит per-customer drill-through?"
  choices={[
    { label: "Каждый уникальный customer_email создаёт новую time series, поэтому число серий взрывается до миллионов и TSDB падает по OOM за часы. Фикс: убрать customer_email, держать bounded label customer_segment и прикрепить exemplar (trace_id) для per-customer drill-through.", correct: true },
    { label: "Всё нормально — counter дёшев в инкременте.", misconception: "Инкремент бесплатен; стоимость — память на число серий. Каждый отдельный customer_email — это отдельная серия по ~3 КБ в head-блоке — это и есть cardinality-бомба." },
    { label: "Prometheus отклонит label, потому что email содержит '@'.", misconception: "Email-строки — валидные значения label; '@' не проблема. Проблема — неограниченная cardinality: Prometheus сохраняет серии и исчерпывает память." },
    { label: "Поднять scrape interval до 60s, чтобы снизить нагрузку.", misconception: "Scrape interval меняет разрешение сэмплов, а не число активных серий. Память head-блока задаётся числом серий, которое раздувает неограниченный label." },
  ]}
/>

## Сниппет 2 — log-строка ретрая

```python
for attempt in range(max_retries):  # max_retries только что подняли 3 -> 20
    try:
        return gateway.charge(order)
    except TimeoutError:
        logger.info("payment retry", extra={"order_id": order.id, "attempt": attempt})
```

<Quiz
  id="01-three-pillars-code-2"
  lessonSlug="01-three-pillars"
  lang="ru"
  question="Трафик ровный, но счёт за логи скакнул после того, как max_retries вырос с 3 до 20. Какое утверждение верно и какой устойчивый фикс?"
  choices={[
    { label: "Хендлер теперь эмитит до 20 INFO-строк на провальный charge вместо 3, поэтому объём логов на сбой вырос ~6-7x. Устойчивый фикс: эмитить одну структурированную сводку в конце последовательности ретраев (attempts, final_outcome) и добавить counter payment_retries_total для объёма.", correct: true },
    { label: "Фикс — перейти с JSON-логирования на plaintext, чтобы сэкономить байты.", misconception: "Проблема не в кодировке, а в КОЛИЧЕСТВЕ строк. Plaintext всё равно эмитил бы строку на попытку и был бы труднее для запросов. Эмить одну сводную строку плюс метрику." },
    { label: "Снизить max_retries обратно до 3 — это и есть фикс.", misconception: "Число ретраев — бизнес-логика; менять его ради счёта за логи — неверный рычаг. Фикс — дисциплина логов: одна сводная строка на логическую операцию, а не на итерацию." },
    { label: "Ничего не изменилось — объём логов следует за трафиком, а трафик ровный.", misconception: "Объём логов следует за эмитируемыми СТРОКАМИ, а не за числом запросов. Цикл, логирующий на итерацию, умножает объём на число итераций даже при ровном трафике." },
  ]}
/>

## Сниппет 3 — алерт на cardinality

```promql
# выражение алерта
rate(prometheus_tsdb_head_series_created_total[5m]) > 1000

# triage-запрос, когда сработал:
topk(10, count by (__name__) ({__name__=~".+"}))
```

<Quiz
  id="01-three-pillars-code-3"
  lessonSlug="01-three-pillars"
  lang="ru"
  question="Что детектит этот алерт и что говорит triage-запрос, когда он срабатывает?"
  choices={[
    { label: "Алерт срабатывает, когда новые серии создаются быстрее ~1000/с — ранняя сигнатура cardinality-бомбы, до OOM. Triage-запрос ранжирует метрики по числу серий, указывая прямо на метрику, чей новый label взрывается, чтобы её labeldrop.", correct: true },
    { label: "Алерт измеряет request rate; запрос перечисляет самые медленные эндпоинты.", misconception: "head_series_created_total считает СОЗДАНИЕ серий, а не запросы. Запрос считает серии на имя метрики, а не задержку эндпоинта. Это observability cardinality, а не RED-метрики." },
    { label: "Алерт срабатывает на высоком CPU; перезапусти Prometheus, чтобы сбросить.", misconception: "Он срабатывает на скорости создания серий — предвестнике OOM. Перезапуск проигрывает WAL и перезагружает те же взрывающиеся серии — фикс это metric_relabel_configs labeldrop, затем удаление label в коде." },
    { label: "Он детектит runaway-ingestion логов по сервисам.", misconception: "Это метрики TSDB Prometheus про серии, а не ingest логов. Runaway логов детектится по GB/день на сервис в дашборде log-вендора — другой сигнал." },
  ]}
/>

## Сниппет 4 — конфиг tail-sampling

```yaml
policies:
  - name: errors-policy
    type: status_code
    status_code: {status_codes: [ERROR]}
  - name: slow-traces-policy
    type: latency
    latency: {threshold_ms: 1000}
  - name: baseline-policy
    type: probabilistic
    probabilistic: {sampling_percentage: 2}
```

<Quiz
  id="01-three-pillars-code-4"
  lessonSlug="01-three-pillars"
  lang="ru"
  question="Ревьюер спрашивает: «зачем три политики и какую стоимость мы всё ещё платим, хотя храним только ~2% успешных трейсов?»"
  choices={[
    { label: "errors-policy и slow-traces-policy держат 100% интересных трейсов (ошибки и задержка выше 1с); baseline-policy держит 2% остального, чтобы низкотрафиковые безошибочные сервисы тоже имели покрытие. Стоимость, что мы всё ещё платим: tail sampling буферизует КАЖДЫЙ span через коллектор до завершения трейса, поэтому CPU и память коллектора масштабируются с сырым трафиком, а не с хранимым объёмом.", correct: true },
    { label: "Три политики избыточны; одной baseline-policy на 2% достаточно.", misconception: "Probabilistic 2% случайно отбросил бы 98% ошибок и медленных трейсов — именно те, что нужны в инциденте. Политики error и latency существуют, чтобы гарантировать 100%-сохранение этих." },
    { label: "Tail sampling делает коллектор дешевле head sampling, потому что хранит меньше.", misconception: "Он хранит меньше, но стоит БОЛЬШЕ на коллекторе: каждый span буферизуется ради решения по контексту, поэтому ресурсы коллектора масштабируются с сырым трафиком. Head sampling отбрасывает в начале трейса и не буферизует." },
    { label: "status_code: [ERROR] держит трейсы, где HTTP-статус 500.", misconception: "Политика status_code ключуется по СТАТУСУ span (статус OTel ERROR), а не по HTTP-коду ответа. 200 с записанным статусом ошибки сохраняется; 4xx, который приложение помечает OK, может не сохраниться." },
  ]}
/>

<Recap lang="ru">
Любая проблема observability читается в коде и конфиге: неограниченный label вроде customer_email — это cardinality-бомба (убери его, держи bounded segment плюс exemplar); log-строка на итерацию умножает объём независимо от трафика (эмить одну сводку плюс counter-метрику); алерт на rate head_series_created ловит бомбу до OOM, а topk-запрос называет виновника; конфиг tail-sampling держит 100% ошибок и медленных трейсов, всё ещё платя стоимость коллектора пропорционально сырому трафику. Читай инструментирование, предсказывай ось стоимости, фикси у источника.
</Recap>
