---
concepts:
- metrics
- logs
- traces
- join-keys
- trace-id
- service-name
- observability
deepensInto: []
estMin: 10
lang: ru
lessonType: topic
level: junior
order: 1
prereqs: []
slug: 01-what-the-three-signals-are
sources:
- https://www.oreilly.com/library/view/distributed-systems-observability/9781492033431/
- https://opentelemetry.io/docs/concepts/signals/
- https://prometheus.io/docs/practices/instrumentation/
spiral:
- latency
status: ready
summary: Чем три телеметрических сигнала отличаются друг от друга, какой сигнал отвечает
  на какой вопрос дешевле и как join-ключи соединяют их в единую навигационную поверхность.
title: 'Что такое три сигнала: метрики, логи, трейсы'
track: observability
unit: 01-three-pillars
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import MetaphorComplete from "~/components/pedagogy/MetaphorComplete.astro";

<Hook>
p99 checkout вырос с 80 мс до 1,2 с. Нужно понять что, почему и где — за 5 минут, а не за 5 часов. Три сигнала дают три разных ответа на три разных вопроса. Выбрать не тот — потратить 20 минут на бесполезные поиски.
</Hook>

<Crux>Метрики, логи и трейсы не взаимозаменяемы. Каждый — самый дешёвый ответ на свой вопрос. Вопрос выбирает pillar, а не наоборот.</Crux>

<Explanation>

## Три сигнала и что каждый сохраняет

Три сигнала отличаются тем, *что сохраняют* и *что выбрасывают*.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Сигнал</th>
        <th class="border border-border px-3 py-2 text-left">Что сохраняет</th>
        <th class="border border-border px-3 py-2 text-left">Что выбрасывает</th>
        <th class="border border-border px-3 py-2 text-left">Ось стоимости</th>
      </tr>
    </thead>
    <tbody>
      <tr><td class="border border-border px-3 py-2">Метрики</td><td class="border border-border px-3 py-2">Числовые агрегаты по label-измерениям, долгая история</td><td class="border border-border px-3 py-2">Всё, что не вошло в label (user_id, request_id и т.д.)</td><td class="border border-border px-3 py-2">Кол-во активных series × retention</td></tr>
      <tr class="bg-surface-2"><td class="border border-border px-3 py-2">Логи</td><td class="border border-border px-3 py-2">Каждое поле каждого события в полном объёме</td><td class="border border-border px-3 py-2">Ничего в пределах записанного; но не записывают причинные цепочки</td><td class="border border-border px-3 py-2">Байты ingestion в день × ставка хранения</td></tr>
      <tr><td class="border border-border px-3 py-2">Трейсы</td><td class="border border-border px-3 py-2">Причинную цепочку одного запроса через сервисы</td><td class="border border-border px-3 py-2">Большинство запросов (сэмплирование обязательно)</td><td class="border border-border px-3 py-2">Span'ы в месяц × ставка backend'а трейсинга</td></tr>
    </tbody>
  </table>
</div>

## Какой сигнал отвечает на какой вопрос

**Метрики отвечают на вопрос «что происходит сейчас?»** — Rate, Errors, Duration по route и region за 2 года. Данные возвращаются за миллисекунды: предагрегация происходит при записи, а не при чтении. Слепые ко всему, что не попало в label.

**Логи отвечают на вопрос «что именно произошло?»** — конкретная трассировка стека, тело ошибки, значение поля `order_id`. Хранят каждое поле; высокая cardinality бесплатна при записи, но стоит байт ingestion.

**Трейсы отвечают на вопрос «где внутри одного запроса было потрачено время?»** — дерево span'ов из 7 сервисов с длительностью каждого. Требуют сэмплирования — хранить 100% запросов при 1000 запросах/с = миллиарды span'ов в день.

## Join-ключи: мост между сигналами

Три сигнала на трёх разных backend'ах бесполезны, если между ними нельзя перейти за один клик. Общие атрибуты — join-ключи — делают три разрозненных инструмента единой навигационной поверхностью.

Обязательные join-ключи:

- `service.name` — какой сервис эмитит данные
- `trace_id` — какой конкретный запрос
- `http.route` — шаблон маршрута (например, `/orders/{id}`, не `/orders/42`)

Когда метрики содержат `service.name` и `http.route` как label'ы, лог-строки содержат `trace_id` запроса, а трейсы содержат те же `service.name` и `http.route` — именно тогда работает «клик от всплеска метрики → лог-строка → трейс» за 30 секунд.

<Inset kind="why" lang="ru">
`trace_id` — самый важный join-ключ. Он соединяет три сигнала в конкретный запрос. Без него ты переключаешься между тремя разрозненными дашбордами и вручную сопоставляешь временны́е метки. Это разница между 5 минутами и 20 минутами триажа на инциденте.
</Inset>

<DragOrder
  id="01-what-the-three-signals-are-drag-1"
  lessonSlug="01-what-the-three-signals-are"
  lang="ru"
  prompt="p99 на checkout вырос в 14:02. Расставь шаги триажа по порядку:"
  items={[
    { id: "a", label: "Открыть дашборд метрик checkout — подтвердить Rate, Errors, Duration аномальны" },
    { id: "b", label: "Определить измерение (region, route, customer-segment) — перефильтровать метрики" },
    { id: "c", label: "Взять exemplar trace_id из медленного bucket'а гистограммы" },
    { id: "d", label: "Открыть трейс — найти span, который занял большую часть времени" },
    { id: "e", label: "Перейти в логи зависимости, отфильтровав по trace_id" },
    { id: "f", label: "Прочитать лог-строку с реальной причиной (timeout, error, queue full)" },
  ]}
  correctOrder={["a", "b", "c", "d", "e", "f"]}
/>

<Quiz
  id="01-what-the-three-signals-are-quiz-1"
  lessonSlug="01-what-the-three-signals-are"
  lang="ru"
  question="p99 API вырос с 80 мс до 1,2 с. Какой сигнал первым подтверждает, что проблема реальная, и на каком измерении?"
  choices={[
    { label: "Метрики — RED-гистограмма по route и region подтверждает всплеск и его масштаб", correct: true },
    { label: "Логи — искать error-строки с latency > 1s", misconception: "Логи дадут детали конкретного запроса, но не подтвердят системный всплеск — для этого нужна агрегация по всему трафику." },
    { label: "Трейсы — открыть последний трейс и замерить длительность", misconception: "Один трейс не показывает, системная ли проблема или изолированный случай; сначала нужно подтверждение на агрегированных метриках." },
    { label: "Профили — посмотреть CPU по функциям", misconception: "Профили показывают куда уходит CPU, но не подтверждают сам факт роста p99 по трафику." },
  ]}
/>

<Quiz
  id="01-what-the-three-signals-are-quiz-2"
  lessonSlug="01-what-the-three-signals-are"
  lang="ru"
  question="Почему трейсы обязательно сэмплируются, а метрики — нет?"
  choices={[
    { label: "Трейсы менее важны, чем метрики", misconception: "Трейсы критичны для причинного анализа; их сэмплируют не из-за низкой ценности, а из-за объёма." },
    { label: "Трейс — это дерево span'ов по всем сервисам одного запроса; хранить каждый span при 1000 запросах/с = миллиарды span'ов в день, что стоит дороже самого трафика. Метрики предагрегируются при записи — счётчик остаётся счётчиком независимо от числа запросов", correct: true },
    { label: "Трейсинг-backend'ы не поддерживают 100% хранение", misconception: "Технически 100% возможно; проблема в экономике: стоимость хранения запретительно высока." },
    { label: "W3C traceparent требует сэмплирования", misconception: "traceparent передаёт решение о сэмплировании, но само решение принимает инженер." },
  ]}
/>

<MetaphorComplete
  id="01-what-the-three-signals-are-metaphor-1"
  pieceSlug="01-what-the-three-signals-are"
  lang="ru"
  setup="Заполни пропуск: _______ — это стрелки на стенах кухни: дёшево читать, всегда включены, но показывают только то, что ты заранее пометил label'ом."
  accepted={["метрики", "метрика"]}
  canonical="метрики"
  explanation="Метрики — предагрегированные числовые измерения с фиксированным набором label'ов. Дёшево читать, потому что данные сворачиваются при записи. Но отвечают только на вопросы об измерениях, которые ты решил записывать заранее."
/>

</Explanation>

<KeyTakeaway>Три сигнала отвечают на три разных вопроса: метрики — «что происходит?» (агрегат, быстрый запрос), логи — «что именно произошло?» (каждое поле, дорогой ingestion), трейсы — «где внутри запроса потрачено время?» (причинная цепочка, обязательное сэмплирование). Join-ключи — service.name, trace_id, http.route — делают три сигнала единой навигационной поверхностью.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="01-what-the-three-signals-are-retrieval"
  lang="ru"
  questions={[
    { q: "Объясни чем метрики дешевле логов при запросе, и почему они слепее.", a: "Метрики предагрегированы при записи: TSDB хранит только итоги по (metric_name, label_set, timestamp), поэтому чтение — это обход предвычисленных bucket'ов, мгновенное за годы истории. Цена: можно задавать только вопросы об измерениях, которые ты заранее пометил label'ами. Логи хранят каждое событие целиком, поэтому любой вопрос разрешим постфактум — но backend должен сканировать или индексировать каждое поле, платя байтами ingestion и задержкой запроса." },
    { q: "Назови три обязательных join-ключа между сигналами и объясни, что сломается без каждого.", a: "service.name — без него нельзя отфильтровать метрики, логи и трейсы по конкретному сервису при навигации между backend'ами. trace_id — без него невозможно перейти от метрики к конкретному запросу: приходится вручную сопоставлять временные метки между тремя инструментами. http.route — без него метрический всплеск по маршруту не сопоставляется с лог-строками и span-атрибутами того же маршрута." },
    { q: "Команда управляет тремя разными backend'ами: Prometheus, Loki, Jaeger. В метриках поле называется route, в логах — http_path, в трейсах — http.route. Что происходит при инциденте?", a: "Автоматические переходы между сигналами (клик из метрики в лог, из лога в трейс) невозможны: у каждого dashboard'а свой ключ для одного поля. Каждый запрос при триаже требует ручного перевода между соглашениями об именовании. Всплеск, который должен занять 30 секунд, занимает 20 минут. Решение: принять OpenTelemetry Semantic Conventions единым источником истины для имён полей во всех трёх сигналах." },
  ]}
/>

<Recap lang="ru">
Три сигнала — метрики, логи, трейсы — существуют потому, что ни одна форма хранения не является дешёвой по трём осям сразу: длительный retention, высокая cardinality, полная fidelity запроса. Метрики предагрегируются при записи — запросы мгновенны за годы истории, но слепы к измерениям, не попавшим в label. Логи хранят каждое поле каждого события — мощный инструмент для любого постфактум-вопроса, но дорогой в байтах ingestion. Трейсы захватывают причинную цепочку одного запроса через сервисы — незаменимы для диагностики latency, но требуют сэмплирования при масштабе. Join-ключи — service.name, trace_id, http.route — это общие атрибуты, которые появляются идентично во всех трёх сигналах и делают переход от всплеска метрики к лог-строке и трейсу за один клик возможным.
</Recap>
