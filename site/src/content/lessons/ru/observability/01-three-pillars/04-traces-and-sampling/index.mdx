---
concepts:
- traces
- spans
- trace-id
- span-id
- traceparent
- head-based-sampling
- tail-based-sampling
- w3c-trace-context
deepensInto: []
estMin: 13
lang: ru
lessonType: topic
level: middle
order: 4
prereqs:
- observability/01-three-pillars/03-logs-and-volume
slug: 04-traces-and-sampling
sources:
- https://opentelemetry.io/docs/concepts/signals/
- https://opentelemetry.io/docs/concepts/sampling/
- https://www.oreilly.com/library/view/distributed-systems-observability/9781492033431/
spiral:
- latency
status: ready
summary: Как строится distributed trace, почему сэмплирование обязательно, и инженерные
  трейдоффы между head-based и tail-based стратегиями.
title: 'Трейсы и сэмплирование: cost-модель distributed tracing'
track: observability
unit: 01-three-pillars
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import NumbersCard from "~/components/layout/NumbersCard.astro";

<Hook>
Запрос на checkout проходит через семь сервисов перед ответом. Что-то занимает 1,2 с суммарно, но метрика говорит только «p99 вырос». Один distributed trace точно показывает, span какого сервиса потратил 1,1 с. Без сэмплирования хранить этот трейс для каждого запроса при 1000 запросах/с производит миллиард span'ов в день.
</Hook>

<Crux>Трейс — причинная цепочка одного запроса через сервисы. Хранить каждый трейс при полном трафике дорого: сэмплирование обязательно.</Crux>

<Explanation>

## Как строится трейс

Запрос, входящий на edge, получает **trace_id** (128-битный, глобально уникальный). Каждый сервис, который он затрагивает, создаёт **span**:

- `operation_name`
- `start_time`, `duration_ms`
- `attributes` (key-value пары: `http.route`, `db.system`, `error.type` и т.д.)
- `span_id` (уникальный в пределах трейса)
- `parent_span_id` (ссылка на span вызывающего сервиса)

`trace_id` и текущий `span_id` распространяются вперёд через W3C заголовок `traceparent`: `traceparent: 00-<trace_id>-<parent_span_id>-<flags>`. Collector восстанавливает дерево из span'ов; UI отображает waterfall, показывающий, где было потрачено wall-clock время.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Поле</th>
        <th class="border border-border px-3 py-2 text-left">Что идентифицирует</th>
        <th class="border border-border px-3 py-2 text-left">Пример</th>
      </tr>
    </thead>
    <tbody>
      <tr><td class="border border-border px-3 py-2">trace_id</td><td class="border border-border px-3 py-2">Весь end-to-end запрос</td><td class="border border-border px-3 py-2">128-битный UUID, общий для всех сервисов</td></tr>
      <tr class="bg-surface-2"><td class="border border-border px-3 py-2">span_id</td><td class="border border-border px-3 py-2">Одна операция внутри трейса</td><td class="border border-border px-3 py-2">64-битный, уникальный на сервис-hop</td></tr>
      <tr><td class="border border-border px-3 py-2">parent_span_id</td><td class="border border-border px-3 py-2">Кто вызвал этот span</td><td class="border border-border px-3 py-2">span_id вызывающего сервиса</td></tr>
      <tr class="bg-surface-2"><td class="border border-border px-3 py-2">traceparent</td><td class="border border-border px-3 py-2">W3C заголовок, распространяющий контекст</td><td class="border border-border px-3 py-2"><code>00-{"{"}trace_id{"}"}-{"{"}span_id{"}"}-01</code></td></tr>
    </tbody>
  </table>
</div>

## Почему сэмплирование обязательно

Хранение пропорционально числу span'ов × объёму трафика. Средний сервис эмитит 10–100 span'ов на запрос. При 1000 запросах/с:

- 10 span'ов/запрос × 1000 запросов/с × 86 400 с = **864 М span'ов/день** (минимум)
- 100 span'ов/запрос × 1000 запросов/с × 86 400 с = **8,64 Б span'ов/день** (сложный service graph)

Несжатый span — 1–5 КБ. Это терабайты trace-данных в день от одного fleet'а сервисов.

## Head-based vs tail-based сэмплирование

**Head-based сэмплирование** принимает решение в начале трейса — хранить или нет. Если решение «отбросить», SDK не эмитит span'ы вовсе — нулевые расходы на collector, нулевые сетевые байты.

- Типичный rate: 1–10% запросов
- Стоимость: предсказуемая, низкий overhead
- Слабость: равномерный random sample под-представляет редкие события (ошибки, медленные tail'ы)

**Tail-based сэмплирование** буферизирует span'ы до завершения трейса, затем принимает решение на основе полного контекста — была ли ошибка, превысила ли latency порог?

- Всегда хранит 100% трейсов с ошибками и медленных tail-трейсов
- Отбрасывает успешные быстрые трейсы по низкому base rate (0,5–5%)
- Стоимость: каждый span всё равно проходит через collector даже если в итоге отброшен — CPU и память collector'а масштабируются с raw трафиком, не с сэмплированным объёмом

Продакшн паттерн совмещает оба: head-based 10–20% baseline для ограничения входного трафика collector'а; tail-based политики поверх для сохранения 100% ошибок и медленных трейсов из того, что поступает.

<Inset kind="why" lang="ru">
Флаг `sampled` заголовка W3C `traceparent` (последний байт: `01` = сэмплирован, `00` = нет) — это то, как head-based решение распространяется вниз по стеку. Если сервис устанавливает флаг в `00`, все downstream-сервисы учитывают это решение и не эмитят span'ы, сохраняя нагрузку на collector пропорциональной rate сэмплирования, а не raw трафику.
</Inset>

<NumbersCard
  id="04-traces-sampling-numbers-ru"
  title="Числа трейсов и сэмплирования"
  stats={[
    { label: "Span'ов на запрос (средняя архитектура)", value: "10–100" },
    { label: "Span'ов в день (1k запросов/с, 10 span'ов/запрос)", value: "~864 М" },
    { label: "Типичный rate head-based сэмплирования", value: "1–10%" },
    { label: "Tail-based: сэмплирование ошибок в продакшне", value: "100%" },
    { label: "Tail-based: сэмплирование медленного tail (p99+)", value: "100%" },
    { label: "Tail-based: baseline успешных в продакшне", value: "0,5–5%" },
    { label: "Окно буферизации tail-sample", value: "30–60 с на трейс" },
    { label: "Wire overhead OTLP vs JSON", value: "~50–70% меньше" },
  ]}
/>

<Quiz
  id="04-traces-and-sampling-quiz-1"
  lessonSlug="04-traces-and-sampling"
  lang="ru"
  question="Сервис эмитит 100% трейсов и хранит все. Счёт вырастает в три раза за неделю. Какое наиболее распространённое продакшн-решение?"
  choices={[
    { label: "Переключиться на tail-based сэмплирование: 100% на ошибки и медленные трейсы, 1–2% на остальные", correct: true },
    { label: "Полностью отключить трейсинг", misconception: "Это убирает критический сигнал для триажа инцидентов." },
    { label: "Перенести трейсы на более дешёвый backend", misconception: "Дешёвый backend снижает unit cost, но не меняет объём span'ов." },
    { label: "Увеличить TTL трейсов", misconception: "Увеличение TTL увеличивает стоимость хранения; проблема — объём, а не duration retention." },
  ]}
/>

<Quiz
  id="04-traces-and-sampling-quiz-2"
  lessonSlug="04-traces-and-sampling"
  lang="ru"
  question="Почему tail-based сэмплирование дороже head-based в overhead collector'а, даже если оба хранят примерно одинаковый финальный объём?"
  choices={[
    { label: "Tail sampling использует более сложный алгоритм", misconception: "Сложность алгоритма — не главная стоимость; overhead — буферизация." },
    { label: "Head sampling отбрасывает span'ы до их поступления в collector; tail sampling буферизирует каждый span в collector'е и принимает решение после завершения трейса — поэтому CPU и память collector'а масштабируются с raw трафиком, а не с сэмплированным объёмом", correct: true },
    { label: "Tail sampling реплицирует span'ы на несколько реплик", misconception: "Репликация — не часть механизма tail sampling; collector буферизирует, а не реплицирует." },
    { label: "Tail sampling должен декодировать и перекодировать каждый span", misconception: "Транскодирование — незначительный overhead; главная стоимость — буфер памяти для всех in-flight span'ов." },
  ]}
/>

<Quiz
  id="04-traces-and-sampling-quiz-3"
  lessonSlug="04-traces-and-sampling"
  lang="ru"
  question="Запрос входит в систему с traceparent, оканчивающимся на '-00' (sampled flag = 0). Downstream-сервис хочет записать свой span. Какая спецификация определяет, что нужно изменить, чтобы последующие downstream-сервисы тоже записывали span'ы?"
  choices={[
    { label: "W3C Trace Context §3.2.2.5 — сервис должен переключить sampled flag на 1 в propagated traceparent заголовке на исходящих вызовах", correct: true },
    { label: "OpenTelemetry Sampler API — определяет SDK-side решения о сэмплировании, но не то, как решение распространяется между сервисами", misconception: "OTel Sampler API управляет принятием решения внутри одного SDK; распространение определяется W3C Trace Context." },
    { label: "W3C Baggage — определяет транспорт для user-defined контекста между сервисами", misconception: "Baggage — для кастомного контекста, а не для sampled flag трейса." },
    { label: "RFC 9110 HTTP Semantics — определяет методы и status codes", misconception: "RFC 9110 охватывает HTTP semantics; распространение trace context — W3C Trace Context." },
  ]}
/>

</Explanation>

<KeyTakeaway>Трейс — это дерево span'ов, связанных trace_id и parent_span_id, распространяемых через W3C traceparent заголовок. При 1000 запросах/с сервис производит сотни миллионов span'ов в день — сэмплирование обязательно. Head-based сэмплирование дешевле всего; tail-based наиболее точно (хранит 100% ошибок и медленных трейсов), но стоит памяти collector'а пропорционально raw трафику.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="04-traces-and-sampling-retrieval"
  lang="ru"
  questions={[
    { q: "Назови четыре поля, связывающие span'ы в дерево трейса, и что каждое из них идентифицирует.", a: "trace_id: весь end-to-end запрос, общий для всех сервисов. span_id: одна операция внутри трейса, уникальная на сервис-hop. parent_span_id: span_id вызывающего сервиса, формирующий родительско-дочернюю связь в дереве. traceparent: W3C заголовок, несущий trace_id, span_id и sampled flag через границы сервисов через HTTP заголовки." },
    { q: "Почему tail-based сэмплирование дороже head-based в overhead collector'а при одинаковом финальном хранимом объёме?", a: "Head-based принимает решение в начале трейса; если решение 'отбросить', SDK не эмитит span'ы — нулевой overhead collector'а. Tail-based должен буферизировать каждый span до завершения трейса (30–60 с) для принятия контекстно-зависимого решения, поэтому каждый span от каждого запроса проходит через collector, даже если в итоге отброшен. CPU, память и network ingress collector'а масштабируются с raw трафиком, а не с сэмплированным объёмом." },
    { q: "Сервис эмитит 10–100 span'ов на запрос при 1000 запросах/с. Оцени ежедневное число span'ов и объясни, почему 100% хранение нежизнеспособно.", a: "При 10 span'ах/запрос: 10 × 1000 × 86400 = 864 М span'ов/день. При 100 span'ах/запрос: 8,64 Б span'ов/день. Каждый span — 1–5 КБ несжатых. При 100 span'ах/запрос — 8,64 ТБ/день от одного fleet'а — больше исходного объёма трафика и по ценам tracing backend это экономически невозможно для большинства организаций." },
  ]}
/>

<Recap lang="ru">
Distributed trace — это дерево span'ов, соединённых trace_id и parent_span_id, распространяемых через границы сервисов через W3C заголовок `traceparent`. Collector восстанавливает дерево; UI показывает waterfall сервисных вызовов и их длительностей. Хранение масштабируется с числом span'ов × трафиком — средний сервис при 1000 запросах/с производит сотни миллионов span'ов в день, делая 100% retention экономически невозможным. Head-based сэмплирование дешевле всего (нулевой overhead collector'а для отброшенных трейсов), но пропускает редкие события ошибок; tail-based сэмплирование хранит 100% ошибок и медленных tail-трейсов, но буферизирует каждый span в памяти collector'а до завершения трейса, поэтому его overhead масштабируется с raw трафиком, а не с сэмплированным объёмом. Продакшн паттерн: head-based 10–20% совместно с tail-based политиками для ошибок и медленных tail'ов.
</Recap>
