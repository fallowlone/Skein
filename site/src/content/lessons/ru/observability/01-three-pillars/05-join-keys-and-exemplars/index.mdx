---
concepts:
- join-keys
- semantic-conventions
- opentelemetry
- exemplars
- otlp
- otel-collector
- service-name
deepensInto: []
estMin: 12
lang: ru
lessonType: topic
level: middle
order: 5
prereqs:
- observability/01-three-pillars/04-traces-and-sampling
slug: 05-join-keys-and-exemplars
sources:
- https://opentelemetry.io/docs/concepts/signals/
- https://www.honeycomb.io/blog/opentelemetry-is-not-three-pillars
- https://prometheus.io/docs/practices/instrumentation/
spiral:
- latency
status: ready
summary: Как общие имена атрибутов соединяют метрики, логи и трейсы в единую навигационную
  поверхность, и как exemplar''''ы соединяют агрегированные метрики с конкретными
  трейсами.
title: 'Join-ключи и exemplar''''''''ы: как три сигнала становятся компонуемыми'
track: observability
unit: 01-three-pillars
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
Метрика показывает скачок p99 на checkout. Открываешь поиск логов — но метрика использует `route="/checkout"`, лог использует `http_path="/checkout"`, а трейс использует `http.route="/checkout"`. Три разных имени поля, три разрозненных поиска. Переход, который должен занять 10 секунд, занимает 20 минут.
</Hook>

<Crux>Join-ключи делают три разрозненных backend единой навигационной поверхностью. Без них каждый триаж требует ручного перевода имён.</Crux>

<Explanation>

## Проблема join-ключей

Три сигнала на трёх разных backend'ах бесполезны, если между ними нельзя навигировать. Мост — небольшой набор общих ключей атрибутов, появляющихся идентично в метриках, логах и трейсах. Когда они расходятся — даже незначительно — каждый дашборд требует ad-hoc перевода запросов, а каждый on-call runbook хрупок.

Стандартный набор, формализованный в **OpenTelemetry Semantic Conventions**:

- `service.name` — какой сервис эмитит эти данные
- `trace_id` + `span_id` — какой запрос, какой шаг
- `http.route` — шаблон маршрута (например, `/orders/{id}`, не полный URL `/orders/42`)
- `http.request.method`, `http.response.status_code`
- `db.system`, `db.operation`, `db.namespace`
- `messaging.system`, `messaging.destination.name`

Когда метрики несут те же `service.name` и `http.route` что и лог-строки, а лог-строки несут `trace_id` запроса, их эмитировавшего, а трейсы несут тот же `http.route` атрибут что и метрика — именно тогда «клик от всплеска метрики к лог-строке к трейсу» работает меньше чем за 30 секунд.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Join-ключ</th>
        <th class="border border-border px-3 py-2 text-left">В метриках</th>
        <th class="border border-border px-3 py-2 text-left">В логах</th>
        <th class="border border-border px-3 py-2 text-left">В трейсах</th>
      </tr>
    </thead>
    <tbody>
      <tr><td class="border border-border px-3 py-2">service.name</td><td class="border border-border px-3 py-2">label</td><td class="border border-border px-3 py-2">поле</td><td class="border border-border px-3 py-2">resource attribute</td></tr>
      <tr class="bg-surface-2"><td class="border border-border px-3 py-2">trace_id</td><td class="border border-border px-3 py-2">exemplar (сэмплированный)</td><td class="border border-border px-3 py-2">обязательное поле</td><td class="border border-border px-3 py-2">корневой идентификатор</td></tr>
      <tr><td class="border border-border px-3 py-2">http.route</td><td class="border border-border px-3 py-2">label (шаблон)</td><td class="border border-border px-3 py-2">поле</td><td class="border border-border px-3 py-2">span attribute</td></tr>
      <tr class="bg-surface-2"><td class="border border-border px-3 py-2">http.response.status_code</td><td class="border border-border px-3 py-2">label (класс)</td><td class="border border-border px-3 py-2">поле (полный код)</td><td class="border border-border px-3 py-2">span attribute (полный код)</td></tr>
    </tbody>
  </table>
</div>

## Exemplar'ы: мост метрика → трейс

Exemplar — это сэмплированный `trace_id`, прикреплённый к наблюдению bucket'а histogram. Когда `http_request_duration_seconds` записывает наблюдение 1,5 с в bucket выше 1 с, клиент histogram опционально прикрепляет один `trace_id`, создавший это наблюдение. Grafana отображает их как точки на тепловой карте histogram; клик по точке открывает соответствующий трейс.

Exemplar'ы (стандартизированы в Prometheus 2.32, начало 2022, теперь стандартны в OTel exporters) устраняют разрыв: «метрики говорят, что что-то медленно, но нет конкретного примера запроса для анализа.» Цепочка перехода:

1. Всплеск histogram метрики → клик по точке exemplar
2. Exemplar открывает трейс конкретного медленного запроса
3. Span трейса показывает `db.query` занял 1,3 с
4. Лог отфильтрован по `trace_id=<тот же>` показывает полный текст запроса

Никакой ручной корреляции по временным меткам. Никакого поиска по дашбордам. Одна цепочка кликов.

## OpenTelemetry: интеграционный слой

Проект OpenTelemetry (CNCF, рождён в 2019 из слияния OpenCensus и OpenTracing) стандартизирует три вещи:

- **API** — поверхность, против которой пишут код инженеры: `tracer.start_span()`, `meter.create_counter()`.
- **SDK** — per-language реализация, строящая OTLP-сообщения.
- **OTLP** — OpenTelemetry Protocol: binary protobuf через gRPC или HTTP, примерно на 50–70% меньше JSON через HTTP.

Суть: **инструментировать один раз, маршрутизировать куда угодно**. Код приложения вызывает OTel API, SDK строит OTLP-сообщения, OTel Collector пересылает их в любой backend — Honeycomb, Datadog, Jaeger, ClickHouse, Loki, Prometheus — всё, что говорит OTLP или имеет contrib exporter.

<Inset kind="why" lang="ru">
Документ OTel Semantic Conventions — самый нагруженный элемент observability стека. Не SDK и не wire format — это сантехника. Имена полей — это семантика. Принять их для всех трёх сигналов — даже если storage backend'ы это Prometheus + Loki + Jaeger — самая высоко-леверидная инвестиция в observability гигиену, потому что именно это делает три pillar'а компонуемыми, а не конкурирующими.
</Inset>

<FlowDiagram
  label="Один trace_id соединяет все три сигнала; exemplar связывает bucket гистограммы с конкретным трейсом"
  hue="var(--d-systems)"
  nodes={[
    { id: "metric", label: "Метрика", sub: "service.name + http.route", col: 0, row: 0 },
    { id: "log", label: "Лог-строка", sub: "поле trace_id", col: 0, row: 2 },
    { id: "tid", label: "trace_id", sub: "общий join-ключ", col: 1, row: 1 },
    { id: "trace", label: "Трейс", sub: "id корневого span", col: 2, row: 1 },
    { id: "exemplar", label: "Exemplar", sub: "сэмпл. trace_id", col: 3, row: 0 },
    { id: "bucket", label: "Гистограмма", sub: "bucket > 1с", col: 4, row: 0 },
  ]}
  edges={[
    { from: "metric", to: "tid", label: "несёт" },
    { from: "log", to: "tid", label: "несёт" },
    { from: "trace", to: "tid", label: "это" },
    { from: "bucket", to: "exemplar", label: "прикрепляет" },
    { from: "exemplar", to: "trace", label: "открывает" },
  ]}
  caption="Все три сигнала несут один и тот же trace_id, поэтому всплеск метрики, его лог-строки и его трейс — в одном клике друг от друга. Exemplar прикрепляет сэмплированный trace_id к медленному bucket'у гистограммы, открывая точный трейс за всплеском."
/>

<DragOrder
  id="05-join-keys-and-exemplars-drag-1"
  lessonSlug="05-join-keys-and-exemplars"
  lang="ru"
  prompt="Пройди трёх-pillar триаж для 'p99 latency checkout вырос только в us-east-1':"
  items={[
    { id: "a", label: "Открыть RED histogram с фильтром по region — подтвердить us-east-1 p99=1,4с, eu-west-1=90мс" },
    { id: "b", label: "Кликнуть по точке exemplar на bucket'е >1с гистограммы" },
    { id: "c", label: "Открыть трейс — увидеть 1,1с потраченных в span payment-gateway" },
    { id: "d", label: "Отфильтровать логи по service=payment-proxy, region=us-east-1, около timestamp трейса" },
    { id: "e", label: "Прочитать лог-строку: 'gateway upstream connect timeout, retrying'" },
    { id: "f", label: "Фикс: добавить region как label метрики, добавить gateway.upstream.host как span attribute" },
  ]}
  correctOrder={["a", "b", "c", "d", "e", "f"]}
/>

<Quiz
  id="05-join-keys-and-exemplars-quiz-1"
  lessonSlug="05-join-keys-and-exemplars"
  lang="ru"
  question="Метрики команды используют route='/checkout', логи — http_path='/checkout', трейсы — http.route='/checkout'. Каково следствие?"
  choices={[
    { label: "Дашборды работают нормально; имена полей косметические", misconception: "Если имена полей различаются, автоматические переходы от метрики к логу к трейсу не могут использовать общий ключ — каждый запрос требует ручного перевода между соглашениями об именовании." },
    { label: "Переход от всплеска метрики к подходящей лог-строке или трейсу требует ручного перевода запросов; 30-секундный триаж превращается в 20-минутный поиск в нескольких вкладках", correct: true },
    { label: "OTel collector не может пересылать данные", misconception: "Collector пересылает данные независимо от именования; проблема — операционность при триаже, а не отказ data pipeline." },
    { label: "Cardinality растёт, потому что имена полей различаются", misconception: "Cardinality — это проблема метрик о количестве значений label, а не о консистентности именования полей." },
  ]}
/>

<Quiz
  id="05-join-keys-and-exemplars-quiz-2"
  lessonSlug="05-join-keys-and-exemplars"
  lang="ru"
  question="Почему OpenTelemetry Semantic Conventions нагружен даже для команд, остающихся на 1.0 backend'ах (Prometheus, Loki, Jaeger)?"
  choices={[
    { label: "Это снижает потребление памяти в OTel collector", misconception: "Semantic conventions о именах полей, а не об оптимизации памяти collector'а." },
    { label: "Это обязывает команды использовать OTel SDK", misconception: "Semantic conventions можно принять независимо от SDK или агента." },
    { label: "Это определяет канонические имена полей (service.name, http.route, trace_id и т.д.) чтобы три backend'а можно было навигировать с общими ключами — без чего каждый дашборд требует ad-hoc перевода, а on-call runbook'и хрупки", correct: true },
    { label: "Это устраняет необходимость в exemplar'ах", misconception: "Exemplar'ы — дополняющий механизм; semantic conventions определяют именование, но не заменяют мост метрика→трейс." },
  ]}
/>

</Explanation>

<KeyTakeaway>Общие join-ключи — service.name, trace_id, http.route — превращают три разрозненных backend'а в единую навигационную поверхность. OpenTelemetry Semantic Conventions определяет канонические имена. Exemplar'ы соединяют всплеск агрегированной метрики напрямую с одним конкретным медленным трейсом запроса, устраняя ручную кросс-сигнальную корреляцию при триаже.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="05-join-keys-and-exemplars-retrieval"
  lang="ru"
  questions={[
    { q: "Назови пять наиболее важных join-ключей для трёх-сигнального триажа и укажи, где каждый появляется.", a: "service.name: label метрики, поле лога, resource attribute трейса — идентифицирует, какой сервис эмитит данные. trace_id: exemplar метрики (сэмплированный), обязательное поле лога, корневой идентификатор трейса — кросс-pillar join-ключ для конкретного запроса. http.route: label метрики (шаблон), поле лога, span attribute трейса — шаблонный маршрут для всех трёх. http.response.status_code: label метрики (класс статуса), поле лога (полный код), span attribute трейса (полный код). db.system / db.operation: поля логов и span attributes трейсов для вызовов к базе данных." },
    { q: "Объясни цепочку клика exemplar от медленной метрики к лог-строке.", a: "1. Histogram метрики показывает всплеск в bucket'е >1 с. 2. Клик по точке exemplar — Grafana открывает trace_id, прикреплённый к этому наблюдению histogram. 3. Waterfall трейса показывает, какой span был медленным (например, db.query = 1,3 с). 4. Фильтрую логи по trace_id=<тот же> и timestamp около трейса — читаю полный текст запроса или сообщение об ошибке. Весь переход занимает меньше 30 с, потому что каждый сигнал разделяет join-ключ trace_id." },
    { q: "Почему OpenTelemetry появился из слияния OpenCensus и OpenTracing, и что он стандартизирует, чего не делал ни один предшественник в одиночку?", a: "OpenCensus (Google) стандартизировал метрики и trace-инструментацию для конкретных языков; OpenTracing стандартизировал trace API между языками, но не метрики. Их слияние в OpenTelemetry создало единый проект, охватывающий: API (language-neutral поверхность), SDK (per-language реализацию) и OTLP (единый binary wire format). Критическое дополнение — Semantic Conventions — канонические имена полей, которые делают сигналы компонуемыми между любыми backend'ами." },
  ]}
/>

<Recap lang="ru">
Три observability сигнала на отдельных backend'ах полезны только если они разделяют join-ключи — имена полей, появляющиеся идентично в label'ах метрик, полях логов и span attributes трейсов. OpenTelemetry Semantic Conventions определяет эти канонические имена: `service.name`, `trace_id`, `http.route`, `db.system` и другие. Без них переход от всплеска метрики к подходящей лог-строке требует ручного перевода; с ними — один клик. Exemplar'ы расширяют это: `trace_id`, прикреплённый к наблюдению bucket'а histogram, соединяет всплеск агрегированной метрики напрямую с трейсом одного конкретного медленного запроса. OpenTelemetry (API + SDK + OTLP wire format) — субстрат, который на практике делает все три сигнала разделяющими эти join-ключи при любой комбинации backend'ов.
</Recap>
