---
concepts:
- docker-rootless
deepensInto: []
estMin: 16
lang: ru
lessonType: topic
level: senior
order: 1
prereqs: []
slug: "01-rootless-and-user-namespaces"
sources:
- https://docs.docker.com/engine/security/rootless/
- https://docs.docker.com/engine/security/userns-remap/
- https://man7.org/linux/man-pages/man7/user_namespaces.7.html
spiral: []
status: ready
summary: 'По умолчанию UID 0 контейнера — это UID 0 хоста, так что побег через bind-mount пишет в ФС хоста от root. User namespaces ремапят root в диапазон из /etc/subuid; rootless Docker гоняет демон без привилегий, платя ~20-30% пропускной способности slirp4netns.'
title: 'Rootless Docker и user namespaces: почему root контейнера — это root хоста и как разорвать связь'
track: "docker"
unit: "07-security"
---

import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
Инцидент начался с однострочного удобства в Compose-файле: разработчик примонтировал хостовый `/var/www` в контейнер, чтобы правки были видны вживую, и контейнер — как почти любой контейнер — гонял свой процесс от root. Спустя месяцы этот образ подцепил уязвимую зависимость, атакующий получил шелл внутри контейнера, и `whoami` вернул `root`. Он сделал `chmod 777` на примонтированном каталоге, положил веб-шелл, а затем поднялся по монтированию вверх: каждый файл, что контейнер писал в `/var/www`, ложился на хост во владение UID 0, потому что UID 0 контейнера *и есть* UID 0 хоста. Mount namespace изолировал, какие пути процесс видит, а не кем он является. Ничто в дефолтах Docker не говорило: «root этого контейнера — это тот самый root, что владеет вашим сервером». Фикс, который команда выкатила на той неделе, был не пропатченной зависимостью — это был userns-remap, делающий root контейнера бессильным хостовым UID в диапазоне 100000, чтобы тот же побег писал файлы во владение пользователя, который не владеет ничем.
</Hook>

<Crux>UID 0 контейнера по умолчанию — это UID 0 хоста; user namespaces ремапят его, чтобы побег был бессильным, а rootless убирает root-демон.</Crux>

<Explanation>

## Дефолт-ловушка, о которой не предупреждают: root общий

Linux-контейнер — это всего лишь процесс с namespaces и cgroups, не VM. По умолчанию user namespace *не* входит в эти namespaces, а значит трансляции UID нет: `root` (UID 0) внутри контейнера буквально тот же UID 0, что ядро использует на хосте. Namespaces PID, mount и network изолируют *что процесс видит* — его дерево процессов, его вид ФС, его интерфейсы — но не меняют, *кто он*. Поэтому как только процесс контейнера касается чего-то общего с хостом, личность важнее видимости. Классический канал — bind mount: `-v /var/www:/data` даёт контейнеру вид реального хостового каталога, и любой файл, что root контейнера туда пишет, принадлежит root хоста. Брось уязвимый образ в эту схему — и компрометация контейнера становится записью в ФС хоста с владением root, без всякого эксплойта ядра, просто из-за дефолта, что root контейнера равен root хоста.

Фикс на уровне ядра — user namespace. Он вводит помапную UID/GID-трансляцию, так что UID 0 внутри namespace отображается в некий непривилегированный UID на хосте. Маппинг настраивается из `/etc/subuid` и `/etc/subgid`, которые выделяют каждому пользователю непрерывный диапазон — по соглашению **65536 подряд идущих подчинённых UID**, начиная с высокой базы вроде 100000. С включённым userns-remap UID 0 контейнера становится хостовым UID 100000, UID 1 контейнера — 100001, и так по всему диапазону шириной 65536. Процесс, сбежавший из контейнера как «root», теперь — хостовый UID 100000, который не владеет ничем и не имеет хостовых привилегий. Радиус поражения полной компрометации контейнера сжимается с «root на хосте» до «аккаунта, что не может даже прочитать файлы других пользователей».

<Quiz
  id="01-rootless-and-user-namespaces-quiz-1"
  lessonSlug="01-rootless-and-user-namespaces"
  lang="ru"
  question="Контейнер гонит процесс от root и примонтировал хостовый каталог. С дефолтными настройками Docker (без userns-remap) атакующий получает шелл и пишет файл в примонтированный каталог. Кому этот файл принадлежит на хосте?"
  choices={[
    { label: "root хоста (UID 0) — без user namespace UID 0 контейнера — это тот же UID 0, что ядро использует на хосте", correct: true },
    { label: "Непривилегированному пользователю, ведь контейнер отделён namespace'ами от хоста", misconception: "Namespaces PID, mount и network изолируют, что процесс видит, а не его UID. Без user namespace ремаппинга UID нет, так что root контейнера пишет хостовые файлы как root хоста." },
    { label: "Пользователю nobody (UID 65534), которого Docker назначает всем контейнерам", misconception: "Docker не дефолтит в nobody; контейнер гонит то, что объявил USER образа, а это root, пока не изменено. Отображение root в непривилегированный UID — это ровно то, что добавляет userns-remap, и он выключен по умолчанию." },
    { label: "Запись падает — ядро блокирует запись контейнера в хостовый путь", misconception: "Bind mounts по умолчанию на чтение-запись, и запись удаётся. Опасность как раз в том, что она удаётся с владением root хоста." },
  ]}
/>

<Inset kind="why" lang="ru">
Почему userns-remap выключен по умолчанию, если он так полезен? Потому что сдвиг UID ломает допущения. Файлы в bind mount должны быть читаемы ремапнутым диапазоном, так что существующие хостовые файлы во владении UID 1000 невидимы контейнеру, чей root теперь — хостовый UID 100000: либо re-chown, либо принимаешь трение. Некоторые нагрузки, что реально нуждаются в хостовой UID-личности (отдельные storage-драйверы, host-networked агенты), перестают работать. Docker выбрал разрешающий дефолт, что «просто работает» с монтированиями, над безопасным дефолтом, что удивляет людей, и оставил remap опциональным. Этот компромисс — вся причина существования этого урока: удобный дефолт и есть небезопасный.
</Inset>

## Rootless Docker: вообще без привилегированного демона

User-namespace remap всё ещё гонит *демон* Docker от root хоста — он лишь ремапит контейнеры. Rootless Docker идёт дальше: весь демон и все контейнеры работают внутри user namespace одного пользователя, запущенные вообще без root-привилегии. Нет root-овладеемого `dockerd`, нет root-овладеемого сокета, так что даже компрометация демона приземляется как непривилегированный пользователь. Это закрывает крупнейшую оставшуюся цель — исторически записываемый `/var/run/docker.sock` — это мгновенный примитив root-на-хосте, ведь любой, кто может говорить с демоном, может запустить контейнер, монтирующий хост, — и rootless делает этот сокет бессильным.

Цена платится в основном в сети. Без root rootless Docker не может создать обычную `veth`/bridge-обвязку, поэтому маршрутизирует трафик контейнера через userspace TCP/IP-стек — **slirp4netns** — который копирует пакеты в userspace и обратно и накладывает штраф пропускной способности, обычно измеряемый около **20-30%** против rootful-bridge (смягчается `slirp4netns` с `sandbox=false` или `pasta`, но не устраняется). Бинд привилегированных портов ниже 1024 тоже требует доп-настройки, раз демону недостаёт `CAP_NET_BIND_SERVICE` на хосте. Для CI-раннера или мультиарендного build-хоста платить пятой частью сетевой пропускной способности, чтобы удалить «побег контейнера равен root хоста» из модели угроз, обычно правильный размен; для упирающегося в пропускную способность сетевого аплайанса — может, и нет.

<Quiz
  id="01-rootless-and-user-namespaces-quiz-2"
  lessonSlug="01-rootless-and-user-namespaces"
  lang="ru"
  question="Какова главная рантайм-цена, что команда принимает, переходя с rootful Docker на rootless Docker, и почему?"
  choices={[
    { label: "Сниженная сетевая пропускная способность (~20-30%), потому что трафик контейнера маршрутизируется через userspace-стек slirp4netns вместо bridge, созданного root", correct: true },
    { label: "Контейнеры больше не могут использовать capabilities, так что большинство образов перестаёт работать", misconception: "Capabilities всё ещё работают внутри user namespace контейнера; cap-drop/cap-add ведут себя нормально. Реальная цена — в хостовой сети, где отсутствие root вынуждает userspace-сетевой стек." },
    { label: "Образы надо пересобрать, ведь rootless использует другой формат образов", misconception: "Rootless использует те же OCI-образы и те же реестры. Формат образов не меняется — меняется лишь то, как демон и его сеть работают на хосте." },
    { label: "Производительность CPU падает, потому что каждый syscall эмулируется в userspace", misconception: "Syscalls выполняются нативно; rootless — не эмулятор вроде gVisor. Лишь сетевой путь идёт через userspace (slirp4netns), так что измеримый штраф — сетевая пропускная способность, а не CPU." },
  ]}
/>

</Explanation>

<FlowDiagram
  label="User-namespace remap транслирует UID 0 контейнера в непривилегированный хостовый диапазон UID, выделенный в /etc/subuid"
  hue="var(--d-security)"
  perRow={3}
  nodes={[
    { id: "cuid", label: "UID 0 контейнера", sub: "процесс думает, что он root" },
    { id: "subuid", label: "/etc/subuid", sub: "диапазон шириной 65536" },
    { id: "map", label: "UID-маппинг", sub: "0 -> 100000" },
    { id: "hostuid", label: "Хостовый UID 100000", sub: "не владеет ничем" },
    { id: "mount", label: "Запись в bind-mount", sub: "во владении 100000" },
    { id: "escape", label: "Побег приземляется", sub: "как бессильный юзер" },
  ]}
  edges={[
    { from: "cuid", to: "subuid", label: "remap" },
    { from: "subuid", to: "map", label: "выделить" },
    { from: "map", to: "hostuid", label: "0 в 100000" },
    { from: "hostuid", to: "mount", label: "пишет" },
    { from: "hostuid", to: "escape", label: "сдержан" },
  ]}
  caption="Без user namespace вся эта цепочка схлопывается: UID 0 контейнера — это UID 0 хоста, так что запись в bind-mount и любой побег приземляются как root хоста. Remap — это то, что превращает «root» в бессильный хостовый аккаунт."
/>

<KeyTakeaway>По умолчанию контейнер делит UID-пространство хоста, так что root контейнера — это root хоста: побег через bind-mount пишет хостовые файлы как UID 0 без всякого бага ядра. User-namespace remap отображает UID 0 контейнера в непривилегированный хостовый UID из диапазона /etc/subuid шириной 65536, так что побег приземляется как бессильный аккаунт. Rootless Docker вообще убирает привилегированный демон, платя ~20-30% сетевой пропускной способности через userspace-стек slirp4netns, чтобы удалить «побег равен root хоста» из модели угроз.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="01-rootless-and-user-namespaces-retrieval"
  lang="ru"
  questions={[
    { q: "Объясни, почему контейнер, работающий от root с bind mount, опасен на дефолтном Docker, и что именно меняет userns-remap.", a: "По умолчанию Docker не включает user namespace, так что между контейнером и хостом нет UID-трансляции: UID 0 контейнера — тот же UID 0, что ядро применяет на хосте. Namespaces PID, mount и network изолируют, что процесс видит, а не его личность. Bind mount вроде -v /var/www:/data открывает реальный хостовый каталог, так что любой файл, что root контейнера туда пишет, во владении root хоста — и скомпрометированный контейнер становится записью в ФС хоста с владением root, без эксплойта ядра. Userns-remap включает помапную UID/GID-трансляцию, настроенную из /etc/subuid (по соглашению диапазон шириной 65536 от базы вроде 100000): UID 0 контейнера отображается в хостовый UID 100000, что не владеет ничем и не держит хостовых привилегий. Тот же побег теперь пишет файлы во владении бессильного аккаунта, сжимая радиус поражения с root хоста до пользователя, что не может даже читать файлы других." },
    { q: "Что rootless Docker добавляет поверх userns-remap, и какова конкретная цена?", a: "Userns-remap всё ещё гонит сам демон Docker от root хоста и ремапит лишь контейнеры; компрометация демона или docker.sock всё ещё root-на-хосте. Rootless Docker гонит весь демон и все контейнеры внутри user namespace одного непривилегированного пользователя, без root-овладеемого dockerd или сокета где-либо — так что даже компрометация демона приземляется как непривилегированный пользователь, а классический примитив «записываемый docker.sock равен мгновенному root хоста» становится бессильным. Цена платится в сети: без root демон не может создать обычную veth/bridge-обвязку, так что трафик контейнера идёт через userspace-стек slirp4netns, который копирует пакеты через userspace и накладывает штраф пропускной способности обычно около 20-30% против rootful-bridge. Привилегированные порты ниже 1024 тоже требуют доп-настройки на хосте. Для CI-раннеров и мультиарендных build-хостов размен обычно того стоит; для упирающихся в пропускную способность сетевых нагрузок — может, и нет." },
  ]}
/>

<Recap lang="ru">
Контейнер — это namespace-отделённый, cgroup-ограниченный процесс, не VM, и по умолчанию user namespace не входит в его namespaces — так что ремаппинга UID нет и UID 0 контейнера — это UID 0 хоста. Namespaces PID, mount и network изолируют, что процесс видит, а не кто он, поэтому bind mount превращает компрометацию контейнера в запись в ФС хоста во владении root: через общий путь утекает личность, а не видимость. Фикс ядра — user namespace, настраиваемый из /etc/subuid и /etc/subgid, которые выделяют каждому пользователю непрерывный диапазон, по соглашению шириной 65536 UID от высокой базы вроде 100000; с включённым userns-remap UID 0 контейнера отображается в хостовый UID 100000, и побег приземляется как аккаунт, что не владеет ничем. Rootless Docker идёт дальше и гонит весь демон и его контейнеры внутри одного непривилегированного user namespace, так что нет root-овладеемого dockerd или docker.sock для компрометации — закрывая примитив «записываемый сокет равен root хоста». Цена в основном сетевая: rootless не может построить обычный bridge, так что трафик маршрутизируется через userspace-стек slirp4netns со штрафом примерно 20-30% пропускной способности, а привилегированные порты требуют доп-настройки. Дефолты удобны и небезопасны по замыслу — userns-remap опционален, потому что сдвиг UID ломает допущения bind-mount — так что сеньорский ход: сознательно выбрать безопасную конфигурацию и заплатить её известную, ограниченную цену.
</Recap>
