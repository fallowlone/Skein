---
concepts:
- docker-namespaces
deepensInto: []
estMin: 16
lang: ru
lessonType: topic
level: senior
order: 1
prereqs: []
slug: "01-namespaces"
sources:
- https://man7.org/linux/man-pages/man7/namespaces.7.html
- https://man7.org/linux/man-pages/man2/clone.2.html
- https://github.com/opencontainers/runtime-spec/blob/main/config-linux.md
spiral: []
status: ready
summary: "Контейнер — обычный процесс, чей вид со стороны ядра огорожен namespaces. Типов 8; runc разворачивает их флагами clone(). PID, mount и network делают видимую изоляцию — а забытый PID namespace означает, что зомби некому пожинать."
title: "Namespaces: вид ядра, который контейнеру дозволено видеть"
track: "docker"
unit: "02-runtime-internals"
---

import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
Пейджер дежурного сообщил: на ноде кончились PID. В worker-поде крутился shell-скрипт, который тысячи раз в час дёргал `curl`, и каждый `curl` оставлял зомби: записи `<defunct>` копились в `ps`, таблица PID ядра заполнялась, новые fork падали с `EAGAIN`. Автор клялся, что скрипт пожинает своих детей. Так и было — в версии, что запускалась под systemd. В контейнере скрипт работал как PID 1. А у PID 1 есть особая роль ядра, унаследованная от загрузочного init: он обязан делать `wait()` на осиротевших процессах, иначе их записи никогда не очищаются. Обычный shell этого не делает. Фикс — три байта в Dockerfile: добавить `--init`, чтобы крошечный reaper работал как PID 1. Урок под этим: у контейнера был PID namespace, namespace перенумеровал скрипт в 1, а PID 1 несёт обязанности ядра, на которые автор не подписывался.
</Hook>

<Crux>Namespace — вид одного глобального ресурса ядра, отдельный для процесса. Контейнер — обычный процесс с подменёнными видами: не VM.</Crux>

<Explanation>

## Восемь видов, один процесс

Namespace оборачивает один глобальный ресурс ядра так, что процессы внутри видят свой экземпляр этого ресурса. На современном ядре **8 типов namespace**, и контейнер — это просто процесс, помещённый в свежий набор из них:

- **PID** — идентификаторы процессов; первый процесс становится PID 1.
- **mount** (`mnt`) — дерево файловой системы; именно это делает `/` контейнера его собственным корнем.
- **network** (`net`) — интерфейсы, таблицы маршрутизации, loopback, iptables.
- **UTS** — hostname и domainname (так `hostname` внутри отличается от хостового).
- **IPC** — System V IPC и POSIX-очереди сообщений.
- **user** — карты UID/GID; позволяет root-в-контейнере (UID 0) отобразиться в непривилегированный хостовый UID.
- **cgroup** — какой вид дерева cgroup видит процесс.
- **time** — смещения boot/monotonic-часов (добавлен в ядре 5.6).

Ключевой сдвиг в голове: процесс-контейнер — **объект того же рода, что любой хостовый процесс**. `ps` на хосте показывает его напрямую, его планирует тот же планировщик, в том же ядре. Нет гостевого ядра, нет ловушки гипервизора. Изоляция — это лишь: когда процесс спрашивает «какие PID существуют?», ядро отвечает из его PID namespace, а не из глобального.

```c
// Примерно то, что рантайм делает, создавая первый процесс контейнера.
// CLONE_NEWPID | CLONE_NEWNS | CLONE_NEWNET | CLONE_NEWUTS |
// CLONE_NEWIPC | CLONE_NEWUSER | CLONE_NEWCGROUP  — по флагу на namespace.
int flags = CLONE_NEWPID | CLONE_NEWNS | CLONE_NEWNET |
            CLONE_NEWUTS | CLONE_NEWIPC | CLONE_NEWUSER | CLONE_NEWCGROUP;
pid_t pid = clone(child_fn, stack_top, flags | SIGCHLD, arg);
// child_fn теперь работает как PID 1 в новеньком наборе namespace.
```

`runc` — OCI-рантайм, который вызывают Docker и containerd, — читает массив `linux.namespaces` из `config.json` контейнера и делает `unshare(2)` / `clone(2)` именно для них. Namespaces ортогональны: можно взять сетевой namespace, но разделить хостовый PID namespace — именно это делает `docker run --pid=host`, и именно это позволяет отладочному sidecar выполнить `ps` целевого контейнера.

<Quiz
  id="01-namespaces-quiz-1"
  lessonSlug="01-namespaces"
  lang="ru"
  question="Главный процесс контейнера завершается чисто, но worker-под медленно копит <defunct> (зомби), пока fork не падают с EAGAIN. Образ запускает shell-скрипт как entrypoint. В чём корневая причина?"
  choices={[
    { label: "Скрипт работает как PID 1 в PID namespace контейнера, а обычный shell не делает wait() на осиротевших детях, поэтому их записи в таблице процессов не пожинаются", correct: true },
    { label: "Утечка памяти в скрипте заполняет RAM, и ядро OOM-убивает fork", misconception: "EAGAIN при fork из-за множества <defunct> — это исчерпание таблицы PID непожатыми зомби, а не давление памяти: OOM-kill даёт SIGKILL и код 137, а не накопление defunct-записей." },
    { label: "У контейнера нет сетевого namespace, поэтому DNS-запросы в curl зависают и текут процессы", misconception: "Зомби — уже завершившиеся процессы, ждущие вызова wait(); они держат слот PID, но не ресурсы и к сети не относятся. Причина в том, кто PID 1, а не в net namespace." },
    { label: "cgroup pids.max выставлен слишком низко, и ядро помечает лишние процессы как defunct", misconception: "Лимит pids.max блокирует новые fork с EAGAIN, но никогда не создаёт <defunct>: defunct означает завершён-но-не-пожат, что объясняется только отсутствием wait() у PID 1." },
  ]}
/>

<Inset kind="why" lang="ru">
Почему PID 1 несёт эту обязанность? Когда родитель процесса умирает, сирота переусыновляется — в PID namespace к PID 1 этого namespace, а не к хостовому init. Ядро отдаёт труп каждого сироты PID 1 и ждёт, что PID 1 сделает `wait()`. Хостовый `/sbin/init` написан, чтобы это делать; ваш `entrypoint.sh` — нет. `docker run --init` (или entrypoint `tini`/`dumb-init`) вставляет 10-КБ reaper как PID 1, который пробрасывает сигналы и пожинает сирот, восстанавливая контракт, который namespace тихо навязал.
</Inset>

## Mount и network: видимая изоляция

Два namespace делают бо́льшую часть того, что вы *видите*. **Mount namespace** даёт контейнеру своё дерево файловой системы: рантайм делает `pivot_root` в распакованный образ, так что `/` контейнера — корень образа, а хостовые пути просто не существуют в его виде, если только не примонтированы через bind. **Network namespace** даёт ему свой loopback, свои интерфейсы, свои таблицы маршрутизации и фаервола — поэтому у свежего контейнера есть только `lo`, пока рантайм не переместит один конец пары `veth` внутрь, а другой не подключит к мосту на хосте. Namespace живёт, пока в нём есть процесс *или* пока его пинит файловый дескриптор либо bind-mount; так `ip netns` и `docker exec` повторно входят в namespaces существующего контейнера через `setns(2)`, а не создают новые.

**User namespace** — краеугольный камень безопасности и тот, что чаще всего забывают: с `userns-remap` UID 0 внутри контейнера отображается в непривилегированный хостовый UID (скажем, 100000), так что «root» контейнера, сбежавший из mount, имеет на хосте привилегии nobody. Без него root-в-контейнере становится root-на-хосте в тот миг, как любой другой барьер (плохой bind-mount, забытая невыброшенная capability) поддастся.

<Quiz
  id="01-namespaces-quiz-2"
  lessonSlug="01-namespaces"
  lang="ru"
  question="Почему отладочный sidecar, запущенный с --pid=host, может выполнить ps и увидеть процессы целевого контейнера, хотя у того свои mount и network namespaces?"
  choices={[
    { label: "Namespaces ортогональны и выбираются по типу; разделение хостового PID namespace делает все процессы видимыми, а mount и network остаются изолированными", correct: true },
    { label: "ps читает /proc по сети, а sidecar по умолчанию разделяет хостовый сетевой namespace", misconception: "ps читает локальную файловую систему /proc, а не сеть. Видимость даёт разделение PID namespace, что и выбирает --pid=host; сетевой namespace независим." },
    { label: "--pid=host отключает для sidecar всё namespacing, помещая его целиком на хост", misconception: "Он разделяет только PID namespace; sidecar сохраняет свои mount, network, UTS и прочие namespaces. Namespaces присоединяются или изолируются по одному типу, а не всё-или-ничего." },
    { label: "Sidecar работает как root, а root может читать namespace любого процесса независимо от членства", misconception: "Быть root не даёт процессу видеть PID в namespace, членом которого он не является: ядро отвечает на запросы к /proc из PID namespace вызывающего. Важно членство, данное здесь через --pid=host." },
  ]}
/>

</Explanation>

<FlowDiagram
  label="Обычный процесс становится контейнером, когда рантайм разворачивает вокруг него свежий набор namespaces"
  hue="var(--d-infra)"
  perRow={3}
  nodes={[
    { id: "proc", label: "Обычный процесс", sub: "хостовый PID, хостовый /" },
    { id: "clone", label: "clone() / unshare()", sub: "флаги CLONE_NEW*" },
    { id: "pid", label: "PID ns", sub: "становится PID 1" },
    { id: "mnt", label: "mount ns", sub: "pivot_root в образ" },
    { id: "net", label: "net ns", sub: "свой lo + veth" },
    { id: "userns", label: "user ns", sub: "root → хостовый nobody" },
  ]}
  edges={[
    { from: "proc", to: "clone", label: "runc применяет config.json" },
    { from: "clone", to: "pid", label: "CLONE_NEWPID" },
    { from: "clone", to: "mnt", label: "CLONE_NEWNS" },
    { from: "clone", to: "net", label: "CLONE_NEWNET" },
    { from: "clone", to: "userns", label: "CLONE_NEWUSER" },
  ]}
  caption="То же ядро, тот же планировщик — меняются только виды. Namespaces ортогональны, поэтому каждый изолируется или разделяется независимо (--pid=host разделяет один, изолирует остальные)."
/>

<KeyTakeaway>Контейнер — обычный Linux-процесс с подменёнными видами со стороны ядра: 8 типов namespace, каждый оборачивает один глобальный ресурс, применяются runc через флаги clone()/unshare() из OCI-конфига. PID перенумеровывает entrypoint в 1 (и нагружает его пожинанием сирот), mount даёт ему образ как корень, network даёт изолированный стек, а user namespace ремапит root в непривилегированный хостовый UID. Гостевого ядра нет — изоляция в том, что ядро отвечает на каждый запрос из namespace вызывающего.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="01-namespaces-retrieval"
  lang="ru"
  questions={[
    { q: "Назови 8 типов namespace и для PID, mount, network и user скажи, какой глобальный ресурс каждый огораживает и одно конкретное следствие этого.", a: "Восемь: PID, mount (mnt), network (net), UTS, IPC, user, cgroup и time. PID огораживает идентификаторы процессов: entrypoint становится PID 1 и наследует обязанность wait() на осиротевших детях, поэтому обычный shell-entrypoint течёт зомби, пока fork не падают с EAGAIN. Mount огораживает дерево ФС: рантайм делает pivot_root в образ, так что / контейнера — корень образа, а хостовые пути невидимы без bind-mount. Network огораживает интерфейсы/маршруты/фаервол: у свежего контейнера есть только lo, пока пара veth не подключит его к хостовому мосту. User огораживает карты UID/GID: с userns-remap UID 0 контейнера отображается в непривилегированный хостовый UID, так что побег приземляется как nobody, а не как хостовый root." },
    { q: "Объясни, почему ортогональность namespaces важна операционно, на примерах --pid=host и setns.", a: "Каждый тип namespace выбирается независимо, поэтому процесс может изолировать одни и разделить другие. docker run --pid=host даёт контейнеру хостовый PID namespace, сохраняя его собственные mount и network namespaces — именно так отладочный sidecar выполняет ps целевого контейнера, ведь запросы к /proc отвечаются из разделяемого PID namespace. Та же ортогональность позволяет docker exec и ip netns повторно входить в существующий контейнер вызовом setns(2) на запиненном дескрипторе namespace, а не создавать новые namespaces, присоединяя только нужные типы." },
  ]}
/>

<Recap lang="ru">
Контейнер — не маленькая виртуальная машина; это обычный процесс, который ядро планирует как любой другой, отличающийся лишь тем, какие namespaced-виды он держит. Namespace оборачивает один глобальный ресурс так, что его члены видят приватный экземпляр, и современное ядро предлагает 8: PID, mount, network, UTS, IPC, user, cgroup и time. OCI-рантайм runc читает массив namespaces из config.json и применяет соответствующие флаги CLONE_NEW* через clone() или unshare(). PID namespace перенумеровывает entrypoint в 1, что тихо передаёт ему работу init по пожинанию осиротевших детей — пропусти reaper вроде --init или tini, и зомби копятся, пока таблица PID не исчерпается и fork не вернёт EAGAIN. Mount namespace делает pivot_root в распакованный образ, так что корень контейнера — образ, а хостовые пути исчезают из вида. Network namespace стартует только с loopback, пока пара veth не соединит его с хостом. User namespace — краеугольный камень безопасности: ремаппинг root контейнера в непривилегированный хостовый UID превращает побег в не-событие. Поскольку namespaces ортогональны, каждый разделяется или изолируется отдельно — --pid=host разделяет ровно один, чтобы sidecar мог сделать ps целевого, а setns позволяет exec войти в запиненный namespace, а не строить новый. Ядро одно и то же; меняются лишь виды.
</Recap>
