---
concepts:
- docker-lifecycle
deepensInto: []
estMin: 16
lang: ru
lessonType: topic
level: senior
order: 4
prereqs: []
slug: "04-the-container-lifecycle"
sources:
- https://github.com/opencontainers/runtime-spec/blob/main/runtime.md
- https://man7.org/linux/man-pages/man2/clone.2.html
- https://github.com/containerd/containerd/blob/main/docs/getting-started.md
spiral: []
status: ready
summary: "Жизнь контейнера — это dockerd → containerd → shim → runc. runc настраивает namespaces/cgroups, делает exec бинаря и выходит — shim остаётся родителем PID. Стоп — это SIGTERM, 10 с grace, затем SIGKILL; сигналы доходят до PID 1, лишь если он их пробрасывает."
title: "Жизненный цикл контейнера: от docker run до пожатого выхода"
track: "docker"
unit: "02-runtime-internals"
---

import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
У деплоев был налог, который никто не мог объяснить: каждый раскат добавлял около десяти секунд на под чистого мёртвого времени. Приложение логировало «shutting down gracefully», оркестратор логировал SIGTERM, но контейнер всегда висел весь grace-период перед смертью. Уликой был entrypoint: `ENTRYPOINT sh -c "node server.js"`. Shell был PID 1. Когда оркестратор слал SIGTERM, тот шёл в PID 1 — в shell, — у которого нет обработчика SIGTERM и который, как PID 1, не получает дефолтного действия от ядра, так что просто игнорировал сигнал. `node` его не видел, продолжал обслуживать и умирал лишь когда grace-таймер истекал и рантайм эскалировал до SIGKILL — ровно десятью секундами позже, каждый раз. Фикс — exec-форма, `ENTRYPOINT ["node", "server.js"]`, чтобы `node` *был* PID 1 и получал SIGTERM напрямую. Десятисекундный налог был стоп-последовательностью цикла, столкнувшейся с shell, который глотал сигнал.
</Hook>

<Crux>Жизнь контейнера — цепочка (dockerd, containerd, shim, runc), создающая процесс, и затем стоп-последовательность: SIGTERM, grace, SIGKILL.</Crux>

<Explanation>

## Цепочка, что запускает контейнер

`docker run` не порождает твой процесс напрямую. Есть продуманная цепочка, у каждого слоя одна работа:

- **dockerd** (демон Docker) — API и управление образами; он не родительствует контейнерам.
- **containerd** — супервизор контейнеров: тянет образы, управляет циклом, говорит с рантаймом.
- **containerd-shim** — по одному на контейнер; он — стойкий **родитель** процесса контейнера. Shim — то, что даёт containerd (и даже dockerd) рестартовать без убийства работающих контейнеров, держит открытым stdio и сообщает код выхода.
- **runc** — OCI-рантайм. Он делает реальную работу ядра: `clone()`/`unshare()` namespaces, пишет лимиты cgroup, ставит capabilities и seccomp-фильтр, `pivot_root` в образ, затем **`execve()` твоего бинаря, чтобы он заменил сам runc**. После runc **выходит** — он не работающий супервизор, а лишь инструмент настройки.

Последнее удивляет людей: после старта `runc` исчез. Твой процесс теперь PID 1 в своём namespace, родитель (с точки зрения хоста) — shim. Поэтому контейнеры дёшево гонять тысячами: нет толстого по-контейнерного демона, лишь крошечный shim и твой процесс прямо на хостовом ядре.

```text
dockerd ──API──▶ containerd ──▶ containerd-shim-runc-v2  (остаётся родителем)
                                      │
                                      └─▶ runc create/start
                                            ├─ unshare namespaces
                                            ├─ применить лимиты cgroup
                                            ├─ задать caps + seccomp
                                            ├─ pivot_root → образ
                                            └─ execve(entrypoint)  ← становится PID 1
                                      runc выходит; shim пожинает код выхода
```

## Стоп: SIGTERM, grace, SIGKILL

Остановка контейнера — не убийство, а переговоры с дедлайном. `docker stop` (и завершение пода в Kubernetes) шлёт **SIGTERM в PID 1**, ждёт grace-период (**дефолт Docker 10 с**; `terminationGracePeriodSeconds` Kubernetes дефолт 30 с), и если процесс ещё жив, шлёт **SIGKILL**, который процесс не может поймать или проигнорировать. Контракт таков: PID 1 должен обработать SIGTERM, перестать брать новую работу, слить запросы в полёте и выйти до таймера. Загвоздка — та, что из Hook: **сигналы доставляются в PID 1, и только в PID 1** — если PID 1 это shell или обёртка, не пробрасывающая сигналы детям, реальный сервер никогда не услышит SIGTERM, и grace-период — чистая латентность перед SIGKILL. Это операционная причина, почему exec-форма (`ENTRYPOINT ["app"]`) и init-шимы (`--init`, tini) важны: они ставят сигнало-осведомлённый процесс на PID 1.

<Quiz
  id="04-the-container-lifecycle-quiz-1"
  lessonSlug="04-the-container-lifecycle"
  lang="ru"
  question="После того как runc запустил контейнер, какой процесс — стойкий родитель PID 1 контейнера на хосте, и какую роль runc играет потом?"
  choices={[
    { label: "containerd-shim — стойкий родитель; runc делает exec entrypoint (так что бинарь заменяет runc) и затем выходит — это инструмент настройки, а не работающий супервизор", correct: true },
    { label: "runc остаётся работать как родитель-супервизор на всю жизнь контейнера", misconception: "runc не супервизор — он настраивает namespaces, cgroups, caps и seccomp, затем делает execve() entrypoint (что заменяет процесс runc) и выходит. Стойкий родитель — shim, поэтому containerd может рестартовать без убийства контейнеров." },
    { label: "dockerd — прямой родитель каждого процесса контейнера", misconception: "dockerd — это API/демон и намеренно не родитель контейнера — эта развязка даёт dockerd рестартовать без снятия контейнеров. Родитель — по-контейнерный shim." },
    { label: "containerd родительствует процессу напрямую без всякого shim", misconception: "Shim есть на каждый контейнер именно чтобы сам containerd не был родителем; shim держит stdio и код выхода и переживает рестарты containerd. containerd управляет, shim родительствует." },
  ]}
/>

<Inset kind="why" lang="ru">
Зачем вообще shim — почему не дать containerd быть родителем? Развязка. Будь containerd прямым родителем, рестарт или апгрейд containerd осиротил бы или убил каждый работающий контейнер, и нельзя было бы пропатчить супервизор без простоя. Shim — крошечный, долгоживущий процесс, владеющий stdio и статусом выхода ровно одного контейнера, так что containerd (и dockerd) может упасть, рестартовать или обновиться, пока твои контейнеры работают, а их коды выхода всё ещё захватываются при завершении. Это тот же инстинкт разделения ответственности, что и сама цепочка цикла: каждый слой может отказать или быть заменён без снятия остальных.
</Inset>

## Пауза, рестарт и состояния

Между created и exited контейнер проходит состояния, которые рантайм отслеживает: **created** (namespaces и cgroups настроены, entrypoint ещё не работает), **running**, **paused**, **stopped/exited**. Пауза — свой механизм: **freezer** cgroup (`cgroup.freeze`) приостанавливает каждый процесс в cgroup *без* отправки сигнала, так что процесс не может среагировать и никакой обработчик SIGSTOP не запускается; он заморожен посреди инструкции и оттаивает ровно там, где был. Политики рестарта (`--restart=on-failure`, `unless-stopped`, `always`) — это containerd, следящий за кодом выхода и перезапускающий ту же цепочку настройки. Ключевая модель: остановленный контейнер всё ещё существует как запись (его writable-слой, его конфиг), пока не удалён — `docker ps -a` его показывает — поэтому логи и код выхода завершённого контейнера остаются инспектируемыми, и поэтому рестарт пере-exec-ает entrypoint, а не возобновляет замороженный процесс.

<Quiz
  id="04-the-container-lifecycle-quiz-2"
  lessonSlug="04-the-container-lifecycle"
  lang="ru"
  question="Entrypoint контейнера — shell-обёртка (sh -c 'без exec'), не пробрасывающая сигналы. При docker stop что происходит в grace-период и почему?"
  choices={[
    { label: "SIGTERM доходит до shell на PID 1, который его не обрабатывает и не пробрасывает, так что реальный сервер работает, пока grace-таймер не истечёт и рантайм не пошлёт неперехватываемый SIGKILL", correct: true },
    { label: "SIGTERM рассылается каждому процессу в контейнере, так что сервер выключается немедленно", misconception: "Стоп шлёт SIGTERM только в PID 1, а не рассылку. Если PID 1 его не пробрасывает, дочерние процессы его не получают — именно поэтому не-пробрасывающий shell на PID 1 вызывает задержку на весь grace-период." },
    { label: "Ядро применяет дефолтное действие SIGTERM к PID 1, убивая shell сразу", misconception: "PID 1 особенный: ядро НЕ применяет к нему дефолтные диспозиции сигналов, так что необработанный SIGTERM отбрасывается, а не завершает процесс. Именно поэтому shell выживает и сервер продолжает работать." },
    { label: "docker stop шлёт SIGKILL первым, так что grace-периода вообще нет", misconception: "Последовательность: SIGTERM, затем grace-период, затем SIGKILL — SIGKILL это эскалация после дедлайна, а не первый сигнал. Вся задержка существует, потому что SIGTERM пришёл первым и был проигнорирован." },
  ]}
/>

</Explanation>

<FlowDiagram
  label="Жизнь контейнера идёт от стартовой цепочки через работу к согласованному стопу"
  hue="var(--d-infra)"
  perRow={3}
  nodes={[
    { id: "run", label: "docker run", sub: "dockerd → containerd" },
    { id: "shim", label: "shim + runc", sub: "ns, cgroups, caps" },
    { id: "exec", label: "execve(entrypoint)", sub: "становится PID 1" },
    { id: "term", label: "SIGTERM → PID 1", sub: "слить, затем выйти" },
    { id: "grace", label: "Grace-период", sub: "дефолт 10 с" },
    { id: "kill", label: "SIGKILL", sub: "неперехватываемый, код 137" },
  ]}
  edges={[
    { from: "run", to: "shim", label: "create" },
    { from: "shim", to: "exec", label: "runc start, затем выходит" },
    { from: "exec", to: "term", label: "docker stop" },
    { from: "term", to: "grace", label: "ещё жив" },
    { from: "grace", to: "kill", label: "дедлайн прошёл" },
  ]}
  caption="runc — инструмент настройки, выходящий после exec; shim остаётся родителем. Стоп — это SIGTERM в PID 1, grace-период, затем неперехватываемый SIGKILL — сигналы доходят лишь до того, кто сидит на PID 1."
/>

<KeyTakeaway>Жизненный цикл контейнера — цепочка: dockerd → containerd → по-контейнерный shim → runc, где runc разворачивает namespaces, пишет лимиты cgroup, ставит caps и seccomp, делает pivot_root, затем execve() твоего бинаря и выходит — shim остаётся стойким родителем. Остановка — переговоры: SIGTERM в PID 1, grace-период (Docker 10 с, Kubernetes 30 с), затем неперехватываемый SIGKILL. Поскольку сигналы доходят лишь до PID 1, не-пробрасывающий shell-entrypoint глотает SIGTERM и сжигает весь grace-период; exec-форма или init-шим это чинят.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="04-the-container-lifecycle-retrieval"
  lang="ru"
  questions={[
    { q: "Пройди стартовую цепочку от docker run до работающего PID 1, называя работу каждого слоя и что runc делает и чем становится.", a: "docker run бьёт в dockerd (API/демон, управление образами — не родитель). dockerd зовёт containerd, супервизор, тянущий образы и управляющий циклом. containerd запускает containerd-shim, по одному на контейнер, который становится стойким родителем процесса контейнера и держит его stdio и код выхода, чтобы containerd/dockerd могли рестартовать без убийства контейнеров. Shim вызывает runc, OCI-рантайм, который делает настройку ядра: unshare/clone namespaces, пишет лимиты cgroup, ставит capabilities и seccomp-фильтр, делает pivot_root в образ, затем execve() entrypoint, чтобы твой бинарь заменил процесс runc и стал PID 1 в своём namespace. После runc выходит — это инструмент настройки, а не супервизор — оставляя твой процесс работать прямо на хостовом ядре, с родителем-shim." },
    { q: "Опиши стоп-последовательность и объясни, почему shell-entrypoint может заставить контейнер умирать весь grace-период.", a: "docker stop (и завершение Kubernetes) шлёт SIGTERM в PID 1, ждёт grace-период (дефолт Docker 10 с, Kubernetes terminationGracePeriodSeconds дефолт 30 с), затем шлёт SIGKILL, который нельзя поймать или проигнорировать. Контракт в том, что PID 1 обрабатывает SIGTERM, перестаёт брать работу, сливает и выходит до дедлайна. Сигналы доставляются в PID 1 и только в PID 1. Если entrypoint — shell (sh -c '...'), не пробрасывающий сигналы, SIGTERM бьёт в shell, у которого нет обработчика — и поскольку ядро не применяет к PID 1 дефолтной диспозиции, сигнал просто отбрасывается. Реальный сервер, дитя, его не получает, продолжает работать и убивается лишь когда grace-таймер истекает и рантайм эскалирует до SIGKILL — так что контейнер висит весь grace-период. Фикс — exec-форма (ENTRYPOINT [\"app\"]), чтобы сервер был PID 1, или init-шим вроде --init/tini, пробрасывающий сигналы." },
  ]}
/>

<Recap lang="ru">
Жизнь контейнера — цепочка сотрудничающих процессов, а не монолит. docker run достигает dockerd, API-демона, что управляет образами, но намеренно не родительствует контейнерам; dockerd зовёт containerd, супервизор, тянущий образы и ведущий цикл; containerd стартует containerd-shim на каждый контейнер, и этот shim — стойкий родитель, держащий stdio и код выхода контейнера, чтобы containerd и dockerd могли рестартовать или обновляться без убийства работающих контейнеров. Shim вызывает runc, OCI-рантайм, выполняющий реальную работу ядра: unshare namespaces, запись лимитов cgroup, установка capabilities и seccomp-фильтра, pivot_root в образ и, наконец, execve() entrypoint, чтобы твой бинарь заменил runc и стал PID 1. После runc выходит — это инструмент настройки, поэтому гонять тысячи контейнеров дёшево: крошечный shim и твой процесс прямо на общем ядре, без по-контейнерного демона. Остановка — переговоры с дедлайном: SIGTERM идёт в PID 1, рантайм ждёт grace-период (10 с в Docker, 30 с в Kubernetes), затем шлёт неперехватываемый SIGKILL. Поскольку сигналы доходят лишь до PID 1, shell или обёртка, не пробрасывающая их, глотает SIGTERM и сжигает весь grace-период мёртвой латентностью перед убийством — лечится exec-формой или init-шимом, сидящим сигнало-осведомлённо на PID 1. Пауза — это freezer cgroup, приостанавливающий cgroup без всякого сигнала; политики рестарта перезапускают ту же цепочку настройки; а остановленный контейнер сохраняется как инспектируемая запись, с логами и кодом выхода, пока не удалён.
</Recap>
