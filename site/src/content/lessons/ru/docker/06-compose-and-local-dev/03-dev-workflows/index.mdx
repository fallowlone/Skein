---
concepts:
- docker-compose-dev
deepensInto: []
estMin: 17
lang: ru
lessonType: topic
level: senior
order: 3
prereqs: []
slug: "03-dev-workflows"
sources:
- https://docs.docker.com/compose/how-tos/file-watch/
- https://docs.docker.com/compose/how-tos/multiple-compose-files/merge/
- https://docs.docker.com/compose/how-tos/profiles/
spiral: []
status: ready
summary: 'Продуктивный dev-цикл Compose слоит базовый файл с override для живой перезагрузки (develop.watch sync/rebuild), прячет опциональные сервисы за profiles и относится к compose-файлу как к контракту, удерживающему dev от дрейфа от prod. У bind mount и watch своя цена.'
title: 'Dev-цикл: override, watch/sync, profiles и недрейфование от prod'
track: "docker"
unit: "06-compose-and-local-dev"
---

import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
Баг существовал только в проде, и существовал из-за доброты в dev-настройке. Чтобы ускорить внутренний цикл, команда bind-смонтировала каталог исходников хоста прямо поверх каталога приложения в контейнере — правишь файл, изменение мгновенно появляется внутри контейнера, без пересборки. Это прекрасно работало месяцами. Потом в `package.json` добавили зависимость, установили локально, и приложение нормально работало на ноутбуке каждого разработчика. CI собрал образ чисто. Прод упал на старте с ошибкой missing-module. Причина — bind mount: он затенил `node_modules` контейнера — собранные во время `docker build` с правильной архитектурой и полным деревом зависимостей — `node_modules` хоста, в которых случайно уже был новый пакет, потому что кто-то запустил `npm install` на хосте. Dev не запускал образ, который собрал CI; он запускал файлы хоста, наброшенные на оболочку контейнера. Install-шаг Dockerfile был мёртвым кодом месяцами, никогда не выполнялся, тихо гнил, пока прод не запустил реальный образ и не обнаружил, что install неверен. Фикс сохранил быстрый цикл, но перестал об этом врать: именованный том для `node_modules`, чтобы install образа пережил bind mount, а позже переход на `watch` Compose, чтобы контейнер запускал собранное, а синки были явными.
</Hook>

<Crux>Быстрый dev-цикл без дрейфа от prod: файл override, watch/sync для перезагрузки, profiles для опций, без bind mount поверх образа.</Crux>

<Explanation>

## Override-файлы: одна база, много окружений

Compose автоматически сливает два файла при их наличии: `compose.yaml` (базовое, общее, prod-образное определение) и `compose.override.yaml` (только локальные правки), с override, **глубоко слитым** поверх базы. Это механизм, позволяющий одному каноническому файлу описывать топологию, пока локальные заботы — debug-порт, bind mount, ослабленный уровень логов — живут в отдельном файле, который можно даже gitignore-нуть. У правил слияния есть senior-тонкость: **отображения сливаются, но большинство последовательностей замещают**. Две карты `environment` объединяются ключ-за-ключом; но `command:` или `entrypoint:` в override **замещает** базу целиком, а поля-списки вроде `ports:` конкатенируются именно для портов, тогда как многие другие списки замещают. Безопасная ментальная модель: скаляры и ключи override побеждают, карты глубоко сливаются, а всё списочное считай замещающим, пока не проверил. Можно быть явным через `-f base.yaml -f dev.yaml`, контролируя, какие файлы сливаются и в каком порядке — порядок важен, поздние файлы побеждают.

```yaml
# compose.yaml — база, prod-образная, без монтирования исходников и debug-портов
services:
  api:
    build: .
    environment:
      LOG_LEVEL: info
    ports:
      - "8080:8080"

# compose.override.yaml — только локальный dev, глубоко слит поверх
services:
  api:
    environment:
      LOG_LEVEL: debug      # сливается в карту environment базы
    ports:
      - "9229:9229"          # порт дебаггера, конкатенируется с 8080
    develop:
      watch:
        - action: sync       # копировать изменённые файлы в работающий контейнер
          path: ./src
          target: /app/src
        - action: rebuild    # изменения здесь форсируют пересборку образа
          path: ./package.json
```

## watch/sync против bind mount: живая перезагрузка без лжи

Старый быстрый цикл был **bind mount** исходников хоста поверх пути контейнера. Это мгновенно, но это ровно ловушка из Hook: каталог хоста *замещает* каталог контейнера, поэтому всё, что образ собрал в этот путь — `node_modules`, скомпилированные ассеты, venv — спрятано за файлами хоста, и dev тихо перестаёт запускать то, что содержит образ. **`develop.watch`** Compose — современная альтернатива, и она инвертирует модель: вместо монтирования хоста поверх контейнера она **следит** за путями хоста и реагирует на изменения. Действие `sync` копирует изменённые файлы *в* работающий контейнер (обычно меньше секунды, ~100–300ms для маленького файла) без затенения вывода сборки образа, так что hot-reloader внутри контейнера их подхватывает — быстрый цикл, но контейнер всё ещё запускает свои собранные зависимости. Действие `rebuild` для изменений, которые нельзя горячо пропатчить: правишь `package.json` или Dockerfile, и watch пересобирает образ и пересоздаёт контейнер (секунды-десятки секунд в зависимости от кеша). Вариант `sync+restart` копирует, затем перезапускает процесс для кода, которому нужен чистый рестарт, а не hot reload. Дисциплина, которую это даёт: dev запускает реальный образ с явным, видимым синком исходников — install-шаг выполняется при каждом изменении зависимостей, а не гниёт.

<Quiz
  id="03-dev-workflows-quiz-1"
  lessonSlug="03-dev-workflows"
  lang="ru"
  question="Команда bind-монтирует хостовый ./ поверх /app для мгновенных правок. После добавления зависимости приложение работает локально, но падает в проде с missing module. Что сделал bind mount?"
  choices={[
    { label: "Он затенил /app/node_modules образа каталогом хоста, поэтому dev никогда не запускал install-шаг образа — install был сломан, но непроверен, пока прод не запустил реальный образ", correct: true },
    { label: "Bind mount только для чтения, поэтому новую зависимость нельзя было записать, и она потерялась", misconception: "Bind mount по умолчанию на чтение-запись; записываемость не проблема. Проблема — затенение: каталог хоста замещает путь контейнера, пряча то, что образ туда собрал, поэтому install зависимостей образа никогда не является тем, что dev реально запускает." },
    { label: "Bind mount сменил архитектуру контейнера, поэтому модули собраны для не той платформы", misconception: "Bind mount ничего не копирует и не меняет архитектуру — он накладывает файлы хоста на путь контейнера. Дрейф в том, что node_modules образа спрятаны за хостовыми, поэтому корректность сборки образа никогда не проверяется в dev." },
    { label: "Ничего — это баг только прода, не связанный с dev-настройкой", misconception: "Это именно баг dev-настройки: bind mount заставил dev запускать файлы хоста поверх оболочки контейнера, а не собранный образ, поэтому install-шаг образа был мёртвым кодом, по-настоящему выполнившимся только в проде, где провалился." },
  ]}
/>

<Inset kind="why" lang="ru">
Почему watch лучше bind mount, хотя оба дают быстрый цикл? Потому что они различаются тем, что dev реально выполняет. Bind mount заставляет dev запускать *файлы хоста поверх контейнера*, поэтому собственный вывод сборки контейнера — его установленные зависимости, скомпилированные артефакты — спрятан, и сборка образа становится непроверенной, пока что-то ниже по потоку не запустит реальный образ. Watch заставляет dev запускать *собранный образ* и копирует твои изменения исходников в него; install зависимостей всё ещё происходит в образе и выполняется при каждом `rebuild`. Bind mount оптимизирует цикл, убирая образ из цикла; watch оптимизирует цикл, сохраняя образ в нём. Первый быстрее в настройке и тихо создаёт дрейф dev/prod; второй стоит немного конфига и держит dev честным. Падение missing-module из Hook — это счёт за первый вариант, оплаченный в проде.
</Inset>

## Profiles: опциональные сервисы выключены по умолчанию

Большой стек часто несёт сервисы, нужные не всем каждый день — стек метрик, mail-catcher, засеянная тестовая фикстура, admin-UI. Помещение их в базовый файл значит, что каждый платит их стоимость старта и след по ресурсам на каждом `up`. **Profiles** прячут сервис так, что он **стартует только когда его профиль явно активирован**: сервис с тегом `profiles: ["debug"]` не стартует на обычном `compose up`; он стартует только с `--profile debug` или когда назван напрямую. Сервисы без ключа `profiles` стартуют всегда — это твой ядровый набор. Senior-подвох — взаимодействие зависимостей: если всегда-включённый сервис `depends_on` профилированный, профилированный подтягивается неявно для удовлетворения зависимости, что может удивить запуском того, что ты считал выключенным. Profiles держат дефолтный `up` лёгким — поднять три реально нужных сервиса за пару секунд — сохраняя опциональные восемь в одном флаге, всё в одном файле. Альтернатива, отдельные compose-файлы под сценарий, фрагментирует топологию и переоткрывает дрейф; profiles держат один файл единственным источником истины.

<Quiz
  id="03-dev-workflows-quiz-2"
  lessonSlug="03-dev-workflows"
  lang="ru"
  question="Сервис метрик помечен profiles: [observability], чтобы быть выключенным по умолчанию. Разработчик делает обычный compose up и удивлён, что тот всё равно стартует. Какова наиболее вероятная причина?"
  choices={[
    { label: "Всегда-включённый сервис (без ключа profiles) имеет depends_on на сервис метрик, поэтому он подтягивается неявно для удовлетворения зависимости", correct: true },
    { label: "Profiles работают только с compose down, не с compose up", misconception: "Profiles гейтят старт на up: сервис с профилем не стартует на обычном compose up, пока его профиль не активирован. Механизм в порядке — причина в зависимости от всегда-включённого сервиса, подтягивающего профилированный." },
    { label: "Тег одного профиля игнорируется; сервису нужно минимум два профиля, чтобы быть выключенным", misconception: "Одного профиля достаточно, чтобы выключить сервис. Правила двух профилей нет. Сервис стартовал, потому что что-то всегда-включённое depends_on его, а не потому что профиль был неверен." },
    { label: "compose up всегда стартует каждый определённый сервис независимо от profiles", misconception: "compose up стартует сервисы без профилей плюс те, чей профиль активирован; он не стартует каждый определённый сервис. Здесь профилированный сервис стартовал лишь потому, что всегда-включённый сервис объявил его зависимостью." },
  ]}
/>

</Explanation>

<FlowDiagram
  label="Один базовый файл, override для dev, watch синкающий исходники в собранный образ, profiles прячущие опциональные сервисы"
  hue="var(--d-backend)"
  perRow={3}
  nodes={[
    { id: "base", label: "compose.yaml", sub: "prod-образная база" },
    { id: "override", label: "override.yaml", sub: "dev-правки" },
    { id: "merged", label: "слитый конфиг", sub: "глубокое слияние" },
    { id: "watch", label: "watch: sync", sub: "~100-300ms" },
    { id: "rebuild", label: "watch: rebuild", sub: "смена deps" },
    { id: "profile", label: "profiles", sub: "выкл по умолчанию" },
  ]}
  edges={[
    { from: "base", to: "merged", label: "+" },
    { from: "override", to: "merged", label: "глуб. слияние" },
    { from: "merged", to: "watch", label: "смена src" },
    { from: "merged", to: "rebuild", label: "смена deps" },
    { from: "merged", to: "profile", label: "--profile" },
  ]}
  caption="watch запускает собранный образ и синкает исходники внутрь (без затенения); rebuild срабатывает на смене зависимостей/Dockerfile. Profiles держат дефолтный up лёгким; всегда-включённый depends_on может неявно подтянуть профилированный сервис."
/>

<KeyTakeaway>Продуктивный dev-цикл остаётся быстрым без дрейфа от prod. Слои prod-образную базу с глубоко слитым override (карты сливаются, списки обычно замещают — проверяй ports). Предпочти watch Compose bind mount-у: sync копирует исходники в собранный образ за ~100-300ms без затенения его node_modules, а rebuild срабатывает при смене зависимостей или Dockerfile, так что install-шаг выполняется, а не гниёт. Прячь опциональные сервисы за profiles, чтобы дефолтный up был лёгким — но помни, что всегда-включённый depends_on может подтянуть профилированный сервис. Один файл остаётся единственным источником истины.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="03-dev-workflows-retrieval"
  lang="ru"
  questions={[
    { q: "Почему bind mount исходников хоста поверх пути контейнера вызывает дрейф dev/prod и как watch Compose этого избегает?", a: "Bind mount накладывает каталог хоста на путь контейнера, поэтому замещает всё, что образ туда собрал — node_modules, скомпилированные ассеты, venv — файлами хоста. Dev тогда запускает файлы хоста поверх оболочки контейнера, а не собранный образ, поэтому install зависимостей образа никогда не выполняется: он может быть сломан месяцами и провалиться только когда что-то ниже по потоку (CI, prod) запустит реальный образ. Watch Compose инвертирует это: он запускает собранный образ и следит за путями хоста, с действием sync, копирующим изменённые файлы в работающий контейнер (~100-300ms) без затенения вывода сборки, и действием rebuild, пересобирающим образ при смене package.json или Dockerfile. Так быстрый цикл сохранён, но контейнер всё ещё запускает свои установленные зависимости, и install-шаг выполняется при каждой смене зависимостей, а не гниёт в мёртвый код." },
    { q: "Объясни слияние override и profiles в Compose, включая подвох каждого.", a: "Compose авто-сливает compose.yaml с compose.override.yaml (или явными -f файлами по порядку, поздний побеждает), глубоко сливая override поверх: отображения сливаются ключ-за-ключом (две карты environment объединяются), но списочные поля в основном замещают — command и entrypoint замещают целиком, ports конкатенируются. Безопасное правило: карты глубоко сливаются, списки считай замещающими, пока не проверил. Profiles гейтят сервис так, что он стартует только когда его профиль активирован (--profile name) или он назван напрямую; сервисы без ключа profiles стартуют всегда. Подвох profiles: если всегда-включённый сервис depends_on профилированный, профилированный подтягивается неявно для удовлетворения зависимости, поэтому он может стартовать даже на обычном up, который, как ты ожидал, оставит его выключенным. Обе фичи держат один файл единственным источником истины вместо фрагментации топологии по многим файлам." },
  ]}
/>

<Recap lang="ru">
Хороший локальный цикл оптимизирует скорость без тихого создания дрейфа dev/prod. Начни с одного prod-образного базового файла и наслои глубоко слитый compose.override.yaml для локальных забот — debug-порты, ослабленное логирование, конфиг watch — помня асимметрию слияния: карты объединяются ключ-за-ключом, тогда как списочные поля вроде command и entrypoint замещают, а ports конкатенируются, поэтому считай список замещающим, пока не проверил. Для живой перезагрузки предпочти develop.watch Compose bind mount-у: bind mount накладывает файлы хоста на контейнер и затеняет то, что образ туда собрал, поэтому dev запускает файлы хоста поверх оболочки, а install-шаг образа гниёт непроверенным, пока прод не запустит реальный образ и не упадёт на missing module — счёт из Hook. Watch вместо этого запускает собранный образ и реагирует на изменения хоста: sync копирует исходники за ~100-300ms без затенения, rebuild срабатывает при смене package.json или Dockerfile, а sync+restart перезапускает процесс, когда hot reload недостаточно — так быстрый цикл сохраняет образ, и install выполняется при каждой смене зависимостей. Наконец, profiles прячут опциональные сервисы от дефолтного up, чтобы старт был лёгким, с одним подвохом: всегда-включённый depends_on может неявно подтянуть профилированный сервис. Через все три красная нить в том, что compose-файл остаётся единственным источником истины, а dev-удобства наслаиваются поверх, а не форкают топологию.
</Recap>
