---
concepts:
- docker-image-slimming
deepensInto: []
estMin: 16
lang: ru
lessonType: topic
level: senior
order: 3
prereqs: []
slug: "03-image-slimming"
sources:
- https://docs.docker.com/build/building/multi-stage/
- https://github.com/GoogleContainerTools/distroless
- https://docs.docker.com/build/cache/
spiral: []
status: ready
summary: 'Образ 1.2 ГБ замедляет каждый pull, деплой и scale-up и тащит shell как attack surface. Multi-stage сборка плюс distroless или scratch финал режут его до ~25 МБ, ужимают число CVE и упорядочивают слои так, что правка кода пересобирается за секунды.'
title: 'Утоньшение образа: multi-stage, distroless и слои, которые тянутся за секунды'
track: "docker"
unit: "10-production-patterns"
---

import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
Автоскейлер был узким местом, которое никто не измерял. Трафик скакнул, оркестратор запланировал новые поды, и они сидели в ContainerCreating 90 секунд — таща образ 1.2 ГБ на холодные узлы — пока очередь копилась, а существующие поды падали под нагрузкой, которую масштабирование должно было разгрузить. Образ был Go-сервисом: единственный статический бинарник 15 МБ, погребённый под полной базой Ubuntu, всем тулчейном Go, кэшами сборки, apt-списками и компилятором. Переписали в multi-stage Dockerfile — собрать в тулчейн-стадии, скопировать *только* бинарник в distroless-базу — и образ ушёл с 1.2 ГБ до 23 МБ. Холодные pull'ы упали с 90 секунд до менее 2. Бонусом security-скан, что раньше флагал 180 CVE в OS-пакетах, флагнул ноль, ведь OS-пакетов не осталось: ни shell, ни менеджера пакетов, ни утилит libc для пивота атакующего. Жирный образ был тремя проблемами в одном пальто — медленные деплои, медленное масштабирование и широкая attack surface.
</Hook>

<Crux>Жирный образ — налог на каждый pull, деплой, scale-up и широкая attack surface; multi-stage в distroless режет 1.2 ГБ до ~25 МБ.</Crux>

<Explanation>

## Multi-stage: собирай тяжело, отгружай легко

Ключевая техника — multi-stage сборка: одна стадия держит тяжёлый тулчейн, финальная — только артефакт. Образом становится лишь последняя стадия; всё в стадии сборки — компиляторы, кэши, dev-зависимости — отбрасывается.

```dockerfile
# ---- стадия сборки: тяжёлая, никогда не отгружается ----
FROM golang:1.23 AS build
WORKDIR /src
COPY go.mod go.sum ./
RUN go mod download                 # кэшируется, пока go.mod/go.sum не изменятся
COPY . .
RUN CGO_ENABLED=0 go build -o /app ./cmd/server

# ---- финальная стадия: крошечная, отгружается ----
FROM gcr.io/distroless/static-debian12   # база ~2 МБ: ни shell, ни менеджера пакетов
COPY --from=build /app /app
USER nonroot:nonroot
ENTRYPOINT ["/app"]
```

Финальный образ — это distroless-база (~2 МБ) плюс бинарник — для статического Go-бинарника ~25 МБ всего против 1.2 ГБ наивной одностадийной сборки на `golang:1.23`. Этот размер — не метрика тщеславия; он возвращается на каждой операции. Pull на холодном узле падает с ~90 с до ~2 с, что напрямую сокращает время реакции автоскейлера и длительность раската. Реестр хранит и передаёт долю байт. И attack surface схлопывается: **distroless не тащит ни shell, ни менеджера пакетов, ни OS-утилит**, так что скомпрометированный процесс не может `sh`, `curl`, `apt-get` или пивотить через libc-инструменты — а CVE-сканер рапортует почти ноль находок, ведь OS-пакетов, у которых были бы уязвимости, почти нет. Для по-настоящему статического бинарника можно даже `FROM scratch` (пустая база), отгружая буквально один бинарник; distroless — прагматичная середина, ведь добавляет CA-сертификаты, `/etc/passwd` и данные таймзон, которых нет у `scratch`.

<Quiz
  id="03-image-slimming-quiz-1"
  lessonSlug="03-image-slimming"
  lang="ru"
  question="Помимо экономии хранилища реестра, какова самая прямая продовая цена отгрузки образа 1.2 ГБ вместо 25 МБ?"
  choices={[
    { label: "Каждый pull на холодный узел медленный (~90 с против ~2 с), что удлиняет реакцию автоскейлера и раскат — а жирная база тащит shell и менеджер пакетов как attack surface", correct: true },
    { label: "Контейнер потребляет больше RAM в рантайме, ведь образ больше на диске", misconception: "Размер образа на диске — не резидентная память; образ 1.2 ГБ не съедает 1.2 ГБ RAM. Рантайм-цена — латентность pull/передачи на каждом событии планирования плюс attack surface лишних пакетов, а не память." },
    { label: "Большие образы работают медленнее, ведь ядру нужно обходить больше слоёв", misconception: "Число слоёв влияет на pull и распаковку, не на скорость стабильного выполнения; работающий процесс исполняется одинаково независимо от числа слоёв образа. Реальная цена — время доставить эти слои на холодный узел." },
    { label: "Docker берёт плату за гигабайт размера образа", misconception: "Нет рантайм-платы за гигабайт; цены операционны — медленные холодные pull'ы тормозят scale-up и раскаты, больше трафика реестра и security-экспозиция полной OS с shell и менеджером пакетов." },
  ]}
/>

## Упорядочивание слоёв: кэшируй медленные шаги

Слои образа контент-адресуемы и кэшируются: слой переиспользуется лишь если его инструкция и все предшествующие слои не изменились. Упорядочь инструкции от наименее- к наиболее-часто-меняющимся, чтобы однострочная правка кода не сбивала слой установки зависимостей. Единственное правило наибольшего рычага — **копируй и устанавливай зависимости до копирования исходного кода**.

```dockerfile
# ХОРОШО — слой зависимостей переживает правки исходника
COPY package.json package-lock.json ./
RUN npm ci                # кэшируется, пока package-файлы не изменятся
COPY . .                  # правки исходника инвалидируют только отсюда вниз
RUN npm run build

# ПЛОХО — каждая правка исходника перезапускает npm ci (минуты)
COPY . .
RUN npm ci                # любая правка файла сбивает это; полная переустановка каждой сборки
```

В плохом порядке `COPY . .` идёт до `npm ci`, так что правка одной строки исходника меняет контекст сборки, инвалидирует тот COPY-слой и форсит полную переустановку зависимостей — превращая 5-секундную инкрементальную сборку в многоминутную. В хорошем порядке установка зависимостей сидит на стабильном слое, ключованном лишь lock-файлами, так что переиспользуется на каждой правке только-исходника. Ещё две дисциплины слоёв важны: `.dockerignore`, исключающий `node_modules`, `.git` и тест-фикстуры, держит мусор вне контекста сборки (меньше, быстрее, и не даёт устаревшему локальному `node_modules` протечь); и объединение связанных `RUN`-шагов с очисткой в *том же* слое (`apt-get install ... && rm -rf /var/lib/apt/lists/*` ) важно, ведь удаление файлов в *более позднем* слое не ужимает образ — байты уже закоммичены в раннем слое и лишь маскируются, не удаляются. Число слоёв тоже реальное число: каждый слой — отдельный tar, тянущийся и распаковывающийся, так что десятки крошечных `RUN`-строк замедляют pull'ы; консолидируй, но не так агрессивно, чтобы уничтожить переиспользование кэша.

<Quiz
  id="03-image-slimming-quiz-2"
  lessonSlug="03-image-slimming"
  lang="ru"
  question="Dockerfile ставит пакет в одном RUN-слое и удаляет кэш в более позднем RUN-слое, но образ не меньше. Почему?"
  choices={[
    { label: "Каждая инструкция коммитит слой; файлы, добавленные в раннем слое, остаются в tar того слоя даже когда поздний слой их удаляет — удаление лишь маскирует байты, так что очистка должна быть в том же RUN, что их создал", correct: true },
    { label: "Команда удаления тихо упала, ведь путь был неверным", misconception: "Даже успешное удаление в позднем слое не может ужать образ: слои неизменяемы и аддитивны, так что байты, закоммиченные в раннем слое, остаются в финальном образе независимо от позднего удаления. Фикс — очистка в том же слое, а не исправленный путь." },
    { label: "Docker заново скачивает пакет на каждой сборке, добавляя кэш заново", misconception: "Перескачивание — вопрос переиспользования кэша, а не почему образ остаётся большим. Размер от удалённых файлов, всё ещё живущих в tar раннего слоя; нужно удалить их в том же RUN, что их создал." },
    { label: "Финальный образ всегда включает все стадии сборки", misconception: "В multi-stage сборке отгружается лишь финальная стадия. Здесь одна стадия; проблема — неизменяемость слоёв внутри неё: байты раннего слоя переживают удаление позднего." },
  ]}
/>

</Explanation>

<FlowDiagram
  label="Multi-stage сборка гонит тяжёлую тулчейн-стадию, которая отбрасывается, копируя только артефакт в крошечный distroless финальный образ"
  hue="var(--d-backend)"
  perRow={3}
  nodes={[
    { id: "deps", label: "Сначала deps", sub: "стабильный кэш-слой" },
    { id: "build", label: "Стадия сборки", sub: "тулчейн ~1.2 ГБ" },
    { id: "artifact", label: "Артефакт", sub: "статич. бинарник ~15 МБ" },
    { id: "distroless", label: "Distroless-база", sub: "~2 МБ, нет shell" },
    { id: "final", label: "Финальный образ", sub: "~25 МБ, CVE ~ноль" },
    { id: "pull", label: "Холодный pull", sub: "~2 с против ~90 с" },
  ]}
  edges={[
    { from: "deps", to: "build", label: "кэш переживает правки" },
    { from: "build", to: "artifact", label: "компиляция" },
    { from: "artifact", to: "distroless", label: "COPY --from" },
    { from: "distroless", to: "final", label: "отбросить тулчейн" },
    { from: "final", to: "pull", label: "быстрый scale-up" },
  ]}
  caption="Отгружается лишь финальная стадия, так что тулчейн отбрасывается. Зависимости, скопированные до исходника, держат медленную установку на кэш-слое; distroless-база не тащит shell или менеджер пакетов, режа и размер, и attack surface."
/>

<KeyTakeaway>Жирный образ — три проблемы в одном: медленные холодные pull'ы, тормозящие каждый scale-up и раскат, больше трафика реестра и полная OS shell и менеджер пакетов, через которые пивотит атакующий. Multi-stage сборка компилирует в тяжёлой тулчейн-стадии и копирует только артефакт в distroless (или scratch) финальную стадию, режа 1.2 ГБ до ~25 МБ, роняя холодные pull'ы с ~90 с до ~2 с и снижая число CVE почти до нуля, ведь OS-пакетов не осталось. Упорядочь слои от наименее- к наиболее-часто-меняющимся — копируй и ставь зависимости до исходника, чтобы правка кода не сбила слой установки — и очищай в том же RUN, что создал файлы, ведь удаление в позднем слое лишь маскирует байты, уже закоммиченные в раннем слое.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="03-image-slimming-retrieval"
  lang="ru"
  questions={[
    { q: "Объясни, как multi-stage сборка с distroless финальной стадией берёт образ 1.2 ГБ до ~25 МБ, и назови три продовые цены, которые утоньшение возвращает.", a: "Multi-stage сборка объявляет стадию сборки на тяжёлой базе (напр. golang:1.23 с полным тулчейном), компилирующую артефакт, и финальную стадию на крошечной базе (distroless ~2 МБ), в которую COPY --from'ится лишь артефакт. Образом становится лишь последняя стадия, так что компилятор, кэши и dev-зависимости отбрасываются — статический Go-бинарник на distroless ~25 МБ против ~1.2 ГБ одностадийно. Три возвращаемые цены: (1) латентность деплоя/масштабирования — pull'ы на холодный узел падают с ~90 с до ~2 с, сокращая реакцию автоскейлера и раскат; (2) трафик и хранилище реестра — доля байт передаётся и хранится; (3) attack surface — distroless не тащит shell, менеджер пакетов или OS-утилиты, так что скомпрометированный процесс не может sh/curl/apt или пивотить через libc, а CVE-сканер рапортует почти ноль, ведь OS-пакетов почти нет. FROM scratch идёт дальше для полностью статического бинарника, но distroless добавляет CA-сертификаты, /etc/passwd и данные таймзон, которых нет у scratch." },
    { q: "Почему копирование исходника до установки зависимостей рушит время сборки, и почему удаление файлов в позднем слое не ужимает образ?", a: "Слои контент-адресуемы и кэшируются: слой переиспользуется лишь если его инструкция и каждый предшествующий слой не изменились. Если COPY . . (исходник) идёт до RUN npm ci, то любая однострочная правка исходника меняет контекст сборки, инвалидирует тот COPY-слой и форсит полную переустановку зависимостей на каждой сборке — минуты вместо секунд. Копирование lock-файлов и установка зависимостей сначала кладёт медленную установку на слой, ключованный лишь lock-файлами, так что она переживает все правки только-исходника. Отдельно, слои неизменяемы и аддитивны: файлы, добавленные в раннем слое, остаются в tar того слоя даже когда поздний RUN их удаляет — удаление лишь маскирует байты в финальном виде файловой системы, оно не убирает их из закоммиченного слоя. Так что очистка (напр. rm -rf /var/lib/apt/lists/*) должна быть в том же RUN, что создал файлы, чтобы реально ужать образ." },
  ]}
/>

<Recap lang="ru">
Жирный образ — налог, платимый на каждой операции, а не разовая цена хранения. Образ 1.2 ГБ тянется ~90 секунд на холодный узел, так что удлиняет реакцию автоскейлера и раскат ровно когда нужно масштабироваться, жжёт трафик реестра на каждой передаче и тащит полную OS — shell, менеджер пакетов, утилиты libc — как attack surface, через которую пивотит скомпрометированный процесс. Лекарство — multi-stage сборка: тяжёлая стадия компилирует артефакт, а финальная копирует лишь этот артефакт в крошечную базу. Поскольку образом становится лишь последняя стадия, тулчейн и кэши отбрасываются, беря статический Go-бинарник с 1.2 ГБ до ~25 МБ на distroless-базе, роняя холодные pull'ы до ~2 с и число CVE OS-пакетов почти до нуля, ведь distroless не тащит shell или менеджер пакетов — FROM scratch идёт дальше для полностью статического бинарника, а distroless — прагматичная середина, добавляющая CA-сертификаты, /etc/passwd и данные таймзон. Упорядочивание слоёв — другая половина: слои контент-адресуемы и кэшируются, переиспользуются лишь если инструкция и все предшествующие слои не изменились, так что копируй и ставь зависимости до исходника — иначе однострочная правка инвалидирует COPY-слой и форсит полную переустановку каждой сборки. .dockerignore держит мусор вроде node_modules и .git вне контекста, а очистка должна быть в том же RUN, что создал файлы, ведь слои аддитивны: позднее удаление лишь маскирует байты, уже закоммиченные в раннем слое, оно их не убирает. Тонкие образы одновременно быстрее деплоятся, быстрее масштабируются, дешевле хранятся и меньше для атаки.
</Recap>
