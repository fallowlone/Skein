---
concepts:
- docker-resource-limits
deepensInto: []
estMin: 16
lang: ru
lessonType: topic
level: senior
order: 1
prereqs: []
slug: "01-resource-limits-and-oom"
sources:
- https://docs.docker.com/engine/containers/resource_constraints/
- https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
- https://pkg.go.dev/runtime/debug#SetMemoryLimit
spiral: []
status: ready
summary: 'Контейнер без лимита памяти может уронить весь узел по OOM, а рантайм, игнорирующий cgroup-лимит, убивает себя сам. --memory задаёт потолок cgroup, код 137 — это OOM-kill, а cgroup-aware рантаймы (GOMEMLIMIT, MaxRAMPercentage) держат кучу под ним.'
title: 'Лимиты ресурсов и OOM: потолок cgroup, код 137 и рантаймы, которые его игнорируют'
track: "docker"
unit: "10-production-patterns"
---

import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
Узел погас в 02:14. Не один контейнер — вся машина: kubelet не отвечает, SSH таймаутит, каждый под на нём флапает. Разбор нашёл единственный батч-воркер, загрузивший в память безграничную выборку. Лимита памяти у него не было, так что потолком cgroup была вся RAM узла. Проснулся OOM-killer ядра, но с хостом без памяти он начал жать по эвристическому скору, и жертвами с высшим скором оказались жирная JVM по соседству и, в итоге, сам kubelet. Один процесс без лимита уронил узел, на котором жили сорок других. Фикс — одна строка в pod spec, лимит памяти, а более глубокий урок: отсутствие лимита это не «безлимитно, но нормально», а «безлимитно, и твой радиус поражения — весь хост».
</Hook>

<Crux>Нет лимита памяти — потолок cgroup это весь узел, так что один жадный контейнер уронит всё; лимит ограничивает радиус поражения им самим.</Crux>

<Explanation>

## Потолок cgroup и код выхода 137

Контейнер — это процесс в cgroup, а `--memory` (Compose `mem_limit`, Kubernetes `resources.limits.memory`) пишет потолок `memory.max` в cgroup v2. Когда резидентная память cgroup переходит этот потолок и не может высвободиться, OOM-killer ядра убивает процесс *внутри этой cgroup* — обычно PID 1, что завершает контейнер. Контейнер записывает **код выхода 137** = `128 + 9`: процесс умер от сигнала 9, SIGKILL, посланного OOM-killer'ом. Это число — самый полезный диагностический сигнал в продовых контейнерах: 137 почти всегда означает «упёрся в потолок памяти», а не падение в твоём коде.

```bash
docker run --memory=512m --memory-swap=512m myapp
#                        ^ равно --memory отключает swap для контейнера;
#                          опусти его — и Docker даёт swap = 2× памяти, так что
#                          контейнер тихо съест 1 ГиБ до OOM и сначала зашуршит диском.
docker inspect --format '{{.State.OOMKilled}} {{.State.ExitCode}}' myapp
# true 137   ← ядро OOM-убило его на потолке cgroup
```

Локализованный случай — хороший: с лимитом умирает только *твой* контейнер, оркестратор его перезапускает, а узел остаётся здоровым. Без лимита (`memory.max = max`) cgroup может расти, пока память не кончится у *хоста*, и тогда системный OOM-killer выбирает жертвы по всем cgroup'ам по `oom_score` — а это может быть другой контейнер или критичный демон. **Отсутствующий лимит не защищает твой контейнер; он подвергает опасности соседей.** Поэтому продовая рекомендация — всегда ставить лимиты памяти и делать requests равными limits для памяти (память несжимаема — её нельзя троттлить как CPU, можно только убить).

<Quiz
  id="01-resource-limits-and-oom-quiz-1"
  lessonSlug="01-resource-limits-and-oom"
  lang="ru"
  question="Контейнер OOM-убит, его состояние показывает ExitCode 137, OOMKilled true. Что именно говорит число 137?"
  choices={[
    { label: "Процесс убит сигналом 9 (SIGKILL) — 128+9 — который OOM-killer ядра послал, когда cgroup упёрлась в потолок памяти", correct: true },
    { label: "Приложение бросило необработанное исключение и вышло со своим кодом 137", misconception: "Коды выхода выше 128 кодируют фатальный сигнал: значение это 128 + номер сигнала. 137 = 128 + 9 = SIGKILL, и вместе с OOMKilled true это OOM-killer ядра, а не код приложения, выбравший выйти." },
    { label: "Docker послал SIGTERM при выключении, а приложение не успело его обработать", misconception: "SIGTERM-затем-таймаут-SIGKILL при docker stop тоже даёт 137, но OOMKilled true — отличающий факт: этот kill пришёл от cgroup памяти, упёршейся в потолок, а не от истечения grace-периода остановки." },
    { label: "Контейнер превысил лимит CPU и был затроттлен до смерти", misconception: "Лимиты CPU троттлят (замедляют) контейнер — они никогда его не убивают, ведь CPU сжимаем. Память несжимаема, так что единственное принуждение — OOM-kill, что ровно и записывают 137 + OOMKilled." },
  ]}
/>

## Рантаймы, которые игнорируют лимит cgroup

Задать потолок cgroup — лишь половина дела: *рантайм внутри* контейнера обязан его уважать. Многие рантаймы по умолчанию размеряют кучу по памяти **хоста**, а не cgroup, потому что читают `/proc/meminfo` (тотал хоста), а не `memory.max`. JVM до контейнерной осведомлённости видела бы хост 64 ГиБ, ставила бы max heap по умолчанию ~16 ГиБ (¼ хоста) и была бы OOM-убита в тот же миг, как попыталась бы вырасти в cgroup 512 МиБ — код 137 на старте, до первого запроса. Сборщик мусора Go на дефолтах срабатывает только когда куча удваивается, так что сервис, держащий 400 МиБ живых, может раздуться за 512 МиБ между сборками и быть убит посреди запроса.

Фиксы — это рантайм-специфичные ручки, привязывающие кучу к cgroup:

```bash
# JVM: размер кучи как процент лимита cgroup (контейнерно-осведомлённая с JDK 10/8u191)
java -XX:MaxRAMPercentage=75.0 -jar app.jar     # 75% от memory.max, не от RAM хоста

# Go (1.19+): мягкая цель по памяти, которую уважает GC, чуть ниже потолка cgroup
ENV GOMEMLIMIT=450MiB        # для лимита 512 МиБ — оставь запас под не-heap память
# GC работает агрессивнее по мере приближения кучи к GOMEMLIMIT вместо ожидания удвоения

# Node: ограничь old-space V8 ниже лимита cgroup
node --max-old-space-size=400 server.js          # МиБ; дефолт V8 ~2 ГиБ независимо от cgroup
```

Паттерн одинаков по стекам: лимит cgroup — твёрдая стена, которую ядро принуждает через SIGKILL; ручка рантайма — мягкая цель, которую рантайм принуждает, собирая мусор *до* упора в стену. Поставь только лимит cgroup — рантайм радостно врастёт в стену и умрёт; поставь только ручку рантайма — утечка за неё всё равно без подстраховки ядра. Нужны обе — и мягкую цель ставят чуть *ниже* твёрдой стены, ведь не-heap память (стеки потоков, нативные буферы, metaspace) живёт в той же cgroup и тоже считается против `memory.max`.

<Quiz
  id="01-resource-limits-and-oom-quiz-2"
  lessonSlug="01-resource-limits-and-oom"
  lang="ru"
  question="Ты задал --memory=512m для Go-сервиса, но он всё равно OOM-убит под нагрузкой, сообщая лишь ~400 МиБ живой кучи. Почему и какая ручка чинит?"
  choices={[
    { label: "GC Go ждёт примерно удвоения кучи перед сборкой, так что живое плюс несобранный мусор переходят 512 МиБ между циклами — задай GOMEMLIMIT чуть ниже лимита, чтобы GC работал жёстче по мере приближения к потолку", correct: true },
    { label: "Флаг --memory не применяется к Go-бинарникам, ведь они управляют памятью сами", misconception: "--memory пишет потолок cgroup, который ядро принуждает на любом процессе независимо от языка; Go-бинарники связаны им как всё остальное. Проблема в дефолтном тайминге GC, а не в том, что ядро игнорирует лимит." },
    { label: "512m трактуется как 512 мегабит, так что реальный лимит — 64 МиБ", misconception: "Суффикс m у Docker — мебибайты, а не мегабиты; 512m это 512 МиБ. Kill реален на ~512 МиБ — разрыв это мусор, который GC ещё не собрал, что и адресует GOMEMLIMIT." },
    { label: "Нужно ещё поднять --memory-swap, чтобы дать контейнеру место под swap", misconception: "Добавление swap лишь оттягивает kill при шуршании диском; оно не мешает куче расти за потолок. Фикс — заставить GC собирать раньше через GOMEMLIMIT, ограничив живое+мусор под стеной." },
  ]}
/>

</Explanation>

<FlowDiagram
  label="Давление памяти течёт от кучи рантайма к потолку cgroup, где OOM-killer ядра решает, кто умрёт"
  hue="var(--d-backend)"
  perRow={3}
  nodes={[
    { id: "runtime", label: "Куча рантайма", sub: "JVM / Go / V8" },
    { id: "soft", label: "Мягкая цель", sub: "GOMEMLIMIT / RAMPercent" },
    { id: "ceiling", label: "cgroup memory.max", sub: "твёрдая стена, --memory" },
    { id: "oom", label: "OOM-killer", sub: "SIGKILL → код 137" },
    { id: "contained", label: "Контейнер умер", sub: "лимит есть: радиус = он сам" },
    { id: "node", label: "OOM узла", sub: "нет лимита: радиус = хост" },
  ]}
  edges={[
    { from: "runtime", to: "soft", label: "GC до стены" },
    { from: "soft", to: "ceiling", label: "держится ниже" },
    { from: "ceiling", to: "oom", label: "перейдён" },
    { from: "oom", to: "contained", label: "лимит есть" },
    { from: "oom", to: "node", label: "лимита нет" },
  ]}
  caption="Мягкая цель держит кучу ниже твёрдой стены; переход стены — это SIGKILL ядра, записывающий код 137. С лимитом kill локализован контейнером; без него cgroup растёт, пока не упадёт хост, и ядро жнёт по всем cgroup'ам."
/>

<KeyTakeaway>Контейнер без лимита памяти имеет потолком весь узел, так что одна утечка может OOM-убить соседей и демоны — всегда ставь лимит памяти, чтобы ограничить радиус поражения самим контейнером. Потолок cgroup `--memory` — твёрдая стена, которую ядро принуждает через SIGKILL, записанный как код 137 с OOMKilled true. Но рантайм обязан его уважать: JVM, Go и Node по умолчанию размеряют кучу от RAM хоста и врастут в стену и умрут, если не задать cgroup-aware мягкую цель (MaxRAMPercentage, GOMEMLIMIT, max-old-space-size) чуть ниже лимита, оставив запас под не-heap память.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="01-resource-limits-and-oom-retrieval"
  lang="ru"
  questions={[
    { q: "Контейнер показывает код выхода 137 с OOMKilled true. Объясни точно, что произошло и почему число именно 137.", a: "Cgroup памяти контейнера упёрлась в потолок memory.max (заданный --memory / limits.memory) и не смогла высвободиться, так что OOM-killer ядра послал SIGKILL (сигнал 9) процессу в cgroup — обычно PID 1, завершив контейнер. Код выхода 137, потому что соглашение для процесса, убитого сигналом — это 128 + номер сигнала, а 128 + 9 = 137. OOMKilled true подтверждает, что kill пришёл от cgroup памяти, а не от stop-grace SIGKILL (который тоже даёт 137, но без флага OOMKilled). Если бы у контейнера не было лимита памяти, потолком был бы весь узел, и OOM-killer выбрал бы жертвы по всем cgroup'ам по oom_score, возможно убив соседа или демон вместо него." },
    { q: "Почему Go- или JVM-сервис может быть OOM-убит в cgroup 512 МиБ, хотя его данные приложения помещаются, и как GOMEMLIMIT / MaxRAMPercentage это предотвращают?", a: "По умолчанию эти рантаймы размеряют кучу по памяти хоста (читаемой из /proc/meminfo), а не по memory.max cgroup. JVM до контейнерной осведомлённости на хосте 64 ГиБ ставит ~16 ГиБ дефолтный max heap и убивается в момент, когда вырастает за 512 МиБ; GC Go ждёт примерно удвоения кучи перед сборкой, так что живое плюс несобранный мусор переходят потолок между циклами. Фикс привязывает кучу к cgroup: -XX:MaxRAMPercentage=75 размеряет кучу JVM как долю memory.max, а GOMEMLIMIT задаёт мягкую цель, которую GC Go уважает, собирая агрессивнее по мере приближения. Мягкую цель ставят чуть ниже твёрдой стены, ведь не-heap память (стеки потоков, нативные буферы, metaspace) тоже считается против memory.max — так что оставь запас, иначе ядро всё равно SIGKILL'нет тебя на коде 137." },
  ]}
/>

<Recap lang="ru">
Контейнер — это процесс в cgroup, а лимит памяти пишет потолок memory.max этой cgroup. Когда резидентная память переходит его и не может высвободиться, OOM-killer ядра SIGKILL'ит процесс внутри cgroup — обычно PID 1 — и контейнер записывает код 137 (128 + сигнал 9) с OOMKilled true, самый ясный сигнал в проде, что ты упёрся в стену памяти, а не упал в коде. Задай --memory равным --memory-swap, чтобы отключить swap контейнера, иначе Docker даёт swap вдвое от лимита и контейнер шуршит диском перед смертью. Причина всегда ставить лимит — радиус поражения: с лимитом убивается и перезапускается только твой контейнер, а узел остаётся здоровым; без него потолок cgroup это весь хост, и когда хост кончается, OOM-killer жнёт по каждой cgroup по oom_score, возможно унося соседа или сам kubelet. Задать потолок ядра — лишь половина дела: рантайм внутри обязан его уважать, ведь JVM, Go и Node по умолчанию размеряют кучу от RAM хоста и врастут прямо в стену: JVM, выбравшая ~16 ГиБ на хосте 64 ГиБ, умирает на старте в 512 МиБ, а Go-сервис, держащий 400 МиБ живых, может раздуться за 512 МиБ между сборками мусора. Привяжи кучу к cgroup мягкой целью — MaxRAMPercentage для JVM, GOMEMLIMIT для Go, max-old-space-size для Node — и поставь её чуть ниже твёрдой стены, ведь не-heap память делит ту же cgroup и тоже считается против memory.max. Лимит cgroup — твёрдая подстраховка ядра; ручка рантайма — мягкая цель, собирающая до стены. Нужны обе.
</Recap>
