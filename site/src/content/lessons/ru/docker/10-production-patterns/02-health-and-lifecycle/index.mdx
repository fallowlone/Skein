---
concepts:
- docker-health-lifecycle
deepensInto: []
estMin: 16
lang: ru
lessonType: topic
level: senior
order: 2
prereqs: []
slug: "02-health-and-lifecycle"
sources:
- https://docs.docker.com/reference/dockerfile/#healthcheck
- https://kubernetes.io/docs/concepts/workloads/pods/pod-lifecycle/
- https://kubernetes.io/docs/tasks/configure-pod-container/configure-liveness-readiness-startup-probes/
spiral: []
status: ready
summary: 'Health-чек, пингующий порт, пока приложение зависло, хуже его отсутствия. Честное здоровье — это раздельные liveness, readiness и startup, а жизненный цикл SIGTERM → grace → SIGKILL с preStop-хуком — то, что заставляет раскат не терять ни одного запроса.'
title: 'Здоровье и жизненный цикл: честные пробы, окно grace при SIGTERM и preStop'
track: "docker"
unit: "10-production-patterns"
---

import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
Все дашборды были зелёными, пока checkout API лежал. Каждый под рапортовал здоров, балансировщик продолжал маршрутизировать, а клиенты получали таймауты. Health-чек был TCP-пробой на слушающий порт: процесс Go был ещё жив и сокет ещё открыт, так что проба проходила — но каждый запрос блокировался на дедлоке пула соединений к базе, что сделала failover. Приложение было трупом с пульсом. Фиксом был не больший мониторинг, а честный health-чек. Readiness-пробу сменили на эндпоинт `/readyz`, реально гонявший 200 мс запрос к пулу, и он покраснел за один интервал. Балансировщик увёл под, трафик ушёл на здоровые реплики, и алерт on-call сработал на *причину*, а не на симптом. Health-чек, который не может упасть, когда приложение зависло, — не страховка, а зелёный свет, нарисованный поверх кирпичной стены.
</Hook>

<Crux>Честная проба проверяет работу, а не порт; liveness, readiness, startup различны; а SIGTERM → grace → SIGKILL с preStop сливает чисто.</Crux>

<Explanation>

## Три пробы, три разных вопроса

Кардинальная ошибка — один health-чек, отвечающий «процесс поднят?», когда проду нужны три разных ответа. `HEALTHCHECK` Docker и пробы Kubernetes формализуют различие:

```dockerfile
HEALTHCHECK --interval=10s --timeout=2s --start-period=30s --retries=3 \
  CMD curl -f http://localhost:8080/healthz || exit 1
#   --interval: каждые 10с   --retries=3: 3 подряд провала → unhealthy
#   --start-period=30s: провалы в первые 30с не считаются (grace на медленный старт)
```

- **Liveness** отвечает «процесс завис и неисправим?» — при провале оркестратор **перезапускает** контейнер. Сделай её дешёвой и без зависимостей: зависший event loop или дедлокнутый процесс должны её провалить, но кратковременный сбой базы — *нет*, иначе ты превратишь downstream-аварию в шторм перезапусков, делающий всё хуже.
- **Readiness** отвечает «должен ли этот инстанс прямо сейчас получать трафик?» — при провале балансировщик **перестаёт маршрутизировать** на него, но не перезапускает. Здесь проверяют реальные зависимости: 200 мс запрос к пулу соединений, как в Hook. Под, потерявший базу, должен стать NotReady (слить трафик), не став non-live (перезапуск не починит аварию базы).
- **Startup** отвечает «медленный старт закончился?» — она гейтит две другие во время инициализации, чтобы JVM, греющаяся 40 с, не была liveness-убита на 10-й секунде. Эквивалент Docker — `--start-period`.

Опасный дефолт — проба, проходящая, пока приложение зависло: TCP-порт-чек или `/healthz`, возвращающий 200 из статичного обработчика, никогда не трогающего путь работы. **Health-чек честен лишь если может покраснеть по той же причине, по которой упал бы запрос пользователя.** Классический режим отказа работает и наоборот: liveness-проба, зовущая базу, превращает 30-секундную икоту базы в одновременный перезапуск каждого пода, стирая тёплые кэши и пулы ровно когда система и так под стрессом.

<Quiz
  id="02-health-and-lifecycle-quiz-1"
  lessonSlug="02-health-and-lifecycle"
  lang="ru"
  question="База сервиса делает failover на 20 секунд. Какая конфигурация проб справляется лучше?"
  choices={[
    { label: "Readiness проверяет базу и становится NotReady (трафик сливается на здоровые реплики); liveness остаётся дешёвой и без зависимостей, так что ни один под не перезапускается из-за кратковременного сбоя", correct: true },
    { label: "Liveness проверяет базу, чтобы падающие поды перезапустились и переподключились", misconception: "Liveness-проба, зависящая от базы, превращает 20-секундный downstream-сбой в синхронный перезапуск каждого пода — стирая тёплые кэши и пулы, когда система уже под стрессом. Здоровье базы — в readiness (слив), не в liveness (перезапуск)." },
    { label: "Одна TCP-порт-проба для liveness и readiness сразу", misconception: "TCP-порт-проба проходит, пока приложение зависло на мёртвой базе — ровно тихая авария из Hook. Она не может покраснеть по причине провала запроса пользователя, так что нечестна для обеих ролей." },
    { label: "Вообще без проб, чтобы сбой разрешился сам без вмешательства", misconception: "Без readiness балансировщик продолжает маршрутизировать на поды, которые не могут обслужить, так что пользователи получают таймауты все 20 с вместо ухода на реплики, достающие здоровый путь к базе." },
  ]}
/>

## Жизненный цикл выключения: SIGTERM, grace, SIGKILL, preStop

Правильно настроенные пробы — это только половина дела. Если последовательность выключения теряет запросы на выходе, дашборды снова покрасят стену в зелёный.

Раскат завершает старые поды, и то, как он это делает, решает, выживут ли запросы в полёте. Контракт — фиксированная последовательность. Решив остановить под, оркестратор (1) опционально запускает **preStop**-хук, затем (2) шлёт **SIGTERM** в PID 1, потом (3) ждёт до `terminationGracePeriodSeconds` (дефолт **30 с**) и наконец (4) шлёт **SIGKILL** всему ещё работающему. Твоя задача — полностью слиться внутри окна grace, чтобы шаг 4 никогда не сработал на живом запросе.

```yaml
lifecycle:
  preStop:
    exec: { command: ["sh", "-c", "sleep 5"] }   # перекрыть гонку де-регистрации LB
terminationGracePeriodSeconds: 45                # > худшего слива (p99 + распространение LB)
```

Тонкий баг — гонка, не имеющая отношения к твоему обработчику сигнала. В момент, когда под помечается Terminating, *одновременно* происходят две вещи: доставляется SIGTERM и эндпоинт убирается из Service. Но удаление эндпоинта распространяется асинхронно на каждый kube-proxy (агент Kubernetes, управляющий сетевыми правилами на узле) и балансировщик — секунду-две трафик ещё маршрутизируется на под, который уже начал выключаться. Если твой SIGTERM-обработчик сразу закрывает listener, эти запросы в пути бьются в закрытый сокет и получают connection-refused. Фикс — **preStop-хук**: короткий `sleep` (обычно 5–15 с), задерживающий SIGTERM, держа под полностью обслуживающим, пока распространяется де-регистрация, чтобы новый трафик не терялся. Затем приходит SIGTERM, обработчик гасит readiness (для надёжности), сливает запросы в полёте и выходит с 0 — всё с запасом внутри grace-периода, размеренного выше p99 плюс эта задержка распространения. Поставь grace слишком малым — SIGKILL рвёт живые соединения посреди ответа (клиент видит RST); забудь preStop — гонка LB теряет запросы даже при идеальном обработчике.

<Quiz
  id="02-health-and-lifecycle-quiz-2"
  lessonSlug="02-health-and-lifecycle"
  lang="ru"
  question="Твой SIGTERM-обработчик корректен — сливает запросы в полёте и выходит чисто — но пара запросов всё равно падает в начале каждого раската. Почему и что закрывает разрыв?"
  choices={[
    { label: "Удаление эндпоинта из Service распространяется асинхронно, так что LB ещё секунду-две маршрутизирует после SIGTERM; preStop sleep задерживает SIGTERM, пока де-регистрация распространится, чтобы запросы в пути попали на обслуживающий под", correct: true },
    { label: "Grace-период слишком мал, так что SIGKILL срабатывает до завершения обработчика", misconception: "Слишком малый grace вызывает SIGKILL посреди слива, но симптом здесь — провалы в НАЧАЛЕ раската на вновь приходящих запросах — это гонка де-регистрации LB, которую чинит задержка preStop, а не больший grace." },
    { label: "Обработчик должен быстрее закрывать listener, чтобы раньше отклонять лишние запросы", misconception: "Быстрее закрыть listener делает хуже: он отклоняет запросы в пути, что LB ещё шлёт. Нужно продолжать обслуживать в окне распространения, что и даёт задержка preStop." },
    { label: "Liveness падает и перезапускает под посреди выключения", misconception: "Liveness не загоняет под в перезапуск при нормальном завершении; провалы — это гонка маршрутизации между SIGTERM и асинхронным удалением эндпоинта, перекрываемая preStop." },
  ]}
/>

</Explanation>

<FlowDiagram
  label="Завершение пода течёт от preStop через SIGTERM и окно grace к SIGKILL, в гонке с асинхронным удалением эндпоинта"
  hue="var(--d-backend)"
  perRow={3}
  nodes={[
    { id: "term", label: "Под Terminating", sub: "раскат начался" },
    { id: "endpoint", label: "Эндпоинт убран", sub: "асинхронно в LB" },
    { id: "prestop", label: "preStop sleep", sub: "перекрыть гонку" },
    { id: "sigterm", label: "SIGTERM → PID 1", sub: "гасим readiness, слив" },
    { id: "grace", label: "Окно grace", sub: "дефолт 30с, слив тут" },
    { id: "sigkill", label: "SIGKILL", sub: "код 137, RST если живо" },
  ]}
  edges={[
    { from: "term", to: "endpoint", label: "одновременно" },
    { from: "term", to: "prestop", label: "хук бежит" },
    { from: "prestop", to: "sigterm", label: "после sleep" },
    { from: "sigterm", to: "grace", label: "слив" },
    { from: "grace", to: "sigkill", label: "если не успел" },
  ]}
  caption="preStop задерживает SIGTERM, чтобы асинхронное удаление эндпоинта распространилось до закрытия listener, предотвращая connection-refused на трафике в пути. Grace выше p99 плюс распространение даёт обработчику слиться полностью, так что SIGKILL не рвёт живой запрос."
/>

<KeyTakeaway>Честный health-чек может покраснеть по той же причине, по которой падает запрос пользователя — порт-пинг или статичный 200, проходящий пока приложение зависло, это зелёный свет поверх кирпичной стены. Раздели три вопроса: liveness (перезапуск если завис, держи без зависимостей, чтобы сбой базы не был штормом перезапусков), readiness (слив трафика когда зависимость лежит) и startup (не убивай медленный старт). Контракт выключения — preStop → SIGTERM → grace (дефолт 30 с) → SIGKILL: размеряй grace выше p99 плюс распространение де-регистрации LB и используй preStop sleep, чтобы перекрыть гонку, где Service ещё маршрутизирует секунду-две после SIGTERM, так что раскат сливает каждый запрос в полёте и в пути до нулевой потери.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="02-health-and-lifecycle-retrieval"
  lang="ru"
  questions={[
    { q: "Различи liveness, readiness и startup пробы по вопросу, на который отвечает каждая, и действию, которое запускает каждая — и дай режим отказа от размещения проверки базы не в той.", a: "Liveness отвечает «процесс завис и неисправим?» — провал перезапускает контейнер; держи её дешёвой и без зависимостей. Readiness отвечает «должен ли этот инстанс получать трафик сейчас?» — провал останавливает маршрутизацию балансировщика на под, не перезапуская его; здесь живут реальные проверки зависимостей (напр. запрос к пулу). Startup отвечает «медленный старт закончился?» — она гейтит две другие во время инициализации, чтобы 40-секундная прогрев JVM не была liveness-убита на 10-й секунде (--start-period Docker). Режим отказа от проверки базы в liveness: 20-секундный сбой базы перезапускает каждый под одновременно, стирая тёплые кэши и пулы, когда система уже под стрессом; та же проверка принадлежит readiness, которая лишь сливает трафик, пока зависимость не восстановится." },
    { q: "Пройди последовательность выключения пода и объясни, почему корректный SIGTERM-обработчик всё равно теряет запросы без preStop-хука.", a: "При завершении оркестратор запускает опциональный preStop-хук, шлёт SIGTERM в PID 1, ждёт до terminationGracePeriodSeconds (дефолт 30 с), затем SIGKILL'ит остаток. Подвох в том, что пометка пода Terminating запускает SIGTERM и удаление эндпоинта из Service одновременно, а удаление эндпоинта распространяется асинхронно на каждый kube-proxy и балансировщик. Так что секунду-две после SIGTERM LB ещё маршрутизирует новые запросы на под; если обработчик сразу закрывает listener, эти запросы в пути бьются в закрытый сокет и получают connection-refused. preStop sleep (5–15 с) задерживает SIGTERM, пока де-регистрация распространится, держа под полностью обслуживающим в окне. Затем приходит SIGTERM, обработчик гасит readiness и сливает запросы в полёте, выходя с 0 внутри grace выше p99 плюс распространение. Слишком малый grace даёт SIGKILL порвать живое соединение (клиент видит RST); без preStop теряются новые запросы в гонке даже при идеальном обработчике." },
  ]}
/>

<Recap lang="ru">
Продовое здоровье — это две раздельные дисциплины, которые команды рутинно схлопывают в одну. Первая — честное пробирование: health-чек обязан мочь покраснеть по той же причине, по которой упал бы реальный запрос, так что TCP-порт-чек или статичный обработчик 200, проходящий пока приложение зависло на мёртвой базе, бесполезен — он держал дашборды зелёными сквозь полную аварию. Раздели три вопроса, что реально задаёт оркестратор. Liveness спрашивает, завис ли процесс и неисправим ли, и перезапускает контейнер при провале, так что держи её дешёвой и без внешних зависимостей, иначе кратковременный сбой базы становится синхронным штормом перезапусков, стирающим тёплые кэши и пулы. Readiness спрашивает, должен ли этот инстанс брать трафик прямо сейчас, и лишь останавливает маршрутизацию балансировщика на него, так что здесь живут настоящие проверки зависимостей — реальный запрос к пулу. Startup гейтит две другие сквозь медленный старт (--start-period Docker), чтобы 40-секундный прогрев JVM не был liveness-убит преждевременно. Вторая дисциплина — жизненный цикл выключения: раскат запускает опциональный preStop-хук, шлёт SIGTERM в PID 1, ждёт до terminationGracePeriodSeconds (дефолт 30 с), затем SIGKILL'ит остаток. Размер grace выше худшего слива (p99 плюс распространение де-регистрации LB) не даёт SIGKILL порвать живое соединение и послать клиенту RST. А тонкую гонку — удаление эндпоинта распространяется асинхронно, пока доставляется SIGTERM, так что LB ещё маршрутизирует секунду-две — перекрывает короткий preStop sleep, задерживающий SIGTERM, пока де-регистрация приземлится, так что корректный обработчик сливает каждый запрос в полёте и в пути до нулевой потери. Теперь, когда ты видишь провалы запросов на границах раскатов, задай два вопроса, прежде чем добавлять ретраи: нет ли зависимости в liveness-пробе, и даёт ли preStop LB время на де-регистрацию до закрытия listener?
</Recap>
