---
concepts:
- docker-capstone-paas
deepensInto: []
estMin: 18
lang: ru
lessonType: topic
level: senior
order: 4
prereqs: []
slug: "04-capstone-mini-paas"
sources:
- https://docs.docker.com/build/building/multi-stage/
- https://distribution.github.io/distribution/
- https://kubernetes.io/docs/concepts/workloads/controllers/deployment/
spiral: []
status: ready
summary: 'Mini-PaaS связывает весь трек: реестр пинует неизменяемые digest, сборка даёт тонкие distroless-артефакты, run принуждает лимиты, честное здоровье гейтит трафик, а rolling-деплой с maxUnavailable и preStop меняет версии без потерянных запросов.'
title: 'Капстоун: mini-PaaS — реестр, сборка, run, здоровье и zero-downtime раскат'
track: "docker"
unit: "10-production-patterns"
---

import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
Откат не откатил. Инцидент только что «починили» передеплоем прошлой версии — `myapp:latest` до плохой правки — но новые поды поднялись с *сломанным* кодом всё равно, и авария тянулась ещё двадцать минут, пока все таращились на деплой со статусом успех. Причина — мутабельный тег: `latest` был перезаписан плохой сборкой, так что «передеплой latest» притянул баг. Хуже, половина выживших старых подов была ещё на прошлом `latest` из кэша, так что кластер гонял две разные версии под одним тегом и никто не мог сказать, у какого пода какой код. Фиксом стало правило, превратившее хрупкий воркфлоу в реальную платформу: каждый образ ссылается по неизменяемому **digest** (sha256-хэш содержимого образа, `myapp@sha256:...`), никогда по движущемуся тегу. Этот капстоун собирает весь трек — реестр, сборка, run, здоровье, раскат — в наименьшую систему с этим свойством и четырьмя другими, что нужны продовой платформе: неизменяемость, ограниченные ресурсы, честное здоровье и раскат, что никогда не теряет запрос.
</Hook>

<Crux>Mini-PaaS — пять контрактов вместе: пинованный по digest реестр, тонкая сборка, ограниченный run, честное здоровье и раскат без потерь.</Crux>

<Explanation>

## Пять стадий как один конвейер

Прежде чем читать пять контрактов ниже, замети, чем был инцидент из Hook на самом деле: не одной упущенной фичей, а упущенной *композицией*. Каждый контракт по отдельности выглядел приемлемым; вместе им не хватало того единственного, что делает остальные осмысленными.

Всё в этом юните — стадия одной машины. Минимальная продовая платформа — твой mini-PaaS — это композиция пяти контрактов, каждый из которых проваливает другие, если неверен.

**1 · Реестр — неизменяемость.** Реестр — источник истины для артефактов, и его единственное непреложное правило: деплои ссылаются по **неизменяемому digest**, а не по мутабельному тегу. `myapp:latest` можно перезаписать; `myapp@sha256:9f2c...` — нет: тот же digest всегда тянет ровно те же байты. Пинуй digest в деплой-манифесте — и откат по-настоящему возвращает к известно-хорошему коду, два пода никогда не разойдутся в том, что значит `latest`, а image-pull воспроизводим по узлам и времени. Теги — удобные ярлыки для людей; digest'ы — контракт для машин.

**2 · Сборка — тонкая и воспроизводимая.** Multi-stage сборка (из урока про утоньшение) компилирует в тулчейн-стадии и отгружает distroless-артефакт ~25 МБ, так что новая версия тянется на холодные узлы за ~2 с во время раската вместо его застопоривания на 90 с. Сборка пинована (база по digest, зависимости по lock-файлу), так что digest детерминирован — тот же исходник даёт тот же образ.

**3 · Run — ограниченные ресурсы.** Каждый контейнер бежит с лимитом памяти (из урока про OOM), так что один плохой релиз не может OOM-уронить узел и убрать другую версию посреди раската, плюс cgroup-aware цель рантайма (GOMEMLIMIT / MaxRAMPercentage), так что рантайм держится под стеной. Ограниченные ресурсы — то, что делает безопасным гонять старую и новую версии бок о бок во время раската.

**4 · Здоровье — честное гейтирование.** Readiness-проба, гоняющая реальный запрос (из урока про здоровье), гейтит трафик: новый под не получает запросов, пока по-настоящему не готов, а больной под сливается. Это сигнал, которого ждёт контроллер раската — без честной readiness «раскат завершён» это ложь, и трафик бьёт в под, что не может обслужить.

<Quiz
  id="04-capstone-mini-paas-quiz-1"
  lessonSlug="04-capstone-mini-paas"
  lang="ru"
  question="Откат передеплоит myapp:latest, но поды всё равно гонят сломанный код, а выжившие старые поды гонят другую сборку под тем же тегом. Какое правило платформы предотвращает этот класс багов?"
  choices={[
    { label: "Ссылаться на образы по неизменяемому digest (myapp@sha256:...) вместо мутабельного тега, так что откат возвращает к точным известно-хорошим байтам и два пода не разойдутся в том, что значит тег", correct: true },
    { label: "Тянуть образы агрессивнее с imagePullPolicy Always", misconception: "Always-перетягивание мутабельного latest лишь гарантирует, что ты притянешь то, что его перезаписало — включая плохую сборку — и ничего не делает с двумя подами в кэше на разных версиях того же тега. Фикс — неизменяемость через пин digest, а не частота pull." },
    { label: "Добавить больше реплик, чтобы сломанные были в меньшинстве", misconception: "Число реплик не меняет, на какой код резолвится тег; если latest указывает на сломанную сборку, больше реплик лишь гонят больше сломанных подов. Пин digest — то, что делает откат детерминированным." },
    { label: "Перезапустить реестр, чтобы очистить его кэш тегов", misconception: "Тег — не устаревший кэш, он был по-настоящему перезаписан на плохой образ. Перезапуск реестра не восстановит старый маппинг; лишь ссылка по неизменяемому digest даёт стабильный, воспроизводимый деплой." },
  ]}
/>

## Раскат — композиция в zero downtime

Пятый контракт — rolling-деплой, и он работает лишь потому, что держатся четыре других. Rolling-обновление заменяет поды инкрементально под двумя ручками: **maxUnavailable** (сколько существующих подов могут быть недоступны разом — ставь низко, напр. 0 или 25%, чтобы сохранить ёмкость) и **maxSurge** (сколько лишних подов можно создать сверх желаемого числа, напр. 25%, чтобы добавить ёмкость новой версии до удаления старой).

```yaml
strategy:
  type: RollingUpdate
  rollingUpdate:
    maxUnavailable: 0      # никогда не падать ниже желаемой ёмкости
    maxSurge: 1            # поднять один новый под до вывода старого
```

Последовательность связывает весь трек. Контроллер создаёт новый под из **пинованного по digest тонкого образа** (стадии 1–2); он тянется за ~2 с и стартует под своим **лимитом памяти** (стадия 3); контроллер ждёт прохождения **честной readiness-пробы** нового пода до отправки ему трафика (стадия 4); и лишь тогда завершает старый под, который гонит свой цикл **preStop → SIGTERM → слив** (из урока про здоровье), так что его запросы в полёте завершаются; и повторяет, пока каждый под не станет новой версией — с `maxUnavailable: 0` ёмкость никогда не проседает, а с честной readiness трафик никогда не приземляется на не-готовый под. Убери любой контракт — и раскат ломается: мутабельный тег делает откат ненадёжным, жирный образ застопоривает surge, отсутствующий лимит памяти даёт новой версии OOM-уронить узел с старой, нечестная проба маршрутизирует трафик на мёртвый под, а отсутствующий preStop теряет запросы в полёте выводимых подов. В этом урок всего юнита: продовые контейнеры — не пять независимых лучших практик, а один конвейер, где каждая стадия несущая для следующей.

<Quiz
  id="04-capstone-mini-paas-quiz-2"
  lessonSlug="04-capstone-mini-paas"
  lang="ru"
  question="В rolling-обновлении maxUnavailable:0, maxSurge:1 чего ждёт контроллер перед завершением старого пода и почему этот гейт важен?"
  choices={[
    { label: "Он ждёт прохождения честной readiness-пробы нового пода, так что ёмкость никогда не проседает ниже желаемой и трафик сдвигается лишь когда новая версия может по-настоящему обслужить", correct: true },
    { label: "Он ждёт фиксированный таймер независимо от состояния нового пода", misconception: "Фиксированный таймер — ровно баг, который честная readiness избегает: новый под может ещё греться, когда старый выводят, проседая ёмкость и маршрутизируя на не-готовый под. Контроллер гейтит на прохождении readiness-пробы, а не на часах." },
    { label: "Он ждёт провала liveness-пробы старого пода", misconception: "Старый под здоров; контроллер завершает его намеренно, он не ждёт провала liveness. Гейт — это становление НОВОГО пода готовым, что и сохраняет ёмкость во время surge." },
    { label: "Он завершает старый под немедленно и создаёт новый после", misconception: "Этот порядок просадил бы ёмкость ниже желаемой (maxUnavailable:0 запрещает) и рискнул бы окном аварии. maxSurge:1 значит новый под поднимается и проходит readiness первым, затем старый выводится." },
  ]}
/>

</Explanation>

<FlowDiagram
  label="Конвейер mini-PaaS течёт от пинованного по digest реестра через сборку, ограниченный по ресурсам run и честное здоровье в zero-downtime раскат"
  hue="var(--d-backend)"
  perRow={3}
  nodes={[
    { id: "registry", label: "Реестр", sub: "пин digest, неизменяемо" },
    { id: "build", label: "Сборка", sub: "multi-stage, ~25 МБ" },
    { id: "run", label: "Run", sub: "лимит памяти + GOMEMLIMIT" },
    { id: "health", label: "Здоровье", sub: "честный readiness-гейт" },
    { id: "surge", label: "maxSurge новый под", sub: "готов до вывода" },
    { id: "drain", label: "Старый под сливается", sub: "preStop → SIGTERM" },
  ]}
  edges={[
    { from: "registry", to: "build", label: "пин digest" },
    { from: "build", to: "run", label: "быстрый холодный pull" },
    { from: "run", to: "health", label: "ограничен, держится" },
    { from: "health", to: "surge", label: "readiness прошла" },
    { from: "surge", to: "drain", label: "затем вывод старого" },
  ]}
  caption="Каждая стадия несущая для следующей: неизменяемые digest делают откат реальным, тонкие образы держат surge быстрым, лимиты памяти дают старой и новой бежать бок о бок, честная readiness гейтит смену, а preStop сливает выводимый под — так что раскат теряет ноль запросов."
/>

<KeyTakeaway>Mini-PaaS — весь юнит, скомпонованный в один конвейер: реестр, ссылающийся на неизменяемые digest (не мутабельные теги), так что откат возвращает к точным известно-хорошим байтам; multi-stage тонкая сборка, что тянется за ~2 с вместо застопоривания раската; ограниченный по ресурсам run, так что один релиз не может OOM-уронить узел с другой версией; честная readiness, что гейтит трафик на по-настоящему готовые поды; и rolling-деплой с maxUnavailable:0 / maxSurge, что поднимает готовый новый под до слива старого через preStop → SIGTERM. Каждый контракт несущий — убери любой, и раскат теряет запросы, отгружает не тот код или роняет узел — в чём и суть: продовые контейнеры это один конвейер, а не пять независимых советов.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="04-capstone-mini-paas-retrieval"
  lang="ru"
  questions={[
    { q: "Почему деплой обязан ссылаться на образ по digest, а не тегу, и какие конкретные отказы пин digest предотвращает?", a: "Тег вроде latest — мутабельный указатель, его можно перезаписать на другой образ, тогда как digest (myapp@sha256:...) контент-адресуем и неизменяем: тот же digest всегда резолвится в ровно те же байты. Пин digest предотвращает три отказа. Первый — сломанный откат: если передеплоить тег, перезаписанный плохой сборкой, ты заново отгрузишь баг; digest возвращает к известно-хорошему коду детерминированно. Второй — расхождение версий: два пода могут кэшировать разные образы под тем же тегом, так что кластер гонит несовпадающий код под одним ярлыком и никто не скажет, у какого пода что; digest делает каждый под доказуемо идентичным. Третий — невоспроизводимые pull'ы: тот же манифест на разных узлах или в разное время может тянуть разные байты с движущегося тега, но digest воспроизводим по узлам и времени. Теги — людские ярлыки; digest'ы — машинный контракт." },
    { q: "Пройди zero-downtime rolling-обновление (maxUnavailable:0, maxSurge:1) и объясни, как каждый из пяти контрактов юнита несущий для него.", a: "Контроллер создаёт новый под из пинованного по digest тонкого образа (неизменяемость реестра + утоньшение сборки — он тянется за ~2 с, так что surge не застопорен), под стартует под лимитом памяти с cgroup-aware целью рантайма (ограничение run — так что новая версия не может OOM-уронить узел с старой), и контроллер ждёт прохождения честной readiness-пробы нового пода до маршрутизации трафика на него (гейтирование здоровья — так что трафик достигает лишь пода, что может по-настоящему обслужить). С maxUnavailable:0 ёмкость никогда не падает ниже желаемой, а maxSurge:1 значит новый под готов до вывода любого старого. Лишь тогда контроллер завершает старый под, который гонит preStop → SIGTERM → слив (жизненный цикл — его запросы в полёте завершаются до выхода), и повторяет, пока все поды не станут новой версией. Убери любой контракт — и оно ломается: мутабельный тег делает откат ненадёжным, жирный образ застопоривает surge, отсутствующий лимит даёт новой версии OOM-уронить узел, нечестная проба маршрутизирует на мёртвый под, а без preStop теряются запросы в полёте выводимых подов." },
  ]}
/>

<Recap lang="ru">
Этот капстоун собирает юнит в наименьшую систему, ведущую себя как продовая платформа: mini-PaaS, что есть пять контрактов, скомпонованных в один конвейер, каждый несущий для следующего. Правило реестра — неизменяемость: ссылайся на образы по digest, а не мутабельному тегу, так что откат возвращает к точным известно-хорошим байтам, два пода не разойдутся в том, что значит тег, а pull'ы воспроизводимы по узлам и времени; открывающий инцидент, где передеплой перезаписанного latest заново отгрузил баг, — ровно то, что убивает пин digest. Сборка multi-stage и тонкая, отгружая distroless-артефакт ~25 МБ, что тянется на холодные узлы за ~2 с, так что surge раската быстр, а не застопорен, и пинована, так что тот же исходник даёт тот же digest. Run ограничен по ресурсам — лимит памяти плюс cgroup-aware цель вроде GOMEMLIMIT — так что плохой релиз не может OOM-уронить узел, что хостит другую версию посреди раската, что и делает безопасным гонять две версии бок о бок. Здоровье — честная readiness, гоняющая реальный запрос, гейтящая трафик так, что новый под не берёт запросов, пока по-настоящему не обслужит, а больной под сливается; это сигнал, которого ждёт контроллер раската. И rolling-деплой связывает всё: с maxUnavailable:0 ёмкость не проседает, а maxSurge поднимает готовый новый под до вывода старого, который затем сливается через preStop → SIGTERM, так что его запросы в полёте завершаются. Потяни любую нить — и весь раскат расплетается: мутабельные теги ломают откат, жирные образы застопоривают surge, отсутствующие лимиты роняют узел, нечестные пробы маршрутизируют не туда, а без preStop теряются запросы. Тезис юнита ровно эта композиция: продовые контейнеры — ограниченные ресурсы, честное здоровье, тонкие образы и плавный жизненный цикл, работающие как одна система под оркестратором. Теперь, когда ты ревьюишь Dockerfile, манифест Kubernetes или runbook деплоя, читай его как конвейер: держит ли каждая стадия контракт, от которого зависит следующая?
</Recap>
