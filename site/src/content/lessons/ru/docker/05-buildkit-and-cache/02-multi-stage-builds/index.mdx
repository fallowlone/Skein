---
concepts:
- docker-multistage
deepensInto: []
estMin: 16
lang: ru
lessonType: topic
level: senior
order: 2
prereqs: []
slug: "02-multi-stage-builds"
sources:
- https://docs.docker.com/build/building/multi-stage/
- https://docs.docker.com/reference/dockerfile/
- https://github.com/moby/buildkit/blob/master/docs/build-repro.md
spiral: []
status: ready
summary: 'Многостадийная сборка компилирует в жирной стадии-билдере, затем тонкая runtime-стадия копирует только артефакт. BuildKit собирает лишь финальную цель и её зависимости, отбрасывает неиспользуемые стадии и шлёт образ в 28 МБ вместо 1.2 ГБ — без тулчейнов и исходников в слоях.'
title: 'Многостадийные сборки: жирный билдер, тонкий runtime и как COPY --from схлопывает 1.2 ГБ в 28 МБ'
track: "docker"
unit: "05-buildkit-and-cache"
---

import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
Команда безопасности заблокировала релиз: сканер нашёл 312 CVE в образе Go-сервиса, большинство — в `gcc`, `git`, `make` и куче `-dev`-заголовков, которых работающий бинарь не использует. Образ весил 1.2 ГБ. Разработчик был в недоумении: Go-программа — единственный статически слинкованный бинарь на 18 МБ. Откуда 1.2 ГБ? От Dockerfile, который тащил весь тулчейн сборки в прод: `FROM golang:1.23`, затем `COPY . .`, `RUN go build` — и это финальный образ: компилятор, кеш модулей, весь Debian-userland и исходники, запечённые в поставляемых слоях. Каждая из этих CVE сидела в софте, существующем лишь чтобы *произвести* бинарь, а не запускать его. Переписка заняла четыре строки: собрать в одной стадии, затем `FROM gcr.io/distroless/static`, `COPY --from=build /app /app`. Образ упал до 28 МБ, число CVE — до однозначных, а исходники перестали уезжать клиентам. В самой программе ничего не изменилось. Сборка просто перестала путать «что мне нужно, чтобы скомпилировать» с «что мне нужно, чтобы запустить».
</Hook>

<Crux>Многостадийная сборка собирает в жирной стадии и копирует только артефакт в тонкую runtime-стадию — тулчейны и исходники не шлются.</Crux>

<Explanation>

## Две задачи, две стадии

Спроси себя: нужен ли работающему контейнеру компилятор, которым он был собран? Почти никогда — но одностадийные сборки шлют обе среды в одних слоях. Одностадийная сборка смешивает две разные файловые системы: ту, что нужна чтобы *собрать* артефакт (компиляторы, пакетные менеджеры, заголовки, исходники, кеши модулей), и ту, что нужна чтобы *запустить* его (бинарь плюс его реальные runtime-зависимости). Многостадийность их разделяет. Вы пишете несколько стадий `FROM` в одном Dockerfile; последняя — образ, который вы шлёте, а ранние стадии существуют лишь чтобы произвести входы, которые финальная стадия копирует.

```dockerfile
# syntax=docker/dockerfile:1
FROM golang:1.23 AS build           # ~840 МБ: компилятор, git, кеш модулей
WORKDIR /src
COPY go.mod go.sum ./
RUN go mod download
COPY . .
RUN CGO_ENABLED=0 go build -o /app ./cmd/server

FROM gcr.io/distroless/static:nonroot AS runtime   # база ~2 МБ
COPY --from=build /app /app          # копируем ТОЛЬКО бинарь
USER nonroot
ENTRYPOINT ["/app"]
```

`COPY --from=build /app /app` — это шарнир. Он заходит в финальную ФС стадии `build` и копирует лишь `/app` в runtime-стадию. Компилятор, 400-МБ кеш модулей, дерево исходников, `git` — ничто не пересекает границу. Поставляемый образ — это distroless-база плюс один слой бинаря на 18 МБ: **28 МБ всего**, против 1.2-ГБ одностадийного. Тот же бинарь, в 40 раз меньше, и весь тулчейн сборки — каждый CVE-несущий dev-пакет — просто отсутствует в слоях, которые тянет клиент.

## Собирается только целевая стадия (и её зависимости)

Второе свойство, которое упускают: `docker build` собирает **финальную целевую стадию и только стадии, от которых она транзитивно зависит**. Неиспользуемые стадии вырезаются из графа LLB и не выполняются. Так что можно держать стадию `test` и стадию `lint` в том же Dockerfile, а обычный `docker build` (с целью runtime-стадии) пропустит их полностью, если только что-то не сделает `COPY --from` их выхода. Можно также собрать конкретную стадию через `--target build`, чтобы получить debug-образ с полным тулчейном, или `--target test` в CI, чтобы прогнать тесты внутри графа. Один Dockerfile, несколько выходов, и прод-сборка не платит ни за одну стадию, которую не ссылает.

<Quiz
  id="02-multi-stage-builds-quiz-1"
  lessonSlug="02-multi-stage-builds"
  lang="ru"
  question="В Dockerfile стадия build (компилятор + исходники, ~840 МБ) и runtime-стадия, делающая COPY --from=build /app /app на 2-МБ distroless-базу. Насколько велик поставляемый образ и куда делся компилятор?"
  choices={[
    { label: "Около 28 МБ — через COPY --from пересекает границу лишь 18-МБ бинарь поверх 2-МБ базы; компилятор остаётся в ФС стадии build, которая никогда не входит в финальный образ", correct: true },
    { label: "Около 858 МБ — финальный образ включает стадию build плюс runtime-стадию, сложенные вместе", misconception: "Стадии не складываются в финальный образ. Шлются только слои целевой стадии; COPY --from копирует выбранные файлы из ранней стадии, но её ФС не дописывается. Компилятор никогда не попадает в поставляемые слои." },
    { label: "Около 840 МБ — COPY --from копирует всю стадию build в runtime", misconception: "COPY --from копирует лишь указанный путь (здесь /app), а не всю стадию. Тулчейн на 840 МБ остаётся позади; границу пересекает только названный бинарь." },
    { label: "Зависит от того, сколько слоёв в стадии build", misconception: "Число слоёв стадии build не влияет на размер финального образа, потому что ни один из её слоёв не шлётся. Считается только то, что COPY --from втянул в runtime-стадию, плюс runtime-база." },
  ]}
/>

<Inset kind="why" lang="ru">
Почему меньший образ важен сверх цифры на диске? Три накапливающихся причины. Поверхность атаки: каждый бинарь в образе — это то, что флагует сканер и может эксплуатировать атакующий; distroless-образ без шелла, пакетного менеджера и компилятора убирает целые классы пост-эксплуатации (нельзя `curl | sh` то, где нет ни curl, ни sh). Латентность пула: образ в 28 МБ холодно тянется на свежую ноду за доли секунды, тогда как 1.2 ГБ — за десятки секунд; умножьте на событие автоскейлинга, поднимающее 50 подов, — это разница между быстро и заметно медленно. И гигиена цепочки поставок: отправка исходников и `.git` в прод утекает интеллектуальную собственность и креды, которым не место за пределами сборки. Размер — прокси; на деле вы минимизируете всё, что не является программой.
</Inset>

## Ловушка кеша при COPY из стадии

Многостадийность взаимодействует с кешированием так, что кусает людей. Вершина `COPY --from=build /app` runtime-стадии ключуется на **дайджесте `/app`**, как его произвела стадия build. Так что если компиляция в стадии build — попадание в кеш и даёт идентичный бинарь, то `COPY --from` тоже попадает, и runtime-слой переиспользуется. Но если сборка недетерминирована — встраивает таймстамп, случайный build ID, или Go-сборка варьируется — дайджест `/app` меняется каждую сборку, `COPY --from` промахивается всегда, и вы пушите новый runtime-слой в реестр каждый раз, даже когда ничего значимого не менялось. Та же контентная адресация, что делает кеш рабочим, делает невоспроизводимые сборки дорогими: воспроизводимая компиляция даёт финальному слою стабильное попадание — ровно об этом следующий урок о воспроизводимых сборках. Дисциплина: упорядочьте стадию build как любую другую (manifest до исходников) и держите артефакт детерминированным, чтобы межстадийное копирование могло кешироваться.

<Quiz
  id="02-multi-stage-builds-quiz-2"
  lessonSlug="02-multi-stage-builds"
  lang="ru"
  question="Dockerfile содержит стадии build, test и lint плюс runtime-стадию. CI запускает обычный docker build с целью runtime, который делает только COPY --from=build. Что происходит со стадиями test и lint?"
  choices={[
    { label: "Они вырезаются из графа LLB и не выполняются, потому что целевая стадия от них транзитивно не зависит; их запускают через --target test или COPY --from их выхода", correct: true },
    { label: "Они всегда собираются, потому что каждая стадия FROM в Dockerfile выполняется", misconception: "BuildKit собирает только целевую стадию и стадии, от которых она транзитивно зависит. Стадия, которую ничто не ссылает — ни как цель, ни через COPY --from, — выбрасывается из графа и пропускается полностью." },
    { label: "Они собираются, но их слои отбрасываются, так что они всё равно стоят полного времени сборки", misconception: "Это сильнее, чем отбрасывание выхода: неиспользуемые стадии вообще не планируются, поэтому стоят нулевого времени сборки. Их включают явно через --target или COPY --from." },
    { label: "Сборка падает, потому что стадии должны быть использованы", misconception: "Неиспользуемые стадии вполне легальны — держать стадию test, которую вызывают только в CI через --target, — частый паттерн. Они просто пропускаются, когда не нужны, а не ошибка." },
  ]}
/>

</Explanation>

<FlowDiagram
  label="Жирная стадия build производит артефакт; COPY --from тянет только его в тонкую runtime-стадию, а неиспользуемые стадии вырезаются"
  hue="var(--d-backend)"
  perRow={3}
  nodes={[
    { id: "src", label: "исходники + go.mod", sub: "контекст сборки" },
    { id: "build", label: "стадия build", sub: "тулчейн ~840 МБ" },
    { id: "artifact", label: "бинарь /app", sub: "18 МБ, детерминирован" },
    { id: "test", label: "стадия test", sub: "вырезается, если не нужна" },
    { id: "runtime", label: "runtime-стадия", sub: "distroless ~2 МБ" },
    { id: "image", label: "поставляемый образ", sub: "~28 МБ, мало CVE" },
  ]}
  edges={[
    { from: "src", to: "build", label: "компиляция" },
    { from: "build", to: "artifact", label: "производит" },
    { from: "src", to: "test", label: "(пропуск)" },
    { from: "artifact", to: "runtime", label: "COPY --from" },
    { from: "runtime", to: "image", label: "шлём цель" },
  ]}
  caption="Собираются только runtime-цель и её транзитивные зависимости; стадия test вырезается, пока не вызвана через --target. COPY --from копирует лишь названный путь, поэтому тулчейн, кеш модулей и исходники не входят в поставляемые слои — 1.2 ГБ схлопывается в 28 МБ."
/>

<KeyTakeaway>Многостадийная сборка разделяет файловую систему сборки (компиляторы, пакетные менеджеры, исходники, кеши модулей) и runtime-ФС (бинарь плюс реальные runtime-зависимости). Вы пишете несколько стадий FROM; COPY --from=build /app тянет только названный артефакт в тонкую финальную стадию на минимальной базе вроде distroless, так что одностадийный образ в 1.2 ГБ становится ~28 МБ и сотни CVE тулчейна исчезают, потому что этого софта просто нет в поставляемых слоях. BuildKit собирает лишь целевую стадию и её транзитивные зависимости, вырезая неиспользуемые стадии test или lint — держите их в том же файле и вызывайте через --target. Следите за ловушкой межстадийного кеша: COPY --from ключуется на дайджесте артефакта, поэтому невоспроизводимая сборка пушит свежий runtime-слой каждый раз, даже когда ничего не менялось.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="02-multi-stage-builds-retrieval"
  lang="ru"
  questions={[
    { q: "Пройди, как многостадийная сборка превращает 1.2-ГБ одностадийный Go-образ в ~28 МБ и что конкретно перестаёт шлаться.", a: "Одностадийная сборка оставляет всю среду сборки в финальном образе: golang-базу (~840 МБ с компилятором, git, заголовками), скачанный кеш модулей (могут быть сотни МБ), скопированное дерево исходников и итоговый бинарь — всё в поставляемых слоях, откуда и берутся 1.2 ГБ и 300+ CVE тулчейна. Многостадийность это расщепляет: стадия build (FROM golang:1.23 AS build) делает компиляцию, давая, скажем, /app на 18 МБ; отдельная runtime-стадия (FROM gcr.io/distroless/static, ~2 МБ) делает COPY --from=build /app /app и больше ничего. Поскольку COPY --from копирует только названный путь из ФС ранней стадии, а слои ранней стадии сами не входят в финальный образ, поставляемый образ — это лишь distroless-база плюс 18-МБ слой бинаря — ~28 МБ. Что перестаёт шлаться: компилятор и тулчейн, кеш модулей, git, -dev-заголовки и сами исходники. Число CVE падает до однозначных, потому что флагованные пакеты существовали лишь чтобы собрать бинарь, а не запускать его, и теперь отсутствуют." },
    { q: "Объясни, какие стадии BuildKit реально собирает, и ловушку межстадийного кеша COPY --from.", a: "BuildKit собирает только целевую стадию (последнюю либо названную через --target) и стадии, от которых она транзитивно зависит через COPY --from или будучи её базой. Стадии, которые ничто не ссылает, вырезаются из графа LLB и не выполняются — так что стадия test или lint в том же Dockerfile стоит нулевого времени сборки при обычной прод-сборке, и её включают через docker build --target test в CI. Ловушка кеша: вершина COPY --from=build /app runtime-стадии ключуется на дайджесте /app, как его произвела стадия build. Если компиляция детерминирована и попала в кеш, тот же дайджест бинаря значит, что COPY --from тоже попадает, и runtime-слой переиспользуется. Но если сборка встраивает таймстамп, случайный build ID или иначе невоспроизводима, дайджест /app меняется каждую сборку, COPY --from промахивается всегда, и новый runtime-слой пушится в реестр на каждой сборке, даже когда ничего значимого не менялось. Лечится тем, чтобы держать артефакт воспроизводимым, и тогда межстадийное копирование остаётся попаданием в кеш." },
  ]}
/>

<Recap lang="ru">
Одностадийная сборка шлёт одну ФС, смешивающую сборку и запуск — компилятор, пакетный менеджер, кеш модулей, исходники и артефакт все в финальном образе, — поэтому единственный статически слинкованный 18-МБ Go-бинарь приезжает образом в 1.2 ГБ, несущим 300+ CVE в софте, который работающая программа никогда не трогает. Многостадийная сборка пишет несколько стадий FROM в одном Dockerfile: жирная стадия build компилирует артефакт, а отдельная тонкая runtime-стадия на минимальной базе (distroless, ~2 МБ) делает COPY --from=build /app /app и больше ничего. COPY --from копирует лишь названный путь из ФС ранней стадии; её слои никогда не дописываются к финальному образу, так что тулчейн, кеш модулей, git, dev-заголовки и исходники не пересекают границу. Результат — ~28 МБ вместо 1.2 ГБ — тот же бинарь, в ~40 раз меньше — с числом CVE в однозначных и исходниками, больше не утекающими клиентам, а меньшая поверхность вдобавок убирает шелл и пакетный менеджер, к которым потянулся бы атакующий, и режет латентность холодного пула при автоскейлинге. BuildKit это подкрепляет, собирая лишь целевую стадию и её транзитивные зависимости: стадия test или lint в том же файле вырезается, пока вы не нацелитесь на неё явно через --target, так что прод платит лишь за то, что ссылает. Единственная ловушка — кеш: runtime-стадия COPY --from ключуется на дайджесте артефакта, поэтому невоспроизводимая сборка, чей дайджест бинаря пляшет каждый прогон, промахивается мимо этого копирования каждый раз и пушит свежий слой реестра за нулевое реальное изменение — что и есть мост к удержанию сборок воспроизводимыми. Теперь, когда видишь Dockerfile — посчитай стадии FROM: одна стадия на прод почти всегда красный флаг; спроси, что попало в поставляемые слои.
</Recap>
