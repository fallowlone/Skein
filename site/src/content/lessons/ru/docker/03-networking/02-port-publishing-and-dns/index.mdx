---
concepts:
- docker-net-ports-dns
deepensInto: []
estMin: 17
lang: ru
lessonType: topic
level: senior
order: 2
prereqs: []
slug: "02-port-publishing-and-dns"
sources:
- https://docs.docker.com/engine/network/
- https://docs.docker.com/engine/network/drivers/bridge/
- https://man7.org/linux/man-pages/man8/iptables.8.html
spiral: []
status: ready
title: "Публикация портов и встроенный DNS: DNAT, bind-адреса и 127.0.0.11"
track: "docker"
unit: "03-networking"
summary: "Публикация порта ставит правило iptables DNAT, переписывающее host:port в container:port, с областью по bind-адресу. Пользовательские сети добавляют встроенный DNS на 127.0.0.11. Большинство багов «работает локально, но не извне» и «имя не найдено» — это эти два механизма."
---

import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import SequenceDiagram from "~/components/diagram/SequenceDiagram.astro";

<Hook>
Демо работало на ноутбуке разработчика и падало в staging, и разница была в одном пропущенном двоеточии. Compose-файл публиковал API как `127.0.0.1:8080:8080`. На ноутбуке разработчик стучался в `localhost:8080`, и всё было хорошо. В staging health-check балансировщика приходил с другого хоста, получал connection-refused и помечал сервис как упавший — поэтому он никогда не получал трафик, а дежурный видел аварию при зелёных логах контейнера. `docker ps` показывал порт «опубликованным», `curl localhost:8080` на самой машине возвращал 200, и всё же порт был недостижим откуда-либо ещё. Bind-адрес `127.0.0.1` молча ограничил опубликованный порт только хостовым loopback: правило DNAT существовало, но матчило лишь пакеты, приходящие на интерфейс loopback. Поменяли на `0.0.0.0:8080:8080` (или просто `8080:8080`), и health-check позеленел. Публикация порта — это правило iptables с областью по интерфейсу, а встроенный DNS — второй механизм, который с ним путают; оба стоит понимать точно, потому что вместе они объясняют большую часть путаницы в сети контейнеров.
</Hook>

<Crux>Публикация порта — правило iptables DNAT с областью по интерфейсу; DNS на 127.0.0.11 разрешает имена. Два механизма, два класса сбоев.</Crux>

<Explanation>

## Публикация порта — это DNAT

`-p 8080:8080` не «открывает» порт так, как правило фаервола — он ставит правило **DNAT** (destination NAT) в iptables, переписывающее назначение пакетов, приходящих на порт 8080 хоста, на IP и порт контейнера. Правило оседает в цепочке `DOCKER` таблицы `nat`, достижимой из `PREROUTING` (для трафика снаружи) и `OUTPUT` (для трафика с самого хоста):

```text
-A DOCKER ! -i docker0 -p tcp --dport 8080 -j DNAT --to-destination 172.17.0.2:8080
```

Спецификация опубликованного порта состоит из трёх частей: `[bind-address:]host-port:container-port`. **Bind-адрес** — это то, что кусает: опусти его (или используй `0.0.0.0`), и порт достижим на каждом интерфейсе хоста; задай `127.0.0.1:8080:8080`, и DNAT матчит лишь трафик, приходящий на loopback — достижимый с самого хоста, невидимый любой другой машине. Это и есть баг из вступления: правило существовало, `docker ps` гордо его перечислял, но область по интерфейсу исключала health-checker. Вторая тонкость: по умолчанию правило DNAT от Docker вставлено *перед* правилами INPUT хостового фаервола, поэтому опубликованный порт может быть достижим снаружи, даже если `ufw`/`firewalld` думают, что заблокировали его — Docker и хостовый фаервол редактируют разные цепочки, и люди удивляются, кто из них побеждает.

### userland-proxy

На самом деле у трафика опубликованного порта два пути. Быстрый путь — DNAT iptables выше, целиком в ядре. Но Docker также запускает небольшой процесс `docker-proxy` в userland на каждый опубликованный порт как запасной вариант — для случаев hairpin и соединений по loopback на том же хосте, которые одно правило DNAT не покрывает. Путь через ядро стоит практически ничего; путь через userland-proxy копирует байты через процесс в пользовательском пространстве, добавляя **единицы микросекунд** задержки на пакет плюс процесс и удерживаемый сокет на каждый опубликованный порт. На хосте, публикующем сотни портов, это реальные накладные расходы, поэтому высоконагруженные конфигурации часто отключают его (`"userland-proxy": false`) и полагаются только на DNAT плюс ядровый hairpin-маршрут.

<Quiz
  id="02-port-publishing-and-dns-quiz-1"
  lessonSlug="02-port-publishing-and-dns"
  lang="ru"
  question="Контейнер публикует 127.0.0.1:5432:5432 для Postgres. С хоста psql подключается нормально. С другой машины в той же сети — connection refused. docker ps перечисляет порт как опубликованный. В чём дело?"
  choices={[
    { label: "Bind-адрес 127.0.0.1 ограничивает правило DNAT только loopback, поэтому опубликованный порт достижим с хоста, но ни с какого другого интерфейса", correct: true },
    { label: "Postgres слушает только loopback контейнера, поэтому внешние хосты не могут достучаться", misconception: "Если бы Postgres биндился лишь на loopback контейнера, даже хостовый psql через опубликованный порт упал бы; здесь хост подключается нормально, значит контейнер слушает на eth0 — ограничение в хостовом bind-адресе, ограничивающем DNAT до loopback." },
    { label: "Хостовый фаервол блокирует 5432 для внешних хостов", misconception: "DNAT от Docker вставлен перед правилами INPUT хостового фаервола, поэтому фаервол обычно не то, что блокирует опубликованный порт; и симптом точно совпадает с bind-адресом, ограниченным loopback, который docker ps всё равно показывает как 'опубликованный'." },
    { label: "Встроенный DNS не смог разрешить контейнер, поэтому внешние клиенты не могут его найти", misconception: "Встроенный DNS разрешает имена контейнеров для других контейнеров в пользовательской сети; внешние клиенты подключаются по IP и порту хоста через DNAT и вообще не касаются DNS Docker." },
  ]}
/>

<Inset kind="why" lang="ru">
Почему `127.0.0.1:port` вообще существует как опция, если вызывает такую путаницу? Потому что это верный дефолт для порта базы данных или админ-порта, достижимого лишь другим процессам на том же хосте (или reverse-proxy на нём), но никогда из сети. Грабли — использовать его рефлекторно и потом ждать удалённой достижимости. Дисциплина: бинди на loopback, когда потребитель рядом, бинди на `0.0.0.0`, когда подключаться должно что-то вне машины, и никогда не давай «опубликованному» в `docker ps` подменять «достижимый оттуда, откуда мне надо».
</Inset>

## Встроенный DNS на 127.0.0.11

Почему `http://api:8080` просто работает внутри Compose-проекта, но перестаёт работать при обращении из другого проекта? Это и есть область DNS в действии — второй класс сбоев, который важно освоить.

На *дефолтном* мосту контейнеры достигают друг друга лишь по IP — разрешения имён нет. В **пользовательской** bridge-сети (которую создаёт `docker network create` или любая сеть сервиса в Compose) Docker запускает **встроенный DNS-сервер**, который каждый контейнер достигает по фиксированному адресу **127.0.0.11:53**. Docker переписывает `/etc/resolv.conf` каждого контейнера, чтобы он указывал на него, и тот разрешает имена контейнеров и сетевые алиасы в текущие IP контейнеров, пересылая всё остальное на резолверы хоста. Поэтому в Compose-сети ты подключаешься к `http://api:8080`, и это просто работает — `api` это имя контейнера, известное встроенному DNS, и возвращаемый IP всегда актуален даже после рестарта, давшего контейнеру новый адрес.

Числа и режимы отказа, которые важны:

- Резолвер живёт на `127.0.0.11` внутри netns *каждого* контейнера — loopback-адрес, поэтому он не сталкивается с DNS хоста и недостижим извне контейнера. Правила NAT iptables внутри netns перенаправляют этот loopback-трафик на DNS-слушатель демона.
- Разрешение имён **per-network**: контейнер разрешает имена лишь контейнеров, подключённых к сети, которую он разделяет. Два контейнера в разных пользовательских сетях не могут разрешить друг друга по имени даже на одном хосте — классический баг «работает в одном compose-файле, не работает между двумя».
- Короткий TTL DNS плюс разрешение на каждом подключении делают рестарты прозрачными, но клиент, кэширующий разрешённый IP навсегда (некоторые дефолты JVM, наивные HTTP-клиенты), продолжает стучаться в *старый* IP после рестарта целевого контейнера — баг устаревшего соединения, который выглядит как сеть, но это кэш резолвера клиента.

<Quiz
  id="02-port-publishing-and-dns-quiz-2"
  lessonSlug="02-port-publishing-and-dns"
  lang="ru"
  question="Сервис A достигает сервис B по имени 'b' в одном Compose-проекте. Сервис C из второго проекта вообще не может разрешить 'b', хотя оба работают на одном хосте. Почему?"
  choices={[
    { label: "Встроенный DNS разрешает имена per-network; C в другой пользовательской сети, а резолвер возвращает лишь имена контейнеров, разделяющих сеть с вызывающим", correct: true },
    { label: "Встроенный DNS на 127.0.0.11 одноэкземплярный, и лишь один проект может регистрировать в нём имена", misconception: "У каждой сети своя область имён, обслуживаемая тем же резолвером демона; ограничение не в одном проекте на резолвер, а в том, что разрешение ограничено сетями вызывающего — подключи C к сети b, и оно разрешится." },
    { label: "C должен использовать IP B, ведь кросс-проектный DNS требует дефолтного моста, у которого нет DNS", misconception: "У дефолтного моста и правда нет DNS, но проблема C в том, что он не разделяет с b ни одной пользовательской сети — подключи оба к общей внешней сети, и разрешение имён заработает без всяких IP." },
    { label: "TTL DNS истёк между проектами, сбросив запись b", misconception: "TTL управляет свежестью кэша внутри сети, а не видимостью между сетями; запись b существует, она просто вне области разрешения C, потому что они не разделяют сеть." },
  ]}
/>

</Explanation>

<SequenceDiagram
  label="Контейнер разрешает имя соседа через встроенный DNS, затем ядро DNAT-ит подключение к опубликованному порту целевого контейнера"
  hue="var(--d-infra)"
  actors={["app", "127.0.0.11", "ядро (DNAT)", "целевой сервис"]}
  messages={[
    { from: "app", to: "127.0.0.11", label: "A? api" },
    { from: "127.0.0.11", to: "app", label: "172.18.0.3 (текущий)" },
    { from: "app", to: "ядро (DNAT)", label: "SYN host:8080" },
    { from: "ядро (DNAT)", to: "целевой сервис", label: "-> 172.18.0.3:8080" },
    { from: "целевой сервис", to: "app", label: "SYN-ACK (un-DNAT)" },
  ]}
  caption="Разрешение имён (встроенный DNS, 127.0.0.11) и достижимость (DNAT опубликованного порта) — два отдельных механизма. 'Имя не найдено' — проблема области DNS; 'connection refused снаружи' — проблема bind-адреса или DNAT. Диагностируй верный."
/>

<KeyTakeaway>Публикация порта — это правило iptables DNAT, а не открытие фаервола: оно переписывает host:port в container-IP:port и ограничено bind-адресом — `127.0.0.1:` делает его только-loopback, пока docker ps всё ещё пишет 'опубликован', и DNAT Docker вставлен перед правилами INPUT хостового фаервола. userland-proxy — запасной путь на порт, добавляющий задержку в единицы микросекунд, часто отключаемый при высоком трафике. Отдельно: пользовательские сети запускают встроенный DNS на 127.0.0.11, разрешающий имена контейнеров per-network в текущие IP; имена не разрешаются между сетями, а клиенты, кэширующие IP, ломаются на рестарте. Достижимость и разрешение — два механизма, сопоставь симптом верному.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="02-port-publishing-and-dns-retrieval"
  lang="ru"
  questions={[
    { q: "Объясни, что именно ставит '-p 127.0.0.1:8080:8080' и почему тот же контейнер достижим с хоста, но не с другой машины.", a: "Оно ставит правило iptables DNAT в nat/DOCKER (достижимое из PREROUTING и OUTPUT), переписывающее назначение пакетов, приходящих на порт 8080 хоста, на IP и порт контейнера. Необязательный префикс bind-адреса ограничивает, какой интерфейс хоста матчит правило: 127.0.0.1 значит, что DNAT срабатывает лишь для пакетов на loopback, поэтому соединение с самого хоста работает, а соединение с любой другой машины (приходящее на реальную сетевую карту) получает connection-refused, ведь нет матчащего DNAT. docker ps всё равно перечисляет порт как 'опубликованный', потому что правило существует — опубликован это не то же, что достижим-откуда-нужно. Используй 0.0.0.0 (или опусти bind-адрес), чтобы матчить каждый интерфейс. Учти: DNAT Docker сидит перед правилами INPUT хостового фаервола, поэтому опубликованный порт может быть достижим, даже когда ufw/firewalld думают, что заблокировали его." },
    { q: "Опиши встроенный DNS: адрес, область, что возвращает и два режима отказа.", a: "В пользовательской bridge-сети (у дефолтного моста DNS нет) Docker запускает встроенный DNS-сервер, достижимый по фиксированному loopback-адресу 127.0.0.11:53 внутри netns каждого контейнера; Docker переписывает /etc/resolv.conf на него, а правила NAT в netns перенаправляют этот loopback на резолвер демона. Он разрешает имена контейнеров и сетевые алиасы в текущие IP контейнеров (пересылая прочие запросы на резолверы хоста), поэтому http://api:8080 работает в Compose-сети и переживает рестарты, меняющие IP. Режим отказа один: разрешение per-network — контейнер разрешает лишь имена контейнеров, разделяющих одну из его сетей, так что два отдельных Compose-проекта не разрешат имена друг друга даже на одном хосте без общей сети. Режим отказа два: клиент, кэширующий разрешённый IP навсегда (некоторые дефолты JVM/HTTP-клиентов), продолжает стучаться в старый IP после рестарта цели — баг устаревшего кэша, маскирующийся под сетевой сбой." },
  ]}
/>

<Recap lang="ru">
Два отдельных механизма объясняют большую часть путаницы в сети контейнеров, и senior-навык — держать их порознь. Публикация порта (-p) ставит правило iptables DNAT в цепочке DOCKER таблицы nat, переписывающее назначение трафика, приходящего на порт хоста, на IP и порт контейнера; это сантехника достижимости, а не открытие фаервола. Спецификация опубликованного порта — [bind-address:]host-port:container-port, и bind-адрес ограничивает правило интерфейсом: 127.0.0.1: делает порт только-loopback — достижимым с хоста, отклонённым с любой другой машины — хотя docker ps всё ещё гордо печатает 'опубликован'. Docker вставляет этот DNAT перед правилами INPUT хостового фаервола, поэтому опубликованный порт может быть достижим снаружи, даже когда ufw или firewalld считают, что заблокировали его. Процесс docker-proxy в userland на каждый порт — запасной вариант для случаев hairpin и loopback на том же хосте, которые DNAT в одиночку упускает; он добавляет задержку в единицы микросекунд на пакет и удерживаемый сокет на порт, поэтому высоконагруженные хосты часто ставят userland-proxy false. Второй механизм — разрешение имён: пользовательская bridge-сеть (у дефолтного моста его нет) запускает встроенный DNS на фиксированном loopback 127.0.0.11:53 внутри каждого контейнера, разрешая имена контейнеров и алиасы в текущие IP и пересылая остальное вверх — поэтому http://api:8080 работает в Compose-сети и переживает рестарты, переназначающие IP. Его область per-network, так что две отдельные сети не разрешат имена друг друга без общей сети, а клиент, кэширующий IP навсегда, продолжает подключаться к мёртвому адресу после рестарта. Когда дебажишь, сначала реши, симптом ли это достижимости (bind-адрес, DNAT, фаервол) или разрешения (область DNS, устаревший кэш) — тогда чинишь верный механизм.
</Recap>
