---
concepts:
- docker-net-bridge
deepensInto: []
estMin: 16
lang: ru
lessonType: topic
level: senior
order: 1
prereqs: []
slug: "01-network-namespaces-and-bridges"
sources:
- https://docs.docker.com/engine/network/drivers/bridge/
- https://man7.org/linux/man-pages/man4/veth.4.html
- https://man7.org/linux/man-pages/man8/iptables.8.html
spiral: []
status: ready
title: "Сетевые namespace и мосты: что на самом деле подключает 'docker run'"
track: "docker"
unit: "03-networking"
summary: "Сеть контейнера — это netns Linux со своими интерфейсами и маршрутами, соединённый с хостом veth-парой на мосту docker0, а iptables MASQUERADE NAT-ит исходящий трафик. Знание четырёх примитивов превращает большинство сетевых багов в диагноз одной командой."
---

import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
Алерт дежурного был расплывчатым: один сервис не мог достучаться до другого, но только иногда. Команда час пялилась в логи приложения, счётчики ретраев и конфиг DNS, прежде чем кто-то выполнил `ip netns` на хосте и увидел реальную картину. Вся сеть контейнера — его интерфейсы, маршруты, ARP-таблица, его представление iptables — живёт в сетевом namespace Linux, объекте ядра таком же конкретном, как процесс. Внутри сбоящего контейнера `ip addr` показывал `eth0` с адресом из `172.17.0.0/16`, но в `ip route` не было маршрута по умолчанию к мосту. Кривой кастомный entrypoint при старте сбросил таблицу маршрутизации. «Перемежающийся» паттерн был лишь тем, какой контейнер рестартовал последним. Починка заняла тридцать секунд, как только перестали дебажить приложение и начали дебажить namespace — потому что всё, что Docker делает с сетью, это четыре примитива ядра, которые можно осмотреть руками: namespace, veth-пара, мост и правило NAT.
</Hook>

<Crux>Сеть контейнера — это netns Linux, подключённый к хосту veth-парой на мосту docker0, а iptables MASQUERADE NAT-ит исходящий трафик.</Crux>

<Explanation>

## Namespace — это граница

Контейнер — это в основном набор namespace, и сетевой namespace (netns) владеет сетью. У него собственный список интерфейсов, таблица маршрутизации, ARP-кэш, `/proc/net`, представление conntrack и правила iptables — полностью изолированные от хоста и от соседних контейнеров. Когда `docker run` запускает контейнер в дефолтной сети `bridge`, демон создаёт свежий netns; внутри контейнер видит ровно два интерфейса:

```text
$ docker run --rm alpine ip addr
1: lo: <LOOPBACK,UP> mtu 65536      # loopback, 127.0.0.1
2: eth0@if47: <UP> mtu 1500         # один конец veth-пары
    inet 172.17.0.2/16 ...          # адрес из подсети docker0
```

Источник истины здесь — хост, а не самоотчёт контейнера. Когда видишь `eth0@if47`, нотация уже подсказывает: `eth0` — одна половина **veth-пары** (виртуального кабеля, соединяющего netns с хостом), а `if47` — индекс её второй половины, живущей на хосте. С хоста `ip netns` и `nsenter --net=/proc/<pid>/ns/net ip addr` позволяют шагнуть *внутрь* сетевого представления контейнера вообще без шелла в нём — диагностика, завершающая большинство споров «это приложение или сеть».

## veth-пары и мост

**veth-пара** — это виртуальный Ethernet-кабель: два интерфейса, соединённые спина к спине, где кадр, записанный в один, выходит из другого. Один конец (`eth0`) сидит внутри netns контейнера; другой (с именем вида `vethXXXX`) остаётся в netns хоста и подчинён **мосту docker0**. Мост — это программный коммутатор Layer-2: хостовый veth каждого контейнера втыкается в него, поэтому контейнеры на одном мосту достигают друг друга напрямую по IP на L2, без маршрутизации и NAT.

Числа по умолчанию, которые стоит запомнить: docker0 несёт подсеть **172.17.0.0/16**, сам мост на `172.17.0.1` (шлюз по умолчанию для контейнеров), и каждый veth-интерфейс по умолчанию имеет **MTU 1500** под стандартный Ethernet. Этот MTU — классический источник сбоев: запусти Docker *внутри* overlay или VPN с эффективным MTU 1450, оставь veth на 1500, и крупные пакеты молча дропаются или фрагментируются — TLS-рукопожатия зависают ровно в точке отправки сертификата (большой кадр). Драйвер моста не определяет MTU подложки автоматически; задаёшь `com.docker.network.driver.mtu` или он тебя укусит.

<Quiz
  id="01-network-namespaces-and-bridges-quiz-1"
  lessonSlug="01-network-namespaces-and-bridges"
  lang="ru"
  question="Контейнер показывает eth0@if47 с 172.17.0.2/16, но не может достучаться ни до чего вне хоста. ip route внутри контейнера пуст. Какой единственный примитив сломан?"
  choices={[
    { label: "Таблица маршрутизации контейнера — без маршрута по умолчанию через 172.17.0.1 (docker0) у netns нет пути за пределы линка, хотя veth и адрес в порядке", correct: true },
    { label: "veth-пара разорвана, поэтому кадры не могут покинуть eth0", misconception: "Будь veth разорван, интерфейс не показывал бы eth0@if47 с живым адресом, и контейнер не достучался бы даже до 172.17.0.1; здесь L2-путь есть, и отсутствует лишь таблица маршрутизации за линк." },
    { label: "docker0 лежит, поэтому вся подсеть моста недоступна", misconception: "Упавший мост сломал бы все контейнеры разом, а не один; и симптом именно в пустой таблице маршрутов внутри одного netns — это проблема маршрутизации в контейнере, а не общехостовый отказ моста." },
    { label: "DNS неправильно настроен, поэтому разрешение имён не работает", misconception: "Сбой — недостижимость чего-либо вне хоста любым способом при пустой таблице маршрутов — это разрыв маршрутизации Layer-3, а не разрешение имён, которое лежит слоем выше и всё равно пропустило бы сырой IP-трафик." },
  ]}
/>

<Inset kind="why" lang="ru">
Почему мост плюс veth-пары, а не просто выдать каждому контейнеру хостовый интерфейс? Изоляция и плотность. Netns даёт каждому контейнеру собственный приватный L3-стек, так что порт 8080 в одном никогда не сталкивается с портом 8080 в другом, а мост позволяет сотням контейнеров делить одну хостовую сетевую карту, всё ещё общаясь друг с другом на L2 — всё программно, без лишнего железа, без IP из физической сети. Цена — лишний прыжок через мост и правило NAT на исходящем, и это ровно та механика, которую разбирает следующий раздел.
</Inset>

## MASQUERADE: как исходящий трафик покидает хост

Подсеть моста `172.17.0.0/16` приватна и не маршрутизируется в физической сети, поэтому контейнеру, говорящему с интернетом, нужно переписать адрес источника на хостовый. Docker ставит правило **iptables MASQUERADE** в цепочку `POSTROUTING` таблицы `nat`:

```text
-A POSTROUTING -s 172.17.0.0/16 ! -o docker0 -j MASQUERADE
```

Читай точно: для любого пакета из подсети моста, *уходящего* через интерфейс, отличный от docker0 (то есть в реальную сетевую карту), переписать IP источника на адрес этой карты. MASQUERADE — это SNAT, автоматически берущий текущий IP исходящего интерфейса, что верно для DHCP-хостов. Таблица **conntrack** (таблица отслеживания соединений ядра) запоминает каждый поток, чтобы ответы были раз-NAT-лены обратно нужному контейнеру. Эта таблица конечна (`net.netfilter.nf_conntrack_max`, часто **262144** на дефолтном хосте): машина с десятками тысяч короткоживущих исходящих соединений в секунду может её исчерпать, и тогда новые соединения падают с `nf_conntrack: table full, dropping packet`, хотя CPU и память выглядят простаивающими. Исходящий трафик, «случайно отказывающий под нагрузкой», — это обычно conntrack, а не приложение.

<Quiz
  id="01-network-namespaces-and-bridges-quiz-2"
  lessonSlug="01-network-namespaces-and-bridges"
  lang="ru"
  question="Два контейнера на дефолтном мосту говорят друг с другом по IP без NAT, но трафик каждого в интернет NAT-ится на IP хоста. Почему асимметрия?"
  choices={[
    { label: "Трафик контейнер-контейнер остаётся на docker0 (L2, та же подсеть), поэтому защита '! -o docker0' в MASQUERADE его исключает; переписывается только трафик, уходящий через реальную сетевую карту", correct: true },
    { label: "Docker NAT-ит весь трафик контейнеров, но трафик на том же хосте NAT-ится на 127.0.0.1, что выглядит как отсутствие NAT", misconception: "Трафик внутри моста не NAT-ится вовсе — это чистая L2-коммутация на docker0; правило MASQUERADE явно исключает пакеты, уходящие через docker0, так что нет переписи, которую можно наблюдать, а не перепись на loopback." },
    { label: "Два контейнера делят один сетевой namespace, поэтому трафик между ними никогда не попадает в сеть", misconception: "У каждого контейнера свой netns; их трафик проходит мост как реальные кадры — он просто не NAT-ится, потому что правило исходящего NAT срабатывает лишь для пакетов, выходящих через физический интерфейс хоста." },
    { label: "iptables не может NAT-ить трафик между приватными адресами", misconception: "iptables может NAT-ить что угодно; причина, по которой этот трафик не NAT-ится, — собственная защита правила по интерфейсу, а не ограничение; измени правило, и трафик той же подсети тоже мог бы переписываться." },
  ]}
/>

</Explanation>

<FlowDiagram
  label="Исходящий пакет идёт из netns контейнера через veth-пару на docker0, затем MASQUERADE-NAT-ится наружу через сетевую карту хоста"
  hue="var(--d-infra)"
  perRow={3}
  nodes={[
    { id: "app", label: "Приложение", sub: "src 172.17.0.2" },
    { id: "eth0", label: "eth0 (netns)", sub: "конец veth, MTU 1500" },
    { id: "veth", label: "vethXXXX (хост)", sub: "подчинён мосту" },
    { id: "br", label: "мост docker0", sub: "172.17.0.1, L2-свитч" },
    { id: "nat", label: "POSTROUTING", sub: "MASQUERADE + conntrack" },
    { id: "nic", label: "хостовый eth0", sub: "src переписан на IP хоста" },
  ]}
  edges={[
    { from: "app", to: "eth0", label: "send" },
    { from: "eth0", to: "veth", label: "veth-пара" },
    { from: "veth", to: "br", label: "L2" },
    { from: "br", to: "nat", label: "за линк" },
    { from: "nat", to: "nic", label: "SNAT" },
  ]}
  caption="Трафик контейнер-контейнер в той же подсети останавливается на мосту (без NAT). Только исходящий за линк попадает в POSTROUTING, где MASQUERADE переписывает источник, а conntrack записывает поток, чтобы ответы вернулись нужному контейнеру."
/>

<KeyTakeaway>Сеть Docker — это четыре осматриваемых примитива ядра, а не магия. Каждый контейнер получает сетевой namespace со своим стеком; veth-пара мостит этот namespace к хосту, хостовый конец подчинён мосту docker0 (172.17.0.0/16, шлюз .1, MTU 1500). Контейнеры на одном мосту говорят на L2 без NAT; исходящий наружу SNAT-ится правилом iptables MASQUERADE, а conntrack отслеживает потоки для обратного трафика. Когда сеть ломается, определи, какой из четырёх примитивов отказал — пропавший маршрут, мёртвый veth, рассогласование MTU, исчерпание conntrack — и починка следует сама.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="01-network-namespaces-and-bridges-retrieval"
  lang="ru"
  questions={[
    { q: "Проследи, что 'docker run' подключает для контейнера на дефолтном мосту, назвав все четыре примитива и числа по умолчанию.", a: "Демон создаёт сетевой namespace (netns), дающий контейнеру свои интерфейсы, таблицу маршрутизации, ARP-кэш и представление iptables. Затем создаёт veth-пару — виртуальный кабель из двух интерфейсов спина к спине; один конец становится eth0 внутри netns с адресом из 172.17.0.0/16, другой (vethXXXX) остаётся на хосте и подчинён мосту docker0, программному L2-свитчу с самим собой на 172.17.0.1 как шлюзом контейнеров. Оба конца veth по умолчанию MTU 1500. Наконец, правило iptables MASQUERADE в nat/POSTROUTING SNAT-ит пакеты, покидающие подсеть моста через реальную сетевую карту, на IP хоста, а conntrack записывает каждый поток, чтобы ответы раз-NAT-лись обратно. Контейнеры на одном мосту достигают друг друга напрямую на L2 без NAT." },
    { q: "Дай режим отказа и однострочный диагноз для каждого примитива: маршрутизация, veth/MTU и conntrack.", a: "Маршрутизация: контейнер с валидным eth0 и адресом, но пустой таблицей маршрутов не может покинуть свой линк — диагностируй через 'ip route' внутри netns (или через nsenter с хоста); причина — отсутствие маршрута по умолчанию через 172.17.0.1, часто кастомный entrypoint, сбросивший маршруты. MTU: veth, прибитый к 1500 поверх подложки с меньшим эффективным MTU (1450 на многих overlay/VPN), молча дропает крупные пакеты — TLS-рукопожатия зависают там, где шлётся кадр сертификата; чини, задав MTU драйвера моста. Conntrack: при десятках тысяч короткоживущих исходящих соединений в секунду таблица conntrack (дефолт nf_conntrack_max ~262144) заполняется, и новые соединения падают с 'nf_conntrack: table full, dropping packet', пока CPU выглядит простаивающим — диагностируй, читая /proc/sys/net/netfilter/nf_conntrack_count против максимума." },
  ]}
/>

<Recap lang="ru">
Сеть контейнера — не чёрный ящик; это четыре примитива ядра Linux, которые можно осмотреть руками. Сетевой namespace — это граница: каждый контейнер получает свои интерфейсы, таблицу маршрутизации, ARP-кэш, представление conntrack и правила iptables, изолированные от хоста и соседей, поэтому порты никогда не сталкиваются между контейнерами. veth-пара — кабель, соединяющий этот namespace с хостом: один конец — eth0 контейнера с адресом из 172.17.0.0/16, другой — хостовый vethXXXX, подчинённый мосту docker0, программному L2-свитчу на 172.17.0.1, позволяющему контейнерам на одном мосту достигать друг друга напрямую без NAT. Оба конца veth по умолчанию MTU 1500, и рассогласование с меньшим MTU подложки — классический баг молчаливого дропа, вешающий TLS-рукопожатия. Исходящий наружу SNAT-ится правилом iptables MASQUERADE в nat/POSTROUTING, срабатывающим лишь для пакетов, уходящих через реальную сетевую карту (защита '! -o docker0' — причина, по которой трафик той же подсети не тронут), а таблица conntrack ядра запоминает каждый поток, чтобы ответы вернулись нужному контейнеру — конечная таблица (дефолт ~262144), исчерпывающаяся при высокой текучести соединений и дропающая новые, пока машина выглядит простаивающей. Senior-ход — перестать дебажить приложение и определить, какой примитив отказал: пропавший маршрут, мёртвый veth, рассогласование MTU или исчерпание conntrack — у каждого есть диагноз одной командой с хоста. Теперь, когда видишь контейнер, который «ни до чего не достучаться», первая мысль — `ip route` внутри netns, а не ещё один час в логах приложения.
</Recap>
