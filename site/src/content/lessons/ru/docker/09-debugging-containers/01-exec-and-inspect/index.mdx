---
concepts:
- docker-exec-inspect
deepensInto: []
estMin: 16
lang: ru
lessonType: topic
level: senior
order: 1
prereqs: []
slug: "01-exec-and-inspect"
sources:
- https://docs.docker.com/reference/cli/docker/container/exec/
- https://docs.docker.com/reference/cli/docker/inspect/
- https://man7.org/linux/man-pages/man1/nsenter.1.html
spiral: []
status: ready
summary: 'docker exec входит в namespace''ы работающего контейнера и даёт шелл, но запускается как сосед PID 1 — не как PID 1 — поэтому живой шелл может прятать мёртвое приложение. inspect читает правду движка: State.ExitCode, где 137 — это OOM-kill, а 143 — SIGTERM.'
title: 'docker exec и inspect: вход в namespace''ы контейнера и чтение истинного состояния от движка'
track: "docker"
unit: "09-debugging-containers"
---

import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
Пейджер сработал в 03:12: API отдавал 502, но контейнер показывал `Up 3 hours`, а readiness-проба была зелёной. Дежурный сделал очевидное — `docker exec -it api sh`, затем `curl localhost:8080/health`. Connection refused. Значит, приложение упало. Но `ps` в этом шелле показывал работающий Java-процесс, PID 1, съедающий 100% одного ядра. Приложение не упало; оно зависло в GC-спирали смерти — достаточно живое, чтобы держать PID 1 открытым и контейнер никогда не выходил, достаточно мёртвое, чтобы ничего не отдавать. Шелл солгал умолчанием: `exec` высадил их в совершенно новый процесс, который был *соседом* приложения, разделял его namespace'ы, но не его судьбу. Настоящий сигнал был в одной команде — `docker inspect` показал бы мерцающий флаг OOMKilled и растущий счётчик рестартов — но шелл ощущался как «я внутри приложения», и это ощущение стоило двадцати минут.
</Hook>

<Crux>docker exec входит в namespace'ы соседом PID 1 — живой шелл может сидеть рядом с мёртвым приложением. inspect читает вердикт движка.</Crux>

<Explanation>

## exec входит в namespace'ы — он не становится приложением

Контейнер — это не коробка; это обычный процесс хоста, обёрнутый в namespace'ы (механизм изоляции ядра Linux: PID, mount, net, IPC, UTS) и cgroup'ы (контрольные группы, ограничивающие ресурсы). `docker exec` просит движок породить новый процесс *внутри тех же namespace'ов*: тот же сетевой стек, тот же вид файловой системы, то же PID-пространство — но это свежий процесс, потомок PID 1 контейнера, а не сам PID 1. Ровно это делает `nsenter --target <PID-на-хосте> --all` (утилита для входа в namespace'ы произвольного процесса), когда вы вручную обходите Docker: он делает `setns()` в каждый namespace целевого процесса. Это один и тот же механизм; `docker exec` — версия через движок, с корректными cgroup'ами, а `nsenter --target <pid> --all` — сырой вызов ядра, к которому тянешься, когда движок завис или у контейнера нет точки входа, которую демон уважит.

```bash
# Через движок: войти в namespace'ы api, попасть потомком его PID 1
docker exec -it api sh

# Сырое ядро: найти PID на хосте, setns во ВСЕ его namespace'ы
PID=$(docker inspect --format '{{.State.Pid}}' api)
sudo nsenter --target "$PID" --all   # --mount --uts --ipc --net --pid
```

Последствие, которое кусается: ваш шелл — не PID 1. Если приложение зависло, exec всё равно успешен — вы отдельный процесс. Если внутренние потоки приложения застряли, ваш `ps` их видит, а шелл продолжает отвечать. Здоровое приглашение доказывает, что жив *namespace*, а не что жива *нагрузка*. Честные проверки изнутри: `ps -ef`, чтобы подтвердить, что PID 1 — именно тот процесс, что вы ждёте, и он не зомби/defunct, и обращение к собственному порту приложения (`curl localhost:PORT`), а не вера в то, что «exec сработал, значит приложение работает».

<Quiz
  id="01-exec-and-inspect-quiz-1"
  lessonSlug="01-exec-and-inspect"
  lang="ru"
  question="Вы запускаете docker exec -it api sh и получаете рабочий шелл, но curl localhost:8080 внутри отвергается. Что на самом деле доказал рабочий шелл?"
  choices={[
    { label: "Только что namespace'ы контейнера живы — ваш шелл сосед PID 1, а не приложение, поэтому приложение может висеть, пока exec успешен", correct: true },
    { label: "Что приложение работает, ведь exec работает лишь на здоровом контейнере", misconception: "exec порождает новый процесс внутри namespace'ов независимо от состояния приложения; PID 1 может быть в дедлоке или крутиться, а exec всё равно высадит вас в свежий отзывчивый шелл. Рабочий шелл ничего не говорит о нагрузке." },
    { label: "Что контейнер перезапустился и потерял слушатель", misconception: "exec не вызывает и не подразумевает рестарт, а рестарт дал бы свежее приложение, а не отвергнутый порт. Отказ указывает на состояние работающего приложения, которое раскрывают inspect и ps — не на exec." },
    { label: "Что вы теперь работаете как PID 1 внутри контейнера", misconception: "exec делает вас потомком PID 1, никогда самим PID 1. Путаница между ними — ровно причина, почему зелёный шелл принимают за зелёное приложение." },
  ]}
/>

## inspect — это истина движка, а код выхода — приговор

Когда вопрос про нагрузку, перестаньте доверять виду изнутри и читайте то, что записал движок. `docker inspect` возвращает структурированное состояние от демона, и `.State.ExitCode` — самый сжатый отчёт об инциденте, который вы получите:

```bash
docker inspect --format '{{.State.Status}} exit={{.State.ExitCode}} oom={{.State.OOMKilled}} restarts={{.RestartCount}}' api
# exited exit=137 oom=true restarts=14
```

Коды выхода следуют формуле `128 + сигнал`: **137 = 128 + 9 (SIGKILL)** — почти всегда OOM-killer, когда `OOMKilled:true`, или `docker kill`; **143 = 128 + 15 (SIGTERM)** — чистый запрос на остановку, который приложение уважило (или сработал таймер grace у `docker stop`). Читать 137 как «приложение упало с багом» — классическая ошибка диагноза: ничто в коде приложения не бросило исключение — контроллер памяти cgroup в ядре убил его за превышение лимита, и фикс — это лимит памяти или поиск утечки, а не стектрейс. Растущий `RestartCount` с `oom=true` — это краш-луп по памяти, а не баг логики. `inspect` также даёт `.State.Pid` (PID на хосте, мост к `nsenter`), разрешённые маунты, действующее окружение и лог health-проверки — факты, которые переживут исчезновение контейнера.

<Quiz
  id="01-exec-and-inspect-quiz-2"
  lessonSlug="01-exec-and-inspect"
  lang="ru"
  question="docker inspect показывает State.ExitCode 137 с OOMKilled true и RestartCount 14. Как правильно это прочесть?"
  choices={[
    { label: "OOM-killer ядра прислал SIGKILL за превышение лимита памяти, и контейнер в краш-лупе — чините лимит или утечку, а не логику кода", correct: true },
    { label: "Приложение бросило непойманное исключение со статусом выхода 137", misconception: "137 — это 128 + 9, внешний SIGKILL, а не возвращаемое значение приложения. С OOMKilled true убийство пришло от контроллера памяти ядра; приложение не выбирало выход, поэтому стектрейс это не объяснит." },
    { label: "143 и 137 — взаимозаменяемые сигналы от docker stop", misconception: "143 — это 128 + 15 (SIGTERM, штатная остановка, которую приложение уважило); 137 — это 128 + 9 (SIGKILL). docker stop сначала шлёт SIGTERM (→143 при послушании) и эскалирует до SIGKILL (→137) только когда истекает grace. Это разные вещи." },
    { label: "RestartCount 14 означает, что человек перезапустил 14 раз", misconception: "RestartCount — это автоматический счётчик restart-политики движка, а не ручные рестарты. Четырнадцать авторестартов с OOMKilled true — сигнатура краш-лупа по памяти, который политика рестартов оживляет." },
  ]}
/>

<Inset kind="why" lang="ru">
Почему доверять inspect, а не шеллу? Потому что шелл — это живой вид движущейся системы, а inspect — записанный вердикт движка. Шелл может показать только текущее мгновение — и если приложение уже умерло, а restart-политика его оживила, шелл показывает *свежий здоровый* процесс, тогда как RestartCount и последний ExitCode у inspect сохраняют тот краш, который вы реально ищете. Истина — это слой, который помнит; вид изнутри забывает в момент рестарта.
</Inset>

</Explanation>

<FlowDiagram
  label="Дерево решений отладки: вопрос про живое состояние namespace'а или про записанный вердикт движка?"
  hue="var(--d-backend)"
  perRow={3}
  nodes={[
    { id: "q", label: "Контейнер сбоит", sub: "куда смотреть?" },
    { id: "live", label: "Нужно живое?", sub: "что делает сейчас" },
    { id: "verdict", label: "Нужен вердикт?", sub: "почему умер" },
    { id: "exec", label: "docker exec", sub: "сосед PID 1" },
    { id: "nsenter", label: "nsenter --target PID --all", sub: "движок завис" },
    { id: "inspect", label: "docker inspect", sub: "ExitCode 137=OOM 143=TERM" },
  ]}
  edges={[
    { from: "q", to: "live", label: "работает" },
    { from: "q", to: "verdict", label: "вышел/луп" },
    { from: "live", to: "exec", label: "демон ок" },
    { from: "live", to: "nsenter", label: "демон завис" },
    { from: "verdict", to: "inspect", label: "читать state" },
  ]}
  caption="exec и nsenter оба делают setns() в namespace'ы контейнера и высаживают вас соседом PID 1 — живой шелл не доказывает живого приложения. inspect читает State.ExitCode/OOMKilled/RestartCount — единственный вид, переживающий рестарт."
/>

<KeyTakeaway>docker exec — это nsenter с бухгалтерией движка: он входит в namespace'ы контейнера потомком PID 1, поэтому отзывчивый шелл доказывает, что жив namespace, но никогда — что жива нагрузка. Когда вопрос «почему умер», читайте docker inspect: State.ExitCode 137 — внешний SIGKILL (OOMKilled:true → killer памяти ядра, краш-луп, а не баг приложения), 143 — штатный SIGTERM. Шелл показывает настоящее и забывает при рестарте; inspect помнит вердикт.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="01-exec-and-inspect-retrieval"
  lang="ru"
  questions={[
    { q: "Почему docker exec может дать абсолютно здоровый шелл, пока приложение внутри мертво, и при чём тут nsenter?", a: "Контейнер — это процесс хоста внутри namespace'ов; docker exec порождает совершенно новый процесс внутри тех же namespace'ов (net, mount, PID, IPC, UTS) потомком PID 1 контейнера — он никогда не становится PID 1. Это форма nsenter --target <PID-на-хосте> --all через движок: nsenter вручную делает setns() в каждый namespace целевого процесса. Поскольку ваш шелл — отдельный от приложения процесс, exec успешен и остаётся отзывчивым, даже когда PID 1 в дедлоке, крутит GC или завис: рабочее приглашение доказывает живой namespace, а не нагрузку. Честные проверки — ps -ef (PID 1 ли это, кого вы ждёте, не зомби?) и обращение к собственному порту приложения, а не трактовка успеха exec как доказательства работы приложения. К сырому nsenter (через .State.Pid из inspect) тянешься, когда сам демон завис и docker exec не отвечает." },
    { q: "Как читать State.ExitCode у docker inspect и почему 137 регулярно диагностируют неверно?", a: "Коды выхода для процессов, убитых сигналом, — это 128 + сигнал. 137 = 128 + 9 (SIGKILL): с OOMKilled:true это контроллер памяти cgroup в ядре убивает процесс за превышение лимита — не ошибка приложения и не то, что объяснит стектрейс; фикс — лимит памяти или поиск утечки. 143 = 128 + 15 (SIGTERM): штатная остановка, которую приложение уважило, например docker stop в пределах grace. docker stop шлёт SIGTERM (→143 при послушании) и эскалирует до SIGKILL (→137) лишь по истечении таймера grace. Ошибка диагноза — читать 137 как «приложение упало с багом 137»; вместе с растущим RestartCount и OOMKilled:true это на самом деле краш-луп по памяти, который оживляет restart-политика. inspect — единственный вид, переживающий рестарт, поэтому он сохраняет краш, который живой шелл уже забыл." },
  ]}
/>

<Recap lang="ru">
Контейнер — это обычный процесс хоста, огороженный namespace'ами и cgroup'ами, и docker exec порождает новый процесс внутри тех же namespace'ов — эквивалент nsenter --target &lt;PID-на-хосте&gt; --all через движок, где nsenter вручную делает setns() во всё это. Подвох, который каждый сеньор однажды узнаёт на своей шкуре: этот новый процесс — сосед PID 1 контейнера, а не сам PID 1, поэтому exec успешен и ваш шелл отзывчив, даже когда приложение в дедлоке или крутится. Зелёное приглашение доказывает живой namespace, никогда — нагрузку; честные проверки изнутри — это ps -ef для подтверждения, что PID 1 — тот процесс, что вы ждёте, и обращение к реальному порту приложения. Когда вопрос меняется с «что оно делает» на «почему умерло», перестаньте доверять живому виду и читайте docker inspect — записанный вердикт движка. State.ExitCode говорит на языке 128 + сигнал: 137 — это внешний SIGKILL (с OOMKilled:true — killer памяти ядра, превышение лимита, краш-луп — не баг приложения и не задача про стектрейс), а 143 — штатный SIGTERM, который приложение уважило. docker stop идёт SIGTERM, затем SIGKILL, поэтому 143 против 137 говорит вам, было выключение чистым или принудительным. Растущий RestartCount с OOMKilled:true — это краш-луп по памяти, и поскольку живой шелл забывает в момент, когда restart-политика оживляет контейнер, inspect — единственный вид, помнящий тот краш, который вы ищете. К сырому nsenter через .State.Pid тянитесь лишь когда сам демон завис. Теперь, когда видите зелёный шелл рядом с отвергнутым портом — первым делом тянитесь к inspect, а не к промпту; и ExitCode 137 читаете как проблему лимита памяти, а не как баг в коде.
</Recap>
