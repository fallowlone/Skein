---
concepts:
- docker-debug-distroless
deepensInto: []
estMin: 17
lang: ru
lessonType: topic
level: senior
order: 3
prereqs: []
slug: "03-debugging-distroless"
sources:
- https://docs.docker.com/reference/cli/docker/debug/
- https://kubernetes.io/docs/tasks/debug/debug-application/debug-running-pod/
- https://github.com/GoogleContainerTools/distroless
spiral: []
status: ready
summary: 'У distroless-образа нет шелла, поэтому docker exec sh падает — войти не во что. Его отлаживают снаружи: эфемерный debug-контейнер (docker debug / kubectl debug), разделяющий namespace''ы цели и приносящий свои инструменты, или доступ к ФС мёртвого контейнера через /proc/PID/root.'
title: 'Отладка distroless: ни шелла, ни exec — вносим свои инструменты в namespace''ы чужого контейнера'
track: "docker"
unit: "09-debugging-containers"
---

import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
Сервис выехал на `gcr.io/distroless/static` — 2 МБ, без шелла, без пакетного менеджера, чистый скан безопасности, все довольны. Пока он не начал отдавать обрезанные ответы под нагрузкой, и дежурный потянулся за рефлексом: `docker exec -it svc sh`. `OCI runtime exec failed: exec: "sh": executable file not found in $PATH`. Нет `sh`. Нет `bash`. Нет `ls`, `cat`, `curl` — образ был *просто бинарником*. Инстинкт, работавший десятилетие — войти внутрь и осмотреться — упёрся в стену, которой образ был намеренно построен. Хуже: когда бинарник через десять минут упал в segfault, контейнер *исчез*, а с ним и любой шанс осмотреть оставленную им файловую систему. Первой реакцией команды было пересобрать на debug-базе и передеплоить — что уничтожило бы то самое состояние, которое они пытались захватить. Настоящий ход был противоположностью входа *внутрь*: инструменты приносишь к контейнеру *снаружи*, входя в его namespace'ы из отдельного образа с шеллом, не модифицируя то, что отлаживаешь.
</Hook>

<Crux>У distroless-образа нет шелла для exec. Отлаживай снаружи: эфемерный контейнер делит namespace'ы и несёт инструменты — не пересборкой.</Crux>

<Explanation>

## Почему exec падает: шелла там никогда не было, чтобы его найти

`docker exec sh` не вызывает шелл; он исполняет бинарник, который должен уже существовать в файловой системе контейнера. Distroless-образы (и на базе `scratch`, и большинство хорошо укреплённых) содержат только бинарник приложения и его рантайм-библиотеки — никакого `/bin/sh`, никакого busybox, никакого coreutils. Это отсутствие и есть фича безопасности: атакующий, добившийся RCE, не находит шелла для разворота, ни `curl` для эксфильтрации — почти нулевая поверхность атаки. Так что `exec: "sh": executable file not found` — не баг, а образ, работающий как задумано, и инструмент, к которому тянешься, должен уважать, что *работающий* контейнер остаётся немодифицированным.

Правильный примитив приносит инструменты, а не ищет их. `docker debug` (Docker Desktop / Pro) подключает эфемерный набор инструментов к работающему контейнеру, давая шелл-с-утилитами *в namespace'ах цели* без изменения её образа или ФС. В Kubernetes та же идея — `kubectl debug -it <pod> --image=busybox --target=<container>`: он поднимает **эфемерный контейнер** в поде, разделяющий PID- и сетевой namespace'ы цели, так что ваши busybox `ps`/`curl`/`nc` видят distroless-процесс и его сокеты напрямую. `--target` — самая важная часть: он входит в *процессный* namespace конкретного контейнера, так что `ps` показывает PID 1 приложения, а не только ваш debug-шелл. Ничто в образе приложения не меняется; вы подключаетесь, смотрите, отключаетесь, а эфемерный контейнер выбрасывается.

```bash
# Kubernetes: подключить busybox, разделяющий namespace'ы distroless-контейнера
kubectl debug -it payments-xyz --image=busybox:1.36 \
  --target=payments        # войти в PID + net namespace ЭТОГО контейнера

# Внутри: теперь есть инструменты, которых нет у distroless-образа
ps -ef                      # видеть PID 1 приложения, общий PID namespace
wget -qO- localhost:8080/health   # бить в порт приложения, общий net namespace
```

<Quiz
  id="03-debugging-distroless-quiz-1"
  lessonSlug="03-debugging-distroless"
  lang="ru"
  question="docker exec -it svc sh на distroless-контейнере возвращает 'exec: sh: executable file not found in $PATH'. Как правильно это понять и что делать дальше?"
  choices={[
    { label: "У образа нет шелла по дизайну — приноси инструменты снаружи эфемерным debug-контейнером (docker debug / kubectl debug --target), разделяющим namespace'ы работающего контейнера, не модифицируя его", correct: true },
    { label: "Контейнер повреждён; пересобери на базовом образе с шеллом и передеплой", misconception: "Пересборка и редеплой уничтожают состояние работающего контейнера — ровно то, что вы пытаетесь осмотреть — и трактуют намеренную фичу укрепления как повреждение. Отсутствующий шелл — это образ, работающий как задумано; отлаживай снаружи." },
    { label: "Установи переменную окружения PATH, чтобы docker нашёл sh", misconception: "sh реально отсутствует в файловой системе, а не просто вне PATH — нет /bin/sh, на который можно указать. Никакая переменная не вызовет бинарник, которого образ никогда не включал." },
    { label: "Используй docker exec с другим шеллом вроде bash", misconception: "Distroless не несёт ни sh, ни bash, ни coreutils — нет бинарника шелла любого имени, чтобы сделать exec. Фикс — не другое имя шелла, а другой подход: подключить инструменты из отдельного образа." },
  ]}
/>

## Когда контейнер уже мёртв: скопируй статический busybox или читай /proc/PID/root

Эфемерным debug-контейнерам нужна *работающая* цель, чтобы разделить namespace'ы. Если distroless-бинарник уже упал, подключаться не к чему — процесс и его namespace'ы исчезли. Два сеньорских хода закрывают пробелы:

- **Для всё ещё работающего контейнера без доступного debug-инструментария**: скопируйте внутрь самодостаточный статический бинарник и запустите, например `docker cp busybox <ctr>:/busybox && docker exec <ctr> /busybox sh`. Поскольку busybox слинкован статически, ему не нужны библиотеки из distroless-образа — он несёт всё с собой. Это *затрагивает* записываемый слой контейнера, поэтому это намеренная модификация на крайний случай, а не чистый путь эфемерного контейнера.
- **Чтобы достать ФС контейнера с хоста**: корень каждого работающего контейнера смонтирован на хосте по `/proc/<PID-на-хосте>/root`. С `PID=$(docker inspect --format '{{.State.Pid}}' svc)` команда `sudo ls /proc/$PID/root/app` даёт читать distroless-ФС — конфиг, бинарник, записанные файлы — инструментами *хоста*, вообще без шелла в контейнере. Так осматривают файлы бесшелльного образа, не входя в него.

Для самого краша отладка смещается влево и наружу: захватите **core dump** (задайте `--ulimit core=...` и хостовый `core_pattern` или пусть рантайм пишет dump в смонтированный том), чтобы segfault оставил артефакт для офлайн-анализа, и опирайтесь на запись движка — `docker inspect` State и `docker events` — раз мёртвый контейнер уже не войти никаким способом.

<Quiz
  id="03-debugging-distroless-quiz-2"
  lessonSlug="03-debugging-distroless"
  lang="ru"
  question="Distroless-контейнер уже упал и исчез. Почему эфемерный debug-контейнер (kubectl debug --target) здесь не поможет и что поможет?"
  choices={[
    { label: "Эфемерные debug-контейнеры разделяют namespace'ы *работающей* цели — раз процесс исчез, входить не во что; вместо этого опирайтесь на захваченные core dump'ы, docker inspect State и events из записи движка", correct: true },
    { label: "Всё равно работает — kubectl debug может подключиться к namespace'ам мёртвого контейнера", misconception: "Namespace'ы существуют лишь пока процесс держит их открытыми; когда distroless-бинарник выходит, его PID- и net-namespace'ы сносятся, поэтому --target не к чему подключаться. Post-mortem требует артефактов, захваченных до смерти." },
    { label: "Просто скопируй статический busybox в мёртвый контейнер", misconception: "docker cp и docker exec оба требуют живого контейнера; нельзя сделать exec в процесс, которого больше нет. Статический busybox помогает *работающему* бесшелльному контейнеру, не упавшему." },
    { label: "Читай /proc/PID/root мёртвого контейнера с хоста", misconception: "/proc/<PID-на-хосте>/root существует лишь пока процесс жив — как только контейнер выходит, PID исчезает и этот путь пропадает. Это инструмент для живых бесшелльных контейнеров, не для уже упавшего." },
  ]}
/>

<Inset kind="why" lang="ru">
Почему «приноси инструменты снаружи» — верная ментальная модель, а не «войди в шелл»? Потому что вся ценность укреплённого образа в том, что внутри нет инструментов, чтобы их найти — ни вам, ни атакующему. Масштабируемый примитив отладки — это разделение: инструменты живут в одноразовом образе, namespace'ы разделены, чтобы эти инструменты видели реальный процесс и сокеты, а образ приложения остаётся байт-в-байт неизменным. Вы не меняете безопасность на отлаживаемость; вы держите и то, и другое, держа их в разных контейнерах.
</Inset>

</Explanation>

<FlowDiagram
  label="Дерево решений для бесшелльного образа: цель ещё работает и можно ли подключить инструменты снаружи?"
  hue="var(--d-backend)"
  perRow={3}
  nodes={[
    { id: "no-shell", label: "exec sh падает", sub: "distroless, нет шелла" },
    { id: "running", label: "Ещё работает?", sub: "namespace'ы есть" },
    { id: "ephemeral", label: "Эфемерный debug-ctr", sub: "docker/kubectl debug --target" },
    { id: "proc", label: "/proc/PID/root", sub: "читать ФС с хоста" },
    { id: "dead", label: "Уже упал?", sub: "namespace'ы ушли" },
    { id: "postmortem", label: "Core dump + inspect + events", sub: "запись движка" },
  ]}
  edges={[
    { from: "no-shell", to: "running", label: "проверь state" },
    { from: "running", to: "ephemeral", label: "общие namespace'ы" },
    { from: "running", to: "proc", label: "читать файлы" },
    { from: "no-shell", to: "dead", label: "вышел" },
    { from: "dead", to: "postmortem", label: "не войти" },
  ]}
  caption="У distroless-образа нет шелла, во что войти через exec. Работает: подключи эфемерный debug-контейнер, разделяющий его namespace'ы, или читай /proc/PID/root с хоста — никогда не пересобирай работающий образ. Упал: namespace'ы ушли, остаются лишь заранее захваченные core dump'ы и inspect/events движка."
/>

<KeyTakeaway>У distroless-образа нет шелла, потому что это отсутствие и есть фича безопасности — поэтому docker exec sh падает по дизайну, а не из-за поломки. Отлаживайте снаружи, не трогая работающий образ: эфемерным debug-контейнером (docker debug, kubectl debug --target), разделяющим PID- и net-namespace'ы цели и приносящим свои инструменты, или читайте ФС по /proc/PID/root с хоста; скопированный внутрь статический busybox — крайняя мера, модифицирующая контейнер. Как только контейнер упал, его namespace'ы ушли — войти нельзя — поэтому post-mortem живёт целиком в заранее захваченных core dump'ах плюс docker inspect и events. Никогда не пересобирайте на debug-базе для расследования: это уничтожает состояние, которое вы ищете.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="03-debugging-distroless-retrieval"
  lang="ru"
  questions={[
    { q: "Почему docker exec sh падает на distroless-образе и как правильно получить инструменты отладки против работающего?", a: "docker exec запускает бинарник, который должен уже существовать в файловой системе контейнера; distroless (и на базе scratch) несут только бинарник приложения и его рантайм-библиотеки — никакого /bin/sh, busybox, coreutils — так что sh для исполнения буквально нет. Это отсутствие и есть фича безопасности: атакующий с RCE не находит шелла для разворота. Правильный ход приносит инструменты снаружи, не модифицируя работающий образ: эфемерный debug-контейнер — docker debug или kubectl debug -it <pod> --image=busybox --target=<container> — поднимает одноразовый образ, разделяющий PID- и сетевой namespace'ы цели, так что его busybox ps/curl/nc видят PID 1 distroless-процесса и сокеты напрямую. --target обязателен: он входит в процессный namespace конкретного контейнера. Вы подключаетесь, осматриваете, отключаетесь, эфемерный контейнер выбрасывается; образ приложения остаётся байт-в-байт неизменным, сохраняя и безопасность, и состояние, которое вы отлаживаете." },
    { q: "Как осмотреть ФС бесшелльного контейнера с хоста и что меняется после краша?", a: "Корень каждого работающего контейнера смонтирован на хосте по /proc/<PID-на-хосте>/root, где PID-на-хосте берётся из docker inspect --format '{{.State.Pid}}'. Так что sudo ls /proc/$PID/root/app читает distroless-ФС — конфиг, бинарник, записанные файлы — собственными инструментами хоста, вообще без шелла внутри контейнера. Как намеренная крайняя мера на работающем контейнере можно также docker cp статически слинкованный busybox внутрь и docker exec /busybox sh, ведь статическому бинарнику не нужны библиотеки из образа (это затрагивает записываемый слой). Всё это требует живого процесса: namespace'ы и /proc/<PID>/root существуют лишь пока процесс держит их открытыми. Как только distroless-контейнер падает, PID и namespace'ы сносятся — ничто не подключится и не прочитает /proc — поэтому post-mortem целиком зависит от артефактов, захваченных до смерти: core dump (через --ulimit core и хостовый core_pattern или dump в смонтированный том) плюс запись движка в docker inspect State и docker events." },
  ]}
/>

<Recap lang="ru">
distroless-образ несёт только бинарник приложения и его рантайм-библиотеки — никакого /bin/sh, busybox, coreutils — поэтому docker exec sh падает с 'executable file not found', потому что шелла для исполнения реально нет, и это отсутствие и есть суть: атакующий с RCE не находит ничего для разворота. Верная модель отладки — приносить инструменты снаружи, а не искать внутри, и никогда не пересобирать работающий образ на debug-базе, что уничтожило бы то самое состояние, которое вы расследуете. Против работающего контейнера эфемерный debug-контейнер — docker debug или kubectl debug --image=busybox --target=<container> — подключает одноразовый набор инструментов, разделяющий PID- и сетевой namespace'ы цели, так что его инструменты видят PID 1 distroless-процесса и сокеты напрямую, пока образ приложения остаётся байт-в-байт неизменным. Чтобы прочитать ФС вообще не входя, корень каждого работающего контейнера смонтирован на хосте по /proc/<PID-на-хосте>/root, с PID из State.Pid у docker inspect. Как намеренную крайнюю меру можно docker cp статически слинкованный busybox в работающий контейнер и сделать exec, ведь статическому бинарнику от образа ничего не нужно — но это затрагивает записываемый слой. Всё выше требует живого процесса: namespace'ы и /proc/<PID>/root существуют лишь пока процесс держит их открытыми. В момент краха distroless-контейнера его namespace'ы сносятся, и ничто не может войти, поэтому post-mortem живёт целиком в артефактах, захваченных до смерти — core dump из --ulimit core плюс хостовый core_pattern или dump на смонтированном томе — и в собственной записи движка в docker inspect State и docker events. Вы держите и безопасность, и отлаживаемость сразу, держа их в разных контейнерах.
</Recap>
