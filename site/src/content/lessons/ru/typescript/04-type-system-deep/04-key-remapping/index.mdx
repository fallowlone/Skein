---
concepts:
  - key-remapping
  - mapped-types
  - template-literal-types
  - filtering-keys
  - intrinsic-string-types
deepensInto:
  - "typescript/05-type-level-programming/03-utility-types-from-scratch"
estMin: 16
lang: ru
lessonType: topic
level: senior
order: 4
prereqs:
  - "typescript/04-type-system-deep/03-mapped-types"
slug: "04-key-remapping"
sources:
  - https://www.typescriptlang.org/docs/handbook/2/mapped-types.html#key-remapping-via-as
  - https://www.typescriptlang.org/docs/handbook/release-notes/typescript-4-1.html#key-remapping-in-mapped-types
spiral:
  - type-level-programming
  - utility-types
status: ready
summary: "Ремаппинг ключей (`as`, TS 4.1) вычисляет новый ключ на каждой итерации. Переименовывайте шаблонными литералами и `Capitalize` или ремаппьте ключ в `never`, чтобы удалить — так фильтруют по типу значения. Ключи должны быть string|number|symbol, а `as` ломает гомоморфность."
title: "Ремаппинг ключей через as: переименование и фильтрация ключей в отображённом типе"
track: "typescript"
unit: "04-type-system-deep"
---

import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
Библиотека управления состоянием типизирует действия вашего стора из состояния: `{ name: string; age: number }` становится `{ setName(v: string): void; setAge(v: number): void }`. Без генерации кода, без декораторов — отображённый тип переименовал каждый ключ из `name` в `setName` и переформировал его значение в сеттер. Механизм — оговорка в четыре слова внутри отображённого типа: `as`, за которым следует вычисленный ключ. И тот же `as` может заставить ключ исчезнуть.
</Hook>

После этого урока вы сможете построить такое переименование, отфильтровать тип до полей-данных по значению — и точно объяснить, что вы теряете при `as`: гомоморфность уходит, `readonly` молча сбрасывается.

<Crux>`as NewKey` в отображённом типе пересчитывает каждый ключ: string/number/symbol переименовывает, `never` удаляет ключ.</Crux>

<Explanation>

## Переименование ключей через `as` и шаблонные литералы

Когда нужно, чтобы все ключи типа следовали соглашению об именовании — добавить префикс `get`, суффикс `Changed`, привести к верхнему регистру — можно написать каждый вручную. Или указать отображённому типу вычислять ключ, а не переиспользовать его.

С TS 4.1 отображённый тип может добавить `as <выражение>` после части `in`. Для каждого `K` свойство выдаётся под ключом, в который разрешается `<выражение>`, а не под самим `K`. В сочетании с шаблонными литеральными типами и встроенным `Capitalize` это переименовывает ключи механически:

```ts
type Getters<T> = {
  [K in keyof T as `get${Capitalize<string & K>}`]: () => T[K];
};

type State = { name: string; age: number };
type G = Getters<State>;
// ^? { getName: () => string; getAge: () => number }
```

`Capitalize<"name">` — это `"Name"` (встроенный тип манипуляции строкой — урок про шаблонные литералы в юните 05 разбирает их полностью), поэтому шаблон `` `get${...}` `` даёт `"getName"`. Пересечение `string & K` несущее: `keyof T` может включать ключи `number` и `symbol`, но `Capitalize` принимает только `string`, поэтому `string & K` сужает каждый ключ до его строковой части перед интерполяцией.

## Фильтрация ключей: ремаппинг в `never`, чтобы удалить их

Вот приём, который делает ремаппинг мощным. **Если вычисленный ключ — `never`, это свойство полностью опускается.** Соедините это с условием на тип значения — и получите фильтрацию по значению, без списка ключей для `Pick`/`Omit`:

```ts
// Убрать каждое свойство, чьё значение — функция
type DataOnly<T> = {
  [K in keyof T as T[K] extends (...a: any[]) => any ? never : K]: T[K];
};

type Model = { id: number; name: string; save(): void; load(): void };
type D = DataOnly<Model>;
// ^? { id: number; name: string }   — save и load ремаппнуты в never, удалены
```

Условие выполняется по ключам: ключи со значениями-функциями ремаппятся в `never` (и исчезают), остальные ремаппятся в самих себя (`K`) и выживают. Обратное — *оставить* только ключи, чьё значение совпадает — просто меняет ветки:

```ts
// Оставить только ключи, чьё значение — строка (Pick-по-типу-значения)
type StringKeysOnly<T> = {
  [K in keyof T as T[K] extends string ? K : never]: T[K];
};

type R = StringKeysOnly<{ a: string; b: number; c: string }>;
// ^? { a: string; c: string }
```

<Inset kind="why" lang="ru">
Почему `never` удаляет ключ, а не создаёт свойство с ключом `never`? Множество ключей отображённого типа — это объединение всех вычисленных ключей, а `never` — *пустое* объединение: оно ничего не добавляет в это объединение, поэтому для этой итерации свойство не порождается. Это тот же факт «never — пустое объединение» из урока про условные типы, теперь использованный как примитив удаления. Важно: *значение* `T[K]` удалённого ключа никогда не вычисляется в выход, поэтому это настоящее опущение, а не член со значением `never`.
</Inset>

## Два ограничения: допустимые типы ключей и потерянная гомоморфность

Вычисленный ключ должен быть совместим с `PropertyKey` — то есть `string | number | symbol`. Ремаппьте в что-то иное (объект, булево) — и TypeScript выдаст ошибку. Поэтому переименования идут через шаблонные литералы (которые дают строковые ключи), а фильтры — через `never` (пустой ключ).

Ремаппинг также **ломает гомоморфность** (прошлый урок): как только вы добавляете `as`, отображённый тип перестаёт быть голой формой `{ [K in keyof T]: ... }`, поэтому связь с источником разрывается — модификаторы вроде `readonly`/`?` больше не копируются автоматически, а массивы/кортежи не сохраняются как контейнеры. Если нужны и переименование, и сохранение модификаторов, вы заново применяете модификаторы явно.

<FlowDiagram
  label="Ремаппинг ключей по итерациям: вычисли новый ключ, затем сохрани или удали свойство в зависимости от того, never ли он."
  hue="var(--accent)"
  perRow={3}
  nodes={[
    { id: "k", label: "Ключ K in keyof T", sub: "одна итерация" },
    { id: "as", label: "Вычислить as NewKey", sub: "переименов/фильтр" },
    { id: "q", label: "NewKey это never?", sub: "пустое объедин.?" },
    { id: "drop", label: "Удалить свойство", sub: "опущено" },
    { id: "key", label: "Использовать NewKey", sub: "string|num|sym" },
    { id: "val", label: "Выдать значение", sub: "T[K] или F<T[K]>" },
  ]}
  edges={[
    { from: "k", to: "as" },
    { from: "as", to: "q" },
    { from: "q", to: "drop", label: "да" },
    { from: "q", to: "key", label: "нет" },
    { from: "key", to: "val" },
  ]}
  caption="Каждый ключ пересчитывается оговоркой as. Ключ never ничего не добавляет в объединение ключей результата, поэтому свойство опускается; любой допустимый PropertyKey становится переименованным ключом, несущим (возможно преобразованное) значение."
/>

## Цена: карта с переименованием ключей — это долг по сопровождению

Переименование ключей — самый магический из трюков с отображаемыми типами, а у магии есть цена. Первая цена — *незаметная поломка*: поскольку `as` рвёт гомоморфизм, `Getters<T>` молча сбрасывает модификаторы `readonly` и `?`, которые нёс источник. Коллега, позже пометивший поле `readonly` в типе состояния, видит, как это ограничение исчезает из сгенерированных сеттеров — реальный баг без ошибки. Вторая цена — *навигируемость*: `getName` не существует нигде в источнике, поэтому Find-References, grep и ментальная модель нового читателя его не находят. Сгенерированный ключ не отлаживается текстовым поиском.

Senior-критерий: переименование уместно, когда правило переименования действительно единообразно, а исходный набор открыт — вывод интерфейса обработчиков событий из типа пропсов, где добавление пропса должно автоматически добавлять обработчик. Это *неверный* инструмент в момент, когда преобразование ключа кодирует бизнес-смысл, который читатель должен видеть, или когда у источника лишь горстка фиксированных полей. Для фильтрации по значению конкретно — взвесьте против рантайм-проверки: если цель — *действовать* над полями-данными в рантайме, обычный `Object.entries(...).filter(...)` читаем и тестируем, тогда как `DataOnly<T>` лишь сдвигает различение на этап компиляции и в рантайме не даёт ничего. А когда внешнему потребителю (SDK, публичному API) нужны эти переименованные формы, **шаг кодогенерации**, выписывающий `interface StoreActions { setName(v: string): void }` как настоящий исходник, лучше переименованного типа, который потребитель не может ни сгрепать, ни чисто навести, ни расширить — сгенерированный `.d.ts` — это артефакт; башня `as` — загадка.

<Inset kind="warning" lang="ru">
Переименование также усиливает риск взрыва шаблонных литералов, с которым вы встретитесь в следующем уроке. Переименование вроде `` as `${Uppercase<K>}_${Uppercase<P>}` `` над двумя объединениями ключей строит *декартово произведение* строковых литералов: два набора по 50 членов дают объединение в 2500 членов за один шаг, а третья ось толкает вас к потолку в **100 000 членов** и `Expression produces a union type that is too complex to represent`. Следите за `Instantiation count` под `tsc --extendedDiagnostics`; переименование, перемножающее наборы ключей, — быстрый путь от отзывчивого редактора к многосекундным наведениям. Держите входные наборы ключей переименования малыми или генерируйте имена заранее.
</Inset>

<Quiz
  id="04-remap-quiz-1"
  lessonSlug="04-key-remapping"
  lang="ru"
  question="Чему равно DataOnly<{ id: number; save(): void }>, где DataOnly<T> = { [K in keyof T as T[K] extends (...a: any[]) => any ? never : K]: T[K] }?"
  choices={[
    { label: "{ id: number } — save имеет значение-функцию, поэтому её ключ ремаппится в never и свойство удаляется, а id ремаппится в самого себя.", correct: true },
    { label: "{ id: number; save: never } — save становится свойством со значением never.", misconception: "Ключ never полностью опускает свойство; он не создаёт член со значением never. Значение T[K] удалённого ключа вообще не выдаётся." },
    { label: "{ id: number; save(): void } — ремаппинг только переименовывает ключи, удалять их он не может.", misconception: "Ремаппинг в never — именно так удаляют ключи. Здесь ключ save разрешается в never (пустое объединение) и ничего не добавляет, поэтому save удаляется." },
    { label: "never — любой ключ, разрешающийся в never, делает весь тип never.", misconception: "Опускается лишь это одно свойство; остальное объединение ключей не затронуто. Результат — { id: number }, а не never." },
  ]}
/>

<DragOrder
  id="04-remap-drag-1"
  pieceSlug="04-key-remapping"
  lang="ru"
  prompt="Упорядочьте, как TypeScript разрешает Getters<{ name: string }>, где Getters<T> = { [K in keyof T as `get${Capitalize<string & K>}`]: () => T[K] }:"
  items={[
    { id: "a", label: "Обходит keyof T; единственный ключ — 'name'" },
    { id: "b", label: "Сужает ключ через string & K до его строковой части, 'name'" },
    { id: "c", label: "Вычисляет новый ключ: Capitalize<'name'> — это 'Name', поэтому шаблон даёт 'getName'" },
    { id: "d", label: "Выдаёт свойство под 'getName' со значением () => T['name'], то есть () => string" },
  ]}
/>

</Explanation>

<KeyTakeaway>
Ремаппинг ключей добавляет в отображённый тип оговорку `as <выражение>` (TS 4.1), которая пересчитывает каждый ключ. Разрешите выражение в `string | number | symbol`, чтобы **переименовать** — обычно шаблонным литералом и встроенным `Capitalize`, где `string & K` отсекает нестроковые ключи перед интерполяцией — или разрешите его в `never`, чтобы **удалить** ключ, ведь `never` — пустое объединение и не даёт свойства. Ремаппинг-в-`never` плюс условие на тип значения дают фильтрацию по значению (убрать функции, оставить строки) без перечисления ключей. Два правила: вычисленный ключ должен быть допустимым `PropertyKey`, и добавление `as` **ломает гомоморфность**, поэтому модификаторы источника и природа массива/кортежа больше не сохраняются автоматически.
</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="04-remap-retrieval"
  lang="ru"
  questions={[
    { q: "Что делает оговорка `as` в отображённом типе и как построить Getters<T>, переименовывающий ключи?", a: "С TS 4.1 отображённый тип может писать `[K in keyof T as <выражение>]`, что пересчитывает выходной ключ для каждой итерации в то, во что разрешается `<выражение>`, вместо переиспользования K. Чтобы переименовать, разрешите выражение в новый строковый ключ, обычно через шаблонный литерал плюс встроенный тип вроде Capitalize: Getters<T> = { [K in keyof T as `get${Capitalize<string & K>}`]: () => T[K] } превращает { name: string; age: number } в { getName: () => string; getAge: () => number }. Пересечение `string & K` обязательно, потому что keyof T может включать ключи number/symbol, а Capitalize принимает только string, поэтому оно сужает каждый ключ до его строковой части перед интерполяцией." },
    { q: "Как фильтровать ключи объекта по типу значения и почему ремаппинг ключа в never удаляет его?", a: "Поставьте условие на значение в оговорке `as` и ремаппьте ненужные ключи в never. DataOnly<T> = { [K in keyof T as T[K] extends (...a:any[])=>any ? never : K]: T[K] } убирает каждый ключ со значением-функцией (save/load) и оставляет поля данных. Поменяйте ветки — `? K : never` — чтобы вместо этого оставлять совпадающие ключи (Pick-по-типу-значения). Это работает, потому что итоговое множество ключей отображённого типа — объединение всех вычисленных ключей, а never — пустое объединение: ключ, вычисленный в never, ничего не добавляет в это объединение, поэтому для этой итерации свойство не порождается, и значение T[K] удалённого ключа не выдаётся. Это настоящее опущение, а не член со значением never." },
    { q: "Какие два ограничения управляют ремаппингом ключей и чего стоит добавление `as`?", a: "(1) Вычисленный ключ должен быть совместим с PropertyKey — string | number | symbol; произведите что-то иное (объект, булево и т. д.) — и TypeScript выдаст ошибку. Поэтому переименования идут через шаблонные литералы (строковые ключи), а фильтры — через never (пустой ключ). (2) Добавление `as` ломает гомоморфность: отображённый тип перестаёт быть голой формой { [K in keyof T]: ... }, которую TypeScript определяет как гомоморфную, поэтому связь с источником T разрывается — модификаторы readonly/? больше не копируются автоматически, а массивы/кортежи не сохраняются как контейнеры. Если нужны переименование или фильтр И сохранение модификаторов, придётся заново объявить модификаторы (например, вернуть readonly/?) явно в отображённом типе." },
  ]}
/>

<Recap lang="ru">
Оговорка `as` превратила сторону ключей отображённого типа в вычисление: переименование через шаблонные литералы или удаление через `never` для фильтрации по типу значения — и вы увидели, что эта мощь стоит вам гомоморфности. Здесь мы опирались на `Capitalize` и `` `get${...}` `` без полного объяснения. Дальше — **шаблонные литеральные типы** делают это главным событием: построение строковых типов интерполяцией, наблюдение, как объединения взрываются в декартово произведение, и использование `infer` *внутри* шаблонного образца для разбора строк — извлечение `:id` из `/users/:id` и разбиение по разделителям. Теперь, когда встречаете `Getters<T>` или `DataOnly<T>` в библиотечном коде, вы сразу видите оговорку `as`, прослеживаете правило переименования и знаете, какие модификаторы молча потерялись.
</Recap>
