---
concepts:
  - mapped-types
  - mapping-modifiers
  - homomorphic-mapped-types
  - keyof
  - utility-types
deepensInto:
  - "typescript/04-type-system-deep/04-key-remapping"
estMin: 16
lang: ru
lessonType: topic
level: middle
order: 3
prereqs:
  - "typescript/04-type-system-deep/01-conditional-types"
slug: "03-mapped-types"
sources:
  - https://www.typescriptlang.org/docs/handbook/2/mapped-types.html
spiral:
  - type-level-programming
  - utility-types
status: ready
summary: "Отображённый тип `{ [K in keyof T]: ... }` обходит ключи T, преобразуя значения, с модификаторами `?`/`-?` и `readonly`/`-readonly`. Тонкость — гомоморфность: отображение по `keyof T` сохраняет модификаторы и массивы/кортежи, а по отдельному объединению — нет."
title: "Отображённые типы: обход ключей типа для построения нового"
track: "typescript"
unit: "04-type-system-deep"
---

import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";
import Infographic from "~/components/diagram/Infographic.astro";
import homomorphismContrast from "./infographics/homomorphism-contrast.svg?raw";

<Hook>
Вы передаёте библиотеке объект конфигурации, и она принимает `Partial<Config>` — каждое поле вдруг опционально, хотя никто не писал второй интерфейс. Наводитесь на `Partial` и находите `{ [P in keyof T]?: T[P] }`. Одна строка породила целого опционального двойника вашего типа. `Partial`, `Required`, `Readonly`, `Pick`, `Record` — почти каждый служебный тип в `lib.es5.d.ts` это отображённый тип: цикл по ключам, который пересобирает объект член за членом.
</Hook>

В этом уроке вы напишете каждый из этих служебных типов с нуля и разберёте негласное правило гомоморфности, которое определяет, сохранит ли ваш отображённый тип массивы — или молча превратит их в обычные объекты.

<Crux>Отображённый тип `{ [K in keyof T]: ... }` пересобирает объект по ключам, преобразуя типы значений и добавляя или снимая `?`/`readonly`.</Crux>

<Explanation>

## Обход ключей через `K in keyof T`

Спросите себя: откуда `Partial<Config>` знает, какие поля сделать опциональными, не перечисляя их? Потому что отображённые типы не знают ключей заранее — они вычисляют их при инстанцировании. В этом вся сила: добавьте поле в `Config` — и оно автоматически станет опциональным в `Partial<Config>` без единой дополнительной строки.

Отображённый тип выглядит как индексная сигнатура, но `in` делает его циклом: для каждого ключа `K` в объединении `keyof T` выдаётся свойство. Внутри `T[K]` — исходный тип значения по этому ключу.

```ts
type Stringify<T> = { [K in keyof T]: string };

type Config = { port: number; host: string; tls: boolean };
type S = Stringify<Config>;
// ^? { port: string; host: string; tls: string }
```

`keyof Config` — это `"port" | "host" | "tls"`; отображённый тип обходит это объединение и ставит каждому значению `string`. Чтобы сохранить исходный тип значения, сошлитесь на `T[K]`:

```ts
type Clone<T> = { [K in keyof T]: T[K] };
type C = Clone<Config>;
// ^? { port: number; host: string; tls: boolean }
```

## Модификаторы: добавить или снять `?` и `readonly`

Отображённый тип может прикрепить или убрать два модификатора свойства. `?` делает опциональным, `-?` *убирает* опциональность; `readonly` добавляет его, `-readonly` убирает. Так строятся стандартные утилиты — вот они с нуля:

```ts
type MyPartial<T>  = { [K in keyof T]?: T[K] };           // добавить ?
type MyRequired<T> = { [K in keyof T]-?: T[K] };          // снять ?
type MyReadonly<T> = { readonly [K in keyof T]: T[K] };   // добавить readonly
type MyMutable<T>  = { -readonly [K in keyof T]: T[K] };  // снять readonly

type Src = { a?: number; readonly b: string };

type P = MyPartial<Src>;  // ^? { a?: number; readonly b?: string }
type R = MyRequired<Src>; // ^? { a: number; readonly b: string }
type M = MyMutable<Src>;  // ^? { a?: number; b: string }
```

Заметьте: `MyRequired<Src>` превращает `a?` в обязательное `a: number` (`?` снят), но оставляет `readonly b` readonly — `-?` касается только опциональности. `MyMutable` снимает `readonly` с `b`, но оставляет `a` опциональным. Модификаторы ортогональны.

## Гомоморфные против негомоморфных — тонкая, ключевая разница

Когда отображённый тип написан как `{ [K in keyof T]: ... }` — обходит *напрямую* `keyof T` одного параметра типа — он **гомоморфен**. Гомоморфные отображённые типы делают две тихие, но важные вещи: **сохраняют исходные модификаторы** (поэтому `Clone` выше оставил `readonly b`) и **сохраняют природу массива и кортежа**:

```ts
type Clone<T> = { [K in keyof T]: T[K] }; // гомоморфный

type CA = Clone<number[]>;       // ^? number[]        — всё ещё массив
type CT = Clone<[1, 2]>;         // ^? [1, 2]          — всё ещё кортеж
type CR = Clone<readonly string[]>; // ^? readonly string[]  — readonly сохранён
```

Теперь сравните отображённый тип, который обходит объединение ключей, полученное *отдельным* параметром — **негомоморфный**:

```ts
type FromKeys<K extends PropertyKey, V> = { [P in K]: V };

type Obj = FromKeys<"a" | "b", number>;
// ^? { a: number; b: number }     — нормально для ключей объекта
type Broken = FromKeys<keyof number[], unknown>;
// ^? обычный объект со строковыми ключами number[] ("length", "push", ...) — НЕ массив
```

`FromKeys` отображает по голому объединению ключей `K`, а не по `keyof T`, поэтому у TypeScript нет исходного контейнера для сохранения — результат всегда обычный объект. Именно поэтому `Record<K, V> = { [P in K]: V }` даёт объекты, а `Partial`/`Readonly` (написанные по `keyof T`) точно сохраняют массивы и кортежи.

<Infographic
  svg={homomorphismContrast}
  label="Сравнение двух форм отображённого типа бок о бок: гомоморфное отображение { [K in keyof T] } сохраняет модификаторы ?/readonly источника и форму массива/кортежа и питает Partial/Required/Readonly/Pick, тогда как негомоморфное отображение по оторванному объединению ключей { [P in K] } сбрасывает модификаторы, схлопывает массивы в обычные объекты и именно поэтому Record всегда даёт объект."
  caption="Тот же цикл по ключам, противоположные гарантии: только гомоморфная форма (ограничение в точности keyof T) сохраняет модификаторы и природу массива/кортежа; пропустите ключи через оторванное объединение — и теряете и то, и другое."
  hue="var(--d-infra)"
/>

<Inset kind="why" lang="ru">
TypeScript определяет гомоморфность синтаксически: ограничение отображённого типа должно быть в точности `keyof T` для одного параметра типа `T` (или сам `T`, когда `T` ограничен типом ключа). Увидев эту форму, он держит связь с источником `T`, чтобы копировать модификаторы `T` по ключам и особо обрабатывать массивы/кортежи — отображая каждый тип элемента и пересобирая тот же контейнер. Потеряйте эту точную форму `keyof T` — пропустив ключи через другой параметр или ремаппинг (следующий урок) — и связь исчезает, модификаторы теряются, а массивы схлопываются в обычные объекты.
</Inset>

<FlowDiagram
  label="Вычисление отображённого типа: обход ключей, преобразование каждого значения, сохранение модификаторов только при гомоморфности."
  hue="var(--accent)"
  perRow={3}
  nodes={[
    { id: "src", label: "Источник T", sub: "{ a?, readonly b }" },
    { id: "keys", label: "keyof T", sub: "объедин. ключей" },
    { id: "iter", label: "Для каждого K", sub: "выдать T[K]" },
    { id: "homo", label: "Огранич. = keyof T?", sub: "гомоморфно?" },
    { id: "keep", label: "Сохранить", sub: "?/readonly, массивы" },
    { id: "drop", label: "Обычный объект", sub: "модиф. потеряны" },
    { id: "out", label: "Новый тип", sub: "пересобран" },
  ]}
  edges={[
    { from: "src", to: "keys" },
    { from: "keys", to: "iter" },
    { from: "iter", to: "homo" },
    { from: "homo", to: "keep", label: "да" },
    { from: "homo", to: "drop", label: "нет" },
    { from: "keep", to: "out" },
    { from: "drop", to: "out" },
  ]}
  caption="Цикл по ключам одинаков в обоих случаях; только гомоморфное отображение (ограничение в точности keyof T) сохраняет модификаторы источника и форму массива/кортежа. Обход оторванного объединения ключей всегда даёт обычный объект."
/>

## Цена: отображаемые типы масштабируются с шириной объекта

Отображаемый тип инстанцирует по одному свойству на ключ, поэтому его стоимость линейна по числу ключей — обычно пренебрежима. Взрыв даёт *композиция*: `Partial<DeepReadonly<Pick<T, ...>>>` над сгенерированным типом из 200 полей заново материализует целый объект на каждом слое, а сложенное над объединением таких типов число свойств перемножается. Жёсткий потолок — те же лимиты объединения/объекта, что и везде: отображаемый тип, чьё выражение значения дистрибутирует условие над большим объединением, может упереться в стену `Expression produces a union type that is too complex to represent` на **100 000 членах**.

Senior-критерий: отображаемый тип окупается, когда преобразование должно применяться единообразно к *неизвестному или эволюционирующему* набору ключей — `Partial<Config>` остаётся верным, когда кто-то добавляет поле. Это *оверинжиниринг*, когда объект мал и фиксирован: написать опциональный двойник руками — две лишние строки, которые читатель понимает мгновенно, тогда как отображаемый тип заставляет его мысленно его развернуть. А правила модификаторов/гомоморфизма — реальный налог на сопровождение: следующий инженер должен знать, что прогон ключей через `as` или отдельный параметр молча сбрасывает `readonly` и схлопывает массивы (баги из этого урока). Когда форма всё равно генерируется из схемы (Prisma, GraphQL, OpenAPI), предпочтите плоский `interface` из **кодогенерации** обёртыванию его в три слоя отображаемых утилит; сгенерированный интерфейс можно навести и сравнить в diff, глубокая отображаемая башня — нет.

<Inset kind="warning" lang="ru">
Налог на ширину измерим. На реальном проекте сплошное обёртывание всей поверхности API в рекурсивный `DeepPartial`/`DeepReadonly` над сгенерированными типами — классический способ добавить 3–10с к `Check time` (видно в `tsc --extendedDiagnostics` как скачок `Instantiation count`) и заставить наведение на глубоко отображённый тип в редакторе тормозить секундами, потому что IntelliSense должен инстанцировать весь результат отображения, чтобы отрисовать подсказку. Лечение обычно не более хитрый тип, а *сужение области* — отображайте только те ключи, что действительно преобразуете, или пусть кодогенерация выдаст плоский тип один раз вместо пересчёта на каждой проверке.
</Inset>

<Quiz
  id="04-mapped-quiz-1"
  lessonSlug="03-mapped-types"
  lang="ru"
  question="Дано type Clone<T> = { [K in keyof T]: T[K] }. Чему равно Clone<readonly [1, 2]>?"
  choices={[
    { label: "readonly [1, 2] — Clone гомоморфен (ограничение keyof T), поэтому сохраняет модификатор readonly и природу кортежа.", correct: true },
    { label: "[1, 2] — отображение всегда сбрасывает readonly.", misconception: "Гомоморфный отображённый тип сохраняет модификаторы, включая readonly. readonly сохраняется; результат — readonly [1, 2]." },
    { label: "{ 0: 1; 1: 2; length: 2; ... } — кортеж схлопывается в обычный объект.", misconception: "Схлопывание в обычный объект происходит для НЕгомоморфных отображений по оторванному объединению ключей. Clone обходит keyof T, поэтому остаётся кортежем." },
    { label: "(1 | 2)[] — кортеж расширяется в массив объединения его элементов.", misconception: "Гомоморфное отображение пересобирает тот же контейнер поэлементно; оно не расширяет кортеж в массив. Результат остаётся readonly [1, 2]." },
  ]}
/>

<DragOrder
  id="04-mapped-drag-1"
  pieceSlug="03-mapped-types"
  lang="ru"
  prompt="Упорядочьте, как TypeScript вычисляет MyPartial<{ a: number; readonly b: string }>, где MyPartial<T> = { [K in keyof T]?: T[K] }:"
  items={[
    { id: "a", label: "Распознаёт, что ограничение это keyof T, значит это гомоморфный отображённый тип, связанный с источником" },
    { id: "b", label: "Вычисляет keyof T как объединение ключей 'a' | 'b'" },
    { id: "c", label: "Для каждого ключа копирует модификаторы источника (readonly у b) и выдаёт тип значения T[K]" },
    { id: "d", label: "Применяет добавленный модификатор ? к каждому ключу, давая { a?: number; readonly b?: string }" },
  ]}
/>

</Explanation>

<KeyTakeaway>
Отображённый тип `{ [K in keyof T]: F<T[K]> }` — это цикл по `keyof T`, который пересобирает объект ключ за ключом, причём `T[K]` даёт исходный тип значения. Хирургия модификаторов — `?`/`-?` для опциональности, `readonly`/`-readonly` для readonly — позволяет написать `Partial`, `Required`, `Readonly` и `Mutable` с нуля; модификаторы ортогональны. Ключевое свойство — **гомоморфность**: когда ограничение в точности `keyof T` одного параметра, отображение сохраняет модификаторы источника *и* его форму массива/кортежа (`Clone<readonly [1,2]>` остаётся `readonly [1, 2]`). Пропустите ключи через оторванное объединение (как `Record`) — и вы это теряете: результат всегда обычный объект со сброшенными модификаторами.
</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="04-mapped-retrieval"
  lang="ru"
  questions={[
    { q: "Что такое отображённый тип, что означает T[K] внутри него и как написать Stringify и Clone?", a: "Отображённый тип `{ [K in keyof T]: ... }` использует `in`, чтобы обойти объединение keyof T, выдавая по одному свойству на ключ — цикл, пересобирающий объект. Внутри T[K] — это индексный доступ, дающий исходный тип значения по ключу K. Stringify<T> = { [K in keyof T]: string } ставит каждому значению string (Stringify<{port:number}> — это {port:string}). Clone<T> = { [K in keyof T]: T[K] } воспроизводит тип, потому что ссылка на T[K] сохраняет каждый исходный тип значения. Разница между ними только в выражении значения справа от двоеточия; обход ключей идентичен." },
    { q: "Какие четыре модификатора отображения и что дают MyPartial/MyRequired/MyReadonly/MyMutable на { a?: number; readonly b: string }?", a: "Модификаторы: `?` (добавить опциональность), `-?` (снять опциональность), `readonly` (добавить readonly) и `-readonly` (снять readonly), пишутся перед значением (для ?) или перед скобкой ключа (для readonly). Они ортогональны — каждый касается только своего модификатора. На Src = { a?: number; readonly b: string }: MyPartial = { [K in keyof T]?: T[K] } даёт { a?: number; readonly b?: string } (добавляет ?, сохраняет readonly). MyRequired = { [K in keyof T]-?: T[K] } даёт { a: number; readonly b: string } (снимает ? с a, оставляет readonly). MyReadonly = { readonly [K in keyof T]: T[K] } делает оба readonly. MyMutable = { -readonly [K in keyof T]: T[K] } даёт { a?: number; b: string } (снимает readonly с b, оставляет a опциональным)." },
    { q: "Что делает отображённый тип гомоморфным, что сохраняет гомоморфность и почему Record даёт обычный объект?", a: "Отображённый тип гомоморфен, когда его ограничение в точности `keyof T` для одного параметра T (или `T`, когда T сам ограничен типом ключа). TypeScript определяет это синтаксически и держит связь с источником T, что позволяет ему (1) копировать модификаторы T по ключам и (2) особо обрабатывать массивы и кортежи, отображая каждый элемент и пересобирая тот же контейнер. Так Clone<T> = { [K in keyof T]: T[K] } отображает number[] в number[], [1,2] в [1,2], readonly string[] в readonly string[]. Тип вроде Record<K, V> = { [P in K]: V } обходит оторванное объединение ключей K, а не keyof T, поэтому нет исходного контейнера для сохранения — результат всегда обычный объект, а любые модификаторы/природа массива несвязанного типа исчезают. Потеряйте точную форму `keyof T` (через ремаппинг или пропущенный параметр) — и вы теряете гомоморфность." },
  ]}
/>

<Recap lang="ru">
Отображённые типы дали вам цикл — обойти `keyof T`, преобразовать каждое значение и переключить `?`/`readonly` — а гомоморфность это тихая гарантия, что цикл сохраняет модификаторы источника и форму контейнера. Дальше — **ремаппинг ключей через `as`** вскрывает сторону ключей этого цикла: вместо переиспользования каждого ключа без изменений вы вычисляете *новый* ключ на каждой итерации — переименовываете через `get${Capitalize<K>}` или вовсе отфильтровываете ключи, ремаппя их в `never`. Теперь, когда видите `Partial<Config>` в кодовой базе команды, вы точно знаете, какая строка за этим стоит — и почему добавление поля в конфиг никогда не ломает отображённый тип.
</Recap>
