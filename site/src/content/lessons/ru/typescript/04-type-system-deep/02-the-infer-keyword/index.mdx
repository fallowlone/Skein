---
concepts:
  - infer
  - conditional-types
  - pattern-matching-types
  - variance
  - constrained-infer
deepensInto:
  - "typescript/05-type-level-programming/01-recursion-in-types"
estMin: 16
lang: ru
lessonType: topic
level: middle
order: 2
prereqs:
  - "typescript/04-type-system-deep/01-conditional-types"
slug: "02-the-infer-keyword"
sources:
  - https://www.typescriptlang.org/docs/handbook/2/conditional-types.html#inferring-within-conditional-types
  - https://www.typescriptlang.org/docs/handbook/release-notes/typescript-4-7.html#extends-constraints-on-infer-type-variables
spiral:
  - type-level-programming
  - variance
status: ready
summary: "`infer` объявляет свежую типовую переменную в части extends условного типа, связанную с тем, что заполняет слот — движок за ReturnType, Awaited и кортежами. Одно имя infer в нескольких позициях объединяет ковариантных кандидатов, но пересекает контравариантных."
title: "Ключевое слово infer: сопоставить тип с образцом и захватить его кусок"
track: "typescript"
unit: "04-type-system-deep"
---

import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";
import Infographic from "~/components/diagram/Infographic.astro";
import recursionDepthCap from "./infographics/recursion-depth-cap.svg?raw";

<Hook>
Вы пишете `ReturnType<typeof fetchUser>`, чтобы получить возвращаемый тип функции, которую не писали, ни разу его не называя. Где-то в `lib.es5.d.ts` есть одна строка: `type ReturnType<T extends (...a: any) => any> = T extends (...a: any) => infer R ? R : any`. Этот `infer R` — весь трюк: это дыра в образце, которая говорит «сопоставь тип-функцию, и что бы ни стояло в слоте возврата — захвати как `R`». Единственная встроенная форма сопоставления с образцом в TypeScript.
</Hook>

К концу урока вы напишете `ReturnType`, `Awaited` и кортежный head/tail по памяти — и поймёте, почему одно и то же имя `infer` в двух позициях иногда даёт объединение, а иногда `never`.

<Crux>`infer X` в части `extends` условного типа захватывает то, что заполняет эту позицию в сопоставленном типе, для истинной ветки.</Crux>

<Explanation>

## Дыра в образце

Когда нужно достать тип, живущий *внутри* чужого типа — возвращаемое значение функции, разрешённое значение промиса, первый элемент кортежа — можно оборачивать и разворачивать вручную. Или пробить дыру в образце и дать TypeScript самому найти решение.

`infer` допустим только внутри части `extends` условного типа. Он объявляет *новую* типовую переменную прямо на месте и позволяет TypeScript решить её, сопоставляя структуру:

```ts
type ElementOf<T> = T extends (infer E)[] ? E : never;

type A = ElementOf<string[]>;       // ^? string
type B = ElementOf<[1, 2, 3]>;      // ^? 1 | 2 | 3   (тип элемента кортежа)
type C = ElementOf<number>;         // ^? never       (не массив — ложная ветка)
```

Вы написали форму `(infer E)[]` — «массив чего-то» — и TypeScript унифицирует её с входом, связывая `E` с этим «чем-то». `E` существует только в истинной ветке. Это та же механика, что использует стандартная библиотека:

```ts
type Unwrap<T> = T extends Promise<infer V> ? V : T;
type R1 = Unwrap<Promise<number>>;  // ^? number
type R2 = Unwrap<string>;           // ^? string  (ложная ветка возвращает T)

type Params<T> = T extends (...args: infer P) => unknown ? P : never;
type R3 = Params<(a: string, b: number) => void>; // ^? [a: string, b: number]
```

## Голова, хвост кортежа и несколько infer в одном образце

Вы можете разместить несколько переменных `infer` в одном образце и захватить разные куски одновременно:

```ts
type Head<T extends readonly unknown[]> =
  T extends [infer H, ...unknown[]] ? H : never;
type Tail<T extends readonly unknown[]> =
  T extends [unknown, ...infer Rest] ? Rest : never;

type H = Head<[1, 2, 3]>; // ^? 1
type Te = Tail<[1, 2, 3]>; // ^? [2, 3]

type Last<T extends readonly unknown[]> =
  T extends [...unknown[], infer L] ? L : never;
type L = Last<[1, 2, 3]>; // ^? 3
```

Вариативные позиции кортежа (`...infer Rest`) делают голову/хвост/последний тривиальными — и они строительный блок для рекурсивных списков на уровне типов, которые вы напишете в юните 05.

## Ограниченный infer (TS 4.7): `infer R extends ...`

До TS 4.7 приходилось захватывать, а затем перепроверять во вложенном условии. С 4.7 можно ограничить вывод прямо в строке, что одновременно валидирует и *сужает захваченный тип*:

```ts
// Захватываем строковый литерал-элемент и сужаем его до string за один шаг
type FirstString<T> =
  T extends [infer S extends string, ...unknown[]] ? S : never;

type S1 = FirstString<["hello", 1, 2]>; // ^? "hello"
type S2 = FirstString<[1, "x"]>;        // ^? never   (первый — не строка)
```

Практическая выгода: `infer N extends number` позволяет пронести числовой литерал без его расширения, что полезно в числовой арифметике типов.

## Подвох с вариантностью: одно имя, несколько позиций

Вот часть, где ошибаются. Когда **одно** имя `infer` встречается в **нескольких позициях**, TypeScript собирает всех кандидатов и комбинирует их — но *как* зависит от вариантности. В **ковариантных** позициях (значения свойств объекта, элементы массива, возвращаемые типы) кандидаты **объединяются**:

```ts
type Co<T> = T extends { a: infer U; b: infer U } ? U : never;
type X = Co<{ a: string; b: number }>;
// ^? string | number   — ковариантные позиции => ОБЪЕДИНЕНИЕ
```

В **контравариантных** позициях — **параметрах** функций — кандидаты **пересекаются**:

```ts
type Contra<T> =
  T extends { f: (x: infer U) => void; g: (x: infer U) => void } ? U : never;
type Y = Contra<{ f: (x: string) => void; g: (x: number) => void }>;
// ^? string & number   — контравариантные позиции => ПЕРЕСЕЧЕНИЕ  (здесь: never)
```

<Inset kind="why" lang="ru">
Правило вытекает из корректности. Захваченный `U` должен быть типом, который работает для *каждой* позиции, где он встретился. В ковариантной (выходной) позиции значение `string | number` приемлемо везде, где производилось любое из них — объединение является самым точным типом, покрывающим оба выхода. В контравариантной (входной) позиции функция, которая должна принять `string`, *и* функция, которая должна принять `number`, безопасно получают только значение, приемлемое для обеих — `string & number`. Поэтому проверщик объединяет выходы и пересекает входы; это та же логика вариантности из юнита 03, применённая к кандидатам вывода.
</Inset>

<FlowDiagram
  label="Комбинация кандидатов infer: как одно имя infer в нескольких позициях схлопывается в объединение или пересечение."
  hue="var(--accent)"
  perRow={3}
  nodes={[
    { id: "pat", label: "Образец с infer U", sub: ">=1 позиция" },
    { id: "match", label: "Унификация со входом", sub: "собрать U" },
    { id: "n", label: "Сколько слотов U?", sub: "кандидаты" },
    { id: "cov", label: "Ковариант. слоты", sub: "выход/значение" },
    { id: "con", label: "Контравар. слоты", sub: "параметр фн" },
    { id: "uni", label: "Объединение", sub: "A | B" },
    { id: "int", label: "Пересечение", sub: "A & B" },
  ]}
  edges={[
    { from: "pat", to: "match" },
    { from: "match", to: "n" },
    { from: "n", to: "cov", label: "значения" },
    { from: "n", to: "con", label: "параметры" },
    { from: "cov", to: "uni" },
    { from: "con", to: "int" },
  ]}
  caption="Одно имя infer в ковариантных позициях объединяет кандидатов (наименьший тип, покрывающий оба выхода); в контравариантных позициях-параметрах пересекает их (тип, безопасный для обоих входов)."
/>

## Цена: `infer` дёшев в написании, дорог в цепочке

Один `infer` почти бесплатен. Ловушка — *рекурсивный* `infer`: снятие кортежа по одной голове или разворачивание вложенных обёрток, потому что каждая рекурсия — это новое инстанцирование условия. TypeScript ограничивает нехвостовую рекурсию глубиной инстанцирования около **50**, прежде чем сдаться с `Type instantiation is excessively deep and possibly infinite`; хвосторекурсивные формы доходят дальше, примерно до **1000** уровней. Обход `infer` по кортежу из 2000 элементов упрётся в эту стену, и симптом — не падение, а молчаливо подставленный на дне рекурсии `any`, который затем отравляет каждый зависящий от него нижестоящий тип.

<Infographic
  svg={recursionDepthCap}
  label="Столбчатая диаграмма потолка рекурсии условных типов: нехвостовые рекурсивные цепочки infer останавливаются примерно на 50 уровнях инстанцирования, а хвосторекурсивные формы доходят примерно до 1000."
  caption="Переписывание рекурсивного infer в хвосторекурсивную форму даёт примерно в 20 раз больше глубины, прежде чем TypeScript молча подставит any — разница между стеной в 50 и стеной в ~1000 уровней."
  hue="var(--d-infra)"
/>

Senior-критерий: `infer` уместен, когда нужный кусок действительно *внутри* типа, которым владеет кто-то другой — библиотечный `ReturnType`, `Awaited` над неизвестным промисом. Он *неуместен*, когда тип контролируете вы и кусок можно просто назвать. `type UserId = User["id"]` — это индексированный доступ, разрешается мгновенно, тривиально наводится — против `type UserId = Extract<...infer...>`, который читатель должен мысленно выполнить. А когда вы пишете 30-строчный рекурсивный `infer`, разбирающий строку или глубоко вложенную форму, это сигнал, что систему типов просят выполнить работу парсера: маленький **шаг кодогенерации**, выдающий плоский именованный тип, компилируется за микросекунды и даёт коллегам читаемый артефакт, тогда как рекурсивный `infer` стоит секунд проверки и порождает нечитаемое сообщение об ошибке.

<Inset kind="warning" lang="ru">
Измеряйте, прежде чем доверять хитрому `infer`. Запустите `tsc --extendedDiagnostics` и следите за `Instantiation count` и `Check time`: невинный на вид рекурсивный `infer` способен загнать `Instantiation count` в миллионы и добавить 5–15с к холодной проверке на реальном проекте, а поскольку тот же checker питает редактор, IntelliSense на затронутом файле деградирует с мгновенного до многосекундного на нажатие. Один недоограниченный рекурсивный `infer` над большим объединением может также упереть вас в потолок объединения в **100 000 членов**, всплывая как `Expression produces a union type that is too complex to represent` — и тогда захваченный тип вырождается в ошибку/`any` вместо точного типа, который вы задумали.
</Inset>

<Quiz
  id="04-infer-quiz-1"
  lessonSlug="02-the-infer-keyword"
  lang="ru"
  question="Для type Contra<T> = T extends { f: (x: infer U) => void; g: (x: infer U) => void } ? U : never чему равно Contra<{ f: (x: string) => void; g: (x: number) => void }>?"
  choices={[
    { label: "string & number — два кандидата infer U в контравариантных позициях (параметрах), поэтому они пересекаются.", correct: true },
    { label: "string | number — несколько кандидатов infer всегда объединяются.", misconception: "Объединение даёт ковариантные позиции (выход/значение). Параметры функций контравариантны, поэтому кандидаты пересекаются в string & number." },
    { label: "string — используется только первое вхождение U.", misconception: "Оба вхождения дают кандидатов; TypeScript их комбинирует. В позиции параметра комбинация — это пересечение, а не «первый побеждает»." },
    { label: "unknown — конфликтующие кандидаты откатываются к unknown.", misconception: "Здесь нет отката к unknown. Контравариантные позиции пересекают кандидатов, давая string & number (что для этих примитивов оказывается never)." },
  ]}
/>

<DragOrder
  id="04-infer-drag-1"
  pieceSlug="02-the-infer-keyword"
  lang="ru"
  prompt="Упорядочьте, как TypeScript разрешает ReturnType<F> для F = (a: number) => string, при ReturnType<T> = T extends (...a: any) => infer R ? R : any:"
  items={[
    { id: "a", label: "Проверяет, совместим ли F с образцом-функцией (...a: any) => (что-то)" },
    { id: "b", label: "Унифицирует F с образцом, трактуя infer R как дыру в слоте возврата" },
    { id: "c", label: "Связывает R с сопоставленным возвращаемым типом, string" },
    { id: "d", label: "Берёт истинную ветку и возвращает R, поэтому ReturnType<F> — это string" },
  ]}
/>

</Explanation>

<KeyTakeaway>
`infer X` — это сопоставление с образцом в TypeScript: допустим только внутри части `extends` условного типа, он объявляет свежую переменную, связанную с тем, что заполняет этот слот в сопоставленном типе, и доступную в истинной ветке — движок за `ReturnType` (`infer R` в слоте возврата), `Awaited`/`Unwrap` (`Promise<infer V>`), `Parameters` (`(...args: infer P)`) и кортежным `[infer H, ...infer Rest]`. С TS 4.7 `infer R extends C` ограничивает и сужает захват в строке. Тонкое правило: когда **одно** имя infer встречается в нескольких позициях, **ковариантные** позиции (значения, возвраты) **объединяют** кандидатов, а **контравариантные** позиции-параметры **пересекают** их — выходы объединяются, входы пересекаются, прямо из вариантности.
</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="04-infer-retrieval"
  lang="ru"
  questions={[
    { q: "Где допустим `infer`, что он делает и как написать с ним ElementOf и ReturnType?", a: "`infer X` допустим только внутри части extends условного типа. Он объявляет совершенно новую типовую переменную на месте и просит TypeScript решить её, структурно унифицируя образец с проверяемым типом; X связывается с тем, что заполняет этот слот, и доступен только в истинной ветке. ElementOf<T> = T extends (infer E)[] ? E : never захватывает тип элемента массива (для кортежа это объединение типов элементов, например ElementOf<[1,2,3]> — это 1 | 2 | 3). ReturnType<T> = T extends (...a: any) => infer R ? R : any сопоставляет любую форму функции и захватывает то, что в позиции возврата, как R. В стиле Awaited: Unwrap<T> = T extends Promise<infer V> ? V : T. Parameters<T> = T extends (...args: infer P) => unknown ? P : never захватывает список параметров как кортеж." },
    { q: "Когда одно имя infer встречается в нескольких позициях, когда получается объединение, а когда пересечение, и почему?", a: "Это зависит от вариантности. Ковариантные позиции — значения свойств объекта, элементы массива/кортежа, ВОЗВРАЩАЕМЫЕ типы функций — объединяют кандидатов. Так Co<T> = T extends { a: infer U; b: infer U } ? U : never даёт Co<{a:string; b:number}> = string | number. Контравариантные позиции — ПАРАМЕТРЫ функций — пересекают кандидатов. Так Contra<T> = T extends { f:(x: infer U)=>void; g:(x: infer U)=>void } ? U : never даёт Contra<{f:(x:string)=>void; g:(x:number)=>void}> = string & number (что для этих примитивов — never). Причина — корректность: захваченный U должен работать для каждой позиции. Для выходов объединение — самый точный тип, покрывающий оба производимых значения; для входов безопасно только значение, приемлемое для обоих потребителей, то есть пересечение. Выходы объединяются, входы пересекаются — то же правило вариантности, что и контравариантность параметров." },
    { q: "Что TS 4.7 добавил с ограниченным infer и почему это больше, чем синтаксический сахар?", a: "TS 4.7 разрешает `infer R extends C` прямо внутри части extends. До него, чтобы потребовать соответствия захваченного типа ограничению, приходилось сначала захватывать, а потом перепроверять во вложенном условии. Помимо краткости, ограниченный infer также СУЖАЕТ захваченный тип до ограничения: FirstString<T> = T extends [infer S extends string, ...unknown[]] ? S : never возвращает точный первый элемент, только если он строка ('hello' для ['hello',1,2], never для [1,'x']). Частое применение — `infer N extends number`, который проносит числовой литерал через арифметику типов без его расширения до number — это важно, когда вы строите счётчики и арифметику на уровне типов в юните 05." },
  ]}
/>

<Recap lang="ru">
`infer` превратил условие из проверки «да/нет» в инструмент захвата — теперь вы можете достать элемент из массива, значение из Promise, голову и хвост из кортежа, и вы знаете, что повтор имени infer объединяет выходы, но пересекает входы. Дальше — **отображённые типы** перестают сопоставлять одну форму с образцом и начинают её *обходить*: `{ [K in keyof T]: ... }` проходит по каждому ключу типа, чтобы преобразовать его значения, с хирургией модификаторов (`?`, `readonly`) и гомоморфным свойством, которое незаметно сохраняет массивы и кортежи. Теперь, когда встречаете `ReturnType`, `Awaited` или любой тип, тянущийся внутрь чужой библиотеки, — вы узнаёте дыру `infer` и сразу понимаете, почему в одних позициях результат объединяется, а в других — пересекается.
</Recap>
