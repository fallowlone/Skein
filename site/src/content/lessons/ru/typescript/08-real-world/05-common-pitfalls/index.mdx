---
concepts:
  - type-pitfalls
  - branding
  - structural-typing
  - object-keys
  - array-covariance
deepensInto: []
estMin: 15
lang: ru
lessonType: topic
level: senior
order: 5
prereqs:
  - "typescript/03-generics/05-variance-intuition"
slug: "05-common-pitfalls"
sources:
  - https://www.typescriptlang.org/docs/handbook/declaration-files/do-s-and-don-ts.html
  - https://www.typescriptlang.org/docs/handbook/2/everyday-types.html
spiral:
  - structural-typing
  - variance
  - narrowing
status: ready
summary: "Сводка senior-ловушек: `as`/`!` прячут баги, утечки `any`, структурная типизация пропускает не те объекты, `Object.keys` возвращает `string[]`, ковариантность массивов, мутация ломает сужение, забытый `as const`. Для каждой: симптом, почему компилируется, фикс."
title: "Частые ловушки: то, что компилируется чисто и кусает позже"
track: "typescript"
unit: "08-real-world"
---

import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";

<Hook>
У каждой ловушки этого урока есть общее: она **компилируется чисто**. Сборка зелёная, PR одобрен, типы выглядят непробиваемо — а потом `userId` передаётся туда, где ждали `orderId`, суженное значение оказывается `undefined` двумя строками позже, или `Object.keys` отдаёт переменную цикла, которой нельзя индексировать. Это баги, что переживают ревью именно потому, что проверщик типов сказал «да». Преимущество senior'а не в знании большего синтаксиса; оно в знании, где «да» проверщика *несостоятельно* или *неточно*, и в рефлекторном выборе нужного фикса.
</Hook>

<Crux>Опасные баги TypeScript — те, что компилируются: несостоятельные по дизайну, неточные по умолчанию или спрятанные за люком.</Crux>

<Explanation>

## Сводная таблица

Каждая строка — ловушка, проходящая компилятор. Читайте как: симптом, почему проскакивает и рефлекторный фикс.

<div data-lesson-visual class="overflow-x-auto my-6">

| Ловушка | Почему компилируется | Фикс |
| --- | --- | --- |
| `as` прячет реальное несовпадение | Каст утверждает тип без рантайм-проверки; компилятор вам верит | Валидируйте на границах (zod); берегите `as` для знания, которого реально нет у компилятора |
| Злоупотребление `!` (non-null) | `!` стирает `null`/`undefined` из типа без доказательства | Сужайте реальной проверкой (`if (x)`) или почините тип, чтобы он не включал null |
| Утечка `any` из библиотеки | `any` присваивается во все стороны; распространяется тихо | Типизируйте границы библиотек как `unknown` и валидируйте; включите `noImplicitAny` |
| Структурная типизация пропускает не те ID | `UserId` и `OrderId` — оба `string` — структурно идентичны | Забрендируйте тип: `string & { __brand: "UserId" }` |
| `Object.keys(o)` — это `string[]` | В рантайме могут быть лишние ключи, так что `keyof` был бы несостоятелен | Кастуйте намеренно: `Object.keys(o) as (keyof typeof o)[]` (риск принят вами) |
| Несостоятельность ковариантности массивов | `Dog[]` присваивается `Animal[]`, но push `Cat` затем ломает | Принимайте `readonly T[]` в API, чтобы вызывающие не могли мутировать |
| Мутация ломает сужение | Суженный `let`/свойство может измениться между проверкой и использованием | Используйте `const`, копируйте в локальную или перепроверяйте после вызова, способного мутировать |
| Забытый `as const` | Без него `{ x: 1 }` расширяется до `{ x: number }`, литералы до базового типа | Добавьте `as const`, чтобы сохранить литеральные типы и `readonly`-кортежи |

</div>

## Брендинг: намеренная победа над структурной типизацией

Раздел 01 учил, что TypeScript структурен — два типа одной формы взаимозаменяемы. Обычно это фича; здесь — опасность. `type UserId = string` и `type OrderId = string` — это *один и тот же тип*, так что передача одного туда, где ждут другой, компилируется. Брендинг добавляет фантомное свойство, существующее только в типе:

```ts
type UserId = string & { readonly __brand: "UserId" };
type OrderId = string & { readonly __brand: "OrderId" };

function getUser(id: UserId) { /* ... */ }
const order = "ord_1" as OrderId;

// getUser(order); // Error: 'OrderId' is not assignable to 'UserId'
//   __brand различает их, хотя оба — string в рантайме
const user = "usr_1" as UserId;
getUser(user); // ok
```

`__brand` никогда не существует в рантайме (под ним `string`), но *тип* теперь различает два, так что перепутанный ID — ошибка компиляции. Это стандартный фикс для «структурная типизация пропустила не тот примитив».

## Ковариантность массивов: каноническая несостоятельность

TypeScript намеренно делает массивы **ковариантными** — `Dog[]` присваивается `Animal[]` — ради эргономики. Но массивы изменяемы, что делает это несостоятельным:

```ts
const dogs: Dog[] = [new Dog()];
const animals: Animal[] = dogs; // разрешено — ковариантность
animals.push(new Cat());        // компилируется! Animal[] принимает Cat
dogs[1].bark();                 // падение в рантайме — это Cat, нет bark()
```

Компилятор принял каждую строку, а `dogs` теперь содержит `Cat`. Фикс в дизайне API: принимайте `readonly Animal[]`, когда только читаете, чтобы вызывающий не мог протащить `Cat` в чужой `Dog[]`.

<Inset kind="why" lang="ru">
Почему TS выбрал несостоятельное правило? Чистая состоятельность запретила бы `Dog[]` там, где ждут `Animal[]`, сломав огромный объём разумного read-only-кода (вы передаёте `Dog[]` функции, которая просто итерирует `Animal[]`). Команда разменяла узкую несостоятельность — срабатывающую только при *мутации через расширенную ссылку* — на повседневную эргономику. `readonly`-массивы восстанавливают состоятельность ровно тем, что убирают ломающую мутацию.
</Inset>

## Мутация ломает сужение

Сужение (раздел 02) валидно лишь пока значение не может измениться. Изменяемая привязка или свойство, суженные в `if`, могут быть сброшены любым промежуточным вызовом, сквозь который компилятор не видит:

```ts
function f(box: { v: string | null }) {
  if (box.v !== null) {
    sideEffect();        // мог сделать box.v = null — компилятор считает, что нет
    box.v.toUpperCase(); // ^? string  — но может упасть в рантайме
  }
}
```

Компилятор сужает `box.v` до `string` внутри `if` и *сохраняет* это допущение сквозь `sideEffect()`, потому что не может доказать, что вызов не тронул `box.v`. Фикс: скопируйте в `const`-локальную сразу после проверки (`const v = box.v;`), чтобы суженное значение не вымутировали из-под вас.

<Quiz
  id="08-pit-quiz-1"
  lessonSlug="05-common-pitfalls"
  lang="ru"
  question="`const animals: Animal[] = dogs` (где dogs — Dog[]) компилируется, затем `animals.push(new Cat())` тоже компилируется и портит dogs. Почему TypeScript это разрешает?"
  choices={[
    { label: "Массивы ковариантны по дизайну — Dog[] присваивается Animal[] ради эргономики — и это правило намеренно несостоятельно при мутации; readonly-массивы это чинят.", correct: true },
    { label: "Это баг TypeScript, который починят в будущем релизе.", misconception: "Это намеренный, задокументированный размен: полная состоятельность отвергла бы слишком много разумного read-only-кода. Выход — принимать readonly T[], когда только читаете." },
    { label: "Dog и Cat структурно идентичны, так что push на самом деле типобезопасен.", misconception: "Это разные типы; push реально небезопасен. Он компилируется лишь потому, что массив расширен до Animal[] через ковариантность, что разрешает push любого Animal." },
    { label: "push отвергается на этапе компиляции, так что реальной проблемы нет.", misconception: "Он компилируется. animals имеет тип Animal[], а Cat — это Animal, так что push разрешён — но тот же подлежащий массив — это dogs, который теперь держит Cat." },
  ]}
/>

<DragOrder
  id="08-pit-drag-1"
  pieceSlug="05-common-pitfalls"
  lang="ru"
  prompt="Расставьте рабочий процесс от диагноза к фиксу для бага, что скомпилировался чисто, но упал в рантайме:"
  items={[
    { id: "a", label: "Симптом: падение в рантайме на коде, одобренном проверщиком типов" },
    { id: "b", label: "Найти несостоятельное или неточное место: люк as/!, ковариантность, мутацию или структурное столкновение" },
    { id: "c", label: "Объяснить ПОЧЕМУ скомпилировалось — какую гарантию TS отдал (или никогда не имел) на той строке" },
    { id: "d", label: "Применить рефлекторный фикс: валидировать, забрендировать, использовать readonly/const или добавить as const" },
    { id: "e", label: "Добавить guardrail (lint-правило, более строгий флаг), чтобы тот же класс багов не повторился" },
  ]}
/>

</Explanation>

<KeyTakeaway>
Баги TypeScript, что кусают реальные кодовые базы, — те, что **компилируются чисто**: они несостоятельны по дизайну, неточны по умолчанию или спрятаны за люком. `as` и `!` утверждают типы без доказательства (валидируйте на границах; сужайте реальными проверками); `any` из библиотеки распространяется тихо (типизируйте границы как `unknown`). **Структурная типизация** считает `UserId` и `OrderId` (оба `string`) взаимозаменяемыми — **забрендируйте** их фантомным `& { __brand }`, чтобы различать на этапе компиляции, оставаясь `string` в рантайме. **`Object.keys` возвращает `string[]`**, не `keyof`, потому что в рантайме могут быть лишние ключи; кастуйте намеренно, если принимаете риск. **Ковариантность массивов** (`Dog[]` присваивается `Animal[]`) — намеренная несостоятельность, ломающаяся при `push` — принимайте `readonly T[]` в read-only-API. **Мутация ломает сужение**: копируйте суженное значение в `const`-локальную перед любым вызовом, способным его изменить. А забытый **`as const`** тихо расширяет литералы. Для каждой: знайте симптом, знайте *почему* скомпилировалось и тянитесь к фиксу рефлекторно.
</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="08-pit-retrieval"
  lang="ru"
  questions={[
    { q: "Зачем нужен брендинг и как `string & { __brand: 'UserId' }` работает, не влияя на рантайм?", a: "TypeScript структурен: два типа одной формы взаимозаменяемы. Так что `type UserId = string` и `type OrderId = string` — буквально один и тот же тип, и передача UserId туда, где ждут OrderId, компилируется — реальный баг, который проверщик не ловит. Брендинг пересекает примитив с фантомным свойством, существующим только в системе типов: `type UserId = string & { readonly __brand: 'UserId' }`. Пересечение делает UserId НЕприсваиваемым OrderId (их литералы __brand различаются), так что перепутанный ID — ошибка компиляции. В рантайме значение — просто строка: свойство __brand никогда не создаётся; вы чеканите брендированное значение намеренным кастом ('usr_1' as UserId) в единственном месте, где ему доверяете. Это стандартный фикс структурных столкновений примитивов (ID, единицы, токены)." },
    { q: "Объясните несостоятельность ковариантности массивов и фикс через readonly.", a: "TypeScript намеренно делает массивы ковариантными: Dog[] присваивается Animal[] ради эргономики (вы хотите передать Dog[] функции, итерирующей Animal[]). Но массивы изменяемы, поэтому это несостоятельно: const animals: Animal[] = dogs; animals.push(new Cat()); компилируется — animals это Animal[], а Cat это Animal — но animals и dogs это ОДИН подлежащий массив, так что dogs теперь содержит Cat, и dogs[1].bark() падает. Каждая строка прошла проверщик. Команда приняла эту узкую несостоятельность (срабатывающую только при мутации через расширенную ссылку) ради повседневного удобства. Фикс в дизайне API — принимать readonly Animal[], когда только читаете: у readonly-массива нет push, так что вызывающий не протащит Cat в чужой Dog[], восстанавливая состоятельность ровно там, где она потеряна." },
    { q: "Почему мутация может сломать сужение, каков рефлекторный фикс и почему Object.keys типизирован как string[]?", a: "Сужение держится лишь пока значение не может измениться. Если вы сузили box.v до string в if, затем вызвали sideEffect(), компилятор СОХРАНЯЕТ допущение string сквозь вызов, потому что не может доказать, что вызов не тронул box.v — но sideEffect() мог сделать box.v = null, так что box.v.toUpperCase() может упасть в рантайме, несмотря на компиляцию. Рефлекторный фикс: сразу скопировать суженное значение в const-локальную (const v = box.v), чтобы ничто не вымутировало его из-под сужения; или перепроверить после любого вызова, способного мутировать. Отдельно: Object.keys(o) типизирован как string[], а не (keyof typeof o)[], намеренно: в рантайме у объекта могут быть лишние собственные перечислимые ключи сверх его статического типа (он мог быть более широким объектом, присвоенным более узкому типу), так что трактовать ключи как keyof было бы несостоятельно. Если вы знаете, что у объекта ровно его объявленные ключи, кастуйте намеренно: Object.keys(o) as (keyof typeof o)[], принимая риск." },
  ]}
/>

<Recap lang="ru">
Теперь вы называете senior-ловушки с ходу — люки `as`/`!`, утечки `any`, структурные столкновения ID, расширение `Object.keys`, ковариантность массивов, сломанное мутацией сужение, забытый `as const` — и для каждой знаете симптом, почему скомпилировалось и рефлекторный фикс (валидировать, забрендировать, `readonly`/`const`, намеренные касты). Это закрывает раздел «реальный мир»: вы умеете типизировать грязные границы реальных систем, безопасно потреблять их через zod и tRPC, проектировать API библиотек, что хорошо выводятся, и замечать ловушки, что компилируются чисто. Всё сходится в капстоуне типизированной фичи в разделе 09, где вы строите одну сквозную фичу, применяя всё это сразу. Теперь, когда в ревью видите зелёную сборку на PR, где голая `string` ID попадает в функцию, ждущую другого вида ID, вы знаете структурное столкновение за этим — и тянетесь к бренду, прежде чем одобрить.
</Recap>
