---
concepts:
  - zod
  - runtime-validation
  - schema-first
  - z-infer
  - safeparse
deepensInto:
  - "typescript/08-real-world/03-trpc-end-to-end"
estMin: 15
lang: ru
lessonType: topic
level: middle
order: 2
prereqs:
  - "typescript/08-real-world/01-typing-apis"
  - "typescript/06-functions-this/03-assertion-functions"
slug: "02-zod-and-validation"
sources:
  - https://zod.dev/
  - https://www.typescriptlang.org/docs/handbook/2/conditional-types.html
spiral:
  - narrowing
  - single-source-of-truth
  - api-contracts
status: ready
summary: "zod — валидация по схеме: одна схема, статический тип через `z.infer`, а `.parse`/`.safeParse` проверяют байты в рантайме и сужают. Тип и страж рождаются из одного определения и не разъедутся — никогда не пишите руками и тип, и схему."
title: "zod: одна схема, статический тип и рантайм-страж"
track: "typescript"
unit: "08-real-world"
---

import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
Прошлый урок закончился незакрытым пробелом: `JSON.parse(...) as T` — всё ещё ложь. Можно написать type guard руками — `function isUser(x: unknown): x is User { return typeof x === "object" && x !== null && typeof (x as any).id === "number" && ... }` — но теперь вы сопровождаете две вещи, которые обязаны совпадать: интерфейс `User` и страж. Добавьте поле в интерфейс, забудьте про страж — и вы снова на пейджере в 02:00: тип говорит, что поле есть, страж его не проверяет, каст снова лжёт. Фикс — сделать тип и проверку *одним и тем же артефактом*. Этот артефакт — zod-схема.
</Hook>

<Crux>Определите zod-схему один раз: `z.infer` выводит тип, а `.parse` валидирует значение — один источник, поэтому тип и страж не разъедутся.</Crux>

<Explanation>

## Одна схема, два выхода

Как не дать типу и стражу разъехаться? Ответ — производить оба из одного определения: редактируете одно, и оба обновляются атомарно. Этим единственным определением и является zod-схема.

zod-схема — это рантайм-значение (объект с методом `.parse`), которое *вдобавок* несёт достаточно информации о типе, чтобы `z.infer` извлёк статический тип. Форму вы пишете один раз:

```ts
import { z } from "zod";

const User = z.object({
  id: z.number(),
  name: z.string(),
  email: z.string().email(),
  profile: z.object({ bio: z.string() }).nullable(),
});

type User = z.infer<typeof User>;
// ^? { id: number; name: string; email: string; profile: { bio: string } | null }
```

*Тип* `User` и *схема* `User` теперь выведены из одного определения. Второй вещи, которую надо синхронизировать, нет. Добавление `z.string()` для нового поля обновляет и рантайм-проверку, и выведенный тип одной правкой — расхождение структурно невозможно.

<Inset kind="why" lang="ru">
Это прямо чинит граничную ложь из урока 1. Там аннотация типа была обещанием, которое компилятор не мог проверить. Здесь *та же* схема, что произвела тип, ещё и работает в рантайме против фактических байтов — так что аннотация больше не обещание, а выход проверки. Тип истинен, потому что единственный способ получить типизированный `User` — пройти `.parse`.
</Inset>

## `.parse` бросает; `.safeParse` возвращает размеченное объединение

Два способа запустить проверку на границе:

```ts
const res = await fetch("/api/user/42");
const json: unknown = await res.json(); // типизируем как unknown, не any

// .parse — бросает ZodError при отказе
const user = User.parse(json); // ^? User  (или бросает)

// .safeParse — возвращает результат, который надо сузить
const result = User.safeParse(json);
if (result.success) {
  result.data; // ^? User  — сужено, валидировано
} else {
  result.error; // ^? ZodError  — пути + сообщения каждого отказа
}
```

`.safeParse` возвращает ровно то размеченное объединение, что вы строили руками в уроке 1, — `{ success: true; data: User } | { success: false; error: ZodError }` — только теперь ветка `data` *заработана* настоящей рантайм-проверкой, а не утверждена. Используйте `.parse` на границах доверия, где хотите громкого падения (серверный маршрут, валидирующий своё же чтение из БД), и `.safeParse` там, где плохое значение — ожидаемый исход, который вы рендерите (ввод формы, сторонний webhook).

Учтите: `z.infer` возвращает **выходной** (output) тип. С трансформами вход и выход различаются; увидим это ниже.

<FlowDiagram
  label="zod единый источник истины: одна схема питает и z.infer (этап компиляции), и parse (рантайм)"
  hue="var(--d-frontend)"
  perRow={3}
  nodes={[
    { id: "schema", label: "z.object schema", sub: "napisana raz" },
    { id: "infer", label: "z.infer<typeof S>", sub: "compile time" },
    { id: "type", label: "static type", sub: "dlya vashego koda" },
    { id: "parse", label: "S.parse(bytes)", sub: "runtime" },
    { id: "value", label: "validated value", sub: "suzheno do tipa" },
  ]}
  edges={[
    { from: "schema", to: "infer", label: "derive" },
    { from: "infer", to: "type" },
    { from: "schema", to: "parse", label: "run" },
    { from: "parse", to: "value", label: "on success" },
  ]}
  caption="Стрелка этапа компиляции (z.infer) и стрелка рантайма (parse) выходят из ОДНОГО узла-схемы. Тип и страж не могут разойтись, потому что у них один исток."
/>

## Композиция: вложенность, объединения, размеченные объединения

Схемы компонуются как типы, которые они отражают. Стройте большие формы из маленьких и моделируйте контракт провода успех/ошибка через `discriminatedUnion`:

```ts
const ApiResponse = z.discriminatedUnion("status", [
  z.object({ status: z.literal("ok"), data: User }),
  z.object({ status: z.literal("error"), code: z.number(), message: z.string() }),
]);

type ApiResponse = z.infer<typeof ApiResponse>;
// ^? { status: "ok"; data: User } | { status: "error"; code: number; message: string }

const parsed = ApiResponse.parse(json);
if (parsed.status === "ok") parsed.data; // ^? User
```

`z.discriminatedUnion` быстрее и даёт лучшие ошибки, чем обычный `z.union`, потому что сначала читает дискриминант и валидирует только подходящий член — рантайм-зеркало сужения размеченных объединений, изученного в разделе 02.

## Трансформы и уточнения: где вход и выход расходятся

Схема может больше, чем проверять — она может *переформировывать* и *навязывать бизнес-правила*:

```ts
const DateFromString = z.string().transform((s) => new Date(s));
type In = z.input<typeof DateFromString>;  // ^? string
type Out = z.output<typeof DateFromString>; // ^? Date  (z.infer === output)

const Password = z
  .object({ pw: z.string().min(8), confirm: z.string() })
  .refine((o) => o.pw === o.confirm, { message: "passwords must match", path: ["confirm"] });
```

`transform` означает, что значение, которое вы разбираете *на входе* (строка), отличается от значения, которое получаете *на выходе* (`Date`) — поэтому `z.infer` определён как **выходной** тип, а `z.input` существует для формы до трансформа. `refine` добавляет предикат, который статический тип выразить не может («эти два поля равны»), и привязывает отказ к `path` поля для отображения в форме (именно так вы подключаете ошибки на уровне полей в работе с формами [трека Frontend](/ru/learn/frontend)).

<Quiz
  id="08-zod-quiz-1"
  lessonSlug="02-zod-and-validation"
  lang="ru"
  question="Почему выводить `type User = z.infer<typeof User>` лучше, чем писать интерфейс `User` И zod-схему отдельно?"
  choices={[
    { label: "Есть один источник истины — добавление поля обновляет тип и рантайм-проверку вместе, так что они не разъедутся.", correct: true },
    { label: "z.infer делает рантайм-валидацию быстрее, чем рукописный интерфейс.", misconception: "z.infer — это чистое извлечение типа на этапе компиляции; на скорость рантайма оно не влияет. Выигрыш — в отсутствии расхождения между рукописным типом и схемой." },
    { label: "Писать оба нормально, пока вы держите их идентичными руками.", misconception: "Это ровно ловушка расхождения: поле, добавленное в одно и забытое в другом, воссоздаёт граничную ложь. Выводите тип из схемы, чтобы держать идентичным было нечего." },
    { label: "z.infer позволяет не вызывать .parse в рантайме.", misconception: "z.infer даёт только статический тип. На границе всё равно нужно вызвать .parse или .safeParse; один тип ничего не доказывает о байтах." },
  ]}
/>

<DragOrder
  id="08-zod-drag-1"
  pieceSlug="02-zod-and-validation"
  lang="ru"
  prompt="Расставьте поток «схема-первая» от определения до типизированного, провалидированного значения:"
  items={[
    { id: "a", label: "Определить zod-схему один раз (z.object, z.discriminatedUnion, уточнения)" },
    { id: "b", label: "Вывести статический тип: type T = z.infer<typeof Schema>" },
    { id: "c", label: "На границе типизировать сырой ввод как unknown и вызвать Schema.safeParse(input)" },
    { id: "d", label: "Сузить по result.success: data — провалидированное значение типа T; иначе читать result.error" },
  ]}
/>

</Explanation>

<KeyTakeaway>
zod — это подход **схема-первая**: вы пишете одну схему, `z.infer<typeof Schema>` выводит статический тип, а `.parse`/`.safeParse` валидируют фактические байты в рантайме и **сужают**. Это правильный фикс граничной лжи — тип и рантайм-страж рождаются из одного артефакта и потому **не могут разъехаться**; никогда не пишите руками и тип, и схему. `.parse` бросает `ZodError`; `.safeParse` возвращает размеченное объединение `{ success: true; data } | { success: false; error }`, где ветка `data` *заработана* проверкой, а не утверждена. Компонуйте вложенностью, `z.union` и более быстрым `z.discriminatedUnion`; переформировывайте через `transform` (из-за чего `z.input` отличается от `z.output`, а `z.infer` равен **output**) и навязывайте межполевые правила через `refine` (привязывая ошибки к `path` для форм). Валидируйте там, где это важно — большие нагрузки стоят реального времени, поэтому валидируйте на границах доверия, а не на каждом объекте горячего пути.
</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="08-zod-retrieval"
  lang="ru"
  questions={[
    { q: "Что значит, что zod-схема — «единый источник истины», и как это чинит граничную ложь из урока 1?", a: "zod-схема — это рантайм-значение (у неё есть метод .parse), которое вдобавок несёт достаточно информации о типе, чтобы z.infer<typeof Schema> извлёк статический тип. Так одно определение производит И статический тип для кода, И рантайм-страж, работающий на границе. Второго артефакта для синхронизации нет, поэтому расхождение структурно невозможно — добавление z.string() для нового поля обновляет тип и проверку одной правкой. Это чинит граничную ложь, потому что единственный способ получить типизированное значение — пройти .parse: аннотация больше не непроверенное обещание, а выход проверки против реальных байтов. Если писать руками И интерфейс, И отдельную схему (или отдельный type guard), пробел снова открывается — забудете обновить одно, и тип заявляет поле, которое рантайм не проверяет." },
    { q: "Сравните .parse и .safeParse, включая тип, который даёт каждый.", a: ".parse(input) возвращает провалидированное значение типа z.infer<Schema> при успехе и БРОСАЕТ ZodError при отказе. .safeParse(input) никогда не бросает; он возвращает размеченное объединение { success: true; data: T } | { success: false; error: ZodError }, которое надо сузить по result.success перед доступом к data или error. Оба запускают одну и ту же рантайм-валидацию. Используйте .parse на границах, где плохое значение — это баг, на котором хочется громко упасть (например, сервер валидирует своё же чтение из БД), и .safeParse там, где невалидные данные — ожидаемый, отображаемый исход (ввод формы, сторонние webhook'и). В любом случае типизированное значение заработано проверкой, а не утверждено кастом." },
    { q: "Почему z.infer возвращает ВЫХОДНОЙ тип, и где тут трансформы и уточнения?", a: "Схема может нести трансформ, например z.string().transform(s => new Date(s)): значение НА ВХОДЕ — строка, но значение НА ВЫХОДЕ — Date. zod различает z.input (форма до трансформа) и z.output (форма после трансформа), и z.infer ОПРЕДЕЛЁН как z.output — потому что разобранное значение, которое вы реально держите, и есть выход. refine добавляет предикат, который статический тип выразить не может (например, 'pw равно confirm'), и позволяет привязать отказ к path поля — так вы показываете ошибки формы на уровне поля. discriminatedUnion('status', [...]) — рантайм-зеркало сужения размеченных объединений: он сначала читает дискриминант и валидирует только подходящий член, поэтому быстрее и даёт более понятные ошибки, чем обычный z.union. Оговорка: валидация очень больших нагрузок стоит реального времени рантайма, поэтому валидируйте на границах доверия, а не на каждом объекте горячего пути." },
  ]}
/>

<Recap lang="ru">
Теперь вы делаете тип и рантайм-страж одним артефактом: определяете zod-схему, выводите тип через `z.infer`, а `.parse`/`.safeParse` превращают непроверенные байты в суженное провалидированное значение. Вы компонуете схемы, моделируете контракты провода через `discriminatedUnion` и переформировываете трансформами и уточнениями. zod закрывает граничную ложь по *одному* эндпоинту за раз. Дальше **tRPC** масштабирует идею на всю границу клиент/сервер сразу: вместо валидации каждого ответа клиент *выводит* весь тип своего API прямо из типа роутера сервера — сквозная типобезопасность без кодогенерации, построенная на той же машинерии вывода, что вы изучали. Теперь, когда встретите рядом рукописный `interface` и отдельный страж `isX`, вы знаете: это две вещи, которые рано или поздно разъедутся — и знаете, как слить их в одну схему.
</Recap>
