---
concepts:
  - trpc
  - end-to-end-types
  - typeof-types
  - inference-cost
  - import-type
deepensInto:
  - "typescript/09-putting-it-together/01-capstone-typed-feature"
estMin: 15
lang: ru
lessonType: topic
level: senior
order: 3
prereqs:
  - "typescript/08-real-world/02-zod-and-validation"
  - "apis/01-rest-modeling/01-overview"
slug: "03-trpc-end-to-end"
sources:
  - https://trpc.io/docs
  - https://www.typescriptlang.org/docs/handbook/2/typeof-types.html
spiral:
  - inference
  - single-source-of-truth
  - api-contracts
status: ready
summary: "tRPC даёт сквозную типобезопасность без кодогена: клиент выводит весь тип API из `typeof appRouter`, и типы текут через провод на этапе компиляции. «Без кодогена» значит, что клиент и сервер делят тип (`import type`); цена — время вывода и лаги редактора."
title: "tRPC: сквозные типы без кодогенерации и чего это стоит"
track: "typescript"
unit: "08-real-world"
---

import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import SequenceDiagram from "~/components/diagram/SequenceDiagram.astro";
import Infographic from "~/components/diagram/Infographic.astro";
import codegenTradeoff from "./infographics/codegen-tradeoff.svg?raw";

<Hook>
Вы переименовываете серверную процедуру с `getUser` на `getUserById` и добавляете аргумент `includeDeleted: boolean`. Жмёте «сохранить». Ещё не трогая клиент, три места вызова в двух фронтенд-файлах загораются красным: `Property 'getUser' does not exist`, а единственное место, что её вызывало, теперь без нового аргумента. Кодогенерация не запускалась. OpenAPI-файл не перегенерировался. Никакого `npm run gen`. Клиент просто *знал* — потому что тип API клиента не отдельная копия, а вывод из типа роутера сервера, пересчитываемый на каждом нажатии клавиши. Это tRPC, и вся магия — система типов, которую вы уже изучили.
</Hook>

<Crux>tRPC выводит тип API клиента из `typeof appRouter` на этапе компиляции — без кодогена, но клиент обязан разделять тип сервера.</Crux>

<Explanation>

## Весь трюк: `typeof appRouter`

На сервере вы строите роутер. Его *тип* — не значение — кодирует каждое имя процедуры, вход и выход. Вы экспортируете этот тип и отдаёте клиенту как дженерик:

```ts
// server/router.ts
import { initTRPC } from "@trpc/server";
import { z } from "zod";
const t = initTRPC.create();

export const appRouter = t.router({
  getUserById: t.procedure
    .input(z.object({ id: z.number(), includeDeleted: z.boolean() }))
    .query(({ input }) => findUser(input.id)), // возвращает User
});

export type AppRouter = typeof appRouter; // ^? тип всего роутера
```

```ts
// client/trpc.ts
import { createTRPCClient, httpBatchLink } from "@trpc/client";
import type { AppRouter } from "../server/router"; // только тип — см. ниже

const trpc = createTRPCClient<AppRouter>({ links: [httpBatchLink({ url: "/trpc" })] });

const user = await trpc.getUserById.query({ id: 42, includeDeleted: false });
// ^? User  — выведено сквозь из типа возврата сервера
// trpc.getUser  // Error: Property 'getUser' does not exist
```

`createTRPCClient<AppRouter>` берёт *тип* роутера сервера и через большой внутренний mapped/conditional-тип превращает его в интерфейс вызовов клиента: процедура `getUserById.query` роутера `appRouter` становится `trpc.getUserById.query(input): Promise<output>`. Тип входа берётся из `z.infer` вашей zod-схемы; тип выхода — из типа возврата резолвера. Оба текут к клиенту без единого сгенерированного файла. (В реальном приложении вы редко вызываете `.query` напрямую — вы прогоняете его через клиентский кэш вроде React Query, где эти выведенные типы доходят до ваших хуков; см. [трек Frontend](/ru/learn/frontend) про клиентские кэши.)

<Inset kind="why" lang="ru">
Тяжёлую работу здесь делает `typeof` в пространстве типов (оператор из раздела 04). `typeof appRouter` поднимает *рантайм-значение* (объект роутера) в *тип*, который клиент может потребить. Клиент никогда не импортирует рантайм роутера — только его тип — так что обращения сервера к БД, секреты и зависимости не попадают в бандл браузера. Контракт — это тип; реализация остаётся дома.
</Inset>

## «Без кодогена» — что это реально значит и жёсткие пределы

Прежде чем брать tRPC в следующий проект, спросите себя: может ли клиент всегда сделать `import type` с сервера? Если да — живой вывод достаётся бесплатно. Если нет — нужен контракт провода, и это меняет всё.

REST + OpenAPI генерирует файл `client.ts` из спеки; GraphQL (язык запросов с типизированной схемой) генерирует типы из схемы. tRPC не генерирует *ничего*. Размен в том, что клиент обязан мочь сделать `import type` серверного `AppRouter` — а это значит:

- **Одна сборка TypeScript / монорепо.** Клиент и сервер компилируются вместе (или сервер поставляет `.d.ts`, который потребляет клиент). Формата провода, описывающего API, нет; *тип* — это контракт, а тип может пересечь только границу компиляции, не границу сети или языка.
- **TypeScript на обоих концах.** Swift- или Kotlin-клиент не может потребить TS-тип. Для полиглот- или сторонних потребителей нужен настоящий контракт провода — OpenAPI или GraphQL — и именно тогда их кодогенерация окупается.

<SequenceDiagram
  label="Путь вывода tRPC: тип роутера сервера к выведенному клиенту к типизированному месту вызова"
  hue="var(--d-frontend)"
  actors={["router", "AppRouter type", "client call"]}
  messages={[
    { from: "router", to: "AppRouter type", label: "typeof appRouter" },
    { from: "AppRouter type", to: "client call", label: "createTRPCClient<AppRouter>" },
    { from: "client call", to: "client call", label: "infer input from z schema" },
    { from: "client call", to: "client call", label: "infer output from resolver" },
  ]}
  caption="Файл не генерируется. Стрелки — чистое вычисление типов на этапе компиляции: значение поднято typeof в AppRouter, отображено в интерфейс клиента, вход/выход выведены на процедуру. Это работает только потому, что AppRouter можно импортировать как тип."
/>

## Цена: время вывода роутера и лаги редактора

За удобство платят работой компилятора. Тип клиента — *функция от всего типа роутера сервера*, пересчитываемая `tsc` и языковым сервисом редактора. На роутере с сотнями процедур, глубоко вложенными саброутерами и сложными zod-схемами вы это чувствуете: всплывающие подсказки крутятся, автодополнение лагает, прогоны `tsc` ползут. Вывод не кэшируется так, как кэшировался бы сгенерированный файл — файла нет. Смягчения:

- **Разбивайте на саброутеры** и избегайте одного монолитного роутера на 500 процедур; меньшие единицы перевыводятся быстрее.
- **Аннотируйте типы возврата процедур явно**, чтобы тип выхода резолвера не выводился каждый раз сквозь весь слой данных.
- **Держите zod-схемы разумными** — гигантский `z.discriminatedUnion` на 40 членов дорог в выводе везде, где используется.
- **Тянитесь к кодогену, когда вывод перестаёт масштабироваться**: это настоящая точка, где модель OpenAPI/GraphQL «сгенерировал раз — прочитал файл» выигрывает по производительности компилятора, разменивая живой вывод на шаг сборки.

Вместе эти смягчения сокращают перевыводимую поверхность; без них монолитный роутер на 500 процедур заставляет весь языковой сервис ползти на каждом нажатии клавиши.

<Infographic
  svg={codegenTradeoff}
  label="Сравнение бок о бок: tRPC без кодогена даёт ноль сгенерированных файлов и живой вывод на каждое нажатие, но требует общего TypeScript-типа и тормозит tsc на больших роутерах; кодоген OpenAPI/GraphQL генерирует языконезависимый файл клиента, читаемый раз, и поддерживает иноязычных и сторонних клиентов ценой шага сборки."
  caption="Размен в одном кадре: tRPC покупает живой вывод, разделяя TS-тип; кодоген покупает языконезависимый контракт ценой шага сборки."
  hue="var(--d-frontend)"
/>

## Главный самострел: случайный импорт серверного рантайма

Поскольку сервер и клиент живут в одном репо, легко написать `import { appRouter }` (значение) вместо `import type { AppRouter }` (тип). Импорт значения тащит *весь серверный рантайм* — драйверы БД, секреты, Node-only-модули — в бандл клиента и даже может отправить учётные данные в браузер. Всегда `import type` для роутера на клиенте. Включите `"verbatimModuleSyntax": true` (раздел 07), чтобы случайный импорт-значением типа был ошибкой компиляции, а не тихим раздуванием бандла.

<Quiz
  id="08-trpc-quiz-1"
  lessonSlug="03-trpc-end-to-end"
  lang="ru"
  question="tRPC даёт «сквозную типобезопасность без кодогена». Что «без кодогена» требует взамен?"
  choices={[
    { label: "Клиент обязан мочь импортировать серверный AppRouter как ТИП — одна TS-сборка/монорепо — потому что сам тип и есть контракт и может пересечь только границу компиляции.", correct: true },
    { label: "Ничего — tRPC работает между любыми языками и отдельными репо, как REST.", misconception: "Контракта формата провода для потребления из другого языка или репо нет. Swift-клиент или собранный отдельно сервис не может импортировать TS-тип, так что им нужны OpenAPI/GraphQL." },
    { label: "По сети шлётся рантайм-схема, чтобы клиент пересобрал типы.", misconception: "Ничего о типах не путешествует в рантайме. Вывод чисто на этапе компиляции из typeof appRouter; клиент разделяет тип на сборке, а не по проводу." },
    { label: "Нужно запускать шаг генерации, просто быстрее, чем OpenAPI.", misconception: "tRPC не генерирует никаких файлов вообще — в этом вся суть. Тип клиента пересчитывается tsc из импортированного типа роутера на каждом нажатии клавиши." },
  ]}
/>

<DragOrder
  id="08-trpc-drag-1"
  pieceSlug="03-trpc-end-to-end"
  lang="ru"
  prompt="Расставьте, как типы серверной процедуры доходят до типизированного места вызова клиента в tRPC:"
  items={[
    { id: "a", label: "Сервер строит appRouter; каждая процедура несёт zod-схему входа и тип возврата резолвера" },
    { id: "b", label: "Экспортировать ТИП роутера: type AppRouter = typeof appRouter" },
    { id: "c", label: "Клиент импортирует его через `import type` и передаёт: createTRPCClient<AppRouter>" },
    { id: "d", label: "mapped/conditional-тип превращает тип роутера в интерфейс клиента, выводя каждый вход/выход" },
    { id: "e", label: "Место вызова trpc.x.query(input) полностью типизировано; переименование серверной процедуры ломает клиент на этапе компиляции" },
  ]}
/>

</Explanation>

<KeyTakeaway>
tRPC даёт **сквозную типобезопасность без кодогена**: `type AppRouter = typeof appRouter` поднимает *значение* роутера сервера в *тип*, а `createTRPCClient<AppRouter>` запускает большой внутренний mapped/conditional-тип, превращающий его в интерфейс вызовов клиента — типы входа из `z.infer` каждой процедуры, типы выхода из типа возврата каждого резолвера, всё вычислено на этапе компиляции. За «без кодогена» платят жёстким ограничением: клиент обязан **разделять** тип сервера (одна TS-сборка / монорепо; TypeScript на обоих концах), так что полиглот- или сторонний потребитель всё равно нуждается в OpenAPI/GraphQL. Текущая цена — **время вывода**: гигантский роутер заставляет `tsc` и редактор ползти; смягчайте разбиением на саброутеры, аннотацией типов возврата резолверов, скромными схемами и переходом на кодоген, когда живой вывод перестаёт масштабироваться. Острый самострел: `import type` для роутера, никогда импорт-значением, иначе вы отгружаете весь серверный рантайм (и секреты) в бандл браузера.
</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="08-trpc-retrieval"
  lang="ru"
  questions={[
    { q: "Механически: как клиент получает типы API в tRPC без кодогена?", a: "Сервер строит значение роутера (appRouter), чей ТИП кодирует каждое имя процедуры, схему входа и тип возврата резолвера. Вы экспортируете `type AppRouter = typeof appRouter`, используя оператор типа typeof, чтобы поднять рантайм-значение в тип. Клиент делает `createTRPCClient<AppRouter>(...)`, передавая этот тип дженериком. Внутри у tRPC есть большой mapped/conditional-тип, который обходит AppRouter и производит интерфейс вызовов клиента: каждая процедура x.query становится trpc.x.query(input): Promise<output>, где тип входа — это z.infer zod-схемы процедуры, а тип выхода — тип возврата резолвера. Всё это пересчитывается tsc/языковым сервисом на этапе компиляции — файл не генерируется, поэтому переименование серверной процедуры мгновенно ломает места вызова клиента." },
    { q: "Что «без кодогена» вынуждает взамен и когда лучше предпочесть кодоген OpenAPI/GraphQL?", a: "Это вынуждает клиента мочь импортировать серверный AppRouter КАК ТИП. Значит — одна TS-сборка или монорепо (или сервер поставляет .d.ts, который потребляет клиент) и TypeScript на обоих концах, потому что контракт ЕСТЬ тип, а тип может пересечь только границу КОМПИЛЯЦИИ, не границу сети или языка. Спеки формата провода нет. Так что Swift/Kotlin-клиент, сторонний потребитель или собранный отдельно сервис не могут использовать вывод tRPC; для них нужны OpenAPI или GraphQL, чья кодогенерация производит настоящий, языконезависимый контракт из спеки/схемы. Также предпочитают кодоген, когда живой вывод tRPC перестаёт масштабироваться по производительности компилятора — сгенерированный файл читается раз вместо перевывода." },
    { q: "Почему гигантский tRPC-роутер замедляет редактор и в чём самострел импорта рантайма?", a: "Тип API клиента — функция от ВСЕГО типа роутера сервера, пересчитываемая tsc и языковым сервисом редактора на каждом нажатии клавиши — кэшируемого сгенерированного файла нет. Роутер с сотнями процедур, глубокими саброутерами и большими zod-схемами (например, discriminatedUnion на 40 членов) заставляет подсказки крутиться, автодополнение лагать, а tsc ползти. Смягчения: разбить на меньшие саброутеры, аннотировать типы возврата резолверов явно, чтобы выход не выводился сквозь весь слой данных, держать схемы скромными и перейти на кодоген, когда вывод больше не масштабируется. Самострел: поскольку клиент и сервер делят репо, запись `import { appRouter }` (импорт ЗНАЧЕНИЯ) вместо `import type { AppRouter }` тащит весь серверный рантайм — драйверы БД, секреты, Node-модули — в бандл браузера. Всегда импортируйте роутер как тип и включите verbatimModuleSyntax, чтобы случайный импорт-значением типа не компилировался." },
  ]}
/>

<Recap lang="ru">
Теперь вы понимаете движок за заявлением tRPC «без кодогена»: `typeof appRouter` поднимает роутер в тип, клиент выводит из него весь свой API на этапе компиляции, цена — связанность через общий тип плюс стоимость вывода, а самострел — импорт значением, утекающий сервер в браузер. Вы видели три точки на спектре граничной типизации — контракты, смоделированные руками, и Result-типы; рантайм-валидация zod; и вывод tRPC на этапе компиляции. Этот капстоун *потребления* типизированных систем готовит обратный навык: в следующем уроке вы будете *проектировать* generic-API библиотек, которые так же красиво выводятся для ваших вызывающих, и всё сходится в капстоуне типизированной фичи в разделе 09. Теперь, когда встретите на клиенте `import { appRouter }` в tRPC-проекте, вы знаете: надо менять на `import type { AppRouter }` — иначе секреты сервера окажутся в бандле браузера.
</Recap>
