---
concepts:
  - api-typing
  - network-boundary
  - discriminated-unions
  - result-type
  - type-assertions
deepensInto:
  - "typescript/08-real-world/02-zod-and-validation"
estMin: 15
lang: ru
lessonType: topic
level: middle
order: 1
prereqs:
  - "typescript/02-everyday-types/04-discriminated-unions"
  - "apis/01-rest-modeling/01-overview"
slug: "01-typing-apis"
sources:
  - https://www.typescriptlang.org/docs/handbook/2/everyday-types.html
  - https://www.typescriptlang.org/docs/handbook/2/narrowing.html
spiral:
  - assignability
  - narrowing
  - api-contracts
status: ready
summary: "Аннотация типа на ответе fetch — это обещание о форме сервера, а не доказательство; `as User` не проверяет ничего. Моделируйте ответы как размеченные объединения и Result-типы и считайте сетевую границу местом, где статические типы кончаются и начинается рантайм-валидация."
title: "Типизация API: тип на fetch — это обещание, а не доказательство"
track: "typescript"
unit: "08-real-world"
---

import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import SequenceDiagram from "~/components/diagram/SequenceDiagram.astro";
import Infographic from "~/components/diagram/Infographic.astro";
import promiseToProof from "./infographics/promise-to-proof.svg?raw";

<Hook>
Пейджер срабатывает в 02:00. На странице профиля растёт `TypeError: Cannot read properties of undefined (reading 'name')`. Вы открываете код, и строка — `user.profile.name`, а `user` имеет тип `User`, где `profile` необязательным не помечен. Компилятор клялся, что это безопасно. Но три часа назад staging-API выкатил изменение: `profile` теперь `null` для удалённых аккаунтов. TypeScript об этом не знал. Аннотация говорила `User`; сервер прислал другое; никто не проверил. Тип был *обещанием о проводе*, и провод его нарушил.
</Hook>

<Crux>Тип на результате `fetch` — это непроверенное обещание о форме сервера: компилятор может доказать ваш код, но никогда — байты на проводе.</Crux>

<Explanation>

## `fetch` отдаёт вам `any`, а `as` делает только хуже

`response.json()` возвращает `Promise<any>`. Этот `any` честен: на этапе компиляции TypeScript действительно не знает, какой JSON пришлёт сервер. В момент, когда вы пишете тип, вы *утверждаете* знание, которое компилятор проверить не может:

```ts
const res = await fetch("/api/user/42");
const user = await res.json(); // ^? any

// «Обещаю, что это User» — но это никто не проверяет
const typed = (await res.json()) as User; // ^? User
```

`as User` — это одностороннее утверждение. Оно заглушает `any` и даёт значение, которое остальная программа считает полностью типизированным, — но при этом не выполняет **никакой** работы в рантайме. Если сервер пришлёт `{ id: 42, profil: null }` (заметьте опечатку, или переименованное поле, или `null` там, где вы ждали объект), `typed.profile.name` скомпилируется, уедет в прод и упадёт в рантайме. Каст был ложью, которую система типов поймать не могла.

<Inset kind="why" lang="ru">
Почему TypeScript вообще разрешает такой явно несостоятельный каст? Потому что `as` — это специально спроектированный люк «я знаю то, чего не знает компилятор». Он уместен, когда знание есть *у вас* — например, `document.getElementById("x") as HTMLInputElement`, когда HTML писали вы. На сетевой границе этого знания у вас **нет**: сервер — это отдельный процесс, возможно отдельная команда, возможно другой деплой. Это ровно та ситуация, для которой `as` никогда не задумывался.
</Inset>

## `as Response` и граница доверия

Данные не заслуживают доверия, и их форма тоже. Представьте круговой рейс: ваш типизированный вызов уходит, возвращаются байты JSON, и тут развилка. Одна ветка *кастит* — быстро, бесплатно и несостоятельно. Другая *валидирует* — рантайм-проверка, которая реально осматривает байты и только потом пропускает типизированное значение.

<SequenceDiagram
  label="Круговой рейс API: где заканчивается статический тип и начинается развилка каст против валидации"
  hue="var(--d-frontend)"
  actors={["client", "server", "boundary"]}
  messages={[
    { from: "client", to: "server", label: "GET /api/user/42" },
    { from: "server", to: "boundary", label: "200 JSON bytes" },
    { from: "boundary", to: "client", label: "as User: net proverki" },
    { from: "boundary", to: "client", label: "validate to User: proveren" },
  ]}
  caption="Статические типы доказывают код слева от границы. Прибывающие байты — это any. Каст доверяет им вслепую; валидация осматривает их и является единственным путём, который делает аннотацию User истинной."
/>

Граница — самое важное понятие этого раздела: это линия, где *гарантии этапа компиляции прекращаются*. Всё слева от неё (ваши места вызовов, ваш код рендера) доказано проверщиком типов. Всё справа (фактический ответ) — рантайм-данные, которых проверщик не видел. Каст притворяется, что линии нет. Валидация — единственное, что *зарабатывает* тип.

## Моделируйте ответ как размеченное объединение, а не как happy-path-форму

Ещё до валидации вы продвигаетесь дальше, если *типизируете реальность*. Реальные API возвращают ошибки, а не только успехи. Поле, которое «всегда есть» на счастливом пути, в ветке ошибки — `undefined`. Смоделируйте оба:

```ts
type ApiOk<T> = { ok: true; data: T };
type ApiErr = { ok: false; error: { code: string; message: string } };
type ApiResult<T> = ApiOk<T> | ApiErr;

declare function getUser(id: number): Promise<ApiResult<User>>;

const r = await getUser(42);
if (r.ok) {
  r.data; // ^? User  — сужено: data есть только в ok-ветке
} else {
  r.error.message; // ^? string  — error есть только здесь
  // r.data;  // Error: Property 'data' does not exist on type 'ApiErr'
}
```

Дискриминант `ok: true | false` (раздел 02) заставляет каждого вызывающего обработать ветку отказа — компилятор *отказывается* пускать к `r.data`, пока не доказано `r.ok`. Это строго лучше, чем возврат `User | undefined`, который потом затирают через `!`. Это не валидирует байты, но делает *контракт* честным в системе типов.

## `Result<T, E>` проталкивает ошибки в значения

Та же идея, обобщённая: вместо того чтобы бросать исключение через границу, возвращайте `Result` и заставьте вызывающего разобраться с обеими ветками. Ошибки становятся обычными значениями, которые отслеживает проверщик типов:

```ts
type Result<T, E = Error> =
  | { kind: "ok"; value: T }
  | { kind: "err"; error: E };

function parseJson<T>(text: string): Result<T, SyntaxError> {
  try {
    return { kind: "ok", value: JSON.parse(text) as T };
  } catch (e) {
    return { kind: "err", error: e as SyntaxError };
  }
}
// Заметьте: `as T` внутри — ВСЁ ЕЩЁ ложь — JSON.parse возвращает any.
// Result честно моделирует развилку бросает/не бросает, но не
// проверяет форму успешного значения. Этот пробел закрывает урок 2.
```

`Result` делает поток управления типобезопасным. Он *не* делает типобезопасной полезную нагрузку — `as T` — это та же граничная ложь в более красивом пальто.

## Разделение типов между клиентом и сервером

Когда вы обнаруживаете, что копируете серверный интерфейс в фронтенд-файл руками, спросите себя: что произойдёт, когда сервер добавит поле на следующей неделе? Самый чистый способ сделать аннотацию *менее* ложью — перестать писать её руками. Если клиент и сервер делят один источник истины, тип не сможет тихо разъехаться с байтами:

- **Один монорепозиторий / общий пакет** — экспортируйте тип ответа из одного модуля, который импортируют обе стороны. Zod-схемы следующего урока и tRPC из урока 3 — два способа сделать это; оба выводят тип из одного определения.
- **Типы, сгенерированные из OpenAPI** (стандарт описания REST API в машиночитаемом формате) — когда сервер публикует OpenAPI-спецификацию, шаг кодогенерации превращает её в клиентские типы (см. [трек APIs](/ru/learn/apis) про OpenAPI). Сгенерированный `User` механически привязан к задокументированному контракту, поэтому он расходится только когда расходится спека — а diff спеки можно отревьюить. Но в рантайме это всё равно не валидирует; баг сервера, нарушающий собственную спеку, проскочит. Генерация сужает *пробел расхождения*; она не закрывает *пробел доверия*.

<Infographic
  svg={promiseToProof}
  label="Восходящая лестница из четырёх ступеней техник типизации API: голый каст as User не проверяет ничего, размеченное объединение или Result делает поток управления честным, но оставляет нагрузку кастом, кодогенерация из OpenAPI привязывает тип к контракту и закрывает расхождение, а zod .parse в рантайме осматривает байты и закрывает пробел доверия."
  caption="Каждая ступень делает аннотацию fetch меньшей ложью — но только рантайм-проверка наверху превращает обещание в доказательство."
  hue="var(--d-infra)"
/>

<Quiz
  id="08-api-quiz-1"
  lessonSlug="01-typing-apis"
  lang="ru"
  question="Вы пишете `const u = (await res.json()) as User`. Сервер меняет `profile` с объекта на `null`. Что произойдёт?"
  choices={[
    { label: "Скомпилируется нормально и упадёт в рантайме при чтении u.profile.name — каст не выполнил рантайм-проверку, так что тип был обещанием, которое сервер нарушил.", correct: true },
    { label: "TypeScript сообщит об ошибке компиляции, потому что ответ больше не соответствует User.", misconception: "TypeScript никогда не видит сервер. res.json() — это any, а `as User` утверждает форму без какой-либо рантайм-проверки, так что против реальных байтов ничего не сверяется." },
    { label: "fetch бросит исключение автоматически, потому что форма JSON изменилась.", misconception: "fetch и res.json() падают только на сетевых ошибках или ошибках разбора, а не на несовпадении формы. Структурно другой, но валидный JSON разберётся нормально; каст затем неверно его пометит." },
    { label: "Значение станет undefined, потому что profile равен null.", misconception: "Весь объект — это разобранный JSON; profile равен null, а не undefined. Чтение .name у null и есть то, что падает — и вас никто не остановил, потому что as не сделал проверки." },
  ]}
/>

<DragOrder
  id="08-api-drag-1"
  pieceSlug="01-typing-apis"
  lang="ru"
  prompt="Расставьте путь полученного значения от запроса до надёжно типизированного значения:"
  items={[
    { id: "a", label: "Вызвать fetch и сделать await res.json() — результат имеет тип any" },
    { id: "b", label: "Достичь сетевой границы: гарантии этапа компиляции прекращаются, байтам нет доверия" },
    { id: "c", label: "Выбрать валидацию, а не каст: запустить рантайм-проверку, осматривающую фактические байты" },
    { id: "d", label: "При успехе сузить до типизированной формы; при отказе явно обработать ветку ошибки" },
  ]}
/>

</Explanation>

<KeyTakeaway>
Аннотация типа на результате `fetch` — это **обещание о форме сервера, а не доказательство**: `res.json()` — это `any`, а `as User` — одностороннее утверждение, не делающее никакой рантайм-работы, так что переименованное поле или `null` проскальзывают и падают позже. Сетевая граница — линия, где гарантии этапа компиляции заканчиваются и начинаются непроверенные рантайм-данные; каст притворяется, что линии нет. Улучшайте контракт, моделируя ответы как **размеченные объединения** (`{ ok: true; data } | { ok: false; error }`), чтобы компилятор заставлял вызывающих обработать отказ, и используйте **`Result<T, E>`**, чтобы превратить брошенные ошибки в отслеживаемые значения. Разделение типов через пакет монорепозитория или **кодогенерацию из OpenAPI** убивает *расхождение* (тип не разъедется с контрактом незаметно), но не *пробел доверия* — только рантайм-проверка может сделать аннотацию реально истинной, что и делает zod дальше.
</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="08-api-retrieval"
  lang="ru"
  questions={[
    { q: "Почему `(await res.json()) as User` называют ложью и когда `as` законно уместен?", a: "res.json() возвращает Promise<any>, потому что на этапе компиляции TypeScript понятия не имеет, что пришлёт сервер. Запись `as User` — одностороннее утверждение типа: оно переименовывает этот any в User и выполняет НОЛЬ рантайм-проверок. Если сервер переименовал поле, прислал null там, где вы ждали объект, или сменил форму целиком, каст всё равно компилируется, и неверно помеченное значение втекает в код, который ему доверяет, и падает позже (например, при чтении .name у null). Это ложь, потому что утверждение заявляет знание, которое компилятор не может и не проверил. `as` законен, когда знание реально есть У ВАС — например, getElementById('x') as HTMLInputElement для HTML, написанного вами. Сетевая граница — противоположный случай: сервер — отдельный процесс/команда/деплой, так что этого знания у вас на самом деле нет." },
    { q: "Что такое «сетевая граница» и почему она важна для типизации?", a: "Это линия, где прекращаются гарантии этапа компиляции. Всё на вашей стороне (места вызовов, код рендера, бизнес-логика) доказано проверщиком типов как состоятельное. Прибывающие от сервера байты ответа проверщик никогда не видел — это непроверенные рантайм-данные, корректно типизированные как any через res.json(). Каст притворяется, что границы нет, и доверяет байтам вслепую. Валидация — единственная операция, которая реально осматривает байты и при успехе даёт значение, действительно совпадающее с аннотацией. Так что граница — это место, где нужно превратить «я утверждаю эту форму» в «я проверил эту форму»." },
    { q: "Как размеченные объединения и Result<T,E> улучшают типизацию API и что они НЕ чинят?", a: "Размеченное объединение вида { ok: true; data: T } | { ok: false; error: E } моделирует то, что реальные API возвращают отказы, и дискриминант ok заставляет каждого вызывающего сузить тип перед доступом к data — компилятор отказывается давать data в ветке ошибки. Result<T,E> обобщает это: вместо броска через границу вы возвращаете значение с веткой ok/err, так что ошибки становятся отслеживаемыми значениями, а не исключениями. Оба делают ПОТОК УПРАВЛЕНИЯ и КОНТРАКТ честными в системе типов. Ни один не валидирует форму полезной нагрузки: успешное значение всё ещё рождается через `JSON.parse(...) as T` — та же граничная ложь. Закрытие пробела нагрузки требует рантайм-проверки (zod), а уменьшение расхождения — общего источника истины (тип в монорепозитории или кодогенерация OpenAPI); кодогенерация привязывает тип к ревьюируемой спеке, но сервер, нарушающий собственную спеку, всё равно проскользнёт." },
  ]}
/>

<Recap lang="ru">
Теперь вы относитесь к аннотации fetch как к непроверенному обещанию, видите, почему `as User` несостоятелен на границе, и моделируете ответы как размеченные объединения и `Result`-типы, чтобы отказ был частью контракта. Но контракт честен лишь настолько, насколько позволяют байты — каст внутри `parseJson` — это незакрытый пробел. Дальше **zod** его закрывает: вы определяете схему один раз, выводите из неё статический тип через `z.infer`, а `.parse`/`.safeParse` осматривают реальные байты в рантайме и *сужают* — превращая граничное обещание в проверенное доказательство из единого источника истины. Теперь, когда встретите `(await res.json()) as ЧтоТо` в PR, вы знаете ровно один вопрос: где рантайм-проверка?
</Recap>
