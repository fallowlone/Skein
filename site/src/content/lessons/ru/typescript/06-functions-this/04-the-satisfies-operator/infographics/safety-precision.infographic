infographic compare-quadrant-quarter-simple-card
data
  title аннотация против as против satisfies: безопасность x точность
  desc Две оси — делает ли реальную проверку и сохраняет ли точный выведенный тип? Оба угла выигрывает только satisfies; угол «без проверки + точно» пуст.
  compares
    - label Аннотация — проверка, расширяет
    - label satisfies — проверка, точный тип
    - label as — нет проверки, расширяет
    - label Точно без проверки — невозможно
theme
  palette #f59e0b #10b981 #f43f5e #6366f1
