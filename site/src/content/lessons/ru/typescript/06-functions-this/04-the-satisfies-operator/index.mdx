---
concepts:
- satisfies-operator
- annotation-vs-cast
- literal-inference
- contextual-typing
spiral:
- literal-types
- api-design
prereqs:
- typescript/01-foundations/04-literal-types
deepensInto:
- typescript/08-real-world/01-typing-apis
estMin: 14
lang: ru
lessonType: topic
level: senior
order: 4
slug: 04-the-satisfies-operator
sources:
- https://www.typescriptlang.org/docs/handbook/release-notes/typescript-4-9.html#the-satisfies-operator
status: ready
summary: Как `satisfies` валидирует значение против типа, не расширяя его — сохраняя точный выведенный тип, который аннотация выбросила бы, — в сравнении с аннотацией и `as`, плюс где всё ещё нужен контекстный тип.
title: Оператор satisfies
track: typescript
unit: 06-functions-this
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import MetaphorComplete from "~/components/pedagogy/MetaphorComplete.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";
import Infographic from "~/components/diagram/Infographic.astro";
import safetyPrecision from "./infographics/safety-precision.svg?raw";

<Hook>
У вас есть таблица маршрутов: `{ home: "/", profile: "/u/:id" }`. Вы хотите сразу двух вещей: чтобы компилятор *проверил*, что каждое значение — корректная строка пути, и чтобы он *запомнил* точные литеральные ключи, так что `routes.home` типизирован как `"/"`, а не `string`. Аннотация даёт проверку, но выбрасывает литералы. Каст хранит литералы, но не делает реальной проверки. Годами приходилось выбирать. `satisfies` — это оператор, дающий и то и другое.
</Hook>

<Crux>`satisfies T` проверяет соответствие значения типу `T`, не меняя его тип, так что вы сохраняете точный выведенный тип вместо расширения до `T`.</Crux>

<Explanation>

## Три инструмента на одном значении

Когда вы ловите себя на выборе между безопасностью типов и их точностью — остановитесь. Вопрос, который стоит задать: нужно ли, чтобы целевой тип *протекал внутрь* и управлял выводом, или достаточно *проверки снаружи*, оставляющей выведенный тип нетронутым?

Прогоните один и тот же объект через каждый инструмент и смотрите, что станет с его типом.

```ts
type Routes = Record<string, `/${string}`>;

// 1) Аннотация — валидирует, но РАСШИРЯЕТ до аннотированного типа
const a: Routes = { home: "/", profile: "/u/:id" };
a.home;
// ^? `/${string}`   — литерал "/" потерян; a.home теперь просто «строка пути»
// a.dashboard;      // нет ошибки — ключи Record<string, ...>, полнота не проверяется

// 2) as — хранит тип, который вы утверждаете, но НЕ делает реальной проверки
const b = { home: "/", dashboard: "oops" } as Routes;
//                     ^^^^^^^^ опечатка + невалидный путь, но `as` это заглушает
b.home;
// ^? `/${string}`   — тоже расширен, и баг проскользнул

// 3) satisfies — валидирует И сохраняет точный выведенный тип
const c = { home: "/", profile: "/u/:id" } satisfies Routes;
c.home;
// ^? "/"            — точный литерал сохранён
c.profile;
// ^? "/u/:id"
// const d = { home: "x" } satisfies Routes;
// Error: Type 'string' is not assignable to type `/${string}` — невалидный путь пойман
```

Аннотация **расширяет**: тип переменной становится `Routes`, поэтому `a.home` забывает, что был `"/"`. Каст **лжёт**: `as` делает проверку присваиваемости лишь в одну сторону и подавляет ошибки лишних свойств и значений — `dashboard: "oops"` проходит. `satisfies` **валидирует, не расширяя**: значение проверяется против `Routes`, но переменная сохраняет свой *выведенный* тип `{ home: "/"; profile: "/u/:id" }`, так что каждый литерал выживает.

<FlowDiagram
  label="Одно и то же значение-объект, обработанное тремя способами: аннотация расширяет до целевого типа, as кастует небезопасно, satisfies валидирует, сохраняя выведенный тип."
  hue="var(--accent)"
  caption="satisfies — единственный, кто и запускает реальную проверку, и сохраняет узкий выведенный тип — аннотация теряет точность, as теряет безопасность."
  perRow={3}
  nodes={[
    { id: "val", label: "значение", sub: "{ home: '/' }", col: 0, row: 0 },
    { id: "ann", label: ": Routes", sub: "проверка, расширение", col: 1, row: 0 },
    { id: "ann2", label: "type Routes", sub: "литералы потеряны", col: 2, row: 0 },
    { id: "as", label: "as Routes", sub: "нет проверки", col: 1, row: 1 },
    { id: "as2", label: "type Routes", sub: "баги проходят", col: 2, row: 1 },
    { id: "sat", label: "satisfies Routes", sub: "проверка, хранение", col: 1, row: 2 },
    { id: "sat2", label: "{home:'/'}", sub: "литералы сохранены", col: 2, row: 2 },
  ]}
  edges={[
    { from: "val", to: "ann", label: "" },
    { from: "ann", to: "ann2", label: "" },
    { from: "val", to: "as", label: "" },
    { from: "as", to: "as2", label: "" },
    { from: "val", to: "sat", label: "" },
    { from: "sat", to: "sat2", label: "" },
  ]}
/>

## И проверка полноты, и точные типы по каждому ключу

Канонический выигрыш — запись, где вы хотите проверить набор ключей *и* сохранить точные типы значений по ключам. Используйте тип-ограничение для формы и `satisfies` для сохранения вывода:

```ts
type Color = "red" | "green" | "blue";

const palette = {
  red: [255, 0, 0],
  green: [0, 255, 0],
  blue: [0, 0, 255],
} satisfies Record<Color, [number, number, number]>;

// Полнота: уберите ключ — и получите ошибку
// const p2 = { red: [255,0,0], green: [0,255,0] } satisfies Record<Color, [number,number,number]>;
// Error: Property 'blue' is missing.

palette.red;
// ^? [number, number, number]   — сохранён как кортеж, а не number[]
palette.red[0].toFixed(2);
// ok — элемент — `number`, доступен, потому что тип-кортеж выжил
const keys = Object.keys(palette);
// у значения по-прежнему точные ключи, поэтому нижестоящий код может на них опираться
```

С простой аннотацией `const palette: Record<Color, ...>` `palette.red` был бы расширен, и вы потеряли бы точность кортеж-против-массива и точный набор ключей. `satisfies` сохраняет оба: ограничение валидирует полноту и форму значений; выведенный тип хранит литералы и кортежи.

<Inset kind="why" lang="ru">
Почему `satisfies` вообще не меняет тип? Потому что это утверждение соответствия *только на уровне системы типов*, а не приведение. Тип выражения вычисляется обычным выводом (точным для литералов без `as const` вплоть до обычных правил расширения), а `satisfies T` просто добавляет ограничение «этот выведенный тип должен быть присваиваем `T`» как проверку на этапе компиляции. Если проверка прошла, выведенный тип выходит наружу неизменным. Это инверсия аннотации, которая *навязывает* `T` как тип, а не *проверяет против* него.
</Inset>

## Ловушка: `satisfies` не даёт контекстный тип

Единственное место, где `satisfies` не может заменить аннотацию, — **контекстная типизация**. Аннотация на переменной протекает своим типом *внутрь*, давая неаннотированным параметрам функций их типы. `satisfies` проверяет только *наружу*, после того как значение уже выведено, поэтому он не засевает параметры.

```ts
type Handlers = { onClick: (e: MouseEvent) => void };

// Аннотация: тип протекает внутрь, поэтому `e` типизирован контекстно
const h1: Handlers = {
  onClick: (e) => { e.clientX; },   // e: MouseEvent — выведен из аннотации
};

// satisfies: проверка ПОСЛЕ вывода, поэтому `e` не получает контекстного типа
const h2 = {
  onClick: (e) => { e.clientX; },
  //         ^ Error: Parameter 'e' implicitly has an 'any' type.
} satisfies Handlers;
```

В `h2` параметр стрелки `e` выводится *до* того, как `satisfies` запускает свою проверку, поэтому контекстно типизировать его нечем — при `noImplicitAny` это ошибка; вы должны сами аннотировать `e: MouseEvent`. Правило: берите аннотацию, когда нужно, чтобы целевой тип *управлял* выводом параметров колбэков; берите `satisfies`, когда хотите *валидировать* значение, сохранив его точный выведенный тип. Они дополняют друг друга, а не взаимозаменяемы.

<Infographic
  svg={safetyPrecision}
  label="Решающее пространство 2x2 с двумя осями — делает ли реальную проверку и сохраняет ли точный выведенный тип. Аннотация в углу «проверяет, но расширяет», as — «нет проверки, расширяет», satisfies один в углу «проверяет и хранит тип», а угол «без проверки, но точно» пуст."
  caption="На осях безопасность x точность satisfies — единственный инструмент в углу «проверяет и хранит тип»; угол «точно без проверки» вообще без инструмента."
  hue="var(--d-infra)"
/>

<Quiz
  id="06-satisfies-quiz-three"
  lessonSlug="04-the-satisfies-operator"
  lang="ru"
  question="Для `const cfg = { port: 8080 } satisfies Record<string, number>` каков тип `cfg.port` и почему?"
  choices={[
    { label: "`8080` (литерал) — `satisfies` валидирует против типа, но не расширяет, поэтому `cfg` сохраняет свой выведенный тип `{ port: 8080 }`.", correct: true },
    { label: "`number` — `satisfies` расширяет `cfg` до `Record<string, number>` так же, как аннотация.", misconception: "Это делает аннотация. `satisfies` проверяет, не меняя тип, поэтому литерал сохраняется, а не расширяется." },
    { label: "`any` — `satisfies` — это небезопасный каст вроде `as`.", misconception: "`satisfies` делает реальную проверку присваиваемости (в отличие от `as`); он никогда не даёт `any` и сохраняет точный выведенный тип." },
    { label: "`string | number` — `satisfies` объединяет тип значения с ограничением.", misconception: "Никакого объединения нет; `satisfies` оставляет выведенный тип нетронутым после проверки присваиваемости ограничению." },
  ]}
/>

<MetaphorComplete
  id="06-satisfies-metaphor"
  pieceSlug="04-the-satisfies-operator"
  lang="ru"
  setup="Представьте три инструмента как способы обработать посылку на таможне. Аннотация перепаковывает всё в стандартную коробку (вы проходите досмотр, но теряете оригинальную упаковку). `as` пропускает с поддельным штампом (без реального досмотра). `satisfies` — это инспектор, который сверяет содержимое с правилами, а затем ___ — именно поэтому ваши литеральные типы выживают."
  accepted={["возвращает её без изменений", "отдаёт её как есть", "оставляет как было", "пропускает нетронутой", "сохраняет оригинальную упаковку", "отдаёт обратно без изменений"]}
  canonical="возвращает её без изменений"
  explanation="`satisfies` запускает реальную проверку соответствия типу-ограничению, а затем оставляет выведенный тип значения ровно таким, каким он был выведен — поэтому литералы и кортежи сохраняются, в отличие от аннотации, которая перепаковывает (расширяет), и `as`, который подделывает штамп (без проверки)."
/>

</Explanation>

<KeyTakeaway>`satisfies T` проверяет, что значение присваиваемо `T`, **не меняя его тип**, — инверсия аннотации, которая навязывает `T` и расширяет, теряя литералы, и в отличие от `as`, который не делает реальной проверки и пропускает баги. Используйте его для записей и объектов конфигурации, где нужны и проверка полноты/формы (через ограничение), и точные выведенные типы по ключам (литералы, кортежи). Единственное ограничение: `satisfies` работает *после* вывода, поэтому не даёт контекстного типа — неаннотированные параметры колбэков в значении им не типизируются. Аннотируйте, когда целевой тип должен управлять выводом; `satisfies` — когда нужна валидация без потери точности.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="06-satisfies-retrieval"
  lang="ru"
  questions={[
    { q: "Сравните аннотацию, `as` и `satisfies` на одном объектном литерале: что каждый делает с типом и с безопасностью?", a: "Возьмём `{ home: '/' }` против `type Routes = Record<string, `/${string}`>`. (1) Аннотация `const a: Routes = {...}` делает реальную проверку присваиваемости И навязывает `Routes` как тип переменной — поэтому она РАСШИРЯЕТ: `a.home` становится ``/${string}``, литерал `'/'` потерян, а полнота ключей не проверяется (это Record). (2) `as Routes` хранит тот тип, что вы утверждаете, но НЕ делает реальной валидации — `as` проверяет присваиваемость в одну сторону и подавляет ошибки лишних свойств/значений, так что опечатанное или невалидное значение проходит насквозь; это люк, а не проверка. (3) `satisfies Routes` делает ту же реальную проверку, что и аннотация, но НЕ меняет тип: значение сохраняет свой точный ВЫВЕДЕННЫЙ тип `{ home: '/' }`, поэтому `c.home` — это `'/'`. Итог: аннотация = проверка + расширение (потеря точности); `as` = нет проверки + утверждение (потеря безопасности); `satisfies` = проверка + сохранение (лучшее из обоих)." },
    { q: "Когда `satisfies` НЕ может заменить аннотацию и почему?", a: "Когда нужна КОНТЕКСТНАЯ ТИПИЗАЦИЯ — целевой тип, протекающий ВНУТРЬ, чтобы дать неаннотированным параметрам колбэков/функций их типы. Аннотация на переменной засевает вывод: `const h: Handlers = { onClick: (e) => ... }` типизирует `e` как MouseEvent, потому что аннотация протекает ожидаемым типом в инициализатор до вывода стрелки. `satisfies` работает в обратном направлении: тип значения выводится СНАЧАЛА, затем `satisfies T` проверяет, что он присваиваем `T`. К моменту проверки параметр стрелки `e` уже выведен без контекстного типа, поэтому при `noImplicitAny` это ошибка неявного any, и вы должны сами аннотировать `e: MouseEvent`. Итак: используйте аннотацию, когда целевой тип должен УПРАВЛЯТЬ выводом внутренних параметров; используйте `satisfies`, когда хотите ВАЛИДИРОВАТЬ значение, сохранив его точный выведенный тип (литералы, кортежи, точные ключи). Они дополняют друг друга." },
  ]}
/>

<Recap lang="ru">
`satisfies` (TS 4.9) проверяет значение против типа, не расширяя его, сохраняя точный выведенный тип, который аннотация выбросила бы, и реальную валидацию, которую `as` пропускает. Три инструмента образуют ясную таблицу: аннотация проверяет, но расширяет (литералы потеряны, полнота на записях не проверяется), `as` хранит утверждённый тип, но не делает реальной проверки (баги проходят), а `satisfies` и валидирует, и сохраняет узкий выведенный тип — идеален для палитр, таблиц маршрутов и записей конфигурации, где нужны проверка полноты плюс точные типы значений по ключам. Его единственное ограничение — контекстная типизация: так как он работает после вывода, он не может засеять неаннотированные параметры колбэков, поэтому берите аннотацию, когда целевой тип должен управлять выводом. На этом юнит про функции и `this` закрыт; вы теперь владеете разрешением перегрузок, типизацией и стиранием `this`, потоком управления на основе утверждений и точным различием между аннотацией, кастом и `satisfies`. Теперь, когда встретишь `as SomeType` в объекте конфигурации или таблице маршрутов, спроси себя: не даст ли `satisfies` ту же безопасность с бонусом — литеральные типы выживут ниже по коду.
</Recap>
