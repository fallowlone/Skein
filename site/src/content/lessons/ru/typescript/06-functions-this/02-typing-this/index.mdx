---
concepts:
- this-parameter
- lost-this
- this-type
- polymorphic-this
spiral:
- type-narrowing
- api-design
prereqs:
- typescript/06-functions-this/01-overloads
deepensInto:
- typescript/06-functions-this/04-the-satisfies-operator
estMin: 14
lang: ru
lessonType: topic
level: middle
order: 2
slug: 02-typing-this
sources:
- https://www.typescriptlang.org/docs/handbook/2/functions.html#declaring-this-in-a-function
- https://www.typescriptlang.org/docs/handbook/2/classes.html#this-types
status: ready
summary: "Как TypeScript типизирует скрытый первый параметр через `this: T` (стирается при эмите), почему метод теряет свой `this` при передаче как колбэк, и как ThisType, полиморфный `this` и this-гарды моделируют fluent- и stateful-API."
title: Типизация this
track: typescript
unit: 06-functions-this
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import MetaphorComplete from "~/components/pedagogy/MetaphorComplete.astro";
import SequenceDiagram from "~/components/diagram/SequenceDiagram.astro";

<Hook>
Классовый компонент работает идеально, пока кто-то не пишет `button.addEventListener("click", this.handleClick)`. В рантайме `this` внутри `handleClick` теперь — это сам `<button>`, а не компонент, и чтения `this.state` оказываются `undefined`. В чистом JavaScript баг невидим, пока не упадёт. TypeScript может поймать его на этапе компиляции — но только если вы понимаете, что у каждого метода есть скрытый первый параметр с именем `this`, и что отделение метода срывает эту привязку.
</Hook>

<Crux>TypeScript типизирует получателя фейковым первым параметром `this: T`, который стирается при эмите; отделение метода теряет привязку.</Crux>

<Explanation>

## Фейковый первый параметр

Зачем TypeScript вообще нужен особый механизм для `this`? Потому что `this` — единственное значение в JavaScript, которое не определяется местом определения функции: оно задаётся заново в каждой точке вызова, и компилятор не может вывести его из исходного положения.

Функции в JavaScript получают неявного получателя: значение `this`, определяемое тем, *как функцию вызвали*, а не где её определили. TypeScript позволяет типизировать этого получателя специальным первым параметром с именем `this`. Он выглядит как параметр, но им не является — он **стирается при эмите** и никогда не появляется в списке аргументов вызова.

```ts
interface Card { suit: string; value: number }

function describe(this: Card): string {
  return `${this.value} of ${this.suit}`;
}

const card: Card = { suit: "hearts", value: 10 };
describe.call(card); // ok — `this` подаётся через .call
describe();
// Error: The 'this' context of type 'void' is not assignable
//        to method's 'this' of type 'Card'.
```

Сэмиттированный JavaScript — это просто `function describe() { return this.value + ...; }`: аннотация `this: Card` исчезла. Её единственная задача была — проверить получателя в каждой точке вызова. При `--noImplicitThis` (входит в `strict`) `this`, использованный внутри функции без аннотации и без выводимого контейнера, помечается как неявный `any`, заставляя вас его объявить.

## Методы несут выведенный `this` и теряют его при отделении

Внутри метода объекта или метода класса TypeScript выводит `this` как содержащий объект/экземпляр. Проблема возникает, когда вы **отделяете** метод — присваиваете его переменной или передаёте как колбэк, — потому что привязку задаёт вызывающий, а не определение.

```ts
class Counter {
  count = 0;
  inc(this: Counter) {     // явный this ради безопасности
    this.count++;
  }
}

const c = new Counter();
c.inc();          // ok — получатель `c`
const f = c.inc;  // отделён
f();
// Error: The 'this' context of type 'void' is not assignable
//        to method's 'this' of type 'Counter'.
```

Это проблема **потерянного `this`**. При проверках `strictBindCallApply` и метода-`this` TypeScript помечает несвязанный вызов. Рантайм-фиксы привычные — `c.inc.bind(c)` или стрелочное поле-функция `inc = () => { this.count++ }`, чей `this` лексически является экземпляром, — но именно система типов выводит баг на поверхность до того, как он уедет в прод.

<SequenceDiagram
  label="Метод, переданный как колбэк: в точке вызова фреймворк зовёт его со своим получателем, поэтому `this` больше не исходный экземпляр."
  hue="var(--accent)"
  caption="Привязка следует за вызовом, а не за определением — отделение c.inc рвёт её, поэтому срабатывает проверка this-параметра в TypeScript."
  actors={["Component", "addEventListener", "DOM"]}
  messages={[
    { from: "Component", to: "addEventListener", label: "pass this.onClick" },
    { from: "DOM", to: "addEventListener", label: "click fires" },
    { from: "addEventListener", to: "Component", label: "call onClick (this = button!)" },
  ]}
/>

<Inset kind="why" lang="ru">
Почему стрелочная функция чинит это, а метод — нет? У метода `this` динамичен — разрешается из вызова. У стрелочной функции нет своего `this`; она замыкается над `this` объемлющей области в момент определения. Поэтому поле класса `onClick = () => …` лексически захватывает экземпляр, и вызывающий не может его переназначить. Цена — одно замыкание на экземпляр вместо одного общего метода прототипа — обычно пренебрежимо, изредка значимо для объектов, создаваемых в горячих циклах.
</Inset>

## This-гарды типов и полиморфный `this`

Две `this`-типизированные возможности питают реальные API. Во-первых, метод может вернуть `this is Derived` — **this-гард типа**, который сужает самого получателя в вызывающей области:

```ts
class FileNode {
  isDir(): this is DirNode {   // сужает `this`, а не параметр
    return "children" in this;
  }
}
class DirNode extends FileNode {
  children: FileNode[] = [];
}

declare const node: FileNode;
if (node.isDir()) {
  node.children;
  //   ^? FileNode[]   — node сужен до DirNode внутри ветки
}
```

Во-вторых, **полиморфный `this`** делает тип возврата метода «каким бы ни был текущий класс», что держит fluent-билдеры (API с цепочкой вызовов вида `.where(...).byEmail(...)`) корректно типизированными при наследовании:

```ts
class Query {
  where(cond: string): this {   // возвращает конкретный подкласс, а не Query
    return this;
  }
}
class UserQuery extends Query {
  byEmail(e: string): this { return this; }
}

new UserQuery().where("active").byEmail("a@b.com");
//                                ^^^^^^^ ok — where() вернул UserQuery, а не Query
```

Если бы `where` вернул `Query` вместо `this`, цепочка сломалась бы на `.byEmail`, потому что у `Query` нет такого метода. Полиморфный `this` — это механизм на уровне типов за цепочечными билдерами. `ThisType<T>` — родственный маркер контекстной типизации, который стандартная библиотека использует, чтобы типизировать `this` внутри объектных литералов с наборами методов (объект options во Vue 2, миксины, fluent-конфиг), так что методы внутри литерала видят весь собранный объект как свой `this`.

<Quiz
  id="06-typing-this-quiz-lost"
  lessonSlug="02-typing-this"
  lang="ru"
  question="Дано `const f = obj.method; f();`, где `method` типизирован с `this: Obj`. Почему TypeScript помечает вызов `f()`?"
  choices={[
    { label: "Отделение метода теряет привязку получателя; вызов `f()` подаёт `this: void`, который не присваиваем требуемому методу `this: Obj`.", correct: true },
    { label: "Так как `this: Obj` — реальный рантайм-параметр, у `f()` не хватает первого аргумента.", misconception: "`this: T` — фейковый параметр, стираемый при эмите; он не передаётся позиционно. Ошибка про привязку получателя, а не про недостающий аргумент." },
    { label: "Потому что стрелочные функции нельзя присваивать переменным.", misconception: "`method` — обычный метод, а не стрелочная функция, и любую функцию можно присвоить переменной. Проблема в потерянной привязке `this`." },
    { label: "Потому что `--noImplicitThis` запрещает вообще хранить метод в переменной.", misconception: "noImplicitThis помечает только нетипизированный `this`; хранить метод можно — ошибка тут в нарушении явного контракта this-параметра при несвязанном вызове." },
  ]}
/>

<MetaphorComplete
  id="06-typing-this-metaphor"
  pieceSlug="02-typing-this"
  lang="ru"
  setup="`this` метода — как ключ-карта отеля, работающая, лишь пока вы заселены в конкретный номер. Пронеси голую карту в другое здание, и система по аналогии сочтёт её принадлежащей ___ — поэтому отделённый метод теряет своего исходного получателя."
  accepted={["тому кто ей проведёт", "новому вызывающему", "новой двери", "вызывающему", "тому кто его вызовет", "новому получателю", "тому, кто ей проведёт"]}
  canonical="тому, кто ей проведёт"
  explanation="`this` привязывается точкой вызова, а не определением. Отделённый и заново «проведённый» новым вызывающим (или DOM-событием) метод получает `this` этого вызывающего — баг потерянного `this`, — если только вы не переназначите через .bind или не захватите лексически стрелочным полем."
/>

</Explanation>

<KeyTakeaway>`this: T` — фейковый первый параметр: он типизирует получателя на момент вызова и **стирается при эмите**. Методы выводят `this` как свой контейнер, но привязка динамична — отделение метода (хранение, передача как колбэк) теряет `this`, и TypeScript выводит это на поверхность проверками this-параметра / `strictBindCallApply` до краха в рантайме. Фикс — `.bind` или стрелочное поле класса. Для API: `this is Derived` сужает получателя как гард, полиморфный `this` держит fluent-билдеры типизированными при наследовании, а `ThisType<T>` типизирует `this` внутри объектных литералов с наборами методов.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="06-typing-this-retrieval"
  lang="ru"
  questions={[
    { q: "Объясните проблему потерянного `this` от начала до конца: как TypeScript типизирует `this`, почему отделение метода её ломает и два стандартных фикса.", a: "В JavaScript `this` привязывается ВЫЗОВОМ, а не определением. TypeScript типизирует получателя фейковым первым параметром `this: T`, который стирается при эмите — он никогда не появляется в списке аргументов, он лишь проверяет получателя в каждой точке вызова. Внутри метода TS выводит `this` как контейнер (экземпляр/объект). Проблема потерянного `this`: когда вы отделяете метод — `const f = obj.method` или передаёте `this.onClick` в addEventListener — новый вызывающий подаёт своего получателя (кнопку, undefined и т. д.), поэтому `this` больше не исходный экземпляр. При проверках метода-`this` / strictBindCallApply вызов отделённого метода (`this: void`) против требуемого `this: Obj` — ошибка типа, ловящая баг на компиляции. Два фикса: (1) переназначить явно через `obj.method.bind(obj)`; (2) определить метод как стрелочное поле класса `onClick = () => {…}`, у которого нет своего `this` и который лексически захватывает экземпляр, так что никакой вызывающий его не переназначит. Стрелочный подход стоит одного замыкания на экземпляр вместо общего метода прототипа." },
    { q: "Сравните this-гард типа, полиморфный `this` и ThisType<T> — что моделирует каждый?", a: "Все три — `this`-типизированные возможности для реальных API. (1) This-гард типа — метод, возвращающий `this is Derived`: он сужает самого ПОЛУЧАТЕЛЯ (а не параметр) в вызывающей области, так что `if (node.isDir())` сужает `node` до подкласса внутри ветки — полезно для иерархий помеченных узлов. (2) Полиморфный `this` — метод, чей тип возврата `this`, то есть «конкретный текущий класс». Он держит FLUENT-БИЛДЕРЫ корректно типизированными при наследовании: `where()`, возвращающий `this`, позволяет подклассу сохранить свои методы в цепочке, тогда как возврат базового `Query` отбросил бы методы подкласса после первого вызова. (3) `ThisType<T>` — маркер контекстной типизации (без рантайм-эффекта), который стандартная библиотека использует, чтобы задать, ЧЕМ является `this` внутри объектных литералов с наборами методов — объект options во Vue 2, миксины, fluent-конфиг, — так что методы, написанные внутри литерала, видят полностью собранный объект как свой `this`, а не только срез самого литерала." },
  ]}
/>

<Recap lang="ru">
TypeScript типизирует получателя на момент вызова фейковым первым параметром `this: T`, который стирается при эмите и нужен лишь для проверки получателя в каждой точке вызова; `--noImplicitThis` (в `strict`) заставляет объявить его, когда вывести нельзя. Методы выводят `this` как свой контейнер, но привязка динамична, поэтому отделение метода теряет `this` — баг потерянного `this`, который TypeScript ловит проверками this-параметра и `strictBindCallApply`, а чинят его `.bind` или стрелочное поле класса. Для API `this is Derived` сужает получателя, полиморфный `this` типизирует fluent-билдеры при наследовании, а `ThisType<T>` типизирует `this` внутри объектных литералов с наборами методов. Дальше мы переходим от типизации получателя к изменению потока управления: функции-утверждения, сигнатуры `asserts x is T`, сужающие остаток области через выброс исключения. Теперь, когда вы видите краш с `this.state is undefined` внутри обработчика событий, вы знаете ровно, что произошло — и знаете две строки, которые нужны, чтобы починить это до того, как баг уедет в прод.
</Recap>
