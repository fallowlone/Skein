infographic hierarchy-tree-curved-line-rounded-rect-node
data
  title Одна схема — все типы выводятся из неё
  desc Объяви домен один раз; каждый другой тип — функция от него
  root
    label Comment — одна zod-схема (источник истины)
    children
      - label type Comment — z.infer из схемы
      - label NewComment — подмножество Comment.pick
      - label CreateResult — объединение с Comment
      - label Paginated — makePage выводит T
      - label AppRouter — typeof appRouter
        children
          - label client Comment[] — выводится сквозь все слои
theme
  palette #6366f1 #14b8a6 #f59e0b
