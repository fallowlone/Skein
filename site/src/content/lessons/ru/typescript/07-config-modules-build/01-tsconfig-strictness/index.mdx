---
concepts:
  - strict
  - strictNullChecks
  - noUncheckedIndexedAccess
  - exactOptionalPropertyTypes
  - useUnknownInCatchVariables
  - incremental-strictness-migration
deepensInto:
  - "typescript/07-config-modules-build/02-module-resolution"
estMin: 14
lang: ru
lessonType: topic
level: middle
order: 1
prereqs:
  - "typescript/01-foundations/03-any-unknown-never"
slug: "01-tsconfig-strictness"
sources:
  - https://www.typescriptlang.org/tsconfig#strict
  - https://www.typescriptlang.org/docs/handbook/compiler-options.html
spiral:
  - assignability
  - sound-vs-unsound
status: ready
summary: "`strict: true` переключает восемь флагов разом; `strictNullChecks` меняет тип каждого значения, отделяя null/undefined от `T`. Главные выигрыши — во флагах вне набора: `noUncheckedIndexedAccess`, `exactOptionalPropertyTypes` — и в постепенной миграции слабо типизированной базы."
title: "Строгость tsconfig: что на самом деле включает `strict` и какие флаги он оставляет за бортом"
track: "typescript"
unit: "07-config-modules-build"
---

import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import Infographic from "~/components/diagram/Infographic.astro";
import bundledVsOptin from "./infographics/bundled-vs-optin.svg?raw";

<Hook>
Сервис месяцами работал в dev. Потом коллега поставил `"strict": true` в `tsconfig.json`, чтобы причесать новый пакет, — и *весь репозиторий* вспыхнул 1400 ошибками, в основном `Object is possibly 'undefined'`. Никто не менял логику. Один флаг разом перетипизировал каждое nullable-значение в кодовой базе. Этот флаг — `strictNullChecks`, и `strict` включил его вместе с семью братьями.
</Hook>

<Crux>`strict` включает восемь проверок сразу; `strictNullChecks` несёт нагрузку, а самые ценные для багов флаги в набор не входят.</Crux>

<Explanation>

## `strict` — один переключатель, замкнутый на восемь

`strict: true` не добавляет ни одной отдельной проверки — он задаёт *значение по умолчанию* для семейства флагов, каждый из которых при этом можно переопределить индивидуально:

```jsonc
// tsconfig.json
{
  "compilerOptions": {
    "strict": true
    // подразумевает, если не переопределить каждый:
    //   strictNullChecks, strictFunctionTypes, strictBindCallApply,
    //   strictPropertyInitialization, noImplicitAny, noImplicitThis,
    //   useUnknownInCatchVariables, alwaysStrict
  }
}
```

Все они меняют только *проверку* — ни один не меняет генерируемый JS (кроме `alwaysStrict`, который добавляет `"use strict"`). Они ужесточают то, что компилятор *примет*, обнажая баги, которые всегда были скрыты. Паттерн переопределения важен при миграции: можно включить `strict`, но выборочно ослабить самый громкий флаг.

```jsonc
{
  "compilerOptions": {
    "strict": true,
    "strictNullChecks": false  // временно; всё остальное остаётся строгим
  }
}
```

<div data-lesson-visual class="overflow-x-auto my-6">

| флаг | что ловит | типичная первая реакция |
| --- | --- | --- |
| `strictNullChecks` | `null`/`undefined` там, где требуется ненулевое значение | тысячи `possibly 'undefined'` |
| `noImplicitAny` | параметр/переменная, чей тип не удалось вывести и он упал в `any` | «добавь сюда аннотацию типа» |
| `strictFunctionTypes` | небезопасная вариантность параметров колбэка (аргументы проверяются контравариантно) | обработчик типизирован слишком широко |
| `strictBindCallApply` | `.call`/`.apply`/`.bind` вызваны с неверными типами аргументов | несоответствие `this`/аргументов |
| `strictPropertyInitialization` | поле класса, которому никогда не присваивается значение в конструкторе | `Property has no initializer` |
| `useUnknownInCatchVariables` | пойманная ошибка трактуется как `any` вместо `unknown` | `err` — это `unknown`, надо сузить |
| `noImplicitThis` | `this` неявного типа `any` в свободной функции | аннотируй параметр `this` |
| `alwaysStrict` | (emit) парсит файлы в strict-режиме + добавляет `"use strict"` | почти не виден |

</div>

## `strictNullChecks`: тот, что перетипизирует всё

Без него `null` и `undefined` присваиваемы *любому* типу — `string` молча включает `null`. С ним они становятся собственными типами и должны моделироваться явно:

```ts
// strictNullChecks: true
function greet(name: string) {
  return name.toUpperCase(); // безопасно: name не может быть null/undefined
}

const el = document.querySelector(".btn"); // Element | null
el.addEventListener("click", handler);
//  ^^ Ошибка: 'el' is possibly 'null'.  ts(18047)
//  фикс: if (el) el.addEventListener(...)  — или el?.addEventListener(...)
```

Именно этот флаг создаёт стену из 1400 ошибок, потому что показывает каждое место, где старая система типов лгала. И это же — главный выигрыш по корректности во всём наборе.

<Inset kind="why" lang="ru">
Почему он *выключен* по умолчанию вне `strict`? Потому что TypeScript вышел раньше, чем он появился (флаг пришёл в 2.0), и включение его на легаси-базе — нетривиальная миграция. Исходное поведение «всё nullable» — *несостоятельное* (unsound): оно пропускает `null` сквозь проверки типов. Поэтому `strictNullChecks` — это флаг, который заставляет систему типов действительно означать то, что она говорит.
</Inset>

## Ценные флаги, которые `strict` НЕ включает

Почему важно знать, каких флагов нет в наборе? Потому что самые частые TypeScript-баги — выход за границы массива, отсутствующий ключ конфига, возвращающий `undefined` — живут именно в пробелах, которые набор `strict` не закрывает. Эти два ловят настоящие баги и *не* входят в набор `strict` — вы подключаете их отдельно:

```jsonc
{ "compilerOptions": { "noUncheckedIndexedAccess": true, "exactOptionalPropertyTypes": true } }
```

<Infographic
  svg={bundledVsOptin}
  label="Дерево-таксономия флагов строгости TypeScript из двух ветвей: strict: true объединяет восемь проверочных флагов, где strictNullChecks несущий, а отдельная ветвь подключаемых вручную содержит два самых ценных флага, которые strict не включает, — noUncheckedIndexedAccess и exactOptionalPropertyTypes."
  caption="strict включает восемь флагов разом, но два самых ценных ловца багов лежат вне набора — их подключают отдельно."
  hue="var(--d-infra)"
/>

`noUncheckedIndexedAccess` делает так, что любой доступ по индексу к массиву или record возвращает `T | undefined`, потому что индекс может выйти за границы, а ключ — отсутствовать:

```ts
// noUncheckedIndexedAccess: true
const xs = [1, 2, 3];
const first = xs[0];   // number | undefined  (а не number!)
first.toFixed();       // Ошибка: 'first' is possibly 'undefined'.

const cfg: Record<string, string> = {};
const v = cfg.missing; // string | undefined
```

Он громкий, и почти каждая его жалоба — настоящий скрытый баг (off-by-one, отсутствующий ключ, доступ к пустому массиву). Цена — реальная церемония в телах циклов и при поиске; выигрыш в том, что «доступ к массиву не может быть `undefined`» перестаёт быть ложью.

`exactOptionalPropertyTypes` закрывает тонкую дыру. Без него `{ a?: string }` и `{ a: string | undefined }` считаются эквивалентными — то есть можно присвоить `{ a: undefined }` «опциональному» свойству. С ним они расходятся: *опциональное* свойство может **отсутствовать**, но писать в него явный `undefined` нельзя.

```ts
// exactOptionalPropertyTypes: true
interface Opts { timeout?: number }       // может ОТСУТСТВОВАТЬ, а не быть undefined

const a: Opts = {};                        // ок — отсутствует
const b: Opts = { timeout: undefined };    // Ошибка: undefined не присваиваем;
//  timeout? значит «отсутствует», а не «есть и undefined»
const c: Opts = { timeout: 200 };          // ок
```

Это важно, когда код делает `"timeout" in opts` или `Object.keys(opts)`: при слабом дефолте эти ветки ведут себя неверно, потому что `{ timeout: undefined }` *имеет* ключ.

<DragOrder
  id="07-strict-drag-1"
  pieceSlug="01-tsconfig-strictness"
  lang="ru"
  prompt="Расставь шаги миграции большой слабо типизированной базы к полной строгости без «большого взрыва» из 1400 ошибок:"
  items={[
    { id: "a", label: "Включи strict: true, но сразу поставь strictNullChecks: false, чтобы билд оставался зелёным" },
    { id: "b", label: "Сначала почини дешёвых братьев (noImplicitAny, strictBindCallApply) — мало ошибок, всё локально" },
    { id: "c", label: "Переключи strictNullChecks: true и чини стену null/undefined пофайлово (или по каталогам)" },
    { id: "d", label: "Добавь noUncheckedIndexedAccess и exactOptionalPropertyTypes последними — они обнажают самые глубокие скрытые баги" },
  ]}
/>

<Quiz
  id="07-strict-quiz-1"
  lessonSlug="01-tsconfig-strictness"
  lang="ru"
  question="При noUncheckedIndexedAccess: true каков тип arr[0], где const arr = [1, 2, 3]?"
  choices={[
    { label: "number | undefined — доступ по индексу может выйти за границы, поэтому тип элемента расширяется через undefined.", correct: true },
    { label: "number — массив непуст, значит элемент 0 всегда существует.", misconception: "TypeScript не отслеживает длину массива через индексацию; даже массив-литерал мог быть изменён. Флаг консервативно возвращает number | undefined." },
    { label: "number[] — индексация возвращает коллекцию элементов.", misconception: "Индексация возвращает один элемент, а не массив. Флаг добавляет | undefined к типу этого элемента." },
    { label: "Не меняется; флаг трогает только поиск в Record<string, T>.", misconception: "Он меняет ОБА случая — доступ по индексу к массиву и индексные сигнатуры объектов: и arr[i], и obj[key] становятся T | undefined." },
  ]}
/>

</Explanation>

<KeyTakeaway>
`strict: true` — это *мета-флаг*: он задаёт значение по умолчанию для восьми проверок (`strictNullChecks`, `noImplicitAny`, `strictFunctionTypes`, `strictBindCallApply`, `strictPropertyInitialization`, `useUnknownInCatchVariables`, `noImplicitThis`, `alwaysStrict`), и любую из них можно переопределить. Все они ужесточают *проверку*, а не emit. `strictNullChecks` несёт основную нагрузку — он убирает ложь о том, что `null`/`undefined` присваиваемы всему, поэтому его включение на легаси-репозитории может обнажить тысячи ошибок `possibly 'undefined'`. Два самых ценных флага *не* входят в набор и подключаются отдельно: `noUncheckedIndexedAccess` делает `arr[i]`/`obj[key]` возвращающими `T | undefined` (ловит выход за границы и отсутствующие ключи), а `exactOptionalPropertyTypes` делает `{ a?: T }` означающим «может отсутствовать», а не «может быть `undefined`», так что `{ a: undefined }` перестаёт молча приниматься. Мигрируй инкрементально: сначала ослабь `strictNullChecks`, почини дешёвых братьев, затем включай его по каталогам.
</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="07-strict-retrieval"
  lang="ru"
  questions={[
    { q: "Что на самом деле делает `strict: true` и какие восемь флагов он подразумевает?", a: "Сам по себе он не добавляет проверку; он задаёт значение по умолчанию для семейства флагов, каждый из которых переопределяем индивидуально. Восемь — это: strictNullChecks, noImplicitAny, strictFunctionTypes, strictBindCallApply, strictPropertyInitialization, useUnknownInCatchVariables, noImplicitThis и alwaysStrict. Все они меняют то, что примет компилятор (проверка), а не генерируемый JS — единственное исключение alwaysStrict, который добавляет \"use strict\" в emit. Поскольку каждый переопределяем, можно написать `strict: true` плюс `strictNullChecks: false`, чтобы оставить одну проверку ослабленной на время миграции, тогда как всё остальное остаётся строгим." },
    { q: "Почему strictNullChecks — самый значимый флаг и что меняется, когда он включён?", a: "Без него null и undefined присваиваемы любому типу — string скрыто включает null — поэтому система типов лжёт о том, какие значения могут отсутствовать. С ним null и undefined становятся собственными типами, которые надо моделировать явно, так что `document.querySelector(...)` — это Element | null, и перед использованием нужно сузить (if (el) / el?.). Этот единственный флаг перетипизирует каждое nullable-значение в кодовой базе, поэтому включение на легаси-репозитории может породить тысячи ошибок 'possibly undefined/null'. Это крупнейший выигрыш по корректности, потому что он делает остальную систему типов состоятельной относительно отсутствия значения." },
    { q: "Что ловят noUncheckedIndexedAccess и exactOptionalPropertyTypes и почему их нет в наборе strict?", a: "noUncheckedIndexedAccess делает любой индекс массива (arr[i]) и поиск по индексной сигнатуре (record[key]) возвращающими T | undefined, потому что индекс может выйти за границы, а ключ отсутствовать — это ловит off-by-one, пустой массив и отсутствующие ключи. exactOptionalPropertyTypes различает `{ a?: T }` (свойство может ОТСУТСТВОВАТЬ) и `{ a: T | undefined }` (присутствует, но undefined): с ним присваивание `{ a: undefined }` опциональному свойству — ошибка, что важно для проверок `'a' in obj` / Object.keys. Оба исключены из `strict`, потому что шумные и требуют ручной обработки во множестве мест вызова; TypeScript держит в наборе strict проверки, которые большинство проектов могут принять целиком, оставляя эти два как opt-in для команд, желающих максимальной состоятельности." },
  ]}
/>

<Recap lang="ru">
Теперь ты знаешь, что `strict` — мета-флаг, замыкающий восемь проверок, что `strictNullChecks` — тот, кто перетипизирует каждое значение, и что `noUncheckedIndexedAccess` и `exactOptionalPropertyTypes` — opt-in флаги, где прячутся самые глубокие баги. Дальше — **разрешение модулей**: когда проверки типов прошли, компилятору всё ещё нужно *найти* файлы и пакеты, которые ты импортируешь; неверный `moduleResolution` — классическая ловушка «работает в редакторе, падает в рантайме». Теперь, когда видишь стену `possibly 'undefined'` после чьего-то `strict: true`, ты знаешь, какой флаг сработал — и какие opt-in флаги добавить, когда пыль уляжется.
</Recap>
