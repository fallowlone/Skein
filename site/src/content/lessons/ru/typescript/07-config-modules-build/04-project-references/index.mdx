---
concepts:
  - project-references
  - composite
  - incremental
  - tsbuildinfo
  - build-graph
  - tsc-build
deepensInto:
  - "typescript/07-config-modules-build/05-build-tooling"
estMin: 14
lang: ru
lessonType: topic
level: middle
order: 4
prereqs:
  - "typescript/07-config-modules-build/02-module-resolution"
slug: "04-project-references"
sources:
  - https://www.typescriptlang.org/docs/handbook/project-references.html
spiral:
  - module-systems
  - incrementality
status: ready
summary: "Project references разбивают монорепо на `tsconfig`-проекты, которые `tsc --build` компилирует по графу зависимостей. `composite` (форсирует `declaration`) включает ссылки и кэш `.tsbuildinfo`. Ловушки: устаревший buildinfo, циклические ссылки, ошибка «must have composite»."
title: "Project references: tsc --build, composite и инкрементальный граф сборки"
track: "typescript"
unit: "07-config-modules-build"
---

import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
Монорепо на 200k строк проверяло типы 90 секунд на каждом сохранении, потому что один большой `tsconfig` перепроверял *все* пакеты при любом изменении файла. Кто-то разбил его на проекты со ссылками и добавил `composite: true` — и однострочная правка в пакете `web` теперь перепроверяет только `web`, переиспользуя кэшированный вывод `core` и `ui`. Первая сборка записала `.tsbuildinfo` на каждый проект; последующие сборки читают его и пропускают неизменённые проекты. Но миграция началась со стены ошибок `error TS6306: Referenced project must have setting "composite": true`.
</Hook>

<Crux>`tsc --build` компилирует `tsconfig` в порядке зависимостей, переиспользуя кэш `.tsbuildinfo` — но ссылаться можно лишь на `composite`.</Crux>

<Explanation>

## Форма: проект, указывающий на свои зависимости

У каждого пакета свой `tsconfig.json`. Пакет, зависящий от другого, перечисляет его в `references`, и *проект, на который ссылаются*, должен быть `composite`:

```jsonc
// packages/web/tsconfig.json
{
  "compilerOptions": { "composite": true, "outDir": "dist" },
  "references": [
    { "path": "../core" },   // web зависит от core
    { "path": "../ui" }      // и от ui
  ]
}
```

```jsonc
// packages/core/tsconfig.json
{
  "compilerOptions": {
    "composite": true,        // ОБЯЗАТЕЛЬНО, чтобы на него ссылались
    "declaration": true,      // composite это подразумевает; типы — это межпроектный контракт
    "outDir": "dist"
  }
}
```

`composite: true` включает три вещи: форсирует `declaration: true` (чтобы проект генерировал `.d.ts` — *интерфейс*, который другие проекты потребляют вместо исходника), форсирует инкрементальное поведение с `.tsbuildinfo` и требует, чтобы каждый входной файл был покрыт `include`/`files` (чтобы сборка надёжно хешировала входы).

<Inset kind="why" lang="ru">
Почему `composite` *требует* `declaration`? Потому что весь смысл проекта-со-ссылкой в том, что нижестоящие проекты не перечитывают его исходник — они читают его сгенерированный `.d.ts`. `.d.ts` — это межпроектный контракт типов. Без declaration emit `web` не на что было бы проверять типы, кроме сырых `.ts` из `core`, что разрушает изоляцию (и скорость). Поэтому `composite` и `declaration` неразделимы: проект-со-ссылкой обязан публиковать свои типы.
</Inset>

## `tsc --build` обходит граф ссылок

Обычная сборка `tsc` проекта со ссылками либо упадёт, либо проигнорирует их. Нужно использовать **режим сборки**: `tsc --build` (или `tsc -b`). Режим сборки читает граф ссылок, топологически сортирует его и собирает зависимости *перед* зависящими — и пропускает любой проект, чьи входы не изменились с момента его `.tsbuildinfo`:

```bash
tsc --build packages/web   # собирает core, потом ui, потом web — в порядке зависимостей
tsc --build --verbose      # печатает, какие проекты актуальны, а какие пересобраны
tsc --build --clean        # удаляет все выводы + .tsbuildinfo (лекарство от устаревшего кэша)
tsc --build --force        # игнорирует .tsbuildinfo, пересобирает всё
```

<FlowDiagram
  label="tsc --build: DAG зависимостей — порядок сборки и инкрементальный пропуск по пакетам монорепозитория."
  hue="var(--accent)"
  perRow={3}
  nodes={[
    { id: "shared", label: "shared", sub: "composite, лист" },
    { id: "core", label: "core", sub: "→ shared" },
    { id: "ui", label: "ui", sub: "→ core" },
    { id: "api", label: "api", sub: "→ core" },
    { id: "web", label: "web", sub: "→ ui, core" },
    { id: "info", label: ".tsbuildinfo", sub: "на каждый проект" },
    { id: "skip", label: "Актуально?", sub: "хеш входов" },
    { id: "emit", label: "Emit .js + .d.ts", sub: "или переиспользовать" },
  ]}
  edges={[
    { from: "shared", to: "core" },
    { from: "core", to: "ui" },
    { from: "core", to: "api" },
    { from: "ui", to: "web" },
    { from: "core", to: "web" },
    { from: "info", to: "skip", label: "читать" },
    { from: "skip", to: "emit", label: "изменено" },
    { from: "skip", to: "emit", label: "без изменений: переиспользовать" },
  ]}
  caption="Режим сборки топологически сортирует DAG ссылок (shared → core → ui/api → web). .tsbuildinfo каждого проекта позволяет пропустить его, когда хеш входов не изменился, поэтому правка листа пересобирает лишь его и зависящих."
/>

## Инкрементальность и `.tsbuildinfo`

`.tsbuildinfo` — это кэш хешей файлов и сигнатур. При пересборке `tsc -b` сравнивает текущие входы с кэшированным состоянием; если входы проекта (и `.d.ts` его зависимостей) не изменились, он *актуален* и пропускается целиком. Важно: проекту нужно пересобирать зависящего, только когда его **публичный `.d.ts` реально изменился** — внутренняя правка, не меняющая генерируемые типы, оставляет зависящих в кэше (оптимизация по «сигнатуре»).

Именно поэтому редакторы шустрее: каждый проект-со-ссылкой проверяется изолированно, так что языковой сервер не переанализирует всё монорепо на каждом нажатии клавиши.

## Ловушки

**`error TS6306: Referenced project must have setting "composite": true`** — ты сослался на проект, который не composite. Добавь `composite: true` в *тот, на который ссылаются* (частая путаница: его добавляют ссылающемуся).

**Устаревший `.tsbuildinfo`** — если изменить что-то, что кэш не отслеживает (апгрейд компилятора с особенностью, ручная правка `dist`), сборка может ошибочно счесть проект актуальным. Лекарство: `tsc -b --clean`, затем пересборка, или `--force`.

**Циклические ссылки** — `A` ссылается на `B`, а `B` на `A` — отвергается (`TS6202`); DAG сборки должен быть ацикличным. Разорви цикл, вынеся общие типы в третий лист-проект, от которого зависят оба.

**Path mapping между проектами** — алиасы `paths` по-прежнему не переписываются при emit (урок 02), поэтому межпроектные импорты должны идти через настоящий entry/`exports` пакета, а не через алиас `paths`, который понимает только проверщик типов.

<DragOrder
  id="07-projref-drag-1"
  pieceSlug="04-project-references"
  lang="ru"
  prompt="Расставь, что делает `tsc --build packages/web` для графа web → ui → core и ui → core:"
  items={[
    { id: "a", label: "Читает references у web, затем транзитивно собирает ui и core в граф сборки" },
    { id: "b", label: "Топологически сортирует: у core нет зависимостей, значит он собирается первым" },
    { id: "c", label: "Для каждого проекта хеширует входы против .tsbuildinfo; пересобирает только при изменении, иначе пропускает" },
    { id: "d", label: "Собирает зависящих после их зависимостей: core → ui → web, генерируя .js + .d.ts" },
  ]}
/>

<Quiz
  id="07-projref-quiz-1"
  lessonSlug="04-project-references"
  lang="ru"
  question="Ты запускаешь `tsc -b` и получаешь TS6306: Referenced project 'packages/core' must have setting 'composite': true. Куда добавить composite?"
  choices={[
    { label: "В packages/core/tsconfig.json — проект, НА КОТОРЫЙ ссылаются, должен быть composite, чтобы генерировать .d.ts и .tsbuildinfo для потребления.", correct: true },
    { label: "В ссылающийся (packages/web), потому что он владеет массивом references.", misconception: "Ссылающийся перечисляет references, но требование к цели: composite должен быть у проекта, на который ссылаются. Добавляй в packages/core, не в packages/web." },
    { label: "В корневой tsconfig, который наследуют все проекты.", misconception: "Общая база может задать composite, но каждый проект-со-ссылкой реально должен быть composite. Ошибка называет именно packages/core — этому проекту (или его базе) нужна настройка." },
    { label: "Никуда; используй `tsc` без -b, и ошибка уйдёт.", misconception: "Обычный tsc игнорирует references и не соберёт граф верно. Project references требуют режим сборки (-b); фикс — сделать проект-со-ссылкой composite, а не отказываться от режима сборки." },
  ]}
/>

</Explanation>

<KeyTakeaway>
Project references разбивают монорепо на `tsconfig` по пакетам; пакет перечисляет зависимости в `references`, и каждый *проект, на который ссылаются*, должен задать `composite: true`. `composite` форсирует `declaration: true` (генерируемый `.d.ts` — межпроектный контракт типов, позволяющий зависящим не перечитывать исходник), включает кэширование `.tsbuildinfo` и требует полного покрытия `include`/`files`. Собирают **режимом сборки** — `tsc --build` / `tsc -b` — который собирает граф ссылок, топологически сортирует его, строит зависимости перед зависящими и **пропускает любой проект, чей хеш входов не изменился**; зависящий пересобирается, только когда публичный `.d.ts` зависимости реально меняется. Редакторы получают ту же изоляцию, так что языковой сервер проверяет по одному проекту, а не всё репо. Ловушки: `TS6306` значит, что *проект, на который ссылаются*, не composite (добавь туда, а не ссылающемуся); устаревший `.tsbuildinfo` лечится `tsc -b --clean`/`--force`; циклические ссылки (`TS6202`) требуют вынесения общих типов в третий лист; межпроектные импорты должны использовать настоящие точки входа, а не алиасы `paths`, которые генерируются непереписанными.
</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="07-projref-retrieval"
  lang="ru"
  questions={[
    { q: "Что делает `composite: true` и почему он форсирует `declaration`?", a: "composite помечает проект как тот, на который можно ссылаться. Он делает три вещи: (1) форсирует declaration: true, чтобы проект генерировал .d.ts; (2) включает инкрементальное кэширование .tsbuildinfo; (3) требует, чтобы каждый входной файл был покрыт include/files для надёжного хеширования входов. Он форсирует declaration, потому что весь смысл проекта-со-ссылкой в том, что зависящие читают его сгенерированный .d.ts — межпроектный контракт типов — вместо перечитывания исходника. Без declaration emit не было бы опубликованных типов, против которых зависящий мог бы проверяться, что разрушает и изоляцию, и скорость, ради которых существуют ссылки." },
    { q: "Что делает `tsc --build`, чего не делает обычный `tsc`, и как он использует граф ссылок?", a: "Обычный tsc игнорирует references и компилирует файлы одного проекта. Режим сборки (tsc --build / tsc -b) читает массивы `references`, собирает DAG зависимостей, топологически сортирует его и строит зависимости ПЕРЕД зависящими (например, core перед ui перед web). Для каждого проекта он сравнивает текущие входы с кэшированным .tsbuildinfo; если входы (и .d.ts зависимостей) не изменились, проект актуален и пропускается целиком. Зависящий пересобирается, только когда ПУБЛИЧНЫЙ .d.ts зависимости реально изменился — внутренняя правка без изменения сигнатуры типов оставляет зависящих в кэше. Полезные флаги: --verbose (показывает актуальные против пересобранных), --clean (удалить выводы + .tsbuildinfo), --force (пересобрать, игнорируя кэш)." },
    { q: "Назови три классических провала project references и их фиксы.", a: "(1) TS6306 'Referenced project must have setting composite: true' — у проекта, НА КОТОРЫЙ ссылаются (не у ссылающегося), нет composite; добавь его в tsconfig зависимости (или базу, которую он наследует). (2) Устаревший .tsbuildinfo — кэш ошибочно сообщает, что проект актуален, после неотслеживаемого изменения (апгрейд компилятора, ручная правка dist); лечится `tsc -b --clean`, затем пересборка, или `--force`. (3) Циклические ссылки (TS6202) — A ссылается на B, а B на A; DAG сборки должен быть ацикличным, поэтому вынеси общие типы в третий лист, от которого зависят оба. Бонусная ловушка: межпроектные алиасы paths не переживают emit (tsc их не переписывает), поэтому импортируй через настоящий entry/exports пакета, а не type-only алиас." },
  ]}
/>

<Recap lang="ru">
Теперь ты умеешь структурировать монорепо для быстрых изолированных сборок: `references` объявляют граф, `composite` (форсирующий `declaration`) делает проект потребляемым, `tsc -b` упорядочивает и кэширует сборку через `.tsbuildinfo`, и ты знаешь, как читать `TS6306`, чистить устаревшие кэши и разрывать циклы. Дальше — **build tooling**: ссылки организуют собственную работу `tsc`, но большинство проектов отдают emit в esbuild/swc/babel; мы увидим, почему эти транспиляторы *не проверяют типы*, почему тогда `isolatedModules` обязателен и как `tsc --noEmit` становится CI-гейтом типов рядом с бандлером.
</Recap>
