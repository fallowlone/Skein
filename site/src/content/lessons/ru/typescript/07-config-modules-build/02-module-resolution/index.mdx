---
concepts:
  - module
  - moduleResolution
  - node16-nodenext
  - bundler-resolution
  - esm-cjs-interop
  - verbatimModuleSyntax
deepensInto:
  - "typescript/07-config-modules-build/03-declaration-files"
estMin: 15
lang: ru
lessonType: topic
level: middle
order: 2
prereqs:
  - "typescript/07-config-modules-build/01-tsconfig-strictness"
slug: "02-module-resolution"
sources:
  - https://www.typescriptlang.org/docs/handbook/modules/theory.html
  - https://www.typescriptlang.org/docs/handbook/modules/reference.html
spiral:
  - module-systems
  - editor-vs-runtime
status: ready
summary: "`module` и `moduleResolution` решают, как строки import превращаются в файлы — классический баг «работает в редакторе, падает в рантайме». `node16`/`nodenext` применяют правила Node ESM; `bundler` (TS 5.0) их ослабляет. `tsc` не переписывает `paths` — это делает бандлер."
title: "module и moduleResolution: как строка импорта становится файлом"
track: "typescript"
unit: "07-config-modules-build"
---

import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";
import Infographic from "~/components/diagram/Infographic.astro";
import modeTradeoff from "./infographics/mode-tradeoff.svg?raw";

<Hook>
В редакторе всё было зелёным. `import { parse } from "./parser"` разрешался, автодополнялся, проходил проверку типов. CI собрал. Потом production упал: `ERR_MODULE_NOT_FOUND: Cannot find module '/app/dist/parser'`. Проект был на ESM (`"type": "module"`), а Node ESM требует `.js`-расширение в импорте — но `moduleResolution` был выставлен в `node`, старый режим, который это правило не применяет. TypeScript спокойно разрешил путь, который Node никогда бы не загрузил.
</Hook>

<Crux>`module` задаёт синтаксис, `moduleResolution` — поиск файлов; разойдись с рантаймом — редактор пропустит код, который тот отвергнет.</Crux>

<Explanation>

## Две настройки, две задачи

Когда видишь `ERR_MODULE_NOT_FOUND` в production на коде, который прошёл проверку типов, виновник почти всегда здесь: две настройки, которые выглядят связанными, но управляют совершенно разными вещами.

`module` решает, **какой синтаксис генерирует TypeScript** (`require`/`exports` для CommonJS, `import`/`export` для ES2020+). `moduleResolution` решает, **по какому алгоритму спецификатор импорта превращается в файл на диске**. Это раздельные вещи, и современный совет — задать `module`, а `moduleResolution` пусть следует из него:

```jsonc
{
  "compilerOptions": {
    "module": "nodenext",          // генерирует ESM или CJS по package.json "type"
    "moduleResolution": "nodenext" // разрешает так, как реально делает Node 16+
  }
}
```

Доступные режимы разрешения, от старых к новым:

<div data-lesson-visual class="overflow-x-auto my-6">

| moduleResolution | обходит | учитывает package.json `exports`? | нужен `.js` в ESM? | когда |
| --- | --- | --- | --- | --- |
| `classic` | только относительные + ambient | нет | нет | никогда (легаси, до 1.6) |
| `node` (он же `node10`) | `node_modules` как CJS `require` | нет | нет | только старые CJS-проекты |
| `node16` | CJS или ESM по типу пакета файла | да | да (в ESM-файлах) | Node, зафиксирован на поведении 16 |
| `nodenext` | то же, что node16, следит за новейшим Node | да | да (в ESM-файлах) | Node-библиотеки/приложения сегодня |
| `bundler` (TS 5.0) | `exports` + без расширений, без правила `.js` | да | нет | esbuild / Vite / webpack |

</div>

<Infographic
  svg={modeTradeoff}
  label="Сравнение двух режимов moduleResolution в TypeScript: устаревший node, который игнорирует exports в package.json и пропускает обязательное расширение .js, против nodenext, который учитывает условия exports и требует .js, так что редактор и рантайм Node разрешают пути одинаково."
  caption="Из этих пяти режимов баг решают два: node пропускает импорты, которые Node отвергает, а nodenext разрешает так, как реально работает рантайм — лекарство от «работает в редакторе, ERR_MODULE_NOT_FOUND в рантайме»."
  hue="var(--d-infra)"
/>

## Как идёт разрешение: bare-спецификаторы и `exports`

Для *относительного* импорта (`./parser`) резолвер пробует файлы рядом с импортирующим. Для *bare*-спецификатора (`lodash`, `@scope/pkg`) он поднимается по каталогам `node_modules`, затем смотрит в `package.json` пакета. При `node16`/`nodenext`/`bundler` он читает поле `exports` и выбирает цель, сопоставляя **условия** — `import` vs `require`, `types`, `node`, `default`:

```jsonc
// node_modules/@acme/sdk/package.json
{
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.mjs",   // выбран для ESM `import`
      "require": "./dist/index.cjs"   // выбран для CJS `require`
    }
  }
}
```

`exports` — это **граница инкапсуляции**: если путь не перечислен, его нельзя импортировать, даже если файл существует. `import "@acme/sdk/internal/util"` падает, если `./internal/util` нет в `exports`. Старый режим `node` полностью игнорирует `exports` — именно поэтому пакет разрешается при `node`, но ломается при `nodenext` (или наоборот).

<FlowDiagram
  label="Алгоритм разрешения модулей: как один спецификатор импорта превращается в файл в режиме node16/nodenext."
  hue="var(--accent)"
  perRow={3}
  nodes={[
    { id: "spec", label: "import \"X\"", sub: "спецификатор" },
    { id: "rel", label: "Относительный?", sub: "./ или ../" },
    { id: "file", label: "Пробовать файл + расширение", sub: ".ts/.tsx/.js" },
    { id: "walk", label: "Обход node_modules", sub: "вверх по дереву" },
    { id: "pkg", label: "Читать package.json", sub: "поле exports" },
    { id: "cond", label: "Совпадение условия", sub: "import/require/types" },
    { id: "target", label: "Разрешённый файл", sub: "+ его .d.ts" },
    { id: "fail", label: "Не найдено", sub: "TS2307 / рантайм" },
  ]}
  edges={[
    { from: "spec", to: "rel" },
    { from: "rel", to: "file", label: "да" },
    { from: "rel", to: "walk", label: "без префикса" },
    { from: "walk", to: "pkg" },
    { from: "pkg", to: "cond", label: "есть exports" },
    { from: "cond", to: "target" },
    { from: "file", to: "target", label: "найден" },
    { from: "file", to: "fail", label: "отсутствует" },
    { from: "cond", to: "fail", label: "нет совпадения" },
  ]}
  caption="Относительный спецификатор разрешается относительно каталога импортирующего; bare обходит node_modules и сопоставляет условие exports. Неверный режим moduleResolution пропускает шаги exports/расширения — так редактор и рантайм расходятся."
/>

## Неожиданное правило: `.js`-расширения в импортах TS

При `node16`/`nodenext` в ESM-файле импорт надо писать с расширением **`.js`**, хотя исходный файл — `parser.ts`:

```ts
// src/index.ts — ESM (package.json "type": "module")
import { parse } from "./parser.js"; // ✅ разрешает parser.ts, генерирует import "./parser.js"
import { parse } from "./parser";    // ❌ TS2835: относительный импорт требует расширения ".js"
import { parse } from "./parser.ts"; // ❌ TS5097: путь импорта не может оканчиваться на ".ts"
```

Ты пишешь расширение **результата** (`.js`), TypeScript проверяет по **исходнику** (`.ts`), а emit оставляет нетронутым, чтобы Node нашёл реальный файл в рантайме. Режим `bundler` отключает это требование, потому что бандлеры сами разрешают импорты без расширений.

<Inset kind="why" lang="ru">
Почему именно `.js`, а не `.ts`? Потому что правило дизайна TypeScript — он **никогда не переписывает спецификаторы импорта** в emit (с единственным исключением — смена *формы* импорта при `module: commonjs`). Строка, которую ты пишешь, — это строка, которую видит Node. Поскольку Node ESM требует расширение, а файл на диске после компиляции — `parser.js`, надо писать `parser.js`. Написать `parser.ts` — значит сгенерировать `parser.ts`, которого в рантайме не существует.
</Inset>

## Алиасы `paths`: tsc их проверяет, но не генерирует

`baseUrl`/`paths` позволяют писать `import { db } from "@/db"` вместо `../../../db`. Критически важно: **`tsc` разрешает их для проверки типов, но не переписывает в генерируемом JS** — литеральная `"@/db"` остаётся в выводе. В рантайме Node понятия не имеет, что такое `@/db`.

```jsonc
{ "compilerOptions": { "baseUrl": ".", "paths": { "@/*": ["src/*"] } } }
```

Поэтому `paths` работает, только если **что-то ещё** переписывает алиас: бандлер (Vite, esbuild, webpack с подходящим `resolve.alias`) или загрузчик в рантайме (`tsconfig-paths` или поле `imports` в `package.json`). Классический сбой: проверки типов и сборка через dev-сервер проходят, а голый `node dist/index.js` бросает `Cannot find module '@/db'`.

## Интероп: `esModuleInterop` и `verbatimModuleSyntax`

У CommonJS нет настоящего default-экспорта; `module.exports = x` — это присваивание всего пространства имён. `esModuleInterop: true` синтезирует default, чтобы `import express from "express"` работал против CJS-модуля, и генерирует хелперы `__importDefault`. Без него понадобилось бы `import express = require("express")` или `import * as express`.

`verbatimModuleSyntax` (TS 5.0, заменивший `importsNotUsedAsValues` и emit-половину забот `isolatedModules`) делает emit буквальным: обычный `import`/`export` генерируется как есть, а стираются только `import type` / `export type`. Это вынуждает явно указывать, что является type-only импортом — именно это нужно однофайловым транспиляторам:

```ts
import { type User, createUser } from "./user.js";
// emit: import { createUser } from "./user.js";  — `User` стёрт, `createUser` сохранён дословно
```

<DragOrder
  id="07-modres-drag-1"
  pieceSlug="02-module-resolution"
  lang="ru"
  prompt="Расставь шаги node16/nodenext при разрешении import { x } from '@acme/sdk' в ESM-файле:"
  items={[
    { id: "a", label: "Видит bare-спецификатор @acme/sdk (не относительный), значит ищем в node_modules" },
    { id: "b", label: "Поднимается по каталогам node_modules, пока не найдёт @acme/sdk/package.json" },
    { id: "c", label: "Читает поле exports пакета вместо угадывания main/index" },
    { id: "d", label: "Сопоставляет условие import (ESM-контекст), чтобы выбрать ./dist/index.mjs и его типы" },
  ]}
/>

<Quiz
  id="07-modres-quiz-1"
  lessonSlug="02-module-resolution"
  lang="ru"
  question="ESM-проект проходит проверку типов в VS Code, но `node dist/index.js` бросает Cannot find module './parser'. В tsconfig стоит moduleResolution: 'node'. В чём корневая причина?"
  choices={[
    { label: "moduleResolution: 'node' не применяет обязательное расширение файла из Node ESM, поэтому TS принял импорт без расширения, который Node отвергает — нужно 'nodenext', а импорт должен быть './parser.js'.", correct: true },
    { label: ".ts-файл не был скомпилирован; dist/parser.js не существует.", misconception: "Если бы компиляция пропустилась, ошибка упоминала бы .ts-исходник или была бы ошибкой сборки. Здесь parser.js существует; спецификатору импорта просто не хватает расширения .js, которого требует Node ESM и которое режим 'node' не применяет." },
    { label: "esModuleInterop выключен, поэтому default-импорт упал.", misconception: "esModuleInterop влияет на форму default-импорта для CJS-модулей, а не на то, разрешается ли относительный путь. Сбой — в отсутствующем расширении .js при неверном режиме разрешения." },
    { label: "Алиасы paths не были переписаны tsc.", misconception: "'./parser' — относительный импорт, а не алиас paths. Проблема в режиме разрешения, не требующем расширения .js, которое нужно Node ESM." },
  ]}
/>

</Explanation>

<KeyTakeaway>
`module` задаёт **синтаксис import/export, который генерирует TypeScript**; `moduleResolution` задаёт **алгоритм, отображающий спецификатор в файл**. Подгоняй их под рантайм: `nodenext`/`node16` следуют настоящим правилам Node ESM/CJS (читают `package.json` `exports`, сопоставляют условия `import`/`require`/`types` и **требуют расширение `.js`** в относительных ESM-импортах `.ts`-файлов); `bundler` (TS 5.0) сохраняет `exports`, но убирает правило расширения для esbuild/Vite; легаси-режим `node` полностью игнорирует `exports` — классический источник «разрешается в редакторе, `ERR_MODULE_NOT_FOUND` в рантайме». TypeScript **никогда не переписывает спецификаторы импорта** в emit, поэтому алиасы `paths`/`baseUrl` — только для проверки типов: бандлер или загрузчик должен их переписать, иначе `node dist/index.js` падает. `esModuleInterop` синтезирует default-экспорт для CJS-модулей; `verbatimModuleSyntax` делает emit буквальным, стирая только `import type`/`export type` — именно это нужно однофайловым транспиляторам.
</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="07-modres-retrieval"
  lang="ru"
  questions={[
    { q: "В чём разница между `module` и `moduleResolution` и почему неверный режим даёт «работает в редакторе, падает в рантайме»?", a: "`module` управляет синтаксисом, который ГЕНЕРИРУЕТ TypeScript — require/exports для CommonJS, import/export для ESM. `moduleResolution` управляет АЛГОРИТМОМ, превращающим спецификатор импорта в файл на диске. Баг рассинхрона возникает, потому что компилятор разрешает по одним правилам, а Node работает по другим: например, moduleResolution: 'node' игнорирует package.json `exports` и не требует расширения `.js`, которого требует Node ESM, так что TS пропускает импорт, который Node отвергнет с ERR_MODULE_NOT_FOUND. Установка module/moduleResolution в nodenext заставляет компилятор разрешать так же, как рантайм." },
    { q: "При node16/nodenext почему надо писать `./parser.js`, чтобы импортировать файл parser.ts, и какому общему правилу это подчиняется?", a: "Правило дизайна TypeScript — он никогда не переписывает спецификаторы импорта в emit (кроме смены формы импорта при module: commonjs). Строка, которую пишешь, — это строка, которую видит Node в рантайме. Node ESM требует расширение, а после компиляции файл на диске — parser.js, поэтому надо писать `./parser.js`. TypeScript проверяет этот спецификатор по исходнику parser.ts, но оставляет `.js` в выводе нетронутым. Написать `./parser` — ошибка (TS2835, требуется расширение), `./parser.ts` — ошибка (TS5097, нельзя оканчивать на .ts). Режим `bundler` отключает это требование, потому что бандлеры сами разрешают импорты без расширений." },
    { q: "Почему алиасы `paths`/`baseUrl` работают в редакторе и бандлере, но ломаются при голом `node dist/index.js`?", a: "Потому что tsc разрешает `paths` для проверки типов, но НЕ переписывает их в генерируемом JS — литеральная строка алиаса (например, '@/db') остаётся в выводе. В рантайме у Node нет понятия '@/db'. Алиас работает, только если что-то ещё выполняет переписывание: бандлер с подходящим resolve.alias (Vite/esbuild/webpack), загрузчик в рантайме вроде tsconfig-paths или поле package.json `imports`. Поэтому dev-сервер, который бандлит, работает, а `node dist/index.js` бросает Cannot find module '@/db'. Фикс — согласовать конфиг алиасов бандлера/рантайма с tsconfig или использовать subpath-паттерны `imports`, которые понимает сам Node." },
  ]}
/>

<Recap lang="ru">
Теперь ты можешь рассуждать о том, как строка импорта становится файлом: `module` генерирует синтаксис, `moduleResolution` находит цель, `exports`/условия гейтят доступ к пакету, правило `.js`-расширения спотыкает новичков в ESM, а `paths` нужен бандлер, чтобы пережить emit. Дальше — **declaration-файлы**: когда код разрешается, как поставлять *типы* к нему? `.d.ts`-файлы, ambient- и module-объявления и ловушки аугментации, которые молча не сливаются. Теперь, когда встречаешь `ERR_MODULE_NOT_FOUND` в production на коде, который редактор принял, первый вопрос — совпадает ли `moduleResolution` с рантаймом и написано ли расширение `.js` в относительном импорте?
</Recap>
