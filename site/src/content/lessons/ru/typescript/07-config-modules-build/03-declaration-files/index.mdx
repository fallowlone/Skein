---
concepts:
  - declaration-files
  - ambient-modules
  - global-augmentation
  - module-augmentation
  - declaration-emit
  - declaration-merging
deepensInto:
  - "typescript/08-real-world/04-library-generics"
estMin: 15
lang: ru
lessonType: topic
level: middle
order: 3
prereqs:
  - "typescript/07-config-modules-build/02-module-resolution"
slug: "03-declaration-files"
sources:
  - https://www.typescriptlang.org/docs/handbook/declaration-files/introduction.html
  - https://www.typescriptlang.org/docs/handbook/declaration-merging.html
spiral:
  - module-systems
  - public-api-surface
status: ready
summary: "`.d.ts`-файлы описывают формы без реализации: ambient-объявления, `declare module`, `declare global`, module-аугментация. Ловушки: аугментация, которая молча не сливается, случайное загрязнение глобального пространства и emit типов со ссылками на приватные пути."
title: ".d.ts-файлы: ambient-объявления, аугментация и публичная поверхность типов"
track: "typescript"
unit: "07-config-modules-build"
---

import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
Ты хотел добавить поле `user` в `Request` Express, чтобы auth-мидлвара могла типизировать `req.user`. Ты написал `declare global { namespace Express { interface Request { user?: User } } }` в `types.ts` — и аугментация просто не применилась; `req.user` остался ошибкой. В файле был импорт верхнего уровня, что сделало его *модулем*, поэтому `declare global` оказался в неверной области видимости, и слияние молча провалилось. Declaration merging безжалостно к тому, *где* ты объявляешь.
</Hook>

<Crux>`.d.ts`-файлы объявляют типы без runtime-кода; аугментация сливается лишь в верном контексте module-vs-script, иначе молча даёт no-op.</Crux>

<Explanation>

## `declare`: формы без реализаций

`.d.ts`-файл содержит только **информацию о типах** — исполняемый код запрещён, поэтому всё `declare`-ится. Так описывают значение, которое существует в рантайме, но которое TypeScript не видит (глобал из тега script, нетипизированный npm-пакет):

```ts
// globals.d.ts — ambient-объявления (нет import/export = это СКРИПТ, глобальная область)
declare const __APP_VERSION__: string;     // внедряется бандлером во время сборки
declare function gtag(...args: unknown[]): void;

interface Window {                          // сливается со встроенным Window
  dataLayer: unknown[];
}
```

Файл **без** import/export верхнего уровня — это *скрипт*: его объявления **глобальны**. Файл с **любым** import/export верхнего уровня — это *модуль*: его объявления локальны, если не обёрнуты в `declare global`. Это различие module-vs-script — корень большинства провалов аугментации.

## Ambient-модули: типизация нетипизированной зависимости

Когда ты `import`-ируешь JS-пакет без типов и без `@types/...`, TypeScript даёт ошибку `TS7016`. `declare module "name"` изобретает ambient-объявление модуля, чтобы закрыть пробел:

```ts
// shims.d.ts
declare module "legacy-chart" {
  export interface ChartOptions { width: number; height: number }
  export function render(el: HTMLElement, opts: ChartOptions): void;
  const _default: { version: string };
  export default _default;
}
```

Wildcard-форма типизирует целые классы импортов — например, импорты ассетов под бандлером:

```ts
declare module "*.svg" {
  const url: string;     // загрузчик Vite/webpack превращает файл в строку-URL
  export default url;
}
```

## Module-аугментация: расширение чужих типов

Чтобы добавить к типу, который экспортирует *другой модуль*, переоткрой этот модуль через `declare module` изнутри **своего модуля** (файл должен иметь собственный import/export):

```ts
// augment.ts — есть импорты, значит это МОДУЛЬ
import "express"; // гарантируем загрузку целевого модуля
declare module "express-serve-static-core" {
  interface Request { user?: { id: string; roles: string[] } } // сливается с Request из Express
}
export {}; // для надёжности: гарантирует контекст модуля
```

Для *глобальных* типов (`Window`, `Array`, `globalThis`) аугментацию надо обернуть в `declare global` — и охватывающий файл всё равно должен быть модулем:

```ts
// env.d.ts
export {}; // делает файл модулем, чтобы `declare global` был здесь легален
declare global {
  interface Window { __APP_VERSION__: string }
  namespace NodeJS { interface ProcessEnv { DATABASE_URL: string } }
}
```

<FlowDiagram
  label="Declaration emit: как экспорты исходного модуля становятся публичной поверхностью .d.ts, которую видит потребитель."
  hue="var(--accent)"
  perRow={3}
  nodes={[
    { id: "src", label: "index.ts", sub: "реализация + типы" },
    { id: "decl", label: "declaration: true", sub: "генерация .d.ts" },
    { id: "pub", label: "Экспортированные символы", sub: "публичная поверхность" },
    { id: "priv", label: "Внутренние типы", sub: "импортированы, но не экспортированы" },
    { id: "leak", label: "Утечка пути", sub: "ссылается на приватный файл" },
    { id: "dts", label: "index.d.ts", sub: "поставляется" },
    { id: "map", label: "declarationMap", sub: ".d.ts.map → src" },
    { id: "consumer", label: "Потребитель", sub: "go-to-definition" },
  ]}
  edges={[
    { from: "src", to: "decl" },
    { from: "decl", to: "pub", label: "экспортировано" },
    { from: "decl", to: "priv", label: "упомянуто" },
    { from: "pub", to: "dts" },
    { from: "priv", to: "leak", label: "не экспортировано" },
    { from: "leak", to: "dts", label: "TS4023/4094" },
    { from: "dts", to: "map" },
    { from: "map", to: "consumer" },
  ]}
  caption="declaration: true генерирует .d.ts, чья поверхность — твои экспортированные символы. Публичный символ, ссылающийся на неэкспортированный внутренний тип, утекает приватным путём (TS4023/4094); declarationMap связывает .d.ts с исходником для go-to-definition."
/>

## Emit и поставка типов

`declaration: true` заставляет `tsc` генерировать `.d.ts` рядом с каждым `.js`. `declarationMap: true` генерирует `.d.ts.map`, чтобы «go to definition» у потребителя прыгал в твой настоящий исходник, а не в сгенерированный `.d.ts`. В `package.json` ты указываешь потребителям на них через `types` (или условие `types` в `exports`):

```jsonc
{
  "types": "./dist/index.d.ts",
  "exports": { ".": { "types": "./dist/index.d.ts", "import": "./dist/index.js" } }
}
```

`typeRoots`/`types` (опции компилятора, отличные от поля в `package.json`) управляют тем, какие `@types`-пакеты *включаются*: `types: []` отключает авто-включение каждого `@types/*` из `node_modules` — частый фикс для «глобал из какого-то транзитивного `@types` загрязнил мой проект».

<Inset kind="why" lang="ru">
Почему публичный экспорт, ссылающийся на неэкспортированный тип, вызывает `TS4023: Exported variable has or is using name 'X' from external module but cannot be named`? Потому что `.d.ts` потребителя должен уметь *именовать* каждый тип в твоей публичной поверхности. Если `export function make(): Internal` возвращает тип `Internal`, который ты импортировал, но не экспортировал, сгенерированный `.d.ts` сослался бы на путь, который потребитель не может импортировать — сломанный публичный API. Фикс — экспортировать `Internal` тоже или встроить его форму. По той же причине ломается поставка типов, ссылающихся на приватные/`src`-пути: опубликованный `.d.ts` указывает на файлы, которых нет в пакете.
</Inset>

## `export type` vs `export`

В `.d.ts`, который ты пишешь руками, предпочитай `export type { Foo }` для type-only экспортов, чтобы потребители (и однофайловые транспиляторы) знали, что он не несёт runtime-значения. Обычный `export { Foo }`, где `Foo` — тип, работает для потребителей типов, но неоднозначен для инструментов, стирающих типы пофайлово.

<DragOrder
  id="07-dts-drag-1"
  pieceSlug="03-declaration-files"
  lang="ru"
  prompt="Расставь шаги, чтобы корректно аугментировать Request из Express полем `user`, чтобы слияние реально применилось:"
  items={[
    { id: "a", label: "Сделай файл модулем: дай ему import/export верхнего уровня (или добавь `export {}`)" },
    { id: "b", label: "Открой правильный целевой модуль: `declare module \"express-serve-static-core\"`" },
    { id: "c", label: "Переобъяви interface Request { user?: ... }, чтобы он СЛИЛСЯ с оригиналом" },
    { id: "d", label: "Убедись, что tsconfig включает .d.ts (через include/files), чтобы аугментация загрузилась" },
  ]}
/>

<Quiz
  id="07-dts-quiz-1"
  lessonSlug="03-declaration-files"
  lang="ru"
  question="Ты добавляешь `declare global { interface Window { x: number } }` в файл, где наверху уже есть `import { z } from './z'`, но нет `export`. Почему аугментация может вести себя неожиданно?"
  choices={[
    { label: "Импорт верхнего уровня делает файл модулем, так что `declare global` — корректная конструкция, но надо сохранить import/export, чтобы файл остался модулем; в файле-скрипте `declare global` — ошибка. Слияние работает только в контексте модуля.", correct: true },
    { label: "declare global никогда не работает в файле с импортами.", misconception: "Всё наоборот: declare global ТРЕБУЕТ контекста модуля (import или export). В файле-скрипте (без import/export) ты аугментировал бы глобальный Window напрямую без declare global, и использование declare global там — ошибка." },
    { label: "Window вообще нельзя аугментировать; надо править lib.dom.d.ts.", misconception: "Встроенные интерфейсы вроде Window спроектированы для аугментации через declaration merging. Файлы lib никогда не правят." },
    { label: "Для Window надо использовать `namespace` вместо `interface`.", misconception: "Window — интерфейс и сливается с интерфейсом. namespace нужен для случаев вроде NodeJS.ProcessEnv, где глобал сам является namespace." },
  ]}
/>

</Explanation>

<KeyTakeaway>
`.d.ts`-файлы несут **только типы** — каждое значение `declare`-ится, runtime-кода нет. Решающее правило — module-vs-script: файл **без** import/export верхнего уровня — это *скрипт*, чьи объявления **глобальны**; любой import/export верхнего уровня делает его *модулем*, чьи объявления локальны. `declare module "pkg"` изобретает ambient-типы для нетипизированной зависимости (а `declare module "*.svg"` типизирует импорты ассетов); **module-аугментация** переоткрывает модуль чужого пакета, чтобы слить в него члены, а **global-аугментация** оборачивает добавления в `declare global` — оба сливаются, только когда файл в *правильном* контексте, поэтому случайный `import` (или отсутствующий `export {}`) делает аугментацию молчаливым no-op. На стороне emit `declaration: true` создаёт публичную поверхность `.d.ts`, а `declarationMap` связывает go-to-definition с исходником; публичный экспорт, ссылающийся на *неэкспортированный* внутренний тип, утекает приватным путём (`TS4023`/`TS4094`), а поставка `.d.ts`, указывающих на `src`/приватные файлы, ломает потребителей. Предпочитай `export type` для type-only экспортов.
</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="07-dts-retrieval"
  lang="ru"
  questions={[
    { q: "Что за правило module-vs-script для .d.ts-файлов и почему оно ломает аугментацию?", a: "Файл БЕЗ import/export верхнего уровня — это СКРИПТ: его объявления попадают в глобальную область, и глобалы аугментируются напрямую. Файл с ЛЮБЫМ import/export верхнего уровня — это МОДУЛЬ: его объявления локальны для этого модуля, и чтобы тронуть глобалы, добавления надо обернуть в `declare global`. Аугментации ломаются при неверном контексте: добавление импорта в файл с глобальной аугментацией переключает его в модуль (и голое глобальное объявление больше не сливается глобально), и наоборот — `declare global` легален только внутри модуля. Фикс — управлять контекстом явно: добавить `export {}` для принудительного контекста модуля или убрать лишние импорты для сохранения контекста скрипта." },
    { q: "Как типизировать нетипизированную зависимость против расширения уже типизированной?", a: "Нетипизированная зависимость (нет типов, нет @types): пиши `declare module \"pkg-name\" { ... }` — ambient-объявление модуля, изобретающее публичные типы пакета, включая wildcard-форму вроде `declare module \"*.svg\"` для импортов ассетов под бандлером. Расширение уже типизированного модуля (module-аугментация): изнутри своего модуля переоткрой цель через `declare module \"настоящий-модуль\"` и переобъяви его интерфейс, чтобы члены СЛИЛИСЬ (например, добавив `user?` в Request из express-serve-static-core). Для глобалов оберни добавления в `declare global` и держи файл модулем через `export {}`. Аугментация сливается с существующим типом; ambient `declare module` для имени, у которого уже есть типы, конфликтовал бы вместо слияния." },
    { q: "Что поставляет declaration emit и какие ловушки утечки/загрязнения?", a: "`declaration: true` заставляет tsc генерировать .d.ts рядом с каждым .js — его поверхность это ровно твои ЭКСПОРТИРОВАННЫЕ символы. `declarationMap: true` добавляет .d.ts.map, чтобы go-to-definition потребителя прыгал в твой настоящий исходник. Ловушка 1 — утечка приватного пути: публичный экспорт, чей тип ссылается на внутренний тип, который ты импортировал, но не экспортировал, даёт TS4023/TS4094, потому что .d.ts потребителя не может именовать этот тип; фикс — экспортировать его или встроить форму. Ловушка 2 — поставка .d.ts, ссылающихся на src/приватные пути, отсутствующие в опубликованном пакете, что ломает разрешение у потребителей. Ловушка 3 — случайное загрязнение глобалов: транзитивный @types/* в node_modules авто-включает свои глобалы; `\"types\": []` (опция компилятора) отключает это авто-включение." },
  ]}
/>

<Recap lang="ru">
Теперь ты умеешь автором и поставщиком создавать чистую поверхность типов: `.d.ts` несёт только типы, правило module-vs-script решает, глобальны твои объявления или локальны, `declare module` и `declare global` расширяют зависимости и глобалы (когда контекст верен), а `declaration`/`declarationMap` генерируют публичную поверхность, которая не должна утекать приватными путями. Дальше — **project references**: когда одна кодовая база разрастается во многие пакеты, как `tsc --build` упорядочивает работу, делит типы между пакетами и остаётся быстрым с `.tsbuildinfo`?
</Recap>
