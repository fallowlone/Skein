---
concepts:
- tuple-length-arithmetic
- type-level-add-subtract
- type-level-compare
- template-literal-number-parse
- bounded-recursion
deepensInto:
- typescript/05-type-level-programming/05-limits-and-perf
estMin: 18
lang: ru
lessonType: topic
level: senior
order: 4
prereqs:
- typescript/05-type-level-programming/01-recursion-in-types
slug: "04-type-arithmetic"
sources:
- https://github.com/type-challenges/type-challenges
- https://www.typescriptlang.org/docs/handbook/2/conditional-types.html
spiral:
- recursion
- tuples
status: ready
summary: В TypeScript нет чисел на уровне типов, поэтому N кодируют как кортеж длины N — конкатенация это сложение, infer с начала это вычитание, а сравнение длин это порядок; всё ограничено и медленно по построению.
title: Арифметика на уровне типов
track: typescript
unit: "05-type-level-programming"
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
Вы типизируете библиотеку матриц фиксированного размера: `Vec<3>` плюс `Vec<3>` — нормально, но `Vec<3>` плюс `Vec<4>` должно быть ошибкой компиляции, а `dot(Vec<3>, Vec<3>)` должен возвращать `number` только когда длины совпадают. Чтобы выразить «3 + 4» или «3 меньше 4?», компилятору нужна арифметика — но в TypeScript нет `+` на уровне типов. Приём, на котором построен весь арифметический канон type-challenges: число `N` представляется кортежем длины `N`, а математика делается ростом, усечением и измерением кортежей. Работает примерно до тысячи — и роскошно медленно.
</Hook>

<Crux>Чисел на уровне типов нет: кодируйте `N` как кортеж длины `N` — сложение это конкатенация, вычитание это infer с начала.</Crux>

<Explanation>

## Представление: число — это длина кортежа

Спросите себя: если `3 + 4` нельзя написать как выражение типа, как вообще проверить на этапе компиляции, что два вектора одной размерности? Весь приём умещается в одну идею. На уровне типов нет оператора `+`. Стандартная кодировка: число `N` — это *любой* кортеж с `length`, равным `N`. Постройте его, хвостово-рекурсивно добавляя `unknown`, пока длина не совпадёт:

```ts
type BuildTuple<N extends number, Acc extends unknown[] = []> =
  Acc["length"] extends N ? Acc : BuildTuple<N, [unknown, ...Acc]>;

type Three = BuildTuple<3>;
//   ^? type Three = [unknown, unknown, unknown]
type Len = Three["length"];
//   ^? type Len = 3
```

Это в точности хвостово-рекурсивный `Build` из урока 1. Вся арифметическая библиотека сводится к: преобразовать числа в кортежи, манипулировать кортежами, считать `["length"]` обратно.

<FlowDiagram
  label="BuildTuple<3>: добавляем unknown в аккумулятор, пока его длина не достигнет цели, затем считываем длину обратно как число."
  hue="var(--accent)"
  perRow={3}
  nodes={[
    { id: "n0", label: "Acc=[]", sub: "длина 0" },
    { id: "n1", label: "Acc=[u]", sub: "длина 1" },
    { id: "n2", label: "Acc=[u,u]", sub: "длина 2" },
    { id: "n3", label: "Acc=[u,u,u]", sub: "длина 3 = N" },
    { id: "n4", label: "читаем длину", sub: "-> 3" },
  ]}
  edges={[
    { from: "n0", to: "n1", label: "push" },
    { from: "n1", to: "n2", label: "push" },
    { from: "n2", to: "n3", label: "push" },
    { from: "n3", to: "n4", label: "стоп, замер" },
  ]}
  caption="Каждый хвостово-рекурсивный шаг растит аккумулятор на один элемент, пока Acc['length'] не станет равным N; кортеж тогда кодирует число, а ['length'] его декодирует."
/>

## Add: конкатенация двух кортежей

Сложение `A + B` — это построить оба кортежа, спредить их в один и считать совместную длину:

```ts
type Add<A extends number, B extends number> =
  [...BuildTuple<A>, ...BuildTuple<B>]["length"] & number;

type Seven = Add<3, 4>;
//   ^? type Seven = 7
```

`& number` сужает `["length"]` (в общем случае типизированную как `number`) обратно к литералу. Конкатенация — это `+` на уровне типов.

## Subtract: вывести разницу с начала

Вычитание `A - B` строит кортеж для `A`, затем срезает `B` элементов с начала, матча префикс `BuildTuple<B>` и выводя остаток:

```ts
type Subtract<A extends number, B extends number> =
  BuildTuple<A> extends [...BuildTuple<B>, ...infer Rest]
    ? Rest["length"]
    : never;   // A < B: такого префикса нет, переполнение вниз -> never

type Two = Subtract<5, 3>;
//   ^? type Two = 2
type Under = Subtract<3, 5>;
//   ^? type Under = never   (отрицательные числа непредставимы)
```

Паттерн `[...BuildTuple<B>, ...infer Rest]` говорит «кортеж, начинающийся с B элементов, где Rest — то, что после». `Rest["length"]` — это `A - B`. Для отрицательных нет представления, поэтому недобор проваливается в `never` — это намеренный сигнал, а не баг.

## Compare: гонка длин

`GreaterThan<A, B>` идёт по обоим кортежам синхронно; кто опустеет первым — тот меньше:

```ts
type GreaterThan<A extends number, B extends number> =
  BuildTuple<B> extends [...BuildTuple<A>, ...infer _Rest]
    ? false        // B не короче A  ->  A <= B
    : true;        // B НЕ является префикс-надмножеством A  ->  A > B

type G1 = GreaterThan<5, 3>;   // ^? type G1 = true
type G2 = GreaterThan<3, 5>;   // ^? type G2 = false
type G3 = GreaterThan<4, 4>;   // ^? type G3 = false
```

## Парсинг числовых литералов из строк

Когда вы пишете тип роутера, вытаскивающий `:42` из строки пути, вам нужен обратно числовой тип, а не строка. `${number}` в шаблонном литерале матчит числовую строку, но захваченный `infer` всё ещё *строка*. Чтобы получить *числовой* тип, выведите его, а затем сразу скоэрсите через ограниченный infer:

```ts
type ParseInt<S extends string> =
  S extends `${infer N extends number}` ? N : never;

type P = ParseInt<"42">;
//   ^? type P = 42   (`infer N extends number` делает приведение string->number)
```

`infer N extends number` (TS 4.8+) одновременно захватывает и приводит: он пытается интерпретировать совпавшую подстроку как числовой литеральный тип. Так типы роутера/путей вытаскивают сегменты вроде `:42` в числа.

<Inset kind="why" lang="ru">
Почему это ограничено и медленно? Каждое число материализуется как реальный кортеж этой длины, а `BuildTuple<N>` выполняет N хвостово-рекурсивных шагов. `Add<400, 400>` строит два 400-элементных кортежа и конкатенирует 800-элементный — и компилятор делает это на каждой ссылке, на каждом нажатии клавиши в редакторе. Потолок длины кортежа сидит около лимита рекурсии (~1000 с хвостовыми вызовами), поэтому арифметика выше нескольких сотен либо падает с «excessively deep», либо заметно тормозит проверку типов. Это инструмент корректности для малых фиксированных размеров, а не общий калькулятор.
</Inset>

<Quiz
  id="04-arith-quiz-1"
  lessonSlug="04-type-arithmetic"
  lang="ru"
  question="Почему `Subtract<3, 5>` разрешается в `never`, а не в `-2`?"
  choices={[
    { label: "Числа кодируются длиной кортежа, а кортежа отрицательной длины не бывает; BuildTuple<3> не может совпасть с префикс-паттерном [...BuildTuple<5>, ...infer Rest], поэтому условие проваливается в ветку `never` — недобор непредставим.", correct: true },
    { label: "TypeScript автоматически округляет отрицательные результаты до never", misconception: "Никакого автоматического округления нет; never берётся из неудавшегося матча префикса, потому что у 3-кортежа нет 5-элементного префикса." },
    { label: "Вычитание на уровне типов невозможно", misconception: "Вычитание работает для A >= B через infer с начала; только отрицательный (недобор) случай даёт never." },
    { label: "Пересечение `& number` даёт never", misconception: "& number сужает длину до литерала; не оно даёт never здесь — это делает неудавшийся матч префикса." },
  ]}
/>

<DragOrder
  id="04-arith-drag-1"
  pieceSlug="04-type-arithmetic"
  lang="ru"
  prompt="Расставьте вычисление Add<2, 3> от кодирования чисел до итогового числового типа."
  items={[
    "BuildTuple<2> -> [unknown, unknown]",
    "BuildTuple<3> -> [unknown, unknown, unknown]",
    "Спредим оба в один кортеж -> [unknown, unknown, unknown, unknown, unknown]",
    "Читаем ['length'] совместного кортежа -> 5",
    "Сужаем через & number к литеральному типу 5",
  ]}
/>

</Explanation>

<KeyTakeaway>Без чисел на уровне типов кодируйте `N` как кортеж длины `N`. Складывайте, конкатенируя два кортежа и читая `["length"]`; вычитайте, матча `[...BuildTuple<B>, ...infer Rest]` и читая `Rest["length"]` (недобор даёт `never` — отрицательные непредставимы); сравнивайте, проверяя, является ли один кортеж префикс-надмножеством другого. Парсите числовые литералы через `` `${infer N extends number}` ``. Это корректно для малых фиксированных размеров, но ограничено около лимита рекурсии ~1000 и медленно — каждое число это реальный кортеж, который компилятор материализует на каждой ссылке.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="04-arith-retrieval"
  lang="ru"
  questions={[
    { q: "Как числа представляются на уровне типов и как реализовать Add и Subtract на этом представлении?", a: "Число N представляется кортежем, длина которого равна N (элементы — заполнители, обычно `unknown`). Строят его хвостово-рекурсивно: type BuildTuple<N, Acc = []> = Acc['length'] extends N ? Acc : BuildTuple<N, [unknown, ...Acc]>. Add<A,B> = конкатенировать два кортежа и считать длину: [...BuildTuple<A>, ...BuildTuple<B>]['length'] & number — конкатенация ЕСТЬ сложение, потому что длины складываются. Subtract<A,B> = срезать B элементов с начала, выводя остаток: BuildTuple<A> extends [...BuildTuple<B>, ...infer Rest] ? Rest['length'] : never. infer захватывает всё после первых B элементов; его длина — это A - B. Когда A < B, префикс не совпадает, и оно разрешается в never, потому что отрицательные длины непредставимы." },
    { q: "Как сравнить два числа на уровне типов и как распарсить числовой литерал из строки?", a: "Сравнение гоняет длины кортежей через матч префикса. GreaterThan<A,B> = BuildTuple<B> extends [...BuildTuple<A>, ...infer _] ? false : true — если кортеж B начинается с кортежа A как префикса, то B >= A (значит A не больше), иначе A > B. Равные числа дают false (>= выполняется). Парсинг использует шаблонный литерал с ограниченным infer: type ParseInt<S> = S extends `${infer N extends number}` ? N : never. `infer N extends number` (TS 4.8+) одновременно матчит числовую подстроку И приводит её из строки к числовому литеральному типу — без ограничения `extends number` вы получили бы лишь строку, а не пригодный числовой тип." },
    { q: "Почему арифметика на длинах кортежей и ограничена, и медленна, и для чего она реально хороша?", a: "Ограничена: каждое число материализуется как реальный кортеж этой длины, поэтому наибольшее число, что можно построить, ограничено лимитом рекурсии/инстанцирования — примерно 1000 с устранением хвостовой рекурсии, гораздо меньше для наивной рекурсии. Превышение запускает 'Type instantiation is excessively deep and possibly infinite.' Медленна: BuildTuple<N> делает N рекурсивных инстанцирований, а операции вроде Add<400,400> материализуют и конкатенируют сотни элементов кортежа — компилятор переоценивает их на каждой ссылке, поэтому тяжёлая арифметика на типах даёт заметный лаг редактора и медленный tsc. Это правильный инструмент ТОЛЬКО для малых фиксированных размеров: валидация арности массива, типизация векторов/матриц фиксированной длины, контроль длины кортежа в API. Для всего, что требует реального диапазона чисел, делайте проверку в рантайме, а не на уровне типов." },
  ]}
/>

<Recap lang="ru">
У арифметики на уровне типов одна центральная идея: поскольку в TypeScript нет чисел на уровне типов, представляйте `N` как кортеж длины `N`. BuildTuple растит кортеж хвостово-рекурсивно; Add конкатенирует и читает `["length"]`; Subtract выводит остаток после префикса длины B (недобор в `never`); GreaterThan проверяет отношения префикс-надмножества; а `` `${infer N extends number}` `` парсит числовые литералы из строк. Вся схема корректна для малых фиксированных размеров и полезна для типизированных API фиксированной длины и валидации арности массива — но ограничена около лимита рекурсии ~1000 и материализует реальные кортежи, которые компилятор постоянно переоценивает. Теперь, когда `Vec<3>` и `Vec<4>` не складываются, вы знаете, что компилятор считает элементы кортежей; а когда он тормозит — знаете почему.
</Recap>
