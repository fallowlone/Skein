---
concepts:
- recursive-conditional-types
- tuple-recursion
- tail-recursion-elimination
- instantiation-depth
- base-case
deepensInto:
- typescript/05-type-level-programming/04-type-arithmetic
estMin: 16
lang: ru
lessonType: topic
level: senior
order: 1
prereqs:
- typescript/04-type-system-deep/02-the-infer-keyword
slug: "01-recursion-in-types"
sources:
- https://www.typescriptlang.org/docs/handbook/2/conditional-types.html#inferring-within-conditional-types
- https://www.typescriptlang.org/docs/handbook/release-notes/typescript-4-5.html#tail-recursion-elimination-on-conditional-types
- https://github.com/type-challenges/type-challenges
spiral:
- recursion
- conditional-types
status: ready
summary: Рекурсивный условный тип отрезает один элемент кортежа, рекурсивно идёт по остатку и останавливается на базовом случае — как рекурсия в рантайме, но интерпретатор — это компилятор, а глубина конечна.
title: Рекурсия в типах
track: typescript
unit: "05-type-level-programming"
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";
import Infographic from "~/components/diagram/Infographic.astro";
import depthWall from "./infographics/depth-wall.svg?raw";

<Hook>
Вы пишете типы для крошечной роутер-библиотеки. Путь `"/users/:id/posts/:slug"` должен давать `{ id: string; slug: string }` — автоматически, на этапе компиляции, без шага кодогенерации. Чтобы туда добраться, нужно, чтобы система типов прошла структуру элемент за элементом и накапливала результат. Это рекурсия, а компилятор — ваш интерпретатор. Он с радостью выполнит вашу типовую программу — ровно до того момента, когда напечатает `Type instantiation is excessively deep and possibly infinite.` и сдастся. Понимание того, где стоит эта стена и как TypeScript 4.5 её сдвинул, отделяет умный тип от поставленного в прод.
</Hook>

<Crux>Рекурсивный тип отрезает элемент через `[infer H, ...infer T]`, рекурсирует по остатку, встаёт на базе — глубина ограничена.</Crux>

Через десять минут вы будете точно знать, почему компилятор сдаётся, и как отодвинуть стену с 50 уровней до 1000.

<Explanation>

## Форма любого рекурсивного типа

У рекурсивного условного типа ровно две ветки, как у корректной рекурсивной функции:

- **базовый случай**, который возвращает конкретный результат без рекурсии, и
- **рекурсивный случай**, который делает немного работы и ссылается на самого себя на меньшем входе.

«Меньший вход» — это почти всегда кортеж с отрезанной головой. Главный рычаг — `[infer Head, ...infer Tail]`. В ветке `extends` условного типа этот паттерн матчит непустой кортеж, связывает первый элемент с `Head`, а *всё, что после него*, — с `Tail`. Базовый случай — пустой кортеж `[]`, который не проходит проверку `extends` и проваливается в ветку `else`.

## Reverse: канонический обход

```ts
type Reverse<T extends readonly unknown[]> =
  T extends readonly [infer Head, ...infer Tail]
    ? [...Reverse<Tail>, Head]
    : [];

type R = Reverse<[1, 2, 3]>;
//   ^? type R = [3, 2, 1]
```

Читайте это как трассировку. `Reverse<[1,2,3]>` матчится, `Head = 1`, `Tail = [2,3]`, поэтому превращается в `[...Reverse<[2,3]>, 1]`. Это рекурсирует в `[...Reverse<[3]>, 2, 1]`, затем `[...Reverse<[]>, 3, 2, 1]`, и `Reverse<[]>` попадает в базовый случай `[]`. Накопленные спреды схлопываются в `[3, 2, 1]`.

<FlowDiagram
  label="Разворачивание Reverse: каждый шаг отделяет [Head, ...Tail] и ставит Head за рекурсивный результат вплоть до базового случая пустого кортежа."
  hue="var(--accent)"
  perRow={3}
  nodes={[
    { id: "n0", label: "Reverse<[1,2,3]>", sub: "H=1 T=[2,3]" },
    { id: "n1", label: "Reverse<[2,3]>", sub: "H=2 T=[3]" },
    { id: "n2", label: "Reverse<[3]>", sub: "H=3 T=[]" },
    { id: "n3", label: "Reverse<[]>", sub: "база -> []" },
    { id: "n4", label: "схлоп", sub: "[3,2,1]" },
  ]}
  edges={[
    { from: "n0", to: "n1", label: "снять 1" },
    { from: "n1", to: "n2", label: "снять 2" },
    { from: "n2", to: "n3", label: "снять 3" },
    { from: "n3", to: "n4", label: "разворот" },
  ]}
  caption="Спуск укорачивает кортеж на один элемент за шаг; пустой кортеж — это базовый случай, который останавливает спуск и запускает разворот."
/>

## Join и Includes — тот же скелет, другое накопление

```ts
type Join<T extends readonly string[], D extends string> =
  T extends readonly [infer H extends string, ...infer R extends string[]]
    ? R extends [] ? H : `${H}${D}${Join<R, D>}`
    : "";

type J = Join<["a", "b", "c"], "-">;
//   ^? type J = "a-b-c"

type Includes<T extends readonly unknown[], U> =
  T extends readonly [infer H, ...infer R]
    ? [H] extends [U] ? [U] extends [H] ? true : Includes<R, U> : Includes<R, U>
    : false;

type Inc = Includes<[1, 2, 3], 2>;
//   ^? type Inc = true
```

`Join` несёт разделитель `D` вдоль рекурсии и обрабатывает последний элемент особым случаем (`R extends []`), чтобы строка не заканчивалась лишним разделителем. `Includes` коротко замыкается в момент, когда находит совпадение — рекурсивный вызов происходит только в ветке «ещё не найдено». (Обратите внимание на обёртки `[H] extends [U]` и `[U] extends [H]` — это отключает дистрибуцию, чтобы сравнение было точным; с этим приёмом вы встретитесь в следующем уроке.)

<Inset kind="why" lang="ru">
Почему `infer H extends string`? Ограничение на `H` позволяет сразу использовать `H` в шаблонном литерале `${H}` без второго условия, доказывающего, что это строка. Ограниченный `infer` (TS 4.7+) — это то, как сохраняют рекурсивные строковые типы читаемыми, а не вкладывают `H extends string ? ... : never` на каждом шаге.
</Inset>

## Стена глубины — и почему важна хвостовая позиция

Когда вы видите «Type instantiation is excessively deep and possibly infinite.» (ошибку «слишком глубокое инстанцирование типа, возможно бесконечное»), первый вопрос — это неверный базовый случай или просто не-хвостовой вызов. Одно сообщение покрывает оба, а фикс разный. Компилятор ограничивает рекурсию, чтобы проверка типов не зависала. Исторически рекурсивный условный тип взрывался около **50** уровней самоссылки с ошибкой:

```ts
type BuildBad<N extends number, Acc extends unknown[] = []> =
  Acc["length"] extends N ? Acc : [unknown, ...BuildBad<N, [unknown, ...Acc]>];
//                                            ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
// Error: Type instantiation is excessively deep and possibly infinite.
// (рекурсивный вызов обёрнут в [unknown, ...] — НЕ в хвостовой позиции)
```

TypeScript 4.5 добавил **устранение хвостовой рекурсии для условных типов**. Когда рекурсивный вызов — это *весь* результат ветки условия (вокруг него ничего нет), компилятор переиспользует кадр вычисления вместо вложения, и эффективный лимит подскакивает примерно до **1000**. Фикс — протащить аккумулятор и вернуть его напрямую:

```ts
type Build<N extends number, Acc extends unknown[] = []> =
  Acc["length"] extends N
    ? Acc                                     // базовый случай
    : Build<N, [unknown, ...Acc]>;            // ХВОСТОВОЙ вызов: вся ветка ЕСТЬ рекурсия

type T999 = Build<999>["length"];
//   ^? type T999 = 999   (без ошибки — устранение хвостовой рекурсии)
```

Структурная разница: `[unknown, ...Build<...>]` оборачивает вызов, поэтому каждый кадр должен ждать внутренний результат — не устраняется. `Build<N, [unknown, ...Acc]>` *и есть* результат — устраняется. Перестройте рекурсию так, чтобы самовызов стоял в хвостовой позиции, и стена сдвинется с 50 до 1000.

<Infographic
  svg={depthWall}
  label="Сравнение бок о бок не-хвостового рекурсивного вызова и хвостового вызова: не-хвостовой вызов оборачивает самовызов в кортеж, держит каждый кадр на стеке и упирается в стену около 50 уровней с ошибкой «слишком глубокое инстанцирование»; хвостовой вызов из TS 4.5 делает самовызовом всю ветку, переиспользует кадр вычисления через устранение хвостовой рекурсии, протягивает аккумулятор и поднимает лимит примерно до 1000, так что Build<999> проходит проверку типов."
  caption="Единственная структурная разница — обёрнут ли самовызов или он И ЕСТЬ вся ветка — и эта одна позиция решает, стоит ли стена глубины на ~50 или ~1000."
  hue="var(--d-infra)"
/>

<Quiz
  id="01-rec-quiz-1"
  lessonSlug="01-recursion-in-types"
  lang="ru"
  question="Почему `type Bad<T> = T extends [infer H, ...infer R] ? [Bad<R>, H] : []` упирается в лимит глубины намного раньше хвостовой версии?"
  choices={[
    { label: "Рекурсивный вызов `Bad<R>` вложен в кортеж `[Bad<R>, H]`, поэтому он не в хвостовой позиции, и устранение хвостовой рекурсии из TypeScript 4.5 неприменимо — каждый кадр остаётся на стеке.", correct: true },
    { label: "Кортежи нельзя возвращать из условных типов", misconception: "Кортежи — вполне допустимый результат условных типов; проблема в позиции вызова, а не в форме возврата." },
    { label: "`infer` запрещён в рекурсивных типах", misconception: "infer — это ровно то, чем рекурсия по кортежу отрезает элементы; он обязателен, а не запрещён." },
    { label: "Базовый случай `[]` неверен и вызывает бесконечную рекурсию", misconception: "Базовый случай корректен; пустой кортеж завершает спуск. Лимит упирается из-за не-хвостового вложения, а не из-за отсутствующей базы." },
  ]}
/>

<DragOrder
  id="01-rec-drag-1"
  pieceSlug="01-recursion-in-types"
  lang="ru"
  prompt="Расставьте вычисление Reverse<[1, 2, 3]> от первого матча до итогового схлопнутого результата."
  items={[
    "Reverse<[1,2,3]> матчится, Head=1, Tail=[2,3] -> [...Reverse<[2,3]>, 1]",
    "Reverse<[2,3]> матчится, Head=2, Tail=[3] -> [...Reverse<[3]>, 2, 1]",
    "Reverse<[3]> матчится, Head=3, Tail=[] -> [...Reverse<[]>, 3, 2, 1]",
    "Reverse<[]> попадает в базовый случай и возвращает []",
    "Спреды схлопываются в итоговый тип [3, 2, 1]",
  ]}
/>

</Explanation>

<KeyTakeaway>Любой рекурсивный тип — это базовый случай плюс рекурсивный случай, идущий по `[infer Head, ...infer Tail]`. Отсутствующая или неверная база — это бесконечный тип; не-хвостовой рекурсивный вызов упирается в стену ~50 уровней и падает с «Type instantiation is excessively deep and possibly infinite.» Поставьте самовызов в хвостовую позицию (вся ветка — это вызов, с аккумулятором), и устранение хвостовой рекурсии из TypeScript 4.5 поднимет лимит до ~1000.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="01-rec-retrieval"
  lang="ru"
  questions={[
    { q: "Какие две ветки обязаны быть у любого корректного рекурсивного условного типа и что делает паттерн [infer Head, ...infer Tail] в рекурсивной ветке?", a: "Базовый случай, который возвращает конкретный тип без рекурсии (обычно ветка пустого кортежа, возвращающая []), и рекурсивный случай, который делает немного работы и вызывает себя на меньшем входе. В рекурсивной ветке [infer Head, ...infer Tail] матчит непустой кортеж, связывая первый элемент с Head, а все остальные — с Tail, поэтому каждый шаг укорачивает вход на один элемент. Пустой кортеж не проходит этот матч и проваливается в базовый случай, завершая спуск. Без корректной базы спуск не останавливается, и тип бесконечен." },
    { q: "Что такое устранение хвостовой рекурсии для условных типов, когда оно применяется и как меняет лимит глубины?", a: "Введено в TypeScript 4.5: когда рекурсивный вызов условного типа — это ВЕСЬ результат ветки (вокруг ничего нет — например, `... ? Acc : Self<N, [unknown, ...Acc]>`), компилятор переиспользует кадр вычисления вместо вложения нового. Это поднимает эффективный лимит рекурсии примерно с 50 до примерно 1000. Оно НЕ применяется, когда вызов вложен, например [unknown, ...Self<...>] или [Self<...>, H], потому что оборачивающий кортеж должен дождаться внутреннего результата. Практический приём: протащить параметр-аккумулятор и вернуть рекурсивный вызов напрямую как результат ветки." },
    { q: "Что именно вызывает ошибку 'Type instantiation is excessively deep and possibly infinite.' и какие две разные корневые причины надо проверить?", a: "Она срабатывает, когда компилятор превышает бюджет глубины инстанцирования типа. Две разные причины: (1) действительно бесконечный тип — отсутствующая или неверная база, поэтому рекурсия не завершается; проверьте, что ветка пустого кортежа (или иной терминальной) возвращает конкретный тип и не рекурсирует. (2) конечная, но не-хвостовая рекурсия, вкладывающаяся глубже ~50 кадров — база корректна, но самовызов обёрнут в кортеж/объект, так что устранение неприменимо; перестройте в хвостовую позицию с аккумулятором, чтобы поднять стену до ~1000. Одно и то же сообщение покрывает оба случая, поэтому сначала проверяйте базу, затем позицию вызова." },
  ]}
/>

<Recap lang="ru">
Рекурсивный тип — это рекурсивная функция, которую выполняет компилятор: базовый случай плюс рекурсивный случай, отрезающий `[infer Head, ...infer Tail]` и вызывающий себя на укороченном кортеже. Reverse, Join и Includes имеют этот скелет, различаясь лишь тем, что накапливают. Компилятор ограничивает глубину: не-хвостовой вызов упирается около 50 с «Type instantiation is excessively deep and possibly infinite.», но устранение хвостовой рекурсии из TypeScript 4.5 поднимает это до ~1000, когда самовызов — вся ветка. Теперь, встретив эту ошибку, вы первым делом проверяете базовый случай, а затем позицию самовызова — два вопроса, которые покрывают каждый случай.
</Recap>
