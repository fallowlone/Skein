---
concepts:
- mapped-types
- utility-types
- deep-partial
- distributive-omit
- conditional-types
deepensInto:
- typescript/08-real-world/04-library-generics
estMin: 18
lang: ru
lessonType: topic
level: senior
order: 3
prereqs:
- typescript/04-type-system-deep/03-mapped-types
slug: "03-utility-types-from-scratch"
sources:
- https://www.typescriptlang.org/docs/handbook/utility-types.html
- https://github.com/type-challenges/type-challenges
spiral:
- mapped-types
- conditional-types
status: ready
summary: Каждый стандартный утилитный тип — это однострочный mapped- или условный тип; переписав Partial, Pick, Omit, ReturnType, Awaited и компанию, вы демистифицируете их и поймёте, почему стандартный Omit не-дистрибутивен.
title: Утилитные типы с нуля
track: typescript
unit: "05-type-level-programming"
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import Infographic from "~/components/diagram/Infographic.astro";
import omitVsDistributive from "./infographics/omit-vs-distributive.svg?raw";

<Hook>
Вы берёте `Omit<Shape, "kind">` для дискриминированного объединения `Circle | Square`, и результат неверен: вместо «каждый вариант минус его kind» вы получаете объект только с ключами, *общими для всех вариантов*, — радиус и сторона исчезли. Вы смотрите на это, уверенные, что стандартная библиотека сломана. Это не так. `Omit` определён как `Pick<T, Exclude<keyof T, K>>`, а `keyof (Circle | Square)` — это только общие ключи. Как только вы напишете `Omit` сами, это перестаёт быть загадкой и становится фиксом в одну строку. Каждый утилитный тип в `lib.es5.d.ts` — это mapped- или условный тип, который можно прочитать, пересобрать и перерасти.
</Hook>

<Crux>Каждый стандартный утилитный тип — однострочный mapped- или условный тип, включая ловушку не-дистрибутивного `Omit`.</Crux>

<Explanation>

## Семейство mapped-типов

Вы использовали `Partial`, `Required` и `Readonly` сотни раз — но написали бы их с нуля? Как только вы можете, стандартная библиотека перестаёт быть чёрным ящиком и превращается в набор блоков, которые можно прочитать, адаптировать и перерасти. `Partial`, `Required` и `Readonly` — mapped-типы с модификатором; `Pick` и `Record` выбирают набор ключей:

```ts
type MyPartial<T>  = { [P in keyof T]?: T[P] };          // добавить ? к каждому ключу
type MyRequired<T> = { [P in keyof T]-?: T[P] };          // -? убирает опциональность
type MyReadonly<T> = { readonly [P in keyof T]: T[P] };   // добавить readonly
type MyPick<T, K extends keyof T> = { [P in K]: T[P] };   // отобразить подмножество ключей
type MyRecord<K extends keyof any, V> = { [P in K]: V };  // построить из ключей + значения
```

Синтаксис модификаторов — это вся суть: `?` и `readonly` добавляют модификатор; `-?` и `-readonly` снимают его. `keyof any` — это `string | number | symbol`, легальное пространство ключей для `Record`.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Утилита</th>
        <th class="border border-border px-3 py-2 text-left">Однострочное определение</th>
        <th class="border border-border px-3 py-2 text-left">Механизм</th>
      </tr>
    </thead>
    <tbody>
      <tr><td class="border border-border px-3 py-2"><code>Partial&lt;T&gt;</code></td><td class="border border-border px-3 py-2"><code>{`{ [P in keyof T]?: T[P] }`}</code></td><td class="border border-border px-3 py-2">mapped + опц. модификатор</td></tr>
      <tr class="bg-surface-2"><td class="border border-border px-3 py-2"><code>Required&lt;T&gt;</code></td><td class="border border-border px-3 py-2"><code>{`{ [P in keyof T]-?: T[P] }`}</code></td><td class="border border-border px-3 py-2">mapped + снять опц.</td></tr>
      <tr><td class="border border-border px-3 py-2"><code>Readonly&lt;T&gt;</code></td><td class="border border-border px-3 py-2"><code>{`{ readonly [P in keyof T]: T[P] }`}</code></td><td class="border border-border px-3 py-2">mapped + readonly</td></tr>
      <tr class="bg-surface-2"><td class="border border-border px-3 py-2"><code>Pick&lt;T,K&gt;</code></td><td class="border border-border px-3 py-2"><code>{`{ [P in K]: T[P] }`}</code></td><td class="border border-border px-3 py-2">отобразить подмножество ключей</td></tr>
      <tr><td class="border border-border px-3 py-2"><code>Record&lt;K,V&gt;</code></td><td class="border border-border px-3 py-2"><code>{`{ [P in K]: V }`}</code></td><td class="border border-border px-3 py-2">ключи -> одно значение</td></tr>
      <tr class="bg-surface-2"><td class="border border-border px-3 py-2"><code>Exclude&lt;T,U&gt;</code></td><td class="border border-border px-3 py-2"><code>{`T extends U ? never : T`}</code></td><td class="border border-border px-3 py-2">дистрибутивный фильтр</td></tr>
      <tr><td class="border border-border px-3 py-2"><code>Extract&lt;T,U&gt;</code></td><td class="border border-border px-3 py-2"><code>{`T extends U ? T : never`}</code></td><td class="border border-border px-3 py-2">дистрибутивное оставление</td></tr>
      <tr class="bg-surface-2"><td class="border border-border px-3 py-2"><code>NonNullable&lt;T&gt;</code></td><td class="border border-border px-3 py-2"><code>{`T & {}`}</code></td><td class="border border-border px-3 py-2">пересечь, чтобы убрать null/undefined (TS 4.8+)</td></tr>
      <tr><td class="border border-border px-3 py-2"><code>Omit&lt;T,K&gt;</code></td><td class="border border-border px-3 py-2"><code>{`Pick<T, Exclude<keyof T, K>>`}</code></td><td class="border border-border px-3 py-2">взять дополнение — НЕ-дистрибутивен</td></tr>
      <tr class="bg-surface-2"><td class="border border-border px-3 py-2"><code>ReturnType&lt;F&gt;</code></td><td class="border border-border px-3 py-2"><code>{`F extends (...a: any) => infer R ? R : any`}</code></td><td class="border border-border px-3 py-2">infer в условии</td></tr>
      <tr><td class="border border-border px-3 py-2"><code>Parameters&lt;F&gt;</code></td><td class="border border-border px-3 py-2"><code>{`F extends (...a: infer P) => any ? P : never`}</code></td><td class="border border-border px-3 py-2">infer кортежа аргументов</td></tr>
      <tr class="bg-surface-2"><td class="border border-border px-3 py-2"><code>InstanceType&lt;C&gt;</code></td><td class="border border-border px-3 py-2"><code>{`C extends new (...a: any) => infer R ? R : any`}</code></td><td class="border border-border px-3 py-2">infer возврата конструктора</td></tr>
    </tbody>
  </table>
</div>

## Семейство условных: вывести интересную часть

`ReturnType`, `Parameters`, `InstanceType` и `Awaited` используют `infer`, чтобы вытащить кусок из структурной формы:

```ts
type MyReturnType<F> = F extends (...args: any) => infer R ? R : any;
type MyParameters<F> = F extends (...args: infer P) => any ? P : never;
type MyInstanceType<C> = C extends new (...args: any) => infer R ? R : any;

type R1 = MyReturnType<() => number>;          // ^? type R1 = number
type P1 = MyParameters<(a: string, b: number) => void>; // ^? type P1 = [a: string, b: number]
```

`Awaited` — рекурсивный: он разворачивает вложенные промисы и `.then`-аблы до итогового значения:

```ts
type MyAwaited<T> =
  T extends null | undefined ? T :
  T extends object & { then(onfulfilled: infer F, ...args: any): any }
    ? F extends (value: infer V, ...args: any) => any ? MyAwaited<V> : never
    : T;

type A1 = MyAwaited<Promise<Promise<string>>>;
//   ^? type A1 = string   (рекурсия разворачивает оба слоя)
```

<Inset kind="why" lang="ru">
Почему `NonNullable<T>` определён как `T & {}`, а не `T extends null | undefined ? never : T`? Работают оба, но `T & {}` (TS 4.8+) сохраняет больше информации: `{}` — это «любое не-null, не-undefined значение», и пересечение с ним убирает ровно `null` и `undefined`, оставляя обобщённые и условные типы не-жадно вычисленными. Условная форма принудительно распределяет и может преждевременно схлопнуть ещё обобщённый `T`. Пересечение — это современное, не-дистрибутивное определение, которое поставляет стандартная библиотека.
</Inset>

## Композиция: DeepPartial и DeepReadonly

Рекурсируйте mapped-тип во вложенные объекты с условным базовым случаем для не-объектов:

```ts
type DeepReadonly<T> = T extends (infer E)[]
  ? ReadonlyArray<DeepReadonly<E>>
  : T extends object
    ? { readonly [K in keyof T]: DeepReadonly<T[K]> }
    : T;

type DeepPartial<T> = T extends object
  ? { [K in keyof T]?: DeepPartial<T[K]> }
  : T;

type Config = { db: { host: string; pool: { max: number } } };
type DC = DeepPartial<Config>;
//   ^? каждый уровень опционален: { db?: { host?: string; pool?: { max?: number } } }
```

Базовый случай — «не объект — вернуть как есть»; рекурсивный случай заново отображает каждое свойство через то же глубокое преобразование.

## Ловушка Omit — и DistributiveOmit

Вот сюрприз дискриминированного объединения из Hook. `Omit<T, K> = Pick<T, Exclude<keyof T, K>>`. Когда `T` — объединение, `keyof T` схлопывается до **только тех ключей, что есть в каждом члене**:

```ts
type Circle = { kind: "circle"; radius: number };
type Square = { kind: "square"; side: number };
type Shape = Circle | Square;

type Bad = Omit<Shape, "kind">;
//   ^? type Bad = {}   (keyof Shape — это только "kind"; убрав его, не остаётся ничего)

// Принудительная дистрибуция, чтобы Omit выполнялся по членам:
type DistributiveOmit<T, K extends keyof any> =
  T extends unknown ? Omit<T, K> : never;

type Good = DistributiveOmit<Shape, "kind">;
//   ^? type Good = { radius: number } | { side: number }
```

`Omit` намеренно **не-дистрибутивен** — он работает с `T` как с целым, поэтому `keyof (Circle | Square)` — это лишь `"kind"` (пересечение наборов ключей). Обёртка в условие с голым параметром (`T extends unknown ? Omit<T, K> : never`) принуждает дистрибуцию: теперь `Omit` выполняется раз на вариант, и вы получаете ожидаемый по-вариантный результат. Этот единственный паттерн — самая частая причина, по которой люди думают, что `Omit` сломан на объединениях.

<Infographic
  svg={omitVsDistributive}
  label="Сравнение стандартного Omit и DistributiveOmit на объединении Circle | Square: стандартный Omit — это Pick<T, Exclude<keyof T, K>>, keyof объединения — лишь общий ключ 'kind', поэтому Omit<Shape, 'kind'> схлопывается в {}, тогда как DistributiveOmit (T extends unknown ? Omit<T, K> : never) выполняет Omit по вариантам и даёт { radius: number } | { side: number }."
  caption="Стандартный Omit работает с объединением как с целым, поэтому keyof держит лишь общие ключи и выходит {}; обёртка с голым условием распределяет Omit по вариантам — фикс в одну строку."
  hue="var(--d-infra)"
/>

<Quiz
  id="03-util-quiz-1"
  lessonSlug="03-utility-types-from-scratch"
  lang="ru"
  question="Почему `Omit<Circle | Square, 'kind'>` (где у каждого свой уникальный лишний ключ) даёт `{}` вместо по-вариантного остатка?"
  choices={[
    { label: "Omit = Pick<T, Exclude<keyof T, K>>, а keyof объединения — это только ключи, общие для каждого члена, здесь лишь 'kind', поэтому, убрав его, не остаётся общих ключей; Omit не-дистрибутивен.", correct: true },
    { label: "Omit работает только с интерфейсами, не с псевдонимами типов", misconception: "Omit работает с любым объектным типом, интерфейсом или псевдонимом; проблема в keyof над объединением, а не в форме объявления." },
    { label: "Объединение нужно сначала обернуть в Partial", misconception: "Partial меняет опциональность, а не то, какие ключи существуют; он не чинит схлопывание keyof объединения." },
    { label: "В Omit есть баг TypeScript", misconception: "Это намеренно и задокументировано: Omit по дизайну не-дистрибутивен; используйте дистрибутивную обёртку для отображения по членам." },
  ]}
/>

<DragOrder
  id="03-util-drag-1"
  pieceSlug="03-utility-types-from-scratch"
  lang="ru"
  prompt="Расставьте вычисление DeepReadonly<{ a: { b: number } }> от внешнего матча до самого глубокого листа."
  items={[
    "DeepReadonly<{ a: { b: number } }>: не массив, объект -> отобразить каждый ключ",
    "Ключ a: DeepReadonly<{ b: number }> -> снова объект -> отобразить каждый ключ",
    "Ключ b: DeepReadonly<number> -> не массив, не объект -> базовый случай возвращает number",
    "Внутреннее отображение даёт { readonly b: number }",
    "Внешнее отображение даёт { readonly a: { readonly b: number } }",
  ]}
/>

</Explanation>

<KeyTakeaway>Mapped-утилиты (Partial, Required, Readonly, Pick, Record) — это `{ [P in keyof T]...: T[P] }` с модификатором; условные (Exclude, Extract, ReturnType, Parameters, InstanceType) используют дистрибуцию или `infer`; NonNullable — это `T & {}` (TS 4.8+); Awaited рекурсирует, разворачивая промисы. Композируйте их рекурсивно для DeepPartial/DeepReadonly. Классическая ловушка: `Omit<T, K> = Pick<T, Exclude<keyof T, K>>` не-дистрибутивен, поэтому на объединении `keyof T` — лишь общие ключи; оберните `T extends unknown ? Omit<T, K> : never` (DistributiveOmit), чтобы получить по-вариантный результат.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="03-util-retrieval"
  lang="ru"
  questions={[
    { q: "Восстановите Partial, Required, Readonly, Pick и Record с нуля и объясните синтаксис модификаторов.", a: "Partial<T> = { [P in keyof T]?: T[P] } добавляет опциональный модификатор `?` к каждому ключу. Required<T> = { [P in keyof T]-?: T[P] } использует `-?`, чтобы СНЯТЬ опциональность (минус убирает модификатор). Readonly<T> = { readonly [P in keyof T]: T[P] } добавляет модификатор readonly; `-readonly` снял бы его. Pick<T, K extends keyof T> = { [P in K]: T[P] } отображает выбранное подмножество ключей вместо keyof T. Record<K extends keyof any, V> = { [P in K]: V } отображает каждый ключ из K в один тип значения V; `keyof any` — это `string | number | symbol`, легальное пространство ключей. Всё семейство — один mapped-тип с разным источником ключей и/или модификатором." },
    { q: "Как работают ReturnType, Parameters, InstanceType и Awaited и какой из них рекурсивный?", a: "Каждый вытаскивает кусок из формы функции/конструктора через `infer` в условии. ReturnType<F> = F extends (...args: any) => infer R ? R : any захватывает тип возврата. Parameters<F> = F extends (...args: infer P) => any ? P : never захватывает список аргументов как помеченный кортеж. InstanceType<C> = C extends new (...args: any) => infer R ? R : any захватывает тип экземпляра конструктора. Awaited<T> — рекурсивный: он проверяет на then-абл (объект с методом `then`), выводит значение колбэка-fulfillment и рекурсирует — поэтому Awaited<Promise<Promise<string>>> разворачивает оба слоя в string. Первые три матчатся один раз; Awaited вызывает себя, пока значение остаётся промисом." },
    { q: "Объясните, почему стандартный Omit не-дистрибутивен, что ломается на дискриминированном объединении и точный фикс DistributiveOmit.", a: "Omit<T, K> определён как Pick<T, Exclude<keyof T, K>>. `keyof T` вычисляется на T как на едином типе — а `keyof` над объединением даёт только ключи, общие для ВСЕХ членов (пересечение наборов ключей). Поэтому Omit<Circle | Square, 'kind'>, где единственный общий ключ — 'kind', убирает 'kind' и оставляет {} — по-вариантных ключей (radius, side) изначально не было в keyof Shape. Фикс — принудить дистрибуцию обёрткой в условие с голым параметром: type DistributiveOmit<T, K extends keyof any> = T extends unknown ? Omit<T, K> : never. Теперь Omit выполняется раз на член объединения, поэтому каждый вариант сохраняет свои ключи минус K: { radius: number } | { side: number }. Стандартная библиотека держит Omit не-дистрибутивным намеренно; DistributiveOmit — это явный opt-in." },
  ]}
/>

<Recap lang="ru">
Стандартная библиотека — не магия: Partial/Required/Readonly/Pick/Record — это mapped-типы с модификатором; Exclude/Extract — дистрибутивные условия; ReturnType/Parameters/InstanceType выводят кусок вызываемого; NonNullable — это `T & {}`; Awaited рекурсирует, разворачивая промисы. Композируйте их рекурсивно для DeepPartial и DeepReadonly. Единственная ловушка для запоминания: Omit — это `Pick<T, Exclude<keyof T, K>>` и потому не-дистрибутивен — на объединении он держит лишь общие ключи, так что берите DistributiveOmit (`T extends unknown ? Omit<T, K> : never`), когда нужно по-вариантное поведение. Теперь, когда `Omit` вернёт пустой объект на дискриминированном объединении, вы сразу знаете почему — и однострочный фикс.
</Recap>
