---
concepts: [discriminated-unions, tagged-unions, literal-discriminant, exhaustiveness, assert-never, state-machines]
deepensInto: ["typescript/02-everyday-types/05-index-access-types"]
estMin: 14
lang: ru
lessonType: topic
level: middle
order: 4
prereqs: ["typescript/02-everyday-types/03-type-guards"]
slug: "04-discriminated-unions"
sources:
  - https://www.typescriptlang.org/docs/handbook/2/narrowing.html#discriminated-unions
spiral: [control-flow, never, state-machines]
status: ready
summary: "Общий литеральный тег превращает объединение в размеченное: switch по тегу сужает каждую ветку, а default с assertNever делает добавление варианта ошибкой компиляции везде, где он не обработан."
title: "Размеченные объединения"
track: "typescript"
unit: "02-everyday-types"
---

import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import FadedExample from "~/components/pedagogy/FadedExample.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
Тип результата вашего API — `{ loading: boolean; error?: string; data?: User }`. Каждый компонент проверяет `if (!loading && !error && data)` и лезет в `data` — иногда `data` определён, хотя `error` тоже задан, иногда нет ни того, ни другого. Тип допускает все восемь комбинаций, но реальных состояний только три. Коллега выкатывает экран, читающий `data.name` во время ошибки, и он падает. Баг не в коде; он в типе. Фикс — сделать недопустимые состояния непредставимыми, и инструмент для этого — размеченное объединение.
</Hook>

<Crux>Общий литеральный тег сужает объединение по ветке до полей одного варианта — а default с `never` ловит каждый необработанный случай.</Crux>

<Explanation>

## Паттерн: общий литеральный тег

**Размеченное (теговое) объединение** — это объединение объектных типов, делящих одно свойство — *дискриминант* — чей тип в каждом члене является отдельным литералом. Switch или ветвление по этому свойству сужает весь объект до подходящего варианта:

```ts
type Result =
  | { kind: "ok"; data: string }
  | { kind: "error"; message: string }
  | { kind: "loading" };

function render(r: Result): string {
  switch (r.kind) {
    case "ok":
      return r.data;
      //     ^? (parameter) r: { kind: "ok"; data: string }
    case "error":
      return r.message;
      //     ^? r: { kind: "error"; message: string }
    case "loading":
      return "…";
  }
}
```

В каждом `case` `r` сужен до единственного члена, чей `kind` совпадает с литералом, поэтому `r.data` доступен в `"ok"`, а `r.message` — в `"error"`, и больше нигде. Это заменяет восьмикомбинационную булеву кашу из Hook тремя взаимоисключающими, полностью типизированными состояниями. Недопустимые состояния (data при ошибке) теперь непредставимы.

<FlowDiagram
  label="Switch по дискриминанту сужает каждую ветку до одного варианта"
  hue="var(--d-frontend)"
  perRow={4}
  nodes={[
    { id: "u", label: "Result", sub: "ok | error | loading" },
    { id: "s", label: "switch", sub: "r.kind" },
    { id: "ok", label: "case ok", sub: "{ data }" },
    { id: "er", label: "case error", sub: "{ message }" },
    { id: "ld", label: "case loading", sub: "{}" },
    { id: "ex", label: "default", sub: "r: never" },
  ]}
  edges={[
    { from: "u", to: "s", label: "читаем тег" },
    { from: "s", to: "ok", label: "ok" },
    { from: "s", to: "er", label: "error" },
    { from: "s", to: "ld", label: "loading" },
    { from: "s", to: "ex", label: "else" },
  ]}
  caption="Каждый case сужает до одного варианта. default получает never, когда все варианты обработаны — основа проверок исчерпания."
/>

## Требования для работы сужения

Проверяющий распознаёт размеченное объединение, только когда:

1. **Дискриминант — литеральный тип**, а не расширенный примитив. `kind: "ok"` работает; `kind: string` — нет. (Вспомните расширение `let`/`const` из урока про сужение — копирование тега в `let` это ломает.)
2. **Каждый член объявляет дискриминант** с отдельным литералом. Если два члена делят `kind: "ok"`, проверяющий не различит их и сольёт их поля.
3. **Вы ветвитесь по самому дискриминанту** (`r.kind`), а не по производному значению.

```ts
// Сломано: расширенный дискриминант
type Bad = { kind: string; data: string } | { kind: string; err: string };
declare const b: Bad;
if (b.kind === "data") {
  b.data;
  // Error: Property 'data' does not exist on type 'Bad'.
  //   — kind это `string`, не литерал, поэтому сужения не происходит
}
```

## Исчерпание через `assertNever`

Настоящая сила — ловить *пропущенные* случаи на компиляции. Когда каждый вариант обработан, значение в ветке `default` имеет тип `never`. Хелпер, принимающий только `never`, тогда вызывает ошибку компиляции в тот момент, когда добавлен новый вариант, но оставлен необработанным:

```ts
function assertNever(x: never): never {
  throw new Error("Unexpected variant: " + JSON.stringify(x));
}

function render(r: Result): string {
  switch (r.kind) {
    case "ok": return r.data;
    case "error": return r.message;
    case "loading": return "…";
    default: return assertNever(r);
    //                          ^? r: never  — сегодня всё в порядке
  }
}
```

Теперь добавьте четвёртый вариант `{ kind: "retrying"; attempt: number }` в `Result` и *не* трогайте `render`. Ветка `default` больше не видит `never` — она видит `{ kind: "retrying"; attempt: number }`:

```ts
// Error: Argument of type '{ kind: "retrying"; attempt: number; }'
//   is not assignable to parameter of type 'never'.
```

Каждый `switch`, забывший новый случай, загорается красным по всей кодовой базе. Это самое ценное свойство размеченных объединений для сеньорской работы: система типов превращает «найти все места, которые надо обновить» из ручного grep в гарантированную ошибку компиляции.

<Inset kind="why" lang="ru">
Почему `never`, а не, скажем, `default: throw`? Голый `throw` срабатывает в *рантайме*; `assertNever` сдвигает сбой на *компиляцию*. Трюк в том, что `never` — пустой тип: только значение типа `never` присваиваемо параметру `never`. Пока объединение полностью обработано, суженный остаток — `never`, и вызов проходит проверку типов. В тот миг, когда объединение растёт, остаток становится реальным типом, уже не присваиваемым к `never`, и сборка ломается раньше, чем кто-либо запустит код. Из одной строки вы получаете и рантайм-защитник, *и* компиляционную гарантию.
</Inset>

## Почему это лучше булевых флагов и `in`

Модель булевых флагов (`loading`, `error`, `data` независимы) допускает `2^n` комбинаций, большинство из которых недопустимы. Модель `in`-защитника из прошлого урока работает, но хрупка при пересечении ключей. Тег-дискриминант однозначен, самодокументируем и — что важнее всего — проверяем на исчерпание. Это каноническая форма для: ответов API (`ok`/`error`), экшенов Redux/редьюсера (поле `type`), стейт-машин UI/сети (`idle`/`loading`/`success`/`failure`), узлов AST и токенов парсера.

```ts
// Экшен в стиле Redux — дискриминант по соглашению `type`
type Action =
  | { type: "increment"; by: number }
  | { type: "reset" };

function reduce(state: number, a: Action): number {
  switch (a.type) {
    case "increment": return state + a.by;
    case "reset": return 0;
    default: return assertNever(a);
  }
}
```

<DragOrder
  id="04-du-dragorder"
  pieceSlug="04-discriminated-unions"
  lang="ru"
  prompt="Упорядочи шаги, чтобы безопасно добавить новый вариант в проверяемое на исчерпание размеченное объединение."
  items={[
    "Добавить новый вариант (с его литеральным тегом) в тип объединения",
    "Перекомпилировать — каждый switch с default-assertNever теперь ошибается",
    "Ветка default больше не сужается до never; она видит новый вариант",
    "Добавить недостающий case в каждый помеченный switch",
    "Перекомпилировать начисто — остаток снова never",
  ]}
/>

<FadedExample
  client:load
  id="04-du-faded"
  pieceSlug="04-discriminated-unions"
  lang="ru"
  title="Смоделируй стейт-машину дискриминантом"
  steps={{
    solved: (
      <div class="space-y-3 text-sm">
        <p><strong>Замени булевы недопустимых состояний теговым объединением:</strong></p>
        <pre class="text-xs bg-zinc-100 p-2 rounded overflow-x-auto"><code>{`// До: 8 комбинаций, валидны лишь 3
type Bad = { loading: boolean; error?: string; data?: User };

// После: 3 состояния, недопустимые непредставимы
type Fetch =
  | { status: "idle" }
  | { status: "loading" }
  | { status: "success"; data: User }
  | { status: "failure"; error: string };

function view(s: Fetch): string {
  switch (s.status) {
    case "idle":    return "Start";
    case "loading": return "Spinner";
    case "success": return s.data.name;   // data только здесь
    case "failure": return s.error;       // error только здесь
    default: return assertNever(s);
  }
}`}</code></pre>
        <p><strong>Почему лучше:</strong> <code>data</code> доступен лишь в <code>"success"</code>, а <code>error</code> — лишь в <code>"failure"</code>. Нельзя случайно прочитать <code>data</code> во время сбоя — тип это запрещает. Default с <code>assertNever</code> гарантирует, что добавление состояния <code>"retrying"</code> позже заставит обработать его здесь.</p>
      </div>
    ),
    semi: {
      prompt: "В switch выше, внутри `case \"success\"`, сколько свойств помимо `status` проверяющий гарантирует на `s`?",
      blanks: [
        { id: "answer", expected: "1", placeholder: "число" },
      ],
    },
    blank: {
      prompt: "Вы меняете объявление дискриминанта с `status: \"loading\"` на `status: string` на одном члене. Что сломается и почему?",
      reveal: (
        <div class="space-y-2 text-sm">
          <p>Сужение рушится. Как только дискриминант любого члена становится расширенным типом <code>string</code> вместо литерала, TypeScript больше не может трактовать объединение как размеченное — сравнение <code>s.status === "success"</code> не может доказать, какой член у вас, ведь <code>string</code> пересекается с каждым литералом. Ветки <code>case</code> перестают сужать (<code>s.data</code> становится ошибкой), а default с <code>assertNever</code> перестаёт проходить проверку типов, ведь остаток больше не <code>never</code>. Дискриминант обязан быть литералом (или объединением литералов) на <em>каждом</em> члене; именно поэтому теги держат как <code>const</code>-строковые литералы и никогда не копируют через <code>let</code>.</p>
        </div>
      ),
    },
  }}
  misconceptions={{
    "answer:0": "В ветке success s сужено до { status: \"success\"; data: User }, поэтому помимо status гарантируется ровно одно свойство: data.",
  }}
/>

<Quiz
  id="04-du-quiz-1"
  lessonSlug="04-discriminated-unions"
  lang="ru"
  question="Каков тип значения в ветке `default` исчерпывающего `switch` по полностью обработанному размеченному объединению?"
  choices={[
    { label: "`never` — каждый член сужен прочь, остаётся пустой тип, поэтому assertNever проходит проверку типов", correct: true },
    { label: "Полное объединение, ведь default ловит всё", misconception: "default достигается лишь после всех известных случаев; проверяющий уже их вычел, поэтому остаток — never, а не полное объединение." },
    { label: "`unknown`", misconception: "Исчерпанное объединение сужается до never (пустого типа), а не unknown (верхнего типа)." },
    { label: "`any`", misconception: "Это never. assertNever на этом и держится: лишь значение never присваиваемо параметру never." },
  ]}
/>

<Quiz
  id="04-du-quiz-2"
  lessonSlug="04-discriminated-unions"
  lang="ru"
  question="Вы добавляете новый вариант в размеченное объединение, но забываете обработать его в switch с default-`assertNever`. Что произойдёт?"
  choices={[
    { label: "Ошибка компиляции на вызове assertNever, ведь ветка default теперь сужается до нового варианта, а не до never", correct: true },
    { label: "Ничего — default молча ловит его в рантайме", misconception: "Смысл assertNever — упасть на КОМПИЛЯЦИИ. Остаток больше не never, поэтому вызов не проходит проверку типов." },
    { label: "Новый вариант автоматически обрабатывается ближайшим подходящим case", misconception: "Автосопоставления нет; необработанный литерал падает в default, и assertNever отвергает его на компиляции." },
    { label: "Рантайм-предупреждение, но сборка всё равно проходит", misconception: "Это жёсткая ошибка компиляции, а не предупреждение — сборка падает, пока не добавишь недостающий case." },
  ]}
/>

</Explanation>

<KeyTakeaway>
Размеченное объединение — это объединение объектных типов, делящих тег с литеральным типом (`kind`/`type`/`status`). Ветвление по этому тегу сужает каждый case ровно до одного варианта, поэтому поля варианта доступны лишь там, где они есть — недопустимые состояния становятся непредставимыми, заменяя кашу булевых флагов. Добавьте default с `assertNever(x: never): never`, и остаточный тип будет `never`, пока объединение полностью обработано; в тот миг, когда вы добавляете вариант и забываете case, остаток становится реальным типом, уже не присваиваемым к `never`, превращая каждое необработанное место в ошибку компиляции. Дискриминант обязан быть литералом на каждом члене — расширьте его до `string` (или скопируйте через `let`), и всё сужение рушится.
</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="04-du-retrieval"
  lang="ru"
  questions={[
    { q: "Что делает объединение размеченным и каковы требования, чтобы проверяющий его сужал?", a: "Размеченное (теговое) объединение — это объединение объектных типов, делящих одно свойство (дискриминант), чей тип в каждом члене — отдельный литерал (например, kind: 'ok' | 'error' | 'loading'). Ветвление по этому свойству сужает объект до подходящего варианта. Требования: (1) дискриминант обязан быть литеральным типом на каждом члене, а не расширенным примитивом вроде string; (2) каждый член обязан объявить его с отдельным литералом, чтобы проверяющий различал члены; (3) ветвиться надо по самому дискриминанту (s.kind), а не по производному значению. Нарушь любое — особенно расширь тег до string копированием через let — и сужение молча прекращается." },
    { q: "Объясни паттерн исчерпания assertNever и почему он сдвигает сбой на компиляцию.", a: "Определи assertNever(x: never): never, который бросает. Поставь `default: return assertNever(x);` в конце switch по дискриминанту. Пока каждый вариант обработан, проверяющий их все вычел, поэтому значение, доходящее до default, сужено до never — пустого типа — и лишь значение never присваиваемо параметру never, поэтому вызов проходит проверку типов. Когда добавляешь новый вариант и забываешь его case, ветка default теперь сужается до этого нового варианта (реального типа), который НЕ присваиваем к never, давая ошибку компиляции на вызове assertNever. Каждый switch, забывший case, загорается по всей кодовой базе. Та же строка ещё и бросает в рантайме, если просочится неожиданное значение, поэтому ты получаешь компиляционную гарантию и рантайм-защитник вместе." },
    { q: "Почему размеченное объединение лучше независимых булевых флагов или in-защитника для моделирования состояния?", a: "Независимые булевы вроде { loading; error?; data? } допускают 2^n комбинаций, большинство недопустимы (data при ошибке), и ничто не мешает компоненту прочитать data в неверном состоянии. in-защитник работает, но хрупок при пересечении ключей между членами. Размеченное объединение с литеральным тегом делает состояния взаимоисключающими и полностью типизированными: поля каждого варианта доступны лишь в его ветке, недопустимые состояния непредставимы, форма самодокументируема и — уникально — проверяема на исчерпание через assertNever. Поэтому это каноническая форма для ответов API, экшенов редьюсера, стейт-машин UI/сети, узлов AST и токенов." },
  ]}
/>

<Recap lang="ru">
Размеченные объединения — повседневный сеньорский паттерн, к которому ведёт весь этот юнит. Дав каждому члену объединения общий литеральный тег, вы позволяете проверяющему сужать весь объект чтением одного поля: в каждом `case` эксклюзивные поля варианта становятся доступны, а недопустимые комбинации — непредставимы — противоядие от каши булевых флагов. Соедините `switch` с default-`assertNever(x: never)`, и исчерпание становится компиляционной гарантией: остаточный тип — `never`, пока обработан каждый вариант, а добавление варианта без обновления `switch` превращается в жёсткую ошибку сборки в каждом необработанном месте. Единственное правило, удерживающее это в работе: дискриминант обязан быть литералом на каждом члене, никогда не расширенным `string`. Далее **типы доступа по индексу** покажут, как выводить типы *из* этих объединений и из данных — `Result["kind"]`, `Action["type"]` — чтобы ваши теги и типы значений оставались единым источником истины, а не дублировались вручную. Теперь, когда встречаешь `{ loading: boolean; error?: string; data?: User }` в кодовой базе, знаешь, чем это заменить — и почему компилятор сам найдёт все места, которые пропустили при миграции.
</Recap>
