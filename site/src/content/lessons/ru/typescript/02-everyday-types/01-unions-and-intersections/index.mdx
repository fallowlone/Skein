---
concepts: [union-types, intersection-types, common-members, never, structural-merge]
deepensInto: ["typescript/02-everyday-types/02-narrowing"]
estMin: 13
lang: ru
lessonType: topic
level: junior
order: 1
prereqs: ["typescript/01-foundations/04-literal-types"]
slug: "01-unions-and-intersections"
sources:
  - https://www.typescriptlang.org/docs/handbook/2/everyday-types.html#union-types
  - https://www.typescriptlang.org/docs/handbook/2/objects.html#intersection-types
spiral: [composition, never, set-theory]
status: ready
summary: "Объединение — это одно-из и открывает лишь общие члены; пересечение — это всё-сразу и сливает члены объектов. Смешай несовместимые примитивы — получишь never."
title: "Объединения и пересечения"
track: "typescript"
unit: "02-everyday-types"
---

import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import FadedExample from "~/components/pedagogy/FadedExample.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import MetaphorComplete from "~/components/pedagogy/MetaphorComplete.astro";
import StackDiagram from "~/components/diagram/StackDiagram.astro";

<Hook>
Коллега пишет `type Handler = ClickHandler | KeyHandler`, а потом вызывает `handler.preventDefault()` — метод, который есть только у одного из них. Проверяющий отказывает: «Property 'preventDefault' does not exist on type 'ClickHandler | KeyHandler'». Коллега «чинит» это, переключившись на `ClickHandler & KeyHandler`. Ошибка ушла, но сломались все места вызова, куда передавали обычный `ClickHandler`. Операторы выглядят симметрично. На деле они противоположны — и путаница между ними одна из самых частых ошибок типов в реальном коде.
</Hook>

<Crux>Объединение `A | B` — значение, являющееся одним из A или B; пересечение `A & B` — значение, являющееся и A, и B одновременно.</Crux>

<Explanation>

## Одно-из против всё-сразу

Прежде чем писать `|` или `&`, спросите себя: вызывающий может передать *одну* из вещей или обязан предоставить *все сразу*? Ответ определяет нужный оператор — а неверный выбор компилируется без ошибок, молча ломая безопасность типов.

**Объединение** `A | B` описывает значение, которое является *либо* `A`, *либо* `B`. Статически вы не знаете, чем именно, поэтому проверяющий разрешает обращаться только к членам, присутствующим в *обоих*.

**Пересечение** `A & B` описывает значение, которое *одновременно* является и `A`, и `B`. У него есть *каждый* член обоих типов, ведь оно должно удовлетворять обоим контрактам сразу.

```ts
type Circle = { kind: "circle"; radius: number };
type Square = { kind: "square"; side: number };

type Shape = Circle | Square;
//   ^? type Shape = Circle | Square

function describe(s: Shape) {
  s.kind;
  // ^? (property) kind: "circle" | "square"   — общий для обоих, OK
  s.radius;
  // Error: Property 'radius' does not exist on type 'Shape'.
  //   Property 'radius' does not exist on type 'Square'.
}
```

`kind` есть у обоих членов, поэтому он доступен, а его тип — объединение двух литеральных типов. `radius` есть только у `Circle`, поэтому в объединении он недоступен, пока вы не *сузите* тип (следующий урок). Именно ради этого сужение и существует.

<StackDiagram
  label="Объединение против пересечения: какие члены доступны"
  hue="var(--d-frontend)"
  layers={[
    { label: "A = { id: number; name: string }", note: "два члена" },
    { label: "B = { id: number; age: number }", note: "два члена" },
    { label: "A | B доступно: только id", note: "общие члены" },
    { label: "A & B доступно: id, name, age", note: "все члены" },
  ]}
/>

## Пересечение сливает члены объектов

Для объектных типов `A & B` — это новый тип, несущий *все* свойства обоих. Это естественный способ «добавить поля» к форме.

```ts
type WithId = { id: string };
type WithTimestamps = { createdAt: Date; updatedAt: Date };

type Entity = WithId & WithTimestamps;

const row: Entity = { id: "u1", createdAt: new Date(), updatedAt: new Date() };
//    ^? const row: Entity
// Убери любое поле — и проверяющий ругается:
const bad: Entity = { id: "u1", createdAt: new Date() };
// Error: Property 'updatedAt' is missing in type
//   '{ id: string; createdAt: Date; }' but required in type 'WithTimestamps'.
```

Если обе стороны объявляют *одно и то же* свойство с *совместимыми* типами, результат его сохраняет. Если же типы свойства *несовместимы*, слитое свойство схлопывается в `never` — см. ниже.

<Inset kind="why" lang="ru">
Почему «всё-сразу» для пересечения поначалу кажется наоборот: в теории множеств *пересечение* двух множеств *меньше*. Но типы TypeScript — это множества *значений*, а тип-значение вроде `{ a: number }` — это множество *всех объектов, у которых есть хотя бы `a`*. Множество объектов, у которых есть хотя бы `a` **и** хотя бы `b`, меньше каждого из них по отдельности — но каждое такое значение несёт *больше* членов. Меньше значений, больше гарантированных полей. Поэтому `A & B` — это более узкое множество значений, но более широкий набор членов.
</Inset>

## Несовместимые примитивы пересекаются в `never`

`never` — это пустой тип, множество из нуля значений. Никакое значение не может быть одновременно и `string`, и `number`, поэтому проверяющий сводит невозможное пересечение к `never`:

```ts
type Impossible = string & number;
//   ^? type Impossible = never

type AlsoNever = "a" & "b";
//   ^? type AlsoNever = never
```

Это происходит молча. Ошибки на самом алиасе типа нет — она появляется лишь позже, когда вы попытаетесь *присвоить* что-то такому типу, ведь в `never` ничего нельзя присвоить:

```ts
const x: string & number = "hi";
// Error: Type 'string' is not assignable to type 'never'.
```

Опасная версия прячется внутри пересечений объектов. Если два объектных типа делят ключ с непересекающимися типами значений, в `never` превращается лишь *это свойство*, а сам объектный тип всё ещё выглядит пригодным:

```ts
type A = { status: "active"; v: number };
type B = { status: "archived"; v: number };

type Both = A & B;
//   ^? type Both = A & B
type S = Both["status"];
//   ^? type S = never   — "active" & "archived" невозможно
```

`Both` необитаем, но вы это обнаружите, лишь попытавшись сконструировать значение или прочитать `status`. Это классическая ловушка использования `&` там, где имелось в виду объединение.

<MetaphorComplete
  id="01-unions-metaphor"
  pieceSlug="01-unions-and-intersections"
  lang="ru"
  setup="Объединение A | B похоже на параметр функции, принимающий либо кота, либо собаку. Вызывать можно только те методы, которые есть у ___ животных."
  accepted={["обоих", "всех", "каждого", "общие", "общих"]}
  canonical="обоих"
  explanation="На объединении доступны лишь члены, общие для каждого члена объединения — то, что умеют и кот, и собака (например, eat()), но никогда не специфичный для кота purr()."
/>

## Поведение в параметрах функции

Функция, принимающая объединение, обязана обработать *каждый* его член, поэтому в теле доступны только общие члены. Функцию, принимающую пересечение, *проще* удовлетворить внутри тела (гарантировано больше членов), но *сложнее* вызвать (аргумент должен быть и тем, и другим):

```ts
declare function logUnion(x: number | string): void;
logUnion(1);        // OK
logUnion("a");      // OK

declare function logBoth(x: { a: 1 } & { b: 2 }): void;
logBoth({ a: 1 });
// Error: Property 'b' is missing in type '{ a: 1; }'
//   but required in type '{ b: 2; }'.
logBoth({ a: 1, b: 2 }); // OK
```

Обратите внимание на асимметрию *присваиваемости*: `number` присваиваем к `number | string` (часть подходит к «целому-или»), но передать частичный объект в параметр-`&` нельзя («целое-и» требует всё).

## `interface extends` против `A & B`

Оба компонуют формы объектов, но они невзаимозаменяемы. `interface Child extends Parent {}` проверяет конфликты *немедленно* и выдаёт ошибку при объявлении. Пересечение откладывает проверку и при конфликте молча выдаёт `never`:

```ts
interface Base { v: string }
interface Wrong extends Base { v: number }
// Error: Interface 'Wrong' incorrectly extends interface 'Base'.
//   Types of property 'v' are incompatible. Type 'number' is not assignable to type 'string'.

type AlsoWrong = Base & { v: number };
//   ^? type AlsoWrong = Base & { v: number }   — здесь ошибки нет…
type V = AlsoWrong["v"];
//   ^? type V = never   — …конфликт всплывает лишь при чтении
```

Предпочитайте `extends` для номинальных иерархий «является», которые хотите проверять в точке объявления; берите `&` для смешивания ортогональных, бесконфликтных аспектов (миксины, «добавить timestamps», «добавить id»). Объединения, различающиеся по тегу, вы позже смоделируете как **размеченные объединения** в этом же юните.

<FadedExample
  client:load
  id="01-unions-faded"
  pieceSlug="01-unions-and-intersections"
  lang="ru"
  title="Доберись до нужного члена"
  steps={{
    solved: (
      <div class="space-y-3 text-sm">
        <p><strong>Даны типы:</strong></p>
        <pre class="text-xs bg-zinc-100 p-2 rounded overflow-x-auto"><code>{`type Loading = { state: "loading" };
type Loaded  = { state: "loaded"; data: string };
type View = Loading | Loaded;

function render(v: View) {
  v.state;     // ok: "loading" | "loaded"
  v.data;      // Error: нет на Loading
}`}</code></pre>
        <p><strong>Почему:</strong> <code>data</code> живёт только на <code>Loaded</code>. На объединении <code>View</code> доступен лишь <code>state</code> (он есть на обоих). Чтобы коснуться <code>data</code>, нужно сначала сузить тип до ветки <code>"loaded"</code> — тема следующего урока.</p>
        <p>Если бы вы написали <code>Loading & Loaded</code>, свойство <code>state</code> стало бы невозможным <code>"loading" &amp; "loaded"</code> = <code>never</code>: необитаемый тип, а не то, что нужно.</p>
      </div>
    ),
    semi: {
      prompt: "Для `type T = { a: number } & { b: string }` — сколько свойств обязано предоставить значение типа T?",
      blanks: [
        { id: "answer", expected: "2", placeholder: "число" },
      ],
    },
    blank: {
      prompt: "Каков тип `('on' | 'off') & 'on'` и почему?",
      reveal: (
        <div class="space-y-2 text-sm">
          <p>Это <code>"on"</code>. Пересечение распределяется по объединению почленно: <code>("on" &amp; "on") | ("off" &amp; "on")</code> = <code>"on" | never</code> = <code>"on"</code>. Невозможная ветка <code>"off" &amp; "on"</code> схлопывается в <code>never</code> и выпадает из объединения, потому что <code>never</code> — нейтральный элемент для <code>|</code>. Это почленное поведение и есть та самая дистрибутивность, на которую вы обопрётесь в условных типах в юните 05.</p>
        </div>
      ),
    },
  }}
  misconceptions={{
    "answer:1": "Пересечение объектных типов несёт ВСЕ члены обеих сторон, поэтому значение обязано предоставить и a, и b — два свойства, а не одно.",
  }}
/>

<Quiz
  id="01-unions-quiz-1"
  lessonSlug="01-unions-and-intersections"
  lang="ru"
  question="Для `type X = { id: string } | { id: string; email: string }` — какое свойство доступно на значении типа X без сужения?"
  choices={[
    { label: "Только `id` — это единственное свойство, присутствующее на обоих членах объединения", correct: true },
    { label: "И `id`, и `email`, ведь пересечение их сливает", misconception: "Это объединение (|), а не пересечение. Объединение открывает лишь общие члены; email есть только у одного члена." },
    { label: "Ни одно — объединения скрывают все члены", misconception: "Общие члены доступны. id есть на обоих членах, поэтому к нему можно обратиться." },
    { label: "Только `email`, как у более конкретного члена", misconception: "email есть лишь у одного члена объединения, поэтому он НЕдоступен без предварительного сужения." },
  ]}
/>

<Quiz
  id="01-unions-quiz-2"
  lessonSlug="01-unions-and-intersections"
  lang="ru"
  question="Что такое `string & number` в TypeScript и когда проявляется проблема?"
  choices={[
    { label: "Это `never`; алиас компилируется молча, а ошибка появляется лишь при попытке присвоить ему значение", correct: true },
    { label: "Ошибка компиляции прямо на строке алиаса типа", misconception: "Алиас `type T = string & number` НЕ даёт ошибки — он молча сводится к never. Ошибка появляется в точке присваивания." },
    { label: "`string | number` — TypeScript расширяет его", misconception: "TypeScript никогда не расширяет пересечение до объединения. Непересекающиеся примитивы пересекаются в never, пустой тип." },
    { label: "`unknown`, верхний тип", misconception: "Невозможное пересечение становится never (нижним/пустым типом), а не unknown (верхним типом)." },
  ]}
/>

</Explanation>

<KeyTakeaway>
`A | B` — это одно-из: трогать можно лишь члены, общие для каждого члена, поэтому и существует сужение. `A & B` — это всё-сразу: для объектов сливает каждый член, требуя их все при конструировании. Непересекающиеся примитивы (`string & number`, `"a" & "b"`) молча пересекаются в `never` — ошибка всплывает только при присваивании или обращении к члену. Использование `&` там, где имелось в виду объединение, или для «расширения» конфликтующей формы, порождает необитаемый тип, который компилируется до самого момента использования.
</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="01-unions-retrieval"
  lang="ru"
  questions={[
    { q: "Почему на объединении доступны только общие члены и что открывает остальные?", a: "Значение объединения в рантайме может быть ЛЮБЫМ из его членов, поэтому проверяющий гарантирует лишь члены, присутствующие в каждом члене объединения — обращение к члену, который есть только в одной ветке, было бы небезопасным, окажись значение другой веткой. Сужение (typeof, in, проверка дискриминанта) убирает ветки в конкретной точке программы; когда остаётся одна ветка, её эксклюзивные члены становятся доступны. Это и есть вся мотивация сужения по потоку управления, разбираемого далее." },
    { q: "Объясни через множества значений, почему `A & B` несёт больше членов, но описывает меньше значений.", a: "Объектный тип в TypeScript обозначает МНОЖЕСТВО всех значений, у которых есть ХОТЯ БЫ его объявленные члены. { a } — каждый объект хотя бы с a; { b } — каждый объект хотя бы с b. Их пересечение — каждый объект хотя бы с a И хотя бы с b — меньшее множество значений (подходит меньше объектов), но каждое подходящее значение гарантированно несёт БОЛЬШЕ членов. Значит, уже множество значений, шире набор членов. Поэтому & требует все поля при конструировании, но даёт все поля в теле." },
    { q: "Как несовместимые типы ведут себя при пересечении и почему случай со свойством объекта — ловушка?", a: "Непересекающиеся типы пересекаются в never (пустой тип): string & number = never, 'a' & 'b' = never, ведь никакое значение их не населяет. Для примитивов never становится весь тип. Для объектов, делящих ключ с непересекающимися типами значений, в never превращается лишь ЭТО свойство, а сам объектный тип всё ещё выглядит конструируемым — например, { status: 'a' } & { status: 'b' } имеет status: never. Тип необитаем, но компилируется; ошибка появляется лишь при попытке собрать значение или прочитать never-свойство. Это классическая ошибка использования & для комбинирования вариантов, которым следовало быть объединением." },
  ]}
/>

<Recap lang="ru">
Объединения и пересечения — это два способа компоновать типы, и они противоположны. Объединение `A | B` — это одно-из: значение, которое является одним из своих членов, открывающее только члены, общие для них всех, поэтому для доступа к остальным понадобится сужение. Пересечение `A & B` — это всё-сразу: для объектных типов оно сливает каждый член, требуя их все при конструировании; непересекающиеся примитивы вроде `string & number` молча схлопываются в пустой тип `never`, и то же происходит по-свойственно, когда объектные типы делят ключ с несовместимыми типами значений. Предпочитайте `interface extends` для проверяемых иерархий «является» и `&` для бесконфликтных миксинов. Далее **сужение** покажет, как проверяющий отслеживает, в какой ветке объединения вы находитесь в каждой точке программы, превращая скрытый член объединения в доступный. Теперь, когда встретишь ошибку «Property does not exist» на объединении, сразу будешь знать: сначала нужно сузить тип — или задуматься, а `|` ли вообще нужен был.
</Recap>
