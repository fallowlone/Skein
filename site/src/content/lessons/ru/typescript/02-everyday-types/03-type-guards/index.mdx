---
concepts: [type-predicates, user-defined-guards, unsound-narrowing, in-guards, array-guards, assertion-functions]
deepensInto: ["typescript/06-functions-this/03-assertion-functions"]
estMin: 14
lang: ru
lessonType: topic
level: middle
order: 3
prereqs: ["typescript/02-everyday-types/02-narrowing"]
slug: "03-type-guards"
sources:
  - https://www.typescriptlang.org/docs/handbook/2/narrowing.html#using-type-predicates
spiral: [control-flow, soundness, predicates]
status: ready
summary: "Пользовательские защитники типов возвращают `x is T`. Проверяющий доверяет предикату несоундно — лгущий защитник компилируется и портит всё сужение ниже по коду."
title: "Пользовательские защитники типов"
track: "typescript"
unit: "02-everyday-types"
---

import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import FadedExample from "~/components/pedagogy/FadedExample.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import MetaphorComplete from "~/components/pedagogy/MetaphorComplete.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";
import Infographic from "~/components/diagram/Infographic.astro";
import guardTradeoffs from "./infographics/guard-tradeoffs.svg?raw";

<Hook>
В вашей общей библиотеке появляется хелпер `isUser(x): x is User`. Каждое место вызова, прошедшее его проверку, трактует значение как полностью типизированный `User` — читает `.email`, `.roles`, `.id`. Спустя месяцы — падение в проде: `Cannot read properties of undefined (reading 'toLowerCase')`. Тело защитника проверяло лишь `typeof x === "object"`. Оно *солгало*. TypeScript ему поверил, сузил тысячи мест вызова до `User` и никого не предупредил. Предикат типа — это обещание, которое проверяющий принимает на веру, а нарушенное обещание невидимо до рантайма.
</Hook>

<Crux>Предикат `x is T` заставляет проверяющего несоундно доверять вердикту функции — если тело лжёт, сужение ниже молча неверно.</Crux>

<Explanation>

## Что такое предикат типа

Когда вы выносите валидацию в хелпер, проверяющий теряет связь между булевым результатом и типом — и возвращает вам полное объединение обратно. Предикаты типа решают эту проблему: они позволяют переиспользуемой функции участвовать в той же машинерии сужения, что `typeof` и `instanceof` используют встроенно.

Обычная функция, возвращающая `boolean`, ничего не сужает для вызывающего — проверяющий не знает, что *означает* результат `true`. Аннотация возврата **предиката типа**, `arg is T`, говорит проверяющему: «когда вернётся `true`, считай `arg` типом `T`; когда `false` — вычти `T`».

```ts
function isString(x: unknown): x is string {
  return typeof x === "string";
}

function handle(v: string | number) {
  if (isString(v)) {
    v;
    // ^? string   — предикат сузил, точь-в-точь как встроенная проверка
    return v.toUpperCase();
  }
  v;
  // ^? number    — ветка false вычитает string
}
```

Параметр предиката (`x` / именованный аргумент) должен быть одним из параметров функции или `this`. Тип возврата `x is T` заменяет обычный `boolean` — в рантайме функция всё равно просто возвращает булево значение.

<FlowDiagram
  label="Пользовательский предикат участвует в сужении по потоку управления"
  hue="var(--d-frontend)"
  perRow={4}
  nodes={[
    { id: "v", label: "v", sub: "string | number" },
    { id: "p", label: "isString(v)", sub: "x is string" },
    { id: "t", label: "ветка true", sub: "v: string" },
    { id: "f", label: "ветка false", sub: "v: number" },
  ]}
  edges={[
    { from: "v", to: "p", label: "вызов" },
    { from: "p", to: "t", label: "true ⇒ is T" },
    { from: "p", to: "f", label: "false ⇒ not T" },
  ]}
  caption="Предикат встраивается в тот же анализ потока, что и typeof: ребро true сужает до T, ребро false вычитает T."
/>

## Проверяющий доверяет вам несоундно

Это надо усвоить: TypeScript **не** проверяет, что тело предиката действительно доказывает заявленный тип. Аннотация `is T` — это ваше утверждение; проверяющий принимает его на веру. Защитник, чьё тело не соответствует его предикату, компилируется без ошибок:

```ts
type User = { id: string; email: string; roles: string[] };

// ЛГУЩИЙ защитник — тело проверяет куда меньше, чем заявляет предикат
function isUser(x: unknown): x is User {
  return typeof x === "object" && x !== null;
}

const maybe: unknown = { id: "u1" };   // ни email, ни roles
if (isUser(maybe)) {
  maybe.email.toLowerCase();
  // ^? проверяющий думает, что maybe: User, email: string
  // Рантайм: TypeError — email это undefined
}
```

Никакой ошибки на этапе компиляции. Предикат — контракт, за соблюдение которого отвечаете только вы. Корректный защитник проверяет *каждое* поле, обещанное предикатом:

```ts
function isUser(x: unknown): x is User {
  return (
    typeof x === "object" && x !== null &&
    "id" in x && typeof (x as Record<string, unknown>).id === "string" &&
    "email" in x && typeof (x as Record<string, unknown>).email === "string" &&
    "roles" in x && Array.isArray((x as Record<string, unknown>).roles)
  );
}
```

<Infographic
  svg={guardTradeoffs}
  label="SWOT пользовательских защитников типов. Сильные стороны: переиспользуемое сужение, включающееся в анализ потока typeof/instanceof, сужают обе ветки, структурные проверки in. Слабые стороны: несоундны по замыслу (тело никогда не проверяется), in хрупок при общем ключе, честная проверка массива — O(n). Возможности: вывести защитники из схемы, разобрать один раз на границе, предпочесть тег-дискриминант. Угрозы: лгущий защитник компилируется, молча мис-типизирует тысячи мест вызова и падает в рантайме вдалеке от причины."
  caption="Каждая сила предиката держится на одном несоундном контракте: проверяющий доверяет `is T` без доказательства, поэтому все угрозы — это следствия тела, проверяющего меньше заявленного."
  hue="var(--d-frontend)"
/>

<Inset kind="why" lang="ru">
Почему TypeScript вообще допускает эту несоундность? Потому что валидация в рантайме в общем случае неразрешима — проверяющий не может статически доказать, что произвольное булево выражение устанавливает структурный тип. Поэтому он проводит намеренную границу: проверяет ваше *использование* предиката (сужение), но делегирует *корректность* тела предиката вам. Это та же модель доверия, что и у `as`-утверждений, просто упакованная в переиспользуемую функцию. Дисциплина, которая это чинит: выводить защитники из единой схемы (Zod `z.infer` + `safeParse`, io-ts, Valibot), чтобы валидатор и тип не разошлись.
</Inset>

## Защитники на основе `in` и различение без тега

Когда у членов нет общего литерального тега, проверка `in` внутри предиката сужает структурно:

```ts
type WithData = { data: string };
type WithError = { error: string };

function hasData(r: WithData | WithError): r is WithData {
  return "data" in r;
}
```

Это работает, но хрупко: если оба члена могли бы нести ключ `data` (пусть даже опционально), тест `in` больше не различает их. Когда вы владеете формами, предпочитайте явный тег-дискриминант — это следующий урок.

## Защитники для массивов и записей

Частая задача — сузить `unknown` (например, JSON) до типизированного массива. Предикат должен проверить и контейнер, *и* выборку элементов — но проверка лишь одного элемента сама по себе несоундный срез:

```ts
function isStringArray(x: unknown): x is string[] {
  return Array.isArray(x) && x.every((e) => typeof e === "string");
}

function isRecordOfNumbers(x: unknown): x is Record<string, number> {
  return (
    typeof x === "object" && x !== null && !Array.isArray(x) &&
    Object.values(x).every((v) => typeof v === "number")
  );
}
```

`x.every(...)` по всему массиву честен; `typeof x[0] === "string"` (выборка одного элемента) — ложь, которой проверяющий всё равно верит. Стоимость `every` реальна для огромных массивов — именно поэтому разобрать-один-раз-на-границе (валидировать payload при входе в систему) лучше, чем перепроверять защитниками повсюду.

<MetaphorComplete
  id="03-guards-metaphor"
  pieceSlug="03-type-guards"
  lang="ru"
  setup="Предикат типа — как вышибала, ставящий браслет «проверенный User». Проверяющий никогда не перепроверяет браслет — поэтому если вышибала ___, все внутри типизированы неверно."
  accepted={["лжёт", "врёт", "ленив", "небрежен", "нечестен", "халтурит"]}
  canonical="лжёт"
  explanation="Проверяющий доверяет вердикту предиката, не перепроверяя его. Лгущее или неполное тело защитника ставит браслет на значения, не являющиеся T, и каждое использование ниже наследует ложное сужение."
/>

## Взгляд вперёд: функции-утверждения

Близкий родственник бросает исключение вместо возврата булева значения. **Функция-утверждение** использует `asserts x is T`: если она вернулась нормально, проверяющий сужает `x` до `T` на *остаток области видимости* — без `if`:

```ts
function assertUser(x: unknown): asserts x is User {
  if (!isUser(x)) throw new Error("not a User");
}

function f(x: unknown) {
  assertUser(x);
  x;
  // ^? User   — сужено отсюда и дальше, ведь управление продолжилось, только если не было броска
}
```

Действует то же несоундное доверие плюс обязанность реально бросить на отрицательном случае. Функции-утверждения вы изучите глубже в юните 06 (`functions-this`).

<FadedExample
  client:visible
  id="03-guards-faded"
  pieceSlug="03-type-guards"
  lang="ru"
  title="Найди лгущий защитник"
  steps={{
    solved: (
      <div class="space-y-3 text-sm">
        <p><strong>Код на ревью:</strong></p>
        <pre class="text-xs bg-zinc-100 p-2 rounded overflow-x-auto"><code>{`type Point = { x: number; y: number };

function isPoint(p: unknown): p is Point {
  return typeof p === "object" && p !== null && "x" in p;
}

const bad: unknown = { x: 1 };  // нет y
if (isPoint(bad)) {
  bad.y.toFixed(2);  // компилируется; рантайм TypeError
}`}</code></pre>
        <p><strong>Почему это ложь:</strong> предикат заявляет <code>p is Point</code> (и <code>x</code>, и <code>y</code> как числа), но тело проверяет лишь наличие <code>x</code> — не его тип, и никогда <code>y</code>. Проверяющий принимает аннотацию <code>is Point</code> на веру и сужает <code>bad</code> до <code>Point</code>, поэтому <code>bad.y</code> типизировано как <code>number</code>. В рантайме <code>y</code> — это <code>undefined</code>, и <code>.toFixed</code> бросает исключение.</p>
        <p><strong>Корректное тело:</strong> <code>typeof p === "object" &amp;&amp; p !== null &amp;&amp; typeof (p as any).x === "number" &amp;&amp; typeof (p as any).y === "number"</code> — проверяй каждое поле, обещанное предикатом.</p>
      </div>
    ),
    semi: {
      prompt: "Функция объявлена `function isBig(n: number): n is number { return n > 100; }` — осмыслен ли здесь предикат `n is number`? Ответь yes или no.",
      blanks: [
        { id: "answer", expected: "no", placeholder: "yes или no" },
      ],
    },
    blank: {
      prompt: "Почему проверяющий НЕ выдаёт ошибку на защитнике, чьё тело проверяет меньше полей, чем заявляет предикат?",
      reveal: (
        <div class="space-y-2 text-sm">
          <p>Потому что доказательство того, что произвольное булево выражение устанавливает структурный тип, неразрешимо, и TypeScript намеренно за это не берётся. Аннотация <code>is T</code> трактуется как утверждение программиста — та же модель доверия, что и каст <code>as</code> — и проверяющий валидирует лишь то, как вызывающие *используют* сужение, но никогда — *заслуживает* ли его тело. Ответственность за корректность целиком на вас; долговечный фикс — генерировать защитники из схемы (например, Zod <code>safeParse</code>), чтобы валидатор и утверждаемый тип не разошлись.</p>
        </div>
      ),
    },
  }}
  misconceptions={{
    "answer:yes": "n уже number — предикат сужает number до number, что бессмысленно. Предикат полезен лишь когда сужает до ПОДТИПА (литерала, члена подмножества или структурного типа), а не до того же типа.",
  }}
/>

<Quiz
  id="03-guards-quiz-1"
  lessonSlug="03-type-guards"
  lang="ru"
  question="Защитнику `isUser(x): x is User`, чьё тело проверяет лишь `typeof x === 'object'`, передан объект без обязательных полей. Что произойдёт?"
  choices={[
    { label: "Он компилируется без ошибок; проверяющий доверяет предикату и сужает x до User, поэтому баг всплывёт лишь в рантайме", correct: true },
    { label: "TypeScript выдаст ошибку, ведь тело не доказывает каждое поле User", misconception: "Проверяющий НЕ сверяет тело предиката с заявленным типом — это и есть та самая несоундность. Он компилируется." },
    { label: "Сужение игнорируется, ведь тело неполное", misconception: "Сужение применяется полностью; проверяющий принимает аннотацию `is User` на веру независимо от полноты тела." },
    { label: "x сужается до `unknown` как защитный запасной вариант", misconception: "Никакого запасного варианта нет — x сужается ровно до заявленного типа User, соундно или нет." },
  ]}
/>

<Quiz
  id="03-guards-quiz-2"
  lessonSlug="03-type-guards"
  lang="ru"
  question="Какое тело предиката честно доказывает `x is string[]`?"
  choices={[
    { label: "`Array.isArray(x) && x.every(e => typeof e === 'string')`", correct: true },
    { label: "`Array.isArray(x) && typeof x[0] === 'string'`", misconception: "Выборка лишь первого элемента несоундна: массив может быть пустым (x[0] это undefined) или иметь нестроковые элементы после индекса 0. Проверяющий всё равно ему верит." },
    { label: "`typeof x === 'object'`", misconception: "Массивы — объекты, но и обычные объекты тоже, а проверки на null нет; это ничего не доказывает ни о типах элементов, ни о массивности." },
    { label: "`x.length > 0`", misconception: "length есть и у строк и ничего не говорит о типах элементов; это не устанавливает string[]." },
  ]}
/>

</Explanation>

<KeyTakeaway>
Пользовательский защитник типа аннотирует возврат как `x is T`, встраиваясь в сужение по потоку управления точно как встроенная проверка: ветка `true` сужает до `T`, ветка `false` его вычитает. Подвох — несоундность: проверяющий доверяет утверждению `is T`, не проверяя тело, поэтому лгущий или неполный защитник компилируется и портит всё сужение ниже, всплывая лишь в рантайме. Пишите защитники, проверяющие каждое обещанное поле, используйте `every` (а не выборку элемента) для массивов, предпочитайте явные теги-дискриминанты `in`, когда владеете формами, а для настоящей безопасности генерируйте защитники из схемы. Функции-утверждения (`asserts x is T`) — это бросающий родственник, разбираемый в юните 06.
</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="03-guards-retrieval"
  lang="ru"
  questions={[
    { q: "Что делает аннотация возврата `x is T`, чего не делает обычный возврат `boolean`?", a: "Обычный возврат boolean ничего не говорит вызывающему о смысле true, поэтому не сужает аргумент. Предикат типа `x is T` связывает булев результат с утверждением о типе: проверяющий сужает аргумент предиката до T на ветке true и вычитает T на ветке false в анализе потока управления вызывающего. Целью предиката должен быть параметр функции или `this`. В рантайме функция всё равно возвращает обычный boolean — `is T` это чисто компиляционная инструкция движку сужения." },
    { q: "Объясни, почему лгущий защитник типа опасен и как возникает несоундность.", a: "Проверяющий НЕ проверяет, что тело предиката действительно устанавливает T — доказать, что произвольное булево выражение влечёт структурный тип, неразрешимо, поэтому TypeScript делегирует корректность автору и валидирует лишь использование сужения вызывающими. Защитник, чьё тело проверяет меньше, чем заявляет предикат (например, лишь typeof === 'object' для `is User`), компилируется чисто и всё равно сужает вызывающих до T. Каждое чтение ниже затем предполагает поля, которых может не быть, и сбой проявляется как рантайм-TypeError далеко от защитника. Фикс — заставить тело проверять каждое обещанное поле или выводить защитник из схемы (Zod safeParse, io-ts), чтобы валидатор и тип шли в ногу." },
    { q: "При структурном сужении почему предпочесть тег-дискриминант защитнику на `in` и каков безопасный паттерн защитника массива?", a: "Защитник на `in` ('k' in x) сужает до тех членов объединения, что объявляют ключ, что хрупко, когда ключ может нести более одного члена (особенно опционально) — тест перестаёт их различать. Когда вы владеете формами, явный литеральный дискриминант (поле kind/type) однозначен, и именно его формализуют размеченные объединения. Для массивов честный защитник проверяет и контейнер, И все элементы: Array.isArray(x) && x.every(e => typeof e === 'string'); выборка лишь x[0] несоундна (пустые массивы, разнородные хвосты), хотя проверяющий ей всё равно доверяет." },
  ]}
/>

<Recap lang="ru">
Пользовательские защитники типов распространяют машинерию сужения из прошлого урока на ваши собственные функции: аннотация возврата `x is T` заставляет проверяющего сузить аргумент до `T` на ветке `true` и вычесть его на ветке `false`. Ключевое свойство — это доверие несоундно: TypeScript никогда не проверяет, что тело предиката доказывает заявленный тип, поэтому неполный или лгущий защитник компилируется чисто и молча портит каждое место вызова, всплывая лишь как падение в рантайме. Честные защитники проверяют каждое обещанное поле, используют `every` по всему массиву, а не выборку, и предпочитают явные теги-дискриминанты тестам `in`, когда вы владеете формами; для продакшен-безопасности выводите защитники из схемы, чтобы валидатор и тип не разошлись. Бросающий вариант, `asserts x is T`, показан здесь и детально разбирается в **юните 06**. Далее **размеченные объединения** превращают хрупкое сужение через `in`/предикат в надёжный паттерн на тегах с проверкой исчерпания. Теперь, когда встречаешь защитник `x is T` на ревью, сразу проверяешь его тело поле за полем — потому что компилятор этого за тебя не сделает.
</Recap>
