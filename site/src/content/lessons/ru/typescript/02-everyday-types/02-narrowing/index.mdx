---
concepts: [control-flow-analysis, narrowing, typeof-guard, truthiness, in-operator, instanceof, exhaustiveness, re-widening]
deepensInto: ["typescript/02-everyday-types/04-discriminated-unions"]
estMin: 14
lang: ru
lessonType: topic
level: junior
order: 2
prereqs: ["typescript/02-everyday-types/01-unions-and-intersections"]
slug: "02-narrowing"
sources:
  - https://www.typescriptlang.org/docs/handbook/2/narrowing.html
spiral: [control-flow, never, closures]
status: ready
summary: "Анализ потока управления отслеживает тип переменной в каждой точке программы. typeof, истинность, in, instanceof и присваивание сужают объединение — но замыкание или await расширяют его обратно."
title: "Сужение и анализ потока управления"
track: "typescript"
unit: "02-everyday-types"
---

import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import FadedExample from "~/components/pedagogy/FadedExample.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
Вы защищаете значение проверкой `if (typeof id === "string")`, и внутри блока проверяющий охотно позволяет вызвать `id.toUpperCase()`. Затем вы оборачиваете этот вызов в колбэк `setTimeout` — и внезапно проверяющий заявляет, что `id` снова `string | number`. С самим `id` ничего не изменилось. Изменилось то, *где* выполняется код относительно проверки. Сужение — это не свойство переменной; это свойство *точки программы*. Понимание того, как проверяющий вычисляет тип в каждой точке — и когда сдаётся, — отделяет борьбу с компилятором от управления им.
</Hook>

<Crux>Сужение — это анализ потока управления: проверяющий пересчитывает тип переменной в каждой точке программы из доминирующих над ней проверок.</Crux>

<Explanation>

## Проверяющий отслеживает тип в каждой точке программы

У переменной-объединения нет одного типа на всю область видимости. Проверяющий обходит граф потока управления и в каждой точке держит *самый узкий* тип, всё ещё совместимый с проверками, через которые туда пришли. Проверка расщепляет поток: ветка `true` несёт одно сужение, ветка `false` — дополнение.

```ts
function format(x: string | number) {
  // x: string | number
  if (typeof x === "string") {
    x;
    // ^? (parameter) x: string
    return x.toUpperCase();
  }
  x;
  // ^? (parameter) x: number   — дополнение к строковой ветке
  return x.toFixed(2);
}
```

`else` (здесь — проход после `return`) получает `number` бесплатно: проверяющий вычитает `string` из объединения, ведь эта ветка уже обработана. Поэтому ранний `return` внутри проверки сужает *остаток* функции.

<FlowDiagram
  label="Сужение по потоку управления для объединения строка-или-число"
  hue="var(--d-frontend)"
  perRow={4}
  nodes={[
    { id: "u", label: "x", sub: "string | number" },
    { id: "g", label: "typeof x", sub: "=== string?" },
    { id: "t", label: "ветка true", sub: "x: string" },
    { id: "f", label: "ветка false", sub: "x: number" },
  ]}
  edges={[
    { from: "u", to: "g", label: "проверка" },
    { from: "g", to: "t", label: "true" },
    { from: "g", to: "f", label: "false" },
  ]}
  caption="Каждая проверка расщепляет поток: ребро true сужает, ребро false несёт дополнение объединения."
/>

## Пять повседневных операторов сужения

```ts
type Val = string | number | boolean | null | string[];

function inspect(v: Val) {
  if (typeof v === "number") v;        // ^? number  (проверка typeof)
  if (v === null) return;              // проверка равенства → ниже v без null
  if (Array.isArray(v)) v;             // ^? string[]  (встроенная проверка)
  if (v) v;                            // ^? string | number | boolean | string[] (истинность убирает null)
}
```

Повседневные инструменты, каждый сужает чуть по-своему:

- **`typeof`** — расщепляет по рантайм-тегам JS (`"string"`, `"number"`, `"boolean"`, `"object"`, `"function"`, `"undefined"`, `"symbol"`, `"bigint"`). Учтите: `typeof null === "object"` — причуда JS, которую проверяющий уважает.
- **Истинность** — `if (v)` убирает из типа `null`, `undefined`, `0`, `""`, `false`, `NaN`. Отлично для `string | null`, опасно для `number | undefined` (заодно отбрасывает `0`).
- **Равенство** — `===`/`!==` против литерала или `null`/`undefined` сужает обе ветки.
- **`in`** — `if ("radius" in shape)` сужает до членов объединения, объявляющих это свойство. Структурно, а не номинально.
- **`instanceof`** — сужает до класса (и подклассов) по цепочке прототипов в рантайме.

<Inset kind="edgecase" lang="ru">
`typeof v === "object"` коварен. Он `true` и для массивов, и для `null`, и для обычных объектов. Под этой проверкой проверяющий сужает `string | number[] | null` до `number[] | null` (в рантайме оба — "object"), а *не* до `number[]`. С `null` всё ещё придётся разбираться. Предпочитайте `Array.isArray` и явные проверки `=== null` голому `typeof === "object"`.
</Inset>

## Сужение присваиванием и разница `let`/`const`

Присваивание значения сужает переменную к типу присвоенного значения начиная с этой точки — но лишь в пределах, разрешённых *объявленным* типом.

```ts
let s: string | number;
s = "hello";
s;
// ^? string   — присваивание сузило
s.toUpperCase();   // здесь OK

declare function rand(): string | number;
s = rand();
s;
// ^? string | number   — расширено обратно до объявленного типа
```

`const` отличается от `let` для вывода *литерала*, который питает сужение. Привязка `const` к литералу получает литеральный тип; привязка `let` расширяется до базового типа:

```ts
const dir = "up";
//    ^? "up"      — литеральный тип, годится как дискриминант
let dir2 = "up";
//    ^? string    — расширен, больше не дискриминант
```

Это колоссально важно для размеченных объединений (через урок): дискриминант, прочитанный в `const`, сохраняет литеральный тип; скопированный в `let`, он расширяется, и сужение молча перестаёт работать.

## Исчерпывание через `never`

Когда вы сузили объединение, убрав каждый его член, оставшийся тип — `never`. Присваивание его переменной типа `never` превращает «я забыл случай» в ошибку компиляции:

```ts
function area(s: { kind: "circle"; r: number } | { kind: "square"; side: number }): number {
  switch (s.kind) {
    case "circle": return Math.PI * s.r ** 2;
    case "square": return s.side ** 2;
    default: {
      const _exhaustive: never = s;
      // Если добавить новый вариант, s здесь уже не never →
      // Error: Type '{ kind: "triangle"; ... }' is not assignable to type 'never'.
      return _exhaustive;
    }
  }
}
```

Это сеньорский паттерн безопасного рефакторинга: добавляешь вариант — и каждый необработанный `switch` загорается красным. Далее вы его формализуете.

## Сужение теряется через границы функций

Это ловушка, в которую попадают все. Проверяющий считает, что любой *вызов функции* между проверкой и использованием мог изменить захваченную переменную, поэтому отбрасывает сужение для переменных, на которые не может доказать отсутствие влияния:

```ts
function handle(id: string | number) {
  if (typeof id === "string") {
    id;
    // ^? string
    setTimeout(() => {
      id;
      // ^? string | number   — расширено обратно внутри замыкания!
      // id.toUpperCase();  // Error: Property 'toUpperCase' does not exist on type 'number'.
    });
  }
}
```

Замыкание может выполниться *позже*, когда `id` (в принципе) уже могли переприсвоить, поэтому проверяющий консервативно расширяет его до объявленного типа. То же происходит после `await`, ведь управление покинуло функцию и вернулось. Решения: скопировать суженное значение в `const` *до* границы (`const sid = id;`, пока `id` — `string`), или сделать захватываемую привязку `const`, чтобы проверяющий знал о невозможности переприсваивания.

```ts
function handle(id: string | number) {
  if (typeof id === "string") {
    const sid = id;        // const захватывает суженную строку
    setTimeout(() => sid.toUpperCase());  // OK — sid это string
  }
}
```

<DragOrder
  id="02-narrowing-dragorder"
  pieceSlug="02-narrowing"
  lang="ru"
  prompt="Упорядочи шаги, которые проверяющий выполняет, чтобы вывести тип переменной внутри защищённой ветки."
  items={[
    "Начать с объявленного (возможно, объединённого) типа переменной",
    "Дойти до выражения-проверки (typeof / in / равенство)",
    "Расщепить поток управления: ребро true и ребро false",
    "На ребре true оставить только члены, совместимые с проверкой",
    "Расширить обратно до объявленного типа, если вмешался вызов функции или await",
  ]}
/>

<FadedExample
  client:load
  id="02-narrowing-faded"
  pieceSlug="02-narrowing"
  lang="ru"
  title="Почему тип снова расширяется?"
  steps={{
    solved: (
      <div class="space-y-3 text-sm">
        <p><strong>Код на ревью:</strong></p>
        <pre class="text-xs bg-zinc-100 p-2 rounded overflow-x-auto"><code>{`async function load(input: string | null) {
  if (input === null) return;
  input;            // ^? string
  await delay(10);
  input;            // ^? string | null  ← расширено
  input.trim();     // Error: 'input' is possibly 'null'.
}`}</code></pre>
        <p><strong>Почему:</strong> <code>await</code> приостанавливает и возобновляет функцию. Проверяющий не может доказать, что <code>input</code> не переприсвоили во время приостановки (мог выполниться другой async-таск), поэтому консервативно восстанавливает объявленный тип <code>string | null</code>. Сужение — это свойство непрерывного пути потока управления; <code>await</code> разрывает путь.</p>
        <p><strong>Фикс:</strong> захватить до границы: <code>const s = input;</code> сразу после проверки на null, затем использовать <code>s</code> (тип <code>string</code>) после await. <code>const</code> нельзя переприсвоить, поэтому его сужение выживает.</p>
      </div>
    ),
    semi: {
      prompt: "После `if (typeof v === 'string') { ... }`, где v: string | number, каков тип v в коде ПОСЛЕ if-блока (на проходе), если блок всегда делает return?",
      blanks: [
        { id: "answer", expected: "number", placeholder: "тип" },
      ],
    },
    blank: {
      prompt: "Почему `let x = 'GET'` даёт x тип `string`, а `const x = 'GET'` — `'GET'`, и почему это важно для сужения?",
      reveal: (
        <div class="space-y-2 text-sm">
          <p><code>let</code> можно переприсвоить любой строкой, поэтому проверяющий расширяет выведенный тип до базового <code>string</code>. <code>const</code> переприсвоить нельзя, поэтому проверяющий хранит точный литеральный тип <code>"GET"</code>. Это важно, ведь дискриминант обязан быть литеральным типом, чтобы сужение работало: <code>switch (req.method)</code> может сузить объединение форм <code>{`{ method: "GET" }`}</code>, только если <code>method</code> — литерал <code>"GET"</code>, а не расширенный <code>string</code>. Копирование дискриминанта в <code>let</code> молча отключает сужение.</p>
        </div>
      ),
    },
  }}
  misconceptions={{
    "answer:string | number": "После защищённого блока, который всегда делает return, проверяющий вычитает обработанный тип. Строковый случай завершился return, поэтому на проходе остаётся только number.",
  }}
/>

<Quiz
  id="02-narrowing-quiz-1"
  lessonSlug="02-narrowing"
  lang="ru"
  question="Внутри `setTimeout(() => { /* использование id */ })` ранее суженный `id` снова показан как `string | number`. Почему?"
  choices={[
    { label: "Колбэк может выполниться позже; проверяющий не может доказать, что id не переприсвоили, поэтому консервативно расширяет до объявленного типа", correct: true },
    { label: "В определениях типов setTimeout есть баг", misconception: "Типы библиотеки верны. Расширение обратно — намеренная консервативность потока управления, а не баг типизации." },
    { label: "Сужение работает только с `const`, никогда с `let`", misconception: "Сужение работает с let на прямолинейном пути; здесь проблема в границе функции, а не в let против const как таковых." },
    { label: "Проверки typeof вообще не сужают внутри замыканий", misconception: "typeof сужает нормально; сужение просто отбрасывается на границе функции, потому что колбэк отложен." },
  ]}
/>

<Quiz
  id="02-narrowing-quiz-2"
  lessonSlug="02-narrowing"
  lang="ru"
  question="Для `v: number | undefined` — почему `if (v) { ... }` рискованная проверка сужения?"
  choices={[
    { label: "Истинность также отвергает 0, поэтому легитимное значение 0 попадает в ветку else вместе с undefined", correct: true },
    { label: "Она вообще не сужает", misconception: "Она сужает — проблема в том, что сужает слишком агрессивно, отбрасывая и 0, и undefined." },
    { label: "Она сужает v до `string`", misconception: "В объединении нет string; истинность на number | undefined даёт number в ветке true." },
    { label: "Проверки истинности — ошибка компиляции", misconception: "Они корректны и распространены; опасность молчаливая, а не ошибка компиляции: 0 ложно и исключается." },
  ]}
/>

</Explanation>

<KeyTakeaway>
Сужение — это анализ потока управления: проверяющий пересчитывает тип переменной в каждой точке программы из проверок на пути к ней. `typeof`, истинность, равенство, `in` и `instanceof` расщепляют поток и оставляют совместимые члены; ветка false получает дополнение; исчерпанное объединение становится `never`. Главные ловушки: истинность на числовых объединениях молча отбрасывает `0`; `let` расширяет литералы, и дискриминанты перестают работать; а любой вызов функции, замыкание или `await` между проверкой и использованием расширяют до объявленного типа. Захватите суженное значение в `const` до границы, чтобы сохранить его.
</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="02-narrowing-retrieval"
  lang="ru"
  questions={[
    { q: "Объясни, что вычисляет анализ потока управления и почему у переменной разные типы на разных строках.", a: "Анализ потока управления обходит граф потока управления и в каждой точке программы вычисляет самый узкий тип, всё ещё совместимый с каждой проверкой на пути туда. Поэтому у переменной-объединения нет одного типа на всю область видимости: в ветке true для `if (typeof x === 'string')` это string; на ребре false/прохода — дополнение (объединение минус string). Проверки расщепляют поток, ранние return вычитают обработанный тип из остатка, а исчерпание объединения даёт never. Тип — функция точки программы, а не объявления." },
    { q: "Перечисли пять повседневных операторов сужения и одну подножку для каждого.", a: "(1) typeof — расщепляет по рантайм-тегам JS; подножка: typeof null === 'object', поэтому не отделяет объекты от null. (2) Истинность if(v) — убирает null/undefined/0/''/false/NaN; подножка: молча отбрасывает 0 из числовых объединений. (3) Равенство ===/!== против литерала или null — сужает обе ветки; подножка: работает только против литерала/известного значения. (4) in — 'k' in obj сужает до членов, объявляющих k; подножка: структурно, член, у которого случайно есть ключ, тоже совпадёт. (5) instanceof — сужает по цепочке прототипов; подножка: подводит между realm'ами/iframe'ами и для обычных объектов, не являющихся экземплярами класса." },
    { q: "Почему сужение теряется через замыкание или await и как его сохранить?", a: "Проверяющий считает, что любой промежуточный вызов функции, отложенный колбэк или await мог переприсвоить захваченную (let/var) привязку, ведь управление может покинуть функцию и вернуться. Чтобы оставаться корректным, он отбрасывает сужение и восстанавливает объявленный тип в этой точке. Чтобы сохранить: скопировать суженное значение в const ДО границы (const s = x, пока x — string), затем использовать const внутри замыкания/после await; const нельзя переприсвоить, поэтому проверяющий доверяет сужению. Иначе объяви исходную привязку как const, и расширение обратно никогда не случится." },
  ]}
/>

<Recap lang="ru">
Сужение — это анализ потока управления проверяющего в действии: в каждой точке программы он держит самый узкий тип, совместимый с проверками на пути туда, поэтому одна и та же переменная может быть `string` на одной строке и `number` на следующей. Повседневные операторы — `typeof`, истинность, равенство, `in`, `instanceof` — каждый расщепляет поток и оставляет совместимые члены, ветка false наследует дополнение, а исчерпанное объединение становится `never` (основа проверок исчерпания). Ловушки, которые надо усвоить: истинность отбрасывает `0` из числовых объединений, `let` расширяет литеральные дискриминанты, а замыкание или `await` расширяют до объявленного типа, если не захватить суженное значение в `const` заранее. Далее **защитники типов** позволят вам писать собственные функции-предикаты, участвующие ровно в этой машинерии потока управления.
</Recap>
