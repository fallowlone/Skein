infographic chart-column-simple
data
  title Рантайм-JS на конструкцию (в байтах)
  desc Объединение литералов стирается в ничто; каждый числовой enum испускает объект с обратным отображением — около 150–400 байт
  values
    - label Объединение литералов
      value 0
    - label enum (лучший случай)
      value 150
    - label enum (худший случай)
      value 400
theme
  palette #14b8a6 #f59e0b #f43f5e
