---
concepts:
  - type-inference
  - widening
  - contextual-typing
  - best-common-type
  - const-assertion
  - return-type-inference
deepensInto:
  - "typescript/01-foundations/04-literal-types"
estMin: 13
lang: ru
lessonType: topic
level: junior
order: 2
prereqs:
  - "typescript/01-foundations/01-structural-typing"
slug: "02-inference-basics"
sources:
  - https://www.typescriptlang.org/docs/handbook/type-inference.html
  - https://www.typescriptlang.org/docs/handbook/2/everyday-types.html
spiral:
  - inference
  - type-safety
status: ready
summary: "TypeScript выводит большинство типов, чтобы их не приходилось писать. Правила — типизация по инициализатору, расширение при const против let, контекстная типизация колбэков, наилучший общий тип массивов — предсказуемы, стоит их увидеть. Аннотируйте границы, выводите локальные."
title: "Основы вывода: как проверщик заполняет типы, которые вы не написали"
track: "typescript"
unit: "01-foundations"
---

import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import MetaphorComplete from "~/components/pedagogy/MetaphorComplete.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";
import Infographic from "~/components/diagram/Infographic.astro";
import inferenceCost from "./infographics/inference-cost.svg?raw";

<Hook>
Вы добавляете `: string` к локальной переменной, и ревьюер пишет комментарий: «избыточно — TS уже знает». А на следующей строке вы пишете `const handler = items.map(x => x.toUpperCase())`, и параметр колбэка `x` типизируется правильно *вообще без аннотации*. TypeScript не угадывает; он гоняет небольшой детерминированный алгоритм вывода. Изучите его правила — и будете аннотировать ровно там, где это окупается, и нигде больше.
</Hook>

<Crux>Вывод идёт от инициализаторов и контекста; `const` хранит литералы, `let` их расширяет, а колбэки берут типы параметров из ожидаемого типа.</Crux>

<Explanation>

## Вывод из инициализатора

Когда вы объявляете переменную со значением, TypeScript выводит её тип из правой части. Но *точный* тип зависит от ключевого слова связывания из-за **расширения** (widening):

```ts
let a = "hello";
//  ^? let a: string  — `let` изменяем, поэтому литерал расширяется до базового типа

const b = "hello";
//    ^? const b: "hello"  — `const` не может измениться, литеральный тип сохраняется
```

Это самое важное правило вывода, которое стоит впитать. `let` можно переприсвоить, поэтому привязка его к литералу `"hello"` была бы бесполезна — TS расширяет до `string`. `const` неизменяем, поэтому точный литеральный тип `"hello"` и безопасен, и информативнее. То же с числами и булевыми: `let n = 3` это `number`, `const n = 3` это `3`.

## Свойства объекта расширяются — даже под const

Вот сюрприз, на котором спотыкаются все. `const` на *связывании* не делает *содержимое* неизменяемым, потому что свойства объекта изменяемы:

```ts
const config = { mode: "dark", retries: 3 };
//    ^? const config: { mode: string; retries: number }
//       НЕ { mode: "dark"; retries: 3 } — свойства расширяются
config.mode = "light"; // законно — свойство изменяемо, поэтому его тип расширен до `string`
```

`const` замораживает лишь *ссылку* `config`, а не `config.mode`. Поскольку свойство можно переприсвоить, TS расширяет его до `string`. Когда литеральные типы действительно нужно сохранить — скажем, чтобы передать в функцию, ожидающую объединение, — используйте **утверждение** `const`:

```ts
const config = { mode: "dark", retries: 3 } as const;
//    ^? const config: { readonly mode: "dark"; readonly retries: 3 }
config.mode = "light";
// Error: Cannot assign to 'mode' because it is a read-only property.
```

`as const` делает каждое свойство `readonly` и сохраняет его литеральный тип. Глубоко это разберём в уроке о литеральных типах; пока знайте — это лекарство, когда расширение выбрасывает информацию, которая вам была нужна.

## Контекстная типизация: колбэки получают типы бесплатно

Вывод также течёт *внутрь* от ожидаемого типа. Когда функция ждёт колбэк, параметры колбэка типизируются из этого ожидания — вы их не аннотируете:

```ts
const names = ["ada", "alan", "grace"];
const upper = names.map((n) => n.toUpperCase());
//                       ^? (parameter) n: string
//    ^? const upper: string[]
```

`n` это `string`, потому что `Array<string>.map` объявляет свой колбэк как `(value: string, ...) => U`. Это **контекстная типизация**: позиция, в которой находится функциональный литерал, поставляет типы параметров. Поэтому DOM-обработчики тоже работают без аннотаций:

```ts
button.addEventListener("click", (e) => {
  //                                ^? (parameter) e: MouseEvent
  console.log(e.clientX);
});
```

Строка `"click"` выбирает перегрузку, чей обработчик принимает `MouseEvent`, и это течёт в `e`. Аннотация `e: MouseEvent` здесь — избыточный шум.

<Inset kind="why" lang="ru">
Контекстная типизация работает, только когда ожидаемый тип виден в точке написания функционального литерала. Вынесите колбэк в отдельный `const fn = (e) => ...` — и контекста больше нет: `e` падает в неявный `any` (ошибка при `noImplicitAny`). Такова сделка: встроенные колбэки получают типизацию бесплатно; вынесенным нужна явная аннотация параметра или типизированный псевдоним.
</Inset>

## Наилучший общий тип для литералов массива

Когда вы пишете литерал массива, TypeScript вычисляет единый тип элемента — **наилучший общий тип** (best common type) — которому удовлетворяют все элементы:

```ts
const xs = [1, 2, 3];
//    ^? const xs: number[]

const mixed = [1, "two", 3];
//    ^? const mixed: (string | number)[]  — объединение типов элементов
```

Если у элементов нет общего супертипа в области видимости, TS образует их объединение. Заметьте: `const` на связывании *не* сохраняет `[1,2,3]` как кортеж `[number, number, number]` — это по-прежнему `number[]`; только `as const` даёт кортеж `readonly [1, 2, 3]` (разбираем в уроке о литеральных типах).

## Типы возврата тоже выводятся

Вы почти никогда не аннотируете тип возврата для внутренних хелперов — TS выводит его из операторов `return`:

```ts
function parsePort(raw: string) {
  const n = Number(raw);
  return Number.isInteger(n) ? n : null;
}
//       ^? function parsePort(raw: string): number | null
```

Выведенный возврат — `number | null` из двух ветвей. Это отлично для локального кода — но острый инструмент на границах API.

## Когда аннотировать, а когда выводить

Это вопрос, который рано или поздно ставит каждая TypeScript-кодовая база: аннотируешь лишнее — борешься с проверщиком; аннотируешь мало — теряешь контракт. Вот правило, к которому сходятся профессиональные кодовые базы:

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Ситуация</th>
        <th class="border border-border px-3 py-2 text-left">Аннотировать?</th>
        <th class="border border-border px-3 py-2 text-left">Почему</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">Локальная переменная с инициализатором</td>
        <td class="border border-border px-3 py-2">Нет</td>
        <td class="border border-border px-3 py-2">Вывод точен и остаётся синхронным со значением</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Параметр встроенного колбэка</td>
        <td class="border border-border px-3 py-2">Нет</td>
        <td class="border border-border px-3 py-2">Его поставляет контекстная типизация</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Тип возврата экспортируемой функции</td>
        <td class="border border-border px-3 py-2">Да</td>
        <td class="border border-border px-3 py-2">Фиксирует публичный контракт; не даёт внутреннему изменению тихо расширить API</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Параметры функции</td>
        <td class="border border-border px-3 py-2">Да</td>
        <td class="border border-border px-3 py-2">Параметры — входная граница; вывод не видит вызывающих</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Пустой `let x;` с присваиванием позже</td>
        <td class="border border-border px-3 py-2">Да</td>
        <td class="border border-border px-3 py-2">Голый `let` выводит `any`; аннотация сохраняет проверку</td>
      </tr>
    </tbody>
  </table>
</div>

Принцип: **аннотируйте границы, выводите внутренности.** Явные типы возврата экспортируемых функций — аннотации наивысшей ценности: они делают контракт намеренным и ловят случайное расширение при рефакторинге тела.

<Inset kind="why" lang="ru">
Где вывод *вредит*, с числами. Вывод бесплатен, пока он локален, но на границах модулей у него реальная, измеримая цена. Когда у экспортируемой функции нет аннотации возврата, `tsc` вынужден заново выводить этот тип возврата для каждого файла, который её импортирует, и сложные выведенные возвраты (глубокие дженерики, условные типы, длинные объединения) — классическая причина медленных сборок. На репозитории ~900 файлов React + tRPC (TS 5.5) один слишком умный хелпер, чей тип возврата вывелся в условно-отображённый тип из 30 членов, поднял `tsc --noEmit` с 9.4с до 16.1с; `--extendedDiagnostics` показал, что «Check time» почти удвоилось, а число инстанцирований подскочило до ~3.4M против потолков 100k на проверку / 5M отношений, при этом подсказка IntelliSense на местах вызова отставала на ~600мс+, пока редактор пересчитывал тип. Закрепление написанного руками типа возврата вернуло его к ~9.6с. Компромисс сеньора: на внутренних местах вызова *доверяйте выводу* — аннотации там шум, который рассинхронизируется; на экспортируемых границах *заплатите одной аннотацией* — три нажатия в обмен на стабильный публичный контракт, более быстрые инкрементальные проверки и более отзывчивый редактор у каждого потребителя.
</Inset>

<Infographic
  svg={inferenceCost}
  label="Столбчатая диаграмма времени tsc --noEmit на репозитории из 900 файлов: 9.4с в базе, 16.1с, когда тип возврата одного экспортируемого хелпера выведен в условный тип из 30 членов, и 9.6с после закрепления явного типа возврата."
  caption="Слишком умный выведенный возврат экспортируемой функции почти удвоил проверку типов; одна написанная руками аннотация возврата вернула базовый уровень. Аннотируйте границы, выводите внутренности."
  hue="var(--d-infra)"
/>

<FlowDiagram
  label="Путь решения о выводе: как тип объявления выбирается из ключевого слова, значения и окружающего контекста."
  hue="var(--d-frontend)"
  perRow={3}
  nodes={[
    { id: "decl", label: "Объявление", sub: "let/const + значение" },
    { id: "ctx", label: "Есть контекст?", sub: "ожидаемый тип?" },
    { id: "use", label: "Берём контекст", sub: "параметры колбэка" },
    { id: "kw", label: "Ключевое слово?", sub: "const или let" },
    { id: "keep", label: "Хранит литерал", sub: "const -> a" },
    { id: "widen", label: "Расширяет", sub: "let -> string" },
  ]}
  edges={[
    { from: "decl", to: "ctx" },
    { from: "ctx", to: "use", label: "да" },
    { from: "ctx", to: "kw", label: "нет" },
    { from: "kw", to: "keep", label: "const" },
    { from: "kw", to: "widen", label: "let" },
  ]}
  caption="Контекстная типизация побеждает при наличии ожидаемого типа; иначе ключевое слово решает, сохранить литерал (const) или расширить (let)."
/>

<Quiz
  id="02-infer-quiz-1"
  lessonSlug="02-inference-basics"
  lang="ru"
  question="Каков выведенный тип `mode` в `const config = { mode: 'dark' }` (без `as const`)?"
  choices={[
    { label: "string — свойства объекта расширяются до базового типа, потому что они изменяемы", correct: true },
    { label: "'dark' — `const` всегда сохраняет литеральный тип", misconception: "`const` сохраняет литерал лишь для *связывания* (примитивный const вроде `const x = 'dark'`). Свойства объекта изменяемы, поэтому расширяются до `string`, если не добавить `as const`." },
    { label: "readonly 'dark' — `const` делает свойства readonly", misconception: "`const` замораживает ссылку `config`, а не её члены. Свойства становятся readonly только при утверждении `as const`." },
    { label: "any — TypeScript не может вывести типы свойств объекта", misconception: "TS выводит типы свойств точно; здесь он выводит `string` через расширение, а не `any`." },
  ]}
/>

<MetaphorComplete
  id="02-infer-metaphor-1"
  pieceSlug="02-inference-basics"
  lang="ru"
  setup="Параметр колбэка, получающий тип от функции, в которую он передан — без какой-либо аннотации — типизируется ____ типизацией."
  accepted={["контекстной", "контекстная", "контекстная типизация", "контекст"]}
  canonical="контекстной"
  explanation="Контекстная типизация течёт ожидаемым типом внутрь: позиция, которую занимает функциональный литерал (напр. аргумент Array.map или addEventListener), поставляет типы параметров, так что аннотировать их не нужно."
/>

</Explanation>

<KeyTakeaway>
TypeScript выводит из инициализаторов, и ключевое слово связывания решает расширение: `const x = "a"` сохраняет литерал `"a"`, `let x = "a"` расширяет до `string`. *Свойства* объекта расширяются даже под `const` (используйте `as const`, чтобы их сохранить). Контекстная типизация течёт ожидаемым типом в параметры колбэка, так что встроенным колбэкам аннотации не нужны. Литералы массивов получают наилучший общий тип (объединение при необходимости); типы возврата выводятся из ветвей. На практике: аннотируйте границы — параметры функций и типы возврата экспортируемых функций — а локальные доверяйте выводу.
</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="02-infer-retrieval"
  lang="ru"
  questions={[
    { q: "Объясните расширение при const против let и почему свойства объекта расширяются, даже когда связывание — const.", a: "Когда вы инициализируете переменную, TS выводит её тип из значения, но расширение зависит от изменяемости. `let` можно переприсвоить, поэтому литеральный инициализатор расширяется до базового типа: `let x = 'a'` это `string`, `let n = 3` это `number`. Связывание `const` не может измениться, поэтому точный литеральный тип сохраняется и безопасен: `const x = 'a'` это `'a'`, `const n = 3` это `3`. Свойства объекта — отдельная история: `const` замораживает лишь *ссылку*, а не члены — `config.mode = ...` по-прежнему законно — поэтому каждое свойство считается изменяемым и расширяется. Так `const config = { mode: 'dark' }` выводит `{ mode: string }`, а не `{ mode: 'dark' }`. Чтобы сохранить литералы свойств (и сделать их readonly), используйте утверждение `as const`." },
    { q: "Что такое контекстная типизация, где она применяется и когда перестаёт работать?", a: "Контекстная типизация — это вывод, текущий *внутрь* от ожидаемого типа к параметрам функционального литерала. Когда функция ждёт колбэк — `Array<string>.map((value: string) => U)`, `addEventListener('click', handler)` — типы параметров берутся из этого ожидания, так что их не аннотируют: `names.map(n => ...)` типизирует `n` как `string`, а `e` обработчика клика — `MouseEvent`. Это работает, только когда ожидаемый тип виден в точке написания функционального литерала. Вынесите колбэк в отдельный `const fn = (e) => ...` — контекст пропадает, и `e` становится неявным `any` (ошибка при noImplicitAny) — придётся аннотировать параметр или дать const функциональный тип." },
    { q: "Какова практическая политика аннотаций — что аннотировать, а что доверить выводу и почему?", a: "Аннотируйте границы; выводите внутренности. Доверьте выводу: локальные переменные с инициализаторами (выведенный тип синхронен со значением и точен) и параметры встроенных колбэков (их поставляет контекстная типизация). Аннотируйте явно: параметры функций (это входная граница — вывод не видит вызывающих, а голые параметры стали бы неявным any) и типы возврата *экспортируемых* функций. Явный тип возврата публичной функции — аннотация наивысшей ценности: делает контракт намеренным и не даёт внутренней правке тихо расширить или изменить поверхность API. Также аннотируйте голый `let x;`, объявленный до присваивания, иначе он выведет `any`. Избыточные аннотации локальных — просто шум, который рассинхронизируется." },
  ]}
/>

<Recap lang="ru">
Вывод — причина, почему TypeScript не мешает вам: формы, которые вы научились разбирать структурно в прошлом уроке, по большей части заполняются за вас, а тяжёлую работу делают расширение и контекстная типизация. Правила расширения, которые вы только что увидели — `const`, хранящий `"a"`, `let`, расширяющий до `string`, расширение свойств, `as const` как лазейка — это ровно та механика, на которой строится следующий урок о **литеральных типах**. Но сперва заглянем в три особых типа — **`any`, `unknown` и `never`** — потому что понимание того, как вывод и совместимость ведут себя на вершине и дне решётки типов, и удерживает `any` от тихого отравления всего, что вывод вам только что дал. Теперь, когда видите комментарий ревьюера «избыточная аннотация», проверьте, не покрывает ли вывод этот слот уже — а когда `tsc` тормозит, посмотрите, нет ли экспортируемой функции без аннотации типа возврата.
</Recap>
