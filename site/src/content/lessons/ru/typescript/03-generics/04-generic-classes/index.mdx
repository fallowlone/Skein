---
concepts:
- generic-classes
- generic-interfaces
- instance-scoped-type-params
- static-side
- container-types
deepensInto:
- typescript/03-generics/05-variance-intuition
estMin: 15
lang: ru
lessonType: topic
level: middle
order: 4
prereqs:
- typescript/03-generics/03-defaults-and-inference
slug: 04-generic-classes
sources:
- https://www.typescriptlang.org/docs/handbook/2/generics.html#generic-classes
spiral:
- relationships
- structural-typing
status: ready
summary: Параметр типа класса привязан к экземпляру, а не к статической стороне.
  Типизированные контейнеры вроде Stack<T> и Result<T, E> показывают почему — а
  структурная совместимость экземпляров предвещает вариантность.
title: Дженерик-классы
track: typescript
unit: 03-generics
---

import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import MetaphorComplete from "~/components/pedagogy/MetaphorComplete.astro";
import StackDiagram from "~/components/diagram/StackDiagram.astro";

<Hook>
Вы добавляете фабрику `static empty(): Box<T>` к классу `Box<T>`, и компилятор отказывает: `Static members cannot reference class type parameters.` Кажется произволом — класс ведь дженерик, почему его статический хелпер не может? Ответ раскрывает, чем именно *является* параметр типа класса и когда он привязывается к конкретному типу.
</Hook>

<Crux>Параметр типа класса привязывается при создании экземпляра; статика существует до любого экземпляра, поэтому увидеть его не может.</Crux>

<Explanation>

## Класс, параметризованный типом

Зачем ставить параметр типа на класс, а не на каждый метод по отдельности? Потому что вы хотите, чтобы все методы экземпляра разделяли один и тот же разрешённый `T` — так `Box<number>` всегда работает только с числами, на каком бы методе вы ни остановились. Дженерик-класс объявляет параметр типа, который каждый *экземпляр* заполняет:

```ts
class Box<T> {
  constructor(public value: T) {}
  map<U>(fn: (v: T) => U): Box<U> {
    return new Box(fn(this.value));
  }
}

const b = new Box(42);
//    ^? const b: Box<number>   — T выведен из аргумента конструктора
const s = b.map((n) => n.toFixed(2));
//    ^? const s: Box<string>   — собственный U метода map выведен из колбэка
```

`T` решается из аргумента конструктора, ровно как дженерик-функция выводит из аргументов вызова. Метод `map<U>` вводит *свой* параметр типа `U`, независимый от `T` экземпляра.

## Ограничение статической стороны

Каждый экземпляр привязывает `T` к конкретному типу. Но статические члены принадлежат *объекту класса*, который существует один раз, до и поверх всех экземпляров — у них нет единого `T`, на который сослаться:

```ts
class Box<T> {
  constructor(public value: T) {}

  // Error: Static members cannot reference class type parameters.
  static empty(): Box<T> {
    return new Box(undefined as any);
  }
}
```

Фикс — сделать сам статический член дженериком: со своим параметром типа, разрешаемым в точке статического вызова:

```ts
class Box<T> {
  constructor(public value: T) {}
  static of<U>(value: U): Box<U> {
    return new Box(value);
  }
}

const b = Box.of("hi");
//    ^? const b: Box<string>
```

`Box.of<U>` не имеет отношения к `T` какого-либо экземпляра; он решается в точке статического вызова. Поэтому фабричные методы пишут как самостоятельные дженерики.

## Реальный контейнер: Result&lt;T, E&gt;

Два параметра типа моделируют успех-или-неудачу без исключений:

```ts
type Result<T, E> =
  | { ok: true; value: T }
  | { ok: false; error: E };

function ok<T>(value: T): Result<T, never> {
  return { ok: true, value };
}
function err<E>(error: E): Result<never, E> {
  return { ok: false, error };
}

function parsePort(s: string): Result<number, string> {
  const n = Number(s);
  return Number.isInteger(n) && n > 0 ? ok(n) : err(`bad port: ${s}`);
}

const r = parsePort("8080");
if (r.ok) {
  r.value;
//  ^? (property) value: number   — сужено дискриминантом
} else {
  r.error;
//  ^? (property) error: string
}
```

`ok` возвращает `Result<T, never>`, а `err` — `Result<never, E>`, так что неиспользуемая сторона это `never` — присваиваемый любому ожидаемому типу ошибки или значения. Дискриминант `ok` затем сужает объединение (это был урок про размеченные объединения) до полезной нагрузки ровно одной ветви.

<StackDiagram
  label="Result сужается от объединения к одной ветви"
  hue="var(--accent)"
  caption="Result<T, E> — размеченное объединение; проверка `.ok` схлопывает двухвариантный тип до единственной ветви, чью нагрузку можно читать."
  layers={[
    { label: "Result<number, string>", note: "полное объединение" },
    { label: "{ ok: true; value: number }", note: "if (r.ok)" },
    { label: "{ ok: false; error: string }", note: "else" },
    { label: "читать value ИЛИ error — не оба", note: "сужено" },
  ]}
/>

## Дженерик-интерфейсы для фабрик

Интерфейсы тоже несут параметры типа — так типизируют фабрику или контракт коллекции независимо от реализации:

```ts
interface Repository<T, Id = string> {
  find(id: Id): T | undefined;
  save(entity: T): void;
}

interface Comparator<T> {
  (a: T, b: T): number;
}

const byLength: Comparator<string> = (a, b) => a.length - b.length;
//    ^? const byLength: Comparator<string>
```

`Repository<T, Id = string>` показывает дефолты на интерфейсах (большинство id — строки); `Comparator<T>` — дженерик *сигнатура вызова*, тип функции, параметризованный `T`.

## Структурная совместимость предвещает вариантность

Два дженерик-экземпляра совместимы, когда совместимо их *содержимое* — TypeScript структурен, поэтому `Box<T>` это просто «объект со свойством `value: T`»:

```ts
class Box<T> {
  constructor(public value: T) {}
}

let named: Box<{ name: string }> = new Box({ name: "Rex" });
let dogBox: Box<{ name: string; legs: number }> = new Box({ name: "Rex", legs: 4 });

named = dogBox;  // ок — Box<{name; legs}> присваиваем Box<{name}>
// dogBox = named; // Error: отсутствует 'legs' — цель уже, источник шире
```

Присваиваем ли `Box<Dog>` к `Box<Animal>`, зависит от того, *как* `T` используется внутри класса — позиции только-чтение ведут себя одним образом, позиции записи — другим. Этот вопрос направления присваиваемости — **вариантность**, и это весь следующий урок.

## Во что дженерик-контейнеры обходятся файлам деклараций

Дженерики стираются из *сгенерированного JS* — `class Box<T>` компилируется в обычный `class Box` без следа `T` в рантайме. Но они вполне присутствуют в *декларациях*. Недженериковый контейнер выдаёт плоскую запись в `.d.ts`; глубоко параметризованный — `Result<T, E>`, `Repository<T, Id>`, билдер, протаскивающий три параметра типа через десяток методов — выдаёт каждую сигнатуру метода в полной дженериковой форме, и авторы библиотек регулярно наблюдают, как `.d.ts` публичного API растёт с нескольких килобайт до десятков, стоит типам контейнеров стать богатыми. Этот бандл уезжает каждому потребителю, и `tsc` каждого потребителя его перепроверяет — так что переусложнённые публичные типы это налог, который платят все ниже по потоку, а не только автор.

Есть и аспект задержки в редакторе. Каждый `new Box(...)` и каждый вызов `.map(...)` заново прогоняет вывод для `T` этого экземпляра (и собственного `U` метода); экран сцепленных операций над контейнером — это экран независимых задач вывода, которые языковой сервер решает на каждое нажатие. Один `Result<T, E>` бесплатен; горячий путь из двадцати сложенных вызовов дженерик-контейнера — это там, где hover и автодополнение начинают тормозить, а `Check time` в `--extendedDiagnostics` для этого файла растёт непропорционально числу строк.

<Inset kind="why" lang="ru">
Почему ограничение статики — не просто недостающая фича? В рантайме существует ровно одна функция-конструктор `Box`. Дженерики стираются — они есть только для проверяющего. `T` экземпляра — это фикция, которую проверяющий поддерживает по-экземплярно из вызова конструктора; но статический метод общий для всех экземпляров независимо от их `T`, поэтому нет согласованного значения, которое `T` мог бы принять в статической области. Сделав статический метод дженериком, восстанавливаем согласованность: он получает свежий параметр типа, решаемый в собственной точке вызова, не связанный ни с одним экземпляром.

Компромисс, который сеньор взвешивает с дженерик-контейнером, — переиспользование против веса деклараций и стоимости вывода, которые он навязывает каждому потребителю. `Result<T, E>` или `Stack<T>`, действительно несущий свой тип-нагрузку насквозь, оправдывает эти издержки — он убирает приведения на каждой точке вызова. Но контейнер, параметризованный «на всякий случай» — третья переменная типа, которую ни один метод не связывает, или `Repository<T, Id, Meta>`, где `Meta` трогается в одном месте — платит полную цену `.d.ts` и вывода, не давая никакой безопасности. Дисциплина зеркалит правило для функций из первого урока: параметр типа класса должен связывать позиции по всей поверхности контейнера; в момент, когда один из них декоративен, уберите его и дайте контейнеру быть конкретным — потому что здесь цену несёт каждый, кто его импортирует.
</Inset>

<Quiz
  id="04-generic-classes-quiz-1"
  lessonSlug="04-generic-classes"
  lang="ru"
  question="Почему `static empty(): Box<T> { ... }` внутри `class Box<T>` не компилируется?"
  choices={[
    { label: "Статические члены принадлежат единственному объекту класса, существующему до любого экземпляра, поэтому им не на что сослаться — нет привязанного к экземпляру T", correct: true },
    { label: "Статические методы не могут возвращать объекты", misconception: "Они могут возвращать что угодно; ошибка именно про ссылку на параметр типа класса." },
    { label: "T нужно дать дефолт, чтобы статика могла им пользоваться", misconception: "Дефолт не помогает; у статики просто нет T экземпляра для чтения, с дефолтом или без." },
    { label: "Статические методы не могут быть дженериками", misconception: "Могут — сделать статику собственным дженериком (со своим U) и есть фикс." },
  ]}
/>

<Quiz
  id="04-generic-classes-quiz-2"
  lessonSlug="04-generic-classes"
  lang="ru"
  question="В `function ok<T>(value: T): Result<T, never>` почему слот ошибки типизирован как `never`?"
  choices={[
    { label: "В случае успеха ошибки нет, а `never` присваиваем любому E, поэтому `ok(x)` подходит везде, где ждут `Result<T, SomeError>`", correct: true },
    { label: "`never` означает, что функция бросает исключение", misconception: "never как тип возврата означает незавершение; здесь это аргумент типа для неиспользуемого слота ошибки, а не тип возврата." },
    { label: "Это заставляет вызывающих обрабатывать ошибку", misconception: "Наоборот — never делает ветвь ошибки недостижимой в конструкторе успеха." },
    { label: "Это плейсхолдер, который нужно заменить перед использованием", misconception: "never — это намеренный тип; нижний тип, присваиваемый везде, замена не нужна." },
  ]}
/>

<DragOrder
  id="04-generic-classes-drag-1"
  pieceSlug="04-generic-classes"
  lang="ru"
  prompt="Расставьте, как проверяющий типизирует `new Box(42).map(n => n.toFixed(2))` для `class Box<T> { constructor(public value: T) {} map<U>(fn: (v: T) => U): Box<U> }`:"
  items={[
    "Вывести T экземпляра = number из аргумента конструктора 42",
    "Типизировать экземпляр как Box<number>",
    "Войти в map, чей параметр fn это (v: number) => U",
    "Вывести собственный U метода map = string из возврата колбэка n.toFixed(2)",
    "Сообщить результат как Box<string>",
  ]}
/>

<MetaphorComplete
  id="04-generic-classes-metaphor-1"
  pieceSlug="04-generic-classes"
  lang="ru"
  setup="Заполните пропуск: параметр типа класса привязан к _______, поэтому статическая сторона — общая для всех них — на него сослаться не может."
  accepted={["экземпляру", "экземпляр", "экземпляра", "уровню экземпляра"]}
  canonical="экземпляру"
  explanation="Параметр типа класса привязывается по-экземплярно, при конструировании. Статические члены живут на единственном объекте класса, существующем до и поверх каждого экземпляра, поэтому нет единого привязанного к экземпляру T, который они могли бы прочитать. Фикс — дать статике собственный параметр типа, решаемый в точке статического вызова, а не из какого-либо экземпляра."
/>

</Explanation>

<KeyTakeaway>Параметр типа класса привязывается при создании экземпляра, поэтому статические члены — предшествующие каждому экземпляру — на него сослаться не могут; вместо этого сделайте статику собственным дженериком. Типизированные контейнеры вроде `Stack<T>` и `Result<T, E>` показывают дженерики классов и интерфейсов в деле, а структурная совместимость двух дженерик-экземпляров зависит от того, как `T` используется внутри — вопрос вариантности следующего урока.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="04-generic-classes-retrieval"
  lang="ru"
  questions={[
    { q: "Почему статический член не может сослаться на параметр типа класса и каков стандартный фикс?", a: "Параметр типа класса вроде `T` в `class Box<T>` привязан по-ЭКЗЕМПЛЯРНО — проверяющий решает его из каждого вызова конструктора, поэтому один `Box` это `Box<number>`, а другой `Box<string>`. Статический же член живёт на единственном объекте класса, существующем один раз, до и поверх всех экземпляров, поэтому нет единого привязанного к экземпляру `T`, на который он мог бы согласованно сослаться — отсюда `Static members cannot reference class type parameters`. (Дженерики стираются в рантайме; по-экземплярный `T` — чисто компиляционная фикция, а статика ни с одним экземпляром не связана.) Фикс — сделать статический метод ЕГО СОБСТВЕННЫМ дженериком: `static of<U>(value: U): Box<U>` вводит свежий `U`, решаемый в точке статического вызова, не связанный с `T` какого-либо экземпляра. Поэтому фабричные функции и хелперы `of`/`from` пишут как самостоятельные дженерики." },
    { q: "Объясните, как `Result<T, E>` плюс конструкторы `ok`/`err` дают обработку ошибок без исключений, включая роль `never`.", a: "`Result<T, E>` — это размеченное объединение: `{ ok: true; value: T } | { ok: false; error: E }`. Конструкторы — дженерики, чтобы выводить нагрузку: `ok<T>(value: T): Result<T, never>` и `err<E>(error: E): Result<never, E>`. НЕИСПОЛЬЗУЕМАЯ сторона типизирована `never`, потому что `never` — нижний тип, присваиваемый любому типу — поэтому `ok(8080): Result<number, never>` всё ещё подходит туда, где ждут `Result<number, string>`, и аналогично для `err`. В точке использования проверяем дискриминант `r.ok`: в ветви `true` объединение сужается до `{ ok: true; value: T }`, открывая `value` (и только value); в ветви `false` — до нагрузки ошибки. Компилятор тем самым заставляет обработать оба исхода и позволяет читать данные ровно одной ветви — без брошенных исключений, без `any`, с полным выводом от начала до конца." },
  ]}
/>

<Recap lang="ru">
Дженерик-классы и интерфейсы параметризуют экземпляры типом, который даёт конструктор (или фабрика), а методы могут добавлять свои независимые параметры типа. Статическая сторона не может сослаться на параметр типа класса — статика существует до того, как любой экземпляр его привяжет, — поэтому фабрики пишут как самостоятельные дженерики. Типизированные контейнеры вроде `Stack<T>` и `Result<T, E>` — повседневная выгода, а дженерик-интерфейсы с дефолтами моделируют репозитории и компараторы. Может ли `Box<Dog>` подменить `Box<Animal>`, зависит от того, как `T` используется внутри класса; этот вопрос направления присваиваемости — вариантность, тема и реальная опасность следующего, заключительного урока этого юнита. Теперь, когда увидишь `Static members cannot reference class type parameters`, — ответ мгновенный: дай статике собственный параметр типа, разрешаемый в её же точке вызова.
</Recap>
