---
concepts:
- variance
- covariance
- contravariance
- bivariance
- strictFunctionTypes
- variance-annotations
deepensInto:
- typescript/08-real-world/05-common-pitfalls
estMin: 16
lang: ru
lessonType: topic
level: senior
order: 5
prereqs:
- typescript/03-generics/04-generic-classes
slug: 05-variance-intuition
sources:
- https://www.typescriptlang.org/docs/handbook/release-notes/typescript-4-7.html#optional-variance-annotations-for-type-parameters
- https://www.typescriptlang.org/docs/handbook/release-notes/typescript-2-6.html#strict-function-types
spiral:
- assignability
- soundness
status: ready
summary: Вариантность — это направление, в котором подтипизация течёт сквозь
  конструктор типа. Параметры функций контравариантны, массивы несоундно
  ковариантны, а параметры методов остаются бивариантными даже под
  strictFunctionTypes — ловушка, пропускающая слишком узкий колбэк.
title: Интуиция вариантности
track: typescript
unit: 03-generics
---

import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import MetaphorComplete from "~/components/pedagogy/MetaphorComplete.astro";
import StackDiagram from "~/components/diagram/StackDiagram.astro";
import Infographic from "~/components/diagram/Infographic.astro";
import bivarianceGotcha from "./infographics/bivariance-gotcha.svg?raw";

<Hook>
Реестр принимает `(handler: (e: Event) => void)`. Вы передаёте `(e: MouseEvent) => void`, потому что обработчик срабатывает лишь на кликах — и это проходит проверку. Потом приходит не-мышиный `Event`, обработчик читает `e.button` и получает `undefined` в рантайме без предупреждения компилятора. Дыра — это вариантность, а причина, по которой она проскользнула, — дефолт TypeScript, о котором большинство инженеров и не подозревают, что включили.
</Hook>

<Crux>Вариантность — как подтипизация течёт сквозь конструктор типа; TS делает параметры методов бивариантными, и узкий колбэк проскальзывает.</Crux>

<Explanation>

## Три отношения, один вопрос

Пусть `Dog extends Animal` (каждая Dog — это Animal). Вариантность спрашивает: для конструктора типа `F<_>` как `F<Dog>` относится к `F<Animal>`?

- **Ковариантно** — `F<Dog>` присваиваем `F<Animal>` (подтипизация течёт в *ту же* сторону). Производители только-чтение.
- **Контравариантно** — `F<Animal>` присваиваем `F<Dog>` (подтипизация течёт в *обратную* сторону). Потребители входа.
- **Инвариантно** — ни в одну сторону; только точные совпадения.
- **Бивариантно** — принимаются обе стороны (несоундно; TS делает это намеренно в одном месте).

Вместе эти четыре случая покрывают любую позицию, которую может занимать переменная типа: производит ли конструктор типа, потребляет или делает и то и другое — это и определяет, какое направление безопасно. Следующие три раздела покажут каждый случай на реальном TypeScript, включая тот, где TS сознательно выбирает несоундный вариант.

<StackDiagram
  label="Направление подтипизации сквозь конструктор типа"
  hue="var(--accent)"
  caption="Начните с Dog extends Animal. Производитель сохраняет направление (ковариантно); потребитель его разворачивает (контравариантно); изменяемый контейнер требует обоих сразу, поэтому для соундности должен быть инвариантным."
  layers={[
    { label: "Dog extends Animal", note: "базовое отношение" },
    { label: "Array<Dog> → Array<Animal>", note: "ковариантно (чтение)" },
    { label: "(a: Animal) => void → (d: Dog) => void", note: "контравариантно (запись)" },
    { label: "изменяемый Box<Dog> vs Box<Animal>", note: "должен быть инвариантным" },
  ]}
/>

## Почему параметры функций контравариантны

Функция, *принимающая* `Animal`, может подменить функцию, принимающую `Dog`, — потому что всё, что передаёт ей `Dog`, передаёт корректный `Animal`. Обратное небезопасно:

```ts
type Animal = { name: string };
type Dog = { name: string; bark(): void };

const feedAnimal = (a: Animal) => a.name;
let feedDog: (d: Dog) => string;

feedDog = feedAnimal; // ок — Dog ЕСТЬ Animal, feedAnimal с ним справится
// небезопасное направление:
let feedAnimal2: (a: Animal) => string;
const handleDog = (d: Dog) => d.bark();
// feedAnimal2 = handleDog;
// Error (под strictFunctionTypes): handleDog нужен bark(), но его могут
//        вызвать с обычным Animal без bark.
```

Принимать *более широкий* вход безопасно; принимать вход *уже*, чем обещает слот, — это дыра: вызов с более широким значением, которое слот допускает, коснулся бы отсутствующих членов. Поэтому параметры самостоятельных функций **контравариантны**.

## Почему массивы (несоундно) ковариантны

TypeScript считает `Array<Dog>` присваиваемым `Array<Animal>`, хотя массивы изменяемы — намеренная, известная несоундность ради эргономики:

```ts
const dogs: Dog[] = [{ name: "Rex", bark: () => {} }];
const animals: Animal[] = dogs; // разрешено — ковариантные массивы
animals.push({ name: "Whiskers" }); // кладём не-Dog в Dog[]!
dogs[1].bark();
// Рантайм: bark is not a function — система типов нас не остановила.
```

Соундная система сделала бы позицию изменяемого элемента инвариантной. TS выбирает ковариантность, потому что преобладает чтение из массивов, а полная инвариантность была бы мучительна. Знать о существовании этой дыры — сеньорский вывод.

## Ловушка бивариантных методов

Теперь баг из хука. `strictFunctionTypes` (TS 2.6) делает **параметры самостоятельных типов-функций** контравариантными — но по замыслу это **не** распространяется на параметры **методов**, которые остаются **бивариантными**. Одинаково выглядящие объявления ведут себя по-разному:

```ts
// tsconfig: { "strictFunctionTypes": true }
type Handler = (e: Event) => void;

// Синтаксис МЕТОДА — параметр БИВАРИАНТЕН (ловушка):
interface BusMethod {
  on(handler: (e: Event) => void): void;
}
// Синтаксис СВОЙСТВА — параметр КОНТРАВАРИАНТЕН (проверяется):
interface BusProp {
  on: (handler: (e: Event) => void) => void;
}

declare const m: BusMethod;
declare const p: BusProp;

const narrow = (e: MouseEvent) => e.button;

m.on(narrow); // ✅ НЕТ ОШИБКИ — параметр метода бивариантен (несоундно!)
p.on(narrow);
// ❌ Error: '(e: MouseEvent) => number' не присваиваем '(e: Event) => void'
//    Типы параметров 'e' несовместимы:
//    в 'Event' отсутствуют свойства 'MouseEvent' (button, ...).
```

Единственное отличие — сокращение метода (`on(...)`) против свойства, хранящего функцию (`on: (...) => ...`). Форма метода пропускает слишком узкий обработчик `MouseEvent`, поэтому при диспетчеризации обычного `Event` поле `e.button` оказывается `undefined`. Поэтому методы `Array.prototype` и многие DOM/event API принимают колбэки, технически слишком узкие, — бивариантность вшита в форму объявления метода.

<Infographic
  svg={bivarianceGotcha}
  label="Сравнение бок о бок одного и того же слота on(handler: (e: Event) => void), записанного двумя способами. Форма метод-сокращения делает параметр бивариантным даже под strictFunctionTypes, поэтому узкий обработчик (e: MouseEvent) принимается без ошибки, а e.button в рантайме равно undefined. Форма свойства-функции оставляет параметр контравариантным, поэтому тот же узкий обработчик отклоняется на этапе компиляции."
  caption="Отличается только форма объявления, но вариантность переворачивается: метод-сокращение молча принимает слишком узкий колбэк, форма свойства-функции его ловит. Берите форму свойства, когда параметр обязан реально проверяться."
  hue="var(--d-infra)"
/>

## Аннотации вариантности: in / out (TS 4.7)

TS 4.7 позволяет *заявить* намеренную вариантность параметра типа через `in` (контравариантно) и `out` (ковариантно). Проверяющий затем сверяет, что ваше использование соответствует — а ещё аннотация документирует намерение и может ускорить проверку:

```ts
interface Producer<out T> {  // T встречается только в позициях вывода
  get(): T;
}
interface Consumer<in T> {   // T встречается только в позициях ввода
  set(value: T): void;
}
interface Box<in out T> {    // T и читается, и пишется → инвариантно
  get(): T;
  set(value: T): void;
}

// Если объявить `out`, но затем написать T во входной позиции, получите:
interface Bad<out T> {
  set(value: T): void;
// Error: тип 'T' объявлен ковариантным ('out'), но встречается
//        в контравариантной позиции.  (примерно)
}
```

Аннотации не *меняют* правила присваиваемости, которые проверяющий уже выводит; они позволяют утвердить и навязать намеренную вариантность, ловя ошибку, например, добавления сеттера к типу, который вы обещали как чистого производителя.

## Числа и версии за этими дырами

Каждое из этих поведений привязано к конкретному релизу, и даты важны, когда читаешь `tsconfig` или гоняешься за вопросом «почему это вообще когда-то проходило проверку». Бивариантные параметры методов и несоундная ковариантность массивов предшествуют strict-режиму целиком. `strictFunctionTypes` появился в **TS 2.6** (окт 2017) и ужесточил *отдельные* параметры функциональных типов до контравариантных — но намеренно оставил параметры метод-сокращений и конструкторов бивариантными, и это ровно та дыра, которой пользуется зачин. Аннотации вариантности `in`/`out` появились в **TS 4.7** (май 2022). Так что баг с обработчиком, проскользнувший через слот-метод, — не дефект компилятора для багрепорта, а девятилетний задокументированный компромисс, и `"strictFunctionTypes": false` (или отсутствие `strict: true`) молча расширяет дыру на *все* параметры функций.

У аннотаций есть и измеренный выигрыш в производительности — поэтому крупные кодовые базы внедряют их сверх документационной ценности. Вывод вариантности для глубоко рекурсивного дженерик-типа — работа, которую проверяющий повторяет; указание `out T` / `in T` позволяет ему *пропустить* этот вывод и использовать вашу декларацию напрямую. Команда TS сообщала, что аннотации вариантности и связанные изменения кэширования заметно сократили время структурного сравнения на тяжёлых дженерик-типах — порядка десятков процентов времени проверки на худших рекурсивных типах — а это разница между отзывчивым редактором и тормозящим на большой схеме или глубоко дженериковом типе ORM. Аннотация, таким образом, — три вещи сразу: документация, страховка корректности против неверно объявленного производителя/потребителя и подсказка проверяющему, способная отыграть время проверки.

<Inset kind="why" lang="ru">
Почему TS вообще выбрал несоундную ковариантность массивов и бивариантные методы? Оба предшествуют `strictFunctionTypes`. Бивариантность методов конкретно сохраняет эргономику `Array<T>`, `Promise<T>` и event-подобных API: `arr.forEach((x: Dog) => ...)` на более широком массиве или `addEventListener("click", (e: MouseEvent) => ...)` иначе постоянно бы ошибались. Цена — реальные дыры. Сеньорский ход: (1) предпочитать форму объявления свойства-функции, когда хотите, чтобы параметр действительно проверялся, и (2) знать, что колбэк, принятый методом, может быть уже объявленной сигнатуры, поэтому никогда не читайте у параметра колбэка поля, которые *объявленный* (более широкий) тип не гарантирует.

Компромисс, который сделал TypeScript — и который вы наследуете, — это соундность, обменянная на эргономику, и сеньор принимает его осознанно, а не борется с ним. Требовать полной соундности здесь значило бы инвариантные массивы (постоянные приведения в read-heavy-коде) и контравариантные параметры методов (каждый `addEventListener("click", (e: MouseEvent) => …)` и `arr.forEach((x: Dog) => …)` падал бы), поэтому язык выбрал дыры. Принимая их, вы отдаёте compile-time-гарантию ровно в двух местах; покупаете — API, которые с вами не воюют. Дисциплина не в том, чтобы устранить несоундность, а в том, чтобы *огородить* её: используйте форму свойства-функции там, где вариантность колбэка реально важна, никогда не доверяйте узкому типу параметра колбэка сверх того, что гарантирует объявленный тип, и тянитесь за `in`/`out` на собственных дженериках, чтобы намеренная вариантность навязывалась, а не оставлялась выводу.
</Inset>

<Quiz
  id="05-variance-intuition-quiz-1"
  lessonSlug="05-variance-intuition"
  lang="ru"
  question="Пусть `Dog extends Animal`. Какое присваивание соундно для самостоятельных типов-функций под strictFunctionTypes?"
  choices={[
    { label: "`(a: Animal) => void` можно присвоить туда, где ждут `(d: Dog) => void` — параметры контравариантны, поэтому принимать более широкий вход безопасно", correct: true },
    { label: "`(d: Dog) => void` можно присвоить туда, где ждут `(a: Animal) => void`", misconception: "Это НЕБЕЗОПАСНОЕ направление; функцию с более узким входом могли бы вызвать с обычным Animal, который она не обработает, и strictFunctionTypes её отвергает." },
    { label: "Оба направления разрешены, потому что функции бивариантны", misconception: "Бивариантность относится только к параметрам в форме метода; параметры самостоятельных типов-функций контравариантны под strictFunctionTypes." },
    { label: "Ни одно направление — типы-функции всегда инвариантны", misconception: "Параметры функций контравариантны, а не инвариантны; направление с более широким входом соундно и разрешено." },
  ]}
/>

<Quiz
  id="05-variance-intuition-quiz-2"
  lessonSlug="05-variance-intuition"
  lang="ru"
  question="Почему `m.on((e: MouseEvent) => ...)` проходит проверку, а `p.on((e: MouseEvent) => ...)` ошибается, хотя оба `on` ждут `(e: Event) => void`?"
  choices={[
    { label: "`on` как МЕТОД имеет бивариантный параметр даже под strictFunctionTypes, поэтому более узкий обработчик MouseEvent (несоундно) принимается; `on` как СВОЙСТВО-функция контравариантно и корректно его отвергает", correct: true },
    { label: "MouseEvent не подтип Event", misconception: "MouseEvent — подтип Event; разница в форме объявления метод-против-свойства, а не в иерархии событий." },
    { label: "strictFunctionTypes отключён для интерфейсов", misconception: "Он действует на интерфейсах; он просто по замыслу исключает параметры в форме метода." },
    { label: "В форме свойства баг старых версий TypeScript", misconception: "Обе ведут себя по замыслу; форма свойства — та, что проверяется корректно." },
  ]}
/>

<DragOrder
  id="05-variance-intuition-drag-1"
  pieceSlug="05-variance-intuition"
  lang="ru"
  prompt="Расставьте рассуждение, показывающее несоундность ковариантности изменяемых массивов (при Dog extends Animal):"
  items={[
    "TS позволяет присвоить Dog[] переменной типа Animal[] (ковариантные массивы)",
    "Через алиас Animal[] вызвать push с обычным Animal, который не Dog",
    "Нижележащий массив — всё ещё Dog[] в другом месте — теперь содержит не-Dog",
    "Код, читающий его как Dog[], вызывает метод только-Dog вроде bark()",
    "В рантайме bark is not a function, и ни одна ошибка компиляции не сработала",
  ]}
/>

<MetaphorComplete
  id="05-variance-intuition-metaphor-1"
  pieceSlug="05-variance-intuition"
  lang="ru"
  setup="Заполните пропуск: параметры функций _______ — функция, принимающая более широкий тип входа, может подменить ту, что принимает более узкий, но не наоборот."
  accepted={["контравариантны", "контравариантность", "контра-вариантны", "контравариантные"]}
  canonical="контравариантны"
  explanation="Потребитель входа разворачивает направление подтипизации: `(a: Animal) => void` присваиваем слоту, ждущему `(d: Dog) => void`, потому что всё, что передаёт Dog, передаёт и корректный Animal. Обратное — дыра: `(d: Dog) => void` в слоте обработчика Animal могли бы вызвать с не-Dog Animal. Под strictFunctionTypes эта контравариантность навязана для параметров самостоятельных типов-функций (но не для параметров в форме метода, которые остаются бивариантными)."
/>

</Explanation>

<KeyTakeaway>Вариантность — это направление, в котором подтипизация течёт сквозь конструктор типа: производители ковариантны, потребители контравариантны, изменяемые контейнеры должны быть инвариантными. TS намеренно делает массивы ковариантными, а параметры методов бивариантными — поэтому слишком узко типизированный колбэк несоундно проскальзывает в слот метода. Предпочитайте форму объявления свойства-функции, когда нужен действительно проверяемый параметр, и используйте аннотации `in`/`out` (TS 4.7), чтобы утвердить и навязать намерение.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="05-variance-intuition-retrieval"
  lang="ru"
  questions={[
    { q: "Определите ковариантность, контравариантность и инвариантность через Dog extends Animal и скажите, что применимо к параметрам функций, массивам и изменяемому боксу.", a: "Пусть Dog extends Animal; вариантность описывает, как F<Dog> относится к F<Animal> для конструктора типа F. КОВАРИАНТНО: F<Dog> присваиваем F<Animal> — подтипизация течёт в ту же сторону; подходит чистым производителям / позициям только-чтение, и TS делает массивы ковариантными (`Dog[]` → `Animal[]`), хотя несоундно, потому что массивы изменяемы. КОНТРАВАРИАНТНО: F<Animal> присваиваем F<Dog> — направление разворачивается; подходит потребителям входа, поэтому ПАРАМЕТРЫ ФУНКЦИЙ контравариантны — `(a: Animal) => void` может занять слот `(d: Dog) => void`, но не наоборот. ИНВАРИАНТНО: ни одно направление, только точные совпадения; изменяемый бокс, который и читает, и пишет `T`, должен быть инвариантным ради соундности, так как он одновременно производитель (ковариантное чтение) и потребитель (контравариантная запись) `T`." },
    { q: "Объясните ловушку метод-против-свойства в strictFunctionTypes и практическое правило, которое из неё следует.", a: "`strictFunctionTypes` (TS 2.6) сделал параметры самостоятельных типов-функций КОНТРАВАРИАНТНЫМИ — но по явному замыслу НЕ охватывает параметры МЕТОДОВ, которые остаются БИВАРИАНТНЫМИ. Поэтому `interface B { on(h: (e: Event) => void): void }` (сокращение метода) принимает более узкий обработчик `(e: MouseEvent) => void` без ошибки, а `interface B { on: (h: (e: Event) => void) => void }` (свойство, хранящее функцию) корректно его отвергает как контравариантное. Единственное отличие — форма объявления. Баг, который это допускает: обработчик, зарегистрированный на `Event`, но типизированный для `MouseEvent`, при диспетчеризации обычного `Event` прочтёт поля вроде `e.button`, которые в рантайме `undefined`. Практические правила: (1) используйте форму свойства-функции, когда хотите, чтобы параметр колбэка действительно проверялся; (2) потребляя колбэк через слот метода, читайте только поля, гарантированные ОБЪЯВЛЕННЫМ (более широким) типом параметра, а не более узким, который вы написали; (3) тянитесь за аннотациями вариантности `in`/`out`, чтобы утвердить намерение на ваших дженерик-типах." },
  ]}
/>

<Recap lang="ru">
Вариантность — это направление, в котором подтипизация течёт сквозь конструктор типа: ковариантно для производителей, контравариантно для потребителей, которыми являются параметры функций, инвариантно для изменяемых контейнеров, требующих обоих. TypeScript сознательно меняет соундность на эргономику в двух местах — массивы ковариантны несмотря на изменяемость, а параметры в форме метода остаются бивариантными даже под `strictFunctionTypes`, поэтому слишком узкий колбэк проскальзывает в слот метода и падает в рантайме. Защита — форма объявления свойства-функции для действительно проверяемых колбэков и аннотации `in`/`out` (TS 4.7) для утверждения намерения. На этом юнит дженериков закрыт; юнит реального мира вновь вернётся к этим самым дырам как к продакшен-ловушкам. Теперь, когда видишь колбэк, зарегистрированный через метод без ошибки компилятора, — первый вопрос: параметр реально проверен или проскользнул через бивариантность метода?
</Recap>
