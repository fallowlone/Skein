---
concepts:
- generic-constraints
- extends
- keyof
- indexed-access
- specificity
deepensInto:
- typescript/04-type-system-deep/01-conditional-types
estMin: 14
lang: ru
lessonType: topic
level: middle
order: 2
prereqs:
- typescript/03-generics/01-generic-functions
slug: 02-constraints
sources:
- https://www.typescriptlang.org/docs/handbook/2/generics.html#generic-constraints
spiral:
- relationships
- assignability
status: ready
summary: "Ограничение `T extends C` — это двусторонний контракт: оно ограничивает, какие аргументы вправе передать вызывающие, и взамен даёт телу доступ к членам C. Классическая ловушка — вернуть тип C вместо T и стереть точность."
title: Ограничения дженериков
track: typescript
unit: 03-generics
---

import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import MetaphorComplete from "~/components/pedagogy/MetaphorComplete.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
Вы пишете `function longest<T>(a: T, b: T) { return a.length > b.length ? a : b }`, и редактор загорается красным: `Property 'length' does not exist on type 'T'`. Все хватаются за `T extends { length: number }`. Но мало кто спрашивает, что эта строка *дала* — она сделала сразу две вещи, и упустить любую из них значит тихо потерять типовую информацию дженерика.
</Hook>

<Crux>`T extends C` — двусторонний контракт: он сужает, какие аргументы вправе передать вызывающие, и взамен даёт телу доступ ко всем членам C.</Crux>

<Explanation>

## Ошибка и контракт

Внутри тела дженерика `T` непрозрачен — проверяющий ничего о нём не знает, поэтому `a.length` отвергается. **Ограничение** сообщает проверяющему нижнюю границу: каждый `T` будет *как минимум* этой формы.

```ts
function longest<T extends { length: number }>(a: T, b: T): T {
  return a.length > b.length ? a : b;
}

const s = longest("alpha", "beta");
//    ^? const s: string
const arr = longest([1, 2], [3, 4, 5]);
//    ^? const arr: number[]
longest(10, 20);
// Error: Argument of type 'number' is not assignable to
//        parameter of type '{ length: number }'.
```

Ограничение выполнило две задачи одновременно:

1. **Ограничило вызывающих.** Принимаются только аргументы, присваиваемые `{ length: number }` — `number` отвергается в точке вызова.
2. **Открыло члены внутри.** Поскольку каждый `T` теперь гарантированно имеет `length: number`, тело вправе читать `a.length`.

Это и есть контракт: вызывающие отдают свободу; тело получает возможности. Упустите любую сторону — и вы либо чрезмерно ограничите API, либо не сможете использовать значение, которое ограничили.

<FlowDiagram
  label="Ограничение течёт в двух направлениях"
  hue="var(--accent)"
  caption="Ограничение C стоит между вызывающим и телом: оно фильтрует входящие аргументы (должны быть присваиваемы C) и экспортирует члены C в тело."
  perRow={3}
  nodes={[
    { id: "caller", label: "аргумент", sub: "должен быть ⊆ C" },
    { id: "c", label: "T extends C", sub: "контракт" },
    { id: "body", label: "тело", sub: "члены C доступны" },
  ]}
  edges={[
    { from: "caller", to: "c", label: "ограничить" },
    { from: "c", to: "body", label: "открыть" },
  ]}
/>

## Ограничение по ключу: keyof

Канонический ограниченный дженерик — чтение свойства по ключу. Нам нужен ключ, который *один из собственных ключей объекта*, а не любая строка:

```ts
function getProperty<T, K extends keyof T>(obj: T, key: K): T[K] {
  return obj[key];
}

const user = { id: 1, name: "Ada", admin: true };

const name = getProperty(user, "name");
//    ^? const name: string
const admin = getProperty(user, "admin");
//    ^? const admin: boolean
getProperty(user, "email");
// Error: Argument of type '"email"' is not assignable to
//        parameter of type '"id" | "name" | "admin"'.
```

`K extends keyof T` ограничивает `K` объединением ключей `T`. Проверяющий выводит `T` из `obj`, затем `keyof T` становится `"id" | "name" | "admin"`, и `key` должен быть одним из этих литералов. Возвращаемый тип `T[K]` — это индексный доступ, точный тип *именно того* свойства. Передайте несуществующий ключ — и ошибка назовёт допустимый набор.

## Ограничение по форме или объединению

Ограничением может быть любой тип, не только объектный литерал:

```ts
// По форме с id — полезно для хелперов «сущностей».
function withId<T extends { id: string }>(items: T[]): Map<string, T> {
  const m = new Map<string, T>();
  for (const it of items) m.set(it.id, it);
  return m;
}

// По объединению — ограничить известным набором тегов.
type Tag = "info" | "warn" | "error";
function level<T extends Tag>(t: T): T {
  return t;
}
const l = level("warn");
//    ^? const l: "warn"   — узкий литерал сохранён
```

## Ловушка точности: вернуть C вместо T

Вот баг, который ловят на ревью. Вы ограничиваете `T extends C`, а потом небрежно типизируете возврат как `C`. Функция всё равно компилируется, но *стирает* точный тип вызывающего вниз до ограничения.

```ts
// БАГ: возвращает ограничение, а не переменную типа.
function identityBad<T extends { id: string }>(x: T): { id: string } {
  return x;
}
const r1 = identityBad({ id: "u1", role: "admin" as const });
//    ^? const r1: { id: string }   — потерян `role`! сужено до ограничения

// ФИКС: вернуть T, сохранив всё, что передал вызывающий.
function identityGood<T extends { id: string }>(x: T): T {
  return x;
}
const r2 = identityGood({ id: "u1", role: "admin" as const });
//    ^? const r2: { id: string; role: "admin" }   — полный тип сохранён
```

Весь смысл вводить `T` был в том, чтобы *пронести* точный тип вызывающего насквозь. Возврат ограничения это выбрасывает, и лучше бы вы взяли обычный параметр типа `{ id: string }`. Дженерик стал декоративным.

## Цена ужесточения ограничения

Ограничение — это ещё и решение про API с измеримым радиусом поражения. Ужесточите `T extends { id: string }` до `T extends { id: string; tenantId: string }` — и каждый вызывающий, кто раньше передавал голый `{ id }`, теперь падает с ошибкой: на сервисе с парой сотен точек вызова это разница между зелёной сборкой и PR, который трогает сорок файлов. Ошибка громкая и в правильном месте (на точке вызова), и в этом весь смысл ограничения, но рефлекс сеньора — спросить, место ли этому требованию в сигнатуре вообще, или более узкий внутренний хелпер локализовал бы churn. Ослабьте ограничение наоборот (уберите обязательный член) — и происходит обратное, молча: вызывающие, опиравшиеся на этот член, продолжают компилироваться, а телу, которое его читает, теперь нужен запасной путь, который вы не написали.

Есть и цена для проверяющего. Ограничение вроде `K extends keyof T` делает `T[K]` *индексным доступом (indexed access)*, который проверяющий разрешает на каждой точке вызова; над широким объектом — типом конфига, скажем, со 120 ключами — `keyof T` это union из 120 членов, и каждый вызов `getProperty` инстанцируется и сопоставляется с ним. Один такой хелпер — ничто; слой из них поверх больших mapped-типов — это там, где вы наблюдаете скачок счётчика `Instantiations` в `--extendedDiagnostics`, и `tsc` из шустрого превращается в заметную паузу на каждое нажатие клавиши в редакторе.

<Inset kind="why" lang="ru">
Ограничение против дефолта — частая путаница: `<T extends string>` и `<T = string>` выглядят похоже, но делают противоположное. `extends` — верхняя граница, *требование*, которому аргумент должен удовлетворять, проверяемое в точке вызова. `=` — это *запасной вариант*, тип, используемый, только когда вывод ничего не нашёл, и никогда не навязываемый. Можно иметь оба: `<T extends string = "id">` значит «должен быть подтипом string, а если вы не задали и не вывели, используй литерал `"id"`». Дефолты — следующий урок; здесь просто не читайте `extends` как «по умолчанию равно».

Компромисс в *жёсткости* ограничения: более слабое ограничение принимает больше вызывающих, но открывает внутри меньше членов, толкая тело к приведениям или лишним рантайм-проверкам; более жёсткое открывает больше, но отвергает вызывающих и расходится по всем точкам вызова при следующем ужесточении. Калибровка сеньора — ограничивать ровно теми членами, которых тело реально касается: не слабее (иначе не напишете тело), не жёстче (иначе вы связали вызывающих требованиями, которые им не нужны). Когда ограничение начинает называть члены, которые тело никогда не читает, это сигнал, что оно сползло в чрезмерную ограничительность.
</Inset>

<Quiz
  id="02-constraints-quiz-1"
  lessonSlug="02-constraints"
  lang="ru"
  question="В `getProperty<T, K extends keyof T>(obj: T, key: K): T[K]` что делает ограничение `K extends keyof T`?"
  choices={[
    { label: "Ограничивает `key` литеральным объединением ключей T в точке вызова И делает возвращаемый тип T[K] точным типом выбранного свойства", correct: true },
    { label: "Даёт K значение по умолчанию keyof T, когда ключ не передан", misconception: "extends — это требование (верхняя граница), а не дефолт; оно навязывается, а не запасной вариант." },
    { label: "Расширяет возвращаемый тип до объединения всех типов свойств T", misconception: "Возврат — это T[K] для конкретного выведенного K, а не объединение по всем ключам." },
    { label: "Позволяет key быть любой строкой, проверяемой только в рантайме", misconception: "Весь смысл — в отклонении на этапе компиляции ключей не из keyof T; ничего не откладывается в рантайм." },
  ]}
/>

<Quiz
  id="02-constraints-quiz-2"
  lessonSlug="02-constraints"
  lang="ru"
  question="Почему `function f<T extends { id: string }>(x: T): { id: string }` — ошибка?"
  choices={[
    { label: "Возвращаемый тип — это ограничение, а не T, поэтому лишние свойства вызывающего стираются — дженерик перестаёт нести точный тип и не лучше обычного параметра `{ id: string }`", correct: true },
    { label: "Из ограниченного дженерика нельзя возвращать объект", misconception: "Возвращать можно что угодно; проблема в возврате C вместо T, теряющем информацию." },
    { label: "`{ id: string }` должно быть `{ id: number }`", misconception: "Тип свойства ни при чём; дефект — возврат ограничения вместо переменной типа." },
    { label: "В ограничении нужно `=` вместо `extends`", misconception: "Дефолт — неверный инструмент; аргумент обязан иметь id, что и делает extends." },
  ]}
/>

<DragOrder
  id="02-constraints-drag-1"
  pieceSlug="02-constraints"
  lang="ru"
  prompt="Расставьте, как проверяющий типизирует `getProperty(user, 'name')`, где `user: { id: number; name: string }`:"
  items={[
    "Вывести T из `user` как { id: number; name: string }",
    "Вычислить ограничение `keyof T` как объединение 'id' | 'name'",
    "Проверить, что аргумент 'name' присваиваем этому объединению, затем вывести K = 'name'",
    "Разрешить индексный доступ T[K] = T['name']",
    "Сообщить тип результата как string",
  ]}
/>

<MetaphorComplete
  id="02-constraints-metaphor-1"
  pieceSlug="02-constraints"
  lang="ru"
  setup="Заполните пропуск: ограничение дженерика — это _______ контракт: оно стоит вызывающим части свободы и возвращает телу гарантированные члены."
  accepted={["двусторонний", "двусторонний контракт", "двунаправленный", "двухсторонний"]}
  canonical="двусторонний (двунаправленный)"
  explanation="`T extends C` одновременно сужает множество допустимых аргументов (вызывающие обязаны передать что-то присваиваемое C) и расширяет то, что вправе делать тело (каждый член C теперь гарантированно присутствует у T). Если думать лишь об одной стороне, вы либо чрезмерно ограничите API, либо напишете тело, которое не может коснуться значения, которое ограничило."
/>

</Explanation>

<KeyTakeaway>Ограничение `T extends C` — это контракт в обе стороны: оно фильтрует, какие аргументы вправе передать вызывающие, и открывает члены C внутри тела. Классическая ловушка — вернуть тип ограничения вместо `T`, что молча стирает точность вызывающего и делает дженерик декоративным.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="02-constraints-retrieval"
  lang="ru"
  questions={[
    { q: "Объясните два эффекта записи `T extends { length: number }` и какую ошибку вы получаете без ограничения.", a: "Без ограничения `T` непрозрачен внутри тела, поэтому `a.length` падает с «Property length does not exist on type T». Добавление `T extends { length: number }` делает сразу две вещи. Первое — ограничивает вызывающих: принимаются только аргументы, присваиваемые `{ length: number }` — `string` и массивы проходят, `number` отвергается в точке вызова. Второе — открывает члены внутри тела: поскольку каждый `T` теперь гарантированно имеет числовой `length`, проверяющий допускает `a.length`. Ограничение — это контракт: вызывающие отказываются от свободы передавать произвольные типы, а тело взамен получает возможность использовать гарантированные ограничением члены. Важно, что возврат всё ещё может быть `T`, поэтому точный тип вызывающего (например, `string`) сохраняется, а не сужается до ограничения." },
    { q: "В чём ловушка точности с ограничениями и как избежать её в хелперах вида `getProperty`?", a: "Ловушка — вернуть тип ОГРАНИЧЕНИЯ вместо ПЕРЕМЕННОЙ типа. Если написать `f<T extends { id: string }>(x: T): { id: string }`, тело всё ещё проходит проверку, но возврат сужает каждый результат до `{ id: string }`, отбрасывая любые лишние свойства вызывающего — и тогда `<T>` бессмыслен, а обычный параметр `{ id: string }` сделал бы то же. Фикс — возвращать `T` (`f<T extends { id: string }>(x: T): T`), чтобы полный тип вызывающего тёк насквозь. Та же дисциплина для доступа по ключу: `getProperty<T, K extends keyof T>(obj: T, key: K): T[K]` возвращает точный индексный тип `T[K]`, а не сужённое объединение — поэтому чтение `name` даёт `string`, а `admin` — `boolean`, каждое точное." },
  ]}
/>

<Recap lang="ru">
Ограничение `T extends C` — это контракт в основе полезных дженериков: оно ограничивает, какие аргументы вправе передать вызывающие (всё, что присваиваемо C), и взамен даёт телу использовать гарантированные C члены. Ограничение `K extends keyof T` — канонический паттерн безопасного доступа к свойству, возвращающий точный индексный тип `T[K]`. Повторяющийся баг на ревью — возврат ограничения вместо `T`, который стирает точный тип вызывающего и делает дженерик декоративным; и не путайте `extends` (требование) с `=` (запасной вариант). Дальше мы добавим дефолты и изучим, как именно они взаимодействуют с выводом, включая утилиту `NoInfer` для блокировки вывода в позиции.
</Recap>
