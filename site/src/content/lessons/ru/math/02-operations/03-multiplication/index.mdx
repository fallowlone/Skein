---
concepts:
- multiplication
- product
estMin: 16
lang: ru
order: 3
prereqs:
- 03-place-value
- 01-addition
slug: 03-multiplication
sources:
- https://openstax.org/books/prealgebra-2e/pages/1-4-multiply-whole-numbers
- https://www.khanacademy.org/math/arithmetic/mult-div
status: ready
summary: Сложение одного и того же числа снова и снова, записанное коротко — равные
  группы, произведение, почему порядок не важен и как умножать большие числа по разрядам.
title: Умножение
track: math
unit: 02-operations
---



import Hook from "~/components/lesson/Hook.astro";
import PracticeSet from "~/components/math/PracticeSet.astro";
import Goal from "~/components/lesson/Goal.astro";
import Step from "~/components/lesson/Step.astro";
import WorkedExample from "~/components/lesson/WorkedExample.astro";
import Check from "~/components/lesson/Check.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import NumberLine from "~/components/math/NumberLine.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
У тебя 4 пакета, и в каждом по 3 яблока. Можно сложить 3 + 3 + 3 + 3 и найти итог. Но
есть короткий способ сказать «четыре тройки»: четыре *раза* по три. Этот короткий
способ — умножение.
</Hook>

<Goal lang="ru">
После этого урока ты можешь сказать, что значит умножение, назвать его результат,
объяснить, почему порядок двух чисел не важен, знать, что делает умножение на 0 и на
1, и умножать большее число, работая по одному разряду за раз.
</Goal>

<Step n={1}>
**Умножение — это сложение равных групп много раз.** Когда у тебя несколько групп
одинакового размера, умножение быстро находит итог. Мы записываем его **знаком
умножения**, `×`. `4 × 3` значит «4 группы по 3», то есть 3 + 3 + 3 + 3 = 12. Первое
число говорит, *сколько групп*, второе — *какого размера каждая группа*.
</Step>

<Step n={2}>
**Числа, которые умножают, — это множители; результат — произведение.** В `4 × 3 = 12`
числа `4` и `3` — **множители**, а `12` — **произведение**. Умножение — это просто
быстрый способ дойти до произведения, до которого можно было бы дойти и сложением
одного и того же числа снова и снова.
</Step>

<Step n={3}>
**До произведения можно дойти, считая равными прыжками.** `4 × 3` — это четыре прыжка
по 3 вдоль числовой прямой: с 0 на 3, на 6, на 9, на 12. Каждый прыжок — одна группа.
Приземлись после четырёх прыжков — и ты на произведении, 12.
</Step>

<NumberLine min={0} max={12} highlight={[3, 6, 9, 12]} />

<Step n={4}>
**Порядок множителей не важен.** `4 × 3` и `3 × 4` дают одно и то же — `12`. Представь
прямоугольник из точек: 4 ряда по 3 точки содержат столько же точек, сколько 3 ряда по
4 — ты просто повернул прямоугольник на бок. Поэтому, когда один множитель мал, ставь
его вторым и считай столько прыжков большего: прыжков меньше, произведение то же.
</Step>

<Step n={5}>
**Два множителя проходят через всякое умножение: 0 и 1.** Любое число, умноженное на
`1`, — это оно само: одна группа из семи — это просто семь. Любое число, умноженное на
`0`, — это `0`: ноль групп чего угодно не содержат ничего. А умножение на `10` сдвигает
каждую цифру на один разряд влево: `7 × 10 = 70`, потому что каждая единица становится
десятком. Большие числа: раздели один множитель по разрядам и умножь каждую часть.
`13 × 4` — это `(10 × 4) + (3 × 4)`.
</Step>

<WorkedExample lang="ru">
**Умножь 13 × 4.**

Раздели `13` по разрядам: это 1 десяток и 3 единицы, значит `13 = 10 + 3`.

Умножь каждую часть на 4. Часть-десятки: `10 × 4 = 40`. Часть-единицы: `3 × 4 = 12`.

Сложи два неполных произведения: `40 + 12 = 52`.

Значит `13 × 4 = 52`. Проверь мысль: 13, сложенное четыре раза, — это 13 + 13 + 13 + 13;
две 13 дают 26, ещё две дают 52. То же произведение.
</WorkedExample>

<Inset kind="why" lang="ru">
Почему множитель можно делить по разрядам и умножать части отдельно? Потому что
умножение распределяется по сложению: четыре группы по (10 + 3) — это то же самое, что
четыре группы по 10 вместе с четырьмя группами по 3. Ты не меняешь итог — только
выбираешь более удобный порядок счёта. На этой мысли построен всякий письменный способ
умножения. Когда встретишь большое умножение, спроси себя: «что если разбить это
число на десятки и единицы и разобраться с каждой частью?» — этот вопрос проведёт
тебя через длинное умножение и дальше.
</Inset>

<Inset kind="mistake" lang="ru">
Частая ошибка — умножить только цифру единиц и забыть десятки: записать `13 × 4 = 12`,
потому что `3 × 4 = 12`. Умножить нужно каждую часть числа. Разделив по разрядам,
проверь, что ты использовал *все* части, прежде чем складывать неполные произведения.
</Inset>

<PracticeSet
  id="multiplication-practice"
  lessonSlug="03-multiplication"
  lang="ru"
  problems={[
    { prompt: "5 × 3 значит пять групп по три. Напиши произведение.", answer: 15, hint: "Посчитай пять прыжков по 3: 3, 6, 9, 12, 15." },
    { prompt: "Умножь: 4 × 0. Напиши произведение.", answer: 0, hint: "Ноль групп не содержат вообще ничего." },
    { prompt: "Умножь: 6 × 1. Напиши произведение.", answer: 6, hint: "Одна группа из шести — это просто шесть." },
    { prompt: "7 × 8 даёт 56. Не пересчитывая, что даёт 8 × 7? Напиши произведение.", answer: 56, hint: "Порядок множителей не меняет произведение." },
    { prompt: "Умножь: 13 × 5, разделив 13 на 10 + 3. Напиши произведение.", answer: 65, hint: "10 × 5 = 50 и 3 × 5 = 15; сложи неполные произведения." },
  ]}
/>

<Check lang="ru">
  <Quiz
    id="multiplication-check"
    pieceSlug="03-multiplication"
    lang="ru"
    question="Чтобы умножить 13 × 4, ты делишь 13 на 10 + 3. Почему так можно?"
    choices={[
      { label: "Четыре группы по (10 + 3) — это четыре группы по 10 плюс четыре группы по 3, итог не меняется", correct: true },
      { label: "Потому что 10 и 3 оба меньше 13, значит с ними проще", misconception: "«Проще» — это выгода, а не причина правильности. Это верно потому, что итог остаётся тем же." },
      { label: "Любое число можно разделить как угодно и умножить только одну часть", misconception: "Умножить нужно каждую часть и сложить результаты — деление не позволяет выбросить часть." },
    ]}
  />
</Check>

<Recap lang="ru">
Умножение — это сложение равных групп много раз: `4 × 3` — четыре группы по три. Числа,
которые умножают, — множители; результат — произведение. Порядок множителей никогда не
меняет произведение, поэтому считай прыжки большего. Умножение на 1 оставляет число
прежним, на 0 даёт 0, на 10 сдвигает каждую цифру на разряд влево. Большие числа: раздели
множитель по разрядам, умножь каждую часть и сложи неполные произведения. Теперь,
когда встретишь `13 × 4` — или любое умножение на двузначное число — первым делом
раздели по разрядам и разбирайся с каждой частью по очереди.
</Recap>
