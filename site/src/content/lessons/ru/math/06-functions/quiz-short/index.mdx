---
concepts:
- function-rule
- function-notation
- domain-range
- linear-function
- graph-point
estMin: 14
lang: ru
level: middle
order: 8
prereqs: []
slug: quiz-short
sources:
- https://openstax.org/books/algebra-and-trigonometry-2e/pages/3-1-functions-and-function-notation
- https://www.khanacademy.org/math/algebra/x2f8bb11595b61c86:functions
status: ready
summary: "Вопросы на свободное припоминание по всему юниту о функциях. Сначала ответь своими словами, затем открой эталонный ответ и сравни."
title: 'Функции: тест на припоминание'
track: math
unit: 06-functions
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";

<Hook>
Припоминание закрепляет идею намного лучше, чем перечитывание. На каждый вопрос скажи или запиши полный ответ по памяти, прежде чем открыть эталон — именно усилие вытащить ответ и делает его прочным.
</Hook>

<Goal lang="ru">
Восстанови по памяти стержень юнита: что такое функция, что означает её запись, что такое домен и область значений, что делает функцию линейной и почему линейная функция рисуется прямой.
</Goal>

<RetrievalDrawer
  client:visible
  id="06-functions-short-retrieval"
  lang="ru"
  questions={[
    { q: "Что такое функция и какому одному правилу она обязана подчиняться?", a: "Функция — это правило, которое превращает вход в выход: ты подаёшь число (или вещь), правило действует на него, и получается результат. Одно правило, которому она обязана подчиняться, — у каждого входа ровно один выход: подай один и тот же вход дважды, и оба раза обязан выйти один и тот же выход. Кнопка автомата, которая иногда роняет один снек, иногда другой, — не функция. Правило не обязано быть формулой; таблица или картинка тоже функция, лишь бы каждый вход отображался ровно в один выход." },
    { q: "Что означает запись f(3) и как найти её значение?", a: "Запись функции называет правило буквой и показывает вход в скобках, поэтому f(3) означает «подать вход 3 в правило f». Чтобы найти значение, подставь 3 везде, где в правиле стоит вход, и посчитай. Если f(n) = n + 5, то f(3) = 3 + 5 = 8. То, что внутри скобок, — всегда вход; то, что получается, — выход. Один и тот же вход всегда возвращает один и тот же выход — именно это и делает f функцией." },
    { q: "Что такое домен и область значений функции и чем они различаются?", a: "Домен — это множество входов, которые разрешено подавать в функцию, то есть то, что может войти. Область значений — это множество выходов, которые она способна выдать, то есть то, что выходит. Это разные стороны машины: домен на стороне входа, область значений на стороне выхода. У автомата, который принимает кнопки A1, A2, A3 и роняет три снека, домен — это три кнопки, а область значений — три снека. Назвать их — значит назвать, что разрешено войти и что может выйти." },
    { q: "Что делает функцию линейной и какие у неё две части?", a: "Функция линейна, когда её выход меняется на одну и ту же фиксированную величину при каждом шаге входа на 1 — постоянный шаг, который не ускоряется и не замедляется. У неё две части: скорость (наклон) — число, на которое умножается вход, равное шагу на единицу; и начальное значение — выход при входе 0. Правило такси 2n + 3 имеет наклон 2 (две монеты за километр) и начальное значение 3 (плата до начала движения). Любая линейная функция вписывается в форму «скорость умножить на вход плюс начальное значение»." },
    { q: "Почему линейная функция всегда рисуется прямой?", a: "На графике горизонтальная ось несёт вход, а вертикальная — выход, и каждая пара вход-выход становится точкой, записанной как (вход, выход) — сначала вправо, потом вверх. Линейная функция поднимается на один и тот же фиксированный шаг при каждом шаге вправо на 1. Раз подъём одинаков при любом входе, точки не могут согнуться: кривая потребовала бы, чтобы шаг менялся, а постоянный шаг способен прочертить только одну прямую. Поэтому увидеть прямую — это и есть способ узнать линейную функцию с первого взгляда." },
    { q: "Имея график функции, как считать выход для выбранного входа?", a: "Начни на горизонтальной оси (входа) в нужном значении — скажем, 4. Иди прямо вверх от этого места, пока не встретишь линию или кривую функции. Затем посмотри влево на вертикальную ось (выхода): высота, где ты оказался, и есть выход для этого входа. Для y = 2x подъём от входа 4 встречает прямую на высоте 8, значит выход равен 8. Это просто чтение точки в обратную сторону: ты задаёшь положение входа, а график выдаёт подходящий выход." },
  ]}
/>

<Recap lang="ru">
Если ты смог восстановить каждый ответ по памяти, ты держишь весь юнит: функция — это правило «один вход — один выход»; её запись f(3) называет правило и вход, который ты в него подаёшь; домен — это то, что можно подать, а область значений — то, что выходит; линейная функция — это постоянная скорость плюс начальное значение; и раз этот шаг постоянен, она всегда рисуется прямой, которую можно читать в обе стороны.
</Recap>
