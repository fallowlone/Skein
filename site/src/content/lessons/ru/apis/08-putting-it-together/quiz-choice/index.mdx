---
concepts:
- resource-modeling
- status-code-retry-contract
- cursor-pagination
- openapi-breaking-change
- grpc-vs-rest
- rate-limit-headers
estMin: 13
lang: ru
level: senior
order: 7
prereqs: []
slug: quiz-choice
sources:
- https://developer.mozilla.org/en-US/docs/Web/HTTP/Reference/Status/429
- https://www.rfc-editor.org/rfc/rfc8594.html
- https://stripe.com/docs/api/idempotent_requests
status: ready
summary: "Multiple-choice синтез по всему треку APIs — моделирование ресурсов, status-код как retry-контракт, cursor-пагинация, OpenAPI break-diff, gRPC-vs-REST и rate-limit-заголовки как один связанный дизайн."
title: 'APIs: review с выбором ответа'
track: apis
unit: 08-putting-it-together
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Шесть вопросов, которые проходят сквозь весь трек. Каждый отражает решение на review реального публичного API — не определение для зубрёжки, а место, где одно слабое звено держит на себе следующее.
</Hook>

<Goal lang="ru">
Убедись, что можешь проследить каскад от начала до конца: как моделирование ресурсов готовит честные status-коды, как status-коды управляют retry клиента, как пагинация и OpenAPI-контракт держат оба корректными под нагрузкой и где здесь протокол и rate limit.
</Goal>

<Quiz
  id="08-putting-it-together-choice-1"
  lessonSlug="08-putting-it-together"
  lang="ru"
  question="Команда смоделировала POST /chargeCard вместо ресурса payments. Через месяцы — двойные списания. Какое звено в каскаде корневое?"
  choices={[
    { label: "Глагольный эндпоинт не имеет адресуемого ресурса, поэтому отвечает 200 на всё, включая дубликаты — а 200 значит «готово, не ретраить», что превращает потерянный ответ в двойное списание. Моделирование существительного (POST /payments → 201/Location, 409 на retry) и сделало бы status-коды честными.", correct: true },
    { label: "Настоящий баг — только отсутствие rate limit; моделирование косметическое.", misconception: "Rate limit — последний guardrail, а не корень. Даже с идеальным лимитом глагольный эндпоинт, отдающий 200 на дубликаты, всё равно дважды спишет на первом же retry. Каскад начинается с моделирования." },
    { label: "POST вообще нельзя использовать для платежей; нужен PUT ради идемпотентности.", misconception: "PUT идемпотентен лишь при полной замене ресурса с известным id; создание платежа — это POST. Идемпотентность создания даёт Idempotency-Key, а не смена метода." },
    { label: "Клиент просто не должен ретраить, и тогда форма глагола неважна.", misconception: "Мобильные сети падают постоянно; запрет retry меняет двойные записи на потерянные. Фикс — адресуемый ресурс плюс idempotency-ключ, делающий retry безопасным." },
  ]}
/>

<Quiz
  id="08-putting-it-together-choice-2"
  lessonSlug="08-putting-it-together"
  lang="ru"
  question="Что лучше всего объясняет, почему HTTP status-код — это контракт, а не украшение?"
  choices={[
    { label: "Клиенты действуют по коду автоматически во время инцидента: 2xx — стоп, 4xx — не ретраить без изменений, 429/503 — отступить и читать Retry-After, 5xx — ретраить только если операция идемпотентна. Код управляет машинным поведением, поэтому неверный код ломает логику клиента.", correct: true },
    { label: "Status-коды в основном для дашбордов логирования; клиенты читают тело ответа.", misconception: "Приоритет обратный. Универсальные клиенты и SDK ветвятся по status-коду и заголовкам, не парся тело; код — машиночитаемая часть контракта." },
    { label: "Любой 2xx годится, лишь бы в теле было поле error для ошибок.", misconception: "Отдавать 200 с ошибкой в теле — классический антипаттерн: retry-логика, кэши и мониторинг читают 200 как успех и не видят сбой. Код обязан отражать исход." },
    { label: "5xx всегда безопасно ретраить, так что сервер может отдавать его свободно.", misconception: "5xx безопасно ретраить только при идемпотентной операции. Retry неидемпотентного POST на 5xx, пришедший после записи, и есть способ списать дважды." },
  ]}
/>

<Quiz
  id="08-putting-it-together-choice-3"
  lessonSlug="08-putting-it-together"
  lang="ru"
  question="Эндпоинт отдаёт список на 2M строк, просматриваемый вживую, через OFFSET/LIMIT, и глубокие страницы тормозят и иногда пропускают строки. Лучший структурный фикс?"
  choices={[
    { label: "Перейти на cursor (keyset) пагинацию: WHERE id > :last ORDER BY id LIMIT n. Стоимость остаётся O(размер страницы) на любой глубине, а страницы стабильны при конкурентных вставках, ценой потери произвольного перехода на страницу.", correct: true },
    { label: "Добавить индекс и оставить OFFSET — индекс ускорит глубокие страницы.", misconception: "Индекс помогает ORDER BY, но OFFSET 100000 всё равно заставляет БД просканировать и отбросить 100k строк на каждой глубокой странице. Стоимость растёт с глубиной независимо от индекса." },
    { label: "Кэшировать каждую страницу на пять минут, чтобы глубокие перестали бить в БД.", misconception: "Кэш живого пагинированного списка отдаёт устаревшие, дрейфующие страницы и ничего не даёт длинному хвосту уникальных глубоких страниц. Keyset убирает медленный путь структурно." },
    { label: "Ограничить API 100 страницами, чтобы никто не дошёл до медленного места.", misconception: "Это прячет симптом и ломает легитимный глубокий просмотр. Cursor-пагинация устраняет медленную часть, а не огораживает её." },
  ]}
/>

<Quiz
  id="08-putting-it-together-choice-4"
  lessonSlug="08-putting-it-together"
  lang="ru"
  question="Деплой переименовал поле конверта ошибки с `message` на `detail` и ужесточил enum статуса. CI был зелёный, а клиенты сломались в проде. Чего не хватало и какие изменения были проблемой?"
  choices={[
    { label: "Spec-first OpenAPI break-diff в CI. Переименование поля и ужесточение enum — breaking changes: старый клиент зависит от точной старой формы, поэтому им место за version bump. Зелёные тесты были написаны под старую форму и не поймали дрейф; diff завалил бы сборку.", correct: true },
    { label: "С изменениями всё в порядке; клиенты просто плохо написаны.", misconception: "Обратная совместимость значит, что немодифицированный старый клиент продолжает работать. Переименование поля и ужесточение enum ломают это по определению — контракт сломал продюсер, а не консьюмер." },
    { label: "Оба изменения были аддитивными и безопасными; сбой — флейки-деплой.", misconception: "Переименование (удаление старого имени) и ужесточение валидации — breaking, а не аддитивные. Аддитивное — это добавление опционального поля или нового значения enum, которое клиент может игнорировать." },
    { label: "Фикс — перестать писать тесты и полагаться на ручной QA.", misconception: "Тесты под старую форму остались зелёными именно потому, что их не перегенерировали из спеки. Фикс — машинный diff новой спеки против старой, а не удаление тестов." },
  ]}
/>

<Quiz
  id="08-putting-it-together-choice-5"
  lessonSlug="08-putting-it-together"
  lang="ru"
  question="Коллега хочет сделать публичный API на gRPC, потому что бенчмарки показывают ~77% ниже latency, ~10x меньше сообщения и выше throughput. Как рассуждать сеньору?"
  choices={[
    { label: "Протокол — ось, независимая от моделирования, версионирования и контракта. Эти числа отвечают на «internal service-to-service при высоком объёме», а не на «публичный контракт». Публичный API живёт на discoverability, доступности из curl/браузера и эргономике SDK — это сильные стороны REST + OpenAPI. REST на edge, gRPC между внутренними сервисами, если объём оправдывает.", correct: true },
    { label: "Перейти на gRPC везде, потому что raw latency — важнейшая метрика.", misconception: "Raw latency — не то ограничение, под которое оптимизируется публичный API. На публичном edge gRPC убивает discoverability, ломает доступ через curl/браузер и повышает порог входа для внешних разработчиков." },
    { label: "Оставить REST везде, включая внутренний хоп на 40k req/s, ради единого стека.", misconception: "На 40k req/s с крошечными payload и стримингом JSON-over-HTTP жжёт лишние CPU, память и трафик и не имеет first-class стриминга. Внутренний хоп — единственное место, где числа gRPC реально важны." },
    { label: "Перейти на GraphQL, ведь он включает в себя и REST, и gRPC.", misconception: "GraphQL решает over/under-fetching под форму клиента; он не закрывает высокопроизводительный внутренний стриминговый хоп и добавляет проблемы query-complexity и кэширования на публичном edge. Это другой инструмент, а не замена." },
  ]}
/>

<Quiz
  id="08-putting-it-together-choice-6"
  lessonSlug="08-putting-it-together"
  lang="ru"
  question="Retry-шторм на пике превращается в outage: десятки тысяч дублирующих записей в секунду бьют в БД. Какой контроль сдержал бы это и как он должен вести себя?"
  choices={[
    { label: "Документированный rate limit, отдающий 429 с заголовком Retry-After (и в идеале X-RateLimit-Remaining/Reset), чтобы клиенты сверх лимита отступали, а не долбили. В связке с Idempotency-Key на записях шторм и задросселирован, и дедуплицирован.", correct: true },
    { label: "Отдавать 503 без заголовков, чтобы клиенты знали, что сервер упал.", misconception: "Голый 503 без Retry-After оставляет клиентам гадать; многие ретраят сразу и усиливают шторм. Дроссель обязан сказать клиенту, когда вернуться, через Retry-After." },
    { label: "Молча дропать лишние запросы, чтобы клиент не знал.", misconception: "Тихий дроп выглядит как сетевой сбой, что провоцирует ещё больше retry и полностью прячет лимит от клиента. 429 с Retry-After — явный, машиночитаемый сигнал отступить." },
    { label: "Отдавать 429, но без Retry-After, ведь клиенты знают дефолтный backoff.", misconception: "Без Retry-After клиенты откатываются на свой (часто агрессивный или фиксированный) backoff, и документированная квота превращается в гадание. Заголовок и делает backoff кооперативным." },
  ]}
/>

<Recap lang="ru">
Трек — это одна цепь, а не чеклист. Моделирование ресурсов (существительные, не-CRUD-действия как суб-ресурсы) решает, могут ли status-коды быть честными; status-коды — машиночитаемый retry-контракт (201/409/429, Idempotency-Key на небезопасных записях, Retry-After на дросселях); cursor-пагинация держит большие/живые чтения плоскими и стабильными; spec-first OpenAPI с break-diff в CI останавливает тихий дрейф; версионирование развивается аддитивно и поднимает версию только на реальных break с политикой Sunset; протокол — ортогональная ось (REST на edge, gRPC внутри); rate limit с 429 + Retry-After не даёт retry-шторму стать outage. Ревьюй каскад, потому что каждое звено держит следующее.
</Recap>
