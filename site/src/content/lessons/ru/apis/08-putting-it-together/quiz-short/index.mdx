---
concepts:
- resource-modeling
- status-code-retry-contract
- cursor-pagination
- openapi-versioning
- grpc-vs-rest
- rate-limiting
estMin: 14
lang: ru
level: senior
order: 8
prereqs: []
slug: quiz-short
sources:
- https://developer.mozilla.org/en-US/docs/Web/HTTP/Reference/Status/429
- https://www.rfc-editor.org/rfc/rfc8594.html
- https://stripe.com/docs/api/idempotent_requests
status: ready
summary: "Free-recall промпты по всему треку APIs. Сначала ответь своими словами, затем открой модельный ответ и сверься с сеньорским end-to-end review."
title: 'APIs: free-recall review'
track: apis
unit: 08-putting-it-together
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";

<Hook>
Извлечение из памяти бьёт перечитывание. Для каждого промпта реконструируй полный ответ по памяти до того, как откроешь модельный — усилие собрать каскад заново и закрепляет его.
</Hook>

<Goal lang="ru">
Реконструируй хребет трека, не подглядывая: как моделирование делает status-коды честными, почему status-код — это retry-контракт, когда cursor-пагинация обязательна, что защищает OpenAPI break-diff, где сидит протокол и как rate limit ограничивает радиус поражения.
</Goal>

<RetrievalDrawer
  client:load
  id="08-putting-it-together-short-retrieval"
  lang="ru"
  questions={[
    { q: "Пройди каскад: как одна ошибка моделирования ресурса становится production-outage, звено за звеном?", a: "Всё начинается с моделирования глагола вместо существительного — POST /chargeCard вместо ресурса payments. Без адресуемого ресурса у эндпоинта нет Location, который вернуть, и нет 409 на дубликат, поэтому он отдаёт 200 на всё. Но 200 значит «готово, не ретраить», так что когда мобильная сеть теряет ответ, клиент ретраит небезопасную запись и карта списывается дважды. Нет spec-first OpenAPI-контракта и нет CI break-diff, поэтому когда форма ответа позже дрейфует, тесты (написанные под старую форму) остаются зелёными, и дрейф уезжает обычным деплоем. Наконец, нет rate limit, поэтому на пике retry собираются в шторм из десятков тысяч дублирующих записей в секунду, который добивает БД до outage. Каждое звено стало фатальным лишь потому, что предыдущее убрало guardrail, который бы его остановил — поэтому сеньор ревьюит их как цепь, а не чеклист." },
    { q: "Почему HTTP status-код — это контракт, а не украшение, и к чему каждый класс обязывает клиента?", a: "Универсальные клиенты, SDK, прокси, кэши и мониторинг ветвятся по status-коду и заголовкам автоматически, не парся тело — поэтому код является машиночитаемой частью контракта, и неверный код ломает логику клиента. 2xx — готово, не ретраить. 4xx — сам запрос неверен; ретраить его без изменений бессмысленно (400, 404, 422). 429 и 503 — ретраить можно, но отступив, читая Retry-After. 5xx — возможно ретраить, но только если операция идемпотентна. Опасный случай — неидемпотентный POST, упавший после записи, но до ответа; честный ответ здесь — Idempotency-Key, потому что без него единственный корректный код для «я не знаю, сработало ли» — это отсутствие кода, по которому клиент мог бы безопасно действовать." },
    { q: "Когда cursor (keyset) пагинация обязательна вместо OFFSET/LIMIT и в чём компромисс?", a: "Используй cursor-пагинацию для больших или живых наборов данных. OFFSET 100000 LIMIT 20 заставляет БД сканировать и отбрасывать 100k строк на каждой глубокой странице — стоимость растёт с глубиной — и она тихо пропускает или дублирует строки при вставке элементов посреди пагинации. Keyset-пагинация — WHERE (sort_key, id) < (:last_key, :last_id) ORDER BY sort_key, id LIMIT n, неся последнюю строку как непрозрачный курсор — остаётся O(размер страницы) на любой глубине и стабильна при конкурентных вставках, потому что опирается на значения строк, а не на числовой offset. Компромисс — теряется произвольный переход на страницу 500; OFFSET годится лишь для маленьких, стабильных, админских списков, где глубокие страницы и живые вставки не проблема." },
    { q: "Что такое breaking change против аддитивного и что реально даёт spec-first OpenAPI + CI break-diff?", a: "Обратная совместимость имеет точный смысл: немодифицированный старый клиент продолжает работать против нового API. Аддитивно и безопасно: добавить опциональное поле, новый эндпоинт или новое значение enum, которое клиент может игнорировать. Breaking: удалить или переименовать поле, ужесточить валидацию, сменить дефолт или тип — потому что какой-то клиент зависит от точного старого поведения. Spec-first OpenAPI делает схему источником истины, из которого генерируются клиенты и серверные стабы, а CI break-diff сравнивает новую спеку со старой и заваливает сборку на breaking change. Этот diff существует потому, что люди стабильно ошибаются, в какую корзину попадает изменение; без него дрейф уезжает зелёным деплоем, ведь тесты были написаны под старую форму и их никто не перегенерировал." },
    { q: "Коллега хочет сделать публичный API на gRPC, потому что бенчмарки куда быстрее. Как рассуждать?", a: "Сначала раздели оси: протокол (REST vs gRPC) независим от моделирования, версионирования и контракта — это выбор транспорта, а не часть истории эволюции API. Числа gRPC реальны — примерно на 77% ниже latency на маленьких payload, около 10x меньше protobuf-сообщения, выше throughput, нативный стриминг — но каждое из них отвечает на «internal service-to-service при высоком объёме», а не на «публичный контракт». Публичный API живёт на discoverability, возможности внешнего разработчика curl-нуть его или ударить из браузера, генерируемых SDK и низком пороге входа — это сильные стороны REST + OpenAPI. Так что сеньорский сплит — REST + OpenAPI на публичном edge и gRPC между своими сервисами тогда и только тогда, когда объём оправдывает. Выбрать gRPC для публичного edge из-за быстрого бенчмарка значит оптимизировать единственную метрику, под которую публичный API не ограничен, жертвуя теми, под которые ограничен." },
    { q: "Как rate limiting и идемпотентность вместе сдерживают retry-шторм и что обязан содержать дроссель-ответ?", a: "Они атакуют шторм с двух сторон. Rate limit ограничивает, сколько запросов клиент может сделать: запросы сверх квоты получают 429 с заголовком Retry-After (и в идеале X-RateLimit-Limit/Remaining/Reset), чтобы кооперативный клиент отступил на указанное окно, а не долбил и не гадал свой backoff. Идемпотентность делает прошедшие запросы безопасными: Idempotency-Key позволяет ретраенной небезопасной записи вернуть исходный результат вместо повторного выполнения, так что даже поток retry не даёт дублирующих side effects. Без Retry-After клиенты откатываются на агрессивный или фиксированный backoff и усиливают шторм; без идемпотентности задросселированные, но всё же приходящие retry всё равно пишут дважды. Вместе они превращают шторм класса outage в ограниченную, дедуплицированную, кооперативную нагрузку." },
  ]}
/>

<Recap lang="ru">
Если можешь реконструировать каждый ответ по памяти, ты держишь хребет трека: моделирование существительных и есть то, что делает status-коды честными; status-коды — машиночитаемый retry-контракт, подкреплённый Idempotency-Key и Retry-After; cursor-пагинация держит большие/живые чтения плоскими и стабильными; spec-first OpenAPI с CI break-diff останавливает тихий дрейф; протокол — ортогональная ось (REST на edge, gRPC внутри); а rate limit плюс идемпотентность вместе ограничивают радиус поражения retry-шторма. Теперь, когда встретишь в ревью маршрут, status-код или запрос пагинации, ты инстинктивно спросишь: какое звено цепи это затрагивает — и на месте ли предохранители вокруг него?
</Recap>
