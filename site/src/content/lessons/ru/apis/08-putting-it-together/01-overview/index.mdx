---
concepts:
- resource-modeling
- http-semantics
- api-versioning
- openapi-contract
- grpc-vs-rest
- rate-limiting
deepensInto: []
estMin: 17
lang: ru
lessonType: topic
level: junior
order: 1
prereqs: []
slug: 01-overview
sources:
- https://developer.mozilla.org/en-US/docs/Web/HTTP/Reference/Status/429
- https://www.rfc-editor.org/rfc/rfc8594.html
- https://markaicode.com/grpc-vs-rest-benchmarks-2025/
- https://spring.io/blog/2025/09/16/api-versioning-in-spring/
spiral: []
status: ready
summary: 'Каждое решение в API связано: плохое моделирование ресурсов вынуждает злоупотреблять
  статус-кодами, из-за чего клиенты повторяют запросы неправильно, что скрывает отсутствие контракта,
  что ломается молча, что без рейт-лимита превращается в аварию. Это сквозное ревью
  сеньора.'
title: Проектирование и ревью публичного API от начала до конца
track: apis
unit: 08-putting-it-together
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";
import Infographic from "~/components/diagram/Infographic.astro";
import restVsGrpc from "./infographics/rest-vs-grpc.svg?raw";

<Hook>
Авария начинается с существительного. Кто-то смоделировал `POST /chargeCard` вместо ресурса `payments`, и на вопрос «а это списание уже произошло?» API не мог ответить. Эндпоинт вернул `200` на дубликат. Мобильные клиенты, видя дёрганую сеть, ретраили — а `200` не безопасен для ретрая, поэтому карты списались дважды. OpenAPI-контракта не было, и QA не заметило дрифт формы ответа. Рейт-лимита не было, и шторм ретраев в 18:00 ударил по базе 40 тысячами дублирующих записей в секунду. Шесть плохих решений, одна цепная реакция. Каждое из них можно было отревьюить месяцем раньше.
</Hook>

<Crux>Дизайн API — одна цепь: плохая модель ломает статус-коды, клиенты повторяют запросы неверно, молчащий контракт скрывает это до нагрузки.</Crux>

<Explanation>

## Цепная реакция: как одно плохое решение становится аварией

К концу урока ты сможешь проследить эту шестизвенную цепь и поймать каждый слом до того, как он уедет в прод.

Весь трек APIs учил одному с разных сторон: API — это контракт, и контракты ломаются на стыках между решениями, а не внутри какого-то одного. Сеньор, который ревьюит публичный API, не проверяет пункты по отдельности — он трассирует цепочку, потому что каждое слабое звено держит на себе следующее.

Начни с **моделирования ресурсов**. Моделируй существительные, а не глаголы: `POST /payments`, а не `POST /chargeCard`. Глагол — это HTTP-метод; URL — это сущность. Действие, которое не CRUD-глагол (publish, refund, retry), становится суб-ресурсом — `POST /payments/{id}/refunds` — а не query-параметром `?action=refund` и не утёкшей колонкой базы. В тот момент, когда URL зеркалит схему базы (`/user_accounts_v2`), ты приварил клиентов к своему внутреннему хранилищу, и каждая миграция стала ломающим изменением.

Это решение о моделировании определяет, могут ли твои **статус-коды** вообще быть честными. Настоящий ресурс `payments` даёт вернуть `201 Created` с `Location` на первой записи и `200`/`409` на повторе с тем же ключом идемпотентности. У RPC `chargeCard` не на что указать, поэтому он возвращает `200` на всё — а `200` говорит клиенту «готово, не ретрай», что ложь, когда сеть съела ответ.

## Статус-коды — это контракт ретрая, а не украшение

Клиенты не читают твою документацию в 18:00 во время инцидента — они читают статус-код и заголовки и действуют по ним автоматически. Это делает статус-код машиночитаемым контрактом:

- `2xx` → готово, не ретрай.
- `4xx` → запрос неверен; ретрай без изменений бессмыслен (`400`, `404`, `422`).
- `429` / `503` → можно ретраить, но с бэкоффом — читай `Retry-After`.
- `5xx` → может, и можно ретраить, но только если операция идемпотентна.

Опасный случай — неидемпотентный `POST`, который упал после записи, но до ответа. Фикс — **`Idempotency-Key`**: клиент шлёт UUID, сервер его запоминает, и ретрай с тем же ключом возвращает исходный результат вместо повторного списания. Поэтому платёжные API в стиле Stripe требуют этот заголовок — он превращает небезопасный ретрай в безопасный. Без него единственный корректный статус-код для «я не знаю, сработало ли» — это отсутствие статус-кода, потому что клиент угадает неверно.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Слой</th>
        <th class="border border-border px-3 py-2 text-left">Сделай правильно</th>
        <th class="border border-border px-3 py-2 text-left">Ошибись → следующий сбой</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">Моделирование ресурсов</td>
        <td class="border border-border px-3 py-2">Существительные; не-CRUD действия как суб-ресурсы; без утечки схемы базы</td>
        <td class="border border-border px-3 py-2">RPC-глаголы без адреса → статус-коды не могут быть честными</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Статус-коды</td>
        <td class="border border-border px-3 py-2"><code>201</code>/<code>409</code>/<code>429</code> с <code>Idempotency-Key</code> + <code>Retry-After</code></td>
        <td class="border border-border px-3 py-2"><code>200</code> на всё → клиенты повторяют небезопасные записи</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Пагинация</td>
        <td class="border border-border px-3 py-2">Курсор (keyset) для больших/живых данных</td>
        <td class="border border-border px-3 py-2">Глубокий <code>OFFSET</code> → полные сканы, дрифт при вставке</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Контракт (OpenAPI)</td>
        <td class="border border-border px-3 py-2">Spec-first; дифф ломающих изменений в CI</td>
        <td class="border border-border px-3 py-2">Нет спеки → дрифт ответа уезжает молча</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Версионирование</td>
        <td class="border border-border px-3 py-2">По умолчанию аддитивно; версия только на слом; заголовок Sunset</td>
        <td class="border border-border px-3 py-2">Мутировать <code>/v1</code> на месте → клиенты ломаются за ночь</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Рейт-лимит</td>
        <td class="border border-border px-3 py-2"><code>429</code> + <code>Retry-After</code>; задокументированная квота</td>
        <td class="border border-border px-3 py-2">Нет лимита → один шторм ретраев становится аварией</td>
      </tr>
    </tbody>
  </table>
</div>

## Пагинация и контракт, который ловит дрифт

`OFFSET 100000 LIMIT 20` заставляет базу посчитать и отбросить 100k строк на каждой глубокой странице — полный скан, который тем медленнее, чем дальше уходишь, и который молча пропускает или дублирует строки, когда элементы вставляются посреди пагинации. Курсорная (keyset) пагинация — `WHERE id > :last_seen ORDER BY id LIMIT 20` — остаётся O(размер страницы) на любой глубине и стабильна при вставках, ценой отсутствия случайного «прыжка на страницу 500». Для больших или живых датасетов отгружай курсоры; offset нормален только для маленьких, стабильных, админских списков.

Ничто из этого не остаётся корректным без **контракта**. Spec-first OpenAPI значит, что схема — источник истины, генерируемые клиенты и серверные стабы оба выводятся из неё, и — то, что реально спасает — CI-джоб диффит новую спеку против старой и валит сборку на ломающем изменении: удалённое поле, ужатый enum, смена типа, переименованный конверт ошибки. Без этого диффа дрифт, который сломал QA в Hook, уезжает обычным деплоем с зелёными тестами, потому что тесты написаны против старой формы и их никто не обновил.

<Inset kind="why" lang="ru">
«Обратно совместимо» имеет здесь точный смысл: старый клиент, работающий без изменений против нового API, всё ещё работает. Добавить опциональное поле, новый эндпоинт или новое значение enum, которое клиент может игнорировать, — аддитивно и безопасно. Удалить поле, переименовать его, ужесточить валидацию или сменить дефолт — ломающее, даже если «кажется мелким», потому что какой-то клиент где-то зависит от точного старого поведения. CI-дифф существует, потому что люди стабильно ошибаются, в какую корзину попадает изменение.
</Inset>

## Версионирование — соединительная ткань; протокол — отдельная ось

Когда встречаешь новое изменение API и думаешь «это ломающее или нет?» — ответ почти никогда не очевиден с первого взгляда. Именно поэтому существуют правила ниже.

Версионирование — это то, что даёт всей системе эволюционировать без цепной реакции. Дефолт сеньора — **аддитивное изменение без бампа версии**: новые опциональные поля и эндпоинты идут прямо в `/v1`. Новую мажорную версию (`/v2` или media-type `Accept: application/vnd.api.v2+json`) ты режешь только на настоящие сломы и держишь старую живой. URL-версионирование (`/v1/...`) выигрывает для публичных API — видимо, дружелюбно к кэшу, тривиально маршрутится — а версионирование через заголовок/media-type держит URL чистыми для внутренних потребителей. В любом случае депрекейшн — это опубликованная политика, а не сюрприз: анонсируй заголовки `Deprecation` и `Sunset` (RFC 8594/9745), дай клиентам 6–12 месяцев и следи за метриками использования, прежде чем реально удалять старую версию.

Выбор протокола — REST vs gRPC — это *ортогональная* ось, а не часть лестницы версий, и именно тут сеньоры чаще всего переусложняют. gRPC реально быстрее: примерно на 77% ниже latency на маленьких payload, ~10x меньше сериализованные сообщения (protobuf vs JSON) и ~50k req/s против ~20k у REST в синтетических бенчмарках, со стримингом как фичей первого класса. Но эти цифры — ответ на «внутренний service-to-service на высоком объёме», а не на «должен ли мой публичный API быть на gRPC». Публичные API живут или умирают на discoverability, доступности из браузера/curl и эргономике для внешних разработчиков — ровно сильные стороны REST + OpenAPI. Ход сеньора — REST на краю, gRPC между своими сервисами, если объём оправдывает, и никогда не давать бенчмарку выбирать твой публичный контракт.

<Infographic
  svg={restVsGrpc}
  label="Сравнение REST + OpenAPI и gRPC бок о бок: REST даёт ~20k req/s, доступен из curl и браузера, с OpenAPI SDK и человекочитаемым JSON; gRPC даёт ~50k req/s, на ~77% ниже latency, ~10x меньше protobuf-сообщения, нативный стриминг, но недоступен из браузера."
  caption="Протокол — ортогональная ось: публичный край оптимизирован под доступность и эргономику (REST), внутренний высокообъёмный хоп — под сырую пропускную способность (gRPC). Выбирай по краю, а не по бенчмарку."
  hue="var(--d-infra)"
/>

<TradeoffMatrix
  id="01-overview-tradeoff-1"
  lessonSlug="01-overview"
  lang="ru"
  prompt="Два твоих сервиса обмениваются ~40k запросов/сек внутри с крошечными payload и стримящим фидом; ты также отдаёшь публичный API сторонним разработчикам. Выбери раздел протоколов."
  options={[
    { name: "gRPC между двумя внутренними сервисами; REST + OpenAPI для публичного API", summary: "Правильный инструмент на каждый край.", correct: true, justification: "Это сопоставляет каждый протокол его задаче. Внутренний высокообъёмный трафик с маленькими payload и стримингом — ровно там, где ~77% ниже latency, ~10x меньше protobuf-сообщения и нативный стриминг gRPC окупаются на 40k req/s. Публичный край нуждается в discoverability, доступности из браузера/curl и лёгком онбординге внешних разработчиков — вся сила REST + OpenAPI. Протокол — ось, независимая от версионирования; можно гонять оба." },
    { name: "gRPC везде, включая публичный API, потому что бенчмарки быстрее", summary: "По бенчмаркам.", justification: "Бенчмарки отвечают на «внутренний service-to-service на объёме», а не на «публичный контракт». gRPC на публичном крае значит, что внешние разработчики не могут curl его, браузеры мучаются без прокси, discoverability падает, а protobuf-тулинг становится барьером к адопшену. Сырой latency — не то ограничение, под которое оптимизируется публичный API." },
    { name: "REST везде, включая внутренний хоп на 40k req/s, чтобы держать один стек", summary: "Простота одного стека.", justification: "Заманчиво ради операционной простоты, но на 40k req/s с крошечными payload и стримингом JSON-over-HTTP/1.1 жжёт CPU, память и пропускную способность (~19%/34%/41% больше в бенчмарках) и не имеет стриминга первого класса. Внутренний хоп — единственное место, где цифры gRPC реально важны." },
    { name: "GraphQL для обоих, раз он поглощает REST и gRPC", summary: "Один язык запросов.", justification: "GraphQL решает over/under-fetching для чтений под форму клиента; он не решает внутренний высокопропускной стримящий хоп (где выигрывает gRPC) и добавляет головную боль со сложностью запросов и кэшированием на публичном крае. Это другой инструмент под другую задачу, а не drop-in для этого раздела." },
  ]}
/>

<Quiz
  id="01-overview-quiz-1"
  lessonSlug="01-overview"
  lang="ru"
  question="POST мобильного клиента на создание платежа упал по таймауту. Сервер мог записать, а мог и нет. Какой дизайн сеньора делает ретрай клиента безопасным?"
  choices={[
    { label: "Требовать Idempotency-Key: клиент шлёт UUID, сервер дедуплицирует, и ретрай возвращает исходный результат вместо повторного списания", correct: true },
    { label: "Всегда возвращать 200, чтобы клиент знал, что можно остановиться", misconception: "200 значит «готово, не ретрай» — но здесь клиент не знает, произошла ли запись. Ложь через 200 ровно и вызывает двойные списания, когда сеть съела настоящий ответ." },
    { label: "Сделать так, чтобы клиент никогда не ретраил POST", misconception: "Мобильные сети падают постоянно; отказ от ретрая меняет двойные записи на потерянные записи. Фикс — сделать ретрай безопасным через ключ идемпотентности, а не запрещать ретраи." },
    { label: "Сказать клиентам ждать 30 секунд и проверять GET-эндпоинт", misconception: "Воркэраунд, который перекладывает гонку на каждого клиента и всё равно не гарантирует дедуп. Idempotency-Key решает это в источнике для всех клиентов." },
  ]}
/>

<Quiz
  id="01-overview-quiz-2"
  lessonSlug="01-overview"
  lang="ru"
  question="Твой API возвращает список, который теперь 2M строк и растёт, и его листают вживую. Эндпоинт использует OFFSET/LIMIT и тормозит на глубоких страницах. Лучший фикс?"
  choices={[
    { label: "Перейти на курсорную (keyset) пагинацию — WHERE id > :last ORDER BY id LIMIT n — чтобы стоимость оставалась плоской на любой глубине и стабильной при вставках", correct: true },
    { label: "Добавить индекс в базу и оставить OFFSET", misconception: "Индекс помогает WHERE/ORDER BY, но OFFSET 100000 всё равно заставляет базу сканировать и отбрасывать 100k строк на каждой глубокой странице. Стоимость растёт с глубиной независимо от индекса." },
    { label: "Ограничить API 100 страницами, чтобы никто не дошёл до медленной части", misconception: "Это прячет симптом и ломает легитимный глубокий просмотр. Курсорная пагинация убирает медленную часть целиком, а не огораживает её." },
    { label: "Кэшировать каждую страницу на 5 минут", misconception: "Кэширование живого пагинированного списка отдаёт устаревшие и дрейфующие страницы и ничего не даёт длинному хвосту уникальных глубоких страниц. Keyset-пагинация — структурный фикс." },
  ]}
/>

<DragOrder
  id="01-overview-drag-1"
  lessonSlug="01-overview"
  lang="ru"
  prompt="Расставь сквозной чек-лист ревью публичного API (каждый шаг де-рискует следующий):"
  items={[
    { id: "a", label: "Ресурсы — существительные; не-CRUD действия — суб-ресурсы; схема базы не утекает в URL" },
    { id: "b", label: "Статус-коды честные: 201/409/429, Idempotency-Key на небезопасных записях, Retry-After на троттлинге" },
    { id: "c", label: "Списки используют курсорную пагинацию для больших/живых данных, а не глубокий OFFSET" },
    { id: "d", label: "Spec-first OpenAPI — источник истины, с диффом ломающих изменений в CI" },
    { id: "e", label: "Изменения по умолчанию аддитивны; настоящие сломы бампят версию с опубликованной политикой Sunset" },
    { id: "f", label: "Рейт-лимиты возвращают 429 + Retry-After с задокументированной квотой, чтобы нагрузка не стала аварией" },
  ]}
  correctOrder={["a", "b", "c", "d", "e", "f"]}
/>

</Explanation>

<FlowDiagram
  label="Каскад ревью: каждое звено снижает риск следующего"
  hue="var(--d-backend)"
  perRow={1}
  nodes={[
    { id: "model", label: "Существительные", sub: "не RPC-глаголы" },
    { id: "status", label: "Честные статус-коды", sub: "Idempotency-Key" },
    { id: "page", label: "Курсорная пагинация", sub: "не глубокий OFFSET" },
    { id: "spec", label: "Spec-first OpenAPI", sub: "CI break-diff" },
    { id: "ver", label: "Аддитивные версии", sub: "Sunset на ломках" },
    { id: "rate", label: "Rate limit", sub: "429 + Retry-After" },
  ]}
  edges={[
    { from: "model", to: "status", label: "позволяет" },
    { from: "status", to: "page", label: "затем" },
    { from: "page", to: "spec", label: "фиксирует" },
    { from: "spec", to: "ver", label: "эволюция" },
    { from: "ver", to: "rate", label: "гейтит" },
  ]}
  caption="Один связанный контракт: плохое моделирование вынуждает нечестные статус-коды, их прячет отсутствие спеки, а отсутствие rate-лимита превращает это в аварию. Ревьюй как цепь."
/>

<KeyTakeaway>API — один контракт. Моделируй существительные, делай статус-коды контрактом ретрая, пагинируй курсором, фиксируй форму в spec-first OpenAPI с CI-диффом, эволюционируй аддитивно, гейти через 429.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="01-overview-retrieval"
  lang="ru"
  questions={[
    { q: "Пройди по тому, как одна ошибка моделирования ресурса перерастает в продакшен-аварию, называя каждое звено цепочки.", a: "Всё начинается с моделирования глагола вместо существительного — POST /chargeCard, а не ресурс payments. Поскольку адресуемого ресурса нет, эндпоинту нечем вернуть Location или 409, поэтому он возвращает 200 на всё, включая дубликаты. Но 200 значит «готово, не ретрай», поэтому когда мобильная сеть теряет ответ, клиент ретраит небезопасную запись, и карта списывается дважды. OpenAPI-контракта и CI-диффа нет, поэтому когда форма ответа позже дрейфует, тесты QA (написанные против старой формы) остаются зелёными, и дрифт уезжает обычным деплоем. Наконец, рейт-лимита нет, поэтому на пике ретраи складываются в шторм — десятки тысяч дублирующих записей в секунду бьют по базе, и всё это сваливается в аварию. Каждое звено стало фатальным только потому, что предыдущее убрало предохранитель, который остановил бы его. Урок в том, что это не независимые пункты чек-листа; они держат друг друга, поэтому сеньор ревьюит их как цепь, а не как список." },
    { q: "Коллега хочет сделать публичный API на gRPC, потому что бенчмарки показывают, что он сильно быстрее. Как рассуждать, верно ли это решение?", a: "Сначала раздели оси: протокол (REST vs gRPC) независим от версионирования, моделирования и контракта — это выбор транспорта, а не часть истории эволюции API. Потом спроси, под что вообще оптимизируется публичный API. Цифры gRPC реальны — примерно на 77% ниже latency на маленьких payload, около 10x меньше protobuf-сообщения, ~50k против ~20k req/s, нативный стриминг — но каждая из них ответ на «внутренний service-to-service на высоком объёме», а не на «публичный контракт». Публичный API живёт на discoverability, на возможности внешнего разработчика curl его или дёрнуть из браузера, на генерируемых SDK и низком трении онбординга — ровно сила REST + OpenAPI. Так что правильный ход обычно — REST на публичном крае и gRPC между своими сервисами, если и только если объём оправдывает. Выбрать gRPC для публичного края, потому что бенчмарк быстрее, — это оптимизировать ту единственную метрику, под которую публичный API не ограничен, жертвуя теми, под которые ограничен." },
  ]}
/>

<Recap lang="ru">
Трек APIs всегда был одним уроком, видимым со многих сторон: API — это связный контракт, и он ломается на стыках между решениями. Моделируй существительные и превращай не-CRUD действия в суб-ресурсы, потому что именно это даёт статус-кодам быть честными. Делай статус-коды машиночитаемым контрактом ретрая — 201/409/429, Idempotency-Key на небезопасных записях, Retry-After на троттлинге — потому что во время инцидента клиенты действуют по кодам, а не по докам. Пагинируй большие или живые списки курсором, а не глубоким OFFSET, чтобы стоимость оставалась плоской, а страницы стабильными. Фиксируй всю форму в spec-first OpenAPI с CI-диффом ломающих изменений, чтобы дрифт не мог уехать молча. Эволюционируй аддитивно и версионируй только на настоящие сломы, с опубликованной политикой Deprecation/Sunset и 6–12 месяцами разбега. Гейти всё задокументированными рейт-лимитами, возвращающими 429 + Retry-After, чтобы шторм ретраев не превратился в аварию. Держи протокол на своей оси: REST + OpenAPI на публичном крае, gRPC между внутренними сервисами, когда объём реально оправдывает. Теперь, когда встретишь маршрут `POST /doSomething` или `200` на создание ресурса, ты сразу увидишь, какое звено в этой цепи трещит — и за каким фиксом тянуться первым.
</Recap>
