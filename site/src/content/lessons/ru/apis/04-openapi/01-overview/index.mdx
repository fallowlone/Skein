---
concepts:
- api-contract
- spec-first
- contract-drift
- breaking-change-detection
- schema-reuse
- generated-clients
deepensInto: []
estMin: 16
lang: ru
lessonType: topic
level: junior
order: 1
prereqs: []
slug: 01-overview
sources:
- https://spec.openapis.org/oas/v3.1.0.html
- https://www.oasdiff.com/docs/breaking-changes
- https://learn.openapis.org/best-practices.html
- https://dev.to/copyleftdev/title-when-swagger-lies-fixing-api-drift-before-it-breaks-you-ijo
spiral: []
status: ready
summary: OpenAPI окупается, только когда спеку проверяют — генерируют клиентов, валидируют
  запросы и роняют CI на ломающем диффе. Спека, которую никто не проверяет, — это
  документация, которая врёт.
title: 'OpenAPI: сделать контракт источником истины, а не доками'
track: apis
unit: 04-openapi
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";
import Infographic from "~/components/diagram/Infographic.astro";
import breakingVsSafe from "./infographics/breaking-vs-safe.svg?raw";

<Hook>
Бэкендер добавляет поле `tenantId` в payload создания заказа и помечает его `required` — изменение в одну строку, выкатил в пятницу. К понедельнику три мобильных клиента, партнёрская интеграция и внутренняя админка возвращают 422 на каждый заказ. Никто из них не слал `tenantId`; никто не знал, что оно есть. Контракт говорил, что поле опционально вчера и обязательно сегодня, и ничто в пайплайне этого не заметило. «Доки» были страницей в Confluence, отредактированной полгода назад. Настоящий контракт жил у кого-то в голове и поменялся, никому не сказав.
</Hook>

<Crux>OpenAPI помогает, только когда спеку проверяют в CI — генерируют, валидируют, диффят. Непроверяемая спека — это документация, которая врёт.</Crux>

<Explanation>

За следующие пятнадцать минут ты увидишь, почему спека без enforcement — это замедленная мина, и какие три шага в пайплайне её обезвреживают до того, как сломается хоть один клиент.

## Спека — это контракт, иначе фикция

OpenAPI — это машиночитаемое описание твоего HTTP API: каждый путь, форма каждого запроса и ответа, каждый статус-код, каждая схема авторизации, записанные в YAML или JSON. Столько у большинства команд есть. Вопрос, который решает, помогает оно или вредит, лежит на уровень глубже: **спека — это источник истины или описание истины, записанное где-то ещё?**

Когда спека — побочный документ (написали один раз, выгрузили на портал доков, никогда не сверяли с работающим сервисом), она гниёт за месяцы. Код меняется, спека — нет, и теперь у тебя два контракта: тот, что сервер обеспечивает, и тот, что заявляет спека. Клиенты интегрируются по спеке, спека — фикция, и интеграция падает в проде. Это *contract drift* (молчаливое расхождение спеки и реализации), и это исход по умолчанию, пока что-то активно ему не мешает.

Фикс — не «писать доки лучше». Фикс — сделать спеку несущей: код генерируется из неё, запросы валидируются по ней, а её дифф гейтит каждый pull request. Контракт, который нельзя нарушить без падения билда, — это контракт. Всё остальное — страница в вики.

## Spec-first vs code-first: где живёт истина

Спроси себя: контракт твоего API ревьюят до того, как написана первая строка хендлера, или он рождается из того, что инженер решил написать в пятницу? От ответа зависит, какую модель выбирать.

Получить спеку можно двумя способами, и разница в том, какой артефакт авторитетен.

**Spec-first (design-first — «спека раньше кода»):** ты пишешь OpenAPI-документ руками, ревьюишь его как дизайн API и *генерируешь* серверные заглушки, типизированных клиентов и моки из него. Спека — источник истины; код ей соответствует. Фронтенд может строить против сгенерированного мока в тот же день, когда бэкенд только начал, потому что контракт существует раньше любой реализации.

**Code-first («сначала код»):** ты пишешь сервер с аннотациями или декораторами (`@ApiProperty`, type hints в FastAPI, springdoc), а инструмент *генерирует* спеку из работающего кода. Код — источник истины; спека — его отражение. Меньше церемоний, и спека не разойдётся с хендлером, из которого её извлекли, — но может разойтись с тем, чего ждали потребители, потому что никто не отревьюил контракт как контракт до выкатки.

Ни то ни другое не ошибка. Решение сеньора такое: spec-first, когда API потребляют несколько команд или внешние партнёры и контракт нужно ревьюить и держать стабильным; code-first, когда одна команда владеет обоими концами и скорость важнее предварительного дизайна. Что *не* опционально в любой модели — это enforcement: спека должна валидироваться и диффиться, как бы она ни родилась.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Измерение</th>
        <th class="border border-border px-3 py-2 text-left">Spec-first</th>
        <th class="border border-border px-3 py-2 text-left">Code-first</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">Источник истины</td>
        <td class="border border-border px-3 py-2">Написанная руками спека</td>
        <td class="border border-border px-3 py-2">Код сервера + аннотации</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Контракт ревьюят до кода?</td>
        <td class="border border-border px-3 py-2">Да — это дизайн-артефакт</td>
        <td class="border border-border px-3 py-2">Нет — рождается из реализации</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Параллельная работа клиент/сервер</td>
        <td class="border border-border px-3 py-2">С первого дня (мок из спеки)</td>
        <td class="border border-border px-3 py-2">После компиляции сервера</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Риск drift</td>
        <td class="border border-border px-3 py-2">Спека vs реализация (валидируй на edge)</td>
        <td class="border border-border px-3 py-2">Контракт vs ожидания потребителя</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Лучше всего для</td>
        <td class="border border-border px-3 py-2">Публичные API, много потребителей</td>
        <td class="border border-border px-3 py-2">Одна команда владеет обоими концами</td>
      </tr>
    </tbody>
  </table>
</div>

## Что реально даёт OpenAPI 3.1

Спека отрабатывает не самим документом, а тулингом, который открывает. Важны четыре выигрыша:

- **Сгенерированные типизированные клиенты и серверные заглушки.** Прогони генератор по спеке и получи SDK на TypeScript, Go, Swift, Kotlin — с методами, моделями, типами ошибок. Класс интеграционных багов («я слал `userId`, серверу нужен `user_id`») исчезает, потому что запрос больше никто не пишет руками. Команды, выкатывающие публичные API, генерируют SDK из спеки именно чтобы убить эту категорию целиком.
- **Валидация запросов на edge.** Middleware валидирует входящие запросы по спеке до того, как они дойдут до хендлера. Битое тело отклоняется точным 400 с именем виноватого поля, а твоя бизнес-логика мусора не видит.
- **Доки, которые не разойдутся молча.** Когда отрендеренные доки генерируются из той же спеки, что гейтит CI, «доки врут» перестаёт быть возможным — доки *и есть* контракт.
- **Мок-серверы.** Инструмент отдаёт фейковые ответы прямо из примеров спеки, и потребители интегрируются раньше, чем появился бэкенд.

Вместе эти четыре выигрыша означают: спека отрабатывает не как документ, а как цепочка инструментов. Без CI-гейта первые три есть на бумаге, но ничто не останавливает ломающее изменение.

Конкретно OpenAPI 3.1 выравнивает язык схем с **JSON Schema 2020-12**, так что твои ключевые слова валидации — это настоящий JSON Schema, а не подмножество эпохи 3.0. Цена этой мощи: `nullable: true` больше нет — теперь пишешь `type: ["string", "null"]` — а `exclusiveMinimum`/`exclusiveMaximum` принимают число, а не булево. 3.1 ещё добавляет верхнеуровневый `webhooks` для документирования событий, которые твой API *шлёт*, а не только принимает. Многие инструменты по-прежнему дефолтят в 3.0, так что проверь поддержку генератора до апгрейда.

<Inset kind="why" lang="ru">
Генераторы кода не магия — они кодируют мнения. Спека с рыхлыми схемами (`additionalProperties: true`, без массивов `required`, нетипизированные `object`-блобы) генерирует рыхлых, нашпигованных `any` клиентов, что убивает смысл. Дисциплина, которая делает генерацию ценной, — это плотные схемы: явные типы, явный `required`, переиспользуемые компоненты. Генератор честен ровно настолько, насколько честен контракт, который ты ему скармливаешь.
</Inset>

## Детект ломающих изменений: гейт, который ловит пятничный деплой

Когда видишь PR, который трогает имя поля, тип или массив `required`, легко пропустить, стало оно `required` или нет — одно слово, а ломает каждого клиента, кто его не шлёт. У истории с `tenantId` один чистый фикс: инструмент, который диффит новую спеку против предыдущей версии и *роняет PR* на ломающем изменении. `oasdiff` (утилита для diff-анализа OpenAPI-спек) — частый выбор: он классифицирует 450+ категорий изменений и знает, какие ломающие. Добавление `required`-поля запроса, удаление поля ответа, которое клиент может читать, сужение типа, удаление значения enum, удаление эндпоинта — всё ломающее, всё ловится до мержа.

<Infographic
  svg={breakingVsSafe}
  label="Сравнение в две колонки: изменения OpenAPI-спеки, которые гейт oasdiff в CI считает ломающими — добавить required-поле запроса, удалить поле ответа, сузить тип, удалить значение enum, удалить эндпоинт — против обратносовместимых, которые он пропускает: добавить опциональное поле или новый эндпоинт, добавить поле ответа, расширить тип."
  caption="Один и тот же дифф, два вердикта: розовые изменения отклоняют существующих клиентов и роняют PR; бирюзовые — аддитивны и безопасно мержатся."
  hue="var(--d-infra)"
/>

Вшитый в CI, чек туп и эффективен: пайплайн диффит спеку из PR против `main`, и если появляется ломающее изменение, билд красный, а инженер видит *ровно* какое поле сломалось — раньше любого клиента. Пятничный деплой не доходит до прода, потому что контракт обеспечивается тем же гейтом, что и тесты. Тонкость в 3.1: так как nullability переехала в массив типов, удаление `"null"` из `type: ["string", "null"]` регистрируется как смена типа, а не как смена nullable — дифф всё равно ловит это, просто по другому правилу, поэтому смотри на *категорию*, которую сообщает инструмент, а не только на количество.

<TradeoffMatrix
  id="04-openapi-overview-tradeoff-1"
  lessonSlug="01-overview"
  lang="ru"
  prompt="Публичный API потребляют мобильные приложения, которые ты не можешь принудительно обновить, и три партнёрские интеграции. Выбери стратегию enforcement."
  options={[
    { name: "Spec-first источник истины + гейт ломающих изменений oasdiff в CI + сгенерированные SDK", summary: "Контракт обеспечен.", correct: true, justification: "Контракт ревьюят до кода, ломающие изменения роняют PR раньше, чем сломается клиент, а потребители интегрируются против сгенерированного SDK, а не пишут запросы руками. Это ровно то, что нужно для API с потребителями, которых нельзя скоординировать — старые мобильные клиенты и внешние партнёры — потому что молча несовместимое изменение становится невозможно смержить." },
    { name: "Генерировать спеку из аннотаций кода и публиковать на портал доков", summary: "Code-first доки.", justification: "Лучше протухшей вики, но это лишь описывает текущий код — не сравнивает сегодняшний контракт со вчерашним. Изменение tenantId-required было бы честно задокументировано и всё равно сломало бы каждого клиента, потому что дифф ничто не гейтит. Code-first нормально, но ему нужен тот же гейт oasdiff сверху." },
    { name: "Писать спеку руками, рендерить красивые доки, ревьюить вручную в PR", summary: "Ручной ревью.", justification: "Человеческий ревью рутинно пропускает ломающие изменения — сужение типа или добавление required-поля выглядит безобидно в диффе. Весь смысл существования oasdiff в том, что «кто-нибудь поймает на ревью» не выживает в столкновении с пятничным деплоем и PR на 400 строк." },
    { name: "Отказаться от спеки; вести changelog API и просить команды читать его перед интеграцией", summary: "Только changelog.", justification: "Changelog — это вики-страница под другим именем: она дрейфует, пишется задним числом и полагается на то, что каждый потребитель её прочтёт. Она ничего не обеспечивает. Клиенты, что сломались в истории, сломались бы точно так же при changelog, который никто не перечитал." },
  ]}
/>

<Quiz
  id="04-openapi-overview-quiz-1"
  lessonSlug="01-overview"
  lang="ru"
  question="PR добавляет новое required-поле в тело запроса. Что должен сделать здоровый API-пайплайн?"
  choices={[
    { label: "Уронить билд — дифф ломающих изменений (например, oasdiff) флагует новое required-поле до мержа", correct: true },
    { label: "Пропустить — добавление поля всегда обратно совместимо", misconception: "Добавление опционального поля совместимо; добавление required отклоняет каждого существующего клиента, который его не шлёт. Это ломающее изменение, ровно то, что гейт диффа существует ловить." },
    { label: "Пропустить, но залогировать предупреждение для ревью позже", misconception: "Предупреждение, на котором никто не блокируется, — то же, что и отсутствие чека: сломанный деплой всё равно уезжает. Ломающие изменения должны ронять PR, а не давать строку в логе." },
    { label: "Пропустить — портал доков перегенерируется, и клиенты увидят изменение", misconception: "Перегенерированные доки описывают изменение, но не предотвращают его. Клиенты интегрируются неделями, а не в день твоего деплоя; контракт нужно обеспечивать, а не просто публиковать." },
  ]}
/>

<Quiz
  id="04-openapi-overview-quiz-2"
  lessonSlug="01-overview"
  lang="ru"
  question="Твои написанные руками доки говорят, что поле опционально, но сервер теперь отклоняет запросы без него. Как это называется и что это предотвращает?"
  choices={[
    { label: "Contract drift — предотвращается валидацией + диффом спеки в CI, чтобы она не могла молча разойтись с сервером", correct: true },
    { label: "Баг 422 — чинится тем, что сервер принимает отсутствующее поле", misconception: "Сервер может быть прав; проблема в том, что контракт и реализация расходятся. Заставлять сервер соответствовать протухшим докам прячет реальную проблему — что ничто не держит спеку и код в синхроне." },
    { label: "Переиспользование схем — решается большим числом `$ref`-компонентов", misconception: "`$ref` сокращает дублирование внутри спеки; он ничего не делает со спекой, которая больше не совпадает с работающим сервером. Это drift, а не дублирование." },
    { label: "Нормальная эволюция API — клиентам надо просто повторить запрос", misconception: "Повтор запроса, в котором нет ставшего обязательным поля, падает одинаково каждый раз. Drift — это сбой процесса, а не временный сбой." },
  ]}
/>

<DragOrder
  id="04-openapi-overview-drag-1"
  lessonSlug="01-overview"
  lang="ru"
  prompt="Расставь шаги spec-first пайплайна, который предотвращает drift:"
  items={[
    { id: "a", label: "Написать / обновить OpenAPI-спеку и отревьюить её как контракт" },
    { id: "b", label: "CI диффит спеку против предыдущей версии (oasdiff) и роняет билд на ломающем изменении" },
    { id: "c", label: "Сгенерировать типизированных клиентов, серверные заглушки и мок из спеки" },
    { id: "d", label: "Валидировать входящие запросы по спеке на edge до того, как отработает хендлер" },
    { id: "e", label: "Рендерить доки из той же спеки, чтобы они не могли противоречить контракту" },
  ]}
  correctOrder={["a", "b", "c", "d", "e"]}
/>

</Explanation>

<FlowDiagram
  label="Одна спека — источник истины"
  hue="var(--d-backend)"
  perRow={4}
  nodes={[
    { id: "spec", label: "OpenAPI spec", sub: "источник истины", col: 0, row: 0 },
    { id: "docs", label: "Docs", sub: "не дрейфуют", col: 0, row: 1 },
    { id: "server", label: "Server stubs", col: 1, row: 1 },
    { id: "client", label: "Client SDKs", col: 2, row: 1 },
    { id: "mocks", label: "Mocks + валидация", col: 3, row: 1 },
    { id: "ci", label: "CI break-diff gate", sub: "ронять PR", col: 0, row: 2 },
  ]}
  edges={[
    { from: "spec", to: "docs", label: "генерит" },
    { from: "spec", to: "server", label: "генерит" },
    { from: "spec", to: "client", label: "генерит" },
    { from: "spec", to: "mocks", label: "генерит" },
    { from: "ci", to: "spec", label: "гейтит" },
  ]}
  caption="Генерируй docs, stubs, SDK и mocks из одной спеки; дифф ломающих изменений в CI гейтит каждое изменение, чтобы контракт не дрейфовал молча."
/>

<KeyTakeaway>Спека — контракт, только когда её обеспечивает CI: генерируй из неё клиентов, валидируй по ней запросы, роняй PR на ломающем диффе. Без enforcement это доки, что дрейфуют в фикцию и ломают клиентов.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="04-openapi-overview-retrieval"
  lang="ru"
  questions={[
    { q: "Коллега говорит: «у нас уже есть OpenAPI-спека, значит, мы защищены от ломающих изменений». Почему это не так и чего на самом деле не хватает?", a: "Иметь спеку необходимо, но недостаточно. Спека — это просто файл; сама по себе она ничего не предотвращает. Ломающие изменения ловит гейт в CI, который диффит новую спеку против предыдущей версии и роняет pull request, когда появляется ломающее изменение — удалённое поле ответа, ставшее обязательным поле запроса, суженный тип, удалённое значение enum или эндпоинт. Инструмент вроде oasdiff классифицирует сотни категорий изменений и знает, какие ломающие. Без этого гейта спека описывает контракт, но ничего не обеспечивает: инженер может добавить required-поле, спека честно его запишет, а каждый существующий клиент всё равно сломается в проде. Спека защищает, только когда она несущая — из неё генерируют или по ней валидируют, её диффят в CI и из неё же рендерят доки. Спека, которую никто не проверяет, — это документация, а документация дрейфует в фикцию." },
    { q: "Когда выбрать code-first вместо spec-first, и какой enforcement всё равно нужен в любом случае?", a: "Code-first — генерация спеки из аннотаций сервера или type hints — подходит, когда одна команда владеет и API, и его потребителями, а скорость важнее предварительного дизайна контракта. Это легче: нет отдельной спеки, которую держишь в синхроне руками, и спека не разойдётся с хендлером, из которого её извлекли. Spec-first подходит, когда API потребляют несколько команд или внешние партнёры, потому что контракт ревьюят как дизайн-артефакт раньше, чем появится код, и фронтенд может строить против сгенерированного мока с первого дня. Но выбор того, как родилась спека, не меняет нужного enforcement: в любом случае спеку надо диффить на ломающие изменения в CI, и в идеале запросы валидировать по ней на edge. Code-first спеки расходятся с ожиданиями потребителя так же легко — код честен о себе, но никто не отревьюил, безопасно ли изменение для клиентов. Гейт ломающих изменений не опционален ни в одной из моделей." },
  ]}
/>

<Recap lang="ru">
OpenAPI — это машиночитаемое описание HTTP API, но описание бесполезно, пока оно не источник истины. Сбойный режим — contract drift: написанные руками доки говорят одно, сервер обеспечивает другое, клиенты интегрируются против фикции, и изменение в одну строку вроде добавления required-поля ломает каждого потребителя в проде. Spec-first считает написанную руками спеку авторитетной и генерирует из неё клиентов, заглушки и моки; code-first генерирует спеку из аннотаций. В любом случае спеку надо обеспечивать — сгенерированные типизированные клиенты убивают целый класс багов с запросами вручную, валидация на edge отклоняет битый ввод до хендлера, а дифф ломающих изменений (oasdiff) роняет PR в тот момент, когда кто-то удаляет поле, сужает тип или добавляет required. OpenAPI 3.1 выравнивает схемы с JSON Schema 2020-12, выкидывает `nullable` ради массивов `type` и добавляет `webhooks`. Уплотни схемы переиспользуемыми `$ref`-компонентами, явным `required` и схемами авторизации, а затем дай пайплайну сделать контракт невозможным сломать молча. Теперь, когда встречаешь PR с изменением имени поля, типа или массива `required`, первый вопрос: запускался ли гейт ломающих изменений — и он зелёный?
</Recap>
