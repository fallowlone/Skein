---
concepts:
- schema-tightness
- breaking-change-detection
- openapi-3-1
- request-validation
- codegen-pitfall
estMin: 14
lang: ru
level: senior
order: 9
prereqs: []
slug: quiz-code
sources:
- https://spec.openapis.org/oas/v3.1.0.html
- https://www.oasdiff.com/docs/breaking-changes
- https://learn.openapis.org/best-practices.html
status: ready
summary: "Читай реальные фрагменты OpenAPI YAML, вердикт oasdiff и сгенерированный клиент, затем выбирай фикс с наибольшим рычагом, чтобы держать contract честным."
title: 'OpenAPI: чтение spec и diff'
track: apis
unit: 04-openapi
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Проблемы контракта ловятся в файле spec, в выводе diff и в сгенерированном клиенте. Читай каждый фрагмент, предсказывай влияние на потребителей и выбирай фикс, который senior-инженер делает первым.
</Hook>

<Goal lang="ru">
Отработай цикл, через который проходит каждое изменение API: читай схему, предсказывай последствия для breaking changes и codegen и тянись к фиксу, который держит spec несущим.
</Goal>

## Фрагмент 1 — слабая схема

```yaml
components:
  schemas:
    Order:
      type: object
      properties:
        id: { type: string }
        items:
          type: object          # нетипизированный блоб
        total: {}               # вообще без типа
      additionalProperties: true # любые другие ключи разрешены
      # нет массива `required:`
```

<Quiz
  id="04-openapi-code-1"
  lessonSlug="04-openapi"
  lang="ru"
  question="Генератор превращает эту схему Order в клиент. Как будет выглядеть сгенерированный тип и каков фикс?"
  choices={[
    { label: "В основном any/unknown: items — нетипизированный object, у total нет типа, additionalProperties пропускает произвольные ключи, и ничего не required, так что каждое поле optional. Фикс: дай items/total конкретные типы (или $ref), добавь массив required и закрой additionalProperties, если не принимаешь лишние ключи всерьёз.", correct: true },
    { label: "Полностью типизированный Order с required id, items и total — генераторы выводят разумные дефолты.", misconception: "Генераторы не выдумывают ограничения, которые схема опускает. Нетипизированный object становится any, отсутствующий массив required делает всё optional, а открытый additionalProperties запрещает закрытый тип. Вывод честно слабый." },
    { label: "Он не сгенерируется, потому что схема невалидна.", misconception: "Эта схема — валидный OpenAPI: слабый, но легальный. Генерируется нормально, просто выдаёт слабый клиент с any, который сводит на нет смысл codegen." },
    { label: "Слабость безвредна, потому что рантайм-валидация поймает плохие данные.", misconception: "Валидировать не по чему — additionalProperties: true и нетипизированные поля принимают почти всё. Слабые схемы ослабляют и сгенерированные типы, и валидацию на краю." },
  ]}
/>

## Фрагмент 2 — вердикт oasdiff

```
$ oasdiff breaking main.yaml pr.yaml
1 breaking changes: 1 error, 0 warning
error  api-required-request-property-added
       in API POST /orders
       added the required request property 'tenantId'
```

<Quiz
  id="04-openapi-code-2"
  lessonSlug="04-openapi"
  lang="ru"
  question="PR добавляет tenantId как required-поле в POST /orders, и oasdiff сообщает об этом в CI. Что должен сделать пайплайн и какова совместимая альтернатива?"
  choices={[
    { label: "Уронить PR — добавление required-свойства запроса отклоняет каждого существующего клиента, который не шлёт tenantId. Совместимый путь: сначала добавить tenantId как optional, дообновить клиентов, затем перевести его в required в более поздней объявленной версии.", correct: true },
    { label: "Пропустить с предупреждением — добавление поля обратно совместимо.", misconception: "Добавление optional-поля совместимо; добавление required — хрестоматийный breaking change, потому oasdiff и классифицирует его как error, а не warning." },
    { label: "Пропустить — вывод oasdiff носит рекомендательный характер, а docs-портал покажет новое поле.", misconception: "Перегенерированные docs описывают изменение, но не предотвращают поломку. Вся ценность gate в том, что breaking change роняет сборку до того, как сломается клиент." },
    { label: "Уронить, затем починить, удалив tenantId из схемы ответа.", misconception: "Поломка в запросе, а не в ответе. Правка ответа не делает новое required-поле запроса совместимым." },
  ]}
/>

## Фрагмент 3 — переписывание nullability с 3.0 на 3.1

```yaml
# OpenAPI 3.0
status:
  type: string
  nullable: true

# OpenAPI 3.1 (JSON Schema 2020-12)
status:
  type: [string, "null"]
```

<Quiz
  id="04-openapi-code-3"
  lessonSlug="04-openapi"
  lang="ru"
  question="Ты мигрируешь spec на 3.1 и переписываешь nullability вот так. За чем надо следить в тулинге и дифах?"
  choices={[
    { label: "nullable: true невалиден в 3.1 — nullability теперь живёт в type-массиве. Проверь, что генератор и валидатор поддерживают 3.1 (многие всё ещё по умолчанию на 3.0), и учти, что позднее удаление 'null' из массива регистрируется в oasdiff как изменение типа, а не nullable.", correct: true },
    { label: "Ничего — nullable: true всё ещё работает в 3.1 как алиас для формы с type-массивом.", misconception: "3.1 полностью убрал nullable; он игнорируется или невалиден. Нужно использовать type: [string, 'null'], а инструменты под 3.0 могут его не понять." },
    { label: "Две формы — это разные контракты: версия 3.1 больше не допускает null.", misconception: "Обе выражают 'строка или null' — контракт идентичен. Миграция синтаксическая; допустимые значения те же." },
    { label: "3.1 делает status required, потому что это теперь тип-массив.", misconception: "type: [string, 'null'] — это объединение допустимых типов, не связанное с тем, required ли поле. Required-ность контролируется только массивом required." },
  ]}
/>

## Фрагмент 4 — code-first аннотации

```python
class CreateOrder(BaseModel):
    id: str
    items: list[Item]
    coupon: str | None = None   # optional, nullable

# FastAPI генерирует OpenAPI spec из этой модели.
# Позднее правка делает coupon required:
    coupon: str                 # теперь required, без дефолта
```

<Quiz
  id="04-openapi-code-4"
  lessonSlug="04-openapi"
  lang="ru"
  question="В code-first-сетапе инженер делает coupon required, убрав его дефолт. Сгенерированный spec обновляется автоматически. В чём риск и что его закрывает?"
  choices={[
    { label: "Code-first держит spec в синхронизации с кодом, но не с ожиданиями потребителей — сгенерированный spec теперь требует coupon и молча ломает каждого клиента, который его опускает. Закрытие требует того же gate oasdiff: диффить сгенерированный spec против предыдущего в CI и ронять сборку на новом required-поле.", correct: true },
    { label: "Риска нет — code-first spec не может дрейфовать, так что изменение безопасно.", misconception: "Code-first предотвращает drift между spec и хендлером, а не между контрактом и тем, на что опираются клиенты. Изменение required-поля — breaking, как бы spec ни был получен." },
    { label: "Риск в том, что FastAPI отвергнет модель на старте.", misconception: "Модель — валидный Python; FastAPI стартует нормально и честно публикует coupon как required. Опасность ниже по течению, на клиентах, а не на старте." },
    { label: "Перейди на spec-first, и проблема исчезнет сама.", misconception: "Spec-first выносит изменение на review, но всё равно не блокирует его без diff-gate. Фикс — это enforcement (oasdiff в CI), который нужен обеим моделям одинаково." },
  ]}
/>

<Recap lang="ru">
Каждое изменение контракта читается в трёх местах: схема (слабые типы и открытый additionalProperties дают клиенты с any — ужесточай явными типами, массивами required и $ref), diff (oasdiff классифицирует добавление required-поля, удаление поля ответа или сужение типа как breaking — роняй PR и мигрируй совместимо) и направление генерации (code-first синхронен с кодом, но не с потребителями, поэтому ему нужен тот же gate, что и spec-first). OpenAPI 3.1 переносит nullability в type-массив и выравнивается с JSON Schema 2020-12 — проверь поддержку инструментов до миграции. Читай артефакт, предсказывай влияние на потребителя, затем чини на уровне контракта.
</Recap>
