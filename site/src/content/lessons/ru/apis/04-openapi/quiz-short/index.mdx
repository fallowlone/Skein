---
concepts:
- spec-first-vs-code-first
- contract-drift
- breaking-change-detection
- codegen
- request-validation
- openapi-3-1
estMin: 14
lang: ru
level: senior
order: 8
prereqs: []
slug: quiz-short
sources:
- https://spec.openapis.org/oas/v3.1.0.html
- https://www.oasdiff.com/docs/breaking-changes
- https://learn.openapis.org/best-practices.html
status: ready
summary: "Промпты на свободное воспроизведение по юниту OpenAPI. Сначала ответь своими словами, затем открой модельный ответ и сравни."
title: 'OpenAPI: тест на свободное воспроизведение'
track: apis
unit: 04-openapi
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";

<Hook>
Воспроизведение бьёт перечитывание. Для каждого промпта проговори или запиши полный ответ по памяти, прежде чем открыть модельный — именно усилие припоминания закрепляет дисциплину контракта.
</Hook>

<Goal lang="ru">
Восстанови костяк юнита — почему неэнфорсенный spec это фикция, spec-first vs code-first, что дают codegen и валидация на краю, что ловит oasdiff и сдвиг схем в 3.1 — не заглядывая в урок.
</Goal>

<RetrievalDrawer
  client:visible
  id="04-openapi-short-retrieval"
  lang="ru"
  questions={[
    { q: "Почему наличие OpenAPI spec не то же самое, что защита от breaking changes? Что на самом деле защищает?", a: "Spec — это просто файл; он описывает contract, но сам по себе ничего не enforce-ит. Защита приходит от того, что spec становится несущим в CI: diff-инструмент на breaking changes (oasdiff) сравнивает новый spec с предыдущей версией и роняет pull request, когда находит удалённое поле ответа, новое required-поле запроса, суженный тип, удалённое enum-значение или убранный endpoint. Без этого gate инженер может добавить required-поле, spec честно его зафиксирует, и каждый существующий клиент всё равно сломается в production. Spec защищает только когда из него генерируют или по нему валидируют, диффят в CI и рендерят docs из того же файла. Spec, который никто не проверяет — это документация, а документация дрейфует в фикцию." },
    { q: "Дай определение contract drift и назови два механизма, которые его предотвращают.", a: "Contract drift — это когда написанный контракт (вручную сделанный spec или устаревшие docs) и работающий сервер молча расходятся: docs говорят, что поле optional, сервер теперь отклоняет запросы без него, и клиенты интегрируются с фикцией и ломаются. Два механизма его предотвращают. Первый — валидация запросов на краю: middleware проверяет каждый входящий запрос по spec до хендлера, так что битое тело получает точный 400, и spec с рантаймом не могут незаметно расходиться. Второй — diff на breaking changes в CI: spec сравнивается с предыдущей версией на каждом PR, так что несовместимое изменение роняет сборку. Вместе они делают spec невозможным для нарушения без того, чтобы пайплайн это заметил." },
    { q: "Сопоставь spec-first и code-first: что различается и какой enforcement всё равно нужен обоим?", a: "Spec-first (design-first) — ты пишешь OpenAPI-документ вручную, ревьюишь его как контракт и генерируешь из него серверные стабы, типизированные клиенты и mock-серверы: spec — источник истины, код подчиняется ему, так что контракт ревьюится до того, как появится код, и фронтенд строится против mock в первый же день. Code-first — ты аннотируешь сервер (type hints, декораторы) и генерируешь spec из работающего кода: код — источник истины, меньше церемоний, и spec не может дрейфовать от хендлера, из которого извлечён. Senior-решение: spec-first для публичных API со множеством или внешних потребителей, которым нужен проверенный стабильный контракт; code-first когда одна команда владеет обоими концами и важна скорость. Но ни один не уходит от enforcement — оба должны диффить spec на breaking changes в CI и, в идеале, валидировать запросы на краю. Code-first spec дрейфуют от ожиданий потребителей столь же легко." },
    { q: "Что даёт codegen и что отличает сильный сгенерированный клиент от клиента с any?", a: "Прогон генератора по spec даёт типизированные SDK и серверные стабы — методы, модели, типы ошибок — на TypeScript, Go, Swift, Kotlin и других. Это убивает весь класс багов с ручной сборкой запроса ('я отправил userId, сервер хотел user_id'), потому что запрос больше никто не пишет руками, а серверные стабы гарантируют, что хендлеры соответствуют контракту. Качество вывода целиком зависит от схемы, которую ты скармливаешь: тугие схемы (явные типы, явные массивы required, закрытый additionalProperties, переиспользуемые $ref-компоненты) дают сильные, точно типизированные клиенты; слабые схемы (нетипизированные object-блобы, отсутствующий required, открытый additionalProperties) дают клиенты с any, которые сводят на нет смысл. Генератор настолько честен, насколько честен контракт." },
    { q: "Перечисли канонические breaking changes, которые ловит diff-инструмент вроде oasdiff, и одну совместимую альтернативу breaking change.", a: "Breaking changes включают: добавление нового required-свойства запроса; удаление или переименование поля ответа, которое клиент может читать; сужение типа (string в enum, ужесточение ограничений); удаление enum-значения; удаление endpoint или операции; превращение ранее optional-параметра в required; ужесточение min/max или pattern. Каждое отклоняет или ломает хотя бы часть существующих клиентов. Совместимая альтернатива для добавления required-поля: сначала добавить его как optional, дать разумный дефолт или backfill, дать клиентам перейти со временем и только потом перевести в required в более поздней объявленной (часто мажорной) версии. Аддитивное-и-optional — безопасная форма; удаление и ужесточение — опасные." },
    { q: "Что изменилось между OpenAPI 3.0 и 3.1 и почему это важно на практике?", a: "OpenAPI 3.1 выравнивает язык схем с JSON Schema 2020-12, так что ключевые слова валидации, которые ты пишешь — настоящий JSON Schema, а не подмножество эпохи 3.0. Конкретно: nullable: true исчез — nullability выражается type-массивом, type: ['string', 'null']; exclusiveMinimum и exclusiveMaximum принимают число вместо boolean; и 3.1 добавляет верхнеуровневый объект webhooks для документирования событий, которые API шлёт, а не только принимает. На практике это важно, потому что многие генераторы и валидаторы всё ещё по умолчанию на 3.0 и могут не понимать 3.1, так что проверяй поддержку инструментов до апгрейда. Это также тонко меняет дифы — удаление 'null' из type-массива позже показывается как изменение типа, а не nullable, так что читай категорию изменения, которую сообщает инструмент, а не только счётчик." },
  ]}
/>

<Recap lang="ru">
Если ты смог восстановить каждый ответ по памяти, у тебя есть костяк юнита: неэнфорсенный spec это фикция, а лекарство от contract drift — сделать spec несущим через валидацию на краю и diff на breaking changes в CI. Spec-first vs code-first решает, где живёт истина и ревьюится ли контракт, но обоим нужен тот же gate. Codegen убивает баги с ручной сборкой запроса только при тугой схеме, oasdiff ловит канонические breaking changes (добавленное required-поле, удалённое поле ответа, суженный тип), а OpenAPI 3.1 переводит схемы на JSON Schema 2020-12 с nullability через type-массив. Всегда: сделай контракт невозможным для тихой поломки.
</Recap>
