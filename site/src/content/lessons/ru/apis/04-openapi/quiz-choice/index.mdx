---
concepts:
- spec-first-vs-code-first
- contract-drift
- breaking-change-detection
- codegen
- request-validation
- openapi-3-1
estMin: 13
lang: ru
level: senior
order: 7
prereqs: []
slug: quiz-choice
sources:
- https://spec.openapis.org/oas/v3.1.0.html
- https://www.oasdiff.com/docs/breaking-changes
- https://learn.openapis.org/best-practices.html
status: ready
summary: "Тест с выбором на синтез по всему юниту OpenAPI — spec-first vs code-first, contract drift, gate на breaking changes, codegen, валидация и сдвиг схем в 3.1."
title: 'OpenAPI: тест с выбором ответа'
track: apis
unit: 04-openapi
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Шесть вопросов поперёк всего юнита. Каждый — это решение по реальному API: не определение для заучивания, а выбор, где живёт истина и как пайплайн держит её честной.
</Hook>

<Goal lang="ru">
Убедись, что связываешь spec-first vs code-first, contract drift, gate на breaking changes, codegen и валидацию на краю в одну историю про enforcement — тот синтез, к которому вёл урок.
</Goal>

<Quiz
  id="04-openapi-choice-1"
  lessonSlug="04-openapi"
  lang="ru"
  question="У команды есть написанный вручную OpenAPI spec, отрендеренный в красивый docs-портал. Коллега говорит: 'мы защищены от breaking changes.' Чего на самом деле не хватает?"
  choices={[
    { label: "Gate в CI, который диффит новый spec против предыдущей версии и роняет PR на breaking change. Spec — это файл; сам по себе он ничего не предотвращает, пока diff-инструмент вроде oasdiff не блокирует merge.", correct: true },
    { label: "Ничего — раз spec есть, клиенты увидят любое изменение до интеграции.", misconception: "Клиенты интегрируются неделями, а не в день твоего деплоя. Опубликованный spec описывает изменение, но не мешает инженеру добавить required-поле и сломать каждого существующего клиента в production." },
    { label: "Больше $ref-компонентов, чтобы убрать дублирование схем.", misconception: "$ref снижает дублирование внутри spec; он никак не обнаруживает несовместимые изменения между версиями spec. Для этого нужен diff на breaking changes." },
    { label: "Переход с code-first на spec-first — это само по себе предотвращает breaking changes.", misconception: "То, как spec рождается, ничего не гейтит. Spec-first так же нуждается в gate oasdiff; иначе проверенное-но-несовместимое изменение мёржится столь же легко." },
  ]}
/>

<Quiz
  id="04-openapi-choice-2"
  lessonSlug="04-openapi"
  lang="ru"
  question="Твои написанные вручную docs помечают поле как optional, но работающий сервер теперь отклоняет запросы без него. Как это называется и что это предотвращает?"
  choices={[
    { label: "Contract drift — spec и реализация молча разошлись. Предотвращается тем, что spec становится несущим: валидируй запросы по нему на краю и диффь его в CI, чтобы он не мог незаметно расходиться с сервером.", correct: true },
    { label: "Баг сервера — почини его, заставив хендлер принимать отсутствующее поле.", misconception: "Сервер может быть прав; дефект в том, что контракт и код не согласуются. Подгонка сервера под устаревшие docs прячет настоящую проблему — ничто не держит spec и код в синхронизации." },
    { label: "Нормальная эволюция API — клиентам надо ретраить.", misconception: "Запрос без теперь-required поля падает одинаково на каждом ретрае. Drift — это сбой процесса, а не транзиентный сбой." },
    { label: "Проблема версионирования — подними API до v2.", misconception: "Новая версия не примиряет spec, который уже лжёт про текущую версию. Сначала нужно, чтобы spec отслеживал сервер; версионирование управляет намеренными изменениями, а не случайным drift." },
  ]}
/>

<Quiz
  id="04-openapi-choice-3"
  lessonSlug="04-openapi"
  lang="ru"
  question="Публичный API потребляют мобильные приложения, которые ты не можешь форс-обновить, и три внешних партнёра. Какая стратегия enforcement подходит?"
  choices={[
    { label: "Spec-first как источник истины, gate на breaking changes (oasdiff) в CI и сгенерированные SDK для потребителей. Контракт проверяется до кода, и молча-несовместимое изменение не может смёржиться.", correct: true },
    { label: "Генерировать spec из аннотаций кода и публиковать в docs-портал.", misconception: "Code-first docs описывают текущий код, но никогда не сравнивают сегодняшний контракт со вчерашним. Новое required-поле честно задокументируется и всё равно сломает каждого старого клиента. Code-first нормален, но ему нужен тот же diff-gate сверху." },
    { label: "Писать spec вручную и проверять его руками в PR.", misconception: "Ручной review регулярно пропускает breaking changes — сужение типа или добавление required-поля выглядит безобидно в дифе на 400 строк. oasdiff существует ровно потому, что 'кто-нибудь заметит' не переживает пятничный деплой." },
    { label: "Отказаться от spec и вести changelog API, который команды читают перед интеграцией.", misconception: "Changelog — это вики-страница под другим именем: дрейфует, пишется постфактум и ничего не enforce-ит. Сломавшиеся клиенты сломались бы так же с changelog, который никто не перечитал." },
  ]}
/>

<Quiz
  id="04-openapi-choice-4"
  lessonSlug="04-openapi"
  lang="ru"
  question="Ты генерируешь TypeScript-клиент из spec, и результат полон any и слабо типизированных объектов. В чём первопричина?"
  choices={[
    { label: "У spec слабые схемы — открытый additionalProperties, отсутствующие required-массивы, нетипизированные object-блобы. Генератор кодирует контракт, который ты ему скармливаешь, поэтому слабый контракт даёт слабый клиент. Ужесточи схемы: явные типы, явный required и переиспользуемые компоненты.", correct: true },
    { label: "Генератор багованный — смени инструмент.", misconception: "Любой соответствующий генератор выдаёт слабый вывод из слабой схемы; это честно, а не баг. Фикс выше по течению — в контракте, а не в инструменте." },
    { label: "TypeScript не может точно выразить схемы OpenAPI.", misconception: "TypeScript прекрасно выражает эти формы, когда схема тугая. Вывод с any отражает недоспецифицированный контракт, а не предел языка." },
    { label: "Code-first-генерация всегда даёт более сильные типы, чем spec-first.", misconception: "Качество типов отслеживает тугость схемы, а не направление, в котором spec сгенерирован. Неряшливый code-first spec генерирует столь же неряшливый клиент." },
  ]}
/>

<Quiz
  id="04-openapi-choice-5"
  lessonSlug="04-openapi"
  lang="ru"
  question="Ты апгрейдишь spec с OpenAPI 3.0 до 3.1. Какое изменение реально и почему оно важно?"
  choices={[
    { label: "3.1 выравнивает схемы с JSON Schema 2020-12: nullable: true исчез в пользу type: ['string', 'null'], а exclusiveMinimum/Maximum принимают число, а не boolean. Ключевые слова валидации теперь — настоящий JSON Schema, но многие инструменты всё ещё по умолчанию на 3.0 — сначала проверь поддержку генератора.", correct: true },
    { label: "3.1 убирает $ref, так что придётся инлайнить каждую схему.", misconception: "$ref не изменился и остаётся способом переиспользовать компоненты. Изменения схем в 3.1 про nullability и выравнивание с JSON Schema, а не про ссылки." },
    { label: "3.1 делает всю валидацию запросов автоматической без middleware.", misconception: "3.1 меняет язык схем, а не рантайм. Тебе всё равно нужен validator-middleware на краю, чтобы enforce-ить запросы по spec." },
    { label: "3.1 чисто косметический и ничего не меняет в схемах.", misconception: "Переписывание nullability — конкретное синтаксическое изменение: удалённый 'null' из type-массива даже регистрируется в дифе как изменение типа, а не nullable." },
  ]}
/>

<Quiz
  id="04-openapi-choice-6"
  lessonSlug="04-openapi"
  lang="ru"
  question="Какое изменение контракта запроса/ответа обратно совместимо и безопасно для выкатки без мажорного бампа версии?"
  choices={[
    { label: "Добавление нового optional-поля в запрос или нового поля в ответ. Существующие клиенты продолжают слать то, что слали, и игнорируют неизвестные поля ответа, так что ни то ни другое их не ломает.", correct: true },
    { label: "Добавление нового required-поля в запрос.", misconception: "Каждый существующий клиент, который его не шлёт, теперь отклоняется. Добавление required-поля — классический breaking change, ровно тот случай, который должен ловить diff-gate." },
    { label: "Удаление поля из тела ответа.", misconception: "Клиент может читать это поле; его удаление молча ломает потребителей. Удаление полей ответа — breaking." },
    { label: "Сужение строкового поля до enum из трёх значений.", misconception: "Сужение типа отклоняет ранее валидные значения. Ужесточение существующего поля — breaking, хотя и выглядит как безобидное уточнение." },
  ]}
/>

<Recap lang="ru">
Сквозная линия юнита — одна история про enforcement: spec становится контрактом, только когда CI делает его несущим. Spec-first vs code-first решает, где живёт истина и проверяется ли контракт до кода, но ни один не уходит от gate — валидируй запросы на краю, диффь каждый PR через oasdiff, чтобы breaking changes (новое required-поле, удалённое поле ответа, суженный тип) роняли сборку, и скармливай тугие схемы генератору, чтобы codegen давал сильные типы. OpenAPI 3.1 переводит схемы на JSON Schema 2020-12, заменяя nullable на type-массивы. Без enforcement spec дрейфует в фикцию и ломает клиентов.
</Recap>
