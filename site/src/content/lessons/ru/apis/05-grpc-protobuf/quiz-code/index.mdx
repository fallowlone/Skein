---
concepts:
- field-tags
- reserved-fields
- schema-evolution
- streaming-signatures
- proto3-defaults
estMin: 14
lang: ru
level: senior
order: 9
prereqs: []
slug: quiz-code
sources:
- https://protobuf.dev/programming-guides/proto3/
- https://grpc.io/docs/what-is-grpc/core-concepts/
status: ready
summary: "Читай реальные .proto-сниппеты — правки field tag, reserved, сигнатуры streaming — и предсказывай поведение на проводе или фикс с наибольшим рычагом."
title: 'gRPC и Protobuf: чтение схемы и proto'
track: apis
unit: 05-grpc-protobuf
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Схема Protobuf — это контракт, который ты читаешь на код-ревью, до того как порча уедет в прод. Прочитай каждый diff или сигнатуру, разберись, что это делает на проводе, и выбери изменение, которое senior реально одобрил бы.
</Hook>

<Goal lang="ru">
Потренируй ревью, которое ты прогоняешь на каждом proto-PR: замечай ловушки field tag, подтверждай, что удаление корректно reserved, и читай сигнатуру streaming на ту форму вызова, которую она реально объявляет.
</Goal>

## Сниппет 1 — diff field tag

```diff
 message Order {
   string id = 1;
   int64 amount_cents = 2;
-  int32 discount_pct = 3;
-  Priority priority = 4;
+  Priority priority = 3;
+  Coupon coupon = 4;
 }
```

<Quiz
  id="05-grpc-protobuf-code-1"
  lessonSlug="05-grpc-protobuf"
  lang="ru"
  question="Старый клиент записал Order с priority в поле 4. Сервис, задеплоенный с этой новой схемой, читает то сообщение. Что он видит и почему?"
  choices={[
    { label: "Он читает старые байты priority в coupon (теперь поле 4) и находит priority пустым (поле 3, бывший discount_pct). Номера переназначены, поэтому значения попадают в неверные поля — молчаливая порча. Фикс — append-only: оставь priority = 4, добавь coupon = 5 и reserve 3.", correct: true },
    { label: "Он читает всё корректно — Protobuf сопоставляет старый priority новому priority по имени поля.", misconception: "Имена никогда не едут по проводу; едут только номера. Старые байты priority помечены 4, а поле 4 теперь coupon, так что значение декодируется как coupon." },
    { label: "Он падает при декодировании, потому что поле 3 сменило тип с int32 на Priority.", misconception: "Декодирование не сравнивает со старым типом — оно читает байты с провода под текущую схему. Здесь нет ошибки несовпадения типов, лишь значение, направленное в неверное поле." },
    { label: "Это безопасно, потому что все четыре номера (1-4) по-прежнему есть в сообщении.", misconception: "Переиспользование тех же номеров под другие поля — это и есть ловушка, а не защита. Wire-совместимость — это идентичность по номеру, а не наличие номера." },
  ]}
/>

## Сниппет 2 — удаление

```proto
message Customer {
  reserved 2, 5 to 7;
  reserved "email", "legacy_tier";
  string id = 1;
  string display_name = 3;
  Address address = 8;
}
```

<Quiz
  id="05-grpc-protobuf-code-2"
  lessonSlug="05-grpc-protobuf"
  lang="ru"
  question="Предложено новое поле: вернуть `string email = 2;`, потому что команда снова хочет email. Это компилируется и это правильный ход?"
  choices={[
    { label: "Не компилируется — 2 и имя email оба reserved, и в этом весь смысл: это мешает команде заново отравить номер, который старые пиры ещё могут слать. Добавь email со свежим, никогда не использованным номером (например, 9).", correct: true },
    { label: "Компилируется нормально; reserved — лишь документация и не мешает переиспользованию.", misconception: "reserved принудительно проверяется protoc — объявить reserved номер или имя и затем использовать его — это ошибка компиляции. Эта проверка и есть вся причина резервировать при удалении." },
    { label: "Компилируется, потому что тип совпадает с исходным полем email, так что переиспользование безопасно.", misconception: "Совпадение типов не важно; старые пиры, всё ещё шлющие исходное поле 2, запишут в новый email независимо от этого. reserved существует, чтобы сделать это невозможным." },
    { label: "Компилируется, но только если ещё убрать email из списка reserved имён.", misconception: "Снос резервации ради переиспользования номера сводит на нет механизм безопасности — именно эту правку reserved и должен помешать одобрить ревьюеру." },
  ]}
/>

## Сниппет 3 — сигнатуры streaming

```proto
service ChatService {
  rpc History(HistoryRequest) returns (stream Message);
  rpc Send(stream Message) returns (SendSummaryResponse);
  rpc Live(stream Message) returns (stream Message);
}
```

<Quiz
  id="05-grpc-protobuf-code-3"
  lessonSlug="05-grpc-protobuf"
  lang="ru"
  question="Сопоставь каждый RPC с его формой вызова и положением ключевого слова stream."
  choices={[
    { label: "History — server streaming (stream на ответе), Send — client streaming (stream на запросе), Live — bidirectional (stream на обоих). Сторона ключевого слова говорит, кто производит поток.", correct: true },
    { label: "Все три unary, потому что у каждого один тип запроса и один тип ответа.", misconception: "Ключевое слово stream меняет форму: stream Message означает последовательность Message, а не одно. Unary — только RPC без stream ни на одной стороне." },
    { label: "History — client streaming, потому что ключевое слово stream на втором типе.", misconception: "stream на возвращаемом типе (втором) означает, что сервер стримит ответы — server streaming. Client streaming ставит stream на типе запроса." },
    { label: "Live нельзя выразить в одном RPC; bidi нужны два отдельных RPC.", misconception: "Bidirectional streaming — это один RPC со stream и на запросе, и на ответе — ровно то, что объявляет Live. Он едет по одному мультиплексированному HTTP/2-потоку." },
  ]}
/>

## Сниппет 4 — presence в proto3

```proto
message UpdateProfile {
  string user_id = 1;
  string bio = 2;            // обычный скаляр
  optional bool marketing_opt_in = 3;  // явное presence
}
```

<Quiz
  id="05-grpc-protobuf-code-4"
  lessonSlug="05-grpc-protobuf"
  lang="ru"
  question="Клиент шлёт UpdateProfile с bio равным пустой строке и опущенным marketing_opt_in. Сервер хочет сделать PATCH только тех полей, которые клиент реально намеревался изменить. Что схема даёт ему различить?"
  choices={[
    { label: "Он не может понять, было ли bio «очищено в пустое» или «не прислано» — обычный скаляр default'ится в пустое без presence-бита. marketing_opt_in объявлен optional, так что сервер отличает «опущено» (не трогать) от «явно false». Для PATCH-семантики помечай поля optional (или используй FieldMask).", correct: true },
    { label: "Он может различить оба — proto3 всегда отслеживает, было ли каждое поле выставлено на проводе.", misconception: "У обычных скаляров proto3 нет presence: пустая строка неотличима от невыставленной. Только optional (или поля-сообщения) несут presence-бит." },
    { label: "Ни одно поле различить нельзя, потому что proto3 полностью убрал presence.", misconception: "proto3 вернул явное presence через ключевое слово optional; marketing_opt_in здесь его несёт. Нельзя различить как раз необъявленное optional bio." },
    { label: "Пустое bio означает, что поле никогда не слалось, так что сервер знает его пропустить.", misconception: "Скаляр-пустая-строка сериализуется так же, как default'нутый/неприсланный — сервер не может вывести намерение из него. Эта двусмысленность — как раз почему PATCH-эндпоинты берут optional или FieldMask." },
  ]}
/>

<Recap lang="ru">
Ревью Protobuf — это чтение номеров, а не имён: diff с перенумерацией молча направляет старые значения в неверное поле, поэтому эволюция должна быть append-only с reserved, охраняющим удаления (а protoc принуждает резервацию). Сигнатуру streaming читают по тому, какая сторона несёт stream — запрос для client, ответ для server, обе для bidi. А presence в proto3 реален, но opt-in: обычные скаляры не отличают unset от default, поэтому PATCH-стиль API тянется к optional или FieldMask. Лови это на ревью, до того как порча задеплоится.
</Recap>
