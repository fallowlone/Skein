---
concepts:
- wire-format
- field-number-contract
- backward-forward-compat
- streaming-shapes
- deadlines
- grpc-vs-rest
estMin: 13
lang: ru
level: senior
order: 8
prereqs: []
slug: quiz-short
sources:
- https://protobuf.dev/programming-guides/proto3/
- https://grpc.io/docs/what-is-grpc/core-concepts/
status: ready
summary: "Вопросы на свободное припоминание по юниту gRPC/Protobuf — wire format, дисциплина field tag, совместимость, streaming, deadlines и решение на границе. Сначала ответь, потом открой."
title: 'gRPC и Protobuf: тест на свободное припоминание'
track: apis
unit: 05-grpc-protobuf
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";

<Hook>
Припоминание бьёт перечитывание. Для каждого вопроса проговори или запиши полный ответ по памяти до того, как откроешь модельный ответ — именно усилие припоминания закрепляет контракт field tag и формы streaming.
</Hook>

<Goal lang="ru">
Восстанови спину юнита — почему провод несёт номера, а не имена, что делает изменение схемы безопасным, четыре формы вызовов, как распространяются deadlines и где место gRPC против REST — не заглядывая назад.
</Goal>

<RetrievalDrawer
  client:load
  id="05-grpc-protobuf-short-retrieval"
  lang="ru"
  questions={[
    { q: "Что именно Protobuf кладёт на провод и что это даёт и чего стоит по сравнению с JSON?", a: "Protobuf сериализует только field tag, wire type и значение — имена полей существуют исключительно в .proto на этапе компиляции и никогда не едут. JSON делает обратное, повторяя каждый ключ как UTF-8-текст в каждом payload'е. Этот единственный выбор делает Protobuf примерно в 4-5x меньше для насыщенных числами данных (varint'ы стоят один байт для малых int'ов, а не длину их десятичной строки) и в 4-10x быстрее в (де)сериализации, потому что нет токенизации текста, аллокации строк-ключей и рефлексии. Цена — обратная сторона суперсилы JSON: нельзя декодировать сообщение Protobuf без его схемы — байты бессмысленны сами по себе — так что payload'ы непрозрачны в DevTools и curl, а выигрыш в размере почти исчезает для насыщенных строками данных, где строки — сырой UTF-8 в обоих форматах." },
    { q: "Почему переименование поля бесплатно, а перенумерация или переиспользование field tag — production-катастрофа?", a: "Потому что идентичность на проводе — это field tag, а не имя. Переименование discount_pct в discountPercent меняет только локальную метку в исходниках; номер не меняется, байты идентичны, старые и новые бинарники прекрасно совместимы. Перенумерация 3 в 4 заставляет каждый пир, ещё на старой схеме, декодировать твоё новое поле 4 в то, что у него зовётся полем 4, а твои данные старого поля 3 попадают туда, где он ждёт 3 — молчаливая порча, которую компилятор не поймает, потому что каждая сторона скомпилировала самосогласованную схему. Переиспользование удалённого номера — та же катастрофа, отложенная: отстающий, всё ещё шлющий исходный номер, записывает старый смысл в новое поле. Дисциплина — add-only эволюция: никогда не меняй и не переиспользуй номер, добавляй со свежими номерами и reserve номер и имя всего, что удаляешь." },
    { q: "Объясни backward и forward совместимость в Protobuf и два механизма, которые их дают.", a: "Backward-совместимость — новый код читает сообщения, записанные старым кодом: отсутствующие новые поля просто берут свои default'ы proto3 (0, пустая строка, false, пусто). Forward-совместимость — старый код читает сообщения от нового кода: неизвестные field tag пропускаются — декодер использует wire type, чтобы узнать, сколько байт перешагнуть — и сохраняются для повторной сериализации, а не вызывают ошибку. Вместе это и позволяет producer'ам и consumer'ам в большой полиглотной системе деплоиться независимо, в любом порядке, без скоординированного релиза в ногу. Два механизма — нулевые default'ы proto3 (для направления отсутствующего поля) и пропуск-и-сохранение неизвестных полей (для направления лишнего поля), а add-only эволюция с reserved — то, что сохраняет обе гарантии целыми." },
    { q: "Назови четыре формы вызовов gRPC и решающий вопрос для выбора одной.", a: "Unary (один запрос, один ответ) — REST-образный случай ~95%. Server streaming (один запрос, поток ответов) подходит для price feed, хвоста логов или server-push прогресса. Client streaming (поток запросов, один ответ) подходит для чанковых загрузок или батчинга телеметрии. Bidirectional streaming (обе стороны стримят независимо в одном вызове) подходит для чата, живой коллаборации или долгоживущего канала управления. Решающий вопрос прост: кому нужно слать больше одного сообщения — если только серверу, это server streaming; если только клиенту, client streaming; если обоим независимо, bidi; если никому, unary. Тянуться к bidi, когда клиент шлёт лишь первичный subscribe — частый запах переусложнения. Все четыре едут по одному мультиплексированному HTTP/2-соединению." },
    { q: "Как работают deadlines в gRPC и какой классический сбивающий с толку сбой они вызывают?", a: "Клиент ставит deadline (относительную длительность, несомую в metadata, а не абсолютное wall-clock время, так что расхождение часов неважно), и он распространяется вниз по цепочке вызовов. При истечении RPC авто-отменяется на обоих концах, так что медленный downstream не может навечно пинить серверные потоки или ресурсы — пропуск deadlines — самая частая причина, по которой gRPC-mesh каскадно уходит в исчерпание ресурсов при частичном сбое. Классический сбивающий с толку сбой — вызов DEADLINE_EXCEEDED, который удался на сервере (обработчик отработал и закоммитил), но провалился у клиента (ответ пришёл слишком поздно). Работа произошла; caller увидел ошибку. Надёжный фикс — идемпотентность (idempotency key / безопасные ретраи) плюс deadlines, рассчитанные под реальную downstream-задержку, чтобы таймаут никогда молча не применял побочный эффект дважды." },
    { q: "Когда gRPC выигрывает у REST/JSON и почему браузер не может говорить на нём напрямую?", a: "gRPC — правильный default для внутреннего service-to-service трафика, где ты контролируешь оба конца: типизированный контракт, общий для полиглотных сервисов, выигрыш 4-10x по скорости/размеру на горячих путях, нативный streaming и распространение deadline/cancellation по графу вызовов — экономия, которая режет реальную хвостовую задержку и CPU в нагруженном mesh. Браузер не может говорить на нативном gRPC, потому что не может управлять фреймингом или трейлерами HTTP/2 из JavaScript, так что браузерному клиенту нужен grpc-web плюс транслирующий прокси (Envoy или собственный фреймворка), а grpc-web всё равно не умеет client или bidirectional streaming. (Connect-RPC обходит часть этого, отдавая unary-вызовы как обычный HTTP.) Поэтому senior-паттерн — gRPC между твоими сервисами и человекочитаемый REST/JSON или Connect-RPC на публичной, обращённой к браузеру границе, где читаемость и достижимость без прокси важнее всего." },
  ]}
/>

<Recap lang="ru">
Если ты смог восстановить каждый ответ по памяти, ты держишь спину юнита: провод несёт field tag и значения, никогда имена, поэтому переименование бесплатно, а перенумерация — порча; add-only эволюция с reserved даёт двустороннюю backward/forward совместимость; четыре формы вызовов выбираются по тому, кто стримит; deadlines распространяются и могут провалить caller'а, чья работа реально приземлилась, поэтому фикс — идемпотентность; а место gRPC — между твоими сервисами, тогда как REST/JSON или Connect-RPC остаётся на браузерной границе.
</Recap>
