---
concepts:
- field-number-contract
- schema-evolution
- http2-multiplexing
- rpc-streaming
- deadlines
- grpc-vs-rest
estMin: 13
lang: ru
level: senior
order: 7
prereqs: []
slug: quiz-choice
sources:
- https://protobuf.dev/programming-guides/proto3/
- https://grpc.io/docs/what-is-grpc/core-concepts/
status: ready
summary: "Синтез юнита gRPC/Protobuf с множественным выбором — совместимость по field tag, эволюция схемы, формы streaming, deadlines и решение gRPC-против-REST на границе."
title: 'gRPC и Protobuf: тест с множественным выбором'
track: apis
unit: 05-grpc-protobuf
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Шесть вопросов поперёк всего юнита. Каждый отражает реальное решение — изменение схемы, которое ты подписываешь, форму streaming для выбора, deadline для установки — а не определение для заучивания. На проводе едут номера, а не имена, и большинство этих ловушек рождается из того, что про это забыли.
</Hook>

<Goal lang="ru">
Убедись, что связываешь wire format Protobuf, дисциплину field tag, четыре формы вызовов, deadlines и границу gRPC-против-REST — синтез, к которому вёл урок.
</Goal>

<Quiz
  id="05-grpc-protobuf-choice-1"
  lessonSlug="05-grpc-protobuf"
  lang="ru"
  question="Коллега переименовывает поле Protobuf с discount_pct на discountPercent и тем же коммитом перенумеровывает его с 3 на 4, чтобы номера шли подряд. Какое изменение безопасно, а какое портит данные?"
  choices={[
    { label: "Переименование бесплатно (имена никогда не едут по проводу); перенумерация катастрофична — пиры, всё ещё на старой схеме, декодируют значение в то, что у них зовётся полем 4, молча портя состояние.", correct: true },
    { label: "Оба безопасны — Protobuf отслеживает поля по имени, так что переименование и перенумерация эквивалентны.", misconception: "Protobuf сериализует только field tag, wire type и значение — имена живут в .proto на этапе компиляции и не едут. Идентичность задаёт номер, поэтому перенумерация переназначает смысл на проводе." },
    { label: "Оба опасны — переименование ломает старых клиентов, потому что они ищут поля по строковому ключу.", misconception: "На проводе нет строкового ключа. Переименование меняет только локальную метку в исходниках; старые и новые бинарники прекрасно совместимы при переименовании." },
    { label: "Перенумерация нормальна, пока перекомпилируешь все сервисы; рискованное — это переименование.", misconception: "Перекомпиляция не помогает: каждая сторона компилирует самосогласованную схему, поэтому компилятор никогда не видит конфликта. Портят именно отстающие на старом номере." },
  ]}
/>

<Quiz
  id="05-grpc-protobuf-choice-2"
  lessonSlug="05-grpc-protobuf"
  lang="ru"
  question="Нужно удалить устаревшее поле с номером 3 из сообщения, живущего в production. Каков senior-ход?"
  choices={[
    { label: "Удали поле и добавь reserved 3; плюс reserved для его имени, чтобы ни номер, ни имя нельзя было вернуть будущей правкой.", correct: true },
    { label: "Просто удали строку — компилятор запомнит, что 3 ушёл, и не даст никому переиспользовать.", misconception: "Компилятор проверяет только самосогласованность текущего файла. Поздняя невинная правка может переназначить 3 новому полю, и старые клиенты, всё ещё шлющие исходное поле 3, отравят его. Именно reserved это блокирует." },
    { label: "Перенумеруй все поля после 3 на единицу вниз, чтобы нумерация шла без пробелов.", misconception: "Перенумерация — ровно та катастрофа, которой надо избегать: пиры на старой схеме декодируют значения в неверные поля. Пробелы в нумерации совершенно нормальны и ничего не стоят на проводе." },
    { label: "Смени тип поля на bytes и оставь номер, чтобы старые payload'ы ещё парсились.", misconception: "Смена типа не делает удаление безопасным и сама может сломать wire-совместимость. Проблема в идентичности номера, которую защищает только reserved." },
  ]}
/>

<Quiz
  id="05-grpc-protobuf-choice-3"
  lessonSlug="05-grpc-protobuf"
  lang="ru"
  question="Новая сборка producer'а добавляет поле 7 и шлёт его consumer'ам, всё ещё на старой схеме; тем временем старый producer вовсе опускает поле 7, общаясь с новыми consumer'ами. Что происходит в каждом направлении?"
  choices={[
    { label: "Forward compat: старые consumer'ы пропускают неизвестный номер 7 (и сохраняют его при сквозной передаче). Backward compat: новые consumer'ы видят, что поля 7 нет, и применяют default proto3 (0 / пусто). Оба направления работают — поэтому producer'ы и consumer'ы деплоятся независимо.", correct: true },
    { label: "Оба направления падают с ошибкой — Protobuf отвергает любое сообщение, чьи поля не совпадают точно с локальной схемой.", misconception: "Protobuf построен под частичные совпадения: неизвестные поля пропускаются, отсутствующие — берут default. Строгое отклонение по точному совпадению сделало бы независимый деплой невозможным." },
    { label: "Старые consumer'ы падают на неизвестном поле 7, потому что для него нет обработчика.", misconception: "Неизвестные field tag пропускаются, используя их wire type, чтобы узнать длину — и сохраняются для повторной сериализации. Никаких падений; это и есть forward compatibility." },
    { label: "Новые consumer'ы читают поле 7 как null и обязаны проверять на null; у Protobuf нет default'ов.", misconception: "У proto3 есть чётко определённые нулевые default'ы (0, пустая строка, false, пусто) для отсутствующих скалярных полей — null для них нет. Поля-сообщения — исключение, доступное через presence." },
  ]}
/>

<Quiz
  id="05-grpc-protobuf-choice-4"
  lessonSlug="05-grpc-protobuf"
  lang="ru"
  question="Дашборду нужно, чтобы сервер пушил живые обновления цены каждому клиенту, пока тот подписан, без запроса на каждое обновление. Какая форма RPC подходит и какова частая ошибка?"
  choices={[
    { label: "Server streaming — один запрос открывает поток ответов. Частая ошибка — тянуться к bidirectional streaming, когда клиент после первичной подписки больше ничего не шлёт, платя за более сложную форму, которую не используешь.", correct: true },
    { label: "Unary, опрашиваемый в цикле — вызывай GetQuote повторно по таймеру.", misconception: "Опрос возвращает накладные расходы запрос-на-обновление, которые server streaming и устраняет, и добавляет задержку, равную интервалу опроса. Server streaming пушит данные по мере готовности по одному открытому потоку." },
    { label: "Client streaming — клиент шлёт много сообщений, сервер отвечает один раз.", misconception: "Это обратное тому, что нужно. Client streaming подходит для загрузок/батчинга телеметрии; здесь поток производит сервер." },
    { label: "Bidirectional streaming — единственная форма, способная пушить от сервера.", misconception: "Server streaming уже пушит поток ответов из одного запроса. Bidi — когда обе стороны стримят независимо (чат, каналы управления); использовать его здесь — переусложнение." },
  ]}
/>

<Quiz
  id="05-grpc-protobuf-choice-5"
  lessonSlug="05-grpc-protobuf"
  lang="ru"
  question="Клиентский вызов возвращает DEADLINE_EXCEEDED, но логи сервера показывают, что обработчик завершился и закоммитил запись. Как это читать и каков надёжный фикс?"
  choices={[
    { label: "Работа произошла, но deadline истёк до прихода ответа, поэтому caller увидел ошибку для побочного эффекта, который таки случился. Фикс: сделай операцию идемпотентной (idempotency key / безопасные ретраи) и ставь deadlines с учётом реальной downstream-задержки — не считай, что ошибка означает, будто ничего не было.", correct: true },
    { label: "Невозможно — DEADLINE_EXCEEDED гарантирует, что сервер никогда не запускал обработчик.", misconception: "Deadline отменяет RPC на обоих концах, но не может откатить уже выполняющуюся на сервере работу. Исход «успех на сервере / ошибка у клиента» — классическая гонка deadline." },
    { label: "Часы сервера врут; синхронизируй NTP и ошибка уйдёт.", misconception: "Deadlines распространяются как относительная длительность в metadata, а не абсолютное wall-clock время, поэтому расхождение часов ни при чём. Причина — ответ проиграл гонку deadline." },
    { label: "Убери deadline, чтобы вызов никогда не мог истечь.", misconception: "Снятие deadlines — путь, которым mesh каскадно уходит в исчерпание ресурсов: медленные downstream'ы навечно пинят caller'ов. Фикс — идемпотентность плюс реалистичные deadlines, а не их удаление." },
  ]}
/>

<Quiz
  id="05-grpc-protobuf-choice-6"
  lessonSlug="05-grpc-protobuf"
  lang="ru"
  question="Публичный REST-эндпоинт, дёргаемый напрямую из React SPA, тормозит под нагрузкой. Коллега предлагает перевести его на нативный gRPC, чтобы браузер звал его напрямую. Каков senior-ход?"
  choices={[
    { label: "Оставь REST/JSON (или Connect-RPC) на браузерной границе и зарезервируй gRPC для внутренних service-to-service хопов. Браузеры не могут управлять фреймингом/трейлерами HTTP/2, так что нативный gRPC на границе заставляет ставить grpc-web плюс транслирующий прокси и всё равно запрещает client/bidi streaming.", correct: true },
    { label: "Переведи на нативный gRPC — браузер позовёт напрямую и получит выигрыш 4-10x.", misconception: "Браузеры вообще не могут делать нативные gRPC-вызовы. Тебя вынудят на grpc-web и хоп через прокси, ты потеряешь читаемые payload'ы в DevTools и почти ничего не выиграешь, если payload насыщен строками." },
    { label: "Переведи каждый ярус — границу и внутренний — на gRPC ради одного единого протокола.", misconception: "У границы и внутреннего яруса противоположные ограничения. Принуждение к одному протоколу тащит требование прокси и бинарную непрозрачность к браузеру, где они стоят дороже всего, ради единообразия, которое тебе не нужно." },
    { label: "Стримь сырой JSON по WebSocket и забей на схему.", misconception: "Это выбрасывает типизированный контракт и гарантии эволюции схемы, ради которых gRPC вообще существует, и заново изобретает фрейминг, deadlines и backpressure вручную — обычно хуже любого из вариантов." },
  ]}
/>

<Recap lang="ru">
Сквозная линия юнита — один факт: провод несёт field tag и значения, никогда имена. Поэтому переименование бесплатно, а перенумерация или переиспользование номера — молчаливая порча; add-only эволюция с reserved — единственный безопасный путь, и именно она даёт Protobuf двустороннюю backward/forward совместимость. Поверх мультиплексирования HTTP/2 четыре формы вызовов отражают, кто стримит (unary, server, client, bidi), deadlines распространяются и могут провалить caller'а, чья работа реально удалась, а решение на границе остаётся прежним: gRPC между твоими сервисами, REST/JSON или Connect-RPC у браузера.
</Recap>
