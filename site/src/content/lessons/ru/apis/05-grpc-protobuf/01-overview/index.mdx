---
concepts:
- protobuf-wire-format
- field-numbers
- schema-evolution
- http2-multiplexing
- rpc-streaming
- grpc-web
deepensInto: []
estMin: 17
lang: ru
lessonType: topic
level: junior
order: 1
prereqs: []
slug: 01-overview
sources:
- https://grpc.io/docs/what-is-grpc/core-concepts/
- https://protobuf.dev/programming-guides/proto3/
- https://earthly.dev/blog/backward-and-forward-compatibility/
- https://kreya.app/blog/grpc-web-deep-dive/
spiral: []
status: ready
summary: 'gRPC меняет человекочитаемый JSON на бинарный формат, привязанный к схеме:
  он в 4-10 раз быстрее и заметно компактнее, но номера полей становятся контрактом,
  который нельзя сломать, а браузер не умеет говорить на нём без прокси.'
title: 'gRPC и Protobuf: бинарный контракт и его цена'
track: apis
unit: 05-grpc-protobuf
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
Выкатили новый микросервис. В течение часа orders-сервис начинает читать неверные значения: `priority` приходит как `discount_pct`, и возвраты срабатывают на здоровых заказах. Данные никто не менял — кто-то перенумеровал поле protobuf с `4` на `3`, чтобы «прибраться в схеме». На проводе нет имён полей, только номера, поэтому каждый потребитель, всё ещё работающий на старой схеме, декодировал поле `3` не в тот слот. Proto скомпилировался чисто. Тесты прошли. Прод сорок минут молча портил состояние.
</Hook>

<Crux>Бинарный формат gRPC несёт номера полей, а не имена — поэтому номера это вечный контракт: нельзя переиспользовать или перенумеровать.</Crux>

<Explanation>

## Формат провода: номера, а не имена

Через десять минут ты поймёшь, почему «чистый» рефакторинг схемы портит продакшн-данные — и как эволюционировать контракт, от которого зависят десятки сервисов, без скоординированного передеплоя.

JSON шлёт схему с каждым сообщением. `{"userId": 42, "active": true}` повторяет ключи `userId` и `active` в каждом payload, как UTF-8 текст, на каждом запросе. Protobuf делает наоборот: сериализует только номер поля, тип на проводе и значение. То же сообщение становится примерно `08 2A 10 01` — горстка байт, где `08` кодирует «поле 1, varint», `2A` — это `42`, и так далее. Имена полей живут только в файле `.proto` на этапе компиляции; они никогда не путешествуют.

Этот один выбор даёт те самые числа, о которых думают сеньоры. Для данных с большим числом чисел protobuf примерно в **4-5 раз компактнее** JSON благодаря varint-кодированию (varint — переменная длина целого: число 1 занимает 1 байт, 300 — 2 байта) (маленькие целые стоят один байт, а не длину их десятичной строки). Сериализация примерно в **3-6 раз быстрее**, а парсинг в **5-10 раз быстрее**, потому что нет токенизации текста, нет аллокации строк-ключей, нет рефлексии — декодер идёт по известной раскладке. В Go-бенчмарках это выливается в ~5-7x быстрее round-trip при ~3x меньше аллокаций. Расплата — зеркало суперсилы JSON: **нельзя декодировать protobuf-сообщение без его схемы.** Байты сами по себе бессмысленны.

<Inset kind="why" lang="ru">
Компактность зависит от формы данных, и сеньоры обжигаются, считая её универсальной. Для payload с большим числом строк protobuf лишь на ~4% меньше JSON — строки хранятся как сырой UTF-8 в обоих, поэтому единственная экономия это отбрасывание повторяющихся имён ключей. Большие выигрыши (4-5x) идут от чисел, булевых, enum и repeated-полей. Если твой payload это в основном свободный текст, аргумент про размер почти исчезает; ты берёшь protobuf ради схемы и скорости, а не ради байт.
</Inset>

## Номера полей это контракт, навсегда

Поскольку на проводе несётся `поле 3`, а никогда `"discount_pct"`, идентичность данных задаёт номер поля, а не имя. Это полностью переворачивает привычную интуицию:

- **Переименовать поле бесплатно.** `discount_pct` → `discountPercent` ничего не меняет на проводе; старые и новые бинарники взаимодействуют идеально. Имя это локальная метка в исходнике.
- **Перенумеровать поле катастрофа.** Поменяй `3` на `4`, и каждый пир всё ещё на старой схеме читает твоё новое поле `4` в то, что *он* зовёт полем `4`, а данные твоего старого поля `3` падают туда, где *он* ждёт `3`. Это и есть баг из Hook. Компилятор его не ловит, потому что каждая сторона компилирует самосогласованную схему.
- **Переиспользовать удалённый номер та же катастрофа, отложенная.** Удали поле `3`, потом позже добавь новое несвязанное поле и присвой ему `3` — теперь старые клиенты, шлющие исходное поле `3`, отравляют новое.

Дисциплина, которая всё это предотвращает, — **add-only эволюция плюс `reserved`**. Ты никогда не меняешь и не переиспользуешь номер; только дописываешь новые поля с новыми номерами, а удаляя поле, резервируешь его номер (и имя), чтобы никто не смог его вернуть:

```proto
message Order {
  reserved 3;                  // discount_pct, удалено 2026-Q1 — не переиспользовать
  reserved "discount_pct";
  string id = 1;
  int64 amount_cents = 2;
  Priority priority = 4;       // безопасно: дописано со свежим номером
}
```

Сделанная так схема одновременно **обратно совместима** (новый код читает сообщения, записанные старым: отсутствующие новые поля просто берут дефолты proto3 — `0`, `""`, пусто) и **прямо совместима** (старый код читает сообщения нового: неизвестные номера полей пропускаются и сохраняются, а не вызывают ошибку). Эта двусторонняя совместимость и есть вся причина, почему большие полиглот-системы могут деплоить продюсеров и консьюмеров независимо.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Изменение схемы</th>
        <th class="border border-border px-3 py-2 text-left">Безопасно?</th>
        <th class="border border-border px-3 py-2 text-left">Почему (причина на уровне провода)</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">Добавить новое поле с новым номером</td>
        <td class="border border-border px-3 py-2">Да</td>
        <td class="border border-border px-3 py-2">Старые пиры пропускают неизвестный номер; новые поля дефолтятся у старых писателей</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Переименовать поле (тот же номер)</td>
        <td class="border border-border px-3 py-2">Да</td>
        <td class="border border-border px-3 py-2">Имена не путешествуют; на проводе только номер</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Удалить поле + <code>reserved</code> его номер</td>
        <td class="border border-border px-3 py-2">Да</td>
        <td class="border border-border px-3 py-2">Reserve блокирует будущее переиспользование, номер нельзя отравить</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Сменить номер поля</td>
        <td class="border border-border px-3 py-2">Нет</td>
        <td class="border border-border px-3 py-2">Пиры декодируют значение не в то поле — тихая порча</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Переиспользовать удалённый номер под новое поле</td>
        <td class="border border-border px-3 py-2">Нет</td>
        <td class="border border-border px-3 py-2">Отстающие на старой схеме пишут туда старый смысл</td>
      </tr>
    </tbody>
  </table>
</div>

## Одно соединение, четыре формы вызова

Спроси себя: сколько одновременных gRPC-вызовов обслуживает твой mesh до того, как само число соединений становится проблемой? Именно здесь мультиплексирование HTTP/2 оправдывает себя.

gRPC работает поверх HTTP/2, и это даёт **мультиплексирование**: множество одновременных RPC переплетаются как независимые стримы по одному TCP-соединению, без head-of-line blocking на уровне HTTP — 50 одновременных вызовов нужны одно соединение, а не 50. Отменённый или истёкший по дедлайну вызов шлёт `RST_STREAM`, который убивает только этот стрим и оставляет остальные нетронутыми. Поэтому пропускная способность gRPC в service mesh держится под конкурентностью там, где модель «соединение на запрос» у HTTP/1.1 рушится.

Поверх этого одного соединения блок `service` в схеме определяет четыре формы вызова, и выбор не той — частый запах дизайна:

1. **Unary** — один запрос, один ответ. REST-образный случай на 95%.
2. **Server streaming** — один запрос, стрим ответов (поток цен, хвост лога, прогресс с пушем от сервера).
3. **Client streaming** — стрим запросов, один ответ (загрузка чанков, батчинг телеметрии).
4. **Bidirectional streaming** — обе стороны стримят независимо в одном вызове (чат, живая коллаборация, долгоживущий канал управления).

Вместе эти четыре формы означают, что ты подбираешь паттерн коммуникации под поток данных, а не наоборот. Когда ты видишь unary-вызов в плотном цикле, опрашивающий живой фид, — это сигнал взять server streaming.

```proto
service PriceService {
  rpc GetQuote(QuoteRequest) returns (Quote);                  // унарный
  rpc WatchPrices(WatchRequest) returns (stream Quote);        // серверный стриминг
  rpc UploadTrades(stream Trade) returns (UploadSummary);      // клиентский стриминг
  rpc Trade(stream Order) returns (stream Fill);               // двунаправленный стриминг
}
```

Вместе с этим едут две операционные фичи, которые легко пропустить, пока они не укусят. **Дедлайны**: клиент задаёт таймаут, который распространяется вниз по цепочке вызовов; когда он истекает, RPC авто-отменяется на *обоих* концах, и медленный downstream не может вечно держать серверные потоки. Классический сбивающий с толку отказ — вызов с deadline-exceeded, который *успел* на сервере («я отправил все ответы»), но *упал* на клиенте («они пришли слишком поздно») — работа сделана, а вызывающий увидел ошибку. **Отмена**: любая сторона может прервать вызов на лету, и проброшенная отмена останавливает downstream-работу вместо того, чтобы дать осиротевшим запросам молоть впустую. Пропуск дедлайнов — самая частая причина, почему gRPC-mesh каскадно уходит в исчерпание ресурсов при частичном отказе.

## Где gRPC выигрывает, а где больно

gRPC — верный дефолт для **внутреннего трафика между сервисами**: типизированный контракт, разделяемый полиглот-сервисами, выигрыши в 4-10x по скорости и размеру на горячих путях, нативный стриминг и проброс дедлайнов по графу вызовов. В нагруженном mesh эта экономия не академическая — она режет хвостовую latency и CPU на пути сериализации, который REST тратит на JSON.

Боль реальна и приходит в двух местах. Во-первых, **браузер не умеет говорить на gRPC.** Браузеры не могут управлять HTTP/2-фреймингом или трейлерами, поэтому нужен **grpc-web** плюс прокси (Envoy или собственный прокси фреймворка), чтобы транслировать между браузером и gRPC-бэкендом — лишняя инфраструктура, и grpc-web всё ещё не умеет client- или bidirectional-стриминг. (Connect-RPC обходит часть этого, отдавая unary-вызовы как обычный HTTP.) Во-вторых, **отлаживаемость рушится.** Бинарный payload нечитаем во вкладке Network в Chrome — ты видишь «передано байт», а не поля. `curl` бесполезен без схемы и декодера; трейлеры прячутся внутри байт тела ответа, где у DevTools нет хука. Логи, реплей запросов и ad-hoc осмотр — всё требует дополнительного тулинга. Эта непрозрачность и есть налог за компактность — то самое свойство, что делает провод маленьким, делает его непостижимым для людей.

<TradeoffMatrix
  id="01-overview-tradeoff-1"
  lessonSlug="01-overview"
  lang="ru"
  prompt="Публичный REST-эндпоинт, в который React-приложение бьёт напрямую, тормозит под нагрузкой. Коллега предлагает перевести его на gRPC. Выбери ход."
  options={[
    { name: "Оставить REST/JSON (или взять Connect-RPC) на браузерном крае; gRPC оставить для внутренних хопов между сервисами", summary: "gRPC за краем.", correct: true, justification: "Браузер не умеет говорить на нативном gRPC — он не управляет HTTP/2-фреймингом и трейлерами — поэтому gRPC на публичном крае вынуждает grpc-web плюс транслирующий прокси (Envoy), добавляет инфру и всё равно запрещает client/bidi-стриминг. Настоящие выигрыши (4-10x скорость/размер, проброс дедлайнов, стриминг) приходят на внутренних хопах между твоими сервисами. Держи человекочитаемый REST/JSON или Connect-RPC на крае, где важны отлаживаемость и доступ без прокси, а gRPC ставь туда, где трафик плотный и машина-машина." },
    { name: "Перевести публичный эндпоинт на нативный gRPC и пусть React-приложение зовёт его напрямую", summary: "gRPC на крае.", justification: "Браузеры вообще не могут делать нативные gRPC-вызовы, поэтому «напрямую» невозможно. Тебя вынудят в grpc-web и прокси-хоп, ты потеряешь читаемые payload в DevTools и получишь лишь маргинальную выгоду по размеру, если payload строковый — сломав при этом простейший путь отладки, что у тебя был." },
    { name: "Перевести всё — край и внутрянку — на gRPC ради одного консистентного протокола", summary: "gRPC везде.", justification: "Консистентность заманчива, но край и внутренние уровни имеют противоположные ограничения. Навязывание одного протокола тащит требование прокси и бинарную непрозрачность до самого браузера, где они стоят дороже всего, ради единообразия, которое тебе на деле не нужно." },
    { name: "Выкинуть схему вовсе и стримить сырой JSON по WebSocket", summary: "Ad-hoc стриминг.", justification: "Это выбрасывает типизированный контракт и гарантии эволюции схемы, которые и есть весь смысл gRPC, и переизобретает фрейминг, дедлайны и backpressure руками — обычно хуже, чем и REST, и gRPC." },
  ]}
/>

<Quiz
  id="01-overview-quiz-1"
  lessonSlug="01-overview"
  lang="ru"
  question="Поле discount_pct = 3 удаляют из proto. Какой ход сеньора, чтобы схема осталась безопасной?"
  choices={[
    { label: "Пометить номер reserved 3; (и зарезервировать имя), чтобы его нельзя было переиспользовать", correct: true },
    { label: "Просто удалить строку — компилятор это отследит", misconception: "Компилятор проверяет только текущий файл. Поздняя правка может невинно переназначить 3 новому полю, а старые клиенты, всё ещё шлющие исходное поле 3, отравят его. Именно reserved это блокирует." },
    { label: "Перенумеровать оставшиеся поля, чтобы закрыть дыру от 3", misconception: "Перенумерация — ровно та катастрофа, которой надо избегать: пиры на старой схеме декодируют значения не в те поля. Дыры в нумерации совершенно нормальны." },
    { label: "Сменить его тип на bytes, чтобы старые данные ещё влезали", misconception: "Жонглирование типами не делает удаление безопасным и рискует порчей данных. Проблема в идентичности номера поля, которую защищает только reserved." },
  ]}
/>

<Quiz
  id="01-overview-quiz-2"
  lessonSlug="01-overview"
  lang="ru"
  question="Почему нельзя вставить захваченный gRPC-запрос в инструмент и прочитать его как тело JSON?"
  choices={[
    { label: "Формат провода несёт только номера полей, типы на проводе и значения — имена живут в .proto, так что для декода нужна схема", correct: true },
    { label: "gRPC по умолчанию шифрует payload end-to-end", misconception: "TLS шифрует транспорт, но нечитаемость не из-за этого — даже расшифрованные байты это бессхемные номера полей, а не текст." },
    { label: "Payload сжат gzip и его просто надо распаковать", misconception: "Сжатие опционально и ортогонально; ключевая причина в том, что protobuf никогда не кладёт имена полей на провод." },
    { label: "Браузеры срезают тело до того, как его можно захватить", misconception: "Тело на месте; это бинарный protobuf-фрейминг, который DevTools не может интерпретировать как поля без схемы." },
  ]}
/>

<DragOrder
  id="01-overview-drag-1"
  lessonSlug="01-overview"
  lang="ru"
  prompt="Расставь безопасные шаги эволюции proto-сообщения в проде:"
  items={[
    { id: "a", label: "Никогда не меняй и не переиспользуй существующий номер поля — номер это идентичность на проводе" },
    { id: "b", label: "Чтобы удалить поле, удали его и добавь reserved для его номера и имени" },
    { id: "c", label: "Чтобы добавить данные, допиши новое поле со свежим, ранее не использованным номером" },
    { id: "d", label: "Полагайся на дефолты proto3, чтобы старые читатели терпели отсутствие новых полей (обратная совм.)" },
    { id: "e", label: "Полагайся на пропуск неизвестных полей, чтобы старые читатели терпели новые поля (прямая совм.)" },
  ]}
  correctOrder={["a", "b", "c", "d", "e"]}
/>

</Explanation>

<FlowDiagram
  label=".proto контракт → codegen → провод"
  hue="var(--d-backend)"
  perRow={2}
  nodes={[
    { id: "proto", label: ".proto контракт", sub: "номера полей", col: 0, row: 0 },
    { id: "stub", label: "Client stub", sub: "сгенерирован", col: 0, row: 1 },
    { id: "skel", label: "Server skeleton", sub: "сгенерирован", col: 1, row: 1 },
    { id: "wire", label: "Бинарные фреймы", sub: "поверх HTTP/2", col: 0, row: 2 },
  ]}
  edges={[
    { from: "proto", to: "stub", label: "codegen" },
    { from: "proto", to: "skel", label: "codegen" },
    { from: "stub", to: "wire", label: "запрос" },
    { from: "wire", to: "skel", label: "декод" },
  ]}
  caption="Один .proto генерит и stub, и skeleton; вызов идёт схемо-связанными бинарными фреймами поверх мультиплексированного HTTP/2 — имена на провод не попадают."
/>

<KeyTakeaway>Protobuf несёт на проводе номера полей, не имена — номера это вечный контракт: только дописывай, резервируй удаления, не перенумеровывай. Плюс 4-10x скорость/размер и стриминг, минус нечитаемый payload.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="01-overview-retrieval"
  lang="ru"
  questions={[
    { q: "Объясни, почему переименовать поле protobuf безопасно, а перенумеровать его — катастрофа продакшен-уровня.", a: "Protobuf никогда не сериализует имена полей — они существуют только в .proto на этапе компиляции. На проводе каждое значение помечено номером поля и типом на проводе, и ничем больше. Поэтому переименование discount_pct в discountPercent меняет лишь локальную метку в исходнике; номер тот же, байты идентичны, и старые и новые бинарники взаимодействуют идеально. Перенумерация — наоборот. Если сменить поле 3 на поле 4, каждый пир всё ещё на старой схеме декодирует твоё новое поле 4 в то, что он зовёт полем 4, а любые данные, отправленные как старое поле 3, падают туда, где он ждёт поле 3. Каждая сторона скомпилировала самосогласованную схему, поэтому компилятор это не ловит и тесты с обеих сторон проходят — но в проде значения молча встают не в те поля и портят состояние. Та же катастрофа происходит, отложенно, если переиспользовать удалённый номер. Защита — add-only эволюция: никогда не меняй и не переиспользуй номер, дописывай новые поля со свежими номерами и резервируй номера (и имена) удалённых полей, чтобы их нельзя было вернуть." },
    { q: "Когда выбрать gRPC вместо REST/JSON, и какие конкретные издержки тянет это решение?", a: "Выбирай gRPC для внутреннего трафика между сервисами, где ты контролируешь оба конца: ты получаешь типизированный контракт, разделяемый полиглот-сервисами, в 4-10 раз более быструю сериализацию и в 4-5 раз меньшие payload на данных с большим числом чисел, нативный стриминг (server, client, bidirectional) по одному мультиплексированному HTTP/2-соединению, и проброс дедлайнов и отмены, который не даёт медленным downstream держать ресурсы по всему графу вызовов. Издержки реальны и приходят в двух местах. Браузер не умеет говорить на нативном gRPC, потому что не управляет HTTP/2-фреймингом и трейлерами, поэтому любому браузерному клиенту нужны grpc-web плюс транслирующий прокси вроде Envoy — лишняя инфраструктура, и grpc-web всё ещё не умеет client- или bidirectional-стриминг. И отлаживаемость рушится: бинарный payload показывается в DevTools как сырые байты, curl бесполезен без схемы и декодера, а трейлеры прячутся в теле ответа, где у тулинга нет хука. Поэтому паттерн сеньора — gRPC между своими сервисами и человекочитаемый REST/JSON (или Connect-RPC) на публичном, обращённом к браузеру крае." },
  ]}
/>

<Recap lang="ru">
Определяющий выбор gRPC — формат провода protobuf: он сериализует номер поля, тип на проводе и значение, но никогда имена полей — те живут только в .proto. Это делает payload в 4-5 раз компактнее и в 4-10 раз быстрее в (де)сериализации, чем JSON, для числовых данных, но это же значит, что идентичность данных задаёт номер поля, а не имя. Поэтому переименование бесплатно, перенумерация или переиспользование удалённого номера молча портит каждого пира на другой схеме, и единственная безопасная эволюция — add-only с reserved-метками для удалений — что и даёт protobuf его двустороннюю обратную/прямую совместимость. Поверх HTTP/2 gRPC мультиплексирует множество RPC на одном соединении и предлагает четыре формы вызова (unary, server, client, bidirectional streaming) плюс дедлайны и отмену, которые пробрасываются вниз по графу вызовов. Цена в том, что браузерам нужны grpc-web и прокси, а бинарный payload непрозрачен для людей и стандартного тулинга. Паттерн сеньора: gRPC для внутренних хопов между сервисами, REST/JSON или Connect-RPC на браузерном крае. Теперь, когда встретишь PR с перенумерацией поля «для порядка», знаешь, что делать: отклони и попроси reserved вместо этого.
</Recap>
