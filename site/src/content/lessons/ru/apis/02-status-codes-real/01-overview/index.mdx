---
concepts:
- status-code-classes
- 200-error-body-antipattern
- retry-idempotency
- retry-after-429
- 5xx-fault-attribution
- validation-409-422
deepensInto: []
estMin: 16
lang: ru
lessonType: topic
level: junior
order: 1
prereqs: []
slug: 01-overview
sources:
- https://datatracker.ietf.org/doc/html/rfc9110
- https://developer.mozilla.org/en-US/docs/Web/HTTP/Reference/Status
- https://docs.cloud.google.com/storage/docs/retry-strategy
- https://docs.stripe.com/error-low-level
spiral: []
status: ready
summary: 'Статус-код — это контракт, который читают три машины раньше любого человека:
  кэши, дашборды и логика ретраев. Ошибиться классом — или спрятать ошибку в теле
  200 — значит сломать все три сразу.'
title: Статус-коды, которые реально важны в проде
track: apis
unit: 02-status-codes-real
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
Платёжный шлюз начал падать на апстриме во время пятничного деплоя. Все дашборды оставались зелёными: success rate 100%, error rate 0%, ни одного алерта. Дежурный инженер узнал об инциденте из твитов клиентов, а не из мониторинга. Причина — одна строка: шлюз ловил падения апстрима и возвращал `200 OK` с `{ "error": "upstream timeout" }` в теле. Метрики считали HTTP-статус, поэтому полный отказ сорок минут выглядел как идеальное здоровье.
</Hook>

<Crux>Статус-код читают кэши, дашборды и логика ретраев раньше любого человека — спрятать ошибку в теле 200 значит сломать все три.</Crux>

<Explanation>

## Классы — это решение о маршрутизации, а не зубрёжка

Запомнить, что 404 — это «не найдено», уровень джуна. Взгляд сеньора: *первая цифра* — это инструкция маршрутизации для машин, которые никогда не читают тело ответа: кэш, балансировщик, APM-агент, цикл ретраев клиента. Они ветвятся по классу, и ветвятся сразу.

- **2xx** — сработало; ответ кэшируем по своим заголовкам, считай успехом.
- **3xx** — иди в другое место; следуй `Location` или используй кэш.
- **4xx** — *ты*, клиент, прислал что-то не то; не ретрай тот же запрос, он упадёт точно так же.
- **5xx** — *я*, сервер, упал; запрос мог быть валидным, поэтому ретрай может сработать.

Раздел 4xx/5xx — несущий. Это разница между «стоп, ты сломал» и «подожди, я сломал, попробуй снова». Ошибись классом — и каждая машина ниже по потоку примет неверное решение: кэш сохранит отказ, цикл ретраев будет долбить запрос, который никогда не пройдёт, дашборд просчитается с инцидентом.

## 2xx — не одно и то же: 200 vs 201 vs 202 vs 204

Класс успеха несёт реальную информацию, которой сеньор формирует контракт API.

- **`200 OK`** — готово, вот результат. Синхронно, полностью.
- **`201 Created`** — теперь существует ресурс; верни заголовок `Location` с указанием на него. Это правильный ответ на успешный `POST /orders`, а не голый 200.
- **`202 Accepted`** — «я принял твой запрос, но он ещё не выполнен». Работа в очереди или асинхронна. Тело должно вернуть способ опросить статус. Вернуть тут 200 — ложь: клиент думает, что заказ ушёл, а он ещё лежит в очереди.
- **`204 No Content`** — успех, и сознательно нечего возвращать (`DELETE` или `PUT`, который не изменил ничего, что клиенту надо отразить). Экономит раунд-трип на парсинг пустого тела.

Различие 202-vs-200 — там, где текут асинхронные системы. Если эндпоинт ставит работу в Kafka и возвращается сразу, 202 говорит клиенту «опрашивай меня». 200 говорит «мы закончили» — и теперь его UI показывает подтверждённый заказ, которого ещё нигде нет.

## Словарь 4xx: 400 vs 422 vs 409 vs 412 и 401 vs 403

Здесь большинство API неряшливы — и здесь точный клиент может дать полезное сообщение об ошибке вместо общего «что-то пошло не так».

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Код</th>
        <th class="border border-border px-3 py-2 text-left">Значит</th>
        <th class="border border-border px-3 py-2 text-left">Что должен сделать клиент</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2"><code>400</code> Bad Request</td>
        <td class="border border-border px-3 py-2">Малформед — битый JSON, нет обязательного поля, не парсится</td>
        <td class="border border-border px-3 py-2">Исправь <em>структуру</em> запроса; не ретрай как есть</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2"><code>422</code> Unprocessable</td>
        <td class="border border-border px-3 py-2">Распарсилось, но значения нарушают бизнес-правила (битый email, qty &lt; 0)</td>
        <td class="border border-border px-3 py-2">Исправь <em>значения</em>; покажи ошибки по полям</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2"><code>409</code> Conflict</td>
        <td class="border border-border px-3 py-2">Конфликт с текущим состоянием ресурса (дубль, гонка версий)</td>
        <td class="border border-border px-3 py-2">Перечитай состояние, разреши, переотправь</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2"><code>412</code> Precondition Failed</td>
        <td class="border border-border px-3 py-2"><code>If-Match</code>/<code>If-Unmodified-Since</code> дали ложь</td>
        <td class="border border-border px-3 py-2">Перефетчи сущность (<code>ETag</code> сдвинулся); ретрай с новым precondition</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2"><code>401</code> Unauthorized</td>
        <td class="border border-border px-3 py-2">Не аутентифицирован — нет/протух/невалиден креденшл</td>
        <td class="border border-border px-3 py-2">Залогинься или обнови токен, затем ретрай</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2"><code>403</code> Forbidden</td>
        <td class="border border-border px-3 py-2">Аутентифицирован, но делать это не разрешено</td>
        <td class="border border-border px-3 py-2">Стоп — смена креденшла не поможет</td>
      </tr>
    </tbody>
  </table>
</div>

RFC 9110 проводит 400 как синтаксис, а 422 как семантику: 400 — «я не смог это распарсить», 422 — «я распарсил, и значения неверны». Граница размытая, и многие API сваливают оба в 400, но различие стоит держать — оно говорит клиенту, чинить форму или данные, а это разница между ошибкой разработчика и ошибкой пользователя.

Пару `401` vs `403` постоянно путают. `401` значит «я не знаю, кто ты» — фикс в аутентификации. `403` значит «я точно знаю, кто ты, и тебе всё равно нельзя» — обновление токена не поможет. Вернуть `401` на отказ авторизации загоняет клиентов в бессмысленный цикл релогина.

<Inset kind="why" lang="ru">
Есть нюанс безопасности на `404` vs `403`. Строгий `403 Forbidden` на записи, которую пользователю видеть не положено, подтверждает, что запись *существует*, — это утечка информации. Для чувствительных ресурсов (чужой счёт по id) сеньоры часто возвращают `404 Not Found`, чтобы атакующий, перебирающий id, не отличал «такой записи нет» от «есть, но не твоя». Ты меняешь корректность REST на то, что не утекает факт существования.
</Inset>

## 5xx: чья вина и что клиент делает дальше

Класс 5xx — там, где живёт стратегия ретраев, а под-коды распределяют вину.

- **`500` Internal Server Error** — твоё приложение бросило исключение и не обработало. Баг в твоём коде.
- **`502` Bad Gateway** — прокси/балансировщик получил малформед или ничего от апстрима, который вызвал. Сам шлюз цел; что-то за ним сломано.
- **`503` Service Unavailable** — сервер сознательно не обслуживает: перегружен, дренится, на обслуживании. Это *единственный* 5xx, который должен нести заголовок `Retry-After`.
- **`504` Gateway Timeout** — шлюз ждал апстрим и сдался. Запрос мог завершиться на бэкенде, хотя клиент увидел таймаут, — именно поэтому слепой ретрай неидемпотентного вызова тут опасен.

Рефлекс сеньора: `5xx` *ретраебельно* (запрос мог быть валидным, а сбой временным), `4xx` *нет* (запрос неверен и упадёт точно так же) — с `429` как явным ретраебельным исключением в диапазоне 4xx.

## 429 и Retry-After: бэкофф, которым управляет сервер

`429 Too Many Requests` говорит, что ты упёрся в rate limit. Правильный ответ несёт заголовок `Retry-After` — либо число секунд (`Retry-After: 30`), либо HTTP-дату. Сервер знает своё окно лимита; уважай его. Откатывайся к клиентскому экспоненциальному бэкоффу с джиттером (1с, 2с, 4с, 8с…) только когда `Retry-After` нет. Google Cloud Storage, OpenAI и Stripe документируют ровно этот порядок: сначала читай `Retry-After`, потом бэкофф с джиттером.

Правило ретраев, которое всё связывает — и которое вызывает реальные инциденты при нарушении: **ретрай `5xx` и `429`; никогда слепо не ретрай неидемпотентный `4xx` или неидемпотентный запрос после таймаута.** `GET`, `PUT`, `DELETE` идемпотентны — ретрай безопасен. `POST` — нет. Если `POST /charge` ловит таймаут (`504`) и клиент ретраит, списание может пройти дважды. Фикс — ключ идемпотентности: Stripe и Square требуют от клиентов слать уникальный ключ на логическую операцию, чтобы сервер дедуплицировал ретраи вместо двойного списания.

<TradeoffMatrix
  id="02-status-codes-real-tradeoff-1"
  lessonSlug="01-overview"
  lang="ru"
  prompt="Клиент делает POST /charge, запрос ловит таймаут 504, ключ идемпотентности не отправлен. Списание могло пройти, а могло и нет. Что делает корректный клиент?"
  options={[
    { name: "Не авторетраить; вывести состояние «статус платежа неизвестен» и сверить, запросив сам charge", summary: "Считать неоднозначным.", correct: true, justification: "504 на неидемпотентном POST без ключа идемпотентности по-настоящему неоднозначен — бэкенд мог завершить списание до того, как шлюз сдался. Слепой ретрай рискует двойным списанием; притвориться, что упало, рискует потерянным платежом. Безопасно — не ретраить запись, пометить операцию «неизвестно» и сверить, прочитав ресурс (или дождавшись webhook). Реальный фикс выше по потоку: в следующий раз слать ключ идемпотентности, чтобы ретраи были безопасны." },
    { name: "Сразу ретраить POST, потому что 504 — это 5xx, а 5xx ретраебельно", summary: "Ретрай на 5xx.", justification: "Правило «5xx ретраебельно» предполагает идемпотентность. POST не идемпотентен, а 504 как раз значит, что бэкенд мог завершить работу до таймаута. Ретрай без ключа идемпотентности — это ровно как «ретраебельный код на неидемпотентном запросе» превращается в двойное списание — классический инцидент." },
    { name: "Считать 504 отказом и сказать пользователю, что платёж не прошёл", summary: "Считать отказом.", justification: "504 не значит, что работа упала, — значит, что шлюз перестал ждать. Списание могло пройти. Сказать пользователю, что упало, может породить дубль ручного платежа плюс реальное списание. Состояние неизвестно, а не «упало»." },
    { name: "Ретраить один раз с тем же телом, потом сдаться, если снова 504", summary: "Ретрай-один-раз.", justification: "Без ключа идемпотентности даже один ретрай может списать дважды, если первая попытка реально прошла. Проблема не в числе ретраев, а в отсутствующем ключе идемпотентности. Почини ключ — и ретраи станут безопасны." },
  ]}
/>

<Quiz
  id="02-status-codes-real-quiz-1"
  lessonSlug="01-overview"
  lang="ru"
  question="Эндпоинт ставит работу в Kafka и возвращается до её завершения. Что он должен вернуть?"
  choices={[
    { label: "202 Accepted, со способом опросить статус в теле", correct: true },
    { label: "200 OK", misconception: "200 говорит, что работа завершена и синхронна. Если она в очереди, UI клиента покажет завершённую операцию, которой ещё нигде нет, — ложь sync-vs-async." },
    { label: "201 Created", misconception: "201 утверждает, что ресурс теперь существует. Если воркер не отработал, ничего ещё не создано; 202 («принято, не готово») — честный ответ." },
    { label: "204 No Content", misconception: "204 значит успех с сознательно пустым телом. Здесь нужно вернуть status/poll URL, и работа даже не закончена — подходит 202." },
  ]}
/>

<Quiz
  id="02-status-codes-real-quiz-2"
  lessonSlug="01-overview"
  lang="ru"
  question="Пользователь залогинен, но пытается удалить запись чужого тенанта. Какой статус корректен?"
  choices={[
    { label: "403 Forbidden — аутентифицирован, но не разрешено", correct: true },
    { label: "401 Unauthorized", misconception: "401 значит «я не знаю, кто ты». Пользователь аутентифицирован; обновление токена ничего не меняет. Это 403, а не 401." },
    { label: "400 Bad Request", misconception: "Запрос корректно сформирован; проблема в правах, а не в синтаксисе. 400 сказал бы клиенту чинить форму запроса, что здесь неверно." },
    { label: "404 Not Found — но только сознательно, чтобы не утечь факт существования", misconception: "Это реальный выбор сеньора для чувствительных ресурсов, но это компромисс безопасности (скрыть существование), а не дефолтный семантический ответ. Прямо-корректный код для «известный пользователь, не разрешено» — 403." },
  ]}
/>

<DragOrder
  id="02-status-codes-real-drag-1"
  lessonSlug="01-overview"
  lang="ru"
  prompt="Клиент получил ответ с ошибкой. Расставь решения, определяющие, ретраить ли и как:"
  items={[
    { id: "a", label: "Прочитай класс статуса: это 4xx (ошибка клиента) или 5xx/429 (ретраебельно)?" },
    { id: "b", label: "Если 4xx и не 429: не ретрай — запрос неверен, упадёт точно так же" },
    { id: "c", label: "Если 5xx или 429: проверь, идемпотентен ли запрос или несёт ли ключ идемпотентности" },
    { id: "d", label: "Если идемпотентен/с ключом: уважай Retry-After если есть, иначе экспоненциальный бэкофф с джиттером" },
    { id: "e", label: "Если неидемпотентен и без ключа (напр. POST после 504): не ретрай слепо — сверяйся вместо этого" },
  ]}
  correctOrder={["a", "b", "c", "d", "e"]}
/>

</Explanation>

<FlowDiagram
  label="Первая цифра маршрутизирует поведение машин"
  hue="var(--d-backend)"
  perRow={4}
  nodes={[
    { id: "resp", label: "Ответ", sub: "статус-код", col: 0, row: 0 },
    { id: "c2", label: "2xx", sub: "сработало", col: 0, row: 1 },
    { id: "c4", label: "4xx", sub: "ты сломал", col: 1, row: 1 },
    { id: "c429", label: "429", sub: "rate-limit", col: 2, row: 1 },
    { id: "c5", label: "5xx", sub: "я сломал", col: 3, row: 1 },
    { id: "a2", label: "Кэш / успех", col: 0, row: 2 },
    { id: "a4", label: "Стоп — без ретрая", col: 1, row: 2 },
    { id: "a429", label: "Бэкофф, Retry-After", col: 2, row: 2 },
    { id: "a5", label: "Ретрай, если идемпотент", col: 3, row: 2 },
  ]}
  edges={[
    { from: "resp", to: "c2" },
    { from: "resp", to: "c4" },
    { from: "resp", to: "c429" },
    { from: "resp", to: "c5" },
    { from: "c2", to: "a2" },
    { from: "c4", to: "a4" },
    { from: "c429", to: "a429" },
    { from: "c5", to: "a5" },
  ]}
  caption="Кэши, дашборды и циклы ретраев ветвятся по классу раньше, чем человек прочитает тело — 4xx значит стоп, 5xx/429 значит можно ретрай."
/>

<KeyTakeaway>Первая цифра — машинная инструкция: 4xx = ты сломал, стоп; 5xx/429 = я сломал, можно ретрай. Не тоннелируй ошибки через тело 200 и не ретрай слепо неидемпотентный запрос.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="02-status-codes-real-retrieval"
  lang="ru"
  questions={[
    { q: "Объясни, почему возвращать 200 OK с объектом ошибки в теле опасно и что это ломает ниже по потоку.", a: "Статус-код читают машины, которые никогда не смотрят в твоё тело: кэши, балансировщики, APM/мониторинг-агенты и циклы ретраев клиента — все ветвятся по классу статуса. Когда ты возвращаешь 200 на отказ, каждый из них введён в заблуждение. Кэш может сохранить упавший ответ и отдавать его другим пользователям как валидный. Мониторинг считает запрос успехом, поэтому дашборды error-rate остаются зелёными во время реального инцидента — случай со шлюзом, где сорок минут отдавался 100% success при полном отказе, каноничен. Логика ретраев, которая ориентируется на 5xx/429, никогда не срабатывает, потому что увидела 2xx, поэтому временные сбои, которые ретрай бы починил, доходят до пользователя как поломка. И клиенты теперь обязаны парсить каждое тело, чтобы понять, был ли «успех» успехом, что вяжет их с твоим конвертом ошибок. Фикс — заставить статус-код говорить правду: 4xx для ошибок клиента, 5xx для ошибок сервера, а человекочитаемые детали класть в тело этого корректного статуса." },
    { q: "Пройди по решению о ретрае упавшего запроса, включая ловушку идемпотентности.", a: "Старт от класса. 4xx (кроме 429) значит, что сам запрос неверен — ретрай шлёт идентичные байты и получает идентичный отказ, поэтому не ретрай; чини запрос. 5xx или 429 значит, что сервер упал или зарейтлимитил тебя, а запрос мог быть валидным, так что ретрай может пройти. Но «ретраебельно» предполагает идемпотентность. GET, PUT и DELETE идемпотентны: выполнить их дважды — тот же эффект, что и один раз, поэтому ретрай безопасен. POST — нет: ретрай может создать второй ресурс или списать карту дважды. Опасный случай — 504 (gateway timeout) на POST: бэкенд мог завершить работу до того, как шлюз сдался, поэтому результат по-настоящему неизвестен. Слепой ретрай рискует дублем; корректный ход — не ретраить запись и свериться, прочитав состояние, или слать ключ идемпотентности (как требуют Stripe и Square), чтобы сервер дедуплицировал ретраи. Когда ретраишь — уважай Retry-After, если сервер его прислал (особенно на 429 и 503); иначе экспоненциальный бэкофф с джиттером, чтобы флот клиентов не синхронизировался в thundering herd." },
  ]}
/>

<Recap lang="ru">
Статус-код — это контракт, который три машины читают раньше любого человека: кэши, мониторинг и логика ретраев, — а первая цифра — инструкция, по которой они ветвятся. 2xx не монолитен: 201 с Location для создания, 202 для принятой-но-асинхронной работы, 204 для сознательно пустого успеха. Словарь 4xx несёт реальный смысл, которым пользуется точный клиент — 400 для малформед-синтаксиса, 422 для значений, нарушающих бизнес-правила, 409 для конфликтов состояния, 412 для проваленных preconditions, 401 для «кто ты» против 403 для «не разрешено», с 404, иногда подставляемым вместо 403, чтобы не утечь факт существования ресурса. Коды 5xx распределяют вину — 500 твой баг, 502 плохой апстрим, 503 сознательная недоступность, которая должна нести Retry-After, 504 таймаут, где работа могла втайне завершиться. Ретрай 5xx и 429; никогда слепо не ретрай неидемпотентный 4xx или неидемпотентный запрос после таймаута, и берись за ключ идемпотентности, чтобы ретраи не списали дважды. Кардинальный грех — тоннелировать ошибку через тело 200: это ослепляет дашборды, отравляет кэши и глушит ретраи, которые спасли бы запрос.
</Recap>
