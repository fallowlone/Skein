---
concepts:
- error-body-truth
- 401-vs-403
- 503-retry-after
- precondition-412
- idempotency-retry
estMin: 14
lang: ru
level: senior
order: 9
prereqs: []
slug: quiz-code
sources:
- https://datatracker.ietf.org/doc/html/rfc9110
- https://developer.mozilla.org/en-US/docs/Web/HTTP/Reference/Status
status: ready
summary: "Читай реальные HTTP-обмены — статус-строку, заголовки и тело вместе — и определяй, что ответ на самом деле говорит, и фикс с наибольшим рычагом."
title: 'Статус-коды: чтение HTTP-обменов'
track: apis
unit: 02-status-codes-real
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Статус-коды диагностируют на проводе, в сыром обмене. Читай каждую пару запрос/ответ — строку, заголовки, тело — и решай, что она реально говорит и что сеньор поменял бы первым.
</Hook>

<Goal lang="ru">
Отработай цикл чтения на проводе: замечай, когда статус и тело противоречат друг другу, когда отсутствует заголовок и когда код приписывает вину не той стороне — и называй фикс.
</Goal>

## Обмен 1 — 200, который на деле отказ

```http
POST /v1/payments HTTP/1.1
Content-Type: application/json

{ "amount": 4200, "currency": "usd" }

HTTP/1.1 200 OK
Content-Type: application/json

{ "ok": false, "error": "upstream_timeout", "code": "GATEWAY_DOWN" }
```

<Quiz
  id="02-status-codes-real-code-1"
  lessonSlug="02-status-codes-real"
  lang="ru"
  question="Апстрим словил таймаут, а статус-строка — 200. Каково последствие и фикс?"
  choices={[
    { label: "Каждая машина, ветвящаяся по статусу — кэши, дашборды APM/error-rate, циклы ретраев клиента — видит успех и делает не то: отказ кэшируется, инцидент остаётся невидимым, а ретрай, который вызвал бы 5xx, не срабатывает. Фикс: вернуть реальный 5xx (тут 504 Gateway Timeout) и держать детали в теле.", correct: true },
    { label: "Ничего — `ok: false` в теле позволяет клиентам обнаружить отказ.", misconception: "Только клиенты, которые парсят и проверяют твой кастомный конверт, обнаружат это. Кэши, дашборды и retry-middleware никогда не читают тело; они ветвятся по 200 и считают полный отказ идеальным здоровьем. Правду должен нести статус." },
    { label: "Для платежей это нормально: 200 не пугает клиентов кодом 5xx.", misconception: "Спрятать 5xx за 200 не убирает тревогу; оно убирает сигнал, который вёл бы к корректному автоматическому восстановлению. Ошибка должна всплыть в статусе, чтобы система отреагировала." },
    { label: "Поменять тело на `code: 500`, оставив статус 200.", misconception: "Положить число в тело ничего не меняет для машин, которые ветвятся по HTTP-статус-строке. Сам статус-код должен быть 5xx." },
  ]}
/>

## Обмен 2 — 401 vs 403

```http
DELETE /tenants/acme/users/77 HTTP/1.1
Authorization: Bearer eyJhbGc...   # валидный, непросроченный токен пользователя тенанта 'globex'

HTTP/1.1 401 Unauthorized
WWW-Authenticate: Bearer

{ "error": "you may not delete users in another tenant" }
```

<Quiz
  id="02-status-codes-real-code-2"
  lessonSlug="02-status-codes-real"
  lang="ru"
  question="Токен валиден; вызывающему просто не хватает прав на другой тенант. Что не так с этим ответом и каким он должен быть?"
  choices={[
    { label: "Код должен быть 403 Forbidden, а не 401. Тело даже говорит 'may not' (авторизация), но 401 значит 'я не знаю, кто ты' (аутентификация). 401 + WWW-Authenticate отправляет аутентифицированного клиента в бессмысленный цикл релогина/рефреша токена, который никогда не даст доступ.", correct: true },
    { label: "401 верен, ведь запрос отклонён по причинам auth.", misconception: "401 и 403 оба про auth, но значат разное. 401 = не аутентифицирован (фикс: войти / рефрешнуть). 403 = аутентифицирован, но не разрешено (смена креденшелов не поможет). Токен здесь валиден, значит, это 403." },
    { label: "Должно быть 400 Bad Request — удаление не разрешено.", misconception: "Запрос корректно сформирован; с его синтаксисом всё в порядке. Проблема в правах, а не в структуре. 400 сказал бы клиенту чинить форму запроса, что нерелевантно." },
    { label: "Должно быть 409 Conflict, потому что два тенанта сталкиваются.", misconception: "Нет конфликта состояния ресурса — пользователь существует и запрос распарсился. Вызывающему просто не разрешено. Это 403, а не 409." },
  ]}
/>

## Обмен 3 — 503 без Retry-After и горячий цикл ретраев

```http
GET /catalog/items?page=3 HTTP/1.1

HTTP/1.1 503 Service Unavailable
Content-Type: application/json

{ "error": "overloaded, shedding load" }

# поведение клиента: тут же переотправляет GET в тесном цикле, без задержки
```

<Quiz
  id="02-status-codes-real-code-3"
  lessonSlug="02-status-codes-real"
  lang="ru"
  question="Сервер сбрасывает нагрузку через 503, но не прислал Retry-After, а клиент ретраит мгновенно. Каковы два фикса — по одному на сторону?"
  choices={[
    { label: "Сторона сервера: 503 должен нести заголовок Retry-After, чтобы клиенты знали, когда вернуться. Сторона клиента: при отсутствии Retry-After ретраить с экспоненциальным бэкоффом плюс джиттер — никогда не тесным мгновенным циклом, который превращает перегрузку в самоиндуцированный thundering herd и держит сервер лежачим.", correct: true },
    { label: "Виноват только клиент; 503 сервера полон как есть.", misconception: "503 — единственный 5xx, который должен нести Retry-After именно для того, чтобы клиенты отступали разумно. Опустить его — оставить клиентов гадать; сервер разделяет вину за шторм." },
    { label: "Виноват только сервер; мгновенный цикл ретраев — приемлемое поведение клиента.", misconception: "Даже без Retry-After долбить заведомо перегруженный сервер без задержки — учебный способ держать его перегруженным. Клиент обязан отступать с джиттером в любом случае." },
    { label: "GET вообще не надо ретраить, потому что ретраи небезопасны.", misconception: "GET идемпотентен, поэтому ретрай на 503 безопасен и корректен — проблема в тайминге (мгновенный цикл) и отсутствующем Retry-After, а не в том, ретраить ли вообще." },
  ]}
/>

## Обмен 4 — условное обновление и 412

```http
PUT /docs/42 HTTP/1.1
If-Match: "v7"
Content-Type: application/json

{ "title": "Q3 plan", "body": "..." }

HTTP/1.1 412 Precondition Failed

# хранимый ETag теперь "v9" — кто-то сохранил дважды с момента последнего чтения клиентом
```

<Quiz
  id="02-status-codes-real-code-4"
  lessonSlug="02-status-codes-real"
  lang="ru"
  question="Условный PUT возвращает 412, потому что ETag сдвинулся с v7 на v9. Что это говорит клиенту и что ему делать?"
  choices={[
    { label: "Проверка оптимистичной конкурентности провалилась: ресурс изменился с момента чтения клиентом, поэтому сервер отказался перезаписать более новую версию (предотвращая lost update). Клиент должен перечитать /docs/42, получить новый ETag и содержимое, свести свою правку с v9, затем повторить PUT с `If-Match: \"v9\"`.", correct: true },
    { label: "412 значит, что тело запроса было малформед; переслать с исправленным JSON.", misconception: "412 — про заголовок-предусловие (If-Match), вычислившееся в ложь, а не про тело. Тело распарсилось нормально. Фикс — перечитать и обновить предусловие, а не чинить JSON." },
    { label: "412 значит, что документ удалён; считать как 404.", misconception: "412 говорит, что предусловие не выполнилось (ETag сдвинулся), а значит, ресурс ещё существует, но изменился. Удаление всплыло бы иначе. Перечитай, чтобы увидеть текущее состояние." },
    { label: "Убрать заголовок If-Match и сделать PUT снова, чтобы он всегда проходил.", misconception: "Убрать If-Match — отключить защиту оптимистичной конкурентности и молча перезаписать чужие правки v8/v9 — ровно тот баг lost update, который предотвращает предусловие. Вместо этого сведи правку с новой версией." },
  ]}
/>

<Recap lang="ru">
На проводе читай статус-строку, заголовки и тело как единое целое. 200 с телом-ошибкой лжёт каждой машине, ветвящейся по статусу, — верни реальный 5xx. 401 vs 403 упирается в аутентификацию vs авторизацию, и ошибка тут зацикливает клиента. 503 должен нести Retry-After, а клиент без него обязан отступать с джиттером, а не долбить. 412 — это работающая оптимистичная конкурентность: перечитай, сведи правку с новым ETag и повтори условную запись, а не форсируй перезапись.
</Recap>
