---
concepts:
- status-code-classes
- 201-202-204-semantics
- 4xx-vs-5xx-ownership
- 429-retry-after
- idempotency-retry
- error-body-truth
estMin: 13
lang: ru
level: senior
order: 7
prereqs: []
slug: quiz-choice
sources:
- https://datatracker.ietf.org/doc/html/rfc9110
- https://developer.mozilla.org/en-US/docs/Web/HTTP/Reference/Status
status: ready
summary: "Синтез по юниту статус-кодов в формате выбора: подобрать верный код под сценарий, разделить ответственность 4xx/5xx, Retry-After и ловушка идемпотентности."
title: 'Статус-коды: тест с выбором ответа'
track: apis
unit: 02-status-codes-real
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Шесть сценариев, в каждом ты выбираешь статус-код в реальных условиях. Не «что значит 404», а какой код заставит кэши, дашборды и циклы ретраев ниже по потоку принять верное решение.
</Hook>

<Goal lang="ru">
Убедиться, что ты можешь подобрать корректный статус под ситуацию и обосновать его — связав нюансы 2xx, словарь 4xx, раздел ответственности 4xx/5xx и правила ретраев/идемпотентности, к которым вёл юнит.
</Goal>

<Quiz
  id="02-status-codes-real-choice-1"
  lessonSlug="02-status-codes-real"
  lang="ru"
  question="Эндпоинт POST /orders валидирует заказ, синхронно пишет его в БД, и ресурс теперь существует. Какой статус, и что ещё должен нести ответ?"
  choices={[
    { label: "201 Created с заголовком Location, указывающим на новый ресурс (например, /orders/9f2). 201 утверждает, что ресурс появился; Location говорит клиенту, где его найти.", correct: true },
    { label: "200 OK с созданным заказом в теле.", misconception: "200 прямо ничего не сломает, но недоопределяет: он не утверждает, что появился новый ресурс, и тулинг, который ходит по Location за каноническим URL, ничего не получает. 201 + Location — точный контракт синхронного создания." },
    { label: "202 Accepted, раз клиент что-то прислал.", misconception: "202 значит «принято, но ещё не сделано» — для async/очередной работы. Здесь запись синхронна и завершена, поэтому 202 солжёт клиенту, что заказ ещё в обработке." },
    { label: "204 No Content.", misconception: "204 значит успех с сознательно пустым телом. Создание обычно хочет вернуть ресурс (или хотя бы его Location), так что 204 выбрасывает ровно ту информацию, которая клиенту нужна." },
  ]}
/>

<Quiz
  id="02-status-codes-real-choice-2"
  lessonSlug="02-status-codes-real"
  lang="ru"
  question="Эндпоинт принимает запрос, ставит его в Kafka и возвращается сразу; воркер обрабатывает его через секунды. Какой статус честен?"
  choices={[
    { label: "202 Accepted с URL для опроса статуса в теле. Он говорит правду: запрос принят, но работа не завершена.", correct: true },
    { label: "200 OK.", misconception: "200 говорит «готово, вот результат» — синхронно и полностью. Работа ещё в очереди, поэтому UI клиента покажет завершённую операцию, которой ещё нигде нет. Это async-vs-sync ложь." },
    { label: "201 Created.", misconception: "201 утверждает, что ресурс теперь существует. Воркер не отработал, значит, ничего ещё не создано. 202 («принято, не сделано») — честный код." },
    { label: "503 Service Unavailable.", misconception: "Ничего не упало и ничего не недоступно — запрос успешно принят. 503 сказал бы клиентам отступить и ретраить запрос, который на самом деле прошёл." },
  ]}
/>

<Quiz
  id="02-status-codes-real-choice-3"
  lessonSlug="02-status-codes-real"
  lang="ru"
  question="Приходит запрос с валидным JSON, все обязательные поля на месте и правильных типов, но quantity = -3 — нарушение бизнес-правила. Какой класс и код подходят лучше всего?"
  choices={[
    { label: "422 Unprocessable Content — распарсилось нормально, но значения нарушают бизнес-правила; верни ошибки по полям, чтобы клиент чинил данные, а не форму запроса.", correct: true },
    { label: "400 Bad Request, потому что ввод невалиден.", misconception: "400 сигналит о малформед-синтаксисе — непарсящийся JSON, отсутствующее обязательное поле. Это тело распарсилось чисто; значения семантически неверны. 400 говорит клиенту чинить структуру, когда чинить надо данные. Многие API сводят оба к 400, но различие 400-vs-422 — это то, что говорит клиенту, что чинить." },
    { label: "409 Conflict.", misconception: "409 — для столкновения с текущим состоянием ресурса: дубликат, гонка версий. Отдельное плохое значение — не конфликт состояния, а нарушенное бизнес-правило, то есть 422." },
    { label: "500 Internal Server Error.", misconception: "Плохое входное значение — вина клиента, а не сервера. 500 ошибочно скажет каждому циклу ретраев, что запрос был валиден и стоит ретрая, и засорит дашборды error-rate небагом." },
  ]}
/>

<Quiz
  id="02-status-codes-real-choice-4"
  lessonSlug="02-status-codes-real"
  lang="ru"
  question="Обратный прокси проксирует запрос в апстрим-сервис, который жив, но возвращает искажённый, обрезанный ответ, который прокси не может распарсить. Какой статус прокси вернёт клиенту?"
  choices={[
    { label: "502 Bad Gateway — прокси получил невалидный ответ от апстрима, который вызвал. Сам шлюз цел; что-то за ним сломано.", correct: true },
    { label: "500 Internal Server Error.", misconception: "500 значит, что приложение, обрабатывающее запрос, бросило исключение и не оправилось — его собственный баг. Здесь прокси здоров; вина — малформед-ответ апстрима. 502 распределяет вину верно, а это важно для триажа." },
    { label: "504 Gateway Timeout.", misconception: "504 — именно таймаут: шлюз ждал апстрим и сдался. Апстрим тут ответил, просто мусором. 502 (плохой ответ) — верный под-код; 504 (нет ответа вовремя) — другой сбой." },
    { label: "503 Service Unavailable.", misconception: "503 значит, что сервер сознательно не обслуживает — перегружен, дренится, на обслуживании — и должен нести Retry-After. Плохой ответ апстрима — не сознательная недоступность; это 502." },
  ]}
/>

<Quiz
  id="02-status-codes-real-choice-5"
  lessonSlug="02-status-codes-real"
  lang="ru"
  question="Клиент упёрся в rate limit, и сервер возвращает 429 с `Retry-After: 30`. Клиент также гоняет общий экспоненциальный бэкофф с джиттером. Что ему делать?"
  choices={[
    { label: "Сначала уважить Retry-After — подождать ~30с — и откатываться к экспоненциальному бэкоффу с джиттером только когда Retry-After нет. Сервер знает своё окно лимита.", correct: true },
    { label: "Игнорировать Retry-After и использовать свой экспоненциальный бэкофф, ведь клиентский бэкофф надёжнее.", misconception: "Retry-After — это сервер, говорящий тебе ровно, когда его окно откроется. Игнор и ретрай раньше зарабатывает ещё один 429 или бан; ретрай сильно позже тратит ёмкость. Бэкофф с джиттером — фолбэк на случай, когда сервер не дал указаний, а не override." },
    { label: "Ретраить сразу, потому что 429 — это 4xx, а 4xx — это вина клиента, чинить сейчас.", misconception: "429 — явное ретраебельное исключение в диапазоне 4xx, и фикс — замедлиться, а не переслать мгновенно. Немедленный ретрай на 429 — ровно то, что бьёт по rate limiter сильнее." },
    { label: "Считать 429 фатальным и показать пользователю ошибку.", misconception: "429 временный и ретраебельный по дизайну. Уважить Retry-After и ретраить — корректное поведение; провалить пользователя — выбросить запрос, который пройдёт через мгновения." },
  ]}
/>

<Quiz
  id="02-status-codes-real-choice-6"
  lessonSlug="02-status-codes-real"
  lang="ru"
  question="Неидемпотентный POST /charge ловит таймаут 504, ключ идемпотентности не отправлен. Списание могло пройти, а могло и нет. Какое поведение клиента корректно?"
  choices={[
    { label: "Не авторетраить запись; пометить операцию «статус неизвестен» и сверить, запросив сам charge (или дождаться webhook). Реальный фикс — в следующий раз слать ключ идемпотентности, чтобы ретраи были безопасны.", correct: true },
    { label: "Ретраить POST сразу — 504 это 5xx, а 5xx ретраебельно.", misconception: "Правило «5xx ретраебельно» предполагает идемпотентность. POST не идемпотентен, а 504 значит, что бэкенд мог завершить списание до того, как шлюз сдался. Слепой ретрай без ключа идемпотентности — ровно то, как это становится двойным списанием." },
    { label: "Считать 504 однозначным отказом и сказать пользователю, что платёж не прошёл.", misconception: "504 значит, что шлюз перестал ждать, а не что работа упала. Списание могло пройти; объявить отказ может породить дубль ручного платежа поверх реального списания. Состояние неизвестно, а не «упало»." },
    { label: "Ретраить ровно один раз, потом сдаться.", misconception: "Без ключа идемпотентности даже один ретрай может списать дважды, если первая попытка реально прошла. Проблема не в числе ретраев, а в отсутствующем ключе идемпотентности. Добавь ключ — и ретраи станут безопасны." },
  ]}
/>

<Recap lang="ru">
Сквозная линия: первая цифра — машинная инструкция, а точный под-код несёт реальную информацию контракта. 2xx делится на 201 (+Location) для синхронного создания, 202 для принято-но-async, 204 для сознательно пустого успеха. Словарь 4xx говорит клиенту, что чинить — 400 (синтаксис) vs 422 (значения) vs 409 (состояние). Под-коды 5xx распределяют вину — 500 твой баг, 502 плохой апстрим, 503 сознательная недоступность, 504 таймаут, где работа могла тайно завершиться. Ретрай 5xx и 429, уважь Retry-After перед бэкоффом и никогда слепо не ретрай неидемпотентный запрос после таймаута — вместо этого тянись за ключом идемпотентности.
</Recap>
