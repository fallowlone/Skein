---
concepts:
- status-code-classes
- 4xx-vs-5xx-ownership
- error-body-truth
- idempotency-retry
- 429-503-retry-after
- 401-vs-403
estMin: 14
lang: ru
level: senior
order: 8
prereqs: []
slug: quiz-short
sources:
- https://datatracker.ietf.org/doc/html/rfc9110
- https://developer.mozilla.org/en-US/docs/Web/HTTP/Reference/Status
status: ready
summary: "Промпты на свободное воспроизведение по юниту статус-кодов. Сначала ответь по памяти, потом раскрой модельный ответ и сравни."
title: 'Статус-коды: тест на свободное воспроизведение'
track: apis
unit: 02-status-codes-real
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";

<Hook>
Воспроизведение бьёт перечитывание. На каждый промпт скажи или запиши полный ответ по памяти, прежде чем открыть модельный, — усилие припоминания и закрепляет различия, когда выбираешь код под давлением.
</Hook>

<Goal lang="ru">
Реконструировать спину юнита, не подглядывая: модель «класс как машинная инструкция», раздел ответственности 4xx/5xx, почему 200-с-телом-ошибки — кардинальный грех, ловушка идемпотентности при ретраях и порядок Retry-After.
</Goal>

<RetrievalDrawer
  client:load
  id="02-status-codes-real-short-retrieval"
  lang="ru"
  questions={[
    { q: "Почему первая цифра статус-кода — машинная инструкция, и какие машины ветвятся по ней раньше, чем человек прочитает ответ?", a: "Класс статуса читает инфраструктура, которая никогда не смотрит в тело ответа: кэши ветвятся, решая, можно ли хранить ответ; балансировщики и обратные прокси ветвятся для маршрутизации и health; агенты APM/мониторинга ветвятся, считая success vs error rate; retry-middleware клиента ветвится, решая, переслать ли. Все действуют по классу немедленно. 2xx = успех, кэшируем по заголовкам; 3xx = иди в другое место, следуй Location или используй кэш; 4xx = клиент прислал что-то не то, не ретрай как есть; 5xx = сервер упал, запрос мог быть валидным, поэтому ретрай может сработать. Так как эти решения принимаются по одному классу, ошибка в нём заставит каждую машину ниже по потоку решить неверно — кэш сохранит отказ, цикл ретраев будет долбить обречённый запрос, дашборд просчитается с инцидентом." },
    { q: "Объясни раздел ответственности 4xx-vs-5xx и почему это несущее различие для ретраев.", a: "4xx значит, что вина клиента: сам запрос неверен — плохой синтаксис, нарушенное бизнес-правило, нет прав — поэтому переслать те же байты даст тот же отказ. 5xx значит, что вина сервера: запрос мог быть совершенно валидным, а сбой временным, поэтому ретрай может сработать. Этот раздел — ровно и есть решение о ретрае: не ретрай 4xx (упадёт точно так же), но 5xx — кандидат на ретрай. Единственное явное исключение — 429 (rate limited), который живёт в диапазоне 4xx, но ретраебелен после задержки. Ошибись классом — скажем, 500 на плохой ввод — и ты скажешь каждому циклу ретраев долбить запрос, который никогда не пройдёт; выдай 200 на реальный сбой сервера — и ретрай, который восстановил бы, не сработает." },
    { q: "Почему вернуть 200 OK с объектом-ошибкой в теле — кардинальный грех, и что именно ломается?", a: "Это заставляет статус-код лгать каждой машине, ветвящейся по нему. Кэши могут сохранить отказ и отдавать его другим пользователям как валидный. Мониторинг считает запрос успехом, поэтому дашборды error-rate остаются зелёными во время реального инцидента — канонический случай: шлюз, ловивший падения апстрима и возвращавший 200 с телом-ошибкой, так что полный отказ выглядел как 100% здоровье сорок минут. Логика ретраев клиента, завязанная на 5xx/429, не срабатывает, ведь она увидела 2xx, поэтому временные сбои, которые ретрай починил бы, всплывают пользователю как поломка. И каждый клиент теперь обязан парсить тело, чтобы узнать, был ли «успех» настоящим, что привязывает его к твоему кастомному конверту ошибок. Фикс — заставить статус говорить правду — корректный 4xx/5xx — и положить человекочитаемые детали в тело этого корректного статуса." },
    { q: "Пройди решение о ретрае, включая ловушку идемпотентности на словившей таймаут записи.", a: "Начни с класса. 4xx, кроме 429, значит, что запрос неверен; не ретрай, чини. 5xx или 429 значит, что сервер упал или зарейтлимитил тебя и запрос мог быть валидным, поэтому ретрай может сработать — но только если операцию безопасно повторить. GET, PUT, DELETE идемпотентны: дважды дают тот же эффект, что и раз, поэтому ретрай безопасен. POST — нет. Опасный случай — 504 (gateway timeout) на POST: бэкенд мог завершить работу до того, как шлюз сдался, поэтому исход по-настоящему неизвестен. Слепой ретрай рискует дублем (например, двойным списанием); корректный ход — не ретраить запись и сверить, прочитав состояние, или слать ключ идемпотентности (как требуют Stripe и Square), чтобы сервер дедуплицировал ретраи. Когда ретраишь — уважь Retry-After, если есть, иначе экспоненциальный бэкофф с джиттером." },
    { q: "Дай порядок Retry-After для 429 и 503 и почему мгновенные ретраи опасны.", a: "И 429 (rate limited), и 503 (service unavailable / сброс нагрузки) должны нести заголовок Retry-After — либо число секунд, либо HTTP-дату — потому что сервер знает своё окно лучше клиента. Порядок клиента: сначала уважить Retry-After; только когда его нет, откатываться к экспоненциальному бэкоффу с джиттером (1с, 2с, 4с, 8с…). Мгновенные ретраи без задержки опасны, потому что флот клиентов, ретраящих разом, синхронизируется в thundering herd, который держит уже перегруженный сервер лежачим — ретраи становятся инцидентом. Джиттер десинхронизирует клиентов, так что нагрузка размазывается, а не приходит пиком. Google Cloud Storage, OpenAI и Stripe документируют ровно этот порядок: читай Retry-After, потом бэкофф с джиттером." },
    { q: "Различи 401 vs 403 и 400 vs 422 и скажи, что клиенту чинить в каждом случае.", a: "401 Unauthorized значит «я не знаю, кто ты» — нет, просроченные или невалидные креденшелы; фикс — аутентифицироваться (войти или рефрешнуть токен) и ретраить. 403 Forbidden значит «я знаю ровно, кто ты, и ты всё равно не можешь это» — смена креденшелов не поможет; клиент должен остановиться. Вернуть 401 на ошибку авторизации — отправить клиента с валидным токеном в бессмысленный цикл релогина. 400 Bad Request значит, что запрос малформед — непарсящийся JSON, отсутствующее обязательное поле — поэтому клиент должен чинить структуру запроса. 422 Unprocessable Content значит, что распарсилось нормально, но значения нарушают бизнес-правила (отрицательное количество, плохой email) — поэтому клиент должен чинить данные, а сервер вернуть ошибки по полям. Паттерн: 401/400 — про идентичность/форму запроса, 403/422 — про то, что разрешено/валидно при корректно сформированном запросе." },
  ]}
/>

<Recap lang="ru">
Если ты воспроизвёл каждый ответ по памяти, ты держишь спину юнита: класс — машинная инструкция, читаемая кэшами, дашбордами и циклами ретраев; 4xx — вина клиента (не ретрай), 5xx — вина сервера (может, ретрай), с 429 как ретраебельным исключением 4xx; 200-с-телом-ошибки — кардинальный грех, ведь он ослепляет каждую машину; ловушка идемпотентности превращает ретрайнутый POST на 504 в двойное списание, если не сверить или не слать ключ идемпотентности; Retry-After уважается перед бэкоффом с джиттером; а 401/400 против 403/422 говорят клиенту, переаутентифицироваться/чинить-форму или остановиться/чинить-данные.
</Recap>
