---
concepts:
- nouns-vs-verbs
- non-crud-actions
- resource-granularity
- representation-vs-resource
- idempotency
- hateoas
estMin: 13
lang: ru
level: senior
order: 7
prereqs: []
slug: quiz-choice
sources:
- https://learn.microsoft.com/en-us/azure/architecture/best-practices/api-design
- https://developer.mozilla.org/en-US/docs/Web/HTTP/Reference/Methods
- https://www.rfc-editor.org/rfc/rfc9110
status: ready
summary: "Тест с выбором на синтез по всему юниту: существительные vs глаголы, не-CRUD переходы, глубина вложенности, representation vs resource, идемпотентность и прагматика HATEOAS."
title: 'REST-моделирование: тест с выбором ответа'
track: apis
unit: 01-rest-modeling
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Шесть вопросов поперёк всего юнита. Каждый — это решение, которое ты принимаешь у доски или в ревью PR: не определение для заучивания, а компромисс, который надо отстоять, когда коллега возражает.
</Hook>

<Goal lang="ru">
Убедись, что связываешь моделирование ресурсов, не-CRUD переходы, глубину вложенности, разделение representation/resource, идемпотентность и прагматику HATEOAS — тот синтез, к которому вёл урок.
</Goal>

<Quiz
  id="01-rest-modeling-choice-1"
  lessonSlug="01-rest-modeling"
  lang="ru"
  question="Коллега предлагает GET /getOpenOrders и POST /createOrder как два новых эндпоинта. Какое возражение самое точное?"
  choices={[
    { label: "Глагол должен быть в HTTP-методе, а не в URL. Resource — это /orders; чтение — GET /orders?status=open, создание — POST /orders. Глагол get/create в пути — это RPC, теряются uniform interface и кэшируемость.", correct: true },
    { label: "Имена слишком длинные и плохо читаются.", misconception: "Длина — косметика. Настоящий дефект в том, что URL несёт глагол, который HTTP-метод уже выражает — это RPC поверх HTTP, и цена — потерянный uniform interface, а не пара символов." },
    { label: "Оба должны быть POST, ведь оба трогают БД.", misconception: "Чтение должно оставаться GET, чтобы быть safe и кэшируемым; делать чтение POST-ом — скрывать, что побочных эффектов нет. Дефект — глагол в пути, а не выбор метода для создания." },
    { label: "Ничего — verb-эндпоинты нормальны, лишь бы были консистентны.", misconception: "Консистентность не оправдывает RPC-форму. Как только URL называет действие, прокси и клиенты больше не могут рассуждать о нём как о resource — а в этом весь смысл REST." },
  ]}
/>

<Quiz
  id="01-rest-modeling-choice-2"
  lessonSlug="01-rest-modeling"
  lang="ru"
  question="Нужно выставить 'отменить заказ' — охраняемый переход с правилами (не отгружен, не отменён ранее). Какая модель достаточно RESTful и безопасна?"
  choices={[
    { label: "POST /orders/42/cancel — охраняемый action-подресурс. Да, это глагол в URL, но он привязан к resource, честно использует POST, и сервер владеет правилами: выполняет весь переход атомарно или отклоняет.", correct: true },
    { label: "PATCH /orders/42 с {'status': 'cancelled'} — просто обновить поле.", misconception: "Сделать status клиент-записываемым — отдать клиенту ключи от стейт-машины: он может прыгнуть в любое состояние, обойти правила отмены и испортить состояние заказа. Переход должен охраняться на сервере." },
    { label: "DELETE /orders/42 — отмена удаляет заказ.", misconception: "Отменённый заказ — не удалённый; он всё равно состоялся и несёт историю. DELETE теряет аудит-трейл и смешивает два разных исхода жизненного цикла." },
    { label: "GET /orders/42/cancel — пусть будет простой ссылкой, по которой кликает клиент.", misconception: "GET должен быть safe и без побочных эффектов, иначе краулер или prefetch отменит заказы. Действие, меняющее состояние, использует POST, а не GET." },
  ]}
/>

<Quiz
  id="01-rest-modeling-choice-3"
  lessonSlug="01-rest-modeling"
  lang="ru"
  question="Дашборд рендерит /customers/7/projects/3/orders/42/items девятью последовательными вызовами, а путь ломается при переносе item между заказами. Какой senior-фикс?"
  choices={[
    { label: "Уплостить до root-ресурсов, адресуемых своим ID, с query-параметрами для связи — /orders?customer=7&project=3 — и добавить ?expand=items,customer, чтобы один вызов вернул нужное экрану.", correct: true },
    { label: "Агрессивно кэшировать каждый уровень, чтобы девять вызовов были быстрыми на тёплых попаданиях.", misconception: "Кэш скрывает только латентность тёплых попаданий, инвалидация поперёк четырёх вложенных уровней зверская, а URL по-прежнему хрупкие при смене связей. Ты оптимизируешь форму, которую надо уплостить." },
    { label: "Добавить отдельный эндпоинт /dashboard, возвращающий ровно то, что нужно этому экрану.", misconception: "Это RPC view-эндпоинт, а не resource. Каждый новый экран захочет свой, и ты дрейфуешь обратно к API «глагол на view». Field-expansion на реальных ресурсах масштабируется; эндпоинт на экран — нет." },
    { label: "Оставить глубокую вложенность; она корректно выражает владение.", misconception: "Вложенность выражает владение, но после одного-двух уровней навязывает chatty N+1 round-trips и хрупкие закодированные пути. Глубокое владение не требует глубоких URL — достигай resource по его собственному ID." },
  ]}
/>

<Quiz
  id="01-rest-modeling-choice-4"
  lessonSlug="01-rest-modeling"
  lang="ru"
  question="API сериализует строки ORM прямо в JSON. Почему добавление явного mapping-слоя (DTO/сериализатор) — решение с наибольшим рычагом в дизайне?"
  choices={[
    { label: "Без него representation ЕСТЬ таблица, поэтому схема БД становится публичным контрактом — переименование колонки или новый constraint становятся ломающим изменением API. Mapping-слой разъединяет wire-форму и хранение, и БД можно рефакторить без скоординированного релиза клиентов.", correct: true },
    { label: "Mapping-слой делает сериализацию JSON быстрее, чем путь ORM.", misconception: "Скорость сериализации ничтожна и не в этом суть. Рычаг — разъединение: representation становится контрактом, которым ты владеешь, а не случайностью схемы." },
    { label: "Он снижает трафик, обрезая колонки.", misconception: "Обрезать колонки можно и так; трафик — побочный эффект. Реальный выигрыш — клиенты перестают зависеть от имён колонок, и БД остаётся рефакторимой." },
    { label: "ORM не умеют сериализовать связи, поэтому нужен сериализатор.", misconception: "ORM прекрасно сериализуют связи — именно в этом риск, ведь они делают тривиальным утечку всей схемы на провод. Mapping-слой — это осознанная граница, а не обходной приём." },
  ]}
/>

<Quiz
  id="01-rest-modeling-choice-5"
  lessonSlug="01-rest-modeling"
  lang="ru"
  question="Клиент ретраит POST /payments из-за таймаута ответа, и покупателя списывают дважды. Какое утверждение об идемпотентности верно?"
  choices={[
    { label: "POST не идемпотентен по спецификации, поэтому повторённое создание может задвоить. Фикс — idempotency key, который клиент шлёт, а сервер дедупит: повтор возвращает исходный результат вместо создания второго платежа.", correct: true },
    { label: "POST идемпотентен, значит двойное списание — это баг БД.", misconception: "Идемпотентность означает, что повтор запроса даёт тот же эффект, что и один раз. PUT и DELETE подходят; POST — нет: два POST создают два resource, если не добавить idempotency key." },
    { label: "Переключить создание на GET, чтобы ретраи были безопасны.", misconception: "GET должен быть safe (без побочных эффектов) — он вообще не может создать платёж. Safety — не то же, что дедупликация создающего запроса; нужен idempotency key на POST." },
    { label: "Сделать PATCH вместо POST; PATCH никогда не задваивает.", misconception: "PATCH тоже не гарантированно идемпотентен (например, патч «увеличить на 1»), и создание — не обновление. Двойное списание предотвращает idempotency key, а не ярлык метода." },
  ]}
/>

<Quiz
  id="01-rest-modeling-choice-6"
  lessonSlug="01-rest-modeling"
  lang="ru"
  question="Пурист блокирует твой PR, потому что ответы не несут гипермедиа-ссылок (HATEOAS). Как senior должен это взвесить?"
  choices={[
    { label: "HATEOAS — теоретический эндгейм: самоописывающие ссылки позволяют серверу свободно эволюционировать URL — но это наименее принятый constraint REST, ведь клиенты хардкодят URL и редко потребляют ссылки. Отгрузка level-2 REST (resource + verb) — прагматичная норма; его отсутствие не блокер.", correct: true },
    { label: "Пурист прав; без гипермедиа это не REST и отгружать нельзя.", misconception: "Почти все production «REST» API — это level-2 по Ричардсону, без гипермедиа. HATEOAS ценен в больших hypermedia-native экосистемах, но редко стоит цены в остальных — его отсутствие не дисквалифицирует дизайн." },
    { label: "HATEOAS устарел и должен быть убран из стандарта.", misconception: "Он не устарел — там, где клиенты потребляют ссылки, он реально разъединяет их со структурой URL. Он недоиспользуется, а не неверен; знай, что он даёт и когда окупается." },
    { label: "Добавление _links делает API автоматически обратно-совместимым.", misconception: "Ссылки помогают только клиентам, которые реально следуют им в рантайме. Клиенты, захардкодившие URL, всё равно сломаются; гипермедиа — это дисциплина обеих сторон, а не бесплатный переключатель совместимости." },
  ]}
/>

<Recap lang="ru">
Сквозная линия: REST API моделирует существительные, нужные клиенту, с глаголом в HTTP-методе, фильтрацией и пагинацией в query-параметрах, а не-CRUD переходы — как охраняемые action- или transition-ресурсы, а не клиент-записываемое поле status. Держи вложенность мелкой и используй expansion против chatty-экранов. Сериализуй через mapping-слой, чтобы representation был контрактом, которым ты владеешь, а не твоей схемой БД. Помни, что POST не идемпотентен (используй idempotency key для безопасных ретраев), а HATEOAS — изящная теория, которая level-2 REST обычно не нужна. Теперь, когда на дизайн-ревью кто-то предложит записываемое поле status или шестиуровневый URL, — ты задашь нужный вопрос прежде, чем PR успеет смерджиться.
</Recap>
