---
concepts:
- resource-url-design
- non-crud-actions
- idempotency-key
- representation-mapping
estMin: 14
lang: ru
level: senior
order: 9
prereqs: []
slug: quiz-code
sources:
- https://developer.mozilla.org/en-US/docs/Web/HTTP/Reference/Methods
- https://learn.microsoft.com/en-us/azure/architecture/best-practices/api-design
- https://www.rfc-editor.org/rfc/rfc9110
status: ready
summary: Читай реальные HTTP-запросы, дизайны URL и формы ответов, предскажи сбой и выбери фикс с наибольшим рычагом, который senior сделает первым.
title: 'REST-моделирование: чтение запросов и дизайна'
track: apis
unit: 01-rest-modeling
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Дефекты дизайна API видны в wire-формате — строке запроса, форме URL, теле ответа. Прочитай каждый сниппет, найди ловушку и выбери фикс, который senior-инженер сделает первым.
</Hook>

<Goal lang="ru">
Отработай цикл, который ты запускаешь в каждом ревью API: прочитай запрос и форму ресурса, предскажи, что сломается в production, и потянись к структурному фиксу, прежде чем латать симптомы.
</Goal>

## Сниппет 1 — запрос смены состояния

```http
PATCH /orders/42 HTTP/1.1
Content-Type: application/json

{ "status": "cancelled" }
```

<Quiz
  id="01-rest-modeling-code-1"
  lessonSlug="01-rest-modeling"
  lang="ru"
  question="Так API отменяет заказ. В чём дефект и какой фикс?"
  choices={[
    { label: "Он делает внутреннее поле status клиент-записываемым, поэтому клиент может прыгнуть в любое состояние и обойти правила отмены (уже отгружен? возврат сделан?). Фикс: охраняемый action-подресурс POST /orders/42/cancel, где сервер запускает правила перехода атомарно.", correct: true },
    { label: "PATCH — неверный verb; должен быть PUT /orders/42 с полным телом.", misconception: "PUT vs PATCH — не проблема: замена всего заказа имеет тот же изъян. Дефект в том, что переход состояния неохраняем — клиент пишет status напрямую вместо того, чтобы сервер навязывал правила." },
    { label: "Content-Type должен быть application/merge-patch+json.", misconception: "Media-тип — деталь; структурный дефект в том, что любой клиент может задать status любым значением. Фикс — охраняемый action-ресурс, а не другой формат патча." },
    { label: "Ничего — PATCH на resource это хрестоматийный REST.", misconception: "Выглядит RESTful, но записываемое поле status отдаёт клиенту стейт-машину. Настоящая отмена — охраняемый переход, которым должен владеть сервер, а не поле, которое флипает клиент." },
  ]}
/>

## Сниппет 2 — дизайн URL ресурсов

```
GET  /getUserOrders?userId=7
POST /createOrderForUser?userId=7
GET  /customers/7/projects/3/orders/42/items/9/discounts
```

<Quiz
  id="01-rest-modeling-code-2"
  lessonSlug="01-rest-modeling"
  lang="ru"
  question="В этих трёх строках два разных запаха моделирования. Назови их и редизайн."
  choices={[
    { label: "Строки 1–2 — RPC: глаголы (getUserOrders, createOrderForUser) в пути вместо GET/POST /orders?customer=7. Строка 3 — over-nesting: шесть уровней, хрупко и chatty. Редизайн: noun-ресурсы с глаголом в методе, и достигай глубокие ресурсы по их ID — /discounts?item=9 — вложенность максимум один-два уровня.", correct: true },
    { label: "Неверна только вложенность; verb-эндпоинты — приемлемые RPC-алиасы.", misconception: "Оба дефекта. getUserOrders и createOrderForUser кладут действие в URL — это RPC, теряющий uniform interface, а не приемлемый алиас рядом с REST-ресурсами." },
    { label: "Проблема в query-параметрах; всё должно быть сегментами пути.", misconception: "Query-параметры для фильтрации и связей — корректный REST. Реальные запахи — глаголы в строках 1–2 и шестиуровневая вложенность в строке 3, а не использование query-строк." },
    { label: "Все три нормальны; нужен лишь консистентный регистр.", misconception: "Регистр — косметика. Две строки кодируют глаголы в пути, одна вложена на шесть уровней — структурные проблемы, которые консистентный регистр не решает." },
  ]}
/>

## Сниппет 3 — идемпотентный ретрай

```http
POST /payments HTTP/1.1
Idempotency-Key: 7f3c9a2e-...-d41
Content-Type: application/json

{ "amount": 4200, "currency": "usd", "source": "card_x" }
```

<Quiz
  id="01-rest-modeling-code-3"
  lessonSlug="01-rest-modeling"
  lang="ru"
  question="Клиент шлёт этот запрос, TCP-соединение рвётся до прихода ответа, и клиент ретраит идентичный запрос с тем же Idempotency-Key. Что должен сделать сервер?"
  choices={[
    { label: "Распознать ключ как уже обработанный и вернуть результат исходного платежа (тот же id, тот же status), не создавая второе списание. Ключ превращает неидемпотентный POST в безопасно ретраимый, дедупя на сервере.", correct: true },
    { label: "Создать новый платёж, ведь каждый POST — независимое создание.", misconception: "Это ровно то двойное списание, которое этот заголовок и предотвращает. Idempotency-Key говорит серверу обнаружить повтор и вернуть первый результат, а не создавать второй платёж." },
    { label: "Отклонить ретрай с 409 Conflict, потому что ключ уже использован.", misconception: "Повтор с тем же ключом и телом — не конфликт; контракт в том, чтобы вернуть исходный успешный ответ, делая ретрай прозрачным для клиента. 409 заставил бы клиента обрабатывать не-ошибку как ошибку." },
    { label: "Игнорировать заголовок — idempotency key применим только к PUT и DELETE.", misconception: "PUT и DELETE идемпотентны по своей семантике; idempotency key существует именно потому, что POST — нет, позволяя безопасно ретраить создающий запрос." },
  ]}
/>

## Сниппет 4 — протекающий ответ

```json
{
  "id": 42,
  "full_name": "Ada Lovelace",
  "password_hash": "$2b$12$...",
  "is_deleted": false,
  "internal_risk_score": 0.13
}
```

<Quiz
  id="01-rest-modeling-code-4"
  lessonSlug="01-rest-modeling"
  lang="ru"
  question="Это ресурс user, сериализованный прямо из строки ORM. В чём ошибка и структурный фикс?"
  choices={[
    { label: "Representation ЕСТЬ таблица: он сливает credential (password_hash) и внутренние поля (is_deleted, internal_risk_score) и приваривает клиентов к именам колонок вроде full_name. Фикс: mapping-слой (DTO/сериализатор), отдающий только нужные wire-поля, разъединяя representation и схему.", correct: true },
    { label: "Проблема только в password_hash; убери его и отгружай остальное.", misconception: "Убрать credential необходимо, но недостаточно. is_deleted и internal_risk_score — внутренние, а сериализация строк напрямую всё равно приваривает контракт к именам колонок — структурный фикс это явный mapping-слой." },
    { label: "id должен быть строкой, а не числом.", misconception: "Число vs строка для id — мелкий выбор конвенции. Реальный урон — слив credential и внутренних полей и связывание wire-контракта со схемой БД." },
    { label: "Ничего структурного — просто добавь field-level авторизацию, чтобы hash видели только админы.", misconception: "Хэш пароля не должен быть ни в каком representation независимо от роли, и связь со схемой остаётся. Фикс — отображать в осознанную wire-форму, а не гейтить сырые колонки по роли." },
  ]}
/>

<Recap lang="ru">
Любой дефект API читается с провода: записываемое поле status — неохраняемый переход (используй охраняемый action-ресурс); глаголы в пути и шестиуровневая вложенность — RPC и chattiness (noun-ресурсы, мелкие пути, ID в корне); ретраенный POST нуждается в idempotency key для безопасного дедупа; а строка, сериализованная прямо в JSON, сливает credential и внутренние поля и приваривает контракт к схеме (отображай через DTO). Читай запрос, найди структурный запах, чини форму — не симптом.
</Recap>
