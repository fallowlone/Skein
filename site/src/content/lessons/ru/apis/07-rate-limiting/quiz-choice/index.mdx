---
concepts:
- fixed-window
- sliding-window
- token-bucket
- leaky-bucket
- distributed-rate-limiting
- retry-after
estMin: 13
lang: ru
level: senior
order: 7
prereqs: []
slug: quiz-choice
sources:
- https://blog.arcjet.com/rate-limiting-algorithms-token-bucket-vs-sliding-window-vs-fixed-window/
- https://redis.io/docs/latest/develop/use-cases/rate-limiter/
- https://datatracker.ietf.org/doc/html/draft-ietf-httpapi-ratelimit-headers
status: ready
summary: "Тест с выбором на синтез по всему юниту rate limiting — компромиссы алгоритмов, граничный всплеск, распределённые счётчики, атомарность и контракт 429."
title: 'Rate limiting: тест с выбором ответа'
track: apis
unit: 07-rate-limiting
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Шесть вопросов поперёк всего юнита. Каждый отражает решение по дизайну, которое ты принимаешь под реальным трафиком — не определение для заучивания, а компромисс, который надо взвесить, когда клиент долбит твою границу сразу через шесть нод.
</Hook>

<Goal lang="ru">
Убедись, что связываешь выбор алгоритма, поведение всплесков, ловушку распределённого счётчика, атомарность и контракт 429 — тот синтез, к которому вёл урок.
</Goal>

<Quiz
  id="07-rate-limiting-choice-1"
  lessonSlug="07-rate-limiting"
  lang="ru"
  question="Лимит 100/мин на fixed window с ключом {key}:{minute}. Сколько запросов клиент в худшем случае легально уложит в промежуток ~1 секунда и почему?"
  choices={[
    { label: "Около 200 — 100 в хвосте одного окна и 100 в начале следующего. Окно сбрасывается по границе настенных часов, поэтому два соседних окна выдают полный бюджет встык через шов.", correct: true },
    { label: "Ровно 100 — счётчик отклоняет 101-й запрос.", misconception: "Одно окно ограничено 100, но у лимитера нет памяти через границу сброса. Клиент заполняет конец одной минуты и начало следующей, поэтому у границы оказывается до 2x." },
    { label: "Около 150 — окно усредняет две минуты.", misconception: "Fixed window ничего не усредняет. Каждое окно независимо выдаёт полные 100, поэтому худший случай на шве — 2x, а не 1.5x." },
    { label: "Неограниченно — fixed window на самом деле не ограничивает всплески.", misconception: "Ограничивает: худший случай строго равен 2x лимита, сконцентрированных у границы. Эта известная утечка — повод тянуться к sliding window или token bucket, а не доказательство отсутствия лимита." },
  ]}
/>

<Quiz
  id="07-rate-limiting-choice-2"
  lessonSlug="07-rate-limiting"
  lang="ru"
  question="Публичный API обслуживает бёрстовых браузерных клиентов — загрузка страницы выстреливает ~12 запросов разом, затем простой. Нужен честный средний rate, терпящий эти бёрсты загрузки. Какой алгоритм подходит лучше?"
  choices={[
    { label: "Token bucket — токены пополняются непрерывно до capacity, поэтому простаивавший клиент может мгновенно потратить ограниченный всплеск, а дальше его дросселирует до rate пополнения. Ты держишь среднее, разрешая всплеск, который клиенты реально производят.", correct: true },
    { label: "Leaky bucket — он выпускает запросы с постоянным rate.", misconception: "Leaky bucket сглаживает выход до устойчивого rate, чем активно отказывает легитимному бёрсту загрузки и добавляет очередную задержку. Он для хрупкого downstream, а не для дружелюбия к бёрстовым клиентам." },
    { label: "Fixed window — он самый простой и дешёвый.", misconception: "Дёшево, но он протекает 2x граничным всплеском и не моделирует бёрсты намеренно — любая терпимость случайна, зависит от того, куда запрос попал в окне, а не настраиваемая ручка." },
    { label: "Sliding window log — он точный.", misconception: "Точный, но для высокообъёмного бёрстового трафика память на ключ в один timestamp на запрос — тяжёлая цена за точность, которая тут не нужна. Прибереги его для audit-grade счёта." },
  ]}
/>

<Quiz
  id="07-rate-limiting-choice-3"
  lessonSlug="07-rate-limiting"
  lang="ru"
  question="In-memory лимитер проходит все тесты на ноутбуке, но в production трафик идёт сильно выше настроенных 100/мин. За балансировщиком четыре app-ноды. Корневая причина?"
  choices={[
    { label: "Каждая нода держит свой счётчик, поэтому это четыре независимых лимитера и эффективный лимит ~4x от настроенного. Клиент, чьи запросы балансировщик размазывает по нодам, никогда не заполняет ни один счётчик.", correct: true },
    { label: "TTL на ключе счётчика слишком долгий, поэтому старые счёты залёживаются.", misconception: "Длина TTL не умножает лимит на число нод. Дефект в том, что счётчик пер-процессный; лимит должен жить в хранилище, общем для всех нод." },
    { label: "Значение лимита настроено на 400 вместо 100.", misconception: "Конфиг верен на каждой ноде; баг архитектурный. N in-memory счётчиков тихо суммируются в N раз больше настроенного лимита независимо от значения." },
    { label: "Балансировщик не использует sticky sessions.", misconception: "Sticky sessions замаскировали бы баг, привязав клиента к одной ноде, но это хрупко и ломает честность. Реальный фикс — общий счётчик в Redis, а не привязка сессий." },
  ]}
/>

<Quiz
  id="07-rate-limiting-choice-4"
  lessonSlug="07-rate-limiting"
  lang="ru"
  question="Ты переносишь счётчик в Redis и реализуешь как INCR, затем EXPIRE. Почему единый Lua-скрипт — более надёжный паттерн?"
  choices={[
    { label: "Lua выполняет весь цикл read-decide-update атомарно. Раздельные INCR и EXPIRE могут гонять, и если процесс умрёт между ними, ключ никогда не получит TTL и счётчик протечёт навсегда.", correct: true },
    { label: "Lua быстрее, чем INCR, на сервере Redis.", misconception: "Скорость ни при чём — INCR и так sub-millisecond. Причина — атомарность: скрипт исключает interleaving и гонку с ключом без TTL." },
    { label: "Lua позволяет Redis сохранять счётчик на диск ради надёжности.", misconception: "Lua-скрипты выполняются в памяти, как любая команда Redis; персистентность ортогональна. Выгода — атомарный цикл read-decide-update." },
    { label: "Lua убирает необходимость для Redis быть однопоточным.", misconception: "Redis и так выполняет команды однопоточно; именно поэтому скрипт исполняется без interleaving других команд. Lua использует это свойство, а не меняет его." },
  ]}
/>

<Quiz
  id="07-rate-limiting-choice-5"
  lessonSlug="07-rate-limiting"
  lang="ru"
  question="Сервис аутентификации должен держать точный пер-аккаунт лимит ради compliance — без граничного зазора, каждая попытка учтена. Какой алгоритм и какую цену ты принимаешь?"
  choices={[
    { label: "Sliding window log — хранить timestamp на запрос в sorted set, обрезать записи старше окна, считать остаток. Он точен на любом скользящем интервале; цена — память и ZADD плюс trim на каждый вызов.", correct: true },
    { label: "Sliding window counter — он достаточно точен при ~0.003% ошибки.", misconception: "Для грубого edge-лимита ~0.003% ошибки прекрасно, но это всё ещё аппроксимация. Audit-grade точный счёт ради compliance — единственный случай, где ты платишь за log." },
    { label: "Fixed window — сбрасывать счётчик ровно каждую минуту.", misconception: "Fixed window точен внутри окна, но протекает до 2x у границы, поэтому скользящий счёт не точен. Этот зазор недопустим для аудируемого лимита." },
    { label: "Token bucket — он точно отслеживает токены.", misconception: "Token bucket намеренно разрешает ограниченный всплеск выше устойчивого rate, поэтому не держит точный скользящий счёт. Он оптимизирует под дружелюбие, а не под audit-grade точность." },
  ]}
/>

<Quiz
  id="07-rate-limiting-choice-6"
  lessonSlug="07-rate-limiting"
  lang="ru"
  question="На fixed window ты отклоняешь с 429 и жёстким Retry-After 30 секунд. На следующей границе окна error rate снова взлетает. Что произошло и какой фикс?"
  choices={[
    { label: "Thundering herd: всем дросселированным клиентам сказали повторить в один момент, поэтому они стампедом возвращаются вместе на границе сброса. Фикс: предпочесть модель, где capacity освобождается непрерывно (token bucket или sliding), и добавить jitter к Retry-After, чтобы клиенты возвращались вразнобой.", correct: true },
    { label: "Redis сбросил ключи счётчиков на границе окна, обнулив всех.", misconception: "Истечение ключей ожидаемо и корректно; оно не вызывает синхронный всплеск повторов. Всплеск — от выдачи всем клиентам одинакового дедлайна Retry-After." },
    { label: "Значение Retry-After слишком велико; снизь до 5 секунд.", misconception: "Сокращение лишь сдвигает синхронную волну раньше — все клиенты всё равно повторяют вместе. Фикс — десинхронизировать их jitter'ом и непрерывно пополняемой моделью, а не меньшей фиксированной задержкой." },
    { label: "Клиенты игнорируют Retry-After и повторяют сразу.", misconception: "Здесь клиенты дисциплинированы — они подчинились Retry-After, именно поэтому проснулись разом. Herd самонанесён одинаковым дедлайном, а не плохим поведением клиентов." },
  ]}
/>

<Recap lang="ru">
Сквозная линия: то, как запросы попадают в окна, — это где fixed window протекает 2x граничным всплеском; sliding window log покупает точность памятью на запрос, sliding counter аппроксимирует её за два integer, а token bucket моделирует всплески как настраиваемый capacity. Ничего из этого не реально, если счётчик живёт в пер-нодовой памяти — раздели его в Redis, сделай read-decide-update атомарным через Lua, а отклоняя — соблюдай контракт: 429 + Retry-After в delta-seconds плюс jitter, чтобы граница сброса не стала thundering herd.
</Recap>
