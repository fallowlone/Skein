---
concepts:
- boundary-burst
- sliding-window
- token-bucket
- distributed-rate-limiting
- atomic-lua
- retry-after
estMin: 14
lang: ru
level: senior
order: 8
prereqs: []
slug: quiz-short
sources:
- https://blog.arcjet.com/rate-limiting-algorithms-token-bucket-vs-sliding-window-vs-fixed-window/
- https://redis.io/docs/latest/develop/use-cases/rate-limiter/
- https://datatracker.ietf.org/doc/html/draft-ietf-httpapi-ratelimit-headers
status: ready
summary: "Промпты на свободное припоминание по всему юниту rate limiting. Отвечай по памяти, затем открой модельный ответ и сравни."
title: 'Rate limiting: тест на свободное припоминание'
track: apis
unit: 07-rate-limiting
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";

<Hook>
Припоминание бьёт перечитывание. На каждый промпт скажи или напиши полный ответ по памяти, прежде чем открыть модельный — усилие припоминания и закрепляет компромиссы, когда ты проектируешь лимитер под давлением.
</Hook>

<Goal lang="ru">
Реконструируй спину юнита — граничный всплеск, компромиссы алгоритмов, ловушку распределённого счётчика, атомарность и контракт 429 — не подглядывая в урок.
</Goal>

<RetrievalDrawer
  client:visible
  id="07-rate-limiting-short-retrieval"
  lang="ru"
  questions={[
    { q: "Почему fixed window протекает 2x всплеском и как sliding window log и token bucket каждый его убирают?", a: "Fixed window считает запросы внутри ведра по настенным часам, вроде календарной минуты, и сбрасывается на границе. Клиент может отправить полный лимит в самом конце одного окна и полный лимит снова в самом начале следующего, поэтому до двойного лимита укладывается в крошечный промежуток через границу. Sliding window log чинит это точно, ставя timestamp каждому запросу и считая любые скользящие 60s, ценой хранения одного timestamp на запрос. Token bucket обходит границу целиком: токены пополняются непрерывно, а не сбрасываются на границе, поэтому нет шва для эксплуатации, а максимальный всплеск — это сколько ты задал в capacity: намеренная ограниченная ручка, а не случайная 2x-утечка." },
    { q: "Сравни sliding window log и sliding window counter. Когда что выбирать?", a: "Log хранит timestamp на запрос в sorted set, обрезает записи старше окна и считает остаток — точно на любом скользящем интервале, но это стоит одной записи на запрос плюс ZADD и ZREMRANGEBYSCORE trim на каждый вызов, так что клиент на 10k req/min держит 10k timestamp'ов. Counter держит лишь два fixed-window integer'а — текущий и предыдущий — и оценивает скользящий счёт как current + previous * доля_перекрытия. Это аппроксимация, но Cloudflare измерил около 0.003% ошибки на примерно 400 миллионах запросов за два integer на ключ. Используй counter как edge-лимитер по умолчанию, где этот обмен точности почти даром идеален; прибереги log для аутентификации или платежей, где audit-grade точный счёт стоит памяти." },
    { q: "Объясни, как token bucket держит средний rate, всё же разрешая всплески, и что контролирует параметр capacity.", a: "Ведро держит до capacity токенов и пополняется с устойчивым rate, скажем 100 токенов в минуту. Каждый запрос тратит токен; запрос отклоняется только когда ведро пусто. Клиент, который простаивал, накапливает токены до потолка, поэтому может выстрелить всплеск мгновенно, а дальше его дросселирует до rate пополнения — так что со временем среднее ограничено rate пополнения, при этом короткие всплески разрешены. Capacity — это ручка всплеска: при лимите 100/мин и capacity 100 свежий клиент может выстрелить 100 разом; задай capacity 10, и то же среднее терпит лишь всплеск в 10 запросов. Всплеск не баг, это параметр, подгоняющий лимитер под реальное поведение клиентов. Он хранит лишь два значения — текущие токены и timestamp последнего пополнения — поэтому Stripe и AWS берут его как общий default." },
    { q: "In-memory лимитер проходит все тесты на ноутбуке, но в production пропускает трафик сильно выше лимита. Объясни сбой и фикс.", a: "Лимитер держит счётчик в памяти собственного процесса. На одной машине это корректно, но production гоняет приложение на нескольких нодах за балансировщиком, и у каждой ноды свой независимый счётчик. При N нодах эффективный лимит в N раз выше настроенного, и клиент, чьи запросы балансировщик размазывает по нодам, никогда не видит, как заполняется один счётчик. Тесты проходят, потому что бьют в один процесс. Фикс — перенести счётчик в хранилище, общее для всех нод (почти всегда Redis), чтобы все ноды читали и писали одно число, и обновлять его атомарно." },
    { q: "Почему запускать проверку rate-limit в Redis единым Lua-скриптом, а не INCR, затем EXPIRE?", a: "Redis выполняет Lua-скрипт атомарно — весь цикл read-decide-update идёт без interleaving других команд, поэтому конкурентные запросы не могут дважды потратить токены или оба пройти проверку, которая должна была отклонить один. Простой INCR с последующим EXPIRE — две отдельные команды: они могут гонять, и если процесс умрёт между ними, ключ никогда не получит TTL и счётчик протечёт навсегда, тихо заблокировав ключ. Проверка token bucket в Lua — горстка операций и выполняется за sub-millisecond, достаточно дёшево, чтобы сидеть на синхронном пути запроса даже при миллионах запросов в секунду." },
    { q: "Что контракт 429 должен дисциплинированному клиенту и почему RateLimit-Reset должен быть delta-seconds, а не UNIX timestamp?", a: "При отклонении верни 429 Too Many Requests с Retry-After в секундах, вычисленным от того, когда освободится следующий токен, чтобы клиенты отступали, а не повторяли в плотном цикле. Добавь IETF-поля RateLimit-* — RateLimit-Limit, RateLimit-Remaining, RateLimit-Reset — чтобы клиенты сами замедлялись до того, как упрутся в стену. Драфт задаёт RateLimit-Reset как delta-seconds, а не UNIX timestamp, по двум причинам: timestamp требовал бы синхронизации часов клиента с сервером, и он сказал бы каждому дросселированному клиенту повторить в один абсолютный момент — самонанесённый thundering herd на границе сброса. Delta-seconds плюс jitter разносят повторы." },
  ]}
/>

<Recap lang="ru">
Если ты смог реконструировать каждый ответ по памяти, ты держишь спину юнита: fixed window протекает 2x граничным всплеском, который sliding window и token bucket убирают по-разному; log точен, но тяжёл по памяти, а counter аппроксимирует его за два integer; token bucket держит среднее, а capacity задаёт всплеск; пер-нодовый счётчик — ложь за балансировщиком; общий счётчик в Redis надо обновлять атомарно через Lua; а контракт 429 должен клиенту Retry-After в delta-seconds плюс jitter, чтобы избежать thundering herd.
</Recap>
