---
concepts:
- distributed-rate-limiting
- token-bucket
- atomic-lua
- retry-after
- per-key-quota
estMin: 240
lang: ru
level: senior
order: 10
prereqs: []
slug: project
sources:
- https://redis.io/docs/latest/develop/use-cases/rate-limiter/
- https://blog.arcjet.com/rate-limiting-algorithms-token-bucket-vs-sliding-window-vs-fixed-window/
- https://datatracker.ietf.org/doc/html/draft-ietf-httpapi-ratelimit-headers
status: ready
summary: "Практический проект — построй распределённый атомарный token-bucket лимитер в Redis, докажи, что он держит реальный лимит на нескольких нодах, и отправь корректный контракт 429."
title: 'Rate limiting: построй распределённый лимитер, который реально держит'
track: apis
unit: 07-rate-limiting
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import ProjectBrief from "~/components/pedagogy/ProjectBrief.astro";

<Hook>
Читать про ложь пер-нодового счётчика — не то же, что смотреть, как твой лимит тихо утроился в production. Построй лимитер, прогони его за балансировщиком на нескольких нодах, затем атакуй так, как делает реальный клиент — через границу окна и через все ноды разом — и докажи, что лимит держит.
</Hook>

<Goal lang="ru">
Преврати ментальную модель юнита в работающий артефакт: распределённый token-bucket лимитер, чей счётчик живёт в Redis, чья проверка атомарна, а чей ответ 429 — корректный контракт, проверенный нагрузочным тестом, который побеждает наивный пер-нодовый лимитер.
</Goal>

<ProjectBrief
  id="07-rate-limiting-project"
  lessonSlug="07-rate-limiting"
  lang="ru"
  objective="Построй rate-limiting middleware, который держит реальный пер-API-key лимит минимум на 3 app-нодах за балансировщиком через атомарный Redis token bucket, и докажи нагрузочным тестом, что глобальный лимит держит, а контракт 429 корректен."
  requirements={[
    "Подними небольшой HTTP-сервис с rate-limit middleware, запусти минимум 3 инстанса за балансировщиком (nginx, Caddy или round-robin в Docker Compose). Лимитер должен ключевать пер-API-key, например 100 req/min с burst capacity 100.",
    "Сначала реализуй намеренно наивный пер-нодовый in-memory счётчик и нагрузи его через все ноды, чтобы продемонстрировать сбой: зафиксируй эффективный лимит и покажи, что он примерно в N раз выше настроенного.",
    "Замени его на token bucket, чьё состояние (tokens + timestamp последнего пополнения) живёт в Redis и оценивается единым Lua-скриптом, чтобы read-decide-update был атомарен. Никакого GET-затем-SET на стороне приложения.",
    "При отклонении верни 429 Too Many Requests с Retry-After (delta-seconds до освобождения следующего токена) и IETF-заголовками RateLimit-Limit / RateLimit-Remaining / RateLimit-Reset; RateLimit-Reset должен быть delta-seconds, а не UNIX timestamp.",
    "Поддержи минимум два пер-ключевых измерения — пер-API-key и пер-IP (или пер-эндпоинт) — и задокументируй компромисс честности каждого (пер-IP наказывает всех за корпоративным NAT; пер-key честен к платящим клиентам).",
    "Добавь jitter к Retry-After, чтобы дросселированные клиенты возвращались вразнобой, и объясни, как это и непрерывно пополняемое ведро избегают thundering herd на границе сброса.",
    "Напиши нагрузочный тест, который (a) шлёт устойчивый поток через все ноды и подтверждает, что глобальный принятый счёт отслеживает настроенный лимит, и (b) выстреливает всплеск через границу и показывает, что token bucket не протекает 2x всплеском, как сделал бы fixed window.",
  ]}
  acceptance={[
    "Результат до/после: наивный пер-нодовый лимитер пропускает ~N раз настроенный лимит под кросс-нодовым тестом; Redis token bucket пропускает в пределах малого допуска от настроенного лимита под идентичным тестом.",
    "Захваченный ответ 429, показывающий статус, Retry-After и три заголовка RateLimit-* с RateLimit-Reset, выраженным в delta-seconds.",
    "Доказательство атомарности проверки: тест конкурентности (много параллельных запросов по одному ключу) показывает отсутствие перепуска выше capacity, и показан Lua-скрипт, обрабатывающий весь read-decide-update.",
    "Короткий разбор: какой алгоритм и пер-ключевое измерение ты выбрал и почему, измеренный глобальный лимит через ноды, и как jitter плюс непрерывное пополнение защищают от thundering herd.",
  ]}
  stretch={[
    "Добавь вариант sliding-window-log для одного чувствительного эндпоинта (например, login) и сравни его след памяти на ключ с token bucket под той же нагрузкой.",
    "Сделай лимитер fail open vs fail closed, когда Redis недоступен, измерь стоимость задержки Redis round trip на пути запроса и добавь локальный in-process fallback с задокументированным компромиссом.",
    "Реализуй ярусные пер-ключевые quota (free vs paid), читаемые из конфига или заголовка, и покажи, что платный ключ получает больший burst и rate под тем же тестом.",
    "Добавь защиту от злоупотреблений: пер-ключевую дневную quota поверх пер-минутного rate, и покажи, что клиент, исчерпавший дневную quota, удерживается, даже оставаясь под пер-минутным rate.",
  ]}
/>

<Recap lang="ru">
Это лимитер, который ты реально отправишь: счётчик живёт в Redis, поэтому все ноды его делят, проверка — один атомарный Lua-скрипт, поэтому конкурентные запросы не могут дважды потратить токен, алгоритм (token bucket) подходит бёрстовому реальному трафику, а отклонение — это контракт: 429 с Retry-After в delta-seconds, заголовки RateLimit-* и jitter, чтобы граница сброса не стампедила. Построив его раз и увидев, как наивная версия проваливает кросс-нодовый тест, ты превращаешь ложь пер-нодового счётчика в то, что никогда не отправишь случайно.
</Recap>
