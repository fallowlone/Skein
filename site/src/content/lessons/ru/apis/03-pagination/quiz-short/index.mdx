---
concepts:
- offset-cost
- keyset-cursor
- result-drift
- stable-ordering
- opaque-cursor
- count-cost
estMin: 14
lang: ru
level: senior
order: 8
prereqs: []
slug: quiz-short
sources:
- https://blog.sequinstream.com/keyset-cursors-not-offsets-for-postgres-pagination/
- https://relay.dev/graphql/connections.htm
- https://wiki.postgresql.org/wiki/Slow_Counting
status: ready
summary: Подсказки на свободное припоминание по всему юниту. Сначала ответьте своими словами, затем откройте модельный ответ и сравните.
title: 'Pagination: тест на припоминание'
track: apis
unit: 03-pagination
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";

<Hook>
Припоминание сильнее перечитывания. На каждую подсказку проговорите или запишите полный ответ по памяти, прежде чем открыть модельный — именно усилие припоминания закрепляет механизм.
</Hook>

<Goal lang="ru">
Восстановите ключевые механизмы юнита — почему offset растёт с глубиной, почему он дрейфует, что делает keyset-ключ валидным, почему cursor непрозрачен и почему точный подсчёт дорог — не подглядывая в урок.
</Goal>

<RetrievalDrawer
  client:load
  id="03-pagination-short-retrieval"
  lang="ru"
  questions={[
    { q: "Почему LIMIT 20 OFFSET 200000 становится медленнее на глубине, хотя всегда возвращает 20 строк?", a: "У базы нет способа реально перепрыгнуть на строку 200000. Чтобы соблюсти ORDER BY, она выдаёт строки в порядке сортировки, читает и отбрасывает первые 200000 и только потом возвращает запрошенные 20. Работа пропорциональна значению offset, а не размеру страницы, поэтому ответ остаётся 20 строк, а стоимость растёт линейно с глубиной. На таблице в миллионы строк это толкает глубокие страницы от миллисекунд к многим секундам, и стоимость повторяется на каждой странице. Индекс может дёшево дать порядок, но не может убрать read-and-discard, потому что OFFSET по определению отсчитывает строки. Именно поэтому keyset, который ищет позицию по значению, а не считает, остаётся плоским." },
    { q: "Объясните result drift: почему offset-лента показывает дубликаты и пропуски и почему даже на маленькой таблице?", a: "Offset адресует страницы по позиции в живом, смещающемся результирующем наборе. Между двумя запросами набор может измениться: вставьте строку у начала, и все последующие строки сдвинутся вниз на одну позицию, поэтому элемент, бывший последним на странице 1, появится первым на странице 2 — пользователь видит его дважды. Удалите строку — всё сдвинется вверх, и один элемент, который должен был начать страницу 2, будет пропущен целиком. Это не проблема нагрузки или кэша; это происходит в момент, когда два запроса оказываются по разные стороны одной записи, что в любой активной ленте постоянно. На крошечных таблицах это по той же причине — позиция просто не является стабильным адресом, когда строки двигаются. Keyset чинит это привязкой к значению ключа конкретной строки, которое вставки и удаления в других местах не могут сдвинуть." },
    { q: "Какими двумя свойствами должен обладать keyset-ключ сортировки и что ломается, если ключ не уникален?", a: "Ключ сортировки должен быть уникальным и стабильным, и под ним должен быть составной индекс ровно в том порядке колонок и направлении, что и ORDER BY. Если ключ не уникален — например, сортировка только по created_at — две строки могут делить одно значение, и на стыке страниц сравнение границы не различит их: строка на стыке пропускается или повторяется. Исправление это добавить уникальный tiebreaker вроде первичного ключа и сравнивать кортеж целиком: ORDER BY (created_at, id) DESC с WHERE (created_at, id) < (lastCreatedAt, lastId). Без совпадающего составного индекса сравнение кортежа всё равно вернёт верные строки, но планировщик откатится к sort-and-scan, и вы потеряете плоскую задержку, ради которой всё затевалось." },
    { q: "Зачем отдавать opaque cursor вместо сырых ключей (created_at, id) и что клиент с ним делает?", a: "Cursor это граница контракта. Кодирование keyset в непрозрачную строку — обычно Base64-JSON значений сортировки, возвращаемый как nextCursor — позволяет клиенту обращаться с ним как с чёрным ящиком и слать обратно дословно. Это даёт серверу свободу позже сменить ключ сортировки, добавить tiebreaker или сменить хранилище, не ломая контракт, и не даёт клиентам собирать вручную cursor, уходящий в дорогую область seek. Спецификация Relay Connection в GraphQL формализует это: edges несут per-row значения cursor, а pageInfo несёт hasNextPage / endCursor. Сырые ключи, напротив, привязывают поверхность API к деталям внутренней схемы и раскрывают колонки сортировки каждому клиенту." },
    { q: "Что keyset отдаёт по сравнению с offset и когда offset всё ещё верный выбор?", a: "Keyset умеет двигаться только относительно известной строки — следующая или предыдущая страница от cursor — поэтому не может прыгнуть прямо на произвольную страницу N. Единственное настоящее преимущество offset ровно в этом: 'перейти на страницу 4000 из 80000' тривиально через LIMIT/OFFSET и невозможно через чистый cursor. Поэтому offset всё ещё подходит для ограниченных поверхностей низкой мощности, где пользователи законно прыгают: внутренняя админская таблица, отчёт с фиксированным небольшим числом страниц, поиск с потолком в пару сотен. Senior-ход это подбирать стратегию под паттерн доступа: keyset для публичных, глубоких, write-heavy лент, где нужны только next/prev; offset там, где произвольные переходы по страницам это реальное требование, а таблица или диапазон страниц малы настолько, что скан дёшев." },
    { q: "Почему точный итог дорог в Postgres и каковы альтернативы в порядке предпочтения?", a: "COUNT(*) нельзя прочитать из поля метаданных из-за MVCC: каждая транзакция видит свой набор видимых версий строк, поэтому движок обязан обойти строки и посчитать видимые именно вашей транзакции. На большой таблице это фактически полный скан, чья стоимость растёт с таблицей и часто превосходит сам запрос страницы. В порядке предпочтения: уберите точный итог — бесконечному скроллу и большинству списков нужно 'есть ли следующая страница', а не 'из 4 812 339'; используйте быструю оценку из pg_class.reltuples, которую ANALYZE держит свежей и которая обычно в пределах десяти процентов от истины, когда 'около 5 миллионов' приемлемо; или берите LIMIT n+1 и используйте только наличие лишней строки для выставления hasNextPage. Настоящий COUNT(*) оставьте для малых таблиц или админских вью, где точное число действительно нужно, а скан дёшев." },
  ]}
/>

<Recap lang="ru">
Если вы восстановили каждый ответ по памяти, у вас есть стержень юнита: стоимость offset растёт с глубиной, потому что движок читает и отбрасывает все предыдущие строки; offset дрейфует, потому что позиция не стабильный адрес при записи; валидный keyset-ключ уникален, стабилен и подкреплён индексом, сравнивается как целый кортеж; cursor непрозрачен, чтобы контракт мог эволюционировать, а клиенты не собирали дорогие seek (поиск по индексу); keyset обменивает произвольные переходы по страницам на плоскую задержку, поэтому offset всё ещё подходит ограниченным админским поверхностям; а точный подсчёт это полный скан под MVCC, поэтому оценивайте или зондируйте через n+1. Теперь, когда будете ревьюить list-эндпоинт, прокрутите эти шесть вопросов в голове — ответы скажут, выдержит ли дизайн десять миллионов строк и постоянные записи.
</Recap>
