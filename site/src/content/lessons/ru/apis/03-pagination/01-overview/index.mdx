---
slug: 01-overview
lang: ru
track: apis
unit: 03-pagination
order: 1
level: junior
lessonType: topic
status: ready
title: "Пагинация на масштабе: почему OFFSET умирает, а keyset-курсоры живут"
summary: "OFFSET заставляет базу просканировать и выбросить каждую строку до твоей страницы, поэтому глубокие страницы тормозят линейно и плывут при изменениях. Keyset-курсоры сикают прямо к следующей странице по индексу и держат latency ровным."
prereqs: []
deepensInto: []
spiral: []
concepts: ["offset-pagination", "keyset-pagination", "opaque-cursor", "composite-sort-key", "count-cost", "result-drift"]
estMin: 16
sources:
  - https://blog.sequinstream.com/keyset-cursors-not-offsets-for-postgres-pagination/
  - https://www.citusdata.com/blog/2016/10/12/count-performance/
  - https://wiki.postgresql.org/wiki/Slow_Counting
  - https://relay.dev/graphql/connections.htm
---

import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";

<Hook>
Лента с бесконечным скроллом плавная первые десять страниц, а потом сыплются тикеты: «приложение виснет, когда долго скроллишь». Эндпоинт — `?page=10000&size=20`. В стейджинге он отвечал за 8мс; в проде, на таблице в 50 миллионов строк, тот же запрос занимает 8.2 секунды и иногда отваливается по таймауту. Код никто не менял. План запроса всегда собирался делать именно это — он читает двести тысяч строк, выбрасывает все, кроме последних двадцати, и чем глубже скроллит пользователь, тем медленнее. Баг был в стратегии пагинации, присутствовал с первого дня, невидимый, пока таблица не выросла.
</Hook>

<Crux>OFFSET сканирует и выбрасывает все строки до страницы, поэтому глубокие страницы тормозят линейно; keyset сикает по индексу, держась ровно.</Crux>

<Explanation>

## Почему OFFSET тем медленнее, чем глубже

`LIMIT 20 OFFSET 200000` читается как «перепрыгни к строке 200000», но у базы нет способа перепрыгнуть. Она обязана выдать строки в отсортированном порядке, отсчитать первые 200000, выбросить их и только потом начать возвращать твои 20. Работа пропорциональна *оффсету*, а не размеру страницы. Страница 1 мгновенна; страница 10000 — это двести тысяч строк впустую потраченного I/O на каждый запрос, и эта цена повторяется для следующей страницы, и следующей.

```sql
-- Offset-пагинация: чем глубже страница, тем больше строк просканировано и выброшено
SELECT id, title, created_at
FROM articles
ORDER BY created_at DESC, id DESC
LIMIT 20 OFFSET 200000;   -- база читает 200020 строк, возвращает 20
```

Цифры жестокие и стабильные по разным бенчмаркам. На таблице в несколько миллионов строк offset-страница 1000 приземляется около 890мс, а страница 10000 вырастает примерно до 8.2 секунды — на порядок медленнее при том же payload, чисто потому что строк, которые надо пропустить, больше. Keyset-пагинация по тем же данным держится ровно: 8мс, 9мс, 11мс, 12мс, страница за страницей, потому что она вообще не отсчитывает прошлые строки. Заявленные ускорения — 17x на наборах в миллион строк и до 177x на тех глубоких страницах, где offset разваливается.

## Result drift: баг, который OFFSET прячет даже на маленьких таблицах

Даже когда offset достаточно быстр, он *неверен* при конкурентных записях. Границы страниц вычисляются по позиции в живом, сдвигающемся результирующем наборе. Вставь строку ближе к началу между страницей 1 и страницей 2 — и каждая последующая строка съезжает на одну вниз: элемент, который был последним на странице 1, снова появляется первым на странице 2 (дубликат, который пользователь видит дважды). Удали строку — и один элемент пропускается целиком, пользователь его не увидит. Этот drift — не проблема нагрузки; он случается на крошечной таблице в тот момент, когда два запроса оказываются по разные стороны записи, а на любой активной ленте это происходит постоянно.

<Inset kind="why" lang="ru">
Вот почему «бесконечный скролл на `?page=N`» ощущается как проклятый: пользователи быстрой ленты видят один пост дважды или клянутся, что элемент «исчез». Фронтенд корректен; модель пагинации позиционная, а позиция не стабильна, когда базовый набор меняется. Keyset чинит это бесплатно — он якорится на значении (`строка после created_at=X, id=Y`), и этот якорь остаётся валидным, сколько бы строк ни вставили или удалили вокруг него.
</Inset>

## Keyset (курсорная) пагинация: сикай, не пропускай

Keyset-пагинация заменяет «пропусти N строк» на «дай мне строки *после этой конкретной строки*». Ты сортируешь по стабильному, уникальному, проиндексированному ключу и переносишь значения ключа последней строки вперёд как курсор. Следующий запрос становится диапазонным сиком, который индекс удовлетворяет напрямую, без отсчёта.

```sql
-- Keyset-пагинация: сикаем сразу за последнюю увиденную строку, O(log n) вне зависимости от глубины
SELECT id, title, created_at
FROM articles
WHERE (created_at, id) < ('2026-05-01 09:00:00', 918273)  -- последняя строка предыдущей страницы
ORDER BY created_at DESC, id DESC
LIMIT 20;
```

Два требования, которые нельзя нарушать. Первое: ключ сортировки должен быть **уникальным и стабильным** — `created_at` сам по себе не уникален (две строки могут делить timestamp, из-за чего строка на стыке страниц теряется или дублируется), поэтому ты добавляешь tiebreaker вроде первичного ключа: сортируй по `(created_at, id)` и сравнивай кортеж целиком. Второе: нужен **составной индекс в точном порядке колонок и направлении** твоего `ORDER BY`:

```sql
CREATE INDEX idx_articles_created_id ON articles (created_at DESC, id DESC);
```

Без этого индекса сравнение кортежа в `WHERE` всё ещё работает, но планировщик откатывается к сортировке+скану, и ты теряешь ровный latency. С ним каждая страница — один диапазонный скан по индексу: база прыгает к позиции курсора и читает ровно 20 строк.

## Курсор непрозрачен, и это решение про контракт

Никогда не отдавай клиентам сырые `(created_at, id)`. Кодируй keyset в непрозрачную строку — Base64 от JSON со значениями сортировки это распространённый паттерн — и возвращай как `nextCursor`. Клиент обращается с ним как с чёрным ящиком и присылает обратно дословно. Это покупает свободу менять ключ сортировки, добавлять поля или переезжать на другое хранилище позже, не ломая контракт клиента; это также не даёт клиентам собирать руками курсоры, которые сикают в дорогую область. Спека Relay Connection в GraphQL формализует ровно это: `edges` несут покурсорные значения `cursor`, а `pageInfo` несёт `hasNextPage` / `endCursor`. Для двунаправленной пагинации ты поддерживаешь и `after`/`first`, и `before`/`last`, переворачивая оператор сравнения (`<` становится `>`) и направление `ORDER BY`, а затем разворачивая возвращённый срез, чтобы клиент видел стабильный порядок.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Свойство</th>
        <th class="border border-border px-3 py-2 text-left">Offset / limit</th>
        <th class="border border-border px-3 py-2 text-left">Keyset / курсор</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">Latency глубокой страницы</td>
        <td class="border border-border px-3 py-2">Растёт линейно (~8.2с на странице 10k)</td>
        <td class="border border-border px-3 py-2">Ровный (~10мс на любой странице)</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Стабильность при вставках/удалениях</td>
        <td class="border border-border px-3 py-2">Нет — дубликаты и пропуски</td>
        <td class="border border-border px-3 py-2">Да — якорь на значении</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Прыжок на произвольную страницу N</td>
        <td class="border border-border px-3 py-2">Да (его единственное преимущество)</td>
        <td class="border border-border px-3 py-2">Нет — только next/prev</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Кэш-дружелюбность</td>
        <td class="border border-border px-3 py-2">Плохая — страница N со временем съезжает</td>
        <td class="border border-border px-3 py-2">Хорошая — курсор мапится на фиксированные строки</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Нужен составной индекс</td>
        <td class="border border-border px-3 py-2">Нет (но всё равно медленно)</td>
        <td class="border border-border px-3 py-2">Да (под порядок сортировки)</td>
      </tr>
    </tbody>
  </table>
</div>

## Скрытая цена: тотальные счётчики

Даже с keyset футер «Показано 1–20 из 4 812 339» — это своя ловушка производительности. В Postgres `COUNT(*)` нельзя ответить из метаданных, потому что MVCC означает, что каждая транзакция видит свой набор видимых строк — движку приходится обойти строки, чтобы посчитать те, что видимы *тебе*. На большой таблице это полный скан, который тем медленнее, чем больше таблица, и часто затмевает страничный запрос, к которому прилагается. Ходы сеньора: убрать точный тотал (большинству продуктов он не нужен), взять быструю оценку из `pg_class.reltuples` (поддерживается актуальной через `ANALYZE`, обычно в пределах ~10% от настоящего значения) или достать `LIMIT n+1` и сообщать только «есть следующая страница» — а это всё, что бесконечному скроллу когда-либо реально нужно.

<TradeoffMatrix
  id="01-overview-tradeoff-1"
  lessonSlug="01-overview"
  lang="ru"
  prompt="Соцлента показывает посты сначала новые в бесконечном скролле на таблице в 50M строк, с постоянными вставками. Выбери стратегию пагинации."
  options={[
    { name: "Keyset-курсор на (created_at, id) с подходящим составным индексом, непрозрачный Base64-курсор", summary: "Сикай, не пропускай.", correct: true, justification: "Это учебниковое попадание. Бесконечному скроллу нужна только следующая страница, которую keyset даёт ровно за ~10мс на любой глубине через один диапазонный скан по индексу. Кортеж (created_at, id) уникален и стабилен, поэтому на стыках страниц нет дублей и пропусков даже при постоянных вставках — курсор якорится на значении, а не на позиции. Непрозрачный курсор оставляет контракт на проводе свободным к эволюции." },
    { name: "Offset/limit через ?page=N&size=20", summary: "Классический пагинатор.", justification: "Тут сразу два провала. Глубокие страницы сканируют и выбрасывают всё до себя, поэтому страница 10000 вырастает к 8 секундам и может отвалиться по таймауту на таблице в 50M строк. И при постоянных вставках позиционные границы плывут, так что пользователи видят дубли постов и пропускают другие — ровно те тикеты «приложение виснет / элемент исчез»." },
    { name: "Offset/limit, но кэшировать каждую страницу в Redis на 5 минут", summary: "Залатать кэшем.", justification: "Кэш не чинит ни одну из корневых причин. Первый некэшированный хит по глубокой странице всё равно делает полный offset-скан, а кэшированные страницы устаревают мгновенно при постоянных вставках — кэшированная сейчас страница 2 уже содержит строки, которые сдвинулись, так что ты отдаёшь дубли и пропуски, ещё и с лишней инфраструктурой сверху." },
    { name: "Загрузить все 50M строк один раз и пагинировать в памяти приложения", summary: "Пагинация в приложении.", justification: "Нестартер на таком масштабе: 50M строк в процессе не удержать, начальный фетч — самый медленный возможный запрос, а снимок в памяти устаревает в тот же миг, когда приземляется следующая вставка. Пагинация существует ровно для того, чтобы не материализовать весь набор." },
  ]}
/>

<Quiz
  id="01-overview-quiz-1"
  lessonSlug="01-overview"
  lang="ru"
  question="Почему LIMIT 20 OFFSET 200000 становится медленнее с ростом оффсета, хотя возвращает всего 20 строк?"
  choices={[
    { label: "База обязана выдать строки в отсортированном порядке и прочитать+выбросить все 200000 строк до страницы; работа масштабируется с оффсетом, а не с размером страницы", correct: true },
    { label: "Payload результата больше для более глубоких страниц", misconception: "Payload всегда 20 строк. Цена — это 200000 строк, которые движок читает и выбрасывает, чтобы до них добраться, и этого не видно в размере ответа." },
    { label: "Глубокие страницы вообще не могут использовать индекс", misconception: "Индекс может использоваться, чтобы упорядочить строки, но всё равно надо пройти мимо каждой offset-строки, чтобы их отсчитать — цена именно в скане-и-выбросе, а не в отсутствии индекса." },
    { label: "OFFSET блокирует таблицу на время отсчёта", misconception: "Он не блокирует; это чтение. Медлительность — это линейная работа скана-и-выброса, пропорциональная значению оффсета." },
  ]}
/>

<Quiz
  id="01-overview-quiz-2"
  lessonSlug="01-overview"
  lang="ru"
  question="Почему сортировка только по created_at небезопасна для keyset-пагинации и что это чинит?"
  choices={[
    { label: "created_at не уникален — две строки могут делить timestamp, поэтому строка теряется или дублируется на границе страницы; добавь tiebreaker вроде id и сравнивай кортеж (created_at, id)", correct: true },
    { label: "created_at — строка и её нельзя сравнивать", misconception: "Timestamp-ы сравниваются нормально. Реальная проблема — неуникальность: ничьи на стыке страниц ломают сик, пока ты не добавишь уникальный tiebreaker." },
    { label: "Надо сортировать только по id и игнорировать created_at", misconception: "Сортировка только по id меняет порядок, который хочет продукт (сначала новые). Оставь created_at и добавь id как уникальный tiebreaker." },
    { label: "Ничего — только created_at это нормально", misconception: "Это реальный баг: любой дублирующийся timestamp на границе страницы может пропустить или повторить строку. Фикс — составной ключ (created_at, id)." },
  ]}
/>

<DragOrder
  id="01-overview-drag-1"
  lessonSlug="01-overview"
  lang="ru"
  prompt="Расставь шаги, чтобы отдать одну keyset-страницу по входящему курсору:"
  items={[
    { id: "a", label: "Декодируй непрозрачный курсор (Base64 JSON) в (created_at, id) последней строки" },
    { id: "b", label: "Выполни WHERE (created_at, id) < (?, ?) ORDER BY created_at DESC, id DESC LIMIT n+1" },
    { id: "c", label: "Используй (n+1)-ю строку только чтобы выставить hasNextPage, затем выкинь её из страницы" },
    { id: "d", label: "Закодируй (created_at, id) последней возвращённой строки как новый nextCursor" },
    { id: "e", label: "Верни клиенту n строк плюс pageInfo (nextCursor, hasNextPage)" },
  ]}
  correctOrder={["a", "b", "c", "d", "e"]}
/>

</Explanation>

<KeyTakeaway>OFFSET сканирует и выбрасывает все прошлые строки: глубокие страницы тормозят линейно и плывут при записях. Keyset сикает по уникальному индексу — ровный latency, без дублей. Избегай COUNT(*); бери LIMIT n+1.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="01-overview-retrieval"
  lang="ru"
  questions={[
    { q: "Объясни коллеге, почему offset-лента с бесконечным скроллом И тормозит на глубине, И показывает дубли или пропуски, и как keyset-курсоры чинят оба бага.", a: "Два отдельных провала делят один корень: offset адресует страницы по позиции в отсортированном результирующем наборе. Провал производительности в том, что база не может по-настоящему «пропустить» — чтобы отдать LIMIT 20 OFFSET 200000, она выдаёт строки по порядку и читает и выбрасывает первые 200000, поэтому цена масштабируется со значением оффсета, а не с 20 строками, которые ты получаешь обратно. На таблице в несколько миллионов строк это толкает глубокие страницы с долей секунды к ~8 секундам и может уронить запрос по таймауту. Провал корректности в том, что позиции нестабильны: когда строку вставляют ближе к началу между двумя запросами, всё съезжает на одну вниз, поэтому последний элемент страницы 1 снова появляется в начале страницы 2 (дубликат); удаление сдвигает в другую сторону, и элемент пропускается целиком. Keyset чинит оба, якорясь на значении вместо позиции: ты сортируешь по уникальному проиндексированному ключу вроде (created_at, id), и следующая страница — это WHERE (created_at, id) < (последняя увиденная) ORDER BY ... LIMIT n. Это один диапазонный сик по индексу — ровный latency на любой глубине — и поскольку якорь это ключ конкретной строки, вставки и удаления в других местах не могут сдвинуть границу, поэтому ни дублей, ни пропусков." },
    { q: "Почему показ точного тотального счётчика дорог и что делают сеньоры вместо этого?", a: "В Postgres COUNT(*) нельзя прочитать из поля метаданных из-за MVCC: каждая транзакция видит свой набор видимых версий строк, поэтому движку приходится реально обойти строки, чтобы посчитать видимые твоей транзакции. На большой таблице это фактически полный скан, чья цена растёт вместе с таблицей и часто стоит больше, чем сам страничный запрос. Три распространённые альтернативы по убыванию предпочтения: убрать точный тотал — большинству UI и всем лентам с бесконечным скроллом не нужно «из 4 812 339», им нужно «есть ли следующая страница»; взять быструю оценку из pg_class.reltuples, которую ANALYZE держит актуальной и которая обычно в пределах около 10% от истины, когда приемлемо приблизительное «примерно 5 миллионов»; или достать LIMIT n+1 и использовать наличие этой лишней строки чисто чтобы выставить hasNextPage. Прибереги настоящий COUNT(*) для маленьких таблиц или админских view, где точное число действительно нужно, а таблица достаточно мала, чтобы скан был дёшев." },
  ]}
/>

<Recap lang="ru">
Стратегия пагинации — это решение про производительность и корректность, принимаемое до того, как таблица большая, и оно остаётся невидимым, пока не перестаёт. OFFSET адресует страницы по позиции, поэтому база читает и выбрасывает каждую строку до твоей страницы — глубокие страницы тормозят линейно, вырастая с миллисекунд до многих секунд на больших таблицах — а позиционные границы плывут при конкурентных вставках и удалениях, всплывая как дубли и пропавшие элементы на любой активной ленте. Keyset (курсорная) пагинация заменяет «пропусти N» на «сикай за эту строку»: сортируй по уникальному, стабильному, проиндексированному ключу вроде (created_at, id), сравнивай кортеж целиком в WHERE, опираясь на составной индекс в подходящем направлении, и latency держится ровно на любой глубине, без дублей и пропусков. Отдавай клиентам непрозрачный курсор, а не сырые ключи, чтобы контракт мог эволюционировать и клиенты не собирали дорогие сики; поддерживай before/after для двунаправленной пагинации. Наконец, относись к тотальному счётчику как к своей цене: COUNT(*) — это полный скан под MVCC, поэтому предпочитай оценку reltuples или пробу LIMIT n+1 «есть следующая страница» точному числу, которое никому не нужно.
</Recap>
