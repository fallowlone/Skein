---
concepts:
- offset-cost
- keyset-cursor
- result-drift
- stable-ordering
- opaque-cursor
- count-cost
estMin: 13
lang: ru
level: senior
order: 7
prereqs: []
slug: quiz-choice
sources:
- https://blog.sequinstream.com/keyset-cursors-not-offsets-for-postgres-pagination/
- https://relay.dev/graphql/connections.htm
- https://wiki.postgresql.org/wiki/Slow_Counting
status: ready
summary: "Синтез по всему юниту в формате выбора: стоимость offset против keyset, дрейф страниц при записи, стабильный ordering, opaque cursor и скрытый налог на COUNT."
title: 'Pagination: тест с выбором ответа'
track: apis
unit: 03-pagination
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Шесть вопросов на весь юнит. Ни один не про определение — каждый это решение, которое вы принимаете, проектируя реальный list-эндпоинт, обязанный пережить таблицу на 50 миллионов строк и постоянные записи.
</Hook>

<Goal lang="ru">
Убедитесь, что вы связываете стоимость offset, дрейф страниц, стабильность ordering, контракт opaque cursor и налог на подсчёт — это синтез, к которому вёл урок, а не отдельные факты.
</Goal>

<Quiz
  id="03-pagination-choice-1"
  lessonSlug="03-pagination"
  lang="ru"
  question="Эндпоинт выполняет LIMIT 20 OFFSET 200000 по таблице на 50M строк. Возвращает 20 строк, но занимает ~8 секунд. Почему стоимость растёт с offset, а не с размером страницы?"
  choices={[
    { label: "У движка нет способа реально пропустить строки — он обязан выдать строки в порядке сортировки, прочитать и отбросить первые 200000, и только потом вернуть 20. Работа пропорциональна offset, а не размеру ответа.", correct: true },
    { label: "Ответ из 20 строк больше на глубоких страницах, поэтому доминирует сериализация.", misconception: "Ответ всегда 20 строк, его размер постоянен. Стоимость это 200000 строк, которые движок читает и выбрасывает по пути к ним — в размере ответа их не видно." },
    { label: "Глубокие страницы вообще не могут использовать индекс, поэтому каждый раз сканируется вся таблица.", misconception: "Индекс по-прежнему упорядочивает строки; движку просто приходится пройти мимо каждой offset-строки, чтобы их отсчитать. Это работа scan-and-discard, а не отсутствие индекса." },
    { label: "OFFSET берёт блокировку во время подсчёта, и конкуренция за блокировку добавляет задержку.", misconception: "OFFSET это обычное чтение и такой блокировки не берёт. Задержка это линейная работа read-and-discard, пропорциональная значению offset." },
  ]}
/>

<Quiz
  id="03-pagination-choice-2"
  lessonSlug="03-pagination"
  lang="ru"
  question="В быстрой ленте пользователи видят один и тот же пост дважды, а другие клянутся, что посты 'исчезли'. Фронтенд корректен, таблица маленькая. В чём причина?"
  choices={[
    { label: "Offset адресует страницы по позиции в живом результирующем наборе. Вставка строки у начала между страницей 1 и страницей 2 сдвигает все последующие строки вниз на одну, поэтому последний элемент страницы 1 появляется на странице 2 (дубликат); удаление сдвигает в другую сторону и элемент пропускается.", correct: true },
    { label: "Слой кэширования отдаёт устаревшие страницы.", misconception: "Никакого кэша нет, и симптом появляется в момент, когда два запроса оказываются по разные стороны одной записи. Дрейф внутренне присущ позиционной пагинации, а не кэшу." },
    { label: "Размер страницы слишком мал, поэтому строки проваливаются между страницами.", misconception: "Размер страницы не вызывает дрейф; при любом размере те же позиционные границы. Дубликаты и пропуски даёт смещение границы при вставках/удалениях." },
    { label: "Всё нормально — дубликаты ожидаемы в любой ленте.", misconception: "Дубликаты и пропуски это реальный баг корректности позиционной пагинации. Keyset с привязкой к значению их устраняет." },
  ]}
/>

<Quiz
  id="03-pagination-choice-3"
  lessonSlug="03-pagination"
  lang="ru"
  question="Keyset-лента сортирует только по created_at DESC и несёт последний created_at как cursor. Иногда пост на стыке страниц пропускается или показывается дважды. Почему и что это исправляет?"
  choices={[
    { label: "created_at не уникален — две строки могут делить одну метку времени, поэтому сравнение границы теряет или повторяет строку на стыке. Добавьте уникальный tiebreaker и сравнивайте кортеж целиком: ORDER BY (created_at, id) с WHERE (created_at, id) &lt; (?, ?).", correct: true },
    { label: "Метки времени хранятся как строки и сравниваются лексически, ломая порядок.", misconception: "Метки времени сравниваются корректно как значения. Дефект в неуникальности: совпадения на стыке ломают seek, если не добавить уникальный tiebreaker вроде первичного ключа." },
    { label: "Уберите created_at и сортируйте только по id.", misconception: "Сортировка только по id отменяет порядок newest-first, нужный продукту. Оставьте created_at и добавьте id как уникальный tiebreaker, чтобы держались и ordering, и стабильность." },
    { label: "Увеличьте LIMIT, чтобы стык никогда не попадал на дублирующуюся метку времени.", misconception: "Большая страница не делает ключ уникальным; совпадения всё равно могут попасть на любую границу. Исправление это уникальный составной ключ сортировки, а не больший размер страницы." },
  ]}
/>

<Quiz
  id="03-pagination-choice-4"
  lessonSlug="03-pagination"
  lang="ru"
  question="Продукту нужны и админская таблица с 'перейти на страницу 4000 из 80000', и публичная лента с бесконечным скроллом по тем же данным. Какое решение верное?"
  choices={[
    { label: "Разные стратегии на разные поверхности: keyset для ленты (нужны только next/prev, остаётся плоской на глубине) и offset для ограниченной админской таблицы, где переход на произвольную страницу это реальное требование.", correct: true },
    { label: "Используйте keyset везде — он строго лучше.", misconception: "Keyset не умеет прыгать на произвольную страницу N; он только идёт next/prev. Требование 'перейти на страницу 4000' это ровно единственное реальное преимущество offset." },
    { label: "Используйте offset везде ради единого кода.", misconception: "Offset на публичной ленте возвращает медленные глубокие страницы и дрейф при записи — те самые отказы, о которых юнит. Подбирайте стратегию под паттерн доступа поверхности." },
    { label: "Используйте keyset, затем синтезируйте номер страницы, считая cursor на клиенте.", misconception: "Подсчёт cursor для подделки номеров страниц всё равно требует знать общую позицию — это и есть тот offset-скан, которого вы избегали. Произвольные переходы это домен offset по своей природе." },
  ]}
/>

<Quiz
  id="03-pagination-choice-5"
  lessonSlug="03-pagination"
  lang="ru"
  question="Коллега предлагает вернуть сырой кортеж (created_at, id) как cursor, 'раз клиент всё равно просто шлёт его обратно'. Почему лучше непрозрачный (opaque) cursor?"
  choices={[
    { label: "Opaque cursor (например, Base64-JSON) это граница контракта: сервер может сменить ключ сортировки, добавить tiebreaker или сменить хранилище, не ломая клиентов, а клиенты не могут вручную собрать cursor, уходящий в дорогую область seek.", correct: true },
    { label: "Opaque cursor декодируется быстрее, чем сырой кортеж.", misconception: "Стоимость декодирования ничтожна в обоих случаях. Причина в эволюции контракта и защите от рукотворных cursor — не в скорости декодирования." },
    { label: "Сырой кортеж нельзя передать по JSON.", misconception: "Кортеж нормально сериализуется в JSON. Суть в том, что раскрытие внутренних ключей привязывает контракт к вашей схеме и позволяет клиентам собирать произвольные seek." },
    { label: "Opaque cursor позволяет клиентам прыгать на любую страницу.", misconception: "Непрозрачность не связана с произвольными переходами; keyset всё равно идёт только next/prev. Непрозрачность защищает контракт и ограничивает рукотворные cursor." },
  ]}
/>

<Quiz
  id="03-pagination-choice-6"
  lessonSlug="03-pagination"
  lang="ru"
  question="Эндпоинт теперь быстр через keyset, но p99 всё ещё высок из-за подвала 'Показано 1–20 из 4 812 339'. Почему точный подсчёт дорог в Postgres и какой senior-ход?"
  choices={[
    { label: "Из-за MVCC каждая транзакция видит свой набор видимых версий строк, поэтому COUNT(*) обязан пройти строки и посчитать видимые именно вам — фактически полный скан, растущий с таблицей. Уберите точный итог, используйте оценку pg_class.reltuples или возьмите LIMIT n+1, чтобы сообщить только 'есть следующая страница'.", correct: true },
    { label: "COUNT(*) блокирует таблицу, поэтому ждёт за писателями.", misconception: "COUNT(*) не блокирует; это чтение. Стоимость это обход строк под MVCC для определения видимости, скан, растущий с таблицей." },
    { label: "С подсчётом всё нормально — читайте его из метаданных таблицы за O(1).", misconception: "Postgres не может прочитать точный видимый счёт из метаданных именно потому, что MVCC даёт каждой транзакции свой видимый набор. reltuples это оценка, а не точные метаданные." },
    { label: "Добавьте индекс по первичному ключу, чтобы COUNT(*) стал мгновенным.", misconception: "Индекс не даёт точный видимый счёт под MVCC; движок всё равно обходит строки ради видимости. Исправление это оценить или взять LIMIT n+1, а не индексировать сильнее." },
  ]}
/>

<Recap lang="ru">
Сквозная линия юнита это одно решение: адресуете страницы по позиции (offset) — платите дважды: глубокие страницы линейно делают scan-and-discard, а позиционные границы дрейфуют при записи в дубликаты и пропуски. Адресуете по значению (keyset) по уникальному, стабильному, индексированному ключу — задержка остаётся плоской без дрейфа, ценой потери произвольных переходов по страницам, поэтому offset всё ещё уместен на ограниченных админских таблицах. Отдавайте клиентам opaque cursor, чтобы контракт мог эволюционировать, и относитесь к точному итогу как к отдельному скану: оценивайте или зондируйте через LIMIT n+1 вместо подсчёта, который никому не нужен.
</Recap>
