infographic hierarchy-tree-curved-line-rounded-rect-node
data
  root
    label API = контракт
    children
      - label Запрос (клиент → сервер)
        children
          - label HTTP-метод (GET / POST / PUT)
          - label Эндпоинт (URL)
          - label Ресурс (существительное)
          - label Тело запроса (опционально)
      - label Ответ (сервер → клиент)
        children
          - label Статус-код (200 / 404 / 500)
          - label Тело ответа (результат)
          - label JSON (общий формат данных)
theme
  palette #6366f1 #14b8a6 #f59e0b
