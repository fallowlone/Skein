---
concepts:
- graphql-nplus1
- resolver-isolation
- lazy-loading
- query-planner
deepensInto:
- apis/06-graphql-n-plus-one/02-dataloader-mechanics
estMin: 10
lang: ru
lessonType: topic
level: junior
order: 1
prereqs:
- performance/05-n-plus-one/01-what-is-nplus1
slug: 01-why-graphql-gets-nplus1
sources:
- https://www.apollographql.com/docs/graphos/schema-design/guides/handling-n-plus-one
- https://graphql.org/learn/execution/
- https://shopify.engineering/solving-the-n-1-problem-for-graphql-through-batching
spiral:
- statefulness
status: ready
summary: 'Модель независимых резолверов GraphQL даёт клиенту гибкость, а серверу —
  случайный шторм запросов: один HTTP-запрос превращается в сотни SQL-вызовов.'
title: Почему GraphQL получает N+1
track: apis
unit: 06-graphql-n-plus-one
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import NumbersCard from "~/components/layout/NumbersCard.astro";
import SequenceDiagram from "~/components/diagram/SequenceDiagram.astro";
import Infographic from "~/components/diagram/Infographic.astro";
import restVsGraphql from "./infographics/rest-vs-graphql.svg?raw";

<Hook>
Bea просит `posts { title, author { name } }` — один HTTP-запрос на 50 постов. Sven смотрит в лог базы данных: 51 запрос. Схема верна. Никаких ошибок. Это GraphQL, работающий точно по задумке — и обходящийся в 600 мс вместо 80 мс.
</Hook>

<Crux>Один GraphQL-запрос на 1+N строк — 1+N походов в БД. REST пишет JOIN вручную; резолверы изолированы и не имеют общего плана.</Crux>

Через десять минут ты будешь точно знать, почему изоляция порождает этот шторм — и какое структурное изменение сворачивает 51 запрос обратно в 2.

<Explanation>

## Как GraphQL выполняет запрос

GraphQL разрешает документ, обходя его в глубину и в ширину на каждом уровне. На каждое поле, которое выбрал клиент, сервер вызывает резолвер, зарегистрированный для пары `Type.field`. Резолверы получают `(parent, args, context, info)` и возвращают значение или Promise. Они намеренно изолированы: каждый резолвер не видит соседей, не знает, как родитель получил данные, и не может посмотреть, какие ещё резолверы запустятся.

Именно эта изоляция делает GraphQL компонуемым — можно добавить поле к типу, не трогая остальные резолверы. Но она же является прямой причиной N+1.

## Почему изоляция порождает N+1

Возьмём запрос `posts { title, author { name } }`. Движок выполнения:

1. Вызывает `Query.posts` → выполняет `SELECT * FROM posts LIMIT 50` → возвращает 50 объектов постов.
2. Для каждого из 50 постов вызывает `Post.author(parent=post, ...)` → выполняет `SELECT * FROM users WHERE id = post.author_id` → 50 отдельных запросов.

Итого: 51 поездка в базу данных на один HTTP-запрос.

REST-контроллер для того же эндпоинта написал бы `SELECT posts JOIN users ON users.id = posts.author_id` — один запрос. Разработчик видит всю форму ответа в одном месте и может оптимизировать SQL. В GraphQL резолвер `Post.author` видит только один пост за раз. Нет глобальной точки планирования.

<Infographic
  svg={restVsGraphql}
  label="Сравнение в две колонки: REST-контроллер против резолверов GraphQL для одного и того же запроса постов с авторами. REST пишется вручную под эндпоинт, заранее пишет JOIN, имеет один глобальный план запроса и стоит 1 обращение к БД. Резолверы GraphQL изолированы по Type.field, им негде написать JOIN, нет глобального плана запроса, и стоят они 1 + N обращений — 51 на 50 постов."
  caption="Та же композируемость, что позволяет добавить поле, не трогая остальные, убирает глобальный план, на который опирался REST — поэтому изоляция, а не ошибка, превращает 1 запрос в 51."
  hue="var(--d-infra)"
/>

## Метафора официанта

Ресторан, где каждый стол заказывает блюда по одному. Стол на шестерых отправляет официанта шесть раз — по разу на блюдо. Кухня быстрая, но официант проводит смену в дороге. Скорость кухни не имеет значения, когда ходьба и есть затраты.

DataLoader — менеджер, говорящий: держи талоны, пока не хлопну, потом неси всё сразу. Хлопок — один тик event loop. Этот урок объясняет проблему; следующие два — решение.

<div data-lesson-visual class="overflow-x-auto my-6">

| Без DataLoader | С DataLoader |
|---|---|
| `Query.posts` → 1 SQL | `Query.posts` → 1 SQL |
| `Post.author(post1)` → 1 SQL | `Post.author` × 50 → ставим в очередь 50 ID |
| `Post.author(post2)` → 1 SQL | DataLoader делает 1 SQL на все 50 |
| ... (50 строк) | Итого: **2 SQL** |
| Итого: **51 SQL** | |

</div>

<NumbersCard
  id="card-nplus1-basics-ru"
  title="GraphQL N+1: ключевые цифры"
  stats={[
    { label: "Наивные поездки на 50 постов × 1 автор", value: "51 (1 + 50)" },
    { label: "С DataLoader", value: "2 (1 + 1)" },
    { label: "Сообщённый ускоритель DataLoader", value: "64–84% (реляционные данные)" },
    { label: "Скоуп DataLoader", value: "per-request, не глобальный" },
  ]}
/>

<Quiz
  id="nplus1-basics-q1"
  lessonSlug="01-why-graphql-gets-nplus1"
  lang="ru"
  question="Что значит «N+1» в контексте GraphQL?"
  choices={[
    { label: "Глубина запроса ровно N+1 уровней", misconception: "N+1 описывает число вызовов, а не уровень вложенности. Глубина — отдельный лимит." },
    { label: "Один запрос за списком из N элементов, затем N запросов — по одному на элемент — за вложенными полями", correct: true },
    { label: "В схеме N+1 типов и один корневой запрос", misconception: "Количество типов не связано. N+1 — паттерн времени разрешения запроса." },
    { label: "GraphQL кеширует N запросов и инвалидирует один", misconception: "Спецификация не определяет автокеша." },
  ]}
/>

<Quiz
  id="nplus1-basics-q2"
  lessonSlug="01-why-graphql-gets-nplus1"
  lang="ru"
  question="Почему GraphQL ловит N+1, а REST часто нет?"
  choices={[
    { label: "REST быстрее GraphQL", misconception: "Скорость не причина. REST избегает N+1, потому что сервер контролирует форму ответа на каждый endpoint." },
    { label: "REST-эндпоинты пишут вручную под use case, поэтому разработчик заранее джойнит данные; GraphQL-резолверы запускаются независимо и не имеют общего плана", correct: true },
    { label: "GraphQL-серверы однопоточные", misconception: "Большинство GraphQL-серверов асинхронны; потоки не виноваты." },
    { label: "REST использует HTTP, а GraphQL нет", misconception: "GraphQL тоже работает поверх HTTP. Различие — в способе разрешения, не в транспорте." },
  ]}
/>

<DragOrder
  id="nplus1-basics-drag1"
  lessonSlug="01-why-graphql-gets-nplus1"
  lang="ru"
  prompt="Поставь шаги наивного GraphQL-разрешения в порядок:"
  items={[
    "Клиент отправляет один GraphQL-запрос: посты и их авторы",
    "Сервер разрешает корневое поле posts и получает 50 постов",
    "Для каждого поста резолвер Post.author запускается — 50 независимых вызовов",
    "Каждый резолвер выполняет SELECT * FROM users WHERE id = post.author_id",
    "База данных выполняет 50 отдельных запросов, по одному на пост",
    "GraphQL собирает финальный ответ и отправляет клиенту",
  ]}
/>

</Explanation>

<SequenceDiagram
  label="Развёртка N+1: один запрос за списком влечёт N запросов за авторами"
  hue="var(--d-backend)"
  actors={["Resolver", "Database"]}
  messages={[
    { from: "Resolver", to: "Database", label: "SELECT * FROM posts LIMIT 50" },
    { from: "Database", to: "Resolver", label: "→ 50 строк постов" },
    { from: "Resolver", to: "Database", label: "Post.author(post1): id = author_id" },
    { from: "Resolver", to: "Database", label: "Post.author(post2): id = author_id" },
    { from: "Resolver", to: "Database", label: "… ещё 48 запросов за авторами" },
  ]}
  caption="1 запрос за списком + 50 изолированных резолверов авторов = 51 поездка. Общего плана для джойна нет."
/>

<KeyTakeaway>GraphQL-резолверы изолированы по замыслу — общего плана запроса нет. Эта изоляция порождает N+1: один запрос за списком влечёт N per-item запросов. DataLoader — структурное решение.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="nplus1-basics-retrieval"
  lang="ru"
  questions={[
    { q: "Почему GraphQL получает N+1, а REST-контроллер на тех же данных обычно нет?", a: "REST-контроллер видит всю форму ответа в одном месте и разработчик естественно пишет JOIN. GraphQL-резолверы изолированы — каждый знает только своё поле и объект-родитель, поэтому нет естественного места написать JOIN. Движок вызывает каждый резолвер независимо, производя по одному запросу на родителя." },
    { q: "В одном предложении: каково решение проблемы GraphQL N+1?", a: "DataLoader объединяет все вызовы .load(id) из одного тика event loop в один SQL-запрос, сворачивая N per-item запросов в один." },
  ]}
/>

<Recap lang="ru">
Выполнение GraphQL — поле за полем, резолвер за резолвером. Когда список из N элементов требует вложенного fetch, сервер делает 1+N запросов к базе по умолчанию — не из-за ошибки, а потому что изоляция резолверов — то, что делает GraphQL компонуемым. Страница с 50 постами и их авторами стоит 51 поездку без вмешательства. DataLoader, рассмотренный в следующем уроке, сворачивает 50 per-author поездок в одну, снижая итог с 51 до 2. Теперь, когда видишь в логах резолверов поток одиночных SELECT-запросов, ты сразу распознаешь N+1 — и поймёшь, что решение структурное, а не точечный JOIN.
</Recap>
