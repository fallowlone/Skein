infographic compare-binary-horizontal-underline-text-vs
data
  title Error в слоте против throw из batchLoadFn
  desc Один и тот же сбой — противоположный радиус поражения
  compares
    - label Вернуть Error в слоте
      children
        - label Отклоняет только этот один Promise из .load()
        - label Остальные резолверы в батче разрешаются нормально
        - label Для известных сбоев: нет строки, нет прав
        - label Apollo вернёт частичный ответ: одно null-поле, одна ошибка
    - label Бросить throw из batchLoadFn
      children
        - label Отклоняет ВСЕ ожидающие Promise из .load() в батче
        - label Каждый резолвер запроса получает отклонение
        - label Падают даже ключи, чьи строки вернулись успешно
        - label Только для катастрофы: база данных недоступна
theme
  palette #14b8a6 #f43f5e
