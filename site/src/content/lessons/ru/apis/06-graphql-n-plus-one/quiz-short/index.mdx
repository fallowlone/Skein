---
concepts:
- resolver-isolation
- tick-boundary-batching
- per-request-scope
- batch-contract
- federation-entities
- query-complexity
estMin: 14
lang: ru
level: senior
order: 8
prereqs: []
slug: quiz-short
sources:
- https://github.com/graphql/dataloader
- https://www.apollographql.com/docs/graphos/schema-design/guides/handling-n-plus-one
- https://graphql.org/learn/security/
status: ready
summary: "Промпты на свободное припоминание через весь юнит GraphQL N+1. Сначала ответь своими словами, затем открой модельный ответ и сравни."
title: 'GraphQL N+1: повторение на припоминание'
track: apis
unit: 06-graphql-n-plus-one
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";

<Hook>
Припоминание сильнее перечитывания. На каждый промпт скажи или напиши полный ответ по памяти, прежде чем открыть модельный — усилие припоминания и закрепляет механизм, когда ты в следующий раз увидишь страницу на 51 запрос.
</Hook>

<Goal lang="ru">
Восстанови хребет юнита — почему изоляция порождает N+1, как DataLoader батчит и почему важен scope, контракт batch, federation и слои защиты по форме запроса — не подглядывая в уроки.
</Goal>

<RetrievalDrawer
  client:visible
  id="06-graphql-n-plus-one-short-retrieval"
  lang="ru"
  questions={[
    { q: "Почему GraphQL получает N+1, когда REST-контроллер на тех же данных обычно нет?", a: "Резолверы GraphQL изолированы по дизайну: Post.author получает один пост за раз и ничего не знает о соседях или о том, как родитель добыл данные, поэтому нет глобального плана запроса и нет естественного места для JOIN. Движок вызывает резолвер автора по разу на пост, давая один запрос на родителя — 1 запрос на список плюс N пер-итемных запросов. REST-контроллер владеет всей формой ответа в одном месте, поэтому разработчик естественно пишет один JOIN. Изоляция и делает GraphQL композируемым, и она же — прямая причина N+1." },
    { q: "Когда DataLoader запускает batch и почему граница тика лучше фиксированного таймера?", a: "DataLoader ставит в очередь каждый вызов .load(key) и диспатчит batch на границе тика event loop — после текущего синхронного кадра и его микрозадач, через enqueuePostPromiseJob (process.nextTick в Node, Promise.resolve().then в браузере). Это самый ранний момент, когда GraphQL закончил резолвить один уровень и поставил в очередь все его ID, поэтому единственный WHERE id = ANY(...) срабатывает с нулевой добавленной задержкой. Фиксированный таймер 5 мс либо ждал бы зря на каждом запросе, либо срабатывал бы до того, как все ID уровня встали в очередь, разбивая batch." },
    { q: "Почему DataLoader надо инстанцировать на каждый запрос и что именно ломается с инстансом в module scope?", a: "Инстанс DataLoader — это кеш, поэтому его время жизни должно быть запросом, а не процессом. Loader в module scope делит этот кеш между всеми запросами: он возвращает устаревшие строки (Запрос A грузит пользователя 7, строка обновляется, Запрос B читает докешированную дообновлённую строку) и протекает между tenant'ами (Tenant B получает закешированную строку Tenant A для того же ID). Оба — баги корректности данных. Инстанцируй loader'ы внутри context factory, чтобы каждый запрос получал чистый изолированный кеш, который GC освобождает по завершении запроса." },
    { q: "Сформулируй контракт порядка batchLoadFn и как ты его выполняешь, включая отсутствующие строки.", a: "batchLoadFn(keys) должен вернуть массив той же длины, что keys, со значением для каждого ключа на соответствующей позиции. БД не гарантирует порядок строк из WHERE id = ANY(...), поэтому строишь Map по id и идёшь по входным ключам: ids.map(id => map.get(id) ?? null). Верни null для nullable отсутствующего поля или Error в этом слоте, чтобы отклонить только .load() этого ключа, пока остальные резолвятся. Бросок из batch-функции вместо этого отклоняет каждый ожидающий .load() в batch. Для one-to-many loader'ов верни пустой массив (никогда undefined) для родителей без детей." },
    { q: "В федеративном графе что router батчит бесплатно, а что всё ещё за тобой внутри subgraph'а?", a: "Когда запрос пересекает границы subgraph'ов, supergraph router собирает все ссылки на сущности (например, 50 ссылок User из subgraph'а постов) и шлёт один вызов _entities со всеми 50 representations в subgraph-владелец — кросс-субграфовый сетевой batch автоматичен и бесплатен. Внутри subgraph'а __resolveReference всё равно вызывается по разу на representation. Если каждый вызов бьёт в БД напрямую, получаешь 50 SQL-запросов внутри этого единственного вызова _entities, поэтому всё ещё нужен пер-запросный DataLoader в __resolveReference, чтобы сбатчить их в один." },
    { q: "DataLoader работает. Перечисли защиты по форме запроса, которые он не заменяет, и что каждая останавливает.", a: "DataLoader лишь схлопывает обращения к БД во время резолва; форму запроса он не трогает. Depth limit (7–10 уровней) отклоняет рекурсивные бомбы на валидации, до запуска любого резолвера. Мультипликативный complexity scoring суммирует веса полей, умноженные на размер list-аргумента (cost = weight + sum(child.first × cost(child))), ловя разрастание в 100^5 строк, которое пропускают счёт полей и проверка depth. Persisted/trusted documents исполняют только предзарегистрированные хеши запросов, закрывая всю поверхность произвольных inline-запросов (APQ, напротив, лишь экономит байты и форму не ограничивает). Алиас-капы (≤20 корневых алиасов) и operation-batch-капы (≤5–10) останавливают амплификацию по числу резолверов, обходящую пер-запросные rate limit'ы. Каждый слой fail closed." },
  ]}
/>

<Recap lang="ru">
Если ты восстановил каждый ответ по памяти, ты держишь хребет юнита: изоляция резолверов порождает N+1, DataLoader батчит на границе тика, но безопасно — только с пер-запросным scope и контрактом batch с тем же порядком; federation отдаёт сетевой batch, но DB-batch в __resolveReference всё ещё за тобой, а защиты по форме запроса (depth, мультипликативный complexity, trusted documents, алиас- и batch-капы) охраняют всё, что DataLoader не может. DataLoader чинит, сколько обращений к БД делает запрос; слои защиты решают, какие формы вообще допущены к запуску.
</Recap>
