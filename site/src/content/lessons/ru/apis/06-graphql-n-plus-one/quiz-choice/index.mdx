---
concepts:
- resolver-isolation
- dataloader-batching
- per-request-scope
- federation-entities
- query-complexity
- alias-bomb
estMin: 13
lang: ru
level: senior
order: 7
prereqs: []
slug: quiz-choice
sources:
- https://github.com/graphql/dataloader
- https://www.apollographql.com/docs/graphos/schema-design/guides/handling-n-plus-one
- https://escape.tech/blog/graphql-batch-attacks-cause-dos/
status: ready
summary: "Синтез юнита GraphQL N+1 в формате с выбором ответа — изоляция резолверов, scope DataLoader, контракты batch, federation и защиты по форме запроса."
title: 'GraphQL N+1: тест с выбором ответа'
track: apis
unit: 06-graphql-n-plus-one
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Шесть вопросов через весь юнит. Каждый — решение, которое ты принимаешь на реальном ревью или в инциденте: не определение для пересказа, а компромисс, который надо взвесить, когда один HTTP-запрос тихо выстреливает тысячу SQL-запросов.
</Hook>

<Goal lang="ru">
Убедись, что связываешь изоляцию резолверов, batching и scope DataLoader, контракты batch-функции, federation и защиты по форме запроса — синтез, к которому вели шесть уроков.
</Goal>

<Quiz
  id="06-graphql-n-plus-one-choice-1"
  lessonSlug="06-graphql-n-plus-one"
  lang="ru"
  question="REST-контроллер для той же страницы «посты с авторами» делает один JOIN; GraphQL-сервер делает 51 запрос. Бага в схеме нет. В чём структурная причина?"
  choices={[
    { label: "Резолверы изолированы по дизайну — Post.author видит один пост за раз, без глобального плана запроса, поэтому движок вызывает его по разу на пост. REST пишет JOIN руками, потому что один контроллер владеет всей формой ответа.", correct: true },
    { label: "GraphQL медленнее REST на транспортном уровне.", misconception: "Оба работают поверх HTTP; транспорт ни при чём. Цена — изоляция резолверов, делающая один fetch на родителя, без места для JOIN." },
    { label: "GraphQL-сервер однопоточный, поэтому не может батчить.", misconception: "Большинство GraphQL-серверов асинхронны и могут батчить — DataLoader это доказывает. N+1 про изоляцию резолверов, а не про потоки." },
    { label: "В GraphQL нет кеширования запросов на уровне спеки.", misconception: "Спека не определяет кеширование, но это не связано. 51 запрос идёт от пер-полевого исполнения резолверов, а не от отсутствия кеша." },
  ]}
/>

<Quiz
  id="06-graphql-n-plus-one-choice-2"
  lessonSlug="06-graphql-n-plus-one"
  lang="ru"
  question="Почему DataLoader батчит на границе тика event loop, а не по фиксированному таймеру 5 мс, и что это даёт?"
  choices={[
    { label: "Граница тика — самый ранний момент, когда все ключи текущего уровня запроса уже поставлены в очередь, поэтому batch срабатывает с нулевой добавленной задержкой, в отличие от таймера, который либо ждёт зря, либо срабатывает до прихода всех ID.", correct: true },
    { label: "Node.js не умеет реализовать таймер 5 мс для batching.", misconception: "Node поддерживает setTimeout и setImmediate; DataLoader мог бы их использовать. Он выбирает границу микрозадач именно ради отсутствия задержки." },
    { label: "Таймер использовал бы больше памяти, чем граница тика.", misconception: "Память задаётся размером batch, а не таймингом dispatch. Причина — задержка: граница тика срабатывает в момент, когда все load уровня поставлены в очередь." },
    { label: "Граница тика гарантирует порядок исполнения резолверов.", misconception: "Тайминг dispatch не меняет порядок резолверов. Выгода — срабатывание в самый ранний полный момент без ожидания." },
  ]}
/>

<Quiz
  id="06-graphql-n-plus-one-choice-3"
  lessonSlug="06-graphql-n-plus-one"
  lang="ru"
  question="Рефактор перенёс один DataLoader из context factory Apollo в module scope. Частота вызовов БД в 6× выше при неизменной частоте HTTP, и один tenant временами видит строки другого tenant. Почему?"
  choices={[
    { label: "Loader в module scope делит кеш между всеми запросами и tenant'ами. Один ключ возвращает одну закешированную строку независимо от tenant, и устаревшие строки переживают апдейты — баг корректности и изоляции, а не просто регресс производительности.", correct: true },
    { label: "Loader стал слишком большим и упирается в память.", misconception: "Размер памяти тут ни при чём. Общий инстанс протекает закешированными строками между tenant'ами и отдаёт устаревшие данные — баг корректности данных и есть симптом." },
    { label: "Module scope отключает batching, поэтому каждый load делает свой запрос.", misconception: "Module scope не отключает batching внутри тика; он делит кеш между запросами. Дефекты — утечка и устаревание, а не потеря batching." },
    { label: "Фикс — выставить cache:false на loader.", misconception: "Отключение кеша прячет устаревание, но инстанс всё равно общий между tenant'ами. Настоящий фикс — пер-запросная инстанциация в context factory." },
  ]}
/>

<Quiz
  id="06-graphql-n-plus-one-choice-4"
  lessonSlug="06-graphql-n-plus-one"
  lang="ru"
  question="batchLoadFn выполняет SELECT ... WHERE id = ANY($1) и возвращает строки в порядке, который дал Postgres. Время от времени у постов отображается неверный автор. Какое правило нарушено?"
  choices={[
    { label: "batchLoadFn должен вернуть по одному значению на входной ключ, в том же порядке, что и ключи. БД не гарантирует порядок строк из IN/ANY-запроса, поэтому строишь Map и идёшь по входным ключам — иначе каждый .load() резолвится со смещённой строкой.", correct: true },
    { label: "В запросе нет ORDER BY, поэтому он невалиден.", misconception: "SQL валиден; порядок в SQL — не контракт. DataLoader мапит i-е возвращённое значение на i-й ключ, поэтому ты переупорядочиваешь в batch-функции через Map." },
    { label: "DataLoader должен был бросить ошибку порядка.", misconception: "DataLoader доверяет позициям массива и не валидирует порядок. Порча тихая — именно поэтому паттерн Map-по-ключу обязателен." },
    { label: "Loader'у нужен cache:false, чтобы не отдавать устаревших авторов.", misconception: "Это баг порядка, а не кеша. Отключение кеша ничего не меняет; нужно выровнять возвращаемые значения по позициям входных ключей." },
  ]}
/>

<Quiz
  id="06-graphql-n-plus-one-choice-5"
  lessonSlug="06-graphql-n-plus-one"
  lang="ru"
  question="Supergraph на Apollo Federation маршрутизирует запрос, ссылающийся на 50 пользователей через два subgraph'а. Что берёт на себя router, а что всё ещё должен subgraph пользователей?"
  choices={[
    { label: "Router автоматически батчит 50 ссылок в один вызов _entities (сетевой batch бесплатен). Subgraph всё ещё нуждается в DataLoader внутри __resolveReference, иначе он сделает 50 SQL-запросов внутри этого единственного вызова _entities.", correct: true },
    { label: "Router сливает оба subgraph'а и делает один SQL-запрос через обе БД.", misconception: "Subgraph'ы не делят БД; router говорит по HTTP, а не по SQL. Он батчит representations, но каждый subgraph владеет своим batching к БД." },
    { label: "Federation подставляет DataLoader автоматически, поэтому subgraph'у он не нужен.", misconception: "Federation батчит только на сетевом уровне. Внутрисубграфовый N+1 в __resolveReference — всё ещё твоя ответственность, добавь DataLoader." },
    { label: "Router шлёт 50 отдельных HTTP-вызовов в subgraph пользователей.", misconception: "Он шлёт один вызов _entities со всеми 50 representations. Оставшаяся проблема — 50 пер-representation обращений к БД внутри subgraph'а." },
  ]}
/>

<Quiz
  id="06-graphql-n-plus-one-choice-6"
  lessonSlug="06-graphql-n-plus-one"
  lang="ru"
  question="DataLoader работает, страница быстрая. Атакующий шлёт один документ с 1000 корневых алиасов (q1: user(id:1)…q1000: user(id:1000)). Обращения к БД схлопываются в один batch, но сервер всё равно страдает. Почему и что это останавливает?"
  choices={[
    { label: "Рычаг — число исполнений резолверов: 1000 алиасов запускают 1000 вызовов резолверов — проверки прав, логирование, lookups context — под одним HTTP-запросом, обходящим наивные пер-запросные rate limit'ы. Ограничь корневые алиасы (≤20) и размер operation-batch; DataLoader не помогает с формой запроса.", correct: true },
    { label: "Единый batch-запрос слишком велик и валит БД по таймауту.", misconception: "DataLoader умеет дробить через maxBatchSize; обращение к БД — не узкое место. Цена — 1000 исполнений резолверов, которые алиас-капы останавливают на валидации." },
    { label: "Отключение introspection блокирует alias bomb.", misconception: "Introspection лишь помогает разведке; схему можно восстановить из ошибок. Alias bomb — атака по форме запроса, останавливается алиас- и batch-капами." },
    { label: "Повышение пер-запросного rate limit это чинит.", misconception: "Alias bomb усиливается внутри одного запроса, поэтому пер-запросные rate limit'ы — именно то, что он обходит. Нужен алиас-кап и complexity scoring." },
  ]}
/>

<Recap lang="ru">
Сквозная линия юнита — один конвейер: изоляция резолверов порождает N+1, DataLoader схлопывает обращения к БД на границе тика, но только при пер-запросном scope и корректном контракте batch с тем же порядком; federation отдаёт сетевой batch, но DB-batch в __resolveReference всё ещё за тобой, а защиты по форме запроса (depth, мультипликативный complexity, persisted/trusted documents, алиас- и batch-капы) останавливают атаки, которых DataLoader не касается. DataLoader чинит, сколько обращений к БД делает запрос; слои защиты управляют тем, какие формы запроса вообще допущены к исполнению.
</Recap>
