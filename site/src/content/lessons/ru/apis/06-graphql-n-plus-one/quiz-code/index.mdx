---
concepts:
- resolver-nplus1
- batch-function
- order-preservation
- depth-limit
- dataloader-scope
estMin: 14
lang: ru
level: senior
order: 9
prereqs: []
slug: quiz-code
sources:
- https://github.com/graphql/dataloader
- https://www.apollographql.com/docs/apollo-server/data/fetching-data
- https://graphql.org/learn/security/
status: ready
summary: "Читай реальный код резолверов и DataLoader, предсказывай поведение N+1 или корректности и выбирай фикс с наибольшим рычагом."
title: 'GraphQL N+1: чтение кода'
track: apis
unit: 06-graphql-n-plus-one
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Проблемы N+1 диагностируются в коде резолверов, в batch-функциях и на слое валидации — не в прозе. Прочитай каждый сниппет, предскажи, что он делает под 50 постов или враждебного клиента, и выбери фикс, который senior-инженер делает первым.
</Hook>

<Goal lang="ru">
Отработай цикл, который ты запускаешь на каждом ревью GraphQL: замечай пер-полевой fetch, проверяй контракт batch и подтверждай гейт по форме запроса — до того как лог БД вспыхнет в production.
</Goal>

## Сниппет 1 — резолвер, выстреливающий N+1

```javascript
const resolvers = {
  Query: {
    posts: () => db.query('SELECT * FROM posts LIMIT 50'),
  },
  Post: {
    // вызывается по разу на пост, в изоляции
    author: (post) => db.query(
      'SELECT * FROM users WHERE id = $1', [post.authorId]
    ),
  },
};
```

<Quiz
  id="06-graphql-n-plus-one-code-1"
  lessonSlug="06-graphql-n-plus-one"
  lang="ru"
  question="Для posts { title author { name } } на 50 постов сколько запросов к БД сработает и какой структурный фикс?"
  choices={[
    { label: "51 — один на список постов плюс по одному на пост в Post.author, потому что резолверы изолированы и Post.author видит один пост за раз. Фикс: пустить author через пер-запросный DataLoader, чтобы 50 load схлопнулись в один WHERE id = ANY(...).", correct: true },
    { label: "2 — движок GraphQL автоматически батчит соседние вызовы резолверов.", misconception: "Движок не батчит; каждый вызов Post.author идёт независимо. Получаешь 1 + 50 = 51 запрос, пока не добавишь DataLoader." },
    { label: "51 — чини повышением размера пула соединений, чтобы запросы шли параллельно.", misconception: "Параллелизм не убирает 50 запросов; он лишь размазывает нагрузку. Фикс — схлопнуть их в один batch через DataLoader, а не ускорить." },
    { label: "1 — Post.author переиспользует строки, уже загруженные Query.posts.", misconception: "Query.posts возвращает посты, а не пользователей; Post.author должен дотягивать каждого автора отдельно. Этот отдельный fetch на пост и есть N+1." },
  ]}
/>

## Сниппет 2 — batch-функция

```javascript
async function batchAuthors(ids) {
  const rows = await db.query(
    'SELECT id, name FROM users WHERE id = ANY($1)', [ids]
  );
  // возвращает строки в том порядке, что выбрал Postgres
  return rows;
}
```

<Quiz
  id="06-graphql-n-plus-one-code-2"
  lessonSlug="06-graphql-n-plus-one"
  lang="ru"
  question="Эта batch-функция возвращает сырые строки. В чём скрытый баг и как его фиксить?"
  choices={[
    { label: "Нарушен контракт порядка: DataLoader мапит i-е возвращённое значение на i-й входной ключ, но Postgres не гарантирует порядок строк из ANY(...). Построй Map из id в строку и верни ids.map(id => map.get(id) ?? null), чтобы значения легли по ключам.", correct: true },
    { label: "Бага нет — ANY($1) сохраняет порядок входного массива.", misconception: "Не сохраняет. Порядок строк из IN/ANY не определён, поэтому значения могут лечь не на те ключи. Переупорядочь через Map по id." },
    { label: "Баг в том, что надо бросать ошибку при отсутствующей строке.", misconception: "Бросок отклоняет каждый ожидающий .load() в batch. Для отсутствующей строки верни null (или Error в этом слоте) — но главный дефект здесь порядок." },
    { label: "Надо использовать IN (...) вместо ANY($1), чтобы сохранить порядок.", misconception: "IN и ANY ведут себя одинаково по порядку — ни один не гарантирует его. Порядок фиксится в batch-функции через Map, а не синтаксисом SQL." },
  ]}
/>

## Сниппет 3 — one-to-many loader

```javascript
async function batchTags(postIds) {
  const rows = await db.query(
    'SELECT post_id, tag FROM tags WHERE post_id = ANY($1)', [postIds]
  );
  const map = new Map();
  rows.forEach(r => {
    if (!map.has(r.post_id)) map.set(r.post_id, []);
    map.get(r.post_id).push(r.tag);
  });
  return postIds.map(id => map.get(id));   // посты без тегов?
}
```

<Quiz
  id="06-graphql-n-plus-one-code-3"
  lessonSlug="06-graphql-n-plus-one"
  lang="ru"
  question="У поста с нулём тегов нет строк, поэтому map.get(id) — undefined. Что ломается и каков фикс?"
  choices={[
    { label: "One-to-many loader обязан вернуть пустой массив для ключей без детей. Возврат undefined резолвит Post.tags в undefined (ошибка валидации на non-null [Tag!]!). Фикс: map.get(id) ?? [], чтобы посты без тегов вернули [].", correct: true },
    { label: "Ничего — undefined и пустой массив эквивалентны для полевого списка.", misconception: "Не эквивалентны. undefined ломает non-null список на валидации, а [] — валидный пустой список. Дефолти отсутствующие ключи в []." },
    { label: "Пост наследует теги предыдущего поста.", misconception: "Так как функция мапит по postIds (а не по сырым строкам), позиции не смещаются — но undefined-слоты всё равно ломают поле. Верни [] для постов без тегов." },
    { label: "DataLoader повторит batch для отсутствующих ключей.", misconception: "DataLoader никогда не ретраит; он резолвит тем значением, что лежит в слоте. undefined нужно заменить пустым массивом твоим кодом." },
  ]}
/>

## Сниппет 4 — гейт валидации

```javascript
const server = new ApolloServer({
  schema,
  validationRules: [depthLimit(10)],   // ограничен только depth
});
```

<Quiz
  id="06-graphql-n-plus-one-code-4"
  lessonSlug="06-graphql-n-plus-one"
  lang="ru"
  question="Этот сервер ограничивает depth до 10, но больше ничего. Клиент шлёт 5-уровневый запрос с first: 100 на каждом уровне. Что проходит и какого слоя не хватает?"
  choices={[
    { label: "Проходит — depth всего 5 — но концептуально читает порядка 100^5 строк. Depth считает вложенность, а не разрастание строк. Добавь мультипликативный complexity scoring (cost умножается на list-аргумент first/last) и пер-документный алиас-кап.", correct: true },
    { label: "Отклоняется — depth 10 покрывает все дорогие запросы.", misconception: "5-уровневый запрос ниже depth-капа, но разрастается до 100^5 строк. Depth limit не видит мультипликацию по list-аргументу; complexity scoring видит." },
    { label: "DataLoader сбатчит разрастание, поэтому доп. гейт не нужен.", misconception: "DataLoader схлопывает обращения к БД по уровням, но 100^5 строк всё равно читаются и материализуются. Защиты по форме запроса должны отклонить документ до запуска резолверов." },
    { label: "Отключение introspection заблокировало бы этот запрос.", misconception: "Introspection лишь прячет схему от разведки; он не останавливает дорогую форму запроса. Защита — complexity scoring." },
  ]}
/>

<Recap lang="ru">
Каждый инцидент N+1 в GraphQL читается в коде: пер-полевой резолвер, тянущий по строке на родителя (51 запрос → DataLoader), batch-функция, доверяющая порядку строк БД (порча → Map по ключу), one-to-many loader, теряющий пустые массивы (ошибка валидации → дефолт в []), и слой валидации, ограничивающий depth, но не разрастание строк (утечка 100^5 → мультипликативный complexity плюс алиас-капы). Читай резолвер, проверяй контракт batch, подтверждай гейт — затем перетрассируй счётчики резолверов, чтобы убедиться, что фикс держится.
</Recap>
