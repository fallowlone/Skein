---
concepts:
- middleware-chain
- plugin-encapsulation
- schema-validation
- json-serialization
- throughput-benchmarks
- async-error-handling
deepensInto: []
estMin: 18
lang: ru
lessonType: topic
level: middle
order: 2
prereqs: []
slug: 02-express-vs-fastify
sources:
- https://expressjs.com/en/guide/using-middleware.html
- https://fastify.dev/docs/latest/Reference/
- https://fastify.dev/benchmarks/
spiral: []
status: ready
summary: 'Express — линейная цепочка middleware (req, res, next): минималистичная,
  с крупнейшей экосистемой. Fastify меняет это на плагин-модель с JSON-Schema-валидацией
  и fast-json-stringify-сериализацией — реальным выигрышем throughput. Оба прод-готовы.'
title: 'Express против Fastify: middleware и пропускная способность'
track: node
unit: 05-http-and-frameworks
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";
import Infographic from "~/components/diagram/Infographic.astro";
import latencyBudget from "./infographics/latency-budget.svg?raw";

<Hook>
Команда перевела JSON API на Fastify после поста в блоге, обещавшего «2x throughput», и потом смотрела, как p99-латентность остаётся ровно там, где была. Бенчмарк был настоящим — Fastify *действительно* отдаёт примерно вдвое больше запросов/сек, чем Express, на крошечном роуте «верни JSON-объект», — но их обработчик тратил 40 мс на ожидание Postgres и 0.2 мс во фреймворке. Фреймворк никогда не был узким местом. Хуже того, два роута тихо сломались: `async`-обработчик Express 4, который раньше бросал 500, теперь утекал reject как `unhandledRejection`, потому что его никто не обернул. Они гнались за числом, которое не имело значения, и зашипили баг, который имел.
</Hook>

К концу урока ты поймёшь, что каждый фреймворк реально отдаёт взамен, и сможешь выбирать осознанно — не гнаться за цифрой бенчмарка.

<Crux>Express — цепочка middleware (req, res, next); Fastify добавляет schema-валидацию и сериализацию ради throughput. Оба прод-готовы.</Crux>

<Explanation>

## Две философии: цепочка vs инкапсулированное дерево плагинов

Прежде чем выбирать фреймворк, ответь на один вопрос: нужна ли тебе максимально широкая экосистема с простейшей моделью, или встроенная schema-валидация и сериализация? Ответ, как правило, и решает выбор. Express намеренно мал: запрос течёт через **линейную цепочку middleware**, каждый из которых — функция `(req, res, next)`, мутирующая `req`/`res` и вызывающая `next()`, чтобы передать управление дальше, или `next(err)`, чтобы прыгнуть в обработчик ошибок. В него почти не вшито мнений — ты собираешь роутинг, парсинг тела, авторизацию и логирование из обширной экосистемы `app.use()`-middleware. Этот минимализм — его суперсила: крупнейший каталог middleware в мире Node, десятилетие боевой обкатки и сигнатура, которую уже знает каждый Node-разработчик.

```js
import express from "express";
const app = express();

// глобальный middleware: бежит на каждый запрос, в порядке регистрации
app.use(express.json());
app.use((req, res, next) => { req.startedAt = Date.now(); next(); });

app.get("/users/:id", (req, res) => {
  res.json({ id: req.params.id });
});
```

Fastify ставит на другое: запрос проходит через **хуки жизненного цикла** (`onRequest`, `preParsing`, `preValidation`, `preHandler`, …), а роуты группируются в **плагины**, зарегистрированные через `fastify.register()`. Определяющая черта — **инкапсуляция**: плагин получает собственную область, поэтому декораторы, хуки и middleware, зарегистрированные внутри него, *не* протекают к соседям. Это фича для модульности, но она удивляет беженцев из Express, ожидающих, что `app.use()` глобален всюду.

```js
import Fastify from "fastify";
const app = Fastify({ logger: true }); // pino встроен

await app.register(async (instance) => {
  // этот хук бежит только для роутов внутри области этого плагина
  instance.addHook("preHandler", async (req) => { req.startedAt = Date.now(); });
  instance.get("/users/:id", async (req) => ({ id: req.params.id }));
});
```

## Настоящий разделитель: schema-driven валидация и сериализация

У Express нет встроенной истории валидации или сериализации — ты тянешь `zod`, `joi` или `express-validator`, а сериализуешь через `res.json()`, который вызывает `JSON.stringify`. Fastify встраивает и то и другое в определение роута через **JSON Schema**. Схема запроса валидирует *и приводит* входящие params/query/body до того, как выполнится обработчик. **Схема ответа** — это рычаг throughput: `fast-json-stringify` компилирует из этой схемы специализированную функцию-сериализатор заранее, так что она никогда не обходит объект неизвестной формы в рантайме, как вынужден делать обобщённый `JSON.stringify`.

```js
app.get("/users/:id", {
  schema: {
    params: { type: "object", properties: { id: { type: "string" } } },
    response: {
      200: {
        type: "object",
        properties: { id: { type: "string" }, name: { type: "string" } },
      },
    },
  },
}, async (req) => getUser(req.params.id));
```

Этот скомпилированный сериализатор — самый релевантный для реального мира выигрыш, потому что он ускоряет *каждый* ответ, а не только синтетический пустой роут, — и в качестве бонуса поля, которых нет в схеме ответа, вырезаются, так что ты не можешь случайно утечь `passwordHash`.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Измерение</th>
        <th class="border border-border px-3 py-2 text-left">Express</th>
        <th class="border border-border px-3 py-2 text-left">Fastify</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">Композиция</td>
        <td class="border border-border px-3 py-2">линейная цепочка middleware</td>
        <td class="border border-border px-3 py-2">плагины + хуки жизненного цикла</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Сигнатура обработчика</td>
        <td class="border border-border px-3 py-2"><code>(req, res, next)</code></td>
        <td class="border border-border px-3 py-2"><code>async (req, reply)</code></td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Валидация</td>
        <td class="border border-border px-3 py-2">своя (zod/joi)</td>
        <td class="border border-border px-3 py-2">встроенный JSON Schema</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Сериализация</td>
        <td class="border border-border px-3 py-2"><code>JSON.stringify</code></td>
        <td class="border border-border px-3 py-2"><code>fast-json-stringify</code> (скомпилирована)</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Экосистема</td>
        <td class="border border-border px-3 py-2">крупнейшая, старейшая</td>
        <td class="border border-border px-3 py-2">большая, плагинная</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Throughput (свой бенч)</td>
        <td class="border border-border px-3 py-2">базовый</td>
        <td class="border border-border px-3 py-2">~2x на простом JSON</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Ошибки async-обработчика</td>
        <td class="border border-border px-3 py-2">Express 4: не ловятся авто</td>
        <td class="border border-border px-3 py-2">await &amp; роутятся в хук ошибок</td>
      </tr>
    </tbody>
  </table>
</div>

<Inset kind="why" lang="ru">
Почему Fastify «~2x» в бенчмарках, но ровный в проде? Его собственный набор бенчмарков молотит роут, возвращающий маленький константный объект, так что единственная работа — парсинг HTTP и сериализация JSON, ровно там, где блестят скомпилированный сериализатор и поджарый роутер. Реальный обработчик добавляет round-trip к БД (часто 5–40 мс) и внешние вызовы, которые затмевают субмиллисекундный оверхед фреймворка. Синтетические числа меряют фреймворк в изоляции; твоя латентность доминируется IO. Преимущество throughput настоящее, но важнее всего оно для high-RPS, тяжёлых по ответам JSON-шлюзов, а не для CRUD-приложений, привязанных к базе.
</Inset>

<Infographic
  svg={latencyBudget}
  label="Горизонтальная столбчатая диаграмма бюджета времени одного запроса: round-trip к Postgres — 40 мс, а оверхед фреймворка — лишь 0.2 мс, едва заметная полоска рядом."
  caption="Тот же запрос в разбивке: фреймворк — это 0.2 мс из ~40 мс запроса. Этот 200-кратный разрыв и есть причина, по которой переход на фреймворк с «2x throughput» не двигает p99 — узкое место база, а не фреймворк."
  hue="var(--d-infra)"
/>

## Ловушка async-ошибок Express 4

Самый острый режим сбоя историчен и до сих пор всюду. **Express 4 не ловит зареджекченные промисы из `async`-обработчиков роутов.** Если `async`-обработчик бросает (или `await`-ит что-то, что реджектится), у Express 4 нет `await` вокруг твоей функции, так что reject становится `unhandledRejection` — который с Node 15 по умолчанию роняет процесс — вместо того, чтобы дойти до твоего middleware обработки ошибок.

```js
// Express 4 — СЛОМАНО: reject ускользает в unhandledRejection
app.get("/u/:id", async (req, res) => {
  const user = await db.find(req.params.id); // если это реджектнется…
  res.json(user);                            // …Express 4 никогда не поймает
});

// Express 4 — ФИКС: try/catch + next или обёртка вроде express-async-errors
app.get("/u/:id", async (req, res, next) => {
  try { res.json(await db.find(req.params.id)); }
  catch (err) { next(err); } // передать в middleware ошибок
});
```

**Express 5 (уже релизнут) это чинит**: зареджекченные промисы из `async`-обработчиков автоматически форвардятся в обработчик ошибок, закрывая дыру. У Fastify этой проблемы не было никогда — обработчики `async` по дизайну, их reject await-ятся и роутятся в хук `setErrorHandler`. Если ты на Express 4, оборачивай каждый `async`-обработчик (или прими `express-async-errors`); тихое падение — самый частый production-баг в старых Express-приложениях.

<TradeoffMatrix
  id="05-frameworks-tradeoff-1"
  lessonSlug="02-express-vs-fastify"
  lang="ru"
  prompt="Ты строишь новый high-RPS публичный JSON API: много мелких read-эндпойнтов, строгая валидация входа, команда готова взяться за TypeScript и есть требование никогда не утекать внутренние поля в ответах. Какой фреймворк подойдёт лучше?"
  options={[
    { name: "Fastify, опираясь на JSON Schema запроса + ответа на каждом роуте", summary: "Schema-driven, быстрая сериализация.", correct: true, justification: "Эта спека — золотая середина Fastify. Схемы запроса дают валидацию и приведение бесплатно на границе; схемы ответа компилируют сериализатор fast-json-stringify, который и ускоряет каждый JSON-ответ, и вырезает любое поле не из схемы — прямо удовлетворяя «никогда не утекать внутренние поля». TypeScript-first эргономика и встроенный pino-логгер ложатся на новую, готовую к типам команду. High-RPS, тяжёлая по ответам форма — ровно там, где преимущество throughput реально, а не синтетично." },
    { name: "Express 4 плюс zod и кастомный сериализатор", summary: "Знакомо, всё вручную.", justification: "Рабочее, но ты руками пересобираешь то, что Fastify даёт нативно: zod для валидации, самодельный фильтр ответа, чтобы не утекать поля, и обёртку async-ошибок, чтобы обойти ловушку reject в Express 4. Больше клея, больше мест забыть allowlist полей и никакого скомпилированного сериализатора. Выбирай это, только если глубокое знание Express командой перевешивает выгоды схем — а спека говорит, что нет." },
    { name: "Express 5 с тем же middleware, что ты уже знаешь", summary: "Современный Express, без схем.", justification: "Express 5 чинит дыру async-ошибок и это нормальный, безопасный выбор — но у него по-прежнему нет встроенной валидации или сериализации, так что «строгая валидация» и «никогда не утекать поля» остаются твоей ручной ответственностью. Для нового high-RPS API, где схемы — заявленная нужда, встроенные средства Fastify делают эту работу за тебя. Хороший второй кандидат, но не лучший выбор здесь." },
    { name: "Тот, что использовал прошлый проект, ради консистентности", summary: "Опереться на прецедент.", justification: "Консистентность — реальная ценность, но здесь спека даёт конкретные технические требования — schema-валидация, безопасность полей ответа, high RPS, — которые и должны вести выбор. Выбор по прецеденту игнорирует, что Fastify нативно покрывает три из четырёх требований. Пусть гринфилд-сервис решают требования, а не инерция." },
  ]}
/>

<Quiz
  id="05-frameworks-quiz-1"
  lessonSlug="02-express-vs-fastify"
  lang="ru"
  question="Что на самом деле делает fast-json-stringify и почему это самая релевантная для реального мира фича производительности Fastify?"
  choices={[
    { label: "Он компилирует JSON Schema ответа роута в специализированный сериализатор заранее, так что ответы минуют обобщённый обход объекта, который вынужден делать JSON.stringify — ускоряя каждый JSON-ответ, а не только синтетический", correct: true },
    { label: "Он валидирует входящие тела запросов быстрее, чем zod", misconception: "Это валидация запроса, отдельная забота. fast-json-stringify про путь ответа: он превращает схему ответа в скомпилированный сериализатор. Валидация запроса в Fastify использует другой скомпилированный валидатор (ajv)." },
    { label: "Он кэширует каждый ответ, чтобы идентичные запросы возвращались мгновенно", misconception: "Никакого кэширования ответов тут нет. Он один раз компилирует сериализатор из схемы, а затем быстро сериализует каждый (потенциально разный) объект ответа — он не мемоизирует тела ответов." },
    { label: "Он заменяет HTTP-парсер на более быстрый на C++", misconception: "Он трогает только сериализацию тела ответа. HTTP-парсер — это парсер Node, неизменный; выигрыш — в пропуске рантайм-определения формы обобщённым JSON.stringify через функцию, скомпилированную из схемы." },
  ]}
/>

<Quiz
  id="05-frameworks-quiz-2"
  lessonSlug="02-express-vs-fastify"
  lang="ru"
  question="async-обработчик роута в приложении на Express 4 await-ит вызов БД, который реджектится, и там нет try/catch. Что произойдёт?"
  choices={[
    { label: "Reject НЕ ловится Express 4 — он становится unhandledRejection, который с Node 15 по умолчанию роняет процесс, вместо того чтобы дойти до middleware обработки ошибок", correct: true },
    { label: "Express 4 автоматически форвардит его в middleware обработки ошибок", misconception: "Это поведение Express 5 (и Fastify). Express 4 не await-ит твой обработчик, так что зареджекченный промис ускользает — нужно try/catch и вызвать next(err) или использовать обёртку вроде express-async-errors." },
    { label: "Express вернёт 500 с сообщением ошибки в теле", misconception: "Только если ты подключил обработку ошибок и ошибка до неё дойдёт. В Express 4 с голым async-обработчиком reject вообще не доходит до middleware ошибок — он всплывает как unhandledRejection." },
    { label: "Запрос висит вечно без ответа и без ошибки", misconception: "Он не зависает молча на уровне фреймворка — reject запускает unhandledRejection уровня процесса (по умолчанию роняющий процесс с Node 15). Клиент может увидеть разорванное соединение, но видимый симптом — падение процесса, а не бесконечное зависание." },
  ]}
/>

<DragOrder
  id="05-frameworks-drag-1"
  lessonSlug="02-express-vs-fastify"
  lang="ru"
  prompt="Расставь путь запроса в Fastify через жизненный цикл, от прибытия до ответа:"
  items={[
    { id: "a", label: "Запрос прибывает; роутер сопоставляет его с роутом и его схемой" },
    { id: "b", label: "Хуки onRequest / preParsing / preValidation бегут для этой области" },
    { id: "c", label: "Схема запроса валидирует и приводит params, query и body" },
    { id: "d", label: "Бегут хуки preHandler, затем выполняется твой async-обработчик" },
    { id: "e", label: "Сериализатор fast-json-stringify из схемы ответа кодирует ответ" },
    { id: "f", label: "Сериализованный ответ отправляется обратно клиенту" },
  ]}
  correctOrder={["a", "b", "c", "d", "e", "f"]}
/>

</Explanation>

<FlowDiagram
  label="Запрос через фреймворк: middleware/хуки, обработчик, затем сериализация"
  hue="var(--d-systems)"
  perRow={4}
  nodes={[
    { id: "r", label: "запрос внутрь", sub: "матч роута" },
    { id: "m", label: "хуки / middleware", sub: "валидация" },
    { id: "h", label: "обработчик", sub: "твоя логика" },
    { id: "s", label: "сериализ. + отправка", sub: "JSON наружу" },
  ]}
  edges={[
    { from: "r", to: "m" },
    { from: "m", to: "h", label: "next / await" },
    { from: "h", to: "s", label: "схема" },
  ]}
  caption="Оба фреймворка делят эту форму: запрос матчится в роут, течёт через middleware (Express) или хуки жизненного цикла (Fastify), попадает в обработчик, затем сериализуется и отправляется. Express сериализует обобщённым JSON.stringify; Fastify — сериализатором fast-json-stringify, скомпилированным из схемы, — рычагом throughput, который ещё и вырезает не указанные поля."
/>

<KeyTakeaway>Express — неопинионированная цепочка с крупнейшей экосистемой; Fastify — schema-driven, его fast-json-stringify даёт реальный выигрыш throughput. Выбирай по экосистеме vs схемам — бенчмарки синтетика, узкое место — БД.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="05-frameworks-retrieval"
  lang="ru"
  questions={[
    { q: "Fastify отчитывается о ~2x throughput Express в собственных бенчмарках. Почему это часто невидимо в реальном приложении и когда оно реально важно?", a: "Бенчмарк изолирует фреймворк: он отдаёт крошечный константный JSON-объект, так что единственная меряемая работа — парсинг HTTP, роутинг и сериализация — ровно там, где побеждают поджарый роутер Fastify и скомпилированный сериализатор fast-json-stringify, примерно удваивая запросы/сек. Реальный же обработчик делает round-trip к базе (обычно 5–40 мс) и, может, внешние HTTP-вызовы, которые затмевают субмиллисекундный оверхед фреймворка. Так что сквозная p99-латентность доминируется IO, и смена фреймворка её почти не двигает. Преимущество throughput реально, но важнее всего оно для high-RPS, тяжёлых по ответам JSON-шлюзов и API, где сериализация — заметная доля работы, а не для CRUD-приложений, привязанных к своей базе. По-настоящему переносимый выигрыш — это скомпилированный сериализатор, ускоряющий каждый ответ (и вырезающий не указанные поля), а не заголовочные 2x." },
    { q: "Почему async-обработчик роута может тихо уронить Express 4-сервис и какие есть два способа это починить?", a: "Express 4 вызывает твой обработчик роута, но не await-ит его, так что если обработчик async и его промис реджектится — например падает заавейченный вызов БД — внутри Express нет try/catch или await вокруг него, чтобы захватить reject. Поэтому reject целиком ускользает из запроса и всплывает как unhandledRejection уровня процесса, который с Node 15 по умолчанию завершает процесс, а не выдаёт 500 через твой middleware обработки ошибок. Два фикса: (1) обернуть тело в try/catch и вызвать next(err), чтобы ошибка дошла до middleware ошибок; или (2) принять обёртку вроде express-async-errors (или высокопорядковый asyncHandler), который делает это за тебя на всех роутах. Долговечный фикс — обновиться до Express 5, который теперь автоматически форвардит зареджекченные промисы из async-обработчиков в обработчик ошибок — то самое поведение, что у Fastify было всегда, поскольку его обработчики async по дизайну и их reject роутятся в setErrorHandler." },
  ]}
/>

<Recap lang="ru">
Express и Fastify решают одну задачу с противоположными темпераментами. Express — минимальная, неопинионированная **линейная цепочка middleware** из функций `(req, res, next)` с крупнейшей экосистемой в Node и десятилетием боевой обкатки — валидацию, сериализацию и всё прочее ты собираешь из стороннего middleware. Fastify — модель **плагинов-и-хуков**, чьи плагины инкапсулированы (так что `app.use()`-стиль глобалов не протекает между областями, что удивляет беженцев из Express), а определяющая черта — **JSON Schema**: схемы запроса валидируют и приводят вход, а схема ответа компилирует сериализатор `fast-json-stringify`, который ускоряет каждый JSON-ответ и вырезает поля не из схемы. По производительности собственные бенчмарки Fastify показывают примерно **2x** запросов/сек Express, но это меряет фреймворк в изоляции на тривиальном роуте; в реальных приложениях round-trip к базе на 5–40 мс доминирует субмиллисекундный оверхед фреймворка, так что настоящий, переносимый выигрыш — это скомпилированный сериализатор, а не заголовочное число. Классический режим сбоя — **Express 4 не ловит reject из `async`-обработчиков** — они ускользают как `unhandledRejection` и роняют процесс с Node 15, чинится через try/catch + `next(err)`, обёртку вроде `express-async-errors` или обновление до Express 5 (который форвардит их автоматически, как Fastify всегда). Выбирай Express ради широты экосистемы, знакомства команды и постепенного внедрения; выбирай Fastify ради чувствительных к производительности JSON API, schema-driven валидации/сериализации и структурированной плагин-модели — оба прод-готовы. Теперь, когда встретишь бенчмарк «2x throughput», ты первым делом спросишь: а что делает обработчик — возвращает константу или ходит в базу?
</Recap>
