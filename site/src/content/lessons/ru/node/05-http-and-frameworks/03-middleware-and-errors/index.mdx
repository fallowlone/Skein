---
concepts:
- middleware-pipeline
- middleware-order
- error-boundary
- async-error-forwarding
- graceful-shutdown
- readiness-drain
deepensInto: []
estMin: 18
lang: ru
lessonType: topic
level: middle
order: 3
prereqs: []
slug: 03-middleware-and-errors
sources:
- https://expressjs.com/en/guide/writing-middleware.html
- https://expressjs.com/en/guide/error-handling.html
- https://nodejs.org/api/http.html#serverclosecallback
spiral: []
status: ready
summary: 'Middleware — это упорядоченный конвейер, завершающийся одной 4-арг границей
  ошибок, которая мапит типизированные ошибки в статусы и безопасное тело. Async-throw
  в Express 4 надо прокидывать, а SIGTERM обязан слить запросы в полёте через server.close().'
title: 'Middleware, границы ошибок и graceful shutdown'
track: node
unit: 05-http-and-frameworks
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
Каждый деплой дашборды загорались всплеском 502 — несколько сотен упавших запросов, потом тишина. Само приложение было здорово; раскатка просто убивала поды посреди запроса. Kubernetes слал `SIGTERM`, старый процесс умирал мгновенно, и каждый запрос в полёте — включая 9-секундный экспорт отчёта — обрывался на сокете, всплывая к балансировщику как 502. Фиксом был не кластер побольше и не библиотека ретраев. Это были восемь строк: поймать `SIGTERM`, вызвать `server.close()`, чтобы перестать принимать новые соединения, дав открытым завершиться, и затем выйти. Авария была не про ёмкость. Она была про процесс, который не умел прощаться.
</Hook>

<Crux>Middleware бежит в порядке регистрации; одна 4-арг граница мапит типы ошибок в статусы; SIGTERM обязан сперва слить запросы в полёте.</Crux>

<Explanation>

## Middleware — это упорядоченный конвейер

HTTP-фреймворк вроде Express в своей сути — это массив функций, вызываемых в том порядке, в каком ты их зарегистрировал. Каждая `(req, res, next)` выполняется, делает свою долю работы и зовёт `next()`, передавая управление следующей, — или зовёт `next(err)`, чтобы пропустить всё и прыгнуть прямо к обработчику ошибок. Запрос течёт через массив; ответ течёт назад. Поскольку это упорядоченный массив, **порядок регистрации — это поведение, а не стиль**: парсер тела, зарегистрированный после маршрута, означает, что маршрут видит `req.body` как `undefined`; проверка авторизации после защищённого маршрута означает, что маршрут отработает для неаутентифицированных пользователей.

```js
import express from "express";
const app = express();

app.use(logger);              // 1. сквозное: бежит для каждого запроса
app.use(express.json());      // 2. парсер тела — ДОЛЖЕН быть до маршрутов, читающих req.body
app.use(authenticate);        // 3. наполняет req.user (или next(err) при плохом токене)

app.get("/orders/:id", requireAuth, getOrder); // 4. маршруты, после их предпосылок

app.use(notFound);            // 5. 404 — достигается, только если ни один маршрут не совпал
app.use(errorHandler);        // 6. граница ошибок — 4 аргумента, регистрируется ПОСЛЕДНЕЙ
```

Ментальная модель — воронка: сквозные заботы первыми (логирование, request id, CORS), затем парсинг, затем аутентификация и авторизация, затем валидация, затем маршрут. Два терминальных обработчика — перехватчик 404 и граница ошибок — идут **после** всех маршрутов, потому что middleware после `res.send()` (или после того как совпавший маршрут завершил ответ) никогда не бежит. Композиция — это выигрыш: логирование, авторизация, rate-limiting и валидация живут каждый в одной переиспользуемой функции вместо копипаста в каждый обработчик.

## Одна централизованная граница ошибок

Express распознаёт одну особую форму: middleware с **четырьмя** аргументами, `(err, req, res, next)`. Арность — это сигнал: Express считает `fn.length === 4`, чтобы классифицировать её как обработчик ошибок, поэтому случайно выброшенный `next` превращает твой обработчик ошибок в молча пропускаемый обычный middleware. Тебе нужна ровно одна такая, зарегистрированная последней, и она делает три работы: мапит типизированную ошибку в код статуса, шлёт **безопасное** тело клиенту и логирует **полную** ошибку для операторов.

```js
function errorHandler(err, req, res, next) {
  if (res.headersSent) return next(err); // ответ начат — пусть Express его закроет

  const status = err.status ?? statusFor(err); // ValidationError→400, NotFoundError→404
  logger.error({ err, reqId: req.id }, "request failed"); // полный стек + cause для ops

  res.status(status).json({
    error: { code: err.code ?? "INTERNAL", message: status < 500 ? err.message : "Internal error" },
  }); // 5xx прячет внутренности; 4xx может отдать сообщение валидации
}
```

Ключевое разделение — **взгляд клиента vs взгляд оператора**. Клиент получает стабильную форму — код и сообщение, — и для 5xx это сообщение — общее `"Internal error"`, никогда не трасса стека, фрагмент SQL или путь к файлу. Утечка стека клиенту вручает атакующему версии твоих зависимостей и внутреннюю структуру. Операторы получают обратное: весь объект `Error` с его `stack` и цепочкой `cause` в логах. Одно место решает маппинг, так что новый тип ошибки — это одна правка, а не grep по всем обработчикам.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Middleware</th>
        <th class="border border-border px-3 py-2 text-left">Должен идти до</th>
        <th class="border border-border px-3 py-2 text-left">Сбой при неверном порядке</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">Парсер тела (<code>express.json()</code>)</td>
        <td class="border border-border px-3 py-2">любого маршрута, читающего <code>req.body</code></td>
        <td class="border border-border px-3 py-2"><code>req.body</code> равен <code>undefined</code></td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Авторизация / <code>requireAuth</code></td>
        <td class="border border-border px-3 py-2">защищённых маршрутов</td>
        <td class="border border-border px-3 py-2">обработчик бежит для анонимов</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Перехватчик 404</td>
        <td class="border border-border px-3 py-2">обработчика ошибок, после всех маршрутов</td>
        <td class="border border-border px-3 py-2">не достигается или заслоняет маршруты</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Обработчик ошибок (4-арг)</td>
        <td class="border border-border px-3 py-2">ничего — он ПОСЛЕДНИЙ</td>
        <td class="border border-border px-3 py-2">если первый — не бежит вообще</td>
      </tr>
    </tbody>
  </table>
</div>

<Quiz
  id="05-middleware-and-errors-quiz-1"
  lessonSlug="03-middleware-and-errors"
  lang="ru"
  question="Почему middleware обработки ошибок должен регистрироваться ПОСЛЕДНИМ, после всех маршрутов и обработчика 404, и почему ему нужны четыре аргумента?"
  choices={[
    { label: "Express направляет к обработчику ошибок только через next(err) из более ранних middleware/маршрутов, поэтому он должен стоять после них, чтобы принять их ошибки; и Express узнаёт его по арности — fn.length === 4, (err, req, res, next) — так что выброшенный next переклассифицирует его в обычный middleware", correct: true },
    { label: "Порядок — просто соглашение; Express находит обработчик ошибок где угодно по его имени", misconception: "У Express нет поиска по имени. Он распознаёт обработчик ошибок чисто по сигнатуре из 4 аргументов и доходит до него только через next(err), текущий вперёд, так что важны и позиция, и арность." },
    { label: "Четыре аргумента заставляют его работать быстрее; последним — просто аккуратно", misconception: "Арность — не подсказка о производительности, это то, как Express классифицирует функцию как обработчик ошибок. И последним — обязательно: зарегистрированный до маршрутов, ни один последующий next(err) до него не дойдёт." },
    { label: "Он должен быть последним, чтобы мог вызвать next() и продолжить к маршрутам", misconception: "Обработчик ошибок завершает ответ; он не проваливается к маршрутам. Он последний именно потому, что он терминальный обработчик, и обычно не зовёт next(), кроме случая, когда заголовки уже отправлены." },
  ]}
/>

## Async-ошибки и валидация на границе

Вот ловушка Express 4, порождающая тихие, зависшие запросы. Синхронный `throw` внутри обработчика ловится Express и направляется к обработчику ошибок. Но reject внутри `async`-обработчика — **нет**: Express 4 никогда не `await`-ит твой обработчик, так что зареджекченный промис ускользает в `process` как `unhandledRejection`, запрос клиента висит до таймаута, а твоя граница ошибок его никогда не видит.

```js
// Express 4: этот reject НЕ ловится — запрос висит, срабатывает unhandledRejection
app.get("/users/:id", async (req, res) => {
  const user = await db.findUser(req.params.id); // reject? ускользает из Express
  res.json(user);
});

// Фикс A — try/catch и явный проброс
app.get("/users/:id", async (req, res, next) => {
  try { res.json(await db.findUser(req.params.id)); }
  catch (err) { next(err); } // теперь доходит до границы ошибок
});

// Фикс B — обёртка (или пакет express-async-errors) делает это для каждого маршрута
const wrap = (fn) => (req, res, next) => Promise.resolve(fn(req, res, next)).catch(next);
app.get("/users/:id", wrap(async (req, res) => res.json(await db.findUser(req.params.id))));
```

**Express 5 чинит это на уровне фреймворка**: зареджекченный промис, возвращённый из обработчика, автоматически прокидывается в `next(err)`, так что обёртка становится не нужна. Пока ты не на 5, оборачивай каждый async-обработчик. Спутник этого паттерна — middleware валидации (zod, joi, celebrate), который отвергает кривой ввод *на границе* с `400` до того, как отработает логика любого обработчика, — так что твой бизнес-код может считать, что `req.body` уже правильной формы, а плохой ввод становится чистой типизированной `ValidationError` вместо глубокого `TypeError`.

<Inset kind="why" lang="ru">
Почему reject «вешает» запрос вместо того, чтобы просто уронить? Когда async-обработчик реджектит, никакой код никогда не зовёт `res.send`, `res.json` или `next` для этого запроса. У Express нет собственного таймаута, так что сокет остаётся открытым, и клиент ждёт. Процесс продолжает обслуживать другие запросы; застряло только это одно соединение, пока клиент или прокси не сдадутся (часто 30–60с). Поэтому это хуже падения — это невидимо в метриках успеха и проявляется лишь как выбросы латентности и `unhandledRejection` в логах.
</Inset>

<Quiz
  id="05-middleware-and-errors-quiz-2"
  lessonSlug="03-middleware-and-errors"
  lang="ru"
  question="На Express 4 async-обработчик маршрута делает `await db.query(...)`, который реджектит, без try/catch и без обёртки. Что происходит?"
  choices={[
    { label: "Express никогда не await-ит обработчик, так что reject ускользает из фреймворка: ответ не отправляется, запрос висит до таймаута клиента, а reject всплывает как unhandledRejection уровня процесса — он НЕ доходит до твоего обработчика ошибок", correct: true },
    { label: "Express ловит reject и автоматически направляет его в твой 4-арг обработчик ошибок", misconception: "Это поведение Express 5. Express 4 не await-ит возвращаемые значения обработчика, так что зареджекченный промис для него невидим и никогда не доходит до next(err) — ты должен прокинуть его сам через try/catch или обёртку." },
    { label: "Запрос сразу возвращает 500, потому что промис зареджектился", misconception: "Ничто не шлёт 500 — никакой код в пути запроса не бежит после reject, так что статус никогда не устанавливается. Сокет просто висит открытым, пока клиент или прокси не уйдут в таймаут." },
    { label: "Node ретраит заавейченный вызов, пока он не выполнится", misconception: "У Node нет автоматического ретрая для зареджекченного await. Reject распространяется как необработанный; ретраи — это то, что ты строишь явно, а не дефолт." },
  ]}
/>

## Graceful shutdown по SIGTERM

Оркестраторы останавливают процесс, посылая `SIGTERM` (так делают Kubernetes, Docker `stop` и systemd). Поведение Node по умолчанию для `SIGTERM` — выйти немедленно, обронив каждый запрос в полёте. Graceful shutdown означает: перестать принимать **новые** соединения, дать **существующим** запросам завершиться, освободить ресурсы, затем выйти.

```js
process.on("SIGTERM", async () => {
  isReady = false; // 1. переключи readiness, чтобы LB перестал слать новый трафик

  server.close(async () => { // 2. стоп новым conn; колбэк бежит, когда полёт завершён
    await pool.end();        // 3. закрой пул БД и другие ресурсы
    process.exit(0);
  });

  setTimeout(() => process.exit(1), 10_000).unref(); // 4. жёсткий потолок — не висни вечно
});
```

Важны четыре момента. **Первое**, переключи readiness-пробу в «не готов» *до* `server.close()`, чтобы балансировщик слил тебя вместо отправки запросов на порт, который вот-вот закроется. **Второе**, `server.close()` останавливает новые соединения и зовёт свой колбэк только когда существующие запросы завершились. **Третье**, жёсткий `setTimeout(..., 10_000)` обязателен: HTTP keep-alive оставляет простаивающие соединения открытыми, и простаивающий keep-alive сокет считается «соединением», которое может не дать `server.close()` сработать никогда, — так что ты форсируешь выход по дедлайну. **Четвёртое**, числа: дефолтный `terminationGracePeriodSeconds` Kubernetes — **30с**, после чего он шлёт `SIGKILL` и ты теряешь контроль, — так что твой жёсткий таймаут (например, 10с) должен быть с запасом внутри этого окна.

<TradeoffMatrix
  id="05-middleware-and-errors-tradeoff-1"
  lessonSlug="03-middleware-and-errors"
  lang="ru"
  prompt="По SIGTERM в поде Kubernetes как HTTP-сервис должен завершаться, чтобы раскатка дала ноль обронённых запросов?"
  options={[
    { name: "Переключить readiness в не-готов, server.close() для слива полёта, затем закрыть ресурсы и выйти — с жёстким таймаутом как запасным вариантом", summary: "Слить, потом выйти.", correct: true, justification: "Это production-верная последовательность. Переключение readiness первым заставляет балансировщик перестать слать НОВЫЙ трафик до того, как ты перестанешь принимать, так что ни один запрос не отвергается у двери. server.close() даёт запросам в полёте завершиться, затем ты освобождаешь пул БД и выходишь с 0. Жёсткий setTimeout(…, 10с).unref() страхует от простаивающих keep-alive сокетов, которые иначе не дали бы server.close() сработать, и остаётся внутри 30с terminationGracePeriodSeconds до SIGKILL." },
    { name: "Вызвать process.exit(0) немедленно в обработчике SIGTERM", summary: "Выйти сейчас.", justification: "Это ровно баг из хука. Немедленный выход обрывает каждый запрос в полёте на сокете, что балансировщик репортит как 5xx/502 на каждом деплое. Он также пропускает закрытие пула БД, рискуя утёкшими соединениями. Быстро, но он меняет чистый процесс на обронённые запросы пользователей." },
    { name: "Игнорировать SIGTERM и дать Kubernetes сделать SIGKILL поду после grace-периода", summary: "Дать убить.", justification: "Хуже быстрого выхода: ты ничего не делаешь весь grace-период и затем получаешь SIGKILL, который нельзя перехватить и который не даёт ни шанса завершить работу в полёте или закрыть ресурсы. Ты роняешь запросы И ждёшь 30с, чтобы это сделать. SIGKILL — крайнее средство, а не стратегия завершения." },
    { name: "server.close(), но пропустить переключение readiness и жёсткий таймаут", summary: "Только слив.", justification: "Лучше мгновенного убийства, но остаются два зазора. Без переключения readiness первым LB может всё ещё слать новые запросы на закрывающийся сервер, вызывая ошибки соединения. Без жёсткого таймаута простаивающий keep-alive сокет может не дать server.close() сработать никогда, так что под висит до SIGKILL на 30с. Сливай верно И страхуй края." },
  ]}
/>

<DragOrder
  id="05-middleware-and-errors-drag-1"
  lessonSlug="03-middleware-and-errors"
  lang="ru"
  prompt="Расставь последовательность graceful shutdown после прихода SIGTERM, чтобы раскатка обронила ноль запросов:"
  items={[
    { id: "a", label: "SIGTERM приходит от оркестратора (Kubernetes / Docker / systemd)" },
    { id: "b", label: "Переключи readiness-пробу в 'не готов', чтобы балансировщик слил новый трафик" },
    { id: "c", label: "Вызови server.close(), чтобы перестать принимать новые соединения, пока запросы в полёте завершаются" },
    { id: "d", label: "В колбэке server.close() закрой пул БД и другие ресурсы" },
    { id: "e", label: "process.exit(0) — или жёсткий setTimeout форсирует exit(1), если слив застрял" },
  ]}
  correctOrder={["a", "b", "c", "d", "e"]}
/>

</Explanation>

<FlowDiagram
  label="Запрос течёт через конвейер middleware к ответу или границе ошибок"
  hue="var(--d-systems)"
  perRow={4}
  nodes={[
    { id: "log", label: "logger", sub: "сквозное" },
    { id: "body", label: "парсер тела", sub: "req.body" },
    { id: "auth", label: "auth + валидация", sub: "req.user" },
    { id: "h", label: "обработчик", sub: "логика" },
    { id: "err", label: "граница ошибок", sub: "4-арг" },
    { id: "res", label: "ответ", sub: "клиент" },
  ]}
  edges={[
    { from: "log", to: "body" },
    { from: "body", to: "auth" },
    { from: "auth", to: "h", label: "next()" },
    { from: "h", to: "res", label: "res.json" },
    { from: "h", to: "err", label: "next(err)" },
    { from: "err", to: "res", label: "status+body" },
  ]}
  caption="Каждый middleware зовёт next(), чтобы продвинуться, или next(err), чтобы прыгнуть к единой 4-арг границе ошибок в конце. Граница мапит типизированную ошибку в статус, шлёт безопасное тело клиенту и логирует полную ошибку — а порядок раньше в цепочке (парсинг до маршрута, auth до защищённого маршрута) решает, увидит ли обработчик вообще валидный ввод."
/>

<KeyTakeaway>Порядок — это поведение: парсинг до маршрутов, auth до защищённых маршрутов, обработчик ошибок ПОСЛЕДНИЙ. В Express 4 прокидывай async-reject к 4-арг границе. По SIGTERM сливай через `server.close()` с жёстким таймаутом.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="05-middleware-and-errors-retrieval"
  lang="ru"
  questions={[
    { q: "В Express 4 почему reject внутри async-обработчика маршрута вешает запрос вместо того, чтобы дойти до твоего обработчика ошибок, и какие два фикса?", a: "Express 4 зовёт твой обработчик, но никогда не await-ит возвращённый им промис, так что rejecту некуда деться: Express его не видит, твоя граница ошибок достигается только через next(err), который не зовётся, и никакой код никогда не зовёт res.send/res.json/next для этого запроса. Сокет остаётся открытым — у Express нет встроенного таймаута запроса — так что клиент висит, пока он или прокси не уйдут в таймаут (часто 30–60с), а reject всплывает как unhandledRejection уровня процесса в логах. Это хуже падения, потому что метрики успеха выглядят нормально; проявляется лишь как выбросы латентности. Два фикса: (A) обернуть тело обработчика в try/catch и явно вызвать next(err), чтобы reject вошёл в обычный путь ошибок; или (B) обернуть каждый async-обработчик хелпером вроде const wrap = fn => (req,res,next) => Promise.resolve(fn(req,res,next)).catch(next) (пакет express-async-errors патчит это глобально). Express 5 убирает ловушку целиком: он автоматически прокидывает зареджекченный промис обработчика в next(err)." },
    { q: "Пройди корректный graceful shutdown по SIGTERM и объясни, почему жёсткий таймаут и переключение readiness оба обязательны.", a: "По SIGTERM: (1) переключи readiness-пробу в 'не готов' первым, чтобы балансировщик перестал направлять НОВЫЕ запросы до того, как ты перестанешь их принимать, — иначе LB шлёт трафик на уже закрывающийся сервер и клиенты получают ошибки соединения. (2) Вызови server.close(), который останавливает приём новых соединений и зовёт свой колбэк только когда все запросы в полёте завершились — именно это предотвращает обрыв живых запросов и всплеск 502. (3) В этом колбэке освободи ресурсы: закрой пул БД (await pool.end()), сбрось логи и т.д. (4) process.exit(0). Жёсткий таймаут — setTimeout(() => process.exit(1), 10_000).unref() — обязателен, потому что HTTP keep-alive оставляет простаивающие сокеты открытыми, и простаивающее keep-alive соединение учитывается в соединениях, которых ждёт server.close(), так что без дедлайна колбэк server.close() может не сработать никогда и под зависнет. Цифра 10с должна сидеть внутри дефолтного terminationGracePeriodSeconds Kubernetes в 30с, после которого оркестратор шлёт неперехватываемый SIGKILL и ты теряешь весь контроль." },
  ]}
/>

<Recap lang="ru">
HTTP-фреймворк — это упорядоченный конвейер функций `(req, res, next)`: каждая зовёт `next()`, чтобы продвинуться, или `next(err)`, чтобы прыгнуть к обработчику ошибок, так что порядок регистрации — это поведение: парсер тела должен предшествовать маршрутам, читающим `req.body`, авторизация — защищённым маршрутам, а перехватчик 404 и граница ошибок должны идти после всех маршрутов. Эта граница — один middleware ровно с четырьмя аргументами, `(err, req, res, next)` — Express классифицирует её по арности, — и она делает три вещи: мапит типизированную ошибку в код статуса (ValidationError→400, NotFoundError→404, иначе 500), шлёт безопасное тело клиенту, прячущее стеки и внутренности на 5xx, и логирует полную ошибку с её цепочкой cause для операторов. Острая ловушка Express 4 в том, что reject внутри `async`-обработчика никогда не await-ится, так что он ускользает в `unhandledRejection`, вешает запрос и не доходит до твоей границы — чини через try/catch + `next(err)` или обёртку, тогда как Express 5 прокидывает зареджекченные промисы автоматически, а middleware валидации отвергает плохой ввод с 400 на краю. Наконец, graceful shutdown: по `SIGTERM` переключи readiness в не-готов, чтобы балансировщик слил тебя, вызови `server.close()`, чтобы завершить запросы в полёте, закрой пул БД и выйди — с жёстким `setTimeout`-форс-выходом, потому что простаивающие keep-alive сокеты иначе могут держать сервер открытым дольше 30с `terminationGracePeriodSeconds` Kubernetes и его неперехватываемого `SIGKILL`.
</Recap>
