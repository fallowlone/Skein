infographic hierarchy-tree-curved-line-rounded-rect-node
data
  root
    label Node.js — это среда выполнения, а не язык
    children
      - label V8 — исполняет код
        children
          - label Компилирует JavaScript в машинный код
          - label Тот же движок, что и в Chrome
          - label Сам по себе: ни файлов, ни сокетов
      - label libuv — берёт на себя ожидание
        children
          - label Цикл событий на одном JS-потоке
          - label Неблокирующий асинхронный I/O
          - label Пул потоков — 4 потока по умолчанию
      - label Стандартная библиотека — интерфейс
        children
          - label fs — файлы
          - label http / net — серверы и сокеты
          - label process, crypto, stream
theme
  palette #6366f1 #14b8a6 #f59e0b
