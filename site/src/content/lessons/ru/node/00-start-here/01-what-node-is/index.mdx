---
concepts:
- javascript-runtime
- v8-engine
- libuv
- event-loop
- non-blocking-io
- standard-library
deepensInto: []
estMin: 14
lang: ru
lessonType: topic
level: junior
order: 1
prereqs: []
slug: 01-what-node-is
sources:
- https://nodejs.org/en/learn/getting-started/introduction-to-nodejs
- https://nodejs.org/docs/latest/api/
- https://nodejs.org/en/learn/asynchronous-work/event-loop-timers-and-nexttick
spiral: []
status: ready
summary: 'Node.js — это JavaScript вне браузера: V8 исполняет код, libuv даёт однопоточный event loop с неблокирующим I/O, а встроенная библиотека открывает файлы, сеть и процесс. Силён в I/O, слаб в тяжёлых вычислениях.'
title: 'Что такое Node.js на самом деле'
track: node
unit: 00-start-here
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";
import Infographic from "~/components/diagram/Infographic.astro";
import runtimeAnatomy from "./infographics/runtime-anatomy.svg?raw";

<Hook>
Ты выучил JavaScript, чтобы кнопка на странице меняла цвет. Потом тебе дают `server.js` и говорят: «запусти через `node`». Ни браузера, ни `<button>`, ни `document` — а оно при этом обслуживает десять тысяч API-запросов в один поток и не плавится. Тот же язык, которым ты переключал CSS-класс, теперь читает файлы с диска и общается с базой данных. Как? Node.js — это не новый язык. Это *runtime*, который берёт уже знакомый тебе JavaScript и запускает его там, куда браузер его не пускал: на сервере.
</Hook>

<Crux>Node.js — это JavaScript вне браузера: V8 исполняет код, libuv даёт event loop и async I/O, а stdlib достаёт до файлов и сети.</Crux>

<Explanation>

## Runtime, а не язык: V8 + libuv + стандартная библиотека

Прежде чем написать первую строку серверного кода, стоит разобраться, что такое Node.js на самом деле — потому что модель, которую ты выстроишь здесь, объяснит каждую странность позже: почему нет `window`, почему один медленный эндпоинт может заморозить весь API.

Когда говорят «Node.js», имеют в виду три вещи, свинченные в одну программу.

Первая — это **V8**, тот же движок, что Chrome использует для исполнения JavaScript. V8 берёт твой `.js`-исходник, компилирует его в машинный код и быстро запускает. Но сам по себе V8 только *понимает* JavaScript; он понятия не имеет, как открыть файл или принять сетевое соединение. В спецификации языка нет ни `fs`, ни `http`, ни сокетов.

Вторая часть закрывает эту дыру: **libuv** — C-библиотека, дающая Node **event loop** (цикл событий — механизм, который бесконечно вынимает завершённые задачи из очереди и запускает колбэки) плюс асинхронный **I/O** (ввод-вывод — диск, сеть, таймеры). Именно libuv позволяет одному потоку ждать тысячи соединений сразу, а не блокироваться на каждом. Ещё она держит в фоне небольшой **пул потоков** для тех немногих операций, что ОС не умеет делать асинхронно (часть работы с файловой системой и криптографией).

Третья — это **стандартная библиотека Node**: встроенные модули, открывающие всё это для JavaScript: `fs` для файлов, `http`/`net` для серверов и сокетов, `path`, `process` для самой работающей программы, `crypto`, `stream` и другие. Именно её ты на самом деле `require` или `import`.

```js
// Никакого браузера — это работа стандартной библиотеки.
import { readFile } from "node:fs/promises";
import http from "node:http";

const html = await readFile("./index.html", "utf8"); // libuv читает диск
http.createServer((req, res) => res.end(html))       // V8 исполняет твой колбэк
    .listen(3000);
console.log(`pid ${process.pid} слушает :3000`);      // process = эта программа
```

Зачем вообще нужна такая связка? До Node JavaScript был заперт в браузере, а серверы писали на PHP, Python, Ruby или Java. Node позволил командам писать на **одном языке по всему стеку** — и фронтенд, и бэкенд — и пользоваться огромной экосистемой пакетов **npm** (Node Package Manager — крупнейший реестр ПО в мире). Именно это решение объясняет большую часть того, почему Node захватил серверный тулинг.

<Infographic
  svg={runtimeAnatomy}
  label="Дерево, раскладывающее Node.js на три свинченные вместе части: V8 исполняет код (компилирует JavaScript в машинный код, тот же движок, что в Chrome, но сам по себе без файлов и сокетов); libuv берёт на себя ожидание (цикл событий на одном JS-потоке, неблокирующий асинхронный I/O, пул из 4 потоков по умолчанию); а стандартная библиотека — это интерфейс (fs для файлов, http/net для серверов и сокетов, process/crypto/stream)."
  caption="Node.js — это не язык, а три части в одной программе: V8 исполняет код, libuv берёт на себя ожидание, а стандартная библиотека — это интерфейс к машине."
  hue="var(--d-infra)"
/>

## Один поток, event loop и неблокирующий I/O

Вот идея, из-за которой Node кажется странным после других бэкендов: **твой JavaScript исполняется в одном потоке.** Никакого «поток на запрос». Как же один поток обслуживает тысячи клиентов?

Фокус в **неблокирующем I/O**. Когда твой код просит прочитать файл или сделать запрос к базе, Node *не* садится и не ждёт ответа. Он передаёт запрос в libuv (которая использует ОС или пул потоков), регистрирует колбэк и тут же переходит к другой работе. Когда данные готовы, libuv кладёт колбэк в очередь, и **event loop** — цикл, работающий бесконечно и вынимающий завершённую работу из очередей, чтобы исполнить колбэки, — его запускает. Один поток почти никогда не простаивает и почти никогда не заблокирован, поэтому он чередует тысячи операций «в полёте».

Есть и обратная сторона. Если дать этому единственному потоку **CPU-bound** работу — плотный цикл, хэширующий миллион паролей, ресайз огромной картинки, синхронный разбор гигантского JSON, — то некому принимать запросы, пока он молотит. Event loop **заблокирован**. Все остальные клиенты ждут. I/O разгружается; вычисления — нет.

<FlowDiagram
  label="Запрос входит в Node, его I/O разгружается в libuv, пока единственный поток продолжает обслуживать других, а event loop запускает колбэк, когда данные готовы"
  hue="var(--d-backend)"
  perRow="3"
  nodes={[
    { id: "req", label: "Запрос", sub: "входящий" },
    { id: "thread", label: "JS-поток", sub: "один" },
    { id: "libuv", label: "libuv", sub: "async I/O" },
    { id: "next", label: "Другие клиенты", sub: "без ожидания" },
    { id: "loop", label: "Event loop", sub: "очередь" },
    { id: "cb", label: "Колбэк", sub: "ответ" },
  ]}
  edges={[
    { from: "req", to: "thread" },
    { from: "thread", to: "libuv", label: "файл / бд" },
    { from: "thread", to: "next", label: "не блокируй" },
    { from: "libuv", to: "loop", label: "данные готовы" },
    { from: "loop", to: "cb", label: "запуск" },
  ]}
  caption="I/O передаётся наружу, чтобы единственный поток оставался свободным. Event loop запускает колбэк только когда libuv сигналит о готовности данных — поэтому тяжёлые вычисления, которым нечего разгружать, блокируют всех."
/>

## Где Node силён, а где нет

Эта архитектура не лучше и не хуже бэкендов с «потоком на запрос» — она *заточена* под определённый род работы. Понимая эту заточку, ты знаешь, когда тянуться за Node.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Аспект</th>
        <th class="border border-border px-3 py-2 text-left">JavaScript в браузере</th>
        <th class="border border-border px-3 py-2 text-left">JavaScript в Node.js</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">Где исполняется</td>
        <td class="border border-border px-3 py-2">Внутри веб-страницы, во вкладке браузера пользователя</td>
        <td class="border border-border px-3 py-2">Как отдельный процесс на сервере (или твоей машине)</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Движок</td>
        <td class="border border-border px-3 py-2">V8 (Chrome) / SpiderMonkey / JSC</td>
        <td class="border border-border px-3 py-2">V8 + libuv (event loop, async I/O)</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Платформенные API</td>
        <td class="border border-border px-3 py-2">DOM, <code>fetch</code>, <code>localStorage</code>, Web API</td>
        <td class="border border-border px-3 py-2"><code>fs</code>, <code>net</code>/<code>http</code>, <code>crypto</code>, <code>child_process</code></td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Глобальный объект</td>
        <td class="border border-border px-3 py-2"><code>window</code> / <code>document</code></td>
        <td class="border border-border px-3 py-2"><code>global</code> / <code>process</code> (env, argv, pid)</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Нет доступа к</td>
        <td class="border border-border px-3 py-2">Локальной файловой системе, сырым сокетам</td>
        <td class="border border-border px-3 py-2">DOM, странице (страницы попросту нет)</td>
      </tr>
    </tbody>
  </table>
</div>

Node **отлично** справляется с I/O-bound работой, потому что именно под неё созданы неблокирующий I/O и event loop: HTTP-API и микросервисы, которые в основном ждут базы и другие сервисы, real-time-системы (чаты, живые дашборды) поверх WebSocket и инструменты разработчика (бандлеры, линтеры, CLI), где доминирует чтение и запись файлов. И он **слаб** в тяжёлых CPU-bound вычислениях — перекодировании видео, больших численных симуляциях, объёмной синхронной криптографии — потому что такая работа блокирует единственный поток. Запасные выходы реальны, но осознанны: `worker_threads`, чтобы вынести вычисления с главного потока, или просто язык, лучше подходящий для перемалывания чисел.

Экосистема — вторая половина картины. **npm** — это реестр пакетов и установщик Node, крупнейший реестр ПО в мире. Поверх него лежат фреймворки, с которыми ты встретишься дальше: **Express** и **Fastify** для HTTP-серверов, **NestJS** для структурированных, «с мнением» бэкендов. Относись к этим названиям как к карте, а не как к домашке на сегодня.

<Inset kind="why" lang="ru">
«Однопоточность» — формулировка слегка неточная, и полезно понимать почему. Твой *JavaScript* исполняется в одном потоке, но Node как процесс — нет. libuv держит фоновый пул потоков (по умолчанию 4) для операций с файловой системой и криптографией, которые ОС не умеет делать асинхронно, а ты можешь поднять настоящие `worker_threads` для параллельных вычислений. Модель «один поток» описывает исполнение *твоего* кода и event loop — а не весь runtime. Практический вывод не меняется: не блокируй этот единственный JS-поток синхронными вычислениями.
</Inset>

<Quiz
  id="01-what-node-is-quiz-1"
  lessonSlug="01-what-node-is"
  lang="ru"
  question="Коллега говорит: «Node.js — это новый язык программирования для сервера». В чём точная поправка?"
  choices={[
    { label: "Node.js — это runtime: он запускает язык JavaScript вне браузера, соединяя V8 (исполняет JS) с libuv (event loop + async I/O) и стандартной библиотекой (fs, http, process)", correct: true },
    { label: "Node.js — это JavaScript с дополнительными ключевыми словами, которых нет в браузере", misconception: "Язык — тот же самый JavaScript, что V8 исполняет в Chrome. Node добавляет возможности через встроенные модули (fs, http) и runtime, а не через новый синтаксис языка." },
    { label: "Node.js — это браузер без экрана", misconception: "В Node нет ни DOM, ни window, ни самой страницы. Он делит V8 с браузером, но соединяет его с libuv и серверными API — это другая платформа, а не headless-браузер." },
    { label: "Node.js — это база данных, которая ещё и запускает JavaScript", misconception: "Node — это runtime, а не хранилище. Он общается с базами через библиотеки, но сам по себе, будучи Node, ничего не хранит." },
  ]}
/>

<Quiz
  id="01-what-node-is-quiz-2"
  lessonSlug="01-what-node-is"
  lang="ru"
  question="Один поток Node обслуживает тысячи HTTP-запросов, каждый из которых ходит в базу. Затем ты добавляешь эндпоинт, синхронно ресайзящий огромные картинки. Что произойдёт?"
  choices={[
    { label: "Работа с картинками — CPU-bound, разгружать нечего, поэтому она блокирует единственный поток: все остальные запросы зависают, пока она не закончится; вынеси её в worker_threads или отдельный сервис", correct: true },
    { label: "Ничего — Node автоматически поднимает новый поток на каждый запрос", misconception: "Node не использует «поток на запрос»; твой JS исполняется в одном потоке. I/O разгружается в libuv, но у CPU-работы нечего разгружать, и она блокирует event loop." },
    { label: "Всё нормально, потому что libuv запускает все колбэки параллельно", misconception: "Пул потоков libuv обрабатывает асинхронные операции с файлами/криптографией, а не твой синхронный JS. Плотный CPU-цикл исполняется в главном потоке и блокирует event loop в любом случае." },
    { label: "Запросы к базе сломаются, потому что Node умеет делать только один I/O за раз", misconception: "Именно неблокирующий I/O и позволяет одному потоку держать тысячи запросов «в полёте». Проблема — в CPU-bound ресайзе, а не в I/O." },
  ]}
/>

<TradeoffMatrix
  id="01-what-node-is-tradeoff-1"
  lessonSlug="01-what-node-is"
  lang="ru"
  prompt="Ты решаешь, подходит ли Node.js под нагрузку. Что из этого — конёк Node, а что воюет с его моделью?"
  options={[
    { name: "Real-time-чат: много одновременных WebSocket-соединений, каждое почти всё время простаивает и изредка передаёт короткие сообщения", summary: "I/O-bound, высокая конкурентность.", correct: true, justification: "Именно для этого созданы event loop и неблокирующий I/O. Тысячи почти простаивающих соединений стоят почти ничего, ведь единственный поток никогда не блокируется в ожидании — он просто запускает колбэк, когда приходит сообщение. Высоконагруженная по конкурентности, I/O-bound работа с малым CPU на запрос — это ядро силы Node и причина, почему он доминирует в real-time и API." },
    { name: "Пакетная задача, которая декодирует и перекодирует 4K-видео часами сплошных вычислений", summary: "CPU-bound, блокирующая.", justification: "Это бьёт прямо по модели Node. Часы CPU без I/O, который можно разгрузить, намертво занимают единственный JS-поток и морят голодом все остальные задачи event loop. Тут берут worker_threads для параллелизации, нативные аддоны или просто язык/инструмент, созданный для перемалывания чисел. Node может оркестрировать задачу, но не должен молотить её в главном потоке." },
    { name: "CLI-инструмент, который читает множество мелких файлов, преобразует их и пишет результат (бандлер или линтер)", summary: "I/O-тяжёлый тулинг.", justification: "Отличное попадание и большая часть причины, почему JS-тулчейн написан на Node: работа сводится к чтению и записи файлов, то есть к I/O, который event loop чисто разгружает. CPU на файл — мал. Поэтому бандлеры, линтеры и тест-раннеры поставляются как Node-программы." },
    { name: "HTTP-API, который в основном пересылает запросы в базу и другие сервисы", summary: "I/O-bound API.", justification: "Прямо в зоне Node: обработчик запроса почти всё время ждёт базу и нижестоящие сервисы — это разгружаемый I/O. Один поток чередует тысячи запросов «в полёте». Это каноничная форма Node-бэкенда — при условии, что ни один обработчик не делает тяжёлую синхронную CPU-работу." },
  ]}
/>

</Explanation>

<KeyTakeaway>Node.js = V8 (исполняет JS) + libuv (event loop + async I/O) + stdlib (fs, http, process). Один поток держит много соединений, разгружая I/O; тяжёлый CPU блокирует всех. Лучше для I/O-bound API, real-time и тулинга.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="01-what-node-is-retrieval"
  lang="ru"
  questions={[
    { q: "Назови три части, из которых состоит Node.js, и что делает каждая.", a: "Node.js — это три вещи, свинченные в одну программу. Первая — V8, тот же движок, что у Chrome, который быстро компилирует и исполняет твой JavaScript, но сам по себе понимает только язык: о файлах и сокетах он не знает. Вторая — libuv, C-библиотека, дающая event loop плюс асинхронный I/O (диск, сеть, таймеры) и небольшой фоновый пул потоков для операций, которые ОС не умеет делать асинхронно. Именно libuv позволяет одному потоку ждать тысячи соединений сразу. Третья — стандартная библиотека Node: встроенные модули (fs, http/net, path, process, crypto, stream, …), открывающие всё это для JavaScript; именно её ты на самом деле import или require. Иначе говоря: V8 исполняет код, libuv берёт на себя ожидание, а стандартная библиотека — это интерфейс к машине." },
    { q: "Node исполняет твой JS в одном потоке. Объясни, как он всё же обслуживает тысячи клиентов, и какой единственный род работы это ломает.", a: "Работает через неблокирующий I/O плюс event loop. Когда твой код просит прочитать файл или запросить базу, Node не садится ждать — он передаёт запрос в libuv (которая использует ОС или пул потоков), регистрирует колбэк и сразу переходит к другой работе. Когда данные готовы, libuv ставит колбэк в очередь, а event loop, бесконечно вынимающий завершённую работу из очередей, его исполняет. Поскольку поток почти никогда не заблокирован, он чередует тысячи операций «в полёте» в одном потоке. Ломает это CPU-bound работа: синхронный плотный цикл (хэширование, ресайз картинок, разбор огромного JSON) не имеет I/O для разгрузки, поэтому занимает единственный поток, и все остальные клиенты ждут — event loop заблокирован. Лечится выносом вычислений с главного потока через worker_threads или инструментом/языком, лучше подходящим для тяжёлых вычислений. I/O разгружается; вычисления — нет." },
  ]}
/>

<Recap lang="ru">
Node.js — это не новый язык, а runtime, который запускает уже знакомый тебе JavaScript вне браузера. Он свинчивает три вещи: V8, исполняющий твой JS; libuv, дающую event loop, асинхронный I/O и небольшой фоновый пул потоков; и стандартную библиотеку (fs, http, net, path, process, crypto), открывающую файловую систему, сеть и сам процесс твоему коду. Он появился потому, что позволил писать на одном языке по всему стеку и пользоваться огромной экосистемой npm. Его определяющая модель — единственный JavaScript-поток, управляемый event loop с неблокирующим I/O: медленное ожидание запроса разгружается в libuv, чтобы один поток оставался свободным и обслуживал тысячи других, запуская каждый колбэк, когда его данные готовы. Это делает Node отличным в I/O-bound API, real-time-системах и тулинге разработчика — и слабым в тяжёлых вычислениях, которые блокируют поток и требуют worker_threads или другого языка. В отличие от браузерного JS здесь нет DOM или window; вместо них — global, process и стандартная библиотека. Теперь, когда встретишь API, который необъяснимо тормозит под нагрузкой — даже если он «только читает из базы», — у тебя будет словарь, чтобы задать правильный вопрос: что-то блокирует event loop?
</Recap>
