---
concepts:
- ffi
- koffi
- ffi-napi
- libffi
- dlopen
- struct-marshalling
- memory-ownership
- native-callbacks
deepensInto: []
estMin: 20
lang: ru
lessonType: topic
level: senior
order: 3
prereqs:
- node/12-native-and-ffi/01-native-addons-napi
slug: 03-ffi-and-dlopen
sources:
- https://koffi.dev/
- https://nodejs.org/api/process.html
- https://github.com/node-ffi-napi/node-ffi-napi
- https://github.com/libffi/libffi
spiral: []
status: ready
summary: 'FFI вызывает уже скомпилированную разделяемую библиотеку из JS — без
  C-обвязки и компилятора — объявляя C-прототип и позволяя libffi построить вызов в
  рантайме. Нативную память GC не видит, поэтому неверная сигнатура или
  неосвобождённый указатель роняет процесс.'
title: 'FFI: вызов готовой разделяемой библиотеки из JS через koffi — и кто владеет памятью'
track: node
unit: 12-native-and-ffi
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";
import Infographic from "~/components/diagram/Infographic.astro";
import bindingChoice from "./infographics/binding-choice.svg?raw";

<Hook>
Команде понадобились три функции из вендорского C SDK — `.so` без JavaScript-биндингов, — и никто не хотел тащить на себе C++-аддон. Взяли `ffi-napi`: объявили сигнатуры функций в JS, указали на библиотеку, готово за полдня. На Node 16 работало. Потом прилетело обновление Node, и `npm install` умер, пересобирая нативный бинарь `ref-napi` под новый ABI — стек FFI *сам* является скомпилированным аддоном. Запинили Node и поехали дальше. Через месяцы пришёл настоящий счёт: одна сигнатура объявила 64-битный хэндл устройства как `'int'`, поэтому каждый возвращаемый указатель тихо обрезался до 32 бит. Чаще всего старшие биты были нулями, и всё работало. Под нагрузкой, когда куча легла выше в адресном пространстве, — нет, и процесс ушёл в `SIGSEGV` прямо в пол, без стектрейса, без catch, посреди запроса. Фикс был не «писать ещё C». Он был в понимании того, что FFI делает на самом деле: объявить *точный* ABI, решить, кто что освобождает, и перестать гонять незаложенный биндинг.
</Hook>

<Crux>FFI зовёт готовую `.so` из JS через libffi — без компилятора. Неверная сигнатура или неосвобождённый указатель роняет процесс.</Crux>

<Explanation>

## Что такое FFI — и чем он отличается от нативного аддона

За десять минут ты поймёшь, почему SIGSEGV из хука случился именно там, как FFI работает на уровне ABI, и три правила владения памятью, отделяющие безопасную интеграцию от бомбы замедленного действия.

Прошлые уроки строили **нативный аддон**: ты пишешь C/C++-обвязку под Node-API, компилируешь её в `.node`-бинарь и делаешь `require`. **FFI** (Foreign Function Interface, интерфейс вызова внешних функций) обходится без обвязки вовсе. Ты берёшь *уже скомпилированную* разделяемую библиотеку — `libfoo.so` на Linux, `.dylib` на macOS, `.dll` на Windows, — объявляешь прототип нужной функции *на JavaScript*, и библиотека маршалит вызов в рантайме. Ни своего C-кода, ни компилятора на установке, ни `.node` для сборки: просто открой библиотеку и опиши сигнатуру.

```js
import koffi from 'koffi';

const libm = koffi.load('libm.so.6');             // dlopen разделяемой библиотеки
const pow  = libm.func('double pow(double, double)'); // объявляем C-прототип

pow(2, 10); // 1024 — libffi пакует double'ы по ABI, вызывает, читает результат
```

Та строка `libm.func('double pow(double, double)')` — и есть вся идея: строка, которая *и есть* C-сигнатура. Нет компилятора, сверяющего её с настоящим `pow` из `libm`. Объявление — контракт, который **обеспечиваешь только ты**, и это центральный размен против нативного аддона, где C-компилятор типизирует твою обвязку. FFI покупает тебе «никакого тулчейна, обернуть существующую библиотеку за минуты» ценой «ошибёшься в одном типе — и сказать об этом будет некому».

## libffi: построение кадра вызова в рантайме

Под каждым FFI-биндингом сидит **libffi** (библиотека, динамически строящая кадры вызовов по соглашениям платформенного ABI). У обычного скомпилированного вызова раскладка аргументов вшита на этапе компиляции — компилятор знает положить первое целое в `rdi`, первый float в `xmm0` и так далее по соглашению о вызовах платформы (System V AMD64 на Linux/macOS, Microsoft x64 ABI на Windows). У FFI такой роскоши нет: он узнаёт сигнатуру лишь в рантайме, из твоего объявления. Поэтому libffi *строит кадр вызова динамически* — читает объявленные типы, кладёт каждый аргумент в нужный регистр или слот стека под ABI, прыгает через трамплин в функцию библиотеки и читает возвращаемое значение из регистра возврата.

Цена — этот динамический шаг. Каждый FFI-вызов платит трамплин плюс помаршаливание каждого аргумента — порядка десятков наносекунд *поверх* собственной работы C-функции. Для горстки крупных вызовов на запрос это незаметно. Для горячего цикла с миллионами мелких вызовов это доминирует, и скомпилированный Node-API-аддон — чьи вызовы прямые, без построения кадра в рантайме — выигрывает решительно. Ниша FFI — **крупнозернистые вызовы в существующую библиотеку**, а не болтливый внутренний цикл.

## koffi против ffi-napi: бери поддерживаемый

В Node две линии FFI, и выбор в 2025-м неравный.

Классический стек — **`ffi-napi` + `ref-napi`** — потомки `node-ffi`. Его фатальное свойство — то самое из хука: он *сам* нативный аддон. Установка компилирует C, так что ты наследуешь всю матрицу node-gyp/prebuild/ABI из прошлого урока — ровно ту боль сборки, которую FFI должен был устранить, — плюс поломки на каждом сдвиге ABI в мажоре Node. Сейчас он практически не поддерживается. Не начинай на нём новую работу.

**koffi** — современный ответ. Он поставляет prebuilt-бинари под распространённые платформы (никакого node-gyp на установке), активно поддерживается, работает заметно быстрее и имеет первоклассную поддержку структур, указателей, выходных параметров, колбэков и disposable-типов. Для новой FFI-работы koffi — senior-дефолт; относись к `ffi-napi` как к легаси, с которого *мигрируют*.

<Infographic
  svg={bindingChoice}
  label="Сравнение двух FFI-биндингов Node в два столбца. koffi: prebuilt-бинари без node-gyp на установке, активно поддерживается, поэтому установка переживает мажорное обновление Node, заметно быстрее вызовы, структуры и указатели из коробки. ffi-napi + ref-napi: сам нативный аддон, тащащий всю матрицу node-gyp/prebuild/ABI, ломается на каждом сдвиге ABI в мажоре Node, практически без поддержки, с него мигрируют."
  caption="Разлом в том, кто избегает компилятора: koffi поставляется prebuilt и поддерживается; ffi-napi сам аддон, заново втаскивающий ту боль сборки, которую FFI должен был устранить."
  hue="var(--d-infra)"
/>

## Владение памятью: граница, которую GC не пересекает

Здесь FFI кусается, и это стоит сформулировать жёстким правилом: **сборщик мусора JavaScript не видит нативную память, а C не видит JS-кучу.** Это два отдельных мира без общего учёта. Три следствия, которыми управляешь руками:

- **C-аллокации освобождаешь ты.** Если функция возвращает `malloc`-нутый указатель (`char*`-строку, структуру), GC *никогда* его не вернёт — нужно самому вызвать парную функцию освобождения библиотеки. Забудешь — получишь классическую утечку C-памяти, в Node-процессе, которую не объяснит ни один heap snapshot, потому что она вне кучи.
- **Буферы, что ты передаёшь, должны пережить вызов.** Когда ты отдаёшь C указатель в JS `Buffer` или типизированный массив, эта память безопасна лишь пока JS-объект жив и не сдвинут. Если C хранит указатель и использует его *после* возврата (async-колбэк, удержанный хэндл), нужно держать живую JS-ссылку, чтобы GC не собрал и не переместил буфер — иначе C разыменует освобождённую память.
- **Выходным параметрам нужна явная аллокация.** Многие C-функции заполняют переданный вызывающим буфер (`int read(fd, void* buf, size_t n)`). Ты аллоцируешь его (`koffi.alloc`), передаёшь указатель, затем декодируешь, что C записал.

```js
// C: char* make_greeting(const char* name);  // возвращает malloc-нутую строку
//    void  free_greeting(char* p);            // вызывающий обязан освободить
const make = lib.func('char* make_greeting(const char*)');
const free = lib.func('void free_greeting(char*)');

const ptr = make('Ada');                 // C это malloc-нул — GC не в курсе
const text = koffi.decode(ptr, 'char*'); // копируем байты в JS-строку
free(ptr);                               // освобождаешь ТЫ, иначе течёт вечно
```

<Inset kind="why" lang="ru">
Почему это куда острее обычного Node-бага? Потому что сбой — не JavaScript-исключение. Разыменование освобождённого или сдвинутого указателя — это `SIGSEGV`: ОС убивает процесс. Нет `try/catch`, который спасёт, нет стектрейса на виноватую строку, и крах часто всплывает далеко от настоящего бага — когда освобождённую страницу следующий раз тронут. Невычитанный стрим течёт мягко; неуправляемый FFI-указатель роняет весь сервер без единого слова. Дисциплина ровно как в C: на каждую аллокацию — освобождение, и держи под ссылкой всё, что нативная сторона ещё удерживает.
</Inset>

## Структуры, колбэки и цена неверного типа

Ещё три места, где контракт обязан быть точным:

**Структуры.** Ты объявляешь раскладку (`koffi.struct({ x: 'int', y: 'double' })`), и порядок полей, типы *и выравнивание/паддинг* должны совпадать с C-определением байт в байт. Ошибёшься в паддинге — и каждое поле после него читается с неверного смещения: мусор, не крах, что отлаживать хуже.

**Колбэки.** Чтобы дать C вызвать *обратно* в JS (компаратор, обработчик события), ты регистрируешь JS-функцию как C-указатель на функцию. Две опасности: JS-функция должна оставаться живой, пока C держит указатель (потеряешь ссылку — GC соберёт её под ногами у C), и если библиотека вызывает твой колбэк на *не-JS-потоке*, заход в V8 оттуда роняет процесс — нужна async/registered-обработка колбэков koffi, а не сырой синхронный.

**Неверные скалярные типы.** Баг из хука обобщается: объявишь `'int'` там, где C возвращает 64-битный `size_t`, `long` или указатель, — значение обрезается; объявишь знаковый тип для беззнакового — большие значения переворачиваются в отрицательные. Ничего из этого не проверяется. Объявление — единственный источник истины, и его правильность на тебе.

<TradeoffMatrix
  id="03-ffi-and-dlopen-tradeoff-1"
  lessonSlug="03-ffi-and-dlopen"
  lang="ru"
  prompt="Вендор поставляет закрытую нативную библиотеку (libvendor.so) с тремя функциями, которые надо вызывать несколько раз на запрос из Node-сервиса. Ни исходников, ни JS-биндингов. Какой подход?"
  options={[
    { name: "Через koffi загрузить .so и объявить три сигнатуры функций", summary: "FFI, без тулчейна.", correct: true, justification: "Закрытую .so не перекомпилировать, вызовы крупные (несколько на запрос, не горячий цикл), а koffi не требует C-обвязки или компилятора на установке — объявляешь три прототипа и поехали. Это ровно ниша FFI: быстро обернуть существующую нативную библиотеку. Ты принимаешь in-process риск (крах в библиотеке убивает Node) и обязанность управлять памятью, которую она аллоцирует, но для нескольких крупных вызовов это верный размен." },
    { name: "Написать Node-API-аддон, линкующий libvendor и отдающий три функции", summary: "Компилировать C-обвязку.", justification: "Работает и даёт типизированные, более быстрые вызовы, но это тяжелее задачи: ты берёшь на себя C++-обвязку плюс матрицу node-gyp/prebuild, чтобы вызывать три функции несколько раз на запрос. Преимущество скорости скомпилированного вызова важно лишь в горячем цикле, а это не он. Оставь аддон для случаев, где нужны много быстрых in-process вызовов или плотная C-интеграция." },
    { name: "Перекомпилировать libvendor в WebAssembly и звать in-process", summary: "Нельзя — нет исходников, OS-зависимости.", justification: "WASM нужны исходники для перекомпиляции, и он работает в песочнице без произвольного доступа к ОС — закрытую .so, линкующую системные библиотеки, в портативный .wasm не превратить. WASM — для твоих собственных портативных вычислительных ядер, не для обёртки вендорской готовой нативной библиотеки." },
    { name: "Спавнить крошечный C-обёрточный бинарь на вызов через child_process", summary: "Процесс на вызов.", justification: "Тебе всё равно пришлось бы написать и скомпилировать этот C-враппер (тот самый тулчейн, которого ты избегал), а spawn + IPC на вызов добавляют задержку и операционный вес. child_process подходит для существующего самостоятельного CLI, а не для трёх функций библиотеки, которые куда дешевле звать in-process через koffi." },
  ]}
/>

<Quiz
  id="03-ffi-and-dlopen-quiz-1"
  lessonSlug="03-ffi-and-dlopen"
  lang="ru"
  question="Почему koffi FFI не нужен C-компилятор на установке, а Node-API-аддону нужен?"
  choices={[
    { label: "FFI объявляет сигнатуру функции в JS и через libffi строит кадр вызова в рантайме против уже скомпилированной .so; аддон содержит твою C-обвязку, которую надо скомпилировать в .node", correct: true },
    { label: "FFI запускает C-библиотеку в песочнице, которая её интерпретирует, поэтому нативный код не исполняется", misconception: "FFI исполняет настоящий нативный код in-process на полной скорости — потому крах в библиотеке и роняет Node. Он строит кадр вызова; он не интерпретирует и не песочничает библиотеку." },
    { label: "koffi переписывает C-библиотеку в JavaScript при загрузке", misconception: "koffi не трогает код библиотеки; он грузит скомпилированную .so и зовёт её напрямую через libffi. Ничего не транспилируется в JS." },
    { label: "Node-API-аддоны тоже без компилятора; разница только в скорости", misconception: "Node-API-аддон — это C/C++, который надо скомпилировать (node-gyp) или поставить как prebuild. Требование компилятора и есть настоящая разница; FFI же объявляет сигнатуры против существующего бинаря." },
  ]}
/>

<Quiz
  id="03-ffi-and-dlopen-quiz-2"
  lessonSlug="03-ffi-and-dlopen"
  lang="ru"
  question="C-функция возвращает malloc-нутый char*, который ты читаешь в JS-строку. В чём риск и как обращаться правильно?"
  choices={[
    { label: "GC никогда не освободит malloc библиотеки, поэтому после декодирования — и не раньше — ты сам должен вызвать функцию освобождения библиотеки на возвращённом указателе", correct: true },
    { label: "GC Node соберёт C-строку, как только исчезнет JS-строка, делать нечего", misconception: "GC не видит память, malloc-нутую C-библиотекой, — она вне кучи. Он никогда не освободит этот указатель; оставить его — настоящая утечка, которую не объяснит heap snapshot." },
    { label: "Освободить указатель сразу после вызова, до декодирования, для надёжности", misconception: "Освобождение до копирования байтов в JS-строку — это use-after-free: ты декодируешь освобождённую память и рискуешь segfault. Сначала декодируй, потом освобождай." },
    { label: "Обернуть чтение в try/catch, чтобы segfault стал ловимой ошибкой", misconception: "Segfault от освобождённой/невалидной нативной памяти — это SIGSEGV, убивающий процесс; try/catch его не перехватит. Фикс — корректное владение (освободить один раз, после декодирования), а не обработка исключений." },
  ]}
/>

</Explanation>

<FlowDiagram
  label="Путь FFI-вызова: JavaScript объявляет C-прототип, libffi строит кадр вызова в рантайме по ABI платформы, прыгает в готовую разделяемую библиотеку и маршалит возврат обратно — пока любая память, пересекающая границу, остаётся вне поля зрения GC"
  hue="var(--d-systems)"
  perRow={4}
  nodes={[
    { id: "decl", label: "JS объявляет C-прототип (koffi.func)", col: 0, row: 0 },
    { id: "frame", label: "libffi строит кадр вызова: аргументы в регистры/стек по ABI", col: 1, row: 0 },
    { id: "call", label: "Трамплин в функцию .so (настоящий нативный код, in-process)", col: 2, row: 0 },
    { id: "ret", label: "Возвращаемое значение маршалится обратно в JS", col: 3, row: 0 },
    { id: "mem", label: "Память, пересекающая линию, ВНЕ GC: C-аллокации освобождаешь ты, переданные Buffer держишь под ссылкой", col: 1, row: 1 },
  ]}
  edges={[
    { from: "decl", to: "frame", label: "сигнатура" },
    { from: "frame", to: "call", label: "ABI" },
    { from: "call", to: "ret", label: "результат" },
    { from: "call", to: "mem", label: "указатели" },
  ]}
  caption="Компилятор не запускается: libffi превращает твою рантайм-сигнатуру в настоящий нативный вызов против существующего бинаря. Объявление и владение памятью — на тебе: неверный тип или неосвобождённый/рано освобождённый указатель — это SIGSEGV, а не ловимая ошибка."
/>

<KeyTakeaway>FFI (koffi) зовёт готовую `.so` из JS без компилятора; libffi маршалит по ABI. Граница на тебе: освобождай C-аллокации, держи переданные буферы живыми, объявляй точные типы — неверное это SIGSEGV, убивающий процесс.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="03-ffi-and-dlopen-retrieval"
  lang="ru"
  questions={[
    { q: "Как FFI вызывает нативный код без компилятора и чем он жертвует против Node-API-аддона?", a: "FFI грузит уже скомпилированную разделяемую библиотеку (dlopen) и позволяет объявить C-прототип функции на JavaScript; биндинг вроде koffi через libffi строит кадр вызова в рантайме — кладёт каждый объявленный аргумент в нужный регистр или слот стека по ABI платформы (System V AMD64, Microsoft x64), прыгает трамплином в функцию и читает регистр возврата. Поскольку библиотека уже скомпилирована, а сигнатура задаётся в рантайме, ни своего C-кода, ни компилятора на установке не нужно — в отличие от Node-API-аддона, который есть C/C++-обвязка, требующая компиляции в .node (node-gyp) или поставки как prebuild. Жертвует FFI типовой проверкой компилятора: строка сигнатуры — контракт, который обеспечиваешь только ты, поэтому неверный тип незаметен. Ещё он платит на каждый вызов трамплин + маршалинг (десятки нс), которых прямые вызовы скомпилированного аддона избегают, поэтому FFI подходит для крупных вызовов в существующую библиотеку, а не для горячего внутреннего цикла." },
    { q: "Сформулируй правила владения памятью при пересечении границы FFI и почему ошибка тут уникально опасна.", a: "GC не видит нативную память, а C не видит JS-кучу, поэтому границей управляешь руками. (1) Если функция возвращает malloc-нутый указатель, ты сам вызываешь парную функцию освобождения библиотеки — GC его никогда не вернёт; забудешь — утечка вне кучи, которую не объяснит snapshot. (2) JS Buffer/типизированный массив, переданный по указателю, должен оставаться живым и несдвинутым, пока C его использует; если C удерживает указатель после вызова (async, сохранённый хэндл), держи живую JS-ссылку, иначе C разыменует освобождённую/перемещённую память. (3) Выходным параметрам нужна явная аллокация (koffi.alloc) перед передачей указателя. Уникально опасно это потому, что сбой — не JS-исключение, а SIGSEGV: ОС убивает процесс, try/catch не перехватит, нет JS-стектрейса, и крах часто всплывает далеко от бага, когда освобождённую страницу тронут следующий раз. Плюс важен выбор биндинга: ffi-napi/ref-napi сами нативные аддоны (матрица сборки + поломки ABI, ныне без поддержки), поэтому предпочитай prebuilt и поддерживаемый koffi." },
  ]}
/>

<Recap lang="ru">
FFI позволяет Node вызывать уже скомпилированную разделяемую библиотеку — вендорскую `.so`, системную библиотеку — прямо из JavaScript, без своей C-обвязки и без компилятора на установке: ты делаешь `dlopen` библиотеки и объявляешь C-прототип функции, а биндинг строит вызов в рантайме. Под капотом libffi читает объявленные типы и динамически конструирует кадр вызова, кладя аргументы по ABI платформы, прыгая трамплином в функцию и маршаля возврат — поэтому каждый вызов стоит чуть дороже прямого вызова скомпилированного аддона и поэтому FFI подходит крупным вызовам, а не болтливому горячему циклу. Выбор биндинга важен: классический стек ffi-napi/ref-napi сам является нативным аддоном, поэтому тащит всю проблему node-gyp/prebuild/поломок-ABI и ныне практически не поддерживается, тогда как koffi поставляется prebuilt, поддерживается, работает быстрее и обрабатывает структуры, указатели, колбэки и disposables — поэтому koffi и есть senior-дефолт. Настоящая дисциплина — память: GC не видит, что аллоцирует C, а C не видит JS-кучу, поэтому ты освобождаешь каждый указатель, что библиотека malloc-нула, держишь любой переданный Buffer живым (и несдвинутым), пока C его удерживает, аллоцируешь выходные буферы явно и объявляешь каждый тип точно — потому что неверная сигнатура или неуправляемый указатель это не ловимая JavaScript-ошибка, а SIGSEGV, роняющий весь процесс без стектрейса. FFI — самый быстрый способ обернуть существующую нативную библиотеку и самый лёгкий способ уронить сервер, если забыть, что ты теперь пишешь C. Теперь, когда потянешься к FFI для вендорского SDK, сверь три вещи первым делом: точна ли сигнатура (особенно ширина указателей), кто освобождает возвращаемую память, и не бывает ли колбэк не с JS-потока — именно там и ждёт SIGSEGV.
</Recap>
