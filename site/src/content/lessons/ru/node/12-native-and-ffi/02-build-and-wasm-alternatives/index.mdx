---
concepts:
- node-gyp
- binding-gyp
- prebuilds
- install-time-compilation
- webassembly-wasi
- when-not-to-go-native
deepensInto: []
estMin: 18
lang: ru
lessonType: topic
level: senior
order: 2
prereqs:
- node/12-native-and-ffi/01-native-addons-napi
slug: 02-build-and-wasm-alternatives
sources:
- https://github.com/nodejs/node-gyp
- https://github.com/prebuild/prebuildify
- https://nodejs.org/api/wasi.html
- https://developer.mozilla.org/en-US/docs/WebAssembly
spiral: []
status: ready
summary: 'Нативные аддоны собираются node-gyp из binding.gyp — значит, компилятор на
  этапе установки, если не отгружены prebuilds. WebAssembly, дочерний процесс или
  обычный JS часто обходят это совсем — поэтому ход сеньора обычно НЕ писать аддон.'
title: 'Сборка нативных аддонов, prebuilds и когда не надо: WASM и альтернативы'
track: node
unit: 12-native-and-ffi
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
Новый сотрудник запускает `npm install`, и он взрывается. Три тысячи строк красного заканчиваются на `gyp ERR! find Python` и `node-gyp rebuild failed`. Транзитивная зависимость двумя уровнями ниже — нативный аддон, а на его свежей машине нет ни Python, ни C++-тулчейна, ни подходящих заголовков, — поэтому установка пытается *компилировать C++ из исходников* и не может. Тот же пакет позже ломает CI-образ на Alpine (musl, не glibc) и контрибьютора на Windows (нет Visual Studio build tools). Никто в команде не выбирал поддерживать C++-сборку; они просто `npm install`-нули JSON-парсер, у которого случайно оказался нативный быстрый путь. Цену одной нативной зависимости платит каждая машина, которая её ставит, — а вопрос, который команда так и не задала: нужен ли тому аддону вообще быть нативным.
</Hook>

<Crux>Нативный аддон собирается node-gyp — компилятор при установке без prebuilds; а WASM, дочерний процесс или обычный JS часто это обходят.</Crux>

<Explanation>

## Как аддоны собираются: node-gyp и binding.gyp

Когда ты ставишь нативный аддон без предсобранных бинарников, npm запускает его `install`-скрипт, который обычно зовёт **node-gyp** — оркестратор сборки Node. node-gyp читает файл `binding.gyp` (Python-образный JSON-диалект, унаследованный от GYP из Chromium), генерирует платформенные build-файлы (Makefile, проект Xcode или проект MSVC) и зовёт хостовый компилятор, чтобы получить бинарник `.node`.

```python
# binding.gyp — описывает, что компилировать и как
{
  "targets": [
    {
      "target_name": "addon",
      "sources": [ "src/addon.cc", "src/resize.cc" ],
      "include_dirs": [
        "<!@(node -p \"require('node-addon-api').include\")"
      ],
      "cflags_cc": [ "-O3", "-std=c++17" ]
    }
  ]
}
```

Следствие — ловушка из хука: **сборка из исходников требует полного тулчейна на устанавливающей машине** — самому node-gyp нужен Python, плюс C/C++-компилятор (GCC/Clang или MSVC на Windows) и заголовки Node под запущенную версию. На dev-машине, которую ты контролируешь, это нормально, но делает аддон хрупким везде остальном: минимальные Docker-образы без build-инструментов падают, musl libc в Alpine ломает допущения, заточенные под glibc, Windows требует Visual Studio build tools, и даже на рабочей конфигурации каждый `npm install` платит минуты времени компиляции. Нативная зависимость превращает `install` из «скачать файлы» в «скомпилировать C++-проект» со всей платформенной разнородностью, которую это влечёт.

## Prebuilds: отгружай бинарник, пропусти компилятор

Фикс боли на этапе установки — **не компилировать при установке**: отгружать предсобранные бинарники внутри пакета, по одному на платформу/архитектуру, а при установке просто выбирать подходящий. Это делают две экосистемы: `node-pre-gyp` (скачивает предсобранные бинарники с хоста вроде S3/GitHub Releases) и **prebuildify** (упаковывает все предсобранные бинарники *в публикуемый npm-тарбол*, так что установка делает ноль сети и ноль компиляции — просто копирует нужный файл). prebuildify — модель проще и надёжнее: нет сервера загрузки, от которого зависишь, полностью офлайновые установки.

```jsonc
// package.json — prebuildify кладёт prebuilds/<platform>-<arch>/*.node
{
  "scripts": {
    // запусти раз на каждой целевой платформе (или в CI-матрице) перед публикацией:
    "prebuild": "prebuildify --napi --strip"
  }
}
```

Вот где Node-API окупается второй раз. Поскольку бинарник Node-API **стабилен по ABI между версиями Node** (прошлый урок), ты отгружаешь *один prebuild на платформу/арх*, и он обслуживает каждую версию Node, которую гоняют твои пользователи. Raw-V8/NAN аддон, напротив, требует prebuild на платформу **на каждую ABI-версию Node** — комбинаторная матрица, раздувающая публикуемый пакет и ломающаяся в момент выхода нового мажора Node, пока ты не опубликовал подходящий бинарник. Флаг `--napi` выше делает именно это: режет матрицу prebuild'ов с (платформы × версии Node) до (платформы). В рантайме загрузчик вроде `node-gyp-build` находит нужный prebuild и падает обратно к компиляции из исходников, только если ни один не подошёл.

<Inset kind="why" lang="ru">
Prebuilds сжимают *перекрёстное произведение* риска нативной сборки, но не стирают его: тебе всё равно нужна сборка каждого бинарника под каждую платформу/арх/libc, которые ты поддерживаешь (linux-x64-glibc, linux-x64-musl, linux-arm64, darwin-arm64, win32-x64…), а это CI-матрица и дисциплина публиковать новый prebuild до того, как появится платформа, которой он нужен. Забудь про musl — и пользователи Alpine откатываются к компиляции из исходников, ровно тому отказу, который prebuilds должны были предотвратить. Prebuilds переносят требование тулчейна с твоих пользователей на твой релизный пайплайн; они его не удаляют.
</Inset>

## Альтернативы: WASM, дочерний процесс или просто JS

Прежде чем писать всё это, спроси, нужен ли тебе нативный аддон вообще. Три альтернативы обходят проблему сборки совсем.

**WebAssembly.** Скомпилируй C, C++ или Rust в `.wasm`-модуль и загрузи через `WebAssembly.instantiate`. Артефакт — *единственный, переносимый бинарник*: нет компиляции под каждую платформу, нет тулчейна при установке, один и тот же `.wasm` бежит на каждой ОС и архитектуре. Он исполняется в песочнице без окружающего доступа к файловой системе или сети; когда коду нужны syscalls, **WASI** (WebAssembly System Interface, доступный через `node:wasi`) выдаёт их явно и с ограничением по capability. Трейдофф: данные переходят в линейную память wasm *копированием* (нельзя передать JS-объект), а сырая интеграция с ОС/C-библиотекой за пределами WASI недоступна. Отлично для переносимых вычислительных ядер (парсеры, кодеки, крипто, фильтры изображений); не путь к зову произвольного OS API.

```js
import { readFile } from "node:fs/promises";
const bytes = await readFile("./resize.wasm");
const { instance } = await WebAssembly.instantiate(bytes);
const out = instance.exports.resize(/* указатели в линейную память */);
```

**Дочерний процесс к нативному бинарнику.** Если обкатанный нативный CLI уже существует (ffmpeg, ImageMagick, Rust-утилита), запусти его через `child_process` и общайся через stdio или файлы. Граница грубая, а процесс *изолирован*: крэш или баг памяти в бинарнике убивает дочерний процесс, а не твой Node-процесс. Цена тоже реальна — накладные расходы сериализации/IPC, надо деплоить и версионировать внешний бинарник, и это процесс-на-вызов, если не пулить. Подходит, когда работа уже оформлена как инструмент и ты ценишь изоляцию выше скорости в процессе.

**Обычный JavaScript.** Часто честный ответ. JIT V8 быстр; для очень многих порывов «нам нужен C ради скорости» аккуратная JS-реализация (типизированные массивы, избегание аллокаций в горячих циклах) укладывается в небольшой множитель и несёт *ноль* риска установки, *ноль* build-матрицы и *ноль* кроссплатформенной разнородности. Бремя доказательства — на уходе в нативный код, а не на оставании в JS.

<TradeoffMatrix
  id="02-build-and-wasm-alternatives-tradeoff-1"
  lessonSlug="02-build-and-wasm-alternatives"
  lang="ru"
  prompt="Тебе нужно быстрое, переносимое CPU-ядро (парсер) в Node-библиотеке, которую тысячи команд npm install-нут на Linux, macOS и Windows. Какой подход?"
  options={[
    { name: "Скомпилировать ядро в WebAssembly и грузить через WebAssembly.instantiate", summary: "Один переносимый артефакт.", correct: true, justification: "WASM даёт почти-нативные вычисления как единственный переносимый .wasm, которому не нужен компилятор при установке и который бежит одинаково на каждой ОС/архитектуре — ровно то, что нужно для библиотеки, ставящейся широко по платформам. Он в песочнице (хорошо), а чистому вычислительному ядру вроде парсера не нужны OS-syscalls, поэтому ограничение WASI/доступа к ОС не кусает. Платишь копированием памяти на границе, что пренебрежимо для грубых вызовов. Это обходит всю матрицу node-gyp/prebuild." },
    { name: "Написать нативный аддон на Node-API и отгружать prebuilds под каждую платформу/арх", summary: "Нативный + матрица prebuild.", justification: "Рабочий и быстрый в процессе вариант, но ты берёшь на себя CI-матрицу prebuild'ов (linux glibc/musl, arm64, darwin, win32) и риск, что отсутствующий prebuild откатит пользователей к компиляции из исходников. Для чистого вычислительного ядра без нужды звать C-библиотеку в процессе WASM даёт сравнимую скорость одним артефактом и без этой операционной нагрузки. Резервируй нативный код для случая, когда надо интегрироваться с существующей C-библиотекой или OS API в процессе." },
    { name: "Запускать нативный CLI-бинарник через child_process на каждый разбор", summary: "Процесс на вызов.", justification: "Изоляция приятна, но процесс-на-разбор добавляет накладные spawn + IPC/сериализации, доминирующие для быстрого ядра, вызываемого часто, и пришлось бы деплоить и версионировать отдельный бинарник под каждую платформу — дистрибуция хуже единственного .wasm в пакете. child_process подходит, когда инструмент уже существует и важна изоляция, а не для горячего внутрибиблиотечного ядра." },
    { name: "Реализовать на обычном JavaScript и положиться на JIT", summary: "Ноль риска сборки.", justification: "Легитимный дефолт и часто достаточно быстро — но посылка здесь в измеренной нужде почти-нативной скорости разбора, которую JS не вытягивает. Когда ты это доказал, WASM доводит до цели, сохраняя нулевой риск установки и переносимость единственного артефакта, которые и делают обычный JS привлекательным. Если бы JS хватало, ты бы вообще не выбирал среди нативных вариантов." },
  ]}
/>

## Правило решения: когда НЕ уходить в нативный код

Собери это в правило, применимое до добавления зависимости или написания кода.

<Quiz
  id="02-build-and-wasm-alternatives-quiz-1"
  lessonSlug="02-build-and-wasm-alternatives"
  lang="ru"
  question="Нативный аддон отгружается с prebuildify --napi. Почему флаг --napi драматически сжимает матрицу prebuild'ов по сравнению с NAN/raw-V8 аддоном?"
  choices={[
    { label: "Бинарник Node-API стабилен по ABI между версиями Node, поэтому отгружаешь один prebuild на платформу/арх вместо одного на платформу на каждую ABI-версию Node", correct: true },
    { label: "--napi сжимает бинарники, чтобы они занимали меньше места", misconception: "--napi про целевой ABI, а не про сжатие (это примерно делает --strip). Матрица сжимается, потому что один Node-API бинарник обслуживает все версии Node, а не потому что каждый бинарник меньше." },
    { label: "--napi делает аддон чистым JavaScript, так что бинарники не нужны", misconception: "Это по-прежнему скомпилированный нативный бинарник. --napi выбирает стабильный Node-API вместо ABI V8; он не убирает нативный код." },
    { label: "--napi скачивает бинарники при установке, а не упаковывает их", misconception: "Это модель загрузки node-pre-gyp. prebuildify упаковывает бинарники в тарбол; --napi лишь меняет, на какой ABI они нацелены, схлопывая измерение по версиям Node." },
  ]}
/>

<Quiz
  id="02-build-and-wasm-alternatives-quiz-2"
  lessonSlug="02-build-and-wasm-alternatives"
  lang="ru"
  question="Тебе нужно звать существующий, хорошо поддерживаемый нативный CLI-инструмент (например, ffmpeg) из Node-сервиса, и ты ценишь изоляцию при крэше. Лучший вариант?"
  choices={[
    { label: "Запустить через child_process и общаться через stdio/файлы — грубая граница, инструмент уже существует, а крэш убивает только дочерний процесс", correct: true },
    { label: "Написать Node-API аддон, линкующий C-библиотеки ffmpeg в процессе", misconception: "Возможно, но тяжело: ты берёшь линковку, build/prebuild-матрицу и теряешь изоляцию — segfault в библиотеке теперь рушит твой Node-процесс. Когда CLI уже существует и важна изоляция, child_process — проще и безопаснее." },
    { label: "Перекомпилировать ffmpeg в WebAssembly и гонять в процессе", misconception: "WASM не может свободно лезть в ОС (только через WASI), а переноc большого инструмента вроде ffmpeg — крупная работа; для существующего CLI обычно просто запускают его. WASM хорош для твоих собственных переносимых вычислительных ядер, а не для обёртки целого внешнего инструмента, который можно запустить напрямую." },
    { label: "Переписать кодеки ffmpeg на обычном JavaScript", misconception: "Переписать зрелый набор C-кодеков на JS — огромно и медленнее; весь смысл тянуться к ffmpeg — переиспользовать его. Это противоположность ходу сеньора." },
  ]}
/>

</Explanation>

<FlowDiagram
  label="Поток решения о переходе в нативный код: начни с обычного JavaScript и эскалируй до WebAssembly, дочернего процесса или нативного аддона, только когда конкретная нужда вынуждает"
  hue="var(--d-systems)"
  perRow={4}
  nodes={[
    { id: "start", label: "Нужно больше обычного JS?", col: 1, row: 0 },
    { id: "js", label: "Нет → отгружай обычный JS (ноль риска сборки)", col: 0, row: 1 },
    { id: "cp", label: "CLI-инструмент есть / нужна изоляция → child_process", col: 1, row: 1 },
    { id: "wasm", label: "Переносимое вычислительное ядро → WebAssembly (+WASI)", col: 2, row: 1 },
    { id: "addon", label: "C-библиотека в процессе / OS API + горячий путь → Node-API аддон + prebuilds", col: 3, row: 1 },
  ]}
  edges={[
    { from: "start", to: "js", label: "нет" },
    { from: "start", to: "cp", label: "spawn" },
    { from: "start", to: "wasm", label: "compute" },
    { from: "start", to: "addon", label: "линковать" },
  ]}
  caption="Дефолт — обычный JS; бремя доказательства на уходе в нативный код. Предпочитай WASM для переносимых вычислений и child_process для существующих инструментов — резервируй настоящий аддон для интеграции в процессе с C-библиотекой или OS API на доказанном горячем пути, и тогда отгружай prebuilds."
/>

<KeyTakeaway>Нативные аддоны компилируются node-gyp без prebuilds — а Node-API даёт один prebuild на платформу для всех версий Node. Дефолт — обычный JS; нативно — лишь для C/ОС-интеграции в процессе, иначе WASM или child_process.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="02-build-and-wasm-alternatives-retrieval"
  lang="ru"
  questions={[
    { q: "Почему нативная зависимость так часто ломает npm install на CI/Alpine/Windows и как prebuilds это чинят?", a: "Без предсобранных бинарников установка нативного аддона запускает node-gyp, который читает binding.gyp и компилирует C/C++ из исходников на устанавливающей машине. Это требует наличия полного тулчейна: Python (для node-gyp), C/C++-компилятор (GCC/Clang или MSVC на Windows) и заголовки Node под запущенную версию. Минимальным Docker-образам не хватает build-инструментов, Alpine использует musl libc вместо glibc, а Windows нужны Visual Studio build tools — поэтому компиляция падает, а когда работает, каждая установка платит минуты времени сборки. Prebuilds чинят это, отгружая предсобранные .node бинарники (по одному на платформу/арх), так что установка просто выбирает подходящий вместо компиляции: node-pre-gyp скачивает их с хоста, а prebuildify упаковывает в npm-тарбол для полностью офлайновой установки без компиляции. Node-API усиливает это, потому что ABI-стабильный бинарник обслуживает все версии Node, схлопывая матрицу prebuild'ов с (платформы × версии Node) до (платформы). Prebuilds переносят требование тулчейна с каждого пользователя на твой релизный пайплайн, но ты всё равно должен собрать по одному под каждую платформу/арх/libc, которые поддерживаешь." },
    { q: "Сформулируй правило решения: когда писать нативный аддон, а когда — альтернативу.", a: "Дефолт — обычный JavaScript: JIT быстр, а аккуратная JS-реализация обычно укладывается в небольшой множитель с нулевым риском установки, нулевой build-матрицей и нулевой кроссплатформенной разнородностью, так что бремя доказательства на уходе в нативный код. Эскалируй только под конкретную, доказанную нужду. Если нужны почти-нативные переносимые вычисления (парсер, кодек, крипто, фильтр изображений), предпочитай WebAssembly: единственный переносимый .wasm не нуждается в компиляторе при установке и бежит везде, в песочнице, с WASI, выдающим syscalls явно при необходимости, — предел в том, что данные переходят копированием памяти и нельзя дотянуться до произвольных OS/C API за пределами WASI. Если обкатанный нативный CLI уже существует (ffmpeg, ImageMagick) или нужна изоляция при крэше, запускай его через child_process и общайся через stdio/файлы, принимая накладные IPC и деплой бинарника. Пиши настоящий Node-API аддон, только когда надо интегрироваться в процессе с существующей C-библиотекой или OS API без JS-биндинга, на горячем пути, где грубая граница реально окупается, — и тогда отгружай prebuilds (prebuildify --napi), чтобы твои пользователи никогда не компилировали." },
  ]}
/>

<Recap lang="ru">
Нативный аддон ставится через компиляцию: npm запускает node-gyp, который читает binding.gyp, генерирует платформенные build-файлы и зовёт хостовый компилятор — поэтому каждая установка требует Python, C/C++-тулчейн и подходящие заголовки Node, и именно поэтому нативные зависимости ломаются на минимальных Docker-образах, musl в Alpine и Windows, и нагружают CI временем компиляции. Фикс этой боли — prebuilds: отгружать предсобранные бинарники (node-pre-gyp их скачивает, prebuildify упаковывает в тарбол), чтобы установка просто выбирала нужный, а поскольку бинарник Node-API ABI-стабилен, ты отгружаешь один prebuild на платформу вместо одного на платформу на версию Node. Но более глубокий ход — усомниться, нужен ли аддон вообще. WebAssembly даёт почти-нативные, песочничные, единый-артефакт переносимые вычисления (WASI для явных syscalls) без компилятора при установке — идеально для парсеров и кодеков, но не для произвольного доступа к ОС. Дочерний процесс к существующему нативному CLI даёт грубую, изолированную границу ценой IPC и деплоя бинарника. А обычного JavaScript часто хватает без всякого риска. Правило: дефолт — JS, предпочитай WASM для переносимых вычислений и child_process для существующих инструментов, и резервируй настоящий Node-API аддон для доказанной интеграции в процессе с C-библиотекой или OS API — и тогда отгружай prebuilds, чтобы никто не компилировал при установке.
</Recap>
