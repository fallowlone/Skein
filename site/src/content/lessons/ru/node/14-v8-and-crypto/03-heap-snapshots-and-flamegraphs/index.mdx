---
concepts:
- heap-snapshots
- retained-size
- three-snapshot-technique
- cpu-profiling
- flame-graphs
- leak-vs-churn
deepensInto: []
estMin: 18
lang: ru
lessonType: topic
level: senior
order: 3
prereqs: []
slug: 03-heap-snapshots-and-flamegraphs
sources:
- https://nodejs.org/en/learn/diagnostics/memory
- https://nodejs.org/en/learn/asynchronous-work/dont-block-the-event-loop
- https://developer.chrome.com/docs/devtools/memory-problems/heap-snapshots
spiral: []
status: ready
summary: 'Heap snapshot и flame graph — две линзы на горячем Node-процессе: retained size и diff трёх снапшотов находят leak, V8-профайлер и flame graph находят, куда уходит CPU, — и важно отличить настоящий leak от безобидного churn.'
title: 'Heap snapshots и flame graphs: ищем leak и горячие пути в production Node'
track: node
unit: 14-v8-and-crypto
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
Под перезапускается каждые шесть часов. RSS лезет ровной диагональю со 180MB к лимиту cgroup в 512MB, ядро OOM-килит процесс (OOM — Out of Memory, нехватка памяти), Kubernetes его перезапускает, и линия начинается заново — идеальный sawtooth на дашборде, который все научились игнорировать. «Это просто Node, он течёт, мы подняли лимит». Через три недели всплески p99-латентности начинают ездить по тому же шестичасовому циклу: GC работает тяжелее по мере роста live set, и последний час перед каждым килом заметно медленнее. Никто не добавлял фичу. Leak был `Map`, использованным как пер-реквест-кэш, из которого ничто никогда не выселяло записи — каждый запрос добавлял ключ, ни один не удалялся, — и heap snapshot назвал бы retainer за две минуты.
</Hook>

<Crux>Снапшоты показывают, что удерживается и кем; flame graph — куда уходит CPU. Leak растёт монотонно, churn просто пилит.</Crux>

<Explanation>

## Снимаем снапшот: вытащить heap из процесса

V8 heap snapshot — это полный граф всех достижимых объектов в один момент, с рёбрами, которые держат каждый объект живым. Снять его можно тремя способами, и выбор зависит от того, здоров ли процесс, труднодоступен или вот-вот умрёт.

Изнутри процесса `v8.writeHeapSnapshot()` синхронно сбрасывает файл `.heapsnapshot` — он останавливает мир на всё время, что на многогигабайтном heap может быть секундами, поэтому никогда не зови его на горячем пути. Повесь его на обработчик сигнала, чтобы триггерить по требованию:

```js
const v8 = require("node:v8");
process.on("SIGUSR2", () => {
  const file = `/tmp/heap-${Date.now()}.heapsnapshot`;
  console.log("writing", v8.writeHeapSnapshot(file));
});
// kill -SIGUSR2 <pid>  — снимок без подключённого дебаггера
```

Для процесса, доступного через инспектор, запусти его с `--inspect` и открой `chrome://inspect` в Chrome; вкладка Memory снимает и диффит снапшоты интерактивно. А для случая из хука — процесса, который умирает раньше, чем ты успеваешь его поймать, — `--heapsnapshot-near-heap-limit=2` велит V8 автоматически записать до двух снапшотов по мере приближения к лимиту heap, так что ты получаешь дамп ровно в момент сбоя, а не гадаешь.

Перед любым снапшотом `v8.getHeapStatistics()` — дешёвое первое чтение: `used_heap_size`, `heap_size_limit` и `number_of_native_contexts` (неуклонно растущее число контекстов само по себе классическая подпись leak). Сэмплируй это по интервалу — и у тебя есть сигнал монотонного роста, который оправдывает вытягивание полного снапшота.

```js
const v8 = require("node:v8");
setInterval(() => {
  const s = v8.getHeapStatistics();
  console.log(JSON.stringify({
    used: s.used_heap_size,
    limit: s.heap_size_limit,
    contexts: s.number_of_native_contexts,
    detached: s.number_of_detached_contexts,
  }));
}, 30_000).unref();
```

## Читаем снапшот: shallow vs retained и retaining path

Снапшот бесполезен, пока ты не знаешь, на какой из двух размеров смотришь. **Shallow size** — память, которую занимает сам объект: для обычного объекта это лишь его заголовки и слоты полей, часто крошечно. **Retained size** — память, которая освободилась бы при удалении этого объекта: сам объект плюс всё, достижимое *только* через него. `Map` на 200 байт с shallow size 200 байт может иметь retained size 400MB, потому что он единственное, что держит 400MB записей живыми. **По retained size ты и сортируешь, чтобы найти leak**; shallow size спрячет виновника в самом низу списка.

**Retaining path** отвечает на настоящий вопрос — *почему это всё ещё живо?* Это цепочка ссылок от GC root вниз к объекту. Если она оканчивается на глобале, модульном `const`, замыкании или активном таймере — это якорь твоего leak. **Доминатор** объекта — узел, через который обязан проходить каждый retaining path; удаление доминатора освобождает всё его поддерево, поэтому вид dominator-tree группирует leak в один сворачиваемый блок вместо десяти тысяч отдельных записей.

Сложное на практике — отделить твои объекты от внутренностей V8. Сырой снапшот полон узлов `(system)`, `(compiled code)`, `Map` (это hidden-class, а не JS `Map`) и `(array)` — это бухгалтерия V8, не твои данные. Отфильтруй вид Summary по именам своих конструкторов, отсортируй по retained size и иди по retaining path — байты, с которыми ты можешь что-то сделать, почти всегда укоренены в твоих собственных замыканиях, кэшах или массивах слушателей event-emitter.

<Inset kind="why" lang="ru">
Снапшот содержит только *достижимые* объекты — V8 прогоняет полный GC прямо перед записью, поэтому всё реально собираемое уже исчезло и не появится. Именно это делает снапшот надёжным для охоты на leak: если объект в снапшоте, он достижим, а значит, что-то *намеренно* держит его живым. Задача никогда не «почему это не собралось» (не может — оно достижимо), а «какую ссылку мне нужно разорвать».
</Inset>

## Техника трёх снапшотов: назови выживших

Один снапшот говорит, что велико *сейчас*; он не может сказать, что *растёт*. Техника трёх снапшотов превращает «велико» в «течёт», изолируя объекты, которые выделяются во время работы и затем никогда не освобождаются.

<FlowDiagram
  label="Workflow поиска leak тремя снапшотами: базлайн, прогон подозреваемого пути, затем diff для изоляции объектов, выделенных во время прогона и всё ещё живых"
  hue="var(--d-systems)"
  perRow={3}
  nodes={[
    { id: "s1", label: "Снапшот 1", sub: "базлайн" },
    { id: "ex", label: "Прогон", sub: "N запросов" },
    { id: "s2", label: "Снапшот 2", sub: "после работы" },
    { id: "ex2", label: "Прогон", sub: "N запросов" },
    { id: "s3", label: "Снапшот 3", sub: "после работы" },
    { id: "diff", label: "Diff 1→3", sub: "выжившие" },
  ]}
  edges={[
    { from: "s1", to: "ex", label: "осесть" },
    { from: "ex", to: "s2" },
    { from: "s2", to: "ex2", label: "повтор" },
    { from: "ex2", to: "s3" },
    { from: "s3", to: "diff", label: "сравнить" },
  ]}
  caption="Сними базлайн, прогони подозреваемый путь, снапшот, прогони ещё раз, снапшот. В виде Comparison между снапшотами 1 и 3 объекты, выделенные после снапшота 1 и всё ещё живые на снапшоте 3, — это leak: work-scoped аллокации, которые должны были освободиться, но не освободились."
/>

Конкретно, во вкладке Memory Chrome DevTools: сними снапшот 1 после того, как процесс осел. Прогони подозреваемую нагрузку (повтори N одинаковых запросов). Сними снапшот 2. Прогони *ту же* нагрузку снова. Сними снапшот 3. Теперь переключи дропдаун на **Comparison** и сравни снапшот 3 со снапшотом 1 — или используй селектор «Objects allocated between snapshot 1 and snapshot 2». Всё, что выделено во время прогона и *всё ещё живо двумя снапшотами позже*, подозрительно: не-текущая нагрузка выделяет и освобождает, поэтому её дельта возвращается к нулю. Leak показывает счётчик `# New`, растущий на ту же величину каждый раунд, и `# Deleted`, равный нулю. Этот линейный, повторяемый рост — а не сырой размер — и есть подпись, которой ты доверяешь.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Сигнал</th>
        <th class="border border-border px-3 py-2 text-left">Здорово (churn)</th>
        <th class="border border-border px-3 py-2 text-left">Leak</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">RSS / heap во времени</td>
        <td class="border border-border px-3 py-2">Sawtooth — лезет, GC отбирает, падает назад</td>
        <td class="border border-border px-3 py-2">Монотонно — растёт и не возвращается к базлайну</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Дельта 3 снапшотов за раунд</td>
        <td class="border border-border px-3 py-2">Возвращается к ~0; выделенные объекты удаляются</td>
        <td class="border border-border px-3 py-2">Растёт на фикс. величину каждый раунд; <code>#Deleted</code> ≈ 0</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Retaining path выживших</td>
        <td class="border border-border px-3 py-2">Нет — объекты недостижимы после запроса</td>
        <td class="border border-border px-3 py-2">Укоренён в глобальном Map, замыкании, массиве слушателей, таймере</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Давление GC / время пауз</td>
        <td class="border border-border px-3 py-2">Высокий темп аллокаций, короткие стабильные паузы</td>
        <td class="border border-border px-3 py-2">Паузы удлиняются по мере роста live set</td>
      </tr>
    </tbody>
  </table>
</div>

## CPU-профилирование и flame graphs: куда уходит время

Leak — вопрос памяти; горячий эндпойнт — вопрос CPU, и инструмент тут профайлер, а не снапшот. Сэмплирующий профайлер V8 прерывает процесс тысячи раз в секунду и записывает стек вызовов каждый раз; доля сэмплов, в которых появляется функция, аппроксимирует долю CPU, которую она съела.

Самый легковесный захват — `node --cpu-prof app.js`, который пишет `.cpuprofile`, загружаемый прямо во вкладку Performance Chrome DevTools. Классический путь без зависимостей — `node --prof app.js`, выдающий сырой `isolate-*.log` тиков V8; `node --prof-process isolate-*.log` превращает его в читаемую сводку с разбивкой по «ticks» и bottom-up деревом вызовов. Для flame graph конкретно `0x app.js` или `clinic flame -- node app.js` рендерят стеки как интерактивный SVG.

```bash
# Разовый CPU-профиль, без зависимостей — открывается в DevTools › Performance
node --cpu-prof --cpu-prof-dir=/tmp/prof server.js

# V8 tick processor: сырой лог → ранжированная сводка
node --prof server.js
node --prof-process isolate-*.log > processed.txt

# Flame graph (граф пламени)
npx 0x -- node server.js
```

Читать flame graph механично, когда знаешь оси. **Ширина — это время, а не порядок вызовов**: ширина фрейма — доля сэмплов, в которых он был на стеке, поэтому *самые широкие* фреймы — там, куда ушёл CPU. **Ось Y — глубина стека**: бокс сидит поверх своего вызывающего. **Self time** функции — её собственная ширина минус ширины детей, сложенных над ней; широкий фрейм без ничего сверху делает работу сам (тугой цикл, синхронный `JSON.parse`, regex), а широкий фрейм, широкий лишь из-за высокого стека над ним, — просто горячий *вызывающий*. Плато — широкие плоские вершины — и есть твои цели оптимизации. Горизонтальный порядок не значит ничего; не читай его слева направо как таймлайн.

<TradeoffMatrix
  id="03-heap-snapshots-and-flamegraphs-tradeoff-1"
  lessonSlug="03-heap-snapshots-and-flamegraphs"
  lang="ru"
  prompt="RSS Node-сервиса часами неуклонно лезет к лимиту контейнера и его OOM-килит. Тебе нужен артефакт, называющий retainer. Выбери первый ход."
  options={[
    { name: "Снять heap-снапшоты и сравнить техникой трёх снапшотов, сортируя выживших по retained size и идя по retaining path", summary: "Diff снапшотов.", correct: true, justification: "Неуклонный рост RSS к OOM-килу — учебниковая подпись leak, и вопрос в том, «что держит эти объекты живыми». Снапшоты — единственный артефакт, записывающий граф объектов и retaining paths. Diff трёх снапшотов изолирует объекты, выделенные во время работы и выжившие, сортировка по retained size выводит доминатор, а retaining path называет якорь — глобальный Map, замыкание, неудалённый слушатель. Так leak называется за минуты." },
    { name: "Снять CPU-профиль через --cpu-prof и прочитать flame graph на самый широкий фрейм", summary: "CPU-профиль.", justification: "Flame graph отвечает «куда уходит CPU», а не «что удерживает память». Leak может сидеть в процессе, чей CPU-профиль выглядит идеально плоским. Ты сожжёшь время, подтверждая, что сервис не CPU-bound, и так и не узнаешь, какую ссылку рвать. CPU-профилирование — правильный инструмент для горячего эндпойнта или высокого времени GC, не для монотонного роста памяти." },
    { name: "Добавить --max-old-space-size, чтобы поднять лимит heap и его перестало килить", summary: "Поднять лимит.", justification: "Это лечит симптом и прячет болезнь. Настоящий leak монотонен — подними потолок, и он просто дольше будет до него ползти, пока паузы GC растут с live set, а p99-латентность деградирует всю дорогу вверх. Ты покупаешь время, не фикс, и так и не нашёл retainer. Используй лишь как затычку, пока снимаешь снапшот." },
    { name: "Перезапускать под по расписанию до того, как он упрётся в лимит", summary: "Плановый рестарт.", justification: "Крон-рестарт вокруг leak — операционное отрицание: он замазывает монотонный рост, принимает периодические холодные старты и потерю in-flight-работы и гарантирует, что leak никогда не диагностируют. Это может быть легитимной временной митигацией, но никогда не первый диагностический ход, когда двухминутный снапшот назвал бы retainer." },
  ]}
/>

<Quiz
  id="03-heap-snapshots-and-flamegraphs-quiz-1"
  lessonSlug="03-heap-snapshots-and-flamegraphs"
  lang="ru"
  question="В heap-снапшоте shallow size объекта 80 байт, а retained size 300MB. О чём это говорит?"
  choices={[
    { label: "Сам объект крошечный, но он единственное, что держит ~300MB других объектов достижимыми — удаление освободило бы их все, так что это первейший подозреваемый на leak, которого сортируешь наверх", correct: true },
    { label: "Объект повреждён, потому что shallow и retained size должны совпадать", misconception: "Они почти никогда не совпадают. Shallow size — собственные байты объекта; retained size включает всё достижимое только через него. Большой разрыв нормален для контейнеров вроде Map и массивов — и это ровно сигнал leak." },
    { label: "300MB — собственная аллокация объекта, и V8 считает дважды", misconception: "Retained size — не собственные байты объекта, это shallow size (здесь 80B). Retained size — транзитивное замыкание того, что держит живым только этот объект; двойного счёта нет." },
    { label: "Ничего полезного — стоит сортировать по shallow size", misconception: "Сортировка по shallow size прячет leak: контейнер-виновник мал и тонет вниз. По retained size сортируют именно чтобы вывести объект, якорящий больше всего памяти." },
  ]}
/>

<Quiz
  id="03-heap-snapshots-and-flamegraphs-quiz-2"
  lessonSlug="03-heap-snapshots-and-flamegraphs"
  lang="ru"
  question="Flame graph показывает один очень широкий фрейм почти без ничего сверху. Что значат ширина и голая вершина?"
  choices={[
    { label: "Ширина = доля сэмплов, в которых фрейм был на стеке (его доля CPU), а голая вершина — высокий self time: функция делает работу сама, так что это цель оптимизации", correct: true },
    { label: "Ширина — сколько раз функцию вызвали", misconception: "Ширина — сэмплированное время на стеке, не число вызовов. Функция, вызванная миллионы раз, но мгновенно возвращающая, — щепка; вызванная раз, но крутящая цикл секунду, — широкая." },
    { label: "Голая вершина значит, что функция простаивает и её можно игнорировать", misconception: "Наоборот. Голая вершина — высокий self time: ничего не сложено сверху, потому что фрейм сам ест сэмплы. Это плато — ровно туда, где оптимизировать." },
    { label: "Горизонтальная позиция показывает, что она бежала первой, так что это точка входа", misconception: "Ось X не несёт порядка времени в flame graph; это просто алфавитный/агрегационный порядок. Чтение слева направо как таймлайна — классический misread." },
  ]}
/>

<DragOrder
  id="03-heap-snapshots-and-flamegraphs-drag-1"
  lessonSlug="03-heap-snapshots-and-flamegraphs"
  lang="ru"
  prompt="Расставь шаги поиска подозреваемого leak техникой трёх снапшотов:"
  items={[
    { id: "a", label: "Дай процессу осесть, затем сними снапшот 1 как базлайн" },
    { id: "b", label: "Прогони подозреваемый путь — повтори N одинаковых запросов" },
    { id: "c", label: "Сними снапшот 2, затем прогони те же N запросов снова" },
    { id: "d", label: "Сними снапшот 3, затем переключись на Comparison и диффни 3 против 1" },
    { id: "e", label: "Отсортируй выживших по retained size и иди по retaining path к GC root" },
  ]}
  correctOrder={["a", "b", "c", "d", "e"]}
/>

</Explanation>

<KeyTakeaway>Сортируй снапшоты по retained size, не shallow; diff трёх снапшотов называет объекты, что должны были умереть, а retaining path находит якорь. Ширина flame graph — время CPU. Монотонный рост — leak; sawtooth — churn.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="03-heap-snapshots-and-flamegraphs-retrieval"
  lang="ru"
  questions={[
    { q: "Пройди, как техника трёх снапшотов отличает настоящий leak от безобидного churn, и что добавляет retained size.", a: "Один снапшот говорит лишь, что велико прямо сейчас, а не что растёт, поэтому снимаешь три. Снапшот 1 — базлайн после того, как процесс осел. Прогоняешь подозреваемый путь — повторяешь N одинаковых запросов — и снимаешь снапшот 2; прогоняешь те же N снова и снимаешь снапшот 3. Затем диффишь снапшот 3 против снапшота 1 (или используешь селектор «allocated between 1 and 2») в виде Comparison. Дискриминатор — дельта за раунд: безобидный churn выделяет и освобождает, поэтому его объекты появляются в снапшоте 2, но исчезают к снапшоту 3, и чистая дельта возвращается примерно к нулю — sawtooth. Настоящий leak выделяет work-scoped объекты, которые никогда не освобождаются, поэтому счётчик '# New' лезет на ту же фиксированную величину каждый раунд, пока '# Deleted' остаётся около нуля — линейный, повторяемый рост. Retained size — то, по чему ты затем сортируешь выживших: shallow size — лишь собственные байты объекта (заголовок Map крошечный и утонул бы вниз), а retained size — всё достижимое только через этот объект, так что сортировка по нему выводит маленький контейнер, якорящий сотни MB. Наконец retaining path идёт от GC root к выжившему и называет якорь — модульный Map, замыкание, неудалённый слушатель события, активный таймер — это и есть ссылка, которую ты рвёшь." },
    { q: "У тебя есть flame graph из --cpu-prof. Как читать его, чтобы найти, что оптимизировать, и почему снапшот тут неправильный инструмент?", a: "Flame graph рисует сэмплированные стеки вызовов: ось Y — глубина стека (бокс сидит на вызывающем), а ширина по оси X — доля сэмплов, в которых фрейм был на стеке, то есть его доля времени CPU, НЕ число вызовов и НЕ порядок вызовов. Поэтому сканируешь самые широкие фреймы, потому что туда ушёл CPU. Затем отделяешь self time от времени вызывающего: self time функции — её ширина минус ширины детей, сложенных сверху. Широкий фрейм с высоким стеком над ним — просто горячий вызывающий; широкий фрейм с голой или почти голой вершиной имеет высокий self time — он делает работу сам (тугой цикл, синхронный JSON.parse, катастрофический regex). Эти плоские плато и есть цели оптимизации. Горизонтальный порядок игнорируешь целиком; чтение слева направо как таймлайна — классический misread. Heap snapshot тут неправильный инструмент, потому что он захватывает граф объектов и retaining paths — он отвечает на «что держит память живой», а это вопрос leak. CPU-bound эндпойнт может иметь идеально стабильный, здоровый heap; снапшот не покажет ничего полезного, пока flame graph указывает прямо на горячую функцию." },
  ]}
/>

<Recap lang="ru">
Heap snapshot и flame graph — две линзы на горячем Node-процессе, и они отвечают на разные вопросы. Снапшот — граф достижимых объектов в один момент; снимай его через `v8.writeHeapSnapshot()` по сигналу, через вкладку Memory инспектора или — для процесса, умирающего раньше, чем ты его поймаешь, — через `--heapsnapshot-near-heap-limit`, предварительно сэмплируя `v8.getHeapStatistics()`, чтобы подтвердить монотонный рост. Читай его по retained size, не shallow size, потому что leak обычно — крошечный контейнер, якорящий огромное поддерево; retaining path от GC root называет якорь, а доминатор группирует поддерево. Техника трёх снапшотов превращает «велико» в «течёт»: базлайн, прогон, снапшот, прогон снова, снапшот, затем diff — объекты, выделенные во время работы и выжившие до третьего снапшота, с плоским `# Deleted`, и есть leak. Для CPU тянешься к профайлеру: `--cpu-prof` или `--prof` + `--prof-process`, отрендеренные как flame graph, где ширина — время CPU, а широкая голая вершина — высокий self time, то плато, что ты оптимизируешь. И дисциплина под всем этим — отличать настоящий leak (монотонный рост, удерживается, укоренён) от churn (высокие аллокации, GC отбирает, sawtooth), потому что поднятие `--max-old-space-size` или крон-рестарт лишь оттягивают leak, который ты мог назвать за две минуты. Теперь, когда видишь под, перезапускающийся по аккуратному sawtooth-расписанию, первое, за чем тянешься, — обработчик сигнала и три снапшота, а не увеличение лимита памяти.
</Recap>
