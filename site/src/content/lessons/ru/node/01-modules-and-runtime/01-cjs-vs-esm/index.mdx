---
concepts:
- commonjs-require
- esm-import-export
- static-vs-dynamic-resolution
- live-bindings
- module-detection
- cjs-esm-interop
deepensInto: []
estMin: 16
lang: ru
lessonType: topic
level: middle
order: 1
prereqs:
- base-cs/10-abstraction/03-modules
- base-cs/04-machine-code-to-language/04-the-runtime
slug: 01-cjs-vs-esm
sources:
- https://nodejs.org/api/esm.html
- https://nodejs.org/api/modules.html
spiral: []
status: ready
summary: 'CommonJS резолвит require() синхронно в рантайме и экспортирует копии значений; ESM — статический, поднятый, async-граф с живыми read-only биндингами и top-level await. Node выбирает систему на файл, а на стыке interop команды теряют время.'
title: 'CommonJS против ESM'
track: node
unit: 01-modules-and-runtime
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";
import Infographic from "~/components/diagram/Infographic.astro";
import moduleSystems from "./infographics/module-systems.svg?raw";

<Hook>
Представь кухню с двумя плитами, которые делают одно и то же — газовая и индукционная, — но требуют несовместимую посуду. Чугунная сковорода на газовой греется, переставь её на индукционную — и ничего, потому что каждая плита ждёт посуду, сделанную именно под неё. У Node ровно две такие «плиты» для загрузки твоего кода в программу — *модули* (файлы, на которые ты разбиваешь код) работают внутри *рантайма* (движка, который их и исполняет), — и большая часть дня, потерянного на настройку Node, — это просто файл, собранный под одну систему, который скармливают другой. Вот как это выглядит. Ты добавляешь `"type": "module"` в `package.json`, чтобы получить top-level await, и следующий `node server.js` падает с `ReferenceError: require is not defined`. Ты меняешь виноватый `require()` на `import` — и теперь зависимость взрывается с `Error [ERR_REQUIRE_ESM]`. Затем всплывает `__dirname is not defined` там, где его раньше не было. Ни одно из этого — не баг в твоём коде; это трение двух модульных систем, которые резолвят в разное время, по-разному копируют значения и расходятся даже в том, как файл именует свои импорты.
</Hook>

<Crux>CommonJS резолвит require() синхронно в рантайме; ESM — статический async-граф с live-биндингами; Node выбирает по файлу.</Crux>

Зачем это тебе как разработчику: любое Node-приложение, тест-раннер и публикуемая библиотека собраны из модулей, и в тот день, когда ты смешаешь две системы — или установишь пакет, выбравший другую, — эти ошибки прилетят именно тебе. Понимание того, какая система у файла и почему, превращает полдня гадания в правку в одну строку.

<Explanation>

## CommonJS: синхронно, резолв в рантайме, копия значений

CommonJS (CJS) — изначальная модульная система Node. Ты тянешь модуль через `require()` — обычную функцию, которая работает *во время выполнения твоего кода*: она читает целевой файл с диска, выполняет его и возвращает то, что файл присвоил в `module.exports`. Поскольку это обычный вызов функции, ты можешь `require()` условно, внутри `if`, или собрать путь в рантайме — резолв динамический и идёт строка за строкой.

```js
// math.cjs
function add(a, b) { return a + b; }
let calls = 0;
module.exports = { add, get calls() { return calls; }, bump() { calls++; } };

// app.cjs
const math = require("./math.cjs");   // синхронно: блокирует, пока math.cjs не выполнится
console.log(math.add(2, 3));          // 5
const { add } = math;                 // копия значения — `add` это снимок биндинга
```

Важны два свойства. Первое: `require()` **синхронен и блокирующий** — вызывающая строка не продолжается, пока требуемый файл не отработает полностью; поэтому старт CJS — это обход дерева зависимостей в глубину. Второе: ты получаешь обратно **копию значения на момент require**. Если ты деструктурируешь `const { add } = require(...)`, ты захватил ссылку, существовавшую тогда; если модуль позже переприсвоит `module.exports.add`, твой `add` не изменится. CJS также бесплатно даёт каждому модулю две path-глобали — `__dirname` и `__filename` — потому что резолв локальный и в рантайме.

## ESM: статический, поднятый, асинхронный, с живыми биндингами

ECMAScript Modules (ESM) — это стандарт. Ты объявляешь зависимости через `import`/`export`, которые **статичны**: они обязаны быть на верхнем уровне (не внутри `if` и не в функции), чтобы движок нашёл каждый импорт *до того, как выполнится хоть строка кода*. Этот предварительный анализ — и есть весь смысл: он строит полный граф модулей, поднимает импорты и позволяет бандлерам точно видеть, какие экспорты используются, чтобы выбросить остальное (tree-shaking). Загрузка графа **асинхронна** — это и разблокирует top-level `await`.

```js
// math.mjs
let calls = 0;
export function add(a, b) { return a + b; }
export function bump() { calls++; }
export function getCalls() { return calls; }

// app.mjs
import { add, bump, getCalls } from "./math.mjs";   // поднят, резолвится до запуска
import { readFile } from "node:fs/promises";

const config = await readFile("config.json", "utf8"); // top-level await — без async-обёртки
console.log(add(2, 3));   // 5
bump();
console.log(getCalls());  // 1 — живой биндинг отражает изменение
```

Импортируемые экспорты — это **живые биндинги**, а не копии: `import { add }` — это read-only *вид* на переменную экспортирующего модуля. Если тот модуль мутирует значение за именем, твой импорт видит новое значение — а сам ты переприсвоить импортированное имя не можешь (`add = ...` это `TypeError`). Нет `__dirname`; вместо этого пути выводят из `import.meta.url` (например, `new URL("./data.json", import.meta.url)`).

<Infographic
  svg={moduleSystems}
  label="Сравнение двух модульных систем бок о бок. CommonJS: синхронный require(), блокирующий до выполнения файла, экспорт-копии значений, замороженные на момент require, динамический построчный резолв, без tree-shaking и top-level await. ESM: асинхронный статический граф с поднятыми импортами, разрешёнными до запуска кода, живые read-only биндинги, отражающие текущее значение экспортёра, статический резолв и tree-shaking плюс top-level await."
  caption="Одно корневое различие определяет всё остальное: CommonJS резолвит динамически в рантайме, ESM анализируется статически до выполнения — и время, семантика биндингов и tree-shaking следуют из этого."
  hue="var(--d-infra)"
/>

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Свойство</th>
        <th class="border border-border px-3 py-2 text-left">CommonJS</th>
        <th class="border border-border px-3 py-2 text-left">ESM</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">Синтаксис</td>
        <td class="border border-border px-3 py-2"><code>require()</code> / <code>module.exports</code></td>
        <td class="border border-border px-3 py-2"><code>import</code> / <code>export</code></td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Время загрузки</td>
        <td class="border border-border px-3 py-2">Синхронно, блокирующе</td>
        <td class="border border-border px-3 py-2">Асинхронный граф</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Резолв</td>
        <td class="border border-border px-3 py-2">Динамический, в рантайме</td>
        <td class="border border-border px-3 py-2">Статический, до выполнения</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Семантика импорта</td>
        <td class="border border-border px-3 py-2">Копия значения на момент require</td>
        <td class="border border-border px-3 py-2">Живой read-only биндинг</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Path-глобали</td>
        <td class="border border-border px-3 py-2"><code>__dirname</code>, <code>__filename</code></td>
        <td class="border border-border px-3 py-2"><code>import.meta.url</code></td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Top-level await</td>
        <td class="border border-border px-3 py-2">Нет</td>
        <td class="border border-border px-3 py-2">Да</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Tree-shakeable</td>
        <td class="border border-border px-3 py-2">Нет (динамическая форма)</td>
        <td class="border border-border px-3 py-2">Да (статическая форма)</td>
      </tr>
    </tbody>
  </table>
</div>

<Inset kind="why" lang="ru">
Почему «статичность» покупает tree-shaking? Потому что `import &#123; add &#125; from "./math.mjs"` называет ровно тот биндинг, который ты используешь, и бандлер может доказать, что `bump` нигде не упомянут, и удалить его из вывода — ещё до запуска строки. Экспорты CJS — это обычный объект, собранный в рантайме (`module.exports.add = ...`); инструмент не может узнать, какие ключи выживут, не выполнив программу, поэтому обязан оставить их все. Статическая структура — не вопрос стиля, а то, что делает граф зависимостей анализируемым.
</Inset>

## Как Node решает, какая система у файла

Node не угадывает по содержимому. Он классифицирует каждый файл как CJS или ESM по явному набору сигналов в порядке приоритета:

- **Расширение перекрывает всё.** `.mjs` — всегда ESM; `.cjs` — всегда CJS. Они побеждают остальное и являются однозначным запасным выходом.
- **Поле `"type"` в ближайшем `package.json`.** Для обычного `.js`-файла Node идёт вверх к ближайшему `package.json`: `"type": "module"` делает `.js`-файлы ESM, `"type": "commonjs"` (или отсутствие поля) делает их CJS. Переключение этого одного поля переинтерпретирует каждый `.js`-файл в пакете — именно поэтому `require is not defined` появляется в тот момент, когда ты его добавляешь.
- **Поле `"exports"`** управляет тем, во что резолвятся `import "pkg"` и `require("pkg")`, и через **условные exports** может отдавать разный файл каждому — паттерн dual-package.

```json
{
  "name": "mylib",
  "type": "module",
  "exports": {
    ".": {
      "import": "./dist/index.mjs",
      "require": "./dist/index.cjs",
      "types": "./dist/index.d.ts"
    },
    "./package.json": "./package.json"
  }
}
```

Карта `"exports"` — это больше, чем двойная публикация: это **граница публичного API** твоего пакета. Всё, чего нет в `exports`, недостижимо снаружи — потребители больше не могут глубоко импортировать `mylib/src/internal.js`, даже если файл существует. Эта инкапсуляция — сеньорская причина определять `exports` осознанно, а не давать утечь всей директории.

<FlowDiagram
  label="Как Node классифицирует файл модуля как CommonJS или ESM в порядке приоритета: сначала расширение, затем поле type ближайшего package.json, затем условия exports"
  hue="var(--d-systems)"
  perRow={3}
  nodes={[
    { id: "file", label: "Загрузка файла", sub: "node x.js" },
    { id: "ext", label: ".mjs / .cjs?", sub: "расширение" },
    { id: "type", label: 'поле "type"', sub: "package.json" },
    { id: "esm", label: "ESM", sub: "import/export" },
    { id: "cjs", label: "CommonJS", sub: "require()" },
    { id: "cond", label: '"exports"', sub: "import vs require" },
  ]}
  edges={[
    { from: "file", to: "ext" },
    { from: "ext", to: "esm", label: ".mjs" },
    { from: "ext", to: "type", label: ".js" },
    { from: "type", to: "esm", label: "module" },
    { from: "type", to: "cjs", label: "commonjs" },
    { from: "cond", to: "esm", label: "import" },
    { from: "cond", to: "cjs", label: "require" },
  ]}
  caption="Расширение — жёсткий override; .js доверяется ближайшему package.json type; затем поле exports маршрутизирует import vs require в разные файлы для dual-package."
/>

## Стык interop — где утекает время

Две системы не композируются симметрично, и эта асимметрия — настоящая боль эпохи миграции:

- **ESM может импортировать CJS.** `import express from "express"` работает; объект `module.exports` из CJS становится `default`-экспортом ESM. **Именованные** импорты из CJS-модуля — best-effort: Node статически анализирует исходник CJS, чтобы *угадать* именованные экспорты, но всё, присвоенное динамически (`module.exports[name] = ...`), невидимо, поэтому `import &#123; Router &#125; from "express"` может упасть там, где `import express from "express"; const &#123; Router &#125; = express;` работает.
- **CJS не может синхронно `require()` ESM-модуль** — исторически это кидало `ERR_REQUIRE_ESM`, потому что загрузка ESM асинхронна, а `require` синхронен. Портативный фикс — **dynamic `import()`**, который возвращает промис и работает из любой системы: `const mod = await import("./esm-only.mjs")`. (Свежие версии Node умеют `require()` *синхронный* ESM-граф, но dynamic `import()` — правило, которое держится всегда.)

```js
// CJS-файл, которому нужна ESM-only зависимость
async function main() {
  const { default: chalk } = await import("chalk"); // chalk теперь ESM-only
  console.log(chalk.green("работает из CJS через dynamic import()"));
}
main();
```

<Quiz
  id="01-cjs-vs-esm-quiz-interop"
  lessonSlug="01-cjs-vs-esm"
  lang="ru"
  question="CommonJS-файлу нужна зависимость, поставляемая как ESM-only. require('the-pkg') кидает ERR_REQUIRE_ESM. Какой портативный фикс?"
  choices={[
    { label: "Использовать dynamic import(): const mod = await import('the-pkg') — он возвращает промис и грузит ESM из CJS-файла", correct: true },
    { label: "Добавить \"type\": \"module\" в package.json, чтобы require снова заработал", misconception: "Добавление \"type\": \"module\" превращает твои собственные .js-файлы в ESM (ломая твои require()) — но не даёт синхронному require() тянуть асинхронный ESM-граф. Стык — это sync-vs-async, а не флаг конфига." },
    { label: "Переименовать файл зависимости в .cjs", misconception: "Ты не можешь переписать расширения чужого пакета, и переименование не изменит того, что его исходник на ESM-синтаксисе. Тянись к dynamic import() на своей стороне." },
    { label: "Обернуть require в try/catch и повторять", misconception: "ERR_REQUIRE_ESM детерминирована, а не временна — повтор вызывает тот же заблокированный путь. Фикс — сменить механизм загрузки (dynamic import), а не повторять падающий." },
  ]}
/>

<Quiz
  id="01-cjs-vs-esm-quiz-bindings"
  lessonSlug="01-cjs-vs-esm"
  lang="ru"
  question="Модуль инкрементирует внутренний счётчик и отдаёт его. Потребитель читает счётчик после инкремента. Когда потребитель увидит новое значение?"
  choices={[
    { label: "В ESM, потому что импорты — живые биндинги: read-only вид, отражающий текущее значение экспортёра", correct: true },
    { label: "В CommonJS, потому что require возвращает живую ссылку на module.exports", misconception: "CJS отдаёт копию значения на момент require; если ты деструктурировал биндинг, это снимок, и он не отслеживает позднейшие переприсвоения. Живые именно ESM-импорты, а не CJS." },
    { label: "В обоих, поведение идентично", misconception: "Они расходятся ровно здесь: ESM-импорты — живые read-only биндинги, а деструктурированные CJS-импорты — копии значения, замороженные на момент require." },
    { label: "Никогда — состояние модуля всегда изолировано на импортёра", misconception: "Экземпляры модулей кэшируются и разделяются, а не копируются на импортёра. Разница live-vs-copy — про семантику биндинга, а не про изоляцию." },
  ]}
/>

<TradeoffMatrix
  id="01-cjs-vs-esm-tradeoff-newcode"
  lessonSlug="01-cjs-vs-esm"
  lang="ru"
  prompt="Ты начинаешь новую Node-библиотеку в 2026, которую другие будут устанавливать. Какую модульную стратегию ты публикуешь?"
  options={[
    { name: "Писать на ESM; отдавать dual-package карту exports (условия import + require) с осознанной публичной поверхностью", summary: "ESM-first, dual exports.", correct: true, justification: "Сеньорский дефолт: пиши современный статический ESM, чтобы потребители получали tree-shaking и top-level await, и поставляй условную карту exports, чтобы и import, и require резолвились корректно в долгую эпоху interop. Перечисление только нужных точек входа в exports также фиксирует публичный API, чтобы потребители не лезли глубоким импортом во внутренности. Основная цена — build-тулинг, который хорошо протоптан." },
    { name: "Писать только на CommonJS", summary: "Только CJS.", justification: "Максимально совместимо со старыми потребителями и просто собирать, но ты теряешь tree-shaking и top-level await, а ESM-потребители получают только default-экспорт с best-effort угадыванием имён. Для совсем нового кода это меняет направление платформы на краткосрочную привычность — разумно лишь для строго внутреннего CJS-only кода." },
    { name: "Писать только на ESM, без CJS-условия", summary: "Только ESM.", justification: "Самый чистый исходник и взгляд вперёд, но любой CommonJS-потребитель вынужден тянуться к dynamic import() и не может синхронно require() тебя, что всё ещё ломает заметную часть экосистемы. Защитимо для приложений, которые ты контролируешь от и до, рискованнее для широко устанавливаемой библиотеки, где ты не диктуешь модульную систему потребителя." },
    { name: "Пропустить поле exports; дать импортировать всю директорию пакета по пути", summary: "Без карты exports.", justification: "Легче всего опубликовать, но каждый внутренний файл становится частью де-факто API — потребители глубоко импортируют внутренности, и ты никогда не отрефакторишь без поломки. Поле exports существует именно чтобы провести эту границу; его пропуск — антипаттерн инкапсуляции." },
  ]}
/>

</Explanation>

<FlowDiagram
  label="Стык interop между CommonJS и ESM: ESM импортирует CJS как default-экспорт, а CJS достаёт ESM-модуль только через dynamic import()"
  hue="var(--d-systems)"
  perRow={2}
  nodes={[
    { id: "esm", label: "ESM-файл", sub: "import" },
    { id: "cjs", label: "CJS-файл", sub: "require" },
    { id: "cjsmod", label: "CJS-модуль", sub: "module.exports" },
    { id: "esmmod", label: "ESM-модуль", sub: "export" },
  ]}
  edges={[
    { from: "esm", to: "cjsmod", label: "import → default" },
    { from: "cjs", to: "esmmod", label: "await import()" },
  ]}
  caption="ESM может статически импортировать CJS-модуль (его module.exports становится default; именованные импорты best-effort). CJS не может синхронно require ESM — он обязан использовать dynamic import(), который возвращает промис."
/>

<KeyTakeaway>Для нового кода выбирай ESM: статическая структура даёт tree-shaking и top-level await. ESM импортирует CJS, но CJS не может require ESM синхронно — используй dynamic import(). Сделай exports публичным API пакета.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="01-cjs-vs-esm-retrieval"
  lang="ru"
  questions={[
    { q: "Почему ESM-импорты tree-shakeable, а экспорты CommonJS — нет, и при чём тут «статичность»?", a: "Инструкции import/export в ESM статичны: они обязаны быть на верхнем уровне и называть ровно те биндинги, что ты тянешь, поэтому движок и бандлеры анализируют полный граф модулей до запуска любого кода. Этот предварительный анализ позволяет инструменту доказать, какие экспорты реально упомянуты, и удалить остальное из вывода — это и есть tree-shaking. Экспорты CommonJS противоположны: module.exports — обычный JavaScript-объект, собранный в рантайме (module.exports.add = ..., или даже вычисляемые ключи), а require() — функция, которую можно вызвать условно с рантайм-путём. Бандлер не может узнать, какие ключи выживут, не выполнив программу, поэтому консервативно оставляет их все. Асинхронный, статически анализируемый ESM-граф также включает top-level await, потому что загрузчик может приостановить выполнение модуля, пока промис не разрешится. Так что «статичность» — не стилевой ярлык, а свойство, делающее граф зависимостей известным до выполнения, а tree-shaking и top-level await — прямые следствия." },
    { q: "Пройди правила interop CJS↔ESM и то, как Node решает, какая система у файла.", a: "Node классифицирует каждый файл в порядке приоритета: расширение .mjs — всегда ESM, а .cjs — всегда CJS (жёсткий override); для обычного .js-файла Node читает ближайший package.json, где \"type\": \"module\" делает его ESM, а \"type\": \"commonjs\" или отсутствие поля — CJS; и поле exports пакета, с условиями import/require, может маршрутизировать две системы в разные файлы (паттерн dual-package), заодно фиксируя публичный API. Interop асимметричен. ESM может импортировать CJS-модуль: его module.exports становится default-экспортом, но именованные импорты лишь best-effort, потому что Node статически угадывает имена экспортов CJS и пропускает присвоенное динамически — поэтому безопасная форма это импорт default и затем деструктуризация. CJS не может синхронно require() ESM-модуль, потому что загрузка ESM асинхронна, а require синхронен; исторически это кидало ERR_REQUIRE_ESM. Портативный фикс, который работает всегда, — dynamic import(), возвращающий промис и способный загрузить ESM-модуль изнутри CJS-файла: const { default: x } = await import('esm-pkg')." },
  ]}
/>

<Recap lang="ru">
CommonJS и ESM — две модульные системы, различающиеся временем, семантикой и структурой. CommonJS использует require()/module.exports: резолв динамический и синхронный, вызов блокирует, пока целевой файл не выполнится, деструктурированные импорты — копии значения, замороженные на момент require, и каждый модуль бесплатно получает __dirname/__filename. ESM использует инструкции import/export, которые статичны и подняты, поэтому движок строит весь граф до выполнения — что включает tree-shaking, асинхронный граф, top-level await и живые read-only биндинги, отслеживающие текущее значение экспортёра; пути берут из import.meta.url вместо __dirname. Node решает систему файла в порядке приоритета: расширение .mjs/.cjs перекрывает всё, затем ближайшее поле "type" в package.json управляет .js-файлами, затем поле exports (с условными import/require) может отдавать разные файлы каждому и задаёт границу публичного API пакета. Стык interop асимметричен: ESM может импортировать CJS-модуль (его module.exports становится default; именованные импорты best-effort), но CJS не может синхронно require ESM — тянись к dynamic import(), который возвращает промис и работает с любой стороны. Для нового кода выбирай ESM и относись к полю exports как к осознанной поверхности API, а не как к запоздалой мысли.
</Recap>
