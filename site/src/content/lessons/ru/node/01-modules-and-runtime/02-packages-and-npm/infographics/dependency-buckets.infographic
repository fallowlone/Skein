infographic hierarchy-tree-curved-line-rounded-rect-node
data
  root
    label package.json: 4 вида зависимостей
    children
      - label dependencies — нужны в рантайме каждому потребителю
      - label devDependencies — только сборка и тесты, не идут в прод
      - label peerDependencies — хост-приложение обязано их предоставить
      - label optionalDependencies — установка может упасть, код это учитывает
theme
  palette #6366f1 #14b8a6 #f59e0b #f43f5e
