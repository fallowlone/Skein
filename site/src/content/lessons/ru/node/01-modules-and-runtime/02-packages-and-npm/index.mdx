---
concepts:
- package-json
- dependency-types
- semver-ranges
- lockfiles
- npm-ci-vs-install
- node-modules-resolution
deepensInto: []
estMin: 16
lang: ru
lessonType: topic
level: middle
order: 2
prereqs: []
slug: 02-packages-and-npm
sources:
- https://docs.npmjs.com/cli/v10/configuring-npm/package-json
- https://docs.npmjs.com/about-semantic-versioning
- https://docs.npmjs.com/cli/v10/commands/npm-ci
spiral: []
status: ready
summary: 'package.json объявляет намерение через semver-диапазоны; lockfile пинит точное
  разрешённое дерево. npm install может переписать lock, npm ci ставит строго из него
  — поэтому коммить lockfile и используй npm ci в CI.'
title: 'package.json, npm, semver и lock-файлы'
track: node
unit: 01-modules-and-runtime
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
Сборка зелёная на твоём ноуте и красная в CI, а кода никто не трогал. Дифф — одна транзитивная зависимость: библиотека логирования, которую ты ни разу не импортировал, выкатила патч за ночь, твой `^`-диапазон молча его проглотил, и он сломался на CI-образе. В `package.json` написано `"pino": "^9.0.0"` — та же строка, что и вчера, — поэтому манифест «не менялся». Изменилось то, во что `^` *разрешается* сегодня. Фикс — одно слово в пайплайне: `npm ci`, который ставит точное дерево, уже кем-то залоканное, вместо того чтобы решать диапазоны заново.
</Hook>

<Crux>package.json объявляет диапазоны; lock-файл пинит точное дерево. npm install может переписать lock; npm ci ставит из него строго.</Crux>

<Explanation>

## package.json: манифест намерения

`package.json` — это контракт Node-проекта: идентичность, скрипты и от чего он зависит. Поля, которые отрабатывают свой хлеб на уровне middle+:

```json
{
  "name": "billing-service",
  "version": "2.4.1",
  "type": "module",
  "engines": { "node": ">=20" },
  "exports": { ".": "./dist/index.js" },
  "scripts": { "build": "tsc", "test": "vitest run" },
  "dependencies": { "pino": "^9.0.0" },
  "devDependencies": { "vitest": "~2.1.0", "typescript": "5.6.3" },
  "peerDependencies": { "react": ">=18" },
  "optionalDependencies": { "fsevents": "^2.3.0" }
}
```

`"type": "module"` решает, как Node трактует `.js`-файлы (ESM vs CommonJS — прошлый урок). `"exports"` — современная карта входов: она задаёт ровно те пути, которые потребителям разрешено импортировать, и *прячет всё остальное*, поэтому глубокие импорты в твои внутренности перестают работать — она вытесняет старое одиночное поле `"main"`. `"engines"` объявляет диапазон Node, который ты поддерживаешь; со строгой конфигурацией установщика это может жёстко уронить установку на неверном runtime, а не падать загадочно во время выполнения.

Четыре корзины зависимостей не взаимозаменяемы:

- **`dependencies`** — нужны в runtime любому, кто ставит твой пакет. Отгружаются.
- **`devDependencies`** — нужны только для сборки и тестов (компиляторы, тест-раннеры, линтеры). Ставятся локально, пропускаются при `npm install --omit=dev` / `NODE_ENV=production`.
- **`peerDependencies`** — «я работаю *вместе* с этим, но host-приложение должно его предоставить». React-плагин указывает `react` здесь, чтобы делить единственную копию хоста, а не тащить второй, дублирующий React.
- **`optionalDependencies`** — установка пробуется, но провал не фатален; твой код обязан подстраховаться на случай отсутствия. Используется для платформенных нативных аддонов вроде `fsevents` (только macOS).

## Semver: диапазон — это обещание, а не пин

Версии — это `MAJOR.MINOR.PATCH`, и по спеке npm каждая цифра — обещание потребителям: **PATCH** = обратно-совместимые багфиксы (`1.0.0` → `1.0.1`), **MINOR** = обратно-совместимые новые фичи (`1.0.0` → `1.1.0`), **MAJOR** = изменения, ломающие обратную совместимость (`1.0.0` → `2.0.0`). Версия может нести prerelease-тег вроде `1.0.0-beta.2`, который сортируется *до* `1.0.0` и исключается из обычных диапазонов, пока ты явно не попросишь.

То, что ты пишешь в `package.json`, — обычно *диапазон*, и оператор решает, сколько дрейфа ты принимаешь на следующей установке:

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Спека</th>
        <th class="border border-border px-3 py-2 text-left">Допускает до (для 1.2.3)</th>
        <th class="border border-border px-3 py-2 text-left">Пускает</th>
        <th class="border border-border px-3 py-2 text-left">Когда</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2"><code>^1.2.3</code></td>
        <td class="border border-border px-3 py-2"><code>&lt;2.0.0</code></td>
        <td class="border border-border px-3 py-2">minor + patch</td>
        <td class="border border-border px-3 py-2">по умолчанию; доверяешь semver либы</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2"><code>~1.2.3</code></td>
        <td class="border border-border px-3 py-2"><code>&lt;1.3.0</code></td>
        <td class="border border-border px-3 py-2">только patch</td>
        <td class="border border-border px-3 py-2">осторожно; нужны фиксы, не фичи</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2"><code>1.2.3</code></td>
        <td class="border border-border px-3 py-2"><code>1.2.3</code></td>
        <td class="border border-border px-3 py-2">ничего</td>
        <td class="border border-border px-3 py-2">точный пин; максимум детерминизма</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2"><code>*</code></td>
        <td class="border border-border px-3 py-2">любую версию</td>
        <td class="border border-border px-3 py-2">всё, включая мажоры</td>
        <td class="border border-border px-3 py-2">никогда в проде</td>
      </tr>
    </tbody>
  </table>
</div>

`^` — это то, что `npm install <pkg>` пишет по умолчанию, потому что он подхватывает багфиксы и новые фичи автоматически, при этом *обещая* никогда не пересекать мажор. Риск прямо в хуке: это обещание зависит от того, версионирует ли мейнтейнер честно. Библиотека, выкатившая ломающее изменение как minor — или просто случайную регрессию в patch — потечёт прямо в твоё дерево на следующей не-запиненной установке. `^` решает *потолок* того, что может войти. Что *реально* вошло в прошлый раз, решает кое-что другое.

<Inset kind="why" lang="ru">
`^0.x` — ловушка. Ниже `1.0.0` caret особым образом схлопывается до только-patch: `^0.2.3` означает `>=0.2.3 <0.3.0`, а не `<1.0.0`. Логика в том, что пакеты до 1.0 объявляют себя нестабильными, поэтому каждый bump `0.MINOR` трактуется как потенциально ломающий. Многие реальные зависимости сидят на `0.x` годами, так что это правило кусает чаще, чем ожидают.
</Inset>

## Lockfile: что реально установилось

`package.json` отвечает на «что я готов принять?» `package-lock.json` отвечает на «что я получил?» Когда npm разрешает твои диапазоны, он обходит весь граф зависимостей — твои зависимости, их зависимости, до самого низа — выбирает конкретную версию для каждого узла и пишет всё уплощённое дерево в lockfile: точные версии, разрешённый URL registry и хэш **integrity** (SHA в духе Subresource Integrity для tarball'а). На следующей установке npm может пересобрать байт-в-байт идентичный `node_modules` и проверить каждую загрузку против её хэша.

Это и есть граница между двумя командами установки, и это сеньорный пойнт урока:

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left"></th>
        <th class="border border-border px-3 py-2 text-left"><code>npm install</code></th>
        <th class="border border-border px-3 py-2 text-left"><code>npm ci</code></th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">Источник истины</td>
        <td class="border border-border px-3 py-2">заново решает диапазоны package.json</td>
        <td class="border border-border px-3 py-2">ставит строго из lockfile</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Меняет lockfile?</td>
        <td class="border border-border px-3 py-2">да — может добавить/обновить записи</td>
        <td class="border border-border px-3 py-2">нет — никогда не пишет lock</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Нужен lockfile?</td>
        <td class="border border-border px-3 py-2">нет (создаёт, если нет)</td>
        <td class="border border-border px-3 py-2">да — без него ошибка</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Lock не совпал с манифестом</td>
        <td class="border border-border px-3 py-2">обновляет lock, чтобы примирить</td>
        <td class="border border-border px-3 py-2">выходит с ошибкой</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">node_modules</td>
        <td class="border border-border px-3 py-2">патчит на месте</td>
        <td class="border border-border px-3 py-2">сперва удаляет, чистая установка</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Поставить один пакет?</td>
        <td class="border border-border px-3 py-2">да (<code>npm i lodash</code>)</td>
        <td class="border border-border px-3 py-2">нет — только весь проект</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Используй для</td>
        <td class="border border-border px-3 py-2">локальная разработка, добавление/удаление deps</td>
        <td class="border border-border px-3 py-2">CI, деплои, любая воспроизводимая сборка</td>
      </tr>
    </tbody>
  </table>
</div>

Сеньорное правило вытекает из таблицы. **Коммить lockfile**, и в CI запускай `npm ci`, а не `npm install`. `npm ci` детерминирован (игнорирует диапазоны и ставит ровно то, что залокано), чист (сначала стирает `node_modules`, так что не остаётся обрывков состояния), быстр (пропускает шаг разрешения) и — критично — *падает громко*, если `package-lock.json` и `package.json` разошлись, вместо того чтобы молча переписать lock, как сделал бы `npm install`. Это падение — фича: оно ловит вручную отредактированный манифест, который никто не перелочил, до того как он уедет.

```bash
# Локально: меняем deps, что обновляет package.json И package-lock.json
npm install lodash      # добавляет "^4.x" в deps, пишет разрешённое дерево в lock
git add package.json package-lock.json   # коммить ОБА, вместе

# CI / Dockerfile: воспроизводим залоканное дерево точно, падаем при дрейфе
npm ci                  # чисто, детерминированно, ошибка если lock рассинхронен
```

## node_modules: уплощение, hoisting и фантомы

npm ставит в `node_modules` как *плоское* дерево там, где может: вместо вложения каждой зависимости внутрь родителя он **поднимает** (hoist) общие, совместимые версии наверх, чтобы много пакетов делили одну копию. Это держит дерево мелким и маленьким — но оно протекает. Пакет, который твоё приложение никогда не объявляло, может оказаться поднятым на верх `node_modules`, и `require("that-package")` *сработает* на твоей машине. Это **фантомная зависимость**: ты используешь то, чего нет в твоём `package.json`. В день, когда апгрейд зависимости перестанет её поднимать — или свежая установка разрешит другое дерево — твой импорт исчезнет, и сборка сломается, а в манифесте ничего не объяснит почему.

Это одна из причин, по которой существует **pnpm**: он строит строгий, симлинкованный `node_modules`, где пакет видит только то, что явно объявил, превращая фантомные зависимости в жёсткую ошибку, а не в тихий костыль. **Yarn** — другая крупная альтернатива, со своим lockfile (`yarn.lock`) и, в современных версиях, режимом Plug'n'Play, который вообще убирает `node_modules`. Все три решают ту же задачу, что и npm; различаются форматом lockfile и тем, насколько агрессивно изолируют дерево. Переносимый урок не зависит от инструмента: объявленная зависимость плюс закоммиченный lockfile — единственная комбинация, которая воспроизводится.

<Inset kind="why" lang="ru">
Lockfile — это ещё и твой первый контроль supply-chain. Хэш integrity у каждой записи означает, что подменённый tarball в registry провалит проверку при установке, а пиннинг через lock значит, что атакующий не подсунет вредоносный патч через твой `^`-диапазон между сборками. Запускай `npm audit`, чтобы вытащить известные CVE из залоканного дерева, и относись к lockfile как к проверяемому, закоммиченному артефакту — внезапное изменение в нём в PR заслуживает того же внимания, что и изменение кода, потому что оно может подменить ровно то, что исполняется.
</Inset>

<FlowDiagram
  label="От semver-диапазона в package.json через разрешение и lockfile к воспроизводимому node_modules"
  hue="var(--d-runtime)"
  perRow="3"
  nodes={[
    { id: "manifest", label: "package.json", sub: "^9.0.0" },
    { id: "resolve", label: "разрешить", sub: "выбор версий" },
    { id: "lock", label: "package-lock", sub: "9.4.2 + хэш" },
    { id: "ci", label: "npm ci", sub: "читать lock" },
    { id: "verify", label: "проверка хэша", sub: "integrity" },
    { id: "tree", label: "node_modules", sub: "байт-в-байт" },
  ]}
  edges={[
    { from: "manifest", to: "resolve", label: "npm install" },
    { from: "resolve", to: "lock", label: "запись" },
    { from: "lock", to: "ci", label: "коммит" },
    { from: "ci", to: "verify" },
    { from: "verify", to: "tree", label: "воспроизвести" },
  ]}
  caption="npm install разрешает диапазоны и пишет lockfile один раз; каждый последующий npm ci читает этот lock, проверяет хэши integrity и пересобирает то же дерево. Диапазон задаёт потолок; lockfile пинит реальность."
/>

<Quiz
  id="node-02-packages-quiz-1"
  lessonSlug="02-packages-and-npm"
  lang="ru"
  question="CI падает на зависимости, которую ты не менял. package.json всё ещё содержит тот же ^-диапазон. Самая вероятная причина и фикс?"
  choices={[
    { label: "Транзитивная зависимость выпустила новый minor/patch, который ^-диапазон и заново-решающий install подтянул; переведи CI на npm ci, чтобы он ставил точное залоканное дерево", correct: true },
    { label: "Lockfile повреждён — удали package-lock.json и запусти npm install", misconception: "Удаление lock и переустановка заново решает все диапазоны с нуля — ровно то, что и пустило новую версию. Ты свежо подтянешь дрейф, а не починишь его. Держи lock и используй npm ci." },
    { label: "package.json наверняка менялся; проверь git blame по нему", misconception: "Манифест реально не менялся — в этом вся ловушка. ^-диапазон — это стабильный текст, под которым двигается разрешённая версия. Изменилось то, во что разрешается диапазон, а не строка." },
    { label: "Запинь каждую зависимость на точную версию для надёжности", misconception: "Точный пин прямых deps не пинит их транзитивные deps, откуда и пришёл этот дрейф. Lockfile уже пинит всё транзитивное дерево; использовать npm ci — точечный фикс." },
  ]}
/>

<Quiz
  id="node-02-packages-quiz-2"
  lessonSlug="02-packages-and-npm"
  lang="ru"
  question="require('lodash') работает локально, но lodash нет в твоём package.json. Что происходит?"
  choices={[
    { label: "Фантомная зависимость: lodash подняли (hoist) на верх node_modules другим пакетом, поэтому require разрешается — пока будущая установка не перестанет его поднимать и импорт не сломается", correct: true },
    { label: "В Node lodash есть как встроенный модуль", misconception: "lodash — сторонний пакет, а не модуль ядра Node. Резолвер нашёл его в node_modules, а не среди встроенных." },
    { label: "Всё нормально — если разрешается, значит фактически это зависимость", misconception: "Оно разрешается по случайности hoisting, а не по объявлению. Ничто не гарантирует, что оно останется поднятым; верный фикс — добавить его в package.json (или убрать использование)." },
    { label: "npm install всегда ставит lodash по умолчанию", misconception: "npm ставит только объявленное плюс их транзитивные deps. lodash присутствует, потому что объявленная зависимость его подтянула и он поднялся, а не потому что npm добавляет его автоматически." },
  ]}
/>

<TradeoffMatrix
  id="node-02-packages-tradeoff-1"
  lessonSlug="02-packages-and-npm"
  lang="ru"
  prompt="Команда хочет воспроизводимые установки в CI и надёжные, отлаживаемые апгрейды. Какую политику установки принять?"
  options={[
    { name: "Коммить package-lock.json; npm install локально для смены deps, npm ci в CI и деплоях", summary: "Lockfile — источник истины.", correct: true, justification: "Это стандартная сеньорная политика. Закоммиченный lockfile пинит точное транзитивное дерево с хэшами integrity, поэтому каждый запуск CI и деплой воспроизводят байт-в-байт идентичный node_modules. npm ci детерминирован, чист, быстр и падает громко, если lock и манифест разошлись — ловя неперелоканные манифесты до отгрузки. Разработчики по-прежнему используют npm install локально, чтобы добавить или обновить зависимости, что обновляет оба файла, и изменение lockfile ревьюится в PR как любой другой артефакт." },
    { name: "gitignore lockfile; запускать npm install везде", summary: "Всегда заново решать диапазоны.", justification: "Без закоммиченного lock каждая машина решает диапазоны независимо и может приземлиться на разные версии — ровно та невоспроизводимость, о которой этот урок. Транзитивный minor, выпущенный за ночь, тихо течёт в CI. Lockfile — это артефакт, делающий установки воспроизводимыми; игнорировать его — выбросить это." },
    { name: "Коммить lockfile, но запускать npm install и в CI", summary: "Lock есть, но install в CI.", justification: "Лучше, чем без lock, но npm install в CI всё ещё может обновить lock, чтобы примирить дрейф, и пропатчить node_modules на месте, а не сделать чистую установку — поэтому он не упадёт громко на устаревшем манифесте и может оставить обрывки состояния. npm ci существует ровно для случая CI: строго, чисто и с ошибкой при несовпадении." },
    { name: "Запинить каждую прямую зависимость на точную версию и пропустить lockfile", summary: "Точные пины, без lock.", justification: "Точный пин прямых deps ничего не даёт их транзитивным deps, которые всё равно разрешаются по своим диапазонам на каждой установке — поэтому дерево всё равно дрейфует и невоспроизводимо. Lockfile — это то, что пинит весь транзитивный граф; точные прямые пины ему не замена." },
  ]}
/>

</Explanation>

<KeyTakeaway>Диапазоны задают потолок (`^` minor+patch, `~` patch, точная — ничего); lockfile пинит точное дерево с хэшами. Коммить его; `npm install` для смены deps, `npm ci` в CI — чисто, детерминированно, падает при дрейфе.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="node-02-packages-retrieval"
  lang="ru"
  questions={[
    { q: "Объясни разницу между npm install и npm ci и почему CI должен использовать npm ci.", a: "package.json объявляет диапазоны зависимостей (намерение); package-lock.json записывает точное разрешённое дерево, которое эти диапазоны дали в прошлый раз — каждую версию, URL registry и хэш integrity. npm install считает источником истины package.json: он заново решает диапазоны, может добавить или обновить записи и пишет эти изменения обратно в lockfile, патча node_modules на месте. Это верно для локальной разработки, когда ты добавляешь или удаляешь зависимости. npm ci считает источником истины lockfile: он требует, чтобы lockfile существовал, игнорирует диапазоны и ставит ровно то, что залокано, сначала удаляет node_modules для чистой установки, никогда не пишет в lock и — критично — выходит с ошибкой, если package.json и lockfile разошлись. CI должен использовать npm ci, потому что он детерминирован (байт-в-байт идентичное дерево каждый запуск), чист (без обрывков состояния), быстр (пропускает разрешение) и падает громко на устаревшем или вручную отредактированном манифесте, вместо того чтобы молча переписать lock, как сделал бы npm install. Сеньорное правило: коммить lockfile, npm install локально для смены deps, npm ci везде, где важна воспроизводимость." },
    { q: "Что такое фантомная зависимость, чем она вызвана и как её предотвратить?", a: "Фантомная зависимость — это пакет, который твой код импортирует и использует, но который не объявлен в твоём package.json. Это происходит потому, что npm ставит плоский node_modules и поднимает (hoist) общие, совместимые версии на верх, чтобы пакеты делили одну копию. Пакет, подтянутый транзитивно одной из твоих реальных зависимостей, может оказаться поднятым на верх, и резолвер Node спокойно найдёт его оттуда — так что require('that-pkg') работает на твоей машине, хотя ты его никогда не объявлял. Это хрупко: в день, когда апгрейд зависимости меняет, как всё поднимается, или свежая установка разрешает другое дерево, пакет перестаёт быть на верхнем уровне, импорт падает, и ничего в манифесте этого не объясняет. Предотвращай двумя способами. Первое — объявляй всё, что импортируешь: если используешь напрямую, это место в твоих dependencies. Второе — используй пакетный менеджер со строгой изоляцией: pnpm строит симлинкованный node_modules, где пакет видит только то, что явно объявил, превращая фантомные зависимости в жёсткую ошибку на установке/сборке, а не в тихий костыль, который ломается позже." },
  ]}
/>

<Recap lang="ru">
package.json — это манифест намерения: идентичность (name, version), как Node его грузит (`type`, `exports`/`main`, `engines`), скрипты и четыре разные корзины зависимостей — dependencies (отгружаются в runtime), devDependencies (только сборка/тесты), peerDependencies (предоставляет host-приложение) и optionalDependencies (не фатально при отсутствии). То, что ты пишешь для зависимости, — обычно semver-диапазон, и оператор задаёт потолок: `^` допускает minor и patch (по умолчанию и источник тихого дрейфа), `~` допускает только patch, точная версия не пускает ничего, а `*` никогда не безопасен в проде; помни, что `^0.x` тихо вырождается до только-patch. Диапазон — лишь потолок, реальность пинит lockfile: package-lock.json записывает точное уплощённое дерево с версиями и хэшами integrity, чтобы установки были воспроизводимы и проверяемы. Это делает две команды установки разными по сути: npm install заново решает диапазоны и может переписать lock (верно для локальной смены зависимостей), тогда как npm ci ставит строго из lock, стирает node_modules для чистой установки, никогда не меняет lock и падает с ошибкой, если он разошёлся с package.json — поэтому сеньорная политика: коммить lockfile и использовать npm ci в CI и деплоях. Наконец, плоский, поднимающий node_modules от npm может породить фантомные зависимости — пакеты, которые ты используешь, но не объявлял, и которые ломаются на следующей перетасовке, — поэтому объявляй то, что импортируешь, относись к lockfile как к проверяемому supply-chain артефакту (хэши integrity, `npm audit`) и тянись к pnpm или yarn, когда хочешь более строгой изоляции, чем даёт уплощение npm.
</Recap>
