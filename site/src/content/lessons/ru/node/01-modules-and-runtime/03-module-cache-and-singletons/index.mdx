---
concepts:
- require-cache
- module-identity
- singleton-modules
- duplicate-package-instances
- esm-module-map
- cache-invalidation
deepensInto: []
estMin: 17
lang: ru
lessonType: topic
level: senior
order: 3
prereqs: []
slug: 03-module-cache-and-singletons
sources:
- https://nodejs.org/api/modules.html#requirecache
- https://nodejs.org/api/esm.html
- https://nodejs.org/api/packages.html#dual-package-hazard
spiral: []
status: ready
summary: 'Модуль Node — синглтон, ключом которого служит разрешённый абсолютный путь: первый require выполняет его один раз, а каждый последующий возвращает тот же объект. Две точки установки одного пакета дают два ключа — два экземпляра — и instanceof и общее состояние тихо ломаются.'
title: 'Кэш модулей: идентичность, синглтоны и дубликаты копий'
track: node
unit: 01-modules-and-runtime
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import StackDiagram from "~/components/diagram/StackDiagram.astro";

<Hook>
Ты выкатываешь клиент фича-флагов как модуль, который экспортирует один общий `EventEmitter` — каждая часть приложения подписывается на `flags.on("change")`, и месяцами всё просто работает. Потом ты добавляешь плагин, живущий в своём пакете и тянущий клиент флагов через транзитивную зависимость, и внезапно половина приложения никогда не видит изменения флагов. Ни ошибки, ни падения. Ты добавляешь логирование и обнаруживаешь, что эмиттеров *два*: слушатели приложения сидят на одном экземпляре, а `emit` плагина срабатывает на другом. Ничто в твоём коде не создавало второй — это сделал npm, установив клиент флагов по двум путям. Твой «синглтон» был синглтоном на разрешённый путь, а путей у тебя оказалось два.
</Hook>

<Crux>Модуль — синглтон на разрешённый путь: тот же путь → тот же объект; два пути → два экземпляра, сломанный `instanceof` и состояние.</Crux>

<Explanation>

## Кэш ключуется разрешённым абсолютным путём

Когда ты вызываешь `require("./config")`, Node сначала *разрешает* спецификатор в единственный абсолютный путь — скажем, `/srv/app/config.js` — и использует этот путь как ключ в `require.cache`. В первый раз случается промах кэша: Node читает файл, оборачивает его в функцию, выполняет один раз и сохраняет получившийся объект `module` (его `exports` — это то, что ты получаешь) под этим ключом. Каждый последующий `require()`, разрешающийся в тот же путь, — это *попадание* в кэш: Node полностью пропускает выполнение и отдаёт **ту же ссылку на объект**. Холодное выполнение парсит и запускает файл (миллисекунды для реального модуля, который открывает пул или читает конфиг); попадание — это поиск по карте, микросекунды, часто разница в 100–1000×.

```js
// config.js — evaluated exactly once
console.log("config evaluating");        // prints on the FIRST require only
module.exports = { db: "postgres://...", loadedAt: Date.now() };

// app.js
const a = require("./config");
const b = require("./config");
console.log(a === b);                     // true — same object reference
console.log(a.loadedAt === b.loadedAt);   // true — body never re-ran

// require.cache is keyed by the resolved absolute path
console.log(Object.keys(require.cache));  // [ '/srv/app/app.js', '/srv/app/config.js' ]
const id = require.resolve("./config");   // '/srv/app/config.js'
console.log(require.cache[id].exports === a); // true
```

Этот единственный механизм и есть *причина*, по которой модуль Node ведёт себя как синглтон. Если ты разместишь пул БД, объект конфига или шину `EventEmitter` на уровне модуля и экспортируешь её, кэш гарантирует, что каждый импортирующий разделяет единственный экземпляр — на каждый разрешённый путь существует ровно один объект `module.exports`, созданный при первой загрузке и переиспользуемый вечно. Ты не писал паттерн синглтона; кэш модулей *и есть* паттерн синглтона. Идентичность экземпляра, которую это даёт, — вся причина, по которой `require("./pool")` из сорока файлов разделяют один пул соединений, вместо того чтобы открыть сорок.

## ESM тоже кэширует — но «отимпортировать» нельзя

ES-модули получают ту же гарантию однократности через другую структуру: **карту модулей**, ключуемую разрешённым *URL* модуля (`file:///srv/app/config.mjs`). Тело ES-модуля выполняется ровно один раз при первом импорте, и каждый последующий `import` этого URL возвращает то же пространство имён модуля. Критичное для синьора отличие — это аварийный выход, которого *не существует*: **поддерживаемого способа инвалидировать кэш ESM нет.** CommonJS позволяет залезть в `require.cache` и `delete`-нуть запись; у карты модулей ESM нет публичного API, чтобы выселить URL. «Отимпортировать» модуль нельзя.

```js
// The only way to get ESM to re-evaluate is to change the KEY — the URL.
// A dev-only trick uses a cache-busting query string so the URL differs:
const fresh = await import(`./plugin.mjs?t=${Date.now()}`); // NEW key → re-evaluates
```

Это работает, потому что `./plugin.mjs?t=1` и `./plugin.mjs?t=2` — это *разные URL*, так что каждый становится свежей записью в карте модулей. Но это утечка памяти, а не механизм перезагрузки: каждая отдельная строка запроса добавляет в карту модулей постоянную запись (модули никогда не собираются из неё сборщиком мусора), так что вотчер, который переимпортирует при каждом изменении файла, накапливает тысячи осиротевших экземпляров модулей и неуклонно наращивает RSS. Это допустимо в одноразовом dev-цикле; это никогда не продакшен-хотрелоад. Перезагрузка в продакшене означает новый процесс под супервизором, а не растущую карту модулей.

<Inset kind="why" lang="ru">
Почему идентичность привязана к *пути*, а не к *имени* пакета? Потому что разрешение производит путь, и только путь однозначен. `require("lodash")` из `/srv/app` может разрешиться в `/srv/app/node_modules/lodash/index.js`, тогда как тот же вызов изнутри `/srv/app/node_modules/some-plugin` может разрешиться в `/srv/app/node_modules/some-plugin/node_modules/lodash/index.js` — *вложенную* копию, установленную из-за конфликта версий. То же имя, два пути, два ключа кэша, два полностью независимых экземпляра модуля. У кэша нет понятия «пакет lodash»; он знает только пути к файлам, так что идентичность пакета и идентичность модуля — не одно и то же в тот момент, когда npm кладёт пакет более чем в одно место.
</Inset>

## Режим отказа: две копии, которые не знают друг о друге

Вот где кэш превращается в продакшен-инцидент. Когда две версии — или две *точки установки* даже одной и той же версии — пакета разрешаются в разные пути, ты получаешь два отдельных экземпляра модуля, у каждого свой `module.exports`, свои классы, своё состояние на уровне модуля. Последствия коварны, потому что ничто не падает:

- **`instanceof` тихо возвращает `false`** через границу. `class Token` копии A и `class Token` копии B — это разные функциональные объекты с разными прототипами, так что `Token`, созданный A, проваливает проверку `value instanceof Token` относительно класса B. Твоя валидация просто отвергает валидные объекты или принимает невалидные, без всякой ошибки.
- **Две шины событий.** Как в завязке: `EventEmitter` копии A и копии B никогда не видят `emit` друг друга. Подписчики одного глухи к другому.
- **Два «синглтона».** Два объекта конфига, два реестра, два пула соединений — ты задаёшь значение на одном и читаешь `undefined` с другого.
- **React «Invalid hook call».** React держит свой диспетчер в состоянии на уровне модуля; две копии React означают, что твой компонент рендерится против одной копии, а хуки разрешаются против другой, и React бросает *Hooks can only be called inside a component* / *you might have more than one copy of React*.
- **Расщеплённые реестры `Symbol`.** Библиотека, использующая приватный для модуля `Symbol` как бренд, не может распознать объекты, помеченные её другой копией.

```js
// node_modules/lib/index.js (copy A)        and a nested copy B at
// node_modules/plugin/node_modules/lib/index.js — SAME source, two paths
class Token {}
module.exports = { Token, make: () => new Token() };

// app pulls copy A; plugin pulls copy B
const a = require("lib");                 // resolves to copy A's path
const t = require("plugin").makeToken();  // built by copy B's Token
console.log(t instanceof a.Token);        // false — different class objects, no error
```

Диагностика механична, как только ты знаешь причину: `npm ls <pkg>` печатает дерево зависимостей и показывает, когда пакет появляется в двух версиях/местах; `require.resolve("pkg")` из двух точек вызова показывает два разных абсолютных пути — это и есть улика. Исправления нацелены на *дедупликацию*: `npm dedupe` сплющивает совместимые копии в одну, `overrides` (npm/pnpm) или `resolutions` (Yarn) форсируют единственную версию, объявление общей библиотеки как `peerDependency` (чтобы хост предоставлял одну копию, вместо того чтобы каждый пакет тащил свою) — стандартное исправление для экосистем плагинов, а сборщики предлагают явный dedupe/alias, чтобы схлопнуть копии в сборке. Цифры важны: две копии библиотеки в 300 КБ не просто рискуют корректностью — они примерно удваивают вес этой библиотеки в бандле и в резидентной памяти, потому что каждый путь — это отдельный модуль, который загружается и остаётся загруженным.

<TradeoffMatrix
  id="03-module-cache-and-singletons-tradeoff-1"
  lessonSlug="03-module-cache-and-singletons"
  lang="ru"
  prompt="Плагин в собственном пакете должен использовать ТУ ЖЕ общую шину EventEmitter, которую экспортирует хост-приложение, но npm устанавливает библиотеку шины по двум путям, так что плагин получает второй экземпляр. Какое исправление долговечно?"
  options={[
    { name: "Объявить библиотеку шины как peerDependency плагина (хост предоставляет единственную копию)", summary: "Одна копия, от хоста.", correct: true, justification: "Это стандартное исправление для экосистемы плагинов, и оно бьёт в настоящую причину: идентичность пакета. peerDependency говорит установщику, что плагин должен использовать копию хоста, а не вкладывать свою, так что обе разрешаются в ОДИН путь, ОДИН ключ кэша, ОДИН экземпляр EventEmitter. Идентичность восстановлена у источника — синглтон по-настоящему общий — и заодно ты перестаёшь поставлять дубликат байтов библиотеки." },
    { name: "В плагине сделать delete require.cache[require.resolve('bus')] и переребовать, чтобы форсировать свежую загрузку", summary: "Сброс кэша на загрузку.", justification: "Задом наперёд — это СОЗДАЁТ экземпляры, а не объединяет их. Удаление записи кэша лишь заставляет следующий require переисполниться и породить ещё один объект эмиттера, тогда как хост по-прежнему держит исходный. Ты размножишь шины, а не сольёшь их, и утечёшь осиротевшими старыми. Проблема — два пути, а удаление кэша их не схлопывает." },
    { name: "Заставить плагин импортировать шину по абсолютному пути: require('/srv/app/node_modules/bus')", summary: "Захардкодить путь.", justification: "Случайно может сработать, форсируя обе точки вызова на один ключ кэша, но это хрупко до халатности: оно хардкодит раскладку node_modules, которая меняется между машинами, ОС, хойстингом монорепо и целями деплоя, и ломается в тот момент, когда путь отличается. Оно лечит симптом (два ключа), приклеивая один путь на место, вместо того чтобы исправить идентичность пакета. peerDependency выражает то же намерение декларативно и переносимо." },
    { name: "Сделать шину настоящим глобалом, присвоив globalThis.__bus = new EventEmitter() при первой загрузке", summary: "Спрятать в global.", justification: "Это обходит кэш, используя единственный глобальный слот, но глобалы — это ровно то общее изменяемое состояние, для изоляции которого и существует система модулей. Ты теряешь типизацию, гоняешься на порядке первой записи, любой код может затереть __bus, и ты сцепил каждого потребителя с магическим глобальным именем. Это замазывает дубликаты экземпляров вместо дедупликации пакета и создаёт новый класс багов действия-на-расстоянии." },
  ]}
/>

## Хак хотрелоада `delete require.cache[id]`

Поскольку CommonJS выставляет `require.cache` как записываемый объект, заманчивый трюк — `delete require.cache[require.resolve("./handler")]`, чтобы следующий `require` переисполнил файл, — хотрелоад для бедных. Он опасен, и все причины — это причины идентичности. Удаление записи кэша **не** меняет ничего, что уже держит старый `exports`: замыкание, зарегистрированный маршрут, слушатель события или другой модуль, захвативший `const h = require("./handler")`, по-прежнему указывает на *старый* объект. Так что теперь у тебя старый код работает для всего, что загрузилось до удаления, и новый код для всего, что после, — две версии живут одновременно, с несовпадениями `instanceof` между ними. Каждая перезагрузка ещё и **утекает памятью**: осиротевший старый экземпляр модуля (и всё, на что он ссылался) остаётся достижимым через эти устаревшие замыкания и никогда не собирается, так что куча долгоживущего вотчера растёт от перезагрузки к перезагрузке. И она тихо ломает гарантию синглтона — всю причину, по которой ты использовал модуль, — чеканя второй `module.exports`, пока первый ещё в ходу. Настоящий хотрелоад (тот эргономичный, что в dev-серверах) работает, снося и пересоздавая *новый процесс или воркер*, и никогда не мутируя кэш модулей одного процесса на месте.

<Quiz
  id="03-module-cache-and-singletons-quiz-1"
  lessonSlug="03-module-cache-and-singletons"
  lang="ru"
  question="Класс, созданный одной копией пакета, проваливает `value instanceof Lib.Token` при проверке против класса другой копии — без всякой ошибки. Почему?"
  choices={[
    { label: "Две копии разрешились в разные абсолютные пути, так что каждая — отдельная запись кэша со своим объектом класса и прототипом; instanceof сравнивает цепочки прототипов, а два класса Token — разные функции, поэтому проверка просто false, а не ошибка", correct: true },
    { label: "instanceof ненадёжен в Node, и его стоит избегать", misconception: "instanceof совершенно надёжен; он верно отвечает на вопрос «есть ли этот прототип в цепочке?». Несовпадение — не изъян instanceof, а то, что есть по-настоящему два разных класса Token из двух экземпляров модуля, так что ответ честно false." },
    { label: "Объект потерял прототип, когда пересёк границу пакета", misconception: "Передача объекта между модулями не меняет его прототип — объект сохраняет Token.prototype копии B. Проверка проваливается, потому что ты сравниваешь его против другого Token копии A, а не потому что что-то срезали." },
    { label: "Нужно очистить require.cache, чтобы обе копии разделяли один класс", misconception: "Удаление записей кэша создаёт больше экземпляров, а не меньше, и не может слить два пути установки в один. Исправление — дедупликация (npm dedupe / overrides / peerDependency), чтобы пакет разрешался в единственный путь." },
  ]}
/>

<Quiz
  id="03-module-cache-and-singletons-quiz-2"
  lessonSlug="03-module-cache-and-singletons"
  lang="ru"
  question="Dev-инструмент переимпортирует ES-модуль при каждом сохранении файла через `import('./m.js?t=' + Date.now())`. Какова цена в долгом запуске?"
  choices={[
    { label: "Каждая уникальная строка запроса — новый URL в карте модулей, так что каждое сохранение добавляет постоянный, никогда не собираемый экземпляр модуля — карта растёт безгранично и RSS лезет вверх; это утечка, а не настоящая инвалидация кэша", correct: true },
    { label: "Ничего — старая версия собирается мусором, как только загрузится новая", misconception: "Записи карты модулей ESM не выселяются и не собираются; старый URL остаётся резидентным. Трюк со строкой запроса добавляет записи, но никогда их не убирает, так что память только растёт." },
    { label: "Строка запроса игнорируется и возвращается тот же закэшированный модуль", misconception: "URL (включая его query) — это ключ кэша, так что другой query — другой ключ, и он ДЕЙСТВИТЕЛЬНО переисполняет — именно поэтому трюк перезапускает модуль и именно поэтому он утекает." },
    { label: "Он запускает поддерживаемое API инвалидации кэша ESM под капотом", misconception: "Поддерживаемого API инвалидации кэша ESM не существует. Смена URL — единственный способ форсировать переисполнение, и именно поэтому перезагрузка в продакшене означает новый процесс, а не растущую карту модулей." },
  ]}
/>

</Explanation>

<StackDiagram
  label="Один разрешённый путь = одна запись кэша = один общий экземпляр; второй путь раздваивает синглтон"
  hue="var(--d-systems)"
  caption="Ключ кэша — разрешённый абсолютный путь, а не имя пакета. Сорок require файла /…/bus.js все попадают в одну запись и разделяют один EventEmitter (настоящий синглтон). Но вложенная установка в /…/plugin/node_modules/bus.js — другой ключ: вторая запись, второй эмиттер, сломанный instanceof и расщеплённое состояние. Dedupe схлопывает два ключа обратно в один."
  layers={[
    { label: "require.resolve → /srv/app/node_modules/bus/index.js", note: "разрешённый путь = ключ" },
    { label: "require.cache[key] — MISS при первой загрузке, выполнить один раз", note: "создан module.exports" },
    { label: "каждый последующий require того же ключа → HIT, тот же объект", note: "синглтон (~µс)" },
    { label: "второй путь /…/plugin/node_modules/bus/index.js", note: "ДРУГОЙ ключ" },
    { label: "вторая запись кэша → второй экземпляр", note: "instanceof false, шина расщеплена" },
  ]}
/>

<KeyTakeaway>Модуль — синглтон на **разрешённый путь**: тот же путь → тот же объект, два пути → два экземпляра, ломая `instanceof` и состояние. Дедуплицируй до одного пути; не делай `delete require.cache` ради перезагрузки.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="03-module-cache-and-singletons-retrieval"
  lang="ru"
  questions={[
    { q: "Две части приложения получают «синглтон» EventEmitter из одного пакета, но события, эмитнутые одной, невидимы для другой. Пройди от симптома к корневой причине и исправлению.", a: "Симптом — общий синглтон, который не общий, — это классический баг дубликата экземпляра. Кэш модулей ключуется РАЗРЕШЁННЫМ АБСОЛЮТНЫМ ПУТЁМ, а не именем пакета, и экспортируемый модулем EventEmitter — синглтон только на ключ кэша. Если npm установил пакет в двух местах (вложенная копия из-за конфликта версий или плагин, тащащий свою копию), две точки вызова разрешаются в два разных пути — два ключа кэша, две записи, два отдельных `module.exports` и, значит, два разных объекта EventEmitter. Слушатели цепляются к одному экземпляру, а `emit` срабатывает на другом, так что события исчезают без ошибки. Подтверждаешь это, вызывая `require.resolve('pkg')` из обеих точек и видя два разных абсолютных пути, или запустив `npm ls pkg` и увидев пакет в двух версиях/местах. Исправление — дедупликация, чтобы пакет разрешался в ОДИН путь: `npm dedupe`, `overrides`/`resolutions` для форсирования единственной версии или — стандартное исправление для плагинов — объявление общей библиотеки как `peerDependency`, чтобы хост предоставлял одну копию, вместо того чтобы каждый пакет вкладывал свою. Две копии библиотеки в 300 КБ ещё и примерно удваивают её след в бандле и RSS, так что dedupe выигрывает и в корректности, и в памяти." },
    { q: "Почему `delete require.cache[id]` — опасный способ хотрелоада, и чем настоящая перезагрузка отличается?", a: "Удаление записи кэша лишь форсирует СЛЕДУЮЩИЙ require переисполнить файл и породить новый `module.exports`; оно ничего не делает с тем, что уже захватило старый exports. Замыкания, зарегистрированные маршруты, слушатели событий и другие модули, сделавшие `const h = require('./handler')`, по-прежнему держат СТАРЫЙ объект, так что после перезагрузки у тебя две версии живут одновременно — старый код для всего, что загрузилось до удаления, новый после — с несовпадениями `instanceof` между объектами из двух версий. Оно также утекает памятью: осиротевший старый экземпляр модуля и всё, на что он ссылается, остаётся достижимым через эти устаревшие замыкания и никогда не собирается мусором, так что куча долгоживущего вотчера растёт при каждой перезагрузке. И оно ломает ту самую гарантию синглтона, на которую ты полагался, чеканя второй экземпляр, пока первый ещё в ходу. ESM даже не предлагает этот хак — поддерживаемого способа инвалидировать карту модулей нет; dev-трюк `?t=Date.now()` переисполняет лишь созданием нового URL-ключа, который утекает ровно так же. Настоящий хотрелоад сносит и пересоздаёт свежий процесс или воркер (под супервизором или dev-сервером), а не мутирует состояние модулей одного процесса на месте, так что идентичность и память остаются чистыми." },
  ]}
/>

<Recap lang="ru">
Модуль Node — синглтон, ключом которого служит его **разрешённый абсолютный путь**: первый `require()` выполняет файл один раз и кэширует получившийся `module.exports` под этим путём в `require.cache`, а каждый последующий `require()` того же пути — попадание в кэш, возвращающее *ту же ссылку на объект* за микросекунды, вместо того чтобы перезапускать тело, — именно поэтому пул, конфиг или `EventEmitter` на уровне модуля общий повсюду. ESM даёт ту же гарантию однократности через карту модулей, ключуемую разрешённым URL, но без поддерживаемой инвалидации: «отимпортировать» нельзя, а dev-трюк `?t=…` переисполняет лишь чеканя новый URL-ключ, утекая записями карты модулей, которые никогда не собираются. Плата за бесплатные синглтоны — сцепление с идентичностью пути, а режим отказа — **дубликаты копий**: когда один пакет разрешается в два пути установки, ты получаешь два экземпляра, так что `instanceof` тихо возвращает false, две шины событий никогда не видят друг друга, два «синглтон»-конфига расходятся, React бросает *Invalid hook call*, а библиотека в 300 КБ удваивается в бандле и RSS — диагностируется через `npm ls` / `require.resolve`, показывающие два пути, и исправляется через `npm dedupe`, `overrides`/`resolutions` или `peerDependency`, чтобы пакет разрешался в один путь. Наконец, `delete require.cache[id]` — опасный хак перезагрузки, потому что устаревшие замыкания держат старый exports (две версии живут одновременно, идентичность сломана), а осиротевшие экземпляры утекают; настоящая перезагрузка пересоздаёт процесс, а не мутирует кэш на месте.
</Recap>
