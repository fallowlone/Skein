---
concepts:
- node-modules-walk
- file-and-directory-resolution
- extension-probing
- esm-url-resolution
- main-module-type-fields
- specifier-kinds
deepensInto: []
estMin: 18
lang: ru
lessonType: topic
level: middle
order: 1
prereqs: []
slug: 01-module-resolution-algorithm
sources:
- https://nodejs.org/api/modules.html#all-together
- https://nodejs.org/api/esm.html#resolution-algorithm
- https://nodejs.org/api/packages.html
spiral: []
status: ready
summary: 'CJS require() выполняет синхронный алгоритм — классифицировать спецификатор, пробовать файл, затем директорию, затем index, вверх по node_modules — тогда как ESM разрешает URL с обязательными расширениями. Знание точных шагов превращает MODULE_NOT_FOUND из загадки в чек-лист.'
title: 'Алгоритм разрешения модулей: как Node находит твой код'
track: node
unit: 13-modules-deep
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";
import Infographic from "~/components/diagram/Infographic.astro";
import resolutionProbeOrder from "./infographics/resolution-probe-order.svg?raw";

<Hook>
`Error: Cannot find module './utils'` — а файл вот он, ты видишь `utils.ts` в редакторе. Или наоборот: на ноутбуке всё работает, а в контейнере падает `ERR_MODULE_NOT_FOUND` на спецификаторе, который не менялся. Оба случая — из одного источника: точный многошаговый алгоритм разрешения, который запускается на каждый `require()` или `import`, и делает он не то, что ты предполагаешь. Он пробует расширения в фиксированном порядке, обращается с директорией иначе, чем с файлом, и — под ESM — вовсе отказывается угадывать расширение. Как только ты можешь прогнать алгоритм в голове, эти ошибки перестают быть везением.
</Hook>

<Crux>Разрешение классифицирует спецификатор: относительные пробуют файл → директорию → index, голые идут вверх по node_modules.</Crux>

<Explanation>

## Три вида спецификатора и развилка, которую они запускают

Прежде чем Node посмотрит хоть на один байт на диске, он классифицирует **спецификатор** — строку, переданную в `require()` или `import`, — в один из трёх видов, потому что каждый идёт своим путём через алгоритм:

- **Относительный** — начинается с `./`, `../` или `/`. `require("./utils")`. Разрешается относительно директории импортирующего файла.
- **Голый** (он же «пакетный») — имя без ведущей точки или слэша. `require("express")`, `import "lodash/fp"`. Запускает обход `node_modules`.
- **Абсолютный / URL** — полный путь в файловой системе, либо под ESM — URL вида `file:` / `node:` / `data:`. `import "node:fs"`.

```js
require("./parser");      // относительный → рядом с текущим файлом
require("parser");        // голый → поиск в node_modules вверх по дереву
require("/opt/app/p.js"); // абсолютный → путь используется напрямую
```

Это первое решение чаще всего пропускают. `"./utils"` и `"utils"` выглядят почти одинаково, но идут через совершенно разные механизмы: первый никогда не трогает `node_modules`, второй никогда не смотрит рядом с твоим файлом. Забытый `./` — или случайно добавленный — отправляет разрешение по совсем другой ветке.

## CJS: пробуем файл, затем директорию, затем index

Для **относительного** спецификатора в CommonJS Node берёт путь и пробует, в строгом порядке, пока что-то не сработает:

1. **Как точный файл.** `LOAD_AS_FILE(X)`: пробуем `X` буквально, затем `X.js`, затем `X.json`, затем `X.node` (скомпилированный аддон). Это *проба расширений* — `require("./utils")` тихо становится `./utils.js`. Порядок фиксирован: соседний `utils.js` всегда выигрывает у `utils.json`.
2. **Как директорию.** Если `X` — директория, `LOAD_AS_DIRECTORY(X)`: читаем `X/package.json` и следуем его полю `main` (снова разрешая как файл); если `package.json` нет или нет `main`, откатываемся к `X/index.js`, затем `X/index.json`, затем `X/index.node`.
3. **Провал.** Если ни то ни другое не сработало, бросаем `MODULE_NOT_FOUND`.

```js
// require("./shapes") пробует, по порядку:
//   ./shapes            (точный файл — обычно его нет)
//   ./shapes.js
//   ./shapes.json
//   ./shapes.node
//   ./shapes/           → её package.json "main", иначе ./shapes/index.js …
```

<Infographic
  svg={resolutionProbeOrder}
  label="Упорядоченный чек-лист путей, которые CommonJS require() пробует для './shapes' в фиксированном порядке: точное имя, затем .js, .json, .node, затем package.json main директории и index.js/json/node, и наконец MODULE_NOT_FOUND, когда всё исчерпано."
  caption="CJS пробует эти пути ровно в этом порядке, и побеждает первый существующий — поэтому соседний .js затеняет .json, а импорт папки ломается, как только пропадает её index.js."
  hue="var(--d-infra)"
/>

Два вывода для загадки `Cannot find module './utils'`. Во-первых, CJS **не** разрешает `.ts` — `require` знает только `.js`/`.json`/`.node`, поэтому голый `./utils`, указывающий на `utils.ts`, упадёт, пока загрузчик (ts-node, tsx, сборщик) не научит Node этому расширению. Во-вторых, папка «просто работает» только из-за отката на `index.js`; удали index — и тот же `require("./shapes")` сломается, хотя директория на месте.

## Обход node_modules: голые спецификаторы карабкаются вверх по дереву

**Голый** спецификатор запускает самую характерную часть алгоритма: `NODE_MODULES_PATHS`. У Node **нет** одной глобальной директории библиотек. Вместо этого, начиная с директории импортирующего файла, он ищет `./node_modules/<pkg>`, и если её там нет — поднимается **на одну директорию вверх и пробует снова**, повторяя до самого корня файловой системы.

```
импорт в /app/src/api/handler.js  →  require("lodash")
  ищем /app/src/api/node_modules/lodash
  ищем /app/src/node_modules/lodash
  ищем /app/node_modules/lodash      ← найдено здесь
  ищем /node_modules/lodash          (только если всё ещё не найдено)
```

Найдя директорию `lodash`, он разрешает *внутри* неё той же логикой файл/директория/index (учитывая `main` или `exports` этого пакета). Этот обход вверх — ровно причина, по которой зависимость, установленная в корне проекта, видна глубоко вложенным файлам, и по которой две копии библиотеки на разной глубине могут сосуществовать — каждый импортёр разрешается к ближайшей. Это же объясняет, почему `MODULE_NOT_FOUND` на голом имени почти всегда значит «не установлено на этом уровне или выше», а не «опечатка в имени файла».

<Inset kind="why" lang="ru">
Этот обход вверх — механизм за npm-овской *дедупликацией* зависимостей и за пресловутым багом «два React». Пакет, поднятый в `/app/node_modules`, разделяется всеми импортёрами ниже. Но если транзитивная зависимость закрепляет другую версию, npm вкладывает вторую копию в *её* `node_modules`, и импортёр там разрешается к вложенной копии. Теперь существуют два экземпляра модуля «той же» библиотеки, каждый со своим состоянием — что ломает всё, опирающееся на синглтон (React-хуки, проверки `instanceof`). Фикс — дедупнуть или поднять до единой версии, а понимание обхода — то, что подсказывает, к чему каждый импортёр на самом деле разрешается.
</Inset>

## Разрешение в ESM: URL, обязательные расширения, без index

ESM выполняет *другой* алгоритм, и различия — ровно там, где живут падения между средами. ESM разрешает всё в **URL** (обычно `file:`), и эта смена рамки меняет правила:

- **Расширения обязательны.** `import "./utils.js"` работает; `import "./utils"` бросает `ERR_MODULE_NOT_FOUND`. Нет пробы расширений и **нет отката на `index.js`** для относительных спецификаторов — пишешь полный путь, или падаешь. (Это самый частый сюрприз при миграции CJS-кода на ESM.)
- **Голые спецификаторы всё ещё используют обход `node_modules`**, но точка входа пакета выбирается полем `exports` / `main`, а подпути должны быть явно экспонированы (следующий урок).
- **`node:` явный.** Встроенные модули разрешаются через схему `node:` (`import "node:fs"`); голая форма `import "fs"` для встроенных всё ещё работает, но `node:` однозначна и современный дефолт.

```js
import { readFile } from "node:fs/promises"; // встроенный, явная схема
import { parse } from "./parser.js";          // расширение ОБЯЗАТЕЛЬНО
import express from "express";                // голый → обход node_modules + exports
// import { parse } from "./parser";          // ✗ ERR_MODULE_NOT_FOUND под ESM
```

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Поведение</th>
        <th class="border border-border px-3 py-2 text-left">CommonJS <code>require()</code></th>
        <th class="border border-border px-3 py-2 text-left">ESM <code>import</code></th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">Расширение в относительном пути</td>
        <td class="border border-border px-3 py-2">Необязательно — пробуется (<code>.js</code>, <code>.json</code>, <code>.node</code>)</td>
        <td class="border border-border px-3 py-2">Обязательно — пишется полностью</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Откат на <code>index</code> в директории</td>
        <td class="border border-border px-3 py-2">Да (<code>index.js</code> …)</td>
        <td class="border border-border px-3 py-2">Нет</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Во что разрешается</td>
        <td class="border border-border px-3 py-2">В путь файловой системы</td>
        <td class="border border-border px-3 py-2">В URL (<code>file:</code>, <code>node:</code>, <code>data:</code>)</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Голый спецификатор</td>
        <td class="border border-border px-3 py-2">обход <code>node_modules</code> + <code>main</code></td>
        <td class="border border-border px-3 py-2">обход <code>node_modules</code> + <code>exports</code></td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Встроенный модуль</td>
        <td class="border border-border px-3 py-2"><code>require("fs")</code></td>
        <td class="border border-border px-3 py-2"><code>import "node:fs"</code> (явная схема)</td>
      </tr>
    </tbody>
  </table>
</div>

## Поля package.json, рулящие разрешением

Три поля `package.json` решают, как разрешается *пакет* (голый импорт), и они образуют цепочку приоритета:

- **`"type"`** — `"module"` делает `.js`-файлы в этом пакете ESM; `"commonjs"` или отсутствие делает их CJS. Управляет тем, как *интерпретируется* найденный файл, а не где он находится.
- **`"main"`** — устаревшая точка входа: файл, в который разрешается голый `require("pkg")`/`import "pkg"`, когда `exports` отсутствует. `"main": "./lib/index.js"`.
- **`"exports"`** — современная карта входов. При наличии **перекрывает `main`** и становится авторитетным: может направлять `import` и `require` на разные файлы и, главное, *блокирует* любой путь, не указанный в нём (полностью — в следующем уроке). `"module"` — нестандартное поле только для сборщиков — Node его игнорирует.

Приоритет и есть подвох: если пакет поставляет поле `exports`, `main` игнорируется Node, и глубокий импорт вроде `require("pkg/lib/internal")`, работавший годами, может вдруг бросить `ERR_PACKAGE_PATH_NOT_EXPORTED` после того, как пакет добавил `exports`. Файл по-прежнему существует; резолвер теперь отказывается на него смотреть.

<Quiz
  id="13-modules-deep-mra-quiz-extension"
  lessonSlug="01-module-resolution-algorithm"
  lang="ru"
  question="Тот же import('./parser') успешен в CommonJS-файле, но бросает ERR_MODULE_NOT_FOUND, как только файл становится ESM. Почему?"
  choices={[
    { label: "ESM не пробует расширения и не откатывается на index — './parser' нужно писать как './parser.js'; CJS автоматически пробует .js/.json/.node", correct: true },
    { label: "ESM вообще не может импортировать относительные пути", misconception: "ESM прекрасно импортирует относительные пути — он лишь требует полного имени файла. Падение — из-за отсутствующего расширения, а не относительной формы." },
    { label: "Файл нужно переименовать в .mjs", misconception: "Режим интерпретатора не связан с этим багом. Даже корректно типизированный ESM-файл падает на './parser' без расширения; фикс — добавить .js в спецификатор." },
    { label: "ESM разрешает относительно node_modules, а не локальной директории", misconception: "Относительные спецификаторы (начинающиеся с ./) разрешаются относительно директории импортирующего файла в обеих системах. Обход node_modules — только для голых спецификаторов." },
  ]}
/>

<Quiz
  id="13-modules-deep-mra-quiz-walk"
  lessonSlug="01-module-resolution-algorithm"
  lang="ru"
  question="require('lodash') в /app/src/api/handler.js падает с MODULE_NOT_FOUND, но lodash установлен в /app/node_modules. Что делает алгоритм?"
  choices={[
    { label: "Он идёт по node_modules вверх: /app/src/api/node_modules, затем /app/src/node_modules, затем /app/node_modules — так что /app/node_modules ДОЛЖЕН быть найден; реальный MODULE_NOT_FOUND здесь значит, что там его на самом деле нет", correct: true },
    { label: "Node смотрит только в директорию импортирующего файла", misconception: "Определяющая черта обхода голого спецификатора в том, что он карабкается вверх по дереву, проверяя node_modules на каждом уровне до корня файловой системы." },
    { label: "require требует абсолютный путь для пакетов", misconception: "Голые имена — обычный способ импортировать пакеты; обход существует именно чтобы не писать абсолютные пути к node_modules." },
    { label: "node_modules должна быть в текущей рабочей директории", misconception: "Разрешение идёт относительно директории импортирующего файла, а не CWD процесса — это различие важно, когда инструмент запущен из другой папки." },
  ]}
/>

<TradeoffMatrix
  id="13-modules-deep-mra-tradeoff"
  lessonSlug="01-module-resolution-algorithm"
  lang="ru"
  prompt="TypeScript-проект компилируется в ESM. Относительные импорты падают с ERR_MODULE_NOT_FOUND в рантайме, потому что в сгенерированном .js нет расширений. Как починить надёжно?"
  options={[
    { name: "Писать расширения .js в импортах TypeScript-исходника (import './x.js'), чтобы сгенерированный ESM разрешался", summary: "В исходнике спецификаторы с .js.", correct: true, justification: "ESM требует полные расширения в рантайме, а компилятор их не добавляет, поэтому спецификатор, который ты пишешь, — это спецификатор, который поедет. Писать import './x.js' в .ts-исходнике (TypeScript разрешит его в x.ts на компиляции, сгенерирует x.js) — официально рекомендованный паттерн для запускаемого ESM. Выглядит странно, но корректно: исходник называет рантайм-артефакт." },
    { name: "Убрать расширение и положиться на хук загрузчика, добавляющий его на каждый импорт", summary: "Загрузчик патчит разрешение.", justification: "Кастомный резолвер или tsx может заклеить отсутствующие расширения в dev, но добавляет рантайм-зависимость и нестандартный слой разрешения, который ломается в момент запуска чистого node на выводе сборки. Годится для локального dev, ненадёжно как долговременный production-ответ." },
    { name: "Вернуть пакет в CommonJS, чтобы вернулась проба расширений", summary: "Отказаться от ESM.", justification: "Откат в CJS действительно возвращает пробу расширений, но жертвует причиной перехода на ESM (tree-shaking, top-level await, соответствие стандарту) ради обхода механической правки. Решать проблему форматирования спецификатора отказом от модульной системы — несоразмерно." },
    { name: "Добавить postbuild-скрипт, regex-переписывающий каждый сгенерированный импорт с добавлением .js", summary: "Переписать вывод.", justification: "Может сработать, но ты поддерживаешь хрупкий codemod над генерированным кодом, который неверно обрабатывает динамические спецификаторы, type-only импорты и реэкспорты. Писать расширение в исходнике — тот же результат без рисков постобработки." },
  ]}
/>

</Explanation>

<FlowDiagram
  label="Алгоритм разрешения CommonJS require(): классифицировать спецификатор, затем пробовать как файл, затем как директорию с откатом на index, затем идти по node_modules вверх для голых имён, иначе бросить MODULE_NOT_FOUND"
  hue="var(--d-systems)"
  perRow={3}
  nodes={[
    { id: "spec", label: "Спецификатор", sub: "require(X)" },
    { id: "rel", label: "Относит. / абс.?", sub: "./ ../ /" },
    { id: "file", label: "Как файл", sub: ".js .json .node" },
    { id: "dir", label: "Как директория", sub: "main → index" },
    { id: "walk", label: "обход node_modules", sub: "вверх по дереву" },
    { id: "fail", label: "MODULE_NOT_FOUND", sub: "исчерпано" },
  ]}
  edges={[
    { from: "spec", to: "rel" },
    { from: "rel", to: "file", label: "относит." },
    { from: "file", to: "dir", label: "промах" },
    { from: "dir", to: "fail", label: "промах" },
    { from: "rel", to: "walk", label: "голый" },
    { from: "walk", to: "fail", label: "достиг корня" },
  ]}
  caption="Относительные спецификаторы пробуют файл, затем директорию, затем index; голые карабкаются по node_modules; исчерпание любой ветки бросает MODULE_NOT_FOUND."
/>

<KeyTakeaway>Разрешение классифицирует спецификатор. Относительный в CJS пробует файл → директорию → index, угадывая расширения; голый идёт по node_modules. ESM разрешает URL с обязательными расширениями, без index — пиши путь.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="13-modules-deep-mra-retrieval"
  lang="ru"
  questions={[
    { q: "Пройди по шагам, что делает require('./shapes'), и где каждый шаг может упасть.", a: "Сначала Node классифицирует спецификатор: './shapes' начинается с './', значит он относительный и разрешается относительно директории импортирующего файла — node_modules не трогается. Затем выполняется LOAD_AS_FILE: пробуем './shapes' буквально (почти всегда отсутствует), затем добавляем расширения в фиксированном порядке — './shapes.js', './shapes.json', './shapes.node' — и первое существующее выигрывает, поэтому можно опускать '.js'. Если ни один файл не подошёл, выполняется LOAD_AS_DIRECTORY: если './shapes' — директория, читаем './shapes/package.json' и разрешаем его поле 'main' как файл; если package.json нет или нет main, откатываемся к './shapes/index.js', затем index.json, затем index.node. Если ни файловая, ни директорная ветка не разрешились, бросается MODULE_NOT_FOUND. Частые причины падения: файл на самом деле './shapes.ts' (require не знает .ts без загрузчика), или папка существовала, но её index.js удалили, или ты имел в виду пакет, но написал './shapes', что заставляет относительную ветку и пропускает node_modules целиком." },
    { q: "Почему тот же спецификатор './parser' работает под CommonJS, но бросает ERR_MODULE_NOT_FOUND под ESM, и что эти две системы делают по-разному?", a: "Они выполняют разные алгоритмы разрешения. CommonJS разрешает в путь файловой системы и пробует: относительный спецификатор без расширения пробуется как './parser', затем './parser.js', './parser.json', './parser.node', а у директории есть откат на index.js — так './parser' тихо становится './parser.js'. ESM разрешает в URL и намеренно ничего этого не делает: расширения обязательны, и нет отката на index директории, поэтому './parser' нужно писать './parser.js', иначе бросается ERR_MODULE_NOT_FOUND. Логика в том, что разрешение по URL должно быть однозначным и не требовать пробы файловой системы на каждый импорт, что также делает ESM-разрешение кэшируемым и соответствующим стандарту браузера. Это сюрприз номер один при миграции CJS на ESM и при компиляции TypeScript в ESM — сгенерированный спецификатор должен нести своё расширение, поэтому пишут import './x.js' даже в .ts-исходнике. Голые спецификаторы всё ещё используют обход node_modules вверх в обеих системах, но ESM затем сверяется с полем exports пакета для точки входа, тогда как CJS использует main." },
  ]}
/>

<Recap lang="ru">
Разрешение модулей — детерминированный алгоритм, который запускается на каждый require() и import, и начинается он с классификации спецификатора на относительный (./, ../, /), голый (имя пакета) или абсолютный/URL — каждый идёт через свою машинерию. Для относительного спецификатора CommonJS выполняет LOAD_AS_FILE (пробует точный путь, затем расширения .js, .json, .node) и затем LOAD_AS_DIRECTORY (следует package.json main, иначе откатывается на index.js/json/node), бросая MODULE_NOT_FOUND, если оба промахнулись; поэтому расширения необязательны, а папки «просто работают». Для голого спецификатора CJS выполняет обход node_modules — начиная с директории импортирующего файла и карабкаясь вверх по дереву, проверяя node_modules на каждом уровне — что является механизмом за подъёмом зависимостей, дедупликацией и багом «две копии одной библиотеки». ESM выполняет другой, URL-ориентированный алгоритм, где расширения обязательны и нет отката на index, поэтому './parser' надо писать './parser.js'; голые спецификаторы всё ещё обходят node_modules, но выбирают точку входа через поле exports пакета, а встроенные модули используют явную схему node:. Три поля package.json рулят разрешением пакета: "type" решает, считать ли найденный .js за CJS или ESM, "main" — устаревшая точка входа, а "exports" — при наличии — перекрывает main и становится авторитетным, поэтому его добавление может вдруг сломать глубокий импорт, работавший годами. Прогоняй алгоритм в голове — и MODULE_NOT_FOUND превратится из загадки в короткий чек-лист.
</Recap>
