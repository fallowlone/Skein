---
concepts:
- module-register
- resolve-hook
- load-hook
- hook-chaining
- off-thread-loaders
- transpile-on-load
deepensInto: []
estMin: 18
lang: ru
lessonType: topic
level: senior
order: 3
prereqs: []
slug: 03-module-customization-hooks
sources:
- https://nodejs.org/api/module.html#customization-hooks
- https://nodejs.org/api/module.html#moduleregisterspecifier-parenturl-options
- https://nodejs.org/api/module.html#hooks
spiral: []
status: ready
summary: 'Хуки кастомизации перехватывают resolve и load для каждого модуля. Зарегистрированные через module.register в отдельном потоке, они образуют цепочку через next…-хуки. Хороши для dev-транспиляции и моков — но нагружают старт, а хук без source maps ломает каждый трейс.'
title: 'Хуки кастомизации модулей: перехват resolve и load'
track: node
unit: 13-modules-deep
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";
import Infographic from "~/components/diagram/Infographic.astro";
import startupTax from "./infographics/startup-tax.svg?raw";

<Hook>
Ты выкатил TypeScript-сервис, который в проде запускался прямо из исходников через загрузчик на `register()` — без шага сборки, «проще пайплайн». Через три недели в 2 часа ночи прилетает алерт: `TypeError` в `billing.ts` на строке 64. Открываешь строку 64 — а там пустая строка между двумя функциями. Каждый кадр стека сдвинут на десяток строк, потому что load-хук на лету транспилировал TS → JS и не выдал source map, так что V8 сообщает позиции в *скомпилированном* выводе, который ты в глаза не видел. Пятиминутная проверка на null превращается в двухчасовые раскопки сгенерированного кода. Хук-загрузчик сделал ровно то, о чём ты просил. Ты просто забыл, что теперь он владеет твоими трейсами.
</Hook>

<Crux>Хуки кастомизации перехватывают `resolve` и `load` вне потока, образуя цепочку через `next…`; транспайлер без source maps портит каждый трейс.</Crux>

<Explanation>

## Два хука: resolve и load

В пайплайне модулей Node есть два шва, в которые можно врезаться, и хук кастомизации — это функция, сидящая на одном из них. **`resolve(specifier, context, nextResolve)`** превращает спецификатор (`"lodash"`, `"./util.js"`, `"data:..."`) в финальный, полностью квалифицированный URL плюс объявленный `format` (`"module"`, `"commonjs"`, `"json"`, …). **`load(url, context, nextLoad)`** берёт этот URL и возвращает сам `source` (строку или буфер) и его `format` — и именно здесь можно *преобразовать* байты до того, как их увидит V8. Resolve отвечает на «*какой* файл»; load — на «*что* в нём». Транспайлер — это `load`-хук: он перехватывает `.ts`-URL, компилирует исходник и отдаёт JavaScript с `format: "module"`.

Третий аргумент — это вся суть. `nextResolve` / `nextLoad` — это *следующий хук в цепочке* (или встроенный дефолт Node, если ты последний). Либо ты обрабатываешь запрос сам, либо вызываешь `next…(specifier, context)`, чтобы делегировать дальше. Поэтому хуки **композируются**: резолвер tsx и резолвер инструмента покрытия могут быть оба зарегистрированы, каждый обрабатывает то, что ему важно, и передаёт остальное вниз.

```js
// loader.js — a load hook that transpiles TS on the fly
import { readFile } from "node:fs/promises";
import { transform } from "some-fast-transpiler"; // e.g. swc/esbuild

export async function load(url, context, nextLoad) {
  if (!url.endsWith(".ts")) {
    return nextLoad(url, context); // not ours — defer down the chain
  }
  const raw = await readFile(new URL(url), "utf8");
  const { code } = await transform(raw, { sourcemap: "inline" }); // ← keep maps!
  return { format: "module", source: code, shortCircuit: true };
}
```

Здесь живут две обязательные детали. `shortCircuit: true` говорит Node, что ты намеренно обработал запрос и *не* вызываешь `nextLoad` — опусти его, и Node бросит ошибку, чтобы хук тихо не съел цепочку. А `sourcemap: "inline"` — та самая строка, которую хук во вступлении забыл: без неё каждая позиция, о которой сообщает V8, — в скомпилированном выводе, а не в твоём `.ts`.

## Регистрация хуков: module.register

Хуки не подключают вручную — их `register()`-ят, и Node поднимает их в отдельном потоке.

```js
// app entry, run first: node --import ./register.js app.js
import { register } from "node:module";
register("./loader.js", import.meta.url); // (specifier, parentURL)
```

`register(specifier, parentURL)` резолвит `specifier` *относительно* `parentURL` (передавай `import.meta.url`, чтобы относительный `./loader.js` работал независимо от cwd), затем загружает этот модуль **в отдельном worker-потоке** и начинает направлять каждый последующий `import`/`require` через его экспорты `resolve` и `load`. Правило порядка, на котором обжигаются: хуки влияют только на модули, загруженные **после** запуска `register`, поэтому регистрация должна произойти до собственных импортов приложения — что как раз и гарантирует `--import` (он запускает файл до главной точки входа). Поставь `register()` в начало `app.js` — и импорт самого `app.js`, и всё, что он статически импортирует, уже зарезолвилось до того, как твой хук вообще существовал.

Чтобы передать конфигурацию *внутрь* хуков, работающих вне потока, `register` принимает опцию `data` и `transferList`, доставляемые в опциональный хук **`initialize`**, который выполняется один раз в потоке хука:

```js
register("./loader.js", {
  parentURL: import.meta.url,
  data: { tsconfig: "./tsconfig.json" },        // structured-cloned to the hook thread
});

// loader.js
export async function initialize(data) {
  // runs once on the hook thread; stash config, open a MessagePort, etc.
  globalThis.__tsconfig = data.tsconfig;
}
```

<Inset kind="why" lang="ru">
Почему *отдельный поток*? Хуки-загрузчики до Node 20 выполнялись **в том же контексте**, что и твоё приложение, разделяя его граф модулей и глобалы. Это создавало проблему змеи, кусающей свой хвост: собственные `import`-ы загрузчика шли через сам загрузчик, так что хук мог уйти в дедлок, загружая свои же зависимости, а любое состояние, которого хук касался (пропатченный глобал, недоинициализированный модуль), утекало прямо в приложение. Перенос хуков на изолированный worker-поток обрубает это: граф хуков и граф приложения полностью раздельны, так что загрузчик может тянуть тяжёлые зависимости (целый транспайлер), не загрязняя и не гоняясь с приложением, *для* которого он и грузит. Цена изоляции — нельзя делиться живыми объектами: общаешься через structured-cloned `data` и `MessagePort` (двунаправленный канал передачи сообщений между потоками), а не через общую переменную.
</Inset>

## Когда тянуться за хуком — и когда нет

Прежде чем подключать кастомный хук, спроси себя: это проблема dev-цикла или требование прода? Ответ почти всегда определяет верный выбор.

Хуки оправдывают себя в узком наборе случаев: транспиляция TypeScript/JSX на лету (`tsx`, `ts-node`, swc-node — все регистрируют `load`-хук), моки и инструментирование в тестах, кастомные протоколы (`import x from "config:db"`), ремаппинг в стиле import-map и инструменты покрытия, переписывающие исходник. Общий мотив — *эргономика разработки и тестов*: запуск кода как написан, без сборки.

Компромиссы реальны и накапливаются. **(а) Хук выполняется для каждого модуля**, так что медленный `load` добавляет свою стоимость линейно по всему дереву зависимостей — шаг транспиляции на load по ~30–50 мс на файл вхолодную превращает приложение из 400 файлов в лишние 12–20 секунд старта на каждый запуск `node`. **(б) Модель «вне потока» означает отсутствие дешёвого общего состояния** — из хука не дотянуться до глобалов приложения; всё идёт через порт. **(в) Это нестабильная поверхность.** API хуков переделывали между Node 18, 20 и 22 (в потоке → вне потока, удаление `globalPreload`, добавление `register`), так что загрузчик, привязанный к одной мажорной версии, может сломаться на следующей.

<Infographic
  svg={startupTax}
  label="Линейный график: дополнительное время старта от хука транспиляции-на-load растёт линейно с числом модулей — около 5 секунд на 100 файлах, 10 на 200, 15 на 300 и 20 на 400, при ~50 мс на файл, и платится на каждом запуске node."
  caption="Хук выполняется для каждого модуля, поэтому стоимость транспиляции складывается линейно по дереву зависимостей — 12-20 лишних секунд для приложения из 400 файлов на каждый холодный старт. Этот налог на каждый запуск и есть причина собирать для прода, а хуки оставлять для dev."
  hue="var(--d-infra)"
/>

В совокупности: хуки дают быстрее dev-цикл ценой налога на старт каждого вызова, отсутствия дешёвого общего состояния и API, который скачет между мажорами. Сеньорское решение: **транспилируй на этапе сборки для прода** (одна стоимость, source maps выписаны на диск, стабильные артефакты), а хуки оставь для dev/тестов, где цикл без сборки стоит платы за каждый запуск.

<TradeoffMatrix
  id="03-module-customization-hooks-tradeoff-1"
  lessonSlug="03-module-customization-hooks"
  lang="ru"
  prompt="Ты держишь TypeScript HTTP-сервис. В ПРОДЕ нужен быстрый, предсказуемый старт и трейсы, указывающие на реальные строки исходника. Как TS должен становиться исполняемым JS?"
  options={[
    { name: "Собирать заранее (tsc/swc/esbuild) в JS + .map-файлы, деплоить скомпилированный артефакт, запускать обычный node", summary: "Собрать раз, везти maps.", correct: true, justification: "Это дефолт для прода. Ты платишь за транспиляцию один раз на сборке, а не на каждом холодном старте каждой реплики; выписанные .map-файлы дают V8 верные позиции, так что ночной трейс указывает на реальную строку .ts; и ты везёшь замороженный, осматриваемый артефакт, а не компилируешь вживую на worker-потоке, чьё API скачет между мажорами. Детерминизм, скорость и точные трейсы — ровно то, что нужно проду." },
    { name: "Зарегистрировать хук транспиляции-на-load (module.register) и запускать .ts прямо в проде", summary: "Налог на старт + риск.", justification: "Это ошибка из вступления. Load-хук перетранспилирует каждый модуль на каждом старте процесса, добавляя десятки мс на файл линейно по дереву (секунды на масштабе), и он безопасен ровно настолько, насколько аккуратно обращается с source maps — потеряешь maps, и каждый прод-трейс врёт. Ты ещё и привязал прод к экспериментальному, быстро меняющемуся API. Отлично для dev-эргономики; неверный инструмент для горячего пути прода." },
    { name: "Использовать require.extensions['.ts'], чтобы компилировать на require в рантайме", summary: "Deprecated + только CJS.", justification: "require.extensions давно deprecated, вообще не участвует в ESM-пайплайне и является легаси-предком load-хуков. Он несёт тот же налог на транспиляцию на каждый запуск, что и загрузчик, без всякой цепочки и изоляции вне потока, и просто не срабатывает на import. Тупик для современного кода." },
    { name: "Пропустить транспиляцию; везти .ts-файлы и дать Node срезать типы в рантайме без source maps", summary: "Теряет позиции.", justification: "Даже где Node может запускать TS напрямую, срезание типов без выписывания source maps воспроизводит ровно тот сбой из вступления: V8 сообщает позиции в срезанном выводе, так что трейсы расходятся с твоим исходником. Исполняемый — не то же, что отлаживаемый: без maps ты подписался на те же ночные раскопки." },
  ]}
/>

<Quiz
  id="03-module-customization-hooks-quiz-1"
  lessonSlug="03-module-customization-hooks"
  lang="ru"
  question="Что делает вызов nextLoad(url, context) внутри load-хука?"
  choices={[
    { label: "Делегирует запрос следующему хуку в цепочке (или встроенному дефолтному загрузчику Node, если этот хук последний), позволяя хукам композироваться — ты вызываешь его для модулей, которые твой хук не обрабатывает", correct: true },
    { label: "Заново рекурсивно запускает твой же load-хук на том же URL", misconception: "next… — это не рекурсия в самого себя: он передаёт запрос ВНИЗ по цепочке следующему зарегистрированному хуку или дефолтному загрузчику. Повторный вызов своего хука зациклил бы; nextLoad явно передаёт дальше." },
    { label: "Перезагружает модуль с диска в обход кэша модулей", misconception: "nextLoad не имеет отношения к кэшу. Это функция продолжения цепочки: она передаёт запрос на загрузку следующему хуку/дефолту, чтобы необработанные URL всё равно загрузились нормально." },
    { label: "Отменяет загрузку и бросает ERR_MODULE_NOT_FOUND", misconception: "Вызов next… — это как раз способ УСПЕШНО обработать модули, которые ты не трогаешь: он продолжает цепочку. Чтобы намеренно обработать модуль самому, ты возвращаешь результат с shortCircuit: true, а не вызываешь next." },
  ]}
/>

</Explanation>

<FlowDiagram
  label="Спецификатор, проходящий через цепочку хуков кастомизации на пути к V8"
  hue="var(--d-systems)"
  perRow={3}
  nodes={[
    { id: "spec", label: "import 'x.ts'", sub: "specifier" },
    { id: "res", label: "resolve chain", sub: "→ url + format" },
    { id: "load", label: "load chain", sub: "→ source" },
    { id: "xform", label: "transform", sub: "TS→JS + map" },
    { id: "v8", label: "V8 evaluates", sub: "compiled module" },
  ]}
  edges={[
    { from: "spec", to: "res", label: "nextResolve" },
    { from: "res", to: "load", label: "url" },
    { from: "load", to: "xform", label: "nextLoad" },
    { from: "xform", to: "v8", label: "source+map" },
  ]}
  caption="Каждый спецификатор проходит цепочку resolve (каждый хук вызывает nextResolve или short-circuit) до URL, затем цепочку load (nextLoad / short-circuit) до байтов исходника, которые хук может преобразовать. Всё это работает в отдельном потоке хука; на основной поток к V8 возвращается только финальный исходник. Потеряй source map в «transform» — и позиции V8 больше не совпадут с твоим файлом."
/>

<KeyTakeaway>Хуки врезаются в `resolve` (спецификатор→URL) и `load` (URL→исходник) через `module.register`, образуя цепочку через `next…`, вне потока ради изоляции. Для dev — транспиляция хуком; для прода — сборка, не теряй maps в `load`.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="03-module-customization-hooks-retrieval"
  lang="ru"
  questions={[
    { q: "Почему хуки кастомизации модулей перенесли с основного потока и что это стоит тебе?", a: "Хуки-загрузчики до Node 20 выполнялись в том же контексте, что и приложение, разделяя его граф модулей и глобалы. Это было структурно опасно: собственные импорты загрузчика шли через загрузчик (хук мог уйти в дедлок, загружая свои же зависимости), а любой глобал, которого хук касался, утекал в приложение, которое он загружал. Дизайн вне потока (вокруг API register()) кладёт хуки на изолированный worker-поток, так что граф зависимостей хука полностью отделён от графа приложения — загрузчик может тянуть тяжёлый транспайлер, не загрязняя и не гоняясь с приложением. Цена — нет дешёвого общего состояния: из хука не дотянуться до живых объектов приложения. Конфигурацию передаёшь один раз через опцию data у register (structured-cloned) в хук initialize, а всё прочее стримишь через MessagePort. Так ты меняешь удобное общее изменяемое состояние на изоляцию, которая предотвращает целые классы дедлоков и загрязнения «загрузчик против приложения»." },
    { q: "Хук транспиляции-на-load — причина того, что каждый прод-трейс указывает не на ту строку. В чём механизм и каков фикс?", a: "Load-хук перехватывает каждый .ts-URL, компилирует его в JavaScript и возвращает этот скомпилированный исходник V8. V8 видит только скомпилированные байты, поэтому, захватывая стек, он сообщает позиции строки/колонки в СГЕНЕРИРОВАННОМ выводе — коде, который ты не писал и не видишь. Редактор показывает .ts; трейс описывает .js; они расходятся ровно на столько строк, сколько добавила или убрала трансформация, так что «ошибка в billing.ts:64» попадает на пустую строку. Фикс — выдать source map и встроить её в возвращаемый исходник (sourcemap: 'inline' в опциях трансформации) и убедиться, что поддержка source map в Node включена (по умолчанию она включена в актуальных версиях; --enable-source-maps форсирует её). С картой V8 ремапит захваченные позиции назад в исходник, и трейсы снова указывают на реальные строки. Глубокий урок: как только load-хук преобразует байты, он владеет точностью каждого трейса для этих модулей — source maps не опциональный лоск, а контракт, удерживающий код отлаживаемым." },
  ]}
/>

<Recap lang="ru">
Пайплайн модулей Node открывает два шва для перехвата, и хук кастомизации — это функция на одном из них: `resolve(specifier, context, nextResolve)` мапит спецификатор в финальный URL плюс `format`, а `load(url, context, nextLoad)` возвращает байты исходника — точку, где транспайлер превращает `.ts` в JavaScript до того, как его увидит V8. Аргумент `next…` — это следующий хук (или дефолт), так что хуки композируются: обработай своё, делегируй остальное, и помечай обработанный запрос через `shortCircuit: true`. Хуки устанавливают через `module.register(specifier, parentURL)`, который грузит их в *отдельном worker-потоке* — изоляция, которая с Node 20 заменила опасную модель `globalPreload` в потоке, где загрузчик мог уйти в дедлок на своих же импортах или утечь глобалами в приложение; цена — нет общего состояния, так что конфиг приходит через `data` у `register` и хук `initialize`, а всё живое идёт через `MessagePort`. Хуки — верный инструмент для эргономики dev/тестов: TS/JSX на лету, моки, кастомные протоколы, покрытие, — но они выполняются для *каждого* модуля (~30–50 мс/файл транспиляции — это секунды старта на масштабе), не могут дёшево делиться состоянием, а API штормит между мажорами, так что прод должен транспилировать на сборке. И сбой, определяющий этот урок: транспилирующий `load`-хук без source maps заставляет V8 сообщать позиции в скомпилированном выводе, так что каждый трейс расходится с реальным исходником — выдавай встроенные maps или владей ночными раскопками.
</Recap>
