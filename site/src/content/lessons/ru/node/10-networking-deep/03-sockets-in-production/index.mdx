---
concepts:
- socket-timeouts
- keepalive-tuning
- econnreset-epipe
- max-connections
- graceful-close
- fd-exhaustion
deepensInto: []
estMin: 18
lang: ru
lessonType: topic
level: senior
order: 3
prereqs: []
slug: 03-sockets-in-production
sources:
- https://nodejs.org/api/net.html
- https://nodejs.org/api/http.html
- https://blog.cloudflare.com/this-is-strictly-a-violation-of-the-tcp-specification/
spiral: []
status: ready
summary: 'Продакшн-сбои сокетов предсказуемы: простаивающие сокеты без таймаутов держат
  файловые дескрипторы, ECONNRESET/EPIPE роняют незакалённые обработчики, а безграничный
  accept исчерпывает fd. Добавь таймауты, лимиты и graceful drain.'
title: 'Сокеты в продакшене: таймауты, лимиты и исчерпание fd'
track: node
unit: 10-networking-deep
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";
import Infographic from "~/components/diagram/Infographic.astro";
import socketHardening from "./infographics/socket-hardening.svg?raw";

<Hook>
В 3 часа ночи сервис перестаёт принимать соединения. CPU около нуля, память в норме, в логах ни ошибки — он просто висит. Дежурный инженер запускает `lsof -p <pid> | wc -l` и видит 65 535 открытых файловых дескрипторов. Виновник: клиенты, открывшие сокет, ничего не приславшие и ушедшие — half-open или тихо умершие соединения без таймаута. Каждое держало файловый дескриптор вечно. Процесс не падал; он задохнулся. Каждый сокет, что выдаёт ядро, — конечный ресурс, и в продакшене вопрос не *если* клиент ведёт себя плохо, а *когда*.
</Hook>

<Crux>Сокеты в продакшене: таймаутить простаивающие, ловить `ECONNRESET`/`EPIPE` как рутину, ограничивать число соединений, чтобы не исчерпать fd.</Crux>

<Explanation>

## Простаивающим сокетам нужен таймаут, иначе они держат fd вечно

Инцидент из хука воспроизводим в любом Node-сервере, который выезжает без трёх осознанных защит. Вот как работает каждый режим отказа и что именно его останавливает.

TCP-сокет после принятия занимает **файловый дескриптор** — конечный per-process ресурс ядра (часто по умолчанию 1024 или ограничен `ulimit -n`). Node не накладывает idle-таймаут по умолчанию: сокет, что подключился и затем замолчал, будет висеть открытым бесконечно. Плохой или мёртвый клиент — упавший без чистого закрытия или half-open соединение, чей FIN так и не пришёл — поэтому течёт дескриптором, который никогда не возвращается. Накопи достаточно — и процесс больше не может ничего `accept()`-ить: зависание из хука.

Защита — `socket.setTimeout(ms)`, который эмитит событие `'timeout'` после `ms` бездействия (нет ни чтений, ни записей). Важно: **событие таймаута не закрывает сокет за тебя** — оно лишь уведомляет — поэтому ты обязан действовать:

```js
server.on("connection", (socket) => {
  socket.setTimeout(30_000);                 // 30с бюджет простоя
  socket.on("timeout", () => {
    socket.destroy(new Error("idle timeout")); // ТЫ обязан закрыть
  });
  socket.on("error", () => {});               // глушим ошибки после destroy
});
```

Для HTTP-серверов та же идея даётся как `server.setTimeout()`, плюс `server.headersTimeout`, `server.requestTimeout` и `server.keepAliveTimeout` — последний охраняет промежуток между keep-alive запросами. Атака Slowloris (DoS через тысячи медленных соединений, держащих сервер занятым) — ровно оружейная версия хука: атакующий открывает много соединений и капает байты медленно, держа каждое чуть под любым наивным таймаутом, исчерпывая ёмкость соединений почти без трафика. Таймауты заголовков и запроса — то, что её останавливает.

## Keep-alive: переиспользуй рукопожатие, но ограничь простой

Keep-alive — переиспользование одного TCP-соединения для многих запросов, избегающее свежего рукопожатия (и TLS-согласования) каждый раз — большой выигрыш по задержке. Но простаивающее keep-alive соединение всё равно держит файловый дескриптор и слот. Натяжение настройки реально: слишком короткий `keepAliveTimeout` — и ты выбрасываешь выгоду переиспользования, переподключаясь постоянно; слишком долгий — и простаивающие клиенты копят дескрипторы, что ты мог бы отдать активным.

Есть и пресловутая **гонка** между сервером, закрывающим простаивающее keep-alive соединение, и клиентом, шлющим ещё один запрос по нему: запрос приземляется на сокет, который сервер разбирает, порождая `ECONNRESET`, который клиент видит как упавший запрос. Смягчение — сделать `keepAliveTimeout` сервера *длиннее* idle-таймаута вышестоящего балансировщика, чтобы LB всегда закрывал первым, на соединении, которое знает простаивающим — классический источник прерывистых 502, если порядок обратный.

<Inset kind="why" lang="ru">
Почему keep-alive таймаут сервера должен превышать таймаут балансировщика? Кто закрывает простаивающее соединение, выигрывает гонку чисто; кто шлёт по соединению, которое *другая* сторона только что закрыла, проигрывает с reset. Если idle-таймаут LB короче, LB шлёт запрос на соединение, что твой сервер уже начал закрывать, и клиент получает 502/ECONNRESET. Сделай сервер живущим дольше LB, и LB всегда инициирует закрытие на реально простаивающем сокете — нет запроса в полёте, который можно сбросить.
</Inset>

## ECONNRESET и EPIPE — не баги, а вторник

Два кода ошибок доминируют в продакшн-логах сокетов, и относиться к ним как к экзотике — ошибка.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Ошибка</th>
        <th class="border border-border px-3 py-2 text-left">Что случилось</th>
        <th class="border border-border px-3 py-2 text-left">Верная реакция</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2"><code>ECONNRESET</code></td>
        <td class="border border-border px-3 py-2">Пир прислал RST — резкое закрытие (падение, kill, обрыв сети)</td>
        <td class="border border-border px-3 py-2">Очисти это соединение; не роняй процесс</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2"><code>EPIPE</code></td>
        <td class="border border-border px-3 py-2">Ты записал в сокет, который пир уже полностью закрыл</td>
        <td class="border border-border px-3 py-2">Прекрати писать; соединения больше нет</td>
      </tr>
    </tbody>
  </table>
</div>

Оба эмитятся как события `'error'` на сокете, и железное правило из урока про TCP применяется в продакшене с силой: **необработанное `'error'` сокета кидает исключение и роняет весь процесс**, убивая вместе с ним каждое другое соединение в полёте. Один плохой клиент никогда не должен мочь уронить сервер, обслуживающий тысячи. Поэтому каждому сокету — принятому на сервере и открытому на клиенте — нужен обработчик `'error'`, который логирует-и-очищает, а не перебрасывает. Асимметрия для усвоения: `ECONNRESET` — это то, что *сделали с тобой* (пир сбросил), `EPIPE` — это *ты* пишешь в закрытую трубу — но оба сводятся к «это одно соединение мертво, отпусти его, продолжай обслуживать остальных».

## Ограничь соединения, затем сливай аккуратно

Корневая причина в хуке — безграничный рост ресурса. `server.maxConnections` ограничивает одновременные сокеты: за лимитом сервер перестаёт принимать (OS backlog поглощает несколько, затем отказывает), что сбрасывает нагрузку вместо коллапса в исчерпание fd. Это грубый инструмент, но реальный страховочный трос. Соедини его с поднятым осознанно `ulimit -n` — и у тебя верхняя граница, что ты выбрал, а не обнаружил в 3 ночи.

Вторая половина — **graceful shutdown**. На `SIGTERM` (деплой, scale-down) ты обязан слить, а не бросить: `server.close()` перестаёт принимать *новые* соединения и зовёт колбэк, как только все *существующие* завершатся — но он будет ждать вечно на долгоживущем простаивающем keep-alive сокете, поэтому ты также проходишь по активным сокетам, сигналишь им завершиться и `destroy()`-ишь отставших после дедлайна. Пропуск этого превращает каждый деплой в волну `ECONNRESET` для пользователей в полёте.

<FlowDiagram
  label="Каскад исчерпания файловых дескрипторов: простаивающие сокеты без таймаутов копятся, пока сервер не перестанет принимать, против закалённого пути с таймаутом, лимитом и сливом"
  hue="var(--d-network)"
  perRow={4}
  nodes={[
    { id: "idle", label: "Idle сокеты", sub: "no timeout" },
    { id: "leak", label: "Утечка FD", sub: "не освоб." },
    { id: "cap", label: "Предел fd", sub: "ulimit" },
    { id: "hang", label: "accept() сбой", sub: "висит" },
    { id: "to", label: "setTimeout", sub: "destroy" },
    { id: "max", label: "maxConnections", sub: "сброс" },
    { id: "drain", label: "close()+drain", sub: "SIGTERM" },
    { id: "ok", label: "Жив", sub: "ограничен" }
  ]}
  edges={[
    { from: "idle", to: "leak", label: "копятся" },
    { from: "leak", to: "cap", label: "рост" },
    { from: "cap", to: "hang", label: "исчерпан" },
    { from: "to", to: "max", label: "+лимит" },
    { from: "max", to: "drain", label: "+слив" },
    { from: "drain", to: "ok", label: "итог" }
  ]}
  caption="Верхний ряд — каскад 3 часов ночи; нижний — три дисциплины, что его предотвращают. Таймауты возвращают утёкшие дескрипторы, maxConnections ограничивает рост, graceful drain избегает шторма reset'ов на деплое."
/>

<Infographic
  svg={socketHardening}
  label="Вертикальный список пяти повторяющихся продакшн-режимов отказа сокетов, каждый в паре с одной защитой: утечка fd на idle-сокетах лечится setTimeout плюс destroy, Slowloris — headersTimeout и requestTimeout, необработанные ECONNRESET/EPIPE — обработчиком error на каждом сокете, безграничный accept — maxConnections и ulimit, а штормы reset'ов на SIGTERM — server.close плюс слив."
  caption="Весь урок одной шпаргалкой: каждый продакшн-сбой сокета предсказуем и отвечает одной осознанной линии защиты."
  hue="var(--d-network)"
/>

<TradeoffMatrix
  id="10-sockets-in-production-tradeoff-1"
  lessonSlug="03-sockets-in-production"
  lang="ru"
  prompt="Твой Node-сервер стоит за балансировщиком, и ты видишь прерывистые 502/ECONNRESET на в остальном здоровых запросах."
  options={[
    { name: "Сделать keepAliveTimeout сервера (и headersTimeout) длиннее idle-таймаута LB", summary: "Сервер живёт дольше LB.", correct: true, justification: "502 идут от гонки: LB переиспользует простаивающее keep-alive соединение, которое Node-сервер уже начал закрывать, и запрос в полёте приземляется на разбираемый сокет → reset. Сделав keepAliveTimeout сервера больше idle-таймаута LB, гарантируешь, что LB всегда инициирует закрытие на реально простаивающем соединении, без запроса в полёте для сброса. Это стандартный фикс keep-alive 502 за прокси." },
    { name: "Агрессивно снизить keepAliveTimeout сервера, чтобы соединения закрывались быстро", summary: "Закрывать раньше.", justification: "Это делает гонку хуже, не лучше: более короткий таймаут сервера значит, что сервер закрывает простаивающие соединения ещё раньше LB, поэтому LB вероятнее шлёт запрос на только что закрытый сокет. Нужно, чтобы сервер закрывал последним, не первым. Агрессивно короткий keep-alive ещё и выбрасывает переиспользование, добавляя задержку рукопожатия." },
    { name: "Полностью отключить keep-alive (Connection: close)", summary: "Без переиспользования.", justification: "Убирает гонку, убирая фичу, но платишь полным TCP- (и TLS-) рукопожатием на каждый запрос — большой регресс задержки и CPU на масштабе. Приемлемо лишь для очень низкого трафика или патологий; правильный фикс — порядок таймаутов, сохраняющий переиспользование." },
    { name: "Добавить retry на ECONNRESET в клиенте", summary: "Просто ретрай.", justification: "Ретрай маскирует симптом и может быть разумным дублирующим поясом для идемпотентных запросов, но не адресует причину, удваивает задержку на каждом попадании и рискует дублем побочных эффектов на неидемпотентных вызовах. Сначала исправь порядок таймаутов; ретрай лишь как защита в глубину." }
  ]}
/>

<Quiz
  id="10-sockets-in-production-quiz-1"
  lessonSlug="03-sockets-in-production"
  lang="ru"
  question="Долго работающий Node-сервис медленно перестаёт принимать новые соединения без падения, без высокого CPU и без роста памяти. Вероятная причина?"
  choices={[
    { label: "Исчерпание файловых дескрипторов из-за idle/half-open сокетов без setTimeout — каждый утёкший сокет держит fd, пока процесс не упрётся в ulimit и не сможет больше accept()", correct: true },
    { label: "Утечка памяти", misconception: "Память явно названа нормальной. Подпись здесь — нет падения, нет CPU, нет памяти, лишь неспособность принимать — указывает на исчерпание дескрипторов, иной конечный ресурс, чем память кучи." },
    { label: "Event loop заблокирован CPU-работой", misconception: "CPU около нуля, значит цикл не заблокирован вычислением. Заблокированный цикл задрал бы CPU или задержку на всю работу, а не тихо ограничил новые соединения." },
    { label: "Сбои разрешения DNS", misconception: "DNS влияет на исходящие разрешения имён, не на способность сервера принимать входящие соединения. Неспособность accept() — симптом предела дескрипторов." }
  ]}
/>

<Quiz
  id="10-sockets-in-production-quiz-2"
  lessonSlug="03-sockets-in-production"
  lang="ru"
  question="socket.setTimeout(30000) эмитит событие 'timeout', но соединение остаётся открытым и продолжает держать fd. Почему?"
  choices={[
    { label: "setTimeout лишь эмитит событие как уведомление — он не закрывает сокет; ты обязан сам вызвать socket.destroy() (или end()) в обработчике", correct: true },
    { label: "30 секунд слишком мало, чтобы подействовать", misconception: "Таймаут сработал — потому ты и видишь событие. Дело не в длительности; обработчик события обязан активно закрыть сокет, чего этот код не сделал." },
    { label: "Таймауты не работают на keep-alive сокетах", misconception: "setTimeout работает независимо от keep-alive. Событие сработало верно; недостающее звено — вызов destroy() в обработчике." },
    { label: "Надо сначала вызвать setKeepAlive(false)", misconception: "Keep-alive (TCP-пробы) и idle-таймаут — независимые ручки. Причина, по которой fd сохраняется, проста: ничто не закрыло сокет после события таймаута." }
  ]}
/>

</Explanation>

<KeyTakeaway>В продакшене сокеты падают предсказуемо: `setTimeout` + `destroy()` для возврата idle fd, обработчик `'error'` на каждом сокете, чтобы `ECONNRESET`/`EPIPE` не уронили процесс, `maxConnections` для ограничения роста и `server.close()` + слив на `SIGTERM` ради избежания штормов reset'ов.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="10-sockets-in-production-retrieval"
  lang="ru"
  questions={[
    { q: "Почему Node-сервер может исчерпать файловые дескрипторы без падения, без скачка CPU и без роста памяти — и как это предотвратить?", a: "Каждый принятый TCP-сокет потребляет файловый дескриптор, конечный per-process ресурс ядра, ограниченный ulimit -n. Node не ставит idle-таймаут по умолчанию, поэтому клиент, что подключился и замолчал — упал без чистого закрытия или half-open соединение, чей FIN не пришёл — оставляет сокет открытым, который никогда не возвращается. Они текут медленно: ошибки не кидается, память кучи не растёт, CPU простаивает, потому что на них ничего не происходит. Они просто копятся, пока процесс не упрётся в предел дескрипторов и не сможет больше accept() новые соединения — сервер висит без падения. Фикс — idle-таймауты на сокет: socket.setTimeout(ms) эмитит событие 'timeout' после ms без чтений и записей, и так как это событие НЕ закрывает сокет само, ты обязан вызвать socket.destroy() (или end()) в обработчике, чтобы реально вернуть дескриптор. Для HTTP добавь headersTimeout и requestTimeout против медленных клиентов в стиле Slowloris, подними ulimit -n осознанно и ограничь конкурентность через server.maxConnections, чтобы рост был ограничен, а не обнаружен в продакшене." },
    { q: "Почему надо обрабатывать ECONNRESET/EPIPE на каждом сокете и что добавляет graceful shutdown?", a: "ECONNRESET (пир прислал RST — резкое закрытие от падения, kill или обрыва сети) и EPIPE (ты записал в сокет, который пир уже полностью закрыл) эмитятся как события 'error' на сокете, а необработанное 'error' сокета в Node кидает исключение и роняет весь процесс — забирая с собой каждое другое соединение в полёте. Поскольку эти ошибки рутинны в продакшене (любой клиент может упасть или отвалиться в любой момент), один плохой пир никогда не должен мочь убить сервер, обслуживающий тысячи, поэтому каждому сокету — принятому сервером и открытому клиентом — нужен слушатель 'error', который логирует и очищает то одно соединение вместо переброса. Graceful shutdown дополняет картину для деплоев и scale-down: на SIGTERM ты зовёшь server.close(), чтобы перестать принимать новые соединения и дать существующим запросам завершиться, но так как close() будет ждать бесконечно на простаивающих keep-alive сокетах, ты также отслеживаешь активные сокеты, прекращаешь их keep-alive и destroy()-ишь те, что пережили дедлайн слива. Без этого каждый деплой шлёт волну ECONNRESET пользователям посреди запроса; с ним работа в полёте завершается, и лишь реально застрявшие сокеты закрываются принудительно." }
  ]}
/>

<Recap lang="ru">
Продакшн-сбои сокетов не случайны — это три повторяющиеся дисциплины, которые ты либо применяешь, либо за них платишь. Первая: таймаутить простаивающие сокеты — каждый принятый сокет держит конечный файловый дескриптор, Node не ставит idle-таймаут по умолчанию, поэтому `socket.setTimeout(ms)` плюс явный `destroy()` в обработчике `'timeout'` — то, что возвращает дескрипторы, утёкшие от мёртвых или half-open клиентов, а HTTP-овские `headersTimeout`/`requestTimeout` — то, что бьёт Slowloris. Вторая: относись к `ECONNRESET` и `EPIPE` как к рутине — оба всплывают как события `'error'` сокета, а необработанное роняет весь процесс и каждое соединение на нём, поэтому каждому сокету нужен обработчик ошибки, очищающий одно соединение. Третья: ограничивай и сливай — `server.maxConnections` и осознанный `ulimit -n` ограничивают рост, чтобы ты не исчерпал дескрипторы тихо, а `server.close()` со сливом активных сокетов на `SIGTERM` превращает деплои из штормов reset'ов в чистые передачи. Настраивай keep-alive между переиспользованием и коплением и сделай `keepAliveTimeout` сервера живущим дольше балансировщика, чтобы убить гонку прерывистых 502. Ограничь ресурс, таймаутни простой, обработай неизбежную ошибку, слей на выходе. Теперь, когда встретишь Node-сервис, который «висит» без падения и без высокого CPU, первый шаг — `lsof -p <pid> | wc -l` — и ты сразу знаешь, какие три недостающие строки это вызвали.
</Recap>
