---
concepts:
- dgram-socket
- datagram-vs-stream
- no-delivery-guarantee
- message-size-mtu
- broadcast-multicast
- when-udp-wins
deepensInto: []
estMin: 16
lang: ru
lessonType: topic
level: middle
order: 2
prereqs: []
slug: 02-udp-and-dgram
sources:
- https://nodejs.org/api/dgram.html
- https://en.wikipedia.org/wiki/User_Datagram_Protocol
- https://github.com/statsd/statsd/blob/master/docs/metric_types.md
spiral: []
status: ready
summary: 'UDP через dgram шлёт независимые датаграммы без соединения, без порядка и
  без гарантии доставки. Каждый send — один пакет, ограниченный MTU; ты меняешь надёжность
  на задержку, и это выигрывает для метрик, discovery, игр и DNS.'
title: 'UDP и dgram: датаграммы, границы сообщений и когда отказаться от надёжности'
track: node
unit: 10-networking-deep
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
Команда переносит метрики приложения с синхронных HTTP POST на StatsD-агент по UDP, и p99 задержки запроса падает на 30 мс за ночь. Причина груба и проста: HTTP-вызовы метрик ждали TCP-рукопожатие, надёжную доставку и ответ — на горячем пути каждого запроса. UDP-`send()` не делает ничего из этого. Он отдаёт одну датаграмму ядру и возвращается; если пакет потерян, метрика потеряна, и это *нормально*. Они осознанно обменяли крошечную долю точности метрик на то, чтобы больше никогда не блокировать запрос пользователя ради телеметрии.
</Hook>

<Crux>UDP через `dgram` шлёт независимые, неупорядоченные best-effort датаграммы — один `send()` это один пакет — меняя надёжность на задержку.</Crux>

<Explanation>

## Датаграммы — это сообщения, а не поток

TCP — надёжный упорядоченный поток байтов; UDP — противоположная точка дизайна. Модуль `dgram` даёт **бессоединительный** сокет, который шлёт и принимает дискретные **датаграммы** — самодостаточные пакеты, каждый маршрутизируется независимо. Нет рукопожатия, нет состояния соединения, нет порядка и нет подтверждения. Датаграмма либо приходит целиком, либо не приходит вовсе.

Это переворачивает проблему фрейминга из `net` с ног на голову. В `net` приходилось *добавлять* границы сообщений, потому что TCP их стирает; в `dgram` границы **сохраняются бесплатно** — один `send()` порождает ровно одно событие `'message'` у получателя ровно с теми байтами. Никогда не получишь частичную датаграмму и никогда не получишь две склеенные.

```js
const dgram = require("node:dgram");

const server = dgram.createSocket("udp4");
server.on("message", (msg, rinfo) => {
  // msg — целая датаграмма; rinfo = { address, port, size }
  console.log(`${rinfo.address}:${rinfo.port} -> ${msg.length} байт`);
});
server.bind(8125);

const client = dgram.createSocket("udp4");
client.send(Buffer.from("requests:1|c"), 8125, "127.0.0.1");  // выстрелил и забыл
```

Заметь, чего нет: ни `connect`, ни `accept`, ни объекта сокета на клиента. Сервер `bind`-ит порт и принимает от кого угодно; `rinfo` говорит, кто прислал каждую датаграмму. `send()` — выстрелил-и-забыл: его колбэк срабатывает, когда датаграмма отдана ОС, а **не** когда она доставлена, потому что у UDP нет понятия подтверждения доставки.

## Ни доставки, ни порядка, ни дедупа — по замыслу

Прежде чем выбрать UDP, спроси себя: реально ли твоему протоколу нужна каждая из гарантий TCP, или ты платишь за надёжность, которую никогда не используешь? Таблица ниже делает компромисс наглядным.

UDP не даёт **ни одной** из гарантий TCP, и в этом весь смысл.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Свойство</th>
        <th class="border border-border px-3 py-2 text-left">TCP (net)</th>
        <th class="border border-border px-3 py-2 text-left">UDP (dgram)</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">Доставка</td>
        <td class="border border-border px-3 py-2">Гарантирована (ретрансмит)</td>
        <td class="border border-border px-3 py-2">Best-effort — может быть потеряна</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Порядок</td>
        <td class="border border-border px-3 py-2">По порядку</td>
        <td class="border border-border px-3 py-2">Любой порядок</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Дубликаты</td>
        <td class="border border-border px-3 py-2">Убраны</td>
        <td class="border border-border px-3 py-2">Возможны</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Границы</td>
        <td class="border border-border px-3 py-2">Стёрты (фреймишь сам)</td>
        <td class="border border-border px-3 py-2">Сохранены (1 send = 1 message)</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Соединение</td>
        <td class="border border-border px-3 py-2">Stateful рукопожатие</td>
        <td class="border border-border px-3 py-2">Бессоединительное</td>
      </tr>
    </tbody>
  </table>
</div>

Если приложению нужна надёжность или порядок поверх UDP, *ты* реализуешь их сам (порядковые номера, ACK, ретрансмит) — ровно это и делают QUIC (протокол HTTP/3, строящий надёжность поверх UDP) и надёжные UDP-протоколы игр. Причина стартовать с UDP и добавлять лишь нужное — задержка: нет круга рукопожатия, нет head-of-line блокировки (потерянный TCP-сегмент стопорит всё за ним; потерянная датаграмма затрагивает только себя) и нет состояния соединения, которым надо управлять на масштабе.

## Размер сообщения и обрыв на MTU

Датаграмма ограничена. Теоретический максимум UDP-payload для IPv4 — 65 507 байт, но в продакшене важно не это число. Важное число — **path MTU** — обычно ~1500 байт на Ethernet, часто ~1400 после туннелей/VPN. Датаграмма больше path MTU должна быть **IP-фрагментирована** на несколько пакетов, и вот обрыв: если потерян *хоть один* фрагмент, **вся датаграмма отбрасывается** — IP не может пересобрать частичную датаграмму. Поэтому датаграмма 4 КБ по теряющему пути имеет куда выше эффективную потерю, чем 1 КБ, потому что это три фрагмента, которые все должны выжить.

Практическое правило: держи датаграммы под path MTU — безопасная переносимая цель ~**1200 байт** payload (рекомендация QUIC/DNS). Нужно больше — фрагментируй на уровне приложения на датаграммы размером с MTU со своим порядком, или используй TCP. `socket.send()` не разбивает за тебя против твоего желания — он отдаёт весь буфер ОС, которая фрагментирует на уровне IP с описанной хрупкостью.

<Inset kind="why" lang="ru">
Почему один потерянный фрагмент убивает всю датаграмму? IP-фрагментация разбивает датаграмму по пакетам, но только первый фрагмент несёт UDP-заголовок, а пересборке нужен *каждый* фрагмент, чтобы восстановить оригинал. В UDP нет ретрансмита по фрагментам, поэтому одна потеря значит, что получатель держит неполные куски, которые таймаутятся и выбрасываются. Если оставаться под MTU, каждая датаграмма — один пакет: атомарно, без пересборки, без усиленной потери.
</Inset>

## Broadcast и multicast: один send, много получателей

UDP может адресовать **группы**, чего TCP принципиально не может. **Broadcast** шлёт одну датаграмму каждому хосту локальной подсети (`socket.setBroadcast(true)`, отправка на broadcast-адрес подсети) — полезно для discovery «есть кто живой?» в LAN. **Multicast** — дисциплинированная версия: получатели делают `addMembership(groupAddr)`, чтобы вступить в multicast-группу (224.0.0.0/4), и один `send()` на адрес группы достигает ровно вступивших членов, а сеть дублирует пакет лишь там, где пути расходятся. `setMulticastTTL` ограничивает число хопов. Так работают service discovery (mDNS/Bonjour), gossip некоторых кластерных кэшей и веерная раздача в стиле IPTV — один send, N получателей, без соединения на каждого.

<FlowDiagram
  label="Веер метрик-агента: один процесс приложения шлёт UDP-датаграммы коллектору StatsD, а service discovery использует multicast-группу, чтобы достичь многих нод одним send"
  hue="var(--d-network)"
  perRow={3}
  nodes={[
    { id: "app", label: "Процесс", sub: "send()" },
    { id: "kernel", label: "Ядро UDP", sub: "no ACK" },
    { id: "statsd", label: "StatsD", sub: "':message'" },
    { id: "disco", label: "Discovery", sub: "send()" },
    { id: "group", label: "Multicast", sub: "224.x" },
    { id: "nodes", label: "N нод", sub: "joined" }
  ]}
  edges={[
    { from: "app", to: "kernel", label: "1 датаграмма" },
    { from: "kernel", to: "statsd", label: "best-effort" },
    { from: "disco", to: "group", label: "1 send" },
    { from: "group", to: "nodes", label: "веер" }
  ]}
  caption="Unicast-телеметрия (сверху) — выстрелил-и-забыл: потерянная датаграмма теряет одну метрику. Multicast (снизу) достигает каждого вступившего члена одним send — невозможно по TCP."
/>

<TradeoffMatrix
  id="10-udp-and-dgram-tradeoff-1"
  lessonSlug="02-udp-and-dgram"
  lang="ru"
  prompt="Ты выбираешь транспорт для высокочастотных метрик приложения на горячем пути запроса."
  options={[
    { name: "UDP-датаграммы локальному StatsD-агенту (выстрелил-и-забыл)", summary: "Задержка важнее точности.", correct: true, justification: "Метрики высокообъёмны и по отдельности одноразовы: потеря одного инкремента счётчика среди тысяч не меняет агрегат. UDP-send выстрелил-и-забыл отдаёт одну датаграмму ядру и сразу возвращается, никогда не блокируя запрос на рукопожатии, доставке или ответе. Редкий потерянный пакет — приемлемый, хорошо понятный компромисс — именно поэтому StatsD по умолчанию на UDP." },
    { name: "Синхронный HTTP POST на каждую метрику по TCP", summary: "Надёжно, но блокирует.", justification: "TCP даёт гарантированную доставку, которая метрикам не нужна, и платит за неё рукопожатием, надёжной доставкой и ответом на горячем пути каждого запроса — добавляя десятки миллисекунд хвостовой задержки. Ты покупаешь надёжность для данных, которые не жалко потерять, ценой именно той пользовательской задержки, что хочешь защитить." },
    { name: "Буферить метрики в памяти и сливать по TCP каждые 10 с", summary: "Батч по TCP.", justification: "Батчинг — разумная середина и то, что многие агенты делают внутри, но свой внутрипроцессный буфер рискует потерять весь батч при падении и добавляет сложность. Для общего случая локальный UDP-агент, который сам батчит выше по потоку, даёт неблокирующие send без риска потери-буфера-при-падении." },
    { name: "Писать метрики в лог-файл и читать его tail'ом", summary: "Лог и скрейп.", justification: "Логирование годится для редких событий, но превращает высокочастотные метрики в дисковый I/O и пайплайн парсинга, со своей задержкой и backpressure на горячем пути. Это другой инструмент для другой задачи, а не транспорт для живых счётчиков." }
  ]}
/>

<Quiz
  id="10-udp-and-dgram-quiz-1"
  lessonSlug="02-udp-and-dgram"
  lang="ru"
  question="Ты шлёшь одну датаграмму 4 КБ через публичный интернет, и она часто не доходит целой, тогда как 1 КБ в основном доходят. Почему?"
  choices={[
    { label: "Датаграмма 4 КБ превышает path MTU (~1500 Б), поэтому IP фрагментирует её на пакеты; если потерян хоть один фрагмент, вся датаграмма отбрасывается — держи payload под ~1200 Б", correct: true },
    { label: "У UDP жёсткий лимит датаграммы в 1 КБ", misconception: "Макс payload UDP — 65 507 байт, не 1 КБ. Практический предел — path MTU; ниже него датаграмма — один пакет, выше — фрагментация усиливает потерю." },
    { label: "Ядро ограничивает скорость больших датаграмм", misconception: "Никакого rate-limit по размеру в ядре здесь нет. Хрупкость идёт от IP-фрагментации: один потерянный фрагмент убивает пересборку всей датаграммы." },
    { label: "У TCP была бы та же проблема", misconception: "TCP сегментирует до MSS и ретрансмитит потерянные сегменты, поэтому не страдает от потери всё-или-ничего. Эта хрупкость специфична для больших UDP-датаграмм." }
  ]}
/>

<Quiz
  id="10-udp-and-dgram-quiz-2"
  lessonSlug="02-udp-and-dgram"
  lang="ru"
  question="В dgram когда socket.send(buf, port, host, cb) вызывает cb?"
  choices={[
    { label: "Когда датаграмма отдана ОС на отправку — а не когда доставлена, потому что у UDP нет подтверждения доставки", correct: true },
    { label: "Когда получатель подтверждает датаграмму", misconception: "У UDP нет подтверждений. Нет сигнала доставки, которого можно ждать; колбэк подтверждает лишь локальную передачу ядру." },
    { label: "Когда обработчик 'message' получателя вернулся", misconception: "У отправителя нет видимости обработки у получателя — в чистом UDP нет соединения или канала ответа. Колбэк чисто локальный." },
    { label: "После дефолтного таймаута отправки в 200 мс", misconception: "В UDP нет таймаута отправки. send() завершается, как только ОС приняла датаграмму в свой путь отправки." }
  ]}
/>

</Explanation>

<KeyTakeaway>`dgram` шлёт бессоединительные best-effort датаграммы с сохранёнными границами: 1 `send()` = 1 `'message'`, без гарантий доставки/порядка/дедупа. Держи payload под ~1200 Б ради избежания потери фрагментов и тянись к UDP, когда задержка важнее надёжности — метрики, discovery, игры, DNS.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="10-udp-and-dgram-retrieval"
  lang="ru"
  questions={[
    { q: "Почему UDP-датаграмма больше path MTU теряется намного чаще маленькой?", a: "Датаграмма больше path MTU (обычно ~1500 байт на Ethernet, меньше после туннелей) не влезает в один IP-пакет, поэтому IP фрагментирует её на несколько пакетов. Пересборка — всё-или-ничего: получателю нужен каждый фрагмент, чтобы восстановить оригинал, только первый фрагмент несёт UDP-заголовок, а в UDP нет ретрансмита по фрагментам. Поэтому если потерян хоть один фрагмент — а по теряющему пути каждый лишний пакет это новый шанс потери — получатель держит неполные куски, которые в итоге таймаутятся и выбрасываются, теряя всю датаграмму. Датаграмма 4 КБ — это три фрагмента, что все должны выжить, давая ей куда выше эффективную потерю, чем однопакетной 1 КБ. Фикс — держать payload под MTU, безопасная переносимая цель ~1200 байт, чтобы каждая датаграмма была одним атомарным пакетом без пересборки и без усиленной потери; нужно больше — фрагментируй и нумеруй на уровне приложения или используй TCP." },
    { q: "Когда UDP — правильный выбор вместо TCP и что ты теряешь?", a: "UDP выигрывает, когда низкая задержка и отсутствие head-of-line блокировки важнее гарантированной упорядоченной доставки. Классические совпадения: метрики/телеметрия приложения (StatsD), где отдельные точки данных одноразовы и нельзя блокировать горячий путь запроса; service discovery (mDNS/Bonjour) и gossip, где multicast даёт одному send достичь многих получателей без соединения на каждого; реалтайм-мультиплеер и голос/видео, где опоздавший пакет хуже потерянного (нужно новейшее состояние, а не ретрансмит устаревшего); и DNS, где один маленький запрос/ответ куда дешевле без рукопожатия. Теряешь ты всё, что TCP даёт бесплатно: доставка best-effort (пакеты могут теряться), нет порядка (датаграммы могут приходить вразнобой), возможны дубликаты, нет соединения или контроля потока. Если протоколу нужно что-то из этого, ты реализуешь это сам поверх — порядковые номера, ACK, ретрансмит — ровно это и делают QUIC и надёжный UDP-netcode игр, стартуя с UDP и добавляя обратно лишь реально нужные гарантии." }
  ]}
/>

<Recap lang="ru">
UDP, доступный через Node-овский `dgram`, — бессоединительная противоположность надёжному стриму `net`. Ты создаёшь сокет, `bind`-ишь порт и принимаешь целые датаграммы как события `'message'` с `rinfo`, описывающим отправителя — без рукопожатия, без сокета на клиента, без порядка и без гарантии доставки. Проблема фрейминга инвертируется: где TCP стирает границы сообщений, UDP их сохраняет, поэтому один `send()` — ровно одно `'message'`, а колбэк `send()` срабатывает на передаче ОС, а не на доставке, потому что подтверждать нечего. Датаграммы ограничены path MTU, и датаграмма больше него IP-фрагментируется на пакеты, где один потерянный фрагмент отбрасывает всё целиком — поэтому держи payload под ~1200 байт или фрагментируй и нумеруй сам. Уникальная сила UDP — групповая адресация: broadcast в подсеть и multicast вступившим членам дают одному send достичь многих получателей, чего TCP не может. Выбирай UDP, когда задержка и отсутствие head-of-line блокировки важнее надёжности — метрики, discovery, игры, голос, DNS — и принимай, что любую нужную надёжность ты строишь поверх. Теперь, когда увидишь метрики или service-discovery по TCP, сразу знаешь вопрос: нужна ли этому трафику гарантированная доставка, или он просто платит за неё по умолчанию?
</Recap>
