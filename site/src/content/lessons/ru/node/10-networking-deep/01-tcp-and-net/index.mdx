---
concepts:
- net-createserver
- socket-lifecycle
- half-open-connections
- nagle-and-nodelay
- socket-backpressure
- ref-unref
deepensInto: []
estMin: 18
lang: ru
lessonType: topic
level: middle
order: 1
prereqs: []
slug: 01-tcp-and-net
sources:
- https://nodejs.org/api/net.html
- https://nodejs.org/api/stream.html
- https://blog.cloudflare.com/the-curious-case-of-slow-downloads/
spiral: []
status: ready
summary: 'Модуль net даёт сырой TCP-стрим под http: дуплексный сокет с data/end/error/close,
  семантику half-open, батчинг Nagle, отключаемый через setNoDelay, и булев результат
  write() — сигнал backpressure на уровне сокета.'
title: 'TCP и модуль net: сокеты, жизненный цикл и backpressure'
track: node
unit: 10-networking-deep
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import SequenceDiagram from "~/components/diagram/SequenceDiagram.astro";
import Infographic from "~/components/diagram/Infographic.astro";
import nagleTradeoff from "./infographics/nagle-tradeoff.svg?raw";

<Hook>
Коллега выкатывает крошечный TCP-прокси. На localhost работает, ревью проходит, а под реальным трафиком течёт память, пока под не падает по OOM. Причина — две недостающие строки: байты клиента пайпили в апстрим-сокет, но ни разу не смотрели на результат `write()` и не повесили `'error'` на апстрим. Медленный апстрим заставлял `write()` возвращать `false` на каждом вызове; непрочитанные байты копились в безграничном внутреннем буфере. `net` отдавал правду на каждой записи — её проигнорировали.
</Hook>

<Crux>Сокет `net` — дуплексный стрим: читай через `data`/`end`, пиши, следя за булевым backpressure, и закрывай чисто по `error`/`close`.</Crux>

<Explanation>

## Сокет — это дуплексный стрим, а не почтовый ящик

Когда пропускаешь хотя бы одно из четырёх событий жизненного цикла, воспроизводишь ровно тот класс бага из хука — будь то утечка памяти, тихое падение или зомби-fd, который никогда не закрывается. Вот полная картина.

Всё, что делает `http`, лежит поверх `net`. `net.Socket` — это **дуплексный стрим**: Readable для байтов, приходящих от пира, и Writable для байтов, которые ты шлёшь. `net.createServer((socket) => { ... })` даёт по сокету на каждое принятое соединение; `net.connect(port, host)` — клиентский конец. Никакого фрейминга сообщений нет — TCP это поток байтов, поэтому одно логическое «сообщение» может прийти разбитым на несколько событий `'data'`, а две твои отправки могут слиться в один `'data'` у пира. Любой протокол поверх `net` обязан делать свой фрейминг сам (префикс длины, разделитель или парсер).

Жизненный цикл — фиксированная последовательность событий, и подключить нужно их все:

```js
const net = require("node:net");

const server = net.createServer((socket) => {
  socket.setEncoding("utf8");            // или оставь Buffer'ы
  socket.on("data",  (chunk) => { /* парсинг — может быть частичным */ });
  socket.on("end",   () => { /* пир прислал FIN, чтений больше нет */ });
  socket.on("error", (err) => { /* ECONNRESET и т.п. — ОБЯЗАН обработать */ });
  socket.on("close", (hadError) => { /* полностью разобран */ });
  socket.write("hello\n");
});

server.listen(7000);
```

Незыблемое правило: **необработанный `'error'` на сокете кидает исключение и роняет процесс.** TCP-ошибки вроде `ECONNRESET` (сигнал RST от пира — резкий обрыв соединения) не экзотика — клиент, захлопнувший крышку ноутбука, штатно их порождает — поэтому каждому сокету нужен слушатель `'error'`, иначе первый же reset положит весь сервер.

## end против close и ловушка half-open

`'end'` и `'close'` — не одно и то же событие, и путаница между ними — классический баг.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Событие</th>
        <th class="border border-border px-3 py-2 text-left">Смысл</th>
        <th class="border border-border px-3 py-2 text-left">Можно ли ещё писать?</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2"><code>'end'</code></td>
        <td class="border border-border px-3 py-2">Пир прислал FIN — <em>читаемая</em> сторона завершена</td>
        <td class="border border-border px-3 py-2">Да — твоя пишущая половина ещё открыта</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2"><code>'finish'</code></td>
        <td class="border border-border px-3 py-2">Ты вызвал <code>end()</code> — <em>пишущая</em> сторона слита</td>
        <td class="border border-border px-3 py-2">Нет — ты закрыл свою половину</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2"><code>'close'</code></td>
        <td class="border border-border px-3 py-2">Обе половины закрыты, сокет полностью освобождён</td>
        <td class="border border-border px-3 py-2">Нет — fd больше нет</td>
      </tr>
    </tbody>
  </table>
</div>

По умолчанию Node автоматически закрывает пишущую сторону при получении FIN, поэтому время жизни сокета симметрично. Но TCP по-настоящему поддерживает **half-open**: пир может перестать слать (`end`), пока ты продолжаешь слать. Передай `allowHalfOpen: true` в `createServer`/`connect`, и Node *не* будет авто-`end()`-ить твою сторону на FIN пира — полезно для протоколов запрос/ответ, где клиент сигналит «закончил слать» через FIN, но ждёт твой полный ответ. Ловушка обратная: с `allowHalfOpen: true` ты теперь сам обязан вызвать `socket.end()`, иначе half-open соединение зависает и течёт файловым дескриптором.

<Inset kind="why" lang="ru">
FIN закрывает одно направление. TCP — это два независимых симплексных стрима, склеенных вместе, поэтому «соединение закрыто» на самом деле значит «одна половина закрыта». Большинству серверов нужно симметричное закрытие (по умолчанию), но прокси или стриминговому протоколу часто надо дочитать до EOF и затем ещё слить трейлер — это и даёт `allowHalfOpen`, ценой того, что закрытие теперь на тебе.
</Inset>

## Nagle, setNoDelay и загадка 40 мс

По умолчанию TCP работает с **алгоритмом Nagle**: он придерживает маленький исходящий сегмент, пока не подтвердится (ACK) предыдущий сегмент или не накопится данных на целый пакет, чтобы не заваливать сеть крошечными пакетами. В сочетании с **delayed ACK** пира (который ждёт до ~40 мс перед подтверждением) Nagle порождает известный сбой: протокол запрос/ответ с мелкими записями застревает на ~40 мс на круг, потому что твоя сторона не шлёт маленький сегмент, пока пир не пришлёт ACK, а пир не шлёт ACK, пока не сработает его таймер delayed-ACK.

Лечится через `socket.setNoDelay(true)`, который отключает Nagle, и каждый `write()` уходит сразу. Для интерактивных, чувствительных к задержке протоколов (RPC, REPL, игра) это правильный дефолт. Для пропускной способности с множеством мелких записей оставить Nagle может быть лучше — он коалесцирует. Пойми, что у тебя: низкая задержка на мелких сообщениях → `setNoDelay(true)`; чистая пропускная способность → оставь. `socket.setKeepAlive(true, delay)` — отдельная ручка: ОС шлёт периодические keepalive-пробы на простаивающем соединении, чтобы мёртвый пир (упавший, сетевой разрыв) обнаружился, а не висел сокетом вечно.

<Infographic
  svg={nagleTradeoff}
  label="Сравнение двух режимов записи в TCP: setNoDelay(true) сливает каждую запись сразу без застревания ~40 мс на delayed-ACK, лучше для RPC/REPL/игр, но шлёт больше мелких пакетов; дефолтный Nagle коалесцирует мелкие записи в целые пакеты ради пропускной способности, но добавляет ~40 мс на круг при мелком запрос/ответе."
  caption="Тот же протокол с мелкими записями, противоположные ручки: setNoDelay покупает задержку ценой пакетов; дефолтный Nagle покупает пропускную ценой застревания ~40 мс на круг."
  hue="var(--d-infra)"
/>

## write() возвращает булево — это твой backpressure

`socket.write(chunk)` возвращает **булево**, и это самое игнорируемое значение в сетевом Node. `true` значит, что чанк ушёл в буфер отправки ядра и можно писать дальше. `false` значит, что внутренний буфер Node теперь выше high-water mark — пир (или сеть) сливает не так быстро, как ты пишешь — и надо **прекратить запись, пока не сработает событие `'drain'`**. Игнорирование `false` и запись дальше не дают ошибки; Node просто продолжает буферизовать в памяти, и против медленного потребителя этот буфер растёт безгранично, пока процесс не упадёт по OOM. Это и есть баг из хука.

```js
// Ручной backpressure: стоп на false, продолжить на 'drain'.
function sendAll(socket, chunks, done) {
  let i = 0;
  (function next() {
    while (i < chunks.length) {
      const ok = socket.write(chunks[i++]);
      if (!ok) return socket.once("drain", next);   // ждём, затем дальше
    }
    done();
  })();
}
```

На практике этот цикл пишут редко: `pipeline(source, socket)` (или `source.pipe(socket)`) подключает backpressure за тебя, ставя источник на паузу при `false` и возобновляя на `'drain'`. Урок: либо доверь управление пайплайну стримов, либо честно соблюдай булево руками — но никогда не пиши «выстрелил-и-забыл» в сокет, чьим drain ты не управляешь.

<TradeoffMatrix
  id="10-tcp-and-net-tradeoff-1"
  lessonSlug="01-tcp-and-net"
  lang="ru"
  prompt="Ты проксируешь байты быстрого клиента в более медленный апстрим-сокет. Как двигать данные?"
  options={[
    { name: "pipeline(clientSocket, upstreamSocket) и обработка ошибки в колбэке", summary: "Backpressure бесплатно.", correct: true, justification: "pipeline подключает backpressure из конца в конец: когда upstream.write() вернул false, он ставит клиентский сокет на паузу и возобновляет на drain, так что память остаётся ограниченной, как бы медленен ни был апстрим. Он же прокидывает ошибки и уничтожает оба стрима при сбое, закрывая дыру ECONNRESET/EPIPE. Это сеньорный дефолт для перекачки байтов между сокетами." },
    { name: "clientSocket.on('data', c => upstreamSocket.write(c)) без проверки результата", summary: "Игнор булева.", justification: "Это утечка из хука. write() вернёт false, как только апстрим отстанет, но ты его не читаешь, поэтому непрочитанные чанки копятся в безграничном внутреннем буфере до OOM. «Выстрелил-и-забыл» в сокет, чьим drain ты не управляешь, — каноничный баг памяти в Node." },
    { name: "Прочитать всё в один большой Buffer, затем записать разом", summary: "Буфер, потом слив.", justification: "Буферизация всего стрима убивает стриминг: большая или безграничная передача держит весь payload в памяти, и тебе всё равно надо обработать backpressure на одной гигантской записи. Плюс задержка — ничего не движется, пока источник не завершится. Годится лишь для заведомо крошечных данных." },
    { name: "upstreamSocket.setNoDelay(true) и наивный data-обработчик", summary: "Отключить Nagle.", justification: "setNoDelay меняет, когда сегменты сливаются (задержка), а не соблюдаешь ли ты backpressure (память). Утечка безграничного буфера никак не затронута — ты покрутил не ту ручку, и OOM всё равно случится." },
  ]}
/>

## ref и unref: должен ли этот сокет держать процесс живым?

Event loop Node жив, пока есть активный handle — а открытый сокет это handle. `socket.unref()` говорит циклу «не считай меня при решении, выходить ли»; `socket.ref()` отменяет это. Кейс — фоновое или опциональное соединение: метрик-сокет, health-check проба или дебаг-листенер, который сам по себе не должен держать CLI-процесс живым после завершения настоящей работы. `server.unref()` делает то же для слушающего сервера. По умолчанию — `ref`'нуто (нормальный сервер должен держать процесс), поэтому тянись к `unref()` только на вспомогательных соединениях, которые не должны пинить процесс открытым.

<Quiz
  id="10-tcp-and-net-quiz-1"
  lessonSlug="01-tcp-and-net"
  lang="ru"
  question="Твой TCP-сервер периодически падает в продакшене с непойманным ECONNRESET. Что чинить?"
  choices={[
    { label: "Повесить слушатель 'error' на каждый сокет — необработанный 'error' сокета кидает исключение и роняет процесс; ECONNRESET это штатный reset пира, а не баг, который надо предотвращать", correct: true },
    { label: "Обернуть server.listen в try/catch", misconception: "try/catch вокруг listen ловит только синхронную ошибку bind. 'error' сокета срабатывает асинхронно на объекте сокета позже; ловит его только слушатель 'error' на уровне сокета." },
    { label: "Вызвать socket.setNoDelay(true), чтобы прекратить reset'ы", misconception: "setNoDelay отключает батчинг Nagle; к reset'ам соединения он отношения не имеет. ECONNRESET идёт от резкого закрытия пиром, что надо обрабатывать, а не предотвращать." },
    { label: "Увеличить server.maxConnections", misconception: "maxConnections ограничивает число одновременных сокетов; он не мешает существующему соединению быть сброшенным и не даёт недостающий обработчик ошибки." },
  ]}
/>

<Quiz
  id="10-tcp-and-net-quiz-2"
  lessonSlug="01-tcp-and-net"
  lang="ru"
  question="Маленький протокол запрос/ответ поверх net добавляет ~40 мс задержки на круг на простаивающих линках. Вероятная причина?"
  choices={[
    { label: "Алгоритм Nagle во взаимодействии с delayed ACK пира — отключи его через socket.setNoDelay(true) для этого чувствительного к задержке протокола с мелкими записями", correct: true },
    { label: "Медленное разрешение DNS", misconception: "DNS происходит один раз при connect, а не на каждый круг. Стабильные ~40 мс на сообщение на установленном соединении — подпись Nagle/delayed-ACK, а не разрешения имён." },
    { label: "Заблокирован event loop", misconception: "Заблокированный event loop добавляет переменные, часто большие задержки на всю работу, а не чистые ~40 мс только на сетевых кругах. Константа 40 мс — это таймер delayed-ACK." },
    { label: "Срабатывают keepalive-пробы TCP", misconception: "Keepalive-пробы срабатывают на простаивающих соединениях с интервалами в секунды-минуты для обнаружения мёртвых пиров; они не добавляют 40 мс к активной задержке запрос/ответ." },
  ]}
/>

</Explanation>

<SequenceDiagram
  label="Жизненный цикл TCP-сокета от соединения до half-open закрытия с событиями data, end и close на net.Socket"
  hue="var(--d-network)"
  actors={["Клиент", "Сервер"]}
  messages={[
    { from: "Клиент", to: "Сервер", label: "connect (SYN)" },
    { from: "Сервер", to: "Клиент", label: "'connection' cb" },
    { from: "Клиент", to: "Сервер", label: "write -> 'data'" },
    { from: "Сервер", to: "Клиент", label: "write -> 'data'" },
    { from: "Клиент", to: "Сервер", label: "end() -> FIN" },
    { from: "Сервер", to: "Клиент", label: "'end' (read done)" },
    { from: "Сервер", to: "Клиент", label: "end() -> FIN" },
    { from: "Клиент", to: "Сервер", label: "'close' обе стороны" },
  ]}
  caption="Каждая сторона закрывает свою пишущую половину через FIN независимо. allowHalfOpen позволяет серверу писать после FIN клиента — ценой собственного end()."
/>

<KeyTakeaway>Сокет `net` — дуплексный поток байтов без фрейминга: обрабатывай `data`/`end`/`error`/`close`, следи за булевым `write()` ради backpressure, `setNoDelay` ради низкой задержки и никогда не оставляй `'error'` без обработки.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="10-tcp-and-net-retrieval"
  lang="ru"
  questions={[
    { q: "Почему write() возвращает булево и что делать, когда оно false?", a: "socket.write() возвращает true, когда чанк принят в буфер отправки ядра и можно писать дальше, и false, когда внутренний writable-буфер Node пересёк high-water mark — то есть пир или сеть сливают медленнее, чем ты производишь. False — сигнал backpressure на уровне сокета: надо прекратить запись и ждать события 'drain' перед продолжением, потому что Node не блокирует и не кидает ошибку на переполненном буфере — он просто продолжает буферизовать в памяти. Против медленного потребителя игнорирование false и запись дальше растят этот буфер безгранично, пока процессу не кончится память. На практике ты даёшь pipeline()/pipe() соблюдать булево автоматически (он ставит источник на паузу на false и возобновляет на drain) или реализуешь цикл стоп-на-false, продолжить-на-drain руками. Единственное, чего нельзя — писать «выстрелил-и-забыл» в сокет, чьим drain ты не управляешь." },
    { q: "Что такое half-open TCP-соединение и что меняет allowHalfOpen?", a: "TCP — это два независимых симплексных стрима, поэтому каждая сторона закрывает своё пишущее направление через FIN. Half-open соединение — это когда одна сторона прислала FIN (перестала слать), пока другая ещё шлёт. В Node по умолчанию при получении FIN сокет эмитит 'end' и затем автоматически вызывает end() и на твоей пишущей половине, давая симметричное закрытие. Передача allowHalfOpen: true в createServer или connect отключает это авто-end: на FIN пира ты получаешь 'end', но твоя пишущая половина остаётся открытой, и можно писать дальше — полезно для протоколов, где клиент сигналит «закончил слать», но ждёт полный ответ, или для прокси, которому надо дочитать до EOF и затем слить трейлер. Цена — закрытие теперь на тебе: надо самому вызвать socket.end(), иначе half-open сокет зависает и течёт файловым дескриптором." },
  ]}
/>

<Recap lang="ru">
Модуль `net` — это сырой TCP-слой под `http`: `net.Socket` — дуплексный стрим без фрейминга сообщений, поэтому ты парсишь чанки `'data'`, которые могут быть частичными, и строишь свой фрейминг. Подключай весь жизненный цикл — `'data'`, `'end'`, `'error'`, `'close'` — и никогда не пропускай `'error'`, потому что необработанная ошибка сокета кидает исключение и роняет процесс, а reset'ы вроде `ECONNRESET` — рутина. `'end'` значит, что пришёл FIN пира (чтение завершено), пока твоя пишущая половина может быть ещё открыта; `allowHalfOpen` держит её открытой ценой того, что `end()` теперь на тебе. Настраивай транспорт осознанно: `setNoDelay(true)` отключает Nagle ради низкой задержки на мелких записях, `setKeepAlive` даёт ОС обнаруживать мёртвых пиров. Главное — `write()` возвращает булево: `false` это backpressure, сигнал остановиться до `'drain'`, и игнор его — каноничный OOM безграничного буфера. Доверяй управление backpressure `pipeline()`, когда можешь, и используй `unref()` только на вспомогательных сокетах, что не должны держать процесс живым. Теперь, когда встретишь OOM в прокси или сервер, который «просто висит» под нагрузкой, — проверь первым делом, смотрит ли код на результат `write()` и есть ли на каждом сокете обработчик `'error'`.
</Recap>
