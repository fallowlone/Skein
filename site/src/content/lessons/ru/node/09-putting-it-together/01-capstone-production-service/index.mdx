---
concepts:
- error-boundary
- graceful-shutdown
- event-loop-discipline
- input-validation
- twelve-factor-config
- observability
deepensInto: []
estMin: 20
lang: ru
lessonType: topic
level: senior
order: 1
prereqs: []
slug: 01-capstone-production-service
sources:
- https://12factor.net/
- https://github.com/goldbergyoni/nodebestpractices
- https://nodejs.org/en/learn/getting-started/nodejs-the-difference-between-development-and-production
spiral: []
status: ready
summary: 'Production Node-сервис — это весь трек, собранный разом: типизированные
  ошибки за одной границей, незаблокированный loop, упорядоченный middleware с таймаутами
  и graceful shutdown, валидированный вход, env-конфиг и наблюдаемость. Каждая пропущенная
  забота — это инцидент.'
title: 'Капстоун: проектируем и закаляем production Node-сервис'
track: node
unit: 09-putting-it-together
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";
import Infographic from "~/components/diagram/Infographic.astro";
import skippedLayerIncident from "./infographics/skipped-layer-incident.svg?raw";

<Hook>
Прототип работал на ноутбуке, поэтому его выкатили. Первый деплой под нагрузкой написал `200 OK` половине запросов оформления заказа в полёте — и тут же их потерял: под пришёл `SIGTERM`, контейнер запускал приложение как PID 1 без обработчика сигнала, и Kubernetes жёстко прибил его через 30 секунд прямо посреди транзакции. Следующий инцидент — хвостовая задержка: один эндпоинт делал `JSON.parse` над телом в 4 МБ прямо в пути запроса и блокировал event loop, так что все остальные запросы вставали в очередь за ним, и p99 прыгнул с 40 мс до 9 секунд. Потом подделанная query-строка дошла до непараметризованной SQL-строки. Ничего экзотического. Каждое — это один юнит этого трека, который прототип пропустил, и каждое стало production-инцидентом в тот момент, когда пришёл реальный трафик.
</Hook>

<Crux>Production-готовность = весь трек собран: одна граница ошибок, незаблокированный loop, упорядоченный middleware, graceful shutdown.</Crux>

<Explanation>

## Чек-лист и есть архитектура

К концу этого урока ты сможешь проследить один запрос через полностью закалённый сервис и назвать инцидент, который порождает каждый пропущенный слой — так что в следующий раз, когда pull request срежет угол, ты распознаешь это до того, как оно попадёт в production.

Путь от прототипа к production — это не переписывание, а применение каждого юнита этого трека к одному маленькому HTTP-сервису и отказ хоть один из них пропустить. Полезная линза — чек-лист жизненного цикла запроса: для каждой сквозной заботы назови *что ты делаешь* и конкретный сигнал *готово, когда*. Пропусти строку — и получишь не сервис поменьше, а будущий инцидент с именем. Манифест `12factor` и репозиторий node-best-practices — это под капотом ровно этот список: конфиг в окружении, зависимости объявлены и залочены, процессы одноразовые, логи как потоки событий.

<Infographic
  svg={skippedLayerIncident}
  label="Список, сопоставляющий каждую пропущенную сквозную заботу с её именованным production-инцидентом: нет границы ошибок — текут стеки, заблокированный event loop гонит p99 с 40 мс до 9 с, нет graceful shutdown — теряются запросы в полёте на каждом деплое, невалидированный вход доходит до непараметризованной SQL-строки, а конфиг, зашитый в образ, блокирует перенос и утекает секретами."
  caption="Пропусти строку — и получишь не сервис поменьше, а конкретную аварию с именем."
  hue="var(--d-infra)"
/>

Сеньорский ход — относиться к этому как к слоям, которые компонуются, а не к бэклогу фич. Ошибки, event loop, порядок middleware, shutdown, валидация, конфиг и наблюдаемость — не независимые галочки, они сцеплены. Graceful shutdown бесполезен, если балансировщик так и не узнал, что ты сливаешься (это readiness-проба). Граница ошибок бесполезна, если заблокированный loop означает, что граница вообще не выполнится. Ниже — чек-лист готовности, который защищает весь этот урок.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Забота (юнит)</th>
        <th class="border border-border px-3 py-2 text-left">Что ты делаешь</th>
        <th class="border border-border px-3 py-2 text-left">Готово, когда</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">Ошибки (03)</td>
        <td class="border border-border px-3 py-2">Типизированные доменные ошибки; одна граница мапит тип→статус; сеть процесса на uncaught</td>
        <td class="border border-border px-3 py-2">Клиенты видят безопасный JSON + коды; в логах полные стеки; сюрприз роняет, а не портит</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Event loop (04)</td>
        <td class="border border-border px-3 py-2">Никогда не блокируй; CPU — в worker pool; следи за loop lag; масштабируй по ядрам</td>
        <td class="border border-border px-3 py-2">p99 ровный под нагрузкой; loop lag &lt; ~50 мс; профиль в одну команду</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">HTTP (05)</td>
        <td class="border border-border px-3 py-2">Упорядоченный middleware; таймауты запроса + заголовков + тела; graceful <code>SIGTERM</code></td>
        <td class="border border-border px-3 py-2">Ни один запрос не бежит без границы; деплой сливает работу без потерянных запросов</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Тесты (06)</td>
        <td class="border border-border px-3 py-2">Юнит-тесты логики; интеграционные (supertest + реальная тестовая БД) для собранного API</td>
        <td class="border border-border px-3 py-2">CI блокирует мерж на зелёном наборе, который гоняет реальные маршруты, а не моки</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Безопасность (07)</td>
        <td class="border border-border px-3 py-2">Валидируй вход на границе; параметризованные запросы; секреты из env; <code>npm ci</code> + audit</td>
        <td class="border border-border px-3 py-2">Невалидированный вход не доходит до логики; секрета нет в git; залоченные, проверенные зависимости</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Упаковка (08)</td>
        <td class="border border-border px-3 py-2">Многостадийный slim/distroless образ, non-root, exec-форма <code>CMD</code> + init, healthcheck</td>
        <td class="border border-border px-3 py-2">Маленький образ бежит non-root и пробрасывает сигналы; healthcheck отражает readiness</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Конфиг и ops (12-factor)</td>
        <td class="border border-border px-3 py-2">Конфиг через env; эндпоинты liveness + readiness; эндпоинт метрик; SLO</td>
        <td class="border border-border px-3 py-2">Один образ бежит во всех env через конфиг; есть пробы + метрики + трейсы; SLO отслеживаются</td>
      </tr>
    </tbody>
  </table>
</div>

## Жизненный цикл запроса, закалённый из конца в конец

Проследи один запрос — и увидишь, как каждый юнит делает свою работу. Ingress или балансировщик маршрутизирует в процесс, у HTTP-сервера которого уже выставлены таймауты — `server.requestTimeout`, `headersTimeout` и таймаут обработчика на маршрут, — так что ни один клиент не удержит соединение открытым вечно (юнит 05). Запрос входит в *упорядоченный* стек middleware, и порядок несущий: сначала логирование (чтобы логировать даже отклонённые запросы), затем парсер тела с лимитом размера (чтобы тело в 4 МБ не разорвало память и не заблокировало парс), затем auth, затем валидация по схеме, затем обработчик. Обработчик зовёт тонкий слой сервисов, тот — репозиторий, выдающий **параметризованные** запросы (никакой конкатенации строк), а доменный сбой становится типизированной ошибкой (юнит 07, юнит 03).

Сквозь всё это продёрнут контекст запроса. Структурный логгер `pino` несёт request/trace ID через `AsyncLocalStorage` (встроенный Node.js API для хранения данных, изолированных по async-цепочке), так что каждая строка лога из глубины стека скоррелирована со своим запросом без ручной передачи ID. `diagnostics_channel` (встроенный pub/sub-канал Node.js для низкоуровневой инструментации без жёсткой связности) даёт публиковать точки инструментирования (старт/конец запроса к БД, попадание в кэш), на которые экспортёры подписываются без связности. В самом конце стоит единая **граница ошибок**: middleware обработки ошибок, которая мапит *тип* ошибки в HTTP-статус (`ValidationError`→400, `NotFoundError`→404, всё неизвестное→500), отправляет безопасное JSON-тело без стека и логирует полный объект ошибки с цепочкой `cause`. Это стратегия юнита 03, выраженная как ровно одно место в проводке.

<TradeoffMatrix
  id="09-capstone-tradeoff-1"
  lessonSlug="01-capstone-production-service"
  lang="ru"
  prompt="CPU-bound шаг (ресайз картинки, дорогой bcrypt, большой JSON-трансформ) сидит внутри горячего обработчика и под нагрузкой раздувает p99. Куда ты его положишь?"
  options={[
    { name: "Вынести в пул worker_threads; обработчик ждёт сообщение обратно", summary: "Освободить loop.", correct: true, justification: "Это сеньорский фикс. CPU-работа бежит вне главного потока, так что event loop остаётся свободным принимать и диспетчеризовать другие запросы; обработчик просто ждёт сообщение с результатом. p99 перестаёт обваливаться, потому что один дорогой вызов больше не голодит все параллельные запросы. Ограниченный пул ещё и кэпит, сколько CPU машина тратит на это, давая backpressure вместо лавины потоков." },
    { name: "Оставить inline, но обернуть в Promise / setImmediate", summary: "Всё равно блокирует.", justification: "Promise или setImmediate лишь перепланируют, КОГДА работа стартует, — они не заставляют синхронную CPU-работу уступать. Как только bcrypt/ресайз запустился, он держит единственный поток до конца, блокируя loop ровно как раньше. Обёртка в микротаск прячет проблему в профайлере, но не чинит хвостовую задержку." },
    { name: "Масштабировать до большего числа подов/реплик и оставить inline", summary: "Маскирует, дорого.", justification: "Больше реплик поднимают суммарную пропускную способность, но каждый инстанс всё равно блокирует свой loop на каждом таком запросе, так что p99 внутри инстанса остаётся плохим, и ты платишь за мощности, чтобы замазать дефект на запрос. Горизонтальный скейл — для реальной нагрузки, а не для сокрытия блокирующего вызова: сперва почини блок, потом масштабируй починенный сервис." },
    { name: "Кэшировать результат и надеяться на попадания", summary: "Частично, хрупко.", justification: "Кэш помогает только для повторных идентичных входов и ничего не делает для холодного пути или уникальных входов, которые всё равно блокируют loop. Это полезная оптимизация поверх worker pool, но как основной фикс он оставляет каждый промах кэша раздувающим p99 — ты снизил частоту, а не убрал опасность." },
  ]}
/>

## Bootstrap и shutdown: проводка, решающая аптайм

Два куска сантехники решают, будут деплои невидимыми или разрушительными: bootstrap, который ставит страховочную сеть до того, как что-либо начнёт обслуживать, и shutdown, который сливает работу до выхода. Когда смотришь на `index.js` нового сервиса, спроси себя: сеть процесса установлена *до* `listen`, а `SIGTERM` обработан *до* первого возможного запроса? На `SIGTERM` ты не захлопываешь сервер — ты переключаешь readiness в *не готов* (чтобы балансировщик перестал слать новые запросы), прекращаешь принимать соединения, даёшь запросам в полёте дойти, закрываешь пул БД и взводишь жёсткий таймаут, чтобы зависший запрос не блокировал shutdown вечно. Пропуск этого — инцидент с потерянными заказами из хука.

```js
import { createServer } from "node:http";
import { app } from "./app.js";          // упорядоченный middleware + граница ошибок
import { logger } from "./logger.js";
import { closePool } from "./db.js";

let ready = false;
const server = createServer(app);
server.requestTimeout = 30_000;          // юнит 05: ограничь каждый запрос
server.headersTimeout = 10_000;

// юнит 03: сеть процесса — зафиксируй, потом умри; супервизор перезапустит чистым
process.on("uncaughtException", (err) => { logger.fatal({ err }); shutdown(1); });
process.on("unhandledRejection", (err) => { logger.fatal({ err }); shutdown(1); });

async function shutdown(code = 0) {
  ready = false;                         // readiness переключился → LB сливает нас
  server.close(async () => {             // прекрати принимать; доделай в полёте
    await closePool();
    process.exit(code);
  });
  setTimeout(() => process.exit(code), 10_000).unref(); // жёсткий кэп
}
process.on("SIGTERM", () => shutdown(0));

server.listen(3000, () => { ready = true; logger.info("listening"); });
// liveness = процесс жив; readiness = флаг `ready` (одноразовость по 12-factor)
```

<Inset kind="why" lang="ru">
Почему переключение readiness должно происходить *до* `server.close`, а не после? Потому что балансировщик узнаёт, что ты уходишь, только опросом readiness-эндпоинта. Если закрыть сервер первым, новые запросы, которые LB ещё не перестал маршрутизировать, получат ошибку соединения вместо чистого слива. Переключи readiness, дай интервалу пробы момент это заметить, *затем* закрывай — этот порядок и есть разница между деплоем без простоя и всплеском 502 на каждом раскате.
</Inset>

<Quiz
  id="09-capstone-quiz-1"
  lessonSlug="01-capstone-production-service"
  lang="ru"
  question="Rolling-деплой теряет горстку запросов в полёте с ошибками сброса соединения на каждом раскате. Какая пропущенная забота виновата?"
  choices={[
    { label: "Нет graceful shutdown: процесс выходит на SIGTERM без переключения readiness, слива запросов в полёте и закрытия сервера — поэтому оркестратор жёстко прибивает его посреди запроса", correct: true },
    { label: "Отсутствует валидация входа на границе", misconception: "Валидация решает, дойдёт ли плохой вход до твоей логики; к запросам, потерянным во время деплоя, она отношения не имеет. Сигнатура «сброс на раскате» прямо указывает на обработку shutdown, а не на тело запроса." },
    { label: "Event loop заблокирован CPU-работой", misconception: "Заблокированный loop проявляется как всплески хвостовой задержки под нагрузкой, а не как сбросы, возникающие только в момент раската. Тайминг — каждый деплой — выдаёт, что это проблема shutdown." },
    { label: "Логи не структурированы, поэтому ошибок не видно", misconception: "Неструктурированные логи усложняют чтение инцидента, но не вызывают потерю запросов. Запросы теряются, потому что никто не слил их до смерти процесса." },
  ]}
/>

<Quiz
  id="09-capstone-quiz-2"
  lessonSlug="01-capstone-production-service"
  lang="ru"
  question="Ты хочешь, чтобы каждая строка лога из глубины стека несла один и тот же request/trace ID без продёргивания его через каждую сигнатуру функции. За чем тянешься?"
  choices={[
    { label: "AsyncLocalStorage: задай trace ID один раз на запрос в раннем middleware и читай его из логгера где угодно, так как он сохраняется через await внутри async-контекста этого запроса", correct: true },
    { label: "Глобальная переменная уровня модуля с текущим request ID", misconception: "Одна глобальная переменная общая для всех параллельных запросов, так что при любой реальной конкуренции запрос B перезаписывает ID запроса A, и корреляция врёт. AsyncLocalStorage существует именно потому, что глобал не умеет изолировать контекст на запрос через await." },
    { label: "Передавать ID аргументом в каждую функцию цепочки", misconception: "Это работает, но это ровно то ручное продёргивание, которого ты избегаешь; оно загрязняет каждую сигнатуру и ломается, как только один библиотечный вызов в середине теряет аргумент. AsyncLocalStorage несёт его неявно." },
    { label: "Хранить его на объекте process", misconception: "process — один глобальный объект, общий для всех запросов, так что у него та же проблема коллизий между запросами, что и у глобала модуля. Контексту на запрос нужна изоляция async-контекста, которую и даёт AsyncLocalStorage." },
  ]}
/>

<DragOrder
  id="09-capstone-drag-1"
  lessonSlug="01-capstone-production-service"
  lang="ru"
  prompt="Расставь пайплайн middleware/обработчика для одного входящего запроса — от края к данным и обратно через границу:"
  items={[
    { id: "a", label: "Логирование + назначение request-ID (чтобы даже отклонённые запросы попали в лог)" },
    { id: "b", label: "Парсер тела с лимитом размера (отклонить негабаритные payload рано)" },
    { id: "c", label: "Аутентификация / авторизация" },
    { id: "d", label: "Валидация распарсенного входа по схеме" },
    { id: "e", label: "Обработчик маршрута → сервис → репозиторий (параметризованный запрос)" },
    { id: "f", label: "Граница ошибок мапит любой брошенный тип → статус + безопасное тело" },
  ]}
  correctOrder={["a", "b", "c", "d", "e", "f"]}
/>

</Explanation>

<FlowDiagram
  label="Один закалённый запрос, текущий через production-сервис от края к данным"
  hue="var(--d-systems)"
  perRow={3}
  nodes={[
    { id: "lb", label: "LB / ingress", sub: "TLS, маршрут" },
    { id: "srv", label: "HTTP-сервер", sub: "таймауты" },
    { id: "mw", label: "middleware", sub: "log→auth→valid" },
    { id: "h", label: "обработчик", sub: "тонкий" },
    { id: "svc", label: "сервис + репо", sub: "param-запрос" },
    { id: "db", label: "база данных", sub: "данные" },
  ]}
  edges={[
    { from: "lb", to: "srv" },
    { from: "srv", to: "mw", label: "по порядку" },
    { from: "mw", to: "h", label: "валидно" },
    { from: "h", to: "svc" },
    { from: "svc", to: "db", label: "параметризовано" },
  ]}
  caption="Сквозное и невидимое в линии выше: trace-контекст AsyncLocalStorage, помечающий каждый лог, единая граница ошибок, мапящая брошенные типы в коды статуса, и канал метрик/diagnostics_channel — плюс путь SIGTERM, который переключает readiness и сливает ровно этот пайплайн до выхода."
/>

<KeyTakeaway>Production-готовность собирает весь трек: ошибки за одной границей, незаблокированный loop, упорядоченный middleware, таймауты, graceful shutdown, валидация, env-конфиг, наблюдаемость. Каждая пропущенная строка — авария.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="09-capstone-retrieval"
  lang="ru"
  questions={[
    { q: "Пройди последовательность shutdown на SIGTERM и объясни, почему порядок важен для деплоя без простоя.", a: "На SIGTERM: (1) переключи флаг readiness в не-готов, чтобы readiness-проба упала и балансировщик перестал маршрутизировать НОВЫЕ запросы на этот инстанс; (2) подожди интервал пробы, чтобы LB действительно заметил; (3) вызови server.close(), чтобы прекратить принимать соединения, дав запросам в полёте доделаться; (4) закрой пул БД и прочие ресурсы; (5) взведи жёсткий таймаут с unref (например, 10 с), чтобы зависший запрос не блокировал shutdown вечно, затем process.exit. Порядок несущий: если закрыть сервер до переключения readiness, запросы, которые LB ещё не перестал слать, упрутся в закрытый сокет и получат сброс соединения — ровно инцидент с потерянными заказами. Переключение readiness первым даёт LB слить тебя чисто; закрытие первым превращает каждый раскат во всплеск 502. Liveness говорит «процесс жив» (не роняй её во время слива, иначе тебя прибьют раньше); readiness говорит «шли мне трафик» — её ты и переключаешь." },
    { q: "Каждая строка чек-листа готовности мапится в конкретный production-инцидент, если её пропустить. Назови инцидент для: нет границы ошибок, заблокированный event loop, невалидированный вход и root-контейнер, игнорирующий SIGTERM.", a: "Нет границы ошибок → незамапленные ошибки протекают клиенту сырыми стеками и внутренностями БД (раскрытие информации), а неожиданные throw роняют запрос или процесс вместо чистого 500. Заблокированный event loop → обвал хвостовой задержки: один синхронный CPU-вызов (parse, ресайз, bcrypt) на единственном потоке ставит все параллельные запросы в очередь за ним, так что p99 прыгает с десятков мс до секунд даже при скромной нагрузке. Невалидированный вход → инъекция и порча: подделанное тело или query-строка доходят до логики и непараметризованного запроса, открывая SQL-инъекцию или невалидное состояние. Root-контейнер, игнорирующий SIGTERM → жёсткие убийства: PID 1 без обработчика сигнала (shell-форма CMD, нет init) никогда не получает и не пробрасывает SIGTERM, так что оркестратор делает SIGKILL после grace-периода, теряя работу в полёте на каждом деплое. Паттерн и есть урок: каждая пропущенная забота — не «меньше кода», а заранее забронированный простой." },
  ]}
/>

<Recap lang="ru">
Production Node-сервис — это весь этот трек, собранный в один маленький HTTP-сервис и защищённый чек-листом готовности, где каждая строка мапится в конкретный инцидент, если её пропустить. Ошибки (юнит 03): типизированные доменные ошибки распространяются с `{ cause }`, единая граница ошибок мапит тип→статус и логирует полный объект, а сеть процесса на `uncaughtException`/`unhandledRejection` фиксирует и затем выходит, чтобы супервизор перезапустил чистым. Event loop (юнит 04) остаётся свободным — CPU-работа выносится в worker pool, loop lag мониторится, а CPU-профиль или heap snapshot в одну команду, когда задержка деградирует. HTTP (юнит 05) — это фреймворк с *упорядоченным* стеком middleware — логирование → парсер-тела-с-лимитом → auth → валидация → обработчик → граница ошибок — с таймаутами запроса, заголовков и тела и путём graceful `SIGTERM`, который переключает readiness, сливает, закрывает и жёстко кэпит. Тесты (юнит 06) блокируют CI на юнит-тестах плюс интеграционных против реальной тестовой БД. Безопасность (юнит 07) валидирует весь вход на границе, использует параметризованные запросы, грузит секреты из окружения и гоняет `npm ci` + audit с залоченным lockfile. Упаковка (юнит 08) поставляет многостадийный slim/distroless образ, бегущий non-root с exec-формой `CMD` + init, чтобы сигналы доходили до процесса. А конфиг/ops следует `12factor`: конфиг через env, пробы liveness и readiness, эндпоинт метрик, логи/метрики/трейсы и SLO. Вся суть: production-готовность — это не лишние фичи, прикрученные позже, а сцепление этих слоёв. Теперь, когда встречаешь сервис без `SIGTERM`-обработчика, с bcrypt inline или без лимита размера тела, — ты уже знаешь название инцидента, который этот пропуск забронировал.
</Recap>
