---
concepts:
- module-formats
- exports-map
- dual-package-hazard
- tree-shaking
- cli-bin
- publish-hygiene
deepensInto: []
estMin: 18
lang: ru
lessonType: topic
level: middle
order: 1
prereqs: []
slug: 01-bundling-and-publishing
sources:
- https://nodejs.org/api/packages.html
- https://nodejs.org/api/packages.html#exports
- https://docs.npmjs.com/cli/v10/configuring-npm/package-json
spiral: []
status: ready
summary: 'Публикация — это проектирование package.json. Карта exports — современная точка входа: выбирает ESM/CJS/types по условию и инкапсулирует внутренности. Dual-publish рискует dual-package hazard; ESM-only часто проще.'
title: 'Бандлинг и публикация: ESM, CJS и exports'
track: node
unit: 08-packaging-and-deploy
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
Команда библиотеки выпустила `2.0`, чтобы починить утечку памяти. Через час CI крупного потребителя стал красным: его бандлер всё это время делал deep-import `their-lib/dist/internal/cache.js`, чтобы пропатчить класс, а рефакторинг переместил этот файл. Авторы библиотеки никогда не публиковали карту `"exports"`, так что каждый файл на диске случайно стал публичным API — и «приватное» переименование превратилось в ломающее изменение для тысяч установок. Фикс был одним полем в `package.json`. Урок: то, что ты публикуешь, — это не твоё дерево исходников, а поверхность, которую объявляет твой `package.json`, и если ты её не объявил, то весь `dist/` и есть контракт.
</Hook>

<Crux>То, что ты публикуешь, задаёт `package.json`: карта `exports` выбирает ESM/CJS/types по условию и инкапсулирует всё остальное.</Crux>

<Explanation>

## Два формата модулей и как Node выбирает

Node запускает твой код в одном из двух форматов, и выбор делается *до* того, как разрешится любой импорт. Пакет — **ESM**, если в его `package.json` есть `"type": "module"` (или файл оканчивается на `.mjs`); иначе это **CommonJS** (или `.cjs`). Это единственное поле меняет всё ниже по течению: ESM использует статические `import`/`export` и top-level `await`; CJS — `require`/`module.exports`. Ты не можешь `require()` ESM-only пакет из CJS без `await import()`, а ESM-потребители могут импортировать CJS-пакет, но чисто получают только его default-экспорт.

Трудная задача — публикация для *обоих* видов потребителя, **dual package**. Соблазнительный ответ — собрать `dist/esm/` и `dist/cjs/` сборки одного и того же кода. Это работает, пока не перестаёт: у потребителя, чей граф зависимостей тянет твою CJS-копию по одному пути и ESM-копию по другому, теперь **загружены сразу два разных экземпляра модуля**. Две копии означают две идентичности класса, так что `instanceof` ломается через границу, а любое состояние уровня модуля (кэш, реестр, синглтон) тихо дублируется. Это **dual-package hazard**, и его действительно трудно полностью избежать. Для большинства новых библиотек прагматичный ответ — **выпускать ESM-only** (Node поддерживает ESM в проде уже годы) — или взять инструмент сборки, который эмитит оба формата, *и* держать всю общую идентичность/состояние в едином внутреннем CJS-ядре, которое обе обёртки реэкспортируют.

```bash
# ESM-only — самый простой контракт: один формат, без hazard
# package.json
{
  "type": "module",
  "exports": "./dist/index.js"
}
```

## Карта exports: точки входа и инкапсуляция

Поле `"exports"` — современная точка входа, и оно **вытесняет `"main"`**. Оно делает две работы сразу. Первая — **условное разрешение**: ты мапишь один и тот же спецификатор на разные файлы в зависимости от того, *как* он загружается — `"import"` для ESM, `"require"` для CJS, `"types"` для TypeScript и `"default"` как запасной вариант. Порядок важен; Node читает условия сверху вниз и берёт первое совпадение, так что `"types"` и самые специфичные условия идут первыми, `"default"` — последним. Вторая — **subpath-экспорты**: `"./feature"` открывает глубокую точку входа, не раскрывая раскладку папок.

То, что сеньоров волнует больше всего, — **инкапсуляция**. Как только ты определил `"exports"`, *импортируемы только перечисленные subpath*. Потребитель больше не может дотянуться до `your-pkg/dist/internal/secret.js` — такой импорт бросает `ERR_PACKAGE_PATH_NOT_EXPORTED`. Это поле спасло бы команду из хука: с картой `exports` внутренний файл никогда не был частью контракта, так что его перемещение бесплатно. Держи `"main"` как фолбэк для древнего тулинга, игнорирующего `exports`, и **всегда давай условие `"types"`**, чтобы TypeScript-потребители получали настоящие типы вместо `any`.

```jsonc
{
  "name": "their-lib",
  "type": "module",
  "main": "./dist/index.cjs",        // фолбэк для старых инструментов
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",  // первым: это читает TS
      "import": "./dist/index.js",   // ESM-потребители
      "require": "./dist/index.cjs", // CJS-потребители
      "default": "./dist/index.js"   // фолбэк на крайний случай
    },
    "./feature": "./dist/feature.js" // явный subpath; больше ничего не достать
  }
}
```

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">поле package.json</th>
        <th class="border border-border px-3 py-2 text-left">Что задаёт</th>
        <th class="border border-border px-3 py-2 text-left">Пропустишь — и…</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2"><code>type</code></td>
        <td class="border border-border px-3 py-2">ESM (<code>module</code>) vs CJS для <code>.js</code></td>
        <td class="border border-border px-3 py-2">по умолчанию CJS; ESM-синтаксис падает</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2"><code>exports</code></td>
        <td class="border border-border px-3 py-2">точки входа + условия + инкапсуляция</td>
        <td class="border border-border px-3 py-2">весь <code>dist/</code> — публичный API</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2"><code>main</code></td>
        <td class="border border-border px-3 py-2">легаси-вход; фолбэк, когда <code>exports</code> игнорят</td>
        <td class="border border-border px-3 py-2">старые тулзы тебя не разрешат</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2"><code>types</code></td>
        <td class="border border-border px-3 py-2">TS-декларации (и условие тоже)</td>
        <td class="border border-border px-3 py-2">TS-потребители получают <code>any</code></td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2"><code>bin</code></td>
        <td class="border border-border px-3 py-2">мапит CLI-команду на файл</td>
        <td class="border border-border px-3 py-2">нет установленной команды</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2"><code>files</code></td>
        <td class="border border-border px-3 py-2">allowlist того, что упаковать</td>
        <td class="border border-border px-3 py-2">отгрузишь тесты, src, секреты</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2"><code>sideEffects</code></td>
        <td class="border border-border px-3 py-2">говорит бандлерам, что модули чисты</td>
        <td class="border border-border px-3 py-2">слабее tree-shaking</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2"><code>engines</code></td>
        <td class="border border-border px-3 py-2">объявляет диапазон Node</td>
        <td class="border border-border px-3 py-2">тихая поломка на старом Node</td>
      </tr>
    </tbody>
  </table>
</div>

<Inset kind="why" lang="ru">
Почему добавление `"exports"` *ломает* существующих потребителей? Потому что инкапсуляция ретроактивна и тотальна. До `exports` достижим был каждый файл пакета; в день, когда ты добавляешь поле, каждый deep-import, не перечисленный явно, начинает бросать `ERR_PACKAGE_PATH_NOT_EXPORTED`. Это намеренно — в этом весь смысл — но значит, что добавление `exports` в зрелый пакет — это **semver-major** изменение. Перечисли каждый subpath, на который твои пользователи законно опираются (`"./package.json"` часто ждут инструменты), выпусти как major и задокументируй deep-import пути, которые выводишь из обихода.
</Inset>

## Tree-shaking, CLI bin и гигиена публикации

Статическая структура ESM — это то, что позволяет бандлеру **tree-shaking**: проанализировать импорты на этапе сборки и выбросить код, до которого никто не доходит. CJS сопротивляется: `require` — динамический вызов функции, так что бандлер обычно не может доказать, что ветка мертва. Ты помогаешь бандлеру через `"sideEffects": false` — обещание, что импорт твоих модулей не делает ничего, кроме объявления экспортов (никакой мутации глобалов, никакой регистрации полифилла). С этим флагом неиспользованный экспорт можно вырезать целиком; без него бандлер обязан держать модули на случай, если важен сам импорт.

**CLI** — это всего лишь маппинг `"bin"` от имени команды к файлу. Три вещи должны сойтись, иначе команда тихо не запустится: целевой файл нуждается в **shebang** `#!/usr/bin/env node` первой строкой, ему нужно **право на выполнение** (`chmod +x`), а при установке npm сам создаёт симлинк в `node_modules/.bin`. Забудешь shebang — оболочка попытается запустить JS как shell-скрипт; забудешь бит прав — получишь «permission denied».

```jsonc
{
  "bin": { "their-cli": "./dist/cli.js" },
  "files": ["dist"],
  "engines": { "node": ">=18.17" },
  "sideEffects": false
}
```

Наконец, **отгружай сборку, а не исходник**. Бандлер вроде tsup/esbuild/rollup эмитит `dist/` плюс `.d.ts`-декларации; используй allowlist `"files"` (или `.npmignore`), чтобы тарбол содержал только `dist` — не твой `src/`, тесты или `.env`. Запусти `npm pack --dry-run`, чтобы *увидеть точный список файлов* до публикации. Поднимай версии по **semver** (удалённый экспорт или новая карта `exports` — это major). И публикуй с `npm publish --provenance` из CI, чтобы npm записал подписанную аттестацию, связывающую артефакт с коммитом и workflow, его собравшим, — защита цепочки поставок от подменённых или тайпсквоттинговых релизов.

<TradeoffMatrix
  id="08-publishing-tradeoff-1"
  lessonSlug="01-bundling-and-publishing"
  lang="ru"
  prompt="Ты публикуешь новую универсальную библиотеку в 2026-м и должен выбрать формат дистрибуции. Каков сеньорский дефолт?"
  options={[
    { name: "Выпустить ESM-only с картой exports и условием types", summary: "Один формат, без hazard.", correct: true, justification: "Для новой библиотеки это прагматичный дефолт. Одна сборка означает ровно один экземпляр модуля, так что dual-package hazard просто не может возникнуть — instanceof и синглтоны уровня модуля работают. ESM полностью готов к проду в Node и открывает потребителям сильнейший tree-shaking. TS ты по-прежнему отдаёшь через условие types. Единственная цена — потеря чисто-CJS потребителей, которые не могут await import(), а это всё более узкая группа." },
    { name: "Dual-publish ESM + CJS как две независимые скомпилированные копии", summary: "Максимум охвата, реальный риск.", justification: "Максимизирует совместимость, но возвращает dual-package hazard: если граф загрузит обе копии, получишь две идентичности класса (instanceof ломается) и продублированное состояние модуля. Защитимо для фундаментального, широко используемого пакета, но только если прогонять всю общую идентичность/состояние через единое внутреннее ядро, которое реэкспортируют обе обёртки, — лишняя сложность сборки и тестов, не нужная большинству библиотек." },
    { name: "Выпустить только CJS", summary: "Совместимо, но слабо.", justification: "Широкая совместимость со старым тулингом, но ты теряешь надёжный tree-shaking (require динамичен, бандлеры не докажут, что код мёртв) и подсовываешь трение современным ESM-потребителям и бандлерам. Как экосистемный дефолт для свежей библиотеки это взгляд назад; оставь для кода, который обязан бежать на очень старых рантаймах." },
    { name: "Опубликовать TypeScript-исходник и дать потребителям его компилировать", summary: "Без сборки, сломанный DX.", justification: "Отгрузка нескомпилированного .ts заставляет каждого потребителя транспилировать твой код, протекает внутреннее предположение о тулчейне и напрямую ломает чистый JS и многие конфиги бандлеров. Ты также теряешь инкапсуляцию, которую даёт собранный dist + карта exports. Всегда эмить сборку и .d.ts; никогда не делай потребителей своим компилятором." },
  ]}
/>

<Quiz
  id="08-publishing-quiz-1"
  lessonSlug="01-bundling-and-publishing"
  lang="ru"
  question="Что карта exports даёт сверх выбора ESM- или CJS-файлов?"
  choices={[
    { label: "Инкапсуляцию: импортируемы только перечисленные subpath, так что deep-import внутренних файлов бросает ERR_PACKAGE_PATH_NOT_EXPORTED, и неперечисленные файлы можно свободно рефакторить", correct: true },
    { label: "Она автоматически минифицирует и tree-shake-ит твой пакет при публикации", misconception: "exports — это чистые метаданные разрешения: маппинг спецификаторов на файлы и условия. Она не делает минификации и tree-shaking; это работа бандлера, которой помогают ESM и sideEffects: false." },
    { label: "Она позволяет потребителям require() ESM-пакета из CommonJS без await import()", misconception: "Карта exports не может снять барьер форматов. CJS-вызывающий всё равно не может синхронно require() ESM-модуль; условия лишь выбирают, какой файл разрешить, а не как рантайм его загружает." },
    { label: "Она гарантирует, что потребители всегда получают свежайшую версию", misconception: "Версионирование управляется полем version и semver-диапазоном / lockfile потребителя. exports не имеет отношения к тому, какая версия установлена." },
  ]}
/>

<Quiz
  id="08-publishing-quiz-2"
  lessonSlug="01-bundling-and-publishing"
  lang="ru"
  question="Потребитель сообщает, что instanceof MyError возвращает false даже для ошибки, которую твоя dual-published библиотека явно бросила. Наиболее вероятная причина?"
  choices={[
    { label: "Dual-package hazard: граф загрузил обе твои копии — ESM и CJS — так что существуют два разных класса MyError, и instanceof сравнивает с неверным", correct: true },
    { label: "instanceof ненадёжен через любую границу пакета в Node", misconception: "instanceof прекрасно работает через границы пакетов, когда идентичность класса одна. Здесь он падает именно потому, что загружены две копии модуля, создавшие два конструктора MyError." },
    { label: "Они забыли задать this.name на классе ошибки", misconception: "this.name влияет на то, как ошибка печатается и на err.name, а не на instanceof. instanceof проверяет цепочку прототипов против конструктора, и сбой — это два разных конструктора, а не отсутствие name." },
    { label: "Ошибку нужно сериализовать в JSON перед пересечением модулей", misconception: "Сериализация тут ни при чём и наоборот уничтожила бы прототип, гарантировав instanceof false. Корневая причина — дублированные экземпляры модуля из-за dual-publishing." },
  ]}
/>

<DragOrder
  id="08-publishing-drag-1"
  lessonSlug="01-bundling-and-publishing"
  lang="ru"
  prompt="Расставь шаги безопасной публикации библиотеки + CLI — от сборки до аттестованного релиза:"
  items={[
    { id: "a", label: "Собрать через tsup/esbuild → эмитить dist/ JS плюс .d.ts-декларации" },
    { id: "b", label: "Добавить условие types, чтобы TS-потребители разрешали настоящие декларации, а не any" },
    { id: "c", label: "Написать карту exports (вход + subpath), чтобы импортировались только нужные пути" },
    { id: "d", label: "Задать files: [\"dist\"] (и bin + shebang + chmod +x для CLI), чтобы тарбол был минимальным и команда запускалась" },
    { id: "e", label: "Опубликовать из CI через npm publish --provenance для аттестации артефакта" },
  ]}
  correctOrder={["a", "b", "c", "d", "e"]}
/>

</Explanation>

<FlowDiagram
  label="От дерева исходников до потребителя, разрешающего нужный файл по условию"
  hue="var(--d-systems)"
  perRow={4}
  nodes={[
    { id: "s", label: "исходник", sub: "src/*.ts" },
    { id: "b", label: "сборка", sub: "tsup/esbuild" },
    { id: "e", label: "карта exports", sub: "условия" },
    { id: "c", label: "потребитель", sub: "разрешает" },
  ]}
  edges={[
    { from: "s", to: "b", label: "компиляция" },
    { from: "b", to: "e", label: "dist + .d.ts" },
    { from: "e", to: "c", label: "import/require/types" },
  ]}
  caption="Исходник компилируется в дерево dist; карта exports превращает это дерево в контракт. Загрузчик потребителя выбирает файл по условию — import → ESM, require → CJS, компилятор TS → types — и не может дотянуться ни до чего, что карта не перечисляет."
/>

<KeyTakeaway>Публикуешь ты поверхность `package.json`, а не дерево папок. Карта `exports` выбирает ESM/CJS/types по условию и инкапсулирует остальное. Предпочитай ESM-only, чтобы обойти dual-package hazard; всегда отгружай `types`.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="08-publishing-retrieval"
  lang="ru"
  questions={[
    { q: "Почему добавление карты exports в зрелый пакет — это ломающее (semver-major) изменение, и как выпустить его безопасно?", a: "Потому что инкапсуляция exports тотальна и ретроактивна. Пока поля нет, достижим каждый файл пакета, так что потребители могут законно делать deep-import путей вроде your-pkg/dist/internal/util.js. В момент добавления exports разрешаются только перечисленные subpath — каждый неперечисленный deep-import немедленно бросает ERR_PACKAGE_PATH_NOT_EXPORTED. Это и есть нужная выгода (теперь неперечисленные файлы можно свободно рефакторить), но это ломает всех, кто опирался на старую достижимость, поэтому выпускать надо как major-версию. Чтобы выпустить безопасно: перечисли каждый subpath, на который пользователи действительно опираются, и явно его укажи (включая ./package.json, который читают многие инструменты), держи main как фолбэк для тулинга, игнорирующего exports, дай условие types, задокументируй, какие deep-пути выводишь из обихода, и выпусти как явно помеченный major, чтобы потребители обновлялись осознанно." },
    { q: "Что именно такое dual-package hazard и как проще всего его избежать?", a: "Это режим сбоя при отгрузке одного пакета как двух независимых сборок — одна ESM, одна CJS. Если единый граф зависимостей в итоге загружает обе копии (по одному пути require() CJS-сборки, по другому import ESM-сборки), рантайм теперь держит два разных экземпляра модуля. Два экземпляра — две отдельные идентичности класса, так что проверки instanceof через границу возвращают false, а любое состояние уровня модуля — кэш, реестр, синглтон-соединение — тихо дублируется, ведя к тонким багам, где «тот же» объект — не тот. Простейший способ избежать для новой библиотеки — выпускать ESM-only: один формат означает ровно один экземпляр, так что hazard не возникает, а TypeScript ты по-прежнему отдаёшь через условие types. Если dual-publish обязателен ради охвата, прогоняй всю общую идентичность и состояние через единый внутренний модуль-ядро, который реэкспортируют обе обёртки — ESM и CJS, — чтобы при двух точках входа был один источник истины." },
  ]}
/>

<Recap lang="ru">
Публикация пакета — это на самом деле проектирование его `package.json`: именно этот файл, а не дерево исходников, задаёт публичную поверхность. Node выбирает формат модуля по `"type"` — `"module"` для ESM, иначе CommonJS — и эти два не смешиваются свободно, так что обслуживание обоих потребителей (*dual package*) рискует **dual-package hazard**, где две загруженные копии дают две идентичности класса (`instanceof` ломается) и продублированное состояние модуля; для новых библиотек выпуск **ESM-only** обходит это полностью. Карта `"exports"` — современная точка входа, вытесняющая `"main"`: она разрешает по условию (`"types"` первым, затем `"import"`/`"require"`, `"default"` последним) и, что критично, **инкапсулирует** — импортируемы только перечисленные subpath, так что неперечисленные внутренности можно свободно рефакторить (поэтому добавление `exports` в существующий пакет — semver-major поломка). ESM плюс `"sideEffects": false` открывают настоящий tree-shaking; CLI нужны `"bin"` плюс shebang `#!/usr/bin/env node` и право на выполнение; а гигиена отгрузки — это эмит сборки с `.d.ts`, allowlist `"files"`, диапазон `"engines"`, semver-инкременты и `npm publish --provenance` из CI ради подписанной аттестации цепочки поставок.
</Recap>
