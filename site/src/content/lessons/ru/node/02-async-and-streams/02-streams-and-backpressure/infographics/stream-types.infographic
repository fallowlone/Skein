infographic hierarchy-tree-curved-line-rounded-rect-node
data
  root
    label Типы потоков в Node
    children
      - label Readable — источник, из которого читают
        children
          - label Примеры: чтение файла, тело HTTP-запроса, сокет
          - label Читает в режиме flowing или paused
      - label Writable — приёмник, в который пишут
        children
          - label Примеры: запись файла, HTTP-ответ, сокет
          - label write() возвращает false после highWaterMark
      - label Duplex — обе стороны, чтение и запись независимы
        children
          - label Пример: TCP-сокет
          - label По одному буферу на направление
      - label Transform — Duplex, отображающий вход в выход
        children
          - label Примеры: gzip, шифр, парсер CSV
          - label Потоковый map: вошло с одной стороны, изменённое вышло
theme
  palette #6366f1 #14b8a6 #f59e0b #8b5cf6
