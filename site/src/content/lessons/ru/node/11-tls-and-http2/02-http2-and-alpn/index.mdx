---
concepts:
- http2-multiplexing
- alpn-negotiation
- head-of-line-blocking
- server-push-deprecation
- h2-vs-h2c
- flow-control
deepensInto: []
estMin: 18
lang: ru
lessonType: topic
level: senior
order: 2
prereqs: []
slug: "02-http2-and-alpn"
sources:
- https://nodejs.org/api/http2.html
- https://www.rfc-editor.org/rfc/rfc9113
- https://developer.chrome.com/blog/removing-push
spiral: []
status: ready
summary: 'HTTP/2 мультиплексирует множество потоков по одному TCP-соединению и согласует h2 через ALPN в TLS-рукопожатии — но одно соединение означает, что transport HoL всё ещё бьёт при потерях.'
title: 'HTTP/2 и ALPN: одно соединение, много потоков, один режим отказа'
track: node
unit: 11-tls-and-http2
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import SequenceDiagram from "~/components/diagram/SequenceDiagram.astro";

<Hook>
Команда включила HTTP/2, ожидая бесплатного выигрыша, и увидела, что p99 для мобильных пользователей стало *хуже*. Синтетические тесты на офисном оптоволокне выглядели прекрасно — десятки ассетов грузились по одному соединению, без лимита в шесть соединений на origin. Затем реальные пользователи на нестабильном LTE начали жаловаться. Один потерянный TCP-сегмент теперь стопорил *каждый* запрос в полёте разом, потому что все они делили одно соединение, а TCP доставляет байты строго по порядку. На HTTP/1.1 с шестью параллельными соединениями один потерянный пакет замораживал одно соединение; остальные пять продолжали течь. HTTP/2 тихо обменял шесть независимых доменов отказа на один. Реально помогла не настройка Node — а HTTP/3 поверх QUIC.
</Hook>

<Crux>HTTP/2 мультиплексирует потоки по одному TCP-соединению (убивает HTTP-HoL), выбирается как `h2` через ALPN — transport HoL бьёт при потерях.</Crux>

<Explanation>

## Мультиплексирование: много потоков, одно соединение

Определяющее ограничение HTTP/1.1 — соединение несёт один запрос за раз. Чтобы загрузить страницу с 40 ассетами, браузер открывает до шести TCP-соединений на origin и шлёт запросы по ним последовательно, а медленный ответ блокирует по принципу head-of-line всё, что стоит за ним в очереди на этом соединении. HTTP/2 заменяет это **мультиплексированием**: одно TCP-соединение несёт множество независимых **потоков (stream)**, каждый — чередующаяся последовательность бинарных кадров, помеченных ID потока. Запрос 7 и запрос 12 едут по одному сокету одновременно, их кадры `DATA` чередуются на проводе и пересобираются по ID потока на другом конце.

В Node основной модуль `http2` даёт это напрямую. Соединение — это `Http2Session`; каждая пара запрос/ответ — `Http2Stream`.

```js
import http2 from "node:http2";
import { readFileSync } from "node:fs";

const server = http2.createSecureServer({
  key: readFileSync("key.pem"),
  cert: readFileSync("cert.pem"),
  // ALPN: предлагаем h2 первым, откат на HTTP/1.1 для старых клиентов
  ALPNProtocols: ["h2", "http/1.1"],
});

server.on("stream", (stream, headers) => {
  // Каждый `stream` — один Http2Stream, мультиплексированный поверх общей сессии.
  stream.respond({ ":status": 200, "content-type": "text/plain" });
  stream.end("hello over " + stream.session.alpnProtocol);
});

server.listen(8443);
```

Событие `stream` срабатывает раз на запрос, и множество потоков принадлежат одной `session`. В этом вся модель мультиплексирования: сессия владеет TCP-сокетом и общим для соединения состоянием (управление потоком, `SETTINGS`), а каждый поток дёшев и независим на уровне HTTP.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Режим</th>
        <th class="border border-border px-3 py-2 text-left">Транспорт</th>
        <th class="border border-border px-3 py-2 text-left">ALPN id</th>
        <th class="border border-border px-3 py-2 text-left">Как клиент начинает</th>
        <th class="border border-border px-3 py-2 text-left">Поддержка браузерами</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2"><code>h2</code></td>
        <td class="border border-border px-3 py-2">HTTP/2 поверх TLS</td>
        <td class="border border-border px-3 py-2"><code>h2</code></td>
        <td class="border border-border px-3 py-2">ALPN в TLS-рукопожатии</td>
        <td class="border border-border px-3 py-2">Да (все современные браузеры)</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2"><code>h2c</code></td>
        <td class="border border-border px-3 py-2">HTTP/2 в открытом виде (без TLS)</td>
        <td class="border border-border px-3 py-2">нет (нет TLS-рукопожатия)</td>
        <td class="border border-border px-3 py-2">Prior-knowledge или HTTP/1.1 <code>Upgrade</code></td>
        <td class="border border-border px-3 py-2">Ни один браузер не поддерживает</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2"><code>http/1.1</code></td>
        <td class="border border-border px-3 py-2">HTTP/1.1 поверх TLS</td>
        <td class="border border-border px-3 py-2"><code>http/1.1</code></td>
        <td class="border border-border px-3 py-2">откат ALPN</td>
        <td class="border border-border px-3 py-2">Да</td>
      </tr>
    </tbody>
  </table>
</div>

Браузеры говорят только на `h2` поверх TLS. `h2c` (HTTP/2 в открытом виде) есть в спецификации — устанавливается либо по *prior knowledge* (клиент просто предполагает, что сервер говорит на h2c), либо через HTTP/1.1-танец `Upgrade: h2c`, — но ни один браузер его не реализует, поэтому на практике он встречается лишь между доверенными бэкенд-сервисами или за прокси, терминирующим TLS. Для всего, к чему прикасается браузер, HTTP/2 означает `h2`, что означает TLS, что означает ALPN.

## ALPN: согласование протокола внутри рукопожатия

Как клиент и сервер договариваются говорить на HTTP/2 *до* отправки хоть одного HTTP-байта? **ALPN** (Application-Layer Protocol Negotiation — согласование протокола прикладного уровня) — расширение TLS. Во время TLS `ClientHello` клиент анонсирует протоколы, на которых может говорить, упорядоченные по предпочтению, в расширении ALPN. Сервер выбирает один из этого списка и отражает его в `ServerHello`. К моменту завершения рукопожатия обе стороны уже знают, `h2` это соединение или `http/1.1` — ноль лишних round-trip, без запроса `Upgrade`.

<SequenceDiagram
  label="Согласование протокола ALPN внутри TLS-рукопожатия: клиент предлагает h2 и http/1.1, сервер выбирает h2"
  hue="var(--d-network)"
  actors={["Клиент", "Сервер"]}
  messages={[
    { from: "Клиент", to: "Сервер", label: "ClientHello + ALPN: [h2, http/1.1]" },
    { from: "Сервер", to: "Клиент", label: "ServerHello + ALPN: h2" },
    { from: "Клиент", to: "Сервер", label: "Finished (теперь говорим h2)" },
    { from: "Сервер", to: "Клиент", label: "кадр SETTINGS" },
  ]}
  caption="Сервер выбирает из списка, предложенного клиентом; порядок выбора — политика сервера. Если клиент не предлагает ни одного принимаемого сервером протокола, поведение зависит от конфигурации — Node делает откат согласно ALPNProtocols."
/>

В Node опция `ALPNProtocols` (и в `createSecureServer`, и в TLS-клиенте) — это упорядоченный список, который вы предлагаете. На сервере это обычно `["h2", "http/1.1"]` — предпочитаем HTTP/2, откат на 1.1 для старых клиентов. Порядок важен: массив сообщает *ваше* предпочтение, и сервер `http2` Node выбирает первую взаимно поддерживаемую запись. Прочитать согласованный результат можно из `stream.session.alpnProtocol` (или `socket.alpnProtocol`), чтобы подтвердить реально выбранное.

<Inset kind="why" lang="ru">
Мультиплексирование чинит **HTTP-уровневый** head-of-line blocking: ни один медленный ответ не блокирует остальные, потому что каждый — свой поток. Но оно не может починить **транспортный** HoL. TCP гарантирует доставку *одного* потока байтов по порядку. Когда вы кладёте 40 HTTP/2-потоков на одно TCP-соединение и один сегмент теряется, TCP удерживает *все* последующие байты — для каждого потока — пока не придёт ретрансмит, потому что ядро не отдаст байт N+1 раньше байта N. Так один потерянный пакет стопорит сразу все мультиплексированные потоки. Шесть соединений HTTP/1.1 случайно изолировали это: потеря замораживала одно соединение, а не все шесть. Реальный фикс — HTTP/3 поверх QUIC, где потоки идут поверх UDP с восстановлением потерь на каждый поток, так что потерянный пакет стопорит только свой поток. Мультиплексирование HTTP/2 действительно лучше на чистом канале и действительно хуже на нестабильном.
</Inset>

## Управление потоком, SETTINGS и почему умер Server Push

Если мультиплексирование — главная фича, то flow control (управление потоком данных) — сантехника, не дающая ей разорваться изнутри, а Server Push — поучительная история о том, как идея звучала отлично и тихо всё ухудшила. Мультиплексированному соединению нужна координация, и HTTP/2 несёт её как управляющие кадры. При установлении каждая сторона шлёт кадр `SETTINGS`, объявляя лимиты — прежде всего `SETTINGS_MAX_CONCURRENT_STREAMS` (сколько потоков пир может открыть разом) и начальное окно управления потоком. **Управление потоком (flow control)** работает на каждый поток *и* на всё соединение: получатель анонсирует окно байтов, которое готов буферизовать, а кадры `WINDOW_UPDATE` пополняют его по мере потребления данных. Это не даёт одному быстрому потоку утопить медленного потребителя, но это и грабли — слишком маленькое окно душит пропускную способность на каналах с большим произведением полосы на задержку (классический случай высокой задержки, где HTTP/2 должен блистать). Node даёт это через опции сессии вроде `settings: { maxConcurrentStreams }` и `peerMaxConcurrentStreams`.

**Server Push** был флагманской фичей HTTP/2: сервер мог послать `PUSH_PROMISE` и проактивно отправить ресурсы (CSS, JS), которые, по его прогнозу, понадобятся клиенту, ещё до запроса. На практике это провалилось. Серверы пушили ассеты, которые уже были в кэше браузера, тратя полосу на тех самых нестабильных каналах, где байты дороже всего; push было трудно приоритизировать правильно, и он часто *задерживал* критичный HTML, который должен был ускорить. Cache-hit был удручающим. Chrome **удалил** поддержку Server Push (изменение приехало в Chrome 106, 2022), а замена — `103 Early Hints`: информационный ответ, сообщающий браузеру, какие ресурсы стоит `preload`/`preconnect`, пока сервер ещё вычисляет настоящий ответ, оставляя *клиенту* решать, нужны ли они ему на самом деле.

<Quiz
  id="02-http2-and-alpn-quiz-1"
  lessonSlug="02-http2-and-alpn"
  lang="ru"
  question="Страница по h2 полностью стопорится при каждой потере пакета в мобильной сети, хотя каждый ассет — свой поток. Почему?"
  choices={[
    { label: "Все потоки делят одно TCP-соединение, и доставка TCP по порядку удерживает каждый последующий байт — по всем потокам — пока потерянный сегмент не будет ретранслирован (транспортный HoL)", correct: true },
    { label: "Мультиплексирование HTTP/2 сломано при потерях пакетов и его надо отключить", misconception: "Мультиплексирование работает как задумано; оно решает HTTP-уровневый HoL. Стопор — это доставка TCP по порядку, транспортное свойство, которое HTTP/2 не может изменить. Фикс — HTTP/3 поверх QUIC, а не выключение h2." },
    { label: "ALPN пересогласует протокол при каждом потерянном пакете, добавляя задержку", misconception: "ALPN происходит один раз, внутри начального TLS-рукопожатия. После установления соединения он не играет роли и никак не связан с потерей пакетов." },
    { label: "Окна управления потоком сбрасываются в ноль при потере пакета", misconception: "Окна управления потоком управляются кадрами WINDOW_UPDATE на уровне HTTP/2; они не сбрасываются при потере TCP. Стопор — это ядро, удерживающее байты по порядку." },
  ]}
/>

<TradeoffMatrix
  id="02-http2-and-alpn-tradeoff-1"
  lessonSlug="02-http2-and-alpn"
  lang="ru"
  prompt="Вы решаете, реально ли включение HTTP/2 поможет данной нагрузке. Какой случай выигрывает больше всего?"
  options={[
    { name: "Страница с множеством мелких ассетов для клиентов на каналах с высокой задержкой", summary: "Много мелких запросов.", correct: true, justification: "Это золотая середина HTTP/2. Мультиплексирование даёт десяткам мелких запросов делить одно соединение без лимита в шесть соединений и без head-of-line блокировки на соединение, а на канале с высокой задержкой избегание лишних установлений соединений (TCP- и TLS-рукопожатий) — большой выигрыш. Чем больше мелких параллелизуемых запросов по одному уже прогретому соединению, тем сильнее окупается мультиплексирование — при условии, что канал не сильно нестабилен." },
    { name: "Скачивание одного большого файла (например, видео 2 ГБ) по одному потоку", summary: "Один большой поток.", justification: "Мультиплексирование здесь не даёт ничего — поток всего один, чередовать нечего. Пропускную способность определяют TCP и окно управления потоком, идентично HTTP/1.1. Слишком маленькое окно flow control HTTP/2 может даже сделать чуть хуже. Преимущество HTTP/2 — параллелизм по многим запросам, которого у одного скачивания нет." },
    { name: "Ассеты, уже отдаваемые и мультиплексируемые CDN по своему h2/h3-краю", summary: "CDN уже делает это.", justification: "Если CDN уже терминирует HTTP/2 или HTTP/3 на краю и мультиплексирует клиентам, включение h2 на origin за ним мало что меняет для конечных пользователей — клиентский выигрыш уже взят на краю. Origin-to-CDN обычно — небольшое число долгоживущих соединений, где мультиплексирование h2 на запрос значит куда меньше. Вы оптимизируете хоп, который пользователи не видят." },
    { name: "Нагрузка на сильно нестабильной сети с одним соединением", summary: "Нестабильный одно соед.", justification: "Здесь HTTP/2 может проиграть HTTP/1.1. Одно TCP-соединение значит, что один потерянный сегмент блокирует по head-of-line каждый мультиплексированный поток через доставку TCP по порядку, тогда как несколько соединений HTTP/1.1 изолируют потерю. На нестабильном канале реальный ответ — HTTP/3 поверх QUIC, а не h2." },
  ]}
/>

</Explanation>

<KeyTakeaway>HTTP/2 мультиплексирует потоки по одному TLS-соединению, выбираясь как `h2` через ALPN. Убивает HTTP-HoL, но не транспортный — потеря сегмента стопорит все потоки; QUIC/HTTP3 — фикс. Push мёртв; шлите `103 Early Hints`.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="02-http2-and-alpn-retrieval"
  lang="ru"
  questions={[
    { q: "Объясните, как браузер и сервер http2 на Node договариваются говорить на HTTP/2 и где тут опция ALPNProtocols.", a: "Они договариваются через ALPN — расширение TLS, согласуемое внутри самого рукопожатия, до того как пойдут HTTP-байты. В ClientHello браузер анонсирует поддерживаемые протоколы, упорядоченные по предпочтению (обычно h2, затем http/1.1), в расширении ALPN. Сервер выбирает один взаимно поддерживаемый протокол из списка и отражает выбор в ServerHello. К моменту завершения TLS-рукопожатия обе стороны уже знают, h2 это соединение или http/1.1 — без лишних round-trip, без запроса HTTP Upgrade. В Node ALPNProtocols в http2.createSecureServer — это упорядоченный список, который сервер предлагает, обычно [\"h2\", \"http/1.1\"]: предпочитаем HTTP/2, откат на 1.1 для старых клиентов. Согласованный результат читается из stream.session.alpnProtocol или socket.alpnProtocol. Поскольку браузеры говорят только на h2 поверх TLS, ALPN фактически обязателен для HTTP/2 к браузерам; h2c в открытом виде браузерами не поддерживается и использует prior-knowledge или Upgrade-рукопожатие." },
    { q: "HTTP/2 починил head-of-line blocking — почему же p99 ухудшается на нестабильных мобильных сетях и что реально это чинит?", a: "Мультиплексирование починило HTTP-уровневый head-of-line blocking: каждый запрос — независимый поток, поэтому ни один медленный ответ не блокирует стоящие за ним. Но оно не может починить транспортный HoL. Все эти потоки делят одно TCP-соединение, а TCP гарантирует доставку одного потока байтов по порядку — при потере одного сегмента ядро удерживает каждый последующий байт, по всем потокам, пока не придёт ретрансмит. Так один потерянный пакет стопорит сразу все мультиплексированные потоки. Шесть параллельных соединений HTTP/1.1 случайно изолировали это: потеря замораживала одно соединение, а остальные текли. Поэтому переход на h2 может поднять p99 на нестабильном LTE, хотя помогает на чистом канале. Реальный фикс — HTTP/3 поверх QUIC, который несёт независимые потоки поверх UDP с восстановлением потерь на каждый поток, так что потерянный пакет стопорит только свой поток, а не всё соединение." },
  ]}
/>

<Recap lang="ru">
HTTP/2 несёт множество независимых потоков по одному TCP-соединению, чередуя бинарные кадры, помеченные ID потока — в Node одна `Http2Session` владеет сокетом и общим для соединения состоянием, а каждый запрос — дешёвый `Http2Stream`. Это мультиплексирование устраняет HTTP-уровневый head-of-line blocking и старый костыль с шестью соединениями на origin. Клиент и сервер договариваются говорить на HTTP/2 через ALPN, расширение TLS: клиент предлагает упорядоченный список протоколов (`ALPNProtocols`, например `["h2", "http/1.1"]`) в ClientHello, сервер выбирает один, и рукопожатие завершается с тем, что обе стороны знают протокол — без лишних round-trip. Браузеры говорят только на `h2` поверх TLS; `h2c` в открытом виде не поддерживается браузерами и живёт между бэкенд-сервисами. Подвох — транспортный HoL: поскольку каждый поток делит одно TCP-соединение, а TCP доставляет байты по порядку, один потерянный сегмент стопорит сразу все потоки, отчего HTTP/2 может ухудшить p99 на нестабильных каналах — и почему HTTP/3 поверх QUIC с восстановлением потерь на каждый поток — реальный фикс. Координация соединения едет на кадрах `SETTINGS` (лимиты конкурентности, окна управления потоком) и `WINDOW_UPDATE` для управления потоком на каждый поток и на всё соединение. HTTP/2 помогает больше всего с множеством мелких ассетов по каналам с высокой задержкой и меньше всего — для одного большого скачивания или ассетов, которые CDN уже мультиплексирует. А Server Push мёртв — Chrome удалил его из-за плохого cache-hit и потраченной впустую полосы; берите `103 Early Hints`, оставляя клиенту решать, что предзагружать. Теперь, когда после включения h2 p99 вырастет на мобильном трафике, первый подозреваемый — транспортный HoL, а ответ — QUIC, а не откат на HTTP/1.1.
</Recap>
