---
concepts:
- undici
- connection-pool
- keep-alive
- http-timeouts
- safe-retries
- body-draining
deepensInto: []
estMin: 16
lang: ru
lessonType: topic
level: middle
order: 3
prereqs: []
slug: 03-http-client-deep
sources:
- https://undici.nodejs.org/
- https://nodejs.org/api/http.html
- https://github.com/nodejs/undici/blob/main/docs/docs/api/Client.md
spiral: []
status: ready
summary: 'undici — современный HTTP-клиент Node и то, на чём работает глобальный fetch. Переиспользуй соединения через пул, ставь таймауты, повторяй только идемпотентные запросы и всегда вычитывай тело, иначе утекут сокеты.'
title: 'HTTP-клиент в Node: undici, пулы, таймауты и утечка сокетов'
track: node
unit: 11-tls-and-http2
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
Сервис оформления заказов был здоров, пока не накатил трафик «чёрной пятницы»: тогда p99 исходящего вызова к платёжному API подскочил с 40мс до 900мс — при том что сам платёжный API чувствовал себя нормально. Причина была в нашем собственном клиенте: каждый исходящий запрос шёл через `http.request` с агентом по умолчанию, а тот открывает свежее TCP-соединение и полный TLS-хендшейк на каждый вызов. При десяти запросах в секунду это десять хендшейков в секунду к одному хосту, каждый добавляет два round-trip ещё до того, как уйдёт первый байт полезной нагрузки. Мы платили за пул соединений, который ни разу не включили. Второй сбой в том квартале был хуже: воркер тёк файловыми дескрипторами до `EMFILE`, потому что кто-то прочитал `res.statusCode`, сделал ветвление и вышел раньше времени — не вычитав тело ответа. Каждое брошенное тело держало сокет, который так и не вернулся в пул.
</Hook>

<Crux>Переиспользуй keep-alive пул, ставь таймаут на запрос, повторяй лишь идемпотентные вызовы и всегда вычитывай тело, иначе утечёт сокет.</Crux>

<Explanation>

## undici — и почему fetch это уже undici

В Node есть два HTTP-клиента. Старый — это `http`/`https` (`http.request`, `http.get`). Современный — **undici**, написанный специально под Node: с keep-alive пулом соединений HTTP/1.1, заметно большей пропускной способностью и аккуратным promise-API. Выбирать между «использовать fetch» и «использовать undici» не нужно: в Node глобальный `fetch` построен на undici. Когда ты вызываешь `fetch(url)`, запрос отправляется через глобальный `Agent` от undici, так что настройка undici настраивает и `fetch`.

```js
import { request, Agent, Pool, setGlobalDispatcher } from "undici";

// Прямой запрос undici — возвращает { statusCode, headers, body }.
const { statusCode, body } = await request("https://api.example.com/orders");
const orders = await body.json(); // вычитывает тело

// fetch ЭТО undici: передай свой пул через опцию dispatcher.
const pool = new Pool("https://api.example.com", { connections: 64 });
const res = await fetch("https://api.example.com/orders", { dispatcher: pool });
```

`Agent` управляет пулом *на каждый origin* (поднимает по `Pool` на каждый хост, с которым ты общаешься); `Pool` управляет множеством соединений к *одному* origin; `Client` — это одно соединение к одному origin. Большинство приложений используют `Agent` — напрямую или неявно через `fetch`.

<Inset kind="why" lang="ru">
Почему `fetch` в Node ведёт себя как undici, а не как в браузере? Потому что `fetch` в Node — это не отдельная реализация, а именно `fetch` из undici, вынесенный в глобальную область, и внутри он использует глобальный диспетчер undici (`Agent`). Это здорово: пулинг соединений достаётся бесплатно, а ещё можно подменить настроенный пул для конкретного вызова опцией `dispatcher` или на весь процесс через `setGlobalDispatcher(new Agent(&#123; connections: 128 &#125;))`. Но это и ловушка, если считать `fetch` «просто HTTP без состояния»: он делит общий пул, поэтому утёкшее тело или исчерпанный пул в одной части кода деградируют `fetch` повсюду.
</Inset>

## Keep-alive: плати за хендшейк один раз, а не на каждый запрос

Новый HTTP-запрос по новому соединению стоит TCP-хендшейка (один round-trip) плюс, для HTTPS, TLS-хендшейка (ещё один-два). На канале с задержкой 30мс это 60–90мс чистой подготовки до того, как пойдут данные — и ты платишь это на *каждый* запрос, если соединения не переиспользуются. Keep-alive держит TCP+TLS-соединение открытым после ответа, чтобы следующий запрос к тому же origin его переиспользовал, сводя стоимость хендшейка почти к нулю.

Именно поэтому поведение старого клиента по умолчанию — ловушка. С Node 19 глобальный агент включает keep-alive, но остаётся ненастроенным — `maxSockets` равен `Infinity`, дисциплины пула нет, — так что на нагрузке агента всё равно конструируют явно, чтобы осознанно ограничить и переиспользовать соединения. undici держит соединения живыми *и* пулит их по умолчанию.

```js
// Старый http: глобальный агент keep-alive'ит с Node 19, но ненастроен —
// задай свой Agent, чтобы ограничить maxSockets и переиспользовать осознанно.
import http from "node:http";
const agent = new http.Agent({ keepAlive: true, maxSockets: 64 });
http.get({ host: "api.example.com", path: "/x", agent }, (res) => {
  res.resume(); // вычитать — см. ниже
});

// undici: keep-alive пул по умолчанию; просто переиспользуй один диспетчер.
import { Agent, setGlobalDispatcher } from "undici";
setGlobalDispatcher(new Agent({ connections: 64, pipelining: 1 }));
```

Опция `connections` ограничивает, сколько соединений undici открывает на origin (`Agent` строит по `Pool` на origin, а `connections` лимитирует этот пул); `pipelining` задаёт, сколько запросов может быть «в полёте» на одном соединении (по умолчанию `1` — оставь так, пока не измеришь выгоду, потому что pipelining HTTP/1.1 порождает head-of-line блокировку). Размеряй пул под ёмкость нижестоящего сервиса, а **не** под бесконечность: безграничный пул превращает всплеск трафика в наводнение соединений, которое валит downstream.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Аспект</th>
        <th class="border border-border px-3 py-2 text-left"><code>http.request</code> (агент по умолчанию)</th>
        <th class="border border-border px-3 py-2 text-left"><code>undici</code> / глобальный <code>fetch</code></th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">Keep-alive</td>
        <td class="border border-border px-3 py-2">Включён с Node 19, но ненастроен (<code>maxSockets: Infinity</code>) — задай <code>new http.Agent(&#123; keepAlive: true, maxSockets &#125;)</code></td>
        <td class="border border-border px-3 py-2">Включён по умолчанию; пул на origin</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Ручка размера пула</td>
        <td class="border border-border px-3 py-2"><code>maxSockets</code></td>
        <td class="border border-border px-3 py-2"><code>connections</code> (на origin)</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">API</td>
        <td class="border border-border px-3 py-2">Колбэки / потоки</td>
        <td class="border border-border px-3 py-2">Promise; <code>body.json()</code> и т.д.</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Нужно вычитывать тело?</td>
        <td class="border border-border px-3 py-2">Да (<code>res.resume()</code>)</td>
        <td class="border border-border px-3 py-2">Да (<code>body.dump()</code> / вычитать)</td>
      </tr>
    </tbody>
  </table>
</div>

## Таймауты и повторы: ограничивай всё, повторяй почти ничего

Запрос без таймаута может висеть вечно, а висящий запрос держит соединение пула вне оборота. undici даёт три ручки, которые важны: `headersTimeout` (время на получение заголовков ответа, по умолчанию `300e3` = 300с), `bodyTimeout` (максимальный промежуток между чанками тела, по умолчанию `300e3`) и `timeout` на подключение (установление TCP/TLS, по умолчанию `10e3` = 10с). Значения по умолчанию в 300 секунд *щедрые* — для пользовательского вызова нужны однозначные секунды, заданные на уровне диспетчера.

```js
import { Agent } from "undici";
const agent = new Agent({
  connections: 64,
  connect: { timeout: 2_000 },   // сдаться при подключении через 2с
  headersTimeout: 5_000,         // заголовки должны прийти за 5с
  bodyTimeout: 10_000,           // нет чанка тела 10с -> отмена
});
```

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Ручка</th>
        <th class="border border-border px-3 py-2 text-left">От чего защищает</th>
        <th class="border border-border px-3 py-2 text-left">По умолчанию в undici</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2"><code>connect.timeout</code></td>
        <td class="border border-border px-3 py-2">Мёртвый хост / SYN в чёрную дыру</td>
        <td class="border border-border px-3 py-2">10с</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2"><code>headersTimeout</code></td>
        <td class="border border-border px-3 py-2">Сервер принял, но не отвечает</td>
        <td class="border border-border px-3 py-2">300с</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2"><code>bodyTimeout</code></td>
        <td class="border border-border px-3 py-2">Медленное / зависшее потоковое тело</td>
        <td class="border border-border px-3 py-2">300с</td>
      </tr>
    </tbody>
  </table>
</div>

Повторы — это место, где наивный код опасен. Повторить `GET` обычно безопасно — он **идемпотентен**, так что дважды даёт тот же эффект, что и один раз. Повторить `POST /charge` — нет: таймаут означает, что ты не знаешь, прошёл ли платёж, и слепой повтор может списать с клиента дважды. Хуже того, когда downstream уже перегружен и *каждый* клиент повторяет при ошибке, начинается **retry-шторм** — сбой запускает волну дублирующих запросов, которая хоронит и без того страдающий сервис ещё глубже. Безопасная политика повторов: повторять только идемпотентные методы (или запросы с ключом идемпотентности), ограничивать число попыток и использовать экспоненциальный backoff с джиттером, чтобы повторы расходились во времени, а не синхронизировались.

<FlowDiagram
  label="Жизненный цикл запроса через keep-alive пул undici: взять соединение, отправить, получить, затем вычитать тело и вернуть в пул"
  hue="var(--d-systems)"
  perRow={3}
  nodes={[
    { id: "req", label: "request()", sub: "твой вызов" },
    { id: "pool", label: "Pool", sub: "connections: N" },
    { id: "conn", label: "Соединение", sub: "живое" },
    { id: "send", label: "Отпр.+приём", sub: "заг./тело" },
    { id: "drain", label: "Вычитать", sub: "consume/dump" },
    { id: "free", label: "Вернуть", sub: "в пул" },
  ]}
  edges={[
    { from: "req", to: "pool", label: "взять" },
    { from: "pool", to: "conn", label: "reuse" },
    { from: "conn", to: "send" },
    { from: "send", to: "drain" },
    { from: "drain", to: "free", label: "снова годен" },
  ]}
  caption="Соединение возвращается в пул только после полного вычитывания тела. Пропустишь вычитку — сокет закреплён; так пул голодает, а дескрипторы текут."
/>

## Тело, которое нужно всегда вычитывать

В undici **каждое тело ответа должно быть полностью вычитано или уничтожено.** Соединение нельзя вернуть в пул, пока его текущий ответ не дочитан до конца — протоколу нужно знать, где заканчивается этот ответ, прежде чем соединение понесёт следующий запрос. Если ты прочитал `statusCode`, сделал ветвление и вышел, не тронув `body`, то это соединение застряло. На нагрузке у пула кончаются свободные соединения (истощение сокетов), а поскольку каждое удерживаемое соединение — это открытый файловый дескриптор, ты в итоге упираешься в `EMFILE` и шторм `ECONNRESET`, когда ОС и пиры рвут зависшее.

```js
import { request } from "undici";
const { statusCode, body } = await request("https://api.example.com/orders");
if (statusCode === 200) {
  return await body.json();   // вычитывает тело — соединение освобождено
}
await body.dump();            // не интересно? всё равно сбрось, не теки
return null;
```

Правило безусловно: вычитывай тело (`body.json()`, `body.text()`, `body.arrayBuffer()` или дочитывай поток) на счастливом пути и делай `body.dump()` на каждом пути, где выходишь раньше. То же касается старого `http`: вызывай `res.resume()`, чтобы вычитать ответ, который ты не читаешь.

<Quiz
  id="03-http-client-deep-quiz-1"
  lessonSlug="03-http-client-deep"
  lang="ru"
  question="На нагрузке воркер дорастает до EMFILE, и пул перестаёт обслуживать запросы. Горячий путь читает res.statusCode, логирует его и выходит, не читая тело. Наиболее вероятная причина?"
  choices={[
    { label: "Тела ответов не вычитываются, поэтому соединения не возвращаются в пул — они держат открытые файловые дескрипторы, пока процесс не упрётся в лимит", correct: true },
    { label: "headersTimeout слишком мал и рано убивает соединения", misconception: "Малый headersTimeout прерывает медленные запросы; он не держит соединения открытыми и не исчерпывает дескрипторы. Симптом здесь — рост открытых FD до EMFILE — указывает на соединения, которые никогда не освобождаются, то есть невычитанные тела." },
    { label: "fetch в Node не использует пул соединений, поэтому каждый вызов открывает новый сокет", misconception: "fetch в Node ЭТО undici и делит общий пуловый диспетчер. Проблема не в отсутствии пулинга, а в том, что пуловые соединения не освобождаются, потому что тела не вычитаны." },
    { label: "connections выставлен слишком высоко, открывая слишком много сокетов", misconception: "Слишком высокий connections упирается в число и переиспользует их; он не растёт безгранично до EMFILE. Безграничный рост FD идёт от соединений, застрявших открытыми из-за невычитанных тел." },
  ]}
/>

<TradeoffMatrix
  id="03-http-client-deep-tradeoff-1"
  lessonSlug="03-http-client-deep"
  lang="ru"
  prompt="Запрос на списание с клиента (POST /charge) истёк по таймауту. Ты не знаешь, прошёл ли он. Какая политика повторов верна?"
  options={[
    { name: "Не повторять вслепую; повторять только если запрос несёт ключ идемпотентности (или сделать операцию идемпотентной), с ограничением попыток и backoff+джиттером", summary: "Сначала идемпотентность.", correct: true, justification: "Таймаут на неидемпотентном POST неоднозначен — списание могло пройти. Слепой повтор рискует двойным списанием. Сеньорный ответ — сделать операцию безопасной к повтору: послать ключ идемпотентности, по которому сервер дедуплицирует, чтобы повтор того же ключа был no-op. Затем ограничить попытки и добавить экспоненциальный backoff с джиттером, чтобы избежать синхронных retry-штормов. Если идемпотентность невозможна — показывай неоднозначность, а не угадывай." },
    { name: "Повторять сразу до 5 раз — большинство таймаутов временные", summary: "Просто повторить.", justification: "Немедленные, почти неограниченные повторы неидемпотентного списания могут списать дважды, а повтор без backoff во время замедления downstream добавляет нагрузку ровно тогда, когда сервису плохо — это retry-шторм. Повторам нужны идемпотентность, лимиты и backoff с джиттером." },
    { name: "Относиться ко всем методам одинаково и повторять GET, POST, DELETE идентично с backoff", summary: "Одинаковые повторы.", justification: "GET идемпотентен и безопасен к повтору; POST /charge — нет. Единая политика на все методы либо недозащищает (пропуская безопасные повторы GET), либо переусердствует в риске (повторяя небезопасные POST). Безопасность повтора — на уровне операции, по ключу идемпотентности, а не глобальный переключатель." },
    { name: "Увеличить bodyTimeout, чтобы запрос вообще не истекал и не возникала неоднозначность", summary: "Убрать таймаут.", justification: "Убрав таймаут, ты не убираешь режим отказа — просто заставляешь зависший запрос держать пуловое соединение минутами, голодая пул. Отказы всё равно будут (сбросы соединений, смерть downstream); ты лишь поменял быстрый явный отказ на медленную утечку ресурсов." },
  ]}
/>

</Explanation>

<KeyTakeaway>undici — современный клиент Node и основа `fetch`. Держи один keep-alive пул, ставь таймауты на connect/headers/body, повторяй лишь идемпотентные вызовы и вычитывай или `dump()` тело — иначе сокет течёт дескрипторами.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="03-http-client-deep-retrieval"
  lang="ru"
  questions={[
    { q: "Почему всегда нужно вычитывать или сбрасывать тело ответа undici, и что ломается, если этого не делать?", a: "В undici соединение HTTP/1.1 может понести следующий запрос только после того, как текущий ответ полностью дочитан — протоколу нужно знать, где заканчивается ответ, прежде чем соединение станет переиспользуемым. Поэтому каждое тело ответа должно быть полностью вычитано (body.json/text/arrayBuffer или дочитан поток) либо явно уничтожено через body.dump(). Если прочитать statusCode, сделать ветвление и выйти, не тронув тело, это соединение никогда не вернётся в пул. На нагрузке у пула кончаются свободные соединения (истощение сокетов) и вызовы встают в очередь или падают; каждое удерживаемое соединение — это ещё и открытый файловый дескриптор, поэтому процесс ползёт к EMFILE, и ты видишь шторм ECONNRESET, когда пиры и ОС рвут застрявшие сокеты. Фикс безусловен: вычитывай тело на счастливом пути и делай body.dump() на каждом пути раннего выхода. У старого http то же правило через res.resume()." },
    { q: "Почему повтор POST /charge после таймаута опасен, и какова безопасная политика?", a: "Таймаут неоднозначен: запрос мог дойти до сервера и успешно выполниться, хотя ответа ты не получил. GET идемпотентен, поэтому повтор безвреден, но POST /charge — нет: слепой повтор может списать с клиента дважды. Вдобавок, когда downstream уже перегружен и каждый клиент повторяет при ошибке, сбои запускают волну дублирующих запросов, которая хоронит сервис глубже: retry-шторм. Безопасная политика — повторять только идемпотентные операции либо сделать операцию идемпотентной, послав ключ идемпотентности, по которому сервер дедуплицирует, чтобы повтор того же ключа был no-op; ограничить число попыток; и использовать экспоненциальный backoff с джиттером, чтобы повторы расходились, а не синхронизировались. Если операцию нельзя сделать идемпотентной — показывай неоднозначность, а не угадывай." },
  ]}
/>

<Recap lang="ru">
У клиентской стороны HTTP в Node есть один современный ответ: undici, на котором также работает глобальный `fetch`, так что запрос через `fetch` течёт через пуловый `Agent` undici, а тюнингованный пул можно подменить опцией `dispatcher`. Первый рычаг — переиспользование соединений: keep-alive держит TCP+TLS-соединение открытым, чтобы платить за хендшейк один раз, а не на каждый запрос; undici делает это и пулит по умолчанию, тогда как легаси-глобальный агент keep-alive'ит с Node 19, но остаётся ненастроенным (безграничный `maxSockets`), так что на нагрузке задаёшь явный `new http.Agent(&#123; keepAlive: true, maxSockets &#125;)`. Размеряй пул через `connections` под реальную ёмкость downstream, а не под бесконечность, и оставляй `pipelining` равным 1, пока не измерил выгоду. Ограничивай каждый запрос: таймаут подключения для мёртвых хостов, `headersTimeout` для серверов, которые приняли, но не отвечают, `bodyTimeout` для зависших потоков — значения по умолчанию в 300 секунд слишком щедры для пользовательских вызовов. Повторяй почти ничего: только идемпотентные методы или запросы с ключом идемпотентности, с лимитом и экспоненциальным backoff плюс джиттером, иначе наивный повтор спишет с клиента дважды, а синхронная волна станет retry-штормом. И правило, что тихо роняет сервисы: всегда вычитывай или делай `body.dump()` ответа, потому что невычитанное тело держит своё соединение вне пула, истощает сокеты и течёт файловыми дескрипторами до `EMFILE` и `ECONNRESET`.
</Recap>
