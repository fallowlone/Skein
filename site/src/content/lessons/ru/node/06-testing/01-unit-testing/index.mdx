---
concepts:
- node-test-runner
- node-assert
- mocking-and-spies
- fake-timers
- vitest
- test-isolation
deepensInto: []
estMin: 18
lang: ru
lessonType: topic
level: middle
order: 1
prereqs: []
slug: 01-unit-testing
sources:
- https://nodejs.org/api/test.html
- https://nodejs.org/api/assert.html
- https://vitest.dev/guide/
spiral: []
status: ready
summary: 'Node несёт стабильный раннер (node:test) и node:assert; Vitest добавляет
  скорость Vite и богатые матчеры. Тестируй поведение, а не реализацию, держи тесты
  независимыми и подделывай часы, чтобы зависимые от времени тесты были быстрыми.'
title: 'Юнит-тесты с node:test и Vitest'
track: node
unit: 06-testing
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
У хелпера ретраев был тест, который реально ждал пять секунд бэкоффа на `setTimeout`, и сюита была зелёной — пока CI не нагрузился. Под нагрузкой таймер срабатывал на пару миллисекунд позже, ассерт выполнялся до завершения ретрая, и тест падал примерно один прогон из двадцати. Команда пометила его `skip`, выкатилась, а через три недели реальная регрессия бэкоффа проскользнула, потому что единственный покрывавший её тест заглушили. Сбой никогда не был в коде ретрая. Это был тест, привязанный к настенным часам, а фикс — четыре строки: подделать таймеры, прокрутить их вручную, и тот же тест стал мгновенным и на 100% детерминированным.
</Hook>

<Crux>Тестируй поведение, а не реализацию: node:test для скорости или Vitest для матчеров; изолируй состояние и подделывай часы.</Crux>

<Explanation>

## Встроенный раннер: node:test и node:assert

С Node 20 раннер тестов стабилен и встроен — без зависимостей, без конфига. Тесты пишешь через `test()` (или `describe()` / `it()`), проверяешь через `node:assert` и запускаешь через `node --test`, который находит файлы вида `*.test.js`, `*-test.js` и файлы в каталогах `test/`. Добавь `--watch` для перезапуска при изменении и `--experimental-test-coverage` для сводки покрытия. Сабтесты вкладываются через `await t.test(...)`, а хуки (`before`, `after`, `beforeEach`, `afterEach`) поднимают и сносят состояние.

```js
import { test, describe, it, beforeEach } from "node:test";
import assert from "node:assert/strict";
import { Cart } from "./cart.js";

describe("Cart", () => {
  let cart;
  beforeEach(() => { cart = new Cart(); }); // свежее состояние на каждый тест

  it("totals line items", () => {
    cart.add({ price: 10, qty: 2 });
    assert.strictEqual(cart.total(), 20);
  });

  it("rejects a negative quantity", () => {
    assert.throws(() => cart.add({ price: 10, qty: -1 }), /quantity/);
  });
});
```

Ключевое с `node:assert` — импортировать **строгий** вариант (`node:assert/strict`), чтобы `assert.equal` означал `===`, а не нестрогий `==`, который тихо считает `"2"` и `2` равными. `assert.strictEqual` сравнивает примитивы; `assert.deepStrictEqual` обходит объекты и массивы структурно; `assert.match` проверяет строку против регулярки. Сбои, которые ты *ожидаешь*, проверяй напрямую: `assert.throws(fn, /pattern/)` для синхронных throw и `await assert.rejects(promise, /pattern/)` для зареджекченных промисов — тест, который проверяет отсутствие ошибки там, где она должна сработать, проходит по неправильной причине.

## Мокаем на границах

Юнит-тест должен прогонять *твою* логику, поэтому ты заменяешь медленные или недетерминированные вещи на её краях — сеть, файловую систему, часы — а собственный код оставляешь настоящим. `node:test` даёт `mock.fn()` для отдельных шпионов, `t.mock.method(obj, "name")` для замены метода (автовосстановление по завершении теста) и `t.mock.module()` для подмены целого модуля. Шпион записывает каждый вызов: `mock.calls` хранит аргументы, возвращаемые значения и число вызовов, так что ты ассертишь *как* был использован коллаборатор.

```js
import { test, mock } from "node:test";
import assert from "node:assert/strict";

test("charges the gateway once with the order total", () => {
  const gateway = { charge: mock.fn(() => ({ id: "ch_1" })) };
  const order = { total: 4200, gateway };

  checkout(order);

  assert.strictEqual(gateway.charge.mock.callCount(), 1);
  assert.deepStrictEqual(gateway.charge.mock.calls[0].arguments, [4200]);
});
```

Дисциплина, которая важна: **мокай границу, а не собственную логику.** Если замокать тестируемую функцию, тест ассертит твой мок, а не твой код. Заглуши HTTP-вызов платёжного шлюза; не заглушай `checkout()`, который ты пытаешься проверить.

<Inset kind="why" lang="ru">
Поддельные таймеры — самый рычажный мок для бэкенд-кода. `mock.timers.enable({ apis: ["setTimeout", "Date"] })` меняет реальные часы на управляемые; `mock.timers.tick(5000)` мгновенно продвигает виртуальное время, срабатывая всеми таймерами в этом окне, при этом настенные часы не двигаются вовсе. Тест бэкоффа на пять секунд бежит за микросекунды и никогда не флакает на загруженном CI, потому что реального времени нет — и значит, нет гонки. `vi.useFakeTimers()` / `vi.advanceTimersByTime()` у Vitest делает то же. Один этот приём превращает самую медленную и флакающую категорию юнит-тестов в самую быструю и надёжную.
</Inset>

## Выбор раннера: node:test vs Vitest vs Jest

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Раннер</th>
        <th class="border border-border px-3 py-2 text-left">Зависимости</th>
        <th class="border border-border px-3 py-2 text-left">Старт / скорость</th>
        <th class="border border-border px-3 py-2 text-left">API и среда</th>
        <th class="border border-border px-3 py-2 text-left">Брать, когда</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2"><code>node:test</code></td>
        <td class="border border-border px-3 py-2">ноль (встроен)</td>
        <td class="border border-border px-3 py-2">быстрейший холодный старт; без бандлера</td>
        <td class="border border-border px-3 py-2">core API + <code>node:assert</code>; без DOM</td>
        <td class="border border-border px-3 py-2">библиотеки, чистый бэкенд, минимум зависимостей</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Vitest</td>
        <td class="border border-border px-3 py-2">одна dev-зависимость (Vite)</td>
        <td class="border border-border px-3 py-2">быстрый watch на HMR; переиспользует трансформ Vite</td>
        <td class="border border-border px-3 py-2">Jest-подобные <code>expect</code>/<code>vi</code>; jsdom; TS из коробки</td>
        <td class="border border-border px-3 py-2">богатые матчеры, снапшоты, DOM, монорепо</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Jest</td>
        <td class="border border-border px-3 py-2">несколько зависимостей</td>
        <td class="border border-border px-3 py-2">медленнее старт; тяжёлый трансформ</td>
        <td class="border border-border px-3 py-2">зрелый <code>expect</code>; jsdom; огромная экосистема</td>
        <td class="border border-border px-3 py-2">уже есть сюита Jest, легаси-плагины</td>
      </tr>
    </tbody>
  </table>
</div>

`node:test` — верный дефолт для библиотеки или бэкенда с небольшим числом зависимостей: ставить нечего, быстрейший старт, и он говорит на core API. Бери **Vitest**, когда нужна эргономика в стиле Jest — цепочечные матчеры `expect`, инлайн-снапшоты, `vi.mock`, среда `jsdom`/`happy-dom` для кода, трогающего DOM, TypeScript без лишнего конфига и шустрый watch на HMR, перезапускающий только затронутые тесты. Vitest почти полностью совместим с Jest (`vi.fn` вместо `jest.fn`, `vi.mock` вместо `jest.mock`), так что это ещё и стандартный путь сбежать от медленной настройки Jest. Сам Jest бери в основном тогда, когда у тебя уже есть сюита Jest и плагины, которые не хочется мигрировать.

<TradeoffMatrix
  id="06-unit-testing-tradeoff-1"
  lessonSlug="01-unit-testing"
  lang="ru"
  prompt="Ты начинаешь новую публикуемую npm-библиотеку: чистый TypeScript, без DOM, и хочешь минимальное дерево зависимостей и быстрейший холодный старт CI. Какой раннер — сеньорский дефолт?"
  options={[
    { name: "node:test с node:assert/strict", summary: "Встроен, ноль зависимостей.", correct: true, justification: "Для библиотеки с лёгкими зависимостями это сильнейший вариант: ставить нечего, быстрейший холодный старт (без трансформа бандлера) и стабильное core API с Node 20. Получаешь describe/it, хуки, шпионов через mock.fn, поддельные таймеры и встроенное покрытие через --experimental-test-coverage. Меньше зависимостей — меньше supply-chain-поверхность для пакета, который ставят другие. Жертвуешь матчерами в стиле Jest, но библиотеке они нужны редко." },
    { name: "Vitest", summary: "Отличный DX, одна dev-зависимость.", justification: "Превосходен для приложений, снапшотов, работы с DOM и монорепо благодаря watch на HMR и богатым матчерам expect — но тянет Vite как dev-зависимость и пайплайн трансформа, который не нужен для чистого TS без DOM. Достойный выбор и почти совместим с Jest; просто тяжелее node:test для заявленной цели — минимальное дерево и быстрейший холодный старт." },
    { name: "Jest", summary: "Зрелый, но тяжёлый.", justification: "Зрелый и проверенный в боях, с огромной экосистемой, но несёт несколько зависимостей и более тяжёлый трансформ, давая медленнее старт, чем оба варианта. Он блещет, когда наследуешь существующую сюиту Jest и плагины; для совсем новой библиотеки с минимумом зависимостей это неверное направление." },
    { name: "Самописный assert-скрипт на голом node", summary: "Изобретает велосипед.", justification: "Пришлось бы заново реализовать обнаружение тестов, хуки, шпионов, поддельные таймеры, репортеры и покрытие, которые node:test уже даёт бесплатно и сопровождает. Это больше кода, больше багов и никакой экосистемы — никогда не сеньорский выбор, когда есть стабильный встроенный раннер." },
  ]}
/>

## Принципы, которые держат тесты честными

Три правила отделяют сюиту, которой доверяешь, от той, что заглушают. **Тестируй поведение, а не реализацию:** ассерти наблюдаемый исход (возвращаемое значение, вызов коллаборатора), а не приватные внутренности, чтобы рефакторинг, сохраняющий поведение, не красил сюиту. Тест, ломающийся при каждом переименовании приватного метода, тестирует не то. **Arrange-Act-Assert:** подготовь входы, выполни одно действие, проверь один исход — один логический ассерт на тест делает место сбоя точным. **Держи каждый тест независимым:** без разделяемого изменяемого состояния, без опоры на порядок прогона. Сбрасывай состояние в `beforeEach`; если тест B проходит лишь потому, что тест A отработал первым и оставил строку в массиве уровня модуля, у тебя порядко-зависимая сюита, которая флакнет в тот же миг, когда раннер распараллелит или перемешает.

```js
// ❌ зависит от порядка: состояние уровня модуля течёт между тестами
const users = [];
test("a: adds a user", () => { users.push({ id: 1 }); assert.strictEqual(users.length, 1); });
test("b: starts empty", () => { assert.strictEqual(users.length, 0); }); // ПАДАЕТ после a

// ✅ независимо: свежее состояние каждый раз
let store;
beforeEach(() => { store = new UserStore(); });
```

<Quiz
  id="06-unit-testing-quiz-1"
  lessonSlug="01-unit-testing"
  lang="ru"
  question="Тест мокает ту функцию, которую должен проверить, и ассертит, что мок был вызван. Что с ним не так?"
  choices={[
    { label: "Он тестирует мок, а не реальный код — заменив тестируемую единицу заглушкой, он никогда не поймает баг в ней; мокать надо границу (сеть, fs, часы), а собственную логику оставлять настоящей", correct: true },
    { label: "Моки всегда запрещены в юнит-тестах", misconception: "Моки необходимы — но на границах. Ты заглушаешь медленных или недетерминированных коллабораторов (HTTP, файловая система, часы). Ошибка здесь — мокать саму тестируемую функцию, а не использование моков вообще." },
    { label: "Нужен await, потому что моки асинхронны", misconception: "mock.fn() синхронен; здесь ничто не требует await. Проблема концептуальная: тест проверяет заглушку вместо реальной реализации, поэтому проходит всегда, независимо от реального поведения." },
    { label: "Надо использовать assert.deepStrictEqual вместо strictEqual", misconception: "Выбор ассерта не при чём. Как бы ты ни проверял, мок тестируемой единицы означает, что реальный код не выполняется, поэтому тест ничего о нём не доказывает." },
  ]}
/>

<Quiz
  id="06-unit-testing-quiz-2"
  lessonSlug="01-unit-testing"
  lang="ru"
  question="Тест реально ждёт 5 секунд ретрая на setTimeout и флакает под нагрузкой CI. Почему поддельные таймеры чинят и медлительность, и флакость?"
  choices={[
    { label: "Поддельные таймеры меняют реальные часы на управляемые: mock.timers.tick(5000) мгновенно продвигает виртуальное время и синхронно срабатывает нужным колбэком, так что реального ожидания нет и нет гонки между таймером и ассертом", correct: true },
    { label: "Поддельные таймеры заставляют CPU выполнять таймаут быстрее", misconception: "Они не ускоряют реальное время — они его убирают. Часы виртуальны, так что продвижение на 5000мс мгновенно, а колбэк срабатывает детерминированно; скорость CPU тут ни при чём." },
    { label: "Они перезапускают тест, пока он не пройдёт", misconception: "Это спрятало бы флакость, а не починило. Поддельные таймеры устраняют источник гонки — зависимость от настенного времени — так что тест детерминирован с первого прогона, а не ретраится." },
    { label: "Они увеличивают таймаут теста, давая больше времени", misconception: "Поднятие таймаута лишь маскирует медлительность и всё ещё зависит от реального времени, так что гонка остаётся. Поддельные таймеры полностью убирают реальное ожидание, управляя виртуальным временем." },
  ]}
/>

<DragOrder
  id="06-unit-testing-drag-1"
  lessonSlug="01-unit-testing"
  lang="ru"
  prompt="Расставь шаги, чтобы написать один детерминированный юнит-тест ретрая на setTimeout:"
  items={[
    { id: "a", label: "Включи поддельные таймеры: mock.timers.enable({ apis: ['setTimeout'] })" },
    { id: "b", label: "Arrange: собери субъект и заглуши его границу (например, шпион fetch: сначала падает, потом успех)" },
    { id: "c", label: "Act: вызови функцию ретрая (она планирует setTimeout вместо ожидания)" },
    { id: "d", label: "Продвинь виртуальное время: mock.timers.tick(5000), чтобы сработал запланированный колбэк" },
    { id: "e", label: "Ассерти поведение: шпион вызван ожидаемое число раз, а результат зарезолвился" },
  ]}
  correctOrder={["a", "b", "c", "d", "e"]}
/>

</Explanation>

<FlowDiagram
  label="Жизненный цикл одного прогона теста — от обнаружения до сноса"
  hue="var(--d-systems)"
  perRow={4}
  nodes={[
    { id: "d", label: "обнаружение", sub: "node --test" },
    { id: "be", label: "beforeEach", sub: "свежее состояние" },
    { id: "aaa", label: "arrange/act/assert", sub: "тело теста" },
    { id: "ae", label: "afterEach", sub: "восстановить моки" },
  ]}
  edges={[
    { from: "d", to: "be", label: "на каждый тест" },
    { from: "be", to: "aaa" },
    { from: "aaa", to: "ae", label: "успех или сбой" },
  ]}
  caption="node --test находит файлы, затем на каждый тест запускает beforeEach для свежего состояния, тело Arrange-Act-Assert и afterEach для сноса — восстанавливая методы t.mock и реальные часы, чтобы следующий тест стартовал чистым и независимым."
/>

<KeyTakeaway>Бери node:test ради быстрых тестов без зависимостей; Vitest — для богатых матчеров и watch на HMR. Тестируй поведение по Arrange-Act-Assert, держи тесты независимыми, подделывай таймеры — и таймаут на 5с идёт мгновенно.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="06-unit-testing-retrieval"
  lang="ru"
  questions={[
    { q: "Почему подделка таймеров превращает медленный, флакающий тест таймаута в быстрый и детерминированный — и как это сделать через node:test?", a: "Тест, который реально ждёт setTimeout, зависит от настенных часов, что делает его медленным (бэкофф на 5с реально длится 5с) и флакающим: под нагрузкой CI таймер может сработать позже, а ассерт обгоняет колбэк, так что тест проходит чаще всего и иногда падает. Поддельные таймеры убирают реальное время из уравнения. С node:test ты вызываешь mock.timers.enable({ apis: ['setTimeout', 'Date'] }), чтобы поменять реальные часы на управляемые виртуальные, запускаешь тестируемый код (который планирует таймер вместо ожидания), затем зовёшь mock.timers.tick(5000), чтобы мгновенно продвинуть виртуальное время на 5000мс. Это синхронно срабатывает всеми таймерами в окне без реального ожидания и без гонки, так что тот же ассерт теперь и мгновенный (микросекунды), и на 100% детерминированный. Vitest делает то же через vi.useFakeTimers() и vi.advanceTimersByTime(5000). Это самый рычажный приём для самой медленной и флакающей категории бэкенд-юнит-тестов." },
    { q: "Когда выбирать node:test вместо Vitest и наоборот?", a: "Выбирай node:test, когда хочешь ноль зависимостей и быстрейший холодный старт: он встроен в Node (стабилен с Node 20), говорит на core API (test/describe/it, node:assert/strict, mock.fn, t.mock.method, поддельные таймеры, встроенное покрытие через --experimental-test-coverage) и не добавляет supply-chain-поверхность — идеален для публикуемой библиотеки или бэкенда с малым числом зависимостей. Выбирай Vitest, когда хочешь богатую эргономику разработчика: цепочечные матчеры expect в стиле Jest, инлайн-снапшоты, vi.mock, среду jsdom/happy-dom для DOM-кода, TypeScript без лишнего конфига и быстрый watch на HMR, перезапускающий только затронутые тесты — идеален для приложений, фронтенда или кода, трогающего DOM, и монорепо. Vitest почти полностью совместим с Jest (vi.fn↔jest.fn, vi.mock↔jest.mock), так что это ещё и стандартный способ сбежать от медленной настройки Jest. Решает вес зависимостей и потребности в матчерах/DOM, а не сама корректность — оба раннера способны." },
  ]}
/>

<Recap lang="ru">
Node несёт стабильный раннер тестов без зависимостей: пиши тесты через `test()` или `describe()`/`it()`, проверяй через `node:assert/strict` (бери строгий импорт, чтобы `equal` означал `===`) и запускай через `node --test`, который находит файлы `*.test.js` и поддерживает `--watch` и `--experimental-test-coverage`; хуки (`before`/`after`/`beforeEach`/`afterEach`) поднимают и сносят состояние. Сбои, которые ожидаешь, проверяй через `assert.throws` и `await assert.rejects`, а мокай только границы — `mock.fn`, `t.mock.method` и особенно поддельные таймеры (`mock.timers.tick`), которые превращают тест реального пятисекундного бэкоффа в мгновенный и детерминированный, продвигая виртуальное время вместо ожидания настенных часов. Бери Vitest, когда нужны матчеры `expect` в стиле Jest, снапшоты, среда DOM, TypeScript без конфига и быстрый watch на HMR — он почти полностью совместим с Jest — и держи node:test дефолтом для библиотек и бэкендов с лёгкими зависимостями. Что ни выбери, принципы одни: тестируй поведение, а не реализацию, чтобы рефакторинги оставались зелёными, следуй Arrange-Act-Assert с одним исходом на тест и держи каждый тест независимым от разделяемого изменяемого состояния и порядка прогона, иначе сюита флакнет и её заглушат — а именно так реальная регрессия проскальзывает мимо единственного покрывавшего её теста.
</Recap>
