---
concepts:
- event-loop-lag
- loop-phases
- monitor-event-loop-delay
- performance-observer
- tail-latency
- load-shedding
deepensInto: []
estMin: 18
lang: ru
lessonType: topic
level: middle
order: 1
prereqs: []
slug: 01-event-loop-monitoring
sources:
- https://nodejs.org/api/perf_hooks.html
- https://nodejs.org/en/learn/asynchronous-work/event-loop-timers-and-nexttick
- https://nodejs.org/en/learn/asynchronous-work/dont-block-the-event-loop
spiral: []
status: ready
summary: 'Node гонит JS в одном потоке, поэтому один медленный синхронный вызов тормозит
  все таймеры и I/O-колбэки в очереди. Мерь задержку event loop через monitorEventLoopDelay,
  следи за p99, а не средним, и выноси CPU с потока.'
title: 'Мониторинг задержки event loop'
track: node
unit: 04-performance
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";
import Infographic from "~/components/diagram/Infographic.astro";
import meanVsTail from "./infographics/mean-vs-tail.svg?raw";

<Hook>
Эндпоинт экспорта добавил «маленькую» фичу: сжимать ответ синхронным `zlib.gzipSync` перед отправкой. В нагрузочных тестах по этому одному маршруту всё было нормально — 40мс, зелёный. В проде p99 латентность на *каждом* эндпоинте — health-чеки, логины, не связанные чтения — прыгала с 8мс до 600мс, как только бежал экспорт. Никто не трогал те маршруты. Дашборд маршрута экспорта выглядел здоровым, потому что медленный путь был CPU-всплеском, который бежал *между* запросами, замораживая единственный поток на ~250мс за раз. Команда пялилась в графики латентности по маршрутам, а реальный сигнал был одним числом, которое они не писали: задержкой event loop.
</Hook>

<Crux>Один блокирующий вызов в единственном потоке Node тормозит все таймеры и I/O-колбэки — следи за p99 задержки event loop, не за средним.</Crux>

<Explanation>

## Почему одна медленная задача замораживает всё

Node гонит твой JavaScript в одном потоке. Event loop — это цикл, который берёт следующий готовый колбэк (истёкший таймер, завершённый I/O, зарезолвленный промис), выполняет его *до конца*, и только потом ищет следующий. Вытеснения нет: пока твой колбэк выполняется, ничто другое в процессе бежать не может. Поэтому один синхронный CPU-всплеск или блокирующий вызов — `zlib.gzipSync`, `crypto.pbkdf2Sync`, `JSON.parse` на 5МБ, `fs.readFileSync` по холодному диску — не просто замедляет эту одну операцию. Он держит поток, и каждый таймер, каждое чтение сокета, каждый колбэк HTTP в очереди ждут за ним. Это время ожидания — **задержка event loop** (event-loop lag): разрыв между тем, когда колбэк *должен был* выполниться, и когда loop до него реально добрался.

Под конкурентностью этот сбой жесток. С одним запросом в полёте блок на 250мс стоит этому одному запросу 250мс. С 200 запросами в полёте блок падает в середину очереди и *добавляет до 250мс латентности целому куску этих 200* — ни один из которых не сделал ничего плохого. Вот почему дашборды по маршрутам из хука врали: собственная латентность маршрута экспорта выглядела нормальной, но он травил tail latency (задержки на хвосте распределения — p99 и выше) всего остального. Медленный код и медленный симптом были на разных графиках.

```js
import http from "node:http";
import zlib from "node:zlib";

http.createServer((req, res) => {
  // ❌ блокирует единственный поток на всё сжатие — колбэк каждого
  // другого запроса в полёте ждёт за этим.
  const body = zlib.gzipSync(bigPayload);   // ~250мс чистого CPU
  res.setHeader("content-encoding", "gzip");
  res.end(body);
}).listen(3000);
```

## Фазы loop (и где копится задержка)

Loop бежит фиксированными фазами, каждая сливает свою очередь колбэков перед переходом дальше: **timers** (колбэки `setTimeout`/`setInterval`, чьё время вышло) → **pending callbacks** (часть отложенных системных колбэков) → **poll** (где забираются завершения I/O и где loop *блокируется в ожидании* I/O, если больше ничего не готово) → **check** (колбэки `setImmediate`) → **close** (например, `socket.on('close')`). Между *каждым* колбэком Node сливает очереди микротасков: сначала `process.nextTick`, потом зарезолвленные промисы.

Практическое следствие: задержка копится везде, где колбэк засиделся. Тяжёлое тело `setTimeout` задержит следующий таймер; тяжёлый `.then` промиса задержит следующий I/O, который фаза poll хотела доставить. А `process.nextTick` — острый край: поскольку он сливается *перед* промисами и *до того, как loop сменит фазу*, рекурсивный `nextTick` может полностью заморить loop, навсегда блокируя I/O, делая «крошечную» работу на каждом тике.

```js
// process.nextTick сливается до того, как loop продолжит — рекурсия здесь
// полностью морит I/O, хотя каждый вызов выглядит тривиально.
function spin() { process.nextTick(spin); }
spin();              // ❌ фаза poll больше никогда не выполнится
```

## Измерение задержки: monitorEventLoopDelay

Правильный инструмент в ядре: `perf_hooks.monitorEventLoopDelay`. Он семплит loop по таймеру с фиксированным `resolution` (по умолчанию **10мс**) и пишет в высокоточную **гистограмму**, насколько поздно был каждый семпл. Ты читаешь с неё перцентили — и перцентили это весь смысл, потому что задержка взрывная: среднее в 2мс может прятать p99 в 400мс во время компакции или GC-паузы. **Следи за хвостом (`p99`/`max`), не за средним.** Все значения в **наносекундах**.

<Infographic
  svg={meanVsTail}
  label="Двухколоночное сравнение одного и того же окна измерения задержки event loop: среднее показывает около 2 мс и выглядит здоровым, а p99/max с гистограммы вскрывает хвост в 400 мс — число, отслеживающее то, что реально чувствуют пользователи."
  caption="Одно окно, две истории: среднее усредняет тысячи семплов около нуля до ~2 мс, а p99/max с наносекундной гистограммы вскрывает хвост в 400 мс. Алертись по хвосту, а не по среднему."
  hue="var(--d-infra)"
/>

```js
import { monitorEventLoopDelay } from "node:perf_hooks";

const h = monitorEventLoopDelay({ resolution: 20 }); // семплить каждые 20мс
h.enable();

setInterval(() => {
  console.log({
    meanMs: (h.mean / 1e6).toFixed(2),       // здоровое: меньше миллисекунды
    p99Ms:  (h.percentile(99) / 1e6).toFixed(2),
    maxMs:  (h.max / 1e6).toFixed(2),
  });
  h.reset();                                  // начать свежее окно
}, 1000).unref();
```

Наивная альтернатива — **дрейф таймера**: запланируй `setInterval(fn, 100)` и измерь, насколько позже 100мс реально срабатывает каждый тик; перебор и есть задержка. Это дёшево и без зависимостей, но грубо — он семплит только с твоим интервалом и не даёт гистограммы, так что упускает всплески внутри интервала и не скажет тебе p99. Для прода `monitorEventLoopDelay` строго лучше. Дальше библиотеки вроде `toobusy-js` непрерывно опрашивают задержку и дают **сбрасывать нагрузку** (shed load): когда задержка пересекает порог, сразу отклоняй новые запросы с `503` вместо того, чтобы брать работу, которую не сможешь обслужить, держа уже принятые запросы быстрыми.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Симптом</th>
        <th class="border border-border px-3 py-2 text-left">Метрика, что его вскрывает</th>
        <th class="border border-border px-3 py-2 text-left">Инструмент</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">Не связанные маршруты прыгают вместе</td>
        <td class="border border-border px-3 py-2"><strong>p99 / max</strong> задержки loop (не среднее)</td>
        <td class="border border-border px-3 py-2">гистограмма <code>monitorEventLoopDelay()</code></td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Одна операция медленна на горячем пути</td>
        <td class="border border-border px-3 py-2">длительность размеченного спана</td>
        <td class="border border-border px-3 py-2"><code>performance.mark/measure</code> + <code>PerformanceObserver</code></td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Сервис перегружен, хвост взрывается</td>
        <td class="border border-border px-3 py-2">живая задержка vs порог</td>
        <td class="border border-border px-3 py-2"><code>toobusy-js</code> → сбрасывай нагрузку <code>503</code></td>
      </tr>
    </tbody>
  </table>
</div>

## Хронометраж подозреваемого: performance.mark, measure, observe

Когда задержка высока, всё равно надо найти, *какой* вызов блокирует. `perf_hooks.performance` даёт высокоточные часы и структурный способ мерить спаны без арифметики `Date.now()`. Используй `performance.mark(name)`, чтобы ставить отметки времени, `performance.measure(name, start, end)`, чтобы записать длительность между двумя отметками, и `PerformanceObserver`, чтобы получать эти записи по мере их выпуска — так хронометраж развязан с горячим путём.

```js
import { performance, PerformanceObserver } from "node:perf_hooks";

const obs = new PerformanceObserver((list) => {
  for (const e of list.getEntries()) {
    if (e.duration > 50) console.warn(`SLOW ${e.name}: ${e.duration.toFixed(1)}ms`);
  }
});
obs.observe({ entryTypes: ["measure"] });

performance.mark("gzip:start");
const body = zlib.gzipSync(bigPayload);
performance.mark("gzip:end");
performance.measure("gzip", "gzip:start", "gzip:end"); // появится в observer
```

Для разовых проверок `performance.now()` — это монотонный, субмиллисекундный таймер (не зависит от смены настенного времени): `const t = performance.now(); …; performance.now() - t` — правильный способ микро-замерить подозрительный вызов.

<Inset kind="why" lang="ru">
Почему наносекунды и гистограмма, а не одно среднее? Потому что задержка event loop — учебниковый случай метрики, где среднее врёт. Большинство семплов около нуля, так что среднее остаётся крошечным, даже пока горстка блоков по 300мс в минуту крушит реальных пользователей. Гистограмма хранит *распределение*, так что `percentile(99)` отвечает на важный вопрос — «насколько плохо невезучему 1% колбэков?» — а это ровно та популяция, из которой сделан твой tail latency.
</Inset>

<TradeoffMatrix
  id="04-event-loop-monitoring-tradeoff-1"
  lessonSlug="01-event-loop-monitoring"
  lang="ru"
  prompt="Эндпоинт должен хешировать пароли и, отдельно, рендерить большой PDF — оба CPU-тяжёлые. Задержка loop скачет, не связанные маршруты тормозят. Какой сеньорский фикс?"
  options={[
    { name: "Вынести CPU-работу с loop: async crypto API для хеширования и пул worker_threads для PDF", summary: "Off-thread / async.", correct: true, justification: "Это единственный фикс, бьющий в корневую причину — единственный поток держится синхронной CPU-работой. Async crypto API отдаёт хеширование пулу потоков libuv, а пул worker_threads рендерит PDF на отдельном потоке V8, так что главный loop продолжает сливать таймеры и I/O. Задержка падает обратно к субмиллисекунде, и не связанные маршруты восстанавливаются, потому что на горячем пути больше ничто не блокирует loop." },
    { name: "Поднять UV_THREADPOOL_SIZE, чтобы больше работы шло параллельно", summary: "Больший пул.", justification: "Это помогает только операциям, которые и так используют пул потоков libuv (fs, dns, часть crypto), и даже тогда добавляет потоки, а не запас loop. Синхронный gzipSync/pbkdf2Sync или CPU-цикл в твоём JS никогда не трогает пул потоков — он бежит на главном потоке независимо от размера пула, так что задержка не меняется. Лечит симптом не на том слое." },
    { name: "Увеличить число воркеров кластера Node / подов", summary: "Масштаб вширь.", justification: "Горизонтальное масштабирование разносит нагрузку по процессам, что маскирует проблему, пока трафик не вырастет — но каждый отдельный воркер всё равно замораживает свой event loop во время CPU-всплеска, так что его tail latency остаётся ужасным, и ты просто купил больше машин под тот же стопор. Это рычаг стоимости, а не фикс; сочетай с off-thread, а не заменяй им." },
    { name: "Добавить кэш, чтобы медленный путь бежал реже", summary: "Кэшировать результат.", justification: "Кэш ценен и снижает, как часто ты платишь цену, но первый запрос (промах кэша) и любой некэшируемый вход всё равно блокируют loop на весь CPU-всплеск — а stampede промахов вернёт стопор в худший момент. Он сужает окно сбоя, не убирая сбой, так что дополняет off-thread, а не заменяет его." },
  ]}
/>

<Quiz
  id="04-event-loop-monitoring-quiz-1"
  lessonSlug="01-event-loop-monitoring"
  lang="ru"
  question="Среднее задержки loop — 1.5мс, но пользователи жалуются на случайные медленные запросы. Почему среднее тут вводит в заблуждение и на что смотреть?"
  choices={[
    { label: "Задержка взрывная: большинство семплов около нуля, так что среднее крошечное, а редкие блоки по 200–400мс (GC, sync CPU) живут в хвосте — смотри percentile(99) и max с гистограммы", correct: true },
    { label: "Среднее неверно; monitorEventLoopDelay даёт его в секундах, так что дели на 1000", misconception: "monitorEventLoopDelay даёт наносекунды, не секунды, и само среднее не неверно — это просто не та статистика. Настоящее среднее 1.5мс может сосуществовать с p99 в 400мс; нужен перцентиль, а не фикс единиц." },
    { label: "Средние всегда ненадёжны, так что переключись на логирование сырой задержки каждого колбэка", misconception: "Логировать задержку каждого колбэка — огромный оверхед, и ты всё равно остаёшься глазеть на шум. Гистограмма уже дёшево хранит распределение — читай percentile(99) и max вместо сброса сырых семплов." },
    { label: "Медленные запросы — проблема на стороне клиента, а не задержка loop", misconception: "Здорово выглядящее среднее рядом с высоким p99 — классический отпечаток серверных стопоров loop, а не клиентских проблем. Проверь хвост гистограммы задержки до того, как винить клиента." },
  ]}
/>

<Quiz
  id="04-event-loop-monitoring-quiz-2"
  lessonSlug="01-event-loop-monitoring"
  lang="ru"
  question="Какая строка, поставленная на горячий путь запроса, наиболее прямо раздует задержку event loop для ВСЕХ конкурентных запросов?"
  choices={[
    { label: "const hash = crypto.pbkdf2Sync(pw, salt, 600000, 32, 'sha256') — синхронный, CPU-связанный вызов, держащий единственный поток до завершения", correct: true },
    { label: "await pool.query('SELECT 1') — заавейченный async-запрос к БД", misconception: "Заавейченный async-запрос уступает loop, пока ждёт I/O — поток свободен выполнять другие колбэки во время round trip. Async, неблокирующие вызовы — ровно то, что держит задержку низкой; виноват синхронный CPU-вызов." },
    { label: "await fetch('https://api.example.com/x') — исходящий HTTP-запрос", misconception: "Заавейченный fetch — это сетевой I/O: он приостанавливается и даёт loop обслуживать другую работу, пока не придёт ответ. Он добавляет латентность одному этому запросу, а не задержку всем. Блокирующий нарушитель — синхронный CPU-вызов." },
    { label: "setImmediate(() => doSmallThing()) — откладывание крошечной работы в фазу check", misconception: "setImmediate откладывает работу в фазу check следующей итерации loop, что уступает между запросами, а не блокирует их. С тривиальной работой он снимает давление; именно синхронный pbkdf2Sync пришпиливает поток." },
  ]}
/>

<DragOrder
  id="04-event-loop-monitoring-drag-1"
  lessonSlug="01-event-loop-monitoring"
  lang="ru"
  prompt="Расставь шаги диагностики высокой tail latency, которую подозреваешь как задержку event loop, — от первого сигнала до устойчивого фикса:"
  items={[
    { id: "a", label: "Замечаешь, что много не связанных маршрутов прыгают по p99 вместе при здоровых средних" },
    { id: "b", label: "Включаешь monitorEventLoopDelay и подтверждаешь, что высок p99/max задержки, а не среднее" },
    { id: "c", label: "Ставишь performance.mark/measure вокруг подозрительных вызовов, чтобы найти, какой блокирует" },
    { id: "d", label: "Выносишь блокирующую работу с потока (async API / worker_threads) или режешь её на куски" },
    { id: "e", label: "Перемеряешь: подтверждаешь, что p99 задержки вернулся к субмиллисекунде, и добавляешь сброс нагрузки как страховку" },
  ]}
  correctOrder={["a", "b", "c", "d", "e"]}
/>

</Explanation>

<FlowDiagram
  label="Один блокирующий колбэк держит единственный поток, пока очередь ждёт"
  hue="var(--d-systems)"
  perRow={4}
  nodes={[
    { id: "q", label: "очередь готовых", sub: "таймеры + I/O" },
    { id: "r", label: "loop берёт один", sub: "до конца" },
    { id: "b", label: "блок-всплеск", sub: "gzipSync ~250мс" },
    { id: "w", label: "все ждут", sub: "задержка p99 ↑" },
  ]}
  edges={[
    { from: "q", to: "r" },
    { from: "r", to: "b", label: "берёт" },
    { from: "b", to: "w", label: "нет вытеснения" },
  ]}
  caption="Loop выполняет каждый готовый колбэк до конца без вытеснения. Когда выбранный колбэк — синхронный CPU-всплеск, любой другой колбэк, уже в очереди готовых, застрял за ним — это ожидание и есть задержка event loop, и она ложится на p99 каждого конкурентного запроса, а не только медленного."
/>

<KeyTakeaway>Node гонит JS в одном потоке, поэтому один sync CPU или блокирующий вызов тормозит все колбэки в очереди. Мерь через `monitorEventLoopDelay`, следи за p99/max, а не средним, и выноси CPU с потока.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="04-event-loop-monitoring-retrieval"
  lang="ru"
  questions={[
    { q: "Почему среднее задержки event loop прячет проблему и что писать вместо него?", a: "Задержка event loop взрывная, а не ровная: подавляющее большинство семплов около нуля (loop простаивает или гонит тривиально короткие колбэки), и лишь несколько в минуту велики — пауза GC, синхронный JSON.parse, sync crypto или zlib, замораживающие поток на сотни миллисекунд. Усредни их вместе, и среднее остаётся уютно субмиллисекундным, даже пока реальные пользователи едят стопоры по 300мс, потому что горстка огромных значений едва двигает среднее, в котором доминируют тысячи крошечных. Среднее отвечает «как loop на типичном тике», но интересующая популяция — невезучие колбэки в конце распределения. Вот почему monitorEventLoopDelay возвращает гистограмму, а не одно число: ты читаешь percentile(99) и max (в наносекундах), чтобы видеть хвост, и делаешь reset() на окно, чтобы всплески не размазывались по времени. Следи за хвостом и алертись по нему — p99 это число, которое отслеживает то, что пользователи реально чувствуют." },
    { q: "Эндпоинт делает синхронную CPU-тяжёлую задачу (sync gzip / pbkdf2) и проваливает throughput для всех клиентов. В чём фикс и почему он не блокирует?", a: "Корневая причина в том, что синхронная CPU-работа бежит в том самом одном потоке, что гонит и event loop, так что на всю её длительность ничто другое — ни таймер, ни завершение I/O, ни колбэк другого запроса — не может продвинуться; это и есть задержка. Фикс — убрать CPU-работу с этого потока. Два пути: использовать асинхронный core API, где он есть (crypto.pbkdf2 / scrypt async, zlib.gzip async), так что работа отдаётся пулу потоков libuv и главный loop свободен обслуживать другие запросы во время неё; или, для чисто-JS CPU-работы без async API, гонять её в пуле worker_threads, который выполняется на отдельном потоке V8 и шлёт результат обратно сообщением. Для очень большой, но делимой работы можно также резать её через границы setImmediate, чтобы loop переплетал другие колбэки между кусками. После изменения перемеряй через monitorEventLoopDelay: верный фикс возвращает p99 к субмиллисекунде. Добавь сброс нагрузки (например, toobusy-js, отдающий 503 выше порога задержки) как страховку, чтобы будущая регрессия деградировала мягко, а не стопорила всех." },
  ]}
/>

<Recap lang="ru">
Node гонит твой JavaScript в одном потоке без вытеснения, так что event loop выполняет каждый готовый колбэк до конца перед тем, как тронуть следующий — а значит один синхронный CPU-всплеск или блокирующий вызов (`zlib.gzipSync`, `pbkdf2Sync`, `JSON.parse` на много МБ, sync `fs`) замораживает весь процесс и заставляет каждый таймер и I/O-колбэк в очереди ждать за ним. Это ожидание — задержка event loop, и под конкурентностью она ложится на p99 *каждого* запроса в полёте, а не только медленного, поэтому графики латентности по маршрутам могут выглядеть здоровыми, пока сервис горит. Loop сливает фиксированные фазы — timers → pending → poll (где блокируется ради I/O) → check (`setImmediate`) → close — и микротаски (`process.nextTick`, потом промисы) между каждым колбэком, так что рекурсивный `nextTick` может полностью заморить I/O. Мерь задержку через `perf_hooks.monitorEventLoopDelay({ resolution })`, который пишет наносекундную гистограмму, читаемую через `.mean`, `.max` и `.percentile(99)` и `.reset()` на окно — и всегда следи за хвостом, потому что среднее взрывного сигнала это ложь. Локализуй виновный вызов через `performance.mark`/`measure` и `PerformanceObserver` (или `performance.now()` для разового замера), затем чини, вынося CPU-работу с loop через async core API или `worker_threads`, либо нарезая её через `setImmediate`. Субмиллисекундный p99 задержки — здоровье; десятки-сотни мс означают, что пользователи это чувствуют — так что алертись по p99, а не по среднему, и добавь сброс нагрузки, чтобы перегрузка деградировала в честные `503`, а не в замороженный поток. Теперь, когда несвязанные маршруты начинают прыгать вместе, твой первый вопрос: каков p99 задержки event loop — и какой синхронный вызов держит поток?
</Recap>
