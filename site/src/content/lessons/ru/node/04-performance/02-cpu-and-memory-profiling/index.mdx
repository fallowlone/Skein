---
concepts:
- cpu-profiling
- flame-graphs
- heap-snapshots
- shallow-vs-retained
- gc-and-heap-limit
- leak-triage
deepensInto: []
estMin: 19
lang: ru
lessonType: topic
level: senior
order: 2
prereqs: []
slug: 02-cpu-and-memory-profiling
sources:
- https://nodejs.org/en/learn/diagnostics/memory/using-heap-snapshot
- https://nodejs.org/en/learn/getting-started/profiling
- https://nodejs.org/api/v8.html
spiral: []
status: ready
summary: 'CPU-профиль показывает, куда уходит время — широкие кадры flame-графа горячие.
  Утечка — это old space, который растёт через GC и не падает; ищи её тремя heap-снимками,
  гонись за retained size и retainer path, потом ограничь кэш или сними слушателя.'
title: 'Профили CPU и охота на утечки по heap-снимкам'
track: node
unit: 04-performance
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
Сервис оформления заказов начало OOM-килить каждые 14 часов, как по часам. Первым рефлексом команды было поднять `--max-old-space-size` с 2048 до 4096 — и краш просто переехал на каждые 28 часов. RSS рос ровной прямой через каждую сборку мусора и никогда не возвращался вниз, а это сигнатура утечки, а не проблемы тюнинга. Три heap-снимка спустя ответ оказался однострочным: модульный `Map` в роли кэша ответов, с ключом по id запроса и без вытеснения. Каждый запрос добавлял одну запись, которую ничто никогда не удаляло. «Фикс» с удвоением кучи лишь купил время, чтобы тот же `Map` вырос вдвое больше перед тем, как снова убить процесс. За следующие двадцать минут ты научишься читать CPU-профиль, снимать три heap-снимка и идти по retainer-цепочке прямо к виновнику.
</Hook>

<Crux>Широкие кадры flame-графа горячие (ширина = self time); утечка — это old space, растущий через GC: ищи её тремя снимками.</Crux>

<Explanation>

## Профилирование CPU: найди широкий кадр

Когда сервис тормозит или жрёт ядро, ты не гадаешь — ты сэмплируешь. **CPU-профиль** записывает, какая функция на стеке в каждый интервал сэмплирования, так что функция, появляющаяся чаще всех, и есть место, куда реально уходит время. Самый быстрый путь в Node — встроенный флаг `--cpu-prof`: он пишет файл `.cpuprofile` при выходе, который открывается во вкладке **Performance** Chrome DevTools. Там ты читаешь **flame-граф**, где каждый прямоугольник — кадр стека, а **ширина = self time** — самые широкие кадры и есть твой горячий путь, точка. Глубина — это лишь вложенность вызовов; игнорируй её и гонись за шириной.

```bash
# пишет isolate-*.cpuprofile при чистом выходе; открой в Chrome DevTools → Performance
node --cpu-prof --cpu-prof-dir=./prof server.js

# классический tick-процессор V8: текстовый отчёт, без GUI
node --prof server.js
node --prof-process isolate-*.log > processed.txt
```

`--prof` плюс `node --prof-process` дают те же сэмплированные данные в виде **текстового tick-отчёта** — удобно на headless-машине, где DevTools не открыть. Инструменты выше оборачивают это: **0x** генерит интерактивный flame-граф одной командой, а **clinic flame** делает то же с проводящим тебя сценарием. Все они — **сэмплирующие** профайлеры (снимают стек каждые N микросекунд, дёшево, ~1–2% накладных) в отличие от **инструментирования** (оборачивают каждый вызов, точные счётчики, но настолько тяжёлые, что искажают сами тайминги, которые ты меришь). Для боевой диагностики почти всегда нужно сэмплирование. Классическая находка: regex или глубокий `JSON.parse` всплывает одним жирным кадром, съедающим 40% self time, — оптимизируй его, закэшируй или вынеси с главного потока в Worker.

## Heap-снимки: охота на утечку тремя снимками

CPU-профиль утечку не найдёт; для этого ты фотографируешь кучу. **Heap-снимок** — это полный граф каждого живого объекта и того, кто на кого указывает. Сделай один через `v8.writeHeapSnapshot()` изнутри процесса, сними по требованию через `--heapsnapshot-signal=SIGUSR2` (затем `kill -USR2 <pid>`) или возьми из вкладки **Memory** в DevTools. Один снимок говорит, что большое *сейчас*; он не скажет, что *течёт*. Для этого ты применяешь **технику трёх снимков**.

```js
import v8 from "node:v8";

// снимок 1 — базовый, после прогрева
v8.writeHeapSnapshot("./snap-1.heapsnapshot");
await exerciseSuspectPath({ times: 5000 }); // долби подозрительный маршрут
// снимок 2 — после нагрузки
v8.writeHeapSnapshot("./snap-2.heapsnapshot");
await exerciseSuspectPath({ times: 5000 }); // сделай ещё раз
v8.writeHeapSnapshot("./snap-3.heapsnapshot");
```

Загрузи все три во вкладку Memory, переключи дропдаун на **Comparison** и используй **«Objects allocated between snapshots»**: всё, чей счётчик только **растёт** от снимка 1 → 2 → 3 и никогда не падает после GC, — это твоя утечка. Стабильная нагрузка должна выходить на «пилу» — выделить, собрать, повторить — поэтому ровная восходящая лестница и есть улика. Следующая колонка важнее всего: не **shallow size** (собственные байты объекта), а **retained size** — память, которая освободилась бы, если освободить этот объект, то есть всё, что только он держит живым. Сортируй по retained size, выбери конструктор, чей счётчик вырос, и иди по его **retainer path** (цепочке ссылок, держащих его) прямо к кэшу, массиву или слушателю, который не отпускает.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Источник утечки</th>
        <th class="border border-border px-3 py-2 text-left">Сигнатура в снимке</th>
        <th class="border border-border px-3 py-2 text-left">Фикс</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">Безграничный кэш <code>Map</code>/массив без вытеснения</td>
        <td class="border border-border px-3 py-2">Счётчик одного конструктора растёт каждый снимок; огромный retained size на одном <code>Map</code></td>
        <td class="border border-border px-3 py-2">Ограничь: LRU с max-размером / TTL или <code>WeakMap</code>, если ключ — объект</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Забытые слушатели событий</td>
        <td class="border border-border px-3 py-2">Счётчик слушателей/замыканий растёт; <code>MaxListenersExceededWarning</code> в логах</td>
        <td class="border border-border px-3 py-2"><code>removeListener</code> / <code>off</code> при teardown; <code>once</code> для одноразовых</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Замыкание, захватившее большой объект</td>
        <td class="border border-border px-3 py-2">Retainer path идёт через контекст замыкания, держащий большой буфер/граф</td>
        <td class="border border-border px-3 py-2">Обнули ссылку; не замыкай больше, чем нужно</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Модульный глобал, который только растёт</td>
        <td class="border border-border px-3 py-2">Верхнеуровневый массив/Set растёт монотонно; не сжимается через GC</td>
        <td class="border border-border px-3 py-2">Ограничь, периодически сбрасывай или вынеси состояние из модульной области</td>
      </tr>
    </tbody>
  </table>
</div>

<Quiz
  id="04-profiling-quiz-1"
  lessonSlug="02-cpu-and-memory-profiling"
  lang="ru"
  question="В heap-снимке ты находишь запись кэша, у которой shallow size 48 байт, а retained size 90 МБ. О чём это говорит?"
  choices={[
    { label: "Сам объект крошечный, но именно он единолично держит живыми ~90 МБ — освободить его (например, вытеснить ключ кэша) значит вернуть эти 90 МБ, так что это и есть retainer, за которым гнаться", correct: true },
    { label: "Объект повреждён, потому что два размера расходятся", misconception: "Тут нет ничего повреждённого. Shallow size — собственные байты объекта; retained size — всё, что освободилось бы при его освобождении. Маленький объект, удерживающий огромный граф, — это ровно нормальная форма корня утечки." },
    { label: "Retained size для кэшей всегда неверен, его надо игнорировать", misconception: "Retained size — это именно то поле, что нужно для охоты на утечку: это память, которую ты вернёшь. Shallow size (48 байт) сам по себе тут почти бесполезен." },
    { label: "Нужно поднять --max-old-space-size, чтобы вместить 90 МБ", misconception: "Поднятие лимита кучи лишь отложит OOM; 90 МБ держатся из-за ссылки, а не из-за того, что куча мала. Обрежь retainer (вытесни ключ), и память освободится на следующем GC." },
  ]}
/>

## GC, лимит кучи и что на самом деле значит «утечка»

Прежде чем тянуться к `--max-old-space-size`, спроси себя: возвращается ли память *обратно* после полного GC, или пол только растёт? Ответ на этот вопрос разделяет проблему провижена и утечку — а фиксы у них принципиально разные.

Куча V8 **поколенческая**. Новые выделения попадают в **new space**, собираемое **Scavenge** — быстрым, частым, копирующим выживших наружу. Объекты, пережившие пару Scavenge, повышаются в **old space**, собираемое медленным **Mark-Sweep-Compact**. Практическое определение утечки выпадает прямо отсюда: **old space (и RSS — Resident Set Size, суммарная физическая память процесса), который растёт через полные GC и никогда не возвращается вниз**. Здоровый процесс пилит; текущий — лестничит. Смотри это напрямую через `--trace-gc`, который печатает по строке на сборку с размерами до/после.

```bash
node --trace-gc server.js
# [12345:0x...] 4210 ms: Mark-Sweep 1850.2 (2048.0) -> 1844.9 (2048.0) MB
#                                    ^ после полного GC почти не упало → утечка
```

Жёсткий потолок — **`--max-old-space-size=<МБ>`**. На 64-битном Node дефолт old space большой, но ограниченный (исторически ~2 ГБ; зависит от платформы и версии), и реальная нагрузка с большой легитимной кучей может потребовать его поднять. Но поднятие — это ручка тюнинга, **а не фикс утечки**: как показывает хук, удвоение лимита лишь удваивает время до OOM. Диагностический вопрос всегда один: возвращается ли память после полного GC? Если да — ты недопровиженен, подними лимит. Если нет — у тебя утечка, снимай снимки.

<Inset kind="why" lang="ru">
Почему необработанный, вечно растущий `Map` полностью побеждает GC? Сборка мусора освобождает только **недостижимое**. Модульный `Map` достижим всю жизнь процесса, и каждое значение, что ты в него кладёшь, достижимо *через* Map — так что с точки зрения V8 ничто из этого никогда не мусор. Сборщик работает идеально; ты просто сказал ему, удерживая ссылку, что всё это живое. Вот почему фикс — никогда не «тюнить GC», а «убрать ссылку»: добавь вытеснение, чтобы старые ключи стали недостижимыми и следующий Mark-Sweep смог их вернуть.
</Inset>

<TradeoffMatrix
  id="04-profiling-tradeoff-1"
  lessonSlug="02-cpu-and-memory-profiling"
  lang="ru"
  prompt="Боевой API на headless-сервере временами упирает одно ядро CPU. Открыть против него Chrome DevTools нельзя, и ты хочешь самый малотрудный способ увидеть горячую функцию. За что хватаешься первым?"
  options={[
    { name: "node --prof, затем node --prof-process ради текстового tick-отчёта", summary: "Встроено, текст, дружит с headless.", correct: true, justification: "Это верный первый ход на машине без GUI. --prof встроен в Node (нулевая установка), дёшево сэмплирует стек, а --prof-process рендерит простой текстовый отчёт, который читается по SSH — верх секций «Summary» и «bottom up» называет самые горячие функции. Добавить ничего не стоит и работает везде, где работает Node, — ровно то, что нужно для первичной диагностики на headless-сервере." },
    { name: "node --cpu-prof и открыть .cpuprofile в Chrome DevTools", summary: "Отличный UI, нужен DevTools.", justification: "Великолепно для визуального чтения flame-графа, но ограничение тут — нет доступа к DevTools. Пришлось бы скопировать .cpuprofile с машины на ту, где есть Chrome — реально, но больше трения, чем просто прочесть текст --prof-process на месте. Это лучший инструмент, когда файл можно дотащить до браузера, а не самый малотрудный первый шаг на сервере." },
    { name: "Установить и запустить clinic flame", summary: "С подсказками, но лишние зависимости.", justification: "clinic flame даёт красивый проводящий flame-граф, но это лишняя зависимость для установки на боевой хост и всё равно рендерит в HTML, который ты откроешь в браузере. Мощно для более глубокого расследования, избыточно и более трудозатратно для быстрой проверки «какая функция горячая» по SSH." },
    { name: "Снять heap-снимок через v8.writeHeapSnapshot()", summary: "Не тот инструмент — это про память.", justification: "Heap-снимок фотографирует память, а не время CPU. Он скажет, какие объекты живы, а не какая функция жжёт ядро. Хвататься за него тут значит анализировать совсем не то измерение — упор ядра это проблема профилирования, а не утечки." },
  ]}
/>

## Раннбук триажа утечки

Когда память растёт и тебя OOM-килит, ходы механические, а не магические. Подтверди, что это утечка (old space не падает после полного GC по `--trace-gc`). Сними базовый снимок, жёстко нагрузи подозрительный путь, сними снова, повтори для третьего. Через вид сравнения «allocated between snapshots» найди конструктор, чей счётчик только растёт. Сортируй по **retained size**, открой **retainer path** и иди по нему к корневой ссылке — почти всегда это безграничный кэш, забытый слушатель, жирное замыкание или модульный глобал. Затем заставь ссылку исчезнуть: добавь LRU-вытеснение или TTL кэшу, `removeListener`/`off` при teardown, обнули захваченный объект. Перезапусти diff снимков, чтобы доказать, что счётчик теперь ровный. Регрессионный тест, утверждающий стабильный RSS после N итераций, не даёт ей вернуться.

```js
// до: течёт по одной записи на запрос, вечно
const cache = new Map();
function handle(req) {
  cache.set(req.id, expensiveResult(req)); // никогда не вытесняется → OOM за часы/дни
}

// после: ограничено — старые ключи становятся недостижимы и GC их возвращает
import { LRUCache } from "lru-cache";
const cache = new LRUCache({ max: 10_000, ttl: 60_000 });
function handle(req) {
  cache.set(req.id, expensiveResult(req)); // вытесняет старейшие за 10k записей
}
```

<DragOrder
  id="04-profiling-drag-1"
  lessonSlug="02-cpu-and-memory-profiling"
  lang="ru"
  prompt="Расставь шаги охоты на утечку по heap-снимкам — от первого подозрения до проверенного фикса:"
  items={[
    { id: "a", label: "Подтверди утечку: --trace-gc показывает, что old space не падает после полного GC" },
    { id: "b", label: "Сними базовый снимок после прогрева" },
    { id: "c", label: "Жёстко нагрузи подозрительный путь, затем сними снова — повтори для третьего" },
    { id: "d", label: "Через вид сравнения найди конструктор, чей счётчик только растёт" },
    { id: "e", label: "Сортируй по retained size и иди по retainer path к корневой ссылке" },
    { id: "f", label: "Обрежь ссылку (LRU-вытеснение / removeListener) и пере-diff снимки, чтобы доказать ровность" },
  ]}
  correctOrder={["a", "b", "c", "d", "e", "f"]}
/>

<Quiz
  id="04-profiling-quiz-2"
  lessonSlug="02-cpu-and-memory-profiling"
  lang="ru"
  question="С запущенным --trace-gc ты видишь, что old space стоит на ~1850 МБ до Mark-Sweep и ~1845 МБ после, прибавляя по несколько МБ каждый цикл. Что значит этот паттерн?"
  choices={[
    { label: "Утечка: полный GC возвращает почти ничего, а old space монотонно растёт через сборки — что-то достижимое всё растёт, так что снимай три heap-снимка", correct: true },
    { label: "Здоровое стабильное состояние — это нормальная пила", misconception: "Здоровая куча пилит: она существенно падает после полного GC. Здесь полный GC возвращает почти ничего (1850→1845), а пол всё растёт — это лестница, сигнатура утечки, а не пила." },
    { label: "GC сломан и нужно поднять --max-old-space-size", misconception: "GC не сломан — он может освобождать только недостижимые объекты, а эти достижимы (держатся каким-то кэшем/слушателем/глобалом). Поднятие лимита лишь отложит OOM; нужно найти и обрезать retainer." },
    { label: "Обычная текучка new space от Scavenge", misconception: "Scavenge собирает new space; Mark-Sweep — это сборщик old space, названный в трассе. То, что пол old space растёт после каждого Mark-Sweep, и исключает безобидную текучку new space." },
  ]}
/>

</Explanation>

<FlowDiagram
  label="Охота на утечку тремя снимками от подозрения до ровного базиса"
  hue="var(--d-systems)"
  perRow={4}
  nodes={[
    { id: "g", label: "--trace-gc", sub: "old space растёт" },
    { id: "s", label: "3 снимка", sub: "diff-сравнение" },
    { id: "r", label: "retained size", sub: "retainer path" },
    { id: "f", label: "обрезать ссылку", sub: "evict / off" },
  ]}
  edges={[
    { from: "g", to: "s", label: "подтвердить" },
    { from: "s", to: "r", label: "найти рост" },
    { from: "r", to: "f", label: "идти + фикс" },
  ]}
  caption="Подтверди утечку через --trace-gc (old space не падает после полного GC), сними три heap-снимка вокруг подозрительного пути, отсортируй единственный растущий конструктор по retained size и пройди по его retainer path к корневой ссылке, затем обрежь эту ссылку — ограничь кэш или сними слушателя — и пере-diff, чтобы доказать ровность счётчика."
/>

<KeyTakeaway>CPU-профиль находит широкий кадр (ширина = self time). Утечка — это old space, растущий через GC и не падающий: сними три снимка, гонись за retained size и retainer path, потом ограничь кэш.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="04-profiling-retrieval"
  lang="ru"
  questions={[
    { q: "В чём разница между shallow и retained size в heap-снимке и по какому из них сортировать, чтобы охотиться на утечку?", a: "Shallow size — память, которую объект занимает сам по себе: лишь его собственные поля, пара десятков байт для большинства объектов. Retained size — суммарная память, которая освободилась бы при его освобождении: он сам плюс всё достижимое *только* через него, то есть весь подграф, что он держит живым. Корень утечки почти всегда — маленький объект с огромным retained size: 48-байтовый Map или замыкание, единолично удерживающее десятки или сотни МБ. Поэтому сортируй по retained size, а не по shallow size: shallow говорит, насколько жирен один объект, retained — сколько памяти он держит в заложниках. Найдя растущего с большим retained size, открой его retainer path — цепочку ссылок, держащих его достижимым, — и пройди к настоящему виновнику: безграничному кэшу, забытому слушателю, модульному глобалу. Обрезание этой одной ссылки даёт следующему GC вернуть весь удержанный подграф." },
    { q: "Почему удвоение --max-old-space-size не чинит утечку памяти и как отличить утечку от настоящего недопровижена?", a: "Потому что утечка — про *достижимость*, а не про размер кучи. Утекающая память держится живой ссылкой — модульным Map, зарегистрированным слушателем, замыканием, — так что V8 справедливо отказывается её собирать, какой бы большой ни была куча. Поднятие --max-old-space-size лишь даёт той же вечно растущей структуре больше места до потолка, так что OOM-кил переезжает с каждых 14 часов на каждые 28; наклон идентичен. Диагностика, различающая эти два случая, — возвращается ли память после полной сборки мусора, что ты смотришь через --trace-gc. Если old space существенно падает после Mark-Sweep и затем наполняется под нагрузкой — пила — ты просто недопровиженен под легитимно большой рабочий набор, и поднять лимит правильно. Если old space почти не падает после полного GC, а пол всё растёт — лестница — у тебя утечка, и фикс в том, чтобы найти retainer тремя heap-снимками и обрезать ссылку, а не кормить её ещё кучей." },
  ]}
/>

<Recap lang="ru">
Профилирование и охота на утечку — две разные работы с двумя разными инструментами. Для CPU ты сэмплируешь: `--cpu-prof` пишет `.cpuprofile` для вкладки Performance Chrome DevTools, `--prof` плюс `node --prof-process` даёт дружащий с headless текстовый tick-отчёт, а 0x или clinic flame оборачивают те же данные во flame-граф, где **ширина — это self time**, — гонись за самым широким кадром, игнорируй глубину. Для памяти ты фотографируешь кучу. Heap-снимок (`v8.writeHeapSnapshot()`, `--heapsnapshot-signal=SIGUSR2` или вкладка Memory в DevTools) показывает, что живо сейчас, но только **техника трёх снимков** — базис, нагрузить путь, снимок, повторить — раскрывает утечку через вид сравнения «objects allocated between snapshots»: конструктор, чей счётчик только растёт, и есть виновник. Сортируй по **retained size** (что освободится, если освободить это), а не по shallow size, и иди по **retainer path** к корню: безграничный кэш `Map`/массив, забытый слушатель событий (`MaxListenersExceededWarning`), жирное замыкание или модульный глобал. Механизм под этим — поколенческая куча V8: быстрый Scavenge на new space, медленный Mark-Sweep-Compact на old space, — поэтому точное определение утечки это **old space, который растёт через полные GC и никогда не падает**, что ты смотришь через `--trace-gc`. Поднятие `--max-old-space-size` — ручка провижена, никогда не фикс утечки: если память возвращается после полного GC, ты недопровиженен, если нет — у тебя утечка. Фикс всегда в том, чтобы сделать ссылку недостижимой — добавь LRU-вытеснение или TTL, `removeListener` при teardown, обнули замыкание, — затем пере-diff снимки, чтобы доказать, что счётчик ровный. Теперь, когда видишь RSS, растущий прямой линией через каждый GC, ты первым делом тянешься к `--trace-gc`, а не к большему лимиту кучи.
</Recap>
