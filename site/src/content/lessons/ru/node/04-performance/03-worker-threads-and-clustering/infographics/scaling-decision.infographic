infographic hierarchy-tree-curved-line-rounded-rect-node
data
  root
    label Что ограничивает масштаб?
    children
      - label I/O-bound — ждёт базу или upstream
        children
          - label Loop не блокируется — уже async
          - label Не делай ничего: потоки лишь добавят накладные
      - label CPU-работа + общие / крупные данные в процессе
        children
          - label пул worker_threads ≈ ядра
          - label Пулить — никогда не воркер на запрос
      - label CPU-работа в stateless-сетевом сервере
        children
          - label cluster или N реплик — задействуй все ядра
          - label Вынеси состояние в общее хранилище (Redis/БД)
theme
  palette #6366f1 #14b8a6 #f59e0b
