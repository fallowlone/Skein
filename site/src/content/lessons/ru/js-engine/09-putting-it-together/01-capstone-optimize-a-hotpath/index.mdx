---
slug: "01-capstone-optimize-a-hotpath"
lang: ru
track: "js-engine"
unit: "09-putting-it-together"
order: 1
title: "Капстоун: оптимизируем горячий путь"
summary: "Один медленный обработчик запросов — и в игре каждый слой движка. Профилируем, читаем трейсы деопта и IC, чиним форму, боксинг, давление GC и затор микрозадач — затем доказываем выигрыш. Весь трек, применённый к одной функции."
estMin: 16
status: ready
lessonType: topic
level: senior
concepts:
  - профилирование
  - стабильность hidden class
  - деоптимизация
  - element kinds
  - давление GC
  - планирование микрозадач
  - регрессионный гейт
prereqs:
  - "08-measuring-optimizing/04-real-world-wins"
spiral:
  - performance-cliffs
  - statefulness
sources:
  - https://v8.dev/docs
  - https://v8.dev/blog/cost-of-javascript-2019
  - https://mrale.ph/blog/2018/02/03/maybe-you-dont-need-rust-to-speed-up-your-js.html
---

import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import NumbersCard from "~/components/layout/NumbersCard.astro";
import Misconception from "~/components/layout/Misconception.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";
import StackDiagram from "~/components/diagram/StackDiagram.astro";

<Hook>
Эндпоинт скоринга, раньше тянувший 40k запросов в секунду, теперь еле держит 6k, и единственным «изменением» был рефакторинг, который «просто прибрал построение объектов». В коде ничего не выглядит неправильным. Всё, что вы изучили в этом треке, сейчас сойдётся на одной функции из 30 строк — потому что движок делает пять разных медленных вещей разом, и вы можете назвать и починить каждую.
</Hook>

<Crux>Выигрыш на горячем пути — назвать механизм за каждой стоимостью: форма, боксинг, GC, диспетчеризация, планирование. Затем фикс.</Crux>

<Explanation>

## Метод — раньше, чем функция

Оптимизация без измерения — это суеверие. Цикл всегда один: **профилируй, чтобы найти доминирующую стоимость, назови механизм, сделай одно изменение, перезамерь.** Этот трек дал вам словарь для шага «назови механизм» — именно здесь большинство инженеров застревают, гадая вместо чтения трейса. К концу этого капстоуна вы назовёте и почините каждую из пяти проблем — и будете точно знать, за каким инструментом тянуться в следующий раз.

<StackDiagram
  label="Цикл оптимизации сверху вниз: измерь, чтобы найти доминирующую стоимость, назови механизм движка, измени одно, затем проверь по защищённому бенчмарку."
  hue="var(--accent)"
  caption="Никогда не пропускайте слой вверх. Затор микрозадач, спрятанный под деопт-циклом, будет вводить в заблуждение, пока сперва не починен деопт."
  layers={[
    { label: "Измерь", note: "--prof, --trace-deopt, heap snapshot" },
    { label: "Назови механизм", note: "форма / боксинг / GC / диспетч / планир." },
    { label: "Измени одно", note: "минимальный фикс под эту стоимость" },
    { label: "Проверь", note: "защищённый бенчмарк + регресс-гейт" },
  ]}
/>

Вот регрессировавший обработчик. Читайте его так, как читал бы движок.

```js
function scoreBatch(rows) {
  const out = [];
  for (const row of rows) {
    const e = {};
    e.id = row.id;
    if (row.name) e.name = row.name;       // условное поле
    e.score = row.weight * 1e9 + row.bonus; // выходит за диапазон Smi
    if (row.flagged) e.flag = true;          // позднее условное поле
    out.push(e);
    log(`scored ${e.id}`);                    // синхронный console в горячем цикле
  }
  return out;
}
```

## Слой 1 — форма (юниты 02–03)

`--trace-ic` показывает, что нижестоящая точка доступа, читающая `.score`, становится **megamorphic** (мегаморфной — IC, видящий более двух разных форм объекта и вынужденный откатиться к медленному поиску). Причина — условные поля: `name` и `flag` добавляются лишь иногда, поэтому объекты идут по разным веткам дерева переходов и приходят в разные hidden class (юнит 03). IC у потребителя видит пять и более map и откатывается к общему поиску — десятки циклов вместо одного. Когда видите `megamorphic` или `GENERIC` в трейсе, первый вопрос такой: какая точка вызова производит объекты с несогласованными формами?

Фикс: инициализируй **каждое** свойство безусловно, в фиксированном порядке, чтобы все объекты разделяли один Map.

```js
const e = {
  id: row.id,
  name: row.name ?? null,
  score: 0,                 // задаётся ниже
  flag: row.flagged === true,
};
```

## Слой 2 — боксинг и деопт (юниты 02, 04)

`--trace-deopt` показывает, что `scoreBatch` деоптимизируется на guard `CheckSmi`. `row.weight * 1e9` выходит за диапазон Smi (Small Integer, целое число, умещающееся в указатель без выделения памяти, ±2³¹), поэтому результат становится **HeapNumber** (объект-обёртка в куче, требующий отдельной аллокации) — боксом в куче (юнит 02). TurboFan спекулировал Smi по ранней обратной связи; первое переполнение проваливает guard и вызывает деопт, а поскольку это повторяется каждый батч, получается **деопт-цикл** (юнит 04): оптимизировать → деопт → переоптимизировать, никогда не оставаясь быстрым.

Фикс: перестань притворяться, что значение целое. Считай в `double` с самого начала, чтобы оптимизатор специализировался на `Float64` и никогда не ставил guard на Smi — или, если потребитель численно-тяжёлый, аккумулируй скоры в `Float64Array` вместо полей объекта, устраняя бокс целиком.

<NumbersCard
  title="Стоимость каждого механизма на этом пути"
  data-lesson-visual
  stats={[
    { label: "Megamorphic-загрузка свойства против monomorphic", value: "~10–50×" },
    { label: "Деопт-цикл (оптимизация/деопт churn)", value: "остаётся интерпрет." },
    { label: "Бокс HeapNumber против Smi", value: "alloc + deref против 1 instr" },
    { label: "Minor GC от аллокации на строку", value: "доли мс, но часто" },
    { label: "Синхронный log() в цикле на 100k", value: "блокирует ход цикла" },
  ]}
/>

## Слой 3 — churn у GC (юнит 06)

Даже с починенными формами аллокация одного объекта `out` на строку заливает **new space**; bump-pointer аллокатор заполняет semi-space и запускает **Scavenge** (юнит 06) куда чаще необходимого. Minor GC дёшев поодиночке, но при таком объёме — это смерть от тысячи порезов, а выжившие промоутятся в old space, в итоге форсируя major GC.

Фикс: уменьши аллокацию. Если `out` идёт в редьюсер, сворачивай строки напрямую, не материализуя массив объектов; если массив нужен, преаллоцируй (`new Array(rows.length)` здесь годится, потому что вы заполняете каждый слот, сохраняя массив `PACKED`). Меньше, дольше живущих аллокаций лучше, чем шторм короткоживущих.

## Слой 4 — планирование (юнит 07)

Вызов `log()` — синхронный ввод-вывод внутри цикла. Помимо собственной стоимости, в контексте запроса он переплетается с циклом событий: поток синхронной работы на стеке откладывает **microtask checkpoint** и морит голодом другие обработчики (юнит 07). Фикс — не «сделать логирование асинхронным», а *не логировать на строку на горячем пути*. Агрегируй и эмить один раз либо сэмплируй.

<FlowDiagram
  label="Пять фиксов в порядке зависимостей: сперва форма, чтобы у оптимизатора была стабильная обратная связь, затем убрать триггер деопта, затем срезать аллокацию, затем планирование, затем закрыть регресс-гейтом."
  hue="var(--accent)"
  perRow={5}
  caption="Порядок важен: стабильная форма — предусловие для хорошего кода оптимизатора, поэтому она первая; регресс-гейт последний и удерживает выигрыш."
  nodes={[
    { id: "shape", label: "Форма", sub: "1 Map", col: 0, row: 0 },
    { id: "deopt", label: "Раз-бокс", sub: "нет Smi-guard", col: 1, row: 0 },
    { id: "gc", label: "Меньше alloc", sub: "меньше объектов", col: 2, row: 0 },
    { id: "sched", label: "Планир.", sub: "нет log/строка", col: 3, row: 0 },
    { id: "gate", label: "Гейт", sub: "регресс-тест", col: 4, row: 0 },
  ]}
  edges={[
    { from: "shape", to: "deopt", label: "затем" },
    { from: "deopt", to: "gc", label: "затем" },
    { from: "gc", to: "sched", label: "затем" },
    { from: "sched", to: "gate", label: "закрыть" },
  ]}
/>

## Слой 5 — докажи, потом защити (юнит 08)

Замерь переписанную версию против оригинала на разогретом коде, потребляя результат, чтобы dead-code elimination не удалил твой бенчмарк (юнит 08), держа аллокацию вне замеряемой области и записав движок. Затем добавь в CI микробенчмарк, утверждающий нижнюю границу пропускной способности, плюс юнит-тест, считающий число различных сигнатур `Object.keys` из `scoreBatch` и падающий, если оно превысит один. Регрессия формы, с которой начался инцидент, теперь не сможет снова уехать тихо.

<Misconception id="mc-rewrite-in-rust">
  <p><strong>«Медленно — перепишем на WASM/Rust».</strong> Здесь выигрыш — стабильная форма и отсутствие деопт-цикла: чисто JS-изменения возвращают почти всю пропускную способность. Тянись за WASM лишь когда JS-путь monomorphic, без боксинга и беден на аллокации, но всё ещё упирается.</p>
</Misconception>

<Quiz
  id="q-order"
  lessonSlug="01-capstone-optimize-a-hotpath"
  lang="ru"
  question="Почему чинить нестабильность hidden class раньше, чем гоняться за деоптом или churn у GC?"
  choices={[
    { label: "Смена формы — единственный реальный баг; остальное шум", misconception: "Все четыре — реальные стоимости; смысл в порядке, а не в отбрасывании остальных." },
    { label: "Стабильная форма даёт оптимизатору чистую обратную связь о типах, поэтому его код (и ваши последующие замеры) заслуживают доверия", correct: true },
    { label: "Hidden classes не связаны с деоптом", misconception: "Связаны: расхождение форм само по себе частый источник деопта и загрязнения обратной связи — ещё причина чинить его первым." },
    { label: "GC нельзя измерить, пока формы не починены", misconception: "GC измерим независимо; форму чинят первой, потому что она задаёт качество оптимизатора, а не потому что блокирует замер GC." },
  ]}
/>

<Quiz
  id="q-smi"
  lessonSlug="01-capstone-optimize-a-hotpath"
  lang="ru"
  question="`row.weight * 1e9 + row.bonus` вызвал деопт-цикл. Каков точный механизм?"
  choices={[
    { label: "Умножение всегда медленнее сложения", misconception: "Дело не в операторе, а в значении, покидающем диапазон Smi." },
    { label: "Результат выходит за диапазон Smi, становится HeapNumber, проваливает guard CheckSmi у TurboFan и переоптимизируется-деоптится каждый батч", correct: true },
    { label: "1e9 — не валидное число JavaScript", misconception: "1e9 — вполне валидный double-литерал; дело в представлении значения в рантайме, а не в литерале." },
    { label: "Имя `score` конфликтует со встроенным", misconception: "Такого конфликта нет; стоимость — переход Smi→HeapNumber и провал guard." },
  ]}
/>

<DragOrder
  id="do-method"
  pieceSlug="01-capstone-optimize-a-hotpath"
  lang="ru"
  prompt="Расставьте метод оптимизации для этого инцидента."
  items={[
    "Профилировать, чтобы найти доминирующую стоимость (--prof, --trace-deopt, --trace-ic, heap snapshot)",
    "Назвать механизм движка за ней (форма / боксинг / GC / планирование)",
    "Сделать минимальное изменение под этот механизм",
    "Перезамерить на разогретом коде с потреблением результата",
    "Добавить регресс-гейт, чтобы выигрыш не откатился тихо",
  ]}
/>

<Inset kind="practice" lang="ru">
Примените это к своему коду: возьмите одну функцию, которую профайлер отмечает как горячую. Прежде чем её трогать, запишите, какой из пяти слоёв вы *ожидаете* увидеть стоимостью, затем запустите `--trace-deopt` и heap snapshot для проверки. Большинство инженеров ошибаются в том, какой слой доминирует — именно этот разрыв между интуицией и трейсом и закрывает этот трек.
</Inset>

</Explanation>

<KeyTakeaway>Горячий путь редко медленен по одной причине. Сперва профилируй, затем точно назови каждый механизм — megamorphic-форма, деопт от переполнения Smi, GC от аллокаций, синхронное планирование — чини форму первой, ведь она задаёт качество оптимизатора, и закрывай каждый выигрыш регресс-гейтом. Называние механизма — вот навык; фиксы следуют за ним.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="capstone-optimize-a-hotpath-retrieval"
  lang="ru"
  questions={[
    { q: "Обработчик медленный. Пройдите полный метод диагностики от начала до конца.", a: "Воспроизведите под нагрузкой и профилируйте (node --prof / --cpu-prof для разбивки CPU; --trace-deopt для бэйлаутов; --trace-ic для состояний IC; heap snapshot для удержания/аллокаций). Найдите ДОМИНИРУЮЩУЮ стоимость первой. Назовите механизм: megamorphic-доступ к свойству (расхождение форм), деопт-цикл Smi→HeapNumber, churn аллокаций в new space, гоняющий Scavenge, или синхронный вызов, стопорящий microtask checkpoint. Сделайте минимальное изменение под этот механизм, затем перезамерьте на разогретом коде с потреблением результата (победить DCE) и аллокацией вне замеряемой области. Итерируйте к следующей доминирующей стоимости. Наконец добавьте регресс-гейт (нижняя граница пропускной способности + проверка стабильности формы)." },
    { q: "Почему условная инициализация свойств регрессирует горячего потребителя и каков фикс?", a: "Добавление name/flag лишь иногда заставляет объекты идти по разным веткам переходов hidden class, давая несколько Map. Нижестоящая точка доступа тогда видит 5+ форм, становится megamorphic и откатывается к общему поиску (десятки циклов против одного), а оптимизатор не может специализироваться. Фикс: инициализируй каждое свойство безусловно в фиксированном порядке (значения по умолчанию null/0/false вместо пропуска), чтобы все объекты разделяли один Map и IC потребителя оставался monomorphic. Защити тестом, считающим число различных сигнатур Object.keys." },
    { q: "Когда переписывание горячего пути на WASM/Rust действительно оправдано?", a: "Только когда JavaScript-путь уже monomorphic (стабильные формы, нет megamorphic-диспетчеризации), без боксинга (числа остаются Smi или живут в типизированных массивах, нет churn HeapNumber), беден на аллокации (не гоняет постоянный GC) и свободен от деопт-циклов — и при этом ВСЁ ЕЩЁ упирается в CPU по важной метрике. Большинство случаев «перепишем на Rust» на деле — случаи «почини форму и деопт», возвращающие пропускную способность на чистом JS. WASM окупается для истинно вычислительно-связанной, бедной на аллокации численной/байтовой работы, а не для замусоренной формами обвязки объектов." },
  ]}
/>

<Recap lang="ru">
Этот капстоун положил весь трек на одну функцию. Регрессировавший обработчик скоринга был медленным по пяти перекрывающимся причинам, каждую из которых можно назвать из этого трека: условная инициализация свойств разнесла объекты по разным hidden class и сделала IC потребителя megamorphic (юниты 02–03); целое, вышедшее за диапазон Smi, стало HeapNumber и вызвало деопт-цикл TurboFan (юниты 02, 04); аллокация объекта на строку гоняла new space в частые Scavenge (юнит 06); синхронный log на итерацию стопорил microtask checkpoint (юнит 07). Метод неизменен: профилируй, чтобы найти доминирующую стоимость, назови точный механизм, измени одно, перезамерь на разогретом коде с потреблением результата и закрой выигрыш регресс-гейтом (юнит 08). Чини форму первой, ведь стабильная форма — предусловие для того, чтобы оптимизатор давал заслуживающий доверия код и заслуживающие доверия замеры. Теперь, встречая медленный обработчик, вы первым делом откроете трейс, точно назовёте механизм и будете чинить по одному слою — потому что именно это превращает гадание в инженерию.
</Recap>
