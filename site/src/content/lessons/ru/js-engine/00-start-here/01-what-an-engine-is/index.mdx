---
slug: "01-what-an-engine-is"
lang: ru
track: "js-engine"
unit: "00-start-here"
order: 1
title: "Что такое движок JavaScript"
summary: "Движок JS превращает исходный текст в работу процессора: парсит, компилирует в байт-код, интерпретирует, затем JIT-оптимизирует горячие участки — а сборщик мусора освобождает память. Движок против рантайма и карта этого трека."
estMin: 10
status: ready
lessonType: topic
level: zero
concepts:
  - движок JavaScript
  - рантайм против движка
  - интерпретатор
  - JIT-компилятор
  - сборщик мусора
  - байт-код
deepensInto:
  - "01-how-js-runs/01-source-to-ast"
spiral:
  - statefulness
  - performance-cliffs
prereqs: []
sources:
  - https://v8.dev/docs
  - https://v8.dev/blog/launching-ignition-and-turbofan
---

import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import NumbersCard from "~/components/layout/NumbersCard.astro";
import Misconception from "~/components/layout/Misconception.astro";
import StackDiagram from "~/components/diagram/StackDiagram.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
Вы пишете `arr.map(x => x * 2)`, и это работает. Но ничто в вашем процессоре не понимает `map`, стрелочные функции или `*` над числом JavaScript. Что-то должно перевести этот текст в те немногие целочисленные инструкции и инструкции с плавающей точкой, которые кремний действительно исполняет — и решить прямо во время работы программы, какие части стоит скомпилировать в быстрый машинный код. Это «что-то» и есть движок.
</Hook>

<Crux>Движок JS парсит исходник в байт-код, интерпретирует, затем JIT-компилирует горячие пути в машинный код; GC освобождает память.</Crux>

<Explanation>

За следующие десять минут вы увидите, какая именно стадия отвечает за «медленный JavaScript» — и почему исправление требует работы с движком, а не в обход него.

## Движок — это программа, которая исполняет вашу программу

Движок JavaScript — это нативная программа (V8 — это примерно два миллиона строк C++), на вход которой подаётся исходный текст JavaScript, а на выходе — работа процессора. Это не чёрный ящик: это конвейер из хорошо изученных стадий, и у каждой стадии есть «характер» по производительности, который можно наблюдать и на который можно влиять.

Наивная модель — «JavaScript интерпретируется строка за строкой» — неверна уже больше десяти лет. Современные движки **адаптивны**: они стартуют дёшево (интерпретируют байт-код) и становятся дорогими только там, где это окупается (компилируют в оптимизированный машинный код тот код, что выполняется много раз). Весь трек — про эту адаптивную машинерию и про то, как держаться её быстрых путей.

## Пять стадий

<StackDiagram
  label="Основные подсистемы движка JavaScript: от исходного текста наверху до кучи, которой управляет сборщик мусора."
  hue="var(--accent)"
  caption="Конвейер V8. Парсер и Ignition работают для всего кода; оптимизирующие компиляторы — только для горячего; GC работает непрерывно и независимо."
  layers={[
    { label: "Парсер", note: "текст → AST" },
    { label: "Ignition (интерпретатор)", note: "AST → байт-код → запуск" },
    { label: "Sparkplug / Maglev / TurboFan", note: "горячий байт-код → маш. код" },
    { label: "Сборщик мусора (Orinoco)", note: "освобождает мёртвые объекты" },
    { label: "Куча + стек вызовов", note: "где живут значения" },
  ]}
/>

Когда встретите flame-граф V8 или профиль Node.js с этими названиями — вы будете знать, куда смотреть. Вот что делает каждая стадия:

1. **Парсер** — читает исходный текст и строит абстрактное синтаксическое дерево (AST), структурированное представление вашего кода. Он также делает быстрый проход «pre-parse», чтобы пропустить тела функций, которые ещё не были вызваны (ленивый парсинг).
2. **Интерпретатор (Ignition в V8)** — один раз обходит AST, чтобы выпустить компактный **байт-код**, а затем исполняет этот байт-код на регистровой виртуальной машине. Здесь стартует весь код. Это быстро производить и легко по памяти.
3. **Оптимизирующие компиляторы (Sparkplug, Maglev, TurboFan)** — для функций, которые выполняются часто («горячих»), движок компилирует байт-код в нативный машинный код, специализируясь по наблюдениям за типами во время работы. Быстрее исполнять, но дороже и прожорливее по памяти производить.
4. **Сборщик мусора (Orinoco в V8)** — работает параллельно со всем остальным, находит объекты, на которые больше никто не ссылается, и освобождает их память. Вы никогда не вызываете `free()`; GC выводит это сам.
5. **Куча и стек вызовов** — память, где живут ваши объекты (куча) и фреймы функций (стек). Форма и время жизни того, что вы туда кладёте, задают стоимость каждой остальной стадии.

Искусство этого трека — читать эти стадии: *почему* функцию скомпилировали, а потом выбросили (деопт), *почему* доступ к объекту медленный (расхождение форм), *почему* память всё растёт (утечка, которую GC не может собрать).

## Движок против рантайма — различие, на котором спотыкаются

**Движок** реализует язык ECMAScript: числа, объекты, функции, прототипы, замыкания, `Promise`, сборку мусора. Это *всё*, что он реализует. Он понятия не имеет, что такое `setTimeout`, `fetch`, узел DOM или файловый дескриптор.

Всё это даёт **рантайм** (хост): браузер, Node.js, Deno, Bun. Рантайм встраивает движок и внедряет хостовые объекты и **цикл событий**, который их планирует. `setTimeout` — это не JavaScript, это функция, которую рантайм передаёт движку.

<NumbersCard
  title="Движок против рантайма: кто что даёт"
  data-lesson-visual
  stats={[
    { label: "Числа, объекты, замыкания, Promise", value: "движок" },
    { label: "Сборка мусора, куча", value: "движок" },
    { label: "setTimeout, queueMicrotask", value: "рантайм" },
    { label: "fetch, DOM, fs, console", value: "рантайм" },
    { label: "Сам цикл событий", value: "рантайм" },
    { label: "Строк C++ в V8 (приблизительно)", value: "~2M" },
  ]}
/>

Вот почему один и тот же движок (V8) питает Chrome, Node, Edge и Deno с совершенно разными возможностями: ядро языка одно, хост разный. Цикл событий мы разбираем от и до в юните 07; пока зафиксируйте границу: движок исполняет язык, рантайм поставляет окружающий мир.

<Misconception id="mc-interpreted">
  <p><strong>«JavaScript — интерпретируемый язык, значит, медленный».</strong> Он стартует интерпретируемым, но горячий код компилируется в машинный, что для численных циклов соперничает с C. «Медленный JS» обычно значит, что вы сбили движок <em>с</em> быстрого пути — смена формы, деопт, давление GC — а не медленная интерпретация.</p>
</Misconception>

<FlowDiagram
  label="Жизненный цикл одной функции: она стартует в интерпретаторе и повышается до скомпилированного машинного кода только после того, как выполнилась достаточно раз, чтобы считаться горячей."
  hue="var(--accent)"
  caption="Путь функции. Холодный код остаётся интерпретируемым; горячий поднимается по ярусам. Плохое предположение отправляет его вниз (деопт) — тема юнита 04."
  nodes={[
    { id: "src", label: "Исходник", sub: "текст", col: 0, row: 0 },
    { id: "bc", label: "Байт-код", sub: "Ignition", col: 1, row: 0 },
    { id: "run", label: "Интерпрет.", sub: "холодный", col: 2, row: 0 },
    { id: "hot", label: "Маш. код", sub: "горячий путь", col: 3, row: 0 },
  ]}
  edges={[
    { from: "src", to: "bc", label: "парсинг" },
    { from: "bc", to: "run", label: "исполн." },
    { from: "run", to: "hot", label: "разогрев" },
  ]}
/>

<Quiz
  id="q-engine-runtime"
  lessonSlug="01-what-an-engine-is"
  lang="ru"
  question="Ваш Node-скрипт вызывает `setTimeout`. Какой компонент на самом деле реализует `setTimeout`?"
  choices={[
    { label: "Движок V8 — это часть JavaScript", misconception: "setTimeout нет в спецификации ECMAScript. Движок реализует ядро языка (числа, объекты, Promise); о таймерах он не знает." },
    { label: "Рантайм (Node) — он внедряет хостовые API и цикл событий", correct: true },
    { label: "Ядро операционной системы напрямую", misconception: "ОС даёт таймеры, на которых Node строится (через libuv), но видимую из JS функцию setTimeout предоставляет рантайм Node, ядро не отдаёт её вашему скрипту." },
    { label: "Сборщик мусора", misconception: "GC освобождает память; к планированию колбэков он отношения не имеет." },
  ]}
/>

<Quiz
  id="q-adaptive"
  lessonSlug="01-what-an-engine-is"
  lang="ru"
  question="Почему современный движок сначала интерпретирует код, а не компилирует всё в машинный код заранее?"
  choices={[
    { label: "Потому что компиляция невозможна, пока программа не завершится", misconception: "Компиляция возможна в любой момент; движок сам выбирает отложить её, он не вынужден." },
    { label: "Потому что большая часть кода выполняется редко — компиляция обошлась бы дороже по времени и памяти, чем сэкономила", correct: true },
    { label: "Потому что машинный код медленнее байт-кода", misconception: "Машинный код исполняется намного быстрее; компромисс в том, что его дорого производить." },
    { label: "Потому что интерпретаторы дают меньшие бинарники", misconception: "Размер бинарника не движущий фактор — фактор в том, чтобы платить за оптимизацию только там, где она окупается." },
  ]}
/>

<DragOrder
  id="do-pipeline"
  pieceSlug="01-what-an-engine-is"
  lang="ru"
  prompt="Расставьте по порядку стадии, через которые проходит совсем новая, ни разу не запущенная функция, когда её впервые вызывают много раз в цикле."
  items={[
    "Парсер строит AST из исходного текста",
    "Ignition выпускает байт-код",
    "Интерпретатор исполняет байт-код (холодно)",
    "Оптимизирующий компилятор производит специализированный машинный код (горячо)",
  ]}
/>

<Inset kind="why" lang="ru">
Почему вообще «движок»? Термин исторический: у ранних браузеров был компонент, который «приводил в движение» скрипты, как игровой движок движет игру. Название прижилось, хотя сегодняшние движки — это полноценные оптимизирующие компиляторы со сборщиком мусора, ближе к маленькой JVM, чем к простому драйверу скриптов.
</Inset>

</Explanation>

<KeyTakeaway>Движок реализует язык и больше ничего; рантайм даёт таймеры, ввод-вывод и цикл событий. Код стартует интерпретируемым как байт-код и JIT-компилируется только там, где становится горячим. Работа над производительностью — это удержание движка на его быстрых путях.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="what-an-engine-is-retrieval"
  lang="ru"
  questions={[
    { q: "Назовите пять основных подсистем движка JavaScript и что делает каждая.", a: "Парсер (исходный текст → AST, с быстрым pre-parse, чтобы пропустить невызванные функции); интерпретатор/Ignition (AST → байт-код, затем исполняет его на регистровой VM); оптимизирующие компиляторы (Sparkplug/Maglev/TurboFan, компилируют горячий байт-код в машинный код по обратной связи о типах); сборщик мусора (Orinoco, освобождает объекты кучи без ссылок); и куча + стек вызовов, где живут объекты и фреймы." },
    { q: "В чём разница между движком и рантаймом? Приведите по два примера каждого.", a: "Движок реализует только язык ECMAScript — числа, объекты, замыкания, Promise, сборку мусора. Рантайм (браузер, Node, Deno, Bun) встраивает движок и поставляет хостовые объекты плюс цикл событий — setTimeout, fetch, DOM, fs, console. setTimeout и fetch — это рантайм, а не движок; разрешение Promise и аллокация объектов — движок. Один V8 питает Chrome и Node с разными возможностями, потому что различается только хост." },
    { q: "Почему «JavaScript интерпретируется, поэтому медленный» — неверно?", a: "Код стартует интерпретируемым как байт-код (дёшево производить), но часто выполняемые функции JIT-компилируются в оптимизированный машинный код, который для численной работы приближается к скорости C. Медленность на практике приходит от сбивания движка с оптимизированного пути — расхождение форм, деоптимизация, давление GC — а не от того, что интерпретация медленна сама по себе." },
  ]}
/>

<Recap lang="ru">
Движок JavaScript — это нативная программа, которая превращает ваш исходный текст в работу процессора через пять подсистем: парсер, строящий AST; интерпретатор (Ignition), выпускающий и исполняющий байт-код; оптимизирующие компиляторы (Sparkplug, Maglev, TurboFan), компилирующие горячий код в машинный; сборщик мусора (Orinoco), освобождающий память; и кучу со стеком, где живут значения. Модель адаптивна: старт дёшев, оптимизируются только горячие пути. Движок реализует лишь язык ECMAScript — рантайм (Chrome, Node, Deno, Bun) поставляет таймеры, ввод-вывод, DOM и цикл событий. Большая часть «медленного JavaScript» — это движок, сбитый с быстрого пути, а не накладные расходы интерпретации. Теперь, когда вы увидите предупреждение о деопте или скачок GC в профиле, вы будете знать, какую подсистему винить и к какому юниту трека обратиться.
</Recap>
