---
slug: "02-the-engine-landscape"
lang: ru
track: "js-engine"
unit: "00-start-here"
order: 2
title: "Ландшафт движков: V8, JSC, SpiderMonkey"
summary: "Четыре движка, которые важны — V8, SpiderMonkey, JavaScriptCore, Hermes — разделяют одну адаптивную форму: интерпретатор плюс ярусный JIT плюс GC. Почему трек учит V8, что переносится, и карта терминологии между движками."
estMin: 11
status: ready
lessonType: topic
level: junior
concepts:
  - V8
  - SpiderMonkey
  - JavaScriptCore
  - Hermes
  - ярусная компиляция
  - терминология hidden class
deepensInto:
  - "01-how-js-runs/01-source-to-ast"
spiral:
  - performance-cliffs
prereqs:
  - "01-what-an-engine-is"
sources:
  - https://v8.dev/docs
  - https://webkit.org/blog/10308/speculation-in-javascriptcore/
  - https://spidermonkey.dev/
---

import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import NumbersCard from "~/components/layout/NumbersCard.astro";
import Misconception from "~/components/layout/Misconception.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
Трюк с hidden class, который ускоряет чтение свойств ваших объектов в Chrome, работает и в Firefox, и в Safari — но в Firefox он зовётся «Shape», а в Safari — «Structure». Четыре боевых движка независимо пришли почти к одной и той же архитектуре. Изучите один глубоко — и сможете читать их все; имена меняются, механизм нет.
</Hook>

<Crux>Движки сошлись к одной форме — интерпретатор + ярусный JIT + поколенческий GC. Учите V8; принципы переносятся, меняются имена.</Crux>

<Explanation>

К концу этого урока вы будете знать, какой движок исполняет ваш код, как выглядит лестница ярусов в каждом из четырёх движков и как переводить термины, когда статья для одного движка описывает паттерн, нужный вам в другом.

## Четыре движка, одна архитектура

Есть четыре движка JavaScript, на которых вы, скорее всего, будете выпускать продукт:

- **V8** (Google) — Chrome, Edge, Node.js, Deno, Electron, Cloudflare Workers. Самый распространённый; главный предмет этого трека.
- **SpiderMonkey** (Mozilla) — Firefox. Старейший движок, написан Бренданом Айхом в 1995-м.
- **JavaScriptCore / Nitro** (Apple) — Safari и движок по умолчанию в Bun. Известен агрессивным многоярусным JIT.
- **Hermes** (Meta) — React Native. Необычный: компилирует в байт-код **заранее** и поставляет этот байт-код, оптимизируя запуск приложения на мобильных устройствах в ущерб пиковой пропускной способности.

Несмотря на независимые команды и кодовые базы, первые три сошлись к одной форме: быстрый **интерпретатор** для холодного кода, один или несколько **оптимизирующих JIT-компиляторов** для горячего кода, управляемых **обратной связью о типах**, **hidden classes** (скрытые классы — внутренние дескрипторы раскладки свойств объекта) для быстрого доступа к свойствам, **inline caches** (встроенные кеши вызовов — заглушки, запоминающие тип на конкретном сайте) и **поколенческий сборщик мусора** (generational GC). Эта схожесть и есть причина, по которой трек стоит вашего времени: это не мелочи про V8, это то, как делают быстрыми динамические языки.

## Лестница ярусов

Каждый движок прогоняет код через ярусы, обменивая стоимость компиляции на скорость исполнения. Больше вызовов — выше ярус:

<FlowDiagram
  label="Лестница ярусов, общая для современных движков: код стартует в интерпретаторе и перекомпилируется на всё более оптимизирующих ярусах по мере того, как выполняется чаще."
  hue="var(--accent)"
  perRow={4}
  caption="Ярусы V8 по состоянию на 2024. Средний ярус (Maglev) появился в 2023, чтобы закрыть разрыв между дешёвым базовым JIT и тяжёлым оптимизатором."
  nodes={[
    { id: "ign", label: "Ignition", sub: "интерпрет.", col: 0, row: 0 },
    { id: "sp", label: "Sparkplug", sub: "базовый JIT", col: 1, row: 0 },
    { id: "mg", label: "Maglev", sub: "средний JIT", col: 2, row: 0 },
    { id: "tf", label: "TurboFan", sub: "оптимизирующий", col: 3, row: 0 },
  ]}
  edges={[
    { from: "ign", to: "sp", label: "тёплый" },
    { from: "sp", to: "mg", label: "теплее" },
    { from: "mg", to: "tf", label: "горячий" },
  ]}
/>

Лестница V8 — это Ignition → Sparkplug → Maglev → TurboFan. JavaScriptCore знаменит четырьмя ярусами (LLInt → Baseline → DFG → FTL). SpiderMonkey идёт Interpreter → Baseline → WarpMonkey. Формы рифмуются: дёшево-и-обобщённо внизу, дорого-и-специализированно наверху, а движок повышает и понижает функции по мере того, как меняется их поведение.

## Короткая история (чтобы старые посты обретали смысл)

Сам V8 прошёл через поколения, и старые посты предполагают старый дизайн:

<NumbersCard
  title="Архитектура V8 по эпохам"
  data-lesson-visual
  stats={[
    { label: "2008–2010: full-codegen + Crankshaft", value: "первый JIT" },
    { label: "2017: Ignition + TurboFan", value: "эра байт-кода" },
    { label: "2021: Sparkplug", value: "базовый JIT" },
    { label: "2023: Maglev", value: "средний JIT" },
    { label: "Объект раскладки свойств (термин V8)", value: "Map" },
    { label: "Та же идея в JSC / SpiderMonkey", value: "Structure / Shape" },
  ]}
/>

Когда вы читаете статью 2015 года про «Crankshaft» и «hidden classes», описанный там оптимизатор уже исчез, но концепция hidden class, которую она учит, по-прежнему ровно верна. Поэтому мы опираемся на механизмы, а не на названия продуктов.

## Карта терминологии

Самый большой источник путаницы при чтении про разные движки — именование. Одна идея, три слова:

- **Hidden class** (дескриптор раскладки свойств объекта): `Map` в V8, `Shape` в SpiderMonkey, `Structure` в JavaScriptCore.
- **Маленькое целое**: `Smi` в V8; в других тегируется похоже.
- **Деоптимизация** (откат из скомпилированного кода обратно в интерпретатор): `deopt` в V8, `bailout` в JSC/SpiderMonkey.

Этот трек повсюду использует словарь V8. Мы будем отмечать межмоторный синоним при первом появлении каждого.

<Misconception id="mc-one-engine">
  <p><strong>«Я целюсь только в Chrome, значит, другие движки не важны».</strong> Паттерн, медленный в V8 (megamorphic-точки вызова, объекты в dictionary-режиме), обычно медленный везде, а зависимость может быть настроена под другой движок. Переносимая ментальная модель — вот актив, а не специфичный для V8 флаг.</p>
</Misconception>

<Quiz
  id="q-converge"
  lessonSlug="02-the-engine-landscape"
  lang="ru"
  question="V8, SpiderMonkey и JavaScriptCore сделаны отдельными командами. Почему они пришли к столь похожим архитектурам?"
  choices={[
    { label: "Они скопировали исходники V8", misconception: "Это независимые кодовые базы с разными лицензиями; схожесть пришла от решения одной задачи, а не от копирования." },
    { label: "У задачи — сделать динамически типизированный язык быстрым — небольшой набор известных хороших решений (ярусный JIT, hidden classes, обратная связь о типах, поколенческий GC)", correct: true },
    { label: "Спецификация ECMAScript предписывает внутреннюю архитектуру", misconception: "Спецификация задаёт наблюдаемое поведение, а не реализацию. Движки вольны различаться внутри; они сошлись всё равно." },
    { label: "Все они работают на одном наборе инструкций процессора", misconception: "Они таргетят много ISA (x86-64, ARM64 и т. д.); архитектурная схожесть — про стратегию оптимизации, а не про целевой процессор." },
  ]}
/>

<Quiz
  id="q-hermes"
  lessonSlug="02-the-engine-landscape"
  lang="ru"
  question="Hermes поставляет предкомпилированный байт-код вместо JIT-компиляции во время работы. Что он оптимизирует?"
  choices={[
    { label: "Пиковую численную пропускную способность на сервере", misconception: "Без JIT пиковая пропускная способность ниже, чем у V8; это компромисс, на который Hermes идёт." },
    { label: "Быстрый, предсказуемый запуск приложения и меньшее потребление памяти на мобильных устройствах", correct: true },
    { label: "Только меньший размер загрузки", misconception: "Байт-код не про размер загрузки прежде всего; цель — пропустить парсинг и разогрев JIT, чтобы приложение быстрее стало интерактивным." },
    { label: "Совместимость с DOM", misconception: "В React Native нет DOM; это не связано с решением про AOT-байт-код." },
  ]}
/>

<DragOrder
  id="do-tiers"
  pieceSlug="02-the-engine-landscape"
  lang="ru"
  prompt="Расставьте ярусы исполнения V8 от самого дешёвого в производстве (запускается первым) до наиболее оптимизированного (только для самого горячего кода)."
  items={[
    "Ignition (интерпретация байт-кода)",
    "Sparkplug (базовый JIT)",
    "Maglev (средний JIT)",
    "TurboFan (оптимизирующий JIT)",
  ]}
/>

<Inset kind="edgecase" lang="ru">
Bun использует JavaScriptCore, а не V8. Это значит, что микробенчмарк, настроенный нативными флагами V8 (`--allow-natives-syntax`, `%OptimizeFunctionOnNextCall`), там просто не запустится, а паттерн деопта, кусающий в V8, может вести себя иначе под ярусами DFG/FTL у JSC. Переносимый урок переживёт смену движка; специфичный для движка инструментарий — нет. Всегда отмечайте, на каком движке получены ваши числа.
</Inset>

</Explanation>

<KeyTakeaway>Четыре движка, одна архитектура: интерпретатор, ярусный оптимизирующий JIT по обратной связи о типах, hidden classes, поколенческий GC. Мы учим V8, потому что он повсюду и хорошо задокументирован; механизмы переносятся в SpiderMonkey и JSC, меняются лишь имена (Map / Shape / Structure).</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="engine-landscape-retrieval"
  lang="ru"
  questions={[
    { q: "Назовите четыре важных движка и по одному хосту для каждого.", a: "V8 (Chrome, Node, Deno, Edge, Cloudflare Workers); SpiderMonkey (Firefox); JavaScriptCore/Nitro (Safari и Bun); Hermes (React Native). Первые три — JIT-движки с одной адаптивной архитектурой; Hermes поставляет байт-код, скомпилированный заранее, чтобы оптимизировать запуск на мобильных в ущерб пиковой пропускной способности." },
    { q: "Во что переводится «hidden class» в SpiderMonkey и JavaScriptCore?", a: "Это называется Map в V8, Shape в SpiderMonkey и Structure в JavaScriptCore. Все три именуют одно и то же: дескриптор раскладки свойств объекта (имена и смещения), позволяющий движку читать свойства по фиксированному смещению, а inline caches — ключеваться по раскладке. Трек использует термин V8 — Map / hidden class." },
    { q: "Почему трек опирается на механизмы, а не на названия продуктов вроде «Crankshaft» или «TurboFan»?", a: "Оптимизирующие компиляторы заменяются каждые несколько лет (full-codegen → Crankshaft → TurboFan → Maglev), поэтому любая статья, называющая конкретный оптимизатор, быстро устаревает. Лежащие в основе механизмы — интерпретация байт-кода, обратная связь о типах, hidden classes, inline caches, поколенческий GC, деопт — стабильны десятилетиями и между движками, поэтому их изучение переносится." },
  ]}
/>

<Recap lang="ru">
Четыре движка несут боевой JavaScript: V8 (Chrome, Node, Deno, Edge), SpiderMonkey (Firefox), JavaScriptCore (Safari, Bun) и Hermes (React Native). Первые три независимо сошлись к одной адаптивной архитектуре — дешёвый интерпретатор, один или несколько оптимизирующих ярусов JIT по обратной связи о типах, hidden classes для раскладки свойств, inline caches и поколенческий сборщик мусора. Лестница ярусов V8 — это Ignition → Sparkplug → Maglev → TurboFan; у JSC и SpiderMonkey свои аналоги. Словарь расходится (Map против Shape против Structure; deopt против bailout), поэтому мы стандартизируемся на терминах V8 и отмечаем синонимы. Мы учим V8, потому что он самый распространённый и лучше всех задокументирован, но ментальная модель переносима. Теперь, когда в статье встретится слово «Shape» или «bailout», вы сразу поймёте, на какой концепт V8 это маппируется — и урок останется в силе вне зависимости от того, на каком движке работает ваш production.
</Recap>
