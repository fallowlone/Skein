---
slug: "02-shape-transitions"
lang: ru
track: "js-engine"
unit: "03-hidden-classes"
order: 2
title: "Деревья переходов, deprecation и миграция"
summary: "Добавление свойства следует по ребру перехода или создаёт его к новому Map; дерево ветвится по порядку добавления, по смене elements kind и по обобщению representation поля. Когда representation должен расшириться по всем экземплярам, старый Map помечается deprecated"
estMin: 15
status: ready
lessonType: topic
level: middle
concepts:
  - transition tree
  - TransitionArray
  - representation generalization
  - map deprecation
  - map migration
  - constness loss
  - elements-kind transition
deepensInto:
  - "04-the-jit/02-type-feedback"
spiral:
  - performance-cliffs
  - statefulness
prereqs:
  - "01-shapes-and-maps"
sources:
  - https://v8.dev/blog/fast-properties
  - https://mathiasbynens.be/notes/shapes-ics
  - https://v8.dev/docs/hidden-classes
---

import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import NumbersCard from "~/components/layout/NumbersCard.astro";
import Misconception from "~/components/layout/Misconception.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
Вы уже знаете из обзора, что «порядок свойств важен». Но вот что удивляет даже сеньоров: Map может стать *deprecated* — помеченным мёртвым, — пока объекты всё ещё указывают на него, и V8 тихо переписывает эти объекты на более новую раскладку при следующем обращении к ним. Присваивание `2.5` полю, которое раньше держало целые, может незаметно перестроить форму каждого объекта, разделяющего её. Этот урок — о том, как граф форм растёт, мутирует и заживает.
</Hook>

<Crux>Переходы: добавление свойства, расширение kind или representation, потеря const; расширение делает Map deprecated, миграция ленива.</Crux>

<Explanation>

## Переходы — это рёбра в общем дереве

Вспомним однострочную версию из `browser/03-v8-internals/03-hidden-classes`: добавление `x` затем `y` приходит в другой лист, чем добавление `y` затем `x`, так что порядок важен. Теперь механизм. Каждый Map хранит forward-**TransitionArray** с ключом `{имя свойства, attributes}`. Добавление свойства:

1. Ищет ребро `{имя, attributes}` в TransitionArray текущего Map.
2. Если ребро есть, **следует** по нему — никакой аллокации, вы переиспользуете существующий дочерний Map и его (общий) DescriptorArray.
3. Если ребра нет, **создаёт** новый дочерний Map (расширяя DescriptorArray родителя на один дескриптор), записывает ребро и направляет объект на новый Map.

Поскольку дерево разделяется всем изолятом, *второй* объект, идущий по пути, бесплатен — он следует по рёбрам, созданным первым объектом. Поэтому строить объекты одинаково дёшево, а строить их в перемешанных порядках — нет: перемешанные порядки никогда не находят ребро заново, так что каждый объект вынуждает свежий Map.

```js
const a = {}; a.x = 1; a.y = 2;   // empty -> +x -> +x,y   (создаёт 2 ребра)
const b = {}; b.x = 3; b.y = 4;   // empty -> +x -> +x,y   (следует по ним; 0 новых Map)
const c = {}; c.y = 5; c.x = 6;   // empty -> +y -> +y,x   (новая ветка; 2 новых Map)
```

## Ещё три способа перейти (не только добавление свойств)

Обзор намекал, что переходы приходят лишь от добавления свойств. Это не так. Есть три других триггера, и они кусаются в проде:

- **Смена elements kind.** Запись float'а в массив `PACKED_SMI_ELEMENTS` или создание дырки переводит Map массива в более широкий elements kind (`PACKED_SMI → PACKED_DOUBLE → PACKED_ELEMENTS` и HOLEY-варианты). Односторонне, как и дерево свойств.
- **Обобщение representation поля.** Это главное. Поле стартует узким — скажем, `Smi`. Если позже какой-то экземпляр сохранит значение, которое не помещается (`2.5` или строку), representation поля должен **расшириться** до `Double` или `Tagged`. Но representation — это свойство *Map*, общее для всех экземпляров. Так что V8 не может изменить один объект; он должен обобщить Map.
- **Потеря constness.** Поле `const`, пока каждый экземпляр присваивал ему одно и то же значение. Первый экземпляр, присвоивший *другое* значение, переводит дескриптор в `mutable`, что само по себе переход (скомпилированный код, заинлайнивший константу, должен быть инвалидирован).

Все три «не-добавляющих» триггера объединяет одно: изменение того, *что поле может держать*, вынуждает менять общий Map, а не отдельные объекты. Без этого понимания можно часами грешить на GC или сеть, когда настоящая причина — одно присваивание float'а двумя фреймами выше.

## Deprecation и миграция: как V8 расширяет общее поле

Вот тонкая часть. Пусть 10000 объектов разделяют Map `M1`, где поле `temp` имеет representation `Smi`. Теперь один объект делает `obj.temp = 98.6`. Поле должно стать `Double` — но для *всей формы*, потому что Map общий. V8 не может переписать 10000 объектов в этот миг (у него даже нет их списка). Вместо этого:

1. Он создаёт *новый* Map `M2`, идентичный `M1`, кроме того, что `temp` — `Double` (более общий representation). Разделение происходит в нужном месте дерева, потомки перенаправляются.
2. Он помечает `M1` как **deprecated** — мёртвый Map, который больше не следует использовать. Существующие объекты пока всё ещё *указывают* на `M1`.
3. **Миграция ленива.** В следующий раз, когда V8 трогает один из этих объектов по медленному пути (доступ к свойству с промахом inline cache, `%DebugPrint` и т. п.), он замечает, что Map объекта deprecated, идёт к соответствующему не-deprecated Map (`M2`) по back-pointer'ам из урока 01, переписывает раскладку объекта на месте (упаковывая целое в double-слот) и обновляет указатель map объекта. Это путь «MigrationMarker» / миграции.

Так что единственное `obj.temp = 98.6` может начать deprecation формы, используемой тысячами объектов, каждый из которых платит небольшую стоимость миграции при следующем доступе. Если это поле колеблется между целым и float'ом, можно повторно churn'ить deprecation — настоящий, трудно замечаемый источник деоптов.

<FlowDiagram
  label="Дерево переходов, где присваивание float'а целочисленному полю обобщает representation поля: исходный Map помечается deprecated, а новый Map с более широким representation становится живой целью, причём экземпляры мигрируют лениво при следующем доступе."
  hue="var(--accent)"
  perRow={3}
  caption="Обобщение в дереве. M1 (temp: Smi) разделяют многие объекты. Одно присваивание float'а вынуждает более широкий representation, поэтому V8 расщепляет на M2 (temp: Double), помечает M1 deprecated и мигрирует каждый экземпляр лениво при следующем касании — идя по back-pointer'ам к живому Map."
  nodes={[
    { id: "empty", label: "M0", sub: "пустой", col: 0, row: 0 },
    { id: "m1", label: "M1 (deprecated)", sub: "temp: Smi", col: 1, row: 0 },
    { id: "m2", label: "M2 (live)", sub: "temp: Double", col: 2, row: 0 },
    { id: "obj", label: "экземпляры", sub: "указывают на M1", col: 1, row: 1 },
    { id: "mig", label: "миграция", sub: "лениво, при доступе", col: 2, row: 1 },
  ]}
  edges={[
    { from: "empty", to: "m1", label: "+temp (Smi)" },
    { from: "m1", to: "m2", label: "обобщение" },
    { from: "obj", to: "m1", label: "устар. ptr" },
    { from: "obj", to: "mig", label: "следующ. доступ" },
    { from: "mig", to: "m2", label: "via back-pointer" },
  ]}
/>

<NumbersCard
  title="Механика переходов (V8)"
  data-lesson-visual
  stats={[
    { label: "Ключ поиска перехода", value: "{имя свойства, attributes}" },
    { label: "Повторный проход существующего пути", value: "0 новых Map — рёбра следуются" },
    { label: "Порядок расширения representation", value: "Smi → Double → Tagged (односторонне)" },
    { label: "Старый Map после обобщения", value: "помечен deprecated" },
    { label: "Миграция экземпляра", value: "лениво, при следующем медленном доступе" },
    { label: "Поиск живого Map", value: "идти по back-pointer'ам к не-deprecated потомку" },
    { label: "Трассировка переходов", value: "--trace-maps в d8 / node" },
  ]}
/>

## Почему перемешанные порядки ключей патологичны

Сложите кусочки. Функция, строящая объекты итерацией ключей входа в произвольном порядке (ре-сериализатор JSON, обобщённый mapper, генерированный код), создаёт *новую ветку* дерева переходов почти на каждый различный порядок, который видит. Формы никогда не повторяются, поэтому:

- Каждый объект аллоцирует свежие Map и DescriptorArray (раздувание памяти из урока 01).
- Нижестоящий inline cache, читающий эти объекты, видит свежий Map почти каждый раз → он становится polymorphic, затем megamorphic, затем сдаётся (урок 05).
- TurboFan не может специализироваться на стабильной форме, так что горячая функция либо не достигает верхнего яруса, либо повторно деоптится.

Фикс всегда один: строй по *фиксированной схеме* в постоянном порядке (отсутствующие поля по умолчанию `null`) или храни действительно динамические наборы ключей в коллекции `Map`, созданной для произвольных ключей.

<Misconception id="mc-only-adding">
  <p><strong>«Hidden class меняется, только когда я добавляю или удаляю свойство».</strong> Он также меняется при расширении elements kind массива, обобщении representation поля (float в int-поле) и потере constness — присваивание <em>существующему</em> свойству значения вне диапазона мутирует форму, не трогая ключи.</p>
</Misconception>

<Quiz
  id="q-generalize"
  lessonSlug="02-shape-transitions"
  lang="ru"
  question="10000 объектов разделяют Map с полем `score`, представленным как `Smi`. Один объект делает `o.score = 1.5`. Что происходит с общим Map?"
  choices={[
    { label: "Только тот один объект получает новый Map; остальные 9999 не тронуты", misconception: "Representation — свойство общего Map, а не экземпляра. V8 не может расширить поле для одного объекта — он должен обобщить Map, который использует вся форма." },
    { label: "Старый Map помечается deprecated, а новый Map с `score: Double` становится живой целью; экземпляры мигрируют лениво при следующем доступе", correct: true },
    { label: "Ничего — числа есть числа, Smi и Double для V8 одно и то же", misconception: "V8 различает Smi (распакованное малое целое) и Double (упакованный/распакованный float). Переход через эту границу вынуждает смену representation и обобщение Map." },
    { label: "Все остальные 9999 объектов переписываются немедленно и синхронно", misconception: "V8 не держит списка всех экземпляров, поэтому не может переписать их жадно. Миграция ленива: каждый объект чинится при следующем доступе по медленному пути." },
  ]}
/>

<Quiz
  id="q-follow-edge"
  lessonSlug="02-shape-transitions"
  lang="ru"
  question="Объект `a` создан `{}; a.p=1; a.q=2`. Объект `b` затем создаётся так же. Сколько новых объектов Map аллоцирует создание `b`?"
  choices={[
    { label: "Два — по одному на добавленное свойство", misconception: "Новые Map аллоцируются лишь при первом создании ребра перехода. Рёбра уже существуют от создания `a`, так что `b` следует по ним." },
    { label: "Ноль — `b` следует по рёбрам перехода, уже созданным `a`", correct: true },
    { label: "Один — переиспользуется только листовой Map", misconception: "И промежуточное (+p), и листовое (+p,q) рёбра уже существуют; b переиспользует весь путь, не аллоцируя Map." },
    { label: "Зависит от значений 1 и 2", misconception: "Пока оба значения разделяют уже записанный representation (оба Smi), обобщение не нужно, и b просто следует по существующим рёбрам." },
  ]}
/>

<DragOrder
  id="do-deprecation"
  pieceSlug="02-shape-transitions"
  lang="ru"
  prompt="Расставьте по порядку, что делает V8, когда один экземпляр присваивает float поле, которое общий Map записывает как Smi."
  items={[
    "Обнаружить, что новое значение не помещается в текущий representation поля (Smi)",
    "Создать новый Map, идентичный, кроме того, что поле обобщено до Double",
    "Пометить старый Map deprecated, чтобы он перестал использоваться для новых объектов",
    "Оставить существующие экземпляры пока указывающими на deprecated Map",
    "При следующем медленном доступе каждого экземпляра идти по back-pointer'ам к живому Map и мигрировать его на месте",
  ]}
/>

<Inset kind="edgecase" lang="ru">
Числа — не единственное обобщение. Присваивание объекта кучи туда, где поле раньше держало лишь `Smi`, обобщает representation до `Tagged` (самого общего), что навсегда отключает часть оптимизаций распаковки TurboFan для этого поля. Поле, держащее `null` для «отсутствует» и объект для «присутствует», уже `Tagged` с первой не-Smi-записи — обычно это нормально, но знайте, что это закрывает быстрый путь распакованного целого. Если поле горячее и числовое, держите его числовым.
</Inset>

</Explanation>

<KeyTakeaway>Переходы — это рёбра в общем дереве, создаваемые не только добавлением свойств, но и сменой elements kind, обобщением representation поля и потерей constness. Когда representation должен расшириться по общей форме, V8 помечает старый Map deprecated, создаёт обобщённый и мигрирует экземпляры лениво через back-pointer'ы при их следующем доступе. Перемешанные порядки создания бесконечно ветвят дерево и питают churn по IC.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="shape-transitions-retrieval"
  lang="ru"
  questions={[
    { q: "Назовите все виды событий, создающих переход к новому Map, помимо добавления свойства.", a: "Четыре вида: (1) добавление свойства следует по ребру `{имя, attributes}` в TransitionArray или создаёт его; (2) смена elements kind расширяет kind массива (PACKED_SMI → PACKED_DOUBLE → PACKED_ELEMENTS плюс HOLEY-варианты), когда вы сохраняете более широкий тип элемента или вводите дырку; (3) обобщение representation поля расширяет representation поля (Smi → Double → Tagged), когда экземпляр сохраняет значение, которое текущий representation не держит; (4) потеря constness переводит поле из const в mutable, когда по экземплярам присвоено второе различное значение, инвалидируя код, заинлайнивший константу. Первое следует по рёбрам; два последних также помечают старый Map deprecated." },
    { q: "Объясните deprecation и ленивую миграцию шаг за шагом.", a: "Когда поле должно обобщиться по общей форме, V8 создаёт новый Map, отличающийся лишь расширенным representation, и делает его живой целью; исходный Map помечается deprecated. V8 не переписывает жадно (возможно, тысячи) существующих экземпляров, потому что не держит их списка. Вместо этого каждый экземпляр продолжает указывать на deprecated Map до следующего доступа по медленному пути (промах IC, %DebugPrint и т. п.). В этот момент V8 видит маркер deprecated, идёт по back-pointer'ам к соответствующему не-deprecated Map, переписывает хранилище объекта на месте (например, упаковывая целочисленный слот в double-слот) и обновляет указатель map объекта. Это путь MigrationMarker / миграции Map." },
    { q: "Почему построение объектов итерацией ключей входа в произвольном порядке разрушает производительность, в терминах дерева переходов?", a: "Каждый различный порядок ключей идёт по другой ветке из пустого Map, так что почти каждый объект заканчивается на свежем листовом Map со своим DescriptorArray — дерево переходов ветвится комбинаторно, тратя память на Map, которые никогда не переиспользуются. Хуже того, inline cache, позже читающий эти объекты, видит новый Map почти каждый раз, так что деградирует через polymorphic в megamorphic и откатывается к общим поискам, а TurboFan не может специализироваться на стабильной форме, так что горячая функция остаётся медленной или деоптится. Фикс — строить по фиксированной схеме в постоянном порядке (отсутствующие поля по умолчанию null) или хранить динамические наборы ключей в коллекции Map." },
  ]}
/>

<Recap lang="ru">
Переход — это ребро в дереве форм, разделяемом всем изолятом. Добавление свойства ищет ребро `{имя, attributes}` в TransitionArray текущего Map и следует по нему (бесплатно) или создаёт его (один новый дочерний Map, расширяющий DescriptorArray родителя). Но три других события тоже дают переход: расширение elements kind массива, обобщение representation поля (Smi → Double → Tagged) и потеря полем constness. Обобщение representation особое, потому что representation принадлежит общему Map: V8 создаёт обобщённый Map, помечает старый deprecated и мигрирует каждый экземпляр лениво — при следующем медленном доступе он идёт по back-pointer'ам к живому Map и переписывает хранилище на месте. Построение объектов в перемешанных порядках ключей бесконечно ветвит дерево, раздувает память Map, гонит нижестоящий inline cache в megamorphic и лишает TurboFan стабильной формы; фикс — порядок создания по фиксированной схеме или коллекция Map для действительно динамических ключей. Трассировать всё это можно через `--trace-maps`. Теперь, когда увидишь внезапный провал пропускной способности после безобидного с виду присваивания существующему полю, загляни в `--trace-maps` на события deprecation — возможно, ты только что расширил representation, разделяемый тысячами объектов.
</Recap>
