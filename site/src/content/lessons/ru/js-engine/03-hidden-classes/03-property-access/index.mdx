---
slug: "03-property-access"
lang: ru
track: "js-engine"
unit: "03-hidden-classes"
order: 3
title: "Быстрые свойства, медленные свойства и slack tracking"
summary: "Три режима хранения: in-object-свойства (встроены в тело, один load), out-of-object fast properties (PropertyArray, +1 разыменование) и dictionary mode (хешмапа NameDictionary, ~50-100 циклов, включается delete'ом или избытком свойств)."
estMin: 14
status: ready
lessonType: topic
level: middle
concepts:
  - in-object properties
  - PropertyArray
  - dictionary mode
  - NameDictionary
  - slack tracking
  - HasFastProperties
  - const folding
deepensInto:
  - "04-the-jit/02-type-feedback"
spiral:
  - performance-cliffs
  - statefulness
prereqs:
  - "02-shape-transitions"
sources:
  - https://v8.dev/blog/fast-properties
  - https://v8.dev/blog/slack-tracking
  - https://mathiasbynens.be/notes/shapes-ics
---

import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import NumbersCard from "~/components/layout/NumbersCard.astro";
import Misconception from "~/components/layout/Misconception.astro";
import StackDiagram from "~/components/diagram/StackDiagram.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
Вы создаёте класс с тремя полями, но самые первые экземпляры, которые V8 аллоцирует, *крупнее*, чем нужно, — дополнены пустыми слотами. Затем, после нескольких сотен экземпляров, V8 тихо ужимает класс и освобождает дополнение. Это slack tracking, и если вы не знали о его существовании, вы неверно читали снимки кучи, снятые во время разогрева. Хранение свойств в V8 имеет три режима и один самонастраивающийся аллокатор, и то, в какой из них попадёт ваш объект, решает, будет ли чтение одной инструкцией или хеш-зондом.
</Hook>

<Crux>Свойства: in-object (один load), в PropertyArray (+1 deref) или в dictionary mode (~50-100 циклов); slack tracking задаёт слоты.</Crux>

<Explanation>

## Три режима хранения, три скорости

DescriptorArray из урока 01 сообщает V8, *где* живёт поле. Возможных ответов три, в порядке убывания скорости:

1. **In-object-свойства.** Хранятся встроенно в собственном блоке памяти объекта, сразу за заголовком. Чтение одного — это единственный load по постоянному смещению, самое быстрое из возможного. Число in-object-слотов фиксируется при создании Map.
2. **Out-of-object («normal») fast properties.** Когда объект перерастает свои in-object-слоты, дальнейшие свойства проливаются в отдельный **PropertyArray**, на который объект указывает. Они всё ещё быстрые (фиксированное смещение, дружелюбны к IC), но стоят **одного лишнего разыменования указателя**: загрузить указатель PropertyArray, затем загрузить слот. DescriptorArray записывает индекс со смыслом «это поле в PropertyArray по индексу N».
3. **Dictionary mode (медленные свойства).** Когда объект становится слишком динамичным — слишком много свойств или любой `delete` — V8 полностью отказывается от раскладки с фиксированными смещениями и переводит объект в **NameDictionary**: пер-объектную хеш-таблицу из имени свойства в значение+attributes. Каждый доступ теперь — общий поиск по хешу, примерно **50–100 циклов** против ~1 для in-object-load, и inline cache не может помочь. `%HasFastProperties(obj)` возвращает `false`.

```js
// d8 --allow-natives-syntax
const a = { p: 1, q: 2, r: 3 };
%HasFastProperties(a);   // true  — in-object, фиксированные смещения
delete a.q;
%HasFastProperties(a);   // false — теперь NameDictionary, навсегда медленный
```

<StackDiagram
  label="Три раскладки хранения свойств в V8: от самых быстрых in-object inline-слотов сверху, через out-of-object PropertyArray, до медленного пер-объектного NameDictionary внизу."
  hue="var(--accent)"
  caption="Режимы хранения по стоимости. In-object — один load; PropertyArray добавляет разыменование; dictionary mode заменяет фиксированные смещения хеш-зондом и отключает inline cache. delete или избыточная динамичность роняют вас на дно — навсегда."
  layers={[
    { label: "In-object-свойства", note: "встроены за заголовком; 1 load по постоянному смещению (быстрее всего)" },
    { label: "Out-of-object PropertyArray", note: "spill-хранилище; +1 разыменование указателя, всё ещё дружелюбно к IC" },
    { label: "Dictionary mode (NameDictionary)", note: "пер-объектная хешмапа; ~50-100 циклов, без inline cache" },
  ]}
/>

## Slack tracking: V8 подгоняет размер ваших объектов

Вот самонастраивающаяся часть, которую обзор пропустил. Когда вы определяете конструктор или класс, V8 ещё не знает, со сколькими свойствами экземпляры окажутся в итоге — за `this.x = ...; this.y = ...` могут последовать другие присваивания в методах, вызванных позже. Аллоцировать ровно два in-object-слота заранее значило бы вынудить пролив в PropertyArray в момент появления третьего поля.

Поэтому V8 **переаллоцирует**: начальный Map конструктора резервирует *лишние* in-object-слоты (slack) — больше, чем объявляет тело конструктора. По мере создания и работы экземпляров V8 следит за максимальным числом in-object-свойств, которые они реально используют. После того как аллоцировано достаточно экземпляров (фиксированный порог по числу созданий), он **завершает slack tracking**: подрезает неиспользованные слоты, ужимает instance size в Map и **финализирует** Map. С этого момента каждый новый экземпляр аллоцируется в подрезанном, точном размере.

Последствия для сеньора:

- **Экземпляры разогрева крупнее экземпляров установившегося состояния.** Снимок кучи, снятый до завершения slack tracking, завышает размер на объект. Измеряйте после разогрева.
- **Map не «финален» сразу.** Код, зависящий от стабилизированной формы (и специализации TurboFan), получает её лишь после завершения slack tracking. Микробенчмарки, аллоцирующие горстку объектов, могут видеть другую форму, чем прод в масштабе.
- **Добавление свойств вне конструктора после финализации** уже не помещается в in-object-область и проливается в PropertyArray (или, если достаточно динамично, в dictionary mode).

<FlowDiagram
  label="Жизненный цикл slack tracking: Map конструктора стартует с лишними in-object-слотами, экземпляры аллоцируются и наблюдаются, и как только достигнут порог числа экземпляров, V8 подрезает неиспользованные слоты и финализирует Map в точном instance size."
  hue="var(--accent)"
  perRow={4}
  caption="Slack tracking. Ранние экземпляры несут дополнение, чтобы поздние добавления полей оставались in-object; после порога по числу созданий V8 подрезает slack, ужимает instance size и финализирует Map — так что измеряйте размер объекта после разогрева, а не во время."
  nodes={[
    { id: "init", label: "начальный Map", sub: "лишние слоты", col: 0, row: 0 },
    { id: "alloc", label: "аллокация", sub: "наблюдение usage", col: 1, row: 0 },
    { id: "thresh", label: "порог", sub: "достаточно экз.", col: 2, row: 0 },
    { id: "final", label: "финализация", sub: "подрезка + ужатие", col: 3, row: 0 },
  ]}
  edges={[
    { from: "init", to: "alloc", label: "new()" },
    { from: "alloc", to: "thresh", label: "счёт" },
    { from: "thresh", to: "final", label: "завершено" },
  ]}
/>

## Что толкает объект в dictionary mode

Dictionary mode — это обрыв. Вы падаете с него через:

- **`delete obj.prop`** на быстром объекте — самая частая причина. V8 не может оставить дырку в раскладке с фиксированными смещениями, поэтому конвертирует весь объект в NameDictionary. Это *навсегда*: объект никогда не поднимается обратно к fast properties.
- **Слишком много свойств**, добавленных динамически (порог большой, порядка многих сотен до ~1000+, и зависит от версии), так что это редко настоящий виновник — виновник `delete`.
- **Добавление свойств способом, который ломает дерево переходов** — экстремальный churn форм может заставить V8 сдаться и переключиться в dictionary mode, чтобы перестать аллоцировать Map.

Заметьте, `const`-folding взаимодействует тут: когда поле `const` (по экземплярам присвоено лишь одно значение), V8 может хранить значение *в Map* (в дескрипторе), а не в объекте, так что чтения становятся «загрузить константу из Map» — но первое расходящееся присваивание теряет constness (урок 02), и значение переезжает в настоящее поле. Dictionary mode отказывается от всего этого.

<NumbersCard
  title="Стоимость хранения свойств (V8, 64 бита)"
  data-lesson-visual
  stats={[
    { label: "In-object-чтение", value: "1 load (~1 цикл)" },
    { label: "Чтение PropertyArray", value: "2 load'а (+1 разыменование)" },
    { label: "Чтение в dictionary mode", value: "~50-100 циклов, хеш-зонд" },
    { label: "Что включает dictionary mode", value: "delete или экстремальная динамичность" },
    { label: "Выход из dictionary mode", value: "никакого — нужен свежий объект" },
    { label: "Slack tracking завершается после", value: "фиксированного порога по числу созданий" },
    { label: "Определить режим", value: "%HasFastProperties(obj) в d8" },
  ]}
/>

<Misconception id="mc-delete-frees">
  <p><strong>«delete освобождает память, значит это хорошая гигиена».</strong> На быстром объекте <code>delete</code> конвертирует его в NameDictionary — пер-объектную хешмапу <em>больше</em> убранного слота и в ~50-100 раз медленнее на чтение, навсегда. Присваивайте <code>null</code>, чтобы сохранить форму.</p>
</Misconception>

<Quiz
  id="q-storage-mode"
  lessonSlug="03-property-access"
  lang="ru"
  question="Чтение свойства компилируется в «загрузить указатель PropertyArray, затем загрузить слот 2 этого массива». В каком режиме хранения это свойство?"
  choices={[
    { label: "In-object — оно читается прямо из тела объекта", misconception: "In-object-чтение — это единственный load по постоянному смещению в сам объект, без промежуточного указателя на массив. Лишнее разыменование здесь означает, что оно пролилось out-of-object." },
    { label: "Out-of-object fast property (PropertyArray)", correct: true },
    { label: "Dictionary mode", misconception: "Dictionary mode делает хеш-зонд по имени свойства, а не load по фиксированному индексу в PropertyArray. Фиксированный индекс здесь означает, что это всё ещё fast property, просто пролившееся." },
    { label: "Const-folding в Map", misconception: "Const-folded значение читается из дескриптора Map, а не через load из PropertyArray. Наличие индекса массива означает, что это настоящее хранимое поле." },
  ]}
/>

<Quiz
  id="q-slack"
  lessonSlug="03-property-access"
  lang="ru"
  question="Снимок кучи после аллокации 10 экземпляров класса показывает, что каждый экземпляр крупнее, чем в снимке после аллокации 10000. Почему?"
  choices={[
    { label: "Инструмент снимков неточен на малых выборках", misconception: "Инструмент точен; размеры объектов действительно различаются, потому что Map изменился между двумя снимками." },
    { label: "Slack tracking не завершился на 10 экземплярах, так что ранние объекты ещё несли переаллоцированные in-object-слоты; к 10000 V8 подрезал их и финализировал Map", correct: true },
    { label: "GC уплотнил поздние объекты до меньшего размера", misconception: "Уплотнение GC перемещает объекты, оно не меняет число слотов свойств. Разница в размере приходит от подрезки in-object-слотов slack tracking'ом, а не от GC." },
    { label: "Поздние объекты ушли в dictionary mode, который меньше", misconception: "Dictionary mode обычно больше, а не меньше, и он не установившееся состояние нормального класса. Ужатие — это slack tracking, подрезающий неиспользованные in-object-слоты." },
  ]}
/>

<DragOrder
  id="do-slack-lifecycle"
  pieceSlug="03-property-access"
  lang="ru"
  prompt="Расставьте по порядку жизненный цикл slack tracking для экземпляров конструктора."
  items={[
    "Начальный Map резервирует лишние in-object-слоты сверх того, что объявляет тело конструктора",
    "Экземпляры аллоцируются; V8 записывает максимум реально использованных in-object-свойств",
    "Достигнут фиксированный порог по числу созданных экземпляров",
    "V8 подрезает неиспользованные in-object-слоты и ужимает instance size",
    "Map финализируется; каждый последующий экземпляр аллоцируется в подрезанном точном размере",
  ]}
/>

<Inset kind="mistake" lang="ru">
Частая самопричинённая рана dictionary mode: использование обычного объекта как хешмапы с ключами от пользователя (`cache[userId] = entry`), а затем `delete cache[userId]` для вытеснения. Первый delete конвертирует `cache` в dictionary mode — что на самом деле *корректно* для настоящей мапы, но вы также теряете любую выгоду IC на нём и платите стоимость хеш-зонда на каждый доступ. Если объект концептуально словарь, используйте настоящий `Map` с самого начала: он создан для произвольных ключей и удалений, никогда не плодит hidden class и имеет предсказуемую производительность.
</Inset>

</Explanation>

<KeyTakeaway>Свойства живут в одном из трёх мест: in-object (один load), out-of-object PropertyArray (одно лишнее разыменование) или пер-объектный NameDictionary в dictionary mode (~50-100 циклов, без inline cache, навсегда). `delete` — обычный триггер обрыва. Slack tracking переаллоцирует in-object-слоты, затем подрезает и финализирует Map после порога по числу созданий, так что размеры объектов устанавливаются лишь после разогрева.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="property-access-retrieval"
  lang="ru"
  questions={[
    { q: "Опишите три режима хранения свойств и стоимость чтения в каждом.", a: "In-object-свойства хранятся встроенно в теле объекта за заголовком; чтение — единственный load по постоянному смещению (~1 цикл), самое быстрое. Out-of-object fast properties живут в отдельном PropertyArray, на который объект указывает; чтение стоит одного лишнего разыменования указателя (загрузить указатель массива, затем слот по фиксированному индексу), но всё ещё дружелюбно к IC. Dictionary mode заменяет раскладку с фиксированными смещениями пер-объектным NameDictionary (хеш-таблицей из имени в значение+attributes); каждое чтение — общий хеш-зонд (~50-100 циклов), inline cache не помогает, а %HasFastProperties возвращает false." },
    { q: "Что такое slack tracking и почему размер объекта надо измерять после разогрева?", a: "Когда конструктор или класс впервые используется, V8 не знает финального числа in-object-свойств, поэтому начальный Map переаллоцирует лишние in-object-слоты (slack), чтобы поздние добавления полей оставались in-object. По мере аллокации экземпляров V8 отслеживает максимум реально использованных in-object-свойств; после фиксированного порога по числу созданий он завершает slack tracking, подрезает неиспользованные слоты, ужимает instance size и финализирует Map. Поэтому ранние (разогревочные) экземпляры крупнее установившихся — снимок кучи до завершения завышает размер на объект, а микробенчмарки, аллоцирующие мало объектов, могут видеть другую (нефинализированную) форму, чем прод." },
    { q: "Почему `delete` хуже присваивания null в терминах хранения?", a: "Удаление свойства из быстрого объекта оставило бы дырку в раскладке с фиксированными смещениями, которую V8 не может представить, поэтому он конвертирует весь объект в dictionary mode — хеш-таблицу NameDictionary. Это навсегда: объект никогда не возвращается к fast properties, каждый доступ становится хеш-зондом ~50-100 циклов, и inline cache перестаёт работать для него. Присваивание null (или undefined) сохраняет поле, смещение, форму и быстрое in-object-чтение; вы меняете лишь значение. Для объектов, которые действительно являются словарями с churn'ящимися ключами, используйте настоящую коллекцию Map с самого начала вместо свойств объекта." },
  ]}
/>

<Recap lang="ru">
V8 хранит именованные свойства объекта в одном из трёх режимов. In-object-свойства лежат встроенно в теле объекта и читаются единственным load'ом по постоянному смещению. Когда объект перерастает свои in-object-слоты, лишние свойства проливаются в out-of-object PropertyArray, стоя одного дополнительного разыменования указателя на чтение, но оставаясь с фиксированным смещением и дружелюбными к IC. Dictionary mode — это обрыв: запускаемый главным образом `delete` (и экстремальной динамичностью), он меняет фиксированную раскладку на пер-объектную хеш-таблицу NameDictionary, делая каждый доступ хеш-зондом ~50-100 циклов без помощи inline cache, и это навсегда — лишь свежий объект восстанавливает fast properties. Slack tracking — самонастраивающийся аллокатор V8 для быстрых объектов: начальный Map конструктора резервирует лишние in-object-слоты, V8 наблюдает, сколько экземпляры реально используют по мере создания, и после фиксированного порога по числу созданий подрезает slack, ужимает instance size и финализирует Map — так что память на объект устанавливается лишь после разогрева. Определяйте режим через `%HasFastProperties(obj)`, предпочитайте `null` вместо `delete` и берите настоящий `Map`, когда ключи действительно динамичны.
</Recap>
