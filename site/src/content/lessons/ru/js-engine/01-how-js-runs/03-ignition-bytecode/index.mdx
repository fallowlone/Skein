---
slug: "03-ignition-bytecode"
lang: ru
track: "js-engine"
unit: "01-how-js-runs"
order: 3
title: "Ignition и байт-код"
summary: "Ignition — это регистровый интерпретатор байт-кода в V8. Байт-код намного компактнее машинного кода — его изначальной мотивацией была память, он урезал память под код в V8 примерно вдвое на мобильных. Один аккумулятор плюс регистровый файл"
estMin: 14
status: ready
lessonType: topic
level: middle
concepts:
  - Ignition
  - байт-код
  - регистровая машина
  - аккумулятор
  - регистровый файл
  - слот feedback vector
deepensInto:
  - "04-interpreter-loop"
  - "04-the-jit/02-type-feedback"
spiral:
  - performance-cliffs
  - statefulness
prereqs:
  - "02-lazy-parsing"
sources:
  - https://v8.dev/blog/ignition-interpreter
  - https://v8.dev/blog/launching-ignition-and-turbofan
  - https://v8.dev/docs/torque
---

import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import NumbersCard from "~/components/layout/NumbersCard.astro";
import Misconception from "~/components/layout/Misconception.astro";
import StackDiagram from "~/components/diagram/StackDiagram.astro";

<Hook>
Когда V8 выпустил Ignition в 2017, заголовком была не скорость — а *память*. На дешёвом Android-телефоне машинный код, который V8 генерировал для каждой функции, съедал десятки мегабайт RAM, и большая часть — для кода, выполнявшегося считанные разы. Решением было перестать генерировать машинный код заранее и вместо этого исполнять компактный байт-код, урезав память под код в V8 примерно вдвое. Этот байт-код и регистровая машина, которая его исполняет, — это то, что стоит между вашим распарсенным AST и быстрым путём.
</Hook>

<Crux>Ignition — регистровый интерпретатор байт-кода: аккумулятор плюс регистровый файл и слот обратной связи на типозависимых операциях.</Crux>

<Explanation>

## Байт-код: компактен по замыслу, и причина — память

После того как парсер строит AST (и только когда функция реально вызвана — вспомните ленивый парсинг), **генератор байт-кода** понижает это дерево в **байт-код Ignition**: плоскую последовательность мелких инструкций для виртуальной машины. Байт-код стоит между исходником и машинным кодом. Он медленнее в исполнении, чем нативный машинный код, но драматически **компактнее** — один байт-код обычно один-два байта против десятков байт машинного кода на ту же операцию.

Эта компактность и была *смыслом*. До Ignition базовый компилятор V8 жадно генерировал машинный код для каждой функции, и на стеснённых по памяти мобильных устройствах накопленный код пробивал бюджет RAM. Ignition заменил жадный машинный код байт-кодом, который V8 интерпретирует, **урезав память под код в V8 примерно вдвое**, сохраняя при этом быстрый старт. Скорость пришла позже — от ярусов JIT, компилирующих горячий байт-код; основополагающей мотивацией был объём.

<NumbersCard
  title="Зачем байт-код и чего он стоит"
  data-lesson-visual
  stats={[
    { label: "Изначальная мотивация Ignition (2016)", value: "память" },
    { label: "Сокращение памяти под код в V8", value: "~50%" },
    { label: "Типичный размер байт-кода", value: "1-2 байта / оп" },
    { label: "Стиль VM в Ignition", value: "регистровая" },
    { label: "Стиль VM в JVM / CPython", value: "стековая" },
    { label: "Флаг для просмотра байт-кода", value: "--print-bytecode" },
  ]}
/>

## Регистровая машина, а не стековая

Есть два классических способа построить VM байт-кода. **Стековая машина** (JVM, CPython) держит операнды на неявном стеке операндов: `PUSH a; PUSH b; ADD` снимает два, кладёт сумму. **Регистровая машина** держит операнды в именованных регистрах, и операции ссылаются на них напрямую: `ADD r1, r2`. Ignition — это **регистровая машина**.

Регистровый байт-код чуть труднее генерировать, но даёт **меньше, более плотных инструкций** — нет постоянной возни с push/pop на каждый операнд — что означает меньше диспетчеризаций в цикле интерпретатора на единицу работы. Эта плотность — часть того, почему регистровый дизайн подходит движку с JIT: меньше байт-кодов интерпретировать в холодном состоянии и более чистый поток для компиляции в горячем.

Регистровая модель Ignition — это конкретно **аккумулятор + регистровый файл**:

- **Аккумулятор** — единственный неявный регистр, из которого большинство байт-кодов читают и в который пишут. Он протягивает промежуточные значения от одной инструкции к следующей, так что не всем операндам нужно явное имя. Многие операции берут *один* явный операнд и неявно используют аккумулятор как другой вход и как выход.
- **Регистровый файл** — набор именованных локальных регистров — `r0`, `r1`, `r2`, … — хранящих локальные переменные функции и временные значения, аллоцированный на каждый фрейм функции.

<StackDiagram
  label="Состояние исполнения Ignition для одного фрейма функции: единственный регистр-аккумулятор протягивает значения между байт-кодами, именованный регистровый файл хранит локальные и временные значения, а feedback vector на каждую функцию собирает наблюдения о типах для JIT."
  hue="var(--accent)"
  caption="Аккумулятор — неявный хаб: большинство байт-кодов читают и пишут его. Регистровый файл хранит именованные локальные; feedback vector пишется по мере исполнения чувствительных к типам байт-кодов."
  layers={[
    { label: "Аккумулятор", note: "единственный неявный регистр; протягивает значения оп-к-оп" },
    { label: "Регистровый файл (r0, r1, r2, …)", note: "именованные локальные и временные этого фрейма" },
    { label: "Feedback vector", note: "по слоту на чувствительный к типам байт-код; готовит JIT" },
    { label: "Массив байт-кода + смещение", note: "инструкции и текущая позиция" },
  ]}
/>

## Читаем настоящий байт-код

Возьмите простейшую функцию и посмотрите, что исполняет Ignition. Запустите `d8 --print-bytecode add.js` на:

```js
function add(a, b) { return a + b; }
add(1, 2);
```

Вы получите что-то близкое к этому (операнды и индексы слотов сокращены):

```text
Ldar a1          // загрузить аргумент b в аккумулятор
Add a0, [0]      // аккумулятор = a0 (a) + аккумулятор (b); слот обратной связи 0
Return           // вернуть аккумулятор
```

Заметьте аккумулятор-центричную форму: `Ldar` (**L**oa**d** **A**ccumulator from **R**egister) кладёт `b` в аккумулятор; `Add a0, [0]` прибавляет регистр `a0` (аргумент `a`) к аккумулятору и оставляет сумму в аккумуляторе; `Return` возвращает аккумулятор. Нет `Push`/`Pop` — аккумулятор и есть общая рабочая область.

## `[slot]` — это место, где JIT получает информацию о типах

Посмотрите снова на `Add a0, [0]`. Этот `[0]` — **индекс слота feedback vector**. Каждый байт-код, чьё поведение зависит от типов времени исполнения — арифметика, загрузки/записи свойств, вызовы, сравнения — несёт индекс слота в **feedback vector** функции, массив на каждую функцию, аллоцированный рядом с байт-кодом. По мере того как интерпретатор исполняет этот байт-код, он записывает *что реально увидел* в этот слот: операнды `Add` были Smi, или объект при загрузке свойства имел определённый hidden class, или эта точка вызова всегда попадала в одну функцию.

Это и есть мост к оптимизатору. Интерпретатор, по сути, **профилирует программу бесплатно**, пока её исполняет. Когда функция становится горячей, JIT (Maglev, TurboFan — разбираются в юните 04) читает feedback vector, чтобы делать спекулятивные предположения: «этот `Add` видел только целые, значит, выпускаем целочисленное сложение». Обратная связь о типах собирается *здесь*, в Ignition, по слоту на чувствительный к типам байт-код — поэтому этот урок стоит прямо выше JIT и обратной связи о типах по потоку.

<Misconception id="mc-bytecode-fast">
  <p><strong>«Байт-код нужен, чтобы сделать JS быстрее машинного кода».</strong> Нет — байт-код Ignition исполняется <em>медленнее</em> кода, который он заменил. Его цель — память: байт-код намного компактнее, урезая память под код в V8 на ~50% на мобильных. Скорость даёт JIT, компилируя горячий байт-код позже.</p>
</Misconception>

## Общие обработчики, написанные на Torque

Можно ожидать, что каждый байт-код — это написанный вручную ассемблер под каждый CPU. Вместо этого V8 пишет **обработчик** каждого байт-кода — процедуру, выполняющую работу этого опкода — один раз, на переносимом низкоуровневом DSL: изначально **CodeStubAssembler (CSA)**, теперь в основном **Torque**, который компилируется в CSA. Эти обработчики генерируются в машинный код на этапе сборки и **разделяются всеми функциями**: есть один обработчик `Add`, один обработчик `LdaNamedProperty` и так далее. Интерпретатор по сути — это таблица этих общих обработчиков, и исполнение функции означает диспетчеризацию через них — тема следующего урока, цикла интерпретатора.

<Quiz
  id="q-why-bytecode"
  lessonSlug="03-ignition-bytecode"
  lang="ru"
  question="Ignition заменил жадно генерируемый машинный код интерпретируемым байт-кодом в 2017. Какой была главная мотивация?"
  choices={[
    { label: "Байт-код исполняется быстрее машинного кода", misconception: "Он исполняется медленнее. Байт-код меняет скорость исполнения на компактность; скорость приходит от JIT, компилирующего горячий байт-код." },
    { label: "Память — байт-код намного компактнее, урезая память под код в V8 примерно вдвое на мобильных", correct: true },
    { label: "Байт-код переносим между CPU, поэтому V8 не нужен код, специфичный для архитектуры", misconception: "Обработчики всё равно компилируются в машинный код под каждую архитектуру; переносимость — приятное свойство, но движущей причиной был объём памяти." },
    { label: "Это устранило необходимость в сборщике мусора", misconception: "GC не связан; Ignition изменил, как представлен код, а не как освобождаются объекты." },
  ]}
/>

<Quiz
  id="q-slot"
  lessonSlug="03-ignition-bytecode"
  lang="ru"
  question="В байт-коде `Add a0, [0]` что такое `[0]`?"
  choices={[
    { label: "Второй регистр-источник, хранящий другой операнд", misconception: "Операнды — это аккумулятор и a0; [0] это не регистр, а индекс слота feedback vector." },
    { label: "Индекс слота feedback vector, куда интерпретатор записывает наблюдаемые типы, чтобы подготовить JIT", correct: true },
    { label: "Число инструкций CPU для сложения", misconception: "Байт-код не кодирует число циклов CPU; [0] индексирует feedback vector, а не тайминг." },
    { label: "Глубина стека перед операцией", misconception: "Ignition регистровый с аккумулятором, а не стековая машина; здесь нет глубины стека операндов." },
  ]}
/>

<DragOrder
  id="do-add-bytecode"
  pieceSlug="03-ignition-bytecode"
  lang="ru"
  prompt="Расставьте по порядку байт-коды, которые Ignition исполняет для `function add(a, b) { return a + b; }`, при условии что a0 хранит a, а a1 хранит b."
  items={[
    "Ldar a1 — загрузить аргумент b в аккумулятор",
    "Add a0, [0] — прибавить регистр a0 к аккумулятору; записать типы в слот обратной связи 0",
    "Return — вернуть значение в аккумуляторе",
  ]}
/>

<Inset kind="why" lang="ru">
Зачем вообще аккумулятор, а не всегда именовать оба операнда, как `Add r1, r2, r3`? Аккумулятор позволяет большинству байт-кодов нести *на один операнд меньше* — результат и один вход неявны — что делает поток байт-кода короче, а цикл диспетчеризации плотнее. Это классический компромисс интерпретатора: чуть больше трафика `Ldar`/`Star` (загрузка/сохранение аккумулятора) в обмен на более плотные, быстрее диспетчеризуемые частые операции. Замеры V8 показали, что дизайн с аккумулятором — чистый выигрыш по пропускной способности интерпретатора и размеру кода.
</Inset>

</Explanation>

<KeyTakeaway>Ignition — это регистровый интерпретатор байт-кода; байт-код существует прежде всего ради памяти (компактен, ~50% меньше памяти под код, чем жадный машинный код). Его модель — единственный аккумулятор плюс именованный регистровый файл, с обработчиками, написанными один раз на Torque/CSA и общими. Каждый чувствительный к типам байт-код несёт `[slot]` feedback vector, куда интерпретатор записывает наблюдаемые типы — данные, которые JIT читает позже.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="ignition-bytecode-retrieval"
  lang="ru"
  questions={[
    { q: "Зачем Ignition вообще использует байт-код и какой была изначальная мотивация?", a: "После того как парсер строит AST и функция впервые вызвана, генератор байт-кода понижает дерево в компактный байт-код Ignition (обычно 1-2 байта на операцию), который V8 интерпретирует. Основополагающей мотивацией в 2016-2017 была память, а не скорость: V8 раньше жадно генерировал машинный код для каждой функции, пробивая бюджет RAM на дешёвых мобильных. Переход на интерпретируемый байт-код урезал память под код в V8 примерно вдвое. Байт-код исполняется медленнее машинного кода; скорость восстанавливается позже ярусами JIT, компилирующими горячий байт-код." },
    { q: "Опишите регистровую модель Ignition и противопоставьте её стековой машине.", a: "Ignition — регистровая машина: операнды живут в именованных регистрах, и операции ссылаются на них напрямую (Add a0, [slot]), в отличие от стековой машины (JVM, CPython), которая кладёт и снимает операнды на неявном стеке операндов. Конкретно Ignition использует аккумулятор плюс регистровый файл: аккумулятор — это один неявный регистр, который большинство байт-кодов читают и пишут, протягивая промежуточные значения оп-к-оп; регистровый файл — это именованные локальные r0, r1, r2... фрейма. Дизайн с регистрами/аккумулятором даёт меньше, более плотных байт-кодов и меньше диспетчеризаций на единицу работы." },
    { q: "Что такое [slot] в байт-коде вроде `Add a0, [0]` и почему он важен для производительности?", a: "Это индекс в feedback vector функции, массив на каждую функцию, аллоцированный рядом с байт-кодом. Каждый чувствительный к типам байт-код (арифметика, загрузка/запись свойства, вызов, сравнение) несёт индекс слота, и по мере исполнения интерпретатор записывает то, что реально наблюдал — типы операндов, hidden classes, цели вызова — в этот слот. Так интерпретатор профилирует программу бесплатно. Когда функция становится горячей, JIT читает feedback vector, чтобы делать спекулятивные предположения о типах (например, выпустить целочисленное сложение, если слот видел только Smi). Так что обратная связь о типах собирается в Ignition, по слоту на чувствительную к типам операцию." },
  ]}
/>

<Recap lang="ru">
Как только функция впервые вызвана, генератор байт-кода V8 понижает её AST в байт-код Ignition — плоскую последовательность мелких (1-2 байта) инструкций для виртуальной машины. Байт-код существует прежде всего ради памяти: замена жадно генерируемого машинного кода интерпретируемым байт-кодом урезала память под код в V8 примерно вдвое на мобильных, что было основополагающей мотивацией Ignition в 2016-2017; скорость исполнения медленнее машинного кода и восстанавливается позже ярусами JIT. Ignition — регистровая машина, а не стековая (JVM/CPython), и конкретно использует аккумулятор плюс регистровый файл: единственный аккумулятор протягивает значения от одного байт-кода к следующему, тогда как именованные регистры r0, r1, … хранят локальные фрейма. Функция настолько простая, как `add(a, b)`, компилируется примерно в `Ldar a1; Add a0, [0]; Return`, показывая аккумулятор-центричную форму и отсутствие push/pop. `[0]` — это индекс слота feedback vector: каждый чувствительный к типам байт-код несёт один, и интерпретатор записывает наблюдаемые типы туда по мере исполнения, профилируя программу бесплатно, чтобы JIT мог позже специализироваться. Обработчики байт-кода написаны один раз на Torque/CSA и общие для всех функций. Посмотреть всё это можно через `d8 --print-bytecode`.
</Recap>
