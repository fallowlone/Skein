---
slug: "02-lazy-parsing"
lang: ru
track: "js-engine"
unit: "01-how-js-runs"
order: 2
title: "Ленивый парсинг и пре-парсер"
summary: "V8 запускает два парсера: полный, который строит AST и аллоцирует области видимости, и PreParser, который бегло проходит тела функций, находя границы и ранние ошибки без построения дерева. Функции пре-парсятся при загрузке и полностью парсятся только при первом вызове"
estMin: 13
status: ready
lessonType: topic
level: junior
concepts:
  - PreParser
  - полный парсер
  - ленивый парсинг
  - двойной парсинг
  - IIFE / PIFE
  - эвристика жадного парсинга
deepensInto:
  - "03-ignition-bytecode"
spiral:
  - performance-cliffs
  - startup-latency
prereqs:
  - "01-source-to-ast"
sources:
  - https://v8.dev/blog/preparser
  - https://v8.dev/blog/scanner
  - https://web.dev/articles/optimizing-content-efficiency-javascript-startup-optimization
---

import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import NumbersCard from "~/components/layout/NumbersCard.astro";
import Misconception from "~/components/layout/Misconception.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
Бандл вашего приложения объявляет при загрузке четыреста функций, но холодный путь вызывает, может, двадцать из них. Если бы V8 полностью парсил все четыреста заранее — строя AST и объект области видимости для каждой — большая часть этой работы была бы выброшена непрочитанной. Поэтому он так не делает. Он делает быстрый беглый проход по каждому телу функции, которое может отложить, и полностью парсит функцию только в тот момент, когда её кто-то реально вызывает. Подвох: функция, которую *вызывают*, в итоге парсится дважды.
</Hook>

<Crux>V8 пре-парсит тела функций при загрузке — быстрый проход за границами и ранними ошибками — а полностью парсит лишь на первом вызове.</Crux>

<Explanation>

## Два парсера, а не один

Предыдущий урок описывал «парсер» как одну штуку. На деле V8 поставляет **два** парсера, разделяющих сканер:

- **Полный парсер.** Строит полное AST, разрешает области видимости переменных, аллоцирует объекты областей видимости — всё, что нужно генератору байт-кода. Это дорогой.
- **PreParser.** Намеренно *неполный* парсер, который быстро бегло проходит тело функции. Он **не** строит AST и **не** аллоцирует области видимости. Его задача узка: найти, где функция заканчивается (чтобы движок знал диапазон байтов, к которому вернуться), и обнаружить **ранние ошибки** — синтаксические ошибки и горстку проблем класса `SyntaxError` (`return` вне функции, дублирующийся параметр в strict-режиме), которые спецификация требует сообщать даже для кода, который никогда не выполняется.

При холодной загрузке V8 **не** парсит большинство тел функций полностью. Он их пре-парсит: бегло проходит, записывает диапазон исходника и немного метаданных, идёт дальше. Полный парсинг данной функции откладывается до момента, когда функцию **впервые вызовут**. Это **ленивый парсинг**, и это крупнейший выигрыш на старте во фронт-энд конвейере, потому что большинство функций в типичном бандле на холодном пути никогда не вызываются.

<FlowDiagram
  label="При загрузке V8 пре-парсит тело функции — быстрый беглый проход, который записывает её диапазон исходника и проверяет ранние ошибки, но не строит AST. При первом вызове функции V8 возвращается и полностью парсит этот диапазон, строя AST и области видимости, затем генерирует байт-код."
  hue="var(--accent)"
  perRow={4}
  caption="Ленивый парсинг разделяет работу во времени: дешёвый беглый проход сейчас, дорогой полный парсинг только при первом вызове. Никогда не вызванные функции никогда не парсятся полностью."
  nodes={[
    { id: "load", label: "При загрузке", sub: "беглый pre-parse", col: 0, row: 0 },
    { id: "span", label: "Запись диапазона", sub: "+ ранние ошибки", col: 1, row: 0 },
    { id: "call", label: "Первый вызов", sub: "триггер полного парсинга", col: 2, row: 0 },
    { id: "ast", label: "Полный парсинг", sub: "AST + байт-код", col: 3, row: 0 },
  ]}
  edges={[
    { from: "load", to: "span", label: "skim" },
    { from: "span", to: "call", label: "defer" },
    { from: "call", to: "ast", label: "parse по-настоящему" },
  ]}
/>

## Ловушка: вызванная функция парсится дважды

Ленивый парсинг — это ставка. Ставка окупается для функций, которые никогда не вызываются: вы потратили один дешёвый беглый проход вместо одного дорогого полного парсинга. Но для функции, которую **вызывают**, вы проигрываете ставку — вы платите за беглый pre-parse *и затем* за полный парсинг. Это **проблема двойного парсинга**: лениво распарсенная функция, которая позже выполняется, стоит примерно **2×** работы парсинга по сравнению с функцией, распарсенной жадно один раз.

Для большинства кода это правильный компромисс: экономия на никогда не вызываемом большинстве перевешивает двойную стоимость на вызываемом меньшинстве. Но на горячем стартовом пути — функции, про которые вы *знаете*, что они выполняются сразу, вроде бутстрапа вашего фреймворка или инициализатора верхнего уровня модуля — платить за два парсинга это чистая трата. Вы бы предпочли, чтобы V8 просто распарсил их жадно, один раз.

<NumbersCard
  title="Пре-парсер против полного парсера"
  data-lesson-visual
  stats={[
    { label: "PreParser строит AST?", value: "нет" },
    { label: "PreParser аллоцирует области видимости?", value: "нет" },
    { label: "Скорость PreParser против полного парсинга", value: "примерно ~2x быстрее" },
    { label: "Вызванная лениво распарсенная функция платит", value: "pre-parse + полный парсинг" },
    { label: "Штраф двойного парсинга", value: "~2x работы парсинга" },
    { label: "Ранние ошибки на pre-parse", value: "синтаксис, дубль параметров (strict)" },
  ]}
/>

## Эвристика жадного парсинга: скобки значат «запусти меня сейчас»

У V8 есть эвристика, чтобы вывести функцию *из* ленивого парсинга: если функциональное выражение **обёрнуто в скобки**, V8 парсит его жадно, потому что скобки — классический сигнал **немедленно вызываемого функционального выражения (IIFE)** — функции, которую автор намерен вызвать сразу.

```js
// Парсится жадно: скобки сигнализируют «это выполняется немедленно».
(function () {
  bootstrapTheApp();
})();

// По умолчанию парсится лениво: выглядит как определение для вызова позже.
function maybeLater() {
  doExpensiveThing();
}
```

Функцию, написанную так, чтобы *выглядеть* как IIFE — даже со скобками, но вызываемую отдельно — называют **PIFE (Possibly-Invoked Function Expression)**, и V8 трактует ведущую `(` как подсказку жадного парсинга. Это ровно тот трюк, который эксплуатировал старый инструмент **`optimize-js`**: он механически оборачивал функции бутстрапа модулей в скобки, чтобы V8 парсил их жадно и пропускал двойной парсинг на стартовом пути. Современные сборщики (webpack, Rollup) выпускают обёртки IIFE/PIFE вокруг фабрик модулей по той же причине — это реальная, измеримая оптимизация старта, а не фольклор.

<Misconception id="mc-lazy-free">
  <p><strong>«Ленивый парсинг всегда выигрыш».</strong> Он выигрывает только для функций, которые никогда не вызываются. Функция, которую <em>вызывают</em>, парсится дважды — pre-parse плюс полный парсинг — поэтому на горячем стартовом пути жадный парсинг (обёртка IIFE) дешевле двойного парсинга.</p>
</Misconception>

## Вложенные функции едут вместе со своей внешней функцией

Есть структурная тонкость: когда V8 полностью парсит внешнюю функцию, он **пре-парсит** вложенные внутри неё функции — он не парсит их тоже полностью. Так что глубоко вложенный хелпер пре-парсится, когда его охватывающая функция парсится полностью, и парсится полностью лишь тогда, когда этот внутренний хелпер сам впервые вызван. Лень рекурсивна: каждый уровень откладывает следующий. Вот почему гигантская функция уровня модуля со множеством вложенных замыканий всё равно может стартовать дёшево — вложенность бегло проходится, а не строится полностью, пока каждый кусок не запустится.

<Quiz
  id="q-double-parse"
  lessonSlug="02-lazy-parsing"
  lang="ru"
  question="Функция на вашем пути бутстрапа лениво распарсена и затем вызвана один раз при старте. Сколько раз парсится её тело и оптимально ли это здесь?"
  choices={[
    { label: "Один раз (только полный парсинг) — ленивый парсинг пропускает pre-parse для вызванных функций", misconception: "Ленивый парсинг не пропускает pre-parse. Беглый проход случился при загрузке; полный парсинг случается при первом вызове." },
    { label: "Дважды (pre-parse при загрузке, затем полный парсинг при первом вызове) — неоптимально для кода, про который вы знаете, что он выполняется сразу", correct: true },
    { label: "Ноль раз — V8 переиспользует закэшированный байт-код с предыдущего запуска", misconception: "Кэширование кода помогает между запусками, но внутри запуска вызванная лениво распарсенная функция парсится; вопрос о числе парсингов, и оно равно двум." },
    { label: "Трижды — pre-parse, полный парсинг, затем повторный парсинг для JIT", misconception: "JIT компилирует байт-код, он не парсит исходник заново. Двойной парсинг — это ровно pre-parse плюс полный парсинг." },
  ]}
/>

<Quiz
  id="q-iife"
  lessonSlug="02-lazy-parsing"
  lang="ru"
  question="Почему обёртывание стартовой функции в скобки — (function(){...})() — заставляет V8 парсить её жадно?"
  choices={[
    { label: "Скобки превращают функцию в другой, более быстрый опкод", misconception: "Это не другой опкод; скобки — это подсказка на этапе парсинга о том, когда функция запустится, а не изменение в рантайме." },
    { label: "Ведущая ( — это эвристика V8 для IIFE — функции, предназначенной выполниться немедленно — поэтому жадный парсинг избегает двойного парсинга", correct: true },
    { label: "Скобки делают функцию глобальной, поэтому её нужно распарсить первой", misconception: "Скобки не меняют область видимости или глобальность; они лишь поднимают флаг эвристики жадного парсинга." },
    { label: "Это заставляет функцию полностью пропустить AST", misconception: "Жадный парсинг всё равно строит AST — он просто строит его один раз, заранее, вместо pre-parse, а затем полного парсинга." },
  ]}
/>

<DragOrder
  id="do-lazy-lifecycle"
  pieceSlug="02-lazy-parsing"
  lang="ru"
  prompt="Расставьте по порядку события парсинга для обычной (не-IIFE) функции, которая определена при загрузке и затем вызвана один раз."
  items={[
    "При загрузке V8 пре-парсит тело: записывает его диапазон исходника и проверяет ранние ошибки",
    "AST или область видимости пока не строятся; движок идёт дальше к остальной части файла",
    "Функцию вызывают в первый раз",
    "V8 возвращается и полностью парсит этот диапазон — строя AST и области видимости — затем выпускает байт-код",
  ]}
/>

<Inset kind="edgecase" lang="ru">
Есть тонкая причина корректности, по которой PreParser вообще существует, а не V8 просто пропускает невызванные функции: спецификация требует сообщать **ранние ошибки** для синтаксически невалидного кода, даже если он никогда не выполняется. Так что функция с синтаксической ошибкой должна бросить на этапе загрузки, а не тихо таиться до вызова. PreParser — самый дешёвый способ соблюсти это: он валидирует синтаксис и несколько правил ранних ошибок (вроде дублирующихся параметров в strict-режиме), не платя за полное AST.
</Inset>

</Explanation>

<KeyTakeaway>У V8 два парсера: полный парсер (AST + области видимости) и быстрый PreParser (беглый проход за границами и ранними ошибками, без AST). Тела функций пре-парсятся при загрузке и парсятся полностью только при первом вызове — экономя старт для невызванного кода, но стоя вызванной функции ~2× работы парсинга. IIFE/PIFE `(function(){…})()` парсится жадно, потому что ведущая `(` сигнализирует немедленное исполнение; сборщики и старый `optimize-js` эксплуатируют это на стартовом пути.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="lazy-parsing-retrieval"
  lang="ru"
  questions={[
    { q: "Какие два парсера у V8 и что делает каждый?", a: "Полный парсер строит полное AST, разрешает области видимости переменных и аллоцирует объекты областей видимости — всё, что нужно генератору байт-кода; это дорогой. PreParser — намеренно неполный беглый проход, который не строит AST и не аллоцирует области видимости: его единственные задачи — найти, где заканчивается тело каждой функции (записав диапазон исходника), и обнаружить ранние ошибки (синтаксические ошибки, дублирующиеся параметры в strict-режиме), которые спецификация требует сообщать даже для никогда не запускаемого кода. Он примерно в 2x быстрее полного парсинга." },
    { q: "Объясните проблему двойного парсинга и когда ленивый парсинг это чистый проигрыш.", a: "Ленивый парсинг откладывает полный парсинг функции до первого вызова, пре-парся её при загрузке. Для никогда не вызываемой функции это чистый выигрыш (один дешёвый беглый проход вместо одного полного парсинга). Но функция, которую ВЫЗЫВАЮТ, платит за оба: беглый pre-parse при загрузке и затем полный парсинг при первом вызове — примерно 2x работы парсинга. Так что на горячем стартовом пути, где вы знаете, что функции выполняются сразу, ленивый парсинг это чистый проигрыш, и жадный парсинг предпочтительнее." },
    { q: "Как заставить стартовую функцию парситься жадно и кто использует это на практике?", a: "Оберните её в скобки как IIFE/PIFE: (function(){...})(). Эвристика жадного парсинга V8 трактует ведущую ( как сигнал, что функция выполняется немедленно, поэтому он парсит её полностью один раз вместо pre-parse-затем-полный. Старый инструмент optimize-js механически добавлял эти обёртки к функциям бутстрапа; современные сборщики (webpack, Rollup) выпускают обёртки IIFE/PIFE вокруг фабрик модулей ради того же измеримого выигрыша на старте. Вложенные функции полностью распарсенной внешней функции сами лишь пре-парсятся, пока их не вызовут." },
  ]}
/>

<Recap lang="ru">
V8 не парсит каждую функцию полностью при загрузке — он поставляет два парсера. Полный парсер строит AST, разрешает области видимости и аллоцирует объекты областей видимости (дорогой путь, нужный генератору байт-кода), тогда как PreParser — это быстрый, намеренно неполный беглый проход, который не строит ни AST, ни областей видимости: он лишь находит диапазон исходника каждой функции и сообщает ранние ошибки (синтаксические ошибки, дублирующиеся параметры в strict-режиме), которые спецификация требует даже для кода, который никогда не выполняется. При холодной загрузке V8 пре-парсит тела функций и полностью парсит данную функцию лишь при первом её вызове — ленивый парсинг, крупнейший выигрыш на старте во фронт-энде, потому что большинство функций на холодном пути никогда не вызываются. Компромисс — проблема двойного парсинга: вызванная функция платит и за pre-parse, и за полный парсинг, примерно 2× работы парсинга, что расточительно для кода, про который вы знаете, что он выполняется сразу. Аварийный выход — эвристика жадного парсинга: функция, обёрнутая в скобки, IIFE/PIFE `(function(){…})()`, парсится жадно, потому что ведущая `(` сигнализирует немедленное исполнение — трюк, который инструмент `optimize-js` и современные сборщики используют, чтобы держать стартовый путь вне двойного парсинга. Вложенные функции пре-парсятся, когда их внешняя функция парсится полностью, так что лень рекурсивна, уровень за уровнем.
</Recap>
