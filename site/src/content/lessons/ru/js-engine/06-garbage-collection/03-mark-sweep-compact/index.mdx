---
slug: "03-mark-sweep-compact"
lang: ru
track: "js-engine"
unit: "06-garbage-collection"
order: 3
title: "Major GC: mark, sweep, compact"
summary: "Old space собирается через mark-sweep-compact. Разметка использует трёхцветную абстракцию (white/grey/black) и идёт инкрементально и конкурентно, чтобы сжать окно stop-the-world; sweep освобождает белые объекты в free lists; compaction эвакуирует живые объекты"
estMin: 15
status: ready
lessonType: topic
level: senior
concepts:
  - mark-sweep-compact
  - трёхцветная разметка (tri-color)
  - incremental marking
  - concurrent marking
  - sweep / free lists
  - compaction / эвакуация
deepensInto:
  - "06-garbage-collection/04-write-barriers"
spiral:
  - latency
  - memory
prereqs:
  - "02-generational-orinoco"
sources:
  - https://v8.dev/blog/trash-talk
  - https://v8.dev/blog/concurrent-marking
  - https://v8.dev/blog/high-performance-cpp-gc
---

import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import NumbersCard from "~/components/layout/NumbersCard.astro";
import Misconception from "~/components/layout/Misconception.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
Scavenger субмиллисекундный, потому что молодое пространство крошечное. Old space — противоположность: это может быть гигабайт долгоживущих объектов, и вы не можете «выкопироваться» из него, как Scavenger, — нет запасного гигабайта, куда копировать. Поэтому major GC принимает другую форму, и именно его пауза ваши пользователи ощущают как пропущенный кадр или зависший запрос. Вся инженерная история современного major GC — это одно предложение: делать как можно больше работы *вне* главного потока, чтобы часть, которая обязана остановить мир, сжималась к нулю.
</Hook>

<Crux>Old space: mark-sweep-compact — трёхцветная разметка (инкрементально/конкурентно), sweep в free lists, compaction фрагментов.</Crux>

<Explanation>

## Почему old space нужен другой сборщик

Копирующему Scavenger'у нужна вторая область размером с живое множество, куда копировать; это нормально для нескольких мегабайт молодых объектов, но невозможно для old generation в гигабайт. Поэтому old space использует **mark-sweep-compact (MSC)**: найти живые объекты на месте (mark), освободить мёртвые на месте (sweep) и лишь изредка переместить выживших, чтобы бороться с фрагментацией (compact). Major GC трассирует *весь* достижимый граф, а не маленькую область, поэтому его стоимость принципиально больше — и поэтому V8 столь усиленно вкладывается в сокрытие этой стоимости.

## Разметка: трёхцветная абстракция

Разметка — это обход графа с приёмом, делающим его паузируемым. Каждый объект концептуально один из трёх цветов (хранится в двух mark-битах):

- **white** (белый) — ещё не достигнут (предполагается мёртвым);
- **grey** (серый) — достигнут, но его исходящие ссылки ещё не просканированы (в рабочей очереди / «marking worklist»);
- **black** (чёрный) — достигнут *и* полностью просканирован (все его ссылки уже посерены/обработаны).

Алгоритм:

1. Покрасить всё в **white**. Покрасить **прямых референтов корней в grey** (положить их в worklist).
2. Извлечь **grey**-объект, просканировать его ссылки — покрасить каждого белого референта, на которого он указывает, в **grey** — затем покрасить сам объект в **black**.
3. Повторять, пока **не останется grey-объектов.**
4. Каждый всё ещё **white** объект недостижим → мусор.

Серое множество — это фронт волны, идущей по графу; когда оно истощается, трассировка завершена. Ключевой инвариант, который это поддерживает: **чёрный объект никогда не должен указывать напрямую на белый** («трёхцветный инвариант»). Если бы это требовалось только для stop-the-world трассировки, было бы тривиально, — но V8 не останавливает мир для разметки, и вот тут становится интересно.

<FlowDiagram
  label="Трёхцветная разметка: корни красят своих референтов в grey; маркер извлекает grey-объект, серит его белых референтов и чернит его; когда не остаётся grey, каждый белый объект — недостижимый мусор."
  hue="var(--accent)"
  caption="Трёхцветный фронт волны. Black готов, grey — фронт в worklist, white — недоказан. Трассировка кончается, когда grey истощается; выживший white = мусор. Инвариант «чёрный никогда не указывает на белый» защищают write barriers (следующий урок)."
  nodes={[
    { id: "root", label: "Корни", sub: "серят референтов", col: 0, row: 1 },
    { id: "blk", label: "чёрный", sub: "просканирован", col: 1, row: 0 },
    { id: "grey", label: "серый", sub: "worklist", col: 2, row: 1 },
    { id: "wht", label: "белый", sub: "не доказан", col: 3, row: 1 },
    { id: "dead", label: "белый", sub: "мусор", col: 3, row: 2 },
  ]}
  edges={[
    { from: "root", to: "grey", label: "посеять" },
    { from: "grey", to: "blk", label: "скан -> почернить" },
    { from: "grey", to: "wht", label: "посерить референтов" },
  ]}
/>

## Инкрементальная и конкурентная разметка

Наивный MSC останавливает весь JavaScript, размечает всю кучу, затем возобновляет — пауза stop-the-world в сотни миллисекунд на большой куче. V8 атакует это с двух сторон:

- **Incremental marking** разбивает трассировку на маленькие шаги, перемежающиеся с исполнением JS. Движок размечает чуть-чуть, запускает JS, размечает ещё чуть-чуть — амортизируя работу, чтобы ни одна пауза не была длинной. Шаги планируются в простой и во время аллокации.
- **Concurrent marking** идёт дальше: фоновые вспомогательные потоки обходят граф объектов *пока главный поток продолжает исполнять JavaScript*. Основная масса работы разметки происходит полностью вне главного потока.

Оба создают одну и ту же опасность: главный поток может изменить граф объектов посреди трассировки — записать указатель, которого маркер не видел, — и нарушить трёхцветный инвариант, заставив чёрный объект указать на белый. Тогда маркер завершится, увидит этот белый объект как недостигнутый и ошибочно соберёт живой объект. Решение — **write barrier**, целиком предмет следующего урока: он перехватывает записи указателей во время разметки и пересеривает затронутый объект, чтобы инвариант держался. Держите эту нить; здесь просто зафиксируйте, что инкрементальная/конкурентная разметка *корректна только благодаря write barriers.*

После конкурентной разметки короткая **финальная атомарная пауза** («finalize») пересканирует корни и любые объекты, помеченные барьерами, истощая последнее серое множество. Эта финальная пауза — единицы миллисекунд даже на больших кучах — в этом и выигрыш.

## Sweep: освобождаем white в free lists

Когда разметка закончена, каждый белый объект мёртв. **Sweep** обходит страницы old space и добавляет память, занятую белыми объектами, в **free lists** по классам размеров — учёт, позволяющий будущим аллокациям old space найти дыру нужного размера. Sweep *ничего* не двигает; он лишь записывает свободные области. V8 делает sweep **конкурентно и лениво**: free lists перестраиваются в фоновых потоках, а страница может быть подметена по требованию при первой аллокации, которой нужно место из неё. Поскольку sweep касается мёртвых объектов (на которые нет ссылок), он естественно распараллеливается и в основном вне критического пути.

## Compaction: эвакуация для дефрагментации

Один mark-sweep оставляет **фрагментацию**: живые и мёртвые объекты перемешаны, поэтому со временем old space становится швейцарским сыром — свободных байтов масса, но нет непрерывной дыры, достаточной для большой аллокации. **Compaction** чинит это **эвакуацией** (evacuation): выбрать самые фрагментированные страницы, скопировать их живые объекты на свежую плотно упакованную страницу и обновить каждый указатель, ссылавшийся на перемещённые объекты. Это та же машинерия copy-and-forward, что у Scavenger'а, применённая выборочно. Compaction дорогой (он двигает объекты и переписывает указатели), поэтому V8 делает его **выборочно** — только на страницах, чья фрагментация это оправдывает, а не на всей куче каждый цикл.

<NumbersCard
  title="Анатомия major GC"
  data-lesson-visual
  stats={[
    { label: "Размер old space", value: "сотни МБ — гигабайты" },
    { label: "Пауза STW до инкрементальности", value: "сотни мс" },
    { label: "Финальная пауза разметки (совр.)", value: "единицы мс" },
    { label: "Цвета разметки", value: "white / grey / black" },
    { label: "Разметка идёт", value: "incremental + concurrent" },
    { label: "Sweep / compact", value: "конкурентно / выборочно" },
  ]}
/>

<Misconception id="mc-sweep-moves">
  <p><strong>«Sweep уплотняет память, сдвигая живые объекты».</strong> Нет — sweep ничего не двигает. Он лишь записывает место, освобождённое мёртвыми (white) объектами, в free lists. Перемещение живых объектов — отдельная, более дорогая фаза <em>compaction</em>, запускаемая выборочно на фрагментированных страницах.</p>
</Misconception>

<Quiz
  id="q-white-after-mark"
  lessonSlug="03-mark-sweep-compact"
  lang="ru"
  question="Разметка завершена, а объект всё ещё white. Что это значит и что с ним произойдёт?"
  choices={[
    { label: "Он ни разу не был достигнут от корней — это мусор, и его освободит sweep", correct: true },
    { label: "Он достигнут, но ещё не просканирован, поэтому разметка должна продолжиться", misconception: "Это состояние grey. Разметка завершена, только когда не осталось grey; оставшийся white-объект потому недостигнут." },
    { label: "Он жив и будет повышен на новую страницу", misconception: "Black значит полностью просканирован и жив. White после разметки значит недостижим; GC его освобождает." },
    { label: "Он закреплён (pinned) и не может быть собран", misconception: "Цвет отражает достижимость, а не закрепление. White после полной разметки недостижим и собираем." },
  ]}
/>

<Quiz
  id="q-why-final-pause"
  lessonSlug="03-mark-sweep-compact"
  lang="ru"
  question="Concurrent marking делает большую часть работы в фоновых потоках, пока JS исполняется. Почему всё ещё есть короткая финальная пауза stop-the-world?"
  choices={[
    { label: "Чтобы пересканировать корни и объекты, помеченные write barriers во время конкурентной разметки, согласованно истощив последнее серое множество", correct: true },
    { label: "Потому что фоновые потоки вообще не могут размечать без остановки главного потока", misconception: "Они могут и размечают конкурентно; финальная пауза существует, чтобы согласовать изменения, сделанные работающим мутатором, а не потому что конкурентная разметка невозможна." },
    { label: "Чтобы запустить sweep, который обязан быть stop-the-world", misconception: "Sweep в современном V8 конкурентный и ленивый; финальная пауза разметки не для него." },
    { label: "Чтобы уплотнить каждую страницу в куче", misconception: "Compaction выборочный и отдельный; финальная пауза разметки про согласованное завершение разметки, а не про compaction всей кучи." },
  ]}
/>

<DragOrder
  id="do-msc"
  pieceSlug="03-mark-sweep-compact"
  lang="ru"
  prompt="Расставьте по порядку современный цикл major GC в V8."
  items={[
    "Покрасить референтов корней в grey, всё остальное в white",
    "Конкурентно/инкрементально извлекать grey-объекты, серить их белых референтов, чернить их",
    "Короткая атомарная пауза: пересканировать корни и помеченные барьером объекты, истощить последнее grey",
    "Sweep: записать white (мёртвые) объекты в free lists, конкурентно и лениво",
    "Compact: выборочно эвакуировать живые объекты с фрагментированных страниц, обновляя указатели",
  ]}
/>

<Inset kind="why" lang="ru">
Зачем вообще делать compaction, а не просто sweep вечно? Потому что долгоживущий сервер аллоцирует и освобождает объекты многих размеров, и аллокация по free-list не всегда может переиспользовать освобождённую дыру — объект на 200 байт не влезет в зазор на 64 байта. За дни old space накапливает непригодные зазоры; резидентная память остаётся высокой, хотя живых данных мало, и большие аллокации начинают падать или вынуждают рост. Выборочный compaction возвращает эту утраченную непрерывность. Это GC-аналог дефрагментации диска.
</Inset>

</Explanation>

<KeyTakeaway>Major GC размечает трёхцветной абстракцией (white недостигнут, grey в worklist, black просканирован), идёт инкрементально и конкурентно, так что большая часть работы вне главного потока, а финальная пауза stop-the-world — единицы мс. Sweep записывает мёртвые (white) объекты в free lists, ничего не двигая; выборочный compaction эвакуирует живые объекты с фрагментированных страниц, возвращая непрерывность.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="mark-sweep-compact-retrieval"
  lang="ru"
  questions={[
    { q: "Объясните трёхцветную абстракцию разметки и что значит «разметка завершена».", a: "Каждый объект — white (недостигнут, предполагается мёртвым), grey (достигнут, но его исходящие ссылки ещё не просканированы — в marking worklist) или black (достигнут и полностью просканирован). Трассировка начинается с посерения референтов корней и побеления всего остального, затем повторно извлекает grey-объект, серит каждый белый объект, на который он ссылается, и чернит себя. Разметка завершена, когда не осталось grey-объектов — фронт волны полностью прошёл по достижимому графу. Каждый всё ещё white в этот момент объект ни разу не был достигнут и потому мусор. Инвариант, поддерживаемый всё время: чёрный объект никогда не указывает напрямую на белый." },
    { q: "Как V8 держит паузы major GC короткими и какова остаточная стоимость stop-the-world?", a: "Две техники. Incremental marking разбивает трассировку на маленькие шаги, перемежающиеся с исполнением JS (планируемые в простой и аллокацию), так что ни одна пауза не длинна. Concurrent marking запускает обход графа в фоновых вспомогательных потоках, пока главный поток продолжает исполнять JavaScript, перенося основную массу разметки полностью вне главного потока. Sweep тоже конкурентный и ленивый. Остаточная стоимость — короткая атомарная пауза «finalize», пересканирующая корни и объекты, помеченные write barriers, согласованно истощая последнее серое множество — единицы миллисекунд даже на большой куче против сотен мс у наивного stop-the-world MSC." },
    { q: "В чём разница между sweep и compaction и когда V8 делает compaction?", a: "Sweep освобождает мёртвые (white) объекты на месте, записывая освобождённую память в free lists по классам размеров; он ничего не двигает и идёт конкурентно/лениво. Compaction перемещает живые объекты: эвакуирует выживших с фрагментированной страницы на плотно упакованную свежую и обновляет каждый указатель на перемещённые объекты (тот же copy-and-forward, что у Scavenger'а). Compaction дорогой, поэтому V8 делает его выборочно — только на страницах, чья фрагментация оправдывает стоимость, — чтобы вернуть непрерывное пространство, которое аллокация по free-list оставляет застрявшим в виде непригодных зазоров." },
  ]}
/>

<Recap lang="ru">
Old space нельзя собирать копированием, как молодое (нет запасного гигабайта, куда копировать), поэтому V8 использует mark-sweep-compact. Разметка использует трёхцветную абстракцию: объекты white (недостигнут), grey (достигнут, но не просканирован, в worklist) или black (достигнут и полностью просканирован); трассировка серит референтов корней, повторно чернит grey-объекты, серя их белых референтов, и кончается, когда не осталось grey, — оставляя каждый всё ещё белый объект мусором. Чтобы держать паузы короткими, V8 размечает инкрементально (маленькие шаги вперемешку с JS) и конкурентно (фоновые потоки размечают, пока JS работает), сводя часть stop-the-world к финальной паузе в единицы миллисекунд, которая согласует мутации через write barrier и истощает последнее серое множество. Затем sweep записывает мёртвые объекты в free lists, ничего не двигая, конкурентно и лениво. Compaction — отдельная выборочная фаза, эвакуирующая живые объекты с самых фрагментированных страниц и переписывающая их указатели, чтобы вернуть непрерывное пространство. Теперь, когда вы видите паузу major GC в трейсе, вы читаете её как финальный шаг в единицы миллисекунд — не сотни миллисекунд наивного stop-the-world MSC, — зная, что основная работа уже шла в фоновом потоке, пока ваш код работал.
</Recap>
