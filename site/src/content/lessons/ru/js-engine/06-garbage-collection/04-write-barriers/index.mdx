---
slug: "04-write-barriers"
lang: ru
track: "js-engine"
unit: "06-garbage-collection"
order: 4
title: "Write barriers: цена инкрементального и поколенческого GC"
summary: "Каждая запись указателя в объект кучи запускает write barrier. Он служит двум господам: поколенческий барьер записывает указатели old→young в remembered set, чтобы minor GC не сканировал old space, а барьер в стиле Дейкстры держит трёхцветный инвариант"
estMin: 15
status: ready
lessonType: topic
level: senior
concepts:
  - write barrier
  - remembered set / store buffer
  - поколенческий барьер
  - инкрементальный барьер Дейкстры
  - трёхцветный инвариант
  - Smi
deepensInto:
  - "06-garbage-collection/05-memory-leaks"
spiral:
  - performance-cliffs
  - latency
prereqs:
  - "03-mark-sweep-compact"
sources:
  - https://v8.dev/blog/concurrent-marking
  - https://mrale.ph/blog/2018/09/01/maybe-you-dont-need-rust-and-wasm.html
  - https://www.memorymanagement.org/glossary/w.html
---

import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import NumbersCard from "~/components/layout/NumbersCard.astro";
import Misconception from "~/components/layout/Misconception.astro";
import SequenceDiagram from "~/components/diagram/SequenceDiagram.astro";

<Hook>
`obj.next = node` выглядит как одна запись — записать указатель, готово. Но у GC есть два открытых вопроса, которые эта запись может обесценить. Если `obj` живёт в old space, а `node` молодой, то minor GC, сканирующий только young space, пропустит эту совсем новую ссылку и освободит живой объект. Если в полёте конкурентная разметка и `obj` уже black, то эта запись может протащить белый объект мимо маркера. Поэтому это одно присваивание — не одна инструкция: V8 тихо оборачивает его в проверку под названием write barrier, и она выполняется почти на каждой записи указателя в вашей программе.
</Hook>

<Crux>Write barrier на каждой записи указателя: пишет ссылки old→young для Scavenger'а и пересеривает цели для конкурентного маркера.</Crux>

<Explanation>

## Почему запись не бесплатна

Прошлые два урока оставили два долга по корректности, оба порождённые одним и тем же — указателем, изменённым *между сборками или во время* них:

1. **Поколенческий долг.** Minor GC сканирует только young space (плюс корни), потому что сканировать гигабайты old space при каждом Scavenge уничтожило бы весь смысл поколенческого GC. Но старые объекты *могут* держать ссылки на молодые (`oldArray.push(youngObj)`). Если бы V8 их игнорировал, он освободил бы молодой объект, на который old space всё ещё ссылается.
2. **Долг разметки.** Конкурентная/инкрементальная разметка даёт JS изменять граф посреди трассировки. Запись может заставить чёрный (просканированный) объект указать на белый (недостигнутый), нарушив трёхцветный инвариант, так что маркер завершится, не посетив живой объект, и sweep его соберёт.

**Write barrier** — это небольшой кусок кода, который компилятор выпускает *вокруг* почти каждой записи указателя в объект кучи. Он выполняется в момент `obj.field = ptr` и делает любой учёт, нужный, чтобы оба вопроса оставались закрыты. Один барьер — две работы.

## Работа 1: поколенческий барьер и remembered set

Чтобы не сканировать old space на minor GC, V8 поддерживает **remembered set** (реализованный через **store buffer**, позже обрабатываемый в наборы слотов по страницам): запись каждого слота в old space, держащего указатель в young space. Когда вы пишете `oldObj.field = youngObj`, поколенческий write barrier замечает, что запись пересекает old→young, и записывает этот слот.

При следующем Scavenge V8 трактует remembered set как **дополнительные корни**: он сканирует эти записанные слоты, чтобы найти молодые объекты, удерживаемые живыми ссылками old space, *без* обхода old space вообще. После того как Scavenge обновляет адреса перемещённых молодых объектов, именно remembered set позволяет починить указатели old space.

```js
const cache = [];          // promoted to old space over time
function handle(req) {
  const entry = { req };   // young
  cache.push(entry);       // OLD-массив теперь ссылается на YOUNG-запись
  //                          ^ барьер записи поколений фиксирует этот слот
}
```

Барьер срабатывает только для направления old→young. Записи young→young и young→old неинтересны minor GC (young всё равно полностью сканируется; old→? — забота major GC), поэтому V8 убирает барьер в этих случаях.

## Работа 2: барьер инкрементальной разметки (стиль Дейкстры)

Во время конкурентной разметки тот же хук записи защищает трёхцветный инвариант. V8 использует **insertion-барьер в стиле Дейкстры**: когда мутатор записывает указатель на **white**-объект в любой объект во время разметки, барьер **серит белую цель** (кладёт её в marking worklist). Это гарантирует, что маркер в итоге её просканирует, поэтому её нельзя оставить белой и ошибочно собрать. (Классическая альтернатива — Yuasa snapshot-at-the-beginning барьер, серящий *перезаписываемого* старого референта; дизайн V8 использует insertion в стиле Дейкстры для конкурентного маркера. В любом случае цель идентична: ни один живой объект не ускользает от трассировки.)

Поскольку обе работы запускаются одним событием — записью указателя в слот кучи — V8 сливает их в единый путь барьера, проверяющий нужные условия (активна ли разметка? пересекает ли запись old→young?) и делающий минимум работы.

<SequenceDiagram
  label="Запись указателя при активной конкурентной разметке: JS-мутатор пишет obj.field = target; write barrier перехватывает это, серит белую цель в marking worklist и, если запись пересекает old→young, записывает слот в remembered set; маркер позже сканирует ставшую серой цель."
  hue="var(--accent)"
  caption="Одна запись, две обязанности. Барьер сидит между мутатором и кучей: пересерить цель, чтобы маркер её посетил, и записать слоты old→young, чтобы Scavenger нашёл молодых выживших без сканирования old space."
  actors={["Mutator", "Barrier", "Marker"]}
  messages={[
    { from: "Mutator", to: "Barrier", label: "obj.field = target" },
    { from: "Barrier", to: "Barrier", label: "marking? grey target" },
    { from: "Barrier", to: "Barrier", label: "old->young? remember slot" },
    { from: "Barrier", to: "Marker", label: "добавить цель в worklist" },
    { from: "Marker", to: "Marker", label: "later: scan target" },
  ]}
/>

## Почему записи Smi пропускают барьер

Барьер существует только для отслеживания **указателей между объектами кучи**. **Smi** (small integer — 31-битное целое в 64-битном V8) — это не указатель: он закодирован прямо внутри tagged-значения с нулевым младшим tag-битом, поэтому он вообще не может ссылаться на объект кучи. Запись Smi в поле — `obj.count = 42` — не создаёт межобъектной ссылки, поэтому V8 **не выпускает write barrier** для неё. То же верно для записи других immediate. Это ещё одна причина, почему различие Smi/HeapNumber из юнита 02 важно для производительности: записи полей с большим числом целых барьерны-свободны, тогда как записи, кладущие указатели на объекты или упакованные double, платят за проверку барьера.

## Стоимость и где она кусает

Каждый барьер дёшев по отдельности — несколько инструкций: загрузить метаданные страницы цели или проверить флаг разметки, ветвление, а в медленном случае положить в worklist или store buffer. Назовём это горсткой циклов на запись указателя. Это невидимо в обычном коде. Это становится измеримым на **горячих путях с большим числом аллокаций и мутаций**: плотные циклы, строящие большие графы объектов, многократно переуказывающие поля или кладущие миллионы ссылок на объекты в долгоживущие контейнеры. Там накладные расходы барьера реальны, и это одна из скрытых стоимостей «просто положить объекты в массив» против работы с типизированными массивами или Smi-кодированными данными.

<NumbersCard
  title="Факты о write barrier"
  data-lesson-visual
  stats={[
    { label: "Выполняется на", value: "~каждой записи указателя в кучу" },
    { label: "Поколенческая работа", value: "записать слот old→young" },
    { label: "Работа разметки (V8)", value: "Дейкстра insertion: серить цель" },
    { label: "Remembered set через", value: "store buffer → наборы слотов" },
    { label: "Запись Smi / immediate", value: "без барьера" },
    { label: "Типичная стоимость", value: "несколько циклов / запись" },
  ]}
/>

<Misconception id="mc-assignment-free">
  <p><strong>«Присваивание бесплатно — это запись слова».</strong> Запись указателя в объект кучи не бесплатна при поколенческом, конкурентном GC. V8 выпускает write barrier почти на каждой такой записи: чтобы записать ссылки old→young и держать трёхцветный инвариант маркера. Запись Smi барьерно-свободна.</p>
</Misconception>

<Quiz
  id="q-remembered-set"
  lessonSlug="04-write-barriers"
  lang="ru"
  question="Зачем minor GC (Scavenge) вообще нужен remembered set?"
  choices={[
    { label: "Потому что старые объекты могут ссылаться на молодые, а Scavenger не сканирует old space — remembered set поставляет эти ссылки old→young как дополнительные корни", correct: true },
    { label: "Чтобы помнить, каких молодых объектов повышать", misconception: "Promotion решается счётчиком выживаний, а не remembered set. Remembered set отслеживает слоты указателей old→young, чтобы найти молодых выживших, удерживаемых old space." },
    { label: "Чтобы ускорить разметку old space", misconception: "Разметка — работа major GC. Remembered set позволяет minor GC избежать сканирования old space, записывая межпоколенческие ссылки." },
    { label: "Чтобы хранить цвета объектов во время конкурентной разметки", misconception: "Цвета — это mark-биты на объектах. Remembered set записывает слоты указателей old→young; это поколенческий механизм, а не хранилище цветов разметки." },
  ]}
/>

<Quiz
  id="q-smi-barrier"
  lessonSlug="04-write-barriers"
  lang="ru"
  question="`obj.count = 42` против `obj.child = someObject`. Какая запись(и) выпускает write barrier и почему?"
  choices={[
    { label: "Только вторая — 42 это Smi (не указатель), поэтому не создаёт межобъектной ссылки; someObject — указатель кучи, который барьер должен отследить", correct: true },
    { label: "Обе, потому что каждая запись свойства проходит через барьер", misconception: "Барьер отслеживает указатели между объектами кучи. Запись Smi не создаёт такого указателя, поэтому V8 не выпускает для неё барьер." },
    { label: "Ни одна, потому что барьеры выполняются только во время полного GC", misconception: "Барьеры выпускаются инлайн на записях и выполняются всякий раз, когда верно нужное условие (активна разметка или old→young), — а не только во время stop-the-world GC." },
    { label: "Только первая, потому что числа нужно упаковывать (box)", misconception: "Малое целое — это Smi, закодированный инлайн, не упакованный и не указатель; это ровно случай, не нуждающийся в барьере. Барьер нужен записи объекта." },
  ]}
/>

<DragOrder
  id="do-barrier"
  pieceSlug="04-write-barriers"
  lang="ru"
  prompt="Расставьте по порядку, что происходит, когда JS исполняет `oldObj.field = youngWhiteObj` во время активной конкурентной разметки."
  items={[
    "Запись попадает в инлайновый write barrier вместо прямой записи",
    "Барьер проверяет: разметка активна, значит серить белую цель в marking worklist",
    "Барьер проверяет: запись пересекает old→young, значит записать слот в remembered set",
    "Фактический указатель записывается в поле",
    "Позже маркер сканирует ставшую серой цель, чтобы её не собрали ошибочно",
  ]}
/>

<Inset kind="why" lang="ru">
Почему вставлять барьер на записи, а не на чтения? Потому что записи в типичном коде гораздо реже чтений и являются единственными операциями, которые могут изменить достижимость или нарушить трёхцветный инвариант, — чтение указателя не может создать новое ребро в графе объектов. Read barrier (используемый некоторыми сборщиками, например для перемещения в ZGC/Shenandoah) дороже именно потому, что чтения доминируют. V8 выбирает write barriers как более дешёвое место платить, поэтому стоимость концентрируется на коде с большим числом мутаций.
</Inset>

</Explanation>

<KeyTakeaway>Write barrier выполняется почти на каждой записи указателя и делает две работы: записывает ссылки old→young в remembered set, чтобы Scavenger находил молодых выживших без сканирования old space, и серит цель (в стиле Дейкстры), чтобы конкурентная разметка никогда не теряла живой объект. Записи Smi и immediate не создают межобъектного указателя и потому пропускают барьер — поэтому стоимость барьера концентрируется на горячих путях с большим числом мутаций.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="write-barriers-retrieval"
  lang="ru"
  questions={[
    { q: "Какие две проблемы корректности решает write barrier и как?", a: "Первая — поколенческая: minor GC сканирует только young space, но старые объекты могут ссылаться на молодые; без помощи эти молодые объекты были бы ошибочно освобождены. Поколенческий write barrier записывает каждый слот указателя old→young в remembered set (через store buffer), и Scavenger использует эти слоты как дополнительные корни, чтобы находить молодых выживших без сканирования old space. Вторая — проблема конкурентной разметки: JS может изменить граф посреди трассировки и заставить чёрный объект указать на белый, нарушив трёхцветный инвариант. Insertion-барьер V8 в стиле Дейкстры серит записанную белую цель в marking worklist, чтобы маркер её посетил и никогда ошибочно не собрал живой объект. Обе работы срабатывают на одном событии — записи указателя, — поэтому V8 сливает их в один барьер." },
    { q: "Почему записи Smi пропускают write barrier и почему это важно для производительности?", a: "Smi (small integer, 31-битное в 64-битном V8) закодирован прямо внутри tagged-значения с нулевым младшим tag-битом; это не указатель и он не может ссылаться на объект кучи. Поэтому запись Smi в поле не создаёт межобъектной ссылки, так что ни поколенческой, ни разметочной работе нечего отслеживать, и V8 не выпускает барьер. Запись ссылки на объект (или указателя на упакованный HeapNumber) такую ссылку создаёт и платит за проверку барьера. На горячих путях с большим числом аллокаций и мутаций выбор Smi-кодированных или типизированных-массивов данных вместо текучки указателей на объекты избегает и аллокации, и стоимости барьера на запись." },
    { q: "Где накладные расходы write barrier реально становятся измеримыми и почему барьеры на записях, а не на чтениях?", a: "По отдельности барьер — это несколько циклов (проверить флаг разметки / метаданные страницы, ветвление, а в медленном пути положить в worklist или store buffer), невидимо в обычном коде. Это становится измеримым в плотных циклах, строящих большие графы объектов, многократно переуказывающих поля или кладущих миллионы ссылок на объекты в долгоживущие контейнеры, — на горячих путях с большим числом мутаций. Барьеры идут на записи, а не чтения, потому что записи реже и являются единственными операциями, меняющими достижимость или нарушающими трёхцветный инвариант; чтение не может создать новое ребро графа. Сборщики на read barrier (ZGC, Shenandoah) платят больше именно потому, что чтения доминируют." },
  ]}
/>

<Recap lang="ru">
Write barrier — это код, который V8 выпускает вокруг почти каждой записи указателя в объект кучи, и он гасит два долга по корректности из прошлых уроков. Поколенчески: поскольку minor GC сканирует только young space, указатели old→young были бы ему невидимы; барьер записывает слот каждой такой записи в remembered set (опирающийся на store buffer), и Scavenger использует эти слоты как дополнительные корни, чтобы находить молодых выживших без сканирования old space. Для разметки: поскольку конкурентная/инкрементальная разметка даёт JS менять граф посреди трассировки, запись может заставить чёрный объект сослаться на белый и нарушить трёхцветный инвариант; insertion-барьер V8 в стиле Дейкстры серит белую цель в worklist, чтобы маркер её всё же посетил и никогда не собрал живой объект. Обе работы делят один триггер — запись указателя, — поэтому V8 их сливает. Запись Smi или другого immediate не создаёт межобъектного указателя, поэтому пропускает барьер, отчего накладные расходы барьера концентрируются на горячих путях с большим числом аллокаций и мутаций и являются реальной, пусть и малой, скрытой стоимостью текучки указателей на объекты. Теперь, когда в flamegraph вы замечаете неожиданную стоимость барьера — плотный цикл, кладущий объекты в долгоживущий массив, — вы знаете, что делать: переключитесь на типизированные массивы или Smi-кодированные данные, и барьер исчезнет.
</Recap>
