---
slug: "04-call-site-polymorphism"
lang: ru
track: "js-engine"
unit: "05-closures-scope"
order: 4
title: "Замыкания, feedback-векторы и полиморфизм точки вызова"
summary: "У каждого замыкания свой feedback vector, но байт-код общий через SharedFunctionInfo. Вызов через переменную образует call IC: monomorphic для одной callee, megamorphic для многих. Горячий диспетчер с многими идентичностями функций нельзя заинлайнить."
estMin: 15
status: ready
lessonType: topic
level: middle
concepts:
  - feedback vector
  - SharedFunctionInfo
  - call IC
  - monomorphic
  - megamorphic
  - инлайнинг
  - функции высшего порядка
deepensInto: []
spiral:
  - performance-cliffs
  - statefulness
prereqs:
  - "03-context-allocation"
  - "03-hidden-classes/05-mono-poly-mega"
sources:
  - https://mathiasbynens.be/notes/shapes-ics
  - https://v8.dev/blog/turbofan-jit
  - https://v8.dev/docs/turbofan
---

import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import NumbersCard from "~/components/layout/NumbersCard.astro";
import Misconception from "~/components/layout/Misconception.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
Вы построили чистый диспетчер: один горячий цикл, вызывающий `handler(event)`, где `handler` — это какой угодно из сорока колбэков, который вернул реестр. Это элегантно — и медленно, медленнее, чем сорок отдельных циклов. Причина не в вашей логике; причина в том, что V8 следит, *какая функция* проходит через эту единственную точку вызова, и, увидев сорок разных идентичностей функций, сдаётся в попытке предсказать и заинлайнить, откатываясь к общему косвенному вызову на самой горячей строке вашей программы.
</Hook>

<Crux>Точка вызова с одной callee monomorphic и инлайнится; много идентичностей делают её megamorphic — косвенный вызов, без инлайна.</Crux>

<Explanation>

## Общий код, приватная обратная связь

Почему одно и то же тело функции иногда оптимизируется хорошо в одном месте и остаётся медленным в другом? Ответ в том, что каждый экземпляр замыкания ведёт свою приватную запись того, что видел во время работы — и именно по ней TurboFan компилирует. Когда вы создаёте много экземпляров одной функции — каждую стрелку, возвращённую из фабрики, каждое замыкание, запушенное в цикле — они **не** несут каждый свою копию байт-кода. Они делят один **SharedFunctionInfo** (SFI): байт-код, ScopeInfo (информацию об областях видимости), метаданные исходника. Что каждый экземпляр замыкания несёт отдельно — это его **feedback vector** (вектор обратной связи): массив на экземпляр, куда движок записывает наблюдения за типами во время работы для точек вызова и доступов к свойствам этого экземпляра.

Это разделение важно для производительности. Общий SFI держит память низкой и позволяет оптимизатору скомпилировать тело один раз. Feedback vector на экземпляр означает, что два замыкания из одной фабрики могут специализироваться по-разному, если им скармливают разные типы — и означает, что замыкание начинает собирать полезную обратную связь лишь после того, как *этот экземпляр* выполнился достаточно раз.

## Точки вызова тоже образуют inline cache

Вы уже знаете, что чтения свойств образуют inline cache, ключуемые по hidden class (см. урок про mono/poly/mega в юните о hidden-классах). Вызовы делают то же. Выражение вызова вида `fn(x)` — где `fn` это переменная или свойство, держащее функцию — это **точка вызова** со своим IC-слотом в feedback vector. Этот слот фиксирует, *какая функция* (какая идентичность callee) здесь вызывалась:

- **Monomorphic** — точка вызывала только одну идентичность функции. V8 может спекулировать жёстко: он знает точную цель и может заинлайнить её тело.
- **Polymorphic** — небольшое число (до ~4) различных callee. V8 держит маленькую таблицу и ветвится между ними.
- **Megamorphic** — слишком много различных callee. V8 перестаёт отслеживать идентичности и выпускает общий косвенный вызов через то, что `fn` держит сейчас.

<FlowDiagram
  label="Одна точка вызова, которая всегда вызывает ту же функцию, оседает monomorphic и инлайнится, тогда как точка вызова, через которую проходит много разных идентичностей функций, деградирует до megamorphic общего косвенного вызова."
  hue="var(--accent)"
  caption="Два диспетчера, по одной точке вызова. Слева: точка видит только fn A — monomorphic, TurboFan инлайнит тело A. Справа: точка видит A, B, C, D, E… — megamorphic, непрозрачный косвенный вызов, который нельзя заинлайнить."
  nodes={[
    { id: "mono", label: "call fn(x)", sub: "всегда A", col: 0, row: 0 },
    { id: "inl", label: "Мономорф.", sub: "инлайн A", col: 1, row: 0 },
    { id: "poly", label: "call fn(x)", sub: "A,B,C,D,E…", col: 0, row: 1 },
    { id: "mega", label: "Мегаморф.", sub: "косвенный вызов", col: 1, row: 1 },
  ]}
  edges={[
    { from: "mono", to: "inl", label: "1 идентичн." },
    { from: "poly", to: "mega", label: "много идентичн." },
  ]}
/>

## Почему инлайнинг — это вся игра

Инлайнинг — это оптимизация, которая открывает остальные. Когда TurboFan инлайнит monomorphic callee в вызывающего, он затем может свернуть константы через границу, устранить накладные расходы на передачу аргументов и специализировать слитый код по наблюдаемым типам. Megamorphic точка вызова блокирует всё это: TurboFan не знает, какая функция выполнится, поэтому обязан выпустить косвенный вызов — положить аргументы, прыгнуть через указатель на функцию, без инлайнинга, без оптимизации через границу. На горячем цикле диспетчеризации эти расходы ложатся на каждую итерацию.

```js
// Megamorphic: одна точка, много идентичностей callee.
const registry = { click: onClick, hover: onHover, /* …ещё 38 */ };
function dispatch(type, ev) {
  return registry[type](ev);   // эта точка видит десятки идентичностей -> megamorphic
}

// Дружелюбно к monomorphic: маршрутизируй на несколько стабильных, отдельно скомпилированных путей.
function dispatchFast(type, ev) {
  switch (type) {
    case 'click': return onClick(ev);  // у каждой точки вызова ОДНА идентичность
    case 'hover': return onHover(ev);  // monomorphic, инлайнится
    default:      return onOther(ev);
  }
}
```

Версия со `switch` имеет много точек вызова, каждая monomorphic, вместо одной megamorphic точки. Каждая становится инлайнимой по отдельности. «Элегантная» версия с единой диспетчеризацией концентрирует всё разнообразие идентичностей на одной строке и отравляет её.

## Функции высшего порядка: держите колбэки единообразными

Тот же эффект бьёт по `map`/`filter`/`reduce` и любому хелперу высшего порядка. Общий хелпер, вызываемый каждый раз с другим колбэком, имеет внутри одну точку вызова (`cb(item)`), которая видит много идентичностей — megamorphic. Он всё ещё работает, но не может заинлайнить колбэк. Паттерны, которые держат его быстрым:

- **Передавайте ту же идентичность колбэка**, когда можете, а не свежую инлайн-стрелку на каждый вызов. Литерал `arr.map(x => f(x))` создаёт новую идентичность функции каждый вызов; `arr.map(f)` переиспользует одну.
- **Специализируйте горячие хелперы.** По-настоящему горячая свёртка над числами может быть рукописным циклом с заинлайненной операцией, а не общим `reduce(fn)`, чья точка `fn(acc, x)` megamorphic.
- **Держите формы аргументов единообразными тоже.** Даже точки с monomorphic callee деоптятся, если *аргументы* дико меняют hidden class; важны и единообразные callee, и единообразные формы аргументов.

<NumbersCard
  title="Состояния IC точки вызова и инлайнинг"
  data-lesson-visual
  stats={[
    { label: "Различных callee: monomorphic", value: "1" },
    { label: "Различных callee: polymorphic", value: "2–4" },
    { label: "Различных callee: megamorphic", value: "~5+" },
    { label: "Monomorphic вызов", value: "инлайнится" },
    { label: "Megamorphic вызов", value: "косвенный, не инлайн." },
    { label: "Общее на семейство функций", value: "SharedFunctionInfo" },
    { label: "Приватное на экземпляр замыкания", value: "feedback vector" },
  ]}
/>

<Misconception id="mc-shared-feedback">
  <p><strong>«Замыкания из одной фабрики делят обратную связь по типам и оптимизируются вместе».</strong> Они делят байт-код через SharedFunctionInfo, но у каждого свой feedback vector. Один экземпляр на чистых типах может быть быстр, пока сосед на смешанных типах остаётся медленным.</p>
</Misconception>

<Quiz
  id="q-callsite-mega"
  lessonSlug="04-call-site-polymorphism"
  lang="ru"
  question="Одна горячая строка `registry[type](ev)` диспетчеризует на ~40 разных функций. В какое состояние IC придёт эта точка вызова и что сделает TurboFan?"
  choices={[
    { label: "Megamorphic — TurboFan выпускает общий косвенный вызов и не может заинлайнить callee", correct: true },
    { label: "Monomorphic — это буквально одно выражение вызова в исходнике", misconception: "Состояние IC — про различные идентичности callee, увиденные во время работы, а не про то, сколько раз выражение встречается в исходнике. ~40 идентичностей — это megamorphic." },
    { label: "Polymorphic — V8 держит таблицу всех 40 callee", misconception: "Polymorphic держит лишь несколько (~4); ~40 превышает это, поэтому точка становится megamorphic и перестаёт отслеживать идентичности." },
    { label: "Бросает ошибку деопта во время работы", misconception: "Megamorphic — не ошибка; это более медленный, но корректный путь общего вызова." },
  ]}
/>

<Quiz
  id="q-callsite-share"
  lessonSlug="04-call-site-polymorphism"
  lang="ru"
  question="Два замыкания пришли из одной фабрики. Что они делят, а что приватно для каждого?"
  choices={[
    { label: "Общее: байт-код/ScopeInfo через SharedFunctionInfo. Приватное: у каждого свой feedback vector наблюдений за типами", correct: true },
    { label: "Общее: и байт-код, и feedback vector", misconception: "Feedback vector — на экземпляр; его разделение смешало бы наблюдения двух замыканий. Общий только SharedFunctionInfo." },
    { label: "Ничего не общее — каждое замыкание полностью независимо", misconception: "Они делят SharedFunctionInfo (код, ScopeInfo, метаданные); дублирование байт-кода на экземпляр тратило бы память." },
    { label: "Общее: feedback vector. Приватное: байт-код", misconception: "Всё наоборот: байт-код общий через SFI, обратная связь приватна на экземпляр." },
  ]}
/>

<DragOrder
  id="do-callsite-degrade"
  pieceSlug="04-call-site-polymorphism"
  lang="ru"
  prompt="Расставьте по порядку, что происходит с одной точкой вызова, когда через неё со временем проходит всё больше различных идентичностей функций."
  items={[
    "Первый вызов: IC-слот неинициализирован, фиксирует первую идентичность callee",
    "Та же идентичность повторяется: точка monomorphic, TurboFan может заинлайнить callee",
    "Появляется ещё несколько идентичностей: точка становится polymorphic с маленькой таблицей диспетчеризации",
    "Через неё проходит много идентичностей: точка становится megamorphic — общий косвенный вызов, без инлайнинга",
  ]}
/>

<Inset kind="why" lang="ru">
Это можно наблюдать напрямую. Запустите с `--trace-ic` и отфильтруйте `CallIC`; деградирующий диспетчер показывает переходы `0 -> 1 -> P -> N` (неинициализирован → monomorphic → polymorphic → megamorphic). Подтвердите потерю инлайнинга через `--trace-turbo-inlining`: monomorphic callee появляется как заинлайненный, megamorphic точка — нет. Для быстрого локального эксперимента `%GetOptimizationStatus(fn)` под `--allow-natives-syntax` в `d8` скажет, оптимизировался ли сам диспетчер.
</Inset>

</Explanation>

<KeyTakeaway>Замыкания делят байт-код через SharedFunctionInfo, но у каждого свой feedback vector. Вызов через переменную образует call IC, ключуемый по идентичности callee: одна идентичность — monomorphic и инлайнится, много — megamorphic, косвенный вызов, который TurboFan не может заинлайнить. Держите горячие точки диспетчеризации нацеленными на немногие стабильные callee.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="call-site-polymorphism-retrieval"
  lang="ru"
  questions={[
    { q: "Что делят соседние замыкания, а что приватно для каждого, и почему это важно?", a: "Все экземпляры одной функции делят один SharedFunctionInfo — байт-код, ScopeInfo и метаданные исходника — поэтому движок компилирует тело один раз и держит память низкой. Приватно для каждого экземпляра замыкания — его feedback vector: массив на экземпляр, фиксирующий наблюдения за типами и callee во время работы для точек вызова и доступов к свойствам этого экземпляра. Это важно, потому что два замыкания из одной фабрики могут специализироваться по-разному в зависимости от типов, которыми каждое кормят, и экземпляр собирает полезную обратную связь лишь после того, как сам выполнился достаточно раз. Они не объединяют обратную связь, поэтому «они оптимизируются вместе» — ложь." },
    { q: "Каковы состояния IC точки вызова и что каждое значит для инлайнинга?", a: "Выражение вызова, где callee приходит из переменной или свойства, имеет свой IC-слот, фиксирующий, какие идентичности функций здесь вызывались. Monomorphic значит, что увидена ровно одна идентичность callee: V8 может на неё спекулировать, а TurboFan заинлайнить тело callee, открывая сворачивание констант и оптимизацию через границу. Polymorphic значит небольшое число (примерно до четырёх) различных callee: V8 держит маленькую таблицу диспетчеризации и ветвится. Megamorphic значит слишком много различных идентичностей (~5+): V8 перестаёт отслеживать и выпускает общий косвенный вызов через то, что держит переменная — без инлайнинга, с полными накладными расходами вызова на каждый вызов. Инлайнинг — оптимизация, которая открывает остальные, поэтому переход в megamorphic на горячей строке — реальный обрыв." },
    { q: "Как не дать горячему диспетчеру или хелперу высшего порядка стать megamorphic?", a: "Концентрируйте разнообразие прочь от горячей строки. Замените одну точку диспетчеризации, видящую много идентичностей (registry[type](ev)), на несколько точек, каждая из которых видит одну идентичность (switch, маршрутизирующий на onClick(ev), onHover(ev), …), чтобы каждая точка была monomorphic и инлайнимой по отдельности. Для хелперов высшего порядка передавайте стабильную идентичность колбэка, а не свежую инлайн-стрелку на каждый вызов (arr.map(f), а не arr.map(x => f(x))), и специализируйте по-настоящему горячие свёртки как рукописные циклы с заинлайненной операцией вместо общего reduce, чья точка колбэка megamorphic. Держите формы аргументов единообразными тоже, ведь даже monomorphic callee может деоптнуться на дико меняющихся hidden class аргументов. Проверяйте через --trace-ic (переходы CallIC) и --trace-turbo-inlining." },
  ]}
/>

<Recap lang="ru">
Много экземпляров одной функции делят единственный SharedFunctionInfo — байт-код, ScopeInfo, метаданные — но каждый экземпляр замыкания несёт свой feedback vector, фиксирующий наблюдения во время работы на его точках вызова и доступах к свойствам, поэтому соседние замыкания специализируются независимо и не объединяют обратную связь. Выражение вызова, чья callee живёт в переменной или свойстве, само является точкой вызова с IC-слотом, ключуемым по идентичности callee: monomorphic, когда видна лишь одна идентичность (TurboFan может заинлайнить callee), polymorphic для нескольких (маленькая таблица диспетчеризации) и megamorphic для многих (общий косвенный вызов, который нельзя заинлайнить). Поскольку инлайнинг — это то, что открывает сворачивание констант и специализацию через границу, megamorphic вызов на горячей строке диспетчеризации — настоящий обрыв производительности: накладные расходы косвенного вызова ложатся на каждую итерацию. Лекарства концентрируют разнообразие идентичностей прочь от горячей строки — заменить одну многоцелевую диспетчеризацию несколькими одноцелевыми точками через switch, передавать стабильные идентичности колбэков в хелперы высшего порядка, а не свежие инлайн-стрелки, специализировать по-настоящему горячие свёртки как явные циклы и держать формы аргументов единообразными. Это взгляд со стороны замыканий и областей на ту же машинерию mono/poly/mega, которую юнит о hidden-классах ввёл для доступа к свойствам, теперь применённую к тому, какая функция проходит через точку вызова — наблюдаемо через --trace-ic и --trace-turbo-inlining. Теперь, когда горячий цикл диспетчеризации работает медленнее ожидаемого, проверьте, сколько различных идентичностей функций проходит через его единственную точку вызова — если больше четырёх, у вас megamorphic-бутылочное горлышко и `switch`, который нужно написать.
</Recap>
