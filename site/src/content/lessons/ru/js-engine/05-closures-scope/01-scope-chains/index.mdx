---
slug: "01-scope-chains"
lang: ru
track: "js-engine"
unit: "05-closures-scope"
order: 1
title: "Области видимости, контексты и scope chain"
summary: "Лексическая область — это разрешение идентификаторов вдоль цепочки. V8 описывает области через ScopeInfo на этапе компиляции и материализует лишь захваченные — как кучевые Context, связанные outer-указателями; большинство чтений — это пара (глубина, слот)."
estMin: 13
status: ready
lessonType: topic
level: middle
concepts:
  - лексическая область видимости
  - scope chain
  - ScopeInfo
  - Context
  - context-allocated переменная
  - индекс слота
  - динамическая область видимости
deepensInto:
  - "05-closures-scope/02-closure-memory"
spiral:
  - performance-cliffs
  - statefulness
prereqs:
  - "04-the-jit/06-osr"
sources:
  - https://v8.dev/blog/scope-info
  - https://v8.dev/docs
  - https://mathiasbynens.be/notes/shapes-ics
---

import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import NumbersCard from "~/components/layout/NumbersCard.astro";
import Misconception from "~/components/layout/Misconception.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
Вы читаете переменную с именем `total` внутри глубоко вложенного колбэка. Движок должен найти, какой именно `total` вы имеете в виду — и сделать это быстро, на горячем пути, миллионы раз. Наивный ответ — «искать в окружающих функциях во время работы». Настоящий ответ — V8 уже вычислил точное место на этапе компиляции, и во время работы это обычно одна индексированная загрузка из кучевого объекта. До тех пор, пока вы не напишете `with` или нестрогий `eval` — и тогда вся схема рушится.
</Hook>

<Crux>V8 планирует области через ScopeInfo при компиляции, поэтому большинство чтений — загрузка по (глубина, слот) из кучевого Context.</Crux>

<Explanation>

## Лексическая область: цепочка, заданная тем, где вы написали код

JavaScript использует **лексическую область видимости**: смысл идентификатора задаётся текстовой вложенностью функций и блоков, а не тем, кто кого вызвал во время работы. Когда вы ссылаетесь на `total`, язык задаёт поиск: смотрим в текущей области, затем в охватывающей, затем в следующей, вплоть до глобальной. Побеждает первое найденное объявление. Эта упорядоченная последовательность охватывающих областей и есть **scope chain** (цепочка областей видимости).

Слово «поиск» наносит большой вред в ментальной модели большинства людей. Линейный поиск именованных переменных во время работы — словарный lookup на каждый доступ — сделал бы каждое чтение переменной в замыкании мучительно медленным. В типичном случае V8 так не делает. Он разбивает работу на две половины: анализ на этапе компиляции, который вычисляет, *где* живёт каждая переменная, и рантайм-структуру, которая хранит лишь те переменные, которым действительно нужно пережить кадр.

## ScopeInfo: чертёж этапа компиляции

Когда парсер и байт-код-компилятор V8 (Ignition) обрабатывают функцию, они строят объект **ScopeInfo** для каждой области. ScopeInfo — это чистая метаинформация этапа компиляции, прикреплённая к `SharedFunctionInfo` функции. Он фиксирует:

- какие переменные объявляет область (имена),
- какие из них **context-allocated** — должны жить в куче, потому что их захватывает внутренняя функция — против stack/register-allocated (чисто локальные),
- **индекс слота**, который занимает каждая context-allocated переменная,
- вид области (function, block, eval, module, with) и ссылку на охватывающий ScopeInfo.

Главное: компилятор обычно может разрешить ссылку на переменную в фиксированную пару — **(глубина контекста, индекс слота)**. «Переменная `total` находится на две области наружу, в слоте 3». Это разрешение запекается в байт-код инструкцией вида `LdaContextSlot`. Во время работы поиска по имени не происходит — это индексация массива в известный объект на известной глубине.

<NumbersCard
  title="Во что обходится разрешение, по видам"
  data-lesson-visual
  stats={[
    { label: "Локальная в регистре/стеке", value: "0 загрузок (в рег.)" },
    { label: "Слот контекста, глубина 0", value: "1 индекс. загрузка" },
    { label: "Каждая лишняя область глубины", value: "+1 hop указателя" },
    { label: "Глобальная / script-context", value: "загрузка cell (кэш.)" },
    { label: "По-настоящему динамич. (with / sloppy eval)", value: "поиск по имени" },
    { label: "ScopeInfo живёт на", value: "SharedFunctionInfo" },
  ]}
/>

## Context: scope chain во время работы

Во время работы области, которые должны пережить свой кадр стека, материализуются как объекты **Context**. Context — это небольшой кучевой объект (внутри — массив фиксированного размера, разновидность `FixedArray`), который хранит context-allocated переменные одной области плюс указатель на свой **outer** Context. Эта цепочка outer-указателей *и есть* scope chain во время работы.

Отсюда следуют три вещи, и они управляют всем в этом юните:

1. **В Context попадают только захваченные переменные.** Если компилятор может доказать, что переменная чисто локальна — объявлена, использована и ни одной внутренней функцией не упоминается — она остаётся в регистре CPU или на стеке и никогда не аллоцируется в куче. Она исчезает, когда кадр возвращается. Ни одно замыкание её не достанет, потому что ни одного над ней не создавалось.
2. **Context индексируется по слоту, а не по имени.** Чтение `total` на два уровня наружу — это: разыменовать outer, разыменовать outer ещё раз, загрузить слот 3. Фиксированная работа, без хеширования.
3. **Глобальная область особая.** Это глобальный объект (`globalThis`) плюс **script context**, который хранит top-level `let`/`const`. Top-level `var` и объявления функций становятся свойствами глобального объекта; top-level `let`/`const` живут в script context как cell'ы. Поэтому глобальные чтения идут через cell или загрузку свойства, а не через слот контекста, и кэшируются inline cache так же, как чтения свойств объектов.

<FlowDiagram
  label="Три вложенные области функций, материализованные как рантайм-цепочка объектов Context, каждый хранит лишь свои захваченные переменные и указывает на свой outer Context, заканчиваясь скриптовой и глобальной областью."
  hue="var(--accent)"
  caption="Scope chain во время работы. Каждый Context — кучевой массив захваченных слотов с outer-указателем. Чтение идентификатора — это (глубина, слот): прыгнуть по outer столько раз, затем индексировать. Чисто локальные переменные сюда не попадают; они живут в регистрах."
  nodes={[
    { id: "inner", label: "Context: inner()", sub: "slot0: i", col: 0, row: 0 },
    { id: "mid", label: "Context: outer()", sub: "slot0: total", col: 0, row: 1 },
    { id: "script", label: "Script context", sub: "let/const верхнего уровня", col: 0, row: 2 },
    { id: "global", label: "Глобальный объект", sub: "var / функции", col: 0, row: 3 },
  ]}
  edges={[
    { from: "inner", to: "mid", label: "outer" },
    { from: "mid", to: "script", label: "outer" },
    { from: "script", to: "global", label: "outer" },
  ]}
/>

## Почему быстрый путь существует — и что его разрушает

Вся оптимизация держится на том, что компилятор *знает расположение статически*. Две возможности языка ломают эту гарантию и загоняют V8 на медленный путь по именам:

- **`with (obj)`** внедряет объект, чьи свойства неизвестны до момента работы, поэтому любой идентификатор внутри может разрешиться в свойство `obj`. Компилятор не может предвычислить слот; он обязан выпустить динамический lookup, который сначала проверяет объект. Это отравляет область для оптимизации.
- **Нестрогий `eval`** может объявить новые переменные в вызывающей области (например, `eval` строки с `var x = 1`). Поскольку компилятор не может знать, какие имена внесёт `eval`, он обязан держать область динамической — переменные больше нельзя разрешить в фиксированные слоты, и охватывающую функцию часто нельзя хорошо оптимизировать.

Strict mode нейтрализует случай `eval`: `eval` в strict mode получает собственную область и не может протекать объявлениями наружу, поэтому окружающая область остаётся статически анализируемой. Это одна из конкретных причин по производительности, ради которых существует strict mode.

```js
function fast(rows) {
  let total = 0;                 // захвачена стрелкой -> context-allocated
  rows.forEach(r => total += r); // 'total' разрешается в (глубина 1, слот 0)
  return total;
}

function slow(rows, cfg) {
  with (cfg) {                   // каждый идентификатор ниже теперь динамический
    let total = 0;               // V8 не может доказать, откуда берётся 'limit'
    rows.forEach(r => { if (total < limit) total += r; });
    return total;
  }
}
```

<Misconception id="mc-runtime-search">
  <p><strong>«Чтение внешней переменной сканирует области по имени во время работы».</strong> Только для `with` / нестрогого `eval`. Обычно V8 разрешает ссылку в фиксированную (глубина, слот) при компиляции, так что чтение в замыкании — пара hop'ов плюс индексированная загрузка, а не поиск по имени.</p>
</Misconception>

<Quiz
  id="q-scope-context-vs-stack"
  lessonSlug="01-scope-chains"
  lang="ru"
  question="Функция объявляет локальную `n`, инкрементирует её, возвращает и не создаёт внутренних функций. Где V8 хранит `n`?"
  choices={[
    { label: "В регистре или слоте стека — она никем не захвачена, поэтому Context не аллоцируется", correct: true },
    { label: "В кучевом Context, потому что каждая привязка `let` context-allocated", misconception: "Context-allocated только переменные, захваченные внутренней функцией. Чисто локальная привязка остаётся в регистре/слоте стека и освобождается с возвратом кадра." },
    { label: "Как свойство глобального объекта", misconception: "Свойствами глобального объекта становятся только top-level `var`/объявления функций; локальная привязка функции — нет." },
    { label: "В script context", misconception: "Script context хранит top-level `let`/`const`, а не переменную, локальную для какой-то внутренней функции." },
  ]}
/>

<Quiz
  id="q-scope-resolution"
  lessonSlug="01-scope-chains"
  lang="ru"
  question="Во что V8 обычно разрешает ссылку на внешнюю переменную на этапе компиляции?"
  choices={[
    { label: "В фиксированную пару (глубина контекста, индекс слота), запечённую в байт-код", correct: true },
    { label: "В строковый ключ, который хешируется по словарю области на каждом чтении", misconception: "Это медленный динамический путь, используемый только под `with`/нестрогим `eval`. Обычный путь — предвычисленные глубина+слот, без хеширования." },
    { label: "В адрес памяти переменной, фиксированный на всё время жизни программы", misconception: "Context'ы — кучевые объекты, которые могут переаллоцироваться (например, на каждую итерацию цикла); разрешение относительное — глубина и слот — а не абсолютный адрес." },
    { label: "Ни во что — разрешение во всех случаях полностью откладывается на время работы", misconception: "Разрешение делается на этапе компиляции через ScopeInfo для всего, кроме по-настоящему динамических областей." },
  ]}
/>

<DragOrder
  id="do-scope-resolution"
  pieceSlug="01-scope-chains"
  lang="ru"
  prompt="Расставьте по порядку, как V8 разрешает чтение внешней переменной на горячем пути — от этапа компиляции до рантайм-загрузки."
  items={[
    "Парсер строит дерево областей и ScopeInfo на каждую область",
    "Компилятор помечает захваченные переменные как context-allocated и назначает каждой индекс слота",
    "Ссылка кодируется как фиксированная (глубина контекста, индекс слота) в байт-коде",
    "Во время работы прыгнуть по outer-указателю 'глубина' раз, затем загрузить этот слот из Context",
  ]}
/>

<Inset kind="why" lang="ru">
Почему бы для простоты не класть каждую переменную в Context? Потому что объекты Context — это кучевые аллокации, которые GC обязан отслеживать, а загрузка из кучи куда дороже регистра. Вся стратегия V8 — держать переменные в регистрах/стеке по умолчанию и платить за Context только тогда, когда захват к этому вынуждает. Следующие два урока — ровно о том, когда эта плата возникает и что она удерживает.
</Inset>

</Explanation>

<KeyTakeaway>Scope chain разрешается лексически. V8 планирует её на этапе компиляции через ScopeInfo — помечая, какие переменные захвачены, и назначая каждой слот — так что большинство чтений становятся фиксированной загрузкой (глубина, слот) из кучевого Context. Незахваченные переменные не покидают регистров; `with` и нестрогий `eval` вынуждают медленный поиск по имени и блокируют оптимизацию.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="scope-chains-retrieval"
  lang="ru"
  questions={[
    { q: "Что такое ScopeInfo и чем он отличается от Context?", a: "ScopeInfo — это метаинформация этапа компиляции, прикреплённая к SharedFunctionInfo функции: он перечисляет переменные, которые объявляет область, какие из них context-allocated против stack/register-allocated, индекс слота каждой context-allocated переменной, вид области и ссылку на охватывающую область. Он существует один раз на функцию, общий для всех вызовов. Context — это рантайм-аналог: кучевой объект на каждый вызов (разновидность FixedArray), который реально хранит значения захваченных переменных одной области, с outer-указателем на охватывающий Context. ScopeInfo — чертёж; Context — материализованный экземпляр. Главное: ScopeInfo позволяет V8 разрешить ссылку в фиксированную (глубина, слот), так что поиск по имени во время работы не нужен." },
    { q: "Какие переменные попадают в Context, а какие остаются в регистрах или на стеке?", a: "Context-allocated только переменные, захваченные внутренней функцией (вложенной функцией или стрелкой, которая на них ссылается), потому что они должны пережить кадр стека, который их создал. Переменная, которая объявлена, использована и ни одной внутренней функцией не упоминается, доказывается компилятором локальной и остаётся в регистре CPU или слоте стека — она освобождается с возвратом кадра, и ни одно замыкание её не достанет. Вот почему создание замыкания, захватывающего переменную, и есть событие, вынуждающее кучевую аллокацию, и почему горячая функция без захваченных переменных не платит за области ничего." },
    { q: "Почему `with` и нестрогий `eval` делают разрешение областей медленным и как помогает strict mode?", a: "Оба ломают способность компилятора знать расположение области статически. `with (obj)` внедряет объект, чьи свойства неизвестны до работы, поэтому любой идентификатор внутри может оказаться свойством obj — V8 обязан выпустить динамический поиск по имени, который сначала проверяет объект, вместо загрузки фиксированного слота. Нестрогий `eval` может объявить новые переменные в вызывающей области, поэтому компилятор не может назначить фиксированные слоты и обязан держать область динамической, часто блокируя оптимизацию всей охватывающей функции. Strict mode чинит случай eval: eval в strict mode получает собственную область и не может протекать объявлениями наружу, поэтому окружающая область остаётся статически анализируемой и быстрой." },
  ]}
/>

<Recap lang="ru">
JavaScript использует лексическую область видимости: смысл идентификатора задан текстовой вложенностью, и разрешение проходит упорядоченную цепочку охватывающих областей вплоть до глобальной. V8 делит это на этап компиляции и время работы. На этапе компиляции парсер строит ScopeInfo на каждую область — фиксируя, какие переменные есть, какие context-allocated из-за захвата внутренней функцией, и индекс слота каждой — так что большинство ссылок компилируются в фиксированную (глубина контекста, индекс слота). Во время работы захваченные области материализуются как объекты Context: небольшие кучевые массивы слотов, связанные outer-указателями, которые и образуют реальную scope chain. Чтение внешней переменной — это несколько hop'ов по указателям плюс одна индексированная загрузка, а не поиск по имени. Переменные, которые компилятор доказывает локальными, остаются в регистрах или на стеке и освобождаются с кадром. Глобальная область — это глобальный объект плюс script context для top-level let/const. Быстрый путь зависит от того, что компилятор знает расположение статически, и именно это разрушают `with` и нестрогий `eval`, вынуждая динамический поиск по имени — strict mode сохраняет его для eval. Это готовит следующий урок: замыкание — это функция плюс указатель на Context, в котором она была создана, и именно этот указатель удерживает захваченные переменные живыми.
</Recap>
