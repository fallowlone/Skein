---
slug: "03-context-allocation"
lang: ru
track: "js-engine"
unit: "05-closures-scope"
order: 3
title: "Когда V8 аллоцирует Context (и чего это стоит)"
summary: "V8 аллоцирует Context только когда внутренняя функция захватывает переменную; иначе переменные в регистрах, бесплатны. Цикл с let, чьё тело захватывает привязку, аллоцирует новый Context на итерацию — реальная цена в горячих циклах. Как сократить захваты."
estMin: 14
status: ready
lessonType: topic
level: middle
concepts:
  - аллокация Context
  - привязка на итерацию
  - захват
  - горячий цикл
  - var против let захват
  - вынос замыканий
deepensInto:
  - "05-closures-scope/04-call-site-polymorphism"
spiral:
  - performance-cliffs
  - statefulness
prereqs:
  - "02-closure-memory"
sources:
  - https://v8.dev/blog/scope-info
  - https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Statements/let
  - https://v8.dev/docs
---

import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import NumbersCard from "~/components/layout/NumbersCard.astro";
import Misconception from "~/components/layout/Misconception.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
Вы меняете `for (var i …)` на `for (let i …)`, потому что так сказал линтер, и микробенчмарк, строящий миллион колбэков, ощутимо замедляется. В вашей логике ничего не изменилось. Изменилось то, что `let` даёт каждой итерации собственную привязку — и когда тело захватывает эту привязку, V8 обязан аллоцировать совершенно новый кучевой Context на каждом обороте цикла. Семантика, которую вы хотели, пришла с аллокацией, которую вы не увидели.
</Hook>

<Crux>V8 аллоцирует Context только при захвате; захваченный `let` на итерацию вынуждает один новый Context на оборот цикла.</Crux>

<Explanation>

## Правило аллокации: захват и ничего больше

Context аллоцируется не потому, что вы открыли функцию или блок. Он аллоцируется только тогда, когда компилятор определяет, что переменная в этой области **захвачена** внутренней функцией — на неё ссылается вложенная функция или стрелка, которая может пережить текущий кадр. Если ни одна внутренняя функция ни над чем не замыкается, области вообще не нужен Context: её переменные живут в регистрах или слотах стека и не стоят ничего сверх кадра.

Это самая важная модель стоимости в этом юните. Большинство функций, которые вы пишете, аллоцируют **ноль** Context'ов, потому что они либо не определяют внутренних функций, либо определяют внутренние функции, которые ничего не захватывают. Цена появляется ровно в момент захвата.

```js
function noCapture(xs) {
  let sum = 0;
  for (let i = 0; i < xs.length; i++) sum += xs[i];
  return sum;            // нет внутренней функции -> нет Context, всё в регистрах
}

function capture(xs) {
  let sum = 0;
  xs.forEach(x => sum += x); // стрелка захватывает 'sum' -> 'sum' context-allocated
  return sum;                // один Context для этой области
}
```

## Ловушка на итерацию: `let` в цикле + захват

`let` и `const`, объявленные в заголовке `for`, создают **свежую привязку на каждую итерацию**. Это та возможность, которая убирает классический баг «все мои колбэки печатают последнее значение». Но у реализации есть цена, когда тело захватывает эту привязку.

```js
const fns = [];
for (let i = 0; i < n; i++) {
  fns.push(() => i);   // захватывает 'i' ЭТОЙ итерации
}
// Каждый колбэк возвращает свой i: fns[3]() === 3. Корректно!
// Но: НОВЫЙ Context аллоцируется на каждую итерацию, чтобы держать её i.
```

Чтобы каждое замыкание видело свой `i`, `i` каждой итерации должен жить в отдельном месте — поэтому V8 аллоцирует свежий Context на итерацию и копирует туда привязку. При большом `n` и захватывающем теле это `n` кучевых аллокаций и `n` объектов для трассировки GC. Сравните с `var`, у которого одна привязка на весь цикл:

```js
const fns = [];
for (var i = 0; i < n; i++) {
  fns.push(() => i);   // все захватывают ОДИН И ТОТ ЖЕ 'i'
}
// Каждый колбэк возвращает финальный i (=== n). Одна привязка, один Context.
```

`var` аллоцирует здесь максимум один Context (всё же один, потому что привязка захвачена) — но не умножает на `n`. Широко повторяемый совет «всегда используй `let`» верен для корректности, но в по-настоящему горячем цикле, создающем замыкания, Context на итерацию — это реальная, измеримая цена.

<NumbersCard
  title="Аллокации Context по паттерну (цикл из n)"
  data-lesson-visual
  stats={[
    { label: "Цикл, нет внутренней функции", value: "0 Context'ов" },
    { label: "Тело захватывает, `var i`", value: "1 Context всего" },
    { label: "Тело захватывает, `let i`", value: "n Context'ов (1/итер)" },
    { label: "Внутр. fn ничего не захватывает", value: "0 Context'ов" },
    { label: "Вынуждает Context: eval / with", value: "всегда" },
    { label: "Объектов GC от n замыканий", value: "растёт с n" },
  ]}
/>

<FlowDiagram
  label="Цикл, захватывающий привязку let, аллоцирует отдельный Context на итерацию, каждый держит значение своей итерации, тогда как эквивалентный цикл с var делит один Context между всеми итерациями."
  hue="var(--accent)"
  caption="На итерацию против общего. С захваченным `let` итерации 0..2 получают по своему Context (корректные различные значения, n аллокаций). С `var` все итерации делят один Context (одна аллокация, одно общее значение)."
  nodes={[
    { id: "let", label: "let i (захвач.)", sub: "на итерацию", col: 0, row: 0 },
    { id: "c0", label: "Context i=0", sub: "итер 0", col: 1, row: 0 },
    { id: "c1", label: "Context i=1", sub: "итер 1", col: 2, row: 0 },
    { id: "c2", label: "Context i=2", sub: "итер 2", col: 3, row: 0 },
    { id: "var", label: "var i (захвач.)", sub: "общий", col: 0, row: 1 },
    { id: "shared", label: "Один Context", sub: "все итер", col: 1, row: 1 },
  ]}
  edges={[
    { from: "let", to: "c0", label: "alloc" },
    { from: "let", to: "c1", label: "alloc" },
    { from: "let", to: "c2", label: "alloc" },
    { from: "var", to: "shared", label: "alloc раз" },
  ]}
/>

## Что вынуждает Context и как его сократить

Что вынуждает аллокацию Context: любое замыкание над переменной, блок `with`, прямой `eval` (он может читать/объявлять локальные) и в некоторых случаях наблюдение `arguments` способами, которые мешают анализу. Рычаги, чтобы сократить это на горячих путях:

- **Вынесите замыкание из цикла.** Если колбэку на самом деле не нужна привязка каждой итерации, определите его один раз вне цикла. Никакого Context на итерацию.
- **Передавайте данные аргументами вместо захвата.** Функция, получающая `x` параметром, его не захватывает; параметры дешевле захваченного слота Context и держат форму вызова предсказуемой (следующий урок).
- **Используйте индекс, а не захваченную привязку, когда нужно значение только в момент вызова.** Иногда обычный `for` с `var` плюс явная копия — правильный инструмент в горячем цикле на миллион итераций — измеряйте.
- **Держите большие или долгоживущие данные вне захваченных областей** (урок об удержании) — отдельная забота от числа аллокаций, но та же дисциплина.

```js
// Горячий путь: избегаем n Context'ов на итерацию, когда захват не нужен.
function attach(handlers, fn) {            // fn определён ОДИН раз, ничего не захват. на итер
  for (let i = 0; i < handlers.length; i++) handlers[i].on(fn);
}
```

<Misconception id="mc-closures-free">
  <p><strong>«Замыкания бесплатны — это просто функции».</strong> Замыкание, захватывающее переменную, вынуждает кучевой Context, который GC обязан трассировать. Захваченный `let` аллоцирует один Context <em>на итерацию</em> цикла. Замыкания дёшевы, но не бесплатны; на горячих путях считайте захваты.</p>
</Misconception>

<Quiz
  id="q-ctx-per-iter"
  lessonSlug="03-context-allocation"
  lang="ru"
  question="`for (let i = 0; i < n; i++) cbs.push(() => i);` — сколько Context'ов V8 аллоцирует для цикла и почему?"
  choices={[
    { label: "n — по одному на итерацию, потому что каждый колбэк захватывает свежую привязку `let` своей итерации", correct: true },
    { label: "1 — V8 переиспользует единственный Context на весь цикл", misconception: "Это поведение `var`. С захваченным `let` каждой итерации нужна своя привязка, поэтому каждая получает свой Context." },
    { label: "0 — замыкания не аллоцируют Context'ов", misconception: "Замыкание, захватывающее переменную, вынуждает Context; здесь оно вынуждает по одному на итерацию." },
    { label: "2 — один для цикла и один для функции", misconception: "Число растёт с итерациями, когда захвачена привязка на итерацию, а не фиксировано на 2." },
  ]}
/>

<Quiz
  id="q-ctx-minimise"
  lessonSlug="03-context-allocation"
  lang="ru"
  question="Горячий цикл пушит колбэк, которому НЕ нужно значение каждой итерации. Какой фикс самый дешёвый?"
  choices={[
    { label: "Определить колбэк один раз вне цикла, чтобы ничего не захватывалось на итерацию", correct: true },
    { label: "Сменить `let` на `const`", misconception: "`const` тоже на итерацию, когда захвачен; дело не в виде привязки, а в захвате." },
    { label: "Обернуть тело цикла в ещё одну стрелку", misconception: "Добавление замыкания добавляет захваты и Context'ы; это делает аллокацию хуже, а не лучше." },
    { label: "Добавить `try/catch` вокруг цикла", misconception: "Обработка исключений не связана с аллокацией контекста и сама может мешать оптимизации." },
  ]}
/>

<DragOrder
  id="do-alloc-cost"
  pieceSlug="03-context-allocation"
  lang="ru"
  prompt="Расставьте эти циклы по числу аллокаций Context для n итераций — от нуля аллокаций к наибольшему."
  items={[
    "Цикл суммирует в локальную, без внутренней функции (0 Context'ов)",
    "Цикл захватывает привязку `var` в колбэке (1 Context)",
    "Цикл захватывает привязку `let` на итерацию (n Context'ов)",
    "Цикл с блоком `with` плюс захват на итерацию (n Context'ов, все неоптимизируемые)",
  ]}
/>

<Inset kind="edgecase" lang="ru">
V8 не наивен в этом: если замыкание тела цикла доказуемо не убегает, а привязка по сути не наблюдается на итерацию, оптимизатор иногда может избежать материализации различных Context'ов. Но полагаться на это нельзя — escape-анализ хрупок и пасует на многих реальных формах. Считайте захват на итерацию как «n аллокаций, пока не доказано обратное», и проверяйте горячие пути через `--trace-gc` или профиль аллокаций, а не доверяйте оптимизатору вас спасти.
</Inset>

</Explanation>

<KeyTakeaway>Context аллоцируется только когда внутренняя функция захватывает переменную — большинство функций аллоцируют ноль. Ловушка — захваченный `let` на итерацию в горячем цикле: это один свежий Context на итерацию. Выносите замыкания из циклов, передавайте данные аргументами и захватывайте только то, что обязаны.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="context-allocation-retrieval"
  lang="ru"
  questions={[
    { q: "Что именно вызывает аллокацию Context, а что — нет?", a: "Context аллоцируется только когда компилятор видит, что переменная в области захвачена внутренней функцией — вложенной функцией или стрелкой, которая на неё ссылается и может пережить кадр. Открытие функции или блока само по себе Context не аллоцирует; область, чьи внутренние функции ничего не захватывают (или у которой их нет), держит переменные в регистрах или слотах стека без кучевой цены. Дополнительно блок `with` и прямой нестрогий `eval` вынуждают Context, потому что делают область динамической. Ментальная модель: ноль Context'ов по умолчанию, один на захваченную область и множитель, когда захват происходит на каждую итерацию цикла." },
    { q: "Почему `for (let …)` может быть медленнее `for (var …)`, когда тело создаёт замыкания, и повод ли это избегать let?", a: "`let`/`const` в цикле создают свежую привязку на итерацию, так что каждое захваченное замыкание видит своё значение — это корректная, желательная семантика, чинящая классический баг последнего значения. Цена в том, что, чтобы дать каждой итерации отдельную привязку, V8 аллоцирует свежий Context на итерацию, когда тело захватывает привязку: n итераций означает n аллокаций Context и n объектов для трассировки GC. У `var` одна привязка и максимум один Context. Это не повод вообще отказываться от let — корректность важнее — но в по-настоящему горячем цикле, строящем много замыканий, Context на итерацию — реальная, измеримая цена, которую стоит убрать выносом замыкания или передачей значений аргументами." },
    { q: "Как сократить аллокацию контекста на горячем пути?", a: "Захватывайте меньше. Если колбэку не нужна привязка каждой итерации, определите его один раз вне цикла, чтобы ничего не захватывалось на итерацию. Предпочитайте передачу данных аргументами функции захвату из охватывающей области — параметры не создают слотов Context и держат формы вызова предсказуемыми. Когда нужно значение только в момент вызова, индекс плюс явное чтение могут обойти захваченную привязку на итерацию в цикле на миллион итераций. Избегайте `with` и нестрогого `eval`, которые вынуждают Context и блокируют оптимизацию. Проверяйте профилем аллокаций или --trace-gc, а не предполагайте, что escape-анализ уберёт аллокации за вас." },
  ]}
/>

<Recap lang="ru">
Модель стоимости областей видимости в V8 сводится к одному правилу: Context аллоцируется только когда внутренняя функция захватывает переменную. Функции без внутренних функций или те, чьи внутренние функции ничего не захватывают, аллоцируют ноль Context'ов и держат всё в регистрах и слотах стека. Дорогой и легко упускаемый случай — цикл, чьё тело захватывает привязку `let`/`const` на итерацию: поскольку каждой итерации нужна своя привязка ради корректной семантики различных значений, V8 аллоцирует свежий Context на каждую итерацию — n итераций, n кучевых объектов, n вещей для трассировки GC — тогда как эквивалентный захваченный `var` делит одну привязку и один Context. `with` и прямой нестрогий `eval` тоже вынуждают Context и вдобавок ломают оптимизацию. Чтобы оставаться дешёвым на горячих путях: определяйте замыкания один раз вне циклов, когда состояние на итерацию не нужно, передавайте данные аргументами вместо захвата и оставляйте захват на итерацию там, где семантика этого по-настоящему требует. Escape-анализ V8 иногда может убрать эти аллокации, но ненадёжен, поэтому измеряйте профилем аллокаций. Сокращение захватов также готовит следующий урок — меньше и единообразнее замыкания держат точки вызова monomorphic.
</Recap>
