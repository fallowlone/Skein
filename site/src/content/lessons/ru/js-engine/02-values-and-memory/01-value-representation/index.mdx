---
slug: "01-value-representation"
lang: ru
track: "js-engine"
unit: "02-values-and-memory"
order: 1
title: "Как V8 представляет значение"
summary: "Каждое значение JS — это одно 64-битное машинное слово: либо тегированное малое целое (Smi), либо указатель на объект в куче. Это слово-тегированное-объединение и есть фундамент: первое слово HeapObject указывает на его Map, а один младший тег-бит решает"
estMin: 12
status: ready
lessonType: topic
level: middle
concepts:
  - tagged union
  - машинное слово
  - Smi
  - HeapObject
  - Map pointer
  - Oddball
  - тег-бит
deepensInto:
  - "02-smis-and-doubles"
spiral:
  - performance-cliffs
  - statefulness
prereqs:
  - "01-how-js-runs/04-interpreter-loop"
sources:
  - https://v8.dev/blog/pointer-compression
  - https://mathiasbynens.be/notes/shapes-ics
  - https://v8.dev/docs
---

import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import NumbersCard from "~/components/layout/NumbersCard.astro";
import Misconception from "~/components/layout/Misconception.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
Вы пишете `let x = 42`, затем `x = { id: 42 }`, затем `x = null`. Для вас это три разных «вида» сущности. Для слота, хранящего `x`, всё это одно и то же: единственное 64-битное машинное слово. Движок должен уместить число, объект и «ничто» в одну коробку фиксированной ширины — и различать их при каждом чтении. Вся модель памяти V8 вытекает из того, как он это делает.
</Hook>

<Crux>Каждое значение JS — одно 64-битное слово: либо тегированный Smi внутри, либо указатель на HeapObject — а младший бит решает, что.</Crux>

<Explanation>

## Одно слово, чтобы хранить любое значение

Переменная, элемент массива, слот свойства объекта, регистр внутри интерпретатора — всё это ровно одно машинное слово шириной, 64 бита на современном процессоре. Эта однородность не случайна; именно она делает движок простым и быстрым. Загрузка элемента массива — это одна и та же инструкция, будь элемент числом или объектом, потому что *загружаемое* — всегда одно слово. Движку никогда не нужно спрашивать «насколько велико это значение», прежде чем его передвигать.

Но значения JavaScript в принципе не одного размера. Малое целое умещается в слово тривиально. Объект, строка, замыкание — они произвольно велики. Поэтому V8 применяет классический системный трюк: слово — это **tagged union** (тегированное объединение). Оно хранит *либо* значение прямо в себе, *либо* указатель на то, где живёт настоящее значение, а тег говорит, какая трактовка применима.

Случаев ровно два:

- **Smi** («Small Integer», малое целое) — малое знаковое целое, хранимое *прямо* в слове. Никакого объекта в куче нет; биты слова *и есть* число. Чтение бесплатно.
- **Указатель на HeapObject** — слово содержит адрес объекта в куче. Настоящее значение (поля объекта, символы строки, 64 бита double) живёт по этому адресу. Чтение стоит разыменования.

<FlowDiagram
  label="Одно слово-значение проверяется по своему младшему тег-биту, затем направляется по одному из двух путей: Smi, чьи биты сами есть целое, или указатель, который нужно разыменовать, чтобы достичь HeapObject в куче."
  hue="var(--accent)"
  caption="Решение, с которого начинается любое обращение к значению. Сначала проверяется младший тег-бит; только путь указателя платит за загрузку из памяти."
  nodes={[
    { id: "word", label: "Слово-значение", sub: "64 бита", col: 0, row: 0 },
    { id: "smi", label: "Smi", sub: "целое внутри", col: 1, row: 0 },
    { id: "ptr", label: "Указ. HeapObject", sub: "адрес", col: 1, row: 1 },
    { id: "obj", label: "HeapObject", sub: "Map + поля", col: 2, row: 1 },
  ]}
  edges={[
    { from: "word", to: "smi", label: "тег 0" },
    { from: "word", to: "ptr", label: "тег 1" },
    { from: "ptr", to: "obj", label: "разымен." },
  ]}
/>

## Тег-бит: как движок их различает

Трюк в выравнивании. Каждый HeapObject размещается по чётному адресу (выровнен как минимум на 2 байта, на практике на 4 или 8), поэтому *настоящий* указатель всегда имеет младший бит, равный 0. V8 этим пользуется: он ставит младший бит указателя в **1**, помечая «это слово — указатель», и оставляет младший бит **0**, помечая «это слово — Smi». (Точную раскладку битов мы увидим в следующих двух уроках — здесь важно, что одного бита достаточно для различения.)

Поэтому каждый раз, глядя на слово-значение, движок первым делом делает один битовый тест по младшему биту:

```js
// Концептуально любое обращение к значению начинается с этой ветки:
if ((word & 1) === 0) {
  // Smi: целое закодировано в старших битах — используем напрямую
} else {
  // HeapObject: сбрасываем тег и разыменовываем, чтобы дойти до объекта
}
```

Эта ветка — одна инструкция процессора. Это входная плата за динамическую типизацию, и V8 два десятилетия делал её дешёвой и предсказуемой.

## Все HeapObject начинаются с указателя на Map

Когда слово *является* указателем, на что оно указывает? Каждый HeapObject — объект, массив, строка, функция, упакованное число — начинает свою раскладку памяти с одного слова: указателя на свой **Map**. Map — это термин V8 для hidden class: он описывает, *какого вида* этот HeapObject и, для обычных объектов, раскладку его свойств (какие имена живут по каким смещениям).

Вот почему движок может разыменовать *любой* указатель на HeapObject и сразу узнать его тип и форму: ответ всегда по смещению 0.

```
Раскладка памяти HeapObject:
  [ Map pointer    ]  <- смещение 0: «кто я и какова моя раскладка»
  [ поле / данные  ]  <- следующее слово: зависит от вида
  [ поле / данные  ]
  ...
```

Map — это стержень всей истории про производительность; hidden classes, inline caches и деопт держатся на нём. Map мы подробно разбираем в юните про hidden classes; здесь просто зафиксируйте структурный факт: значение — это слово; если его тег говорит «указатель», оно указывает на HeapObject; а первое слово HeapObject — это его Map.

## Oddballs: true, false, null, undefined и hole

Так где же живут `true`, `false`, `null` и `undefined`? Они не Smi (это не целые) и не «объекты» в смысле JS. V8 представляет их как **Oddballs** — особые синглтонные HeapObject. В целом изоляте существует ровно *один* Oddball `undefined`, один `null`, один `true`, один `false`. Переменная, хранящая `undefined`, хранит указатель на этот единственный общий Oddball.

Есть и скрытый Oddball, который вы никогда не видите напрямую: **hole** (`TheHole`). V8 использует его как сторожевое значение, помечая пустой слот массива или неинициализированную привязку `let` (временная мёртвая зона — это буквально «в слоте всё ещё лежит hole»). Чтение hole и есть то, что вызывает `ReferenceError` при доступе в TDZ или проверку на дырку в разреженном массиве.

<NumbersCard
  title="Зоопарк слов-значений"
  data-lesson-visual
  stats={[
    { label: "Ширина любого слота-значения (64-бит)", value: "1 слово = 8 байт" },
    { label: "Проверка тега Smi", value: "1 битовый тест, 0 загрузок" },
    { label: "Доступ к HeapObject", value: "+1 разыменование" },
    { label: "Смещение указателя на Map", value: "0 (первое слово)" },
    { label: "undefined / null / true / false", value: "синглтонные Oddballs" },
    { label: "Различных undefined на изолят", value: "1 (общий)" },
  ]}
/>

<Misconception id="mc-typeof-cost">
  <p><strong>«typeof должен заглядывать в класс значения, поэтому он медленный».</strong> Для Smi движок знает тип по одному тег-биту — без обращения к куче. Для HeapObject он читает Map по смещению 0, обычно уже в кэше. Тег типа встроен в представление; узнать «что это за сущность» дёшево по дизайну.</p>
</Misconception>

<Quiz
  id="q-tagged-union"
  lessonSlug="01-value-representation"
  lang="ru"
  question="Переменную переприсваивают из числа в объект. Почему слоту, хранящему её, не нужно менять размер?"
  choices={[
    { label: "Потому что V8 хранит всё как слово-tagged-union фиксированной ширины — слот всегда одно машинное слово, содержащее либо Smi прямо в себе, либо указатель в кучу", correct: true },
    { label: "Потому что числа и объекты случайно одного размера в памяти", misconception: "Объект произвольно велик; малое целое — нет. Они не одного размера — именно поэтому для объекта слот хранит указатель, а не сам объект." },
    { label: "Потому что V8 переаллоцирует слот под то значение, что больше", misconception: "Слоты фиксированной ширины. Движок никогда не меняет размер слота под значение; он хранит указатель, когда значение не умещается внутри." },
    { label: "Потому что объект тоже хранится как Smi", misconception: "Только малые целые — Smi. Объект всегда HeapObject, достигаемый через указатель; он никогда не Smi." },
  ]}
/>

<Quiz
  id="q-map-offset"
  lessonSlug="01-value-representation"
  lang="ru"
  question="Движок разыменовывает указатель на HeapObject. Что гарантированно лежит по смещению 0?"
  choices={[
    { label: "Первое пользовательское свойство объекта", misconception: "Пользовательские свойства идут после заголовка. Самое первое слово зарезервировано под метаданные движка, а не под ваши данные." },
    { label: "Указатель на Map объекта (hidden class), описывающий его вид и раскладку свойств", correct: true },
    { label: "Длина объекта", misconception: "Длина зависит от вида (массивы, строки) и не лежит по смещению 0 для каждого HeapObject. Универсальное первое слово — это указатель на Map." },
    { label: "Только бит-метка для сборки мусора", misconception: "Метаданные GC существуют, но универсальное первое слово каждого HeapObject — это указатель на Map, который и позволяет движку узнать тип объекта." },
  ]}
/>

<DragOrder
  id="do-value-access"
  pieceSlug="01-value-representation"
  lang="ru"
  prompt="Расставьте шаги, которые делает движок, когда читает слово-значение, оказавшееся объектом, и ему нужно узнать его тип."
  items={[
    "Проверить младший тег-бит слова-значения",
    "Тег равен 1 — трактовать слово как указатель на HeapObject",
    "Сбросить тег-бит, чтобы получить настоящий адрес",
    "Разыменовать смещение 0, чтобы загрузить указатель на Map",
    "Прочитать Map, чтобы узнать вид и раскладку объекта",
  ]}
/>

<Inset kind="why" lang="ru">
Почему смещать тегом именно *указатель*, а не Smi? Потому что арифметика Smi — это горячий случай, который движок больше всего хочет держать дешёвым. Оставив тег Smi равным 0, мы получаем целое в старших битах, и движок делает целочисленную математику с минимальным маскированием — без лишнего шага «снятия тега» на общем пути. Стоимость тега несёт указатель, поскольку его всё равно нужно маскировать перед разыменованием.
</Inset>

</Explanation>

<KeyTakeaway>Значение JS — одно 64-битное слово-tagged-union: Smi (целое внутри, тег-бит 0) или указатель на HeapObject (тег-бит 1). Каждый HeapObject начинается с указателя на Map по смещению 0; true/false/null/undefined и hole — синглтонные Oddball-HeapObject. Один битовый тест направляет любое обращение — это однородное слово и есть фундамент всего дальнейшего.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="value-representation-retrieval"
  lang="ru"
  questions={[
    { q: "Какие две трактовки возможны у слова-значения V8 и как движок выбирает между ними?", a: "Слово-значение — это tagged union ровно с двумя случаями: Smi (малое знаковое целое, хранимое прямо в слове, с младшим тег-битом 0) или указатель на HeapObject (адрес объекта в куче, с младшим тег-битом 1). Движок решает одним битовым тестом по младшему биту перед каждым обращением — Smi используются прямо, без загрузки из памяти, а указатели на HeapObject надо маскировать и разыменовывать. Трюк опирается на то, что HeapObject размещаются по выровненным адресам, поэтому младший бит настоящего указателя естественно равен 0, оставаясь свободным под тег." },
    { q: "Что гарантированно лежит по смещению 0 каждого HeapObject и почему это важно?", a: "Первое слово каждого HeapObject — это указатель на его Map (hidden class в терминах V8). Map описывает вид объекта и, для обычных объектов, раскладку его свойств — какие имена сидят по каким смещениям. Поскольку он всегда по смещению 0, движок может разыменовать любой указатель на HeapObject и сразу узнать тип и форму объекта. Map — якорь для hidden classes, inline caches и деоптимизации, поэтому это единственное однородное заголовочное слово лежит в основе всей машинерии быстрых свойств и обратной связи о типах в V8." },
    { q: "Как V8 представляет true, false, null, undefined и hole?", a: "Это Oddballs — особые синглтонные HeapObject. На изолят существует ровно один undefined, один null, один true и один false, поэтому переменная, хранящая undefined, хранит указатель на этот единственный общий объект. Hole (TheHole) — внутренний Oddball, используемый как сторож: он помечает пустые слоты массива и неинициализированные привязки let/const, поэтому чтение hole и поднимает ReferenceError во временной мёртвой зоне или запускает проверку на дырку при доступе к разреженному массиву." },
  ]}
/>

<Recap lang="ru">
V8 представляет каждое значение JavaScript как единственное 64-битное машинное слово, поэтому переменные, элементы массивов, слоты свойств и регистры интерпретатора однородно одной ширины в слово и перемещаются одной инструкцией независимо от типа. Это слово — tagged union с двумя случаями: Smi, где целое живёт прямо в битах с младшим тег-битом 0, и указатель на HeapObject, где слово — это выровненный адрес в куче с младшим тег-битом 1. Движок начинает любое обращение к значению с одного битового теста по младшему биту; только случай указателя платит за разыменование. Когда слово — указатель, оно достигает HeapObject, чьё первое слово, по смещению 0, всегда указатель на его Map (hidden class) — универсальный заголовок, сообщающий движку вид и раскладку объекта. Особые значения true, false, null, undefined и внутренний hole — синглтонные Oddball-HeapObject, общие на весь изолят. Это однородное тегированное слово — фундамент, на котором строятся следующие уроки: как кодируются Smi и double, как тег-биты на самом деле раскладываются и сжимаются, как устроена куча и как строки эксплуатируют ту же схему. Теперь, когда встретишь деопт, схему раскладки памяти в исходниках V8 или «указатель на HeapObject» в баг-репорте — ты знаешь, какое однобитовое решение лежит в основе.
</Recap>
