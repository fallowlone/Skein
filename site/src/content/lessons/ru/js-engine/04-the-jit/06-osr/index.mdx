---
slug: "06-osr"
lang: ru
track: "js-engine"
unit: "04-the-jit"
order: 6
title: "On-Stack Replacement: подмена фрейма посреди цикла"
summary: "Функция, вызванная однажды, но содержащая длинный горячий цикл, не возвращается, чтобы войти заново, так что обычное повышение яруса не может её ускорить. OSR подменяет исполняющийся фрейм на лету"
estMin: 14
status: ready
lessonType: topic
level: senior
concepts:
  - on-stack replacement
  - OSR entry
  - back-edge counter
  - loop header
  - live state transfer
  - "--trace-osr"
  - microbenchmark warm-up
deepensInto:
  - "08-measuring-optimizing/01-trace-opt-and-deopt"
spiral:
  - performance-cliffs
  - statefulness
prereqs:
  - "05-deoptimization"
sources:
  - https://v8.dev/blog/turbofan-jit
  - https://github.com/thlorenz/v8-perf/blob/master/compiler.md
  - https://mrale.ph/blog/2016/08/29/heap-numbers.html
---

import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import NumbersCard from "~/components/layout/NumbersCard.astro";
import Misconception from "~/components/layout/Misconception.astro";
import SequenceDiagram from "~/components/diagram/SequenceDiagram.astro";

<Hook>
Вы пишете скрипт с одним гигантским циклом `for`, перемалывающим десять миллионов строк. Функция вызывается ровно один раз — `main()` — и не возвращается, пока цикл не закончен. По правилам ярусов, которые вы изучили, эта функция никогда не сможет ускориться: повышение яруса подменяет оптимизированный код при *следующем вызове*, а следующего вызова нет. И всё же вы её замеряете, и цикл *действительно* ускоряется на полпути, пока ещё исполняется. Единственный способ, как это может случиться, — если V8 подменил код прямо под ногами работающего цикла. Этот трюк — On-Stack Replacement (замена на стеке).
</Hook>

<Crux>OSR оптимизирует работающий цикл: бюджет обратных дуг срабатывает, V8 компилирует OSR-entry-версию и переносит состояние во фрейм.</Crux>

<Explanation>

## Проблема, которую обычное повышение яруса не решает

Вспомните, как повышение яруса устанавливает оптимизированный код (урок 01): когда бюджет функции срабатывает, V8 компилирует более быструю версию и подменяет её **для следующего входа** в функцию. У этой модели есть слепое пятно. Рассмотрим функцию, которая вызывается однажды и проводит всё своё время в одном длинном цикле:

```js
function crunch(rows) {
  let acc = 0;
  for (let i = 0; i < rows.length; i++) {   // 10 000 000 итераций
    acc += score(rows[i]);
  }
  return acc;
}
crunch(tenMillionRows); // вызвана ровно один раз
```

Цикл ослепительно горячий — миллионы обратных дуг — так что его **счётчик обратных дуг** срабатывает по бюджету почти немедленно (урок 01 ввёл этот счётчик ровно для этого случая). V8 хочет оптимизировать. Но обычный механизм здесь бесполезен: он установил бы оптимизированный код для *следующего* вызова `crunch`, а `crunch` на лету посреди единственного вызова, который она когда-либо получит. К моменту возврата `crunch` работа уже сделана. Ждать повторного входа значит никогда не оптимизировать тот единственный цикл, что важен.

## OSR: замена фрейма на лету

On-Stack Replacement — это механизм, разрывающий этот тупик. Вместо ожидания, пока функция вернётся и будет вызвана снова, V8 **заменяет исполняющийся фрейм на месте, посреди цикла**. Последовательность:

1. Счётчик обратных дуг цикла срабатывает по бюджету повышения яруса, пока цикл работает в интерпретаторе (или базовом ярусе).
2. V8 компилирует специальную **OSR-entry-версию** функции — оптимизированный машинный код, чья точка входа не вершина функции, а **заголовок цикла**. Она специализирована так, чтобы начать исполнение, как будто управление только что пришло к началу итерации цикла, со всеми живыми переменными цикла уже на месте.
3. V8 выполняет **перенос живого состояния** (live-state transfer): он читает текущие значения каждой живой переменной из интерпретаторного фрейма (индекс цикла `i`, аккумулятор `acc`, ссылка `rows`) и пишет их в регистры и слоты стека оптимизированного фрейма в тех представлениях, которые ожидает оптимизированный код. Это инверсия трансляции фрейма при деопте из урока 05 — там мы перестраивали интерпретаторный фрейм из оптимизированного; здесь мы строим оптимизированный фрейм из интерпретаторного.
4. Управление переходит в OSR-entry-оптимизированный код на заголовке цикла, и **тот же цикл продолжается** — но теперь в оптимизированном машинном коде. Оставшиеся девять с половиной миллионов итераций работают быстро.
5. После того как цикл закончен и `crunch` в итоге возвращается, обычный оптимизированный код (обычная компиляция со входом сверху) берёт на себя любые *последующие* вызовы.

<SequenceDiagram
  label="Замена на стеке: интерпретатор исполняет цикл, когда счётчик обратных дуг срабатывает; V8 компилирует OSR-entry-версию, специализированную под заголовок цикла, переносит живые переменные цикла из интерпретаторного фрейма в оптимизированный и продолжает тот же цикл в оптимизированном коде."
  hue="var(--accent)"
  actors={["Loop", "V8", "OSRCode", "OptFrame"]}
  messages={[
    { from: "Loop", to: "V8", label: "back-edge budget trips" },
    { from: "V8", to: "OSRCode", label: "compile OSR-entry @ loop header" },
    { from: "V8", to: "OptFrame", label: "transfer live state (i, acc...)" },
    { from: "OSRCode", to: "Loop", label: "resume SAME loop, optimised" },
  ]}
  caption="Фрейм подменяется, пока цикл работает. Граница вызова не пересекается; живые переменные переносятся, и цикл продолжается в оптимизированном коде со следующей итерации."
/>

## Почему микробенчмарки живут и умирают по OSR

Это механизм за печально известной ловушкой бенчмаркинга. Микробенчмарк, оборачивающий свою работу в один огромный цикл — `for (let i = 0; i < 1e9; i++) work()`, — не разогревает `work` через повышение яруса по счётчику вызовов обычным образом; он полагается на то, что OSR включится на полпути, чтобы оптимизировать тело цикла на лету. Из этого следуют два последствия для всякого, кто замеряет производительность:

- Первый кусок итераций работает неоптимизированным (интерпретатор/базовый ярус), затем есть разрыв, где срабатывает OSR, и остаток работает быстро. Если вы замеряете весь цикл одним числом, вы смешиваете холодное и горячее исполнение и получаете бессмысленное среднее.
- OSR-entry-код может быть *немного* хуже обычной оптимизации со входом сверху, ведь он вынужден принять живое состояние цикла таким, каким нашёл, а не от чистого входа в функцию, что ограничивает некоторые оптимизации. Так что замер, разогретый через OSR, может занижать установившуюся скорость, которую вы увидели бы, если бы функцию вызывали много раз обычным образом.

Починка стандартная: разогрейте функцию многими *отдельными вызовами* до замера (чтобы она получила обычную, не-OSR оптимизацию), и замеряйте установившиеся итерации, а не разогревочный переходный процесс. Вы можете увидеть OSR через `--trace-osr`; вместе с `--trace-opt` он показывает OSR-entry-компиляцию отдельно от обычной.

<NumbersCard
  title="OSR с одного взгляда"
  data-lesson-visual
  stats={[
    { label: "Запускается", value: "счётчиком обратных дуг цикла" },
    { label: "Точка входа", value: "заголовок цикла (не вершина)" },
    { label: "Переносится состояние", value: "интерпр. фрейм -> оптим." },
    { label: "Связь с деоптом", value: "инверсия трансляции фрейма" },
    { label: "OSR-код против обычной опт.", value: "иногда немного хуже" },
    { label: "После цикла", value: "обычная опт. для след. вызовов" },
    { label: "Флаг трассировки", value: "--trace-osr" },
    { label: "Релевантность бенчмаркам", value: "бенчи с одним гигант. циклом" },
  ]}
/>

<Misconception id="mc-loop-must-finish">
  <p><strong>«Горячий цикл ускоряется лишь после того, как закончится и функцию вызовут снова».</strong> Нет — ровно это OSR предотвращает. Когда счётчик обратных дуг срабатывает, V8 компилирует OSR-entry-версию и переносит состояние в оптимизированный фрейм, так что <em>тот же цикл на лету</em> идёт оптимизированным.</p>
</Misconception>

<Quiz
  id="q-osr-need"
  lessonSlug="06-osr"
  lang="ru"
  question="Почему обычное повышение яруса не может оптимизировать функцию, вызванную однажды и проводящую всё время в одном длинном цикле?"
  choices={[
    { label: "Обычное повышение яруса устанавливает оптимизированный код для следующего ВЫЗОВА, но следующего вызова нет — функция на лету посреди единственного", correct: true },
    { label: "Циклы вообще не могут быть оптимизированы TurboFan", misconception: "Циклы оптимизируются превосходно; проблема чисто в моменте, когда обычное повышение яруса устанавливает код, а не в какой-либо неспособности оптимизировать циклы." },
    { label: "Счётчик обратных дуг не считает итерации цикла", misconception: "Счётчик обратных дуг считает ровно это, и он действительно срабатывает по бюджету здесь; разрыв в том, как был бы установлен оптимизированный код, и это чинит OSR." },
    { label: "Однажды вызванные функции никогда не считаются горячими", misconception: "Горячесть приходит и от обратных дуг, не только от вызовов; однажды вызванная функция с огромным циклом очень горяча." },
  ]}
/>

<Quiz
  id="q-osr-state"
  lessonSlug="06-osr"
  lang="ru"
  question="Что должен сделать OSR в момент подмены фрейма и как это связано с деоптом?"
  choices={[
    { label: "Перенести живые переменные цикла из интерпретаторного фрейма в оптимизированный — инверсия трансляции фрейма при деопте", correct: true },
    { label: "Перезапустить цикл с нулевой итерации в оптимизированном коде", misconception: "OSR продолжает ТОТ ЖЕ цикл с того места, где он есть; перезапуск с нуля переделал бы выполненную работу и мог бы изменить результаты." },
    { label: "Дождаться, пока цикл дойдёт до последней итерации, прежде чем подменять", misconception: "OSR подменяет посреди цикла именно чтобы не ждать; ожидание свело бы на нет его смысл." },
    { label: "Отбросить живое состояние и вывести его заново с нуля", misconception: "Живое состояние (индекс цикла, аккумуляторы) должно быть перенесено в точности, иначе вычисление было бы неверным." },
  ]}
/>

<DragOrder
  id="do-osr-steps"
  pieceSlug="06-osr"
  lang="ru"
  prompt="Расставьте, как OSR оптимизирует долго работающий цикл в однажды вызванной функции."
  items={[
    "Счётчик обратных дуг цикла срабатывает по бюджету повышения яруса посреди исполнения",
    "V8 компилирует OSR-entry-версию, чья точка входа — заголовок цикла",
    "V8 переносит живое состояние цикла из интерпретаторного фрейма в оптимизированный",
    "Управление возобновляется в оптимизированном коде, и тот же цикл продолжается быстро",
    "После возврата цикла обычный оптимизированный код обслуживает любые поздние вызовы",
  ]}
/>

<Inset kind="why" lang="ru">
Почему OSR-entry-версия иногда оптимизируется немного хуже обычной компиляции? Потому что обычная оптимизация со входом сверху может предполагать чистый вход в функцию — свежие аргументы, никакого живого состояния цикла посреди вычисления — и может разложить всю функцию оптимально. OSR-entry-версия вместо этого вынуждена принять живые значения цикла ровно такими, какими они существуют в точке срабатывания, и начать с заголовка цикла, что закрепляет некоторые представления и ограничивает пару перестановок, которые компилятор иначе бы сделал. Она всё равно гораздо быстрее интерпретатора; просто изредка на волосок позади установившегося кода, который вы получаете от многих обычных вызовов, — ровно поэтому разогрев отдельными вызовами даёт более чистые числа бенчмарка.
</Inset>

</Explanation>

<KeyTakeaway>OSR оптимизирует уже работающий цикл. Когда счётчик обратных дуг срабатывает, V8 компилирует OSR-entry-версию, специализированную под заголовок цикла, и переносит живые переменные цикла из интерпретаторного фрейма в оптимизированный — инверсия деопта — так что тот же цикл на лету продолжается в оптимизированном коде. Это то, почему однажды вызванная функция с гигантским циклом всё же может ускориться, и почему микробенчмарки с одним циклом надо разогревать отдельными вызовами.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="osr-retrieval"
  lang="ru"
  questions={[
    { q: "Какую проблему OSR решает, а обычное повышение яруса — нет?", a: "Обычное повышение яруса устанавливает оптимизированный код для СЛЕДУЮЩЕГО входа в функцию. Это не работает для функции, вызванной однажды и проводящей всё время в одном длинном цикле: цикл интенсивно горяч (его счётчик обратных дуг немедленно срабатывает по бюджету), но функция на лету посреди единственного вызова, который она когда-либо получит, так что нет следующего входа, для которого установить оптимизированный код, а к моменту возврата функции работа закончена. OSR разрывает этот тупик, оптимизируя цикл, который исполняется прямо сейчас, вместо ожидания повторного входа." },
    { q: "Пройдитесь по механизму OSR шаг за шагом.", a: "1) Счётчик обратных дуг цикла срабатывает по бюджету повышения яруса, пока цикл работает в интерпретаторе или базовом ярусе. 2) V8 компилирует специальную OSR-entry-версию функции, чья точка входа — заголовок цикла, а не вершина функции, специализированную для возобновления посреди цикла. 3) V8 выполняет перенос живого состояния: он читает каждую живую переменную (индекс цикла, аккумуляторы, ссылочные объекты) из интерпретаторного фрейма и пишет их в регистры и слоты стека оптимизированного фрейма в ожидаемых представлениях — это инверсия трансляции фрейма при деопте, построение оптимизированного фрейма из интерпретаторного. 4) Управление прыгает в OSR-entry-код на заголовке цикла, и ТОТ ЖЕ цикл продолжается, теперь в оптимизированном машинном коде, на оставшихся итерациях. 5) Когда цикл заканчивается и функция позже возвращается, обычная оптимизация со входом сверху обслуживает любые последующие вызовы." },
    { q: "Почему микробенчмарки с одним гигантским циклом надо разогревать отдельными вызовами и при чём тут OSR?", a: "Бенчмарк вида for (i=0; i<1e9; i++) work() не повышает ярус work() через обычные пороги счётчика вызовов в пределах одного цикла; он зависит от срабатывания OSR на полпути, чтобы оптимизировать тело цикла на лету. Из этого следуют две проблемы: во-первых, ранние итерации работают неоптимизированными, затем OSR вызывает разрыв в быстрое исполнение, так что замер всего цикла одним числом смешивает холодное и горячее и бессмыслен; во-вторых, OSR-entry-код может быть немного хуже обычной оптимизации со входом сверху, ведь он вынужден принять живое состояние цикла как нашёл, так что может занижать установившуюся скорость. Починка — разогреть функцию многими отдельными вызовами (давая чистую обычную оптимизацию) и замерять только установившиеся итерации, исключая разогревочный переходный процесс. Используйте --trace-osr (с --trace-opt), чтобы увидеть OSR-entry-компиляцию отдельно от обычной." },
  ]}
/>

<Recap lang="ru">
On-Stack Replacement — это механизм, позволяющий V8 оптимизировать уже работающий цикл, закрывая единственный пробел в обычном повышении яруса. Обычное повышение яруса устанавливает оптимизированный код для следующего входа в функцию, что бесполезно для функции, вызванной однажды и проводящей всё время в одном длинном цикле: цикл интенсивно горяч через свой счётчик обратных дуг, но нет следующего вызова, для которого установить код. OSR чинит это, заменяя исполняющийся фрейм на лету. Когда бюджет обратных дуг срабатывает, V8 компилирует OSR-entry-версию функции, чья точка входа — заголовок цикла, затем выполняет перенос живого состояния — читая каждую живую переменную цикла из интерпретаторного фрейма и записывая её в оптимизированный фрейм в представлениях, которые ожидает оптимизированный код, точная инверсия трансляции фрейма при деопте. Управление прыгает в оптимизированный код на заголовке цикла, и тот же цикл на лету продолжается быстро, а обычный оптимизированный код со входом сверху берёт на себя любые поздние вызовы, как только цикл вернётся. Поскольку микробенчмарки с одним гигантским циклом полагаются на OSR, а не на обычное повышение яруса, и поскольку OSR-entry-код может быть незначительно хуже чистой оптимизации, такие бенчмарки надо разогревать многими отдельными вызовами и замерять в установившемся режиме. Наблюдайте за всем этим через --trace-osr вместе с --trace-opt.
</Recap>
