---
slug: "02-type-feedback"
lang: ru
track: "js-engine"
unit: "04-the-jit"
order: 2
title: "Обратная связь о типах: топливо для оптимизатора"
summary: "Оптимизатор бесполезен без рантайм-информации о типах. Как feedback vector записывает на каждом IC-сайте карты, решётку примитивных типов, цели вызовов и типы бинарных операций — и почему мусорная обратная связь даёт обобщённый код или отсутствие оптимизации вовсе."
estMin: 14
status: ready
lessonType: topic
level: senior
concepts:
  - feedback vector
  - SharedFunctionInfo
  - feedback slot
  - type lattice
  - polymorphism
  - megamorphic
  - speculation
deepensInto:
  - "04-the-jit/03-turbofan"
spiral:
  - performance-cliffs
  - statefulness
prereqs:
  - "01-tiers-and-warmup"
sources:
  - https://v8.dev/blog/maglev
  - https://mathiasbynens.be/notes/shapes-ics
  - https://github.com/thlorenz/v8-perf/blob/master/compiler.md
---

import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import NumbersCard from "~/components/layout/NumbersCard.astro";
import Misconception from "~/components/layout/Misconception.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
TurboFan компилирует функцию, которая делает `a + b`, и производит на эту одну операцию единственную целочисленную инструкцию сложения — без боксинга, без проверки типа сверх дешёвого guard. Откуда компилятор для динамически типизированного языка знает, что `a` и `b` — целые? Он не знает — он спрашивает интерпретатор, что тот *видел*. Уберите это наблюдение, и тот же компилятор сможет выпустить лишь обобщённую, переполненную ветвлениями процедуру «сложить что угодно с чем угодно». Топливо, которое превращает обобщённый оптимизатор в специализирующий, — это обратная связь о типах (type feedback).
</Hook>

<Crux>Оптимизатор не видит типы статически; он специализируется по рантайм-наблюдениям, которые интерпретатор пишет в feedback vector.</Crux>

<Explanation>

## Почему оптимизатор слеп без обратной связи

У JavaScript нет статических типов, которым движок мог бы доверять. В точке, где компилируется `function add(a, b) { return a + b; }`, движок не может доказать, что `a` — число: это может быть строка, объект с `valueOf`, BigInt. Наивно скомпилированный `+` поэтому обязан обработать каждый случай: проверить теги, ветвиться на строку-против-числа-против-объекта, возможно вызвать пользовательский `valueOf`, забоксить результат. Этот обобщённый путь корректен, но медленный.

Выход — **спекуляция**: предположить те типы, которые вы реально наблюдали, выпустить быстрый код ровно под них и защитить его дешёвым guard, который откатывается, если предположение нарушится (урок 04). Но спекуляции нужны улики — и улики приходят от единственного яруса, который исполняет каждую операцию, наблюдая за её операндами: от Ignition, интерпретатора. Пока Ignition работает, он записывает то, что видит в каждом «интересном» месте. Эта запись и есть **feedback vector** (вектор обратной связи).

## Feedback vector: структура и владение

Feedback vector — это массив фиксированного размера, выделенный в куче GC. Ключевая деталь, в которой ошибаются, — это *кто им владеет*. Байт-код и *раскладка* слотов обратной связи разделяются: они живут в **SharedFunctionInfo** (SFI) функции — дедуплицированной записи движка «код этой функции», одна на определение в исходнике. Но *сам вектор обратной связи* — массив, хранящий наблюдённые данные, — выделяется **на каждое замыкание**. Два замыкания, созданные из одного исходника функции, разделяют байт-код и раскладку слотов, но у каждого свой feedback vector, так что их рантайм-наблюдения не загрязняют друг друга.

Каждый слот соответствует одному **IC-сайту** (inline cache) в байт-коде — месту, где тип операндов важен:

- **Доступ к свойству** (`obj.x`, `obj.x = v`) — записывает **карту** (hidden class) увиденного объекта плюс handler, описывающий, как загрузить/сохранить на этой карте.
- **Вызовы** (`fn()`, `obj.method()`) — записывает наблюдённую цель(и) вызова, что позволяет инлайнинг.
- **Бинарные операции** (`a + b`, `a < b`) — записывает **решётку типов** (type lattice) операндов: были они всегда Smi (малое целое)? Smi-или-HeapNumber? String? Any?
- **Доступ к элементу** (`arr[i]`) — записывает elements kind (packed Smi, packed double, holey, dictionary).

<FlowDiagram
  label="Интерпретатор исполняет байт-код операция за операцией и для каждого IC-сайта пишет наблюдённую карту, цель вызова или тип операнда в feedback vector замыкания функции; затем оптимизатор читает этот вектор, чтобы решить, под какие конкретные типы специализироваться."
  hue="var(--accent)"
  perRow={4}
  caption="Петля обратной связи. Ignition — единственный ярус, заполняющий вектор; Maglev и TurboFan только читают его. Нет разогрева — пустой вектор — нет специализации."
  nodes={[
    { id: "bc", label: "Ignition работает", sub: "каждый IC-сайт", col: 0, row: 0 },
    { id: "obs", label: "Наблюдает операнд", sub: "карта / тип / цель", col: 1, row: 0 },
    { id: "vec", label: "Feedback vector (вектор ОС)", sub: "слот на замыкание", col: 2, row: 0 },
    { id: "opt", label: "Оптимизатор читает", sub: "Maglev / TurboFan", col: 3, row: 0 },
  ]}
  edges={[
    { from: "bc", to: "obs", label: "следит" },
    { from: "obs", to: "vec", label: "пишет" },
    { from: "vec", to: "opt", label: "читает" },
  ]}
/>

## Решётка примитивных типов

Для арифметики обратная связь — это не просто «число». V8 отслеживает типы операндов на небольшой **решётке** (lattice), которую монотонно расширяет по мере того, как видит больше случаев. Полезная модель для слота `+`:

```
None  ->  SignedSmall (Smi)  ->  Number (Smi или HeapNumber)  ->  NumberOrOddball  ->  Any
                       \-> String (если появляется строковый операнд) -> Any
```

Каждое новое наблюдение может двигать слот только *вверх* по решётке (расширять), никогда не сужать. Слот, который видел только малые целые, остаётся на `SignedSmall`, и TurboFan может выпустить 32-битное целочисленное сложение, защищённое единственной проверкой «это Smi». Как только этот слот впервые увидит значение, переполняющее диапазон Smi, он расширяется до `Number`, и следующая оптимизация выпустит float64-арифметику. Если он когда-либо увидит строку, он расширяется к `Any`, и арифметика откатывается к полностью обобщённой процедуре. Это монотонное расширение — ровно причина, почему смешивание типов так разъедает: одно значение не того типа навсегда загрязняет слот на всё время жизни этого feedback vector.

## Мусор на входе — обобщённое на выходе

Оптимизатор хорош ровно настолько, насколько хороша обратная связь. Для любого IC-слота важны три состояния:

- **Мономорфный (monomorphic)** — наблюдена одна карта / один стабильный тип. Оптимизатор специализируется жёстко: прямая загрузка по смещению, инлайн вызова, целочисленное сложение. Самый быстрый.
- **Полиморфный (polymorphic)** — небольшая горстка (предел V8 — 4) карт/типов. Оптимизатор всё ещё может инлайнить с предварительной проверкой карты, ветвящейся между известными случаями. Медленнее, но всё ещё оптимизированно.
- **Мегаморфный (megamorphic)** — больше предела. Слот помечается мегаморфным sentinel; оптимизатор сдаётся со специализацией и выпускает обобщённую диспетчеризацию (рантайм-поиск). Он может вообще отказаться оптимизировать окружающую функцию.

<NumbersCard
  title="Состояния обратной связи и что делает оптимизатор"
  data-lesson-visual
  stats={[
    { label: "Мономорфный", value: "1 карта / тип -> специализация" },
    { label: "Полиморфный", value: "2-4 карты -> инлайн с проверкой" },
    { label: "Порог мегаморфности", value: "больше 4 карт" },
    { label: "Мегаморфный", value: "обобщ. диспетчеризация / нет опт." },
    { label: "Расширение решётки", value: "монотонное, в одну сторону" },
    { label: "Владелец feedback vector", value: "замыкание" },
    { label: "Владелец раскладки слотов / байт-кода", value: "SharedFunctionInfo" },
    { label: "Инспектор d8", value: "%DebugPrintFeedback(fn)" },
  ]}
/>

Практическое правило, вытекающее отсюда: **разогревайте функцию согласованными типами до того, как она повысит ярус**. Если горячая функция в основном будет обрабатывать целые, не давайте первой дюжине вызовов кормить её строками; если место вызова в основном будет получать одну форму объекта, не пускайте через него пять форм. Feedback vector заполняется во время интерпретируемой фазы, и какие решётка и набор карт он накопит — ровно то, на чём оптимизатор сможет (или не сможет) специализироваться.

<Misconception id="mc-feedback-static">
  <p><strong>«Типы TypeScript говорят V8, что оптимизировать».</strong> Нет. Типы TS стираются на компиляции и не доходят до движка. V8 специализируется чисто по рантайм-наблюдениям в feedback vector. Идеально типизированная функция с полиморфными формами получает полиморфную (или никакую) оптимизацию.</p>
</Misconception>

<FlowDiagram
  label="Два слияния обратной связи показаны как решётка: слот, видевший только малые целые, остаётся на SignedSmall и получает целочисленный код; как только он видит ещё и heap-число, он расширяется до Number и получает float64-код; как только видит строку — расширяется к Any и откатывается к обобщённой арифметике."
  hue="var(--accent)"
  perRow={4}
  caption="Монотонное расширение одного слота обратной связи бинарной операции. Каждое новое значение не того типа может только расширить слот; назад он никогда не сужается. Обобщённый код — это поглощающее состояние."
  nodes={[
    { id: "smi", label: "SignedSmall", sub: "int add + Smi guard", col: 0, row: 0 },
    { id: "num", label: "Number", sub: "float64 add", col: 1, row: 0 },
    { id: "ood", label: "NumberOrOddball", sub: "шире числовой", col: 2, row: 0 },
    { id: "any", label: "Any", sub: "обобщ. + (медленно)", col: 3, row: 0 },
  ]}
  edges={[
    { from: "smi", to: "num", label: "видит HeapNumber" },
    { from: "num", to: "ood", label: "видит null/undef" },
    { from: "ood", to: "any", label: "видит строку" },
  ]}
/>

<Quiz
  id="q-vector-owner"
  lessonSlug="02-type-feedback"
  lang="ru"
  question="Два замыкания созданы из одного исходника функции. Что они разделяют, а что у каждого своё?"
  choices={[
    { label: "Они разделяют байт-код и раскладку слотов обратной связи (на SharedFunctionInfo), но у каждого свой feedback vector", correct: true },
    { label: "Они разделяют всё, включая feedback vector — обратная связь ключуется по позиции в исходнике", misconception: "Разделяемый вектор позволил бы наблюдениям одного замыкания загрязнять другое; вектор сделан на замыкание именно чтобы этого избежать." },
    { label: "Не разделяют ничего; каждое замыкание перекомпилирует свой байт-код", misconception: "Байт-код и раскладка слотов дедуплицированы на SharedFunctionInfo; различаются только наблюдённые данные." },
    { label: "Они разделяют feedback vector, но имеют отдельный байт-код", misconception: "Всё наоборот: байт-код разделяется через SFI, а feedback vector — на замыкание." },
  ]}
/>

<Quiz
  id="q-lattice"
  lessonSlug="02-type-feedback"
  lang="ru"
  question="Сайт `+` обрабатывает 9 999 пар целых, затем одну пару, где один операнд — строка, затем ещё миллион пар целых. Какую обратную связь видит TurboFan и какой код выпускает?"
  choices={[
    { label: "Он самоисцеляется обратно к целочисленной обратной связи после строки, ведь целые доминируют", misconception: "Расширение решётки монотонно — одна строка навсегда расширяет слот; назад он не сужается, сколько бы целых ни шло следом." },
    { label: "Слот расширился к Any в момент, когда увидел строку, так что TurboFan выпускает обобщённое сложение на всё", correct: true },
    { label: "TurboFan игнорирует одну аномалию и специализируется на целых", misconception: "Отбрасывания аномалий нет; слот записывает объединение всего наблюдённого, и строка подвинула его по решётке навсегда." },
    { label: "Он остаётся Smi, ведь 99.99% значений — целые", misconception: "Обратная связь — не голосование большинством; слот расширяется, чтобы покрыть каждый тип, что он когда-либо видел." },
  ]}
/>

<DragOrder
  id="do-feedback-flow"
  pieceSlug="02-type-feedback"
  lang="ru"
  prompt="Расставьте жизненный цикл одного слота обратной связи бинарной операции от холодной функции до специализированного машинного кода."
  items={[
    "Слот стартует с None (функция холодная, ни разу не запущена)",
    "Ignition исполняет операцию и записывает наблюдённый тип операнда (например, SignedSmall)",
    "Повторные вызовы держат слот мономорфным на SignedSmall",
    "Бюджет срабатывает; оптимизатор читает слот и выпускает целочисленное сложение с Smi-guard",
  ]}
/>

<Inset kind="edgecase" lang="ru">
Тонкое следствие: feedback vector — это объекты в куче GC, и они могут быть собраны под давлением памяти, а затем заново выделены пустыми. Долгоживущий процесс, переживший сброс feedback vector, фактически заново охлаждает затронутые функции — они должны заново наблюдать типы, прежде чем смогут переоптимизироваться. Это редко, но объясняет периодическую загадку «почему эта горячая функция деоптнулась до скорости интерпретатора через час аптайма без изменения кода?»: вектор собрали, и ему пришлось заполняться заново.
</Inset>

</Explanation>

<KeyTakeaway>Оптимизатор специализируется только по тому, что наблюдал интерпретатор. Ignition заполняет feedback vector на замыкание — по слоту на IC-сайт, записывая карты, цели вызовов и монотонно расширяющуюся решётку типов операндов. Согласованные типы держат слоты мономорфными, а код быстрым; одно значение не того типа расширяет слот навсегда и форсирует обобщённый код.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="type-feedback-retrieval"
  lang="ru"
  questions={[
    { q: "Что такое feedback vector, что записывает каждый слот и кто им владеет?", a: "Массив фиксированного размера в куче GC, по слоту на IC-сайт в байт-коде функции. Слоты доступа к свойству записывают карту объекта (hidden class) плюс handler загрузки/сохранения; слоты вызовов записывают наблюдённые цели вызова (для инлайнинга); слоты бинарных операций записывают решётку типов операндов (SignedSmall / Number / String / Any); слоты доступа к элементу записывают elements kind. Владение разделено: байт-код и раскладка слотов живут в дедуплицированном SharedFunctionInfo (один на исходную функцию), но сам feedback vector выделяется на замыкание, так что два замыкания одной функции записывают независимые наблюдения." },
    { q: "Почему решётка типов операндов монотонна и какова практическая цена?", a: "Каждый слот бинарной операции отслеживает объединение типов операндов, что он когда-либо видел, и наблюдения могут только расширять слот вверх по решётке (None -> SignedSmall -> Number -> NumberOrOddball -> Any, с веткой String), никогда не сужая. Поэтому одно значение не того типа навсегда двигает слот вверх: одна строка через горячий сайт целочисленного сложения расширяет его к Any, и TurboFan вынужден впредь выпускать обобщённое сложение с полными проверками типов и боксингом вместо целочисленного сложения с guard — даже если следом идёт миллион целых. Починка — держать арифметические сайты стабильными по типам; не давать патологическим значениям делить горячий слот." },
    { q: "Объясните мономорфную, полиморфную и мегаморфную обратную связь и что делает оптимизатор в каждом случае.", a: "Мономорфная = наблюдена одна карта/тип: оптимизатор специализируется жёстко (прямая загрузка по смещению, инлайн вызова, целочисленное сложение) — самый быстрый. Полиморфная = 2-4 карты/типа: оптимизатор всё ещё может инлайнить за предварительной проверкой карты, ветвящейся между известными случаями — медленнее, но оптимизированно. Мегаморфная = больше 4: слот помечается мегаморфным sentinel, оптимизатор перестаёт его специализировать и выпускает обобщённую диспетчеризацию (рантайм-поиск), и может отказаться оптимизировать всю функцию. Инспектируйте через %DebugPrintFeedback(fn) в d8, чтобы увидеть состояние каждого слота и найти загрязнённый." },
  ]}
/>

<Recap lang="ru">
Оптимизатор JavaScript не может прочитать ваши типы статически, поэтому специализируется спекуляцией по тому, что реально наблюдалось в рантайме. Эти наблюдения живут в feedback vector: массиве фиксированного размера в куче GC, по слоту на каждый inline-cache-сайт. Каждый слот записывает то, что важно на этом сайте — карту для доступа к свойству, цель вызова для вызова, elements kind для доступа к массиву и монотонно расширяющуюся решётку типов для бинарной операции. Владение разделено между SharedFunctionInfo (держит дедуплицированный байт-код и раскладку слотов, один на исходную функцию) и feedback vector на замыкание (держит собственные наблюдения этого замыкания). Только Ignition заполняет вектор; Maglev и TurboFan лишь читают его. Качество обратной связи определяет качество кода: мономорфный слот специализируется жёстко, полиморфный (до 4 карт) инлайнится за проверкой, а мегаморфный слот схлопывается в обобщённую диспетчеризацию и может вовсе заблокировать оптимизацию. Поскольку решётка только расширяется, одно значение не того типа загрязняет слот на всё время жизни вектора — так что разогрев функции согласованными типами до повышения яруса и есть вся игра, а аннотации TypeScript, будучи стёртыми, не вносят ничего.
</Recap>
