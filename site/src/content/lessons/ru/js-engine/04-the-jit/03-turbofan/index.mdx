---
slug: "03-turbofan"
lang: ru
track: "js-engine"
unit: "04-the-jit"
order: 3
title: "TurboFan: оптимизатор на море узлов"
summary: "TurboFan представляет код как граф «море узлов» — рёбра значений, эффектов и управления — вместо CFG из операторов, что освобождает планировщик размещать вычисления поздно. Полный конвейер фаз от байт-кода и обратной связи до аллокации регистров и генерации кода."
estMin: 15
status: ready
lessonType: topic
level: senior
concepts:
  - sea-of-nodes
  - value/effect/control edges
  - typed lowering
  - inlining
  - escape analysis
  - representation selection
  - scheduling
deepensInto:
  - "04-the-jit/04-speculative-optimization"
spiral:
  - performance-cliffs
  - statefulness
prereqs:
  - "02-type-feedback"
sources:
  - https://v8.dev/blog/turbofan-jit
  - https://mrale.ph/blog/2016/08/29/heap-numbers.html
  - https://github.com/thlorenz/v8-perf/blob/master/compiler.md
---

import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import NumbersCard from "~/components/layout/NumbersCard.astro";
import Misconception from "~/components/layout/Misconception.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
Два вопроса про одну и ту же строку кода, `const d = a.x - b.x`, раскрывают, как мыслит TurboFan. Традиционный компилятор спрашивает: «какой оператор идёт перед этим?» TurboFan спрашивает: «от какого значения зависит `a.x` и есть ли хоть какой-то побочный эффект между этим местом и местом использования?» — и если ответ «никакого», он волен перенести это вычитание куда угодно, хоть из цикла, хоть мимо других операторов. Эта свобода — весь смысл IR «море узлов», и именно она позволяет TurboFan оптимизировать намного агрессивнее, чем компилятор, идущий оператор за оператором.
</Hook>

<Crux>TurboFan оптимизирует граф «море узлов», а не CFG, так что планировщик размещает вычисление так поздно, как позволяет поток данных.</Crux>

<Explanation>

## Почему граф, а не список операторов

Традиционный компилятор моделирует функцию как граф потока управления (CFG) из базовых блоков, каждый — фиксированная последовательность операторов. Порядок зашит с самого начала: оператор 3 идёт после оператора 2, потому что вы так написали. Чтобы перенести вычисление, такой компилятор должен доказать законность переноса относительно этого навязанного порядка.

TurboFan переворачивает это. Его IR — это граф **«море узлов»** (sea-of-nodes), где узел — это операция, а *рёбра* — не исходный порядок — кодируют каждое ограничение, которое реально важно. Понятия «порядок инструкций» нет до самого конца. Граф несёт три вида рёбер:

- **Рёбра значений** (зависимости по данным): у `subtract` есть рёбра значений к двум загрузкам, которые он потребляет. Это говорит «мне нужны эти входы», и ничего о том, когда.
- **Рёбра эффектов** (порядок наблюдаемых побочных эффектов): запись, вызов, который может изменить состояние, загрузка свойства, способная вызвать getter, — они сцеплены в *цепочку эффектов*, чтобы их относительный порядок сохранялся. Чистые операции (арифметика над известными числами) не несут ребра эффекта и свободно плавают.
- **Рёбра управления** (ветвления, слияния, циклы): скелет из `if`/`loop`/`return`, решающий, какие узлы вообще исполнятся.

Поскольку у чистого вычитания есть только рёбра значений и нет ограничений по эффекту или управлению, **планировщик** (поздняя фаза) может разместить его где угодно, где доступны его входы — включая вынос из цикла, если его входы инвариантны относительно цикла. Это и имеет в виду mrale.ph под «планировщик размещает вычисления поздно»: у узлов нет фиксированного дома до планирования, и оно назначает самый дешёвый законный.

## Конвейер фаз

TurboFan — это последовательность хорошо определённых проходов над этим графом. Форма, по порядку:

<FlowDiagram
  label="Конвейер фаз TurboFan: байт-код плюс feedback vector питают построение графа, затем типизацию и типизированное понижение, затем проходы оптимизации (инлайнинг, escape-анализ, устранение загрузок/сохранений и избыточности, свёртка констант, выбор представления), затем планирование, выбор инструкций, аллокацию регистров и наконец генерацию нативного машинного кода."
  hue="var(--accent)"
  perRow={4}
  caption="Конвейер. Передние фазы рассуждают о программе абстрактно (нетипизированный, затем типизированный граф); задние фазы фиксируют её на машине (планируют, выбирают инструкции, аллоцируют регистры, выпускают байты)."
  nodes={[
    { id: "bc", label: "Байт-код + обратн. связь", sub: "вход", col: 0, row: 0 },
    { id: "graph", label: "Построение графа", sub: "море узлов", col: 1, row: 0 },
    { id: "type", label: "Типизация + пониж.", sub: "вывод + сужение", col: 2, row: 0 },
    { id: "opt", label: "Оптимизации", sub: "инлайн, EA, свёртка", col: 3, row: 0 },
    { id: "repr", label: "Выбор представления", sub: "tagged / int / float", col: 0, row: 1 },
    { id: "sched", label: "Планирование", sub: "узлы по блокам", col: 1, row: 1 },
    { id: "isel", label: "Выбор инструкций", sub: "граф -> маш. опер.", col: 2, row: 1 },
    { id: "regs", label: "Аллокация регистров", sub: "+ генерация кода", col: 3, row: 1 },
  ]}
  edges={[
    { from: "bc", to: "graph", label: "строить" },
    { from: "graph", to: "type", label: "разметить" },
    { from: "type", to: "opt", label: "переписать" },
    { from: "opt", to: "repr", label: "понизить" },
    { from: "repr", to: "sched", label: "упорядоч." },
    { from: "sched", to: "isel", label: "выбрать" },
    { from: "isel", to: "regs", label: "выпустить" },
  ]}
/>

**Построение графа** превращает байт-код в начальный граф «море узлов», прикрепляя наблюдения из feedback vector, чтобы поздние фазы знали спекулируемые типы. **Типизация и типизированное понижение** распространяют типы по графу (`+` двух узлов типа `SignedSmall` типизируется как число в известном диапазоне) и заменяют абстрактные операции на специализированные по типу — обобщённый `JSAdd` становится `NumberAdd`, затем потенциально `Int32Add`.

Затем **проходы оптимизации** переписывают граф:

- **Инлайнинг** — узел вызова, чья обратная связь называет небольшую известную цель, заменяется копией графа вызываемого, вплетая его рёбра значений и эффектов в вызывающего. Это то, что разблокирует межфункциональную оптимизацию; без него каждый вызов — непрозрачный эффект.
- **Escape-анализ со скалярной заменой** — объект, чья ссылка никогда не покидает функцию (подробно в уроке 04), теряет свою аллокацию, а его поля превращаются в обычные узлы графа (регистры), устраняя аллокацию в куче и давление на GC.
- **Устранение загрузок/сохранений** — загрузка, которая обязана прочитать только что сохранённое значение, заменяется самим сохранённым значением; сохранение, перезаписанное до любого чтения, удаляется.
- **Устранение избыточности и свёртка констант** — CheckMaps, который уже доказан доминирующим CheckMaps, удаляется; `2 * 3` становится `6`; ветка по заведомо истинному условию схлопывается.
- **Выбор представления** — решает машинное представление каждого значения: тегированный указатель, 32-битное целое в регистре или 64-битный float в FP-регистре, вставляя преобразования только там, где граница представления реально пересекается.

**Планирование** — это фаза, которая наконец назначает узлы базовым блокам и порядок внутри них — размещая чистые узлы так поздно и так вне-циклово, как законно. **Выбор инструкций** сопоставляет паттерны графа машинным инструкциям целевой ISA. **Аллокация регистров** (TurboFan использует продвинутый аллокатор, а не линейное сканирование, как Maglev) назначает физические регистры и вставляет сбросы (spills). **Генерация кода** выпускает финальные байты. На выходе — блоб нативного машинного кода, установленный на функцию.

<NumbersCard
  title="TurboFan против более лёгких ярусов"
  data-lesson-visual
  stats={[
    { label: "Форма IR", value: "граф «море узлов»" },
    { label: "Виды рёбер", value: "значения / эффекты / управление" },
    { label: "Аллокатор регистров", value: "на графе (не линейное скан.)" },
    { label: "Предел инлайнинга (полиморф.)", value: "~4 известных цели" },
    { label: "Время компиляции", value: "десятки-сотни мс / функцию" },
    { label: "Работает на", value: "фоновом потоке" },
    { label: "Качество Maglev против TurboFan", value: "~50-70%" },
    { label: "Флаг трассировки", value: "--trace-turbo / --trace-opt" },
  ]}
/>

## Где он работает и как его инспектировать

TurboFan компилирует на **фоновом потоке** (параллельная компиляция, урок 01): главный поток продолжает исполнять функцию на её текущем ярусе, а оптимизированный код подменяется, когда готов. Это источник самой частой ментальной ошибки о нём (см. ниже). Чтобы инспектировать граф, `--trace-turbo` дампит IR на каждой фазе в файлы, которые можно открыть в визуализаторе Turbolizer; `--trace-opt` логирует решения более высокого уровня. mrale.ph и посты v8.dev/blog — канонические глубокие референсы по графу и его фазам.

<Misconception id="mc-turbofan-runs">
  <p><strong>«TurboFan исполняет мой код».</strong> Нет. TurboFan — компилятор, а не исполнитель. Он <em>компилирует</em> функцию однажды, на фоновом потоке, в нативный код; затем CPU исполняет этот код напрямую. Время «в TurboFan» — это компиляция, а не исполнение, и оно вне главного потока.</p>
</Misconception>

<Quiz
  id="q-edges"
  lessonSlug="03-turbofan"
  lang="ru"
  question="В IR «море узлов» TurboFan у чистого целочисленного вычитания внутри цикла есть только рёбра значений (нет ребра эффекта или управления, привязывающего его). Что может сделать с ним планировщик?"
  choices={[
    { label: "Разместить его где угодно, где доступны его входы, включая вынос из цикла, если входы инвариантны относительно цикла", correct: true },
    { label: "Ничего — он обязан исполниться ровно там, где появился в исходнике", misconception: "Исходный порядок не является ограничением в море узлов; ограничения — только рёбра. Узел без ребра эффекта/управления плавает свободно до планирования." },
    { label: "Удалить его, ведь чистые операции всегда мёртвый код", misconception: "Чистота позволяет свободное размещение, а не удаление; узел удаляется, только если его результат действительно не используется." },
    { label: "Затолкать его в цепочку эффектов, чтобы сохранить порядок", misconception: "Чистые операции по определению не несут ребра эффекта; именно поэтому их можно планировать свободно." },
  ]}
/>

<Quiz
  id="q-phase-order"
  lessonSlug="03-turbofan"
  lang="ru"
  question="Почему типизированное понижение происходит до планирования и аллокации регистров, а не после?"
  choices={[
    { label: "Потому что передние фазы рассуждают о программе абстрактно, чтобы включить переписывания; только после оптимизации и типизации графа задние фазы фиксируют его на конкретной машине", correct: true },
    { label: "Потому что аллокация регистров производит типы, нужные понижению", misconception: "Типы приходят из обратной связи и типизации, а не из аллокации регистров; зависимость идёт в обратную сторону." },
    { label: "Потому что планирование должно идти первым, чтобы знать порядок операторов", misconception: "У моря узлов нет порядка операторов до планирования; планирование намеренно позднее, после оптимизации." },
    { label: "Они работают параллельно, так что порядок не важен", misconception: "Конвейер — последовательность зависимых проходов; каждый потребляет граф, произведённый предыдущим." },
  ]}
/>

<DragOrder
  id="do-tf-pipeline"
  pieceSlug="03-turbofan"
  lang="ru"
  prompt="Расставьте фазы TurboFan от входа до нативного кода."
  items={[
    "Построить граф «море узлов» из байт-кода + обратной связи",
    "Типизация и типизированное понижение (специализация абстрактных операций)",
    "Проходы оптимизации (инлайнинг, escape-анализ, свёртка)",
    "Выбор представления (tagged / int32 / float64)",
    "Планирование (назначить узлы блокам и порядок)",
    "Выбор инструкций + аллокация регистров + генерация кода",
  ]}
/>

<Inset kind="why" lang="ru">
Почему разделение рёбер значений, эффектов и управления так важно на практике? Потому что большая часть критичного к производительности JS — это числовая или массивная работа, где тяжёлые операции *чистые*: арифметика, сравнения, вычисление индексов. В CFG они приколочены к своему базовому блоку, и только вынос инвариантного кода может их сдвинуть, с консервативными предположениями об алиасинге. В графе «море узлов» у них нет дома до планирования, так что компилятор может выносить их из циклов, делать общую подвыражение-элиминацию через ветки и сворачивать избыточные с куда меньшей церемонией. Цена реальна — компилятор сложнее и труднее отлаживается, — но для специализации динамического языка она того стоит.
</Inset>

</Explanation>

<KeyTakeaway>TurboFan оптимизирует граф «море узлов», где рёбра значений, эффектов и управления кодируют единственные реальные ограничения, так что у чистого вычисления нет фиксированного дома до тех пор, пока планирование не разместит его так дёшево, как законно. Его конвейер идёт от построения графа и типизированного понижения через инлайнинг, escape-анализ, выбор представления, планирование, выбор инструкций и аллокацию регистров — и всё это происходит однажды, на фоновом потоке.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="turbofan-retrieval"
  lang="ru"
  questions={[
    { q: "Какие три вида рёбер в IR «море узлов» TurboFan и почему это различие включает агрессивную оптимизацию?", a: "Рёбра значений (зависимости по данным: чьи выходы потребляет операция), рёбра эффектов (порядок наблюдаемых побочных эффектов — записи, getters, вызовы, — сцеплённых в цепочку эффектов) и рёбра управления (ветвления, слияния, циклы). Различие важно потому, что у чистой операции (арифметики над известными числами) есть только рёбра значений — нет ребра эффекта или управления — так что у неё нет фиксированной позиции в программе до фазы планирования. Планировщик может тогда разместить её так поздно и так вне-циклово, как позволяет поток данных: вынос инвариантного кода из циклов, общая подвыражение-элиминация через ветки и свёртка избыточности — всё без борьбы с навязанным порядком операторов, как вынужден компилятор на CFG." },
    { q: "Пройдитесь по конвейеру TurboFan от байт-кода до нативного кода.", a: "1) Построение графа: байт-код плюс feedback vector становятся начальным графом «море узлов» с прикреплёнными спекулируемыми типами. 2) Типизация и типизированное понижение: типы распространяются по графу, абстрактные операции специализируются (JSAdd -> NumberAdd -> Int32Add). 3) Проходы оптимизации: инлайнинг (вплести граф небольшой известной цели в вызывающего), escape-анализ со скалярной заменой (удалить непокидающие аллокации, превратить поля в узлы), устранение загрузок/сохранений, устранение избыточности, свёртка констант. 4) Выбор представления: выбрать tagged / int32 / float64 на значение, вставляя преобразования на границах. 5) Планирование: назначить узлы базовым блокам и упорядочить их, размещая чистые узлы поздно. 6) Выбор инструкций: сопоставить паттерны графа машинным инструкциям. 7) Аллокация регистров (на графе, а не линейное сканирование Maglev) и генерация кода: выпустить нативные байты, установленные на функцию." },
    { q: "Почему «TurboFan исполняет мой код» неверно и где на самом деле проявляется его цена?", a: "TurboFan — компилятор, а не интерпретатор или исполнитель. Он компилирует горячую функцию однажды в нативный машинный код, затем уходит с дороги — CPU исполняет выпущенный код напрямую. Его цена — время компиляции (десятки-сотни мс для сложной функции), и важно, что эта компиляция идёт на фоновом потоке под параллельной компиляцией, так что не блокирует главный поток. Пока компиляция в полёте, главный поток продолжает исполнять функцию на её текущем (более низком) ярусе; оптимизированный код подменяется при следующем входе. Так что «время в TurboFan» — это фоновая цена компиляции, никогда не время исполнения главного потока." },
  ]}
/>

<Recap lang="ru">
TurboFan — это полный оптимизирующий компилятор V8, и его определяющий выбор — промежуточное представление «море узлов». Вместо графа потока управления из упорядоченных операторов он моделирует функцию как граф, чьи рёбра — значений (зависимости по данным), эффектов (порядок наблюдаемых побочных эффектов) и управления (ветвления, циклы, слияния) — кодируют единственные важные ограничения; исходный порядок не кодирует. У чистой операции есть только рёбра значений, так что у неё нет фиксированной позиции до фазы планирования, которая размещает её так поздно и так дёшево, как законно, — это и делает вынос из циклов, общую подвыражение-элиминацию и свёртку избыточности столь естественными. Конвейер идёт по порядку: построение графа из байт-кода и обратной связи, типизация и типизированное понижение, специализирующее абстрактные операции, проходы оптимизации (инлайнинг, escape-анализ со скалярной заменой, устранение загрузок/сохранений и избыточности, свёртка констант), выбор представления (tagged против int32 против float64), планирование, выбор инструкций, аллокация регистров на графовом аллокаторе и генерация кода. Вся компиляция идёт однажды на фоновом потоке, так что TurboFan никогда не исполняет ваш код и никогда не блокирует главный поток — он производит нативный код, который затем CPU исполняет напрямую. Инспектируйте через --trace-turbo и Turbolizer; глубокие референсы — mrale.ph и блог v8.dev.
</Recap>
