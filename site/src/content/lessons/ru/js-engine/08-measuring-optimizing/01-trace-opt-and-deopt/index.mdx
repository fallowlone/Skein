---
slug: "01-trace-opt-and-deopt"
lang: ru
track: "js-engine"
unit: "08-measuring-optimizing"
order: 1
title: "Трассировка оптимизации и деопта"
summary: "Инструментарий практика для наблюдения, как V8 оптимизирует и откатывается: --trace-opt, --trace-deopt, --trace-ic, --print-bytecode, --allow-natives-syntax (%OptimizeFunctionOnNextCall, %GetOptimizationStatus) и node --prof / --cpu-prof."
estMin: 14
status: ready
lessonType: topic
level: senior
concepts:
  - --trace-opt
  - --trace-deopt
  - --trace-ic
  - --allow-natives-syntax
  - "%GetOptimizationStatus"
  - "%OptimizeFunctionOnNextCall"
  - node --prof
  - --cpu-prof
spiral:
  - performance-cliffs
  - observability
prereqs:
  - "07-async-deep/04-async-await-desugaring"
sources:
  - https://v8.dev/blog/launching-ignition-and-turbofan
  - https://v8.dev/docs/profile
  - https://nodejs.org/en/learn/getting-started/profiling
---

import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import NumbersCard from "~/components/layout/NumbersCard.astro";
import Misconception from "~/components/layout/Misconception.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
Функция на пути рендера быстра 200 мс, затем еле ползёт. CPU-профиль показывает, что она каждые несколько секунд снова всплывает на вершину flame graph. Вы подозреваете цикл деопта — но подозрение это не диагноз. V8 точно скажет, что случилось, на каком смещении байт-кода и почему, если спросить его правильными флагами. Этот урок — инструментарий для такого вопроса.
</Hook>

<Crux>V8 логирует каждую оптимизацию, деопт и переход IC; чтение строк превращает «кажется медленным» в названную причину.</Crux>

<Explanation>

## Мышление: воспроизвести, трассировать, прочитать, исправить

Работа над производительностью в V8 — это не гадание. Движок инструментирован изнутри, и Node/d8 раскрывают эту инструментацию через флаги. Цикл всегда один и тот же: детерминированно воспроизвести медленный путь, запустить его под нужным флагом трассировки, прочитать события, которые флаг выпускает, затем исправить данные или поток управления, вызвавшие их — но не движок.

<FlowDiagram
  label="Четырёхшаговый рабочий процесс диагностики проблемы производительности V8: детерминированно воспроизвести медленный путь, запустить под нужным флагом трассировки или natives, прочитать выпущенные события, чтобы найти названную причину, затем исправить данные или поток управления."
  hue="var(--accent)"
  caption="Цикл диагностики. У каждого шага свой инструмент: детерминированный воспроизводящий стенд, флаг трассировки, лог событий и фикс модели данных. Вы никогда не настраиваете движок — вы меняете то, чем его кормите."
  nodes={[
    { id: "repro", label: "Воспроизвести", sub: "детерм. репро", col: 0, row: 0 },
    { id: "trace", label: "Трассировать", sub: "--trace-* / %natives", col: 1, row: 0 },
    { id: "read", label: "Прочитать события", sub: "назвать причину", col: 2, row: 0 },
    { id: "fix", label: "Исправить", sub: "данные / поток", col: 3, row: 0 },
  ]}
  edges={[
    { from: "repro", to: "trace", label: "запуск" },
    { from: "trace", to: "read", label: "лог" },
    { from: "read", to: "fix", label: "диагноз" },
    { from: "fix", to: "repro", label: "перепроверка" },
  ]}
/>

Эти флаги работают с `node` (он встраивает V8) и с `d8` (отдельная оболочка V8). Для разовых экспериментов `d8` чище, потому что в нём нет шума Node. Для вопроса «что делает моё настоящее приложение» нужен `node --prof` или `--cpu-prof`.

## Трассировка ярусов: --trace-opt и --trace-deopt

`--trace-opt` печатает строку каждый раз, когда функция выбрана для оптимизации, и каким ярусом компилятора. Вы узнаёте, *когда* функция стала достаточно горячей, чтобы скомпилироваться, и дошла ли она до Maglev или TurboFan:

```
$ node --trace-opt app.js
[marking 0x... <JSFunction parseRow> for optimization to MAGLEV, ...]
[completed optimizing 0x... <JSFunction parseRow> (target MAGLEV)]
[marking 0x... <JSFunction parseRow> for optimization to TURBOFAN, ...]
```

`--trace-deopt` — флаг, к которому тянешься чаще всего. Он печатает строку каждый раз, когда оптимизированная функция откатывается обратно в интерпретатор — событие, стоящее почти за каждой регрессией «быстро, потом медленно»:

```
[deoptimizing (DEOPT eager): begin 0x... <JSFunction sum> (opt #3) @5, FP to SP ...
 ;;; deoptimize at <app.js:12:14>, reason: Smi, ...
```

Читайте слева направо: **вид** (`eager` — guard упал на входе в проверяемую операцию; `lazy` — функцию инвалидировали, пока она висела приостановленной на стеке; `soft` — откат из-за бедности обратной связи, который не выбрасывает код), **функция** и id оптимизации, **смещение байт-кода** (`@5`), **место в исходнике** и **причина** — здесь `Smi`, означающая, что значение, которое оптимизированный код считал малым целым, им не было. Это одно слово и есть диагноз: число вышло за диапазон Smi или пришло не целое.

<NumbersCard
  title="Набор флагов практика"
  data-lesson-visual
  stats={[
    { label: "--trace-opt", value: "когда + каким ярусом оптимизировано" },
    { label: "--trace-deopt", value: "вид деопта, смещение, причина" },
    { label: "--trace-ic", value: "переходы состояния IC по точкам" },
    { label: "--print-bytecode", value: "дамп байт-кода Ignition" },
    { label: "--print-opt-code --code-comments", value: "маш. код TurboFan" },
    { label: "node --prof / --cpu-prof", value: "сэмпл. / DevTools профиль" },
  ]}
/>

## Трассировка IC и байт-кода

`--trace-ic` логирует каждый переход состояния inline cache в каждой точке доступа к свойству и вызова: продвижение от неинициализированного (`0`) к premonomorphic, monomorphic (`1`), polymorphic (`P`) и megamorphic (`N`). Когда горячая точка, которую вы ждали monomorphic, показывает `N`, вы нашли расхождение форм — тема юнита 03. В логе есть указатели map (hidden class), которые она видела, так что можно посчитать число различных форм, попавших в точку.

`--print-bytecode` выгружает байт-код Ignition для каждой скомпилированной функции — полезно подтвердить, что интерпретатор реально исполняет (например, что загрузка свойства стала общим `LdaNamedProperty`, а не чем-то дешевле). `--print-opt-code` с `--code-comments` выгружает сгенерированный TurboFan машинный код с аннотациями, привязывающими инструкции к исходнику; к нему тянутся только когда нужно подтвердить предположение на уровне инструкций, не для повседневной работы.

## Принуждаем к делу через --allow-natives-syntax

Для детерминированных экспериментов `--allow-natives-syntax` разблокирует интринзики с префиксом `%`, чтобы можно было управлять оптимизатором вручную, не дожидаясь естественного разогрева функции:

```js
function sum(a, b) { return a + b; }

sum(1, 2);                         // warm up feedback (it has seen Smis)
%OptimizeFunctionOnNextCall(sum);  // request optimisation
sum(3, 4);                         // this call runs optimised code
console.log(%GetOptimizationStatus(sum));
```

`%GetOptimizationStatus(fn)` возвращает **битовую маску**, которую декодируешь бит за битом. Биты, которые важны: *оптимизирована*, *turbofanned*, *maglevved*, *интерпретируется*, *помечена на деоптимизацию*, *является функцией* и *никогда не оптимизировать* (функция содержит конструкцию, которую V8 отказывается оптимизировать). Значение со взведёнными битами «оптимизирована» и «turbofanned» означает, что исполняется код TurboFan; если взведён бит «помечена на деоптимизацию», следующий вызов откатится. Прочие интринзики дополняют набор: `%HasFastProperties(obj)` говорит, остаётся ли объект в быстром (не словарном) режиме, а `%DebugPrint(obj)` выгружает его полную внутреннюю раскладку — указатель map, значения свойств и **element kind** массива (PACKED_SMI против HOLEY и так далее).

<Misconception id="mc-intrinsics-prod">
  <p><strong>«Я выкачу `%OptimizeFunctionOnNextCall`, чтобы форсировать горячий путь в продакшене».</strong> Интринзики `%` существуют только под <code>--allow-natives-syntax</code>, иначе кидают ошибку — это диагностика d8/Node, а не runtime-API. На оптимизатор влияют стабильными формами и типами, а не командами.</p>
</Misconception>

## Профилируем настоящее приложение

Флаги трассировки отвечают на вопрос «почему медленна *вот эта функция*». Чтобы сначала найти, *какая* функция медленна, профилируйте:

- **`node --prof`** пишет `isolate-*.log` из сэмплированных стеков; **`node --prof-process isolate-*.log`** превращает его в человекочитаемый отчёт с разбивкой тиков по функциям и секцией, выделяющей время Ignition против оптимизированного кода и время GC.
- **`node --cpu-prof`** пишет `.cpuprofile`, который грузишь прямо в панель Performance Chrome DevTools (или VS Code) ради flame graph — читать его гораздо легче, чем текстовый профиль.

Дисциплина: сначала профиль, чтобы найти доминирующую стоимость, затем нацелить `--trace-deopt` / `--trace-ic` на функцию, которую обвинил профиль. Слепая трассировка всего приложения топит вас в строках.

<Inset kind="why" lang="ru">
Чем отличается `soft`-деопт: eager/lazy-деопт выбрасывает оптимизированный код, и функция должна заново разогреться и перекомпилироваться. *Soft*-деопт случается, когда оптимизированный код попадает на путь, для которого у него нет обратной связи (причина `insufficient type feedback`); V8 сохраняет код и просто собирает обратную связь. Горстка soft-деоптов на старте — норма. Повторяющийся *eager*-деопт на той же функции в том же смещении — это патология, цикл деопта, и слово-причина говорит о сломанном предположении.
</Inset>

<Quiz
  id="q-deopt-reason"
  lessonSlug="01-trace-opt-and-deopt"
  lang="ru"
  question="Строка `--trace-deopt` гласит `deoptimizing (DEOPT eager) ... reason: Smi` на функции, суммирующей массив. Какова наиболее вероятная причина?"
  choices={[
    { label: "Значение, которое оптимизированный код считал малым целым, оказалось double или вне диапазона Smi", correct: true },
    { label: "Сборщик мусора приостановил функцию посреди исполнения", misconception: "Паузы GC останавливают мир, но не дают деопт с причиной `Smi`. Слово-причина называет упавший guard типа или диапазона, а не событие GC." },
    { label: "Функция вообще не была оптимизирована изначально", misconception: "Деопт по определению означает, что функция БЫЛА оптимизирована — интерпретируемый код деоптимизировать нельзя. Строка — это откат из скомпилированного кода в интерпретатор." },
    { label: "Inline cache стал megamorphic", misconception: "Megamorphic IC видны под --trace-ic как состояние N и дают причину, связанную с формой (например, wrong map). Причина `Smi` — именно сбой числового guard типа/диапазона." },
  ]}
/>

<Quiz
  id="q-which-flag"
  lessonSlug="01-trace-opt-and-deopt"
  lang="ru"
  question="Вы пока не представляете, какая функция горячая. Какой инструмент берёте ПЕРВЫМ?"
  choices={[
    { label: "node --prof (или --cpu-prof), чтобы найти доминирующую функцию, затем трассировать её", correct: true },
    { label: "--trace-deopt на всё приложение и читать каждую строку", misconception: "Слепая трассировка всего приложения выпускает тысячи строк, включая нормальные стартовые деопты. Сначала профилируешь, чтобы найти одну функцию, достойную трассировки." },
    { label: "%DebugPrint на каждом объекте", misconception: "%DebugPrint выгружает раскладку одного объекта — полезно только после того, как знаешь, какой объект и функция важны. Куда уходит время, он не говорит." },
    { label: "--print-opt-code, чтобы прочитать машинный код", misconception: "Дампы машинного кода — это шаг подтверждения в крайнем случае, а не способ найти горячий путь. Сначала профиль." },
  ]}
/>

<DragOrder
  id="do-diagnosis-loop"
  pieceSlug="01-trace-opt-and-deopt"
  lang="ru"
  prompt="Расставьте по порядку шаги грамотной диагностики производительности V8 с нуля."
  items={[
    "Собрать детерминированное репро, прогоняющее медленный путь",
    "Профилировать через node --prof / --cpu-prof, чтобы найти доминирующую функцию",
    "Запустить эту функцию под --trace-deopt / --trace-ic, чтобы назвать причину",
    "Исправить данные или поток управления, сломавшие предположение, затем перепроверить",
  ]}
/>

</Explanation>

<KeyTakeaway>V8 полностью инструментирован: --trace-opt показывает, что скомпилировано и каким ярусом, --trace-deopt называет каждый откат с причиной и смещением байт-кода, --trace-ic показывает расхождение форм, а --allow-natives-syntax даёт декодировать %GetOptimizationStatus и %DebugPrint вручную. Сначала профилируй через --prof/--cpu-prof, затем трассируй функцию, которую обвинил профиль.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="trace-opt-and-deopt-retrieval"
  lang="ru"
  questions={[
    { q: "Разберите чтение одной строки --trace-deopt. Какие у неё поля и какое из них диагноз?", a: "Строка деопта даёт: вид — eager (guard упал на входе в проверяемую операцию), lazy (оптимизированный фрейм инвалидирован, пока висел приостановленным на стеке) или soft (недостаток обратной связи, код сохранён); имя JSFunction и его id оптимизации (opt #N); смещение байт-кода (@N), где случился откат; место в исходнике (file:line:col); и слово-причину. Причина и есть диагноз: 'Smi' значит, что значение, считавшееся малым целым, было double или вне диапазона; 'wrong map' / 'wrong instance type' — неожиданная форма объекта; 'lost precision' / 'not a heap number' называют сбои числовых guard. Повторяющийся eager-деопт на том же смещении — цикл деопта; сопоставь причину со сломанным предположением и исправь вход." },
    { q: "Как декодировать %GetOptimizationStatus и что делать с %HasFastProperties и %DebugPrint?", a: "%GetOptimizationStatus(fn) возвращает целочисленную битовую маску (доступна только под --allow-natives-syntax). Проверяешь отдельные биты: оптимизирована, turbofanned, maglevved, интерпретируется, помечена-на-деоптимизацию, является-функцией и никогда-не-оптимизировать. Оптимизирована + turbofanned взведены = исполняется код TurboFan; взведён помечена-на-деоптимизацию = следующий вызов откатится. %HasFastProperties(obj) возвращает false, как только объект упал в словарный режим (слишком много свойств или delete) — постоянное замедление. %DebugPrint(obj) выгружает полную внутреннюю раскладку объекта: указатель map (hidden class), значения свойств и element kind массива (PACKED_SMI, PACKED_DOUBLE, HOLEY и т. д.), так что предположение о форме или element kind подтверждается напрямую." },
    { q: "В чём разница между node --prof и node --cpu-prof и где каждый в рабочем процессе?", a: "Оба периодически сэмплируют стек вызовов, чтобы приписать время функциям. --prof пишет низкоуровневый isolate-*.log, который постобрабатываешь через node --prof-process ради текстового отчёта — тики на функцию плюс разбивка времени интерпретации против оптимизированного кода против GC. --cpu-prof пишет .cpuprofile, который открываешь прямо в Chrome DevTools (или VS Code) ради интерактивного flame graph, по нему ориентироваться куда легче. Оба — шаг первый: они локализуют доминирующую функцию, чтобы знать, на какую функцию нацелить --trace-deopt / --trace-ic. Трассировка без профиля сначала хоронит вас в несвязанных стартовых событиях." },
  ]}
/>

<Recap lang="ru">
Диагностика производительности V8 — это цикл из четырёх шагов: детерминированно воспроизвести, трассировать, прочитать события, исправить данные или поток управления. Флаги — это инструментарий. `--trace-opt` сообщает, когда функция оптимизирована и каким ярусом (Maglev/TurboFan); `--trace-deopt` сообщает каждый откат с его видом (eager/lazy/soft), смещением байт-кода, местом в исходнике и словом-причиной — и эта причина (`Smi`, `wrong map`, `lost precision`) и есть диагноз. `--trace-ic` раскрывает переходы состояния inline cache, чтобы заметить расхождение форм; `--print-bytecode` и `--print-opt-code --code-comments` выгружают байт-код Ignition и машинный код TurboFan для подтверждения. Под `--allow-natives-syntax` управляешь оптимизатором вручную через `%OptimizeFunctionOnNextCall`, декодируешь битовую маску `%GetOptimizationStatus` и осматриваешь раскладку через `%HasFastProperties` и `%DebugPrint`. Чтобы найти, какую функцию трассировать, сначала профилируй через `node --prof` (затем `--prof-process`) или `--cpu-prof` ради flame graph в DevTools. Профиль чтобы локализовать, трассировка чтобы диагностировать, фикс выше движка.
</Recap>
