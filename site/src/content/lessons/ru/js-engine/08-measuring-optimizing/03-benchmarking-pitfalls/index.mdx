---
slug: "03-benchmarking-pitfalls"
lang: ru
track: "js-engine"
unit: "08-measuring-optimizing"
order: 3
title: "Почему ваш микробенчмарк врёт"
summary: "Ловушки, заставляющие микробенчмарки сообщать фантастические числа: устранение мёртвого кода, свёртка констант и вынос инвариантов, неучтённый разогрев JIT, OSR посреди измерения, шум GC, деопты от console.log/try-catch в замеряемой зоне и несовпадение micro и macro"
estMin: 15
status: ready
lessonType: topic
level: senior
concepts:
  - dead-code elimination
  - constant folding
  - JIT warm-up
  - on-stack replacement
  - GC noise
  - black-hole sink
  - performance.now
  - macro vs micro
spiral:
  - performance-cliffs
  - measurement
prereqs:
  - "02-monomorphism-discipline"
sources:
  - https://mrale.ph/blog/2012/12/15/microbenchmarks-fairy-tales.html
  - https://github.com/tinylibs/tinybench
  - https://v8.dev/blog/jetstream2
---

import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import NumbersCard from "~/components/layout/NumbersCard.astro";
import Misconception from "~/components/layout/Misconception.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
Вы написали цикл, чтобы доказать, что версия A быстрее версии B. Цикл отработал за 0.0 мс. Вы выкатываете версию A. В продакшене она не быстрее — иногда медленнее. Бенчмарк не соврал про время; он замерил программу, которую оптимизатор тихо переписал в ничто, потому что результат ни разу не использовался. Почти каждый уверенный микробенчмарк, что вы читали, неверен одним из горстки конкретных способов.
</Hook>

<Crux>Оптимизирующий JIT удалит, свернёт, вынесет или разогреет ваш бенчмарк, пока тот не начнёт мерить нечто иное, чем ваш код.</Crux>

<Explanation>

## Почему JIT — противник вашего бенчмарка

Микробенчмарк — это крошечная программа, вывод которой вы игнорируете, прогоняемая в плотном цикле — ровно та программа, которую оптимизирующий компилятор *лучше всего* умеет сносить. Вся работа TurboFan — убрать работу, не влияющую на наблюдаемое поведение. Если результат вашего бенчмарка никогда не наблюдается, то работа, его породившая, по определению устранима. Ловушки ниже — все следствия того, что оптимизатор делает свою работу над кодом, который вы не намеревались оптимизировать в ничто.

<FlowDiagram
  label="Анатомия вводящего в заблуждение бенчмарка против здравого: вводящий в заблуждение путь даёт оптимизатору устранить, свернуть и вынести тело и мерит разогрев, тогда как здравый путь потребляет результат в sink, рандомизирует входы, разогревается до замера и изолирует аллокацию."
  hue="var(--accent)"
  caption="Два стенда, одно намерение. Верхний путь мерит фантом; нижний мерит установившуюся работу, потому что нарочно побеждает каждую ловушку оптимизатора."
  nodes={[
    { id: "mislead", label: "Вводит в заблуждение", sub: "результат не нужен", col: 0, row: 0 },
    { id: "dce", label: "DCE + свёртка", sub: "тело удалено", col: 1, row: 0 },
    { id: "phantom", label: "Фантом 0мс", sub: "фантаст. число", col: 2, row: 0 },
    { id: "sound", label: "Здравый", sub: "потреби + рандом", col: 0, row: 1 },
    { id: "warm", label: "Разогрей", sub: "дойди до яруса", col: 1, row: 1 },
    { id: "real", label: "Реальное число", sub: "устан. стоимость", col: 2, row: 1 },
  ]}
  edges={[
    { from: "mislead", to: "dce", label: "оптимизатор" },
    { from: "dce", to: "phantom", label: "мерит ничто" },
    { from: "sound", to: "warm", label: "sink + входы" },
    { from: "warm", to: "real", label: "мерит работу" },
  ]}
/>

## Ловушка 1 — устранение мёртвого кода (DCE)

Если тело цикла вычисляет значение, которое никто не читает, оптимизатор доказывает, что вычисление наблюдаемо бесполезно, и удаляет его. Ваш бенчмарк «1 миллиард операций» становится пустым циклом — иногда удаляется и сам цикл. Фикс — это **black-hole sink**: накапливайте каждый результат в переменную, которую читаете *после* замеряемой зоны (и в идеале печатаете или возвращаете), чтобы оптимизатор не смог доказать, что работа мёртвая.

```js
// ОБМАН — результат выброшен, тело устранено
for (let i = 0; i < 1e9; i++) { expensive(i); }

// ЧЕСТНО — sink делает работу наблюдаемой
let sink = 0;
for (let i = 0; i < 1e9; i++) { sink += expensive(i); }
console.log(sink); // read it so DCE cannot remove the loop
```

## Ловушка 2 — свёртка констант и вынос инвариантов цикла

Если вход — это литеральная константа, оптимизатор вычисляет ответ один раз на этапе компиляции (**свёртка констант**), и ваш цикл мерит ничто. Если подвыражение не меняется между итерациями, оно **выносится** из цикла (вынос инвариантного кода) и выполняется один раз вместо N. Фикс: делайте входы непредсказуемо меняющимися — выводите их из индекса цикла или заранее сгенерированного массива случайных значений, которые оптимизатор не может вычислить заранее («blackbox»-вход).

## Ловушка 3 — неучтённый разогрев JIT

Первые итерации идут в Ignition (интерпретируются), затем в Sparkplug, затем в Maglev/TurboFan, как только функция станет горячей. Если замерять с итерации ноль, вы усредняете холодную интерпретацию, паузы компиляции и установившийся машинный код в одно бессмысленное число — и «быстрый» результат может быть просто слишком коротким бенчмарком, который никогда не достигнет TurboFan. Отбросьте фазу разогрева, затем замеряйте; или сообщайте ярусы отдельно, если важны оба — и старт, и установившееся состояние.

## Ловушка 4 — OSR посреди измерения

Один гигантский цикл `for` может вызвать **on-stack replacement**: V8 компилирует и подставляет оптимизированный код *пока цикл ещё выполняется*, так что ранняя часть того самого цикла, который вы мерите, идёт интерпретируемо, а остаток — оптимизированно — разрыв, вшитый в одну выборку. Предпочитайте много вызовов меньшей бенчмарк-функции (это разогревает *функцию*, а не один цикл) одному огромному циклу.

## Ловушка 5 — шум GC в замеряемой зоне

Если тело аллоцирует, сборщик мусора рано или поздно встанет на паузу внутри вашего измерения, приписав время GC вашему коду как случайный всплеск. Либо уберите аллокацию из замеряемой зоны, либо прогоните бенчмарк много раз и сообщайте устойчивую статистику (медиана/минимум), а не среднее, чтобы шальная пауза GC не доминировала. Сообщайте распределение, а не одно число.

## Ловушка 6 — деопты, которые вы протащили в замеряемую зону

`console.log` внутри замеряемого цикла, `try/catch`, через который V8 не будет оптимизировать, или полиморфный вызов, внесённый самим стендом, могут деоптить ту самую функцию, которую вы мерите — так что вы мерите деоптимизированную версию. Держите замеряемую зону чистой: без логирования, без исключений, одна стабильная форма и тип, и проверьте через `--trace-deopt`, что горячая функция не откатывается во время замера.

## Ловушка 7 — несовпадение micro и macro

Даже идеальный микробенчмарк отвечает лишь на вопрос «насколько быстр этот изолированный паттерн в синтетическом цикле». Ваше приложение выполняет его вперемешку с другой работой, с холодными кэшами, реальными распределениями входов и конкуренцией за GC — где победитель micro может проиграть. Микробенчмарки выбирают между двумя реализациями известного горячего места; они не говорят, что это горячее место важно. Для сквозной правды используйте **macro**-наборы (JetStream, Speedometer) или профилируйте настоящее приложение. Для честных *micro*-чисел используйте стенд, построенный побеждать эти ловушки: `tinybench` или `mitata` берут на себя разогрев, sink и статистику.

<NumbersCard
  title="Чек-лист честного бенчмарка"
  data-lesson-visual
  stats={[
    { label: "Потребляй результаты в", value: "black-hole sink" },
    { label: "Входы", value: "рандомизированы / blackbox" },
    { label: "Разогрев", value: "отбрось, затем замеряй" },
    { label: "Таймер", value: "performance.now() (суб-мс)" },
    { label: "Сообщай", value: "медиана/мин, не среднее" },
    { label: "Проверь горячую fn", value: "--trace-deopt = без отката" },
  ]}
/>

<Misconception id="mc-zero-ms">
  <p><strong>«Цикл отработал за 0мс, значит этот код по сути бесплатен».</strong> 0мс почти никогда не значит «бесконечно быстро» — устранение мёртвого кода удалило тело, потому что его результат не использовался. Потребляйте результат в sink и читайте его после замера; честное число никогда не ноль.</p>
</Misconception>

<Quiz
  id="q-zero-ms-cause"
  lessonSlug="03-benchmarking-pitfalls"
  lang="ru"
  question="Ваш цикл-бенчмарк над чистой функцией сообщает 0.0мс на миллиард итераций. Что произошло?"
  choices={[
    { label: "Устранение мёртвого кода убрало тело цикла, потому что его результат ни разу не читался", correct: true },
    { label: "V8 реально так быстр — чистые функции бесплатны", misconception: "Никакое реальное вычисление миллиарда итераций не бесплатно. Результат 0мс — это оптимизатор, доказавший, что работа ненаблюдаема, и удаливший её, а не мгновенная работа." },
    { label: "performance.now() не имеет разрешения, чтобы это замерить", misconception: "У performance.now() суб-миллисекундное разрешение; для миллиарда реальных итераций он всё равно показал бы ненулевое. Тело было устранено." },
    { label: "Сработал сборщик мусора и сбросил таймер", misconception: "GC не сбрасывает таймеры; он лишь добавил бы время. Нулевой результат — это DCE, а не GC." },
  ]}
/>

<Quiz
  id="q-warmup"
  lessonSlug="03-benchmarking-pitfalls"
  lang="ru"
  question="Вы замеряете с самой первой итерации горячей функции. Почему число вводит в заблуждение?"
  choices={[
    { label: "Оно усредняет холодную интерпретацию, паузы компиляции и установившийся машинный код в одно число", correct: true },
    { label: "Первая итерация всегда самая быстрая, занижая результат", misconception: "Первые итерации САМЫЕ МЕДЛЕННЫЕ (интерпретация + компиляция), завышая среднее с нуля — и смешивая ярусы независимо от направления." },
    { label: "performance.now() ещё не инициализирован на итерации ноль", misconception: "Таймер готов сразу; проблема в смешивании ярусов разогрева в измерение, а не в инициализации таймера." },
    { label: "Разогрев не влияет; V8 оптимизирует до запуска main()", misconception: "V8 оптимизирует адаптивно во время работы после того, как функция стала горячей, а не заранее. Ранние итерации действительно идут на нижних ярусах." },
  ]}
/>

<DragOrder
  id="do-honest-harness"
  pieceSlug="03-benchmarking-pitfalls"
  lang="ru"
  prompt="Расставьте по порядку шаги превращения наивного цикла в честный микробенчмарк."
  items={[
    "Подавай рандомизированные / blackbox входы, чтобы ничего нельзя было свернуть в константу",
    "Накапливай каждый результат в black-hole sink, читаемый после замера",
    "Прогони фазу разогрева и отбрось её, чтобы функция дошла до установившегося яруса",
    "Замеряй много прогонов и сообщай медиану/минимум, затем проверь отсутствие деопта через --trace-deopt",
  ]}
/>

<Inset kind="edgecase" lang="ru">
Жестокий поворот: микробенчмарк может быть *слишком* благосклонным. В крошечном цикле в горячую функцию приходят одна форма объекта и один тип, поэтому IC идеально monomorphic, и TurboFan инлайнит всё — быстрое число. В продакшене та же функция видит пять форм, становится megamorphic и никогда не инлайнится. Micro-число было честным про micro-условия и нечестным про ваше приложение. Вот почему проходящий микробенчмарк необходим, но никогда не достаточен: подтвердите macro-прогоном или продакшен-профилем.
</Inset>

</Explanation>

<KeyTakeaway>Оптимизирующий JIT устранит мёртвый код, свернёт константы, вынесет инварианты и смешает ярусы разогрева, пока ваш бенчмарк не начнёт мерить фантом. Побеждайте каждую ловушку нарочно: потребляйте результат в sink, рандомизируйте входы, отбрасывайте разогрев, изолируйте аллокацию, держите замеряемую зону без деоптов и подтверждайте micro-победы macro-прогоном.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="benchmarking-pitfalls-retrieval"
  lang="ru"
  questions={[
    { q: "Объясните устранение мёртвого кода в бенчмарке и black-hole sink, который его побеждает.", a: "Оптимизирующий компилятор убирает любое вычисление, результат которого ненаблюдаем. В микробенчмарке вы обычно отбрасываете результат тестируемой функции, поэтому оптимизатор может доказать, что вычисление не имеет наблюдаемого эффекта, и удалить тело цикла — иногда цикл целиком — давая ~0мс «бесконечно быстрый» результат, мерящий ничто. Фикс — black-hole sink: накапливайте каждый результат в переменную (sink += f(i)), затем читайте эту переменную после замеряемой зоны (печатайте или возвращайте). Поскольку значение sink наблюдается, оптимизатор не может доказать, что работа мёртвая, и обязан её сохранить. Настоящие стенды (tinybench, mitata) предоставляют не-устраняемый sink внутри; классическая статья mrale о сказках микробенчмарков описывает ловушку в деталях." },
    { q: "Что такое разогрев JIT и OSR и как они портят измерение с нуля?", a: "Код стартует в Ignition (интерпретируется), повышается до Sparkplug, затем Maglev/TurboFan, как только выполнится достаточно, чтобы стать горячим. Если начать замер на итерации ноль, вы смешиваете холодную интерпретацию, сами паузы компиляции и установившийся машинный код в одно число — и слишком короткий бенчмарк, не достигающий TurboFan, сообщает число, не имеющее ничего общего с оптимизированным установившимся состоянием. On-stack replacement (OSR) делает хуже для бенчмарков из одного гигантского цикла: V8 может скомпилировать и подставить оптимизированный код, пока цикл на лету, так что часть того самого цикла, который вы мерите, идёт интерпретируемо, а часть — оптимизированно. Фиксы: прогоните и отбросьте фазу разогрева, затем замеряйте установившееся состояние (или сообщайте ярусы отдельно); и предпочитайте много вызовов малой бенчмарк-функции одному огромному циклу, чтобы разогревать функцию, а не вызывать OSR внутри замеряемого цикла." },
    { q: "Почему проходящий микробенчмарк необходим, но не достаточен, и что добавляют macro-наборы?", a: "Микробенчмарк мерит один изолированный паттерн в синтетических условиях: одна форма входа, горячие кэши, без конкурирующей работы. Эти условия часто делают код лучше реальности — monomorphic IC и полный инлайнинг, которых та же функция никогда не получает в продакшене, где она видит разные формы, холодные кэши, реальные распределения входов и конкуренцию за GC. Так что micro-победа может испариться или развернуться сквозным образом. Микробенчмарки хороши для выбора между двумя реализациями известного горячего места, не для доказательства, что горячее место важно или что изменение помогает всему приложению. Macro-наборы (JetStream, Speedometer) и CPU-профили настоящего приложения мерят сквозные реалистичные нагрузки, ловя взаимодействия, которые микробенчмарк прячет. Используйте micro чтобы выбрать реализацию, macro/профиль чтобы подтвердить, что она помогает на месте." },
  ]}
/>

<Recap lang="ru">
Микробенчмарк — это программа, которую оптимизирующий JIT лучше всего умеет сносить, потому что её результат игнорируется, а цикл плотный. Отсюда семь ловушек: устранение мёртвого кода удаляет тело, чей результат не нужен (побеждайте black-hole sink, читаемым после замера); свёртка констант и вынос инвариантов вычисляют заранее литеральные или неменяющиеся входы (побеждайте рандомизированными/blackbox входами); неучтённый разогрев смешивает Ignition, паузы компиляции и установившийся TurboFan в одно число (отбросьте фазу разогрева или сообщайте ярусы отдельно); OSR подставляет оптимизированный код в ещё работающий гигантский цикл (предпочитайте много малых вызовов); шум GC впрыскивает паузы в замеряемую зону (изолируйте аллокацию, сообщайте медиану/минимум); console.log/try-catch/полиморфизм протаскивают деопт в замеряемую функцию (держите зону чистой, проверяйте через --trace-deopt); а несовпадение micro и macro значит, что даже идеальное micro-число не отражает приложение (подтвердите через JetStream/Speedometer или продакшен-профиль). Для честных micro-чисел используйте tinybench или mitata, которые берут на себя sink, разогрев и статистику.
</Recap>
