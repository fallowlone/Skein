---
slug: "02-monomorphism-discipline"
lang: ru
track: "js-engine"
unit: "08-measuring-optimizing"
order: 2
title: "Дисциплина мономорфизма"
summary: "Действенный чек-лист, связывающий весь трек: строй объекты с полями в фиксированном порядке, никогда не delete, держи массивы в одном element kind, передавай в горячие функции аргументы одной формы, держи числа в диапазоне Smi и избегай megamorphic-диспетчеризации"
estMin: 14
status: ready
lessonType: topic
level: senior
concepts:
  - monomorphism
  - shape transition
  - element kind
  - inline cache state
  - Smi overflow
  - megamorphic dispatch
  - dictionary mode
spiral:
  - performance-cliffs
  - discipline
prereqs:
  - "01-trace-opt-and-deopt"
sources:
  - https://mathiasbynens.be/notes/shapes-ics
  - https://v8.dev/blog/elements-kinds
  - https://github.com/thlorenz/v8-perf/blob/master/compiler.md
---

import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import NumbersCard from "~/components/layout/NumbersCard.astro";
import Misconception from "~/components/layout/Misconception.astro";
import StackDiagram from "~/components/diagram/StackDiagram.astro";

<Hook>
У каждого цикла деопта, megamorphic-точки вызова и объекта в словарном режиме, которые вы трассировали в прошлом уроке, один корень: движок сделал ставку на форму или тип, а ваш код поменял её под ногами оптимизированного кода. Этот урок — обратная сторона: малый набор привычек, которые держат эти ставки выигрышными. Ни одна из них не микрооптимизация. Это то, как пишут JavaScript, остающийся на быстрых путях V8 по умолчанию.
</Hook>

<Crux>Мономорфизм — одна дисциплина: держи форму каждого горячего объекта и тип каждого значения стабильными, чтобы ставки V8 не ломались.</Crux>

<Explanation>

## Один принцип, шесть правил

Оптимизированный код V8 *спекулятивен*: TurboFan компилирует функцию, предполагая, что формы и типы, которые он видел до сих пор, продолжат приходить. Сломайте это предположение — и платите: деопт, polymorphic IC, падение в словарный режим, откат из-за переполнения Smi. Дисциплина ниже — это просто «не удивляй оптимизатор». Каждое правило отображается на механизм, уже встреченный в этом треке ([mono/poly/mega IC](/learn/js-engine/05-mono-poly-mega), [переходы форм](/learn/js-engine/02-shape-transitions), element kinds, Smi против HeapNumber); здесь они становятся чек-листом.

<StackDiagram
  label="Сравнение «делай и не делай» для шести правил мономорфизма: каждая строка объединяет привычку, удерживающую V8 на быстром пути, с антипаттерном, ломающим соответствующий механизм."
  hue="var(--accent)"
  caption="Шесть правил сверху вниз. Левая половина каждой заметки — дисциплина; правая — антипаттерн и механизм, который он ломает."
  layers={[
    { label: "Создание в фиксированном порядке", note: "ДЕЛАЙ: class/factory, все поля сразу · НЕ: условные/поздние поля → расхождение форм" },
    { label: "Никогда не delete", note: "ДЕЛАЙ: присваивай null · НЕ: delete obj.x → словарный режим (навсегда)" },
    { label: "Один element kind на массив", note: "ДЕЛАЙ: держи PACKED_SMI · НЕ: смешивай Smi/double/object или вноси дырки" },
    { label: "Согласованные формы аргументов", note: "ДЕЛАЙ: одна форма/тип в горячую fn · НЕ: разные формы → polymorphic IC" },
    { label: "Оставайся в диапазоне Smi", note: "ДЕЛАЙ: ограничь int / типизир. массивы · НЕ: переполни ±2^31 → деопт HeapNumber" },
    { label: "Стабильная диспетчеризация", note: "ДЕЛАЙ: мало стабильных callee · НЕ: много callee в одной точке → megamorphic" },
  ]}
/>

## Правило 1 — создавай со всеми полями в фиксированном порядке

Стройте объекты так, чтобы каждый экземпляр шёл по *одному и тому же* пути переходов hidden class: объявляйте все поля в конструкторе или фабрике, в постоянном порядке, без условных добавлений. Два объекта с одними полями, добавленными в разном порядке, — это разные формы; поле, добавляемое лишь иногда, создаёт вторую форму. Фикс для отсутствующих данных — значение по умолчанию, а не пропуск:

```js
// DON'T — conditional + late fields → many shapes
function makeRow(r) {
  const o = {};
  o.id = r.id;
  if (r.name) o.name = r.name;     // shape A vs shape B
  o.score = r.score;
  if (r.flagged) o.flag = true;    // shape C, D...
  return o;
}

// DO — one shape, every time
function makeRow(r) {
  return {
    id: r.id,
    name: r.name ?? null,
    score: r.score,
    flag: r.flagged === true,
  };
}
```

Это единственное правило предотвращает самую частую продакшен-регрессию: горячая точка доступа становится megamorphic, потому что объекты строились в порядке ключей входа. (См. [mono/poly/mega IC](/learn/js-engine/05-mono-poly-mega) о цене megamorphic.)

## Правило 2 — никогда не `delete`; присваивай `null`

`delete obj.x` не может просто очистить слот — он вытесняет объект из раскладки с фиксированными смещениями в **словарный режим**, представление на хеш-таблице, где каждый доступ к свойству идёт медленным общим путём (~50–100 циклов против 1), и объект уже не восстановится. Память, которую вы «экономите», крошечная; замедление постоянно. Присвойте полю `null` вместо этого — и форма останется целой.

## Правило 3 — держи массив в одном element kind

V8 отслеживает **element kind** каждого массива и только расширяет его (в одну сторону): `PACKED_SMI_ELEMENTS` (самый быстрый) → `PACKED_DOUBLE_ELEMENTS` → `PACKED_ELEMENTS` (любой тип) → варианты `HOLEY_*` (добавляют проверку дырки на каждом доступе). Подмешивание `1.5` в массив целых расширяет его навсегда; запись за `length` или `new Array(n)` делает его HOLEY. Привычки, держащие его узким:

- Предпочитайте `[]` + `push` вместо `new Array(n)` или присваивания за длину (оба создают HOLEY).
- Предпочитайте `Array.from({length}, fn)` вместо `new Array(n).fill()`, когда нужен PACKED-массив заданного размера.
- Не храните смешанные типы (Smi, double, object) в одном горячем массиве; разделяйте по типу или используйте типизированный массив.

## Правило 4 — корми горячие функции аргументами согласованной формы

Inline cache функции ключуются по формам и типам её аргументов. Вызывайте `render(node)` с одной формой узла — и IC остаются monomorphic; вызывайте с пятью разными формами — они становятся megamorphic, и TurboFan не может специализироваться. Если горячая функция обязана принимать варианты, разбейте её на функции под конкретные формы или нормализуйте входы к одной форме *до* горячего цикла.

## Правило 5 — держи числа в диапазоне Smi (или иди в типизированные)

Малое целое (Smi, ±2^31 на 64-битах) живёт inline в слоте указателя с арифметикой в одной инструкции; пересеките эту границу хоть раз — и значение станет аллоцированным в куче **HeapNumber**, а цикл TurboFan, предполагавший Smi, деоптится с `reason: Smi`. Ограничьте счётчики и id диапазоном Smi или сразу заложитесь на double через `Float64Array`, чтобы оптимизатор специализировался на double и никогда не откатывался. (См. [Smi и double](/learn/js-engine/02-smis-and-doubles) о границе Smi/HeapNumber.)

## Правило 6 — держи диспетчеризацию стабильной

Точка вызова, вызывающая *одну и ту же* функцию (или одну из немногих стабильных), оптимизируется хорошо. Точка, вызывающая много разных функций — гигантский `switch`, диспетчеризующий к десяткам обработчиков, или метод, вызываемый полиморфно по многим подклассам, — становится megamorphic и теряет инлайнинг. Предпочитайте малый стабильный набор callee на горячих путях; `Map` типизированных обработчиков, построенный однажды, лучше пересоздания замыканий на каждый вызов.

<NumbersCard
  title="Цена нарушения каждого правила"
  data-lesson-visual
  stats={[
    { label: "Monomorphic загрузка свойства", value: "~1 цикл" },
    { label: "Megamorphic общая загрузка", value: "~в 10-50x медленнее" },
    { label: "Доступ в словарном режиме (после delete)", value: "~50-100 циклов, навсегда" },
    { label: "Арифметика Smi", value: "1 инструкция, без аллокации" },
    { label: "Арифметика HeapNumber", value: "аллок + разыменование + риск деопта" },
    { label: "Доступ к HOLEY-массиву", value: "+1 проверка дырки на доступ" },
  ]}
/>

<Misconception id="mc-defaults-waste">
  <p><strong>«Всегда присваивать каждое поле (даже `null`) тратит память и медленнее».</strong> Наоборот: один общий hidden class — каждый экземпляр переиспользует кэшированные смещения, и точка доступа остаётся monomorphic. Несколько null-слотов ничего не стоят рядом с megamorphic-поисками от условных полей.</p>
</Misconception>

<Quiz
  id="q-delete-vs-null"
  lessonSlug="02-monomorphism-discipline"
  lang="ru"
  question="Нужно убрать поле из горячего долгоживущего объекта. Что делаете?"
  choices={[
    { label: "Присваиваете null — hidden class сохраняется, объект остаётся в режиме быстрых свойств", correct: true },
    { label: "delete obj.field — он немедленно освобождает память", misconception: "delete вытесняет объект в словарный режим навсегда; каждый доступ затем идёт медленным общим путём. Экономия памяти крошечная рядом с этим." },
    { label: "Присвоить undefined, что V8 трактует как удаление", misconception: "Присвоение undefined сохраняет слот и форму (это нормально), но не «удаляет» свойство — и дело не в этом; правило просто избегать delete." },
    { label: "Пересобрать объект без поля через spread", misconception: "Это аллоцирует новый объект с другой формой на горячем пути — больше churn, не меньше. Присвойте null и держите одну форму." },
  ]}
/>

<Quiz
  id="q-array-kind"
  lessonSlug="02-monomorphism-discipline"
  lang="ru"
  question="Горячий числовой массив стартует как PACKED_SMI. Какая одна операция деградирует его наиболее устойчиво?"
  choices={[
    { label: "Push дробного значения вроде 1.5 (расширяет до PACKED_DOUBLE) или объекта (расширяет до PACKED_ELEMENTS)", correct: true },
    { label: "Чтение arr[0] миллион раз", misconception: "Чтения никогда не меняют element kind. Переход даёт только запись, вносящая более широкий тип или дырку." },
    { label: "Вызов arr.length", misconception: "Чтение length бесплатно и не влияет на element kind." },
    { label: "Итерация через for...of", misconception: "Итерация читает элементы; она не расширяет kind. Element kind расширяется только при записи более широкого типа или при создании дырки." },
  ]}
/>

<DragOrder
  id="do-divergence-severity"
  pieceSlug="02-monomorphism-discipline"
  lang="ru"
  prompt="Расставьте эти привычки работы с объектами от самой дружелюбной к мономорфизму к самой разрушительной."
  items={[
    "Литерал объекта со всеми полями в фиксированном порядке",
    "Конструктор класса, присваивающий каждое поле безусловно",
    "Добавление некоторых полей условно после создания",
    "delete на свойстве (вынуждает постоянный словарный режим)",
  ]}
/>

<Inset kind="practice" lang="ru">
Практический аудит: возьмите одну горячую фабрику или конструктор и прогоните её сборку под `--trace-ic` (или смоделируйте как песочница в этом юните — посчитайте число различных сигнатур `Object.keys`). Если вход с фиксированной схемой даёт более одной сигнатуры формы, условное или позднее поле протекает вторым hidden class. Исправьте, инициализируя каждое поле безусловно, затем перетрассируйте и подтвердите, что точка доступа сообщает monomorphic.
</Inset>

</Explanation>

<KeyTakeaway>Мономорфизм — это одна дисциплина: строй объекты со всеми полями в фиксированном порядке (никогда не delete — присваивай null), держи каждый массив в одном element kind, корми горячие функции аргументами согласованной формы, держи числа в диапазоне Smi или иди в типизированные, и держи диспетчеризацию к нескольким стабильным callee. Каждое правило не даёт сломаться конкретной спекуляции V8.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="monomorphism-discipline-retrieval"
  lang="ru"
  questions={[
    { q: "Почему инициализация каждого поля безусловно (пусть и в null) лучше условного добавления полей?", a: "V8 представляет каждый объект как указатель на hidden class плюс слоты свойств, и hidden class кодирует точный набор и ПОРЯДОК полей. Условные или поздние добавления полей отправляют разные экземпляры по разным ветвям дерева переходов, давая несколько различных hidden class для логически одной и той же записи. Горячая точка доступа тогда видит несколько форм и деградирует из monomorphic в polymorphic и megamorphic, где каждый доступ откатывается к общему поиску в хеш-таблице (в 10-50 раз медленнее). Инициализация всех полей в одном фиксированном порядке — в конструкторе класса или литерале объекта — делает так, что каждый экземпляр разделяет один листовой hidden class, поэтому IC остаётся monomorphic, а TurboFan может вшить смещение свойства в скомпилированный код. Несколько null-слотов бесплатны по сравнению с megamorphic-ценой, которую они предотвращают." },
    { q: "Разберите лестницу element kind и привычки, держащие массив быстрым.", a: "Element kind расширяется в одну сторону: PACKED_SMI_ELEMENTS (малые int, без дырок — самый быстрый) → PACKED_DOUBLE_ELEMENTS → PACKED_ELEMENTS (любой тип/объекты) → варианты HOLEY_*, добавляющие проверку дырки на каждом доступе. После расширения сузить обратно нельзя. Чтобы остаться на быстром kind: создавайте через [] и растите через push (никогда new Array(n) или присваивание за длину — оба создают HOLEY); используйте Array.from({length}, fn) для PACKED-массива заданного размера; не смешивайте Smi, double и object в одном горячем массиве (одно 1.5 или объект расширяет его навсегда); а для числовой работы используйте типизированный массив (Float64Array/Int32Array), который вообще обходит отслеживание element kind. Разрыв в скорости между PACKED_SMI и смешанным HOLEY-массивом на узком цикле — в 2-3 раза и более." },
    { q: "Что делает точку вызова megamorphic и как держать диспетчеризацию стабильной?", a: "Inline cache в точке вызова или доступа к свойству записывает формы/функции, которые видит. До одной — monomorphic, до четырёх — polymorphic, при пяти и более различных записях становится megamorphic — V8 сдаётся с пер-точечным кэшем и использует общий generic-стаб, теряя инлайнинг и специализацию. На горячих путях это случается, когда функцию вызывают со многими формами объектов или точка диспетчеризации вызывает десятки разных функций (жирный switch, метод по многим подклассам). Держите диспетчеризацию стабильной, нормализуя входы к одной форме до горячего цикла, разбивая полиморфную функцию на варианты под конкретные формы и держа малый фиксированный набор callee (заранее построенный Map обработчиков, а не пересоздаваемые на вызов замыкания). Цель — чтобы каждая горячая точка видела одну форму и одного (или несколько) callee." },
  ]}
/>

<Recap lang="ru">
Дисциплина мономорфизма — это обратная сторона каждого провала в этом треке: вместо того чтобы удивлять спекулятивный оптимизатор V8, вы держите его ставки выигрышными. Шесть правил, каждое привязано к механизму. Стройте объекты со всеми полями в фиксированном порядке через class или фабрику, чтобы каждый экземпляр разделял один hidden class (фикс для отсутствующих данных — `null`, а не пропуск). Никогда не `delete` — он вынуждает постоянный словарный режим; присваивайте `null`. Держите каждый горячий массив в одном element kind (предпочитайте `[]` + `push` и `Array.from` вместо `new Array(n)`; никогда не смешивайте Smi/double/object и не создавайте дырки). Кормите горячие функции аргументами согласованной формы, чтобы их IC оставались monomorphic. Держите числовые значения в диапазоне Smi или заложитесь на double через типизированный массив, чтобы избежать деоптов HeapNumber. И держите диспетчеризацию к малому стабильному набору callee, чтобы точки вызова не стали megamorphic. Ни одно из этого не микротрюк; вместе это то, как пишут JavaScript, остающийся на быстром пути по умолчанию.
</Recap>
