---
concepts:
- react-cache-invalidation
deepensInto: []
estMin: 19
lang: ru
lessonType: topic
level: senior
order: 4
prereqs: []
slug: "04-cache-invalidation-patterns"
sources:
- https://tanstack.com/query/latest/docs/framework/react/guides/query-invalidation
- https://tanstack.com/query/latest/docs/framework/react/guides/optimistic-updates
- https://tanstack.com/query/latest/docs/framework/react/guides/infinite-queries
spiral: []
status: ready
summary: 'После записи кеш лжёт, пока вы его не примирите. Инвалидация покупает правду сервера ценой запроса; setQueryData мгновенен, но дублирует серверную логику; оптимистичным обновлениям нужны cancel + snapshot + rollback. Плюс infinite queries и websocket-синхронизация.'
title: 'Паттерны инвалидации кеша: invalidate против write-through, оптимистичный rollback и live-синхронизация'
track: "react"
unit: "03-data-fetching"
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";
import SequenceDiagram from "~/components/diagram/SequenceDiagram.astro";
import Infographic from "~/components/diagram/Infographic.astro";
import requestCost from "./infographics/request-cost.svg?raw";

<Hook>
Два тикета с разницей в две недели, одно семейство первопричин. Тикет первый: «Создал счёт, получил тост об успехе, попал в список счетов — а моего счёта там нет. Обновил страницу — появился. Ваше приложение теряет данные». Оно не теряет данные: у запроса списка ради производительности стоял `staleTime: 5 минут`, мутация никогда не инвалидировала `['invoices']`, и кеш честно отдавал свою пятиминутную правду. Тикет второй пришёл со стороны инфраструктуры: p99-латентность API скачет каждый день после обеда. Трейс показал, что отдельные пользователи генерируют 40 запросов за секунду — «фикс» первого тикета добавил `invalidateQueries()` *без ключа* в `onSuccess` каждой мутации, и сохранение одного чекбокса перезапрашивало каждый активный запрос на дашборде с сорока виджетами. Шторм перезапросов, нанесённый себе самим. Это урок, где встречаются две знаменитые трудные вещи: после каждой записи ваш кеш неверен, пока вы его не примирите, — а примирение — это ручка между «перезапросить всё истинное» и «запатчить ровно то, что изменилось», где один конец стоит запросов, а другой — корректности.
</Hook>

<Crux>После записи кеш лжёт. invalidate перезапрашивает правду сервера; setQueryData мгновенен, но дублирует сервер. Оптимизму нужен rollback.</Crux>

<Explanation>

## invalidate против setQueryData: правда перезапросом или правда репликацией

`invalidateQueries({ queryKey: ['invoices'] })` — консервативный ход: сматчить затронутые ключи по префиксу, пометить устаревшими, перезапросить активные. Вы получаете *правду сервера* — порядок сортировки, вычисленные суммы, строки, добавленные другими пользователями, — ценой одного лишнего круга и короткого окна, когда старые данные ещё на экране. `setQueryData(['invoices'], updater)` — это **write-through**: синхронно запатчить закешированное значение, ноль запросов, мгновенный UI. Его цена коварнее: ваш updater обязан реплицировать серверную логику. Вставить новый счёт — на какую позицию? Список отсортирован по номеру, который назначает сервер. Подходит ли он под текущий фильтр? Не проставил ли сервер `dueDate`, не нормализовал ли валюту, не зацепил ли побочным эффектом другую строку? Каждое расхождение — *молчаливая* ложь в кеше. Сеньорский дефолт: **write-through ответом сервера** (`onSuccess: (created) => setQueryData(...)`, вписывающий созданный объект), либо write-through, *а затем* инвалидация — мгновенный UI сейчас, правда примиряется в фоне. Чистый `setQueryData` приберегите для кешей, чью форму вы контролируете полностью, и никогда не инвалидируйте без ключа: ограничивайте областью реально изменившегося семейства ресурсов, иначе дашборд с 40 виджетами превращает каждый чекбокс в шторм запросов.

## Оптимистичные обновления: трёхшаговый контракт с откатом

Для взаимодействий, где даже один круг ощущается поломкой — лайки, переключатели, drag-перестановка, — обновляйте кеш *до* подтверждения сервера. У контракта три обязательных шага, все в `onMutate`:

```jsx
const mutation = useMutation({
  mutationFn: toggleTodo,
  onMutate: async (todo) => {
    // 1. Cancel: летящий refetch перезаписал бы нашу оптимистичную запись
    await queryClient.cancelQueries({ queryKey: ["todos"] });
    // 2. Snapshot: к чему откатываемся, если сервер скажет «нет»
    const previous = queryClient.getQueryData(["todos"]);
    // 3. Оптимистичный write-through
    queryClient.setQueryData(["todos"], (old) =>
      old.map((t) => (t.id === todo.id ? { ...t, done: !t.done } : t))
    );
    return { previous };                      // → context
  },
  onError: (_err, _todo, context) =>
    queryClient.setQueryData(["todos"], context.previous), // откат
  onSettled: () =>
    queryClient.invalidateQueries({ queryKey: ["todos"] }), // примирение
});
```

Каждый шаг существует из-за конкретного отказа. Пропустите **cancel** — и refetch, начавшийся до вашего клика, может приземлиться *после* оптимистичной записи и откатить её на лету: чекбокс мигает «выключено», потом снова «включено», когда мутация завершилась; пользователи описывают это как «приложение со мной боролось». Пропустите **snapshot/rollback** — и отказ сервера оставит UI навсегда утверждающим ложь: лайк, которого не было. Пропустите **примирение в onSettled** — и ваша оптимистичная догадка (даже верная) разъедется с полями, вычисляемыми сервером. Трейдофф честный: оптимистичный UI превращает p50-латентность в ноль *воспринимаемой*, но покупает путь ошибки, который надо спроектировать — откат плюс видимое «не сохранилось», — и плохо компонуется со многими одновременными мутациями одной сущности (снапшот мутации B может захватить неподтверждённую догадку мутации A; документация TanStack Query отправляет тяжёлые случаи в одиночные мутации или per-mutation variables).

<SequenceDiagram
  label="Таймлайн отказа оптимистичной мутации: cancel и снапшот предшествуют оптимистичной записи, отказ сервера запускает откат к снапшоту, а инвалидация в onSettled перезапрашивает серверную истину"
  hue="var(--d-frontend)"
  actors={["mutate()", "Кеш запросов", "Сервер"]}
  messages={[
    { from: "mutate()", to: "Кеш запросов", label: "cancelQueries(key)" },
    { from: "mutate()", to: "Кеш запросов", label: "снапшот previous" },
    { from: "mutate()", to: "Кеш запросов", label: "оптимистичная запись" },
    { from: "mutate()", to: "Сервер", label: "POST toggle" },
    { from: "Сервер", to: "mutate()", label: "отказ (500)" },
    { from: "mutate()", to: "Кеш запросов", label: "откат к снапшоту" },
    { from: "mutate()", to: "Кеш запросов", label: "onSettled: invalidate" },
    { from: "Кеш запросов", to: "Сервер", label: "refetch → истина" },
  ]}
  caption="Три шага onMutate в их реальном порядке: cancel убивает летящий refetch, который затоптал бы оптимистичное значение, снапшот делает отказ обратимым, запись идёт последней. При успехе отличается только середина — сообщение отката не срабатывает, но onSettled всё равно примиряет догадку с истиной, вычисленной сервером."
/>

<Quiz
  id="04-cache-invalidation-patterns-quiz-1"
  lessonSlug="04-cache-invalidation-patterns"
  lang="ru"
  question="Оптимистичный переключатель реализует snapshot и rollback, но пропускает cancelQueries в onMutate. QA сообщает: чекбокс иногда сам выключается через мгновение после клика, а затем снова включается. Что произошло?"
  choices={[
    { label: "Фоновый refetch, уже летевший в момент клика, зарезолвился после оптимистичной записи и перезаписал кеш серверными данными до переключения; cancelQueries существует ровно затем, чтобы убить летящие запросы, чьи ответы растоптали бы оптимистичное значение", correct: true },
    { label: "Сработал откат, потому что сервер вернул ошибку", misconception: "Откат по ошибке выключает чекбокс один раз и оставляет так — он не включается снова через мгновение. Сигнатура «мигнул и восстановился» означает, что кеш перезаписал устаревший сетевой ответ, а затем поправило примирение, когда мутация завершилась и refetch принёс свежую правду." },
    { label: "setQueryData асинхронен, поэтому запись приземлилась поздно", misconception: "setQueryData применяется синхронно — подписчики видят оптимистичное значение в том же тике. Отложенный откат — это не поздняя запись, а более старый сетевой ответ, пришедший позже и заменивший запись; от этого спасает только отмена летящих запросов." },
    { label: "Снапшот случайно захватил оптимистичное значение вместо предыдущего", misconception: "Баги порядка снапшота (снимок после записи) ломают путь отката — это видно только при ошибке сервера. Здесь мигание происходит и на успешных мутациях, что указывает на путь чтения: неотменённый refetch гонится с оптимистичной записью." },
  ]}
/>

## За пределами одного списка: зависимости, страницы и приходящая правда

Освоив паттерны одного списка, спросите себя: где граф данных усложняется? Зависимые запросы, постраничность и real-time push каждый ломают одно из допущений выше — и каждый несёт свою цену отказа, которая не проявляется, пока не вырастет трафик или число страниц.

**Зависимые запросы** цепляются через `enabled`: `useQuery({ queryKey: ['projects', user?.id], enabled: !!user })` держит второй запрос выключенным, пока не зарезолвился первый. Это осознанная версия водопада из прошлых уроков — нормально, когда зависимость реальна, но каждая цепочка `enabled` — это сериализованная латентность, которую вы выбрали сами, так что аудируйте цепочки, как аудируете последовательности `await`. **Infinite queries** делают инвалидацию дорогой неожиданным образом: инвалидированный `useInfiniteQuery` перезапрашивает *каждую загруженную страницу последовательно*, чтобы курсоры остались согласованными — пользователь на глубине 10 страниц запускает 10 серийных запросов на каждую инвалидацию. Смягчения: ограничить хранимые страницы через `maxPages`, предпочесть точечный `setQueryData`-патч страницы для правок одной строки и использовать `placeholderData: keepPreviousData` на обычных постраничных запросах, чтобы листание показывало старую страницу вместо спиннера. **Синхронизация по websocket** — эндшпиль многопользовательского устаревания, и сеньорский паттерн контринтуитивен: на push-событие *не* пишите payload в кеш — вызовите `invalidateQueries` с ключом сущности из события. Присланные payload приходят не по порядку, могут не содержать вычисляемых сервером полей и дублируют вашу write-through-логику; события как сигналы «что-то изменилось, спроси сервер» сохраняют один источник правды, а при высокой частоте инвалидации дебаунсятся по ключу — 50 событий в секунду схлопываются в считаные перезапросы, — ровно тот дроссель шторма, которого не хватало тикету два.

<Infographic
  svg={requestCost}
  label="Горизонтальная столбчатая диаграмма: сколько перезапросов запускает одно действие — write-through через setQueryData 0, точечная инвалидация 1, инвалидация бесконечного списка из 10 страниц 10, инвалидация без ключа на дашборде из 40 виджетов 40 и наивная websocket-синхронизация при 50 событиях в секунду 50."
  caption="Та же ручка в запросах: write-through бесплатен, но реплицирует серверную логику, точечная инвалидация стоит ровно один запрос, а каждый неверно ограниченный паттерн — глубокий бесконечный список, invalidate без ключа, прямые websocket-записи — взрывается штормом перезапросов. Этот разрыв и есть причина брать самый узкий ключ, покрывающий изменившееся."
  hue="var(--d-frontend)"
/>

<Quiz
  id="04-cache-invalidation-patterns-quiz-2"
  lessonSlug="04-cache-invalidation-patterns"
  lang="ru"
  question="Realtime-доска получает websocket-события об изменениях карточек и пишет каждый payload прямо в кеш через setQueryData. Карточки то и дело показывают устаревшие заголовки и потерянный вычисляемый бейдж &quot;blocked&quot;. Каков сеньорский фикс?"
  choices={[
    { label: "Относиться к событиям как к сигналам, а не данным: на каждое событие инвалидировать queryKey сущности (с дебаунсом по ключу при высокой частоте), чтобы кеш перезапросил правду сервера — присланные payload приходят не по порядку и не содержат вычисляемых сервером полей, так что их прямая запись возвращает обе проблемы", correct: true },
    { label: "Буферизовать события на клиенте и сортировать по таймстемпу перед применением", misconception: "Клиентский порядок не сделать надёжным — часы расходятся, доставки падают и ретраятся, а в payload всё равно нет полей, которые вычисляет только сервер (бейдж blocked выводится из других карточек). Вы пересобирали бы упорядочивание репликации, которое паттерн «refetch по сигналу» обходит целиком." },
    { label: "Увеличить staleTime, чтобы websocket-записям доверяли дольше", misconception: "staleTime управляет тем, когда триггеры вроде маунта и фокуса перезапрашивают; он ничего не делает с порчеными записями, которые вы внесли сами. Дольше доверять плохим данным — хуже симптом: бейдж так и не появится, пока что-то ещё не вынудит перезапрос." },
    { label: "Заменить websocket поллингом каждые 2 секунды", misconception: "Поллинг такой частоты стоит больше запросов, чем дебаунсная инвалидация, и всё равно даёт до 2 с устаревания. Push-канал — эффективная часть; баг только в использовании payload как правды вместо нотификаций об изменениях, привязанных к инвалидации." },
  ]}
/>

</Explanation>

<FlowDiagram
  label="Мутация примиряет кеш по одному из трёх путей: оптимистичная запись с откатом, write-through ответом сервера или инвалидация с перезапросом"
  hue="var(--d-frontend)"
  perRow={3}
  nodes={[
    { id: "mutate", label: "mutate()", sub: "действие пользователя" },
    { id: "onmutate", label: "onMutate", sub: "cancel + snapshot + запись" },
    { id: "server", label: "Сервер", sub: "принял / отверг" },
    { id: "rollback", label: "Откат", sub: "вернуть снапшот" },
    { id: "settle", label: "onSettled", sub: "инвалидировать ключ" },
    { id: "truth", label: "Перезапрос", sub: "правда сервера в кеше" },
  ]}
  edges={[
    { from: "mutate", to: "onmutate", label: "оптимистичный путь" },
    { from: "onmutate", to: "server", label: "запрос" },
    { from: "server", to: "rollback", label: "ошибка" },
    { from: "server", to: "settle", label: "успех или ошибка" },
    { from: "settle", to: "truth", label: "примирение" },
  ]}
  caption="Cancel убивает летящие перезапросы, которые растоптали бы оптимистичное значение; снапшот делает отказ обратимым; инвалидация в onSettled примиряет даже верные догадки с вычисленной сервером правдой. Ключи инвалидации держите узкими — invalidate без ключа на странице с 40 виджетами — самострельный шторм перезапросов."
/>

<KeyTakeaway>Каждая запись делает кеш неверным, пока вы его не примирите, и паттерны образуют ручку. Инвалидация: ограниченный префиксный перезапрос, правда сервера, один лишний запрос — никогда без ключа. Write-through через setQueryData: мгновенно и бесплатно, но updater реплицирует серверную логику и молча разъезжается — предпочитайте патч ответом сервера. Оптимизм: отменить летящие запросы, снять снапшот, записать, откатить при ошибке, инвалидировать при завершении — пропуск любого шага даёт мигание, застрявшую ложь или дрейф. Infinite queries при инвалидации перезапрашивают все страницы; websocket-события должны запускать дебаунсную инвалидацию, а не прямые записи.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="04-cache-invalidation-patterns-retrieval"
  lang="ru"
  questions={[
    { q: "Разложите ручку примирения после мутации: три стратегии, что каждая стоит, чем рискует и какова сигнатура продакшен-бага при ошибке в каждой.", a: "Стратегия один — инвалидация: пометить совпавшие ключи устаревшими и перезапросить активные. Цена: один лишний круг и короткое окно устаревания. Риски: недоинвалидация — пропустите зависимый ключ и получите классику «создал элемент, тост об успехе, в списке нет до F5» (список со staleTime отдаёт старую правду); переинвалидация — invalidate без ключа или слишком широкий на оживлённой странице перезапрашивает десятки запросов на мутацию — самострельный шторм, проявляющийся послеобеденными скачками p99. Стратегия два — write-through через setQueryData: синхронный патч кеша, ноль запросов, мгновенный UI. Риск: updater реплицирует серверную логику — позицию вставки при серверной сортировке, принадлежность фильтру, вычисляемые поля вроде нормализованной валюты или производных бейджей, — и каждое расхождение лжёт молча, пока что-то ещё не перезапросит. Сигнатура: данные тонко неверны (строка не на месте, потеряно вычисляемое поле), а не отсутствуют. Практика: патчить объектом из ответа сервера, а не клиентской догадкой, либо write-through и следом инвалидация — мгновенный UI плюс примирение. Стратегия три — оптимистичное обновление: запись до ответа сервера, воспринимаемая латентность ноль. Обязательные шаги: await cancelQueries (иначе летящий refetch зарезолвится после записи и откатит её — мигание «чекбокс со мной боролся»), снапшот предыдущих данных, setQueryData догадки, откат к снапшоту в onError (иначе отказ оставит постоянную ложь в UI), инвалидация в onSettled (иначе даже верные догадки разъедутся с вычисленной сервером правдой). С одновременными мутациями одной сущности компонуется плохо — снапшоты могут захватывать чужие неподтверждённые догадки, — тяжёлые случаи идут через одиночные (single-flight) мутации." },
    { q: "Как меняется цена инвалидации для infinite queries и почему «инвалидируй по событию», а не «запиши payload» — стандартный паттерн websocket-синхронизации кеша?", a: "Infinite queries хранят массив страниц за одним ключом, и инвалидация перезапрашивает каждую загруженную страницу последовательно, чтобы курсоры остались согласованными — пользователь на глубине 10 страниц платит 10 серийных кругов за инвалидацию, и паттерн мутаций, походя инвалидирующий бесконечный список, превращает правку одной строки в многосекундный каскад перезапросов. Смягчения: maxPages для ограничения хранимых страниц, точечный setQueryData-патч затронутой строки для правок одной сущности и keepPreviousData через placeholderData на обычных постраничных запросах, чтобы листание показывало предыдущую страницу вместо спиннера. В websocket-синхронизации соблазн — считать каждый присланный payload правдой и записывать его через setQueryData. Это ломается трижды: порядок — события приходят не по порядку при реконнектах и ретраях, и старый payload может перезаписать более новое состояние; полнота — payload обычно не содержит вычисляемых сервером полей (бейдж «blocked», выводимый из других сущностей, нормализованные значения), и записанные напрямую записи тонко неполны; дублирование — каждому типу сущности нужна рукописная merge-логика, повторяющая серверную. Стандартный паттерн переворачивает это: событие — нотификация об изменении, а не данные; по событию — invalidateQueries ключа сущности, и пусть перезапрос принесёт полную, упорядоченную, вычисленную сервером правду. Источник правды один — API. При высокой частоте дебаунсите инвалидации по ключу — 50 событий в секунду схлопываются в считаные перезапросы, — а для по-настоящему высокочастотных числовых потоков (тикеры, курсоры) обходите query-кеш и кормите выделенный realtime-store: кеш запрос-ответ — неправильный инструмент для субсекундного push." },
  ]}
/>

<Recap lang="ru">
Мутация делает каждое закешированное чтение этих данных неверным, и этот урок — каталог стратегий примирения. Инвалидация — правда перезапросом: `invalidateQueries` матчит ограниченный ключ по префиксу, помечает записи устаревшими, перезапрашивает активные — её отказы это недоинвалидация (созданный счёт отсутствует в пятиминутно-свежем списке до F5) и переинвалидация (invalidate без ключа превращает один чекбокс в шторм из 40 запросов и послеобеденные скачки p99). Write-through `setQueryData` — правда репликацией: мгновенно и без запросов, но updater обязан зеркалить серверную логику — позицию сортировки, принадлежность фильтру, вычисляемые поля — и каждое расхождение лжёт молча, поэтому предпочитайте патч реальным ответом сервера или инвалидацию следом за записью. Оптимистичные обновления переносят запись до ответа сервера и являются трёхшаговым контрактом в `onMutate`: await `cancelQueries`, чтобы летящий refetch не растоптал оптимистичное значение (фирменное мигание), снапшот для отката в `onError`, чтобы отказ не оставил постоянную ложь в UI, и инвалидация в `onSettled`, чтобы даже верные догадки примирились с вычисленной сервером правдой. Зависимые запросы через `enabled` — выбранные вами водопады, аудируйте их как цепочки `await`. Infinite queries при инвалидации перезапрашивают все загруженные страницы последовательно — ограничивайте страницы и патчите строки на месте. Websocket-события — сигналы, а не данные: инвалидируйте ключ сущности (с дебаунсом под нагрузкой) вместо прямой записи payload, потому что присланное приходит неупорядоченным и неполным. Ручка идёт от «перезапросить всё» до «запатчить ровно это» — запросы на одном конце, риск корректности на другом. Теперь, когда пишешь onSuccess мутации, берёшь самый узкий ключ, покрывающий реально изменившееся, — а когда видишь мигание или устаревший список после сохранения, знаешь, какой из трёх шагов проверить первым.
</Recap>
