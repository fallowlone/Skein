---
concepts:
- react-query-libraries
deepensInto: []
estMin: 18
lang: ru
lessonType: topic
level: middle
order: 3
prereqs: []
slug: "03-query-libraries"
sources:
- https://tanstack.com/query/latest/docs/framework/react/overview
- https://tanstack.com/query/latest/docs/framework/react/guides/important-defaults
- https://tanstack.com/query/latest/docs/framework/react/guides/caching
spiral: []
status: ready
summary: 'TanStack Query относится к серверным данным как к кешу, а не состоянию: queryKey — идентичность кеша (дедупликация), staleTime и gcTime — два независимых таймера (перезапрос и память), refetch по фокусу держит данные живыми, а мутации с invalidateQueries замыкают цикл записи.'
title: 'Query-библиотеки: идентичность queryKey, два таймера и что TanStack Query реально экономит'
track: "react"
unit: "03-data-fetching"
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import SequenceDiagram from "~/components/diagram/SequenceDiagram.astro";
import Infographic from "~/components/diagram/Infographic.astro";
import twoClocks from "./infographics/two-clocks.svg?raw";

<Hook>
Первой эскалировала команда бэкенда: «/api/me — это 30% нашего трафика. Почему ваша SPA зовёт его шесть раз на навигацию?» Фронтенд-аудит был унизителен знакомым образом. Шапка запрашивала текущего пользователя ради аватара. Сайдбар — ради прав. Три виджета — ради персонализации. У каждого компонента — свой прилежный `useEffect`, свой флаг `loading`, своя копия ответа в своём `useState`: шесть запросов на один ресурс, ни один не разделён, ни один не обновляется после маунта. Пользователь, сменивший имя в настройках, видел старое имя в шапке до нажатия F5. Первым порывом команды был самописный контекст-плюс-кеш; через два спринта в нём были утечка памяти, отсутствие ретраев и гонка, которой не было даже у исходных эффектов. То, что они плохо пересобирали, — это кеш серверного состояния: дедупликация запросов, учёт устаревания, фоновый перезапрос, сборка мусора. Вот настоящий продукт TanStack Query и его собратьев: не «удобный fetch», а признание, что серверные данные — это *кеш* с кешевыми проблемами, у которых сорок лет известных решений.
</Hook>

<Crux>queryKey — идентичность кеша: один ключ — одна запись. Два таймера: staleTime решает перезапрос, gcTime — память. Мутации инвалидируют.</Crux>

<Explanation>

## queryKey — идентичность кеша, а не подпись

Сдвиг в голове: серверные данные — не ваше состояние, а *кеш чужого состояния*. TanStack Query делает это явным. Каждый запрос идентифицируется своим `queryKey` — массивом, хешируемым структурно (порядок ключей объекта не важен, порядок массива важен): `['todos', { status, page }]`. Ключ — это адрес в кеше. Каждый компонент, зовущий `useQuery` с тем же ключом, читает *ту же запись кеша* — шесть компонентов, просящих `['me']`, дают один запрос, дедуплицированный в полёте, и один общий ответ. Отсюда дисциплина: **каждая переменная, которую читает queryFn, обязана быть в ключе** — ровно как массив зависимостей useEffect. Забудьте `page` в ключе — и страница 2 перезапишет страницу 1 по тому же адресу; симптом — «мелькают чужие данные», диагноз почти всегда — ключ, недоописывающий запрос.

```jsx
function Todos({ status, page }) {
  const { data, isPending, isError, error } = useQuery({
    queryKey: ["todos", { status, page }],   // идентичность = все входы
    queryFn: ({ signal }) =>
      fetchTodos({ status, page, signal }),  // abort подключён бесплатно
    staleTime: 30_000,                        // свежо 30 секунд
  });
  // isPending → первая загрузка, кеша ещё нет
  // data может быть и показана, и устаревшей: отдана мгновенно, перезапрошена сзади
}

function AddTodo() {
  const queryClient = useQueryClient();
  const mutation = useMutation({
    mutationFn: createTodo,
    onSuccess: () =>
      queryClient.invalidateQueries({ queryKey: ["todos"] }), // по префиксу
  });
  return <button onClick={() => mutation.mutate({ title: "ship" })}>Add</button>;
}
```

## Два таймера: staleTime решает перезапрос, gcTime решает память

Каждой записью кеша управляют два независимых таймера, и их смешивание — недоразумение номер один вокруг TanStack Query. **`staleTime`** отвечает: «достаточно ли эти данные свежи, чтобы отдать их, не спрашивая сервер снова?» Пока запись свежа, любой маунт или фокус окна отдаёт её из кеша, точка — ноль запросов. Когда запись устарела, данные всё равно отдаются мгновенно, но фоновый перезапрос стреляет на следующем *триггере*: монтируется новый компонент, окно получает фокус, сеть переподключается. Это stale-while-revalidate: пользователь всегда видит данные сразу; свежесть тихо приходит следом. **`gcTime`** отвечает на другой вопрос: «после размонтирования последнего компонента, использующего запись, сколько держать её в памяти?» *Активная* запись не собирается никогда; неактивная живёт `gcTime` (по умолчанию 5 минут), чтобы возврат назад в этом окне был мгновенным кеш-хитом, а не спиннером.

Дефолты сознательно агрессивны: `staleTime: 0` — всё устаревает немедленно, поэтому каждый маунт и каждый фокус окна запускают фоновый перезапрос; плюс упавшие запросы ретраятся 3 раза с экспоненциальным бэкоффом. Команды открывают это как «почему наш API долбят?» — и ответ не в глобальном отключении `refetchOnWindowFocus` (перезапрос по фокусу — то, что делает дашборд честным, когда пользователь вернулся с обеда), а в честном `staleTime` на ресурс: 30 секунд для списка задач, 10 минут для списка стран, 0 для котировки. Неверная настройка таймеров ломается по-разному: слишком большой `staleTime` отдаёт неправду; слишком большой `gcTime` держит мёртвые данные в памяти; а `gcTime` *меньше* `staleTime` тихо обессмысливает свежесть — запись соберут раньше, чем вернувшийся посетитель ею воспользуется.

<Infographic
  svg={twoClocks}
  label="Сравнение staleTime и gcTime бок о бок: staleTime управляет перезапросом (дефолт 0, запускается триггером, слишком большой отдаёт неправду, настраивается на ресурс от 0 до 10 минут), а gcTime управляет памятью (дефолт 5 минут, запускается размонтажом, слишком большой держит мёртвые данные и бессмыслен, если меньше staleTime)."
  caption="staleTime отвечает за свежесть (когда перезапрашивать); gcTime — за память (когда вытеснять). Они срабатывают на разных событиях — возраст загрузки против времени с размонтирования, — поэтому ломаются по-разному и настраиваются раздельно."
  hue="var(--d-infra)"
/>

<Quiz
  id="03-query-libraries-quiz-1"
  lessonSlug="03-query-libraries"
  lang="ru"
  question="У запроса staleTime: 60_000 и gcTime: 300_000. Компонент размонтируется через 10 секунд; пользователь возвращается через 40 секунд. Что происходит при ремаунте?"
  choices={[
    { label: "Мгновенный кеш-хит и ни одного запроса: запись пережила память (40 с неактивности < gcTime 5 мин) и всё ещё свежа (50 с жизни < staleTime 60 с), так что кеш её отдаёт и фоновый перезапрос не стреляет", correct: true },
    { label: "Кеш-хит, но фоновый перезапрос стреляет: размонтирование сделало данные устаревшими", misconception: "Размонтирование не трогает свежесть — оно лишь запускает отсчёт gcTime для вытеснения из памяти. Устаревание — это чисто таймер staleTime от момента загрузки данных; при возрасте 50 секунд против staleTime в 60 запись ещё свежа, и ремаунт ничего не запускает." },
    { label: "Полный спиннер загрузки: размонтирование удалило запись из кеша", misconception: "Вытеснение происходит через gcTime (здесь 5 минут) после размонтирования последнего наблюдателя, а не в момент размонтирования. Это окно и есть смысл gcTime — возврат назад внутри него попадает в кеш мгновенно. Лишь после 5 неактивных минут ремаунт начнёт с нуля." },
    { label: "Запрос стреляет, потому что ремаунт всегда ревалидирует серверные данные", misconception: "Маунт — триггер перезапроса только для устаревших записей. Это и контролирует staleTime: пока данные свежи, маунты и фокус окна — чистые кеш-хиты без сети. С дефолтным staleTime 0 каждый маунт правда перезапрашивает — но это выбор конфигурации, а не механизм." },
  ]}
/>

## Мутации замыкают цикл — и настоящий счёт библиотеки

Чтения — половина истории. `useMutation` оборачивает запись, и в `onSuccess` вы примиряете кеш с новой серверной реальностью — проще всего через `invalidateQueries({ queryKey: ['todos'] })`: он матчит по префиксу каждый ключ, начинающийся с `'todos'`, помечает их устаревшими и немедленно перезапрашивает *активные* (неактивные лишь получают флаг и перезапросятся при следующем использовании). Это то, чего у паттерна с эффектами не было никогда: после POST ваши рукописные `useState`-кеши в пяти компонентах все молча неверны, и никакая гигиена эффектов не чинит данные, которые больше не отражают сервер.

Подобьём, что библиотека реально экономит против базы из прошлого урока: **дедупликация** в полёте (шесть подписчиков — один запрос) и общие записи кеша; встроенная **защита от гонок** — ответы сопоставляются со своими запросами, а `queryFn` получает `AbortSignal`, подключённый к отмене запроса; **ретраи** с экспоненциальным бэкоффом; **жизненный цикл** — устаревание, перезапрос по фокусу и реконнекту, сборка мусора; и **путь записи** — инвалидация плюс оптимистичные обновления (следующий урок). Вместе эти пять свойств означают, что каждый компонент, читающий одни данные, остаётся синхронизированным при записях, сбоях сети и возвратах на вкладку без единой строки boilerplate — уберите любое из них, и вы снова пишете его сами. Честные издержки: зависимость в критическом пути (~12 КБ gzip ядро), дисциплина проектирования ключей (ключи — схема вашего кеша; централизуйте их в фабрике, иначе разбросанные ad-hoc-ключи превращают точечную инвалидацию в гадание) и дефолты, которые надо осознанно настраивать. Трейдофф вечный: кеша вы не избегаете — вы выбираете между проверенным и тем, что напишете случайно.

<Quiz
  id="03-query-libraries-quiz-2"
  lessonSlug="03-query-libraries"
  lang="ru"
  question="Список задач постраничный: queryFn запрашивает /api/todos?page=N, но queryKey — просто ['todos']. Пользователь листает со страницы 1 на страницу 2. Что делает кеш и что видит пользователь?"
  choices={[
    { label: "Оба ответа пишут в одну запись кеша — ответ страницы 2 перезаписывает страницу 1 по адресу ['todos'], компоненты на миг показывают данные старой страницы, а возврат назад не восстановит страницу 1 из кеша; ключ обязан включать страницу: идентичность = все входы queryFn", correct: true },
    { label: "TanStack Query заметит разницу URL и сам разветвит запись", misconception: "Библиотека никогда не заглядывает в ваш queryFn и его URL — queryKey единственная идентичность. Всё, что функция читает мимо ключа, для кеша невидимо — ровно как устаревшее замыкание при пропущенной зависимости useEffect. Лечение структурное: ['todos', { page }]." },
    { label: "Второй запрос дедуплицируется с первым, и страница 2 не загрузится", misconception: "Дедупликация касается одновременных запросов одного ключа и склеила бы только пересекающиеся в полёте вызовы — последовательное листание всё равно шлёт настоящий запрос. Поломка не в пропавшем запросе, а в двух разных ресурсах, дерущихся за один адрес кеша." },
    { label: "Ничего не ломается, но теряется возможность префетчить следующую страницу", misconception: "Префетчинг тоже теряется, но главный отказ — целостность данных: один адрес, хранящий «кто последний записал», означает, что читатели ['todos'] показывают не ту страницу после любого перезапроса, фокуса или ремаунта. Недоключованные запросы портят кеш, а не только оптимизации." },
  ]}
/>

</Explanation>

<SequenceDiagram
  label="Stale-while-revalidate во времени: монтирующемуся useQuery мгновенно отдаётся устаревшая запись кеша, кеш перезапрашивает данные в фоне, и компонент перерисовывается, когда приходит свежий ответ"
  hue="var(--d-frontend)"
  actors={["Компонент", "Кеш запросов", "Сервер"]}
  messages={[
    { from: "Компонент", to: "Кеш запросов", label: "useQuery(['todos',2])" },
    { from: "Кеш запросов", to: "Компонент", label: "stale-запись, сразу" },
    { from: "Кеш запросов", to: "Сервер", label: "фоновый refetch" },
    { from: "Сервер", to: "Кеш запросов", label: "свежий ответ" },
    { from: "Кеш запросов", to: "Компонент", label: "ререндер со свежим" },
  ]}
  caption="Свежая запись (возраст меньше staleTime) пропускает три нижних сообщения целиком — отдаётся без запроса. Мутации инвалидируют совпавшие ключи: помечают их устаревшими и перезапрашивают активные; gcTime вытесняет запись только после ухода последнего подписчика. Фокус и реконнект прогоняют ту же проверку свежий/устаревший — поэтому лечение штормов перезапросов — честный staleTime на ресурс, а не отключение refetch по фокусу."
/>

<KeyTakeaway>Query-библиотека — кеш серверного состояния, и queryKey — его идентичность: каждый вход queryFn принадлежит ключу, одинаковый ключ — одна общая дедуплицированная запись. Каждой записью правят два независимых таймера: staleTime решает, отдают ли маунт/фокус/реконнект кеш молча или ревалидируют за показанными данными; gcTime решает, сколько неактивные записи живут ради мгновенного возврата назад. Мутации примиряют кеш через invalidateQueries по префиксу. Вы получаете дедупликацию, закрытые гонки, ретраи с бэкоффом и жизненный цикл кеша — в обмен на дисциплину ключей и осознанные дефолты.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="03-query-libraries-retrieval"
  lang="ru"
  questions={[
    { q: "Объясните staleTime против gcTime точно: на какой вопрос отвечает каждый таймер, каковы дефолты и каким отдельным отказом оборачивается неверная настройка каждого?", a: "staleTime отвечает: сколько после загрузки данные достаточно свежи, чтобы отдавать их без ревалидации? Пока свежи, каждый триггер — маунт компонента, фокус окна, реконнект сети — чистый кеш-хит с нулём запросов. Когда устарели, данные всё равно отдаются мгновенно (UI не ждёт), но следующий триггер запускает фоновый перезапрос: stale-while-revalidate. Дефолт 0 — всё устаревает сразу, каждый маунт и фокус перезапрашивают; это безопасная-по-умолчанию свежесть, и поэтому ненастроенные приложения выглядят так, будто долбят API. gcTime отвечает на ортогональный вопрос: после размонтирования последнего подписанного компонента сколько неактивная запись живёт в памяти? Активные записи не собираются никогда; неактивные живут gcTime (дефолт 5 минут), делая возврат назад в этом окне мгновенным хитом. Отдельные режимы отказа: слишком большой staleTime отдаёт неправду — цена или набор прав, лгущие минутами; ненастроенный staleTime 0 даёт штормы перезапросов и нагрузку на бэкенд; слишком большой gcTime держит мёртвые данные в памяти (важно на больших списках); gcTime короче staleTime тихо обнуляет окно свежести — запись вытеснят раньше, чем вернувшийся посетитель ею воспользуется. Лечение штормов — честный staleTime на ресурс: 0 для котировки, 30 с для задач, 10 мин для списка стран, — а не глобальное отключение refetch по фокусу, который и держит долгоживущие вкладки честными." },
    { q: "Коллега говорит: «TanStack Query — это useEffect с лишними шагами, я и сам напишу fetch». Перечислите по пунктам, что заменяет библиотека, привязав каждый пункт к отказу, который он предотвращает.", a: "На эндпоинт база с эффектом — это ~20 строк состояний loading/error/data плюс защита от гонки ignore-флагом и abort — и это лёгкая часть. Библиотека заменяет: (1) дедупликацию запросов и общий кеш — N компонентов, читающих один queryKey, дают один запрос в полёте и одну общую запись, против N копий в N useState, рассинхронизирующихся после любой записи (аудит шести вызовов /api/me); (2) защиту от гонок — ответы сопоставляются со своими запросами, а queryFn получает AbortSignal, привязанный к отмене, заменяя рукописную пару ignore/abort в каждом эффекте; (3) ретраи — 3 попытки с экспоненциальным бэкоффом по умолчанию, тогда как самописные fetch обычно падают от первого чиха сети; (4) жизненный цикл устаревания — staleTime/gcTime, перезапрос по фокусу и реконнекту, сборка мусора: вся задача «это ещё правда и можно ли освободить», которую ручные кеши (тот контекст-плюс-кеш с утечкой памяти) решают неверно; (5) путь записи — invalidateQueries по префиксу, примиряющий все зависимые чтения после мутации, чего у паттерна с эффектами просто нет: после POST пять скопированных useState молча лгут. Честная цена взамен: ~12 КБ gzip в критическом пути, дисциплина ключей (ключи — схема кеша; централизуйте фабрику, иначе инвалидация — гадание) и агрессивные дефолты (staleTime 0), которые надо настраивать на ресурс. Аргумент не в удобстве: серверные данные — это кеш, и вы либо берёте проверенный, либо случайно пишете непроверенный." },
  ]}
/>

<Recap lang="ru">
Query-библиотеки начинаются с переосмысления: серверные данные — не состояние компонента, а клиентский кеш состояния, которым владеет сервер, — а у кешей известные проблемы с известными решениями. В TanStack Query `queryKey` — идентичность кеша: структурно хешируемый массив, куда обязан попасть каждый вход `queryFn` (недоключованный постраничный запрос позволяет странице 2 перезаписать страницу 1 по одному адресу), и каждый компонент с тем же ключом разделяет одну дедуплицированную запись — шесть читателей `['me']` ради аватара, прав и персонализации схлопываются в один запрос. Каждая запись живёт на двух независимых таймерах: `staleTime` управляет ревалидацией — свежие записи отдают маунты и фокусы из кеша с нулём запросов, устаревшие отдаются мгновенно, пока следующий триггер запускает фоновый перезапрос (stale-while-revalidate); `gcTime` управляет памятью — активные записи не собираются, неактивные живут по умолчанию пять минут, делая возврат назад мгновенным. Дефолты агрессивны (staleTime 0, три ретрая с экспоненциальным бэкоффом, перезапрос по фокусу и реконнекту), поэтому штормы перезапросов лечатся честным staleTime на ресурс, а не отключением фокус-рефетча, который держит долгоживущие вкладки честными. Записи замыкают цикл: `useMutation` плюс `invalidateQueries` матчит затронутые ключи по префиксу, помечает их устаревшими и перезапрашивает активные — шаг примирения, которого у паттерна с эффектами не было. Счёт: ~12 КБ зависимость, дисциплина ключей (централизуйте фабрики) и осознанно настроенные дефолты — против кеша, который вы иначе напишете случайно. Теперь, когда увидишь API под бомбардировкой или данные, лгущие до F5, первый вопрос звучит так: каков staleTime для этого ресурса и входит ли в queryKey каждый вход, который читает fetcher?
</Recap>
