---
concepts:
- react-fetch-effects
deepensInto: []
estMin: 17
lang: ru
lessonType: topic
level: middle
order: 1
prereqs: []
slug: "01-fetching-in-effects"
sources:
- https://react.dev/learn/synchronizing-with-effects
- https://react.dev/reference/react/useEffect
- https://react.dev/learn/you-might-not-need-an-effect
spiral: []
status: ready
summary: 'Запрос в useEffect — честная база: эффект по параметрам, три состояния boilerplate и две структурные ошибки — гонки, потому что ответы приходят не по порядку, и водопады, потому что дерево компонентов сериализует запросы. Каждая библиотека данных чинит именно это.'
title: 'Запросы в эффектах: гонки, водопады и зачем знать базовый вариант'
track: "react"
unit: "03-data-fetching"
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
Баг-репорт пришёл со скринкастом: пользователь набирает «warsaw» в поиске рейсов, правильный список мигает на полсекунды, затем страница молча подменяет его рейсами по запросу «wa» — все города штата Вашингтон. Ни ошибки, ни падения, и воспроизводилось примерно раз из десяти, всегда на офисном Wi-Fi и никогда на оптике у разработчика. Запрос жил в `useEffect` с зависимостью от строки поиска — ровно как в туториале. Чего туториал не показал: запрос «wa» попал в холодный кеш и шёл 900 мс, запрос «warsaw» — в тёплый и вернулся за 120 мс, а `setResults(data)` в эффекте радостно коммитил тот ответ, который пришёл *последним*. Сеть переставила ответы местами, а компонент понятия не имел, на какой вопрос отвечает каждый из них. Лечение заняло четыре строки — флаг `ignore` в cleanup эффекта — и остаток спринта команда искала ту же гонку ещё в одиннадцати компонентах. Запросы в эффектах — не ошибка; это базовый уровень. Но у базы два структурных режима отказа, и сейчас вы встретите оба.
</Hook>

<Crux>Запросы в эффектах гонятся: побеждает последний ответ, а не запрос. Спасают ignore/abort — а дерево компонентов рождает водопады.</Crux>

<Explanation>

## Честная база: эффект, параметр, три состояния

Запрос в эффекте — это синхронизация: «пока компонент показывает запрос *q*, на экране должен быть ответ сервера для *q*». Эффект перезапускается при каждом изменении `query`, и вы вручную пишете три состояния, нужные любому UI данных, — загрузку, ошибку, данные:

```jsx
function FlightSearch({ query }) {
  const [results, setResults] = useState(null);
  const [error, setError] = useState(null);
  const [loading, setLoading] = useState(false);

  useEffect(() => {
    let ignore = false;                    // защита от гонки
    const controller = new AbortController(); // отмена на сети
    setLoading(true);
    setError(null);

    fetch(`/api/flights?q=${encodeURIComponent(query)}`, {
      signal: controller.signal,
    })
      .then((res) => {
        if (!res.ok) throw new Error(`HTTP ${res.status}`);
        return res.json();
      })
      .then((data) => {
        if (!ignore) { setResults(data); setLoading(false); }
      })
      .catch((err) => {
        if (err.name === "AbortError") return; // ожидаемо при cleanup
        if (!ignore) { setError(err); setLoading(false); }
      });

    return () => {            // выполняется перед перезапуском и при размонтировании
      ignore = true;
      controller.abort();
    };
  }, [query]);
  // рендер loading / error / results...
}
```

Это примерно двадцать строк на эндпоинт, и каждая строка заслужила своё место. Форма, которую стоит усвоить: тело эффекта *запускает* запрос, привязанный к одному значению `query`; cleanup *отрекается* от него. React гарантирует: cleanup выполняется перед следующим телом эффекта и при размонтировании — на этом порядке держится вся корректность.

## Гонка: ответы приходят не в том порядке, в каком вы их послали

Наберите быстро «w», «wa», «war» — и вы выпустили три запроса. HTTP не гарантирует порядок между запросами: кеши, ретраи и нагрузка на сервер переставляют их свободно. Без защиты каждый ответ вызывает `setResults`, и последним рисуется *самый медленный* запрос. Две защиты делают разную работу. **Флаг `ignore`** — это корректность: переменная замыкания на каждый запуск эффекта; когда cleanup её переключает, `setResults` устаревшего замыкания становится no-op. Ответ всё равно скачивается — вы просто отказываетесь его коммитить. **`AbortController`** — это эффективность: он отменяет саму сетевую работу, освобождая слоты соединений браузера на источник (HTTP/1.1 даёт 6) и время сервера. Одного abort *почти* хватает — отклонённый promise выходит через `AbortError`, — но флаг ignore защищает ещё и неотменяемую асинхронную работу после fetch (уже запущенный `res.json()`, цепочку трансформаций), поэтому идиома — пара. StrictMode прогоняет setup→cleanup→setup при dev-монтировании ровно затем, чтобы доказать: ваш cleanup действительно отрекается от первого запроса. Если в dev видны двойные коммиты — продакшен-гонка реальна.

<Quiz
  id="01-fetching-in-effects-quiz-1"
  lessonSlug="01-fetching-in-effects"
  lang="ru"
  question="Эффект поиска использует только AbortController, без флага ignore. query меняется с &quot;wa&quot; на &quot;warsaw&quot;; cleanup отменяет первый запрос. Гонка закрыта полностью?"
  choices={[
    { label: "Почти — отменённый fetch отклоняется с AbortError и ничего не коммитит; но асинхронные шаги, не привязанные к сигналу (уже идущий res.json(), цепочка трансформаций), всё ещё могут зарезолвиться и вызвать setState, так что флаг ignore остаётся герметичной защитой", correct: true },
    { label: "Да — abort гарантирует, что код после fetch никогда не выполнится", misconception: "abort() отклоняет promise самого fetch, но только те части, что подключены к сигналу. Если заголовки ответа уже пришли и вы вызвали res.json(), или нацепили работу, не знающую о сигнале, эти promise могут зарезолвиться после cleanup и закоммитить устаревшее состояние. Флаг ignore покрывает всё, до чего сигнал не дотягивается." },
    { label: "Нет — AbortController работает только для POST-запросов", misconception: "AbortController работает с любым fetch независимо от метода. Дыра не в HTTP-глаголе, а в том, что abort отменяет только работу, привязанную к сигналу, тогда как флаг ignore нейтрализует все пути кода устаревшего запуска эффекта — отменяемые и нет." },
    { label: "Да, если сервер обрабатывает запросы по порядку", misconception: "Порядок обработки на сервере не важен и всё равно не гарантирован — кеши, переиспользование соединений и нагрузка заставляют ответы приходить в любом порядке. Корректность на клиенте не может зависеть от серверного порядка; защита обязана жить в cleanup эффекта." },
  ]}
/>

## Водопады: дерево компонентов становится расписанием сети

Вторая структурная проблема невидима внутри отдельного компонента. `ProfilePage` запрашивает пользователя, рендерит `<Posts userId={...}>` только когда пользователь пришёл, а `Posts` запрашивает посты и рендерит `<Comments>`, который запрашивает снова. Каждый fetch стартует только после того, как данные родителя приземлились и ребёнок *смонтировался*, — три логически независимых запроса идут последовательно. При 200 мс на круг это 600 мс чистой задержки, навязанной структурой, ещё до серверного времени; на LTE с RTT 75 мс и TLS-рукопожатием реальные трейсы регулярно показывают водопады в 1,5–2 с на страницах, которые могли бы загрузиться за один круг. Сигнатура в network-панели безошибочна: лесенка. Дерево решило ваше расписание, а дерево ничего не знает о сети. Лекарства по нарастающей амбициозности: поднять запросы в самый верхний компонент, знающий все параметры, и запустить их через `Promise.all`; стартовать запросы *до* рендера (в лоадере роута или обработчике события) и передавать promise вниз; либо отдать проблему библиотеке или фреймворку, чей кеш разделяет «кто это рендерит» и «кто это запрашивает». Последний ход — тема остатка этого юнита.

<Quiz
  id="01-fetching-in-effects-quiz-2"
  lessonSlug="01-fetching-in-effects"
  lang="ru"
  question="ProfilePage запрашивает пользователя, затем рендерит Posts, который запрашивает посты, который рендерит Comments с ещё одним запросом. RTT — 200 мс. Какова минимальная навязанная структурой задержка до начала загрузки комментариев и почему?"
  choices={[
    { label: "Около 400 мс: эффект каждого ребёнка стартует только после коммита данных родителя и монтирования ребёнка, поэтому три независимых запроса сериализуются — комментарии ждут два полных круга, которые параллельный запрос бы устранил", correct: true },
    { label: "Ноль — React запускает все эффекты параллельно при монтировании", misconception: "React действительно запускает эффекты смонтированных компонентов вместе, но Posts и Comments ещё не смонтированы — они рендерятся только после прихода данных родителя. Сериализация не в планировщике эффектов, а в форме дерева, которая решает, когда компонент вообще существует." },
    { label: "Около 200 мс суммарно: браузер конвейеризует запросы", misconception: "Браузер может конвейеризовать только те запросы, которые ему отдали. Каждый fetch здесь выпускает компонент, не существующий до прихода предыдущего ответа, так что в конвейере нечего параллелить — лесенка создаётся до того, как сетевой слой её увидит." },
    { label: "600 мс: каждый useEffect ждёт лишний цикл рендера", misconception: "Эффекты срабатывают после отрисовки — это миллисекунды, а не круги по сети. Цифра 400 мс складывается из двух вышестоящих циклов fetch+commit, отпирающих третий запрос; тайминг самих эффектов — погрешность округления рядом с сериализованными сетевыми хопами." },
  ]}
/>

</Explanation>

<FlowDiagram
  label="Один запуск эффекта владеет одним запросом: cleanup отрекается от него при смене параметра, а дети, запертые деревом, превращают независимые запросы в водопад"
  hue="var(--d-frontend)"
  perRow={3}
  nodes={[
    { id: "mount", label: "Эффект выполняется", sub: "query = \"wa\"" },
    { id: "req1", label: "Запрос A", sub: "в полёте" },
    { id: "change", label: "query меняется", sub: "\"warsaw\"" },
    { id: "cleanup", label: "Cleanup", sub: "ignore + abort" },
    { id: "req2", label: "Запрос B", sub: "коммитит состояние" },
    { id: "waterfall", label: "Ребёнок монтируется поздно", sub: "серийная цепочка запросов" },
  ]}
  edges={[
    { from: "mount", to: "req1", label: "старт" },
    { from: "change", to: "cleanup", label: "перед перезапуском" },
    { from: "cleanup", to: "req1", label: "отречься" },
    { from: "change", to: "req2", label: "новый эффект" },
    { from: "req2", to: "waterfall", label: "данные запирают ребёнка" },
  ]}
  caption="Флаг ignore превращает setState устаревшего замыкания в no-op; AbortController отменяет саму сетевую работу. Дети, запрашивающие при монтировании, стартуют только после коммита данных родителя — три независимых ресурса становятся лесенкой из трёх ступеней."
/>

<KeyTakeaway>Запрос в эффекте — синхронизация с двумя встроенными режимами отказа. Гонки: HTTP-ответы приходят в любом порядке, поэтому каждый запуск эффекта обязан отречься от своего запроса в cleanup — флаг ignore для корректности, AbortController для отмены реальной сетевой работы. Водопады: компоненты, запрашивающие при монтировании, сериализуют логически параллельные запросы вниз по дереву; поднимайте запросы, стартуйте их до рендера или заводите кеш. Двадцать строк boilerplate на эндпоинт — ровно то, что query-библиотеки продают вам обратно.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="01-fetching-in-effects-retrieval"
  lang="ru"
  questions={[
    { q: "Объясните гонку fetch-в-эффекте механически: откуда берётся устаревшая запись и что именно дают флаг ignore и AbortController по отдельности?", a: "Каждый запуск эффекта замыкается на одно значение зависимости (один query) и стартует один запрос. HTTP не гарантирует порядок между запросами: запрос «wa» с холодным кешем может идти 900 мс, пока тёплый «warsaw» возвращается за 120 мс, — без защиты .then более старого запроса выполняется последним, и setState коммитит ответ на вопрос, который UI уже не задаёт. Гарантия React — cleanup выполняется перед следующим телом эффекта и при размонтировании — это крючок для лечения. Флаг ignore — переменная замыкания на запуск: cleanup её переключает, и setState устаревшего запуска становится no-op; это защита корректности, и она покрывает каждый асинхронный шаг этого запуска, включая неотменяемые — уже идущий res.json() или цепочки трансформаций. AbortController — защита эффективности: controller.abort() отклоняет fetch с AbortError и реально отменяет сетевую работу, освобождая слоты соединений на источник (6 в HTTP/1.1) и время сервера. Один abort упускает асинхронную работу, не подключённую к сигналу; один ignore тратит трафик впустую — продакшен-код использует оба. StrictMode с его setup→cleanup→setup при dev-монтировании — бесплатный тест, что cleanup действительно отрекается от первого запроса." },
    { q: "Страница показывает лесенку из 4 ступеней в network-панели. Почему запросы в эффектах дают такую форму, сколько это стоит и какие три лекарства по нарастающей?", a: "Лесенка — это дерево компонентов в роли сетевого планировщика. Ребёнок, запрашивающий в эффекте монтирования, не может стартовать до монтирования, а монтируется он только после прихода и коммита данных родителя — каждый уровень дерева добавляет полный цикл fetch+commit, прежде чем следующий запрос вообще будет выпущен. Четыре уровня при RTT 200 мс — это ~600 мс последовательной задержки, навязанной структурой, до любой серверной обработки; на мобильных RTT та же форма растягивается до нескольких секунд для логически параллельных данных. Браузер помочь не может: он не видит поздние запросы, пока не приземлились ранние ответы. Лекарства по нарастающей: (1) поднять — перенести все запросы в самый верхний компонент, знающий параметры, и выпустить их вместе через Promise.all, передавая данные вниз пропсами; (2) запрос-до-рендера — стартовать запросы вне жизненного цикла компонента (лоадеры роутов, обработчики событий), чтобы рендер и загрузка шли внахлёст, а не по очереди; (3) завести слой кеша (query-библиотека или data-API фреймворка), который разделяет «какой компонент рендерит данные» и «когда стартует запрос», открывая прелоадинг и дедупликацию. Глубокий вывод: где зависимость данных реальна (постам правда нужен user.id), водопад неустраним; где она — артефакт структуры компонентов, это чистые потери." },
  ]}
/>

<Recap lang="ru">
Запрос в `useEffect` — базовый паттерн, относительно которого меряется каждое улучшение: эффект с зависимостью от параметров запроса стартует fetch, рукописные состояния `loading`/`error`/`data` закрывают три момента UI, а cleanup — гарантированно выполняющийся перед следующим телом эффекта и при размонтировании — место, где живёт корректность. Первая структурная ошибка — гонка: HTTP-ответы приходят в любом порядке, и последним рисуется самый медленный запрос, если каждый запуск эффекта не отрекается от своего запроса в cleanup; флаг `ignore` превращает `setState` устаревшего замыкания в no-op (покрывая даже неотменяемые шаги вроде идущего `res.json()`), а `AbortController` отменяет реальную сетевую работу и освобождает слоты соединений — продакшен-код использует пару, а dev-цикл StrictMode setup→cleanup→setup дымово тестирует защиту. Вторая структурная ошибка — водопад: компоненты, запрашивающие при монтировании, стартуют только после коммита данных родителей, и логически параллельные запросы сериализуются вниз по дереву — три уровня при RTT 200 мс дают 600 мс задержки, которую network-панель рисует лесенкой. Подъём запросов к общему предку с `Promise.all`, старт запросов до рендера или кеш, отделяющий загрузку от рендеринга, — лекарства по нарастающей. Двадцать строк boilerplate на эндпоинт и два системных режима отказа — вот честная цена, которую инструменты остатка юнита существуют, чтобы вернуть.
</Recap>
