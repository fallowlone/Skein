---
concepts:
- react-reconciliation
deepensInto: []
estMin: 18
lang: ru
lessonType: topic
level: middle
order: 2
prereqs: []
slug: "02-reconciliation-and-keys"
sources:
- https://react.dev/learn/preserving-and-resetting-state
- https://react.dev/learn/rendering-lists
- https://react.dev/learn/render-and-commit
spiral: []
status: ready
summary: 'React сравнивает по типу элемента и key, а не по «экземплярам»: тот же тип на той же позиции сохраняет fiber и его состояние, смена типа размонтирует поддерево. Index в роли key привязывает состояние к позиции — пересортировка молча отдаёт состояние одной строки другой.'
title: 'Reconciliation и keys: где на самом деле живёт ваше состояние'
track: "react"
unit: "01-rendering-model"
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
Баг-репорт читался как история о привидениях: «Я печатал ответ своему руководителю, и черновик переехал в переписку с кандидатом. Я чуть не отправил кандидату обсуждение зарплаты». Сайдбар мессенджера показывал переписки, отсортированные по последней активности; в каждой строке жил неконтролируемый input с черновиком, и всё это рендерилось с `key={index}`. Пока инженер поддержки печатал в строке 0, в другом треде пришло новое сообщение, список пересортировался, и строкой 0 стала другая переписка. React сделал ровно то, что ему сказали: элемент на позиции 0 имел тот же тип и тот же key, что и раньше, поэтому он сохранил существующий fiber — а именно fiber владел состоянием инпута. Текст черновика никуда не «переезжал». Он остался прикручен к позиции 0, пока данные под ним уехали. Ни исключения, ни предупреждения, ни упавшего теста — diff был *корректен* по правилам React; просто правилам сообщили, что позиция и есть идентичность. Трое пользователей наступили на это раньше, чем баг сумели воспроизвести: для репро требовалось фоновое событие точно посреди набора текста.
</Hook>

<Crux>React сопоставляет элементы с fiber по типу + key; состояние живёт на fiber. Index-key: пересортировка — состояние осталось на месте.</Crux>

<Explanation>

## Diff — это эвристика, а не поиск по дереву

Настоящий минимальный diff двух деревьев стоит O(n³) — для тысячи элементов это порядка миллиарда сравнений, мертворождённая идея для UI, который ре-рендерится на каждое нажатие. React отказывается играть в эту игру и ставит на две эвристики, почти всегда верные в реальных интерфейсах. Первая: **элементы разных типов порождают разные деревья** — если на позиции был `div`, а стал `section`, или был `UserProfile`, а стал `LoginForm`, React внутрь не заглядывает. Он размонтирует всё старое поддерево (уничтожая всё состояние в нём, выполняя cleanup-эффекты, удаляя DOM) и монтирует новое с нуля. Вторая: **разработчик может пометить стабильную идентичность между рендерами через key**. С этими двумя допущениями reconciliation выполняется за O(n).

Само сравнение идёт позиция за позицией. Тот же тип на той же позиции: React сохраняет существующий fiber, обновляет его пропсы и рекурсивно спускается в детей — экземпляр компонента выживает, его хуки выживают. Другой тип: полный снос этого поддерева. Поэтому же сравниваются именно *описания-элементы*, а не «экземпляры» компонентов — элементы пересоздаются свежими объектами на каждом рендере и не несут никакого состояния. Это одноразовые описания; персистентная сущность, с которой их сопоставляют, — fiber.

## Состояние живёт на fiber, а key — правило сопоставления

Всё, что выдаёт `useState`, хранится на fiber — не в вашем JSX и не в замыкании функции: функция отработала и вернулась, её локальные переменные исчезли. Так что у вопроса «где текст моего инпута?» есть точный ответ: на том fiber, который React сопоставил вашему элементу при последнем reconciliation. Для одиночного ребёнка сопоставление идёт по позиции и типу. Для массива соседей позиция бессмысленна — списки пересортировывают, фильтруют, дополняют сверху — поэтому соседей React сопоставляет **по key**: старый fiber с key «a» соединяется с новым элементом с key «a», куда бы тот ни переехал. Сопоставленный fiber сохраняет состояние и свой DOM-узел, а перемещение стоит одну перестановку DOM-узла вместо сноса.

Теперь механизм поломки из пролога, точно. С `key={index}` ключ *и есть* позиция — вы переформулировали дефолт и отключили весь механизм сопоставления. Пересортируйте данные, и React видит: на позиции 0 всё ещё key 0, тот же тип → тот же fiber → то же состояние. Fiber, державший черновик для руководителя, теперь получает пропсы переписки с кандидатом. Всё, что живёт на fiber или его DOM-узле, прилипает к позиции вместо того, чтобы следовать за данными: текст неконтролируемого инпута, чекбоксы, фокус, позиция скролла, `useState` внутри строки, даже мемоизированные значения. Удаление первого элемента — тот же баг в другом костюме: каждый fiber сдвигается на новый элемент данных, а размонтируется *последний* fiber — отсюда «я удалил первую строку, но пропали заметки второй, а первая сохранила заметки удалённого тикета».

```jsx
const [convos, setConvos] = useState(initialConvos);
// convos пересортируется при каждом новом сообщении в треде

// Баг: key повторяет позицию. После пересортировки fiber (и состояние
// неконтролируемого инпута на нём) на позиции 0 сопоставляется с тем,
// какой разговор теперь оказался на позиции 0.
{convos.map((c, index) => (
  <ConversationRow key={index} convo={c} />
))}

// Исправление: key = стабильная идентичность из данных. Fiber следует за
// разговором куда угодно; React переставляет DOM-узел.
{convos.map((c) => (
  <ConversationRow key={c.id} convo={c} />
))}
```

Два следствия, о которых сеньоров спрашивают на ревью. `key={Math.random()}` — *противоположный* баг, и строго хуже: ни один key никогда не совпадает, поэтому каждый рендер размонтирует и заново монтирует все строки — состояние стирается на каждом рендере, эффекты переигрываются, DOM перестраивается целиком; таблица на 200 строк превращается из горстки точечных патчей в 200 циклов снос-и-создание на каждое нажатие. И второе: key обязан быть уникален только **среди соседей**, не глобально — `item.id` годится, даже если другой список в приложении использует те же id.

<Inset kind="why" lang="ru">
Почему команда React не стала диффить по идентичности «экземпляров» компонентов и не обошлась без key вовсе? Потому что идентичности экземпляров не существует. Функция компонента отрабатывает и возвращает простые объекты-элементы; `convos.map` на каждом рендере порождает новый массив новых объектов. Ничто в JavaScript не связывает «второй элемент массива прошлого рендера» со «вторым элементом массива этого рендера», кроме того, что React может прочитать с объектов: тип, позиция, key. Key — это разработчик, одалживающий React единственный факт, который тот не может вывести сам: какое описание относится к той же логической сущности, что и раньше. Поэтому же предупреждение про key — warning, а не ошибка: сопоставление по индексу честно годится для списка, который никогда не пересортировывается, не фильтруется и не держит состояния — статический список ссылок в футере ничего не теряет.
</Inset>

## Идентичность элемента vs идентичность компонента: сбросы, которых вы не заказывали

Обратная сторона правила «тот же тип сохраняет состояние»: *тот же тип компонента на той же позиции сохраняет состояние даже между совершенно разными ветками JSX*. Отрендерите `<Counter person="Taylor" />` в true-ветке тернарника и `<Counter person="Sarah" />` в false-ветке: переключение **не** сбрасывает счётчик. React видит позицию 1, тип `Counter` — оба раза: тот же fiber, пропсы обновлены, состояние сохранено. Если логически это разные счётчики, это баг, и лечится он разными key у веток — `key="taylor"` и `key="sarah"` — превращая key в осознанный рычаг сброса. Тем же рычагом сбрасывают форму при смене сущности: `<ProfileForm key={userId} />` перемонтирует всю форму при переключении пользователя, вместо того чтобы правки одного пользователя протекли в профиль другого.

И обратный сценарий отказа: **определение компонента внутри другого компонента**. Внутренняя функция пересоздаётся на каждом рендере родителя, поэтому её идентичность каждый раз другая; для функциональных типов React сравнивает по ссылке, видит «другой тип» на той же позиции — и размонтирует поддерево: каждый рендер родителя стирает состояние ребёнка, сбрасывает фокус, переигрывает эффекты монтажа. Выглядит как «мой инпут теряет фокус на каждом нажатии» и входит в число самых частых не-багов в трекере React. Поднимите определение на уровень модуля; нужны данные родителя — передайте пропсы.

<Quiz
  id="02-reconciliation-and-keys-quiz-1"
  lessonSlug="02-reconciliation-and-keys"
  lang="ru"
  question="Сортируемый список строк с неконтролируемыми инпутами использует key={index}. Список пересортировался, пока пользователь печатал в верхней строке. Почему набранный текст оказывается под другим элементом данных?"
  choices={[
    { label: "Состояние инпута живёт на fiber, а с index-key fiber на позиции 0 сопоставляется с новым элементом на позиции 0 — тот же key, тот же тип — и старое состояние соединяется с теми данными, что теперь лежат на этой позиции", correct: true },
    { label: "React сбатчил нажатие и пересортировку в один рендер и применил их в неверном порядке", misconception: "Батчинг схлопывает несколько обновлений состояния в один рендер, но никогда не меняет правил сопоставления. Даже по одному обновлению за рендер index-key всё так же говорит reconciliation, что позиция равна идентичности, и пересортировка всё так же отдаёт fiber позиции 0 другому элементу данных." },
    { label: "Неконтролируемые инпуты не управляются React, поэтому при ре-рендере их никогда не перемещают", misconception: "DOM-узлом владеет его fiber, и React спокойно переставляет DOM-узлы, когда fiber совпадают по key — ровно этого и добиваются стабильные ключи. Текст прилип к позиции 0 не потому, что инпут неконтролируемый, а потому, что index-key сказал React, что fiber позиции 0 — та же логическая строка." },
    { label: "Пересортировка мутировала массив на месте, и это портит diff React", misconception: "Мутация на месте может привести к пропущенным ре-рендерам (ссылка не меняется), но здесь рендер очевидно случился — строки видимо пересортировались. Порча в идентичности, а не в детекции изменений: key={index} переформулирует дефолт позиции, и сопоставление игнорирует, какую переписку представляет каждая строка." },
  ]}
/>

<Quiz
  id="02-reconciliation-and-keys-quiz-2"
  lessonSlug="02-reconciliation-and-keys"
  lang="ru"
  question="Разработчик определил ChildInput как функцию внутри тела компонента Parent. Пользователи жалуются, что инпут теряет фокус и очищается на каждом нажатии. Каков механизм?"
  choices={[
    { label: "Каждый рендер Parent создаёт новый объект функции, и тип элемента — каждый раз другая ссылка; React трактует это как смену типа и размонтирует/монтирует поддерево, уничтожая состояние и фокус", correct: true },
    { label: "Внутренним компонентам нельзя использовать хуки, поэтому состояние молча не сохраняется", misconception: "Хуки работают в любом функциональном компоненте независимо от места определения — правила хуков касаются порядка вызовов, а не места объявления. Состояние корректно создаётся при каждом монтаже; беда в том, что каждый рендер родителя вызывает свежий монтаж, потому что идентичность компонента (ссылка на функцию) изменилась." },
    { label: "Нажатие ре-рендерит Parent, а любой ре-рендер родителя всегда перемонтирует детей", misconception: "Ре-рендеры текут вниз, но reconciliation сохраняет fiber детей всякий раз, когда совпадают тип и key — в этом весь смысл диффа. Стабильные типы уровня модуля переживают миллионы рендеров родителя. Перемонтаж форсирует только смена типа (или key), а пересозданная внутренняя функция — это смена типа по ссылке." },
    { label: "У инпута нет key, а React требует key у всех детей с состоянием", misconception: "Key обязателен только для динамических массивов соседей, где позиция не выражает идентичность. Одиночный статический ребёнок прекрасно сопоставляется по позиции и типу. Key здесь не поможет — сама ссылка типа меняется каждый рендер, а несовпадение типов перекрывает любое совпадение key." },
  ]}
/>

</Explanation>

<FlowDiagram
  label="Reconciliation проводит каждый новый элемент через сопоставление по типу и key, решающее между дешёвым обновлением на месте и полным перемонтажом поддерева"
  hue="var(--d-frontend)"
  perRow={3}
  nodes={[
    { id: "elements", label: "Новые элементы", sub: "свежие объекты каждый рендер" },
    { id: "match", label: "Сопоставление с fiber", sub: "тип + key" },
    { id: "keep", label: "Fiber сохранён", sub: "состояние + DOM живы" },
    { id: "remount", label: "Снос поддерева", sub: "состояние уничтожено" },
    { id: "siblings", label: "Соседи в списке", sub: "сопоставление по key" },
    { id: "dom", label: "Commit", sub: "минимальные патчи DOM" },
  ]}
  edges={[
    { from: "elements", to: "match", label: "по позициям" },
    { from: "match", to: "keep", label: "тип+key те же" },
    { from: "match", to: "remount", label: "тип сменился" },
    { from: "elements", to: "siblings", label: "массивы" },
    { from: "siblings", to: "keep", label: "key найден" },
    { from: "keep", to: "dom", label: "обновить/переместить" },
  ]}
  caption="Элементы — одноразовые описания; fiber персистентны и держат состояние. Совпадение тип+key сохраняет fiber (пропсы обновлены, DOM-узел может переехать); смена типа сносит поддерево. Index-key схлопывает идентичность в позицию — сопоставление успешно, но не с той логической строкой."
/>

<KeyTakeaway>Состояние живёт на fiber; элементы пересоздаются каждый рендер и сопоставляются с fiber по типу и key. Совпадение типа+key сохраняет fiber и состояние; смена типа размонтирует поддерево. Index-key делает позицию идентичностью — пересортировка отдаёт состояние одной строки другой; случайные key перемонтируют всё; стабильный id из данных заставляет состояние следовать за данными. Key — это ещё и осознанный рычаг сброса.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="02-reconciliation-and-keys-retrieval"
  lang="ru"
  questions={[
    { q: "Объясните точный механизм, которым key=index портит состояние при пересортировке, включая то, где состояние физически живёт.", a: "Состояние хуков, значения неконтролируемых инпутов, фокус и сам DOM-узел живут на fiber — персистентном узле, который React держит на каждый экземпляр компонента, — а не в элементах, которые пересоздаются одноразовыми объектами на каждом рендере. При reconciliation React сопоставляет каждый новый элемент со старым fiber; среди соседей списка сопоставление идёт по key. С key=index ключ просто переформулирует позицию в массиве, поэтому после пересортировки fiber, стоявший на позиции 0, сопоставляется с тем элементом, что теперь на позиции 0: тот же key, тот же тип, сопоставление успешно — но не с той логической строкой. Fiber сохраняет своё состояние (полунабранный черновик, отмеченный чекбокс, значения useState внутри строки) и получает пропсы новой строки, поэтому состояние будто «перепрыгивает» на другой элемент данных. Удаление первого элемента сдвигает все пары на единицу и размонтирует последний fiber, а не строку удалённого. Лечение — key со стабильной идентичностью из данных (item.id): тогда старый fiber соединяется с тем же логическим элементом, куда бы тот ни переехал, React переставляет его DOM-узел, и состояние путешествует вместе с данными. key=Math.random() — противоположная крайность: ничто никогда не совпадает, каждый рендер размонтирует и монтирует все строки заново, стирая состояние и перестраивая DOM." },
    { q: "Когда React размонтирует поддерево, хотя вы не убирали его из JSX, и как использовать key, чтобы форсировать или предотвращать сбросы осознанно?", a: "React размонтирует поддерево всякий раз, когда между рендерами меняется тип элемента на позиции: div на section, UserProfile на LoginForm, или — скрытая версия — функция компонента, пересоздаваемая внутри тела родителя: её ссылка каждый рендер другая и читается как новый тип, перемонтируя ребёнка и уничтожая его состояние на каждом рендере родителя (классическое «инпут теряет фокус на каждом нажатии»). И наоборот: тот же тип компонента на той же позиции сохраняет fiber и состояние даже между разными ветками JSX — тернарник, рендерящий Counter в обеих ветках, сохраняет счётчик при переключении, потому что React видит тот же тип на той же позиции и просто обновляет пропсы. Оба направления управляются ключами. Чтобы форсировать сброс, смените key: ProfileForm с key=userId перемонтирует всю форму при смене пользователя, гарантируя, что устаревшие правки не протекут между сущностями — это сильно надёжнее ручной очистки каждого поля в эффекте. Чтобы предотвратить случайный сброс, дайте компоненту стабильную идентичность: поднимайте определения внутренних компонентов на уровень модуля, чтобы ссылка типа была постоянной, и держите key стабильными между рендерами. Правило: тип и key вместе определяют идентичность; идентичность решает, выживет ли состояние." },
  ]}
/>

<Recap lang="ru">
Reconciliation — это ответ React за O(n) на задачу за O(n³), построенный на двух эвристиках: разные типы элементов порождают разные деревья, а key помечает стабильную идентичность. Каждый рендер производит новенькие объекты-элементы — чистые описания без состояния — и React сопоставляет их с персистентным деревом fiber, где на самом деле живут состояние хуков, значения неконтролируемых инпутов, фокус и DOM-узлы. Тот же тип и key на позиции: fiber выживает, пропсы обновляются, дети согласуются дальше. Смена типа: всё поддерево размонтируется — состояние уничтожено, cleanup-и выполнены, DOM удалён — и строится заново. Для массивов соседей сопоставление идёт по key, и потому index-в-роли-key — канонический продакшен-баг: key переформулирует позицию, и пересортировка сопоставляет fiber позиции 0 (вместе с набранным в него черновиком) с тем элементом данных, что теперь занял позицию 0, а удаление сдвигает все пары на единицу, размонтируя последнюю строку вместо удалённой. Случайные key ломаются в другую сторону, перемонтируя каждую строку на каждом рендере. Стабильный id из данных заставляет состояние и DOM следовать за логическим элементом ценой перестановки узла вместо сноса. Идентичность режет в обе стороны: тот же тип компонента на той же позиции сохраняет состояние между ветками JSX, хотели вы того или нет, а смена key — осознанный рычаг сброса: key, равный userId, перемонтирует форму при смене сущности. Скрытая смена типа — определение компонента внутри другого компонента, пересоздающее ссылку функции каждый рендер, — перемонтирует поддерево на каждом рендере родителя и выглядит как потеря фокуса инпутом на каждом нажатии. Тип плюс key — это идентичность; идентичность решает, выживет ли состояние.
</Recap>
