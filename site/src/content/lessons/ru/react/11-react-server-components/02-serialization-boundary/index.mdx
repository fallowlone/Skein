---
concepts:
- react-rsc-serialization
deepensInto: []
estMin: 18
lang: ru
lessonType: topic
level: senior
order: 2
prereqs: []
slug: "02-serialization-boundary"
sources:
- https://react.dev/reference/rsc/use-client
- https://react.dev/reference/rsc/use-server
- https://react.dev/reference/rsc/server-components
spiral: []
status: ready
summary: 'use client помечает разрез графа модулей: всё, что импортирует клиентский файл, едет в бандл. Пропсы обязаны сериализоваться — простые данные, Date, Map да; функции и экземпляры классов нет. Children вкладывают серверный вывод в клиентскую оболочку; use server — путь назад.'
title: 'Граница сериализации: use client режет граф модулей'
track: "react"
unit: "11-react-server-components"
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";
import Infographic from "~/components/diagram/Infographic.astro";
import serializationOutcomes from "./infographics/serialization-outcomes.svg?raw";

<Hook>
Секьюрити-ревью нашло это за пятнадцать минут через view-source. Страница профиля — серверный компонент — забирала текущего пользователя через ORM и передавала строку таблицы прямо в клиентский компонент: `<ProfileCard user={user} />`. ORM вернула простой объект, сериализация прошла без единого звука, и Flight-пейлоад, встроенный в каждую страницу профиля, содержал строку целиком: `email`, `phone`, `stripeCustomerId`, `passwordHash`, `mfaSecret`. Никто ничего не взламывал; сервер вежливо публиковал колонки своей базы каждому, кто нажмёт Ctrl+U. Тот же аудит неделей раньше поймал обратный отказ: общий хелпер `format.ts` импортировали в файл с `'use client'`, а `format.ts` импортировал `config.ts`, который читал `process.env.STRIPE_SECRET_KEY` в экспортируемую константу — один переход по графу, и секрет лежал в публичном чанке. Ни один из багов не экзотика. Оба — одно и то же непонимание: `'use client'` — не аннотация компонента, а разрез графа модулей, и пропсы, пересекающие разрез, сериализуются и публикуются. У границы точные правила, и этот урок — про них.
</Hook>

<Crux>'use client' режет граф модулей, а не компонент. Всё пересекающее разрез сериализуется и публично — стройте пропсы как API.</Crux>

За следующие десять минут ты узнаешь, какие значения могут пересекать границу сервер→клиент, какие молча публикуют колонки базы данных — и почему оба бага из Hook полностью предотвратимы, если знать правила.

<Explanation>

## Директива на модуле, а не флаг на компоненте

`'use client'` ставится в начале *файла* и означает: отсюда этот модуль и **всё, что он импортирует, транзитивно** принадлежат клиентскому бандлу. Это разрез графа, а не свойство компонента. Пометив 40-строчный `Tabs.tsx`, вы добавили в бандл не 40 строк — вы добавили `Tabs.tsx` плюс его импорты, плюс *их* импорты, до самого дна графа. Один неосторожный `import { format } from "../lib/format"`, где `format.ts` тянет утилиту для графиков, притянет и библиотеку графиков. Поэтому же серверный компонент, импортированный *клиентским* файлом, молча становится клиентским: на самом компоненте нет маркера, есть только положение его модуля в графе относительно ближайшего разреза `'use client'`. Дисциплина отсюда: держите клиентские файлы маленькими листьями и относитесь к каждому добавленному в них импорту как к решению о размере бандла и о секретности.

Защитный инструмент для секретной половины — пакет `server-only`: импортируйте его в любом модуле, который никогда не должен достичь клиента (`db.ts`, `config.ts`, всё, что трогает `process.env`), и сборка упадёт в момент, когда какой-нибудь рефакторинг утащит модуль через разрез. Цена — одна строка импорта; молчаливая утечка превращается в ошибку компиляции. Зеркальный пакет `client-only` существует для модулей, которым нельзя выполняться на сервере.

## Что может пересекать границу: правила сериализации

Пропсы из серверного компонента в клиентский сериализуются в Flight-пейлоад. Разрешённый список шире JSON, но строг: примитивы (string, number, bigint, boolean, undefined, null, символы только через `Symbol.for`), простые объекты и массивы, `Date`, `Map`, `Set`, TypedArray и `ArrayBuffer`, `FormData`, JSX, произведённый серверными компонентами, Promise (клиент разворачивает их через `use()` — так через границу едут стримящиеся данные) и ссылки на серверные функции. Список отказов — то, что определяет архитектуру: **функции** (кроме функций с `'use server'`), **экземпляры классов** (всё, чей прототип не `Object.prototype`, — сущность ORM, объект `Temporal`, ваша доменная модель), незарегистрированные символы. Отвергнутое значение бросает ошибку сериализации в момент рендера с именем пропса.

Опасен случай, который *не* бросает. Большинство ORM возвращают простые объекты — и полная строка базы проплывает сериализацию и ложится, колонка за колонкой, в пейлоад, который может прочитать любой пользователь. Утечка `passwordHash` из Hook — ровно эта форма. Когда ты видишь в ревью пропс, принимающий объект модели целиком, спроси себя: какие колонки ты готов публиковать всем, у кого есть Ctrl+U? Правило, переживающее аудиты: считайте поверхность пропсов сервер→клиент публичным API. Маппируйте строки в явные DTO (Data Transfer Object — объект передачи данных с ровно теми полями, которые нужны UI) на границе — `{ id, name, avatarUrl }` — так же, как вы никогда не вернули бы `SELECT *` из публичного эндпоинта.

<Infographic
  svg={serializationOutcomes}
  label="Две колонки, разделённые осью VS. Левая, зелёная колонка перечисляет значения, которые сериализуются и пересекают границу use client: примитивы, простые объекты и массивы, Date/Map/Set, буферы и FormData, JSX и Promise, ссылки на серверные функции и простую ORM-строку. Правая, красная колонка перечисляет значения, которые React отвергает ошибкой при рендере: функции, экземпляры классов, ORM-сущности и незарегистрированные символы. Простая ORM-строка стоит на стороне пересечения, потому что сериализуется молча, но утекает каждой колонкой."
  caption="Пересечение разреза сериализует значение в публичный Flight-пейлоад — поэтому успешная сериализация не равна безопасности. Простая ORM-строка проплывает и утекает каждой колонкой (включая passwordHash), а функции и экземпляры классов громко бросают ошибку при рендере. Маппируйте строки в DTO."
  hue="var(--d-frontend)"
/>

<Quiz
  id="02-serialization-boundary-quiz-1"
  lessonSlug="02-serialization-boundary"
  lang="ru"
  question="Серверный компонент передаёт клиентскому четыре пропса: Date, Map с настройками, функцию formatRow и полную ORM-строку пользователя (простой объект с passwordHash). Что произойдёт с каждым?"
  choices={[
    { label: "Date и Map сериализуются нормально; функция бросает ошибку сериализации при рендере; строка пользователя сериализуется молча и публикует каждую колонку — включая passwordHash — в Flight-пейлоаде", correct: true },
    { label: "Провалятся все четыре: границу могут пересекать только JSON-безопасные значения (string, number, boolean, простой объект)", misconception: "Сериализатор Flight намеренно богаче JSON — Date, Map, Set, TypedArray, FormData, Promise и серверный JSX пересекают границу целыми. Строгость нацелена на код и идентичность: отказы — это функции и экземпляры классов. Предполагая правила JSON, вы одновременно перестрахуетесь (запретив Map) и недозащититесь (забыв, что строки-простые-объекты проплывают)." },
    { label: "Пройдут все четыре: React сериализует что угодно, включая функции, — отправляя их исходный код", misconception: "Функции не пересекают границу никогда — их замыкания, область видимости и окружение нельзя воссоздать в браузере, а отправка произвольных исходников серверных функций была бы дырой в безопасности. React бросает ошибку рендера с именем виновного пропса. Единственная функция-образная вещь, которая едет, — ссылка на use server, и она сериализуется как непрозрачный id эндпоинта, а не как код." },
    { label: "Бросит именно ORM-строка, потому что сущности базы — экземпляры классов", misconception: "Бросит или нет — зависит от формы, а не происхождения: сущность-настоящий-экземпляр класса действительно бросает, но большинство ORM возвращают простые объекты, и те сериализуются беззвучно. Эта тишина и есть ловушка — путь без ошибок оказывается утечкой данных. Защита — DTO-маппинг на границе, а не надежда, что сериализатор отвергнет секреты." },
  ]}
/>

## Переплетение без импорта: паттерн children

Кажется, что разрез графа требует: всё ниже клиентского компонента — тоже клиентское. Это не так, и лазейка — старейший проп в React. Клиентский компонент не может *импортировать* серверный, но может *получить отрендеренный на сервере вывод* как `children` (или любой проп типа JSX), потому что серверный JSX есть в списке сериализуемого:

```tsx
// Tabs.tsx — 'use client': владеет состоянием activeTab, ~2 кБ
export function Tabs({ children }: { children: React.ReactNode }) {
  const [active, setActive] = useState(0);
  return <div>{/* заголовки вкладок */}{children[active]}</div>;
}

// page.tsx — серверный компонент
<Tabs>
  <ReportPanel data={await db.reports.q3()} />   {/* остаётся серверным */}
  <AuditPanel rows={await db.audit.recent()} />  {/* остаётся серверным */}
</Tabs>
```

Панели рендерятся на сервере — прямой доступ к базе, ноль веса в бандле — и приезжают внутри пейлоада уже разрешёнными деревьями; `Tabs` лишь решает, какую показать. Правило композиции одной строкой: среду определяет *импортирующий*, а не *рендерящий*. Клиентская оболочка с серверными children — рабочая лошадь каждой серьёзной RSC-кодовой базы, и привычка тянуться к ней — то, что отличает хирургический лист `'use client'` от директивы на уровне страницы, утаскивающей весь маршрут в бандл.

## Обратный канал: серверные функции

Данные текут сервер→клиент через пропсы; *единственный* санкционированный путь обратно — серверная функция. Пометьте функцию (или целый модуль) директивой `'use server'`, и React заменит её в клиентском бандле сериализуемой ссылкой; вызов из браузера выполнит POST под капотом: аргументы сериализуются по тем же правилам Flight, а возвращаемое значение стримится обратно. Отсюда два продакшен-следствия. Первое: **каждая серверная функция — публичный, по умолчанию неаутентифицированный эндпоинт** — фреймворк сам прокладывает маршрут, и атакующему не нужен ваш UI, чтобы её вызвать; валидируйте и авторизуйте внутри функции, каждый раз, как в любом обработчике. Второе: типы аргументов и возврата подчиняются тем же правилам сериализации, что и пропсы, — ни экземпляров классов на входе, ни на выходе, — так что DTO-дисциплина работает в обе стороны.

<Quiz
  id="02-serialization-boundary-quiz-2"
  lessonSlug="02-serialization-boundary"
  lang="ru"
  question="Клиентский файл импортирует formatPrice из lib/format.ts; format.ts импортирует config.ts, который читает process.env.STRIPE_SECRET_KEY в экспортируемую константу. Сборка прошла успешно. Что произошло и что бы это предотвратило?"
  choices={[
    { label: "Транзитивный импорт утащил config.ts — вместе с секретом — в публичный клиентский чанк; импорт пакета server-only внутри config.ts превратил бы этот рефакторинг в ошибку сборки", correct: true },
    { label: "Ничего не произошло: бандлеры автоматически вырезают чтения process.env из клиентских чанков", misconception: "Бандлеры делают противоположное — они инлайнят значение process.env на этапе сборки, и именно так буквальная строка секрета оказывается в чанке. Tree-shaking убирает неиспользуемые экспорты, но константу использует импортированный хелпер, и она остаётся. Автоматического анализа секретности не существует; разрез чисто структурный." },
    { label: "Сборка должна была упасть: импорт любого серверного файла из модуля с use client — это ошибка", misconception: "«Серверного файла» по умолчанию не существует — непомеченный модуль нейтрален к среде и попадает в тот граф, который его импортирует. Эта нейтральность — и фича (общие утилиты работают везде), и ловушка (секреты путешествуют). Только явный импорт server-only даёт модулю сторону и заставляет сборку её охранять." },
    { label: "Секрет в безопасности: переменные окружения резолвятся в рантайме и только на сервере", misconception: "Это верно для кода, который выполняется на сервере, — но данный модуль утащили в клиентский граф, где бандлер резолвит process.env на этапе сборки и запекает значение внутрь. Безопасно ли чтение env, зависит от того, по какую сторону разреза use client оказался модуль, — именно поэтому разрез нужно охранять, а не предполагать." },
  ]}
/>

</Explanation>

<FlowDiagram
  label="Разрез графа модулей: что use client утягивает в бандл и что может пересечь границу как пропсы"
  hue="var(--d-frontend)"
  perRow={3}
  nodes={[
    { id: "page", label: "page.tsx", sub: "серверный компонент" },
    { id: "db", label: "db.ts + config.ts", sub: "секреты, страж server-only" },
    { id: "props", label: "пропсы через разрез", sub: "только сериализуемое, DTO" },
    { id: "chart", label: "Chart.tsx", sub: "директива 'use client'" },
    { id: "deps", label: "chart-lib + все импорты", sub: "попадают в клиентский бандл" },
    { id: "action", label: "функция 'use server'", sub: "POST назад, валидация внутри" },
  ]}
  edges={[
    { from: "page", to: "db", label: "импорт, остаётся на сервере" },
    { from: "page", to: "props", label: "рендерит Chart" },
    { from: "props", to: "chart", label: "Flight-сериализация" },
    { from: "chart", to: "deps", label: "транзитивные импорты" },
    { from: "chart", to: "action", label: "вызов → POST" },
    { from: "action", to: "db", label: "выполняется на сервере" },
  ]}
  caption="Директива режет граф модулей: Chart.tsx и всё, что он импортирует, становятся клиентским бандлом, а page.tsx и db.ts не уезжают никогда. Пропсы через разрез сериализуются в пейлоад — публичный по определению, поэтому строки маппируются в DTO. Единственная дорога назад — серверная функция: POST-эндпоинт, обязанный валидировать и авторизовать, как любой обработчик."
/>

<KeyTakeaway>'use client' режет граф модулей: файл плюс все транзитивные импорты уходят в бандл — охраняйте секретные модули пакетом server-only. Пропсы через разрез сериализуются по точным правилам — простые данные, Date, Map, Set, Promise, серверный JSX да; функции и экземпляры классов нет. Строки-простые-объекты утекают молча: маппируйте в DTO. Children вкладывают серверный вывод в клиентскую оболочку; 'use server' — проверяемый путь назад.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="02-serialization-boundary-retrieval"
  lang="ru"
  questions={[
    { q: "Сформулируйте правила сериализации пропсов сервер→клиент: что проходит, что бросает ошибку, и какой случай не бросает, но и не безопасен?", a: "Проходит: примитивы (string, number, bigint, boolean, undefined, null, символы через Symbol.for), простые объекты и массивы, Date, Map, Set, TypedArray и ArrayBuffer, FormData, JSX, отрендеренный серверными компонентами, Promise (клиент разворачивает через use()) и ссылки на функции use server. Бросает при рендере: функции, не являющиеся серверными, экземпляры классов (любой прототип кроме Object.prototype — сущности ORM, доменные модели), незарегистрированные символы. Опасный третий случай: строка базы как простой объект проходит молча и публикует каждую колонку — включая passwordHash — в пейлоад, читаемый через view-source. Ни сериализатор, ни TypeScript её не пометят, потому что структурно всё в порядке. Дисциплина, переживающая аудиты, — считать поверхность пропсов публичным API: явный DTO-маппинг на границе, выбирающий ровно те поля, которые нужны UI, точно так же, как вы никогда не отдали бы SELECT * из публичного эндпоинта." },
    { q: "Почему use client — утверждение о графе модулей, а не флаг компонента, и какие два паттерна держат под контролем бандл и секреты?", a: "Директива стоит в начале файла и приписывает этот модуль плюс каждый транзитивный импорт клиентскому бандлу; у компонентов нет собственного маркера среды — компонент серверный или клиентский лишь по положению своего модуля относительно ближайшего разреза, поэтому серверный компонент, импортированный клиентским файлом, молча становится клиентским. Паттерн один, про вес бандла: держать файлы use client маленькими листьями и переплетать через children — клиентская оболочка (Tabs со своим состоянием) получает серверные children как пропсы, поскольку серверный JSX сериализуем; тяжёлые панели сохраняют прямой доступ к данным и нулевую цену в бандле, потому что среду определяет импортирующий, а не рендерящий. Паттерн два, про секреты: импортировать server-only в каждом модуле, трогающем process.env или базу, превращая случайное перетаскивание через разрез из молчаливой утечки в ошибку сборки. Обратный канал — функции use server: сериализуемые аргументы, POST под капотом и публичный эндпоинт по определению, поэтому авторизация живёт внутри функции." },
  ]}
/>

<Recap lang="ru">
Граница между сервером и клиентом — разрез графа модулей, объявленный директивой use client в начале файла. Всё, что этот файл импортирует, транзитивно, попадает в клиентский бандл — поэтому каждый импорт, добавленный в клиентский файл, есть решение о размере бандла и о секретности, а ухоженная кодовая база держит клиентские файлы маленькими интерактивными листьями. Непомеченные модули нейтральны к среде и попадают в тот граф, который их импортирует; эта нейтральность и позволяет общему хелперу утащить конфиг со Stripe-секретом в публичный чанк, и однострочный страж — пакет server-only, превращающий такое перетаскивание в ошибку сборки. Пропсы через разрез сериализуются в Flight-пейлоад по точным правилам: примитивы, простые объекты и массивы, Date, Map, Set, TypedArray, FormData, серверный JSX, Promise и ссылки на серверные функции проходят; функции и экземпляры классов бросают ошибку при рендере. Случай, который не бросает и не безопасен, — ORM-строка как простой объект: она сериализуется молча и публикует каждую колонку в view-source, поэтому поверхность пропсов трактуется как публичный API с явным DTO-маппингом. Переплетение не требует импорта: клиентская оболочка получает серверные children как пропсы, оставляя тяжёлые панели на сервере, пока двухкилобайтный Tabs владеет состоянием. А единственная дорога назад через разрез — функция use server: настоящий POST-эндпоинт с Flight-сериализованными аргументами, публичный по построению, так что валидация и авторизация живут внутри неё — каждый раз. Теперь, когда встретишь в PR пропс, принимающий объект целиком, у тебя будет готовый вопрос: что из этого я хочу видеть в view-source — и DTO-маппинг появится раньше, чем придёт секьюрити-ревью.
</Recap>
