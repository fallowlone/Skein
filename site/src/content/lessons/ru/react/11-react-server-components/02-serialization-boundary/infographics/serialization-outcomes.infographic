infographic compare-binary-horizontal-underline-text-vs
data
  title Граница use client: сериализация vs ошибка
  desc Слева — попадает в публичный payload; справа — ошибка рендера
  compares
    - label Пересекает границу
      children
        - label Примитивы
        - label Объекты, массивы
        - label Date, Map, Set
        - label Буферы, FormData
        - label JSX и промисы
        - label Ссылки server-fn
        - label ORM → утечка!
    - label Отклоняется (ошибка)
      children
        - label Функции
        - label Экземпляры классов
        - label ORM-сущности
        - label Symbol без .for
theme
  palette #10b981 #f43f5e
