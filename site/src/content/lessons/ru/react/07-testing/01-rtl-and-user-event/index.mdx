---
concepts:
- react-testing-library
deepensInto: []
estMin: 18
lang: ru
lessonType: topic
level: senior
order: 1
prereqs: []
slug: "01-rtl-and-user-event"
sources:
- https://testing-library.com/docs/queries/about
- https://testing-library.com/docs/user-event/intro
- https://testing-library.com/docs/guiding-principles
- https://react.dev/reference/react/act
spiral: []
status: ready
summary: 'Testing Library запрашивает дерево доступности — role выше label выше text выше testid — тесты падают, когда ломается UX, а не разметка. user-event проигрывает реальные цепочки событий, fireEvent шлёт одно синтетическое. act-предупреждения — это неожиданные обновления.'
title: 'RTL и user-event: тестируйте контракт, который видит пользователь'
track: "react"
unit: "07-testing"
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";
import Infographic from "~/components/diagram/Infographic.astro";
import eventFidelity from "./infographics/event-fidelity.svg?raw";

<Hook>
Команда дизайн-системы выкатила визуальный рефреш: старый `Button` заменили стилизованным `div` с `onClick` — без элемента `button`, без обработки клавиатуры, без focus ring. Все 1 400 тестов в приложении-потребителе остались зелёными, потому что каждый из них находил элементы через `getByTestId` и кликал через `fireEvent.click` — а `fireEvent` радостно диспатчит синтетический клик на любой узел, фокусируемый или нет. Через три дня после релиза государственный клиент завёл эскалацию по комплаенсу: его сотрудники работают с клавиатуры, и каждое действие отправки в приложении молча перестало работать — Tab перепрыгивал фальшивые кнопки, Enter ничего не делал, скринридеры объявляли безымянные группы. В контракте был пункт о доступности; в копию переписки добавили юристов. Фраза из постмортема, которая прижилась: у сьюта был 100% pass rate и 0% detection rate, потому что `data-testid` переживает любую разметку, а `fireEvent` работает на чём угодно — тесты проверяли реализацию, и реализация была в порядке. `getByRole("button", { name: /submit/i })` упал бы в первом же PR: `div` не даёт роль button. Починка компонента заняла час. Три недели ушли на миграцию 1 400 запросов — чтобы сьют упал в *следующий* раз, когда сломается UX.
</Hook>

<Crux>Запрашивайте дерево доступности — role, label, text. user-event проигрывает реальные цепочки событий; fireEvent шлёт одно синтетическое.</Crux>

<Explanation>

## Запрос — это и есть проверка

Главный принцип Testing Library — тесты должны походить на то, как софт используют, — не лозунг: он закодирован в API запросов как порядок приоритета, и каждый шаг вниз по этому порядку отдаёт класс регрессий, который тест способен поймать. **`getByRole`** запрашивает вычисленное дерево доступности — ту же структуру, которую потребляют вспомогательные технологии, — а опция `name` фиксирует *доступное имя* (вычисляемое из label, `aria-label`, `aria-labelledby` или текста по алгоритму accname W3C). Запрос по роли — это две проверки в одной: элемент существует *и* он показан пользователю как правильный тип контрола с правильным именем. **`getByLabelText`** проверяет привязку подписи к полю формы — `htmlFor`, обёртывание или `aria-labelledby` — ровно ту связь, которую разрешает скринридер; визуально соседняя, но не привязанная подпись проходит проверку `getByText` и валит эту. **`getByText`** утверждает видимый контент для неинтерактивных элементов. **`getByTestId`** не утверждает ничего, кроме того, что тест и разметка согласны насчёт одной строки. Это осознанный аварийный люк — область с canvas, сторонний виджет, чью разметку вы не можете поправить, — а не дефолт.

`screen` запрашивает весь документ. `within(node)` ограничивает запросы поддеревом — и ради читаемости («кнопка Save *внутри этого диалога*»), и ради производительности на больших DOM, что важнее, чем ожидает большинство команд (цифры ниже).

<Quiz
  id="01-rtl-and-user-event-quiz-1"
  lessonSlug="01-rtl-and-user-event"
  lang="ru"
  question="PR заменяет getByTestId('save-btn') на getByRole('button', { name: /save/i }), и тест падает: кнопка существует, но без доступного имени — в ней только SVG-иконка. Какой вывод сделает сеньор?"
  choices={[
    { label: "Запрос по роли вскрыл настоящий баг доступности: кнопка-иконка без aria-label не имеет имени для вспомогательных технологий. Чинить компонент (добавить aria-label), запрос оставить", correct: true },
    { label: "Запросы по роли слишком строгие для кнопок-иконок — вернуть getByTestId для этого компонента", misconception: "Строгость и есть ценность: запрос упал ровно потому, что пользователь вспомогательных технологий не может опознать этот контрол. Возврат к testid заставит тест проходить, перестав проверять то, что сломано. Testid — для областей действительно без ролей, а не для заглушения находок на обычной кнопке." },
    { label: "Использовать getByRole('button') без опции name, чтобы тест прошёл", misconception: "Убрав name, вы сохраняете проверку роли, но отказываетесь от проверки имени, а на экране с несколькими кнопками запрос перестаёт указывать, какой контрол имеется в виду, — первая же перестановка разметки подсунет не ту. Доступное имя — половина контракта; чинить нужно компонент, а не запрос." },
    { label: "Обернуть иконку в aria-hidden, чтобы кнопка матчилась по тексту тултипа", misconception: "aria-hidden убирает иконку из дерева доступности, но имени не добавляет — кнопка по-прежнему безымянна, а тултипы не становятся доступными именами без aria-describedby или aria-labelledby. Прямое лечение — явный aria-label на кнопке." },
  ]}
/>

## Что на самом деле диспатчит user-event

`fireEvent.click` диспатчит ровно одно синтетическое `MouseEvent`. Реальный клик пользователя — это *последовательность*: pointerover → pointerenter → pointermove → pointerdown → mousedown → focus → pointerup → mouseup → click — и `user-event` v14 проигрывает всё взаимодействие целиком, порядка девяти событий с правильным порядком, координатами и побочными эффектами фокуса. `user.type` диспатчит keydown → keypress → input → keyup на каждый символ: строка из 20 символов — это 80 с лишним событий, каждое из которых уважает `maxLength`, `disabled`, `readonly` и текущее выделение. `fireEvent.change` телепортирует значение в инпут целиком — он никогда не вызывает обработчики keydown, его не ограничивает `maxLength`, и он даст зелёный свет состояниям, которые пользователь буквально не может создать. Компонент, отправляющий форму по Enter через `onKeyDown`, нетестируем через `fireEvent.change` и тривиально тестируется через `await user.type(input, "query{Enter}")`.

<Infographic
  svg={eventFidelity}
  label="Сравнение в две колонки того, что диспатчат fireEvent и user-event: fireEvent шлёт одно синтетическое событие на вызов (один MouseEvent клика, один телепорт значения) и пропускает невозможные состояния, тогда как user-event v14 шлёт полную цепочку устройства — около девяти событий на клик и четыре на нажатие клавиши (80+ на 20 символов), уступает поток асинхронно, чтобы React успел отрисовать, и бросает исключение на невозможных взаимодействиях."
  caption="fireEvent телепортирует одно синтетическое событие на любой узел; user-event проигрывает реальную цепочку ввода под ограничениями браузера — поэтому ловит регрессии клавиатуры, maxLength и pointer-events, мимо которых fireEvent проходит."
  hue="var(--d-frontend)"
/>

Механика v14 важна на код-ревью. `const user = userEvent.setup()` *до* `render` создаёт инстанс, привязанный к одному согласованному состоянию устройств ввода (клавиатура, указатель, буфер обмена). Каждый API асинхронный — `await user.click(...)` — потому что user-event уступает поток между событиями цепочки, чтобы React успевал коммитить промежуточное состояние, как реальный браузер перемежает диспатч событий с рендерингом. Пропущенный `await` — не вопрос стиля: взаимодействие начинает гоняться с идущими следом проверками и становится постоянным источником act-предупреждений и флейков.

```tsx
// ❌ проходит, даже когда пользователь заблокирован:
fireEvent.change(input, { target: { value: "abcdefgh" } }); // игнорирует maxLength
fireEvent.click(saveButton); // срабатывает на disabled и нефокусируемых узлах

// ✅ падает так же, как падает пользователь:
const user = userEvent.setup();
await user.type(input, "abcdefgh"); // остановится на maxLength, события на каждую клавишу
await user.click(saveButton);      // бросит исключение на pointer-events: none
```

Компромисс — цена и строгость. user-event медленнее — больше событий, точки await между ними — и он отказывается выполнять невозможные взаимодействия: клик по элементу с `pointer-events: none` бросает исключение. Эта строгость и есть смысл: когда `user.click` падает, пользователь тоже заблокирован. Берите `fireEvent` только для событий, которые пользователь не может породить напрямую (`scroll`, кастомное событие из не-UI источника), а не по умолчанию.

<Quiz
  id="01-rtl-and-user-event-quiz-2"
  lessonSlug="01-rtl-and-user-event"
  lang="ru"
  question="У инпута maxLength={5}. Тест делает fireEvent.change(input, { target: { value: 'abcdefgh' } }) и проверяет, что значение равно 'abcdefgh', — проходит. Тот же тест на await user.type(input, 'abcdefgh') падает. Кто прав?"
  choices={[
    { label: "user-event прав: он печатает по клавишам и уважает maxLength, значение останавливается на 'abcde' — как у реального пользователя. fireEvent.change присвоил состояние, которое пользователь создать не может", correct: true },
    { label: "Прав fireEvent: DOM принял значение, значит компонент с ним справляется", misconception: "Программное присваивание значения обходит ограничения набора — maxLength лимитирует нажатия клавиш, а не сеттер value. Тест доказал, что компонент обрабатывает состояние, недостижимое через UI, то есть проверил вымысел. Контракт под тестом — то, что может сделать пользователь." },
    { label: "Оба неправы; тест должен напрямую выставить состояние компонента, минуя капризы DOM", misconception: "Прямое выставление состояния привязывает тест к внутренностям и пропускает весь конвейер ввода — события, ограничения, фокус. Это ловушка теста реализации: он переживает рефакторинги и пропускает каждый баг обработки ввода — ровно тот класс регрессий, ради которого этот слой тестов существует." },
    { label: "В user-event баг с maxLength; добавить {skipClick: true}, чтобы тест прошёл", misconception: "skipClick лишь пропускает фокусирующий клик перед набором — к длине ввода он отношения не имеет. Падение не баг, а верность симуляции: по-клавишные события упираются в maxLength точно так же, как в браузере. Заставить этот тест проходить — значит заставить симуляцию врать." },
  ]}
/>

## act()-предупреждения: симптом, а не цель

Предупреждение — «An update to X inside a test was not wrapped in act(...)» — не означает «сыпьте `act`, пока не замолчит». RTL уже оборачивает `render`, `fireEvent` и каждый вызов user-event в `act`. Предупреждение означает: обновление состояния произошло, *когда ни один act-осведомлённый скоуп не был открыт* — почти всегда это асинхронная операция, которую тест не дождался: fetch, зарезолвившийся после последней проверки, таймер, сработавший после возврата из теста, пропущенный `await` у user-event. Правильное лечение — дождаться видимого пользователю результата (`await screen.findByText(...)`) или заставить тест владеть своей незавершённой работой; обёртывание случайных строк в `act()` глушит пожарную сигнализацию, пока обновление всё равно приземляется в неконтролируемый момент. Команды, глобально фильтрующие act-предупреждения из `console.error`, встречают их снова как флейки в CI, и хуже: позднее обновление протекает в *следующий* тест — так рождаются падения, зависящие от порядка.

Инвентарь отказов из Hook этого урока обобщается: сьют на сплошных testid переживает рефакторинги и пропускает сломанный UX; сьют только на fireEvent остаётся зелёным, пока гниёт обработка клавиатуры и ограничений; сьют, глушащий act, конвертирует детерминированные предупреждения в вероятностные флейки. У всех трёх один корень: тест перестал походить на пользователя.

<Inset kind="why" lang="ru">
Почему запросы по роли медленнее — и насколько, честно? `getByRole` вычисляет роли и доступные имена: алгоритм accname обходит поддеревья, разрешает ссылки `aria-labelledby` и проверяет видимость. В jsdom `getByRole` по всему документу на DOM из тысяч узлов может стоить десятки миллисекунд за вызов, тогда как `getByTestId` — это `querySelector` за доли миллисекунды. На нескольких сотнях тестов это складывается в минуты — обычный аргумент сторонников testid. Сеньорский ответ — скоупинг, а не капитуляция: `within(screen.getByRole("table"))` ограничивает дорогое вычисление поддеревом, и сьют сохраняет проверки контракта. Ещё честность: у jsdom нет лэйаут-движка, поэтому видимость в вычислении имени аппроксимируется по разобранным стилям — небольшой класс случаев, где разрешение роли отличается от реального браузера. Эти случаи — для слоя E2E, а не повод отказаться от ролей везде.
</Inset>

</Explanation>

<FlowDiagram
  label="Приоритет запросов — регулятор силы контракта: каждый шаг вниз отдаёт класс регрессий, который тест способен поймать"
  hue="var(--d-frontend)"
  perRow={3}
  nodes={[
    { id: "role", label: "getByRole + name", sub: "контракт a11y-дерева" },
    { id: "label", label: "getByLabelText", sub: "привязка подписи поля" },
    { id: "text", label: "getByText", sub: "видимый контент" },
    { id: "testid", label: "getByTestId", sub: "совпадение строк" },
    { id: "contract", label: "Тест контракта", sub: "падает, когда ломается UX" },
    { id: "impl", label: "Тест реализации", sub: "переживает сломанный UX" },
  ]}
  edges={[
    { from: "role", to: "label", label: "роль не видна" },
    { from: "label", to: "text", label: "не поле формы" },
    { from: "text", to: "testid", label: "крайний случай" },
    { from: "role", to: "contract", label: "проверяет семантику" },
    { from: "label", to: "contract", label: "проверяет привязку" },
    { from: "testid", to: "impl", label: "нет сигнала UX" },
  ]}
  caption="getByRole утверждает, что элемент показан вспомогательным технологиям как правильный контрол с правильным именем; getByLabelText — привязку подписи, которую разрешает скринридер; getByText — видимый контент; getByTestId — лишь согласие двух строк. 1 400 зелёных тестов из Hook жили в правом нижнем блоке."
/>

<KeyTakeaway>Приоритет запросов — регулятор силы контракта: role утверждает дерево доступности, label — привязку поля, text — видимый контент, testid — строку. user-event проигрывает реальную цепочку событий под ограничениями браузера; fireEvent шлёт одно непроверенное событие. act-предупреждение значит недождавшуюся асинхронную работу — ждите результат, а не глушите сигнал.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="01-rtl-and-user-event-retrieval"
  lang="ru"
  questions={[
    { q: "Почему getByRole ловит регрессии, которые getByTestId поймать не может, и какова честная цена его использования?", a: "getByRole запрашивает вычисленное дерево доступности и, с опцией name, доступное имя по алгоритму accname — то есть утверждает, что элемент показан пользователям и вспомогательным технологиям как правильный тип контрола с правильным именем. Кнопка, заменённая на div с onClick, поле, потерявшее привязку подписи, кнопка-иконка без aria-label — всё падает сразу. getByTestId — это сравнение строк, безразличное к разметке: оно переживает каждую из этих поломок, поэтому сьют на сплошных testid может держать 100% pass rate сквозь релиз со сломанным UX. Цена: вычисление accname обходит поддеревья и разрешает ссылки — запросы по роли на больших jsdom-DOM по всему документу стоят десятки миллисекунд против долей миллисекунды у testid; лечится скоупингом через within(), а не отказом от ролей. jsdom также аппроксимирует видимость (нет лэйаута), поэтому небольшой класс разрешений роли и имени отличается от реального браузера — эти случаи принадлежат E2E. Testid остаётся легитимным для областей действительно без ролей: canvas, неисправимые сторонние виджеты." },
    { q: "Сравните, что реально диспатчат fireEvent.click / fireEvent.change и user-event, и назовите случаи, когда fireEvent — правильный инструмент.", a: "fireEvent диспатчит ровно одно синтетическое событие без состояния устройства: click — одинокое MouseEvent, срабатывающее на disabled, нефокусируемых и перекрытых узлах; change телепортирует значение в инпут целиком, обходя maxLength, readonly и все обработчики keydown — он может утверждать состояния, которые пользователь создать не способен. user-event v14 симулирует устройство: setup() создаёт инстанс с согласованным состоянием клавиатуры, указателя и буфера; click проигрывает полную цепочку от pointerover до click (~9 событий с эффектами фокуса); type диспатчит keydown/keypress/input/keyup на символ — 80+ событий на 20 символов — уважая maxLength, disabled и выделение; каждый API асинхронный и уступает поток между событиями, чтобы React коммитил промежуточное состояние, — поэтому пропущенный await гоняется с проверками и порождает act-предупреждения. user-event также отказывается от невозможных взаимодействий (клик по pointer-events: none бросает) — строгость, зеркалящая заблокированного пользователя. fireEvent остаётся правильным для событий, которые пользователь не порождает напрямую, — scroll, медиа-события, кастомные события из не-UI источников — и это весь список; взаимодействия по умолчанию за user-event." },
  ]}
/>

<Recap lang="ru">
Testing Library переворачивает привычный вопрос тестирования: не «работает ли моя реализация», а «держится ли контракт, который воспринимает пользователь». API запросов кодирует это как порядок приоритета с механическими зубами. getByRole разрешает дерево доступности и доступное имя — две проверки в одной, и причина, по которой рефакторинг div-с-onClick падает немедленно под запросами по роли, проплывая сквозь testid-сьют, как показала комплаенс-эскалация из Hook ценой трёхнедельной миграции запросов. getByLabelText утверждает ровно ту привязку подписи, которую разрешает скринридер; getByText — видимый контент; getByTestId — лишь согласие строк между тестом и разметкой, поэтому он переживает любую поломку и зарезервирован за областями действительно без ролей. Сторона взаимодействий зеркалит сторону запросов. fireEvent диспатчит одно синтетическое событие без состояния устройства: change телепортирует значения мимо maxLength и обработчиков keydown, click срабатывает на узлах, до которых пользователь не доберётся. user-event v14 симулирует устройство ввода — setup() привязывает согласованное состояние клавиатуры и указателя, каждый API асинхронный и уступает поток между событиями настоящей цепочки (девять событий на клик, четыре с лишним на нажатие), чтобы React коммитил так, как браузер перемежает диспатч с рендером, а невозможные взаимодействия бросают исключение — симуляция встаёт на сторону заблокированного пользователя. Цена — скорость и строгость, окупаемые пойманными регрессиями. Наконец, act-предупреждения: RTL уже оборачивает render и user-event в act, поэтому предупреждение всегда означает обновление вне ожидаемого скоупа — недождавшийся fetch, таймер или вызов user-event. Ждите видимый пользователю результат через findBy; не глушите предупреждение — подавленный act-сигнал это будущий флейк, зависящий от порядка тестов. Общий корень всех отказов урока — testid везде, только fireEvent, глушение act — тест, переставший походить на пользователя.
</Recap>
