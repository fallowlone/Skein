---
concepts:
- react-optimistic-ui
deepensInto: []
estMin: 19
lang: ru
lessonType: topic
level: senior
order: 4
prereqs: []
slug: "04-optimistic-ui"
sources:
- https://react.dev/reference/react/useOptimistic
- https://react.dev/reference/react/useActionState
- https://react.dev/reference/react-dom/components/form
spiral: []
status: ready
summary: 'useOptimistic рендерит желаемое состояние, пока летит мутация, и возвращается к каноничному, когда transition завершился — откат бесплатен. Сверка с истиной сервера, ключи идемпотентности против двойного сабмита и знание, где оптимизм неуместен (платежи, удаления), — на вас.'
title: 'Оптимистичный UI и useOptimistic: честно врать пользователю'
track: "react"
unit: "04-forms-and-mutations"
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";
import SequenceDiagram from "~/components/diagram/SequenceDiagram.astro";
import Infographic from "~/components/diagram/Infographic.astro";
import optimismDecision from "./infographics/optimism-decision.svg?raw";

<Hook>
Два бага от одной команды ленты с разницей в шесть недель. Первый — двойной лайк: пользователь на гостиничном wifi жмёт сердечко, две секунды не видит ничего (команда показывала лайк только после подтверждения сервера), решает, что промахнулся, и жмёт снова. Оба запроса в итоге доезжают; счётчик показывает 2 от одного человеческого жеста, а тумблер лайка инвертирован — ещё одно нажатие «снимает лайк» до 1 вместо 0. Второй — исчезающий комментарий: когда команда вручную перешла на оптимизм, пользователь постит комментарий, видит его мгновенно — и через три секунды смотрит, как тот испаряется. Самодельный откат был `catch`-блоком, выsplice-ивающим комментарий из локального состояния, но с ним сгонялся refetch, запущенный другим компонентом: снапшот refetch-а (снятый до вставки) перезаписал список, а splice затем отработал по чужому массиву. Комментарий на самом деле сохранился; UI просто потерял нить. Оптимистичный UI — правильный выбор для кнопки лайка: воспринимаемая задержка падает с round-trip до нуля. Но самодельный оптимизм — это домашка по распределённым системам: вы ведёте две линии времени, локальную надежду и серверную истину, и каждое слияние между ними — шанс соврать. React 19 поставляет дисциплину слияния как хук.
</Hook>

<Crux>useOptimistic накладывает желаемое состояние, пока летит transition; на завершении — авто-возврат к каноничному. Истина у сервера.</Crux>

<Explanation>

## Механизм: оверлей с истекающим сроком

`useOptimistic(canonicalState, mergeFn)` возвращает состояние для рендера плюс `addOptimistic`. Семантика — то, что нужно усвоить: хук **не** форкает ваше состояние — он рендерит *оверлей*. Пока никакая мутация не висит, он возвращает каноничное состояние нетронутым. Когда вы вызываете `addOptimistic(input)` внутри transition (form action — это transition), React рендерит `mergeFn(canonicalState, input)` — каноничное состояние плюс вашу надежду — ровно столько, сколько transition в полёте. Когда transition завершается, оверлей истекает, и хук возвращает то, чем каноничное состояние стало *теперь*. Отменять нечего, потому что ничего не было записано: откат при провале — не код, который вы пишете, а истечение оверлея при каноничном состоянии, которое не успело измениться.

```jsx
function Comments({ comments, postComment }) {
  const [optimisticComments, addOptimistic] = useOptimistic(
    comments,
    (current, newComment) => [
      ...current,
      { ...newComment, pending: true },
    ]
  );

  async function action(formData) {
    const text = formData.get("text");
    addOptimistic({ id: crypto.randomUUID(), text });
    await postComment(text); // on success, parent refetches → comments updates
  }

  return (
    <form action={action}>
      <ul>
        {optimisticComments.map((c) => (
          <li key={c.id} style={{ opacity: c.pending ? 0.5 : 1 }}>
            {c.text}
          </li>
        ))}
      </ul>
      <input name="text" />
    </form>
  );
}
```

Путь успеха: action ждёт POST, родитель обновляет `comments` (refetch или слияние ответа), transition завершается, и оверлей сменяется каноничным состоянием, которое теперь *содержит* комментарий — пользователь не видит шва. Путь провала: action бросает или возвращает ошибку, каноничное состояние не менялось, оверлей истекает — комментарий гаснет сам. Флаг `pending: true` — маркер честности: приглушение или лёгкий спиннер говорят пользователю, что этот элемент — обещание, а не факт, и редкое исчезновение становится объяснимым, а не мистическим. Баг исчезающего комментария из Hook в этой модели невыразим — нечему гнаться со splice-ом, потому что второй записываемой копии списка не существует.

## Сверка: истина сервера побеждает — даже когда не согласна

Оверлей отвечает на «что показывать, пока ждём» — сверка отвечает на «что делать, когда пришла истина». Правило абсолютно: оптимистичное значение — это *предсказание*, и ответ сервера замещает его, даже если они расходятся. Сервер может нормализовать текст комментария, навесить флаги модерации, выдать настоящие id и timestamp; каноничное состояние строится из ответа (или refetch-а) — никогда повышением оптимистичного объекта до «настоящего». Команды, повышающие предсказание, получают дрейф: UI показывает домодерационный текст до следующей полной перезагрузки, а временный клиентский id протекает в якоря и аналитику.

Идентичность — острая кромка. У оптимистичного элемента клиентский ключ; у серверного — настоящий id. Если слияние идёт по позиции в массиве или по небрежному равенству, медленный успех ненадолго покажет *оба* — приглушённое предсказание и подтверждённый элемент. Несите на оптимистичном элементе клиентский ключ, пусть сервер вернёт его эхом (большинство API возвращают присланное) и сшивайте по нему.

## Идемпотентность: пережить второе нажатие

Оптимизм делает UI мгновенным, но с сетью не делает ничего, а пользователи дабл-сабмитят по уважительным причинам: кнопка не отреагировала визуально, страница замерла, гостиничный wifi. Отключение кнопки на время `isPending` помогает локально, но бессильно, если первый запрос уже улетел, а ответ потерялся — пользователь повторяет, и сервер, не имея памяти, выполняет мутацию дважды. Двойной лайк, двойной комментарий, а в худшем жанре — двойное списание.

Лекарство — **ключ идемпотентности**: клиент генерирует уникальный ключ на *намерение* (не на запрос) — `crypto.randomUUID()` в момент жеста — и шлёт его с мутацией; ретрай шлёт *тот же* ключ. Сервер хранит обработанные ключи и на повтор возвращает записанный результат вместо повторного исполнения. Это превращает доставку «хотя бы раз» в эффект «ровно раз» — тот же контракт, который ровно по этой причине выставляют платёжные API. Заметьте разделение труда: React даёт оверлей и pending-флаг; идемпотентность — протокол между вашим клиентским кодом и вашим API, и никакой хук её не подвезёт.

<SequenceDiagram
  label="Идемпотентный ретрай во времени: первый запрос исполняется, его ответ теряется на обратном пути, пользователь повторяет с тем же ключом, и сервер проигрывает записанный результат вместо повторного исполнения"
  hue="var(--d-frontend)"
  actors={["Клиент", "Сервер"]}
  messages={[
    { from: "Клиент", to: "Сервер", label: "POST like, ключ K1" },
    { from: "Сервер", to: "Клиент", label: "201 — не дошёл" },
    { from: "Клиент", to: "Сервер", label: "ретрай — тот же K1" },
    { from: "Сервер", to: "Клиент", label: "повтор K1, без записи" },
  ]}
  caption="Сервер исполнил мутацию один раз и сохранил результат под K1; ответ потерялся, пользователь нажал ещё раз, и совпавший ключ позволил серверу ответить из памяти, а не выполнить запись дважды. Доставка «хотя бы раз» становится эффектом «ровно раз» — при условии, что ключ генерируется на намерение, в момент жеста, а не на запрос."
/>

<Inset kind="why" lang="ru">
Почему useOptimistic требует transition или action? Потому что время жизни оверлея определяется именно им: показ длится от addOptimistic до завершения объемлющего transition. Вне всякого transition нет скобки, к которой привязать надежду, — React 19.x предупреждает, что оптимистичное обновление сделано вне transition, и значение почти сразу отщёлкивает назад. Эта пара и есть дизайн: actions дают мутациям отслеживаемое время жизни, а useOptimistic арендует ровно это время для своей лжи.
</Inset>

## Когда оптимизм — неверная ставка

Оптимизм — пари, что успех подавляюще вероятен, а провал дёшево забрать обратно. Лайки, подписки, эмодзи-реакции, отметка прочитанным, перестановка задач в списке: успех в высоких 99-х, откат — лёгкое затухание; ставьте всегда. Пари переворачивается в трёх ситуациях. **Деньги и всё юридически значимое**: показать «Платёж выполнен» до ответа процессинга — не сокрытие задержки, а ложный чек: пользователь закрывает вкладку и уходит, веря в то, что может оказаться неправдой. **Деструктивные, трудно обратимые операции**: оптимистично исчезающие «Удалить аккаунт» или «Отменить подписку» подтверждают необратимый акт, который сервер ещё может отвергнуть, — а пользователь уже действует исходя из подтверждения. **Мутации с низкой уверенностью**: всё с заметной долей отказов (модерируемый контент в строгом сообществе, выбор мест под конкуренцией, резервирование остатков) — когда провал рутина, а не исключение, UI постоянно «забирает обратно» показанное, и каждое изъятие жжёт доверие, которое оптимизм должен был строить. Здесь выигрывает честный pending: отключить, показать прогресс, подтвердить по истине. Пользователь прощает 800 мс спиннера на платеже; он не прощает чек, который отозвали.

<Infographic
  svg={optimismDecision}
  label="Решающая сетка 2x2 для оптимистичного UI: ось вероятности успеха пересекается с осью цены изъятия, и только квадрант «успех почти гарантирован плюс дешёвый откат» говорит «ставь», а остальные три — «откажись» и оставайся честно-pending."
  caption="Оптимизм — пари с двумя переменными. Ставьте только там, где успех почти гарантирован, а откат — лёгкое затухание: лайки, подписки, перестановки. Откажитесь на платежах (ложный чек), деструктивных операциях вроде удаления аккаунта и рутинно отвергаемых мутациях вроде конкурентного выбора мест, где UI, забирающий показанное, жжёт доверие, ради которого строился оптимизм."
  hue="var(--d-frontend)"
/>

<Quiz
  id="04-optimistic-ui-quiz-1"
  lessonSlug="04-optimistic-ui"
  lang="ru"
  question="С useOptimistic мутация провалилась (action бросил исключение), и оптимистичный комментарий исчез. Где код отката, который его убрал?"
  choices={[
    { label: "Его нет — useOptimistic рендерит оверлей поверх каноничного состояния только пока transition в полёте; на завершении оверлей истекает, а раз каноничное состояние не менялось, комментарий просто больше не рендерится", correct: true },
    { label: "React снимает снапшот состояния перед addOptimistic и восстанавливает его во внутреннем catch", misconception: "Восстанавливать нечего, потому что ничего не мутировалось — addOptimistic никогда не пишет в каноничное состояние. Модель «снапшот-и-восстановление» заставляет ждать гонок порядка восстановления; в модели оверлея такого окна нет, потому что записываемое состояние всего одно." },
    { label: "Функция слияния выполняется в обратную сторону, отменяя свой результат", misconception: "Функция слияния — чистое вычисление времени рендера: каноничное состояние плюс висящая надежда на входе, отображаемое значение на выходе. У неё нет обратной операции, и она не нужна — её просто перестают применять, когда transition завершился." },
    { label: "Catch-и-splice надо писать самому; хук только добавляет элемент", misconception: "Catch-и-splice — ровно тот самодельный паттерн, который породил гонку исчезающего комментария. Вся ценность хука в том, что удаление — не код: оверлей привязан к времени жизни transition и истекает вместе с ним." },
  ]}
/>

<Quiz
  id="04-optimistic-ui-quiz-2"
  lessonSlug="04-optimistic-ui"
  lang="ru"
  question="Пользователь жмёт «лайк», запрос успешен, но ответ теряется при обрыве wifi; пользователь жмёт снова, и счётчик приходит к 2. Какой фикс бьёт в корневую причину?"
  choices={[
    { label: "Слать клиентский ключ идемпотентности на жест; сервер запоминает обработанные ключи и на ретрай возвращает исходный результат вместо повторного исполнения мутации", correct: true },
    { label: "Отключать кнопку, пока isPending равен true", misconception: "Это спасает от быстрых двойных нажатий внутри одной отслеживаемой мутации, но здесь ретрай случился после завершения первого запроса (его ответ потерялся). Клиент честно не знает, что лайк доехал — обезвредить второй запрос может только серверная память о первом намерении." },
    { label: "Перевести лайк на useOptimistic, чтобы первое нажатие показывалось мгновенно", misconception: "Оптимизм убирает причину ретрая из-за воспринимаемой задержки и снижает вероятность — но не делает дублирующий запрос безопасным. Ретрай после потерянного ответа всё равно исполнится дважды на сервере без памяти идемпотентности; оверлей — это UX, а не гарантия доставки." },
    { label: "Задебаунсить обработчик клика окном в 2 секунды", misconception: "Дебаунс — клиентский таймер, исчезающий вместе с вкладкой и гадающий о тайминге ретрая. Пользователь может повторить через 3 секунды, со свежей страницы или с другого устройства — дедуплицировать все эти случаи может только сервер, ключуясь на намерении." },
  ]}
/>

</Explanation>

<FlowDiagram
  label="Оптимистичный оверлей показывает слитую надежду, пока transition в полёте; при успехе каноничное состояние впитывает истину сервера, при провале оверлей просто истекает — а ключ идемпотентности заставляет неизбежный ретрай исполниться один раз"
  hue="var(--d-frontend)"
  perRow={3}
  nodes={[
    { id: "gesture", label: "Жест", sub: "тап + ключ идемпотентности" },
    { id: "overlay", label: "addOptimistic", sub: "рендер merge(canon, hope)" },
    { id: "flight", label: "Мутация в полёте", sub: "transition pending" },
    { id: "success", label: "Истина сервера", sub: "каноничное обновляется" },
    { id: "fail", label: "Провал", sub: "canon не менялся" },
    { id: "settle", label: "Оверлей истекает", sub: "рендер каноничного" },
  ]}
  edges={[
    { from: "gesture", to: "overlay", label: "мгновенно" },
    { from: "overlay", to: "flight", label: "await" },
    { from: "flight", to: "success", label: "2xx" },
    { from: "flight", to: "fail", label: "ошибка" },
    { from: "success", to: "settle", label: "истина показана" },
    { from: "fail", to: "settle", label: "надежда гаснет" },
  ]}
  caption="Одно записываемое состояние (каноничное, серверное) и один эфемерный оверлей, привязанный к transition. Сверка всегда строит каноничное состояние из ответа сервера — сшивайте оптимистичные элементы по клиентскому ключу-эху, не повышайте предсказание. Пропускайте оптимизм там, где изъятие — ложный чек: платежи, деструктивные операции, мутации с рутинными отказами."
/>

<KeyTakeaway>useOptimistic рендерит merge(canonicalState, hope) только пока объемлющий transition висит — он никогда не пишет, поэтому откат при провале — это истечение оверлея, а не код, способный сгоняться с refetch-ем. Сверка на вас: истина сервера замещает предсказание, даже когда они расходятся, сшивка — по клиентскому ключу, который сервер возвращает эхом. Ключи идемпотентности — один на жест, хранение на сервере — то, что заставляет двойное нажатие исполниться один раз; этого не подвезёт никакой хук. И оптимизм — пари: берите его для лайков и перестановок, отказывайтесь для платежей, деструктивных операций и любой мутации с рутинными отказами.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="04-optimistic-ui-retrieval"
  lang="ru"
  questions={[
    { q: "Объясните оверлейную модель useOptimistic и почему она структурно исключает гонку исчезающего комментария, от которой страдает самодельный оптимизм.", a: "useOptimistic принимает каноничное состояние и функцию слияния, а возвращает значение для рендера плюс addOptimistic. Он никогда не форкает и не пишет состояние: вне висящего transition возвращается каноничное состояние дословно; после addOptimistic(input) внутри transition (form actions считаются) возвращается mergeFn(canonical, input) — оверлей времени рендера — до завершения этого transition, после чего оверлей истекает и рендерится то, чем каноничное состояние стало. Успех означает, что action обновил каноничное состояние из ответа сервера, и истекающий оверлей бесшовно сменяется подтверждённой истиной; провал означает, что каноничное состояние не менялось, и одного истечения достаточно, чтобы надежда исчезла — откат существует без кода отката. Самодельная версия ведёт вторую записываемую линию времени: вставить в локальное состояние, затем catch-и-splice на провале. Этот splice — императивная запись, гоняющаяся с каждым другим писателем того же массива: refetch со снапшотом, снятым до вставки, может приземлиться первым, и splice отработает по чужому списку — ровно инцидент исчезающего комментария. В оверлейной модели одно записываемое состояние и одна эфемерная проекция, привязанная к transition, — второй записи, которую можно перепутать местами, не существует. Помечайте оптимистичные элементы (pending: true), чтобы UI приглушал их — честно показывая, какие элементы обещания, а какие факты." },
    { q: "Чек-лист сеньорского ревью оптимистичной мутации: какие три гарантии должны стоять помимо вызова useOptimistic, и когда ревьюер должен отвергнуть оптимизм целиком?", a: "Первая — дисциплина сверки: каноничное состояние пересобирается из ответа сервера или refetch-а — никогда повышением оптимистичного объекта — потому что сервер может нормализовать текст, выдать настоящие id и timestamp, навесить модерацию; и слияние обязано сшивать оптимистичный элемент с подтверждённым по клиентскому ключу, который сервер возвращает эхом, иначе медленный успех ненадолго отрендерит оба — приглушённое предсказание и настоящий элемент. Вторая — идемпотентность: ключ, сгенерированный на намерение (crypto.randomUUID() в момент жеста), едет с мутацией и идентичен на ретрае; сервер хранит обработанные ключи и переигрывает записанный результат вместо повторного исполнения — это единственный фикс, покрывающий ретрай после потерянного ответа, когда клиент в принципе не может знать, что первая попытка доехала; isPending-блокировка и дебаунс лишь сужают окно. Третья — честная подача pending: оптимистичные элементы визуально помечены, и редкое изъятие объяснимо. Отвергать оптимизм — когда пари перевёрнуто: платежи и юридически значимые подтверждения (отозванный чек — ложь, на которую пользователь может успеть опереться), деструктивные и трудно обратимые операции (удаление аккаунта, отмены) и мутации с рутинной долей отказов — модерация контента, конкурентный инвентарь, выбор мест, — где UI будет постоянно забирать показанное обратно. Там сеньорский выбор — честный pending: отключить, показать прогресс, подтвердить по истине." },
  ]}
/>

<Recap lang="ru">
Оптимистичный UI схлопывает воспринимаемую задержку мутации с сетевого круга до нуля, рендеря желаемое состояние сразу — а useOptimistic в React 19 делает опасную половину этой сделки структурной. Хук возвращает оверлей, а не форк: вне висящего transition вы видите каноничное состояние; после addOptimistic внутри transition — слияние каноничного состояния с вашей надеждой, пока transition не завершится и оверлей не истечёт. Успех заменяет надежду истиной сервера, потому что action обновил каноничное состояние; провал не требует кода отката вовсе, потому что ничего не было записано — именно поэтому самодельная гонка catch-и-splice (исчезающий комментарий, перезаписанный протухшим refetch-ем) в этой модели невыразима. Что остаётся на вас: сверка — ответ сервера всегда замещает предсказание: пересобирайте каноничное состояние из ответа, сшивайте элементы по клиентскому ключу-эху, не повышайте оптимистичный объект; идемпотентность — ключ на жест, хранимый сервером, превращает неизбежный двойной сабмит (потерянный ответ, второе нажатие, двойной лайк со счётчиком 2) в переигранный результат вместо второго исполнения; и суждение — оптимизм есть пари, что успех почти гарантирован, а изъятие дёшево. Берите пари для лайков, подписок, перестановок, отметок прочитанным. Отказывайтесь для платежей, деструктивных операций и любой мутации с рутинными отказами — UI, постоянно отбирающий показанное, тратит доверие, ради которого строился.
</Recap>
