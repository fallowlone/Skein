---
concepts:
- react-form-validation
deepensInto: []
estMin: 18
lang: ru
lessonType: topic
level: middle
order: 2
prereqs: []
slug: "02-form-state-and-validation"
sources:
- https://react.dev/learn/reacting-to-input-with-state
- https://react.dev/reference/react-dom/components/input
- https://react.dev/reference/react-dom/components/form
spiral: []
status: ready
summary: 'Форма — три карты (values, errors, touched) плюс политика тайминга: валидируем на blur, перепроверяем на change после касания, отсекаем на submit. Одна zod-схема на клиент и сервер; ошибкам нужны aria-describedby и управление фокусом, иначе часть пользователей их не найдёт.'
title: 'Состояние формы и валидация: values, errors, touched — и когда запускать проверку'
track: "react"
unit: "04-forms-and-mutations"
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
Воронка регистрации теряла 31% на шаге email, и записи сессий объяснили это безжалостно. Форма валидировала по onChange с первого символа: пользователь набирал «j» — поле мгновенно краснело: *Введите корректный email* — и оставалось красным, крича на него, все одиннадцать символов до конца ввода. Кто-то останавливался перечитывать набранное; кто-то решал, что его адрес «не принимают», и уходил. Первый фикс команды — валидировать только на submit — нанёс противоположную травму: пользователи заполняли девять полей, жали «Отправить» и получали шесть ошибок разом, причём вьюпорт был проскроллен вниз и первое сломанное поле осталось за кадром. Худший репорт прислал незрячий пользователь со скринридером: submit «ничего не делал» — текст ошибки рендерился отдельным красным div-ом, который ни одна ассистивная технология не связывала ни с каким инпутом, а фокус оставался на кнопке. Три бага, одна корневая причина: команда считала валидацию булевым переключателем — вкл или выкл, — тогда как на деле это машина состояний над тремя картами и политикой тайминга.
</Hook>

<Crux>Храните values, errors, touched. Валидация на blur, перепроверка на change после касания, барьер на submit. Одна zod-схема на всё.</Crux>

<Explanation>

## Форма: values, errors, touched

Все библиотеки форм сходятся к одним и тем же трём параллельным картам, потому что те отвечают на три независимых вопроса. `values` — что пользователь ввёл (controlled-состояние из прошлого урока). `errors` — что сейчас не так с каждым полем; выводится валидатором из values. `touched` — какие поля пользователь *закончил трогать*; ставится, когда поле получает `blur`. Третью карту самодельные формы забывают — а именно она делает валидацию вежливой: ошибку стоит *показывать* только для тронутого поля. Валидация может выполняться сколь угодно часто; `touched` — это шлюз показа, отделяющий «это значение невалидно» (факт о данных) от «сказать об этом пользователю сейчас» (UX-решение).

```jsx
const schema = z.object({
  email: z.string().email("Enter a valid email"),
  password: z.string().min(12, "At least 12 characters"),
});

function Signup() {
  const [values, setValues] = useState({ email: "", password: "" });
  const [touched, setTouched] = useState({});
  const result = schema.safeParse(values);
  const errors = result.success
    ? {}
    : Object.fromEntries(
        result.error.issues.map((i) => [i.path[0], i.message])
      );

  const showError = (field) => touched[field] && errors[field];

  return (
    <form noValidate onSubmit={/* gate on result.success, see below */ undefined}>
      <input
        value={values.email}
        onChange={(e) => setValues({ ...values, email: e.target.value })}
        onBlur={() => setTouched({ ...touched, email: true })}
        aria-invalid={Boolean(showError("email"))}
        aria-describedby={showError("email") ? "email-error" : undefined}
      />
      {showError("email") ? (
        <p id="email-error" role="alert">{errors.email}</p>
      ) : null}
    </form>
  );
}
```

Заметьте: `errors` здесь — *производное состояние*, вычисляемое из `values` при рендере и никогда не хранимое отдельно. Хранение ошибок в собственном `useState` приглашает классический рассинхрон: значение поменялось, протухшая ошибка продолжает показываться.

## Тайминг: три политики и кого каждая наказывает

**onChange с первого символа** наказывает людей за незаконченную мысль: каждое поле «невалидно», пока не дописано, и форма кричит во время набора. **Только onSubmit** наказывает за доверие: десять полей усилий — и стена ошибок, часто за пределами экрана. **onBlur** — компромисс, к которому раз за разом приходят полевые UX-исследования: уходя из поля, пользователь сигналит «я закончил», и вердикт в этот момент честен.

Продакшен-политика гибридная, её называют *reward early, punish late* — награждай рано, наказывай поздно: валидируем поле при первом blur; как только поле тронуто **и содержит ошибку** — перепроверяем на каждый change, чтобы ошибка исчезала в момент исправления (награда обязана быть мгновенной: заставлять снова делать blur, чтобы убрать уже исправленную ошибку, читается как поломка); и прогоняем полную схему на submit как финальный шлюз, потому что какие-то поля могли вообще не быть тронуты. Цифры с полей: замена агрессивной посимвольной валидации на blur-гибрид — одно из самых стабильно положительных изменений UX форм, которые команды измеряют, именно потому, что преждевременные ошибки читаются как отказ.

<Inset kind="why" lang="ru">
Зачем вообще шлюз на submit, если каждое поле валидируется на blur? Две причины. Нетронутые поля: пользователь может протабать мимо обязательного поля, не набрав ничего — нет события change, возможно нет осмысленного blur, — и пополевая валидация не запускалась. И межполевые правила: «пароли должны совпадать» или «дата конца позже даты начала» не имеют единственного «своего» поля; submit — единственный момент, когда объект целиком судится честно. Обработчик submit перегоняет полную схему, помечает все поля touched (чтобы каждая ошибка стала видимой) и отказывается отправлять, если что-то не прошло.
</Inset>

## Одна схема, две среды исполнения

Клиентская валидация — это UX-приём, а не безопасность: клиент — машина атакующего, и любой человек с DevTools отправит в ваш API что угодно. Значит, сервер обязан валидировать в любом случае — что исторически означало писать каждое правило дважды и смотреть, как они расползаются: клиент разрешает 100 символов, сервер обрезает на 80, и пользователь получает тост «сохранено» для молча искалеченных данных. Схемные библиотеки растворяют дублирование. Zod-схема — обычное JavaScript-значение: определите её один раз в общем модуле, вызывайте `safeParse` на вводе пользователя в браузере ради мгновенной обратной связи и `safeParse` на теле запроса на сервере как настоящий шлюз. Одни правила, одни сообщения, один источник истины — а TypeScript выводит тип формы из схемы (`z.infer`), так что и типы расползтись не могут. Остающаяся асимметрия легитимна: сервер может проверить то, что клиенту недоступно (уникальность email по базе), и такие ошибки возвращаются асинхронно и вливаются в ту же карту `errors`.

## Ошибки, которые пользователь способен найти

Сообщение об ошибке, визуально прилегающее к инпуту, для скринридера — посторонний абзац где-то в документе. Это чинят три провода. `aria-invalid` на инпуте объявляет его состояние. `aria-describedby`, указывающий на `id` элемента с ошибкой, заставляет ассистивные технологии читать сообщение *при фокусе на инпуте* — связь явная, а не пространственная. И при провалившемся submit — переместить фокус: либо на первый невалидный инпут (пользователь приземляется туда, где работа), либо на сводку ошибок сверху со ссылками на каждое сломанное поле. Без управления фокусом неудачный submit — тишина: зрячий видит «где-то красное», пользователь скринридера не слышит вообще ничего. Атрибут `noValidate` на форме тоже важен: он отключает нативные браузерные пузыри, чтобы ваши доступные стилизованные ошибки были единственным голосом, а не вторым из двух несогласных.

<Quiz
  id="02-form-state-and-validation-quiz-1"
  lessonSlug="02-form-state-and-validation"
  lang="ru"
  question="Почему продакшен-форма держит отдельную карту touched, а не показывает просто всё, что лежит в errors?"
  choices={[
    { label: "Потому что валидность и показ — разные вопросы: errors описывает данные в любой момент, а touched решает, когда вердикт честно показать — без него полунабранный email помечен невалидным с первого символа", correct: true },
    { label: "Потому что валидация слишком медленная, чтобы гонять её на каждый символ", misconception: "Zod-парс объекта из десяти полей занимает микросекунды — производительность тут ни при чём. Валидировать можно непрерывно и дёшево; touched управляет таймингом показа, а не стоимостью вычислений." },
    { label: "Потому что ошибки можно вычислить только после потери фокуса полем", misconception: "Ошибки выводятся из values и вычислимы в любой момент, включая время набора. Событие blur не «включает» валидацию; оно сигналит, что пользователь закончил поле, — и именно это делает показ вердикта вежливым." },
    { label: "Потому что серверу нужно знать, какие поля пользователь реально редактировал", misconception: "touched — чисто клиентский UX-шлюз и никуда не отправляется. Сервер перевалидирует весь присланный объект независимо от того, что трогал пользователь, — никакому клиентскому состоянию он доверять не может." },
  ]}
/>

<Quiz
  id="02-form-state-and-validation-quiz-2"
  lessonSlug="02-form-state-and-validation"
  lang="ru"
  question="Команда тщательно валидирует zod-ом в браузере и пропускает серверную валидацию, чтобы не дублировать правила. Какова реальная экспозиция?"
  choices={[
    { label: "До API долетает что угодно: клиентская валидация выполняется на машине атакующего и тривиально обходится DevTools или curl — паттерн общей схемы существует именно затем, чтобы сервер применял те же правила без дублирования", correct: true },
    { label: "Никакой, если zod-схема достаточно строгая", misconception: "Строгость проверки, которую атакующий может удалить, не значит ничего — браузерный код полностью под контролем пользователя. Запрос через curl вообще не исполняет ваш клиентский бандл, и API получает то, что отправитель решил положить в тело." },
    { label: "Только риск расхождения текстов ошибок между клиентом и сервером", misconception: "Дрейф сообщений — косметический симптом написания правил дважды; полный пропуск серверной стороны — другая и худшая проблема: второго шлюза нет, и сломанные или злонамеренные payload-ы идут прямо в бизнес-логику и базу." },
    { label: "Экспозиция ограничена, потому что HTTPS не даёт подделать запрос", misconception: "HTTPS защищает канал между клиентом и сервером от третьих лиц; он ничего не делает с тем, что сам клиент враждебен. Легитимный пользователь с DevTools или любой скрипт с URL вашего эндпоинта говорят на идеальном HTTPS." },
  ]}
/>

</Explanation>

<FlowDiagram
  label="Жизненный цикл поля: набор обновляет values, blur помечает touched и открывает показ ошибки, исправление перепроверяется на change, submit гонит полную схему как финальный шлюз — а сервер валидирует снова"
  hue="var(--d-frontend)"
  perRow={3}
  nodes={[
    { id: "typing", label: "Набор", sub: "values обновляются" },
    { id: "blur", label: "Blur", sub: "touched = true" },
    { id: "show", label: "Ошибка показана", sub: "touched && invalid" },
    { id: "fix", label: "Перепроверка на change", sub: "награда мгновенно" },
    { id: "submit", label: "Шлюз submit", sub: "вся схема, фокус на 1-ю ошибку" },
    { id: "server", label: "Сервер перевалидирует", sub: "та же zod-схема" },
  ]}
  edges={[
    { from: "typing", to: "blur", label: "уход из поля" },
    { from: "blur", to: "show", label: "если невалидно" },
    { from: "show", to: "fix", label: "пользователь правит" },
    { from: "fix", to: "submit", label: "все поля" },
    { from: "submit", to: "server", label: "POST" },
    { from: "server", to: "show", label: "async-ошибки вливаются" },
  ]}
  caption="Валидация выполняется сколь угодно часто; touched решает, когда показывать вердикты. Blur открывает шлюз для поля, перепроверка на change мгновенно гасит исправленные ошибки, submit судит объект целиком и переносит фокус на первый провал, а сервер гонит ту же общую схему — клиентский шлюз это UX, а не безопасность."
/>

<KeyTakeaway>Форма — это values, errors и touched, причём errors выводится из values при рендере и никогда не хранится рядом, чтобы не рассинхронизироваться. Политика тайминга, переживающая контакт с пользователями: показывать ошибку поля после первого blur, перепроверять на change, пока ошибка видна, чтобы исправление гасило её мгновенно, и отсекать submit полной схемой с пометкой всех полей touched. Схему определите один раз в zod и гоняйте в обеих средах — клиентский парс это UX, серверный закон. Подключайте каждую ошибку через aria-invalid и aria-describedby и переносите фокус на первый провал при submit — иначе ошибка существует только для тех, кто видит красное.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="02-form-state-and-validation-retrieval"
  lang="ru"
  questions={[
    { q: "Опишите гибридную политику тайминга валидации и что каждая чистая политика (всегда onChange, только onSubmit, только onBlur) делает не так.", a: "onChange с первого символа помечает недописанные значения — поле красное, пока пользователь ещё думает; записи сессий показывают, что это читается как отказ и гонит людей из воронки. Только onSubmit позволяет вложиться в форму целиком и вываливает все ошибки разом, часто с первым сломанным полем за пределами экрана. Чистый onBlur честен при первом контакте, но протухает после: когда ошибка уже показана, заставлять пользователя снова уходить из поля, чтобы её погасить, делает исправленное поле всё ещё «сломанным». Гибрид — reward early, punish late — склеивает хорошие половины: валидируем поле при первом blur (пользователь просигналил, что закончил), затем для любого поля, которое тронуто и сейчас в ошибке, перепроверяем на каждый change, чтобы ошибка исчезла в момент валидности, и наконец перегоняем всю схему на submit, помечая все поля touched, потому что нетронутые поля не судились по отдельности, а межполевые правила (подтверждение пароля, диапазоны дат) осмыслены только над объектом целиком. Шлюз submit владеет и фокусом: перенести его на первый невалидный инпут или на сводку ошибок — чтобы провал был объявлен, а не нем." },
    { q: "Почему паттерн общей zod-схемы строго лучше раздельной клиентской и серверной валидации, и какая асимметрия легитимно остаётся?", a: "Раздельные реализации одних правил дрейфуют — клиент разрешает то, что сервер отвергает, или хуже: сервер молча принимает то, что клиент якобы заблокировал, и каждое изменение правила нужно вносить дважды. Zod-схема — обычное значение, импортируемое обеими средами: браузер вызывает safeParse на вводе пользователя ради мгновенной вежливой обратной связи, а сервер вызывает safeParse на теле запроса как настоящий шлюз безопасности — потому что клиентский шлюз работает на машине, полностью подконтрольной пользователю, и обходится DevTools или голым curl. Один модуль — единственный источник истины для правил, сообщений и — через z.infer — TypeScript-типов данных формы, так что типы тоже не могут разойтись с валидацией. Легитимная асимметрия: сервер проверяет то, что клиент не может — уникальность email по базе, правила авторизации; такие проверки порождают ошибки, возвращающиеся асинхронно после submit, и вливаются в ту же карту errors, которую наполняет клиентский парс, отображаясь теми же компонентами с touched-шлюзом и aria-проводкой." },
  ]}
/>

<Recap lang="ru">
Продакшен-состояние формы — три карты с разными работами: values хранит ввод пользователя, errors выводится валидатором из values на каждом рендере (отдельно не хранится — и потому не рассинхронизируется), touched фиксирует, какие поля пользователь закончил, — это шлюз показа, отделяющий факт «значение невалидно» от решения «сказать пользователю сейчас». Политика тайминга, уважающая обе половины: поле показывает ошибку после первого blur; будучи тронутым и в ошибке, перепроверяется на каждый change, чтобы исправление гасило ошибку мгновенно; submit перегоняет полную схему со всеми полями touched, ловя нетронутые поля и межполевые правила, и переносит фокус на первый провал или сводку ошибок. Правила валидации живут один раз — в zod-схеме, общей для двух сред: браузерный парс — быстрая обратная связь, серверный — настоящий шлюз (клиентские проверки выполняются на машине атакующего и не доказывают ничего), а z.infer приваривает TypeScript-типы к тому же источнику истины; по-настоящему серверные проверки вроде уникальности email вливают свои асинхронные ошибки в ту же карту. Доступность — это проводка, а не стилизация: noValidate глушит нативные пузыри, aria-invalid на инпуте, aria-describedby на id элемента с ошибкой, чтобы сообщение объявлялось вместе с полем, и явное управление фокусом при провале submit — без этого красный текст невидим для всех, кто на него не смотрит.
</Recap>
