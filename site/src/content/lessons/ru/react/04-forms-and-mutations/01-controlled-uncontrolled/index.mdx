---
concepts:
- react-forms
deepensInto: []
estMin: 17
lang: ru
lessonType: topic
level: middle
order: 1
prereqs: []
slug: "01-controlled-uncontrolled"
sources:
- https://react.dev/reference/react-dom/components/input
- https://react.dev/learn/reacting-to-input-with-state
- https://react.dev/reference/react-dom/components/form
spiral: []
status: ready
summary: 'Controlled-инпут хранит значение в state React и ререндерится на каждый символ; uncontrolled оставляет его в DOM и читает через ref или FormData на submit. Каждый режим выигрывает в своих формах — а value без onChange или смена режима на лету дают реальные продакшен-баги.'
title: 'Controlled и uncontrolled инпуты: кто владеет значением и сколько это стоит'
track: "react"
unit: "04-forms-and-mutations"
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";
import Infographic from "~/components/diagram/Infographic.astro";
import valueOwnership from "./infographics/value-ownership.svg?raw";

<Hook>
Тикеты в поддержку пошли во вторник в 9:14: «поле для промокода сломано — печатаю, и ничего не появляется». Команда чекаута не могла воспроизвести баг, потому что в dev-сборке сигнал тонул в шуме: консоль кричала уже два спринта — *You provided a `value` prop to a form field without an `onChange` handler. This will render a read-only field.* Во время рефакторинга кто-то поднял промокод в Redux, чтобы показывать его в сводке заказа, привязал `value` к стору — и удалил локальный `onChange` как «мёртвый код», потому что в ручном тесте сводка всё ещё обновлялась (тестировали вставкой через автозаполнение браузера, которое заодно тоже сломали). Инпут теперь контролировался значением, которое никто никогда не обновлял: React послушно сбрасывал DOM обратно к протухшему значению из стора после каждого нажатия клавиши. Три строки кода, одиннадцать дней в продакшене, измеримая просадка применённых промокодов. У этого класса багов есть имя — полуконтролируемый инпут — и существует он потому, что у React две модели владения состоянием формы, и компонент тихо деградирует, когда вы не выбираете ни одну.
</Hook>

<Crux>Controlled: значением владеет state, ререндер на каждый символ. Uncontrolled: владеет DOM, чтение через ref на submit. Режим не менять.</Crux>

<Explanation>

## Два владельца, один инпут

У каждого поля формы ровно один источник истины, и React заставляет выбрать, какой именно. В **controlled**-инпуте значением владеет state React: проп `value` прибивает то, что показывает DOM, а `onChange` — единственная дверь, через которую нажатие клавиши становится видимым: событие срабатывает, вы вызываете `setState`, React ререндерит, и новое значение стекает обратно в DOM. DOM-инпут низведён до тупого дисплея вашего состояния. В **uncontrolled**-инпуте значением владеет DOM — ровно как на обычной HTML-странице: браузер сам отслеживает ввод, React не слышит отдельных нажатий, а результат вы читаете тогда, когда он нужен — через `ref` или весь разом через `new FormData(form)` в обработчике submit. `defaultValue` (не `value`) задаёт начальный текст, не претендуя на владение.

Различие не стилистическое; оно решает, *когда выполняется ваш код*. Controlled означает ререндер компонента на каждый символ — это входной билет. Uncontrolled означает один рендер, а ваш код выполняется в момент submit.

```jsx
function CouponControlled() {
  const [code, setCode] = useState("");
  // Ре-рендер на каждое нажатие; можно валидировать, трансформировать, блокировать вживую.
  return (
    <input
      value={code}
      onChange={(e) => setCode(e.target.value.toUpperCase())}
    />
  );
}

function CheckoutUncontrolled() {
  // Рендерится один раз; DOM хранит значения до момента submit.
  function handleSubmit(e) {
    e.preventDefault();
    const data = new FormData(e.currentTarget);
    submitOrder(data.get("coupon"), data.get("email"));
  }
  return (
    <form onSubmit={handleSubmit}>
      <input name="coupon" defaultValue="" />
      <input name="email" type="email" />
      <button>Pay</button>
    </form>
  );
}
```

## Что controlled покупает и какой счёт выставляет

Выбирайте controlled, когда UI должен *реагировать на значение по мере ввода*: живые сообщения валидации, счётчик символов, форматирование на лету (номер карты группами по четыре), фильтрация списка на каждый символ, активация кнопки submit только при валидной форме, синхронизация двух виджетов одним значением. Ничего из этого невозможно, если React узнаёт значение только на submit.

Счёт приходит в виде работы рендера. Один controlled-инпут, ререндерящий собственный маленький компонент, — ничто: рендер — это вызов функции, производящий элементы, сильно меньше миллисекунды. Провальный режим структурный: команды поднимают состояние всех полей в один объект на вершине формы из 60 полей, и каждый символ в любом поле ререндерит всё дерево формы. При 5–15 мс на полный рендер формы это ещё влезает в кадр; добавьте пару дорогих дочерних компонентов — и вы на 40–80 мс на символ, и набор текста заметно заикается, потому что каждое нажатие теперь запускает работу размером с layout. Лечение тоже структурное: держите состояние поля в наименьшем компоненте, которому оно нужно, мемоизируйте тяжёлых соседей или перестаньте контролировать поля, которые никто не читает до submit.

Выбирайте uncontrolled, когда форма «сабмитной» формы: ничто не зависит от промежуточных значений, нужна минимальная цена ререндеров, вы интегрируете не-React-виджет, который сам мутирует DOM, или работаете с файлами — `<input type="file" />` *всегда* uncontrolled, потому что его значение может установить только пользователь, никогда код.

<Infographic
  svg={valueOwnership}
  label="Сравнение двух моделей владения бок о бок. Controlled: значением владеет state React, ререндер на каждый символ, покупает живую валидацию/форматирование/счётчики/зависимые кнопки, стоит 5-15 мс на полный рендер формы (40-80 мс с тяжёлыми детьми), выбирается, когда UI должен реагировать до submit. Uncontrolled: значением владеет DOM, рендерится один раз, чтение через ref или FormData, минимальная цена ререндеров, выбирается для сабмитных форм и файловых инпутов."
  caption="Controlled платит посимвольным рендером за реакцию на значение вживую; uncontrolled рендерится один раз и читает на submit. Подбирайте владельца под то, нужно ли какому-то UI значение до submit."
  hue="var(--d-infra)"
/>

<Inset kind="why" lang="ru">
Зачем React вообще навязывает выбор, если обычный HTML просто позволяет DOM хранить значение? Потому что базовый контракт React — UI есть функция состояния, а DOM-узел, мутирующий сам себя на каждый символ, — это состояние, живущее вне этой функции. Controlled-инпуты восстанавливают контракт, низводя DOM до проекции состояния React. Uncontrolled-инпуты сохраняют нативное поведение DOM и принимают, что этот кусок состояния невидим для рендеринга. Легитимны оба; нелегитимен инпут, где два владельца дерутся, — а это и есть полуконтролируемый баг.
</Inset>

## Полуконтролируемый инпут: как режимы портят друг друга

Почти все баги форм в этой области — два сценария. **Первый: `value` без `onChange`.** Инпут становится контролируемым константой. Пользователь печатает, DOM на мгновение показывает символ, React ререндерит (или просто сбрасывает узел) обратно к прибитому значению — поле съедает нажатия. React один раз предупреждает в консоли и предлагает легальные выходы: добавить `onChange`, поставить `readOnly`, если поле действительно только для чтения, или использовать `defaultValue`, если вы хотели лишь задать начальное значение.

**Второй: смена режима в течение жизни компонента.** Инпут монтируется uncontrolled (`value` равен `undefined` — например, данные профиля ещё не загрузились), а позже получает строку, когда fetch разрешился. React пишет: *A component is changing an uncontrolled input to be controlled.* Опасность не в тексте предупреждения, а в том, что всё набранное пользователем за uncontrolled-окно молча уничтожается, когда проп value забирает владение. Обратное переключение (controlled → uncontrolled, value становится `undefined`) бросает поле с замороженным последним значением. Правило: инпут controlled или uncontrolled *на всю свою жизнь*. Если начальное состояние может отсутствовать — инициализируйте `""`, никогда `undefined`, или не рендерите инпут, пока данных нет.

<Quiz
  id="01-controlled-uncontrolled-quiz-1"
  lessonSlug="01-controlled-uncontrolled"
  lang="ru"
  question="Форма профиля инициализируется через const [name, setName] = useState(user.name), где user.name равен undefined до завершения fetch, а затем становится строкой. У инпута value={name} и корректный onChange. Что на самом деле идёт не так?"
  choices={[
    { label: "Инпут монтируется uncontrolled (value undefined), затем переключается в controlled, когда приходит fetch — React предупреждает, а всё набранное в этот промежуток затирается загруженным значением", correct: true },
    { label: "Ничего — onChange есть, значит инпут полностью контролируемый с самого начала", misconception: "Контролируемость определяется пропом value, а не onChange. value={undefined} говорит React вообще не управлять узлом; обработчик обновляет state, который пока никто не прибивает к DOM. Переключение режима происходит в момент, когда value становится строкой." },
    { label: "React бросит ошибку на рендере, потому что value не может быть undefined", misconception: "undefined — легальное значение пропа value, оно означает «uncontrolled». React никогда не бросает за это исключение; он лишь позже пишет предупреждение, когда тот же узел переходит из uncontrolled в controlled — именно поэтому баг переживает ревью." },
    { label: "Инпут навсегда останется uncontrolled, потому что режим фиксируется при монтировании", misconception: "Режим должен быть зафиксирован на всю жизнь, но React этого не форсит — это контракт разработчика. Когда value меняется с undefined на строку, React забирает управление и затирает промежуточное значение DOM. Предупреждение существует потому, что переключение возможно, а не невозможно." },
  ]}
/>

<Quiz
  id="01-controlled-uncontrolled-quiz-2"
  lessonSlug="01-controlled-uncontrolled"
  lang="ru"
  question="Страховая форма на 60 полей держит все поля в одном объекте useState в корне формы; ввод в любое поле заметно лагает. Каков механизм лага?"
  choices={[
    { label: "Каждое нажатие обновляет корневой объект состояния, и React ререндерит всё дерево формы — все 60 полей с детьми — на каждый символ; как только эта работа превышает кадр, набор заикается", correct: true },
    { label: "Парсинг FormData медленный для больших форм", misconception: "FormData в controlled-форме вообще не участвует — это инструмент чтения uncontrolled-стороны, применяемый один раз на submit. Здесь же цена платится на каждое нажатие, во время набора, что указывает на работу рендера, а не на парсинг при отправке." },
    { label: "DOM-инпут стал read-only, и браузер повторяет каждое нажатие", misconception: "Это полуконтролируемый баг (value без onChange), и его симптом — символы не появляются вовсе, а не появляются медленно. Лаг при корректном тексте указывает на объём ререндеров, а не на владение." },
    { label: "Controlled-инпуты пишут в DOM синхронно, что форсит layout на каждый символ", misconception: "Обновление value инпута дёшево и само по себе layout не форсит. Дорого вызвать функции 60 полей (плюс их детей) на каждое нажатие — бюджет кадра съедает объём рендера, а не одна запись в DOM." },
  ]}
/>

</Explanation>

<FlowDiagram
  label="Нажатие клавиши идёт двумя путями: через state React и ререндер в controlled-модели — или прямо в DOM с однократным чтением на submit в uncontrolled-модели"
  hue="var(--d-frontend)"
  perRow={3}
  nodes={[
    { id: "key", label: "Нажатие", sub: "пользователь печатает" },
    { id: "onchange", label: "onChange", sub: "controlled-путь" },
    { id: "state", label: "setState", sub: "state владеет значением" },
    { id: "render", label: "Ререндер", sub: "value течёт вниз" },
    { id: "dom", label: "DOM хранит значение", sub: "uncontrolled-путь" },
    { id: "submit", label: "Чтение на submit", sub: "ref / FormData" },
  ]}
  edges={[
    { from: "key", to: "onchange", label: "controlled" },
    { from: "onchange", to: "state", label: "setCode(v)" },
    { from: "state", to: "render", label: "на каждый символ" },
    { from: "key", to: "dom", label: "uncontrolled" },
    { from: "dom", to: "submit", label: "один раз, на submit" },
    { from: "render", to: "dom", label: "value прибивает" },
  ]}
  caption="Controlled: каждое нажатие гонит onChange → setState → ререндер, и проп value прибивает DOM к состоянию — полная власть над значением, оплаченная посимвольно. Uncontrolled: DOM сохраняет нативное поведение, React читает результат один раз на submit. Полуконтролируемый баг — взять проп value и не замкнуть петлю обратно."
/>

<KeyTakeaway>Один инпут — один владелец. Controlled: state React и есть значение, onChange → setState → ререндер на каждый символ; это покупает живую валидацию, форматирование и зависимый UI ценой посимвольных рендеров, которая становится реальной в широких формах. Uncontrolled: значением владеет DOM, вы читаете его один раз через ref или FormData на submit — дёшево и правильно для «сабмитных» форм и файловых инпутов. value без onChange заставляет поле съедать нажатия, а скачок между undefined и строкой переключает владение на лету, уничтожая ввод пользователя: инициализируйте controlled-состояние пустой строкой и держите один режим всю жизнь инпута.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="01-controlled-uncontrolled-retrieval"
  lang="ru"
  questions={[
    { q: "Проследите одно нажатие клавиши через controlled-инпут и через uncontrolled — какой код выполняется, когда, и кто держит значение на каждом шаге?", a: "Controlled: пользователь нажимает клавишу, DOM генерирует событие, ваш onChange читает e.target.value и вызывает setState. React ререндерит компонент, новое состояние попадает в проп value, и React приводит DOM-узел в соответствие — значением владеет state, DOM лишь проекция. Эта петля крутится на каждый символ, поэтому всё, что лежит на пути рендера, оплачивается посимвольно. Uncontrolled: нажатие уходит прямо в DOM-узел, который сам ведёт своё значение, как в обычном HTML; во время набора React не выполняет вообще ничего. На submit вы читаете результат один раз — через ref (inputRef.current.value) или всю форму разом через new FormData(e.currentTarget) по атрибутам name. defaultValue задаёт начальный текст, не забирая владение. Правило выбора: если какой-то UI должен реагировать на значение до submit (валидация, счётчики, форматирование, активация кнопок) — нужен controlled; если форма «сабмитная» — uncontrolled дешевле и проще, а файловые инпуты всегда uncontrolled, потому что коду запрещено устанавливать их значение." },
    { q: "Назовите два полуконтролируемых сценария отказа, их видимые пользователю симптомы и правило, предотвращающее оба.", a: "Первый: проп value без обработчика onChange. Инпут контролируется константой — пользователь печатает, React сбрасывает узел к прибитому значению, поле съедает нажатия. React предупреждает и предлагает легальные формы: добавить onChange, или readOnly для действительно нередактируемого поля, или defaultValue, если вы хотели лишь задать начальный текст. Второй: смена режима в течение жизни инпута — обычно value={undefined} при монтировании (данные не загрузились), позже становящийся строкой. React пишет предупреждение uncontrolled-to-controlled, и — хуже предупреждения — всё набранное за uncontrolled-окно молча затирается в момент, когда value забирает владение; обратное переключение вместо этого замораживает поле. Правило профилактики в одно предложение: инпут controlled или uncontrolled на всю свою жизнь. Практически — инициализируйте controlled-состояние пустой строкой, а не undefined, или не рендерите инпут, пока его данных нет. Оба сценария — драка за владение: два писателя, один DOM-узел — и оба невидимы в беглом ручном тесте, поэтому и доезжают до продакшена." },
  ]}
/>

<Recap lang="ru">
React даёт полю формы двух возможных владельцев и требует выбрать одного. Controlled: проп value прибивает DOM к состоянию React, onChange — единственный способ сделать нажатие видимым, и петля «нажатие → setState → ререндер» крутится на каждый символ. Это покупает всё, чему нужно живое значение — валидацию по мере ввода, форматирование, счётчики символов, зависимые кнопки — и выставляет счёт в виде ререндера на нажатие: пренебрежимого для одного маленького компонента и очень реального, когда форма из 60 полей держит всё состояние в корне и ререндерит всё дерево посимвольно; лечение — колокация состояния полей, мемоизация тяжёлых соседей или отказ от контроля над тем, что никто не читает. Uncontrolled: значением владеет DOM с нативным поведением браузера, React ничего не выполняет во время набора, а результат читается один раз на submit через ref или FormData; defaultValue задаёт значение, не владея им, файловые инпуты всегда uncontrolled. Два продакшен-класса багов — это драки за владение: value без onChange контролирует инпут константой, и поле съедает нажатия (консольное предупреждение React называет это прямо), а value, скачущий между undefined и строкой, переключает режим инпута на лету, уничтожая всё набранное в промежутке. Инициализируйте controlled-состояние пустой строкой, никогда undefined, и держите один режим всю жизнь инпута. Теперь, когда встретишь поле, молча поглощающее нажатия, или предупреждение о переключении из uncontrolled в controlled, — ты точно знаешь, какое правило владения нарушено и что однострочный фикс исправит.
</Recap>
