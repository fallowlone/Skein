---
concepts:
- react-external-stores
deepensInto: []
estMin: 19
lang: ru
lessonType: topic
level: senior
order: 3
prereqs: []
slug: "03-external-stores"
sources:
- https://react.dev/reference/react/useSyncExternalStore
- https://github.com/pmndrs/zustand
- https://react.dev/learn/scaling-up-with-reducer-and-context
spiral: []
status: ready
summary: 'useSyncExternalStore — контракт чтения состояния, которым React не владеет: subscribe + кешированный getSnapshot, с проверками tearing. Zustand строит на нём селекторы — без провайдера, ре-рендер только при изменении вашего среза — и бьёт контекст на частых записях.'
title: 'Внешние сторы: useSyncExternalStore, подписки-селекторы и tearing'
track: "react"
unit: "06-state-architecture"
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import StackDiagram from "~/components/diagram/StackDiagram.astro";
import Infographic from "~/components/diagram/Infographic.astro";
import contextVsStore from "./infographics/context-vs-store.svg?raw";

<Hook>
У трейдингового дашборда был баг, который не доказывался скриншотами и не скриптовался в QA: изредка итог в шапке портфеля не равнялся сумме видимых строк. Пока кто-то успевал кликнуть, всё снова было верно. Самописный хук стора — `useEffect` для подписки, `useState` для зеркала значения — работал три года. Изменилось вот что: апгрейд на React 18 и `startTransition` вокруг тяжёлого фильтра. Рендеры стали прерываемыми: шапка читала стор в начале длинного прохода рендера, тик цены мутировал стор посреди рендера, и строки читали уже *новое* значение, когда рендер возобновился. Один рендер — две версии истины: **tearing**. Скриншот в итоге прислал клиент-фанат Bloomberg с монитором 144 Гц. Лечением стало удаление сорока строк кода подписки ради одного вызова `useSyncExternalStore` — контракта React ровно для этой проблемы: он сверяет снапшот после рендеринга и ре-рендерит синхронно, если стор уехал из-под него. Баг не воспроизводился, потому что требовал проиграть гонку, о существовании которой никто не знал.
</Hook>

<Crux>useSyncExternalStore читает внешний стор без tearing; сторы вроде zustand ре-рендерят лишь компоненты с изменившимся выводом селектора.</Crux>

К концу урока вы поймёте, почему апгрейд до React 18 может молча сломать хук стора, работавший три года, — и что именно API-контракт требует для защиты от этого.

<Explanation>

## Контракт: subscribe, getSnapshot и проверка на tearing

Состояние, которым владеет React (useState, useReducer), версионируется вместе с fiber-деревом, поэтому конкурентный рендеринг никогда не увидит две версии за один проход. У состояния, живущего *вне* React — модульного синглтона, кеша WebSocket, `navigator.onLine`, — такой гарантии нет: прерываемый рендер может растянуться поверх мутации, и компоненты, отрендеренные до и после неё, разойдутся в одном закоммиченном кадре. Это **tearing** (разрыв — когда один рендер-кадр показывает две разные версии одних и тех же данных), и `useSyncExternalStore` — контракт, который его предотвращает:

```jsx
const value = useSyncExternalStore(
  subscribe,    // (callback) => unsubscribe; звать callback на каждое изменение стора
  getSnapshot,  // () => текущее значение; ОБЯЗАН возвращать кеш, пока ничего не изменилось
  getServerSnapshot // опционально: значение для SSR + гидратации
);
```

`subscribe` регистрирует в вашем сторе собственный колбэк React и возвращает очистку. `getSnapshot` возвращает текущее значение — и **обязан возвращать ту же ссылку, пока стор реально не изменился**, потому что React зовёт его многократно и решает по `Object.is`, сдвинулось ли что-то. Верните свежий объект на каждый вызов (`() => ({ ...state })`) — и React увидит вечно меняющийся стор: печально известная ошибка «The result of getSnapshot should be cached» и бесконечный цикл ре-рендеров. Анти-tearing-механизм: когда обновление стора приходит во время неблокирующего рендера, React перепроверяет снапшоты и заставляет конфликтующее обновление отрендериться **синхронно**, жертвуя time-slicing ради согласованности. У старого хука с подпиской через `useEffect` был и второй изъян помимо tearing: обновления между рендером и подпиской эффекта молча терялись — uSES закрывает и эту щель.

## Zustand: подписки-селекторы поверх контракта

`useSyncExternalStore` намеренно низкоуровневый — один стор, подписка на значение целиком. Zustand — то, как этот паттерн выглядит в виде продукта. Стор создаётся *вне* React как синглтон уровня модуля — **без провайдера**, без контекста, импортируем откуда угодно, включая не-React-код:

```jsx
const useCartStore = create((set, get) => ({
  items: [],
  promo: null,
  addItem: (item) => set((s) => ({ items: [...s.items, item] })),
  total: () => get().items.reduce((sum, i) => sum + i.price, 0),
}));

// Компонент подписывается на СРЕЗ через селектор:
function CartBadge() {
  const count = useCartStore((s) => s.items.length);
  return <span>{count}</span>; // ре-рендер ТОЛЬКО при изменении items.length
}
```

Механизм — **подписка-селектор**: хук прогоняет ваш селектор по снапшоту и ре-рендерит компонент, только когда изменился *вывод* селектора (`Object.is` по умолчанию; кастомное равенство вроде shallow — по желанию). Запись промокода ре-рендерит ноль компонентов, выбравших только `items.length`. Сравните с контекстом: каждый потребитель ре-рендерится на любую смену идентичности значения, точка. Классическое самострельное ранение — селектор, возвращающий свежий объект: `useCartStore((s) => ({ a: s.a, b: s.b }))` — новая ссылка на каждый снапшот, ре-рендер на каждую запись в стор, точность молча исчезла (лечение: два селектора или shallow-равенство).

## Когда внешний стор бьёт контекст — и когда состоянию надо обойти React вовсе

Прежде чем тянуться к внешнему стору, спросите: сколько потребителей ре-рендерится на запись, и как часто эти записи происходят? Это не вопрос моды; за каждой стороной механизм. **Частые записи**: контекст переуведомляет каждого потребителя на запись; стор уведомляет только совпавшие селекторы — при 20 записях в секунду и 50 потребителях это 1 000 рендеров компонентов в секунду против горстки. **Доступ сквозь дерево**: стор — это импорт модуля; обработчики событий, лоадеры роутов, аналитика и код вне иерархии провайдеров могут читать и писать его; контекст требует находиться под провайдером. **Инструменты**: middleware (persist, immer, devtools с историей экшенов) подключаются на границе стора. Контекст по-прежнему выигрывает для редко пишущихся, скоупо-образных значений — тема, локаль, текущий пользователь, — где иерархия провайдеров и *есть* фича, а зависимость — лишний вес.

<Infographic
  svg={contextVsStore}
  label="Сравнение контекста и внешнего стора при одной и той же нагрузке на запись: контекст уведомляет каждого потребителя на запись (~1 000 рендеров/сек при 20 записях на 50 потребителей), требует провайдер в дереве и выигрывает на редких записях со скоупо-образными значениями; внешний стор ре-рендерит только совпавшие селекторы (горстка рендеров на ту же нагрузку), это синглтон модуля, импортируемый сквозь дерево, и несёт инструменты границы стора — persist и devtools"
  caption="Аргумент против контекста для частых записей — механический, не модный: кто уведомляется на запись, тот и задаёт цену — каждый потребитель против лишь совпавших селекторов."
  hue="var(--d-infra)"
/>

<Inset kind="how" lang="ru">
Транзиентные обновления — сеньорский ход для данных на 60 Гц: подписывайтесь императивно и пропускайте рендеринг целиком. `useCartStore.subscribe(...)` (или middleware subscribeWithSelector у zustand) внутри эффекта, с записью в ref или прямой мутацией DOM-узла — позиция курсора, живой график, призрак перетаскивания — обновляется с частотой кадров при нулевом reconciliation. React рендерит компонент один раз; пиксели ведёт стор. Эмпирическое правило: если значение меняется быстрее, чем пользователь способен его прочитать, ему, скорее всего, вообще не место в состоянии React.
</Inset>

<Quiz
  id="03-external-stores-quiz-1"
  lessonSlug="03-external-stores"
  lang="ru"
  question="getSnapshot, реализованный как () => ({ ...store.state }), вызывает ошибку «The result of getSnapshot should be cached» и бесконечный цикл. Каков механизм?"
  choices={[
    { label: "React зовёт getSnapshot многократно и сравнивает результаты через Object.is; свежий объект на каждый вызов выглядит как стор, меняющийся всегда, и React планирует ещё рендер, который снова зовёт getSnapshot — навсегда", correct: true },
    { label: "Spread состояния слишком медленный, React выходит по таймауту и перезапускает рендер", misconception: "Тайминг тут ни при чём — spread маленького объекта стоит наносекунды. Цикл целиком про ссылочную идентичность: React не может знать, что новый объект структурно равен; Object.is говорит «изменилось», а изменившийся стор требует ре-рендера, который порождает следующий новый объект." },
    { label: "getSnapshot разрешено возвращать только примитивы, объекты всегда падают", misconception: "Объекты в порядке — каждый настоящий стор возвращает объекты. Требование в том, чтобы одно и то же состояние стора давало одну и ту же ссылку между вызовами: стор кеширует снапшот и подменяет его только на реальных записях. Сторы с иммутабельными обновлениями удовлетворяют этому естественно." },
    { label: "Ошибка означает, что сломан subscribe: он не отписывается и течёт слушателями, пока React не зациклится", misconception: "Утечки слушателей дают рост памяти и протухшие колбэки, а не эту ошибку. Правило кешированного снапшота — именно про идентичность возврата getSnapshot: React ловит его, вызвав getSnapshot дважды и увидев разные ссылки без изменения стора между ними." },
  ]}
/>

<Quiz
  id="03-external-stores-quiz-2"
  lessonSlug="03-external-stores"
  lang="ru"
  question="Дашборд получает ~20 записей в стор в секунду, подписано ~50 компонентов, каждому важен свой срез. Почему стор в стиле zustand здесь принципиально обгоняет контекст?"
  choices={[
    { label: "Стор прогоняет селектор каждого компонента и ре-рендерит только те, чей вывод селектора изменился, а контекст переуведомляет всех потребителей на каждую смену идентичности значения — порядка 1 000 рендеров/сек против горстки", correct: true },
    { label: "Zustand батчит 20 записей в один рендер в секунду, а контекст батчить не умеет", misconception: "Ни один инструмент не меняет батчинг React; записи в одном таске батчатся в обоих случаях, а 20 отдельных тиков — это всё равно до 20 раундов уведомлений. Разница в том, кто уведомляется за раунд: только совпавшие селекторы — или каждый потребитель контекста." },
    { label: "Сторы живут вне React, поэтому их обновления вообще не вызывают ре-рендеров", misconception: "Подписанные компоненты обязаны ре-рендериться, чтобы показать новые данные — в этом смысл подписки. Жизнь вне React убирает провайдер и даёт доступ из не-React-кода; выигрыш производительности — из точности селекторов, а не из пропуска рендеринга. (Транзиентные подписки рендеринг пропускают, но это опция на подписчика.)" },
    { label: "Контекст медленнее, потому что на каждом рендере глубоко сравнивает значение, решая, кого обновить", misconception: "Контекст никогда не сравнивает глубоко — это дешёвый Object.is по идентичности значения, и сама эта дешевизна и есть проблема: любое изменение уведомляет всех, потому что React не знает, какую часть значения читает каждый потребитель. Селекторы — ровно недостающее объявление намерения." },
  ]}
/>

</Explanation>

<StackDiagram
  label="Слои между стором, живущим вне React, и компонентами на экране: стор в области модуля, контракт подписки useSyncExternalStore, селекторы на компонент и только те компоненты, чей срез изменился"
  hue="var(--d-frontend)"
  layers={[
    { label: "Внешний стор", note: "область модуля · set()" },
    { label: "useSyncExternalStore", note: "subscribe + getSnapshot" },
    { label: "Селекторы", note: "Object.is по выводу" },
    { label: "Компоненты", note: "рендер только изменившихся срезов" },
  ]}
  caption="Запись уведомляет слой подписки; getSnapshot обязан возвращать кешированную ссылку, пока стор реально не изменился — React сравнивает через Object.is. Селекторы решают, какие компоненты ре-рендерить; транзиентная (императивная) подписка минует два нижних слоя и пишет в ref или DOM без рендера; а когда запись приходит посреди рендера, React замечает расхождение снапшотов и ре-рендерит синхронно, а не коммитит порванный кадр."
/>

<KeyTakeaway>useSyncExternalStore — контракт React для состояния, которым он не владеет: subscribe подключает стор к React, getSnapshot обязан возвращать кешированную ссылку до реального изменения, а React перепроверяет снапшоты против tearing — жертвуя time-slicing ради согласованности, когда запись попадает в середину рендера. Zustand превращает это в продукт: сторы-синглтоны и подписки-селекторы — без провайдера, ре-рендер только при изменении выбранного среза. Выбирайте внешний стор вместо контекста для частых записей, доступа сквозь дерево и devtools; для данных на частоте кадров — транзиентная подписка без рендеринга.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="03-external-stores-retrieval"
  lang="ru"
  questions={[
    { q: "Объясните tearing: почему самописная подписка через useEffect + useState может показать две версии стора в одном кадре под конкурентным React, и как useSyncExternalStore это предотвращает?", a: "Состояние, которым владеет React, версионируется с fiber-деревом: проход рендера видит одно согласованное значение везде. У внешнего стора версионирования нет, а конкурентный рендеринг прерываем — рендер в transition может встать на паузу между компонентами. С самописной подпиской компонент A читает стор в начале прохода, запись мутирует стор, пока рендер на паузе, а компонент B читает уже новое значение после возобновления. Оба результата коммитятся в одном кадре: шапка, не согласная со своими строками, — tearing. У зеркала на эффекте есть и вторая щель: записи между рендером и подпиской эффекта теряются целиком. useSyncExternalStore чинит обе: он подписывает в стор собственный колбэк React, а после рендеринга перечитывает getSnapshot и сравнивает — если стор изменился во время неблокирующего рендера, React отбрасывает нарушающий согласованность вывод и рендерит обновление синхронно, разменивая time-slicing на единственную версию истины. Цена контракта: getSnapshot обязан возвращать кешированную ссылку до настоящего изменения, потому что «изменилось» React определяет через Object.is — свежий объект на вызов читается как вечно меняющийся стор и зацикливается." },
    { q: "Пройдите решение: контекст против внешнего стора в стиле zustand против транзиентной подписки — какой механизм делает каждый инструмент правильным и для какого состояния?", a: "Контекст уведомляет каждого потребителя при любой смене идентичности значения; селекторов нет, точность — всё или ничего. Это годится для редко пишущихся, скоупо-образных значений — тема, локаль, текущий пользователь, — где изменения редки, а иерархия провайдеров выражает область видимости; зависимость ничего бы не купила. Стор в стиле zustand — синглтон модуля на базе useSyncExternalStore с подписками-селекторами: каждый компонент объявляет, что читает, и стор ре-рендерит только компоненты с изменившимся выводом селектора (Object.is, опционально shallow). Механизм окупается, когда записи часты (20 записей/сек на 50 потребителей контекста — ~1 000 рендеров/сек; с селекторами рендерятся только задетые срезы), когда доступ сквозной (импорт модуля работает в обработчиках и не-React-коде; провайдер не нужен) и когда нужны инструменты границы стора — persist, история экшенов в devtools, middleware. Известный самострел: селекторы, возвращающие объект, пересоздают ссылку на каждый снапшот и молча обнуляют точность. Транзиентная подписка — третий ярус для данных на частоте кадров: подписывайтесь императивно в эффекте и пишите в ref или DOM-узел, вообще без ре-рендера — курсоры, живые графики, превью перетаскивания. Если пользователь не успевает читать значение с той скоростью, с какой оно меняется, ему не место в состоянии React." },
  ]}
/>

<Recap lang="ru">
Внешнее состояние — всё, что живёт вне useState/useReducer, — ломает инвариант, который React иначе гарантирует: один проход рендера — одна версия каждого значения. Конкурентный рендеринг делает поломку видимой как tearing: прерываемый рендер растягивается поверх записи в стор, и компоненты, закоммиченные в одном кадре, расходятся. useSyncExternalStore — контракт, возвращающий согласованность: subscribe даёт React колбэк изменений в ваш стор; getSnapshot возвращает текущее значение и обязан возвращать ту же ссылку до настоящего изменения, потому что React сравнивает снапшоты через Object.is — свежий объект на вызов означает ошибку кешированного снапшота и бесконечный цикл. Когда запись приходит посреди рендера, React замечает расхождение и ре-рендерит синхронно, сознательно жертвуя time-slicing ради корректности; заодно закрывается щель потерянных обновлений эффектных подписок. Zustand — этот контракт как продукт: сторы — синглтоны модуля, созданные вне React — без провайдера, импортируемые из обработчиков и не-React-кода, — а компоненты подписываются селекторами и ре-рендерятся только при изменении выбранного вывода. Эта точность селекторов — количественный аргумент против контекста для частых записей: уведомлять совпавшие срезы, а не каждого потребителя. За контекстом остаются редко пишущиеся скоупо-образные задачи. А для данных на частоте кадров пропускайте рендеринг целиком: транзиентные подписки пишут в ref или DOM из императивного subscribe — стор ведёт пиксели, React рендерит один раз. Теперь, когда вы увидите дашборд, где несвязанные компоненты мигают на каждый тик, вы первым делом проверите селекторы — и вспомните, что классический самострел — это селектор, возвращающий новый объект на каждый вызов.
</Recap>
