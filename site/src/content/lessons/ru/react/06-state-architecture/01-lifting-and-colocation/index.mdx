---
concepts:
- react-state-colocation
deepensInto: []
estMin: 17
lang: ru
lessonType: topic
level: middle
order: 1
prereqs: []
slug: "01-lifting-and-colocation"
sources:
- https://react.dev/learn/sharing-state-between-components
- https://react.dev/learn/scaling-up-with-reducer-and-context
spiral: []
status: ready
summary: 'Состояние живёт в нижнем компоненте, которому оно нужно. Поднимайте лишь до ближайшего общего владельца: каждый лишний уровень расширяет зону ре-рендера. «Глобально по умолчанию» делает нажатие клавиши рендером всего приложения; композиция через children изолирует состояние.'
title: 'Подъём состояния и колокация: состояние живёт у ближайшего общего владельца'
track: "react"
unit: "06-state-architecture"
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import StackDiagram from "~/components/diagram/StackDiagram.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";

<Hook>
Страница настроек была самым медленным экраном продукта, и никто не мог объяснить почему: это была форма из девяти полей. Профайлер рассказал всё одним флейм-графом — одно нажатие клавиши в поле «отображаемое имя» ре-рендерило 1 400 компонентов, включая навигационную оболочку, бейдж уведомлений и таблицу тарифов на экране, на который пользователь даже не смотрел. Причиной была командная конвенция, записанная два года назад: «клади состояние в стор приложения, чтобы потом было доступно отовсюду». Черновое значение каждого инпута жило в корне. Каждое нажатие обновляло состояние на уровне корня, и React делал ровно то, что обещает: ре-рендерил владельца и обходил всё его поддерево. Починка заняла полдня и удалила код — черновики переехали в форму, форма опустила общие куски в самый нижний компонент, у которого реально были оба читателя, и рендер на нажатие упал с 1 400 компонентов до 12. Ничего не мемоизировали. Состояние просто переехало туда, где ему место.
</Hook>

<Crux>Состояние живёт у ближайшего общего владельца. Поднимайте лишь до общего предка — каждый уровень выше расширяет зону ре-рендера.</Crux>

К концу урока вы будете точно знать, куда перенести состояние, когда одно нажатие клавиши даёт 1 400 ре-рендеров — и почему лечение удаляет код, а не добавляет мемоизацию.

<Explanation>

## Один владелец: данные вниз, события вверх

Состояние в React локально по построению: `useState` принадлежит ровно одному экземпляру компонента, и этот экземпляр — **единственный владелец**, единственное место, где значение может измениться. Когда одно и то же изменяющееся значение нужно двум компонентам, его не дублируют (две копии *обязательно* разъедутся; баг-репорт звучит как «чип фильтра активен, а список не отфильтрован»). Его **поднимают**: находят самый нижний компонент, который является предком всех читателей — *ближайшего общего владельца*, — переносят состояние туда, передают значение вниз пропсами, а обработчики изменений — вниз детям, чтобы те могли запрашивать обновления. Данные текут вниз, события — вверх, и у каждого куска состояния один источник истины.

```jsx
function SearchSection() {
  // Ближайший общий владелец <SearchInput> и <ResultsList>:
  // query нужен обоим, поэтому живёт здесь — и не выше.
  const [query, setQuery] = useState("");
  return (
    <section>
      <SearchInput value={query} onChange={setQuery} />
      <ResultsList query={query} />
    </section>
  );
}

function SearchInput({ value, onChange }) {
  // Контролируемый: компонент ничем не владеет; он сообщает намерение наверх.
  return <input value={value} onChange={(e) => onChange(e.target.value)} />;
}
```

Ключевое слово — *ближайший*. Подъём — это не «унести наверх», а «поднять ровно до первого общего предка и остановиться». Каждый уровень выше этой точки добавляет подписчиков, которым значение не нужно.

## Зона поражения: почему высота — это решение о производительности

Когда вы видите компоненты, ре-рендерящиеся без видимой причины, первый вопрос — структурный: как высоко сидит их владелец? Когда состояние меняется, React ре-рендерит компонент-владельца и затем, по умолчанию, **всё его поддерево** — каждый потомок вызывается заново, читает он изменившееся значение или нет. (Ре-рендер — это дешёвая фаза render, а не мутация DOM, но вызывать 1 400 функций и согласовывать их вывод 60 раз, пока человек печатает, — не бесплатно.) Высота куска состояния, таким образом, задаёт его **зону поражения**: состояние в листе ре-рендерит лист; состояние в корне ре-рендерит приложение. Это и есть механизм анти-паттерна «глобально по умолчанию» — складывать всё в корневой стор или верхнеуровневый контекст не просто запутывает код: *каждая* запись становится рендером всего приложения, и дальше приходится отбиваться `React.memo` на сотнях компонентов. **Колокация** (colocation — хранение состояния там, где оно используется) — обратная дисциплина: заметив, что значение читает лишь одно поддерево, опустите состояние обратно *вниз*. Спуск состояния — чистый выигрыш: меньше рендеров, меньше протаскивания пропсов, и компонент становится удаляемым целиком.

<Inset kind="why" lang="ru">
Почему React ре-рендерит всё поддерево, а не только компоненты, читающие изменившееся состояние? Потому что с пропсами «читает» — не подписка, которую React способен увидеть: любой потомок может получать значение, выведенное из этого состояния произвольным JavaScript. Единственное безопасное допущение после ре-рендера владельца — что пропсы любого ребёнка могли измениться, поэтому React перезапускает их и даёт reconciliation отбросить идентичный вывод. Точечные обновления по подписке требуют явного контракта — селекторов внешних сторов, это урок 3 этого юнита. С обычным состоянием ваш рычаг структурный: держите владельца низко.
</Inset>

## Композиция: изоляция состояния через проп children

Есть структурный приём, который для типового случая бьёт мемоизацию: **передавайте тяжёлые поддеревья как `children`**. Дети в JSX — это элементы, объекты, создаваемые там, где JSX *написан*, а не там, где он рендерится. Если обёртка с состоянием получает содержимое через `children`, её ре-рендер переиспользует те же самые объекты-элементы, что ей передали (родитель, который их создал, не ре-рендерился). React видит идентичную ссылку на элемент и **выходит досрочно**, не ре-рендеря это поддерево вовсе.

```jsx
function ScrollTracker({ children }) {
  const [y, setY] = useState(0); // обновляется на каждый тик скролла
  // children создал родитель, и они не менялись —
  // та же ссылка на элемент, поэтому React пропускает их ре-рендер.
  return <div onScroll={(e) => setY(e.target.scrollTop)}>
    <ProgressBar y={y} />
    {children}
  </div>;
}

// <ScrollTracker><HeavyTable rows={rows} /></ScrollTracker>
// HeavyTable НЕ ре-рендерится на скролл.
```

Это изоляция состояния архитектурой: высокочастотное состояние (скролл, hover, черновики ввода) получает узкий компонент для жизни, а тяжёлое содержимое проходит *сквозь* него нетронутым. **Компромисс:** подъём и колокация — это рефакторинг при каждом сдвиге требований: значение, нужное сегодня одному компоненту, завтра получает второго читателя, и его придётся переносить. Это трение реально, и это честная цена контроля зоны поражения; команды, отказывающиеся её платить, сползают обратно к «глобально по умолчанию» и платят рендерами. **Сценарий отказа:** дублирование состояния вместо подъёма («заведу ещё и локальную копию») — два владельца, неизбежное расхождение и класс багов, где UI противоречит сам себе.

<Quiz
  id="01-lifting-and-colocation-quiz-1"
  lessonSlug="01-lifting-and-colocation"
  lang="ru"
  question="Двум компонентам-сиблингам нужно читать и обновлять одно значение фильтра. Каков правильный ход и почему именно туда?"
  choices={[
    { label: "Поднять фильтр в ближайшего общего предка обоих сиблингов — ближайшего общего владельца — и передать вниз значение плюс обработчик: один источник истины с минимально возможным поддеревом ре-рендера", correct: true },
    { label: "Дать каждому сиблингу свой useState и синхронизировать их эффектом", misconception: "Два владельца — это два источника истины, расходящиеся между рендерами: синхронизирующий эффект срабатывает после commit, так что всегда есть кадр, где они не согласны, а паттерн разрастается в циклы эффектов. Подъём устраняет дублирование, а не латает его." },
    { label: "Положить фильтр в корневой контекст, чтобы любой будущий компонент тоже мог до него дотянуться", misconception: "Это размен нужды двух компонентов на зону поражения всего приложения: каждое изменение фильтра ре-рендерит всё под провайдером. Поднимайте ровно до ближайшего общего предка и останавливайтесь — выше двигайте только когда дальний читатель реально появится." },
    { label: "Хранить в первом сиблинге и читать из второго через ref", misconception: "Ref невидим для рендеринга — второй сиблинг не ре-рендерится при изменении значения, и его UI протухает. Общие изменяющиеся значения обязаны течь через состояние и пропсы — именно это и устраивает подъём." },
  ]}
/>

<Quiz
  id="01-lifting-and-colocation-quiz-2"
  lessonSlug="01-lifting-and-colocation"
  lang="ru"
  question="Обёртка с быстро меняющимся состоянием скролла получает тяжёлую таблицу через проп children. Почему таблица не ре-рендерится при обновлении состояния обёртки?"
  choices={[
    { label: "Элементы children создал родитель, который не ре-рендерился — обёртка пропускает те же ссылки на элементы насквозь, и React выходит досрочно на идентичных элементах", correct: true },
    { label: "React автоматически оборачивает children в React.memo", misconception: "Никакой мемоизации тут нет и пропсы не сравниваются. Досрочный выход — это ссылочная идентичность самих объектов-элементов: обёртка ре-рендерилась, но возвращает ровно те объекты children, что получила, и React нечего согласовывать заново." },
    { label: "Children рендерятся в цикле рендера родителя, поэтому обёртка в принципе не может на них повлиять", misconception: "Дети рендерятся там, где размещены в дереве — под обёрткой. Если бы обёртка сама создавала эти элементы в своём JSX, они были бы новыми объектами на каждый рендер и ре-рендерились бы как обычно; изоляцию даёт то, кто создал элементы." },
    { label: "Состояние скролла особое — React батчит его, и потомки не ре-рендерятся", misconception: "В состоянии скролла нет ничего особенного: любой useState в обёртке ведёт себя так же. Батчинг склеивает несколько set-вызовов в один рендер, но не освобождает потомков от этого рендера — освобождает идентичность элементов." },
  ]}
/>

<TradeoffMatrix
  id="01-lifting-and-colocation-tradeoff"
  lessonSlug="01-lifting-and-colocation"
  lang="ru"
  prompt="Значение фильтра читают и обновляют два компонента-сиблинга на одной странице. Где должен жить единственный useState?"
  options={[
    {
      name: "Поднять к ближайшему общему предку",
      summary: "Один источник истины на нижнем общем родителе, данные вниз, обработчики вверх.",
      correct: true,
      justification: "Ближайший общий владелец — самый глубокий компонент, являющийся предком обоих читателей. Размещение useState там минимизирует зону поражения: только это поддерево ре-рендерится на каждое нажатие, при этом сохраняется единый источник истины. Пропсы несут значение вниз; обработчик изменений передаёт намерение вверх. Это канонический контракт потока данных React и вариант, позволяющий удалить код позже, если читатели сузятся.",
    },
    {
      name: "Каждый сиблинг держит свой useState, синхронизируемый через useEffect",
      summary: "Две независимые копии, синхронизированные после коммита.",
      justification: "Два владельца — два источника истины, расходящихся между рендерами. Синхронизирующий эффект срабатывает после коммита, поэтому всегда есть хотя бы один кадр, где сиблинги не согласны. Хуже того, эффект может зациклиться: A обновляется, эффект обновляет B, который может ре-рендерить A. Этот паттерн повсеместно считается багом, а не решением — устраните дублирование подъёмом вместо того, чтобы латать расхождение эффектами.",
    },
    {
      name: "Переместить в корневой контекст, чтобы любой будущий компонент тоже мог достучаться",
      summary: "Трансляция на всё приложение, без протаскивания пропсов.",
      justification: "Продвижение в глобальный контекст максимизирует зону поражения: каждое нажатие клавиши теперь ре-рендерит всех потребителей контекста в дереве, а не только двух сиблингов. Контекст — правильный инструмент, когда состояние действительно кросс-трёвое и низкочастотное (тема, авторизация, локаль). Фильтр, используемый двумя сиблингами на одной странице, — ни то ни другое. Помещение его в корневой контекст разменивает нужду двух компонентов на налог рендера для всего приложения, а потом заставляет добавлять React.memo повсюду для возврата производительности.",
    },
    {
      name: "Держать в первом сиблинге; второй читает через ref",
      summary: "Первый сиблинг владеет состоянием; ref пробивается ко второму.",
      justification: "Ref-ы находятся вне цикла рендера React — мутации ref.current не вызывают ре-рендеров. Второй сиблинг отображал бы устаревший UI при каждом изменении фильтра; механизма сообщить ему об обновлении нет. Ref-ы — для чтения DOM-узлов или хранения нереактивных значений (id таймеров, предыдущие значения). Общее изменяющееся состояние обязано течь через useState и пропсы — именно это и обеспечивает подъём.",
    },
  ]}
/>

</Explanation>

<StackDiagram
  label="Лестница уровней проживания состояния, корень сверху и лист снизу: по умолчанию колокация на листе, подъём только до ближайшего общего владельца читателей, а каждый уровень выше — оплачиваемая зона поражения ре-рендеров"
  hue="var(--d-frontend)"
  layers={[
    { label: "Корень приложения — контекст / стор", note: "крайняя мера: сквозь дерево" },
    { label: "Секция страницы — слишком высоко", note: "ре-рендер без чтения" },
    { label: "Ближайший общий владелец — подъём сюда", note: "значение общее для соседей" },
    { label: "Листовой компонент — колокация", note: "один читатель: по умолчанию" },
  ]}
  caption="Состояние живёт ровно на одном уровне этой лестницы, и каждая ступень выше ближайшего общего владельца добавляет компоненты, ре-рендерящиеся без чтения значения. Опускайте состояние обратно, когда читатели сужаются; прежде чем лезть на верхнюю ступень, изолируйте горячее состояние в узкой обёртке через children — тяжёлое содержимое проходит сквозь неё со стабильной идентичностью элементов."
/>

<KeyTakeaway>У каждого куска состояния один владелец, и высота владельца задаёт зону ре-рендера: на каждую запись React ре-рендерит владельца и всё его поддерево. Поднимайте общее состояние до ближайшего общего предка читателей и не выше; опускайте обратно, когда читатели сужаются. «Глобально по умолчанию» переворачивает это и превращает нажатия клавиш в рендеры всего приложения. Проп children изолирует горячее состояние структурно — элементы, созданные не-ре-рендерящимся родителем, ссылочно идентичны, и React выходит досрочно без всякой мемоизации.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="01-lifting-and-colocation-retrieval"
  lang="ru"
  questions={[
    { q: "Объясните механизм, связывающий высоту куска состояния со стоимостью рендеринга, и что операционно значит «ближайший общий владелец».", a: "При изменении состояния React ре-рендерит компонент-владельца и затем всё его поддерево по умолчанию — каждая функция-потомок выполняется заново, читает она значение или нет, потому что чтение через пропсы — не подписка, которую React способен наблюдать. Поэтому чем выше владелец, тем больше поддерево, ре-рендерящееся на запись: состояние в листе стоит лист, состояние в корне стоит приложение. Ближайший общий владелец — самый глубокий компонент, являющийся предком всех читателей значения. Операционно: когда одно меняющееся значение нужно двум компонентам, идите вверх от обоих, пока пути не сойдутся, кладите единственный useState туда, значение передавайте вниз пропсами, обновления — вверх обработчиками. Не поднимайте выше точки встречи, а когда читатели сужаются (значение читает одно поддерево), опускайте состояние обратно. Поэтому «глобально по умолчанию» — архитектурный баг, а не вопрос стиля: он максимизирует поддерево для каждой записи, а потом требует мемоизацию как ликвидацию последствий." },
    { q: "Почему передача дорогого поддерева как children изолирует его от ре-рендеров обёртки с состоянием, и когда это лучше React.memo?", a: "Элементы JSX — обычные объекты, создаваемые там, где JSX написан. Если родитель рендерит обёртку и передаёт содержимое как children, эти объекты-элементы создал родитель. Когда собственное состояние обёртки меняется, обёртка ре-рендерится, но возвращает те же объекты children, что получила — родитель не ре-рендерился, и ничто их не пересоздало. React сравнивает ссылки элементов при reconciliation, находит их идентичными и досрочно выходит из всего поддерева. Без сравнения пропсов, без memo — чистая ссылочная идентичность. Это бьёт React.memo в структурном случае — высокочастотное состояние вроде позиции скролла, hover или черновиков ввода в узкой обёртке — потому что не нужна функция сравнения, приём нельзя сломать нестабильным пропом (новый инлайновый объект или колбэк молча обезвреживает memo), и он документирует замысел: эта обёртка владеет горячим состоянием, содержимое течёт сквозь неё. Memo остаётся инструментом, когда тот же компонент ре-рендерится от собственного родителя с действительно неизменными пропсами." },
  ]}
/>

<Recap lang="ru">
У состояния в React ровно один владелец, и архитектура — это решение, насколько высоко он сидит. По умолчанию — колокация: состояние живёт в самом нижнем компоненте, которому нужно. Когда значение делят сиблинги, его поднимают: найдите ближайшего общего предка всех читателей, перенесите туда единственный useState, значение передавайте вниз пропсами, намерения — вверх обработчиками. Один источник истины, без дублирования и синхронизирующих эффектов. Модель стоимости, из-за которой высота важна: запись в состояние ре-рендерит владельца и всё его поддерево, потому что React не может знать, какие потомки читают значение через пропсы — каждый уровень выше ближайшего общего владельца добавляет компоненты, ре-рендерящиеся впустую. «Глобально по умолчанию» — эта ошибка, возведённая в правило: корневой стор для черновиков ввода превратил одно нажатие клавиши в рендер 1 400 компонентов в истории из Hook, и именно спуск состояния — не мемоизация — сократил его до 12. Структурное дополнение — композиция: обёртка, владеющая быстрым состоянием и получающая тяжёлое содержимое через children, ре-рендерится одна, потому что элементы children создал родитель, ссылка не изменилась, и React выходит из них досрочно. У подъёма честная цена — состояние переезжает при смене читателей, и этот рефакторинг платится снова и снова, — но альтернатива — платить зоной поражения на каждую запись. Опускайте состояние при сужении читателей; поднимайте, только когда они действительно расходятся. Теперь, когда вы увидите в профайлере флейм-граф из компонентов, которые не должны ре-рендериться, первый вопрос — структурный: как высоко сидит владелец, и можно ли его опустить?
</Recap>
