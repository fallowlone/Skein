---
concepts:
- react-server-client-state
deepensInto: []
estMin: 19
lang: ru
lessonType: topic
level: senior
order: 4
prereqs: []
slug: "04-server-vs-client-state"
sources:
- https://react.dev/learn/you-might-not-need-an-effect
- https://tanstack.com/query/latest
- https://react.dev/learn/sharing-state-between-components
spiral: []
status: ready
summary: 'Серверное состояние — кеш истины сервера: асинхронной, общей, устаревшей по умолчанию; его жизненный цикл ведёт query-библиотека. Клиентское — истина UI. Копирование серверного в useState форкает истину; черновик формы — единственный намеренный форк с явным коммитом.'
title: 'Серверное против клиентского состояния: кто владеет какой истиной'
track: "react"
unit: "06-state-architecture"
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import Infographic from "~/components/diagram/Infographic.astro";
import statePhysics from "./infographics/state-physics.svg?raw";

<Hook>
Саппорт прозвал это «призраком переименования», и у бага был собственный тег в тикет-системе: 31 обращение за квартал. Пользователь переименовывает проект на странице деталей, возвращается назад — а список показывает старое имя до жёсткого рефреша. Страница списка фетчила проекты в эффекте и складывала в `useState`; страница деталей делала то же со своим фетчем; мутация переименования обновляла сервер и локальную копию страницы деталей. Три компонента, три приватные копии одной серверной строки, ноль договорённостей о том, когда обновляться. Первая «починка» команды — глобальная шина событий, рассылающая «project-changed», — добавила 400 строк и два новых бага. Настоящей починкой стало признание категориальной ошибки: данные проекта никогда не были *их* собственностью. Они принадлежат серверу; клиент держит **кеш**. Чтения переехали в query-библиотеку с ключами `["projects"]` и `["project", id]`, мутация переименования стала инвалидировать оба ключа, а каждый `useState`, изображавший из себя базу данных, удалили. Тег призрака закрыли вместе с 600 строками кода синхронизации.
</Hook>

<Crux>Серверное состояние — кеш чужой истины, им владеет query-библиотека. Клиентское — истина UI. Копирование между ними плодит устаревание.</Crux>

К концу урока вы сможете назвать владельца любого куска состояния в своём приложении — и понять, почему ошибка с владением породила 31 тикет поддержки о призраке переименования.

<Explanation>

## Два вида состояния с противоположной физикой

Прежде чем писать следующий `useEffect` для фетча, спросите себя: кто реально владеет этой истиной? Инструментарий useState/useReducer/стор из предыдущих уроков управляет **клиентским состоянием**: истиной, рождающейся в UI — какая вкладка активна, открыта ли модалка, ширина сайдбара. Источник истины — вы; оно синхронно, всегда корректно и умирает вместе с сессией. **Серверное состояние** — другая субстанция, которая лишь протекает через те же хуки, если ей позволить: список проектов, профиль пользователя, цены. Этой истиной владеет сервер. Ваша копия — **кеш чужих данных**: асинхронный, общий с другими пользователями, которые их мутируют, и **устаревший в момент прибытия**. У кешей есть жизненный цикл, которого у клиентского состояния нет: фетч и рефетч, дедупликация параллельных запросов, инвалидация после мутаций, фоновая ревалидация, сборка мусора неиспользуемых записей. Query-библиотека (TanStack Query, SWR, RTK Query) — не «библиотека для фетчинга» (фетч — одна строка). Это **менеджер кеша**: записи по ключам запросов, отслеживание устаревания, дедуп, ревалидация по фокусу и интервалу, инвалидация от мутаций. Этот жизненный цикл и есть настоящая работа, и его ручная сборка покомпонентно — то, как рождается призрак переименования.

<Infographic
  svg={statePhysics}
  label="Сравнение в две колонки серверного и клиентского состояния по пяти осям: владелец (сервер против вас, UI), время (асинхронно и устаревает при получении против синхронно и всегда корректно), общность (мутируется другими писателями против приватно для сессии), жизненный цикл (цикл кеша из дедупа, ревалидации, инвалидации и сборки мусора против отсутствия цикла кеша) и где живёт (кеш запросов по ключу против useState, редьюсера или стора)."
  caption="Те же хуки, противоположная физика: серверное состояние — кеш чужой истины: асинхронной, общей, устаревшей, с жизненным циклом, который ведёт query-библиотека; клиентское — истина UI, которой владеете вы: синхронная и корректная, без удалённой истины для ревалидации."
  hue="var(--d-infra)"
/>

## Класс багов: копирование серверных данных в useState

Анти-паттерн выглядит безобидно и прекрасно компилируется:

```jsx
// АНТИ-ПАТТЕРН: форк серверной истины в локальное состояние
function ProjectList() {
  const [projects, setProjects] = useState([]);
  useEffect(() => {
    fetchProjects().then(setProjects); // приватная копия, замороженная на момент фетча
  }, []);
  // ...
}

// Под управлением кеша: одна общая запись, инвалидируемая мутациями
function ProjectListFixed() {
  const { data: projects } = useQuery({
    queryKey: ["projects"],
    queryFn: fetchProjects,
  });
  // переименование где-то ещё: queryClient.invalidateQueries({ queryKey: ["projects"] })
}
```

Механизм бага: в момент, когда серверные данные попадают в `useState`, они **форкаются**. Компонент владеет снапшотом без ключа, говорящего, копией *чего* он является, — поэтому никакая мутация не может его найти и инвалидировать; два компонента, фетчащие один ресурс, держат независимые форки, расходящиеся после любой записи; рефетч гоняется с мутацией, и более медленный ответ побеждает, воскрешая старые данные. Каждая «починка» — шины событий, прокинутые колбэки refresh, ручной `refetchAll()` — это самописная, баг-в-баг, реимплементация инвалидации кеша. Производный вариант коварнее и переживает ревью: `useState(props.user.name)` «для инициализации» — инициализатор выполняется один раз при маунте, поэтому когда запрос рефетчится и пропсы обновляются, состояние хранит протухший форк. Нужно преобразованное представление серверных данных — выводите его при рендере (`const sorted = useMemo(...)`): деривация пересчитывается от свежей истины, копия замораживает мёртвую.

## Состояние формы: третий вид — намеренный форк

Формы выглядят противоречием: форма редактирования *обязана* копировать серверные данные в локальное состояние — пользователь печатает в черновик. Разрешение противоречия: **черновик — форк нарочно, с явным коммитом**. Расхождение с сервером — это фича (пользователь *редактирует*); submit — точка коммита, отправляющая черновик назад и инвалидирующая кеш. Дисциплина, делающая намеренные форки безопасными: черновик инициализируется из серверных данных *один раз на сессию редактирования* и никогда молча не пересинхронизируется посреди правки (представьте фоновый рефетч, переписывающий полунабранное поле), а компонент перемонтируется при смене субъекта — `key={project.id}` на форме, чтобы редактирование другого проекта получало свежий черновик, а не протухший. Библиотеки форм (react-hook-form, формы с actions) — эта дисциплина как продукт: dirty-трекинг, reset-on-success, валидация черновика.

<Inset kind="why" lang="ru">
Почему вопрос — «это копия серверной истины?», а не «это фетчится?» Потому что сценарий отказа — путаница владения, а не сетевой код. Состояние URL (текущий фильтр в query-параметрах) принадлежит роутеру; положить его ещё и в useState — форкнуть точно так же. Сессия авторизации — серверная истина, кешируемая вашим auth-провайдером. Решение обобщается: для каждого куска состояния назовите владельца — сервер, роутер, черновик формы или UI, — храните его в инструменте этого владельца и всё остальное *выводите*. Состояние с двумя владельцами — баг, ещё не выбравший себе шаги воспроизведения.
</Inset>

## Таблица решений

| Состояние… | Владелец | Живёт в | Никогда |
| --- | --- | --- | --- |
| Фетчится, общее, мутируется другими (списки, профили, цены) | Сервер | Кеш запросов, по ключу | Не копируется в useState |
| Истина UI (открытая модалка, активная вкладка, шаг визарда) | Клиент | useState / редьюсер / стор (уроки 1–3) | Не кладётся в кеш запросов |
| Правка серверных данных в процессе | Черновик формы | Состояние формы, с ключом по id записи, коммит на submit | Не пересинхронизируется молча посреди правки |
| Шарируемая конфигурация вида (фильтры, страница, сортировка) | URL | Параметры роутера | Не дублируется в useState |

<Quiz
  id="04-server-vs-client-state-quiz-1"
  lessonSlug="04-server-vs-client-state"
  lang="ru"
  question="Страница списка и страница деталей фетчат один проект каждая в свой useState. После мутации переименования на странице деталей список показывает старое имя до жёсткого рефреша. В чём корневая причина?"
  choices={[
    { label: "Копирование серверных данных в useState форкает истину: каждый компонент владеет снапшотом без ключа, который никакая мутация не может найти и инвалидировать, и копии расходятся после любой записи — лечение: одна запись кеша по ключу, инвалидируемая мутациями", correct: true },
    { label: "В эффекте списка пропущена зависимость, и он не перезапускается после мутации", misconception: "Не существует зависимости, способной выразить «другой компонент изменил сервер» — эффекты реагируют на локальные входы рендера, а не на удалённые записи. Добавлять deps или слушатели событий — собирать инвалидацию кеша вручную; категориальная ошибка — вообще держать приватную копию." },
    { label: "useState асинхронен, поэтому вызов setProjects применяется слишком поздно", misconception: "Тайминг setState тут ни при чём — копия списка была корректно установлена в момент фетча и внутренне согласованна. Она устарела, потому что ничто не связывает её со строкой, которую мутировала страница деталей; проблема во владении и инвалидации, не во времени обновления." },
    { label: "Двум компонентам нужен общий родитель, который фетчит и передаёт данные вниз", misconception: "Подъём фетча дедуплицирует копии, но только внутри этого поддерева, и у копии по-прежнему нет истории инвалидации — мутация из route action или другого дерева всё так же оставит её протухшей. Серверному состоянию нужен кеш с ключами по ресурсам, а не более высокий владелец." },
  ]}
/>

<Quiz
  id="04-server-vs-client-state-quiz-2"
  lessonSlug="04-server-vs-client-state"
  lang="ru"
  question="Форма редактирования копирует загруженную запись в локальный черновик. По правилам этого юнита — когда такая копия корректна и какие две дисциплины делают её безопасной?"
  choices={[
    { label: "Корректна, потому что черновик — намеренный форк с явным коммитом на submit; безопасность дают инициализация один раз на сессию редактирования (без молчаливой пересинхронизации посреди правки) и ключ формы по id записи, чтобы другая запись перемонтировала свежий черновик", correct: true },
    { label: "Некорректна — форма должна прописывать каждое нажатие клавиши сквозь мутацию, чтобы форка не было вовсе", misconception: "Сквозная запись превращает набор текста в сетевой трафик и делает недописанные правки видимыми другим пользователям — расхождение черновика и есть фича: пользователь сочиняет изменение. Форк безопасен именно потому, что у него есть определённая точка коммита, а не потому, что его никогда нет." },
    { label: "Корректна, и форма должна ещё подписаться на рефетчи, чтобы черновик непрерывно синхронизировался с сервером", misconception: "Непрерывная синхронизация — единственное, чего черновик делать не должен: фоновый рефетч перепишет текст, который пользователь печатает прямо сейчас. Черновик намеренно игнорирует серверные обновления в течение сессии редактирования и согласуется при коммите (или через явный диалог конфликта)." },
    { label: "Некорректна — черновикам место в кеше запросов, рядом с серверными данными, которые они правят", misconception: "Кеш хранит истину сервера; черновик — расходящееся намерение пользователя. Запись черновиков в кеш выдаёт несохранённые правки за подтверждённое серверное состояние каждому читателю этого ключа — ровно та путаница владения, ради предотвращения которой существует разделение." },
  ]}
/>

<TradeoffMatrix
  id="04-server-vs-client-state-tradeoff"
  lessonSlug="04-server-vs-client-state"
  lang="ru"
  prompt="Приложение показывает список проектов пользователя, загруженных из API. Мутация переименования срабатывает со страницы деталей. Какой подход держит все представления согласованными без ручной синхронизации?"
  options={[
    {
      name: "Query-библиотека с кешем по ключам + инвалидация от мутации",
      summary: "Чтения делят одну запись кеша; мутация инвалидирует ключ и запускает рефетч.",
      correct: true,
      justification: "Query-библиотека задаёт записи по ключам (например ['projects']), так что каждый компонент, читающий этот ключ, делит одну запись кеша — приватных копий нет и разъезжаться нечему. Когда мутация переименования завершается, инвалидация ключа говорит библиотеке перефетчить, и каждый подписчик получает свежую истину на следующем рендере. Дедупликация, фоновая ревалидация, состояния загрузки и ошибок управляются библиотекой. Цена — добавить зависимость и освоить её ментальную модель; выгода — удалить весь ручной код синхронизации, что и закрыло 600 строк в истории из Hook.",
    },
    {
      name: "Фетчить в useState в каждом компоненте, которому нужен список",
      summary: "Каждый компонент держит свою приватную копию серверных данных.",
      justification: "Каждый компонент, фетчащий в useState, создаёт независимый снапшот без ключа, связывающего его с другими копиями. После переименования мутация обновляет сервер и локальную копию страницы деталей; страница списка всё ещё держит старое имя, и ничто не может его найти и обновить — ровно призрак переименования. Каждая последующая «починка» (шина событий, прокинутый колбэк) — самописная реимплементация инвалидации кеша, добавляющая строки и новые сценарии отказа вместо устранения категориальной ошибки.",
    },
    {
      name: "Скопировать серверные данные в глобальный стор Zustand при первом фетче",
      summary: "Один стор, несколько подписчиков, ручное обновление при мутации.",
      justification: "Стор на уровне модуля устраняет проблему дублирования на уровне компонентов, но всё ещё держит копию серверной истины вместо делегирования кешу. После мутации переименования нужно помнить записать обновлённую запись в стор и держать её форму синхронизированной с тем, что возвращает сервер: если API меняется или мутация возвращает частичное обновление, стор уплывает. Query-библиотека лучше для серверных данных именно потому, что её кеш ведётся ответом сервера, а не кодом приложения, решающим, что обновить.",
    },
    {
      name: "Делать полную перезагрузку страницы после каждой мутации",
      summary: "Грубая согласованность: перезагрузка сбрасывает всё устаревшее состояние.",
      justification: "Полная перезагрузка гарантирует согласованность, но уничтожает всё состояние UI (позиция скролла, открытые панели, черновики форм), добавляет паузу с чистым экраном при каждой записи и масштабируется как O(мутаций) в плане пользовательского трения. Это аварийная мера, к которой команды тянутся до понимания инвалидации кеша — работает, но отказывается от всей интерактивности, ради которой существует SPA. Точечная инвалидация достигает той же согласованности без этих издержек.",
    },
  ]}
/>

</Explanation>

<FlowDiagram
  label="Маршрутизация куска состояния к владельцу: серверная истина — в кеш запросов с инвалидацией от мутаций, истина UI — в клиентские инструменты, черновики — форки с ключом и явным коммитом, конфигурация вида — в URL"
  hue="var(--d-frontend)"
  perRow={3}
  nodes={[
    { id: "state", label: "Кусок состояния", sub: "кто владеет истиной?" },
    { id: "server", label: "Кеш запросов", sub: "по ключу, знает об устаревании" },
    { id: "client", label: "Клиентское состояние", sub: "useState / стор" },
    { id: "draft", label: "Черновик формы", sub: "форк + коммит" },
    { id: "url", label: "URL / роутер", sub: "шарируемая конфигурация" },
    { id: "invalidate", label: "Инвалидация", sub: "мутация → рефетч" },
  ]}
  edges={[
    { from: "state", to: "server", label: "владеет сервер" },
    { from: "state", to: "client", label: "владеет UI" },
    { from: "state", to: "draft", label: "сессия редактирования" },
    { from: "state", to: "url", label: "шарируемый вид" },
    { from: "draft", to: "invalidate", label: "submit коммитит" },
    { from: "invalidate", to: "server", label: "рефетч ключей" },
  ]}
  caption="Серверное состояние — запись кеша: с ключом, дедупликацией, ревалидацией, инвалидацией от мутаций. Клиентское состояние — собственная истина. Черновик — единственная санкционированная копия серверных данных: с ключом по id записи и согласованием в явной точке коммита, без молчаливой пересинхронизации посреди правки."
/>

<KeyTakeaway>Называйте владельца каждого куска состояния. Серверное состояние — кеш истины сервера: асинхронной, общей, устаревшей по умолчанию; query-библиотека ведёт его жизненный цикл по ключам (дедуп, ревалидация, инвалидация от мутаций). Клиентское состояние — истина UI, инструменты уроков 1–3. Копирование серверных данных в useState форкает истину в снапшот без ключа, который нечем инвалидировать, — класс багов «призрак переименования». Черновик формы — санкционированный форк: ключ по id записи, коммит на submit. Преобразованные представления выводите при рендере; никогда не дублируйте.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="04-server-vs-client-state-retrieval"
  lang="ru"
  questions={[
    { q: "Объясните механически, почему копирование загруженных данных в useState порождает класс багов «протухло после мутации», и почему кеш запросов чинит это там, где шины событий и колбэки refresh — нет.", a: "В момент попадания загруженных данных в useState они становятся анонимным форком: снапшотом без ключа, объявляющего, копией какого серверного ресурса он является. Поэтому мутациям нечем его найти и инвалидировать — компонент будет показывать данные времён фетча, пока что-то случайно не перезапустит его эффект. Умножьте на несколько компонентов, фетчащих один ресурс, — получите независимые форки, расходящиеся после любой записи; добавьте гонки рефетча с мутацией — и более медленный ответ воскресит старые данные. Шины событий и колбэки refresh бьют по симптомам: это самописная инвалидация с ручной разводкой на каждого потребителя, потому она и растит код и новые баги. Кеш запросов чинит структуру: каждое чтение идёт через запись с ключом (ключ списка проектов и ключ конкретного проекта), параллельные читатели делят одну запись вместо форка, запросы дедуплицируются, а мутация объявляет, что тронула, инвалидируя ключи — каждый подписчик рефетчится или обновляется автоматически. Устаревание становится управляемым (stale-while-revalidate, рефетч по фокусу), а не случайным. Коварный вариант для ревью: затравка useState из пропсов или данных запроса — инициализатор выполняется один раз, и поздние рефетчи до копии не доходят. Преобразованные представления выводите при рендере из данных кеша, а не храните." },
    { q: "Разложите полную таблицу решений, где живёт состояние, включая два вида помимо серверного/клиентского, и обоснуйте исключение для черновика формы из правила «не копировать».", a: "Спросите, кто владеет истиной. (1) Серверное — фетчится, общее, мутируется другими акторами: списки, профили, цены — живёт в кеше запросов, с ключом по ресурсу, мутации инвалидируют ключи; никогда не копируется в useState. (2) Клиентская истина UI — открытая модалка, активная вкладка, шаг визарда — живёт в useState, редьюсере или внешнем сторе по урокам 1–3; в кеше запросов ей не место, потому что нет удалённой истины для ревалидации. (3) Конфигурация вида, принадлежащая URL — фильтры, сортировка, страница — живёт в параметрах роутера: шарируема и переживает перезагрузку; дублирование в useState форкает её так же, как серверные данные. (4) Черновики форм — правка серверных данных в процессе — санкционированная копия: намеренный форк, чьё расхождение и есть смысл, под двумя дисциплинами: инициализация один раз на сессию редактирования без молчаливой пересинхронизации посреди правки (фоновый рефетч не должен переписывать полунабранный ввод; согласование — при коммите или через явный диалог конфликта) и ключ компонента формы по id записи, чтобы смена записи перемонтировала свежий черновик, а не протаскивала старый. Submit — коммит: отправить черновик, затем инвалидировать задетые ключи, чтобы все кешированные читатели сошлись. Всё остальное — деривация при рендере: два владельца у одной истины — латентный баг." },
  ]}
/>

<Recap lang="ru">
Инструменты юнита — колокация, редьюсеры, внешние сторы — управляют клиентским состоянием: истиной, рождающейся в UI и принадлежащей вам. Серверное состояние лишь похоже: список проектов и профиль пользователя — истина сервера, а приложение держит кеш — асинхронный, общий с другими писателями, устаревший с момента прибытия. У кешей жизненный цикл: записи по ключам, дедупликация запросов, фоновая ревалидация и, главное, инвалидация после мутаций — этим query-библиотеки и являются: менеджеры кеша, а не обёртки над fetch. Класс багов рождается из игнорирования границы: копирование загруженного в useState форкает истину в снапшот без ключа, который мутациям не найти, и компоненты расходятся после записи — призрак переименования; затравочный вариант, useState из пропсов или данных запроса, замораживает форк, потому что инициализаторы выполняются один раз. Починки через рассылку событий — самописная инвалидация; структурная починка — записи кеша по ключам плюс мутации, инвалидирующие задетые ключи, а преобразованные представления — деривация при рендере. Формы — санкционированное исключение: черновик — намеренный форк, чьё расхождение и есть фича, безопасный благодаря инициализации раз на сессию, запрету пересинхронизации посреди правки, ключу по id записи и коммиту на submit. Общее правило, закрывающее юнит: назовите владельца — сервер, роутер, черновик формы или UI, — храните состояние в инструменте владельца и выводите остальное. Теперь, когда вы увидите `useEffect`, фетчащий данные в `useState`, вы сразу спросите: что произойдёт, когда мутация придёт из другого компонента — и кто инвалидирует эту копию?
</Recap>
