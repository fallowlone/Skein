---
concepts:
- react-reducer-architecture
deepensInto: []
estMin: 18
lang: ru
lessonType: topic
level: middle
order: 2
prereqs: []
slug: "02-reducers-and-context-architecture"
sources:
- https://react.dev/reference/react/useReducer
- https://react.dev/learn/scaling-up-with-reducer-and-context
spiral: []
status: ready
summary: 'useReducer моделирует многопольные обновления как именованные переходы в одной чистой тестируемой функции вместо разбросанных setState. Раздельные контексты state и dispatch: dispatch стабилен, пишущие компоненты не ре-рендерятся. Часто это заменяет стейт-библиотеку.'
title: 'Редьюсеры и контекст: из разрозненных setState — в тестируемые переходы'
track: "react"
unit: "06-state-architecture"
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
Баг в чекауте, дошедший до финансового директора, формулировался просто, а воспроизводился мучительно: заказ ушёл с бесплатной доставкой *и* со стоимостью доставки, включённой в итог. Компонент чекаута держал одиннадцать полей `useState` — товары, промокод, скидка, способ доставки, её стоимость, итоги — и инвариант «итог = товары − скидка + доставка» поддерживался вручную в шести разных обработчиках событий. Обработчик «применить промокод» обновлял скидку и итог; обработчик «сменить доставку» — доставку и итог; коллега месяцем раньше добавил экспресс-чекаут и обновил пять мест из шести. QA не поймал, потому что сломанный путь требовал применить промокод, *пока* оценка доставки ещё грузится. Переписывание оказалось почти механическим: один `useReducer`, экшены `promo-applied` и `shipping-selected`, итоги пересчитываются ровно в одном месте — в редьюсере — и юнит-тест на двенадцать строк, проигрывающий падающую последовательность экшенов без DOM, без React, без моков. Тест падал на старой логике, проходил на новой — и класс багов исчез.
</Hook>

<Crux>Редьюсер превращает многопольные обновления в именованные чистые переходы; раздельные контексты state и dispatch сдерживают ре-рендеры.</Crux>

<Explanation>

## Переходы, а не поля: что на самом деле меняет useReducer

У формы с десятью вызовами `useState` нет архитектуры — есть десять независимых значений и надежда, что каждый обработчик обновит нужное подмножество. Сценарий отказа — **дрейф инварианта**: правило вроде «итог отражает скидку и доставку» не живёт ни в одном конкретном месте, и каждый новый обработчик — шанс обновить четыре поля из пяти нужных. `useReducer` переворачивает структуру. Состояние становится одним объектом; обновления — **экшенами**, именованными событиями, описывающими, *что произошло* (`promo-applied`), а не *какие поля записать* (`setDiscount` + `setTotal`). Редьюсер — единственное место, где события превращаются в следующее состояние:

```jsx
function checkoutReducer(state, action) {
  switch (action.type) {
    case "promo-applied": {
      const discount = computeDiscount(state.items, action.code);
      return withTotals({ ...state, promoCode: action.code, discount });
    }
    case "shipping-selected": {
      return withTotals({ ...state, shipping: action.method });
    }
    default:
      throw new Error(`Unknown action: ${action.type}`);
  }
}

// withTotals — ЕДИНСТВЕННОЕ место, где живёт инвариант:
function withTotals(state) {
  return { ...state, total: subtotal(state) - state.discount + shippingCost(state) };
}

const [state, dispatch] = useReducer(checkoutReducer, initialCheckout);
// Обработчики сжимаются до намерения: dispatch({ type: "promo-applied", code });
```

Механика: `dispatch` планирует ре-рендер, и React вызывает ваш редьюсер с текущим состоянием и экшеном при следующем рендере — поэтому редьюсер **обязан быть чистым**: никаких запросов, никакого `Date.now()`, никакой мутации входящего состояния (в StrictMode редьюсер вызывается дважды ровно затем, чтобы это поймать). Компоненты сжимаются до диспетчеризации намерений; *логика решений* покидает компонент целиком.

## Чистая логика — тестируемая логика

Поскольку редьюсер — чистая функция `(state, action) => state`, самая баго-опасная логика фичи — многопольные инварианты, последовательности краевых случаев — становится юнит-тестируемой без рендеринга чего-либо. Регрессионный тест из Hook — три строки проигрывания: стартуем с известного состояния, диспатчим `shipping-estimate-loaded`, затем `promo-applied`, проверяем итог. Без JSDOM, без `act()`, без замоканной сети — тесты редьюсера выполняются за микросекунды и прибивают *последовательности*, а именно там прячутся баги разрозненных setState. Это недооценённый выигрыш: команды берут редьюсеры «для порядка», а обнаруживают, что главный приз — логика состояния наконец получает ту же тестовую дисциплину, что и остальная кодовая база. **Компромисс:** церемония реальна — типы экшенов, switch, прослойка между кликом и записью. Для двух полей без инвариантов `useState` строго лучше; редьюсер окупается, когда поля обновляются *вместе* по правилам.

## Разделение state и dispatch: гигиена ре-рендеров как архитектура

Чтобы раздать состояние редьюсера по поддереву фичи, его передают через контекст — и тут гигиена ре-рендеров из юнита 02 становится архитектурным решением. Контекст ре-рендерит каждого потребителя, когда идентичность его значения меняется. Если публиковать `{ state, dispatch }` одним объектом, каждый потребитель ре-рендерится на каждое изменение состояния — включая кнопки тулбара, которые только *диспатчат*. Лечение структурное: **два контекста**.

```jsx
const CheckoutStateContext = createContext(null);
const CheckoutDispatchContext = createContext(null);

function CheckoutProvider({ children }) {
  const [state, dispatch] = useReducer(checkoutReducer, initialCheckout);
  return (
    <CheckoutDispatchContext.Provider value={dispatch}>
      <CheckoutStateContext.Provider value={state}>
        {children}
      </CheckoutStateContext.Provider>
    </CheckoutDispatchContext.Provider>
  );
}
```

React гарантирует, что `dispatch` **ссылочно стабилен** между рендерами — значение dispatch-контекста никогда не меняется, поэтому компоненты «только записи» (кнопки, пункты меню, баннер экспресс-чекаута) подписываются на него и *никогда не ре-рендерятся* от изменений состояния. Читающие компоненты подписаны на state-контекст и ре-рендерятся при изменении состояния — честно, ведь они его отображают.

<Inset kind="why" lang="ru">
Почему этот паттерн часто заменяет стейт-библиотеку? Потому что для домена в масштабе фичи — чекаут, панель редактора, визард — он уже даёт три вещи, ради которых импортируют Redux: централизованные переходы, тестируемую чистую логику и общий доступ вниз по поддереву. Чего он *не* даёт — того, что библиотеки реально добавляют: точности подписки уровня селекторов (каждый потребитель state-контекста ре-рендерится на любое изменение состояния — подписаться только на `state.total` невозможно), devtools с историей экшенов и middleware. Правило выбора: масштаб фичи, умеренная частота записи, потребители читают пересекающееся состояние → редьюсер + контекст, ноль зависимостей. Всё приложение, частые записи или потребители с непересекающимися срезами → внешний стор, следующий урок.
</Inset>

<Quiz
  id="02-reducers-and-context-architecture-quiz-1"
  lessonSlug="02-reducers-and-context-architecture"
  lang="ru"
  question="Форма чекаута поддерживает инвариант «итог = товары − скидка + доставка» в шести обработчиках через несколько useState, и инвариант постоянно ломается. Что структурно меняет переход на useReducer?"
  choices={[
    { label: "Обновления становятся именованными экшенами, описывающими произошедшее, а инвариант пересчитывается ровно в одной чистой функции — обработчики больше не могут обновить несогласованное подмножество полей", correct: true },
    { label: "Редьюсеры батчат обновления полей, и они коммитятся атомарно, чего useState не умеет", misconception: "React батчит обновления состояния внутри обработчика и для useState — атомарность коммитов никогда не была проблемой. Баг был в том, что каждый обработчик заново реализовывал инвариант вручную; редьюсер чинит это централизацией логики перехода, а не изменением батчинга." },
    { label: "useReducer ускоряет обновления, потому что объект состояния один", misconception: "Производительность практически идентична — dispatch планирует тот же ре-рендер, что и setState. Ценность редьюсера — архитектура корректности: одно место, где события становятся состоянием, тестируемое без React. Выбирать его ради скорости — не понимать, зачем он." },
    { label: "Редьюсер может await-ить оценку доставки перед расчётом итога и тем устранить гонку", misconception: "Редьюсеры обязаны быть чистыми и синхронными — без запросов и await; в StrictMode React даже вызывает их дважды. Асинхронная работа живёт в обработчиках или эффектах, которые затем диспатчат экшены-результаты вроде shipping-estimate-loaded; редьюсер лишь корректно упорядочивает переходы." },
  ]}
/>

<Quiz
  id="02-reducers-and-context-architecture-quiz-2"
  lessonSlug="02-reducers-and-context-architecture"
  lang="ru"
  question="Почему разделение состояния редьюсера и dispatch на два отдельных контекста избавляет пишущие компоненты от ре-рендеров, а один общий контекст — нет?"
  choices={[
    { label: "dispatch ссылочно стабилен между рендерами, поэтому значение dispatch-контекста никогда не меняет идентичность — его потребители не уведомляются, а общий объект — новая ссылка на каждое изменение состояния", correct: true },
    { label: "Два контекста позволяют React точнее диффить значение и уведомлять только потребителей изменившейся половины", misconception: "У контекста нет диффинга — каждый контекст сравнивает своё значение по идентичности и уведомляет всех своих потребителей при изменении. Разделение работает только потому, что dispatch не меняет идентичность; React не анализирует, какое свойство читал потребитель." },
    { label: "Dispatch-контекст вообще обходит рендеринг, потому что функции не могут вызывать ре-рендеры", misconception: "Любое значение контекста, функция или нет, переуведомляет потребителей при смене идентичности. Если бы вы передавали новую инлайновую обёртку над dispatch на каждом рендере, потребители dispatch-контекста ре-рендерились бы каждый раз — механизм в стабильности ссылки, а не в типе." },
    { label: "Это вдвое уменьшает работу, потому что каждый потребитель подписан на вдвое меньше данных", misconception: "Объём данных не влияет на стоимость контекста — потребитель ре-рендерится при смене идентичности значения, будь это одна функция или объект на тысячу полей. Разделение меняет то, какие значения меняются, а не сколько данных течёт." },
  ]}
/>

</Explanation>

<FlowDiagram
  label="Событие течёт через dispatch в чистый редьюсер, владеющий всеми инвариантами; новое состояние уведомляет только потребителей state-контекста, а потребители dispatch-контекста не затронуты, потому что dispatch не меняет идентичность"
  hue="var(--d-frontend)"
  perRow={3}
  nodes={[
    { id: "event", label: "Событие пользователя", sub: "клик, ввод" },
    { id: "dispatch", label: "dispatch(action)", sub: "именованное намерение" },
    { id: "reducer", label: "Чистый редьюсер", sub: "инварианты в одном месте" },
    { id: "state", label: "Следующее состояние", sub: "один объект" },
    { id: "readers", label: "State-контекст", sub: "читатели ре-рендерятся" },
    { id: "writers", label: "Dispatch-контекст", sub: "писатели: без ре-рендера" },
  ]}
  edges={[
    { from: "event", to: "dispatch", label: "намерение" },
    { from: "dispatch", to: "reducer", label: "экшен" },
    { from: "reducer", to: "state", label: "переход" },
    { from: "state", to: "readers", label: "новая идентичность" },
    { from: "dispatch", to: "writers", label: "стабильная ссылка" },
    { from: "readers", to: "event", label: "следующее взаимодействие" },
  ]}
  caption="Редьюсер — единственная точка, где события становятся состоянием: инварианты становятся проверяемыми, а логика — юнит-тестируемой без React. Разделение на два контекста — гигиена ре-рендеров, применённая архитектурно: стабильный dispatch писателям, меняющееся состояние читателям."
/>

<KeyTakeaway>useReducer заменяет разрозненные setState именованными переходами в одной чистой функции — единственном доме инвариантов и причине, по которой логика состояния тестируется без рендеринга. Раздавайте через два контекста, а не один: dispatch ссылочно стабилен, и пишущие компоненты, подписанные на dispatch-контекст, не ре-рендерятся. Для домена в масштабе фичи это стейт-библиотека минус селекторы и devtools — за внешним стором идите, только когда нужна точность подписки.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="02-reducers-and-context-architecture-retrieval"
  lang="ru"
  questions={[
    { q: "Многопольная форма постоянно ломает инвариант, который несколько обработчиков поддерживают вручную. Объясните, как редьюсер устраняет этот класс багов и почему он обязан оставаться чистым.", a: "С несколькими полями useState у инварианта вроде «итог отражает скидку и доставку» нет дома — каждый обработчик, трогающий любое участвующее поле, должен реализовать его заново, и каждый новый обработчик или путь кода — шанс обновить несогласованное подмножество. Редьюсер перестраивает это: состояние — один объект, обработчики диспатчат именованные экшены о произошедшем, и единственная чистая функция отображает (state, action) в следующее состояние. Инвариант пересчитывается ровно в одном месте на переход и не может дрейфовать между обработчиками. Чистота требуется моделью исполнения: dispatch лишь планирует ре-рендер, а React выполняет редьюсер с текущим состоянием во время рендеринга — возможно, дважды в StrictMode, возможно, для рендеров, которые будут выброшены. Любой запрос, Date.now() или мутация входящего состояния внутри редьюсера срабатывает непредсказуемо. Асинхронная работа живёт в обработчиках или эффектах и возвращается в редьюсер экшенами-результатами. Награда за чистоту — тестируемость: проигрывайте последовательность экшенов в обычном юнит-тесте — без DOM, без act(), за микросекунды — именно так прибиваются баги последовательностей вроде «промокод применён, пока грузится оценка доставки»." },
    { q: "Когда редьюсер + контекст законно заменяет стейт-библиотеку и от чего вы отказываетесь — назовите точное ограничение по ре-рендерам.", a: "Редьюсер + раздельные контексты закрывает домен в масштабе фичи — чекаут, панель редактора, визард — тремя вещами, ради которых импортируют библиотеку: централизованные именованные переходы, чистая тестируемая логика и общий доступ вниз по поддереву, при нулевой стоимости зависимостей. Разделение на два контекста даёт частичный контроль ре-рендеров: dispatch ссылочно стабилен, и компоненты «только записи» никогда не ре-рендерятся от изменений состояния. Точное ограничение — на стороне чтения: контекст уведомляет каждого потребителя при смене идентичности значения, и механизма селекторов нет — компонент, читающий только state.total, всё равно ре-рендерится при изменении несвязанного поля, потому что объект состояния — новая ссылка на каждый переход. Также вы лишаетесь истории экшенов в devtools и middleware. Граница такая: масштаб фичи, умеренная частота записи, читатели потребляют пересекающееся состояние → редьюсер + контекст. Состояние всего приложения, частые записи или много потребителей непересекающихся срезов → внешний стор с подписками-селекторами, где компонент ре-рендерится только при изменении выбранного среза." },
  ]}
/>

<Recap lang="ru">
Несколько полей useState, обновляющихся вместе по правилам, — это инвариант, ждущий дрейфа: правило живёт в каждом обработчике и потому ни в одном, а баг приезжает со следующим обработчиком, который кто-то добавит. useReducer перестраивает фичу вокруг переходов — обработчики диспатчат именованные экшены о произошедшем, и одна чистая функция превращает текущее состояние и экшен в следующее состояние. Инварианты получают единственный дом; компоненты сжимаются до выражения намерений. Механика: dispatch планирует ре-рендер, и React выполняет редьюсер во время рендеринга — поэтому он обязан быть чистым: без запросов, без чтения часов, без мутаций; StrictMode вызывает его дважды, чтобы ловить нарушения. Чистота покупает практический приз: логика состояния становится юнит-тестируемой проигрыванием последовательностей экшенов за микросекунды, без DOM и моков — так баги, зависящие от порядка событий, получают регрессионные тесты. Раздача редьюсера по поддереву применяет гигиену ре-рендеров архитектурно: публикуйте state и dispatch двумя отдельными контекстами. Dispatch ссылочно стабилен, и пишущие компоненты не ре-рендерятся; потребители state-контекста ре-рендерятся на каждый переход — честно для отображающих, впустую для читающих неизменившийся срез, потому что у контекста нет селекторов. Эта недостающая точность плюс devtools и middleware — то, что всё ещё добавляет стейт-библиотека: редьюсер + контекст для доменов масштаба фичи, внешние сторы — когда записи частые или читатели не пересекаются.
</Recap>
