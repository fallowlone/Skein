---
concepts:
- react-profiling
deepensInto: []
estMin: 17
lang: ru
lessonType: topic
level: middle
order: 1
prereqs: []
slug: "01-profiler-and-measurement"
sources:
- https://react.dev/reference/react/Profiler
- https://react.dev/learn/react-developer-tools
- https://web.dev/articles/inp
spiral: []
status: ready
summary: 'Профилируйте до оптимизации: флеймграф React DevTools показывает цену каждого коммита, Profiler API превращает её в числа для мониторинга, а бюджет пользователя — это INP, а не миф о 16 мс на кадр. Команда два спринта мемоизировала 4 мс и не заметила 620.'
title: 'Profiler и измерения: найдите коммит на 600 мс прежде, чем трогать React.memo'
track: "react"
unit: "05-performance"
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DiagramFrame from "~/components/diagram/DiagramFrame.astro";
import PacketDot from "~/components/diagram/PacketDot.astro";
import Infographic from "~/components/diagram/Infographic.astro";
import noiseVsSignal from "./infographics/noise-vs-signal.svg?raw";

<Hook>
Команда дашборда потратила два спринта на кампанию мемоизации: сорок компонентов обёрнуты в `React.memo`, каждый колбэк — в `useCallback`, для пропсов графика написана своя функция сравнения. В описании PR красовалось «ре-рендеров меньше на 60%» с гифкой в доказательство. А потом всё равно загорелся канал дежурных — саппорт пересылал одну и ту же жалобу: «фильтры замирают». Полевой мониторинг показывал INP 750 мс на выпадающем списке фильтров — ровно столько же, сколько до кампании. Час с профайлером React DevTools рассказал настоящую историю. Лишние ре-рендеры, которые команда убрала, стоили около 4 мс на коммит. А зависание было одним коммитом на 620 мс: смена диапазона дат заставляла SVG-график на 9 000 точек размонтироваться и смонтироваться заново, потому что родитель передавал ему свежий `key` при каждой смене фильтра. Два спринта оптимизировали шум; сигналом была одна полоса флеймграфа, видная через всю комнату. Никто не профилировал перед тем, как оптимизировать.
</Hook>

<Crux>Сначала измеряйте: Profiler показывает цену коммитов, INP — что чувствует пользователь. Без замеров чините не то.</Crux>

<Explanation>

## Чтение флеймграфа: коммиты, а не компоненты

Профайлер React DevTools записывает сессию взаимодействий и нарезает её на **коммиты** — каждый раз, когда React реально донёс изменения до DOM, в таймлайне появляется один столбик. Выбрав коммит, вы видите **флеймграф**: каждая строка дерева — компонент, ширина его полосы — сколько он (вместе с детьми) рендерился в этом коммите, а серые полосы — компоненты, которые не рендерились вовсе. Эта рамка принципиальна: вопрос никогда не звучит «какой компонент медленный» вообще, он звучит «какой коммит медленный и что внутри него сожгло время». Один коммит на 600 мс с одной широкой полосой — совсем другая болезнь, чем шестьдесят коммитов по 10 мс от болтливой подписки, и лечатся они противоположно.

Две настройки превращают инструмент из картинки в диагноз. **«Record why each component rendered»** подписывает каждую полосу причиной: изменились пропсы (и какие именно), изменился стейт, сработал хук или просто отрендерился родитель. Одна эта подпись убивает большую часть гаданий — ремаунт графика из истории выше отобразился как «key changed», чего никакое разглядывание кода компонента не выявило бы. Вторая — галочка **«Highlight updates»**, которая в реальном времени подсвечивает всё, что ре-рендерится: самый быстрый способ заметить провайдер контекста, перерисовывающий полстраницы на каждое нажатие клавиши. Профилируйте сначала в development ради богатых данных, но помните: dev-сборка React заметно медленнее — любое число, на котором строится решение, перепроверяйте на продакшен-сборке с профилированием (`react-dom/profiling`), её накладные расходы — единицы процентов.

## Profiler API: числа, которые можно отправить в мониторинг

DevTools интерактивен; компонент `<Profiler>` программируем. Оберните поддерево — и React после каждого коммита внутри него вызовет `onRender` с таймингами, которые можно логировать, агрегировать или слать в систему мониторинга:

```jsx
import { Profiler } from "react";

function onRender(id, phase, actualDuration, baseDuration, startTime, commitTime) {
  // actualDuration: сколько рендерился этот коммит (с учётом мемоизации)
  // baseDuration: оценка худшего случая — все потомки рендерятся, memo нет
  metrics.send("react-commit", {
    id,                       // "FilterPanel"
    phase,                    // "mount" | "update" | "nested-update"
    actual: actualDuration,   // напр. 4.2 (мс)
    base: baseDuration,       // напр. 38.7 (мс)
  });
}

<Profiler id="FilterPanel" onRender={onRender}>
  <FilterPanel />
</Profiler>
```

Пара, которую стоит усвоить, — `actualDuration` против `baseDuration`. `actualDuration` — реальная цена этого коммита; `baseDuration` — сумма последних времён рендера каждого потомка, то есть оценка худшего случая, где ничто не мемоизировано. Когда `actualDuration` сильно ниже `baseDuration`, мемоизация отрабатывает своё; когда числа идут вровень — обёртки `React.memo` стали декорацией. Именно этих доказательств команда из истории так и не собрала: их memo-кампания сдвинула `actualDuration` с 42 до 38 мс на коммите, который имел значение, — в пределах шума, потому что ремаунт на 620 мс обходил мемоизацию целиком. **Компромисс:** `<Profiler>` дёшев, но не бесплатен (React ведёт дополнительный учёт на каждый коммит внутри профилируемых деревьев), поэтому в продакшене оборачивайте две-три реально отслеживаемые интеракции, а не корень.

<Infographic
  svg={noiseVsSignal}
  label="Сравнение бок о бок: кампания мемоизации (ре-рендеров меньше на 60%, около 4 мс на коммит, actualDuration с 42 до 38 мс) против того, что реально чувствовал пользователь (INP дропдауна 750 мс, один коммит на 620 мс из-за свежего key, обошедшего мемоизацию)."
  caption="Два спринта сдвинули экономию около 4 мс, которую пользователь не ощутит, пока ремаунт на 620 мс, определивший INP, остался неспрофилированным — довод за то, чтобы измерять до оптимизации."
  hue="var(--d-frontend)"
/>

<Quiz
  id="01-profiler-and-measurement-quiz-1"
  lessonSlug="01-profiler-and-measurement"
  lang="ru"
  question="В данных onRender для коммита actualDuration равен 39 мс, а baseDuration — 41 мс, хотя поддерево плотно обёрнуто в React.memo. О чём это говорит?"
  choices={[
    { label: "Мемоизация в этом коммите почти ничего не даёт — платится почти полная цена без memo, значит пропсы либо реально меняются, либо проверки memo сломаны (свежие объекты, меняющиеся key)", correct: true },
    { label: "Накладные расходы профилирования раздувают actualDuration, на деле рендер куда быстрее", misconception: "Профилирование добавляет небольшую цену учёта, но раздувает оба числа одновременно — оно не объясняет, почему actual догнал base. Когда числа близки, потомки действительно рендерятся; сравнение осмысленно именно потому, что оба значения из одного инструментированного прогона." },
    { label: "baseDuration включает время мутаций DOM в фазе commit, поэтому числа несравнимы", misconception: "Оба значения измеряют только работу фазы рендера — baseDuration это сумма последних времён рендера потомков, оценка худшего случая без мемоизации. Времени мутаций DOM нет ни в одном из них; они сравнимы по построению." },
    { label: "Так и должно быть: actualDuration всегда держится в нескольких мс от baseDuration", misconception: "В хорошо мемоизированном поддереве actualDuration часто составляет малую долю baseDuration — большинство потомков выходят по bail-out и не добавляют ничего. Сближение двух чисел — это сигнатура мемоизации, которая перестала работать." },
  ]}
/>

## INP и миф о 16 мс

Число, которое цитируют все, — 16,7 мс, один кадр при 60 Гц — это бюджет **непрерывного движения**: анимации, скролла, перетаскивания. Промахнётесь во время драга — пользователь увидит рывок. Но клик — не анимация. Для дискретных взаимодействий пользовательская метрика — **INP (Interaction to Next Paint)**: от нажатия до отрисовки следующего кадра, включая **input delay** (главный поток был занят, когда пришло событие), **processing** (ваши обработчики, рендер и коммит React) и **presentation delay** (стили, лейаут, отрисовка). Пороги: 200 мс — «хорошо», свыше 500 мс — «плохо», то есть примерно от 12 до 30 кадровых бюджетов, причём INP страницы — это примерно её *худшее* взаимодействие за визит, а не среднее.

Поэтому рамка «16 мс» сбивает оптимизацию в обе стороны. Она выставляет коммит в 30 мс по клику на кнопку нарушением, достойным рефакторинга, хотя пользователь не отличит 30 мс от мгновенного. И она молчит о настоящем убийце: любая задача в главном потоке дольше 50 мс — это **long task**, добавляющий input delay всему, что пользователь сделает следом; коммит на 620 мс не просто тормозит своё взаимодействие — он замораживает каждый клик, прилетевший во время него. Рабочий цикл, по порядку: прочитать INP из полевых данных (метрика привязана к конкретным взаимодействиям, поэтому лабораторные замеры на быстром ноутбуке её регулярно не видят), найти худшее взаимодействие, спрофилировать *его*, починить самую широкую полосу, измерить снова. Команда из истории прошла этот цикл задом наперёд — чинить сразу, не измерять никогда.

<Quiz
  id="01-profiler-and-measurement-quiz-2"
  lessonSlug="01-profiler-and-measurement"
  lang="ru"
  question="Обработчик клика вызывает коммит React длительностью 35 мс. Коллега бьёт тревогу: «это больше двух кадров, надо мемоизировать». Какое решение верное?"
  choices={[
    { label: "Оставить как есть: 16,7 мс — бюджет непрерывного движения, а дискретный клик оценивается по INP, где 35 мс глубоко внутри порога «хорошо» в 200 мс — усилия нужны худшему взаимодействию, не этому", correct: true },
    { label: "Коллега прав: каждый коммит, не уложившийся в кадр, — это заметный пользователю рывок", misconception: "Кадровый бюджет работает, когда пользователь следит за непрерывным движением — анимацией, скроллом, драгом. Отзывчивость дискретного клика воспринимается через время до следующей отрисовки, и 35 мс неотличимы от мгновенного; порог «хорошо» у INP — 200 мс, а не 16." },
    { label: "Разбить коммит на несколько мелких, чтобы каждый уложился в 16 мс", misconception: "Разбиение одного дешёвого коммита на несколько добавляет работы (больше проходов согласования, больше отрисовок) и не улучшает ничего ощутимого. Дробление окупается только для long tasks дольше 50 мс, блокирующих ввод, — 35 мс к ним не относятся." },
    { label: "Замерить среднюю длительность коммитов за сессию; если среднее меньше 16 мс — страница в порядке", misconception: "INP — это примерно худшее взаимодействие визита, а не среднее: усреднение прячет ровно тот выброс в 600 мс, который и определяет опыт пользователя. Один ужасный клик по фильтру перевешивает тысячу быстрых и в метрике, и в памяти пользователя." },
  ]}
/>

</Explanation>

<DiagramFrame label="Одно дискретное взаимодействие едет по таймлайну INP: нажатие пережидает input delay, пока занят главный поток, проходит через processing — обработчики, рендер, коммит — и часы останавливаются только на следующей отрисовке" hue="var(--d-frontend)" caption="Латентность одного взаимодействия — сумма трёх фаз: input delay, processing (здесь живёт доля React — её Profiler и фиксирует по коммитам) и presentation до следующего отрисованного кадра. Полевой INP — примерно худшее взаимодействие, а не среднее: оптимизация идёт справа налево — полевые данные называют худшее взаимодействие, флеймграф называет дорогой коммит внутри него. Бюджет кадра в 16 мс правит непрерывным движением; у дискретных взаимодействий есть 200 мс.">
  <svg viewBox="0 0 360 130" width="360" height="130" style="display:block;margin:0 auto;max-width:100%;height:auto;">
    <text x="180" y="20" text-anchor="middle" font-size="10" font-family="var(--font-body)" fill="var(--muted)">часы INP идут от нажатия до следующей отрисовки</text>
    <rect x="6" y="46" width="42" height="32" rx="6" fill="var(--card-2)" stroke="var(--hairline-strong)" stroke-width="0.8" />
    <text x="27" y="65" text-anchor="middle" font-size="11" font-family="var(--font-body)" fill="var(--ink)">тап</text>
    <rect x="60" y="46" width="86" height="32" rx="6" fill="var(--card-2)" stroke="var(--hairline-strong)" stroke-width="0.8" />
    <text x="103" y="60" text-anchor="middle" font-size="10" font-family="var(--font-body)" fill="var(--ink)">input delay</text>
    <text x="103" y="72" text-anchor="middle" font-size="8" font-family="var(--font-body)" fill="var(--muted)">главный поток занят</text>
    <rect x="158" y="46" width="100" height="32" rx="6" fill="var(--card-2)" stroke="var(--hairline-strong)" stroke-width="0.8" />
    <text x="208" y="60" text-anchor="middle" font-size="10" font-family="var(--font-body)" fill="var(--ink)">processing</text>
    <text x="208" y="72" text-anchor="middle" font-size="8" font-family="var(--font-body)" fill="var(--muted)">обработчики + рендер + коммит</text>
    <rect x="270" y="46" width="84" height="32" rx="6" fill="var(--card-2)" stroke="var(--hairline-strong)" stroke-width="0.8" />
    <text x="312" y="60" text-anchor="middle" font-size="10" font-family="var(--font-body)" fill="var(--ink)">presentation</text>
    <text x="312" y="72" text-anchor="middle" font-size="8" font-family="var(--font-body)" fill="var(--muted)">следующая отрисовка</text>
    <path data-path d="M48 62 L350 62" fill="none" stroke="var(--hairline-2)" stroke-width="1.4" stroke-dasharray="4 4" />
    <text x="180" y="106" text-anchor="middle" font-size="10" font-family="var(--font-body)" fill="var(--muted)">INP ≈ худшее взаимодействие на странице — бюджет 200 мс</text>
    <PacketDot color="var(--d-frontend)" durationMs={1500} size={8} />
    <PacketDot color="var(--d-frontend)" durationMs={1500} delayMs={500} size={8} />
  </svg>
</DiagramFrame>

<KeyTakeaway>Работа над производительностью начинается с измерения, а не с гипотезы. Профайлер DevTools нарезает сессию на коммиты и показывает по каждому, какой компонент сжёг время и почему он рендерился; Profiler API отправляет те же тайминги (actualDuration против baseDuration) в мониторинг и обнажает, приносит ли мемоизация хоть что-то. Влияние на пользователя оценивайте по INP — 200 мс на дискретное взаимодействие, метрику определяет худший случай, — а не по кадровому бюджету в 16 мс, который относится к непрерывному движению. Чините самую широкую полосу худшего взаимодействия и измеряйте снова.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="01-profiler-and-measurement-retrieval"
  lang="ru"
  questions={[
    { q: "Мониторинг показывает INP 700 мс на выпадающем списке. Опишите полный диагностический цикл до любых правок кода — с инструментами и числами, которые вы будете читать.", a: "Начните с полевых данных: подтвердите, какое именно взаимодействие худшее (INP — это примерно худшее взаимодействие визита, так что число у дропдауна — конкретное событие, а не среднее). Воспроизведите его локально с записью в профайлере React DevTools: сессия делится на коммиты, и столбчатая диаграмма сразу показывает, что это — один гигантский коммит или шторм мелких; болезни противоположные. Откройте флеймграф худшего коммита: ширина полосы — цена рендера, серое — не рендерилось, а «record why each component rendered» подписывает причину (пропсы, стейт, родитель, смена key). Разложите по компонентам INP: input delay (главный поток уже был заблокирован long task дольше 50 мс?), processing (обработчики плюс рендер плюс коммит — доля React), presentation delay. Решающие числа перепроверьте на продакшен-сборке с профилированием — dev-React заметно медленнее. Только после этого меняйте код, целясь в самую широкую полосу, и перемерьте то же взаимодействие, чтобы доказать: фикс сдвинул INP, а не просто счётчики рендеров." },
    { q: "Объясните, что означают actualDuration и baseDuration в колбэке onRender и как их сравнение показывает, окупилась ли кампания мемоизации.", a: "Оба числа описывают фазу рендера одного коммита внутри профилируемого поддерева. actualDuration — сколько коммит реально рендерился, со всеми bail-out и мемоизацией. baseDuration — синтетический худший случай: сумма последних индивидуальных времён рендера всех потомков, то есть примерная цена коммита, если бы ничто не мемоизировалось и всё поддерево перерендеривалось. Здоровое мемоизированное дерево показывает actualDuration малой долей baseDuration — большинство детей вышли по bail-out и ничего не добавили. Если же actualDuration идёт вровень с baseDuration при React.memo повсюду — обёртки кто-то обезоруживает: свежие объекты или функции в пропсах на каждом рендере либо меняющийся key, форсирующий ремаунты (а ремаунт пропускает сравнение memo целиком). Сравнение страхует и от обратной ошибки: если baseDuration на взаимодействии, на которое жалуются, всего 40 мс, мемоизация не может быть лекарством — даже худший случай дёшев; настоящая цена живёт в каком-то другом коммите. Это и есть ловушка оптимизации без чтения этих двух чисел." },
  ]}
/>

<Recap lang="ru">
У работы над производительностью React есть необсуждаемый первый шаг: измерить, потому что интуиция стабильно указывает не на то поддерево. Профайлер React DevTools записывает взаимодействия как серию коммитов; флеймграф каждого коммита показывает цену рендера шириной полосы, серым — компоненты с bail-out, а при включённом «record why each component rendered» — причину каждой полосы: изменившиеся пропсы, стейт, рендер родителя или смена key (тихий триггер ремаунта, который обесценил двухспринтовую memo-кампанию команды из истории, пока ремаунт графика на 620 мс прятался у всех на виду). Программируемый напарник — компонент Profiler: его onRender сообщает actualDuration (цена коммита с мемоизацией) против baseDuration (оценка худшего случая без неё), и зазор между ними — единственная честная мера того, зарабатывает ли React.memo хоть что-нибудь; оборачивайте несколько отслеживаемых интеракций, а не корень — профилирование имеет небольшую цену на каждый коммит. Влияние меряйте в единицах пользователя, а не React: дискретными взаимодействиями правит INP — input delay, processing, presentation delay; хорошо — до 200 мс, плохо — после 500, и метрику определяет худшее взаимодействие визита, — а знаменитые 16,7 мс относятся к непрерывному движению вроде скролла и анимации. Коммиту клика в 35 мс не нужно ничего; коммит в 620 мс — это long task, замораживающий заодно каждый клик, прилетевший во время него. Цикл такой: полевой INP называет взаимодействие, флеймграф — коммит, чините самую широкую полосу, измеряете снова. Теперь, когда коллега потянется к `React.memo` ещё до открытия профайлера, вы знаете, что спросить: какой коммит, какая полоса и что полевой INP говорит об этом взаимодействии?
</Recap>
