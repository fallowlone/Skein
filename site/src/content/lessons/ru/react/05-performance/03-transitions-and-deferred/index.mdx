---
concepts:
- react-transitions
deepensInto: []
estMin: 19
lang: ru
lessonType: topic
level: senior
order: 3
prereqs: []
slug: "03-transitions-and-deferred"
sources:
- https://react.dev/reference/react/useTransition
- https://react.dev/reference/react/useDeferredValue
- https://react.dev/reference/react/Suspense
spiral: []
status: ready
summary: 'Transition делит апдейты на срочные и фоновые: срочный коммит проходит первым, тяжёлый рендер прерывается каждым нажатием. useDeferredValue — то же для получаемых значений; с Suspense transition держит старый UI без вспышки fallback. Один гигантский компонент прервать нельзя.'
title: 'Transitions и useDeferredValue: медленный рендер делаем прерываемым, а не быстрым'
track: "react"
unit: "05-performance"
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";
import DiagramFrame from "~/components/diagram/DiagramFrame.astro";
import PacketDot from "~/components/diagram/PacketDot.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import Infographic from "~/components/diagram/Infographic.astro";
import apiChoice from "./infographics/api-choice.svg?raw";

<Hook>
Команда продуктового поиска читала про конкурентный React, поэтому, когда фильтр каталога начал терять нажатия — 280 мс рендера на символ по 20 000 карточек товаров, — фикс казался очевидным: обернуть обновление стейта в `startTransition`. Обернули `setQuery(e.target.value)` и зарелизили. Ничего не улучшилось. Хуже: теперь лагал *сам инпут*, потому что текст в поле управлялся тем же `query`, а transition вежливо понизил приоритет ровно того апдейта, который отзеркаливает нажатие пользователя. Сеньор команды заметил это на ревью неделей позже: transition не делает работу дешевле, он раскладывает её по полосам — а они затолкали единственную задачу срочной полосы в медленную. Фиксом стали два стейта: `setText(value)` остаётся срочным, чтобы инпут откликался мгновенно, а `startTransition(() => setQuery(value))` уносит 280-миллисекундный рендер списка в фон, перезапускаемый каждым новым нажатием. Печать стала зеркально гладкой, результаты отставали на долю секунды — рендер в 280 мс никуда не делся, просто пользователь перестал платить за него своими нажатиями.
</Hook>

<Crux>Transition помечает апдейт несрочным: React рендерит его фоном, прерываемо, и держит старый UI — без вспышки fallback.</Crux>

<Explanation>

## Две полосы: срочная и несрочная

`startTransition` ничего не ускоряет, не дебаунсит и не уносит работу с главного потока. Он меняет *приоритет* обновлений стейта внутри своего колбэка: они становятся **transitions** — несрочными апдейтами, которые React рендерит в фоне, пока экран показывает последний закоммиченный UI. Срочные апдейты (печать, клики, всё необёрнутое) прерывают этот фоновый рендер: React бросает наполовину построенное work-in-progress-дерево, сначала коммитит срочный апдейт, затем перезапускает рендер transition с самого свежего стейта. Во время фонового рендера React к тому же уступает главный поток между компонентами (примерно каждые 5 мс), так что браузер может рисовать и обрабатывать события посреди рендера, а не замирать на все 280 мс.

```jsx
function CatalogSearch({ products }) {
  const [text, setText] = useState("");        // срочно: отзеркаливает нажатие
  const [query, setQuery] = useState("");      // transition: питает тяжёлый список
  const [isPending, startTransition] = useTransition();

  function handleChange(e) {
    setText(e.target.value);                   // коммитится немедленно
    startTransition(() => {
      setQuery(e.target.value);                // рендерится фоном, прерываемо
    });
  }

  return (
    <>
      <input value={text} onChange={handleChange} />
      <div style={{ opacity: isPending ? 0.6 : 1 }}>
        <ProductGrid products={products} query={query} />
      </div>
    </>
  );
}
```

Разделение — и есть вся конструкция: рендер срочного стейта крошечный (один инпут) и коммитится за кадр-два; дорогое дерево зависит только от transition-стейта, поэтому его рендер прерываем и перезапускаем. `isPending` даёт честный сигнал загрузки для устаревшего, но видимого контента. Первая попытка команды из истории провалилась потому, что один стейт делал обе работы — обёртка в transition сделала несрочным и эхо инпута. **Эвристика:** значение, которым пользователь манипулирует напрямую, никогда не должно жить внутри transition.

<DiagramFrame label="Time-slicing на transition-полосе: попытки рендера тяжёлого дерева едут к коммиту по дорожке, разрывы которой — паузы по ~5 мс, а каждое новое нажатие бросает попытку в полёте и перезапускает её" hue="var(--d-frontend)" caption="Каждая точка — одна попытка рендера тяжёлого дерева: она едет с прерываниями — разрывы и есть возвраты управления главному потоку, — и коммитится только попытка, доехавшая до конца. Срочная полоса (эхо инпута) коммитится за кадр, и именно она всё время прерывает эту дорожку: бросить, перезапустить от последнего состояния, повторить.">
  <svg viewBox="0 0 360 130" width="360" height="130" style="display:block;margin:0 auto;max-width:100%;height:auto;">
    <text x="180" y="20" text-anchor="middle" font-size="10" font-family="var(--font-body)" fill="var(--muted)">transition-полоса: фоновый рендер, паузы ~5 мс</text>
    <rect x="6" y="46" width="70" height="32" rx="6" fill="var(--card-2)" stroke="var(--hairline-strong)" stroke-width="0.8" />
    <text x="41" y="65" text-anchor="middle" font-size="11" font-family="var(--font-body)" fill="var(--ink)">нажатие</text>
    <rect x="290" y="46" width="64" height="32" rx="6" fill="var(--card-2)" stroke="var(--hairline-strong)" stroke-width="0.8" />
    <text x="322" y="65" text-anchor="middle" font-size="11" font-family="var(--font-body)" fill="var(--ink)">commit</text>
    <path data-path d="M76 62 L290 62" fill="none" stroke="var(--hairline-2)" stroke-width="1.4" stroke-dasharray="12 6" />
    <text x="180" y="106" text-anchor="middle" font-size="10" font-family="var(--font-body)" fill="var(--muted)">новое нажатие = бросить попытку и перезапустить от свежего состояния</text>
    <PacketDot color="var(--d-frontend)" durationMs={1400} size={8} />
    <PacketDot color="var(--d-frontend)" durationMs={1400} delayMs={450} size={8} />
    <PacketDot color="var(--d-frontend)" durationMs={1400} delayMs={900} size={8} />
  </svg>
</DiagramFrame>

<Quiz
  id="03-transitions-and-deferred-quiz-1"
  lessonSlug="03-transitions-and-deferred"
  lang="ru"
  question="Управляемый поисковый инпут питает дорогой список результатов через одно значение стейта. Разработчик оборачивает весь setState в startTransition — и инпут начинает лагать. Что произошло?"
  choices={[
    { label: "Отображаемое значение инпута управляется тем же стейтом, так что его эхо-апдейт тоже стал несрочным — фикс в разделении стейта: срочное значение для инпута, transition-значение для дорогого списка", correct: true },
    { label: "startTransition выполняет апдейт в фоновом потоке, и инпут ждёт синхронизации обратно", misconception: "Второго потока нет — весь рендеринг остаётся на главном потоке. Transitions меняют приоритет планирования, а не место исполнения: React нарезает фоновый рендер по времени и даёт срочной работе его прерывать. Лаг возник из-за приоритета, не из-за потоков." },
    { label: "Transitions добавляют намеренную задержку, чтобы React успел сбатчить больше апдейтов", misconception: "Transition не добавляет фиксированной задержки и не является дебаунсом. Рендер transition начинается сразу, если нет ничего срочного; изменилось то, что апдейт самого инпута теперь ни за кем не стоял — он сам стал работой с пониженным приоритетом." },
    { label: "Дорогой список внутри transition всё равно блокирует главный поток, так что улучшения невозможны в принципе", misconception: "Нарезка по времени уступает поток между компонентами во время рендера transition, так что главный поток остаётся отзывчивым — паттерн работает, что доказала вторая попытка команды. Провал был в том, что срочный апдейт попал внутрь transition, а не в ограничении нарезки." },
  ]}
/>

## useDeferredValue: откладываем значения, которыми не владеем

`useTransition` требует владеть вызовом `setState`. Часто это не так — проп приходит от родителя, роутера или библиотечного хука. `useDeferredValue(value)` — эквивалент для принимающей стороны: он возвращает *предыдущее* значение, пока фоновый ре-рендер с новым значением в полёте, позволяя держать дорогое поддерево на устаревшем значении, не трогая производителя.

```jsx
function SearchResults({ query }) {           // query обновляется срочно выше
  const deferredQuery = useDeferredValue(query);
  const isStale = deferredQuery !== query;
  const grid = useMemo(
    () => <HeavyGrid query={deferredQuery} />,
    [deferredQuery]
  );
  return <div style={{ opacity: isStale ? 0.6 : 1 }}>{grid}</div>;
}
```

Механика: когда `query` меняется, React сначала срочно ре-рендерит с `deferredQuery`, всё ещё держащим старое значение, — этот рендер обязан быть дешёвым, поэтому дорогое поддерево мемоизировано по `deferredQuery` и выходит по bail-out, — а затем запускает фоновый рендер, где `deferredQuery` догоняет. Этот фоновый рендер — transition: прерываемый, перезапускаемый при следующем изменении. Мемоизация здесь не декоративна: без неё срочный рендер всё равно перерендерит `HeavyGrid` со *старым* значением, и вы заплатите полную цену дважды за нажатие. **Выбор между ними:** владеете сеттером → `useTransition` (плюс `isPending` бесплатно); получаете значение → `useDeferredValue` (сравнивайте значения для определения устаревания).

<Infographic
  svg={apiChoice}
  label="Сравнение двух API, каждый из которых запускает прерываемый фоновый рендер дорогого поддерева. useTransition: вы владеете setState (оборачиваете setQuery в startTransition), получаете isPending бесплатно, разделяете состояние на срочное значение инпута и transition-значение и держите напрямую управляемое значение вне transition. useDeferredValue: вы получаете значение пропом, оборачивать нечего, сравниваете deferred с current на устаревание, мемоизируете поддерево по deferred-значению, иначе платите дважды, и делаете дешёвый срочный рендер со старым значением перед тем, как догнать."
  caption="Оба дают одинаковый прерываемый фоновый рендер; выбирайте по владению — владеете сеттером берите useTransition, получаете значение берите useDeferredValue (и мемоизируйте по нему поддерево)."
  hue="var(--d-infra)"
/>

## Transitions и Suspense: без вспышки fallback

Вторая суперспособность — то, что transitions делают с **Suspense**. Срочный апдейт, который приостанавливается — скажем, смена вкладки заставляет компонент бросить промис за данными, которых нет, — прячет уже видимый контент за ближайшим fallback: страница, которую вы читали, заменяется спиннером. Тот же апдейт внутри transition ведёт себя иначе: React держит на экране *текущий закоммиченный* UI, рендерит приостановленное дерево в фоне и коммитит только когда данные готовы. Никакой вспышки fallback; старая вкладка остаётся видимой (затемните её через `isPending`). Поэтому навигации роутеров в современных фреймворках по умолчанию обёрнуты в transitions — клик по ссылке не должен превращать работающую страницу в спиннер. Исключение точное: защищён только *уже показанный* контент. Граница `Suspense`, монтирующаяся впервые внутри transition, всё равно покажет свой fallback — прежнего UI, который можно удержать, не существует.

## Когда transitions не делают ничего

Transitions покупают прерываемость *между* компонентами и применяются к работе *фазы рендера*. Три ситуации их обезоруживают. **Один гигантский компонент:** React уступает поток между рендерами компонентов и никогда — внутри одного; монолитный график, строящий 10 000 SVG-элементов в одной функции за 200 мс, блокирует полосу при любом приоритете; разбейте его на мелкие компоненты или виртуализируйте. **Цена не в рендере:** если тормозит синхронный `filter` по 100k элементов прямо в обработчике события или layout thrash в эффектах — работа происходит вне фазы рендера, которую планируют transitions. **Один дешёвый коммит:** если апдейт был 20-миллисекундным рендером с одним коммитом, резать нечего — обёртка не меняет ничего наблюдаемого. Transition — это подсказка планировщику, а не ускорение: суммарная работа CPU та же или чуть выше (брошенные рендеры оплачены и выброшены — такова сделка: потраченные ватты в обмен на отзывчивый ввод).

<Quiz
  id="03-transitions-and-deferred-quiz-2"
  lessonSlug="03-transitions-and-deferred"
  lang="ru"
  question="Клик по вкладке подставляет компонент, который приостанавливается на время загрузки. Без transition вся страница заменяется спиннером; внутри startTransition — нет. Что именно меняет transition?"
  choices={[
    { label: "React держит закоммиченный UI на экране и рендерит приостановленное дерево в фоне, коммитя только по готовности — уже показанный контент во время transition никогда не заменяется на fallback", correct: true },
    { label: "Transition заставляет запрос стартовать раньше, и данные успевают до того, как понадобится fallback", misconception: "Transitions вообще не трогают загрузку данных — запрос стартует в тот же момент и длится столько же. Меняется то, что показывается в ожидании: прежний закоммиченный UI вместо fallback границы." },
    { label: "Transition полностью подавляет Suspense, и компонент рендерится с пустыми данными", misconception: "Компонент всё равно приостанавливается — рендер не может продолжиться без данных. Transition меняет видимое поведение Suspense для показанного контента: держать старый UI, а не прятать его за fallback. Новые границы, монтирующиеся внутри transition, всё равно показывают fallback." },
    { label: "Fallback пропускается лишь потому, что transition откладывает апдейт до попадания данных в кеш", misconception: "Никакой задержки в ожидании кеша нет: фоновый рендер реально начинается, приостанавливается и возобновляется с приходом данных. Если пользователь снова навигирует в полёте, transition прерывается и перезапускается — поведение, которого простая задержка дать не может." },
  ]}
/>

<DragOrder
  id="03-transitions-and-deferred-order"
  lessonSlug="03-transitions-and-deferred"
  lang="ru"
  prompt="Расставьте шаги по порядку, чтобы сделать медленный рендер списка в 280 мс неблокирующим с помощью startTransition:"
  items={[
    { id: "a", label: "Разделить на две переменные состояния: одна для текста инпута (срочная), другая для query, питающего тяжёлый список (transition) — чтобы эхо и рендер шли разными полосами" },
    { id: "b", label: "В обработчике изменений вызвать setText(value) напрямую, чтобы эхо инпута коммитилось за кадр и UI оставался мгновенно отзывчивым" },
    { id: "c", label: "Обернуть setQuery(value) в startTransition(() => ...), чтобы пометить рендер списка несрочным и прерываемым последующими нажатиями" },
    { id: "d", label: "Считать isPending из useTransition и добавить визуальный сигнал (например opacity: 0.6) к устаревшему списку, чтобы пользователи знали, что результаты обновляются" },
    { id: "e", label: "Убедиться в Profiler, что нажатия клавиш больше не блокируют срочную полосу — рендер списка по-прежнему 280 мс, но инпут больше не тормозит" },
  ]}
  correctOrder={["a", "b", "c", "d", "e"]}
/>

</Explanation>

<FlowDiagram
  label="Нажатие клавиши расходится по двум полосам: срочное эхо коммитится за кадр, а transition рендерит тяжёлое дерево в фоне, прерываемое следующим нажатием"
  hue="var(--d-frontend)"
  perRow={3}
  nodes={[
    { id: "keystroke", label: "Нажатие клавиши", sub: "обработчик события" },
    { id: "urgent", label: "Срочная полоса", sub: "setText — эхо инпута" },
    { id: "commit1", label: "Коммит ≤1 кадра", sub: "инпут обновился" },
    { id: "transition", label: "Полоса transition", sub: "setQuery — тяжёлое дерево" },
    { id: "bg", label: "Фоновый рендер", sub: "уступает каждые ~5 мс" },
    { id: "commit2", label: "Коммит по готовности", sub: "или сброс + рестарт" },
  ]}
  edges={[
    { from: "keystroke", to: "urgent", label: "без обёртки" },
    { from: "keystroke", to: "transition", label: "startTransition" },
    { from: "urgent", to: "commit1", label: "немедленно" },
    { from: "transition", to: "bg", label: "планирование" },
    { from: "bg", to: "commit2", label: "завершение" },
    { from: "commit1", to: "bg", label: "прерывает + перезапускает" },
  ]}
  caption="Экран всегда показывает последний закоммиченный UI: инпут откликается мгновенно из срочной полосы, а рендер transition бросается и перезапускается на каждом новом нажатии. С Suspense тот же механизм удерживает показанный контент на экране вместо вспышки fallback."
/>

<KeyTakeaway>startTransition переклассифицирует апдейты в несрочные: React рендерит их в фоне, уступая главный поток и перезапускаясь при срочных апдейтах, — та же цена CPU, отзывчивый ввод. Держите напрямую управляемое значение вне transition; берите useDeferredValue, когда получаете значение, а не владеете сеттером, и мемоизируйте дорогое поддерево по отложенному значению. Внутри transition приостановка держит показанный UI вместо вспышки fallback. Ничто из этого не помогает, когда цена сидит в одном гигантском компоненте, в обработчиках или в единственном дешёвом коммите.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="03-transitions-and-deferred-retrieval"
  lang="ru"
  questions={[
    { q: "Разберите по кадрам, что делает React, когда пользователь быстро печатает два символа в инпут, чей обработчик делает setText срочно и setQuery внутри startTransition.", a: "Первый символ: setText планирует срочный апдейт; React рендерит маленькое срочное дерево (инпут показывает «a») и коммитит за кадр. Transition-апдейт для query начинает рендерить тяжёлый список в фоне — React обрабатывает компоненты по одному, уступая главный поток примерно каждые 5 мс, так что страница остаётся интерактивной, пока растёт work-in-progress-дерево. Второй символ прилетает посреди рендера: срочный setText прерывает; React бросает наполовину построенное дерево transition целиком (экран его не видел — видимы только коммиты), немедленно коммитит эхо «ab», затем перезапускает рендер transition с самого свежего стейта, query = «ab» — он не доделывает протухший рендер с «a». Когда рендер transition наконец завершается без прерываний, он коммитится одним атомарным проходом, и список догоняет. Итог: задержка ввода — один дешёвый коммит независимо от размера списка; тяжёлый рендер оплачивается, возможно, несколько раз (брошенные попытки — потраченный CPU), и это явная сделка — ватты в обмен на отзывчивость. isPending всё это время true, так что UI может честно затемнять устаревший список." },
    { q: "Назовите три ситуации, в которых обёртка апдейта в startTransition ничего не меняет, и каков настоящий фикс в каждой.", a: "Первая: дорогая работа живёт в одном гигантском компоненте. React уступает поток между рендерами компонентов и никогда внутри одной функции, так что монолитный компонент-график на 200 мс блокирует главный поток на любой полосе. Фикс: декомпозировать на много мелких компонентов, чтобы появились точки уступки, либо виртуализировать/ограничить отрисовку. Вторая: цена не в фазе рендера — синхронный фильтр по 100k элементов прямо в обработчике, тяжёлый useLayoutEffect, layout thrash в коммите. Transitions планируют только работу рендера; код обработчика выполняется до всего этого по полной цене. Фикс: перенести вычисление в рендер, выведенный из transition-стейта (сделав его нарезаемым), мемоизировать или унести с главного потока (worker). Третья: апдейт и так был одним дешёвым коммитом — рендер в 20 мс коммитится один раз что с обёрткой, что без; прерывать нечего, наблюдаемой разницы нет. Фикс: никакого — проблемы не было, обёртка — шум. Объединяющая модель: transition — это метка приоритета на обновлениях стейта, которую планировщик читает в фазе рендера. Работе неделимой, лежащей вне рендера или уже быстрой эта метка дать ничего не может." },
  ]}
/>

<Recap lang="ru">
Ответ конкурентного React на дорогие апдейты — не делать их быстрее, а делать прерываемыми. startTransition помечает обновления стейта в своём колбэке несрочными: React рендерит их в фоне в work-in-progress-дерево, уступает главный поток между компонентами примерно каждые 5 мс, позволяет любому срочному апдейту прервать себя — бросая недостроенное дерево и перезапускаясь с новейшего стейта — и коммитит только готовый результат. Экран всегда показывает последний закоммиченный UI, а isPending сообщает о зазоре. Кардинальная ошибка — обернуть значение, которым пользователь манипулирует напрямую: управляемый инпут на transition-стейте отзеркаливает нажатия с фоновым приоритетом — тот самый лаг, который зарелизила команда каталога. Разделяйте стейт: срочный для эха, transition для тяжёлого дерева. Когда меняющееся значение приходит пропом, а не вашим setState, useDeferredValue — зеркало для принимающей стороны: он удерживает старое значение через дешёвый срочный рендер (мемоизируйте дорогое поддерево по отложенному значению, иначе трюк не даёт ничего) и догоняет в фоновом. С Suspense transitions убирают вспышку fallback: приостановившийся апдейт держит уже показанный контент на экране и коммитит по приходе данных — причина, по которой роутеры фреймворков оборачивают навигации в transitions, — хотя впервые монтирующиеся границы всё равно показывают fallback. И знайте пределы: уступки происходят между компонентами, так что монолитный компонент на 200 мс блокирует любую полосу; вычисления в обработчиках и эффекты выполняются вне планируемого рендера; единственный дешёвый коммит нарезать нечем. Суммарный CPU растёт, а не падает — брошенные рендеры суть цена ввода, который никогда не заикается. Теперь, когда увидишь лагающий инпут рядом с обновляющимся списком, первым делом потянешься за разделением стейта на два, а не за дебаунсом или `useMemo`.
</Recap>
