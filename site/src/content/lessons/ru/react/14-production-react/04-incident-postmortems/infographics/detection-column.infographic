infographic hierarchy-tree-curved-line-rounded-rect-node
data
  root
    label Три инцидента React — читай по колонке обнаружения
    children
      - label Цикл рендера (тихо жжёт CPU)
        children
          - label Обнаружение: RUM длинные задачи 12×, без исключений
          - label Причина: нестабильная объектная dep + async setState
          - label Предотвращение: алерт длинных задач по кольцу релиза
      - label Шторм расхождений гидрации
        children
          - label Обнаружение: 40 000 ошибок/мин через onRecoverableError
          - label Причина: toLocaleString, зависящий от таймзоны
          - label Предотвращение: метрика расхождений гейтит раскат
      - label Утечка подписки
        children
          - label Обнаружение: тикеты следуют за возрастом сессии, без алерта
          - label Причина: subscribe без cleanup
          - label Предотвращение: длинносессионная синтетика кучи
theme
  palette #6366f1 #f59e0b #f43f5e
