---
concepts:
- hidden-class
- inline-cache
- jit-tiers
- deopt-loop
- megamorphic
- orinoco-gc
estMin: 13
lang: ru
level: senior
order: 7
prereqs: []
slug: quiz-choice
sources:
- https://v8.dev/blog/maglev
- https://mathiasbynens.be/notes/shapes-ics
- https://v8.dev/blog/concurrent-marking
status: ready
summary: "Синтез по всему юниту в формате выбора — переходы hidden class, состояния inline cache, четыре уровня JIT, deopt-loop, megamorphic-сайты и взаимодействие с Orinoco GC."
title: 'Внутренности V8: тест с множественным выбором'
track: browser
unit: 03-v8-internals
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Шесть вопросов, проходящих сквозь весь юнит. Каждый — это решение, которое вы принимаете, читая реальную трассу производительности: не определение для заучивания, а механизм, который надо разобрать под нагрузкой.
</Hook>

<Goal lang="ru">
Убедитесь, что вы связываете форму hidden class, состояние IC, продвижение по уровням, поведение deopt и барьеры GC — синтез, к которому вели отдельные уроки.
</Goal>

<Quiz
  id="03-v8-internals-choice-1"
  lessonSlug="03-v8-internals"
  lang="ru"
  question="Горячая render-функция выдавала 5µs в тестах, но 250µs в проде. Снимок кучи показывает, что объекты, приходящие в один сайт доступа к свойству, созданы 7 разными конструкторами. В чём причина на уровне V8 и каков настоящий фикс?"
  choices={[
    { label: "IC на этом сайте стал megamorphic — после 4 hidden class V8 перестаёт отслеживать классы по отдельности и откатывается к общему поиску за ~50–100 циклов. Фикс выше по потоку: свести форму к одному hidden class или разбить цикл по формам.", correct: true },
    { label: "TurboFan был отключён в прод-сборке, поэтому функция не оптимизировалась.", misconception: "TurboFan нельзя выборочно отключить для окружения. Функция оптимизировалась; её IC деградировал до megamorphic, потому что прод-трафик несёт разнообразие форм, которого тесты не покрывали." },
    { label: "Прод-машина медленнее, поэтому тот же monomorphic-код просто работает на меньшей частоте.", misconception: "Состояние IC зависит от наблюдаемых hidden class, а не от частоты процессора. 7 конструкторов на одном сайте — это сигнатура megamorphic, штраф в 10–50× по алгоритму, а не разница в железе." },
    { label: "Выросло давление на GC, добавив паузу к каждому вызову.", misconception: "GC влияет на частоту пауз, а не на пропускную способность одного вызова. Замедление в 50× на вызов, локализованное в одном сайте, — это деградация IC, а не GC." },
  ]}
/>

<Quiz
  id="03-v8-internals-choice-2"
  lessonSlug="03-v8-internals"
  lang="ru"
  question="Два объекта построены как {x:1, y:2} и {y:2, x:1}. Функция читает .x на обоих в горячем цикле. В какое состояние придёт сайт доступа и почему?"
  choices={[
    { label: "Polymorphic — hidden class кодирует ПОРЯДОК добавления свойств, поэтому два объекта оказываются в разных листьях дерева переходов и выглядят как две разные формы на сайте.", correct: true },
    { label: "Monomorphic — у обоих объектов одинаковый набор свойств, так что V8 считает их одной формой.", misconception: "V8 привязывает IC к hidden class, который фиксирует порядок добавления свойств, а не только итоговый набор. Разный порядок — разный лист, значит две формы." },
    { label: "Megamorphic — любые два разных литерала объекта сразу переполняют кэш.", misconception: "Megamorphic требует 5+ различных hidden class на сайте. Две формы — это polymorphic, короткая цепочка ветвлений, всё ещё быстро." },
    { label: "Uninitialized — V8 не может отслеживать сайт, видящий больше одного объекта.", misconception: "V8 отслеживает hidden class на каждом сайте по всем вызовам; он переходит uninitialized → monomorphic → polymorphic → megamorphic по мере накопления форм." },
  ]}
/>

<Quiz
  id="03-v8-internals-choice-3"
  lessonSlug="03-v8-internals"
  lang="ru"
  question="Почему V8 поставляет четыре уровня JIT (Ignition, Sparkplug, Maglev, TurboFan), а не один хороший оптимизатор?"
  choices={[
    { label: "JavaScript динамически типизирован, поэтому знания типов на этапе компиляции неполны; V8 должен наблюдать типы в рантайме, прежде чем специализировать — а прогрев сам должен быть дешёвым, поэтому Ignition и Sparkplug дают быстрый старт, а Maglev и TurboFan окупают стоимость компиляции только на горячем и стабильном коде.", correct: true },
    { label: "Каждый уровень нацелен на свой браузер или версию ECMAScript.", misconception: "Все уровни поставляются вместе в одном движке и исполняют один ECMAScript. Они различаются компромиссом стоимость-компиляции-против-скорости-исполнения, а не языком или поддержкой браузеров." },
    { label: "Больше уровней — больше параллелизма, поэтому JS работает на четырёх ядрах сразу.", misconception: "Исполнение JS остаётся однопоточным; уровни — это слои оптимизации, а не параллельные воркеры. Конкурентная компиляция строит следующий уровень вне основного потока, но исполнение идёт на одном уровне за раз." },
    { label: "TurboFan слишком багованный, чтобы работать в одиночку, поэтому нижние уровни — запасные.", misconception: "Нижние уровни существуют ради задержки старта и сбора фидбэка по типам, а не из-за ненадёжности TurboFan. TurboFan нужен FeedbackVector, который заполняет Ignition, чтобы хорошо специализировать." },
  ]}
/>

<Quiz
  id="03-v8-internals-choice-4"
  lessonSlug="03-v8-internals"
  lang="ru"
  question="Критичная по производительности функция показывает в --trace-deopt повторяющиеся пары 'optimized' затем 'deoptimized (not a Smi)' в пределах одной секунды. Что происходит и каков самый эффективный фикс?"
  choices={[
    { label: "Это deopt-loop: TurboFan продолжает спекулировать, что значение — Smi, редкое большое значение или NaN срабатывает guard на каждом вызове, V8 делает deopt и заново продвигает (~100 мс компиляции каждый раз). Фикс: направить патологические значения на медленный путь до горячего цикла, чтобы быстрый путь оставался стабильным по Smi.", correct: true },
    { label: "Нормальное поведение — любая оптимизированная функция делает несколько deopt на прогреве.", misconception: "Разовый deopt при прогреве нормален; повторяющаяся пара deopt/re-opt на одной строке на каждом вызове — это death-spiral, обходящийся дороже, чем не оптимизировать вовсе." },
    { label: "Функция сломана и больше никогда не оптимизируется; перепишите её на WebAssembly.", misconception: "Deopt понижает уровень, а не отключает оптимизацию навсегда — функция вернётся на уровень, как только фидбэк стабилизируется. Фикс — стабилизировать тип входа, а не переписывать." },
    { label: "Поднимите порог продвижения в TurboFan, чтобы он перестал перекомпилировать.", misconception: "Отсрочка продвижения лишь откладывает цикл; guard всё равно сработает, когда появится значение. Надёжный фикс убирает патологическое значение с горячего пути, а не из оптимизатора." },
  ]}
/>

<Quiz
  id="03-v8-internals-choice-5"
  lessonSlug="03-v8-internals"
  lang="ru"
  question="Почему конкурентная маркировка в Orinoco нуждается в write barrier и что ломается без него?"
  choices={[
    { label: "Пока фоновый маркер обходит кучу, основной поток JS может перезаписать ссылку на ещё белый (непосещённый) объект. Барьер snapshot-at-the-beginning красит перезаписанный объект в серый, чтобы маркер всё равно его посетил; без него живой объект не помечается и ошибочно собирается — тихое повреждение кучи.", correct: true },
    { label: "Барьер нужен, чтобы два GC-воркера не посетили один объект дважды.", misconception: "Двойные посещения обрабатываются work-stealing и серой/чёрной раскраской. Write barrier — про то, как мутатор (JS) меняет ссылки во время маркировки, а не про координацию потоков." },
    { label: "Он ускоряет маркировку, распараллеливая обход кучи.", misconception: "Барьер добавляет ~3–5 циклов на запись ссылки — это механизм корректности, а не ускорение. Параллелизм даёт воркеры, а не барьер." },
    { label: "Он отслеживает указатели old-gen → young-gen для scavenger.", misconception: "Это remembered set / card table — отдельный механизм. Здесь write barrier сохраняет инвариант tri-color во время конкурентной маркировки old gen." },
  ]}
/>

<Quiz
  id="03-v8-internals-choice-6"
  lessonSlug="03-v8-internals"
  lang="ru"
  question="Анимационный цикл с тяжёлой численностью обрабатывает 10M сущностей за кадр и продолжает уходить с 60fps при прогреве, с deopt на арифметике и растущей частотой minor GC. Какая раскладка данных чинит и IC, и GC сразу?"
  choices={[
    { label: "Параллельные TypedArray (Float32Array для x, y, vx, vy), выделенные заранее. Доступ к TypedArray полностью обходит слой IC по hidden class, а буферы выделяются один раз, так что нет покадрового мусора для scavenger.", correct: true },
    { label: "Массив обычных объектов, форма которых меняется на каждую сущность ради гибкости.", misconception: "Меняющиеся формы гонят IC по свойству в megamorphic (в 10–50× медленнее) и выделяют по объекту на сущность, повышая частоту young-gen GC. Это усугубляет обе проблемы из вопроса." },
    { label: "Map(), ключуемый по id сущности, мутируемый на месте каждый кадр.", misconception: "Map несёт накладные расходы на хеширование и общую итерацию и не обходит IC при доступе к значению. Для фиксированных числовых полей в горячем цикле он медленнее TypedArray и всё равно выделяет записи." },
    { label: "Оставить объекты, но увеличить размер young generation, чтобы minor GC запускался реже.", misconception: "Больший young gen откладывает scavenge, но ничего не делает с megamorphic IC или deopt при переполнении Smi. Это лечит симптом; TypedArray убирает обе первопричины." },
  ]}
/>

<Recap lang="ru">
Сквозная линия юнита — одна цепочка: порядок добавления свойств задаёт hidden class, hidden class задаёт состояние IC, состояние IC (вместе со стабильностью типов) решает, поднимется ли функция до TurboFan или уйдёт в deopt-loop, а барьеры Orinoco держат кучу корректной, пока всё это бурлит. Любая прод-регрессия из юнита — megamorphic-путь рендера, deopt-loop при переполнении Smi, утечка замыканий в old gen — сводится к одному рычагу: стабилизируйте форму и числовой тип выше по потоку, в модели данных, прежде чем браться за любой флаг.
</Recap>
