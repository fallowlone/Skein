---
concepts:
- hidden-class-breakage
- monomorphic-to-megamorphic
- deopt-trigger
- dictionary-mode
estMin: 14
lang: ru
level: senior
order: 9
prereqs: []
slug: quiz-code
sources:
- https://mathiasbynens.be/notes/shapes-ics
- https://github.com/thlorenz/v8-perf/blob/master/compiler.md
status: ready
summary: "Читайте реальные JS-сниппеты и строку deopt-трассы, предсказывайте поведение V8 — поломку hidden class, дрейф monomorphic→megamorphic, триггеры deopt — и выбирайте самый эффективный фикс."
title: 'Внутренности V8: чтение кода и трасс'
track: browser
unit: 03-v8-internals
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Баги производительности V8 прячутся в обычном на вид коде. Прочитайте каждый сниппет и строку трассы, предскажите, что V8 делает с hidden class, IC или оптимизированным уровнем — затем выберите фикс, который сеньор делает первым, до того как трогать флаг.
</Hook>

<Goal lang="ru">
Отработайте цикл, который вы запускаете в каждом инциденте V8: прочитать горячий путь, предсказать, откуда берётся нестабильность формы или типа, и взять фикс выше по потоку, который держит сайт вызова monomorphic, а значения — стабильными по типу.
</Goal>

## Сниппет 1 — позднее свойство

```js
class Point {
  constructor(x, y) {
    this.x = x;
    this.y = y;
  }
}

function build(coords) {
  const out = [];
  for (const c of coords) {
    const p = new Point(c.x, c.y);
    if (c.label) p.label = c.label;   // только иногда
    out.push(p);
  }
  return out;
}

// позже, в горячем цикле:
function sumX(points) {
  let s = 0;
  for (const p of points) s += p.x;   // горячий IC-сайт
  return s;
}
```

<Quiz
  id="03-v8-internals-code-1"
  lessonSlug="03-v8-internals"
  lang="ru"
  question="Некоторым точкам .label добавляется после конструктора, некоторым — нет. Что происходит на сайте доступа p.x в sumX и каков фикс?"
  choices={[
    { label: "Условный переход p.label делит массив на два hidden class (Point и Point+label), поэтому сайт p.x становится polymorphic. Фикс: всегда объявлять label в конструкторе (this.label = c.label ?? null), чтобы у всех точек был один hidden class.", correct: true },
    { label: "Ничего — читается .x, а не .label, так что лишнее свойство к этому сайту не относится.", misconception: "IC привязан ко всему hidden class, а не к читаемому свойству. Добавление .label переводит объект в другой класс, так что сайт p.x теперь видит два класса и становится polymorphic." },
    { label: "Он сразу уходит в megamorphic, потому что массив смешанный.", misconception: "В игре только два hidden class (с label и без), так что сайт polymorphic, а не megamorphic. Megamorphic требует 5+ классов — но фикс тот же: свести форму к одной." },
    { label: "Поднимите GOGC, чтобы точки собрались до запуска цикла.", misconception: "В V8 нет GOGC, и GC тут ни при чём — проблема в расхождении hidden class на IC-сайте, чинится объявлением label заранее." },
  ]}
/>

## Сниппет 2 — ловушка dictionary mode

```js
function makeConfig(overrides) {
  const cfg = { host: 'localhost', port: 8080, retries: 3 };
  for (const key of Object.keys(overrides)) {
    cfg[key] = overrides[key];
  }
  delete cfg.retries;          // "почистить" неиспользуемый дефолт
  return cfg;
}

function readPort(cfg) {
  return cfg.port;             // горячий путь, вызывается на запрос
}
```

<Quiz
  id="03-v8-internals-code-2"
  lessonSlug="03-v8-internals"
  lang="ru"
  question="readPort находится на горячем пути запроса. Что делает с ним delete в makeConfig и каков фикс?"
  choices={[
    { label: "delete переводит cfg в dictionary mode — хешмап-раскладку, где каждое чтение свойства, включая cfg.port, занимает ~50–100 циклов через общий поиск вместо чтения по фиксированному смещению. Фикс: присвоить cfg.retries = undefined (или не задавать его) вместо delete.", correct: true },
    { label: "delete просто освобождает немного памяти и не влияет на readPort.", misconception: "Крошечная экономия памяти ничтожна по сравнению с ценой: delete навсегда выводит объект из режима fast-property, так что любое последующее чтение любого свойства идёт по медленному общему пути." },
    { label: "delete ускоряет readPort, потому что у cfg теперь меньше свойств для сканирования.", misconception: "Объекты в режиме fast-property читаются по предвычисленному смещению независимо от числа свойств; сканирования нет. delete заменяет это хешмап-поиском, который строго медленнее." },
    { label: "Проблема только в динамическом цикле Object.keys; delete — нормально.", misconception: "Цикл присваивания по ключам может перекручивать hidden class, но delete — решающий дефект: он опрокидывает cfg в dictionary mode, из которого V8 не восстановится без нового объекта." },
  ]}
/>

## Сниппет 3 — переполнение Smi

```js
function checksum(ids) {
  let acc = 0;
  for (let i = 0; i < ids.length; i++) {
    acc = (acc * 31 + ids[i]);     // быстро растёт на больших массивах
  }
  return acc;
}
```

<Quiz
  id="03-v8-internals-code-3"
  lessonSlug="03-v8-internals"
  lang="ru"
  question="checksum оптимизирован TurboFan и быстр на малых входах, но на больших массивах внезапно делает deopt и замедляется. В чём причина на уровне V8 и каков фикс?"
  choices={[
    { label: "acc начинается как Smi, и TurboFan специализирует арифметику на Smi. Когда acc * 31 + ids[i] перерастает диапазон Smi (±2³¹), значение становится HeapNumber, guard на Smi срабатывает и V8 делает deopt. Фикс: держать аккумулятор в диапазоне 32-битного целого через acc = (acc * 31 + ids[i]) | 0.", correct: true },
    { label: "Проверка границ по ids.length в цикле for и есть триггер deopt.", misconception: "Индексированный цикл в пределах границ — именно тот паттерн, который TurboFan хорошо оптимизирует. Причина deopt здесь — переход acc из Smi в HeapNumber, а не проверка границ." },
    { label: "Умножение на 31 с самого начала навязывает плавающую точку, поэтому он никогда не был оптимизирован.", misconception: "Целочисленное умножение остаётся Smi, пока результат влезает в ±2³¹; V8 его оптимизирует. Deopt случается только когда acc выходит из диапазона и боксируется в HeapNumber." },
    { label: "Большие массивы вызывают паузу GC посреди цикла; это и есть замедление.", misconception: "Замедление локализуется в этой функции после события deopt, а не глобальная пауза. Триггер — переход числового типа acc, который предотвращает приведение | 0." },
  ]}
/>

## Сниппет 4 — трасса deopt

```
[marking 0x... <JSFunction processItem> for optimization using TurboFan]
[deoptimizing (DEOPT eager): begin <JSFunction processItem> (opt #42) @14, ;;; deoptimize at <main.js:42:18>, wrong map]
[marking 0x... <JSFunction processItem> for optimization using TurboFan]
[deoptimizing (DEOPT eager): begin <JSFunction processItem> (opt #43) @14, ;;; deoptimize at <main.js:42:18>, wrong map]
[marking 0x... <JSFunction processItem> for optimization using TurboFan]
[deoptimizing (DEOPT eager): begin <JSFunction processItem> (opt #44) @14, ;;; deoptimize at <main.js:42:18>, wrong map]
```

<Quiz
  id="03-v8-internals-code-4"
  lessonSlug="03-v8-internals"
  lang="ru"
  question="Читая этот вывод --trace-deopt, какое утверждение верно?"
  choices={[
    { label: "Это deopt-loop с причиной 'wrong map' на строке 42:18 — TurboFan продолжает предполагать один hidden class, сайт вызова продолжает видеть другой, поэтому он переоптимизируется и делает deopt каждый цикл (~100 мс компиляции каждый). Фикс выше по потоку: свести все объекты, доходящие до строки 42:18, к одному hidden class.", correct: true },
    { label: "'wrong map' означает ошибку source map в сборке; почините sourcemaps.", misconception: "'map' — это внутреннее имя V8 для hidden class, а не отладочный source map. 'wrong map' значит, что hidden class объекта не совпал с тем, на который спекулировал TurboFan." },
    { label: "Повторяющиеся строки 'marking for optimization' означают, что V8 здорово держит функцию горячей.", misconception: "Повторная разметка сразу после каждого deopt на той же строке — сигнатура death-spiral, а не здоровья. Стабильная оптимизированная функция размечается один раз и остаётся оптимизированной." },
    { label: "Три deopt в сумме — нормальный шум прогрева; игнорируйте.", misconception: "Эти три — последовательные циклы opt #42/#43/#44 на одной строке — цикл, а не прогрев. Deopt при прогреве прекращаются, когда фидбэк стабилизируется; здесь он никогда не стабилизируется, потому что форма продолжает меняться." },
  ]}
/>

<Recap lang="ru">
Любой инцидент V8 читается одинаково в коде и трассах: условно добавленное свойство делит один массив на два hidden class и толкает горячий сайт в polymorphic; delete опрокидывает объект в постоянный dictionary mode; аккумулятор, переросший диапазон Smi, боксируется в HeapNumber и делает deopt цикла TurboFan; а повторяющийся 'wrong map' deopt на одной позиции — сигнатура deopt-loop. Диагностируйте по трассе, затем чините выше по потоку в модели данных — сведите форму к одной, уберите delete, ограничьте числовой диапазон — прежде чем браться за флаг.
</Recap>
