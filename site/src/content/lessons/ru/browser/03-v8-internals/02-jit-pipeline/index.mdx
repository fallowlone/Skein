---
concepts:
- Ignition
- Sparkplug
- Maglev
- TurboFan
- FeedbackVector
- profile-guided tiering
- concurrent compilation
- TypedArrays
deepensInto:
- 06-turbofan-and-deopt
estMin: 14
lang: ru
lessonType: topic
level: middle
order: 2
prereqs:
- 01-what-v8-is
slug: 02-jit-pipeline
sources:
- https://v8.dev/blog/maglev
- https://v8.dev/blog/sparkplug
- https://github.com/thlorenz/v8-perf/blob/master/compiler.md
spiral:
- statefulness
status: ready
summary: Как Ignition, Sparkplug, Maglev и TurboFan работают вместе — затраты на компиляцию,
  скорость выполнения, FeedbackVector и рычаги управления для удержания кода на быстром
  уровне.
title: Четырёхуровневый JIT-конвейер V8 и профилированная тиеризация
track: browser
unit: 03-v8-internals
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import NumbersCard from "~/components/layout/NumbersCard.astro";
import TraceScenario from "~/components/pedagogy/TraceScenario.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
Страница ощущается отзывчивой через 200мс после загрузки. Через несколько секунд становится ещё быстрее. Третье ускорение появляется после тысяч взаимодействий. Это не магия — V8 перемещает горячий код выше по уровням, оставляя холодный дешёвым.
</Hook>

<Crux>Четыре JIT-уровня покрывают кривую стоимость/скорость; тиеризация продвигает функцию только когда FeedbackVector оправдывает.</Crux>

<Explanation>

## Четырёхуровневый конвейер

Почему одна и та же функция ощущается медленной при старте и быстрой через несколько секунд? Потому что V8 не ставит всё на один компилятор — код поднимается по уровням только когда профиль оправдывает стоимость.

V8 имеет четыре режима выполнения, упорядоченных по соотношению стоимость компиляции / скорость выполнения.

**Ignition** — интерпретатор байткода. Парсер создаёт AST; Ignition преобразует его в компактный регистровый байткод и выполняет на виртуальной машине. При интерпретации Ignition записывает type feedback в FeedbackVector функции — слоты для каждой «интересной» точки (загрузка свойства, вызов функции, арифметика, сравнение). Дёшево входить (без затрат на компиляцию), ~10× медленнее на инструкцию по сравнению с оптимизированным нативным кодом.

**Sparkplug** — базовый JIT, добавлен в 2021 году. Sparkplug генерирует неспециализированный машинный код за один линейный проход по байткоду — без SSA, без реальной оптимизации, просто точный перевод каждого байткода в небольшой блок машинных инструкций плюс хвостовой вызов. Стоимость компиляции практически нулевая (~1 мс/кБ байткода); ускорение по сравнению с Ignition — 1.5–2×.

**Maglev** — оптимизирующий компилятор среднего уровня, добавлен в 2023 году. Maglev использует наблюдаемые типы из FeedbackVector и SSA-оптимизации для генерации специализированного машинного кода, но пропускает тяжёлые проходы TurboFan. ~10× медленнее компиляция, чем Sparkplug, ~10× быстрее, чем TurboFan; код примерно на полпути между ними по скорости выполнения.

**TurboFan** — тяжёлый оптимизирующий компилятор. Агрессивный инлайнинг, escape-анализ, анализ диапазонов, специализация на основе hidden class, спекулятивные оптимизации с возможностью deopt. Высокая стоимость компиляции (десятки-сотни мс для сложных функций); выдаёт самый быстрый код.

<FlowDiagram
  label="Источник становится AST, затем байткодом Ignition; горячие функции профилируются и продвигаются через Sparkplug, Maglev и TurboFan; когда спекуляция TurboFan нарушается, V8 деоптимизирует функцию обратно в байткод Ignition."
  perRow={6}
  hue="var(--d-frontend)"
  nodes={[
    { id: "src", label: "JS-источник", sub: ".js текст" },
    { id: "ast", label: "AST", sub: "вывод парсера" },
    { id: "ignition", label: "Ignition", sub: "байткод" },
    { id: "sparkplug", label: "Sparkplug", sub: "базовый JIT" },
    { id: "maglev", label: "Maglev", sub: "средний уровень" },
    { id: "turbofan", label: "TurboFan", sub: "оптимизир." },
  ]}
  edges={[
    { from: "src", to: "ast" },
    { from: "ast", to: "ignition" },
    { from: "ignition", to: "sparkplug", label: "горячо" },
    { from: "sparkplug", to: "maglev", label: "горячее" },
    { from: "maglev", to: "turbofan", label: "самое горячее" },
    { from: "turbofan", to: "ignition", label: "deopt" },
  ]}
  caption="Функции поднимаются по уровням по мере нагрева (профилированная тиеризация). Ребро deopt указывает назад: когда предположение TurboFan нарушается во время выполнения, V8 выбрасывает оптимизированный код и возобновляет работу в байткоде Ignition."
/>

<NumbersCard
  id="card-jit-numbers"
  title="Стоимость компиляции и выполнения на уровнях JIT"
  data-lesson-visual
  stats={[
    { label: "Скорость компиляции Sparkplug", value: "~1 мс / кБ байткода" },
    { label: "Ускорение Sparkplug vs Ignition", value: "~1.5–2×" },
    { label: "Время компиляции Maglev", value: "~10 мс / функция" },
    { label: "Качество кода Maglev vs TurboFan", value: "~50–70%" },
    { label: "Время компиляции TurboFan", value: "~100 мс / функция" },
    { label: "Порог продвижения Sparkplug", value: "~100 вызовов" },
    { label: "Порог продвижения Maglev", value: "несколько тысяч вызовов" },
    { label: "Порог продвижения TurboFan", value: "десятки тысяч вызовов" },
  ]}
/>

## Профилированная тиеризация

Функции попадают в Ignition. При превышении порогов счётчика выполнения (для каждой функции, динамически) V8 продвигает на следующий уровень. FeedbackVector отслеживает не только типы, но и частоты веток, поэтому TurboFan может расположить горячий путь с оптимальным предсказанием ветвления. Продвижение происходит вне потока (concurrent compilation) — JS основной поток продолжает работать на текущем уровне, пока рабочий поток компилирует следующий.

Функции могут пропускать уровни: очень горячая функция, увиденная при старте, может прыгнуть с Ignition прямо в Maglev. Функции с нестабильным feedback (частые переходы IC) откладываются или пропускаются — TurboFan на нестабильной функции произведёт код, который немедленно deopt'нется, потратив 100 мс работы по компиляции впустую.

## Операционные рычаги

Восемь шаблонов, удерживающих код на быстром уровне V8:

1. **Шаблон конструктора** — объявляйте все свойства объекта в одном месте, чтобы hidden classes были стабильными.
2. **Избегайте `delete`** — используйте присвоение `null` или `undefined`; `delete` принудительно переводит в dictionary mode.
3. **Держите функции monomorphic** — не передавайте разные формы на одну точку вызова.
4. **Избегайте `arguments`** в старом коде; используйте rest-параметры.
5. **Предварительно выделяйте массивы** с известной ёмкостью, чтобы избежать изменения размера.
6. **Переиспользуйте пулы объектов** для короткоживущих нагрузок (canvas, анимация) вместо создания новых объектов на каждый кадр.
7. **Избегайте огромных объектных литералов** в горячих путях — они быстро становятся megamorphic.
8. **Для горячих числовых циклов** используйте TypedArrays — V8 имеет высокоточные пути для `Uint32Array` и других, которые полностью обходят IC-слой.

Все восемь правил вместе держат FeedbackVector чистым: стабильные формы означают отсутствие IC-переходов, а без переходов TurboFan компилирует один раз и остаётся скомпилированным. Нарушь любое из них в горячем пути — особенно шаблон конструктора или `delete` — и сведёшь на нет остальные семь.

<TraceScenario
  id="mid-trace-jit"
  pieceSlug="jit-pipeline"
  lang="ru"
  scenario="React-приложение падает до 20fps после добавления нового пропа к компоненту. Найдите JS-причину на уровне производительности."
  steps={[
    { prompt: "Шаг 1: 20fps означает, что кадр занимает >50мс при норме <16мс. Какие подсистемы могут поедать бюджет?", reveal: "Возможные виновники: больше JS-работы на рендер (время скриптинга), больше layout/paint, GC-пауза, переходы hidden class в пути рендера. Начните с панели Performance — разбейте кадр на Scripting / Rendering / Painting / GC." },
    { prompt: "Шаг 2: Performance показывает 40мс Scripting на кадр вместо прежних 8мс. Новый проп задействован. Какова форма проблемы на уровне V8?", reveal: "Добавление пропа к React-компоненту безвредно на уровне React — но если проп динамический (иногда строка, иногда число, иногда undefined), объект props компонента теперь имеет разные hidden classes на каждый рендер. Любой IC компонента, завязанный на props.X, переходит в polymorphic или megamorphic." },
    { prompt: "Шаг 3: как подтвердить нестабильность hidden class?", reveal: "Запустите с --trace-opt --trace-deopt. Ищите deopt-сообщения о функции рендера компонента. Или используйте %GetOptimizationStatus на функции рендера — если она перескакивает между 'optimized' и 'not optimized' на разных рендерах, деградация IC нарушает оптимизацию." },
    { prompt: "Шаг 4: структурное исправление?", reveal: "Сделайте тип пропа консистентным. Если он может быть string-or-undefined, используйте значение по умолчанию, чтобы он всегда был определён. Если number-or-string — выберите одно каноническое представление. Документация React намекает на это в рекомендации 'избегайте изменения идентичности объекта' — глубинная причина — стабильность hidden class для V8." },
    { prompt: "Шаг 5: предотвратить регрессию?", reveal: "TypeScript strict mode ловит большинство случаев (нет implicit any, нет | undefined без явного указания). Добавьте тест с бюджетом производительности: рендерите компонент N раз, утверждайте, что общее время остаётся ниже порога. Запускайте на каждом PR, касающемся компонента." },
  ]}
/>

<DragOrder
  id="mid-drag-jit"
  pieceSlug="jit-pipeline"
  lang="ru"
  prompt="Упорядочите шаги доступа к свойству в monomorphic IC, скомпилированном TurboFan:"
  items={[
    "TurboFan-скомпилированная функция получает указатель на объект в регистре",
    "Первая инструкция: загрузить указатель hidden class из заголовка объекта",
    "Сравнить загруженный hidden class с ожидаемым из времени компиляции",
    "Если равно: прочитать свойство по заранее вычисленному смещению (1 инструкция MOV)",
    "Если не равно: deopt — выбросить скомпилированный код, откатиться на нижний уровень",
    "Продолжить выполнение на нижнем уровне; IC записывает новый hidden class и может сменить состояние",
    "Функция может быть повторно оптимизирована позже с обновлёнными данными IC",
  ]}
  correctOrder={["0", "1", "2", "3", "4", "5", "6"]}
/>

<Quiz
  id="mid-quiz-jit-1"
  pieceSlug="jit-pipeline"
  lang="ru"
  question="Почему V8 имеет ЧЕТЫРЕ уровня компиляции вместо двух?"
  choices={[
    { label: "Историческая случайность, нет реальной причины", misconception: "Каждый уровень был добавлен намеренно для заполнения определённой ниши в компромиссе стоимость компиляции / скорость выполнения." },
    { label: "Каждый уровень покрывает разную точку кривой время компиляции / скорость выполнения — Sparkplug для быстрого прогрева, Maglev для среднего уровня кода, не требующего TurboFan", correct: true },
    { label: "Разные уровни для разных браузеров", misconception: "V8 один движок; все уровни поставляются вместе." },
    { label: "Маркетинг", misconception: "Каждый уровень имеет измеримое, задокументированное влияние на реальные нагрузки." },
  ]}
/>

<Quiz
  id="mid-quiz-jit-2"
  pieceSlug="jit-pipeline"
  lang="ru"
  question="Функция внезапно замедляется с 50мкс до 2мс после 'небольшого' рефакторинга. Куда смотреть в первую очередь?"
  choices={[
    { label: "Паузы сборщика мусора", misconception: "GC — возможная причина, но замедление одной функции чаще происходит от deopt или деградации IC." },
    { label: "Была ли функция deopt'нута (смотрите --trace-deopt логи или 'recompile' события в DevTools Performance) — и какая точка вызова стала mega/polymorphic", correct: true },
    { label: "Задержка сети", misconception: "Если функция — чистая вычислительная, сеть не при чём." },
    { label: "Разница V8 на Mac vs Linux", misconception: "Один и тот же V8 поставляется на все ОС; различия производительности в пределах ~5%." },
  ]}
/>

<Inset kind="why" lang="ru">
Почему не компилировать всё через TurboFan с самого начала? Три причины. Первая: большинство функций выполняется один раз или никогда — стоимость компиляции TurboFan была бы чистыми потерями. Вторая: TurboFan нужен type feedback от Ignition для хороших спекуляций; без прогрева нет информации для специализации. Третья: задержка компиляции напрямую влияет на воспринимаемое время загрузки. Страница, загружающаяся 2 секунды, потому что каждая onload-функция была TurboFan-скомпилирована, ощущается сломанной. Многоуровневый дизайн ставит на первое место дешёвый Ignition, добираясь до дорогого TurboFan только для малой части кода, которая это оправдывает.
</Inset>

</Explanation>

<KeyTakeaway>Четыре уровня позволяют V8 платить стоимость компиляции только за код, который это заслуживает. Профилированная тиеризация плюс данные FeedbackVector позволяют TurboFan специализироваться на наблюдаемых типах — и пропускать функции, типы которых слишком нестабильны для безопасной оптимизации.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="jit-pipeline-retrieval"
  lang="ru"
  questions={[
    { q: "Почему V8 не компилирует каждую функцию через TurboFan немедленно?", a: "Три причины: (1) большинство функций выполняется один раз или никогда — стоимость TurboFan была бы потерями; (2) TurboFan нужен type feedback от прогрева Ignition для генерации хорошего спекулятивного кода — без него результат был бы таким же медленным, как Sparkplug, при в 1000 раз большей стоимости; (3) задержка компиляции бьёт по воспринимаемому времени загрузки. Многоуровневый дизайн ставит на первое место дешёвый Ignition, добирается до дорогого TurboFan только для горячих и стабильных функций." },
    { q: "Что такое FeedbackVector и кто его читает?", a: "Массив фиксированного размера в куче V8, по одному слоту на IC-сайт (загрузка свойства, вызов, бинарная операция). Ignition записывает наблюдаемые hidden classes и цели вызовов при интерпретации. Sparkplug читает минимальный feedback. Maglev использует более богатые данные (цепочки классов, частоты вызовов). TurboFan читает всё плюс выводит количество итераций цикла и вероятности ветвлений. Megamorphic-слот блокирует оптимизацию этой точки TurboFan." },
    { q: "Каковы две главные причины существования Maglev рядом с TurboFan?", a: "Maglev покрывает 'средне горячий' уровень — функции, вызываемые тысячи раз, но не десятки тысяч — где стоимость компиляции TurboFan ~100мс слишком дорога, а неспециализированный код Sparkplug слишком медленный. Maglev компилирует за ~10мс и производит код с ~50-70% качества TurboFan. Это даёт измеримое ускорение без задержки компиляции TurboFan, сохраняя бюджеты времени кадра во время фазы прогрева." },
  ]}
/>

<Recap lang="ru">
Четыре уровня V8 охватывают всю ось компромисса стоимость компиляции / скорость выполнения: Ignition (бесплатная компиляция, код в 10× медленнее), Sparkplug (~1мс компиляция, в 1.5–2× быстрее), Maglev (~10мс компиляция, 50–70% скорости TurboFan), TurboFan (~100мс компиляция, максимальная скорость). Функции поднимаются по уровням только когда они достаточно горячи и их FeedbackVector достаточно стабилен, чтобы оправдать стоимость компиляции. FeedbackVector — ключевая структура данных: по одному слоту на точку вызова, отслеживающему наблюдаемые hidden classes и частоты вызовов. Конкурентная компиляция держит основной поток работающим на текущем уровне, пока рабочий поток строит следующий. Операционные рычаги — стабильные конструкторы, без `delete`, monomorphic точки вызова, TypedArrays для числовых циклов — удерживают функции на быстром уровне на протяжении всего срока их жизни. Теперь, когда встретишь функцию, быструю вчера и медленную после рефакторинга, первый вопрос: изменились ли формы объектов или типы аргументов, которые она получает? Именно там FeedbackVector хранит ответ.
</Recap>
