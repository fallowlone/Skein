---
concepts:
- V8
- JIT compilation
- compilation tiers
- Ignition
- TurboFan
- monomorphic
- hidden class
deepensInto:
- 02-jit-pipeline
- 04-inline-caches
estMin: 10
lang: ru
lessonType: topic
level: junior
order: 1
prereqs: []
slug: 01-what-v8-is
sources:
- https://v8.dev/blog/maglev
- https://v8.dev/blog/sparkplug
- https://en.wikipedia.org/wiki/V8_(JavaScript_engine)
spiral:
- statefulness
status: ready
summary: V8 преобразует JS-код в машинный через четыре уровня компиляции — одна и
  та же функция может работать в 100 раз быстрее или медленнее в зависимости от того,
  удаётся ли V8 удержать её на быстром уровне.
title: Что такое V8 и почему производительность различается в 100 раз
track: browser
unit: 03-v8-internals
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import MetaphorComplete from "~/components/pedagogy/MetaphorComplete.astro";
import NumbersCard from "~/components/layout/NumbersCard.astro";
import PersonaTag from "~/components/pedagogy/PersonaTag.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
React рендерит 1000 строк. При стабильных формах объектов — наносекундные поиски и 60fps. При смешанных формах — в 10–50 раз медленнее, тот же код, та же функция, разная скорость из-за того, что V8 наблюдал.
</Hook>

<Crux>V8 имеет четыре JIT-уровня; одна функция работает в 100 раз быстрее или медленнее в зависимости от стабильности форм объектов.</Crux>

<Explanation>

## Что такое V8

V8 — это движок JavaScript внутри Chrome, Edge и Node.js. Он берёт исходный JS-код и преобразует его в машинный код, который выполняет CPU. Одна и та же JS-функция может работать в 100 раз быстрее или медленнее в зависимости от того, может ли V8 её оптимизировать. Несколько привычек — стабильные формы объектов, мономорфные точки вызова — решают разницу между анимацией на 60fps и тормозящей страницей.

<FlowDiagram
  label="V8 преобразует JS-код в машинный: парсер строит AST, Ignition интерпретирует байткод, а горячие функции поднимаются до оптимизированного кода TurboFan, тогда как сборщик мусора освобождает память параллельно всему конвейеру."
  perRow={5}
  hue="var(--d-frontend)"
  nodes={[
    { id: "src", label: "JS-источник", sub: ".js текст" },
    { id: "parser", label: "Парсер", sub: "→ AST" },
    { id: "ignition", label: "Ignition", sub: "интерп. байткода" },
    { id: "tiers", label: "Sparkplug / Maglev", sub: "тёплые уровни" },
    { id: "turbofan", label: "TurboFan", sub: "машинный код" },
    { id: "gc", label: "Сборщик мусора", sub: "чистит кучу" },
  ]}
  edges={[
    { from: "src", to: "parser" },
    { from: "parser", to: "ignition" },
    { from: "ignition", to: "tiers", label: "горячо" },
    { from: "tiers", to: "turbofan", label: "горячее" },
  ]}
  caption="Источник течёт слева направо: парсинг → интерпретация → оптимизация. Сборщик мусора работает параллельно всему движку, освобождая мёртвые объекты во время выполнения кода."
/>

## Метафора кухни

V8 — это кухня с четырьмя поварами:

- **Стажёр (Ignition)** читает каждый рецепт медленно, но записывает, что заказывали.
- **Линейный повар (Sparkplug)** быстро выдаёт универсальные блюда.
- **Су-шеф (Maglev)** делает специализированные версии популярных блюд.
- **Шеф-повар (TurboFan)** вручную создаёт идеальное блюдо для самого популярного заказа — но только если заказы остаются стабильными. Измените заказ на полуслове — и TurboFan выбросит блюдо.

<NumbersCard
  id="card-v8-overview"
  title="Уровни V8 в кратком обзоре"
  data-lesson-visual
  stats={[
    { label: "Уровни", value: "4 (Ignition / Sparkplug / Maglev / TurboFan)" },
    { label: "Ignition vs TurboFan скорость", value: "~100× медленнее" },
    { label: "Замедление при megamorphic", value: "10–50× vs monomorphic" },
    { label: "IC monomorphic доступ", value: "5–10 тактов CPU" },
  ]}
/>

## Конкретный разбор

<PersonaTag id="bea" lang="ru" /> запускает JS в браузере. Ignition сначала интерпретирует, записывая типы. После тысяч вызовов V8 продвигает код в TurboFan с предположениями «аргумент всегда число, объект всегда имеет .x». Если <PersonaTag id="sven" lang="ru" /> однажды пришлёт строку, предположения нарушатся — deopt, откат. «Быстрая» функция снова медленная, пока V8 не переобучится.

React рендерит 1000 строк. Стабильные ключи = monomorphic IC, наносекундные поиски. Смешанные ключи = polymorphic, в 2–3 раза медленнее. 10+ форм = megamorphic, в 10–50 раз медленнее. Тот же код, кардинально разная скорость.

<DragOrder
  id="jr-drag-1"
  pieceSlug="what-v8-is"
  lang="ru"
  prompt="Упорядочите путь горячей функции через уровни V8:"
  items={[
    "JS-источник разбирается в AST",
    "AST преобразуется в байткод компилятором Ignition",
    "Ignition интерпретирует байткод и записывает type feedback на каждую точку вызова",
    "После тысяч вызовов Sparkplug генерирует базовый машинный код без специализации",
    "При дальнейшем нагреве Maglev компилирует оптимизированную версию среднего уровня",
    "Для самых горячих путей TurboFan выполняет агрессивную спекулятивную оптимизацию",
    "При нарушении предположения V8 выполняет deopt и откатывается на нижний уровень",
  ]}
  correctOrder={["0", "1", "2", "3", "4", "5", "6"]}
/>

<Quiz
  id="jr-quiz-1"
  pieceSlug="what-v8-is"
  lang="ru"
  question="Почему V8 использует несколько уровней компиляции вместо одного?"
  choices={[
    { label: "Для поддержки большего числа браузеров", misconception: "V8 поставляется в Chrome и Node.js — уровни касаются баланса производительности, а не переносимости." },
    { label: "Разные уровни обменивают время компиляции на скорость выполнения: Ignition стартует мгновенно, но работает медленно; TurboFan компилирует долго, но выполняется молниеносно", correct: true },
    { label: "Уровни для разных версий JS", misconception: "Все уровни выполняют одинаковый ECMAScript; они различаются стратегией оптимизации." },
    { label: "Только один уровень настоящий, остальные — декорация", misconception: "Все четыре уровня — реальные компиляторы, работающие в текущем Chrome." },
  ]}
/>

<Quiz
  id="jr-quiz-2"
  pieceSlug="what-v8-is"
  lang="ru"
  question="Что делает один фрагмент JS в 100 раз быстрее другого, делающего «то же самое»?"
  choices={[
    { label: "Больше ядер CPU", misconception: "JS в V8 однопоточный; ядра не влияют на производительность отдельных строк." },
    { label: "Разные парсеры", misconception: "Парсинг происходит один раз и быстро в любом случае." },
    { label: "Удаётся ли V8 удержать функцию на быстром уровне (стабильные типы, monomorphic точки вызова) или приходится откатиться на общий интерпретатор (загрязнённые IC, deopt)", correct: true },
    { label: "Расширение файла .mjs или .js", misconception: "Тип модуля не влияет на производительность V8 для отдельных функций." },
  ]}
/>

<MetaphorComplete
  id="jr-metaphor-1"
  pieceSlug="what-v8-is"
  lang="ru"
  setup="Заполните пробел: точка вызова, которая видела ровно одну форму объекта, называется _______, и V8 генерирует для неё максимально быстрый машинный код."
  accepted={["мономорфной", "monomorphic", "mono"]}
  canonical="мономорфной (одна форма — самое быстрое состояние IC)"
  explanation="Monomorphic — золотой стандарт состояния IC. V8 генерирует машинный код с жёстко закодированным смещением свойства. Polymorphic (2-4 формы) добавляет ветвления; megamorphic (5+ форм) откатывается к универсальному поиску, который в 10-50 раз медленнее monomorphic."
/>

<Inset kind="why" lang="ru">
Зачем движку динамического языка знать типы? Машинный код типизирован — инструкция ADD для целых отличается от инструкции для чисел с плавающей точкой. V8 наблюдает, какие типы реально видит точка вызова, и генерирует специализированный код для этих типов. При изменении типа во время выполнения V8 должен выбросить специализированный код и начать заново. Это и есть цена deopt.
</Inset>

</Explanation>

<KeyTakeaway>V8 поднимается от медленного интерпретатора до быстрого машинного кода только при стабильных типах. Monomorphic точки вызова работают за 5-10 тактов; megamorphic — в 10-50 раз медленнее.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="what-v8-is-retrieval"
  lang="ru"
  questions={[
    { q: "Назовите четыре уровня компиляции V8 по порядку от самого медленного к самому быстрому.", a: "Ignition (интерпретатор байткода) → Sparkplug (базовый JIT) → Maglev (оптимизирующий среднего уровня) → TurboFan (тяжёлый оптимизирующий). Ignition примерно в 100 раз медленнее TurboFan на инструкцию, но не требует затрат на компиляцию. TurboFan компилирует десятки-сотни миллисекунд, но генерирует самый быстрый код." },
    { q: "Почему добавление нового свойства к объекту в середине кода замедляет последующие функции?", a: "Добавление свойства меняет hidden class объекта. Каждая функция, работающая с этим объектом через inline cache, специализировалась на СТАРОМ hidden class. При следующем вызове IC видит другой hidden class и переходит из monomorphic в polymorphic. Решение: конструировать объекты со всеми свойствами заранее в конструкторе, чтобы hidden class был стабильным с момента создания." },
    { q: "Что такое monomorphic inline cache и почему это самое быстрое состояние IC?", a: "Кэш на уровне точки вызова, где V8 записывает единственный увиденный hidden class и точное смещение свойства для него. При следующем выполнении той же строки с тем же hidden class V8 читает свойство по заранее вычисленному смещению за 5-10 тактов — без хеш-поиска, без проверки типов, одна инструкция MOV. Это быстрее всего, потому что нет ветвления — V8 жёстко кодирует смещение во время компиляции." },
  ]}
/>

<Recap lang="ru">
V8 — движок JS в Chrome и Node.js. Код выполняется через четыре уровня: Ignition интерпретирует медленно, собирая type feedback; Sparkplug генерирует дешёвый базовый машинный код; Maglev оптимизирует с наблюдаемыми типами; TurboFan создаёт агрессивно оптимизированный код для самых горячих функций. Функция остаётся на быстром уровне только пока формы объектов — hidden classes — остаются стабильными. Одно лишнее свойство в неподходящий момент меняет hidden class, деградирует inline cache с monomorphic до polymorphic или megamorphic и обходится в 10–50 раз дороже на этой точке вызова. Одна и та же JS-функция может работать за 5 тактов или за 500 тактов на доступ к свойству в зависимости от стабильности форм.
</Recap>
