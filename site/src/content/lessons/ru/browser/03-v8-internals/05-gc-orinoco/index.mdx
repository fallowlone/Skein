---
concepts:
- young generation
- old generation
- scavenger
- Mark-Compact
- concurrent marking
- write barrier
- tri-color marking
- GC pause
- incremental marking
deepensInto:
- 07-production-perf
estMin: 14
lang: ru
lessonType: topic
level: middle
order: 5
prereqs:
- 01-what-v8-is
slug: 05-gc-orinoco
sources:
- https://v8.dev/blog/orinoco-parallel-scavenger
- https://v8.dev/blog/trash-talk
- https://v8.dev/blog/concurrent-marking
- https://github.com/thlorenz/v8-perf/blob/master/gc.md
spiral:
- statefulness
status: ready
summary: Как сборщик мусора Orinoco в V8 разделяет молодое и старое поколения, запускает
  малый GC параллельно, перемещает разметку крупного GC на фоновый поток и использует
  барьеры записи для корректности при выполнении JS.
title: 'Orinoco GC: параллельный scavenger, конкурентная разметка и барьеры записи'
track: browser
unit: 03-v8-internals
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import TraceScenario from "~/components/pedagogy/TraceScenario.astro";
import NumbersCard from "~/components/layout/NumbersCard.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
Node.js-сервис нормально работает неделю, затем каждые 60 секунд появляется 5мс пауза в латентности p99. Код приложения не менялся. Цикл GC пересёк порог старого поколения. Понимание Orinoco — это разница между угадыванием и диагностикой.
</Hook>

<Crux>Orinoco запускает малый GC параллельно, разметку крупного GC — в фоне; паузы основного потока сокращаются с сотен мс до &lt;1мс.</Crux>

<Explanation>

## Структура кучи

Прежде чем тянуться к `--max-old-space-size` для решения проблемы с памятью, нужно понять: ты регулируешь две совершенно разные GC-машины с разными профилями пауз.

Куча V8 разделена на два поколения:

- **Молодое поколение** — только что выделенные объекты, обычно 1–8 МБ. Собирается часто (малый GC).
- **Старое поколение** — долгоживущие объекты, может быть сотни МБ. Собирается редко (крупный GC).

Объекты начинают в молодом поколении. Пережившие два малых GC **продвигаются** в старое. Этот генерационный дизайн использует наблюдение, что большинство объектов умирают молодыми — малый GC может быть быстрым, поскольку живое множество мало.

<FlowDiagram
  label="Новые объекты выделяются в молодом поколении, собираются Scavenger в малом GC; объекты, пережившие два малых GC, продвигаются в старое поколение, которое собирается редко через Mark-Sweep-Compact в крупном GC."
  perRow={3}
  hue="var(--d-frontend)"
  nodes={[
    { id: "alloc", label: "Новый объект", sub: "аллокация" },
    { id: "young", label: "Молодое поколение", sub: "Scavenge · малый GC" },
    { id: "old", label: "Старое поколение", sub: "Mark-Compact · крупный GC" },
  ]}
  edges={[
    { from: "alloc", to: "young" },
    { from: "young", to: "old", label: "пережил 2 малых GC" },
  ]}
  caption="Генерационная гипотеза: большинство объектов умирают молодыми. Молодое поколение (1–8 МБ) собирается часто и дёшево копирующим Scavenger (<1 мс). Пережившие два малых GC продвигаются в большое старое поколение, собираемое редко через Mark-Sweep-Compact (крупный GC)."
/>

## Малый GC: параллельный Scavenger

Scavenger использует **копирующий сборщик** с двумя полупространствами:
1. From-space содержит живые объекты; to-space пусто.
2. При сборке V8 обходит корни (стек, глобальные, регистры), копирует достижимые молодые объекты в to-space, обновляет указатели.
3. Мёртвые объекты брошены в from-space.
4. Пережившие два малых GC продвигаются в старое поколение.

Scavenger параллелен с первой фазы Orinoco (V8 6.2, 2017): несколько рабочих потоков делят работу через **динамическое воровство задач (work-stealing)** — механизм, при котором свободный поток «крадёт» задачи у занятого, максимизируя параллелизм. Паузы основного потока ~1мс, поскольку живое множество молодого поколения мало, а параллелизм держит wall-clock время низким.

## Крупный GC: Mark-Compact с конкурентной разметкой

Сборка старого поколения трассирует все живые объекты, подметает мёртвые и уплотняет выжившие для снижения фрагментации. До Orinoco это была операция **stop-the-world** длительностью сотни мс на больших кучах.

Ключевое нововведение Orinoco: **конкурентная разметка** — фоновый поток обходит кучу, пока JavaScript выполняется на основном потоке. Основной поток платит краткую финальную паузу разметки (однозначные мс) плюс фазу подметания/уплотнения (параллельная по рабочим потокам, но всё ещё блокирующая). Результат: пауза основного потока снижена ~на 50% на WebGL-нагрузках.

<NumbersCard
  id="card-gc-numbers"
  title="Числа Orinoco GC"
  data-lesson-visual
  stats={[
    { label: "Пауза малого GC (Scavenger)", value: "<1 мс типично" },
    { label: "Снижение паузы конкурентной разметки", value: "~50% на WebGL" },
    { label: "Параллельный Scavenger отгружен", value: "V8 6.2 (2017)" },
    { label: "Размер кучи молодого поколения", value: "1–8 МБ" },
    { label: "Порог продвижения", value: "пережил 2 малых GC" },
    { label: "Цель паузы GC при 60fps", value: "<16.6 мс на кадр" },
  ]}
/>

## Барьеры записи

Чтобы конкурентная разметка была корректной, пока JS одновременно мутирует кучу, V8 использует **барьеры записи**: небольшой код, генерируемый при каждой записи свойства, которая может пересечь от чёрного (уже помеченного) объекта к белому (ещё не посещённому).

V8 использует семантику **snapshot-at-the-beginning** (Юаса): когда основной поток перезаписывает ссылку на белый объект, барьер затемняет **исходный** (перезаписанный) референт в серый, чтобы маркер всё равно его посетил. Это сохраняет инвариант «каждый объект, достижимый при начале разметки, будет посещён», даже если основной поток опережает. Барьер записи стоит ~3–5 тактов на запись; V8 значительно вкладывает в то, чтобы держать его дешёвым.

Современный V8 использует **три-цветную разметку** (белый/серый/чёрный) с семантикой snapshot-at-the-beginning и гибридным конкурентным + инкрементным планированием. Инкрементная разметка амортизирует работу GC по выполнению JS без наблюдаемой единой паузы.

<DragOrder
  id="mid-drag-gc"
  pieceSlug="gc-orinoco"
  lang="ru"
  prompt="Упорядочите шаги параллельного малого GC (Scavenger):"
  items={[
    "Аллокация переполняет кучу молодого поколения",
    "Основной поток инициирует Scavenger, рабочие потоки присоединяются",
    "Обходим корни: стек, глобальные, регистры — находим живые молодые объекты",
    "Копируем живые объекты из from-space в to-space через work-stealing",
    "Обновляем указатели по всей куче на новые расположения",
    "Объекты, пережившие два GC-цикла, продвигаются в старое поколение",
    "From-space объявляется пустым, to-space становится новым from-space",
  ]}
  correctOrder={["0", "1", "2", "3", "4", "5", "6"]}
/>

<TraceScenario
  id="mid-trace-gc"
  pieceSlug="gc-orinoco"
  lang="ru"
  scenario="Node.js-процесс потребляет 4ГБ RAM через неделю работы. Трасируйте паттерн GC."
  steps={[
    { prompt: "Шаг 1: 4ГБ через неделю — медленная утечка, не краш. Какие V8-наблюдения покажут тренд?", reveal: "Запустите процесс с --trace-gc — каждое GC-событие логирует heap-before/heap-after. Постройте график по времени. Если размер старого поколения монотонно растёт понедельно — утечка. Если осциллирует вокруг стабильного среднего — GC справляется." },
    { prompt: "Шаг 2: heap-after стабильно растёт на 50МБ/день. Какой вид утечки?", reveal: "Объекты выживают до старого поколения, но никогда не становятся недостижимыми. Частые причины: кэши без TTL, обработчики событий, которые не удаляются, замыкания, удерживающие ссылки в долгоживущих коллбэках, scope запросов, утекающих в scope модуля." },
    { prompt: "Шаг 3: как найти источник утечки?", reveal: "Снимок кучи через Chrome DevTools (подключите к Node через --inspect). Сделайте снимки в день 1, 4, 7. Сравните их. Ищите типы объектов, чей счётчик рос неограниченно. Часто: десятки новых 'Closure' или 'Detached HTMLDivElement' узлов в день, указывающих на старый scope. Путь удержания показывает, что их держит." },
    { prompt: "Шаг 4: утечка найдена — LRU-кэш без максимального размера. Исправление?", reveal: "Ограничьте размер кэша N записями или N МБ. Добавьте вытеснение. Используйте lru-cache или аналог — он берёт на себя ведение учёта. Утечка исчезает в следующий раз, когда кэш достигает ограничения и вытесняет записи." },
    { prompt: "Шаг 5: предотвратить повторение?", reveal: "Снимок кучи при деплое как perf-CI шаг: деплой → ожидание N часов → снимок. Сравните с baseline. Регрессии счётчика объектов проваливают деплой. В основном ловит паттерн 'добавил Map(), но никогда не удалял записи' — ведущая причина медленных утечек в Node." },
  ]}
/>

<Quiz
  id="mid-quiz-gc-1"
  pieceSlug="gc-orinoco"
  lang="ru"
  question="Параллельный scavenger Orinoco снижает паузы малого GC через work-stealing. Почему крупный GC Mark-Compact всё ещё сложнее?"
  choices={[
    { label: "Старое поколение требует больше обходов указателей и больше по размеру; разметка всего достижимого графа не может завершиться менее чем за 1мс даже конкурентно", correct: true },
    { label: "Старое поколение не имеет мусора", misconception: "Старое поколение накапливает мусор от долгоживущих объектов, ставших недостижимыми; оно определённо нуждается в сборке." },
    { label: "Scavenger медленнее Mark-Compact", misconception: "Наоборот: scavenger быстрее на байт из-за копирования малого живого множества." },
    { label: "Старое поколение не нуждается в GC", misconception: "Все поколения нуждаются в GC; GC старого поколения просто имеет больше работы." },
  ]}
/>

<Quiz
  id="mid-quiz-gc-2"
  pieceSlug="gc-orinoco"
  lang="ru"
  question="Почему конкурентная разметка нуждается в барьерах записи?"
  choices={[
    { label: "Чтобы не дать двум GC-потокам посетить один объект дважды", misconception: "Work-stealing обрабатывает дублирование посещений; барьеры записи о корректности при мутации JS-кодом ссылок во время разметки." },
    { label: "Потому что основной поток JS может перезаписать ссылку на непосещённый объект, пока GC-поток выполняет разметку — без барьеров GC никогда бы не посетил этот объект и ложно собрал бы его", correct: true },
    { label: "Чтобы ускорить фазу разметки", misconception: "Барьеры записи добавляют стоимость (~3-5 тактов на запись) — они обеспечивают корректность, а не скорость." },
    { label: "Для обработки указателей из старого поколения в молодое", misconception: "Это remembered sets / card tables — другой механизм отслеживания указателей." },
  ]}
/>

<Inset kind="why" lang="ru">
Зачем вообще нужно деление на поколения? Большинство выделяемых объектов умирают быстро — временный буфер, промис, React-элемент на каждый рендер. Собирать только молодое поколение (которое мало) намного дешевле, чем сканировать всю кучу. Старое поколение нуждается в сборке только редко. Эта «генерационная гипотеза» эмпирически верна для большинства программ и является причиной, по которой генерационные GC стали отраслевым стандартом в 1990-х годах.
</Inset>

</Explanation>

<KeyTakeaway>Orinoco разделяет GC на параллельный малый (молодое поколение, менее 1мс) и конкурентный крупный (старое поколение, разметка на фоне, уплотнение параллельно). Компромисс: барьеры записи стоят 3–5 тактов на запись свойства, но устраняют стоп-мир паузы в сотни мс, бывшие нормой до Orinoco.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="gc-orinoco-retrieval"
  lang="ru"
  questions={[
    { q: "Опишите цикл малого GC (Scavenger) в V8.", a: "Scavenger использует копирующий сборщик с двумя полупространствами (from-space и to-space). При сборке V8 обходит корни (стек, глобальные, регистры), копирует достижимые молодые объекты в to-space и обновляет указатели. Объекты, пережившие два малых GC, продвигаются в старое поколение. Мёртвые объекты брошены в from-space без явной очистки. Scavenger параллелен (несколько рабочих потоков через work-stealing) с V8 6.2. Типичная пауза: <1мс, поскольку живое множество молодого поколения мало." },
    { q: "Как конкурентная разметка в Orinoco не допускает повреждения кучи?", a: "С помощью барьеров записи. Когда основной поток перезаписывает ссылку, барьер записи V8 затемняет ранее упомянутый (перезаписанный) объект в серый, чтобы маркер всё равно его посетил. Это сохраняет snapshot-at-the-beginning инвариант: каждый объект, достижимый при начале разметки, будет посещён, даже если основной поток изменяет ссылки во время разметки. После конкурентной разметки краткая stop-the-world пауза повторно размечает изменившиеся корни, затем sweep/compact работают параллельно. Суммарная пауза основного потока идёт от сотен-мс (stop-the-world) к однозначным мс." },
    { q: "Какова самая частая причина медленной утечки памяти в Node.js и как её найти?", a: "Кэши, maps или sets, накапливающие записи без TTL или вытеснения по максимальному размеру. Долгоживущие переменные уровня модуля, удерживающие данные уровня запроса. Обработчики событий, зарегистрированные, но никогда не удалённые. Для поиска: снимки кучи с интервалами (день 1, 4, 7) через --inspect + панель DevTools Memory. Сравните снимки. Ищите типы объектов, растущие неограниченно. Путь удержания показывает, какой объект их держит. Исправление: добавьте вытеснение, ограничьте размер кэша, удаляйте обработчики при очистке." },
  ]}
/>

<Recap lang="ru">
Orinoco — генерационный сборщик мусора V8. Новые объекты попадают в молодое поколение (~1–8 МБ); пережившие два малых GC-цикла продвигаются в старое поколение. Малый GC использует копирующий scavenger, работающий параллельно на рабочих потоках — паузы обычно менее 1мс. Крупный GC (старое поколение) использует mark-compact; конкурентная разметка перемещает обход графа объектов на фоновый поток, пока выполняется JS, снижая паузы основного потока ~на 50% на WebGL-нагрузках. Барьеры записи при каждой записи свойства держат конкурентный маркер корректным при изменении JS-потоком ссылок в середине цикла. Инкрементная разметка дополнительно амортизирует работу по выполнению JS. Фундаментальный компромисс: 3–5 тактов на запись (накладные расходы барьера) в обмен на устранение стоп-мир пауз в сотни мс, бывших нормой до Orinoco. Теперь, когда встретишь периодические скачки p99 каждые N секунд в Node-сервисе, первый вопрос — не кэш, не балансировщик, а: пересекает ли цикл GC старого поколения порог? Проверяй `--trace-gc` прежде всего остального.
</Recap>
