---
concepts:
- TurboFan
- sea-of-nodes
- escape analysis
- polymorphic inlining
- speculative guard
- deopt-loop
- FeedbackVector pollution
- Maglev SSA
- Sparkplug baseline JIT
deepensInto:
- 07-production-perf
estMin: 18
lang: ru
lessonType: topic
level: senior
order: 6
prereqs:
- 02-jit-pipeline
- 04-inline-caches
- 03-hidden-classes
slug: 06-turbofan-and-deopt
sources:
- https://v8.dev/blog/maglev
- https://v8.dev/blog/sparkplug
- https://github.com/thlorenz/v8-perf/blob/master/compiler.md
- https://mathiasbynens.be/notes/shapes-ics
spiral:
- statefulness
status: ready
summary: Sea-of-nodes IR TurboFan, escape-анализ, полиморфный инлайнинг, спекулятивные
  защиты, FeedbackVector подробно, однопроходный Sparkplug и диагностика deopt-loop
  с --trace-deopt.
title: Спекулятивный движок TurboFan и ловушка deopt-loop
track: browser
unit: 03-v8-internals
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import TraceScenario from "~/components/pedagogy/TraceScenario.astro";
import DebugLog from "~/components/pedagogy/DebugLog.astro";
import NumbersCard from "~/components/layout/NumbersCard.astro";
import Misconception from "~/components/layout/Misconception.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
Критичная для производительности функция повторно deopt'нется в production — каждый вызов запускает 100мс перекомпиляцию TurboFan, затем снова deopt. Ваш горячий путь на порядки медленнее, чем некомпилированный. Это deopt-loop, и он начинается с одной неправильно понятой защиты.
</Hook>

<Crux>TurboFan охраняет каждую спекуляцию; одна сработавшая защита в плотном цикле запускает deopt-loop дороже, чем не оптимизировать.</Crux>

<Explanation>

## Спекулятивный движок оптимизации TurboFan

Когда функция достаточно горяча и её FeedbackVector достаточно стабилен, V8 передаёт её TurboFan — и именно его оптимизации позволяют горячему JS-циклу конкурировать с рукописным Си.

TurboFan строит **sea-of-nodes** граф IR (промежуточное представление, где и поток управления, и поток данных — это рёбра одного графа) и выполняет:
- **Агрессивный инлайнинг** — точки вызова с известными целями встраиваются, устраняя накладные расходы вызова и открывая возможности кросс-функциональных оптимизаций.
- **Escape-анализ** — объекты, не выходящие за пределы функции, выделяются на стеке или вовсе устраняются (ноль аллокаций, нет давления на GC).
- **Полиморфный инлайнинг** — до 4 известных целей вызова встраиваются с предварительной проверкой класса; общий медленный путь обрабатывает редкие формы.
- **Анализ диапазонов** — переменная, известная как 0..255, остаётся в байтовом регистре; индекс цикла с известными границами избегает проверок переполнения.
- **Сужение типов** — итеративное уточнение типов через граф, позволяющее создавать более точный машинный код.

При каждом спекулятивном предположении TurboFan устанавливает **инструкцию защиты**: «это smi» перед арифметикой, «этот объект имеет hidden class HCx» перед доступом к свойству. Сбой защиты означает deopt. Компромисс: код TurboFan часто в 3–10× быстрее Maglev **при соблюдении защит**; при их нарушении накладные расходы хуже, чем никогда не компилировать через TurboFan.

<FlowDiagram
  label="TurboFan спекулятивно оптимизирует функцию с защитами; когда защищённое предположение, например о типе, нарушается во время выполнения, V8 деоптимизирует и делает bailout, возобновляя выполнение в байткоде Ignition, и переоптимизирует позже, если функция всё ещё горячая."
  perRow={4}
  hue="var(--d-frontend)"
  nodes={[
    { id: "opt", label: "Оптимизация TurboFan", sub: "ставит защиты" },
    { id: "violate", label: "Предположение нарушено", sub: "защита упала" },
    { id: "deopt", label: "Деоптимизация", sub: "bailout" },
    { id: "bytecode", label: "Возврат в байткод", sub: "Ignition" },
  ]}
  edges={[
    { from: "opt", to: "violate", label: "тип сменился" },
    { from: "violate", to: "deopt" },
    { from: "deopt", to: "bytecode" },
    { from: "bytecode", to: "opt", label: "переопт. если горячо" },
  ]}
  caption="Цикл спекуляции. Один проход по нему стоит микросекунды и переживаем. Ловушка — deopt-loop: когда нарушающее значение повторяется при каждом вызове, V8 переоптимизирует и деоптимизирует бесконечно — медленнее, чем не оптимизировать вовсе."
/>

## FeedbackVector подробно

Если хочешь понять, почему функция отказывается оставаться оптимизированной несмотря на многократные перекомпиляции, смотри в FeedbackVector — это память движка о том, что он наблюдал, и одного загрязнённого слота достаточно, чтобы заблокировать TurboFan.

У каждой функции есть связанный **FeedbackVector** — массив фиксированного размера в куче GC, по одному слоту на IC-сайт (загрузка свойства, вызов, бинарная операция). Слоты хранят:
- Один указатель на hidden class (monomorphic)
- Небольшой массив классов (polymorphic)
- `MegamorphicSentinel` (сдался)

Все четыре уровня читают его: Sparkplug читает минимальный feedback (тип), Maglev читает более богатые данные (цепочки классов, частоты целей вызовов), TurboFan читает всё плюс количество итераций цикла и вероятности ветвлений.

**Загрязнение FeedbackVector**: один megamorphic-слот объясняет, почему V8 не может держать функцию оптимизированной даже после deopt-и-reopt циклов. `%DebugPrintFeedback(fn)` в d8 выводит вектор; его инспекция — отправная точка для диагностики постоянного deopt-поведения.

## Sparkplug: JIT, который не оптимизирует

Sparkplug (V8 9.1, май 2021) генерирует машинный код из байткода за один линейный проход без SSA, инлайнинга или планирования инструкций. Для каждого байткода Ignition Sparkplug генерирует фиксированный блок нативных инструкций — примерно в 1.5–2× быстрее Ignition, так как накладные расходы цикла диспетчеризации интерпретатора (поиск в таблице, косвенный прыжок) заменяются прямым выполнением. Скорость компиляции: ~1мс/кБ байткода. Задокументированная средняя польза: 5–15% на реальных нагрузках. Цель: дать горячим-но-не-достаточно-горячим-для-Maglev функциям ускорение без оплаты стоимости компиляции Maglev.

## Maglev: SSA, но дёшево

Maglev (2023) заполняет пробел между Sparkplug и TurboFan. Конвейер: байткод → Maglev IR (SSA) → линейный распределитель регистров → нативный код. IR проще, чем у TurboFan (без цепочек эффектов, без проходов перезаписи графа); распределитель регистров линейного сканирования вместо раскраски графа. Maglev выполняет некоторые спекулятивные оптимизации — специализирует загрузки свойств и арифметику по наблюдаемым типам — но пропускает escape-анализ, полиморфный инлайнинг и итерацию сужения типов. Результат: ~10мс компиляция, ~50–70% качества кода TurboFan, правильный компромисс для средне горячего кода.

<NumbersCard
  id="card-senior-numbers"
  title="V8: числа старшего уровня"
  data-lesson-visual
  stats={[
    { label: "Sparkplug отгружен", value: "V8 9.1 (май 2021)" },
    { label: "Maglev отгружен", value: "V8 11.0 (середина 2023)" },
    { label: "Время компиляции TurboFan", value: "~100 мс / функция" },
    { label: "Время компиляции Maglev", value: "~10 мс / функция" },
    { label: "Размер IC-слота", value: "16 байт (8Б класс + 8Б хендлер)" },
    { label: "Скорость компиляции Sparkplug", value: "~1 мс / кБ байткода" },
    { label: "TurboFan vs Maglev скорость", value: "TurboFan ~1.5–3× быстрее при стабильных защитах" },
  ]}
/>

<TraceScenario
  id="sr-trace-deopt"
  pieceSlug="turbofan-and-deopt"
  lang="ru"
  scenario="Критичная для производительности функция deopt-loop'ится в production. Трасируйте и исправьте."
  steps={[
    { prompt: "Шаг 1: deopt-loop означает непрерывный tier-down затем tier-up. Что в --trace-deopt это подтвердит?", reveal: "Ищите повторяющиеся пары 'deoptimized' / 'optimized' на одной функции в короткое окно. Поле 'reason' говорит, какая защита сработала — часто: 'not a smi', 'wrong map', 'out of bounds'." },
    { prompt: "Шаг 2: причина — 'not a smi'. Что это означает?", reveal: "Защита малого целого сработала. TurboFan предположил, что значение всегда smi (малое целое, 31-битное знаковое), но во время выполнения увидел число, выходящее за диапазон или NaN. Частая причина: арифметика, иногда дающая NaN (0/0, Math.sqrt(-1)) или значения выше 2^30." },
    { prompt: "Шаг 3: найдите проблемную строку. Как?", reveal: "DevTools deopt traces включают позицию в источнике. Смотрите на строку — скорее всего умножение или деление, иногда идущее по пути, дающему NaN или большое значение. Часто: парсинг пользовательских данных, где большинство значений малы, но один пользователь присылает огромное число." },
    { prompt: "Шаг 4: исправление?", reveal: "Два варианта. (а) Проверяйте ввод перед горячим циклом: if (x > 1e9 || isNaN(x)) handleSlow(x); else fastLoop(x). Это держит быстрый цикл monomorphic-on-smi, направляя патологические значения на медленный путь. (б) Конвертируйте все значения в известный тип заранее: Number(x) | 0 для int32, или всегда используйте float64 через Math.fround для типизированной численной работы." },
    { prompt: "Шаг 5: предотвратить повторение?", reveal: "Добавьте тест, утверждающий, что --trace-deopt не производит вывода для функции при репрезентативных входных данных. CI проваливается при любом deopt в горячем пути. Для наблюдаемости в production, откройте метрику deopt-counter через Node.js V8 inspector — оповещение при ненулевом росте." },
  ]}
/>

<DebugLog
  id="sr-debug-deopt"
  pieceSlug="turbofan-and-deopt"
  lang="ru"
  title="Диагностируйте лог deopt V8 — в чём первопричина?"
  output={`[deoptimizing (DEOPT eager): begin 0x... <JSFunction processItem (sfi = 0x...)> (opt #42) @14, FP to SP delta: 128, caller sp: 0x...]
            ;;; deoptimize at <main.js:42:18>, not a Smi
   bytecode position 14
[deoptimizing (eager): end 0x... processItem  @14 => node=4, pc=0x..., caller sp=0x..., took 0.012 ms]
[marking 0x... <JSFunction processItem (sfi = 0x...)> for non-concurrent optimization]
[compiling method 0x... <JSFunction processItem (sfi = 0x...)> using TurboFan]
[deoptimizing (DEOPT eager): begin 0x... <JSFunction processItem (sfi = 0x...)> (opt #43) @14, FP to SP delta: 128, caller sp: 0x...]
            ;;; deoptimize at <main.js:42:18>, not a Smi
   bytecode position 14`}
  outputLang="log"
  question="Одна функция продолжает deopt'иться с причиной 'not a Smi' в строке 42:18. Что происходит и как это исправить?"
  hint="Обратите внимание на цикл deopt + перекомпиляция. Какая защита в строке 42:18 может срабатывать повторно?"
  answer="Функция в deopt-loop. TurboFan компилирует её, предполагая, что значение в строке 42:18 — Smi (малое целое). При выполнении это значение не Smi (либо > 2^30, либо NaN), поэтому защита срабатывает и V8 деоптимизирует. V8 затем снова продвигает функцию (поскольку она всё ещё горячая), компилирует с TurboFan, делает то же неверное предположение, снова deopt. Стоимость за цикл: ~12мкс накладных расходов + ~100мс компиляции TurboFan + время, проведённое на более медленном уровне между ними. Если это происходит при каждом вызове, функция работает на порядки хуже некомпилированной. Исправление: найдите операцию в строке 42:18 — вероятно, арифметическая операция, результат которой иногда слишком большой или NaN. Либо добавьте явную проверку типа перед горячим блоком (typeof === 'number' && Number.isFinite(x)) для обработки патологических значений на медленном пути, либо используйте Math.fround / |0 / +x для приведения к известному стабильному типу, чтобы предположение о Smi всегда выполнялось."
/>

<Misconception id="mc-deopt-fatal">
  <p><strong>«Deopt ломает функцию навсегда.»</strong> Нет. Deopt откатывает на нижний уровень; функция снова поднимается при стабильном feedback. Проблема — deopt в плотном цикле: каждая итерация deopt, оптимизация, deopt. Единичный deopt — микросекунды; deopt-loop — катастрофа.</p>
</Misconception>

<Quiz
  id="sr-quiz-td-1"
  pieceSlug="turbofan-and-deopt"
  lang="ru"
  question="V8 имеет ЧЕТЫРЕ JIT-уровня и конкурентный GC. Какова глубинная причина производительности, почему они необходимы вместо одного хорошего оптимизатора?"
  choices={[
    { label: "Динамизм JavaScript означает, что знание типов во время компиляции неполное; V8 должен наблюдать типы во время выполнения для специализации, что работает только после некоторого прогрева — а сам прогрев должен быть быстрым, отсюда Ignition + Sparkplug", correct: true },
    { label: "Аппаратная вариативность требует нескольких уровней", misconception: "Аппаратная вариативность обрабатывается на этапе генерации кода, а не уровнями." },
    { label: "Браузеры должны поддерживать старые стандарты", misconception: "Стандарты обрабатываются на уровне парсера; все уровни выполняют современный ECMAScript." },
    { label: "Маркетинговое требование", misconception: "Каждый уровень имеет задокументированную инженерную мотивацию." },
  ]}
/>

<Quiz
  id="sr-quiz-td-2"
  pieceSlug="turbofan-and-deopt"
  lang="ru"
  question="Функция обрабатывает 10М элементов на кадр и monomorphic в тестах, но megamorphic в production. Какова наиболее вероятная причина?"
  choices={[
    { label: "Тестовые машины быстрее", misconception: "Состояние IC зависит от наблюдаемых форм, а не от скорости машины." },
    { label: "В production больше путей кода, задействующих функцию — сторонние интеграции или пути с feature-flag конструируют входные данные с разными формами объектов, которые тестовый набор никогда не покрывает", correct: true },
    { label: "TurboFan отключён в production сборках", misconception: "TurboFan нельзя избирательно отключить по окружению." },
    { label: "Функция была встроена в тестах, изменив её IC", misconception: "Инлайнинг меняет расположение IC, но не влияет на monomorphic состояние при консистентных входных формах." },
  ]}
/>

<Inset kind="why" lang="ru">
Почему TurboFan использует sea-of-nodes IR вместо традиционного CFG с базовыми блоками? Sea-of-nodes представляет как поток управления, так и поток данных как рёбра в одном графе — нет понятия «порядок инструкций» до планирования. Это позволяет более агрессивные оптимизации: escape-анализ может поднять аллокации из циклов, анализ диапазонов распространяет границы через ветки, а устранение мёртвого кода работает на уровне выражений, а не только базовых блоков. Цена: компилятор значительно сложнее и труднее для отладки. Но для динамически типизированного языка, где компилятор должен рассуждать о типах, наблюдаемых во время выполнения, а не объявленных во время компиляции, гибкость стоит этого.
</Inset>

</Explanation>

<KeyTakeaway>Сила TurboFan — в спекулятивных защитах. При соблюдении защит код в 3–10× быстрее Maglev. При нарушении в плотном цикле deopt-loop стоит дороже, чем никогда не оптимизировать. Стабильные hidden classes и ограниченные числовые диапазоны — предусловия для защит, которые всегда выполняются.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="turbofan-and-deopt-retrieval"
  lang="ru"
  questions={[
    { q: "Что такое sea-of-nodes IR TurboFan и какие оптимизации он позволяет?", a: "Sea-of-nodes представляет как поток управления, так и поток данных как рёбра в одном графе — нет порядка инструкций до планирования. Это позволяет: escape-анализ (выделять на стеке или устранять объекты, не выходящие за пределы функции), полиморфный инлайнинг (встраивать 2-4 известных цели вызова с предварительной проверкой класса), анализ диапазонов (держать переменную 0-255 в байтовом регистре, пропускать проверки переполнения), устранение мёртвого кода на уровне выражений. Цена — сложность компилятора; польза — качество кода, приближающееся к тому, что может создать компилятор со статической типизацией, несмотря на динамические типы JavaScript." },
    { q: "Объясните 'загрязнение FeedbackVector' и как его диагностировать.", a: "Слот FeedbackVector становится megamorphic при наблюдении 5+ различных hidden classes. TurboFan читает все слоты при компиляции функции. Один megamorphic-слот не даёт TurboFan встроить соответствующую точку вызова или специализировать доступ к свойству — он должен генерировать общий хендлер. Если загрязнённый слот в плотном внутреннем цикле, функция не может быть полностью оптимизирована и может не продвинуться в TurboFan вовсе. Диагностика: %DebugPrintFeedback(fn) в d8 показывает состояние каждого слота. Ищите записи MegamorphicSentinel и трассируйте, какой строке источника они соответствуют." },
    { q: "Чем deopt-loop отличается от разового deopt?", a: "Разовый deopt: TurboFan компилирует функцию с предположением, которое оказывается неверным один раз, deopt'ится, функция перегревается с новым feedback, включающим исключительный случай, TurboFan перекомпилирует с обновлёнными предположениями, функция остаётся оптимизированной. Разовый deopt стоит ~микросекунды + одна перекомпиляция. Deopt-loop: условие, вызывающее защиту, присутствует при каждом вызове — TurboFan не может включить исключение в специализацию, поэтому деоптимизирует при каждом вызове, немедленно повторно продвигает, компилирует снова (100мс), снова deopt. Стоимость за итерацию: 12мкс deopt + 100мс компиляция + замедление от работы на нижнем уровне. Исправление: переместить патологические значения на отдельный медленный путь перед горячим циклом, чтобы быстрый путь оставался типостабильным." },
  ]}
/>

<Recap lang="ru">
TurboFan строит sea-of-nodes IR и применяет escape-анализ, полиморфный инлайнинг, сужение диапазонов и агрессивную специализацию типов. Каждое спекулятивное предположение становится инструкцией защиты; сбой защиты вызывает деоптимизацию. Единичный deopt стоит микросекунды; deopt-loop — где TurboFan перекомпилирует и deopt'ится при каждом вызове — стоит дороже, чем никогда не оптимизировать. FeedbackVector — профильные данные, движущие каждым уровнем: Ignition пишет его, TurboFan читает для знания форм и типов специализации. Загрязнение FeedbackVector (megamorphic-слоты) может полностью заблокировать TurboFan. Sparkplug (V8 9.1) — базовый уровень стоимостью ~1мс/кБ, дающий 1.5–2× над Ignition; Maglev (2023) добавляет SSA-специализацию за ~10мс компиляции, достигая 50–70% качества TurboFan. Правильная ментальная модель: уровни — не водопад, а лестница с лифтами — функции могут быть на разных уровнях одновременно, а эвристики тиеризации постоянно пересматриваются на основе данных FeedbackVector. Теперь, когда встретишь горячую функцию, работающую на порядки хуже ожиданий, первым делом запускай `--trace-deopt`: видишь одну и ту же функцию в цикле deopt+перекомпиляция — нашёл deopt-loop, и исправление живёт в модели данных, а не в движке.
</Recap>
