---
concepts:
- hidden-class
- inline-cache-states
- jit-tiers
- deopt-loop
- orinoco-gc
- typed-arrays
estMin: 14
lang: ru
level: senior
order: 8
prereqs: []
slug: quiz-short
sources:
- https://mathiasbynens.be/notes/shapes-ics
- https://v8.dev/blog/maglev
- https://v8.dev/blog/concurrent-marking
status: ready
summary: "Промпты на свободное воспроизведение по всему юниту. Сначала ответьте своими словами — hidden classes, состояния IC, уровни JIT, deopt-loop, Orinoco — затем откройте модельный ответ и сравните."
title: 'Внутренности V8: тест с краткими ответами'
track: browser
unit: 03-v8-internals
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";

<Hook>
Воспроизведение по памяти бьёт перечитывание. На каждый промпт восстановите полный ответ из памяти, прежде чем открыть модельный — именно усилие припоминания закрепляет ментальную модель V8.
</Hook>

<Goal lang="ru">
Восстановите ключевые механизмы юнита — переходы hidden class, машину состояний IC, четырёхуровневый конвейер, deopt-loop и барьеры Orinoco — не подглядывая в уроки.
</Goal>

<RetrievalDrawer
  client:visible
  id="03-v8-internals-short-retrieval"
  lang="ru"
  questions={[
    { q: "Что такое hidden class и почему ПОРЯДОК добавления свойств решает, делят ли его два объекта?", a: "Hidden class (в исходниках V8 — 'Map', в SpiderMonkey — 'Shape') — это внутренний дескриптор раскладки объекта: какие свойства есть и по каким смещениям в памяти. Каждое добавление свойства идёт по ребру дерева переходов или создаёт его, поэтому итоговый класс зависит от последовательности добавлений, а не только от финального набора. {x:1, y:2} проходит empty → x → x+y; {y:2, x:1} проходит empty → y → y+x и попадает в другой лист. Два объекта с одинаковыми свойствами, добавленными в разном порядке, для любого inline cache — разные формы. Фикс: строить объекты со всеми свойствами в одном фиксированном порядке, в конструкторе или классе." },
    { q: "Опишите машину состояний IC и что запускает каждый переход.", a: "Inline cache — это заглушка на сайт вызова, хранящая увиденный hidden class и смещение свойства для него. Uninitialized → monomorphic на первом вызове (один класс, прямое чтение ~5–10 циклов). Monomorphic → polymorphic при появлении второго класса (IC держит 2–4 пары класс/обработчик, короткую цепочку ветвлений, всё ещё быстро). Polymorphic → megamorphic на 5-м классе: V8 бросает поклассовое отслеживание и ставит общий рантайм-поиск за ~50–100 циклов — в 10–50× медленнее. Переходы односторонние; V8 никогда не возвращает сайт из megamorphic в polymorphic, поэтому фикс делается выше по потоку, подавая на сайт одну форму." },
    { q: "Назовите четыре уровня JIT V8 по порядку и нишу каждого.", a: "Ignition (интерпретатор байткода): нулевая стоимость компиляции, в ~10× медленнее оптимизированного кода, пишет фидбэк по типам в FeedbackVector. Sparkplug (baseline JIT, V8 9.1/2021): один линейный проход байткод→машкод, без специализации, ~1 мс/кБ, ~1.5–2× над Ignition для быстрого прогрева. Maglev (средний уровень, 2023): SSA-специализация по наблюдаемым типам за ~10 мс, ~50–70% качества TurboFan, для средне-горячего кода. TurboFan (тяжёлый оптимизатор): IR sea-of-nodes, инлайнинг, escape analysis, спекулятивные guard, ~100 мс компиляции, самый быстрый код — для горячих, стабильных по типу функций. Profile-guided tiering продвигает функцию вверх, только когда её FeedbackVector достаточно стабилен, чтобы оправдать стоимость компиляции." },
    { q: "Чем разовый deopt отличается от deopt-loop и почему loop катастрофичен?", a: "TurboFan ставит guard перед каждой спекуляцией ('это Smi', 'у объекта hidden class HCx'). Разовый deopt: guard срабатывает один раз, V8 выбрасывает оптимизированный код и откатывается в Ignition, функция заново прогревается с фидбэком, включающим исключительный случай, и стабильно переоптимизируется — цена в несколько микросекунд плюс одна перекомпиляция. Deopt-loop: условие, ломающее guard, присутствует на каждом вызове, поэтому V8 делает deopt, тут же заново продвигает (функция всё ещё горячая), перекомпилирует с тем же неверным предположением (~100 мс) и снова deopt. За цикл это ~12 µs служебных операций плюс 100 мс компиляции плюс время на медленном уровне — на порядки хуже, чем не оптимизировать. Фикс: направить патологическое значение на медленный путь до горячего цикла, чтобы быстрый путь оставался стабильным по типу." },
    { q: "Как Orinoco делит работу GC и почему конкурентной маркировке нужен write barrier?", a: "Orinoco поколенческий. Young generation (~1–8 МБ) собирается параллельным копирующим Scavenger с work-stealing — паузы под ~1 мс, потому что живой набор мал; пережившие два minor GC продвигаются. Old generation использует Mark-Compact с конкурентной маркировкой: фоновый поток обходит живой граф, пока JS работает, срезая паузу основного потока с сотен мс до однозначных мс. Поскольку мутатор может менять ссылки во время маркировки, V8 применяет write barrier по схеме snapshot-at-the-beginning: при перезаписи ссылки основным потоком барьер красит перезаписанный (белый) объект в серый, чтобы маркер всё равно его посетил. Без этого чёрный объект мог бы держать единственную ссылку на белый, который не пометится и будет ошибочно собран — тихое повреждение кучи. Барьер стоит ~3–5 циклов на запись ссылки." },
    { q: "Почему TypedArray обходят слой IC и когда это правильный инструмент?", a: "У TypedArray (Int32Array, Float64Array, …) тип элемента фиксирован при создании, поэтому V8 эмитит плоские load/store с одной лишь проверкой границ — без проверки hidden class, без поиска смещения свойства, вообще без IC. Это правильный инструмент для горячих числовых циклов (системы частиц, 3D, аудио, ML), для заранее выделенных буферов, избегающих покадрового выделения и потому давления на GC, и для любой нагрузки, где даже monomorphic IC объекта остаётся узким местом. Цена — эргономика: фиксированный тип элемента, нет именованных свойств, менее идиоматичный код. Для анимационного цикла на 10M сущностей за кадр параллельные TypedArray убирают и проблему megamorphic IC, и покадровый GC одновременно." },
  ]}
/>

<Recap lang="ru">
Если вы смогли восстановить каждый ответ по памяти, у вас в руках хребет юнита: порядок добавления свойств задаёт hidden class, hidden class ведёт машину состояний IC, FeedbackVector и стабильность типов управляют тем, как высоко по четырёхуровневой лестнице поднимется функция (и не уйдёт ли она в deopt-loop), барьеры Orinoco держат конкурентную сборку корректной, а TypedArray обходят слои IC и GC целиком. Любой фикс юнита живёт выше по потоку от V8 — в форме и числовом типе ваших данных.
</Recap>
