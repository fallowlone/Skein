---
concepts:
- lcp-phases
- inp-parts
- cls-scoring
- field-vs-lab
- metric-tradeoffs
- rum-attribution
estMin: 14
lang: ru
level: senior
order: 8
prereqs: []
slug: quiz-short
sources:
- https://web.dev/articles/vitals
- https://web.dev/articles/inp
- https://web.dev/articles/cls
status: ready
summary: "Промпты на свободное припоминание по всему юниту Core Web Vitals. Сначала ответь своими словами, затем раскрой эталонный ответ и сравни."
title: 'Core Web Vitals: тест на припоминание'
track: browser
unit: 07-core-web-vitals
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";

<Hook>
Припоминание бьёт перечитывание. Для каждого промпта произнеси или запиши полный ответ по памяти, прежде чем открыть эталонный — именно усилие припоминания закрепляет материал.
</Hook>

<Goal lang="ru">
Восстанови хребет юнита — что измеряет каждая vital и её порог, разбиение LCP на фазы, части INP и ловушку гидрации, подсчёт CLS, правило field-vs-lab и дисциплину общего бюджета — не подглядывая в уроки.
</Goal>

<RetrievalDrawer
  client:visible
  id="07-core-web-vitals-short-retrieval"
  lang="ru"
  questions={[
    { q: "Что измеряет каждая Core Web Vital, каков её порог 'good' и где их меряют?", a: "LCP (Largest Contentful Paint): время до paint крупнейшего контентного элемента; good ≤2.5 с. INP (Interaction to Next Paint): задержка от тапа/клика/нажатия клавиши до следующего видимого paint, репортится по высокому перцентилю по всем взаимодействиям; good ≤200 мс. CLS (Cumulative Layout Shift): безразмерная оценка неожиданного движения раскладки; good ≤0.1. Все три меряют на 75-м перцентиле реальных пользователей Chrome в field (CrUX), а не по одному прогону в лабе — p75 фокусируется на медленной четверти пользователей, а не на быстрых выбросах." },
    { q: "Назови четыре фазы LCP и фикс для каждой, и объясни, почему надо прочитать разбиение перед применением фикса.", a: "TTFB — медленный сервер/CDN/БД на пути рендера; чини кэшированием, CDN и выносом медленных данных с пути рендера. Resource load delay — картинка обнаружена поздно (упомянута в CSS или вставлена через JS), так что preload scanner её не находит; чини размещением в начальном HTML с fetchpriority='high' или preload-ссылкой. Resource load time — слишком большая/неверный формат картинка; чини AVIF/WebP в размер через srcset. Element render delay — render-blocking CSS/JS всё ещё занимает поток; чини инлайнингом критического CSS и отсрочкой скриптов. Сначала читаешь разбиение, потому что фикс работает только на доминирующей фазе — сжатие картинки, когда доминирует load delay, не даёт ничего." },
    { q: "Назови три части INP и объясни паттерн INP только в начале и на что он указывает.", a: "Input delay (поток занят длинной задачей, когда приходит ввод), processing time (выполняется обработчик события) и presentation delay (style/layout/paint/composite после обработчика). Если INP плох только для взаимодействий в первые несколько секунд и нормален потом, причина — гидрация: длинная задача гидрации блокирует основной поток, поэтому ранние тапы стоят за ней в очереди (input delay). Когда гидрация завершается, поток свободен. Фикс — уменьшить то, что гидрируется — Server Components, островки, code-splitting, прогрессивная гидрация — а не оптимизировать код обработчика, который и так быстр." },
    { q: "Как считается один сдвиг CLS, что такое session window и что делает исключение в 500 мс?", a: "Оценка одного сдвига = impact fraction (суммарная площадь сдвигающихся элементов как доля вьюпорта) × distance fraction (наибольшее смещение как доля высоты вьюпорта). Так элемент в пол-вьюпорта, сдвинувшийся на 20% высоты вьюпорта, набирает 0.5 × 0.2 = 0.1 — ровно на границе good. CLS — не сумма за всё время жизни; это сумма худшего session window — кластера, где каждый сдвиг в пределах 1 с от предыдущего и кластер длится максимум 5 с (это заменило сумму за всё время, несправедливо наказывавшую долгоживущие страницы и бесконечный скролл). Сдвиги в пределах 500 мс после взаимодействия исключаются, потому что это движение ожидаемо (раскрытие аккордеона, разворот дропдауна). CLS наказывает только движение, которое пользователь не вызывал." },
    { q: "Объясни различие lab-vs-field и дай правило, когда использовать каждое.", a: "Lab data — синтетическое измерение — одно устройство, одна сеть, воспроизводимо — из инструментов вроде Lighthouse или трейса DevTools; хорошо для отладки (атрибутировать проблему, бисектить коммиты) и для CI-гейтов на регрессии (быстро, без реальных пользователей). Field data — мониторинг реальных пользователей, агрегированный на p75; публичный полевой датасет Google — CrUX, и CrUX p75 — то, что использует ранжирование Search. Они регулярно расходятся, потому что один синтетический прогон не моделирует разнообразие реальных пользователей (старые телефоны, перегруженные сети). Правило: отлаживай в лабе, суди по field. Улучшение в лабе, не двигающее field p75, не помогло реальным пользователям. INP почти не существует в лабе, поэтому CI использует Total Blocking Time как прокси." },
    { q: "Почему надо мерить все три vitals до и после любого изменения, и что требует полная production-наблюдаемость?", a: "Три vitals делят бюджет производительности, поэтому фикс одной может сломать другую: инлайнинг критического CSS помогает render delay у LCP, но раздувает HTML и поднимает TTFB; ленивая загрузка ужимает bundle для INP, но рушит LCP, если зацепит hero; щедрое резервирование места для CLS может вытолкнуть LCP-элемент из видимости и сменить кандидата; тяжёлый интерактивный фреймворк добавляет гидрацию, задирающую ранний INP. Без проверки всех трёх чистый эффект не узнать. Полная наблюдаемость требует обе половины: RUM (библиотека web-vitals на PerformanceObserver, отправляющая атрибутированные LCP/INP/CLS — элемент, разбиение по фазам, цель взаимодействия, сдвигающиеся узлы — с тегами route, класс устройства, страна, релиз) как вердикт field, плюс throttled CI-гейт (Lighthouse CI или трейс Playwright с бюджетами на LCP/TBT/CLS на каждый PR), чтобы ловить регрессии до production. RUM без CI отправляет регрессии пользователям первыми; CI без RUM пропускает отказы по классу устройства и реальным взаимодействиям." },
  ]}
/>

<Recap lang="ru">
Если ты восстановил каждый ответ по памяти, ты держишь хребет юнита: LCP/INP/CLS — полевые метрики на p75 с порогами ≤2.5 с, ≤200 мс, ≤0.1; LCP читают по четырём фазам, а INP — по трём частям, чтобы чинить доминирующую; паттерн INP только в начале — сигнатура гидрации; CLS — это impact × distance по худшему окну 5 с с исключением 500 мс после взаимодействия; вердикт живёт в field (CrUX), лаба — для отладки и CI; и так как все три делят бюджет, каждое изменение мерят против всех них через RUM плюс CI-гейт.
</Recap>
