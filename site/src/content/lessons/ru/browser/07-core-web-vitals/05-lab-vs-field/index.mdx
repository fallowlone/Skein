---
concepts:
- CrUX
- Lighthouse
- TBT
- FCP
- TTFB
- lab-data
- field-data
- DevTools-trace
- p75-percentile
deepensInto:
- 06-tradeoffs-and-observability
estMin: 12
lang: ru
lessonType: topic
level: middle
order: 5
prereqs:
- 01-what-vitals-measure
- 02-lcp-mechanics
- 03-inp-mechanics
slug: 05-lab-vs-field
sources:
- https://web.dev/articles/vitals
- https://developer.chrome.com/docs/crux
- https://developer.chrome.com/docs/devtools/performance
spiral:
- latency
status: ready
summary: Lighthouse — лабораторный инструмент, воспроизводимый и хороший для отладки.
  CrUX — полевые данные реальных пользователей Chrome — и только поле влияет на ранжирование
  в поиске. Понять, какому из них доверять для какого вопроса — значит устранить путаницу.
title: 'Lab vs field: почему они расходятся и как использовать каждый'
track: browser
unit: 07-core-web-vitals
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import TraceScenario from "~/components/pedagogy/TraceScenario.astro";
import MetaphorComplete from "~/components/pedagogy/MetaphorComplete.astro";
import NumberDrill from "~/components/pedagogy/NumberDrill.astro";
import NumbersCard from "~/components/layout/NumbersCard.astro";
import Misconception from "~/components/layout/Misconception.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
PageSpeed Insights показывает LCP 1.8 с в секции Lighthouse — зелёный, хорошо. Секция field data показывает LCP 3.9 с на p75 — красный, плохо. Команда говорит «лаб говорит, мы в порядке». Они ничего не шипают. Через три месяца Search Console всё ещё показывает плохой LCP. Оценка лаба и ранжирование измеряют разные вещи. Понять, какая из них вердикт, меняет всё.
</Hook>

<Crux>Отлаживай в лабе, суди по полю — только CrUX p75 влияет на ранжирование; лабораторные инструменты не моделируют реальных пользователей.</Crux>

<Explanation>

**Lab-данные vs field-данные — два разных вопроса.**

Lab-данные — синтетическое измерение: инструмент как Lighthouse или DevTools загружает страницу один раз, на симулированном устройстве и зажатой сети, в контролируемой среде. Полностью воспроизводимо и точно атрибутирует проблемы — отлично для отладки. Одно устройство, одна сеть, один момент. Если изменить страницу и перезапустить, результат сопоставим.

Field-данные — real-user monitoring: реальные Core Web Vitals, испытываемые реальными посетителями на их реальных устройствах и сетях, агрегированные. Публичный полевой датасет Google — **CrUX** (Chrome User Experience Report). Он агрегирует визиты от пользователей Chrome, давших согласие на отчётность, и именно field p75 из CrUX использует ранжирование в поиске.

Они расходятся постоянно, потому что измеряют разное. Lab-прогон на симулированном быстром устройстве показывает LCP, которого четверть реальных пользователей — на старых телефонах, на перегруженном 4G — никогда не достигает. INP в лабе почти не существует, потому что лабораторные инструменты не скриптуют реалистичные последовательности взаимодействий; lab-прокси — Total Blocking Time (TBT). CLS в лабе близок к реальному, но динамический контент, появляющийся только в реальных сессиях, может быть пропущен.

<NumbersCard
  id="card-lab-vs-field"
  title="Lab vs field — ключевые различия"
  stats={[
    { label: "Lab (Lighthouse, DevTools)", value: "Синтетика, воспроизводимо, отлаживаемо" },
    { label: "Field (CrUX, web-vitals RUM)", value: "Реальные пользователи, устройства, сети" },
    { label: "Что влияет на ранжирование", value: "CrUX field p75" },
    { label: "Lab-прокси для INP", value: "Total Blocking Time (TBT)" },
    { label: "Правило", value: "Отлаживай в лабе; суди по полю" },
  ]}
  data-lesson-visual
/>

<FlowDiagram
  label="Два источника данных измеряют одни и те же три Core Web Vitals: лаб (Lighthouse, DevTools — синтетика) и поле (CrUX, web-vitals RUM — реальные пользователи). Lab-путь питает отладку и CI-гейтинг; только field-путь даёт CrUX p75, который использует ранжирование в поиске."
  hue="var(--d-frontend)"
  nodes={[
    { id: "lab", label: "Лаб", sub: "Lighthouse", col: 0, row: 0 },
    { id: "field", label: "Поле", sub: "CrUX / RUM", col: 0, row: 2 },
    { id: "vitals", label: "Те же 3 vitals", sub: "LCP·INP·CLS", col: 1, row: 1 },
    { id: "debug", label: "Отладка + CI", sub: "регрессии", col: 2, row: 0 },
    { id: "verdict", label: "Вердикт ранжир.", sub: "p75", col: 2, row: 2 },
  ]}
  edges={[
    { from: "lab", to: "vitals", label: "синтетика" },
    { from: "field", to: "vitals", label: "реальные" },
    { from: "vitals", to: "debug" },
    { from: "vitals", to: "verdict" },
  ]}
  caption="Оба источника измеряют одни метрики, но отвечают на разные вопросы. Лаб воспроизводим — поэтому он для отладки и CI-гейтов регрессий; field p75 из CrUX — единственное число, которое использует ранжирование. Отлаживай в лабе, суди по полю."
/>

**Правило: отлаживай в лабе, суди по полю.**

Используйте лаб для воспроизведения и атрибуции проблемы (быстро, детерминированно, легко бисектировать коммиты) и для CI-гейтинга регрессий (Lighthouse CI на каждом PR, быстро, без реальных пользователей). Используйте field p75 как вердикт о том, реально ли страница хороша для пользователей и будет ли она ранжироваться. Lab-улучшение, не сдвинувшее field p75, не помогло пользователям. Только field-число — правда.

**Вспомогательные диагностические метрики.**

Три Core Web Vitals сидят поверх слоя диагностических метрик, которые не оцениваются для ранжирования, но необходимы для их понимания:

- **TTFB** (Time to First Byte) — серверно-сетевая часть. Это пол под LCP: LCP никогда не может быть быстрее TTFB.
- **FCP** (First Contentful Paint) — когда появляется *первый* пиксель контента, в противоположность *самому крупному* у LCP. Большой зазор FCP-to-LCP означает, что страница быстро показывает что-то, но главный контент отстаёт.
- **TBT** (Total Blocking Time) — сумма блокирующей части каждой длинной задачи во время загрузки. Это lab-прокси для INP: истинный INP нельзя измерить в лабе (нужны реальные взаимодействия), поэтому CI-гейты используют TBT вместо.

Знание этих метрик позволяет читать отчёт Lighthouse полностью — Lighthouse никогда не говорит просто «LCP плох», он показывает TTFB и FCP под ним, чтобы видеть, где в цепочке ушло время.

**Чтение DevTools performance-трассы.**

Откройте DevTools, панель Performance, запишите загрузку страницы и взаимодействие. Трасса показывает несколько дорожек:

- Дорожка *Timings*: маркеры FCP, LCP и DCL вертикальными линиями. Hover по маркеру LCP называет элемент.
- Дорожка *Frames*: каждый отрисованный кадр; layout shifts появляются как красные маркеры — кликните, чтобы увидеть сдвинувшиеся узлы.
- Дорожка *Main*: flame chart работы главного потока; длинные задачи флагуются красным углом.
- Дорожка *Interactions*: click/tap-события, разбитые на input delay, processing и presentation delay — цветокодированные — так что видно, какая часть доминирует без догадок.

Воркфлоу INP: взаимодействуйте во время записи, найдите взаимодействие в дорожке Interactions, прочитайте трёхчастный сплит. Воркфлоу LCP: найдите маркер LCP в дорожке Timings, наведите для идентификации элемента, затем посмотрите на network waterfall выше, чтобы увидеть, когда ресурс был запрошен vs скачан vs когда элемент отрисовался.

<Inset kind="why" lang="ru">
Почему используется p75, а не медиана? Медианная производительность тянется вниз быстрыми выбросами — разработчики на быстром Wi-Fi, закешированные повторные визиты. 75-й перцентиль фокусирует внимание на медленной четверти реальных пользователей: тех на старых телефонах, мобильных сетях или слабых процессорах. Изменение, улучшающее медиану, но не p75, помогло уже-быстрым пользователям и ничего не сделало для тех, кому помощь нужна больше. p75 — порог, делающий целью оптимизации хвост распределения, а не его центр. Важное следствие: иногда p75 плох не потому что страница в целом медленная, а потому что один сегмент (одна страна, один класс устройств) тянет хвост — поэтому RUM-данные нужно резать по сегментам, а не смотреть один агрегат по всему трафику.
</Inset>

<TraceScenario
  id="mid-trace-1"
  pieceSlug="05-lab-vs-field"
  lang="ru"
  scenario="PageSpeed Insights показывает LCP 1.8 с в секции Lighthouse (lab) — хорошо — но секция полевых данных показывает LCP 3.9 с на p75 — плохо. Команда говорит «лаб говорит, мы в порядке». Кто прав?"
  steps={[
    { prompt: "Полевые данные — реальный вердикт — лаб прогнался на быстром симулированном устройстве; реальные пользователи на p75 на медленных телефонах и сетях, и 3.9 с — то, что они реально испытывают", reveal: "Верно. Lab-число — один синтетический прогон на симулированном устройстве среднего класса с щедрой зажатой сетью. Field-число агрегировано с реальных пользователей Chrome — и p75 означает, что четверть реальных визитов на 3.9 с или выше. Ранжирование Search использует field (CrUX) данные. Команда должна воспроизвести медленные условия (тяжелее CPU-троттл, медленнее сеть в DevTools), пока лаб не покажет проблему, затем чинить." },
    { prompt: "Лаб прав, потому что он воспроизводим", reveal: "Воспроизводимый не значит репрезентативный. Лаб воспроизводим именно потому, что фиксирует одно устройство и сеть — но реальные пользователи не одно устройство. Воспроизводимость делает лаб хорошим для отладки, не для вердикта." },
    { prompt: "Оба неправы; важен только TTFB", reveal: "TTFB — одна фаза LCP, не замена ему. И оба числа — валидные измерения, отвечающие на разные вопросы." },
    { prompt: "Усредните оба: 2.85 с", reveal: "Усреднять lab-число и field-перцентиль бессмысленно — это разные виды измерений. Field p75 стоит сам по себе как испытываемое пользователем и значимое для ранжирования значение." },
  ]}
/>

<MetaphorComplete
  id="mid-metaphor-1"
  pieceSlug="05-lab-vs-field"
  lang="ru"
  setup="Google публикует публичный датасет Core Web Vitals реальных пользователей, агрегированный из реальных посещений Chrome, и ранжирование в поиске основано на нём — не на каком-либо лабораторном тесте. Как называется этот полевой датасет?"
  accepted={["crux", "chrome user experience report", "the chrome ux report", "хром user experience репорт"]}
  canonical="CrUX"
  explanation="CrUX — Chrome User Experience Report — публичный датасет Google с полевыми метриками, агрегированными с реальных пользователей Chrome, давших согласие на отчётность. Это источник «полевых данных», которые видите в PageSpeed Insights и Search Console, и именно то, что ранжирование реально использует. Поэтому отличный Lighthouse-score не гарантирует хорошего ранжирования: Lighthouse — лаб, CrUX — поле, и только поле считается для вердикта."
/>

<NumberDrill
  id="mid-drill-1"
  pieceSlug="05-lab-vs-field"
  lang="ru"
  question="«Хороший» INP требует задержки взаимодействия не более скольки миллисекунд на 75-м перцентиле?"
  expected={200}
  tolerance={0}
  unit="мс"
  explanation="200 мс — «хороший» порог INP на p75. 200–500 мс — «нужно улучшение»; больше 500 мс — «плохо». Цепочка обратно к event loop прямая: поддержание каждой задачи под порогом длинной задачи в 50 мс — это то, что держит input delay малым, а INP под 200 мс."
/>

<Misconception id="mc-lab-vs-field">
**Миф:** Хороший Lighthouse-score означает, что CWV хороши для ранжирования.

**Реальность:** Lighthouse — лабораторный тест на одном симулированном устройстве. Ранжирование использует полевые данные CrUX — реальные пользователи на p75. Зелёный lab-результат и плохой field p75 регулярно сосуществуют.
</Misconception>

</Explanation>

<KeyTakeaway>Лабораторные инструменты (Lighthouse, DevTools) — для отладки и CI-гейтинга; CrUX field p75 — вердикт ранжирования в поиске. Lab-фикс, не сдвинувший field p75, не помог пользователям.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="05-lab-vs-field-retrieval"
  lang="ru"
  questions={[
    { q: "Объясните различие lab vs field и дайте правило, когда использовать каждый.", a: "Lab: синтетический тест, одно устройство, одна сеть, воспроизводимо — используйте для отладки (атрибуция проблемы, бисект коммитов) и CI-гейтинга регрессий (быстро, без пользователей). Field: реальные пользователи, реальные устройства, реальные сети, агрегировано на p75 — CrUX, который использует ранжирование Search. Правило: отлаживай в лабе, суди по полю. Lab-улучшение, не сдвинувшее field p75, не помогло реальным пользователям." },
    { q: "Почему Total Blocking Time используется как INP-прокси в CI, а не измеряется INP напрямую?", a: "Истинный INP требует реальных пользовательских взаимодействий — CI-прогон загружает страницу синтетически без симуляции реалистичных последовательностей кликов. TBT — сумма блокирующей части длинных задач во время загрузки, что коррелирует с input delay (занятый поток задерживает вводы). TBT измеримо в лабе, предсказывает INP и может гейтировать CI. Это прокси, не идеальная замена — страница может иметь низкий TBT, но плохой INP от медленных обработчиков, которых TBT не видит." },
    { q: "В DevTools performance-трассе что показывает дорожка Interactions для медленного взаимодействия, и как её использовать для диагностики INP?", a: "Дорожка Interactions показывает каждый click/tap/keypress как полосу, разделённую на три цветокодированных сегмента: input delay (поток был занят), processing time (обработчик выполнялся) и presentation delay (стиль/компоновка/отрисовка после обработчика). Найдите медленное взаимодействие, прочитайте, какой сегмент доминирует, затем посмотрите на Main flame chart для этого временного диапазона, чтобы идентифицировать конкретную длинную задачу (input delay) или тяжёлый рендер (presentation delay)." },
  ]}
/>

<Recap lang="ru">
Lab-данные — из Lighthouse или DevTools — синтетические: одно устройство, одна сеть, воспроизводимо, отлично для отладки и CI-гейтинга регрессий. Field-данные — CrUX — с реальных пользователей Chrome на p75, и именно их использует ранжирование Search. Они расходятся постоянно, потому что реальное разнообразие пользователей (более медленные телефоны, перегруженные сети) не захватывается одним симулированным прогоном. INP почти не существует в лабе, потому что лабораторные инструменты не симулируют реальные взаимодействия; lab-прокси — Total Blocking Time. Правило: отлаживай в лабе, суди по полю. TTFB, FCP и TBT — вспомогательные диагностические метрики под тремя Core Web Vitals; знание их позволяет читать отчёт Lighthouse и отслеживать, где в цепочке ушло время. Lab-улучшение, не сдвинувшее field p75, не помогло пользователям; только field-число — реальный вердикт.
</Recap>
