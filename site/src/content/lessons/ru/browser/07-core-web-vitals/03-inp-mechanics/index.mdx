---
concepts:
- INP
- input-delay
- processing-time
- presentation-delay
- long-task
- hydration-INP
- scheduler.yield
- startTransition
- LoAF
deepensInto:
- 06-tradeoffs-and-observability
estMin: 14
lang: ru
lessonType: topic
level: middle
order: 3
prereqs:
- 01-what-vitals-measure
- browser/01-event-loop/01-loop-model
- browser/06-ssr-vs-ssg/03-hydration-cost
slug: 03-inp-mechanics
sources:
- https://web.dev/articles/inp
- https://web.dev/articles/optimize-inp
- https://developer.chrome.com/docs/devtools/performance
spiral:
- latency
status: ready
summary: Interaction to Next Paint раскладывается на три части — и плохой INP только
  в первые секунды указывает прямо на длинную задачу гидратации, а не на обработчики
  событий.
title: 'INP: input delay, processing, presentation'
track: browser
unit: 07-core-web-vitals
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import NumbersCard from "~/components/layout/NumbersCard.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
Страница результатов поиска ощущается быстрой после нескольких секунд — но в первые 3 секунды после загрузки каждый тап по кнопке занимает почти полсекунды. Сами обработчики работают 8 мс. Остальное время — не в обработчике вообще. Это ловушка INP: симптом в метрике, но причина в архитектуре.
</Hook>

<Crux>INP = input delay + processing time + presentation delay — ранний плохой INP почти всегда длинная задача гидратации, блокирующая поток.</Crux>

<Explanation>

**Что измеряет INP — и что заменило FID.**

Если ты когда-нибудь гонялся за «тормозящей» страницей и не находил ничего плохого в обработчиках событий — INP это та метрика, которая называет то, что ты пропускал. FID (First Input Delay, задержка первого ввода) был предшественником INP и замерял только задержку до старта обработчика первого ввода. Interaction to Next Paint заменила его как Core Web Vital в марте 2024. FID измерял только задержку до старта обработчика *первого* ввода — она пропускала весь остальной жизненный цикл страницы и пропускала взаимодействия, которые были медленными потому что сам обработчик или результирующий рендер были тяжёлыми. INP измеряет полную задержку input-to-paint *каждого* клика, тапа и нажатия клавиши и репортит высокий перцентиль (фактически худший, с малой поблажкой на страницах с многими взаимодействиями). Задержка одного взаимодействия: от момента ввода пользователя до следующего кадра, видимо отражающего изменение. Хорошо: ≤200 мс на p75.

**Три части задержки одного взаимодействия.**

INP любого взаимодействия раскладывается на три последовательных части:

1. **Input delay** — время, которое событие ввода ждёт, прежде чем его обработчик вообще сможет стартовать, потому что главный поток занят другой задачей. Это урок event loop напрямую: длинная задача в полёте задерживает ввод. Задача может быть JS, пересчётом стилей или компоновкой — чем угодно, удерживающим поток.

2. **Processing time** — сколько реально работают обработчики событий. Для большинства взаимодействий на хорошо написанной странице это 10–30 мс. Если 200 мс — обработчик и есть проблема.

3. **Presentation delay** — время после окончания обработчиков, пока браузер прогоняет стили, компоновку, отрисовку и композитинг и кладёт результат на экран. Обработчик, запускающий ре-рендер 5 000 узлов, имеет большой presentation delay, даже если логика обработчика была быстрой.

Плохой INP всегда одно из этих трёх. Назвать, какая часть доминирует — вся диагностика: фикс input delay (разбить длинные задачи) полностью отличается от фикса presentation delay (сократить DOM-обновление), и применить не тот — значит ничего не изменить.

<NumbersCard
  id="card-inp-parts"
  title="Разбивка задержки INP (одно взаимодействие)"
  stats={[
    { label: "Задержка ввода", value: "Поток занят — длинная задача в полёте" },
    { label: "Время обработки", value: "Выполнение обработчика событий" },
    { label: "Задержка отрисовки", value: "Стиль + компоновка + отрисовка + композитинг" },
    { label: "Хороший порог INP (p75)", value: "≤200 мс" },
    { label: "Порог длинной задачи (держит input delay малым)", value: "50 мс" },
  ]}
  data-lesson-visual
/>

<FlowDiagram
  label="Задержка INP одного взаимодействия идёт тремя последовательными частями: input delay, пока поток занят, затем processing time в обработчике, затем presentation delay, пока браузер отрисовывает результат."
  perRow={3}
  hue="var(--d-frontend)"
  nodes={[
    { id: "input", label: "Задержка ввода", sub: "поток занят" },
    { id: "processing", label: "Обработка", sub: "обработчик" },
    { id: "presentation", label: "Отрисовка", sub: "рендер+пейнт" },
  ]}
  edges={[
    { from: "input", to: "processing", label: "тап → обработчик" },
    { from: "processing", to: "presentation", label: "→ next paint" },
  ]}
  caption="От тапа до следующей видимой отрисовки. Плохой INP доминируется ровно одной из этих трёх: input delay (длинная задача, часто гидратация, блокирует поток), processing (медленный обработчик) или presentation (тяжёлый рендер). Назвать доминирующую часть — вся диагностика."
/>

**Оптимизация каждой части.**

*Input delay:* Перестаньте слать длинные задачи. Разбивайте работу на куски под 50 мс, дефер-ите несрочный JS, и помните, что гидратация — одна большая длинная задача рано в жизни страницы. Используйте `scheduler.yield()` между кусками, чтобы вернуть управление браузеру.

*Processing time:* Держите обработчики маленькими. Если обработчик должен делать тяжёлую работу, делайте минимум синхронно для обновления UI, затем уступайте и продолжайте — или перенесите чистое вычисление в Worker. Мощный паттерн: обновить видимое состояние синхронно и обернуть дорогую работу ниже по потоку в `startTransition`, чтобы отрисовка, которую ждёт пользователь, не блокировалась ею.

*Presentation delay:* Избегайте принудительной синхронной компоновки в обработчике (чтение layout-свойства после DOM-записи принуждает браузер немедленно перезапустить компоновку). Держите обновления DOM малыми — обработчик, перерендеривающий 5 000 узлов, имеет тяжёлый presentation delay независимо от скорости логики обработчика.

**Окно гидратации — это ловушка INP.**

Самый частый INP-провал на server-rendered сайте — взаимодействия, происходящие во время гидратации. Страница отрисована, пользователь видит кнопку и тапает её — но главный поток выполняет длинную задачу гидратации. Обработчик тапа может ещё не быть привязан, и даже когда привяжется, ввод стоит в очереди за остатком гидратации. INP записывает большую задержку для того раннего взаимодействия.

Паттерн характерный: INP плох *только* для взаимодействий в первые несколько секунд, потом нормален. Если обработчики были причиной, INP был бы плохим для всех взаимодействий. Ограниченный по времени паттерн указывает прямо на гидратацию. Каждый приём, сокращающий гидратацию — Server Components, острова, выборочная и прогрессивная гидратация, code-splitting — одновременно оптимизация INP.

<Inset kind="why" lang="ru">
Long Animation Frames (LoAF) — браузерный API, сопоставляющий медленные взаимодействия со скриптами, вызвавшими их. Для кадра, в который попало медленное взаимодействие, LoAF даёт массив скриптов, выполнившихся в нём — с URL исходников и именами функций. Комбинирование LoAF с INP event timing entries (дающими разбивку input delay / processing / presentation) превращает «p75 INP 340 мс» в «функция поиска на search.js:88 — доминирующий скрипт в медленных кадрах». Именно это делает field INP-числа actionable.
</Inset>

<Quiz
  id="mid-quiz-1"
  pieceSlug="03-inp-mechanics"
  lang="ru"
  question="У взаимодействия INP 220 мс. Сам обработчик работает 8 мс. Где остальное время скорее всего?"
  choices={[
    { label: "Input delay и/или presentation delay — поток был занят, когда пришёл тап, или результирующий рендер был тяжёлым", correct: true },
    { label: "Обработчик в 8 мс — вся проблема", misconception: "8 мс processing не могут произвести 220 мс INP. Остальные ~212 мс — input delay (занятый поток) плюс presentation delay (тяжёлый рендер)." },
    { label: "INP всегда округляется до 220 мс", misconception: "INP — реально измеренная задержка, не округлённая константа. 220 мс — фактическое время input-to-paint того взаимодействия." },
    { label: "Сеть была медленной", misconception: "INP измеряет задержку взаимодействия на главном потоке, не сеть. Медленная сеть напрямую не добавляется к INP." },
  ]}
/>

<Quiz
  id="mid-quiz-2"
  pieceSlug="03-inp-mechanics"
  lang="ru"
  question="INP страницы плох только для взаимодействий в первые ~3 секунды, потом нормален. Что скорее всего причина?"
  choices={[
    { label: "Гидратация — ранние взаимодействия встают в очередь за длинной задачей гидратации; как только гидратация заканчивается, поток свободен", correct: true },
    { label: "Обработчики событий медленные", misconception: "Если обработчики были причиной, INP был бы плохим для всех взаимодействий, не только ранних. Ограниченный по времени паттерн указывает на гидратацию." },
    { label: "Сервер медленный", misconception: "Задержка сервера влияет на TTFB и LCP, не на задержку взаимодействия на главном потоке, которую измеряет INP. И это не создало бы ограниченного по времени паттерна INP." },
    { label: "Картинки слишком большие", misconception: "Размер картинок влияет на LCP. Они не блокируют главный поток для взаимодействий — это задача JS/гидратации." },
  ]}
/>

<Quiz
  id="mid-quiz-3"
  pieceSlug="03-inp-mechanics"
  lang="ru"
  question="Страница результатов поиска имеет плохой INP — взаимодействия в первые 3 секунды медленные, потом нормальные. Что является основным фиксом?"
  choices={[
    { label: "Сократить стоимость гидратации — code-split, Server Components, гидрировать меньше компонентов", correct: true },
    { label: "Оптимизировать код самого обработчика поиска", misconception: "Ускорение обработчика уменьшает processing time — полезно — но если INP нормален после 3 секунд, обработчик уже быстр после освобождения потока. Ранняя медленность — input delay от гидратации." },
    { label: "Добавить spinner при тапе", misconception: "Spinner — обратная связь, не фикс — взаимодействие всё ещё медленное, INP всё равно записывает задержку." },
    { label: "Предзагрузить ответ search API", misconception: "Предзагрузка данных влияет на скорость появления результатов, не на задержку взаимодействия на главном потоке, которую измеряет INP." },
  ]}
/>

</Explanation>

<KeyTakeaway>INP делится на input delay (занятый поток), processing (обработчик) и presentation (стоимость рендера). Плохой INP только в начале — это гидратация блокирует поток; сокращайте что гидрирует, а не обработчики.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="03-inp-mechanics-retrieval"
  lang="ru"
  questions={[
    { q: "Назовите три части задержки INP одного взаимодействия и что делает каждую высокой.", a: "Input delay: главный поток занят длинной задачей, когда приходит ввод — гидратация, неразбитый JS. Processing time: сам обработчик медленный — дорогое вычисление, большая синхронная работа. Presentation delay: результирующее обновление DOM большое или вызывает принудительную синхронную компоновку — стиль/компоновка/отрисовка после обработчика тяжёлая." },
    { q: "Как гидратация создаёт ловушку INP, и какой характерный паттерн диагностики?", a: "Гидратация выполняется как большая длинная задача рано в жизненном цикле server-rendered страницы. Пользователь тапает кнопку, пока гидратация идёт — ввод ждёт за длинной задачей (input delay). INP записывает большую задержку. Паттерн: INP плох только в первые секунды, потом нормален. Как только гидратация заканчивается, поток свободен и обработчики работают быстро. Этот диагноз указывает на гидратацию, а не код обработчиков." },
    { q: "Что делает scheduler.yield() для INP и когда его использовать?", a: "scheduler.yield() возвращает управление браузеру в середине задачи, позволяя обработать ожидающие события ввода и рендеринг до продолжения работы. Используйте, разбивая большое вычисление на куски — уступайте между кусками, чтобы каждый был меньше 50 мс и любой тап, пришедший между кусками, обслуживался немедленно, а не ждал всю задачу." },
  ]}
/>

<Recap lang="ru">
INP измеряет полную задержку input-to-paint каждого взаимодействия на протяжении всего жизненного цикла страницы и репортит высокий перцентиль — это куда репрезентативнее FID, измерявшего только задержку очереди первого ввода. Задержка одного взаимодействия раскладывается на input delay (занятый поток), processing time (обработчик) и presentation delay (стоимость рендера после обработчика). Плохой INP всегда доминируется одним из трёх, и фикс разный для каждого. Характерный паттерн «только в первые секунды» — подпись гидратации: длинная задача гидратации блокирует поток, не сами обработчики. Каждый приём, сокращающий охват гидратации (Server Components, острова, code-splitting), одновременно является INP-фиксом. Хороший порог ≤200 мс на p75; поддержание каждой задачи под 50 мс — это механизм, держащий input delay малым и INP под порогом. Теперь, когда встретишь жалобу на задержку взаимодействия, первый вопрос: когда это происходит — всегда или только в первые секунды? Один этот вопрос покажет, смотреть ли на обработчики или на гидратацию.
</Recap>
