---
concepts:
- metric-tradeoffs
- RUM
- web-vitals-library
- PerformanceObserver
- LoAF
- INP-attribution
- LCP-attribution
- CI-gate
- soft-navigation
- SPA-vitals
deepensInto: []
estMin: 16
lang: ru
lessonType: topic
level: senior
order: 6
prereqs:
- 02-lcp-mechanics
- 03-inp-mechanics
- 04-cls-causes-and-fixes
- 05-lab-vs-field
slug: 06-tradeoffs-and-observability
sources:
- https://web.dev/articles/vitals
- https://web.dev/articles/optimize-inp
- https://developer.chrome.com/docs/crux
- https://web.dev/articles/optimize-lcp
spiral:
- latency
- statefulness
status: ready
summary: Три vital разделяют бюджет производительности — фикс одной может сломать
  другую. Полная production-observability требует web-vitals RUM для field-вердикта
  и throttled CI-гейта для регрессий — ни одного в отдельности недостаточно.
title: Трейдоффы метрик, RUM-атрибуция и цикл CI+поле
track: browser
unit: 07-core-web-vitals
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DebugLog from "~/components/pedagogy/DebugLog.astro";
import DesignPrompt from "~/components/pedagogy/DesignPrompt.astro";
import RFCQuiz from "~/components/pedagogy/RFCQuiz.astro";
import NumbersCard from "~/components/layout/NumbersCard.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
Команда фиксит LCP инлайнингом полного блока критического CSS — render delay LCP падает на 400 мс. Но TTFB растёт на 60 мс из-за того, что HTML теперь на 40 КБ больше. Другая команда lazy-load-ит каждую не-hero картинку для сокращения бандла — LCP остаётся нормальным, но hero случайно попал и LCP регрессирует на 800 мс. Ещё одна команда шипает полный SPA и получает зелёные vitals при первой загрузке — но каждый клиентский переход маршрута невидим для метрик. Один бюджет, три метрики, четыре команды и ни одной ручки.
</Hook>

<Crux>LCP, INP и CLS разделяют бюджет производительности — каждое изменение тратит из него; измеряйте все три до и после, никогда одну в изоляции.</Crux>

<Explanation>

**Метрики — не независимые ручки.**

Фикс одной Core Web Vital может навредить другой, и senior-ход — видеть систему, а не отдельные метрики. Частые трейдоффы:

- **Инлайнинг большого блока критического CSS** помогает render delay LCP (нет render-blocking запроса), но раздувает HTML, вредя TTFB. Итоговый эффект зависит от скорости соединения и размера HTML.
- **Lazy-load всего** для сокращения бандла помогает INP (меньше JS парсить и выполнять), но, применённый случайно к hero-картинке, добавляет большой load delay и рушит LCP.
- **Резервирование щедрого места для CLS** — большие `min-height` контейнеры для рекламы — может опустить LCP-элемент ниже сгиба, и он перестаёт быть LCP-кандидатом. Иногда это нормально; иногда нет.
- **Отгрузка большого интерактивного фреймворка** ради отзывчивого ощущения добавляет гидратацию — одну большую длинную задачу, взрывающую INP для ранних взаимодействий.

У этих трейдоффов общий паттерн: каждая оптимизация берёт из одного бюджета производительности, и выигрыш в одной метрике часто оборачивается потерей в другой. Единой ручки нет. Дисциплина: измерять все три до и после любого изменения, в поле где возможно, в throttled-лабе как минимум. Изменение, улучшающее одну метрику за счёт другой, может быть оправдано или нет — но знать это нельзя без взгляда на все три.

**Атрибуция INP в поле: от метрики к строке кода.**

Полевое число INP само по себе не actionable — это задержка без прикреплённой причины. Цепочка атрибуции:

1. `PerformanceObserver`, подписанный на `event` записи, даёт вам для каждого взаимодействия разбивку input delay / processing time / presentation delay.
2. Long Animation Frames (`long-animation-frame` записи через `PerformanceObserver`) дают для кадра, в который попало медленное взаимодействие, массив выполнившихся скриптов — с URL исходников, именами функций и длительностями.
3. Совмещайте: INP с высоким input-delay → найдите длинную задачу в записи LoAF; с высоким processing → обработчик, атрибутированный LoAF; с высоким presentation delay → тяжёлый рендер или принудительная синхронная компоновка в обработчике.

Весь смысл встройки LoAF в RUM — превратить «p75 INP 340 мс» в «функция фильтра поиска на search.js:88 — доминирующий скрипт в медленных кадрах». Это actionable.

**Атрибуция LCP в поле.**

Колбэк `onLCP` библиотеки web-vitals доставляет не только время LCP, но и *элемент* (чтобы знать, что браузер выбрал как LCP), *URL* ресурса (если картинка) и четырёхчастную разбивку: TTFB, load delay, load time, render delay. Логирование этого в телеметрию означает, что регрессия «LCP вырос с 1.9 с до 3.8 с» немедленно показывает, какая фаза выросла — был ли это TTFB (медленный деплой), load delay (случайный `loading="lazy"`), load time (неоптимизированная картинка) или render delay (новый render-blocking скрипт)?

<NumbersCard
  id="card-rum-attribution"
  title="Атрибутированный field-пример из web-vitals RUM"
  stats={[
    { label: "LCP-элемент", value: "img.hero" },
    { label: "LCP фаза: load time", value: "3510 мс (доминирует — JPEG 4 МБ)" },
    { label: "LCP фазы: TTFB + delay + render", value: "610 мс в сумме" },
    { label: "INP", value: "38 мс — хорошо" },
    { label: "CLS", value: "0.02 — хорошо" },
  ]}
  data-lesson-visual
/>

<FlowDiagram
  label="Полевой пайплайн атрибуции превращает сырую vital в строку кода: происходит реальное взаимодействие или загрузка, PerformanceObserver захватывает запись плюс Long Animation Frames, библиотека web-vitals добавляет атрибуцию, телеметрия тегирует по маршруту и устройству, а field p75 становится actionable-вердиктом."
  perRow={5}
  hue="var(--d-frontend)"
  nodes={[
    { id: "event", label: "Реальное событие", sub: "load / tap" },
    { id: "observer", label: "PerfObserver", sub: "+ LoAF" },
    { id: "attribution", label: "web-vitals", sub: "элемент + фаза" },
    { id: "telemetry", label: "RUM", sub: "маршрут·устр." },
    { id: "verdict", label: "Поле p75", sub: "к действию" },
  ]}
  edges={[
    { from: "event", to: "observer" },
    { from: "observer", to: "attribution" },
    { from: "attribution", to: "telemetry" },
    { from: "telemetry", to: "verdict" },
  ]}
  caption="От метрики к строке кода. Сырая vital — просто задержка; PerformanceObserver плюс LoAF и библиотека web-vitals прикрепляют элемент, четырёхфазную разбивку и медленный скрипт, так что «p75 INP 340 мс» становится «фильтр поиска на search.js:88 доминирует в медленных кадрах». RUM — field-вердикт; throttled CI-гейт ловит регрессии до отгрузки."
/>

**Цикл RUM и CI — оба нужны.**

Полный production-observability-стек состоит из двух половин.

*RUM*: отгрузить библиотеку `web-vitals` (или аналог), использующую `PerformanceObserver` для захвата LCP, INP и CLS ровно так, как Chrome их оценивает, плюс атрибуцию (элемент, разбивка фаз, цель взаимодействия, сдвигающиеся узлы). Отправлять в телеметрию с тегами маршрут, класс устройства, страна, релиз. Это реальный вердикт — ловит регрессии, которые лаб никогда не раскроет, особенно регрессии класса устройств и взаимодействия, которые запускают только реальные пользователи.

*CI*: синтетический гейт — Lighthouse CI или Playwright-трасса — на каждом PR, throttled под реалистичное mid-tier устройство, с бюджетами по LCP, total blocking time (lab-прокси для INP) и CLS, падающий на регрессии.

Ни одного в отдельности недостаточно. Лаб без RUM шипает регрессии, которые раскрывают только реальные устройства. RUM без CI-гейта означает, что каждая регрессия достигает production до того, как кто-то её замечает.

**Разрыв soft-navigation — почему SPA-vitals требуют явной инструментации.**

Core Web Vitals разрабатывались для полных загрузок страниц. В SPA первая загрузка имеет реальный LCP — но последующие клиентские переходы маршрутов («мягкие навигации») исторически не имели измерения LCP вообще, потому что не срабатывает событие document load. Для пользователя мягкая навигация ощущается ровно как загрузка страницы — он кликнул ссылку и ожидает быстрого нового контента — но метрика этого не видела.

Chrome шипает поддержку soft-navigation для атрибуции LCP и других vitals клиентским переходам маршрутов, но покрытие ещё созревает. Следствие: для SPA не предполагайте, что история vitals завершена только потому что числа первой загрузки зелёные. Быстрая первая загрузка + вялые переходы маршрутов — реальный, частый и исторически недоизмеренный провал. Инструментируйте мягкие навигации явно с `PerformanceObserver` soft-navigation записями или собственными RUM-марками на `router.beforeEach` / событиях смены маршрута.

<Inset kind="why" lang="ru">
CLS session window (худший 5-секундный кластер вместо пожизненной суммы) — конкретный пример того, как спецификация эволюционирует к репрезентации реального пользовательского опыта, а не просто чистого инженерного измерения. Оригинальный lifetime-sum CLS несправедливо наказывал долго живущие страницы и infinite scroll — сдвиг, произошедший через четыре минуты после загрузки, считался так же как на второй секунде. Session window фокусирует метрику на концентрированном плохом поведении: всплеск сдвигов при перезагрузке рекламы или пакет картинок без размеров грузится. Session window более репрезентативен для того, что пользователь реально замечает в контексте.
</Inset>

<DebugLog
  id="sr-debug-1"
  pieceSlug="06-tradeoffs-and-observability"
  lang="ru"
  label="web-vitals RUM — атрибутированный field-пример"
  output={`[LCP] value: 4120 ms  rating: poor
  element: img.hero
  url: /assets/hero-original.jpg  (3.8 MB, JPEG, 4000x3000)
  phase split: ttfb 280ms | loadDelay 90ms | loadTime 3510ms | renderDelay 240ms

[INP] value: 38 ms  rating: good
[CLS] value: 0.02  rating: good`}
  outputLang="log"
  question="Прочитайте разбивку по фазам и детали элемента — какая фаза доминирует, какова первопричина, и каков фикс? Что команде НЕ делать, и почему?"
  hint="Сравните четыре числа фаз. Одна на порядок больше остальных. Затем посмотрите на размер и размеры картинки."
  answer="Разбивка по фазам решающая: loadTime 3510 мс — ~85% от 4120 мс LCP — тогда как TTFB, loadDelay и renderDelay вместе меньше 700 мс. Доминирующая фаза — resource load time: сама LCP-картинка скачивается 3.5 секунды. Детали элемента объясняют почему: hero-original.jpg — 3.8 МБ, сырой JPEG 4000x3000 — необработанное фото в полном разрешении, намного больше, чем нужно любому вьюпорту. Фикс: отдавать корректно отмасштабированную картинку в AVIF или WebP (типично на 30-50% меньше JPEG при том же качестве), с srcset для разных вьюпортов. Что НЕ делать: не добавлять preload-подсказку или fetchpriority='high'. LoadDelay уже только 90 мс — обнаружение не проблема. Фиксить не ту фазу — классическая LCP-ошибка; разбивка по фазам говорит, какую чинить."
/>

<RFCQuiz
  id="sr-rfc-1"
  pieceSlug="06-tradeoffs-and-observability"
  lang="ru"
  question="Какой браузерный API является основой для измерения LCP, INP, layout shifts и Long Animation Frames в real-user monitoring?"
  choices={[
    { rfc: "PerformanceObserver", title: "Подписывается на типы записей производительности — largest-contentful-paint, event, layout-shift, long-animation-frame", correct: true },
    { rfc: "console.time", title: "Ручной секундомер для отладки — не источник стандартизированных vitals-записей" },
    { rfc: "Date.now", title: "Настенное время; не может наблюдать записи отрисовки, layout-shift или взаимодействий" },
    { rfc: "ResizeObserver", title: "Наблюдает изменения размера элементов — не тайминг отрисовки или задержку взаимодействий" },
  ]}
/>

<DesignPrompt
  id="sr-design-1"
  pieceSlug="06-tradeoffs-and-observability"
  lang="ru"
  prompt="Спроектируйте стратегию Core Web Vitals для медиасайта: страницы статей с hero-картинкой, рекламой, эмбедами и секцией комментариев. Добейтесь хорошего LCP, INP и CLS на p75 в поле, и держите их зелёными со временем."
  constraints={[
    "Hero-картинка — LCP-элемент на каждой статье.",
    "Реклама и социальные эмбеды загружаются поздно и в тело статьи.",
    "Секция комментариев интерактивна и ниже сгиба.",
    "Сайт server-rendered и гидрируется.",
    "Цели: LCP ≤2.5 с, INP ≤200 мс, CLS ≤0.1 — все на field p75.",
    "Регрессии должны быть пойманы до production.",
  ]}
  canonical="LCP: поместить hero-картинку в начальный HTML как обычный <img> с явными width/height и fetchpriority='high'; никогда не lazy-load-ить; отдавать отмасштабированной в AVIF/WebP через srcset; держать TTFB низким с CDN и, если SSR, убрать медленные загрузки данных с пути рендера. CLS: давать hero и каждой картинке в теле явные размеры или aspect-ratio; оборачивать каждый рекламный и embed-слот в контейнер с фиксированным min-height; нейтрализовать reflow веб-шрифта через size-adjust или font-display: optional; никогда не вставлять контент над читаемым. INP: текст статьи почти не нуждается в JS — сделать в основном статическим/Server Components; секция комментариев — основная интерактивная поверхность, поэтому сделать островом, гидрирующимся при скролле во вьюпорт (ниже сгиба), а не частью жадной полностраничной гидратации, блокирующей ранние тапы. Observability: отгрузить web-vitals с атрибуцией, отправлять LCP/INP/CLS в RUM с тегами маршрут, класс устройства, страна, релиз — это field-вердикт. Добавить Lighthouse CI-гейт на каждый PR, throttled под mid-tier устройство, с бюджетами LCP, TBT, CLS. Ни одного в отдельности недостаточно."
  keyPoints={[
    "Hero LCP: в HTML, с размерами, fetchpriority='high', никогда lazy, современный формат.",
    "CLS: резервировать место для всего, что грузится поздно — картинки, реклама, эмбеды, шрифты.",
    "INP: минимизировать гидратацию — статика/Server Components для статьи, комментарии как скролл-гидрируемый остров.",
    "RUM с web-vitals + атрибуцией — field-вердикт; throttled CI-гейт ловит регрессии до production.",
    "Измерять все три до и после каждого изменения — фикс одной может сломать другую.",
  ]}
/>

<Quiz
  id="sr-quiz-1"
  pieceSlug="06-tradeoffs-and-observability"
  lang="ru"
  question="Почему страница может показывать отличный Lighthouse-score, но всё равно быть флагнутой за плохие Core Web Vitals в Search Console?"
  choices={[
    { label: "Lighthouse — лабораторный тест на симулированном устройстве; Search Console репортит field data от реальных пользователей (CrUX) — только field data влияет на ранжирование", correct: true },
    { label: "Search Console использует более строгую версию Lighthouse", misconception: "Search Console не запускает Lighthouse вообще для отчёта по vitals — он репортит CrUX field data. Lab и field — разные источники данных." },
    { label: "Lighthouse-score закеширован и устарел", misconception: "Расхождение — не сталость, а lab vs field. Свежий прогон Lighthouse на быстром симулированном устройстве всё равно может расходиться с реальным p75." },
    { label: "В Search Console баг", misconception: "Это ожидаемое поведение, не баг. Lab и field измеряют разное; field (CrUX) p75 — значимый для ранжирования вердикт." },
  ]}
/>

</Explanation>

<KeyTakeaway>LCP, INP, CLS разделяют бюджет — фиксите все три или создаёте скрытые регрессии. RUM даёт field-вердикт (PerformanceObserver + web-vitals); throttled CI Lighthouse ловит регрессии до production. Ни одно не заменяет другое.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="06-tradeoffs-and-observability-retrieval"
  lang="ru"
  questions={[
    { q: "LCP страницы плох. Опишите, как диагностировать через разбивку фаз, и почему чтение разбивки — ключевой шаг.", a: "LCP раскладывается на четыре фазы: TTFB, resource load delay, resource load time, element render delay. Получите разбивку из LCP attribution библиотеки web-vitals или DevTools-трассы. Прочитайте, какая фаза доминирует. TTFB: починить путь сервера (CDN, кеш, вынести медленные данные). Load delay: картинка обнаруживается поздно — поместить в начальный HTML с fetchpriority='high'. Load time: картинка слишком большая — AVIF/WebP корректного размера через srcset. Render delay: убрать render-blocking CSS/JS. Применить неправильный фикс (сжать картинку при доминирующем load delay) — потратить усилия впустую." },
    { q: "Опишите полный production observability-стек для Core Web Vitals — что даёт RUM, что CI, и почему ни одного в отдельности недостаточно.", a: "RUM: отгрузить web-vitals библиотеку, PerformanceObserver для захвата LCP, INP, CLS с атрибуцией (элемент, разбивка фаз, цель взаимодействия, сдвигающиеся узлы), отправлять в телеметрию с тегами маршрут/устройство/страна/релиз. Реальный вердикт — ловит регрессии только на реальных устройствах и взаимодействиях. CI: Lighthouse CI или Playwright-трасса на каждом PR, throttled под mid-tier устройство, с бюджетами LCP, TBT, CLS, падающий на регрессии. Ловит очевидные регрессии до мержа. Ни одного: лаб без RUM шипает регрессии, которые раскрывают реальные устройства; RUM без CI означает, что каждая регрессия достигает production первой." },
    { q: "Почему INP и материал по SSR/гидратации неразделимы, и что говорит паттерн «только в первые секунды»?", a: "Гидратация выполняется как большая длинная задача рано в жизненном цикле server-rendered страницы. Пользователь тапает кнопку, пока гидратация идёт — ввод ждёт за длинной задачей (input delay). INP записывает большую задержку. Паттерн «только в первые секунды»: INP плох для взаимодействий в первые несколько секунд, нормален после. Как только гидратация заканчивается, поток свободен и обработчики быстрые. Диагноз указывает на гидратацию, не код обработчиков. Каждый приём, сокращающий гидратацию — code-splitting, Server Components, острова, прогрессивная гидратация — одновременно является INP-фиксом." },
  ]}
/>

<Recap lang="ru">
Три Core Web Vitals разделяют бюджет производительности: инлайн CSS помогает render delay LCP, но вредит TTFB; lazy-load уменьшает бандл, но может добавить LCP load delay; щедрое место для CLS может изменить LCP-кандидат. Каждое изменение нужно измерять по всем трём метрикам, а не одной в изоляции. Полная production-observability требует как RUM (библиотека web-vitals + PerformanceObserver, отправляющая атрибутированные LCP/INP/CLS в телеметрию с тегами по маршруту и классу устройства), так и throttled CI-гейта (Lighthouse CI или Playwright, бюджеты LCP/TBT/CLS на каждый PR). RUM — реальный вердикт; CI ловит регрессии до production. INP в SPA дополнительно усложняется мягкими навигациями (soft navigation — клиентский переход маршрута без перезагрузки документа), исторически не имевшими атрибуции LCP — инструментируйте смены маршрутов явно. Именно CrUX field p75 определяет ранжирование; lab-фикс, не сдвинувший его, не помог реальным пользователям. Теперь, когда шипаешь изменение производительности, будешь измерять все три vitals до и после — потому что тихая регрессия в метрике, которую не проверил, обходится дороже всего.
</Recap>
