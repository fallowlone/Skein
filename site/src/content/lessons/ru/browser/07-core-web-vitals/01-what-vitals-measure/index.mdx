---
concepts:
- LCP
- INP
- CLS
- field-metrics
- p75
- CrUX
deepensInto:
- 02-lcp-mechanics
- 03-inp-mechanics
- 04-cls-causes-and-fixes
estMin: 10
lang: ru
lessonType: topic
level: junior
order: 1
prereqs: []
slug: 01-what-vitals-measure
sources:
- https://web.dev/articles/vitals
- https://web.dev/articles/lcp
- https://developer.chrome.com/docs/crux
spiral:
- latency
- statefulness
status: ready
summary: Три полевые метрики, которые реальные пользователи Chrome генерируют при
  каждом посещении — и почему вся остальная работа по производительности в этом пилларе
  в конечном счёте сводится к этим трём числам.
title: 'Core Web Vitals: что измеряют LCP, INP и CLS'
track: browser
unit: 07-core-web-vitals
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import MetaphorComplete from "~/components/pedagogy/MetaphorComplete.astro";
import NumberDrill from "~/components/pedagogy/NumberDrill.astro";
import NumbersCard from "~/components/layout/NumbersCard.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
Страница загружается. Появляется hero-фото. Пользователь тапает кнопку и ждёт. Реклама врывается сверху и текст прыгает — пользователь тапает не на ту ссылку. Всё в этом пилларе — event loop, пайплайн рендера, V8, гидратация — порождает именно такой опыт. Три полевые метрики его измеряют, и по ним Google ранжирует страницы.
</Hook>

<Crux>Три полевые метрики: быстро ли появилось (LCP), отвечает ли (INP), стоит ли на месте (CLS)?</Crux>

<Explanation>

**Три метрики — по одному вопросу каждая.**

Каждая Core Web Vital отвечает на один вопрос, который у реального пользователя возникает в момент захода на страницу:

- **LCP — Largest Contentful Paint** — быстро ли появился главный контент? Хорошо: ≤2.5 с.
- **INP — Interaction to Next Paint** — отвечает ли страница, когда я тапаю или печатаю? Хорошо: ≤200 мс.
- **CLS — Cumulative Layout Shift** — стояло ли всё на месте, или кнопка выпрыгнула из-под пальца? Хорошо: ≤0.1 (безразмерная оценка).

Каждый порог измеряется на **75-м перцентиле** реальных пользователей. Это важно: оптимизация только под быстрые устройства прячет медленную четверть аудитории — а именно её отражает оценка.

**Метафора магазина.**

Зайти в магазин — самый чёткий фрейм для этих трёх метрик. *LCP* — сколько времени, прежде чем вы видите, что продаёт магазин: если полки ещё заполняют, вы ждёте у двери. *INP* — отвечает ли продавец сразу, когда вы задаёте вопрос: хороший магазин отвечает немедленно, плохой заставляет повторять. *CLS* — стоит ли пол на месте: представьте, что тянетесь за товаром, и полка съезжает вбок ровно в этот момент — вы хватаете не то. Хорошая страница — хороший магазин.

**Откуда берутся числа — и почему это важно.**

Core Web Vitals в том виде, в котором их оценивает Google, берутся с **реальных пользователей Chrome в поле**, а не из тестового прогона на машине разработчика. Публичный датасет называется **CrUX** — Chrome User Experience Report. Он агрегирует визиты от пользователей Chrome, давших согласие на отчётность, и именно field p75 из CrUX использует ранжирование в поиске. Отличный Lighthouse-score на ноутбуке и плохие field vitals — вполне совместимые вещи. Именно field-число имеет значение.

На практике это означает: каждый фикс производительности в этом пилларе в конечном счёте живёт или умирает в зависимости от того, двигает ли он field p75 для реальных пользователей. Лабораторные инструменты (Lighthouse, DevTools) — для отладки и перехвата регрессий; CrUX — вердикт.

<NumbersCard
  id="card-cwv-thresholds"
  title="Хорошие пороги Core Web Vitals (при field p75)"
  stats={[
    { label: "LCP — Largest Contentful Paint", value: "≤2.5 с" },
    { label: "INP — Interaction to Next Paint", value: "≤200 мс" },
    { label: "CLS — Cumulative Layout Shift", value: "≤0.1" },
  ]}
  data-lesson-visual
/>

<FlowDiagram
  label="Вдоль таймлайна загрузки страницы каждая Core Web Vital владеет одной фазой пользовательского опыта: загрузку измеряет LCP, интерактивность — INP, визуальную стабильность — CLS."
  perRow={3}
  hue="var(--d-frontend)"
  nodes={[
    { id: "loading", label: "Загрузка", sub: "LCP ≤2.5 с" },
    { id: "interactivity", label: "Интерактивность", sub: "INP ≤200 мс" },
    { id: "stability", label: "Визуальная стабильность", sub: "CLS ≤0.1" },
  ]}
  edges={[
    { from: "loading", to: "interactivity", label: "таймлайн загрузки" },
    { from: "interactivity", to: "stability" },
  ]}
  caption="Один пользовательский опыт, три фазы. Быстро ли появилось (LCP)? Отвечает ли на тап (INP)? Стоит ли на месте (CLS)? Каждая vital оценивает одну фазу, все на field p75 реальных пользователей Chrome."
/>

<Inset kind="why" lang="ru">
INP заменила First Input Delay (FID) как Core Web Vital в марте 2024. FID измерял только задержку до старта обработчика *первого* ввода — она пропускала весь остальной жизненный цикл страницы. INP измеряет полную задержку input-to-paint каждого взаимодействия на протяжении всей сессии и репортит высокий перцентиль из них — это куда ближе к тому, что пользователь реально чувствует, используя страницу, а не просто заходя на неё.
</Inset>

<DragOrder
  id="jr-drag-1"
  pieceSlug="01-what-vitals-measure"
  lang="ru"
  prompt="Сопоставьте жалобу пользователя с Core Web Vital — расставьте в порядке LCP, INP, CLS."
  items={[
    { id: "lcp", label: "LCP — «страница вечность показывала статью»" },
    { id: "inp", label: "INP — «я тапнул кнопку, и секунду ничего не происходило»" },
    { id: "cls", label: "CLS — «текст прыгнул, я потерял место / тапнул не туда»" },
  ]}
  correctOrder={["lcp", "inp", "cls"]}
/>

<Quiz
  id="jr-quiz-1"
  pieceSlug="01-what-vitals-measure"
  lang="ru"
  question="Пользователь тапает кнопку, и страница видимо не отвечает почти секунду. Какая Core Web Vital это ловит?"
  choices={[
    { label: "INP — Interaction to Next Paint — задержка от тапа до отклика экрана", correct: true },
    { label: "LCP — Largest Contentful Paint", misconception: "LCP измеряет, когда самый крупный контент впервые появляется при загрузке. Задержанный отклик на тап — это про интерактивность, это INP." },
    { label: "CLS — Cumulative Layout Shift", misconception: "CLS измеряет неожиданное движение лейаута. Медленный отклик — не движение, а задержка, это INP." },
    { label: "TTFB — Time to First Byte", misconception: "TTFB — сетевая метрика для первого байта от сервера. Задержка кнопки случается намного позже, в браузере." },
  ]}
/>

<Quiz
  id="jr-quiz-2"
  pieceSlug="01-what-vitals-measure"
  lang="ru"
  question="Откуда берутся числа Core Web Vitals, когда Google использует их для ранжирования в поиске?"
  choices={[
    { label: "С реальных пользователей Chrome в поле — CrUX, а не из теста на машине разработчика", correct: true },
    { label: "Из одного лабораторного теста, прогнанного один раз на билде", misconception: "Лабораторный тест — один синтетический прогон. Core Web Vitals в том виде, в каком их оценивает Google, — полевые данные, агрегированные с реальных пользователей." },
    { label: "Из локальной DevTools-сессии разработчика", misconception: "Локальный DevTools — лабораторный инструмент. Он полезен для отладки, но оцениваемые vitals берутся из полевых данных реальных пользователей." },
    { label: "Из access-логов сервера", misconception: "Серверные логи не захватывают тайминг отрисовки или задержку взаимодействия в браузере. Vitals измеряются на клиенте с реальных пользователей." },
  ]}
/>

<MetaphorComplete
  id="jr-metaphor-1"
  pieceSlug="01-what-vitals-measure"
  lang="ru"
  setup="В метафоре магазина одна vital про то, стоит ли пол на месте — съезжает ли полка вбок ровно когда вы тянетесь за товаром, заставляя схватить не то. Какая это Core Web Vital?"
  accepted={["cls", "cumulative layout shift", "layout shift", "сдвиг лейаута", "та cls"]}
  canonical="CLS"
  explanation="CLS — Cumulative Layout Shift — измеряет, насколько неожиданно лейаут страницы движется. Классическая причина — контент, грузящийся поздно (картинка без зарезервированного места, реклама, смена веб-шрифта), и сдвигающий всё ниже вниз. Пользователю это ощущается как съезжающий пол магазина: он тянется к кнопке, а она вдруг в другом месте, и он тапает не туда. Хороший CLS означает, что лейаут стабилен: вещи грузятся в место, зарезервированное под них заранее."
/>

<NumberDrill
  id="jr-drill-1"
  pieceSlug="01-what-vitals-measure"
  lang="ru"
  question="«Хороший» LCP достигается, когда самый крупный контент рисуется в пределах скольки секунд, на 75-м перцентиле реальных пользователей?"
  expected={2.5}
  tolerance={0}
  unit="секунды"
  explanation="2.5 секунды — «хороший» порог LCP. Самый крупный contentful-элемент — обычно hero-картинка или блок заголовка — должен нарисоваться в пределах 2.5 с для 75% реальных визитов. Между 2.5 с и 4 с — «нужно улучшение»; больше 4 с — «плохо». 75-й перцентиль важен: оптимизация только под быстрые устройства прячет медленную четверть ваших пользователей, и именно её отражает оценка."
/>

</Explanation>

<KeyTakeaway>LCP, INP, CLS — полевые метрики на p75 реальных пользователей Chrome, не lab-оценки. Каждая отвечает на один вопрос: появилось ли, отвечает ли, стоит ли на месте.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="01-what-vitals-measure-retrieval"
  lang="ru"
  questions={[
    { q: "Что измеряет каждая Core Web Vital — одной фразой на метрику?", a: "LCP: время до отрисовки самого крупного контента (хорошо ≤2.5 с). INP: задержка от тапа до видимого отклика экрана (хорошо ≤200 мс). CLS: насколько лейаут неожиданно сдвигается (хорошо ≤0.1)." },
    { q: "Почему порог на 75-м перцентиле полезнее среднего для этих метрик?", a: "Среднее тянется вниз быстрыми выбросами — разработчики на быстром Wi-Fi, закешированные повторные загрузки. p75 фокусируется на медленной четверти реальных пользователей, которая и является целью порогов." },
    { q: "Что такое CrUX и почему он важнее Lighthouse для SEO?", a: "CrUX (Chrome User Experience Report) — публичный датасет Google с полевыми метриками от реальных пользователей Chrome. Ранжирование в поиске использует CrUX p75, а не lab-оценки Lighthouse. Зелёный результат Lighthouse и плохой CrUX p75 вполне совместимы." },
  ]}
/>

<Recap lang="ru">
Core Web Vitals — это три полевые метрики, отвечающие на то, что замечают реальные пользователи: LCP (появился ли главный контент за 2.5 с), INP (ответила ли страница на тап за 200 мс), CLS (стоял ли лейаут на месте, оценка ≤0.1). Они измеряются на 75-м перцентиле реальных пользователей Chrome через CrUX — не из лабораторного теста — и именно CrUX p75 использует ранжирование в поиске. Каждый приём оптимизации производительности в этом пилларе — сокращение длинных задач для INP, оптимизация доставки изображений для LCP, резервирование места для CLS — в конечном счёте существует, чтобы двигать эти три field-числа. Отлаживайте в лабе; судите по полю.
</Recap>
