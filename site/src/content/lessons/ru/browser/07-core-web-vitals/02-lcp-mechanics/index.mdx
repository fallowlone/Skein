---
concepts:
- LCP
- preload-scanner
- fetchpriority
- TTFB
- resource-load-delay
- resource-load-time
- render-delay
- lazy-loading
deepensInto:
- 06-tradeoffs-and-observability
estMin: 14
lang: ru
lessonType: topic
level: middle
order: 2
prereqs:
- 01-what-vitals-measure
slug: 02-lcp-mechanics
sources:
- https://web.dev/articles/lcp
- https://web.dev/articles/optimize-lcp
- https://developer.chrome.com/docs/devtools/performance
spiral:
- latency
status: ready
summary: Largest Contentful Paint раскладывается на TTFB, задержку загрузки ресурса,
  время загрузки ресурса и задержку рендера — диагностировать и починить правильную
  фазу и есть вся игра.
title: 'LCP: четыре фазы, одна доминирующая стоимость'
track: browser
unit: 07-core-web-vitals
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import MetaphorComplete from "~/components/pedagogy/MetaphorComplete.astro";
import NumbersCard from "~/components/layout/NumbersCard.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
Команда подключает CDN, сжимает hero-картинку — и LCP почти не двигается. Проблема была не в размере картинки — браузер обнаруживал её поздно, после выполнения JS-бандла. Фикс — один атрибут в HTML. LCP ломается четырьмя разными способами, и только один фикс работает на каждую фазу.
</Hook>

<Crux>LCP делится на четыре фазы — TTFB, задержка загрузки, время загрузки, задержка рендера — и фикс работает только если целит в доминирующую.</Crux>

<Explanation>

**Что измеряет LCP и когда он срабатывает.**

Когда диагностируешь медленный LCP, первый вопрос не «как починить?» — а «какая из четырёх фаз медленная?» Без этого любой фикс — угадайка. Largest Contentful Paint — время отрисовки самого крупного contentful-элемента (англ. — элемента с наполнением), видимого во вьюпорте во время загрузки. «Контент» — конкретный список: `<img>`, постер `<video>`, фоновое изображение через `url()`, блочные элементы с текстом. Браузер наблюдает за элементами по мере их отрисовки, обновляет кандидата на «самый крупный» и финализирует LCP на первом взаимодействии пользователя (после того как пользователь взаимодействует, дальнейшая «загрузка» уже не загрузка). На большинстве страниц LCP-элемент — hero-картинка или главный заголовок.

**Четыре фазы.**

Время LCP — это сумма четырёх последовательных частей. Фикс разный для каждой.

1. **TTFB** — время до первого байта от сервера. Причины: медленный SSR, нет CDN, медленный запрос к базе на пути рендера. Фикс: кеш, CDN, вынести медленные загрузки данных с пути рендера (streaming SSR с Suspense-границей).

2. **Resource load delay** — зазор между TTFB и началом загрузки LCP-картинки браузером. Причина: браузер обнаруживает картинку поздно — она ссылается только в CSS или инжектируется JS, не в начальном HTML, preload-сканер её не видит. Фикс: поместить LCP-картинку в начальный HTML как обычный `<img>` с `fetchpriority="high"` или `<link rel="preload">`.

3. **Resource load time** — сколько сама LCP-картинка скачивается. Причина: огромный файл, неправильный формат, не сжата. Фикс: правильный формат (AVIF, WebP), корректный размер для вьюпорта через `srcset`, сжатие.

4. **Element render delay** — зазор между окончанием скачивания картинки и её отрисовкой. Причина: render-blocking CSS или JS всё ещё занимает главный поток после готовности картинки. Фикс: инлайн критического CSS, defer некритичного JS.

Все четыре фазы — это дерево решений: замерить каждую, найти доминирующую, применить ровно тот фикс. Сжать картинку при доминирующем load delay — или добавить preload при доминирующем времени загрузки — значит потратить спринт и оставить LCP без изменений.

<NumbersCard
  id="card-lcp-phases"
  title="Четыре фазы LCP"
  stats={[
    { label: "TTFB", value: "Сервер + CDN + БД" },
    { label: "Задержка загрузки ресурса", value: "Зазор обнаружения в HTML/CSS/JS" },
    { label: "Время загрузки ресурса", value: "Размер файла × скорость соединения" },
    { label: "Задержка рендера элемента", value: "Render-blocking ресурсы" },
  ]}
  data-lesson-visual
/>

<FlowDiagram
  label="Время LCP — сумма четырёх последовательных фаз в фиксированном порядке: TTFB, затем задержка загрузки ресурса, затем время загрузки ресурса, затем задержка рендера элемента."
  perRow={4}
  hue="var(--d-frontend)"
  nodes={[
    { id: "ttfb", label: "TTFB", sub: "байт сервера" },
    { id: "delay", label: "Задержка загрузки", sub: "обнаружение" },
    { id: "load", label: "Время загрузки", sub: "скачивание" },
    { id: "render", label: "Задержка рендера", sub: "отрисовка" },
  ]}
  edges={[
    { from: "ttfb", to: "delay" },
    { from: "delay", to: "load" },
    { from: "load", to: "render" },
  ]}
  caption="Четыре фазы идут в этом порядке, и фикс работает, только если целит в доминирующую: TTFB → сервер/CDN; load delay → размещение в HTML + fetchpriority; load time → формат + размер; render delay → убрать blocking-ресурсы."
/>

**Preload-сканер — почему обнаружение решает всё.**

Браузеры запускают вторичный парсер — preload-сканер — который обгоняет основной HTML-парсер в поиске ресурсов для загрузки, даже пока основной парсер заблокирован скриптом. Именно поэтому `<img>` в начальном HTML грузится рано: сканер видит его и немедленно стартует загрузку. И именно поэтому картинка, ссылающаяся только в CSS (`background-image`) или инжектируемая JS, грузится поздно: сканер её не видит — картинка не обнаруживается, пока CSS не парснется или JS не выполнится.

Глубинная причина, почему «поместить LCP-картинку в HTML» работает: только в HTML preload-сканер может её найти без ожидания. `fetchpriority="high"` затем поднимает ту загрузку выше других ранних ресурсов; `<link rel="preload">` — тяжёлая артиллерия для ресурсов, которые сканер всё равно не видит.

**Самая частая самонанесённая LCP-регрессия.**

Добавление `loading="lazy"` на hero-картинку. Lazy loading существует для defer-а картинок ниже сгиба — тех, до которых пользователь ещё не доскроллил. Применить его к hero означает сказать браузеру отложить загрузку ровно той картинки, которую таймит метрика LCP. Результат — почти всегда большой resource load delay, которого раньше не было. Никогда не lazy-load-ьте LCP-элемент.

<Inset kind="why" lang="ru">
Частая ошибка — сжимать картинку, когда реальная проблема — load delay. Если resource load delay — доминирующая фаза (браузер обнаруживает картинку поздно), даже файл в 10× меньше не сдвинет LCP — задержка до начала загрузки остаётся бутылочным горлышком. Дисциплина: сначала прочитать разбивку по фазам (доступна из LCP attribution web-vitals библиотеки или DevTools), затем чинить доминирующую фазу. Применить неправильный фикс — потратить усилия впустую и оставить число плохим.
</Inset>

<Quiz
  id="mid-quiz-1"
  pieceSlug="02-lcp-mechanics"
  lang="ru"
  question="Hero-картинка — LCP-элемент, и LCP плох. У картинки loading='lazy'. В чём вероятная проблема?"
  choices={[
    { label: "`loading=\"lazy\"` говорит браузеру отложить загрузку ровно той картинки, которую таймит LCP — hero должен грузиться нетерпеливо", correct: true },
    { label: "Lazy loading делает картинку быстрее отрисовываемой после появления", misconception: "Lazy loading откладывает старт загрузки; на скорость отрисовки он не влияет. На LCP-элементе он напрямую задерживает метрику." },
    { label: "Lazy loading требуется для хорошего LCP", misconception: "Наоборот — lazy-load LCP-элемента — это классическая LCP-регрессия. Lazy-load-ьте картинки ниже сгиба, никогда hero." },
    { label: "Проблема не связана с атрибутом lazy", misconception: "Очень связана: `loading=\"lazy\"` на LCP-картинке добавляет load delay к ровно к измеряемому элементу." },
  ]}
/>

<Quiz
  id="mid-quiz-2"
  pieceSlug="02-lcp-mechanics"
  lang="ru"
  question="Время LCP доминируется 'resource load delay' — зазором между TTFB и началом скачивания картинки. Какой фикс целит в эту фазу?"
  choices={[
    { label: "Поместить картинку в начальный HTML как обычный `<img>` с `fetchpriority=\"high\"`, чтобы preload-сканер нашёл её немедленно", correct: true },
    { label: "Сжать картинку до меньшего файла", misconception: "Сжатие уменьшает resource LOAD TIME, не load DELAY. Если delay — доминирующая фаза, меньший файл не помогает — браузер всё равно обнаруживает её поздно." },
    { label: "Добавить более быстрый CDN", misconception: "CDN улучшает TTFB и load time, не задержку обнаружения. Если картинка ссылается поздно в CSS или JS, CDN не поможет браузеру найти её раньше." },
    { label: "Инлайнить критический CSS", misconception: "Инлайн CSS целит в render delay (render-blocking ресурсы), не resource load delay. Не та фаза." },
  ]}
/>

<DragOrder
  id="mid-drag-1"
  pieceSlug="02-lcp-mechanics"
  lang="ru"
  prompt="Расставьте четыре фазы времени LCP — от запроса, покидающего браузер, до отрисовки LCP-элемента."
  items={[
    { id: "ttfb", label: "TTFB — сервер производит и отправляет первый байт" },
    { id: "delay", label: "Resource load delay — браузер обнаруживает и начинает загрузку LCP-картинки" },
    { id: "load", label: "Resource load time — LCP-картинка скачивается" },
    { id: "render", label: "Element render delay — картинка рисуется после очистки render-blocking работы" },
  ]}
  correctOrder={["ttfb", "delay", "load", "render"]}
/>

</Explanation>

<KeyTakeaway>Четыре фазы LCP — каждая с разным фиксом: TTFB (сервер/CDN), load delay (HTML + fetchpriority), load time (формат + размер), render delay (убрать blocking-ресурсы). Сначала диагностируйте доминирующую фазу.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="02-lcp-mechanics-retrieval"
  lang="ru"
  questions={[
    { q: "Назовите четыре фазы времени LCP и что делает каждую медленной.", a: "TTFB: медленный сервер, нет CDN, медленная БД на пути рендера. Resource load delay: картинка обнаруживается поздно (в CSS/JS, не в начальном HTML) — preload-сканер её не видит. Resource load time: слишком большая или неправильный формат картинка. Element render delay: render-blocking CSS или JS всё ещё работает после окончания загрузки картинки." },
    { q: "Почему помещение LCP-картинки в начальный HTML помогает LCP, и что добавляет fetchpriority='high'?", a: "Preload-сканер браузера обгоняет основной парсер в поиске ресурсов. <img> в начальном HTML виден сканеру немедленно, поэтому загрузка стартует до выполнения blocking-скриптов. fetchpriority='high' поднимает ту загрузку выше других ранних конкурирующих ресурсов (стили, другие картинки)." },
    { q: "Команда замеряет разбивку по фазам LCP и находит: load delay 80 мс, resource load time 3.5 с. Какую фазу чинить первой и как?", a: "Resource load time доминирует — файл картинки слишком большой. Фикс: отдавать в AVIF или WebP (на 30–50% меньше JPEG при том же качестве), корректно отмасштабированной для отображаемых размеров через srcset. Load delay уже мал, поэтому preload-подсказка не сдвинет LCP." },
  ]}
/>

<Recap lang="ru">
LCP измеряет время отрисовки самого крупного contentful-элемента во вьюпорте и раскладывается на четыре последовательных фазы: TTFB, resource load delay, resource load time и element render delay. Фикс работает только если целит в доминирующую фазу — сжать картинку, когда бутылочное горлышко в load delay, ничего не даст. Preload-сканер — причина, почему размещение картинки в начальном HTML важно: только HTML-встроенные картинки видны сканеру до выполнения blocking-скриптов. Никогда не применяйте `loading="lazy"` к LCP-элементу — это добавляет load delay к ровно измеряемому элементу. Полная разбивка по фазам доступна из LCP attribution библиотеки web-vitals или DevTools performance-трассы — прочитать её перед любым фиксом это дисциплина, которая отделяет быструю итерацию от потраченных усилий. Теперь, когда увидишь плохой LCP в поле, первый шаг — вытащить четырёхфазную разбивку, а не тянуться за инструментом сжатия.
</Recap>
