---
concepts:
- CLS
- layout-shift
- session-window
- impact-fraction
- distance-fraction
- aspect-ratio
- font-display
- size-adjust
deepensInto:
- 06-tradeoffs-and-observability
estMin: 12
lang: ru
lessonType: topic
level: middle
order: 4
prereqs:
- 01-what-vitals-measure
slug: 04-cls-causes-and-fixes
sources:
- https://web.dev/articles/cls
- https://web.dev/articles/optimize-cls
- https://developer.chrome.com/docs/devtools/performance
spiral:
- statefulness
status: ready
summary: 'Cumulative Layout Shift оценивает худший всплеск неожиданного движения —
  картинки без размеров, поздняя реклама, смены шрифтов, динамически инжектируемые
  баннеры — и фикс всегда один: зарезервировать место до прихода байтов.'
title: 'CLS: почему происходят сдвиги лейаута и как их остановить'
track: browser
unit: 07-core-web-vitals
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import FadedExample from "~/components/pedagogy/FadedExample.tsx";
import NumbersCard from "~/components/layout/NumbersCard.astro";
import Misconception from "~/components/layout/Misconception.astro";
import DiagramFrame from "~/components/diagram/DiagramFrame.astro";

<Hook>
Статья загружается быстро. Пользователь начинает читать второй абзац. Затем рекламный блок над статьёй заполняется и сдвигает всё вниз на 300 пикселей. Пользователь был на полуслове; теперь он на середине другого абзаца. Он ничего не тапал. Страница сдвинулась сама. Это CLS — и он проще всего предотвращается из трёх vital, если понять одно правило, стоящее за всеми четырьмя классическими причинами.
</Hook>

<Crux>CLS измеряет неожиданное движение — и фикс всегда один: зарезервировать место для всего, чей конечный размер неизвестен во время парсинга.</Crux>

<Explanation>

**Как считается оценка.**

Прежде чем рассуждать о том, сдвинет ли фикс оценку CLS, нужно понять, что именно браузер замеряет на каждый сдвиг. Каждый раз, когда видимый элемент меняет позицию между двумя кадрами без причины, инициированной пользователем, браузер записывает *layout shift* (сдвиг лейаута). Оценка одного сдвига:

**impact fraction × distance fraction**

*Impact fraction*: совокупная площадь всех сдвинувшихся элементов как доля вьюпорта. Если картинка, покрывающая половину вьюпорта, прыгает — impact fraction 0.5. *Distance fraction*: наибольшее расстояние, на которое сдвинулся любой элемент, как доля высоты вьюпорта. Сдвиг на 20% высоты вьюпорта даёт 0.2.

Таким образом, один сдвиг, двигающий полувьюпортный элемент на 20% высоты вьюпорта, оценивается 0.5 × 0.2 = 0.1 — ровно на «хорошей» границе.

CLS — не пожизненная сумма всех сдвигов. Это сумма сдвигов в **худшем session window**: кластер сдвигов, где каждый в пределах 1 секунды от предыдущего, и весь кластер охватывает не более 5 секунд.

<NumbersCard
  id="card-cls-formula"
  title="Правила подсчёта CLS"
  stats={[
    { label: "Оценка сдвига", value: "impact fraction × distance fraction" },
    { label: "CLS", value: "Сумма худшего 5-с session window" },
    { label: "Окно исключения сдвига после ввода", value: "500 мс" },
    { label: "Хороший порог (p75)", value: "≤0.1" },
  ]}
  data-lesson-visual
/>

**Окно в 500 мс исключения — что CLS не наказывает.**

Сдвиги в пределах 500 мс от взаимодействия пользователя исключаются. Открытие аккордеона, раскрытие выпадающего меню, клик «показать ещё» — результирующее движение ожидается пользователем и не засчитывается в CLS. CLS наказывает только движение, которое пользователь не вызвал и не ожидал.

**Четыре классические причины и их фиксы.**

1. **Картинки без размеров.** `<img>` без атрибутов `width` и `height` имеет нулевую высоту до прихода байтов. Когда картинка загружается, браузер узнаёт её размеры, перезапускает компоновку, и всё ниже прыгает вниз. Фикс: всегда задавать атрибуты `width` и `height` (современные браузеры автоматически выводят `aspect-ratio` из них и резервируют правильно пропорциональный блок). CSS может делать картинку fluid с `width: 100%; height: auto` — атрибуты дают пропорцию, CSS — адаптивный размер.

2. **Реклама, эмбеды и iframe, инжектируемые в незарезервированное место.** Рекламный блок, рендерящий 250 пикселей контента в контейнер без зарезервированной высоты, сдвигает всё ниже. Фикс: оборачивать каждый рекламный и embed-слот в контейнер с `min-height`, равным наибольшему ожидаемому размеру.

3. **Reflow от веб-шрифтов.** Фоллбэк-шрифт с другими метриками рендерится первым; когда веб-шрифт загружается, браузер перекомпоновывает текст — символы шире или уже, строки переносятся, элементы ниже сдвигаются. Фикс: использовать `size-adjust` и дескрипторы `ascent-override`/`descent-override` шрифта, чтобы метрики фоллбэка совпадали с веб-шрифтом; или `font-display: optional` для применения веб-шрифта только при повторных визитах.

4. **Динамически инжектируемый контент над существующим.** Cookie-баннер, панель уведомлений или виджет чата, вставленный над телом страницы в runtime, сдвигает всё вниз. Фикс: вставить в заранее зарезервированное место (контейнер с известной высотой в лейауте), или рендерить как оверлей (fixed/absolute), чтобы не участвовал в flow-лейауте вообще.

Объединяющий принцип: **резервировать место для всего, чей конечный размер неизвестен во время парсинга, или убедиться, что оно никогда не участвует в flow-лейауте**.

<DiagramFrame
  label="Без зарезервированного места поздно загружаемая картинка схлопывается в блок нулевой высоты, поэтому текст статьи рендерится высоко; когда байты картинки приходят, браузер перезапускает компоновку и текст прыгает вниз — layout shift. С явными width и height браузер резервирует правильно пропорциональный блок заранее, картинка заполняет его и ничто не движется."
  hue="var(--d-frontend)"
>
  <svg viewBox="0 0 460 230" width="460" height="230" role="presentation" style="display:block;margin:0 auto;max-width:100%;height:auto;font-family:var(--font-body)">
    {/* LEFT: no reserved space */}
    <text x="105" y="16" text-anchor="middle" font-size="12" fill="var(--ink)">Без width/height — сдвиг</text>
    {/* before frame */}
    <text x="50" y="38" text-anchor="middle" font-size="9" font-family="var(--font-mono)" fill="var(--faint)">до байтов</text>
    <rect x="14" y="44" width="72" height="150" rx="5" fill="none" stroke="var(--hairline-strong)" stroke-width="0.8" stroke-dasharray="3 3" />
    <rect x="22" y="52" width="56" height="10" rx="2" fill="none" stroke="var(--muted)" stroke-width="0.8" />
    <text x="50" y="60" text-anchor="middle" font-size="6.5" font-family="var(--font-mono)" fill="var(--muted)">img 0px</text>
    <rect x="22" y="68" width="56" height="6" rx="1.5" fill="var(--card-2)" />
    <rect x="22" y="78" width="56" height="6" rx="1.5" fill="var(--card-2)" />
    <rect x="22" y="88" width="40" height="6" rx="1.5" fill="var(--card-2)" />
    {/* after frame */}
    <text x="160" y="38" text-anchor="middle" font-size="9" font-family="var(--font-mono)" fill="var(--faint)">после загрузки</text>
    <rect x="124" y="44" width="72" height="150" rx="5" fill="none" stroke="var(--hairline-strong)" stroke-width="0.8" stroke-dasharray="3 3" />
    <rect x="132" y="52" width="56" height="40" rx="2" fill="var(--card-2)" stroke="var(--muted)" stroke-width="0.8" />
    <text x="160" y="75" text-anchor="middle" font-size="7" font-family="var(--font-mono)" fill="var(--muted)">img загр.</text>
    <rect x="132" y="98" width="56" height="6" rx="1.5" fill="var(--card-2)" />
    <rect x="132" y="108" width="56" height="6" rx="1.5" fill="var(--card-2)" />
    <rect x="132" y="118" width="40" height="6" rx="1.5" fill="var(--card-2)" />
    {/* shift arrow */}
    <line x1="206" y1="72" x2="206" y2="104" stroke="var(--ink)" stroke-width="1.2" marker-end="url(#cls-shift-arrow)" />
    <text x="232" y="92" text-anchor="middle" font-size="8" font-family="var(--font-mono)" fill="var(--ink)">текст прыгает</text>
    {/* divider */}
    <line x1="270" y1="30" x2="270" y2="200" stroke="var(--hairline-2)" stroke-width="0.5" />
    {/* RIGHT: reserved space */}
    <text x="365" y="16" text-anchor="middle" font-size="12" fill="var(--ink)">width + height — без сдвига</text>
    <text x="320" y="38" text-anchor="middle" font-size="9" font-family="var(--font-mono)" fill="var(--faint)">до байтов</text>
    <rect x="284" y="44" width="72" height="150" rx="5" fill="none" stroke="var(--hairline-strong)" stroke-width="0.8" stroke-dasharray="3 3" />
    <rect x="292" y="52" width="56" height="40" rx="2" fill="none" stroke="var(--muted)" stroke-width="0.8" stroke-dasharray="2 2" />
    <text x="320" y="75" text-anchor="middle" font-size="6.5" font-family="var(--font-mono)" fill="var(--muted)">место</text>
    <rect x="292" y="98" width="56" height="6" rx="1.5" fill="var(--card-2)" />
    <rect x="292" y="108" width="56" height="6" rx="1.5" fill="var(--card-2)" />
    <rect x="292" y="118" width="40" height="6" rx="1.5" fill="var(--card-2)" />
    <text x="430" y="38" text-anchor="middle" font-size="9" font-family="var(--font-mono)" fill="var(--faint)">после загрузки</text>
    <rect x="394" y="44" width="72" height="150" rx="5" fill="none" stroke="var(--hairline-strong)" stroke-width="0.8" stroke-dasharray="3 3" />
    <rect x="402" y="52" width="56" height="40" rx="2" fill="var(--card-2)" stroke="var(--muted)" stroke-width="0.8" />
    <text x="430" y="75" text-anchor="middle" font-size="7" font-family="var(--font-mono)" fill="var(--muted)">img загр.</text>
    <rect x="402" y="98" width="56" height="6" rx="1.5" fill="var(--card-2)" />
    <rect x="402" y="108" width="56" height="6" rx="1.5" fill="var(--card-2)" />
    <rect x="402" y="118" width="40" height="6" rx="1.5" fill="var(--card-2)" />
    <text x="430" y="138" text-anchor="middle" font-size="8" font-family="var(--font-mono)" fill="var(--muted)">текст на месте</text>
    <defs>
      <marker id="cls-shift-arrow" viewBox="0 0 10 10" refX="9" refY="5" markerWidth="7" markerHeight="7" orient="auto-start-reverse">
        <path d="M0 0 L10 5 L0 10 z" fill="var(--ink)" />
      </marker>
    </defs>
  </svg>
</DiagramFrame>

**Ловушка с анимациями.**

Анимирование layout-свойств — `top`, `left`, `height`, `width`, `margin` — *создаёт* layout shifts и может навредить CLS, даже если движение выглядит преднамеренным для разработчика. Фикс — анимировать `transform` вместо (`translateY`, `scale`), который композитируется и никогда не запускает компоновку. Сдвиг, вызванный CSS-анимацией, всё равно является сдвигом, если он не следует за взаимодействием пользователя в пределах 500 мс.

<Inset kind="why" lang="ru">
Модель session window заменила оригинальную «пожизненную сумму», потому что долго живущие страницы и infinite scroll несправедливо наказывались за сдвиги, происходившие через пять минут после загрузки — далеко за пределами контекста чтения пользователя. Session window фокусирует CLS на всплесках плохого поведения: кластер сдвигов при перезагрузке рекламы или пакет картинок без размеров загружается, а не накопленная стоимость сайта, которым кто-то пользуется час. CLS теперь более репрезентативен для того, что пользователь реально замечает.
</Inset>

<FadedExample
  client:visible
  id="sr-faded-1"
  pieceSlug="04-cls-causes-and-fixes"
  lang="ru"
  title="Зарезервировать место для поздно загружаемой картинки и устранить CLS"
  steps={{
    solved: (
      <pre><code>{`{/* width + height дают браузеру зарезервировать
     правильно пропорциональный блок до прихода байтов.
     Современные браузеры выводят из них aspect-ratio. */}
<img
  src="/hero.avif"
  width="1200"
  height="800"
  fetchpriority="high"
  alt="Product hero"
/>`}</code></pre>
    ),
    semi: {
      prompt: (
        <p>Заполните два атрибута, позволяющих браузеру зарезервировать место для картинки до её загрузки.</p>
      ),
      blanks: [
        { id: "w", expected: /width/i, placeholder: "атрибут размера 1" },
        { id: "h", expected: /height/i, placeholder: "атрибут размера 2" },
      ],
    },
    blank: {
      prompt: (
        <p><code>&lt;img&gt;</code> без размеров загружается поздно, прыгает с нулевой высоты до полного размера и сдвигает текст статьи вниз — это CLS-сдвиг. Перепишите тег, чтобы браузер зарезервировал правильное место до прихода байтов картинки.</p>
      ),
      reveal: (
        <p>Добавьте явные атрибуты <code>width</code> и <code>height</code>. Браузер вычисляет соотношение сторон во время парсинга и размечает правильно пропорциональный блок до скачивания картинки. CSS может применять <code>width: 100%; height: auto</code> для адаптивного размера — атрибуты дают пропорцию. Когда картинка приходит, она заполняет зарезервированное место без сдвига.</p>
      ),
    },
  }}
/>

<Quiz
  id="mid-quiz-1"
  pieceSlug="04-cls-causes-and-fixes"
  lang="ru"
  question="Пользователь открывает аккордеон, и контент под ним сдвигается вниз. Засчитывается ли это в CLS?"
  choices={[
    { label: "Нет — сдвиги в пределах 500 мс от взаимодействия пользователя исключаются; это движение инициировано пользователем и ожидаемо", correct: true },
    { label: "Да — любое движение лейаута засчитывается в CLS", misconception: "CLS специально исключает сдвиги в пределах 500 мс от взаимодействия пользователя. Он измеряет только неожиданное, не-вызванное движение." },
    { label: "Да, но только половина оценки", misconception: "Полуоценки нет. Инициированные пользователем сдвиги в окне 500 мс полностью исключаются, не частично засчитываются." },
    { label: "Только если в аккордеоне есть картинка", misconception: "Тип контента не важен. Исключение про сдвиг, следующий за взаимодействием, независимо от того, что сдвинулось." },
  ]}
/>

<Quiz
  id="mid-quiz-2"
  pieceSlug="04-cls-causes-and-fixes"
  lang="ru"
  question="Cookie-баннер инжектируется над телом статьи после загрузки страницы, сдвигая весь контент вниз на 60 пикселей. Какой правильный CLS-фикс?"
  choices={[
    { label: "Рендерить баннер как fixed-position оверлей, или предварительно зарезервировать 60-пиксельный контейнер в лейауте, чтобы баннер заполнял зарезервированное место", correct: true },
    { label: "Отложить инжекцию баннера до 5 секунд", misconception: "Задержка не убирает сдвиг — только переносит session window. Лейаут всё равно движется при появлении баннера; CLS всё равно его записывает." },
    { label: "Сделать баннер появляющимся fade-in, а не slide-in", misconception: "Анимация видимости не влияет на лейаут. Сдвиг лейаута происходит, когда баннер участвует в flow и толкает контент — fade или slide не важен для CLS." },
    { label: "Использовать CSS-анимацию на баннере", misconception: "Анимирование layout-свойства (height, margin) на баннере всё равно вызывает layout shift. Только оверлей-позиционирование или предварительно зарезервированное место предотвращает сдвиг." },
  ]}
/>

<Misconception id="mc-cls-images">
**Миф:** Анимирование элемента через CSS всегда безопасно для CLS.

**Реальность:** Анимирование layout-свойств (`top`, `left`, `height`, `width`, `margin`) запускает пересчёт компоновки и записывает layout shifts. Анимируйте `transform` (`translateY`, `scale`) вместо — он композитируется и никогда не влияет на лейаут.
</Misconception>

</Explanation>

<KeyTakeaway>CLS наказывает неожиданное движение лейаута — оценка impact × distance, худший 5-с window. Фикс всегда — резервирование места: width/height на картинках, min-height на рекламных контейнерах, size-adjust на шрифтах, overlay-позиционирование для инжектируемых баннеров.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="04-cls-causes-and-fixes-retrieval"
  lang="ru"
  questions={[
    { q: "Как считается оценка одного layout shift и что такое CLS session window?", a: "Оценка одного сдвига = impact fraction (совокупная площадь сдвигающихся элементов как доля вьюпорта) × distance fraction (наибольшее перемещение как доля высоты вьюпорта). CLS — сумма худшего session window: кластер сдвигов, где каждый в пределах 1 секунды от предыдущего и весь кластер охватывает ≤5 секунд." },
    { q: "Назовите четыре классические причины CLS и фикс для каждой.", a: "1. Картинки без размеров — добавить атрибуты width/height, чтобы браузер резервировал место. 2. Реклама/эмбеды в незарезервированных контейнерах — предрезервировать min-height. 3. Reflow от веб-шрифтов — использовать size-adjust/font-override дескрипторы для совпадения метрик фоллбэка. 4. Динамически инжектируемый контент над сгибом — вставлять в предрезервированное место или оверлей-позиционирование." },
    { q: "CSS-анимация двигает элемент через 'top' и страница не проходит CLS-аудит. Как починить, не убирая анимацию?", a: "Заменить анимацию top на transform: translateY(). Анимирование layout-свойств (top, left, height, width) запускает пересчёт компоновки между кадрами и генерирует layout shifts. Transform композитируется — он двигает элемент без затрагивания лейаута, поэтому никакой сдвиг не записывается." },
  ]}
/>

<Recap lang="ru">
CLS оценивает худший всплеск неожиданного движения лейаута: оценка одного сдвига — impact fraction × distance fraction, и CLS репортит худший 5-секундный session window. Сдвиги в пределах 500 мс от взаимодействия пользователя исключаются — CLS наказывает только движение, которое пользователь не вызвал. Четыре классические причины: картинки без размеров, реклама и эмбеды в контейнерах без зарезервированной высоты, reflow от веб-шрифта при несовпадении метрик фоллбэка, и динамически инжектируемый контент над существующим flow. Фикс для всех четырёх следует одному правилу: всё, чей конечный размер неизвестен во время парсинга, должно иметь зарезервированное место, или использовать overlay-позиционирование вне flow-лейаута. Анимирование layout-свойств (top, left, height) генерирует сдвиги — анимируйте transform вместо. Теперь, когда увидишь скачок CLS, первый вопрос не «что сдвинулось?», а «было ли зарезервировано место для этого?» — и если нет, вот твой фикс.
</Recap>
