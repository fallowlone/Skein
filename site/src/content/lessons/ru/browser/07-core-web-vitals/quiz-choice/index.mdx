---
concepts:
- lcp-inp-cls-thresholds
- field-vs-lab
- hydration-inp
- lcp-phases
- cls-session-window
- metric-tradeoffs
estMin: 13
lang: ru
level: senior
order: 7
prereqs: []
slug: quiz-choice
sources:
- https://web.dev/articles/vitals
- https://web.dev/articles/inp
- https://developer.chrome.com/docs/crux
status: ready
summary: "Тест с выбором на синтез по всему юниту Core Web Vitals — пороги, вердикт field-vs-lab, фазы LCP, ловушка гидрации для INP, подсчёт CLS и компромиссы общего бюджета."
title: 'Core Web Vitals: тест с выбором ответа'
track: browser
unit: 07-core-web-vitals
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Шесть вопросов поперёк всего юнита. Каждый отражает решение, которое ты принимаешь, читая реальный отчёт PageSpeed или полевой дашборд — не определение для заучивания, а какому числу верить и за какой фикс хвататься.
</Hook>

<Goal lang="ru">
Убедись, что связываешь три порога, вердикт field-vs-lab, разбиения на фазы и части, и общий бюджет производительности — тот синтез, к которому вели отдельные уроки.
</Goal>

<Quiz
  id="07-core-web-vitals-choice-1"
  lessonSlug="07-core-web-vitals"
  lang="ru"
  question="PageSpeed Insights показывает LCP 1.8 с в секции Lighthouse (зелёный) и LCP 3.9 с по field p75 (красный). Команда говорит: 'лаба говорит, что всё ок, ничего не делаем.' Как правильно это читать?"
  choices={[
    { label: "Вердикт — field p75: ранжирование Search использует field data из CrUX, а четверть реальных пользователей на 3.9 с или выше. Воспроизведи медленные условия в лабе (жёстче throttle CPU/сети), пока лаба не покажет проблему, и тогда чини.", correct: true },
    { label: "Лаба права, потому что воспроизводима и детерминирована.", misconception: "Воспроизводимо — не значит репрезентативно. Лаба намеренно фиксирует одно устройство и сеть — это делает её хорошей для отладки, но не для вердикта. Ранжирование использует field p75." },
    { label: "Усредни их: реальный LCP около 2.85 с, это нормально.", misconception: "Число из лабы и полевой перцентиль — разные виды измерений; усреднять их бессмысленно. Field p75 самодостаточен как пережитое пользователем и релевантное ранжированию значение." },
    { label: "Важен только TTFB; оба числа LCP — шум.", misconception: "TTFB — одна фаза LCP, а не замена ей. Оба числа LCP валидны — они по-разному отвечают на вопрос синтетика-vs-реальный пользователь — и полевое и есть вердикт." },
  ]}
/>

<Quiz
  id="07-core-web-vitals-choice-2"
  lessonSlug="07-core-web-vitals"
  lang="ru"
  question="У server-rendered страницы плохой INP, но только для взаимодействий в первые ~3 секунды; после этого каждый тап нормальный. Обработчики тапа выполняются за 8 мс. Где проблема и каков фикс?"
  choices={[
    { label: "Гидрация. Ранние тапы стоят в очереди за длинной задачей гидрации (input delay); когда гидрация завершается, поток свободен. Фикс: уменьшить то, что гидрируется — Server Components, островки, code-splitting — а не обработчики.", correct: true },
    { label: "Обработчики событий слишком медленные; оптимизируй код обработчика.", misconception: "Если бы причиной были обработчики, INP был бы плохим для каждого взаимодействия, а не только для ранних. Обработчик за 8 мс не создаёт ограниченный во времени паттерн — эта сигнатура указывает на гидрацию." },
    { label: "Сервер медленный; снизь TTFB.", misconception: "Задержка сервера влияет на TTFB и LCP, а не на задержку взаимодействия в основном потоке, которую измеряет INP — и не создала бы паттерн INP только в начале." },
    { label: "Картинки слишком большие; сожми их.", misconception: "Размер картинки определяет load time у LCP. Он не блокирует основной поток для взаимодействий; это вопрос JavaScript/гидрации, на что и указывает паттерн только в начале." },
  ]}
/>

<Quiz
  id="07-core-web-vitals-choice-3"
  lessonSlug="07-core-web-vitals"
  lang="ru"
  question="LCP-атрибуция web-vitals показывает TTFB 280 мс, load delay 90 мс, load time 3510 мс, render delay 240 мс. Элемент — hero-картинка JPEG 3.8 МБ 4000×3000. Каков фикс с наибольшим рычагом?"
  choices={[
    { label: "Доминирует load time (~85% LCP), потому что hero — полноразмерный необработанный JPEG. Отдай его в размер вьюпорта в AVIF или WebP через srcset — одно это срежет load time с 3.5 с до пары сотен мс.", correct: true },
    { label: "Добавь fetchpriority='high' и preload-хинт, чтобы починить обнаружение.", misconception: "Load delay всего 90 мс — обнаружение уже быстрое. fetchpriority/preload чинят load DELAY, а не load TIME. Чинить не ту фазу — классическая ошибка с LCP." },
    { label: "Снизь TTFB через CDN и edge-кэширование.", misconception: "TTFB — 280 мс, маленький кусок. Срезать его почти ничего не даст против load time в 3510 мс. Сначала читай разбиение по фазам; чини доминирующую." },
    { label: "Инлайни критический CSS, чтобы срезать render delay.", misconception: "Render delay — 240 мс. Инлайнинг CSS бьёт по render-blocking ресурсам, которые здесь не узкое место. Узкое место — скачивание картинки в 4 МБ." },
  ]}
/>

<Quiz
  id="07-core-web-vitals-choice-4"
  lessonSlug="07-core-web-vitals"
  lang="ru"
  question="Один layout shift сдвигает элемент в пол-вьюпорта вниз на 20% высоты вьюпорта, без взаимодействия пользователя за предыдущие 500 мс. Сколько он набирает, и CLS страницы автоматически плохой?"
  choices={[
    { label: "0.5 × 0.2 = 0.1 — impact fraction на distance fraction, ровно на границе 'good'. CLS — это сумма худшего session window в 5 с, поэтому один пограничный сдвиг не делает CLS плохим автоматически, если не кластеризуется с другими.", correct: true },
    { label: "Набирает 0.7 — складываешь impact и distance fractions.", misconception: "Оценка — произведение, а не сумма: impact fraction × distance fraction = 0.5 × 0.2 = 0.1. Сложение — распространённая арифметическая ошибка." },
    { label: "Набирает 0, потому что взаимодействия пользователя не было.", misconception: "Наоборот — исключение в 500 мс убирает сдвиги, которые СЛЕДУЮТ за взаимодействием. Беспричинный сдвиг — именно то, что наказывает CLS; этот набирает 0.1." },
    { label: "Автоматически плохой, потому что любой одиночный сдвиг свыше 0.05 проваливается.", misconception: "Нет порога pass/fail на отдельный сдвиг. CLS — сумма худшего session window в 5 с; одиночный сдвиг 0.1 сидит на границе good, а не в 'poor'." },
  ]}
/>

<Quiz
  id="07-core-web-vitals-choice-5"
  lessonSlug="07-core-web-vitals"
  lang="ru"
  question="Команда инлайнит весь bundle критического CSS, чтобы срезать render delay у LCP на 400 мс. LCP в лабе улучшается, но ты возражаешь перед мержем. Почему?"
  choices={[
    { label: "Три vitals делят бюджет — инлайнинг раздувает HTML на десятки КБ, поднимая TTFB, а TTFB — это пол под LCP. Меряй все три (и TTFB) до и после; выигрыш в render delay может быть съеден регрессией TTFB на медленных соединениях.", correct: true },
    { label: "Инлайнинг CSS всегда вредит INP, поэтому так делать нельзя.", misconception: "Инлайнинг критического CSS сам по себе не задирает INP — он убирает render-blocking запрос. Реальная забота — компромисс размер-HTML/TTFB, знак которого зависит от скорости соединения и размера bundle." },
    { label: "CSS никак не связан с LCP, так что изменение бессмысленно.", misconception: "Render-blocking CSS напрямую задерживает paint LCP-элемента (render delay), так что инлайнинг критического CSS может помочь LCP. Подвох — в цене TTFB, а не в нерелевантности." },
    { label: "Улучшения в лабе никогда не переносятся в field, так что игнорируй.", misconception: "Фиксы из лабы вполне могут двигать field — в этом и смысл отладки в лабе. Проблема в проверке чистого эффекта по всем трём метрикам, а не в игнорировании результатов лабы." },
  ]}
/>

<Quiz
  id="07-core-web-vitals-choice-6"
  lessonSlug="07-core-web-vitals"
  lang="ru"
  question="SPA выдаёт зелёные Core Web Vitals на первой загрузке, но пользователи жалуются, что клиентские смены маршрута ощущаются вялыми. Почему метрики могут это пропустить и каков фикс?"
  choices={[
    { label: "Core Web Vitals проектировались вокруг полных загрузок страницы; soft navigation не фаерит событие load документа, поэтому смены маршрута исторически не имели атрибуции LCP. Инструментируй soft navigations явно — записи soft-navigation PerformanceObserver или RUM-метки на событиях смены маршрута.", correct: true },
    { label: "Всё в порядке — зелёные vitals на первой загрузке значат, что всё приложение быстрое.", misconception: "Зелёные числа на первой загрузке не покрывают soft navigations. Быстрая первая загрузка плюс вялые переходы маршрута — реальный, исторически недомеренный режим отказа в SPA." },
    { label: "Датасет CrUX просто неверен для SPA.", misconception: "CrUX не неверен; разрыв в том, что soft navigations не испускают стандартное событие load, поэтому vitals им не атрибутировались. Поддержка soft-navigation в Chrome и явная RUM-инструментация закрывают разрыв." },
    { label: "Переходи на multi-page архитектуру; у SPA не может быть хороших vitals.", misconception: "У SPA могут быть хорошие vitals — проблема в покрытии измерений смен маршрута, а не в архитектурной невозможности. Инструментируй soft navigations, а не бросай SPA." },
  ]}
/>

<Recap lang="ru">
Сквозная линия юнита — одна диагностическая дисциплина: LCP/INP/CLS — это полевые метрики на p75 (≤2.5 с, ≤200 мс, ≤0.1), и только field data из CrUX — вердикт ранжирования: отлаживай в лабе, суди по field. LCP читают по разбиению на четыре фазы, а INP — по разбиению на три части, поэтому чинишь доминирующую фазу, а не удобную. Паттерн INP только в начале означает гидрацию, а не обработчики. CLS — это impact × distance, просуммированный по худшему окну 5 с, с исключением 500 мс после взаимодействия. И все три делят бюджет — каждое изменение надо мерить против всех них, никогда одну в изоляции.
</Recap>
