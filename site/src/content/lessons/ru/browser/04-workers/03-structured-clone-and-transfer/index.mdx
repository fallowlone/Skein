---
slug: 03-structured-clone-and-transfer
lang: ru
track: browser
unit: 04-workers
order: 3
title: "Structured clone и transferables"
summary: "postMessage делает глубокую копию через structured clone (~1 мс/МБ) — transferables передают ArrayBuffer за O(1), передавая владение принимающему потоку."
estMin: 12
status: ready
lessonType: topic
level: middle
prereqs: ["browser/04-workers/01-what-workers-are", "browser/04-workers/02-web-worker-mechanics"]
deepensInto: ["05-shared-array-buffer-and-atomics", "07-worker-pools-and-observability"]
spiral: ["encapsulation"]
concepts: ["structured-clone", "transferable", "ArrayBuffer", "postMessage", "clone-cost", "ImageBitmap"]
sources:
  - https://html.spec.whatwg.org/multipage/structured-data.html
  - https://developer.mozilla.org/en-US/docs/Web/API/Web_Workers_API/Transferable_objects
  - https://developer.mozilla.org/en-US/docs/Web/API/structuredClone
---

import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import MetaphorComplete from "~/components/pedagogy/MetaphorComplete.astro";
import NumberDrill from "~/components/pedagogy/NumberDrill.astro";
import FadedExample from "~/components/pedagogy/FadedExample.tsx";
import NumbersCard from "~/components/layout/NumbersCard.astro";
import RFCQuiz from "~/components/pedagogy/RFCQuiz.astro";

<Hook>
Вы переносите задачу обработки изображения на 40 МБ в воркер — и главный поток всё равно замирает на 40 мс, прежде чем воркер даже начнёт. Работа переехала, но данные — нет.
</Hook>

<Crux>postMessage копирует через structured clone; передача ArrayBuffer перемещает владение за O(1) и делает ссылку отправителя непригодной.</Crux>

<Explanation>

## Алгоритм structured clone

`postMessage(data)` не делит `data` — он делает его глубокую копию через **алгоритм structured clone**. Structured clone обрабатывает объекты, массивы, Map, Set, Date, типизированные массивы, Blob и ArrayBuffer. Он **не обрабатывает**:
- Функции (молча отбрасываются)
- DOM-узлы (бросает исключение)
- Прототипы классов — методы теряются, выживают только свойства данных

Копия синхронна на отправляющем потоке и масштабируется с размером payload-а: примерно **1 мс на МБ** для плоских объектов, больше для глубоко вложенных структур. Отправьте объект на 50 МБ — и вы заблокировали отправляющий поток на ~50 мс — вы переместили работу, но заплатили за это налог на клонирование.

<NumbersCard
  id="card-clone-cost"
  title="Стоимость structured clone"
  data-lesson-visual
  stats={[
    { label: "Скорость клона плоского объекта", value: "~1 мс / МБ" },
    { label: "Клон объекта на 30 МБ", value: "~30 мс блокировки отправителя" },
    { label: "Transfer (ArrayBuffer)", value: "O(1), микросекунды" },
    { label: "Transferable: ArrayBuffer", value: "Отсоединяет отправителя (byteLength → 0)" },
    { label: "Transferable: ImageBitmap", value: "Передаётся, источник становится закрытым" },
    { label: "Transferable: OffscreenCanvas", value: "Эксклюзивное владение переходит" },
  ]}
/>

## Transferables: передача владения за O(1)

Побег от стоимости клона — механизм **transferable**. Передайте второй аргумент в `postMessage` — список transferable-объектов:

```js
postMessage({ pixels, width, height }, [pixels.buffer]);
```

Вместо копирования байтов браузер отдаёт нижележащую память принимающему потоку и **отсоединяет** её от отправителя. После передачи:
- `pixels.buffer.byteLength` равен `0` у отправителя
- Получатель имеет новый view с полными данными

Стоимость падает с O(size) до O(1). Transfer — правильный ход для больших бинарных payload-ов: bitmap-ы изображений, аудиобуферы, содержимое файлов.

**Типы transferable:** `ArrayBuffer`, `MessagePort`, `ImageBitmap`, `OffscreenCanvas`, `ReadableStream`, `WritableStream`, `TransformStream`.

## Что теряется при structured clone

При postMessage объекта с методами:

```js
class Point {
  constructor(x, y) { this.x = x; this.y = y; }
  distanceTo(other) { ... }
}
const p = new Point(1, 2);
worker.postMessage(p);
// Воркер получает: { x: 1, y: 2 } — метода distanceTo нет
```

Structured clone копирует свойства данных, но не имеет представления для функций или прототипных цепочек. По дизайну: функции ссылаются на замыкания, которые могут держать DOM-узлы или другое состояние только главного потока. Правило: сериализуйте данные, не поведение. Пересоздайте класс на принимающей стороне при необходимости.

<Quiz
  id="mid-quiz-clone-1"
  lessonSlug="03-structured-clone-and-transfer"
  lang="ru"
  question="Вы `postMessage`-ите Float32Array на 40 МБ воркеру, и отправляющий поток замирает на ~40 мс. Каков фикс?"
  choices={[
    { label: "Передать буфер: `postMessage(arr, [arr.buffer])` — владение переходит за O(1) вместо копии на 40 МБ", correct: true },
    { label: "Послать как JSON вместо этого", misconception: "JSON.stringify типизированного массива на 40 МБ намного медленнее structured clone и производит куда большую строку. Это делает хуже, не лучше." },
    { label: "Разбить на 40 отдельных вызовов postMessage по 1 МБ", misconception: "Суммарная стоимость клона — те же 40 МБ, теперь размазанные по 40 task hop-ам с 40-кратным оверхедом. Transfer убирает копию полностью." },
    { label: "Сжать gzip-ом перед отправкой", misconception: "Само сжатие — CPU-работа на отправляющем потоке, плюс распаковка на воркере. Transfer бесплатен; сжатие — нет." },
  ]}
/>

<Quiz
  id="mid-quiz-clone-2"
  lessonSlug="03-structured-clone-and-transfer"
  lang="ru"
  question="`onmessage` воркера получает объект, у которого были методы до отправки. Методы пропали. Почему?"
  choices={[
    { label: "Structured clone копирует только данные — прототипы и функции не клонируемы, поэтому методы отбрасываются", correct: true },
    { label: "Воркер срезал их ради безопасности", misconception: "Срезания ради безопасности нет. У structured clone просто нет представления для функций или цепочек прототипов — они не могут пересечь границу." },
    { label: "Методы там, но загружаются лениво", misconception: "Ничего ленивого тут нет. Клон произвёл плоский объект только со свойствами данных; методы никогда не пересекали границу." },
    { label: "postMessage шлёт только JSON", misconception: "postMessage использует structured clone, который богаче JSON (он обрабатывает Map, Date, ArrayBuffer). Но функции вне обоих." },
  ]}
/>

<MetaphorComplete
  id="mid-metaphor-clone-1"
  lessonSlug="03-structured-clone-and-transfer"
  lang="ru"
  setup="Когда вы postMessage-ите большой ArrayBuffer, по умолчанию делается глубокая копия (налог structured clone). Альтернатива перемещает владение буфером к принимающему потоку за константное время, оставляя его непригодным у отправителя. Как называется эта альтернатива?"
  accepted={["transfer", "transferable", "transferables", "transferable objects", "передача", "передать буфер", "трансфер"]}
  canonical="transfer"
  explanation="Передача (transfer) использует второй аргумент postMessage — список transferable-объектов: `postMessage(data, [data.buffer])`. Вместо копирования байтов браузер отдаёт нижележащую память принимающему потоку и отсоединяет её от отправителя (byteLength становится 0). Стоимость падает с O(size) до O(1). Transferable включают ArrayBuffer, MessagePort, ImageBitmap и OffscreenCanvas — все тяжёлые бинарные типы, где копия была бы дорогой."
/>

<NumberDrill
  id="mid-drill-clone-1"
  lessonSlug="03-structured-clone-and-transfer"
  lang="ru"
  question="Structured clone плоского объекта стоит примерно 1 мс на МБ. Воркеру шлётся объект на 30 МБ по значению. Примерно сколько миллисекунд отправляющий поток блокируется на клоне?"
  expected={30}
  tolerance={5}
  unit="мс"
  explanation="При ~1 мс/МБ structured clone на 30 МБ блокирует отправителя на ~30 мс — больше половины бюджета кадра 60 fps несколько раз подряд, и сам по себе почти длинная задача. Это скрытая цена «просто перенесём в воркер»: клон гоняется синхронно на потоке, который вы пытались разблокировать. Передайте буфер вместо этого, или используйте SharedArrayBuffer, и 30 мс исчезают."
/>

<FadedExample
  client:visible
  id="sr-faded-clone-1"
  lessonSlug="03-structured-clone-and-transfer"
  lang="ru"
  title="Передать буфер воркеру вместо клонирования"
  steps={{
    solved: (
      <pre><code>{`// main.js
const worker = new Worker('process.js');
const pixels = new Uint8ClampedArray(width * height * 4);
// Второй аргумент передаёт буфер: O(1), без копии.
// pixels.buffer после этого отсоединён здесь.
worker.postMessage({ pixels, width, height }, [pixels.buffer]);

// process.js
self.onmessage = (e) => {
  const { pixels, width, height } = e.data;
  applyFilter(pixels, width, height);
  // Передать обратно тем же способом.
  self.postMessage({ pixels }, [pixels.buffer]);
};`}</code></pre>
    ),
    semi: {
      prompt: (
        <p>Заполните аргумент списка передачи на обоих вызовах postMessage, чтобы буфер пикселей двигался без копии.</p>
      ),
      blanks: [
        { id: "send", expected: /\[\s*pixels\.buffer\s*\]/i, placeholder: "список передачи для отправки" },
        { id: "back", expected: /\[\s*pixels\.buffer\s*\]/i, placeholder: "список передачи для возврата" },
      ],
    },
    blank: {
      prompt: (
        <p>Вы постите мульти-МБ <code>Uint8ClampedArray</code> пикселей воркеру по значению, и отправляющий поток блокируется на ~25 мс на structured clone. Перепишите вызовы postMessage так, чтобы буфер двигался за O(1). Какое структурное изменение?</p>
      ),
      reveal: (
        <p>Передайте второй аргумент в <code>postMessage</code> — список передачи, содержащий нижележащий <code>ArrayBuffer</code> массива: <code>postMessage(&#123; pixels &#125;, [pixels.buffer])</code>. Браузер отдаёт память принимающему потоку вместо копирования; буфер отсоединяется у отправителя (его <code>byteLength</code> становится 0). Сделайте то же на ответе воркера. Стоимость клона падает с O(size) до O(1) в обе стороны. Ограничение: отправитель не должен использовать буфер после передачи — если ему всё ещё нужны данные, transfer — неправильный инструмент, и SharedArrayBuffer — правильный.</p>
      ),
    },
  }}
/>

<RFCQuiz
  id="sr-rfc-clone-1"
  lessonSlug="03-structured-clone-and-transfer"
  lang="ru"
  question="Какая спецификация определяет алгоритм structured clone, используемый postMessage, IndexedDB и Cache API?"
  choices={[
    { rfc: "HTML Living Standard § structured data", title: "Определяет StructuredSerialize / StructuredDeserialize и механизм transferable-объектов", correct: true },
    { rfc: "ECMA-262 (JavaScript)", title: "Определяет объекты и язык, но не алгоритм кросс-realm сериализации" },
    { rfc: "W3C Service Workers", title: "Использует structured clone для сообщений, но не определяет алгоритм" },
    { rfc: "WHATWG Streams", title: "Определяет ReadableStream / WritableStream, не связано со structured clone" },
  ]}
/>

<Inset kind="why" lang="ru">
Почему structured clone синхронен и почему он блокирует отправителя? Потому что спецификация JavaScript гарантирует: вызов postMessage — единственная, атомарная операция: данные полностью сериализуются до возврата функции. Если бы сериализация была асинхронной, вы могли бы мутировать данные между вызовом postMessage и моментом, когда воркер их читает, нарушая изоляцию. Синхронная стоимость — цена гарантии изоляции. Механизм transfer обходит это, перемещая указатель памяти вместо байтов — ОС может атомарно передать владение регионом памяти без копирования.
</Inset>

</Explanation>

<KeyTakeaway>Structured clone делает глубокую копию данных со скоростью ~1 мс/МБ — бесплатно для маленьких объектов, дорого для больших буферов. Передача ArrayBuffer перемещает владение за O(1): ссылка отправителя отсоединяется, получатель получает байты. Используйте transfer для всех больших бинарных payload-ов; используйте SharedArrayBuffer, когда оба потока нуждаются в параллельном доступе.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="03-structured-clone-and-transfer-retrieval"
  lang="ru"
  questions={[
    { q: "Что structured clone обрабатывает и что отбрасывает?", a: "Обрабатывает: объекты, массивы, Map, Set, Date, типизированные массивы, Blob, ArrayBuffer. Отбрасывает: функции (молча), DOM-узлы (бросает), методы прототипов классов (выживают только свойства данных). Алгоритм определён в HTML Living Standard § structured data." },
    { q: "Как передать ArrayBuffer вместо клонирования, и что происходит со ссылкой отправителя?", a: "Поместите буфер во второй аргумент (список передачи): postMessage({ pixels }, [pixels.buffer]). Браузер отдаёт память принимающему потоку за O(1). Буфер отправителя отсоединяется — его byteLength становится 0, любой доступ бросает исключение. Получатель имеет новый view с полными данными." },
    { q: "Когда следует использовать SharedArrayBuffer вместо transfer?", a: "Когда оба потока нуждаются в параллельном доступе к одним данным — например, пул WASM-потоков, где главный поток и рабочие потоки все читают/пишут одинаковый буфер кадров. Transfer перемещает эксклюзивное владение; SharedArrayBuffer разделяет его. SharedArrayBuffer требует cross-origin isolation (заголовки COOP + COEP)." },
  ]}
/>

<Recap lang="ru">
`postMessage` делает глубокую копию данных через алгоритм structured clone со скоростью ~1 мс на МБ — объект на 30 МБ блокирует отправляющий поток на ~30 мс, сводя на нет пользу от переноса работы вне главного потока. Фикс — механизм transferable: поместите ArrayBuffer во второй аргумент postMessage, и браузер отдаёт владение получателю за O(1), отсоединяя ссылку отправителя. Функции и методы прототипов не могут пересекать границу — structured clone это сериализатор данных, не копировщик объектов. Для случаев, когда оба потока нуждаются в параллельном доступе к одной памяти, ответ — SharedArrayBuffer, но он требует cross-origin isolation.
</Recap>
