---
concepts:
- transferable-handoff
- structured-clone-cost
- service-worker-waitUntil
- cache-first-strategy
- offscreen-canvas-transfer
estMin: 14
lang: ru
level: senior
order: 9
prereqs: []
slug: quiz-code
sources:
- https://developer.mozilla.org/en-US/docs/Web/API/Web_Workers_API/Transferable_objects
- https://web.dev/articles/service-worker-lifecycle
- https://developer.mozilla.org/en-US/docs/Web/API/OffscreenCanvas
status: ready
summary: "Читай реальные сниппеты воркера, postMessage и service worker — предскажи поведение во время выполнения или баг, потом выбери фикс с наибольшим рычагом, который сеньор делает первым."
title: 'Воркеры: чтение кода и сниппетов'
track: browser
unit: 04-workers
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Баги воркеров прячутся на границе сообщений и в обработчике жизненного цикла, а не в алгоритме. Прочитай каждый сниппет, предскажи, что реально происходит на границе, и выбери фикс прежде, чем тянуться к профайлеру.
</Hook>

<Goal lang="ru">
Отработай цикл, который ты гоняешь в каждом инциденте с воркером: прочитать вызов postMessage или обработчик жизненного цикла, найти скрытый clone-налог, или детачнутый буфер, или забытый waitUntil, и взять фикс с наибольшим рычагом.
</Goal>

## Сниппет 1 — клон, который не переместился

```js
// main.js — выносим тяжёлый фильтр изображения в воркер
const worker = new Worker('filter.js', { type: 'module' });
const pixels = new Uint8ClampedArray(width * height * 4); // ~32 МБ

worker.postMessage({ pixels, width, height });   // (A)
applyOtherUiWork();                                // (B) выполняется на ~32 мс позже
```

<Quiz
  id="04-workers-code-1"
  lessonSlug="04-workers"
  lang="ru"
  question="Строка (B) выполняется примерно на 32 мс позже ожидаемого, хотя фильтр идёт в воркере. Что происходит на строке (A) и какой однострочный фикс?"
  choices={[
    { label: "postMessage синхронно делает structured clone буфера на 32 МБ на главном потоке (~1 мс/МБ), так что отправитель блокируется ~32 мс до возврата. Фикс: передать через transfer — postMessage({ pixels, width, height }, [pixels.buffer]) — владение переходит за O(1).", correct: true },
    { label: "Задержку строки (B) даёт старт воркера (5–20 мс).", misconception: "Старт воркера идёт вне вызывающего потока и не блокирует строку (B). Блокировка — это синхронный structured clone буфера на 32 МБ на главном потоке." },
    { label: "Строка (B) задержана, потому что воркер ещё не ответил.", misconception: "Строка (B) ничего не await — она выполняется сразу после возврата postMessage. Задержка — синхронный клон внутри postMessage, а не ответ воркера." },
    { label: "Чинить нечего — 32 мс это неизбежная цена использования воркера.", misconception: "Это полностью избежимо. Передай буфер в transfer-списке, и копия станет O(1)-передачей владения; ~32 мс исчезают." },
  ]}
/>

## Сниппет 2 — буфер, использованный после transfer

```js
// main.js
const buf = new Float32Array(1_000_000);
fillSamples(buf);

worker.postMessage(buf, [buf.buffer]);   // передан через transfer
const sum = buf.reduce((a, x) => a + x); // (C) снова читает buf
```

<Quiz
  id="04-workers-code-2"
  lessonSlug="04-workers"
  lang="ru"
  question="Что происходит на строке (C) и что это говорит о семантике transfer?"
  choices={[
    { label: "buf детачнут после transfer — его byteLength равен 0 и length равен 0, так что reduce видит пустой массив (или бросает на доступе в строгих путях). Transfer передаёт эксклюзивное владение; отправитель не должен трогать буфер после. Если данные ещё нужны — transfer не тот инструмент, бери SharedArrayBuffer.", correct: true },
    { label: "buf сохраняет значения — transfer копирует, так что обе стороны держат рабочий вью.", misconception: "Transfer — противоположность копии. Он передаёт память приёмнику и детачит вью отправителя. Второй рабочей копии у отправителя нет." },
    { label: "Строка (C) суммирует то, что воркер успел записать обратно.", misconception: "Здесь нет разделяемой памяти — transfer это односторонняя передача владения, а не живой общий вью. Буфер отправителя просто детачнут и пуст." },
    { label: "reduce автоматически выполняется на потоке воркера.", misconception: "reduce выполняется синхронно на вызывающем потоке. Суть в том, что читаемый им буфер детачнут, а не в переносе вычисления." },
  ]}
/>

## Сниппет 3 — install-обработчик, теряющий свой кэш

```js
// sw.js
self.addEventListener('install', () => {
  caches.open('shell-v3').then(cache =>
    cache.addAll(['/index.html', '/app.4f3a1c.js'])
  );
});
```

<Quiz
  id="04-workers-code-3"
  lessonSlug="04-workers"
  lang="ru"
  question="Под нагрузкой этот service worker иногда активируется с пустым или наполовину заполненным кэшем. В чём дефект и фикс?"
  choices={[
    { label: "install-обработчик не вызывает event.waitUntil, поэтому браузер может продвинуть жизненный цикл (или убить простаивающий воркер) до того, как промис cache.addAll завершится. Фикс: install-обработчик принимает event и зовёт event.waitUntil(caches.open('shell-v3').then(c => c.addAll([...]))).", correct: true },
    { label: "addAll не атомарен, так что частичные кэши ожидаемы — повторяй на activate.", misconception: "addAll отклоняется целиком, если хоть один запрос упал; частичное наполнение здесь — от неподождавшегося промиса, а не от addAll. Обёртка в event.waitUntil держит воркер живым до завершения." },
    { label: "shell-v3 коллизит со старым кэшем и очищается.", misconception: "Версионные имена — это как раз способ избежать коллизий, и shell-v3 здесь ничто не удаляет. Баг — пропущенный waitUntil, дающий воркеру продвинуться рано." },
    { label: "URL ассетов должны быть относительными, а не абсолютными.", misconception: "Абсолютные same-origin пути кэшируются нормально. Недетерминизм — это неподождавшийся промис, чинится event.waitUntil." },
  ]}
/>

## Сниппет 4 — передача OffscreenCanvas

```js
// main.js
const canvas = document.querySelector('#viz');
const offscreen = canvas.transferControlToOffscreen();
worker.postMessage({ offscreen }, [offscreen]);

// позже, на главном потоке:
const ctx = canvas.getContext('2d');   // (D)
ctx.fillRect(0, 0, 100, 100);          // (E)
```

<Quiz
  id="04-workers-code-4"
  lessonSlug="04-workers"
  lang="ru"
  question="Воркер рендерит нормально, но строки (D)–(E) на главном потоке ведут себя неправильно. Что происходит и почему?"
  choices={[
    { label: "Контроль передан воркеру, так что canvas детачнут на главном потоке — getContext / draw-вызовы бросают (или уходят в никуда). Владение OffscreenCanvas эксклюзивно: после transfer рисовать может только воркер. Если рисовать должны оба потока — нужны два canvas.", correct: true },
    { label: "И воркер, и главный поток могут рисовать в один и тот же bitmap.", misconception: "Платформа запрещает двум потокам гонять один bitmap. transferControlToOffscreen делает владение эксклюзивным — главный поток больше не может рисовать в этот canvas." },
    { label: "Рисование на главном потоке ставится в очередь, пока воркер не освободит canvas.", misconception: "Механизма освобождения-и-возврата нет. Transfer постоянен на время жизни canvas; контекст главного потока просто недоступен." },
    { label: "getContext('2d') возвращает свежий независимый контекст для главного потока.", misconception: "После transferControlToOffscreen у элемента нет своего контекста рендеринга — bitmap'ом владеет воркер. Понадобится отдельный элемент canvas." },
  ]}
/>

<Recap lang="ru">
Каждый инцидент с воркером читается на границе, а не в алгоритме: postMessage крупного буфера по значению блокирует отправителя на синхронном structured clone — передавай ArrayBuffer через transfer; переданный буфер детачнут у отправителя — не читай его после (используй SharedArrayBuffer, если данные нужны обоим); install у service worker должен оборачивать асинхронное наполнение кэша в event.waitUntil, иначе воркер продвинется или умрёт рано; а transferControlToOffscreen делает владение canvas эксклюзивным, так что главный поток больше не рисует. Сначала читай обработчики сообщений и жизненного цикла — там и стоимость, и баги.
</Recap>
