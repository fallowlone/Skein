---
concepts:
- dedicated-worker
- shared-worker
- module-worker
- OffscreenCanvas
- DedicatedWorkerGlobalScope
- worker-pool
deepensInto:
- 03-structured-clone-and-transfer
- 07-worker-pools-and-observability
estMin: 14
lang: ru
lessonType: topic
level: middle
order: 2
prereqs:
- browser/04-workers/01-what-workers-are
slug: 02-web-worker-mechanics
sources:
- https://developer.mozilla.org/en-US/docs/Web/API/Web_Workers_API
- https://developer.mozilla.org/en-US/docs/Web/API/OffscreenCanvas
- https://html.spec.whatwg.org/multipage/workers.html
spiral:
- encapsulation
status: ready
summary: Dedicated vs shared vs module-воркеры, лазейка OffscreenCanvas и почему URL
  скрипта воркера должен быть same-origin.
title: 'Механика web workers: dedicated, shared и OffscreenCanvas'
track: browser
unit: 04-workers
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import MetaphorComplete from "~/components/pedagogy/MetaphorComplete.astro";
import NumbersCard from "~/components/layout/NumbersCard.astro";
import SequenceDiagram from "~/components/diagram/SequenceDiagram.astro";

<Hook>
Вы знаете, что воркеры существуют. Но `new Worker('script.js')` создаёт dedicated-поток, умирающий с вкладкой — что делать, если пяти вкладкам нужно одно WebSocket-соединение, или WebGL-рендеринг должен пережить перегрузку главного потока?
</Hook>

<Crux>Dedicated, shared и module-воркеры покрывают разные области; OffscreenCanvas — единственная DOM-лазейка для рендеринга из воркера.</Crux>

<Explanation>

## Dedicated-воркеры

`new Worker('worker.js')` поднимает свежий JS-realm на своём OS-потоке, со своим event loop, своим глобалом (`DedicatedWorkerGlobalScope`) и без доступа к `document`, `window` или DOM. Он может использовать `fetch`, `IndexedDB`, `WebSocket`, `OffscreenCanvas`, таймеры, `importScripts`/ES-модули. Коммуникация — исключительно `postMessage` в обе стороны.

Воркер привязан к создавшей его странице: закройте вкладку — и воркер умирает. Используйте его для всего CPU-bound, что иначе пробило бы бюджет задачи 50 мс — парсинг больших JSON-payload-ов, фильтры изображений, криптографическое хеширование, диффинг, сжатие, подсветка синтаксиса, физика.

**Ограничение same-origin.** URL скрипта воркера должен быть same-origin (или blob-URL). Нельзя загрузить воркер прямо со стороннего CDN, не проксировав его через свой origin. Это та же origin-граница, что защищает остальную модель безопасности браузера.

<SequenceDiagram
  label="Страница постит входные данные воркеру; воркер вычисляет на своём потоке и постит результат обратно. Каждая сторона получает через свой обработчик onmessage."
  hue="var(--d-frontend)"
  actors={["Главный поток", "Воркер"]}
  messages={[
    { from: "Главный поток", to: "Воркер", label: "postMessage(data) → worker.onmessage" },
    { from: "Воркер", to: "Главный поток", label: "postMessage(result) → main onmessage" },
  ]}
  caption="Коммуникация — исключительно postMessage в обе стороны. Страница шлёт вход вниз (срабатывает onmessage воркера, и он вычисляет вне главного потока); воркер шлёт результат вверх (срабатывает onmessage главного потока и применяет его). Без общего состояния, без блокировки."
/>

## Shared-воркеры

`new SharedWorker('s.js')` создаёт воркер, разделяемый между всеми вкладками одного origin. Каждая вкладка коннектится через `MessagePort`, и воркер живёт, пока жива хоть одна вкладка — полезно для одного общего WebSocket-соединения или согласованного кеша между вкладками. Поддержка слабее, чем у dedicated-воркеров, и отладка тяжелее, потому что время жизни воркера отвязано от любой отдельной вкладки.

## Module-воркеры

Если вы начинаете новый проект сегодня, способ загрузки зависимостей воркером влияет на размер бандла и поддерживаемость. Опция `{ type: 'module' }` меняет эту стратегию:

- **Classic-воркер** — использует `importScripts()` для синхронной загрузки зависимостей.
- **Module-воркер** — использует обычный статический `import`. Это даёт tree-shaking, top-level `await` и тот же граф модулей, что и остальное приложение.

Module-воркеры — современный дефолт. Classic `importScripts` остаётся только для легаси-кода и случаев динамической сборки имени скрипта в рантайме.

<NumbersCard
  id="card-worker-mechanics-numbers"
  title="Сравнение вариантов воркеров"
  data-lesson-visual
  stats={[
    { label: "Время жизни dedicated-воркера", value: "Привязан к создавшей странице" },
    { label: "Время жизни shared-воркера", value: "Последняя подписанная вкладка" },
    { label: "Module-воркер", value: "Опция { type: 'module' }" },
    { label: "Оверхед старта воркера", value: "5–20 мс + парсинг скрипта" },
    { label: "Максимальный размер пула (правило)", value: "hardwareConcurrency − 1" },
  ]}
/>

## OffscreenCanvas: единственная DOM-лазейка

Воркер не может тронуть DOM, но canvas — особый случай: bitmap-канвас не является частью DOM-дерева, это просто буфер пикселей. `OffscreenCanvas` — канвас, отвязанный от элемента `<canvas>`.

Паттерн:
1. Главный поток вызывает `canvas.transferControlToOffscreen()`, получает `OffscreenCanvas`.
2. Передаёт его воркеру через transfer-список postMessage.
3. Воркер получает 2D-контекст или WebGL-контекст и рисует целиком на своём потоке.
4. Браузер отражает результат в видимый `<canvas>` — без участия главного потока.

Это делает возможными плавные 60 fps WebGL-визуализации, рендеринг карт и игры даже когда главный поток забит.

Воркер также может создать standalone `new OffscreenCanvas(w, h)` чисто для off-screen рендеринга — генерации текстур, обработки изображений — и вернуть результат как transferable `ImageBitmap`.

**Ограничение исключительности.** OffscreenCanvas, чей контроль был передан, нельзя также рисовать из главного потока — владение эксклюзивно, ровно как у переданного `ArrayBuffer`. Если оба потока должны рисовать, нужны два canvas или другая архитектура; платформа намеренно запрещает двум потокам гонку на одном bitmap.

<Quiz
  id="mid-quiz-web-mechanics-1"
  lessonSlug="02-web-worker-mechanics"
  lang="ru"
  question="Пяти вкладкам браузера одного origin нужно одно общее WebSocket-соединение, управляемое в одном месте. Какой тип воркера подходит?"
  choices={[
    { label: "SharedWorker — он разделяется между всеми вкладками origin, поэтому одно WebSocket-соединение обслуживает все пять вкладок через MessagePort каждой", correct: true },
    { label: "Пять dedicated-воркеров, по одному на вкладку", misconception: "Это пять WebSocket-соединений и пять независимых воркеров. SharedWorker — инструмент для разделяемых соединений между вкладками." },
    { label: "Service worker — он перехватывает fetch-запросы от всех вкладок", misconception: "Service worker перехватывает HTTP-запросы, а не WebSocket-соединения, управляемые в JS. SharedWorker — правильный инструмент для общего состояния между вкладками." },
    { label: "Module-воркер с { type: 'module' }", misconception: "Module-воркер означает способ загрузки скриптов воркером (ES-модули вместо importScripts). Это всё ещё dedicated-воркер, привязанный к одной вкладке." },
  ]}
/>

<Quiz
  id="mid-quiz-web-mechanics-2"
  lessonSlug="02-web-worker-mechanics"
  lang="ru"
  question="После `canvas.transferControlToOffscreen()` главный поток вызывает `ctx.fillRect(0,0,100,100)`. Что происходит?"
  choices={[
    { label: "Бросает исключение — контроль canvas передан, и главный поток больше не может рисовать в него", correct: true },
    { label: "Оба потока рисуют в один canvas одновременно", misconception: "Владение OffscreenCanvas эксклюзивно. После передачи оригинальный canvas отсоединён — главный поток не может рисовать и не должен, чтобы избежать гонок." },
    { label: "Рисование главного потока ставится в очередь, пока воркер не освободит canvas", misconception: "Нет ни очереди, ни механизма освобождения. Передача постоянна на время жизни OffscreenCanvas. Оба потока не могут держать один контекст рисования." },
    { label: "Рисование главного потока молча игнорируется", misconception: "Вызов методов рисования на отсоединённом canvas бросает DOMException, он не молча успешен." },
  ]}
/>

<MetaphorComplete
  id="mid-metaphor-web-mechanics-1"
  lessonSlug="02-web-worker-mechanics"
  lang="ru"
  setup="Воркер, разделяемый между всеми вкладками одного origin — каждая вкладка коннектится через отдельный порт — называется как?"
  accepted={["shared worker", "sharedworker", "shared web worker", "SharedWorker", "шэредворкер"]}
  canonical="SharedWorker"
  explanation="SharedWorker отличается от dedicated-воркера (принадлежит одной странице) и service worker (сетевой прокси). Он создаёт один JS-realm, разделяемый всеми вкладками origin, каждая с MessagePort в него. Это делает его правильным инструментом для разделяемого WebSocket-соединения, кеша между вкладками или любого состояния, которое должно быть согласованным между вкладками."
/>

<Inset kind="why" lang="ru">
Почему OffscreenCanvas существует, если воркеры не могут трогать DOM? Canvas особый, потому что его вывод — плоский bitmap — у него нет DOM-событий, нет CSS, нет дерева доступности. Пайплайн рендеринга браузера читает bitmap и компонует его; ничему остальному в DOM-дереве не важно, кто писал пиксели. Это позволяет безопасно отдать запись пикселей любому потоку. Обобщить это на весь DOM потребовало бы блокировок всего дерева — а это именно то, что модель воркеров избегает.
</Inset>

</Explanation>

<KeyTakeaway>Dedicated-воркеры принадлежат одной странице; shared-воркеры охватывают все вкладки origin; module-воркеры используют ES-imports вместо importScripts. OffscreenCanvas позволяет воркеру рендерить 2D/WebGL без DOM — владение эксклюзивно и передача постоянна.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="02-web-worker-mechanics-retrieval"
  lang="ru"
  questions={[
    { q: "Какие три варианта воркеров и когда какой использовать?", a: "Dedicated-воркер: один к одному со страницей, для вычислений вне главного потока. SharedWorker: разделяется между вкладками origin, для разделяемых соединений или состояния между вкладками. Module-воркер: использует ES-модули вместо importScripts — современный дефолт для dedicated-воркеров." },
    { q: "Почему скрипт воркера должен быть same-origin (или blob-URL)?", a: "Политика same-origin не даёт кроссоригинным скриптам работать в вашем security-контексте. Загрузка воркера с CDN позволила бы этому CDN выполнять произвольный JS в origin вашей страницы. Проксируйте скрипт через свой origin или создайте blob-URL из загруженного текста." },
    { q: "После canvas.transferControlToOffscreen(), может ли главный поток рисовать в этот canvas?", a: "Нет. transferControlToOffscreen отсоединяет canvas от главного потока — последующие вызовы рисования бросают исключение. Владение эксклюзивно: после передачи воркеру только воркер может рисовать в него. Если оба потока должны рисовать, нужны два отдельных canvas-элемента." },
  ]}
/>

<Recap lang="ru">
Dedicated-воркер — свежий JS-realm, event loop и OS-поток, привязанный к создавшей его странице — умирает, когда страница закрывается. Shared-воркер охватывает все вкладки origin через MessagePort-соединения, живя пока жива хоть одна вкладка. Module-воркеры используют ES-imports вместо legacy `importScripts`. OffscreenCanvas — единственный способ воркерам рендерить: передайте контроль canvas воркеру, и он рисует 2D или WebGL на своём потоке, браузер компонует результат — но владение эксклюзивно и передача постоянна. Теперь, выбирая воркер, ты знаешь критерии: dedicated — для вычислений в рамках одной вкладки, shared — для состояния, общего для нескольких вкладок, module-стиль — для современного графа модулей, OffscreenCanvas — когда узкое место в самом GPU-рендеринге.
</Recap>
