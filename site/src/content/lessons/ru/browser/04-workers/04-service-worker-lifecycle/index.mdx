---
concepts:
- service-worker
- install-event
- activate-event
- fetch-event
- cache-api
- skipWaiting
- cache-first
- network-first
- stale-while-revalidate
deepensInto:
- 06-service-worker-edge-cases
estMin: 15
lang: ru
lessonType: topic
level: middle
order: 4
prereqs:
- browser/04-workers/01-what-workers-are
slug: 04-service-worker-lifecycle
sources:
- https://developer.mozilla.org/en-US/docs/Web/API/Service_Worker_API
- https://web.dev/articles/service-worker-lifecycle
- https://web.dev/articles/workbox
spiral:
- statefulness
status: ready
summary: 'Register → install → waiting → activate → fetch: жизненный цикл, который
  делает возможными офлайн-приложения, мгновенные повторные загрузки и ловушку waiting-состояния.'
title: Жизненный цикл service worker и стратегии кеширования
track: browser
unit: 04-workers
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import TraceScenario from "~/components/pedagogy/TraceScenario.astro";
import NumbersCard from "~/components/layout/NumbersCard.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
Вы деплоите новый service worker, перезагружаете страницу — и видите старое поведение. Никакой ошибки: новый воркер установился нормально. Он просто ждёт, пока вы закроете все вкладки.
</Hook>

<Crux>Service worker перехватывает fetch после активации; waiting-состояние блокирует новую версию над открытыми страницами без skipWaiting.</Crux>

<Explanation>

## Регистрация и область видимости

`navigator.serviceWorker.register('sw.js')` запускает жизненный цикл. Scope по умолчанию — директория `sw.js`: воркер по пути `/app/sw.js` контролирует все страницы под `/app/`. Можно сузить: `register('sw.js', { scope: '/app/checkout/' })`.

У service worker нет DOM и нет постоянного глобального состояния — браузер убивает и перезапускает его свободно ради экономии памяти. Всё постоянное состояние живёт в **Cache API** или **IndexedDB**. Он работает на собственном потоке, независимо от любой страницы — он может получать push-уведомления и запускать background sync даже когда нет ни одной открытой вкладки.

## Жизненный цикл

```
register → install → [waiting] → activate → idle ↔ running
```

**install** — срабатывает, когда браузер впервые парсит новый воркер. Здесь выполняется pre-cache app shell:

```js
self.addEventListener('install', event => {
  event.waitUntil(
    caches.open('v1').then(cache => cache.addAll(['/app.js', '/index.html']))
  );
});
```

`event.waitUntil(promise)` говорит браузеру: не переходить дальше по жизненному циклу, пока промис не завершится. Забудьте его — браузер может убить воркер на полпути к заполнению кеша.

**waiting** — свежеустановленный воркер не берёт под контроль уже открытые страницы по умолчанию. Он ждёт, пока каждая вкладка со старым воркером закроется. Это предотвращает ситуацию, когда страница запускает старый HTML с новыми кешированными ресурсами.

**activate** — срабатывает после ожидания. Здесь удаляются старые кеши:

```js
self.addEventListener('activate', event => {
  event.waitUntil(
    caches.keys().then(keys =>
      Promise.all(keys.filter(k => k !== 'v1').map(k => caches.delete(k)))
    )
  );
});
```

**fetch** — перехватывает каждый сетевой запрос в области видимости. `event.respondWith(response)` определяет, что получит страница.

<FlowDiagram
  label="Жизненный цикл service worker как упорядоченный конечный автомат: срабатывает install, затем воркер ждёт в состоянии installed (waiting), затем срабатывает activate, затем он activated и обрабатывает fetch, и наконец становится redundant, когда его заменяют."
  hue="var(--d-frontend)"
  perRow={5}
  nodes={[
    { id: "install", label: "install", sub: "pre-cache shell" },
    { id: "waiting", label: "installed", sub: "(waiting)" },
    { id: "activate", label: "activate", sub: "очистка кешей" },
    { id: "activated", label: "activated", sub: "обрабатывает fetch" },
    { id: "redundant", label: "redundant", sub: "заменён" },
  ]}
  edges={[
    { from: "install", to: "waiting" },
    { from: "waiting", to: "activate", label: "вкладки / skipWaiting" },
    { from: "activate", to: "activated" },
    { from: "activated", to: "redundant" },
  ]}
  caption="Всегда этот порядок. Переход waiting → activate — это ловушка: по умолчанию он срабатывает, только когда закроется каждая старая вкладка — skipWaiting() его перепрыгивает. Когда более новый воркер берёт верх, старый activated-воркер становится redundant."
/>

<NumbersCard
  id="card-sw-lifecycle-ru"
  title="Состояния жизненного цикла service worker"
  data-lesson-visual
  stats={[
    { label: "install → activate", value: "Только после закрытия всех старых вкладок (без skipWaiting)" },
    { label: "Убийство idle-воркера браузером", value: "Секунды после последнего события" },
    { label: "Проверка состояния (DevTools)", value: "Application → Service Workers" },
    { label: "Кеш sw.js браузером (по умолчанию)", value: "До 24 часов — используйте Cache-Control: no-cache" },
  ]}
/>

## Практика: Cache-Control: no-cache для sw.js

Браузер кеширует файл `sw.js` по HTTP-кешированию — по умолчанию до 24 часов. Это означает: вы задеплоили новый воркер, но браузер подаёт пользователям старый файл в течение суток, и обновление до них не доходит.

Правильная настройка — отдавать `sw.js` с заголовком `Cache-Control: no-cache` (или `max-age=0, must-revalidate`). Тогда браузер каждый раз на навигации перепроверяет файл у сервера и видит обновление немедленно.

## skipWaiting + clients.claim()

Чтобы активироваться немедленно, не дожидаясь закрытия вкладок:

```js
// install-обработчик
self.skipWaiting();

// activate-обработчик
self.addEventListener('activate', event => {
  event.waitUntil(clients.claim());
});
```

`skipWaiting()` — новый воркер активируется немедленно, даже при открытых страницах.
`clients.claim()` — новый воркер берёт под контроль все открытые страницы в своей области.

Используйте осознанно. Без content-hashed имён файлов ресурсов `skipWaiting` — это способ задеплоить version-mismatch failure mode.

## Стратегии кеширования

Внутри `fetch`-обработчика выбирается стратегия в зависимости от типа запроса:

**Cache-first** — смотреть в кеш, при промахе идти в сеть. Подходит для immutable content-hashed статики (`app.4f3a1c.js`):
```js
event.respondWith(caches.match(event.request).then(r => r || fetch(event.request)));
```

**Network-first** — сначала сеть, при сбое — кеш. Подходит для API-ответов, которые должны быть свежими, но должны деградировать gracefully офлайн.

**Stale-while-revalidate** — отдать кешированную копию немедленно, в фоне запросить свежую и обновить кеш. Подходит для контента, который может быть слегка устаревшим (аватары, ленты).

**Network-only** и **cache-only** — вырожденные края.

Библиотека Workbox даёт эти стратегии декларативно с сопоставлением маршрутов, но сами стратегии — это десять строк кода `fetch`-обработчика.

<DragOrder
  id="mid-drag-sw-lifecycle-1"
  lessonSlug="04-service-worker-lifecycle"
  lang="ru"
  prompt="Расставьте события жизненного цикла service worker от регистрации до стабильного состояния."
  items={[
    { id: "register", label: "register(): браузер скачивает и парсит sw.js" },
    { id: "install", label: "событие install: pre-cache app shell" },
    { id: "wait", label: "waiting: новый воркер ждёт, пока старые страницы закроются (без skipWaiting)" },
    { id: "activate", label: "событие activate: очистка старых кешей" },
    { id: "fetch", label: "fetch-события: перехват и ответ на запросы" },
  ]}
  correctOrder={["register", "install", "wait", "activate", "fetch"]}
/>

<Quiz
  id="mid-quiz-sw-1"
  lessonSlug="04-service-worker-lifecycle"
  lang="ru"
  question="Вы деплоите новый service worker, перезагружаете страницу — и видите старое поведение. Почему?"
  choices={[
    { label: "Новый воркер в состоянии 'waiting' — он не возьмёт контроль, пока все вкладки со старым воркером не закроются, если не вызвать skipWaiting()", correct: true },
    { label: "Service worker кеширует себя и игнорирует новые версии", misconception: "Браузер скачивает и устанавливает новый воркер. Он просто не активирует его поверх открытых страниц по умолчанию — это waiting-состояние, а не баг кеширования." },
    { label: "Нужно вручную отменять регистрацию старого воркера при каждом деплое", misconception: "Ручная отмена регистрации не нужна. skipWaiting() + clients.claim() обрабатывает передачу управления; без них то же делает закрытие всех вкладок." },
    { label: "Перезагрузка не проверяет service worker повторно", misconception: "Браузер перепроверяет файл sw.js на навигации. Новый воркер устанавливается — он просто ждёт активации." },
  ]}
/>

<Quiz
  id="mid-quiz-sw-2"
  lessonSlug="04-service-worker-lifecycle"
  lang="ru"
  question="Для какой задачи service worker является правильным инструментом?"
  choices={[
    { label: "Отдача app shell из кеша, чтобы повторный визит загружался мгновенно и работал офлайн", correct: true },
    { label: "Запуск CPU-тяжёлого фильтра изображений без заморозки страницы", misconception: "Это задача web worker — чистый вынос вычислений. Service worker — сетевой прокси, а не вычислительный поток." },
    { label: "Плавная анимация transform, пока главный поток занят", misconception: "Это задача animation worklet (или CSS-анимации на compositor). Service worker не касается рендеринга." },
    { label: "Разделение буфера памяти между двумя потоками", misconception: "Это SharedArrayBuffer + Atomics. Service worker общается через сообщения и Cache API, а не через разделяемую память." },
  ]}
/>

<TraceScenario
  id="mid-trace-sw-1"
  lessonSlug="04-service-worker-lifecycle"
  lang="ru"
  scenario="Страница включает COOP + COEP для использования многопоточного WASM. После деплоя WASM-модуль не запускается, отсутствует hero-изображение и сторонний analytics-скрипт. Что произошло?"
  steps={[
    { prompt: "COEP: require-corp заблокировал кроссоригинное hero-изображение и analytics-скрипт — они не отправляют заголовки Cross-Origin-Resource-Policy", reveal: "Верно. COEP требует, чтобы каждый кроссоригинный подресурс предоставлял CORP (или CORS). Hero-изображение на CDN и сторонний analytics-скрипт не настроены для этого — браузер заблокировал их. Сбой WASM — следствие: если COEP блокирует нужный ресурс, страница может не достичь состояния crossOriginIsolated, или команда отключила COEP ради изображения и потеряла SAB вместе с ним." },
    { prompt: "Бинарник WASM повреждён", reveal: "Бинарник WASM в порядке — сбой среды. crossOriginIsolated = false, потому что COEP либо не применился полностью, либо был отключён — SharedArrayBuffer не определён, и pthreads не могут инициализироваться." },
    { prompt: "Браузер не поддерживает SharedArrayBuffer", reveal: "Все современные браузеры поддерживают SAB — но только при cross-origin isolation. Проблема не в поддержке; она в условии COOP/COEP." },
    { prompt: "Service worker закешировал устаревший WASM-файл", reveal: "Устаревший кеш загрузил бы старый, но рабочий модуль, а не вызвал сбой запуска. Симптом — отсутствующие кроссоригинные ресурсы — прямо указывает на COEP." },
  ]}
/>

<Inset kind="why" lang="ru">
Зачем существует waiting-состояние? Представьте: вы деплоите версию N+1. Открытая вкладка загрузила HTML версии N и работает с воркером версии N. Если новый воркер активировался бы немедленно, он начал бы раздавать кешированные ресурсы версии N+1 странице, которая ожидала ресурсы версии N — version mismatch, способный сломать страницу на полпути. Waiting-состояние это предотвращает: пользователь, завершающий работу в открытой вкладке, завершит сессию с консистентным набором ресурсов. `skipWaiting` — аварийный выход, когда вы уверены, что ресурсы имеют content-hash (старые и новые сосуществуют) и новый воркер нужен немедленно.
</Inset>

</Explanation>

<KeyTakeaway>Service worker перехватывает все fetch-события в своей области после активации; новая версия ждёт закрытия всех старых вкладок. skipWaiting + clients.claim активирует немедленно. Стратегии кеширования — cache-first, network-first, stale-while-revalidate — это десять строк кода fetch-обработчика. Файл sw.js должен отдаваться с Cache-Control: no-cache.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="04-service-worker-lifecycle-retrieval"
  lang="ru"
  questions={[
    { q: "Опишите жизненный цикл service worker от register() до обработки fetch-событий.", a: "register() → браузер скачивает и парсит sw.js → событие install (pre-cache app shell, waitUntil) → waiting-состояние (новый воркер ждёт закрытия старых страниц, без skipWaiting) → событие activate (очистка старых кешей, clients.claim) → цикл idle/running, реагирующий на fetch, push и message-события." },
    { q: "Что такое waiting-состояние и как его обойти?", a: "Свежеустановленный service worker не берёт под контроль уже открытые страницы — он ждёт в 'waiting', пока все вкладки со старым воркером закроются. Это предотвращает version skew в середине сессии. Обход: вызвать self.skipWaiting() в install-обработчике и clients.claim() в activate-обработчике. Использовать осознанно — без content-hashed ресурсов немедленная активация приводит к module-mismatch." },
    { q: "Какие три основные стратегии кеширования и когда использовать каждую?", a: "Cache-first: отдать из кеша, при промахе — сеть. Для immutable content-hashed статики. Network-first: сначала сеть, при сбое — кеш. Для API-ответов, которые должны быть свежими, но должны деградировать офлайн. Stale-while-revalidate: немедленно отдать кешированную копию, в фоне обновить кеш. Для контента, который может быть слегка устаревшим (аватары, ленты)." },
  ]}
/>

<Recap lang="ru">
Service worker перехватывает каждый fetch-запрос в своей области после активации. Жизненный цикл: register, install (pre-cache), waiting (новый воркер ждёт закрытия старых вкладок), activate (очистка старых кешей), затем цикл idle/running fetch. `event.waitUntil(promise)` в любом обработчике не даёт браузеру убить воркер на полпути. `skipWaiting()` + `clients.claim()` обходит waiting-состояние, активируя немедленно — безопасно только с content-hashed ресурсами. Service worker не имеет постоянного глобального состояния; браузер агрессивно убивает его и перезапускает при следующем событии, поэтому всё постоянное состояние должно жить в Cache API или IndexedDB. Файл `sw.js` следует отдавать с `Cache-Control: no-cache`, иначе браузер кешируется его до 24 часов и обновление до пользователей не доходит.
</Recap>
