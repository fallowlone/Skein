---
concepts:
- worker-isolation
- structured-clone-vs-transfer
- service-worker-waiting
- shared-array-buffer-gate
- worker-pool-backpressure
- dom-bottleneck
estMin: 13
lang: ru
level: senior
order: 7
prereqs: []
slug: quiz-choice
sources:
- https://developer.mozilla.org/en-US/docs/Web/API/Web_Workers_API
- https://web.dev/articles/service-worker-lifecycle
- https://developer.mozilla.org/en-US/docs/Web/API/SharedArrayBuffer
status: ready
summary: "Тест с выбором на синтез по всему юниту — изоляция потоков, стоимость structured clone и transfer, ловушки жизненного цикла service worker, гейт COOP/COEP, backpressure пула и ошибка с DOM в воркере."
title: 'Воркеры: тест с выбором ответа'
track: browser
unit: 04-workers
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Шесть вопросов поперёк всего юнита. Каждый отражает архитектурное решение под реальной нагрузкой — какой примитив подходит, чего он стоит и какой сбой он не способен решить — а не определение для заучивания.
</Hook>

<Goal lang="ru">
Убедись, что выбираешь правильный воркер-примитив под задачу, рассуждаешь о стоимости передачи данных и распознаёшь ловушки жизненного цикла и изоляции, которые три примитива сами по себе не решают.
</Goal>

<Quiz
  id="04-workers-choice-1"
  lessonSlug="04-workers"
  lang="ru"
  question="Коллега переносит медленный ре-рендер React-списка в web worker, чтобы убрать джанк при скролле, и ничего не меняется. Почему и что реально помогает?"
  choices={[
    { label: "У воркера нет DOM — фаза commit в React мутирует реальные узлы и может идти только на главном потоке. Перенеси в воркер лишь вывод данных (filter/sort/aggregate) и верни маленький результат; сам commit чини виртуализацией списка и time-slicing реконсиляции.", correct: true },
    { label: "Воркер слишком медленный, чтобы поспевать за render-циклом React.", misconception: "Дело не в скорости — воркер буквально не может трогать DOM. Commit в React пишет реальные элементы, которыми владеет только главный поток. Вычисления переносятся, commit — нет." },
    { label: "React не работает внутри воркеров без полифила.", misconception: "Нет полифила, дающего воркеру DOM. Ограничение архитектурное: у WorkerGlobalScope нет document. Выноси вывод данных, а commit оставляй на главном потоке." },
    { label: "Воркеру нужен OffscreenCanvas, чтобы отрендерить список.", misconception: "OffscreenCanvas рендерит пиксели, а не DOM-список элементов. Виртуализированный React-список — это DOM-узлы: commit остаётся на главном потоке, выносится только чистое вычисление." },
  ]}
/>

<Quiz
  id="04-workers-choice-2"
  lessonSlug="04-workers"
  lang="ru"
  question="Ты делаешь postMessage с Float32Array на 40 МБ в воркер, и отправляющий поток всё равно замирает на ~40 мс ещё до старта воркера. Лучший фикс?"
  choices={[
    { label: "Передай буфер через transfer: postMessage(arr, [arr.buffer]). Владение переходит за O(1), вью отправителя детачится (byteLength становится 0), и копирование structured clone на ~40 мс исчезает.", correct: true },
    { label: "Сначала сериализовать в JSON, чтобы клон был дешевле.", misconception: "JSON.stringify типизированного массива на 40 МБ намного медленнее structured clone и даёт куда большую строку — блокировка только усугубится." },
    { label: "Разбить на 40 отдельных postMessage по 1 МБ.", misconception: "Суммарная стоимость клона — те же 40 МБ, но размазана на 40 task hop с 40-кратными накладными планирования. Transfer убирает копирование полностью." },
    { label: "Использовать SharedArrayBuffer, чтобы копирования не было.", misconception: "SAB тоже избегает копии, но он для конкурентного общего доступа и требует COOP+COEP. Если отправитель передаёт данные и больше их не трогает, transfer проще и без заголовков." },
  ]}
/>

<Quiz
  id="04-workers-choice-3"
  lessonSlug="04-workers"
  lang="ru"
  question="Ты деплоишь новый service worker, перезагружаешь — и всё ещё старое поведение без ошибок. Новый воркер установился нормально. Что происходит?"
  choices={[
    { label: "Новый воркер в состоянии waiting — он не возьмёт контроль над уже открытыми страницами, пока не закроется каждая вкладка на старом воркере, если не вызвать skipWaiting(). Это предотвращает version skew посреди сессии.", correct: true },
    { label: "Service worker кэширует сам себя и игнорирует новые версии.", misconception: "Браузер скачивает и устанавливает новый воркер; он просто не активирует его над открытыми страницами по умолчанию. Это состояние waiting, а не баг кэша." },
    { label: "Старый воркер нужно вручную unregister на каждом деплое.", misconception: "Ручной unregister никогда не нужен. skipWaiting() + clients.claim() передаёт контроль сразу; иначе это делает закрытие всех вкладок." },
    { label: "Перезагрузка не перезапросила sw.js, поэтому остался старый воркер.", misconception: "Браузер перепроверяет sw.js при навигации и устанавливает новый воркер — он лишь ждёт активации. (Отдельно: отдавай sw.js с Cache-Control: no-cache, чтобы перепроверка не была устаревшей.)" },
  ]}
/>

<Quiz
  id="04-workers-choice-4"
  lessonSlug="04-workers"
  lang="ru"
  question="Многопоточный WASM-модуль тихо работает однопоточно в проде. typeof SharedArrayBuffer равно 'undefined', а crossOriginIsolated равно false; COOP: same-origin выставлен, а COEP — нет. Точный фикс и его цена?"
  choices={[
    { label: "Добавить COEP: require-corp. С обоими COOP+COEP crossOriginIsolated становится true и SAB определён — но COEP затем блокирует каждый кросс-доменный image, font и script, не присылающий Cross-Origin-Resource-Policy. Сначала выкатывай в report-only, чтобы перечислить, что сломается.", correct: true },
    { label: "Пересобрать WASM с большей начальной памятью.", misconception: "Размер памяти ни при чём — модуль деградирует, потому что SharedArrayBuffer не определён. SAB требует обоих заголовков изоляции; разблокирует его только недостающий COEP." },
    { label: "Задать бюджет памяти воркер-потокам.", misconception: "Никакая ручка памяти не разблокирует SAB. Гейт — это пара кросс-доменной изоляции COOP/COEP, и только она." },
    { label: "Заменить SharedArrayBuffer на transferable ArrayBuffer.", misconception: "Transfer передаёт эксклюзивное владение — он не может питать потоки, которые все читают/пишут одну память, а именно это нужно pthread. Фикс — разблокировать SAB через COEP, а не заменять его." },
  ]}
/>

<Quiz
  id="04-workers-choice-5"
  lessonSlug="04-workers"
  lang="ru"
  question="Кнопка запускает задачу обработки изображения на 400 мс. Пользователи жмут часто; вкладка спавнит по воркеру на клик, доходит до 400 МБ и убивается на мобиле. Какая правильная архитектура?"
  choices={[
    { label: "Переиспользуемый пул воркеров размером hardwareConcurrency − 1, с ограниченной очередью (backpressure) и приоритетной маршрутизацией — переиспользуй потоки, ограничь память, отклоняй или дропай работу при полной очереди, чтобы вызывающие отступили.", correct: true },
    { label: "Спавнить воркеры, но звать terminate() сразу после каждого результата.", misconception: "Терминирование после каждой задачи выбрасывает воркер и заставляет платить свежие 5–20 мс старта на следующем клике. Постоянный пул амортизирует старт; спавн-и-убийство на задачу — нет." },
    { label: "Вернуть работу на главный поток кусками по 50 мс.", misconception: "Куски сохраняют отзывчивость ввода, но 400 мс всё ещё конкурируют с рендером на одном потоке, и неограниченные клики копятся. Пул + backpressure выносит вычисление И ограничивает очередь." },
    { label: "Поднять лимит памяти вкладки, чтобы 400 МБ были приемлемы.", misconception: "Потолок памяти мобилки не поднять, а реальный баг — утечка воркеров. Ограничь число воркеров пулом, а очередь — backpressure." },
  ]}
/>

<Quiz
  id="04-workers-choice-6"
  lessonSlug="04-workers"
  lang="ru"
  question="Service worker перехватывает навигацию и отдаёт app shell cache-first. Ты выкатываешь баг в shell; часть пользователей бьётся в сломанный кэшированный shell на каждой перезагрузке без кнопки восстановления. Какая архитектурная защита?"
  choices={[
    { label: "Отдавать навигацию network-first (короткий таймаут, фолбэк в кэш), чтобы фикс дошёл до пользователя на первой успешной загрузке, и держать kill-switch endpoint, который воркер проверяет на activate — по сигналу он зовёт self.registration.unregister() и чистит кэши.", correct: true },
    { label: "Сказать пользователям почистить данные сайта или открыть DevTools.", misconception: "Обычные пользователи этого не сделают. Восстановление должно быть инженерным: network-first навигация плюс удалённый kill switch." },
    { label: "Вызвать skipWaiting(), чтобы следующий деплой активировался сразу.", misconception: "Воркер с навигацией cache-first может отдать кэшированный HTML ещё до проверки обновления, так что skipWaiting не получит шанса. Network-first навигация и kill switch заставляют пройти путь обновления." },
    { label: "Положиться на то, что браузер обнаружит и снимет сломанный воркер.", misconception: "У браузера нет понятия «сломанный» воркер — успешный сломанный кэш-ответ выглядит как корректный. Путь восстановления нужно строить заранее." },
  ]}
/>

<Recap lang="ru">
Сквозная линия юнита — одно дерево решений: выбери примитив под задачу — web worker для вычислений вне главного потока, service worker для сетевого прокси, SharedArrayBuffer для разделяемой памяти — потом заплати правильную стоимость транспорта (clone для мелких данных, transfer для крупного бинарного, SAB для конкурентного доступа за COOP+COEP). Вокруг этого повторяются три ловушки: воркер не может трогать DOM (поэтому мутацию DOM не вынести), service worker ждёт перед активацией и труден в восстановлении, когда ломает навигацию, а пулам нужен backpressure, иначе они текут. Ни один примитив не чинит DOM-боттлнек на главном потоке — эта работа остаётся там, где живёт DOM.
</Recap>
