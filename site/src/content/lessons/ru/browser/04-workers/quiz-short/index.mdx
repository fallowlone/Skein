---
concepts:
- worker-dom-boundary
- structured-clone-vs-transfer
- service-worker-lifecycle
- cross-origin-isolation
- worker-pool-sizing
- comlink-task-hop
estMin: 14
lang: ru
level: senior
order: 8
prereqs: []
slug: quiz-short
sources:
- https://developer.mozilla.org/en-US/docs/Web/API/Web_Workers_API
- https://web.dev/articles/service-worker-lifecycle
- https://developer.mozilla.org/en-US/docs/Web/API/SharedArrayBuffer
status: ready
summary: "Промпты на свободное припоминание по всему юниту воркеров. Сначала отвечай своими словами, потом раскрывай модельный ответ и сверяйся с версией senior-глубины."
title: 'Воркеры: тест на свободное припоминание'
track: browser
unit: 04-workers
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";

<Hook>
Припоминание сильнее перечитывания. Для каждого промпта проговори или запиши полный ответ по памяти, прежде чем открыть модельный — усилие реконструкции механизма и закрепляет его.
</Hook>

<Goal lang="ru">
Восстанови костяк юнита, не подглядывая — границу DOM, стоимость clone против transfer, жизненный цикл service worker и его ловушку waiting, гейт COOP/COEP на SharedArrayBuffer и правила размера и формы пула воркеров.
</Goal>

<RetrievalDrawer
  client:visible
  id="04-workers-short-retrieval"
  lang="ru"
  questions={[
    { q: "Почему ни один тип воркера не может трогать DOM и какое единственное исключение?", a: "Глобал воркера — это WorkerGlobalScope: в нём нет document, нет window, нет элементов — по дизайну, а не как ограничение, которое надо обходить. Единоличное владение DOM означает, что мутирует его только главный поток, поэтому нет межпоточных гонок и не нужны локи на дереве. Воркер может вычислить, что рендерить, и отправить результат обратно, но применяет его главный поток. Единственное исключение — OffscreenCanvas: bitmap холста это плоский пиксельный буфер, не часть дерева DOM (нет событий, CSS, дерева доступности), поэтому его безопасно передать любому потоку. Передай OffscreenCanvas через transfer воркеру, и он рисует 2D или WebGL на своём потоке, пока браузер композитит результат." },
    { q: "Сравни structured clone и transfer: чего стоит каждый и когда вместо них SharedArrayBuffer.", a: "postMessage глубоко копирует полезную нагрузку алгоритмом structured clone — синхронно, на отправляющем потоке, со скоростью около 1 мс на МБ для обычных объектов. Объект на 30 МБ блокирует отправителя ~30 мс, стирая выгоду от выноса работы с потока. Transfer передаёт объект во втором аргументе postMessage (transfer-список); браузер передаёт память приёмнику за O(1) и детачит вью отправителя (byteLength становится 0). К transferable относятся ArrayBuffer, MessagePort, ImageBitmap, OffscreenCanvas и типы стримов. Используй transfer для крупных бинарных нагрузок, когда отправитель с данными закончил; используй SharedArrayBuffer, когда обоим потокам нужен конкурентный доступ к одной памяти (например, WASM-пул потоков, делящий буфер кадров) — transfer передаёт эксклюзивное владение, SAB его разделяет." },
    { q: "Пройди жизненный цикл service worker от register() до fetch и объясни состояние waiting.", a: "navigator.serviceWorker.register('sw.js') скачивает и парсит воркер (scope по умолчанию — его директория). install срабатывает один раз, чтобы предзакэшировать app shell — оберни наполнение кэша в event.waitUntil, чтобы браузер не продвинул и не убил воркер посреди операции. Свежеустановленный воркер входит в waiting: он не берёт контроль над уже открытыми страницами, пока не закроется каждая вкладка на старом воркере. Это предотвращает version skew — страница, загрузившая HTML версии N, завершает сессию против ассетов N+1. activate срабатывает после waiting (чистим устаревшие кэши, опционально clients.claim()), затем воркер обрабатывает события fetch, перехватывая запросы в scope. skipWaiting() + clients.claim() обходит waiting и активирует сразу — безопасно лишь с контент-хэшированными ассетами." },
    { q: "Перечисли всё, что нужно для существования SharedArrayBuffer, и цену включения.", a: "Документ должен отдаваться с двумя заголовками ответа: Cross-Origin-Opener-Policy: same-origin (разрывает связь группы browsing-контекстов с другими origin, которые могли бы делить процесс) и Cross-Origin-Embedder-Policy: require-corp или credentialless (заставляет каждый кросс-доменный субресурс явно согласиться). Когда оба в силе, self.crossOriginIsolated равно true и SharedArrayBuffer определён; иначе он undefined и многопоточный WASM тихо деградирует до однопоточного. Гейт существует, потому что Spectre сделал высокоточные таймеры на разделяемой памяти риском side-channel. Цена: COEP блокирует каждый кросс-доменный image, font, script и iframe, не присылающий Cross-Origin-Resource-Policy — так что аудитируй каждый ассет (отдай CORP, проксируй или используй credentialless для публичных) и выкатывай в report-only сначала, чтобы перечислить поломки до энфорса." },
    { q: "Как выбрать размер пула воркеров и какие два контроля держат его безопасным под нагрузкой?", a: "Размер пула — navigator.hardwareConcurrency − 1, оставляя одно ядро главному потоку (рендеринг, ввод, оркестрация), чтобы он держал 60 fps, пока пул работает на полную — это −1 эвристика, профилируй для очень коротких или I/O-bound задач. Два контроля безопасности — backpressure и приоритетная маршрутизация. Backpressure ограничивает очередь задач: при заполнении либо дропай старейшие задачи, либо отклоняй новые (возвращай отклонённый промис), чтобы вызывающие отступили, а не давай очереди расти безгранично и вкладке раздуваться. Приоритетная маршрутизация ставит интерактивные задачи (превью фильтра, которого ждёт пользователь) впереди фоновой работы (генерация миниатюр). Переиспользуй воркеры между задачами, чтобы амортизировать старт в 5–20 мс; никогда не спавни на задачу для повторяющейся работы." },
    { q: "В чём проблема task-hop у Comlink и как проектировать интерфейсы воркера вокруг неё?", a: "Comlink оборачивает воркер в Proxy, так что await worker.compute(data) читается как локальный вызов, но абстракция прячет две реальные стоимости: каждый аргумент проходит structured clone, если явно не обёрнут в Comlink.transfer, и каждый вызов — это round-trip postMessage, два task hop по ~1 мс в каждую сторону. Иллюзия ломается на болтливых интерфейсах: много мелких методов в тесном цикле платят task hop за вызов и сериализуют программу на round-trip'ах. Проектируй API воркера грубо — один вызов делает пакет работы и возвращает пакет результатов — ровно как минимизация сетевых round-trip. Используй Comlink.transfer для крупных бинарных аргументов, чтобы они передавались за O(1), а не клонировались." },
  ]}
/>

<Recap lang="ru">
Если ты смог восстановить каждый ответ по памяти — ты держишь костяк юнита: воркеры никогда не трогают DOM (OffscreenCanvas — единственное исключение), postMessage клонирует со скоростью ~1 мс/МБ, тогда как transfer передаёт за O(1), а SharedArrayBuffer разделяет конкурентно за COOP+COEP, жизненный цикл service worker ждёт перед активацией, чтобы избежать version skew, SAB требует кросс-доменной изоляции и аудита каждого кросс-доменного ассета, а пул размером hardwareConcurrency − 1 безопасен лишь с backpressure, приоритетной маршрутизацией и грубыми (учитывающими task-hop) интерфейсами.
</Recap>
