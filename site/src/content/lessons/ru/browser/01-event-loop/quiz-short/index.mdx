---
concepts:
- microtask-checkpoint
- render-step
- timer-throttling
- microtask-starvation
- loaf-inp
- node-loop-lag
estMin: 14
lang: ru
level: senior
order: 8
prereqs: []
slug: quiz-short
sources:
- https://html.spec.whatwg.org/multipage/webappapis.html#event-loops
- https://developer.mozilla.org/en-US/docs/Web/API/PerformanceLongAnimationFrameTiming
- https://web.dev/articles/inp
status: ready
summary: "Промпты на свободное припоминание по всему юниту. Сначала ответь своими словами, затем раскрой эталонный ответ и сравни."
title: 'Event loop: тест на припоминание'
track: browser
unit: 01-event-loop
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";

<Hook>
Припоминание сильнее перечитывания. На каждый промпт сначала проговори или запиши полный ответ по памяти — именно усилие припоминания закрепляет модель тайминга loop.
</Hook>

<Goal lang="ru">
Реконструируй ключевые механизмы юнита — пятишаговую итерацию, microtask checkpoint, троттлинг таймеров, режим starvation, пайплайн INP→LoAF и переупорядочивание в Node — не подсматривая в уроки.
</Goal>

<RetrievalDrawer
  client:visible
  id="01-event-loop-short-retrieval"
  lang="ru"
  questions={[
    { q: "Пройди одну полную итерацию browser event loop, называя, что выполняется и когда каждый шаг пропускается.", a: "Шаг 1: выбрать один task из непустой task-очереди (пропускается, только если все очереди пусты — тогда loop простаивает). Шаг 2: выполнить этот task до завершения — без вытеснения. Шаг 3: microtask checkpoint дренирует microtask-очередь до пустоты, включая microtask, поставленные другими microtask. Шаг 4: обновить рендеринг — выполнить колбэки requestAnimationFrame, затем style, layout, paint, composite — но только когда браузер решил, что кадр пора рисовать (примерно каждые 16.67 мс при 60 Гц); иначе пропускается. Шаг 5: вернуться к шагу 1. Главное следствие: microtask всегда выполняются до шага рендеринга, поэтому с точки зрения рендерера они ведут себя как продолжение выполняющегося task." },
    { q: "Почему await Promise.resolve() не уступает поток браузеру, хотя приостанавливает вызывающую функцию, и что реально уступает поток?", a: "await — это сахар над Promise.then, поэтому await уже зарезолвленного промиса ставит возобновление функции как microtask. Microtask checkpoint дренируется до пустоты раньше, чем loop вообще дойдёт до шага рендеринга или возьмёт следующий task, поэтому функция приостанавливается, но рендерер и ввод не получают хода между паузой и возобновлением. Чтобы реально уступить поток, нужно поставить task: setTimeout(fn, 0) (грубо, кламп 4 мс после глубины вложенности 5), MessageChannel.postMessage (без клампа — это использует React 18) или scheduler.yield() (Chrome 115+, возобновляет task с сохранённым приоритетом)." },
    { q: "Назови три слоя троттлинга, складывающихся на точность setTimeout, и одно правило, связывающее их.", a: "Слой 1: кламп вложенности 4 мс — после 5 уровней вложенности setTimeout/setInterval минимальная задержка клампится до 4 мс, поэтому цикл на setTimeout(0) выполняется максимум ~250 раз в секунду. Слой 2: фоновый троттлинг вкладок — скрытые вкладки клампят таймеры до ≥1 с, затем до ≥1 мин после нескольких минут в фоне, а rAF останавливается полностью. Слой 3: коалесинг таймеров — времена срабатывания выравниваются по общей сетке, чтобы CPU просыпался один раз на много таймеров. Объединяющее правило: setTimeout(fn, delay) гарантирует лишь нижнюю границу — колбэк сработает не раньше delay, но никогда точно в delay. Для субкадровой точности используй часы AudioContext или дельты performance.now() внутри rAF-цикла." },
    { q: "Опиши режим microtask starvation, как он выглядит в DevTools и один production-сценарий, где он появляется случайно.", a: "Microtask, который ставит другой microtask до возврата, держит microtask-очередь постоянно непустой, поэтому loop никогда не выходит из checkpoint шага 3 к рендерингу или вводу — страница замерзает с нулём отрисованных кадров и INP свыше 1 с. В DevTools Performance это выглядит как одна непрерывная жёлтая scripting-полоса без доминирующей функции во flame chart. Production-пример: реактивная библиотека состояния запускает обновление внутри обработчика реакции, где реакция мутирует состояние, которое снова триггерит реакцию — microtask-цикл, который никогда не оседает. Лекарство — вставить один task-уровневый yield (setTimeout, MessageChannel, scheduler.postTask) где-то в цепочке, чтобы loop мог вздохнуть." },
    { q: "Ты видишь регрессию INP в production. Пройди пайплайн от LoAF до фикса, превращающий метрику в развёртываемое изменение.", a: "INP лишь сообщает, что взаимодействие было медленным. Подпиши PerformanceObserver на записи long-animation-frame: каждая LoAF-запись несёт массив scripts с per-script-атрибуцией (sourceURL, sourceFunctionName, sourceCharPosition) и длительность кадра. Скоррелируй LoAF, перекрывшую медленное взаимодействие, возьми самый тяжёлый скрипт и разреши его URL и char-позицию через production-sourcemap до file:line в исходнике. Классифицируй работу (синхронный Redux-редьюсер, фильтрующий большой список, JSON.parse большого ответа, пересчёт производного состояния), затем чини в источнике — мемоизированный selector, оффлоад на Worker, потоковый парсинг — и закрой это синтетическим INP-тестом с троттлингом CPU, чтобы регрессия не вернулась тихо." },
    { q: "Как Node event loop переупорядочивает всё относительно браузера и что является headless-эквивалентом INP?", a: "Node-loop на libuv проходит шесть упорядоченных фаз — timers, pending callbacks, idle/prepare, poll (блок на I/O), check (setImmediate), close — и между каждой фазой дренирует две очереди: сначала Node-специфичную process.nextTick, затем стандартную microtask-очередь. Поэтому process.nextTick стоит выше microtask, делая его высшим по приоритету async-примитивом и footgun-ом при рекурсивном использовании. setImmediate срабатывает в check-фазе текущей итерации; setTimeout(0) — в timers-фазе следующей итерации, поэтому внутри I/O-колбэка setImmediate стабильно выигрывает. У Node нет кадров и ввода, поэтому эквивалент INP — event-loop lag: запланируй таймер на T мс, измерь, насколько поздно он сработал; perf_hooks.monitorEventLoopDelay() даёт гистограмму, а p99 в сотнях мс означает, что обработчик монополизирует единственный поток." },
  ]}
/>

<Recap lang="ru">
Если ты смог реконструировать каждый ответ по памяти — у тебя есть позвоночник юнита: один task выполняется до завершения, затем microtask checkpoint дренируется полностью до любого кадра, затем рендеринг идёт только на границе кадра. Microtask никогда не уступают поток рендереру — это делают только task — поэтому самопланирующиеся microtask и starve страницу. Таймеры — нижние границы, троттленные тремя складывающимися слоями. INP измеряет воспринимаемую стоимость, LoAF плюс sourcemap её атрибутируют, а Node прогоняет всю модель заново с process.nextTick, поднятым выше microtask, и event-loop lag вместо INP.
</Recap>
