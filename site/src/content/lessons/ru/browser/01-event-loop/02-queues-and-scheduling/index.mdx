---
concepts:
- task-source
- microtask-checkpoint
- queueMicrotask
- MessageChannel
- scheduler-yield
- requestAnimationFrame
- input-dispatch
- INP
deepensInto:
- 04-microtask-starvation
estMin: 16
lang: ru
lessonType: topic
level: middle
order: 2
prereqs:
- 01-loop-model
slug: 02-queues-and-scheduling
sources:
- https://html.spec.whatwg.org/multipage/webappapis.html#event-loops
- https://developer.chrome.com/blog/introducing-scheduler-yield-origin-trial
- https://web.dev/articles/optimize-long-tasks
spiral:
- statefulness
- latency
status: ready
summary: Модель обработки HTML-спецификации — источники задач, 5-шаговая итерация,
  как ввод попадает в цикл и как правильно уступать без потери рендеринга.
title: Задачи, микрозадачи и scheduler.yield()
track: browser
unit: 01-event-loop
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import MetaphorComplete from "~/components/pedagogy/MetaphorComplete.astro";
import NumberDrill from "~/components/pedagogy/NumberDrill.astro";
import TraceScenario from "~/components/pedagogy/TraceScenario.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import NumbersCard from "~/components/layout/NumbersCard.astro";

<Hook>
Вы пишете `await Promise.resolve()`, чтобы «уступить» внутри длинного цикла. Браузер всё равно зависает. Пробуете `setTimeout(fn, 0)`. Теперь мигает странно. Проблема не в коде — вы просто не знаете, в какую очередь попадает каждый примитив.
</Hook>

<Crux>Микрозадачи дренируются до рендеринга; задачи уступают ему — выберите не ту и либо заголодите рендерер, либо потеряете позицию в очереди.</Crux>

<Explanation>

## Модель HTML-спецификации: источники задач и очереди задач

HTML-стандарт (§ event loops) определяет event loop как поток с набором **очередей задач**. Каждая задача связана с **источником задач** — типом породившей её работы (манипуляции с DOM, пользовательские взаимодействия, сеть, навигация по истории). Каждый источник имеет собственную очередь, и пользовательский агент выбирает одну очередь за итерацию, вытягивает старейшую задачу и исполняет её до конца без прерываний. Когда задача завершается, агент выполняет microtask checkpoint, затем опционально шаг рендеринга, затем возвращается в начало.

## Микрозадачи vs задачи: решающее правило

**Микрозадача** ставится в очередь: `Promise.then`, `Promise.catch`, `Promise.finally`, `queueMicrotask`, `MutationObserver` и `await` (синтаксический сахар над Promise.then).

**Задача** ставится в очередь: `setTimeout`, `setInterval`, `MessageChannel.postMessage`, сетевые ответы, отправленные DOM-события, распарсенные куски HTML.

Решающее операционное правило: **микрозадачи дренируются до пустой между каждой задачей и каждым кадром**. Если микрозадача планирует ещё одну микрозадачу, обе выполняются до того, как цикл перейдёт дальше. Именно поэтому `Promise.resolve().then(self)` в тесной цепочке вешает страницу: очередь микрозадач никогда не пуста, цикл никогда не доходит до шага 4, и браузер никогда не рисует.

<NumbersCard
  id="card-queue-types"
  title="Что куда попадает"
  data-lesson-visual
  stats={[
    { label: "Promise.then / queueMicrotask", value: "очередь микрозадач" },
    { label: "setTimeout / setInterval", value: "очередь задач (источник таймеров)" },
    { label: "MessageChannel.postMessage", value: "очередь задач (источник сообщений)" },
    { label: "DOM-событие click", value: "очередь задач (источник пользовательского ввода)" },
    { label: "Колбэк MutationObserver", value: "очередь микрозадач" },
    { label: "scheduler.yield()", value: "очередь задач (возобновляется как следующая задача)" },
  ]}
/>

## queueMicrotask vs Promise.resolve().then vs MessageChannel

Три способа «запланировать что-то скоро», каждый с разной семантикой:

- `queueMicrotask(fn)` — ставит микрозадачу напрямую. Выполняется после текущей задачи, до следующей. Рендеринга между ними нет.
- `Promise.resolve().then(fn)` — идентична queueMicrotask по времени, но аллоцирует Promise, замыкание и элемент цепочки. Чуть медленнее, по сути то же самое. **Рендереру не уступает.**
- `new MessageChannel().port1.postMessage(...)` — ставит **задачу**. Выполняется после текущей задачи, после дренажа микрозадач, возможно после кадра. **Уступает рендереру.**

Это важно, когда нужно уступить рендерингу: микрозадачи не уступают; MessageChannel — уступает. Современный код использует `scheduler.yield()` (Chrome 115+) для явных точек уступки.

## Где находится requestAnimationFrame

Колбэки rAF выполняются на шаге 4 цикла, непосредственно перед стилями/компоновкой/отрисовкой. Браузер входит в шаг 4 только когда решает, что кадр пора показать — обычно каждые 16,67 мс при 60 Гц. Если длинная задача пропустила несколько дедлайнов кадра, браузер **не** ставит несколько rAF-колбэков в очередь; он запускает rAF-набор ровно один раз. Вот почему длинная задача всегда выглядит как один пропущенный кадр: rAF управляется бюджетом рендеринга, а не таймером.

## Где находится ввод

События ввода (mousemove, click, keydown, touchstart) ставятся в очередь как задачи потоком ввода браузера (отдельный поток ОС). Главный поток вытягивает их на шаге 1 как любую задачу. Прерываний нет: если ваша JS-задача выполняется 80 мс, а клик пришёл на отметке 20 мс, клик ждёт 60 мс до запуска обработчика. INP измеряет именно эту задержку: когда пользователь кликнул, когда следующая отрисовка отразила результат. INP &lt; 200 мс p75 считается «хорошим». Достичь этого значит не допускать задач длиннее примерно 50 мс.

## Как DOM-событие становится задачей

Когда пользователь кликает, поток ввода браузера записывает цель попадания и ставит единственную задачу в очередь задач источника пользовательских взаимодействий главного потока. Когда главный поток вытягивает эту задачу, он выполняет полный диспатч события за один раз: фаза захвата вниз к цели, затем сама цель, затем фаза всплытия обратно вверх. Все совпадающие слушатели вдоль этого пути выполняются синхронно внутри этой одной задачи — между ними нет точек уступки. Именно поэтому медленный слушатель на высоком предке (обработчик `mousemove` на `document`) нагружает каждое взаимодействие с потомком. Опция `passive: true` важна для scroll, wheel и touch: она обещает, что слушатель не вызовет `preventDefault()`, позволяя браузеру начать прокрутку на композиторе без ожидания окончания задачи диспатча.

## Уступка внутри длинной задачи

Если нужно выполнить 200 мс работы, разбейте её на куски ≤50 мс, разделённые точками уступки, чтобы ввод мог вклиниваться. Три техники:

1. `setTimeout(fn, 0)` — ставит задачу примерно через 4 мс (зажато); грубо, но универсально.
2. `MessageChannel` с `postMessage` — ставит задачу немедленно, без зажима. Именно это использует React 18 внутри для time-slicing.
3. `scheduler.yield()` (Chrome 115+) — обещает точку уступки, которая позволяет запустить ввод и рендеринг, затем возобновляет ту же логическую задачу с приоритетом. Современная рекомендация: используйте `scheduler.yield()`, при недоступности — `setTimeout(0)`. **Не используйте** `await Promise.resolve()` для уступки — это ставит микрозадачу, которая не позволяет запустить рендерер.

<Quiz
  id="mid-quiz-1"
  pieceSlug="02-queues-and-scheduling"
  lang="ru"
  question="`setTimeout(fn, 0)` и `queueMicrotask(fn)` оба «планируют что-то скоро». В чём операционная разница?"
  choices={[
    { label: "queueMicrotask выполняется до следующей задачи и до любого кадра; setTimeout(0) выполняется как задача — после дренажа микрозадач, возможно после кадра", correct: true },
    { label: "Они идентичны — оба планируют «после этого кода»", misconception: "Время совпадает в однострочных примерах, но они живут в разных очередях. Разница видна, когда в полёте есть рендеринг и другие задачи." },
    { label: "setTimeout(0) быстрее, потому что использует нативные таймеры", misconception: "Браузеры зажимают setTimeout до минимума 4 мс после 5 уровней вложенности. queueMicrotask не зажат и выполняется строго раньше." },
    { label: "queueMicrotask выполняется в отдельном потоке", misconception: "Все микрозадачи выполняются на главном потоке. Отдельного воркера для них нет." },
  ]}
/>

<Quiz
  id="mid-quiz-2"
  pieceSlug="02-queues-and-scheduling"
  lang="ru"
  question="Тело цикла: `await fetch(url)`. Почему страница всё равно рендерится между итерациями, хотя цикл никогда не возвращает управление?"
  choices={[
    { label: "Каждый `await` приостанавливает функцию; резолв fetch доставляется как задача из источника сети — управление возвращается в цикл, который запускает рендеринг до срабатывания задачи возобновления", correct: true },
    { label: "fetch асинхронный, поэтому выполняется в воркере", misconception: "fetch работает в сетевом стеке, а не в воркере; резолв промиса всё равно происходит на главном потоке." },
    { label: "Браузер прерывает долго выполняющиеся задачи каждые 50 мс", misconception: "Прерываний нет. Цикл кооперативный: задачи выполняются до конца. Страница рендерится здесь только потому, что await реально уступает." },
    { label: "await — особый синтаксис, всегда позволяющий рендереру выполниться первым", misconception: "await приостанавливает async-функцию и возобновляет её как микрозадачу, когда ожидаемый промис резолвится. Выполнится ли рендерер до этого — зависит от того, является ли резолв задачей (сеть) или микрозадачей (Promise.resolve)." },
  ]}
/>

<TraceScenario
  id="mid-trace-1"
  pieceSlug="02-queues-and-scheduling"
  lang="ru"
  scenario="DevTools Performance показывает «длинную задачу» 320 мс на обработчике поля поиска. INP страницы — 410 мс p75. Обработчик делает: парсит ввод → fetch подсказок → JSON.parse ответа → ре-рендер дропдауна. Где время?"
  steps={[
    { prompt: "JSON.parse большого ответа — наиболее вероятный виновник: синхронный на главном потоке, линейно растёт с объёмом байт", reveal: "JSON.parse держит главный поток всю свою длительность. Блоб подсказок 2 МБ может занять 80–150 мс на среднем Android. Вместе со следующей за ним React-рекоцилиацией суммарная задача легко достигает 320 мс. Лечение: стримите ответ (чанкованный парсер), парсите в Worker или уменьшите ответ на стороне сервера." },
    { prompt: "fetch — узкое место: задержка сети", reveal: "Задержка fetch невидима для счётчика длинных задач. Таймер тикает только пока главный поток занят — а он не занят во время ожидания сети. Видимые 320 мс — работа после fetch." },
    { prompt: "React-ре-рендер дропдауна — узкое место", reveal: "Ре-рендер React — часть стоимости, но редко большинство. Типичный рекоцилятор дропдауна занимает 5–20 мс. Доминируют JSON.parse и парсинг." },
    { prompt: "Таймаут сети с откатом", reveal: "Сетевые операции не блокируют главный поток. Они отображаются как серое время простоя в DevTools, а не как длинные задачи." },
  ]}
/>

<Quiz
  id="mid-quiz-3"
  pieceSlug="02-queues-and-scheduling"
  lang="ru"
  question="Нужно разбить задачу 200 мс на куски по 50 мс, чтобы ввод мог вклиниваться. Какая техника реально позволяет браузеру обрабатывать ввод между кусками?"
  choices={[
    { label: "Уступка через `await scheduler.yield()` (или `await new Promise(r => setTimeout(r, 0))` как запасной вариант)", correct: true },
    { label: "Уступка через `await Promise.resolve()`", misconception: "Это ставит микрозадачу. Микрозадачи не позволяют запустить рендеринг или ввод — они продлевают эффективную длительность текущей задачи." },
    { label: "Уступка через `requestAnimationFrame`", misconception: "rAF выполняется только на границе кадра. Если уступить rAF посреди задачи, ждёте следующей границы кадра — это может быть много мс, не точка уступки для ввода." },
    { label: "Уступка через `process.nextTick`", misconception: "process.nextTick существует только в Node, а не в браузере. В Node он запускается до микрозадач — ещё более агрессивное голодание." },
  ]}
/>

<Quiz
  id="mid-quiz-4"
  pieceSlug="02-queues-and-scheduling"
  lang="ru"
  question="Worker вызывает `postMessage(data)` в главный поток. data — Float32Array 5 МБ. Что является доминирующей стоимостью?"
  choices={[
    { label: "Structured clone буфера (по умолчанию копирует байты); используйте `postMessage(data, [data.buffer])` для передачи вместо копирования", correct: true },
    { label: "Переключение задачи между потоками (~1–2 мс)", misconception: "Переключение задачи пренебрежимо мало рядом с клонированием 5 МБ. Сам clone стоит ~20 мс; transfer снижает это до микросекунд." },
    { label: "JSON-сериализация массива", misconception: "postMessage использует structured clone, а не JSON. JSON был бы намного медленнее, так как преобразовывал бы float в строки." },
    { label: "Сборка мусора буфера со стороны воркера", misconception: "Сборка мусора происходит позже. Видимая синхронная стоимость при postMessage — это clone." },
  ]}
/>

<DragOrder
  id="mid-drag-1"
  pieceSlug="02-queues-and-scheduling"
  lang="ru"
  prompt="Click-обработчик выполняет синхронный код, затем `Promise.resolve().then(...)`, затем `setTimeout(..., 0)`, затем ещё синхронный код. Расставьте колбэки в порядке их реального выполнения."
  items={[
    { id: "sync1", label: "Синхронный код в начале обработчика" },
    { id: "sync2", label: "Синхронный код в конце обработчика (перед return)" },
    { id: "promise", label: "Колбэк Promise.then (микрозадача)" },
    { id: "timer", label: "Колбэк setTimeout (задача)" },
  ]}
  correctOrder={["sync1", "sync2", "promise", "timer"]}
/>

<MetaphorComplete
  id="mid-metaphor-1"
  pieceSlug="02-queues-and-scheduling"
  lang="ru"
  setup="Когда два потока (главный и воркер) обмениваются данными, сообщения пересекаются через очередь задач, а не очередь микрозадач. Значит, нижняя граница задержки туда-обратно — одно переключение задачи в каждую сторону. Как это переключение обычно называют в обсуждениях производительности?"
  accepted={["task hop", "переключение задачи", "прыжок задачи", "task boundary", "переход задачи"]}
  canonical="переключение задачи (task hop)"
  explanation="Task hop — это один маршрут через «подождать, пока принимающий поток закончит текущую задачу, затем это сообщение становится следующей задачей, которую он вытягивает». На свободном главном потоке это ~1 мс; на занятом — ограничено длительностью текущей задачи. Переключения задач — вот почему высокочастотный кросс-поточный обмен (много мелких сообщений) намного хуже пакетных сообщений: каждое переключение платит один и тот же минимум плюс structured clone плюс джиттер планировщика."
/>

<TradeoffMatrix
  id="card-task-vs-microtask"
  pieceSlug="02-queues-and-scheduling"
  lang="ru"
  prompt="Нужно запланировать код «после этого синхронного блока, как можно скорее». Какой примитив?"
  options={[
    { name: "queueMicrotask(fn) — выполняется как следующая микрозадача", summary: "Для: работы, производной от состояния, которая должна выполниться до любого рендеринга или другой задачи.", correct: true, justification: "Микрозадачи не зажаты, выполняются между текущей задачей и следующей — но важно, что до шага 4 (рендеринг). Используйте, когда нижестоящий код должен видеть новое состояние до того, как пользователь увидит кадр, но вы не хотите чередоваться с другими задачами." },
    { name: "setTimeout(fn, 0) — выполняется как задача после зажима 4 мс", summary: "Для: уступки рендереру и пользовательскому вводу.", justification: "Другая семантика. setTimeout ставит задачу, значит рендеринг и ввод могут выполниться до fn. Зажим 4 мс делает его плохим выбором для тесных циклов; используйте MessageChannel или scheduler.yield() для этого случая." },
    { name: "scheduler.yield() — явная уступка платформе", summary: "Для: продолжения длинной задачи после того, как браузер обработает ввод/рендеринг.", justification: "Современный примитив, Chrome 115+. Правильный инструмент когда есть длинная задача, которую хочется разбить, сохраняя логическую непрерывность. Откатывается на setTimeout(0) где не поддерживается. Всегда используйте внутри цикла чанкованной работы, а не для планирования несвязанного кода." },
    { name: "requestAnimationFrame(fn) — выполняется на следующей границе кадра", summary: "Для: кода, мутирующего DOM прямо перед отрисовкой.", justification: "Неправильный примитив для общего планирования. rAF выполняется на шаге 4 цикла только на границах кадра, что может быть на много мс позже. Используйте для анимационной работы, которую хочется объединить с рендерингом, а не для общего «скоро» планирования." },
  ]}
/>

<NumberDrill
  id="mid-drill-1"
  pieceSlug="02-queues-and-scheduling"
  lang="ru"
  question="Колбэк `setTimeout(fn, 0)` после 5-го уровня вложенности зажимается до минимума. По HTML-спецификации этот минимум (в мс) равен:"
  expected={4}
  tolerance={0}
  unit="мс"
  explanation="Браузеры зажимают setTimeout до минимума 4 мс, когда вложенность достигает 5 уровней — правило возникло для предотвращения злоупотреблений через цепочки setTimeout(0) в начале 2000-х. Вот почему setTimeout(0) — плохой примитив планирования для тесных циклов уступки: цикл из 100 итераций с setTimeout(0) занимает минимум 400 мс независимо от объёма работы. Используйте MessageChannel или scheduler.yield() — они не имеют зажима 4 мс."
/>

</Explanation>

<KeyTakeaway>Микрозадачи выполняются до любого рендеринга; задачи уступают ему. Используйте queueMicrotask, когда состояние должно быть видно до следующей отрисовки; используйте scheduler.yield() или MessageChannel, когда нужно дать рендереру и вводу выполниться между кусками работы.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="02-queues-and-scheduling-retrieval"
  lang="ru"
  questions={[
    { q: "Объясните, почему `await Promise.resolve()` не «уступает браузеру», хотя и приостанавливает вызывающую функцию.", a: "await — синтаксический сахар для Promise.then. Когда вы await уже резолвнутого промиса, движок ставит возобновление функции в очередь микрозадач. Microtask checkpoint выполняется после окончания текущей задачи — но он дренируется до пустой до того, как цикл достигнет шага 4 (рендеринг) или вытянет другую задачу. Так что функция паузируется, но цикл никогда не достигает рендерера или очереди ввода между паузой и возобновлением. Чтобы реально уступить рендерингу и вводу — нужно поставить задачу: setTimeout(fn, 0), new MessageChannel().port1.postMessage(), или scheduler.yield()." },
    { q: "Воркер отправляет 60 сообщений в секунду в главный поток, каждое несёт небольшой JSON. Почему это всё равно вызывает джанк главного потока, даже если каждое сообщение крошечное?", a: "Каждый postMessage ставит задачу в главный поток. 60 сообщений в секунду = 60 переключений задач в секунду. Каждое переключение платит: стоимость structured clone, накладные расходы планирования задачи (~0,1 мс) и принудительное выполнение обработчика получения. Если главный поток также занят рендерингом, обработчиками ввода и другими задачами, эти 60 переключений фрагментируют цикл на очень короткие срезы. Лечение: пакетировать в одно сообщение каждые 16 мс с массивом событий." },
    { q: "Почему медленный слушатель событий на `document` (например, обёртка аналитики для mousemove) нагружает каждое взаимодействие с потомком?", a: "События ввода ставятся в очередь как единственная задача из источника пользовательских взаимодействий. Когда главный поток вытягивает задачу, он выполняет полный диспатч события синхронно: захват вниз, цель, всплытие вверх. Все совпадающие слушатели вдоль пути выполняются внутри этой одной задачи без точек уступки между ними. Медленный слушатель высоко в дереве выполняется на каждом совпадающем событии, сработавшем любым потомком, увеличивая общую длительность задачи диспатча. Нет способа «пропустить» слушатель предка без его удаления." },
  ]}
/>

<Recap lang="ru">
HTML-спецификация определяет несколько очередей задач — по одной на источник задач — и браузер выбирает одну очередь за итерацию. Микрозадачи, напротив, живут в едином checkpoint, который дренируется до пустой после каждой задачи и до каждого кадра. Три примитива планирования имеют разную семантику: queueMicrotask работает до рендеринга, setTimeout(0)/MessageChannel — после рендеринга (уступка на уровне задач), а scheduler.yield() — структурированная уступка уровня задач с сохранением приоритета. События ввода попадают в очередь задач из потока ввода ОС; задача длиной 80 мс задерживает пришедший в середине клик на всю оставшуюся длительность. Опция passive на слушателях scroll и touch позволяет потоку композитора начать немедленно вместо ожидания задачи диспатча — конкретный выигрыш для отзывчивости скролла без изменения JS-логики.
</Recap>
