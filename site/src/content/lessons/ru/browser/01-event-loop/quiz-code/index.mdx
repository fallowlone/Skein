---
concepts:
- execution-order
- microtask-task-interleave
- async-await-ordering
- requestAnimationFrame
- node-phase-ordering
estMin: 14
lang: ru
level: senior
order: 9
prereqs: []
slug: quiz-code
sources:
- https://html.spec.whatwg.org/multipage/webappapis.html#event-loops
- https://jakearchibald.com/2015/tasks-microtasks-queues-and-schedules/
- https://nodejs.org/en/learn/asynchronous-work/event-loop-timers-and-nexttick
status: ready
summary: "Читай реальные JS-сниппеты и предсказывай точный порядок выполнения — синхронный код, microtask, task, rAF и порядок фаз в Node."
title: 'Event loop: предскажи вывод'
track: browser
unit: 01-event-loop
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Предсказание точного порядка лога — чистейшая проверка, действительно ли ты держишь модель loop. Читай каждый сниппет, отслеживай, в какую очередь попадает каждый колбэк, и выбери вывод, который предскажет внимательный инженер.
</Hook>

<Goal lang="ru">
Потренируй трассировку, которую мысленно прогоняешь в каждом async-баге: классифицируй каждый колбэк как sync, microtask, task или работу кадра и упорядочи по фиксированным правилам loop — включая Node-специфичное переупорядочивание.
</Goal>

## Сниппет 1 — sync, microtask и task в одном витке

```javascript
console.log('A');
setTimeout(() => console.log('B'), 0);
Promise.resolve().then(() => console.log('C'));
console.log('D');
```

<Quiz
  id="01-event-loop-code-1"
  lessonSlug="01-event-loop"
  lang="ru"
  question="Что это залогирует и в каком порядке?"
  choices={[
    { label: "A, D, C, B — синхронный код выполняется первым (A, D), затем дренируется microtask checkpoint (C), затем loop берёт task setTimeout (B) на более поздней итерации.", correct: true },
    { label: "A, B, C, D — всё выполняется сверху вниз в порядке исходника.", misconception: "setTimeout и Promise.then откладывают свои колбэки. Синхронно выполняются только A и D; B и C — после текущего task, причём microtask C раньше task B." },
    { label: "A, D, B, C — таймер срабатывает раньше промиса, потому что 0 мс ближе, чем microtask.", misconception: "Microtask checkpoint дренируется раньше, чем loop вообще возьмёт task setTimeout. C (microtask) всегда раньше B (task), независимо от задержки 0 мс." },
    { label: "A, C, D, B — колбэк промиса выполняется в момент резолва промиса, прерывая синхронный код.", misconception: "Microtask никогда не прерывают синхронный код. C ставится в очередь при резолве, но выполняется только на checkpoint, после завершения всего синхронного кода (A и D)." },
  ]}
/>

## Сниппет 2 — async/await без сахара

```javascript
async function run() {
  console.log('1');
  await null;            // suspends; resume is queued as a microtask
  console.log('2');
}
console.log('3');
run();
console.log('4');
Promise.resolve().then(() => console.log('5'));
```

<Quiz
  id="01-event-loop-code-2"
  lessonSlug="01-event-loop"
  lang="ru"
  question="Каков порядок вывода?"
  choices={[
    { label: "3, 1, 4, 2, 5 — '1' выполняется синхронно до await, управление возвращается вызывающему (4), затем microtask дренируются в порядке очереди: возобновление run() ('2') было поставлено раньше .then ('5').", correct: true },
    { label: "3, 1, 2, 4, 5 — await блокирует, пока ожидаемое значение не готово, поэтому '2' печатается до возврата управления.", misconception: "await никогда не блокирует поток. Он приостанавливает run() и планирует возобновление как microtask; синхронный код после run() (печать 4) выполняется до '2'." },
    { label: "3, 4, 1, 2, 5 — вызов run() лишь планирует его; синхронно внутри ничего не выполняется.", misconception: "Код до первого await выполняется синхронно при вызове run(), поэтому '1' печатается до '4'." },
    { label: "3, 1, 4, 5, 2 — явный Promise.then зарегистрирован последним, но microtask — это LIFO.", misconception: "Microtask-очередь — это FIFO. Возобновление после await ('2') поставлено раньше .then ('5'), поэтому '2' дренируется первым." },
  ]}
/>

## Сниппет 3 — rAF против microtask против task

```javascript
console.log('start');
requestAnimationFrame(() => console.log('raf'));
setTimeout(() => console.log('timeout'), 0);
Promise.resolve().then(() => console.log('promise'));
console.log('end');
```

<Quiz
  id="01-event-loop-code-3"
  lessonSlug="01-event-loop"
  lang="ru"
  question="Если браузер рисует кадр на этом витке, каков самый надёжный порядок?"
  choices={[
    { label: "start, end, promise, raf, timeout — сначала sync, затем дренируется microtask (promise), затем шаг рендеринга выполняет rAF на границе кадра, затем task setTimeout на более поздней итерации.", correct: true },
    { label: "start, end, raf, promise, timeout — rAF выполняется первым, потому что следующий кадр — ближайший дедлайн.", misconception: "rAF выполняется на шаге 4 (шаг рендеринга), который идёт после microtask checkpoint. Microtask promise дренируется раньше любого колбэка rAF." },
    { label: "start, end, timeout, promise, raf — таймер на 0 мс — ближайший из отложенных колбэков.", misconception: "Microtask всегда предшествует task. promise выполняется раньше timeout, а rAF — на границе кадра, которая обычно тоже раньше следующего task." },
    { label: "start, promise, end, raf, timeout — промис резолвится посреди скрипта и печатается между start и end.", misconception: "Microtask никогда не выполняются между синхронными инструкциями. promise ставится при резолве и дренируется только после end, на checkpoint." },
  ]}
/>

## Сниппет 4 — порядок фаз в Node

```javascript
// Run with: node script.js
const fs = require('fs');
fs.readFile(__filename, () => {
  setTimeout(() => console.log('timeout'), 0);
  setImmediate(() => console.log('immediate'));
  process.nextTick(() => console.log('nextTick'));
  Promise.resolve().then(() => console.log('promise'));
});
```

<Quiz
  id="01-event-loop-code-4"
  lessonSlug="01-event-loop"
  lang="ru"
  question="Внутри этого I/O-колбэка каков детерминированный порядок вывода в Node?"
  choices={[
    { label: "nextTick, promise, immediate, timeout — между фазами сначала дренируется очередь nextTick, затем microtask (promise); затем setImmediate выполняется в check-фазе этой итерации, а setTimeout(0) ждёт timers-фазы следующей итерации.", correct: true },
    { label: "nextTick, promise, timeout, immediate — у setTimeout(0) наименьшая задержка, поэтому он срабатывает раньше setImmediate.", misconception: "Внутри I/O-колбэка check-фаза (setImmediate) выполняется в текущей итерации, до timers-фазы следующей итерации (setTimeout). Поэтому immediate здесь предшествует timeout." },
    { label: "promise, nextTick, immediate, timeout — Promise.then стандартизирован, поэтому он выше Node-only nextTick.", misconception: "Всё наоборот: у process.nextTick собственная очередь, дренируемая до microtask-очереди, поэтому nextTick печатается до promise." },
    { label: "timeout, immediate, nextTick, promise — колбэки срабатывают в порядке исходника.", misconception: "Порядок исходника нерелевантен. Сначала между фазами дренируются nextTick и microtask; затем порядок фаз (check раньше timers следующей итерации) решает immediate против timeout." },
  ]}
/>

<Recap lang="ru">
Любая головоломка с порядком async решается одной трассировкой: сначала весь синхронный код, затем microtask checkpoint дренируется до пустоты (возобновления await и колбэки .then в порядке FIFO), затем шаг рендеринга выполняет rAF на границе кадра, затем loop берёт следующий task (setTimeout, MessageChannel). Node наслаивает сверху машину фаз: process.nextTick дренируется до microtask между каждой фазой, а внутри I/O-колбэка setImmediate (check-фаза, эта итерация) обходит setTimeout(0) (timers-фаза, следующая итерация). Классифицируй каждый колбэк по очереди, затем упорядочивай по этим фиксированным правилам — никогда по позиции в исходнике.
</Recap>
