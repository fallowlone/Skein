---
concepts:
- microtask-starvation
- PerformanceLongTaskTiming
- LoAF
- scheduler-yield
- scheduler-postTask
- INP
- sourcemap
deepensInto: []
estMin: 16
lang: ru
lessonType: topic
level: senior
order: 4
prereqs:
- 01-loop-model
- 02-queues-and-scheduling
slug: 04-microtask-starvation
sources:
- https://html.spec.whatwg.org/multipage/webappapis.html#event-loops
- https://developer.mozilla.org/en-US/docs/Web/API/PerformanceLongAnimationFrameTiming
- https://developer.chrome.com/blog/introducing-scheduler-yield-origin-trial
- https://web.dev/articles/inp
spiral:
- statefulness
status: ready
summary: Паттерн голодания, как обнаружить его в продакшене с помощью PerformanceLongTaskTiming
  и LoAF, и scheduler.yield() / scheduler.postTask() как лечение.
title: Голодание микрозадач, длинные задачи и LoAF
track: browser
unit: 01-event-loop
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import FadedExample from "~/components/pedagogy/FadedExample.tsx";
import RFCQuiz from "~/components/pedagogy/RFCQuiz.astro";
import DesignPrompt from "~/components/pedagogy/DesignPrompt.astro";
import DebugLog from "~/components/pedagogy/DebugLog.astro";
import NumbersCard from "~/components/layout/NumbersCard.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";
import Pulse from "~/components/diagram/Pulse.astro";

<Hook>
Пользователь сообщает, что страница «полностью зависла» на несколько секунд. DevTools Performance показывает один непрерывный жёлтый скриптовый прямоугольник. Ни один вызов функции не доминирует в flame chart — работа продолжает перепланировать себя как микрозадачи, и цикл никогда не вырывается.
</Hook>

<Crux>Микрозадачи, планирующие микрозадачи до возврата, не позволяют циклу достичь шага рендеринга — страница зависает до разрыва цепочки.</Crux>

<Explanation>

## Паттерн отказа: голодание микрозадач

Микрозадачи дренируются до пустой между задачами. Если микрозадача планирует ещё одну микрозадачу до возврата, цикл никогда не вырывается из microtask checkpoint — нет рендеринга, нет ввода, нет дальнейших задач. Патологический пример:

```javascript
function loop() {
  Promise.resolve().then(loop);  // планирует себя как микрозадачу
}
loop();
```

Это вешает страницу бесконечно: очередь микрозадач никогда не пуста, шаг 4 (рендеринг) никогда не выполняется, пользователь видит замороженную вкладку. DevTools Performance показывает это как один непрерывный жёлтый скриптовый прямоугольник. Тот же паттерн встречается в продакшене как случайная рекурсия через async-цепочки: `.then`, который переподписывается на тот же observable, роутер-мидлвар, который ре-резолвится при каждом изменении, библиотека состояния, которая перезапускает реакции внутри реакции.

**Обнаружение:** записи длинных задач без явной доминирующей функции, скриптовый прямоугольник растёт без границ, INP &gt; 1 с.

**Лечение:** вставить хотя бы одну уступку уровня задачи (`setTimeout`, `MessageChannel`, `scheduler.postTask`) в цепочку.

<FlowDiagram
  label="Цикл голодания микрозадач: выполнение микрозадачи ставит в очередь ещё одну микрозадачу, поэтому очередь микрозадач остаётся непустой и цикл возвращается, чтобы выполнить её снова — никогда не достигая макрозадачи или шага рендеринга, которые голодают."
  perRow={3}
  hue="var(--d-frontend)"
  nodes={[
    { id: "run", label: "Микрозадача", sub: "чекпойнт" },
    { id: "enqueue", label: "Ставит микрозадачу", sub: ".then(loop)" },
    { id: "nonempty", label: "Очередь непуста", sub: "дренировать снова" },
    { id: "starved", label: "Макрозадача + рендер", sub: "ГОЛОДАЕТ — никогда" },
  ]}
  edges={[
    { from: "run", to: "enqueue", label: "планирует себя" },
    { from: "enqueue", to: "nonempty", label: "в очереди" },
    { from: "nonempty", to: "run", label: "возврат в цикл" },
    { from: "nonempty", to: "starved", label: "никогда не достигается" },
  ]}
  caption="Три верхних узла образуют замкнутый цикл: микрозадача → ставит микрозадачу → очередь непуста → микрозадача. Поскольку очередь микрозадач никогда не пуста, цикл не выходит из checkpoint, и макрозадача и шаг рендеринга (внизу) никогда не достигаются."
/>

Горячий путь — это самозамкнутый цикл: <Pulse>микрозадача → ставит микрозадачу → очередь непуста → микрозадача</Pulse>. Пока он крутится, не рисуется ни один кадр.

<NumbersCard
  id="card-starvation-signals"
  title="Сигналы голодания микрозадач в DevTools"
  data-lesson-visual
  stats={[
    { label: "Жёлтый скриптовый прямоугольник", value: "непрерывный, никогда не прерывается" },
    { label: "Записи Long Tasks API", value: "длительность растёт без границ" },
    { label: "INP", value: "> 1 000 мс" },
    { label: "Счётчик кадров", value: "0 fps во время голодания" },
    { label: "Лечение", value: "вставить уступку уровня задачи в цепочку" },
  ]}
/>

<Quiz
  id="sr-starvation-quiz-1"
  pieceSlug="04-microtask-starvation"
  lang="ru"
  question="Колбэк `MutationObserver` срабатывает синхронно после DOM-мутации. Каким видом работы он ставится в очередь?"
  choices={[
    { label: "Микрозадача — срабатывает на следующем microtask checkpoint, до следующей задачи или кадра", correct: true },
    { label: "Задача из источника DOM-манипуляций", misconception: "MutationObserver явно определён как использующий очередь микрозадач именно для того, чтобы наблюдатели видели все мутации из синхронного блока сразу, а не чередуясь с рендерингом." },
    { label: "Синхронный — срабатывает в момент завершения мутации", misconception: "Синхронный режим сломал бы смысл пакетирования MutationObserver. Он накапливает записи, затем срабатывает один раз на следующем microtask checkpoint." },
    { label: "Задача из источника пользовательских взаимодействий", misconception: "Программные DOM-мутации не имеют ничего общего с пользовательскими взаимодействиями. Очередь — микрозадач, независимо от того, кто вызвал мутацию." },
  ]}
/>

## Long Tasks API и Long Animation Frames

**PerformanceLongTaskTiming.** Появился в 2017, вызывает запись `PerformanceObserver` для любой задачи, превышающей 50 мс на главном потоке. Каждая запись включает время начала, длительность и массив `attribution` с исходным контекстом просмотра. Ограничение: attribution редко указывает на конкретную функцию; он говорит «что-то внутри iframe A заняло 230 мс» — полезно для SLO-дашбордов, менее полезно для поиска корневой причины. Используйте Long Tasks API для **подсчёта** длинных задач за сессию, а не для их отладки.

**PerformanceLongAnimationFrameTiming (LoAF).** Появился в 2023–2024 в Chromium. Срабатывает для любого кадра, время рендеринга которого превышает 50 мс. Запись LoAF включает атрибуцию по скриптам: массив того, какие скрипты выполнялись в кадре, что они делали (event-handler, classic-script, module-script, user-callback), сколько каждый занял. Это именно тот продакшен-диагностический инструмент, которым Long Tasks должны были быть с самого начала. Совместно с INP: когда INP регрессирует, запрашивайте записи LoAF из той же сессии, находите виновный кадр, получайте атрибуцию скриптов, деплойте фикс. Полный конвейер: от воспринимаемой пользователем метрики (INP) до конкретного скрипта (LoAF) до конкретной функции (sourcemap по URL скрипта).

<DebugLog
  id="sr-debug-1"
  pieceSlug="04-microtask-starvation"
  lang="ru"
  label="DevTools Performance — сводка длинных задач"
  output={`[Long Task] 312 мс
  attribution: same-origin
  startTime: 2456.3
  duration: 312.4

[Long Task] 287 мс
  attribution: same-origin
  startTime: 5102.7
  duration: 287.1

[INP candidate] 410 мс
  type: pointerdown -> click -> next paint
  startTime: 2401.0
  processingStart: 2456.3
  processingEnd: 2768.7
  presentationTime: 2811.0
  attribution: handleSearch (search.js:142)`}
  outputLang="log"
  question="Поле поиска показывает INP 410 мс, и лог длинных задач указывает на handleSearch. Обработчик делает: валидация запроса → вызов setSearchTerm (Redux) → запуск дебаунсированного fetch → ожидание результатов. Где 312 мс?"
  hint="Временная метка события ввода — 2401, processingStart — 2456, processingEnd — 2768. processingEnd - processingStart и есть длинная задача. Что выполняется между вводом и processingStart?"
  answer="55 мс между временной меткой ввода (2401) и processingStart (2456) — это ожидание освобождения главного потока: другая задача держала его. Затем 312 мс (processingStart до processingEnd) — сам обработчик. 42 мс между processingEnd и presentationTime (2811) — работа рендеринга, следующая за ним. 312 мс почти наверняка — синхронный Redux-редьюсер, выполняющийся на всём дереве состояния (распространённый React-антипаттерн: вычисление больших отфильтрованных списков в редьюсере вместо мемоизированного селектора). Лечение: перенести фильтрацию в селектор с reselect/createSelector, чтобы она пересчитывалась только при изменении входных данных, и запустить тяжёлую фильтрацию в Worker, если датасет настолько большой, что даже оптимизированный селектор превышает 50 мс."
/>

## scheduler.yield() и Scheduler API

**Что делает scheduler.yield().** Scheduler API (Chrome 115+, частично в Edge, Firefox/Safari за флагом на конец 2025) даёт платформе первоклассный примитив уступки. `await scheduler.yield()` приостанавливает текущую задачу, дренирует очередь микрозадач, позволяет ввоту и рендерингу выполниться, и возобновляет приостановленную задачу как **следующую** задачу — с приоритетом, который предотвращает низкоприоритетную работу от вклинивания вперёд. Раньше уступка через `setTimeout(0)` работала для рендеринга, но теряла позицию в очереди (любая задача, запланированная тем временем, выполнялась первой). С `scheduler.yield()` можно разбить задачу 200 мс на четыре куска по 50 мс, которые с точки зрения пользователя остаются одной логической операцией.

**scheduler.postTask() с приоритетами.** Тот же API предоставляет `scheduler.postTask(callback, { priority })` с тремя приоритетами: `user-blocking` (работа ответа на ввод), `user-visible` (по умолчанию), `background` (несрочная). Практический паттерн: направлять обработчики ввода через `user-blocking`, чтобы они прыгали в очереди вперёд фоновой работы вроде сброса аналитики; направлять фоновую аналитику через `background`, чтобы она уступала срочному. Планировщик имеет доступ к системным сигналам (батарея, тепловой троттлинг, видимость страницы), которых нет у JS-кода.

<FadedExample
  client:visible
  id="sr-faded-1"
  pieceSlug="04-microtask-starvation"
  lang="ru"
  title="Уступка длинной задачи без потери логической непрерывности"
  steps={{
    solved: (
      <pre><code>{`async function processItems(items) {
  let lastYield = performance.now();
  const slice = 50; // целевой размер куска в мс
  for (const item of items) {
    doWork(item);
    if (performance.now() - lastYield > slice) {
      // Уступает рендереру и вводу, затем возобновляется
      await (scheduler.yield?.() ?? new Promise(r => setTimeout(r, 0)));
      lastYield = performance.now();
    }
  }
}`}</code></pre>
    ),
    semi: {
      prompt: (
        <p>Заполните недостающее условие и недостающее выражение уступки для нарезки работы на куски по 50 мс.</p>
      ),
      blanks: [
        { id: "condition", expected: /performance\.now/i, placeholder: "JS-выражение для проверки истёкшего времени" },
        { id: "yield", expected: /scheduler\.yield|setTimeout/i, placeholder: "примитив уступки" },
      ],
    },
    blank: {
      prompt: (
        <p>У вас есть функция, обрабатывающая 10 000 элементов и занимающая 500 мс — это регрессирует INP каждый раз, когда пользователь печатает на той же странице. Перепишите её так, чтобы ни одна задача в цикле не превышала 50 мс. В чём структурное изменение?</p>
      ),
      reveal: (
        <p>Отслеживайте истёкшее время внутри цикла. После каждых ~50 мс работы уступайте через <code>scheduler.yield()</code> (или <code>setTimeout(0)</code> как запасной вариант) перед продолжением. Работа теперь распределена по ~10 задачам по ~50 мс каждая, с рендерингом и вводом между ними. INP возвращается к приемлемым значениям, потому что обработчики ввода могут выполняться между кусками. Стоимость — общее wall-clock время: задача 500 мс становится задачей ~600 мс — но пользователь воспринимает это как плавное, потому что каждое взаимодействие подтверждается в пределах одного кадра.</p>
      ),
    },
  }}
/>

<RFCQuiz
  id="sr-rfc-1"
  pieceSlug="04-microtask-starvation"
  lang="ru"
  question="Какая спецификация определяет event loop, очереди задач, очередь микрозадач и шаги рендеринга пошагово?"
  choices={[
    { rfc: "HTML Living Standard § event loops", title: "Определяет event loop, источники задач, microtask checkpoint, алгоритм update-the-rendering", correct: true },
    { rfc: "ECMA-262 (JavaScript)", title: "Определяет микрозадачи (очередь jobs), но не задачи, рендеринг или браузерное планирование" },
    { rfc: "WHATWG Fetch", title: "Определяет fetch и CORS, а не event loop" },
    { rfc: "W3C DOM", title: "Определяет DOM-дерево и события, но модель обработки цикла живёт в HTML" },
  ]}
/>

<DesignPrompt
  id="sr-design-1"
  pieceSlug="04-microtask-starvation"
  lang="ru"
  prompt="Спроектируйте конвейер ввода для поля поиска, фильтрующего 50 000 клиентских элементов и обязанного держать INP ниже 200 мс p75 на среднем Android."
  constraints={[
    "Бюджет кадра: ~10 мс после накладных расходов браузера.",
    "Цель INP: ≤200 мс p75.",
    "Нет длинных задач > 50 мс на главном потоке во время печати.",
    "Результаты фильтрации должны обновляться видимо в пределах 200 мс от последнего нажатия клавиши.",
    "Внеэкранные результаты могут рендериться лениво, но экранные должны быть корректными.",
    "Поддержка браузеров: Chrome, Safari, Firefox (без проблем с воркер-фолбэком).",
  ]}
  canonical="Конвейер работает так: нажатие → дебаунс 100 мс → планирование фильтрации через scheduler.postTask({ priority: 'user-visible' }) → фильтрация работает кусками по 5 000 элементов за задачу, уступая через scheduler.yield() между кусками → после каждого куска отправка частичного обновления результатов в React через useTransition (низкий приоритет) → React рекоцилирует кусками по 5 мс, никогда не блокируя ввод. Сам алгоритм фильтрации предвычислен: строится строчный индекс при загрузке, затем на каждое нажатие выполняется проверка indexOf (O(N) но кеш-дружелюбно). Для датасетов > 50К — перенести фильтрацию в Worker: нажатие на главном → postMessage в воркер → воркер фильтрует → postMessage результатов обратно → главный поток рендерит. Worker платит одним structured-clone за пакет результатов (~5 мс для 1К элементов), но главный поток остаётся свободным. Проверить с DevTools: печатать быстро, подтвердить отсутствие длинных задач > 50 мс, INP < 200 мс p75, поле ввода отвечает в один кадр. Добавить записи LoAF в продакшен-телеметрию для обнаружения регрессий."
  keyPoints={[
    "Дебаунсировать нажатия для объединения работы.",
    "Нарезать фильтрацию на задачи ≤50 мс с scheduler.yield() между ними.",
    "Использовать useTransition, чтобы рекоцилиация React не блокировала ввод.",
    "Для очень больших датасетов — перенести фильтрацию в Worker, главный поток остаётся свободным для ввода/рендеринга.",
    "Подключить LoAF-телеметрию, чтобы регрессии отлавливались в продакшене, а не в локальной разработке.",
  ]}
/>

<Quiz
  id="sr-quiz-2"
  pieceSlug="04-microtask-starvation"
  lang="ru"
  question="LoAF (PerformanceLongAnimationFrameTiming) отличается от PerformanceLongTaskTiming в ключевом аспекте. В каком?"
  choices={[
    { label: "LoAF срабатывает на кадр (время рендеринга > 50 мс) и включает атрибуцию по скриптам; Long Tasks срабатывает на задачу (время выполнения > 50 мс) только с атрибуцией контекста просмотра", correct: true },
    { label: "LoAF срабатывает на задачу; Long Tasks — на кадр: они обратны друг другу", misconception: "Всё наоборот. Long Tasks = на задачу > 50 мс выполнения; LoAF = на кадр > 50 мс времени рендеринга. LoAF — более новый и полезный сигнал, дающий атрибуцию по скриптам." },
    { label: "Они идентичны — LoAF заменил Long Tasks с той же семантикой", misconception: "LoAF имеет более богатую атрибуцию (по скриптам) и срабатывает на время рендеринга кадра, а не на время выполнения задачи. Long Tasks всё ещё существует и полезен для подсчёта, но LoAF — лучший инструмент отладки." },
    { label: "LoAF срабатывает только на кадры, вызванные вводом пользователя; Long Tasks — на все задачи", misconception: "LoAF срабатывает для любого кадра, время рендеринга которого превышает 50 мс, независимо от причины. Взаимодействие — лишь один из триггеров." },
  ]}
/>

</Explanation>

<KeyTakeaway>Микрозадача, планирующая другую микрозадачу до возврата, навсегда голодает шаг рендеринга. В продакшене LoAF даёт атрибуцию по скриптам для регрессий INP; scheduler.yield() разбивает длинные задачи на куски, позволяя вводу и рендерингу выполняться между ними.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="04-microtask-starvation-retrieval"
  lang="ru"
  questions={[
    { q: "Поле поиска показывает INP 410 мс. LoAF сообщает о скрипте длительностью 312 мс, атрибутированном handleSearch. Проследите путь от телеметрии до фикса.", a: "Шаг 1: подтвердить массив scripts записи LoAF — найти запись с наибольшей длительностью и прочитать её sourceURL, sourceFunctionName и sourceCharPosition. Разрешить позицию через sourcemap продакшена, чтобы получить файл:строку в исходном коде. Шаг 2: классифицировать работу — распространённые тяжёлые операции: синхронные Redux-редьюсеры, фильтрующие большие датасеты, синхронное вычисление производного состояния, JSON.parse больших ответов, перестройка хеш-мап. Шаг 3: выбрать правильный фикс — для фильтрации датасета перейти на мемоизированные селекторы (reselect) или перенести в Worker; для производного состояния — вынести вычисление из рендера и мемоизировать; для JSON.parse — стримить или сжать на стороне сервера. Шаг 4: деплоить с INP как CI-гейтом — применить бюджет INP p75 на синтетическом тесте, воспроизводящем последовательность ввода." },
    { q: "В чём разница между scheduler.yield() и await Promise.resolve() как механизмами уступки?", a: "scheduler.yield() ставит задачу — приостановленная функция возобновляется после того, как браузер запустил рендеринг и обработчики ввода. await Promise.resolve() ставит микрозадачу — функция возобновляется до любого рендеринга или ввода, что делает её невидимой для цикла рендеринга. С точки зрения пользователя scheduler.yield() реально уступает; Promise.resolve() — нет." },
    { q: "Опишите паттерн голодания микрозадач и приведите один продакшен-сценарий, где он появляется случайно.", a: "Паттерн: микрозадача планирует ещё одну микрозадачу до возврата, создавая бесконечную цепочку, держащую microtask checkpoint открытым вечно. Цикл никогда не достигает шага 4 (рендеринг). Один продакшен-сценарий: библиотека управления состоянием вызывает реактивные обновления внутри обработчика реакции. Если реакция изменяет состояние, которое запускает другую реакцию, а та изменяет исходное состояние, получается цикл микрозадач, который никогда не разрешается, замораживая страницу. Обнаружение: непрерывный жёлтый скриптовый прямоугольник в DevTools Performance, INP > 1 с, ноль нарисованных кадров во время блокировки." },
  ]}
/>

<Recap lang="ru">
Голодание микрозадач возникает когда микрозадача ставит в очередь другую микрозадачу до возврата — цикл застрял в microtask checkpoint и никогда не достигает шагов рендеринга или ввода. В DevTools это проявляется как один непрерывный жёлтый прямоугольник, а в продакшене — как INP &gt; 1 с. Long Tasks API (PerformanceLongTaskTiming, 2017) считает задачи, превышающие 50 мс, но даёт только атрибуцию контекста просмотра. LoAF (PerformanceLongAnimationFrameTiming, 2023) срабатывает на кадр и даёт атрибуцию по скриптам, что делает его правильным инструментом для поиска корневых причин регрессий INP в продакшене. `scheduler.yield()` из Scheduler API (Chrome 115+) обеспечивает структурированную уступку уровня задач с сохранением приоритета очереди, а `scheduler.postTask()` позволяет назначать приоритеты `user-blocking`, `user-visible` или `background`, чтобы планировщик браузера — имеющий доступ к тепловым и батарейным сигналам — принимал лучшие решения, чем любая написанная вручную очередь приоритетов.
</Recap>
