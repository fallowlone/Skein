---
concepts:
- event-loop
- task-queue
- microtask-queue
- rendering-step
- long-task
- INP
deepensInto:
- 02-queues-and-scheduling
estMin: 12
lang: ru
lessonType: topic
level: junior
order: 1
prereqs: []
slug: 01-loop-model
sources:
- https://html.spec.whatwg.org/multipage/webappapis.html#event-loops
- https://developer.mozilla.org/en-US/docs/Web/API/HTML_DOM_API/Microtask_guide
- https://web.dev/articles/inp
spiral:
- statefulness
status: ready
summary: Как единственный поток браузера чередует задачи, микрозадачи и рендеринг
  — и почему тот, кто работает дольше всех, блокирует всех.
title: 'Event loop: один поток, три очереди'
track: browser
unit: 01-event-loop
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import MetaphorComplete from "~/components/pedagogy/MetaphorComplete.astro";
import NumberDrill from "~/components/pedagogy/NumberDrill.astro";
import NumbersCard from "~/components/layout/NumbersCard.astro";
import PersonaTag from "~/components/pedagogy/PersonaTag.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
Клик по кнопке ощущается мгновенным. Скролл дёргается. Модалка замирает на 300 мс, пока парсится JSON. То, что решает, какой опыт получит пользователь, — это один механизм старше современного веба: event loop.
</Hook>

<Crux>Один поток, две очереди, один шаг рендеринга — и предел в 50 мс на того, кто держит пол.</Crux>

<Explanation>

## Что делает event loop

У браузера один поток, исполняющий ваш JavaScript. Пока он занят, ничего другого не запускается — ни кликов, ни анимаций, ни рендеринга. Event loop решает, что запустить следующим, когда поток освобождается.

В цикле три игрока:

- **Очередь задач** (task queue, макрозадачная очередь) — дискретные работы: колбэки `setTimeout`, распарсенные куски HTML, сработавшие DOM-события, сообщения message-channel.
- **Очередь микрозадач** (microtask queue) — колбэки резолва промисов и работы `queueMicrotask`. Дренируется до пустой после каждой задачи.
- **Шаги рендеринга** — стили, компоновка, отрисовка, композитинг. Идут между итерациями, но только когда браузер считает, что кадр пора показать.

<FlowDiagram
  label="Цикл event loop: синхронный код выполняется на стеке вызовов, асинхронная работа передаётся Web/Host API, завершённые колбэки попадают в очередь задач, а event loop возвращает колбэк на стек вызовов только когда стек пуст."
  perRow={4}
  hue="var(--d-frontend)"
  nodes={[
    { id: "stack", label: "Стек вызовов", sub: "runs JS now" },
    { id: "apis", label: "Web / Host API", sub: "timers, fetch, DOM" },
    { id: "queue", label: "Очередь задач", sub: "ready callbacks" },
    { id: "loop", label: "Event loop", sub: "stack empty?" },
  ]}
  edges={[
    { from: "stack", to: "apis", label: "hand off async" },
    { from: "apis", to: "queue", label: "callback ready" },
    { from: "queue", to: "loop", label: "next callback" },
    { from: "loop", to: "stack", label: "only if empty" },
  ]}
  caption="Ребро цикла срабатывает только когда стек вызовов пуст. Пока ваш синхронный код не вернётся, ни один колбэк из очереди — таймер, клик или резолв fetch — не сможет войти на стек."
/>

<NumbersCard
  id="card-loop-numbers"
  title="Бюджеты и лимиты event loop"
  data-lesson-visual
  stats={[
    { label: "Бюджет кадра при 60 fps", value: "16,67 мс" },
    { label: "Порог длинной задачи", value: "50 мс" },
    { label: "Хороший INP (p75)", value: "≤200 мс" },
    { label: "Зажим setTimeout(0) после 5 уровней", value: "4 мс" },
    { label: "Срез работы React 18", value: "5 мс" },
    { label: "Cross-thread postMessage hop", value: "~1 мс + клон" },
  ]}
/>

## Метафора бариста

Представьте кофейню с одним сотрудником. Клиенты кладут заказы на стойку (очередь задач). Закончив один напиток, бариста сразу проверяет стикеры на эспрессо-машине (микрозадачи — мелкие продолжения вроде «добавить корицу») и делает все стикеры до приветствия следующего клиента. Если она пишет себе стикеры быстрее, чем успевает их выполнять, она вообще не доходит до следующего клиента. Этот режим отказа — голодание микрозадачами (microtask starvation).

Каждая анимация, каждый напечатанный символ, каждый клик, каждый скролл — это задача в очереди, ждущая поток. Держите поток 200 мс — пользователь видит 200 мс замороженного UI. Лечение — не «более быстрый код», а «более короткие задачи».

## Одна итерация, шаг за шагом

Одна итерация цикла делает ровно следующее:

| Шаг | Что выполняется | Когда пропускается |
| --- | --- | --- |
| 1. Выбор задачи | Вытянуть одну задачу из непустой очереди | Если все очереди пусты — простой |
| 2. Выполнение задачи | Исполнить колбэк до конца | Никогда, если шаг 1 выполнился |
| 3. Microtask checkpoint | Дренировать очередь микрозадач | Очередь микрозадач пуста |
| 4. Обновление рендеринга | rAF → стили → компоновка → отрисовка → композитинг | Только на границе кадра (~16,67 мс) |
| 5. В начало | Вернуться к шагу 1 | Никогда |

## Конкретный сценарий

<PersonaTag id="bea" lang="ru" /> жмёт Submit. <PersonaTag id="sven" lang="ru" /> комментирует: «Событие клика встаёт в очередь как задача. Запускается submit-обработчик — 4 мс на валидацию, ставит fetch. Fetch резолвится позже, планирует микрозадачу `.then()`. Микрозадача выполняется, меняет состояние. React делает re-render — ещё JS, ещё работа на том же потоке. Итого: 28 мс. Бюджет кадра — 16,67 мс, значит пользователь видит один пропущенный кадр.»

<DragOrder
  id="jr-drag-1"
  pieceSlug="01-loop-model"
  lang="ru"
  prompt="Внутри одной итерации event loop браузера эти шаги идут в фиксированном порядке. Расставьте их в правильную последовательность."
  items={[
    { id: "task", label: "Вытянуть одну задачу из очереди задач и выполнить до конца" },
    { id: "microtasks", label: "Дренировать очередь микрозадач (выполнять колбэки промисов до пустой)" },
    { id: "raf", label: "Запустить колбэки requestAnimationFrame (только если кадр пора показать)" },
    { id: "render", label: "Стили → компоновка → отрисовка → композитинг (только если кадр пора)" },
    { id: "next", label: "Вернуться в начало: вытянуть следующую задачу" },
  ]}
  correctOrder={["task", "microtasks", "raf", "render", "next"]}
/>

<Quiz
  id="jr-quiz-1"
  pieceSlug="01-loop-model"
  lang="ru"
  question="Внутри обработчика `setTimeout` вы вызываете `Promise.resolve().then(work)`. Когда выполнится `work`?"
  choices={[
    { label: "Сразу после возврата из обработчика `setTimeout`, до того как цикл вытянет следующую задачу", correct: true },
    { label: "На следующем тике setTimeout, через ~4 мс", misconception: "Промисы — это микрозадачи. Микрозадачи дренируются до того, как цикл перейдёт к следующей задаче — они не ждут очередь таймеров." },
    { label: "На следующем кадре анимации", misconception: "rAF и микрозадачи — разные очереди. Микрозадачи дренируются после каждой задачи; rAF идёт только когда кадр пора." },
    { label: "Только когда браузер простаивает", misconception: "Простой — это для `requestIdleCallback`. У микрозадач нет требования простоя — они всегда дренируются." },
  ]}
/>

<Quiz
  id="jr-quiz-2"
  pieceSlug="01-loop-model"
  lang="ru"
  question="Click-обработчик работает 400 мс. Что из этого продолжает работать во время блокировки?"
  choices={[
    { label: "CSS-анимация на `transform` элемента, продвинутого в слой композитора", correct: true },
    { label: "Колбэк `setInterval`", misconception: "setInterval нужен главный поток. Заблокированный главный поток не может вызывать колбэки таймера — они копятся и взрываются пачкой, когда поток освобождается." },
    { label: "Новое событие клика от пользователя", misconception: "События кликов встают в очередь, но не могут быть обработаны, пока не закончится текущая задача. Пользователь воспринимает это как зависший UI." },
    { label: "Колбэк .then у ожидающего промиса", misconception: "Микрозадачам тоже нужен главный поток. Они в очереди, но не могут запуститься, пока поток держит другая задача." },
  ]}
/>

<MetaphorComplete
  id="jr-metaphor-1"
  pieceSlug="01-loop-model"
  lang="ru"
  setup="В метафоре бариста заказы клиентов лежат на стойке — это очередь задач. Стикеры самой бариста от предыдущего заказа, которые она доделывает до приветствия следующего клиента, — это какой вид очереди?"
  accepted={["microtask queue", "microtask", "microtasks", "очередь микрозадач", "микрозадачи", "микрозадача"]}
  canonical="очередь микрозадач"
  explanation="Микрозадачи — короткие продолжения, которые движок гарантированно дренирует до вытягивания следующей задачи. Резолвы промисов, queueMicrotask, колбэки MutationObserver — всё живёт там. Бариста не может начать заказ нового клиента, пока на эспрессо-машине ещё есть стикеры — движок не может обработать следующую задачу, пока очередь микрозадач не пуста. Поэтому неуправляемая цепочка промисов может заголодить цикл: она пишет себе новые стикеры быстрее, чем бариста успевает их доделать, и следующего клиента так и не обслуживают."
/>

<NumberDrill
  id="jr-drill-1"
  pieceSlug="01-loop-model"
  lang="ru"
  question="Задача длится дольше скольки миллисекунд, и браузер официально называет её «длинной задачей» (long task), вносящей вклад в плохой INP?"
  expected={50}
  tolerance={0}
  unit="мс"
  explanation="W3C Long Tasks API определяет 50 мс как предел. Всё длиннее считается «длинной задачей», слишком долго блокирующей ввод. INP (Interaction to Next Paint) берёт самую медленную задержку ввод-до-отрисовки за сессию и репортит p98. «Хороший» INP — ≤200 мс; цепочка от одной длинной задачи к плохому INP прямая: длинная задача → отложенный rAF → отложенная отрисовка → высокий INP."
/>

</Explanation>

<KeyTakeaway>Один поток, две очереди, один шаг рендеринга. Микрозадачи дренируются до рендеринга. Задачи длиннее 50 мс голодают ввод и раздувают INP — единственное лечение — более короткие задачи.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="01-loop-model-retrieval"
  lang="ru"
  questions={[
    { q: "Назовите трёх игроков event loop браузера.", a: "Очередь задач (task queue / macrotask queue), очередь микрозадач (microtask queue) и шаги рендеринга (стили/компоновка/отрисовка/композитинг). Одна задача выполняется до конца, затем микрозадачи дренируются до пустой, затем опционально идёт рендеринг — затем цикл повторяется." },
    { q: "Почему click-обработчик на 400 мс замораживает страницу, хотя CSS-анимации продолжают работать?", a: "CSS transform-анимации на элементах-слоях композитора работают на отдельном GPU-потоке и не нуждаются в главном потоке. Всё остальное — JS-колбэки, новые click-обработчики, микрозадачи — стоит в очереди за выполняющейся задачей. Пользователь видит зависший UI, потому что поток ввода записывает события, но главный поток не может их обработать." },
    { q: "Какой порог у W3C для «длинной задачи» и почему это важно для INP?", a: "50 мс. Задачи длиннее 50 мс откладывают microtask checkpoint и шаг рендеринга. INP (Interaction to Next Paint) измеряет время от пользовательского ввода до следующей отрисовки; длинная задача между вводом и отрисовкой напрямую раздувает INP. Хороший INP — ≤200 мс p75." },
  ]}
/>

<Recap lang="ru">
Event loop браузера — однопоточный движок, обрабатывающий одну задачу за раз — колбэк `setTimeout`, click-обработчик, распарсенный кусок HTML. После каждой задачи он полностью дренирует очередь микрозадач (резолвы промисов, колбэки MutationObserver), прежде чем рендерить или обрабатывать ввод. Это делает очередь микрозадач продолжением выполняющейся задачи с точки зрения рендерера. Задачи длиннее 50 мс — длинные задачи; они откладывают рендеринг и обработку ввода, напрямую раздувая INP. Бюджет кадра при 60 fps — 16,67 мс, поэтому 200 мс задача роняет примерно 12 кадров и пользователь ощущает очевидный jank.
</Recap>
