---
concepts:
- timer-clamp
- background-throttle
- timer-coalescing
- requestIdleCallback
- promise-combinators
- page-lifecycle
deepensInto: []
estMin: 14
lang: ru
lessonType: topic
level: middle
order: 3
prereqs:
- 01-loop-model
- 02-queues-and-scheduling
slug: 03-timer-accuracy
sources:
- https://html.spec.whatwg.org/multipage/webappapis.html#timers
- https://developer.mozilla.org/en-US/docs/Web/API/requestIdleCallback
- https://developer.mozilla.org/en-US/docs/Web/API/Page_Visibility_API
spiral:
- latency
status: ready
summary: Почему setTimeout(fn, 100) редко срабатывает ровно через 100 мс — три слоя
  троттлинга, объединение фоновых вкладок, requestIdleCallback и семантика комбинаторов
  промисов.
title: Точность таймеров, троттлинг и фоновая работа
track: browser
unit: 01-event-loop
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import NumberDrill from "~/components/pedagogy/NumberDrill.astro";
import NumbersCard from "~/components/layout/NumbersCard.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
Ваш цикл опроса дрейфует. `setInterval` фоновой вкладки срабатывает раз в минуту вместо каждой секунды. Диагностический таймер, измеряющий 50 мс, показывает 120 мс. Таймеры в браузере — это обещание со звёздочками, и звёздочек становится больше, как только что-то занято.
</Hook>

<Crux>Таймер срабатывает не раньше своей задержки, но может сработать намного позже — три слоя троттлинга накапливаются на главном потоке.</Crux>

<Explanation>

## Почему setTimeout — нижняя граница, а не гарантия

`setTimeout(fn, 100)` не обещает запустить `fn` ровно через 100 мс — он обещает поставить задачу в очередь **не раньше** 100 мс. Фактическое время выполнения зависит от того, когда главный поток следующий раз освободится. Если во время срабатывания таймера выполняется задача длиной 300 мс, ваш колбэк ждёт все 300 мс плюс свою позицию в очереди.

Три слоя троттлинга усугубляют это:

**Слой 1 — зажим вложенности 4 мс.** После 5 уровней вложенности `setTimeout` (или `setInterval`) браузеры зажимают задержку до минимума 4 мс. Это правило спецификации, а не особенность: оно предотвращает busy-loop через цепочки `setTimeout(0)`. Цикл из 100 итераций с `setTimeout(0)` занимает минимум 400 мс независимо от скорости работы.

**Слой 2 — троттлинг фоновой вкладки.** Когда вкладка скрыта (`document.visibilityState === 'hidden'`), браузеры зажимают таймеры до минимума 1 секунда. После того как вкладка несколько минут была скрытой, Chrome дополнительно снижает до одного раза в минуту. Ваш фоновый цикл опроса тихо замедляется до черепашьего темпа.

**Слой 3 — выравнивание/объединение таймеров.** Для экономии батареи браузеры округляют времена срабатывания таймеров до общей сетки, чтобы процессор просыпался один раз для многих таймеров вместо многих раз. Практическое следствие: никогда не используйте `setTimeout`/`setInterval` для чего-либо, требующего субкадровой точности. Для анимаций — `requestAnimationFrame`; для точного планирования — часы `AudioContext` или дельты `performance.now()` внутри rAF-цикла.

<FlowDiagram
  label="Жизнь setTimeout(fn, d): запрошенная задержка зажимается до минимума (около 4 мс при вложенности 5 уровней), задача ставится в очередь, и fn выполняется только когда задержка истекла И стек вызовов пуст."
  perRow={4}
  hue="var(--d-frontend)"
  nodes={[
    { id: "request", label: "setTimeout(fn, d)", sub: "requested delay" },
    { id: "clamp", label: "Зажим до мин.", sub: "~4 ms nested" },
    { id: "queue", label: "В очередь задач", sub: "timer source" },
    { id: "run", label: "Запуск fn", sub: "stack empty" },
  ]}
  edges={[
    { from: "request", to: "clamp", label: "floor delay" },
    { from: "clamp", to: "queue", label: "after delay" },
    { from: "queue", to: "run", label: "stack empty" },
  ]}
  caption="d — лишь нижняя граница. fn не может запуститься, пока И (зажатая) задержка не истекла, И стек вызовов не пуст — выполняющаяся длинная задача отодвигает реальное время срабатывания далеко за d."
/>

<NumbersCard
  id="card-timer-layers"
  title="Три слоя троттлинга"
  data-lesson-visual
  stats={[
    { label: "Нормальная минимальная задержка", value: "0 мс (зависит от позиции в очереди)" },
    { label: "Зажим вложенности (глубина ≥5)", value: "минимум 4 мс" },
    { label: "Фоновая вкладка", value: "≥1 000 мс" },
    { label: "Фоновая вкладка (много минут)", value: "≥60 000 мс" },
    { label: "Объединение для экономии батареи", value: "выравнивание по сетке, ~1 мс джиттер" },
  ]}
/>

<Quiz
  id="mid-timer-quiz-1"
  pieceSlug="03-timer-accuracy"
  lang="ru"
  question="Страница вызывает `setTimeout(loop, 0)`, где `loop` перепланирует себя. После 5 уровней вложенности что происходит с эффективной задержкой?"
  choices={[
    { label: "Зажимается до минимума 4 мс, значит цикл выполняется максимум 250 раз в секунду", correct: true },
    { label: "Остаётся 0 мс; зажим для setInterval, а не setTimeout", misconception: "Зажим применяется к setTimeout (и setInterval) после 5 уровней вложенности. Это правило HTML-спецификации, специально созданное для предотвращения busy-loop через цепочки setTimeout(0)." },
    { label: "Становится отрицательной (способ браузера пометить паттерн голодания)", misconception: "Отрицательные задержки зажимаются до 0, затем до 4 мс после вложенности. Никакой специальной отрицательной пометки нет." },
    { label: "Микрозадачи берут верх и очередь таймеров обходится", misconception: "setTimeout всегда ставит задачу. Микрозадачи не связаны с зажимом." },
  ]}
/>

## Видимость страницы и жизненный цикл event loop

Event loop не останавливается, когда вкладка уходит в фон, но меняет режим. `document.visibilityState` переключается в `hidden`, `requestAnimationFrame` перестаёт срабатывать совсем (нет рисования = нет rAF), и таймеры троттлятся как описано выше.

Событие `visibilitychange` — правильное место для остановки опроса, паузы видео, сброса несохранённого состояния. Современная замена ненадёжного события `unload` — `pagehide` плюс Page Lifecycle API с состояниями `frozen` и `terminated`: браузер может полностью заморозить фоновую вкладку (остановив её цикл), чтобы освободить память, затем оттаять при возврате. Код с открытыми соединениями или таймерами должен слушать `freeze`/`resume`, иначе после оттаивания он работает с устаревшим состоянием — закрытым WebSocket, истёкшим токеном, отсоединённым observer.

<Quiz
  id="mid-timer-quiz-2"
  pieceSlug="03-timer-accuracy"
  lang="ru"
  question="Фоновая вкладка запускает setInterval с опросом раз в секунду. После 5 минут в фоне, как часто Chrome обычно вызывает колбэк?"
  choices={[
    { label: "Раз в минуту — Chrome зажимает фоновые таймеры до минимума 1 минуты после нескольких минут скрытости", correct: true },
    { label: "Раз в секунду — setInterval освобождён от фонового троттлинга", misconception: "setInterval подчиняется тому же фоновому троттлингу, что и setTimeout. Минимум 1 секунда начинается немедленно; минимум 1 минута применяется после более длительного периода." },
    { label: "Каждые 4 мс — зажим вложенности перекрывает фоновый троттлинг", misconception: "Зажим вложенности и фоновый троттлинг накапливаются: фоновый троттлинг всегда побеждает зажим 4 мс в скрытой вкладке." },
    { label: "Вообще не срабатывает — Chrome приостанавливает все таймерные колбэки в фоновых вкладках", misconception: "Таймеры в фоновых вкладках срабатывают, но с троттлингом. Они не приостанавливаются полностью." },
  ]}
/>

## requestIdleCallback — планирование несрочной работы

Не вся работа срочна. Аналитические маяки, предзагрузка, прогрев кеша и сброс логов могут подождать, пока цикл ничем лучшим не занят. `requestIdleCallback(fn)` ставит в очередь колбэк, который браузер запускает только когда итерация заканчивается с запасным временем до следующего дедлайна кадра. Колбэк получает объект `deadline`, чей `timeRemaining()` сообщает, сколько мс можно безопасно использовать (ограничено примерно 50 мс).

Дисциплина: делайте небольшой срез, проверяйте `timeRemaining()`, и если он близок к нулю — перепланируйте остаток ещё одним `requestIdleCallback`. Необязательный параметр `{ timeout }` принудительно запускает колбэк после дедлайна, чтобы работа не голодала вечно.

`requestIdleCallback` — кооперативная противоположность `requestAnimationFrame`: rAF говорит «запусти меня прямо перед следующей отрисовкой», ridle говорит «запусти меня только если никому больше не нужен поток». Вместе они позволяют держать срочную работу на rAF и откладывать всё остальное с критического пути.

<NumberDrill
  id="mid-timer-drill-1"
  pieceSlug="03-timer-accuracy"
  lang="ru"
  question="deadline.timeRemaining() у requestIdleCallback ограничен примерно этим количеством миллисекунд — тем же, что и порог длинной задачи."
  expected={50}
  tolerance={0}
  unit="мс"
  explanation="Браузер ограничивает время простоя ~50 мс, чтобы один idle-колбэк не монополизировал длинный промежуток простоя. Это совпадает с порогом длинной задачи: если ваш idle-колбэк выполняется более 50 мс, он становится длинной задачей с точки зрения Long Tasks API. Ограничение означает, что нужно всегда проверять timeRemaining() и нарезать работу соответствующим образом."
/>

## Комбинаторы промисов и планирование

`Promise.all`, `Promise.allSettled`, `Promise.race` и `Promise.any` меняют **что** вы ожидаете, но не то, как цикл планирует. Все четыре запускают каждый переданный промис немедленно — параллелизм достигается за счёт начала асинхронных операций до их ожидания.

- `Promise.all` — отклоняется при первой ошибке (fail-fast). Подходит, когда любая ошибка делает весь результат бесполезным.
- `Promise.allSettled` — никогда не отклоняется; резолвится с массивом статусов. Подходит когда нужны частичные результаты (пять независимых виджетов дашборда, где ошибка одного не должна обнулять остальные).
- `Promise.race` — завершается при первом урегулировании, успехе или ошибке. Классическое использование: таймаут: `Promise.race([fetchData(), rejectAfter(5000)])`.
- `Promise.any` — резолвится при первом **успехе**, игнорируя отклонения пока все не провалятся. Подходит для избыточности, например гонки трёх CDN-зеркал.

Выбор неправильного комбинатора — ошибка корректности, а не производительности: `Promise.all` для дашборда означает, что ошибка одного медленного виджета обнуляет страницу.

Распространённый баг: написать `const a = await fetchA(); const b = await fetchB();` когда оба fetch независимы. Два запроса выполняются последовательно, потому что второй `await` не начинается до резолва первого. `const [a, b] = await Promise.all([fetchA(), fetchB()])` запускает оба немедленно. Разница в стоимости — один полный RTT. Признак в DevTools — два сетевых запроса с непересекающимся временем там, где оба могли бы выполняться параллельно.

<Quiz
  id="mid-timer-quiz-3"
  pieceSlug="03-timer-accuracy"
  lang="ru"
  question="У вас пять независимых API-вызовов для дашборда. Один из них может провалиться. Какой комбинатор подходит?"
  choices={[
    { label: "Promise.allSettled — никогда не отклоняется; вы получаете результат или ошибку для каждого вызова независимо", correct: true },
    { label: "Promise.all — fail-fast при ошибке любого вызова, показать состояние ошибки", misconception: "Promise.all отклоняется при первой ошибке, что обнулит весь дашборд. allSettled позволяет отрисовать четыре успешных виджета даже если один провалился." },
    { label: "Promise.race — завершиться как только самый быстрый вызов резолвится", misconception: "Promise.race завершается при первом урегулировании, успехе или ошибке. Вы получите только один результат, а не все пять." },
    { label: "Promise.any — завершиться при первом успехе", misconception: "Promise.any резолвится при первом успехе и игнорирует остальное. Вы получите только один результат." },
  ]}
/>

<Quiz
  id="mid-timer-quiz-4"
  pieceSlug="03-timer-accuracy"
  lang="ru"
  question="Срабатывает событие `visibilitychange` фоновой вкладки. Код держит открытый WebSocket и цикл опроса setInterval. Что делать?"
  choices={[
    { label: "Приостановить цикл опроса; держать WebSocket открытым, но прекратить keep-alive; возобновить при visibilitychange обратно в visible", correct: true },
    { label: "Немедленно закрыть WebSocket для экономии ресурсов", misconception: "Закрытие WebSocket означает полное переподключение когда пользователь вернётся. Держать его открытым (без лишних keep-alive) дешевле переподключения." },
    { label: "Ничего не делать — браузер автоматически управляет фоновым троттлингом", misconception: "Браузер троттлит таймеры, но не останавливает вашу логику опроса. WebSocket продолжит keep-alive, расходуя батарею и серверные соединения. Нужно явно приостанавливать." },
    { label: "Использовать событие `freeze` вместо этого — visibilitychange ненадёжно", misconception: "visibilitychange надёжно и срабатывает когда вкладка уходит в фон. freeze срабатывает только когда браузер полностью замораживает вкладку — более сильный сигнал, который происходит позже, если вообще происходит." },
  ]}
/>

</Explanation>

<KeyTakeaway>Таймеры срабатывают не раньше своей задержки, но могут сработать намного позже. Три слоя троттлинга — зажим вложенности (4 мс), троттлинг фоновой вкладки (1 с → 1 мин) и объединение — накапливаются друг на друге. Для субкадровой точности — AudioContext или rAF; для несрочной работы — requestIdleCallback.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="03-timer-accuracy-retrieval"
  lang="ru"
  questions={[
    { q: "Назовите три слоя троттлинга, влияющих на точность setTimeout.", a: "1. Зажим вложенности: после 5 уровней вложенности setTimeout/setInterval минимальная задержка зажимается до 4 мс. 2. Троттлинг фоновой вкладки: скрытые вкладки получают таймеры с зажимом ≥1 с, в итоге ≥1 мин. 3. Объединение таймеров: браузеры выравнивают времена срабатывания таймеров по общей сетке для сокращения пробуждений CPU и экономии батареи." },
    { q: "Когда использовать requestIdleCallback вместо setTimeout(fn, 0)?", a: "Когда работа действительно несрочна и может подождать настоящего простоя — аналитика, предзагрузка, прогрев кеша. requestIdleCallback выполняется только когда цикл имеет запасную ёмкость до следующего дедлайна кадра, а timeRemaining() сообщает, сколько мс у вас есть. setTimeout(fn, 0) выполняется как только главный поток свободен независимо от бюджета кадра, что может всё равно мешать рендерингу." },
    { q: "Почему `const a = await fetchA(); const b = await fetchB();` — баг когда оба fetch независимы?", a: "Последовательный await означает, что fetchB не начинается до резолва fetchA. Если каждый fetch занимает один RTT, ждёте два RTT суммарно. Promise.all([fetchA(), fetchB()]) запускает оба немедленно и ждёт обоих, тратя только один RTT. В DevTools баг виден как два непересекающихся сетевых прямоугольника." },
  ]}
/>

<Recap lang="ru">
`setTimeout(fn, delay)` ставит задачу в очередь не раньше `delay` миллисекунд, но фактическое время срабатывания зависит от доступности главного потока и трёх слоёв троттлинга: зажим вложенности 4 мс (предотвращает busy-loop через setTimeout(0)), троттлинг фоновой вкладки (1 с → 1 мин) и объединение таймеров для экономии батареи. Для точного времени в анимациях — `requestAnimationFrame` или часы `AudioContext`: оба управляются конвейером рендеринга, а не очередью таймеров. Для несрочной работы `requestIdleCallback` выполняется только в настоящие промежутки простоя и даёт бюджет `timeRemaining()` для безопасного нарезания работы. Комбинаторы промисов не меняют планирование цикла, но меняют корректность: `Promise.all` для fail-fast, `Promise.allSettled` для частичных результатов, `Promise.race` для таймаутов, `Promise.any` для избыточности при первом успехе.
</Recap>
