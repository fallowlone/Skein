---
concepts:
- microtask-vs-task
- rendering-step
- requestAnimationFrame
- microtask-starvation
- scheduler-yield
- node-nexttick
estMin: 13
lang: ru
level: senior
order: 7
prereqs: []
slug: quiz-choice
sources:
- https://html.spec.whatwg.org/multipage/webappapis.html#event-loops
- https://web.dev/articles/inp
- https://nodejs.org/en/learn/asynchronous-work/event-loop-timers-and-nexttick
status: ready
summary: "Тест с выбором на синтез по всему юниту — семантика очередей, шаг рендеринга, requestAnimationFrame, starvation, scheduler.yield и Node event loop."
title: 'Event loop: тест с выбором ответа'
track: browser
unit: 01-event-loop
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Шесть вопросов поперёк всего юнита. Каждый отражает решение из реального инцидента производительности — не определение для заучивания, а в какую очередь попадает примитив и чего это стоит следующему кадру.
</Hook>

<Goal lang="ru">
Убедись, что связываешь семантику очередей, шаг рендеринга, троттлинг таймеров, starvation и различия рантаймов — тот синтез, к которому вели отдельные уроки.
</Goal>

<Quiz
  id="01-event-loop-choice-1"
  lessonSlug="01-event-loop"
  lang="ru"
  question="Внутри click-обработчика ты вызываешь setTimeout(t, 0), затем queueMicrotask(m), затем обработчик возвращается. В каком порядке выполнятся t и m относительно следующего paint?"
  choices={[
    { label: "Сначала m (microtask drain до пустоты сразу после обработчика, до любого кадра), затем браузер может отрисовать кадр, затем t как task на более поздней итерации.", correct: true },
    { label: "Сначала t, потому что setTimeout(0) означает нулевую задержку, потом m как microtask.", misconception: "setTimeout(0) ставит task; microtask checkpoint опустошается до того, как loop возьмёт следующий task. queueMicrotask всегда выполняется раньше любого setTimeout-колбэка, поставленного в том же витке." },
    { label: "В порядке регистрации: сначала t, потом m, оба до следующего paint.", misconception: "Тайминг задаёт очередь, а не порядок регистрации. m — microtask (до рендера, немедленно); t — task (после рендера, после checkpoint и, возможно, кадра)." },
    { label: "Оба после следующего paint, потому что обработчик — это task.", misconception: "Microtask drain происходит до шага рендеринга, а не после. m выполнится до любого paint; только t ждёт более поздней итерации." },
  ]}
/>

<Quiz
  id="01-event-loop-choice-2"
  lessonSlug="01-event-loop"
  lang="ru"
  question="Click-обработчик выполняется 400 мс. Что из этого продолжает работать во время блокировки и почему?"
  choices={[
    { label: "CSS-анимация transform на элементе, поднятом на композитор — она идёт на GPU/compositor-потоке и не нуждается в main thread, который держит обработчик.", correct: true },
    { label: "Колбэк .then у pending-промиса, потому что microtask приоритетнее task.", misconception: "Microtask тоже нужен main thread. Он стоит в очереди за выполняющимся task и не может запуститься, пока 400-мс обработчик не вернётся." },
    { label: "Колбэк setInterval, потому что таймеры тикают по своим часам.", misconception: "Таймер лишь ставит task; колбэку всё равно нужен заблокированный main thread. Накопившиеся interval-колбэки прорываются пачкой, когда поток освобождается." },
    { label: "Новый клик от пользователя, потому что у ввода отдельный поток.", misconception: "Поток ввода фиксирует событие, но обработчик идёт на main thread. Клик встаёт в очередь и ждёт — пользователь видит замороженный UI." },
  ]}
/>

<Quiz
  id="01-event-loop-choice-3"
  lessonSlug="01-event-loop"
  lang="ru"
  question="Тебе нужно сделать 200 мс работы, не заморозив ввод. Ты пробуешь await Promise.resolve() между чанками, и страница всё равно зависает. Почему и что реально уступает поток?"
  choices={[
    { label: "await Promise.resolve() ставит microtask, поэтому loop между чанками никогда не доходит до шага рендеринга или ввода — твоя работа просто продлевает текущий task. Уступает поток task-уровневый yield (scheduler.yield(), setTimeout(0), MessageChannel), позволяя выполниться рендерингу и вводу.", correct: true },
    { label: "Promise.resolve() слишком медленный; аллокация промисов в цикле и есть заморозка. Используй await Promise.all.", misconception: "Стоимость аллокации тут пренебрежима. Заморозка структурная: microtask drain происходит до шага рендеринга, поэтому yield к вводу не случается никогда." },
    { label: "await уступает поток только если промис ещё не зарезолвлен; await уже зарезолвленного — это no-op, выполняемый синхронно.", misconception: "Он всё равно приостанавливает функцию и возобновляет её через microtask-очередь — но microtask по определению выполняется до любого кадра, поэтому рендереру он не уступает." },
    { label: "requestAnimationFrame — единственный корректный yield; setTimeout всё равно блокирует.", misconception: "rAF выполняется только на границе кадра, поэтому как общий yield он плох. И setTimeout(0), и scheduler.yield() ставят task, позволяя вводу и рендерингу чередоваться." },
  ]}
/>

<Quiz
  id="01-event-loop-choice-4"
  lessonSlug="01-event-loop"
  lang="ru"
  question="Фоновая вкладка крутит setInterval(poll, 1000). Спустя несколько минут в фоне, как часто срабатывает колбэк и что с этим складывается?"
  choices={[
    { label: "Примерно раз в минуту — скрытые вкладки сразу клампят таймеры до ≥1 с и до ≥1 мин после нескольких минут в фоне; rAF останавливается полностью, а microtask по той же логике дренируются как обычно.", correct: true },
    { label: "Каждую секунду — setInterval освобождён от фонового троттлинга, потому что сам себя перепланирует.", misconception: "setInterval троттлится ровно как setTimeout в скрытых вкладках. Минимум в 1 с применяется сразу, минимум в 1 мин — после более долгого периода в фоне." },
    { label: "Каждые 4 мс — клампинг вложенности это пол и он перебивает видимость.", misconception: "Клампинг вложенности в 4 мс применяется к глубоким цепочкам setTimeout/setInterval; в скрытой вкладке выигрывает гораздо больший фоновый троттлинг." },
    { label: "Никогда — браузер приостанавливает все таймеры в фоновых вкладках.", misconception: "Таймеры срабатывают по троттленной частоте, а не нулевой. Полная приостановка случается только если Page Lifecycle API заморозит вкладку — более сильный и поздний сигнал." },
  ]}
/>

<Quiz
  id="01-event-loop-choice-5"
  lessonSlug="01-event-loop"
  lang="ru"
  question="Поисковый инпут показывает INP 410 мс. Long Tasks API логирует task на 312 мс, но атрибутирует его лишь как 'same-origin'. Какой следующий инструмент верный и почему?"
  choices={[
    { label: "LoAF (PerformanceLongAnimationFrameTiming): он срабатывает на каждый медленный кадр с массивом per-script-атрибуции, так что URL и char-позицию самого тяжёлого скрипта можно разрешить через sourcemap до file:line — Long Tasks даёт лишь атрибуцию на уровне browsing-context, годную для счёта, но не для root-cause.", correct: true },
    { label: "Жми на поле attribution у Long Tasks API сильнее — оно отдаёт функцию, если включить buffered:true.", misconception: "buffered:true лишь добирает записи, появившиеся до подключения observer; атрибуции на уровне скрипта он не добавляет. Атрибуция Long Tasks остаётся на гранулярности browsing-context." },
    { label: "Воспроизведи локально в DevTools — production-атрибуции доверять нельзя никогда.", misconception: "Локальное воспроизведение часто промахивается мимо production-размера payload и класса устройства. LoAF плюс разрешение sourcemap — верный путь production-атрибуции." },
    { label: "Используй метки User Timing API; LoAF и Long Tasks оба лишены деталей тайминга.", misconception: "User Timing требует заранее знать, куда ставить метки. LoAF авто-атрибутирует самый тяжёлый скрипт на медленный кадр без ручной инструментации." },
  ]}
/>

<Quiz
  id="01-event-loop-choice-6"
  lessonSlug="01-event-loop"
  lang="ru"
  question="Библиотека, протестированная только в Node, цепляет process.nextTick(a), затем Promise.resolve().then(b) и полагается, что a выполнится до b. Перенесённая в браузер через polyfill для nextTick, она переворачивает порядок. Почему?"
  choices={[
    { label: "В Node у process.nextTick собственная очередь, дренируемая до microtask-очереди, поэтому a предшествует b. В браузере nextTick нет; polyfill маппит его на queueMicrotask, давая a и b равный microtask-приоритет — порядок становится порядком регистрации, который может отличаться.", correct: true },
    { label: "Браузер выполняет Promise.then на отдельном worker-потоке, поэтому b вырывается вперёд.", misconception: "Все microtask выполняются на main thread. Переворот — про приоритет очереди: nextTick в Node выше microtask; полифилленная версия — нет." },
    { label: "ECMA-262 определяет nextTick, поэтому соответствующие браузеры обязаны соблюдать тот же порядок.", misconception: "process.nextTick специфичен для Node и в ECMA-262 отсутствует. Спека определяет лишь microtask-очередь (jobs), поэтому polyfill схлопывается до queueMicrotask." },
    { label: "setTimeout(0) в polyfill понижает a до task, поэтому он выполняется после b.", misconception: "Корректный polyfill использует queueMicrotask, а не setTimeout — но в любом случае суть та же: пред-microtask-приоритета nextTick в браузере не существует." },
  ]}
/>

<Recap lang="ru">
Сквозная линия юнита — одно решение: в какую очередь попадает этот примитив и чего это стоит следующему кадру. Microtask (колбэки промисов, queueMicrotask, MutationObserver) дренируются до рендеринга и starve его при самопланировании; task (setTimeout, MessageChannel, диспетчеризация ввода) уступают рендерингу, но платят клампами и троттлингом; rAF выполняется только на границе кадра. Диагностируй воспринимаемую задержку через INP, атрибутируй её через LoAF плюс sourcemap и помни, что Node переупорядочивает всё это, ставя process.nextTick выше microtask.
</Recap>
