---
concepts:
- three-track-method
- DevTools-performance
- RUM-attribution
- web-vitals-library
- CI-gate
- LoAF
- capstone-discipline
- measure-before-touch
deepensInto: []
estMin: 20
lang: ru
lessonType: topic
level: senior
order: 4
prereqs:
- browser/08-putting-it-together/03-five-canonical-breaks
- browser/07-core-web-vitals/06-tradeoffs-and-observability
slug: 04-three-track-method
sources:
- https://developer.chrome.com/docs/devtools/performance
- https://web.dev/articles/vitals
- https://web.dev/articles/optimize-inp
- https://web.dev/articles/lcp
- https://developer.chrome.com/docs/lighthouse/overview
spiral:
- latency
- statefulness
- encapsulation
status: ready
summary: Сеньорная диагностическая дисциплина — найди симптом, найди трек-ограничение
  (сеть/основной поток/GPU), исправь доминирующую причину, переизмерь. Плюс многоуровневый
  стек телеметрии, превращающий понимание всей цепи в систему мониторинга производства.
title: 'Метод трёх треков: чтение трасс и построение системы мониторинга'
track: browser
unit: 08-putting-it-together
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import NumbersCard from "~/components/layout/NumbersCard.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
Джуниор-инженер оптимизирует обработчик «Добавить в корзину» — он уже занимает 12 мс, значительно ниже бюджета. INP остаётся на 480 мс. Второй инженер находит 900-миллисекундную задачу гидратации, начинающуюся перед каждым ранним тапом. Исправление обработчика оптимизировало то, что не было ограничением. Ограничением была задача, за которой тап стоял в очереди. Чтение трассы — это не знание того, что вещи медленны — это знание того, какой трек, в какой момент, был фактическим узким местом.
</Hook>

<Crux>Три хода: измерь прежде чем трогать, найди трек-ограничение в момент ограничения, исправь доминирующую причину, затем переизмерь.</Crux>

<Explanation>

**Метод трёх треков.**

Трасса производительности DevTools страницы товара показывает три перекрывающихся трека на одной временно́й шкале: сетевой трек, трек основного потока и трек GPU/композитора. Сеньорный метод диагностики любой медленной страницы:

1. **Найди симптом для пользователя.** Поздний маркер LCP. Долгое взаимодействие в треке Interactions. Красный маркер сдвига макета.
2. **Перейди к моменту, когда симптом произошёл, и спроси, какой трек был ограничением.**
   - Поздний LCP + hero ещё на сетевом треке в момент LCP → проблема сети или обнаружения (поломка 1).
   - Поздний LCP + hero закончился рано + основной поток сплошной → блокирующий JS, задержка рендеринга элемента.
   - Медленное взаимодействие + долгая задача основного потока перекрывает его → гидратация или тяжёлый обработчик (поломки 3 и 5).
3. **Назови ограничение как: «этот трек, в этот момент».** Это исправимое утверждение. «Страница медленная» — нет.

Это превращает «страница медленная» в «основной поток был заблокирован задачей гидратации с 1.6 с до 2.5 с, и тап пользователя в 2.0 с стоял в очереди за ней». Это то, что можно починить.

<NumbersCard
  id="card-three-tracks"
  title="Структура трассы трёх треков"
  stats={[
    { label: "Сетевой трек", value: "HTML → CSS → JS → изображения, параллельно" },
    { label: "Трек основного потока", value: "Парсинг → компиляция → отрисовка → гидратация → обработка" },
    { label: "Трек GPU", value: "Композиция → вывод кадров" },
    { label: "Паттерн узкого места: до отрисовки", value: "Ограничение сети (загрузка hero, блокирующий JS)" },
    { label: "Паттерн узкого места: после отрисовки", value: "Ограничение основного потока (задача гидратации, тяжёлый обработчик)" },
  ]}
  data-lesson-visual
/>

**Глубокое погружение в гидратацию — где встречаются пять уроков.**

Гидратация — шарнир всей трассы, поэтому стоит ещё один проход. Это рекурсор React (урок 05), работающий в специальном режиме: вместо того, чтобы `createRoot` строил свежий DOM, `hydrateRoot` обходит дерево компонентов и *принимает* существующий серверный DOM. Фаза рендеринга выполняется — твои компоненты выполняются, дерево фибр строится — но фаза фиксации, вместо вставки узлов, прикрепляет обработчики событий и запускает эффекты.

Гидратация ограничена JS-бандлом: ничто не гидрируется, пока V8 (урок 03) не распарсил, скомпилировал и не начал выполнять этот бандл. Это одна или несколько задач на цикле событий (урок 01), и большая жадная гидратация — одна длинная задача, блокирующая ввод — которую Core Web Vitals (урок 07) записывает как плохой INP для любого взаимодействия в этом окне. И всё стоимость существует только потому, что страница была серверно-рендерена (урок 06) с самого начала. Гидратация — это где встречаются пять из семи браузерных уроков.

**Глубокое погружение в первую отрисовку — ещё одно плотное пересечение.**

Другое плотное пересечение — последовательность первой отрисовки. Конвейер рендеринга (урок 02) нуждается в DOM и CSSOM; DOM приходит из HTML-парсера, CSSOM из парсинга CSS — обе задачи основного потока (урок 01). JavaScript, который может модифицировать DOM перед первой отрисовкой, сначала должен быть распарсен и скомпилирован V8 (урок 03), и если этот JS блокирует рендеринг, отрисовка ждёт его. Стадия компоновки передаёт слои GPU. LCP (урок 07) срабатывает, когда наибольший элемент этой отрисовки приземляется.

Поэтому «первая отрисовка» — не один механизм — это конвейер рендеринга, парсер, V8 и композитор, скоординированные, с Core Web Vital, наблюдающим за результатом. Ни один слой не является островом; каждая реальная страница — весь стек сразу.

**Финальная дисциплина — три хода.**

Отступи от страницы и посмотри на метод сам. Сеньорный навык, который строит весь этот раздел — не запоминание восьми слоёв — это дисциплина диагностики. У неё три хода:

1. **Измерь прежде чем трогать.** Реальная трасса и полевой RUM, никогда угадывание, потому что симптом рутинно называет неправильный слой. Джуниор, оптимизировавший обработчик 12 мс, ничего не измерял — он угадывал.

2. **Найди ограничение, а не ограничение.** На перекрывающейся временно́й шкале всегда есть один трек, в один момент, который является фактическим узким местом — оптимизация чего-либо другого двигает число, которое не было проблемой. Задача гидратации 900 мс была ограничением; обработчик не был.

3. **Исправь доминирующую причину, затем переизмерь.** Потому что слои взаимодействуют, исправление в одном слое меняет бюджет в других, поэтому трасса после исправления — новый диагноз, а не подтверждение. Сжатие бандла меняет стоимость V8, которая меняет задачу гидратации, которая меняет профиль INP — новая трасса может обнаружить вторичное узкое место.

Джуниор-инженер оптимизирует то, что умеет оптимизировать; сеньор-инженер оптимизирует то, что трасса называет ограничением. Восемь слоёв — это карта; дисциплина — знание, что карта не говорит, где проблема — это делает измерение.

<FlowDiagram
  label="Финальная дисциплина из трёх ходов как цикл: измерь прежде чем трогать, найди один трек-ограничение в момент ограничения, исправь доминирующую причину, затем переизмерь — что питает новый диагноз."
  perRow={3}
  hue="var(--d-frontend)"
  nodes={[
    { id: "measure", label: "1. Измерь", sub: "трасса + RUM" },
    { id: "constraint", label: "2. Найди ограничение", sub: "трек + момент" },
    { id: "fix", label: "3. Исправь + переизмерь", sub: "доминир. причина" },
  ]}
  edges={[
    { from: "measure", to: "constraint" },
    { from: "constraint", to: "fix" },
    { from: "fix", to: "constraint", label: "новый диагноз" },
  ]}
  caption="Никогда не угадывай слой — сначала измерь. Найди один трек в один момент, который является фактическим ограничением, а не просто что-то медленное. Исправление меняет всю систему, поэтому переизмерение — новый диагноз, а не подтверждение: цикл повторяется, пока не появится следующее ограничение."
/>

**Наблюдаемость производства — превращение понимания в систему.**

Ментальная модель конца в конец становится практикой производства через многоуровневую телеметрию, каждый слой наблюдает за границей, которой владеет:

- **RUM с `web-vitals`** захватывает LCP, INP и CLS с атрибуцией — LCP-элемент, INP-скрипт, источник CLS — так что регрессия называет свой собственный слой.
- **Long Animation Frames** (`PerformanceObserver` записи `long-animation-frame`) атрибутируют медленные кадры конкретным скриптам, ловя задачу гидратации и тяжёлые обработчики.
- **Серверное трассирование** наблюдает за TTFB и путём получения данных.
- **Телеметрия сервис-воркера** наблюдает за показателями попаданий в кеш и длительностью `fetch`-обработчика.
- **Синтетические CI-ворота** — регулируемая трасса Lighthouse или Playwright на каждый PR — ловят очевидные регрессии до слияния.

Поскольку медленная страница — проблема цепи, мониторинг должен охватывать каждое звено. И каждая метрика должна нести достаточно атрибуции, чтобы указывать обратно на слой, который сломался. Вот как понимание всей цепи перестаёт быть навыком отладки и становится системой, которая ловит поломку за тебя.

<Quiz
  id="sr-quiz-1"
  pieceSlug="04-three-track-method"
  lang="ru"
  question="Трасса DevTools показывает длинную задачу основного потока 900 мс, начинающуюся в 1.6 с. Тап пользователя в 2.0 с имеет INP 700 мс. Каков правильный диагноз?"
  choices={[
    { label: "Тап стоял в очереди за задачей гидратации — задержка ввода составляет большую часть INP 700 мс; ограничение — трек основного потока, а не обработчик", correct: true },
    { label: "Сам обработчик «Добавить в корзину» занимает 700 мс — оптимизируй обработчик", misconception: "Время обработки обработчика и задержка ввода — разные компоненты INP. INP 700 мс с задачей основного потока 900 мс и тапом в 2.0 с означает, что тап провёл ~500 мс ожидая в очереди — это задержка ввода, не время обработчика." },
    { label: "Трек GPU был узким местом", misconception: "Длинная задача основного потока — проблема трека основного потока. Трек GPU/композитора работает после того, как основной поток зафиксирует — он не источник задержки ввода." },
    { label: "Сеть всё ещё скачивала в момент тапа", misconception: "Длинная задача 900 мс на треке основного потока — не сетевое событие. Сетевые запросы не блокируют основной поток как длинную задачу — они показываются на сетевом треке, пока основной поток простаивает или делает другую работу." },
  ]}
/>

<Quiz
  id="sr-quiz-2"
  pieceSlug="04-three-track-method"
  lang="ru"
  question="Почему «исправь доминирующую причину, затем переизмерь» — лучший подход, чем «исправь всё сразу и проверь»?"
  choices={[
    { label: "Слои взаимодействуют — исправление в одном слое меняет бюджет в других, поэтому трасса после исправления — новый диагноз, а не подтверждение. Пакетные исправления делают невозможным атрибуцию улучшений.", correct: true },
    { label: "Переизмерение нужно только если первое исправление не сработало", misconception: "Переизмерение нужно даже когда исправление сработало — потому что оно меняет систему, и может появиться новое узкое место, ранее скрытое за доминирующим." },
    { label: "Исправить всё сразу быстрее", misconception: "Исправить всё сразу быстрее только если ты прав во всех исправлениях. Если любое исправление неверно или перекорректирует, нельзя изолировать, какое изменение вызвало регрессию." },
    { label: "'Исправить всё' — это сеньорный подход", misconception: "Сеньорный подход — последовательная диагностика — измерь, исправь доминирующую причину, переизмерь. Пакетные изменения без измерения — это джуниорный подход." },
  ]}
/>

<Quiz
  id="sr-quiz-3"
  pieceSlug="04-three-track-method"
  lang="ru"
  question="Почему «гидратация — где встречаются пять уроков» важно для диагностики производства?"
  choices={[
    { label: "Стоимость гидратации зависит от скорости парсинга V8, структуры задач цикла событий, стратегии рендеринга и React fiber — поэтому регрессия INP гидратации может иметь причину в любом из этих слоёв", correct: true },
    { label: "Гидратация всегда является узким местом", misconception: "Гидратация часто является узким местом в серверно-рендеренных приложениях с большими бандлами, но не всегда. Трасса должна это показать прежде, чем ты её исправишь." },
    { label: "Это значит нужно знать все пять уроков, чтобы работать с гидратацией", misconception: "Суть диагностическая, не пресупозициональная. Нужно знать, что гидратация касается всех пяти уроков, чтобы искать в правильном месте, когда INP плохой на странице с быстрой отрисовкой." },
    { label: "Это значит React — основной источник проблем производительности", misconception: "React — один урок. Стоимость гидратации включает парсинг+компиляцию V8 (слой 4), структуру задач цикла событий (слой 1), выбор стратегии рендеринга (слой 6) и рекурсор (слой 5) — не только React." },
  ]}
/>

<Quiz
  id="sr-quiz-4"
  pieceSlug="04-three-track-method"
  lang="ru"
  question="Почему стек наблюдаемости производства нуждается в RUM и синтетических CI-воротах — почему ни один из них не достаточен?"
  choices={[
    { label: "RUM ловит регрессии класса устройств и взаимодействий, которые лабораторные тесты не могут симулировать; CI-ворота ловят регрессии до того, как они достигнут реальных пользователей. Каждый заполняет слепое пятно другого.", correct: true },
    { label: "RUM точнее, поэтому CI-ворота опциональны", misconception: "RUM — реальный вердикт, но требует, чтобы регрессии сначала достигли производства. CI-ворота существуют для ловли регрессий до приземления — RUM один означает, что каждая регрессия попадает к реальным пользователям." },
    { label: "CI-ворота надёжнее, поэтому RUM опционален", misconception: "Лабораторные тесты не могут симулировать реальные распределения классов устройств, реальные взаимодействия пользователей или реальные медленные пути. RUM ловит регрессии, которые быстрое симулированное устройство пропустит." },
    { label: "Оба делают одно и то же по разным расписаниям", misconception: "Они измеряют принципиально разные вещи: RUM захватывает реальные пользовательские сессии с реальными ограничениями устройств; CI захватывает синтетическую сессию на контролируемом устройстве. Они взаимодополняющие, не избыточные." },
  ]}
/>

<Inset kind="why" lang="ru">
Финальная дисциплина отражает классическое insight теории систем: ограничение управляет пропускной способностью всей системы, и оптимизация чего угодно, что не является ограничением, не двигает выход системы. Это Теория ограничений Голдратта, применённая к производительности страниц: найди один трек, в один момент, блокирующий результат — затем сосредоточься исключительно на нём, пока он больше не является ограничением, что обнажает следующее ограничение. Дисциплина трёх ходов (измерь, найди ограничение, исправь и переизмерь) — просто этот принцип, сделанный конкретным для работы с производительностью браузера.
</Inset>

</Explanation>

<KeyTakeaway>Три хода: измерь прежде чем трогать (никогда не угадывай слой), найди трек-ограничение в момент ограничения (не просто медленную вещь), исправь доминирующую причину затем переизмерь (слои взаимодействуют — исправление создаёт новую систему). RUM + CI-ворота превращают эту дисциплину в систему, которая ловит поломки автоматически.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="04-three-track-method-retrieval"
  lang="ru"
  questions={[
    { q: "Объясни метод чтения трасс по трём трекам и почему «симптом и причина находятся в разных слоях» — центральный урок этого раздела.", a: "Трасса DevTools показывает треки сети, основного потока и GPU на одной временно́й шкале. Метод: (1) найди симптом для пользователя — поздний маркер LCP, долгое взаимодействие, маркер сдвига макета; (2) перейди к моменту, когда симптом произошёл, и спроси, какой трек был ограничением; (3) назови его как «этот трек, в этот момент» — это исправимо. Центральный урок: симптомы рутинно называют неправильный слой. «Медленное изображение» был баг обнаружения в слое парсинга, не баг размера сети. «React медленный» был баг мегаморфной формы V8, не баг рекурсора. Тот, кто знает только один слой, чинит слой, который симптом называет. Видение всех трёх треков на одной временно́й шкале — то, что позволяет найти причину, живущую в другом слое, чем симптом." },
    { q: "Что делает гидратацию 'шарниром всей трассы' — какие пять уроков она затрагивает и почему?", a: "Гидратация — место, где пересекаются проблемы пяти уроков в одной задаче. (1) Цикл событий (урок 01): гидратация — задача основного потока — возможно одна длинная задача, блокирующая ввод. (2) V8 (урок 03): гидратация не может начаться, пока V8 не распарсил и скомпилировал JS-бандл. (3) React fiber (урок 05): hydrateRoot запускает рекурсор в режиме принятия, выполняя все компоненты и прикрепляя обработчики. (4) Стратегии рендеринга (урок 06): гидратация существует только потому, что страница была серверно-рендерена — SSG без интерактивности не нуждается в гидратации. (5) Core Web Vitals (урок 07): любое взаимодействие во время задачи гидратации записывается как плохой INP. Это пересечение означает, что регрессия INP гидратации может иметь первопричину в V8 (слишком много для компиляции), стратегии рендеринга (слишком много для гидратации), рекурсоре (дорогие деревья компонентов) или цикле событий (длинная задача без уступки). Трасса — то, что говорит, какой именно." },
    { q: "Опиши стек наблюдаемости производства, превращающий дисциплину трёх треков в систему, автоматически ловящую поломки.", a: "Четыре слоя. (1) RUM с web-vitals: библиотека на основе PerformanceObserver захватывает LCP, INP и CLS с атрибуцией — LCP-элемент плюс разбивка фаз, INP цель взаимодействия плюс скрипт LoAF, CLS сдвигающиеся узлы — с тегами маршрута, класса устройства, страны и релиза. Это реальный вердикт; он ловит регрессии класса устройств и взаимодействий, которые лаборатория никогда не поймает. (2) Long Animation Frames: записи LoAF атрибутируют медленные кадры конкретным скриптам с URL источников и длительностями, превращая 'INP — 340 мс' в 'search.js:88 — доминирующий скрипт.' (3) Серверное трассирование: TTFB и путь получения данных, так что медленный деплой или новый медленный запрос показывается в той же панели. (4) Синтетические CI-ворота: регулируемый Lighthouse или Playwright на каждый PR, бюджетируя LCP, TBT, CLS — ловит очевидные регрессии до достижения производства. Ни RUM, ни CI одного не достаточно: RUM без CI первым доставляет каждую регрессию реальным пользователям; CI без RUM пропускает регрессии класса устройств и взаимодействий." },
  ]}
/>

<Recap lang="ru">
Метод трёх треков превращает «страница медленная» в исправимое утверждение: найди симптом для пользователя (поздний LCP, плохой INP, всплеск CLS), перейди к моменту его возникновения и спроси, какой трек — сеть, основной поток или GPU — был ограничением в тот момент. Гидратация — шарнир трассы, потому что она касается цикла событий, V8, рекурсора React, стратегии рендеринга и Core Web Vitals одновременно; регрессия INP гидратации может иметь первопричину в любом из этих слоёв. Финальная дисциплина имеет три хода: измерь прежде чем трогать (никогда не угадывай), найди трек-ограничение в момент ограничения (а не просто что-то медленное), исправь доминирующую причину затем переизмерь (исправление меняет систему и может обнажить новое узкое место). Многоуровневая телеметрия производства — RUM с web-vitals и атрибуцией LoAF, серверное трассирование, мониторинг сервис-воркера и синтетические CI-ворота — превращает эту диагностическую дисциплину в систему, ловящую поломки до того, как они достигнут пользователей. Восемь слоёв — это карта; измерение — то, что говорит, где проблема действительно живёт.
</Recap>
