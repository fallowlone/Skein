---
concepts:
- relay-race-chain
- hydration-uncanny-valley
- three-track-bottleneck
- canonical-breaks
- ttfb-leverage
- measure-before-touch
estMin: 14
lang: ru
level: senior
order: 7
prereqs: []
slug: quiz-choice
sources:
- https://web.dev/articles/howbrowserswork
- https://web.dev/articles/optimize-inp
- https://web.dev/articles/optimize-lcp
status: ready
summary: "MCQ на синтез поперёк всего раздела браузера — цепочка-эстафета, uncanny valley гидратации, чтение узкого места по трём трекам и пять канонических поломок."
title: 'Всё вместе: тест с выбором ответа'
track: browser
unit: 08-putting-it-together
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Шесть вопросов поперёк всего трека браузера — event loop, render pipeline, V8, воркеры, fiber, SSR/SSG и Core Web Vitals, собранные на одной странице. Каждый — решение, которое ты принимаешь, глядя на реальный trace, а не определение для заучивания.
</Hook>

<Goal lang="ru">
Убедись, что связываешь каждый слой эстафеты загрузки страницы в одну цепочку: почему симптом регулярно называет не тот слой, почему гидратация — это шарнир и какой трек является узким местом в тот момент, который важен.
</Goal>

<Quiz
  id="08-putting-it-together-choice-1"
  lessonSlug="08-putting-it-together"
  lang="ru"
  question="Server-rendered страница товара достигает LCP за 1.4 с (хорошо), но первый тап по 'Add to cart' на 2.0 с фиксирует INP 700 мс. Каково структурное объяснение?"
  choices={[
    { label: "Uncanny valley гидратации: paint (слой 5) завершился быстро, поэтому страница выглядит готовой, но длинная задача гидратации (слой 6) ещё выполняется, так что тап встаёт за ней в очередь как input delay — хороший LCP и плохой ранний INP сосуществуют в SSR-приложениях by design.", correct: true },
    { label: "LCP и INP измеряют одно и то же, поэтому INP 700 мс означает, что LCP на самом деле тоже был плохой.", misconception: "LCP измеряет момент отрисовки крупнейшего элемента; INP — задержку взаимодействия. Они независимы — быстрая отрисовка серверного HTML не защищает интерактивность, пока гидратация блокирует main thread." },
    { label: "Сам обработчик 'Add to cart' занимает 700 мс и его надо оптимизировать.", misconception: "При тапе внутри окна гидратации большая часть 700 мс — это input delay, время в очереди за длинной задачей, а не processing обработчика. Оптимизация обработчика, который не был узким местом, ничего не сдвинет." },
    { label: "Сеть всё ещё качала страницу на 2.0 с.", misconception: "Страница уже отрисовалась на 1.4 с, значит байты пришли. Задержка взаимодействия после отрисовки — это проблема main thread (гидратация), а не сети." },
  ]}
/>

<Quiz
  id="08-putting-it-together-choice-2"
  lessonSlug="08-putting-it-together"
  lang="ru"
  question="DevTools trace показывает поздний LCP, при этом hero-картинка всё ещё на network-треке в момент LCP. Какой слой — реальная причина, а какой за неё платит?"
  choices={[
    { label: "Discovery (слой 2, preload scanner) — причина: hero задан как CSS background-image или lazy-loaded, поэтому найден и загружен поздно; paint (слой 5) платит за это поздним LCP. Фикс: обычный eager img с fetchpriority high в исходном HTML.", correct: true },
    { label: "Render pipeline слишком медленно компонует картинку.", misconception: "Если картинка всё ещё качается в момент LCP, до композитора она не дошла. Pipeline в порядке; сбой в том, что discovery произошёл слишком поздно на слое парсинга." },
    { label: "V8 блокирует отрисовку (render-blocking).", misconception: "Render-blocking JS — это другая ветка: hero загрузился рано, но main thread занят. Здесь hero всё ещё на network-треке, так что узкое место — discovery, а не V8." },
    { label: "Узкое место — GPU-трек.", misconception: "Ресурс, всё ещё качающийся в момент LCP, — это симптом network-трека, коренящийся в discovery. GPU компонует только то, что уже пришло и было отрисовано." },
  ]}
/>

<Quiz
  id="08-putting-it-together-choice-3"
  lessonSlug="08-putting-it-together"
  lang="ru"
  question="Ре-рендер по 'Add to cart', который должен стоить ~5 мс, стоит ~80 мс и роняет INP, хотя бандл маленький, а обработчик короткий. Какова наиболее вероятная межслойная причина?"
  choices={[
    { label: "Мегаморфная горячая функция на пути рендера: аксессор свойства или компаратор увидел слишком много форм объектов, поэтому V8 (слой 3) не может использовать inline cache и сваливается в медленные lookup'ы — reconciler (слой 5) в порядке, а формы в V8 нет. Фикс: стабилизировать формы объектов, чтобы IC оставались мономорфными.", correct: true },
    { label: "Reconciler React по сути медленный и его надо заменить.", misconception: "Reconciler в других местах проходил то же дерево быстро. Ре-рендер 80 мс при коротком обработчике указывает на мегаморфный путь в V8, а не на reconciler — это классическая мисатрибуция 'React медленный'." },
    { label: "Сеть перезапросила данные во время ре-рендера.", misconception: "Синхронная стоимость ре-рендера на main thread — не сетевое событие. Запрос данных показался бы на network-треке, пока main thread свободен, а не как scripting внутри взаимодействия." },
    { label: "CLS — во время взаимодействия сместился layout.", misconception: "CLS измеряет визуальную стабильность, а не задержку взаимодействия. Полоса scripting 80 мс внутри тапа — это проблема INP из-за медленного выполнения, а не сдвиг layout." },
  ]}
/>

<Quiz
  id="08-putting-it-together-choice-4"
  lessonSlug="08-putting-it-together"
  lang="ru"
  question="Деплой выкатывает новый HTML и новый JS-бандл, но часть вернувшихся пользователей ловит runtime-ошибки, а некоторые застревают на сломанной странице даже после деплоя фикса. Какая каноническая поломка и какой слой?"
  choices={[
    { label: "Устаревший кэш service worker (слои 1 и 8): воркер отдаёт прошлонедельный кэшированный app.js cache-first против HTML этой недели — рассинхрон версий модулей — а закэшированная сломанная оболочка ловит вернувшихся пользователей, минуя сеть, где живёт фикс. Фикс: имена файлов с content-hash, кэши с тегом версии, очищаемые в activate, network-first для навигации, kill switch.", correct: true },
    { label: "Hydration mismatch между серверным и клиентским HTML.", misconception: "Mismatch мигает контентом и поднимает CLS внутри сессии; он не переживает деплой и не ловит пользователей на старом коде. Сохранение поперёк деплоев и обход фикса — это сигнатура устаревшего кэша воркера." },
    { label: "CDN слишком агрессивно закэшировал новый бандл.", misconception: "Новый бандл задеплоился нормально; проблема в клиентском кэше (service worker), который отдаёт СТАРЫЙ бандл против нового HTML. Сброс CDN не освободит пользователей, запертых за их собственным воркером." },
    { label: "Мегаморфный путь V8 замедлил новый бандл.", misconception: "Мегаморфизм бьёт по INP, а не по корректности поперёк деплоев. Runtime-ошибки от рассинхрона версий плюс пользователи, запертые на сломанной оболочке, — это statefulness: кэш пережил свою корректность." },
  ]}
/>

<Quiz
  id="08-putting-it-together-choice-5"
  lessonSlug="08-putting-it-together"
  lang="ru"
  question="Джуниор оптимизирует обработчик 'Add to cart' с 12 мс до 6 мс; полевой INP остаётся 480 мс. Что, по методу трёх треков, надо было сделать сначала?"
  choices={[
    { label: "Измерить до того, как трогать, затем найти узкое место — именно ТО, а не какое-нибудь. Trace показывает длинную задачу гидратации 900 мс перед каждым ранним тапом; узкое место — это input delay, а не processing обработчика. Обработчик никогда не был на критическом пути; реальный рычаг — сократить бандл, чтобы укоротить длинную задачу.", correct: true },
    { label: "Оптимизировать обработчик дальше — 6 мс всё ещё слишком много.", misconception: "Processing-время обработчика никогда не было узким местом. INP держится на 480 мс, потому что тап встаёт в очередь за длинной задачей гидратации; никакая оптимизация обработчика этот input delay не трогает." },
    { label: "Переключить страницу с SSR на client-side rendering, чтобы убрать гидратацию.", misconception: "CSR убирает гидратацию, но возвращает пустую первую отрисовку и худшие LCP и SEO — обмен одной проблемы на две. Фикс — сократить то, что гидратируется (code-split, Server Components, islands), а не отказываться от SSR." },
    { label: "Добавить ядер CPU, чтобы гидратация шла параллельно.", misconception: "Гидратация — это в основном одна длинная задача main thread; лишние ядра её не делят. Рычаг — меньше парсить, компилировать и выполнять: меньший бандл и меньше гидратируемых islands." },
  ]}
/>

<Quiz
  id="08-putting-it-together-choice-6"
  lessonSlug="08-putting-it-together"
  lang="ru"
  question="Одна RUM-сессия плоха по всем метрикам: TTFB 2980 мс, LCP 6800 мс (hero — это CSS background-image), INP 540 мс (LoAF называет скрипт гидратации), CLS 0.21 (цена ре-рендерилась). Какой единственный фикс имеет наибольший рычаг для старта и почему?"
  choices={[
    { label: "Чинить TTFB первым. Это пол: ничто ниже по цепочке — FCP, LCP, старт парсинга V8, старт гидратации — не может начаться до первого байта. Срезание ~3 с TTFB (CDN, кэш origin, service-worker оболочка для повторных визитов) роняет FCP и LCP вместе бесплатно, затем discovery LCP, стоимость гидратации и mismatch атакуются параллельно.", correct: true },
    { label: "Чинить CLS первым — он виднее всего пользователям.", misconception: "CLS — реальный баг, но фикс mismatch цены оставляет пол TTFB ~3 с нетронутым. Рычаг — это про то, сколько метрик ниже по цепочке зависят от фикса, а FCP и LCP оба сидят поверх TTFB." },
    { label: "Добавить skeleton loader, чтобы страница ощущалась быстрее.", misconception: "Skeleton прячет ожидание, но не двигает ни одну метрику — main thread всё ещё заблокирован, а байты всё ещё приходят на 2980 мс. Это лечит восприятие, а не узкое место." },
    { label: "Оптимизировать INP первым, потому что это метрика взаимодействия.", misconception: "INP важен, но его корень здесь — длинная задача гидратации ниже бандла. При TTFB 2980 мс paint не может даже начаться ~3 с; фикс пола каскадит в FCP и LCP, так что TTFB — рычаг более высокого приоритета на старте." },
  ]}
/>

<Recap lang="ru">
Сквозная линия через весь трек — одна цепочка: TTFB — это пол, discovery кормит LCP, парсинг V8 плюс reconciler кормят длинную задачу гидратации, и эта задача — шарнир, определяющий ранний INP. Симптом регулярно называет не тот слой — 'медленная картинка' — это discovery, 'React медленный' — это мегаморфная форма V8, 'сломалось после деплоя' — это устаревший кэш воркера. Дисциплина всегда одна: измерить до того, как трогать, найти узкий трек в узкий момент, починить доминирующую причину (TTFB и стоимость гидратации первыми), затем перемерить.
</Recap>
