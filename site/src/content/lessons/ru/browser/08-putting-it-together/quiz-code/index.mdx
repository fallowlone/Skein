---
concepts:
- microtask-ordering
- layout-thrash
- hydration-mismatch
- inp-long-task
- scheduler-yield
- forced-reflow
estMin: 14
lang: ru
level: senior
order: 9
prereqs: []
slug: quiz-code
sources:
- https://web.dev/articles/optimize-inp
- https://developer.chrome.com/docs/devtools/performance
- https://web.dev/articles/howbrowserswork
status: ready
summary: "Прочитай четыре сниппета браузера — порядок microtask, layout thrash, hydration mismatch и блокирующий обработчик — и выбери поведение или фикс с наибольшим рычагом."
title: 'Всё вместе: чтение кода и trace'
track: browser
unit: 08-putting-it-together
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Trace говорит, куда ушло время; код говорит, почему. Читай каждый сниппет так, как читал бы на ревью медленной страницы, и выбирай поведение или фикс, к которому senior-инженер тянется первым.
</Hook>

<Goal lang="ru">
Потренируйся отображать код обратно на слой, который он нагружает: порядок очередей event loop, связку layout/paint в render pipeline, детерминизм гидратации и бюджет INP на main thread.
</Goal>

## Сниппет 1 — порядок очередей

```js
console.log("A");
setTimeout(() => console.log("B"), 0);
Promise.resolve().then(() => console.log("C"));
queueMicrotask(() => console.log("D"));
console.log("E");
```

<Quiz
  id="08-putting-it-together-code-1"
  lessonSlug="08-putting-it-together"
  lang="ru"
  question="В каком порядке это залогируется и почему?"
  choices={[
    { label: "A, E, C, D, B. Синхронный код выполняется первым (A, E). Затем вся очередь microtask осушается до следующей задачи: callback промиса C и queueMicrotask D в порядке постановки. Callback setTimeout B — это macrotask, поэтому выполняется только после всех microtask.", correct: true },
    { label: "A, B, C, D, E — всё выполняется в порядке исходника.", misconception: "Синхронны только A и E. setTimeout и промис планируют callback'и на потом; они не могут выполниться до E, который ещё в текущем синхронном прогоне." },
    { label: "A, E, B, C, D — таймер срабатывает первым, потому что его задержка 0.", misconception: "Таймер на 0 мс всё равно macrotask. Очередь microtask (C, D) всегда осушается полностью до того, как event loop возьмёт следующий macrotask (B)." },
    { label: "A, E, D, C, B — queueMicrotask приоритетнее промиса.", misconception: "И C, и D — microtask в одной очереди; они выполняются в порядке постановки. Промисный .then (C) был поставлен до queueMicrotask (D)." },
  ]}
/>

## Сниппет 2 — цикл ресайза

```js
function fitAll(boxes) {
  for (const box of boxes) {
    const w = box.offsetWidth;     // READ — forces layout
    box.style.width = w + 10 + "px"; // WRITE — invalidates layout
  }
}
```

<Quiz
  id="08-putting-it-together-code-2"
  lessonSlug="08-putting-it-together"
  lang="ru"
  question="При 500 боксах этот обработчик — длинная задача, разрушающая INP. В чём дефект и фикс?"
  choices={[
    { label: "Layout thrash (forced synchronous reflow): каждая итерация читает offsetWidth после того, как write инвалидировал layout, поэтому браузер вынужден пересчитывать layout каждый проход — O(n) reflow. Фикс: сначала все чтения, потом все записи (или кэшировать ширины), превращая n reflow в один.", correct: true },
    { label: "offsetWidth устарел и медленный; используй getBoundingClientRect.", misconception: "getBoundingClientRect тоже форсирует layout — смена API не помогает. Дефект — это чередование чтений и записей, которое форсирует reflow на итерацию независимо от того, какой read-API используется." },
    { label: "Цикл слишком много аллоцирует и триггерит GC.", misconception: "Здесь почти нет аллокаций; стоимость — это повторный forced layout, а не сборка мусора. Trace показывает полосы recalculate-style/layout, а не GC." },
    { label: "style.width надо задавать через CSS-класс для скорости.", misconception: "Класс всё равно инвалидирует layout и, прочитанный в том же цикле, всё равно вызовет thrash. Фикс — разделить фазу чтения и фазу записи, а не механизм стилизации." },
  ]}
/>

## Сниппет 3 — рендер на сервере/клиенте

```jsx
function Price({ cents }) {
  // runs on both server and client during hydration
  const formatted = new Intl.NumberFormat(navigator.language, {
    style: "currency", currency: "USD",
  }).format(cents / 100);
  return <span id="price">{formatted}</span>;
}
```

<Quiz
  id="08-putting-it-together-code-3"
  lessonSlug="08-putting-it-together"
  lang="ru"
  question="Этот компонент периодически мигает ценой и поднимает CLS при гидратации. В чём межслойный баг?"
  choices={[
    { label: "Hydration mismatch (слой 6): navigator.language не существует на сервере, и даже дефолтная локаль может отличаться от клиентской, поэтому серверный HTML и первый клиентский рендер расходятся; React выбрасывает и ре-рендерит поддерево, мигая и сдвигая layout. Фикс: рендерить детерминированное значение на сервере и применять локаль-специфичный формат в post-hydration useEffect.", correct: true },
    { label: "Intl.NumberFormat слишком медленный и блокирует main thread.", misconception: "Intl здесь достаточно быстр; сбой — это недетерминизм, а не стоимость. Сервер и клиент производят разные строки, что и триггерит ре-рендер mismatch и сдвиг." },
    { label: "id='price' конфликтует с другим элементом.", misconception: "Дубликат id невалиден, но не он мигает значением. Мигание — это React, примиряющий рассинхрон текста сервер/клиент: баг детерминизма, а не id." },
    { label: "cents / 100 теряет точность и портит значение.", misconception: "Деление float — не причина периодического мигания при гидратации. Периодичность отслеживает различие локали между сервером и клиентом — проблема детерминизма рендера." },
  ]}
/>

## Сниппет 4 — обработчик клика

```js
button.addEventListener("click", () => {
  cart.add(item);
  renderCartBadge();                  // small DOM update
  const report = JSON.parse(hugeBlob); // ~120 ms on a mid-range phone
  analytics.sendSync(report);          // synchronous beacon
});
```

<Quiz
  id="08-putting-it-together-code-4"
  lessonSlug="08-putting-it-together"
  lang="ru"
  question="Badge обновляется, но INP этого клика ~180 мс даже на свободном потоке. Какой фикс уважает бюджет INP?"
  choices={[
    { label: "Сделать минимум для обновления UI (добавить в корзину, отрисовать badge), затем уступить через await scheduler.yield() перед тяжёлым JSON.parse и аналитикой — или вынести парсинг в worker и слать аналитику async. INP измеряет input-to-next-paint; синхронный parse плюс sync beacon выполняются ДО paint, поэтому засчитываются взаимодействию.", correct: true },
    { label: "Обернуть весь обработчик в requestAnimationFrame.", misconception: "rAF планирует callback перед следующим paint, так что тяжёлая работа всё равно выполнится до этого paint и всё равно засчитается в INP. Уступать надо ПОСЛЕ визуального обновления, а не просто отложить всё на один кадр." },
    { label: "Переместить renderCartBadge в конец обработчика.", misconception: "Перестановка не убирает parse ~120 мс и sync beacon из участка перед paint. Badge обновился бы позже, а не раньше — INP был бы таким же или хуже." },
    { label: "Обработчик в порядке; INP 180 мс неизбежен на mid-range телефоне.", misconception: "180 мс в пределах бюджета, но целиком вызван избегаемой синхронной работой до paint. Уступка после обновления UI и вынос parse режут его до стоимости обновления badge." },
  ]}
/>

<Recap lang="ru">
Четыре сниппета, четыре слоя. Event loop осушает все microtask (промис, queueMicrotask) до следующего macrotask (setTimeout), поэтому порядок — синхронное, затем microtask, затем macrotask. Чередование чтений и записей DOM форсирует reflow на итерацию — сначала чтения, потом записи. Компонент, рендерящий локаль- или браузер-зависимое значение на сервере, — это hydration mismatch: сделай серверный рендер детерминированным, а клиент-only форматирование отложи в useEffect. И любая синхронная тяжёлая работа в обработчике до paint засчитывается в INP — обнови UI минимально, затем уступи. Навык — читать код прямо обратно на трек, который он нагружает.
</Recap>
