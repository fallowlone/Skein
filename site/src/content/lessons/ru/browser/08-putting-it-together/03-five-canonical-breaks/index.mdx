---
concepts:
- uncanny-valley
- late-hero-LCP
- hydration-mismatch
- megamorphic-IC
- stale-service-worker
- synchronous-handler
- scheduler-yield
deepensInto:
- browser/08-putting-it-together/04-three-track-method
estMin: 22
lang: ru
lessonType: topic
level: senior
order: 3
prereqs:
- browser/08-putting-it-together/02-eight-layers-traced
- browser/03-v8-internals/06-turbofan-and-deopt
- browser/04-workers/06-service-worker-edge-cases
- browser/05-react-fiber/06-bailout-and-memoisation
- browser/06-ssr-vs-ssg/04-hydration-mismatch
- browser/07-core-web-vitals/03-inp-mechanics
slug: 03-five-canonical-breaks
sources:
- https://web.dev/articles/optimize-lcp
- https://web.dev/articles/optimize-inp
- https://developer.chrome.com/docs/devtools/performance
- https://web.dev/articles/lcp
- https://web.dev/articles/vitals
spiral:
- latency
- statefulness
- encapsulation
status: ready
summary: Зловещая долина гидратации как объединяющий сбой и пять канонических поломок
  — поздний hero-элемент (LCP), несоответствие гидратации (CLS), мегаморфный путь
  отрисовки (INP), устаревший кеш сервис-воркера (корректность), синхронный обработчик
  (INP) — каждая владеет другим слоем.
title: 'Пять канонических поломок: где производство стабильно ломается'
track: browser
unit: 08-putting-it-together
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DebugLog from "~/components/pedagogy/DebugLog.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import DesignPrompt from "~/components/pedagogy/DesignPrompt.astro";
import RFCQuiz from "~/components/pedagogy/RFCQuiz.astro";
import FadedExample from "~/components/pedagogy/FadedExample.tsx";
import NumbersCard from "~/components/layout/NumbersCard.astro";

<Hook>
Команда запускает Lighthouse: 94, зелёный. Затем приходит RUM-отчёт — LCP в поле 6.8 с, INP 540 мс, CLS 0.21, все плохо. Лаборатория тестировала чистую сессию без сервис-воркера; в поле — реальные пользователи на среднебюджетных телефонах, тапающие кнопки во время гидратации. Лаборатория и поле измеряют одну страницу — с разных углов, и угол поля — это то, в чём живут пользователи.
</Hook>

<Crux>Пять канонических поломок расширяют долину гидратации или происходят внутри неё — знай их, и знаешь, где производство стабильно ломается.</Crux>

<Explanation>

**Зловещая долина как объединяющий сбой.**

Единственный сбой, связывающий весь браузерный раздел воедино — это зазор между «нарисовано» и «интерактивно» — зловещая долина гидратации. Это не один баг; это структурное свойство серверно-рендеренных, клиентски-гидрируемых приложений. Каждый другой сбой в этом уроке либо расширяет эту долину, либо происходит внутри неё.

Сеньорный фрейм: страница имеет два момента завершения — LCP-ish («выглядит готово») и TTI-ish («работает»), и всё мастерство современной фронтенд-производительности — это сжатие расстояния между ними. Вот пять канонических поломок — места, где производство наиболее стабильно ломается вдоль трассы.

<NumbersCard
  id="card-five-breaks"
  title="Пять канонических поломок — слой и метрика"
  stats={[
    { label: "Поломка 1: поздний hero-элемент", value: "Слои 2+5 → LCP" },
    { label: "Поломка 2: несоответствие гидратации", value: "Слой 6 → CLS + корректность" },
    { label: "Поломка 3: мегаморфный путь отрисовки", value: "Слои 4+7 → INP" },
    { label: "Поломка 4: устаревший кеш сервис-воркера", value: "Слои 1+8 → корректность" },
    { label: "Поломка 5: синхронная работа в обработчике", value: "Слой 7 → INP" },
  ]}
  data-lesson-visual
/>

**Поломка 1 — Поздняя загрузка hero-изображения (LCP).**

Hero-изображение — LCP-элемент, но оно указано в CSS как `background-image`, или несёт `loading="lazy"`, или внедряется JavaScript-ом. Сканер предзагрузки не может его увидеть, поэтому загрузка начинается поздно — и LCP, который измеряет именно этот элемент, приземляется на 4+ секундах. Трасса показывает длинную фазу задержки загрузки ресурса.

Исправление: LCP-изображение должно быть обычным `<img>` в исходном HTML, загружаться жадно, с `fetchpriority="high"`. Это слой 2 (обнаружение), падающий, и слой 5 (отрисовка), платящий за это.

**Поломка 2 — Несоответствие гидратации (CLS плюс корректность).**

Компонент рендерит значение, которое различается между сервером и клиентом — временну́ю метку, флаг из `localStorage`, цену в локальном формате. Серверный HTML и первый рендер клиента расходятся; React выбрасывает серверное поддерево и перерендеривает его, мигая изменившимся контентом и вызывая всплеск CLS. Трасса показывает маркер сдвига макета сразу после задачи гидратации.

Исправление: детерминизм рендеринга — откладывать только-клиентские значения в `useEffect` после гидратации. Это слой 6 (гидратация), падающий потому что сериализованное состояние не было точно воспроизведено на клиенте.

**Поломка 3 — Мегаморфный путь отрисовки (INP).**

Обработчик «Добавить в корзину» запускает перерисовку, и горячая функция на этом пути — средство доступа к свойству, компаратор — мегаморфна: она видела слишком много форм объектов, поэтому V8 не может использовать встроенный кеш и откатывается к медленным поискам. Перерисовка, которая должна занять 5 мс, занимает 80 мс, и INP для взаимодействия плохой. Трасса показывает длинную полосу выполнения скриптов внутри взаимодействия.

Исправление: стабилизировать формы объектов, чтобы встроенные кеши V8 оставались мономорфными. Это слои 4 (V8) и 7 (взаимодействие), переплетённые.

**Поломка 4 — Устаревший кеш сервис-воркера (корректность).**

Деплой привозит новый HTML и новый JS-бандл, но сервис-воркер отдаёт прошлонедельный закешированный `app.js` в режиме cache-first против HTML этой недели — несоответствие версий модулей, ошибка во время выполнения. Или хуже — сломанная оболочка приложения закешированна, и каждый повторный посетитель застрял на ней, минуя сеть, где живёт исправление.

Исправление: имена ассетов с хешем содержимого, кеши с версионными метками, очищаемые в `activate`, навигация network-first, рубильник. Это слои 1 / 8 (воркер), падающие через состояние — кеш пережил свою корректность.

**Поломка 5 — Синхронная работа в обработчике события (INP).**

Обработчик «Добавить в корзину» делает что-то синхронное и тяжёлое в основном потоке перед уступкой — отправляет синхронный аналитический маяк, запускает большой `JSON.parse`, пересчитывает производный список. Время обработки обработчика само по себе превышает бюджет INP в 200 мс. Трасса показывает длинную жёлтую полосу скриптинга между вводом и следующей отрисовкой.

Исправление: делать минимум для обновления UI, затем уступать (`scheduler.yield()`), и переносить чистые тяжёлые вычисления в воркер. Это слой 7 (взаимодействие), падающий напрямую из-за правил цикла событий.

**Почему каждую поломку сложно диагностировать с одного слоя.**

Поломка 1 выглядит как «медленное изображение» для того, кто знает только сеть — но изображение было нормального размера; настоящая причина — обнаружение, проблема слоя парсинга. Поломка 3 выглядит как «React медленный» для того, кто знает только рекурсор — но рекурсор был в порядке; настоящая причина — мегаморфная форма, проблема V8. Весь смысл трассировки от начала до конца в том, что симптом и причина рутинно находятся в разных слоях, и только тот, кто видит всю цепь, перестаёт чинить симптомы.

<DebugLog
  id="sr-debug-1"
  pieceSlug="03-five-canonical-breaks"
  lang="ru"
  label="RUM — атрибуция всей цепи для одной медленной сессии"
  output={`[TTFB]  2980 мс  (попадание в сервис-воркер не произошло; холодный источник)
[FCP]   3240 мс
[LCP]   6800 мс  элемент: div.hero[style*=background-image]
[INP]   540 мс   взаимодействие: click .add-to-cart
                 скрипт LoAF: vendor.js (гидратация) 482 мс
[CLS]   0.21     источник: #price (несоответствие гидратации)`}
  outputLang="log"
  question="Одна RUM-сессия, все метрики плохи. Пройди цепь: назови слой за каждым числом и одно наиболее высокоэффективное исправление, с которого начать."
  hint="Читай её как цепь сверху вниз. TTFB питает FCP питает LCP. Затем INP и CLS — отдельные сбои. Какое одно число, исправленное, помогает большинству других?"
  answer="Идём по слоям. TTFB 2980 мс (слой 1, сеть): попадания в сервис-воркер не было, холодный медленный источник — пол под всем уже 3 секунды. FCP 3240 мс следует почти сразу за TTFB — как только байты пришли, произошла отрисовка, поэтому сам конвейер рендеринга в порядке; FCP просто стоит поверх ужасного TTFB. LCP 6800 мс (слои 2 и 5): элемент — div.hero с CSS background-image — сканер предзагрузки не может видеть background-image, поэтому он был обнаружен поздно и загружен поздно; это поломка 1, поздняя загрузка hero. INP 540 мс (слои 6 и 7): атрибуция LoAF называет vendor.js работающим 482 мс — это задача гидратации; клик встал в очередь за ней; это зловещая долина. CLS 0.21 (слой 6): атрибутирован #price, несоответствие гидратации — цена рендерилась по-разному на сервере и клиенте (вероятно, форматирование локали или валюты), React перерендерил её и сдвинул макет; это поломка 2. Наиболее высокоэффективное исправление для начала: TTFB. Это пол — 3 секунды до того, как может произойти что-либо ещё — и он питает FCP и LCP напрямую. Исправь источник (кеширование, CDN, заставь сервис-воркер отдавать оболочку) и FCP, и LCP упадут вместе примерно на 3 секунды бесплатно. Затем параллельно: сделай hero настоящим img в HTML, чтобы исправить обнаружение LCP, откладывай только-клиентское форматирование цены в useEffect, чтобы убить CLS-несоответствие, и сократи и раздроби бандл, чтобы задача гидратации перестала доминировать в INP. Но сначала TTFB — это одно число, исправление которого помогает наибольшему числу других."
/>

<TradeoffMatrix
  id="card-failure-cost"
  pieceSlug="03-five-canonical-breaks"
  lang="ru"
  prompt="Страница товара имеет хороший LCP, но плохой INP в первые несколько секунд. У тебя есть время на одно исправление в этом спринте. Выбери."
  options={[
    { name: "Сократить и разбить JS-бандл, чтобы задача гидратации была меньше", summary: "Атакует зловещую долину в корне.", correct: true, justification: "Хороший LCP плюс плохой только-ранний INP — это точная сигнатура зловещей долины гидратации: страница рисуется быстро, но задача гидратации блокирует ранние взаимодействия. Сокращение бандла (разделение кода, Server Components, меньше гидрируемых островов) напрямую укорачивает эту длинную задачу — меньше для скачивания, меньше для V8 для парсинга и компиляции, меньше для выполнения. Это атакует причину по слоям 4, 6 и 7 одновременно. Это наиболее высокоэффективное одиночное исправление." },
    { name: "Оптимизировать сам код обработчика «Добавить в корзину»", summary: "Помогает поздним взаимодействиям, не ранним.", justification: "Более быстрый обработчик сокращает время обработки, но если INP нормален после первых нескольких секунд, обработчик уже достаточно быстр, как только поток свободен. Ранняя медлительность — задержка ввода от гидратации — более быстрый обработчик её не затрагивает." },
    { name: "Дополнительно оптимизировать hero-изображение", summary: "LCP уже хороший — неправильная метрика.", justification: "LCP хорош, поэтому hero-изображение не проблема. Тратить спринт здесь улучшает метрику, которая уже проходит, и оставляет фактический сбой — INP — нетронутым." },
    { name: "Добавить скелетный загрузчик во время гидратации", summary: "Скрывает симптом, не исправляет его.", justification: "Скелет делает ожидание выглядящим намеренным, что является UX-удобством, но взаимодействие всё ещё задержано и INP всё ещё записывает задержку. Основной поток всё ещё заблокирован — метрика не сдвигается." },
  ]}
/>

<RFCQuiz
  id="sr-rfc-1"
  pieceSlug="03-five-canonical-breaks"
  lang="ru"
  question="Какой инструмент показывает работу сети, основного потока и GPU страницы на одной перекрывающейся временно́й шкале — основу метода диагностики по трём трекам?"
  choices={[
    { rfc: "Панель Performance в DevTools", title: "Записывает и накладывает треки сети, основного потока, GPU и взаимодействий для одной сессии", correct: true },
    { rfc: "Консоль DevTools", title: "Показывает логи и ошибки — нет временно́й шкалы перекрывающихся треков" },
    { rfc: "Панель Elements в DevTools", title: "Инспектирует живой DOM и стили — не временна́я шкала производительности" },
    { rfc: "Панель Network в DevTools", title: "Показывает сетевой трек детально, но не треки основного потока или GPU рядом с ним" },
  ]}
/>

<DesignPrompt
  id="sr-design-1"
  pieceSlug="03-five-canonical-breaks"
  lang="ru"
  prompt="Ты наследуешь страницу товара e-commerce из этой трассы. Данные поля: LCP 4.8 с, INP 480 мс, CLS 0.18 — все плохи. Разработай полный план исправления, слой за слоем, в порядке приоритета."
  constraints={[
    "Среднебюджетный Android, 4G — реальный сегмент p75 пользователей.",
    "Серверно-рендеренный React с уже зарегистрированным сервис-воркером.",
    "Hero-изображение, цена, Добавить-в-корзину, отзывы ниже сгиба.",
    "Нужно улучшить все три Core Web Vitals до «хорошего» на поле p75.",
    "Каждое исправление должно называть целевой слой и метрику, которую оно двигает.",
    "Упорядочи работу так, чтобы наиболее высокоэффективные исправления шли первыми.",
  ]}
  canonical="Сначала диагностируй с трассой DevTools и атрибуцией RUM, затем упорядочи по рычагу. (1) TTFB / сеть: подтверди, что сервис-воркер отдаёт оболочку приложения для повторных визитов и источник закеширован на CDN для первых визитов — TTFB это пол под LCP, поэтому это первое. (2) Обнаружение LCP: сделай hero простым img в исходном HTML, жадно загружаемым, fetchpriority high, в AVIF/WebP с srcset — исправляет поломку позднего hero, двигает LCP. (3) Стоимость гидратации / INP: это наибольший рычаг интерактивности — раздели бандл, преврати только-для-отображения компоненты (статическое описание товара, оболочку списка отзывов) в Server Components, чтобы они не несли клиентский JS и никогда не гидрировались, сделай отзывы ниже сгиба островом, который гидрируется при входе в область видимости, чтобы жадная задача гидратации сжалась и ранние тапы перестали становиться в очередь за ней. (4) CLS: задай явные width/height для hero и каждого изображения, зарезервируй min-height для отзывов и любых рекламных слотов, нейтрализуй перетекание веб-шрифтов с size-adjust, и откладывай любое только-клиентское значение (цена в формате локали, персонализированный бейдж) в useEffect после гидратации, чтобы первый клиентский рендер совпадал с сервером — убивает и сдвиг макета, и несоответствие гидратации. (5) Обработчик взаимодействия: обеспечь, чтобы Добавить-в-корзину делал минимальную синхронную работу, уступал с scheduler.yield() перед любым тяжёлым продолжением, и отправлял аналитику асинхронно. Наблюдаемость: подключи RUM web-vitals с атрибуцией и регулируемый Lighthouse CI на каждый PR, чтобы регрессия каждого слоя была поймана. Проверь: LCP поля p75 ниже 2.5 с, INP ниже 200 мс, CLS ниже 0.1."
  keyPoints={[
    "Упорядочи по рычагу: сначала TTFB (это пол), затем обнаружение LCP, затем стоимость гидратации.",
    "Гидратация — наибольший рычаг INP — разделение кода, Server Components, острова для ниже сгиба.",
    "Исправь CLS с зарезервированным пространством и детерминизмом рендеринга (откладывай только-клиентские значения).",
    "Каждое исправление называет свой слой и метрику, которую оно двигает — без угадывания.",
    "Атрибуция RUM плюс CI-ворота превращает понимание всей цепи в систему мониторинга.",
  ]}
/>

<Quiz
  id="sr-quiz-1"
  pieceSlug="03-five-canonical-breaks"
  lang="ru"
  question="Поломка 1 (поздний hero) выглядит как «медленное изображение», поломка 3 (мегаморфный путь) выглядит как «React медленный». Что это иллюстрирует о трассировке?"
  choices={[
    { label: "Симптом и первопричина рутинно находятся в разных слоях — только видя всю цепь, перестаёшь чинить не то", correct: true },
    { label: "Обе поломки — на самом деле один и тот же баг", misconception: "Это разные баги в разных слоях — обнаружение против форм V8. Суть в том, что каждый неправильно назван, если знаешь только один слой." },
    { label: "Симптомы всегда правильно называют свою причину", misconception: "Наоборот — весь аргумент раздела в том, что симптомы рутинно указывают на неправильный слой. «Медленное изображение» было багом обнаружения; «React медленный» был багом форм V8." },
    { label: "Трассировка не нужна, если хорошо знаешь React", misconception: "Глубокое знание одного слоя — это именно то, что вызывает неверный диагноз. Трассировка по слоям — это то, что позволяет найти причину, живущую за пределами твоего любимого слоя." },
  ]}
/>

<Quiz
  id="sr-quiz-2"
  pieceSlug="03-five-canonical-breaks"
  lang="ru"
  question="В RUM-логе TTFB — 2980 мс. Почему исправление TTFB — наиболее высокоэффективная отправная точка?"
  choices={[
    { label: "TTFB — это пол: FCP и LCP не могут произойти до первого байта, поэтому исправление TTFB ~3 с улучшает FCP и LCP вместе", correct: true },
    { label: "TTFB — метрика, которую Google весит наиболее тяжело", misconception: "TTFB — это даже не Core Web Vital. Точка рычага — потому что это пол под LCP — исправление двигает LCP, а не потому что он сам взвешен." },
    { label: "TTFB напрямую определяет INP", misconception: "TTFB не питает INP — INP это задержка взаимодействия основного потока. Рычаг TTFB над FCP и LCP — метриками времени отрисовки." },
    { label: "TTFB — самое лёгкое в исправлении", misconception: "Рычаг не про лёгкость — это про то, сколько нижестоящих метрик зависит от него. FCP и LCP оба стоят поверх TTFB, поэтому исправление каскадирует." },
  ]}
/>

<FadedExample
  client:visible
  id="sr-faded-1"
  pieceSlug="03-five-canonical-breaks"
  lang="ru"
  title="Диагностируй медленную страницу, проходя по цепи"
  steps={{
    solved: (
      <pre><code>{`// Диагностика прохода по цепи, по порядку:
// 1. TTFB высокий?         -> сеть / сервер / сервис-воркер
// 2. LCP поздний, изображение
//    ещё скачивается?      -> обнаружение (сканер предзагрузки) / размер изображения
// 3. LCP поздний, изображение
//    закончилось рано?     -> блокирующий JS (V8 / парсинг)
// 4. INP плохой, длинная задача
//    перекрывает тап?      -> гидратация или тяжёлый обработчик
// 5. CLS, сдвиг после
//    гидратации?           -> несоответствие гидратации (детерминизм рендеринга)
// Симптом и причина часто в РАЗНЫХ слоях.`}</code></pre>
    ),
    semi: {
      prompt: (
        <p>Заполни слой, на который указывает каждый симптом: поздний LCP с изображением, всё ещё скачивающимся, и плохой INP, длинная задача которого перекрывает тап.</p>
      ),
      blanks: [
        { id: "lcp", expected: /обнаружение|предзагрузка|сеть|изображение|discovery|preload/i, placeholder: "слой для позднего-LCP-ещё-скачивается" },
        { id: "inp", expected: /гидратация|обработчик|hydration|handler/i, placeholder: "слой для плохого-INP-длинная-задача" },
      ],
    },
    blank: {
      prompt: (
        <p>Страница «медленная» и команда угадывает. Опиши метод прохода по цепи, который превращает «медленно» в конкретный, исправимый слой — и сформулируй принцип о том, где живут симптомы и причины.</p>
      ),
      reveal: (
        <p>Иди по трассе как по цепи, сверху вниз. TTFB высокий? — сеть, сервер, или пропущенный кеш сервис-воркера. LCP поздний, изображение всё ещё скачивается в момент LCP? — проблема обнаружения (сканер предзагрузки не мог его видеть) или большое изображение. LCP поздний, но изображение закончилось рано? — JavaScript, блокирующий рендеринг, проблема V8/парсинга. INP плохой, длинная задача перекрывает тап? — гидратация или тяжёлый обработчик. Есть сдвиг макета сразу после гидратации? — несоответствие гидратации, проблема детерминизма рендеринга. Каждый вопрос отображает симптом на конкретный слой и конкретное исправление. Принцип: симптом и первопричина рутинно находятся в <em>разных</em> слоях — «медленное изображение» был баг обнаружения, «React медленный» был баг форм V8 — поэтому нужно проходить всю цепь, а не чинить слой, который симптом случайно называет.</p>
      ),
    },
  }}
/>

<Inset kind="edgecase" lang="ru">
Поломка 4 (устаревший кеш сервис-воркера) — это поломка, которая не даёт спать ночами худшим инженерам, потому что она может поймать каждого повторного посетителя за сломанным деплоем. Событие `activate` сервис-воркера — правильное место для очистки старых кешей: проверь версию кеша, удали всё, что не совпадает, и только потом `clients.claim()`. Без этого сломанная кешированная оболочка выживает до тех пор, пока пользователи не очистят данные сайта вручную — чего большинство пользователей никогда не делает.
</Inset>

</Explanation>

<KeyTakeaway>Пять канонических поломок владеют разными слоями: поздний hero (обнаружение), несоответствие гидратации (детерминизм), мегаморфный путь (формы V8), устаревший кеш (состояние), синхронный обработчик (цикл событий). Все пять связаны с зловещей долиной — структурным зазором между «нарисовано» и «интерактивно», который определяет современную фронтенд-производительность.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="03-five-canonical-breaks-retrieval"
  lang="ru"
  questions={[
    { q: "Назови пять канонических поломок, слой и метрику каждой, и почему все пять связаны с зловещей долиной гидратации.", a: "Поломка 1, поздняя загрузка hero-изображения — слои 2 и 5, вредит LCP: hero — CSS background-image или несёт ленивую загрузку, поэтому сканер предзагрузки не может его обнаружить и он скачивается поздно. Поломка 2, несоответствие гидратации — слой 6, вредит CLS и корректности: компонент рендерит значение, которое различается между сервером и клиентом, React перерендеривает поддерево и сдвигает макет. Поломка 3, мегаморфный путь отрисовки — слои 4 и 7, вредит INP: горячая функция на пути взаимодействия видела слишком много форм объектов, V8 не может использовать встроенный кеш, перерисовка медленная. Поломка 4, устаревший кеш сервис-воркера — слои 1 и 8, вредит корректности: воркер отдаёт старый закешированный бандл против нового HTML, несоответствие версий, или ловит пользователей на сломанной оболочке. Поломка 5, синхронная работа в обработчике — слой 7, вредит INP: обработчик делает тяжёлую синхронную работу перед уступкой, превышая бюджет времени обработки. Все пять связаны с зловещей долиной, потому что долина — структурная слабость серверно-рендеренных, гидрируемых приложений, и каждая поломка либо расширяет это окно, либо портит состояние, от которого зависит вся цепь." },
    { q: "Для каждой из пяти поломок — каково исправление?", a: "Поломка 1: сделать LCP-изображение обычным <img> в исходном HTML, жадно загружаемым, fetchpriority='high', в AVIF/WebP. Поломка 2: откладывать только-клиентские значения (localStorage, цены в формате локали, временны́е метки) в useEffect после гидратации, чтобы первый клиентский рендер совпадал с сервером. Поломка 3: стабилизировать формы объектов, чтобы встроенные кеши V8 оставались мономорфными — избегать перемешивания порядков свойств, избегать паттернов delete-затем-добавить, держать горячие объекты одной формы. Поломка 4: хеши содержимого в именах ассетов, версионные метки кешей и очистка старых в событии activate, network-first для навигации, рубильник. Поломка 5: делать минимальную синхронную работу для обновления UI, затем уступать с scheduler.yield(), и переносить тяжёлые чистые вычисления в воркер." },
    { q: "Почему TTFB 2980 мс делает его наиболее высокоэффективным первым исправлением, даже когда LCP и INP тоже плохи?", a: "TTFB — пол под каждой метрикой, зависящей от прибытия HTML. Ничто нижестоящее — FCP, LCP, начало V8-парсинга, начало гидратации — не может начаться до первого байта. При TTFB 2980 мс FCP и LCP уже задержаны почти на 3 секунды прежде, чем любой другой слой что-либо сделал. Исправление TTFB (CDN, кеширование, оболочка сервис-воркера для повторных визитов) вместе двигает FCP и LCP бесплатно. Затем исправления INP и CLS могут атаковать оставшиеся проблемы гидратации и детерминизма. TTFB первый, потому что он причинный — это зависимость, поверх которой стоит каждое другое измерение временно́й шкалы." },
  ]}
/>

<Recap lang="ru">
Зловещая долина гидратации — структурный зазор между «нарисовано» и «интерактивно» — объединяющий режим сбоя серверно-рендеренных, клиентски-гидрируемых приложений. Пять канонических поломок каждая расширяет её или происходит внутри неё. Поломка 1 (поздний hero, CSS background-image или с ленивой загрузкой) атакует LCP, задерживая обнаружение на слое парсинга. Поломка 2 (несоответствие гидратации, только-клиентские значения отрендеренные на сервере) атакует CLS через недетерминированность рендеринга. Поломка 3 (мегаморфный встроенный кеш V8, слишком много форм объектов на горячем пути) атакует INP, замедляя перерисовки. Поломка 4 (устаревший кеш сервис-воркера, переживший деплой) атакует корректность, портя состояние, от которого зависит вся цепь. Поломка 5 (синхронная тяжёлая работа в обработчике события перед уступкой) атакует INP, исчерпывая бюджет времени обработки. В каждом случае симптом называет неправильный слой — диагноз требует прохода по всей цепи и нахождения места, где причина действительно живёт.
</Recap>
