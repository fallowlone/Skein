---
concepts:
- TTFB
- preload-scanner
- CSSOM
- V8-parse-compile
- hydration-long-task
- INP
- LCP
- service-worker-cache
- three-track-timeline
deepensInto:
- browser/08-putting-it-together/03-five-canonical-breaks
- browser/08-putting-it-together/04-three-track-method
estMin: 18
lang: ru
lessonType: topic
level: middle
order: 2
prereqs:
- browser/08-putting-it-together/01-the-full-picture
- browser/01-event-loop/02-queues-and-scheduling
- browser/02-render-pipeline/02-stages-and-threads
- browser/03-v8-internals/02-jit-pipeline
- browser/04-workers/04-service-worker-lifecycle
- browser/05-react-fiber/03-render-and-commit-phases
- browser/06-ssr-vs-ssg/03-hydration-cost
- browser/07-core-web-vitals/01-what-vitals-measure
slug: 02-eight-layers-traced
sources:
- https://web.dev/articles/howbrowserswork
- https://developer.chrome.com/docs/devtools/performance
- https://web.dev/articles/rendering-on-the-web
- https://web.dev/articles/lcp
- https://web.dev/articles/optimize-inp
- https://html.spec.whatwg.org/multipage/parsing.html
spiral:
- latency
- statefulness
- encapsulation
status: ready
summary: Одна страница товара e-commerce, прослеженная через все восемь слоёв — сеть,
  парсинг HTML, CSS, V8, первая отрисовка, гидратация, взаимодействие, вторая навигация
  — с бюджетными числами для каждого.
title: 'Восемь слоёв трассировки: от service worker до второй навигации'
track: browser
unit: 08-putting-it-together
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import TraceScenario from "~/components/pedagogy/TraceScenario.astro";
import MetaphorComplete from "~/components/pedagogy/MetaphorComplete.astro";
import NumberDrill from "~/components/pedagogy/NumberDrill.astro";
import NumbersCard from "~/components/layout/NumbersCard.astro";

<Hook>
Одна страница товара e-commerce: серверно-отрендеренная, React-based, hero-картинка, кнопка «В корзину», зарегистрированный service worker. Средний Android-телефон, 4G. Что именно происходит — в каком порядке, на каком потоке, тратя сколько времени — от момента, когда пользователь тапает ссылку, до обновления значка корзины?
</Hook>

<Crux>Восемь слоёв перекрываются на трёх дорожках — читать медленную страницу значит находить, какая дорожка была узким местом в важный момент.</Crux>

<Explanation>

**Страница, которую мы трассируем.**

Одна конкретная страница: страница товара e-commerce, серверно-отрендеренная, на React, с hero-картинкой, ценой, кнопкой «В корзину», секцией отзывов ниже сгиба и service worker, зарегистрированным с прошлого визита. Пользователь на среднем Android-телефоне на 4G. Мы следуем за этой страницей от момента, когда он тапает ссылку, до момента, когда он успешно добавляет товар в корзину.

**Слой 1 — Сеть.**

Тап триггерит навигацию. DNS резолвит хост, TLS-рукопожатие защищает соединение, HTTP-запрос уходит. Но есть нюанс: service worker с прошлого визита зарегистрирован. Браузер даёт `fetch`-обработчику service worker первый взгляд — он может отдать навигацию из кеша или пропустить в сеть. В любом случае первый байт HTML приходит. Это TTFB, и это пол под всем: ничего ниже по потоку не может стартовать до прихода байтов.

**Слой 2 — Парсинг HTML и preload-сканер.**

HTML-парсер начинает превращать байты в DOM-дерево, сверху вниз. Впереди него мчится preload-сканер, замечая ссылки `<img>`, `<script>` и `<link>` и запуская их загрузки рано. Поэтому hero-картинка, сидящая в начальном HTML как обычный `<img>`, начинает скачиваться немедленно — часы LCP уже тикают, и обнаружение происходит так рано, как возможно. Render-blocking `<script>` в `<head>` поставил бы главный парсер на паузу здесь; `defer`-нутый — нет.

**Слой 3 — CSS и CSSOM.**

Когда ресурсы `<link rel="stylesheet">` приходят, браузер парсит их в CSSOM. CSS render-blocking по дизайну: браузер не нарисует, пока у него нет CSSOM, потому что отрисовка с неполными стилями означала бы вспышку нестилизованного контента. DOM и CSSOM вместе образуют render tree. Если CSS большой или отдаётся медленно, каждая отрисовка ниже по потоку ждёт.

**Слой 4 — JavaScript: парсинг и компиляция V8.**

JavaScript-бандл приходит, и V8 берётся за дело — но не запуская его немедленно. V8 сначала парсит исходник и компилирует его в байткод (вход интерпретатора Ignition). Для большого бандла один этот шаг parse-and-compile — это десятки-сотни миллисекунд времени главного потока на среднем телефоне, до того как выполнилась хоть одна строка. Это скрытая цена внутри «JavaScript слишком большой»: байты — не просто загрузка, это CPU-работа.

**Слой 5 — Первая отрисовка.**

С готовым render tree пайплайн рендера гоняется: стили, компоновка, отрисовка, композитинг. Фото товара, цена и лейаут появляются. Hero-картинка, если она закончила скачиваться, рисуется — и это очень вероятно момент LCP, прибытие самого крупного contentful-элемента. Страница теперь *выглядит готовой*. Но это картинка: ни один обработчик не прицеплен, никакое состояние не живо.

**Слой 6 — Гидратация.**

Теперь React гоняет `hydrateRoot`. Он обходит то же дерево компонентов, что обошёл сервер, и вместо создания DOM-узлов усыновляет существующий серверный DOM, цепляя обработчики событий и подключая состояние и эффекты — реконсилер в режиме гидратации. Это одна длинная задача: V8 исполняет дерево компонентов, реконсилер делает свой render и commit. Пока она гоняется, главный поток заблокирован. Страница выглядела готовой на слое 5; она становится *реально* интерактивной, только когда гидратация доходит до каждого компонента.

**Слой 7 — Взаимодействие.**

Пользователь тапает «В корзину». Если он тапает до того, как гидратация дошла до этой кнопки, ввод задержан — поставлен в очередь за длинной задачей гидратации — и INP записывает большую задержку. Если он тапает после, обработчик гоняется: он обновляет состояние корзины, реконсилер перерендеривает бейдж корзины, пайплайн рендера рисует изменение. Время от тапа до этой отрисовки — один сэмпл INP.

**Слой 8 — Вторая навигация и воркер.**

Пользователь навигирует на checkout. Теперь service worker отрабатывает своё содержание: он отдаёт app shell из кеша, поэтому TTFB второй навигации — десятки миллисекунд вместо сетевого round trip.

<NumbersCard
  id="card-trace-numbers"
  title="Трасса, в числах (средний телефон, 4G)"
  stats={[
    { label: "TTFB — отдан сетью", value: "~300 мс" },
    { label: "TTFB — кеш service worker", value: "~30 мс" },
    { label: "Парсинг + компиляция JS (V8)", value: "~150 мс" },
    { label: "Длинная задача гидратации", value: "300 мс – 1 с+" },
    { label: "Хорошие LCP / INP / CLS", value: "2.5 с / 200 мс / 0.1" },
    { label: "Число канонических поломок", value: "5, по одной на домен" },
  ]}
  data-lesson-visual
/>

**Наложенный таймлайн — три дорожки, не одна последовательность.**

Ключевой инсайт в том, что эти слои — не аккуратная последовательность — они перекрываются на таймлайне через три ресурса:

- Дорожка **сети** занята фетчем HTML, потом CSS, потом JS-бандла, потом картинки, параллельно где протокол позволяет.
- Дорожка **главного потока** занята парсингом HTML, потом компиляцией JS, потом отрисовкой, потом длинной задачей гидратации.
- Дорожка **GPU** делает финальный композитинг.

Performance-трасса DevTools показывает ровно эти три дорожки. Читать медленную страницу значит находить, какая дорожка — бутылочное горлышко в момент, который важен — network-bound до первой отрисовки, main-thread-bound во время гидратации.

**Параллельность: почему слои не последовательны.**

Браузер не делает один шаг, ждёт, делает следующий — он держит все три ресурса занятыми всегда, когда есть работа. Пока сеть скачивает JS-бандл, главный поток уже парсит HTML, который пришёл раньше. Пока главный поток компилирует JS, сеть уже фетчит картинку. Preload-сканер существует ровно ради этой параллельности — он находит ресурсы и кидает их в сеть, пока главный парсер занят чем-то ещё.

Следствие для диагностики: «медленная страница» почти никогда не один медленный шаг. Это момент, где параллельность сорвалась, где одна дорожка стала ограничением, и две другие простаивали, ожидая её. Render-blocking скрипт — классический пример: он останавливает главный парсер, и пока он качается, главный поток простаивает, хотя мог бы парсить остальной HTML.

<Quiz
  id="mid-quiz-1"
  pieceSlug="02-eight-layers-traced"
  lang="ru"
  question="На второй навигации (на checkout) TTFB падает с ~300 мс до ~30 мс. Какой слой ответственен?"
  choices={[
    { label: "Service worker — он отдаёт app shell из кеша, пропуская сетевой round trip", correct: true },
    { label: "DNS-кеш", misconception: "Кеширование DNS экономит lookup, но не весь round trip к серверу. TTFB ~30 мс означает, что ответ пришёл из локального кеша — service worker." },
    { label: "Гидратация закончилась, поэтому страница быстрее", misconception: "Гидратация влияет на интерактивность, не на TTFB новой навигации. TTFB настолько низкий означает, что кеш отдал документ — service worker." },
    { label: "Пайплайн рендера стал быстрее", misconception: "Пайплайн рендера вообще не влияет на TTFB — TTFB это прибытие байта, до любого рендеринга. Причина — кеш service worker." },
  ]}
/>

<Quiz
  id="mid-quiz-2"
  pieceSlug="02-eight-layers-traced"
  lang="ru"
  question="На трассе V8 тратит ~150 мс до того, как выполнилась хоть одна строка бандла. Что он делает?"
  choices={[
    { label: "Парсит исходник JavaScript и компилирует его в байткод — CPU-работа, масштабирующаяся с размером бандла, до начала выполнения", correct: true },
    { label: "Скачивает бандл", misconception: "Скачивание — дорожка сети. ~150 мс это CPU-работа главного потока — V8 парсит и компилирует байты, которые уже получил." },
    { label: "Ждёт сервер", misconception: "Ждать сервер — это TTFB, намного раньше. Эти 150 мс — V8 активно работает на главном потоке, превращая исходник в байткод." },
    { label: "Гоняет гидратацию", misconception: "Гидратация — это выполнение, оно идёт после парсинга и компиляции. 150 мс здесь — именно шаг parse+compile до любого выполнения." },
  ]}
/>

<TraceScenario
  id="mid-trace-1"
  pieceSlug="02-eight-layers-traced"
  lang="ru"
  scenario="DevTools-трасса страницы товара: LCP срабатывает на 1.5 с (хорошо). Но есть длинная задача главного потока на 900 мс, стартующая на 1.6 с, и тап пользователя по «В корзину» на 2.0 с показывает INP 700 мс. Что за длинная задача, и почему INP плох, несмотря на хороший LCP?"
  steps={[
    { prompt: "Длинная задача на 900 мс — гидратация: V8 исполняет дерево компонентов, реконсилер усыновляет серверный DOM; тап на 2.0 с приземлился внутри неё, поэтому INP записал ожидание", reveal: "Верно. LCP измеряет, когда самый крупный контент нарисовался — 1.5 с, хорошо, потому что серверный HTML нарисовался быстро. Но страница ещё не была интерактивной: длинная задача на 900 мс, стартующая на 1.6 с — гидратация. Тап на 2.0 с пришёл через 400 мс в эту задачу, и ввод встал в очередь за оставшимися ~500 мс гидратации — это ожидание в очереди — основная часть 700 мс INP. Хороший LCP и плохой INP вместе — сигнатура зловещей долины гидратации: нарисовано быстро, интерактивно медленно. Фикс — сжать гидратацию: код-сплит, Server Components, меньше гидрируемых островов." },
    { prompt: "Длинная задача — сеть фетчит картинки", reveal: "Сетевые фетчи не длинные задачи главного потока — они показываются на дорожке сети, пока главный поток простаивает или делает другую работу. Задача главного потока на 900 мс настолько рано — это гидратация (или тяжёлое исполнение скрипта), не загрузка." },
    { prompt: "INP плох, потому что LCP был плох", reveal: "LCP был 1.5 с — хорошо. INP и LCP — независимые метрики, измеряющие разное (отрисовка vs интерактивность). Хороший LCP не защищает INP; здесь гидратация рушит INP, пока LCP в порядке." },
  ]}
/>

<Quiz
  id="mid-quiz-3"
  pieceSlug="02-eight-layers-traced"
  lang="ru"
  question="Почему чтение DevTools-трассы описывают как поиск бутылочного горлышка-«дорожки», а не горлышка-«слоя»?"
  choices={[
    { label: "Слои перекрываются на таймлайне через дорожки сети, главного потока и GPU — горлышко это та дорожка, что занята в важный момент", correct: true },
    { label: "Слои идут в строгой последовательности без перекрытия", misconception: "Они сильно перекрываются — сеть фетчит ресурсы, пока главный поток парсит и компилирует. Трасса показывает параллельные дорожки, не одну последовательность." },
    { label: "DevTools показывает только одну дорожку за раз", misconception: "DevTools показывает сеть, главный поток, GPU и больше одновременно. Чтение трассы — про сравнение дорожек ради поиска ограничения." },
    { label: "Дорожки и слои — несвязанные понятия", misconception: "Они связаны: работа каждого слоя гоняется на одной из дорожек. Суть в том, что работа перекрывается, поэтому горлышко локализуют по дорожке и времени." },
  ]}
/>

<DragOrder
  id="mid-drag-1"
  pieceSlug="02-eight-layers-traced"
  lang="ru"
  prompt="Расставьте восемь слоёв жизни страницы, от тапа пользователя до второй навигации."
  items={[
    { id: "network", label: "Сеть — DNS, TLS, HTTP; service worker получает первый взгляд" },
    { id: "htmlparse", label: "Парсинг HTML — DOM-дерево, preload-сканер обнаруживает ресурсы" },
    { id: "css", label: "CSS — построен CSSOM, сформирован render tree" },
    { id: "v8", label: "V8 — JS-бандл распарсен и скомпилирован в байткод" },
    { id: "paint", label: "Первая отрисовка — компоновка, отрисовка, композитинг; LCP срабатывает" },
    { id: "hydrate", label: "Гидратация — реконсилер усыновляет серверный DOM, цепляет обработчики" },
    { id: "interact", label: "Взаимодействие — тап обработан, re-render, INP измерен" },
    { id: "secondnav", label: "Вторая навигация — service worker отдаёт app shell" },
  ]}
  correctOrder={["network", "htmlparse", "css", "v8", "paint", "hydrate", "interact", "secondnav"]}
/>

<MetaphorComplete
  id="mid-metaphor-1"
  pieceSlug="02-eight-layers-traced"
  lang="ru"
  setup="Хороший LCP и плохой INP вместе — страница рисуется быстро, но по ней нельзя тапнуть — имеет имя. Оно описывает окно, где страница — убедительная картинка, не отвечающая. Как называется это окно?"
  accepted={["uncanny valley", "зловещая долина", "зловещая долина гидратации", "долина гидратации"]}
  canonical="зловещая долина"
  explanation="Зловещая долина гидратации — окно между первой отрисовкой и полной интерактивностью: страница выглядит полностью готовой — текст, картинки, кнопки все видны — но JavaScript ещё не закончил гидрироваться, поэтому тапы отбрасываются или задерживаются. Она «зловещая», потому что страница выглядит живой, но не живая. Хороший LCP с плохим INP — точная измеримая сигнатура страницы, застрявшей в этой долине."
/>

<NumberDrill
  id="mid-drill-1"
  pieceSlug="02-eight-layers-traced"
  lang="ru"
  question="На трассе parse-and-compile JS-бандла в V8 занимает примерно сколько миллисекунд для типового бандла на среднем телефоне — цена до выполнения любой строки?"
  expected={150}
  tolerance={50}
  unit="мс"
  explanation="Примерно 150 мс (масштабируется с размером бандла и куда хуже на слабых устройствах). Это чистый V8 parse-and-compile — превращение байтов исходника в байткод — до того как выполнится хоть одна строка. Это скрытая CPU-цена внутри «бандл слишком большой»: байты — не только загрузка, это работа главного потока, задерживающая всё после них, включая гидратацию."
/>

<Inset kind="why" lang="ru">
Восемь слоёв — не произвольный список; это форма, которую веб приобрёл за тридцать лет эволюции. Ранние страницы были слои 1–3 и всё: сеть, парсинг, отрисовка — статический документ. JavaScript добавил исполнение, и слой 4 (V8) стал важным. AJAX и SPA сдвинули рендеринг в браузер, и первая отрисовка перестала нести полезный контент. SSR вернул серверный рендер ради быстрой первой отрисовки — но он же ввёл слой 6, гидратацию, и вместе с ней зловещую долину. Service worker (слой 8) добавился, чтобы вернуть мгновенность повторных загрузок. Каждый слой — это решение прошлого компромисса, ставшее новым компромиссом.
</Inset>

</Explanation>

<KeyTakeaway>Восемь слоёв работают на трёх перекрывающихся дорожках (сеть, главный поток, GPU). TTFB — это пол; V8 parse+compile добавляет ~150 мс до выполнения; гидратация — одна длинная задача главного потока, блокирующая ранние тапы. Зловещая долина — хороший LCP, плохой ранний INP — структурная сигнатура серверно-отрендеренных приложений.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="02-eight-layers-traced-retrieval"
  lang="ru"
  questions={[
    { q: "Проследите страницу товара e-commerce от тапа пользователя до работающей кнопки «В корзину», называя каждый из восьми слоёв.", a: "Слой 1, сеть: DNS, TLS, HTTP — service worker получает первый взгляд (кеш или сквозной), приходит первый байт как TTFB. Слой 2, парсинг HTML: строится DOM; preload-сканер обнаруживает hero img и стартует его загрузку. Слой 3, CSS: стили парсятся в CSSOM; DOM + CSSOM = render tree; CSS render-blocking. Слой 4, V8: JS-бандл распарсен и скомпилирован в байткод (~150 мс до выполнения). Слой 5, первая отрисовка: компоновка, отрисовка, композитинг; hero рисуется и LCP срабатывает — страница выглядит готовой. Слой 6, гидратация: hydrateRoot гоняет реконсилер в режиме усыновления, цепляет обработчики — одна длинная задача главного потока. Слой 7, взаимодействие: тап; если гидратация дошла до кнопки, обработчик гоняется и корзина перерендеривается (сэмпл INP); если нет, тап встаёт в очередь (плохой INP). Слой 8, вторая навигация: service worker отдаёт app shell из кеша (~30 мс TTFB)." },
    { q: "Какова трёхдорожечная структура DevTools-трассы, и почему «найти горлышко-дорожку» важнее «найти медленный слой»?", a: "Три дорожки: сеть (фетч HTML, CSS, JS, картинок), главный поток (парсинг, компиляция, отрисовка, гидратация, обработка), GPU/композитор. Слои перекрываются через эти дорожки параллельно. Нахождение горлышка-дорожки в момент, который важен, — не просто какого слоя, — это то, что делает диагноз починяемым. Если LCP поздний и hero всё ещё на дорожке сети в момент LCP: проблема обнаружения или сети. Если LCP поздний, но картинка закончилась, а главный поток плотно занят: render-blocking JS. Дорожка-ограничение указывает на реальный фикс." },
    { q: "Почему V8 тратит ~150 мс до того, как выполнится хоть одна строка бандла, и что эта цена затрагивает?", a: "V8 должен распарсить исходник JavaScript в AST и скомпилировать его в байткод для интерпретатора Ignition до того, как начнётся выполнение. Для типового бандла на среднем телефоне это ~150 мс работы главного потока. Это задерживает всё, что идёт после: первое выполнение, которое ведёт к гидратации, которая определяет, когда страница становится интерактивной (INP). Это скрытая CPU-цена внутри «бандл слишком большой» — байты это не только загрузка, они также задача parse-and-compile." },
  ]}
/>

<Recap lang="ru">
Серверно-отрендеренная React-страница товара проходит восемь слоёв: сеть (TTFB ~300 мс, или ~30 мс из кеша service worker), парсинг HTML с preload-сканером, запускающим hero рано, CSS строит CSSOM (render-blocking), V8 parse-and-compile (~150 мс работы главного потока до выполнения), первая отрисовка и LCP, длинная задача гидратации (300 мс до 1 с+), блокирующая ранние тапы, взаимодействие (INP измерен тап-до-отрисовки) и вторая навигация из кеша. Эти восемь слоёв работают на трёх перекрывающихся дорожках — сеть, главный поток и GPU — не в одной последовательности. Параллельность — норма; горлышко — момент, когда одна дорожка застопорилась и другие ждут. Зловещая долина гидратации — хороший LCP, плохой ранний INP — структурный отказ серверно-отрендеренных приложений: страница выглядит готовой до того, как работает, и каждый тап внутри длинной задачи гидратации — задержанное взаимодействие, которое чувствует пользователь.
</Recap>
