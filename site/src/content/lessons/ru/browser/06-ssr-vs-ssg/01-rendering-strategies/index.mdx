---
concepts:
- SSG
- SSR
- ISR
- streaming-SSR
- hydration
- time-to-interactive
deepensInto:
- 02-strategy-mechanisms
- 03-hydration-cost
estMin: 12
lang: ru
lessonType: topic
level: junior
order: 1
prereqs:
- browser/02-render-pipeline/01-the-six-stages
slug: 01-rendering-strategies
sources:
- https://web.dev/articles/rendering-on-the-web
- https://react.dev/reference/react-dom/client/hydrateRoot
- https://nextjs.org/docs/app/building-your-application/data-fetching/incremental-static-regeneration
spiral:
- statefulness
- latency
status: ready
summary: Когда производится HTML — на билде, на запрос, по расписанию или кусками
  — и почему гидратация это отдельный шаг, оживляющий этот HTML.
title: 'Стратегии рендеринга: SSG, SSR, ISR, streaming и гидратация'
track: browser
unit: 06-ssr-vs-ssg
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";
import MetaphorComplete from "~/components/pedagogy/MetaphorComplete.astro";
import NumberDrill from "~/components/pedagogy/NumberDrill.astro";
import NumbersCard from "~/components/layout/NumbersCard.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
Пользователь тапает страницу товара. Сервер присылает готовый HTML — текст, цена, фото приходят меньше чем за секунду, страница выглядит готовой. Потом ничего не происходит при тапе «В корзину» ещё две секунды. Страница нарисована, но ещё не живая.
</Hook>

<Crux>SSR, SSG, ISR, streaming — когда рендерить HTML. Гидратация — цена сделать этот HTML интерактивным.</Crux>

<Explanation>

Стратегии рендеринга расходятся по таймингу. **SSG** (Static Site Generation) рендерит на билде — HTML это файл на CDN, мгновенно отдаваемый каждому посетителю. **SSR** (Server-Side Rendering) рендерит на каждый запрос — свежий HTML на каждый хит, но сервер работает на каждый запрос. **ISR** (Incremental Static Regeneration) — SSG, обновляющий себя по расписанию или по требованию. **Streaming SSR** шлёт HTML кусками по мере готовности каждой части, не дожидаясь всей страницы.

Пересекая все из них: **гидратация** — клиентский шаг, где React (или любой фреймворк) переисполняет дерево компонентов, чтобы прицепить обработчики событий и состояние к серверному HTML. Пока гидратация не закончится, страница выглядит готовой, но не реагирует на клики.

<FlowDiagram
  label="Четыре стратегии рендеринга различаются тем, когда производится HTML — SSG на билде, ISR на билде с ревалидацией на запрос, SSR на запрос, streaming на запрос кусками — и все четыре ведут в один и тот же клиентский шаг гидратации, делающий HTML интерактивным."
  perRow={2}
  hue="var(--d-frontend)"
  nodes={[
    { id: "ssg", label: "SSG", sub: "@ билд" },
    { id: "isr", label: "ISR", sub: "билд + ревалидация" },
    { id: "ssr", label: "SSR", sub: "на запрос" },
    { id: "stream", label: "Streaming", sub: "на запрос, кусками" },
    { id: "hydrate", label: "Гидратация", sub: "клиент, оживляет" },
  ]}
  edges={[
    { from: "ssg", to: "hydrate" },
    { from: "isr", to: "hydrate" },
    { from: "ssr", to: "hydrate" },
    { from: "stream", to: "hydrate" },
  ]}
  caption="Ось «когда»: SSG рендерит один раз на билде; ISR — HTML с билда, ревалидируемый на запрос; SSR и streaming рендерят на запрос (streaming — кусками). Чем бы ни был произведён HTML, общий клиентский шаг гидратации — то, что наконец делает его интерактивным."
/>

<div data-lesson-visual class="my-8 rounded-xl border border-edge bg-surface-2 p-6">
  <p class="text-sm font-semibold text-ink mb-4">Четыре стратегии и один общий шаг</p>
  <div class="grid grid-cols-1 gap-3 text-sm">
    <div class="flex items-start gap-3 rounded-lg bg-surface-3 px-4 py-3">
      <span class="font-mono font-bold text-accent-blue w-10 shrink-0">SSG</span>
      <span>HTML рендерится <strong>один раз на билде</strong> → файл на CDN → мгновенный TTFB для каждого. Заморожен на момент билда.</span>
    </div>
    <div class="flex items-start gap-3 rounded-lg bg-surface-3 px-4 py-3">
      <span class="font-mono font-bold text-accent-blue w-10 shrink-0">SSR</span>
      <span>HTML рендерится <strong>на каждый запрос</strong> → свежие, пользовательские данные. TTFB включает время серверного рендера + загрузку данных.</span>
    </div>
    <div class="flex items-start gap-3 rounded-lg bg-surface-3 px-4 py-3">
      <span class="font-mono font-bold text-accent-blue w-10 shrink-0">ISR</span>
      <span>SSG + <strong>правило ревалидации</strong> → отдать протухшее, пока регенерация идёт в фоне. stale-while-revalidate для страниц.</span>
    </div>
    <div class="flex items-start gap-3 rounded-lg bg-surface-3 px-4 py-3">
      <span class="font-mono font-bold text-accent-blue w-12 shrink-0">Stream</span>
      <span>Сервер шлёт <strong>оболочку немедленно</strong> → медленные части стримятся по мере резолва границ Suspense.</span>
    </div>
    <div class="flex items-start gap-3 rounded-lg bg-amber-500/10 border border-amber-500/30 px-4 py-3">
      <span class="font-mono font-bold text-amber-600 dark:text-amber-400 w-10 shrink-0">Гидр.</span>
      <span>Клиент скачивает JS, переисполняет компоненты, <strong>цепляет обработчики</strong>. Происходит после прихода HTML независимо от стратегии.</span>
    </div>
  </div>
</div>

**Метафора ресторана.** SSG — блюдо, приготовленное заранее и держащееся готовым — клиента обслуживают мгновенно, но еду сделали часы назад. SSR — готовка на заказ — свежо и точно правильно, но клиент ждёт. ISR — шведский стол, который повар обновляет время от времени — большей частью мгновенно, иногда лоток меняют. Streaming — вынос каждого блюда в момент готовности. И гидратация — часть после того, как тарелка приземлилась: еда выглядит идеально, но клиент всё ещё не может есть, пока не принесут приборы.

**Почему зазор важен.** Серверно-отрендеренная страница выглядит готовой почти мгновенно — текст и картинки прямо в HTML. Но если кликнуть по кнопке до окончания гидратации, ничего не происходит: картинка на экране, приложение ещё не работает. Зазор между «выглядит готовым» и «реально работает» — самая недопонятая вещь в современной производительности веба.

<DragOrder
  id="01-drag-1"
  lessonSlug="01-rendering-strategies"
  lang="ru"
  prompt="Серверно-отрендеренная страница идёт от запроса к работающей кнопке. Расставьте шаги по порядку."
  items={[
    { id: "request", label: "Браузер запрашивает страницу" },
    { id: "html", label: "Сервер рендерит компоненты в HTML и шлёт его" },
    { id: "paint", label: "Браузер рисует HTML — страница выглядит готовой" },
    { id: "js", label: "Браузер скачивает и запускает JavaScript-бандл" },
    { id: "hydrate", label: "Гидратация: фреймворк цепляет обработчики — кнопки работают" },
  ]}
  correctOrder={["request", "html", "paint", "js", "hydrate"]}
/>

<Quiz
  id="01-quiz-1"
  lessonSlug="01-rendering-strategies"
  lang="ru"
  question="Серверно-отрендеренная страница выглядит полностью загруженной, но кнопка ничего не делает при клике. Самая вероятная причина?"
  choices={[
    { label: "Гидратация не закончилась — HTML нарисован, но JavaScript, подключающий кнопку, ещё не выполнился", correct: true },
    { label: "Кнопка сломана в серверном коде", misconception: "Кнопка работает, когда гидратация завершается. Проблема — тайминг: JS, активирующий её, ещё не выполнился, не баг кода." },
    { label: "Сервер забыл прислать кнопку", misconception: "Кнопка видна, значит HTML для неё пришёл. Не хватает клиентского JavaScript, делающего её интерактивной." },
    { label: "CSS не загрузился", misconception: "Отсутствующий CSS сделал бы страницу криво выглядящей, не сделал бы видимую кнопку неотзывчивой." },
  ]}
/>

<Quiz
  id="01-quiz-2"
  lessonSlug="01-rendering-strategies"
  lang="ru"
  question="Какая стратегия рендерит HTML один раз, на билде, чтобы каждый посетитель получал готовый файл?"
  choices={[
    { label: "SSG — Static Site Generation", correct: true },
    { label: "SSR — Server-Side Rendering", misconception: "SSR рендерит свежо на каждый запрос, не один раз на билде. SSG — та, что на билде." },
    { label: "Гидратация", misconception: "Гидратация — не стратегия рендеринга, а клиентский шаг, делающий уже отрендеренный HTML интерактивным." },
    { label: "Streaming", misconception: "Streaming про отправку HTML кусками — как HTML доставляется, не когда он отрендерен." },
  ]}
/>

<MetaphorComplete
  id="01-metaphor-1"
  lessonSlug="01-rendering-strategies"
  lang="ru"
  setup="В метафоре ресторана тарелка еды приходит, выглядя идеально, но клиент всё ещё не может есть, пока не доставят приборы. Еда — это серверный HTML. Что такое приборы — шаг, делающий страницу реально пригодной?"
  accepted={["hydration", "hydrate", "гидратация", "гидрация", "клиентская гидратация"]}
  canonical="гидратация"
  explanation="Гидратация — клиентский шаг, где фреймворк скачивает свой JavaScript, переисполняет компоненты и цепляет обработчики событий и состояние к серверно-отрендеренному HTML. До гидратации страница — картинка, видимая, но инертная. После гидратации — живое приложение."
/>

<NumberDrill
  id="01-drill-1"
  lessonSlug="01-rendering-strategies"
  lang="ru"
  question="Грубое правило: гидратация дерева компонентов стоит примерно во сколько раз больше работы, чем начальный серверный рендер того же дерева?"
  expected={2}
  tolerance={0}
  unit="раза"
  explanation="Гидратация примерно удваивает работу рендера: сервер отрендерил дерево один раз, чтобы произвести HTML, а клиент переисполняет то же дерево снова, чтобы прицепить интерактивность. Поэтому тяжело серверно-отрендеренная страница быстро появляется, но медленно отвечает — второй рендер гоняется на устройстве пользователя."
/>

<NumbersCard
  id="01-numbers"
  title="Числа стратегий рендеринга"
  stats={[
    { label: "SSG time-to-first-byte", value: "Десятки мс (хит CDN)" },
    { label: "SSR TTFB", value: "Серверный рендер + загрузка данных" },
    { label: "Сброс оболочки streaming", value: "~50 мс" },
    { label: "Стоимость гидратации", value: "≈ 2× серверного рендера" },
    { label: "Хороший INP (p75)", value: "≤200 мс" },
  ]}
/>

</Explanation>

<KeyTakeaway>SSG рендерит на билде (заморожен, мгновенный), SSR на запрос (свежо, серверная цена), ISR обновляет статику по расписанию, streaming сбрасывает оболочку первой. Гидратация — ~2× серверного рендера — общий последний шаг, подключающий кнопки.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="01-retrieval"
  lang="ru"
  questions={[
    { q: "Что означает SSG и когда рендерится HTML?", a: "Static Site Generation — HTML рендерится один раз на билде и отдаётся как статический файл с CDN. Контент заморожен на момент билда." },
    { q: "Что такое гидратация и почему она происходит после того, как страница уже нарисована?", a: "Гидратация — клиентский процесс, где фреймворк переисполняет дерево компонентов, чтобы прицепить обработчики событий и состояние к серверному HTML. HTML приходит первым (быстрая отрисовка), но JS, подключающий интерактивность, выполняется после — во время гидратации." },
    { q: "Что такое ISR и чем он отличается от чистого SSG?", a: "Incremental Static Regeneration — SSG с правилом ревалидации. Страницы отдаются статически из кеша, но после настроенного времени сервер регенерирует свежую версию в фоне — stale-while-revalidate для целых страниц." },
  ]}
/>

<Recap lang="ru">
Четыре стратегии отвечают на вопрос, когда производится HTML: SSG на билде (мгновенный хит CDN, замороженный контент), SSR на запрос (свежо, серверная вычислительная цена на каждый хит), ISR по расписанию ревалидации (ограниченная протухлость, большей частью статическая доставка), и streaming SSR кусками (оболочка появляется за ~50 мс, медленные виджеты стримятся позже). Гидратация — отдельный, ~2×-ный шаг, гоняющийся независимо от стратегии: фреймворк переисполняет дерево компонентов на клиенте, чтобы прицепить обработчики событий к серверному HTML. Пока гидратация не завершилась для данного элемента, он видим, но мёртв. Зазор между первой отрисовкой и интерактивностью — это место, где живут баги «кликнул, ничего не произошло».
</Recap>
