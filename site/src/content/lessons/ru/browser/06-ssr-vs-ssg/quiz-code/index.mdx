---
concepts:
- hydration-mismatch-source
- ssr-data-fetching
- isr-revalidate
- suspense-boundary
estMin: 14
lang: ru
level: senior
order: 9
prereqs: []
slug: quiz-code
sources:
- https://react.dev/reference/react-dom/client/hydrateRoot
- https://nextjs.org/docs/app/building-your-application/data-fetching/incremental-static-regeneration
- https://react.dev/reference/react-dom/server/renderToPipeableStream
status: ready
summary: "Читайте реальные сниппеты React/Next — источник hydration mismatch, гонку SSR-фетча, конфиг ISR revalidate и расстановку Suspense — и выбирайте исправление с наибольшим рычагом."
title: 'Стратегии рендеринга: чтение кода и конфигов'
track: browser
unit: 06-ssr-vs-ssg
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Баги стратегий рендеринга читаются в коде и конфигах, а не в документации. Прочитайте каждый сниппет, предскажите его поведение в рантайме и выберите исправление, которое senior-инженер сделает первым.
</Hook>

<Goal lang="ru">
Отработайте цикл, который проходит каждый инцидент со стратегией рендеринга: прочитать компонент или конфиг, предсказать поведение hydration, свежести или streaming и взяться за структурное исправление — а не за заплатку поверх симптома.
</Goal>

## Сниппет 1 — мигающее приветствие

```jsx
function Greeting() {
  // читается во время рендера, на сервере И на клиенте
  const hour = new Date().getHours();
  const msg = hour < 12 ? 'Good morning' : 'Good evening';
  return <h1>{msg}</h1>;
}
```

<Quiz
  id="06-ssr-vs-ssg-code-1"
  lessonSlug="06-ssr-vs-ssg"
  lang="ru"
  question="Этот компонент выбрасывает 'Text content did not match' при hydration и поднимает CLS. Где баг и каково верное исправление?"
  choices={[
    { label: "Он читает new Date() во время рендера, поэтому сервер (один момент, серверная таймзона) и клиент (секундой позже, таймзона пользователя) могут вычислить разные сообщения — hydration mismatch из-за недетерминизма. Исправление: сначала отрендерить стабильный плейсхолдер, затем выставить реальное сообщение в useEffect после hydration.", correct: true },
    { label: "Тег h1 неправильный; используйте div, чтобы избежать mismatch.", misconception: "Тип элемента нерелевантен — диф в текстовом контенте, вызванном чтением живого значения во время рендера. Смена тега ничего не даёт." },
    { label: "getHours() устарел; используйте getUTCHours().", misconception: "getHours() в порядке; UTC лишь замаскировал бы половину проблемы с таймзоной. Разница в моменте рендера остаётся, поэтому mismatch сохраняется. Лечение — вообще не читать время во время рендера." },
    { label: "Обернуть компонент в Suspense, чтобы React дождался часов.", misconception: "Suspense стримит асинхронные данные; он не делает синхронное чтение Date детерминированным. Два рендера всё равно расходятся. Вместо этого отложите значение в эффект." },
  ]}
/>

## Сниппет 2 — SSR-фетч данных

```jsx
// Client Component, рендерится на сервере, затем гидрируется
function Profile({ userId }) {
  const [user, setUser] = useState(null);
  useEffect(() => {
    fetch(`/api/users/${userId}`).then(r => r.json()).then(setUser);
  }, [userId]);
  return <span>{user?.name ?? 'Loading…'}</span>;
}
```

<Quiz
  id="06-ssr-vs-ssg-code-2"
  lessonSlug="06-ssr-vs-ssg"
  lang="ru"
  question="Страница рендерится на сервере, но этот компонент всегда печатает 'Loading…' в начальном HTML, а реальное имя появляется только после hydration. Почему и какой подход даёт больший рычаг?"
  choices={[
    { label: "useEffect никогда не выполняется на сервере, поэтому SSR-проход рендерит null-состояние ('Loading…'), а данные загружаются только после hydration. Перенесите фетч на сервер — загрузите в Server Component (или getServerSideProps) и передайте данные пропом, либо встройте их через dehydrated query cache, чтобы первый рендер уже содержал имя.", correct: true },
    { label: "URL фетча неправильный, поэтому данные не приходят.", misconception: "URL в порядке — данные приходят, просто после hydration. Проблема в том, что useEffect не может выполниться во время SSR, поэтому серверный HTML никогда не включает загруженное имя." },
    { label: "useState(null) надо заменить на useState('Loading…').", misconception: "Это лишь меняет текст плейсхолдера; сервер всё равно не может фетчить в эффекте. Реальное исправление — фетч на сервере, чтобы имя было в начальном HTML." },
    { label: "Добавить 'use server', чтобы эффект выполнялся на сервере.", misconception: "'use server' помечает Server Actions, а не способ запускать useEffect на сервере — эффекты никогда не выполняются во время SSR. Фетчите в Server Component или загрузчике и передавайте результат вниз." },
  ]}
/>

## Сниппет 3 — конфиг ISR

```jsx
// Сегмент маршрута Next.js App Router
export const revalidate = 3600; // секунды

async function ProductPage({ params }) {
  const product = await getProduct(params.id); // кэшируется, ревалидируется раз в час
  return <ProductView product={product} />;
}
```

<Quiz
  id="06-ssr-vs-ssg-code-3"
  lessonSlug="06-ssr-vs-ssg"
  lang="ru"
  question="Цены меняются непредсказуемо, иногда с интервалом в минуты, но `revalidate = 3600` означает, что цена может быть устаревшей до часа. Как сохранить выигрыш ISR в статической доставке, ограничив устаревание секундами?"
  choices={[
    { label: "Оставить временной revalidate как страховку, но добавить on-demand ревалидацию: пусть вебхук прайс-фида вызывает revalidatePath/revalidateTag при изменении цены, чтобы закэшированная страница инвалидировалась в течение секунд после реального обновления.", correct: true },
    { label: "Снизить revalidate до 1, чтобы страница регенерировала каждую секунду.", misconception: "revalidate = 1 молотит регенерацию в каждом окне запроса и всё равно отдаёт устаревшие данные между непредсказуемыми изменениями цены. On-demand ревалидация обновляет ровно тогда, когда цена реально меняется." },
    { label: "Перейти на полный SSR per request.", misconception: "SSR платит серверной вычислительной ценой на каждый запрос для контента, одинакового у всех, теряя выигрыш CDN-кэша ISR. On-demand ревалидация сохраняет статическую доставку и ограничивает устаревание секундами." },
    { label: "Поставить revalidate = 0, чтобы отключить кэширование.", misconception: "revalidate = 0 полностью выводит маршрут из кэширования — фактически SSR — отбрасывая ту самую выгоду статической доставки, которую вы пытаетесь сохранить." },
  ]}
/>

## Сниппет 4 — расстановка Suspense

```jsx
async function Dashboard() {
  return (
    <Shell>
      <Stats />                       {/* быстро: ~10 мс */}
      <Suspense fallback={<Spinner/>}>
        <OrderHistory />              {/* медленно: запрос ~400 мс */}
      </Suspense>
    </Shell>
  );
}
```

<Quiz
  id="06-ssr-vs-ssg-code-4"
  lessonSlug="06-ssr-vs-ssg"
  lang="ru"
  question="При streaming SSR (renderToPipeableStream) чего достигает такая расстановка Suspense и что было бы, если убрать границу?"
  choices={[
    { label: "Сервер сбрасывает оболочку — Shell и Stats — за ~50 мс, пока запрос OrderHistory на 400 мс ещё в процессе, затем стримит чанк истории заказов, когда он разрешится; React гидрирует каждую границу по мере прихода. Уберите границу — и весь документ ждёт 400 мс до отправки первого байта.", correct: true },
    { label: "Ничего — Suspense влияет только на клиентский lazy loading, не на SSR.", misconception: "С renderToPipeableStream Suspense-границы — единица streaming на сервере: оболочка сбрасывается сразу, и каждая граница стримится по мере разрешения её данных." },
    { label: "Он заставляет OrderHistory рендериться раньше Stats.", misconception: "Расстановка границы не переупорядочивает рендеринг; Stats быстрый и сбрасывается с оболочкой. Граница позволяет медленной части стримиться позже, не блокируя быструю." },
    { label: "Он заставляет весь дашборд ждать OrderHistory.", misconception: "Это происходит БЕЗ границы. Граница делает обратное — изолирует медленный запрос, чтобы остальное стримилось сразу." },
  ]}
/>

<Recap lang="ru">
Четыре паттерна чтения покрывают большинство инцидентов со стратегиями рендеринга: живое значение (new Date(), Math.random(), window), прочитанное во время рендера, — это hydration mismatch, отложите его в useEffect; useEffect никогда не выполняется во время SSR, поэтому данные надо фетчить на сервере и передавать вниз (или встраивать в dehydrated cache), чтобы они появились в начальном HTML; временной ISR ограничивает устаревание слабо, но on-demand ревалидация через вебхук сужает его до секунд, сохраняя доставку с CDN; а Suspense-граница вокруг медленного запроса — это то, что позволяет streaming SSR сбросить оболочку первой вместо блокировки на самой медленной части. Диагностируйте по коду и конфигу, исправляйте структуру, затем проверяйте TTFB, INP и число mismatch.
</Recap>
