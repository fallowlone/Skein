---
concepts:
- hydration-mismatch
- CLS
- determinism
- useEffect
- dehydrated-query-cache
- renderToPipeableStream
- Suspense-boundary
deepensInto:
- 05-rsc-and-observability
estMin: 18
lang: ru
lessonType: topic
level: senior
order: 4
prereqs:
- 01-rendering-strategies
- 03-hydration-cost
- browser/05-react-fiber/03-render-and-commit-phases
slug: 04-hydration-mismatch
sources:
- https://react.dev/reference/react-dom/client/hydrateRoot
- https://web.dev/articles/rendering-on-the-web
- https://react.dev/reference/react-dom/server/renderToPipeableStream
- https://web.dev/articles/inp
spiral:
- statefulness
status: ready
summary: Как работает контракт гидратации, что его нарушает, как React восстанавливается
  и какое правило детерминизма предотвращает CLS и повторный рендер в продакшне.
title: 'Hydration mismatch: причины, обнаружение и правило детерминизма'
track: browser
unit: 06-ssr-vs-ssg
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import DebugLog from "~/components/pedagogy/DebugLog.astro";
import FadedExample from "~/components/pedagogy/FadedExample.tsx";
import TraceScenario from "~/components/pedagogy/TraceScenario.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
Компонент приветствия отображает "Доброе утро" или "Добрый вечер" в зависимости от часа. В режиме разработки всё работает. В продакшне пользователи видят мигание: текст меняется в момент загрузки страницы, и layout смещается на 0.14. В консоли браузера: "Text content did not match. Server: 'Добрый вечер' Client: 'Доброе утро'." Причина — нарушенный контракт под названием hydration mismatch.
</Hook>

<Crux>Гидратация — контракт: DOM сервера должен совпадать с DOM клиента при первом рендере. Недетерминизм нарушает его — CLS и recovery re-render.</Crux>

<Explanation>

**Механизм, точно.** Гидратация — это контракт: DOM, который клиент создаёт при первом рендере, должен быть структурно идентичен DOM, сериализованному сервером в HTML. React обходит серверный DOM и виртуальное дерево клиента синхронно, усваивая каждый узел. Когда узел не совпадает — разный текст, разные атрибуты, другой элемент, лишний или отсутствующий дочерний элемент — контракт нарушен. Восстановление React: логирует ошибку несоответствия и для затронутого поддерева выбрасывает серверный HTML и рендерит его на клиенте с нуля. Этот recovery render — видимый симптом: мигание изменённого контента и layout shift, который спайкует CLS (Cumulative Layout Shift, накопленный сдвиг раскладки).

<FlowDiagram
  label="React сравнивает сериализованный сервером DOM с первым рендером клиента узел за узлом: при совпадении он усваивает существующие узлы, но при расхождении логирует mismatch, выбрасывает серверный HTML для этого поддерева и перерендерит его на клиенте, спайкуя CLS."
  hue="var(--d-frontend)"
  nodes={[
    { id: "server", label: "Серверный DOM", sub: "сериализ. HTML", col: 0, row: 0 },
    { id: "client", label: "Рендер клиента", sub: "первый проход", col: 0, row: 1 },
    { id: "compare", label: "Сравнение", sub: "узел за узлом", col: 1, row: 0 },
    { id: "adopt", label: "Усвоить узлы", sub: "контракт цел", col: 2, row: 0 },
    { id: "mismatch", label: "Несоответствие", sub: "контракт нарушен", col: 2, row: 1 },
    { id: "discard", label: "Выбросить HTML", sub: "поддерево", col: 3, row: 1 },
    { id: "rerender", label: "Ре-рендер клиента", sub: "мигание + CLS", col: 4, row: 1 },
  ]}
  edges={[
    { from: "server", to: "compare" },
    { from: "client", to: "compare" },
    { from: "compare", to: "adopt", label: "равны" },
    { from: "compare", to: "mismatch", label: "разошлись" },
    { from: "mismatch", to: "discard" },
    { from: "discard", to: "rerender" },
  ]}
  caption="Контракт гидратации в одной картинке. Равные деревья: React дёшево усваивает серверные узлы. Разошедшиеся деревья: React выбрасывает серверный HTML для этого поддерева и рендерит его с нуля на клиенте — видимое мигание и спайк CLS."
/>

Распространённые причины — все виды недетерминизма между двумя средами: `Date.now()`, `Math.random()`, чтение `window`/`document`/`localStorage` во время рендера (undefined на сервере), форматирование `Intl` в зависимости от локали или timezone, ветки `typeof window` и расширения браузера, мутирующие DOM до гидратации React.

**Правило детерминизма.** Лекарство — сделать первый клиентский рендер чистой функцией тех же входных данных, что были у сервера. Любое значение, которое может отличаться между сервером и клиентом, не должно присутствовать во время рендера — оно откладывается в `useEffect`, который запускается после гидратации, или скрывается за флагом `useState(false)` "mounted", переключаемым в эффекте, чтобы и серверный рендер, и первый клиентский рендер выдавали placeholder, и только более поздний рендер вводил клиент-специфичное значение. Когда видишь мигание при загрузке, которое появляется только в продакшне, — спроси себя: что компонент читает во время рендера, что сервер и клиент вычисляют в разные моменты времени.

<div data-lesson-visual class="my-8 rounded-xl border border-edge bg-surface-2 p-6">
  <p class="text-sm font-semibold text-ink mb-4">Hydration mismatch: что ломается и как</p>
  <div class="space-y-2 text-sm">
    <div class="grid grid-cols-3 gap-2 font-semibold text-xs text-ink-2 border-b border-edge pb-2">
      <div>Источник недетерминизма</div>
      <div>Почему несоответствие</div>
      <div>Исправление</div>
    </div>
    <div class="grid grid-cols-3 gap-2 py-2 border-b border-edge/50 text-xs">
      <div class="font-mono">new Date() / Date.now()</div>
      <div>Сервер и клиент запускаются в разные моменты</div>
      <div>Отложить в useEffect; рендерить null или значение сервера первым</div>
    </div>
    <div class="grid grid-cols-3 gap-2 py-2 border-b border-edge/50 text-xs">
      <div class="font-mono">Math.random()</div>
      <div>Разные случайные значения при каждом запуске</div>
      <div>Генерировать на сервере, передавать как сериализованный пrop</div>
    </div>
    <div class="grid grid-cols-3 gap-2 py-2 border-b border-edge/50 text-xs">
      <div class="font-mono">window / localStorage</div>
      <div>undefined на сервере, объект на клиенте</div>
      <div>Проверка typeof window отложена в эффект</div>
    </div>
    <div class="grid grid-cols-3 gap-2 py-2 border-b border-edge/50 text-xs">
      <div class="font-mono">Intl / timezone</div>
      <div>Timezone сервера отличается от timezone пользователя</div>
      <div>Передавать timezone явно; форматировать на сервере</div>
    </div>
    <div class="grid grid-cols-3 gap-2 py-2 text-xs">
      <div>Мутация DOM расширением браузера</div>
      <div>Расширение добавляет/удаляет узлы до гидратации</div>
      <div>suppressHydrationWarning на затронутых узлах</div>
    </div>
  </div>
</div>

**Ловушка несоответствия данных.** То же ограничение применяется к данным. Если сервер рендерит из одного снимка ответа API, а клиент перезапрашивает более новый до гидратации, деревья расходятся — hydration mismatch, вызванный данными, а не временем или локалью. Исправление: данные сервера должны быть встроены в HTML и повторно использованы клиентом для первого рендера. Именно это делают фреймворки с dehydrated query cache (React Query, Apollo, SWR): сериализуют данные сервера в HTML, реgидрируют их на клиенте, рендерят первый проход из этого кэша, и только потом разрешают свежие запросы.

**Сериализация данных и dehydrated cache.** Размер встроенного dehydrated cache имеет значение. Очень большой ответ API, сериализованный в HTML, раздувает документ и время парсинга. Иногда правильный компромисс — встроить минимум для above-the-fold контента и перезапросить остальное после гидратации, смирившись с одним дополнительным фоновым запросом ради небольшого документа.

<Inset kind="edgecase" lang="ru">
Размер встроенного dehydrated cache важен. Очень большой ответ API, сериализованный в HTML, раздувает документ и время его парсинга — иногда правильный компромисс — встроить минимум для above-the-fold контента и перезапросить остальное после гидратации, смирившись с одним дополнительным фоновым запросом ради небольшого документа.
</Inset>

**Внутренности streaming SSR.** `renderToPipeableStream` рендерит дерево depth-first и сбрасывает оболочку (shell) — всё вне любых ожидающих `Suspense`-границ — как только она готова. Для каждой `Suspense`-границы, данные которой ещё не разрешились, сервер эмитирует fallback inline и держит соединение открытым. Когда данные границы разрешаются, сервер рендерит настоящий контент этой границы и записывает его как чанк: скрытый `<div>` с контентом плюс крошечный inline `<script>`, перемещающий его в нужный слот и удаляющий fallback. Браузер, парсящий HTML прогрессивно, выполняет этот скрипт в момент прибытия чанка. Границы стримятся в порядке завершения, а не порядке документа — быстрый виджет внизу страницы может прийти до медленного виджета выше.

**Streaming и гидратация переплетаются.** По мере того как HTML каждой `Suspense`-границы поступает потоком, React на клиенте может гидрировать эту границу независимо — не ожидая всего документа. Если пользователь взаимодействует с уже поступившей, но ещё не гидрированной границей, React записывает событие, приоритизирует гидрирование этой границы первой, затем воспроизводит событие. Импликация для senior-разработчика: расположение `Suspense`-границ — это решение о производительности, а не только о состояниях ошибки/загрузки. Граница вокруг медленного виджета ниже fold позволяет остальной части страницы стримиться и гидрироваться без ожидания; без границы этот виджет блокирует весь документ.

<DebugLog
  id="04-debug-1"
  lessonSlug="04-hydration-mismatch"
  lang="ru"
  label="Консоль браузера — предупреждение о гидратации"
  output={`Warning: Text content did not match.
  Server: "Добрый вечер" Client: "Доброе утро"
    at Greeting (greeting.tsx:6)
    at Header
    at App

Warning: An error occurred during hydration. The server HTML
was replaced with client content in <div id="greeting">.

[CLS] layout shift: 0.14  (source: #greeting subtree)`}
  outputLang="log"
  question="Компонент Greeting рендерит 'Доброе утро' или 'Добрый вечер' в зависимости от часа. При гидратации возникает несоответствие и CLS 0.14. Почему несоответствие, и каково правильное исправление?"
  hint="Сервер и клиент запускают компонент в разные моменты — и, возможно, в разных timezone. Что Greeting читает во время рендера?"
  answer="Greeting читает текущий час во время рендера. Сервер отрендерил его в timezone сервера в момент запроса; клиент гидрировал несколько секунд спустя, в timezone пользователя — поэтому два рендера вычислили разные приветствия, React обнаружил несоответствие текста, выбросил серверное поддерево и перерендерил на клиенте, что сдвинуло layout (CLS 0.14). Корневая причина — недетерминизм: вывод компонента зависит от Date/timezone, которые отличаются между двумя средами. Правильное исправление: сделать первый клиентский рендер совпадающим с серверным. Вариант первый: рендерить нейтральный, стабильный placeholder при первом проходе, затем вычислять реальное приветствие по времени суток в useEffect после гидратации и обновлять состояние — первый клиентский рендер точно совпадает с сервером, и только пост-гидрационное обновление меняет его, без несоответствия. Вариант второй: решить приветствие на сервере из источника, с которым обе стороны согласны (например, передать timezone пользователя явно, вычислить на сервере, передать результат как сериализованный prop), чтобы оба окружения вычисляли из одинаковых входных данных. В любом случае, никогда не читайте живое, зависящее от окружения значение непосредственно в рендере серверного компонента. Подключите ошибки hydration-mismatch к телеметрии, чтобы они обнаруживались в CI, а не в продакшне."
/>

<FadedExample
  client:visible
  id="04-faded-1"
  lessonSlug="04-hydration-mismatch"
  lang="ru"
  title="Устранение hydration mismatch из клиент-специфичного значения"
  steps={{
    solved: (
      <pre><code>{`function LocalTime() {
  // Первый рендер (сервер И первый клиентский рендер)
  // показывает placeholder — они совпадают, нет mismatch.
  const [time, setTime] = useState(null);

  useEffect(() => {
    // Запускается только на клиенте, ПОСЛЕ гидратации.
    setTime(new Date().toLocaleTimeString());
  }, []);

  return <span>{time ?? 'Загрузка…'}</span>;
}`}</code></pre>
    ),
    semi: {
      prompt: (
        <p>Заполните хук, откладывающий клиент-специфичное значение, и укажите, где вычисляется живое значение, чтобы оно запускалось после гидратации.</p>
      ),
      blanks: [
        { id: "state", expected: /useState/i, placeholder: "хук, хранящий отложенное значение" },
        { id: "effect", expected: /useEffect/i, placeholder: "хук, запускающийся после гидратации" },
      ],
    },
    blank: {
      prompt: (
        <p>Компонент рендерит <code>new Date().toLocaleTimeString()</code> напрямую. Он выдаёт предупреждение о hydration-mismatch и мигает при загрузке. Перепишите его, чтобы серверный рендер и первый клиентский рендер совпадали. В чём структурное изменение?</p>
      ),
      reveal: (
        <p>Не читайте живое значение во время рендера. Инициализируйте состояние стабильным placeholder (<code>null</code> или значением сервера), чтобы серверный рендер и первый клиентский рендер производили идентичный DOM — это удовлетворяет контракту гидратации и mismatch исчезает. Затем вычислите реальное клиент-специфичное значение внутри <code>useEffect</code>, который запускается только на клиенте и только после завершения гидратации, и вызовите <code>setState</code> для обновления. Пользователь видит placeholder один кадр, затем реальное время — без mismatch, без мигания, без CLS. Общее правило: любое значение, отличающееся между сервером и клиентом (время, локаль, <code>window</code>, <code>localStorage</code>, случайное), должно вводиться после гидратации, а не во время рендера.</p>
      ),
    },
  }}
/>

<TraceScenario
  id="04-trace-1"
  lessonSlug="04-hydration-mismatch"
  lang="ru"
  scenario="Серверно-рендеренная страница показывает в консоли предупреждение 'Text content did not match. Server: 14:32 Client: 14:33' и временная метка кратко мигает при загрузке. Что произошло и как исправить?"
  steps={[
    { prompt: "Компонент рендерит текущее время напрямую — сервер отрендерил его в одну секунду, клиент гидрировал секундой позже, поэтому два рендера не согласуются, и React перерендерил поддерево", reveal: "Верно. Рендеринг живого значения, такого как текущее время, во время рендера гарантирует несогласованность сервера и клиента, поскольку они запускаются в разные моменты. React обнаруживает несоответствие, отбрасывает серверный HTML для этого поддерева и перерендерит его на клиенте — видимое мигание. Исправление: рендерить стабильный placeholder при первом проходе (или значение сервера), затем обновиться до живого времени в useEffect после гидратации, чтобы первый клиентский рендер точно совпадал с сервером." },
    { prompt: "Часы сервера неверны", reveal: "Часы в порядке — они просто считываются в разные моменты. Даже идеально синхронизированные часы отличаются на секунды между рендером сервера и гидратацией клиента. Исправление — не синхронизация часов; это нерендеринг живого значения во время рендера." },
    { prompt: "CDN закэшировал устаревшую страницу", reveal: "Устаревший кэш показал бы старую, но внутренне согласованную страницу, а не предупреждение о несоответствии server/client. Предупреждение специфически говорит о несогласии двух рендеров." },
    { prompt: "JavaScript bundle повреждён", reveal: "Повреждённый bundle полностью не смог бы гидрироваться, а не производил бы точный diff 'text content did not match'. Это проблема детерминизма в одном компоненте." },
  ]}
/>

<Quiz
  id="04-quiz-1"
  lessonSlug="04-hydration-mismatch"
  lang="ru"
  question="Почему RSC и SSR описываются как ортогональные, а не конкурирующие?"
  choices={[
    { label: "SSR решает, когда/где генерируется HTML; RSC решает, какие компоненты вообще доставляют клиентский код — страница часто использует оба вместе", correct: true },
    { label: "RSC заменил SSR в React 18", misconception: "RSC не заменил SSR. Современное приложение серверно рендерит HTML (SSR) и использует RSC, чтобы держать серверные компоненты вне клиентского bundle — оба одновременно." },
    { label: "Это одно и то же с разными названиями", misconception: "Это разные оси. SSR — о времени генерации HTML; RSC — о том, какой код компонента достигает клиента." },
    { label: "RSC работает только без SSR", misconception: "RSC компонуется с SSR — Server Components рендерятся в потоковый HTML, а Client Components внутри SSR-рендерятся, затем гидрируются." },
  ]}
/>

</Explanation>

<KeyTakeaway>Гидратация — контракт: DOM сервера должен совпасть с первым клиентским рендером. Любое зависящее от окружения значение, читаемое во время рендера, нарушает его, вызывая CLS-спайкующий recovery re-render. Исправление: откладывать клиент-специфичные значения в useEffect; встраивать данные сервера в HTML, чтобы первый клиентский проход использовал тот же снимок.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="04-retrieval"
  lang="ru"
  questions={[
    { q: "Что такое hydration mismatch и каковы три наиболее частые причины?", a: "Hydration mismatch — когда DOM, созданный клиентом при первом рендере, отличается от DOM, сериализованного сервером. React обходит их синхронно, и при расхождении отбрасывает серверное поддерево и перерендерит его на клиенте — вызывая мигание контента и CLS. Три частые причины: 1) чтение Date/time во время рендера (сервер и клиент запускаются в разные моменты); 2) чтение window/localStorage во время рендера (undefined на сервере, объект на клиенте); 3) форматирование Intl с различиями локали или timezone между сервером и пользователем." },
    { q: "Почему данные ответа API сервера должны быть встроены в HTML для первого клиентского рендера?", a: "Если клиент перезапрашивает endpoint API до гидратации и получает более новый ответ, чем использовал сервер, первый клиентский рендер производит контент, отличный от HTML сервера — hydration mismatch, вызванный данными. Встраивание данных сервера в HTML (dehydrated query cache) позволяет первому клиентскому рендеру использовать точно тот же снимок, удовлетворяя контракту гидратации. Свежие запросы происходят только после завершения гидратации." },
    { q: "Как размещение Suspense-границы вокруг медленного виджета влияет на streaming и гидратацию?", a: "С Suspense-границей вокруг медленного виджета сервер отправляет оболочку (всё вне границы) немедленно, стримит HTML медленного виджета как чанк когда его данные разрешатся, и React может гидрировать каждую границу независимо по мере поступления. Без границы запрос медленного виджета блокирует весь HTML-ответ и весь проход гидратации. Размещение границы — это решение о производительности: оно контролирует, какой контент ждёт каких данных." },
  ]}
/>

<Recap lang="ru">
Гидратация — это контракт: React обходит серверный DOM и виртуальное дерево клиента синхронно, усваивая каждый узел. Когда любой узел не совпадает — потому что компонент читал `Date.now()`, `Math.random()`, `window` или локаль-зависимый формат во время рендера — React отбрасывает затронутое поддерево и перерендерит его на клиенте. Видимые симптомы — мигание контента и спайк CLS. Правило детерминизма: первый клиентский рендер должен быть чистой функцией тех же входных данных, что были у сервера. Клиент-специфичные значения перемещаются в `useEffect`, чтобы оба окружения производили идентичный DOM при первом проходе. То же ограничение применяется к данным: снимок запроса сервера должен быть встроен в HTML (dehydrated cache), чтобы первый клиентский проход использовал те же данные без перезапроса. Streaming SSR с `renderToPipeableStream` отправляет оболочку немедленно и стримит Suspense-границы по мере разрешения их данных — размещение границ определяет, какой контент платит какую latency, делая это решением о производительности, а не только о состоянии загрузки. Теперь, когда видишь непонятный спайк CLS на странице, которая визуально не прыгает при нормальном просмотре, — сначала проверь hydration-mismatch: сдвиг раскладки — это React, выбрасывающий серверное поддерево и рендерящий его заново.
</Recap>
