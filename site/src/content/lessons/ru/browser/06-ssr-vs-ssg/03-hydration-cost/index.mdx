---
slug: 03-hydration-cost
lang: ru
track: browser
unit: 06-ssr-vs-ssg
order: 3
title: "Цена гидратации: selective, progressive, острова, resumability"
summary: "Почему полная гидратация дорогая, как selective hydration и острова сокращают объём гидратации, и как resumability полностью устраняет переисполнение."
estMin: 16
status: ready
lessonType: topic
level: middle
prereqs: ["01-rendering-strategies", "02-strategy-mechanisms", "browser/05-react-fiber/03-render-and-commit-phases"]
deepensInto: ["04-hydration-mismatch", "05-rsc-and-observability"]
spiral: ["statefulness", "latency"]
concepts: ["full-hydration", "selective-hydration", "progressive-hydration", "islands-architecture", "resumability", "INP", "hydrateRoot"]
sources:
  - https://react.dev/reference/react-dom/client/hydrateRoot
  - https://docs.astro.build/en/concepts/islands/
  - https://qwik.dev/docs/concepts/resumable/
  - https://web.dev/articles/inp
---

import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import MetaphorComplete from "~/components/pedagogy/MetaphorComplete.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";

<Hook>
Страница новостной статьи: 2 000 слов текста, один бокс комментариев, кнопка лайка. Классическая полностраничная гидратация гоняет фреймворк по всем 2 000 словам текста — делая текст без какой-либо интерактивности «интерактивным». Бандл больше, чем нужно, и длинная задача гидратации блокирует главный поток, взрывая INP. Есть лучшие варианты.
</Hook>

<Crux>Полная гидратация переисполняет всё дерево — острова и selective hydration дают платить цену только интерактивным частям.</Crux>

<Explanation>

**Почему гидратация дорогая.** Гидратация примерно удваивает работу рендера — дерево рендерится один раз на сервере и фактически рендерится снова на клиенте. Но это хуже простого удвоения, потому что клиентский рендер гоняется на устройстве пользователя, которое может быть средним телефоном на порядок медленнее вашего сервера. И это всё JavaScript на главном потоке: скачать бандл, распарсить и скомпилировать его (работа V8), потом гонять дерево компонентов и цеплять обработчики. На большом приложении это сотни миллисекунд — секунды времени главного потока, во время которых страница выглядит готовой, но не готова. Всё это окно — «зловещая долина» SSR — страница нарисована и выглядит интерактивной, пользователь пытается взаимодействовать, и ввод отбрасывается или встаёт в очередь за длинной задачей гидратации, взрывая INP.

**Full vs selective гидратация.** Классическая гидратация React — полная и нетерпеливая: всё приложение гидрируется одним блокирующим проходом до того, как что-либо станет интерактивным. React 18 ввёл selective hydration: с границами `Suspense` части дерева гидрируются независимо, и React приоритизирует гидратацию того, с чем пользователь только что взаимодействовал. Если пользователь кликает по ещё-не-гидрированной секции, React гидрирует эту секцию первой. Гидратация становится прерываемой — может уступить вводу посреди прохода. Selective hydration не убирает суммарную цену; она переупорядочивает её.

<div data-lesson-visual class="my-8 rounded-xl border border-edge bg-surface-2 p-6">
  <p class="text-sm font-semibold text-ink mb-4">Спектр гидратации — от полной до нулевой</p>
  <div class="space-y-3 text-sm">
    <div class="rounded-lg bg-red-500/10 border border-red-500/20 px-4 py-3">
      <div class="font-semibold text-red-700 dark:text-red-400 mb-1">Полная гидратация (классический React)</div>
      <div class="text-ink-2">Гидрирует всё дерево в один блокирующий проход. Ничего не интерактивно, пока вся страница не закончит. Бандл включает код каждого компонента.</div>
    </div>
    <div class="rounded-lg bg-amber-500/10 border border-amber-500/20 px-4 py-3">
      <div class="font-semibold text-amber-700 dark:text-amber-400 mb-1">Selective hydration (React 18 + Suspense)</div>
      <div class="text-ink-2">Части дерева гидрируются независимо. Взаимодействие пользователя приоритизирует ту границу. Прерываемо — уступает вводу. Та же суммарная цена, лучший порядок.</div>
    </div>
    <div class="rounded-lg bg-blue-500/10 border border-blue-500/20 px-4 py-3">
      <div class="font-semibold text-blue-700 dark:text-blue-400 mb-1">Архитектура островов (Astro, Marko)</div>
      <div class="text-ink-2">Страница — статический HTML по умолчанию. Только интерактивные компоненты («острова») шлют JS и гидрируются. Статический текст: ноль JS, никогда не гидрируется. Задержка: client:visible, client:idle.</div>
    </div>
    <div class="rounded-lg bg-green-500/10 border border-green-500/20 px-4 py-3">
      <div class="font-semibold text-green-700 dark:text-green-400 mb-1">Resumability (Qwik)</div>
      <div class="text-ink-2">Никакой гидратации. Состояние приложения + расположения обработчиков сериализуются в HTML. Клиент возобновляется из сериализованного состояния, цепляет обработчик только на первом взаимодействии. Стартовый JS ≈ 0.</div>
    </div>
  </div>
</div>

**Progressive hydration и архитектура островов.** Более глубокий фикс — гидрировать меньше. Архитектура островов (Astro, Marko, Fresh) стартует от противоположного дефолта: страница — это статический HTML, и только конкретные интерактивные компоненты — «острова» — шлют JavaScript и гидрируются. Пост блога на 95% текст: с островами эти 95% шлют ноль JavaScript и никогда не гидрируются, и только бокс комментариев и кнопка лайка несут JS. Каждый остров гидрируется независимо, и ему можно сказать, когда (`client:visible` гидрирует при скролле в зону видимости, `client:idle` когда главный поток свободен).

**Resumability: пропуск гидратации совсем.** Qwik доводит идею до предела через resumability. Вместо переисполнения дерева компонентов на клиенте, чтобы пересобрать состояние и обработчики, Qwik сериализует всё состояние приложения — включая то, какой обработчик к чему прицеплен — в сам HTML. Клиент не перерендеривает, чтобы гидрироваться; он возобновляется с сериализованного состояния, цепляя обработчик только когда пользователь реально взаимодействует с этим конкретным элементом. Стартовый JavaScript приближается к нулю независимо от размера приложения. Компромисс — более сложный формат сериализации и фреймворк, построенный вокруг него с нуля.

<Quiz
  id="03-quiz-1"
  lessonSlug="03-hydration-cost"
  lang="ru"
  question="Что архитектура островов делает иначе, чем классическая полностраничная гидратация?"
  choices={[
    { label: "Страница по умолчанию статический HTML; только конкретные интерактивные компоненты шлют JavaScript и гидрируются — остальное никогда", correct: true },
    { label: "Она гидрирует всю страницу быстрее, используя Web Worker", misconception: "Острова не переносят гидратацию в воркер (гидратация трогает DOM). Они уменьшают, сколько вообще гидрируется — большая часть страницы никогда не гидрируется." },
    { label: "Она рендерит всё на клиенте вместо сервера", misconception: "Острова — серверно-отрендеренный HTML. Только острова получают клиентский JS — противоположность большему клиентскому рендерингу." },
    { label: "Она убирает нужду в сервере", misconception: "Острова всё ещё нуждаются в HTML, отрендеренном где-то (билд или сервер). Архитектура про объём гидратации, не про устранение сервера." },
  ]}
/>

<Quiz
  id="03-quiz-2"
  lessonSlug="03-hydration-cost"
  lang="ru"
  question="Страница полностью нарисована и визуально завершена, но тап по кнопке в первые 2 секунды ничего не делает. Какая метрика это ловит и в чём причина?"
  choices={[
    { label: "INP — JavaScript гидратации занимает главный поток, поэтому тап отброшен или поставлен в очередь за длинной задачей гидратации", correct: true },
    { label: "LCP — самый крупный элемент не нарисовался", misconception: "Страница полностью нарисована, значит LCP уже сработал. Проблема — интерактивность, не отрисовка — это домен INP." },
    { label: "TTFB — сервер был медленным", misconception: "TTFB измеряет server-to-first-byte. HTML уже пришёл и нарисовался; проблема в пост-отрисовочной интерактивности." },
    { label: "CLS — лейаут сдвинулся", misconception: "CLS измеряет движение лейаута. Мёртвая кнопка — проблема тайминга интерактивности, ловится INP." },
  ]}
/>

<MetaphorComplete
  id="03-metaphor-1"
  lessonSlug="03-hydration-cost"
  lang="ru"
  setup="Qwik избегает переисполнения дерева компонентов на клиенте. Вместо этого он сериализует всё состояние приложения — включая то, какой обработчик к чему относится — в HTML, и клиент подхватывает ровно там, где сервер остановился, цепляя обработчик только на первом взаимодействии. Как называется этот безгидратационный подход?"
  accepted={["resumability", "resumable", "resume", "возобновляемость", "резюмируемость"]}
  canonical="resumability"
  explanation="Resumability означает, что клиент возобновляет приложение из сериализованного состояния вместо пересборки. Классическая гидратация переисполняет всё дерево компонентов на клиенте; resumability сериализует всё это в HTML, поэтому клиент никогда не переисполняет дерево — он просто цепляет конкретный обработчик в момент первого взаимодействия. Стартовый JavaScript остаётся около нуля независимо от размера приложения."
/>

<TradeoffMatrix
  id="03-tradeoff-1"
  lessonSlug="03-hydration-cost"
  lang="ru"
  prompt="Контентный сайт: длинные статьи (большей частью текст) с боксом комментариев и несколькими интерактивными виджетами. Выберите подход рендеринга и гидратации."
  options={[
    { name: "Статический HTML + острова — только бокс комментариев и виджеты шлют JS и гидрируются", summary: "Минимум JS, минимум гидратации.", correct: true, justification: "Статья на 95% текст — эти 95% должны слать ноль JavaScript и никогда не гидрироваться. Острова отдают статический HTML для текста и гидрируют только реально интерактивные части. Первая отрисовка мгновенна, стоимость гидратации крошечная, и INP остаётся низким." },
    { name: "Полный SSR + полностраничная гидратация", summary: "Работает, но гидрирует 95%, которым ничего не нужно.", justification: "Серверный рендер всей страницы потом гидратация всего платит, чтобы сделать статический текст 'интерактивным', когда у него нет интерактивности. Бандл больше, длинная задача гидратации длиннее — реальная INP-цена за ноль выгоды на тексте." },
    { name: "Чистый клиентский рендеринг (SPA)", summary: "Худшая первая отрисовка для контента.", justification: "CSR шлёт пустую оболочку и рендерит в браузере — худшая возможная первая отрисовка и LCP для контентного сайта, плюс SEO-цена." },
    { name: "SSR без границ Suspense", summary: "Один медленный виджет блокирует всю статью.", justification: "Без границ Suspense загрузка данных одного медленного виджета держит весь HTML-ответ — текст статьи ждёт запрос счётчика комментариев." },
  ]}
/>

</Explanation>

<KeyTakeaway>Полная гидратация переисполняет всё дерево — дорого на медленных устройствах и блокирует INP. Selective hydration (React 18) переупорядочивает цену, приоритизируя взаимодействие. Острова гидрируют только интерактивные части. Resumability полностью пропускает переисполнение, сериализуя состояние в HTML.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="03-retrieval"
  lang="ru"
  questions={[
    { q: "Каковы четыре этапа стоимости гидратации на главном потоке?", a: "1. Скачать JS-бандл (сеть). 2. Распарсить и скомпилировать его (работа V8, жестокая на медленных мобильных CPU). 3. Исполнить модуль и дерево компонентов для пересборки виртуального дерева. 4. Прицепить — обойти серверный DOM, усыновляя узлы и подключая обработчики. Все четыре происходят после того, как страница уже нарисована." },
    { q: "Как selective hydration меняет опыт пользователя по сравнению с полной гидратацией?", a: "Selective hydration (React 18 + Suspense) позволяет каждой границе гидрироваться независимо и приоритизирует ту границу, с которой пользователь только что взаимодействовал. Делает гидратацию прерываемой — уступает вводу посреди прохода. Суммарная работа та же, но часть, которую пользователь тронул, оживает первой, а не ждёт всего дерева." },
    { q: "Каков дефолтный посыл архитектуры островов и чем он отличается от дефолта полной гидратации?", a: "Полная гидратация предполагает, что все компоненты должны быть интерактивными — они все гидрируются. Архитектура островов предполагает, что страница — статический HTML по умолчанию — только компоненты, явно отмеченные как острова, шлют JS и гидрируются. Статический текст, лейаут и неинтерактивный UI несут ноль клиентского JS." },
  ]}
/>

<Recap lang="ru">
Стоимость гидратации состоит из четырёх частей — скачать, распарсить/скомпилировать, исполнить, прицепить — все на главном потоке, все после того, как страница нарисована. Бандл на 500 КБ — это не просто 500 КБ сети; это сотни миллисекунд parse-compile-execute на телефоне. Зловещая долина: страница выглядит интерактивной, но ввод отбрасывается, потому что длинная задача гидратации владеет главным потоком — полевой INP. Selective hydration React 18 делает цену прерываемой и приоритизированной, но не сокращает суммарную. Архитектура островов атакует суммарную, гидрируя только интерактивные части — текстоёмкая страница с островами шлёт ноль JS для текста. Resumability (Qwik) полностью устраняет переисполнение: состояние и расположения обработчиков сериализуются в HTML, и клиент возобновляется из этого сериализованного состояния с нулевым стартовым JavaScript независимо от размера приложения.
</Recap>
