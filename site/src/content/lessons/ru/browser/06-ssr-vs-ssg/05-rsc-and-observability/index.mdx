---
slug: 05-rsc-and-observability
lang: ru
track: browser
unit: 06-ssr-vs-ssg
order: 5
title: "RSC, стратегия на маршрут и production-наблюдаемость"
summary: "Как React Server Components сокращают поверхность гидратации, фрейм принятия решений по маршруту для выбора SSG/ISR/SSR/streaming и как наблюдать за стратегиями рендеринга в продакшне."
estMin: 20
status: ready
lessonType: topic
level: senior
prereqs: ["01-rendering-strategies", "02-strategy-mechanisms", "03-hydration-cost", "04-hydration-mismatch"]
deepensInto: []
spiral: ["statefulness", "observability"]
concepts: ["RSC", "use-client", "per-route-strategy", "ISR", "streaming-SSR", "TTFB", "LCP", "INP", "hydration-mismatch-telemetry"]
sources:
  - https://react.dev/reference/rsc/server-components
  - https://nextjs.org/docs/app/building-your-application/rendering
  - https://web.dev/articles/rendering-on-the-web
  - https://web.dev/articles/inp
---

import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import DesignPrompt from "~/components/pedagogy/DesignPrompt.astro";
import RFCQuiz from "~/components/pedagogy/RFCQuiz.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";

<Hook>
Менеджер продукта спрашивает: маркетинговая главная, страницы товаров с обновлением цен несколько раз в день, залогиненный дашборд аккаунта с медленным запросом истории заказов и флоу оформления — должны ли они все использовать одну стратегию рендеринга? Ответ — нет, и неправильный выбор стоит либо TTFB, либо свежести, либо INP, либо SEO. Правильный ответ требует решения на маршрут, понимания роли RSC и production-телеметрии, которая ловит проблемы до пользователей.
</Hook>

<Crux>RSC ограничивает, какие компоненты доставляют клиентский код — ортогонально SSR. Стратегия на маршрут. Гейтируйте mismatch и INP в CI.</Crux>

<Explanation>

**RSC и SSR — ортогональные оси.** React Server Components контролируют, какие компоненты вообще доставляют JavaScript на клиент. Server Component запускается на сервере, рендерится в HTML (или RSC wire format) и вносит ноль байт в клиентский bundle — его код никогда не достигает браузера, поэтому он никогда не гидрируется. Client Component (`'use client'` в начале файла) отправляет код на клиент, SSR-рендерится в HTML при начальном рендере (если SSR включён), и затем гидрируется. SSR контролирует *когда и где генерируется HTML* — в момент запроса на сервере (SSR), на билде (SSG) или по расписанию с кэшированием (ISR). RSC контролирует *сколько из этого HTML нужно сделать интерактивным*. Страница, состоящая на 90% из Server Components, доставляет маленький клиентский bundle и имеет минимальную стоимость гидратации — даже при полном SSR. Распространённое заблуждение — думать, что RSC заменил SSR; это не так. Хорошо спроектированная страница использует оба: Server Components доставляют server-вычисленный HTML без клиентской стоимости, а небольшие `'use client'`-острова обрабатывают реально интерактивные части.

**SSR и SEO.** SSR и SSG одинаково хорошо работают для SEO — оба доставляют полный HTML поисковым роботам. Разница в стоимости и свежести: SSG бесплатен на запрос (CDN-хит), SSR стоит серверного времени на каждый запрос. CSR — худший вариант для SEO: Googlebot индексирует JS-рендеренный контент, но с задержкой и менее надёжно. Streaming SSR с правильными границами Suspense аналогично хорош для SEO, поскольку shell доставляет полный HTML — медленный фрагмент ещё не прибыл, но заголовок, title и основной контент уже доступны роботу.

<div data-lesson-visual class="my-8 rounded-xl border border-edge bg-surface-2 p-6">
  <p class="text-sm font-semibold text-ink mb-4">RSC и SSR — две независимые оси</p>
  <div class="space-y-3 text-sm">
    <div class="grid grid-cols-2 gap-4">
      <div class="rounded-lg bg-blue-500/10 border border-blue-500/20 px-4 py-3">
        <div class="font-semibold text-blue-700 dark:text-blue-400 mb-1">Ось SSR</div>
        <div class="text-ink-2 text-xs">Когда/где генерируется HTML. Варианты: на билде (SSG), на запрос (SSR), по расписанию+кэш (ISR) или только на клиенте (CSR). Контролирует TTFB и свежесть.</div>
      </div>
      <div class="rounded-lg bg-purple-500/10 border border-purple-500/20 px-4 py-3">
        <div class="font-semibold text-purple-700 dark:text-purple-400 mb-1">Ось RSC</div>
        <div class="text-ink-2 text-xs">Какие компоненты доставляют JS. Server Components → ноль клиентских байт, ноль гидратации. Client Components → доставляют код, SSR'd затем гидрируются. Контролирует размер bundle и поверхность гидратации.</div>
      </div>
    </div>
    <div class="rounded-lg bg-green-500/10 border border-green-500/20 px-4 py-3">
      <div class="font-semibold text-green-700 dark:text-green-400 mb-1">Оба вместе (Next.js App Router)</div>
      <div class="text-ink-2 text-xs">Server Components SSR'd и стримятся → быстрый TTFB, маленький bundle. 'use client'-листья гидрируются → только интерактивные части платят стоимость гидратации. Streaming Suspense-границы → быстрый shell, медленные данные не блокируют.</div>
    </div>
  </div>
</div>

**Фрейм решений на маршрут.** Правильный подход — никогда не одна стратегия для всего приложения. Каждый маршрут имеет разные требования к свежести, потребность в интерактивности и профиль latency данных — и эти три измерения определяют стратегию:

- **Маркетинговая главная** — контент одинаков для всех, меняется по расписанию маркетинга (дни, не минуты). Стратегия: SSG. Отдаётся прямо с edge CDN, TTFB — десятки миллисекунд, нулевая серверная работа на запрос, и новый билд при деплое маркетинга обновляет её.

- **Страницы товаров** — одинаковый контент для каждого пользователя, но цены обновляются несколько раз в день, и SEO требует быстрого LCP. Стратегия: ISR с интервалом ревалидации (например, 4 часа) плюс on-demand ревалидация, триггерируемая вебхуком ценового фида. Страница — кэшированный статический документ для подавляющего большинства запросов — отличный LCP и SEO — и свежесть ограничена: ни один запрос не ждёт больше одного интервала от обновления цены.

- **Дашборд аккаунта** — на пользователя, должен быть свежим, но имеет один медленный запрос (история заказов). Стратегия: streaming SSR. Рендер на запрос для свежести, но обернуть запрос истории заказов в Suspense-границу. Shell и быстрые секции стримятся немедленно (~50 мс TTFB); граница истории заказов стримится, когда её запрос разрешается. Пользователь видит контент быстро; медленный запрос не блокирует его.

- **Оформление заказа** — на пользователя, корректность превыше всего, высокая интерактивность. Стратегия: SSR на запрос. Держать в большей степени Server Components с маленькими `'use client'`-островами для полей формы и платёжных инпутов. Поверхность гидратации остаётся минимальной — только интерактивные части формы гидрируются, что держит INP низким даже под высокой нагрузкой на главный поток.

**ISR на грани: кэш, инвалидация, сборка.** Каждый CDN POP хранит свою собственную запись кэша. On-demand ревалидация (Next.js `revalidatePath` / Vercel purge API) инвалидирует запись глобально — но есть краткое окно (секунды до десятков секунд), в течение которого одни POPs отдают старую версию, а другие — новую. Для цено-чувствительных страниц ограниченная протухлость ISR — осознанный компромисс: все POPs протухают не более чем на один интервал, а on-demand ревалидация — клапан сброса давления. Если требуется истинная согласованность по всем POPs в момент обновления — единственный вариант это полный SSR на запрос.

<Inset kind="insight" lang="ru">
ISR на edge: каждый CDN POP держит свою запись кэша. On-demand ревалидация инвалидирует её глобально, но есть окно (секунды — низкие десятки секунд), когда одни POPs отдают старую версию, другие — новую. Для цено-чувствительных страниц ограниченная протухлость ISR — осознанный компромисс: все POPs протухают не более одного интервала, on-demand ревалидация — клапан сброса. Если нужна истинная согласованность — только полный SSR.
</Inset>

**Минимизация стоимости гидратации по всем маршрутам.** Универсальный принцип: толкать `'use client'` как можно ниже по дереву компонентов. Layout-компонент, карточка, таблица данных — почти всегда Server Components. Только лист, которому реально нужен `onClick`, `useState` или browser API, должен быть Client Component. Когда `'use client'` вытолкнут к листьям, клиентский bundle сжимается, проход гидратации крошечный, и INP уверенно остаётся под 200 мс.

**Production-наблюдаемость.** Стратегия рендеринга работает только тогда, когда вы знаете, когда она ломается. Три категории сигналов:

1. **TTFB и LCP на маршрут** — SSG-маршруты должны иметь TTFB под 50 мс от CDN-хитов; streaming SSR-маршруты должны сбрасывать shell за ~50–100 мс. LCP должен быть под 2,5 с при p75. Детектируется в field data (CrUX) и синтетическом мониторинге (Lighthouse CI).

2. **INP на страницу** — длинная задача гидратации — наиболее частая INP-регрессия. Если INP при p75 превышает 200 мс на странице, недавно получившей Client Component или увеличившей размер bundle, стоимость гидратации выросла. Коррелируйте с изменениями кода. RUM-инструменты сегментируют INP по страницам.

3. **Частота hydration mismatch** — React логирует предупреждения о несоответствии в разработке, но в продакшне молчит, если не инструментировать. Подключите `window.addEventListener('error', ...)` для перехвата отчётов React error boundary, или используйте framework-интеграцию (Next.js `instrumentation.ts`, Sentry React plugin), захватывающую ошибки гидратации и отправляющую в трекер ошибок. CI-гейт: ноль новых ошибок hydration-mismatch на деплой — несоответствие в продакшне означает CLS-событие плюс recovery re-render.

<RFCQuiz
  id="05-rfc-1"
  pieceSlug="05-rsc-and-observability"
  lang="ru"
  question="Какой серверный API React DOM рендерит дерево как стрим, сбрасывая оболочку первой и стримя границы Suspense по мере резолва их данных?"
  choices={[
    { rfc: "renderToPipeableStream", title: "Node streaming SSR API — сбрасывает оболочку, стримит границы Suspense на резолве", correct: true },
    { rfc: "renderToString", title: "Рендерит всё дерево в одну строку синхронно — без streaming, блокируется на самой медленной части" },
    { rfc: "hydrateRoot", title: "Клиентский API, гидрирующий серверный HTML — не серверный рендерер" },
    { rfc: "createRoot", title: "Клиентский API для свежего клиентского рендера — не усыновляет серверный HTML" },
  ]}
/>

<Quiz
  id="05-quiz-1"
  lessonSlug="05-rsc-and-observability"
  lang="ru"
  question="Почему RSC и SSR описываются как ортогональные, а не конкурирующие?"
  choices={[
    { label: "SSR решает, когда/где генерируется HTML; RSC решает, какие компоненты вообще доставляют клиентский код — страница часто использует оба вместе", correct: true },
    { label: "RSC заменил SSR в React 18", misconception: "RSC не заменил SSR. Современное приложение серверно рендерит HTML (SSR) и использует RSC, чтобы держать серверные компоненты вне клиентского bundle — оба одновременно." },
    { label: "Это одно и то же с разными названиями", misconception: "Это разные оси. SSR о времени генерации HTML; RSC о том, какой код компонента достигает клиента. Страница использует каждое для своей цели." },
    { label: "RSC работает только без SSR", misconception: "RSC компонуется с SSR — Server Components рендерятся в потоковый HTML, а Client Components внутри SSR'd затем гидрируются." },
  ]}
/>

<TradeoffMatrix
  id="05-tradeoff-1"
  lessonSlug="05-rsc-and-observability"
  lang="ru"
  prompt="E-commerce страница товара: одинаковый контент для каждого пользователя, цены обновляются 4–6 раз в день, должна ранжироваться в поиске и загружаться быстро. Выберите стратегию рендеринга."
  options={[
    { name: "ISR — статический документ с ограниченным интервалом ревалидации + on-demand ревалидация при обновлениях цен", summary: "Лучший TTFB, SEO и ограниченная свежесть.", correct: true, justification: "Страница одинакова для всех пользователей (нет данных на пользователя), поэтому нет причины рендерить её на каждый запрос. ISR отдаёт её из CDN-кэша — TTFB в десятки миллисекунд, отличный LCP и SEO — а интервал ревалидации ограничивает протухлость не более чем несколькими часами, и on-demand ревалидация через вебхук ценового фида гарантирует обновление страницы в течение секунд от изменения цены. Полный SSR платил бы серверные вычисления на каждый запрос без пользы для одинакового контента." },
    { name: "Полный SSR на запрос", summary: "Свежий, но платит серверные вычисления на каждый запрос.", justification: "Для контента, одинакового для всех пользователей, SSR перерендеривается на каждый запрос — ненужная CPU и TTFB цена без пользы свежести над ISR. Приемлемо для действительно уникальных данных на запрос; неправильно для общих страниц товаров." },
    { name: "SSG — рендер на билде, никогда не ревалидировать", summary: "Быстро, но цены протухают до следующего билда.", justification: "Чистый SSG без ревалидации означает, что цены заморожены на момент билда. Для товаров с ежедневными ценами это вызывает видимые несоответствия цен. ISR с on-demand ревалидацией — правильная модель." },
    { name: "CSR — загрузить данные товара в браузере", summary: "Пустая оболочка при первой загрузке, плохой LCP и SEO.", justification: "CSR отправляет пустую оболочку и загружает данные в браузере. LCP — это рендер после загрузки данных, а не начальный HTML — ужасно для SEO и первой отрисовки. Никогда не подходит для контента, который должен ранжироваться в поиске." },
  ]}
/>

<DesignPrompt
  id="05-design-1"
  pieceSlug="05-rsc-and-observability"
  lang="ru"
  prompt="Спроектируйте стратегию рендеринга для e-commerce сайта: маркетинговая главная, страницы категорий, страницы товаров с ежедневно меняющимися ценами, залогиненный дашборд аккаунта и флоу оформления. Оптимизируйте первую отрисовку, свежесть и интерактивность на маршрут."
  constraints={[
    "Маркетинговая главная: одинакова для всех, меняется по расписанию маркетинга.",
    "Страницы товаров: одинаковы для всех, цена обновляется несколько раз в день, должны грузиться быстро и ранжироваться в поиске.",
    "Дашборд аккаунта: на пользователя, должен быть свежим, имеет один медленный запрос 'история заказов'.",
    "Оформление: на пользователя, корректность важнее всего, интерактивно.",
    "Стоимость гидратации не должна блокировать ранние взаимодействия — INP под 200 мс.",
    "Никаких ошибок hydration-mismatch в продакшене.",
  ]}
  canonical="Выбирайте на маршрут. Маркетинговая главная: SSG — отрендерено на билде, отдаётся статически с CDN, мгновенный TTFB; ребилд на деплое маркетинга. Страницы товаров: ISR — отдаются статически для подавляющего большинства запросов, с интервалом ревалидации (например, каждые несколько часов) плюс on-demand ревалидация, когда обновляется ценовой фид, чтобы свежесть была ограничена без полного ребилда; это также даёт лучший LCP и SEO, поскольку страница — кэшированный статический документ. Дашборд аккаунта: streaming SSR — рендер на запрос для свежести, но обернуть медленный запрос 'история заказов' в Suspense-границу, чтобы оболочка дашборда и быстрые виджеты стримились немедленно (~50 мс TTFB), а история заказов стримилась, когда её запрос резолвится. Оформление: SSR, на запрос, корректность-в-первую-очередь; держать в большей степени Server Components с маленькими 'use client'-островами для интерактивных полей формы, чтобы поверхность гидратации была минимальной. По всем маршрутам: минимизировать клиентский bundle, держа компоненты Server Components и толкая 'use client' как можно ниже по дереву, чтобы стоимость гидратации оставалась малой и INP оставался под 200 мс. Гарантировать детерминизм: никакого чтения Date/Math.random/window во время рендера — отложить клиентские значения в useEffect — и подключить ошибки hydration-mismatch к телеметрии с CI-гейтом. Проверка: каждый тип маршрута протестирован на TTFB, LCP, зазор FCP-to-interactive и ноль предупреждений гидратации."
  keyPoints={[
    "Выбирайте стратегию на маршрут по свежести данных и интерактивности, никогда одну на всё приложение.",
    "SSG для замороженного-на-билде контента, ISR для ограниченной протухлости, streaming SSR для на-запрос с медленной зависимостью.",
    "Оборачивайте медленные данные в Suspense-границы, чтобы быстрый контент стримился без ожидания.",
    "Минимизируйте гидратацию: Server Components по умолчанию, 'use client' толкается вниз к маленьким листьям.",
    "Энфорсите детерминизм рендера и подключите ошибки hydration-mismatch к телеметрии + CI-гейт.",
  ]}
/>

</Explanation>

<KeyTakeaway>RSC и SSR — независимые оси — используйте оба. Применяйте стратегию на маршрут: SSG для статического, ISR для ограниченно-протухающего, streaming SSR для на-пользователя с медленными зависимостями. Толкайте 'use client' к листьям для минимизации гидратации. Подключите TTFB, LCP, INP и частоту hydration-mismatch к телеметрии с CI-гейтом.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="05-retrieval"
  lang="ru"
  questions={[
    { q: "Страница серверно-отрендерена и рисуется за 400 мс, но пользователи репортят, что кнопки не отвечают ещё ~2 секунды. Объясните механизм и назовите три способа сократить мёртвое окно.", a: "400 мс отрисовки — это приход серверного HTML и его рендеринг браузером — пиксели, но инертные. ~2-секундное мёртвое окно — гидратация: браузер должен скачать JavaScript-bundle, распарсить и скомпилировать его (работа V8, медленная на мобильных), исполнить дерево компонентов и обойти серверный DOM, цепляя обработчики событий. Пока гидратация не дошла до данной кнопки, у этой кнопки нет обработчика — тапы отбрасываются или встают в очередь за длинной задачей гидратации, что ровно и взрывает INP. Три сокращения: 1) Слать меньше JavaScript — использовать Server Components, чтобы display-only компоненты несли ноль клиентского кода и никогда не гидрировались. 2) Гидрировать меньше страницы — архитектура островов делает так, что статический контент вообще не шлёт JS; progressive hydration откладывает некритичные острова до idle или видимости. 3) Переупорядочить цену — selective hydration React 18 с границами Suspense гидрирует часть, с которой пользователь взаимодействовал, первой и уступает вводу посреди гидратации." },
    { q: "RSC, SSR и streaming часто смешивают. Разложите, что каждое независимо контролирует, и как одна страница Next.js App Router использует все три.", a: "Это три отдельные оси. SSR контролирует, когда и где генерируется HTML — на сервере, на запрос, в противоположность билду (SSG) или браузеру (CSR). Streaming контролирует, как HTML доставляется — сбрасывается кусками по мере резолва границ Suspense, а не держится до готовности всей страницы. RSC контролирует, какие компоненты вообще доставляют код на клиент — Server Components запускаются только на сервере, их код никогда не достигает браузера, а Client Components ('use client') — интерактивные части, чей код доставляется. Одна страница App Router использует все три: её Server Components запускаются на сервере и рендерятся в HTML (SSR), HTML сбрасывается прогрессивно по мере резолва данных (streaming), и только 'use client'-листья вносят вклад в клиентский bundle и нуждаются в гидратации (RSC ограничивает поверхность гидратации). RSC уменьшает, сколько гидрируется; SSR плюс streaming контролирует, как быстро появляется HTML." },
    { q: "Какие три категории production-сигналов нужно мониторить для стратегии рендеринга, и каков CI-гейт для hydration mismatch?", a: "1. TTFB и LCP на маршрут — SSG-маршруты должны показывать TTFB под 50 мс от CDN-хитов; shell streaming SSR должен сбрасываться за 50–100 мс; LCP под 2,5 с при p75. Детектируется через CrUX и Lighthouse CI. 2. INP на страницу — длинная задача гидратации — наиболее частая INP-регрессия; если INP при p75 превышает 200 мс после добавления Client Component или роста bundle, стоимость гидратации выросла. Сегментируйте INP по страницам с помощью RUM-инструментов. 3. Частота hydration mismatch — React логирует предупреждения в разработке, но молчит в продакшне без инструментации. Подключите window.addEventListener('error', ...) или framework-интеграцию (Next.js instrumentation.ts, Sentry React plugin) для захвата ошибок гидратации и отправки в трекер ошибок. CI-гейт: ноль новых ошибок hydration-mismatch на деплой — несоответствие означает CLS-событие плюс recovery re-render." },
  ]}
/>

<Recap lang="ru">
RSC и SSR решают разные проблемы на разных осях. SSR контролирует, когда генерируется HTML (билд, запрос, кэшированный интервал); RSC контролирует, какие компоненты вообще доставляют JavaScript — Server Components вносят ноль клиентских байт и никогда не гидрируются. Комбинация: большей частью Server Components держит bundle маленьким и стоимость гидратации низкой, SSR или streaming доставляет быстрый TTFB и LCP, а маленькие `'use client'`-листья гидрируются быстро. Фрейм решений на маршрут: маркетинговый контент → SSG; общий контент с ограниченной протухлостью → ISR с on-demand ревалидацией; на-пользователя с медленной зависимостью → streaming SSR с Suspense вокруг медленного запроса; на-пользователя высокой интерактивности → SSR с минимальной `'use client'`-поверхностью. Толкайте `'use client'` к наименьшему листу, которому это реально нужно. В продакшне: мониторьте TTFB и LCP на маршрут, INP на страницу и частоту hydration-mismatch с CI-гейтом ноля новых несоответствий на деплой. Гидратация — вопрос состояния: сервер захватил снимок состояния, который клиент должен реконструировать идентично; каждая стратегия здесь — разное решение о том, какое состояние заморожено когда и что стоит перенести его через провод.
</Recap>
