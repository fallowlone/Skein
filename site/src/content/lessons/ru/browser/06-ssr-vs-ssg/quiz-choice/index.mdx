---
concepts:
- render-strategy-selection
- isr-revalidation
- streaming-ssr
- hydration-cost
- rsc-vs-ssr
- hydration-mismatch
estMin: 13
lang: ru
level: senior
order: 7
prereqs: []
slug: quiz-choice
sources:
- https://web.dev/articles/rendering-on-the-web
- https://react.dev/reference/rsc/server-components
- https://nextjs.org/docs/app/building-your-application/data-fetching/incremental-static-regeneration
status: ready
summary: "Синтез с множественным выбором по юниту о стратегиях рендеринга: выбор SSG/ISR/SSR/streaming по маршруту, ортогональность RSC и SSR и реальная цена hydration."
title: 'Стратегии рендеринга: тест с множественным выбором'
track: browser
unit: 06-ssr-vs-ssg
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Шесть вопросов, проходящих сквозь весь юнит. Каждый — решение, которое вы принимаете, проектируя реальный маршрут: не определение для заучивания, а компромисс между свежестью, ценой сервера и мёртвым окном до того, как страницу можно кликнуть.
</Hook>

<Goal lang="ru">
Убедитесь, что вы связываете когда производится HTML (SSG/ISR/SSR/streaming) с тем, сколько из него должно стать интерактивным (hydration, RSC) — синтез, к которому вели отдельные уроки.
</Goal>

<Quiz
  id="06-ssr-vs-ssg-choice-1"
  lessonSlug="06-ssr-vs-ssg"
  lang="ru"
  question="Страница товара в e-commerce одинакова для всех пользователей, цена меняется 4–6 раз в день, и она должна ранжироваться в поиске с быстрым LCP. Какая стратегия подходит лучше всего и почему?"
  choices={[
    { label: "ISR — отдавать закэшированный статический документ для подавляющего большинства запросов, с интервалом ревалидации плюс on-demand ревалидацией по вебхуку прайс-фида. Лучший LCP/SEO, а устаревание ограничено одним интервалом.", correct: true },
    { label: "Чистый SSG без ревалидации — отрендерить один раз на билде.", misconception: "Чистый SSG замораживает цену на момент билда; цена, меняющаяся несколько раз в день, будет видимо устаревшей до следующей полной пересборки. ISR добавляет ограниченную ревалидацию, не теряя статическую доставку." },
    { label: "Полный SSR на каждый запрос — рендерить заново каждый раз.", misconception: "Контент одинаков для всех, поэтому SSR платит серверной вычислительной ценой на каждый запрос без выигрыша в свежести по сравнению с ISR. SSR — для действительно per-request или per-user данных." },
    { label: "CSR — загружать данные товара в браузере.", misconception: "CSR отдаёт пустую оболочку и рендерит после клиентского запроса, поэтому LCP — это рендер после загрузки данных: худший first paint и SEO для контента, который должен ранжироваться." },
  ]}
/>

<Quiz
  id="06-ssr-vs-ssg-choice-2"
  lessonSlug="06-ssr-vs-ssg"
  lang="ru"
  question="Залогиненный дашборд должен быть свежим per-user, но имеет один медленный запрос истории заказов (~400 мс). Обычный SSR заставляет каждого пользователя ждать 400 мс TTFB. Какое решение верное?"
  choices={[
    { label: "Streaming SSR — рендерить per request, но обернуть запрос истории заказов в Suspense-границу, чтобы оболочка и быстрые виджеты сбрасывались за ~50 мс, а история заказов подгружалась потоком, когда её запрос разрешится.", correct: true },
    { label: "Перевести дашборд на ISR.", misconception: "ISR кэширует один общий документ; per-user дашборд нельзя отдавать из общего кэша. ISR — для контента, одинакового для всех пользователей." },
    { label: "Перевести весь дашборд на SSG.", misconception: "SSG рендерит один раз на билде без per-user и per-request данных — он вообще не может отразить историю заказов конкретного залогиненного пользователя." },
    { label: "Оставить обычный SSR, но увеличить выделенный серверу CPU.", misconception: "Больше CPU не сократит запрос на 400 мс, который блокирует весь ответ. Streaming меняет единицу ожидания со всей страницы на одну Suspense-границу — это и есть реальное решение." },
  ]}
/>

<Quiz
  id="06-ssr-vs-ssg-choice-3"
  lessonSlug="06-ssr-vs-ssg"
  lang="ru"
  question="Коллега говорит: «мы внедрили RSC, значит SSR нам больше не нужен». Почему это неверно?"
  choices={[
    { label: "RSC и SSR — ортогональные оси. SSR решает когда/где генерируется HTML; RSC решает, какие компоненты вообще отправляют клиентский код. Современная страница App Router использует оба — Server Components рендерятся в HTML через SSR, а 'use client'-листья гидрируются.", correct: true },
    { label: "Верно — RSC заменил SSR в React 18.", misconception: "RSC не заменил SSR. Server Components всё равно рендерятся в HTML на сервере (SSR), часто потоком; RSC лишь убирает их код из клиентского бандла." },
    { label: "RSC и SSR — один механизм под двумя именами.", misconception: "Это разные оси: SSR про время и место генерации HTML; RSC про то, какой код компонентов вообще достигает браузера." },
    { label: "Неверно, потому что RSC требует CSR для работы.", misconception: "RSC не зависит от клиентского рендеринга; Server Components производят HTML и RSC-payload на сервере. Суть в том, что RSC сочетается с SSR, а не в том, что ему нужен CSR." },
  ]}
/>

<Quiz
  id="06-ssr-vs-ssg-choice-4"
  lessonSlug="06-ssr-vs-ssg"
  lang="ru"
  question="Новостная статья — 2000 слов текста, одно поле комментариев, одна кнопка лайка — полностью отрендерена через SSR и полностью гидрирована. INP на p75 плохой. Какое архитектурное исправление даёт наибольший рычаг?"
  choices={[
    { label: "Гидрировать меньше. Использовать острова (или опустить 'use client' только до поля комментариев и кнопки лайка), чтобы 95% текста отправляли ноль JS и никогда не гидрировались — long task гидратации сокращается, и INP падает.", correct: true },
    { label: "Перенести работу hydration в Web Worker.", misconception: "Hydration трогает DOM и не может выполняться вне главного потока. Решение — гидрировать меньше страницы, а не перемещать ту же работу." },
    { label: "Перевести статью с SSR на CSR.", misconception: "CSR ухудшает first paint и LCP для контента и всё равно отправляет JS для всего. Это увеличивает, а не уменьшает клиентскую работу, которая поднимает INP." },
    { label: "Увеличить бюджет рендера на сервере, чтобы hydration завершалась быстрее.", misconception: "Hydration выполняется на устройстве пользователя, не на сервере. Серверный бюджет не меняет клиентскую цену parse/compile/execute; уменьшение того, что гидрируется, — меняет." },
  ]}
/>

<Quiz
  id="06-ssr-vs-ssg-choice-5"
  lessonSlug="06-ssr-vs-ssg"
  lang="ru"
  question="Компонент приветствия читает текущий час во время рендера и печатает «Доброе утро» или «Добрый вечер». В продакшене пользователи видят мигание и скачок CLS 0.14, с предупреждением в консоли, что текст сервера и клиента расходятся. Какова корневая причина и верное исправление?"
  choices={[
    { label: "hydration mismatch из-за недетерминизма: сервер и клиент запускают компонент в разные моменты и, возможно, в разных таймзонах, поэтому React выбрасывает серверное поддерево и ре-рендерит, сдвигая layout. Исправление: отложить живое значение в useEffect, чтобы первый клиентский рендер совпал с серверным.", correct: true },
    { label: "Серверные часы неправильные и им нужна синхронизация NTP.", misconception: "Даже идеально синхронизированные часы различаются на секунды между серверным рендером и клиентской hydration. Лечение — не синхронизация часов, а отказ от чтения живого значения во время рендера." },
    { label: "CDN закэшировал устаревшую версию страницы.", misconception: "Устаревший кэш отдаёт старую, но внутренне согласованную страницу — без предупреждения о расхождении сервер/клиент. Это предупреждение именно о несовпадении двух рендеров." },
    { label: "JavaScript-бандл слишком велик, поэтому hydration не успела.", misconception: "Размер бандла влияет на длительность hydration, а не на то, совпадают ли два рендера. Точный диф «text content did not match» — баг детерминизма в одном компоненте." },
  ]}
/>

<Quiz
  id="06-ssr-vs-ssg-choice-6"
  lessonSlug="06-ssr-vs-ssg"
  lang="ru"
  question="По всему e-commerce-приложению вы хотите минимизировать цену hydration и держать INP под 200 мс на каждом маршруте. Какое одно правило самое надёжное?"
  choices={[
    { label: "Держать компоненты Server Components по умолчанию и опускать 'use client' как можно ниже по дереву — только лист, которому реально нужны onClick/useState/браузерные API, отправляет код, сокращая бандл и поверхность hydration.", correct: true },
    { label: "Пометить корневой layout 'use client', чтобы всё приложение гидрировалось одним предсказуемым проходом.", misconception: "Граница 'use client' высоко в дереве затягивает всех потомков в клиентский бандл — максимально большая поверхность hydration, противоположность цели." },
    { label: "Отключить streaming, чтобы hydration начиналась только после прихода полного документа.", misconception: "Streaming улучшает TTFB и позволяет границам гидрироваться по мере прихода; его отключение откладывает интерактивность. Оно не уменьшает, сколько нужно гидрировать." },
    { label: "Рендерить каждый маршрут через SSG, чтобы ничего не гидрировалось.", misconception: "SSG определяет когда генерируется HTML, а не гидрируется ли он — полностью SSG-страница с Client Components всё равно их гидрирует. Per-user маршруты вообще нельзя делать через SSG." },
  ]}
/>

<Recap lang="ru">
Сквозная линия юнита — решение по двум осям. Ось первая — когда производится HTML — идёт SSG (билд, заморожено, мгновенный хит CDN) → ISR (ограниченное устаревание через stale-while-revalidate) → SSR (свежесть per-request, цена сервера на критическом пути) → streaming SSR (оболочка первой за ~50 мс, медленные Suspense-границы позже). Ось вторая — сколько этого HTML должно стать интерактивным — управляется RSC (Server Components отправляют ноль клиентского кода) и стратегией hydration (полная → выборочная → острова → resumability). Продакшен-сбои сводятся к этому: устаревшие цены хотят ISR, медленные per-user запросы хотят streaming плюс Suspense, плохой INP хочет меньше hydration, а мигание контента и скачок CLS — это hydration mismatch, исправляемый детерминизмом рендера.
</Recap>
