---
concepts:
- ssg-ssr-isr
- streaming-ssr
- hydration-cost
- hydration-mismatch
- rsc-orthogonality
- per-route-strategy
estMin: 14
lang: ru
level: senior
order: 8
prereqs: []
slug: quiz-short
sources:
- https://web.dev/articles/rendering-on-the-web
- https://react.dev/reference/react-dom/server/renderToPipeableStream
- https://react.dev/reference/rsc/server-components
status: ready
summary: "Подсказки на свободное воспроизведение по юниту о стратегиях рендеринга — сначала восстановите механизм своими словами, затем откройте модельный ответ и сравните."
title: 'Стратегии рендеринга: тест на свободное воспроизведение'
track: browser
unit: 06-ssr-vs-ssg
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";

<Hook>
Воспроизведение сильнее перечитывания. На каждую подсказку проговорите или запишите полный ответ по памяти, прежде чем открыть модельный — именно усилие припоминания закрепляет механизм, а не узнавание на странице.
</Hook>

<Goal lang="ru">
Восстановите ключевые механизмы юнита — когда рендерит каждая стратегия, почему hydration стоит ~2×, что ломает контракт hydration и как сочетаются RSC и streaming — не подглядывая в уроки.
</Goal>

<RetrievalDrawer
  client:visible
  id="06-ssr-vs-ssg-short-retrieval"
  lang="ru"
  questions={[
    { q: "SSG, SSR, ISR, streaming SSR — определите каждую по тому, КОГДА производится HTML, и назовите цену каждой.", a: "SSG рендерит один раз на билде и отдаёт статический файл с CDN — TTFB в десятки миллисекунд, но контент заморожен до следующей полной пересборки. SSR рендерит per request с живыми данными — свежо и per-user, но TTFB теперь включает время рендера сервера плюс любой fetch данных, на критическом пути. ISR — это SSG с правилом ревалидации: отдаётся статикой из кэша, а после интервала (или on-demand триггера) следующий запрос получает устаревшую копию, пока сервер регенерирует в фоне — stale-while-revalidate для целых страниц, дающий статическую доставку с ограниченным устареванием. Streaming SSR отправляет оболочку сразу и стримит каждую Suspense-границу по мере разрешения её данных, так что TTFB падает до скорости самой быстрой части вместо самой медленной." },
    { q: "Почему hydration стоит примерно 2× серверного рендера и почему это хуже простого удвоения?", a: "Сервер рендерит дерево компонентов один раз, чтобы получить HTML; клиент перезапускает то же дерево, чтобы навесить обработчики событий и state — то есть рендер происходит дважды, примерно 2×. Это хуже чистого удвоения, потому что клиентский рендер выполняется на устройстве пользователя, которое может быть среднебюджетным телефоном на порядок медленнее сервера, и вся работа идёт в главном потоке: скачать бандл, распарсить и скомпилировать (V8), выполнить дерево, затем пройти серверный DOM, навешивая обработчики. В это окно страница отрисована, но мертва — тапы теряются или встают в очередь за long task hydration, что и поднимает INP." },
    { q: "Что такое hydration mismatch, каковы его частые причины и каково правило детерминизма, которое его предотвращает?", a: "Hydration — это контракт: DOM, который клиент производит на первом рендере, должен быть структурно идентичен DOM, сериализованному сервером. Когда узел расходится, React выбрасывает это поддерево и ре-рендерит его на клиенте — видимое мигание контента и скачок CLS. Частые причины — весь недетерминизм между двумя средами: чтение Date.now()/new Date(), Math.random(), window/localStorage (undefined на сервере) или Intl/таймзонозависимое форматирование во время рендера. Правило детерминизма: первый клиентский рендер должен быть чистой функцией тех же входов, что были у сервера — отложите любое зависящее от среды значение в useEffect, выполняющийся после hydration, чтобы оба рендера дали одинаковый DOM на первом проходе." },
    { q: "Почему данные API сервера должны быть встроены в HTML и какой механизм используют фреймворки?", a: "Если клиент перезапрашивает эндпоинт до hydration и получает более новый ответ, чем использовал сервер, два дерева расходятся — hydration mismatch из-за данных, а не из-за времени или локали. Исправление — встроить точный снимок данных сервера в HTML и переиспользовать его для первого клиентского рендера. Фреймворки делают это через dehydrated query cache (React Query, Apollo, SWR): сериализуют данные сервера в HTML, регидрируют их на клиенте, рендерят первый проход из этого кэша и только потом разрешают свежие fetch. Компромисс — размер документа: большой ответ раздувает время парсинга, поэтому иногда встраивают только данные above-the-fold, а остальное перезапрашивают после hydration." },
    { q: "RSC и SSR называют ортогональными. Что контролирует каждая ось и как одна страница Next.js App Router использует обе?", a: "SSR контролирует когда и где генерируется HTML — на сервере, per request (или на билде для SSG, по расписанию для ISR). RSC контролирует, какие компоненты вообще отправляют код клиенту: Server Component выполняется только на сервере, добавляет ноль байт в клиентский бандл и никогда не гидрируется; Client Component ('use client') отправляет свой код, рендерится через SSR в HTML для first paint, а затем гидрируется. Одна страница App Router использует обе — Server Components рендерятся на сервере (часто потоком) в HTML, и только 'use client'-листья добавляются в бандл и поверхность hydration. RSC уменьшает, сколько гидрируется; SSR плюс streaming контролирует, как быстро появляется HTML. RSC не заменил SSR." },
    { q: "Дайте per-route фрейм решения для маркетинговой главной, страниц товаров, дашборда аккаунта и checkout — с причиной для каждого.", a: "Маркетинговая главная: SSG — одинакова для всех, меняется по медленному расписанию, поэтому рендерим на билде и отдаём с edge CDN для TTFB в десятки мс. Страницы товаров: ISR — контент тот же для всех, но цены меняются несколько раз в день и SEO нужен быстрый LCP, поэтому отдаём закэшированный статический документ с интервалом ревалидации плюс on-demand ревалидацией по прайс-фиду, давая лучший LCP с ограниченным устареванием. Дашборд аккаунта: streaming SSR — per-user и должен быть свежим, но один медленный запрос истории заказов оборачивается в Suspense-границу, чтобы оболочка сбрасывалась за ~50 мс, а медленный запрос её не блокировал. Checkout: SSR per request — per-user, корректность прежде всего, держится в основном на Server Components с маленькими 'use client'-островами, чтобы поверхность hydration оставалась минимальной, а INP низким. Правило: никогда одна стратегия на всё приложение; выбирайте по маршруту по свежести × интерактивности × задержке данных." },
  ]}
/>

<Recap lang="ru">
Если вы смогли восстановить каждый ответ по памяти, вы держите хребет юнита: КОГДА производится HTML охватывает SSG → ISR → SSR → streaming, обменивая свежесть на цену сервера на критическом пути; hydration — это ~2× ре-рендер, выполняющийся на устройстве пользователя и блокирующий интерактивность (INP); контракт hydration ломается на любом недетерминированном значении, прочитанном во время рендера, и на снимке данных, отличном от серверного; RSC — ортогональная ось, полностью убирающая серверные компоненты из клиентского бандла; и верный ответ всегда per-route, выбранный по свежести, интерактивности и задержке данных.
</Recap>
