---
concepts:
- lanes
- priority-scheduling
- time-slicing
- useTransition
- useDeferredValue
- MessageChannel
- INP
deepensInto:
- 06-bailout-and-memoisation
estMin: 14
lang: ru
lessonType: topic
level: middle
order: 5
prereqs:
- 01-reconciler-overview
- 03-render-and-commit-phases
slug: 05-lanes-and-time-slicing
sources:
- https://react.dev/reference/react/useTransition
- https://react.dev/reference/react/useDeferredValue
- https://github.com/facebook/react/blob/main/packages/react-reconciler/src/ReactFiberLane.js
- https://web.dev/articles/inp
spiral:
- statefulness
status: ready
summary: React 18 присваивает каждому обновлению lane (приоритетный бакет). Time-slicing
  разбивает работу рендера на слайсы по 5 мс. useTransition и useDeferredValue переводят
  дорогие обновления в transition-lane, чтобы нажатия клавиш могли их прерывать.
title: Приоритетные lanes, time-slicing и useTransition
track: browser
unit: 05-react-fiber
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import MetaphorComplete from "~/components/pedagogy/MetaphorComplete.astro";
import NumberDrill from "~/components/pedagogy/NumberDrill.astro";
import FadedExample from "~/components/pedagogy/FadedExample.tsx";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";
import Pulse from "~/components/diagram/Pulse.astro";

<Hook>
Вы оборачиваете медленную фильтрацию списка в `startTransition`. Набор текста остаётся мгновенным, список догоняет при наличии свободного времени, а спиннер сигнализирует об ожидании. Без этого каждое нажатие клавиши вызывает блокирующий рендер на 50 мс. Механизм, делающий это возможным, — система приоритетных lanes React.
</Hook>

<Crux>Lanes задают приоритет; time-slicing нарезает рендер по 5 мс; transitions уводят тяжёлую работу в прерываемый lane, освобождая ввод.</Crux>

<Explanation>

**Приоритетные lanes.** React 18 присваивает каждому обновлению *lane* — приоритетный бакет. Модель lanes (битмаска из 31 lane) позволяет React сначала работать над высокоприоритетными обновлениями и откладывать низкоприоритетные. Важные уровни:

- **Sync / discrete:** Клик, нажатие клавиши. React рендерит немедленно и блокирующе, потому что пользователь ждёт именно этого взаимодействия.
- **Default:** Обычные обновления состояния без transition-обёртки.
- **Transition:** Задаётся через `useTransition` или `useDeferredValue`. Используется для обновлений, которых пользователь не ждёт напрямую — фильтрация большого списка, переход на новый вид. Может быть прервано чем-то более срочным.
- **Idle:** Самый низкий. Фоновая работа, которую приложение может делать, когда главный поток свободен.

Когда нажатие клавиши приходит в середине transition-рендера, React бросает незавершённый transition-рендер, обрабатывает нажатие с высоким приоритетом и перезапускает transition после.

<div data-lesson-visual class="my-6 rounded-lg border border-border bg-surface-1 p-4 font-mono text-sm">
  <div class="mb-2 text-ink-2 text-xs uppercase tracking-wider">Уровни приоритета lanes</div>
  <div class="flex flex-col gap-1 text-xs">
    <div class="flex items-center gap-2">
      <span class="w-32 text-right font-semibold text-red-700 dark:text-red-400">sync / клик</span>
      <span class="flex-1 rounded bg-red-100 dark:bg-red-950/30 px-2 py-1">Рендерится немедленно, блокирующе — пользователь ждёт</span>
    </div>
    <div class="flex items-center gap-2">
      <span class="w-32 text-right font-semibold text-orange-700 dark:text-orange-400">default</span>
      <span class="flex-1 rounded bg-orange-100 dark:bg-orange-950/30 px-2 py-1">Обычный setState — планируется скоро, не блокирует</span>
    </div>
    <div class="flex items-center gap-2">
      <span class="w-32 text-right font-semibold text-blue-700 dark:text-blue-400">transition</span>
      <span class="flex-1 rounded bg-blue-100 dark:bg-blue-950/30 px-2 py-1">startTransition / useDeferredValue — прерываемый, time-sliced</span>
    </div>
    <div class="flex items-center gap-2">
      <span class="w-32 text-right font-semibold text-gray-500">idle</span>
      <span class="flex-1 rounded bg-gray-100 dark:bg-gray-800/50 px-2 py-1">Самый низкий — запускается только когда больше ничего нет</span>
    </div>
  </div>
</div>

**Time-slicing: как фаза render уступает.** Длинный синхронный рендер блокирует ввод — именно тот режим отказа, который описывается в теме об event loop. Планировщик React разбивает фазу render на слайсы. После примерно 5 мс работы реконсиляции планировщик проверяет `shouldYield()`; если true — останавливается, публикует задачу продолжения через `MessageChannel` (кросс-браузерный способ запланировать задачу без clamp-а) и отдаёт браузеру обработку событий и рендеринг. Задача продолжения возобновляет рендер с сохранённого указателя fiber.

Рендер на 50 мс превращается в десять слайсов по 5 мс, перемежающихся с работой браузера, и ввод остаётся отзывчивым. Это применимо только к *конкурентным* рендерам — transitions и другим несрочным обновлениям. Обновление с sync-приоритетом (дискретный ввод) всё равно рендерится одним блокирующим проходом, потому что заставить пользователя ждать yield противоречило бы цели.

<FlowDiagram
  label="Конкурентный рендер разбивается на единицы примерно по пять миллисекунд; после каждой единицы React уступает браузеру, чтобы прошли ввод и отрисовка, затем возобновляет следующую единицу. Более приоритетное нажатие клавиши полностью прерывает слайсы и обрабатывается первым."
  hue="var(--d-frontend)"
  nodes={[
    { id: "slice1", label: "Единица рендера", sub: "~5 мс", col: 0, row: 0 },
    { id: "yield", label: "Yield", sub: "ввод + отрисовка", col: 1, row: 0 },
    { id: "slice2", label: "Единица рендера", sub: "~5 мс", col: 2, row: 0 },
    { id: "key", label: "Нажатие", sub: "приоритетный lane", col: 1, row: 1 },
  ]}
  edges={[
    { from: "slice1", to: "yield", label: "shouldYield()" },
    { from: "yield", to: "slice2", label: "resume" },
    { from: "key", to: "yield", label: "прерывает" },
  ]}
  caption="В каждой точке yield React проверяет shouldYield(); если слайс ~5 мс исчерпан, он публикует продолжение через MessageChannel и отдаёт ход браузеру. Нажатие клавиши сидит в более приоритетном lane, поэтому прерывает time-sliced transition: React бросает незавершённый рендер, обрабатывает ввод, затем перезапускается."
/>

В каждой точке <Pulse>yield</Pulse> браузер получает свой ход — именно эта пауза сохраняет ввод мгновенным во время длинного рендера.

**Битмаска lanes.** Lane — это один бит в 31-битном целом числе. Представление приоритета как битмаски позволяет React делать операции над множествами за одну инструкцию CPU: «есть ли pending высокоприоритетные lanes?» — это mask-and-test; «рендерить все lanes на уровне или выше этого приоритета» — сравнение битмасок. Обновления в совместимых lanes *батчатся* — несколько вызовов `setState` в одном обработчике события (или, с React 18, через промисы и таймауты через автоматический батчинг) сворачиваются в один рендер. Именно поэтому нельзя прочитать новое состояние синхронно сразу после `setState`: рендер, применяющий его, ещё не произошёл.

**useTransition и useDeferredValue.** Это два хука, переводящих работу в transition-lane. `useTransition` даёт функцию `startTransition` — обновления состояния внутри неё помечаются низкоприоритетными и прерываемыми, плюс флаг `isPending` для показа спиннера. Канонический случай: поле поиска, где само значение поля обновляется синхронно (набор текста ощущается мгновенным), а дорогое обновление отфильтрованного списка обёрнуто в `startTransition` (уступает следующим нажатиям клавиш).

`useDeferredValue` — та же идея со стороны потребителя: даёт копию значения, «отстающую» от оригинала — React обновляет её с transition-приоритетом, так что компонент, читающий отложенное значение, рендерится только когда есть свободное время. Используйте `useTransition`, когда вы владеете обновлением состояния; `useDeferredValue` — когда значение приходит из props или контекста, которыми вы не управляете.

**Suspense и приостановка дерева.** Lanes тесно взаимодействуют с Suspense. Когда компонент внутри `<Suspense>` бросает промис (ожидая данные), React приостанавливает рендер этого поддерева и показывает fallback. В concurrent-режиме это позволяет React продолжать рендерить другие части дерева в более приоритетных lanes, пока данные загружаются. Transition-lane делает Suspense неблокирующим: можно обернуть навигацию в `startTransition`, и React будет показывать старый экран (с индикатором pending), пока новый экран данных ещё загружается, а не показывать fallback немедленно.

<FadedExample
  client:visible
  id="sr-faded-1"
  pieceSlug="05-react-fiber"
  lang="ru"
  title="Не давать дорогому фильтрованному списку блокировать ввод"
  steps={{
    solved: (
      <pre><code>{`function Search({ allItems }) {
  const [query, setQuery] = useState('');
  const [isPending, startTransition] = useTransition();
  const [results, setResults] = useState(allItems);

  function onChange(e) {
    setQuery(e.target.value);              // срочно: ввод остаётся мгновенным
    startTransition(() => {                // низкий приоритет, прерываемо
      setResults(filter(allItems, e.target.value));
    });
  }

  return (
    <>
      <input value={query} onChange={onChange} />
      {isPending && <Spinner />}
      <List items={results} />
    </>
  );
}`}</code></pre>
    ),
    semi: {
      prompt: (
        <p>Заполните два пропущенных места: хук, создающий transition, и обёртку, помечающую дорогое обновление как низкоприоритетное.</p>
      ),
      blanks: [
        { id: "hook", expected: /useTransition/i, placeholder: "хук для transition" },
        { id: "wrap", expected: /startTransition/i, placeholder: "обёртка для дорогого обновления" },
      ],
    },
    blank: {
      prompt: (
        <p>Поле ввода фильтрует большой список. Каждое нажатие клавиши запускает дорогой <code>filter</code>, и поле отстаёт от набора. Перепишите обработчик change, чтобы ввод оставался мгновенным, а обновление списка могло прерываться следующим нажатием. В чём структурное изменение?</p>
      ),
      reveal: (
        <p>Разбить одно обновление состояния на два приоритета. Обновление <code>query</code>, управляющее полем ввода, остаётся обычным <code>setState</code> — synchronous lane, поле отражает каждое нажатие немедленно. Дорогой <code>setResults(filter(...))</code> идёт внутрь <code>startTransition</code> — transition lane, прерываемый и time-sliced. Теперь, когда приходит следующее нажатие посреди фильтрации, React бросает незавершённый рендер списка, применяет нажатие с высоким приоритетом и перезапускает фильтрацию. Ввод никогда не отстаёт; список догоняет, когда есть время. <code>isPending</code> управляет спиннером, чтобы задержка была видимой обратной связью, а не зависанием.</p>
      ),
    },
  }}
/>

<MetaphorComplete
  id="mid-metaphor-lanes"
  pieceSlug="05-react-fiber"
  lang="ru"
  setup="React 18 присваивает каждому обновлению приоритетный бакет, чтобы рендерить срочную работу первой и откладывать остальное. Нажатие клавиши попадает в синхронный бакет; дорогая фильтрация списка, обёрнутая в startTransition, — в низкий прерываемый бакет. Как React называет эти приоритетные бакеты?"
  accepted={["lanes", "lane", "lanes-model", "приоритетные lanes", "приоритетные бакеты"]}
  canonical="lanes"
  explanation="Lanes — это модель приоритетов React 18 — внутренне 31-битная битмаска, каждый бит — один lane. Дискретный ввод попадает в синхронный lane и рендерится немедленно блокирующе; transitions попадают в transition lane, который может прервать любой более срочный lane; idle-работа попадает в самые низкие lanes. Битмаска lanes позволяет React батчить совместимые обновления и выбирать, какой набор рендерить первым."
/>

<Quiz
  id="mid-quiz-lanes-1"
  pieceSlug="05-react-fiber"
  lang="ru"
  question="Вы обернули дорогое обновление списка в `startTransition`. В середине этого рендера приходит нажатие клавиши. Что делает React?"
  choices={[
    { label: "Бросает незавершённый transition-рендер, обрабатывает нажатие клавиши с высоким приоритетом, затем перезапускает transition-рендер", correct: true },
    { label: "Сначала завершает transition-рендер, затем обрабатывает нажатие клавиши", misconception: "Это заставило бы нажатие клавиши ждать — противоположность тому, для чего нужны transitions. React прерывает transition ради срочного ввода." },
    { label: "Запускает оба рендера параллельно в двух потоках", misconception: "Реконсилер React работает в одном главном потоке. Нет параллельного рендеринга — есть прерывание и возобновление." },
    { label: "Выбрасывает нажатие клавиши", misconception: "Нажатие никогда не выбрасывается. Оно обрабатывается с высоким приоритетом, впереди прерываемой transition-работы." },
  ]}
/>

<Quiz
  id="mid-quiz-lanes-2"
  pieceSlug="05-react-fiber"
  lang="ru"
  question="Какой вид обновления React рендерит одним блокирующим проходом без time-slicing?"
  choices={[
    { label: "Дискретное обновление ввода — клик или нажатие клавиши — потому что пользователь ждёт именно этого взаимодействия", correct: true },
    { label: "Любое обновление внутри startTransition", misconception: "Transition-обновления — именно те, которые time-sliced и прерываемы. Дискретный ввод — противоположное." },
    { label: "Все обновления — React никогда не использует time-slicing", misconception: "React 18 использует time-slicing для transition и других несрочных обновлений. Дискретный ввод — исключение, рендерящееся синхронно." },
    { label: "Обновления, вызванные useDeferredValue", misconception: "Обновления useDeferredValue работают с transition-приоритетом — прерываемые и time-sliced, а не блокирующие." },
  ]}
/>

<NumberDrill
  id="mid-drill-lanes"
  pieceSlug="05-react-fiber"
  lang="ru"
  question="Планировщик React делает примерно 5 мс работы рендера за слайс. Бюджет кадра при 60 fps — 16,67 мс. Примерно сколько слайсов рендера React помещается в один кадр при ~6 мс накладных расходов браузера?"
  expected={2}
  tolerance={1}
  unit="слайса"
  explanation="16,67 мс минус ~6 мс накладных расходов браузера оставляет ~10 мс для всего остального. При ~5 мс за слайс это около 2 слайсов за кадр. Time-slicing означает, что рендер на 50 мс распределяется примерно по 5 кадрам — каждый кадр всё равно отрисовывается, ввод продолжает работать, — вместо одной 50-миллисекундной задачи, пропускающей 3 кадра и поднимающей INP."
/>

</Explanation>

<KeyTakeaway>Lanes задают приоритет: дискретный ввод синхронен и блокирует; transitions прерываемы. Планировщик нарезает рендеры на куски по ~5 мс через MessageChannel, сохраняя отзывчивость ввода. useTransition помечает дорогие обновления как transition-приоритетные; useDeferredValue делает то же со стороны потребителя.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="05-lanes-and-time-slicing-retrieval"
  lang="ru"
  questions={[
    { q: "Почему событие клика рендерится синхронно и блокирующе, а обновление через startTransition — нет?", a: "Клик — дискретный пользовательский ввод: пользователь ожидает результат немедленно и смотрит на экран. Заставить его уступать ощущалось бы сломанным. Обновление через startTransition — работа, которой пользователь не ждёт напрямую (фильтрация списка, переход). React может прерывать и перезапускать её незаметно для пользователя, поэтому она работает с transition-приоритетом — прерываемая и time-sliced." },
    { q: "Как React уступает браузеру между слайсами рендера?", a: "Через MessageChannel. После ~5 мс работы планировщик публикует задачу продолжения на порт MessageChannel. Браузер может запустить обработчики ввода и задачи рендеринга до того, как подхватит это продолжение. setTimeout тоже работал бы, но зажат минимумом в 4 мс, что делает его менее точным." },
    { q: "В чём разница между useTransition и useDeferredValue?", a: "Оба переводят работу в transition-lane. useTransition даёт функцию startTransition, которую вы вызываете при отправке обновления, которым вы владеете, плюс флаг isPending. useDeferredValue даёт отстающую копию значения, которое вы получаете из props или контекста — полезно, когда вы не владеете обновлением состояния. Они решают одну проблему с разных сторон потока данных." },
  ]}
/>

<Recap lang="ru">
Система lanes React 18 присваивает каждому обновлению приоритетный бакет. Дискретный ввод (клики, нажатия) рендерится в sync-lane — один блокирующий проход без yield. Обычные обновления состояния используют default-lane. Работа, обёрнутая в `startTransition` или читаемая через `useDeferredValue`, работает в transition-lane — прерываемая и time-sliced. Планировщик разбивает transition-рендеры на слайсы по ~5 мс, публикует продолжение через `MessageChannel` между слайсами и возобновляет с сохранённого указателя fiber. Именно поэтому рендер списка на 50 мс не роняет кадры: он превращается в десять слайсов по 5 мс, перемежающихся с перерисовками браузера и обработкой ввода. Когда нажатие клавиши приходит посреди transition, React бросает незавершённый рендер, обрабатывает нажатие с sync-приоритетом и перезапускает transition с нуля.
</Recap>
