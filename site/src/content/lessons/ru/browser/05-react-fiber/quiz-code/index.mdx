---
concepts:
- key-prop-bug
- render-side-effect
- time-slicing-priority
- referential-stability
estMin: 14
lang: ru
level: senior
order: 9
prereqs: []
slug: quiz-code
sources:
- https://react.dev/learn/rendering-lists
- https://react.dev/reference/react/useTransition
- https://react.dev/reference/react/memo
status: ready
summary: "Читайте реальные сниппеты React — баг индексного ключа при reorder, побочный эффект в render-фазе, неправильное разделение приоритетов и сорванный bailout — и выбирайте сеньорский фикс."
title: 'React fiber: чтение кода'
track: browser
unit: 05-react-fiber
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Баги fiber читаются в коде и в Profiler, а не в документации. Прочитайте каждый сниппет, предскажите, как реконсилер его обработает, и выберите изменение, которое сеньор сделал бы первым.
</Hook>

<Goal lang="ru">
Отработайте цикл, который вы запускаете в каждом инциденте производительности или корректности React: прочитать компонент, предсказать, как reconciliation, разделение render/commit и планировщик lanes его обработают, и выбрать самый рычажный фикс.
</Goal>

## Сниппет 1 — переупорядочиваемый список

```jsx
function TodoList({ todos }) {
  // todos можно переупорядочивать перетаскиванием, у каждой строки
  // есть неуправляемый <input>, куда пользователь печатает заметки
  return (
    <ul>
      {todos.map((todo, i) => (
        <li key={i}>
          <span>{todo.title}</span>
          <input type="text" placeholder="notes…" />
        </li>
      ))}
    </ul>
  );
}
```

<Quiz
  id="05-react-fiber-code-1"
  lessonSlug="05-react-fiber"
  lang="ru"
  question="Пользователь печатает заметки в строку 3, затем перетаскивает строку 1 в самый низ. Заметки как будто прыгают на неправильную строку. В чём баг и исправление?"
  choices={[
    { label: "key={i} ключует строки по позиции. При reorder reconciliation сопоставляет каждого нового ребёнка со старым fiber по тому же индексу, поэтому набранный текст неуправляемого input (который живёт на fiber, а не в пропсах) следует за позицией, а не за todo. Фикс: key={todo.id} со стабильным id.", correct: true },
    { label: "Input нужен проп value, чтобы стать управляемым; неуправляемые input нельзя переупорядочивать.", misconception: "Неуправляемые input корректно переупорядочиваются со стабильным ключом. Дефект — позиционный ключ, а не отсутствие пропа value; сделать его управляемым — другое изменение, требующее ещё и состояния на строку." },
    { label: "Нужен useEffect, чтобы пересинхронизировать input после перетаскивания.", misconception: "Никакой эффект не исправит несовпавшую идентичность; input уже привязаны к неправильным fiber к моменту запуска любого эффекта. Фикс выше по потоку, в ключе, чтобы reconciliation спарил fiber с правильными todo." },
    { label: "Колбэк map нужно мемоизировать через useMemo.", misconception: "Мемоизация JSX не меняет, как reconciliation сопоставляет детей — он всё равно ключует по i. Проблема идентичности — сам ключ, а не частота пересборки массива." },
  ]}
/>

## Сниппет 2 — работа в теле функции

```jsx
function Chart({ points }) {
  // строим производный датасет и логируем аналитическое событие
  const series = expensiveTransform(points);
  analytics.track('chart_rendered', { count: series.length });

  return <svg>{series.map(p => <Dot key={p.id} {...p} />)}</svg>;
}
```

<Quiz
  id="05-react-fiber-code-2"
  lessonSlug="05-react-fiber"
  lang="ru"
  question="Какая строка — латентный баг при concurrent rendering в React 18 и почему?"
  choices={[
    { label: "analytics.track(...) — побочный эффект в render-фазе. Так как React 18 может переиграть, приостановить или отбросить рендер, он срабатывает непредсказуемое число раз — переучёт событий. Перенесите его в useEffect (после commit) или в событие, вызвавшее изменение.", correct: true },
    { label: "expensiveTransform(points) — вот баг: тяжёлые вычисления в render запрещены.", misconception: "Чистые вычисления в render допустимы; если они медленные, их мемоизируют через useMemo, но это не баг корректности. Баг корректности — нечистый вызов аналитики, дающий наблюдаемые эффекты при каждом повторе." },
    { label: "Спред {...p} на Dot — вот баг: он ломает reconciliation.", misconception: "Спред пропсов нормален и не влияет на идентичность reconciliation, которая управляется ключом. К нарушению чистоты рендера он отношения не имеет." },
    { label: "Бага нет; React гарантирует ровно один рендер компонента на обновление.", misconception: "React 18 явно не гарантирует один вызов на обновление — рендер может быть переигран или отброшен. Именно поэтому побочные эффекты в теле функции небезопасны." },
  ]}
/>

## Сниппет 3 — разделение приоритетов

```jsx
function Search({ allItems }) {
  const [query, setQuery] = useState('');
  const [results, setResults] = useState(allItems);
  const [isPending, startTransition] = useTransition();

  function onChange(e) {
    startTransition(() => {
      setQuery(e.target.value);                          // (A)
      setResults(filter(allItems, e.target.value));      // (B)
    });
  }
  return <><input value={query} onChange={onChange} />{isPending && <Spinner/>}<List items={results}/></>;
}
```

<Quiz
  id="05-react-fiber-code-3"
  lessonSlug="05-react-fiber"
  lang="ru"
  question="Ввод дёргается, хотя дорогой фильтр внутри startTransition. Что не так с этим разделением приоритетов?"
  choices={[
    { label: "Строка (A) — значение самого input — внутри startTransition, поэтому обновление query идёт с transition-приоритетом и может быть отложено или прервано, заставляя поле отставать от нажатия. В transition должна быть только дорогая (B); setQuery должна остаться синхронным обновлением вне его, чтобы input отражал каждое нажатие немедленно.", correct: true },
    { label: "useTransition не может обернуть два setState; разбейте их на два transition.", misconception: "startTransition может обернуть любое число обновлений. Проблема — какие обновления туда относятся: срочное значение input не должно откладываться, а дорогое обновление списка — должно." },
    { label: "Фильтр надо обернуть в useDeferredValue вместо этого — startTransition не делает time-slicing.", misconception: "startTransition как раз помещает обновление в time-sliced transition-lane. Дело не в выборе API; срочное обновление query ошибочно помещено в низкоприоритетную lane." },
    { label: "isPending надо проверить перед вызовом setResults, иначе спиннер не покажется.", misconception: "isPending выводится React из ожидающего transition; setResults не гейтят по нему. Спиннер рендерится нормально — реальный дефект в отложенном значении input." },
  ]}
/>

## Сниппет 4 — сорванный bailout

```jsx
const Row = React.memo(function Row({ item, onSelect }) {
  return <li onClick={() => onSelect(item.id)}>{item.label}</li>;
});

function List({ items, onSelect }) {
  return (
    <ul>
      {items.map(item => (
        <Row key={item.id} item={item} onSelect={() => onSelect(item.id)} />
      ))}
    </ul>
  );
}
```

<Quiz
  id="05-react-fiber-code-4"
  lessonSlug="05-react-fiber"
  lang="ru"
  question="Каждый Row перерендеривается всякий раз, когда перерендеривается List, хотя React.memo оборачивает Row и items не менялись. Почему и какой минимальный фикс?"
  choices={[
    { label: "onSelect={() => onSelect(item.id)} создаёт свежую стрелочную функцию для каждого Row при каждом рендере List. React.memo сравнивает пропсы через Object.is, а новая функция никогда не равна предыдущей, поэтому bailout не срабатывает. Фикс: передавать стабильный onSelect вниз и перенести привязку item.id в собственный обработчик клика Row, чтобы Row получал одну и ту же ссылку на функцию каждый рендер.", correct: true },
    { label: "React.memo не работает на компонентах, рендерящих <li>; он мемоизирует только листовой текст.", misconception: "React.memo работает на любом функциональном компоненте независимо от того, что он рендерит. Его срывает нестабильный inline-проп-стрелка, а не тип элемента." },
    { label: "key={item.id} форсирует перерендер каждой строки; уберите ключ.", misconception: "Ключ корректен и обязателен — именно он даёт каждой строке стабильную идентичность. Его удаление вызвало бы предупреждения и худшую reconciliation. Срыв — inline-функция-проп, а не ключ." },
    { label: "items — новый массив каждый рендер, поэтому memo не помогает.", misconception: "Row мемоизирован по собственным пропсам (item, onSelect), а не по массиву items родителя. Даже если items новый, каждая ссылка item может быть стабильной; настоящий виновник — свежая стрелка onSelect, создаваемая на строку." },
  ]}
/>

<Recap lang="ru">
Четыре чтения на уровне fiber: индексный ключ следует за позицией, поэтому неуправляемое состояние на fiber дрейфует к неправильному элементу при reorder — используйте стабильный id; побочный эффект в теле функции выполняется непредсказуемое число раз при повторе — перенесите его в useEffect или в событие; срочное значение input должно оставаться вне startTransition, а внутрь идёт только дорогое обновление; и inline-стрелка-проп — свежая ссылка каждый рендер, тихо срывающая React.memo. Прочитайте код, предскажите, как реконсилер его обработает, исправьте идентичность или ссылку прежде, чем тянуться к любому другому рычагу.
</Recap>
