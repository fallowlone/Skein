---
concepts:
- react-devtools-profiler
- loaf
- react-compiler
- production-observability
- TradeoffMatrix
- DesignPrompt
deepensInto: []
estMin: 14
lang: ru
lessonType: topic
level: senior
order: 7
prereqs:
- 06-bailout-and-memoisation
- 05-lanes-and-time-slicing
slug: 07-profiler-and-compiler
sources:
- https://react.dev/learn/react-developer-tools
- https://react.dev/blog/2024/10/21/react-compiler
- https://web.dev/articles/inp
- https://web.dev/articles/loaf
spiral:
- statefulness
status: ready
summary: React DevTools Profiler показывает, почему каждый fiber перерендерился и
  сколько это заняло. React Compiler автоматизирует мемоизацию. LoAF-записи связывают
  продакшн-жалобы на лаги с конкретными React-коммитами.
title: React Profiler, компилятор и продакшн-наблюдаемость
track: browser
unit: 05-react-fiber
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DebugLog from "~/components/pedagogy/DebugLog.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import DesignPrompt from "~/components/pedagogy/DesignPrompt.astro";
import Term from "~/components/prose/Term.astro";
import SpiralCue from "~/components/prose/SpiralCue.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
Пользователи сообщают о лагах. В офисе всё работает нормально. Profiler показывает commit на 38 мс, вызванный нажатием клавиши в поле поиска — и 240 ProductCard перерендериваются с причиной «parent rendered, props unchanged». Это сигнатура бага. Без Profiler вы гадаете; с ним исправление механично.
</Hook>

<Crux>Profiler называет причину и время каждого рендера; Compiler автоматизирует мемоизацию; LoAF связывает продакшн-лаги с конкретным коммитом.</Crux>

<Explanation>

**Чтение React Profiler.** React DevTools Profiler записывает timeline по commit-ам. Для каждого commit он показывает, какие файберы рендерились, почему каждый рендерился («props changed», «hook changed», «parent rendered», «context changed») и сколько каждый занял. Senior-воркфлоу: запись взаимодействия, поиск слишком длинного commit-а, сортировка файберов по self-time, чтение причины «why did this render».

- **«Parent rendered» при неизменившихся props** → отсутствующий bailout. Обернуть в `React.memo`, стабилизировать ссылку на нарушающий prop.
- **«Context changed» на множестве компонентов** → нестабильное значение контекста. Мемоизировать значение провайдера через `useMemo`.
- **«Hook changed»** → изменилось значение `useState` или `useReducer`. Проверить, мемоизировано ли вычисление, производящее его.

<DebugLog
  id="sr-debug-1"
  pieceSlug="05-react-fiber"
  lang="ru"
  label="React DevTools Profiler — детали commit"
  output={`Commit #47  —  render: 38.2 мс  (1 из 1 закоммичено)

  <App>                отрендерился  — 0.4 мс  — "hook changed"
    <Header>           отрендерился  — 0.2 мс  — "parent rendered"
    <Sidebar>          отрендерился  — 0.3 мс  — "parent rendered"
    <ProductGrid>      отрендерился  — 36.9 мс — "parent rendered"
      <ProductCard> x240  отрендерился  — 0.15 мс каждый — "parent rendered"

  Почему отрендерился <App>?  -> useState (searchTerm) изменился
  Примечание: props у <Header>, <Sidebar>, <ProductGrid> не изменились`}
  outputLang="log"
  question="Нажатие клавиши в поле поиска вызывает commit на 38 мс. Header, Sidebar и все 240 ProductCard рендерятся с причиной 'parent rendered', и Profiler говорит, что их props не изменились. Как исправить?"
  hint="Причина — 'parent rendered', а не 'props changed'. Что предотвращает перерендеринг дочернего компонента при рендере родителя, если props равны?"
  answer="Все дочерние компоненты рендерятся, потому что перерендерился <App>, и ничто не позволяет им сделать bailout. 'Parent rendered' плюс 'props unchanged' — точная сигнатура отсутствующего bailout. Исправление многослойное. Первое: обернуть Header, Sidebar и ProductCard в React.memo, чтобы они делали bailout при ссылочно равных props — это сразу избавляет Header и Sidebar, чьи props никогда не меняются. Второе: сделать props ProductCard ссылочно стабильными: если App передаёт инлайновые объекты или стрелочные функции (onClick={() => ...}, style={{...}}), мемоизировать их через useCallback/useMemo, иначе React.memo на ProductCard никогда не сработает, потому что свежий литерал никогда не равен через Object.is. Третье — структурное исправление — опустить состояние searchTerm вниз: если только поле поиска и грид нуждаются в нём, его подъём из <App> останавливает перерендеринг всего дерева при каждом нажатии. Четвёртое: для 240 карточек — виртуализировать, чтобы монтировались только видимые ~20. И обернуть обновление грида в startTransition, чтобы даже тяжёлый рендер уступал нажатиям. Проверить в Profiler: после исправления commit должен показывать только ProductGrid и изменившиеся карточки, хорошо меньше 16 мс."
/>

**Продакшн-наблюдаемость: LoAF + Profiler.** Profiler даёт локальное воспроизведение; <Term k="loaf" lang="en">Long Animation Frames</Term> (LoAF) дают продакшн-атрибуцию. Когда LoAF-запись атрибутирует медленный кадр React-бандлу, Profiler воспроизводит это локально и называет точные файберы. Вместе они закрывают петлю от «пользователи сообщают о лагах» до «этот компонент рендерится 200× из-за инлайнового style-объекта на строке 44».

<div data-lesson-visual class="my-6 rounded-lg border border-border bg-surface-1 p-4 font-mono text-sm">
  <div class="mb-2 text-ink-2 text-xs uppercase tracking-wider">Воркфлоу Profiler → исправление</div>
  <div class="flex flex-col gap-2 text-xs">
    <div class="rounded bg-surface-2 border border-border px-3 py-2">
      <span class="font-semibold">1. Запись</span> взаимодействия в Profiler
    </div>
    <div class="rounded bg-surface-2 border border-border px-3 py-2">
      <span class="font-semibold">2. Поиск</span> самого длинного commit — сортировка по общему времени рендера
    </div>
    <div class="rounded bg-surface-2 border border-border px-3 py-2">
      <span class="font-semibold">3. Чтение</span> причины «why did this render» для медленных файберов
    </div>
    <div class="rounded bg-surface-2 border border-border px-3 py-2">
      <span class="font-semibold">4. Исправление</span>: «parent rendered» + стабильные props → React.memo; «context changed» → мемоизировать значение провайдера; state вниз → коллокация состояния
    </div>
    <div class="rounded bg-surface-2 border border-border px-3 py-2">
      <span class="font-semibold">5. Проверка</span> — исправленный commit должен показывать только реально изменившиеся файберы
    </div>
  </div>
</div>

**React Compiler и будущее мемоизации.** Десятилетие поддержание bailout означало ручной разброс `useMemo`, `useCallback` и `React.memo` — утомительно, подвержено ошибкам, и само по себе не бесплатно (каждая memo-ячейка стоит памяти и сравнения). React Compiler (бывший «React Forget», стабильный трек с React 19) — build-time инструмент, анализирующий код компонентов и вставляющий мемоизацию автоматически и корректно, с более тонкой гранулярностью, чем делает человек. Он понимает, какие значения зависят от каких входных данных, и мемоизирует именно их, так что bailout срабатывает без рукописных хуков.

<FlowDiagram
  label="React Compiler работает во время сборки: он анализирует исходный код каждого компонента, отслеживает, какие значения зависят от каких входных данных, и вставляет мемоизацию автоматически, поэтому тот же bailout по ссылочной стабильности срабатывает в рантайме без рукописных useMemo или useCallback."
  perRow={4}
  hue="var(--d-frontend)"
  nodes={[
    { id: "source", label: "Исходник компонента", sub: "без ручной memo" },
    { id: "compiler", label: "React Compiler", sub: "анализ при сборке" },
    { id: "memo", label: "Авто-мемоизация", sub: "стабильные ссылки" },
    { id: "bailout", label: "Bailout срабатывает", sub: "пропуск рендеров" },
  ]}
  edges={[
    { from: "source", to: "compiler" },
    { from: "compiler", to: "memo", label: "вставляет кеш" },
    { from: "memo", to: "bailout" },
  ]}
  caption="Компилятор делает при сборке то, что раньше разбрасывали вручную. Он отслеживает зависимости каждого значения и мемоизирует именно те, что нужно — тоньше, чем сделал бы человек. Рантайм-механизм неизменен: стабильные ссылки открывают bailout. Поэтому его всё ещё нужно понимать — чтобы читать вывод компилятора и отлаживать его консервативные bailout-ы."
/>

Senior-вывод: *механизм* — ссылочная стабильность, открывающая bailout — никуда не денется; это фундамент, на котором строится компилятор. Понимание того, почему стабильная ссылка позволяет fiber пропустить работу, — это то, что позволяет читать вывод компилятора, отлаживать его при консервативных bailout-ах и рассуждать о производительности вне зависимости от того, написана мемоизация вручную или сгенерирована компилятором.

<Inset kind="why" lang="ru">
**Почему виртуальный DOM — не просто «накладные расходы».** «Виртуальный DOM» — маркетинговое название для дерева элементов, которое диффит React. Стоит быть точным: виртуальный DOM не быстрее прямой манипуляции DOM — рукописная, оптимальная последовательность DOM-вызовов всегда победит React. Что покупает виртуальный DOM — это *модель программирования*: вы описываете UI как чистую функцию состояния и никогда не пишете императивный DOM-код, а React делает результат достаточно быстрым через дифф. Стоимость реальна — построение и дифф дерева элементов — это CPU-работа, которой избегает компилируемый фреймворк (Svelte) или fine-grained реактивный (SolidJS). Ставка React: выигрыш DX от «UI = f(state)» стоит накладных расходов реконсиляции, а time-slicing не даёт этим накладным расходам никогда блокировать пользователя.
</Inset>

<TradeoffMatrix
  id="card-fiber-cost"
  pieceSlug="05-react-fiber"
  lang="ru"
  prompt="Поле поиска фильтрует таблицу из 5000 строк. Набор текста тормозит. Выберите основное исправление."
  options={[
    { name: "Виртуализировать таблицу + обернуть обновление фильтра в startTransition", summary: "Ограничивает монтированные узлы; уступает нажатиям клавиш.", correct: true, justification: "Виртуализация делает commit дешёвым — только ~20 видимых строк монтируются вне зависимости от размера датасета, так что непрерываемая фаза commit остаётся маленькой. startTransition переводит перерендер при фильтрации в transition-lane — time-sliced, и любое нажатие клавиши его прерывает. Вместе они исправляют обе половины: маленький commit, прерываемый render. Это канонический fix." },
    { name: "Обернуть каждый компонент строки в React.memo", summary: "Помогает, но не ограничивает стоимость commit.", justification: "React.memo позволяет неизменившимся строкам делать bailout из фазы render — это полезно — но все 5000 строк всё ещё смонтированы, поэтому фаза commit и сам DOM остаются огромными. Memo без виртуализации лечит симптом; дерево из 5000 узлов — болезнь." },
    { name: "Добавить debounce 300 мс к полю поиска", summary: "Уменьшает частоту, не стоимость.", justification: "Debounce заставляет дорогой рендер происходить реже, маскируя торможение, но каждый рендер всё равно блокирующее обновление 5000 строк. Когда оно срабатывает — всё равно роняет кадры. Допустимо как дополнение, никогда как основное исправление." },
    { name: "Перенести рендер таблицы в web worker", summary: "Невозможно — у worker-ов нет DOM.", justification: "Worker не может рендерить React DOM. Он мог бы предварительно фильтровать датасет из 5000 строк вне главного потока и возвращать маленький результат, что помогает если узкое место — вычисление фильтра. Но сам рендеринг должен оставаться в главном потоке." },
  ]}
/>

<DesignPrompt
  id="sr-design-1"
  pieceSlug="05-react-fiber"
  lang="ru"
  prompt="Спроектируйте стратегию рендеринга для real-time торгового дашборда: грид цен из 2000 строк, обновляющийся ~10× в секунду, плюс интерактивные фильтры и панель деталей. Необходимо держать INP ниже 200 мс и никогда не жертвовать отзывчивостью поля ввода."
  constraints={[
    "Тики цен приходят ~10 раз в секунду через WebSocket.",
    "Правильными должны быть только видимые строки; грид прокручивается.",
    "Элементы управления фильтром и сортировкой должны ощущаться мгновенными.",
    "Фаза commit должна оставаться короткой — никаких длинных непрерываемых блоков.",
    "Внешний store цен не должен рваться (tearing) при конкурентных рендерах.",
    "Цель INP: ниже 200 мс на p75.",
  ]}
  canonical="Виртуализировать грид, чтобы только ~30 видимых строк плюс overscan были когда-либо смонтированы — это ограничивает фазу commit вне зависимости от датасета 2000 строк. Подписаться на WebSocket-store цен через useSyncExternalStore, чтобы конкурентные рендеры не могли порваться: каждый компонент в commit видит один согласованный снимок. Батчить входящие тики: вместо setState на каждый тик (10/с × много символов), накапливать тики и сбрасывать один раз за animation frame через одно обновление store, сворачивая работу в один рендер за кадр. Маршрутизировать обновление price-грида через startTransition (или полагаться на useDeferredValue для значений цен грида), чтобы он рендерился с transition-приоритетом — прерываемый, time-sliced, никогда не блокирующий ввод фильтра. Держать состояние фильтра/сортировки локальным и синхронным, чтобы эти элементы управления рендерились в sync-lane и ощущались мгновенными. Обернуть каждую строку в React.memo и держать её props ссылочно стабильными (мемоизированные обработчики, никаких инлайновых объектов), чтобы неизменившиеся строки делали bailout. Ключевать каждую строку по стабильному symbol id, никогда по индексу, чтобы перестановки при сортировке корректно сохраняли per-row состояние. Проверка: в React Profiler сброс тиков должен перерендеривать только видимые строки с изменившейся ценой, commit меньше 8 мс; в DevTools Performance — никаких длинных задач во время 30-секундной сессии живых тиков плюс непрерывного ввода в поле фильтра."
  keyPoints={[
    "Виртуализация для ограничения фазы commit вне зависимости от размера датасета.",
    "useSyncExternalStore для store цен — предотвращает tearing при конкурентном рендеринге.",
    "Батчить WebSocket-тики в один сброс store за кадр, а не один setState за тик.",
    "Грид цен рендерится с transition-приоритетом; ввод фильтра остаётся в sync-lane.",
    "Стабильные symbol-id ключи + React.memo + стабильные props, чтобы неизменившиеся строки делали bailout.",
  ]}
/>

<Quiz
  id="sr-quiz-compiler-1"
  pieceSlug="05-react-fiber"
  lang="ru"
  question="React Compiler вставляет мемоизацию автоматически. Важно ли по-прежнему понимать useMemo и useCallback?"
  choices={[
    { label: "Да — механизм (ссылочная стабильность, открывающая bailout) — это то, на чём строится компилятор. Нужно понимать его, чтобы читать вывод компилятора, отлаживать консервативные bailout-ы и рассуждать о производительности.", correct: true },
    { label: "Нет — компилятор обрабатывает это, поэтому разработчикам больше не нужно понимать мемоизацию", misconception: "Компилятор может делать консервативный bailout, генерировать неожиданные рендеры или отключаться на сложных паттернах. Без понимания механизма невозможно отлаживать или переопределять его." },
    { label: "Только для классовых компонентов — функциональные компоненты полностью автоматизированы", misconception: "Компилятор нацелен на функциональные компоненты. Понимание bailout всё равно необходимо для отладки вывода компилятора и для кодовых баз, ещё не использующих компилятор." },
    { label: "Только если вы используете React 17 или более ранний", misconception: "Компилятор — это фича React 19+, но механизм, который он автоматизирует, фундаментален для всех версий React." },
  ]}
/>

<p class="text-ink-2 my-6">Fiber — это место, где живёт состояние компонента между рендерами: значения `useState`, рефы, список хуков — всё это висит на fiber, а не на функции компонента. Рендер за рендером, fiber сохраняется и несёт это состояние вперёд; уничтожить fiber (размонтировать, сменить тип, сменить ключ) — и состояние исчезнет. Реконсилер в основе своей — машина для принятия решений о том, какие файберы выживают, а какие перестраиваются: тот же вопрос о statefulness, о том, что сохраняется при переходе, а что пересчитывается, который проходит через каждый уровень стека. <SpiralCue thread="statefulness" lang="ru" /></p>

</Explanation>

<KeyTakeaway>Profiler показывает длительность каждого commit и причину рендера каждого fiber — «parent rendered + unchanged props» — сигнатура отсутствующего bailout. React Compiler автоматизирует мемоизацию, но опирается на тот же bailout-механизм. Связывайте Profiler с LoAF для замыкания петли продакшн-лага.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="07-profiler-and-compiler-retrieval"
  lang="ru"
  questions={[
    { q: "Трейс Profiler показывает 240 ProductCard, перерендеривающихся с причиной 'parent rendered, props unchanged'. Что это говорит и что делать?", a: "'Parent rendered + props unchanged' — сигнатура отсутствующего bailout. React.memo либо отсутствует, либо обходится нестабильной ссылкой prop. Обернуть ProductCard в React.memo. Затем проверить, нет ли инлайнового объекта или стрелочной функции среди props — мемоизировать через useMemo/useCallback. Наконец проверить, может ли родитель коллоцировать состояние ближе к тем компонентам, которые в нём нуждаются, уменьшив объём перерендеривающегося дерева." },
    { q: "Что такое Long Animation Frame (LoAF) и как он помогает диагностировать проблемы производительности React?", a: "LoAF — это запись производительности браузера для кадров, занявших более 50 мс (более мягкий порог, чем у Long Task, но покрывающий весь кадр включая рендеринг). Когда LoAF-запись атрибутирует медленный кадр React-бандлу, Profiler воспроизводит это локально и находит конкретный commit и файберы. LoAF даёт продакшн-атрибуцию; Profiler даёт локальную детализацию." },
    { q: "Какой ключевой инсайт о React Compiler должен держать в голове senior-инженер?", a: "Компилятор автоматизирует мемоизацию, но не меняет механизм. Ссылочная стабильность, открывающая bailout — всё ещё то, как React избегает лишних перерендеров, компилятор просто генерирует вызовы useMemo и useCallback, которые написал бы человек. Нужно понимать механизм, чтобы отлаживать компилятор при консервативных bailout-ах, рассуждать о производительности в кодовых базах без компилятора и валидировать вывод компилятора." },
  ]}
/>

<Recap lang="ru">
React DevTools Profiler записывает каждый commit с причинами рендера и длительностью на fiber. «Parent rendered, props unchanged» — механическая сигнатура отсутствующего bailout: добавьте `React.memo` и стабилизируйте нестабильные ссылки prop через `useMemo`/`useCallback`. «Context changed» на множестве компонентов указывает на нестабильное значение контекста. В продакшне Long Animation Frames (LoAF) атрибутируют медленные кадры React-коду — связывайте их с Profiler, чтобы назвать точный commit и файберы. React Compiler (стабильный трек в React 19) вставляет мемоизацию автоматически во время сборки, устраняя ручной разброс `useMemo`/`useCallback`. Но underlying-механизм — ссылочная стабильность, открывающая bailout — остаётся: компилятор строится на нём, и нужно понимать его для отладки консервативных решений компилятора и рассуждений о производительности в любой React-кодовой базе.
</Recap>
