---
concepts:
- fiber-object
- workInProgress-tree
- current-tree
- alternate-pointer
- interruptible-rendering
deepensInto:
- 06-bailout-and-memoisation
estMin: 12
lang: ru
lessonType: topic
level: middle
order: 2
prereqs:
- 01-reconciler-overview
slug: 02-fiber-data-structure
sources:
- https://github.com/acdlite/react-fiber-architecture
- https://github.com/facebook/react/blob/main/packages/react-reconciler/src/ReactFiberLane.js
- https://react.dev/learn/preserving-and-resetting-state
spiral:
- statefulness
status: ready
summary: Fiber — простой JS-объект, один на компонент, хранящий состояние, хуки и
  указатели дерева. Связно-списочная структура делает рендеринг прерываемым, а два
  дерева — двойная буферизация — делают прерывание безопасным.
title: Объект fiber и дерево с двойной буферизацией
track: browser
unit: 05-react-fiber
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import DiagramFrame from "~/components/diagram/DiagramFrame.astro";

<Hook>
React не может приостановить рекурсивную функцию — стек вызовов принадлежит движку, а не React. Fiber-архитектура решила это, заменив стек на структуру данных, которой React управляет сам: связное дерево объектов, в котором можно остановиться и продолжить с любого узла.
</Hook>

<Crux>Один fiber на компонент; связно-списочная форма позволяет React паузироваться после любого узла. Двойная буферизация делает экран атомарным.</Crux>

<Explanation>

**Что такое fiber на самом деле.** Fiber — обычный JavaScript-объект, один на каждый экземпляр компонента, один на каждый DOM-элемент в дереве. Каждый fiber хранит: `type` компонента, его `props`, его `key`, текущее `state` и хуки, указатели на `child`, следующий `sibling` и `return` (родитель), плюс указатель `alternate` на двойника в другом дереве. Файберы образуют связное дерево, а не массив — и именно связность является ключевым свойством.

Рекурсивный обход дерева живёт в стеке вызовов и не может быть приостановлен; обход связного списка хранит свою позицию в обычной переменной, так что React может остановиться после любого fiber, сохранить указатель и продолжить с точно того же места позже. Fiber — структура данных, сделавшая прерываемый рендеринг возможным. Название появилось до React 18 — архитектура появилась в React 16 (2017) — но только React 18 включил конкурентные функции, эксплуатирующие её.

<div data-lesson-visual class="my-6 rounded-lg border border-border bg-surface-1 p-4 font-mono text-sm overflow-x-auto">
  <div class="mb-2 text-ink-2 text-xs uppercase tracking-wider">Поля объекта fiber</div>
  <table class="w-full text-xs">
    <thead>
      <tr class="border-b border-border">
        <th class="text-left py-1 pr-4 text-ink-2">Поле</th>
        <th class="text-left py-1 text-ink-2">Что хранит</th>
      </tr>
    </thead>
    <tbody class="text-ink">
      <tr class="border-b border-border/50"><td class="py-1 pr-4 font-semibold">type</td><td class="py-1">Функция компонента или тег DOM</td></tr>
      <tr class="border-b border-border/50"><td class="py-1 pr-4 font-semibold">props</td><td class="py-1">Текущие props, переданные этому узлу</td></tr>
      <tr class="border-b border-border/50"><td class="py-1 pr-4 font-semibold">key</td><td class="py-1">Ключ реконсиляции</td></tr>
      <tr class="border-b border-border/50"><td class="py-1 pr-4 font-semibold">memoizedState</td><td class="py-1">Связный список записей хуков (useState, useEffect …)</td></tr>
      <tr class="border-b border-border/50"><td class="py-1 pr-4 font-semibold">child / sibling / return</td><td class="py-1">Указатели навигации по дереву (не стек вызовов)</td></tr>
      <tr><td class="py-1 pr-4 font-semibold">alternate</td><td class="py-1">Указатель на двойника в другом дереве</td></tr>
    </tbody>
  </table>
</div>

<DiagramFrame
  label="Родительский fiber и два его дочерних fiber, связанные тремя указателями: child ведёт от родителя к первому потомку, sibling связывает первого потомка со следующим, а return ведёт от каждого потомка обратно к родителю."
  hue="var(--d-frontend)"
  caption="Никакого стека вызовов: всё дерево достижимо через три обычных указателя. child достаёт первого потомка, sibling связывает сиблингов слева направо, return ведёт вверх к родителю. React держит позицию в одной переменной, поэтому может остановиться после любого fiber и продолжить ровно оттуда."
>
  <svg viewBox="0 0 460 280" width="460" height="280" role="presentation" style="display:block;margin:0 auto;max-width:100%;height:auto;font-family:var(--font-body);">
    <defs>
      <marker id="fiber-arrow-ru" viewBox="0 0 10 10" refX="9" refY="5" markerWidth="7" markerHeight="7" orient="auto-start-reverse">
        <path d="M0 0 L10 5 L0 10 z" fill="var(--muted)" />
      </marker>
    </defs>
    {/* child: parent -> first child */}
    <path d="M195 72 L120 168" fill="none" stroke="var(--muted)" stroke-width="1.6" marker-end="url(#fiber-arrow-ru)" />
    <text x="138" y="116" font-size="11.5" font-family="var(--font-mono)" fill="var(--ink)">child</text>
    {/* sibling: first child -> second child */}
    <path d="M186 196 L268 196" fill="none" stroke="var(--muted)" stroke-width="1.6" marker-end="url(#fiber-arrow-ru)" />
    <text x="200" y="188" font-size="11.5" font-family="var(--font-mono)" fill="var(--ink)">sibling</text>
    {/* return: first child -> parent */}
    <path d="M138 168 L210 76" fill="none" stroke="var(--hairline-strong)" stroke-width="1.4" stroke-dasharray="4 3" marker-end="url(#fiber-arrow-ru)" />
    <text x="150" y="146" font-size="11" font-family="var(--font-mono)" fill="var(--muted)">return</text>
    {/* return: second child -> parent */}
    <path d="M312 168 L242 76" fill="none" stroke="var(--hairline-strong)" stroke-width="1.4" stroke-dasharray="4 3" marker-end="url(#fiber-arrow-ru)" />
    <text x="300" y="146" font-size="11" font-family="var(--font-mono)" fill="var(--muted)">return</text>
    {/* parent fiber */}
    <rect x="150" y="24" width="160" height="48" rx="7" fill="var(--card-2)" stroke="var(--hairline-strong)" stroke-width="0.8" />
    <text x="230" y="44" text-anchor="middle" font-size="13" fill="var(--ink)">Родитель-fiber</text>
    <text x="230" y="60" text-anchor="middle" font-size="10" font-family="var(--font-mono)" fill="var(--muted)">&lt;List&gt;</text>
    {/* child 1 fiber */}
    <rect x="40" y="172" width="146" height="48" rx="7" fill="var(--card-2)" stroke="var(--hairline-strong)" stroke-width="0.8" />
    <text x="113" y="192" text-anchor="middle" font-size="13" fill="var(--ink)">Потомок-fiber 1</text>
    <text x="113" y="208" text-anchor="middle" font-size="10" font-family="var(--font-mono)" fill="var(--muted)">&lt;Item&gt;</text>
    {/* child 2 fiber */}
    <rect x="268" y="172" width="146" height="48" rx="7" fill="var(--card-2)" stroke="var(--hairline-strong)" stroke-width="0.8" />
    <text x="341" y="192" text-anchor="middle" font-size="13" fill="var(--ink)">Потомок-fiber 2</text>
    <text x="341" y="208" text-anchor="middle" font-size="10" font-family="var(--font-mono)" fill="var(--muted)">&lt;Item&gt;</text>
  </svg>
</DiagramFrame>

**Два дерева: current и workInProgress.** React всегда держит два fiber-дерева. Дерево *current* — то, чьё состояние совпадает с тем, что сейчас нарисовано на экране. Когда начинается обновление, React строит дерево *workInProgress* — клонируя файберы из current по мере прохода, применяя новые props и state. Указатель `alternate` каждого fiber связывает его с двойником в другом дереве, поэтому клонирование дёшево: неизменившиеся поддеревья переиспользуются по ссылке.

Когда дерево workInProgress завершено и закоммичено, два дерева меняются местами: workInProgress становится current, а старое current хранится, чтобы стать следующим workInProgress. Это двойная буферизация. Она гарантирует, что дерево на экране никогда не наблюдается в полуобновлённом состоянии — и именно поэтому рендер, прерванный и выброшенный, ничего не стоит: выброшенное дерево workInProgress просто никогда не коммитится.

<Inset kind="why" lang="ru">
**Почему fiber, а не рекурсия?** До React 16 реконсиляция была обычной рекурсивной функцией. Рекурсия живёт в стеке вызовов JS-движка, который нельзя приостановить и возобновить — функция либо выполняется до конца, либо не выполняется вовсе. Это означало, что весь рендер большого дерева был одной неделимой синхронной задачей. Fiber заменил рекурсию явным обходом связного списка: «стек вызовов» стал структурой данных в куче, которую React владеет и может остановить, сохранить и продолжить. Название отсылает к концепции «fiber» из системного программирования — пользовательского облегчённого потока, — потому что fiber-дерево по сути является вручную реализованным прерываемым стеком вызовов поверх обычного JS.
</Inset>

**Stack-реконсилер: чем fiber лучше предшественника.** До React 16 реконсилер был написан как стек — один большой синхронный обход. Команда React называла его «stack reconciler». Его фатальный изъян: рендер большого приложения мог занимать 60–100 мс, и всё это время главный поток был заблокирован. Нажатия клавиш, анимации, скролл — всё ждало завершения React. Fiber-реконсилер сохраняет прогресс в явном heap-объекте, что позволяет прерваться после любого fiber и уступить браузеру. Это структурное изменение, а не оптимизация: рекурсию нельзя «ускорить» до прерываемости — нужна другая структура данных.

<Quiz
  id="mid-quiz-fiber-1"
  pieceSlug="05-react-fiber"
  lang="ru"
  question="Почему реконсиляция со связным fiber-деревом прерываема, а рекурсивный подход — нет?"
  choices={[
    { label: "Обход связного списка держит позицию в переменной, которой управляет React; стек вызовов принадлежит JS-движку и не может быть приостановлен на полпути", correct: true },
    { label: "Связные списки быстрее рекурсии для больших деревьев", misconception: "Скорость — не главное. Ключевое свойство — возможность паузы: JS-движок владеет стеком; React владеет cursor-переменной fiber." },
    { label: "React 18 отключает рекурсию", misconception: "React 18 всё ещё использует рекурсию на некоторых внутренних путях. Изменение, позволяющее прерывание, — структурное: fiber вместо стека." },
    { label: "Указатель alternate позволяет React пропускать узлы дерева", misconception: "Указатель alternate связывает двойников в двух деревьях для клонирования и bailout — это не механизм пропуска." },
  ]}
/>

<Quiz
  id="mid-quiz-fiber-2"
  pieceSlug="05-react-fiber"
  lang="ru"
  question="Рендер наполовину завершён, когда приходит более приоритетное обновление. React выбрасывает незавершённое дерево workInProgress и начинает заново. Почему это ничего не стоит навсегда?"
  choices={[
    { label: "Дерево workInProgress никогда не коммитилось — дерево current на экране не тронуто, поэтому выбрасывание workInProgress ничего не откатывает видимым образом", correct: true },
    { label: "React делает checkpoint дерева workInProgress в localStorage перед выбрасыванием", misconception: "Никакого механизма checkpoint нет. Стоимость нулевая, потому что ничего не коммитилось — дерево current на экране осталось прежним." },
    { label: "Указатель alternate позволяет React дёшево переиграть мутации", misconception: "Указатель alternate нужен для клонирования, а не переигровки. Выброс дёшев, потому что мутаций DOM ещё не было." },
    { label: "React сохраняет полупостроенное дерево для повторного использования узлов", misconception: "Выброшенные деревья не переиспользуются напрямую. React перестраивает из current при следующем рендере. Клонирование из current уже дёшево через alternate-указатели." },
  ]}
/>

<DragOrder
  id="mid-drag-fiber-1"
  pieceSlug="05-react-fiber"
  lang="ru"
  prompt="Расставьте шаги одного цикла рендеринга React по порядку — от изменения состояния до смены деревьев."
  items={[
    { id: "schedule", label: "Приходит обновление — React планирует работу на соответствующем lane" },
    { id: "clone", label: "Начинается обход fiber — клонирование файберов из current в workInProgress" },
    { id: "run", label: "Запуск функций компонентов, реконсиляция нового дерева элементов" },
    { id: "commit", label: "Commit: применение мутаций DOM из workInProgress" },
    { id: "swap", label: "Swap: workInProgress становится current; старый current хранится для следующего цикла" },
  ]}
  correctOrder={["schedule", "clone", "run", "commit", "swap"]}
/>

</Explanation>

<KeyTakeaway>Fiber — JS-объект с указателями child/sibling/return, образующими связное дерево, которым управляет React — именно поэтому рендеры прерываемы. Двойная буферизация (current + workInProgress) гарантирует, что дерево на экране никогда не бывает полуобновлённым, а выброшенные рендеры ничего не стоят.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="02-fiber-data-structure-retrieval"
  lang="ru"
  questions={[
    { q: "Какие поля объекта fiber обеспечивают обход связного дерева?", a: "child (первый дочерний fiber), sibling (следующий сиблинг в родителе) и return (родительский fiber). Вместе они образуют связное дерево, которое React обходит без стека вызовов." },
    { q: "На что указывает поле alternate у fiber?", a: "На двойника в другом дереве. Указатель alternate у current-fiber ведёт на его аналог в workInProgress, и наоборот. Это делает клонирование дешёвым — неизменившиеся поддеревья переиспользуются по ссылке." },
    { q: "Почему выброшенное дерево workInProgress можно выбросить с нулевыми видимыми последствиями?", a: "Потому что дерево workInProgress никогда не коммитилось. Дерево current, совпадающее с экраном, не тронуто. Выброс workInProgress означает просто, что мутаций DOM не произошло — нечего откатывать." },
  ]}
/>

<Recap lang="ru">
Fiber — обычный JS-объект, хранящий type, props, key, state и хуки компонента, плюс указатели child/sibling/return, образующие связное дерево. В отличие от рекурсивного стека вызовов, это связное дерево можно остановить после любого узла, сохранив cursor-переменную, — что и является структурной основой прерываемого рендеринга. React всегда держит два таких дерева: current (совпадающее с экраном) и workInProgress (строящееся обновление). Указатель alternate каждого fiber связывает его с двойником в другом дереве, делая клонирование дешёвым. При коммите деревья меняются местами; при отмене рендера дерево workInProgress выбрасывается с нулевой стоимостью — оно никогда не трогало DOM. Теперь, когда компонент необъяснимо теряет состояние или рендер вдруг начинается заново, вы знаете, где копать: что-то уничтожило fiber, или обновление с более высоким приоритетом выбросило незавершённое дерево.
</Recap>
