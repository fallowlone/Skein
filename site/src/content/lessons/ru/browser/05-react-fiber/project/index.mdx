---
concepts:
- react-profiler
- bailout-memoisation
- time-slicing
- virtualization
- tearing-prevention
estMin: 240
lang: ru
level: senior
order: 10
prereqs: []
slug: project
sources:
- https://react.dev/learn/react-developer-tools
- https://react.dev/reference/react/useTransition
- https://react.dev/reference/react/useSyncExternalStore
status: ready
summary: "Практический проект — соберите дёргающуюся живую сетку данных, диагностируйте шторм перерендеров в Profiler и примените лестницу фиксов fiber, пока INP не удержится под 200 мс, доказывая каждый шаг цифрами до/после."
title: 'React fiber: усмирить шторм перерендеров'
track: browser
unit: 05-react-fiber
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import ProjectBrief from "~/components/pedagogy/ProjectBrief.astro";

<Hook>
Читать про bailout и lanes — не то же самое, что вытащить реальный UI из шторма перерендеров. Соберите живую интерактивную сетку данных, доведите её до jank, затем примените лестницу фиксов модуля — мемоизация, collocation, time-slicing, виртуализация, защита от tearing — пока цифры не вернутся, с доказательствами из Profiler на каждом шаге.
</Hook>

<Goal lang="ru">
Превратите ментальную модель модуля в воспроизводимый инженерный цикл: инструментируйте React Profiler, читайте причину перерендера каждого fiber, чините на нужном уровне (референциальная стабильность, затем collocation состояния, затем приоритет, затем виртуализация), защититесь от tearing и проверьте фикс цифрами INP и времени commit до/после.
</Goal>

<ProjectBrief
  id="05-react-fiber-project"
  lessonSlug="05-react-fiber"
  lang="ru"
  objective="Возьмите намеренно дёргающуюся живую сетку данных (свою или по спецификации ниже) и доведите её INP по нажатиям до значения под 200 мс, а время commit — под один кадр, не переписывая на другом фреймворке, доказывая каждый шаг измерениями из React Profiler и панели Performance."
  requirements={[
    "Соберите на React 18+ сетку минимум из 2 000 строк, питаемую симулированным живым стором, который тикает ~10 раз/секунду (setInterval, мутирующий внешний объект, подойдёт). Добавьте поле поиска, фильтрующее сетку, и контрол сортировки, переупорядочивающий её. Это стенд воспроизведения.",
    "Засейте jank намеренно: держите состояние поискового запроса наверху дерева, передавайте inline-стрелку-обработчик и inline-объект стиля в каждую строку, ключуйте строки по индексу массива и читайте живой стор простой переменной модуля (не useSyncExternalStore). Убедитесь, что ввод в поле поиска заметно дёргается.",
    "Инструментируйте React DevTools Profiler: запишите сессию ввода плюс живые тики, найдите самый длинный commit, отсортируйте fiber по self-time и зафиксируйте причину 'why did this render' для медленных fiber. Зафиксируйте базовый INP по нажатиям из панели Performance (или из атрибуции INP web-vitals).",
    "Почините референциальную стабильность: оберните строки в React.memo и уберите inline-стрелку/стиль-пропсы, чтобы пропсы строки были Object.is-стабильны (useCallback для обработчиков, вынос или useMemo для стиля). Перепрофилируйте и покажите, что строки теперь делают bailout при неизменных данных.",
    "Почините идентичность и размещение состояния: ключуйте строки по стабильному id, чтобы сортировка сохраняла неуправляемое состояние строк, и сделайте collocation состояния запроса, чтобы нажатие больше не перерендеривало всё дерево. Задокументируйте число перерендеров до/после.",
    "Почините приоритет и размер commit: оберните обновление отфильтрованной/отсортированной сетки в startTransition (или useDeferredValue), чтобы оно уступало нажатиям, и виртуализируйте сетку, чтобы монтировались только ~30 видимых строк — ограничив непрерываемую commit-фазу независимо от размера датасета.",
    "Почините tearing: маршрутизируйте живой стор через useSyncExternalStore и батчите входящие тики в один сброс стора на кадр анимации, чтобы concurrent-рендеры видели один согласованный снапшот, а сетка не перерендеривалась раз на тик на символ.",
  ]}
  acceptance={[
    "Таблица до/после: INP по нажатиям (p75), самое длинное время commit и число перерендеров на нажатие — всё измерено под той же нагрузкой живых тиков, не оценено.",
    "Трейсы Profiler до и после: базовый показывает широкий шторм 'parent rendered, props unchanged'; исправленный трейс показывает рендер только реально изменившихся видимых строк, с commit под ~8 мс.",
    "INP по нажатиям держится под 200 мс p75 при непрерывном вводе, пока стор тикает ~10/секунду, и ни один Long Animation Frame не атрибутируется сетке за 30-секундную сессию.",
    "Абзац-описание, называющий, какой рычаг исправил каждый симптом (референциальная стабильность vs collocation состояния vs приоритет vs виртуализация vs tearing) и почему этот рычаг занял своё место.",
  ]}
  stretch={[
    "Включите React Compiler и перепрофилируйте: убедитесь, что он вставляет мемоизацию, которую вы написали вручную, отметьте компонент, где он делает bailout консервативно, и объясните, почему понимание механизма bailout позволило вам прочитать его вывод.",
    "Добавьте намеренный побочный эффект в render-фазе (вызов аналитики в теле компонента), запустите под StrictMode и покажите двойной учёт; затем перенесите в useEffect и покажите исправленный счёт — задокументировав, почему render должна быть чистой.",
    "Добавьте useLayoutEffect, измеряющий строку через getBoundingClientRect и меняющий стиль, затем покажите в Profiler, как он удлиняет commit (forced reflow); сравните с той же работой в useEffect после paint.",
    "Напишите однострочный on-call runbook: шаги триажа в Profiler, сигнатуру 'parent rendered + unchanged props', лестницу фиксов по порядку и чек-лист верификации, связывающий каждый фикс с измеримой дельтой Profiler/INP.",
  ]}
/>

<Recap lang="ru">
Это цикл, который вы запустите в каждом реальном инциденте производительности React: сначала инструментируйте Profiler, читайте причину перерендера по каждому fiber, затем чините на нужном уровне — референциальная стабильность до collocation состояния до приоритета до виртуализации — защититесь от tearing через useSyncExternalStore и проверьте цифрами INP и commit до/после под одинаковой нагрузкой. Один раз сделав это на игрушечной сетке, вы доводите продакшен-версию до мышечной памяти и превращаете вывод React Compiler в то, что можно читать, а не слепо доверять.
</Recap>
