---
concepts:
- render-vs-commit
- interruptible-rendering
- reconciliation-keys
- bailout-memoisation
- lanes-time-slicing
- tearing
estMin: 13
lang: ru
level: senior
order: 7
prereqs: []
slug: quiz-choice
sources:
- https://react.dev/learn/render-and-commit
- https://github.com/acdlite/react-fiber-architecture
- https://react.dev/reference/react/useTransition
status: ready
summary: "Синтез всего модуля fiber в формате вопросов с выбором — render vs commit, дерево с двойной буферизацией, ключи реконсиляции, lanes и time-slicing, bailout и tearing."
title: 'React fiber: тест с выбором ответа'
track: browser
unit: 05-react-fiber
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Шесть вопросов, проходящих через весь модуль. Ни один из них — не определение для заучивания: каждый отражает решение, которое вы принимаете в реальном инциденте производительности, где render-фаза, commit-фаза, ключи, lanes и bailout взаимодействуют между собой.
</Hook>

<Goal lang="ru">
Убедитесь, что умеете связать две фазы реконсилера, двойную буферизацию fiber, идентичность при reconciliation, планировщик lanes и механизм bailout — тот синтез, к которому вели отдельные уроки.
</Goal>

<Quiz
  id="05-react-fiber-choice-1"
  lessonSlug="05-react-fiber"
  lang="ru"
  question="Компонент инкрементирует счётчик уровня модуля прямо в теле функции. В React 17 это работало; в React 18 StrictMode счётчик вдвое больше ожидаемого, а в продакшене под нагрузкой иногда утраивается. В чём корневая причина?"
  choices={[
    { label: "Render-фаза не чистая. React 18 может переиграть, приостановить или отбросить рендер, поэтому побочный эффект в теле функции выполняется непредсказуемое число раз; StrictMode двойным вызовом вскрывает именно это. Перенесите мутацию в useEffect или в обработчик события.", correct: true },
    { label: "Состояние гонки между двумя потоками, инкрементирующими счётчик.", misconception: "Реконсилер React работает в единственном главном потоке — второго потока, участвующего в гонке, нет. Недетерминизм происходит из переигрываемости render-фазы, а не из конкурентности между потоками." },
    { label: "StrictMode содержит баг, и его надо убрать в продакшене.", misconception: "StrictMode работает только в разработке и намеренно вызывает компонент дважды, чтобы вскрыть нечистый рендер. Удаление скрывает симптом; нечистый побочный эффект всё равно неверен при concurrent rendering в продакшене." },
    { label: "Счётчик надо обернуть в useState, чтобы он стал безопасным.", misconception: "useState не исправит побочный эффект, срабатывающий во время рендера; исправление — вообще перестать выполнять эффект в теле функции. Render должна быть чистой независимо от того, где хранится значение." },
  ]}
/>

<Quiz
  id="05-react-fiber-choice-2"
  lessonSlug="05-react-fiber"
  lang="ru"
  question="Рендер идёт уже 30 мс, когда приходит более приоритетное нажатие клавиши. React отбрасывает незавершённое дерево workInProgress и начинает заново. Почему отбрасывание полупостроенного дерева ничего не стоит визуально?"
  choices={[
    { label: "Дерево workInProgress никогда не коммитилось, поэтому current-дерево — то, что совпадает с экраном — не тронуто. Мутаций DOM не было, значит откатывать нечего. Именно двойная буферизация делает отброшенный рендер бесплатным.", correct: true },
    { label: "React делает чекпойнт частичного дерева в память и откатывает его транзакционно.", misconception: "Никакой транзакции отката нет. Стоимость нулевая, потому что ничего не было применено — DOM трогает commit, а не render, и commit не начинался." },
    { label: "Указатель alternate позволяет React дёшево переиграть и отменить мутации.", misconception: "Указатель alternate связывает каждый fiber с его близнецом для дешёвого клонирования и bailout — это не журнал отмены. Отменять нечего, потому что render-фаза не производит изменений DOM." },
    { label: "Это всё-таки стоит — пользователь на миг видит наполовину отрисованный кадр.", misconception: "Пользователь никогда не видит workInProgress. Рисуется только current-дерево, и оно остаётся согласованным, потому что отброшенное дерево живёт исключительно в памяти." },
  ]}
/>

<Quiz
  id="05-react-fiber-choice-3"
  lessonSlug="05-react-fiber"
  lang="ru"
  question="Список рендерится с key={index}. Чекбокс, отмеченный в третьей строке, остаётся отмеченным на неправильной строке после удаления первой строки, но текстовые подписи при этом все корректны. Чем объясняется такое расщепление поведения?"
  choices={[
    { label: "Reconciliation сопоставляет детей с fiber по ключу. С индексными ключами ключ — это позиция, поэтому при удалении каждый fiber связывается с данными следующего элемента. Управляемые пропсы (текст подписи) передаются заново каждый рендер и выглядят верно; неуправляемое DOM-состояние (отмеченность) живёт на fiber и следует за неправильным элементом.", correct: true },
    { label: "React потерял состояние чекбокса при удалении и восстановил устаревшее значение.", misconception: "Состояние не было потеряно — оно сохранилось на неправильном fiber. Fiber остались на местах; сместились только данные, с которыми они спарены, потому что индексные ключи позиционны." },
    { label: "Commit-фаза применила мутации не в том порядке.", misconception: "Порядок commit корректен и детерминирован. Рассогласование произошло раньше, в reconciliation, где индексные ключи заставили React спарить fiber с неправильными элементами." },
    { label: "Чекбоксы всегда сбрасываются, когда из списка удаляют любой соседний элемент.", misconception: "Со стабильными ключами-идентичностями чекбокс корректно отслеживает свой элемент при удалениях. Баг специфичен для индекса-как-ключа, а не общее свойство чекбоксов." },
  ]}
/>

<Quiz
  id="05-react-fiber-choice-4"
  lessonSlug="05-react-fiber"
  lang="ru"
  question="Поле поиска фильтрует список из 5 000 строк. Ввод дёргается. Вы оборачиваете setState отфильтрованного списка в startTransition. Ввод теперь мгновенный — но почему сам список не замораживает страницу, когда наконец рендерится?"
  choices={[
    { label: "Transition-обновление выполняется в transition-lane: разбито time-slicing на куски ~5 мс с проверкой shouldYield между ними и прерываемо любым нажатием клавиши. Рендер размазан по кадрам, а не одна длинная блокирующая задача, поэтому ввод продолжает идти, пока список догоняет.", correct: true },
    { label: "startTransition переносит рендер списка в фоновый поток.", misconception: "Фонового потока нет — React рендерит в главном потоке. startTransition понижает приоритет обновления, делая его прерываемым и разбитым time-slicing, а не распараллеленным." },
    { label: "startTransition делает debounce фильтра, чтобы он запускался реже.", misconception: "startTransition не делает debounce; фильтр всё равно выполняется. Он меняет приоритетную lane обновления, чтобы рендер уступал срочному вводу, а не уменьшает частоту срабатываний." },
    { label: "Рендер списка теперь происходит в commit-фазе, которая быстрее.", misconception: "Time-slicing применяется к render-фазе, а не к commit. Commit всегда атомарна и непрерываема; прерываемой startTransition делает именно работу рендера, а не commit." },
  ]}
/>

<Quiz
  id="05-react-fiber-choice-5"
  lessonSlug="05-react-fiber"
  lang="ru"
  question="Дочерний компонент, обёрнутый в React.memo, перерендеривается при каждом рендере родителя. Причина в Profiler — 'props changed', но содержимое пропа каждый раз идентично. Что происходит?"
  choices={[
    { label: "Проп — это inline-объект, массив или стрелочная функция, созданные в JSX родителя: свежая ссылка каждый рендер. React.memo сравнивает пропсы через Object.is, а новый литерал никогда не равен предыдущему по Object.is, поэтому bailout не срабатывает. Стабилизируйте ссылку через useMemo/useCallback.", correct: true },
    { label: "React.memo делает только глубокое сравнение на равенство, и оно проваливается на вложенных данных.", misconception: "Сравнение по умолчанию в React.memo — поверхностное Object.is по каждому пропу, не глубокое. Он сообщает о реальном различии ссылок: проп — новый объект каждый рендер, хотя содержимое совпадает." },
    { label: "Хуки внутри дочернего компонента отключают React.memo.", misconception: "Хуки не отключают memo. memo решает, произойдёт ли вообще вход в render-фазу; как только рендер пошёл, хуки компонента выполняются обычным образом. Срыв вызван нестабильной ссылкой пропа." },
    { label: "React.memo вырезается в сборках для разработки.", misconception: "React.memo активен в разработке. Перерендер реален и вызван неравенством ссылок пропа, а не отключением memo." },
  ]}
/>

<Quiz
  id="05-react-fiber-choice-6"
  lessonSlug="05-react-fiber"
  lang="ru"
  question="Дашборд управляется внешним стором. После включения concurrent-фич вы иногда видите, что хедер показывает одну сумму, а соседняя панель — другую сумму в том же кадре. Какой механизм виноват и что это исправляет?"
  choices={[
    { label: "Tearing: прерываемый рендер идёт слайсами, и внешний стор мутировал между двумя слайсами, поэтому ранние компоненты прочитали старое значение, а поздние — новое в одном commit. Маршрутизация стора через useSyncExternalStore позволяет React заметить изменение в середине рендера и перезапуститься, гарантируя один согласованный снапшот на commit.", correct: true },
    { label: "Устаревшее замыкание в useEffect захватило старое значение стора.", misconception: "Устаревшее замыкание даёт стабильно старое значение, а не два разных значения в одном кадре. Признак tearing — внутренняя несогласованность в пределах одного commit, вызванная мутацией стора между слайсами рендера." },
    { label: "Стор надо обернуть в useMemo, чтобы стабилизировать его ссылку.", misconception: "useMemo кэширует вычисленное значение внутри одного компонента; он ничего не делает с внешним стором, меняющимся в середине рендера по всему дереву. Безопасная для конкурентности точка интеграции — useSyncExternalStore." },
    { label: "Собственный useState React делает tearing, и его надо заменить.", misconception: "Внутреннее состояние React не может tearing, потому что React контролирует точный момент его изменения. Tearing возникает только с внешними сторами, которые React не контролирует — именно для этого и существует useSyncExternalStore." },
  ]}
/>

<Recap lang="ru">
Сквозная линия модуля — одна машина, увиденная с шести сторон: render вычисляет диф (прерываема, должна быть чистой), а commit его применяет (атомарна); двойная буферизация fiber делает прерывание бесплатным; ключи reconciliation привязывают состояние к идентичности, а не к позиции; lanes и time-slicing держат срочный ввод впереди дорогой работы; bailout — управляемый референциальной стабильностью через Object.is — держит большие деревья дешёвыми; а useSyncExternalStore не даёт внешним сторам делать tearing при concurrent rendering. Каждый продакшен-симптом здесь сводится к одному из этих шести рычагов.
</Recap>
