---
slug: 01-reconciler-overview
lang: ru
track: browser
unit: 05-react-fiber
order: 1
title: "Что делает реконсилер: render vs commit"
summary: "React делит каждое обновление на прерываемую фазу render, вычисляющую дифф, и атомарную фазу commit, применяющую мутации DOM — именно в зазоре между ними живут и выигрыши производительности, и баги."
estMin: 10
status: ready
lessonType: topic
level: junior
prereqs: []
deepensInto: ["02-fiber-data-structure", "03-render-and-commit-phases"]
spiral: ["statefulness"]
concepts: ["reconciler", "render-phase", "commit-phase", "double-buffering", "time-slicing"]
sources:
  - https://react.dev/learn/render-and-commit
  - https://github.com/acdlite/react-fiber-architecture
  - https://web.dev/articles/inp
---

import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import MetaphorComplete from "~/components/pedagogy/MetaphorComplete.astro";
import NumberDrill from "~/components/pedagogy/NumberDrill.astro";
import NumbersCard from "~/components/layout/NumbersCard.astro";
import PersonaTag from "~/components/pedagogy/PersonaTag.astro";

<Hook>
Вы вызываете `setState`. Где-то между этим вызовом и сменой пикселя на экране React решил, какие компоненты перезапустить, сравнил два дерева, вычислил минимальный набор мутаций DOM и применил их — возможно, остановившись на полпути, чтобы пропустить нажатие клавиши. Машина, которая это делает, — fiber-реконсилер.
</Hook>

<Crux>Дерево компонентов становится мутациями DOM: render вычисляет дифф (прерываемо), commit применяет его (атомарно).</Crux>

<Explanation>

**Что делает реконсилер.** Когда ваши данные меняются, React вычисляет наименьший набор изменений страницы, совпадающий с новыми данными, и применяет только их. DOM медленно мутировать — тронуть его тысячу раз намного медленнее, чем тронуть десять. Вся работа React — вычислить десять реальных изменений вместо слепого повтора тысячи.

**Метафора чертежа.** Наивный строитель снёс бы дом и отстроил с нуля при каждом изменении чертежа. React — умный строитель: он держит *текущий* чертёж приколотым к стене, рисует *новый* на втором листе, кладёт два рядом и отмечает только различия. Потом он шлёт бригаде один наряд работ ровно с этими изменениями. Рисование второго чертежа — фаза render; передача наряда бригаде — фаза commit.

**Почему две фазы.** React делит каждое обновление на две фазы. **Фаза render** обходит дерево компонентов, запускает функциональные компоненты и строит новое дерево узлов *fiber* — эта фаза чистая, не производит видимых изменений и с React 18 может быть приостановлена, возобновлена или выброшена. **Фаза commit** берёт готовое новое дерево и применяет мутации DOM одним синхронным, непрерываемым взрывом. Всё хорошее и всё опасное в производительности React живёт в зазоре между «прерываемым render» и «атомарным commit».

<div data-lesson-visual class="my-6 rounded-lg border border-border bg-surface-1 p-4 font-mono text-sm">
  <div class="mb-2 text-ink-2 text-xs uppercase tracking-wider">setState → экран</div>
  <div class="flex flex-col gap-2">
    <div class="rounded bg-amber-50 dark:bg-amber-950/30 border border-amber-200 dark:border-amber-800 px-3 py-2">
      <span class="font-semibold text-amber-800 dark:text-amber-300">ФАЗА RENDER</span>
      <span class="ml-2 text-ink-2">Перезапуск компонентов · построение fiber-дерева · дифф</span>
      <span class="ml-2 text-xs text-green-700 dark:text-green-400">← прерываема</span>
    </div>
    <div class="rounded bg-blue-50 dark:bg-blue-950/30 border border-blue-200 dark:border-blue-800 px-3 py-2">
      <span class="font-semibold text-blue-800 dark:text-blue-300">ФАЗА COMMIT</span>
      <span class="ml-2 text-ink-2">Применение мутаций DOM · запуск эффектов · отрисовка</span>
      <span class="ml-2 text-xs text-red-700 dark:text-red-400">← атомарна, не прерывается</span>
    </div>
  </div>
</div>

<PersonaTag id="bea" lang="ru" /> печатает в поле поиска, фильтрующем длинный список. <PersonaTag id="sven" lang="ru" /> комментирует: «Каждое нажатие — это изменение состояния. React перезапускает компоненты, строит новое fiber-дерево, диффит его против текущего. Список из 800 строк изменился — но React находит только 12 строк, которые реально отличаются, и мутирует только эти 12 DOM-узлов. Остальные 788 не тронуты. Поэтому поле всё ещё ощущается мгновенным».

<DragOrder
  id="jr-drag-1"
  pieceSlug="05-react-fiber"
  lang="ru"
  prompt="Вызов setState проходит через React к экрану. Расставьте шаги по порядку."
  items={[
    { id: "setstate", label: "Вызван setState — React планирует обновление" },
    { id: "render", label: "Фаза render: перезапуск компонентов, построение нового fiber-дерева" },
    { id: "diff", label: "Реконсиляция: дифф нового дерева против текущего" },
    { id: "commit", label: "Фаза commit: применение мутаций DOM одним взрывом" },
    { id: "paint", label: "Браузер рисует изменённые пиксели" },
  ]}
  correctOrder={["setstate", "render", "diff", "commit", "paint"]}
/>

<Quiz
  id="jr-quiz-1"
  pieceSlug="05-react-fiber"
  lang="ru"
  question="Почему React диффит два дерева вместо того, чтобы просто переписать весь DOM?"
  choices={[
    { label: "Мутировать DOM дорого — дифф находит минимальный набор изменений, поэтому трогается только то, что реально изменилось", correct: true },
    { label: "Дифф требуется языком JavaScript", misconception: "Ничто в JS этого не требует. Это дизайн-решение React, принятое именно потому, что слепое переписывание DOM медленно." },
    { label: "Браузер не может переписать весь DOM сразу", misconception: "Браузер может переписать весь DOM — это просто медленно. React диффит, чтобы избежать этой медленности, не потому что это невозможно." },
    { label: "Дифф уменьшает JS-бандл", misconception: "Реконсилер — это код, добавляющий в бандл. Его выгода — скорость рантайма, не размер бандла." },
  ]}
/>

<Quiz
  id="jr-quiz-2"
  pieceSlug="05-react-fiber"
  lang="ru"
  question="Какую фазу React может приостановить на полпути?"
  choices={[
    { label: "Фазу render — построение и дифф дерева не производят видимых изменений, поэтому прерывать безопасно", correct: true },
    { label: "Фазу commit", misconception: "Commit применяет мутации DOM и должен быть атомарным — пауза оставила бы страницу в полуобновлённом, несогласованном состоянии." },
    { label: "Обе фазы одинаково", misconception: "Только render прерываем. Commit всегда один синхронный, непрерываемый взрыв." },
    { label: "Ни одну — React всегда работает от начала до конца", misconception: "С React 18 фаза render прерываема. Это ядро конкурентного рендеринга." },
  ]}
/>

<MetaphorComplete
  id="jr-metaphor-1"
  pieceSlug="05-react-fiber"
  lang="ru"
  setup="В метафоре строителя React держит текущий чертёж приколотым к стене и рисует новый на втором листе, чтобы их сравнить. Удержание двух версий дерева сразу — одной показанной, одной строящейся — называется как?"
  accepted={["double buffering", "double-buffering", "двойная буферизация", "два дерева", "current и workInProgress"]}
  canonical="двойная буферизация"
  explanation="Двойная буферизация означает, что React держит два fiber-дерева: дерево 'current', совпадающее с тем, что на экране, и дерево 'workInProgress', которое он строит для следующего обновления. Он работает с копией workInProgress, не трогая current, поэтому полупостроенное обновление никогда не видно. Когда дерево workInProgress готово и закоммичено, два меняются ролями — новое становится current. Это та же техника, что игры используют, чтобы не показать полунарисованный кадр."
/>

<NumberDrill
  id="jr-drill-1"
  pieceSlug="05-react-fiber"
  lang="ru"
  question="Планировщик React делает render-работу маленькими кусками, потом уступает браузеру. Примерно сколько миллисекунд один кусок, прежде чем React проверяет, не пора ли уступить?"
  expected={5}
  tolerance={1}
  unit="мс"
  explanation="React работает срезами примерно по 5 мс, потом спрашивает «уступить?». Если у браузера есть ввод или рендеринг, React паузится и возобновляется позже. 5 мс достаточно мало, что даже несколько подряд помещаются в один кадр 16.67 мс рядом с работой самого браузера — поэтому ввод остаётся отзывчивым даже во время большого рендера."
/>

<NumbersCard
  id="card-fiber-numbers"
  title="Числа реконсилера React"
  stats={[
    { label: "Срез до shouldYield()", value: "~5 мс" },
    { label: "Сложность реконсиляции", value: "O(n) (эвристика)" },
    { label: "Сложность наивного дифф-дерева", value: "O(n³)" },
    { label: "Ширина битовой маски lane", value: "31 lane" },
    { label: "Фаза commit", value: "Атомарна, не sliceable" },
    { label: "Сравнение bailout", value: "Object.is на props" },
  ]}
/>

</Explanation>

<KeyTakeaway>Render прерываем и чист; commit атомарен и синхронен. React держит два дерева одновременно, чтобы полупостроенное обновление никогда не было видно. Каждые 5 мс планировщик спрашивает, не уступить ли браузеру.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="01-reconciler-overview-retrieval"
  lang="ru"
  questions={[
    { q: "Почему фазу render можно прервать, а фазу commit — нет?", a: "Render не производит видимых изменений — он только строит дерево в памяти. Если прервать, ничего на экране не меняется, и React может возобновить или выбросить безопасно. Commit применяет мутации DOM; пауза посреди commit оставила бы страницу в полуобновлённом, несогласованном состоянии, видимом пользователю." },
    { q: "Что такое двойная буферизация в React?", a: "React держит два fiber-дерева: дерево 'current', совпадающее с тем, что на экране, и дерево 'workInProgress', строящееся для следующего обновления. Он модифицирует workInProgress, не трогая current, поэтому дерево на экране никогда не бывает в частичном состоянии. При commit деревья меняются ролями." },
    { q: "Примерно сколько длится один render-срез React, прежде чем он проверяет shouldYield()?", a: "Около 5 мс. После этого среза планировщик проверяет, есть ли у браузера срочная работа (ввод, рендеринг), и при необходимости паузится, возобновляясь через задачу MessageChannel." },
  ]}
/>

<Recap lang="ru">
Реконсилер React делит каждое обновление на фазу render (прерываема, чистая, строит новое fiber-дерево в памяти) и фазу commit (атомарна, синхронна, применяет мутации DOM). Фаза render может быть приостановлена и возобновлена, потому что не производит видимых изменений; фаза commit не может, потому что мутирует DOM и должна оставить страницу в согласованном состоянии. React использует двойную буферизацию — два дерева одновременно — чтобы обновление в процессе никогда не было видно. Планировщик нарезает render-работу на ~5 мс кусков и уступает браузеру между ними, сохраняя отзывчивость страницы даже при большом перерендеривании.
</Recap>
