---
concepts:
- render-vs-commit
- fiber-double-buffer
- reconciliation-keys
- lanes-time-slicing
- bailout-mechanism
- tearing
estMin: 14
lang: ru
level: senior
order: 8
prereqs: []
slug: quiz-short
sources:
- https://react.dev/learn/render-and-commit
- https://github.com/acdlite/react-fiber-architecture
- https://react.dev/reference/react/useSyncExternalStore
status: ready
summary: "Промпты на свободное припоминание по всему модулю fiber. Сначала ответьте своими словами, затем раскройте эталонный ответ и сравните."
title: 'React fiber: свободное припоминание'
track: browser
unit: 05-react-fiber
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";

<Hook>
Припоминание сильнее перечитывания. Для каждого промпта проговорите или запишите полный ответ по памяти, прежде чем открыть эталон — именно усилие припоминания закрепляет механизм.
</Hook>

<Goal lang="ru">
Восстановите ключевую машину модуля — разделение render/commit, двойную буферизацию fiber, идентичность reconciliation, планировщик lanes, bailout и tearing — не подглядывая в уроки.
</Goal>

<RetrievalDrawer
  client:visible
  id="05-react-fiber-short-retrieval"
  lang="ru"
  questions={[
    { q: "Почему render-фазу можно прервать, а commit-фазу — нет?", a: "Render-фаза обходит дерево fiber, выполняет ваши компоненты и строит дерево workInProgress в памяти — она не производит видимых изменений, поэтому React может приостановить её после любого fiber (сохранив курсор), возобновить позже или вовсе отбросить с нулевой видимой стоимостью. Commit-фаза применяет очередь мутаций DOM; пауза посреди commit оставила бы страницу в наполовину обновлённом, внутренне несогласованном состоянии, которое пользователь мог бы увидеть. Поэтому commit выполняется как один синхронный непрерываемый рывок. Это разделение — прерываемая render, атомарная commit — хребет всего модуля: time-slicing и transitions используют прерываемость render, а удержание commit маленькой (виртуализация, меньше мутаций) — рычаг производительности commit-фазы." },
    { q: "Что такое двойная буферизация fiber и почему она делает отброшенный рендер бесплатным?", a: "React всегда держит два дерева fiber. Current-дерево совпадает с тем, что нарисовано на экране; дерево workInProgress — строящееся обновление, клонированное из current через указатель alternate каждого fiber, так что неизменные поддеревья переиспользуются по ссылке. React мутирует только workInProgress, никогда current, поэтому экранное дерево никогда не наблюдается наполовину обновлённым. Когда рендер коммитится, оба меняются ролями. Когда более приоритетное обновление прерывает, React просто отбрасывает незавершённое дерево workInProgress: так как оно не коммитилось, мутаций DOM не было, откатывать нечего и стоимость нулевая. Именно это позволяет time-slicing дёшево отбрасывать и перезапускать рендеры." },
    { q: "Как reconciliation сводит O(n³) сравнение деревьев к O(n) и какова роль ключей?", a: "Общее сравнение деревьев — O(n³); React делает его O(n) двумя эвристиками. Первая: тот же тип элемента — обнови существующий fiber на месте (состояние сохраняется); другой тип — снеси всё поддерево и построй заново (состояние уничтожается), React никогда не диффит через границу типов. Вторая: внутри списка React сопоставляет новых детей со старыми fiber по ключу, строя карту ключ→fiber и спаривая по идентичности, а не по позиции. Fiber несёт всё состояние — значения useState, ref, неуправляемое DOM-состояние, сам DOM-узел — поэтому корректный ключ привязывает это состояние к правильному логическому элементу. key={index} позиционен, поэтому любой reorder, вставка или удаление сдвигают, какой fiber получает элемент, тихо повреждая неуправляемое состояние, тогда как управляемые пропсы (передаваемые заново) всё ещё выглядят верно." },
    { q: "Объясните модель lanes и как time-slicing держит ввод отзывчивым.", a: "React 18 назначает каждому обновлению lane — приоритетную корзину в 31-битной битмаске. Дискретный ввод (клик, нажатие) попадает в синхронную lane и рендерится немедленно и блокирующе, потому что пользователь его ждёт; обычные обновления используют default-lane; работа в startTransition или читаемая через useDeferredValue попадает в transition-lane, которая прерываема; idle — самая низкая. Для concurrent (несрочных) рендеров планировщик делает time-slicing: после ~5 мс reconciliation он проверяет shouldYield(), и если да — постит задачу-продолжение через MessageChannel и даёт браузеру выполнить обработчики ввода и отрисовку, прежде чем возобновить от сохранённого указателя fiber. Рендер на 50 мс становится десятью слайсами по 5 мс, чередующимися с работой браузера; когда нажатие приходит посреди transition, React отбрасывает тот рендер, обрабатывает нажатие с синхронным приоритетом, затем перезапускает transition." },
    { q: "Что такое bailout, что им управляет и почему inline-объект как проп его срывает?", a: "Когда React доходит до fiber во время рендера, он проверяет: менялись ли пропсы (Object.is по каждому пропу), менялось ли состояние, есть ли ожидающее обновление контекста, который он потребляет? Если ничего — он делает bailout: клонирует fiber из его alternate, переиспользует всё дочернее поддерево по ссылке и не вызывает функцию компонента вовсе. Перерендер родителя не заставляет детей перерендериваться; он предлагает им шанс на bailout, а референциальная стабильность — то, что позволяет им его взять. React.memo делает рендер ребёнка зависимым от Object.is-равных пропсов. Inline-объект, массив или стрелочная функция, созданные в JSX — свежая ссылка каждый рендер, поэтому Object.is всегда сообщает об изменении и bailout не срабатывает — даже с React.memo. useMemo и useCallback существуют, чтобы сохранять эти ссылки; React Compiler автоматизирует ту же мемоизацию на этапе сборки, не меняя механизм." },
    { q: "Что такое tearing, почему его вскрывает только concurrent rendering и как useSyncExternalStore его предотвращает?", a: "Tearing — закоммиченный UI, внутренне несогласованный: одни компоненты отрендерились со старым значением внешнего стора, другие — с новым. Он появляется только при прерываемом рендеринге, потому что concurrent-рендер идёт слайсами, и внешний стор может мутировать между двумя слайсами — ранние компоненты читают старое значение, поздние — новое. Внутреннее состояние React не может tearing, потому что React контролирует точный момент его изменения; внешние сторы могут, потому что React их не контролирует. useSyncExternalStore даёт React функции subscribe и getSnapshot. React читает снапшот, и если замечает, что снапшот изменился во время concurrent-рендера, перезапускает рендер, чтобы каждый компонент в одном commit наблюдал одно значение. Каждая серьёзная библиотека состояния React 18 (Redux, Zustand, Jotai) проходит через него по этой причине." },
  ]}
/>

<Recap lang="ru">
Если вы смогли восстановить каждый ответ по памяти, вы держите хребет модуля: render вычисляет диф и может быть приостановлена; commit применяет его атомарно; двойная буферизация делает прерывание бесплатным; эвристики reconciliation плюс ключи привязывают состояние к идентичности; lanes и time-slicing держат срочный ввод впереди дорогой работы; bailout — управляемый референциальной стабильностью через Object.is — пропускает неизменные поддеревья; а useSyncExternalStore не даёт внешним сторам делать tearing. Реконсилер по сути — машина для решения, какие fiber переживут обновление, а какие будут перестроены.
</Recap>
