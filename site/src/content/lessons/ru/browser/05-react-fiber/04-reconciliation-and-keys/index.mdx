---
slug: 04-reconciliation-and-keys
lang: ru
track: browser
unit: 05-react-fiber
order: 4
title: "Реконсиляция: эвристики диффа и ловушка ключей"
summary: "React сводит O(n³) дифф деревьев к O(n) двумя эвристиками — одинаковый тип сохраняет состояние, разный тип уничтожает его. Ключи идентифицируют элементы списка между рендерами: индексные ключи молча портят неуправляемое состояние при перестановке."
estMin: 14
status: ready
lessonType: topic
level: middle
prereqs: ["02-fiber-data-structure", "03-render-and-commit-phases"]
deepensInto: ["06-bailout-and-memoisation"]
spiral: ["statefulness"]
concepts: ["reconciliation", "diffing-heuristics", "key-prop", "list-reconciliation", "state-corruption", "controlled-vs-uncontrolled"]
sources:
  - https://react.dev/learn/preserving-and-resetting-state
  - https://react.dev/learn/rendering-lists
  - https://github.com/acdlite/react-fiber-architecture
---

import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import Misconception from "~/components/layout/Misconception.astro";

<Hook>
Чекбокс, отмеченный в строке 3, остаётся отмеченным после удаления строки 1 — теперь на том месте, что было строкой 4. Никакого предупреждения React. Никакой ошибки в консоли. Одно изменение — `key={index}` на `key={item.id}` — и баг исчезает. Ключи — не подсказки. Это механизм, которым React сопоставляет файберы с элементами между рендерами, и если ошибиться — состояние молча портится.
</Hook>

<Crux>Реконсиляция O(n): разный тип сбрасывает состояние; ключи привязывают файберы к идентичности элемента, а не к позиции.</Crux>

<Explanation>

**Реконсиляция: правила диффа.** Реконсиляция сравнивает новое дерево элементов с текущим fiber-деревом, чтобы решить, что сохранить, обновить или заменить. Полный дифф дерева — O(n³) в общем случае; React делает его O(n) двумя эвристиками.

**Эвристика 1 — одинаковый тип, тот же fiber.** Если `<div>` остаётся `<div>` или `<ComponentA>` остаётся `<ComponentA>`, React обновляет существующий fiber на месте — патчит props, запускает реконсиляцию для потомков. Fiber сохраняет своё состояние.

**Эвристика 2 — разный тип, полная замена.** Если `<div>` становится `<span>` или `<ComponentA>` становится `<ComponentB>`, React сносит всё поддерево (размонтирует его, уничтожает состояние) и строит новое с нуля. Он не пытается делать дифф через границу типов. Именно поэтому условный рендеринг двух разных компонентов в одном слоте сбрасывает всё состояние — даже если новый компонент выглядит похоже.

**Ключи идентифицируют дочерние элементы списка между рендерами.** В списке React сопоставляет старые и новые дочерние элементы по их `key`, а не по позиции. Стабильный ключ говорит React: «это тот же логический элемент, даже если он переместился». В этом и есть весь смысл ключей. React строит карту из `key` в старый fiber, затем обходит новые дочерние элементы, сопоставляя каждый со старым fiber по ключу. Сопоставленный fiber несёт вперёд всё статeful: значения `useState`, содержимое `useRef`, неуправляемое состояние DOM (введённый текст input, отмеченность checkbox, позицию скролла, фокус) и сам DOM-узел.

<div data-lesson-visual class="my-6 rounded-lg border border-border bg-surface-1 p-4 font-mono text-sm">
  <div class="mb-2 text-ink-2 text-xs uppercase tracking-wider">key=&#123;index&#125; vs key=&#123;id&#125; — что происходит при удалении</div>
  <div class="grid grid-cols-2 gap-4 text-xs">
    <div>
      <div class="mb-1 font-semibold text-red-700 dark:text-red-400">key=&#123;index&#125; (сломано)</div>
      <div class="rounded bg-red-50 dark:bg-red-950/30 border border-red-200 dark:border-red-800 p-2">
        <div>До: key=0 → элемент А ✓отмечен</div>
        <div>До: key=1 → элемент Б</div>
        <div>До: key=2 → элемент В</div>
        <div class="border-t border-red-200 dark:border-red-800 mt-1 pt-1">Удаляем элемент А</div>
        <div>После: key=0 → элемент Б (но fiber от А — ✓отмечен!)</div>
        <div>После: key=1 → элемент В</div>
      </div>
    </div>
    <div>
      <div class="mb-1 font-semibold text-green-700 dark:text-green-400">key=&#123;item.id&#125; (правильно)</div>
      <div class="rounded bg-green-50 dark:bg-green-950/30 border border-green-200 dark:border-green-800 p-2">
        <div>До: key="a" → элемент А ✓отмечен</div>
        <div>До: key="b" → элемент Б</div>
        <div>До: key="c" → элемент В</div>
        <div class="border-t border-green-200 dark:border-green-800 mt-1 pt-1">Удаляем элемент А</div>
        <div>После: key="b" → элемент Б (правильный fiber, без состояния)</div>
        <div>После: key="c" → элемент В</div>
      </div>
    </div>
  </div>
</div>

**Почему `key={index}` — ловушка.** С `key={index}` ключ — это позиция, поэтому при перестановке списка или вставке элемента в начало ключ каждого элемента смещается. React тогда считает, что элемент-по-индексу-0 — «тот же элемент», хотя данные переместились — он сохраняет состояние и DOM старого fiber, но передаёт ему props нового элемента. Видимый баг: чекбокс, отмеченный в строке 3, остаётся отмеченным после удаления строки 1, теперь прикреплённый к тому, что было строкой 4.

Управляемые props обновляются корректно (они передаются заново при каждом рендере); неуправляемое состояние — нет (оно живёт на fiber). `key={index}` безопасен только для списка, который статичен и никогда не переупорядочивается; для любого динамического списка он молча портит состояние.

<Inset kind="edgecase" lang="ru">
`Math.random()` в качестве ключа хуже, чем `key={index}`. Это присваивает новый ключ при каждом рендере, поэтому React всегда видит «другой» элемент на каждой позиции — он размонтирует и перемонтирует каждый элемент списка при каждом обновлении. Вы получаете правильное состояние (потому что fiber всегда новый), но платите за размонтирование + монтирование каждого элемента при каждом нажатии клавиши. `key={index}` неправильный для динамических списков; `key={Math.random()}` неправильный для всех списков.
</Inset>

<Misconception id="mc-key-index">
**Миф:** `key={index}` нормально — ключи просто должны быть уникальными.

**Реальность:** Ключи идентифицируют элементы между рендерами. Индекс — это позиция, не идентичность: при перестановке или удалении индексы смещаются и React привязывает state к неверному элементу. Используйте стабильный id.
</Misconception>

<Quiz
  id="mid-quiz-keys-1"
  pieceSlug="05-react-fiber"
  lang="ru"
  question="Список элементов рендерится с `key={index}`. Вы удаляете первый элемент. Чекбокс, который был отмечен на старом втором элементе, теперь отмечен на неправильной строке. Почему?"
  choices={[
    { label: "С индексными ключами удаление элемента 0 смещает каждый ключ вниз на один — React сопоставляет файберы по ключу, поэтому сохраняет состояние каждого fiber, но сопоставляет его с данными следующего элемента", correct: true },
    { label: "React потерял состояние чекбокса во время удаления", misconception: "Состояние не было потеряно — оно сохранилось на неправильном fiber. Файберы остались; данные, с которыми они были сопоставлены, сдвинулись." },
    { label: "Чекбоксы всегда сбрасываются при изменении списка", misconception: "Чекбоксы прекрасно сохраняют состояние со стабильными ключами. Баг специфически в несоответствии индекса-как-ключа." },
    { label: "Фаза commit применила мутации не в том порядке", misconception: "Порядок commit правильный. Несоответствие произошло в реконсиляции, где индексные ключи заставили React сопоставить файберы с неправильными элементами." },
  ]}
/>

<Quiz
  id="mid-quiz-keys-2"
  pieceSlug="05-react-fiber"
  lang="ru"
  question="Вы рендерите `condition ? <ComponentA /> : <ComponentB />` в одном слоте. Когда условие меняется, что происходит с состоянием ComponentA?"
  choices={[
    { label: "Оно уничтожается — разный тип элемента вызывает полную замену поддерева: ComponentA размонтируется, ComponentB монтируется заново", correct: true },
    { label: "Состояние ComponentA переносится в ComponentB, поскольку они занимают один слот", misconception: "React не переносит состояние через границы типов. Смена типа — это всегда полный снос и перестройка." },
    { label: "React объединяет состояния двух компонентов", misconception: "Объединения состояний нет. Разный тип = полная замена = ComponentA размонтируется и теряет всё состояние." },
    { label: "Состояние сохраняется в рефе и передаётся ComponentB", misconception: "Ничего не сохраняется автоматически при смене типа. Если нужно сохранить состояние — надо поднять его вручную." },
  ]}
/>

<Quiz
  id="mid-quiz-keys-3"
  pieceSlug="05-react-fiber"
  lang="ru"
  question="Почему управляемое состояние (value, checked, переданные как props) выглядит правильно с `key={index}`, а неуправляемое (введённый текст, позиция скролла) — нет?"
  choices={[
    { label: "Управляемые props передаются заново при каждом рендере, поэтому всегда отражают данные нового элемента независимо от того, на какой fiber они попали. Неуправляемое состояние живёт на fiber и не передаётся заново — поэтому следует за неправильным fiber.", correct: true },
    { label: "Управляемое состояние хранится в глобальной переменной, а не на fiber", misconception: "Управляемое состояние — это React-состояние или props, живущее в дереве компонентов. Разница в том, как DOM-поле заполняется, а не где хранится." },
    { label: "Браузер обрабатывает управляемые и неуправляемые inputs по-разному", misconception: "Браузер не имеет специальной обработки для различия React controlled/uncontrolled. Разница полностью в том, как React заполняет DOM-узел при каждом рендере." },
    { label: "Неуправляемое состояние хранится в localStorage", misconception: "Неуправляемое состояние DOM (отмеченность чекбокса, введённый текст) хранится на DOM-узле, прикреплённом к fiber. Fiber перемещается с неправильными данными; DOM-состояние перемещается вместе с ним." },
  ]}
/>

</Explanation>

<KeyTakeaway>Реконсиляция O(n) благодаря двум эвристикам: одинаковый тип обновляет fiber на месте (состояние сохраняется); разный тип сносит поддерево (состояние уничтожается). Ключи привязывают файберы к идентичности элемента — индексные ключи портят неуправляемое состояние при любой перестановке, вставке или удалении.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="04-reconciliation-and-keys-retrieval"
  lang="ru"
  questions={[
    { q: "Объясните на уровне fiber, почему key={index} портит состояние при перестановке списка.", a: "Реконсиляция сопоставляет новые дочерние элементы списка со старыми файберами по ключу. С индексными ключами ключ — это позиция. При перестановке списка данные по индексу 2 меняются, но React всё равно сопоставляет новый элемент с индексом 2 со старым fiber с ключом 2. Этот fiber несёт вперёд всё stateful — значения useState, рефы, неуправляемое состояние DOM, сам DOM-узел. Управляемые props передаются заново и выглядят правильно; неуправляемое состояние остаётся с неправильным элементом." },
    { q: "Что делает React, когда тип компонента меняется в одном слоте?", a: "Сносит всё поддерево — размонтирует старый компонент (уничтожая всё его состояние и рефы), затем монтирует новый с нуля. React никогда не пытается делать дифф через границы типов." },
    { q: "Когда key={index} действительно безопасен?", a: "Только для списка, который только отображает данные, никогда не переупорядочивается и не имеет вставок или удалений, и не содержит неуправляемых stateful-inputs. Любой динамический список — фильтруемый, сортируемый или редактируемый — требует стабильных ключей идентичности." },
  ]}
/>

<Recap lang="ru">
Реконсиляция сводит O(n³) дифф деревьев к O(n) двумя эвристиками: одинаковый тип элемента означает обновление fiber на месте (состояние сохраняется); разный тип означает снос поддерева и перестройку с нуля (состояние уничтожается). Для списков React использует `key` для сопоставления новых дочерних элементов со старыми файберами по идентичности, а не позиции. Fiber несёт всё stateful — значения `useState`, рефы, неуправляемое состояние DOM — поэтому сопоставление неправильного fiber с неправильным элементом молча портит это состояние. `key={index}` — ловушка: индексные ключи позиционные, поэтому любая перестановка, вставка или удаление смещает, какой fiber достанется элементу. Используйте стабильный ключ идентичности (id из базы данных, slug) всегда, когда список динамический.
</Recap>
