---
concepts:
- pipeline-stages
- invalidation
- compositor-layers
- frame-lifecycle
- layout-thrash
- inp-loaf
estMin: 14
lang: ru
level: senior
order: 8
prereqs: []
slug: quiz-short
sources:
- https://web.dev/articles/rendering-performance
- https://developer.chrome.com/blog/inside-browser-part3
- https://web.dev/articles/inp
status: ready
summary: "Промпты на свободное вспоминание по всему юниту render pipeline. Сначала ответь своими словами, затем раскрой модельный ответ и сравни."
title: 'Render pipeline: тест на свободное вспоминание'
track: browser
unit: 02-render-pipeline
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";

<Hook>
Вспоминание бьёт перечитывание. Для каждого промпта скажи или запиши полный ответ по памяти, прежде чем открыть модельный ответ — именно усилие вспоминания закрепляет pipeline.
</Hook>

<Goal lang="ru">
Реконструируй ключевые механизмы юнита — шесть стадий и два потока, правила инвалидации, compositor layers, frame lifecycle, layout thrashing и production-петлю — не заглядывая в уроки.
</Goal>

<RetrievalDrawer
  client:visible
  id="02-render-pipeline-short-retrieval"
  lang="ru"
  questions={[
    { q: "Назови шесть стадий pipeline по порядку и скажи, какой поток владеет каждой.", a: "Parse HTML, Build CSSOM, Style, Layout, Paint, Composite. Main thread владеет первыми пятью (parse, CSSOM, style, layout, paint setup) плюс твоим JavaScript; compositor thread владеет Composite — сборкой отрисованных битмапов слоёв и отправкой кадра на GPU. Main thread однопоточный по дизайну, потому что DOM, CSSOM и состояние JS могут мутироваться только одним контекстом за раз, поэтому каждая длинная задача конкурирует за один и тот же бюджет кадра 16.67 мс." },
    { q: "Сопоставь три класса CSS-изменений со стадией, которую они инвалидируют, и объясни, чем различается стоимость.", a: "Flow-affecting свойства (width, height, top, left) инвалидируют Layout, что форсирует Paint и Composite вниз по потоку — самый дорогой класс. Paint-affecting свойства (background-color, color, box-shadow, filter) инвалидируют Paint плюс Composite, минуя layout. transform и opacity на promoted-элементе инвалидируют только Composite — самый дешёвый, потому что compositor просто перепозиционирует или пере-смешивает существующий GPU-битмап. Dirty bits всегда текут вниз по потоку, никогда вверх, поэтому стадия, которую ты трогаешь, задаёт нижнюю границу стоимости." },
    { q: "Что делает overlap rule и почему один will-change промоутит десяток соседей?", a: "Compositor layer — это заранее отрисованный GPU-битмап. Чтобы сохранить корректный порядок наложения, любой элемент, рисующий поверх promoted-слоя, тоже должен быть промоучен (или squashed в общий слой) — иначе promoted-слой отрисовался бы поверх элемента, который должен сидеть выше. Так один will-change: transform на карточке в ленте вынуждает браузер промоутить перекрывающих соседей, о которых тебя не спросили. Каждый слой стоит width × height × 4 байта GPU-памяти, так что неявные промоушены могут тихо исчерпать мобильный GPU-бюджет (~256 МБ) и вызвать вытеснение тайлов." },
    { q: "Объясни forced synchronous layout и фикс в два прохода.", a: "Layout управляется dirty-флагами: браузер группирует записи стилей и делает flush только когда требуется свежая геометрия. Чтение свойства геометрии (offsetWidth, offsetTop, getBoundingClientRect, scrollTop) после записи стиля форсирует немедленный flush layout. В цикле, где чередуются read-then-write, это выполняет один полный layout на итерацию — N строк, N layout, блокируя main thread на N × layout_cost. Фикс структурный: two-pass batch — прочитать каждое значение геометрии в массив в первом проходе, затем применить все записи во втором — чтобы браузер сделал flush layout один раз. Обёртка цикла в rAF не помогает: rAF двигает границу вызова, а не границу инвалидации." },
    { q: "Каков порядок событий внутри одного кадра и почему rAF отличается от ResizeObserver?", a: "По спецификации HTML: события ввода, затем сработавшие таймеры (setTimeout/setInterval), затем microtasks дренируются до пустоты, затем requestAnimationFrame колбэки, затем ResizeObserver/IntersectionObserver, затем style + layout, затем paint, затем composite, затем уступка vsync. rAF выполняется до style/layout, поэтому запись там сопровождается ровно одним проходом layout — но чтение геометрии там форсирует flush. ResizeObserver срабатывает после layout, но до paint, поэтому можно прочитать свежевычисленный размер и среагировать без второго forced layout. Самопланирующийся цикл microtask морит рендеринг голодом, потому что очередь microtask должна дренироваться до пустоты, прежде чем кадр достигнет style/layout." },
    { q: "Как INP и LoAF замыкают production-петлю и какие две проблемы pipeline обычно вызывают плохой INP?", a: "INP (Interaction to Next Paint, Core Web Vital с марта 2024) измеряет задержку от ввода пользователя до следующей отрисовки, которая видимо отвечает — то, что пользователь реально почувствовал, снятое RUM на реальных устройствах. LoAF (Long Animation Frame Timing) сообщает о любом кадре дольше 50 мс с разбивкой, что доминировало (блокирующий JS, render, forced layout) — диагностика. Плохой INP (>200 мс p75) почти всегда сводится к одной из двух проблем pipeline: длинная задача JS в обработчике ввода, задерживающая rAF, или forced synchronous layout из чтения геометрии внутри обработчика. LoAF атрибутирует медленный кадр; INP — метрика, которую ты защищаешь." },
  ]}
/>

<Recap lang="ru">
Если ты смог реконструировать каждый ответ по памяти, ты держишь хребет юнита: шесть стадий на двух потоках под бюджетом 16.67 мс, свойство, которое ты меняешь, выбирает стадию для инвалидации, dirty bits текут вниз по потоку, compositor layers дёшевы для движения, но дороги для удержания (а overlap rule промоутит соседей), layout thrashing — это чтение после записи, исправляемое two-pass batch, frame lifecycle фиксирует, когда срабатывают rAF и ResizeObserver, а INP плюс LoAF замыкают петлю от ощущаемого пользователем медленного взаимодействия назад к точной стадии, которая его вызвала.
</Recap>
