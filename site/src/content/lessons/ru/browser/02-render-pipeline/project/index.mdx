---
concepts:
- frame-profiling
- layout-thrash
- compositor-layers
- content-visibility
- inp-loaf
estMin: 240
lang: ru
level: senior
order: 10
prereqs: []
slug: project
sources:
- https://web.dev/articles/rendering-performance
- https://developer.chrome.com/docs/devtools/performance/reference
- https://web.dev/articles/inp
status: ready
summary: "Практический проект — возьми тормозящий скроллируемый список, продиагностируй каждую стадию pipeline в DevTools, исправь layout thrashing и злоупотребление слоями и докажи 60 fps числами до/после."
title: 'Render pipeline: спасение тормозящего списка'
track: browser
unit: 02-render-pipeline
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import ProjectBrief from "~/components/pedagogy/ProjectBrief.astro";

<Hook>
Читать про layout thrashing — не то же, что вытащить реальный список из него. Собери намеренно тормозящую скроллируемую ленту, профилируй её в DevTools и применяй порядок фиксов юнита — спрогнозируй стадию, исправь структурно, тянись к compositor в последнюю очередь — пока числа кадров не вернутся, с доказательствами на каждом шаге.
</Hook>

<Goal lang="ru">
Преврати ментальную модель юнита в воспроизводимый инженерный цикл: профилируй pipeline, атрибутируй медленную стадию, убей forced sync layout, направь движение через compositor, защити границу GPU-памяти и подтверди фикс числами кадров до/после на реальном железе.
</Goal>

<ProjectBrief
  id="02-render-pipeline-project"
  lessonSlug="02-render-pipeline"
  lang="ru"
  objective="Возьми намеренно тормозящий скроллируемый список (свой или стартер из 5 000+ строк) и доведи его до устойчивых 60 fps на среднем Android — без пропущенных кадров при скролле, без предупреждений Forced reflow, число GPU-слоёв под 30 — доказывая каждый шаг измерениями DevTools, а не оценками."
  requirements={[
    "Собери или возьми список минимум из 5 000 строк с намеренным источником джанка: обработчик resize или scroll, который читает offsetWidth/getBoundingClientRect, затем пишет стиль в том же цикле, и анимацию, двигающую строки через top/left вместо transform.",
    "Запиши трейс DevTools Performance, скролля 5 секунд. Определи доминирующую стадию по цвету: жёлтый (JS), фиолетовый (style/layout), зелёный (paint), тёмно-синий (composite). Зафиксируй базовую линию: p95 длительность кадра, число пропущенных кадров и любые предупреждения Forced reflow while executing JavaScript.",
    "Устрани forced synchronous layout: отрефактори обработчик в two-pass batch (сначала все чтения геометрии, затем все записи стилей) или перенеси чтения, реагирующие на размер, в ResizeObserver. Перезапиши трейс и покажи, что предупреждения Forced reflow исчезли, а фиолетовые полосы layout уменьшились.",
    "Переведи анимацию строки с top/left на transform (translate), чтобы движение стало composite-only. Подтверди в трейсе, что layout больше не перевыполняется покадрово при анимации.",
    "Ограничь стоимость внеэкранной отрисовки через content-visibility: auto плюс contain-intrinsic-size на строки (или виртуализируй список). Покажи, что style+layout при первой отрисовке резко падает и внеэкранные строки больше не стоят layout.",
    "Проаудируй compositor layers через DevTools Rendering > Layer Borders. Убери любой постоянный will-change; ограничь его mouseenter/animationend. Держи живое число слоёв под 30 и задокументируй оценку GPU-памяти (width × height × 4 байта на слой).",
    "Перезапусти идентичный 5-секундный трейс скролла на среднем Android через удалённую отладку и запиши числа после рядом с до.",
  ]}
  acceptance={[
    "Таблица до/после: p95 длительность кадра, число пропущенных кадров, layout мс на кадр, composite мс на кадр и живое число слоёв — измеренные при одном и том же скролле, не оценённые.",
    "Ноль предупреждений Forced reflow while executing JavaScript в трейсе после при устойчивом скролле.",
    "Дорожка Frames в DevTools показывает зелёные (вовремя) кадры при скролле без красных пропущенных; p95 длительность кадра после — под бюджетом для целевой частоты обновления.",
    "Абзац-описание, называющий для каждого фикса, какую стадию он адресовал и почему встал именно туда в порядке фиксов (устрани forced layout, направь на compositor, пропусти внеэкранную работу, и только потом тюнинг).",
  ]}
  stretch={[
    "Добавь инструментацию LoAF и INP (PerformanceObserver для long-animation-frame плюс INP из web-vitals) и одностраничный on-call runbook: прочитай четыре сигнала, сопоставь каждый со стадией pipeline, примени лестницу фиксов, проверь.",
    "Добавь аудит passive: true — найди каждый слушатель scroll/wheel/touchmove, отметь действительно non-passive и покажи, что compositor-thread скролл восстановлен там, где preventDefault не нужен.",
    "Добавь gate Lighthouse CI на PR, который роняет сборку, если INP регрессирует выше 200 мс или CLS выше 0.1, и синтетический бенчмарк, диффающий p95 длительность кадра против ветки main.",
    "Перенеси CPU-тяжёлое вычисление на строку (например, рендер markdown или подсветку синтаксиса) в Web Worker и покажи, что main-thread полосы скриптинга уменьшаются, а скролл остаётся на 60 fps.",
  ]}
/>

<Recap lang="ru">
Это цикл, который ты запустишь в каждом реальном инциденте рендеринга: сначала профилируй, атрибутируй медленную стадию по цветам flame-strip, устрани forced synchronous layout через two-pass batch, направь движение через compositor с transform, пропусти внеэкранную работу через content-visibility, защити GPU-память, ограничив will-change, и подтверди числами кадров до/после на реальном железе. Сделав это один раз на тормозящем игрушечном списке, ты превращаешь production-спасение в мышечную память.
</Recap>
