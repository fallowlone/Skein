---
concepts:
- pipeline-stages
- invalidation
- compositor-layers
- layout-thrash
- frame-lifecycle
- inp-loaf
estMin: 13
lang: ru
level: senior
order: 7
prereqs: []
slug: quiz-choice
sources:
- https://web.dev/articles/rendering-performance
- https://developer.chrome.com/blog/inside-browser-part3
- https://csstriggers.com/
status: ready
summary: "Тест с выбором на синтез по всему юниту render pipeline — стоимость стадий, радиус инвалидации, compositor layers, layout thrashing и production-телеметрия."
title: 'Render pipeline: тест с выбором ответа'
track: browser
unit: 02-render-pipeline
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Шесть вопросов поперёк всего pipeline. Каждый отражает решение, которое ты принимаешь в реальном расследовании джанка — не стадию для называния, а стоимость для прогноза и фикс для ранжирования.
</Hook>

<Goal lang="ru">
Убедись, что связываешь шесть стадий, правила инвалидации, compositor layers, layout thrashing и production-метрики — тот синтез, к которому вели отдельные уроки.
</Goal>

<Quiz
  id="02-render-pipeline-choice-1"
  lessonSlug="02-render-pipeline"
  lang="ru"
  question="Обработчик скролла анимирует элемент списка, записывая element.style.top каждый кадр, и страница дёргается на среднем телефоне. Коллега говорит: это на компоненте композитинга, значит дело в GPU. Почему он неправ?"
  choices={[
    { label: "top — flow-affecting свойство, поэтому каждая запись инвалидирует layout на main thread, а дальше paint и composite вниз по потоку. Работа никогда не попадает на compositor дёшево; пять стадий перевыполняются каждый кадр. Анимируй transform.", correct: true },
    { label: "GPU и правда узкое место; скролл — это всегда операция композитинга.", misconception: "Off-main-thread скролл обрабатывает сдвиг вьюпорта, но запись top внутри обработчика — это мутация, инвалидирующая layout, и она идёт на main thread, а не на GPU." },
    { label: "top и transform стоят одинаково; джанк — просто медленный телефон.", misconception: "transform на promoted layer — composite-only; top инвалидирует layout. Разница в стоимости на порядок, именно поэтому телефон дёргается на одном и не на другом." },
    { label: "Обработчик надо обернуть в requestAnimationFrame, что перенесёт работу на GPU.", misconception: "rAF группирует вызов к границе кадра, но всё равно выполняется на main thread; он не переносит инвалидирующие layout записи с main thread." },
  ]}
/>

<Quiz
  id="02-render-pipeline-choice-2"
  lessonSlug="02-render-pipeline"
  lang="ru"
  question="Страница рендерит ленту из 10 000 строк. style + layout на первой отрисовке стоят 1 200 мс. Нужна быстрая первая отрисовка без виртуализации (без переписывания DOM). Какое одно изменение даёт максимальный рычаг?"
  choices={[
    { label: "Добавить content-visibility: auto плюс contain-intrinsic-size на строки. Внеэкранные поддеревья display-locked — ни style, ни layout, ни paint, пока не пересекут вьюпорт, — что роняет первую отрисовку к ~10 мс.", correct: true },
    { label: "Добавить will-change: transform на каждую строку, чтобы их обрабатывал compositor.", misconception: "will-change промоутит строки в GPU-слои (стоимость памяти), но никак не пропускает style/layout внеэкранных строк. На мобильной GPU-памяти это только усугубляет проблему." },
    { label: "Обернуть рендер в requestAnimationFrame, чтобы сгруппировать layout.", misconception: "rAF группирует момент вызова колбэка, а не объём layout для 10 000 строк. Полный style+layout всё равно выполняется для каждой строки." },
    { label: "Добавить contain: paint на каждую строку.", misconception: "contain: paint обрезает отрисовку и помогает пропустить внеэкранный paint, но не пропускает style и layout так, как делает display-locking через content-visibility: auto. Layout всё равно выполняется для всех строк." },
  ]}
/>

<Quiz
  id="02-render-pipeline-choice-3"
  lessonSlug="02-render-pipeline"
  lang="ru"
  question="Ты добавляешь will-change: transform на одну карточку в ленте ради плавного hover. DevTools Layer Borders теперь показывает сорок promoted-боксов. Почему и какова стоимость?"
  choices={[
    { label: "Правило перекрытия (overlap rule): элементы, рисующие поверх promoted-карточки, тоже должны быть промоучены (или squashed), чтобы сохранить корректный порядок наложения. Каждый layer — GPU-битмап width × height × 4 байта, так что неявные промоушены жгут GPU-память, которую ты не просил.", correct: true },
    { label: "will-change запускает больше JavaScript за кадр, который DevTools рисует как лишние слои.", misconception: "will-change — это CSS-подсказка рендеринга без стоимости JS. Лишние боксы — это compositor-induced слои из overlap rule, а стоимость — GPU-память, не CPU." },
    { label: "Каждый promoted-бокс перерисовывается каждый кадр, и это и есть реальная стоимость.", misconception: "Promoted layer отрисовывается один раз и затем двигается на compositor. Стоимость — удержание битмапа в GPU-памяти, а не перерисовка покадрово." },
    { label: "Сорок боксов — это баг отрисовки DevTools и реальной стоимости не несут.", misconception: "Это настоящие compositor layers, созданные overlap rule. На мобильном бюджете GPU 256 МБ десятки полноразмерных слоёв вызывают вытеснение тайлов и худший джанк." },
  ]}
/>

<Quiz
  id="02-render-pipeline-choice-4"
  lessonSlug="02-render-pipeline"
  lang="ru"
  question="Main thread заблокирован на 400 мс синхронным JSON.parse. CSS-анимация на transform продолжает идти 60 fps; командная rAF-анимация на том же элементе застывает. Каков механизм?"
  choices={[
    { label: "CSS-анимация на transform/opacity promoted-элемента передаётся compositor thread на старте анимации, поэтому интерполируется без main thread. rAF-колбэки выполняются на main thread, так что блок на 400 мс их замораживает.", correct: true },
    { label: "CSS-анимация троттлится к меньшей частоте кадров, поэтому и выживает.", misconception: "Она выживает, потому что идёт на compositor на полных 60 fps, независимо от заблокированного main thread, — а не потому, что её троттлят." },
    { label: "rAF и CSS-анимации обе идут на main thread; разница — в случайном планировании.", misconception: "Composite-only CSS-анимации передаются compositor thread; rAF идёт на main thread. Разница архитектурная, не случайная." },
    { label: "JSON.parse идёт на воркере, поэтому затронута только rAF-анимация по случайности.", misconception: "Синхронный JSON.parse идёт на main thread и блокирует его; именно поэтому main-thread rAF-анимация застывает, а compositor-анимация нет." },
  ]}
/>

<Quiz
  id="02-render-pipeline-choice-5"
  lessonSlug="02-render-pipeline"
  lang="ru"
  question="Production RUM сообщает p75 INP 340 мс, тогда как Lighthouse в офисе ставит 95. Обработчик клика читает getBoundingClientRect(), а затем пишет стили в цикле. Как это читать и куда смотреть первым делом?"
  choices={[
    { label: "INP измеряет реальный input-to-next-paint на реальных устройствах; Lighthouse — лабораторный прокси. Обработчик читает геометрию после записи стилей, форсируя синхронный layout, который задерживает следующую отрисовку. Используй LoAF, чтобы атрибутировать медленный кадр, затем сгруппируй чтения перед записями.", correct: true },
    { label: "Lighthouse 95 значит, что страница в порядке; число RUM — шум.", misconception: "INP из RUM — это то, что реальные пользователи почувствовали на реальном железе. p75 340 мс — настоящая регрессия интерактивности, которую лабораторный прогон Lighthouse на быстром железе легко пропускает." },
    { label: "Фикс — снизить CLS, ведь INP и CLS отслеживают одну проблему.", misconception: "CLS измеряет неожиданный сдвиг layout; INP измеряет задержку input-to-next-paint. Это разные vitals; forced sync layout в обработчике — проблема INP." },
    { label: "Перевести обработчик на requestAnimationFrame, и forced layout исчезнет.", misconception: "rAF двигает границу вызова, но чтение после записи всё равно форсирует синхронный flush layout. Структурный фикс — сгруппировать все чтения перед всеми записями." },
  ]}
/>

<Quiz
  id="02-render-pipeline-choice-6"
  lessonSlug="02-render-pipeline"
  lang="ru"
  question="Слушатель touchmove добавлен без { passive: true } на контейнере скролла, и fps скролла обваливается на мобильном. Что изменилось в pipeline?"
  choices={[
    { label: "Off-main-thread (compositor) скролл отключается, потому что non-passive слушатель может вызвать preventDefault(); браузер должен выполнить обработчик на main thread, прежде чем сможет скроллить, так что любая main-thread работа теперь задерживает каждый кадр скролла.", correct: true },
    { label: "Non-passive слушатель срабатывает чаще, и это стоимость.", misconception: "passive против non-passive не меняет частоту срабатывания. Это меняет, может ли compositor скроллить, не дожидаясь main-thread обработчика." },
    { label: "Слушатель поглощает touch-событие, поэтому страница не может скроллить.", misconception: "Non-passive слушатель не поглощает событие; он вынуждает браузер ждать обработчик, прежде чем решить скроллить, откатываясь к main-thread скроллу." },
    { label: "DevTools показывает предупреждение, но производительность скролла идентична.", misconception: "Регрессия реальна: compositor-thread скролл становится main-thread скроллом, так что fps скролла падает всякий раз, когда main thread занят." },
  ]}
/>

<Recap lang="ru">
Сквозная линия юнита — одна цепочка решений: свойство, которое ты меняешь, выбирает стадию для инвалидации (transform = composite, color = paint, top/width = layout), dirty bits текут вниз по потоку, а containment или content-visibility ограничивают радиус поражения. Compositor layers дёшевы для движения, но дороги для удержания, overlap rule промоутит соседей, которых ты не просил, layout thrashing берётся из чтения геометрии после записи, а INP плюс LoAF замыкают петлю от production-телеметрии назад к конкретной стадии. Спрогнозируй стадию, ранжируй фикс, затем тянись к compositor.
</Recap>
