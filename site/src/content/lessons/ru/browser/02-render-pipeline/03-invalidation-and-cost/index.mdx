---
concepts:
- invalidation
- dirty-bit
- style-propagation
- layout-propagation
- contain
- content-visibility
- css-containment
- csstriggers
- display-locking
- FOIT
- FOUT
- CLS
deepensInto:
- 04-compositor-layers
- 06-layout-thrash
estMin: 14
lang: ru
lessonType: topic
level: middle
order: 3
prereqs:
- 02-stages-and-threads
slug: 03-invalidation-and-cost
sources:
- https://csstriggers.com/
- https://drafts.csswg.org/css-will-change/
- https://web.dev/articles/content-visibility
- https://web.dev/articles/rendering-performance
spiral:
- statefulness
- encapsulation
status: ready
summary: Как изменения CSS-свойств распространяются через пайплайн, что контролирует
  радиус взрыва, и как contain/content-visibility изолируют ущерб.
title: Инвалидация, dirty-биты и contain
track: browser
unit: 02-render-pipeline
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
Вы меняете один класс на одном элементе — браузер перерисовывает тысячу элементов. Вы меняете другой класс на том же элементе — ничто больше не шелохнулось. Разница в том, какую стадию пайплайна инвалидирует свойство — и может ли что-нибудь нижележащее спастись.
</Hook>

<Crux>Каждое изменение CSS-свойства инвалидирует одну стадию; dirty-биты текут вниз оттуда — если только containment их не остановит.</Crux>

<Explanation>

## Правило инвалидации

Почему смена одного свойства стоит миллисекунду, а другого — двадцать? Ответ в том, какую стадию пайплайна оно инвалидирует — и какие нижележащие стадии тащит за собой.

Изменение CSS-свойства инвалидирует одну или несколько стадий пайплайна. Всё начиная с этой стадии и ниже перезапускается.

| Изменение CSS | Инвалидирует | Цена |
| --- | --- | --- |
| `width`, `height`, `top`, `left` | Компоновку (+ отрисовку + композитинг) | Высокая |
| `background-color`, `color` | Отрисовку (+ композитинг) | Средняя |
| `transform`, `opacity` (на собственном слое) | Только композитинг | Низкая |
| `filter: blur(...)` | Отрисовку (+ композитинг) | Средняя–Высокая |

[csstriggers.com](https://csstriggers.com/) публикует карту по свойствам. Выучите дешёвые (transform, opacity, filter на продвинутом элементе) и дорогие (всё, что влияет на flow).

<FlowDiagram
  label="Три вида CSS-изменений входят в один и тот же пайплайн от стилей до композитинга в разных точках: свойство, влияющее на компоновку, входит на стадии стилей, paint-only-свойство — на отрисовке, а transform или opacity — на композитинге. Чем позже входит изменение, тем меньше стадий перезапускается и тем оно дешевле."
  perRow={4}
  hue="var(--d-frontend)"
  nodes={[
    { id: "style", label: "Стили", sub: "пересчёт" },
    { id: "layout", label: "Компоновка", sub: "измерение" },
    { id: "paint", label: "Отрисовка", sub: "растеризация" },
    { id: "composite", label: "Композитинг", sub: "GPU" },
    { id: "e_layout", label: "width / top", sub: "layout-свойство" },
    { id: "e_paint", label: "background-color", sub: "только paint" },
    { id: "e_comp", label: "transform / opacity", sub: "только composite" },
  ]}
  edges={[
    { from: "style", to: "layout" },
    { from: "layout", to: "paint" },
    { from: "paint", to: "composite" },
    { from: "e_layout", to: "style", label: "входит" },
    { from: "e_paint", to: "paint", label: "входит" },
    { from: "e_comp", to: "composite", label: "входит" },
  ]}
  caption="Пайплайн идёт слева направо. Свойство, меняющее компоновку, входит на стадии стилей и платит за все четыре стадии; paint-only-свойство входит на отрисовке и платит за две; transform/opacity входит на композитинге и платит за одну. Дешёвые изменения входят позже."
/>

## Как распространяются dirty-биты

<div data-lesson-visual class="my-6 rounded-lg border border-border bg-surface-2 p-4 text-sm">
  <p class="font-semibold mb-2">Правила распространения dirty-битов</p>
  <ul class="space-y-2 list-disc list-inside">
    <li><strong>Style-флаги</strong> текут вниз по DOM-дереву — смена класса на родителе делает dirty всех потомков, чьи селекторы матчатся.</li>
    <li><strong>Layout-флаги</strong> — по box-дереву — изменение width у родителя может сделать dirty сиблингов, если их позиция зависит от него.</li>
    <li><strong>Paint-флаги</strong> ограничены paint-слоем — paint-меняющее изменение инвалидирует bitmap этого слоя; сиблинг-слои со своими bitmap-ами не затрагиваются.</li>
    <li><strong>Композитинг</strong> всегда dirt-free — работает с уже растеризованными тайлами, лишь перезапускает GPU draw call.</li>
  </ul>
  <p class="mt-3 font-mono text-xs text-ink-2">Правило: dirty-биты текут ВНИЗ по графу зависимостей, никогда вверх.</p>
</div>

Вместе эти правила объясняют, почему всплывающая подсказка, меняющая `background-color`, дешёвая (paint-флаг, один слой), а меняющая `width` — может перекомпоновать всю страницу (layout-флаг каскадирует ко всем зависимым узлам). Радиус взрыва определяется тем, насколько высоко в графе зависимостей флаг стартует и сколько узлов зависит от него ниже.

Если удаётся изолировать изменения в независимом поддереве, инвалидация останавливается на границе изоляции.

## CSS Containment: ограничение радиуса взрыва

CSS Containment даёт ручку, ограничивающую распространение инвалидации.

- **`contain: layout`** — «компоновка этого элемента не может затронуть предков». Если ребёнок ресайзится, сиблинги родителя не нужно перекомпоновывать.
- **`contain: paint`** — отрисовка ограничена коробкой (переполнение клипуется). Браузер может пропустить отрисовку, когда элемент за экраном.
- **`contain: strict`** — комбинирует layout, paint и size containment.

Более новое **`content-visibility: auto`** идёт дальше: говорит браузеру пропустить элемент полностью (без пересчёта стилей, без компоновки, без отрисовки), когда он за экраном. В паре с `contain-intrinsic-size` (даёт браузеру placeholder-размер) это позволяет странице с тысячами элементов ниже первого экрана рендериться за миллисекунды — только видимые элементы платят цену.

Это современные «бесплатные победы» для страниц с длинными списками, infinite scroll-лентами и длинным контентом.

<Inset kind="edgecase" lang="ru">
**Шрифты и FOIT/FOUT.** Веб-шрифт — это входной байт для пайплайна, который часто упускают. Шрифт начинает грузиться, когда CSS-парсер видит `font-family`, использующий `@font-face` с `src`. Пока шрифт не пришёл, браузер либо показывает текст шрифтом-фоллбэком (FOUT, flash of unstyled text), либо прячет текст (FOIT, flash of invisible text). Свойство `font-display` управляет этим: `swap` — фоллбэк сразу, потом сменить; `optional` — фоллбэк, если шрифт не успел за ~100 мс; `block` — спрятать до 3 секунд. При смене шрифта на загруженный браузер запускает пересчёт стилей и компоновку, потому что метрики шрифта меняют ширину текста. На большом текстовом блоке эта смена видна как кадровый jank через 200–500 мс после первой отрисовки.

**Изображения и CLS.** Изображение без указанной ширины и высоты форсирует перекомпоновку, как только грузится: коробка была 0×0, стала фактическим размером, всё вокруг сдвигается — главная причина CLS-регрессий. Лечение — задавать explicit `width`/`height` в HTML или `aspect-ratio` в CSS; браузер резервирует место заранее, и загрузка изображения становится только paint-инвалидацией. Современное `loading="lazy"` грузит off-screen изображения лениво, но всё равно нужны заранее заданные размеры, иначе layout shift в момент попадания во viewport.
</Inset>

<Quiz
  id="03-invalidation-and-cost-quiz-1"
  lessonSlug="03-invalidation-and-cost"
  lang="ru"
  question="Какое изменение свойства инвалидирует только стадию композитинга (без компоновки, без отрисовки)?"
  choices={[
    { label: "`opacity` и `transform` у элемента, уже имеющего свой слой композитора", correct: true },
    { label: "`width` и `height`", misconception: "Width и height влияют на flow — инвалидируют компоновку, которая форсирует отрисовку и композитинг вниз по потоку." },
    { label: "`background-color`", misconception: "Background-color инвалидирует отрисовку (bitmap нужно перерисовать), но не компоновку. Дешевле flow-меняющих изменений, но дороже composite-only." },
    { label: "`top` и `left` у элемента с position: absolute", misconception: "Top и left влияют на flow-геометрию (где коробка сидит относительно контейнера), значит инвалидируют компоновку. Поэтому transform предпочитают для анимаций." },
  ]}
/>

<Quiz
  id="03-invalidation-and-cost-quiz-2"
  lessonSlug="03-invalidation-and-cost"
  lang="ru"
  question="Вы добавляете `contain: layout` к компоненту-карточке. Дочерний элемент внутри неё меняет размер. Какие элементы нужно перекомпоновывать?"
  choices={[
    { label: "Только элементы внутри карточки — contain: layout не даёт инвалидации выйти за границу", correct: true },
    { label: "Карточка и все предки до корня", misconception: "Без contain: layout это было бы правдой. Весь смысл contain: layout — остановить dirty-флаг на границе карточки." },
    { label: "Ничего — contain: layout предотвращает всю компоновку", misconception: "contain: layout предотвращает распространение наружу. Компоновка всё равно происходит внутри contained-поддерева." },
    { label: "Только сиблинги карточки", misconception: "Сиблинги карточки — это именно то, что contain: layout защищает. Без containment они были бы инвалидированы; с ним — в безопасности." },
  ]}
/>

<Quiz
  id="03-invalidation-and-cost-quiz-3"
  lessonSlug="03-invalidation-and-cost"
  lang="ru"
  question="Страница использует `content-visibility: auto` на 10 000 элементов списка ниже первого экрана. Что происходит при первой отрисовке?"
  choices={[
    { label: "Только видимые элементы платят цену стилей, компоновки и отрисовки — off-screen элементы заперты и пропущены", correct: true },
    { label: "Все 10 000 элементов рендерятся обычно, просто за экраном", misconception: "Без content-visibility: auto — да. С ним off-screen элементы display-locked: без пересчёта стилей, без компоновки, без отрисовки до попадания во viewport." },
    { label: "Браузер откладывает JavaScript для off-screen элементов", misconception: "content-visibility: auto влияет на рендеринг, не на исполнение JavaScript. JS для off-screen компонентов всё равно работает нормально." },
    { label: "Off-screen элементы удаляются из DOM", misconception: "Они остаются в DOM; просто не рендерятся. Удаление — это то, что делает виртуализация. content-visibility — оптимизация рендеринга, не управление DOM." },
  ]}
/>

</Explanation>

<KeyTakeaway>Dirty-биты текут вниз от инвалидированной стадии — компоновка запускает отрисовку и композитинг; отрисовка — композитинг. CSS Containment (`contain`, `content-visibility`) останавливает взрыв на границе, превращая общесайтовую перекомпоновку в перекомпоновку поддерева.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="03-invalidation-and-cost-retrieval"
  lang="ru"
  questions={[
    { q: "Какие стадии пайплайна инвалидирует изменение `width`?", a: "Компоновку, затем отрисовку, затем композитинг — все нижележащие стадии перезапускаются, потому что width влияет на flow." },
    { q: "Что говорит браузеру `contain: layout`?", a: "Что компоновка внутри этого элемента не может затронуть предков или сиблингов. Если ребёнок ресайзится, инвалидация останавливается на границе contain." },
    { q: "Каков механизм `content-visibility: auto`?", a: "Display locking: браузер ставит на паузу рендеринг off-screen поддеревьев (без стилей, без компоновки, без отрисовки) и использует placeholder-размер. Когда поддерево пересекает viewport, оно разблокируется и рендерится по требованию." },
  ]}
/>

<Recap lang="ru">
Каждое изменение CSS-свойства инвалидирует ровно одну или несколько стадий пайплайна; dirty-биты текут вниз. `width` инвалидирует компоновку, которая форсирует отрисовку и композитинг. `opacity` на продвинутом элементе инвалидирует только композитинг — поэтому он «бесплатный». CSS Containment (`contain: layout`, `contain: strict`) останавливает взрыв инвалидации на границе. `content-visibility: auto` идёт дальше: off-screen поддеревья полностью пропускаются (display-locked), превращая отрисовку 1200 мс в 10 мс для длинных страниц. Изображения без объявленных размеров вызывают layout-инвалидацию при загрузке — это корневая причина большинства CLS-регрессий. Смена шрифта при загрузке вызывает пересчёт стилей и компоновку из-за смены метрик текста. Теперь, когда потянетесь за CSS-свойством для анимации или переключения состояния, — инстинктивно спросите: это свойство входит в пайплайн на уровне компоновки, отрисовки или только композитинга? Ответ решит, стоит ли смена стиля 0.5 мс или 20 мс.
</Recap>
