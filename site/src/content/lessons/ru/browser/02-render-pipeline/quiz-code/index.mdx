---
concepts:
- layout-thrash
- forced-synchronous-layout
- transform-vs-top
- two-pass-batch
- resize-observer
estMin: 14
lang: ru
level: senior
order: 9
prereqs: []
slug: quiz-code
sources:
- https://web.dev/articles/rendering-performance
- https://gist.github.com/paulirish/5d52fb081b3570c81e3a
- https://csstriggers.com/
status: ready
summary: "Читай реальные сниппеты манипуляции DOM, прогнозируй, какие стадии pipeline перевыполняет каждый, и выбирай фикс с максимальным рычагом до обращения к compositor."
title: 'Render pipeline: чтение кода и трейсов'
track: browser
unit: 02-render-pipeline
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Баги рендеринга диагностируются в коде обработчика и в полосе DevTools, а не в абстракции. Прочитай каждый сниппет, спрогнозируй, какие стадии перевыполняются, и выбери фикс, который senior-инженер делает первым.
</Hook>

<Goal lang="ru">
Отработай цикл, который ты запускаешь в каждом инциденте джанка: прочитай горячий путь, спрогнозируй, где происходит flush layout, и тянись к структурному фиксу — сгруппировать чтения, использовать transform — прежде чем трогать что-либо ещё.
</Goal>

## Сниппет 1 — цикл изменения размера

```javascript
function applyRowWidths(rows, padding) {
  for (const row of rows) {
    const w = row.offsetWidth;            // read: forces a layout flush
    row.style.width = (w + padding) + 'px'; // write: marks layout dirty
  }
}
```

<Quiz
  id="02-render-pipeline-code-1"
  lessonSlug="02-render-pipeline"
  lang="ru"
  question="С 5 000 строк это блокирует main thread на секунды. Что происходит и какой один фикс даёт максимальный рычаг?"
  choices={[
    { label: "Forced synchronous layout: чтение offsetWidth после записи делает flush отложенного layout, а следующая запись снова помечает его dirty — N строк означает N полных layout. Фикс: two-pass batch — сначала прочитать каждый offsetWidth в массив, затем записать все ширины, чтобы layout сделал flush один раз.", correct: true },
    { label: "Мусор — это строки; интернируй px-значения, чтобы сократить аллокации.", misconception: "Аллокация здесь не узкое место. Стоимость — N синхронных flush layout от чередования чтений и записей; группировка чтений перед записями роняет это до одного layout." },
    { label: "Обернуть цикл в requestAnimationFrame, и он сгруппирует в один layout.", misconception: "rAF группирует вызов, а не инвалидацию. Чтение после записи внутри колбэка всё равно форсирует синхронный flush. Нужно разделить чтения и записи." },
    { label: "Заменить style.width на transform: scaleX, чтобы стало composite-only.", misconception: "scaleX избежал бы layout при записи, но чтение offsetWidth всё равно форсирует flush на каждой итерации. Структурная проблема — чередование read/write, а не свойство." },
  ]}
/>

## Сниппет 2 — два кандидата на анимацию

```css
/* A */ .card.move-a { top: 200px; transition: top 300ms; }
/* B */ .card.move-b { transform: translateY(200px); transition: transform 300ms; }
```

<Quiz
  id="02-render-pipeline-code-2"
  lessonSlug="02-render-pipeline"
  lang="ru"
  question="Оба анимируют карточку вниз на 200px за 300ms. Какие стадии перевыполняются покадрово для A против B, и какой ты отправишь в прод?"
  choices={[
    { label: "A инвалидирует layout на каждый тик (затем paint, затем composite), потому что top — flow-affecting — ~18 layout за 300ms. B composite-only: элемент промоучен один раз, и compositor двигает битмап. Отправляй B.", correct: true },
    { label: "Оба composite-only, потому что оба используют transition.", misconception: "transition лишь описывает интерполяцию. top всё ещё flow-affecting, поэтому A перевыполняет layout каждый кадр; только transform/opacity остаются на compositor." },
    { label: "A дешевле, потому что top — более простое свойство, чем матрица transform.", misconception: "Простота свойства нерелевантна. top инвалидирует layout на main thread; transform на promoted layer полностью пропускает layout и paint." },
    { label: "B перерисовывается каждый кадр, потому что transform меняет пиксели.", misconception: "transform двигает уже отрисованный битмап на compositor; содержимое не меняется, поэтому перерисовки нет. Именно поэтому B дёшев." },
  ]}
/>

## Сниппет 3 — чередование в двух хелперах

```javascript
function expand(items) {
  items.forEach((el) => {
    const top = el.offsetTop;             // read
    el.style.height = top / 2 + 'px';     // write
    const h = el.getBoundingClientRect().height; // read again — flush!
    el.style.marginTop = h / 4 + 'px';    // write again
  });
}
```

<Quiz
  id="02-render-pipeline-code-3"
  lessonSlug="02-render-pipeline"
  lang="ru"
  question="DevTools логирует предупреждение Forced reflow while executing JavaScript, указывающее на эту функцию. Сколько forced layout на элемент и каков фикс?"
  choices={[
    { label: "Два forced layout на элемент: offsetTop делает flush один раз, затем getBoundingClientRect после записи height делает flush снова. Фикс: вычислять всё из одного чтения на элемент (или прочитать всю геометрию в первом проходе), затем применить все записи во втором проходе, чтобы layout сделал flush один раз на батч.", correct: true },
    { label: "Один forced layout на элемент, потому что чтения коалесцируются автоматически.", misconception: "Чтения не коалесцируются через запись. Запись между двумя чтениями снова делает layout dirty, поэтому второе чтение форсирует второй flush — два на элемент." },
    { label: "Ноль forced layout; getBoundingClientRect кэшируется браузером.", misconception: "getBoundingClientRect возвращает живую геометрию и форсирует flush отложенных записей. Это одно из канонических чтений, триггерящих layout." },
    { label: "Заменить forEach на for-цикл, чтобы убрать reflow.", misconception: "Конструкция цикла нерелевантна. Reflow берётся из чтения геометрии после записи стилей; фикс — разделение чтений и записей, а не стиль итерации." },
  ]}
/>

## Сниппет 4 — реакция на изменение размера

```javascript
const box = document.querySelector('.panel');
box.addEventListener('transitionend', () => {
  requestAnimationFrame(() => {
    const w = box.offsetWidth;     // inside rAF, before style/layout step
    sibling.style.width = w + 'px';
  });
});
```

<Quiz
  id="02-render-pipeline-code-4"
  lessonSlug="02-render-pipeline"
  lang="ru"
  question="Разработчик использовал rAF, ожидая, что чтение offsetWidth будет бесплатным. Что на самом деле происходит и какой API — правильный инструмент, чтобы прочитать новый размер без forced flush?"
  choices={[
    { label: "rAF выполняется до шага style/layout, поэтому чтение offsetWidth там вынуждает браузер сделать flush отложенного layout, чтобы ответить на чтение — forced sync layout. ResizeObserver срабатывает после layout, но до paint, поэтому его колбэк читает новый размер без лишнего flush.", correct: true },
    { label: "rAF делает чтение бесплатным, потому что выполняется на границе кадра.", misconception: "rAF выполняется до style/layout в кадре; чтение геометрии там всё равно форсирует flush. То, что чтение внутри rAF, не делает его бесплатным." },
    { label: "Использовать IntersectionObserver, чтобы прочитать размер после layout.", misconception: "IntersectionObserver срабатывает асинхронно между кадрами и сообщает о пересечении, а не о размере элемента. Это не инструмент для чтения размера в том же кадре; для этого ResizeObserver." },
    { label: "Использовать MutationObserver, который срабатывает после вычисления нового размера layout.", misconception: "MutationObserver срабатывает на мутации DOM, а не после layout. Чтение геометрии в его колбэке само может форсировать flush. ResizeObserver — это хук после layout." },
  ]}
/>

<Recap lang="ru">
Каждый инцидент рендеринга читается в коде: чередующиеся чтения геометрии и записи стилей форсируют один flush layout на чтение, так что N строк стоят N layout — сгруппируй все чтения перед всеми записями. top анимируется на main thread, transform на compositor, поэтому для движения предпочитай transform. rAF выполняется до шага style/layout, так что чтения там всё равно делают flush; ResizeObserver — хук после layout и до paint для обновлений, реагирующих на размер. Спрогнозируй стадию из свойства и порядка чтения, исправь структурно, затем подтверди в полосе DevTools.
</Recap>
