---
concepts:
- performance-panel
- flame-strip
- frame-lifecycle
- raf
- resize-observer
- intersection-observer
- microtask
- task
- scheduler-yield
- vsync
- contain
- content-visibility
deepensInto:
- 06-layout-thrash
- 08-observability-and-attack-surface
estMin: 16
lang: ru
lessonType: topic
level: middle
order: 5
prereqs:
- 04-compositor-layers
slug: 05-devtools-and-frame-lifecycle
sources:
- https://developer.chrome.com/docs/devtools/performance/reference
- https://html.spec.whatwg.org/multipage/webappapis.html#event-loops
- https://developer.chrome.com/blog/renderingng-architecture
spiral:
- statefulness
status: ready
summary: Как читать флейм-стрип Performance, что делают contain/content-visibility,
  точный порядок событий внутри одного кадра и чем микрозадачи отличаются от задач.
title: Флейм-стрип DevTools и жизненный цикл кадра
track: browser
unit: 02-render-pipeline
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
Вы открываете DevTools Performance, записываете скролл и видите море цветных полос. Некоторые красные. Большинство жёлтые. Вы знаете, что jank где-то там — но не понимаете, какую полосу чинить. Флейм-стрип — это карта. Этот урок учит её читать.
</Hook>

<Crux>Флейм-стрип кодирует стадии цветами; rAF до стилей, ResizeObserver после компоновки, микрозадачи между шагами.</Crux>

<Explanation>

## Чтение флейм-стрипа Performance в DevTools

Откройте DevTools → Performance, нажмите запись, воспроизведите jank, остановите.

**Цвета дорожек (главный поток):**
- **Жёлтый** — scripting (исполнение JavaScript)
- **Фиолетовый** — rendering (пересчёт стилей + компоновка)
- **Зелёный** — paint
- **Тёмно-синий** — compositing

**Цвета дорожек (кадры):**
- **Зелёный** — кадр вовремя
- **Жёлтый** — опоздавший кадр
- **Красный** — пропущенный кадр

Флейм-стрип укладывает вызовы сверху вниз: верхняя полоса — то, что вызвал JS; каждая ниже — то, что вызвала она. Найдите длинную красную или жёлтую полосу — это ваше бутылочное горлышко. Наведите для точного значения в мс.

<div data-lesson-visual class="my-6 rounded-lg border border-border bg-surface-2 p-4 text-sm">
  <p class="font-semibold mb-3">Распространённые паттерны DevTools → диагноз</p>
  <div class="space-y-3">
    <div class="rounded border border-border bg-surface p-2">
      <p class="font-semibold text-yellow-600 dark:text-yellow-400">Жёлтая JS-полоса → фиолетовая Layout-полоса</p>
      <p class="text-ink-2 text-xs mt-1">JS вызвал форсированную синхронную компоновку. Самый частый баг производительности рендеринга.</p>
    </div>
    <div class="rounded border border-border bg-surface p-2">
      <p class="font-semibold text-purple-600 dark:text-purple-400">Широкая фиолетовая Recalculate Style после смены класса высоко в дереве</p>
      <p class="text-ink-2 text-xs mt-1">Вы каскадировали инвалидацию стилей на слишком много потомков. Перенесите смену класса ниже или используйте contain: style.</p>
    </div>
    <div class="rounded border border-border bg-surface p-2">
      <p class="font-semibold text-green-600 dark:text-green-400">Широкая зелёная Paint после изменения filter: blur(...)</p>
      <p class="text-ink-2 text-xs mt-1">Вы превысили сложность отрисовки. filter тяжёл — каждый пиксель требует мультипиксельной выборки.</p>
    </div>
    <div class="rounded border border-border bg-surface p-2">
      <p class="font-semibold text-blue-600 dark:text-blue-400">Широкий тёмно-синий Composite Layers, когда всё остальное выглядит нормально</p>
      <p class="text-ink-2 text-xs mt-1">Слишком много слоёв — часто из-за злоупотребления will-change. Проверьте Layer Borders.</p>
    </div>
  </div>
</div>

## Жизненный цикл кадра: точный порядок

Внутри одного кадра браузер выполняет работу в строгом порядке, заданном HTML-спецификацией:

1. **Сбор input-событий** (mousemove, keypress)
2. **Запуск setTimeout / setInterval-колбэков**, если их время пришло
3. **Запуск микрозадач** (promise-резолюции) до опустошения очереди
4. **Запуск requestAnimationFrame-колбэков**
5. **Запуск ResizeObserver и IntersectionObserver-колбэков**
6. **Пересчёт стилей и компоновка**
7. **Отрисовка**
8. **Композитинг**

После шага 8 браузер отдаёт управление ОС и ждёт следующего vsync.

**Ключевое свойство:** rAF запускается *до* стилей/компоновки, поэтому запись внутри rAF приводит ровно к одному проходу компоновки на кадр — в этом весь смысл rAF. ResizeObserver срабатывает *после* компоновки, но *до* отрисовки: можно реагировать на изменения компоновки без форсированной повторной компоновки. IntersectionObserver срабатывает асинхронно между кадрами, не блокируя текущий.

<FlowDiagram
  label="Жизненный цикл одного кадра в порядке HTML-спецификации: input-события, затем готовые таймеры, затем опустошается очередь микрозадач, затем колбэки requestAnimationFrame, затем ResizeObserver и IntersectionObserver, затем стили и компоновка, затем отрисовка, затем композитинг, затем браузер ждёт следующего vsync для показа."
  perRow={5}
  hue="var(--d-frontend)"
  nodes={[
    { id: "input", label: "Input", sub: "события" },
    { id: "timers", label: "Таймеры", sub: "setTimeout" },
    { id: "micro", label: "Микрозадачи", sub: "до опустошения" },
    { id: "raf", label: "rAF", sub: "колбэки" },
    { id: "observers", label: "Observers", sub: "Resize / Intersection" },
    { id: "stylelayout", label: "Стили + компоновка", sub: "пересчёт, измерение" },
    { id: "paint", label: "Отрисовка", sub: "растеризация" },
    { id: "composite", label: "Композитинг", sub: "композитор" },
    { id: "vsync", label: "vsync", sub: "показ" },
  ]}
  edges={[
    { from: "input", to: "timers" },
    { from: "timers", to: "micro" },
    { from: "micro", to: "raf" },
    { from: "raf", to: "observers" },
    { from: "observers", to: "stylelayout" },
    { from: "stylelayout", to: "paint" },
    { from: "paint", to: "composite" },
    { from: "composite", to: "vsync" },
  ]}
  caption="Браузер выполняет каждый кадр в этом фиксированном порядке. rAF (шаг 4) приходится прямо перед стилями + компоновкой, поэтому запись там стоит ровно один проход компоновки; кадр завершается передачей скомпонованного результата в vsync для показа."
/>

## Микрозадачи vs задачи: границы планирования

Очередь микрозадач (промисы, `queueMicrotask`) **опустошается полностью** между каждым шагом event loop, в том числе между rAF-колбэками.

Некорректная promise-цепочка, планирующая себя бесконечно, голодит рендеринг — браузер не может добраться до шага 6, пока очередь не опустеет. Именно поэтому длинные promise-цепочки и тесные `await`-циклы отображаются в DevTools как единая непрерывная жёлтая scripting-полоса, а не серия коротких.

Современный код, желающий уступить рендереру в середине задачи, использует:
- `scheduler.yield()` (Scheduler API, Chrome 115+) — создан именно для этого
- `setTimeout(fn, 0)` — помещает работу в очередь макрозадач и разблокирует рендеринг

## Реактивные фреймворки и пайплайн

Когда React рендерит, DOM-обновления происходят на главном потоке, затем пайплайн запускает пересчёт стилей и компоновку на изменённых узлах. Сама реконсиляция — чистый JS, она отображается жёлтым в DevTools. Если ре-рендер затрагивает узел у вершины дерева, инвалидация стилей каскадирует вниз; затрагивает изолированный лист — инвалидация локальна.

**Ключевая практика:** держите состояние локальным, как можно ближе к листу. `useState` в листовом компоненте инвалидирует один узел; `useContext` в провайдере верхнего уровня может инвалидировать сотни. Тот же принцип работает для Svelte-stores, MobX-observables, Vue refs — каждая реактивная модель в итоге пишет в DOM, а DOM-запись запускает тот же пайплайн.

<Inset kind="why" lang="ru">
Понимание порядка жизненного цикла кадра отделяет «трюки планирования, которые работают» от «трюков, которые незаметно трэшат». Частая ошибка: запись CSS-свойства внутри `setTimeout(fn, 0)` в расчёте, что она забатчится с rAF-записями. Не забатчится — `setTimeout` срабатывает на шаге 2, *до* rAF на шаге 4, поэтому запись происходит на шаг раньше rAF-записей и не может быть с ними забатчена. Всегда помещайте визуальные записи внутрь rAF, а не в макрозадачные колбэки.
</Inset>

<DragOrder
  id="05-devtools-and-frame-lifecycle-drag-1"
  lessonSlug="05-devtools-and-frame-lifecycle"
  lang="ru"
  prompt="Расставьте события в порядке их появления внутри одного кадра, согласно шагам рендеринга из HTML-спецификации."
  items={[
    { id: "input", label: "Браузер обрабатывает input-события (mousemove, keypress)" },
    { id: "timer", label: "setTimeout / setInterval-колбэки срабатывают, если пришло время" },
    { id: "microtasks", label: "Микрозадачи опустошаются (обработчики promise.then)" },
    { id: "raf", label: "requestAnimationFrame-колбэки запускаются" },
    { id: "observers", label: "ResizeObserver / IntersectionObserver-колбэки срабатывают" },
    { id: "stylelayout", label: "Пересчитываются стили и компоновка" },
    { id: "paint", label: "Отрисовка выполняется на главном потоке" },
    { id: "composite", label: "Поток композитора собирает и отправляет кадр" },
  ]}
  correctOrder={["input", "timer", "microtasks", "raf", "observers", "stylelayout", "paint", "composite"]}
/>

<Quiz
  id="05-devtools-and-frame-lifecycle-quiz-1"
  lessonSlug="05-devtools-and-frame-lifecycle"
  lang="ru"
  question="В панели Performance DevTools жёлтая JS-полоса сразу за ней фиолетовая Layout-полоса сопоставимой длины. Что произошло?"
  choices={[
    { label: "JS вызвал форсированную синхронную компоновку — прочитал геометрическое свойство после записи стиля", correct: true },
    { label: "Браузер по дизайну планирует компоновку после каждой JS-задачи", misconception: "Компоновка не запускается после каждой JS-задачи. Она запускается только при установленном dirty-флаге компоновки, а форсированная синхронная компоновка происходит только когда JS читает геометрию после записи стиля." },
    { label: "CSS-анимация запустилась и потребовала компоновки за кадр", misconception: "CSS-анимации на composite-only свойствах (transform, opacity) вообще не показывают фиолетовую Layout-полосу. Анимации, инвалидирующие компоновку, показывают компоновочные полосы, но они приходят из движка CSS-анимаций, а не JS." },
    { label: "JavaScript распарсил большой JSON-объект", misconception: "JSON.parse отображается как жёлтая scripting-полоса без следующей за ней фиолетовой Layout-полосы. Парсинг JS-объектов не трогает движок компоновки." },
  ]}
/>

<Quiz
  id="05-devtools-and-frame-lifecycle-quiz-2"
  lessonSlug="05-devtools-and-frame-lifecycle"
  lang="ru"
  question="Бесконечный `await`-цикл в promise-цепочке работает 200 мс. Что показывает панель Performance DevTools?"
  choices={[
    { label: "Одна длинная непрерывная жёлтая полоса — микрозадачи опустошаются до рендеринга, поэтому рендерер голодает", correct: true },
    { label: "Много коротких жёлтых полос, разделённых зелёными paint-полосами", misconception: "Такой паттерн был бы при уступке микрозадач рендеру. Но микрозадачи опустошаются до рендеринга — бесконечный цикл никогда не уступает." },
    { label: "Фиолетовая Recalculate Style, потому что промисы трогают CSSOM", misconception: "Промисы — чистый JS-механизм, не связанный с CSSOM. Бесконечный цикл промисов отображается как жёлтая scripting-полоса, не фиолетовая." },
    { label: "Чередующиеся жёлтые и синие полосы", misconception: "Синие полосы — это compositing. Compositing не может перемежаться с заблокированным главным потоком, потому что compositor работает на своём потоке — но этот паттерн не соответствует голоданию от микрозадач." },
  ]}
/>

<Quiz
  id="05-devtools-and-frame-lifecycle-quiz-3"
  lessonSlug="05-devtools-and-frame-lifecycle"
  lang="ru"
  question="Вы хотите прочитать новый размер изменённого элемента и обновить стиль сиблинга в ответ. Какой observer срабатывает в нужное время, чтобы не вызвать вторую компоновку?"
  choices={[
    { label: "ResizeObserver — срабатывает после компоновки, но до отрисовки, позволяя реагировать на изменения размера без форсированной повторной компоновки", correct: true },
    { label: "IntersectionObserver", misconception: "IntersectionObserver срабатывает асинхронно между кадрами, а не синхронно после компоновки. Его нельзя использовать для одно-кадровых обновлений, реагирующих на размер." },
    { label: "MutationObserver", misconception: "MutationObserver срабатывает при DOM-мутациях — не конкретно после вычисления новых размеров компоновкой. Чтение геометрии в колбэке MutationObserver может вызвать форсированную компоновку." },
    { label: "requestAnimationFrame", misconception: "rAF запускается *до* стилей и компоновки (шаг 4), поэтому чтение размеров элементов внутри rAF заставит браузер сбросить ожидающую компоновку для ответа на чтение — форсированная синхронная компоновка." },
  ]}
/>

</Explanation>

<KeyTakeaway>Флейм-стрип отображает стадии пайплайна цветами: жёлтый=JS, фиолетовый=компоновка, зелёный=отрисовка, синий=composite. rAF запускается до компоновки; ResizeObserver после. Микрозадачи опустошаются между каждым шагом — бесконечный promise-цикл голодит рендеринг.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="05-devtools-and-frame-lifecycle-retrieval"
  lang="ru"
  questions={[
    { q: "В каком порядке срабатывают внутри одного кадра: rAF, setTimeout, микрозадачи, ResizeObserver, style/layout?", a: "setTimeout → микрозадачи → rAF → ResizeObserver → style/layout. (Сначала input-события, затем таймеры, затем микрозадачи опустошаются, затем rAF, затем observers, затем style+layout, затем paint, затем composite.)" },
    { q: "Почему бесконечная promise-цепочка голодает рендеринг?", a: "Очередь микрозадач опустошается полностью до того, как браузер может перейти к шагу 6 (style/layout). Самопланирующийся промис бесконечно поддерживает очередь непустой, поэтому рендеринг никогда не запускается." },
    { q: "Какого цвета полосы компоновки в панели Performance, и какого — scripting?", a: "Полосы компоновки (и пересчёта стилей) — фиолетовые. Полосы scripting (JavaScript) — жёлтые. Жёлтая полоса, сразу за которой следует фиолетовая схожей длины — классическая сигнатура форсированной синхронной компоновки." },
  ]}
/>

<Recap lang="ru">
Панель Performance DevTools кодирует стадии пайплайна цветами: жёлтый для JS, фиолетовый для компоновки/стилей, зелёный для отрисовки, тёмно-синий для composite. Жизненный цикл кадра зафиксирован HTML-спецификацией — rAF срабатывает до style/layout, ResizeObserver после компоновки перед отрисовкой. Микрозадачи полностью опустошаются между каждым шагом, поэтому бесконечный promise-цикл голодает рендерер. Для уступки в середине задачи используйте `scheduler.yield()` (Chrome 115+) или `setTimeout(fn, 0)`. Реактивные фреймворки запускают тот же пайплайн при DOM-записях; держите состояние локальным ближе к листовым компонентам — это минимизирует каскад инвалидации стилей.
</Recap>
