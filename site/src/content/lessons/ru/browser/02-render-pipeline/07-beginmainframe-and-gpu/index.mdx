---
slug: 07-beginmainframe-and-gpu
lang: ru
track: browser
unit: 02-render-pipeline
order: 7
title: "BeginMainFrame, анимации на потоке compositor и память GPU"
summary: "Как работает двухпоточное рукопожатие Chromium, почему CSS-анимации на transform/opacity выживают при заблокированном главном потоке, и что происходит при исчерпании памяти GPU."
estMin: 14
status: ready
lessonType: topic
level: senior
prereqs: ["06-layout-thrash"]
deepensInto: ["08-observability-and-attack-surface"]
spiral: ["statefulness"]
concepts: ["BeginMainFrame", "CommitMainFrame", "compositor-driven-animation", "rAF-animation", "gpu-memory-eviction", "passive-scroll", "will-change-lifecycle", "tradeoff-matrix"]
sources:
  - https://developer.chrome.com/blog/inside-browser-part3
  - https://developer.chrome.com/blog/renderingng-architecture
  - https://drafts.csswg.org/css-will-change/
  - https://web.dev/articles/rendering-performance
---

import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";

<Hook>
CSS-анимация работает плавно на 60 fps, пока главный поток заблокирован на 400 мс парсингом JSON-блоба. rAF-анимация на том же элементе замерзает на те же 400 мс. Одно железо, одно свойство, противоположные результаты — потому что одна передаёт управление compositor, другая нет.
</Hook>

<Crux>BeginMainFrame — каждые 16.67 мс; при задержке главного потока отправляется старый кадр. Composite-only CSS-анимации обходят главный поток.</Crux>

<Explanation>

## Рукопожатие BeginMainFrame

В процессе рендерера Chromium **поток compositor является драйвером**, а главный поток — исполнителем. Поток за каждый vsync:

1. Compositor посылает `BeginMainFrame` главному потоку
2. Главный поток выполняет всю покадровую работу: rAF-колбэки, стили, компоновку, подготовку отрисовки
3. Главный поток посылает `CommitMainFrame` обратно, передавая новое дерево слоёв
4. Compositor просит растровых воркеров заполнить грязные тайлы bitmap-ами
5. Compositor выполняет GPU-draw и отображает кадр

Если главный поток слишком медленный, **compositor не ждёт**: он отправляет дерево слоёв предыдущего кадра снова («нарисованный, но устаревший» кадр). Пользователь воспринимает это как пропущенный ввод или заикание.

`BeginMainFrame` приходит каждые ~16.67 мс, готов главный поток или нет. Это глубинная причина, почему работа главного потока не может превышать бюджет кадра: метроном неустанен.

<div data-lesson-visual class="my-6 rounded-lg border border-border bg-surface-2 p-4 text-sm">
  <p class="font-semibold mb-3">Поток BeginMainFrame</p>
  <div class="flex flex-col gap-1">
    <div class="flex gap-2 items-center">
      <span class="w-40 text-xs text-ink-2 text-right">Compositor</span>
      <span class="font-mono text-xs px-2 py-1 rounded bg-blue-100 dark:bg-blue-900/30">BeginMainFrame →</span>
    </div>
    <div class="flex gap-2 items-center">
      <span class="w-40 text-xs text-ink-2 text-right">Главный поток</span>
      <span class="font-mono text-xs px-2 py-1 rounded bg-yellow-100 dark:bg-yellow-900/30">rAF + style + layout + paint setup</span>
    </div>
    <div class="flex gap-2 items-center">
      <span class="w-40 text-xs text-ink-2 text-right">Главный поток</span>
      <span class="font-mono text-xs px-2 py-1 rounded bg-yellow-100 dark:bg-yellow-900/30">← CommitMainFrame</span>
    </div>
    <div class="flex gap-2 items-center">
      <span class="w-40 text-xs text-ink-2 text-right">Compositor</span>
      <span class="font-mono text-xs px-2 py-1 rounded bg-blue-100 dark:bg-blue-900/30">растровые воркеры заполняют грязные тайлы → GPU draw</span>
    </div>
    <div class="mt-2 font-mono text-xs text-ink-2">Если главный поток опаздывает: compositor отправляет предыдущий кадр («устаревший кадр»)</div>
  </div>
</div>

## CSS-анимации на потоке compositor

Когда вы пишете CSS-анимацию, изменяющую только `transform` или `opacity` на элементе с продвижением compositor, браузер обнаруживает это в начале анимации и **передаёт таймлайн анимации напрямую потоку compositor**. С этого момента главный поток вообще не участвует — compositor интерполирует значение кадр за кадром и рендерит следующий bitmap напрямую.

Вот почему CSS-анимация может работать плавно, даже когда главный поток полностью заблокирован (alert-диалог, долгий синхронный парсинг, пауза дебаггера): compositor всё равно работает.

Та же анимация, написанная на JS через rAF, **не может** этого делать, потому что rAF работает на главном потоке. Заблокированный главный поток замораживает rAF-анимацию.

CSS-анимации на потоке compositor — **единственный способ** гарантировать непрерывность анимации под нагрузкой на главный поток.

## Исчерпание памяти GPU и вытеснение слоёв

Слои не бесплатны. Каждый слой — GPU-bitmap, стоящий ширина × высота × 4 байта. На телефоне с 256 МБ GPU-памяти пятьдесят слоёв 1080p исчерпывают бюджет. Когда ОС начинает вытеснять тайлы:

1. Браузер обнаруживает вытесненные тайлы
2. Растровые воркеры растеризуют их повторно на лету
3. Пока растеризация идёт, compositor компонует с неполными тайлами
4. Страница заикается — часто хуже, чем если бы слои вообще не запрашивались

Анти-паттерн `will-change` вызывает это: `will-change: transform` на каждом компоненте постоянно резервирует слой для каждого экземпляра. Список из 100 карточек с 5 элементами с will-change каждая держит 500 слоёв.

**Правильный паттерн:** устанавливайте `will-change` непосредственно перед началом анимации (на `mouseenter`, или в слушателе `transitionstart`), убирайте на `animationend` или `transitionend`. Это держит GPU-память вблизи нуля когда пользователь не взаимодействует активно.

<Inset kind="why" lang="ru">
Почему `will-change: transform` вызывает продвижение ещё до начала анимации? Браузеру нужно время для растеризации слоя в GPU-память до первого кадра анимации. Если он ждал до начала анимации, первые 1–3 кадра были бы пустыми пока шла растеризация. `will-change` — подсказка «подготовься сейчас, не при старте анимации». Цена — хранение bitmap на протяжении действия подсказки — вот почему подсказку нужно убирать по окончании анимации.
</Inset>

<TradeoffMatrix
  id="07-beginmainframe-and-gpu-tradeoff"
  lessonSlug="07-beginmainframe-and-gpu"
  lang="ru"
  prompt="Анимировать карточку с y=0 до y=200 за 300 мс при 60 fps. Выберите реализацию."
  options={[
    { name: "CSS-анимация на transform: translateY(...)", summary: "Composite-only, нет нагрузки на главный поток.", correct: true, justification: "Изменения transform не инвалидируют компоновку или отрисовку. Элемент продвигается в слой один раз; compositor перемещает GPU-bitmap следующие 18 кадров. Главный поток свободен делать что угодно. Это дефолт для почти каждой UI-анимации." },
    { name: "CSS-анимация на top: 0 → 200px", summary: "Компоновка каждый кадр.", justification: "Top — flow-влияющее свойство. Каждый тик анимации инвалидирует компоновку карточки и любого сиблинга, зависящего от неё. 18 компоновок за 300 мс. На сложной странице это разница между 60 fps и 20 fps." },
    { name: "transform: translateY с will-change: transform, установленным при монтировании и никогда не удаляемым", summary: "Утечка GPU-памяти.", justification: "will-change резервирует слой на время жизни элемента. Список из 100 карточек постоянно держит 100 слоёв — возможно, 200 МБ GPU-памяти. Удалённые карточки могут не освобождать слой немедленно. Лечение — устанавливать will-change перед анимацией и убирать на animationend." },
    { name: "Цикл requestAnimationFrame, устанавливающий element.style.top каждый кадр", summary: "Худшее из обоих.", justification: "rAF всё равно поднимает dirty-флаги компоновки через записи стилей в top. Вы также теряете анимационно-осведомлённое планирование браузера. Та же цена, что у второго варианта, плюс больше кода." },
  ]}
/>

<Quiz
  id="07-beginmainframe-and-gpu-quiz-1"
  lessonSlug="07-beginmainframe-and-gpu"
  lang="ru"
  question="Главный поток заблокирован на 400 мс синхронным JSON.parse. Какой вид анимации продолжает работать плавно на 60 fps во время блокировки?"
  choices={[
    { label: "CSS-анимация на `transform` элемента, продвинутого в собственный слой compositor", correct: true },
    { label: "Цикл `requestAnimationFrame`, инкрементирующий `element.style.transform`", misconception: "rAF работает на главном потоке. Заблокированный главный поток не может диспетчировать rAF-колбэки, поэтому анимация замерзает на все 400 мс." },
    { label: "CSS-анимация на `width`", misconception: "Width инвалидирует компоновку, которая работает на главном потоке. Заблокированный главный поток = нет компоновки = нет анимации." },
    { label: "setInterval, подталкивающий `element.style.left`", misconception: "setInterval работает на главном потоке и записывает инвалидирующее компоновку свойство. Обе части этого подхода провальны: таймер не может сработать, и свойство не может быть применено." },
  ]}
/>

<Quiz
  id="07-beginmainframe-and-gpu-quiz-2"
  lessonSlug="07-beginmainframe-and-gpu"
  lang="ru"
  question="GPU-память раздувается на мобильном устройстве. Профилирование показывает 500 слоёв compositor. Большинство от карточек в списке, каждая с `will-change: transform`, установленным постоянно при монтировании. Каково точечное лечение?"
  choices={[
    { label: "Устанавливать will-change на mouseenter, убирать на animationend — слои существуют только во время активных анимаций", correct: true },
    { label: "Заменить will-change на transform: translateZ(0)", misconception: "transform: translateZ(0) тоже продвигает в слой compositor и имеет ту же постоянную цену памяти, если никогда не убирается. Это хак, страдающий той же проблемой." },
    { label: "Увеличить бюджет GPU-памяти в настройках браузера", misconception: "Бюджет GPU-памяти устанавливается ОС и устройством. Нет настройки браузера для его увеличения. Лечение — уменьшить количество слоёв, а не увеличить бюджет." },
    { label: "Добавить contain: layout каждой карточке для предотвращения создания слоёв", misconception: "contain: layout влияет на распространение компоновки, не на продвижение слоёв. Оно не предотвращает продвижение will-change в слой compositor." },
  ]}
/>

</Explanation>

<KeyTakeaway>Compositor посылает BeginMainFrame каждые ~16.67 мс; если главный поток опаздывает — отправляется предыдущий кадр. CSS-анимации на composite-свойствах полностью обходят главный поток — они выживают при 400-мс блокировке. will-change, ограниченный длительностью анимации — правильный паттерн; постоянный will-change исчерпывает мобильную GPU-память и вызывает вытеснение тайлов.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="07-beginmainframe-and-gpu-retrieval"
  lang="ru"
  questions={[
    { q: "Что делает compositor, если главный поток пропускает дедлайн BeginMainFrame?", a: "Он повторно отправляет дерево слоёв предыдущего кадра (устаревший кадр), сохраняя дисплей работающим на частоте vsync. Пользователь видит пропущенный ввод или заикание — не пустой экран." },
    { q: "Почему CSS transform-анимация выживает при заблокированном главном потоке, а rAF-анимация нет?", a: "Браузер передаёт CSS-анимацию на composite-свойствах напрямую потоку compositor при старте. Compositor интерполирует без участия главного потока. rAF-колбэки работают на главном потоке — заблокированный главный поток их замораживает." },
    { q: "Что такое анти-паттерн will-change и как его лечить?", a: "Установка `will-change: transform` постоянно на каждый компонент. Каждый экземпляр держит GPU-bitmap на время жизни элемента. Лечение: устанавливать на mouseenter (или перед стартом анимации), убирать на animationend. GPU-память возвращается к базовому уровню, когда активных анимаций нет." },
  ]}
/>

<Recap lang="ru">
Compositor Chromium посылает `BeginMainFrame` каждые ~16.67 мс. Главный поток отвечает `CommitMainFrame`, если успевает; если нет — compositor отправляет предыдущий кадр как устаревший. CSS-анимации на `transform` или `opacity` продвинутых элементов передаются compositor при старте — главный поток вне игры, поэтому они выживают при заблокированном главном потоке на 60 fps. rAF-анимации работают на главном потоке и замерзают при блокировке. Анти-паттерн `will-change` постоянно резервирует GPU-bitmap; на телефоне с 256 МБ GPU-памяти 500 слоёв от дизайн-системы исчерпывают бюджет, ОС вытесняет тайлы. Ограничивайте `will-change` длительностью анимации, чтобы держать GPU-память вблизи нуля в покое.
</Recap>
