---
concepts:
- compositor-layer
- layer-promotion
- will-change
- overlap-rule
- layer-squashing
- stacking-context
- gpu-memory
- layer-borders
deepensInto:
- 07-beginmainframe-and-gpu
- 08-observability-and-attack-surface
estMin: 14
lang: ru
lessonType: topic
level: middle
order: 4
prereqs:
- 03-invalidation-and-cost
slug: 04-compositor-layers
sources:
- https://drafts.csswg.org/css-will-change/
- https://developer.chrome.com/blog/inside-browser-part3
- https://developer.chrome.com/docs/devtools/performance/reference
spiral:
- statefulness
- encapsulation
status: ready
summary: Как элементы попадают в слои композитора, почему один transform может утащить
  за собой дюжину соседей, и во сколько обходится слой в памяти GPU.
title: 'Слои композитора: продвижение, перекрытие и память GPU'
track: browser
unit: 02-render-pipeline
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import MetaphorComplete from "~/components/pedagogy/MetaphorComplete.astro";
import NumberDrill from "~/components/pedagogy/NumberDrill.astro";
import StackDiagram from "~/components/diagram/StackDiagram.astro";

<Hook>
Вы добавляете `will-change: transform` одной карточке в списке — чтобы анимация ховера была плавной. Панель Layer Borders в DevTools загорается сорока цветными рамками, о которых вы не просили. Браузер повысил соседей, потому что не мог иначе.
</Hook>

<Crux>Слой compositor — GPU-bitmap: дёшево перемещать, дорого хранить — правило перекрытия создаёт слои, которых вы не просили.</Crux>

<Explanation>

## Как элементы попадают к композитору

Одни элементы получают собственный слой композитора:

- `transform: translate3d(...)` и любой 3D-transform
- `will-change: transform` (или любое компонуемое свойство)
- `position: fixed`
- `<video>`, `<canvas>`
- Анимируемый прямо сейчас `opacity`
- `filter: blur(...)` и другие графические фильтры
- Изолированные stacking-контексты, перекрывающие другой слой

Каждый слой растеризуется один раз (отрисовка), загружается в GPU, а потом дёшево рекомпонуется следующие тысячи кадров. Анимировать `transform` слоя «бесплатно» — главный поток может спать. Анимировать его `top` дорого — главный поток пересчитывает мир каждый кадр.

<div data-lesson-visual class="my-6 rounded-lg border border-border bg-surface-2 p-4 text-sm">
  <p class="font-semibold mb-3">Жизненный цикл слоя</p>
  <div class="flex flex-col gap-2">
    <div class="rounded border border-border bg-surface p-2">1. Элемент получает триггер продвижения (transform3d, will-change и т.д.)</div>
    <div class="text-center text-ink-2">↓</div>
    <div class="rounded border border-border bg-surface p-2">2. Браузер рисует пиксели элемента в GPU-bitmap (разовая цена)</div>
    <div class="text-center text-ink-2">↓</div>
    <div class="rounded border border-border bg-surface p-2">3. Композитор перемещает/смешивает bitmap каждый кадр — главный поток не участвует</div>
    <div class="text-center text-ink-2">↓</div>
    <div class="rounded border border-border bg-surface p-2">4. Память GPU держит bitmap, пока слой не уничтожится (элемент удалён или триггер снят)</div>
  </div>
</div>

Шаги 1–2 происходят один раз; шаги 3–4 повторяются каждый кадр. В этом весь экономический аргумент в пользу слоёв: платим за растеризацию один раз при продвижении, дальше анимируем бесплатно. Без шага 4 выгода исчезает — если никогда не снять триггер, GPU держит bitmap всё время жизни элемента.

<StackDiagram
  label="Отдельные слои композитора, сложенные и смешиваемые на GPU, верхний слой первым: header с position:fixed, карточка с will-change:transform, модалка с анимируемым opacity, элемент video и базовый слой документа. Каждый слой — заранее отрисованный GPU-bitmap, а примечание показывает, что его повысило."
  hue="var(--d-frontend)"
  layers={[
    { label: "Фиксированный header", note: "position: fixed" },
    { label: "Анимируемая карточка", note: "will-change: transform" },
    { label: "Затухающая модалка", note: "анимируемый opacity" },
    { label: "Элемент video", note: "<video>" },
    { label: "Базовый слой документа", note: "без продвижения" },
  ]}
  caption="GPU держит каждый повышенный элемент как собственный заранее отрисованный bitmap и смешивает их сверху вниз в один кадр. Примечание на каждом слое — триггер, который его повысил (transform / will-change / opacity / video); базовый слой держит всё непродвинутое."
/>

## Правило перекрытия: слои, которых вы не просили

Композитор обязан сохранять визуальную корректность. Если элемент A — слой (скажем, с `transform`) и элемент B рисуется поверх A в document order без собственного слоя, композитор не сможет разрешить их z-стек: слой A окажется поверх B, хотя B должен быть выше.

Браузер решает это, повышая B до собственного слоя, нередко утащив за собой соседние элементы. Затем компоzитор **сжимает** соседние неанимируемые слои в общий «squashed»-слой, чтобы ограничить взрыв, но эвристика несовершенна.

Один невинный `will-change: transform` на карточке в середине ленты может повысить дюжину соседей.

Используйте Rendering-панель DevTools → **«Layer Borders»**, чтобы видеть, что повышено. Обращайте внимание на слои с метками «compositor-induced» или «overlap» — они пробрались без вашего ведома.

## `will-change` как обоюдоострое оружие

`will-change: transform` — подсказка браузеру: «Я скоро анимирую это свойство, заранее подготовь bitmap, чтобы композитор мог двигать его, не возвращаясь к компоновке или отрисовке». Браузер немедленно повышает элемент.

Цена: **память GPU** — ширина × высота × 4 байта на слой.

**Анти-паттерн will-change:** `will-change: transform` на каждом компоненте в дизайн-системе постоянно резервирует слой для каждого экземпляра. В списке из 100 карточек с 5 элементами с will-change каждая — страница держит 500 слоёв, возможно, сотни МБ памяти GPU. На телефоне с 256 МБ GPU-памяти ОС начинает вытеснять; браузер растеризует повторно на лету, и страница заикается сильнее, чем без слоёв.

**Лечение:** устанавливайте `will-change` непосредственно перед началом анимации (например, на `mouseenter`) и убирайте на `animationend`. Относитесь к нему как к сигналу «вперёд», а не постоянной настройке.

<Inset kind="edgecase" lang="ru">
Правило перекрытия взаимодействует со stacking-контекстами z-index неочевидным образом. Элемент с `z-index: 1` и `position: relative` создаёт изолированный stacking-контекст. Если этот контекст перекрывает слой композитора, вся его закрашенная область должна быть повышена, чтобы избежать межслоевого кровотечения. Вот почему добавление `position: relative; z-index: 1` к всплывающей подсказке над анимированным фоном может вызвать полностраничный каскад слоёв в сложных layouts. Решение — держать повышаемые элементы (анимируемые, fixed, video) в отдельном stacking-контексте от непродвигаемых соседей, или использовать `isolation: isolate` явно, чтобы ограничить эффекты stacking-контекста.
</Inset>

<MetaphorComplete
  id="04-compositor-layers-metaphor-1"
  lessonSlug="04-compositor-layers"
  lang="ru"
  setup="В метафоре кухни слой — это готовая тарелка, стоящая на раздаче: уже приготовлена, готова для раздатчика. Какое CSS-свойство превращает обычный элемент в такую «предварительно приготовленную тарелку», чтобы композитор мог перемещать её без повторной готовки?"
  accepted={["will-change", "will-change: transform", "css will-change"]}
  canonical="will-change"
  explanation="CSS-свойство `will-change` — подсказка браузеру: «Я скоро анимирую это свойство, подготовь bitmap заранее, чтобы композитор мог двигать его без возврата к компоновке или отрисовке». Браузер реагирует, немедленно повышая элемент до собственного слоя. Цена — память GPU (один bitmap на слой); выгода — последующие изменения transform/opacity касаются только композитора. Используйте в анимации и убирайте после. Постоянный `will-change: transform` на каждом компоненте постоянно тратит GPU-память на гипотетические анимации, которые могут никогда не выполниться."
/>

<NumberDrill
  id="04-compositor-layers-drill-1"
  lessonSlug="04-compositor-layers"
  lang="ru"
  question="Элемент 1920 × 1080 продвинут в собственный слой композитора. Каждый пиксель — 4 байта (RGBA). Сколько мегабайт GPU-памяти занимает этот единственный слой?"
  expected={7.91}
  tolerance={0.2}
  unit="МБ"
  explanation="1920 × 1080 = 2 073 600 пикселей. Каждый пиксель 4 байта: 2 073 600 × 4 = 8 294 400 байт. Делим на 1 048 576 (байт в МБ) = 7,91 МБ. Умножьте на N слоёв: 30 полноэкранных слоёв — ~237 МБ GPU-памяти. На телефоне с 256 МБ GPU-памяти это весь бюджет, и ОС начнёт вытеснять тайлы. Вот почему «просто добавьте will-change везде для надёжности» — самый дорогостоящий анти-паттерн производительности на мобильных."
/>

<Quiz
  id="04-compositor-layers-quiz-1"
  lessonSlug="04-compositor-layers"
  lang="ru"
  question="Команда добавляет `will-change: transform` каждой карточке в дизайн-системе. GPU-память на мобильном раздувается. Каков механизм?"
  choices={[
    { label: "Каждая карточка продвигается в слой, каждый держит GPU-bitmap по размеру отрисованной области элемента. При сотнях экземпляров GPU-память исчерпывается.", correct: true },
    { label: "will-change заставляет браузер запускать больше JavaScript за кадр", misconception: "will-change — CSS-подсказка для рендеринга. Она не запускает JS. Цена — память GPU, а не CPU." },
    { label: "Браузер перерисовывает каждую карточку каждый кадр, потому что will-change сигнализирует о скором изменении", misconception: "will-change продвигает элемент в слой именно для того, чтобы НЕ нужно было перерисовывать. Элемент рисуется один раз. Цена памяти — хранение bitmap, не его повторная отрисовка." },
    { label: "will-change: transform форсирует компоновку каждый кадр", misconception: "will-change: transform — composite-only. Оно специально предотвращает компоновку и отрисовку каждый кадр. Цена памяти — хранение GPU-bitmap." },
  ]}
/>

<Quiz
  id="04-compositor-layers-quiz-2"
  lessonSlug="04-compositor-layers"
  lang="ru"
  question="Элемент A имеет `will-change: transform`. Элемент B перекрывает A в document order. У элемента B нет триггера продвижения. Что делает браузер?"
  choices={[
    { label: "Продвигает B в собственный слой (или сжимает в общий) для сохранения корректного визуального стека", correct: true },
    { label: "Оставляет B непродвинутым и рендерит в неправильном порядке", misconception: "Браузер обязан сохранять визуальную корректность. Оставить B непродвинутым при перекрытии слоя означало бы рендер A поверх B, хотя B должен быть выше." },
    { label: "Убирает слой у A, чтобы избежать конфликта", misconception: "Браузер уважает подсказки продвижения (will-change). Он не убирает слои для избежания перекрытия. Вместо этого он продвигает перекрывающий элемент." },
    { label: "Продвигает B только если у него есть `position: relative`", misconception: "Позиция — не решающий фактор. Решающий — перекрытие слоя. position: relative само по себе не запускает продвижение; это делает перекрытие с существующим слоем." },
  ]}
/>

</Explanation>

<KeyTakeaway>Слои композитора — GPU-bitmap: дёшево анимировать, дорого хранить. will-change продвигает элемент, но запускает и правило перекрытия, которое продвигает соседей. Ограничивайте will-change длительностью анимации; используйте Layer Borders в DevTools для аудита непредвиденных продвижений.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="04-compositor-layers-retrieval"
  lang="ru"
  questions={[
    { q: "Какова цена GPU-памяти слоя compositor 1920×1080?", a: "Около 7,91 МБ (1920 × 1080 × 4 байта = 8,29 МБ). Тридцать таких слоёв = ~237 МБ — исчерпывает большинство мобильных GPU-бюджетов." },
    { q: "Почему `will-change: transform` на одном элементе иногда продвигает соседей?", a: "Правило перекрытия: если перекрывающий элемент не является слоем, композитор не может поддерживать корректный визуальный стек относительно продвинутого элемента. Браузер продвигает соседа (или сжимает его), чтобы сохранить z-порядок." },
    { q: "Каков правильный паттерн использования `will-change`?", a: "Устанавливать непосредственно перед началом анимации (например, на mouseenter), убирать по окончании. Постоянный will-change на каждом экземпляре компонента — самый дорогостоящий анти-паттерн мобильной производительности." },
  ]}
/>

<Recap lang="ru">
Слой compositor — GPU-bitmap, который compositor может позиционировать и смешивать дёшево каждый кадр, не трогая главный поток. Триггеры продвижения: 3D-трансформы, `will-change`, `position: fixed`, video, canvas и анимируемый opacity. Правило перекрытия вынуждает браузер продвигать элементы, перекрывающие слой — один will-change в списке может вызвать дюжину неявных продвижений. Каждый полноэкранный слой стоит ~7,91 МБ GPU-памяти; исчерпание мобильных GPU-бюджетов вызывает вытеснение тайлов и повторную растеризацию, создавая худший jank, чем без слоёв вообще. Правильный паттерн — ограничивать `will-change` длительностью анимации. Layer Borders в DevTools визуализирует каждый продвинутый элемент и его причину. Теперь, когда потянетесь за `will-change: transform` для плавной анимации, — сначала откройте Layer Borders: если увидите сорок рамок вместо одной, знайте, что сработало правило перекрытия, и пора переосмыслить stacking-контекст.
</Recap>
