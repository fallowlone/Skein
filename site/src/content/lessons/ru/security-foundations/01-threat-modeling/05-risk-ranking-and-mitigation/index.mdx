---
concepts:
- risk-ranking
- likelihood-impact
- dread-critique
- owasp-risk-rating
- mitigate-transfer-accept
- residual-risk
deepensInto: []
estMin: 18
lang: ru
lessonType: topic
level: senior
order: 5
prereqs: []
slug: 05-risk-ranking-and-mitigation
sources:
- https://owasp.org/www-community/OWASP_Risk_Rating_Methodology
- https://www.first.org/cvss/
spiral: []
status: ready
summary: 'Список угроз — это бэклог, а не план. Урок превращает перечисленные угрозы в приоритеты через вероятность x ущерб, объясняет, почему DREAD утратил доверие, и проводит четыре ответа — снизить, устранить, передать, принять — вплоть до остаточного риска.'
title: 'От угроз к приоритетам: ранжирование риска и выбор мер защиты'
track: security-foundations
unit: 01-threat-modeling
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
Проход STRIDE по сервису оформления заказа выдал сорок одну угрозу. DFD сделала видимой каждую границу доверия, деревья атак показали три правдоподобных пути к подделке цены, а abuse cases читались как враждебный набор QA. Сейчас четверг, после обеда, в спринте есть место максимум для двух security-историй, и техлид задаёт единственный важный вопрос: *какие две?* Сорок одна угроза — это не план, это бэклог без порядка. Вся ценность модели испаряется, если ты не можешь сказать, какая угроза получит инженеро-неделю, а какая подождёт квартал. Ранжирование — это момент, когда моделирование угроз перестаёт быть рисованием диаграмм и начинает окупаться.
</Hook>

К концу урока ты сможешь взять перечисленный список угроз и превратить его в обоснованный, считающийся с бюджетом порядок приоритетов, а затем выбрать правильный ответ для каждой угрозы — и понимать, какую из них ты сознательно примешь.

<Crux>Ранжирование по вероятности x ущербу превращает бэклог в план; у каждой угрозы один из ответов: снизить, устранить, передать, принять.</Crux>

<Explanation>

## От списка к порядку: вероятность x ущерб

Результат STRIDE, DFD и деревьев атак — это *множество*, неупорядоченное по построению. Приоритизация навязывает порядок, и рабочая ось здесь — **риск = вероятность x ущерб**. Вероятность — это насколько правдоподобна атака для реального противника: нужен ли ему государственный масштаб и zero-day или достаточно скучающего пользователя, правящего URL? Ущерб — это во что тебе обойдётся, когда атака сработает: испорченная маркетинговая страница — это не утёкшая таблица клиентов, и ни то ни другое — не баг с подменой цены, из-за которого каждый заказ уезжает за одну копейку.

Причина, по которой множат, а не складывают, в том, что риск определяется своим самым слабым обоснованием. Катастрофический ущерб при ничтожной вероятности — это ячейка низкого риска; тривиальный ущерб, происходящий постоянно, — тоже низкий. Угрозы, которые должны лишать сна, — те, что высоки по *обеим* осям, и умножение делает так, что это вытекает из арифметики, а не из спора. Смысл оценки — не ложная точность. Смысл в том, чтобы навязать *согласованный, воспроизводимый* разговор: чтобы два инженера, оценивая одну угрозу, попали в одну окрестность, и чтобы ранжирование пережило уход того, кто его построил.

<FlowDiagram
  label="Конвейер приоритизации: неупорядоченное множество угроз из STRIDE, DFD и деревьев атак оценивается по вероятности и ущербу, раскладывается в матрицу риска, и верхние ячейки становятся security-историями спринта, а нижние — явно принимаются с записью об остаточном риске."
  perRow={5}
  hue="var(--d-crypto)"
  nodes={[
    { id: "set", label: "Множество угроз", sub: "41, без порядка" },
    { id: "score", label: "Оценить каждую", sub: "вероятность x ущерб" },
    { id: "matrix", label: "Матрица риска", sub: "ячейки 5x5" },
    { id: "rank", label: "Ранжированный список", sub: "высокие → низкие" },
    { id: "respond", label: "Выбрать ответ", sub: "4 варианта" },
  ]}
  edges={[
    { from: "set", to: "score" },
    { from: "score", to: "matrix", label: "разместить" },
    { from: "matrix", to: "rank", label: "сортировать" },
    { from: "rank", to: "respond", label: "бюджет" },
  ]}
  caption="Ранжирование — мост между перечислением и действием. Матрица — это форсирующая функция для воспроизводимого разговора, а не заявка на числовую точность, и каждая ранжированная угроза выходит через один из четырёх ответов."
/>

## Почему DREAD утратил доверие

Годами схемой оценки по умолчанию был **DREAD** — Damage, Reproducibility, Exploitability, Affected users, Discoverability — каждый фактор по шкале 1–10 и суммируется. Звучит научно, но это не так. Проблема в том, что входы субъективны, а шкала не определена: нет общего рубрикатора, что делает Damage шестёркой, а что семёркой, поэтому два рецензента оценивают одну угрозу по-разному, и тот же рецензент во вторник оценит её иначе. *Discoverability* (обнаруживаемость) — фактор активно вредный: он поощряет security-through-obscurity, занижая оценку бага лишь потому, что его сейчас трудно найти, — а это ровно наоборот тому, как следует рассуждать о противнике, у которого всё время мира. Microsoft, популяризовавшая DREAD, тихо отказалась от неё внутри по этим причинам. Система оценки, вывод которой нельзя воспроизвести, — это не измерение; это число, которое *выглядит* как измерение, что хуже, потому что выдаёт догадку за данные.

Вместо этого команды берут небольшую лесенку лучше откалиброванных инструментов, выбираемых по тому, что именно оценивается:

- **Качественные матрицы риска** для угроз из модели. Сетка 3x3 или 5x5 «вероятность x ущерб», где каждый диапазон *определён* («высокая вероятность = эксплуатируется неаутентифицированным пользователем с публичным инструментарием»). Определённые диапазоны — в этом весь смысл: они делают оценку воспроизводимой, не притворяясь точностью до второго знака.
- **CVSS** для *известных уязвимостей* — CVE в зависимости, находка сканера. CVSS даёт стандартизированный, вендоронезависимый базовый балл 0–10 из определённых метрик (вектор атаки, сложность, требуемые привилегии, область, влияние на CIA), так что «CVE-2023-xxxx — это 9.8» означает одно и то же между командами и инструментами. CVSS — для *уязвимостей, на которые можно указать пальцем*; он не предназначен для ранжирования *проектных угроз*, выведенных из DFD.
- **OWASP Risk Rating Methodology**, когда нужна определённая, но гибкая факторная модель. Она разделяет вероятность (факторы агента угрозы + уязвимости: навык, мотив, лёгкость обнаружения, лёгкость эксплуатации) и ущерб (технические + бизнес-факторы), оценивает каждый фактор по определённой шкале 0–9 и сводит их в матрицу Low/Medium/High — амбиция DREAD, но уже с настоящими рубрикаторами под ней.

Зрелый ход — подбирать инструмент под вход: матрица для смоделированных угроз, CVSS для каталогизированных CVE, OWASP Risk Rating, когда нужна аудируемая разбивка по факторам. Смешивание — оценка проектной угрозы через CVSS или CVE в зависимости через взмах руки — даёт числа, которым никто не доверяет.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <caption class="text-left text-xs text-muted mb-2">Качественная матрица риска (вероятность x ущерб). Читай ячейку, а не число: диагональ и правый верх — туда конечный инженерный бюджет идёт в первую очередь.</caption>
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Вероятность ↓ / Ущерб →</th>
        <th class="border border-border px-3 py-2 text-left">Низкий ущерб</th>
        <th class="border border-border px-3 py-2 text-left">Средний ущерб</th>
        <th class="border border-border px-3 py-2 text-left">Высокий ущерб</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2 font-medium">Высокая вероятность</td>
        <td class="border border-border px-3 py-2">Средний — починить скоро</td>
        <td class="border border-border px-3 py-2">Высокий — этот спринт</td>
        <td class="border border-border px-3 py-2">Критический — бросить всё</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2 font-medium">Средняя вероятность</td>
        <td class="border border-border px-3 py-2">Низкий — в бэклог</td>
        <td class="border border-border px-3 py-2">Средний — починить скоро</td>
        <td class="border border-border px-3 py-2">Высокий — этот спринт</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2 font-medium">Низкая вероятность</td>
        <td class="border border-border px-3 py-2">Низкий — принять / мониторить</td>
        <td class="border border-border px-3 py-2">Низкий — в бэклог</td>
        <td class="border border-border px-3 py-2">Средний — ячейка-ловушка</td>
      </tr>
    </tbody>
  </table>
</div>

## Ячейка-ловушка: низкая вероятность, высокий ущерб

Самая дорогая ошибка в ранжировании — не переоценка шума. Это округление **низковероятной, высокоущербной** угрозы до нуля, потому что «этого никогда не случится». Та правая нижняя ячейка матрицы — место, где живёт хвостовой риск, а хвостовой риск — это то, что хоронит компании. Никогда не проверявшийся путь восстановления из бэкапа, SSRF к cloud-metadata, требующий конкретной мисконфигурации, баг с подменой цены, требующий перехвата запроса, — каждый кажется отдалённым, пока в один день условия не сойдутся, и тогда ущерб тотален. Дисциплина в том, чтобы относиться к этой ячейке как к *Среднему, а не нулю*: она не обгоняет высоковероятную-высокоущербную позицию в этом спринте, но заслуживает отслеживаемого решения — компенсирующего контроля, монитора, алерта, — а не молчаливого пропуска. Сбой именно в *молчаливом* пропуске: угроза, которую не записали, никогда не будет пересмотрена, и «мы решили, что риск приемлем» — позиция защитимая только тогда, когда есть запись, что вы это решили.

<Inset kind="why" lang="ru">
Почему не ранжировать всё и не идти сверху вниз, пока спринт не заполнится? Потому что инженерный бюджет конечен, а меры стоят дико по-разному. Высокорисковая угроза, закрываемая добавлением одной серверной проверки авторизации, — дешёвая ценность; среднерисковая угроза, требующая трёхнедельной переархитектуры, — дорогая ценность. Зрелая приоритизация взвешивает снижение риска *на единицу усилий*, а не сырой риск — иногда ты закрываешь четыре средних угрозы дешёвыми фиксами вместо одной высокой, которая стоит месяц, потому что суммарно сожжённого риска больше. Сырой ранг говорит, что важно; ранг с поправкой на стоимость говорит, что делать в четверг.
</Inset>

## Четыре ответа: снизить, устранить, передать, принять

Ранжированная угроза не закрыта, пока ты не выбрал *ответ*, и их ровно четыре. **Снизить** (mitigate) — добавить контроль, уменьшающий вероятность или ущерб: ограничить частоту запросов на сброс пароля, ограничить запрос владельцем, добавить правило WAF. Это частый случай, и он почти никогда не сводит риск к нулю — то, что остаётся, — это **остаточный риск**, риск, остающийся после контроля. **Устранить** (eliminate) — удалить функциональность или актив целиком: если SSRF — это функция «загрузить произвольный URL», и продукт может жить без неё, удаление убирает угрозу вместо того, чтобы её сторожить. Самая дешёвая уязвимость для починки — та, которую ты так и не отгрузил. **Передать** (transfer) — переложить финансовое последствие на кого-то ещё: кибер-страхование или платёжный процессор, владеющий PCI-областью, чтобы утечка карточных данных была по договору их. Передача двигает *стоимость*, а не *событие* — утечка всё равно происходит, клиент всё равно затронут, а твою репутацию не застрахуешь. **Принять** (accept) — решить, что остаточный риск ниже твоей планки, и сознательно ничего не делать, *с зафиксированным согласованием* от того, у кого есть полномочия владеть этим решением.

<TradeoffMatrix
  id="05-risk-ranking-tradeoff-1"
  lessonSlug="05-risk-ranking-and-mitigation"
  lang="ru"
  prompt="Твоя модель помечает низковероятную, высокоущербную угрозу: функция «загрузить картинку по URL» может быть превращена в SSRF против cloud-metadata эндпоинта. Функцию использует ~2% клиентов, и продакт-команда к ней прохладна. Какой ответ — сильнейший первый ход?"
  options={[
    { name: "Устранить: убрать функцию загрузки по URL; пусть пользователи загружают картинку напрямую", summary: "Избежать угрозы.", correct: true, justification: "Когда функция несёт высокоущербную угрозу, мало используется и не несёт стратегической нагрузки, её удаление стирает угрозу начисто вместо того, чтобы тратить инженерные усилия на вечную охрану. Нет остаточного риска для мониторинга и нет контроля для поддержки — самая дешёвая уязвимость для починки та, которую перестаёшь отгружать. Устранение — ровно правильный рефлекс для малоценной, высокорисковой поверхности; к снижению ты бы откатился, только если функция действительно нужна." },
    { name: "Снизить: оставить функцию, добавить allowlist разрешённых хостов и блокировать link-local/metadata диапазоны IP", summary: "Добавить контроль.", justification: "Это легитимный ответ и правильный, ЕСЛИ функция должна остаться, — но он принимает постоянный остаточный риск (пробелы allowlist, DNS-rebinding, новые metadata-эндпоинты) и бремя поддержки. Для функции, которой пользуются 2% клиентов и до которой продакту нет дела, платить эту цену вечно — худшая ценность, чем удалить функцию. Снижай, когда обязан её сохранить; здесь ты не обязан." },
    { name: "Передать: купить кибер-страхование, покрывающее стоимость утечки metadata-учёток", summary: "Сдвинуть стоимость.", justification: "Передача двигает финансовое последствие, а не событие. SSRF всё равно срабатывает, облачные учётки всё равно утекают, а латеральное движение, которое за этим следует, страховой выплатой не отменяется. Страховка — это подстраховка для остаточного риска, а не замена удалению или контролю известной, устранимой, высокоущербной угрозы." },
    { name: "Принять: отметить, что вероятность низкая, и идти дальше без контроля", summary: "Ничего не делать.", justification: "Это сбой ячейки-ловушки. Низкая вероятность + высокий ущерб — это хвостовой риск, а не нулевой, и молчаливое принятие — без компенсирующего контроля, без монитора и без зафиксированного согласования — означает, что угроза тотального ущерба остаётся неотслеженной. Принятие — валидный ответ только для действительно низкого остаточного риска и только с явным, владеемым решением на записи." },
  ]}
/>

## Замыкание цикла: хорошо ли мы поработали?

Модель угроз — это живой артефакт, а не документ, который подшивают. Честный закрывающий вопрос — *хорошо ли мы поработали?* — состоит из двух половин. Первая, **проверить модель**: покрыли ли мы систему (каждый элемент DFD, каждую границу доверия), нашли ли значимые угрозы и действительно ли меры отгружены и работают? Мера, помеченная «готово» в модели, но так и не влитая, — это ложь, которую наследует следующий рецензент. Вторая, **замкнуть цикл во времени**: перезапускать модель при изменении архитектуры, скармливать реальные инциденты и находки пентеста обратно для рекалибровки вероятности (угроза, которую ты оценил как «низковероятную» и которая только что произошла, — теперь данные, а не догадка) и переранжировать по мере движения системы и ландшафта угроз. Модель, к которой возвращаются после каждого инцидента и каждой крупной фичи, остаётся правдивой; написанная однажды и положенная на полку устаревает за квартал.

<DragOrder
  id="05-risk-ranking-drag-1"
  lessonSlug="05-risk-ranking-and-mitigation"
  lang="ru"
  prompt="Расположи сквозной мини-цикл моделирования угроз по порядку — от сырого перечисления до замыкания цикла:"
  items={[
    { id: "a", label: "Перечислить угрозы (STRIDE по DFD, деревья атак, abuse cases)" },
    { id: "b", label: "Оценить каждую угрозу по вероятности x ущербу с определёнными диапазонами" },
    { id: "c", label: "Ранжировать в матрицу и применить приоритизацию с поправкой на стоимость в рамках бюджета" },
    { id: "d", label: "Выбрать ответ на каждую угрозу: снизить / устранить / передать / принять" },
    { id: "e", label: "Проверить модель и скармливать инциденты обратно, чтобы переранжировать во времени" },
  ]}
  correctOrder={["a", "b", "c", "d", "e"]}
/>

<Quiz
  id="05-risk-ranking-quiz-1"
  lessonSlug="05-risk-ranking-and-mitigation"
  lang="ru"
  question="Почему DREAD вышел из моды для ранжирования смоделированных угроз?"
  choices={[
    { label: "Его факторы субъективны без определённого рубрикатора, поэтому оценки не воспроизводятся между рецензентами — а «Discoverability» поощряет security-through-obscurity", correct: true },
    { label: "Он даёт слишком высокие оценки, поэтому всё выглядит критичным", misconception: "Проблема не в инфляции, а в воспроизводимости: два рецензента (или один и тот же в другой день) оценивают одну угрозу по-разному, потому что диапазоны не определены. Согласованно-неверная шкала хотя бы была бы аудируемой." },
    { label: "Он работает только для известных CVE и не может оценивать проектные угрозы", misconception: "Это ограничение области CVSS, а не DREAD. DREAD был общей схемой оценки; его провал — субъективность и вредный фактор Discoverability, а не дизайн «только под уязвимости»." },
    { label: "Он никогда широко не использовался, поэтому инструменты его не поддержали", misconception: "DREAD использовался широко — Microsoft популяризовала его и позже отказалась внутри именно из-за субъективности, что противоположно «никогда не использовался»." },
  ]}
/>

<Quiz
  id="05-risk-ranking-quiz-2"
  lessonSlug="05-risk-ranking-and-mitigation"
  lang="ru"
  question="Ты добавляешь кибер-страхование, покрывающее стоимость возможной утечки данных клиентов. Какой это ответ и что он НЕ меняет?"
  choices={[
    { label: "Передать — он сдвигает финансовую стоимость, но утечка всё равно происходит, клиенты всё равно затронуты, а репутацию не застрахуешь", correct: true },
    { label: "Снизить — он уменьшает вероятность того, что утечка случится", misconception: "Страховка не влияет ни на вероятность, ни на технический ущерб утечки; атака столь же осуществима. Она лишь двигает того, кто платит счёт после, — это передача, а не снижение." },
    { label: "Устранить — риск исчезает, как только он застрахован", misconception: "Устранить означает убрать актив или функцию, чтобы угроза не могла произойти. Застрахованная система столь же эксплуатируема; событие неизменно, поэтому ничего не устранено." },
    { label: "Принять — ты решил жить с этим риском", misconception: "Принять означает сознательно ничего не делать с остаточным риском. Покупка полиса — это активный финансовый контроль, сдвигающий стоимость на третью сторону, то есть передача." },
  ]}
/>

</Explanation>

<KeyTakeaway>Ранжируй угрозы по вероятности x ущербу, оценивай правильным инструментом (качественная матрица для смоделированных угроз, CVSS для CVE, OWASP Risk Rating для аудируемой факторной модели — но не DREAD, чьи субъективные оценки не воспроизводятся), приоритизируй по снижению риска на единицу усилий и дай каждой угрозе один из четырёх ответов: снизить, устранить, передать или принять. Никогда не округляй молча низковероятную, высокоущербную угрозу до нуля — эта ячейка хвостового риска заслуживает отслеживаемого решения, а не пропуска.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="05-risk-ranking-retrieval"
  lang="ru"
  questions={[
    { q: "Почему DREAD широко критикуют и что команды используют вместо него для ранжирования угроз?", a: "DREAD (Damage, Reproducibility, Exploitability, Affected users, Discoverability, каждый 1–10 и суммируется) критикуют, потому что его входы субъективны, а шкала не определена — нет общего рубрикатора, что делает фактор шестёркой, а что семёркой, поэтому два рецензента оценивают одну угрозу по-разному, и тот же рецензент в другой день оценивает иначе. Вывод не воспроизводится, поэтому это не измерение; оно лишь выглядит как измерение. Хуже того, фактор Discoverability поощряет security-through-obscurity, занижая оценку угрозы за то, что её сейчас трудно найти, — обратное рассуждение против терпеливого противника. Microsoft, популяризовавшая DREAD, отказалась от неё внутри по этим причинам. Вместо этого команды берут небольшую лесенку под вход: качественные матрицы риска (3x3 / 5x5 вероятность x ущерб с КАЖДЫМ ОПРЕДЕЛЁННЫМ диапазоном) для угроз, выведенных из модели; CVSS для известных, каталогизированных уязвимостей (CVE, находки сканеров), где стандартизированный базовый балл 0–10 означает одно и то же между командами; и OWASP Risk Rating Methodology, когда нужна определённая-но-гибкая факторная модель, разделяющая факторы вероятности (навык, мотив, лёгкость обнаружения/эксплуатации) и факторы ущерба (технические + бизнес) и сводящая их в матрицу Low/Medium/High — амбиция DREAD с настоящими рубрикаторами под ней." },
    { q: "Проведи одну низковероятную, высокоущербную угрозу через четыре ответа и объясни остаточный риск.", a: "Возьмём SSRF: функцию «загрузить картинку по URL» можно навести на cloud-metadata эндпоинт, чтобы украсть учётки — низкая вероятность (нужна конкретная конфигурация), но высокий ущерб (полная компрометация). Четыре ответа: СНИЗИТЬ (mitigate) — оставить функцию, но добавить allowlist разрешённых хостов и блокировать link-local/metadata диапазоны IP; это снижает вероятность, но оставляет ОСТАТОЧНЫЙ РИСК — риск, остающийся после контроля (пробелы allowlist, DNS-rebinding, новые metadata-эндпоинты), плюс постоянное бремя поддержки. УСТРАНИТЬ (eliminate) — удалить функцию загрузки по URL и дать загружать напрямую; угроза исчезает без остаточного риска для мониторинга, и это сильнейший ход, когда функция малоценна и не несёт нагрузки (самая дешёвая уязвимость для починки та, которую перестаёшь отгружать). ПЕРЕДАТЬ (transfer) — купить кибер-страхование или переложить последствие на процессор; это двигает финансовую СТОИМОСТЬ, а не СОБЫТИЕ: SSRF всё равно срабатывает, учётки всё равно утекают, а репутацию не застрахуешь. ПРИНЯТЬ (accept) — счесть остаточный риск ниже планки и сознательно ничего не делать, но только с зафиксированным согласованием от владельца. Кардинальный грех — молча округлить эту ячейку хвостового риска до нуля: угрозу, которую не записали, нельзя пересмотреть, и «мы приняли риск» защитимо только тогда, когда есть запись, что вы действительно это решили." },
  ]}
/>

<Recap lang="ru">
Список угроз — это бэклог, а не план: приоритизация навязывает порядок, и рабочая ось — риск = вероятность x ущерб (умножаем, поэтому угроза набирает высоко, только когда обе оси высоки). Смысл оценки — согласованный, воспроизводимый разговор, а не ложная точность, — и именно поэтому DREAD утратил доверие: его субъективные, неопределённые факторы не воспроизводятся, а фактор Discoverability поощряет обскурность. Подбирай инструмент под вход: качественные матрицы риска для смоделированных угроз, CVSS для известных CVE, OWASP Risk Rating для аудируемой разбивки по факторам. При конечном бюджете приоритизируй по снижению риска на единицу усилий, а не по сырому рангу. Затем каждая угроза выходит через один из четырёх ответов — снизить (оставляя остаточный риск), устранить, передать (стоимость, а не событие) или принять (с зафиксированным согласованием). Определяющий сбой — молчаливое округление низковероятной, высокоущербной угрозы до нуля; эта ячейка хвостового риска хоронит компании, поэтому она заслуживает отслеживаемого решения. Наконец, замкни цикл: проверь, что меры действительно отгружены, и скармливай реальные инциденты обратно, чтобы рекалибровать вероятность и переранжировать по мере движения системы. Теперь, когда тебе вручают сорок одну угрозу и два слота спринта, твой первый вопрос: какие ячейки правые-верхние, какие фиксы дёшевы и какую угрозу мы сознательно решаем принять на записи?
</Recap>
