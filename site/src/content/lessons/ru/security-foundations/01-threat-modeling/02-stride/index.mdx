---
concepts:
- stride
- spoofing
- tampering
- repudiation
- information-disclosure
- denial-of-service
- elevation-of-privilege
deepensInto: []
estMin: 17
lang: ru
lessonType: topic
level: middle
order: 2
prereqs: []
slug: 02-stride
sources:
- https://learn.microsoft.com/en-us/azure/security/develop/threat-modeling-tool-threats
- https://owasp.org/www-community/Threat_Modeling_Process
spiral: []
status: ready
summary: 'STRIDE — это шесть подсказок: Spoofing, Tampering, Repudiation, Information disclosure, Denial of service, Elevation of privilege, каждая из которых отрицает одно свойство безопасности. Это словарь полноты, а не чек-лист, применяемый поэлементно.'
title: 'STRIDE: словарь того, что может пойти не так'
track: security-foundations
unit: 01-threat-modeling
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
Вы на дизайн-ревью новой фичи «экспорт моих данных». Кто-то спрашивает: «это безопасно?» — и в комнате повисает тишина, потому что никто не знает, с чего начать. Один инженер беспокоится об SQL-инъекции, другой — о лимитах запросов, третий говорит: «у нас есть авторизация, всё нормально». Три человека, три угрозы, ни одной общей карты. Именно эту тишину STRIDE и создан убивать. Это не сканер и он не ищет баги; это словарь из шести слов, превращающий вопрос «это безопасно?» в шесть конкретных, отвечаемых вопросов — так что комната спорит о полноте покрытия, а не смотрит в потолок.
</Hook>

К концу урока вы сможете назвать шесть категорий STRIDE, сопоставить каждую с тем единственным свойством безопасности, которое она отрицает, и применить их поэлементно к небольшой системе, не перегибая и не недогибая палку.

<Crux>STRIDE — это шесть угроз, каждая из которых обратна одному свойству безопасности; это подсказка для полноты, а не чек-лист багов.</Crux>

<Explanation>

## Откуда взялся STRIDE и что это вообще такое

STRIDE — мнемоника, придуманная в Microsoft в 1999 году, чтобы дать инженерам *структурированный* способ спрашивать «как это может сломаться?». До неё мозговой штурм угроз был тем, что вспоминал самый параноидальный человек в комнате. Идея STRIDE мала, но мощна: у каждой цели безопасности есть обратная сторона, и эта обратная сторона — угроза. Поэтому вместо перечисления атак (их бесконечно много) вы перечисляете шесть способов нарушить *свойство* безопасности. Шесть подсказок покрывают всё пространство.

Секрет того, как усвоить STRIDE, — читать его как список *отрицаний*. Аутентификация говорит «вы тот, за кого себя выдаёте» — отрицаем и получаем **Spoofing**. Целостность говорит «данные и код не изменены» — отрицаем и получаем **Tampering**. Каждая буква — это свойство, вывернутое наизнанку:

- **S**poofing отрицает **аутентификацию** — выдача себя за кого-то или что-то, кем вы не являетесь.
- **T**ampering отрицает **целостность** — изменение данных или кода, которые вы менять не должны.
- **R**epudiation отрицает **неотказуемость** — совершить действие и затем правдоподобно его отрицать, потому что ничто его не зафиксировало.
- **I**nformation disclosure отрицает **конфиденциальность** — раскрытие данных тому, кто не имеет права их видеть.
- **D**enial of service отрицает **доступность** — приведение системы в состояние, непригодное для легитимных пользователей.
- **E**levation of privilege отрицает **авторизацию** — получение возможностей сверх выданных вам.

Это взаимно однозначное соответствие — весь движок. Если вы можете перечислить шесть свойств, вы можете воссоздать STRIDE с нуля — и, что важнее, найдя угрозу, вы можете назвать, *какую гарантию она ломает*, а это говорит, какой класс контролей её исправляет.

## Шесть угроз — с конкретной формой и митигацией для каждой

Словарь бесполезен, пока к каждому слову не прикреплена картинка. Вот каждая буква STRIDE, привязанная к свойству, конкретному примеру из обычного веб-приложения и семейству контролей, которое на неё отвечает.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">STRIDE</th>
        <th class="border border-border px-3 py-2 text-left">Нарушенное свойство</th>
        <th class="border border-border px-3 py-2 text-left">Конкретный пример</th>
        <th class="border border-border px-3 py-2 text-left">Семейство митигаций</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2"><strong>S</strong>poofing</td>
        <td class="border border-border px-3 py-2">Аутентификация</td>
        <td class="border border-border px-3 py-2">Подделанная сессионная cookie или повторно проигранный токен даёт чужую личность</td>
        <td class="border border-border px-3 py-2">Сильная аутентификация, MFA, подписанные короткоживущие токены, mutual TLS</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2"><strong>T</strong>ampering</td>
        <td class="border border-border px-3 py-2">Целостность</td>
        <td class="border border-border px-3 py-2">Клиент меняет скрытое поле <code>price</code>; строка изменена в пути</td>
        <td class="border border-border px-3 py-2">Серверная валидация, HMAC/подписи, TLS, ограничения БД</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2"><strong>R</strong>epudiation</td>
        <td class="border border-border px-3 py-2">Неотказуемость</td>
        <td class="border border-border px-3 py-2">Админ удаляет запись, и нет лога, доказывающего, кто это сделал</td>
        <td class="border border-border px-3 py-2">Защищённые от подделки журналы аудита, подписанные квитанции, метки времени</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2"><strong>I</strong>nformation disclosure</td>
        <td class="border border-border px-3 py-2">Конфиденциальность</td>
        <td class="border border-border px-3 py-2">Подробная ошибка раскрывает стек-трейс; PII другого пользователя в ответе</td>
        <td class="border border-border px-3 py-2">Шифрование на диске/в пути, наименьшие привилегии, фильтрация вывода</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2"><strong>D</strong>enial of service</td>
        <td class="border border-border px-3 py-2">Доступность</td>
        <td class="border border-border px-3 py-2">Неограниченный запрос или zip-бомба исчерпывают CPU/память</td>
        <td class="border border-border px-3 py-2">Лимиты запросов, квоты, таймауты, ограничения размера ввода, автомасштабирование</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2"><strong>E</strong>levation of privilege</td>
        <td class="border border-border px-3 py-2">Авторизация</td>
        <td class="border border-border px-3 py-2">Обычный пользователь напрямую бьёт в <code>POST /admin/promote</code>, и это срабатывает</td>
        <td class="border border-border px-3 py-2">Серверная авторизация, deny-by-default, песочницы, наименьшие привилегии</td>
      </tr>
    </tbody>
  </table>
</div>

Обратите внимание: колонка митигаций — это *семейства*, а не отдельные фиксы. «Сильная аутентификация» — это направление, а не строка кода. Задача STRIDE — указать вам на нужное семейство; выбор конкретного контроля — это следующий, отдельный шаг. Ценность в том, что вы больше никогда не спрашиваете «это безопасно?» — вы задаёте шесть вопросов, ответы на которые реально можете защитить на ревью.

## STRIDE-per-element: куда направлять шесть вопросов

Шесть категорий — лишь половина метода. Вторая половина — это *где* вы их задаёте. STRIDE-per-element означает, что вы не допрашиваете всю систему разом — вы проходите её по одному элементу за раз и применяете релевантное подмножество STRIDE к каждому.

Почему это важно: не каждая угроза применима к каждому элементу. Категории группируются по типу элемента. Внешние сущности (пользователи, сторонние сервисы) могут быть **S**poofed и могут **R**epudiate, но вы не «подделываете» пользователя. Процессы (ваши сервисы) подвержены всем шести. Хранилища данных (базы, кэши, файлы) ощущают **T**ampering, **I**nformation disclosure, **D**enial of service и **R**epudiation (если там живут логи) — но базу не «спуфят» так, как пользователя. Потоки данных (запросы, пересекающие границу) — там концентрируются **T**ampering, **I**nformation disclosure и **D**enial of service. Сопоставление тип элемента → применимые буквы делает упражнение конечным и не даёт вам генерировать бессмысленные угрозы.

<FlowDiagram
  label="STRIDE-per-element проходит небольшую веб-систему по одному элементу за раз и задаёт только те буквы STRIDE, что подходят типу элемента: внешний пользователь может быть Spoofed; поток запроса может быть подделан (Tampering) или прочитан (Information disclosure); процесс API подвержен всем шести, особенно Elevation of privilege; хранилище БД подвержено Tampering и Information disclosure. К каждому элементу вы применяете релевантное подмножество, а не весь список ко всей системе."
  perRow={4}
  hue="var(--d-crypto)"
  nodes={[
    { id: "user", label: "Пользователь (сущность)", sub: "S, R" },
    { id: "flow", label: "Запрос (поток)", sub: "T, I, D" },
    { id: "api", label: "API (процесс)", sub: "S T R I D E" },
    { id: "db", label: "БД (хранилище)", sub: "T, I, D, R" },
  ]}
  edges={[
    { from: "user", to: "flow", label: "шлёт" },
    { from: "flow", to: "api", label: "пересекает границу" },
    { from: "api", to: "db", label: "читает/пишет" },
  ]}
  caption="STRIDE-per-element: каждый тип элемента притягивает своё подмножество шести. Пользователя можно Spoof; поток запроса можно подделать или раскрыть; процесс подвержен всем шести (главная из них — Elevation of privilege); хранилище подвержено Tampering, Information disclosure и Denial of service."
/>

## Разбор: эндпоинт «экспорт моих данных»

Возьмём фичу из хука — `GET /export` отдаёт данные авторизованного пользователя файлом на скачивание. Пройдём три элемента и зададим STRIDE.

**Пользователь (внешняя сущность).** *Spoofing:* может ли кто-то предъявить чужую сессию и выгрузить *его* данные? Семейство контролей — сильная аутентификация: короткоживущие подписанные сессионные токены, а не угадываемый id. *Repudiation:* если пользователь потом заявит «я ничего не экспортировал», сможете ли вы доказать обратное? Нужна запись в журнале аудита, привязанная к личности, и метка времени.

**Поток запроса (`GET /export`, пересекающий сетевую границу).** *Tampering:* может ли клиент изменить параметр запроса — скажем, `?user_id=` — чтобы перенаправить экспорт на кого-то другого? *Information disclosure:* идёт ли ответ по TLS, или его можно прочитать на проводе? Семейства фиксов — серверная привязка параметров (выводить пользователя из сессии, никогда из запроса) и шифрование транспорта.

**Процесс API и база данных (процесс + хранилище).** *Elevation of privilege:* обработчик достаёт по `WHERE id = ?` в одиночку, позволяя любому аутентифицированному вызывающему экспортировать любую строку — или он ограничивает выборку владельцем? *Denial of service:* может ли кто-то запросить экспорт настолько большой, что он положит CPU или заполнит диск, без ограничения размера и лимита запросов? *Information disclosure:* не включает ли экспорт случайно колонки, которые пользователь видеть не должен, — внутренние флаги, ссылки на чужие данные?

Этот единственный эндпоинт, пройденный через STRIDE-per-element, дал восемь конкретных, исправимых вопросов примерно за минуту. Ни один не потребовал инструмента. В этом весь смысл: структура побеждает память.

<Inset kind="why" lang="ru">
Почему шесть и почему именно эти шесть? STRIDE сознательно ложится на классические свойства безопасности (триада CIA — конфиденциальность, целостность, доступность — плюс аутентификация, авторизация и неотказуемость). Это не совпадение, это замысел. Поскольку каждая категория — отрицание свойства, которое система уже обещает, STRIDE наследует полноту этого набора свойств. Если ваша система даёт гарантию, которую STRIDE не покрывает — скажем, приватность как несвязываемость, — то один STRIDE неполон, и именно поэтому существуют более поздние фреймворки вроде LINDDUN для приватности. Знание того, что STRIDE покрывает, точно говорит, когда выйти за его пределы.
</Inset>

## Старшая тонкость: подсказка, а не чек-лист

Самый частый способ злоупотребить STRIDE — относиться к шести буквам как к чек-листу для галочек: «Spoofing — готово, у нас есть логин. Дальше». Это схлопывает инструмент мышления в ритуал соответствия и именно так реальные угрозы проскальзывают. STRIDE — это *подсказка для полноты*: его задача — убедиться, что вы рассмотрели каждую категорию, а не сертифицировать, что одно предложение на букву делает вас в безопасности.

Два режима отказа стоят по обе стороны этой черты. **Перебор:** механическая генерация угрозы для каждой буквы на каждом элементе порождает болото малоценных записей — «балансировщик можно заспуфить», — которое хоронит те три угрозы, что реально важны, и истощает аппетит команды к упражнению. **Недобор:** остановка на первой правдоподобной угрозе на элемент, так что вторая, более мерзкая, так и не всплывает. Старший приём — это суждение поверх структуры: используйте все шесть букв, чтобы *сгенерировать* кандидатов, затем безжалостно отсортируйте по вероятности и влиянию и запишите только те угрозы, что стоят решения о митигации. STRIDE даёт вам вопросы; думать над ответами всё равно придётся вам. Модель угроз с сорока механическими записями и без приоритетов хуже, чем модель с пятью настоящими и планом.

<TradeoffMatrix
  id="02-stride-tradeoff-1"
  lessonSlug="02-stride"
  lang="ru"
  prompt="Команда внедряет STRIDE для новой платёжной фичи. Как реально его прогнать, чтобы он давал ценность, а не превратился в ритуал по чек-листу?"
  options={[
    { name: "Пройти каждый элемент и всеми шестью буквами сгенерировать кандидатов угроз, затем отсортировать по вероятности × влияние и записать только те, что стоят решения о митигации", summary: "Сгенерировать, затем отсортировать.", correct: true, justification: "Это STRIDE по замыслу: шесть категорий обеспечивают полноту покрытия (вы рассмотрели каждое свойство, которое можно нарушить), а поэлементность держит охват конечным. Сортировка — старший слой сверху: вы всё равно ранжируете по вероятности и влиянию и берёте на себя только те митигации, что себя оправдывают. На выходе — короткий список реальных угроз с решениями, а не стена механических записей." },
    { name: "Написать ровно одну угрозу на букву для всей системы и помечать каждую «обработана», как только кто-то назовёт любой существующий контроль", summary: "Поставить шесть галочек.", justification: "Это ловушка чек-листа. Применение STRIDE ко всей системе вместо поэлементного скрывает, где каждая угроза реально живёт, а «у нас есть логин, значит Spoofing обработан» не сертифицирует ничего — оно пропускает конкретный элемент, где контроля может не быть. STRIDE — подсказка для полноты, а не форма соответствия из шести пунктов." },
    { name: "Сгенерировать все возможные угрозы для каждой буквы на каждом элементе и записать их все с равным весом", summary: "Максимизировать записи.", justification: "Перебор. Производство угрозы для каждой ячейки (балансировщик можно заспуфить, кэш можно repudiate) хоронит те немногие угрозы, что важны, под шумом, а неприоритизированный список из сорока записей не даёт команде ничего, с чем работать. Цель — полнота категорий, а не полнота тривиальных записей." },
    { name: "Пропустить STRIDE и положиться на OWASP Top 10 плюс сканер, раз уж они покрывают реальные баги", summary: "Инструменты вместо моделирования.", justification: "Сканеры и Top 10 находят известные паттерны в уже существующем коде; STRIDE — инструмент рассуждения на этапе проектирования, выявляющий недостающие требования до того, как код написан. Они дополняют, а не заменяют друг друга — отказ от STRIDE означает, что угрозы уровня дизайна (поток без лимита запросов, экспорт без проверки владельца) не поднимутся, пока не уйдут в прод." },
  ]}
/>

<Quiz
  id="02-stride-quiz-1"
  lessonSlug="02-stride"
  lang="ru"
  question="Админ удаляет запись клиента, и через недели никто не может доказать, кто это сделал, потому что ничего не логировалось. Какая это категория STRIDE и какое свойство нарушено?"
  choices={[
    { label: "Repudiation — нарушена неотказуемость; семейство фикса — защищённый от подделки журнал аудита, привязанный к личности и времени", correct: true },
    { label: "Tampering — нарушена целостность, ведь запись изменили", misconception: "Tampering — про неавторизованное изменение данных, которые вы менять не должны. Здесь удаление могло быть авторизованным; сбой в том, что нет надёжной записи, кто его совершил. Это свойство неотказуемости — Repudiation." },
    { label: "Elevation of privilege — у админа было слишком много прав", misconception: "Elevation of privilege — это получение возможностей сверх выданных. Админу было позволено удалять; пробел — в подотчётности, а не в авторизации. Категория — Repudiation." },
    { label: "Information disclosure — удаление данных это вопрос конфиденциальности", misconception: "Information disclosure — это раскрытие данных тому, кто не имеет права их видеть. Удаление без следа — обратная забота: потеря возможности атрибутировать действие. Это ложится на неотказуемость, то есть Repudiation." },
  ]}
/>

<Quiz
  id="02-stride-quiz-2"
  lessonSlug="02-stride"
  lang="ru"
  question="Вы применяете STRIDE-per-element к базе Postgres (хранилище данных). Какую букву наименее естественно задавать ей?"
  choices={[
    { label: "Spoofing — хранилище не является личностью, под которую выдают себя, как пользователь или сервис; Spoofing вы задаёте сущностям и процессам, которые к нему подключаются", correct: true },
    { label: "Tampering — неавторизованное изменение хранимых строк это ключевая угроза хранилища", misconception: "Tampering — ровно та угроза, что притягивает хранилище; поэтому она естественно подходит, а не выбивается." },
    { label: "Information disclosure — утёкшее или избыточно-привилегированное хранилище раскрывает конфиденциальные данные", misconception: "Information disclosure — первичная угроза хранилища (вспомните выставленный наружу бэкап или слишком широкую роль на чтение). Она естественна; это не ответ." },
    { label: "Denial of service — исчерпание соединений или диска может положить хранилище", misconception: "Denial of service реально применим к хранилищам (исчерпание соединений, заполнение диска). Это естественное соответствие, так что это не наименее применимая буква." },
  ]}
/>

<DragOrder
  id="02-stride-drag-1"
  lessonSlug="02-stride"
  lang="ru"
  prompt="Расставьте категории STRIDE в каноническом порядке S-T-R-I-D-E:"
  items={[
    { id: "s", label: "Spoofing (отрицает аутентификацию)" },
    { id: "t", label: "Tampering (отрицает целостность)" },
    { id: "r", label: "Repudiation (отрицает неотказуемость)" },
    { id: "i", label: "Information disclosure (отрицает конфиденциальность)" },
    { id: "d", label: "Denial of service (отрицает доступность)" },
    { id: "e", label: "Elevation of privilege (отрицает авторизацию)" },
  ]}
  correctOrder={["s", "t", "r", "i", "d", "e"]}
/>

</Explanation>

<KeyTakeaway>STRIDE — это шесть угроз: Spoofing, Tampering, Repudiation, Information disclosure, Denial of service, Elevation of privilege, каждая из которых отрицает одно свойство безопасности (аутентификацию, целостность, неотказуемость, конфиденциальность, доступность, авторизацию). Применяйте поэлементно и используйте для генерации кандидатов, затем сортируйте. Это подсказка для полноты, а не чек-лист.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="02-stride-retrieval"
  lang="ru"
  questions={[
    { q: "Перечислите шесть категорий STRIDE и свойство безопасности, которое каждая нарушает, с одним конкретным примером на каждую.", a: "STRIDE — это шесть угроз, каждая обратна свойству безопасности. Spoofing нарушает аутентификацию — например, повторно проигранный или подделанный сессионный токен даёт чужую личность; митигируется сильной аутентификацией, MFA и короткоживущими подписанными токенами. Tampering нарушает целостность — например, клиент правит скрытое поле price или меняет строку в пути; митигируется серверной валидацией, HMAC/подписями, TLS и ограничениями БД. Repudiation нарушает неотказуемость — например, админ удаляет запись, и ничто не залогировало, кто это сделал; митигируется защищёнными от подделки журналами аудита, привязанными к личности и времени. Information disclosure нарушает конфиденциальность — например, подробная ошибка раскрывает стек-трейс или PII другого пользователя в ответе; митигируется шифрованием в пути/на диске, наименьшими привилегиями и фильтрацией вывода. Denial of service нарушает доступность — например, неограниченный запрос или zip-бомба исчерпывают CPU или диск; митигируется лимитами запросов, квотами, таймаутами и ограничениями размера ввода. Elevation of privilege нарушает авторизацию — например, обычный пользователь напрямую бьёт в POST /admin/promote и преуспевает; митигируется серверной авторизацией deny-by-default и наименьшими привилегиями." },
    { q: "Что означает STRIDE-per-element и почему STRIDE — это «подсказка для полноты, а не чек-лист»?", a: "STRIDE-per-element означает, что вы не допрашиваете всю систему разом — вы проходите её по одному элементу за раз (внешние сущности, процессы, хранилища данных, потоки данных) и применяете только те буквы STRIDE, что подходят типу элемента. Внешние сущности могут быть Spoofed и могут Repudiate; процессы подвержены всем шести; хранилища данных подвержены Tampering, Information disclosure, Denial of service и Repudiation; потоки данных концентрируют Tampering, Information disclosure и Denial of service. Сопоставление типа элемента с применимыми буквами держит упражнение конечным и не даёт генерировать бессмысленные угрозы. «Подсказка для полноты, а не чек-лист» схватывает старшую тонкость: шесть категорий существуют, чтобы гарантировать, что вы рассмотрели каждый способ нарушить свойство безопасности, но галочка «Spoofing — у нас есть логин» не сертифицирует ничего. Правильное применение — сгенерировать кандидатов угроз всеми шестью буквами, затем безжалостно отсортировать по вероятности и влиянию и записать только те, что стоят решения о митигации. Перебор хоронит реальные угрозы под шумом; недобор останавливается на первой. STRIDE даёт вопросы; суждение даёт ответы." },
  ]}
/>

<Recap lang="ru">
STRIDE — это словарь из шести слов: Spoofing, Tampering, Repudiation, Information disclosure, Denial of service, Elevation of privilege — чтобы структурированно спрашивать «как это может сломаться?». Его движок в том, что каждая категория — отрицание одного свойства безопасности: аутентификации, целостности, неотказуемости, конфиденциальности, доступности и авторизации соответственно. Каждая ложится на *семейство* контролей, а не на единственный фикс, так что STRIDE указывает вам на нужный род защиты и оставляет конкретный выбор вам. Вы применяете его поэлементно (STRIDE-per-element) — проходя внешние сущности, процессы, хранилища и потоки и задавая только подходящие типу элемента буквы, — что держит упражнение конечным, как показал разбор эндпоинта «экспорт моих данных», давший восемь конкретных вопросов за минуту. Старшая дисциплина — относиться к шести как к подсказке для полноты, а не к чек-листу: сгенерировать кандидатов каждой буквой, затем отсортировать по вероятности и влиянию и записать только угрозы, стоящие решения. Перебор топит сигнал; недобор пропускает мерзкую вторую угрозу. В следующий раз, когда кто-то спросит «это безопасно?», у вас будет шесть вопросов вместо тишины.
</Recap>
