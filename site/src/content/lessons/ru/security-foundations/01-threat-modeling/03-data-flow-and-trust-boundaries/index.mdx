---
concepts:
- data-flow-diagram
- trust-boundary
- attack-surface
- stride-per-element
- external-dependency
deepensInto: []
estMin: 17
lang: ru
lessonType: topic
level: middle
order: 3
prereqs: []
slug: 03-data-flow-and-trust-boundaries
sources:
- https://cheatsheetseries.owasp.org/cheatsheets/Attack_Surface_Analysis_Cheat_Sheet.html
- https://cheatsheetseries.owasp.org/cheatsheets/Threat_Modeling_Cheat_Sheet.html
spiral: []
status: ready
summary: Диаграмма потоков данных превращает систему в элементы и линии, которые данные пересекают между ними. Границы доверия — это линии, где меняется владелец или привилегия и где концентрируются угрозы. DFD делает STRIDE-по-элементам системой, а не догадками.
title: Диаграммы потоков данных и границы доверия
track: security-foundations
unit: 01-threat-modeling
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
Команда правильно построила модель угроз для оформления заказа: они перечислили STRIDE-угрозы для каждой коробки на доске. Через полгода их взломали через сервис рекомендаций — внутренний gRPC-эндпоинт, который «вызывают только другие сервисы». Никто не нарисовал линию там, где запрос на самом деле зарождался, поэтому никто не спросил, достижим ли этот сервис из открытого интернета. Он был достижим: неправильно настроенный ingress-маршрут открывал его напрямую, и сервис доверял любому вызывающему, потому что на диаграмме жил безопасно «внутри». Угрозы упустили не потому, что команда не знала STRIDE. Их упустили потому, что граница доверия на диаграмме была нарисована не в том месте — или не нарисована вовсе.
</Hook>

К концу этого урока ты сможешь нарисовать диаграмму потоков данных реального веб-приложения, разместить границы доверия там, где реально меняется владелец или привилегия, и использовать эти пересечения, чтобы вести STRIDE по элементам, а не смотреть на пустой лист.

<Crux>Граница доверия — это линия на DFD, где данные переходят между зонами доверия, и именно на ней концентрируются угрозы.</Crux>

<Explanation>

## Четыре элемента диаграммы потоков данных

STRIDE даёт *виды* угроз — спуфинг, подмену (tampering), отрицание (repudiation), раскрытие информации, отказ в обслуживании, повышение привилегий. Но STRIDE, применённый ко «всей системе», — это туман. Чтобы сделать его системным, сначала нужна модель того, как данные реально движутся, и стандартная модель — это **диаграмма потоков данных (DFD)**. DFD намеренно крошечная: в ней ровно четыре типа элементов, и каждая коробка на твоей доске — один из них:

- **Внешняя сущность** — актор вне твоего контроля, который порождает или принимает данные: браузер, мобильное приложение, API партнёра, человек. Ты не запускаешь их код; ты можешь только проверять то, что они передают. Рисуется прямоугольником.
- **Процесс** — код, который запускаешь *ты* и который преобразует данные: API-сервер, Lambda, фоновый воркер, парсер. Здесь живёт логика — а значит, и большинство багов. Рисуется кругом.
- **Хранилище данных** — место, где данные покоятся: таблица Postgres, бакет S3, кэш Redis, файл логов, очередь сообщений в покое. Рисуется двумя параллельными линиями.
- **Поток данных** — стрелка, соединяющая любые два из перечисленных: HTTP-запрос, SQL-запрос, публикация в Kafka. Поток несёт данные, и именно поток атакующий наблюдает или подменяет в транзите. Рисуется направленным ребром.

Это весь словарь. Сила не в коробках — она в том, что тебя заставляют нарисовать *каждую* стрелку, потому что каждая стрелка — место, где данные раскрыты, а каждая коробка — место, где данным доверяют или их преобразуют. DFD, которую ты не можешь нарисовать, — это система, которую ты на самом деле не понимаешь.

## Границы доверия: где живут угрозы

Теперь наложи одну аннотацию, которая превращает DFD из документации в инструмент моделирования угроз: **границу доверия**. Граница доверия — это линия, которую ты проводишь поперёк потоков данных везде, где данные переходят от одного субъекта (principal), уровня привилегий или домена владения к другому. Конкретно, граница стоит в каждой точке, где меняется ответ на вопрос «кто контролирует данные по эту сторону?»:

- Между браузером и твоим API (недоверенный клиент → твой код).
- Между твоим процессом API и базой данных (привилегия приложения → привилегия данных; SQL-инъекция здесь — это *пересечение границы*).
- Между твоим кодом и сторонним сервисом (твои данные покидают твой контроль).
- Между двумя твоими собственными микросервисами, когда они работают на разных уровнях привилегий или доверия.
- Между низкодоверенным процессом в DMZ и высокодоверенным внутренним.

Причина, по которой границы так важны, столь же статистическая, сколь и концептуальная: **угрозы концентрируются на пересечениях.** Данные, которые никогда не покидают одну зону доверия, сравнительно безопасны — они уже внутри контроля одного субъекта. Опасные моменты — это передачи, потому что именно там одна сторона должна *решить, доверять ли другой*, и неверное решение есть уязвимость. Спуфинг происходит при пересечении в твою границу аутентификации; подмена и раскрытие — на потоках, пересекающих границу; повышение привилегий — когда процесс на доверенной стороне принимает ввод, который следовало считать недоверенным. Граница — это адрес, по которому каждая буква STRIDE приходит за сбором.

<FlowDiagram
  label="Рабочая диаграмма потоков данных для веб-приложения: браузер (внешняя сущность) шлёт HTTP-запрос через первую границу доверия к процессу API, который запрашивает базу данных (хранилище) и вызывает сторонний сервис через вторую границу доверия; две границы — это ровно те места, где концентрируются угрозы."
  perRow={4}
  hue="var(--d-security)"
  nodes={[
    { id: "browser", label: "Браузер", sub: "внешняя сущность" },
    { id: "api", label: "Процесс API", sub: "твой код" },
    { id: "db", label: "База данных", sub: "хранилище" },
    { id: "third", label: "Платёжный API", sub: "внешняя завис." },
  ]}
  edges={[
    { from: "browser", to: "api", label: "HTTP · граница 1" },
    { from: "api", to: "db", label: "SQL" },
    { from: "api", to: "third", label: "HTTPS · граница 2" },
  ]}
  caption="Четыре элемента DFD, две границы доверия. Граница 1 (браузер → API) — там, где клиент перестаёт быть доверенным; граница 2 (API → Платёжный API) — там, где твои данные покидают твой контроль. Каждое пересечение — это чек-лист STRIDE, который ждёт заполнения; стрелки между API и БД лежат внутри твоей зоны, но всё равно пересекают линию привилегий «приложение → данные»."
/>

## Поверхность атаки: сумма всех пересечений

Когда границы проведены, **поверхность атаки** вытекает из диаграммы почти бесплатно. Анализ поверхности атаки, в формулировке OWASP, — это работа по перечислению каждой точки, где атакующий может попытаться ввести данные в систему или извлечь их из неё, и на DFD эти точки — ровно *те потоки данных, которые пересекают границу доверия*. Каждое такое пересечение — точка входа или выхода: форма логина, эндпоинт API, загрузка файла, приёмник вебхуков, исходящий вызов к третьей стороне, очередь, из которой читает твой код.

Старший разработчик относится к поверхности атаки как к чему-то, что *измеряют и сокращают*, а не просто инвентаризируют. Cheat sheet прямо говорит о компромиссе: больше точек входа, больше привилегированного кода и больше доверенных внешних зависимостей — всё это увеличивает поверхность, и именно поверхность, а не число строк кода, атакующий реально прощупывает. Поэтому ты спрашиваешь по каждому пересечению: должен ли этот эндпоинт вообще существовать? Может ли он требовать аутентификацию? Может ли он работать с меньшими привилегиями? Эндпоинт, который ты удалил, нельзя проэксплуатировать; внутренний сервис, который ты сделал недостижимым извне, убирает целую границу с карты атакующего. Сокращение поверхности — самый дешёвый контроль, которым ты владеешь, потому что оно убирает угрозу, а не смягчает её.

<Inset kind="why" lang="ru">
Почему круги, прямоугольники и параллельные линии, а не просто подписанные коробки? Формы — не украшение: они кодируют, *кто запускает код*. Прямоугольник (внешняя сущность) значит «не твой код, проверяй всё, что он шлёт». Круг (процесс) значит «твой код, где живут логические баги». Параллельные линии (хранилище) значат «данные в покое, спроси о шифровании и контроле доступа». Когда ты неправильно подписываешь коробку — скажем, рисуешь сервис партнёра как процесс, потому что он ощущается «частью системы», — ты тихо двигаешь границу доверия, и стёртая граница — это именно та, через которую атакующий проходит насквозь. Нотация — это форсирующая функция честности о том, что ты на самом деле контролируешь.
</Inset>

## От DFD к STRIDE-по-элементам

Вот выгода, ради которой стоит рисовать диаграмму: **STRIDE-по-элементам**. Вместо свободного брейншторма угроз ты методично обходишь DFD и применяешь только те буквы STRIDE, которые каждый тип элемента реально может пострадать. Тип элемента подсказывает, какие буквы вообще в игре, что схлопывает открытый брейншторм в конечный, проверяемый чек-лист:

- **Внешние сущности** уязвимы к **S** и **R** (спуфинг, отрицание) — может ли кто-то выдать себя за этого актора и может ли актор отрицать действие?
- **Процессы** могут пострадать от **всего набора STRIDE** — это самая богатая мишень, особенно **T**, **I** и **E** (подмена, раскрытие, повышение).
- **Хранилища данных** в основном подвержены **T**, **R**, **I**, **D** (подмена, отрицание при слабом логировании, раскрытие, отказ в обслуживании).
- **Потоки данных** подвержены **T**, **I**, **D** (подмена, раскрытие, отказ) — именно поэтому поток, пересекающий границу, заслуживает TLS, проверок целостности и rate-лимитов.

Ты применяешь это *строже всего на пересечениях границ*. Поток данных, остающийся внутри одной зоны доверия, тоже проверяют, но поток, пересекающий границу, — там, где подмена и раскрытие наиболее вероятны и опасны, поэтому он заслуживает самого глубокого разбора. DFD плюс границы плюс отображение «тип элемента → STRIDE» — это и есть весь движок: он превращает «что может пойти не так?» — вопрос, рождающий панику или махание руками, — в «для каждого элемента какие из этих конкретных букв применимы и контролируется ли пересечение?».

<TradeoffMatrix
  id="03-dft-trust-boundaries-tradeoff-1"
  lessonSlug="03-data-flow-and-trust-boundaries"
  lang="ru"
  prompt="Твоя DFD показывает внутренний gRPC-процесс `recommendations`, который «должны» вызывать только другие сервисы, поэтому команда нарисовала его внутри доверенной зоны без границы на входящем потоке. Как модель угроз должна с ним поступить?"
  options={[
    { name: "Провести границу доверия по реальной сетевой достижимости сервиса, а не по предполагаемому вызывающему — если ingress может до него дотянуться, он стоит на интернет-границе и обязан аутентифицировать каждого вызывающего", summary: "Граница следует за достижимостью.", correct: true, justification: "Граница доверия определяется тем, где данные реально могут пересечь, а не тем, где ты хочешь, чтобы они пересекали. Если неправильно настроенный маршрут делает сервис достижимым извне, реальная граница — у его входной двери, и STRIDE-по-элементам тогда требует контролей против спуфинга и повышения (authn на каждом вызове, наименьшие привилегии). Это ровно сбой пропущенной границы: диаграмма доверилась намерению вместо достижимости." },
    { name: "Оставить внутри доверенной зоны — это внутренний сервис, а внутренний трафик доверен по определению", summary: "Доверять сети.", justification: "Это допущение, на котором команды и взламывают. «Внутренний» — это намерение деплоя, а не навязанное свойство; один неправильный ingress или SSRF делает сервис достижимым, и поскольку граница не нарисована, аутентификация не требовалась. Доверие должно следовать за реальной достижимостью, а не за оргструктурой сервисов." },
    { name: "Зашифровать gRPC-канал через mTLS и считать дело сделанным", summary: "Зашифровать трубу.", justification: "mTLS — настоящий пограничный контроль, но одно шифрование защищает поток в транзите — оно не решает, авторизован ли вызывающий, как только канал установлен. Тебе всё равно нужны authn/authz на процессе и граница, отмеченная на диаграмме; контроль, для которого граница не нарисована, обычно применяется непоследовательно или пропускается." },
    { name: "Поставить WAF перед всем кластером, чтобы ничему внутреннему не нужна была своя граница", summary: "Только периметр.", justification: "Единый периметр предполагает, что всё за ним доверено — плоская сетевая модель, в которой одна точка опоры дотягивается до всего. Сервису рекомендаций всё равно нужна своя граница, потому что атаки зарождаются и изнутри (SSRF, скомпрометированный сосед). Рисование границ на каждом пересечении, а не только на периметре, — вот что ограничивает радиус поражения." },
  ]}
/>

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Элемент DFD</th>
        <th class="border border-border px-3 py-2 text-left">Форма</th>
        <th class="border border-border px-3 py-2 text-left">Применимый STRIDE</th>
        <th class="border border-border px-3 py-2 text-left">Вопрос на его границе</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">Внешняя сущность</td>
        <td class="border border-border px-3 py-2">Прямоугольник</td>
        <td class="border border-border px-3 py-2">S, R</td>
        <td class="border border-border px-3 py-2">Можно ли выдать себя за неё? Может ли она отрицать действие?</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Процесс</td>
        <td class="border border-border px-3 py-2">Круг</td>
        <td class="border border-border px-3 py-2">S, T, R, I, D, E (все)</td>
        <td class="border border-border px-3 py-2">Доверяет ли вводу, которому не должен? Можно ли повысить?</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Хранилище данных</td>
        <td class="border border-border px-3 py-2">Параллельные линии</td>
        <td class="border border-border px-3 py-2">T, R, I, D</td>
        <td class="border border-border px-3 py-2">Шифруется в покое? Контроль доступа? Аудит-лог?</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Поток данных</td>
        <td class="border border-border px-3 py-2">Стрелка</td>
        <td class="border border-border px-3 py-2">T, I, D</td>
        <td class="border border-border px-3 py-2">TLS? Проверка целостности? Rate-лимит на пересечении?</td>
      </tr>
    </tbody>
  </table>
</div>

## Сбойный режим: граница, которую ты не нарисовал

Инцидент с рекомендациями из хука — это канонический сбой DFD, и у него есть имя, которое стоит усвоить: **пропущенная граница доверия**. Он почти никогда не выглядит как некомпетентность. Команда сделала STRIDE; они просто смоделировали систему как развёрнутые *намерения*, а не реальную *достижимость*. Внутренний сервис был «внутренним», поэтому его нарисовали внутри доверенной зоны, поэтому он стоял ни на какой границе, поэтому STRIDE-по-элементам так и не спросил «можно ли выдать себя за этого вызывающего?» — и контроль, которого этот вопрос потребовал бы (аутентифицировать каждого вызывающего, наименьшие привилегии), так и не построили.

Дисциплина, которая это предотвращает: **рисуй границы там, где данные реально могут пересечь, а не там, где ты намереваешься.** Каждый раз, размещая коробку внутри доверенной зоны, задавай враждебный вопрос — «а что, если это достижимо извне?» — и проверяй достижимость по реальному конфигу (правила ingress, security groups, политики service mesh), а не по слайду архитектуры. Сеть, которую ты считаешь сегментированной, но никогда не проверял, — это плоская сеть с лишними шагами; классическая атака SSRF-к-метаданным (сервер обманом заставляют сходить за `http://169.254.169.254/`) — это ровно использование твоего же процесса для пересечения границы, до которой ты считал атакующего недосягаемым. Граница на диаграмме — это утверждение о мире. Если ты не проверил утверждение, ты не нарисовал границу — ты нарисовал желание.

<Quiz
  id="03-dft-trust-boundaries-quiz-1"
  lessonSlug="03-data-flow-and-trust-boundaries"
  lang="ru"
  question="На диаграмме потоков данных что именно определяет, где должна быть проведена граница доверия?"
  choices={[
    { label: "Везде, где данные переходят от одного субъекта, уровня привилегий или домена владения к другому", correct: true },
    { label: "Везде, где в деплое уже существует сетевой firewall или VLAN", misconception: "Существующие сетевые контроли часто совпадают с границами, но граница определяется сменой доверия/владения, а не тем, оказался ли там firewall. Граница может быть без firewall (это и есть зазор, который надо найти), а firewall может стоять посреди зоны, где смены доверия нет." },
    { label: "Только на внешнем периметре, между интернетом и всем, что ты запускаешь", misconception: "Моделирование только периметра — это ошибка плоской сети. Границы стоят и между твоими собственными сервисами на разных уровнях привилегий, и между твоим кодом и третьими сторонами — каждое внутреннее пересечение это место, где меняется доверие и концентрируются угрозы." },
    { label: "Везде, где к потоку данных применено шифрование (TLS/mTLS)", misconception: "Шифрование — это контроль, который ты можешь применить на границе; оно не определяет границу. Сначала ты рисуешь границу (где меняется доверие), затем решаешь, какие контроли — TLS, authn, валидация — нужны пересечению." },
  ]}
/>

<Quiz
  id="03-dft-trust-boundaries-quiz-2"
  lessonSlug="03-data-flow-and-trust-boundaries"
  lang="ru"
  question="Почему STRIDE-по-элементам начинается с ТИПА элемента DFD (внешняя сущность, процесс, хранилище, поток), а не просто с брейншторма угроз?"
  choices={[
    { label: "Потому что тип элемента ограничивает, какие буквы STRIDE применимы, превращая открытый брейншторм в конечный, проверяемый чек-лист", correct: true },
    { label: "Потому что хранилища данных — единственные элементы, которые можно атаковать, так что тип отсеивает остальные", misconception: "Атаковать можно каждый тип элемента — процессы могут пострадать от всех шести букв STRIDE. Тип не отсеивает элементы; он определяет, КАКИЕ буквы применимы к каждому, и именно это делает разбор конечным." },
    { label: "Потому что внешние сущности всегда безопасны и их можно пропустить полностью", misconception: "Внешние сущности подвержены спуфингу и отрицанию — это ключевая причина, почему существует аутентификация. Их не пропускают; тип просто говорит, что S и R — релевантные для них буквы." },
    { label: "Потому что это позволяет игнорировать потоки данных и сосредоточиться только на коробках", misconception: "Потоки данных — там, где подмена и раскрытие происходят в транзите, особенно на пересечениях границ — они среди важнейших элементов для разбора, а не тех, что стоит игнорировать." },
  ]}
/>

<DragOrder
  id="03-dft-trust-boundaries-drag-1"
  lessonSlug="03-data-flow-and-trust-boundaries"
  lang="ru"
  prompt="Расставь шаги построения модели угроз из системы, от первого к последнему:"
  items={[
    { id: "a", label: "Нарисовать DFD: каждую внешнюю сущность, процесс, хранилище и поток данных" },
    { id: "b", label: "Разместить границы доверия там, где реально меняется владелец или привилегия" },
    { id: "c", label: "Перечислить поверхность атаки: потоки, пересекающие границу" },
    { id: "d", label: "Применить STRIDE по элементам, строже всего на пересечениях" },
    { id: "e", label: "Решить, какие контроли или сокращение поверхности нужны каждой неустранённой угрозе" },
  ]}
  correctOrder={["a", "b", "c", "d", "e"]}
/>

</Explanation>

<KeyTakeaway>У DFD четыре элемента — внешняя сущность, процесс, хранилище, поток данных — а границы доверия это линии, где данные пересекают смену владения или привилегии. Угрозы концентрируются на этих пересечениях, что и делает STRIDE-по-элементам системным. Смертельнейший сбой — пропущенная граница: моделируй достижимость, а не намерение.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="03-dft-trust-boundaries-retrieval"
  lang="ru"
  questions={[
    { q: "Назови четыре элемента диаграммы потоков данных и объясни, что такое граница доверия и почему на ней концентрируются угрозы.", a: "У DFD ровно четыре типа элементов: внешние сущности (акторы вне твоего контроля — браузер, API партнёра — которые ты можешь только проверять, рисуются прямоугольниками), процессы (код, который запускаешь ты и который преобразует данные — API-сервер, воркер — где живёт большинство логических багов, рисуются кругами), хранилища данных (данные в покое — таблица Postgres, бакет S3, очередь — рисуются двумя параллельными линиями) и потоки данных (стрелки, несущие данные между любыми двумя, наблюдаемые и подменяемые в транзите). Граница доверия — это линия, проведённая поперёк потоков данных везде, где данные переходят от одного субъекта, уровня привилегий или домена владения к другому: браузер→API (недоверенный клиент к твоему коду), API→БД (привилегия приложения к привилегии данных), твой код→третья сторона (твои данные покидают твой контроль) или между двумя внутренними сервисами на разных уровнях доверия. Угрозы концентрируются на этих пересечениях, потому что пересечение — ровно то место, где одна сторона должна решить, доверять ли другой, и неверное решение о доверии ЕСТЬ уязвимость. Данные, никогда не покидающие одну зону доверия, сравнительно безопасны; именно передачи — там, где реально происходят спуфинг, подмена, раскрытие и повышение." },
    { q: "Как DFD ведёт STRIDE-по-элементам и что такое сбой «пропущенной границы доверия»?", a: "STRIDE-по-элементам методично обходит DFD и применяет только те буквы STRIDE, которые каждый ТИП элемента реально может пострадать: внешние сущности → S, R (спуфинг, отрицание); процессы → все шесть, особенно T, I, E; хранилища → T, R, I, D; потоки → T, I, D. Поскольку тип элемента ограничивает, какие буквы в игре, открытый брейншторм «что может пойти не так?» схлопывается в конечный, проверяемый чек-лист, применяемый строже всего на пересечениях границ (потому что там подмена и раскрытие наиболее вероятны и опасны). Поверхность атаки вытекает бесплатно — это набор потоков, пересекающих границу, каждый из которых точка входа или выхода, которую можно попытаться удалить, аутентифицировать или лишить привилегий. Сбой пропущенной границы — это моделирование намерения вместо достижимости: команда рисует «только внутренний» сервис внутри доверенной зоны, поэтому он стоит ни на какой границе, поэтому STRIDE так и не спрашивает, можно ли выдать себя за его вызывающих — и тогда неправильный ingress (или SSRF) делает его достижимым извне, и он доверяет каждому вызывающему, потому что граница, которая потребовала бы аутентификацию, так и не была нарисована. Фикс: рисуй границы там, где данные реально могут пересечь, проверяя по реальному конфигу (правила ingress, security groups, политики mesh), а не там, где ты намереваешься." },
  ]}
/>

<Recap lang="ru">
Диаграмма потоков данных моделирует систему всего четырьмя элементами — внешняя сущность (прямоугольник), процесс (круг), хранилище данных (параллельные линии) и поток данных (стрелка) — и заставляет тебя нарисовать каждую стрелку, потому что каждая стрелка это место, где данные раскрыты, а каждая коробка — место, где им доверяют или их преобразуют. Границы доверия — это линии, которые ты проводишь поперёк потоков везде, где меняется владелец или привилегия: браузер→API, API→БД, твой код→третья сторона или между внутренними сервисами на разных уровнях доверия. Угрозы концентрируются на этих пересечениях, потому что пересечение — там, где одна сторона решает, доверять ли другой, и неверное решение это баг. Границы делают поверхность атаки видимой (каждое пересечение — точка входа/выхода, которую надо аутентифицировать, лишить привилегий или удалить) и делают STRIDE системным: по типу элемента ты точно знаешь, какие буквы применимы, и проверяешь их строже всего на пересечениях. Сбой, который взламывает команды, сделавшие всё остальное правильно, — это пропущенная граница: моделирование сервиса по тому, куда ты намереваешься направить трафик, вместо того, куда он реально может дотянуться. Поэтому, рисуя коробку внутри доверенной зоны, задай враждебный вопрос и проверь его по реальному конфигу: действительно ли это недостижимо извне — или я просто нарисовал желание?
</Recap>
