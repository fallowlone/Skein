---
concepts:
- asymmetric-encryption
- rsa
- elliptic-curve
- diffie-hellman
- forward-secrecy
deepensInto: []
estMin: 15
lang: ru
lessonType: topic
level: middle
order: 3
prereqs: []
slug: 03-asymmetric-and-key-exchange
sources:
- https://cheatsheetseries.owasp.org/cheatsheets/Cryptographic_Storage_Cheat_Sheet.html
- https://datatracker.ietf.org/doc/html/rfc8446
spiral: []
status: ready
summary: 'Криптография с открытым ключом решает проблему распределения ключей, которую симметричные шифры решить не могут, но она в 1000 раз медленнее — поэтому её применяют лишь для согласования симметричного ключа. Урок про RSA и ECC, обмен Диффи-Хеллмана и прямую секретность.'
title: 'Асимметричная криптография и обмен ключами'
track: security-foundations
unit: 02-cryptography-for-engineers
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
Симметричное шифрование быстрое и нерушимое — и бесполезное, пока две стороны не разделят ключ. Вот загвоздка, о которой никто не предупреждает: AES-256 прекрасен, но как браузер и сервер *оба* узнают одни и те же 32 случайных байта, когда их первый пакет идёт через открытый Wi-Fi, полный людей, способных прочитать всё? Послать ключ в открытую нельзя — перехватчик заберёт его и расшифрует всю сессию. Заранее договориться нельзя — вы никогда не встречались с сервером. Десятилетиями это было настоящей стеной — пока в 1976-м Диффи и Хеллман не опубликовали трюк, позволяющий двум незнакомцам вывести общий секрет по прослушиваемой линии, где атакующий видит каждый байт и всё равно не может его вычислить. Всё, что вы зовёте «HTTPS», держится на этом одном приёме.
</Hook>

К концу урока вы будете знать, зачем существует асимметричная криптография, где она применяется, а где нет, как Диффи-Хеллман позволяет незнакомцам согласовать ключ в открытую и что на самом деле даёт прямая секретность.

<Crux>Асимметрию применяют лишь чтобы согласовать быстрый симметричный ключ; эфемерный Диффи-Хеллман даёт прямую секретность обмена.</Crux>

<Explanation>

## Зачем существует второй вид криптографии

Симметричные шифры — AES, ChaCha20 — используют *один* ключ и для шифрования, и для расшифровки. Они быстрые (современный CPU выдаёт гигабайты в секунду с AES-NI) и при 256 битах недостижимы для перебора. Их фатальное ограничение — **проблема распределения ключей**: обеим сторонам нужен один и тот же секрет, прежде чем они смогут общаться, а безопасно доставить его по недоверенной сети — ровно та задача, которую вы и пытались решить.

Асимметричная криптография (с открытым ключом) ломает симметрию. У каждой стороны есть **пара ключей**: открытый ключ, который она свободно раздаёт, и закрытый ключ, который она никогда не разглашает. Они математически связаны так, что сделанное одним отменяет только другой. Это даёт две разные возможности:

- **Шифрование:** кто угодно шифрует на ваш *открытый* ключ; расшифровывает только ваш *закрытый*. Конфиденциальность без заранее разделённого секрета.
- **Подписи:** вы подписываете своим *закрытым* ключом; кто угодно проверяет вашим *открытым*. Подлинность и целостность — доказательство, что сообщение пришло от владельца этого закрытого ключа и не было изменено.

Загвоздка в цене. Расшифровка RSA-2048 примерно в тысячу раз медленнее AES на байт, а у асимметричных алгоритмов есть жёсткий предел на размер того, что можно зашифровать напрямую (RSA не зашифрует больше, чем размер ключа минус набивка). Поэтому никто не шифрует тело запроса в 4 МБ через RSA. Универсальный приём — **гибридное шифрование**: дорогой асимметричный шаг выполняют *один раз*, чтобы согласовать свежий случайный симметричный ключ, а затем шифруют сам массовый трафик быстрым AES. Асимметрия — это рукопожатие; симметрия — это разговор.

## RSA против эллиптических кривых: одна задача, разная математика

Доминируют два семейства. **RSA** опирается на сложность факторизации произведения двух больших простых чисел. Оно старое, проверенное и простое для рассуждений — но его ключи большие и растут по мере улучшения атак факторизации. **Криптография на эллиптических кривых (ECC)** опирается на другую трудную задачу (дискретный логарифм на эллиптической кривой), которая сопротивляется атакам куда лучше на бит, поэтому даёт эквивалентную стойкость при кардинально меньших ключах и более быстрых операциях. Именно поэтому мобильные платформы, TLS и современные протоколы перешли на кривые вроде P-256 и X25519.

Разрыв в стойкости-на-бит — это вся суть; вот эквивалентность, которую публикуют органы стандартизации:

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Уровень стойкости</th>
        <th class="border border-border px-3 py-2 text-left">Симметрия (AES)</th>
        <th class="border border-border px-3 py-2 text-left">Размер ключа RSA</th>
        <th class="border border-border px-3 py-2 text-left">Размер ключа ECC</th>
        <th class="border border-border px-3 py-2 text-left">Что это значит</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">~112 бит</td>
        <td class="border border-border px-3 py-2">3DES (наследие)</td>
        <td class="border border-border px-3 py-2">2048 бит</td>
        <td class="border border-border px-3 py-2">224 бита</td>
        <td class="border border-border px-3 py-2">Минимум, приемлемый сегодня</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">128 бит</td>
        <td class="border border-border px-3 py-2">AES-128</td>
        <td class="border border-border px-3 py-2">3072 бита</td>
        <td class="border border-border px-3 py-2">256 бит</td>
        <td class="border border-border px-3 py-2">Современный дефолт (P-256 / X25519)</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">192 бита</td>
        <td class="border border-border px-3 py-2">AES-192</td>
        <td class="border border-border px-3 py-2">7680 бит</td>
        <td class="border border-border px-3 py-2">384 бита</td>
        <td class="border border-border px-3 py-2">Высокая гарантия (P-384)</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">256 бит</td>
        <td class="border border-border px-3 py-2">AES-256</td>
        <td class="border border-border px-3 py-2">15360 бит</td>
        <td class="border border-border px-3 py-2">521 бит</td>
        <td class="border border-border px-3 py-2">Заметьте жестокое масштабирование RSA: впятеро больше бит ради вдвое большей стойкости</td>
      </tr>
    </tbody>
  </table>
</div>

Вчитайтесь в нижнюю строку: чтобы сравняться с AES-256, RSA нужен ключ в 15360 бит — на котором генерация ключа и операции мучительно медленны, — тогда как ECC хватает 521 бита. Размер ключа RSA растёт примерно как *кубический корень* из работы по его взлому, поэтому удвоение уровня стойкости более чем удваивает ключ. Этот нелинейный штраф — причина, по которой новые системы по умолчанию берут кривые, а RSA вы встречаете в основном лишь в наследных сертификатах и существующей PKI.

## Диффи-Хеллман: согласовать секрет в открытую

Передача ключа в стиле RSA (одна сторона выбирает симметричный ключ и шифрует его на открытый ключ другой) работает, но у неё есть изъян, к которому мы вернёмся. Более важный примитив — **обмен ключами Диффи-Хеллмана (DH)** и его форма на эллиптических кривых **ECDH**. DH — это не шифрование, а способ для двух сторон *совместно вычислить* общий секрет, который ни одна из них не выбирала в одиночку, по каналу, который атакующий читает целиком.

Механизм, очищенный до сути: обе стороны договариваются об открытых параметрах. Каждая сторона выбирает **закрытое** случайное значение, выводит из него **открытое** значение и посылает по проводу только открытое. Затем каждая сторона комбинирует *своё закрытое значение* с *чужим открытым* — и по алгебре конструкции обе приходят к идентичному общему секрету. Перехватчик видел оба открытых значения, но ни одного закрытого, а восстановить секрет из того, что прошло по проводу, — та самая трудная задача (дискретный логарифм), для которой нет эффективной атаки. Затем общий секрет прогоняют через функцию выведения ключа, получая симметричный сессионный ключ.

<FlowDiagram
  label="Обмен ключами Диффи-Хеллмана по открытому каналу: Алиса и Боб каждый выбирают закрытое значение, выводят и посылают открытое значение, затем комбинируют своё закрытое со чужим открытым и приходят к одному общему секрету; перехватчик видит оба открытых значения, но не может вычислить секрет, потому что задача дискретного логарифма не имеет эффективного решения."
  perRow={5}
  hue="var(--d-crypto)"
  nodes={[
    { id: "param", label: "Открытые параметры", sub: "согласованы открыто" },
    { id: "apriv", label: "Закрытое Алисы", sub: "a (секрет)" },
    { id: "apub", label: "Открытое Алисы", sub: "идёт по проводу" },
    { id: "bpub", label: "Открытое Боба", sub: "идёт по проводу" },
    { id: "shared", label: "Общий секрет", sub: "→ ключ AES" },
  ]}
  edges={[
    { from: "param", to: "apriv" },
    { from: "apriv", to: "apub", label: "вывести" },
    { from: "apub", to: "shared", label: "скомбинировать" },
    { from: "bpub", to: "shared", label: "с b" },
  ]}
  caption="DH позволяет двум незнакомцам вычислить один и тот же секрет, ни разу не передав его. Атакующий видит оба открытых значения на проводе; вычислить из них общий секрет — это задача дискретного логарифма, у которой при таких размерах нет эффективного решения."
/>

Есть критическая оговорка: чистый DH даёт конфиденциальность против *пассивного* перехватчика, но **не** говорит, кто на другом конце. Активный атакующий, севший посередине, может провести отдельный DH с каждой стороной и ретранслировать между ними — человек посередине (MITM). Поэтому DH обязан сочетаться с **аутентификацией**: в TLS сервер *подписывает* своё открытое значение DH закрытым ключом из-за своего сертификата, чтобы клиент знал, что значение пришло от настоящего сервера, а не от самозванца. Обмен ключами устанавливает секрет; подписи и сертификаты устанавливают, *с кем* вы им делитесь. Нужно и то, и другое.

<Inset kind="why" lang="ru">
Почему не продолжать пользоваться передачей ключа RSA, где клиент шифрует выбранный сессионный ключ на открытый ключ сервера? Из-за того, что происходит *после* взлома. При передаче ключа RSA сессионный ключ защищён долгоживущим закрытым ключом сервера — поэтому атакующий, записавший ваш зашифрованный трафик сегодня и укравший этот закрытый ключ через два года, сможет вернуться и расшифровать каждую записанную сессию. Статический ключ никогда не менялся, поэтому одна кража отпирает весь архив. Именно эту слабость убирает эфемерный Диффи-Хеллман, и именно поэтому TLS 1.3 полностью отказался от передачи ключа RSA и предписывает эфемерный (EC)DH для каждого рукопожатия.
</Inset>

## Прямая секретность: делая прошлое нерасшифровываемым

Лекарство от той архивной проблемы — **прямая секретность** (forward secrecy, она же совершенная прямая секретность). Идея: генерировать *совершенно новую, одноразовую* пару ключей DH для каждой сессии — это и есть **эфемерность** в ECDHE — и выбрасывать закрытые значения в тот же миг, как рукопожатие завершилось. Долговременный ключ сертификата используется лишь чтобы *подписать* эфемерный обмен, доказывая личность; он никогда не шифрует сессионный ключ и никогда не касается общего секрета.

Выигрыш — это свойство, при котором атакующий, скомпрометировавший долговременный закрытый ключ сервера *позже*, не может расшифровать трафик, захваченный *раньше*. Секрет каждой сессии умер вместе с эфемерными ключами, которые его породили; на диске не осталось ничего, из чего его можно восстановить. Без прямой секретности один украденный закрытый ключ задним числом отпирает годы записанных сессий — атака «собери сейчас, расшифруй потом», где противник записывает шифртекст сегодня именно для того, чтобы взломать его, как только получит ключ. С прямой секретностью каждая сессия — свой остров. Именно поэтому TLS 1.3 делает эфемерный (EC)DH необсуждаемым.

<TradeoffMatrix
  id="03-asym-key-exchange-tradeoff-1"
  lessonSlug="03-asymmetric-and-key-exchange"
  lang="ru"
  prompt="Вы настраиваете TLS для сервиса, обрабатывающего чувствительные долгоживущие данные, и должны выбрать режим обмена ключами. Какой выбор даёт прямую секретность и почему это здесь важно?"
  options={[
    { name: "Эфемерный ECDH (ECDHE): свежая пара ключей DH на сессию, подписанная сертификатом; закрытые значения выбрасываются после рукопожатия", summary: "С прямой секретностью.", correct: true, justification: "Это и есть прямая секретность. Общий секрет каждой сессии происходит из одноразовых эфемерных ключей, уничтожаемых по завершении рукопожатия, поэтому сессионный ключ потом нигде не существует. Если долговременный ключ сертификата украдут в следующем году, записанный трафик этого года останется нечитаемым — эфемерные секреты, породившие те сессионные ключи, исчезли. Для долгоживущих чувствительных данных это прямо побеждает «собери сейчас, расшифруй потом». Это дефолт TLS 1.3 ровно по этой причине." },
    { name: "Передача ключа RSA: клиент шифрует выбранный сессионный ключ на статический открытый ключ RSA сервера", summary: "Без прямой секретности.", justification: "Здесь нет прямой секретности. Сессионный ключ защищён исключительно долговременным закрытым ключом сервера, поэтому атакующий, записавший сегодняшний шифртекст и укравший этот один ключ позже, сможет задним числом расшифровать каждую записанную сессию. TLS 1.3 полностью убрал этот режим. Статический ключ — единая точка катастрофического, дотягивающегося в прошлое отказа." },
    { name: "Статический (неэфемерный) Диффи-Хеллман с повторным использованием одной пары ключей DH во всех сессиях", summary: "DH без сути.", justification: "Повторное использование фиксированной пары ключей DH выбрасывает всю выгоду DH. Поскольку одно и то же закрытое значение защищает каждую сессию, его однократная компрометация снова отпирает все записанные сессии — вы воссоздали слабость статического ключа, заплатив за DH. Прямая секретность требует, чтобы ключи были эфемерными; повторно используемый ключ DH не эфемерен." },
    { name: "Более длинный ключ AES (AES-256 вместо AES-128) для массового шифрования", summary: "Не тот слой.", justification: "Размер ключа AES — про сопротивление перебору симметричного шифра; к прямой секретности он отношения не имеет, ведь та — свойство того, как ключ был *согласован*. Более длинный ключ AES всё равно оставляет сессию расшифровываемой, если режим обмена ключами позволяет будущей краже закрытого ключа восстановить этот сессионный ключ. Вы бы укрепляли разговор, оставив архивную слабость рукопожатия нетронутой." },
  ]}
/>

## Где асимметрия применяется, а где нет

Зрелая ментальная модель: асимметрия — для *начальной загрузки доверия и ключей*, а не для *массовой работы*. Она появляется ровно в трёх моментах — согласование сессионного ключа (ECDHE), доказательство личности (подпись сертификата над рукопожатием) и проверка подлинности в покое (подписанные обновления ПО, подписанные JWT, подпись кода). Всё остальное — гигабайты реальных тел запросов и ответов, данные, зашифрованные в вашей БД, файлы на диске — это симметричный AES, потому что асимметрия слишком медленна и ограничена по размеру для такой работы. Когда вы видите «зашифруй эту загрузку в 50 МБ», ответ никогда не «RSA»; он — «сгенерируй случайный ключ AES, зашифруй данные через AES и защити этот один маленький ключ асимметричным слоем».

<Quiz
  id="03-asym-key-exchange-quiz-1"
  lessonSlug="03-asymmetric-and-key-exchange"
  lang="ru"
  question="Почему реальные системы используют гибридное шифрование (асимметрия для согласования ключа, затем симметрия для данных), а не просто шифруют всё открытым ключом получателя?"
  choices={[
    { label: "Асимметрия примерно в 1000 раз медленнее симметрии и имеет жёсткий предел на размер прямого шифрования, поэтому её применяют лишь для защиты маленького симметричного ключа", correct: true },
    { label: "Шифрование с открытым ключом недостаточно стойкое, чтобы защитить реальные данные само по себе", misconception: "Это не вопрос стойкости — корректно применённые RSA/ECC стойки. Причина — производительность и предел на размер прямого шифрования, поэтому асимметрия защищает лишь маленький сессионный ключ." },
    { label: "Симметричные шифры взломать нельзя, а асимметричные можно, поэтому асимметрию минимизируют", misconception: "Оба семейства стойки при правильных размерах ключей. Гибридное шифрование — про скорость и пределы размера, а не про взламываемость одного из семейств." },
    { label: "Браузеры не поддерживают шифрование больших данных открытым ключом", misconception: "Ограничение в математике, а не в поддержке браузеров: асимметричные алгоритмы по своей природе медленны и ограничены по размеру, поэтому стандарт повсюду — шифровать массовые данные симметричным AES." },
  ]}
/>

<Quiz
  id="03-asym-key-exchange-quiz-2"
  lessonSlug="03-asymmetric-and-key-exchange"
  lang="ru"
  question="Атакующий два года молча записывал ваш TLS-трафик. Сегодня он крадёт долговременный закрытый ключ вашего сервера. При эфемерном ECDH из TLS 1.3 что он сможет расшифровать?"
  choices={[
    { label: "Ничего из записанного прошлого — секрет каждой сессии происходил из одноразовых эфемерных ключей, уничтоженных после рукопожатия; украденный долговременный ключ только подписывал, он никогда не порождал сессионные ключи", correct: true },
    { label: "Всё, что записал, потому что закрытый ключ использовался для выведения каждого сессионного ключа", misconception: "Это описывает передачу ключа RSA, а не эфемерный ECDH. При ECDHE долговременный ключ только подписывает рукопожатие; сессионные ключи происходят из эфемерных значений DH, которых больше нет, поэтому прошлый трафик остаётся запечатанным." },
    { label: "Только сессии за последние 24 часа, потому что ключи ротируются ежедневно", misconception: "Прямая секретность — не про окно ротации: эфемерные ключи посессионны и выбрасываются немедленно, поэтому здесь нет понятия суточного ключа; защищены все прошлые сессии, а не только недавние." },
    { label: "Весь будущий трафик, но не записанное прошлое", misconception: "С украденным ключом он мог бы выдавать себя за сервер впредь (пока вы не проротируете), но вопрос — о записанном прошлом, а прямая секретность означает, что прошлое остаётся нерасшифровываемым в любом случае." },
  ]}
/>

<DragOrder
  id="03-asym-key-exchange-drag-1"
  lessonSlug="03-asymmetric-and-key-exchange"
  lang="ru"
  prompt="Упорядочьте шаги рукопожатия и сессии TLS с прямой секретностью, от первого к последнему:"
  items={[
    { id: "a", label: "Каждая сторона генерирует свежую эфемерную пару ключей DH для этой сессии" },
    { id: "b", label: "Каждая сторона посылает своё эфемерное открытое значение DH по проводу" },
    { id: "c", label: "Сервер подписывает своё эфемерное значение закрытым ключом сертификата (аутентификация)" },
    { id: "d", label: "Обе стороны комбинируют своё закрытое значение с открытым значением партнёра и выводят один общий секрет" },
    { id: "e", label: "Массовые данные приложения идут зашифрованными быстрым симметричным ключом (AES), выведенным из этого секрета" },
  ]}
  correctOrder={["a", "b", "c", "d", "e"]}
/>

</Explanation>

<KeyTakeaway>Асимметрия решает распределение ключей, но примерно в 1000 раз медленнее, поэтому лишь согласует симметричный ключ (гибридное шифрование). ECC бьёт RSA на бит. Диффи-Хеллман позволяет незнакомцам вывести общий секрет по открытому проводу; делая его эфемерным (ECDHE), вы получаете прямую секретность — будущая кража закрытого ключа не расшифрует записанные прошлые сессии.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="03-asym-key-exchange-retrieval"
  lang="ru"
  questions={[
    { q: "Объясните, почему системы используют гибридное шифрование и что делает каждое семейство криптографии в TLS-сессии.", a: "Симметричные шифры вроде AES быстры и нерушимы при 256 битах, но страдают проблемой распределения ключей: обеим сторонам нужен один секрет до начала общения, а безопасно послать его по недоверенной сети нельзя. Асимметрия решает распределение — у каждой стороны есть открытый ключ, который она раздаёт, и закрытый, который хранит, — но она примерно в 1000 раз медленнее AES на байт и имеет жёсткий предел на размер прямого шифрования. Поэтому реальные системы используют гибридное шифрование: дорогой асимметричный шаг выполняется один раз, чтобы согласовать свежий случайный симметричный ключ, а затем быстрый AES шифрует весь массовый трафик. В TLS-сессии асимметрия делает ровно три работы — согласует сессионный ключ (эфемерный ECDH), аутентифицирует личность (сервер подписывает рукопожатие закрытым ключом сертификата) и проверяет подлинность вещей в покое (подписанные обновления, JWT). Всё остальное — реальные тела запросов и ответов — это симметричный AES, потому что асимметрия слишком медленна и ограничена по размеру для массовой работы." },
    { q: "Что такое Диффи-Хеллман, почему ему нужна аутентификация и как эфемерный DH даёт прямую секретность?", a: "Диффи-Хеллман — это примитив обмена ключами (не шифрование), позволяющий двум незнакомцам совместно вычислить общий секрет по каналу, который атакующий читает целиком. Обе стороны договариваются об открытых параметрах, каждая выбирает закрытое случайное значение и посылает лишь выведенное открытое; затем каждая комбинирует своё закрытое значение с открытым значением партнёра и приходит к идентичному секрету. Перехватчик видит оба открытых значения, но восстановить секрет — это задача дискретного логарифма, у которой при таких размерах нет эффективного решения. Форма на эллиптических кривых — ECDH. Чистый DH защищает только от пассивного перехватчика — он не доказывает, кто на другом конце, поэтому активный человек посередине мог бы провести отдельные обмены с каждой стороной. Вот почему DH обязан сочетаться с аутентификацией: в TLS сервер подписывает своё открытое значение DH закрытым ключом сертификата. Прямая секретность приходит от того, что ключи DH делают эфемерными (ECDHE): совершенно новая одноразовая пара ключей DH на сессию, выбрасываемая сразу после рукопожатия, при этом долговременный ключ сертификата только подписывает — никогда не порождает сессионный ключ. Поэтому если атакующий записывает трафик годами и позже крадёт долговременный закрытый ключ, он всё равно не может расшифровать записанное прошлое, ведь эфемерный секрет каждой сессии уже исчез. Это побеждает «собери сейчас, расшифруй потом» и именно поэтому TLS 1.3 предписывает эфемерный (EC)DH и убрал передачу ключа RSA." },
  ]}
/>

<Recap lang="ru">
Симметричная криптография (AES) быстра и стойка, но не может решить распределение ключей: обеим сторонам сначала нужен один и тот же секрет. Асимметрия чинит это парами открытый/закрытый ключ — кто угодно шифрует на ваш открытый ключ, расшифровывает только ваш закрытый; вы подписываете закрытым ключом, проверяет кто угодно открытым. Но она примерно в 1000 раз медленнее и ограничена по размеру, поэтому универсальный приём — гибридное шифрование: применить асимметрию один раз для согласования симметричного ключа, затем AES для массива. ECC кардинально бьёт RSA на бит (256-битная кривая равна 3072-битному ключу RSA), поэтому современные протоколы берут P-256 и X25519. Диффи-Хеллман позволяет двум незнакомцам вывести общий секрет по прослушиваемой линии, потому что восстановить его — задача дискретного логарифма, — но ему нужна аутентификация (подпись), чтобы остановить человека посередине. Делая ключи DH эфемерными и посессионными, вы получаете прямую секретность: будущая кража долговременного ключа не расшифрует записанный прошлый трафик, поэтому TLS 1.3 предписывает эфемерный (EC)DH. В следующий раз, увидев «зашифруй эту большую нагрузку», ваш рефлекс — сгенерировать случайный ключ AES, зашифровать данные через AES и защитить этот один маленький ключ асимметричным слоем — никогда не «RSA» целиком.
</Recap>
