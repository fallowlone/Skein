---
concepts:
- hashing
- encryption
- digital-signature
- one-way-function
- integrity-vs-confidentiality
deepensInto: []
estMin: 15
lang: ru
lessonType: topic
level: middle
order: 1
prereqs: []
slug: 01-hashing-encryption-signing
sources:
- https://cheatsheetseries.owasp.org/cheatsheets/Cryptographic_Storage_Cheat_Sheet.html
- https://datatracker.ietf.org/doc/html/rfc8446
spiral: []
status: ready
summary: 'Хеширование, шифрование и подпись — три разных примитива, которые инженеры
  постоянно путают. Каждый гарантирует своё — а выбор не того примитива и есть способ,
  которым утекают пароли, подделываются сессии, а «оно зашифровано» оказывается пустым
  словом.'
title: Хеширование против шифрования против подписи
track: security-foundations
unit: 02-cryptography-for-engineers
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
Джуниор из твоей команды выкатывает «безопасное» хранилище паролей: `AES-256` поверх каждого пароля, ключ в конфиге. Ревью одобряет — AES-256, звучит надёжно. Через полгода утекает бэкап, вместе с ним утекает ключ, и каждый пароль в базе восстанавливается в открытом виде. Баг был не в слабом алгоритме. Это был *полностью неверный примитив*: пароли нужно **хешировать**, а не шифровать, именно потому что хеширование — единственная операция, которую нельзя обратить. На той же неделе другой инженер «подписывает» API-нагрузку, прогоняя её через SHA-256, — и не доказывает ничего, ведь этот хеш может пересчитать кто угодно. Три примитива, три разные гарантии, и обращение не к тому из них — один из самых частых способов, которыми в остальном аккуратные системы получают пробой.
</Hook>

К концу этого урока ты сможешь для хеширования, шифрования и подписи назвать ровно то, что каждый из них гарантирует, чего он *не* гарантирует и какой из них на самом деле нужен конкретной задаче.

<Crux>Хеш доказывает целостность, шифрование скрывает содержимое, подпись — происхождение; спутай их, и «безопасно» ничего не значит.</Crux>

<Explanation>

## Три примитива и единственное свойство, которое их разделяет

Самый быстрый способ держать их в голове — задать каждому один вопрос: **можно ли вернуть оригинал?**

- **Хеширование** — это *односторонняя функция*. `SHA-256("hello")` всегда даёт один и тот же 256-битный дайджест, но `unhash()` не существует — вход уничтожен. Проверяешь, пересчитав хеш кандидата и сравнив. Его работа — **целостность** и **отпечаток**: совпали ли эти байты бит-в-бит? Он ничего не говорит о конфиденциальности (дайджест публичен) и ничего о том, *кто* его создал.
- **Шифрование** — *обратимо при наличии ключа*. `encrypt(plaintext, key)` даёт шифротекст, который `decrypt(ciphertext, key)` превращает обратно в открытый текст. Его работа — **конфиденциальность**: держать содержимое в секрете от всех без ключа. Само по себе классическое шифрование *не* доказывает, что сообщение не подменили, — поэтому современные системы используют аутентифицированное шифрование (AES-GCM, ChaCha20-Poly1305), привинчивающее тег целостности.
- **Подпись** — *асимметричная*. Подписываешь **приватным** ключом, а проверяет кто угодно соответствующим **публичным**. Её работа — **аутентичность** (это пришло от держателя приватного ключа) плюс **целостность** (и не было изменено с тех пор). Она намеренно *не* скрывает содержимое — подписанный JWT читается кем угодно.

<FlowDiagram
  label="Три примитива, разложенные по обратимости и модели ключа: хеширование одностороннее, без ключа, и даёт дайджест для целостности; шифрование обратимо одним общим секретным ключом и даёт шифротекст для конфиденциальности; подпись использует приватный ключ для создания подписи и публичный для проверки, давая аутентичность и целостность."
  perRow={3}
  hue="var(--d-crypto)"
  nodes={[
    { id: "hash", label: "Хеширование", sub: "односторон., без ключа" },
    { id: "enc", label: "Шифрование", sub: "обратимо, ключ" },
    { id: "sign", label: "Подпись", sub: "приват/публич" },
    { id: "digest", label: "Дайджест", sub: "целостность" },
    { id: "cipher", label: "Шифротекст", sub: "конфиденциальность" },
    { id: "sig", label: "Подпись", sub: "аутентичность" },
  ]}
  edges={[
    { from: "hash", to: "digest", label: "отпечаток" },
    { from: "enc", to: "cipher", label: "скрыть" },
    { from: "sign", to: "sig", label: "доказать источник" },
  ]}
  caption="Их разделяет один вопрос: можно ли обратить? Хеширование — никогда (нет ключа). Шифрование — да, ключом. Подпись — проверяешь другим (публичным) ключом, чем подписывал. Столбец выхода называет единственное свойство, которое каждый из них покупает."
/>

## Чего каждый из них *не* гарантирует — режимы отказа

Пробои берутся из негативного пространства: когда примитиву приписывают свойство, которое он никогда не обещал.

**Хеширование — не шифрование.** Хеш безопасен для публикации, но не для секрета. «Мы хешируем СНИЛСы» не защищает ничего, если пространство входов мало — атакующий хеширует все возможные значения (их порядка 10⁹) и сопоставляет за секунды. Хеширование скрывает только *высокоэнтропийное и неугадываемое*. Поэтому же простой хеш неверен для паролей: пароли низкоэнтропийны, так что нужен *медленный, посоленный* парольный хеш (`bcrypt`, `scrypt`, `Argon2id`), весь смысл которого — быть дорогим в вычислении, побеждая офлайн-перебор. `SHA-256(пароль)` — это миллиарды попыток в секунду на GPU; Argon2id с правильными параметрами — единицы.

**Шифрование — не целостность и не аутентификация.** Классический шифротекст в режиме CBC может быть *изменён* атакующим, который не может его прочитать, — переверни биты в шифротексте, перевернутся биты в расшифрованном открытом тексте (семейства атак padding-oracle и bit-flipping). «Оно зашифровано» не значит «оно неподменено», если ты не используешь AEAD-режим, который аутентифицирует. И шифрование ничего не говорит о том, *кто* отправил: общий ключ означает, что сообщение мог создать любой, у кого этот ключ есть.

**Подпись — не шифрование.** Подпись прикреплена к данным, которые остаются в открытом виде. Люди регулярно думают, что подписанный JWT «безопасен», и кладут секреты в его нагрузку — но нагрузка это просто base64, читаемый кем угодно, кто перехватит токен. Подпись защищает от подделки и подмены, но никогда от подслушивания.

<Inset kind="why" lang="ru">
Почему подпись *асимметрична* (приватным подписываешь, публичным проверяешь), если MAC на общем ключе вроде HMAC тоже доказывает целостность? Из-за того, *кто что может*. С HMAC проверяющий держит тот же секрет, что и подписывающий, — значит, проверяющий тоже мог подделать сообщение; он доказывает целостность лишь между взаимно доверяющими сторонами. Цифровая подпись расщепляет ключ: только держатель приватного ключа может создать валидную подпись, а проверить может *весь мир*, не получая возможности подделать. Поэтому TLS-сертификаты, релизы ПО и JWT, подписанные `RS256`/`ES256`, используют асимметричные подписи — нужно, чтобы третьи стороны проверяли происхождение, не доверяя им силу подделки. HMAC (`HS256`) уместен только тогда, когда одна сторона и выпускает, и проверяет токен.
</Inset>

## Выбор верного примитива под задачу

Зрелый ход — отображать *требование* на примитив, а не имя алгоритма на ощущение «надёжности». Пройди требование: мне нужно держать это в секрете (конфиденциальность → шифрование), доказать, что это не изменили (целостность → хеш/MAC/подпись), доказать, кто это создал (аутентичность → подпись или MAC), или хранить секрет, который мне нужно лишь *проверять*, никогда не считывая обратно (односторонне → парольный хеш)?

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Требование</th>
        <th class="border border-border px-3 py-2 text-left">Примитив</th>
        <th class="border border-border px-3 py-2 text-left">Конкретный выбор</th>
        <th class="border border-border px-3 py-2 text-left">Ловушка, которой избежать</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">Хранить пароль</td>
        <td class="border border-border px-3 py-2">Медленный парольный хеш</td>
        <td class="border border-border px-3 py-2">Argon2id (или bcrypt/scrypt) + соль на пользователя</td>
        <td class="border border-border px-3 py-2">Шифровать его (утечка ключа = открытый текст) или простой SHA-256 (перебор на GPU)</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Держать данные в секрете на хранении / в передаче</td>
        <td class="border border-border px-3 py-2">Аутентифицированное шифрование</td>
        <td class="border border-border px-3 py-2">AES-256-GCM или ChaCha20-Poly1305</td>
        <td class="border border-border px-3 py-2">Неаутентифицированный CBC (поддаётся подмене); повтор nonce</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Проверить, что файл/загрузка целы</td>
        <td class="border border-border px-3 py-2">Хеш (отпечаток)</td>
        <td class="border border-border px-3 py-2">Дайджест SHA-256, сравнение за постоянное время</td>
        <td class="border border-border px-3 py-2">MD5/SHA-1 (коллизии); доверие хешу из того же канала</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Доказать происхождение третьим сторонам</td>
        <td class="border border-border px-3 py-2">Асимметричная подпись</td>
        <td class="border border-border px-3 py-2">Ed25519 / ECDSA (RS256/ES256 для JWT)</td>
        <td class="border border-border px-3 py-2">Класть секреты в подписанную, но незашифрованную нагрузку</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Защитить токен от подмены между двумя доверяющими сервисами</td>
        <td class="border border-border px-3 py-2">MAC (общий ключ)</td>
        <td class="border border-border px-3 py-2">HMAC-SHA-256</td>
        <td class="border border-border px-3 py-2">Считать HMAC проверяемой третьими сторонами подписью</td>
      </tr>
    </tbody>
  </table>
</div>

Реальная система обычно *композирует* их, а не выбирает один. TLS 1.3 — протокол за каждым `https://` — использует все три в одном рукопожатии: асимметричную **подпись** для аутентификации сертификата сервера, асимметричный обмен ключами для согласования секрета, этот секрет питает **аутентифицированное шифрование** сессии, а **хеширование** (HKDF) вплетено в вывод ключей и в транскрипт, доказывающий, что само рукопожатие не было подменено. Понимание, какой примитив делает какую работу внутри этого стека, — разница между настройкой TLS и слепым копированием.

<TradeoffMatrix
  id="01-hashing-encryption-signing-tradeoff-1"
  lessonSlug="01-hashing-encryption-signing"
  lang="ru"
  prompt="Ты хранишь пароли пользователей для системы входа. Выбери верный подход."
  options={[
    { name: "Хешировать каждый пароль медленным, посоленным парольным хешем (Argon2id / bcrypt / scrypt); при входе хешировать попытку и сравнивать", summary: "Односторонне, медленно, с солью.", correct: true, justification: "Хранение пароля — односторонняя задача: оригинал тебе никогда не нужен, только проверить кандидата. Медленный посоленный парольный хеш создан ровно для этого — соль побеждает предвычисленные радужные таблицы, а намеренная стоимость (память/итерации) делает офлайн-перебор утёкших хешей экономически болезненным. Даже если база утечёт, нет ни ключа, ни обратной функции, чтобы восстановить открытый текст." },
    { name: "Шифровать каждый пароль AES-256 и хранить ключ в конфиге приложения", summary: "Обратимо ключом.", justification: "Это классический неверный примитив. Шифрование обратимо по замыслу, поэтому вся безопасность сводится к защите ключа — а ключи утекают через конфиги, бэкапы, дампы env и историю исходников. В момент раскрытия ключа каждый пароль — открытый текст. Пароли должны быть необратимы, что даёт ровно хеш и не может дать шифрование." },
    { name: "Хешировать каждый пароль одним раундом SHA-256 (быстро и стандартно)", summary: "Быстрый хеш, без соли.", justification: "Это верное семейство (одностороннее), но неверный его представитель. Быстрый непосоленный хеш проверяем на миллиардах попыток в секунду на GPU и уязвим к предвычисленным радужным таблицам. Пароли низкоэнтропийны, поэтому нужен намеренно медленный, посоленный, требовательный к памяти хеш — скорость здесь уязвимость, а не достоинство." },
    { name: "Подписывать каждый пароль приватным ключом сервера, чтобы его нельзя было подделать", summary: "Асимметричная подпись.", justification: "Подпись решает аутентичность, а не хранение секрета. Подпись не скрывает пароль (данные остаются в открытом виде) и не даёт одностороннего сравнения для входа. Неверная задача, неверный примитив." },
  ]}
/>

<Quiz
  id="01-hashing-encryption-signing-quiz-1"
  lessonSlug="01-hashing-encryption-signing"
  lang="ru"
  question="Коллега говорит: «JWT подписан нашим ключом, так что класть API-токен пользователя внутрь нагрузки безопасно». В чём ошибка?"
  choices={[
    { label: "Подпись доказывает, что токен не подделали и не изменили, но не шифрует нагрузку — любой, кто перехватит JWT, может декодировать base64 и прочитать API-токен", correct: true },
    { label: "Ничего не так; подписанный JWT зашифрован end-to-end", misconception: "Стандартный подписанный JWT (JWS) не зашифрован. Заголовок и нагрузка закодированы base64url, а не являются шифротекстом — читаемы любым держателем токена. Чтобы скрыть нагрузку, нужен JWE (зашифрованный JWT)." },
    { label: "Проблема в том, что JWT должны использовать хеш, а не подпись", misconception: "Простой хеш не может аутентифицировать токен между сторонами — кто угодно его пересчитает. Подпись — верный механизм целостности/аутентичности; реальная ошибка в допущении, что подпись ещё и даёт конфиденциальность." },
    { label: "Всё нормально, пока используешь HS256 вместо RS256", misconception: "Выбор алгоритма не меняет того, что подпись оставляет нагрузку в открытом виде. Ни HS256, ни RS256 не шифруют тело; оба лишь подписывают его." },
  ]}
/>

<Quiz
  id="01-hashing-encryption-signing-quiz-2"
  lessonSlug="01-hashing-encryption-signing"
  lang="ru"
  question="Почему шифрование данных одним AES-256-CBC (без MAC, без AEAD) недостаточно, когда атакующий может изменить шифротекст в передаче?"
  choices={[
    { label: "CBC даёт конфиденциальность, но не целостность — атакующий может переворачивать биты шифротекста, предсказуемо меняя расшифрованный текст, поэтому нужно аутентифицированное шифрование (AES-GCM) или MAC поверх шифротекста", correct: true },
    { label: "AES-256 слишком слаб; нужно использовать AES-512", misconception: "AES-512 не существует, и размер ключа не в этом дело. Пробел — в целостности, а не в стойкости конфиденциальности: неаутентифицированный CBC поддаётся подмене независимо от длины ключа." },
    { label: "CBC со временем утекает ключ, так что атакующий в итоге его расшифрует", misconception: "CBC не утекает ключ. Уязвимость в том, что режим ничего не аутентифицирует, поэтому шифротекст можно изменить (bit-flipping, padding oracle) без ключа." },
    { label: "Всё нормально — шифрование всегда гарантирует, что сообщение нельзя изменить", misconception: "Это и есть корневое заблуждение. Простое шифрование гарантирует конфиденциальность, а не целостность. Обнаружение подмены даёт только AEAD-режим или явный MAC." },
  ]}
/>

<DragOrder
  id="01-hashing-encryption-signing-drag-1"
  lessonSlug="01-hashing-encryption-signing"
  lang="ru"
  prompt="Сопоставь каждое требование с его примитивом — упорядочь требования от «держать в секрете» до «доказать, кто отправил»:"
  items={[
    { id: "a", label: "Сделать содержимое нечитаемым для всех без ключа → шифрование" },
    { id: "b", label: "Обнаружить любое изменение скачанного файла → хеш (отпечаток)" },
    { id: "c", label: "Хранить пароль, который только проверяешь, никогда не считывая → медленный посоленный парольный хеш" },
    { id: "d", label: "Доказать, что сообщение пришло от конкретной стороны, с проверкой кем угодно → асимметричная подпись" },
  ]}
  correctOrder={["a", "b", "c", "d"]}
/>

</Explanation>

<KeyTakeaway>Подбирай примитив под требование: шифрование для конфиденциальности (и AEAD-режим, чтобы заодно дать целостность), хеширование для целостности/отпечатка, медленный посоленный хеш для паролей и подписи для проверяемого третьими сторонами происхождения. «Оно зашифровано/хешировано/подписано» бессмысленно, пока не назовёшь, какое свойство ты на самом деле купил.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="01-hashing-encryption-signing-retrieval"
  lang="ru"
  questions={[
    { q: "Для хеширования, шифрования и подписи назови, что гарантирует каждый, и единственное свойство, которое их различает.", a: "Различающий вопрос — «можно ли обратить и каким ключом?». Хеширование — односторонняя функция без ключа: отображает вход в дайджест фиксированного размера, который проверяешь только пересчётом, поэтому покупает целостность и отпечаток (совпали ли байты?), но никогда конфиденциальность (дайджест публичен) и никогда происхождение (вычислить может кто угодно). Шифрование обратимо ключом: зашифровать, затем расшифровать тем же ключом, покупая конфиденциальность — держать содержимое в секрете от всех без ключа, — но само по себе (классические режимы) не гарантирует целостность или аутентичность, поэтому современные системы используют аутентифицированное шифрование (AES-GCM, ChaCha20-Poly1305), добавляющее тег целостности. Подпись асимметрична: подписываешь приватным ключом, а проверяет кто угодно соответствующим публичным, покупая аутентичность (пришло от держателя приватного ключа) плюс целостность (не изменено с тех пор), но намеренно не скрывает данные — подписанный JWT читается кем угодно. Итого: хеш = целостность, шифрование = конфиденциальность, подпись = происхождение + целостность, разделены обратимостью и моделью ключа." },
    { q: "Почему пароли нужно хешировать медленным посоленным хешем, а не шифровать или прогонять через простой SHA-256, и почему подписанный JWT небезопасен для секретов?", a: "Хранение пароля — односторонняя задача: при входе тебе нужно лишь проверить кандидата, никогда не восстанавливать оригинал. Шифровать их — неверный примитив, потому что шифрование обратимо: вся схема сводится к защите ключа, а ключи утекают через конфиги, бэкапы и историю исходников, возвращая каждый пароль в открытый текст в момент раскрытия ключа. Простой однораундовый SHA-256 — верное семейство, но неверный представитель: он быстр (миллиарды попыток/сек на GPU) и, непосоленный, уязвим к предвычисленным радужным таблицам. Пароли низкоэнтропийны, поэтому нужен намеренно медленный, посоленный, требовательный к памяти хеш (Argon2id, bcrypt, scrypt): соль убивает радужные таблицы, а стоимость делает офлайн-перебор утёкшего хеша экономически болезненным. Отдельно: стандартный подписанный JWT (JWS) только подписывает нагрузку — заголовок и тело это base64url, а не шифротекст, — поэтому любой, кто перехватит токен, прочтёт любой «секрет» внутри. Подпись доказывает, что токен не подделали и не изменили; конфиденциальности она не даёт. Чтобы скрыть содержимое нагрузки, нужен зашифрованный JWT (JWE), а не подписанный." },
  ]}
/>

<Recap lang="ru">
Хеширование, шифрование и подпись — три разных примитива, разделённых одним вопросом: можно ли обратить и каким ключом? Хеширование одностороннее и без ключа — покупает целостность и отпечаток, никогда конфиденциальность или происхождение. Шифрование обратимо ключом — покупает конфиденциальность, но классические режимы не дают целостности, так что тянись за аутентифицированным шифрованием (AES-GCM, ChaCha20-Poly1305). Подпись асимметрична — приватным ключом подписать, публичным проверить — покупает проверяемую третьими сторонами аутентичность плюс целостность, оставляя данные читаемыми. Пробои живут в негативном пространстве: шифровать пароли вместо хеширования (утечка ключа = открытый текст), прогонять низкоэнтропийные данные через простой SHA-256 (перебор на GPU), доверять целостность неаутентифицированному CBC (bit-flipping) или набивать секреты в подписанный, но незашифрованный JWT. Зрелый рефлекс — сначала назвать требование (секретно, цело, атрибутируемо или только-проверять-не-читать), и только потом выбирать примитив. Реальные системы вроде TLS 1.3 композируют все три; понимание, что делает каждый, и отличает настройку криптографии от слепого копирования.
</Recap>
