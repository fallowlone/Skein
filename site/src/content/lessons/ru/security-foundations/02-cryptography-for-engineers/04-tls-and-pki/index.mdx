---
concepts:
- tls-handshake
- pki
- certificate-chain
- certificate-authority
- trust-store
deepensInto: []
estMin: 15
lang: ru
lessonType: topic
level: middle
order: 4
prereqs: []
slug: 04-tls-and-pki
sources:
- https://cheatsheetseries.owasp.org/cheatsheets/Cryptographic_Storage_Cheat_Sheet.html
- https://datatracker.ietf.org/doc/html/rfc8446
status: ready
spiral: []
summary: 'TLS 1.3 строит зашифрованный канал за один round trip и доказывает, с кем вы говорите, цепочкой сертификатов, закреплённой в доверенном CA. Так браузер решает, что зелёный замок честен, — и так это ломается.'
title: 'TLS и PKI'
track: security-foundations
unit: 02-cryptography-for-engineers
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
Пейджер звонит в 03:00: каждый запрос к `api.payments.internal` падает с `x509: certificate has expired`. Сервис не менялся, код не менялся — число в файле перевалило с `notAfter: 2026-06-13` на секунду позже, и парк клиентов, доверявший этому серверу два года, разом отказался с ним говорить. Ни атакующего, ни бага, ни деплоя. Просто дата. Та же машинерия, что тихо не дала этому сбою стать незаметным man-in-the-middle, и есть машинерия, которая только что вас уронила: сертификат, цепочка и клиент, решающий, доверять ли. Поймёшь эту машинерию — перестанешь ею удивляться.
</Hook>

К концу урока ты будешь знать, как TLS 1.3 поднимает зашифрованный, аутентифицированный канал за один round trip, и как именно браузер решает, что сертификат валиден, — цепочка, CA, trust store и проверки между ними.

<Crux>TLS 1.3 строит зашифрованный канал за один round trip и подтверждает личность сервера цепочкой, закреплённой в доверенном CA.</Crux>

<Explanation>

## Что TLS на самом деле обязан сделать

У TLS две задачи, которые постоянно путают: **конфиденциальность** (никто на проводе не прочитает байты) и **аутентификация** (вы действительно говорите с `payments.example.com`, а не с самозванцем посередине). Одно шифрование без второго бесполезно — атакующий, который перехватывает трафик, с тем же успехом поднимет свой зашифрованный туннель к вам, читая всё насквозь. Весь смысл сертификатной машинерии — привязать ключи шифрования к проверенной личности, чтобы канал был и приватным, *и* доказуемо к нужной стороне.

TLS 1.3 (RFC 8446) — это намеренный сброс протокола. Он удалил опасные части TLS 1.2 — статический обмен ключами RSA, шифры в режиме CBC, RC4, сжатие, renegotiation — и оставил только конструкции с forward secrecy и AEAD. Практических следствий два: рукопожатие быстрее (**один round trip** вместо двух для нового соединения и опциональный **zero round trip** для возобновления), и множество способов всё неправильно настроить резко сократилось. Не осталось рычага «откатиться на экспортную крипту», который можно повернуть не туда.

## Рукопожатие 1.3 за один round trip

Вот последовательность для свежего соединения. Клиент открывает `ClientHello`, который уже содержит **key share** — эфемерное публичное значение Диффи-Хеллмана — для кривых, которые сервер, по его догадке, примет. Эта догадка и есть приём, схлопывающий два round trip в один: клиент не ждёт, пока ему скажут, какие параметры использовать, — он спекулирует.

<FlowDiagram
  label="Полное рукопожатие TLS 1.3 за один round trip: клиент шлёт ClientHello со своим эфемерным key share; сервер отвечает ServerHello (своим key share), затем — уже зашифрованно под выведенными ключами рукопожатия — шлёт Certificate, подпись CertificateVerify, доказывающую владение приватным ключом, и Finished; клиент проверяет цепочку и подпись и шлёт свой Finished, после чего идут данные приложения."
  perRow={5}
  hue="var(--d-crypto)"
  nodes={[
    { id: "hello", label: "ClientHello", sub: "+ key share" },
    { id: "shello", label: "ServerHello", sub: "+ key share" },
    { id: "cert", label: "Certificate", sub: "зашифрован" },
    { id: "verify", label: "CertificateVerify", sub: "подпись транскрипта" },
    { id: "data", label: "Данные приложения", sub: "1-RTT готов" },
  ]}
  edges={[
    { from: "hello", to: "shello", label: "1 RTT" },
    { from: "shello", to: "cert", label: "ключи выведены" },
    { from: "cert", to: "verify", label: "доказ. владения ключом" },
    { from: "verify", to: "data", label: "Finished клиента" },
  ]}
  caption="Клиент спекулирует key share в первом потоке, поэтому сервер сразу выводит общий секрет и шифрует остаток своего рукопожатия — включая сертификат. Всё от ServerHello и далее уже конфиденциально; сертификат больше не идёт открытым текстом, как было в TLS 1.2."
/>

Сервер отвечает `ServerHello` со *своим* key share. В этот миг обе стороны могут вычислить общий секрет через Диффи-Хеллмана и вывести симметричные ключи рукопожатия — поэтому всё, что сервер шлёт дальше, **уже зашифровано**, включая сертификат. Затем идёт часть, которая собственно кого-то аутентифицирует:

- **Certificate**: листовой сертификат сервера плюс промежуточные, нужные для построения цепочки.
- **CertificateVerify**: подпись, сделанная приватным ключом сертификата, над транскриптом всего рукопожатия до этого момента. Это шаг, доказывающий, что сервер *владеет приватным ключом*, соответствующим публичному ключу в сертификате, — а не просто скопировал чужой файл сертификата.
- **Finished**: MAC над транскриптом, запирающий рукопожатие от подмены.

Клиент проверяет цепочку и подпись `CertificateVerify`, шлёт свой `Finished`, и идут данные приложения. Один round trip из конца в конец.

<Inset kind="why" lang="ru">
Почему `CertificateVerify` — несущий шаг? Потому что сертификат публичен — кто угодно скачает сертификат `payments.example.com`. Чего самозванец не сможет — это произвести валидную подпись над *этим конкретным транскриптом рукопожатия* без соответствующего приватного ключа. Эта подпись, пересчитанная над данными, включающими свежие key share обеих сторон, и связывает «я предъявляю эту личность» с «я могу прямо сейчас доказать, что владею ею». Уберите её — и TLS не аутентифицирует ничего.
</Inset>

## Сертификаты, цепочки и CA

Сертификат — это подписанное утверждение: «этот публичный ключ принадлежит этому имени, и я, издатель, ручаюсь за это до этой даты истечения». Имя живёт в поле **Subject Alternative Name (SAN)** (старый Common Name современные клиенты игнорируют). Критично: **листовому сертификату сервера не доверяют напрямую**. Он подписан **промежуточным CA**, который подписан **корневым CA**. Эта последовательность — лист → промежуточный → корень — и есть **цепочка сертификатов**.

Причина этого слоя косвенности операционная, а не криптографическая. Приватные ключи корневых CA хранят офлайн в аппаратных модулях безопасности (HSM) и почти никогда не трогают, потому что утечка корневого ключа обесценивает все сертификаты, что он когда-либо закреплял, и его нельзя тихо заменить — он зашит в миллиарды устройств. Промежуточные делают повседневную подпись и могут быть отозваны и ротированы без апокалипсиса. Сервер обязан слать свой лист *и* промежуточные; сервер, отдающий только лист, выдаёт классическую ошибку «цепочка не доверена» на клиентах, у которых этот промежуточный не закэширован.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Звено цепочки</th>
        <th class="border border-border px-3 py-2 text-left">Кем подписан</th>
        <th class="border border-border px-3 py-2 text-left">Где живёт ключ</th>
        <th class="border border-border px-3 py-2 text-left">Если скомпрометирован</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">Лист (ваш сервер)</td>
        <td class="border border-border px-3 py-2">Промежуточный CA</td>
        <td class="border border-border px-3 py-2">На сервере / балансировщике</td>
        <td class="border border-border px-3 py-2">Отозвать + перевыпустить; срок ~90 дней ограничивает радиус</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Промежуточный CA</td>
        <td class="border border-border px-3 py-2">Корневой CA</td>
        <td class="border border-border px-3 py-2">В HSM удостоверяющего центра, онлайн для подписи</td>
        <td class="border border-border px-3 py-2">Отозвать промежуточный; ротировать; перевыпустить его листы</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Корневой CA</td>
        <td class="border border-border px-3 py-2">Сам себя (самоподписанный)</td>
        <td class="border border-border px-3 py-2">Офлайн HSM, трогают редко</td>
        <td class="border border-border px-3 py-2">Катастрофа: убирается из всех trust store, заменяется медленно</td>
      </tr>
    </tbody>
  </table>
</div>

## Как браузер решает, что сертификат валиден

Вот часть, которую инженеры чаще всего отмахивают. Когда приходит лист, клиент прогоняет цепочку проверок, и **все они должны пройти** — провал любой одной и вызывает страшное полноэкранное предупреждение:

1. **Построить цепочку к доверенному якорю.** Клиент идёт лист → промежуточный → корень и подтверждает, что корень есть в его **trust store** — предустановленном списке корневых CA операционной системы или браузера, которым решено доверять априори. Доверие не магия; оно упирается в этот выверенный список из нескольких сотен корней. Если цепочка не оканчивается на одном из них — сертификат не доверен, точка.
2. **Проверить каждую подпись вверх по цепочке.** Подпись каждого сертификата должна валидироваться против публичного ключа *следующего* сертификата. Разрыв где угодно означает, что цепочка подделана или неполна.
3. **Проверить даты валидности.** Каждый сертификат в цепочке должен быть в своём окне `notBefore`/`notAfter`. Это и есть пейджер в 03:00 про истёкший сертификат.
4. **Проверить имя.** Имя хоста, которое запросил клиент, должно совпасть с записью SAN на листе. Подключение к `payments.example.com` и получение сертификата для `example.com` без совпадающего SAN — провал; именно это останавливает атакующего, предъявляющего валидный сертификат для домена, которым он *владеет*.
5. **Проверить отзыв.** Не отозвал ли CA этот сертификат досрочно (компрометация ключа, ошибочная выдача)? Проверяется через CRL или, чаще, **OCSP / OCSP stapling**, когда сервер прикладывает свежее подписанное доказательство «всё ещё годен», чтобы клиенту не звонить CA.

<TradeoffMatrix
  id="04-tls-and-pki-tradeoff-1"
  lessonSlug="04-tls-and-pki"
  lang="ru"
  prompt="Ваш внутренний сервис `payments.internal` предъявляет сертификат. Клиенты отвергают его с 'certificate signed by unknown authority', хотя сам сертификат корректен и не истёк. Какой фикс верный?"
  options={[
    { name: "Сертификат цепляется к приватному внутреннему корню, которого нет в trust store клиентов — раздайте этот корневой CA клиентам (или используйте публично доверенный CA)", summary: "Чинить якорь.", correct: true, justification: "'Unknown authority' — это именно сбой якоря доверия: цепочка внутренне валидна, но оканчивается на корне, которому клиенту никогда не велели доверять. Правильный фикс — сделать этот якорь доверенным: установить приватный корневой CA в trust store каждого клиента (управление конфигурацией, базовый образ контейнера, политика ОС) или выпустить лист у публично доверенного CA. Это единственный фикс, сохраняющий аутентификацию, а не выбрасывающий её." },
    { name: "Сказать клиентам пропускать проверку сертификата (`InsecureSkipVerify` / `-k`) для внутреннего трафика", summary: "Выключить проверку.", justification: "Это самый опасный «фикс» в TLS. Отключение проверки не добавляет доверия — оно убирает аутентификацию целиком, так что любой хост на пути может сделать man-in-the-middle, по-прежнему отдавая зашифрованные байты. «Внутренний» — не граница безопасности; это ровно то место, где живут атакующие латерального движения. Вы сохранили конфиденциальность и выбросили половину TLS, которая здесь важна." },
    { name: "Перевыпустить лист с более долгим сроком, чтобы перестал жаловаться", summary: "Дольше срок.", justification: "Истечение — другой сбой (`certificate has expired`) с другой ошибкой. 'Unknown authority' — про якорь доверия, а не про даты, поэтому более долгий лист ничего не меняет: цепочка по-прежнему не доходит до доверенного корня, и клиент по-прежнему её отвергает." },
    { name: "Добавить сам листовой сертификат в trust store каждого клиента", summary: "Пинить лист.", justification: "Это узко работает, но это неверный примитив: вам пришлось бы перераздавать и передоверять каждый лист при каждой ротации в ~90 дней, и вы теряете весь смысл CA (свободно ротировать листы под одним доверенным корнем). Доверяйте корню или промежуточному, а не каждому эфемерному листу — если только вы намеренно не делаете certificate pinning, отдельный продвинутый контроль со своей операционной ценой." },
  ]}
/>

<Quiz
  id="04-tls-and-pki-quiz-1"
  lessonSlug="04-tls-and-pki"
  lang="ru"
  question="Что в рукопожатии TLS 1.3 сообщение сервера `CertificateVerify` на самом деле доказывает, чего не доказывает само по себе сообщение `Certificate`?"
  choices={[
    { label: "Что сервер владеет приватным ключом, соответствующим публичному ключу сертификата, — это подпись над транскриптом рукопожатия", correct: true },
    { label: "Что сертификат не истёк", misconception: "Истечение проверяется чтением дат notBefore/notAfter в самом сертификате; CertificateVerify — это подпись-доказательство владения приватным ключом, совсем другая проверка." },
    { label: "Что сертификат цепляется к доверенному корню", misconception: "Построение цепочки и закрепление в trust store — отдельная валидация, которую клиент делает над сообщением Certificate; CertificateVerify доказывает владение ключом, а не доверие цепочки." },
    { label: "Что соединение зашифровано", misconception: "Шифрование берётся из обмена Диффи-Хеллмана в сообщениях Hello; CertificateVerify про аутентификацию — доказательство личности, — а не про конфиденциальность." },
  ]}
/>

<Quiz
  id="04-tls-and-pki-quiz-2"
  lessonSlug="04-tls-and-pki"
  lang="ru"
  question="Клиент подключается к `payments.example.com`, и сервер предъявляет криптографически валидный, не истёкший, корректно собранный в цепочку сертификат — но для имени `cdn.example.com`. Что произойдёт?"
  choices={[
    { label: "Рукопожатие падает: запрошенное имя хоста не совпадает ни с одним SAN на листе, поэтому проверка имени проваливается, хотя все остальные прошли", correct: true },
    { label: "Успех, потому что сертификат валиден и цепляется к доверенному корню", misconception: "Должны пройти все проверки валидности, включая проверку имени. Идеально валидный сертификат для другого имени — ровно та атака, ради остановки которой существует проверка SAN." },
    { label: "Успех, потому что оба имени делят домен example.com", misconception: "Никакого автоматического доверия по суффиксу домена нет; лист обязан перечислять точное имя хоста (или совпадающий wildcard) в своём SAN. Общий родительский домен не даёт ничего." },
    { label: "Клиент откатывается на открытый HTTP", misconception: "Проваленная валидация сертификата обрывает соединение с предупреждением; она не делает тихий откат на нешифрованный транспорт." },
  ]}
/>

<DragOrder
  id="04-tls-and-pki-drag-1"
  lessonSlug="04-tls-and-pki"
  lang="ru"
  prompt="Расположи шаги свежего рукопожатия TLS 1.3 от первого к последнему:"
  items={[
    { id: "a", label: "ClientHello с эфемерным key share" },
    { id: "b", label: "ServerHello с key share сервера — обе стороны выводят ключи" },
    { id: "c", label: "Сервер шлёт Certificate + CertificateVerify (уже зашифрованно)" },
    { id: "d", label: "Клиент валидирует цепочку, подпись, имя и даты" },
    { id: "e", label: "Клиент шлёт Finished; идут данные приложения" },
  ]}
  correctOrder={["a", "b", "c", "d", "e"]}
/>

</Explanation>

<KeyTakeaway>TLS 1.3 делает рукопожатие за 1-RTT: спекулятивный key share позволяет обеим сторонам сразу вывести ключи, поэтому сертификат шлётся зашифрованно, а CertificateVerify доказывает владение приватным ключом. Сертификат валиден только когда цепочка закреплена в корне из trust store, все подписи сходятся, даты в окне, SAN совпадает с именем хоста и он не отозван — провали любую одну, и соединение обрывается.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="04-tls-and-pki-retrieval"
  lang="ru"
  questions={[
    { q: "Пройди по полному рукопожатию TLS 1.3 и объясни, как оно аутентифицирует сервер за один round trip.", a: "Клиент шлёт ClientHello, уже содержащий эфемерный key share Диффи-Хеллмана для кривой(-ых), которые сервер, по догадке клиента, поддерживает, — спекулируя вместо ожидания, и именно это схлопывает прежние два round trip в один. Сервер отвечает ServerHello и своим key share; в этот момент обе стороны вычисляют общий секрет и выводят симметричные ключи рукопожатия, поэтому всё, что сервер шлёт дальше, уже зашифровано. Затем сервер шлёт Certificate (лист плюс промежуточные), подпись CertificateVerify, сделанную приватным ключом сертификата над всем транскриптом рукопожатия, и MAC Finished. CertificateVerify — это шаг, который собственно аутентифицирует сервер: сертификат публичен, и кто угодно его скопирует, но только владелец соответствующего приватного ключа может подписать этот конкретный транскрипт, включающий свежие key share обеих сторон. Клиент валидирует цепочку и эту подпись, шлёт свой Finished, и идут данные приложения — один round trip из конца в конец. Конфиденциальность берётся из обмена DH в сообщениях Hello; аутентификация — из CertificateVerify, связывающего предъявленную личность с доказательством владения ключом." },
    { q: "Перечисли каждую проверку, которую клиент выполняет, решая, что серверный сертификат валиден, и объясни, от чего каждая защищает.", a: "Их пять, и все должны пройти, иначе соединение обрывается с предупреждением. (1) Построить цепочку к доверенному якорю: пройти лист → промежуточный → корень и подтвердить, что корень есть в trust store клиента — выверенном списке ОС/браузера из нескольких сотен априори доверенных корневых CA; здесь упирается доверие, поэтому цепочка, не дошедшая до одного из этих корней, — это 'unknown authority'. (2) Проверить каждую подпись вверх по цепочке: подпись каждого сертификата должна валидироваться против публичного ключа следующего, защищая от подделанной или неполной цепочки. (3) Проверить даты валидности: каждый сертификат должен быть в окне notBefore/notAfter — это сбой с истёкшим сертификатом. (4) Проверить имя: запрошенное имя хоста должно совпасть с записью SAN на листе (CN современные клиенты игнорируют), что останавливает атакующего, предъявляющего настоящий сертификат для домена, которым он реально владеет. (5) Проверить отзыв: убедиться, что CA не отозвал сертификат досрочно за компрометацию ключа или ошибочную выдачу, через CRL или, чаще, OCSP / OCSP stapling, когда сервер прикладывает свежее подписанное доказательство «всё ещё годен». Слоистая цепочка (лист → промежуточный → корень) существует ради операционной безопасности: корни живут офлайн в HSM, потому что утёкший корень незаменим, тогда как промежуточные делают повседневную подпись и могут быть отозваны и ротированы, не сжигая якорь в каждом устройстве." },
  ]}
/>

<Recap lang="ru">
У TLS две задачи: конфиденциальность (шифрование на проводе) и аутентификация (вы действительно говорите с названным сервером), и сертификатная машинерия существует ради второй. TLS 1.3 удалил небезопасные части 1.2 и делает рукопожатие за один round trip: клиент спекулирует эфемерный key share в ClientHello, сервер отвечает своим, обе стороны сразу выводят ключи, и остаток рукопожатия — включая сертификат — идёт зашифрованно. CertificateVerify — несущий шаг: подпись над транскриптом рукопожатия, доказывающая, что сервер владеет приватным ключом, а не просто копией публичного сертификата. Сертификаты образуют цепочку — лист → промежуточный → корень, — потому что корни держат офлайн и почти не трогают, тогда как промежуточные делают повседневную подпись и могут ротироваться. Браузер принимает сертификат только когда проходят все пять проверок: цепочка закреплена в корне из trust store, все подписи сходятся, даты в окне, SAN совпадает с именем хоста и сертификат не отозван. Теперь, когда клиент говорит 'unknown authority', ты знаешь, что это проблема якоря доверия, — и фикс в том, чтобы доверять якорю, а не отключать проверку.
</Recap>
