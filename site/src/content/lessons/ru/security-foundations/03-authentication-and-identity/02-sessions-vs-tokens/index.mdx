---
concepts:
- session-cookie
- bearer-token
- stateful-vs-stateless
- token-revocation
- session-fixation
deepensInto: []
estMin: 15
lang: ru
lessonType: topic
level: middle
order: 2
prereqs: []
slug: 02-sessions-vs-tokens
sources:
- https://cheatsheetseries.owasp.org/cheatsheets/Authentication_Cheat_Sheet.html
- https://www.w3.org/TR/webauthn-2/
spiral: []
status: ready
summary: Серверные сессии — это состояние и мгновенный отзыв; bearer-токены — без состояния и быстрые, но живут до истечения. Настоящий компромисс — задержка отзыва против стоимости lookup, и где каждый ломается.
title: Сессии против токенов
track: security-foundations
unit: 03-authentication-and-identity
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
Пользователь нажимает «выйти со всех устройств» после того, как потерял ноутбук. С серверными сессиями вы выполняете один `DELETE FROM sessions WHERE user_id = ?` — и каждая украденная кука мертва на следующем запросе. С JWT access-токенами, которые ваша команда выкатила в прошлом квартале, вы жмёте кнопку, получаете зелёный тост — а токен атакующего работает ещё четырнадцать минут, потому что на сервере ничто фактически не проверяет, нужен ли этот токен. Подпись валидна, срок не истёк — и ваш API его пропускает. Эта дыра между «я отозвал» и «оно перестало работать» и есть вся разница между двумя подходами; именно её тихо пропускают на демо и обнаруживают во время инцидента.
</Hook>

К концу урока вы будете точно знать, что вы отдаёте, выбирая stateless bearer-токены вместо серверных сессий, почему отзыв — это стержень, и где каждый подход ломается в продакшене.

<Crux>Сессии — состояние на сервере и мгновенный отзыв; токены — без состояния и быстрые, но валидны до истечения. Отзыв и есть компромисс.</Crux>

<Explanation>

## Два способа помнить вошедшего пользователя

После того как пользователь доказал, кто он, сервер должен помнить этот факт на каждом следующем запросе, потому что сам HTTP не имеет состояния — каждый запрос приходит без памяти о предыдущем. Есть два доминирующих способа нести этот факт «вы вошли», и они стоят на противоположных концах одной оси: где живёт истина.

**Серверная сессия** держит истину на сервере. При входе сервер создаёт запись — `session_id → {user_id, created_at, roles, …}` — в хранилище (Postgres, Redis, подписанное зашифрованное cookie-хранилище) и выдаёт клиенту непрозрачный случайный `session_id` в куке. Кука — это лишь ключ для поиска; сама по себе она ничего не значит. На каждом запросе сервер берёт этот id, находит запись, и *именно эта запись* — источник истины. Это **stateful** (с состоянием): сервер держит состояние сессии и обращается к нему на каждом вызове.

**Bearer-токен** (JWT — частая форма) держит истину *внутри токена*. При входе сервер собирает небольшой JSON-payload — `{sub: user_id, roles, exp}` — и подписывает его секретом. Клиент хранит токен и шлёт его на каждом запросе, обычно как `Authorization: Bearer <token>`. Сервер проверяет подпись тем же секретом и *верит заявленным внутри claims* — без обращения к БД, без общего хранилища. Это **stateless** (без состояния): любой сервер с ключом проверки валидирует токен в одиночку. «Bearer» — буквально: владение и есть авторизация. Кто держит токен, того и считают пользователем, ровно как наличные.

<FlowDiagram
  label="Сессия против токена при валидации: cookie-сессия — это непрозрачный id, который сервер ищет в хранилище сессий на каждом запросе, поэтому хранилище — источник истины, и удаление мгновенно убивает сессию; bearer-токен несёт подписанные claims, которые сервер проверяет ключом и доверяет напрямую, поэтому нет lookup и негде отозвать до того, как пройдёт claim exp."
  perRow={3}
  hue="var(--d-crypto)"
  nodes={[
    { id: "req-s", label: "Запрос + кука", sub: "session_id=opaque" },
    { id: "lookup", label: "Поиск в хранилище", sub: "SELECT … WHERE id" },
    { id: "truth-s", label: "Хранилище — истина", sub: "delete = отозвано" },
    { id: "req-t", label: "Запрос + токен", sub: "Bearer eyJ…" },
    { id: "verify", label: "Проверка подписи", sub: "без БД" },
    { id: "truth-t", label: "Claims — истина", sub: "валиден до exp" },
  ]}
  edges={[
    { from: "req-s", to: "lookup", label: "каждый req" },
    { from: "lookup", to: "truth-s", label: "stateful" },
    { from: "req-t", to: "verify", label: "каждый req" },
    { from: "verify", to: "truth-t", label: "stateless" },
  ]}
  caption="Раскол — в том, где живёт истина. Сессии: к хранилищу обращаются на каждом запросе, поэтому удаление строки отзывает мгновенно. Токены: подписанным claims верят напрямую, поэтому удалять нечего — токен валиден до exp."
/>

## Настоящий компромисс: задержка отзыва против стоимости lookup

Токены продают как «масштабируемые, без обращения к БД на запрос», а сессии — как «старые и медленные». Эта подача прячет реальное решение. Честный компромисс — это обмен одной стоимости на другую.

Сессии покупают **мгновенный отзыв**, расплачиваясь **lookup на каждом запросе**. Поскольку сервер читает запись сессии на каждом вызове, вы можете изменить или убить её в любой момент: удалить строку для принудительного выхода, переключить поле `roles`, чтобы понизить пользователя посреди сессии, или инвалидировать все сессии аккаунта после сброса пароля. Цена — этот lookup (Redis `GET` — субмиллисекунда, чтение Postgres — дольше), и хранилище теперь становится зависимостью на горячем пути каждого аутентифицированного запроса.

Токены покупают **stateless горячий путь**, расплачиваясь **отзывом, которого у вас нет**. Нет обращения к хранилищу на запрос, поэтому любой узел с ключом валидирует независимо — это реально помогает флоту stateless-сервисов и кросс-доменным API. Но то же свойство, что убирает lookup, убирает и место сказать «стоп». Подписанный токен валиден, пока не пройдёт его `exp`; у сервера нет записи, которую можно удалить. Поэтому в момент, когда нужен настоящий отзыв — выход со всех устройств, украденный токен, экстренное понижение прав, — вам приходится прикручивать состояние обратно (denylist, схема short-lived-access плюс refresh), и тут вы снова платите за lookup, а «stateless»-преимущество, ради которого вы это брали, частично исчезло.

Вот зрелая формулировка: **вы выбираете не быстро против медленно, а то, где живёт задержка отзыва.** Сессии ставят её в ноль и берут плату lookup'ом. Чистые токены выталкивают её на время жизни токена и берут плату инцидентом, когда это окно неверно.

<Inset kind="mistake" lang="ru">
Самая частая продакшен-ошибка — выкатить долгоживущие JWT (`exp` в часах или днях), будто это сессии, а потом во время инцидента обнаружить, что «выйти» и «забанить пользователя» на самом деле не работают. Смягчение, делающее токены отзываемыми, — короткоживущий **access-токен** (5–15 мин) в паре с **refresh-токеном**, хранимым на сервере. Access-токен остаётся stateless и быстро умирает; refresh-токен — это stateful, отзываемая часть: отозвав его, вы останавливаете *новые* access-токены, а максимум, сколько проживёт уже выданный access-токен, — его короткий срок. Вы не сбежали от состояния; вы сжали окно, которое оно должно покрыть, до нескольких минут.
</Inset>

## Где ломается каждый

У каждого подхода есть фирменный сбой, и знание их — это то, как вы выбираете.

**Сессии ломаются на фиксации сессии и на хранилище.** *Фиксация сессии:* если сервер принимает session id, подсунутый атакующим (например, из крафченой ссылки), а затем *повышает этот же id* до аутентифицированного при входе, то атакующий — который id уже знает — теперь вошёл как жертва. Фикс не обсуждается и держится на одной строке дисциплины: **регенерируйте session id при каждой смене привилегий**, особенно при входе, чтобы доаутентификационный id никогда не перетёк в аутентифицированную сессию. Сессии также делают хранилище жёсткой зависимостью: если Redis лежит, никто не аутентифицирован, поэтому ему нужна та же доступность, что и приложению.

**Токены ломаются на отзыве и на хранении.** *Отзыв:* разобрано выше — утёкший токен годен до `exp`, а «выход» — ложь, пока вы не добавили denylist или короткий срок. *Хранение:* bearer-токен — это наличные, поэтому то, где браузер его держит, решает радиус поражения. В `localStorage` он читаем любым JavaScript на странице, поэтому одна XSS его выносит; в `HttpOnly`-куке он невидим для JS (XSS не может его прочитать), но теперь ездит автоматически, и вы снова внесли экспозицию CSRF, которую надо парировать `SameSite` и анти-CSRF-токенами. Нет места хранения без компромиссов — есть лишь то, чей компромисс вы осознанно обработали.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Измерение</th>
        <th class="border border-border px-3 py-2 text-left">Серверная сессия</th>
        <th class="border border-border px-3 py-2 text-left">Bearer-токен (JWT)</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">Источник истины</td>
        <td class="border border-border px-3 py-2">Серверное хранилище (кука = непрозрачный ключ)</td>
        <td class="border border-border px-3 py-2">Сам подписанный токен (claims)</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Состояние на горячем пути</td>
        <td class="border border-border px-3 py-2">Lookup на каждом запросе (stateful)</td>
        <td class="border border-border px-3 py-2">Проверка подписи, без lookup (stateless)</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Отзыв</td>
        <td class="border border-border px-3 py-2">Мгновенный — удалить запись</td>
        <td class="border border-border px-3 py-2">Нет до <code>exp</code>, пока не добавите denylist</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Форма масштабирования</td>
        <td class="border border-border px-3 py-2">Нужно общее/доступное хранилище</td>
        <td class="border border-border px-3 py-2">Любой узел с ключом валидирует один</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Фирменный сбой</td>
        <td class="border border-border px-3 py-2">Фиксация сессии; падение хранилища = нет аутентификации</td>
        <td class="border border-border px-3 py-2">Утёкший токен годен до <code>exp</code>; хранение XSS/CSRF</td>
      </tr>
    </tbody>
  </table>
</div>

## Как зрелый инженер реально решает

По умолчанию — **серверные сессии для first-party веб-приложений**: один origin, браузер, который и так хорошо умеет куки, и жёсткое требование, чтобы «выйти» и «забанить» работали *сейчас*. Lookup на запрос против Redis дёшев и покупает отзыв, который понадобится в день, когда что-то утечёт. Берите **токены, когда форма требует statelessness** — вызовы сервис-сервис, мобильные и сторонние API-клиенты, или флот, куда не вписывается общее хранилище сессий, — и когда берёте, делайте их **короткоживущими access-токенами с серверным refresh**, чтобы stateless-часть была ограничена минутами, а отзываемая часть жила в refresh-токене. Неверный ответ — середина: долгоживущий JWT в роли де-факто сессии, который одновременно даёт вам проблему отзыва токена и радиус поражения сессии.

<TradeoffMatrix
  id="03-sessions-vs-tokens-tradeoff-1"
  lessonSlug="02-sessions-vs-tokens"
  lang="ru"
  prompt="First-party веб-приложение должно поддерживать «выход со всех устройств» и мгновенный бан при злоупотреблении. Аутентификация работает против Redis, который вы уже эксплуатируете. Выберите дизайн."
  options={[
    { name: "Серверные сессии: непрозрачная кука, запись в Redis, регенерация id при входе, удаление записей для отзыва", summary: "Stateful, мгновенно отзываемо.", correct: true, justification: "Требование — мгновенный отзыв, и сессии дают его даром: сервер читает запись на каждом запросе, поэтому её удаление (или всех записей пользователя) убивает доступ на следующем вызове. Lookup в Redis — субмиллисекунда, и вы уже его эксплуатируете, поэтому цена «состояния на запрос» пренебрежима. Регенерация id при входе закрывает фиксацию сессии. Это точно совпадает с требованием без прикруток." },
    { name: "Долгоживущий JWT (exp = 24ч) в localStorage, без серверного состояния", summary: "Stateless, не отзываемо.", justification: "Это дизайн-ловушка. 24-часовой JWT нельзя отозвать — «выход со всех устройств» и «бан сейчас» — ложь на сутки, — а localStorage превращает любую XSS в полную кражу токена. Вы получаете проблему отзыва токена и радиус поражения сессии разом, не выигрывая ничего, ведь общее хранилище у вас уже есть." },
    { name: "Короткоживущий JWT (15 мин) + серверный refresh-токен", summary: "Ограничено, но здесь не нужно.", justification: "Это здравый дизайн, и он бы сработал, но решает проблему, которой у вас нет: statelessness нужна была через флот или для сторонних, а это same-origin first-party приложение с уже имеющимся хранилищем. Он добавляет машинерию ротации refresh и всё равно оставляет 15-минутное окно отзыва, тогда как сессии дают ноль. Верный инструмент, не та задача." },
    { name: "JWT без exp, отзыв через denylist, проверяемый на каждом запросе", summary: "Состояние возвращено, хуже.", justification: "Проверка denylist на каждом запросе заново вносит lookup на запрос, ради избегания которого вы и выбрали токены, — так что вы заплатили цену сессии и сохранили минусы токена (риск хранения, больший payload, управление ключами). Это строго хуже, чем просто сессии, которые делают тот же lookup как свой родной, простой дизайн." },
  ]}
/>

<Quiz
  id="03-sessions-vs-tokens-quiz-1"
  lessonSlug="02-sessions-vs-tokens"
  lang="ru"
  question="Коллега говорит: «JWT безопаснее сессий, потому что на сервере ничего не хранится». В чём точная поправка?"
  choices={[
    { label: "Statelessness — не свойство безопасности: оно убирает lookup на запрос, но убирает и место для отзыва, поэтому утёкший токен остаётся валиден до exp", correct: true },
    { label: "Он прав: раз нет серверного состояния, атакующему нечего красть", misconception: "То, что крадёт атакующий, — это сам bearer-токен, с клиента. «Нет серверного состояния» это не уменьшает; оно убирает вашу способность инвалидировать украденный токен, что хуже, а не лучше." },
    { label: "Сессии небезопасны, потому что session id шлётся в куке", misconception: "Токен тоже шлётся на каждом запросе (в заголовке или куке) и так же является bearer-учёткой. Транспорт в куке не слабость; настоящая разница — отсутствие отзыва у чистых токенов." },
    { label: "JWT зашифрованы, поэтому claims нельзя прочитать, в отличие от cookie-сессий", misconception: "Стандартный подписанный JWT подписан, а не зашифрован — его payload это base64url, читаемый кем угодно. Подпись доказывает целостность, а не конфиденциальность." },
  ]}
/>

<Quiz
  id="03-sessions-vs-tokens-quiz-2"
  lessonSlug="02-sessions-vs-tokens"
  lang="ru"
  question="Почему приложение на сессиях обязано регенерировать session id при входе?"
  choices={[
    { label: "Чтобы предотвратить фиксацию сессии — иначе подсунутый атакующим до входа известный id был бы повышен до аутентифицированной сессии", correct: true },
    { label: "Чтобы ротировать ключ шифрования, чтобы старые куки нельзя было расшифровать", misconception: "Session id — это непрозрачные случайные ключи поиска, а не зашифрованные payload'ы; на сессию нет ключа для ротации. Смысл — в инвалидации любого доаутентификационного id, который атакующий уже может знать." },
    { label: "Чтобы улучшить производительность, укорачивая id на каждом запросе", misconception: "Регенерация происходит при смене привилегий, а не на запрос, и она про безопасность, а не про длину id или скорость." },
    { label: "Потому что спецификация требует новое имя куки после входа", misconception: "Такого требования нет. Причина именно в том, чтобы остановить перетекание доаутентификационного id в аутентифицированную сессию." },
  ]}
/>

<DragOrder
  id="03-sessions-vs-tokens-drag-1"
  lessonSlug="02-sessions-vs-tokens"
  lang="ru"
  prompt="Упорядочьте шаги, делающие stateless access-токен реально отзываемым в продакшене, от выдачи до принудительного выхода:"
  items={[
    { id: "a", label: "Выдать короткоживущий access-токен (5–15 мин) плюс refresh-токен, хранимый на сервере" },
    { id: "b", label: "Клиент зовёт API с access-токеном — stateless, без lookup, пока тот не истечёт" },
    { id: "c", label: "При выходе/бане отозвать refresh-токен в серверном хранилище" },
    { id: "d", label: "Истёкший access-токен нельзя обновить, поэтому доступ кончается в пределах короткого окна exp" },
  ]}
  correctOrder={["a", "b", "c", "d"]}
/>

</Explanation>

<KeyTakeaway>Сессии держат истину на сервере: lookup на запрос покупает мгновенный отзыв. Токены держат истину в подписанном payload: без lookup, но и без отзыва до <code>exp</code>. Решение — где живёт задержка отзыва: ноль с зависимостью от хранилища или время жизни токена, которое надо держать коротким.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="03-sessions-vs-tokens-retrieval"
  lang="ru"
  questions={[
    { q: "Объясните настоящий компромисс между серверными сессиями и bearer-токенами — не «быстро против медленно», а что на самом деле обменивается.", a: "Ось — это где живёт истина о том, что «пользователь вошёл». Серверная сессия держит её в серверном хранилище и выдаёт клиенту непрозрачный id в куке; сервер находит запись на каждом запросе, поэтому хранилище — источник истины (stateful). Bearer-токен держит истину внутри подписанного payload, который несёт клиент; сервер проверяет подпись и верит claims без lookup (stateless). Честный компромисс — задержка отзыва против стоимости lookup. Сессии платят чтением хранилища на запрос и получают взамен мгновенный отзыв — удалите запись, и следующий запрос отклонён, поэтому выход со всех устройств, понижение прав посреди сессии и бан работают сразу. Токены пропускают lookup, поэтому любой узел с ключом валидирует один, но удалять нечего: подписанный токен валиден, пока не пройдёт exp. Так что вы выбираете не быстро против медленно, а где сидит задержка отзыва. Сессии ставят её в ноль и берут плату зависимостью от хранилища на горячем пути. Чистые токены выталкивают её на время жизни токена и берут плату инцидентом в день, когда это окно слишком длинное." },
    { q: "Где каждый подход ломается в продакшене и как смягчить каждый сбой?", a: "Сессии ломаются на фиксации сессии и на доступности хранилища. Фиксация: если сервер повышает подсунутый атакующим доаутентификационный session id до аутентифицированного при входе, атакующий (знающий id) уже внутри — фиксится регенерацией session id при каждой смене привилегий, особенно при входе, чтобы доаутентификационный id никогда не перетёк в аутентифицированную сессию. Доступность: хранилище на горячем пути, поэтому ему нужен тот же аптайм, что и приложению, иначе никто не аутентифицируется. Токены ломаются на отзыве и на хранении. Отзыв: утёкший токен валиден до exp, и «выход» ничего не делает, пока вы не добавите denylist (который заново вносит lookup на запрос) или, лучше, не используете короткоживущие access-токены (5–15 мин) плюс серверный refresh-токен — отзыв refresh останавливает новые access-токены, а выданный умирает за минуты. Хранение: bearer-токен — наличные, поэтому localStorage открывает его любой XSS, а HttpOnly-кука прячет от JS, но заново вносит CSRF, который вы парируете SameSite и анти-CSRF-токенами. Нет хранения с нулевым компромиссом — есть лишь то, чей компромисс вы обработали." },
  ]}
/>

<Recap lang="ru">
HTTP забывает вас между запросами, поэтому серверу нужен способ нести «этот пользователь вошёл» — и их два, разделённых тем, где живёт истина. Серверные сессии держат её на сервере: кука — непрозрачный ключ, запись в хранилище — истина, и поскольку сервер читает её на каждом запросе, вы получаете мгновенный отзыв — удалите запись, и следующий вызов отклонён. Bearer-токены (JWT) держат истину в подписанном payload, который несёт клиент; сервер проверяет и верит ему без lookup, что stateless и отлично для флотов и сторонних, но означает, что подписанный токен валиден до exp, и удалять нечего. Настоящий компромисс — это задержка отзыва против стоимости lookup, а не скорость: сессии ставят отзыв в ноль и платят зависимостью от хранилища; чистые токены выталкивают его на время жизни токена. Каждый ломается по-своему — сессии на фиксации (регенерируйте id при входе) и падении хранилища; токены на отзыве (короткий access плюс серверный refresh) и на том, где браузер их хранит (localStorage против HttpOnly, XSS против CSRF). Зрелый дефолт — сессии для first-party веб-приложений и короткоживущие access-плюс-refresh токены, когда форма действительно требует statelessness, — и никогда долгоживущий JWT, притворяющийся сессией.
</Recap>
