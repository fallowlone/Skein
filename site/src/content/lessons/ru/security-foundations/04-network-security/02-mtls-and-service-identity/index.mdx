---
concepts:
- mutual-tls
- service-identity
- spiffe
- certificate-rotation
- zero-trust-network
deepensInto: []
estMin: 15
lang: ru
lessonType: topic
level: middle
order: 2
prereqs: []
slug: 02-mtls-and-service-identity
sources:
- https://csrc.nist.gov/pubs/sp/800/207/final
- https://datatracker.ietf.org/doc/html/rfc8446
spiral: []
status: ready
summary: 'Обычный TLS доказывает клиенту подлинность сервера. mTLS заставляет обе
  стороны предъявить сертификаты, превращая криптографическую личность в единицу
  авторизации между сервисами — а короткоживущие сертификаты и автоматическую ротацию
  делает самой трудной частью.'
title: 'mTLS и идентичность сервиса'
track: security-foundations
unit: 04-network-security
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
Платёжный сервис доверяет сервису заказов, потому что запросы приходят по приватной сети кластера с общим API-ключом в заголовке. Один скомпрометированный под — и атакующий уже *внутри* этой сети, держит ключ из утёкшей переменной окружения и переигрывает его из рабочей нагрузки, которой нечего делать рядом с платежами. Каждая проверка, которую выполнил платёжный сервис, — исходный IP в нужном CIDR, валидный bearer-токен — прошла. Сеть сообщила ему «это пришло из доверенного места», и в этом была вся история авторизации. Лечится это не более удачным правилом фаервола. Лечится тем, что каждый сервис доказывает, *кто он есть*, ключом, который держит только он, на каждом соединении, в обе стороны.
</Hook>

К концу этого урока ты будешь знать, как mTLS превращает криптографический сертификат в идентичность сервиса, почему короткоживущие сертификаты и автоматическая ротация — это и есть настоящая инженерная работа, и где mTLS заканчивается, а в дело должна вступить политика.

<Crux>mTLS заставляет обе стороны доказать личность сертификатами, поэтому одно лишь положение в сети уже не выдаёт себя за сервис.</Crux>

<Explanation>

## От «сервер настоящий» к «настоящие обе стороны»

Обычный TLS — это односторонняя аутентификация. Во время рукопожатия сервер присылает свой сертификат, клиент сверяет его с доверенным CA, и дальше клиент знает, что говорит с настоящим `api.example.com`. А вот сервер не узнаёт *ничего* проверяемого о клиенте — кто дотянулся до порта, тот и получил TLS-сессию. Эта асимметрия нормальна для браузера, заходящего на публичный сайт, потому что человек потом залогинится. Но для трафика между сервисами это дыра: там нет человека, а «логином» исторически был bearer-токен или сетевой ACL — и то и другое атакующий, уже находящийся внутри периметра, может украсть или удовлетворить.

mTLS (взаимный TLS) закрывает эту асимметрию. Рукопожатие TLS 1.3 (RFC 8446) поддерживает `CertificateRequest`: после того как аутентифицировался сервер, он просит *клиента* тоже предъявить сертификат, и клиент доказывает владение соответствующим приватным ключом подписью `CertificateVerify` поверх транскрипта рукопожатия. Теперь у обоих концов есть криптографически доказанная личность ещё до того, как пройдёт хоть один байт прикладных данных. Что критично: приватный ключ никогда не пересекает провод — сторона доказывает, что *владеет* ключом, подписывая, поэтому наблюдение или переигрывание рукопожатия не даёт атакующему ничего.

Это и есть несущий сдвиг для сети с нулевым доверием (NIST SP 800-207): доверие больше не свойство того, *откуда* пришёл пакет, а того, *какая личность* предъявила валидный учётный признак. Приватная сеть перестаёт быть границей доверия. Скомпрометированный под всё равно не сможет выдать себя за сервис заказов, потому что не держит его приватный ключ.

<FlowDiagram
  label="Рукопожатие mTLS для аутентификации между сервисами: клиент открывает TLS к серверу, сервер предъявляет свой сертификат и CertificateRequest, клиент предъявляет собственный сертификат и подпись CertificateVerify, доказывающую владение приватным ключом, обе стороны сверяют с доверенным CA, и только потом течёт авторизованный прикладной трафик."
  perRow={5}
  hue="var(--d-crypto)"
  nodes={[
    { id: "hello", label: "Hello клиента", sub: "открыть TLS 1.3" },
    { id: "srvcert", label: "Серт сервера", sub: "+ CertRequest" },
    { id: "clicert", label: "Серт клиента", sub: "+ подпись CertVerify" },
    { id: "validate", label: "Оба сверяют", sub: "доверенный CA" },
    { id: "authz", label: "Личность → authz", sub: "SAN = SPIFFE ID" },
  ]}
  edges={[
    { from: "hello", to: "srvcert" },
    { from: "srvcert", to: "clicert", label: "просит клиента" },
    { from: "clicert", to: "validate", label: "докажи ключ" },
    { from: "validate", to: "authz", label: "затем политика" },
  ]}
  caption="Односторонний TLS аутентифицирует только сервер. mTLS добавляет обмен CertificateRequest / CertificateVerify, чтобы и клиент доказал личность. Проверенная личность клиента (в поле SAN сертификата) становится субъектом, против которого принимается каждое решение об авторизации."
/>

## Что на самом деле идентифицирует сертификат

Сертификат аутентифицирует личность, но для сервисов личность — это не имя хоста, которое набрал бы браузер, а *рабочая нагрузка*. Господствующее соглашение здесь — SPIFFE: личность рабочей нагрузки кодируется как URI в поле Subject Alternative Name (SAN) сертификата, SPIFFE ID вида `spiffe://prod.example/ns/payments/sa/orders`. Эта строка говорит: в домене доверия `prod.example` это сервисная учётная запись `orders` в пространстве имён `payments`. Когда сервис заказов соединяется с платежами, платежи читают проверенный SAN, видят знакомый SPIFFE ID и *затем* решают, разрешено ли этой личности делать то, что она просит.

Вот эту последнюю оговорку инженеры и пропускают. mTLS даёт тебе **аутентификацию** — сильный, неподделываемый ответ на вопрос «кто это?». Он не даёт **авторизации** — «разрешено ли этой личности делать это?». Валидный сертификат из твоего домена доверия доказывает, что вызывающий — легитимная рабочая нагрузка; он ничего не говорит о том, должна ли эта нагрузка иметь право оформлять возвраты. Полный дизайн таков: mTLS устанавливает личность на уровне соединения, а слой политики (SPIFFE-осведомлённая авторизация, `AuthorizationPolicy` в Istio, проверка в OPA) принимает по этой личности решение allow/deny на каждый запрос. Смешать одно с другим — «сертификат прошёл проверку, значит, пропускаем» — это и есть способ построить плоский, полностью доверенный mesh, где любая скомпрометированная нагрузка может вызвать что угодно.

<Inset kind="why" lang="ru">
Почему не оставить bearer-токены между сервисами? Bearer-токен по определению — учётный признак, который может переиграть любой, кто им владеет: он не несёт доказательства того, *кто* его предъявляет. Утёк он в лог, переменную окружения, неверно смаршрутизированный запрос — это отмычка до самого истечения (часто часы). Клиентский сертификат mTLS привязан к приватному ключу, который никогда не покидает рабочую нагрузку; владение доказывается подписью живого рукопожатия, так что красть с провода нечего — переигрывать нечего. Токены отвечают на «валидна ли эта строка?»; mTLS отвечает на «участвовал ли владелец именно этого ключа именно в этом рукопожатии?» — вещь куда более узкую и куда труднее подделываемую.
</Inset>

## Самое трудное: ротация в масштабе

Криптография mTLS решена; жизнь или смерть — в эксплуатации. Если каждому сервису нужен сертификат и приватный ключ, ты теперь держишь центр сертификации и конвейер раздачи для тысяч короткоживущих учётных признаков. Зрелый инстинкт — делать сертификаты **короткоживущими**: часы, а не годы. Сертификат, живущий час, сжимает окно, в котором украденный ключ полезен, и почти снимает проблему отзыва: ты не воюешь с печально ненадёжной машинерией отзыва CRL/OCSP, а просто *даёшь сертификату истечь* и не перевыпускаешь его нагрузке, которую отрезал. Истечение становится твоим отзывом.

Но короткоживущие сертификаты работают, только если **ротация полностью автоматизирована** и перекрывается. Вот сбой, который поднимает людей среди ночи: задача обновления умерла, сертификаты истекли в 03:00, и внезапно каждый сервис отвергает каждую сторону разом — самонанесённый общекластерный отказ без всякого атакующего. Защиты таковы: ротировать задолго до истечения (обновлять на ~50–66% срока жизни, чтобы у провалившегося обновления оставались часы запаса), перезагружать новый сертификат без обрыва живых соединений и делать ротацию якоря доверия (CA) отдельным, более медленным процессом с перекрытием, где во время переключения одновременно доверяют и старому, и новому корню. На практике этот цикл за тебя крутит service mesh (Istio, Linkerd) или SPIRE из SPIFFE — выпуская, раздавая и ротируя сертификаты нагрузок через sidecar или агент, так что код приложения никогда не касается ключа. Сделка, которую ты покупаешь: настоящая личность на каждую нагрузку в обмен на эксплуатацию высокодоступного CA, чей отказ теперь — продакшен-отказ.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Аспект</th>
        <th class="border border-border px-3 py-2 text-left">Сетевой ACL / общий токен</th>
        <th class="border border-border px-3 py-2 text-left">mTLS + идентичность нагрузки</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">Единица доверия</td>
        <td class="border border-border px-3 py-2">Исходный IP / владение строкой</td>
        <td class="border border-border px-3 py-2">Криптографическая личность (SPIFFE ID в SAN)</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Украденный признак</td>
        <td class="border border-border px-3 py-2">Переигрываем весь срок жизни токена</td>
        <td class="border border-border px-3 py-2">Ключ не покидает нагрузку; на проводе нечего переиграть</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Скомпрометированный под</td>
        <td class="border border-border px-3 py-2">Внутри сети → доверенный по умолчанию</td>
        <td class="border border-border px-3 py-2">Всё равно не предъявит чужую идентичность сервиса</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Отзыв</td>
        <td class="border border-border px-3 py-2">Ротировать общий секрет повсюду</td>
        <td class="border border-border px-3 py-2">Дать короткоживущему сертификату истечь; не перевыпускать</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Главная цена эксплуатации</td>
        <td class="border border-border px-3 py-2">Разрастание фаервола; раздача секретов</td>
        <td class="border border-border px-3 py-2">Держать HA CA + автоматическую ротацию с перекрытием</td>
      </tr>
    </tbody>
  </table>
</div>

## Как зрелый инженер рассуждает о внедрении

mTLS не бесплатен, и честная формулировка — это компромисс, а не серебряная пуля. Ты получаешь доверие на основе личности, переживающее пробой сети и превращающее латеральное движение из «бесплатно внутри периметра» в «всё ещё нужен приватный ключ каждого сервиса». Платишь же ты тем, что CA должен быть высокодоступным (его отказ — твой отказ), циклом автоматической ротации, который нельзя дать молча сломаться, и реальной ценой CPU/задержки на каждом рукопожатии — поэтому ты терминируешь mTLS на sidecar и держишь сессии долгоживущими, чтобы её амортизировать. И ты должен помнить границу: mTLS аутентифицирует *рабочую нагрузку*, а не конечного пользователя. Личность человека (его сессия, его скоупы) по-прежнему едет внутри запроса на прикладном уровне; mTLS защищает трубу и доказывает идентичность сервиса-вызывающего, но сервис, которому доверено тебя вызывать, всё ещё может нести поддельное или раздутое по правам пользовательское утверждение, если ты его не проверишь.

<TradeoffMatrix
  id="02-mtls-service-identity-tradeoff-1"
  lessonSlug="02-mtls-and-service-identity"
  lang="ru"
  prompt="Твои сервисы уже работают по mTLS через mesh: каждое соединение предъявляет валидный сертификат нагрузки из твоего домена доверия. Новое требование: только сервис `orders` может вызывать `POST /refunds` у `payments`. Как правильно это обеспечить?"
  options={[
    { name: "Добавить правило политики, разрешающее POST /refunds только когда проверенный SPIFFE ID стороны — orders, и запрет по умолчанию", summary: "Авторизуй по личности.", correct: true, justification: "mTLS уже аутентифицировал вызывающего — у тебя есть достоверный SPIFFE ID. Авторизация — отдельный слой: явная политика, сопоставляющая проверенную личность с разрешённым действием, запрет по умолчанию. Это ровно разделение «mTLS аутентифицирует / политика авторизует», и это единственный вариант, реально ограничивающий, кто может делать возвраты." },
    { name: "Ничего дополнительно не делать — любой вызывающий с валидным сертификатом из нашего домена уже доверенный, проверка избыточна", summary: "Серт == разрешено.", justification: "Это классическое смешение аутентификации с авторизацией. Валидный сертификат доказывает, что вызывающий — легитимная нагрузка в твоём домене; он ничего не говорит о том, может ли ИМЕННО эта нагрузка оформлять возвраты. Без политики скомпрометированный или чрезмерно ретивый сервис вызывает /refunds свободно — плоский, полностью доверенный mesh." },
    { name: "Проверять исходный IP вызывающего и разрешать только диапазон IP подов orders", summary: "Назад к IP-allowlist.", justification: "Это возвращает ровно то доверие к сетевому положению, ради замены которого mTLS и внедряли. IP подов эфемерны и переназначаются, правило гниёт мгновенно, и любая нагрузка, попавшая на разрешённый IP, становится доверенной — это и есть дыра латерального движения, а не её фикс." },
    { name: "Выпустить orders второй, особый сертификат «refunds» и проверять его наличие", summary: "Перегрузить сертификат.", justification: "Сертификаты кодируют личность, а не разрешения на действия; чеканка особых сертификатов под каждую возможность превращает CA в хрупкую базу авторизации и взрывает поверхность ротации. Возможности живут в слое политики, ключом по одной стабильной личности, а не контрабандой в лишних сертификатах." },
  ]}
/>

<Quiz
  id="02-mtls-service-identity-quiz-1"
  lessonSlug="02-mtls-and-service-identity"
  lang="ru"
  question="Что в рукопожатии mTLS мешает атакующему, полностью записавшему рукопожатие легитимного клиента, переиграть его и выдать себя за этого клиента?"
  choices={[
    { label: "Клиент доказывает владение приватным ключом, подписывая живой транскрипт рукопожатия (CertificateVerify); сам ключ не пересекает провод, поэтому в записи нет ничего переиспользуемого", correct: true },
    { label: "Сертификат зашифрован в передаче, поэтому атакующий не может его прочитать", misconception: "Сертификат публичен по дизайну — его видит каждый. Секретен приватный ключ, а безопасность идёт от доказательства владения им подписью поверх ИМЕННО этого рукопожатия, а не от сокрытия сертификата." },
    { label: "CA обнаруживает дублирующее рукопожатие и отзывает сертификат", misconception: "CA не находится в пути данных каждого рукопожатия и не видит переигрываний. Стойкость к переигрыванию даёт подпись CertificateVerify на каждое рукопожатие, а не обнаружение на стороне CA." },
    { label: "Проверки исходного IP отвергают переигрывание, так как оно идёт с другой машины", misconception: "mTLS намеренно убирает доверие к сетевому положению. Рукопожатие безопасно независимо от исходного IP именно потому, что личность доказана криптографически, а не тем, откуда исходит пакет." },
  ]}
/>

<Quiz
  id="02-mtls-service-identity-quiz-2"
  lessonSlug="02-mtls-and-service-identity"
  lang="ru"
  question="Команда делает сертификаты нагрузок валидными на 2 года, «чтобы не возиться с ротацией». Почему для mTLS-mesh это неверное решение?"
  choices={[
    { label: "Долгий срок делает украденный ключ опасным годами и вынуждает полагаться на ненадёжный отзыв (CRL/OCSP); короткоживущие сертификаты позволяют истечению БЫТЬ отзывом", correct: true },
    { label: "TLS 1.3 отвергает любой сертификат, валидный дольше 90 дней", misconception: "В TLS 1.3 нет такого протокольного ограничения. Короткий срок жизни — операционно-безопасностный выбор, а не требование рукопожатия." },
    { label: "Долгоживущие сертификаты используют более слабую криптографию, чем короткоживущие", misconception: "Срок жизни и стойкость ключа независимы. Двухлетний сертификат может использовать те же алгоритмы; проблема — в долгом окне полезности украденного ключа, а не в слабой криптографии." },
    { label: "Ротация чисто косметическая, и единственный реальный риск — падение CA", misconception: "Ротация — это сам по себе механизм безопасности: она сжимает окно украденного ключа и заменяет хрупкий отзыв. Доступность CA тоже реальная забота, но это не делает ротацию косметической." },
  ]}
/>

<DragOrder
  id="02-mtls-service-identity-drag-1"
  lessonSlug="02-mtls-and-service-identity"
  lang="ru"
  prompt="Упорядочи шаги mTLS-соединения между сервисами, от открытия рукопожатия до принятия решения о доступе:"
  items={[
    { id: "a", label: "Клиент открывает TLS 1.3-соединение к серверу" },
    { id: "b", label: "Сервер предъявляет свой сертификат и CertificateRequest" },
    { id: "c", label: "Клиент предъявляет свой сертификат + подпись CertificateVerify, доказывающую владение ключом" },
    { id: "d", label: "Обе стороны сверяют предъявленные сертификаты с доверенным CA" },
    { id: "e", label: "Слой политики авторизует запрос против проверенной SPIFFE-личности" },
  ]}
  correctOrder={["a", "b", "c", "d", "e"]}
/>

</Explanation>

<KeyTakeaway>mTLS заставляет обе стороны предъявить сертификаты, поэтому единицей доверия становится идентичность сервиса, а не его положение в сети. Криптография проста; короткоживущие сертификаты и автоматическая ротация с перекрытием — вот настоящая работа. И mTLS лишь аутентифицирует рабочую нагрузку — авторизация остаётся отдельным слоем политики поверх проверенной личности.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="02-mtls-service-identity-retrieval"
  lang="ru"
  questions={[
    { q: "Объясни, как mTLS меняет единицу доверия для трафика между сервисами и почему скомпрометированный под внутри сети всё равно не может выдать себя за другой сервис.", a: "Обычный TLS аутентифицирует только сервер: клиент сверяет сертификат сервера с CA, но сервер не узнаёт ничего проверяемого о клиенте, поэтому скатывается к доверию сетевому положению (исходный IP в нужном CIDR) или bearer-токену — и то и другое удовлетворяет или крадёт атакующий, уже находящийся внутри периметра. mTLS добавляет CertificateRequest в рукопожатие TLS 1.3, так что клиент тоже должен предъявить сертификат и доказать владение соответствующим приватным ключом подписью CertificateVerify поверх живого транскрипта. Теперь у обоих концов есть криптографически доказанная личность ещё до любых прикладных данных, и единица доверия смещается с «откуда пришёл пакет» на «какая личность предъявила валидный признак» — ядро сети с нулевым доверием (NIST SP 800-207). Скомпрометированный под по-прежнему в доверенной сети, но он не держит приватный ключ сервиса заказов, а ключ не пересекает провод, поэтому он не может предъявить идентичность orders. Сеть перестаёт быть границей доверия; ею становится ключ." },
    { q: "Почему короткоживущие сертификаты и автоматическая ротация — самая трудная часть mTLS, и какой сбой кладёт весь кластер?", a: "Криптография mTLS решена; жизнь или смерть — в эксплуатации, потому что каждой нагрузке теперь нужен сертификат и приватный ключ, выданные CA, который ты должен держать. Зрелые инженеры делают сертификаты короткоживущими (часы, не годы), чтобы украденный ключ был полезен лишь недолго, а отзыв стал почти тривиальным — вместо войны с ненадёжным CRL/OCSP ты просто даёшь сертификату истечь и не перевыпускаешь его отрезанной нагрузке; истечение становится отзывом. Но короткий срок работает, только если ротация полностью автоматизирована и перекрывается. Сбой, поднимающий людей: задача обновления умерла, сертификаты истекли разом в 03:00, и каждый сервис отвергает каждую сторону одновременно — самонанесённый общекластерный отказ без атакующего. Защиты: обновлять задолго до истечения (~50–66% срока жизни, чтобы у провалившегося обновления были часы запаса), горячо перезагружать новые сертификаты без обрыва живых соединений и ротировать якорь доверия CA отдельным, более медленным процессом с перекрытием, где во время переключения доверяют и старому, и новому корню. На практике этот цикл крутит mesh (Istio, Linkerd) или SPIRE через sidecar/агент, так что код приложения никогда не касается ключа. Компромисс: настоящая личность на каждую нагрузку в обмен на эксплуатацию высокодоступного CA, чей отказ теперь — продакшен-отказ." },
  ]}
/>

<Recap lang="ru">
Обычный TLS доказывает клиенту только сервер, поэтому трафик между сервисами скатывается к доверию сетевому положению или переигрываемому bearer-токену — и то и другое побеждает внутренний атакующий. mTLS добавляет обмен CertificateRequest/CertificateVerify, так что обе стороны доказывают личность, подписывая живое рукопожатие приватным ключом, который никогда не покидает нагрузку, смещая единицу доверия с «откуда пришёл пакет» на «какая личность предъявила валидный признак» — сердце сети с нулевым доверием. Для сервисов эта личность — рабочая нагрузка, обычно SPIFFE ID в поле SAN сертификата. Что критично, mTLS лишь аутентифицирует; авторизация остаётся отдельным слоем политики, решающим, может ли проверенная личность совершить действие. Криптография проста — инженерия в том, чтобы держать высокодоступный CA и автоматический цикл ротации с перекрытием, с короткоживущими сертификатами, чтобы истечение заменяло хрупкий отзыв, а украденный ключ умирал за часы. Поэтому когда кто-то говорит «у нас mTLS, значит, мы zero-trust», твой первый вопрос: авторизует ли хоть что-то проверенную личность на каждом запросе — или ты лишь доказал, кто звонит, и затем доверил ему всё?
</Recap>
