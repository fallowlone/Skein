---
concepts:
- dns-security
- dnssec
- zero-trust
- beyondcorp
- identity-aware-proxy
deepensInto: []
estMin: 18
lang: ru
lessonType: topic
level: senior
order: 4
prereqs: []
slug: 04-dns-and-zero-trust
sources:
- https://csrc.nist.gov/pubs/sp/800/207/final
- https://datatracker.ietf.org/doc/html/rfc8446
spiral: []
status: ready
summary: 'DNS решает, кому доверять, ещё до того как TLS это подтвердит, — поэтому сам запрос остаётся поверхностью атаки, о которой забывают. Урок разбирает отравление кэша, DNSSEC и DoH и то, почему индустрия сменила сетевой периметр на идентичность в каждом запросе.'
title: 'Безопасность DNS и сети с нулевым доверием'
track: security-foundations
unit: 04-network-security
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
Ваш сервис делает один исходящий вызов: `https://payments.internal/charge`. Вы запинили TLS-сертификат, проротировали ключ, включили HSTS. Вы чувствуете себя в безопасности. Но прежде чем что-либо из этого запустится, резолвер спрашивает: «какой IP у `payments.internal`?» — по UDP на порт 53, открытым текстом, без аутентификации и без проверки целостности, и любое устройство на пути или отравленный кэш может ответить адресом атакующего. TLS затем честно устанавливает шифрованный, аутентифицированный канал именно к *этому* хосту. Криптография отработала идеально — она просто защитила разговор не с тем сервером. DNS — это решение о доверии, которое принимается раньше, чем просыпается ваш стек безопасности, и почти всю историю интернета оно по умолчанию было неаутентифицированным.
</Hook>

К концу урока вы поймёте, почему DNS — поверхность атаки, лежащая под TLS, что именно защищают (и не защищают) DNSSEC и DoH и почему индустрия отказалась от сетевого периметра в пользу модели нулевого доверия, построенной на идентичности в каждом запросе.

<Crux>DNS выбирает собеседника раньше, чем TLS его подтвердит; DNSSEC и DoH защищают сам запрос, а zero-trust меняет периметр на личность.</Crux>

<Explanation>

## DNS — это решение о доверии, а не просто поиск

Считайте DNS трубопроводом — и будете защищать не тот слой. Резолюция имени в адрес — это момент, когда ваша система *выбирает, кому доверять*, и классический DNS делает этот выбор по неаутентифицированному UDP открытым текстом. В продакшене доминируют два сценария отказа.

**Отравление кэша.** Резолвер, кэширующий ответы, можно обманом заставить закэшировать поддельный. Атака Камински 2008 года сделала это особенно жёстким: рассылая поддельные ответы для множества случайных поддоменов, атакующему нужно выиграть лишь одну гонку с легитимным ответом, а поскольку у ранней версии протокола был всего лишь 16-битный идентификатор транзакции (65 536 значений) и известный исходный порт, эту гонку можно было выиграть за секунды на быстром канале. Отравленная запись затем отдаётся всем нижестоящим клиентам, пока не истечёт TTL. Патч — рандомизация исходного порта, добавляющая ~16 бит энтропии, — превратил секундную атаку в статистически непрактичную, но это смягчение, а не исправление: ничто в протоколе так и не *аутентифицировало* ответ.

**Атакующий на пути и привилегия резолвера.** Любой между вами и резолвером — вредоносная Wi-Fi-точка, скомпрометированный роутер, враждебный провайдер — может прочитать каждое имя, которое вы ищете (утечка приватности), и переписать ответ (атака на целостность). Ваш резолвер — один из самых привилегированных хостов в стеке: он молча решает, куда пойдёт каждое ваше соединение.

<FlowDiagram
  label="Путь атаки с отравлением DNS: приложение просит резолвер найти внутренний хост по открытому UDP на порту 53, атакующий на пути или отравленный кэш отвечает первым с подконтрольным ему IP, TLS затем строит совершенно корректный шифрованный канал не к тому серверу, а исправление — аутентифицировать сам ответ через DNSSEC и зашифровать запрос через DoH или DoT."
  perRow={5}
  hue="var(--d-crypto)"
  nodes={[
    { id: "app", label: "Приложение", sub: "payments.internal" },
    { id: "udp", label: "Открытый UDP/53", sub: "без аутент." },
    { id: "forge", label: "Ответ атакующего", sub: "поддельный IP" },
    { id: "tls", label: "TLS не к тому", sub: "крипто «ок»" },
    { id: "fix", label: "DNSSEC + DoH", sub: "подпись + шифр" },
  ]}
  edges={[
    { from: "app", to: "udp" },
    { from: "udp", to: "forge", label: "гонка / на пути" },
    { from: "forge", to: "tls", label: "не тот адрес" },
    { from: "udp", to: "fix", label: "исправление" },
  ]}
  caption="Классический DNS в одном пути: резолвер доверяет тому ответу, что пришёл первым, по неаутентифицированному каналу. DNSSEC аутентифицирует запись; DoH/DoT шифруют запрос, чтобы сторона на пути не могла ни прочитать, ни переписать его."
/>

## DNSSEC и DoH защищают разные вещи

Их постоянно путают, и эта путаница рождает ложное чувство безопасности. Они решают ортогональные проблемы.

**DNSSEC** подписывает DNS-записи криптографией с открытым ключом, выстраивая цепочку доверия от корневой зоны до ответа. Он гарантирует **целостность и подлинность**: запись, которую вы получили, — это запись, опубликованная владельцем зоны, без изменений. Он *ничего* не шифрует — DNSSEC-запрос и подписанный ответ по-прежнему видны на проводе — и решает отравление кэша в корне, потому что поддельный ответ не пройдёт проверку подписи. Его цена реальна: развёртывание операционно тяжёлое (ротации ключей, разрыв цепочки доверия уводит зону в офлайн), а внедрение годами буксует значительно ниже половины зон, так что полагаться на него от края до края обычно нельзя.

**DoH / DoT** (DNS over HTTPS / over TLS) оборачивают запрос в шифрованный транспорт. Они гарантируют **конфиденциальность и целостность транспорта** от наблюдателя на пути: атакующий из кофейни больше не видит и не переписывает ваши запросы. Они *не* аутентифицируют саму запись — ваш резолвер всё ещё может вернуть отравленный или цензурированный ответ; вы просто перенесли своё доверие на того, кто держит резолвер. То, что DoH туннелирует DNS внутри обычного HTTPS (порт 443), также объясняет его спорность: он обходит сетевой мониторинг DNS, на который опираются корпоративные и родительские фильтры.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Механизм</th>
        <th class="border border-border px-3 py-2 text-left">Защищает</th>
        <th class="border border-border px-3 py-2 text-left">НЕ защищает</th>
        <th class="border border-border px-3 py-2 text-left">Реальная цена / подвох</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">DNSSEC</td>
        <td class="border border-border px-3 py-2">Целостность + подлинность записи (убивает отравление кэша)</td>
        <td class="border border-border px-3 py-2">Конфиденциальность — запрос и ответ по-прежнему открытым текстом</td>
        <td class="border border-border px-3 py-2">Тяжёлые операции (ротация ключей, разрыв цепочки); внедрение &lt;50% зон</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">DoH / DoT</td>
        <td class="border border-border px-3 py-2">Конфиденциальность от стороны на пути; целостность транспорта</td>
        <td class="border border-border px-3 py-2">Подлинность записи — резолвер всё ещё может солгать</td>
        <td class="border border-border px-3 py-2">Доверие смещается на оператора резолвера; DoH обходит сетевую фильтрацию</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Рандомизация исходного порта</td>
        <td class="border border-border px-3 py-2">Поднимает сложность отравления (~+16 бит энтропии)</td>
        <td class="border border-border px-3 py-2">Всё, что дал бы аутентифицированный канал — это лишь налог на вероятность</td>
        <td class="border border-border px-3 py-2">Смягчение, а не исправление; считает атакующего не на пути</td>
      </tr>
    </tbody>
  </table>
</div>

Зрелое прочтение: **DNSSEC плюс шифрованный транспорт — это полный ответ** — подпишите запись, чтобы её нельзя было подделать, зашифруйте запрос, чтобы его нельзя было прочитать или переписать в полёте. Каждое в отдельности оставляет реальную брешь. И ни то ни другое не отвечает на более глубокий вопрос, ради которого этот урок и существует: даже при идеально разрешённом, идеально зашифрованном соединении с правильным хостом — *должен ли этот вызывающий вообще иметь к нему доступ?* Вот где ломается модель периметра.

## Почему периметр умер: нулевое доверие

Классическая модель — это замок: жёсткий сетевой периметр (файрвол, VPN) вокруг мягкой, доверенной внутренней части. Попал внутрь VPN — и ты «в корпоративной сети», что неявно означало *доверенный* — плоский east-west доступ к внутренним сервисам. У этой модели один катастрофический сценарий отказа: **как только атакующий внутри, он владеет всем.** Фишинговый ноутбук, скомпрометированная VPN-учётка подрядчика, одно проэксплуатированное краевое устройство — и плоская внутренняя часть превращается в площадку для свободного латерального движения. Вторжение SolarWinds 2020 года — канонический пример: доверенная внутренняя позиция превратила одну точку опоры в цепочке поставок в широкий охват.

Нулевое доверие, закреплённое в **NIST SP 800-207**, переворачивает допущение: **никогда не доверяй, всегда проверяй.** Доверенной сети не существует. Расположение запроса — внутри VPN или в Wi-Fi отеля — даёт *ноль* неявной авторизации. Каждый отдельный запрос к ресурсу аутентифицируется и авторизуется по собственным заслугам, оценивается против *идентичности* пользователя и устройства, а не IP, с которого пришёл. Доверие никогда не выдаётся по сетевому положению и непрерывно переоценивается, а не раздаётся один раз при входе.

<Inset kind="why" lang="ru">
Почему «сеть — это периметр» перестаёт работать именно сейчас? Два структурных сдвига. Первый: периметр растворился — SaaS, нагрузки в публичном облаке и удалённая работа означают, что ваши пользователи и сервисы больше не внутри какой-то одной сети, которую вы контролируете; обводить стеной больше нечего. Второй: допущение «внутри = безопасно» всегда было латентным багом — просто раньше оно было переживаемым. По мере того как пробои росли, а латеральное движение становилось доминирующей пост-компрометационной тактикой, цена допущения о неявном доверии в конце концов превысила цену аутентификации каждого запроса. Нулевое доверие — это не новый продукт, который покупают, а снятие допущения, которое вы больше не можете себе позволить.
</Inset>

## Как нулевое доверие выглядит на практике: BeyondCorp и IAP

**BeyondCorp** от Google — эталонная реализация, рождённая из атаки Aurora 2009 года, прошедшей прямо сквозь их периметр. Ключевой компонент — **identity-aware proxy (IAP)**: вместо VPN, который высаживает вас в сеть, каждый запрос к внутреннему приложению проходит через прокси, который *в каждом запросе* проверяет аутентифицированную идентичность пользователя, состояние устройства (управляемое ли оно, пропатченное, здоровое?) и политику авторизации для конкретного ресурса — и затем разрешает или запрещает. Нет сети, в которой можно было бы «находиться». Инженер попадает на внутренний дашборд из кафе ровно так же, как из офиса: через прокси, доказывая, кто он и с какого устройства, каждый раз.

Это схлопывает поверхность атаки так, как VPN никогда не мог. Украденная сессия или фишинговая учётка больше не отдают сеть; они отдают, самое большее, узкий набор ресурсов, на которые авторизована эта пара идентичность-плюс-устройство, перепроверяемый в каждом запросе — а состояние устройства может отозвать доступ в момент, когда машина выпадает из соответствия. Рукопожатие TLS 1.3 (**RFC 8446**), аутентифицирующее каждое из этих соединений, — криптографический фундамент, на котором стоит нулевое доверие: взаимная идентичность, прямая секретность и более быстрое рукопожатие делают проверку в каждом запросе достаточно дешёвой, чтобы делать её везде.

<TradeoffMatrix
  id="04-dns-and-zero-trust-tradeoff-1"
  lessonSlug="04-dns-and-zero-trust"
  lang="ru"
  prompt="Удалённому инженеру нужен доступ к внутреннему админ-дашборду. Из вашего доступа на основе VPN только что выфишили учётку. Выберите дизайн, следующий нулевому доверию."
  options={[
    { name: "Поставить дашборд за identity-aware proxy, который проверяет идентичность пользователя, состояние устройства и политику для конкретного ресурса в каждом запросе", summary: "Идентичность в каждом запросе.", correct: true, justification: "Это модель BeyondCorp / NIST SP 800-207: нет доверенной сети, каждый запрос аутентифицируется и авторизуется по идентичности пользователя и устройства против политики для конкретного ресурса, непрерывно переоценивается. Одна фишинговая учётка даёт самое большее узкий набор, на который авторизована пара идентичность-плюс-здоровое-устройство, — и проверки состояния могут его отозвать. Доверие никогда не выводится из сетевого положения." },
    { name: "Оставить VPN, но требовать его с корпоративного allowlist IP, чтобы дашборд достигали только офисные или VPN-выходные IP", summary: "Allowlist IP.", justification: "Это удваивает ставку на сетевое положение как сигнал доверия — ровно то допущение, что нулевое доверие убирает. Фишинговая учётка по-прежнему работает с разрешённого выхода, а любой, кто попал внутрь VPN, получает плоский east-west охват. Вы сузили исходный IP, а не аутентифицировали запрос." },
    { name: "Добавить второй хоп VPN и более длинный общий секрет, чтобы туннель было труднее взломать", summary: "Прочнее туннель.", justification: "Упрочнение туннеля не меняет модель: попав внутрь, внутренняя часть всё ещё неявно доверена и плоская. Украденная учётка аутентифицируется в туннель и затем бродит. Больше VPN — это больше стены замка вокруг той же мягкой доверенной внутренней части." },
    { name: "Заблокировать дашборд на файрволе для всех внешних IP и требовать, чтобы инженеры физически были в офисе", summary: "Только из офиса.", justification: "Это модель периметра в чистейшем, самом хрупком виде — она путает физическое расположение с авторизацией, полностью ломает удалённую работу и всё равно выдаёт плоское доверие любому (или чему угодно), уже находящемуся в офисной сети. Она путает «где» с «кто»." },
  ]}
/>

<Quiz
  id="04-dns-and-zero-trust-quiz-1"
  lessonSlug="04-dns-and-zero-trust"
  lang="ru"
  question="Вы включаете DoH (DNS over HTTPS) для всех клиентов и считаете, что подмена DNS теперь решена. Что всё ещё открыто?"
  choices={[
    { label: "Подлинность записи — ваш резолвер всё ещё может вернуть отравленный или цензурированный ответ; DoH лишь шифрует запрос от наблюдателей на пути", correct: true },
    { label: "Ничего — сквозное шифрование запроса полностью аутентифицирует ответ", misconception: "Шифрование транспорта — не аутентификация полезной нагрузки. DoH защищает конфиденциальность и целостность транспорта от атакующего на пути, но резолвер, которому вы доверяете, всё ещё может отдать поддельную запись. Только DNSSEC аутентифицирует саму запись." },
    { label: "Конфиденциальность — DoH сливает каждое запрошенное имя в сеть", misconception: "Всё наоборот: вся суть DoH в том, чтобы скрыть запрашиваемые имена от наблюдателей на пути. Брешь — в подлинности, а не в конфиденциальности." },
    { label: "TLS — DoH понижает TLS вашего приложения до более старой версии", misconception: "DoH никак не влияет на TLS прикладного уровня. Эти два независимы; нерешённая проблема — подлинный ли сам DNS-ответ." },
  ]}
/>

<Quiz
  id="04-dns-and-zero-trust-quiz-2"
  lessonSlug="04-dns-and-zero-trust"
  lang="ru"
  question="В архитектуре нулевого доверия (NIST SP 800-207) инженер подключён к корпоративному VPN. Что этот факт ему даёт?"
  choices={[
    { label: "Ничего неявного — каждый запрос всё равно аутентифицируется и авторизуется по ресурсу против его идентичности и состояния устройства", correct: true },
    { label: "Доверенный доступ ко всем внутренним сервисам, ведь он теперь внутри сети", misconception: "Это модель периметра, которую нулевое доверие явно отвергает. Сетевое положение даёт ноль авторизации; «быть на VPN» — не сигнал доверия по SP 800-207." },
    { label: "Подписанную DNSSEC-запись для каждого внутреннего хоста", misconception: "Нулевое доверие — модель авторизации, а не механизм DNS. DNSSEC и нулевое доверие решают разные слои; членство в VPN не имеет отношения к подписи записей." },
    { label: "Постоянный доступ до выхода, ведь он аутентифицировался в момент подключения", misconception: "Нулевое доверие переоценивает доверие непрерывно и в каждом запросе, а не один раз при входе. Единичная аутентификация в момент подключения — ровно то допущение о статичном доверии, что модель убирает." },
  ]}
/>

<DragOrder
  id="04-dns-and-zero-trust-drag-1"
  lessonSlug="04-dns-and-zero-trust"
  lang="ru"
  prompt="Упорядочьте, что происходит в запросе нулевого доверия (IAP) к внутреннему приложению, от вызова клиента до решения разрешить/запретить:"
  items={[
    { id: "a", label: "Клиент запрашивает внутреннее приложение; запрос попадает на identity-aware proxy (нет VPN, нет доверенной сети)" },
    { id: "b", label: "Прокси аутентифицирует идентичность пользователя (SSO / взаимный TLS)" },
    { id: "c", label: "Прокси проверяет состояние устройства — управляемое, пропатченное, здоровое?" },
    { id: "d", label: "Прокси оценивает политику авторизации для этого конкретного ресурса" },
    { id: "e", label: "Разрешить или запретить — переоценивается на следующем запросе, не кэшируется как «доверенный»" },
  ]}
  correctOrder={["a", "b", "c", "d", "e"]}
/>

</Explanation>

<KeyTakeaway>DNS выбирает, кому доверять, раньше, чем TLS это подтвердит. DNSSEC аутентифицирует запись (убивает отравление); DoH/DoT шифруют запрос (убивают подслушивание на пути) — нужны оба. Затем нулевое доверие вовсе убирает сетевое положение как сигнал доверия: каждый запрос аутентифицируется и авторизуется по ресурсу на основе идентичности + устройства, через identity-aware proxy, и никогда не выводится из того, что ты «внутри».</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="04-dns-and-zero-trust-retrieval"
  lang="ru"
  questions={[
    { q: "Почему DNS — поверхность атаки, лежащая под TLS, и что защищает каждый из DNSSEC и DoH?", a: "Резолюция DNS — это момент, когда система решает, кому доверять: она превращает имя в IP, а TLS всегда работает только против того адреса, что вернула резолюция. Классический DNS делает это по неаутентифицированному открытому UDP/53, так что атакующий на пути или отравленный кэш (атака Камински: подделать ответы и выиграть гонку против 16-битного идентификатора транзакции при известном исходном порту) может подставить подконтрольный ему IP. TLS затем строит безупречный шифрованный, аутентифицированный канал — не к тому хосту. Криптография отработала; она защитила разговор с атакующим. DNSSEC и DoH чинят ортогональные половины. DNSSEC подписывает записи цепочкой доверия от корня, гарантируя целостность и подлинность (запись — это то, что опубликовал владелец зоны, поэтому поддельные ответы не проходят проверку и отравление кэша умирает), но не шифрует, так что запросы по-прежнему видны, а внедрение ниже половины зон, потому что ротации ключей операционно тяжелы. DoH/DoT оборачивают запрос в TLS, гарантируя конфиденциальность и целостность транспорта от наблюдателя на пути, но не аутентифицируют запись, так что резолвер, которому вы теперь доверяете, всё ещё может вернуть отравленный ответ. Полная защита — это оба: подписать запись (DNSSEC) и зашифровать запрос (DoH/DoT)." },
    { q: "Что нулевое доверие (NIST SP 800-207) заменяет и как identity-aware proxy его обеспечивает?", a: "Нулевое доверие заменяет модель периметра («замок и ров»), где жёсткая сетевая граница окружает мягкую, неявно доверенную внутреннюю часть, и быть «в сети» (например, на VPN) означает доверенный, плоский east-west доступ. Катастрофический отказ этой модели в том, что одна точка опоры — фишинговая учётка, скомпрометированный VPN-аккаунт, проэксплуатированное краевое устройство — отдаёт всю внутреннюю часть латеральному движению (SolarWinds, Aurora). NIST SP 800-207 переворачивает допущение в «никогда не доверяй, всегда проверяй»: доверенной сети нет, расположение даёт ноль авторизации, и каждый запрос к каждому ресурсу аутентифицируется и авторизуется по собственным заслугам против идентичности пользователя и состояния устройства, непрерывно переоцениваясь, а не выдаваясь один раз при входе. BeyondCorp от Google реализует это через identity-aware proxy (IAP): вместо VPN, высаживающего вас в сеть, каждый запрос к внутреннему приложению проходит через прокси, который в каждом запросе проверяет, кто пользователь, управляемо/пропатчено/здорово ли устройство и политику авторизации для конкретного ресурса, затем разрешает или запрещает. Выигрыш — схлопнутая поверхность атаки: украденная учётка отдаёт лишь узкий набор, на который авторизована пара идентичность-плюс-здоровое-устройство, перепроверяемый в каждом запросе, — стоящая на взаимной аутентификации TLS 1.3 (RFC 8446) как криптографическом фундаменте." },
  ]}
/>

<Recap lang="ru">
DNS — это решение о доверии, принимаемое раньше, чем просыпается ваш стек безопасности: классическая резолюция идёт по неаутентифицированному открытому UDP/53, так что отравление кэша (Камински) или атакующий на пути могут перенаправить вас на вредоносный хост, к которому ваш идеально настроенный TLS затем безупречно подключится — защитив разговор не с тем сервером. DNSSEC и DoH чинят ортогональные половины: DNSSEC подписывает записи ради целостности и подлинности (убивая отравление, но оставляя запросы открытым текстом и застряв ниже 50% внедрения), а DoH/DoT шифруют запрос ради конфиденциальности от наблюдателей на пути (но не остановят лгущий резолвер) — поэтому полный ответ это оба. Над резолюцией стоит более глубокий сдвиг: сетевой периметр умер, потому что SaaS, облако и удалённая работа растворили стену, а «внутри = доверенный» превращал каждую точку опоры в плоское латеральное движение. Нулевое доверие (NIST SP 800-207) вовсе убирает сетевое положение как сигнал доверия — никогда не доверяй, всегда проверяй, — а identity-aware proxy из BeyondCorp обеспечивает это, аутентифицируя идентичность пользователя, состояние устройства и политику для конкретного ресурса в каждом отдельном запросе, стоя на TLS 1.3. Теперь, увидев систему, которая выдаёт доступ потому, что запрос пришёл «изнутри VPN», ваш первый вопрос: что здесь на самом деле аутентифицируется — идентичность и устройство или просто IP?
</Recap>
