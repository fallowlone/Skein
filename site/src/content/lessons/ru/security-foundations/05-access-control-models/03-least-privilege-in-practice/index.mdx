---
concepts:
- least-privilege
- privilege-creep
- just-in-time-access
- deny-by-default
- authorization-bug
deepensInto: []
estMin: 15
lang: ru
lessonType: topic
level: middle
order: 3
prereqs: []
slug: 03-least-privilege-in-practice
sources:
- https://cheatsheetseries.owasp.org/cheatsheets/Authorization_Cheat_Sheet.html
- https://csrc.nist.gov/projects/role-based-access-control
spiral: []
status: ready
summary: 'Минимальные привилегии — принцип, который все цитируют и почти никто не удерживает. Деградация — это расползание прав: гранты копятся, ничего не отзывается. Лекарство операционное: deny by default, доступ точно вовремя и цикл отзыва.'
title: Минимальные привилегии на практике
track: security-foundations
unit: 05-access-control-models
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
Ты открываешь IAM-политику, привязанную к главной сервисной роли твоей команды, чтобы отладить ошибку прав, — и вот оно: `s3:*` на `Resource: "*"`. Никто не решал, что биллинговый воркер должен уметь удалить любой бакет в аккаунте — оно просто *так получилось*. Полтора года назад кому-то понадобился доступ на чтение к одному бакету, он упёрся в deny, нашлёпал на него wildcard, чтобы разблокировать релиз, и пошёл дальше. Грант пережил и задачу, и человека, и причину. Теперь эта роль в одной утёкшей утечке ключа от того, чтобы стереть боевое хранилище, — и самое страшное, что всё работает идеально. Минимальные привилегии не упали с грохотом. Они истёрлись, один удобный грант за раз.
</Hook>

К концу этого урока ты поймёшь, почему минимальные привилегии деградируют в избыточные, и три операционных хода — deny by default, доступ точно вовремя и цикл отзыва, — которые на самом деле удерживают линию.

<Crux>Минимальные привилегии — это практика, а не настройка: с расползанием прав борются deny-by-default и доступом точно вовремя.</Crux>

<Explanation>

## Принцип — и почему его недостаточно просто провозгласить

Принцип минимальных привилегий гласит: каждый субъект — пользователь, сервис, процесс — должен держать ровно те права, что нужны ему для работы, и ни одним больше. NIST называет его фундаментом управления доступом; OWASP Authorization Cheat Sheet открывается словами «enforce least privileges» и «deny by default». Каждый инженер согласно кивает. Почти ни в одной боевой системе этого на самом деле нет.

Причина в том, что минимальные привилегии — это не свойство, которое *настраивают один раз*, а свойство, которое приходится *поддерживать против постоянного давления*. Права липки в одну сторону. Выдать легко: кто-то заблокирован, ты добавляешь allow, тикет закрывается, *сегодня* все довольны. Отозвать тяжело: тебя никто не блокирует правом, которое у тебя всё ещё есть, поэтому нет тяги его убрать, нет красного теста, нет алерта с пейджера. Система, которая выдаёт по запросу и никогда не отзывает, не дрейфует к минимальным привилегиям — она монотонно дрейфует от них. Вся проблема в этой асимметрии, и у неё есть имя.

## Расползание прав: медленная течь

**Расползание прав (privilege creep)** — это постепенное накопление прав, которые субъекту больше не нужны. Оно приходит из горстки совершенно будничных источников, ни один из которых в момент не ощущается как решение по безопасности:

- **Смена роли, которая добавляет, не вычитая.** Инженер переходит из команды платежей в платформу. Доступ группы платформы он получает в первый же день. А доступ к платежам? Он всё ещё на месте через год — никто не владеет вычитанием, а отозвать его может *что-то* сломать, поэтому он остаётся.
- **Удобный wildcard.** `deny` блокирует релиз в 16:00. Уезжает в прод фикс `s3:*` / `Resource: *`, потому что сузить его до трёх реально нужных действий и одного бакета — это двадцать минут, которых у тебя нет. Wildcard постоянен; дедлайн был временным.
- **Постоянный админ.** Break-glass-доступ для инцидента в 2 ночи так и не отзывают на следующее утро. Теперь это просто «доступ, который у меня есть», а через три месяца он несущий для какой-то рутинной задачи, для которой никогда не предназначался.
- **Скопированная роль.** Новому сервису нужны права? Клонируй роль у соседнего сервиса. Теперь он наследует каждый грант, который *тот* сервис нарастил, включая те, которые никто не может объяснить.

Каждый шаг локально рационален и глобально разъедающ. В итоге — флот идентичностей, каждая из которых держит куда больше власти, чем требует её реальная работа, а значит, радиус поражения любой одной скомпрометированной учётки огромен и полностью невидим до того дня, когда его эксплуатируют.

<FlowDiagram
  label="Храповик расползания прав: субъект стартует с минимальных привилегий, затем смена роли, удобный wildcard и неотозванный break-glass-доступ каждый добавляют права, которые никогда не убираются, поэтому эффективные привилегии только растут; deny-by-default плюс грант точно вовремя с истечением — это рычаг, который толкает их обратно вниз."
  perRow={5}
  hue="var(--d-crypto)"
  nodes={[
    { id: "start", label: "Минимум прав", sub: "scoped grants" },
    { id: "move", label: "Смена роли", sub: "add, no subtract" },
    { id: "wild", label: "Удобный wildcard", sub: "s3:* on *" },
    { id: "glass", label: "Break-glass", sub: "не отозван" },
    { id: "jit", label: "JIT + истечение", sub: "auto-revoke" },
  ]}
  edges={[
    { from: "start", to: "move", label: "creep" },
    { from: "move", to: "wild", label: "creep" },
    { from: "wild", to: "glass", label: "creep" },
    { from: "glass", to: "jit", label: "рычаг" },
  ]}
  caption="Расползание прав — это храповик: каждый грант щёлкает эффективные привилегии вверх, и ничто не щёлкает их назад. Доступ точно вовремя с жёстким истечением — единственный шаг, двигающий храповик в обратную сторону: он выдаёт право, когда нужно, и отзывает автоматически, когда окно закрывается."
/>

## Deny by default: сделать «нет» точкой старта

Структурный фикс расползания начинается с фундамента — **deny by default**. Авторизация начинается с отказа, а доступ выдаётся только явным положительным правилом, совпадающим по субъекту, действию и ресурсу. Всё, что не разрешено явно, — запрещено, включая, что критично, всё *новое*.

Это звучит очевидно, пока не сопоставишь это со сбоем, который оно предотвращает. Система *allow-by-default* (или любой allowlist с wildcard-фолбэком) отказывает «открыто» (fails open): добавь новый эндпоинт, новый тип ресурса, новое админ-действие — и оно достижимо, *если кто-то не вспомнил* написать для него deny-правило. Deny by default отказывает «закрыто» (fails closed): новая штука недостижима, пока кто-то не напишет allow. Разница в том, утекает ли забытое правило в доступ или просто выдаёт 403, по которому кто-то заводит тикет. Fail-closed превращает твои упущения в неудобства, а не в пробои.

На практике это значит: никаких `Resource: "*"` и никаких `action: *` в политиках, где их можно избежать; права, ограниченные конкретными действиями на конкретных ресурсах; и ответ по умолчанию на «может ли эта идентичность сделать эту штуку» — *нет*, пока правило не скажет да. Цена реальна — ты пишешь больше узких правил и ешь больше 403 во время разработки. Эта цена и есть смысл. Это и есть то трение, что держит wildcard снаружи.

<Inset kind="why" lang="ru">
Почему не аудитить права раз в квартал и не вычищать избыток? Потому что периодический аудит борется с асимметрией, которую не может выиграть. Гранты накапливаются непрерывно — каждый день, под реальными дедлайнами, — а вычистка происходит пакетом четыре раза в год и не является ничьей любимой работой. Хуже того, нельзя безопасно убрать право, про которое нельзя *доказать*, что оно не используется, а доказать отрицание («ничто на это не опирается») по живой системе по-настоящему трудно, поэтому аудиты склоняются к тому, чтобы оставлять. Аудит не бесполезен, но это швабра, а не кран. Устойчивый фикс меняет то, *как* гранты выдаются — короткоживущими и самоистекающими, — чтобы избыток вовсе не накапливался.
</Inset>

## Доступ точно вовремя: гранты, которые истекают сами

Асимметрия, которая гонит расползание, — это «легко выдать, тяжело отозвать». **Доступ точно вовремя (JIT, just-in-time)** атакует её напрямую, делая отзыв автоматическим: вместо того чтобы держать постоянное право вечно, субъект *запрашивает* повышенный доступ под конкретную задачу, получает его на ограниченное окно, и система *забирает его назад*, когда окно закрывается, — никому не приходится помнить.

Паттерн всплывает на каждом слое:

- **Люди-админы:** постоянное членство в роли администратора убирается; инженер запрашивает повышение (часто с причиной и согласованием) и получает его, скажем, на 60 минут, после чего членство в роли автоматически снимается. Состояние по умолчанию каждого человека — *не* привилегированное.
- **Сервисы:** вместо долгоживущего API-ключа с широким scope, зашитого в деплой, нагрузка ассумит роль и получает короткоживущие учётки (минуты — час), которые авто-ротируются и авто-истекают. Утёкшая учётка полезна только внутри своего крошечного окна.
- **Break-glass:** аварийный доступ сам является JIT-грантом — явно запрашивается, ограничен по времени, громко логируется и авто-отзывается, — поэтому инцидент в 2 ночи не оставляет за собой постоянный админ-грант.

JIT переворачивает устойчивое состояние. С постоянным доступом по умолчанию «у всех больше, чем нужно, всё время», и ты тратишь усилия на *удаление* избытка, который никогда полностью не выловишь. С JIT по умолчанию «ни у кого ничего нет, пока он не попросит», и привилегия существует только те минуты, что реально используется. Ты больше не борешься с расползанием уборкой — ты убрал поверхность, на которой расползание накапливается.

<TradeoffMatrix
  id="03-least-privilege-tradeoff-1"
  lessonSlug="03-least-privilege-in-practice"
  lang="ru"
  prompt="У сервисной роли стоит `s3:*` на `Resource: *`, потому что кто-то добавил wildcard 18 месяцев назад, чтобы разблокировать релиз. Сервис на деле когда-либо вызывает только GetObject и PutObject на одном бакете. Какой фикс правильный?"
  options={[
    { name: "Сузить политику ровно до `s3:GetObject` и `s3:PutObject` на ARN одного бакета и перевести роль на короткоживущие assumed-role учётки", summary: "Минимум прав + JIT.", correct: true, justification: "Это и есть фикс: сузить права до конкретных действий на конкретном ресурсе, который использует нагрузка, и заменить любой долгоживущий ключ на короткоживущие, авто-истекающие учётки, чтобы у утечки было крошечное окно. Deny-by-default плюс минимум прав, применённые и к гранту, и к его времени жизни." },
    { name: "Оставить wildcard, но ротировать ключ доступа каждые 90 дней", summary: "Ротировать ключ.", justification: "Ротация ограничивает, как долго *утёкший* ключ остаётся валидным, но ничего не делает со scope — роль всё то время, что ключ валиден, может удалить любой бакет в аккаунте. Ты ограничил время жизни учётки, не тронув избыточность прав, которая и делает учётку опасной." },
    { name: "Оставить `s3:*`, но добавить deny-правило на `s3:DeleteBucket`, чтобы самое страшное действие было заблокировано", summary: "Запретить страшное.", justification: "Блок-листинг отдельных опасных действий — проигрышная игра: нельзя перечислить каждое вредное действие, и следующий рискованный API, который забыл запретить, достижим. Это allow-by-default с заплатками, инверсия deny-by-default. Разрешай только нужное, а не запрещай то, чего боишься." },
    { name: "Ограничить роль, привязав её только к продакшен-VPC, чтобы внешние атакующие не могли её использовать", summary: "Ограничить по сети.", justification: "Сетевые контроли — это defense in depth, а не авторизация. Компрометация, которая уже выполняется внутри VPC (типичный случай для утёкшей учётки нагрузки), всё равно наследует `s3:*` на всё. Это не снижает привилегии, которые держит идентичность, а это и есть настоящая проблема." },
  ]}
/>

## Как зрелые инженеры удерживают линию

Удержание минимальных привилегий — это операционный цикл, а не разовая настройка. Зрелые ходы: сделать deny дефолтом, чтобы новая поверхность отказывала закрыто; делать гранты узкими (конкретные действия на конкретных ресурсах, без рефлекторных wildcard) и короткоживущими (JIT с жёстким истечением), чтобы привилегия существовала только пока используется; и замкнуть цикл отзывом — когда кто-то меняет команду, когда проект заканчивается, когда срабатывает break-glass, *вычитанием* должен кто-то владеть, а не оставлять его квартальному аудиту, который склоняется к тому, чтобы оставлять. Грант без истечения и без владельца — это тот самый, что станет инцидентом `s3:*` в следующем году.

<Quiz
  id="03-least-privilege-quiz-1"
  lessonSlug="03-least-privilege-in-practice"
  lang="ru"
  question="Почему расползание прав почти всегда заставляет права со временем расти, а не сжиматься, даже в хорошо организованных командах?"
  choices={[
    { label: "Выдачу тянет реальный, немедленный блокер (кто-то застрял сейчас), а у отзыва нет такой же тяги — ничего не ломается, если оставить право, которое больше не нужно", correct: true },
    { label: "Потому что IAM-системы технически не поддерживают удаление прав после выдачи", misconception: "Удаление полностью поддерживается везде. Проблема в асимметрии стимулов, а не в отсутствующей фиче — никого не заставляют отзывать, потому что неиспользуемые права не вызывают видимых сбоев." },
    { label: "Потому что атакующие добавляют права ролям в рамках эксплуатации", misconception: "Расползание — это внутренний процесс с благими намерениями: смены ролей, удобные wildcard, неотозванный break-glass. Это то, как нормальные команды работают под дедлайнами, а не шаг атаки." },
    { label: "Потому что минимум прав применим только к людям, а сервисам законно нужен широкий доступ", misconception: "Минимум прав применим к каждому субъекту, особенно к сервисам — сервис с s3:* и есть та избыточная идентичность, что превращает одну утёкшую учётку в полный пробой." },
  ]}
/>

<Quiz
  id="03-least-privilege-quiz-2"
  lessonSlug="03-least-privilege-in-practice"
  lang="ru"
  question="В чём ключевое преимущество по безопасности у доступа точно вовремя перед постоянным админ-грантом?"
  choices={[
    { label: "Утёкшая или злоупотреблённая учётка полезна только внутри короткого ограниченного окна, потому что доступ авто-истекает — состояние по умолчанию каждой идентичности непривилегированное", correct: true },
    { label: "Он шифрует учётки, чтобы их вообще нельзя было украсть", misconception: "JIT — про время жизни и scope авторизации, а не про шифрование. Учётку всё ещё можно перехватить, пока она живая; смысл в том, что через минуты она бесполезна, а не в том, что её нельзя взять." },
    { label: "Он делает аудиты ненужными, потому что система автоматически соответствует требованиям", misconception: "JIT снижает постоянные привилегии, но не убирает необходимость пересматривать, кто может запрашивать повышение и какие согласования есть. Он сужает поверхность, но не делает надзор излишним." },
    { label: "Он выдаёт больше прав быстрее, поэтому инженеры никогда не заблокированы", misconception: "Цель противоположная — никто не держит повышенный доступ по умолчанию; его запрашивают под задачу и теряют автоматически. Скорость разблокировки — не свойство безопасности; ограниченное время жизни — да." },
  ]}
/>

<DragOrder
  id="03-least-privilege-drag-1"
  lessonSlug="03-least-privilege-in-practice"
  lang="ru"
  prompt="Упорядочь эти шаги, чтобы исправить избыточную сервисную роль без простоя, от первого к последнему:"
  items={[
    { id: "a", label: "Понаблюдать, какие действия/ресурсы роль реально использует (логи доступа / IAM access analyzer)" },
    { id: "b", label: "Написать новую политику, ограниченную ровно этими наблюдёнными действиями на этих конкретных ресурсах" },
    { id: "c", label: "Выкатить суженную политику и следить за deny-ошибками / 403 в staging и канарейке" },
    { id: "d", label: "Заменить долгоживущий ключ на короткоживущие assumed-role учётки" },
    { id: "e", label: "Добавить владельца и дату истечения/пересмотра, чтобы грант не стал тихо снова постоянным" },
  ]}
  correctOrder={["a", "b", "c", "d", "e"]}
/>

</Explanation>

<KeyTakeaway>Минимальные привилегии деградируют, потому что у выдачи есть срочность, а у отзыва нет — эта асимметрия и есть расползание прав. Удерживай линию операционно: deny by default, чтобы новая поверхность отказывала закрыто; ограничивай гранты конкретными действиями на конкретных ресурсах; делай их точно вовремя с жёстким истечением; и владей циклом отзыва. Грант без владельца и без истечения станет инцидентом `s3:*` в следующем году.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="03-least-privilege-retrieval"
  lang="ru"
  questions={[
    { q: "Объясни, почему минимальные привилегии со временем истираются в избыточные, и назови механизм.", a: "Механизм — расползание прав (privilege creep): постепенное накопление прав, которые субъекту больше не нужны. Оно истирается, потому что выдача и отзыв асимметричны. У выдачи есть реальная, немедленная тяга — кто-то заблокирован, ты добавляешь allow, проблема решена сегодня, — а у отзыва её нет: тебя никто не блокирует правом, которое у тебя всё ещё есть, ничего не падает, тест не краснеет, пейджер не звонит. Поэтому права липки в одну сторону, и система монотонно дрейфует прочь от минимальных привилегий. Источники будничны и в момент ощущаются как ничто: смена роли, которая добавляет доступ новой команды, не вычитая доступ старой; удобный wildcard (s3:* на Resource: *), отгруженный, чтобы успеть к дедлайну, вместо узкого гранта, который занял бы ещё двадцать минут; break-glass админ-доступ, который никогда не отзывают после инцидента; и роли, клонированные у соседа, наследующие гранты, которые никто не может объяснить. Каждый шаг локально рационален и глобально разъедающ, а в итоге — флот идентичностей, каждая держит куда больше власти, чем требует её работа, поэтому радиус поражения любой одной утёкшей учётки огромен и невидим до того, как его эксплуатируют." },
    { q: "Что именно чинят deny-by-default и доступ точно вовремя, и почему JIT — более устойчивое лекарство?", a: "Deny by default чинит *режим отказа* новой поверхности. Авторизация начинается с отказа, и доступ выдаётся только явным положительным правилом, совпадающим по субъекту, действию и ресурсу; всё, что не разрешено, — включая всё новое, — запрещено. Контраст — система allow-by-default, которая отказывает открыто: новый эндпоинт или ресурс достижим, если кто-то не вспомнил написать deny, поэтому забытое правило утекает в доступ. Deny-by-default отказывает закрыто: забытое правило даёт 403, по которому кто-то заводит тикет, а не пробой. Доступ точно вовремя чинит *саму асимметрию* — «легко выдать, тяжело отозвать» — делая отзыв автоматическим. Вместо постоянного права субъект запрашивает повышенный доступ под конкретную задачу, получает на ограниченное окно, и система снимает его, когда окно закрывается: люди запрашивают 60-минутное повышение, сервисы ассумят роли для короткоживущих авто-истекающих учёток, break-glass ограничен по времени и авто-отзывается. JIT — более устойчивое лекарство, потому что deny-by-default и периодические аудиты всё ещё борются с накапливающейся кучей — ты вечно убираешь избыток, который не выловишь полностью, — тогда как JIT убирает саму поверхность, на которой избыток накапливается: привилегия существует только те минуты, что используется, состояние по умолчанию каждой идентичности непривилегированное, а утёкшая учётка бесполезна вне своего крошечного окна." },
  ]}
/>

<Recap lang="ru">
Минимальные привилегии — это принцип, который все цитируют и почти никто не удерживает, потому что это не настройка, которую конфигурируют один раз, а свойство, которое поддерживают против постоянного давления. Выдачу тянет реальный, немедленный блокер; у отзыва нет такой же тяги, поэтому права монотонно дрейфуют вверх. Эта деградация и есть расползание прав, питаемое сменами ролей, что добавляют не вычитая, удобными wildcard, отгруженными ради дедлайна, неотозванным break-glass и скопированными ролями. У операционного лекарства три хода. Deny by default делает так, что авторизация начинается с «нет», поэтому новая поверхность отказывает закрыто — забытое правило даёт 403, а не пробой. Узкие, короткоживущие гранты ограничивают права конкретными действиями на конкретных ресурсах. А доступ точно вовремя делает отзыв автоматическим: повышение запрашивается под задачу, держится на ограниченное окно и снимается, когда оно закрывается, поэтому состояние по умолчанию каждой идентичности непривилегированное, а утёкшая учётка ничего не стоит через минуты. Теперь, когда ты видишь `s3:*` на `Resource: *`, твой первый вопрос: кто владеет вычитанием, и когда этот грант истекает?
</Recap>
