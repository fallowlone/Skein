---
concepts:
- go-modules-tooling
deepensInto: []
estMin: 16
lang: ru
lessonType: topic
level: middle
order: 4
prereqs: []
slug: "04-modules-and-tooling"
sources:
- https://go.dev/ref/mod
- https://go.dev/blog/using-go-modules
- https://pkg.go.dev/cmd/vet
spiral: []
status: ready
summary: 'require в go.mod — минимумы, MVS берёт их максимум: детерминированно, без солвера и внезапных апгрейдов; go.sum — журнал хешей, не lock-файл. replace действует только в главном модуле; go.work — для мультимодульной разработки. CI: vet, staticcheck, -race, проверки дрейфа.'
title: 'Модули и тулчейн: MVS, go.sum, воркспейсы и инструменты CI'
track: "go"
unit: "04-testing-and-tooling"
---

import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";
import Infographic from "~/components/diagram/Infographic.astro";
import mvsVsSemver from "./infographics/mvs-vs-semver.svg?raw";

<Hook>
В пятницу после обеда покраснели разом все пайплайны организации: verifying github.com/acme/sigutil@v1.4.2: checksum mismatch. Инцидент безопасности? Почти наоборот. Автор библиотеки нашёл баг в теге, удалил v1.4.2 и перетегировал исправленный коммит той же версией — тихая маленькая переписка истории. Go отказался подыгрывать: go.sum в каждом потребляющем репозитории хранил хеш *исходного* v1.4.2, публичная база контрольных сумм соглашалась, и новые байты под старым именем провалили верификацию везде. Половина команды с npm-прошлым прочитала ошибку как аварию; вторая половина — как работу системы: кто-то изменил опубликованный код, не изменив версию, и сборка сказала «нет». Автор выпустил v1.4.3, одна строка go get — и всё зазеленело, а единственным пунктом постмортема стала ссылка на документацию MVS: в Go ничто в графе зависимостей не двигается, пока не подвинете вы.
</Hook>

<Crux>MVS берёт минимальную версию, устраивающую всех, — сборка воспроизводима по построению. go.sum сверяет байты; vet и -race стерегут CI.</Crux>

<Explanation>

## MVS: минимальный выбор версий, а не солвер

Почему Go-сборка воспроизводится байт-в-байт на машине коллеги через шесть месяцев, тогда как аналогичный npm-проект требует lock-файла и всё равно порой отличается? Ответ — один алгоритм, умещающийся на салфетке.

Строка `require` в go.mod — это *минимум*, а не диапазон. Чтобы собрать итоговый список версий, Go обходит граф модулей, собирает требуемый минимум каждого модуля для каждой зависимости и берёт **максимум из минимумов** — наименьшую версию, устраивающую всех. Это весь алгоритм:

```go
// ваш модуль требует:  A v1.2.0,  B v1.1.0
// A требует:           C v1.3.0
// B требует:           C v1.5.0
//
// MVS выбирает C v1.5.0 — максимум из требуемых минимумов.
// НЕ последний релиз C v1.9.2: MVS никогда не прыгает дальше
// того, что кто-то в графе явно попросил.
//
// Апгрейды всегда явные:
//   go get C@v1.6.0    // поднять собственный минимум
//   go get -u ./...    // поднять всё (осознанно)
//   go mod tidy        // синхронизировать go.mod с импортами
```

Сравните с моделью npm: semver-диапазоны (`^1.3.0`), разрешаемые в момент установки, означают, что один и тот же package.json в разные дни даёт разные деревья, — и для восстановления детерминизма приходится прикручивать lock-файл. MVS для *выбора версий* lock-файл не нужен: список сборки — чистая функция от go.mod-файлов графа, вычислимая вручную, одинаковая на каждой машине, всегда. Размен честный: автоматического подхвата багфиксов нет; security-патч в C v1.5.1 доедет до вас, только когда вы или зависимость его попросите — отсюда `govulncheck` и регулярные PR с `go get -u` как противовес. go.sum добавляет не пиннинг версий, а **верификацию содержимого**: журнал криптографических хешей каждой когда-либо использованной версии модуля, сверяемый с transparency-логом (публичным append-only реестром хешей модулей) sum.golang.org при первом скачивании. Версии берутся из go.mod; go.sum гарантирует, что названная версия байт-в-байт совпадает с тем, что получил остальной мир, — именно это и сработало в Hook.

<Infographic
  svg={mvsVsSemver}
  label="Сравнение бок о бок: Go MVS считает каждый require минимумом и берёт максимум из минимумов, поэтому список сборки — чистая функция от go.mod без lock-файла и без автоматических апгрейдов; semver-диапазоны npm вроде ^1.3.0 разрешаются к последнему совместимому релизу в момент установки, давая разные деревья в разные дни и требуя прикрученного lock-файла для восстановления детерминизма."
  caption="Весь разрыв в детерминизме в одном кадре: Go выбирает версии из go.mod по построению, а npm разрешает диапазоны при установке и фиксирует постфактум lock-файлом."
  hue="var(--d-infra)"
/>

<Quiz
  id="04-modules-and-tooling-quiz-1"
  lessonSlug="04-modules-and-tooling"
  lang="ru"
  question="Ваше приложение требует A и B. A требует zap v1.21.0, B требует zap v1.24.0, последний релиз zap — v1.27.0. Какая версия попадёт в сборку и когда она изменится?"
  choices={[
    { label: "v1.24.0 — максимум из требуемых минимумов; изменится только когда вы запустите go get или зависимость поднимет своё требование в версии, которую вы примете", correct: true },
    { label: "v1.27.0 — Go разрешает к последнему совместимому релизу, как caret-диапазоны npm", misconception: "MVS никогда не выбирает дальше явных требований графа. Последний-совместимый — модель npm, и именно она делает установки зависимыми от времени; Go меняет автоматическую свежесть на список сборки как чистую функцию от go.mod." },
    { label: "v1.21.0 — минимальный выбор версий означает, что побеждает наименьшая версия в графе", misconception: "Минимальная — значит наименьшая версия, удовлетворяющая каждому требованию, а v1.21.0 не проходит заявленный B минимум v1.24.0. MVS берёт максимум из минимумов — минимальный относительно солверов с диапазонами и latest, а не абсолютно." },
    { label: "Ту, что запинена в go.sum — go.sum и есть lock-файл Go", misconception: "go.sum хранит хеши содержимого для верификации, а не выбор версий; его удаление не меняет ни одной версии (Go перекачает и заново сверит хеши). Выбор версий целиком живёт в go.mod через MVS." },
  ]}
/>

## replace и go.work: локальная обвязка, которая не должна утекать

Два инструмента перенаправляют, откуда берётся исходник модуля, — с очень разным радиусом поражения. Директива `replace` в go.mod (`replace github.com/acme/lib => ../lib`) действует, **только когда этот модуль — главный**: replace в go.mod ваших зависимостей игнорируются целиком. Это делает replace безопасным для форков и аварийных пинов, но и классической миной: закоммиченный `=> ../lib` собирается на вашем ноутбуке и умирает в Docker и у каждого потребителя, потому что относительный путь существует только на вашем диске. С Go 1.18 правильный инструмент мультимодульной разработки — **воркспейс**: файл go.work со строками `use ./app` и `use ./lib` заставляет тулчейн разрешать lib из локальных исходников для всех перечисленных модулей — без правок go.mod, и go.work по конвенции не коммитится (локальная обвязка разработчика, как конфиг IDE), так что в опубликованный модуль ничего не утекает. CI, собирающий без воркспейса, видит ровно то, что увидят потребители; `GOFLAGS=-mod=readonly` (поведение по умолчанию с 1.16) плюс отсутствие go.work в CI — честная конфигурация.

## Тулчейн в CI: vet, staticcheck, -race, generate

Компилятор принимает массу неправильных программ; тулчейн сужает зазор слоями. `go vet` гоняет анализаторы багов, которые система типов не видит, — несовпадения аргументов printf, скопированные мьютексы (`copylocks`), недостижимый код; подмножество vet уже выполняется автоматически при `go test`, но CI должен гонять полный `go vet ./...`. **staticcheck** идёт на порядок дальше своими SA-проверками (запись в nil-map, неверный time.Format, отложенный Close на nil-теле ответа) и де-факто является вторым линтером серьёзных Go-команд. **Детектор гонок** — динамическая инструментация: `go test -race` ловит только гонки, которые тесты реально исполнили, по реальной цене — примерно 5–10× CPU и 5–10× памяти; поэтому его место — на CI-прогоне тестов (всегда) и, возможно, на канареечной реплике, но не на каждом продакшен-поде. `go generate` — это контракт с людьми, а не со сборкой: он никогда не запускается автоматически, поэтому сгенерированный код (моки, protobuf, stringer) коммитится, а CI стережёт дрейф, перегенерируя и падая на непустом `git diff` — тот же трюк, что и `go mod tidy -diff`. Для воспроизводимых релизных бинарей: запиненная директива `toolchain go1.24.x` в go.mod, `-trimpath`, чтобы пути сборщика не вшивались в бинарь, и module proxy плюс go.sum довершают дело — одинаковые входы, одинаковые байты.

<Quiz
  id="04-modules-and-tooling-quiz-2"
  lessonSlug="04-modules-and-tooling"
  lang="ru"
  question="Коллега удаляет go.sum, чтобы починить ошибку checksum mismatch, и сборка зеленеет. Что проект на самом деле потерял?"
  choices={[
    { label: "Верификацию содержимого — версии не изменились (их решает go.mod), но свежезаписанный go.sum теперь доверяет тем байтам, что прокси отдал сегодня, принимая ровно ту переписку, о которой кричала ошибка", correct: true },
    { label: "Пиннинг версий — без go.sum MVS уплывает к последним совместимым релизам", misconception: "go.sum не участвует в выборе версий; MVS вычисляет тот же список сборки из go.mod с ним или без него. Считать go.sum lock-файлом — привычка из npm, от которой этот дизайн нарочно уходит." },
    { label: "Ничего — go.sum это производный кэш, который Go регенерирует с теми же гарантиями", misconception: "Регенерация записывает хеши того, что скачивается сейчас, а не того, что было проверено раньше. Mismatch и был гарантией в действии: опубликованные байты изменились под существующей версией. Удаление журнала превращает тревогу в молчаливое согласие." },
    { label: "Скорость сборки — go.sum позволяет Go пропускать скачивание не изменившихся модулей", misconception: "Кэширование — работа кэша модулей (GOMODCACHE); go.sum используется для верификации скачанного, а не для его избегания. Его потеря не стоит скорости — она стоит доказательства, что дерево зависимостей байт-в-байт совпадает с проверенным всеми остальными." },
  ]}
/>

</Explanation>

<FlowDiagram
  label="Требования go.mod проходят через MVS в детерминированный список сборки, верифицируются go.sum и базой контрольных сумм, затем гейтуются vet, staticcheck и тестами с -race в CI"
  hue="var(--d-backend)"
  perRow={3}
  nodes={[
    { id: "gomod", label: "Граф go.mod", sub: "require = минимумы" },
    { id: "mvs", label: "MVS", sub: "максимум минимумов" },
    { id: "sum", label: "go.sum + sumdb", sub: "хеши содержимого" },
    { id: "vet", label: "vet + staticcheck", sub: "статический анализ" },
    { id: "race", label: "go test -race", sub: "цена 5-10x, только CI" },
    { id: "bin", label: "Релизный бинарь", sub: "-trimpath, пин тулчейна" },
  ]}
  edges={[
    { from: "gomod", to: "mvs", label: "вычислить" },
    { from: "mvs", to: "sum", label: "сверить байты" },
    { from: "sum", to: "vet", label: "список готов" },
    { from: "vet", to: "race", label: "затем тесты" },
    { from: "race", to: "bin", label: "релиз" },
  ]}
  caption="Выбор версий — чистая функция от go.mod-файлов: без солвера, без lock-файла, без зависимости от времени. go.sum верифицирует содержимое и никогда не выбирает версии. Динамический детектор гонок живёт там, где живут тесты; воспроизводимость — пин тулчейна плюс trimpath."
/>

<KeyTakeaway>MVS делает список сборки чистой функцией от go.mod-файлов: require — минимумы, побеждает максимум из минимумов, и ничего не апгрейдится, пока вы не попросите; go.sum затем сверяет байты с transparency-логом — это журнал, а не lock-файл. Не коммитьте replace (действует только в главном модуле, ломает потребителей); для мультимодульной разработки — некоммитимый go.work. CI гоняет go vet, staticcheck, тесты с -race (цена 5–10×, ловит только исполненные гонки) и проверки дрейфа tidy и generate.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="04-modules-and-tooling-retrieval"
  lang="ru"
  questions={[
    { q: "Объясни, как MVS выбирает версии, почему Go не нужен lock-файл для этого, и точное разделение труда между go.mod и go.sum.", a: "Каждый require — минимум. Go обходит граф модулей, собирает требуемые минимумы каждой зависимости и выбирает их максимум — наименьшую версию, устраивающую всех, и никогда ничего новее явно запрошенного. Список сборки — чистая функция от go.mod-файлов: тот же граф — тот же результат, любая машина, любой день; именно этого нет у npm с разрешением диапазонов при установке, потому ему и нужен прикрученный lock-файл. Апгрейды — только через go get (точечно или -u). go.mod владеет выбором версий; go.sum — верификацией содержимого: журнал хешей каждой использованной версии модуля, сверяемый с transparency-логом sum.golang.org, так что перетегированный v1.4.2 с другими байтами падает у всех. Удаление go.sum не меняет версий — оно удаляет доказательство." },
    { q: "Спроектируй тулчейн-гейты CI для репозитория сервиса: что запускается, что каждый слой ловит и что не попадает в продакшен.", a: "Гейт PR: go vet ./... (несовпадения printf, copylocks — подмножество в go test не полный набор), staticcheck (SA-проверки: запись в nil-map, неверный time.Format), go test -race ./... — динамический детектор, 5–10× CPU/память, ловит только исполненные тестами гонки, поэтому живёт в CI и, может, на канарейке, но никогда на всём прод-флоте. Гейты дрейфа: go mod tidy -diff падает на рассинхронном go.mod; перегенерировать выводы go generate и падать на git diff, потому что generate не запускается сам, а сгенерированный код коммитится. Гигиена: никаких replace в коммитах (только главный модуль, относительные пути умирают в Docker); go.work не трекается, чтобы CI видел то же, что потребители. Релиз: пин директивы toolchain плюс -trimpath для воспроизводимых бинарей; govulncheck и регулярные go get -u PR как противовес отказу MVS от автоапгрейдов." },
  ]}
/>

<Recap lang="ru">
История зависимостей Go детерминирована по построению. Строки require — минимумы; MVS собирает минимумы всех модулей и берёт их максимум, никогда не прыгая к latest, — поэтому список сборки является чистой функцией от go.mod-файлов, воспроизводим на любой машине без lock-файла, и ничего не двигается, пока его не подвинет go get. Цена — отсутствие автоматического подхвата багфиксов, компенсируемая govulncheck и регулярными PR на обновление. go.sum — вторая половина и другая работа: журнал хешей содержимого, сверяемый с публичным transparency-логом, — потому тихо перетегированная версия и валит сборку каждого потребителя; пятничный инцидент из Hook был успехом механизма. У локальной обвязки острые края: replace действует только в главном модуле, и закоммиченный replace с относительным путём ломает Docker и всех потребителей, тогда как go.work даёт мультимодульной разработке тот же эффект в виде локальной, по конвенции некоммитимой конфигурации. Дальше тулчейн наслаивает то, чего компилятор не обещает: go vet и staticcheck для статически находимых багов, go test -race для динамически исполненных гонок по цене 5–10× (CI — да, прод-флот — нет), go generate как запускаемый человеком шаг, чьи выводы коммитятся и проверяются на дрейф, и go mod tidy -diff, держащий манифест честным. Воспроизводимые бинари замыкают круг: пин тулчейна, -trimpath, верифицированные модули — одинаковые входы, одинаковые байты. Теперь, когда вы увидите checksum mismatch в CI или коллегу, удаляющего go.sum «чтобы починить», вы будете точно знать, что сообщение об ошибке говорило правду — и что правильный ответ это один go get с новой версией, а не удаление журнала.
</Recap>
