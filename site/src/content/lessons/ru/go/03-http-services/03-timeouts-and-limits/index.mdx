---
concepts:
- go-http-timeouts
deepensInto: []
estMin: 19
lang: ru
lessonType: topic
level: senior
order: 3
prereqs: []
slug: "03-timeouts-and-limits"
sources:
- https://pkg.go.dev/net/http
- https://pkg.go.dev/context
spiral: []
status: ready
summary: 'У http.Server нулевые таймауты: ReadHeader, Read, Write и Idle по умолчанию бесконечны, и один slowloris-клиент исчерпывает соединения. Задайте их, добавьте контекстные дедлайны на запрос, настройте таймауты Transport у исходящих клиентов и ограничьте тело через MaxBytesReader.'
title: 'Таймауты и лимиты: нулевые значения, которые держат соединения вечно'
track: "go"
unit: "03-http-services"
---

import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";
import Infographic from "~/components/diagram/Infographic.astro";
import timeoutBudget from "./infographics/timeout-budget.svg?raw";

<Hook>
Отчёт об аварии читался как загадка: API умер, но CPU на 4%, память ровная, база простаивает. Исчерпались файловые дескрипторы — все 65 535, почти все в ESTABLISHED, почти все принадлежали нескольким сотням IP, каждый из которых открыл тысячи соединений и слал по одной строке HTTP-заголовка в минуту. Хрестоматийный slowloris (атака, намеренно растягивающая отправку заголовков, чтобы удерживать соединение бесконечно). Сервер был `http.ListenAndServe(addr, mux)` — а это конструирует `http.Server` со всеми полями таймаутов в нулевом значении, и в Go ноль означает *никакого таймаута вообще*. Каждое цедящее байты соединение бессрочно держало горутину и FD, accept-цикл продолжал принимать, пока ядро не сказало «нет», а легитимные клиенты получали connection refused. Фикс занял восемь строк: сконфигурированный Server с ReadHeaderTimeout и компанией. Самая острая строка постмортема: никто не принимал решения, что эти таймауты должны быть бесконечными, — нулевое значение решило за всех, двумя годами раньше, в первый день.
</Hook>

<Crux>Все HTTP-таймауты в Go по умолчанию бесконечны. Без таймаутов Server и Transport один медленный пир держит соединения вечно.</Crux>

<Explanation>

## Таймауты Server, которые никто не задаёт

Почему API умирает при 4% CPU и простаивающей базе? Почти наверняка потому, что нулевое значение приняло решение за тебя — а в Go нулевой таймаут означает бесконечный. Вот что нужно задать вместо него.

`http.ListenAndServe(addr, h)` враждебен продакшену ровно по одной причине: на нём нельзя задать таймауты. Четыре важных поля живут на `http.Server`, и каждое сторожит свою фазу соединения:

```go
srv := &http.Server{
	Addr:              ":8080",
	Handler:           mux,
	ReadHeaderTimeout: 5 * time.Second,   // строка запроса + заголовки
	ReadTimeout:       10 * time.Second,  // заголовки И всё тело
	WriteTimeout:      30 * time.Second,  // от конца заголовков до последнего байта ответа
	IdleTimeout:       120 * time.Second, // пауза keep-alive между запросами
}
log.Fatal(srv.ListenAndServe())
```

- **ReadHeaderTimeout** — убийца slowloris: пир обязан доставить весь блок заголовков за это время. Можно ставить жёстко (2–5 с): честные клиенты шлют заголовки одним пакетом.
- **ReadTimeout** покрывает заголовки *плюс всё тело*. Жёсткое значение ломает легитимные большие загрузки — поэтому многие сервисы ставят маленький ReadHeaderTimeout и щедрый ReadTimeout либо управляют дедлайнами тела по маршрутам.
- **WriteTimeout** ограничивает запись ответа. Жестокая деталь: дедлайн абсолютный с момента окончания чтения заголовков, а не на каждую запись — стриминговый эндпоинт, который шлёт события дольше WriteTimeout, получит закрытое соединение посреди стрима, каким бы живым он ни был.
- **IdleTimeout** ограничивает простой keep-alive-соединения между запросами; если не задан — берётся ReadTimeout, а если и тот ноль, простаивающие соединения живут вечно. Конвенция — 60–120 с.

Ещё одна честность: **WriteTimeout не останавливает хендлер.** Когда он срабатывает, net/http закрывает соединение; горутина хендлера продолжает считать до конца, и её записи проваливаются. Таймауты Server защищают *соединения и FD*, не CPU.

<Infographic
  svg={timeoutBudget}
  label="Горизонтальная столбчатая диаграмма четырёх полей таймаутов http.Server в секундах: ReadHeaderTimeout 5, ReadTimeout 10, WriteTimeout 30, IdleTimeout 120 — бюджет растёт от самого жёсткого на фазе заголовков до самого щедрого для простоя keep-alive."
  caption="Все четыре поля по умолчанию равны нулю (бесконечность). Поставьте окно заголовков жёстко, чтобы убить slowloris, затем расширяйте бюджет по фазам тела, ответа и простоя."
  hue="var(--d-infra)"
/>

<Quiz
  id="03-timeouts-and-limits-quiz-1"
  lessonSlug="03-timeouts-and-limits"
  lang="ru"
  question="Сервер ставит WriteTimeout: 10s. Хендлер стримит длинный отчёт, записывая чанк каждую секунду. На t=10s соединение умирает. Хендлер не простаивал ни секунды — почему сработал таймаут?"
  choices={[
    { label: "WriteTimeout — абсолютный дедлайн от конца чтения заголовков, а не таймаут бездействия: стабильная запись его не продлевает", correct: true },
    { label: "Хендлер забыл вызвать Flush, и записанное не засчитывалось", misconception: "Flush влияет на то, когда байты дойдут до клиента, а не на дедлайн. WriteTimeout ставит один фиксированный дедлайн на соединение для всего ответа; даже идеально флашащиеся чанки умирают, когда абсолютный дедлайн проходит." },
    { label: "WriteTimeout сбрасывается на каждом Write — значит, какой-то чанк шёл дольше 10 секунд", misconception: "Это модель бездействия, которую многие ожидают — и которую можно собрать через SetWriteDeadline на каждую запись через ResponseController, — но поле Server задаёт один абсолютный дедлайн на всю фазу ответа." },
    { label: "Клиент перестал читать, и TCP-backpressure застопорил записи", misconception: "Backpressure может стопорить записи, но в сценарии соединение умерло ровно на сконфигурированных 10 секундах при здоровых чанках раз в секунду — подпись абсолютного дедлайна, а не медленного читателя." },
  ]}
/>

## Дедлайны на запрос: контекст и что на самом деле отменяется

Таймауты сервера — сантехника соединений; *работа* ограничивается на каждый запрос, контекстом:

```go
func handler(w http.ResponseWriter, r *http.Request) {
	ctx, cancel := context.WithTimeout(r.Context(), 2*time.Second)
	defer cancel()

	row := db.QueryRowContext(ctx, query, id) // уважает дедлайн
	// r.Context() отменяется и при отключении КЛИЕНТА —
	// работа вниз по стеку останавливается, когда ответа никто не ждёт.
}
```

`r.Context()` отменяется, когда клиент уходит или соединение рвётся, поэтому прокидывание его в каждый вызов базы и каждый запрос вниз — бесплатный сброс нагрузки: работа по брошенному запросу останавливается, а не завершается в пустоту. Сверху накладывайте свой `WithTimeout` для бюджета на маршрут. Дисциплина, делающая это реальным: ctx обязан принимать *каждый* блокирующий вызов в хендлере — один `QueryRow` вместо `QueryRowContext`, и в вашем двухсекундном бюджете дыра. Stdlib также предлагает `http.TimeoutHandler(h, 2*time.Second, "timeout")`, возвращающий 503 по дедлайну; честная оговорка — как и WriteTimeout, он не умеет убить горутину хендлера, лишь перестаёт её ждать (а его ResponseWriter прячет Flusher и ломает стриминг внутри).

## Клиентская сторона: дефолтный http.Client ждёт вечно

Та же ловушка нулевых значений существует на исходящих, и она хуже, потому что `http.DefaultClient` так удобен. У него **нет никакого таймаута**: зависший апстрим держит вашу горутину, её FD и захваченную память запроса вечно. Продакшен-конфигурация задаёт бюджеты на двух уровнях:

```go
client := &http.Client{
	Timeout: 10 * time.Second, // жёсткий потолок: dial + TLS + заголовки + ЧТЕНИЕ ВСЕГО ТЕЛА
	Transport: &http.Transport{
		DialContext:           (&net.Dialer{Timeout: 2 * time.Second}).DialContext,
		TLSHandshakeTimeout:   3 * time.Second,
		ResponseHeaderTimeout: 5 * time.Second,  // «время на подумать» апстрима
		IdleConnTimeout:       90 * time.Second, // жизнь соединения в пуле
		MaxIdleConnsPerHost:   32,               // дефолт 2 душит fan-out
	},
}
```

`Client.Timeout` — грубый инструмент: он накрывает всё, включая чтение тела, и потому должен превышать ваш самый медленный легитимный полный обмен. Поля Transport режут тоньше: dial, TLS-рукопожатие и время до первого байта заголовков — ровно те места, где зависшие апстримы на самом деле виснут. Два дефолта больно кусают в проде: `MaxIdleConnsPerHost` равен **2**, так что сервис с fan-out (веерные параллельные вызовы к одному хосту) в 200 одновременных запросов молотит 198 свежих соединений (TLS-рукопожатия, нарастающий TIME_WAIT); а дедлайны на запрос по-прежнему живут в контексте — `http.NewRequestWithContext(ctx, ...)`, — чтобы бюджет вызывающего распространялся, а не каждый хоп ждал свои полные 10 секунд.

<FlowDiagram
  label="Исходящее зеркало: у каждой фазы вызова http.Client свой таймаут Transport, а Client.Timeout — жёсткий потолок над всем обменом"
  hue="var(--d-backend)"
  perRow={3}
  nodes={[
    { id: "dial", label: "Dial", sub: "Dialer.Timeout 2 с" },
    { id: "tls", label: "TLS-рукопожатие", sub: "TLSHandshakeTimeout 3 с" },
    { id: "hdr", label: "Первый байт заголовков", sub: "ResponseHeaderTimeout 5 с" },
    { id: "body", label: "Чтение тела", sub: "Client.Timeout накрывает всё" },
    { id: "pool", label: "Пул простоя", sub: "IdleConnTimeout 90 с" },
    { id: "reuse", label: "Реюз или новый dial", sub: "MaxIdleConnsPerHost 32" },
  ]}
  edges={[
    { from: "dial", to: "tls", label: "TCP открыт" },
    { from: "tls", to: "hdr", label: "запрос ушёл" },
    { from: "hdr", to: "body", label: "заголовки пришли" },
    { from: "body", to: "pool", label: "соединение свободно" },
    { from: "pool", to: "reuse", label: "следующий вызов" },
  ]}
  caption="http.DefaultClient не задаёт ничего из этого — апстрим, принявший TCP и замолчавший, паркует горутину навсегда. А MaxIdleConnsPerHost по умолчанию 2: поднимите его, иначе большой fan-out к одному хосту молотит свежие TLS-рукопожатия вместо реюза пула."
/>

## MaxBytesReader: лимит на то, что вы готовы проглотить

Таймауты ограничивают *время*; нужно ограничивать и *байты*. Неаутентифицированный `io.ReadAll(r.Body)` — приглашение: тело JSON на 2 ГБ — это аллокация на 2 ГБ, и несколько одновременных кладут под по OOM. Ответ stdlib:

```go
func createUser(w http.ResponseWriter, r *http.Request) {
	r.Body = http.MaxBytesReader(w, r.Body, 1<<20) // потолок 1 МиБ
	if err := json.NewDecoder(r.Body).Decode(&u); err != nil {
		var mbe *http.MaxBytesError
		if errors.As(err, &mbe) {
			http.Error(w, "body too large", http.StatusRequestEntityTooLarge)
			return // 413, и сервер перестаёт читать остальное
		}
		http.Error(w, "bad json", http.StatusBadRequest)
		return
	}
}
```

`MaxBytesReader` делает больше, чем обрезка: при переполнении он возвращает `*http.MaxBytesError` и сообщает серверу прекратить чтение соединения, чтобы враждебный клиент не мог продолжать качать. Ставьте его по маршрутам — 1 МиБ для JSON-API, больше только на эндпоинтах загрузки — и помните: `Content-Length` — это заявление, а не гарантия; reader контролирует фактические байты.

<Quiz
  id="03-timeouts-and-limits-quiz-2"
  lessonSlug="03-timeouts-and-limits"
  lang="ru"
  question="Сервис ходит к апстриму через http.DefaultClient. Апстрим принимает TCP-соединения, но никогда не присылает заголовки ответа. Что происходит с каждой вызывающей горутиной?"
  choices={[
    { label: "Она блокируется бессрочно — у DefaultClient нет Timeout, а дефолтный Transport не задаёт ResponseHeaderTimeout, так что не сработает ничто и никогда", correct: true },
    { label: "Она упадёт примерно через 30 секунд, когда ОС сдастся на соединении", misconception: "Таймаут connect в ОС сторожит только установку соединения, а этот апстрим dial принимает исправно. После установления молчащий пир не вызывает никакой ошибки уровня ядра — только дедлайны, которые задали вы." },
    { label: "Рантайм Go заметит застрявшее сетевое чтение и отменит его, когда GC обратит внимание", misconception: "Рантайм никогда не отменяет заблокированный I/O за вас — горутина, припаркованная на сетевом чтении, для планировщика совершенно нормальна. Отмена в Go всегда явная: дедлайн, поле таймаута или контекст." },
    { label: "Она блокируется до таймаута контекста запроса, который DefaultClient ставит в 30 секунд", misconception: "DefaultClient не ставит на ваш запрос никакого контекста — контексты приходят от вызывающего через NewRequestWithContext. Запрос из http.NewRequest несёт context.Background, который не истекает никогда." },
  ]}
/>

<Inset kind="why" lang="ru">
Почему Go по умолчанию делает все таймауты бесконечными, вместо разумных дефолтов? Потому что stdlib не может угадать ваш трафик: дефолтный ReadTimeout в 5 секунд сломал бы загрузку файлов, ResponseHeaderTimeout в 30 секунд сломал бы long-polling, а молча меняющиеся между версиями Go дефолты стали бы кошмаром совместимости при обещании Go 1. Ноль-как-бесконечность последовательна и предсказуема — но перекладывает обязанность на вас, и дешевле всего исполнить её одним конструктором newServer() / newClient() в кодовой базе, который никто не обходит: отревьюен один раз, используется везде.
</Inset>

</Explanation>

<FlowDiagram
  label="Каждую фазу соединения сторожит свой таймаут — каждое незаданное поле есть фаза, где медленный пир может припарковаться навсегда"
  hue="var(--d-backend)"
  perRow={3}
  nodes={[
    { id: "accept", label: "Accept", sub: "FD + горутина" },
    { id: "hdr", label: "Чтение заголовков", sub: "ReadHeaderTimeout" },
    { id: "body", label: "Чтение тела", sub: "ReadTimeout + MaxBytes" },
    { id: "handle", label: "Хендлер", sub: "дедлайн ctx" },
    { id: "write", label: "Запись ответа", sub: "WriteTimeout (абсолютный)" },
    { id: "idle", label: "Простой keep-alive", sub: "IdleTimeout" },
  ]}
  edges={[
    { from: "accept", to: "hdr", label: "риск slowloris" },
    { from: "hdr", to: "body", label: "заголовки готовы" },
    { from: "body", to: "handle", label: "запрос собран" },
    { from: "handle", to: "write", label: "ответ" },
    { from: "write", to: "idle", label: "переиспользование" },
  ]}
  caption="Исходящие зеркалят входящие: таймаут Dialer, TLSHandshakeTimeout, ResponseHeaderTimeout, IdleConnTimeout на Transport, а Client.Timeout — жёсткий потолок на весь обмен, включая тело."
/>

<KeyTakeaway>Нулевой таймаут в net/http означает бесконечный — по обе стороны провода. Продакшен-серверы задают ReadHeaderTimeout (slowloris), ReadTimeout, WriteTimeout (абсолютный, убивает стримы) и IdleTimeout; хендлеры ограничивают работу контекстными дедлайнами, которые обязан принимать каждый блокирующий вызов; клиенты ставят Client.Timeout плюс бюджеты Transport на dial/TLS/заголовки и поднимают MaxIdleConnsPerHost с дефолтных 2. MaxBytesReader ограничивает тело и даёт 413. Ничего из этого не сработает, пока не записано в коде.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="03-timeouts-and-limits-retrieval"
  lang="ru"
  questions={[
    { q: "Назови четыре поля таймаутов http.Server, какую фазу сторожит каждое, и острый край WriteTimeout.", a: "ReadHeaderTimeout ограничивает доставку строки запроса и заголовков — защита от slowloris, безопасно ставить жёстко, 2–5 с. ReadTimeout охватывает заголовки плюс всё тело, поэтому обязан вмещать самую большую легитимную загрузку. WriteTimeout ограничивает фазу ответа одним абсолютным дедлайном от конца чтения заголовков — не бездействием, — так что здоровый стрим с чанком в секунду всё равно умирает на дедлайне; при срабатывании горутина хендлера не останавливается, закрывается только соединение. IdleTimeout ограничивает паузу keep-alive между запросами и при отсутствии берёт значение ReadTimeout. Все четыре по умолчанию ноль, то есть бесконечность, — а http.ListenAndServe не даёт их задать, поэтому продакшен-код конструирует http.Server сам." },
    { q: "Исходящий вызов должен уложиться в бюджет 2 секунды от и до. Где ты его обеспечиваешь и какие дефолты тебя предадут?", a: "Собрать запрос через http.NewRequestWithContext с контекстом, несущим двухсекундный дедлайн, — он распространяет бюджет вызывающего на dial, запись и чтение и отменяет попытку в момент истечения. Ниже — настроить Transport, чтобы отдельные фазы падали быстро и без контекста: Dialer.Timeout на connect, TLSHandshakeTimeout, ResponseHeaderTimeout на «время на подумать» апстрима, IdleConnTimeout для гигиены пула; Client.Timeout оставить страховкой на весь обмен, включая чтение тела. Предающие дефолты: у http.DefaultClient нет таймаута вовсе, дефолтный Transport не задаёт ResponseHeaderTimeout — апстрим, принявший TCP и замолчавший, паркует вашу горутину навсегда; а MaxIdleConnsPerHost по умолчанию 2, так что высокий fan-out к одному хосту молотит свежие TLS-рукопожатия вместо переиспользования пула." },
  ]}
/>

<Recap lang="ru">
HTTP-стек Go трактует нулевой таймаут как отсутствие таймаута, и http.ListenAndServe не даёт это изменить — продакшен-сервисы конструируют http.Server с четырьмя полями, выбранными осознанно. ReadHeaderTimeout закрывает дыру slowloris, ограничивая доставку заголовков; ReadTimeout покрывает всё тело и должен вмещать легитимные загрузки; WriteTimeout — один абсолютный дедлайн на фазу ответа, убивающий даже здоровые стримы и никогда не останавливающий горутину хендлера, только соединение; IdleTimeout пожинает простаивающие keep-alive-соединения и молча наследует ReadTimeout, если не задан. Внутри хендлера время принадлежит контексту: r.Context() отменяется при уходе клиента, WithTimeout на маршрут задаёт бюджет работы, и бюджет держится, только если каждый блокирующий вызов принимает ctx — один запрос без контекста, и в нём дыра. http.TimeoutHandler возвращает 503 по дедлайну, но не умеет убить горутину за ним. Исходящие — та же ловушка в зеркале: http.DefaultClient ждёт вечно, поэтому настоящие клиенты ставят Client.Timeout как потолок на весь обмен и бюджеты Transport на dial, TLS-рукопожатие и заголовки ответа, поднимают MaxIdleConnsPerHost выше душащих fan-out дефолтных 2 и передают контексты на запрос, чтобы дедлайны распространялись. Байтам тоже нужны границы: MaxBytesReader ограничивает тело, возвращает MaxBytesError для чистого 413 и прекращает чтение враждебного потока. Теперь, когда видишь аварию с пустым CPU и молчащей базой, первый вопрос: какое поле таймаута по-прежнему равно нулю?
</Recap>
