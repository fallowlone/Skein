---
concepts:
- go-pipelines
deepensInto: []
estMin: 18
lang: ru
lessonType: topic
level: senior
order: 2
prereqs: []
slug: "02-pipelines-under-cancellation"
sources:
- https://go.dev/blog/pipelines
- https://pkg.go.dev/context
- https://pkg.go.dev/golang.org/x/sync/errgroup
spiral: []
status: ready
summary: 'Стадии пайплайна связаны каналами; каждый send обязан идти через select с ctx.Done, иначе горутина течёт, едва потребитель перестал читать. Закрывает канал только отправитель; fan-in закрывает после WaitGroup. Буферы гасят джиттер, а не дисбаланс.'
title: 'Пайплайны под отменой: защищённые send, дисциплина закрытия и каноничная утечка горутин'
track: "go"
unit: "10-advanced-concurrency"
---

import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DiagramFrame from "~/components/diagram/DiagramFrame.astro";
import PacketDot from "~/components/diagram/PacketDot.astro";
import Infographic from "~/components/diagram/Infographic.astro";
import bufferThroughput from "./infographics/buffer-throughput.svg?raw";

<Hook>
Ночной батч сверки работал 14 месяцев. Потом кто-то добавил шаг валидации в последнюю стадию, и на плохом входе она сделала естественное: залогировала и вернулась. Одна битая запись в 02:13, горутина-потребитель выходит — а три стадии выше по течению этого не замечают, им и не положено. Они продолжают производить, пока каждая не заблокируется на отправке в канал, которую никто никогда не примет. За один прогон так замерзало около 40 000 горутин посреди send: ~160 МБ стеков плюс все распарсенные записи, которые они держали. Батч «завершался» (планировщик видел возврат главной горутины), просто память не возвращалась. Через восемь ночей под начал ловить OOMKill в 03:00, и goroutine-профиль читался как чистосердечное признание: один стек, `chan send`, счётчик 312 887. Паттерн пайплайнов — из поста в блоге Go 2014 года; утечка — из забытого единственного правила, о котором этот пост на самом деле и написан: отправка, которую нельзя бросить, — это горутина, которая не может выйти.
</Hook>

<Crux>Каждый send в пайплайне — в select с ctx.Done: неприкрытая отправка течёт горутиной, как только потребитель перестаёт читать.</Crux>

<Explanation>

## Стадии и каналы — форма

Пайплайн — это горутины, связанные каналами: каждая *стадия* принимает значения сверху, преобразует и отправляет вниз. Версия из блога 2014 года использовала канал `done` на пайплайн; современная форма — `context`:

```go
func parse(ctx context.Context, in <-chan []byte) <-chan Record {
	out := make(chan Record)
	go func() {
		defer close(out) // стадия владеет out — закрывает только отправитель
		for raw := range in {
			rec, err := decode(raw)
			if err != nil {
				continue // или: сообщить в канал ошибок, см. ниже
			}
			select {
			case out <- rec:
			case <-ctx.Done():
				return // бросаем отправку; defer закроет out
			}
		}
	}()
	return out
}
```

Композируемость дают два структурных факта. Стадия *владеет* своим выходным каналом: она его создаёт, только она отправляет, и `defer close(out)` сигнализирует вниз «значений больше не будет» — именно это позволяет следующей стадии использовать простой `for range`. И функция сразу возвращает канал только-для-чтения; пайплайн собран до того, как потекли данные.

## Каноничная утечка: неприкрытый send

Уберите `select` — стадия станет читаться чище, `out <- rec`, — и станет неправильной. Отправка в канал блокируется, пока получатель не готов. В момент, когда нижняя стадия перестала читать — упала, вернулась раньше на плохом входе, как в Hook, или просто решила, что ей хватит, — эта отправка блокируется **навсегда**. Горутина припаркована на `chan send`, GC её не тронет (припаркованная горутина — корень, пришпиливающий её стек и каждую запись, на которую он ссылается), и нигде не всплывает никакая ошибка. Это самая частая реальная утечка горутин, и правило против неё механическое: **каждая отправка в канал пайплайна сидит внутри `select` с веткой `ctx.Done()`**. Приём через `for range` безопасен лишь потому, что `defer close` отправителя гарантирован — если стадия может выйти, не закрыв выход, нижние range текут точно так же.

<Quiz
  id="02-pipelines-under-cancellation-quiz-1"
  lessonSlug="02-pipelines-under-cancellation"
  lang="ru"
  question="В трёхстадийном пайплайне отправки не защищены (голая отправка в out, без select). Конечный потребитель натыкается на битую запись и возвращается раньше, ничего не отменяя. Каково установившееся состояние?"
  choices={[
    { label: "Каждая верхняя стадия рано или поздно навсегда блокируется на своей отправке; горутины и всё, на что ссылаются их стеки, живут до смерти процесса", correct: true },
    { label: "Верхние стадии выйдут, когда дочитают свои входные каналы", misconception: "Дочитать им не суждено — они застряли посреди send. Заблокированная отправка не возвращается в цикл за новым входом; стадия замирает ровно там, где была, когда исчез потребитель, держа значение, которое не смогла доставить." },
    { label: "Рантайм заметит заблокированные отправки и запаникует с ошибкой дедлока", misconception: "Паника all-goroutines-asleep срабатывает, только когда заблокирована каждая горутина процесса. Главная горутина вернулась, остальная программа живёт дальше, и рантайм не видит ничего плохого — потому эта утечка достаточно тиха, чтобы пережить 14 месяцев." },
    { label: "GC соберёт заблокированные горутины, раз на их каналы никто не ссылается", misconception: "Всё наоборот: припаркованная горутина — корень GC. Она ссылается на канал и собственный стек, так что горутина, канал и записи в полёте остаются достижимыми. GC освобождает память, но никогда — горутины." },
  ]}
/>

## Дисциплина закрытия и fan-in

Когда вы впервые собираете fan-in, вопрос «кто закрывает объединённый канал?» легко решить неправильно: каждый отправитель кажется кандидатом, но любой из них, закрыв канал раньше времени, вызовет панику у остальных. У правила закрытия нет исключений: **канал закрывает только отправитель**, потому что отправка в закрытый канал паникует и повторное закрытие паникует — получатель в принципе не может знать, что закрывать безопасно. Стадии с одним отправителем получают это бесплатно через `defer close(out)`. Fan-in — N воркеров, шлющих в один общий канал, — место, где дисциплина обретает форму: закрывать нельзя, пока не закончили *все* отправители, поэтому их считают:

```go
func merge(ctx context.Context, ins ...<-chan Record) <-chan Record {
	out := make(chan Record)
	var wg sync.WaitGroup
	for _, in := range ins {
		wg.Add(1)
		go func() { // по форвардеру на вход
			defer wg.Done()
			for rec := range in {
				select {
				case out <- rec:
				case <-ctx.Done():
					return
				}
			}
		}()
	}
	go func() { wg.Wait(); close(out) }() // WaitGroup, затем close — в отдельной горутине
	return out
}
```

Пара `wg.Wait(); close(out)` живёт в отдельной горутине, чтобы `merge` вернулся сразу — закрытие случается ровно один раз, ровно после выхода последнего форвардера, чем бы этот выход ни был: «вход дочитан» или «ctx отменён посреди send».

## Осушать или бросать — кто кого разблокирует при отмене

Отмена разбирает пайплайн с обоих концов, и стоит знать, на какой механизм вы полагаетесь. **Бросание** — дефолт выше: каждый отправитель защищает свой send через `ctx.Done`, и при отмене каждая стадия разблокирует *себя сама* и возвращается; значения в полёте теряются. **Осушение** — альтернатива: потребитель продолжает `for range` до закрытия канала, верхние стадии завершаются естественно и ничего не теряется — ценой обработки (или хотя бы приёма) всего уже произведённого. Осушение без защищённых отправок — способ убедить себя, что защита необязательна; оно держится, лишь пока дисциплинирован *каждый* потребитель, что и сломал один ранний `return` из Hook. Надёжная стойка: защищайте каждый send в любом случае, а осушение выбирайте, только когда данные нельзя терять (и тогда сам цикл осушения нуждается в границе — счётчик или дедлайн, — иначе медленный производитель превращает остановку в зависание).

<Inset kind="why" lang="ru">
Почему утечка указывает на отправителя, а не на ушедшего получателя? Потому что в каналах CSP-стиля отправка — это *обязательство*: отправитель посреди send не умеет ни таймаутиться, ни проверить флаг, ни попасть под сборку мусора — его единственные выходы: пришедший получатель или сработавшая другая ветка select. Получатель, вернувшийся раньше, не совершает преступления, видимого системе типов; он просто отнимает будущее, которого ждал отправитель. Эта асимметрия — весь аргумент за идиому защищённого send: спасти отправителя может только он сам, поэтому запасной выход должен быть вкомпилирован в место отправки.
</Inset>

## Буферы: что они покупают, честно

Сделать каналы стадий буферизованными — `make(chan Record, 1024)` — выглядит как фикс пропускной способности. Будьте точны в том, что это делает. Буфер поглощает *джиттер*: короткие всплески, паузы GC, на миг замедлившегося потребителя; производители движутся дальше вместо передачи в ногу, и латентность небуферизованного рандеву (~сотни наносекунд на пару send-receive) перестаёт доминировать на мелких элементах. Чего буфер **не** чинит — *дисбаланс*: если стадия производит 12 тыс./с, а следующая потребляет 9 тыс./с, буфер на 1024 слота полон через ~340 мс, и пайплайн идёт ровно на 9 тыс./с — как небуферизованный, плюс 1024 записи лишней памяти и устаревания. Установившаяся пропускная способность — это самая медленная стадия, точка. Буферы ещё и оттягивают симптомы утечки: неприкрытый send в большой буфер отказывает, только когда буфер заполнится, нередко через минуты после смерти потребителя — баг из 02:13 всплывает в 02:47 со стеком, который уже не указывает на причину.

<Infographic
  svg={bufferThroughput}
  label="Столбчатая диаграмма пропускной способности в тысячах записей в секунду: стадия-производитель идёт на 12 тыс./с, но стадия-потребитель, пайплайн с буфером и пайплайн без буфера — все на 9 тыс./с."
  caption="Буфер на 1024 слота ничего не меняет в установившемся режиме: и с буфером, и без него пайплайн оседает на 9 тыс./с потребителя. Пропускную способность задаёт самая медленная стадия; буфер гасит лишь джиттер, а не постоянный разрыв скоростей."
  hue="var(--d-infra)"
/>

<Quiz
  id="02-pipelines-under-cancellation-quiz-2"
  lessonSlug="02-pipelines-under-cancellation"
  lang="ru"
  question="Стадия производит 12 тыс. записей/с; следующая часами потребляет 9 тыс./с. Инженер вставляет между ними буфер на 1024 слота. Что изменится?"
  choices={[
    { label: "Около 340 мс развязки, затем буфер навсегда полон и пайплайн идёт на 9 тыс./с — буфер не чинит постоянный дисбаланс скоростей", correct: true },
    { label: "Пропускная способность вырастет к 12 тыс./с, потому что производитель больше не ждёт потребителя", misconception: "Производитель не ждёт, лишь пока в буфере есть место. При постоянном дефиците в 3 тыс./с буфер прибавляет 3 000 записей каждую секунду и полон примерно за треть секунды; дальше каждая отправка блокируется до освобождения слота — снова в ногу, на скорости потребителя." },
    { label: "Буфер сгладит скорости, и обе стадии устаканятся около 10.5 тыс./с", misconception: "Буфер канала — очередь, а не балансировщик: он не делает потребителя быстрее, а производителя в среднем медленнее. Он лишь временно хранит разницу, а конечное хранилище бесконечной разницы обязано заполниться." },
    { label: "Не изменится вообще ничего, значит буферы в пайплайнах бессмысленны", misconception: "С джиттером буферы честно помогают: всплески, паузы GC, накладные рандеву на мелких элементах. Чего они не могут — починить постоянный разрыв пропускной способности: тут нужна более быстрая стадия, больше параллельных воркеров на медленной стадии или сброс нагрузки." },
  ]}
/>

## Ошибки и наблюдаемость

Стадии, которая может отказать, нужен путь для ошибок. Два рабочих дизайна. *Канал ошибок на стадию*: стадия шлёт ошибки в `errc`, оркестратор делает select по нему — явно, но каждый потребитель обязан осушать `errc`, иначе вы построили вторую утечку. *Стадии под errgroup* — обычно лучший дефолт: каждая стадия бежит как `g.Go`, возвращает свою ошибку, а групповой ctx отменяет защищённые отправки всех остальных; `g.Wait` отдаёт первый отказ после того, как весь пайплайн размотался. Для видимости инструментируйте сами каналы: периодический gauge `len(ch)` против `cap(ch)` по стадиям показывает, где сидит очередь — буфер, прибитый к ёмкости, метит стадию-бутылочное-горлышко, буфер, прибитый к нулю, метит голодание выше по течению, а растущий `runtime.NumGoroutine()` при ровном трафике — сигнал утечки из прошлого урока. Дешёвые числа, превращающие «батч тормозит» в «стена — стадия 3».

</Explanation>

<DiagramFrame label="Записи текут сквозь стадии; каждый переход — защищённая отправка: когда ctx умирает, каждая стадия бросает свой send, и точки останавливаются, а не копятся" hue="var(--d-backend)" caption="Каждая стадия владеет своим выходом и закрывает его; fan-in считает отправителей WaitGroup-ом перед закрытием. Неприкрытый send на любом переходе — место, где замёрзли 40 тысяч горутин из Hook.">
  <svg viewBox="0 0 460 150" width="460" height="150" style="display:block;margin:0 auto;max-width:100%;height:auto;">
    <rect x="10" y="48" width="80" height="40" rx="6" fill="var(--card-2)" stroke="var(--hairline-strong)" stroke-width="0.8" />
    <text x="50" y="65" text-anchor="middle" font-size="11" font-family="var(--font-body)" fill="var(--ink)">gen</text>
    <text x="50" y="79" text-anchor="middle" font-size="9" font-family="var(--font-mono)" fill="var(--muted)">читает источник</text>
    <rect x="130" y="48" width="90" height="40" rx="6" fill="var(--card-2)" stroke="var(--hairline-strong)" stroke-width="0.8" />
    <text x="175" y="65" text-anchor="middle" font-size="11" font-family="var(--font-body)" fill="var(--ink)">parse</text>
    <text x="175" y="79" text-anchor="middle" font-size="9" font-family="var(--font-mono)" fill="var(--muted)">select + ctx</text>
    <rect x="250" y="48" width="90" height="40" rx="6" fill="var(--card-2)" stroke="var(--hairline-strong)" stroke-width="0.8" />
    <text x="295" y="65" text-anchor="middle" font-size="11" font-family="var(--font-body)" fill="var(--ink)">enrich</text>
    <text x="295" y="79" text-anchor="middle" font-size="9" font-family="var(--font-mono)" fill="var(--muted)">select + ctx</text>
    <rect x="370" y="48" width="80" height="40" rx="6" fill="var(--card-2)" stroke="var(--hairline-strong)" stroke-width="0.8" />
    <text x="410" y="65" text-anchor="middle" font-size="11" font-family="var(--font-body)" fill="var(--ink)">sink</text>
    <text x="410" y="79" text-anchor="middle" font-size="9" font-family="var(--font-mono)" fill="var(--muted)">потребитель</text>
    <path data-path d="M90 68 L370 68" fill="none" stroke="var(--hairline-2)" stroke-width="1.4" stroke-dasharray="4 4" />
    <PacketDot color="var(--d-backend)" durationMs={1600} size={8} />
    <PacketDot color="var(--d-backend)" durationMs={1600} delayMs={400} size={8} />
    <PacketDot color="var(--d-backend)" durationMs={1600} delayMs={800} size={8} />
    <text x="230" y="22" text-anchor="middle" font-size="9.5" font-family="var(--font-mono)" fill="var(--muted)">каждый send: select с ctx.Done — иначе стадия замерзает посреди отправки</text>
    <text x="230" y="128" text-anchor="middle" font-size="9.5" font-family="var(--font-mono)" fill="var(--muted)">закрывает только отправитель · fan-in: WaitGroup, затем close</text>
  </svg>
</DiagramFrame>

<KeyTakeaway>Пайплайн переживает отмену, только если каждая отправка — select с ctx.Done: неприкрытый send замораживает горутину, едва низ перестал читать, и GC хранит труп. Стадии владеют выходами и закрывают их; fan-in закрывает после того, как WaitGroup отсчитал отправителей. Буферы гасят джиттер, но не постоянный дисбаланс; пропускную способность задаёт самая медленная стадия. Оборачивайте стадии в errgroup ради одного пути ошибок и снимайте глубины каналов.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="02-pipelines-under-cancellation-retrieval"
  lang="ru"
  questions={[
    { q: "Объясни каноничную утечку пайплайна: механизм, почему молчат и рантайм, и GC, и идиому, которая её предотвращает.", a: "Отправка в канал блокируется, пока получатель не заберёт значение. Когда нижняя стадия выходит раньше — падение, return на валидации, баг — каждая верхняя горутина рано или поздно навсегда блокируется внутри своего send. Паника рантайма про дедлок молчит, потому что срабатывает, лишь когда спят ВСЕ горутины, а остальной процесс живёт; GC молчит, потому что припаркованная горутина — сама по себе корень GC, удерживающий её стек, канал и значения в полёте. Предотвращение — защищённая отправка: каждый send в канал пайплайна записывается как select с двумя ветками — отправкой и возвратом по ctx.Done. Тогда отмена позволяет каждому отправителю бросить свою отправку и выйти через свой defer close. Приём через for range безопасен лишь потому, что отправитель гарантирует close; стадия, способная выйти, не закрыв выход, воссоздаёт ту же утечку на переход ниже." },
    { q: "Сформулируй дисциплину закрытия, как её реализует fan-in, и выбор осушать-или-бросать при отмене.", a: "Канал закрывает только отправитель — получатель не может доказать безопасность закрытия, а отправка в закрытый канал и повторное закрытие паникуют. Стадия с одним отправителем закрывает через defer close(out). У fan-in N отправителей в один канал, и ни один из них закрывать не вправе; вместо этого каждый форвардер отмечается в WaitGroup через defer wg.Done, а отдельная горутина выполняет wg.Wait и затем close(out) — ровно одно закрытие, после выхода последнего отправителя. При отмене два режима разборки: бросание (дефолт) — каждая защищённая отправка разблокирует себя через ctx.Done, значения в полёте теряются; осушение — потребитель принимает до закрытия, верх завершается естественно и ничего не теряется, ценой приёма всего уже произведённого, и ему нужна граница, иначе остановка зависает. Защищайте каждый send в любом случае: осушение — политика, защищённые отправки — страховочный пол." },
  ]}
/>

<Recap lang="ru">
Паттерн пайплайна — стадии-горутины, связанные каналами, и в современной форме сквозь каждую стадию продет context. Композирующая структура: стадия создаёт и владеет своим выходным каналом, отправляет в него и гарантирует defer close(out), чтобы низ мог for-range; функция возвращает канал сразу, данные текут после сборки. Правило, сохраняющее жизнь под отказом: каждая отправка — select с веткой ctx.Done, потому что неприкрытый send блокируется навсегда, едва низ перестал читать — горутина паркуется на chan send, GC считает её корнем, пришпиливая стек и записи в полёте, рантайм не видит дедлока, пока живы другие горутины, и 40 000 замёрзших отправителей за ночной прогон — цена этой тишины. Дисциплина закрытия — только отправитель; fan-in считает форвардеров WaitGroup-ом и закрывает в отдельной горутине wait-затем-close. Отмена разбирает либо бросанием — каждый отправитель спасает себя через Done, теряя значения в полёте, — либо осушением, где потребитель принимает до close и ничего не теряется, с границей — иначе зависание. Буферы — поглотители джиттера, не лекарство от дисбаланса: буфер на 1024 слота перед стабильно более медленной стадией заполняется за секунды, и пайплайн оседает на скорости самой медленной стадии, платя лишней памятью и оттянутыми симптомами утечек. Гоняйте стадии под errgroup, чтобы один отказ отменял каждую защищённую отправку, а Wait возвращал первую ошибку; экспортируйте len против cap по каналам и число горутин — и пайплайн сам расскажет, где живут его очередь и его утечки. Теперь, когда в goroutine-профиле вы видите сотни одинаковых стеков `chan send`, — вы знаете, какая стадия осталась без потребителя, и знаете одну строку, которая позволила бы тем горутинам спасти себя самостоятельно.
</Recap>
