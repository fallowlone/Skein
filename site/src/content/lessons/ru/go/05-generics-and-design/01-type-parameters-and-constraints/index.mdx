---
concepts:
- go-generics
deepensInto: []
estMin: 18
lang: ru
lessonType: topic
level: middle
order: 1
prereqs: []
slug: "01-type-parameters-and-constraints"
sources:
- https://go.dev/blog/intro-generics
- https://go.dev/blog/deconstructing-type-parameters
spiral: []
status: ready
summary: 'Параметры типов и constraints как множества типов: аппроксимация ~T, объединения, comparable. Где вывод типов работает и где останавливается. Инстанцирование через GCShape-стенсилинг: типы-значения получают свой машинный код, все указатели делят один инстанс и словарь.'
title: 'Параметры типов и constraints: множества типов, пределы вывода и GCShape-стенсилинг'
track: "go"
unit: "05-generics-and-design"
---

import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
Когда вышел Go 1.18, команда хранилища переписала свой LRU-кеш с `interface{}` на `Cache[K comparable, V any]`. Type assertions исчезли с горячего пути, а в design-доке обещали то же ускорение, которое команда помнила по шаблонам C++: полная мономорфизация, всё инлайнится. Бенчмарки вернулись расколотыми пополам. `Cache[string, int64]` оказался на 28% быстрее старого кода. А инстанцирование, которое реально жило в проде — `Cache[string, *Session]`, — измерилось в пределах шума от версии на `interface{}`. Дизассемблер объяснил почему: каждое инстанцирование с указателями скомпилировалось в одну общую функцию со скрытым аргументом-словарём, и вызовы внутри неё остались косвенными. Дженерики Go — не шаблоны C++; это осознанный гибрид, и шов этого гибрида проходит ровно между типами-значениями и указателями. Команда оставила дженерик-API — он убрал целый класс паник на assertion, — но слайд про производительность тихо исчез из презентации.
</Hook>

<Crux>Дженерики Go компилируются по GC-шейпам: все указатели делят один инстанс и словарь. Constraint — это множество типов, а не класс.</Crux>

За следующие десять минут ты узнаешь, где именно проходит этот шов — и почему указательный случай молча поглотил ожидаемое ускорение.

<Explanation>

## Constraints — множества типов, а не иерархии классов

Список параметров типа — `func Sum[T Number](xs []T) T` — вводит `T` как плейсхолдер, который должен удовлетворять своему **constraint**. Constraint — это обычный интерфейс, но в новой роли: вместо того чтобы описывать, что значение умеет *делать*, он описывает **множество типов**, которые разрешены. Внутри такого интерфейса можно перечислять типы напрямую, объединять их через `|` и ставить префикс `~`, означающий «любой тип, чей *underlying type* — вот этот»:

```go
// Constraint — это интерфейс, используемый как множество типов.
type Number interface {
	~int | ~int64 | ~float64 // ~T: любой тип с underlying-типом T
}

func Sum[T Number](xs []T) T {
	var total T
	for _, x := range xs {
		total += x // легально: каждый тип множества поддерживает +
	}
	return total
}

type Celsius float64 // именованный тип, underlying — float64

var _ = Sum([]Celsius{20.5, 21.0}) // компилируется только благодаря ~
```

Тильда важнее, чем кажется. Код на Go полон именованных типов — `type UserID int64`, `type Port int`, — а constraint, записанный как голый `int64`, допускает *только* `int64`, но не `UserID`. Без тильды ваша «числовая» библиотека отвергает половину доменных типов кодовой базы, причём провал — это ошибка компиляции у вызывающего, далеко от constraint, который её вызвал. Эмпирическое правило: в переиспользуемых constraints всегда пишите `~T`, если нет конкретной причины исключить именованные типы. Словарь дополняют ещё две детали: `comparable` — предобъявленный constraint со смыслом «поддерживает `==` и `!=`» (именно он нужен ключам `map`, и его нельзя выразить объединением — множество бесконечно), а ещё constraints могут содержать методы, и тогда тип должен одновременно входить в множество *и* иметь эти методы. Чего constraint не может никогда — выступать обычным типом: нельзя объявить `var n Number`; множества типов существуют только в момент инстанцирования.

<FlowDiagram
  label="Две записи одного constraint: ~int64 допускает каждый тип, чей underlying-тип int64, а голый int64 допускает ровно один тип и отвергает все доменные"
  hue="var(--d-backend)"
  perRow={3}
  nodes={[
    { id: "tilde", label: "~int64", sub: "проверка underlying-типа" },
    { id: "base", label: "сам int64", sub: "всегда в обоих множествах" },
    { id: "named", label: "UserID, Duration", sub: "именованные над int64" },
    { id: "bare", label: "int64 — без тильды", sub: "проверка точного типа" },
    { id: "rej", label: "UserID отвергнут", sub: "ошибка компиляции у вызова" },
  ]}
  edges={[
    { from: "tilde", to: "base", label: "допускает" },
    { from: "tilde", to: "named", label: "допускает" },
    { from: "bare", to: "base", label: "только его" },
    { from: "bare", to: "rej", label: "исключает" },
  ]}
  caption="Доменные типы — type UserID int64, time.Duration — живут или умирают на тильде. Без неё провал всплывает ошибкой компиляции в точке вызова, далеко от constraint-виновника; в переиспользуемых constraints пишите ~T по умолчанию."
/>

## Вывод типов: мощный в точке вызова, отсутствующий везде ещё

Прежде чем рассчитывать на вывод типов для чистоты вызовов, стоит знать, где он просто останавливается, — чтобы поймать запах дизайна до того, как о него споткнётся коллега.

Вывод типов — то, что делает дженерики читаемыми: `Sum(prices)` вместо `Sum[Celsius](prices)`. Компилятор выводит аргументы типов из типов аргументов функции и затем проверяет их по constraints. Но у вывода есть жёсткие края, и senior-ревью означает знать, где они:

```go
func Map[T, U any](xs []T, f func(T) U) []U { /* ... */ }

ys := Map(ids, strconv.Itoa) // T=int, U=string — выведены из аргументов

func Zero[T any]() T { var z T; return z }

v := Zero[int]() // обязательное явное инстанцирование:
                 // T встречается только в результате — выводу не на что смотреть
```

Параметр типа, который встречается **только в позиции результата**, вывести нельзя никогда — нет аргумента, несущего информацию. Нетипизированные константы — вторая ловушка: передача `1` в параметр `[T Number]` выводит `int` (тип константы по умолчанию), а не `int64`, и если constraint исключает `int`, вызов не компилируется, хотя `1` — совершенно валидный `int64`. Третий предел структурный: вывод работает только в точках вызова. Нет вывода из контекста присваивания, нет частичной специализации, нет перегрузки по constraints. Когда вывод не срабатывает, фикс механический — написать аргумент типа явно, — но API, который *регулярно* заставляет вызывающих писать `F[конкретный](...)`, — это запах дизайна, лечащийся переносом параметра типа в аргумент.

<Quiz
  id="01-type-parameters-and-constraints-quiz-1"
  lessonSlug="01-type-parameters-and-constraints"
  lang="ru"
  question="Библиотека объявляет constraint type ID interface { int64 } — без тильды. Коллега пишет type UserID int64 и вызывает Lookup[UserID](...). Что произойдёт?"
  choices={[
    { label: "Ошибка компиляции: без ~ множество типов содержит ровно int64, а UserID — другой тип, хотя его underlying-тип совпадает", correct: true },
    { label: "Скомпилируется, потому что underlying-тип UserID — это int64", misconception: "Именно это поведение включается тильдой. Голый int64 в constraint допускает только сам int64; ~int64 — вот запись, которая допускает любой тип с underlying-типом int64." },
    { label: "Скомпилируется, но упадёт с паникой в рантайме при проверке constraint", misconception: "Constraints — чисто компайл-тайм механизм, никакой рантайм-проверки, способной паниковать, не существует. Принадлежность множеству типов решается при инстанцировании, то есть во время компиляции." },
    { label: "Скомпилируется, потому что Go неявно сконвертирует UserID в int64 в точке вызова", misconception: "Go никогда не конвертирует именованные типы неявно, и дженерик-инстанцирование не добавляет конверсий. Вызывающему пришлось бы конвертировать явно — потеряв при этом свой доменный тип." },
  ]}
/>

## Инстанцирование: GCShape-стенсилинг, честно

Что происходит, когда вы реально вызываете `Sum[Celsius]`? C++ бы *мономорфизировал*: сгенерировал свежий машинный код под каждый аргумент типа — быстрый код, медленная компиляция, толстые бинарники. Java стирает типы: одна копия кода, всё в боксах. Go 1.18 выбрал середину — **GCShape-стенсилинг со словарями**. Компилятор группирует аргументы типов по *GC-шейпу*: два типа делят шейп, если для аллокатора и сборщика мусора они выглядят одинаково — тот же размер, та же раскладка указателей. Но главный коллапс вот в чём: **все типы-указатели — один шейп**. `*User`, `*Order`, `*bytes.Buffer` — одно общее инстанцирование. Чтобы различать их в рантайме, компилятор передаёт скрытый аргумент-**словарь** с метаданными конкретного типа: его `runtime._type`, адреса методов, производные типы. Код, вызывающий метод параметра типа, загружает адрес из словаря и вызывает косвенно — а значит, через этот вызов нет инлайнинга.

Честные последствия для производительности: инстанцирования с типами-значениями получают выделенный код, прямые вызовы, полный инлайнинг — реальный выигрыш над версиями с `interface{}`, которым приходилось боксить и делать assertions. Инстанцирования с указателями получают общий код и косвенные вызовы через словарь — примерно профиль стоимости интерфейсного вызова метода, иногда чуть хуже из-за лишней загрузки словаря. Ровно в этот шов и врезалась команда из Hook: `Cache[string, int64]` мономорфизировался; `Cache[string, *Session]` пошёл через словарь. Стенсилинг — деталь реализации, которую спецификация не обещает (стратегия gc остаётся шейповой с 1.18), но проектировать API так, будто дженерики — бесплатная мономорфизация, — верный способ занести бенчмарк-сюрпризы в продакшен-ревью.

<Quiz
  id="01-type-parameters-and-constraints-quiz-2"
  lessonSlug="01-type-parameters-and-constraints"
  lang="ru"
  question="Вы инстанцируете Cache[string, *User] и Cache[string, *Order]. Сколько копий машинного кода кеша сгенерирует gc для типа-значения и как диспетчеризуются вызовы на нём?"
  choices={[
    { label: "Одно общее инстанцирование — у всех указателей одинаковый GC-шейп, — а вызовы методов параметра типа идут косвенно через скрытый аргумент-словарь", correct: true },
    { label: "Две полностью мономорфизированные копии, по одной на тип, с прямыми инлайнируемыми вызовами", misconception: "Это модель C++, и Go так делает только для различных шейпов-значений. Все указатели схлопываются в один GC-шейп, так что *User и *Order делят одно инстанцирование и платят за словарь." },
    { label: "Ноль — Go стирает дженерики в рантайме и откатывается к боксингу через interface{}, как Java", misconception: "Go генерирует настоящий код по шейпам; инстанцирования с типами-значениями реально специализированы и инлайнируются. При стирании Sum по []int64 боксил бы каждый элемент — а это измеримо не так." },
    { label: "По копии на каждую точку вызова, заново везде, где кеш используется", misconception: "Инстанцирование идёт по шейпу аргумента типа, а не по точке вызова. Тысяча вызовов с *User и *Order всё равно делит единственное указательное инстанцирование и его словари." },
  ]}
/>

</Explanation>

<FlowDiagram
  label="От дженерик-функции к машинному коду: аргументы типов проверяются по множеству типов constraint, группируются по GC-шейпу и стенсилируются — шейпы-значения получают выделенный код, указательные шейпы делят одно тело плюс словарь"
  hue="var(--d-backend)"
  perRow={3}
  nodes={[
    { id: "src", label: "Дженерик-функция", sub: "Sum[T Number]" },
    { id: "typeset", label: "Проверка множества", sub: "~T, объединения, comparable" },
    { id: "shape", label: "Группировка по шейпу", sub: "размер + раскладка указателей" },
    { id: "value", label: "Шейп-значение", sub: "int64, float64…" },
    { id: "ptr", label: "Указательный шейп", sub: "*User = *Order" },
    { id: "dict", label: "Словарь", sub: "метаданные типа, косвенные вызовы" },
  ]}
  edges={[
    { from: "src", to: "typeset", label: "инстанцирование" },
    { from: "typeset", to: "shape", label: "принято" },
    { from: "shape", to: "value", label: "свой код" },
    { from: "shape", to: "ptr", label: "общий код" },
    { from: "ptr", to: "dict", label: "+ скрытый аргумент" },
  ]}
  caption="Инстанцирования шейпов-значений специализированы и инлайнируются. Все указатели схлопываются в один шейп, и вызовы на параметре типа грузят адреса из словаря — примерно цена интерфейсного вызова."
/>

<KeyTakeaway>Constraint — это интерфейс, читаемый как множество типов: `~T` допускает именованные типы по underlying-типу, объединения перечисляют альтернативы, `comparable` покрывает `==`. Вывод читает только аргументы в точке вызова — параметры типов, живущие лишь в результате, инстанцируются явно. gc компилирует дженерики по GC-шейпам: типы-значения получают выделенный инлайнируемый код; все указатели делят одно инстанцирование со скрытым словарём — не ждите мономорфизационных выигрышей от API на указателях.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="01-type-parameters-and-constraints-retrieval"
  lang="ru"
  questions={[
    { q: "Объясни constraints как множества типов: что дают ~T, объединения и comparable, и почему constraint, записанный как голый int64, отвергает type UserID int64?", a: "Constraint — это интерфейс, описывающий множество типов, которые может принять параметр. Голый тип вроде int64 кладёт в множество ровно один этот тип, поэтому именованный тип UserID — другой тип, лишь разделяющий underlying-тип, — исключён, и вызов не компилируется. ~int64 расширяет множество до всех типов с underlying-типом int64 — именно это почти всегда и нужно переиспользуемым constraints. Объединения (A | B | C) перечисляют альтернативы, и оператор доступен в теле дженерика, только если его поддерживает каждый член множества. comparable — предобъявленный constraint для == и != — бесконечное множество, которое объединением не записать. Constraints могут содержать и методы, но никогда не могут использоваться как обычные типы значений." },
    { q: "Разбери GCShape-стенсилинг: что получает свой машинный код, что делится, что делает словарь, и в какое следствие для производительности упёрлась команда из Hook.", a: "gc группирует аргументы типов по GC-шейпу — одинаковый размер и раскладка указателей. Каждый отдельный шейп-значение (int64, float64, маленькая структура…) получает свою стенсилированную копию функции: прямые вызовы, инлайнинг, выигрыш в духе C++. Все указатели делят единственный шейп, поэтому *User и *Order компилируются в одно общее тело, получающее скрытый аргумент-словарь с метаданными конкретного типа и адресами методов; вызовы на параметре типа становятся косвенными загрузками через словарь и не инлайнируются. В итоге Cache[string, int64] обогнал версию на interface{} на ~28%, а Cache[string, *Session] сыграл с ней вничью — дженерики на указательном шейпе стоят примерно как интерфейсный вызов, иногда чуть дороже из-за загрузки словаря." },
  ]}
/>

<Recap lang="ru">
Дженерики в Go — это две идеи, скрученные вместе, и у обеих есть края. Первая идея — constraint как множество типов: интерфейс, перечисляющий типы вместо методов, где `~T` допускает любой тип с underlying-типом `T` (забудьте тильду — и каждый `type UserID int64` в кодовой базе отскочит от вашей библиотеки), объединения перечисляют альтернативы, `comparable` называет бесконечное множество типов с `==`, а тело дженерик-функции может использовать только операции, которые поддерживает каждый член множества. Constraints — граждане исключительно компайл-тайма: они не могут быть типом переменной. Вторая идея — стратегия компиляции: вывод типов заполняет аргументы типов из аргументов в точке вызова и больше ниоткуда, поэтому параметр, встречающийся только в результатах, требует явного инстанцирования, а нетипизированные константы выводят свои типы по умолчанию. Само инстанцирование идёт по GC-шейпам, а не по типам — GCShape-стенсилинг. Различные шейпы-значения получают выделенный машинный код с прямыми инлайнируемыми вызовами; все указатели схлопываются в одно общее инстанцирование, параметризованное скрытым словарём метаданных типа, и вызовы на параметре типа становятся косвенными. Этот шов — главная история про производительность: дженерик-код над типами-значениями может всерьёз обогнать код на `interface{}`, а дженерик-код над указателями обычно играет с ним вничью, как и показал расколотый бенчмарк команды кеша. Используйте дженерики ради типобезопасности и ясности API везде, где они уместны, — но обещайте мономорфизационные ускорения только там, где аргументы типов — значения. Теперь, когда встретишь дженерик-API в бенчмарке, который не обгоняет версию на `interface{}`, — сразу проверь аргумент типа: если это указатель, причина уже известна.
</Recap>
