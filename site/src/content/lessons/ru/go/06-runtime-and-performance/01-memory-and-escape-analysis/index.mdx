---
concepts:
- go-escape-analysis
deepensInto: []
estMin: 18
lang: ru
lessonType: topic
level: senior
order: 1
prereqs: []
slug: "01-memory-and-escape-analysis"
sources:
- https://go.dev/doc/gc-guide
- https://go.dev/doc/diagnostics
spiral: []
status: ready
summary: 'Escape-анализ решает «стек или куча» на компиляции: в кучу уходит то, что переживает кадр, попадает в interface или захвачено замыканием. Читайте -gcflags=-m, помните: стек растёт копированием, а настоящая цена аллокации — давление на GC, а не ~25 нс самого malloc.'
title: 'Память и escape-анализ: кто решает «стек или куча» и сколько на самом деле стоит аллокация'
track: "go"
unit: "06-runtime-and-performance"
---

import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";
import StackDiagram from "~/components/diagram/StackDiagram.astro";
import Infographic from "~/components/diagram/Infographic.astro";
import leverageOrder from "./infographics/leverage-order.svg?raw";

<Hook>
Сервис оформления заказов тратил 31% CPU на сборку мусора, и первой реакцией команды было «потюнить GC». Кто-то вместо этого снял профиль аллокаций. На вершине оказался не разбор JSON и не драйвер базы — а обёртка логгера, вызываемая на каждом запросе и форматирующая request ID через `fmt.Sprintf("%d", id)`. Одна невинная строка. Поскольку `fmt` принимает `...interface{}`, каждое целое, переданное туда, упаковывалось в кучу: миллион запросов в час, три строки лога на запрос, три heap-аллокации на строку. Компилятор сообщал об этом всё время — `go build -gcflags=-m` печатал `id escapes to heap` любому, кто посмотрит. Замена форматирования на горячем пути на `strconv.AppendInt` в переиспользуемый буфер срезала скорость аллокаций на 60%, а GC — с 31% до 9% CPU. Никто ничего не тюнил. Просто перестали аллоцировать — а чтобы перестать, надо сначала понять, почему компилятор решил, что вы аллоцируете.
</Hook>

<Crux>Не докажет компилятор, что значение умирает с кадром стека, — оно уйдёт в кучу. -gcflags=-m объяснит; interface и fmt — главные виновники.</Crux>

За следующие пятнадцать минут вы увидите, почему `fmt.Sprintf` оказался виновником, как заставить компилятор показать своё решение и что делать вместо этого — чтобы в следующий раз, когда GC CPU взлетит, вы не тянулись первым делом к ручкам GC.

<Explanation>

## «Стек или куча» — решение компилятора, а не синтаксиса

В C `malloc` означает кучу, а локальная переменная — стек. В Go синтаксис ничего не значит: `new(T)`, `&T{}` и обычный `var t T` означают одно — «мне нужен T», а *где* он будет жить, решает **escape-анализ**, доказательный проход на этапе компиляции. Про каждое значение компилятор спрашивает: *может ли оно пережить кадр стека, который его создал?* Если доказуемо нет — значение живёт на стеке: аллокация — сдвиг указателя кадра (практически бесплатно), освобождение — возврат из функции (буквально бесплатно). Если да — или если компилятор *не может доказать*, что нет, — значение убегает в кучу, где аллокатор размещает его (~25 нс для мелкого объекта по быстрому пути `mcache`), а сборщик мусора обязан его потом найти, пометить и подмести.

В этой асимметрии вся суть темы. Стековая аллокация не стоит ничего ни сейчас, ни потом. Heap-аллокация стоит немного сейчас и — именно это показывают профили аллокаций — *повторяющийся налог* потом: каждый живой байт кучи удлиняет следующий цикл GC, а каждый выделенный байт приближает его. Компилятор покажет свои рассуждения, если спросить:

```go
// $ go build -gcflags=-m ./... 2>&1 | grep -E 'escapes|moved'
//
// ./handler.go:14:6: moved to heap: user        ← &user переживает кадр
// ./handler.go:22:13: id escapes to heap        ← передан в fmt (interface)
// ./handler.go:30:12: make([]byte, n) escapes   ← размер неизвестен при компиляции
// ./handler.go:41:9: func literal escapes       ← замыкание хранится дольше вызова

func newUser(name string) *User {
	user := User{Name: name} // "moved to heap": указатель возвращается,
	return &user             // значит значение обязано пережить этот кадр
}
```

Запускайте `-gcflags=-m` (дважды, `-m -m`, для подробных доказательств) на горячих пакетах. Вывод читается как судебное решение: не «это медленно», а «вот почему я не смог оставить это на стеке».

## Что вызывает escape — пять главных подозреваемых

Escape-анализ консервативен: всё, что нельзя *доказать* локальным, уходит в кучу. На практике почти все escape в сервисном коде дают пять паттернов:

1. **Возврат указателя на локальную переменную.** `return &user` — кадр умирает, значение не может. Это нормально и идиоматично: конструкторы и *должны* аллоцировать. Расточительство — делать это на каждой итерации горячего цикла.
2. **Помещение в interface.** `fmt.Println(id)`, `logger.Info("n", anyValue)` — преобразование конкретного значения в `interface{}` обычно аллоцирует его в куче: вызываемая сторона получает непрозрачную коробку, сквозь которую компилятор не видит. Это баг из пролога — и самый частый случайный escape в кодовых базах Go.
3. **Захват замыканием по ссылке.** Литерал `func(){ ... }`, который захватывает локальную переменную и сам сохраняется, возвращается или передаётся в `go`, — захваченная переменная переезжает в кучу, чтобы оба кадра могли её разделить.
4. **Размер неизвестен на этапе компиляции.** `make([]byte, n)` с рантаймовым `n` убегает; `make([]byte, 4096)` с константой может остаться на стеке (неявные аллокации стекопригодны до 64 КБ). Одинаковая форма кода — разное доказательство.
5. **Передача указателей через границы горутин.** Значение, отправленное в канал или записанное в поле структуры, переживающей вызов, по определению уже убежало.

Вместе паттерны 2 и 3 — преобразования в interface и замыкания — дают подавляющее большинство случайных escape в сервисном коде; уберите эти два, и большинство горячих путей никогда не коснётся кучи.

Вердикт меняется и от инлайнинга: маленькая функция, встроенная в вызывающую, может позволить компилятору заново доказать локальность и *отменить* escape. Поэтому поведение escape может сдвигаться между версиями Go — изменилось доказательство, а не ваш код.

<Quiz
  id="01-memory-and-escape-analysis-quiz-1"
  lessonSlug="01-memory-and-escape-analysis"
  lang="ru"
  question="Вы передаёте локальный int в fmt.Println, и -gcflags=-m сообщает, что он убегает в кучу. Каков фактический механизм?"
  choices={[
    { label: "Преобразование int в interface{} упаковывает его; fmt получает непрозрачную ссылку, и компилятор не может доказать, что она не сохраняется, — значит, значение обязано жить в куче", correct: true },
    { label: "Вариадические функции всегда копируют аргументы в кучу", misconception: "Вариадические аргументы становятся слайсом, а слайс конкретных значений с доказуемо локальным временем жизни может жить на стеке. Escape вызывает преобразование в interface{}, а не синтаксис «...»." },
    { label: "fmt внутри использует reflect, а любое значение, тронутое рефлексией, навсегда прибивается к куче", misconception: "Рефлексия работает с interface-значением, которое ей передали; аллокацию вызывает преобразование в точке вызова. Рефлексия не прибивает стековые значения задним числом." },
    { label: "Целые числа в Go неизменяемы и потому всегда живут в общем пуле в куче", misconception: "В Go нет пула интернирования целых (кеш маленьких int — деталь Python/Java). Обычный int — стековое значение, пока что-то — например, преобразование в interface — не вытолкнет его наружу." },
  ]}
/>

## Рост стека: почему 2 КБ не переполняются

Escape-анализ был бы бесполезен, если бы стеки не вмещали реальную нагрузку. Горутина стартует с ~2 КБ, и пролог каждой функции проверяет, помещается ли кадр. При переполнении рантайм не падает — он выделяет стек **вдвое больше, копирует старый и переписывает каждый указатель**, который указывал внутрь него (это возможно, потому что компилятор точно знает, какие слова — указатели). Старый сегмент освобождается; горутина продолжает, ничего не заметив. Стеки ещё и *сжимаются*: при GC горутине, использующей четверть стека, его урезают вдвое. Модель стоимости: рост — это копирование, O(размер стека), оплачиваемое редко и амортизированно почти бесплатно; патологический случай — паттерн вызовов, осциллирующий через границу роста и платящий за копирование снова и снова. Поэтому глубокая рекурсия на горячем пути в Go небесплатна, даже если ничего не падает: каждое удвоение от 2 КБ до, скажем, 1 МБ — это десять копирований. Отсюда же дешевизна обещания стековой аллокации: рантайм всегда может переместить стек, но никогда не может переместить кучу (об этом в уроке про GC — куча Go не компактируется).

<StackDiagram
  label="Стек горутины растёт копированием: каждое переполнение выделяет вдвое больший стек, копирует кадры и переписывает каждый указатель в старый стек"
  hue="var(--d-backend)"
  layers={[
    { label: "2 КБ — стек при рождении", note: "пролог проверяет запас" },
    { label: "4 КБ — первое переполнение", note: "alloc ×2, копия, правка ptr" },
    { label: "8 КБ … 1 МБ — удвоения", note: "десять копирований по пути" },
    { label: "сжатие при GC", note: "занята четверть → урезан вдвое" },
    { label: "потолок ~1 ГБ", note: "сбежавшая рекурсия паникует" },
  ]}
  caption="Рост — O(размер стека), но редкий и амортизированный; патология — паттерн вызовов, осциллирующий через границу роста и платящий за копию снова и снова. Рантайм может перемещать стеки, потому что компилятор знает каждое слово-указатель, — куча не двигается никогда."
/>

## Честный прайс-лист

Когда видите «GC тормозит» — спросите себя: тормозит из-за чего? Прежде чем касаться `GOGC`, посмотрите на скорость аллокаций — именно об этом раздел ниже.

Числа, которые стоит держать в голове: мелкая heap-аллокация через per-P `mcache` — примерно **20–40 нс**; стековая — **~0 нс** (кадр и так существует). Один escape — ничто. Сервисы убивает *скорость*: при 2 ГБ/мин аллокаций пейсер ставит циклы GC впритык, mark assist начинает напрямую облагать налогом аллоцирующие горутины, и внезапно «GC тормозит» — хотя честная формулировка: «мы аллоцируем 30 миллионов раз в минуту». Порядок рычагов на горячем пути: (1) не аллоцировать (вычистить escape через `-m`), (2) аллоцировать один раз и переиспользовать (`sync.Pool`, слайсы с заранее заданной ёмкостью, `strconv.Append*` в существующие буферы), (3) и только потом думать о ручках GC. Профиль аллокаций (`pprof -sample_index=alloc_space`) ранжирует, откуда именно берутся байты, — команда из пролога нашла ответ на одном экране.

<Infographic
  svg={leverageOrder}
  label="Пирамида порядка рычагов для починки аллокационно-тяжёлого горячего пути: широкое основание — «не аллоцировать» (вычистить escape через -gcflags=-m), середина — переиспользование (sync.Pool, слайсы с ёмкостью, strconv.Append), узкая вершина — тюнинг ручек GC как крайняя мера."
  caption="Работайте снизу вверх: сначала перестать аллоцировать (наибольший рычаг), затем переиспользовать, и лишь в конце — GOGC, вершина, которая нужна редко."
  hue="var(--d-infra)"
/>

<Quiz
  id="01-memory-and-escape-analysis-quiz-2"
  lessonSlug="01-memory-and-escape-analysis"
  lang="ru"
  question="Рекурсия горутины выросла за пределы её текущего стека в 2 КБ. Что делает рантайм?"
  choices={[
    { label: "Выделяет стек примерно вдвое больше, копирует старый и переписывает все указатели внутрь старого стека, затем освобождает его", correct: true },
    { label: "Подцепляет новый сегмент 2 КБ к старому и держит оба, связывая кадры через сегменты", misconception: "Это сегментированные стеки — Go давно от них отказался из-за проблемы hot split: граница вызова на стыке двух сегментов платила цену на каждом вызове. Современный Go использует непрерывные стеки, растущие копированием." },
    { label: "Горутина паникует со stack overflow, как только превысит начальное выделение", misconception: "Переполнение текущего сегмента запускает рост, а не панику. Паника случается только на абсолютном потолке (~1 ГБ по умолчанию) — рекурсия должна полностью убежать, чтобы его достичь." },
    { label: "ОС прозрачно расширяет отображение стека через page fault, как для главного потока", misconception: "Стеки горутин — обычная память из кучи, управляемая рантаймом и невидимая для ОС. Трюк с guard page обслуживает только настоящие потоки ОС; стеки Go переезжают, фиксированное отображение даже не сработало бы." },
  ]}
/>

</Explanation>

<FlowDiagram
  label="Каждое значение проходит через доказательство escape-анализа: стек — бесплатное освобождение вместе с кадром, куча — работа аллокатора сейчас и работа GC всегда потом"
  hue="var(--d-backend)"
  perRow={3}
  nodes={[
    { id: "value", label: "Создано значение", sub: "var / new / &T{}" },
    { id: "proof", label: "Escape-анализ", sub: "переживёт кадр?" },
    { id: "stack", label: "Стек", sub: "умирает с кадром" },
    { id: "heap", label: "Куча", sub: "mcache, ~25 нс" },
    { id: "gc", label: "Давление на GC", sub: "налог mark + sweep" },
    { id: "grow", label: "Рост стека", sub: "копия + правка ссылок" },
  ]}
  edges={[
    { from: "value", to: "proof", label: "компиляция" },
    { from: "proof", to: "stack", label: "доказуемо локально" },
    { from: "proof", to: "heap", label: "escape" },
    { from: "heap", to: "gc", label: "повторный налог" },
    { from: "stack", to: "grow", label: "при переполнении" },
  ]}
  caption="Доказательство консервативно: interface, захваченные замыкания, make с рантаймовым размером и возвращённые указатели его проваливают. -gcflags=-m печатает вердикт по каждому значению; профиль аллокаций показывает, за какие вердикты вы платите."
/>

<KeyTakeaway>Escape-анализ — доказательство на этапе компиляции: значения, доказуемо умирающие с кадром, живут на стеке (бесплатно), всё остальное убегает в кучу (~25 нс сейчас, давление на GC навсегда). Interface, замыкания, `fmt`, `make` с рантаймовым размером и возвращённые указатели — классические триггеры escape: читайте вердикт через `-gcflags=-m`, а горячие пути чините отказом от аллокаций, прежде чем трогать ручки GC.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="01-memory-and-escape-analysis-retrieval"
  lang="ru"
  questions={[
    { q: "Назовите пять типичных паттернов, выталкивающих значение в кучу, и инструмент, показывающий рассуждения компилятора.", a: "Возврат указателя на локальную переменную (значение переживает кадр); преобразование конкретного значения в interface — классика это fmt и логгеры, где вызываемая сторона получает непрозрачную коробку; захват замыканием по ссылке, когда замыкание переживает вызов (сохранено, возвращено или запущено через go); make/new с размером, неизвестным при компиляции, либо неявные аллокации крупнее 64 КБ; и передача указателей через границы горутин или структур, переживающих вызов. Инструмент — go build -gcflags=-m (удвоенный, -m -m, для полных доказательств): печатает moved to heap и escapes to heap со строкой и причиной. Вердикты меняются между версиями Go, потому что инлайнинг меняет видимость для доказателя." },
    { q: "Почему настоящая цена heap-аллокации — не ~25 нс malloc, и каков порядок рычагов для аллокационно-тяжёлого горячего пути?", a: "Сам malloc дёшев: мелкий объект через per-P mcache — 20–40 нс. Повторяющаяся цена — давление на GC: каждый выделенный байт продвигает пейсер к следующему циклу, каждый живой байт удлиняет маркировку; при высокой скорости аллокаций GC работает впритык, а mark assist напрямую облагает аллоцирующие горутины — скорость аллокаций конвертируется в CPU и латентность. Порядок рычагов: сначала перестать аллоцировать — прогнать -gcflags=-m по горячему пути и убрать escape (конкретные типы вместо interface-параметров, strconv.Append в буферы вместо fmt); затем переиспользовать — sync.Pool, слайсы с заданной ёмкостью; и лишь третьим — тюнинг GC. Проверка — heap-профиль alloc_space, ранжирующий точки аллокации по байтам." },
  ]}
/>

<Recap lang="ru">
Go выбирает между стеком и кучей доказательством, а не синтаксисом: escape-анализ спрашивает, может ли значение пережить свой кадр, и всё недоказуемо локальное уходит в кучу. Стековая аллокация — арифметика указателя кадра, освобождаемая возвратом; heap-аллокация стоит 20–40 нс по быстрому пути mcache и затем повторяющийся налог GC — больше живых байт для маркировки, больше выделенных байт, приближающих следующий цикл. Пять escape, доминирующих в реальных кодовых базах: возвращённые указатели на локальные, преобразования в interface (прежде всего fmt и структурные логгеры), замыкания с захватом по ссылке дольше вызова, make с рантаймовым размером (константы до 64 КБ остаются стекопригодными) и указатели, пересекающие границы горутин и долгоживущих структур. Каждый вердикт компилятор публикует через -gcflags=-m, а инлайнинг может перевернуть вердикт между релизами. Стеки стартуют с ~2 КБ и растут схемой «выделить вдвое — скопировать — переписать указатели» (непрерывные стеки, выбранные вместо сегментированных ради избавления от hot split) и сжимаются при GC, так что глубокая рекурсия платит за копирования, даже когда ничего не падает. Когда GC ест много CPU, честный диагноз обычно читается из профиля аллокаций: команда из пролога срезала GC с 31% до 9% не тюнингом, а удалением fmt.Sprintf с горячего пути. Сначала не аллоцировать, потом переиспользовать, и лишь изредка — тюнить. Теперь, когда встретите рост GC CPU, ваш первый шаг — `-gcflags=-m` на горячем пакете и профиль `alloc_space`, а не ручки GOGC.
</Recap>
