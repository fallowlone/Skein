---
concepts:
- go-benchmarking
deepensInto: []
estMin: 18
lang: ru
lessonType: topic
level: senior
order: 4
prereqs: []
slug: "04-benchmarking-real-services"
sources:
- https://go.dev/blog/pprof
- https://go.dev/doc/diagnostics
spiral: []
status: ready
summary: 'Микробенчмарки не предсказывают латентность сервиса. Нагружайте open-loop с фиксированной частотой, читайте p99/p999 вместо средних, помните о coordinated omission, профилируйте под нагрузкой, а в CI — benchstat по многим прогонам: один прогон ничего не доказывает.'
title: 'Бенчмарки реальных сервисов: перцентили, coordinated omission и цикл оптимизации'
track: "go"
unit: "06-runtime-and-performance"
---

import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";
import Infographic from "~/components/diagram/Infographic.astro";
import coordinatedOmission from "./infographics/coordinated-omission.svg?raw";

<Hook>
Перед запуском нагрузочный тест говорил, что новый эндпоинт прайсинга в порядке: стабильные 5 000 rps, средняя латентность 9 мс, p99 — комфортные 31 мс. В день запуска — тикеты в поддержку: «чекаут виснет на секунды». Дашборды соглашались с пользователями, а не с тестом — реальный p99 всплесками доходил до 2,4 с. Нагрузочный инструмент, строго говоря, не врал. Это был closed-loop генератор с 50 виртуальными пользователями, каждый из которых ждал ответа перед следующим запросом. Всякий раз, когда сервис замирал — шторм GC assist, затык пула соединений, — *инструмент замедлялся вместе с ним*, вежливо посылая меньше запросов ровно в те окна, где латентность была худшей, и записывая в основном хорошие времена между ними. Coordinated omission: измерение и отказ скоординированно прятали друг друга. Перезапуск с open-loop генератором, бьющим фиксированные 5 000 rps независимо от ответов, не оставил затыкам укрытия: p99 2,1 с — ровно там, где жили пользователи. Тот же сервис, та же нагрузка — честные часы.
</Hook>

<Crux>Нагружайте сервис open-loop и смотрите перцентили, а не средние. Профилируйте под нагрузкой; в CI — benchstat, а не один прогон.</Crux>

К концу урока вы поймёте, почему нагрузочный тест может врать в сто раз, что такое coordinated omission и как его устранить, и как выглядит защищаемый CI-гейт бенчмарков.

<Explanation>

## Микробенчмарки: точные ответы на маленькие вопросы

`testing.B` превосходен в том, что он реально измеряет: стоимость пути кода в тёплом кеше, без соседей, без давления GC от остального сервиса и без сети. Это делает его правильным инструментом для сравнения двух реализаций одной функции — и неправильным для предсказания латентности сервиса. Даже в своих рамках есть два классических самообмана. Первый — **удаление мёртвого кода**: если результат бенчмарка не используется, компилятор может выкинуть работу; держите результаты живыми (присваивайте в sink уровня пакета или используйте современный паттерн `b.Loop()`, который сам сохраняет результаты тела живыми). Второй — **замалчивание аллокаций**: всегда запускайте с `b.ReportAllocs()` — функция, «ставшая быстрее на 10%» при удвоении allocs/op, в контексте сервиса обычно регрессия: аллокации — это налог GC из предыдущих уроков.

```go
var sink int // уровень пакета: отключает удаление мёртвого кода

func BenchmarkParsePrice(b *testing.B) {
	data := loadFixture()
	b.ReportAllocs()
	for b.Loop() {           // Go 1.24+; заменяет for i := 0; i < b.N; i++
		v, _ := ParsePrice(data)
		sink = v
	}
}

// $ go test -bench=ParsePrice -count=10 > old.txt
// … вносим изменение …
// $ go test -bench=ParsePrice -count=10 > new.txt
// $ benchstat old.txt new.txt
//             │   old.txt   │            new.txt            │
// ParsePrice    412.0n ± 2%    389.5n ± 1%   -5.46% (p=0.000 n=10)
```

Числа микробенчмарка реальны — для микромира. Сервис добавляет очереди, contention, взаимные помехи GC между запросами и холодные кеши. Мост между мирами — правило: *микробенчмарк — чтобы выбрать реализацию; нагрузочный тест — чтобы делать заявления о латентности.*

## Честное нагрузочное тестирование: перцентили и открытый цикл

Средняя латентность — самое бесполезное число в отчёте: его определяет быстрое большинство, и оно структурно слепо к хвосту, который реально чувствуют пользователи. Отчитывайтесь **перцентилями** — p50, p99, p99.9 — и уважайте, как хвосты складываются: страница, разветвляющаяся на 100 бэкенд-вызовов, испытывает p99 бэкенда на *большинстве загрузок* (шанс, что все 100 вызовов увернутся от худшего 1%, равен 0,99^100 ≈ 37%). Хвостовая латентность — не краевой случай; при fan-out это медианный опыт.

И есть ловушка из пролога — **coordinated omission** (скоординированный пропуск: инструмент и отказ договариваются прятать друг друга). Closed-loop инструменты (фиксированный пул виртуальных пользователей, каждый ждёт ответа перед следующим запросом) измеряют время обслуживания, но тихо перестают *приходить* во время затыков — ровно когда приходящие запросы встали бы в очередь и пострадали. Записанная гистограмма пропускает худшие секунды скоординированно с их наступлением. Open-loop инструменты (wrk2, vegeta, всё с фиксированной частотой прихода) продолжают бить по расписанию; запрос, пришедший во время затыка, ждёт, и всё его время ожидания попадает в гистограмму. Различие — не педантизм: оно регулярно меняет p99 в 10–100 раз, а это разница между «выкатили» и «разбудили дежурного». Правила ведения боя: нагружайте систему *фиксированной частотой*, выведенной из реального трафика (плюс запас), гоняйте достаточно долго, чтобы пересечь несколько циклов GC и перевыпусков пула (минуты, не секунды), и меряйте со стороны клиента, включая установку соединения, — как меряют пользователи.

<Infographic
  svg={coordinatedOmission}
  label="Сравнение closed-loop и open-loop генераторов нагрузки на одном сервисе при 5 000 rps: closed-loop использует фиксированный пул виртуальных пользователей, которые перестают приходить во время затыков, поэтому его гистограмма пропускает худшие секунды и показывает p99 = 31 мс; open-loop бьёт с фиксированной частотой прихода, поэтому время затыка попадает в гистограмму, и он показывает p99 = 2,1 с — в 10–100 раз выше."
  caption="Тот же сервис, та же нагрузка: closed-loop координируется с затыками и прячет хвост (31 мс), open-loop меряет его честно (2,1 с). Сдвигает p99 в 10–100× модель цикла, а не данные."
  hue="var(--d-infra)"
/>

<Quiz
  id="04-benchmarking-real-services-quiz-1"
  lessonSlug="04-benchmarking-real-services"
  lang="ru"
  question="Closed-loop инструмент с 50 виртуальными пользователями показывает p99 = 31 мс, но пользователи в проде видят зависания на секунды. Какой дефект измерения наиболее вероятен?"
  choices={[
    { label: "Coordinated omission: инструмент шлёт запрос только после получения ответа, поэтому во время затыков он шлёт меньше, и гистограмма пропускает ровно худшие периоды", correct: true },
    { label: "50 пользователей слишком мало для осмысленных перцентилей, p99 статистически невалиден", misconception: "Размер выборки влияет на доверительные интервалы, а не на системную ошибку. Миллионы запросов через закрытый цикл всё равно недомеряют затыки: частота семплирования сама падает при деградации сервиса — дефект структурный, а не статистический." },
    { label: "Инструмент мерил серверное время обработки вместо полного round-trip", misconception: "Измерение на стороне клиента — норма для нагрузочных инструментов; разрыв не здесь. Разрыв в том, какие моменты вообще попадают в выборку: закрытый цикл семплирует окно затыка с пониженной частотой именно потому, что пережидает затык." },
    { label: "Продакшен-трафик использует другие payload, тест был просто нерепрезентативен", misconception: "Реализм данных важен, но он не объясняет разрыв 31 мс против 2,4 с, исчезающий при повторе тех же payload через open-loop с фиксированной частотой. Изменилась модель цикла, а не данные." },
  ]}
/>

## Цикл оптимизации: измерь, выдвини гипотезу, измени одно, измерь снова

Оптимизация без цикла вырождается в фольклор. Senior-workflow механичен: **(1) измерить под реалистичной нагрузкой** — open-loop, фиксированная частота, перцентили, *с профилями, снятыми во время прогона* (CPU- или mutex-профиль простаивающего сервиса описывает другую программу); **(2) сформулировать фальсифицируемую гипотезу** — «p99 — это GC assist во время маркировки, разогнанный аллокациями в сериализаторе», а не «наверное, GC»; **(3) изменить ровно одну вещь** — как только два изменения едут вместе, выигрыш неатрибутируем, а следующая регрессия неотлаживаема; **(4) перемерить тем же способом** и позволить числам наложить вето на нарратив. Профили под нагрузкой — фабрика гипотез: CPU-профиль говорит, куда уходят такты на 5 000 rps, mutex-профиль объясняет латентность, которую CPU не объясняет, heap alloc называет источник давления на GC. Два правила честности из горького опыта: остерегайтесь оптимизаций, которые перемещают стоимость, а не убирают её (`sync.Pool`, меняющий churn аллокаций на удержание; кеш, чинящий p50 и отравляющий p999 штормами вытеснения), и держите *генератор нагрузки* на отдельном железе — насыщенный генератор измеряет сам себя.

## Регрессии производительности в CI: benchstat или этого не было

Одиночный прогон бенчмарка — подброшенная монета в лабораторном халате. Шум между прогонами на общих CI-раннерах — стандартные ±5–10% (тепловое состояние, соседи, частотный скейлинг), поэтому пайплайн, падающий на «на 3% медленнее прошлого прогона», падает случайно, а пайплайн, сравнивающий одиночные прогоны, пропускает реальные 10%-е регрессии, спрятанные в шуме. Честная схема: гонять каждый бенчмарк с `-count=10` (или больше), сравнивать распределения через **benchstat**, который выдаёт дельту *с p-value*, — и гейтить только статистически значимые изменения за порогом, выбранным осознанно (например, «падать на ≥5% при p < 0,05»). Фиксируйте, что можете: выделенные или помеченные раннеры, фиксированная частота CPU где возможно, явный `GOMAXPROCS`, изоляция бенчмарков от параллельных джоб. Для чисел, которые реально будят людей — сервисный p99 под нагрузкой, — ночной open-loop тест против стейджинга с архивированием профилей ловит то, что пофункциональные бенчмарки структурно не видят: сквозные регрессии давления GC, contention локов и работы с соединениями. CI-бенчмарки охраняют функции; регулярные нагрузочные тесты охраняют сервис.

<Quiz
  id="04-benchmarking-real-services-quiz-2"
  lessonSlug="04-benchmarking-real-services"
  lang="ru"
  question="CI-джоба гоняет каждый бенчмарк один раз и валит сборку, потому что функция на 4% медленнее прошлого прогона. Какова корректная оценка?"
  choices={[
    { label: "Гейт бессмыслен: одиночные прогоны на общих раннерах несут ±5–10% шума — сравнивайте несколько прогонов через benchstat и гейтите статистически значимые дельты", correct: true },
    { label: "4% — реальная регрессия, гейт работает как задуман", misconception: "Без распределения невозможно отличить 4% сигнала от 4% шума тепла и соседей. benchstat по прогонам с -count=10 существует именно потому, что одиночные дельты такого размера регулярно разворачиваются на следующем коммите." },
    { label: "Гейт слишком мягкий — надо падать на любом замедлении, даже 0,1%", misconception: "Ужесточение шумного гейта усиливает шум: при ±5–10% дисперсии между прогонами порог 0,1% валит почти каждую сборку в обе стороны. Лечение — статистическое сравнение, а не более строгий порог на бессмысленном числе." },
    { label: "Бенчмаркам вообще не место в CI — важны только продакшен-метрики", misconception: "Продакшен-метрики ловят регрессии после того, как их почувствуют пользователи. Правильные CI-бенчмарки — несколько прогонов, benchstat, пороги значимости — ловят пофункциональные регрессии до мержа; дополнение — регулярный нагрузочный тест для сервисных чисел, а не отказ от гейта." },
  ]}
/>

</Explanation>

<FlowDiagram
  label="Честный цикл производительности: open-loop нагрузка с фиксированной частотой питает измерение перцентилей, профили под нагрузкой рождают одну гипотезу, едет одно изменение, и то же измерение голосует"
  hue="var(--d-backend)"
  perRow={3}
  nodes={[
    { id: "load", label: "Open-loop нагрузка", sub: "фиксированная частота" },
    { id: "measure", label: "Измерение", sub: "p50 / p99 / p999" },
    { id: "profile", label: "Профили под нагрузкой", sub: "CPU, mutex, alloc" },
    { id: "hypo", label: "Гипотеза", sub: "фальсифицируемая" },
    { id: "change", label: "Одно изменение", sub: "атрибутируемое" },
    { id: "gate", label: "CI-гейт", sub: "benchstat, n≥10" },
  ]}
  edges={[
    { from: "load", to: "measure", label: "гистограмма" },
    { from: "measure", to: "profile", label: "во время прогона" },
    { from: "profile", to: "hypo", label: "улики" },
    { from: "hypo", to: "change", label: "проверка" },
    { from: "change", to: "gate", label: "перемер" },
  ]}
  caption="Closed-loop инструменты координируются с затыками и пропускают худшие секунды; средние хоронят хвост, который fan-out усиливает. Каждая стрелка в обход шага — менять без профиля, гейтить по одному прогону — возвращает фольклор."
/>

<KeyTakeaway>Микробенчмарки выбирают между реализациями; честные заявления о латентности делают только open-loop тесты с фиксированной частотой прихода — закрытые циклы координируются с затыками и прячут хвост (ошибки p99 в 10–100×). Отчитывайтесь перцентилями, не средними; fan-out делает p99 медианным опытом. Оптимизируйте циклом: профиль под нагрузкой, одна фальсифицируемая гипотеза, одно изменение, перемер. В CI — benchstat по ≥10 прогонам с порогами значимости: одиночные дельты — шум.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="04-benchmarking-real-services-retrieval"
  lang="ru"
  questions={[
    { q: "Объясните coordinated omission: механизм, симптом и лечение.", a: "Механизм: closed-loop инструменты держат N виртуальных пользователей, каждый ждёт ответа перед следующим запросом, поэтому при затыке сервиса — шторм GC, исчерпание пула, конвой на локе — частота прихода у инструмента падает в том же окне; запросы, которые реальный трафик поставил бы в очередь во время затыка, вообще не отправляются, и их латентность не записывается. Измерение пропускает данные скоординированно с отказом. Симптом: p99 нагрузочного теста прекрасен (десятки мс), а пользователи в проде видят секунды; разрыв бывает 10–100×. Лечение: open-loop генерация — фиксированная частота прихода независимо от ответов (wrk2, vegeta), — тогда запрос, пришедший в затык, ждёт и записывает полное время очереди; плюс гонять минуты, а не секунды (пересечь несколько циклов GC), мерить на стороне клиента и держать генератор на отдельном железе, чтобы он не измерял собственное насыщение." },
    { q: "Спроектируйте CI-схему, ловящую реальные регрессии производительности без ложных срабатываний на шуме.", a: "Два слоя. Пофункциональный: гонять бенчмарки с -count=10 и больше, с b.ReportAllocs и sink (или b.Loop), чтобы были видны удаление мёртвого кода и регрессии аллокаций; сравнивать старое и новое через benchstat, дающий дельту с p-value; гейтить только статистически значимые изменения за осознанным порогом (например, ≥5% медленнее при p < 0,05); стабилизировать среду — выделенные раннеры, явный GOMAXPROCS, без параллельных джоб на той же машине. Одиночные прогоны флапают, потому что шум общих раннеров ±5–10%. Сервисный: регулярный (например, ночной) open-loop тест против стейджинга на фиксированной частоте, выведенной из продакшен-трафика, с гейтом по бюджетам p99/p999 и архивом CPU/mutex/alloc-профилей прогона — он ловит сквозные регрессии (давление GC, contention, работа с соединениями), которые ни один пофункциональный бенчмарк не увидит." },
  ]}
/>

<Recap lang="ru">
testing.B точно отвечает на маленькие вопросы — какая реализация быстрее в тёплом изолированном мире, — если вы отключили удаление мёртвого кода через sink или b.Loop и следите за allocs/op через b.ReportAllocs, потому что регрессия аллокаций — это регрессия сервиса, даже когда наносекунды улучшились. Заявлениям о латентности сервиса нужна другая машинерия: перцентили, поскольку средние слепы к хвосту, а fan-out делает p99 медианным опытом страницы (0,99^100 ≈ 37% страниц увернутся от худшего 1% при 100 вызовах); и open-loop нагрузка с фиксированной частотой прихода, потому что closed-loop инструменты совершают coordinated omission — замедляются вместе с сервисом и пропускают ровно окна затыков, пряча 10–100× p99: так эндпоинт прайсинга из пролога прошёл на 31 мс и выкатился на 2,4 с. Оптимизация — цикл, а не акт: измерить под нагрузкой с профилями во время прогона, написать фальсифицируемую гипотезу с названным механизмом, изменить одно, перемерить идентично — и не доверять победам, перекладывающим стоимость в удержание или хвост. В CI одиночные прогоны — монетки при шуме раннеров ±5–10%: гоняйте -count=10, сравнивайте через benchstat, гейтите значимые дельты за выбранным порогом и дополняйте пофункциональный гейт регулярным open-loop тестом, чьи архивные профили охраняют сервисные числа, которые реально будят людей. Теперь, когда настраиваете или разбираете нагрузочный тест, первый вопрос: open-loop или closed-loop? Если ответ — закрытый цикл, p99 в отчёте — не тот, что видят пользователи.
</Recap>
