---
concepts:
- go-gc
deepensInto: []
estMin: 19
lang: ru
lessonType: topic
level: senior
order: 2
prereqs: []
slug: "02-gc-mechanics-and-tuning"
sources:
- https://go.dev/doc/gc-guide
- https://go.dev/doc/diagnostics
spiral: []
status: ready
summary: 'Сборщик Go — конкурентный трёхцветный mark-sweep с барьерами записи и двумя STW-паузами меньше миллисекунды. GOGC меняет запас кучи на частоту циклов; GOMEMLIMIT — мягкий потолок, у границы возможна спираль смерти. Без компактизации и поколений — осознанный выбор, а не пробел.'
title: 'Механика и тюнинг GC: трёхцветный mark-sweep, пейсер, GOGC и GOMEMLIMIT без мифов'
track: "go"
unit: "06-runtime-and-performance"
---

import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DiagramFrame from "~/components/diagram/DiagramFrame.astro";
import PacketDot from "~/components/diagram/PacketDot.astro";
import Infographic from "~/components/diagram/Infographic.astro";
import memlimitHeadroom from "./infographics/memlimit-headroom.svg?raw";

<Hook>
Сервису рекомендаций прилетел тикет на экономию: под просил 4 GiB, а живая куча была лишь 1,2 GiB, так что memory request урезали до 1,5 GiB и «для подстраховки» добавили `GOMEMLIMIT=1400MiB`. Стейджинг прошёл гладко. В проде пятничное обновление каталога ненадолго подняло живую кучу до 1,3 GiB — 93% лимита — и сервис не словил OOM. Случилось хуже: он остался жить. GC, зажатый между кучей, которую нечем уменьшить, и потолком, который нельзя пересечь, начал гонять циклы впритык, сжигая 40%+ CPU на маркировку. p99 вырос с 18 мс до 900 мс. Дашборды не показывали ни падений, ни OOM — просто сервис тихо тратил половину мозга на сборку мусора. Дежурный час перезапускал поды, пока кто-то не построил график GC CPU. Лимит ошибался ненамного: 200 MiB запаса отделяли настроенный сервис от спирали смерти.
</Hook>

<Crux>GC в Go — конкурентный трёхцветный mark-sweep: темп задаёт GOGC, мягкий потолок — GOMEMLIMIT. Паузы меньше 1 мс оплачены процессором.</Crux>

К концу урока вы будете знать, где прячется настоящая цена GC (не в паузах), как читать GOGC и GOMEMLIMIT без гадания и как увидеть спираль смерти раньше, чем она вас разбудит.

<Explanation>

## Трёхцветный mark-sweep, работающий параллельно с вашим кодом

Сборщик Go — **конкурентный mark-sweep**: он находит живые объекты и освобождает остальное, причём почти вся работа идёт, *пока ваши горутины продолжают выполняться*. Фаза маркировки описывается трёхцветной моделью: каждый объект **белый** (ещё не увиден — презумпция мусора), **серый** (увиден, содержимое не просканировано) или **чёрный** (увиден и полностью просканирован). Маркировка стартует с корней — глобальных переменных и стека каждой горутины, — красит их в серый и затем вычерпывает серое множество: просканировать указательные поля объекта, посереть всё, на что они ссылаются, зачернить сам объект. Когда серых не осталось, всё достижимое — чёрное, а всё ещё белое — мусор. Sweep возвращает белые спаны аллокатору лениво, амортизированно в последующие аллокации.

Цикл обрамляют две stop-the-world паузы, обе обычно **короче миллисекунды, как правило десятки–сотни микросекунд**: одна запускает маркировку (включить барьер записи на каждом P), вторая её завершает (убедиться, что серой работы не осталось). Этот заголовочный показатель честный — но это *не* цена GC. Цена конкурентна: ~25% `GOMAXPROCS` зарезервированы под фоновую маркировку во время цикла, плюс **mark assist** — горутину, аллоцирующую быстрее, чем фоновые воркеры успевают сканировать, мобилизуют маркировать пропорционально её аллокациям. Субмиллисекундные паузы не бесплатны; это паузы, конвертированные в налог на пропускную способность.

Когда встречаете описание GC как «low-pause», помните: пауза не исчезла — она просто сменила форму.

## Барьер записи: почему конкурентная маркировка не теряет объекты

У маркировки, идущей одновременно с мутациями, есть классическая опасность: код может скопировать указатель на белый объект в уже чёрный объект и стереть единственную другую ссылку. Сборщик, закончивший с чёрным объектом, никогда к нему не вернётся — белый объект сметут, хотя он достижим. Go закрывает дыру **барьером записи**: во время маркировки каждая запись указателя проходит через маленький хук рантайма. Гибридный барьер Go (с 1.8) затеняет *старый* затираемый указатель (deletion-стиль, Yuasa), а для записей в стек — и новый (insertion-стиль, Дейкстра) — комбинация гарантирует, что ни один достижимый объект не останется белым, и именно она избавила от повторного сканирования стеков горутин на завершении, ограничив финальную паузу. Цена: каждая запись указателя в программе чуть дороже, пока идёт цикл. Структуры, плотные по указателям (связные списки, деревья мелких узлов, `map[string]*Thing`), облагаются дважды — больше работы барьеру, больше погони маркировщику. Это механический аргумент за value-ориентированный дизайн: `[]Item` лучше `[]*Item` не из идеологии, а из экономики барьера и сканирования.

<Quiz
  id="02-gc-mechanics-and-tuning-quiz-1"
  lessonSlug="02-gc-mechanics-and-tuning"
  lang="ru"
  question="Во время конкурентной маркировки код записывает указатель на объект W (белый) в объект B (уже чёрный) и удаляет единственный другой путь к W. Почему GC не освободит W?"
  choices={[
    { label: "Барьер записи перехватывает запись указателя и затеняет затронутый объект в серый, поэтому маркировщик вернётся к нему до завершения цикла", correct: true },
    { label: "GC повторно сканирует все чёрные объекты в конце цикла, чтобы поймать поздние записи", misconception: "Пересканирование всех чёрных объектов сделало бы финальную паузу пропорциональной куче — ровно того конкурентный дизайн и избегает. Гибридный барьер избавил даже от пересканирования стеков." },
    { label: "Освободит — эта гонка и есть причина, по которой в Go иногда нужны защитные вызовы runtime.KeepAlive", misconception: "KeepAlive решает другую проблему: финализаторы срабатывают, когда компилятор больше не видит живости у ещё используемого объекта. Гонку потерянного объекта полностью закрывает барьер; корректный GC никогда не освобождает достижимую память." },
    { label: "Чёрные объекты на время маркировки защищены от записи через page fault, и запись блокируется до конца цикла", misconception: "Go никогда не защищает кучу от записи ради GC — это приём некоторых компактирующих сборщиков. Записи идут на полной скорости через несколько лишних инструкций барьера; блокировать писателей на всю фазу маркировки — это многомиллисекундный стоп." },
  ]}
/>

## Пейсер, GOGC и GOMEMLIMIT — две ручки, два режима

Когда стартует цикл? **Пейсер** стремится закончить маркировку ровно к моменту, когда куча достигнет цели: примерно `живая куча × (1 + GOGC/100)` (плюс стеки и глобальные в современной формуле). С дефолтным `GOGC=100` сервис с 1 GiB живых данных запускает GC около 2 GiB — потолок памяти примерно *вдвое* больше живой кучи. Поднимите GOGC (200, 400) — циклы станут реже: меньше CPU на GC, больше RAM; опустите — обмен в другую сторону. Пейсер самокорректируется по обратной связи от прошлых циклов и мобилизует аллоцирующие горутины через mark assist, когда они его обгоняют, — поэтому у аллокационно-тяжёлого кода латентность деградирует *раньше*, чем память начинает пугать.

`GOMEMLIMIT` (Go 1.19+) — второй режим: **мягкий лимит на всю память рантайма** (куча, стеки, структуры рантайма — но не аллокации C). По мере приближения к нему GC работает агрессивнее, чем диктовал бы один GOGC, пытаясь удержаться ниже. Используйте его, когда настоящее ограничение — контейнер: `GOMEMLIMIT` на ~90–95% лимита пода превращает «OOMKilled в два ночи» в «больше GC под давлением». Режим отказа — пролог: **живая куча у самого лимита не оставляет GC ничего освобождать**, и он работает непрерывно. Рантайм ограничивает GC половиной CPU, гарантируя прогресс, — осознанное проектное решение, предотвращающее полный клин, но всё равно означающее, что сервис может законно тратить половину процессора на маркировку. Оставляйте запас: при живой куче 1,3 GiB лимит 1,4 GiB — ловушка; 2 GiB — бюджет.

<Infographic
  svg={memlimitHeadroom}
  label="Сравнение двух значений GOMEMLIMIT при одной и той же живой куче 1,3 GiB: потолок 1,4 GiB оставляет ~100 MiB запаса, гонит циклы GC впритык вплоть до потолка 50% CPU и поднимает p99 до ~900 мс; потолок 2,0 GiB оставляет ~700 MiB запаса, даёт пейсеру разнести циклы при ~2% CPU на GC и держит p99 на ~18 мс."
  caption="Меняется только потолок; лишние ~600 MiB запаса отделяют спираль смерти от настроенного сервиса — а графики памяти в обоих случаях выглядят одинаково."
  hue="var(--d-infra)"
/>

```go
// Честное наблюдение за GC в проде:
// GODEBUG=gctrace=1 печатает строку на каждый цикл в stderr —
// gc 412 @1572.337s 2%: 0.06+312+0.04 ms clock, ...,
//        1290->1310->1190 MB, 1400 MB goal, ...
// читается так: 2% CPU на GC с момента старта; 0.06 и 0.04 мс STW вокруг
// 312 мс конкурентной маркировки; куча 1290 MB на старте, живых 1190 после;
// цель 1400 — живое составляет 85% цели: сервис у края спирали.

debug.SetGCPercent(200)                    // GOGC, в рантайме
debug.SetMemoryLimit(1900 << 20)           // GOMEMLIMIT, байты
var m runtime.MemStats
runtime.ReadMemStats(&m)                   // m.NumGC, m.PauseNs, m.GCCPUFraction
```

## Мифы: нет компактизации, нет поколений — и почему

Две вещи, которые инженеры «знают» про GC и которые сборщик Go сознательно не делает. **Нет компактизации:** объекты никогда не переезжают после аллокации. Фрагментацию вместо этого сдерживает аллокатор с размер-классами (~70 классов; объект на 18 байт живёт в слоте на 24 байта, в спане, выделенном под этот класс, — внутренние потери ограничены в худшем случае). Неперемещающий дизайн покупает дешёвый и стабильный интероп (указатели, отданные в syscall или cgo, остаются валидными) и отсутствие фазы копирования, конкурирующей за пропускную способность памяти. **Нет поколений:** нет «ясель»; каждый цикл сканирует весь живой набор. Генерационная гипотеза — большинство объектов умирает молодыми — в Go в значительной мере *обработана заранее* escape-анализом: самые молодые и короткоживущие значения вообще не доходят до кучи, бесплатно умирая на стеках. Команда Go прототипировала генерационный сборщик и обнаружила, что выигрыш не оправдывает цену: поколениям нужен ещё более дорогой барьер записи для отслеживания указателей из старого в молодое, что усложняет конкурентный дизайн, удерживающий паузы под миллисекундой. Это компромиссы, а не пробелы — но они означают, что GC Go вознаграждает *маленький живой набор и низкую скорость аллокаций* и не даёт «ясель», за которыми можно спрятать дизайн с высоким churn.

<Quiz
  id="02-gc-mechanics-and-tuning-quiz-2"
  lessonSlug="02-gc-mechanics-and-tuning"
  lang="ru"
  question="Сервис с живой кучей 1,3 GiB работает с GOMEMLIMIT=1400MiB. Трафик обычный, утечек нет. Какой симптом в проде наиболее вероятен?"
  choices={[
    { label: "Циклы GC идут почти впритык, пытаясь удержаться под лимитом, и съедают вплоть до 50%-го потолка GC CPU — латентность сильно деградирует, хотя графики памяти выглядят нормально", correct: true },
    { label: "Сервис убивается OOM, как только куча пересекает 1400 MiB", misconception: "GOMEMLIMIT мягкий — рантайм отвечает более агрессивным GC, а не завершением. OOM killer ядра реагирует на лимит cgroup, поэтому мягкий лимит и должен стоять ниже него с настоящим запасом." },
    { label: "Рантайм начинает компактировать кучу, чтобы уместиться под лимит", misconception: "Сборщик Go никогда не перемещает объекты; компактизации, которую можно включить под давлением, не существует. Единственный рычаг рантайма — собирать чаще, что и порождает сжигание CPU." },
    { label: "Аллокации начинают возвращать nil по достижении лимита", misconception: "В Go нет отказа аллокатора — аллокация либо успешна, либо процесс умирает. Видимый сигнал дефицита памяти в Go — частота GC и время assist, но никогда не nil из new или make." },
  ]}
/>

</Explanation>

<DiagramFrame label="Один цикл GC как таймлайн: пауза меньше миллисекунды включает барьеры записи, серые объекты потоком идут через конкурентную маркировку параллельно с кодом, вторая пауза подтверждает завершение, sweep амортизируется в аллокации" hue="var(--d-backend)" caption="Пейсер запускает цикл около live×(1+GOGC/100); GOMEMLIMIT под давлением сдвигает старт раньше — вплоть до маркировки впритык с потолком 50% CPU. Точки — серое множество, вычерпываемое на ~25% GOMAXPROCS плюс mark assist; две полосы STW стоят лишь микросекунды.">
  <svg viewBox="0 0 460 150" width="460" height="150" style="display:block;margin:0 auto;max-width:100%;height:auto;">
    <rect x="10" y="50" width="56" height="40" rx="6" fill="var(--card-2)" stroke="var(--hairline-strong)" stroke-width="0.8" />
    <text x="38" y="67" text-anchor="middle" font-size="11" font-family="var(--font-body)" fill="var(--ink)">корни</text>
    <text x="38" y="81" text-anchor="middle" font-size="9" font-family="var(--font-mono)" fill="var(--muted)">стеки, глобалы</text>
    <rect x="92" y="42" width="12" height="56" rx="3" fill="var(--d-backend)" opacity="0.25" />
    <text x="98" y="32" text-anchor="middle" font-size="9" font-family="var(--font-mono)" fill="var(--muted)">STW №1</text>
    <text x="98" y="116" text-anchor="middle" font-size="8.5" font-family="var(--font-mono)" fill="var(--muted)">барьер вкл.</text>
    <rect x="356" y="42" width="12" height="56" rx="3" fill="var(--d-backend)" opacity="0.25" />
    <text x="362" y="32" text-anchor="middle" font-size="9" font-family="var(--font-mono)" fill="var(--muted)">STW №2</text>
    <text x="362" y="116" text-anchor="middle" font-size="8.5" font-family="var(--font-mono)" fill="var(--muted)">финал mark</text>
    <text x="230" y="32" text-anchor="middle" font-size="9.5" font-family="var(--font-mono)" fill="var(--muted)">конкурентный mark · 25% CPU + assist</text>
    <rect x="392" y="50" width="58" height="40" rx="6" fill="var(--card-2)" stroke="var(--hairline-strong)" stroke-width="0.8" />
    <text x="421" y="67" text-anchor="middle" font-size="11" font-family="var(--font-body)" fill="var(--ink)">sweep</text>
    <text x="421" y="81" text-anchor="middle" font-size="9" font-family="var(--font-mono)" fill="var(--muted)">лениво, при alloc</text>
    <path data-path d="M66 70 L392 70" fill="none" stroke="var(--hairline-2)" stroke-width="1.4" stroke-dasharray="4 4" />
    <PacketDot color="var(--d-backend)" durationMs={1500} size={8} />
    <PacketDot color="var(--d-backend)" durationMs={1500} delayMs={400} size={8} />
    <PacketDot color="var(--d-backend)" durationMs={1500} delayMs={800} size={8} />
    <text x="230" y="134" text-anchor="middle" font-size="9.5" font-family="var(--font-mono)" fill="var(--muted)">мутаторы продолжают работать · барьер записи затеняет перезаписанные указатели в серый</text>
  </svg>
</DiagramFrame>

<KeyTakeaway>GC Go — конкурентный трёхцветный mark-sweep: две паузы короче миллисекунды, настоящая цена платится конкурентно — ~25% CPU плюс mark assist, корректность под мутациями гарантирует гибридный барьер записи. GOGC задаёт запас кучи (по умолчанию потолок ≈ 2× живого); GOMEMLIMIT — мягкий потолок под контейнер, уходящий в спираль смерти без запаса: рантайм ограничивает GC половиной CPU, но половина CPU всё равно потеряна. Отсутствие компактизации и поколений — осознанно: яслями работает escape-анализ.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="02-gc-mechanics-and-tuning-retrieval"
  lang="ru"
  questions={[
    { q: "Пройдите один цикл GC: две паузы, что выполняется конкурентно и где живёт настоящая цена в CPU.", a: "Пейсер запускает цикл, когда куча приближается к живое×(1+GOGC/100) с поправкой на давление GOMEMLIMIT. STW-пауза №1 (десятки мкс) включает барьер записи на всех P. Дальше маркировка идёт конкурентно: корни (глобальные, стеки горутин) сереют, выделенные воркеры на ~25% GOMAXPROCS вычерпывают серое множество — сканируют указатели, серят ссылки, чернят объект, — а гибридный барьер записи затеняет указатели, затираемые или записываемые работающим кодом, чтобы ни один достижимый объект не остался белым. Горутины, аллоцирующие быстрее маркировки, мобилизуются в mark assist — здесь скорость аллокаций становится видимой пользователю латентностью. STW-пауза №2 подтверждает, что серой работы нет. Sweep ленивый, амортизирован в последующие аллокации. Субмиллисекундные паузы реальны, но цена — конкурентные 25% плюс assist: паузы обменяли на пропускную способность." },
    { q: "Когда брать GOGC, а когда GOMEMLIMIT, и в чём случай спирали смерти с мягким лимитом?", a: "GOGC настраивает обмен частоты на память: дефолтные 100 означают потолок кучи примерно вдвое больше живого набора; поднимайте, чтобы тратить RAM и экономить CPU на GC (батч-задачи, памяти много), опускайте, чтобы ужать след ценой CPU. GOMEMLIMIT (1.19+) — для вписывания в контейнер: мягкий потолок всей памяти рантайма, ставится на ~90–95% лимита пода, чтобы давление давало больше GC вместо OOM kill. Спираль: когда живая куча стоит у лимита, GC почти нечего освобождать, и циклы идут впритык; рантайм ограничивает GC половиной CPU ради прогресса, но сервис всё равно теряет до половины процессора, и латентность рушится при здоровых графиках памяти. Диагноз: GODEBUG=gctrace=1 (goal против live) или доля GC CPU. Лечение: настоящий запас — поднять лимит, уменьшить живой набор или и то и другое." },
  ]}
/>

<Recap lang="ru">
Сборщик Go — конкурентный трёхцветный mark-sweep: объекты переходят белый→серый→чёрный по мере вычерпывания от корней, цикл обрамляют две stop-the-world паузы в десятки–сотни микросекунд, а гибридный барьер записи — затеняющий затираемые указатели — гарантирует, что конкурентные мутации не спрячут достижимый объект; он же убрал пересканирование стеков из финальной паузы. Честная цена конкурентна: ~25% GOMAXPROCS во время маркировки плюс mark assist, напрямую облагающий аллокационно-тяжёлые горутины, — скорость аллокаций превращается в латентность раньше, чем память выглядит тревожно. Пейсер запускает циклы около живое×(1+GOGC/100) — при дефолте потолок примерно вдвое больше живой кучи. GOMEMLIMIT добавляет мягкий потолок всей памяти под контейнеры; ставьте его с запасом, потому что живая куча у лимита заставляет циклы идти впритык с проектным потолком 50% CPU — сервис из пролога тратил половину мозга на маркировку при зелёных дашбордах. Плотные по указателям структуры платят дважды (трафик барьера плюс сканирование) — это механический довод за value-ориентированные данные. А два знаменитых отсутствия — выбор: нет компактизации (спаны с размер-классами ограничивают фрагментацию; указатели валидны для syscall и cgo) и нет поколений (escape-анализ и так даёт молодым умирать на стеках; прототип генерационного сборщика не окупил более тяжёлый барьер). GC вознаграждает ровно две вещи: маленький живой набор и низкую скорость аллокаций. Теперь, потянувшись к ручке GC, сначала проверьте долю GC CPU и запас между живой кучей и GOMEMLIMIT — ответ обычно указывает обратно на скорость аллокаций, а не на тюнинг.
</Recap>
