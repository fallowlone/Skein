---
concepts:
- go-interfaces
deepensInto: []
estMin: 19
lang: ru
lessonType: topic
level: senior
order: 3
prereqs: []
slug: "03-interfaces"
sources:
- https://go.dev/doc/effective_go
- https://go.dev/doc/faq
- https://go.dev/blog/laws-of-reflection
spiral: []
status: ready
summary: 'Интерфейсы Go удовлетворяются неявно и хранятся как пара (тип, значение) — поэтому nil *T, возвращённый как error, делает err != nil истинным: классический прод-баг. Интерфейсы малы; принимай интерфейсы, возвращай структуры; диспетчеризация блокирует инлайнинг и гонит в кучу.'
title: 'Интерфейсы: неявное удовлетворение, ловушка typed-nil и цена косвенности'
track: "go"
unit: "01-language-core"
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";
import Infographic from "~/components/diagram/Infographic.astro";
import indirectionCost from "./infographics/indirection-cost.svg?raw";

<Hook>
Деплой-гейт проверял здоровье сервиса: `if err := check(); err != nil { rollback() }`. Через один рефакторинг каждый деплой стал откатываться — включая те, где сервис был совершенно здоров. Виновник: `check()` был объявлен возвращающим конкретный тип `*HealthError`, а обёртка присваивала результат переменной типа `error`. На здоровом пути `check()` возвращал nil `*HealthError` — но, попав в интерфейс `error`, этот nil-указатель стал интерфейсным значением с заполненным словом *типа* (`*HealthError`) и nil-словом *данных*. Интерфейс равен nil, только когда **оба** слова nil, поэтому `err != nil` было истинно без единой ошибки в поле зрения. Три релиза откатились, прежде чем кто-то напечатал `fmt.Sprintf("%#v", err)` и увидел в ответ `(*HealthError)(nil)`. FAQ называет это самым частым вопросом про ошибки в Go; продакшен называет это вторником.
</Hook>

<Crux>Интерфейсное значение — пара (тип, значение), удовлетворяется неявно. nil *T внутри — НЕ nil. Держите интерфейсы маленькими.</Crux>

<Explanation>

## Неявное удовлетворение: контракт, который никто не подписывает

Тип в Go удовлетворяет интерфейс, просто *имея методы* — без ключевого слова `implements`, без декларации, без импорта. Если у вашей структуры есть `Read(p []byte) (int, error)`, она — `io.Reader`, даже если `io` никогда не импортировался, а интерфейс написан годами позже. Это структурная типизация с проверкой на этапе компиляции, и она переворачивает стрелку зависимости: **потребитель определяет интерфейс, который ему нужен**, а любой производитель, который случайно подходит, — подходит. Вы можете объявить однометодный интерфейс в *своём* пакете — и типы стандартной библиотеки уже его удовлетворяют: абстракция задним числом натягивается на чужой код без единого изменения в нём. Поскольку никто не декларирует намерение, идиома «обещаю, что этот тип удовлетворяет тот интерфейс, завали сборку, если нет» — compile-time-утверждение:

```go
type Store interface {
    Get(ctx context.Context, key string) ([]byte, error)
}

// Compile-time доказательство: *RedisStore удовлетворяет Store. Ничего не стоит в рантайме.
var _ Store = (*RedisStore)(nil)
```

## Пара (тип, значение) — и ловушка typed-nil

Под капотом интерфейсное значение — **два машинных слова** (16 байт на 64-битной платформе): указатель на метаданные типа и методов — *itab* (interface table, таблица интерфейса), хранящий конкретный тип и разрешённые адреса методов, — и указатель на данные. Присваивание конкретного значения интерфейсу заполняет оба слова. Это представление объясняет всё странное в интерфейсах. Равенство сравнивает оба слова. Утверждение типа читает слово типа. И ловушка из вступления: интерфейс равен `nil`, **только когда оба слова nil**. Положите nil `*HealthError` в `error` — слово типа заполнится (`*HealthError`), слово данных останется nil, и сам интерфейс *не* nil:

```go
type HealthError struct{ Reason string }

func (e *HealthError) Error() string { return e.Reason }

func check() *HealthError {
    return nil // здоровый путь: nil-указатель КОНКРЕТНОГО типа
}

func main() {
    var err error = check()         // (тип=*HealthError, данные=nil)
    fmt.Println(err == nil)         // false — слово типа заполнено!
    fmt.Printf("%#v\n", err)        // (*main.HealthError)(nil) — вот улика
}
```

Правило, которое это предотвращает: **функции, которые могут отказать, возвращают интерфейсный тип `error`, и никогда — конкретный указательный тип ошибки**. Внутри возвращайте литеральный `nil`, а не возможно-nil типизированную переменную. Когда дебажите «ненулевой nil», `%#v` (или `%T`) обнажает слово типа, которое прячет `%v`. Это не баг компилятора, который нужно обойти, — это прямое следствие двухсловного представления, и стоит увидеть пару, как поведение становится единственно возможным.

## Маленькие интерфейсы и «принимай интерфейсы, возвращай структуры»

Как понять, когда вводить интерфейс и каким его делать? Стандартная библиотека отвечает на это последовательно: у самых используемых интерфейсов один метод: `io.Reader`, `io.Writer`, `fmt.Stringer`. Это намеренно — *чем больше интерфейс, тем слабее абстракция*. Однометодный интерфейс удовлетворяется сотнями типов и компонуется в большие (`io.ReadWriteCloser` — три встроенных однометодных интерфейса); двенадцатиметодный удовлетворяется ровно одной структурой плюс моком, который вы для неё написали, — а значит, это не абстракция, это заголовочный файл. Парное правило: **принимай интерфейсы, возвращай структуры**. Параметры с типами маленьких интерфейсов просят минимальную способность — берите `io.Reader`, а не `*os.File`, и функция заработает на файлах, сетевых соединениях, буферах и тестовых строках одинаково. Возвраты с конкретными типами сохраняют полный набор методов и оставляют *вызывающим* решать, что абстрагировать; возврат интерфейса прячет методы, которые могут понадобиться, и замораживает ваш API вокруг сегодняшней догадки. (Известное исключение: возвращайте интерфейс `error` — почему он обязателен, см. предыдущий раздел.)

```go
// Запрашивает минимум, работает с любым читаемым источником.
func CountLines(r io.Reader) (int, error) { ... }

// Возвращает конкретный тип: вызывающие получают полный API *Parser
// и могут сами присвоить его любому интерфейсу, который определят.
func NewParser(opts ...Option) *Parser { ... }
```

**Переключатели типов** — инструмент инспекции, когда одно значение может быть несколькими вещами: `switch v := x.(type)` ветвится по динамическому типу и связывает `v` с правильным статическим типом в каждой ветке. Идиоматично для декодирования протоколов и проверки способностей (реализует ли этот `io.Writer` ещё и `Flusher`?); запах — когда растущий switch по вашим собственным типам подменяет то, что должно было быть методом интерфейса.

## Что стоит косвенность — и когда это важно

Вызов через интерфейс — динамическая диспетчеризация: загрузить адрес метода из itab, прыгнуть. Сам вызов — пара наносекунд, близко к прямому, и редко является проблемой. Настоящие издержки — второго порядка. **Блокируется инлайнинг**: компилятор не может встроить вызываемого, которого не может идентифицировать, поэтому однострочный геттер за интерфейсом остаётся настоящим вызовом, и всё, что инлайнинг разблокировал бы (свёртка констант, удаление мёртвого кода, значения в регистрах), отключено. **Побег в кучу**: помещение значения в интерфейс, как правило, вынуждает аллоцировать его в куче, потому что слово данных интерфейса хранит указатель — передайте `int` как `interface{}` в горячем цикле, и вы можете аллоцировать на каждой итерации: это давление на GC, а не просто наносекунды. Честные числа: прямой вызов ~1–2 нс, интерфейсный ~2–4 нс, но потерянный инлайн плюс вынужденная аллокация превращают бесплатную операцию в ~25–50 нс с амортизацией GC — разница в 10–30× в самых горячих циклах и ничто вне их. Компилятор **девиртуализует**, когда может доказать конкретный тип (а profile-guided optimization, Go 1.21+, девиртуализует горячие вызовы по продакшен-профилям), но рассчитывать на это через границы пакетов не стоит. Инженерная позиция: проектируйте интерфейсы на архитектурных швах — хранилище, транспорт, часы, — где частота вызовов per-request, а поэлементные горячие пути (кодеки, парсеры, тугие циклы по миллионам элементов) держите конкретными или дженериковыми; и измеряйте профилировщиком, прежде чем сплющивать любую абстракцию во имя скорости.

<Infographic
  svg={indirectionCost}
  label="Столбчатая диаграмма цены одного вызова в наносекундах: прямой вызов — около 2 нс, вызов через интерфейс — около 4 нс, но вызов с потерей инлайна и вынужденным escape в кучу подскакивает до ~50 нс."
  caption="Сам диспатч почти бесплатен (~2 против ~4 нс); настоящий счёт — второго порядка: потеря инлайна плюс упаковка значения в кучу превращают бесплатный доступ в ~50 нс — удар в 10-30x на горячем цикле."
  hue="var(--d-infra)"
/>

<Inset kind="why" lang="ru">
Почему язык просто не сделает typed-nil-интерфейс равным nil? Потому что это сломало бы честность представления. Интерфейс хранит реальную информацию — «во мне лежит *HealthError*» — и она легитимно полезна: у nil *bytes.Buffer всё ещё есть методы, работающие на nil-получателях, и код может звать их через интерфейс. Схлопывание (тип, nil) в nil сделало бы поведение значения зависящим от того, проверили ли вы его на nil первым, а сохранение-и-извлечение nil-указателя — теряющим информацию: положили типизированный nil, достали нетипизированное ничто. Go выбрал последовательное правило — nil значит «оба слова пусты» — и переложил бремя на одну конвенцию: возвращайте интерфейс error, возвращайте литеральный nil. Ловушка существует, но это хотя бы стабильная, выучиваемая ловушка, а не особый случай, который лжёт о содержимом значения.
</Inset>

<Quiz
  id="03-interfaces-quiz-1"
  lessonSlug="03-interfaces"
  lang="ru"
  question="func check() *HealthError возвращает nil при успехе. Вызывающий пишет var err error = check(). Чему равно err != nil и почему?"
  choices={[
    { label: "true — интерфейс теперь хранит (тип=*HealthError, данные=nil), а интерфейс равен nil, только когда ОБА слова nil; функция должна была возвращать интерфейсный тип error с литеральным nil", correct: true },
    { label: "false — nil есть nil, через какой бы тип он ни проехал", misconception: "Присваивание конкретного значения интерфейсу заполняет слово типа значением *HealthError, даже когда указатель nil. Пара (тип заполнен, данные nil) — не нулевое интерфейсное значение, так что сравнение с nil даёт неравенство — err != nil истинно. Слово типа несёт реальную информацию." },
    { label: "Это не скомпилируется: нельзя присвоить *HealthError переменной error", misconception: "Компилируется именно потому, что у *HealthError есть метод Error() string, и он удовлетворяет error неявно — без деклараций. Та самая неявность, которая делает интерфейсы Go гибкими, и впускает эту ловушку." },
    { label: "true, но лишь потому, что сравнение интерфейса с nil всегда даёт один и тот же результат", misconception: "Сравнение интерфейса с nil осмысленно и бывает и истинным, и ложным в зависимости от содержимого: переменная error, которой присвоен литеральный nil или которая не присваивалась, равна nil. Результат здесь вызван именно заполненным словом типа, а не каким-то общим правилом интерфейсных сравнений." },
  ]}
/>

<Quiz
  id="03-interfaces-quiz-2"
  lessonSlug="03-interfaces"
  lang="ru"
  question="Профилировщик показывает горячий цикл, зовущий однострочный геттер через интерфейс с аллокацией на каждой итерации. Цикл обрабатывает 50 миллионов элементов. Какова доминирующая цена и правильный фикс?"
  choices={[
    { label: "Потерянный инлайнинг плюс побег в кучу: интерфейсный вызов не инлайнится, а упаковка значения аллоцирует — сделайте горячий путь конкретным (или дженериковым) и держите интерфейсы на архитектурном шве, где вызовы per-request", correct: true },
    { label: "Сам lookup в itab; закэшируйте указатель метода в локальной переменной, чтобы его пропустить", misconception: "Диспетчеризация — пара наносекунд, и itab уже кэширован рантаймом — ручное кэширование значений-методов почти ничего не даёт. Настоящие издержки второго порядка: компилятор не может инлайнить сквозь интерфейс, а слово данных гонит значения в кучу — аллокация плюс давление на GC." },
    { label: "Ничего значимого: интерфейсные вызовы в современном Go стоят столько же, сколько прямые", misconception: "Сырой вызов действительно близок (~2–4 нс против ~1–2 нс), но это не весь счёт. Заблокированный инлайнинг отменяет последующие оптимизации, а упакованные значения аллоцируют; на 50 млн итераций бесплатный встроенный доступ, ставший вызовом-плюс-аллокацией, — регрессия горячего цикла в 10–30x, которую профилировщик показывает как время GC." },
    { label: "Интерфейсы всегда слишком медленны для циклов; уберите все интерфейсы из кодовой базы", misconception: "Вне поэлементных горячих путей цена несущественна — интерфейсный вызов per-request в сервисе на тысячи QPS — это шум. Сплющивание каждой абстракции меняет реальную архитектуру на неизмеримые выигрыши. Дисциплина такова: интерфейсы на швах, конкретный или дженериковый код в измеренных горячих циклах." },
  ]}
/>

</Explanation>

<FlowDiagram
  label="Помещение конкретного значения в интерфейс строит двухсловную пару (тип, данные); вызовы методов диспетчеризуются через itab, а nil-указатель с заполненным словом типа — не nil-интерфейс"
  hue="var(--d-backend)"
  perRow={3}
  nodes={[
    { id: "concrete", label: "Конкретное значение", sub: "*HealthError" },
    { id: "conv", label: "Конверсия", sub: "присваивание интерфейсу" },
    { id: "pair", label: "Пара (тип, данные)", sub: "itab + указатель, 16 Б" },
    { id: "dispatch", label: "Диспетчеризация itab", sub: "вызов через таблицу методов" },
    { id: "typednil", label: "Typed nil", sub: "тип есть, данные nil" },
    { id: "notnil", label: "err != nil: true", sub: "nil — оба слова пусты" },
  ]}
  edges={[
    { from: "concrete", to: "conv", label: "неявно" },
    { from: "conv", to: "pair", label: "заполняет 2 слова" },
    { from: "pair", to: "dispatch", label: "вызов метода" },
    { from: "conv", to: "typednil", label: "nil *T внутрь" },
    { from: "typednil", to: "notnil", label: "сравнение" },
  ]}
  caption="Удовлетворение структурное и неявное: иметь методы — и есть контракт. Интерфейсное значение — два слова: тип/itab и данные. Диспетчеризация грузит адрес метода из itab (дёшево), но блокирует инлайнинг и может выгнать значение в кучу. nil *T заполняет слово типа, и интерфейс не nil — возвращайте интерфейс error и литеральный nil."
/>

<KeyTakeaway>Интерфейсы удовлетворяются наличием методов — их определяет потребитель, а `var _ I = (*T)(nil)` доказывает это на компиляции. Значение — пара (тип, данные): nil только когда оба слова nil, поэтому никогда не возвращайте конкретные указательные типы ошибок. Держите интерфейсы в один–три метода, принимайте интерфейсы и возвращайте структуры, а измеренные горячие циклы держите конкретными — диспетчеризация блокирует инлайнинг и упаковывает значения в кучу.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="03-interfaces-retrieval"
  lang="ru"
  questions={[
    { q: "Объясните баг typed-nil-в-интерфейсе: механизм, симптом и конвенцию, которая его предотвращает.", a: "Интерфейсное значение — два машинных слова: слово типа/itab (конкретный тип плюс разрешённые адреса методов) и слово данных (указатель на значение). Оно равно nil, только когда ОБА слова nil. Если функция объявлена возвращающей конкретный указательный тип вроде *HealthError и возвращает nil, присваивание результата переменной error заполняет слово типа (*HealthError), а слово данных остаётся nil — и err != nil истинно без настоящей ошибки: ветки отказа (откаты, 500-ки, ретраи) срабатывают на здоровых результатах. Диагностика: печать через %#v или %T, которая показывает (*HealthError)(nil) там, где %v выводит что-то похожее на nil. Профилактика — конвенция, а не фича компилятора: функции, которые могут отказать, возвращают ИНТЕРФЕЙСНЫЙ тип error, никогда не конкретный указательный тип ошибки, и возвращают литеральный nil при успехе, а не возможно-nil типизированную переменную. Go сохраняет это поведение, потому что слово типа несёт реальную информацию — методы легитимно работают на nil-получателях через интерфейс, — и схлопывание (тип, nil) в nil лгало бы о содержимом значения." },
    { q: "Что дают маленькие интерфейсы и «принимай интерфейсы, возвращай структуры», и сколько на самом деле стоит интерфейсная косвенность на горячем пути?", a: "Неявное удовлетворение переворачивает зависимости: потребитель определяет минимальный интерфейс (часто один метод, как io.Reader), и любой существующий тип с этим методом его удовлетворяет — абстракции натягиваются на чужой код задним числом. Маленькие интерфейсы удовлетворяются множеством типов и компонуются встраиванием (io.ReadWriteCloser); большие удовлетворяются одной структурой плюс её моком и не абстрагируют ничего. Принимая интерфейсы, вы просите у вызывающих минимальную способность; возвращая конкретные структуры — сохраняете полный набор методов и оставляете вызывающим выбор абстракции, с постоянным исключением: отказы возвращают интерфейс error. Издержки: сама динамическая диспетчеризация ~2–4 нс против ~1–2 нс прямого вызова — обычно шум, — но она блокирует инлайнинг (отменяя свёртку констант и оптимизации, которые он разблокирует), а помещение значений в интерфейсы обычно вынуждает побег в кучу, то есть аллокацию плюс давление на GC на каждую операцию. В поэлементном горячем цикле это регрессия в 10–30x; per-request — несущественно. Компилятор девиртуализует доказуемые случаи, а PGO (Go 1.21+) девиртуализует горячие точки по профилям, но дисциплина такова: интерфейсы на архитектурных швах, конкретный или дженериковый код в измеренных горячих циклах." },
  ]}
/>

<Recap lang="ru">
Интерфейсы Go удовлетворяются неявно: тип с нужными методами удовлетворяет интерфейс без деклараций, что позволяет потребителю определить нужную ему абстракцию и натянуть её на типы, которыми он не владеет; compile-time-утверждение var _ I = (*T)(nil) документирует и закрепляет это. В рантайме интерфейсное значение — два слова: указатель itab с конкретным типом и разрешёнными адресами методов и указатель данных, — и всё странное поведение следует из этой пары. Классика продакшена, ловушка typed-nil: функция, объявленная возвращающей *HealthError, возвращает nil, вызывающий кладёт его в error, слово типа заполняется при пустом слове данных, и err != nil истинно на здоровом пути — деплой-гейты откатывают рабочие релизы, пока кто-то не напечатает %#v и не увидит типизированный nil. Конвенция-предохранитель: возвращайте интерфейс error и литеральный nil. Давление дизайна — в сторону маленьких интерфейсов: у io.Reader и io.Writer один метод, они компонуются встраиванием и удовлетворяются сотнями типов, тогда как двенадцатиметодный интерфейс абстрагирует ровно свою единственную реализацию, — и в сторону «принимай интерфейсы, возвращай структуры»: параметры просят минимальную способность, возвраты сохраняют конкретный набор методов, чтобы вызывающие абстрагировали как хотят. Переключатели типов ветвятся по динамическому типу для декодирования и проверки способностей. Цена косвенности — второго порядка: диспетчеризация через itab стоит пару наносекунд, но блокирует инлайнинг и упаковывает значения в кучу, так что бесплатный встроенный доступ может стать вызовом плюс аллокацией — размах в 10–30x на миллионах итераций и шум per-request. Девиртуализация и PGO возвращают часть случаев; дисциплина — интерфейсы на архитектурных швах, конкретный или дженериковый код в измеренных горячих циклах. Теперь, когда видите функцию, возвращающую конкретный `*FooError` вместо `error`, вы знаете ловушку, в которую она идёт, — и однострочный фикс, который предотвращает три откаченных релиза.
</Recap>
