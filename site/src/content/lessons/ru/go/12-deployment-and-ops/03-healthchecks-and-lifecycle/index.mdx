---
concepts:
- go-healthchecks
deepensInto: []
estMin: 18
lang: ru
lessonType: topic
level: senior
order: 3
prereqs: []
slug: "03-healthchecks-and-lifecycle"
sources:
- https://kubernetes.io/docs/tasks/configure-pod-container/configure-liveness-readiness-startup-probes/
- https://kubernetes.io/docs/concepts/workloads/pods/pod-lifecycle/
- https://pkg.go.dev/net/http
spiral: []
status: ready
summary: 'Liveness перезапускает, readiness маршрутизирует, startup даёт фору на старте — проверка зависимостей в liveness превращает блип базы в шторм рестартов. Настоящий readiness пингует нужное сервису, гаснет первым на SIGTERM, а весь дрейн умещается в terminationGracePeriodSeconds.'
title: 'Health-пробы и жизненный цикл: liveness, readiness, startup и последовательность остановки без потерянных запросов'
track: "go"
unit: "12-deployment-and-ops"
---

import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import SequenceDiagram from "~/components/diagram/SequenceDiagram.astro";

<Hook>
Файловер базы занял 28 секунд. Вызванный им инцидент — 40 минут. Каждый под флота гонял liveness-пробу по /health — хендлеру, который кто-то однажды *улучшил* пингом базы, потому что больше проверок звучало надёжнее. Когда начался файловер, двести совершенно здоровых подов стали проваливать liveness одновременно; три промаха спустя кубелеты убили их все. У инцидента появился второй акт: сотни перезапускающихся процессов с холодными кэшами одновременно переподключаются к только что ожившей базе — лавина коннектов, которая снова её роняет, что снова проваливает liveness, что снова перезапускает флот. 28-секундный блип, закольцованный в 40 минут теми самыми пробами, что должны были добавить надёжности. Постмортем уложился в одно предложение: liveness отвечает «перезапускать ли меня», readiness — «слать ли мне трафик», а проверку базы привязали к тому вопросу, где ответом служит рестарт. У другой команды в том же квартале был зеркальный баг — без startup-пробы, с 90-секундной миграцией схемы на старте и liveness, взводящимся на тридцатой секунде: CrashLoopBackOff, навсегда, на каждом деплое. Kubernetes добросовестно исполняет любую ошибку, которую вы закодируете.
</Hook>

<Crux>Liveness — перезапусти меня, readiness — шли трафик. Перепутай — и блип базы перезапустит флот. На остановке снимай readiness первым.</Crux>

<Explanation>

## Три пробы — три разных вопроса

Liveness спрашивает: сломан ли процесс без шанса на самопочинку — дедлок, клин, порча состояния? Провал означает рестарт контейнера кубелетом, поэтому хендлер обязан проверять *только* то, что рестарт чинит: цикл обработки отвечает — и всё. Зависимости он не проверяет никогда: рестарт не чинит базу — он лишь конвертирует любой сбой зависимости в шторм рестартов на весь флот, ровно как в крючке. Readiness спрашивает: слать ли этому поду трафик *прямо сейчас*? Провал убирает под из эндпоинтов сервиса — обратимо, дёшево и ровно то, что нужно при сбое зависимости, перегрузке или дрейне. Вот где живут проверки зависимостей. Startup спрашивает: закончилась ли загрузка? Пока она не пройдена, liveness и readiness приостановлены — бюджет на миграции и прогрев кэшей равен failureThreshold, умноженному на periodSeconds, а без неё liveness убивает медленный старт на своём пороге и зацикливается навсегда.

```yaml
livenessProbe:
  httpGet: { path: /healthz, port: 8080 }
  periodSeconds: 10
  failureThreshold: 3        # 30 с настоящего клина до рестарта
readinessProbe:
  httpGet: { path: /readyz, port: 8080 }
  periodSeconds: 5
  failureThreshold: 2        # вывод из ротации за ~10 с
startupProbe:
  httpGet: { path: /healthz, port: 8080 }
  periodSeconds: 5
  failureThreshold: 36       # до 180 с на загрузку, прежде чем взведётся liveness
```

<Quiz
  id="03-healthchecks-quiz-1"
  lessonSlug="03-healthchecks-and-lifecycle"
  lang="ru"
  question="Ваша liveness-проба зовёт хендлер, который пингует базу. У базы 30-секундный файловер. Что произойдёт с флотом?"
  choices={[
    { label: "Все поды одновременно провалят liveness, кубелеты массово перезапустят здоровые процессы, и блип станет штормом рестартов с холодными кэшами и лавиной переподключений", correct: true },
    { label: "Ничего — failureThreshold поглотит 30-секундный блип", misconception: "С ходовыми настройками 3 x 10 с 30-секундный сбой сидит ровно на линии убийства: часть флота перезапускается, рестарты добавляют нагрузку переподключений и удлиняют блип для остальных — шторм кормит сам себя. Пороги покупают секунды, а не иммунитет от категориальной ошибки." },
    { label: "Поды выведут из балансировщика до восстановления базы, потом вернут", misconception: "Это семантика readiness — и именно такого поведения здесь и хочется. Провал liveness эндпоинты не трогает; он перезапускает контейнер. Весь баг — в том, что проверку зависимости привязали к пробе, чей ответ — рестарт." },
    { label: "Кубелет поймёт, что отказ вызван зависимостью, и будет ретраить с бэкоффом вместо рестарта", misconception: "Кубелет понятия не имеет, что проверяет ваш хендлер, — проваленная проба есть проваленная проба. После failureThreshold промахов подряд он перезапустит контейнер независимо от причины; именно это и делает проверки зависимостей в liveness опасными." },
  ]}
/>

## Настоящий readiness на Go

Readiness-хендлер, безусловно возвращающий 200, — это театр: он сообщает Kubernetes, что под готов обслуживать, когда сам под не знает ничего. Настоящий readiness проверяет то, без чего сервис не обслужит *ни один* запрос, с таймаутом, и несёт флаг дрейна:

```go
var ready atomic.Bool // станет true после завершения загрузки

mux.HandleFunc("GET /healthz", func(w http.ResponseWriter, r *http.Request) {
	w.WriteHeader(http.StatusOK) // liveness: процесс жив — и ничего больше
})

mux.HandleFunc("GET /readyz", func(w http.ResponseWriter, r *http.Request) {
	if !ready.Load() {
		http.Error(w, "draining", http.StatusServiceUnavailable)
		return
	}
	ctx, cancel := context.WithTimeout(r.Context(), 1*time.Second)
	defer cancel()
	if err := db.PingContext(ctx); err != nil { // зависимость, нужная каждому запросу
		http.Error(w, "db unreachable", http.StatusServiceUnavailable)
		return
	}
	w.WriteHeader(http.StatusOK)
})
```

Дисциплина режет в обе стороны: сюда входят только *жёсткие* зависимости — те, без которых падает каждый запрос. Вписать в readiness необязательный кэш или некритичный даунстрим — значит позволить одному мигающему сайдкару вывести весь флот из ротации; необязательные зависимости деградируют ответы, а не «разготавливают» поды. И держите таксономию эндпоинтов в порядке: /healthz тривиален и существует для кубелета, /readyz проверяет зависимости и ворота трафика, /debug/pprof — для инженеров и *никогда* не публичен: импорт net/http/pprof побочным эффектом регистрирует хендлеры на DefaultServeMux — так профайлеры и оказываются торчащими в интернет; отдавайте его на отдельном localhost- или внутрикластерном листенере.

<Inset kind="why" lang="ru">
Почему проверки зависимостей достаются readiness и никогда — liveness? Потому что два ответа различаются обратимостью. «Не готов» — это смена состояния без побочного ущерба: под остаётся тёплым, сохраняет кэши и соединения и возвращается в строй в момент, когда зависимость оживает. Рестарт уничтожает состояние процесса и добавляет нагрузку переподключений ровно в момент наибольшей слабости зависимости — он превращает частичный отказ в больший. Проверяйте зависимости там, где лекарство — подождать, а не там, где лекарство — убить.
</Inset>

## Последовательность остановки и бюджет грейса

Завершение — это распределённое рукопожатие, и пробы — его половина. На SIGTERM удаление из эндпоинтов распространяется асинхронно — балансировщики шлют новые запросы ещё несколько секунд. Сервер, мгновенно перестающий принимать соединения, отдаёт connection refused живому трафику на каждом деплое. Правильный порядок: сначала провалить readiness, переждать распространение, затем дрейнить — прямая спираль в урок graceful shutdown из go/03, теперь с кластерной хореографией:

```go
<-ctx.Done()                 // SIGTERM от кубелета
ready.Store(false)           // 1. /readyz теперь 503 — началось удаление из эндпоинтов
time.Sleep(5 * time.Second)  // 2. поглощаем лаг распространения
shutdownCtx, cancel := context.WithTimeout(context.Background(), 20*time.Second)
defer cancel()
_ = srv.Shutdown(shutdownCtx) // 3. дрейним запросы в полёте
```

Арифметика не обсуждается: terminationGracePeriodSeconds (по умолчанию 30) должен покрыть сон на распространение плюс дедлайн дрейна плюс запас — по его истечении SIGKILL приходит безусловно и забирает недоделанный дрейн с собой. Пять плюс двадцать оставляют пять секунд запаса внутри дефолта — а если ваш самый медленный легитимный запрос идёт 25 секунд, дефолтный бюджет уже сломан: поднимайте грейс до 40, а не сбривайте сон. Тайминг проб входит в ту же арифметику с другой стороны: readiness переключается за failureThreshold, умноженный на periodSeconds, то есть при 2 x 5 с балансировщик в худшем случае *начнёт* убирать вас секунд через десять после флипа — сон покрывает лаг от контроллера до дата-плейна, каденс пробы покрывает обнаружение.

<Quiz
  id="03-healthchecks-quiz-2"
  lessonSlug="03-healthchecks-and-lifecycle"
  lang="ru"
  question="Почему на SIGTERM флип readiness обязан случиться до srv.Shutdown, и зачем между ними сон?"
  choices={[
    { label: "Удаление из эндпоинтов распространяется асинхронно — флип его запускает, сон поглощает лаг, и только потом безопасно перестать принимать соединения", correct: true },
    { label: "Shutdown и так отказывает новым соединениям, порядок чисто косметический", misconception: "Shutdown закрывает листенеры немедленно — это и есть проблема, а не решение: балансировщики маршрутизируют ещё секунды после SIGTERM, и закрытый листенер превращает этот запоздалый трафик в connection refused на каждом деплое." },
    { label: "Сон даёт запросам в полёте время завершиться до дрейна", misconception: "Дрейн запросов в полёте — собственная работа Shutdown, ограниченная дедлайном его контекста. Сон закрывает совсем другую дыру: секунды, которые нужны кластеру, чтобы перестать слать новый трафик поду, уже решившему уйти." },
    { label: "Проваленный readiness велит кубелету пропустить дедлайн SIGKILL", misconception: "SIGKILL не пропускает ничто. Когда terminationGracePeriodSeconds истекает, он приходит безусловно — именно поэтому флип-плюс-сон-плюс-дрейн обязаны помещаться внутрь с запасом." },
  ]}
/>

</Explanation>

<SequenceDiagram
  label="Жизненный цикл проб во времени: фора startup, ворота readiness, затем рукопожатие SIGTERM с лагом распространения"
  hue="var(--d-backend)"
  actors={["kubelet", "Pod", "Endpoints"]}
  messages={[
    { from: "kubelet", to: "Pod", label: "startup /healthz" },
    { from: "Pod", to: "kubelet", label: "200 после загрузки" },
    { from: "kubelet", to: "Pod", label: "readyz: деп ок?" },
    { from: "Pod", to: "kubelet", label: "200, пул отпингован" },
    { from: "kubelet", to: "Endpoints", label: "под добавлен" },
    { from: "kubelet", to: "Pod", label: "SIGTERM" },
    { from: "Pod", to: "kubelet", label: "readyz 503 первым" },
    { from: "kubelet", to: "Endpoints", label: "удаление ползёт" },
    { from: "Endpoints", to: "Pod", label: "хвост трафика ~5 с" },
  ]}
  caption="Startup приостанавливает остальные пробы, пока идёт загрузочная работа; readiness привязывает членство в эндпоинтах к реальным зависимостям. На SIGTERM флип readiness идёт первым, сон на распространение поглощает окно запоздалого трафика, затем Shutdown дрейнит — всё внутри terminationGracePeriodSeconds, потому что SIGKILL не договаривается."
/>

<KeyTakeaway>Liveness перезапускает, readiness маршрутизирует, startup даёт фору на загрузку — а проверки зависимостей живут только в readiness, где ответом служит обратимый вывод из ротации, а не рестарт, усиливающий сбой. Настоящий readiness пингует жёсткие зависимости с таймаутом и по совместительству служит флагом дрейна: на SIGTERM — 503, сон на распространение эндпоинтов, затем Shutdown — и вся последовательность в бюджете terminationGracePeriodSeconds. /healthz тривиален, /readyz честен, /debug/pprof — никогда не публичен.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="03-healthchecks-retrieval"
  lang="ru"
  questions={[
    { q: "Назови точную семантику трёх проб и продовую историю отказа за каждым правилом подключения.", a: "Liveness: провал перезапускает контейнер, поэтому проверять можно только то, что рестарт чинит, — заклинивший или задедлоченный процесс — и никогда зависимости; проверка базы в liveness превратила 28-секундный файловер в 40-минутный шторм рестартов: двести здоровых подов убили одновременно, и лавина переподключений с холодными кэшами заново уронила восстанавливающуюся базу. Readiness: провал убирает под из эндпоинтов сервиса — обратимо и без потери тепла, — поэтому проверки жёстких зависимостей с таймаутами живут здесь; он же гаснет при перегрузке и дрейне. Startup: приостанавливает обе другие пробы до первого успеха с бюджетом failureThreshold, умноженным на periodSeconds; без неё 90-секундная миграция встречает 30-секундный порог liveness, и под уходит в CrashLoopBackOff на каждом деплое, навсегда. Кубелет никогда не знает, почему проба провалилась, — вся семантика в том, что вы подключили к какому вопросу." },
    { q: "Пройди последовательность от SIGTERM до выхода с арифметикой грейс-бюджета для сервиса, чей самый медленный легитимный запрос идёт 20 секунд.", a: "На SIGTERM: первое — ready=false, чтобы /readyz отдавал 503: начинается удаление из эндпоинтов, но обнаружение занимает failureThreshold на periodSeconds, а распространение по дата-плейну — ещё секунды. Второе — сон около 5 секунд, поглощающий этот лаг; пропустить его — получить connection refused для трафика, который балансировщик ещё шлёт. Третье — srv.Shutdown с дедлайном контекста не меньше самого медленного запроса — здесь 20 секунд — для дрейна работы в полёте; затем выход. Бюджет: 5 на распространение + 20 на дрейн + 5 запаса = 30 — ровно дефолтный terminationGracePeriodSeconds без всякого люфта на занятой ноде, поэтому поднимайте его до 35-40, а не сбривайте сон или дрейн: по истечении SIGKILL приходит безусловно и выбрасывает всё, что ещё в полёте." },
  ]}
/>

<Recap lang="ru">
Три пробы — три разных вопроса, и каждая продовая история этого урока выросла из ответа не на тот. Liveness спрашивает, поможет ли рестарт: держите его хендлер тривиальным — 200, доказывающий, что процесс обрабатывает запросы, — потому что единственный ответ кубелета — убийство, а убийство не чинит базу; привязанный к зависимости, liveness конвертирует 28-секундный блип в самоподдерживающийся шторм рестартов флота. Readiness спрашивает, слать ли трафик сейчас: вот где живёт пинг пула базы с секундным таймаутом, потому что ответ — вывод из эндпоинтов — обратим, сохраняет процесс тёплым и ровно уместен при сбоях, перегрузке и дрейне; ограничьте его жёсткими зависимостями, иначе один мигающий необязательный сайдкар разготовит весь флот. Startup спрашивает, закончилась ли загрузка, приостанавливая остальные пробы в бюджете failureThreshold на period — недостающая деталь каждого CrashLoopBackOff на медленной миграции. Таксономия эндпоинтов следует вопросам: /healthz тривиален, /readyz честен, /debug/pprof никогда не публичен — pprof сам регистрируется на DefaultServeMux. Завершение — это флаг readiness по совместительству: SIGTERM, флип /readyz в 503, сон около пяти секунд, пока удаление из эндпоинтов проходит каденс обнаружения и лаг дата-плейна, затем srv.Shutdown с границей по самому медленному легитимному запросу — хореография graceful shutdown из go/03, теперь знающая о кластере. Сложите последовательность против terminationGracePeriodSeconds с запасом и поднимайте грейс, а не сбривайте шаги: SIGKILL приходит по расписанию и не договаривается.
</Recap>
