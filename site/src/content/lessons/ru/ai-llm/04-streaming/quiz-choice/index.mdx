---
concepts:
- sse-protocol
- time-to-first-token
- delta-accumulation
- partial-json-tool-args
- proxy-buffering
- stream-reconnect
estMin: 13
lang: ru
level: senior
order: 7
prereqs: []
slug: quiz-choice
sources:
- https://docs.anthropic.com/en/docs/build-with-claude/streaming
- https://platform.openai.com/docs/api-reference/responses-streaming
status: ready
summary: "Синтез всего юнита в формате выбора — TTFT против общего времени, жизненный цикл событий SSE, накопление delta, частичный JSON в аргументах tool, reconnect и буферизующий прокси."
title: 'Streaming: тест с множественным выбором'
track: ai-llm
unit: 04-streaming
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Шесть вопросов, которые проходят сквозь весь юнит. Каждый — это решение, которое ты принимаешь в реальном инциденте со streaming: не определение для зубрёжки, а компромисс, который надо взвесить, когда спиннер висит в проде.
</Hook>

<Goal lang="ru">
Убедись, что ты связываешь модель латентности, жизненный цикл SSE, накопление delta, контракт частичного JSON и отказ из-за буферизации — тот синтез, к которому вёл урок.
</Goal>

<Quiz
  id="04-streaming-choice-1"
  lessonSlug="04-streaming"
  lang="ru"
  question="Ответ на 400 токенов генерируется со скоростью 60 ток/с. Продукт просит «сделать ответ вдвое быстрее», включив streaming. Что ты ответишь на senior-уровне?"
  choices={[
    { label: "Streaming не меняет общее время генерации (~7с) — он схлопывает time-to-first-token с 7с до сотен мс. Выигрыш в воспринимаемой отзывчивости: пользователь читает первые токены, пока генерируются последующие. Реальное ускорение — это меньше токенов или более быстрая модель.", correct: true },
    { label: "Streaming примерно вдвое сокращает общее время, потому что токены передаются параллельно с генерацией.", misconception: "Токены по-прежнему генерируются последовательно с той же скоростью; SSE лишь доставляет каждый по мере появления. Общее время неизменно — падает только TTFT." },
    { label: "Streaming заставляет модель генерировать токены быстрее, убирая финальную буферизацию.", misconception: "Скорость генерации (TPOT) задаётся моделью и железом, а не тем, стримишь ты или нет. Streaming — это выбор транспорта/UX, а не оптимизация вычислений." },
    { label: "Streaming уменьшает число токенов, поэтому ответ реально короче.", misconception: "Число токенов фиксируется промптом и ответом. Streaming меняет, когда приходят байты, а не сколько токенов выдаёт модель." },
  ]}
/>

<Quiz
  id="04-streaming-choice-2"
  lessonSlug="04-streaming"
  lang="ru"
  question="Твой чат идеально стримит на localhost, но в staging каждый ответ появляется разом после долгой паузы. Куда смотреть в первую очередь?"
  choices={[
    { label: "В сетевой путь, а не в код. «Работает на localhost, валит пачкой в проде» — отпечаток буферизации в reverse proxy / CDN / serverless-слое: nginx по умолчанию proxy_buffering on и держит весь ответ до конца. Фикс: proxy_buffering off / X-Accel-Buffering: no, без gzip на SSE-маршруте.", correct: true },
    { label: "В свой SSE-парсер — он наверняка копит delta и сбрасывает всё в конце.", misconception: "Если тот же код инкрементально стримит на localhost, парсер заведомо исправен. Единственная новая переменная в staging — инфраструктура на пути между сервером и клиентом." },
    { label: "В модель — переключиться на более быструю, чтобы первый токен пришёл раньше.", misconception: "Более быструю модель прокси всё равно соберёт в один сброс. TTFT останется равным общему времени, пока не починен буферизующий слой." },
    { label: "Перейти с SSE на WebSockets, раз SSE явно не умеет стримить через прокси.", misconception: "SSE отлично стримит токен за токеном, когда ничто не буферизует; тот же прокси может ломать и WS-upgrade. Ты сменишь транспорт и снова упрёшься в буферизующий слой." },
  ]}
/>

<Quiz
  id="04-streaming-choice-3"
  lessonSlug="04-streaming"
  lang="ru"
  question={"Модель вызывает tool; аргументы приходят как input_json_delta-чанки: '{\"city\": \"San Fran', затем 'cisco\", \"unit\":', затем ' \"celsius\"}'. Когда можно сделать JSON.parse буфера?"}
  choices={[
    { label: "Только после content_block_stop для этого блока. Каждый input_json_delta — фрагмент одного JSON-документа, а не валидный JSON сам по себе; документ полон только когда блок закрылся. Частичную строку можно показать как подсказку «вызываю tool…», но действовать по ней нельзя.", correct: true },
    { label: "После каждой delta, чтобы распарсенный объект аргументов всегда был актуален.", misconception: "Парсинг промежуточного буфера вроде '{\"city\": \"San Fran' каждый раз бросает syntax error. Фрагменты не являются независимо валидным JSON." },
    { label: "Сразу после message_start, ведь именно этот кадр начинает аргументы.", misconception: "message_start несёт только конверт с пустым content — delta аргументов ещё не приходили, парсить нечего." },
    { label: "Не парсить вовсе — передать сырую частичную строку прямо в tool-функцию.", misconception: "Tool нужны структурированные аргументы. Передача полусобранной JSON-строки запустит функцию с битыми/пустыми входами. Парси собранный буфер на границе блока." },
  ]}
/>

<Quiz
  id="04-streaming-choice-4"
  lessonSlug="04-streaming"
  lang="ru"
  question="В проде tool-вызов срабатывает с input = {} (пустые аргументы), но тот же промпт локально напрямую к провайдеру работает. Самая вероятная причина?"
  choices={[
    { label: "Промежуточный слой (прокси или SDK-адаптер — у LiteLLM были такие регрессии) теряет или неправильно маршрутизирует кадры input_json_delta. Конверт tool-вызова доходит, но delta аргументов проглочены до того, как их увидел твой аккумулятор.", correct: true },
    { label: "В аккумуляторе баг — он сбрасывает буфер на каждой delta.", misconception: "Если локально напрямую к провайдеру всё работает, аккумулятор исправен — разница в слое, добавленном в проде. Подозревай путь раньше своего кода." },
    { label: "Модель решила, что в этот раз tool не нужны аргументы.", misconception: "Схема tool требует аргументы; модель их выдала. Пустой input при рабочем прямом пути указывает на потерю delta в транзите, а не на выбор модели." },
    { label: "Ты парсил слишком поздно — парси на каждой delta, чтобы не терять аргументы.", misconception: "Парсинг по каждой delta бросает на фрагментах и никогда не даст {}. Пустые аргументы — проблема доставки (потеряны кадры), а не времени парсинга." },
  ]}
/>

<Quiz
  id="04-streaming-choice-5"
  lessonSlug="04-streaming"
  lang="ru"
  question="Соединение рвётся на 200-м токене из 400. Какой честный senior-дефолт для обработки?"
  choices={[
    { label: "Считать оборванный stream повторяемым retry всего хода и сделать новый запрос безопасным к повтору (идемпотентность). Настоящий resume посреди генерации через Last-Event-ID требует серверного переигрывания delta и почти всегда не стоит затрат.", correct: true },
    { label: "Всегда реализовывать resume через Last-Event-ID, чтобы stream продолжился с 200-го токена.", misconception: "SSE поддерживает Last-Event-ID, но resume посреди генерации требует буферизации и переигрывания delta на сервере — заметная сложность, которую большинство приложений обходят повтором всего хода." },
    { label: "Отрисовать имеющиеся 200 токенов и считать дело сделанным.", misconception: "Обрезанный ответ — это неверный ответ. Stop reason так и не пришёл, значит у тебя не полное сообщение, а его половина." },
    { label: "Перейти на не-streaming запрос, чтобы обрывов не было.", misconception: "Не-streaming запросы тоже рвутся, а ты полностью теряешь выигрыш TTFT, ожидая всю генерацию. Обрывы лечатся безопасными retry, а не отказом от streaming." },
  ]}
/>

<Quiz
  id="04-streaming-choice-6"
  lessonSlug="04-streaming"
  lang="ru"
  question="Reasoning-модель с chain-of-thought держит TTFT 30с, не выдав ни одного токена; пользователи говорят, что приложение «зависло». Как правильно это прочитать и починить?"
  choices={[
    { label: "Это ожидаемо: пока модель думает, стримить нечего, поэтому наивный UI «только токены» выглядит зависшим. Чини UX — показывай состояние размышления/прогресса (или стримь summaries рассуждений, если провайдер их отдаёт), а не транспорт.", correct: true },
    { label: "Stream буферизуется прокси; примени proxy_buffering off.", misconception: "Буферизация даёт «всё разом после паузы» для любого типа модели. Reasoning-модель реально не выдаёт токенов в фазе размышления — это настоящая латентность генерации, а не буферизация." },
    { label: "SSE-соединение оборвалось; добавь авто-reconnect.", misconception: "Соединение открыто и здорово; модель просто ещё не выдала первый content-токен. Reconnect перезапустил бы дорогую фазу размышления." },
    { label: "Уменьши max_tokens, чтобы модель думала меньше и отвечала раньше.", misconception: "max_tokens ограничивает длину вывода, а не объём рассуждения, который тут задаёт TTFT. Фикс — сообщать пользователю о прогрессе в фазе размышления." },
  ]}
/>

<Recap lang="ru">
Сквозная линия юнита — одна модель: streaming платит за воспринимаемую латентность скоростью чтения пользователя, схлопывая TTFT, тогда как общее время и число токенов неизменны. SSE доставляет типизированный жизненный цикл (message_start → content_block_start → content_block_delta ×N → content_block_stop → message_delta/stop), который ты накапливаешь; text-delta рендерятся сразу, а фрагменты input_json_delta для tool парсятся только на content_block_stop. Пустые аргументы tool означают, что delta съел промежуточный слой; оборванные stream — повторяемые retry всего хода; а отказ, стирающий весь выигрыш и не требующий обрыва соединения, — буферизующий прокси, превращающий TTFT обратно в общее время.
</Recap>
