---
concepts:
- sse-protocol
- time-to-first-token
- delta-accumulation
- partial-json-tool-args
- proxy-buffering
- stream-reconnect
deepensInto: []
estMin: 16
lang: ru
lessonType: topic
level: junior
order: 1
prereqs: []
slug: 01-overview
sources:
- https://docs.anthropic.com/en/docs/build-with-claude/streaming
- https://www.ibm.com/think/topics/time-to-first-token
- https://platform.openai.com/docs/api-reference/responses-streaming
- https://oneuptime.com/blog/post/2025-12-16-server-sent-events-nginx/view
spiral: []
status: ready
summary: 'Стриминг меняет общее время генерации на time-to-first-token. SSE шлёт delta-события,
  которые ты накапливаешь; аргументы tool-call приходят как частичный JSON, его нельзя
  парсить до конца. Классика прода: буферизующий прокси держит весь ответ — и стриминг
  тихо умирает.'
title: 'Стриминг ответов LLM: SSE, частичные токены и прокси, который их съедает'
track: ai-llm
unit: 04-streaming
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import DiagramFrame from "~/components/diagram/DiagramFrame.astro";
import PacketDot from "~/components/diagram/PacketDot.astro";

<Hook>
На ноутбуке демо было идеальным: вводишь промпт, слова появляются мгновенно, курсор летит по экрану. Потом это выкатили за корпоративный nginx. Теперь каждый пользователь смотрит на спиннер девять секунд, а потом весь ответ вваливается разом. В приложении ничего не менялось. Модель по-прежнему стримила токен за токеном — но прокси с `proxy_buffering on` тихо держал весь ответ в буфере и сбросил его только в конце. Стриминг был настоящим; пользователь не увидел ни байта из него.
</Hook>

<Crux>Стриминг выигрывает на time-to-first-token, а не на общем времени — и один буферизующий прокси на пути стирает весь выигрыш.</Crux>

<Explanation>

За следующие десять минут ты поймёшь, почему именно прокси убил твой стриминг и что нужно проверить до того, как написать хоть строчку кода фикса.

## Зачем вообще стримить: TTFT важнее общего времени

Нестримящийся ответ заставляет пользователя ждать *всю* генерацию. Ответ в 400 токенов на скорости 60 токенов/сек — это примерно семь секунд пустого экрана, а потом всё разом. Стриминг ничего не меняет в этом итоге — генерация всё равно длится семь секунд, — но меняет единственное число, которое чувствует пользователь: **time-to-first-token (TTFT)**, разрыв между отправкой запроса и первым видимым словом.

Для чат-приложений TTFT меньше ~1 секунды ощущается мгновенным; продакшен на горячих путях целит в 200–500 мс. Когда токены пошли, чтение определяет **time-per-output-token (TPOT)**: 50 токенов/сек ощущаются вялыми, 200 токенов/сек (~150 слов/сек) читаются быстрее, чем кто-либо успевает. Стриминг даёт примерно 10–20x улучшения *воспринимаемой* отзывчивости при нулевом изменении реальных вычислений. Взгляд сеньора: ты не делаешь быстрее, ты гасишь латентность скоростью чтения самого пользователя.

За этим прячутся две формы провала. Reasoning-модели с chain-of-thought могут сидеть на 10–150 секундах TTFT до первого токена — пока они думают, стримить нечего, и наивный UI выглядит замёрзшим. А если транспорт буферизует (следующий раздел), TTFT схлопывается обратно в общее время, и ты получаешь худшее из двух миров: сложность стриминга, латентность батча.

## Протокол событий SSE

Каждый крупный провайдер стримит через **Server-Sent Events (SSE)** — долгоживущий HTTP-ответ с `Content-Type: text/event-stream`, где сервер пушит разделённые переводом строки кадры `data:` по одному соединению. Он однонаправленный (сервер→клиент), переживает обычную HTTP-инфраструктуру и автоматически переподключается в браузерном `EventSource` — именно поэтому он победил WebSockets для этой задачи.

Кадры — это типизированный жизненный цикл, а не сырой текст. Последовательность Anthropic: `message_start` (конверт, пустой content) → один или несколько блоков, каждый `content_block_start` → много `content_block_delta` → `content_block_stop` → затем `message_delta` (несёт причину остановки и финальный расход токенов) → `message_stop`. У OpenAI имена другие, но идея та же: старт, поток дельт, терминальное событие. Ты никогда не получаешь ответ одним кадром; ты **накапливаешь** дельты в снимок.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Событие SSE</th>
        <th class="border border-border px-3 py-2 text-left">Несёт</th>
        <th class="border border-border px-3 py-2 text-left">Что делаешь</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2"><code>message_start</code></td>
        <td class="border border-border px-3 py-2">id сообщения, роль, пустой content</td>
        <td class="border border-border px-3 py-2">Открой пузырь ассистента; запусти таймер TTFT</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2"><code>content_block_start</code></td>
        <td class="border border-border px-3 py-2">Индекс блока + тип (<code>text</code> / <code>tool_use</code>)</td>
        <td class="border border-border px-3 py-2">Реши: рендерить как текст или буферить как аргументы tool</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2"><code>content_block_delta</code></td>
        <td class="border border-border px-3 py-2"><code>text_delta</code> или <code>input_json_delta</code></td>
        <td class="border border-border px-3 py-2">Добавь кусок в свой аккумулятор</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2"><code>content_block_stop</code></td>
        <td class="border border-border px-3 py-2">Индекс блока</td>
        <td class="border border-border px-3 py-2">Теперь блок завершён — безопасно парсить JSON tool</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2"><code>message_delta</code> / <code>message_stop</code></td>
        <td class="border border-border px-3 py-2">Причина остановки, финальный расход</td>
        <td class="border border-border px-3 py-2">Финализируй; запиши токены для биллинга/метрик</td>
      </tr>
    </tbody>
  </table>
</div>

## Накопление частичных токенов — и ловушка частичного JSON

Почему это важно до того, как напишешь первый streaming-обработчик? Потому что текст и аргументы tool-вызовов ведут себя принципиально по-разному — и смешение этих двух случаев порождает самые распространённые скрытые баги в прод-интеграциях с tool calling.

Текст прощающий: каждый `text_delta` — валидный фрагмент строки, поэтому ты добавляешь и рендеришь сразу. Аккумулятор — это просто `text += delta`, и UI рисует на ходу.

Tool-вызовы не прощают. Когда модель вызывает функцию, аргументы стримятся кусками `input_json_delta` — и **каждый кусок это фрагмент JSON-документа, а не валидный JSON сам по себе.** Дельта может быть `{"city": "San Fran`, потом `cisco", "unit":`, потом ` "celsius"}`. Если вызвать `JSON.parse` на любом промежуточном буфере, ты упадёшь с синтаксической ошибкой. Правило жёсткое: **накапливай каждый `input_json_delta` в строковый буфер и парси только после `content_block_stop`.** Частичную строку можно показать как индикатор «вызываю инструмент…», но действовать по аргументам нельзя, пока блок не закрыт.

Это реальный и повторяющийся прод-баг. Слои прокси и адаптеров (LiteLLM выкатывал несколько таких регрессий) иногда роняют или неверно обрабатывают кадры `input_json_delta` — tool-вызов приходит с `input` в виде пустого `{}`, и твоя функция запускается без аргументов. Если tool-вызов загадочно срабатывает с пустыми аргументами, подозревай слой между тобой и моделью, который проглотил JSON-дельты, а не свой собственный парсер.

<Inset kind="why" lang="ru">
Зачем вообще слать аргументы JSON непарсящимися фрагментами? Потому что модель генерирует их токен за токеном, как любой другой текст — нет момента, когда у провайдера рано появляется «весь объект аргументов». Стриминг их позволяет UI показывать прогресс, а провайдеру — начать слать в момент, когда есть первый токен. Контракт сдвигает разовую стоимость парсинга к границе блока, где JSON гарантированно полон.
</Inset>

## Переподключение, возобновление и буферизующий прокси, который всё это убивает

SSE построен на переподключение: сервер может слать `id:` с каждым событием, и при обрыве соединения браузер переотправляет запрос с заголовком `Last-Event-ID`, чтобы сервер возобновился после последнего доставленного события. На практике возобновление LLM посреди генерации редко реализуют (нужно переигрывать буферизованные дельты на стороне сервера); большинство приложений трактуют оборванный стрим как «повторить весь ход» и опираются на идемпотентность. Честный дефолт сеньора: проектируй под смерть стрима на токене 200 из 400 и делай свежий запрос безопасным для повтора.

Но больнее всего бьёт провал, которому вообще не нужен обрыв соединения — это **буферизация в пути**. nginx идёт с `proxy_buffering on` по умолчанию: он читает ответ апстрима в буфер и пересылает клиенту только когда буфер наполнится или ответ закончится. Для обычной страницы это оптимизация; для SSE это фатально — клиент не получает ничего, пока модель не закончит, и TTFT превращается в общее время, а твой спиннер висит всю генерацию. Та же ловушка возникает на serverless-платформах и CDN, которые буферизуют или вводят лимиты на размер/время ответа, и в любом gzip-слое, который ждёт достаточно байтов для сжатия.

Фиксы конкретны. На nginx: `proxy_buffering off`, `proxy_http_version 1.1`, очисти `Connection` и большой `proxy_read_timeout`. Когда конфиг прокси трогать нельзя, выставь заголовок ответа `X-Accel-Buffering: no` (nginx уважает его пер-ответ) плюс `Cache-Control: no-cache`. Диагностика, экономящая часы: если стриминг работает против `localhost`, но в стейджинге сбрасывается разом, перестань дебажить свой код — что-то в сетевом пути буферизует.

<TradeoffMatrix
  id="04-streaming-tradeoff-1"
  lessonSlug="01-overview"
  lang="ru"
  prompt="Твой чат стримит нормально локально, но в проде каждый ответ появляется разом после долгой паузы. Выбери, что расследовать первым."
  options={[
    { name: "Буферизация прокси/платформы: выстави proxy_buffering off (или X-Accel-Buffering: no) и отключи gzip на SSE-роуте", summary: "Это путь, а не код.", correct: true, justification: "Сигнатура «работает на localhost, батчит в проде» почти всегда означает, что слой между сервером и клиентом буферизует. nginx по умолчанию ставит proxy_buffering on, и он держит весь ответ, пока буфер не наполнится или стрим не закончится — схлопывая TTFT в общее время. Фикс живёт в конфиге прокси/CDN/serverless, а не в приложении: proxy_buffering off, X-Accel-Buffering: no и никакого сжатия на потоке событий." },
    { name: "Переключиться с SSE на WebSockets, полагая, что узкое место — SSE", summary: "Сменить транспорт.", justification: "Тяжёлая переделка, не бьющая по причине. SSE стримит токен за токеном идеально, когда его ничто не буферизует; тот же прокси, что буферизует SSE, может ломать и WS-апгрейд. Ты сменишь один транспорт на другой и всё равно упрёшься в буферизующий слой." },
    { name: "Уменьшить max_tokens, чтобы ответ был короче и буфер сбрасывался раньше", summary: "Урезать вывод.", justification: "Пластырь, прячущий баг на коротких ответах и возвращающийся на длинных. Он не восстанавливает инкрементальную доставку — пользователь всё равно ждёт всю (более короткую) генерацию, прежде чем что-то увидит. Стриминг по-прежнему мёртв." },
    { name: "Добавить на клиенте анимацию печатной машинки, постепенно раскрывающую текст", summary: "Подделать стрим.", justification: "Косметика и обман про латентность: анимировать можно только уже полностью полученный текст, поэтому TTFT не меняется — пользователь всё равно ждёт всю генерацию, а затем смотрит фальшивое раскрытие. Это маскирует симптом, пока реальный стрим остаётся буферизованным." },
  ]}
/>

<Quiz
  id="04-streaming-quiz-1"
  lessonSlug="01-overview"
  lang="ru"
  question="Tool-вызов стримит свои аргументы кусками input_json_delta. Когда безопасно делать JSON.parse накопленного буфера?"
  choices={[
    { label: "Только после content_block_stop для этого блока — каждая дельта это фрагмент, поэтому JSON валиден лишь когда полон", correct: true },
    { label: "После каждой дельты, чтобы всегда иметь свежий распарсенный объект", misconception: "Каждый input_json_delta — это срез JSON-документа, а не валидный JSON сам по себе. Парсинг промежуточного буфера вроде '{\"city\": \"San Fran' падает с синтаксической ошибкой. Накапливай, затем парси один раз на границе блока." },
    { label: "После message_start, ведь именно тогда начинаются аргументы", misconception: "message_start несёт только конверт с пустым content. Ни одной дельты аргументов ещё не пришло — парсить нечего." },
    { label: "Не парсить вообще — передать сырую частичную строку прямо в инструмент", misconception: "Инструменту нужны структурированные аргументы. Передача недоформированной JSON-строки запускает функцию с битым или отсутствующим вводом; сначала надо распарсить завершённый буфер." },
  ]}
/>

<Quiz
  id="04-streaming-quiz-2"
  lessonSlug="01-overview"
  lang="ru"
  question="Почему стриминг улучшает UX, хотя не сокращает общее время генерации?"
  choices={[
    { label: "Он схлопывает time-to-first-token: пользователь читает ранние токены, пока поздние ещё генерируются", correct: true },
    { label: "Он заставляет модель генерировать токены быстрее", misconception: "Стриминг меняет доставку, а не скорость генерации. Модель производит токены с той же скоростью; ты просто показываешь их по мере прихода, а не после последнего." },
    { label: "Он сокращает общее число токенов, которые производит модель", misconception: "Число токенов определяется промптом и ответом, а не тем, стримишь ты или нет. Стриминг — чисто транспортный/UX-выбор." },
    { label: "Он сжимает ответ, чтобы он передавался быстрее", misconception: "SSE не сжимает контент; наоборот, gzip на роуте потока может вызвать буферизацию. Выигрыш перцептивный — первый токен приходит за сотни мс вместо ожидания всей генерации." },
  ]}
/>

<DragOrder
  id="04-streaming-drag-1"
  lessonSlug="01-overview"
  lang="ru"
  prompt="Расставь жизненный цикл SSE для одного стримящегося сообщения:"
  items={[
    { id: "a", label: "message_start — приходит конверт с пустым content; запусти таймер TTFT" },
    { id: "b", label: "content_block_start — открывается блок text или tool_use" },
    { id: "c", label: "content_block_delta (×N) — добавляй каждый text_delta или input_json_delta в аккумулятор" },
    { id: "d", label: "content_block_stop — блок завершён; теперь безопасно JSON.parse аргументов tool" },
    { id: "e", label: "message_delta, затем message_stop — причина остановки + финальный расход; финализируй" },
  ]}
  correctOrder={["a", "b", "c", "d", "e"]}
/>

</Explanation>

<DiagramFrame label="Токены вытекают по одному через SSE по мере генерации модели — пользователь читает ранние токены, пока поздние ещё производятся" hue="var(--d-ai)" caption="Стриминг не меняет общее время генерации; он схлопывает time-to-first-token. Выигрыш исчезает, если любой прокси на пути буферизует ответ.">
  <svg viewBox="0 0 360 80" width="360" height="80" style="display:block;margin:0 auto;max-width:100%;height:auto;">
    <rect x="6" y="28" width="86" height="28" rx="6" fill="var(--card-2)" stroke="var(--hairline-strong)" stroke-width="0.8" />
    <text x="49" y="46" text-anchor="middle" font-size="11" font-family="var(--font-body)" fill="var(--ink)">Модель</text>
    <rect x="268" y="28" width="86" height="28" rx="6" fill="var(--card-2)" stroke="var(--hairline-strong)" stroke-width="0.8" />
    <text x="311" y="46" text-anchor="middle" font-size="11" font-family="var(--font-body)" fill="var(--ink)">Клиент</text>
    <text x="180" y="20" text-anchor="middle" font-size="10" font-family="var(--font-body)" fill="var(--muted)">SSE: токен за токеном</text>
    <path data-path d="M92 42 L268 42" fill="none" stroke="var(--hairline-2)" stroke-width="1.4" stroke-dasharray="4 4" />
    <PacketDot color="var(--d-ai)" durationMs={1400} size={9} />
  </svg>
</DiagramFrame>

<KeyTakeaway>Стримь ради выигрыша по TTFT, не по общему времени. Накапливай SSE-дельты; парси JSON tool-вызова только на content_block_stop. Провал №1 в проде — буферизующий прокси: ставь proxy_buffering off / X-Accel-Buffering: no.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="04-streaming-retrieval"
  lang="ru"
  questions={[
    { q: "Приложение стримит идеально на localhost, но в проде каждый ответ появляется разом после долгой паузы. Пройди диагностику и фикс.", a: "Сигнатура «нормально на localhost, батчит в проде» — это отпечаток буферизации в сетевом пути, а не баг в твоём коде стриминга: твой код доказуемо работает, ведь на localhost нет прокси между. Дефолтный виновник — nginx с proxy_buffering on, который читает весь ответ апстрима в буфер и пересылает только когда буфер наполнится или ответ закончится. Для обычной страницы это оптимизация; для SSE-потока это фатально, потому что клиент не получает ни байта, пока модель не закончит генерировать, и time-to-first-token схлопывается в общее время, а спиннер висит весь ответ. Та же ловушка есть на CDN, serverless-платформах с буферизацией ответа или лимитами размера/времени, и в gzip-слоях, ждущих достаточно байтов для сжатия. Фикс в конфиге пути, а не в приложении: на nginx выстави proxy_buffering off, proxy_http_version 1.1, очисти заголовок Connection и подними proxy_read_timeout. Когда прокси трогать нельзя, шли X-Accel-Buffering: no плюс Cache-Control: no-cache на SSE-ответе и убедись, что сжатие выключено для этого роута." },
    { q: "Почему аргументы tool-вызова надо накапливать перед парсингом, и какой баг возникает, когда промежуточный слой портит эти дельты?", a: "Аргументы tool-вызова стримятся как последовательность кусков input_json_delta, и каждый кусок это фрагмент одного JSON-документа, а не валидный JSON сам по себе — ты можешь увидеть '{\"city\": \"San Fran', потом 'cisco\", \"unit\":', потом ' \"celsius\"}'. Вызов JSON.parse на любом промежуточном буфере падает с синтаксической ошибкой, поэтому контракт — добавлять каждую дельту в строковый буфер и парсить ровно один раз, после того как content_block_stop закроет блок, когда документ гарантированно полон. Частичную строку можно показать как подсказку «вызываю инструмент…», но действовать по аргументам до этого нельзя. Классический прод-баг — слой прокси или SDK-адаптера (LiteLLM выкатывал несколько таких регрессий), который роняет или неверно маршрутизирует кадры input_json_delta: tool-вызов всё равно приходит, но его input — пустой {}, и функция запускается без аргументов. Так что если инструмент срабатывает с пустыми аргументами, подозревай слой между тобой и моделью, проглотивший JSON-дельты, прежде чем винить свой аккумулятор." },
  ]}
/>

<Recap lang="ru">
Стриминг не делает генерацию быстрее — он гасит латентность скоростью чтения пользователя, схлопывая time-to-first-token с полной семисекундной генерации до пары сотен миллисекунд, тогда как time-per-output-token определяет чтение, когда токены пошли. Транспорт — это SSE: долгоживущий text/event-stream, где сервер пушит типизированный жизненный цикл — message_start, content_block_start, череда content_block_delta, content_block_stop, затем message_delta и message_stop — и ты накапливаешь дельты в снимок, а не получаешь ответ целиком. Текстовые дельты безопасно рендерить сразу; аргументы tool-вызова приходят фрагментами input_json_delta, которые не валидны как JSON, пока блок не закрыт, поэтому ты буферишь и парсишь только на content_block_stop, а tool-вызов с пустыми аргументами обычно означает, что промежуточный слой съел эти дельты. SSE умеет переподключаться через Last-Event-ID, но большинство приложений трактуют оборванный стрим как повторяемый whole-turn-ретрай. Провал, стирающий весь выигрыш, не требует обрыва: буферизующий обратный прокси или serverless-слой (nginx с proxy_buffering on по умолчанию) держит весь ответ и сбрасывает в конце, превращая TTFT обратно в общее время. Признак — «работает на localhost, батчит в проде», а фикс живёт в конфиге пути — proxy_buffering off или X-Accel-Buffering: no, со сжатием, выключенным на роуте потока. Теперь, когда встретишь спиннер, который висит всю генерацию перед тем как что-то показать, первый вопрос не «что сломалось в коде?» — а «что буферизует путь?»
</Recap>
