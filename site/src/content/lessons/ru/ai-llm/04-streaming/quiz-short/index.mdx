---
concepts:
- time-to-first-token
- sse-protocol
- delta-accumulation
- partial-json-tool-args
- proxy-buffering
- stream-reconnect
estMin: 14
lang: ru
level: senior
order: 8
prereqs: []
slug: quiz-short
sources:
- https://docs.anthropic.com/en/docs/build-with-claude/streaming
- https://www.ibm.com/think/topics/time-to-first-token
- https://platform.openai.com/docs/api-reference/responses-streaming
status: ready
summary: "Промпты на свободное припоминание по всему юниту streaming. Сначала ответь своими словами, затем открой модельный ответ и сравни."
title: 'Streaming: повторение на припоминание'
track: ai-llm
unit: 04-streaming
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";

<Hook>
Припоминание сильнее перечитывания. Для каждого промпта проговори или запиши полный ответ по памяти, прежде чем открыть модельный, — именно усилие припоминания закрепляет ментальную модель streaming.
</Hook>

<Goal lang="ru">
Восстанови основные механизмы юнита — модель латентности TTFT, жизненный цикл SSE, накопление delta, контракт частичного JSON для tool, стратегию reconnect и отказ из-за буферизации — не заглядывая обратно в урок.
</Goal>

<RetrievalDrawer
  client:load
  id="04-streaming-short-retrieval"
  lang="ru"
  questions={[
    { q: "Почему streaming улучшает UX, не сокращая общее время генерации? Назови две метрики латентности, на которых он играет.", a: "Streaming ничего не меняет в общем времени генерации — ответ на 400 токенов при 60 ток/с всё равно завершается примерно за семь секунд. Он меняет единственное число, которое чувствует пользователь: time-to-first-token (TTFT) — промежуток между отправкой запроса и первым видимым словом. Без streaming TTFT равен общему времени; со streaming TTFT падает до сотен миллисекунд, пока генерируются последующие токены. Когда токены пошли, чтение управляется time-per-output-token (TPOT): 50 ток/с ощущается вяло, 200 ток/с читается быстрее, чем кто-либо успевает. Senior-формулировка: ты не делаешь быстрее, ты гасишь воспринимаемую латентность скоростью чтения пользователя — часто это 10–20x по отзывчивости при нулевом изменении вычислений." },
    { q: "Пройди жизненный цикл событий SSE для одного streamed-сообщения и скажи, что делаешь на каждом этапе.", a: "Все крупные провайдеры стримят поверх Server-Sent Events: долгоживущий HTTP-ответ text/event-stream с newline-разделёнными кадрами data:, только сервер→клиент. Кадры — это типизированный жизненный цикл, а не сырой текст. message_start несёт конверт (id, role, пустой content) — открой пузырь ассистента и запусти таймер TTFT. content_block_start открывает блок и сообщает его тип, text или tool_use, чтобы ты решил, рендерить или буферизовать. content_block_delta приходит много раз, неся text_delta или input_json_delta — добавляй каждую в аккумулятор. content_block_stop закрывает блок — точка, где JSON для tool безопасно парсить. Затем message_delta несёт stop reason и финальный usage токенов, а message_stop завершает сообщение — финализируй и запиши токены для биллинга. Ты никогда не получаешь ответ одним кадром; ты накапливаешь delta в снапшот." },
    { q: "Почему аргументы tool-вызова нужно накапливать до парсинга и какой прод-баг возникает, когда промежуточный слой портит эти delta?", a: "Аргументы tool-вызова приходят последовательностью чанков input_json_delta, каждый — фрагмент одного JSON-документа, а не валидный JSON сам по себе: ты можешь увидеть '{\"city\": \"San Fran', затем 'cisco\", \"unit\":', затем ' \"celsius\"}'. Вызов JSON.parse на любом промежуточном буфере бросает syntax error, поэтому контракт такой: добавляй каждую delta в строковый буфер и парси ровно один раз — после content_block_stop, когда документ гарантированно полон. Частичную строку можно показать как подсказку «вызываю tool…», но действовать по аргументам до этого нельзя. Классический прод-баг — прокси или SDK-адаптер (у LiteLLM были такие регрессии), теряющий или неправильно маршрутизирующий кадры input_json_delta: tool-вызов всё равно доходит, но его input — пустой {}, и твоя функция запускается без аргументов. Если tool сработал с пустыми аргументами, подозревай слой между тобой и моделью, проглотивший delta, прежде чем винить свой аккумулятор." },
    { q: "Stream рвётся на 200-м токене из 400. Сравни полный resume через Last-Event-ID и прагматичный дефолт и скажи, что бы ты выкатил.", a: "SSE спроектирован для reconnect: сервер может прикреплять id: к каждому событию, и при обрыве браузер переотправляет запрос с заголовком Last-Event-ID, чтобы сервер возобновился после последнего доставленного события. Для LLM настоящий resume посреди генерации означает, что сервер должен буферизовать и переигрывать уже отправленные delta и продолжать ту же генерацию — заметная сложность, которую большинство приложений не реализуют. Прагматичный и честный senior-дефолт — считать оборванный stream повторяемым retry всего хода: проектируй под обрыв на 200-м токене из 400, делай новый запрос безопасным к повтору через идемпотентность и переспрашивай ход. Подход «повторить ход» ты выкатываешь почти везде; resume через Last-Event-ID строишь, только когда повтор хода действительно слишком дорог или неидемпотентен." },
    { q: "Опиши прод-отказ №1 для streaming, его сигнатуру и конкретные фиксы.", a: "Отказ, стирающий весь выигрыш streaming, не требует обрыва соединения: это буферизация в сетевом пути. nginx по умолчанию идёт с proxy_buffering on, читая upstream-ответ в буфер и пересылая клиенту только когда буфер заполнится или ответ закончится. Для обычной страницы это оптимизация; для SSE это фатально — клиент получает ноль байт, пока модель не закончит, поэтому TTFT схлопывается обратно в общее время, и спиннер висит всю генерацию. Та же ловушка в CDN, serverless-платформах с буферизацией ответа или лимитами размера/времени и в любом gzip-слое, который ждёт достаточно байт для сжатия. Сигнатура безошибочна: streaming работает на localhost, но валит пачкой в staging/prod. Фиксы живут в конфиге пути, а не в приложении: на nginx — proxy_buffering off, proxy_http_version 1.1, очистить Connection, поднять proxy_read_timeout; когда нельзя править прокси — слать X-Accel-Buffering: no и Cache-Control: no-cache на SSE-ответе и отключить компрессию на этом маршруте." },
    { q: "Почему reasoning-модель с chain-of-thought может заставить streaming-UI выглядеть зависшим и как это обработать?", a: "Reasoning-модели делают длинный внутренний chain-of-thought до выдачи любого output-токена, поэтому TTFT может быть десятки секунд — в худших случаях 10–150с. В этом окне стримить реально нечего: модель ещё не выдала content-токен, так что наивный UI «только токены» показывает пустой, как будто зависший экран. Это не буферизация и не обрыв соединения — это настоящая латентность генерации в фазе размышления, поэтому proxy_buffering off или логика reconnect ничего не дадут. Фикс — UX-ный: показывай явное состояние размышления/прогресса в фазе think, и если провайдер отдаёт reasoning-summary или thinking-delta, стримь их, чтобы пользователь видел активность. Задай ожидание, что первый токен ответа может занять время, вместо голого спиннера, неотличимого от зависания." },
  ]}
/>

<Recap lang="ru">
Если ты смог восстановить каждый ответ по памяти, ты держишь хребет юнита: streaming меняет общее время на TTFT и читается по TPOT; SSE доставляет типизированный жизненный цикл, который ты накапливаешь в снапшот; text-delta рендерятся сразу, а фрагменты input_json_delta для tool парсятся только на content_block_stop, и пустые аргументы означают, что delta съел промежуточный слой; оборванные stream по умолчанию — повторяемые retry всего хода; TTFT reasoning-модели нужен UX-прогресс, а не фикс транспорта; а прод-убийца №1 — буферизующий прокси, превращающий TTFT обратно в общее время, чинится в конфиге пути, а не в приложении. Теперь, когда увидишь семисекундный спиннер, который разрешается разом, — тянешься к конфигу прокси, а не открываешь редактор.
</Recap>
