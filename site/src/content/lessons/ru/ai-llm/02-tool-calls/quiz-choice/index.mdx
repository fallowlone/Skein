---
concepts:
- tool-use-loop
- argument-validation
- max-iteration-guard
- parallel-tool-calls
- tool-choice
- schema-token-cost
estMin: 13
lang: ru
level: senior
order: 7
prereqs: []
slug: quiz-choice
sources:
- https://platform.claude.com/docs/en/agents-and-tools/tool-use/how-tool-use-works
- https://www.anthropic.com/engineering/advanced-tool-use
status: ready
summary: "Тест с выбором на синтез по всему юниту — цикл round-trip, валидация JSON schema как граница доверия, защита от runaway-цикла, parallel tool use и tool_choice."
title: 'Tool calls: тест с выбором ответа'
track: ai-llm
unit: 02-tool-calls
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Шесть вопросов поперёк всего юнита. Ни один не про определение для заучивания — каждый отражает решение, которое ты принимаешь, собирая реальный agent-цикл против реальных, иногда мутирующих, tools.
</Hook>

<Goal lang="ru">
Убедись, что связываешь контракт round-trip, границу доверия при валидации, защиту от runaway, параллелизм и tool_choice — тот синтез, к которому вёл урок.
</Goal>

<Quiz
  id="02-tool-calls-choice-1"
  lessonSlug="02-tool-calls"
  lang="ru"
  question="Задача пользователя требует четырёх tool call на разных шагах. Сколько примерно вызовов модели сделает цикл и почему это важно для стоимости?"
  choices={[
    { label: "Около пяти — каждый round-trip tool call это свежий вызов модели, который заново отправляет весь растущий transcript плюс полный массив tools, так что входные токены и стоимость накапливаются с каждым шагом.", correct: true },
    { label: "Один — модель сама прогоняет все четыре tools внутри и возвращает финальный ответ за один запрос.", misconception: "Модель никогда не исполняет client tools; она лишь эмитит tool_use-запрос. Каждое исполнение требует, чтобы твой код его выполнил и отправил совершенно новый запрос с результатами." },
    { label: "Четыре, и стоимость плоская, потому что биллится только последний ход.", misconception: "Каждый запрос заново шлёт всю накапливающуюся историю плюс блок tools, так что входные токены растут на каждом шаге. Именно это накопление и есть причина, по которой важен prompt caching статичного блока tools." },
    { label: "Два — один вызов чтобы спланировать все шаги и один чтобы их выполнить.", misconception: "Никакого отдельного разделения plan/execute нет. Цикл делает по одному вызову модели на каждый раунд tool_use, пока stop_reason не станет end_turn." },
  ]}
/>

<Quiz
  id="02-tool-calls-choice-2"
  lessonSlug="02-tool-calls"
  lang="ru"
  question="Модель возвращает tool_use для cancel_order с id 'ord_9f3c' — id, которого никогда не было в разговоре. Каков senior-ход?"
  choices={[
    { label: "Провалидировать аргументы по схеме, затем подтвердить, что заказ существует и принадлежит этому вызывающему, и только потом исполнить; при любой ошибке вернуть tool_result с ошибкой, чтобы модель могла само-исправиться.", correct: true },
    { label: "Исполнить — JSON schema в запросе ограничивает модель, значит id точно настоящий.", misconception: "input_schema направляет модель, но не связывает её; модели регулярно эмитят правдоподобные, но выдуманные id. Считать аргументы tool call доверенными — это ровно тот путь, которым галлюцинированный id мутирует не ту запись." },
    { label: "Обернуть вызов в try/catch и дать любому исключению вылететь из цикла.", misconception: "Бросок из цикла завершает ход и не даёт модели шанса восстановиться. Хуже того, голый catch вокруг невалидированной мутации может выстрелить плохим запросом до того, как бросит. Сначала валидируй; ошибку возвращай как tool_result." },
    { label: "Автоматически повторить тот же вызов пару раз — вдруг это временный сбой.", misconception: "Выдуманный аргумент — это неправильно, а не временно. Слепые ретраи лишь жгут токены и могут повторить деструктивное действие. Решение — валидация плюс ошибка, возвращённая модели, а не ретраи." },
  ]}
/>

<Quiz
  id="02-tool-calls-choice-3"
  lessonSlug="02-tool-calls"
  lang="ru"
  question="Агент в проде иногда накручивает огромные счета на отдельных ходах. Логи показывают: он зовёт lookup_order, получает ошибку и зовёт снова с теми же аргументами — снова и снова. В чём корневая причина и фикс?"
  choices={[
    { label: "Незащищённый цикл: ничто не ограничивает итерации, так что запутавшаяся модель бесконечно перезапускает падающий вызов, и каждая итерация — полный биллящийся вызов модели. Добавь жёсткий cap итераций (цикл for по MAX_STEPS, а не while True) плюс repeat-detection, который прерывает при двух подряд одинаковых вызовах с одинаковыми аргументами.", correct: true },
    { label: "Сетевой сбой на lookup_order; добавь exponential backoff и цикл сам выздоровеет.", misconception: "Вызов не падает временно — модель повторяет одинаковые аргументы и получает одинаковую детерминированную ошибку. Backoff лишь замедляет прожигание, но никогда его не завершает; недостающее звено — cap итераций и repeat-detection." },
    { label: "Переполнилось контекстное окно модели; подними max_tokens и она перестанет зацикливаться.", misconception: "max_tokens ограничивает длину вывода одного ответа, а не число итераций цикла. Runaway — это цикл, заново вызывающий модель, и никакой настройкой выходных токенов это не ограничить." },
    { label: "Баг в lookup_order; как только tool починят, цикл не сможет уйти в разнос.", misconception: "Починка tool убирает этот триггер, но любая будущая ошибка — другой tool, плохой аргумент — пересоздаёт runaway. Защита цикла — это структурная оборона, нужная независимо от того, какой tool сломался." },
  ]}
/>

<Quiz
  id="02-tool-calls-choice-4"
  lessonSlug="02-tool-calls"
  lang="ru"
  question="Есть две цепочки. Цепочка A: get_weather(NYC) и get_weather(SF). Цепочка B: find_user(email) затем cancel_user_order(userId). Какую модель может распараллелить и почему?"
  choices={[
    { label: "Только цепочку A. Её два вызова независимы, так что модель может эмитнуть оба tool_use-блока в одном ответе, а ты исполняешь их конкурентно. Цепочка B по своей сути последовательна — cancel_user_order нуждается в выводе find_user, чтобы заполнить аргументы, так что модель должна увидеть первый результат прежде, чем выпустить второй вызов.", correct: true },
    { label: "Обе — модель эмитит все tool_use-блоки заранее, а рантайм сам разрулит порядок.", misconception: "Модель не может заполнить userId для cancel_user_order, пока не увидит результат find_user, так что зависимый вызов нельзя выпустить в том же ходе. Параллелизм здесь блокирует зависимость по данным, а не рантайм." },
    { label: "Ни одну — tool call всегда по одному за ход.", misconception: "Современные модели действительно эмитят несколько tool_use-блоков в одном ответе для независимых вызовов; это и есть выигрыш по латентности от parallel tool use. Можно отказаться через disable_parallel_tool_use, но это не дефолт." },
    { label: "Только цепочку B, потому что мутирующие вызовы надо батчить вместе ради атомарности.", misconception: "Параллелизм про независимость данных, а не про семантику мутаций. Цепочка B как раз последовательная именно потому, что её второй вызов зависит от вывода первого; чтения цепочки A независимы и параллелятся." },
  ]}
/>

<Quiz
  id="02-tool-calls-choice-5"
  lessonSlug="02-tool-calls"
  lang="ru"
  question="Ты строишь классификатор, который всегда должен возвращать одну из фиксированного набора категорий как структурированный JSON — проза недопустима как ответ. Какая настройка tool_choice подходит и почему не остальные?"
  choices={[
    { label: "Форсировать единственный tool классификации (tool_choice: tool). Это гарантирует, что модель зовёт именно его на каждом ходе, так что ты всегда получаешь JSON в той единственной схеме, что определил — стандартный способ получить structured output гарантированной формы.", correct: true },
    { label: "Использовать auto, чтобы модель сама решала, нужен ли tool.", misconception: "auto позволяет модели ответить прозой вместо вызова tool, что ломает контракт «ответ всегда структурированный JSON». auto — для агентов, где проза иногда правильный ответ." },
    { label: "Использовать any, чтобы модель обязана была позвать какой-то tool, но выбирала сама.", misconception: "any форсирует вызов tool, но даёт модели выбирать среди всех; с одним tool это почти эквивалентно, но как только добавишь второй, модель может выбрать не тот. Форсирование именованного tool — точный контроль для извлечения гарантированной формы." },
    { label: "Использовать none и самому парсить прозу в JSON.", misconception: "none запрещает все tools, так что ты получишь свободную прозу и будешь её парсить — ровно тот хрупкий путь, который форсирование tool и призвано заменить. none нужен, чтобы выбить текстовое резюме, когда результаты уже на руках." },
  ]}
/>

<Quiz
  id="02-tool-calls-choice-6"
  lessonSlug="02-tool-calls"
  lang="ru"
  question="Агент с 10 tools прогоняет 6-шаговую задачу, и латентность и стоимость куда выше ожидаемого, хотя каждый tool быстрый. В чём доминирующий оверхед и стандартное смягчение?"
  choices={[
    { label: "Массив tools (~500 токенов на tool, ~5000 здесь) заново шлётся в каждом запросе цикла, так что он биллится и обрабатывается шесть раз. Кэшируй статичный блок tools через prompt caching — это срезает входную стоимость на 40–80% и улучшает time-to-first-token.", correct: true },
    { label: "Сами tools медленные; оптимизируй внутреннюю латентность каждого.", misconception: "По условию каждый tool быстрый. Оверхед — это потокеновый налог на повторную отправку полного массива схем каждый раунд, а не время исполнения tool." },
    { label: "Модель слишком большая; переключись на меньшую, чтобы срезать стоимость вызова.", misconception: "Меньшая модель может быть дешевле за токен, но это не решает повторную отправку 5000 токенов схемы шесть раз. Кэширование статичного блока tools бьёт ровно в реальный оверхед и сохраняет качество модели." },
    { label: "Слишком много определений tools путают модель и провоцируют лишние вызовы; убери tools.", misconception: "Раздутые наборы tools действительно вредят выбору, но оверхед в вопросе — это потокеновая стоимость повторной отправки схемы каждый раунд. Prompt caching бьёт в это напрямую, не выбрасывая нужные tools." },
  ]}
/>

<Recap lang="ru">
Сквозная линия — один контракт: модель лишь запрашивает tool, твой код его исполняет, и цикл заново вызывает модель после каждого результата — так что стоимость накапливается, схемы шлются заново каждый раунд (кэшируй их), а незащищённый цикл уходит в разнос. Валидация — это граница доверия: провалидируй по схеме, затем authorize и existence-check, затем исполни, возвращая ошибки как tool_result, чтобы модель само-исправлялась. Параллелизм помогает лишь независимым вызовам; tool_choice (auto/any/tool/none) задаёт, сработает ли tool и какой.
</Recap>
