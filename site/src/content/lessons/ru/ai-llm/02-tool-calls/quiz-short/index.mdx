---
concepts:
- tool-use-loop
- argument-validation
- max-iteration-guard
- parallel-tool-calls
- tool-choice
- schema-token-cost
estMin: 14
lang: ru
level: senior
order: 8
prereqs: []
slug: quiz-short
sources:
- https://platform.claude.com/docs/en/agents-and-tools/tool-use/how-tool-use-works
- https://www.anthropic.com/engineering/advanced-tool-use
status: ready
summary: "Промпты на свободное воспроизведение по всему юниту tool calls. Сначала ответь своими словами, затем открой эталонный ответ и сравни."
title: 'Tool calls: тест на воспроизведение'
track: ai-llm
unit: 02-tool-calls
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";

<Hook>
Воспроизведение бьёт перечитывание. На каждый промпт скажи или напиши полный ответ по памяти прежде, чем открыть эталон — именно усилие припоминания закрепляет механизм.
</Hook>

<Goal lang="ru">
Восстанови ядро механизмов юнита — контракт round-trip, границу доверия при валидации, защиту от runaway, параллелизм, tool_choice и токеновую стоимость схемы — не подглядывая в урок.
</Goal>

<RetrievalDrawer
  client:load
  id="02-tool-calls-short-retrieval"
  lang="ru"
  questions={[
    { q: "Опиши цикл tool use. Кто вызывающий, кто вызываемый и что делает каждую итерацию дорогой?", a: "Tool calling инвертирует обычный контракт: модель — вызывающий, а твой код — вызываемый. Ты шлёшь запрос с массивом tools; модель отвечает stop_reason: \"tool_use\" и одним или несколькими tool_use-блоками, каждый из которых держит имя tool и JSON-объект аргументов. Твой код исполняет каждый tool и форматирует выводы как tool_result-блоки, затем шлёт совершенно новый запрос с полной историей плюс эти результаты. Цикл повторяется, пока stop_reason остаётся \"tool_use\", и выходит на \"end_turn\" (или max_tokens, stop_sequence, refusal). Каждая итерация дорогая, потому что шаг четыре — это свежий вызов модели, заново отправляющий весь растущий transcript плюс полный массив tools, так что входные токены и стоимость накапливаются с каждым шагом — задача из пяти tools это примерно шесть вызовов модели." },
    { q: "Зачем валидировать аргументы tool call, и что значит «валидировать» для мутирующего эндпоинта вроде cancel_order?", a: "Модель эмитит правдоподобный JSON, а не гарантированно корректный: она может галлюцинировать id, выдумать enum-значение, которого схема не перечисляла, выкинуть required-поле или передать не тот тип. input_schema направляет модель, но не связывает её, так что аргументы tool call — это недоверенный вход, пересекающий границу доверия, ровно как сырой POST формы. Валидировать значит три упорядоченных слоя. Один: провалидировать аргументы по input_schema настоящим валидатором (Pydantic, jsonschema) и отвергнуть кривые формы — никогда не eval и не разбирать их вслепую. Два: authorize и existence-check сущности, потому что корректно сформированный id всё ещё недоверенный — подтверди, что заказ существует и принадлежит этому вызывающему, до мутации. Три: при любом отказе верни tool_result с ошибкой, а не бросай из цикла, чтобы модель прочла ошибку и само-исправилась на следующем ходе. Пропуск любого слоя — это путь, которым выдуманный ord_9f3c отменяет заказ не того клиента." },
    { q: "Почему незащищённый цикл tool use — это и риск корректности, и риск стоимости, и какие два guard ты добавляешь?", a: "Цикл заново вызывает модель после каждого tool_result, так что запутавшаяся модель может застрять: зовёт tool, получает ошибку и зовёт тот же tool с теми же аргументами снова, вечно. Каждая итерация — полный вызов модели, заново шлющий весь transcript плюс массив tools, так что один застрявший ход может тихо сжечь несколько долларов и никогда не завершиться — это риск стоимости. Риск корректности в том, что ничто не мешает модели перезапустить частично-сработавший или деструктивный вызов. Два guard, оба обязательны в проде: первый — жёсткий cap итераций (цикл for по MAX_STEPS, обычно 8–15, никогда while True), который изящно выходит при достижении; второй — loop/repeat-detection: если модель зовёт тот же tool с идентичными аргументами дважды подряд, считай застрявшей и прерви или впрысни сообщение, что вызов уже упал, чтобы она перестала повторять." },
    { q: "Когда модель может распараллелить tool call, а когда цепочка по своей сути последовательна?", a: "Современные модели, когда нужны несколько независимых tools, эмитят несколько tool_use-блоков в одном ответе. Ты исполняешь их конкурентно и возвращаешь все tool_result-блоки вместе, схлопывая несколько последовательных round-trip в один — реальный выигрыш по латентности, ведь каждый round-trip это свежий вызов модели в сотни миллисекунд — секунды. Параллелизм помогает лишь когда вызовы независимы, как get_weather(NYC) и get_weather(SF). Цепочка, где второй вызов нуждается в выводе первого — find_user затем cancel_user_order — по сути последовательна: модель не может заполнить аргументы второго tool, пока не увидит первый результат, так что вынуждена ждать. Если твой слой исполнения не может безопасно гонять конкурентно, выстави disable_parallel_tool_use: true, чтобы форсировать один tool за ход." },
    { q: "Объясни четыре настройки tool_choice и когда senior тянется к каждой.", a: "tool_choice управляет тем, сработает ли tool и какой. auto: модель сама решает, звать tool или ответить прозой — дефолт для агентов, где модель должна судить, нужен ли tool. any: модель обязана позвать какой-то tool, но выбирает сама — когда проза никогда не валидный ответ, как роутер, который обязан диспетчеризовать. tool (форсированный): модель обязана позвать один именованный tool — стандартный способ получить structured output гарантированной формы, ведь форсирование одной схемы делает ответ всегда тем JSON. none: запретить все tools на этом ходе — чтобы выбить текстовое резюме, когда результаты уже на руках. Senior-паттерн — ослаблять от форсированного к auto только когда проза реально приемлемый ответ." },
    { q: "Откуда на самом деле берётся токеновая стоимость tools и каково стандартное смягчение?", a: "Каждое определение tool стоит примерно 500 токенов, и полный массив tools шлётся заново в каждом запросе цикла — он не шлётся один раз. Так что десять tools это около 5000 токенов оверхеда на каждый раунд, и агент с десятью tools, прогоняющий шестишаговую задачу, платит этот налог в 5000 токенов шесть раз, хотя схемы никогда не меняются. Стандартное смягчение — prompt caching статичного блока tools: поскольку массив tools идентичен каждый раунд, его кэширование срезает входную стоимость на 40–80% и улучшает time-to-first-token. Это первое место, где caching окупается в агенте, и поэтому же сокращение числа tools и обрезка многословных схем тоже окупаются." },
  ]}
/>

<Recap lang="ru">
Если ты смог восстановить каждый ответ по памяти, ты держишь хребет юнита: модель — вызывающий, а твой код — вызываемый, так что каждый round-trip это свежий, заново шлющий transcript вызов модели; аргументы — недоверенный вход, который ты валидируешь по схеме, затем authorize и existence-check, возвращая ошибки как tool_result; незащищённый цикл уходит в разнос, так что ограничь итерации и лови повторы; параллелизм помогает лишь независимым вызовам; tool_choice (auto/any/tool/none) задаёт, сработает ли tool и какой; а заново шлющийся массив tools — та токеновая стоимость, ради которой и существует prompt caching.
</Recap>
