---
concepts:
- json-schema-tools
- parallel-tool-calls
- error-as-tool-result
- max-iteration-guard
- argument-validation
estMin: 14
lang: ru
level: senior
order: 9
prereqs: []
slug: quiz-code
sources:
- https://platform.claude.com/docs/en/agents-and-tools/tool-use/how-tool-use-works
- https://platform.claude.com/docs/en/agents-and-tools/tool-use/overview
status: ready
summary: "Читай реальные сниппеты tool calling — JSON schema tool, диспетчер parallel-вызовов, цикл с timeout/обработкой ошибок и валидатор аргументов — и выбирай фикс с наибольшим рычагом."
title: 'Tool calls: чтение кода и схем'
track: ai-llm
unit: 02-tool-calls
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Баги tool calling живут в схеме, диспетчере и цикле — не в модели. Читай каждый сниппет так, как читал бы на ревью, и выбирай фикс, который senior сделал бы первым.
</Hook>

<Goal lang="ru">
Отработай петлю диагностики, которую ты запускаешь на каждом агенте: читай tool schema и код цикла, предскажи, как поведут себя модель и твой handler, и тянись к фиксу, который закрывает границу доверия или останавливает runaway.
</Goal>

## Сниппет 1 — JSON schema tool

Когда видишь схему tool на ревью, первый вопрос не «выглядит ли это правильно?», а «что сделает модель, когда не знает id?» Слабая схема (JSON Schema, описывающая аргументы инструмента) — это приглашение к галлюцинации.

```json
{
  "name": "cancel_order",
  "description": "Cancel an order.",
  "input_schema": {
    "type": "object",
    "properties": {
      "order_id": { "type": "string" },
      "reason":   { "type": "string" }
    }
  }
}
```

<Quiz
  id="02-tool-calls-code-1"
  lessonSlug="02-tool-calls"
  lang="ru"
  question="Мутирующий tool cancel_order выходит с этой схемой. В чём главная слабость и фикс с наибольшим рычагом?"
  choices={[
    { label: "Ничего не required, а description пустой, так что модель угадывает оба поля и может эмитнуть order_id, которого никогда не видела. Добавь \"required\": [\"order_id\"], ограничь reason через enum и напиши description, который скажет модели, откуда берётся валидный id — схема и направляет модель, и служит твоим контрактом для валидации.", correct: true },
    { label: "Схема в порядке; фикс — добавить больше tools, чтобы у модели были альтернативы.", misconception: "Добавление tools не чинит этот. Дефект в том, что схема разрешает свободный, необязательный, неописанный order_id, так что у модели нет ограничения, а у тебя нет ничего строгого для валидации." },
    { label: "Сменить order_id на тип integer, чтобы id нельзя было галлюцинировать.", misconception: "Один лишь тип не останавливает галлюцинацию — модель так же легко выдумает правдоподобный integer, как и строку. Фикс — required-поля, enum где применимо, реальный description плюс рантайм-проверки существования/авторизации." },
    { label: "Убрать input_schema целиком и дать модели слать свободные аргументы.", misconception: "input_schema — это то, что направляет модель к корректным вызовам и даёт тебе контракт для валидации. Убрать её — значит убрать и подсказку, и цель валидации, что есть противоположность фикса." },
  ]}
/>

## Сниппет 2 — диспетчер parallel-вызовов

```python
results = []
for block in response.tool_use_blocks:          # их может быть несколько за ход
    output = TOOLS[block.name](**block.input)    # исполняем последовательно
    results.append(tool_result(block.id, output))
send(messages + results)
```

<Quiz
  id="02-tool-calls-code-2"
  lessonSlug="02-tool-calls"
  lang="ru"
  question="Когда модель эмитит три независимых tool_use-блока за один ход, что этот диспетчер делает верно и что оставляет на столе?"
  choices={[
    { label: "Верно: он собирает каждый tool_use-блок и возвращает все tool_result-блоки в одном последующем запросе. Упущено: независимые вызовы идут последовательно, так что теряется выигрыш по латентности от parallel tool use — гоняй независимые вызовы конкурентно (asyncio.gather / пул потоков) и всё равно возвращай их вместе.", correct: true },
    { label: "Неверно возвращать все результаты одним запросом — каждому tool_result нужен свой round-trip.", misconception: "Все tool_result-блоки для tool_use-блоков одного хода уходят обратно в одном последующем запросе; это ровно то, что делает код. Улучшение — конкурентность исполнения, а не разбиение ответа." },
    { label: "Нужно вызвать disable_parallel_tool_use, иначе результаты перепутаются.", misconception: "Каждый tool_result ссылается на свой block.id, так что сопоставление по id, а не по порядку. Модель намеренно эмитнула параллельные блоки; фикс — исполнять их конкурентно, а не отключать параллелизм." },
    { label: "Передавать **block.input прямо в tool нормально, ведь модель его уже провалидировала.", misconception: "Разворачивать вход от модели прямо в функцию — это недостающая граница доверия: аргументы могут быть галлюцинированными или кривыми. Валидируй по input_schema до вызова, независимо от вопроса параллелизма." },
  ]}
/>

## Сниппет 3 — цикл без защиты

```python
while True:
    resp = model.create(messages=messages, tools=TOOLS)
    if resp.stop_reason != "tool_use":
        break
    for b in resp.tool_use_blocks:
        out = run_tool(b.name, b.input)          # может бросить / зависнуть
        messages.append(tool_result(b.id, out))
    messages.append(resp.message)
```

<Quiz
  id="02-tool-calls-code-3"
  lessonSlug="02-tool-calls"
  lang="ru"
  question="Этот цикл работает в проде против client tools. Какие два дефекта укусят первыми при сбое и как их чинить?"
  choices={[
    { label: "У while True нет cap итераций, так что модель, продолжающая эмитить tool_use, крутится вечно и биллит каждый ход; и у run_tool нет timeout, так что зависший tool стопорит весь ход. Фикс: for step in range(MAX_STEPS) с graceful-выходом и repeat-detection, плюс оборачивай каждый tool в timeout, возвращающий tool_result с ошибкой.", correct: true },
    { label: "Дефект в том, что resp.message добавляется после tool_results; переставь их — и всё верно.", misconception: "Порядок сообщений — реальная забота, но не то, что кусает первым при сбое. Опасные дыры — отсутствующий cap итераций (runaway-стоимость) и отсутствующий per-tool timeout (зависший tool стопорит ход)." },
    { label: "stop_reason надо явно проверять на end_turn, иначе цикл никогда не выйдет.", misconception: "break при stop_reason, отличном от tool_use, уже выходит на end_turn, max_tokens и т.д. Уязвимость — безграничный while True на пути tool_use и отсутствие timeout у tool." },
    { label: "Нужен disable_parallel_tool_use, иначе конкурентные tools портят messages.", misconception: "Этот цикл и так гоняет tools последовательно, так что параллельная безопасность тут ни при чём. Дыры режима сбоя — защита от runaway и per-tool timeout." },
  ]}
/>

## Сниппет 4 — валидатор

```python
def handle(block):
    args = block.input
    try:
        validated = ToolArgs.model_validate(args)   # pydantic: форма + типы
    except ValidationError as e:
        return tool_result(block.id, f"invalid arguments: {e}", is_error=True)
    return tool_result(block.id, run(validated))
```

<Quiz
  id="02-tool-calls-code-4"
  lessonSlug="02-tool-calls"
  lang="ru"
  question="Этот handler валидирует по схеме через Pydantic и возвращает ошибки как tool_result. Для мутирующего tool против multi-tenant БД чего всё ещё не хватает?"
  choices={[
    { label: "Валидация формы/типов пропускает корректно сформированный id, которого может не существовать или который принадлежит другому тенанту. Добавь проверку авторизации и существования после model_validate — подтверди, что сущность есть и принадлежит этому вызывающему, до run() — возвращая tool_result с ошибкой, если нет.", correct: true },
    { label: "Ничего — валидация Pydantic это полная граница доверия для мутирующего вызова.", misconception: "Валидация по схеме лишь доказывает, что аргументы правильной формы, а не то, что сущность реальна или принадлежит вызывающему. Провалидированный, но выдуманный id всё равно мутирует не ту запись без проверки авторизации/существования." },
    { label: "Надо бросать на ValidationError, а не возвращать tool_result, чтобы цикл остановился.", misconception: "Возврат ошибки как tool_result верен — он даёт модели прочитать ошибку и само-исправиться на следующем ходе. Бросок из цикла завершает ход и убирает этот путь восстановления." },
    { label: "Надо автоматически ретраить run(validated) при сбое ради устойчивости.", misconception: "Слепые ретраи мутирующего вызова рискуют повторить деструктивное действие и сжечь токены. Недостающий слой — проверка авторизации/существования до исполнения, а не retry-on-failure." },
  ]}
/>

<Recap lang="ru">
Каждый баг tool calling читается в схеме, диспетчере, цикле или валидаторе. Слабая схема (нет required, нет enum, пустой description) даёт модели угадывать и не даёт тебе что валидировать. Последовательный диспетчер корректен, но теряет выигрыш по латентности от parallel-вызовов для независимых tools. while True без per-tool timeout — это runaway и зависание, ждущие случиться: ограничь итерации и тайм-боксь каждый tool. А валидация формы через Pydantic необходима, но недостаточна для мутирующего вызова: добавь проверку авторизации и существования и возвращай каждый отказ как tool_result, чтобы модель само-исправлялась. Теперь, когда встретишь `while True` в ревью или схему без `required`, ты знаешь, какой режим сбоя каждый из них открывает — и какой фикс его закрывает.
</Recap>
