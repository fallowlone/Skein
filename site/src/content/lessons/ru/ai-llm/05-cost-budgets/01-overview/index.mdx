---
concepts:
- token-pricing-asymmetry
- context-accumulation
- model-routing
- prompt-caching
- budget-caps
- runaway-agent-loop
deepensInto: []
estMin: 17
lang: ru
lessonType: topic
level: junior
order: 1
prereqs: []
slug: 01-overview
sources:
- https://www.cloudzero.com/blog/claude-api-pricing/
- https://platform.claude.com/docs/en/build-with-claude/prompt-caching
- https://relayplane.com/blog/agent-runaway-costs-2026
- https://www.truefoundry.com/blog/rate-limiting-ai-agents-preventing-llm-api-exhaustion
spiral: []
status: ready
summary: Output-токены стоят ~5x от input, а system prompt + история + RAG-контекст
  пересылаются каждый ход — поэтому зацикленный агент сжигает тысячи за ночь. Роутинг,
  кэширование и жёсткие лимиты бюджета на запрос держат трату в рамках.
title: 'Бюджеты на LLM: асимметрия токенов, роутинг и kill switch'
track: ai-llm
unit: 05-cost-budgets
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
Вечер пятницы, агент выкатывается за фича-флагом. У него есть tool-loop (цикл «наблюдай—думай—действуй—повтори») — и нет лимита на итерации. Документ, который он подтягивает, ссылается на другой, тот — на третий. Агент дописывает каждый в контекст и пересылает весь растущий транскрипт модели каждый ход. К понедельнику счёт показывает $4 300 за одну «задачу», которая так и не завершилась. Месячный алерт на $1 000 сработал — в 2 ночи, в Slack-канал, который по выходным никто не читает.

Через десять минут ты будешь знать, почему цикл ускорялся, почему месячный cap не помог и какие рычаги действительно держат счёт в рамках.
</Hook>

<Crux>Стоимость LLM определяют пересылаемый контекст и output-токены; цикл без лимита на запрос сожжёт тысячи раньше любого алерта.</Crux>

<Explanation>

## Output-токены — дорогая половина

Первое, что удивляет инженеров в их первом счёте: input и output стоят не одинаково. У всех текущих фронтир-моделей output идёт примерно в 5x от input. На май 2026 Anthropic указывает Haiku 4.5 за $1/M input и $5/M output, Sonnet 4.6 за $3/$15, Opus 4.7 за $5/$25. GPT-4o у OpenAI — $2.50/$10, та же 4x-асимметрия.

Рычаг, который это даёт, конкретен: многословный ответ стоит куда дороже многословного вопроса. Ограничение `max_tokens`, просьба о кратком или структурированном выводе и запрет модели «рассуждать вслух» в финальном ответе — всё это бьёт по дорогой стороне баланса. Сеньор, разбирающий скачок стоимости, сначала проверяет объём output, а не input.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Модель (май 2026)</th>
        <th class="border border-border px-3 py-2 text-left">Input /M</th>
        <th class="border border-border px-3 py-2 text-left">Output /M</th>
        <th class="border border-border px-3 py-2 text-left">Output : input</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">Claude Haiku 4.5</td>
        <td class="border border-border px-3 py-2">$1.00</td>
        <td class="border border-border px-3 py-2">$5.00</td>
        <td class="border border-border px-3 py-2">5x</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Claude Sonnet 4.6</td>
        <td class="border border-border px-3 py-2">$3.00</td>
        <td class="border border-border px-3 py-2">$15.00</td>
        <td class="border border-border px-3 py-2">5x</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Claude Opus 4.7</td>
        <td class="border border-border px-3 py-2">$5.00</td>
        <td class="border border-border px-3 py-2">$25.00</td>
        <td class="border border-border px-3 py-2">5x</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">OpenAI GPT-4o</td>
        <td class="border border-border px-3 py-2">$2.50</td>
        <td class="border border-border px-3 py-2">$10.00</td>
        <td class="border border-border px-3 py-2">4x</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">GPT-4o mini</td>
        <td class="border border-border px-3 py-2">$0.15</td>
        <td class="border border-border px-3 py-2">$0.60</td>
        <td class="border border-border px-3 py-2">4x</td>
      </tr>
    </tbody>
  </table>
</div>

## Где деньги на самом деле накапливаются

Прежде чем тянуться за дешёвой моделью или поднимать лимит, спроси себя: сколько раз каждый токен реально пересылается? Ответ — там и живёт настоящий счёт.

Таблица цен — ловушка, если на ней остановиться, потому что она оценивает один вызов. Счёт же строится на том, что LLM stateless: каждый ход пересылает весь контекст целиком. Три вещи тихо раздувают этот пересылаемый payload.

**System prompt** (системная инструкция, отправляемая перед каждым диалогом) отправляется на каждом ходу. System prompt в 4 000 токенов в разговоре из 50 ходов — это не 4 000 токенов, а 200 000 input-токенов, оплаченных 50 раз. **История разговора** растёт линейно: ход 20 тащит с собой ходы 1–19, поэтому цена за ход растёт по ходу чата, а длинная сессия квадратична по сумме. **RAG-контекст** (Retrieval-Augmented Generation — подход, при котором релевантные документы извлекаются и дословно вставляются в запрос) — самая тяжёлая разовая добавка: запихивать 30 извлечённых чанков (скажем, 15 000 токенов) в каждый запрос, когда хватило бы 3, — это 5x-множитель input-стоимости, которого никто не видит до счёта.

Вот почему postmortem про runaway-loop — каноничный AI-инцидент по стоимости. Агент, который дописывает результат tool в контекст на каждой итерации без лимита, не просто делает больше вызовов — каждый вызов *больше* предыдущего. Стоимость растёт суперлинейно. Среди реальных случаев — агент, сделавший 14 000 избыточных tool-вызовов, и разработчики, наблюдавшие, как $15 испаряются меньше чем за 10 минут. Урон нанесён до того, как человек вообще посмотрит.

<Inset kind="why" lang="ru">
Почему месячный лимит не спас пятничного агента? Лимиты трат у провайдера работают на уровне биллингового цикла — это бэкстоп, измеряемый в днях, а не в секундах. Лимит `$1,000/month` не может остановить цикл, сжигающий `$6` за 30 секунд; к тому моменту, как сумма цикла пересечёт черту, тысячи уже потрачены. Тормоз должен жить in-process, на скорости трат на запрос и на сессию, а не в биллинговом дашборде.
</Inset>

## Роутинг: не плати по тарифу Opus за работу для Haiku

Большинство запросов в реальном продукте несложные. Классификация, извлечение, короткие переписывания, решения о маршрутизации — маленькая дешёвая модель справляется с ними за долю стоимости. Паттерн сеньора — каскад: отправить запрос сначала в дешёвую модель и *эскалировать на дорогую только когда проверка качества провалилась* или задача распознана как сложная заранее. Перенаправление лёгких 80% на Haiku ($1/$5) вместо Opus ($5/$25) — это 5x-срез на этой доле без видимой пользователю потери.

Трейдофф реален и стоит назвать: роутинг добавляет шаг классификации (latency, иногда собственный вызов модели), а неверный маршрут тихо ухудшает качество. Ты платишь за путь эскалации — дешёвый вызов плюс дорогой ретрай стоят *дороже*, чем сразу пойти в дорогую модель, когда дешёвая провалилась. Роутинг выигрывает, только если дешёвая модель преуспевает достаточно часто, чтобы среднее село заметно ниже плоского дорогого тарифа. Меряй частоту эскалаций; если она выше ~30%, твой роутер или твоя дешёвая модель неверны.

## Prompt caching: перестань платить за неизменный префикс

Самый крупный рычаг против пересылаемого контекста — prompt caching. Ты помечаешь стабильный префикс — system prompt, определения tool, фиксированный RAG-корпус — и провайдер его кэширует. У Anthropic чтение из кэша стоит `0.1x` от базового input-тарифа (скидка 90%), а запись для заполнения кэша — `1.25x` (TTL 5 минут) или `2x` (TTL 1 час) от базового. Арифметика: 5-минутный кэш окупается после первого попадания; часовой — после второго.

Это превращает катастрофу «system prompt в 4 000 токенов, отправленный 50 раз» в одну полноценную запись плюс 49 чтений по десятой цены. Кэширование не помогает частям, которые меняются каждый ход (последнее сообщение пользователя, свежее извлечение), поэтому инженерный ход — упорядочить промпт под кэш: стабильное содержимое первым, изменчивое последним, чтобы кэшируемый префикс был как можно длиннее.

<TradeoffMatrix
  id="ai-cost-budgets-tradeoff-1"
  lessonSlug="01-overview"
  lang="ru"
  prompt="Чат-продукт пересылает system prompt в 4 000 токенов каждый ход в сессиях по 50 ходов, и input-счёт доминирует в тратах. Выбери фикс с наибольшим рычагом."
  options={[
    { name: "Prompt-кэшировать стабильный префикс (system prompt + tool defs), изменчивое последним", summary: "Кэшировать неизменную часть.", correct: true, justification: "System prompt идентичен каждый ход — учебная цель кэширования. При 0.1x на чтение это превращает 50 полноценных отправок в одну запись плюс 49 чтений по десятой тарифа, ~90% скидки на префикс. Порядок «стабильное первым, изменчивое последним» максимизирует кэшируемый отрезок. Это бьёт ровно по тому, что раздувает счёт: пересылаемый неизменный input." },
    { name: "Перевести каждый запрос на модель подешевле независимо от сложности", summary: "Глобальный даунгрейд.", justification: "Срезает тариф, но игнорирует причину высокого счёта — пересылаемый контекст — и слепо торгует качеством на сложных ходах. Модель подешевле всё равно пересылает префикс в 4 000 токенов каждый ход; ты уменьшил множитель, а не убрал расход. Роутинг должен быть избирательным, а не глобальным даунгрейдом." },
    { name: "Поднять месячный лимит трат, чтобы алерты перестали срабатывать", summary: "Заглушить алерт.", justification: "Это сам failure mode, а не фикс — он убирает единственный сигнал, пока базовый расход продолжается. Лимиты — бэкстоп, а не оптимизация, и их заглушение — это как раз способ не заметить runaway-loop до счёта." },
    { name: "Обрезать историю разговора до последних 2 ходов", summary: "Сбросить историю.", justification: "Помогает оси роста истории и иногда верно, но здесь названная стоимость — это system prompt, пересылаемый каждый ход; обрезка истории не трогает этот фиксированный префикс, а агрессивная обрезка ломает многоходовую связность. Кэширование — точечный фикс; обрезка истории — отдельный, дополняющий рычаг." },
  ]}
/>

<Quiz
  id="ai-cost-budgets-quiz-1"
  lessonSlug="01-overview"
  lang="ru"
  question="У текущих фронтир-моделей как цены на output-токены соотносятся с input?"
  choices={[
    { label: "Output стоит примерно 4–5x от input, поэтому многословный ответ заметно дороже многословного промпта", correct: true },
    { label: "Input и output тарифицируются одинаково", misconception: "Они не симметричны — output идёт около 4–5x от input у Claude и GPT-4o, поэтому ограничение max_tokens и просьба о кратком выводе — реальный рычаг стоимости." },
    { label: "Input дороже, потому что модель должна его прочитать", misconception: "Наоборот — генерация и есть дорогая половина. Чтение input дёшево; производство output-токенов и есть то, что оценивается высоким тарифом." },
    { label: "Output бесплатен, как только заплатил за input", misconception: "Output тарифицируется по токенам по самому высокому тарифу прайс-листа; это та половина, которую сеньор проверяет первой при скачке стоимости." },
  ]}
/>

<Quiz
  id="ai-cost-budgets-quiz-2"
  lessonSlug="01-overview"
  lang="ru"
  question="Автономный агентский цикл без бюджета на запрос работает ночью и выставляет счёт $4 300. Почему месячный лимит трат на $1 000 его не остановил?"
  choices={[
    { label: "Лимиты провайдера действуют на уровне биллингового цикла — слишком грубо и медленно, чтобы остановить цикл, жгущий стоимость за секунды", correct: true },
    { label: "Лимит применяется только к output-токенам, не к input", misconception: "Лимиты трат покрывают общую трату, а не один класс токенов. Проблема в тайминге и гранулярности: месячный бэкстоп не ловит посекундный runaway." },
    { label: "Агент был на другом API-ключе, который лимит не покрывал", misconception: "Даже на нужном ключе месячный лимит срабатывает после того, как сумма цикла пересечёт черту — слишком поздно для цикла, тратящего тысячи за часы." },
    { label: "Кэширование отключило лимит", misconception: "Кэширование не связано с применением лимита. Реальный разрыв в том, что тормоз должен жить in-process на скорости трат, а не в месячной биллинговой сумме." },
  ]}
/>

<DragOrder
  id="ai-cost-budgets-drag-1"
  lessonSlug="01-overview"
  lang="ru"
  prompt="Расставь контроли стоимости от самого дешёвого/первой линии до kill switch последней инстанции:"
  items={[
    { id: "a", label: "Перенаправь лёгкое большинство на маленькую дешёвую модель; эскалируй только на провале" },
    { id: "b", label: "Prompt-кэшируй стабильный префикс, чтобы пересылаемый контекст упал до 0.1x" },
    { id: "c", label: "Ограничь max_tokens и обрежь историю/RAG до того, что нужно задаче" },
    { id: "d", label: "Применяй бюджет на запрос и на пользователя in-process, перед каждым вызовом" },
    { id: "e", label: "Жёсткий kill switch: размыкай circuit breaker по скорости трат или счётчику итераций" },
  ]}
  correctOrder={["a", "b", "c", "d", "e"]}
/>

</Explanation>

<FlowDiagram
  label="Стоимость = пересылаемый контекст + output-токены; рычаги бьют по каждому, заканчиваясь kill switch"
  caption="Каждый запрос платит за пересылаемый system prompt + историю + RAG плюс сгенерированный output. Применяй рычаги от дешёвого к дорогому: перенаправь лёгкое большинство на маленькую модель, кэшируй стабильный префикс по 0.1x, ограничь output и обрежь контекст, применяй бюджет на запрос in-process и размыкай kill switch по скорости трат."
  hue="var(--d-ai)"
  perRow={5}
  nodes={[
    { id: "route", label: "Роутинг дешёвых", sub: "малая модель; эскалация на фейле" },
    { id: "cache", label: "Кэш префикса", sub: "пересылаемый контекст → 0.1x" },
    { id: "cap", label: "Лимит output / обрезка", sub: "max_tokens, история, RAG" },
    { id: "budget", label: "Бюджет на запрос", sub: "in-process, перед вызовом" },
    { id: "kill", label: "Kill switch", sub: "размыкай по скорости / циклу" },
  ]}
  edges={[
    { from: "route", to: "cache" },
    { from: "cache", to: "cap" },
    { from: "cap", to: "budget" },
    { from: "budget", to: "kill", label: "последняя инстанция" },
  ]}
/>

<KeyTakeaway>Output ~5x от input; prompt, история и RAG шлются каждый ход. Роуть дешёвое первым, кэшируй стабильный префикс, ограничивай output и применяй бюджеты на запрос с in-process kill switch.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="ai-cost-budgets-retrieval"
  lang="ru"
  questions={[
    { q: "Разбери по шагам, почему длинный агентский цикл стоит суперлинейно, а не просто линейно относительно числа итераций.", a: "Два эффекта складываются. Первый: больше итераций — больше вызовов модели, и это само по себе линейно. Второй, и это ловушка: каждая итерация дописывает свой результат tool (или новое извлечение, или рассуждение) в контекст, который пересылается на следующем вызове. Так итерация 1 шлёт маленький payload, итерация 20 пересылает всё из итераций 1–19 плюс system prompt, а итерация 50 тащит весь накопленный транскрипт. Стоимость за вызов растёт с номером итерации, а суммарная стоимость — это сумма растущего ряда, примерно квадратичная по итерациям, а не линейная. Поэтому цикл без лимита не падает мягко: он ускоряется. Фикс — бюджет токенов на запрос и лимит итераций, применяемые in-process, плюс обрезка контекста, чтобы старые результаты tool не ехали с собой вечно. Месячный лимит трат не помогает, потому что меряет не то и не на том масштабе времени — к моменту, как сумма цикла сработает, суперлинейное горение уже потратило тысячи." },
    { q: "Твой input-счёт доминирует над output, и трейсинг показывает один и тот же system prompt в 4 000 токенов, отправляемый каждый ход. Какие конкретные шаги это срежут и каков порядок рычага?", a: "Сначала распознай форму: фиксированный префикс, пересылаемый каждый ход, — это учебная цель prompt caching. Пометь system prompt и определения tool как кэшируемый префикс; при чтении из кэша по 0.1x от базового input-тарифа ты превращаешь 50 полноценных отправок в одну запись (по 1.25x для TTL 5 минут или 2x для часа) плюс 49 чтений по десятой цены — около 90% среза на этом префиксе, окупается после первого попадания. Упорядочь промпт «стабильное первым, изменчивое последним», чтобы кэшируемый отрезок был максимально длинным. Второй: бей по росту истории отдельно — обрезай или суммируй старые ходы, чтобы разговор не тащил всё своё прошлое на каждом вызове. Третий: проаудируй RAG — если ты пихаешь 30 чанков, когда хватает 3, ты платишь 5x-множитель input; ужми извлечение. Кэширование здесь рычаг с наибольшим эффектом, потому что бьёт ровно по названному — неизменному пересылаемому префиксу — без потери качества." },
  ]}
/>

<Recap lang="ru">
Трату на LLM определяет не столько заголовочный тариф за токен, сколько то, что пересылается каждый ход, и сколько output ты генерируешь. Output стоит примерно 5x от input у текущих моделей (Haiku 4.5 $1/$5, Sonnet 4.6 $3/$15, Opus 4.7 $5/$25, GPT-4o $2.50/$10), поэтому сначала ограничивай output и проси краткие ответы. Реальный накопитель — контекст: stateless-модель пересылает system prompt, всю растущую историю и все RAG-чанки на каждом вызове, поэтому длинный агентский цикл стоит суперлинейно, а runaway-loop может сжечь тысячи за ночь. Перенаправляй лёгкое большинство на дешёвую модель и эскалируй только на провале; prompt-кэшируй стабильный префикс, чтобы уронить пересылаемый контекст до 0.1x; обрезай историю и ужимай извлечение. И главное — применяй бюджеты на запрос и на пользователя in-process с жёстким kill switch по скорости трат: месячные лимиты провайдера — это бэкстоп, измеряемый в днях, слишком медленный, чтобы остановить цикл, тратящий доллары в секунду. Теперь, когда встретишь скачок в счёте, первым делом проверяй объём output и пересылаемый контекст — тормоз ставь in-process, а не в биллинговом дашборде.
</Recap>
