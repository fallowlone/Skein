---
concepts:
- token-pricing-asymmetry
- context-accumulation
- model-routing
- prompt-caching
- budget-caps
- runaway-agent-loop
estMin: 13
lang: ru
level: senior
order: 7
prereqs: []
slug: quiz-choice
sources:
- https://platform.claude.com/docs/en/build-with-claude/prompt-caching
- https://www.cloudzero.com/blog/claude-api-pricing/
- https://relayplane.com/blog/agent-runaway-costs-2026
status: ready
summary: "Синтез с множественным выбором по юниту cost budgets — асимметрия цен на token, повторно отправляемый context, model routing, prompt caching и in-process budget-гардрейлы."
title: 'LLM cost budgets: тест с множественным выбором'
track: ai-llm
unit: 05-cost-budgets
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Шесть вопросов через весь юнит. Каждый отражает решение, которое ты принимаешь в реальном cost-инциденте — не определение для пересказа, а tradeoff, который нужно взвесить, пока счётчик крутится.
</Hook>

<Goal lang="ru">
Убедись, что можешь связать цены на token, повторно отправляемый context, routing, caching и in-process budget в одно решение — синтез, к которому вёл обзорный урок.
</Goal>

<Quiz
  id="05-cost-budgets-choice-1"
  lessonSlug="05-cost-budgets"
  lang="ru"
  question="Чат-бот поддержки на Sonnet 4.6 ($3/M вход, $15/M выход) отправляет вопрос на 200 token и получает ответ на 1500 token, по большей части chain-of-thought, который пользователь не видит. Где расход и какой первый рычаг?"
  choices={[
    { label: "Доминирует выход — 1500 output token по $15/M это $0.0225 против $0.0006 за вход, ~37x. Первый рычаг: ограничить max_tokens и не давать модели рассуждать вслух в финальном ответе, чтобы перестать платить 5x output rate за невидимые token.", correct: true },
    { label: "Доминирует вход, потому что system prompt тоже едет с запросом — сократи вопрос.", misconception: "Даже с system prompt выход по $15/M против $3/M входа — дорогая половина здесь. Ответ на 1500 token многократно перевешивает вопрос на 200 token; цель — многословный ответ, а не вопрос." },
    { label: "Стоят одинаково; цена за token симметрична.", misconception: "Выход идёт ~5x входа на Sonnet ($15 против $3 за M). Многословный ответ намного дороже многословного prompt — в этом весь смысл юнита." },
    { label: "Reasoning-token бесплатны, раз пользователь их не видит.", misconception: "Каждый сгенерированный token биллится по output rate, показан он или нет. Скрытый chain-of-thought — это оплаченный выход, поэтому senior на скачке расходов первым делом смотрит объём выхода." },
  ]}
/>

<Quiz
  id="05-cost-budgets-choice-2"
  lessonSlug="05-cost-budgets"
  lang="ru"
  question="Чат на 50 ходов повторно отправляет system prompt на 4000 token каждый ход, и input-счёт растёт. У какого фикса наибольший рычаг?"
  choices={[
    { label: "Prompt-кэшировать стабильный префикс (system prompt + tool defs). Чтения из cache стоят 0.1x базового входа, превращая 50 полноценных отправок в одну запись плюс 49 чтений по десятой части — ~90% экономии на префиксе, окупается после первого попадания.", correct: true },
    { label: "Перевести весь продукт на более дешёвую модель.", misconception: "Более дешёвая модель всё равно повторно отправляет префикс на 4000 token каждый ход; ты режешь rate, но не сам waste, и вслепую жертвуешь качеством на сложных ходах. Caching бьёт точно по неизменному повторно отправляемому входу." },
    { label: "Поднять месячный spend cap, чтобы алерты перестали срабатывать.", misconception: "Это глушит единственный сигнал, пока waste продолжается. Cap — это backstop, а не оптимизация; убрать алерт — так runaway и остаётся незамеченным до инвойса." },
    { label: "Обрезать history до последних 2 ходов.", misconception: "Обрезка history — реальный, но отдельный рычаг; она ничего не делает с фиксированным system-префиксом, отправляемым каждый ход, а агрессивная обрезка ломает связность multi-turn. Caching — точечный фикс здесь." },
  ]}
/>

<Quiz
  id="05-cost-budgets-choice-3"
  lessonSlug="05-cost-budgets"
  lang="ru"
  question="Команда направляет лёгкие 80% запросов на Haiku ($1/$5) и эскалирует неудачи на Opus ($5/$25). После запуска счёт почти не сдвинулся. Самая вероятная причина?"
  choices={[
    { label: "Слишком высокий escalation rate. Когда дешёвый вызов часто проваливается, ты платишь за Haiku и затем за Opus — больше, чем сразу за Opus. Routing выигрывает, только когда дешёвая модель успешна достаточно часто, чтобы среднее легло ниже плоского дорогого rate; выше ~30% эскалаций — router или дешёвая модель неправильны.", correct: true },
    { label: "Routing всегда экономит деньги, значит измерение неверно.", misconception: "Routing может стоить дороже, чем без него, когда путь дёшево-потом-дорого срабатывает слишком часто. Шаг классификации плюс retry эскалации добавляют стоимость; экономия зависит от низкого escalation rate." },
    { label: "Haiku и Opus стоят одинаково, так что routing бессмыслен.", misconception: "Haiku — $1/$5, Opus — $5/$25 — разрыв в 5x на дёшево направленном срезе. Разрыв в ценах реален; проблема в том, что эскалации съедают экономию." },
    { label: "Классификатор должен всегда использовать Opus для надёжности.", misconception: "Запускать дорогую модель, чтобы решить, какую модель запустить, обнуляет смысл. Тяжёлый router может стоить дороже, чем экономит; держи решение о routing дешёвым или эвристическим." },
  ]}
/>

<Quiz
  id="05-cost-budgets-choice-4"
  lessonSlug="05-cost-budgets"
  lang="ru"
  question="Автономный agent loop без лимита итераций крутится всю ночь и выставляет счёт $4300. Почему месячный cap в $1000 его не остановил?"
  choices={[
    { label: "Provider spend cap действует на уровне billing-цикла — в днях, не секундах. Месячный backstop не остановит loop, сжигающий доллары в секунду; пока итог цикла пересечёт черту, тысячи уже потрачены. Тормоз должен жить in-process на cost velocity по запросу и по сессии.", correct: true },
    { label: "Cap считает только output-token, не input.", misconception: "Spend cap считает общий расход, а не один класс token. Разрыв в тайминге и гранулярности — месячный итог не ловит per-second runaway." },
    { label: "Agent работал на API-ключе, который cap не покрывал.", misconception: "Даже на покрытом ключе месячный cap срабатывает лишь после того, как итог цикла пересечёт порог — слишком поздно для loop, тратящего тысячи за часы." },
    { label: "Caching отключил проверку cap.", misconception: "Caching не связан с enforcement cap. Реальный фикс — in-process kill switch на cost velocity и счётчик итераций, а не лимит в billing-дашборде." },
  ]}
/>

<Quiz
  id="05-cost-budgets-choice-5"
  lessonSlug="05-cost-budgets"
  lang="ru"
  question="Почему agent loop без лимита стоит суперлинейно по числу итераций, а не просто линейно?"
  choices={[
    { label: "Складываются два эффекта: больше итераций — больше вызовов (линейно), и каждая итерация дописывает свой tool-результат в context, повторно отправляемый следующим ходом, так что итерация 50 несёт весь накопленный транскрипт. Стоимость вызова растёт с номером итерации, делая итог примерно квадратичным.", correct: true },
    { label: "Это линейно — каждый вызов стоит одинаково, просто их больше.", misconception: "Вызовы не одного размера. Stateless-модель повторно отправляет растущий транскрипт, так что каждый поздний вызов больше предыдущего; итог — сумма растущего ряда, а не плоская стоимость за вызов." },
    { label: "Output-token растут экспоненциально за ход.", misconception: "Суперлинейный рост идёт от накопления повторно отправляемого input-context, а не от взрыва выхода. Каждый ход переплачивает за содержимое всех предыдущих ходов." },
    { label: "Provider поднимает rate с ростом объёма.", misconception: "Цены за token плоские; многие провайдеры дают объёмные скидки, а не надбавки. Суперлинейность структурна — накопление повторно отправляемого context — а не ценовой tier." },
  ]}
/>

<Quiz
  id="05-cost-budgets-choice-6"
  lessonSlug="05-cost-budgets"
  lang="ru"
  question="Ты проектируешь cost-контроли для LLM-фичи. Какой порядок — от самой дешёвой первой линии обороны до крайнего средства — отражает приоритет юнита?"
  choices={[
    { label: "Направить лёгкий трафик дёшево → prompt-кэшировать стабильный префикс → ограничить выход и обрезать history/RAG → ввести per-request и per-user budget in-process → жёсткий kill switch на cost velocity или счётчик loop.", correct: true },
    { label: "Сначала kill switch, остальное — как оптимизация.", misconception: "Kill switch — backstop крайнего средства, а не первая линия. Ты снижаешь расход обычного пути (routing, caching, обрезка) до того, как опираться на circuit breaker, который срабатывает только в runaway." },
    { label: "Поднять месячный cap, затем добавить caching при необходимости.", misconception: "Поднять cap — вообще не контроль, это ослабление backstop. Caching и routing снижают расход; cap лишь ограничивает худший случай постфактум." },
    { label: "Перейти на самую дешёвую модель глобально, больше ничего не нужно.", misconception: "Глобальный даунгрейд игнорирует повторно отправляемый context и вслепую жертвует качеством. Routing должен быть выборочным, а caching плюс in-process budget всё равно важны — один рычаг не ограничит runaway." },
  ]}
/>

<Recap lang="ru">
Сквозная линия — одно дерево решений: выход стоит ~5x входа, поэтому ограничь его первым; stateless-модель повторно отправляет system prompt, history и RAG каждый ход, поэтому кэшируй стабильный префикс и обрезай волатильные части; направляй лёгкое большинство дёшево и следи за escalation rate; а поскольку runaway loop стоит суперлинейно, а месячный cap меряется в днях, реальный тормоз — это in-process budget плюс kill switch на cost velocity. Каждый контроль снижает или ограничивает повторно отправляемый context и выход раньше, чем ограничивает счёт.
</Recap>
