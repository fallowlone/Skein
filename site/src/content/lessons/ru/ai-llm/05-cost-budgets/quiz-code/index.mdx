---
concepts:
- token-cost-arithmetic
- per-tenant-budget
- model-routing
- cache-savings
estMin: 14
lang: ru
level: senior
order: 9
prereqs: []
slug: quiz-code
sources:
- https://platform.claude.com/docs/en/build-with-claude/prompt-caching
- https://www.cloudzero.com/blog/claude-api-pricing/
- https://www.truefoundry.com/blog/rate-limiting-ai-agents-preventing-llm-api-exhaustion
status: ready
summary: "Читай реальный код учёта token и ценовую арифметику, предсказывай расход и выбирай фикс с наибольшим рычагом — cost-арифметика, per-tenant budget, routing и cache savings."
title: 'LLM cost budgets: код и cost-арифметика'
track: ai-llm
unit: 05-cost-budgets
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Cost-инциденты диагностируются в счётчиках token и ценовой математике, а не в прозе. Прочитай код и числа, затем выбери фикс, который senior-инженер сделает первым.
</Hook>

<Goal lang="ru">
Отработай цикл, который ты запускаешь на каждом скачке расходов: прочитай место вызова, посчитай per-token арифметику в уме, найди, где утекает budget, и потянись за фиксом с наибольшим рычагом раньше, чем поднимешь любой cap.
</Goal>

## Сниппет 1 — per-turn cost-арифметика

```ts
// Sonnet 4.6: $3 / 1M вход, $15 / 1M выход
const PRICE = { in: 3 / 1e6, out: 15 / 1e6 };

// сессия на 50 ходов, system prompt на 4000 token повторно каждый ход,
// history растёт ~600 token/ход, каждый ответ ~400 output token.
let inputTotal = 0;
for (let turn = 1; turn <= 50; turn++) {
  const history = 600 * (turn - 1);
  inputTotal += 4000 + history;        // system prompt + накопленный history
}
const outputTotal = 50 * 400;
const cost = inputTotal * PRICE.in + outputTotal * PRICE.out;
```

<Quiz
  id="05-cost-budgets-code-1"
  lessonSlug="05-cost-budgets"
  lang="ru"
  question="Какое утверждение о стоимости этой сессии верно?"
  choices={[
    { label: "Доминирует вход, и большая часть — повторно отправляемый system prompt: 4000 × 50 = 200 000 token фиксированного префикса (~$0.60), плюс ~735 000 от накопленного history — квадратичный член history и фиксированный префикс перекрывают 20 000 output token (~$0.30).", correct: true },
    { label: "Доминирует выход, потому что выход стоит 5x за token.", misconception: "Per-token выход 5x, но output-token всего 20 000 (~$0.30) против ~935 000 input-token (~$2.80). Объём бьёт rate здесь — повторно отправляемый префикс и растущий history затмевают выход." },
    { label: "Стоимость плоская за ход, ведь system prompt постоянен.", misconception: "System prompt постоянен, но history растёт ~600 token/ход, так что per-turn вход поднимается с 4000 до ~33 400. Сумма квадратична по числу ходов, а не плоская." },
    { label: "Префикс на 4000 token оплачивается один раз, не 50.", misconception: "Stateless-модель повторно отправляет полный префикс каждый ход, если не prompt-кэшировать его. Здесь он оплачен все 50 раз — 200 000 token — главная цель для caching." },
  ]}
/>

## Сниппет 2 — per-tenant budget enforcement

```ts
async function callModel(tenantId: string, req: Request) {
  const spent = await budget.getMonthlySpend(tenantId);   // async-чтение
  if (spent >= LIMIT[tenantId]) throw new BudgetExceeded();
  const res = await llm.create(req);                        // биллит token
  await budget.add(tenantId, costOf(res));                  // запись после
  return res;
}
```

<Quiz
  id="05-cost-budgets-code-2"
  lessonSlug="05-cost-budgets"
  lang="ru"
  question="У этого per-tenant budget-гарда есть дефект, который вскроет всплеск конкурентных запросов. Какой и в чём фикс?"
  choices={[
    { label: "Check-then-act гонка: множество конкурентных запросов читают spent < LIMIT до того, как сработает любой add(), так что все проходят и пробивают cap (TOCTOU). Плюс он ограничивает лишь месячные итоги, а не per-request burn. Фикс: атомарный reserve/commit (например атомарный инкремент с откатом) плюс per-request token-cap перед вызовом.", correct: true },
    { label: "Ничего не сломано; await getMonthlySpend сериализует проверки.", misconception: "await отдаёт управление event loop — он не сериализует между конкурентными запросами. Несколько вызовов в полёте читают один и тот же pre-update spend и все проходят проверку. Это классическая check-then-act гонка." },
    { label: "Баг в том, что стоимость пишется после вызова, а не до.", misconception: "Запись после необходима, ведь истинную стоимость в token знаешь только из ответа. Дефект — неатомарный read-check-write, а не тайминг записи; зарезервируй оценку, затем сверь." },
    { label: "LIMIT должен быть глобальным, а не per-tenant.", misconception: "Per-tenant лимиты верны для честного изолированного cost-контроля. Дефект — concurrency-safety и отсутствие per-request cap, а не гранулярность лимита." },
  ]}
/>

## Сниппет 3 — решение о model routing

```ts
// Haiku $1/$5, Opus $5/$25 (за 1M вход/выход). Средняя задача: 1k вход, 500 выход.
const opusOnly = 1000 * 5/1e6 + 500 * 25/1e6;             // сразу на Opus
// Cascade: сначала Haiku; при провале платим ещё и Opus. p = escalation rate.
function cascade(p) {
  const haiku = 1000 * 1/1e6 + 500 * 5/1e6;
  const opus  = 1000 * 5/1e6 + 500 * 25/1e6;
  return haiku + p * opus;                                  // Haiku платим всегда
}
```

<Quiz
  id="05-cost-budgets-code-3"
  lessonSlug="05-cost-budgets"
  lang="ru"
  question="opusOnly ≈ $0.0175/задача. При каком escalation rate p cascade перестаёт экономить деньги?"
  choices={[
    { label: "cascade(p) = $0.0035 + p × $0.0175. Равно opusOnly при 0.0035 + 0.0175p = 0.0175, т.е. p ≈ 0.8. Выше ~80% эскалаций ты платишь больше, чем сразу на Opus — но практический алярм гораздо ниже (~30%), ведь дешёвый путь добавляет latency и риск качества, который эта математика игнорирует.", correct: true },
    { label: "p = 0.5 — половина эскалирующих запросов даёт break-even.", misconception: "При p = 0.5 cascade = 0.0035 + 0.5 × 0.0175 = $0.01225, всё ещё ниже $0.0175 Opus-only. Break-even по стоимости ~0.8; операционный порог ниже, но не 0.5." },
    { label: "Cascade всегда экономит, ведь Haiku дешевле.", misconception: "Только пока p достаточно мал. Так как ты всегда платишь за Haiku плюс долю за Opus, высокий escalation rate делает cascade дороже Opus-only — break-even здесь p ≈ 0.8." },
    { label: "p = 0.2, ведь 20% — правило большого пальца для routing.", misconception: "При p ≈ 0.2 ещё экономишь по стоимости ($0.0035 + 0.2 × 0.0175 ≈ $0.007). Цифра ~30% — алярм по качеству/latency, а не cost break-even, который для этих чисел ~0.8." },
  ]}
/>

## Сниппет 4 — расчёт cache savings

```ts
// Anthropic: cache read = 0.1x базового входа; 5-мин write = 1.25x базового входа.
// Стабильный префикс = 10 000 token, базовый вход $3/1M, переиспользуется N ходов.
function prefixCost(N: number, cached: boolean) {
  const base = 10000 * 3/1e6;                 // полноценная отправка
  if (!cached) return N * base;               // повторно каждый ход
  const write = 10000 * (3 * 1.25)/1e6;       // одна cache write
  const read  = 10000 * (3 * 0.1)/1e6;        // каждая cache read
  return write + (N - 1) * read;
}
```

<Quiz
  id="05-cost-budgets-code-4"
  lessonSlug="05-cost-budgets"
  lang="ru"
  question="Для префикса на 10 000 token, переиспользуемого 50 ходов, сколько примерно экономит caching и когда окупается?"
  choices={[
    { label: "Без cache: 50 × $0.03 = $1.50. С cache: одна write $0.0375 + 49 × $0.003 чтений ≈ $0.18 — около 88% экономии. Окупается после первого попадания, ведь одна write (1.25x) плюс одна read (0.1x) ≈ 1.35x уже дешевле двух полноценных отправок.", correct: true },
    { label: "Caching экономит ~10%, ведь чтения 0.1x.", misconception: "0.1x — это per-read rate, а не итог экономии. За 50 ходов ты заменяешь 50 полноценных отправок на одну write 1.25x плюс 49 чтений по 0.1x — примерно 88% сокращения на префиксе, не 10%." },
    { label: "Caching стоит дороже, ведь write — 1.25x базы.", misconception: "Write 1.25x платится один раз. Уже ко второму ходу итог write-плюс-read (~1.35x) бьёт две полноценные отправки (2x); за 50 ходов чтения 0.1x доминируют и экономишь ~88%." },
    { label: "Экономия применяется только к output-token.", misconception: "Prompt caching удешевляет повторно отправляемый вход — стабильный префикс. Выход генерируется заново каждый ход и не кэшируется, так что экономия целиком на повторяемом входном префиксе." },
  ]}
/>

<Recap lang="ru">
Каждое cost-решение читается в счётчиках и арифметике: повторно отправляемый префикс плюс растущий history делают долгую сессию квадратичной по входу; per-tenant budget-гард должен быть атомарным (reserve/commit) и в паре с per-request cap, иначе гонки конкурентности пробьют лимит; cascade Haiku→Opus экономит лишь пока escalation rate низок (cost break-even ~0.8, операционный алярм ~30%); а prompt-кэширование префикса 10k за 50 ходов режет ~88% и окупается на первом попадании. Сначала посчитай, затем чини самый большой член — обычно повторно отправляемый context — раньше, чем тронешь cap. Теперь, когда читаешь код учёта токенов на реальном инциденте, ты знаешь, какое число посчитать первым и за каким фиксом потянуться раньше, чем поднимать любой лимит.
</Recap>
