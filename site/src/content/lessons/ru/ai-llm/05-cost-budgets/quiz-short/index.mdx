---
concepts:
- token-pricing-asymmetry
- context-accumulation
- model-routing
- prompt-caching
- in-process-budget
- runaway-agent-loop
estMin: 14
lang: ru
level: senior
order: 8
prereqs: []
slug: quiz-short
sources:
- https://platform.claude.com/docs/en/build-with-claude/prompt-caching
- https://relayplane.com/blog/agent-runaway-costs-2026
- https://www.truefoundry.com/blog/rate-limiting-ai-agents-preventing-llm-api-exhaustion
status: ready
summary: "Промпты на свободное воспроизведение по юниту cost budgets. Ответь сначала своими словами, затем открой эталонный ответ и сравни."
title: 'LLM cost budgets: тест на свободное воспроизведение'
track: ai-llm
unit: 05-cost-budgets
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";

<Hook>
Воспроизведение бьёт перечитывание. На каждый промпт скажи или напиши полный ответ по памяти раньше, чем откроешь эталон — усилие припоминания и закрепляет cost-модель.
</Hook>

<Goal lang="ru">
Восстанови хребет юнита — асимметрию token, где накапливается context, экономику routing, prompt caching и in-process kill switch — не подглядывая в урок.
</Goal>

<RetrievalDrawer
  client:load
  id="05-cost-budgets-short-retrieval"
  lang="ru"
  questions={[
    { q: "Почему выход — дорогая половина LLM-счёта и какие конкретные рычаги по нему бьют?", a: "На текущих frontier-моделях выход стоит ~4–5x input rate — Haiku 4.5 это $1/M вход против $5/M выход, Sonnet 4.6 $3/$15, Opus 4.7 $5/$25, GPT-4o $2.50/$10. Так что многословный ответ дороже многословного вопроса, и именно генерация, а не чтение, бьёт по rate. Все рычаги бьют по выходу: ограничь max_tokens, чтобы убегающий ответ не раздулся; проси краткий или структурированный (JSON) вывод вместо прозы; не давай модели рассуждать вслух в финальном ответе — скрытый chain-of-thought всё равно биллится по output rate. Когда senior разбирает скачок расходов, объём выхода — первое, что он проверяет, ведь у него высшая цена за token." },
    { q: "Stateless-модель повторно отправляет context каждый ход. Назови три вещи, раздувающие повторно отправляемый payload, и как каждая растёт.", a: "Первое — system prompt: он одинаков и отправляется каждый ход, так что prompt на 4000 token за чат на 50 ходов — это 200 000 input-token, а не 4000, фиксированная стоимость, оплаченная N раз. Второе — conversation history: он растёт линейно за ход, так что ход 20 несёт ходы 1–19, и итог сессии квадратичен по числу ходов. Третье — RAG context: набивать 30 извлечённых chunk (~15 000 token) в каждый запрос, когда хватило бы 3, — это ~5x input-множитель, который никто не видит до инвойса. Фиксы один-к-одному: кэшируй system prompt, обрезай или суммаризируй history, ужми retrieval до chunk, реально нужных задаче." },
    { q: "Когда model routing (cheap-first cascade) реально экономит, а когда бьёт по тебе же?", a: "Routing шлёт лёгкое большинство на малую дешёвую модель (Haiku $1/$5) и эскалирует на дорогую (Opus $5/$25), только когда quality-проверка провалилась или задача заранее определена как сложная. Он выигрывает, когда дешёвая модель успешна достаточно часто, чтобы среднее легло заметно ниже плоского дорогого rate. Бьёт двумя способами: высокий escalation rate значит, что ты платишь за дешёвый вызов и затем за дорогой retry — больше, чем сразу за дорогой; а неверный route молча роняет качество. Классификатор сам добавляет latency и иногда свой model-вызов. Правило большого пальца: меряй escalation rate, и если он выше ~30% — router или дешёвая модель неправильны." },
    { q: "Объясни prompt caching: что удешевляется, насколько и как структурировать prompt для максимума выгоды.", a: "Prompt caching даёт провайдеру хранить помеченный стабильный префикс — system prompt, tool definitions, фиксированный RAG-корпус — чтобы он не биллился по полной цене каждый вызов. У Anthropic cache read стоит 0.1x базового input rate (~90% скидка), а write для заполнения cache — 1.25x (TTL 5 минут) или 2x (TTL 1 час). Арифметика: 5-минутный cache окупается после одного попадания, 1-часовой — после второго. Для максимума упорядочь prompt cache-дружественно — стабильное в начале, волатильное (последнее сообщение пользователя, свежий retrieval) в конце — чтобы кэшируемый префикс был как можно длиннее. Caching ничего не даёт частям, меняющимся каждый ход, поэтому структура важна." },
    { q: "Почему agent loop без лимита жжёт деньги суперлинейно и почему месячный provider cap его не остановит?", a: "Складываются два эффекта. Больше итераций — больше вызовов, само по себе линейно. Но каждая итерация дописывает свой tool-результат (или новый retrieval, или reasoning) в context, повторно отправляемый следующим вызовом, так что итерация 1 шлёт малый payload, а итерация 50 повторно отправляет весь накопленный транскрипт плюс system prompt. Стоимость вызова растёт с номером итерации, так что итог — сумма растущего ряда, примерно квадратичная. Месячный spend cap не остановит это, ведь он действует на уровне billing-цикла, в днях; loop может жечь доллары в секунду и потратить тысячи раньше, чем итог цикла пересечёт черту. Тормоз должен быть in-process: per-request token-budget, лимит итераций и circuit breaker на cost velocity." },
    { q: "Перечисли cost-контроли в порядке приоритета, от дешёвой первой линии до крайнего средства, и скажи, что каждый ограничивает.", a: "(1) Направить лёгкое большинство на дешёвую модель, эскалируя только при провале — режет rate на лёгком срезе. (2) Prompt-кэшировать стабильный префикс — роняет повторно отправляемый неизменный вход до 0.1x. (3) Ограничить max_tokens и обрезать history/RAG — ограничивает выход и ужимает повторно отправляемый payload. (4) Ввести per-request и per-user budget in-process, проверяемый перед каждым вызовом — ограничивает blast radius любого одного запроса или tenant. (5) Жёсткий kill switch / circuit breaker на cost velocity или счётчик loop — backstop крайнего средства, останавливающий runaway за секунды. Верх лестницы снижает расход обычного пути; низ лишь ограничивает худший случай, так что kill switch необходим, но это пол, а не стратегия. Месячные provider cap стоят вне этого как грубый billing-backstop, слишком медленный для per-second runaway." },
  ]}
/>

<Recap lang="ru">
Если смог восстановить каждый ответ по памяти — ты держишь хребет юнита: выход стоит ~5x входа, поэтому ограничь его; system prompt, history и RAG отправляются каждый ход (фиксированно, линейно и мультипликативно соответственно); routing экономит лишь при низком escalation rate; caching стабильного префикса роняет его до 0.1x и окупается на первом попадании; а поскольку runaway loop суперлинеен, а месячный cap меряется в днях, реальный тормоз — in-process budget плюс kill switch на cost velocity.
</Recap>
