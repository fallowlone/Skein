---
concepts:
- token
- prompt
- context-window
- model
- inference
- embedding
- hallucination
- temperature
deepensInto: []
estMin: 10
lang: ru
lessonType: topic
level: zero
order: 1
prereqs: []
slug: 01-overview
sources:
- https://platform.openai.com/docs/guides/text-generation
- https://docs.anthropic.com/en/docs/about-claude/models/overview
- https://platform.openai.com/tokenizer
- https://docs.anthropic.com/en/docs/build-with-claude/overview
spiral: []
status: ready
summary: Интегрировать LLM — значит вызывать вероятностную текстовую модель как сервис
  и проектировать код с учётом её ограничений — конечного контекстного окна, оплаты за
  токены и галлюцинаций. Это карта и слова, которые остальной трек считает знакомыми.
title: 'С нуля: что такое интеграция LLM на самом деле'
track: ai-llm
unit: 00-start-here
---

import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
Ты задал вопрос в ChatGPT и получил на удивление толковый ответ. Теперь менеджер хочет эту возможность внутри продукта — свой API-ключ компании, свои промпты, свои защитные барьеры. И магия чат-бота сразу растворяется в стопке инженерных решений: как передавать контекст? Что делать, если модель выдумывает факт? Во сколько это обойдётся, когда тысяча пользователей ударит одновременно? Этот урок — карта перед восхождением: восемь слов, которые старшие уроки используют, не останавливаясь на определениях, и один абзац, показывающий, как они связаны.
</Hook>

<Crux>Интегрировать LLM — значит вызывать вероятностную текстовую модель как сервис и проектировать код с учётом её ограничений.</Crux>

<Explanation>

## Одна идея, лежащая в основе интеграции LLM

Большая языковая модель — это не база данных, у которой запрашивают сохранённые факты. Это статистический движок, обученный на текстах: зная токены, которые он уже видел, он предсказывает распределение вероятностей по следующему токену, сэмплирует его и повторяет. Этот процесс — **инференс** — выполняется на мощном железе, которое обслуживает кто-то другой. Ты обращаешься к нему через HTTP. Все инженерные трудности этого трека вытекают из одной механической реальности: модель вероятностна, ограничена и тарифицируется.

«Интегрировать» LLM означает написать код, который сидит между твоим приложением и этим HTTP-эндпоинтом, — формировать ввод, обрабатывать вывод, агрессивно кэшировать, измерять стоимость и защищаться от режимов отказа, которых нет в детерминированных API.

## Восемь слов, которые остальной трек считает знакомыми

Старшие уроки дальше используют эти термины, не останавливаясь на определениях. Прежде чем рассуждать о кэшировании промптов, защите от галлюцинаций или конвейерах извлечения данных, нужно, чтобы эти восемь слов уже ощущались как привычная мебель — тогда, встретив одно из них в живом инциденте, ты сразу тянешься к нужному инструменту. Вот они, по одному предложению — что это и зачем оно.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Слово</th>
        <th class="border border-border px-3 py-2 text-left">Что это</th>
        <th class="border border-border px-3 py-2 text-left">Зачем оно</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2"><strong>Токен</strong></td>
        <td class="border border-border px-3 py-2">Атомарная единица, которую модель читает и пишет — примерно фрагмент слова (≈¾ английского слова).</td>
        <td class="border border-border px-3 py-2">Чтобы модель работала с любым текстом через фиксированный словарь; цены и лимиты всегда указываются в токенах, а не символах.</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2"><strong>Промпт</strong></td>
        <td class="border border-border px-3 py-2">Текст, который ты отправляешь модели на вход — инструкции, контекст, примеры и вопрос пользователя вместе.</td>
        <td class="border border-border px-3 py-2">Чтобы модель имела всё необходимое для полезного ответа в одном безсостоятельном запросе.</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2"><strong>Контекстное окно</strong></td>
        <td class="border border-border px-3 py-2">Максимальное количество токенов, которое модель видит за раз — промпт плюс ответ вместе.</td>
        <td class="border border-border px-3 py-2">Чтобы знать жёсткий потолок для истории и данных; превышение усекает ввод или вызывает ошибку.</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2"><strong>Модель</strong></td>
        <td class="border border-border px-3 py-2">Конкретный обученный чекпоинт, вызываемый по имени (например, <code>gpt-4o</code>, <code>claude-3-5-sonnet</code>).</td>
        <td class="border border-border px-3 py-2">Чтобы зафиксировать приложение на известных возможностях и стоимости и обновляться осознанно.</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2"><strong>Инференс</strong></td>
        <td class="border border-border px-3 py-2">Запуск модели на промпте для получения вывода — вычислительный шаг на железе провайдера.</td>
        <td class="border border-border px-3 py-2">Чтобы понимать, за что платишь: GPU-время, пропорциональное сумме входных и выходных токенов.</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2"><strong>Эмбеддинг</strong></td>
        <td class="border border-border px-3 py-2">Вектор чисел фиксированной длины, кодирующий смысл фрагмента текста.</td>
        <td class="border border-border px-3 py-2">Чтобы делать семантический поиск в векторной базе — основа RAG (Retrieval-Augmented Generation, подача извлечённых фактов в промпт).</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2"><strong>Галлюцинация</strong></td>
        <td class="border border-border px-3 py-2">Правдоподобно звучащий вывод модели, который фактически неверен или полностью выдуман.</td>
        <td class="border border-border px-3 py-2">Чтобы строить шаги верификации и заземления; доверять сырому выводу модели на критичных путях — ошибка проектирования.</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2"><strong>Температура</strong></td>
        <td class="border border-border px-3 py-2">Число (0–2), масштабирующее то, насколько узким или плоским будет распределение вероятностей токенов при сэмплировании.</td>
        <td class="border border-border px-3 py-2">Чтобы управлять балансом между детерминизмом (0 = самый вероятный токен) и творчеством (выше = больше разнообразия).</td>
      </tr>
    </tbody>
  </table>
</div>

## Как они складываются вместе

Прочитанные по порядку, слова рассказывают одну историю: ты пишешь **промпт** — блок текста из **токенов** — и отправляешь его именованной **модели** в пределах её **контекстного окна**. Модель запускает **инференс**: она сэмплирует следующий токен из распределения вероятностей, настроенного **температурой**, повторяет до стоп-токена и стримит результат обратно. Ты платишь за каждый входной и выходной **токен**. Если у модели нет заземления, она может **галлюцинировать** — именно поэтому продакшен-системы заранее извлекают факты как **эмбеддинги**. Это одно предложение — весь трек в миниатюре; каждый следующий юнит увеличивает один из этих шагов.

<Inset kind="why" lang="ru">
Почему бы просто не пользоваться чат-интерфейсом и копировать ответы? Потому что твоим пользователям нужны ответы о *твоих* данных, *твоих* бизнес-правилах, *состоянии твоего* пользователя — ничего из этого публичный чат-бот не знает. Вызов API напрямую позволяет вставить этот контекст в промпт, стримить ответы в свой UI, агрессивно кэшировать для снижения затрат и добавлять защитные барьеры, которые публичный UI не предоставляет. Инженерная работа — это разрыв между «впечатляющим демо» и «надёжным продуктом».
</Inset>

## Это не нужно зубрить

Две честные ремарки перед восхождением. Первая: никто не держит все восемь слов в голове сразу в первый день — ты встретишь каждое снова, в глубине, в своём юните, и тогда оно уляжется. Эта страница — вешалка, а не экзамен. Вторая: не каждая интеграция использует каждое слово. Простой суммаризатор может никогда не трогать эмбеддинги. Senior-трек учит полной картине, потому что этого требует продакшен под нагрузкой, — но «начни с простейшего промпта, добавляй части когда заболит» — и есть senior-инстинкт.

<Quiz
  id="00-start-here-ai-llm-quiz-1"
  lessonSlug="01-overview"
  lang="ru"
  question="Почему LLM может дать уверенный, но неверный ответ?"
  choices={[
    { label: "Потому что она предсказывает токены по вероятности, а не извлекает сохранённые факты — поэтому правдоподобно звучащие последовательности могут быть сгенерированы без фактической основы", correct: true },
    { label: "Потому что она работает слишком быстро, чтобы проверить себя", misconception: "Скорость — не причина. Модель вообще не «проверяет» себя — она сэмплирует следующий токен из распределения вероятностей. Неверные ответы — фундаментальное свойство вероятностной генерации, а не вопрос времени." },
    { label: "Потому что контекстное окно слишком мало, чтобы вместить все факты", misconception: "Маленькое контекстное окно ограничивает, сколько *ты* можешь передать модели, но галлюцинации случаются и при большом окне. Корень — в том, что модель предсказывает токены статистически, а не ищет информацию." },
    { label: "Потому что температура всегда равна нулю", misconception: "Температура 0 делает вывод более детерминированным, но не более точным. Детерминированно неверный ответ всё равно неверен. Галлюцинация — свойство статистической природы модели, а не стратегии сэмплирования." },
  ]}
/>

<DragOrder
  id="00-start-here-ai-llm-drag-1"
  lessonSlug="01-overview"
  lang="ru"
  prompt="Расставь путь от твоего кода до стримингового ответа, который получает пользователь:"
  items={[
    { id: "a", label: "Собрать промпт (токены) из системных инструкций, извлечённого контекста и сообщения пользователя" },
    { id: "b", label: "Отправить промпт на эндпоинт модели — он должен поместиться в контекстное окно" },
    { id: "c", label: "Модель запускает инференс, сэмплируя токены из распределения вероятностей, настроенного температурой" },
    { id: "d", label: "Стримить выходные токены обратно, проверить на галлюцинации и показать пользователю" },
  ]}
  correctOrder={["a", "b", "c", "d"]}
/>

</Explanation>

<FlowDiagram
  label="Промпт (токены: инструкции, извлечённые эмбеддинги и вопрос пользователя) отправляется именованной модели в пределах её контекстного окна; инференс сэмплирует следующий токен из вероятностного распределения, сформированного температурой, и повторяет до стоп-токена"
  caption="Модель никогда не ищет факт — она сэмплирует наиболее вероятный следующий токен. Любой токен, смещающий распределение к правдоподобной, но неверной последовательности, — это галлюцинация: единственная защита — заземление через эмбеддинги до вызова."
  hue="var(--d-ai)"
  perRow={4}
  nodes={[
    { id: "prompt", label: "Промпт", sub: "токены: system + эмбеддинги + вопрос пользователя", col: 0, row: 0 },
    { id: "window", label: "Контекстное окно", sub: "жёсткий лимит: токены промпта + ответа", col: 1, row: 0 },
    { id: "inference", label: "Инференс", sub: "GPU на оборудовании провайдера — оплата за токен", col: 2, row: 0 },
    { id: "output", label: "Выходные токены", sub: "сэмплируются из P(next token | context), формируются температурой", col: 3, row: 0 },
  ]}
  edges={[
    { from: "prompt", to: "window", label: "должен поместиться" },
    { from: "window", to: "inference" },
    { from: "inference", to: "output", label: "повторять до стоп-токена" },
  ]}
/>

<KeyTakeaway>Вы отправляете промпт (токены) модели в пределах контекстного окна; инференс возвращает вероятности токенов, а не факты. Платите за токен — и берегитесь галлюцинаций.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="00-start-here-ai-llm-retrieval"
  lang="ru"
  questions={[
    { q: "В одном дыхании: что такое интеграция LLM и в чём её ключевая инженерная проблема?", a: "Интеграция LLM — это вызов вероятностной текстовой модели через HTTP API и написание кода, который сидит между твоим приложением и этим эндпоинтом: формировать промпт, обрабатывать стриминговый вывод, агрессивно кэшировать, измерять стоимость за токен и защищаться от режимов отказа. Ключевая инженерная проблема в том, что модель — не детерминированная база данных: она предсказывает токены по вероятности, может галлюцинировать правдоподобные, но неверные ответы, ограничена конечным контекстным окном и тарифицирует каждый входной и выходной токен. Всё остальное — кэширование промптов, RAG, стриминг, бюджеты стоимости, эвалы — это техники поверх этой вероятностной, ограниченной и тарифицированной реальности." },
    { q: "Проследи путь от вопроса пользователя до ответа, называя каждое понятие.", a: "Ты собираешь промпт — блок текста из токенов — объединяя системные инструкции, любой извлечённый контекст (эмбеддинги из векторного хранилища) и вопрос пользователя. Отправляешь этот промпт именованной модели; он должен поместиться в контекстное окно модели, иначе придётся усекать. Модель запускает инференс на железе провайдера: зная токены, которые уже видела, сэмплирует следующий из распределения вероятностей, настроенного температурой, затем повторяет до конца. Выходные токены стримятся обратно по HTTP. Ты платишь за входные токены плюс выходные токены. Поскольку модель генерирует по вероятности, а не по поиску, она может галлюцинировать — выдавать уверенный, правдоподобно звучащий текст, который фактически неверен, — поэтому продакшен-системы добавляют шаг верификации или заземления перед тем, как показать ответ пользователю." },
  ]}
/>

<Recap lang="ru">
Интеграция LLM — это одна идея с кучей навешанной инженерии: вызывай вероятностную текстовую модель как сервис и строй каркас, который делает её надёжной, быстрой и доступной по цене в реальном продукте. Половина модели — это инференс: предсказание следующего токена из распределения вероятностей, ограниченное контекстным окном, тарифицируемое за токен. Инженерная половина — всё остальное: собрать промпт, который влезает в окно; кэшировать повторяющиеся префиксы, чтобы не платить дважды; извлекать эмбеддинги, чтобы модель имела заземление вместо угадывания; стримить токены обратно, не блокируя UI; выставлять температуру так, чтобы балансировать творчество и детерминизм; запускать эвалы, чтобы галлюцинации всплывали в CI, а не перед пользователями. Тебе не нужно держать все восемь слов сразу — у каждого впереди свой юнит. Теперь, когда коллега скажет «модель галлюцинировала» или «мы упираемся в лимит контекста», ты точно знаешь, какой рычаг сломан и где в конвейере искать — вот что дают тебе эти восемь слов. Дальше: Unit 01, кэширование промптов — как перестать платить за одни и те же токены дважды.
</Recap>
