---
concepts:
- golden-dataset
- llm-as-judge
- programmatic-scorer
- offline-online-eval
- judge-calibration
- regression-gate
estMin: 14
lang: ru
level: senior
order: 8
prereqs: []
slug: quiz-short
sources:
- https://github.com/openai/evals/blob/main/docs/build-eval.md
- https://arxiv.org/html/2406.07791v5
- https://www.braintrust.dev/articles/llm-evaluation-guide
status: ready
summary: "Промпты на свободное воспроизведение по всему юниту evals. Ответь на каждый своими словами, затем открой модельный ответ и сравни."
title: 'LLM-evals: тест на свободное воспроизведение'
track: ai-llm
unit: 07-evals
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";

<Hook>
Воспроизведение по памяти бьёт перечитывание. На каждый промпт скажи или запиши полный ответ из памяти, прежде чем открыть модельный, — именно усилие припоминания закрепляет материал.
</Hook>

<Goal lang="ru">
Восстанови спину юнита — почему недетерминизм ломает обычные тесты, как собрать golden set, когда использовать программный скоринг против judge, как откалибровать judge и в чём разделение offline/online — не подглядывая в урок.
</Goal>

<RetrievalDrawer
  client:load
  id="07-evals-short-retrieval"
  lang="ru"
  questions={[
    { q: "Почему «я попробовал, и работало» — это не тестирование LLM-фичи, и что утверждает eval вместо f(x) === y?", a: "Обычный unit-тест утверждает точное равенство: f(x) === y. LLM такого контракта не даёт — один и тот же промпт при той же temperature может вернуть разный текст на каждый вызов, и даже при temperature 0 вывод не гарантирован идентичным между версиями модели или запросами, потому что провайдеры батчат и роутят недетерминированно. Так что одну ожидаемую строку зафиксировать нельзя. Eval вместо этого утверждает, что вывод «достаточно хорош» по нужным измерениям — корректен, в формате, в рамках политики, основан на полученном контексте — и скорит распределение поведения по множеству примеров, а не один кейс. Режим отказа тихий: смена модели, правка промпта, новый system message или изменившийся индекс retrieval могут уронить качество на ~15% без исключения, без упавшего теста, без строки лога со словом «хуже». Eval-набор — единственный механизм, превращающий эту тихую регрессию в число, которое CI может провалить." },
    { q: "Как собрать golden set, который реально ловит регрессии, и какая дисциплина держит его честным?", a: "Eval честен лишь настолько, насколько честен датасет. Senior-ошибка — засеять его чистыми on-distribution примерами из разработки: они и так работают. Полезный golden set строится из продакшна: реальные запросы пользователей с намеренной переоценкой edge cases, прошлых сбоев и ценных потоков, каждый в паре с заведомо верным выводом или рубрикой, где единственно правильного ответа нет. Покрытие важнее голого количества — 80 кейсов, охватывающих реальные категории запросов, ловят больше, чем 2000 почти одинаковых лёгких, — и значимо не число, а сколько живого распределения ввода набор представляет. Дисциплина, которая окупается: каждый продакшн-сбой становится golden-кейсом в тот же день, чтобы фикс был доказуем, а тот баг уже не мог тихо регрессировать снова." },
    { q: "Когда использовать программную проверку против LLM-as-judge и почему предпочесть программную?", a: "Скорь каждый кейс самым дешёвым работающим методом. Программные проверки — regex, JSON-match, валидация схемы, исполнение кода — по сути бесплатны, выполняются за миллисекунды и детерминированы, поэтому никогда не лгут и не флакают. Используй их там, где у вывода есть структура: валидный JSON, число, метка класса, парсимый формат. Тянись к LLM-as-judge только для действительно субъективных измерений — полезность, тон, faithfulness, «ответил ли на вопрос», — потому что judge стоит лишний вызов модели на кейс, выполняется секунды и сам по себе шумная, смещённая модель. Для смешанного вывода (структурные поля плюс свободный текст) раздели: программно по полям, откалиброванный judge только по прозе. Никогда не отдавай структурное утверждение стохастическому скореру." },
    { q: "Назови задокументированные bias у LLM-as-judge и единственный шаг, делающий judge надёжным.", a: "Judge — не ground truth, а другая стохастическая модель с воспроизводимыми bias. Position bias: в попарном сравнении перестановка того, какой ответ первый, может перевернуть вердикт, причём сама judge-модель — главный драйвер; смягчают прогоном обоих порядков с усреднением. Self-preference: модель выше оценивает собственные генерации, что коррелирует с тем, насколько хорошо она распознаёт свой текст, поэтому избегай судить вывод модели judge из того же семейства. Verbosity bias: судьи систематически предпочитают длинные ответы независимо от корректности. Единственный шаг, делающий judge надёжным, — калибровка: размечаешь вручную выборку, измеряешь согласие judge с этими человеческими метками и продвигаешь его в гейт лишь когда согласие превышает порог — команды целятся в ~75–90%, около уровня, на котором согласуются люди. Критично: consistency (низкая дисперсия между прогонами) — это не accuracy; уверенно смещённый judge опаснее шумного, ведь его стабильность читается как надёжность." },
    { q: "В чём разница между offline и online оценкой и что каждая ловит, чего не может другая?", a: "Offline прогоняет eval-набор в CI против golden set на каждую правку промпта, смену модели или изменение retrieval; он выдаёт оценку, а regression gate валит билд, когда оценка падает ниже baseline или прошлого релиза. Это блокирует плохой деплой до выкатки. Но offline привязан к репозиторию, поэтому структурно не видит молчаливое обновление модели на стороне провайдера (нет diff, чтобы триггернуть перепрогон) и сдвиг в том, что реально спрашивают пользователи. Online закрывает разрыв: семплируешь срез реального продакшн-трафика (скажем, 1–5%), скоришь теми же автоматическими методами и следишь за трендом, ловя дрейф провайдера, дрейф распределения ввода и дрейф индекса retrieval. A/B-тесты затем сравнивают кандидата против живой версии на реальных пользователях и связывают качество с бизнес-метриками. Нужны оба слоя — ни один сам по себе недостаточен." },
    { q: "Назови два разных способа, которыми eval-набор бывает зелёным, пока пользователи натыкаются на сбои, и защиту для каждого.", a: "Первый: golden set больше не покрывает живое распределение ввода. Набор показывает 95%, потому что полон ожидаемых запросов, но продакшн-трафик дрейфанул к категории, которую ты не семплировал, — новый язык, новый тип вопроса, новый формат документа. Eval честно измеряет мир, который больше не совпадает с тем, где живут пользователи, поэтому остаётся зелёным, пока реальная категория падает. Защита: online-мониторинг — отслеживай embedding-распределение реального ввода и алертируй, когда продакшн-запросы приземляются далеко от всего в golden set, затем подтягивай эти кейсы внутрь. Второй: ты доверился шумному judge. Набор зелёный, потому что неоткалиброванный judge продолжает скорить многословный, уверенно-неверный ответ как «хороший» — position, self-preference и verbosity bias в игре. Ты выкатил мнение генератора чисел, а не измерение. Защита: откалибруй judge против человеческих меток до доверия и относись к резкому скачку оценок judge после правки промпта как к поводу перепроверить judge, а не праздновать." },
  ]}
/>

<Recap lang="ru">
Если ты смог восстановить каждый ответ по памяти, ты держишь спину юнита: недетерминизм значит, что eval скорит распределение «достаточно хорошего», а не утверждает одно точное значение; golden set строится из реального трафика с покрытием важнее количества и питается каждым продакшн-сбоем; программные проверки скорят структуру бесплатно, а judge берёт лишь open-ended качество; judge смещён (position, self-preference, verbosity) и должен быть откалиброван против человеческих меток — consistency это не accuracy; и ты гейтишь offline в CI, семплируя online для дрейфа, который offline не видит. Зелёный набор лжёт двумя способами — устаревший датасет и неоткалиброванный judge, — так что защищайся от обоих. Теперь, когда коллега пропускает калибровку, потому что «judge выглядел стабильным», ты знаешь, где именно рассыпается доверие — и что проверить первым.
</Recap>
