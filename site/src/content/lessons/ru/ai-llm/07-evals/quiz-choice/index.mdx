---
concepts:
- golden-dataset
- llm-as-judge
- offline-online-eval
- regression-gate
- judge-calibration
- distribution-drift
estMin: 13
lang: ru
level: senior
order: 7
prereqs: []
slug: quiz-choice
sources:
- https://github.com/openai/evals/blob/main/docs/build-eval.md
- https://arxiv.org/html/2406.07791v5
- https://www.braintrust.dev/articles/llm-evaluation-guide
status: ready
summary: "Тест с множественным выбором на синтез по всему юниту evals — golden set, программные проверки против judge, калибровка judge, offline-гейт против online-семплинга и режимы, когда зелёный набор лжёт."
title: 'LLM-evals: тест с множественным выбором'
track: ai-llm
unit: 07-evals
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Шесть вопросов, пронизывающих весь юнит. Каждый отражает решение, которое ты принимаешь, выкатывая реальную LLM-фичу, — не определение для пересказа, а компромисс, который надо взвесить, когда набор зелёный и нужно решить, веришь ли ты ему на самом деле.
</Hook>

<Goal lang="ru">
Убедись, что умеешь связать дизайн golden set, выбор скорера, калибровку judge и разделение offline/online — и распознать, когда проходящий eval всё равно тебе лжёт.
</Goal>

<Quiz
  id="07-evals-choice-1"
  lessonSlug="07-evals"
  lang="ru"
  question="Провайдер за ночь молча подменил снапшот твоей модели. Репозиторий не менялся, CI зелёный, качество упало на 15%. Какой слой структурно способен это поймать и почему?"
  choices={[
    { label: "Online-eval на семпле продакшн-трафика — он непрерывно скорит живой вывод, поэтому сдвиг поведения роняет тренд даже при нулевом diff в репозитории. Offline-гейт, привязанный к коммитам, не перезапускается, когда в репозитории ничего не менялось.", correct: true },
    { label: "Offline regression gate, потому что CI прогоняется на каждом деплое.", misconception: "CI запускается на изменения кода. Подмена снапшота на стороне провайдера меняет поведение без diff в репозитории, поэтому гейт, триггерящийся на коммиты, никогда не переоценивает новую модель. Именно для этого существует online-семплинг." },
    { label: "Ничего — молчаливое обновление провайдера принципиально не обнаружимо.", misconception: "Online-оценка существует ровно для этого: семплируй реальный трафик, скорь теми же автоматическими методами, и тренд качества падает при сдвиге поведения провайдера. Это обнаружимо, нужен лишь online-слой." },
    { label: "Unit-тесты при temperature 0, ведь это делает вывод детерминированным.", misconception: "Temperature 0 снижает, но не гарантирует детерминизм, и ничего не гарантирует между версиями модели — новый снапшот может дать другой вывод и при temperature 0. Поймать это он не может." },
  ]}
/>

<Quiz
  id="07-evals-choice-2"
  lessonSlug="07-evals"
  lang="ru"
  question="Ты собрал golden set из чистых примеров, которые использовал при разработке фичи. Набор показывает 96%, но пользователи натыкаются на сбои. В чём ошибка дизайна?"
  choices={[
    { label: "Это on-distribution happy-path кейсы, которые и так работают. Полезный golden set строится из продакшна — реальные запросы с намеренной переоценкой edge cases, прошлых сбоев и ценных потоков — и растёт за счёт добавления каждого продакшн-сбоя кейсом в тот же день.", correct: true },
    { label: "Набор слишком мал; 96% просто значит, что нужны тысячи кейсов.", misconception: "Покрытие важнее количества. 80 кейсов, охватывающих реальные категории запросов, ловят больше, чем 2000 почти одинаковых лёгких. Фикс — представить живое распределение, а не раздуть число." },
    { label: "Стоит заменить рубрику на exact string match, чтобы повысить строгость.", misconception: "Проблема не в скорере, а в датасете. Более строгий скорер на неверных лёгких кейсах всё равно измеряет мир, в котором пользователи не живут." },
    { label: "Примеры из разработки нормальны; модель деградировала сама.", misconception: "Нет признаков смены модели. Набор зелёный, потому что тестирует ожидаемые лёгкие кейсы, а не продакшн-трафик, который реально падает, — это пробел в покрытии датасета, а не регрессия модели." },
  ]}
/>

<Quiz
  id="07-evals-choice-3"
  lessonSlug="07-evals"
  lang="ru"
  question="Твоя фича возвращает JSON-объект с обязательным enum-полем `status` плюс свободный текст `rationale`. Какая стратегия скоринга верна?"
  choices={[
    { label: "Программная schema/JSON-match по структурным полям плюс откалиброванный LLM-as-judge только по свободному rationale. Используй детерминированный, бесплатный, не флакающий скорер там, где есть ground truth; judge оставь для действительно субъективной части.", correct: true },
    { label: "LLM-as-judge по всему выводу, включая структурные поля.", misconception: "Расточительно и рискованно — ты платишь за лишний вызов модели и наследуешь bias и флакость judge, чтобы оценить поля с точным ответом. Проверка схемы по enum детерминирована и бесплатна; никогда не отдавай структурное утверждение стохастическому скореру." },
    { label: "Exact string match по всему сериализованному JSON-блобу.", misconception: "Порядок ключей и пробелы в JSON варьируются, а свободный rationale законно отличается на каждом прогоне, поэтому exact-string match помечает корректный вывод как сбой. Используй структурный JSON-match по полям и judge по прозе." },
    { label: "Пропустить скоринг и глазами проверять пару выводов перед релизом.", misconception: "Это ловушка «у меня же работало»: ручные проверки не масштабируются, не воспроизводимы и не гейтят CI. Спот-чеки питают golden set, но не заменяют его." },
  ]}
/>

<Quiz
  id="07-evals-choice-4"
  lessonSlug="07-evals"
  lang="ru"
  question="LLM-as-judge выдаёт идентичный вердикт на десяти повторных прогонах одного кейса. Коллега называет его «откалиброванным». Он прав?"
  choices={[
    { label: "Нет. Это consistency (низкая дисперсия), а не accuracy (согласие с людьми). Judge может быть стабильно неправ в одну сторону; калибровка — это измерение согласия с размеченными вручную кейсами, пока оно не превысит твой порог (~75–90%).", correct: true },
    { label: "Да — воспроизводимые оценки и есть определение откалиброванного judge.", misconception: "Воспроизводимость — это лишь низкая дисперсия. Уверенно смещённый judge стабилен и неправ; его стабильность читается как надёжность, пока он пропускает регрессии, которые поймали бы люди. Калибровка — это согласие с человеческими метками, а не самосогласованность." },
    { label: "Да, если judge использует то же семейство модели, что и фича.", misconception: "Это только хуже — судейство тем же семейством провоцирует self-preference bias, когда модель выше оценивает собственные генерации. Нужно согласие с людьми, а не выравнивание с тем, что оценивается." },
    { label: "Нет, потому что judge никогда не должен быть детерминированным.", misconception: "Дело не в детерминизме; недостающий шаг — валидация против человеческих меток. Стабильный judge нормален, как только ты измерил, что его согласие с людьми превышает порог." },
  ]}
/>

<Quiz
  id="07-evals-choice-5"
  lessonSlug="07-evals"
  lang="ru"
  question="В попарном сравнении LLM-as-judge перестановка того, какой ответ идёт первым, иногда переворачивает вердикт. Что это и как смягчить?"
  choices={[
    { label: "Position bias — задокументированный сбой judge, где вердикт определяет порядок, а не качество, причём сама judge-модель — главный драйвер. Смягчают прогоном обоих порядков с усреднением (и следят также за self-preference и verbosity bias).", correct: true },
    { label: "Случайный шум API; просто поставь temperature 0 на вызов judge.", misconception: "Temperature 0 снижает шум семплинга, но не убирает position bias — это систематическое предпочтение порядка. Фикс — симметризовать сравнение (оба порядка, усреднение), а не понижать temperature." },
    { label: "Баг в твоём харнессе, путающий, какой ответ A, а какой B.", misconception: "Переворот — реальный воспроизводимый bias judge, наблюдаемый и в корректных харнессах; исследования сильных моделей-судей показывают разворот предпочтения при перестановке. Аудит меток полезен, но bias не уберёт." },
    { label: "Признак того, что ответы реально равны, поэтому порядок неважен.", misconception: "Если порядок меняет вердикт, judge не считает их равными — его сбивает позиция. Симметризуй и усредни, чтобы вердикт отражал качество, а не расположение." },
  ]}
/>

<Quiz
  id="07-evals-choice-6"
  lessonSlug="07-evals"
  lang="ru"
  question="В чём точный механизм regression gate и какая senior-дисциплина заставляет его со временем ужесточаться, а не гнить?"
  choices={[
    { label: "Набор перепрогоняет golden set на каждую правку промпта, смену модели или изменение retrieval и выдаёт оценку; гейт валит билд, когда оценка падает ниже baseline или предыдущего релиза. Он ужесточается, потому что каждый продакшн-сбой добавляется в golden set в тот же день.", correct: true },
    { label: "Он пинит каждый вывод к точной ожидаемой строке и валит на любой diff.", misconception: "Вывод LLM не фиксированная строка даже при temperature 0; гейт со string-pin валился бы на законных вариациях. Гейт скорит распределение против порога baseline, а не байтовое равенство." },
    { label: "Он запускается лишь по ручному расписанию, инженеры смотрят оценки еженедельно.", misconception: "Гейт, который не блокирует merge, — это не гейт, а дашборд. Смысл в том, чтобы автоматически свалить билд на изменении, вызвавшем регрессию, до выкатки." },
    { label: "Он замораживает golden set на старте, чтобы baseline оставался сопоставим.", misconception: "Замороженный набор устаревает по мере дрейфа трафика и перестаёт покрывать новые категории сбоев. Дисциплина обратная: возвращай каждый продакшн-сбой внутрь, чтобы гейт расширялся, отслеживая сдвиг baseline." },
  ]}
/>

<Recap lang="ru">
Сквозная линия юнита — один пайплайн: golden set из реального трафика (покрытие важнее количества), скоринг самым дешёвым честным методом — программный там, где у вывода есть структура, *откалиброванный* judge только для open-ended качества, — затем offline-гейт в CI и online-семплинг для дрейфа. Зелёный набор лжёт двумя способами: датасет, который больше не совпадает с живым распределением, и judge, который ты не валидировал против людей. Consistency — это не accuracy; position, self-preference и verbosity bias реальны; а дисциплина, держащая гейт честным, — превращать каждый продакшн-сбой в golden-кейс в тот же день. Теперь, когда перед деплоем видишь зелёный eval-набор, ты знаешь, что спросить: совпадает ли этот golden set с живым трафиком, и был ли judge откалиброван против человеческих меток — или я смотрю на генератор чисел с порогом?
</Recap>
