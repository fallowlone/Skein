---
concepts:
- agentic-loop
- react-pattern
- tool-execution-cycle
- context-accumulation
- max-step-guard
- agent-autonomy-tradeoff
deepensInto: []
estMin: 17
lang: ru
lessonType: topic
level: junior
order: 1
prereqs: []
slug: 01-overview
sources:
- https://docs.langchain.com/oss/python/langgraph/graph-api
- https://huggingface.co/learn/agents-course/en/unit1/agent-steps-and-structure
- https://online.stevens.edu/blog/hidden-economics-ai-agents-token-costs-latency/
- https://www.ibm.com/think/topics/react-agent
spiral: []
status: ready
summary: 'LLM-агент — это while-цикл: вызвать модель, выполнить инструмент, добавить
  результат, снова вызвать модель. Опасность — цикл, который не останавливается, и
  история, которая не перестаёт расти; за оба платишь по токенам.'
title: 'Цикл агента: ReAct, неуправляемые шаги и контекст, растущий каждый ход'
track: ai-llm
unit: 06-agents
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
Алерт у дежурного — это всплеск по счёту, а не падение. Один запрос агента, который должен стоить $0.08, крутится уже девять минут и сжёг около $12. Открываешь трейс: агент вызвал `search_orders`, получил пустой результат, вызвал `search_orders` снова с теми же аргументами, получил тот же пустой результат — и сделал так ещё 140 раз. Ни исключения, ни падения: каждый шаг был валидным вызовом модели с валидным вызовом инструмента. У цикла просто не было причины остановиться, и никто не сказал ему, когда это сделать.
</Hook>

<Crux>Агент — это while-цикл вокруг модели: думает, вызывает инструмент, добавляет результат, повторяет — счёт это цикл на растущую историю.</Crux>

За десять минут ты узнаешь, как устроен цикл изнутри, почему его стоимость растёт квадратично и какие защиты не дадут ему превратиться в тот трейс на $12.

<Explanation>

## Цикл — это пять строк

Сними обёртку фреймворков, и LLM-агент окажется почти неловко простым. Это паттерн ReAct — Reason, Act, Observe (рассуждай, действуй, наблюдай) — обёрнутый в `while`:

```text
messages = [system, user_task]
while True:
    response = model(messages, tools)        # THINK: модель рассуждает + решает
    if not response.tool_calls: break         # инструмент не нужен → это ответ
    result = run_tool(response.tool_calls)    # ACT: выполнить выбранный инструмент
    messages.append(response)                 # запомнить, что решила
    messages.append(result)                   # OBSERVE: вернуть результат обратно
```

Это весь движок. Модель смотрит на диалог, решает — вызвать инструмент или ответить, и если она вызвала инструмент, ты его выполняешь и отдаёшь вывод обратно как следующее наблюдение. Модель сама ничего не выполняет — это делает твой код. «Агентность» только в том, что следующее действие на каждом ходу выбирает модель, а не твой control flow. Всё трудное в агентах в проде — следствие этих двух фактов: цикл может выполниться сколько угодно раз, и `messages` становится длиннее на каждом проходе.

## Контекст накапливается, и цена не линейна

Вот что упускают джуны. Каждая итерация добавляет в `messages` рассуждение модели, её вызов инструмента и результат инструмента, а затем ты отправляешь модели *всю* историю снова на следующем ходу. Ты платишь не за один шаг — ты платишь за перечитывание всего транскрипта на каждом шаге.

Если шаг _n_ добавляет примерно _k_ токенов, то к шагу _N_ каждый вызов читает около _N·k_ токенов, а суммарная стоимость прогона — это сумма 1+2+3+…+N, квадратичная по числу шагов. Задача в 5 шагов дешёвая. Задача в 15 шагов — это не тройная стоимость, а ближе к восьмикратной, плюс latency, ведь каждый вызов ещё и ждёт промпт побольше. Reflexion-цикл на десять итераций измеряли примерно в 50× токенов одного линейного прохода. Этот квадратичный рост — самая опасная экономическая ловушка в дизайне агентов, и она невидима, пока трейс не станет длинным.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Шаг</th>
        <th class="border border-border px-3 py-2 text-left">Что добавляется</th>
        <th class="border border-border px-3 py-2 text-left">Токенов в ЭТОМ вызове</th>
        <th class="border border-border px-3 py-2 text-left">Накопленная стоимость</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">1</td>
        <td class="border border-border px-3 py-2">задача + 1-й think/act/observe</td>
        <td class="border border-border px-3 py-2">~1k</td>
        <td class="border border-border px-3 py-2">~1k</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">5</td>
        <td class="border border-border px-3 py-2">ещё 4 триплета</td>
        <td class="border border-border px-3 py-2">~5k</td>
        <td class="border border-border px-3 py-2">~15k</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">15</td>
        <td class="border border-border px-3 py-2">ещё 10 триплетов</td>
        <td class="border border-border px-3 py-2">~15k</td>
        <td class="border border-border px-3 py-2">~120k (квадратично)</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">40+</td>
        <td class="border border-border px-3 py-2">история превышает окно</td>
        <td class="border border-border px-3 py-2">переполнение / усечение</td>
        <td class="border border-border px-3 py-2">ошибки посреди задачи</td>
      </tr>
    </tbody>
  </table>
</div>

В конце этой таблицы ждёт второй провал: **переполнение контекстного окна посреди задачи.** История растёт монотонно, поэтому задача с длинным горизонтом рано или поздно выходит за лимит контекста модели. Теперь фреймворк либо падает с ошибкой, либо молча усекает самые старые сообщения — а это часто включает исходную инструкцию. Агент забывает, о чём его просили, и уверенно завершает не ту задачу. Модели к тому же непропорционально внимательны к началу и концу длинного контекста, так что важный факт, найденный на шаге 5 из 15-шаговой цепочки, может фактически исчезнуть ещё до всякого усечения.

## Завершение: циклу нужен не один выход

У наивного цикла ровно один выход — модель отказалась вызывать инструмент. В проде этого мало, потому что застрявшая, запутавшаяся или мечущаяся модель с радостью будет вызывать инструменты вечно. Сеньор даёт циклу несколько независимых выходов:

- **Естественное завершение** — модель возвращает финальный ответ без вызова инструмента. Хороший случай.
- **Ограничитель шагов** — жёсткий потолок итераций. LangGraph (граф-фреймворк для агентов поверх LangChain) поставляет его как `recursion_limit` (по умолчанию **25** супершагов в 1.0.x), и ты почти всегда ставишь его сильно ниже — 10–25 для большинства задач, ведь задача, которой нужно 1000 шагов, — это задача, потерявшая нить.
- **Бюджет по времени / токенам** — остановка после, скажем, 60 секунд или N токенов, независимо от числа шагов. Именно это реально ограничивает сумму в долларах.
- **Проверка прогресса / дедуп** — если последние несколько шагов повторяют тот же инструмент с теми же аргументами, агент мечется; разорви цикл или впрысни сообщение «ты повторяешься, остановись».

Вместе эти четыре выхода закрывают четыре разных режима отказа: застрявшую модель, неограниченный счёт, thrash и переполнение контекста. Без дедупа в частности любой пустой результат инструмента выглядит для модели как повод попробовать ещё раз — и вот ты в трейсе на $12.

История со всплеском по счёту — классический недостающий выход: у цикла была только дверь «модель не вызвала инструмент», а модель продолжала вызывать инструмент, так что до двери она не дошла.

<Inset kind="why" lang="ru">
Относись к ограничителю шагов как к *ремню безопасности, а не к рулю.* Жёсткий `max_steps` — это страховка, ограничивающая худший случай, но если агент регулярно в него упирается, ограничитель прячет настоящий баг — плохой инструмент, расплывчатый промпт, отсутствующий сигнал завершения. Основной контроль — модель достигает настоящего `stop`, потому что задача выполнена; ограничитель спасает тебя в ту ночь, когда этого не происходит.
</Inset>

## Восстановление после ошибок и ретрай, который не сдаётся

Когда инструмент падает, ты обычно возвращаешь текст ошибки обратно как наблюдение, чтобы модель адаптировалась — поправила аргументы, попробовала другой инструмент. Это одна из суперсил цикла. И она же ловушка. Допустим, `search_orders` возвращает пустой список (даже не ошибку — просто ничего полезного). Плохо запромпченный агент читает это, решает, что наверняка ошибся, и вызывает `search_orders` снова. Тот же ввод, тот же пустой результат, тот же вывод. Без дедупа или проверки прогресса это бесконечный цикл из совершенно валидных вызовов — ровно тот трейс на $12 из хука. Фикс структурный: ограничь ретраи на инструмент, детектируй идентичные подряд идущие вызовы и сделай «я не могу это найти, вот что я пробовал» допустимым финальным ответом, а не провалом, с которым модель обязана продолжать бороться.

## Скриптовый воркфлоу против открытого агента

Вопрос сеньора редко звучит как «как мне построить агента» — это «а это вообще должно быть агентом?». Скриптовый воркфлоу жёстко прописывает шаги: вызвать инструмент A, потом B, потом C, с `if`/`else` для ветвлений. Он дешевле (один-два вызова модели, а не пятнадцать), быстрее, полностью предсказуем и тривиально тестируется. Открытый агент позволяет модели выбирать шаги в рантайме; он берёт задачи, которые ты не смог бы перечислить заранее, ценой недетерминизма, большего расхода токенов и куда большей поверхности для сбоя цикла.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Измерение</th>
        <th class="border border-border px-3 py-2 text-left">Скриптовый воркфлоу</th>
        <th class="border border-border px-3 py-2 text-left">Открытый агент</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">Кто выбирает следующий шаг</td>
        <td class="border border-border px-3 py-2">Ты, при написании кода</td>
        <td class="border border-border px-3 py-2">Модель, в рантайме</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Стоимость / latency</td>
        <td class="border border-border px-3 py-2">Низкая, ограниченная</td>
        <td class="border border-border px-3 py-2">Высокая, квадратичная по шагам</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Предсказуемость</td>
        <td class="border border-border px-3 py-2">Детерминированный, тестируемый</td>
        <td class="border border-border px-3 py-2">Недетерминированный</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Берёт новые задачи</td>
        <td class="border border-border px-3 py-2">Нет — только то, что закодил</td>
        <td class="border border-border px-3 py-2">Да — адаптируется в рантайме</td>
      </tr>
    </tbody>
  </table>
</div>

Трейдофф — это автономия против контроля, стоимости и предсказуемости. Давай модели свободу выбирать свой путь только там, где эта свобода окупается; везде, где путь известен, скриптуй его. Динамические лимиты ходов, подстраивающиеся под вероятность успеха задачи, по измерениям срезают стоимость на ~24%, удерживая долю решённых, — доказательство, что нужная доля автономии обычно меньше дефолтной.

<TradeoffMatrix
  id="06-agents-tradeoff-1"
  lessonSlug="01-overview"
  lang="ru"
  prompt="Саппорт-флоу всегда делает одни и те же три шага: найти заказ, проверить право на возврат, оформить или отказать. Выбери дизайн."
  options={[
    { name: "Скриптовый воркфлоу: три явных вызова с if/else-ветвлениями, без цикла агента", summary: "Захардкодить известный путь.", correct: true, justification: "Шаги фиксированы и известны при написании кода, поэтому циклу агента нечего выяснять. Скриптовый воркфлоу — это один-два вызова модели вместо растущего цикла: дёшево, низкий latency, детерминированно и юнит-тестируемо. Можно заассертить точную последовательность в CI. Оставь цикл агента для задач, шаги которых ты честно не можешь перечислить заранее." },
    { name: "Открытый агент со всеми тремя инструментами и высоким recursion_limit, пусть сам разберётся с порядком", summary: "Пусть рулит модель.", justification: "Это полная плата за агента — квадратичный рост токенов, недетерминизм, цикл, который может метаться, — ради задачи без ветвящейся неопределённости. Порядок не меняется, поэтому давать модели эту свободу не покупает ничего и добавляет стоимость, latency и класс сбоев с неуправляемым циклом, от которых теперь надо защищаться." },
    { name: "Агент без ограничителя шагов, ведь задача короткая и очевидно завершается", summary: "Доверить остановку.", justification: "«Очевидно завершается» — ровно то допущение, что порождает трейс на $12. Пустой результат инструмента или запутавшаяся модель могут превратить трёхшаговую задачу в 140-шаговый цикл ретраев. Даже коротким задачам нужен жёсткий потолок и дедуп-проверка как ремень безопасности." },
    { name: "Скриптовый happy path, но переход на цикл агента только когда шаг вернул неожиданное", summary: "Гибрид.", justification: "Защитимо и часто идеально в реальных системах, но больше, чем нужно по условию: в этом флоу нет описанной неожиданной ветки. Добавлять агентный фолбэк здесь — спекулятивная сложность. Самый чистый ответ для полностью известного трёхшагового пути — скриптовать его; тянись к гибриду, когда появляется реальное ветвление." },
  ]}
/>

<Quiz
  id="06-agents-quiz-1"
  lessonSlug="01-overview"
  lang="ru"
  question="Почему прогон агента на 15 шагов стоит сильно больше, чем 3× прогона на 5 шагов?"
  choices={[
    { label: "Потому что вся история переотправляется на каждом шаге, поэтому суммарные токены растут квадратично по числу шагов", correct: true },
    { label: "Потому что модель замедляется тем сильнее, чем дольше думает, независимо от токенов", misconception: "Latency и правда растёт, но доминирует объём токенов: каждый вызов перечитывает весь накопленный транскрипт, поэтому стоимость масштабируется как сумма 1+2+...+N, а не линейно." },
    { label: "Потому что у каждого вызова инструмента большой фиксированный сетевой оверхед", misconception: "Сетевой оверхед примерно на вызов и линеен; суперлинейный взрыв идёт от переотправки истории, растущей каждый ход." },
    { label: "Потому что длинные прогоны используют более дорогую модель", misconception: "Модель та же; меняется лишь то, сколько токенов накопленного контекста ты платишь перечитывать на каждой итерации." },
  ]}
/>

<Quiz
  id="06-agents-quiz-2"
  lessonSlug="01-overview"
  lang="ru"
  question="Агент вызывает один и тот же инструмент с идентичными аргументами пять раз подряд, получая тот же пустой результат. Какой правильный guard?"
  choices={[
    { label: "Проверка прогресса/дедуп, которая детектирует повторяющиеся идентичные вызовы и разрывает цикл", correct: true },
    { label: "Увеличить recursion_limit, чтобы дать больше шансов на успех", misconception: "Больше итераций идентичного падающего вызова просто жгут больше денег — цикл мечется, а не прогрессирует. Поднятие потолка ухудшает счёт, а не результат." },
    { label: "Автоматически ретраить инструмент с теми же аргументами, пока не сработает", misconception: "Те же аргументы дают тот же пустой результат; слепые ретраи — это и есть баг бесконечного цикла, а не его фикс. Восстановление — это сменить подход или принять «не найдено» как финал." },
    { label: "Полностью убрать инструмент у агента", misconception: "Инструмент может быть нужен для других входов; баг — в отсутствующей логике дедупа/завершения, а не в существовании инструмента." },
  ]}
/>

<DragOrder
  id="06-agents-drag-1"
  lessonSlug="01-overview"
  lang="ru"
  prompt="Расставь одну итерацию цикла ReAct-агента:"
  items={[
    { id: "a", label: "Отправить всю историю сообщений + определения инструментов модели (THINK)" },
    { id: "b", label: "Модель возвращает либо финальный ответ, либо вызов инструмента" },
    { id: "c", label: "Если это финальный ответ без вызова инструмента → разорвать цикл" },
    { id: "d", label: "Иначе выполнить выбранный инструмент (ACT)" },
    { id: "e", label: "Добавить результат инструмента в историю (OBSERVE) и снова в цикл" },
  ]}
  correctOrder={["a", "b", "c", "d", "e"]}
/>

</Explanation>

<FlowDiagram
  label="Цикл ReAct: THINK → вызов инструмента? → ACT → OBSERVE → снова в THINK, пока финальный ответ не разорвёт цикл"
  caption="Одна итерация: отправь модели всю историю (THINK), она возвращает финальный ответ или вызов инструмента. Финальный ответ разрывает цикл; вызов инструмента выполняется (ACT), а результат добавляется (OBSERVE), затем цикл повторяется — переотправляя теперь уже бо́льшую историю, поэтому стоимость растёт квадратично."
  hue="var(--d-ai)"
  perRow={3}
  nodes={[
    { id: "think", label: "THINK", sub: "вся история модели", col: 0, row: 0 },
    { id: "decide", label: "Ответ или вызов инструмента?", sub: "решает модель", col: 1, row: 0 },
    { id: "done", label: "break — вернуть ответ", sub: "нет вызова", col: 2, row: 0 },
    { id: "act", label: "ACT", sub: "выполнить инструмент", col: 1, row: 1 },
    { id: "observe", label: "OBSERVE", sub: "добавить результат", col: 0, row: 1 },
  ]}
  edges={[
    { from: "think", to: "decide" },
    { from: "decide", to: "done", label: "нет инструмента" },
    { from: "decide", to: "act", label: "вызов" },
    { from: "act", to: "observe" },
    { from: "observe", to: "think", label: "цикл" },
  ]}
/>

<KeyTakeaway>Агент — это while-цикл вокруг модели. Стоимость квадратична по шагам, ведь история переотправляется каждый ход, поэтому дай циклу выходы — стоп, потолок шагов, бюджет, дедуп — и скриптуй известные пути.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="06-agents-retrieval"
  lang="ru"
  questions={[
    { q: "Проведи коллегу через то, почему стоимость агента растёт квадратично с числом шагов и что переполняется в самом конце.", a: "Каждая итерация цикла добавляет в список сообщений три вещи — рассуждение модели, выбранный ею вызов инструмента и результат инструмента — а затем следующая итерация переотправляет модели всю накопленную историю. Так что ты платишь не один раз за шаг; ты платишь за перечитывание всего транскрипта на каждом шаге. Если каждый шаг добавляет около k токенов, шаг N читает примерно N·k токенов, а итог прогона — это 1+2+3+...+N, что квадратично по N. Поэтому задача в 15 шагов ближе к восьмикратной от задачи в 5 шагов, а не к тройной, и поэтому reflexion-цикл на десять итераций измеряли в ~50x токенов одного прохода — и latency растёт тоже, ведь каждый вызов ждёт промпт побольше. В самом конце история рано или поздно превышает контекстное окно модели: фреймворк либо падает посреди задачи, либо молча усекает старейшие сообщения, что обычно включает исходную инструкцию, и агент забывает задачу и уверенно завершает не ту." },
    { q: "Почему жёсткий потолок шагов необходим, но недостаточен для безопасного завершения, и что ещё добавляет сеньор?", a: "У наивного цикла ровно один выход: модель отказывается вызывать инструмент и возвращает ответ. В проде это не работает, ведь застрявшая, запутавшаяся или мечущаяся модель будет вызывать инструменты вечно, поэтому нужен жёсткий потолок — recursion_limit у LangGraph, по умолчанию 25 супершагов, но обычно ставят 10-25 — чтобы ограничить худший случай. Но потолок — это ремень безопасности, а не руль: если агент регулярно в него упирается, это прячет настоящий баг, а потолок в, скажем, 25 всё равно даёт квадратичному циклу накрутить серьёзный счёт, прежде чем сработает. Поэтому сеньор добавляет независимые выходы: бюджет по времени или токенам, который реально ограничивает сумму в долларах независимо от числа шагов; проверку прогресса/дедуп, разрывающую цикл, когда последние несколько шагов повторяют тот же инструмент с теми же аргументами; и лимиты ретраев на инструмент, чтобы падающий инструмент нельзя было долбить бесконечно. Основной контроль — модель достигает настоящего done; всё остальное — на ту ночь, когда она этого не делает." },
  ]}
/>

<Recap lang="ru">
LLM-агент — это while-цикл вокруг модели: он думает (модель рассуждает и выбирает действие), действует (твой код выполняет выбранный инструмент), наблюдает (результат добавляется) и повторяет — паттерн ReAct, и «агентным» его делает только свобода модели выбирать следующее действие. Всё трудное в нём задают два факта. Цикл может выполниться сколько угодно раз, и история растёт на каждом проходе, поэтому стоимость квадратична по шагам, ведь весь транскрипт переотправляется каждый ход — прогон в 15 шагов это ~9× от прогона в 5 шагов, а задача с длинным горизонтом рано или поздно переполняет контекстное окно и молча роняет собственную инструкцию. У наивного цикла один выход (модель перестаёт вызывать инструменты), до которого застрявшая или мечущаяся модель никогда не доходит; трейс со всплеском по счёту — это агент, ретраящий тот же пустой вызов инструмента 140 раз. Поэтому дай циклу несколько независимых выходов: естественное завершение, жёсткий ограничитель шагов (recursion_limit у LangGraph), бюджет по времени или токенам, который ограничивает реальную стоимость в долларах, и дедуп/проверку прогресса, убивающую метания. Прежде всего взвешивай автономию против контроля, стоимости и предсказуемости — скриптовый воркфлоу дешевле, быстрее и тестируем везде, где путь известен, так что оставь открытого агента для задач, шаги которых ты честно не можешь перечислить заранее. Теперь, когда увидишь алерт по счёту агента, первое, что сделаешь — откроешь трейс, посчитаешь повторяющиеся вызовы и найдёшь недостающий выход, а не будешь винить модель.
</Recap>
