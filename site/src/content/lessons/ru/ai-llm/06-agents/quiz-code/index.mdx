---
concepts:
- react-loop-skeleton
- termination-exits
- context-trimming
- dedup-thrash-guard
- error-recovery-cap
estMin: 14
lang: ru
level: senior
order: 9
prereqs: []
slug: quiz-code
sources:
- https://docs.langchain.com/oss/python/langgraph/graph-api
- https://www.ibm.com/think/topics/react-agent
status: ready
summary: "Чтение реальных сниппетов agent-цикла — диспетч инструментов, лимиты шагов, обрезка контекста, error recovery — предскажите поведение и выберите фикс с наибольшим рычагом."
title: 'Агенты: чтение кода и цикла'
track: ai-llm
unit: 06-agents
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Agent-цикл — это место, где тратятся деньги и прячутся баги. Читайте каждый сниппет так, как читали бы его на код-ревью сервиса, который тарифицируется по токенам, и выбирайте фикс, который сеньор делает первым.
</Hook>

<Goal lang="ru">
Отработать цикл, который вы прогоняете на каждом агенте: прочитать поток управления, предсказать, где он крутится вечно, переполняется или молотит (thrash), и взяться за структурный ограничитель прежде, чем винить модель.
</Goal>

## Сниппет 1 — цикл с единственным выходом

```python
def run_agent(task, tools):
    messages = [SYSTEM, {"role": "user", "content": task}]
    while True:
        resp = model(messages, tools)          # THINK
        if not resp.tool_calls:
            return resp.content                 # единственный выход: модель перестаёт звать инструменты
        messages.append(resp)
        for call in resp.tool_calls:
            result = dispatch(call, tools)       # ACT
            messages.append(result)              # OBSERVE
```

<Quiz
  id="06-agents-code-1"
  lessonSlug="06-agents"
  lang="ru"
  question="Это корректный ReAct, но небезопасный для продакшена. Какой один ограничитель важнее всего добавить и почему именно его первым?"
  choices={[
    { label: "Жёсткий лимит шагов (например, `for _ in range(max_steps)`) плюс бюджет по wall-clock/токенам. Единственный текущий выход — модель решит остановиться; застрявшая или молотящая модель до него не дойдёт, поэтому прогон может расти безгранично и квадратично по стоимости.", correct: true },
    { label: "Обернуть dispatch() в try/except, чтобы ошибки инструмента не роняли цикл.", misconception: "Обработка ошибок важна, но вторична — а отдача ошибок назад без лимита сама может вызвать бесконечный retry. Первый дефект в том, что ничто вообще не ограничивает число итераций." },
    { label: "Кэшировать ответы model(), чтобы повторные состояния были дешевле.", misconception: "Кэш срезает стоимость, но не останавливает runaway-цикл; агент, молотящий новые-но-бесполезные состояния, всё равно крутится вечно. Нужен жёсткий предел на итерации и траты." },
    { label: "Перейти на стриминговый API, чтобы снизить задержку на вызов.", misconception: "Стриминг меняет, как приходит вывод, а не то, завершается ли цикл. Не хватает ограничителя termination, а не смены транспорта." },
  ]}
/>

## Сниппет 2 — лимит шагов, прячущий баг

```python
for step in range(MAX_STEPS):          # MAX_STEPS = 100
    resp = model(messages, tools)
    if not resp.tool_calls:
        return resp.content
    messages.append(resp)
    for call in resp.tool_calls:
        messages.append(dispatch(call, tools))
# вывалились из цикла: лимит достигнут
return "Извините, не удалось это выполнить."
```

<Quiz
  id="06-agents-code-2"
  lessonSlug="06-agents"
  lang="ru"
  question="Телеметрия показывает, что ~30% прогонов вываливаются и возвращают извинение. Лимит делает свою работу — какой вывод делает сеньор?"
  choices={[
    { label: "Регулярный упор в лимит — это симптом, а не решение: лимит — ремень, ловящий реальный баг (плохой инструмент, расплывчатый промпт, отсутствующий dedup). Разберитесь, почему модель не доходит до настоящего stop, и снизьте MAX_STEPS, чтобы ограничить квадратичный счёт, пока чините.", correct: true },
    { label: "Работает как задумано — поднять MAX_STEPS до 500, чтобы больше прогонов завершалось.", misconception: "Поднятие лимита позволит квадратичному циклу сжечь куда больше токенов до срабатывания, а цикл, которому нужны сотни шагов, обычно потерял нить. Лечат причину сбоя, а не дают больше верёвки." },
    { label: "Баг — само сообщение-извинение; верните частичный результат вместо него.", misconception: "Вернуть частичный результат бывает разумно, но это не объясняет, почему треть прогонов не может завершиться. Столь частое срабатывание лимита указывает на структурный дефект выше." },
    { label: "Делать нечего — 70% успеха приемлемо для агента.", misconception: "30% упоров в лимит — громкий сигнал thrashing или сломанного инструмента, а не базовая норма. Лимит это маскирует; нужна диагностика по трейсам." },
  ]}
/>

## Сниппет 3 — обрезка контекста

```python
def trim(messages, budget=8000):
    # держим самые свежие сообщения, пока укладываемся в бюджет токенов
    kept = []
    total = 0
    for m in reversed(messages):
        total += count_tokens(m)
        if total > budget:
            break
        kept.append(m)
    return list(reversed(kept))
```

<Quiz
  id="06-agents-code-3"
  lessonSlug="06-agents"
  lang="ru"
  question="Это держит цикл под окном, но на долгих задачах всплывает один режим отказа. Какой и каков фикс?"
  choices={[
    { label: "Оно роняет сначала самые старые сообщения — а это SYSTEM-промпт и исходная задача — поэтому на долгом прогоне агент теряет собственные инструкции и завершает не то. Фикс: всегда закреплять (pin) системные/задачные сообщения, а обрезать или суммаризировать середину.", correct: true },
    { label: "Оно неверно считает токены, потому что count_tokens приблизительна.", misconception: "Приблизительный счёт нормален с запасом — это не структурный баг. Реальный дефект в том, что безусловное «держим самые свежие» выселяет инструкции, от которых зависит задача." },
    { label: "Двойной разворот списка — проблема производительности на больших историях.", misconception: "Два разворота — это O(n) и пренебрежимо рядом с вызовами модели. Содержательный баг — что именно выселяется (закреплённый контекст), а не механика списка." },
    { label: "Надо вместо этого держать самые старые сообщения, ведь они важнее всего.", misconception: "Держать только старые — значит отбросить свежие результаты инструментов, нужные модели прямо сейчас. Верный ответ — закрепить инструкции и обрезать/суммаризировать середину, а не менять конец, который держим." },
  ]}
/>

## Сниппет 4 — error recovery

```python
for step in range(MAX_STEPS):
    resp = model(messages, tools)
    if not resp.tool_calls:
        return resp.content
    messages.append(resp)
    for call in resp.tool_calls:
        try:
            result = dispatch(call, tools)
        except ToolError as e:
            result = {"role": "tool", "content": f"Error: {e}"}  # отдаём ошибку назад
        messages.append(result)
```

<Quiz
  id="06-agents-code-4"
  lessonSlug="06-agents"
  lang="ru"
  question="Отдача ошибки назад позволяет модели адаптироваться — но нагрузочный тест показывает прогоны, где один и тот же инструмент падает идентично десятки раз. Какой ограничитель закрывает брешь?"
  choices={[
    { label: "Считать на инструмент число падений/идентичных вызовов и dedup-проверку: после N идентичных неудачных вызовов перестать отдавать их назад и либо сменить стратегию, либо принять «я попробовал X, не вышло» терминальным ответом. Error-feedback без лимита retry — бесконечный цикл из валидных вызовов.", correct: true },
    { label: "Ловить более широкий класс исключений, чтобы ни одна ошибка не ускользнула из цикла.", misconception: "Ловля большего числа исключений делает цикл устойчивее к падению, но склоннее к thrashing — он бесконечно отдаёт сбои назад. Не хватает лимита на идентичные/неудачные retry." },
    { label: "Повторять dispatch() с экспоненциальным backoff внутри блока except.", misconception: "Backoff помогает при транзиентных/rate-limit ошибках, но для детерминированного сбоя (плохие аргументы, не найдено) он лишь оттягивает тот же идентичный сбой. На уровне цикла всё равно нужен dedup/лимит retry." },
    { label: "Убрать try/except и дать циклу упасть на первой же ошибке.", misconception: "Падение выбрасывает способность модели восстановиться из исправимых ошибок. Цель — ограниченный recovery (отдавать назад, но с лимитом идентичных retry), а не отсутствие recovery." },
  ]}
/>

<Recap lang="ru">
Любой инцидент агента читается в цикле: единственный выход «модель остановилась» небезопасен, поэтому добавляют жёсткий лимит шагов плюс бюджет по wall-clock/токенам; лимит, срабатывающий регулярно, — это ремень, ловящий реальный баг, а не повод его ослабить; наивная обрезка контекста выселяет закреплённые системные/задачные сообщения и заставляет агента забыть задачу, поэтому инструкции закрепляют, а середину суммаризируют; а error-feedback без лимита retry/dedup на инструмент превращает recovery в бесконечный цикл из валидных вызовов. Прочитайте поток управления, найдите неограниченный путь, добавьте структурный ограничитель и перепрогоните под нагрузкой для подтверждения.
</Recap>
