---
concepts:
- agentic-loop
- quadratic-context-cost
- termination-guards
- error-recovery-loop
- scripted-vs-agent
- step-cap-seatbelt
estMin: 13
lang: ru
level: senior
order: 7
prereqs: []
slug: quiz-choice
sources:
- https://docs.langchain.com/oss/python/langgraph/graph-api
- https://www.ibm.com/think/topics/react-agent
- https://online.stevens.edu/blog/hidden-economics-ai-agents-token-costs-latency/
status: ready
summary: "Синтез по всему юниту в формате выбора — цикл ReAct, квадратичная стоимость контекста, выходы из цикла (termination), ловушки error recovery и когда писать скрипт вместо agent."
title: 'Агенты: тест с множественным выбором'
track: ai-llm
unit: 06-agents
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Шесть вопросов через весь юнит. Каждый — это решение, которое вы принимаете, проектируя или отлаживая реального агента: не определение для пересказа, а компромисс, который надо взвесить, пока цикл крутится на чьём-то счёте.
</Hook>

<Goal lang="ru">
Убедиться, что вы связываете механику цикла, экономику растущего контекста, набор выходов, которые нужны циклу, гигиену error recovery и выбор «скрипт или agent» — тот синтез, к которому вёл урок.
</Goal>

<Quiz
  id="06-agents-choice-1"
  lessonSlug="06-agents"
  lang="ru"
  question="Задача на 5 шагов агента стоит около $0.05. Тот же агент на задаче в 15 шагов стоит около $0.40 — примерно 8×, а не 3×. Почему?"
  choices={[
    { label: "Вся история сообщений переотправляется на каждой итерации, поэтому суммарные токены — это сумма 1+2+…+N, квадратично от числа шагов. Утройте шаги — и вы примерно возводите в квадрат транскрипт, который платите перечитывать.", correct: true },
    { label: "Когда прогон становится длинным, модель переключается на более дорогой тариф.", misconception: "Модель всё время одна и та же. Меняется объём токенов: каждый вызов перечитывает весь накопленный транскрипт, а эта история растёт каждый ход." },
    { label: "Каждый дополнительный вызов инструмента добавляет большие фиксированные накладные расходы на сеть и сериализацию.", misconception: "Накладные расходы на вызов примерно линейны по числу шагов и не объясняют сверхлинейный рост. Квадратичный член — это переотправка транскрипта, который удлиняется каждый ход." },
    { label: "Длинные прогоны вызывают больше повторов (retry), а повторы тарифицируются с наценкой.", misconception: "Повторы стоят столько же за токен, сколько любой вызов. Даже чистый прогон в 15 шагов без единого retry будет ~8× от прогона в 5 шагов — чисто из-за переотправки растущего контекста." },
  ]}
/>

<Quiz
  id="06-agents-choice-2"
  lessonSlug="06-agents"
  lang="ru"
  question="Запрос агента, который должен стоить $0.08, крутился девять минут и сжёг $12. В трейсе search_orders вызван 142 раза с одинаковыми аргументами, каждый раз возвращая один и тот же пустой результат. Исключений не было. В чём корень?"
  choices={[
    { label: "У цикла был лишь один выход — модель отказывается вызвать инструмент — а запутавшаяся модель бесконечно делала валидные вызовы. Не хватает dedup/progress-проверки плюс бюджета по wall-clock или токенам.", correct: true },
    { label: "В search_orders баг, который должен был выбросить исключение.", misconception: "Пустой результат — не ошибка: инструмент отработал. Сбой в цикле: ничто не заметило повтор идентичного вызова и не ограничило траты. Лечение — считать «не найдено» терминальным ответом." },
    { label: "recursion_limit был слишком мал, поэтому агент не дошёл до ответа.", misconception: "Низкий лимит остановил бы кровотечение раньше, а не вызвал его. Проблема — отсутствие dedup и бюджета; поднятие лимита только увеличит счёт." },
    { label: "Модели нужно было окно контекста побольше, чтобы вместить результаты поиска.", misconception: "Размер окна тут ни при чём — результат был пустым каждый раз. Агент молотил идентичный неудачный вызов, и больше контекста это не лечит." },
  ]}
/>

<Quiz
  id="06-agents-choice-3"
  lessonSlug="06-agents"
  lang="ru"
  question="Поток поддержки всегда делает одни и те же три шага: найти заказ, проверить право на возврат, выдать или отклонить возврат. Коллега предлагает открытого агента со всеми тремя инструментами и recursion_limit=25. Что лучше и почему?"
  choices={[
    { label: "Написать скрипт — три явных вызова с ветвлениями if/else. Путь полностью известен на этапе кода, поэтому agent-цикл ничего не даёт, а добавляет квадратичную стоимость, недетерминизм и поверхность для runaway-цикла. Скрипт дёшев, детерминирован и юнит-тестируем.", correct: true },
    { label: "Взять агента — он гибче и подстрахует, если поток когда-нибудь изменится.", misconception: "Гибкость, которая не нужна, — это спекулятивная сложность. Порядок не ветвится, поэтому свобода модели лишь добавляет стоимость, задержку и класс отказов от thrashing, от которых теперь надо защищаться." },
    { label: "Взять агента, но снизить recursion_limit до 3, чтобы ограничить стоимость.", misconception: "Лимит 3 всё равно платит agent-налог — недетерминизм и переотправку контекста — ради задачи без ветвлений. Фиксированный путь нужно просто заскриптовать." },
    { label: "Любой вариант годится; выбор чисто стилистический.", misconception: "Это инженерный компромисс с реальными последствиями: стоимость, задержка, тестируемость и поверхность отказов — всё в пользу скрипта, когда путь известен. Агента берегут для шагов, которые нельзя перечислить заранее." },
  ]}
/>

<Quiz
  id="06-agents-choice-4"
  lessonSlug="06-agents"
  lang="ru"
  question="Сеньор говорит: «относись к лимиту шагов как к ремню безопасности, а не как к рулю». Что это значит на практике?"
  choices={[
    { label: "Лимит ограничивает худший случай, но если агент регулярно в него упирается — он прячет реальный баг: плохой инструмент, расплывчатый промпт, отсутствующий сигнал termination. Главный выход — модель действительно завершает; лимит спасает в ту ночь, когда этого не происходит.", correct: true },
    { label: "Значит, лимит надо ставить как можно выше, чтобы он не мешал обычным прогонам.", misconception: "Очень высокий лимит позволит квадратичному циклу сжечь куда больше токенов до срабатывания. Лимит — это страховка, которую ставят низко; регулярные упоры в него сигналят о глубже лежащем дефекте, а не о том, что его надо ослабить." },
    { label: "Значит, на лимит надо полагаться как на основной способ остановки агента.", misconception: "Это переворачивает метафору. Цикл, который зависит от лимита, чтобы остановиться, рулит ремнём — он должен сам доходить до настоящего done, а лимит лишь ловит сбой." },
    { label: "Значит, лимиты не нужны, как только промпт стал достаточно хорош.", misconception: "Даже идеальный промпт встречает пустые результаты и запутавшиеся модели. Ремень — именно на ту ночь, когда руль откажет; его держат независимо от качества промпта." },
  ]}
/>

<Quiz
  id="06-agents-choice-5"
  lessonSlug="06-agents"
  lang="ru"
  question="Долгий агент дошёл до шага 40, и история переросла окно контекста модели. Фреймворк молча обрезает самые старые сообщения. Каково самое вероятное последствие?"
  choices={[
    { label: "Исходные системные/задачные инструкции выпадают, поэтому агент забывает, о чём его просили, и уверенно завершает не ту задачу. Модели к тому же сильнее взвешивают начало/конец длинного контекста, так что факты из середины могут эффективно исчезнуть ещё до обрезки.", correct: true },
    { label: "Агент падает с исключением переполнения контекста, которое ловится и повторяется.", misconception: "Молчаливая обрезка не бросает исключение — именно это её и опасно. Прогон продолжается без своих инструкций, выдавая правдоподобный, но неверный ответ." },
    { label: "Пропускная способность растёт, потому что каждый вызов теперь шлёт меньше токенов.", misconception: "Меньше токенов на вызов — правда, но не к месту: выпавшие токены были определением задачи и ранними находками. Дешёвые вызовы, решающие не ту задачу, — не выигрыш." },
    { label: "Ничего — недавние сообщения всегда содержат всё, что модели нужно.", misconception: "Самые старые сообщения обычно хранят исходные инструкции и первые результаты инструментов — именно тот контекст, на который опирается долгая задача. Их обрезка и есть то, как агент теряет нить." },
  ]}
/>

<Quiz
  id="06-agents-choice-6"
  lessonSlug="06-agents"
  lang="ru"
  question="Когда вызов инструмента падает, частый паттерн — отдать текст ошибки модели как следующее наблюдение. Почему это одновременно суперсила и ловушка?"
  choices={[
    { label: "Суперсила: модель читает ошибку и адаптируется — чинит аргументы, меняет инструмент. Ловушка: без лимита retry на инструмент и без dedup модель, читающая бесполезный результат как «я ошиблась», будет повторять идентичный вызов вечно — бесконечный цикл из валидных вызовов.", correct: true },
    { label: "Это чистая суперсила; отдавать ошибки не имеет минусов, если модель достаточно способна.", misconception: "Даже способные модели зацикливаются на идентичных пустых/неудачных результатах. Без лимита retry, dedup и «я попробовал X, не нашёл» как допустимого терминального ответа error-feedback становится багом thrashing." },
    { label: "Это чистая ловушка; нельзя показывать модели ошибки инструментов.", misconception: "Скрывать ошибки — значит лишить модель главного канала recovery. Лечение — структурные ограничители вокруг error-feedback (лимиты и dedup), а не подавление ошибок." },
    { label: "Ловушка в том, что текст ошибки утекает внутренними деталями к пользователю.", misconception: "Это отдельная задача редактирования/редакции. Ловушка на уровне цикла — неограниченные повторы идентичного неудачного вызова, что и порождает runaway-стоимость." },
  ]}
/>

<Recap lang="ru">
Сквозная линия — одна проектная дисциплина: цикл переотправляет всю историю каждый ход, поэтому стоимость квадратична по шагам, а долгий прогон переполняет окно и роняет собственные инструкции. У наивного цикла единственный выход, до которого модель может не дойти, поэтому добавляют независимые: естественный stop, лимит шагов (ремень), бюджет по wall-clock/токенам, реально ограничивающий доллары, и dedup/progress-проверку, убивающую thrashing. Error-feedback — суперсила recovery и ловушка runaway; ограничьте её лимитом retry на инструмент. И прежде всего спросите, известен ли путь — если да, заскриптуйте, а открытого агента берегите для шагов, которые честно нельзя перечислить заранее. Теперь, когда встретите вопрос о дизайне или ревью агента, первое, что проверяете: сколько у цикла независимых выходов — и нужен ли вообще агент для этой задачи?
</Recap>
