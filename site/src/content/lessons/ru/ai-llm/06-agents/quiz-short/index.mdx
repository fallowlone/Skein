---
concepts:
- react-loop
- quadratic-context-cost
- termination-exits
- error-recovery-trap
- context-window-overflow
- scripted-vs-agent
estMin: 14
lang: ru
level: senior
order: 8
prereqs: []
slug: quiz-short
sources:
- https://docs.langchain.com/oss/python/langgraph/graph-api
- https://www.ibm.com/think/topics/react-agent
- https://online.stevens.edu/blog/hidden-economics-ai-agents-token-costs-latency/
status: ready
summary: "Запросы на свободное воспроизведение по всему юниту. Сначала ответьте своими словами, затем раскройте модельный ответ и сверьтесь."
title: 'Агенты: тест на свободное воспроизведение'
track: ai-llm
unit: 06-agents
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";

<Hook>
Воспроизведение по памяти бьёт перечитывание. Для каждого запроса скажите или запишите полный ответ из памяти, прежде чем открыть модельный, — именно усилие припоминания закрепляет ментальную модель агента.
</Hook>

<Goal lang="ru">
Восстановить хребет юнита — цикл ReAct, почему стоимость контекста квадратична, набор выходов, нужных циклу, ловушку error recovery и выбор «скрипт или agent» — не подглядывая в урок.
</Goal>

<RetrievalDrawer
  client:load
  id="06-agents-short-retrieval"
  lang="ru"
  questions={[
    { q: "Опишите цикл агента ReAct в терминах «кто что делает» и укажите, что делает его «агентным».", a: "Цикл — это Reason–Act–Observe в обёртке while: вы шлёте историю сообщений плюс определения инструментов модели (THINK); модель либо возвращает финальный ответ, либо выбирает вызов инструмента; если выбрала — ваш код, а не модель, исполняет его (ACT) и добавляет результат в историю как следующее наблюдение (OBSERVE); затем цикл повторяется. Модель сама ничего не запускает; она лишь решает следующее действие. Это весь движок — пять строк. «Агентным» его делает исключительно то, что следующее действие выбирает модель, а не ваш поток управления. Всё трудное в агентах вытекает из двух фактов: цикл может прокрутиться любое число раз, и история сообщений растёт на каждом проходе." },
    { q: "Проведите коллегу через то, почему стоимость агента растёт квадратично с числом шагов и что переполняется на дальнем конце.", a: "Каждая итерация добавляет в список рассуждение модели, её вызов инструмента и результат инструмента, а следующая итерация переотправляет всю накопленную историю. Вы платите не один раз за шаг — вы платите перечитывать весь транскрипт на каждом шаге. Если шаг добавляет ~k токенов, шаг N читает ~N·k токенов, а итог прогона — это 1+2+…+N, квадратично по N. Поэтому задача на 15 шагов — это ~8–9× от задачи на 5 шагов, а не 3×, и задержка тоже растёт, ведь каждый вызов ждёт больший промпт; цикл reflexion на десять циклов был измерен в ~50× от одного прохода. На дальнем конце история превышает окно контекста: фреймворк либо падает с ошибкой, либо молча обрезает самые старые сообщения — обычно исходные инструкции — и агент забывает задачу и уверенно завершает не ту." },
    { q: "Почему жёсткий лимит шагов необходим, но недостаточен для безопасного termination, и что ещё добавляет сеньор?", a: "У наивного цикла ровно один выход: модель отказывается вызвать инструмент и отвечает. В продакшене это подводит, потому что застрявшая, запутавшаяся или молотящая модель будет звать инструменты вечно, поэтому нужен жёсткий потолок — recursion_limit в LangGraph (по умолчанию 25 super-steps, обычно ставят 10–25) — чтобы ограничить худший случай. Но лимит — это ремень безопасности, а не руль: если агент в него регулярно упирается, это прячет реальный баг, и даже лимит в 25 даёт квадратичному циклу сжечь серьёзный счёт. Поэтому сеньор добавляет независимые выходы: бюджет по wall-clock или токенам, который реально ограничивает доллары вне зависимости от числа шагов; progress/dedup-проверку, обрывающую цикл, когда последние шаги повторяют тот же инструмент с теми же аргументами; и лимиты retry на инструмент, чтобы падающий инструмент нельзя было молотить вечно. Главный контроль — модель доходит до настоящего done." },
    { q: "Отдача ошибок инструмента модели — суперсила recovery цикла. Как она становится багом бесконечного цикла и как её ограничить?", a: "Когда инструмент падает, вы обычно добавляете текст ошибки как следующее наблюдение, чтобы модель адаптировалась — починила аргументы или сменила инструмент. Ловушка возникает, когда результат бесполезен, но не является ошибкой: search_orders возвращает пустой список, модель заключает, что ошиблась, и снова зовёт search_orders с теми же аргументами, получая тот же пустой результат и тот же вывод. Без dedup или progress-проверки это бесконечный цикл из идеально валидных вызовов — классический трейс на $12. Лечение структурное: лимит retry на инструмент, детектор идентичных подряд вызовов с обрывом или впрыском сообщения «ты повторяешься, остановись», и принятие «не могу найти, вот что я пробовал» допустимым терминальным ответом, а не сбоем, с которым модель обязана бороться." },
    { q: "Когда писать скрипт-поток вместо открытого агента и чего стоит агент, когда путь известен?", a: "Скриптуйте всякий раз, когда шаги известны на этапе кода. Скрипт-поток жёстко прописывает последовательность — вызвать A, затем B, затем C, с if/else для ветвлений — поэтому это один-два вызова модели вместо растущего цикла: дёшево, низкая задержка, полностью детерминированно и юнит-тестируемо (точную последовательность можно проверить в CI). Открытый агент даёт модели выбирать шаги в рантайме, что нужно лишь для задач, которые нельзя перечислить заранее; он стоит недетерминизма, квадратичного роста токенов, большей задержки и крупной поверхности для thrashing или runaway. Компромисс — автономия против контроля, стоимости и предсказуемости, поэтому давайте модели свободу только там, где она окупается. Динамические лимиты ходов под вероятность успеха задачи срезали стоимость на ~24% при сохранении доли решений — свидетельство, что нужный объём автономии обычно меньше дефолтного." },
    { q: "Назовите независимые выходы, которые должен иметь продакшен-цикл агента, и от чего защищает каждый.", a: "Четыре, работающие вместе. (1) Естественное завершение — модель возвращает финальный ответ без вызова инструмента; хороший случай, ради подстраховки которого существуют остальные. (2) Лимит шагов — жёсткий потолок итераций (recursion_limit), поставленный низко, ограничивает худший случай, когда модель сама не останавливается; защищает от застрявшей или молотящей модели. (3) Бюджет wall-clock/токенов — стоп после N секунд или N токенов вне зависимости от числа шагов; именно это реально ограничивает доллары, ведь даже низкий лимит шагов дорог при квадратичном росте. (4) Progress/dedup-проверка — если последние шаги повторяют тот же инструмент с теми же аргументами, оборвать или подтолкнуть; защищает от thrash на пустом результате, который один лишь лимит шагов прокрутит до предела. Лимиты retry на инструмент дополняют (4) для падающих инструментов." },
  ]}
/>

<Recap lang="ru">
Если вы смогли восстановить каждый ответ по памяти, вы держите хребет юнита: агент — это while-цикл, где модель выбирает действия, а ваш код их исполняет; стоимость квадратична, потому что весь транскрипт переотправляется каждый ход, а долгий прогон переполняет окно и роняет собственные инструкции; единственного выхода «модель остановилась» всегда мало, поэтому добавляют лимит шагов, бюджет wall-clock/токенов и dedup-проверку; error-feedback — суперсила recovery и ловушка runaway, ограниченная лимитом retry на инструмент; а известный путь скриптуют, берегя открытого агента для шагов, которые честно нельзя перечислить заранее.
</Recap>
