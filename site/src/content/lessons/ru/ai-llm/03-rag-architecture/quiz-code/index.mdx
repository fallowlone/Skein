---
concepts:
- chunking
- cosine-similarity
- reranking
- context-assembly
- vector-search
estMin: 14
lang: ru
level: senior
order: 9
prereqs: []
slug: quiz-code
sources:
- https://www.elysiate.com/blog/rag-systems-production-guide-chunking-retrieval-2025
- https://developers.openai.com/api/docs/guides/embeddings
- https://arxiv.org/abs/2307.03172
status: ready
summary: "Чтение реального кода RAG-pipeline — chunker, поиск по cosine similarity, стадия retrieve-then-rerank и сборщик контекста — с выбором исправления наибольшего рычага."
title: 'RAG architecture: чтение кода и pipeline'
track: ai-llm
unit: 03-rag-architecture
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Баги RAG прячутся в коде, который работает без ошибок и возвращает правдоподобный текст. Читайте каждый сниппет так, как читали бы на ревью, и выбирайте дефект, который senior ловит до того, как тот выдаст уверенный неверный ответ.
</Hook>

<Goal lang="ru">
Отработайте цикл ревью RAG-pipeline: заметить изъян chunking, баг в математике similarity, отсутствующую стадию rerank и ошибку сборки контекста, которая тихо роняет качество ответов.
</Goal>

## Сниппет 1 — chunker

```python
def chunk(text, size=1000):
    # разбивка по фиксированному числу символов, без overlap
    return [text[i:i + size] for i in range(0, len(text), size)]
```

<Quiz
  id="03-rag-architecture-code-1"
  lessonSlug="03-rag-architecture"
  lang="ru"
  question="Какая проблема этого chunker для прозового корпуса даёт наибольший рычаг и каково первое исправление?"
  choices={[
    { label: "Разбиение фиксированного размера без overlap режет предложения и правила на полумысли, поэтому факт на границе попадает наполовину в каждый чанк и ни в одном не эмбедится чисто. Исправление: бить по смысловым единицам (абзацы/предложения) и добавить overlap ~10–15%.", correct: true },
    { label: "1000 символов мало; поднять size до 100000, чтобы каждый документ был одним чанком.", misconception: "Один гигантский чанк так разбавляет embedding, что recall на узких запросах рушится — один вектор теперь усредняет весь документ. Больше — не исправление; исправление — уважать смысловые границы с overlap." },
    { label: "Бить надо по байтам, а не по символам, ради Unicode-безопасности.", misconception: "Разбиение по сырым байтам может ломать многобайтовые символы и делает только хуже. Реальный дефект — рассечение смысла по произвольным смещениям без overlap, а не выбор символ-vs-байт." },
    { label: "Ничего — chunking фиксированного размера без overlap это рекомендованный дефолт.", misconception: "Нулевой overlap гарантирует разрезание граничных фактов; overlap ~10–15% — та дешёвая страховка, которой требует юнит, а разбиение по смысловым единицам сохраняет цельные идеи." },
  ]}
/>

## Сниппет 2 — оценка similarity

```python
import numpy as np

def score(query_vec, chunk_vec):
    # ранжируем кандидатов по этому значению
    return np.dot(query_vec, chunk_vec)
```

<Quiz
  id="03-rag-architecture-code-2"
  lessonSlug="03-rag-architecture"
  lang="ru"
  question="Ранжирование по сырому скалярному произведению вместо cosine similarity опасно, когда embeddings не нормализованы. Почему и каково исправление?"
  choices={[
    { label: "Сырое скалярное произведение смешивает направление (смысловую близость) и величину (длину вектора). На ненормализованных векторах более длинный вектор чанка может обойти более релевантный по смыслу короткий. Исправление: cosine similarity (скалярное произведение, делённое на нормы) или L2-нормализация векторов до индексации.", correct: true },
    { label: "Скалярное произведение не определено для высокоразмерных векторов; сначала надо снизить размерность.", misconception: "Скалярное произведение определено при любой размерности. Проблема в том, что без нормализации оно смешивает величину с релевантностью, а не в числе измерений." },
    { label: "Всё нормально — скалярное произведение и cosine similarity всегда дают одинаковое ранжирование.", misconception: "Они совпадают, только когда у всех векторов одна норма. На ненормализованных embeddings величина просачивается в score и может менять порядок; сначала нормализуйте, чтобы сделать их эквивалентными." },
    { label: "Скалярное произведение слишком медленное; перейдите на евклидово расстояние ради скорости.", misconception: "Скалярное произведение — дешёвая операция, на которой строятся ANN-индексы. Дефект — корректность на ненормализованных векторах, а исправление — нормализация/cosine, а не смена метрики ради скорости." },
  ]}
/>

## Сниппет 3 — retrieve и rerank

```python
def answer(query):
    qv = embed(query)
    candidates = vector_store.search(qv, k=3)   # top-3 by cosine
    context = "\n".join(c.text for c in candidates)
    return llm(f"Answer using:\n{context}\n\nQ: {query}")
```

<Quiz
  id="03-rag-architecture-code-3"
  lessonSlug="03-rag-architecture"
  lang="ru"
  question="Senior отмечает это как причину низкого качества ответов на сложных запросах. Какое изменение даёт наибольший рычаг?"
  choices={[
    { label: "Сделать двухстадийным: retrieve широко (k=20–50) ради recall, затем прогнать cross-encoder reranker по этим кандидатам и оставить top 3–8. k=3 без rerank означает, что один промах ANN полностью оставляет ответ вне prompt.", correct: true },
    { label: "Поднять k до 50 и отдать все 50 чанков прямо модели — без reranking.", misconception: "Это чинит recall, но рушит precision: 50 чанков раздувают токены и стоимость и хоронят решающий в малозаметной середине. Широкий retrieval требует сужающей стадии rerank." },
    { label: "Заменить LLM на более крупную модель с окном 1M токенов.", misconception: "Узкое место — то, что извлекает k=3, а не генерация. Если нужного чанка не было в top-3, ни модель крупнее, ни окно больше не порассуждают о тексте, который не извлекли." },
    { label: "Снизить k до 1, чтобы убрать из контекста весь шум.", misconception: "k=1 максимизирует хрупкость — один промах ANN даёт ноль релевантного контекста и гарантированную галлюцинацию. Паттерн — retrieve широко, затем rerank узко, а не retrieve узко." },
  ]}
/>

## Сниппет 4 — сборка контекста

```python
def assemble(reranked, query):
    # reranked отсортирован от лучшего к худшему
    blocks = [c.text for c in reranked]          # лучший чанк первым, остальные следом
    context = "\n\n".join(blocks)
    return f"{context}\n\nQuestion: {query}"
```

<Quiz
  id="03-rag-architecture-code-4"
  lessonSlug="03-rag-architecture"
  lang="ru"
  question="Для длинного собранного контекста: что говорит lost-in-the-middle об этом порядке и как собирать вместо этого?"
  choices={[
    { label: "Лучший чанк в начале, а слабейшие в хвосте — решающее доказательство может остаться в порядке, но посредственные срединные чанки попадают в зону слабейшего внимания модели. Ставьте сильнейшие доказательства по обоим краям (лучший — первым И сильный — последним), оставляйте мало чанков и кладите вопрос в самый конец.", correct: true },
    { label: "Порядок не важен после reranking — ответ определяет релевантность, а не позиция.", misconception: "Reranking задаёт, какие чанки оставить, но позиция всё ещё важна: внимание U-образное, поэтому даже релевантный текст в середине недоиспользуется. Ставьте лучшие доказательства по краям." },
    { label: "Всегда кладите единственный лучший чанк ровно в центр, чтобы модель сфокусировалась на нём.", misconception: "Центр — наименее внимаемая зона. Ровно центр — худшее место для решающего чанка; края лучше." },
    { label: "Сортируйте в обратном порядке, чтобы худший чанк был первым — модели больше всего внимают началу.", misconception: "Модели хорошо внимают началу, так что вы потратите сильнейшую позицию на худший чанк. Сильные доказательства идут в начало и конец, а не слабейшие." },
  ]}
/>

<Recap lang="ru">
Каждый баг RAG читается в коде: chunking фиксированного размера без overlap рассекает факты на границах; ранжирование по сырому скалярному произведению на ненормализованных векторах даёт величине обойти смысл, поэтому используйте cosine; одиночный retrieve с k=3 без rerank — один промах ANN до галлюцинации, поэтому retrieve широко, затем rerank узко; а порядок сборки важен, потому что внимание U-образное — оставляйте мало чанков и ставьте сильнейшие доказательства по краям. Чините стадию pipeline с наибольшим рычагом, затем переоцените на отложенном наборе для подтверждения.
</Recap>
