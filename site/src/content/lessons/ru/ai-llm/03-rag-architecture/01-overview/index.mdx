---
concepts:
- chunking
- embedding
- vector-store
- top-k-retrieval
- reranking
- context-window-budget
deepensInto: []
estMin: 17
lang: ru
lessonType: topic
level: junior
order: 1
prereqs: []
slug: 01-overview
sources:
- https://www.elysiate.com/blog/rag-systems-production-guide-chunking-retrieval-2025
- https://arxiv.org/abs/2406.16008
- https://developers.openai.com/api/docs/guides/embeddings
- https://www.nb-data.com/p/23-rag-pitfalls-and-how-to-fix-them
spiral: []
status: ready
summary: RAG — это задача ретривала в костюме генерации. Размер чанка, top-k, реранкинг
  и порядок контекста двигают recall и стоимость, а промах ретривала заставляет модель
  уверенно выдумывать ответ вместо признания «не знаю».
title: 'Архитектура RAG: пайплайн, который ломается на ретривале, а не на генерации'
track: ai-llm
unit: 03-rag-architecture
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
Выкатили саппорт-бота на корпусе документов. Пользователь спрашивает: «какой у нас отток за Q3?» Ретривер достаёт чанк с оттоком за Q2 и другой с выручкой за Q3 — близко, но не ответ. Модель не говорит «не могу найти отток за Q3». Она смешивает два чанка и выдаёт уверенное, конкретное, неверное число. Месяц никто не ловит ошибку, потому что ответ *выглядит* правильным. Шаг генерации был безупречен. Шаг ретривала промахнулся, а промах ретривала не падает громко — он бегло галлюцинирует.
</Hook>

<Crux>RAG — задача ретривала в костюме генерации: промах заставляет модель уверенно выдумать ответ вместо признания, что найти не может.</Crux>

За следующие несколько минут ты увидишь, откуда именно берётся этот промах, почему очевидные фиксы его не предотвращают и какие ручки реально что-то меняют — так что когда твой собственный RAG-бот начнёт бегло галлюцинировать, ты сразу знаешь, куда смотреть.

<Explanation>

## Пайплайн от начала до конца

RAG (retrieval-augmented generation) — это фиксированная последовательность этапов, и большая часть продакшен-боли живёт в ранних. На этапе индексации ты **чанкуешь** документы на пассажи, **эмбеддишь** каждый чанк в вектор (массив float-ов фиксированной длины) и кладёшь векторы в **векторное хранилище** с индексом приближённого ближайшего соседа (ANN). На этапе запроса ты эмбеддишь вопрос, делаешь **top-k ретривал** — достаёшь k ближайших чанков, опционально **реранкируешь** их более точной моделью, вписываешь выживших в **бюджет контекстного окна**, **собираешь промпт** и даёшь LLM генерировать.

Два факта переосмысливают всё ниже. Первый: генератор настолько хорош, насколько хорошо то, что ему отдал ретривал — мусорные чанки на входе, уверенный мусор на выходе. Второй: в продакшен-RAG доминирующий провал — это ретривал, а не генерация. Индустриальные разборы относят большинство плохих ответов на сторону ретривала: достали не те чанки, или нужный чанк вообще не был проиндексирован. Так что инженерные усилия, которые окупаются, в основном выше по течению, до модели.

## Чанкинг: лезвие «размер vs recall»

Прежде чем трогать модель или ретривер, спроси себя: какова наименьшая самодостаточная единица смысла в твоём корпусе? Это и есть вопрос чанкинга, и неверный ответ молча отравит все последующие стадии.

Чанкинг — решение, которое тихо задаёт твой потолок. Эмбеддишь чанк — и сжимаешь весь его смысл в один вектор; чанк — это атомарная единица, которую ретривал вообще способен вернуть. Ошибёшься — и никакой реранкер не спасёт.

Трейдофф острый. **Маленькие чанки** (скажем, 128–256 токенов) эмбеддятся точно — один вектор, одна плотная мысль — поэтому поиск по сходству целится хорошо. Но они дробят любое правило, пересекающее границу: условие попадает в чанк 7, исключение — в чанк 8, и top-k, взявший только один, вернёт полуправду. **Большие чанки** (800–1000+ токенов) держат контекст целым, но размывают эмбеддинг — один вектор теперь усредняет несколько идей, поэтому сигнал для *конкретного* подфакта, который тебе нужен, вымывается, и recall для узких запросов падает. Частая продакшен-точка старта — 300–800 токенов для прозы с оверлапом 10–15%, дальше тюнят на eval-сете.

**Оверлап** — дешёвая страховка от потери на границе: повтори последние ~10–15% каждого чанка в начале следующего, чтобы факт, оседлавший шов, выжил хотя бы в одном чанке. Слишком много оверлапа — и индекс заполняется почти-дубликатами: один и тот же пассаж достаётся трижды, съедая слоты top-k и бюджет контекста.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Ручка</th>
        <th class="border border-border px-3 py-2 text-left">Крутишь вверх →</th>
        <th class="border border-border px-3 py-2 text-left">Крутишь вниз →</th>
        <th class="border border-border px-3 py-2 text-left">Что взвешивает сеньор</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">Размер чанка</td>
        <td class="border border-border px-3 py-2">Контекст цел, но размытый эмбеддинг бьёт по recall узких запросов</td>
        <td class="border border-border px-3 py-2">Точный эмбеддинг, но правила рвутся по границам</td>
        <td class="border border-border px-3 py-2">Подгони чанк под единицу-носитель ответа; тюнь на eval</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Оверлап</td>
        <td class="border border-border px-3 py-2">Граничные факты выживают; индекс пухнет от дубликатов</td>
        <td class="border border-border px-3 py-2">Худой индекс; факты на шве теряются</td>
        <td class="border border-border px-3 py-2">~10–15% для прозы; меньше для FAQ</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Top-k</td>
        <td class="border border-border px-3 py-2">Выше recall; раздувание контекста, стоимость и lost-in-the-middle</td>
        <td class="border border-border px-3 py-2">Дёшево, сфокусированно; один промах = нет ответа</td>
        <td class="border border-border px-3 py-2">Достань широко (k=20–50), реранкни до 3–8</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Размерность эмбеддинга</td>
        <td class="border border-border px-3 py-2">Лучше точность; больше индекс, медленнее ANN, больше хранилища</td>
        <td class="border border-border px-3 py-2">Быстрее, дешевле поиск; часть точности теряется</td>
        <td class="border border-border px-3 py-2">1024 dims часто ~= 3072 при 1/3 хранилища</td>
      </tr>
    </tbody>
  </table>
</div>

## Эмбеддинг и векторное хранилище

Модель эмбеддингов отображает текст в вектор, чья геометрия кодирует смысл — близкие векторы означают близкий смысл. Когда ты выбираешь модель и настраиваешь размерность, ты задаёшь не только точность, но и бюджет хранилища и латентности, который будешь платить на каждом запросе. Размерность — реальный рычаг стоимости, а не деталь. OpenAI `text-embedding-3-small` по умолчанию 1536 dims; `text-embedding-3-large` — 3072. Большие векторы обычно ретривят точнее, но раздувают индекс и замедляют ANN-поиск: грубо, падение с ~1536 до ~768 dims может срезать латентность поиска примерно с 50ms к 20ms. С усечением в стиле Matryoshka можно гонять `text-embedding-3-large` на 1024 dims и приземлиться близко к качеству 3072 dims за треть хранилища (≈4KB против ≈12KB на вектор) — дефолтный размен сеньора, когда важны хранилище и p99-латентность.

Векторное хранилище не сканирует каждый вектор — на масштабе в миллионы это слишком медленно. Оно использует ANN-индекс (HNSW — частый выбор), который меняет щепотку recall на огромное ускорение, отвечая на запросы ближайшего соседа за единицы–десятки миллисекунд. «Приближённый» — слово, которое кусает: индекс может тихо *промахнуться* мимо настоящего ближайшего соседа, и чанк с ответом существует, но никогда не достаётся. Этот промах невидим — он выглядит идентично «ответа нет в корпусе».

## Top-k, реранкинг и бюджет контекста

Top-k ретривал — ручка «recall vs шум». Маленькое k (3) дёшево и сфокусированно, но беспощадно: один промах ретривала — и ответа в промпте просто нет. Большое k (50) почти гарантирует, что нужный чанк где-то *в наборе* — но теперь ты набил контекст 45 нерелевантными пассажами, которые стоят токенов, денег, латентности и, хуже того, отвлекают.

Паттерн сеньора двухстадийный: **доставай широко, реранкируй узко.** Стадия один (ANN-поиск по эмбеддингам) оптимизирует recall — широкая сеть, k=20–50 кандидатов, дёшево. Стадия два гоняет **кросс-энкодер реранкер**, который читает запрос и каждого кандидата *вместе* (а не как независимые векторы) и оценивает релевантность куда точнее, затем оставляет top 3–8. Реранкеры медленнее на единицу — поэтому их и запускают только на шорт-листе, а не на всём корпусе.

Что выжило, должно влезть в **бюджет контекстного окна**: окно модели конечно и делится с системным промптом, вопросом и генерацией. Даже с окном на 128k токенов больше доставленного текста не бесплатно и даже не нейтрально — и здесь кусает порядок.

<Inset kind="why" lang="ru">
«Lost in the middle» (потеря в середине): у LLM наблюдается U-образный перекос внимания — они лучше всего внимают началу и концу контекста и хуже всего середине, *независимо от релевантности*. Когда чанк-носитель ответа оседает в середине длинного набитого промпта, измеренная точность может упасть на 30%+ против того же чанка по краям. Практический ход: не сваливай 50 чанков в порядке ретривала. Реранкни, оставь немного и помести самое сильное доказательство в самое начало или самый конец собранного контекста.
</Inset>

## Провал, который определяет RAG в продакшене

Опасный провал — не падение, а уверенный неверный ответ. Когда ретривал промахивается (промах ANN, плохой чанкинг, факт не проиндексирован или проиндексирован, но устарел), модель всё равно получает *какой-то* контекст и обучена быть полезной, поэтому экстраполирует из того, что рядом, и выдаёт беглый, конкретный, неверный ответ вместо «не знаю». Баг с оттоком за Q3 из Hook — ровно это: близкие-но-неверные чанки, смешанные с уверенностью.

Два родственника делают хуже. **Устаревший индекс:** источник изменился, эмбеддинги — нет, и запрос, работавший в прошлый вторник, сегодня возвращает прошлоквартальную политику, без единого изменения кода и без ошибки. **Отравленный индекс:** атакующий (или небрежный ингест) подсаживает зловредный или противоречивый чанк; исследования показывают, что *один* внедрённый пассаж может перевернуть ответ, и модель не отметит противоречие — она выберет одно и подаст как решённый факт. Митигации все про признание незнания: ставь гейт на скор сходства ретривала, инструктируй модель отвечать *только* из предоставленного контекста и говорить «не знаю», когда ничто не проходит порог, и держи индекс свежим и под контролем доступа.

<TradeoffMatrix
  id="03-rag-architecture-tradeoff-1"
  lessonSlug="01-overview"
  lang="ru"
  prompt="RAG-бот по юридическим документам не должен выдумывать ссылки, а ответы могут отставать от корпуса на час. Выбери схему ретривала."
  options={[
    { name: "Доставать широко (k≈30) через ANN, реранкнуть кросс-энкодером до top 5, гейт на скор реранка и инструкция модели отвечать только из контекста или говорить «не знаю»", summary: "Две стадии + гейт на абстейн.", correct: true, justification: "Это продакшен-форма. Широкая первая стадия максимизирует recall, чтобы нужный пассаж оказался в наборе кандидатов; реранк кросс-энкодером читает запрос+чанк вместе, чтобы вытолкнуть истинную релевантность наверх и срезать шум, который раздул бы контекст и спровоцировал lost-in-the-middle. Гейт на скор плюс инструкция «отвечай только из контекста / говори не знаю» — прямая защита от уверенной галлюцинации, ровно то, чего требует «не выдумывать ссылки»." },
    { name: "Достать top-3 только по сходству эмбеддингов и подать прямо модели — быстро и дёшево", summary: "k=3, без реранка.", justification: "Слишком хрупко для требования «не выдумывать». При k=3 и без реранка один промах ANN или пункт, разрезанный по границе, означает, что ответа в промпте нет — и модель тогда уверенно выдумает ссылку вместо абстейна. Дёшево, но оптимизирует не ту ось для юридического текста." },
    { name: "Достать top-50 и засунуть всё в окно на 128k в порядке ретривала; реранк пропустить", summary: "Засунуть всё.", justification: "Recall высок, но точность и порядок — нет. Сорок с лишним нерелевантных чанков стоят токенов и латентности, а решающий пассаж часто оседает в середине, где модель внимает хуже всего (lost in the middle), так что падение точности на 30%+ правдоподобно. Большое окно не замена реранкингу." },
    { name: "Пропустить ретривал; полагаться на параметрические знания базовой модели о праве", summary: "Без RAG.", justification: "Это и есть проблема, которую RAG существует, чтобы решать. Параметрические знания заморожены на момент обучения, без ссылок и свободно галлюцинируют по конкретике — противоположность аудируемому, ограниченному по свежести юридическому боту." },
  ]}
/>

<Quiz
  id="03-rag-architecture-quiz-1"
  lessonSlug="01-overview"
  lang="ru"
  question="В продакшен-RAG пользователь получил уверенный, но неверный ответ. Где чаще всего кроется вина?"
  choices={[
    { label: "Ретривал — достали не те чанки или нужный не был проиндексирован, и модель галлюцинировала, чтобы заполнить пробел", correct: true },
    { label: "Качество генерации LLM — модель побольше или поновее это починит", misconception: "Генератор может работать только с тем, что отдал ретривал. В продакшене большинство плохих ответов восходят к ретривалу, а не к генерации; смена модели не трогает проблему пропавшего чанка." },
    { label: "Слишком низкая размерность эмбеддинга", misconception: "Размерность влияет на точность по краям, но доминирующий провал структурный — промах в чанкинге/индексации/ANN, а не пара dims точности эмбеддинга." },
    { label: "Слишком маленькое контекстное окно", misconception: "Промах означает, что нужный чанк вообще не попал в промпт; большее окно не включит текст, который ретривал никогда не достал." },
  ]}
/>

<Quiz
  id="03-rag-architecture-quiz-2"
  lessonSlug="01-overview"
  lang="ru"
  question="Ты достаёшь 40 чанков-кандидатов и кладёшь самый релевантный ровно в середину промпта. Что предсказывает «lost in the middle»?"
  choices={[
    { label: "Точность может упасть на 30%+ против размещения чанка в начале или конце, потому что внимание U-образное и слабее всего в середине", correct: true },
    { label: "Ничего — современные LLM внимают равномерно по всему контексту", misconception: "Эмпирически внимание U-образное: сильное по краям, слабое в середине, независимо от релевантности. Позиция важна даже внутри большого окна." },
    { label: "Ответ улучшается, ведь середина — самая внимаемая зона", misconception: "Наоборот — середина наименее внимаемая зона. Клади самое сильное доказательство в начало или конец." },
    { label: "Латентность падает, потому что серединные токены дешевле обрабатывать", misconception: "Позиция не меняет стоимость токена; она меняет, насколько хорошо модель использует информацию, а в середине она использует её хуже всего." },
  ]}
/>

<DragOrder
  id="03-rag-architecture-drag-1"
  lessonSlug="01-overview"
  lang="ru"
  prompt="Расставь пайплайн RAG на этапе запроса от вопроса к ответу:"
  items={[
    { id: "a", label: "Заэмбеддить вопрос пользователя в вектор запроса" },
    { id: "b", label: "ANN top-k ретривал: достать k ближайших чанков (широко, recall-first)" },
    { id: "c", label: "Реранкнуть кандидатов кросс-энкодером; оставить top немного" },
    { id: "d", label: "Вписать выживших в бюджет контекста; сильнейшее доказательство — по краям" },
    { id: "e", label: "Собрать промпт и дать LLM генерировать (по контексту или абстейн)" },
  ]}
  correctOrder={["a", "b", "c", "d", "e"]}
/>

</Explanation>

<FlowDiagram
  label="Пайплайн RAG на этапе запроса: достань широко, реранкни узко, потом генерируй — ответ решают ранние стадии"
  caption="Большинство продакшен-провалов случаются до генерации: промах ретривала заставляет модель выдумать уверенный ответ. Достань широко (k=20–50), реранкни до нескольких, впиши в бюджет контекста, потом генерируй."
  hue="var(--d-ai)"
  perRow={6}
  nodes={[
    { id: "query", label: "Вопрос", sub: "эмбеддишь в вектор запроса" },
    { id: "ann", label: "ANN top-k", sub: "широко, recall-first (k=20–50)" },
    { id: "rerank", label: "Реранк", sub: "кросс-энкодер → top 3–8" },
    { id: "context", label: "Бюджет контекста", sub: "лучшее по краям" },
    { id: "assemble", label: "Собрать промпт", sub: "запрос + чанки" },
    { id: "generate", label: "Генерация LLM", sub: "по контексту или абстейн" },
  ]}
  edges={[
    { from: "query", to: "ann" },
    { from: "ann", to: "rerank" },
    { from: "rerank", to: "context" },
    { from: "context", to: "assemble" },
    { from: "assemble", to: "generate" },
  ]}
/>

<KeyTakeaway>RAG живёт и умирает на ретривале. Чанкуй под единицу-носитель ответа, доставай широко и реранкируй узко, клади лучшее по краям и ставь гейт на скор, чтобы промах давал «не знаю», а не уверенный неверный ответ.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="03-rag-architecture-retrieval"
  lang="ru"
  questions={[
    { q: "Разбери, почему ретривал, а не генерация, — доминирующий провал в продакшен-RAG, и что промах ретривала на самом деле делает.", a: "Генератор может рассуждать только над чанками, которые отдал ретривал, поэтому весь ответ ограничен тем, что достали. У промаха ретривала несколько причин: чанкинг разрезал ответ по границе, ANN-индекс приблизил и пропустил настоящего ближайшего соседа, факт не был проиндексирован или проиндексирован, но устарел. Главное — промах невидим: модель всё равно получает какой-то близкий контекст и, обученная быть полезной, экстраполирует из него и выдаёт беглый, конкретный, уверенный ответ вместо признания, что найти не может. Это и есть баг с оттоком за Q3: система достаёт отток за Q2 и выручку за Q3, смешивает и заявляет неверное число, которое выглядит верным, поэтому переживает ревью неделями. Поэтому усилия сеньора уходят выше модели: лучший чанкинг, широкий ретривал плюс реранкинг, свежесть и гейт на абстейн по скору сходства, чтобы низкоуверенный ретривал возвращал «не знаю», а не галлюцинацию." },
    { q: "Объясни двухстадийный паттерн «достань широко, потом реранкни узко» и почему одного эмбеддинг-top-k мало.", a: "Один эмбеддинг-поиск оценивает каждый чанк против запроса независимо как dot-product би-энкодера — быстро, дружелюбно к ANN, но грубо, потому что чанк и запрос эмбеддились раздельно и никогда не сравнивались токен-к-токену. Поэтому стадия один кидает широкую сеть: достань k=20–50 кандидатов, чтобы максимизировать шанс, что нужный пассаж где-то в наборе (recall-first). Стадия два гоняет кросс-энкодер реранкер, который читает запрос и каждого кандидата вместе за один проход и оценивает истинную релевантность куда точнее, затем оставляет только top 3–8. Гонять кросс-энкодер по всему корпусу нельзя — он слишком медленный на единицу — поэтому он и работает по шорт-листу. Выигрыш двойной: точность растёт, и ты несёшь куда меньше чанков в контекстное окно, что экономит токены и стоимость и обходит lost-in-the-middle, где решающий чанк, погребённый в свалке из 50, получает слабейшее внимание модели. Широко-потом-узко берёт recall со стадии один и точность со стадии два." },
  ]}
/>

<Recap lang="ru">
RAG — это задача ретривала в костюме генерации, и почти вся продакшен-боль выше модели. Чанкинг задаёт потолок: маленькие чанки эмбеддятся точно, но рвут правила по границам, большие держат контекст, но размывают эмбеддинг и бьют по recall — поэтому подгоняй чанк под единицу-носитель ответа, бери оверлап ~10–15% и тюнь на eval. Размерность эмбеддинга — реальный рычаг стоимости (1536 против 3072; 1024 часто ≈ 3072 за треть хранилища), а ANN-индекс меняет щепотку recall на миллисекундный поиск — но «приближённый» означает, что он может тихо промахнуться мимо настоящего соседа. Достань широко (k=20–50), потом реранкни узко кросс-энкодером до 3–8, впиши в бюджет контекста и помести сильнейшее доказательство по краям, чтобы обойти lost-in-the-middle, где точность в середине может упасть на 30%+. Определяющий провал — это промах, который модель замазывает уверенным, беглым, неверным ответом; устаревший и отравленный индексы делают хуже. Фикс — признавать незнание: ставь гейт на скор ретривала, инструктируй модель отвечать только из контекста и говорить «не знаю» и держи индекс свежим и под контролем доступа. Теперь, когда встретишь RAG-бот, выдающий уверенный, но неверный ответ, твой первый рефлекс — проверять ретривал, а не модель.
</Recap>
