---
concepts:
- chunking
- embedding
- top-k-retrieval
- reranking
- context-assembly
- retrieval-failure
estMin: 13
lang: ru
level: senior
order: 7
prereqs: []
slug: quiz-choice
sources:
- https://www.elysiate.com/blog/rag-systems-production-guide-chunking-retrieval-2025
- https://arxiv.org/abs/2307.03172
- https://developers.openai.com/api/docs/guides/embeddings
status: ready
summary: "Синтез по всему юниту RAG в формате выбора — chunking, стоимость embedding, retrieval по схеме wide-then-narrow, lost-in-the-middle и режим уверенной галлюцинации."
title: 'RAG architecture: тест с множественным выбором'
track: ai-llm
unit: 03-rag-architecture
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Шесть вопросов, проходящих сквозь весь pipeline. Ни один не про заучивание определения — каждый это решение, которое вы принимаете, пока RAG-система молча выдаёт уверенные и неверные ответы в продакшене.
</Hook>

<Goal lang="ru">
Убедитесь, что вы связываете chunking, стоимость embedding, двухстадийный retrieval, сборку контекста и режим отказа, обусловленный retrieval, в единый диагноз — тот синтез, к которому вёл весь юнит.
</Goal>

<Quiz
  id="03-rag-architecture-choice-1"
  lessonSlug="03-rag-architecture"
  lang="ru"
  question="Бот поддержки уверенно сообщает конкретную, но неверную цифру оттока за Q3; ответ месяц проходил ревью незамеченным. Где чаще всего лежит причина и почему она невидима?"
  choices={[
    { label: "Retrieval — он вытянул близкие, но неверные чанки (отток Q2, выручка Q3), и поскольку промах всё равно отдаёт модели какой-то контекст, она смешивает их в гладкий, конкретный, уверенный ответ вместо отказа.", correct: true },
    { label: "Качество генерации — более новая и крупная модель поймала бы ошибку.", misconception: "Генератор может рассуждать только о чанках, которые ему отдал retrieval. Нужный чанк вообще не попал в prompt, поэтому более крупная модель просто галлюцинирует глаже на тех же отсутствующих данных." },
    { label: "Context window был слишком мал, чтобы вместить ответ.", misconception: "Промах означает, что чанк с ответом вообще не был извлечён; размер окна не может включить текст, который retrieval не вернул." },
    { label: "Баг в коде бросил исключение, которое было проглочено.", misconception: "Определяющий сбой RAG это не падение — он выдаёт правдоподобный ответ без ошибки, именно поэтому он неделями переживает ревью." },
  ]}
/>

<Quiz
  id="03-rag-architecture-choice-2"
  lessonSlug="03-rag-architecture"
  lang="ru"
  question="В документе политика указывает правило, а исключение к нему стоит абзацем ниже. Запросы про исключение возвращают полуправду. Какое изменение chunking — правильный первый шаг?"
  choices={[
    { label: "Правило и его исключение разрезаны границей чанка; добавьте overlap ~10–15% (и/или подберите размер чанка под смысловую единицу ответа), чтобы факт на стыке целиком сохранялся хотя бы в одном чанке.", correct: true },
    { label: "Уменьшить чанки до 64 токенов, чтобы каждый embedding был точнее.", misconception: "Мелкие чанки дают точный embedding, но ещё сильнее дробят сквозные правила — вы разрежете правило и исключение по ещё большему числу границ, усугубив полуправду." },
    { label: "Поднять top-k до 100, чтобы возвращалось больше чанков.", misconception: "Более широкая сеть может помочь recall, но не чинит факт, который разрезал chunking; она заваливает контекст шумом и провоцирует lost-in-the-middle, а проблема стыка остаётся." },
    { label: "Перейти на более крупную модель embedding.", misconception: "Точность embedding не при чём — текст с ответом физически разрезан надвое на этапе индексации. Никакая модель embedding не восстановит факт, который не целостен ни в одном чанке." },
  ]}
/>

<Quiz
  id="03-rag-architecture-choice-3"
  lessonSlug="03-rag-architecture"
  lang="ru"
  question="p99 поиска по индексу слишком велик на 3072 измерениях, а стоимость хранения растёт. Какой senior-компромисс до смены инфраструктуры?"
  choices={[
    { label: "Усечь embeddings (по схеме Matryoshka) — запустить text-embedding-3-large на ~1024 измерениях, получив качество, близкое к 3072, примерно за треть хранения и с более быстрым ANN-поиском.", correct: true },
    { label: "Размер embeddings фиксирован; чтобы снизить latency, надо шардировать по большему числу узлов.", misconception: "Обученные по Matryoshka embeddings явно усекаемы — можно отбросить измерения, сохранив большую часть точности, и снизить и хранение, и latency поиска без лишнего железа." },
    { label: "Перейти с HNSW на полный перебор ради точности.", misconception: "Полный перебор убирает миллисекундное ускорение ANN; на масштабе в миллионы он делает latency куда хуже. Рычаг здесь — размер вектора, а не отказ от ANN-индекса." },
    { label: "Снизить top-k, чтобы сравнивалось меньше векторов.", misconception: "ANN-поиск сравнивает запрос с индексом независимо от k; уменьшение k урезает число возвращаемых кандидатов, а не стоимость поиска на запрос, которую задаёт размерность." },
  ]}
/>

<Quiz
  id="03-rag-architecture-choice-4"
  lessonSlug="03-rag-architecture"
  lang="ru"
  question="Почему «retrieve широко (k=20–50), затем rerank узко до 3–8 через cross-encoder» лучше одиночного embedding top-3?"
  choices={[
    { label: "Широкая стадия ANN ориентирована на recall (дешёвые скалярные произведения bi-encoder), так что нужный чанк попадает в набор кандидатов; затем cross-encoder читает запрос и чанк вместе, точно оценивая релевантность, выводит истинное совпадение наверх и срезает шум.", correct: true },
    { label: "Cross-encoder достаточно быстр, чтобы прогнать его по всему корпусу, так что reranking бесплатен.", misconception: "Cross-encoder медленный на элемент именно потому, что совместно кодирует запрос и чанк — поэтому его запускают только на шорт-листе со стадии один, а не на всём корпусе." },
    { label: "Одиночный top-3 достаточен; reranking лишь добавляет latency без выигрыша в recall.", misconception: "Top-3 без rerank хрупок: один промах ANN или разрез по границе — и ответа нет в prompt. Двухстадийная схема разводит recall (стадия 1) и precision (стадия 2)." },
    { label: "Reranking увеличивает число чанков, отправляемых модели, улучшая grounding.", misconception: "Наоборот — он сужает 20–50 кандидатов до 3–8, что режет токены, стоимость и lost-in-the-middle, повышая precision." },
  ]}
/>

<Quiz
  id="03-rag-architecture-choice-5"
  lessonSlug="03-rag-architecture"
  lang="ru"
  question="У вас окно на 128k токенов, поэтому вы запихиваете все 40 извлечённых чанков в порядке retrieval и ставите решающий примерно в середину. Что предсказывает литература?"
  choices={[
    { label: "Точность может упасть на 30%+ по сравнению с размещением этого чанка в начале или в конце — внимание имеет U-образную форму, сильнее всего по краям и слабее всего в середине, независимо от релевантности.", correct: true },
    { label: "Ничего не меняется — современные long-context модели внимают окну равномерно.", misconception: "Эмпирически внимание U-образное: эффект lost-in-the-middle сохраняется даже в long-context моделях, так что позиция внутри окна важна." },
    { label: "Станет лучше, потому что середина — самая внимаемая зона.", misconception: "Наоборот: середина — наименее внимаемая зона. Делайте rerank, оставляйте мало и ставьте сильнейшие доказательства в самое начало или конец." },
    { label: "Большее окно всегда нейтрализует эффекты порядка.", misconception: "Большее окно не заменяет reranking; набивка нерелевантными чанками всё равно хоронит решающий в малозаметной середине и жжёт токены." },
  ]}
/>

<Quiz
  id="03-rag-architecture-choice-6"
  lessonSlug="03-rag-architecture"
  lang="ru"
  question="RAG-бот, работавший на прошлой неделе, теперь возвращает политику прошлого квартала без изменений кода и без ошибок. Самая вероятная причина и структурное исправление?"
  choices={[
    { label: "Устаревший индекс — источник изменился, а embeddings нет, поэтому retrieval молча отдаёт неактуальные чанки. Исправление: pipeline переиндексации/свежести плюс gate по score retrieval и инструкция «отвечать только из контекста», чтобы уверенность отражала то, что реально извлечено.", correct: true },
    { label: "Модель молча понизили; зафиксируйте версию модели.", misconception: "Замороженный генератор не перевернул бы прежде верный ответ в конкретное старое значение — этот паттерн про индекс, отдающий устаревшие embeddings, а не про смену модели." },
    { label: "Дрейф embedding — переэмбедьте запрос другой моделью.", misconception: "Embedding запроса не менялся; устарели проиндексированные embeddings документов относительно обновлённого источника. Исправление — переиндексация корпуса, а не переэмбеддинг запросов." },
    { label: "Top-k слишком мал; поднимите его, чтобы всплыла новая политика.", misconception: "Если новую политику не переиндексировали, никакое k её не извлечёт — её нет в индексе. Недостающее звено — свежесть, а не ширина." },
  ]}
/>

<Recap lang="ru">
Сквозная линия юнита — единый диагноз: продакшен-RAG падает на retrieval, а не на генерации. Chunking задаёт потолок (размер под смысловую единицу ответа, overlap для выживания стыков); размерность embedding — реальный рычаг стоимости, который можно усекать; разрыв recall-vs-precision решается схемой retrieve-wide-then-rerank-narrow; порядок контекста должен обходить lost-in-the-middle, ставя лучшие доказательства по краям; а устаревший или отравленный индекс превращает тихий промах в уверенный неверный ответ — от чего защищают gate по score, свежесть и инструкция отвечать только из контекста или говорить «я не знаю».
</Recap>
