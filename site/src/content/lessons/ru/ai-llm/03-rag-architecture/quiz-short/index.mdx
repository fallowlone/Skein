---
concepts:
- chunking
- embedding
- vector-search
- reranking
- context-assembly
- retrieval-failure
estMin: 14
lang: ru
level: senior
order: 8
prereqs: []
slug: quiz-short
sources:
- https://www.elysiate.com/blog/rag-systems-production-guide-chunking-retrieval-2025
- https://arxiv.org/abs/2307.03172
- https://www.nb-data.com/p/23-rag-pitfalls-and-how-to-fix-them
status: ready
summary: "Промпты на свободное воспроизведение по юниту RAG — компромиссы chunking, стоимость embedding, retrieval wide-then-narrow, lost-in-the-middle и abstain-gate. Сначала ответьте, потом откройте."
title: 'RAG architecture: тест со свободным воспроизведением'
track: ai-llm
unit: 03-rag-architecture
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";

<Hook>
Воспроизведение из памяти бьёт перечитывание. Для каждого промпта реконструируйте полный ответ из памяти до того, как откроете модельный ответ — именно усилие припоминания закрепляет RAG-pipeline как ментальную модель.
</Hook>

<Goal lang="ru">
Соберите по памяти хребет юнита — почему retrieval (а не генерация) доминирует среди сбоев, лезвие chunking, стоимость embedding, двухстадийная схема retrieve-then-rerank, порядок контекста и abstain-gate.
</Goal>

<RetrievalDrawer
  client:load
  id="03-rag-architecture-short-retrieval"
  lang="ru"
  questions={[
    { q: "Почему retrieval, а не генерация — доминирующий режим сбоя в продакшен-RAG, и что на самом деле делает промах retrieval?", a: "Генератор может рассуждать только о чанках, которые ему отдал retrieval, поэтому весь ответ ограничен тем, что извлечено. У промаха несколько причин: chunking разрезал ответ по границе, ANN-индекс аппроксимировал и пропустил истинного ближайшего соседа, факт не был проиндексирован, либо проиндексирован, но устарел. Главное — промах невидим: модель всё равно получает какой-то близкий контекст и, обученная быть полезной, экстраполирует и выдаёт гладкий, конкретный, уверенный ответ вместо отказа. Это баг с оттоком за Q3: система тянет отток Q2 и выручку Q3, смешивает их и выдаёт неверную цифру, которая выглядит правильной и переживает ревью. Отраслевые разборы относят большинство плохих ответов на сторону retrieval, поэтому усилия senior уходят выше модели." },
    { q: "Объясните лезвие chunking «размер vs recall» и роль overlap.", a: "Чанк эмбедится в один вектор и является атомарной единицей, которую retrieval вообще способен вернуть, поэтому chunking задаёт потолок. Мелкие чанки (128–256 токенов) эмбедятся точно — одна плотная идея, острое попадание similarity — но дробят любое правило, переходящее границу, так что top-k, схвативший лишь кусок, возвращает полуправду. Крупные чанки (800–1000+ токенов) сохраняют контекст, но разбавляют embedding: один вектор усредняет несколько идей, и сигнал для конкретного подфакта смывается, роняя recall на узких запросах. Частый старт — 300–800 токенов для прозы, подстроенный на eval-наборе. Overlap (~10–15%) это дешёвая страховка: повторяйте хвост каждого чанка в начале следующего, чтобы факт на стыке целиком выжил хотя бы в одном чанке — но слишком большой overlap заполняет индекс почти-дубликатами, съедающими слоты top-k и бюджет контекста." },
    { q: "Как размерность embedding выступает рычагом стоимости и каков компромисс Matryoshka?", a: "Embedding отображает текст в вектор, чья геометрия кодирует смысл; близкие векторы значат близкий смысл. Размерность — реальный рычаг стоимости, а не деталь: text-embedding-3-small по умолчанию 1536 измерений, text-embedding-3-large — 3072. Большие векторы обычно извлекают точнее, но раздувают индекс и замедляют ANN-поиск — переход с ~1536 на ~768 измерений может срезать latency поиска примерно с 50ms к 20ms. С усечением по схеме Matryoshka можно запустить text-embedding-3-large на 1024 измерениях и получить качество, близкое к 3072, примерно за треть хранения (≈4KB vs ≈12KB на вектор). Когда важны хранение и p99 latency, усечение до ~1024 измерений — дефолтный компромисс senior." },
    { q: "Опишите двухстадийную схему retrieve-wide-then-rerank-narrow и почему одного embedding top-k недостаточно.", a: "Одиночный embedding-поиск оценивает каждый чанк против запроса независимо как скалярное произведение bi-encoder — быстро и удобно для ANN, но грубо, потому что чанк и запрос эмбедились по отдельности и никогда не сравнивались токен-к-токену. Стадия один кидает широкую сеть: извлечь k=20–50 кандидатов, чтобы максимизировать шанс, что нужный фрагмент где-то в наборе (recall-first). Стадия два запускает cross-encoder reranker, который читает запрос и каждого кандидата вместе за один проход, точнее оценивая релевантность, и оставляет top 3–8. Нельзя гонять cross-encoder по всему корпусу — слишком медленно на элемент — именно поэтому он работает по шорт-листу. Выигрыш: precision растёт, и вы несёте куда меньше чанков в окно, экономя токены и стоимость и обходя lost-in-the-middle." },
    { q: "Что такое lost-in-the-middle и как из-за этого собирать финальный контекст?", a: "LLM показывают U-образное смещение внимания: они лучше всего внимают началу и концу контекста и хуже всего середине, независимо от релевантности. Когда чанк с ответом попадает в середину длинного набитого prompt, измеренная точность может упасть на 30%+ относительно того же чанка по краям. Поэтому большое окно не заменяет reranking. Практический ход сборки: сделать rerank, оставить мало чанков и поставить сильнейшие доказательства в самое начало или конец собранного контекста — никогда не вываливать 50 чанков в сыром порядке retrieval с решающим, похороненным в середине." },
    { q: "Что такое режим уверенной галлюцинации и как от него защититься (включая устаревший и отравленный индекс)?", a: "Опасный сбой это не падение, а уверенный неверный ответ. Когда retrieval промахивается, модель всё равно получает какой-то контекст и, обученная быть полезной, экстраполирует в гладкий, конкретный, неверный ответ вместо «я не знаю». Два родственника усугубляют: устаревший индекс (источник изменился, а embeddings нет, так что запрос возвращает политику прошлого квартала без ошибки) и отравленный индекс (единственный внедрённый или противоречивый фрагмент может перевернуть ответ, а модель не отметит конфликт — выберет один и подаст как установленный факт). Меры все про признание незнания: gate по score similarity/rerank, инструкция отвечать только из предоставленного контекста и говорить «я не знаю», когда ничто не проходит порог, плюс свежесть индекса и контроль доступа." },
  ]}
/>

<Recap lang="ru">
Если вы реконструировали каждый ответ из памяти, у вас в руках хребет юнита: retrieval — а не генерация — место, где падает продакшен-RAG; chunking задаёт потолок (размер под смысловую единицу ответа, overlap ~10–15%); размерность embedding — усекаемый рычаг стоимости; разрыв recall-vs-precision решается схемой retrieve-wide-then-rerank-narrow; порядок контекста должен обходить lost-in-the-middle, ставя лучшие доказательства по краям; а режим уверенной галлюцинации — усугубляемый устаревшим и отравленным индексом — гасится gate по score, pipeline свежести и инструкцией отвечать только из контекста или отказываться.
</Recap>
