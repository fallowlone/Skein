---
concepts:
- prefix-caching
- cache-breakpoints
- cache-ttl
- cache-write-premium
- prefix-invalidation
- prompt-ordering
deepensInto: []
estMin: 16
lang: ru
lessonType: topic
level: junior
order: 1
prereqs: []
slug: 01-overview
sources:
- https://platform.claude.com/docs/en/build-with-claude/prompt-caching
- https://platform.claude.com/docs/en/about-claude/pricing
- https://www.finout.io/blog/anthropic-api-pricing
- https://dev.to/whoffagents/claude-prompt-caching-in-2026-the-5-minute-ttl-change-thats-costing-you-money-4363
spiral: []
status: ready
summary: Кэш переиспользует точный префикс токен-в-токен по цене ~0.1x от входа. Любой
  байт, сдвигающий префикс — таймстамп, переставленный tool — инвалидирует весь кэш
  и тихо умножает счёт на 10.
title: 'Кэширование промпта: как стабильный префикс режет стоимость входа в 10 раз'
track: ai-llm
unit: 01-prompt-caching
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
Дашборд показывал cache hit rate: 94%. Счёт говорил иное — траты на вход утроились месяц к месяцу при плоском трафике. Виноваты четыре символа: кто-то добавил `Current time: {now}` в начало системного промпта «для контекста». Теперь у каждого запроса уникальный префикс с нулевого токена, поэтому каждый запрос платил премию за запись в кэш и ни разу не читал из него. Эти 94% мерили метрику, которая больше не срабатывала. Таймстамп в начале промпта — одна из самых дорогих строк кода, что можно выкатить.
</Hook>

<Crux>Кэш переиспользует точный префикс токен-в-токен по цене ~0.1x входа; один изменённый байт в префиксе инвалидирует всё после него.</Crux>

<Explanation>

Через десять минут ты будешь знать, почему одна невинная строка ломает кэш полностью — и как это выглядит на продакшене, когда счёт уже пришёл.

## Что именно кэшируется: префикс, сопоставленный токен-в-токен

Кэширование промпта не семантическое. Провайдер не понимает, что два промпта «значат одно и то же». Он хэширует префикс запроса — ведущий ряд токенов — и ищет сохранённую запись, совпадающую с ним точно, токен в токен, с нулевой позиции. Совпадение должно быть идентичным вплоть до помеченной границы — cache breakpoint (контрольная точка кэша, явно помеченный рубеж, до которого префикс сохраняется). В тот миг, когда потоки расходятся — другое слово, лишний пробел, переставленный блок — совпадение там и заканчивается, а всё, что после, обрабатывается (и тарифицируется) как свежий вход.

Вот почему *порядок* промпта — это вся игра. Кэшируемое — всегда начало. Иерархия запроса у Anthropic фиксирована: сначала `tools`, затем `system`, потом `messages`. Так что правило проектирования пишется само: всё стабильное — в начало (определения tools, системный промпт, большой документ, few-shot примеры), всё волатильное — в конец (реальный вопрос пользователя, поданные данные запроса, всё с таймстампом). Breakpoint ставишь на последний блок, идентичный между запросами. Поставишь на один блок позже — на то, что меняется — и lookback не найдёт прежней записи в этой позиции, так что каждый раз чистый промах при полной оплате записи.

## Экономика: пошлина 1.25x за скидку 10x

Кэширование — это ставка, и шансы напечатаны на коробке. Запись в кэш стоит *дороже* обычного запроса: тариф на 5 минут тарифицирует кэшированные токены по 1.25x базовой ставки входа, а тариф на 1 час — по 2x. Чтение из кэша — где ты выигрываешь: попадание тарифицирует те токены по 0.1x — скидка 90%. Конкретно: на Opus 4.7 стандартная ставка входа $5.00 / MTok, а попадание роняет её до $0.50 / MTok; на Sonnet 4.6 — с $3.00 до $0.30.

Точка окупаемости вытекает из этого отношения. Платишь 1.25x один раз за запись, затем 0.1x за каждое последующее чтение. Первый кэшированный запрос — небольшой убыток; ко второму запросу ты в плюсе, а с третьего и дальше — почти бесплатно. Сеньорская рамка не «кэшировать ли», а «будет ли этот префикс перечитан внутри окна TTL достаточно часто, чтобы амортизировать премию за запись». Префикс, который записали и ни разу не перечитали до истечения, строго хуже, чем не кэшировать вовсе — ты заплатил пошлину 1.25x впустую.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Фактор</th>
        <th class="border border-border px-3 py-2 text-left">Множитель к базовому входу</th>
        <th class="border border-border px-3 py-2 text-left">Что это значит</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">Запись в кэш, TTL 5 мин</td>
        <td class="border border-border px-3 py-2">1.25x</td>
        <td class="border border-border px-3 py-2">Разовая премия за хранение префикса; тариф по умолчанию</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Запись в кэш, TTL 1 час</td>
        <td class="border border-border px-3 py-2">2x</td>
        <td class="border border-border px-3 py-2">Дороже на запись, но живёт в 12 раз дольше</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Чтение из кэша (попадание)</td>
        <td class="border border-border px-3 py-2">0.1x</td>
        <td class="border border-border px-3 py-2">Скидка 90%; вся причина, ради которой кэш существует</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Некэшированный вход</td>
        <td class="border border-border px-3 py-2">1.0x</td>
        <td class="border border-border px-3 py-2">Всё после точки расхождения платит полную ставку</td>
      </tr>
    </tbody>
  </table>
</div>

## TTL: 5 минут по умолчанию, 1 час, если заплатишь вперёд

Запись кэша эфемерна. Время жизни по умолчанию — 5 минут, и важно: часы сбрасываются на каждом чтении — активный префикс остаётся тёплым, пока в него попадают. Тариф на 1 час нужен для медленного или всплескового трафика, где запросы приходят с разрывом больше 5 минут, но всё ещё внутри часа. Сделка прямая: 1 час стоит 2x на запись вместо 1.25x, так что окупается, только если более длинное окно превращает промахи в попадания, которые иначе ты бы потерял. На начало 2026 года дефолт стоит на 5 минутах; команды, построившие модель затрат на более длинный дефолт, получили тихий шок по счёту, когда запись, которую ждали живой между запросами, уже истекла.

Есть и порог: префикс ниже минимальной кэшируемой длины модели тихо не кэшируется — без ошибки, просто оплата по полной ставке. Порог зависит от модели.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Модель</th>
        <th class="border border-border px-3 py-2 text-left">Минимум кэшируемых токенов</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">Opus 4.7 / 4.6 / 4.5</td>
        <td class="border border-border px-3 py-2">4096</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Sonnet 4.6 / 4.5, Opus 4.1</td>
        <td class="border border-border px-3 py-2">1024</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Haiku 4.5</td>
        <td class="border border-border px-3 py-2">4096</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Haiku 3.5</td>
        <td class="border border-border px-3 py-2">2048</td>
      </tr>
    </tbody>
  </table>
</div>

<Inset kind="why" lang="ru">
У тебя максимум 4 явных `cache_control` breakpoint на запрос, и каждый ищет назад в ограниченном окне прежнюю запись. Поэтому breakpoint'ы стакают на длинном слоёном промпте — один после tools, один после статического системного блока, один после большого документа — чтобы изменение внизу инвалидировало только хвост, а не дорогую верхушку. Breakpoint'ы — это вложенные префиксы, а не независимые кэши: попадание на позднем breakpoint означает, что и ранние совпали.
</Inset>

## Производственный режим отказа: тихое отравление префикса

Спроси себя: как ты узнаешь, что кэш перестал работать завтра? Скорее всего — никак, пока не придёт счёт. Опасно то, что слом кэша никогда не бросает исключения. Ничто в ответе не говорит «ты промахнулся». Вывод побайтно идентичен, попал ты или заплатил по полной; единственный сигнал живёт в блоке `usage`, в `cache_read_input_tokens` против `cache_creation_input_tokens`. Так что одна небрежная правка может увести нагрузку с 90% чтений к 0% чтений, и единственный симптом — счёт через три недели.

Классические отравители — всё, что кажется безобидным: таймстамп или request id, впихнутый в начало системного промпта; tools, сериализованные в недетерминированном порядке (`Map` или `dict`, не сохраняющий порядок вставки между прогонами); апгрейд библиотеки, заново форматирующий твою JSON-схему tool с другими пробелами; добавление сообщения пользователя *перед* статическим контекстом, а не после. Каждое сдвигает префикс на нулевом токене или рядом, так что всё кэшированное тело — возможно, десятки тысяч токенов системного промпта и документов — пере-тарифицируется по ставке записи 1.25x на каждом вызове. Фикс — дисциплина: заморозь префикс, проверь его побайтно тестом и пропускай любое изменение system/tools через ревью, спрашивающее «не сдвигает ли это cache breakpoint».

<TradeoffMatrix
  id="01-prompt-caching-tradeoff-1"
  lessonSlug="01-overview"
  lang="ru"
  prompt="RAG-сервис шлёт системный промпт на 30k токенов + найденные документы, затем вопрос пользователя. Трафик всплесковый: запросы кучкуются, потом тишина ~15 минут. Выбери настройку кэша."
  options={[
    { name: "Кэшировать стабильный префикс 30k с TTL на 1 час; вопрос пользователя — последним, после breakpoint", summary: "Длинный TTL, стабильное в начале.", correct: true, justification: "Префикс большой и стабильный, значит он доминирует в счёте — ровно то, ради чего кэш и нужен. TTL на 1 час (запись 2x) тут верен, потому что 15-минутные паузы тишины истекли бы запись на 5 минут, превращая первый запрос каждого всплеска снова в полную запись; более длинное окно держит префикс тёплым через паузы. Волатильный вопрос после breakpoint сохраняет префикс токен-в-токен, так что каждый запрос во всплеске читает по 0.1x." },
    { name: "Кэшировать с дефолтным TTL 5 минут, чтобы запись была дешевле — 1.25x", summary: "Тариф по умолчанию.", justification: "Дешевле за запись, но описание самой нагрузки это убивает: 15-минутные паузы тишины превышают TTL 5 минут, поэтому запись истекает между всплесками и каждый всплеск платит свежую запись 1.25x на 30k токенов. Ты оптимизировал множитель записи и потерял скидку на чтение, которая и важна." },
    { name: "Поставить найденные документы и вопрос первыми, чтобы «важный» контент кэшировался", summary: "Волатильное в начале.", justification: "Инвертирует правило. Найденные документы и вопрос меняются на каждый запрос, так что префикс, начинающийся с них, расходится на нулевом токене — ничего стабильного никогда не кэшируется. Порядок фиксирован: стабильное в начало, волатильное в конец." },
    { name: "Не кэшировать; 30k токенов малы настолько, что полная ставка входа норм", summary: "Без кэша.", justification: "30k, пере-тарифицируемых по 1.0x на каждом запросе, когда 0.1x доступен после одной записи 1.25x, — это оставить ~90% трат на вход на столе. Для большого, перечитываемого, стабильного префикса это хрестоматийный случай для кэша." },
  ]}
/>

<Quiz
  id="01-prompt-caching-quiz-1"
  lessonSlug="01-overview"
  lang="ru"
  question="После деплоя cache hit rate упал почти до нуля, но ошибок нет и вывод выглядит верно. Какова самая вероятная причина?"
  choices={[
    { label: "Изменение в начале промпта (таймстамп, переставленные tools, переформатированная схема) сдвинуло префикс, и токен-в-токен больше ничего не совпадает", correct: true },
    { label: "Сервер кэша лёг — промах кэша всегда бросает ошибку, которую можно поймать", misconception: "Промах кэша тихий: вывод идентичен, попал ты или платишь по полной. Единственный сигнал — в полях usage, никогда не исключение." },
    { label: "Версия модели изменила ответы, что сломало семантическое совпадение", misconception: "Кэширование не семантическое — оно сопоставляет префикс токен-в-токен, а не смысл. Промпты с одинаковым смыслом, но разными байтами не попадают." },
    { label: "Истёк TTL, что навсегда отключает кэширование для этого промпта", misconception: "Истечение TTL лишь значит, что следующий запрос перезапишет запись; оно не отключает кэширование. Устойчивые почти-нулевые попадания указывают на изменённый префикс, а не на истечение." },
  ]}
/>

<Quiz
  id="01-prompt-caching-quiz-2"
  lessonSlug="01-overview"
  lang="ru"
  question="Префикс записали в кэш (1.25x), затем прочитали три раза (0.1x каждое) до истечения. Против полной ставки входа (1.0x) четыре раза — кэш сэкономил деньги?"
  choices={[
    { label: "Да — 1.25 + 0.1 + 0.1 + 0.1 = 1.55x всего против 4.0x без кэша; премия за запись отбивается после первого чтения", correct: true },
    { label: "Нет — премия за запись 1.25x значит, что на первом запросе ты всегда в убытке", misconception: "В убытке ты только на записи, которую ни разу не перечитали. Уже с одним чтением математика переворачивается; к третьему чтению это большой выигрыш." },
    { label: "Ничья — премия за запись и скидка на чтение взаимно гасятся", misconception: "Они не гасятся: 1.25x один раз плюс 0.1x за чтение намного ниже 1.0x за запрос, как только у тебя два попадания и больше." },
    { label: "Только если использовать TTL на 1 час", misconception: "Выбор TTL влияет на то, доживёт ли запись до чтения, а не на скидку за токен. С чтениями внутри окна дефолтный тариф 5 минут уже выигрывает." },
  ]}
/>

<DragOrder
  id="01-prompt-caching-drag-1"
  lessonSlug="01-overview"
  lang="ru"
  prompt="Расставь промпт от начала (кэшируется) к концу (свежее на каждый запрос) для максимального переиспользования кэша:"
  items={[
    { id: "a", label: "Определения tools (стабильные JSON-схемы, детерминированный порядок)" },
    { id: "b", label: "Системный промпт / инструкции роли (стабильные между запросами)" },
    { id: "c", label: "Большой статический контекст или few-shot примеры (сюда ставится cache breakpoint)" },
    { id: "d", label: "Найденные документы для запроса (меняются на каждом вызове)" },
    { id: "e", label: "Реальный вопрос пользователя (самое волатильное, всегда последним)" },
  ]}
  correctOrder={["a", "b", "c", "d", "e"]}
/>

</Explanation>

<FlowDiagram
  label="Префикс сопоставляется токен-в-токен; один сдвинутый байт инвалидирует всё после него"
  caption="Стабильный префикс (tools → system → контекст) читается из кэша по 0.1x. Breakpoint ставится на последний неизменный блок. Всё волатильное идёт после него и пересчитывается по полной ставке."
  hue="var(--d-ai)"
  perRow={5}
  nodes={[
    { id: "tools", label: "Tools", sub: "стабильные JSON-схемы" },
    { id: "system", label: "Системный промпт", sub: "стабильный" },
    { id: "context", label: "Статический контекст", sub: "breakpoint здесь — кэш 0.1x" },
    { id: "docs", label: "Найденные документы", sub: "пересчёт 1.0x" },
    { id: "question", label: "Вопрос пользователя", sub: "волатильное — последним" },
  ]}
  edges={[
    { from: "tools", to: "system" },
    { from: "system", to: "context" },
    { from: "context", to: "docs", label: "префикс кончается" },
    { from: "docs", to: "question" },
  ]}
/>

<KeyTakeaway>Стабильное (tools, system, контекст) — в начало, волатильное (ввод, таймстампы) — в конец, breakpoint на последнем неизменном блоке, и не мутируй префикс. Один сдвинутый байт превращает попадание 0.1x в запись 1.25x.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="01-prompt-caching-retrieval"
  lang="ru"
  questions={[
    { q: "Пройди по шагам, как один впихнутый таймстамп в начале системного промпта может умножить счёт за вход на 10, и почему ошибки не возникает.", a: "Кэширование сопоставляет префикс токен-в-токен с нулевой позиции. Если первое в твоём промпте — `Current time: 2026-05-23T14:02:09Z`, то у каждого запроса уникальный ведущий ряд токенов, так что сохранённая запись прошлого запроса никогда не совпадёт — расхождение на нулевом токене, до всего твоего стабильного системного промпта и документов. В итоге всё тело пере-тарифицируется как запись кэша по 1.25x базовой ставки входа вместо чтения по 0.1x. Это скачок в 12.5 раз на тех токенах (1.25 против 0.1), и через десятки тысяч кэшированных токенов это проявляется как раздувшиеся траты на вход. Ошибки не возникает, потому что промах кэша — не ошибочное условие: модель всё равно выдаёт корректный, побайтно идентичный ответ; правда видна лишь в блоке usage, где cache_creation_input_tokens высок, а cache_read_input_tokens почти ноль. Фикс — перенести всё волатильное — таймстампы, id, сообщение пользователя — в конец, после breakpoint, чтобы стабильный префикс оставался идентичным." },
    { q: "Когда TTL на 1 час стоит своей премии за запись 2x против дефолтного тарифа 5 минут, и как рассуждать о точке окупаемости?", a: "TTL решает лишь то, как долго запись живёт между чтениями; он не меняет скидку за токен при чтении (всегда 0.1x). Так что выбор чисто про то, перечитывает ли твой трафик префикс до истечения. С дефолтом 5 минут запись остаётся тёплой, только если запросы продолжают приходить внутри скользящего окна 5 минут (каждое чтение сбрасывает часы). Если трафик всплесковый или малообъёмный — паузы дольше 5 минут, но короче часа — запись на 5 минут истекает в паузе, так что каждый всплеск платит свежую запись. Тариф на 1 час стоит 2x на запись вместо 1.25x, но держит префикс живым через эти паузы, превращая то, что было бы повторными записями, в чтения. Рассуждение об окупаемости: 1 час отбивается, когда число чтений, которые он спасает (которые запись на 5 минут промахнула бы), экономит больше, чем те лишние 0.75x, что ты заплатил на записи. Для ровного высокочастотного трафика дефолт 5 минут дешевле и уже держит запись тёплой; для редкого, но устойчивого трафика выигрывает тариф на 1 час." },
  ]}
/>

<Recap lang="ru">
Кэширование промпта переиспользует точный, токен-в-токен префикс примерно по 0.1x базовой цены входа, но требует разовой премии за запись — 1.25x для дефолтного тарифа на 5 минут, 2x для тарифа на 1 час. Совпадение позиционное, а не семантическое, поэтому правило проектирования абсолютно: стабильное (tools, затем system, затем большой контекст и примеры) — в начало, волатильное (данные запроса, таймстампы, вопрос пользователя) — в конец, а breakpoint сидит на последнем неизменном блоке. Ниже минимальной кэшируемой длины модели (от 1024 до 4096 токенов в зависимости от модели) ничего не кэшируется, и тебя тарифицируют по полной без предупреждения. Производственный режим отказа — тихое отравление префикса: таймстамп, переставленная схема tool или переформатированный пробел рядом с нулевым токеном инвалидирует всё кэшированное тело, переворачивая каждый запрос с чтения 0.1x на запись 1.25x без ошибки и с идентичным выводом. Единственный сигнал — блок usage. Теперь, когда встретишь высокий `cache_creation_input_tokens` и почти нулевой `cache_read_input_tokens` на каждом запросе, ты знаешь: ищи, что изменилось у нулевого токена.
</Recap>
