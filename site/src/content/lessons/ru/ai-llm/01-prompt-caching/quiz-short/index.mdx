---
concepts:
- prefix-matching
- write-read-economics
- cache-ttl
- prompt-ordering
- minimum-cacheable-length
- prefix-poisoning
estMin: 14
lang: ru
level: senior
order: 8
prereqs: []
slug: quiz-short
sources:
- https://platform.claude.com/docs/en/build-with-claude/prompt-caching
- https://platform.claude.com/docs/en/about-claude/pricing
status: ready
summary: "Промпты на свободное припоминание по всему юниту prompt caching. Сначала ответь своими словами, затем раскрой модельный ответ и сравни."
title: 'Prompt caching: тест на свободное припоминание'
track: ai-llm
unit: 01-prompt-caching
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";

<Hook>
Припоминание бьёт перечитывание. На каждый промпт скажи или запиши полный ответ по памяти, прежде чем открыть модельный — усилие припоминания и закрепляет механизм.
</Hook>

<Goal lang="ru">
Реконструируй ключевые механизмы юнита — сопоставление префикса токен-в-токен, экономику записи/чтения, выбор TTL, правило порядка и тихое отравление префикса — не подглядывая в урок.
</Goal>

<RetrievalDrawer
  client:visible
  id="01-prompt-caching-short-retrieval"
  lang="ru"
  questions={[
    { q: "Почему prompt caching позиционное, а не семантическое, и что это значит для дизайна промпта?", a: "Провайдер не понимает, что два промпта значат одно и то же. Он хэширует ведущий ряд токенов — префикс — с нулевой позиции и ищет сохранённую запись, совпадающую точно, токен в токен, вплоть до помеченной границы cache breakpoint. В тот миг, когда потоки токенов расходятся (другое слово, лишний пробел, переставленный блок), совпадение там обрывается, и всё после тарифицируется как свежий вход. Следствие абсолютно: проектируй промпт так, чтобы начало было неизменным. Иерархия запроса у Anthropic фиксирована — tools, затем system, затем messages — поэтому всё стабильное ставишь вперёд (определения tools, системный промпт, большие документы, few-shot примеры), а всё волатильное — назад (данные на запрос, таймстампы, вопрос пользователя), размещая breakpoint на последнем блоке, идентичном между запросами." },
    { q: "Пройди по экономике записи/чтения и как выводится точка окупаемости.", a: "Запись в кэш дороже обычного запроса: 5-минутный тариф тарифицирует кэшированные токены по 1.25x базовой ставки входа, 1-часовой — по 2x. Чтение попадания тарифицирует те токены по 0.1x — скидка 90%. Так что платишь премию один раз на записи, затем 0.1x за каждое последующее чтение. Первый кэшированный запрос — небольшой убыток; ко второму запросу ты в плюсе, а с третьего — почти бесплатно. Сеньорская рамка не «кэшировать ли», а «будет ли этот префикс перечитан внутри TTL достаточно часто, чтобы амортизировать премию записи». Префикс, записанный и ни разу не перечитанный до истечения, строго хуже, чем не кэшировать — ты заплатил пошлину впустую." },
    { q: "Как работает TTL, когда 1-часовой тариф отбивает свою премию 2x, и как рассуждать о точке окупаемости между тарифами?", a: "Запись эфемерна. Дефолтный TTL — 5 минут, и таймер сбрасывается на каждом чтении, так что активно перечитываемый префикс остаётся тёплым бесконечно. Выбор TTL не меняет скидку на чтение токена (всегда 0.1x); он лишь решает, доживёт ли запись между чтениями. 1-часовой тариф (2x запись вместо 1.25x) отбивает премию, когда трафик всплесками или низкочастотный — паузы длиннее 5 минут, но короче часа — так что 5-минутная запись истекла бы в паузе и каждый всплеск платил бы свежую запись. 1-часовое окно превращает эти потенциальные записи в чтения. Оно окупается, когда спасённые им чтения экономят больше, чем лишние 0.75x, потраченные на запись." },
    { q: "Что такое минимальная кэшируемая длина и в чём опасность пересечения её снизу?", a: "У каждой модели есть порог, ниже которого префикс тихо не кэшируется — 1 024 токена для Sonnet 4.6/4.5 и Opus 4.1, 4 096 для Opus 4.7/4.6/4.5 и Haiku 4.5, 2 048 для Haiku 3.5. Опасность в том, что уход ниже порога не бросает ошибки: запрос успешен, вывод корректен, и ты просто платишь полную ставку входа без скидки и без предупреждения. Команда, добавившая cache_control к системному промпту на 700 токенов на Sonnet и считающая, что экономит, платит 1.0x на каждом вызове. Поймать это можно только читая блок usage и видя cache_read и cache_creation на нуле." },
    { q: "Объясни тихое отравление префикса: как одна небрежная правка умножает счёт за вход на 10 без ошибки.", a: "Обрыв кэша никогда не бросает исключение. Вывод побайтно идентичен, попал ты или платишь полную ставку; единственный сигнал живёт в блоке usage — cache_read_input_tokens против cache_creation_input_tokens. Так что одна правка у нулевого токена может увести воркфлоу с 90% чтений на 0% чтений, и единственный симптом — счёт через недели. Классические отравители кажутся безобидными: таймстамп или request id в начале системного промпта; tools, сериализованные в недетерминированном порядке (dict или Map, не сохраняющие порядок между прогонами); апгрейд библиотеки, заново форматирующий JSON-схему tools с другими пробелами; добавление сообщения пользователя перед статическим контекстом, а не после. Каждый сдвигает префикс у нулевого токена, поэтому всё кэшированное тело — десятки тысяч токенов — перетарифицируется по ставке записи 1.25x на каждом вызове." },
    { q: "Что такое cache breakpoints и зачем их складывают на длинном слоёном промпте?", a: "У тебя максимум 4 явных cache_control breakpoints на запрос, и каждый ищет назад в ограниченном окне прежнюю запись. Breakpoint кэширует весь префикс вплоть до своего блока включительно. На длинном слоёном промпте — tools, статический блок system, большой документ — breakpoint ставят после каждого слоя, чтобы изменение внизу стека инвалидировало только хвост, а не дорогую верхушку. Важно, что breakpoints — это вложенные префиксы, а не независимые кэши: попадание на позднем breakpoint означает, что все ранние совпали тоже. Это структурная защита от шевеления: держи самый стабильный, самый дорогой контент в начале за своим breakpoint, и пусть переписывается только волатильный хвост, когда нижние слои меняются." },
  ]}
/>

<Recap lang="ru">
Если ты смог реконструировать каждый ответ по памяти — ты держишь хребет юнита: сопоставление позиционное и токен-в-токен с нулевой позиции, поэтому стабильный контент идёт первым, а волатильный последним, с breakpoint на финальном неизменном блоке. Платишь 1.25x один раз и 0.1x за чтение, так что кэш выигрывает после первого перечитывания внутри TTL — 5 минут по дефолту, 1 час для всплесков с паузами. Ниже минимальной кэшируемой длины модели ничего не кэшируется, тихо. А производственный режим отказа всегда один — отравление префикса у нулевого токена, видимое только в блоке usage. Теперь, когда соберёшься добавить что-нибудь в системный промпт — таймстамп, флаг, новый tool — первый вопрос будет: это идёт до или после breakpoint?
</Recap>
