---
concepts:
- cache-control-placement
- usage-metrics
- prefix-ordering
- ttl-tier
- prefix-poisoning
estMin: 14
lang: ru
level: senior
order: 9
prereqs: []
slug: quiz-code
sources:
- https://platform.claude.com/docs/en/build-with-claude/prompt-caching
- https://platform.claude.com/docs/en/about-claude/pricing
status: ready
summary: "Читай реальные тела запросов Anthropic SDK и блоки usage, предсказывай, где встаёт cache breakpoint и попадёт ли он, и выбирай фикс наибольшего рычага."
title: 'Prompt caching: чтение запроса и usage'
track: ai-llm
unit: 01-prompt-caching
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Тело запроса решает, что кэшируется; блок usage говорит, сработало ли. Читай оба, предсказывай поведение и выбирай фикс, который сеньор делает первым — до того, как трогать TTL.
</Hook>

<Goal lang="ru">
Отработай цикл, который ты гоняешь на каждом инциденте с кэшем: читай, где стоит cache_control breakpoint, читай поля usage для подтверждения попадания или тихого промаха, и тянись к фиксу порядка раньше, чем к ручке настройки.
</Goal>

## Сниппет 1 — куда встаёт breakpoint?

```python
system = [
    {"type": "text", "text": SYSTEM_PROMPT},          # ~2k токенов, стабильно
    {"type": "text", "text": BIG_POLICY_DOC,           # ~28k токенов, стабильно
     "cache_control": {"type": "ephemeral"}},
]
messages = [
    {"role": "user", "content": f"As of {now()}: {question}"},
]
```

<Quiz
  id="01-prompt-caching-code-1"
  lessonSlug="01-prompt-caching"
  lang="ru"
  question="Breakpoint поставлен корректно на последнем стабильном блоке, но кэш ни разу не попадает. Почему?"
  choices={[
    { label: "Поскольку единственная вариация на запрос (таймстамп) сидит в сообщении пользователя после breakpoint, префикс стабилен и кэш ДОЛЖЕН попадать. Устойчивый промах означает, что якобы стабильный блок не побайтно идентичен между запросами — например SYSTEM_PROMPT встраивает build hash, или BIG_POLICY_DOC ре-сериализуется с другими пробелами на каждом вызове.", correct: true },
    { label: "Таймстамп в сообщении пользователя отравляет кэш, поэтому breakpoint никогда не попадёт.", misconception: "Таймстамп сидит после breakpoint, поэтому он вообще не влияет на кэшируемый префикс — это ровно правильное размещение. Волатильный контент должен быть после breakpoint; он не причина промаха." },
    { label: "cache_control должен быть на системном промпте (первом блоке), а не на документе политики, иначе ничего не кэшируется.", misconception: "Breakpoint кэширует весь префикс вплоть до своего блока включительно, так что breakpoint на последнем стабильном блоке кэширует оба блока. Размещение на документе корректно, а не причина промаха." },
    { label: "Нельзя кэшировать два текстовых блока в одном массиве system; кэшируется только первый.", misconception: "Один breakpoint на втором блоке кэширует весь ведущий ряд, включая оба блока. Несколько текстовых блоков в system — норма и полностью кэшируемы." },
  ]}
/>

## Сниппет 2 — блок usage

```json
{
  "usage": {
    "input_tokens": 41,
    "cache_creation_input_tokens": 30218,
    "cache_read_input_tokens": 0,
    "output_tokens": 215
  }
}
```

<Quiz
  id="01-prompt-caching-code-2"
  lessonSlug="01-prompt-caching"
  lang="ru"
  question="Этот блок usage повторяется на каждом запросе в устойчивом высокочастотном трафике. О чём он говорит?"
  choices={[
    { label: "Каждый запрос платит премию записи 1.25x (cache_creation 30k) и ни разу не читает (cache_read 0). Префикс отравляется на каждом вызове — кэш записывается и тут же инвалидируется до того, как следующий запрос успеет прочитать. Ищи, что мутирует у нулевого токена.", correct: true },
    { label: "Кэширование работает идеально — закэшировано 30k токенов, в этом весь смысл.", misconception: "cache_creation означает запись, а не чтение. Здоровый устойчивый трафик показывает cache_read_input_tokens высоким и cache_creation около нуля после прогрева. Устойчивые записи с нулевыми чтениями — сигнатура отказа." },
    { label: "41 input_tokens доказывают, что основная масса промпта отдана из кэша.", misconception: "input_tokens (41) — это лишь некэшированный хвост. 30 218 токенов cache_creation тарифицируются по 1.25x как запись; ничего не прочитано по 0.1x." },
    { label: "cache_read равен 0 лишь потому, что это первый запрос; он сам вырастет.", misconception: "Это повторяется на каждом запросе в устойчивом трафике, так что это не одноразовый прогрев. Чтения остаются на нуле, потому что префикс меняется на каждом вызове." },
  ]}
/>

## Сниппет 3 — TTL и порядок tools

```python
client.messages.create(
    model="claude-sonnet-4-6",
    tools=serialize_tools(registry.values()),   # dict → list, порядок не гарантирован
    system=[{"type": "text", "text": SYSTEM,
             "cache_control": {"type": "ephemeral", "ttl": "1h"}}],
    messages=[{"role": "user", "content": q}],
)
```

<Quiz
  id="01-prompt-caching-code-3"
  lessonSlug="01-prompt-caching"
  lang="ru"
  question="1-часовой TTL выставлен и системный промпт стабилен, но hit rate скачет от деплоя к деплою. Какой фикс с наибольшим рычагом?"
  choices={[
    { label: "tools идут раньше system в иерархии кэша, а serialize_tools перебирает dict без гарантии порядка — поэтому блок tools (самое начало префикса) может переставляться между процессами, отравляя всё после. Сортируй tools детерминированно перед сериализацией; TTL нерелевантен, пока префикс нестабилен.", correct: true },
    { label: "Поднять премию TTL ещё выше — скачущие попадания означают, что 1-часовое окно всё ещё мало.", misconception: "TTL контролирует время жизни, а не стабильность префикса. Переставленный блок tools в начале ломает совпадение при любом TTL; длина окна не спасает меняющийся префикс." },
    { label: "Перенести cache_control на блок tools вместо system.", misconception: "Перенос breakpoint не останавливает перестановку tools. Фикс — детерминированный порядок tools; только тогда важно размещение breakpoint." },
    { label: "Баг в том, что 1h TTL стоит 2x — переключись на 5 минут, чтобы сэкономить.", misconception: "Смена тарифа меняет множитель записи, а не корневую причину скачущих попаданий. При недетерминированном порядке tools ты и так платишь записи постоянно." },
  ]}
/>

## Сниппет 4 — арифметика точки окупаемости

```
# Sonnet 4.6: базовый вход $3.00/MTok, запись в кэш (5м) $3.75/MTok, чтение из кэша $0.30/MTok
# стабильный префикс на 20k токенов, перечитан N раз внутри окна TTL
write_cost = 20_000/1e6 * 3.75          # одна запись
read_cost  = 20_000/1e6 * 0.30 * N      # N чтений
uncached   = 20_000/1e6 * 3.00 * (N+1)  # те же N+1 запросов, без кэша
```

<Quiz
  id="01-prompt-caching-code-4"
  lessonSlug="01-prompt-caching"
  lang="ru"
  question="При такой цене после скольких чтений кэширование префикса на 20k становится дешевле, чем не кэшировать вовсе?"
  choices={[
    { label: "После первого чтения. Запись + 1 чтение = (3.75 + 0.30)/MTok·20k против 2 некэшированных запросов = 6.00/MTok·20k — кэш уже дешевле при N=1, и разрыв растёт с каждым следующим чтением.", correct: true },
    { label: "Никогда при 5-минутном TTL — только 1-часовой тариф когда-либо окупается.", misconception: "TTL меняет множитель записи (1.25x против 2x), а не то, бьют ли чтения полную ставку. При $0.30 против $3.00 за чтение даже 5-минутный тариф окупается на первом чтении." },
    { label: "После ~12 чтений — премия записи 1.25x столько амортизируется.", misconception: "Премия — это лишь 0.25x одной записи (3.75 против 3.00). Она отбивается почти сразу, потому что каждое чтение экономит 2.70/MTok (3.00 − 0.30); одно чтение её с лихвой покрывает." },
    { label: "Только если N достаточно велико, чтобы доминировали выходные токены.", misconception: "Выходные токены не зависят от кэша и идентичны в обоих случаях; они выпадают из сравнения. Точка окупаемости задаётся целиком математикой записи против чтения на входе." },
  ]}
/>

<Recap lang="ru">
Любой вопрос о кэше читается в теле запроса и блоке usage. Breakpoint кэширует весь префикс вплоть до своего блока включительно, поэтому он принадлежит последнему стабильному блоку — но стабильный breakpoint бесполезен, если блоки перед ним (сначала tools, затем system) не побайтно идентичны, отчего недетерминированный порядок tools и ре-сериализованные пробелы — топовые отравители. Поля usage — единственная правда: высокий cache_creation при cache_read около нуля на устойчивом трафике — отравленный префикс, а не успех. А арифметика окупаемости жестока в пользу кэша — перечитываемый префикс бьёт полную ставку уже после одного чтения, так что реальная работа — держать префикс стабильным, а не тюнить TTL. Теперь, когда откроешь блок usage и увидишь устойчивый `cache_creation` при нулевом `cache_read`, твой первый вопрос не «какой TTL поставить», а «что изменилось у нулевого токена».
</Recap>
