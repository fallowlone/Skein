---
concepts:
- prefix-matching
- cache-write-premium
- cache-ttl
- prompt-ordering
- prefix-invalidation
- break-even
estMin: 13
lang: ru
level: senior
order: 7
prereqs: []
slug: quiz-choice
sources:
- https://platform.claude.com/docs/en/build-with-claude/prompt-caching
- https://platform.claude.com/docs/en/about-claude/pricing
status: ready
summary: "Синтез по всему юниту prompt caching в формате выбора: сопоставление префикса, экономика записи/чтения, выбор TTL, правила порядка и тихое отравление префикса."
title: 'Prompt caching: тест с множественным выбором'
track: ai-llm
unit: 01-prompt-caching
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Шесть вопросов через весь юнит. Каждый отражает реальное решение — куда ставить cache breakpoint, какой TTL покупать, почему счёт утроился — а не определение для пересказа.
</Hook>

<Goal lang="ru">
Убедись, что можешь связать сопоставление префикса токен-в-токен, экономику записи/чтения, выбор TTL и тихий режим отказа, к которому вёл урок.
</Goal>

<Quiz
  id="01-prompt-caching-choice-1"
  lessonSlug="01-prompt-caching"
  lang="ru"
  question="Два запроса несут один и тот же системный промпт и документ на 30k токенов, но в запросе B внутри этого блока один лишний пробел. Сколько в B прочитается из кэша?"
  choices={[
    { label: "Ничего, начиная с этой точки — сопоставление идёт токен-в-токен с нулевой позиции, поэтому расхождение обрывает совпадение там, и всё после лишнего пробела тарифицируется как свежая запись.", correct: true },
    { label: "Около 99% — кэш терпит мелкие различия в пробелах и просто перетарифицирует изменённый токен.", misconception: "Кэширование точное по байтам/токенам, не нечёткое. Один вставленный пробел ре-токенизирует ряд и обрывает префикс в этой позиции; частичного прощения токена нет." },
    { label: "Всё целиком — промпты значат одно и то же, поэтому семантическое совпадение срабатывает.", misconception: "Prompt caching позиционное, не семантическое. Провайдер никогда не сравнивает смысл — он хэширует ведущий ряд токенов и сопоставляет точно." },
    { label: "Промахивается только системный промпт; документ после него всё равно попадает независимо.", misconception: "Cache breakpoints — это вложенные префиксы, а не независимые кэши. Обрыв в начале префикса инвалидирует всё, что ниже него." },
  ]}
/>

<Quiz
  id="01-prompt-caching-choice-2"
  lessonSlug="01-prompt-caching"
  lang="ru"
  question="Префикс записан один раз (1.25x, дефолтный тариф), затем прочитан дважды (0.1x каждый) до истечения. По сравнению с полной ставкой (1.0x) трижды — кэш помог?"
  choices={[
    { label: "Да — 1.25 + 0.1 + 0.1 = 1.45x суммарно против 3.0x без кэша; премия за запись уже отбита после первого чтения.", correct: true },
    { label: "Нет — премия 1.25x за запись означает, что первый запрос всегда в убытке, так что ты никогда не выйдешь в плюс.", misconception: "В убытке ты только на записи, которую ни разу не перечитали. Уже первое чтение опускает сумму ниже точки окупаемости; ко второму чтению — явный выигрыш." },
    { label: "Это в ноль — запись 1.25x и чтения 0.1x взаимно сокращаются.", misconception: "Они не сокращаются: 1.25 один раз плюс 0.1 за чтение — намного ниже 1.0 за запрос, как только у тебя два и более попадания." },
    { label: "Только с 1-часовым TTL — 5-минутный тариф никогда не отбивает премию.", misconception: "TTL управляет тем, доживёт ли запись до чтения, а не скидкой на токен. При чтениях внутри окна дефолтный тариф 1.25x уже выигрывает." },
  ]}
/>

<Quiz
  id="01-prompt-caching-choice-3"
  lessonSlug="01-prompt-caching"
  lang="ru"
  question="У RAG-сервиса стабильный префикс на 30k токенов, но трафик всплесками: запросы кучкуются, затем тишина ~15 минут. Какая настройка кэша верна?"
  choices={[
    { label: "1-часовой TTL на стабильном префиксе, вопрос пользователя после breakpoint. 15-минутные паузы истекли бы 5-минутную запись, так что первый запрос каждого всплеска переписывал бы 30k токенов; длинное окно держит префикс тёплым через паузы.", correct: true },
    { label: "Дефолтный 5-минутный TTL, чтобы запись была дешевле на 1.25x.", misconception: "15-минутные паузы превышают 5-минутный TTL, поэтому запись истекает между всплесками и каждый всплеск платит свежую запись 1.25x на 30k токенов. Ты оптимизировал множитель записи и потерял скидку на чтение, которая важна." },
    { label: "Поставить найденные документы и вопрос вперёд, чтобы «важный» контент кэшировался.", misconception: "Это инвертирует правило. Документы и вопрос меняются на запрос, поэтому префикс, начинающийся с них, расходится на нулевом токене и ничего стабильного не кэшируется." },
    { label: "Не кэшировать — 30k токенов достаточно мало, чтобы полная ставка была норм.", misconception: "30k, перетарифицированные по 1.0x на каждом запросе, когда 0.1x доступно после одной записи, оставляют ~90% трат на вход на столе. Большой, перечитываемый, стабильный префикс — учебный случай для кэша." },
  ]}
/>

<Quiz
  id="01-prompt-caching-choice-4"
  lessonSlug="01-prompt-caching"
  lang="ru"
  question="Cache hit rate упал почти до нуля после деплоя. Ошибок нет, выводы выглядят корректно. Наиболее вероятная причина?"
  choices={[
    { label: "Изменение у начала промпта — впрыснутый таймстамп, переставленные схемы tools или библиотека, заново форматирующая JSON с другими пробелами — сдвинуло префикс, так что больше ничего не совпадает токен-в-токен.", correct: true },
    { label: "Кэш-бэкенд лежит — промах всегда поднимает ошибку, которую можно поймать и заалертить.", misconception: "Промах кэша тихий: вывод побайтно идентичен, попал ты или платишь полную ставку. Единственный сигнал живёт в полях usage, никогда не в исключении." },
    { label: "Смена версии модели изменила ответы, сломав семантическое совпадение.", misconception: "Кэширование не семантическое — оно сопоставляет префикс токен-в-токен, не смысл. Внезапный устойчивый почти-нулевой hit rate указывает на сдвинутый префикс, а не на выводы модели." },
    { label: "TTL истёк, что навсегда отключает кэширование для этого промпта.", misconception: "Истечение TTL означает лишь то, что следующий запрос перепишет запись. Устойчивые почти-нулевые попадания на многих запросах указывают на изменённый префикс, а не на истечение." },
  ]}
/>

<Quiz
  id="01-prompt-caching-choice-5"
  lessonSlug="01-prompt-caching"
  lang="ru"
  question="На Sonnet ты кэшируешь системный промпт на 600 токенов и не видишь скидки на чтение на повторных запросах, без ошибки. Почему?"
  choices={[
    { label: "Он ниже минимальной кэшируемой длины модели (1 024 токена для Sonnet 4.6/4.5). Префикс ниже минимума тихо не кэшируется и тарифицируется по полной ставке — без ошибки, просто без скидки.", correct: true },
    { label: "Кэширование применяется только к блоку system, не к коротким промптам, и его надо перенести в messages.", misconception: "Дело не в типе блока, а в пороге. Ниже токенного минимума модели ничего не кэшируется, где бы контент ни лежал." },
    { label: "Первый запрос всегда не кэшируется; попадания начнутся к третьему-четвёртому вызову.", misconception: "Счётчика прогрева нет. Обычная запись происходит на первом запросе, чтения идут со второго. Префикс на 600 токенов просто никогда не пересекает минимум, так что запись вообще не создаётся." },
    { label: "Sonnet не поддерживает prompt caching; только Opus.", misconception: "Все актуальные модели поддерживают кэширование. Они различаются минимальной кэшируемой длиной — 1 024 для Sonnet, 4 096 для Opus/Haiku 4.5 — а не наличием кэширования." },
  ]}
/>

<Quiz
  id="01-prompt-caching-choice-6"
  lessonSlug="01-prompt-caching"
  lang="ru"
  question="У тебя длинный слоёный промпт: tools, статический блок system, затем большой документ, меняющийся раз в сутки. Как расставить до 4 cache breakpoints?"
  choices={[
    { label: "Сложить breakpoints после tools, после статического блока system и после документа — так изменение внизу стека инвалидирует только хвост, а не дорогую верхушку, и попадание на позднем breakpoint означает, что ранние совпали тоже.", correct: true },
    { label: "Поставить один breakpoint в самом конце всего промпта, чтобы закэшировать максимум токенов.", misconception: "Один концевой breakpoint означает, что любое изменение где угодно перед ним переписывает всё тело. Стек вложенных breakpoints защищает стабильную верхушку от шевеления ниже." },
    { label: "Поставить один breakpoint перед tools, чтобы волатильные части кэшировались первыми.", misconception: "Порядок фиксирован — tools идут первыми и самые стабильные. Breakpoint должен стоять на последнем идентичном блоке, а не перед самым стабильным." },
    { label: "Использовать отдельный breakpoint на каждый запрос, чтобы у каждого был свой независимый кэш.", misconception: "Breakpoints — это вложенные префиксы внутри одного запроса, а не независимые кэши на запрос. Записи на запрос с волатильным контентом никогда не перечитаются." },
  ]}
/>

<Recap lang="ru">
Сквозная линия юнита — одно правило с экономическим движком за ним: сопоставление идёт токен-в-токен с нулевой позиции, поэтому стабильный контент (tools, затем system, затем большой контекст) идёт первым, а волатильный (таймстампы, найденные документы, вопрос пользователя) — последним, с breakpoint на финальном неизменном блоке. Платишь 1.25x один раз за запись и 0.1x за каждое чтение, так что кэш выигрывает после первого перечитывания внутри TTL — 5 минут по дефолту, 1 час для всплесков с паузами. Ниже минимальной кэшируемой длины модели ничего не кэшируется, тихо. Производственный режим отказа всегда один: байт у нулевого токена отравляет префикс, переворачивает каждый запрос с чтения 0.1x на запись 1.25x, и единственный сигнал — в блоке usage.
</Recap>
