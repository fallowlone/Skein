---
concepts:
- cache-prefix-invalidation
- tool-call-streaming
- agent-budget-cap
- rag-eval-coverage
- request-tracing
- seam-failure
estMin: 13
lang: ru
level: senior
order: 7
prereqs: []
slug: quiz-choice
sources:
- https://platform.claude.com/docs/en/build-with-claude/prompt-caching
- https://dev.to/waxell/the-47000-agent-loop-why-token-budget-alerts-arent-budget-enforcement-389i
- https://dev.to/adamo_software/tool-use-api-design-for-llms-5-patterns-that-prevent-agent-loops-and-silent-failures-f29
status: ready
summary: "Тест с выбором на синтез по всему треку AI/LLM — где caching, tools, RAG, streaming, бюджеты, агенты и evals падают на швах между собой."
title: 'Сборка LLM-приложений: тест с выбором ответа'
track: ai-llm
unit: 08-putting-it-together
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Шесть вопросов поперёк всего трека. Ни один не тестирует один слой — каждый ставит два корректных слоя вместе и спрашивает, где ломается композиция. Этот шов и есть место, где production LLM-приложения реально падают.
</Hook>

<Goal lang="ru">
Убедись, что рассуждаешь о швах: как RAG взаимодействует с кэшем, как tool call взаимодействует со стримом, как agent loop взаимодействует с бюджетом и почему evals обязаны включать живой retrieval-путь.
</Goal>

<Quiz
  id="08-putting-it-together-choice-1"
  lessonSlug="08-putting-it-together"
  lang="ru"
  question="У RAG-ассистента длинный статический system-промпт с пометкой cache_control, но cache hit rate в проде около 0% — хотя на демо он выглядел закэшированным. В чём причина и фикс?"
  choices={[
    { label: "Полученные чанки вшиты в кэшируемый system-блок, поэтому байт-в-байт префикс отличается на каждом запросе и кэш бьётся. Перенеси попроцессный RAG-контекст за брейкпойнт cache_control, в messages; статические правила + схемы тулзов оставь до него.", correct: true },
    { label: "TTL в 5 минут слишком короткий для production-трафика; подними до 1 часа.", misconception: "TTL управляет тем, сколько живёт запись, а не тем, совпадает ли префикс. Если префикс разный на каждом запросе, дольше живущая запись всё равно не матчится — hit rate остаётся нулём. Проблема в нестабильности префикса, а не в вытеснении." },
    { label: "На демо закэшировалось из-за прогрева; проду просто нужно больше трафика, чтобы прогреть кэш.", misconception: "Объём не помогает, когда у каждого запроса разный префикс. На демо «закэшировалось» только потому, что повторялся один и тот же вопрос; реальные запросы несут разные чанки внутри префикса." },
    { label: "Отключи RAG, чтобы префикс снова стал стабильным.", misconception: "Это меняет фичу заземления на метрику кэша — оптимизация не того слоя. Фикс сохраняет retrieval и переносит его за брейкпойнт, чтобы статический префикс остался в кэше." },
  ]}
/>

<Quiz
  id="08-putting-it-together-choice-2"
  lessonSlug="08-putting-it-together"
  lang="ru"
  question="Стримящийся ход заканчивается с stop_reason: tool_use посередине, и UI вечно висит на спиннере. Какая ментальная модель чинит это?"
  choices={[
    { label: "Стрим — это конечный автомат, а не труба токенов. tool_use — это переход (пауза рендера, запуск тулзы, отправка tool_result обратно в следующем запросе, возобновление) — а не конец хода.", correct: true },
    { label: "Модель ошиблась посреди стрима; повтори весь запрос с нуля.", misconception: "Ничего не ошиблось — модель намеренно запросила тулзу. Перезапуск повторяет работу и может зациклиться; нужно обработать переход tool_use и продолжить, а не рестартовать." },
    { label: "Упёрлись в max_tokens; подними его, чтобы модель дописала прозу вместо остановки.", misconception: "max_tokens ограничивает длину, а не решение модели вызвать тулзу. Она остановилась, чтобы запросить тулзу; больший лимит не меняет эту ветку." },
    { label: "Буферизируй весь ответ перед рендером, чтобы частичные токены не показывались.", misconception: "Буферизация выбрасывает выигрыш стриминга по латентности и всё равно не запускает тулзу — забуферишь, а потом всё равно надо выполнить и возобновить. Ветка tool_use в любом случае не обработана." },
  ]}
/>

<Quiz
  id="08-putting-it-together-choice-3"
  lessonSlug="08-putting-it-together"
  lang="ru"
  question="У агента есть дашборд token-бюджета с алертами, но плохой ввод загнал его в retry-цикл, спаливший тысячи долларов за ночь. Почему бюджет не защитил?"
  choices={[
    { label: "Алерты срабатывают после трат; это не enforcement. Фикс — жёсткий гейт перед каждым вызовом модели: проверь step ≤ MAX_STEPS и spent ≤ BUDGET_USD (иначе откажи) плюс dedupe по hash(tool_name, args), чтобы убить повторные циклы.", correct: true },
    { label: "Дашборд семплировал слишком медленно; обновление раз в секунду поймало бы это.", misconception: "Более быстрая наблюдаемость всё равно только наблюдает. К моменту срабатывания любого алерта доллары уже потрачены; только pre-call гейт, который отказывает следующему запросу, реально ограничивает стоимость." },
    { label: "Модель слишком дорогая; переключись на дешевле, и цикл остановится.", misconception: "Более дешёвая модель делает runaway-цикл дешевле за шаг, но он всё равно бежит бесконечно на плохом вводе. Дефект — отсутствие условия остановки, а не цена за токен." },
    { label: "Добавь больше retry с backoff, чтобы malformed-результат в итоге распарсился.", misconception: "Больше retry — это противоположность фикса, оно кормит цикл. Знаменитый случай бежал 11 дней на $47 000 именно потому, что ничто не ограничивало число итераций." },
  ]}
/>

<Quiz
  id="08-putting-it-together-choice-4"
  lessonSlug="08-putting-it-together"
  lang="ru"
  question="Твой офлайн eval-набор зелёный на каждом деплое, но пользователи жалуются на худшие ответы сразу после смены ретривера. Почему evals пропустили регрессию?"
  choices={[
    { label: "Набор оценивает генерацию на фиксированном golden-контексте и никогда не гоняет живой retrieval-путь, поэтому регрессия retrieval уезжает зелёной. Добавь end-to-end evals, которые варьируют retrieval, оценивая ответ, построенный из того, что реально вернул живой ретривер.", correct: true },
    { label: "Evals флейки и им нужно больше retry для стабильности.", misconception: "Флейкость проявляется как шумный pass/fail, а не как стабильно зелёное, скрывающее реальную регрессию. Это пробел в покрытии: шов retrieval просто не в тестовом пути." },
    { label: "Модель тихо изменилась; перегони те же generation-evals на новой модели.", misconception: "Изменился ретривер, а не модель. Перегон generation-only evals на замороженном контексте всё равно пропускает путь, который реально регрессировал." },
    { label: "Метрики генерации неверны, их надо полностью заменить.", misconception: "Метрики генерации могут быть нормальны в изоляции; проблема в том, что они гоняются на замороженном контексте. Нужен end-to-end eval с живым retrieval, а не другие метрики генерации." },
  ]}
/>

<Quiz
  id="08-putting-it-together-choice-5"
  lessonSlug="08-putting-it-together"
  lang="ru"
  question="Сетевой сбой обрывает стримящийся ход с stop_reason: tool_use, но с нулём tool_use-блоков, и агент тихо замирает. Поздний запрос отвергается с 'unexpected tool_use_id found in tool_result block'. Какова общая первопричина?"
  choices={[
    { label: "Композиция не трактует tool_use/tool_result как строгую пару между запросами. Каждый tool_use id должен матчиться ровно одним tool_result в следующем запросе; оборванные или осиротевшие блоки надо детектировать и ход чинить или рестартовать, а не оставлять висящим.", correct: true },
    { label: "Два несвязанных бага — один в стриминге, один в API тулзов; чини их отдельно.", misconception: "Оба — один и тот же шов: стрим и протокол тулзов делят состояние между запросами, а композиция никогда не валидировала эту пару. Пустой tool_use-стоп и осиротевший tool_use_id — два симптома невалидированных переходов." },
    { label: "Модель галлюцинирует вызовы тулзов; ужесточи system-промпт.", misconception: "Ни один симптом — не галлюцинация: один — оборванный стрим без tool-блока, другой — устаревший id от упавшего хода. Фикс — парность на уровне протокола и восстановление, а не формулировка промпта." },
    { label: "Idempotency-ключи на HTTP-слое предотвратили бы оба.", misconception: "HTTP-идемпотентность дедуплицирует retry; она ничего не делает с пустым tool_use-стопом или с tool_result, ссылающимся на id, который модель не выдавала. Нужен state-machine-трекинг tool_use ↔ tool_result." },
  ]}
/>

<Quiz
  id="08-putting-it-together-choice-6"
  lessonSlug="08-putting-it-together"
  lang="ru"
  question="Выкатывается RAG-агентный ассистент: каждый компонент прошёл свой тест, но счёт утроился и ответы кажутся медленнее в проде. Какой senior-ход для дебага?"
  choices={[
    { label: "Трассируй один реальный production-запрос через все слои (cache → RAG → tools → stream → loop) и на каждой границе спрашивай, что следующий слой считал само собой, а этот слой только что изменил. Баг живёт в шве, а не внутри компонента.", correct: true },
    { label: "Перегони unit-тест каждого компонента в проде, чтобы найти, какой регрессировал.", misconception: "Они все всё ещё проходят — в этом ловушка. Сбой между корректными компонентами (например, RAG бьёт кэш-префикс, пока зацикленные запросы растят транскрипт), поэтому покомпонентные тесты его не вскроют." },
    { label: "Откатись к версии с одной фичей и выкатывай фичи по одной всегда.", misconception: "Фичи обязаны сосуществовать; цель — корректная композиция, а не избегание композиции. Трассировка шва позволяет оставить все шесть слоёв и починить границу." },
    { label: "Масштабируй железо, чтобы поглотить стоимость и латентность.", misconception: "Больше железа оплачивает сломанный шов вместо его починки — кэш всё ещё бит, а цикл всё ещё растит транскрипт. И стоимость, и латентность идут из шва, а не из ёмкости." },
  ]}
/>

<Recap lang="ru">
Сквозная линия трека — одно предложение: каждый слой корректен по своему контракту, а сбой живёт там, где выход одного слоя тихо нарушает входное допущение следующего. RAG меняет префикс, который кэш считал стабильным; tool_use-стоп меняет стрим, который рендерер считал прозой; реальный ввод меняет цикл, который бюджет считал конечным; retrieval-путь меняется под eval-набором, считавшим контекст фиксированным. Эти баги не дебажат, перетестируя куски — трассируют один реальный запрос насквозь и на каждой границе спрашивают, что следующий слой считал само собой, а предыдущий это только что изменил. Теперь, когда видишь собранное приложение, которое проходит все тесты, но ломается в проде, ты берёшь трассировку запроса раньше отладчика.
</Recap>
