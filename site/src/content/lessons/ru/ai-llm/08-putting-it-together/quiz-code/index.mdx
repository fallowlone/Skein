---
concepts:
- cache-control-layout
- streaming-state-machine
- agent-budget-gate
- rag-retrieval-call
- tool-result-pairing
estMin: 14
lang: ru
level: senior
order: 9
prereqs: []
slug: quiz-code
sources:
- https://platform.claude.com/docs/en/build-with-claude/prompt-caching
- https://docs.anthropic.com/en/docs/build-with-claude/streaming
- https://dev.to/waxell/the-47000-agent-loop-why-token-budget-alerts-arent-budget-enforcement-389i
status: ready
summary: "Читай реальные сниппеты композиции — раскладку кэша, state machine стрим/тулзы, agent loop с бюджет-гейтом и RAG retrieve+inject — и выбирай фикс с наибольшим рычагом."
title: 'Сборка LLM-приложений: чтение кода и трейсов'
track: ai-llm
unit: 08-putting-it-together
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Швы видны в коде, не на слайдах. Читай каждый сниппет так, как читал бы PR для production LLM-фичи: найди границу, где один слой тихо нарушает допущение следующего, и выбери фикс, который senior-инженер делает первым.
</Hook>

<Goal lang="ru">
Отработай цикл, который гоняешь при сборке слоёв: прочитай раскладку запроса или цикл, найди границу, ломающуюся под реальным трафиком, и тянись к структурному фиксу — а не к ручке тюнинга.
</Goal>

## Сниппет 1 — раскладка кэша

```python
system = [
    {"type": "text", "text": SYSTEM_RULES},          # статика
    {"type": "text", "text": tool_schemas_json},      # статика
    {"type": "text", "text": "Relevant docs:\n" + retrieved_chunks,
     "cache_control": {"type": "ephemeral"}},          # попроцессный RAG, затем брейкпойнт
]
resp = client.messages.create(model=MODEL, system=system,
                              messages=[{"role": "user", "content": query}])
```

<Quiz
  id="08-putting-it-together-code-1"
  lessonSlug="08-putting-it-together"
  lang="ru"
  question="Брейкпойнт стоит после полученных чанков. Что эта раскладка реально кэширует и как это починить?"
  choices={[
    { label: "Ничего переиспользуемого: брейкпойнт cache_control покрывает весь префикс до попроцессных RAG-чанков включительно, а они разные на каждом запросе — поэтому кэшируемый префикс никогда не матчится. Поставь брейкпойнт после статических правил + схем тулзов, а RAG-чанки перенеси за него (в messages), чтобы статическая голова осталась в кэше.", correct: true },
    { label: "Он корректно кэширует system-правила и схемы тулзов; раскладка нормальная.", misconception: "Кэш переиспользует самый длинный идентичный префикс до ПОСЛЕДНЕГО брейкпойнта. С брейкпойнтом после чанков кэшируемый отрезок включает чанки, меняющиеся на каждом запросе — поэтому он никогда не хитит. Под брейкпойнтом должно быть только то, что до чанков." },
    { label: "Он кэширует чанки — самую дорогую часть; это правильная оптимизация.", misconception: "Попроцессные чанки — это ровно то, что не надо кэшировать: они меняются на каждом запросе, так что кэширование их — впустую. Переиспользуемая дорогая часть — длинные статические правила + схемы; они должны стоять до брейкпойнта." },
    { label: "Добавь второй брейкпойнт после чанков, чтобы кэшировать больше.", misconception: "Больше брейкпойнтов внутри попроцессного контента не помогает — префикс всё равно отличается с первого байта, который меняют чанки. Фикс — вообще убрать динамику из кэшируемого префикса, а не добавлять в неё брейкпойнты." },
  ]}
/>

## Сниппет 2 — state machine стрима / тулзы

```python
async for event in stream:
    if event.type == "content_block_delta":
        ui.append(event.delta.text)
    elif event.type == "message_stop":
        ui.done()                       # спиннер гаснет здесь
# (других веток нет)
```

<Quiz
  id="08-putting-it-together-code-2"
  lessonSlug="08-putting-it-together"
  lang="ru"
  question="Ход с тулзой стримит текст, затем заканчивается с stop_reason: tool_use. Протрассируй поведение этого цикла и выбери фикс."
  choices={[
    { label: "message_stop срабатывает со stop_reason tool_use, поэтому ui.done() выполняется, но тулза не запущена и запрос-продолжение не отправлен — ответ неполный и сессия застопорена. Ветвись по stop_reason: на tool_use запусти тулзу, добавь по tool_result на каждый tool_use id и отправь follow-up-запрос до вызова ui.done().", correct: true },
    { label: "Работает — message_stop всегда значит, что ход полностью завершён.", misconception: "message_stop срабатывает для любого stop_reason, включая tool_use. Трактовка его как «ход завершён» гасит UI, пока модель на деле ждёт tool_result, который ты не отправил." },
    { label: "Баг в append; он должен буферизировать до message_stop.", misconception: "Аппенд дельт — корректное поведение стриминга. Не хватает ветки на tool_use-стоп — буферизация ничего не меняет в необработанном переходе тулзы." },
    { label: "Добавь таймаут, чтобы спиннер гас через N секунд.", misconception: "Таймаут скрывает застой вместо завершения хода; пользователь всё равно получает полу-ответ. Надо детектить tool_use-стоп, запускать тулзу и возобновлять ход." },
  ]}
/>

## Сниппет 3 — agent loop

```python
def run_agent(task):
    msgs = [task]
    while True:
        resp = client.messages.create(model=MODEL, messages=msgs, tools=TOOLS)
        if resp.stop_reason != "tool_use":
            return resp
        for call in tool_uses(resp):
            msgs.append(tool_result(call, execute(call)))   # транскрипт растёт каждый цикл
```

<Quiz
  id="08-putting-it-together-code-3"
  lessonSlug="08-putting-it-together"
  lang="ru"
  question="Malformed-результат тулзы заставляет модель переформулировать и вечно повторять тот же вызов. Сколько это стоит и какой минимальный фикс?"
  choices={[
    { label: "Каждая итерация пересылает весь растущий транскрипт, поэтому стоимость растёт суперлинейно, а цикл никогда не завершается. Добавь жёсткий гейт перед каждым create(): проверь step < MAX_STEPS и spent < BUDGET_USD (иначе откажи) и dedupe по hash(tool_name, args), чтобы оборвать повторные вызовы.", correct: true },
    { label: "Стоимость плоская на итерацию, так что это просто медленно, а не дорого.", misconception: "Стоимость не плоская — msgs растёт каждый цикл, и весь транскрипт пересылается на каждом вызове, поэтому input-стоимость за шаг растёт с числом итераций. Незавершённый цикл и медленный, И суперлинейно дорогой." },
    { label: "Оберни execute() в retry с backoff, чтобы результат в итоге распарсился.", misconception: "Retry детерминированно malformed-результата лишь добавляет итераций — это кормит цикл. Циклу нужен завершающий гейт (step/budget-капы + dedupe), а не больше попыток." },
    { label: "Поймай исключение и продолжи цикл.", misconception: "Исключения нет — тулза возвращает значение, которое не нравится модели, поэтому цикл идёт на валидных ответах. Останавливают только явный потолок step/budget и dedupe вызовов." },
  ]}
/>

## Сниппет 4 — RAG retrieve + расчёт стоимости

```python
hits = vectordb.query(embed(user_q), top_k=20)        # без rerank, без порога
context = "\n".join(h.text for h in hits)             # ~20 чанков × ~800 ток ≈ 16k ток
# input ≈ 16k контекст + 2k промпт = 18k ток; цена $3 / 1M input ток
cost_per_call = 18_000 / 1_000_000 * 3                # ≈ $0.054, на каждый запрос, без кэша
```

<Quiz
  id="08-putting-it-together-code-4"
  lessonSlug="08-putting-it-together"
  lang="ru"
  question="Этот retrieve-and-inject путь корректен в изоляции, но дорог в собранном приложении. Прочитай числа и выбери изменение с наибольшим рычагом."
  choices={[
    { label: "Запихивание top-20 сырых чанков кладёт ~16k динамических токенов в каждый запрос — некэшируемых и ~$0.054/вызов до всякого ответа. Применяй rerank до top 3–5 релевантных чанков (срезая контекст ~4×) и держи этот контекст за брейкпойнтом cache_control, чтобы статический префикс всё ещё кэшировался. Меньше контекста ещё и снижает отвлечение и улучшает качество ответа.", correct: true },
    { label: "Стоимость нормальная на $0.054; просто подними top_k до 40 ради лучшего recall.", misconception: "Удвоение top_k удваивает динамическую токен-стоимость и отвлечение, при этом recall за пределами первых нескольких релевантных чанков добавляет мало. Рычаг — меньше, но лучше чанков (rerank + порог), а не больше чанков." },
    { label: "Закэшируй полученный контекст, чтобы срезать стоимость.", misconception: "Этот контекст попроцессный, поэтому его нельзя закэшировать — кэшу нужен стабильный префикс. Выигрыш — сжать динамический контекст (rerank) и держать его вне кэшируемого префикса, а не кэшировать то, что меняется на каждом вызове." },
    { label: "Переключись на дешёвую модель, чтобы 18k токенов стоили меньше.", misconception: "Дешёвая модель снижает цену за токен, но ты всё равно шлёшь 16k в основном нерелевантного контекста на каждом вызове, вредя качеству и стоимости. Сначала чини retrieval: rerank до тех немногих чанков, что важны." },
  ]}
/>

<Recap lang="ru">
Баги композиции видны в коде: брейкпойнт кэша, поставленный после попроцессных RAG-чанков, не кэширует ничего переиспользуемого; стрим-цикл без ветки tool_use гасит UI, пока модель ждёт tool_result; agent loop без гейта step/budget вечно пересылает растущий транскрипт; а неранжированный top-20 retrieval впрыскивает ~16k некэшируемых токенов по ~$0.054 за вызов. Фикс в каждом случае структурный — перенести динамику, ветвиться по stop_reason, загейтить цикл, переранжировать контекст — а не ручка тюнинга. Теперь, когда читаешь PR, который задевает кэширование, стриминг или agent loop, первое, что проверяешь — шов: что следующий слой считает само собой и ломает ли этот диф это допущение?
</Recap>
