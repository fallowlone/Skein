---
concepts:
- seam-failure
- cache-prefix-invalidation
- streaming-state-machine
- agent-budget-cap
- rag-eval-coverage
- request-tracing
estMin: 14
lang: ru
level: senior
order: 8
prereqs: []
slug: quiz-short
sources:
- https://platform.claude.com/docs/en/build-with-claude/prompt-caching
- https://www.getmaxim.ai/articles/rag-evaluation-a-complete-guide-for-2025/
- https://dev.to/adamo_software/tool-use-api-design-for-llms-5-patterns-that-prevent-agent-loops-and-silent-failures-f29
status: ready
summary: "Промпты на свободное воспроизведение поперёк всего трека AI/LLM. Сначала ответь по памяти, затем открой модельный ответ и сверься на швах между слоями."
title: 'Сборка LLM-приложений: тест на воспроизведение'
track: ai-llm
unit: 08-putting-it-together
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";

<Hook>
Воспроизведение бьёт перечитывание. На каждый промпт восстанови полный ответ по памяти — поперёк всего трека, а не одного слоя — прежде чем открыть модельный. Усилие воспоминания и закрепляет рассуждение на уровне швов.
</Hook>

<Goal lang="ru">
Восстанови хребет трека: как caching, RAG, streaming, tool calls, agent loop и evals собираются вместе — и где каждая пара корректных слоёв ломается на шве.
</Goal>

<RetrievalDrawer
  client:load
  id="08-putting-it-together-short-retrieval"
  lang="ru"
  questions={[
    { q: "Почему у RAG-ассистента с длинным статическим system-промптом cache hit rate в проде может быть почти нулевым и как это починить?", a: "Prompt caching в Anthropic — это байт-в-байт prefix match: запрос раскладывается как tools → system → messages, и кэш переиспользует самый длинный идентичный префикс до твоего последнего брейкпойнта cache_control. Наивная сборка собирает один system-блок из правил + схем тулзов + полученных RAG-чанков. На демо с одним повторяющимся вопросом выглядит закэшированным, но в проде чанки разные на каждом запросе, поэтому префикс отличается с первого байта и read rate схлопывается к нулю — ты платишь полную input-цену за длинный префикс на каждом вызове, ровно там, где кэш был важен. Фикс — раскладка с учётом шва: держи статический префикс (правила, схемы тулзов) до брейкпойнта, а попроцессный RAG-контекст — после него, в messages. Статическая голова остаётся в кэше; перекодируется только дешёвый динамический хвост. Подъём TTL не помогает, потому что TTL управляет временем жизни, а не тем, матчится ли префикс." },
    { q: "Почему стримящийся ход — это конечный автомат, а не труба токенов, и что будет, если это игнорировать?", a: "Стримящийся ход не всегда кончается прозой: модель может выдать stop_reason: tool_use посередине. Если фронтенд трактует стрим как «токены до message_stop», он рендерит частичный текст и гасит спиннер — но реальное продолжение это другой запрос, который ты не отправил (tool_result, поданный обратно). Состояния: text, tool_use_requested, awaiting_tool_result, resumed; tool_use-стоп — переход, а не конец. Два полевых режима сбоя усугубляют: сетевой сбой может оборвать стрим с tool_use-стопом, но нулём tool_use-блоков, и агент тихо замирает; а падение посреди исполнения тулзы осиротит результат, и следующий запрос отвергается с 'unexpected tool_use_id found in tool_result block'. Каждый tool_use id должен матчиться ровно одним tool_result в следующем запросе, иначе API отвергает весь ход." },
    { q: "Почему token-бюджетные алерты — не то же, что enforcement бюджета для агента, и как выглядит enforcement?", a: "Алерты срабатывают после трат — к моменту, когда дашборд загорается, деньги уже ушли. Agent loop — это «вызови модель, запусти тулзы, подай результаты обратно, повтори», и плохой ввод не кооперируется: malformed-результат заставляет модель переформулировать и повторять, а поскольку каждая итерация пересылает весь растущий транскрипт, стоимость растёт суперлинейно. Цитируемый постмортем: четыре агента без капов крутились 11 дней и спалили $47 000, пока никто не заметил. Enforcement — жёсткий гейт перед каждым вызовом модели: проверь step ≤ MAX_STEPS и spent ≤ BUDGET_USD и откажи в вызове при достижении потолка, и dedupe по hash(tool_name, args), чтобы убить повторные вызовы. Бюджет-осознающий шлюз возвращает ошибку вместо проброса запроса; команды, добавившие это, обычно режут стоимость агента на 55–75%." },
    { q: "Почему зелёный офлайн eval-набор всё равно пропускает регрессию retrieval и как закрыть пробел?", a: "Generation-evals обычно оценивают модель на фиксированном golden-контексте — они замораживают полученные чанки, чтобы тест был детерминированным. Это значит, что набор никогда не гоняет живой retrieval-путь, поэтому изменение ретривера (новая embedding-модель, другой размер чанка, твик reranker'а) может ухудшить то, что достаётся, при том что каждый generation-eval остаётся зелёным, потому что оценивается против замороженного контекста. Фикс — end-to-end evals с живым retrieval: гоняй реальный ретривер на eval-запросах и оценивай ответ, построенный из того, что он реально вернул, плюс retrieval-специфичные метрики (recall@k, context precision), чтобы регрессия retrieval валила набор, а не уезжала тихо." },
    { q: "Сформулируй тезис «баг живёт в шве» и как он меняет дебаг собранного LLM-приложения.", a: "Каждый слой — cache, RAG, tools, streaming, agent loop, evals — может быть корректен по своему контракту и всё равно ломать систему, потому что композиция рождает новые режимы сбоя на границах. RAG изменил префикс, который кэш считал стабильным. tool_use-стоп изменил стрим, который рендерер считал прозой. Реальный ввод изменил цикл, который бюджет считал конечным. Ретривер изменился под eval-набором, считавшим контекст фиксированным. Ни один из них — не баг внутри компонента; каждый — баг между двумя корректными компонентами. Поэтому ты дебажишь не перетестированием кусков — ты трассируешь один реальный production-запрос насквозь и на каждой границе спрашиваешь, что следующий слой считал само собой, а предыдущий это только что изменил. Senior-навык на капстоуне — моделировать угрозы и стоимость всего пути запроса, а не строить лучшие отдельные куски." },
    { q: "Пройди порядок, в котором ты дебажил бы собранный ассистент, у которого после выката утроилась стоимость и ответы кажутся медленнее.", a: "Сначала трассируй ОДИН реальный production-запрос через все слои — cache, RAG, tools, stream, loop — а не перегоняй покомпонентные тесты, потому что они все всё ещё проходят. Второе: на каждой границе спрашивай, что следующий слой считал само собой, а этот слой только что изменил. Третье: ты находишь шов — попроцессный RAG-контекст сидит внутри кэшируемого префикса, поэтому hit rate почти ноль и ты платишь полную input-цену на каждом вызове (это и утроенная стоимость, и латентность). Четвёртое: чинишь структурно — переноси RAG-контекст за брейкпойнт cache_control, а статические правила оставь до него. Пятое: добавь end-to-end eval, который варьирует retrieval, чтобы тот же шов больше не регрессировал тихо. Инструментируй, трассируй шов, чини границу и фиксируй eval'ом — не крути ручки и не масштабируй железо, чтобы оплачивать сломанный шов." },
  ]}
/>

<Recap lang="ru">
Если смог восстановить каждый ответ по памяти, ты держишь хребет трека: caching — это байт-в-байт prefix match, поэтому динамический RAG-контекст — за брейкпойнтом; стрим — конечный автомат, где tool_use это переход, а каждый id должен быть спарен; agent loop'ам нужны enforced step/dollar-капы, а не алерты; evals обязаны гонять живой retrieval-путь, иначе регрессии retrieval уезжают зелёными. И мета-урок над всем: баг живёт в шве — трассируй один реальный запрос насквозь и моделируй весь поток. Теперь, когда встречаешь неожиданный всплеск стоимости или тихую регрессию после деплоя, ты ищешь проблему на границе между слоями, а не внутри компонента.
</Recap>
