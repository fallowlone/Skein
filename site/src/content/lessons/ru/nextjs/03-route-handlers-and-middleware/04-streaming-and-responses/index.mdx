---
concepts:
- nextjs-streaming
deepensInto: []
estMin: 18
lang: ru
lessonType: topic
level: senior
order: 4
prereqs: []
slug: "04-streaming-and-responses"
sources:
- https://nextjs.org/docs/app/building-your-application/routing/route-handlers
- https://nextjs.org/docs/app/building-your-application/routing/loading-ui-and-streaming
spiral: []
status: ready
summary: 'Route handlers стримят через ReadableStream и SSE — транспорт LLM-интерфейсов. Backpressure работает по pull: продюсер пишет, когда потребитель читает. Suspense стримит HTML; хендлеры — данные. Оба молча ломаются за буферизующими прокси и сжатием — шлите no-transform.'
title: 'Стриминговые ответы: ReadableStream, SSE для LLM-интерфейсов и прокси, которые их буферизуют'
track: "nextjs"
unit: "03-route-handlers-and-middleware"
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";
import DiagramFrame from "~/components/diagram/DiagramFrame.astro";
import PacketDot from "~/components/diagram/PacketDot.astro";

<Hook>
Команда выкатывает AI-чат. Локально он прекрасен: токены текут в UI как печать на машинке, время до первого токена около 300 мс. Они деплоятся за корпоративный nginx-реверс-прокси — и демо руководству показывает спиннер на девять секунд, после чего весь ответ влетает разом. Ошибок ноль. Хендлер стримил идеально; `curl -N` прямо в под это доказал. Виновник был двумя слоями выше: nginx-овский `proxy_buffering` (включён по умолчанию) собирал весь ответ, прежде чем переслать хоть байт, а gzip-модуль для верности делал то же самое — сжатию нужны куски, чтобы их сжимать, и оно их ждёт. Один заголовок из хендлера — `X-Accel-Buffering: no` плюс `Cache-Control: no-cache, no-transform` — вернул поток. Урок обобщается: стрим — это контракт между *каждым* хопом от вашего кода до экрана пользователя, и любой один буферизующий хоп — прокси, middleware сжатия, serverless-платформа, собирающая ответы целиком, — молча превращает ваш стриминговый UX обратно в батч.
</Hook>

<Crux>Хендлеры стримят данные через ReadableStream и SSE; Suspense стримит HTML. Буферизующие прокси и сжатие молча ломают оба потока.</Crux>

<Explanation>

## Возврат стрима из route handler

Route handler возвращает web `Response`, а телом `Response` может быть **`ReadableStream`** — в момент возврата Next немедленно отправляет заголовки и сбрасывает чанки по мере того, как ваш код их enqueue-ит. Никто не ждёт «завершения» функции:

```ts
// app/api/chat/route.ts — proxy an LLM, re-emitting tokens as SSE
export async function POST(request: Request) {
  const { prompt } = await request.json();
  const upstream = await llm.stream(prompt);          // async iterable of tokens
  const encoder = new TextEncoder();

  const stream = new ReadableStream({
    async pull(controller) {
      const { value, done } = await upstream.next();
      if (done) {
        controller.enqueue(encoder.encode("data: [DONE]\n\n"));
        controller.close();
        return;
      }
      controller.enqueue(encoder.encode(`data: ${JSON.stringify(value)}\n\n`));
    },
    cancel() {
      upstream.return?.();   // client disconnected — stop paying the LLM
    },
  });

  return new Response(stream, {
    headers: {
      "content-type": "text/event-stream",
      "cache-control": "no-cache, no-transform",
      "x-accel-buffering": "no",                     // nginx: do not buffer
    },
  });
}
```

Три детали в этом коде несут продакшен-вес. **Колбэк `pull`** — крючок backpressure, о нём ниже. **Колбэк `cancel`** срабатывает при отключении клиента; без него пользователь, закрывший вкладку на середине ответа, оставляет хендлер радостно потреблять (и оплачивать) остаток LLM-генерации. А **заголовки** — броня против девятисекундного спиннера из Hook.

<DiagramFrame label="Токены уходят в браузер по мере enqueue — поток это непрерывное течение чанков, а не готовый ответ" hue="var(--d-ai)" caption="Каждый чанк покидает хендлер в момент, когда pull() его кладёт в очередь: сначала заголовки, затем data:-фреймы верхом на chunked HTTP до парсера. Любой буферизующий хоп — nginx, gzip, собирающий ответ CDN — замораживает это течение обратно в батч.">
  <svg viewBox="0 0 360 120" width="360" height="120" style="display:block;margin:0 auto;max-width:100%;height:auto;">
    <rect x="10" y="44" width="56" height="32" rx="6" fill="var(--card-2)" stroke="var(--hairline-strong)" stroke-width="0.8" />
    <text x="38" y="63" text-anchor="middle" font-size="11" font-family="var(--font-body)" fill="var(--ink)">LLM</text>
    <rect x="146" y="44" width="68" height="32" rx="6" fill="var(--card-2)" stroke="var(--hairline-strong)" stroke-width="0.8" />
    <text x="180" y="59" text-anchor="middle" font-size="10.5" font-family="var(--font-mono)" fill="var(--ink)">route.ts</text>
    <text x="180" y="71" text-anchor="middle" font-size="9" font-family="var(--font-body)" fill="var(--muted)">pull() → SSE</text>
    <rect x="290" y="44" width="60" height="32" rx="6" fill="var(--card-2)" stroke="var(--hairline-strong)" stroke-width="0.8" />
    <text x="320" y="63" text-anchor="middle" font-size="11" font-family="var(--font-body)" fill="var(--ink)">Браузер</text>
    <path data-path d="M66 60 L290 60" fill="none" stroke="var(--hairline-2)" stroke-width="1.4" stroke-dasharray="4 4" />
    <PacketDot color="var(--d-ai)" durationMs={1400} size={8} />
    <PacketDot color="var(--d-ai)" durationMs={1400} delayMs={350} size={8} />
    <PacketDot color="var(--d-ai)" durationMs={1400} delayMs={700} size={8} />
  </svg>
</DiagramFrame>

## SSE: скучный, правильный протокол для потоков токенов

Для LLM-интерфейсов нужна конвенция фрейминга — у сырых чанков нет границ сообщений; токен может приехать разрезанным между двумя сетевыми чтениями. **Server-Sent Events** — устоявшийся ответ: content type `text/event-stream`, сообщения в рамке `data: <payload>\n\n`, в браузере читается `EventSource` (авто-реконнект, докачка через `Last-Event-ID`) или, как делают все AI SDK для POST-тел, чтением `response.body` с парсером. SSE выигрывает у WebSockets здесь потому, что поток токенов **однонаправленный**: без upgrade-рукопожатия, чистая HTTP-семантика (работает сквозь стандартные прокси и мультиплексирование HTTP/2), тривиально возобновляем и доступен через обычный `fetch`. WebSockets оправдывают свою сложность, только когда *клиент* должен часто слать сообщения по тому же соединению.

Сценарий отказа, уникальный для SSE, — **порча фрейминга**: если вы выдадите payload с сырым переводом строки, не разрезав его по `data:`-строкам, клиентский парсер молча обрежет сообщения — JSON-кодируйте каждый payload (как выше), и проблема исчезнет.

<Quiz
  id="04-streaming-and-responses-quiz-1"
  lessonSlug="04-streaming-and-responses"
  lang="ru"
  question="Хендлер LLM-чата стримит ответы, а облачный счёт показывает генерации полной длины даже для пользователей, закрывших вкладку через две секунды. Чего не хватает?"
  choices={[
    { label: "У ReadableStream нет обработчика cancel(), прерывающего upstream-вызов LLM — при отключении клиента рантайм отменяет стрим ответа, но upstream-генерация продолжает работать (и тарифицироваться), пока хендлер не пропагирует прерывание", correct: true },
    { label: "SSE-соединения не умеют детектировать отключения, эта цена неизбежна; переходите на WebSockets", misconception: "Детект отключения — не забота протокола: рантайм сигнализирует о нём, отменяя поток ответа. Хендлеру остаётся отреагировать на сигнал: cancel() (или AbortSignal, переданный наверх) останавливает генерацию. Перед WebSockets стоял бы идентичный вопрос пропагации." },
    { label: "Хендлер должен поставить заголовок Connection: close, чтобы стрим завершался, когда пользователь уходит", misconception: "Connection: close управляет переиспользованием keep-alive после завершения ответа — посреди стрима он не делает ничего и за клиентом не следит. Механизм разрыва посреди потока — отмена стрима, втекающая в ваш колбэк cancel." },
    { label: "Стримы удерживает кеш Next; force-dynamic на маршруте это починит", misconception: "Стриминговые ответы не кешируются — кеширование здесь ортогонально. Хвататься за конфиг кеша, когда симптом — осиротевшая upstream-работа, значит принять проблему жизненного цикла (пропагация прерывания) за проблему кеширования." },
  ]}
/>

## Backpressure: производить в темпе потребителя

У стрима есть продюсер (ваш LLM-прокси, курсор базы, читатель файла) и потребитель (соединение пользователя — часто медленный телефон на слабом Wi-Fi). Когда продюсер быстрее, разница накапливается **в памяти вашего сервера**. Web streams решают это **pull-моделью**: рантайм вызывает ваш `pull(controller)`, когда во внутренней очереди есть место (отслеживается `controller.desiredSize`, освобождается по мере того, как потребитель вычитывает байты), — корректно написанный источник генерирует *только по запросу*. Антипаттерн — обход модели: колбэк `start()` с циклом, который enqueue-ит целый скан таблицы, или прокачка быстрого upstream в медленного клиента без ожидания writer-а:

```ts
// WRONG: ignores backpressure — the whole cursor lands in memory
start(controller) {
  for await (const row of hugeCursor) controller.enqueue(encode(row));
}
// RIGHT: pull() reads one item per request from the consumer side
async pull(controller) {
  const { value, done } = await hugeCursor.next();
  done ? controller.close() : controller.enqueue(encode(value));
}
```

Для потоков токенов ставки низки — токены крошечные, а люди читают медленно. Для эндпоинтов экспорта данных (CSV-дампы, хвосты логов) backpressure — это разница между ровным профилем памяти в 30 МБ и OOM-убитым подом, когда один клиент с медленным соединением запросил экспорт на 2 ГБ. Если вы компонуете трансформации, `pipeThrough`/`pipeTo` пропагируют backpressure за вас; утекает он в самописных циклах.

## Два стриминга, одно слово — и строй буферизаторов

Next.js стримит в двух разных смыслах, и их смешение путает отладку. **Suspense-стриминг страниц** — это App Router, прогрессивно рендерящий HTML: оболочка уходит сразу, рендерятся фолбэки `loading.tsx`/`<Suspense>`, а опоздавшие серверные компоненты приезжают чанками вне порядка, которые inline-скрипты вставляют на место. Его единица — *UI*; протоколом владеет фреймворк. **Хендлерный стриминг данных** — этот урок — это вы возвращаете байты, которые потребляет *клиентский код*: SSE-события, NDJSON-строки, CSV-ряды. Его единица — *данные*; фрейминг, заголовки и отмена — ваши. Они делят транспорт (chunked HTTP) и потому делят врага: **всё, что буферизует**.

Строй, хоп за хопом. **nginx**: `proxy_buffering on` по умолчанию — отключается per-response заголовком `X-Accel-Buffering: no`. **Сжатие**: middleware gzip/brotli накапливает вход для окон сжатия; `Cache-Control: no-transform` велит благовоспитанным посредникам не перекодировать, а собственный слой сжатия обязан исключить `text/event-stream`. **CDN**: некоторые буферизуют или даже кешируют стриминговые ответы, если не пометить их `no-cache`/`private`. **Serverless-платформы**: классическая AWS Lambda за API Gateway буферизовала весь ответ by design — стримингу нужна явно стриминго-способная инфраструктура (Lambda response streaming или edge/Node-серверы, которые flush-ат). Диагностика, разрезающая всё это: `curl -N` прямо в приложение (стримит? ваш код в порядке), затем в каждый слой наружу, пока стрим не перестанет стримить, — этот хоп и есть виновник.

<Quiz
  id="04-streaming-and-responses-quiz-2"
  lessonSlug="04-streaming-and-responses"
  lang="ru"
  question="Хендлер CSV-экспорта стримит результат на 2 ГБ. С быстрыми клиентами память ровная. Когда экспорт запрашивает клиент на медленном соединении, память пода растёт до OOM. Какой механизм отказал?"
  choices={[
    { label: "Backpressure: продюсер enqueue-ит строки, не дожидаясь, пока потребитель их вычитает (например, цикл в start() по всему курсору), и разница скоростей между БД и медленным клиентом накапливается в очереди стрима на сервере", correct: true },
    { label: "В ответе нет X-Accel-Buffering: no, и nginx буферизует 2 ГБ в памяти пода", misconception: "Буферизация прокси происходит в памяти или на диске прокси, а не в поде приложения — и её симптом — задержанная доставка, а не OOM на стороне продюсера. Рост, повторяющий темп медленного клиента, указывает внутрь хендлера: темп производства игнорирует темп потребления." },
    { label: "Стримы больше 1 ГБ нужно резать на несколько ответов; HTTP столько не стримит", misconception: "У chunked transfer encoding нет потолка размера — многогигабайтные стриминговые ответы рутинны. Выдуманный лимит протокола уводит внимание от настоящего дефекта: хендлер генерирует быстрее, чем соединение вычитывает." },
    { label: "Медленный клиент слишком долго держит соединение с БД, и драйвер буферизует результат", misconception: "Долго держать курсор открытым — ровно то, для чего стриминг задуман, и курсоры читают инкрементально именно чтобы не материализовывать результат. Буферизация происходит после БД — в очереди хендлера, — когда enqueue бежит впереди потребителя вместо того, чтобы управляться pull()-ом." },
  ]}
/>

</Explanation>

<FlowDiagram
  label="Путь токена от LLM до пикселя — и три хопа, где стримы молча умирают"
  hue="var(--d-ai)"
  perRow={3}
  nodes={[
    { id: "llm", label: "Upstream LLM", sub: "итератор токенов" },
    { id: "handler", label: "route.ts", sub: "pull() → SSE-фреймы" },
    { id: "gzip", label: "Сжатие", sub: "буферизует — исключить SSE" },
    { id: "proxy", label: "nginx / CDN", sub: "X-Accel-Buffering: no" },
    { id: "client", label: "Парсер браузера", sub: "читает response.body" },
    { id: "cancel", label: "Вкладка закрыта", sub: "cancel() прерывает LLM" },
  ]}
  edges={[
    { from: "llm", to: "handler", label: "await next()" },
    { from: "handler", to: "gzip", label: "chunked HTTP" },
    { from: "gzip", to: "proxy", label: "no-transform" },
    { from: "proxy", to: "client", label: "сброс вживую" },
    { from: "client", to: "cancel", label: "отключение" },
  ]}
  caption="На стрим должен согласиться каждый хоп: хендлер тянет в темпе потребителя (backpressure), сжатию и прокси велено не буферизовать (no-transform, X-Accel-Buffering), а отключения пропагируются назад через cancel(), чтобы брошенные генерации перестали стоить денег."
/>

<KeyTakeaway>Верните ReadableStream — и Next сбрасывает чанки по мере enqueue; оформляйте токены как SSE с no-cache, no-transform и X-Accel-Buffering: no. Backpressure работает по pull — производите по запросу, иначе медленные клиенты устроят OOM. Реализуйте cancel(), чтобы останавливать upstream. Suspense стримит HTML; хендлеры — данные; буферизующие хопы убивают оба.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="04-streaming-and-responses-retrieval"
  lang="ru"
  questions={[
    { q: "Каковы три продакшен-критичные части стримингового SSE-хендлера и что ломается без каждой?", a: "(1) Фрейминг и заголовки: text/event-stream с фреймами data: <json>\\n\\n — JSON-кодируйте payload-ы, иначе сырые переводы строк портят клиентский парсер; Cache-Control: no-cache, no-transform и X-Accel-Buffering: no не дают слоям сжатия, nginx и CDN сбуферизовать стрим в батч-ответ. (2) cancel(): срабатывает при отключении клиента; без него upstream-вызов LLM продолжает генерировать и тарифицироваться после закрытия вкладки — пропагируйте прерывание. (3) Производство по pull: рантайм зовёт pull(controller), когда очередь вычитана (desiredSize), и продюсер совпадает с темпом потребителя; цикл в start(), enqueue-ящий всё подряд, игнорирует backpressure и копит разницу скоростей в памяти сервера — ровные 30 МБ против OOM на экспорте 2 ГБ медленному клиенту. Компонуйте через pipeThrough/pipeTo — пропагация backpressure бесплатно." },
    { q: "Разграничьте Suspense-стриминг и хендлерный стриминг и приведите диагностику строя буферизаторов.", a: "Suspense-стриминг — это фреймворк, прогрессивно рендерящий HTML страниц: сначала оболочка, фолбэки loading.tsx, опоздавшие серверные компоненты чанками вне порядка, вставляемые inline-скриптами, — единица UI, протоколом владеет Next. Хендлерный стриминг — ваш route handler возвращает сырые байты (SSE, NDJSON, CSV), которые парсит клиентский код, — фрейминг, заголовки, отмена ваши. Оба едут на chunked HTTP, поэтому оба умирают на любом буферизующем хопе: nginx proxy_buffering (включён по умолчанию — гасится X-Accel-Buffering: no), middleware gzip/brotli (исключайте text/event-stream; шлите no-transform), CDN, собирающие стримы, и serverless-платформы, буферизующие ответы целиком, если они явно не stream-capable. Диагностика: curl -N прямо в приложение, затем в каждый слой наружу — хоп, где стриминг прекращается, и есть виновник." },
  ]}
/>

<Recap lang="ru">
Route handler стримит, возвращая Response с телом-ReadableStream: заголовки уходят немедленно, чанки сбрасываются по мере enqueue. Для LLM-интерфейсов конвенция фрейминга — SSE: text/event-stream, строки data:, разделённые пустыми строками, JSON-кодированные payload-ы, чтобы вложенные переводы строк не портили парсер, — выбранная вместо WebSockets потому, что поток токенов однонаправлен, а чистая HTTP-семантика переживает прокси, мультиплексируется по HTTP/2 и возобновляется через Last-Event-ID. Три части хендлера несут продакшен-вес: производство по pull, где рантайм запрашивает данные по мере вычитывания (desiredSize), и медленный телефон не заставит под накопить двухгигабайтный экспорт в памяти — разница между ровным профилем и OOM-kill, причём pipeThrough/pipeTo пропагируют backpressure автоматически, а самописные enqueue-циклы его теряют; cancel(), который срабатывает при отключении клиента и обязан прервать upstream-вызов, иначе закрытые вкладки дожигают LLM-бюджет до конца каждой генерации; и заголовочная броня — Cache-Control: no-cache, no-transform плюс X-Accel-Buffering: no. Эта броня существует потому, что стрим — контракт с каждым хопом между кодом и экраном: nginx буферизует по умолчанию, middleware сжатия копит окна перед выдачей, CDN могут собирать или кешировать стримы, а классические serverless-гейтвеи буферизовали ответы целиком — любой из них молча превращает стриминг в батч, поэтому демо с девятисекундным спиннером провалилось, хотя curl -N в под стримил отлично. Держите два стриминга раздельно: Suspense-стриминг — фреймворк прогрессивно доставляет HTML страницы чанками вне порядка; хендлерный стриминг — ваш протокол данных; но транспорт, враги и похоповая curl-диагностика у них общие.
</Recap>
