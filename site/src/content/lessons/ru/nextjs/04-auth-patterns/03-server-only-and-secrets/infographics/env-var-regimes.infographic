infographic compare-binary-horizontal-underline-text-vs
data
  title Два режима env-переменных: одна падает, другая утекает навсегда
  desc Переименование в один префикс, превращающее падение в вечную утечку
  compares
    - label Серверная
      children
        - label process.env.SECRET — серверная переменная
        - label Читается в рантайме, на сервере
        - label В клиентском бандле: просто undefined
        - label Падение или тихий сбой, но не утечка
        - label Ротация в любой момент, без пересборки
    - label Публичная
      children
        - label NEXT_PUBLIC_SECRET — публичная переменная
        - label Зашита строковым литералом при сборке
        - label Читаема каждым посетителем в devtools
        - label Ротация требует пересборки и редеплоя
        - label Отгружена однажды значит скомпрометирована
theme
  palette #14b8a6 #f43f5e
