---
concepts:
- misconfiguration
- public-bucket
- exposed-service
- guardrails
- preventive-control
deepensInto: []
estMin: 15
lang: ru
lessonType: topic
level: middle
order: 3
prereqs: []
slug: 03-misconfiguration-detection
sources:
- https://owasp.org/www-project-cloud-native-application-security-top-10/
- https://csrc.nist.gov/pubs/sp/800/137/final
spiral: []
status: ready
summary: 'Публичный бакет и открытый порт Postgres — два пробоя, которые случаются
  снова и снова. Детект находит их постфактум; превентивные ограждения делают
  мисконфиг структурно невозможным. Зрелый инженер строит оба и ведёт превенцией.'
title: Детект мисконфигураций
track: security-cloud
unit: 04-cloud-detection-and-posture
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
На непропорционально большую долю облачных инцидентов приходятся всего два пробоя, и оба почти комично скучны. Первый: у бакета с экспортами клиентов ACL переключают в публичный — иногда инженер делится демо-ассетом, иногда так выставлен дефолт в Terraform, который никто не прочитал, — и исследователь (или краулер) находит его по словарю имён бакетов за один вечер. Второй: ingress security-group открывает `0.0.0.0/0` на порту 5432, и управляемый Postgres, который должен общаться только с прикладным слоем, теперь отвечает всему интернету, где боты подбора учёток найдут его за часы. Ни 0-day, ни вредоноса, ни хитрой цепочки — просто конфиг, который говорит «да» там, где должен сказать «нет». Детект сообщает, что это случилось. Зрелый ход — сделать так, чтобы это *не могло* случиться.
</Hook>

К концу урока ты будешь знать, как обнаружить публичный бакет или открытый сервис в масштабе, почему один детект всегда проигрывает гонку и какие превентивные ограждения превращают мисконфиг из находки в невозможность.

<Crux>Детект находит публичный бакет после экспозиции; превентивные ограждения делают его структурно невозможным. Веди превенцией.</Crux>

<Explanation>

## Две экспозиции, которые реально пробивают

Мисконфигурация — это ресурс, чьё *эффективное* состояние выдаёт доступ, которого не должно быть, и слово «эффективное» здесь несущее. Случай публичного бакета редко сводится к одному переключателю: достижимость S3-бакета — это разрешённая сумма его ACL, политики бакета, настройки Block Public Access на уровне аккаунта и любой политики access-point поверх. Инженер может оставить политику бакета закрытой и всё равно выставить наружу каждый объект, перещёлкнув один ACL объекта, потому что самый разрешающий грант побеждает, если только явный deny или ограждение Block Public Access его не перебьёт. Вот почему «мы проверили политику, она в порядке» — это не то же самое, что «бакет приватный».

Случай открытого сервиса — та же форма на другом ресурсе. Управляемая БД, кэш Redis, внутренняя админ-панель, узел Elasticsearch — каждый должен жить за приватной подсетью или жёстким allowlist. Мисконфиг — это ingress security-group (или правило NSG, или правило файрвола) с источником `0.0.0.0/0`, дотягивающийся до чувствительного порта: 5432, 3306, 6379, 9200, 27017. Ущерб не теоретический: достижимую из интернета базу находят массовые сканеры — Shodan, Censys и флот ботов подбора учёток — обычно за часы после выхода в онлайн, а не за недели. Решает окно экспозиции, а не изощрённость эксплойта.

## Как на самом деле работает детект

Детект механически — это обход: принять read-only роль в каждом аккаунте, вызвать describe/list API (`GetBucketPolicyStatus`, `GetPublicAccessBlock`, `DescribeSecurityGroups`, `DescribeNetworkAcls`), разрешить *эффективную* экспозицию каждого ресурса и выдать находку, когда она пересекает правило. Трудная часть — не вызовы API, а корректный расчёт эффективного доступа. Наивное правило, читающее только политику бакета, упускает путь через ACL объекта; наивное правило по security-group, флагующее ingress `0.0.0.0/0`, упускает, что инстанс может стоять в приватной подсети без маршрута к интернет-шлюзу, делая открытое правило безвредным — это ложноположительное срабатывание, которое, повторённое тысячу раз, приучает команду игнорировать инструмент.

<FlowDiagram
  label="Путь экспозиции и детекта публичного бакета: изменение в консоли или Terraform переключает ACL объекта в публичный, эффективный доступ бакета теперь открыт, хотя политика выглядит закрытой, краулер или сканер дотягивается до объектов, а периодический обход CSPM в конце концов выдаёт находку — но разрыв между изменением и обходом и есть окно экспозиции, до которого первым доберётся атакующий."
  perRow={5}
  hue="var(--d-crypto)"
  nodes={[
    { id: "change", label: "ACL переключён", sub: "консоль / дефолт TF" },
    { id: "effective", label: "Эффективно: публично", sub: "ACL бьёт политику" },
    { id: "scan", label: "Краулер находит", sub: "словарь бакетов" },
    { id: "sweep", label: "Обход CSPM", sub: "периодично, часы спустя" },
    { id: "block", label: "Block Public Access", sub: "превенция: не откроется" },
  ]}
  edges={[
    { from: "change", to: "effective" },
    { from: "effective", to: "scan", label: "окно экспозиции" },
    { from: "effective", to: "sweep", label: "детект, поздно" },
    { from: "change", to: "block", label: "фикс" },
  ]}
  caption="Детект (периодический обход) всегда бежит наперегонки с атакующим и иногда проигрывает, потому что окно экспозиции открывается в момент переключения ACL. Block Public Access на уровне аккаунта — превентивный контроль: он отклоняет само изменение, поэтому «публично» никогда не становится достижимым состоянием."
/>

Поэтому зрелый инженер считает задержку детекта ключевой метрикой. Инструмент CSPM, обходящий ресурсы раз в 24 часа, оставляет 24-часовое окно, в котором публичный бакет полностью открыт, а ты об этом не знаешь. Событийный детект — привязка событий об изменении конфигурации (CloudTrail, правила AWS Config, EventBridge) к оценке ресурса в момент его изменения — схлопывает это окно с часов до секунд. Но даже секунды — это окно, а окно — это шанс. Что приводит к настоящему уроку.

## Превенция бьёт детект

Детект отвечает на «совершил ли кто-то эту ошибку?» постфактум. Превентивный контроль — ограждение — отвечает «эту ошибку нельзя совершить» до факта. Канонический пример — Block Public Access для S3 на уровне аккаунта: с ним *никакая* политика бакета, ACL или грант объекта не сделают ни один бакет публичным, точка. Вызов API, который создал бы публичный грант, отклоняется. Окна экспозиции нет, потому что «публично» никогда не было достижимым состоянием. Сравни с правилом детекта, которое находит публичные бакеты, и раннбуком, который их закрывает: подход на детекте имеет окно, зависит от того, прочтёт ли кто-то алерт, и срабатывает снова каждый раз, когда новый инженер делает ту же ошибку.

Механизм, делающий превенцию масштабируемой по всей организации, — это **политика-ограждение**, навязанная над аккаунтом: AWS Service Control Policies (SCP) на уровне Organization, Azure Policy с эффектом `deny` или ограничения GCP Organization Policy. Они оцениваются *до* создания или изменения ресурса и отклоняют действие напрямую — `s3:PutBucketPublicAccessBlock`, который отключил бы блок; правило ingress с `0.0.0.0/0` на порту БД; назначение публичного IP инстансу базы. Ограждение на уровне организации нельзя обойти инженеру с admin в отдельном аккаунте — ровно то свойство, которого детекту не хватает.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Слой</th>
        <th class="border border-border px-3 py-2 text-left">Когда действует</th>
        <th class="border border-border px-3 py-2 text-left">Контроль публичного бакета</th>
        <th class="border border-border px-3 py-2 text-left">Контроль открытого сервиса</th>
        <th class="border border-border px-3 py-2 text-left">Окно?</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">Превентивное ограждение (орг)</td>
        <td class="border border-border px-3 py-2">До применения изменения</td>
        <td class="border border-border px-3 py-2">SCP запрещает отключать Block Public Access</td>
        <td class="border border-border px-3 py-2">SCP запрещает <code>0.0.0.0/0</code> на портах БД</td>
        <td class="border border-border px-3 py-2">Нет</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Гейт IaC / пайплайна</td>
        <td class="border border-border px-3 py-2">На plan / до apply</td>
        <td class="border border-border px-3 py-2">Policy-as-code (OPA, Checkov) валит plan</td>
        <td class="border border-border px-3 py-2">Скан plan отклоняет открытый ingress</td>
        <td class="border border-border px-3 py-2">Изменения в консоли проскальзывают</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Событийный детект</td>
        <td class="border border-border px-3 py-2">Секунды после изменения</td>
        <td class="border border-border px-3 py-2">Config-правило срабатывает на смену ACL</td>
        <td class="border border-border px-3 py-2">Правило срабатывает на мутацию SG</td>
        <td class="border border-border px-3 py-2">Секунды</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Периодический обход CSPM</td>
        <td class="border border-border px-3 py-2">Часы / раз в сутки</td>
        <td class="border border-border px-3 py-2">Обход эффективного доступа находит его</td>
        <td class="border border-border px-3 py-2">Межаккаунтный скан находит его</td>
        <td class="border border-border px-3 py-2">Часы — сутки</td>
      </tr>
    </tbody>
  </table>
</div>

<Inset kind="why" lang="ru">
Почему бы не включить орг-ограждение везде и не отказаться от детекта совсем? Потому что превентивные контроли грубы, а легитимные исключения существуют — публичный бакет статического сайта, origin для CDN, намеренно смотрящий в интернет сервис за WAF. Сплошной SCP, запрещающий все публичные бакеты, ломает их, поэтому команды вырезают исключения, а исключения — это места, где прячется дрейф. Детект — страховочная сетка, ловящая то, что проскользнуло через путь-исключение или слой, который ты забыл оградить. Зрелая позиция — эшелонированная защита: превенция как основной контроль, детект как подстраховка, исходящая из того, что у превенции есть дыра, — никогда одно вместо другого.
</Inset>

## Как зрелый инженер выстраивает контроли по порядку

Перед лицом «у нас постоянно появляются публичные бакеты» рефлекс джуна — купить сканер и написать раннбук. Рефлекс зрелого инженера — спросить, почему это состояние вообще достижимо, и продвинуть контроль настолько влево и настолько вверх, насколько он дотянется: орг-ограждение, запрещающее опасное действие, затем гейт IaC, чтобы ошибка падала на ревью с привязанным владельцем кода, затем событийный детект для консольных путей, которых гейт не видит, затем периодический обход как финальная подстраховка для всего, что верхние слои упустили. Каждый нижний слой существует лишь чтобы ловить сбои слоя над ним. Детект сам по себе никогда не ответ — это признание, что превенция несовершенна, развёрнутое осознанно как сетка, а не как пол.

<TradeoffMatrix
  id="03-misconfig-detection-tradeoff-1"
  lessonSlug="03-misconfiguration-detection"
  lang="ru"
  prompt="Бакет с PII клиентов постоянно переключают в публичный инженеры, делящиеся демо-ассетами. Данные никогда не должны быть публичными. Выбери контроль, который реально закрывает дыру."
  options={[
    { name: "Включить Block Public Access на уровне аккаунта через орг-SCP, который ещё и запрещает его отключать", summary: "Сделать публичность недостижимой.", correct: true, justification: "Это превентивное ограждение, навязанное над аккаунтом: с Block Public Access на уровне аккаунта никакой ACL, политика бакета или грант объекта не сделают бакет публичным, а SCP не даёт админу аккаунта отключить блок. «Публично» никогда не становится достижимым состоянием, поэтому окна экспозиции нет, и та же ошибка следующего инженера отклоняется на API, а не ловится постфактум." },
    { name: "Запускать ежедневный скан CSPM, который находит публичные бакеты и пейджит on-call для их закрытия", summary: "Детект и ремедиация.", justification: "Детект необходим как подстраховка, но не может быть здесь основным контролем: ежедневный обход оставляет до 24-часового окна экспозиции, зависит от того, прочтёт ли кто-то алерт и среагирует, и срабатывает снова каждый раз, когда новый инженер делает ту же ошибку. Дыра остаётся открытой между изменением и ремедиацией, а для PII, до которого дотянется краулер, это ровно то окно, что тебя пробивает." },
    { name: "Переименовать бакеты в случайные GUID, чтобы их нельзя было найти по словарю", summary: "Спрятать имя.", justification: "Это security through obscurity. Имена бакетов утекают в логах, коде, конфигах CDN и прошлых ответах; публичный бакет с неугадываемым именем всё равно публичен и всё равно индексируется, как только URL где-то всплывёт. Эффективный доступ не меняется — грант по-прежнему открыт, — лишь откладывается обнаружение, а «отложить обнаружение» — не контроль." },
    { name: "Поставить WAF перед бакетом, чтобы фильтровать анонимные чтения", summary: "Накладной фильтр.", justification: "WAF спереди не меняет эффективный доступ бакета: объект по-прежнему публично читаем на своём родном S3-эндпоинте, по которому сканер бьёт напрямую, полностью обходя WAF. Навешивание периметрового фильтра на ресурс, чей собственный грант открыт, ничего не чинит у источника и даёт ложную уверенность." },
  ]}
/>

<Quiz
  id="03-misconfig-detection-quiz-1"
  lessonSlug="03-misconfiguration-detection"
  lang="ru"
  question="Почему расчёт «эффективного доступа» — трудная часть детекта публичного бакета, а не просто чтение политики бакета?"
  choices={[
    { label: "Достижимость — это разрешённая сумма ACL, политики бакета, Block Public Access аккаунта и политики access-point — ACL объекта может выставить бакет наружу, даже когда политика выглядит закрытой", correct: true },
    { label: "Потому что политики бакетов зашифрованы и сканер не может их прочитать", misconception: "Политики бакетов читаемы через GetBucketPolicy с read-only ролью; они не зашифрованы от аккаунта. Сложность — в сведении нескольких источников разрешений к одному эффективному ответу, а не в расшифровке." },
    { label: "Потому что у S3 нет API для чтения текущего ACL", misconception: "API есть для каждого слоя (GetBucketAcl, GetBucketPolicyStatus, GetPublicAccessBlock). Задача — корректно их скомбинировать в эффективный доступ, а не обойти отсутствующий API." },
    { label: "Потому что только AWS видит, публичен ли бакет, а твоё тулирование — нет", misconception: "Твоя read-only роль сама может разрешить эффективный доступ. Суть в том, что проверка по одному источнику (только политика) упускает путь через ACL объекта, давая ложноотрицательные срабатывания." },
  ]}
/>

<Quiz
  id="03-misconfig-detection-quiz-2"
  lessonSlug="03-misconfiguration-detection"
  lang="ru"
  question="Postgres, достижимый из интернета на `0.0.0.0/0:5432`, создан через консоль в 14:00. Твой CSPM обходит раз в сутки в 02:00. В чём реальный риск и устойчивый фикс?"
  choices={[
    { label: "Массовые сканеры найдут его за часы, но детект не сработает до 02:00 — фикс это орг-ограждение, запрещающее 0.0.0.0/0 на портах БД, чтобы правило вообще нельзя было создать", correct: true },
    { label: "Реального риска нет — Postgres требует пароль, так что открытый порт нормален", misconception: "Открытый порт выставляет сервис под подбор учёток и любую слабость аутентификации/CVE; массовые сканеры находят открытые порты БД за часы. «Нужен пароль» — не сетевой контроль." },
    { label: "Риск низкий, потому что обход рано или поздно его поймает", misconception: "Разрыв ~12 часов между 14:00 и обходом в 02:00 и есть окно экспозиции, до которого первым доберётся атакующий. Запоздалый детект не закрывает окно; это делает превенция." },
    { label: "Просто сократи обход до часового — и проблема решена", misconception: "Час всё равно оставляет окно, всё равно зависит от срабатывания ремедиации и срабатывает снова на каждую новую ошибку. Превентивное ограждение убирает окно целиком, запрещая изменение." },
  ]}
/>

<DragOrder
  id="03-misconfig-detection-drag-1"
  lessonSlug="03-misconfiguration-detection"
  lang="ru"
  prompt="Упорядочь контроли мисконфигурации от действующего раньше всех (наибольшее плечо) к действующему последним (финальная подстраховка):"
  items={[
    { id: "a", label: "Орг-превентивное ограждение (SCP / Org Policy) — запрещает опасное действие до его применения" },
    { id: "b", label: "Гейт IaC / пайплайна (policy-as-code) — валит plan на ревью, с владельцем кода" },
    { id: "c", label: "Событийный детект — срабатывает за секунды после изменения в консоли/SDK, которого гейт не видит" },
    { id: "d", label: "Периодический обход CSPM — суточный межаккаунтный скан как финальная подстраховка" },
  ]}
  correctOrder={["a", "b", "c", "d"]}
/>

</Explanation>

<KeyTakeaway>Публичный бакет и открытый порт базы — два скучных пробоя, которые продолжают побеждать, потому что их решает окно экспозиции, а не изощрённость эксплойта. Детект всегда бежит наперегонки с атакующим и иногда проигрывает. Веди превентивными ограждениями (орг-SCP / Block Public Access), делающими плохое состояние недостижимым, затем наслаивай гейты IaC, событийный детект и периодический обход как подстраховки. Превенция как пол, детект как сетка.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="03-misconfig-detection-retrieval"
  lang="ru"
  questions={[
    { q: "Пройди по тому, как ты бы обнаруживал публичный S3-бакет в масштабе, и объясни, почему «эффективный доступ» делает наивный детект ненадёжным.", a: "Детект в масштабе — это read-only обход: принять read-only IAM-роль в каждом аккаунте, вызвать describe/list API (GetBucketAcl, GetBucketPolicyStatus, GetPublicAccessBlock и эквиваленты для access-point) и для каждого бакета разрешить, публичен ли он *эффективно*. Ловушка в том, что достижимость — это разрешённая сумма четырёх источников разрешений: ACL бакета, политики бакета, настройки Block Public Access на уровне аккаунта и любой политики access-point, — и самый разрешающий грант побеждает, если только явный deny или Block Public Access его не перебьёт. Наивное правило, читающее только политику бакета, отрапортует «приватно» для бакета, чьи объекты выставлены наружу через ACL объекта, — это ложноотрицательное срабатывание, скрывающее живой пробой. Поэтому корректный детект обязан считать эффективный доступ по всем источникам, а не проверять один слой. Глубже: даже корректный детект периодичен и потому запаздывает — между изменением и обходом есть окно экспозиции, до которого дотянется краулер, поэтому детект — подстраховка, а не основной контроль." },
    { q: "Почему зрелый инженер ведёт превентивными ограждениями вместо детекта для проблемы публичного бакета и открытого сервиса, и как здесь выглядит эшелонированная защита?", a: "Потому что детект отвечает на «совершил ли кто-то эту ошибку?» постфактум, тогда как превентивное ограждение делает ошибку несовершаемой. Block Public Access для S3 на уровне аккаунта означает, что никакой ACL, политика или грант объекта не сделают бакет публичным — вызов API отклоняется, поэтому окна экспозиции нет, и идентичная ошибка следующего инженера запрещается у источника, а не ловится после. Навязанное как орг-уровневая Service Control Policy (или Azure Policy deny / ограничение GCP Org Policy), ограждение к тому же нельзя отключить админу аккаунта — свойство, которого детекту структурно не хватает. Подвох в том, что ограждения грубы, а легитимные исключения существуют (публичный бакет сайта, намеренно смотрящий в интернет сервис), и исключения — место, где прячется дрейф, — поэтому превенцию запускают не *вместо* детекта. Эшелонированная защита выстраивает слои по плечу: орг-ограждение запрещает опасное действие, гейт IaC валит plan на ревью с владельцем кода, событийный детект за секунды ловит консольные пути, которых гейт не видит, а периодический обход — финальная подстраховка для всего, что проскользнуло через исключение. Превенция — пол; детект — сетка, развёрнутая с пониманием, что у пола есть дыры." },
  ]}
/>

<Recap lang="ru">
Два облачных пробоя, которые продолжают случаться, скучны намеренно: бакет хранилища, переключённый в публичный, и база или админ-сервис, выставленные на `0.0.0.0/0`. Оба решаются окном экспозиции, а не мастерством эксплойта — открытый порт базы массовые сканеры находят за часы, а публичный бакет краулер по словарю — за один вечер. Обнаружение их в масштабе — это read-only обход, который обязан разрешить *эффективный* доступ (ACL плюс политика плюс Block Public Access плюс политика access-point), потому что проверка по одному слою даёт ложноотрицательные срабатывания, скрывающие живую экспозицию. Но детект всегда периодичен и потому запаздывает, бежа наперегонки с атакующим через окно экспозиции. Зрелый ход — сделать плохое состояние недостижимым: вести орг-уровневыми превентивными ограждениями — Block Public Access, SCP, запрещающими `0.0.0.0/0` на портах баз, — которые отклоняют изменение до его применения и не обходятся админом аккаунта, затем наслаивать гейты IaC, событийный детект и периодический обход как подстраховки. Превенция — пол; детект — сетка, которую ты разворачиваешь, зная, что у пола есть прорехи. Так что когда кто-то говорит «у нас постоянно публичные бакеты», твой первый вопрос не «какой сканер» — а «почему публичность всё ещё достижимое состояние и какое ограждение делает её недостижимой?»
</Recap>
