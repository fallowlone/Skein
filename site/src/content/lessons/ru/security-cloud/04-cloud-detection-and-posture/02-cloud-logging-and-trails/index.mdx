---
concepts:
- cloudtrail
- audit-trail
- cloud-logging
- tamper-resistance
- detection
deepensInto: []
estMin: 15
lang: ru
lessonType: topic
level: middle
order: 2
prereqs: []
slug: 02-cloud-logging-and-trails
sources:
- https://owasp.org/www-project-cloud-native-application-security-top-10/
- https://csrc.nist.gov/pubs/sp/800/137/final
spiral: []
status: ready
summary: 'Журнал управляющего слоя — единственная запись о том, кто что сделал в
  облаке, и первое, что удаляет атакующий. Если он может его править, у вас лог, а
  не доказательство. Защита от подделки превращает одно в другое.'
title: Облачное логирование и журналы аудита
track: security-cloud
unit: 04-cloud-detection-and-posture
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
Звонок о взломе приходит в 02:00. Кто-то поднял парк GPU-инстансов в регионе, которым вы не пользуетесь, на счёт от ключа сервисного аккаунта. Первый инстинкт верен: вытащить журнал аудита управляющего слоя и восстановить, какая именно личность сделала какой API-вызов, с какого IP и в каком порядке. Вы открываете журнал — а нужных часов нет. Не повреждены, не пропуски из-за сбоя. *Удалены.* У того же ключа, что запустил майнеры, были права `cloudtrail:StopLogging` и `cloudtrail:DeleteTrail`, и самое первое, что он сделал, ещё не трогая вычисления, — выключил камеру. У вас больше нет хронологии инцидента. У вас есть догадка.
</Hook>

К концу урока ты будешь понимать, почему журнал аудита — источник истины обо всём, что происходит в облаке, и какие именно меры не дают атакующему, добравшемуся до него, переписать историю.

<Crux>Журнал аудита — единственная запись о действиях в облаке, и он бесполезен как доказательство, едва атакующий сможет его править.</Crux>

<Explanation>

## Что на самом деле записывает журнал аудита

В любом облаке два слоя логов, и их постоянно путают. **Логи слоя данных** фиксируют трафик *через* твои сервисы: HTTP-запросы к балансировщику, чтения объекта, запросы к базе. **Логи управляющего слоя** — AWS CloudTrail, GCP Cloud Audit Logs, Azure Activity Log — фиксируют *управляющие* действия против самого облачного API: кто вызвал `CreateUser`, кто привязал политику, кто изменил security group, кто проротировал ключ, кто удалил бакет. Когда ты восстанавливаешь взлом, важен именно журнал управляющего слоя, потому что любое повышение привилегий и любой акт закрепления — это под капотом последовательность аутентифицированных API-вызовов.

Одно событие CloudTrail — это структурированная запись с полями, которые реально нужны расследователю: `eventTime`, `eventName` (вызванный API), `userIdentity` (IAM-принципал, включая сессию принятой роли и исходный аккаунт), `sourceIPAddress`, `userAgent`, параметры запроса и ответ. Прочитай эти пять полей по нескольким тысячам событий — и ты ответишь на единственный важный в 02:00 вопрос: *какая личность это сделала, когда, откуда и чего коснулась.* Поэтому журнал и называют источником истины — это единственное место, где само облако, а не твоё приложение, свидетельствует о том, что произошло.

Подвох — в **покрытии по умолчанию**. Из коробки журнал обычно логирует *управляющие* события, но **не события данных**: атакующий, вычитывающий каждый объект из S3-бакета, не порождает ни одной записи управляющего слоя, пока ты явно не включишь логирование событий данных — а оно отключено по умолчанию отчасти потому, что объёмно и тарифицируется поштучно. Зрелый рефлекс: точно знать, что твой журнал захватывает, а что — нет, *до* инцидента, потому что «мы предполагали, что S3 GetObject логируется» — открытие, которое не хочется делать, когда эксфильтрация уже идёт.

## Почему журнал — первая цель атакующего

Подумай как противник один абзац. Ты скомпрометировал учётку. Твои цели — повысить привилегии, закрепиться и действовать: майнить крипту, выкачивать данные, шантажировать аккаунт. Каждое из этих действий оставляет в журнале следы API-вызовов. Поэтому ход с наибольшим рычагом, который можно сделать *перед* всем шумным, — ослепить защитника: остановить журнал, удалить его или прервать доставку в место назначения. Это не теория — «уклонение от защиты через подрыв логирования» — поименованная, каталогизированная техника в облачной матрице MITRE ATT&CK именно потому, что это стандартный дебютный ход. Журнал, который атакующий может выключить, — это датчик дыма с доступным выключателем: он защищает ровно до момента, когда становится нужен.

<FlowDiagram
  label="Гонка атакующего против журнала: скомпрометированная учётка сначала пытается ослепить журнал, остановив или удалив логирование; если журнал доставляет в неизменяемое append-only хранилище в отдельном аккаунте, попытка убийства проваливается и срабатывает алерт на API-вызов StopLogging, так что и доказательства, и детектирование выживают."
  perRow={3}
  hue="var(--d-crypto)"
  nodes={[
    { id: "cred", label: "Учётка скомпрометирована", sub: "valid session" },
    { id: "blind", label: "Ослепить журнал", sub: "StopLogging" },
    { id: "act", label: "Эскалация / эксфил", sub: "сама атака" },
    { id: "immut", label: "Неизменяемый сток", sub: "отд. аккаунт" },
    { id: "alert", label: "Алерт срабатывает", sub: "на StopLogging" },
    { id: "evidence", label: "Хронология цела", sub: "доказательства" },
  ]}
  edges={[
    { from: "cred", to: "blind", label: "первый ход" },
    { from: "blind", to: "act", label: "если вышло" },
    { from: "blind", to: "immut", label: "не вышло" },
    { from: "blind", to: "alert", label: "API залогирован" },
    { from: "immut", to: "evidence" },
  ]}
  caption="Решающая мера — доставка в append-only хранилище в аккаунте, до которого атакующий не дотянется, плюс алерт на сам акт отключения логирования. Тогда «выключить камеру» и проваливается, и громко себя выдаёт."
/>

## Защита от подделки: превращаем лог в доказательство

Разница между логом и *доказательством* — в том, мог ли записываемый его изменить. До цели доводят четыре меры, наложенные слоями.

**Доставляй в неизменяемое append-only хранилище.** Журнал должен писать в объектное хранилище с политикой object-lock / удержания в режиме compliance, чтобы даже администратор аккаунта не мог удалить или перезаписать объект до истечения срока удержания. Append-only — то свойство, что бьёт «правку истории»: можно добавлять записи, но никогда не переписывать.

**Размести сток в отдельном аккаунте.** Это единственный архитектурный ход с наибольшей ценностью. У скомпрометированного принципала в рабочем аккаунте вообще не должно быть IAM-прав над архивом логов. Централизуй журналы со всех аккаунтов в один выделенный, наглухо запертый *логирующий аккаунт*, единственная работа которого — принимать и хранить. Теперь, чтобы ослепить журнал, нужно скомпрометировать *второй*, куда меньший и куда строже контролируемый аккаунт — ты поднял стоимость уклонения на порядок.

**Проверяй целостность криптографически.** Включи валидацию целостности файлов журнала (CloudTrail подписывает каждый доставленный файл и выпускает дайджест, связанный хеш-цепочкой). На этапе расследования ты сможешь доказать, что файл не изменялся после доставки, — превращая «мы думаем, что это полно» в «мы можем продемонстрировать, что это полно», а это и делает журнал пригодным в форензном или юридическом контексте.

**Алертируй на сам акт подделки.** API-вызовы, отключающие логирование — `StopLogging`, `DeleteTrail`, сужающий охват `PutEventSelectors`, удаление стока логов, — сами являются логируемыми событиями. Повесь на них алерт в реальном времени. Первый ход атакующего становится его самым громким. Это же и ответ на премису непрерывного мониторинга из NIST SP 800-137: мера, которую выставили один раз и никогда не наблюдают, имеет неизвестный статус; состояние безопасности нужно *наблюдать* на постоянной основе, и «логирование всё ещё включено?» — самое базовое, что стоит наблюдать.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Мера</th>
        <th class="border border-border px-3 py-2 text-left">Какую атаку блокирует</th>
        <th class="border border-border px-3 py-2 text-left">Чего стоит</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">Append-only / object-lock сток</td>
        <td class="border border-border px-3 py-2">Удаление или перезапись прошлых записей</td>
        <td class="border border-border px-3 py-2">Затраты на хранение; нельзя рано чистить</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Отдельный логирующий аккаунт</td>
        <td class="border border-border px-3 py-2">Доступ к логам через взлом рабочего аккаунта</td>
        <td class="border border-border px-3 py-2">Кросс-аккаунт-настройка + управление org</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Валидация целостности файлов</td>
        <td class="border border-border px-3 py-2">Тихие правки после доставки</td>
        <td class="border border-border px-3 py-2">Шаг проверки на этапе расследования</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Алерт на StopLogging / DeleteTrail</td>
        <td class="border border-border px-3 py-2">Тихое выключение камеры</td>
        <td class="border border-border px-3 py-2">Конвейер алертов; борьба с ложными</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Мультирегион + логи событий данных</td>
        <td class="border border-border px-3 py-2">Действия в немониторируемом регионе / слое данных</td>
        <td class="border border-border px-3 py-2">Поштучная тарификация; больше объёма</td>
      </tr>
    </tbody>
  </table>
</div>

<Inset kind="why" lang="ru">
Почему бы просто не хранить логи вечно и не возиться со сложностью object-lock? Потому что «сохранён» и «неизменяем» — разные свойства. Обычный бакет, в который пишет рабочий аккаунт, может быть и *опустошён* любым, у кого есть право на удаление в нём, — а учётка, владеющая рабочим аккаунтом, часто имеет такое право или может выдать его себе сама. Удержание без неизменяемости защищает от случайного удаления и отказа диска; против мотивированного инсайдера или атакующего с правами админа оно не даёт ничего. Object-lock в режиме compliance — та часть, что говорит *даже root не укоротит срок*, а это и есть весь смысл, когда модель угроз включает скомпрометированного администратора.
</Inset>

## Читать журнал хорошо, а не только хранить

Защита от подделки держит запись честной; полезной саму по себе она её не делает. Журнал, к которому ты никогда не делаешь запросов, — это просто дорогое хранилище. Две зрелые привычки закрывают разрыв. Первая — **централизуй и нормализуй**: сливай журнал каждого аккаунта в одно запросопригодное хранилище, чтобы расследователь делал один запрос по всей организации, а не двенадцать. Вторая — **выстрой базовую линию нормы, чтобы аномалия бросалась в глаза**: новый IAM-пользователь, созданный в 03:00, `console login` из страны, где вы не работаете, внезапный всплеск `RunInstances` в неиспользуемом регионе — это сигнал только тогда, когда ты знаешь, как выглядит скучное. Это ровно тот цикл, что NIST SP 800-137 называет непрерывным мониторингом: собрать, сравнить с известной базовой линией, отреагировать, повторить — статус безопасности это то, что ты продолжаешь измерять, а не галочка, поставленная на старте.

<TradeoffMatrix
  id="02-cloud-logging-trails-tradeoff-1"
  lessonSlug="02-cloud-logging-and-trails"
  lang="ru"
  prompt="Твой журнал управляющего слоя сейчас доставляет в S3-бакет в том же рабочем аккаунте, с включённым версионированием. Атакующий, скомпрометировавший роль администратора в этом аккаунте, хочет стереть последние 6 часов активности. Какое изменение реально его остановит?"
  options={[
    { name: "Централизовать журнал в отдельный, наглухо запертый логирующий аккаунт и применить удержание object-lock к архиву", summary: "Убрать из досягаемости + сделать неизменяемым.", correct: true, justification: "Это закрывает оба реальных сбоя. Сток в отдельном аккаунте означает, что у скомпрометированного администратора рабочего аккаунта вообще нет IAM-прав над архивом, так что удалить его он не может; object-lock в режиме compliance означает, что даже администратор логирующего аккаунта не перезапишет и не укоротит срок удержания до его истечения. Подделка теперь требует компрометации второго, куда меньшего аккаунта — ровно того кратного роста стоимости, что и должна давать защита от подделки." },
    { name: "Оставить бакет на месте, но включить версионирование и lifecycle-политику на 1 год", summary: "Просто хранить дольше.", justification: "Версионирование и lifecycle дают долговечность против случайного удаления и отказа диска, но администратор рабочего аккаунта всё ещё может навсегда удалить версии (или опустошить бакет), потому что у него есть право удаления в нём. Удержание — не неизменяемость; это защищает от ошибок, а не от противника в твоей модели угроз." },
    { name: "Зашифровать бакет логов клиентским ключом KMS", summary: "Шифрование на диске.", justification: "Шифрование защищает конфиденциальность логов, если хранилище украдут, — но не делает ничего против администратора, который просто удалит объекты через обычный API. Атакующему не нужно читать логи; ему нужно, чтобы их не стало, и он всё ещё может это сделать." },
    { name: "Поднять порог алертов, чтобы тебя будили только на события высокой важности", summary: "Прикрутить алерты.", justification: "Это шиворот-навыворот: снижает видимость как раз тех административных действий (StopLogging, DeleteTrail, массовые удаления), которые важнее всего ловить, и ничего не делает с защитой самих хранимых записей от подделки. Запись всё ещё можно стереть; ты просто реже об этом услышишь." },
  ]}
/>

<Quiz
  id="02-cloud-logging-trails-quiz-1"
  lessonSlug="02-cloud-logging-and-trails"
  lang="ru"
  question="Атакующий компрометирует учётку и сразу вызывает `StopLogging` на журнале, прежде чем сделать что-либо ещё. Почему это стандартный дебютный ход?"
  choices={[
    { label: "Каждое последующее действие (эскалация, закрепление, эксфильтрация) оставляет в журнале следы API, поэтому ослепление журнала первым убирает у защитника хронологию самой атаки", correct: true },
    { label: "Остановка логирования выдаёт атакующему дополнительные IAM-права", misconception: "StopLogging влияет на видимость, а не на авторизацию. Он не выдаёт никаких новых прав — его ценность для атакующего лишь в том, что он убирает запись о том, что атакующий сделает дальше." },
    { label: "Это требуется, прежде чем атакующий сможет запустить любые вычислительные ресурсы", misconception: "Отключение логирования никак не является предусловием для запуска ресурсов. Атакующий отключает логирование, чтобы скрыть эти запуски, а не потому, что это технически нужно для их выполнения." },
    { label: "Остановка журнала шифрует существующие записи, чтобы их нельзя было прочитать", misconception: "StopLogging останавливает будущую доставку; он не шифрует и не меняет уже сохранённое. Угроза касается записи в будущем, а прошлых записей — только если хранилище не неизменяемо." },
  ]}
/>

<Quiz
  id="02-cloud-logging-trails-quiz-2"
  lessonSlug="02-cloud-logging-and-trails"
  lang="ru"
  question="Что даёт валидация целостности файлов журнала (подписанные дайджесты, связанные хеш-цепочкой), чего не даёт одно лишь удержание?"
  choices={[
    { label: "Возможность доказать, что доставленный файл журнала не изменялся после доставки, — превращая «мы думаем, что это полно» в «мы можем продемонстрировать, что это полно»", correct: true },
    { label: "Она мешает журналу когда-либо быть остановленным", misconception: "Валидация целостности доказывает, что файлы не менялись после доставки; она ничего не делает, чтобы остановить StopLogging или DeleteTrail. Это отдельная мера — алерт на эти API-вызовы." },
    { label: "Она шифрует логи, чтобы атакующие не могли их прочитать", misconception: "Валидация целостности — про обнаружение подделки, а не про конфиденциальность. Она подписывает и хеш-связывает файлы, делая изменение доказуемым, но не скрывает их содержимое." },
    { label: "Она автоматически удаляет логи старше срока удержания", misconception: "Это вопрос lifecycle/удержания. Валидация целостности лишь свидетельствует, что файлы не изменены, — она не имеет отношения к расписанию удаления." },
  ]}
/>

<DragOrder
  id="02-cloud-logging-trails-drag-1"
  lessonSlug="02-cloud-logging-and-trails"
  lang="ru"
  prompt="Расставь шаги, чтобы сделать журнал управляющего слоя действительно защищённым от подделки, от самого широкого архитектурного хода к самому тонкому детектированию:"
  items={[
    { id: "a", label: "Доставлять журнал в отдельный, наглухо запертый логирующий аккаунт" },
    { id: "b", label: "Применить object-lock / удержание в режиме compliance, чтобы записи были append-only" },
    { id: "c", label: "Включить валидацию целостности файлов журнала (подписанные, хеш-связанные)" },
    { id: "d", label: "Алертировать в реальном времени на StopLogging / DeleteTrail / удаление стока" },
  ]}
  correctOrder={["a", "b", "c", "d"]}
/>

</Explanation>

<KeyTakeaway>Журнал аудита управляющего слоя — источник истины о том, кто что сделал в облаке, и первая цель атакующего. Сделай из него доказательство, а не просто лог: append-only хранилище, отдельный логирующий аккаунт, валидация целостности и алерт в реальном времени на любого, кто пытается его выключить.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="02-cloud-logging-trails-retrieval"
  lang="ru"
  questions={[
    { q: "Почему журнал аудита управляющего слоя — источник истины при расследовании облачного взлома, и какой пробел есть в его покрытии по умолчанию?", a: "Логи управляющего слоя (CloudTrail, GCP Cloud Audit Logs, Azure Activity Log) фиксируют управляющие действия против самого облачного API — CreateUser, AttachPolicy, RunInstances, DeleteBucket — каждое как структурированное событие с eventTime, eventName, userIdentity (IAM-принципал и сессия принятой роли), sourceIPAddress, userAgent и параметрами запроса/ответа. Поскольку любое повышение привилегий и акт закрепления под капотом — последовательность аутентифицированных API-вызовов, журнал — единственное место, где само облако свидетельствует, кто что сделал, когда, откуда и к какому ресурсу, — поэтому он источник истины, а не логи приложения. Пробел в покрытии по умолчанию — события данных: из коробки журнал логирует управляющие события, но не чтения/записи слоя данных (например, S3 GetObject), потому что они объёмны и тарифицируются поштучно. Так что атакующий, вычитывающий каждый объект из бакета, может не породить ни одной записи управляющего слоя, пока логирование событий данных не включено явно, — и это надо знать до инцидента, а не выяснять во время эксфильтрации." },
    { q: "Какие четыре наложенных слоями меры превращают журнал из лога, который атакующий может править, в доказательство, и какая из них самая ценная?", a: "Первая — доставлять в неизменяемое append-only хранилище: object-lock / удержание в режиме compliance, чтобы даже админ не удалил и не перезаписал запись до истечения удержания (одного удержания мало; бакет с правом записи можно опустошить). Вторая — разместить сток логов в отдельном выделенном логирующем аккаунте, чтобы у скомпрометированного принципала рабочего аккаунта вообще не было IAM-прав над архивом, — это самый ценный ход, потому что он вынуждает атакующего скомпрометировать второй, куда меньший и строже контролируемый аккаунт, поднимая стоимость уклонения на порядок. Третья — включить валидацию целостности файлов (подписанные, хеш-связанные дайджесты), чтобы доказать, что доставленный файл не менялся, — превращая «мы думаем, что это полно» в «мы можем это продемонстрировать». Четвёртая — алертировать в реальном времени на сами API подделки — StopLogging, DeleteTrail, сужающий охват PutEventSelectors, удаление стока, — так как они логируемые события, и ослепляющий ход атакующего становится его самым громким. Последняя мера — практическое выражение непрерывного мониторинга NIST SP 800-137: состояние безопасности нужно наблюдать постоянно, и «логирование всё ещё включено?» — самое базовое для наблюдения." },
  ]}
/>

<Recap lang="ru">
Журнал аудита управляющего слоя — CloudTrail и его собратья — источник истины при любом облачном взломе: он фиксирует каждый управляющий API-вызов с личностью, временем, исходным IP и параметрами, нужными расследователю, — именно поэтому его отключение и есть стандартный дебютный ход атакующего (каталогизированная техника уклонения от защиты в MITRE ATT&CK). Журнал, который атакующий может остановить или удалить, — это датчик дыма с выключателем. Четыре наложенных слоями меры делают из него доказательство: доставка в неизменяемое append-only хранилище; изоляция стока в отдельном запертом логирующем аккаунте, до которого скомпрометированный рабочий аккаунт не дотянется; криптографическая валидация целостности, делающая правки после доставки доказуемыми; и алерт в реальном времени на StopLogging / DeleteTrail, чтобы ослепляющий ход сам себя выдавал. Следи за пробелом покрытия по умолчанию — события данных вроде чтений S3 отключены. И помни цикл NIST SP 800-137: хранить журнал недостаточно — за ним надо продолжать наблюдать, ведь мера, которую не наблюдаешь, имеет неизвестный статус. В следующий раз, поднимая облачный аккаунт, твой первый вопрос: если бы учётку, которую я только что выдал, скомпрометировали, смогла бы она стереть собственные следы?
</Recap>
