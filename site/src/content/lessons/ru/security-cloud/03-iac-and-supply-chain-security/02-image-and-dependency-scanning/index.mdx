---
concepts:
- dependency-scanning
- sca
- cve
- image-scanning
- false-positive
deepensInto: []
estMin: 15
lang: ru
lessonType: topic
level: middle
order: 2
prereqs: []
slug: 02-image-and-dependency-scanning
sources:
- https://slsa.dev/
- https://owasp.org/www-project-devsecops-guideline/
spiral: []
status: ready
summary: 'Сканеры находят каждый CVE в зависимостях и базовых образах, но большинство не достигает эксплуатируемого кода. Достижимость, серьёзность и наличие фикса — то, чем сеньор превращает стену красного в короткий ранжированный список.'
title: Сканирование образов и зависимостей
track: security-cloud
unit: 03-iac-and-supply-chain-security
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
В пятницу ты подключаешь Trivy в CI. Первый же пуш в понедельник роняет сборку: **312 уязвимостей, 41 критическая**. Образ — это стоковый `node:20` плюс десяток npm-пакетов, которые ты не писал. На выходных никто не трогал криптографию; ты просто включил фонарик, который всё это время светил на кучу долга. Соблазн — поставить `--severity HIGH,CRITICAL --exit-code 0` и забыть: оставить свет, но выдернуть сигнализацию. Это неверный рефлекс — и он же самый частый. Настоящая работа не в том, чтобы *найти* CVE; сканер делает это бесплатно. Работа — решить, какие из 312 реально достижимы и эксплуатируемы в твоём работающем сервисе, потому что 280 из них — никогда.
</Hook>

К концу урока ты будешь понимать, как SCA- и image-сканеры порождают эту стену красного и как достижимость, наличие фикса и серьёзность превращают её в короткий ранжированный список, по которому можно действовать, не теряя чувствительности к алертам.

<Crux>Найти CVE бесплатно; навык сеньора — ранжировать по достижимости и наличию фикса, чтобы реальный риск не утонул в шуме.</Crux>

<Explanation>

## Два сканера, одна проблема инвентаризации

Образ контейнера — это стопка из двух цепочек поставок ПО, и сканировать нужно обе. **Слой ОС** — это всё, что приносит базовый образ: `glibc`, `openssl`, `zlib`, сотни пакетов Debian или Alpine, большинство из которых ты никогда не вызываешь. **Слой приложения** — твои прямые и транзитивные зависимости: `package-lock.json`, `go.sum` или `requirements.txt`, тянущие код, который ты действительно зовёшь. Обе цепочки сканируются одинаково, и стоит точно понять как — потому что механизм объясняет шум.

Сканер **не** анализирует твой код на ошибки. Он строит *ведомость материалов* — каждый пакет и точную версию — а затем сопоставляет этот список с базой уязвимостей (NVD плюс дистрибутивные ленты вроде Debian Security Tracker, GitHub Advisories и языковые экосистемы). Вывод — это арифметика множеств: `installed_packages ∩ known_vulnerable_versions`. Именно из-за этого пересечения свежий образ `node:20` сообщает о десятках CVE в первый же день — ты установил ОС с известно-уязвимыми пакетами, и сканер сообщает факт, а не суждение. В руководстве OWASP DevSecOps это называется **Software Composition Analysis (SCA)**, и это самый дешёвый высокоценный контроль, который можно добавить в пайплайн. И это же причина, по которой его включение ощущается как погребение под завалом.

## Почему стена красного — в основном шум

Та же арифметика множеств, что делает сканирование дешёвым, делает его шумным. CVE из `installed_packages ∩ known_vulnerable_versions` говорит, что *уязвимый код присутствует*. Он **не** говорит, что код загружен, вызван или достижим с подконтрольным атакующему вводом. Этот разрыв — источник почти всего шума, и у него три различимые формы, которые сеньор учится распознавать.

<FlowDiagram
  label="Воронка от сырого вывода сканера к реальному риску: сканер сообщает каждый CVE, где установлена уязвимая версия, но большинство не достигает эксплуатируемого кода. Фильтрация по достижимости (вызывается ли уязвимая функция?), наличию фикса (есть ли пропатченная версия?) и контексту эксплуатации (серьёзность в твоём развёртывании) схлопывает 312 находок до примерно 8, требующих действия в этом спринте."
  perRow={5}
  hue="var(--d-crypto)"
  nodes={[
    { id: "all", label: "312 CVE найдено", sub: "пакет ∩ база CVE" },
    { id: "reach", label: "Достижим?", sub: "код реально зовут" },
    { id: "fix", label: "Есть фикс?", sub: "патч существует" },
    { id: "ctx", label: "Эксплуатируем тут?", sub: "твоё развёртывание" },
    { id: "act", label: "~8 на действие", sub: "ранжир, в спринт" },
  ]}
  edges={[
    { from: "all", to: "reach", label: "не используется" },
    { from: "reach", to: "fix" },
    { from: "fix", to: "ctx", label: "или won't-fix" },
    { from: "ctx", to: "act", label: "реальный риск" },
  ]}
  caption="Левую коробку сканер даёт бесплатно. Каждая стрелка вправо — человеческое суждение, которое сканер сделать не может: достижимость, наличие фикса и контекст развёртывания превращают 312 находок в те ~8, что действительно требуют работы."
/>

Три формы шума:

- **Недостижимый код.** CVE в NIS-резолвере `glibc`, в пути разбора шрифтов библиотеки изображений, которую ты используешь только для миниатюр, или в подкоманде CLI, которую сервис никогда не вызывает. Уязвимые байты лежат на диске; уязвимый *путь* мёртв. Анализ достижимости — достигает ли хоть одна цепочка вызовов от твоей точки входа уязвимой функции? — это крупнейший единичный редуктор шума, и новейшие сканеры (и стандарт **VEX**, Vulnerability Exploitability eXchange) существуют именно чтобы записывать «присутствует, но не затронут».
- **Фикса ещё нет (или `won't-fix`).** Дистрибутивы рутинно помечают низкосерьёзные CVE как `won't-fix`, потому что фикс не стоит риска регрессии. Ронять сборку на CVE без апстрим-патча означает, что сборка никогда не позеленеет — ты построил храповик, который только затягивается.
- **Серьёзность ≠ эксплуатируемость в контексте.** CVSS 9.8 в dev-зависимости, вырезанной tree-shaking из продакшен-бандла, — это не 9.8 *для тебя*. CVSS оценивает уязвимость в абстракции; только ты знаешь, экспонирована ли она.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Находка</th>
        <th class="border border-border px-3 py-2 text-left">CVSS</th>
        <th class="border border-border px-3 py-2 text-left">Достижим?</th>
        <th class="border border-border px-3 py-2 text-left">Фикс?</th>
        <th class="border border-border px-3 py-2 text-left">Действие сеньора</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">RCE в JSON-либе, вызываемой на каждый запрос</td>
        <td class="border border-border px-3 py-2">9.8</td>
        <td class="border border-border px-3 py-2">Да</td>
        <td class="border border-border px-3 py-2">Патч есть</td>
        <td class="border border-border px-3 py-2">Бампнуть сейчас; это и есть алерт</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">RCE в dev-CLI, вырезанной из прода</td>
        <td class="border border-border px-3 py-2">9.8</td>
        <td class="border border-border px-3 py-2">Нет</td>
        <td class="border border-border px-3 py-2">Патч есть</td>
        <td class="border border-border px-3 py-2">Бампнуть попутно; не гейтить</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">DoS в NIS-пути glibc, который не вызывают</td>
        <td class="border border-border px-3 py-2">7.5</td>
        <td class="border border-border px-3 py-2">Нет</td>
        <td class="border border-border px-3 py-2">won't-fix</td>
        <td class="border border-border px-3 py-2">Подавить через VEX + обоснование</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Обход аутентификации в твоём web-фреймворке</td>
        <td class="border border-border px-3 py-2">8.1</td>
        <td class="border border-border px-3 py-2">Да</td>
        <td class="border border-border px-3 py-2">Патча нет</td>
        <td class="border border-border px-3 py-2">Компенсирующий контроль + отслеживать</td>
      </tr>
    </tbody>
  </table>
</div>

## Где ставить гейт и на что он гейтит

Понимание форм шума позволяет поставить гейт правильно. Неверный дизайн — тот, к которому тянутся первым, — это «ронять сборку, если есть хоть одна CRITICAL». Он даёт три сбоя на первой же неделе: сборки, которые не могут позеленеть (CVE без фикса), усталость от алертов (так что единственный реальный RCE проскальзывает мимо вместе с 40 недостижимыми) и неизбежный `--exit-code 0`, тихо отключающий весь контроль. Тихо обезвреженный сканер *хуже*, чем его отсутствие, потому что позволяет верить, что ты защищён.

Гейт сеньора собирает три предиката: **серьёзность И наличие фикса И не-подавлено**. Ронять сборку только на исправимой, high-или-critical, неподавленной находке — потому что это то множество, где падение сборки реально меняет чьё-то поведение (бампни версию). Всё остальное идёт в другой поток: отслеживать, подавить с записанной причиной или принять. Цель — гейт, где красная сборка всегда означает «сделай вот это конкретное», а не «иди подтверди 40 вещей, с которыми ничего не сделать».

<Inset kind="why" lang="ru">
Зачем сканировать реестр непрерывно, а не только на сборке? Потому что пересечение между твоей ведомостью материалов и базой CVE двигается *под тобой*. Образ, чисто отсканированный в понедельник, может иметь три новые критические в четверг — не потому что образ изменился, а потому что изменилась база, когда опубликовали новый CVE против пакета, который ты уже отгрузил. Знаменитый пример — Log4Shell: миллионы артефактов за ночь перешли из «чистых» в «критический RCE» при нулевом изменении кода. Сканирование на сборке отвечает на «чисто ли сейчас?»; сканирование реестра/рантайма отвечает на «чисто ли ещё то, что мы *запускаем*?» — и только второе ловит CVE, раскрытый после деплоя.
</Inset>

## Резать вход, а не только фильтровать выход

Самый дешёвый способ выиграть игру в триаж — иметь меньше того, что триажить. Два структурных хода сжимают вход ещё до всякой фильтрации:

- **Минимальные базовые образы.** Образ `node:20` весит ~1,1 ГБ и тащит весь userland Debian — shell, `apt`, десятки библиотек, которых твоё приложение не касается, каждая — потенциальный CVE. Образ `distroless` или `alpine` несёт только рантайм. Замена `node:20` на `gcr.io/distroless/nodejs20` рутинно роняет находки слоя ОС со ~150 до единиц — потому что ты удалил пакеты, а не подавил их CVE. Меньше пакетов — единственный фикс, удаляющий *сам уязвимый код*, а не спорящий о его достижимости.
- **Пинить и обновлять осознанно.** Lock-файлы делают сканы воспроизводимыми (ты сканируешь ровно то, что отгружается), а ровный темп обновления зависимостей (Dependabot/Renovate) означает, что ты применяешь маленькие патчи непрерывно, а не упираешься в обрыв на 200 CVE при следующем вынужденном мажорном апгрейде.

<TradeoffMatrix
  id="02-image-dep-scanning-tradeoff-1"
  lessonSlug="02-image-and-dependency-scanning"
  lang="ru"
  prompt="Скан CI сообщает 312 CVE (41 критическая) на только что включённом пайплайне. Выбери ответ, который сеньор отгрузит первым."
  options={[
    { name: "Гейтить сборку только на исправимых + high/critical + неподавленных находках; остальное — в отслеживаемый бэклог", summary: "Составной гейт.", correct: true, justification: "Это делает так, что красная сборка всегда означает конкретное выполнимое действие: патч есть, серьёзность реальна, и никто это не пересмотрел-и-подавил. Неисправимые и недостижимые находки получают другой поток (отслеживать/подавить с обоснованием), а не блокируют пайплайн. Единственный вариант, сохраняющий гейт осмысленным И оставляющий команде возможность отгружать." },
    { name: "Поставить --severity HIGH,CRITICAL --exit-code 0, чтобы скан сообщал, но никогда не ронял сборку", summary: "Обезвредить гейт.", justification: "Самый частый рефлекс и ловушка. Сканер, который никогда не может уронить сборку, — это дашборд, который никто не читает; единственный реальный эксплуатируемый RCE проскальзывает с 40 недостижимыми. Тихо отключённый контроль хуже отсутствия, потому что производит ложную уверенность." },
    { name: "Ронять сборку на любой CRITICAL, без исключений, начиная с сегодня", summary: "Жёсткий гейт на всё.", justification: "Звучит строго, ломается сразу. У многих критических нет апстрим-фикса (won't-fix) или они недостижимы, так что сборка никогда не позеленеет, и первый ход команды — отключить гейт целиком. Гейт, который нельзя удовлетворить, приучает людей обходить его." },
    { name: "Переключить базовый образ на distroless и считать дело сделанным", summary: "Урезать и стоп.", justification: "Минимальные базовые образы — реальный и мощный ход: они удаляют CVE слоя ОС у источника, но касаются только слоя ОС. Твои зависимости приложения (npm/pip/go-пакеты, которые ты реально зовёшь) не тронуты, а именно там чаще всего живут достижимые эксплуатируемые CVE. Необходимо, но недостаточно." },
  ]}
/>

<Quiz
  id="02-image-dep-scanning-quiz-1"
  lessonSlug="02-image-and-dependency-scanning"
  lang="ru"
  question="Образ, прошедший скан чисто в понедельник, в четверг показывает 3 новые критические без изменения кода или зависимостей. Что произошло?"
  choices={[
    { label: "Изменилась база уязвимостей: опубликованы новые CVE против пакетов, уже бывших в образе", correct: true },
    { label: "Сканер флакает и выдал false positive — перезапусти его", misconception: "Перезапуск не поможет; находка реальна. Результаты скана — это join против живой ленты CVE, поэтому тот же артефакт законно меняет статус по мере раскрытия новых CVE. Именно поэтому реестр сканируют непрерывно, а не только на сборке." },
    { label: "Кто-то запушил новый образ; для фиксированного дайджеста результаты не меняются", misconception: "Результаты абсолютно меняются для фиксированного дайджеста. Ведомость материалов заморожена, но база CVE, с которой она сопоставляется, — нет: Log4Shell за ночь перевёл миллионы неизменных артефактов из чистых в критические." },
    { label: "Космический шум в подсчёте CVSS; игнорируй внутринедельные изменения", misconception: "Это вновь раскрытые уязвимости, а не дрожь подсчёта. Игнорировать их — так пост-деплойный CVE (самый опасный вид, ведь он уже работает) остаётся непропатченным." },
  ]}
/>

<Quiz
  id="02-image-dep-scanning-quiz-2"
  lessonSlug="02-image-and-dependency-scanning"
  lang="ru"
  question="CVSS 9.8 RCE найден в dev-only build-инструменте, вырезанном tree-shaking из продакшен-бандла. Должен ли он ронять продакшен-гейт деплоя?"
  choices={[
    { label: "Нет — он недостижим в развёрнутом артефакте; подави его с записанной причиной и пропатчь попутно", correct: true },
    { label: "Да — 9.8 это критично, и все критические обязаны блокировать, независимо от контекста", misconception: "CVSS оценивает уязвимость в абстракции, а не в твоём развёртывании. Критическая в коде, который никогда не отгружается в прод, — не 9.8 для тебя. Гейт на ней приучает команду отключать гейт." },
    { label: "Нет — и удали находку из отчёта полностью, чтобы она больше не появлялась", misconception: "Тихо удалять неверно; правильный ход — записанное подавление (например, VEX «не затронут») с обоснованием, чтобы решение было аудируемым и пережило следующий скан, а не молча переразбиралось заново." },
    { label: "Да — dev-зависимости всегда продакшен-риск, ведь сборки бегут в CI", misconception: "Экспозиция в CI — реальная, но отдельная забота с другим радиусом поражения; она не делает пакет достижимым в развёрнутом сервисе. Смешивать это — так каждая находка становится блокером деплоя." },
  ]}
/>

<DragOrder
  id="02-image-dep-scanning-drag-1"
  lessonSlug="02-image-and-dependency-scanning"
  lang="ru"
  prompt="Упорядочь воронку триажа от сырого вывода сканера до отгружаемого решения:"
  items={[
    { id: "a", label: "Сканер сопоставляет твою ведомость материалов с базой CVE → сырые находки" },
    { id: "b", label: "Отбрось находки, чей уязвимый код недостижим от точки входа" },
    { id: "c", label: "Отдели исправимые находки от won't-fix / патча-ещё-нет" },
    { id: "d", label: "Ранжируй исправимое+достижимое множество по серьёзности в контексте развёртывания" },
    { id: "e", label: "Гейти сборку на этом ранжированном множестве; остальное отслеживай/подавляй с причиной" },
  ]}
  correctOrder={["a", "b", "c", "d", "e"]}
/>

</Explanation>

<KeyTakeaway>Найти CVE бесплатно; сканер сообщает каждую уязвимую версию, которую может сопоставить с базой CVE. Навык сеньора — воронка: отбросить недостижимый код, отделить исправимое от won't-fix, ранжировать по эксплуатируемости в твоём развёртывании и гейтить только на исправимом+high+неподавленном, чтобы красная сборка всегда означала конкретное действие. Урежь базовый образ и обновляй непрерывно, чтобы сжать вход ещё до фильтрации.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="02-image-dep-scanning-retrieval"
  lang="ru"
  questions={[
    { q: "Объясни, как SCA/image-сканер порождает свои находки и почему свежий образ node:20 сообщает о десятках CVE в первый день.", a: "Сканер не анализирует твой код на баги — он строит ведомость материалов (каждый пакет ОС и каждую прямую/транзитивную зависимость приложения с точными версиями) и сопоставляет этот инвентарь с базой уязвимостей: NVD плюс дистрибутивные ленты (Debian Security Tracker), GitHub Advisories и экосистемы по языкам. Результат — арифметика множеств: installed_packages ∩ known_vulnerable_versions. Свежий образ node:20 сообщает о десятках CVE, потому что несёт полный userland Debian (shell, apt, сотни библиотек), у многих из которых опубликованы CVE; сканер сообщает фактическое присутствие уязвимых версий, а не суждение, что ты эксплуатируем. В этом же корень шума: присутствие уязвимых байтов — не то же самое, что загруженный, вызванный или достижимый с вводом атакующего уязвимый код." },
    { q: "Почему «ронять сборку на любой CRITICAL» — худший гейт, чем «ронять только на исправимом + high/critical + неподавленном», и как сюда вписываются достижимость и сканирование реестра?", a: "«Ронять на любой CRITICAL» ломается на первой неделе по трём причинам: у многих критических нет апстрим-фикса (won't-fix), так что сборка никогда не позеленеет; многие недостижимы (CVE в пути glibc или dev-инструменте, который не зовут), так что не несут реального риска; а возникающая усталость от алертов прячет единственный эксплуатируемый RCE среди 40 шумовых находок. Предсказуемый ответ команды — --exit-code 0, тихо отключающий контроль, что хуже отсутствия сканера, потому что производит ложную уверенность. Составной гейт (серьёзность И наличие фикса И не-подавлено) делает так, что красная сборка всегда означает конкретное действие: патч есть для реальной непересмотренной находки, так что «бампни версию» — однозначный фикс. Анализ достижимости (достигает ли хоть одна цепочка вызовов от точки входа уязвимой функции?) — крупнейший редуктор шума и основа стандарта VEX «присутствует, но не затронут». Сканирование реестра/рантайма важно, потому что пересечение двигается под тобой: ведомость материалов заморожена для дайджеста, а база CVE — нет, так что чисто отсканированный артефакт может стать критическим за ночь при раскрытии нового CVE — канонический случай Log4Shell. Сканирование на сборке отвечает на «чисто ли сейчас?»; непрерывное сканирование реестра — на «чисто ли ещё то, что мы запускаем?»" },
    { q: "Ты наследуешь пайплайн с --severity HIGH,CRITICAL --exit-code 0 (скан сообщает, но никогда не роняет) и ~300 стоящими находками. Какова твоя последовательность, чтобы вернуть гейту смысл, не останавливая поставку?", a: "Сначала сожми вход структурно: замени жирный базовый образ на distroless/alpine, что обычно удаляет большинство находок слоя ОС у источника (меньше пакетов, а не подавленные CVE), и убедись, что lock-файлы запинены, чтобы сканы были воспроизводимы. Затем триажь остаток через воронку — отбрось недостижимые находки (запиши их как VEX «не затронут» с обоснованием, чтобы они не всплывали снова), отдели исправимые от won't-fix/патча-нет и ранжируй исправимое+достижимое множество по эксплуатируемости в твоём реальном развёртывании. Переключи гейт обратно как составной предикат — исправимо И high/critical И неподавлено → ненулевой код выхода — чтобы красная сборка всегда отображалась в «бампни эту версию», а всё остальное направь в отслеживаемый бэклог с владельцами. Наконец, добавь непрерывное сканирование реестра, чтобы пост-деплойные CVE (случай Log4Shell) ловились, и ровный темп обновлений (Dependabot/Renovate), чтобы применять маленькие патчи непрерывно, а не упираться в следующий обрыв на 200 CVE. Сквозная мысль: никогда не гейти на находках, с которыми команда ничего не может сделать, — именно это приучило её отключать гейт изначально." },
  ]}
/>

<Recap lang="ru">
Контейнер — это две цепочки поставок: базовый образ ОС и зависимости твоего приложения, — и SCA/image-сканер инвентаризирует обе, затем сопоставляет эту ведомость материалов с базой CVE. Поэтому находить уязвимости бесплатно и шумно: CVE означает, что уязвимая версия *присутствует*, а не что её код достижим, эксплуатируем или хотя бы исправим. Ход сеньора — воронка триажа: отбросить недостижимые находки, отделить исправимое от won't-fix, ранжировать остальное по эксплуатируемости в твоём развёртывании и гейтить сборку только на исправимом + high/critical + неподавленном, чтобы красная сборка всегда означала конкретное действие — а не «иди подтверди 40 вещей, которые не починить». Сначала сожми вход минимальным базовым образом и непрерывными обновлениями и сканируй реестр непрерывно, потому что база CVE двигается под тобой (Log4Shell перевернул миллионы неизменных артефактов за ночь). Рефлекс, который надо убить, — `--exit-code 0`: тихо обезвреженный сканер хуже отсутствия, потому что позволяет верить, что ты защищён.
</Recap>
