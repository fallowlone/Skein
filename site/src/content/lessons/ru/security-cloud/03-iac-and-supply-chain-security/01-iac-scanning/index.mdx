---
concepts:
- iac-scanning
- terraform-security
- policy-as-code
- misconfiguration
- pre-deploy-gate
deepensInto: []
estMin: 15
lang: ru
lessonType: topic
level: middle
order: 1
prereqs: []
slug: 01-iac-scanning
sources:
- https://slsa.dev/
- https://owasp.org/www-project-devsecops-guideline/
spiral: []
status: ready
summary: 'Облачные пробои — это обычно мисконфиг, а не эксплойт: публичный бакет, security-group,
  открытая на 0.0.0.0/0. Сканирование IaC читает план Terraform и роняет пайплайн
  до apply, поэтому плохой конфиг не доходит до облака.'
title: IaC scanning
track: security-cloud
unit: 03-iac-and-supply-chain-security
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
Кто-то мёржит изменение в Terraform, чтобы добавить бакет под логи CDN. Три строки. Ревьюер бегло смотрит диф, не видит `acl = "public-read"`, апрувит. Чего диф не показывает — это умолчание модуля: этот S3-модуль поставляется с *незаблокированным* публичным доступом, а на уровне аккаунта нет `BlockPublicAccess`. В деплой-джобе отрабатывает `terraform apply`, бакет становится живым, и через четыре часа его индексирует сканер-как-сервис, который ползает по облачным диапазонам в поисках открытых бакетов. Ни CVE, ни эксплойта, ни мастерства атакующего — просто умолчание, которое человеческий глаз не разглядел в дифе. Это и есть модальный облачный пробой, и ровно его ловит сканирование до apply.
</Hook>

К концу урока ты будешь знать, как сканирование IaC читает план Terraform, чтобы поймать облачный мисконфиг *до* `apply`, почему гейт в пайплайне лучше пост-деплой-детекта и где у этого гейта слепые зоны.

<Crux>Сканируй Terraform на мисконфиг до apply и роняй пайплайн, чтобы публичный бакет или открытая security-group не дошли до прода.</Crux>

<Explanation>

## Почему угроза облака — мисконфиг, а не эксплойт

Картинка «облачного пробоя», которую носит в голове большинство инженеров, — это атакующий, выстраивающий хитрый эксплойт. Реальность скучнее и куда массовее: ресурс *настроили* открытым, и кто-то его нашёл. Бакет S3, чья политика разрешает `Principal: "*"`. Security-group с `0.0.0.0/0` на порту 22 или 5432. Инстанс RDS с `publicly_accessible = true`. Незашифрованный том без KMS, на котором лежит PII. Ни одно из этого — не дефект софта: облако сделало ровно то, что сказал конфиг. Поэтому задача защищающегося не «патчить быстрее», а *не дать неправильному конфигу вообще примениться*.

Infrastructure-as-Code — это то, что делает такую профилактику возможной. Когда каждый бакет, security-group и IAM-политика объявлены в Terraform и проходят через пул-реквест, конфигурация становится ревьюируемым, тестируемым артефактом *до* того, как коснётся облака. Загвоздка: люди плохо читают дифы конфигов на безопасность. Опасная настройка — обычно *отсутствие*: пропущенный `aws_s3_bucket_public_access_block`, не указанный блок `encryption`, который падает на слабое умолчание, — а отсутствия в дифе не видны. Этот пробел и закрывает сканер: он оценивает *эффективную* конфигурацию, включая умолчания модулей, которых диф не показал, против библиотеки известных плохих паттернов.

## Сканируй план, а не только файлы

Сканировать можно в двух местах, и разница между ними — это вся игра. **Статическое сканирование исходников `.tf`** (быстрый путь) парсит HCL и флагует очевидные плохие литералы — захардкоженный `cidr_blocks = ["0.0.0.0/0"]`, `acl = "public-read"`, написанный открытым текстом. Это мгновенно и не требует облачных учёток, но слепо ко всему вычисляемому: переменной, умолчанию модуля, `for_each`, значению, подтянутому из удалённого data-source.

**Сканирование *плана* Terraform** — зрелый ход. Ты запускаешь `terraform plan -out=tfplan && terraform show -json tfplan > plan.json`, затем направляешь сканер на `plan.json`. План — это *разрешённое* итоговое состояние: каждая переменная подставлена, каждый модуль развёрнут, каждое умолчание материализовано, — поэтому сканер видит бакет таким, каким он реально будет существовать, вместе с public-access-block. Именно это ловит баг из Hook: диф исходника был чистым, но у *запланированного* бакета нет блока публичного доступа, и план делает это видимым. Цена в том, что для получения плана нужен доступ на чтение к состоянию провайдера, поэтому он запускается чуть позже и с учётками — реальный компромисс, а не бесплатный апгрейд.

<FlowDiagram
  label="Гейт сканирования IaC в пайплайне: разработчик открывает пул-реквест с изменением Terraform; CI запускает terraform plan, чтобы разрешить эффективный конфиг с умолчаниями модулей, экспортирует его в JSON, и сканер policy-as-code проверяет его по правилам; при провале мёрж и apply блокируются, и мисконфиг не доходит до облака, при проходе — переходит к apply."
  perRow={3}
  hue="var(--d-crypto)"
  nodes={[
    { id: "pr", label: "Пул-реквест", sub: "правка .tf" },
    { id: "plan", label: "terraform plan", sub: "разрешить умолчания" },
    { id: "json", label: "show -json", sub: "plan.json" },
    { id: "scan", label: "Скан политик", sub: "tfsec / checkov / OPA" },
    { id: "gate", label: "Гейт", sub: "провал = блок" },
    { id: "apply", label: "apply", sub: "доходит до облака" },
  ]}
  edges={[
    { from: "pr", to: "plan" },
    { from: "plan", to: "json", label: "экспорт" },
    { from: "json", to: "scan" },
    { from: "scan", to: "gate", label: "находки" },
    { from: "gate", to: "apply", label: "проход" },
  ]}
  caption="Гейт стоит между plan и apply. Сканирование разрешённого plan.json — а не только исходника — это то, что вскрывает пропущенный блок публичного доступа, спрятанный в умолчании модуля. Сработавшее правило высокой важности блокирует мёрж, и конфиг не доходит до облака."
/>

## Policy-as-code: правила и есть гейт

Сканер хорош ровно настолько, насколько хороша его политика. Первый слой — встроенный набор правил вендора: tfsec, Checkov, Trivy или Terrascan поставляют сотни выверенных проверок («бакет S3 должен блокировать публичный доступ», «security-group не должна разрешать вход с 0.0.0.0/0 на порт 22», «хранилище RDS должно быть зашифровано»). Это даёт тебе базовую линию OWASP/CIS бесплатно в первый же день.

Слой, который разделяет команды, — это **кастомный policy-as-code**: твои собственные правила, под версионным контролем рядом с инфраструктурой, выраженные в движке политик (Rego у Open Policy Agent, кастомные проверки Checkov на Python/YAML или HashiCorp Sentinel). Здесь живут инварианты, специфичные для организации: «каждый ресурс обязан нести тег `cost-center`», «ни одна IAM-политика не может давать `*:*`», «все бакеты с данными должны использовать наш конкретный ключ KMS». Смысл policy-as-code в том, что правило *само по себе* — ревьюируемый, тестируемый, диффируемый артефакт: требование безопасности, закодированное один раз и применяемое на каждом PR всегда, вместо строки в вики, которую ревьюер якобы должен помнить. Это та же дисциплина shift-left, что и с мисконфигом: превращаешь то, что люди забывают, в то, что обеспечивает пайплайн.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Мисконфиг в Terraform</th>
        <th class="border border-border px-3 py-2 text-left">Чем опасен</th>
        <th class="border border-border px-3 py-2 text-left">Что флагует скан</th>
        <th class="border border-border px-3 py-2 text-left">Фикс в HCL</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2"><code>Бакет S3 без public_access_block</code></td>
        <td class="border border-border px-3 py-2">Бакет можно сделать публичным; утечка данных</td>
        <td class="border border-border px-3 py-2">HIGH — нет блока (умолчание модуля)</td>
        <td class="border border-border px-3 py-2"><code>aws_s3_bucket_public_access_block</code> всё true</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2"><code>SG вход 0.0.0.0/0 → 22</code></td>
        <td class="border border-border px-3 py-2">SSH открыт в интернет; брутфорс / скан</td>
        <td class="border border-border px-3 py-2">HIGH — широко открытый вход</td>
        <td class="border border-border px-3 py-2">Ограничь <code>cidr_blocks</code> до бастиона / VPN</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2"><code>RDS publicly_accessible = true</code></td>
        <td class="border border-border px-3 py-2">База достижима из публичного интернета</td>
        <td class="border border-border px-3 py-2">HIGH — публичная БД</td>
        <td class="border border-border px-3 py-2">Поставь <code>false</code>; доступ через приватную подсеть</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2"><code>EBS / RDS без блока encryption</code></td>
        <td class="border border-border px-3 py-2">PII в покое не зашифрован; провал комплаенса</td>
        <td class="border border-border px-3 py-2">MEDIUM — шифрование выключено</td>
        <td class="border border-border px-3 py-2"><code>storage_encrypted = true</code> + ключ KMS</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2"><code>IAM-политика с "*:*"</code></td>
        <td class="border border-border px-3 py-2">Скомпрометированная роль = захват всего аккаунта</td>
        <td class="border border-border px-3 py-2">HIGH — wildcard-привилегия</td>
        <td class="border border-border px-3 py-2">Least-privilege: действия + ARN ресурсов</td>
      </tr>
    </tbody>
  </table>
</div>

<Inset kind="why" lang="ru">
Почему гейтить в пайплайне, а не просто гонять CSPM-инструмент (Cloud Security Posture Management), который сканирует *живой* аккаунт? Потому что CSPM обнаруживает бакет уже *после* того, как он стал публичным: окно между `apply` и следующим сканом CSPM — это реальная экспозиция, и ровно в это окно краулеры его находят. Гейт в пайплайне — это профилактика: мисконфиг отклоняется до того, как он возникнет. Эти двое не соперники — CSPM твоя страховка от дрейфа, ручных изменений в консоли и ресурсов, созданных вне Terraform, — но самый дешёвый мисконфиг для починки тот, что вообще не дошёл до облака, и только гейт до apply даёт тебе это.
</Inset>

## Слепые зоны гейта — и где он кусается

Скан до apply необходим, но не достаточен, и зрелый инженер знает его границы. Он видит только то, чем управляет Terraform: бакет, который коллега создал руками в консоли, или настройку, изменённую вживую после `apply` (дрейф), для него невидимы. Он ловит *известные* плохие паттерны, поэтому новый мисконфиг без правила проходит чистым. И у него есть острая эксплуатационная грань — **ложные срабатывания**. Включи все правила на HIGH и блокируй по всем — и первая неделя превратится в стену находок на легитимных исключениях (по-настоящему публичный бакет маркетинговых ассетов, порт, открытый по дизайну). Сбой здесь — *усталость от алертов*: разработчики учатся лепить сплошной `# tfsec:ignore` на всё, и гейт становится театром. Зрелая практика — жёстко гейтить только по выверенному набору высокой важности, отправлять средние в предупреждения и требовать, чтобы каждое подавление несло встроенное обоснование и владельца, — чтобы исключение было зафиксированным решением, а не молчаливым лазом.

<TradeoffMatrix
  id="01-iac-scanning-tradeoff-1"
  lessonSlug="01-iac-scanning"
  lang="ru"
  prompt="Команда добавляет сканирование IaC. Новый бакет S3 под логи проходит скан исходников, но фактически публичен, потому что умолчание модуля оставляет публичный доступ незаблокированным. Выбери подход, который надёжно ловит этот класс багов как гейт до деплоя."
  options={[
    { name: "Сканировать разрешённый JSON `terraform plan` в CI и ронять пайплайн на находках высокой важности до `apply`", summary: "Гейтить план.", correct: true, justification: "Сканирование разрешённого plan.json видит бакет таким, каким он реально будет существовать — каждое умолчание модуля и переменная материализованы, — поэтому пропущенный блок публичного доступа виден, хотя диф исходника был чистым. Падение пайплайна до apply означает, что мисконфиг не доходит до облака. Это профилактика в самой дешёвой точке и ровно то, для чего нужен гейт до деплоя." },
    { name: "Сканировать только сырые файлы `.tf` — это быстро и не требует учёток", summary: "Скан только исходников.", justification: "Статический скан исходников — хороший быстрый первый проход, но он слеп ко всему вычисляемому: умолчаниям модулей, переменным, for_each. Это ровно тот баг, который он упускает — опасная настройка есть отсутствие, внесённое умолчанием модуля, и она никогда не появляется в исходнике. Быстро, но даёт ложную уверенность именно на этом классе." },
    { name: "Пропустить гейт в пайплайне и положиться на CSPM, который сканирует живой аккаунт раз в несколько часов", summary: "Детект после apply.", justification: "CSPM — ценная страховка от дрейфа и внеполосных изменений, но он обнаруживает публичный бакет *после* apply — окно до следующего скана это реальная экспозиция, а краулеры находят открытые бакеты за минуты. Детект — не профилактика; нужны оба, но именно гейт не даёт этому дойти до прода." },
    { name: "Добавить пункт чек-листа с просьбой к ревьюерам подтвердить, что ни один бакет не публичен", summary: "Ручное ревью.", justification: "Это контроль, который уже провалился в Hook. Люди не видят отсутствующую настройку в дифе, а чек-лист полагается на память и старательность на каждом PR всегда. Кодирование правила как policy-as-code — в этом весь смысл: превратить то, что люди забывают, в то, что обеспечивает пайплайн." },
  ]}
/>

<Quiz
  id="01-iac-scanning-quiz-1"
  lessonSlug="01-iac-scanning"
  lang="ru"
  question="Почему зрелый инженер сканирует JSON `terraform plan`, а не только исходники `.tf`?"
  choices={[
    { label: "План — это разрешённое итоговое состояние: умолчания модулей, переменные и развёртывания материализованы, поэтому сканер видит конфиг таким, каким он реально будет существовать", correct: true },
    { label: "Сканировать план быстрее, чем парсить исходник HCL", misconception: "Всё наоборот: получение плана медленнее и требует учёток провайдера. Эту цену платят именно потому, что план разрешает вычисляемые значения, которых исходник показать не может." },
    { label: "Файлы `.tf` зашифрованы и не парсятся напрямую", misconception: "Исходник Terraform — обычный HCL и парсится нормально; статические сканеры читают его напрямую. Причина сканировать ещё и план — покрытие вычисляемых и умолчательных значений, а не читаемость." },
    { label: "Сканирование плана снимает необходимость в кастомных правилах policy-as-code", misconception: "Что ты сканируешь и какие правила гоняешь — независимы. Кастомная политика для специфичных для организации инвариантов нужна независимо от того, сканируешь ты исходник или план." },
  ]}
/>

<Quiz
  id="01-iac-scanning-quiz-2"
  lessonSlug="01-iac-scanning"
  lang="ru"
  question="Команда включает все правила на HIGH и блокирует пайплайн по всем. За неделю разработчики начинают лепить сплошные ignore-комментарии повсюду. Что пошло не так и каков зрелый фикс?"
  choices={[
    { label: "Усталость от алертов из-за пере-гейтинга — жёстко гейтить только по выверенному набору высокой важности, остальное в предупреждения, и требовать у каждого подавления обоснование и владельца", correct: true },
    { label: "Сканер сломан — переходи на другого вендора", misconception: "Проблема не в инструменте, а в конфигурации политики. Блокировка по каждому правилу, включая малоценные и контекстно-зависимые, и породила усталость. Смена вендора повторяет ту же ошибку." },
    { label: "Разработчикам вообще нельзя разрешать подавлять находки", misconception: "Некоторые находки — легитимные исключения (намеренно публичный бакет ассетов). Фикс не в запрете подавлений, а в том, чтобы каждое подавление было зафиксированным, владеемым, обоснованным решением, а не молчаливым сплошным лазом." },
    { label: "Гейт надо перенести так, чтобы он запускался только после apply, в живом аккаунте", misconception: "Перенос детекта после apply полностью отказывается от профилактики и возвращает окно экспозиции. Фикс — настроить важность и подавления, а не бросать гейт до apply." },
  ]}
/>

<DragOrder
  id="01-iac-scanning-drag-1"
  lessonSlug="01-iac-scanning"
  lang="ru"
  prompt="Упорядочь гейт сканирования IaC до apply от пул-реквеста до облака:"
  items={[
    { id: "a", label: "Разработчик открывает PR с изменением Terraform" },
    { id: "b", label: "CI запускает `terraform plan -out=tfplan`" },
    { id: "c", label: "Экспорт разрешённого плана в JSON (`terraform show -json`)" },
    { id: "d", label: "Сканер policy-as-code проверяет plan.json по встроенным + кастомным правилам" },
    { id: "e", label: "Провал на находках высокой важности → мёрж/apply заблокированы; проход → `apply` отрабатывает" },
  ]}
  correctOrder={["a", "b", "c", "d", "e"]}
/>

</Explanation>

<KeyTakeaway>Большинство облачных пробоев — это мисконфиг, который облако честно применило: публичный бакет, security-group на 0.0.0.0/0, — и опасная настройка обычно есть отсутствие, которое человек не видит в дифе. Сканируй разрешённый `terraform plan` (а не только исходник) встроенным плюс кастомным policy-as-code и роняй пайплайн до `apply`. Жёстко гейть только по выверенным правилам высокой важности, чтобы гейту доверяли, а не игнорировали.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="01-iac-scanning-retrieval"
  lang="ru"
  questions={[
    { q: "Объясни, почему зрелый инженер сканирует разрешённый план Terraform, а не только исходник .tf, и чего это стоит.", a: "Опасный облачный мисконфиг — обычно отсутствие, а не литерал: пропущенный aws_s3_bucket_public_access_block, не указанный блок encryption, умолчание модуля, оставляющее бакет открытым. Статический скан исходника парсит HCL и флагует очевидные плохие литералы (захардкоженный 0.0.0.0/0, acl = \"public-read\"), но слеп ко всему вычисляемому — переменным, for_each и особенно умолчаниям модулей, — поэтому самый частый реальный баг, чисто выглядящий диф, прячущий небезопасное умолчание модуля, проходит насквозь. Сканирование плана это чинит: ты запускаешь terraform plan -out=tfplan и terraform show -json tfplan > plan.json, затем направляешь сканер на разрешённый JSON. План — материализованное итоговое состояние: каждая переменная подставлена, каждый модуль развёрнут, каждое умолчание сделано явным, — поэтому сканер видит бакет ровно таким, каким он будет существовать, вместе с блоком публичного доступа. Цена реальна: получение плана требует доступа на чтение к состоянию/учёткам провайдера и запускается чуть позже в пайплайне, чем чистый линт исходника, поэтому скан исходника остаётся полезным как быстрый первый проход без учёток, а скан плана — авторитетный гейт." },
    { q: "Что такое policy-as-code, почему гейтить в пайплайне, а не полагаться на CSPM, сканирующий живой аккаунт, и где слепые зоны гейта?", a: "Policy-as-code означает, что сами правила безопасности — это версионируемые, тестируемые, диффируемые артефакты рядом с инфраструктурой: базовая линия вендора (tfsec/Checkov/Trivy поставляют CIS-выверенные проверки вроде «блокируй публичный S3» и «нет 0.0.0.0/0 на порт 22») плюс кастомные правила в движке вроде OPA/Rego, кастомных проверок Checkov или Sentinel для инвариантов организации («нет IAM *:*», «каждый ресурс с тегом cost-center», «бакеты данных обязаны использовать наш ключ KMS»). Ты гейтишь в пайплайне, а не полагаешься только на CSPM, потому что CSPM обнаруживает публичный бакет ПОСЛЕ apply — окно между apply и следующим живым сканом это настоящая экспозиция, а краулеры находят открытые бакеты за минуты, — тогда как гейт до apply это профилактика: мисконфиг отклоняется до того, как вообще возникнет, в самой дешёвой точке для починки. CSPM всё равно важен как страховка от дрейфа, изменений в консоли и ресурсов, созданных вне Terraform, так что они взаимодополняемы. Слепые зоны гейта: он видит только ресурсы под управлением Terraform (созданный руками или дрейфнувший конфиг невидим), ловит только известные паттерны (новый мисконфиг без правила проходит), а пере-гейтинг по каждому правилу на HIGH вызывает усталость от алертов и сплошные ignore — поэтому жёстко гейть только по выверенному набору высокой важности, предупреждай по остальному и делай каждое подавление обоснованным, владеемым, зафиксированным решением." },
  ]}
/>

<Recap lang="ru">
Большинство облачных пробоев — не эксплойты, а мисконфиг, который облако применило ровно как сказано: публичный бакет S3, security-group, открытая на 0.0.0.0/0 по SSH, публично доступная база, незашифрованный том с PII. Infrastructure-as-Code делает профилактику возможной, превращая каждый ресурс в ревьюируемый артефакт до того, как он коснётся облака, но люди не могут надёжно заметить опасность, потому что это обычно отсутствие — пропущенный блок публичного доступа, не выставленный флаг шифрования, — которого в дифе не видно. Сканирование IaC закрывает этот пробел: сканируй разрешённый JSON `terraform plan` (а не только исходник .tf, который слеп к умолчаниям модулей и вычисляемым значениям) против базовой линии вендора плюс кастомного policy-as-code и роняй пайплайн до `apply`, чтобы плохой конфиг не дошёл до аккаунта. Policy-as-code делает каждый инвариант организации тестируемым, диффируемым правилом, применяемым на каждом PR, вместо строки в вики, которую ревьюер должен помнить. У гейта есть границы — он видит только ресурсы под управлением Terraform, ловит только известные паттерны, а пере-гейтинг плодит усталость от алертов, превращающую его в театр, — поэтому сочетай его со страховкой CSPM от дрейфа, жёстко гейть только по выверенному набору высокой важности и делай каждое подавление владеемым, обоснованным решением. Теперь, читая трёхстрочный диф Terraform, добавляющий бакет, твой первый вопрос: что говорит *план* о том, как этот ресурс реально выглядит, когда разрешатся умолчания модуля?
</Recap>
