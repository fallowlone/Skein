---
concepts:
- cloud-iam
- principal
- policy
- identity-vs-resource-policy
- trust-policy
deepensInto: []
estMin: 15
lang: ru
lessonType: topic
level: middle
order: 1
prereqs: []
slug: 01-cloud-identity-model
sources:
- https://aws.amazon.com/iam/
- https://cheatsheetseries.owasp.org/cheatsheets/Authorization_Cheat_Sheet.html
spiral: []
status: ready
summary: 'Облачная авторизация — не один ACL, а несколько политик, вычисляемых вместе: запрос разрешён, только если какая-то явно разрешает и ни одна не запрещает. Разберись с принципалом, типами политик и порядком deny-by-default — и загадки доступа исчезают.'
title: Модель облачной идентичности
track: security-cloud
unit: 01-cloud-iam
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
Ваш сервис читает бакет, к которому ему никогда не выдавали доступ. Вы смотрите в политику роли — про этот бакет ни слова. Смотрите в политику бакета — она разрешает доступ совсем другому аккаунту. И всё же `aws s3 ls` работает. Никто не ошибся в очевидном файле, потому что облачная авторизация никогда не решается одним файлом. Решение — это слияние *нескольких* документов: identity-политики вызывающего, политики самого ресурса, общеаккаунтного ограничителя, может быть, permission boundary, — вычисляемых по фиксированным правилам. Бакет читался, потому что его resource-политика выдала доступ вашей роли, а у роли не было deny, чтобы это перебить. Пока вы не сможете прокрутить это слияние в голове, каждый сюрприз с облачным доступом выглядит как магия.
</Hook>

К концу урока вы сможете назвать, кто такой принципал, перечислить типы политик, которые крепятся к идентичностям и к ресурсам, и провести запрос на авторизацию через точное deny-by-default вычисление, которое запускают облачные платформы.

<Crux>Облачное authz-решение собирает identity-, resource- и trust-политики; по умолчанию — deny, пока что-то явно не разрешит.</Crux>

<Explanation>

## Принципал: кто спрашивает

Каждое решение об авторизации начинается с **принципала** — сущности, делающей запрос. В облачной IAM-модели принципал почти никогда не человек, набирающий пароль. Это *идентичность, которая уже прошла аутентификацию* и теперь несёт набор короткоживущих учёток: IAM-роль, принятая EC2-инстансом; Kubernetes service account, обменянный на облачный токен через workload identity; CI-задача, федерировавшаяся по OIDC-токену; execution-роль Lambda. Логин уже состоялся; к моменту вычисления политики платформа знает, *какой* принципал спрашивает. IAM — это слой авторизации, а не аутентификации: он отвечает на вопрос «можно ли этому уже-известному принципалу выполнить это действие над этим ресурсом», и именно с этого различия начинается бо́льшая часть путаницы.

Это важно по зрелой причине: принципалы обычно **машины, и они долгоживущие по замыслу, но короткоживущие по учётке**. У роли нет пароля; принципал *принимает* её и получает учётки, истекающие через час. Поэтому интересная поверхность атаки — не «угадать пароль», а «кому разрешено *стать* этим принципалом», и это управляется отдельной политикой, к которой мы перейдём.

## Два вопроса политики: identity против resource

Облачная платформа задаёт один и тот же запрос с двух сторон, и обе должны согласиться.

- **Identity-политика** крепится к принципалу (роли, пользователю, группе). Она говорит, что может *этот принципал*: «роль `report-writer` может `s3:PutObject` в `reports-bucket`». Читайте как предложение, начинающееся с актора.
- **Resource-политика** крепится к самому ресурсу (S3-бакету, SQS-очереди, KMS-ключу). Она говорит, кто может действовать *над этим ресурсом*: «этот бакет разрешает `arn:aws:iam::111:role/report-writer` читать». Читайте как предложение, начинающееся с объекта.

Один и тот же доступ можно выразить с любой из сторон, и именно *комбинация* делает межаккаунтный доступ работающим без правки ролей в чужом аккаунте. Если у бакета аккаунта A есть resource-политика, называющая роль аккаунта B, а у роли B есть identity-политика, разрешающая действие, запрос проходит — в одиночку ни одна сторона не смогла бы выдать его через границу аккаунтов. Это же — самый частый источник «почему оно это читает?»: грант живёт на ресурсе, а не на идентичности, поэтому чтение только политики роли никогда этого не объяснит.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Тип политики</th>
        <th class="border border-border px-3 py-2 text-left">Крепится к</th>
        <th class="border border-border px-3 py-2 text-left">Отвечает на</th>
        <th class="border border-border px-3 py-2 text-left">Типичное применение</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">Identity-based</td>
        <td class="border border-border px-3 py-2">Роль / пользователь / группа</td>
        <td class="border border-border px-3 py-2">Что может <em>этот принципал</em>?</td>
        <td class="border border-border px-3 py-2">Выдать приложению его права</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Resource-based</td>
        <td class="border border-border px-3 py-2">Бакет / очередь / ключ</td>
        <td class="border border-border px-3 py-2">Кто может действовать <em>над ресурсом</em>?</td>
        <td class="border border-border px-3 py-2">Межаккаунтный доступ</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Trust-политика</td>
        <td class="border border-border px-3 py-2">Роль (дверь <code>AssumeRole</code>)</td>
        <td class="border border-border px-3 py-2">Кто может <em>стать</em> принципалом?</td>
        <td class="border border-border px-3 py-2">Федерация, цепочки ролей</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Permission boundary / SCP</td>
        <td class="border border-border px-3 py-2">Идентичность / весь аккаунт</td>
        <td class="border border-border px-3 py-2">Каков <em>потолок</em>, независимо от грантов?</td>
        <td class="border border-border px-3 py-2">Организационные ограничители</td>
      </tr>
    </tbody>
  </table>
</div>

## Trust-политика: кто может стать этим принципалом

У роли есть дверь, и **trust-политика** — это замок на ней. Это resource-политика, живущая на роли и отвечающая на вопрос, которого identity-политика вообще не касается: *кому в принципе разрешено принять эту роль?* Identity-политика говорит, что роль может, когда вы уже внутри; trust-политика говорит, можете ли вы войти.

Это разделение — весь движок облачной федерации. Задача GitHub Actions, предъявляющая OIDC-токен, может стать deploy-ролью *только потому*, что trust-политика этой роли говорит: «принципалы, федерированные от этого OIDC-провайдера, с этим claim `sub`, могут меня принять». Человек из вашего SSO-каталога становится admin-ролью, потому что trust-политика доверяет провайдеру идентичности. Ошибитесь в trust-политике — доверьте `*` или OIDC `sub` с небрежным wildcard вроде `repo:org/*` — и вы отдали роль всему интернету, как бы туго ни была закручена её identity-политика. Trust-политика — это та часть, о существовании которой джуны забывают, а сеньоры ревьюят первой, потому что это и есть настоящая граница аутентификации для машинного принципала.

<Inset kind="why" lang="ru">
Почему две отдельные политики на одной роли, а не один объединённый список прав? Потому что «кому можно пользоваться этой возможностью» и «что эта возможность может делать» меняются независимо и принадлежат разным людям. Платформенная команда курирует trust-политику (какие OIDC-провайдеры, какие аккаунты, какие SSO-группы могут принять роль); прикладная команда курирует identity-политику (какие API-вызовы нужны приложению). Слияние заставило бы каждое изменение прав заново открывать границу федерации, а каждое изменение федерации — заново аудитить права приложения. Отделить *дверь* от *комнаты* — это least privilege, применённый к самой структуре.
</Inset>

## Как решение вычисляется на самом деле

Вот правило, которое растворяет загадки. Облачная авторизация — **deny by default**, и вычисление — это не «найти подходящий allow», а фиксированный приоритет:

1. **Старт — неявный deny.** Ни одно утверждение не подходит → запрос отклонён. Ничего не разрешено, пока что-то не выдаст грант.
2. **Явный `Deny` всегда побеждает.** Если *любая* применимая политика — identity, resource, boundary или организационный ограничитель — содержит подходящий `Deny`, запрос отклонён, точка. Никакой allow его не перебьёт.
3. **Явный `Allow` снимает неявный deny** — но только в пределах потолка. Permission boundary или организационный ограничитель (SCP) могут урезать достижимое, даже когда identity-политика говорит `Allow`; эффективное право — это *пересечение* выданного и того, что разрешает потолок.

Так что реальное вычисление таково: *deny, пока (какая-то политика разрешает) И (ни одна не запрещает) И (грант внутри каждого применимого потолка).* Этот порядок объясняет, почему чрезмерно широкий `Allow *` гораздо менее опасен, чем боятся, когда его ограничивает boundary, — и почему один залётный `Deny` способен сломать деплой, у которого «очевидно есть права».

<FlowDiagram
  label="Как вычисляется запрос на облачную авторизацию: он стартует с неявного deny; явный deny в любой применимой политике немедленно его обрывает; иначе явный allow снимает deny, но только внутри потолка permission boundary или организационного ограничителя, поэтому эффективное право — пересечение гранта и потолка."
  perRow={5}
  hue="var(--d-crypto)"
  nodes={[
    { id: "start", label: "Запрос", sub: "principal + action" },
    { id: "deny", label: "Явный Deny?", sub: "any policy" },
    { id: "allow", label: "Явный Allow?", sub: "id or resource" },
    { id: "ceiling", label: "Внутри потолка?", sub: "boundary / SCP" },
    { id: "permit", label: "Разрешено", sub: "grant ∩ ceiling" },
  ]}
  edges={[
    { from: "start", to: "deny", label: "default: deny" },
    { from: "deny", to: "allow", label: "no deny" },
    { from: "allow", to: "ceiling", label: "allowed" },
    { from: "ceiling", to: "permit", label: "within cap" },
  ]}
  caption="Deny-by-default с фиксированным приоритетом: неявный deny → явный Deny побеждает безусловно → явный Allow снимает deny только внутри потолка. Эффективное право — пересечение гранта и каждого применимого boundary/ограничителя."
/>

## Чтение реального гранта: межаккаунтный доступ в одном слиянии

Прокрутим стартовую загадку через модель. Ваша роль в аккаунте B может `s3:ListBucket` на бакете в аккаунте A. Проследим:

- **Trust-политика** роли B разрешает вашему EC2-инстансу / CI-задаче принять её. Аутентификация машинного принципала: готово.
- **Identity-политика** роли B разрешает `s3:ListBucket` на `arn:...:bucket-in-A`. Сторона гранта со стороны B: есть.
- **Resource-политика** бакета A разрешает `arn:aws:iam::B:role/yours` делать `s3:ListBucket`. Сторона гранта со стороны A: есть.
- **Нигде нет `Deny`**, и действие лежит внутри permission boundary роли B и организационных ограничителей A.

Обе стороны согласились, ни один deny не вмешался, потолок разрешил → разрешено. Причина, по которой чтение одной роли не сработало, в том, что *половина гранта жила на ресурсе в другом аккаунте*. Зрелая привычка — для любого неожиданного доступа спросить: «покажите мне каждую политику в слиянии — обе идентичности, оба ресурса, boundary, организационный ограничитель, — а не только ту, которую я ожидал».

<TradeoffMatrix
  id="01-cloud-identity-model-tradeoff-1"
  lessonSlug="01-cloud-identity-model"
  lang="ru"
  prompt="GitHub Actions-воркфлоу должен деплоить в ваш облачный аккаунт. Вы хотите, чтобы CI принимал deploy-роль. Какой подход — корректная модель идентичности?"
  options={[
    { name: "Настроить trust-политику роли так, чтобы она разрешала OIDC-провайдера, привязанного к конкретному репозиторию и claim ветки/окружения; дать роли least-privilege identity-политику", summary: "Федерация, привязать sub.", correct: true, justification: "Это модель, работающая как задумано: trust-политика — граница аутентификации машинного принципала (только OIDC-токен этого репозитория может принять роль), а identity-политика — least-privilege авторизация (только deploy-действия). Нигде не хранится долгоживущий секрет; учётки короткоживущие и принимаются на каждый запуск. Привязка claim sub к репозиторию + ветке/окружению — это то, что не даёт любому другому репозиторию в организации принять роль." },
    { name: "Создать долгоживущий access key для пользователя с deploy-правами и положить его в GitHub secret", summary: "Статический ключ в секрете.", justification: "Это работает, но возвращает ровно то, что федерация убирает: долгоживущую учётку, которая может утечь, требует ротации и даёт постоянный доступ при эксфильтрации. Весь смысл федерации через trust-политику — не хранить статический ключ; выбор пути с ключом меняет сильнейшее свойство модели на удобство." },
    { name: "Настроить trust-политику роли так, чтобы она доверяла OIDC-провайдеру с sub = repo:org/*", summary: "Wildcard на sub.", justification: "Это классический самострел trust-политики. Wildcard на всю организацию означает, что любой репозиторий в ней — включая форк или малодоверенный репозиторий — может выпустить токен, принимающий вашу deploy-роль. Trust-политика — граница аутентификации; небрежный wildcard в ней раздаёт роль независимо от того, как туго закручена identity-политика." },
    { name: "Пропустить trust-политику и просто прикрепить к роли широкую identity-политику", summary: "Без trust-политики.", justification: "Пропустить нельзя: без trust-политики, называющей, кто может принять роль, её не сможет принять никто — identity-политика управляет тем, что принципал может делать, но никогда тем, кто может им стать. Это смешивает дверь с комнатой — ровно то различие, которое модель разделяет." },
  ]}
/>

<Quiz
  id="01-cloud-identity-model-quiz-1"
  lessonSlug="01-cloud-identity-model"
  lang="ru"
  question="Identity-политика на роли говорит `Allow s3:*`, но permission boundary на той же роли разрешает только `s3:GetObject`. Принципал вызывает `s3:DeleteObject`. Что произойдёт?"
  choices={[
    { label: "Отклонено — эффективное право есть пересечение гранта и потолка, а DeleteObject вне boundary", correct: true },
    { label: "Разрешено — identity-политика говорит s3:*, а identity-политики — источник истины", misconception: "Identity-политики выдают грант, но permission boundary урезает. Эффективное право — пересечение; Allow вне потолка недостижим." },
    { label: "Разрешено — boundary применяются только к resource-политикам, не к identity-политикам", misconception: "Permission boundary применяются к идентичности (роли/пользователю) и урезают то, что её identity-политики могут выдать. Это именно потолок со стороны идентичности." },
    { label: "Ошибка — нельзя иметь Allow и boundary, которые противоречат", misconception: "Противоречие нормально и чётко определено: boundary побеждает, урезая. Ошибка не возникает; действие просто отклонено, потому что вне потолка." },
  ]}
/>

<Quiz
  id="01-cloud-identity-model-quiz-2"
  lessonSlug="01-cloud-identity-model"
  lang="ru"
  question="Бакет в аккаунте A читается ролью в аккаунте B, но identity-политики A никогда не упоминают B. Где живёт грант и почему чтение роли B его не объяснило?"
  choices={[
    { label: "На бакете A как resource-политика, называющая роль B — половина межаккаунтного гранта живёт на ресурсе, а не на идентичности", correct: true },
    { label: "В trust-политике B — trust-политики выдают доступ на чтение бакетов чужих аккаунтов", misconception: "Trust-политики управляют тем, кто может принять роль, а не тем, что роль может читать. Это дверь AssumeRole, а не грант доступа к данным." },
    { label: "Это баг — межаккаунтный доступ невозможен без правки ролей A", misconception: "Межаккаунтный доступ — по замыслу: resource-политика на бакете A может назвать роль B напрямую, без изменения идентичностей A. В этом и смысл resource-политик." },
    { label: "В организационном ограничителе (SCP), который авто-разрешает чтение по всей организации", misconception: "SCP — потолки, которые могут только ограничивать, но никогда выдавать. Они не могут быть источником allow; грант должен идти от identity- или resource-политики." },
  ]}
/>

<DragOrder
  id="01-cloud-identity-model-drag-1"
  lessonSlug="01-cloud-identity-model"
  lang="ru"
  prompt="Упорядочьте шаги вычисления решения об облачной авторизации, от старта до вердикта:"
  items={[
    { id: "a", label: "Старт с неявного deny (по умолчанию ничего не разрешено)" },
    { id: "b", label: "Проверить каждую применимую политику на явный Deny — если хоть один подходит, отклонить безусловно" },
    { id: "c", label: "Искать явный Allow в identity- или resource-политике" },
    { id: "d", label: "Подтвердить, что грант лежит внутри потолка permission boundary / организационного ограничителя" },
    { id: "e", label: "Разрешить только если allowed, не denied и внутри потолка" },
  ]}
  correctOrder={["a", "b", "c", "d", "e"]}
/>

</Explanation>

<KeyTakeaway>Облачная авторизация собирает несколько документов, а не один ACL. Identity-политики говорят, что может принципал; resource-политики — кто может действовать над ресурсом; trust-политика — кто может *стать* принципалом. Вычисление — deny-by-default: явный Deny всегда побеждает, Allow снимает deny только внутри потолка boundary/SCP, поэтому эффективное право — grant ∩ ceiling.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="01-cloud-identity-model-retrieval"
  lang="ru"
  questions={[
    { q: "Различите identity-политику принципала, resource-политику и trust-политику. На какой вопрос отвечает каждая и почему они разделены?", a: "Все три — документы-политики, но они отвечают на разные вопросы о разных вещах. Identity-политика крепится к принципалу (роли, пользователю, группе) и отвечает «что может этот принципал?» — например, роль report-writer может PutObject в reports-bucket. Resource-политика крепится к ресурсу (бакету, очереди, ключу) и отвечает «кто может действовать над этим ресурсом?» — например, этот бакет разрешает роли аккаунта B читать; именно это делает межаккаунтный доступ работающим, потому что грант может жить на ресурсе без правки идентичностей чужого аккаунта. Trust-политика — особая resource-политика, живущая на роли и отвечающая «кому вообще можно стать (принять) этим принципалом?» — это дверь AssumeRole и реальная граница аутентификации для машинного принципала. Их держат раздельно, потому что «кому можно пользоваться возможностью» (trust, владеет платформенная команда) и «что возможность может делать» (identity-политика, владеет прикладная команда) меняются независимо; слияние заставило бы каждое изменение прав заново открывать границу федерации и наоборот." },
    { q: "Пройдите через то, как облачная платформа вычисляет запрос на авторизацию, включая роль permission boundary.", a: "Вычисление — deny-by-default с фиксированным приоритетом, а не поиск любого подходящего allow. (1) Старт с неявного deny: если ни одно утверждение не подходит, запрос отклонён — ничего не разрешено, пока что-то явно не выдаст грант. (2) Явный Deny всегда побеждает: если любая применимая политика — identity, resource, permission boundary или организационный SCP — содержит подходящий Deny, запрос отклонён безусловно, и никакой Allow его не перебьёт. (3) Явный Allow снимает неявный deny, но только внутри потолка: permission boundary (на идентичности) или SCP (на всём аккаунте) урезает достижимое, даже когда identity-политика говорит Allow. Поэтому эффективное право — пересечение того, что выдано, и того, что разрешает каждый применимый потолок: deny, пока (какая-то политика разрешает) И (ни одна не запрещает) И (грант внутри каждого boundary/ограничителя). Вот почему залётный Deny может сломать действие, у которого «очевидно есть права», и почему чрезмерно широкий Allow гораздо безопаснее, когда его ограничивает boundary." },
  ]}
/>

<Recap lang="ru">
Облачный IAM никогда не решает доступ по одному файлу. Принципал — это уже-аутентифицированная идентичность, почти всегда машина с короткоживущими принятыми учётками, а не человек с паролем. Сливаются три вопроса политики: identity-политика говорит, что может принципал, resource-политика говорит, кто может действовать над ресурсом (механизм межаккаунтных грантов), а trust-политика на роли говорит, кому вообще можно стать этим принципалом — реальная граница аутентификации и то, что сеньоры ревьюят первым, потому что wildcard там раздаёт роль всем. Вычисление — deny-by-default с фиксированным приоритетом: любой явный Deny побеждает, явный Allow снимает неявный deny, но только внутри потолка permission boundary или организационного ограничителя, поэтому эффективное право — пересечение гранта и потолка. В следующий раз, когда доступ вас удивит, не читайте одну политику — прокрутите всё слияние: обе идентичности, оба ресурса, trust-дверь и каждый потолок.
</Recap>
