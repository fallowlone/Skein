---
concepts:
- iam-misconfiguration
- privilege-escalation
- confused-deputy
- over-permissioned
- blast-radius
deepensInto: []
estMin: 18
lang: ru
lessonType: topic
level: senior
order: 4
prereqs: []
slug: 04-iam-misconfigurations
sources:
- https://aws.amazon.com/iam/
- https://cheatsheetseries.owasp.org/cheatsheets/Authorization_Cheat_Sheet.html
spiral: []
status: ready
summary: 'Конфиги IAM, превращающие одну точку опоры в захват аккаунта: iam:PassRole
  плюс сервис, исполняющий код, — это лестница эскалации привилегий, а сторонний сервис,
  которому вы дали AssumeRole без ExternalId, — это confused deputy. И то и другое
  — выданные права, а не баги.'
title: IAM misconfigurations
track: security-cloud
unit: 01-cloud-iam
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
У пентестера не было админских прав. У него был один утёкший CI-ключ для деплоя с политикой, которая команде читалась как «может деплоить наш сервис». На деле она давала `iam:PassRole` на wildcard плюс `lambda:CreateFunction`. Через сорок минут он стал root в аккаунте — не эксплуатируя CVE, а *используя права ровно так, как они написаны*: создать Lambda, передать ей админскую роль, которая уже валялась в аккаунте, вызвать её, прочитать учётки из окружения. Каждый вызов API возвращал `200`. CloudTrail логировал каждый как авторизованный, потому что таким он и был. Пробой был не дырой в IAM. Это IAM делал ровно то, что велела политика.
</Hook>

К концу урока ты сможешь распознать два конфига IAM, превращающих единственную точку опоры в захват аккаунта, — цепочки эскалации привилегий и confused deputy — и назвать то единственное свойство каждого, что рвёт цепочку.

<Crux>Большинство пробоев IAM — не баги, а слишком широкие права: pass-role-цепочка или deputy, действующий по вводу атакующего.</Crux>

<Explanation>

## Почему конфиги IAM — отдельная категория

Ранее в этом юните ты разобрал модель облачной идентичности, написание least-privilege-политик и то, как работают assume-роли и федерация. Этот урок — о том, где всё это ломается в продакшене: не через эксплуатируемый код, а через права, по отдельности защитимые, а в сумме катастрофические.

Это различие — суть всего. SQL-инъекция — это *баг*: код делает то, чего автор не задумывал. Конфиг IAM — это *выданное право*: система делает ровно то, что говорит политика, а политика говорит слишком много. Сканеры, ищущие искажённый ввод, не находят ничего, потому что искажать нечего. CloudTrail показывает чистый `200` на каждом вызове, потому что каждый вызов был авторизован. Ущерб нанесён целиком внутри границ права, которое ты выписал. Поэтому ревью IAM читается как **достижимость**, а не синтаксис: вопрос никогда не «валидна ли эта политика?» — он «каково *транзитивное замыкание* всего, до чего этот принципал может дотянуться?».

Два сценария доминируют в реальных инцидентах, и зрелый инженер учится видеть оба как пути, а не утверждения: **эскалация привилегий** (право, позволяющее принципалу выдать *себе* больше) и **confused deputy** (доверенный сервис, действующий по вводу атакующего своей *собственной* властью, а не властью атакующего).

## Эскалация привилегий: когда право позволяет выдать себе больше

Эскалация привилегий — это конфиг, при котором у принципала есть право, которое, используемое по назначению, позволяет ему получить права, которые ему никогда не давали. Самое важное для распознавания — `iam:PassRole`.

`PassRole` существует по легитимной причине: когда ты создаёшь Lambda-функцию, EC2-инстанс или ECS-задачу, *что-то* должно прикрепить исполнительную роль, чтобы воркод мог вызывать API AWS. `PassRole` — это право передать роль сервису. Ловушка — в области ресурса. Политика с `iam:PassRole` на ресурсе-wildcard означает не «передай свою узкую роль» — она означает **передай любую роль в аккаунте, включая админскую**. Соедини это с любым сервисом, исполняющим код (`lambda:CreateFunction` + `lambda:InvokeFunction` или `ec2:RunInstances`), — и у тебя готова целая лестница:

<FlowDiagram
  label="Лестница эскалации привилегий через PassRole: низкопривилегированный принципал держит iam:PassRole на wildcard-ресурсе плюс право создать и вызвать Lambda; он создаёт функцию, передаёт себе админскую роль аккаунта как исполнительную роль функции, вызывает её, и функция теперь исполняется с админскими правами — каждый вызов авторизован. Сужение PassRole до одного явного ARN роли рвёт лестницу на первой ступени."
  perRow={5}
  hue="var(--d-crypto)"
  nodes={[
    { id: "start", label: "Низкопр. ключ", sub: "PassRole: *" },
    { id: "create", label: "CreateFunction", sub: "lambda" },
    { id: "pass", label: "Передать админа", sub: "как exec-роль" },
    { id: "invoke", label: "Invoke", sub: "идёт как админ" },
    { id: "own", label: "Захват аккаунта", sub: "blast radius" },
  ]}
  edges={[
    { from: "start", to: "create" },
    { from: "create", to: "pass", label: "PassRole: *" },
    { from: "pass", to: "invoke" },
    { from: "invoke", to: "own", label: "теперь админ" },
  ]}
  caption="Каждый вызов возвращает 200 и логируется как авторизованный — эскалация живёт целиком внутри выданных прав. Фикс на первой ступени: сузить iam:PassRole до конкретного ARN роли (и закрыть условием iam:PassedToService), чтобы низкопривилегированный ключ никогда не смог передать себе админскую роль."
/>

`PassRole` — заголовок, но семейство большое, и зрелый навык — распознавать *паттерн*, а не зубрить список. Любое из этих — одношаговая эскалация, если выдано на wildcard:

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Право</th>
        <th class="border border-border px-3 py-2 text-left">Выглядит как</th>
        <th class="border border-border px-3 py-2 text-left">На деле позволяет</th>
        <th class="border border-border px-3 py-2 text-left">Сужение, что останавливает</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2"><code>iam:PassRole</code> + run-code</td>
        <td class="border border-border px-3 py-2">«Деплоить Lambda»</td>
        <td class="border border-border px-3 py-2">Исполнить код под <em>любой</em> ролью, вкл. админскую</td>
        <td class="border border-border px-3 py-2">Сузить до одного ARN + <code>iam:PassedToService</code></td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2"><code>iam:CreatePolicyVersion</code></td>
        <td class="border border-border px-3 py-2">«Править наши политики»</td>
        <td class="border border-border px-3 py-2">Переписать прикреплённую политику на «всё»</td>
        <td class="border border-border px-3 py-2">Deny на своём boundary; permissions boundary</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2"><code>iam:AttachUserPolicy</code></td>
        <td class="border border-border px-3 py-2">«Управлять доступом команды»</td>
        <td class="border border-border px-3 py-2">Прикрепить <code>AdministratorAccess</code> себе</td>
        <td class="border border-border px-3 py-2">Permissions boundary ограничивает эффект</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2"><code>lambda:UpdateFunctionCode</code></td>
        <td class="border border-border px-3 py-2">«Залить хотфикс»</td>
        <td class="border border-border px-3 py-2">Подменить код в функции с высокопривилегированной ролью</td>
        <td class="border border-border px-3 py-2">Разделить деплой-идентичность и привилегированные exec-роли</td>
      </tr>
    </tbody>
  </table>
</div>

Защита — не «запретить эти действия»: они несущие. Это **сужение по ресурсу плюс permissions boundary**. Сузь `PassRole` до точного ARN роли, нужной воркоду; никогда не wildcard. Затем оберни любую идентичность, способную создавать или прикреплять права, в *permissions boundary* — потолок, который принципал не может превысить, какую бы политику он себе ни выдал. Boundary — это то, что превращает `AttachUserPolicy` из «прикрепить админа» в «прикрепить что угодно, ограниченное boundary».

## Confused deputy: когда доверенный сервис действует по вводу атакующего

Второй сценарий тоньше, потому что атакующий никогда не касается твоего аккаунта напрямую. **Confused deputy** — это привилегированный компонент, обманом заставленный злоупотребить *своей собственной* властью в пользу менее привилегированного вызывающего. Классический облачный случай: ты даёшь стороннему SaaS (инструменту мониторинга, бэкап-вендору) право assume-роли в твоём аккаунте, чтобы он читал твои данные. Вендор принимает одну роль, чтобы обслуживать *всех* своих клиентов.

Вот ловушка. Вызову assume-роли вендора нужны лишь id целевого аккаунта и имя роли — и то и другое может угадать вредоносный *другой клиент* того же вендора. Клиент B просит вендора «мониторить аккаунт 1234 / роль MyAuditRole». Вендор, как и спроектировано, принимает эту роль. Теперь вендор — deputy — читает *твой* аккаунт по поручению *клиента B*. Вендор не был скомпрометирован; он сделал ровно свою работу. Он был *запутан* в том, чей запрос он обслуживает.

<FlowDiagram
  label="Межаккаунтная атака confused deputy: сторонний вендор принимает роль, чтобы обслуживать всех клиентов; вредоносный клиент B подсовывает id твоего аккаунта и имя роли, вендор послушно принимает эту роль своей собственной властью и читает твои данные по поручению B. Требование пер-клиентского секрета ExternalId в trust-политике роли означает, что вендор должен предъявить значение, известное только тебе и вендору, которое B подсунуть не может, — это рвёт подмену."
  perRow={5}
  hue="var(--d-net)"
  nodes={[
    { id: "b", label: "Атакующий (кл. B)", sub: "знает acct+role" },
    { id: "ask", label: "Просит вендора", sub: "монитор 1234" },
    { id: "vendor", label: "Вендор (deputy)", sub: "AssumeRole" },
    { id: "read", label: "Читает данные", sub: "как сам" },
    { id: "ext", label: "Проверка ExternalId", sub: "trust-политика" },
  ]}
  edges={[
    { from: "b", to: "ask" },
    { from: "ask", to: "vendor", label: "лишь acct+role" },
    { from: "vendor", to: "read", label: "запутан" },
    { from: "vendor", to: "ext", label: "фикс" },
  ]}
  caption="Вендор не скомпрометирован — он запутан в том, чей запрос обслуживает. Условие trust-политики sts:ExternalId требует секрет, уникальный для твоей тенантности, известный только тебе и вендору; атакующий может угадать id аккаунта и имя роли, но не ExternalId, поэтому AssumeRole отклоняется."
/>

Фикс — **общий секрет, вшитый в доверительную связь**: `ExternalId`. Настраивая вендора, ты договариваешься о пер-тенантном секрете и кладёшь `"sts:ExternalId": "твоё-уникальное-значение"` условием в trust-политику роли. Теперь вендор должен предъявить *твой* ExternalId, чтобы принять *твою* роль, — а клиент B, который может угадать id аккаунта и имя роли, не может угадать секрет, который ему не давали. Тот же принцип обобщается: confused deputy побеждается **привязкой решения об авторизации к идентичности исходного запрашивающего**, а не только к идентичности deputy. В твоих собственных сервисах эквивалент — пробрасывать и проверять идентичность конечного пользователя до самого ресурса, никогда не позволяя привилегированному бэкенду действовать по сырой, подверженной влиянию атакующего цели.

<Inset kind="why" lang="ru">
Почему «роль вендора в IAM — только на чтение» сама по себе недостаточна? Потому что чтение в *чужом* аккаунте — всё равно пробой: confused deputy не повышает привилегию deputy, он перенаправляет уже имеющуюся привилегию deputy на жертву, которую тот трогать был не должен. ExternalId работает на ином уровне, чем область прав: область отвечает «что deputy может делать?», ExternalId отвечает «*для кого* ему разрешено это делать?». Нужно и то и другое. Роль только на чтение без ExternalId всё равно сливает данные каждого клиента любому другому клиенту, способному назвать аккаунт.
</Inset>

## Как зрелый инженер ревьюит изменение IAM: думай в blast radius

Объединяющая мысленная модель — **blast radius** (радиус поражения): если долгоживущая учётка ровно этого принципала утечёт атакующему завтра, каково *транзитивное замыкание* всего, до чего он дотянется? Не «для чего эта роль» — а чем она может *стать*. Деплой-ключ, способный передать любую роль, имеет blast radius во весь аккаунт, хотя его работа — пушить один сервис.

Этот сдвиг меняет то, как ты читаешь политики. Ты перестаёшь искать синтаксические ошибки и начинаешь трассировать достижимость: позволяет ли какое-то право этому принципалу получить другую идентичность (эскалация)? Позволяет ли какая-то trust-политика внешней стороне одолжить власть этого аккаунта, не доказав, что она — предполагаемая сторона (confused deputy)? Контроли чисто ложатся на эти два вопроса — least-privilege-сужение по ресурсу и permissions boundary ограничивают, *чем принципал может стать*; `ExternalId` и проброс идентичности на каждый запрос ограничивают, *по чьему поручению* deputy может действовать. Default-deny — пол под обоими: каждое право начинается с «нет» и расширяется лишь до названного ресурса по названной причине.

<TradeoffMatrix
  id="04-iam-misconfigurations-tradeoff-1"
  lessonSlug="04-iam-misconfigurations"
  lang="ru"
  prompt="CI-роль деплоя должна создавать Lambda-функции и прикреплять им исполнительную роль. Ревью безопасности отметило iam:PassRole, сужённый до wildcard-ресурса. Выбери верное исправление."
  options={[
    { name: "Сузить iam:PassRole до конкретного ARN исполнительной роли, добавить условие iam:PassedToService для lambda.amazonaws.com и обернуть деплой-идентичность в permissions boundary", summary: "Сужение по ресурсу + boundary.", correct: true, justification: "Это убивает эскалацию в корне. Сужение PassRole до одного ARN означает, что деплой-ключ может передать только нужную низкопривилегированную exec-роль, никогда — админскую роль аккаунта; условие PassedToService гарантирует, что роль можно отдать только Lambda и не перенаправить; а permissions boundary ограничивает деплой-идентичность, так что даже будущий чрезмерный грант не превысит потолок. Деплой по-прежнему работает — ему всегда нужно было передавать лишь эту одну роль." },
    { name: "Оставить wildcard-PassRole, но добавить алерт в CloudTrail, чтобы уведомляться, если когда-либо передаётся админская роль", summary: "Детект, а не предотвращение.", justification: "Детект — не предотвращение. К моменту срабатывания алерта атакующий уже создал функцию, передал админа, вызвал её и эксфильтровал учётки — минуты, а не часы. Каждый вызов был авторизован, так что алерт конкурирует с потоком легитимных вызовов PassRole. Мониторинг — ценный второй слой, но он не заменяет сужение гранта, раздающего захват аккаунта." },
    { name: "Полностью убрать iam:PassRole и поручить админу вручную прикреплять исполнительную роль после каждого деплоя", summary: "Убрать возможность.", justification: "Это ломает легитимный поток — PassRole существует именно для того, чтобы сервис прикреплял исполнительную роль воркоду, который создаёт. Навязывание ручного админского шага на каждый деплой операционно неработоспособно, будет обходиться и не отражает, как ремедиируют: грант сужают до нужного, а не удаляют несущее право, заменяя его человеком." },
    { name: "Сменить исполнительную роль на менее привилегированную, но оставить wildcard на PassRole", summary: "Урезать одну роль.", justification: "Это упускает уязвимость. Wildcard означает, что деплой-ключ может передать ЛЮБУЮ роль в аккаунте, а не только урезанную, — включая любую админскую или высокопривилегированную, существующую по иным причинам. Контроль — это область ресурса на PassRole; сужение одной нижестоящей роли при открытом wildcard оставляет лестницу эскалации целиком." },
  ]}
/>

<Quiz
  id="04-iam-misconfigurations-quiz-1"
  lessonSlug="04-iam-misconfigurations"
  lang="ru"
  question="Почему атаки эскалации привилегий IAM и confused deputy обычно ускользают от сканеров ввода и показывают чистые логи CloudTrail?"
  choices={[
    { label: "Потому что атака использует права ровно так, как выданы, — ничто не искажено, каждый вызов API подлинно авторизован", correct: true },
    { label: "Потому что CloudTrail не логирует вызовы API IAM или STS", misconception: "CloudTrail логирует вызовы IAM и STS (CreateFunction, PassRole, AssumeRole — все видны). Суть в том, что каждый логируется как УСПЕШНЫЙ, авторизованный вызов — конфиг в гранте, а не в аномальном запросе." },
    { label: "Потому что атакующий сперва отключает логирование украденными админскими правами", misconception: "Эскалация происходит ДО получения админа, используя лишь изначально выданные права. Логирование не отключается; вызовы просто все авторизованы, что и позволяет им сливаться." },
    { label: "Потому что эти атаки эксплуатируют zero-day в движке IAM", misconception: "В IAM нет бага. Движок корректно вычисляет политику; политика просто даёт слишком много. Поэтому сканеры, ищущие искажённый ввод или известные CVE, не находят ничего." },
  ]}
/>

<Quiz
  id="04-iam-misconfigurations-quiz-2"
  lessonSlug="04-iam-misconfigurations"
  lang="ru"
  question="Сторонний бэкап-вендор принимает роль в твоём аккаунте. Роль — только на чтение и узко сужена. Другой клиент этого вендора подсовывает id твоего аккаунта и имя роли и заставляет вендора прочитать твои бакеты. Чего не хватало?"
  choices={[
    { label: "Условия sts:ExternalId в trust-политике роли — пер-тенантного секрета, известного только тебе и вендору, чтобы другой клиент не выдал себя за твой запрос", correct: true },
    { label: "Более узкой области прав на роли вендора", misconception: "Область уже была узкой и только на чтение — а чтение НЕ ТОГО аккаунта всё равно полный пробой данных. Область отвечает «что deputy может делать»; она не отвечает «по чьему поручению», в чём и пробел confused deputy. ExternalId чинит недостающее." },
    { label: "Шифрования данных в покое в бакетах", misconception: "Вендор принимает роль, которая может легитимно читать данные, поэтому читает открытый текст через авторизованный путь. Шифрование в покое защищает от украденных дисков, а не от deputy, авторизованного читать по поручению не того клиента." },
    { label: "MFA на AWS-аккаунте вендора", misconception: "MFA защищает доступ к консоли/учёткам самого вендора; оно ничего не делает с доверительной связью, позволяющей любому клиенту назвать твой аккаунт и роль. Защита — привязка assume к пер-тенантному ExternalId." },
  ]}
/>

<DragOrder
  id="04-iam-misconfigurations-drag-1"
  lessonSlug="04-iam-misconfigurations"
  lang="ru"
  prompt="Упорядочь шаги цепочки эскалации привилегий через PassRole, от исходной точки опоры до захвата аккаунта:"
  items={[
    { id: "a", label: "Атакующий держит низкопривилегированный ключ с iam:PassRole на wildcard-ресурсе" },
    { id: "b", label: "Вызывает lambda:CreateFunction, чтобы создать новую функцию" },
    { id: "c", label: "Передаёт админскую роль аккаунта как исполнительную роль функции" },
    { id: "d", label: "Вызывает функцию, которая теперь исполняется с админскими правами" },
    { id: "e", label: "Читает учётки / действует как админ — полный захват аккаунта" },
  ]}
  correctOrder={["a", "b", "c", "d", "e"]}
/>

</Explanation>

<KeyTakeaway>Большинство пробоев IAM — слишком широкие права, а не баги. Эскалация привилегий (wildcard `iam:PassRole` + исполнение кода) позволяет принципалу выдать себе админа; confused deputy позволяет доверенному вендору действовать по поручению атакующего. Первое чинится сужением по ресурсу + permissions boundary, второе — `ExternalId`. Ревьюй каждое изменение IAM по его blast radius: чем этот принципал может *стать*?</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="04-iam-misconfigurations-retrieval"
  lang="ru"
  questions={[
    { q: "Пройди по цепочке эскалации привилегий через iam:PassRole и назови единственное изменение сужения, которое её рвёт.", a: "PassRole — это право передать IAM-роль сервису, исполняющему код (Lambda, EC2, ECS). Эскалация появляется, когда низкопривилегированный принципал держит iam:PassRole на wildcard-ресурсе вместе с правом создать и вызвать воркод — например, lambda:CreateFunction + lambda:InvokeFunction. Цепочка: (1) атакующий, держа лишь низкопривилегированный ключ, вызывает CreateFunction; (2) передаёт существующую админскую роль аккаунта как исполнительную роль этой функции — разрешено, потому что PassRole на wildcard для любой роли; (3) вызывает функцию, которая теперь идёт с админом; (4) читает учётки или вызывает любой API как админ — полный захват аккаунта. Принципиально: каждый вызов возвращает 200, и CloudTrail логирует каждый как авторизованный, потому что он подлинно таким и был; ничто не искажено, поэтому сканеры ввода не находят ничего. Единственное изменение, что её рвёт, — сужение iam:PassRole до конкретного ARN исполнительной роли, реально нужной воркоду (никогда не wildcard), в идеале с условием iam:PassedToService, чтобы роль уходила только нужному сервису. Затем permissions boundary на деплой-идентичности ограничивает как страховка. Работа всё ещё идёт — ему всегда нужно было передавать лишь эту одну роль." },
    { q: "Объясни межаккаунтную атаку confused deputy против стороннего вендора и почему роль только на чтение и узко сужённая недостаточна без ExternalId.", a: "Confused deputy — это привилегированный компонент, обманом заставленный злоупотребить СВОЕЙ властью в пользу менее привилегированного вызывающего. В облачном случае ты позволяешь стороннему SaaS принять роль в твоём аккаунте, чтобы читать твои данные; вендор использует одну роль для всех клиентов. Вызову assume нужны лишь id твоего аккаунта и имя роли — и то и другое угадываемо. Вредоносный ДРУГОЙ клиент этого вендора просит «мониторить аккаунт 1234 / роль MyAuditRole»; вендор послушно принимает эту роль своей собственной властью и читает ТВОИ данные по поручению атакующего. Вендор не был скомпрометирован — он сделал ровно свою работу, запутавшись в том, чей запрос обслуживает. Роль только на чтение и узко сужённая недостаточна, потому что область отвечает «что deputy может делать?», а пробел — «по чьему поручению ему разрешено действовать?» — чтение не того аккаунта всё равно полный пробой. Фикс — sts:ExternalId: пер-тенантный секрет, известный только тебе и вендору, заданный условием в trust-политике роли. Вендор должен предъявить ТВОЙ ExternalId, чтобы принять ТВОЮ роль; другой клиент, способный угадать id аккаунта и имя роли, не угадает секрет, поэтому AssumeRole отклоняется. Общий принцип: привязывай решение об авторизации к идентичности исходного запрашивающего, а не только deputy." },
  ]}
/>

<Recap lang="ru">
Конфиги IAM — категория отдельная от обычных багов: система делает ровно то, что говорит политика, а политика просто говорит слишком много, поэтому сканеры ввода не находят ничего, а CloudTrail показывает чистый 200 на каждом шаге. Два сценария двигают реальные пробои. Эскалация привилегий — это грант, позволяющий принципалу выдать себе больше: канонический случай — wildcard iam:PassRole в паре с возможностью исполнять код, что позволяет низкопривилегированному ключу создать Lambda, передать ей админскую роль аккаунта, вызвать и захватить аккаунт; фикс — сужение PassRole по ресурсу до одного ARN плюс permissions boundary как потолок. Confused deputy — это доверенный сервис, обманом заставленный использовать свою власть для атакующего: сторонний вендор, принимающий роль для каждого клиента, может быть заставлен прочитать твой аккаунт любым, кто знает id аккаунта и имя роли; фикс — sts:ExternalId, пер-тенантный секрет, вшитый в trust-политику, который атакующий подсунуть не может. Оба сводятся к одному зрелому рефлексу: ревьюй каждое изменение IAM по его blast radius — если эта учётка утечёт завтра, каково транзитивное замыкание всего, чем она может стать или на что быть нацелена? Сужай, чем принципал может стать; привязывай, по чьему поручению deputy может действовать; default-deny под обоими.
</Recap>
