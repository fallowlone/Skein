---
concepts:
- least-privilege
- iam-policy
- wildcard-permissions
- permission-boundary
- policy-review
deepensInto: []
estMin: 15
lang: ru
lessonType: topic
level: middle
order: 2
prereqs: []
slug: 02-least-privilege-policies
sources:
- https://aws.amazon.com/iam/
- https://cheatsheetseries.owasp.org/cheatsheets/Authorization_Cheat_Sheet.html
spiral: []
status: ready
summary: Узкая политика выдаёт ровно те действия и ресурсы, что нужны роли. Подстановочные знаки (`Action:*`, `Resource:*`) кажутся удобными и тихо становятся радиусом поражения. Permission boundary ограничивает потолок прав роли — ограждение, нужное least privilege на масштабе.
title: Least-privilege policies
track: security-cloud
unit: 01-cloud-iam
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
Сервису нужно прочитать один объект конфигурации из одного S3-бакета. В тикете написано «выдайте роли доступ к S3», поэтому кто-то прикрепляет `AmazonS3FullAccess` — управляемую политику, дающую `s3:*` на `Resource: *`. Работает, деплой зелёный, все забыли. Через два года ключ доступа этой же роли утекает через переменную окружения, попавшую в отчёт о падении. Атакующий получает не один объект конфигурации. Он получает `s3:GetObject` на каждый бакет в аккаунте, `s3:DeleteBucket`, `s3:PutBucketPolicy`, чтобы сделать любой бакет публичным, и `s3:PutObject`, чтобы подложить payload в бакет вашего статического сайта. Один подстановочный знак, выданный ради чтения одного объекта, стал всей плоскостью данных аккаунта. Никто не решал давать сервису такую власть. Политика просто никогда не сказала «нет».
</Hook>

К концу урока ты будешь знать, как написать политику, выдающую ровно те действия и ресурсы, что нужны роли, почему подстановочные знаки превращают маленькую утечку в крупный пробой и как permission boundary ограничивает ущерб даже тогда, когда кто-то всё равно написал небрежную политику.

<Crux>Выдавай ровно те действия и ресурсы, что нужны роли; подстановочные знаки и отсутствие boundary — это то, как права тихо разрастаются.</Crux>

<Explanation>

## Как на самом деле выглядит узкая политика

Least privilege — это одно правило: у субъекта должны быть ровно те права, что нужны для его работы, ровно столько времени, сколько нужно, — и ни одним правом больше. Сложность не в принципе, а в том, что *удобная* и *правильная* политики выглядят почти одинаково, и удобная отгружается быстрее.

IAM-политика отвечает на два вопроса для каждого запроса: **какие действия** (`s3:GetObject`, `dynamodb:Query`) и **на каких ресурсах** (конкретный ARN бакета, конкретная таблица). Узкая политика называет оба. Сервис, читающий один объект конфигурации, должен держать вот это:

```json
{
  "Effect": "Allow",
  "Action": "s3:GetObject",
  "Resource": "arn:aws:s3:::acme-config/service-x/*"
}
```

Одно действие, один префикс ресурса. Если этот ключ утечёт, атакующий получит доступ на чтение к одной папке в одном бакете — сдержанный, скучный инцидент. Радиус поражения — это политика, и ты намеренно написал политику маленькой.

Рефлекс, который надо выработать: **начни с deny, добавь единственное действие, которое можешь назвать, ограничь его единственным ARN, который можешь назвать, и остановись.** Каждый раз, тянясь к `*`, ты говоришь «я не знаю, какие действия или ресурсы здесь нужны», — а атакующий, укравший учётку, отлично знает, что делать с этой неопределённостью.

## Чем кусаются подстановочные знаки

Подстановочные знаки — это режим отказа по умолчанию, потому что они по-настоящему полезны, пока ты строишь, и по-настоящему опасны, как только отгрузил. Знаков два, и оба ранят.

`Action: "s3:*"` расширяет, *что* может субъект. Сервису нужен был `GetObject`; подстановочный знак заодно вручил ему `PutObject`, `DeleteObject`, `PutBucketPolicy`, `PutBucketAcl`. Теперь учётка для чтения может перезаписать данные, удалить их или сделать приватный бакет публичным. `Resource: "*"` расширяет, *где* это применяется — с одного бакета на каждый бакет, включая ещё не существующие. Два знака компонуются: `s3:*` на `*` — это полный контроль над всем твоим объектным хранилищем из учётки, выданной ради чтения файла конфигурации.

<FlowDiagram
  label="Как подстановочный знак превращает единственную утёкшую учётку для чтения в компрометацию всего аккаунта: роль, выданная под s3:GetObject на один бакет, вместо этого получает s3:* на Resource звёздочка; когда ключ утекает, атакующий наследует удаление, публикацию и запись payload на каждый бакет, тогда как ограниченная политика сдержала бы ту же утечку до одной папки."
  perRow={5}
  hue="var(--d-crypto)"
  nodes={[
    { id: "need", label: "Реальная нужда", sub: "GetObject, 1 бакет" },
    { id: "grant", label: "Что выдали", sub: "s3:* на *" },
    { id: "leak", label: "Ключ утёк", sub: "env в крэш-логе" },
    { id: "blast", label: "Атакующий получает", sub: "delete, публикация" },
    { id: "tight", label: "Узкая политика", sub: "утечка = 1 папка" },
  ]}
  edges={[
    { from: "need", to: "grant", label: "пере-выдача" },
    { from: "grant", to: "leak" },
    { from: "leak", to: "blast", label: "весь контроль s3" },
    { from: "need", to: "tight", label: "фикс" },
  ]}
  caption="Подстановочный знак отвязывает власть учётки от её цели. Роли всегда нужно было одно действие на одном бакете; s3:* на Resource * означает, что в день утечки ключа охват атакующего — всё объектное хранилище, а не та единственная папка, которую сервис реально трогал."
/>

Есть и более тихая цена: подстановочные знаки делают аудит невозможным. Когда политика говорит `s3:GetObject` на именованном ARN, рецензент может прочитать её и точно знать, что роль умеет. Когда она говорит `s3:*` на `*`, единственный честный ответ на «что эта роль может?» — «всё со всем», и радиус поражения такого размера никто не способен осмыслить. Узкие политики самодокументируемы; подстановочные знаки стирают документацию.

<Inset kind="why" lang="ru">
Почему `iam:*` заслуживает особого страха, даже выше `s3:*`? Потому что IAM-права *самоусиливаются*. Роль с `iam:PutRolePolicy` или `iam:AttachRolePolicy` может выдать себе — или любой роли — любое право в аккаунте. Это **повышение привилегий**: атакующему не нужно опасное право напрямую, нужно лишь право его назначать. Роль с `iam:*` и больше ничем фактически является админом аккаунта, потому что шаг первый — написать себе админскую политику. Именно поэтому и существует следующее ограждение — permission boundary: чтобы сделать «выдай себе больше» невозможным, даже когда `iam:PutRolePolicy` присутствует.
</Inset>

## Permission boundaries: ограждение, нужное least privilege на масштабе

Написать одну узкую политику легко. Удержать узкими *все* политики, по сотням ролей и десяткам инженеров, у каждого из которых есть право создавать роли, — вот настоящая проблема. Её решают **permission boundaries**.

Permission boundary — это политика, прикреплённая к субъекту, задающая **максимум** прав, которые он когда-либо может иметь, — потолок, а не выдача. Эффективные права субъекта — это **пересечение** его identity-политик и его boundary: право разрешено, только если *обе* его разрешают. Так что даже если разработчик (или атакующий, скомпрометировавший роль разработчика) прикрепит `AdministratorAccess` к новой роли, boundary обрежет это обратно до того, что boundary разрешает.

Канонический сценарий — **делегированное создание ролей**. Ты позволяешь разработчикам создавать роли для собственных сервисов — это хорошо для скорости, — но требуешь, чтобы каждая создаваемая ими роль несла контролируемый тобой boundary. Теперь разработчик может выдать своему сервису `s3:GetObject` на свой бакет, но *не может* создать роль с `iam:*` или доступом к продакшен-базе, потому что boundary это запрещает, а пересечение побеждает. Boundary превращает «доверяй каждому инженеру написать идеальную политику» в «доверяй одной boundary-политике и дай инженерам быстро двигаться внутри неё».

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Identity-политика говорит</th>
        <th class="border border-border px-3 py-2 text-left">Boundary говорит</th>
        <th class="border border-border px-3 py-2 text-left">Эффективно (пересечение)</th>
        <th class="border border-border px-3 py-2 text-left">Почему</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2"><code>s3:GetObject</code></td>
        <td class="border border-border px-3 py-2"><code>s3:&#42;</code></td>
        <td class="border border-border px-3 py-2"><code>s3:GetObject</code></td>
        <td class="border border-border px-3 py-2">Обе разрешают; побеждает более узкая выдача</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2"><code>s3:&#42;</code></td>
        <td class="border border-border px-3 py-2"><code>s3:GetObject, s3:PutObject</code></td>
        <td class="border border-border px-3 py-2"><code>s3:GetObject, s3:PutObject</code></td>
        <td class="border border-border px-3 py-2">Boundary — это потолок; подстановочный знак обрезан</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2"><code>iam:&#42;</code> (админ)</td>
        <td class="border border-border px-3 py-2">без <code>iam:&#42;</code></td>
        <td class="border border-border px-3 py-2"><em>запрещено</em></td>
        <td class="border border-border px-3 py-2">Эскалация заблокирована: boundary её не разрешал</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2"><code>dynamodb:Query</code></td>
        <td class="border border-border px-3 py-2">только <code>s3:&#42;</code></td>
        <td class="border border-border px-3 py-2"><em>запрещено</em></td>
        <td class="border border-border px-3 py-2">Нет в boundary → нет пересечения → нет доступа</td>
      </tr>
    </tbody>
  </table>
</div>

Boundary не заменяет узкие политики — это страховочный рубеж. Identity-политика — место, где ты выражаешь *намерение* (эта роль читает конфиг); boundary — место, где ты выражаешь *предел* (ни одна роль в этом аккаунте не должна трогать IAM или продакшен-данные). Нужны оба: узкие политики, чтобы типовой случай был корректен, и boundary, чтобы единственная небрежная политика не стала компрометацией аккаунта.

## Как рецензировать политику по-сеньорски

Когда политика приходит к тебе на ревью, ты сканируешь три вещи по порядку. Первое — **подстановочные знаки**: любой `Action: "*"`, `"s3:*"` или `Resource: "*"` — это вопрос, не обязательно баг, но он должен быть обоснован, и «так было проще» не обоснование. Второе — **опасные глаголы**: `iam:*`, `iam:PutRolePolicy`, `iam:PassRole`, `sts:AssumeRole` на широком ресурсе, `*:Delete*` — всё, что может менять *чужие* права или уничтожать данные. Третье — **охват ресурсов**: называет ли каждый statement конкретные ARN, которых касается, или оставляет `Resource: "*"`, потому что никто их не перечислил?

Сеньорский ход — *снижать* права со временем, а не только пропускать новые. Облачные провайдеры отдают данные **access-analyzer / last-accessed**: какие действия роль реально использовала за последние 90 дней. Роль с `s3:*`, которая вызывала только `GetObject` и `ListBucket`, сама сообщает свою настоящую политику — сгенерируй узкую из наблюдаемого использования и замени подстановочный знак. Least privilege — не разовая выдача, которую ты получаешь правильной при создании; это храповик, который ты подтягиваешь по мере того, как узнаёшь, что роль на самом деле делает.

<TradeoffMatrix
  id="02-least-privilege-tradeoff-1"
  lessonSlug="02-least-privilege-policies"
  lang="ru"
  prompt="У сервисной роли сейчас `AmazonS3FullAccess` (`s3:*` на `*`), но по данным last-accessed она вызывала только `s3:GetObject` и `s3:ListBucket` на одном бакете. Выбери правильный способ привести её к least privilege."
  options={[
    { name: "Заменить на политику, выдающую `s3:GetObject` и `s3:ListBucket` с охватом ARN этого одного бакета, и прикрепить permission boundary, чтобы будущие правки не смогли снова её расширить", summary: "Сузить до наблюдаемого + boundary.", correct: true, justification: "Это least privilege по-правильному: выдача соответствует тому, что роли доказуемо нужно (по данным last-accessed), охвачена именованным ресурсом, а boundary делает сужение устойчивым — даже если позже кто-то прикрепит широкую политику, пересечение её обрежет. Ты починил политику и предотвратил регрессию одним движением." },
    { name: "Оставить `s3:*`, но добавить явный `Deny` на `s3:DeleteBucket` и `s3:PutBucketPolicy`", summary: "Deny-лист страшных действий.", justification: "Deny-лист — это игра в «убей крота»: ты заблокировал два опасных действия и оставил десятки (перезапись PutObject, PutBucketAcl, GetObject на каждый другой бакет) нараспашку. Least privilege — это allow-лист нужного, а не deny-лист того, о чём ты вспомнил. Радиус поражения всё ещё «весь S3 минус два глагола»." },
    { name: "Сохранить `AmazonS3FullAccess` — это управляемая AWS политика, значит, она поддерживается и безопасна", summary: "Довериться управляемой политике.", justification: "Управляемая не значит least-privilege. `AmazonS3FullAccess` поддерживается в том смысле, что AWS держит её список действий актуальным, — то есть она становится *шире* по мере добавления действий в S3, а не уже. Она по замыслу даёт `s3:*` на `*`; применять её для чтения одного бакета — ровно та пере-выдача, что превращает утёкший ключ в компрометацию всего S3." },
    { name: "Заменить `Resource: \"*\"` на ARN бакета, но оставить `Action: \"s3:*\"`", summary: "Сузить ресурс, оставить знак в действии.", justification: "Половина фикса. Сужение ресурса — реальный прогресс: утечка больше не охватывает каждый бакет, — но `s3:*` на этом одном бакете всё ещё даёт Delete, PutBucketPolicy (сделать публичным) и перезапись. Учётка для чтения не должна уметь удалять бакет, который она читает. Нужно сузить и действие тоже." },
  ]}
/>

<Quiz
  id="02-least-privilege-quiz-1"
  lessonSlug="02-least-privilege-policies"
  lang="ru"
  question="Единственное право роли — `iam:PutRolePolicy` на `*`. Ни `s3:*`, ни `dynamodb:*`, ничего больше. Насколько она опасна?"
  choices={[
    { label: "Фактически админ аккаунта — она может написать себе политику с любым правом, так что один IAM-глагол — это полная эскалация", correct: true },
    { label: "Безвредна — она не может читать или писать данные, она лишь редактирует IAM-политики", misconception: "Редактирование IAM-политик — самая опасная власть из существующих: первый ход роли — прикрепить себе политику `AdministratorAccess`, после чего у неё есть любое право на данные. Право выдавать права — это право выдать себе всё." },
    { label: "Умеренно рискованна — она может редактировать политики, но boundary всегда останавливает её автоматически", misconception: "Boundary останавливает её, только если он прикреплён. Boundary не автоматичны; без него `iam:PutRolePolicy` — явный путь эскалации. Boundary — именно тот контроль, что ты бы добавил, чтобы это нейтрализовать, но он должен быть намеренно на месте." },
    { label: "Низкий риск, потому что изменение политики требует отдельного шага одобрения в IAM", misconception: "В IAM нет встроенного шлюза одобрения на `PutRolePolicy`. Если API-вызов разрешён, он вступает в силу немедленно. Любой workflow одобрения — это то, что ты строишь вокруг IAM, а не гарантия, которую даёт IAM." },
  ]}
/>

<Quiz
  id="02-least-privilege-quiz-2"
  lessonSlug="02-least-privilege-policies"
  lang="ru"
  question="У роли identity-политика разрешает `s3:*`, а permission boundary разрешает только `s3:GetObject` и `s3:ListBucket`. Роль пробует `s3:DeleteObject`. Что произойдёт и почему?"
  choices={[
    { label: "Запрещено — эффективные права это пересечение identity-политики и boundary, а DeleteObject нет в boundary", correct: true },
    { label: "Разрешено — identity-политика даёт `s3:*`, а identity-политики приоритетнее boundary", misconception: "Boundary — это потолок, а не выдача с более низким приоритетом. Эффективный набор прав — пересечение: действие разрешено, только если И identity-политика, И boundary его разрешают. Boundary обрезает `s3:*` до своих двух действий." },
    { label: "Разрешено, но залогировано как нарушение boundary для последующего разбора", misconception: "Это не разрешено-потом-помечено; это запрещено в момент вычисления. Boundary не разрешает DeleteObject, поэтому пересечение его исключает и вызов проваливается." },
    { label: "Запрещено, но только потому, что DeleteObject — по своей сути привилегированное действие", misconception: "DeleteObject не особо привилегирован в IAM — здесь он запрещён исключительно потому, что boundary его не включает. Если бы boundary перечислял DeleteObject, пересечение бы его разрешило." },
  ]}
/>

<DragOrder
  id="02-least-privilege-drag-1"
  lessonSlug="02-least-privilege-policies"
  lang="ru"
  prompt="Упорядочь шаги, чтобы привести пере-привилегированную роль (`s3:*` на `*`) к least privilege:"
  items={[
    { id: "a", label: "Вытяни данные last-accessed / access-analyzer, чтобы увидеть, какие действия роль реально использовала" },
    { id: "b", label: "Напиши политику, перечисляющую только эти наблюдаемые действия" },
    { id: "c", label: "Ограничь Resource каждого statement конкретными ARN, которых роль касалась" },
    { id: "d", label: "Замени политику с подстановочным знаком и прикрепи permission boundary как потолок" },
    { id: "e", label: "Перепроверь last-accessed через цикл, чтобы поймать действие, которое ты срезал слишком агрессивно" },
  ]}
  correctOrder={["a", "b", "c", "d", "e"]}
/>

</Explanation>

<KeyTakeaway>Выдавай ровно те действия на ровно тех ресурсах, что нужны роли, — начни с deny и добавь один ARN, который можешь назвать. Подстановочные знаки (`s3:*`, `Resource: *`) делают утёкшую учётку столь же мощной, как весь твой аккаунт, и стирают аудируемость. Permission boundary ограничивает потолок прав любой роли, так что одна небрежная политика не станет пробоем. Подтягивай к наблюдаемому использованию; least privilege — это храповик, а не разовая выдача.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="02-least-privilege-retrieval"
  lang="ru"
  questions={[
    { q: "Объясни, почему один подстановочный знак вроде `s3:*` на `Resource: *` так опасен, даже когда роль всегда лишь читает один объект.", a: "Подстановочный знак отвязывает власть учётки от её цели. Роль была выдана под одно дело — прочитать один объект конфигурации, — но `s3:*` даёт каждое действие S3 (GetObject, а ещё PutObject, DeleteObject, DeleteBucket, PutBucketPolicy, PutBucketAcl), а `Resource: *` применяет их к каждому бакету в аккаунте, включая ещё не существующие. В день, когда эта учётка утечёт — через залогированную переменную окружения, отчёт о падении, скомпрометированную зависимость, — атакующий получает не тот один объект, что трогал сервис; он наследует полный контроль над всем объектным хранилищем: может читать каждый бакет, перезаписывать или удалять данные, делать любой приватный бакет публичным, чтобы подложить payload. Радиус поражения определяется политикой, а не тем, что реально делает код, поэтому чтение одного объекта становится компрометацией плоскости данных всего аккаунта. Подстановочные знаки также уничтожают аудируемость: рецензент может осмыслить `s3:GetObject` на именованном ARN, но единственный честный ответ на «что может роль с `s3:*` на `*`?» — «всё со всем», а это радиус поражения, который никто не способен осмыслить." },
    { q: "Что такое permission boundary, чем он отличается от identity-политики и почему это контроль, делающий least privilege выживаемым на масштабе?", a: "Permission boundary — это политика, прикреплённая к субъекту, задающая максимум прав, которые он когда-либо может иметь, — потолок, а не выдача. Эффективные права субъекта — это пересечение его identity-политик и его boundary: действие разрешено, только если ОБЕ его разрешают. Identity-политика выражает намерение («эта роль читает конфиг»); boundary выражает предел («ни одна роль в этом аккаунте не должна трогать IAM или продакшен-данные»). Они различаются направлением — identity-политики выдают, boundary ограничивают, — и нужны оба. На масштабе проблема не в том, чтобы написать одну узкую политику, а в том, чтобы держать узкими все политики по сотням ролей и многим инженерам, способным создавать роли. Boundary решает делегированное создание ролей: ты разрешаешь разработчикам создавать роли ради скорости, но требуешь, чтобы каждая созданная роль несла контролируемый тобой boundary, так что даже если кто-то прикрепит `AdministratorAccess` или `iam:*`, пересечение это обрежет. Это также конкретная защита от эскалации привилегий в IAM — роль с `iam:PutRolePolicy` иначе может выдать себе что угодно, но не может превысить boundary, который она не писала. Boundary не заменяет узкие политики; это страховочный рубеж, не дающий одной небрежной политике стать компрометацией аккаунта." },
  ]}
/>

<Recap lang="ru">
Least privilege — это одно правило: субъект держит ровно те действия, на ровно тех ресурсах, что ему нужны, — и ничего больше. Ловушка в том, что удобная и правильная политики выглядят почти одинаково, а удобная (`AmazonS3FullAccess`, `s3:*` на `*`) отгружается быстрее. Подстановочные знаки — доминирующий режим отказа: `Action: *` расширяет, что роль умеет, `Resource: *` расширяет, где, и вместе они превращают утёкшую учётку для чтения в полный контроль над объектным хранилищем, стирая любую возможность аудита радиуса поражения. IAM-глаголы вроде `iam:PutRolePolicy` ещё хуже, потому что право выдавать права — это право выдать себе всё; это эскалация привилегий. Permission boundaries — ограждение, делающее это выживаемым на масштабе: потолок, чьё пересечение с identity-политикой обрезает любую пере-выдачу, так что делегированное создание ролей остаётся быстрым без доверия каждому инженеру написать идеальную политику. И least privilege — это храповик, а не разовая выдача: со временем снижай права к наблюдаемому last-accessed использованию. В следующий раз, увидев `s3:*` на роли, читающей один бакет, твой первый вопрос: что эта роль реально вызывала и какой один ARN я могу ей задать?
</Recap>
