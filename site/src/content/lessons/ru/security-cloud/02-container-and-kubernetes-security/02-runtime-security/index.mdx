---
concepts:
- runtime-security
- container-escape
- seccomp
- capabilities
- read-only-filesystem
deepensInto: []
estMin: 15
lang: ru
lessonType: topic
level: middle
order: 2
prereqs: []
slug: 02-runtime-security
sources:
- https://kubernetes.io/docs/concepts/security/
- https://owasp.org/www-project-kubernetes-top-ten/
spiral: []
status: ready
summary: 'Контейнер — это процесс, а не VM: он делит ядро хоста. Сбрось Linux-капабилити,
  добавь seccomp-профиль и запусти read-only, чтобы баг одного процесса остался багом
  процесса, а не стал компрометацией хоста.'
title: Container runtime security
track: security-cloud
unit: 02-container-and-kubernetes-security
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
У Node-сервиса есть SSRF-баг. Неприятно, но локализовано — пока ты не замечаешь, что контейнер запущен как `--privileged`, потому что год назад кому-то это понадобилось, чтобы примонтировать FUSE-файловую систему для разовой задачи, и откатить забыли. Атакующий разворачивает SSRF в исполнение кода внутри контейнера — и стены больше нет: `--privileged` отдаёт процессу все Linux-капабилити, снимает seccomp-фильтр и открывает все устройства хоста. Через `mknod` он создаёт блочное устройство для корневого диска хоста, монтирует его и пишет cron-задание прямо на хост. Один веб-баг — и узел захвачен, а из узла в общем кластере ты в одном «scrape `kubectl`-токена» от всего кластера. Ядро всегда было общим; единственным, что стояло между багом процесса и хостом, была песочница, которую ты выключил.
</Hook>

К концу урока ты поймёшь, почему контейнер — это процесс, делящий ядро, а не маленькая VM, и как три контроля — сброшенные капабилити, seccomp-профиль и read-only корневая ФС — превращают локализованный баг в *локализованный* баг вместо захвата хоста.

<Crux>Сбрось Linux-капабилити, добавь seccomp-профиль, запусти read-only — чтобы баг в контейнере не стал компрометацией хоста.</Crux>

<Explanation>

## Контейнер — это процесс, а не VM

Самый важный факт о безопасности контейнеров: контейнер — это не маленькая виртуальная машина. Это обычный Linux-процесс на хосте, обёрнутый в namespace'ы (которые меняют то, что он *видит* — своё дерево PID, монтирования, сеть) и cgroup'ы (которые ограничивают то, что он *использует* — CPU, память). Чего у него принципиально **нет**, так это собственного ядра. Каждый контейнер на узле вызывает *то же самое* ядро хоста, что и всё остальное. У VM между нагрузкой и железом стоят гипервизор и гостевое ядро; у контейнера — одно общее ядро и немного учётной механики.

Это полностью меняет модель угроз. В VM выйти на хост означает победить гипервизор — твёрдую, хорошо изученную границу. В контейнере «побег» означает заставить общее ядро сделать что-то от твоего имени, что пересекает забор namespace'а: баг ядра, чрезмерно широкую капабилити, доступный на запись путь хоста, syscall, который никогда не фильтровался. **Container escape (побег из контейнера)** — это событие, когда код внутри контейнера получает исполнение кода или доступ к файлам на хосте. Это не экзотика: CVE-2019-5736 в runc позволял вредоносному образу перезаписать бинарь `runc` на хосте и выполниться от root на хосте; CVE-2022-0492 злоупотребляла путём release-agent в cgroups v1, чтобы сбежать из контейнера вообще без экзотических привилегий. Граница реальна, но тонка, и твоя задача в рантайме — сделать её толще дефолта.

## Capabilities: перестань быть root в 40 степенях

В классическом UNIX всё бинарно: ты либо root (uid 0, может всё), либо нет. Linux **capabilities** дробят всемогущество root на ~40 отдельных привилегий, которые можно выдавать или сбрасывать по одной: `CAP_NET_BIND_SERVICE` (привязка к портам ниже 1024), `CAP_NET_RAW` (raw-пакеты, ping), `CAP_SYS_ADMIN` (свалка настолько широкая, что её зовут «новым root»), `CAP_SYS_PTRACE`, `CAP_DAC_OVERRIDE` (обход проверки прав на файлы) и так далее.

Вот ловушка: контейнер, работающий от `root`, — это **не** то же самое, что контейнер от всемогущего root хоста, но по умолчанию он всё равно получает *щедрый* дефолтный набор капабилити. Рефлекс зрелого инженера — `сбросить ALL, потом добавить обратно только то, что нагрузке доказуемо нужно`, — почти всегда ничего, потому что типичный веб-сервис привязывается к высокому порту и читает свои же файлы. Капабилити, которые делают побег возможным — `CAP_SYS_ADMIN` (mount, BPF, множество путей побега), `CAP_SYS_MODULE` (загрузка модуля ядра = мгновенный конец игры), `CAP_DAC_READ_SEARCH` (чтение файлов CVE-2014-9357 / Docker «Shocker»), `CAP_NET_ADMIN` — это ровно те, которые нормальное приложение никогда не использует. Сбросить их ничего не стоит, а целые классы побега исчезают. Антипаттерн обратный: `--privileged`, который не просто выдаёт все капабилити — он ещё и отключает seccomp/AppArmor и открывает устройства хоста, из-за чего и фигурирует почти в каждом реальном разборе побега из контейнера.

<FlowDiagram
  label="Цепочка побега из контейнера и где её рвёт каждый контроль: баг приложения даёт исполнение кода внутри контейнера; при дефолтных или privileged-капабилити, нефильтрованной поверхности syscall'ов и записываемой ФС процесс достигает ядра или пути хоста и сбегает на узел, откуда вытягивается токен кластера. Сброс капабилити, seccomp-профиль и read-only корневая ФС рвут по одному звену каждый."
  perRow={3}
  hue="var(--d-crypto)"
  nodes={[
    { id: "bug", label: "Баг приложения", sub: "RCE в контейнере" },
    { id: "cap", label: "Широкие caps", sub: "drop ALL рвёт это" },
    { id: "sys", label: "Опасный syscall", sub: "seccomp рвёт это" },
    { id: "fs", label: "Запись на хост", sub: "read-only рвёт это" },
    { id: "host", label: "Побег на хост", sub: "код на узле" },
    { id: "cluster", label: "Пивот в кластер", sub: "scrape SA-токена" },
  ]}
  edges={[
    { from: "bug", to: "cap" },
    { from: "cap", to: "sys", label: "если выдан" },
    { from: "sys", to: "fs", label: "если разрешён" },
    { from: "fs", to: "host", label: "если запись" },
    { from: "host", to: "cluster", label: "пивот" },
  ]}
  caption="Побег — это цепочка, а рантайм-харденинг — это эшелонированная защита: каждый из drop-capabilities, seccomp и read-only рвёт своё звено, поэтому ни один отсутствующий контроль в одиночку не отдаёт узел. --privileged снимает все три разом."
/>

## Seccomp: сократи поверхность атаки через syscall'ы

Капабилити гейтят *привилегированные* операции; **seccomp** (secure computing mode) гейтит сами *syscall'ы*. Ядро Linux экспонирует ~300–400 системных вызовов, и эта поверхность *и есть* поверхность атаки — любой эксплойт ядра в контейнере в конечном счёте это «вызови такой syscall с такими аргументами». seccomp-профиль — это allowlist (или denylist) syscall'ов, применённый к процессу; заблокированный syscall возвращает `EPERM` или убивает процесс вместо входа в путь ядра.

Практические числа важны. *Дефолтный* seccomp-профиль Docker уже блокирует ~44 самых опасных syscall'а — `mount`, `ptrace` чужих процессов, `kexec_load` (загрузить новое ядро), `bpf`, `keyctl`, namespace-трюки `unshare`/`setns`, — и один этот дефолт нейтрализовал несколько реальных CVE-побегов, зависевших от теперь заблокированного syscall'а. Зрелый сбой, который надо распознать: в Kubernetes seccomp **не включён по умолчанию** для старых кластеров. Под без заданного `seccompProfile` исторически работал *unconfined* — вся поверхность syscall'ов открыта, — поэтому современный рефлекс это задать `seccompProfile: { type: RuntimeDefault }` на весь кластер (а новые версии Kubernetes ставят это по умолчанию). Цена реальна, но мала: слишком тугой кастомный профиль может сломать нагрузку, которой легитимно нужен необычный syscall (некоторые JIT, приложения с активным `io_uring`), поэтому ход такой — `RuntimeDefault` как базовая линия, а кастомный профиль только когда ты измерил, какие syscall'ы приложение реально делает.

## Read-only корневая ФС и остальной набор харденинга

Третий столп — **read-only корневая файловая система** (`readOnlyRootFilesystem: true`). Если файловая система контейнера неизменяема, атакующий, получивший исполнение кода, не может подбросить веб-шелл, перезаписать бинарь на `$PATH`, изменить конфиг ради закрепления или подготовить инструменты для следующего шага. Большинство приложений вообще не пишут в свой образ — они пишут в базу, в лог-сток или во временную папку, — поэтому ты монтируешь маленький `emptyDir` на `/tmp` для по-настоящему эфемерных записей и замораживаешь всё остальное. Это превращает «у меня есть шелл» в «у меня есть шелл, который ничего не может сохранить», что ломает закрепление и большую часть подготовки инструментов.

Эти контроли взаимно усиливают друг друга, и таблица делает соответствие конкретным: каждый рвёт *своё* звено цепочки побега, в чём и весь смысл эшелонированной защиты — единичный обход не должен отдавать хост.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Контроль</th>
        <th class="border border-border px-3 py-2 text-left">Что ограничивает</th>
        <th class="border border-border px-3 py-2 text-left">Какое звено побега рвёт</th>
        <th class="border border-border px-3 py-2 text-left">Pod spec / флаг</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">Сброс capabilities</td>
        <td class="border border-border px-3 py-2">Привилегированные операции ядра (mount, BPF, модули)</td>
        <td class="border border-border px-3 py-2">Убирает пути побега через CAP_SYS_ADMIN/MODULE</td>
        <td class="border border-border px-3 py-2"><code>capabilities.drop: ALL</code></td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">seccomp-профиль</td>
        <td class="border border-border px-3 py-2">Какие из ~350 syscall'ов процесс может вызывать</td>
        <td class="border border-border px-3 py-2">Блокирует опасный syscall, нужный эксплойту</td>
        <td class="border border-border px-3 py-2"><code>seccompProfile.type: RuntimeDefault</code></td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Read-only корневая ФС</td>
        <td class="border border-border px-3 py-2">Запись в собственную ФС контейнера</td>
        <td class="border border-border px-3 py-2">Останавливает подброс веб-шелла / перезапись бинаря / закрепление</td>
        <td class="border border-border px-3 py-2"><code>readOnlyRootFilesystem: true</code></td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Запуск не от root</td>
        <td class="border border-border px-3 py-2">uid 0 внутри контейнера (ограничивает ущерб при побеге)</td>
        <td class="border border-border px-3 py-2">Сужает радиус поражения частичного побега</td>
        <td class="border border-border px-3 py-2"><code>runAsNonRoot: true</code></td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Запрет эскалации привилегий</td>
        <td class="border border-border px-3 py-2">setuid-бинари, вновь получающие привилегию</td>
        <td class="border border-border px-3 py-2">Закрывает путь повторной эскалации через setuid</td>
        <td class="border border-border px-3 py-2"><code>allowPrivilegeEscalation: false</code></td>
      </tr>
    </tbody>
  </table>
</div>

<Inset kind="why" lang="ru">
Почему `runAsNonRoot` в одиночку недостаточно? Потому что не-root внутри контейнера всё равно делит ядро хоста, а багу ядра нет дела до твоего uid — CVE в пути syscall'а может отдать исполнение кода на хосте из непривилегированного пользователя контейнера. Не-root — это необходимое сокращение радиуса поражения (оно останавливает тривиальное злоупотребление привилегией внутри контейнера и требуется большинством Pod Security Standards), но это *слой*, а не стена. Стена строится из capabilities + seccomp + read-only + non-root вместе, потому что каждый закрывает путь, который другие оставляют открытым. Именно поэтому `--privileged` так опасен: он не ослабляет один слой, он снимает сразу несколько.
</Inset>

## Когда флагов харденинга мало: более сильная изоляция

Иногда нагрузка по-настоящему недоверенная — ты запускаешь чужой код (CI-задачи, мультитенантную платформу функций, сгенерированный ИИ код в песочнице). Флаги харденинга сокращают поверхность атаки общего ядра, но не устраняют её; ядро всё ещё общее, поэтому достаточно хороший 0-day в ядре всё равно сбегает. Для этого уровня ответ — **более сильный рантайм изоляции**: gVisor (ядро в user-space, перехватывающее syscall'ы, так что контейнер никогда не касается реального ядра напрямую) или Kata Containers / Firecracker microVM (реальное лёгкое гостевое ядро на каждую нагрузку, изоляция уровня гипервизора со стартом почти как у контейнера). Они стоят некоторой производительности и операционной сложности, поэтому ты не тянешься к ним ради своих first-party сервисов — но для по-настоящему враждебного кода «закалять общее ядро» — неверная высота, а «не делить ядро» — верная.

<TradeoffMatrix
  id="02-runtime-security-tradeoff-1"
  lessonSlug="02-runtime-security"
  lang="ru"
  prompt="У first-party веб-сервиса был RCE. Он работает как `--privileged`, потому что обслуживающей задаче когда-то понадобился расширенный доступ. Выбери изменение, которое сильнее всего снижает шанс того, что RCE внутри контейнера станет компрометацией хоста."
  options={[
    { name: "Убери `--privileged`; задай drop ALL capabilities, `seccompProfile: RuntimeDefault`, `readOnlyRootFilesystem: true`, `runAsNonRoot: true`", summary: "Закали рантайм.", correct: true, justification: "Это рвёт каждое звено цепочки побега разом. Сброс капабилити убирает привилегированные операции (mount, загрузка модуля), на которые опираются побеги; RuntimeDefault seccomp блокирует опасные syscall'ы, нужные эксплойту; read-only корень не даёт атакующему закрепиться или подготовить инструменты; non-root сужает радиус поражения. --privileged отключал seccomp/AppArmor и открывал устройства хоста, поэтому его удаление — самый ценный ход. Для first-party приложения этот набор практически бесплатен — приложению эти привилегии и не были нужны." },
    { name: "Оставь `--privileged`, но добавь network policy, чтобы под не достучался до интернета", summary: "Только сеть.", justification: "Ограничение egress — хорошая эшелонированная защита и замедляет эксфильтрацию, но ничего не делает с самим побегом: --privileged всё равно отдаёт процессу все капабилити, нефильтрованную поверхность syscall'ов и устройства хоста, так что атакующий всё равно сбегает на узел и пивотит локально. Ты закрыл один выход, оставив парадную дверь открытой." },
    { name: "Пересобери образ на меньшей distroless-базе, чтобы было меньше бинарей для злоупотребления", summary: "Урежь образ.", justification: "Меньший образ снижает некоторое удобство пост-эксплуатации и его стоит делать, но он не меняет привилегий рантайма: --privileged контейнер может сделать mknod корневого диска хоста или загрузить модуль ядра независимо от того, сколько бинарей в образе. Гигиена образа — контроль времени сборки; побег здесь — проблема привилегий рантайма." },
    { name: "Перенеси нагрузку на gVisor ради ядра в user-space и считай готовым", summary: "Песочница-рантайм.", justification: "gVisor действительно усиливает изоляцию и это верный инструмент для недоверенного кода, но это неверная высота для first-party сервиса, который ты контролируешь, добавляет реальную цену по производительности/совместимости и — критично — запуск его всё ещё --privileged или с широкими капабилити сводит выгоду на нет. Более дешёвый, высокорычажный фикс — сначала применить стандартный набор харденинга; к песочнице-рантайму тянись, когда нагрузка действительно враждебна." },
  ]}
/>

<Quiz
  id="02-runtime-security-quiz-1"
  lessonSlug="02-runtime-security"
  lang="ru"
  question="Почему побег из контейнера угрожает хосту так, как побег из VM не угрожает, — и что делает `--privileged` уникально опасным?"
  choices={[
    { label: "Контейнер делит ядро хоста (нет гостевого ядра/границы гипервизора), поэтому баг ядра или широкая привилегия пересекает забор namespace'а; `--privileged` выдаёт все капабилити И отключает seccomp/AppArmor И открывает устройства хоста разом", correct: true },
    { label: "Контейнеры медленнее VM, поэтому у атакующего больше времени на эксплуатацию", misconception: "Производительность не имеет отношения к границе изоляции. Суть архитектурная: контейнер делит ядро хоста, а у VM своё гостевое ядро за гипервизором, из-за чего граница контейнера тоньше." },
    { label: "`--privileged` лишь выдаёт дополнительные CPU и память, что позволяет запускать более тяжёлые эксплойты", misconception: "--privileged про привилегии, а не ресурсы. Он выдаёт все Linux-капабилити, отключает фильтры seccomp/AppArmor и открывает устройства хоста — вот в чём провал безопасности, а не в добавке ресурсов." },
    { label: "VM и контейнеры изолированы одинаково; разница только во времени старта", misconception: "Они изолированы не одинаково. У VM есть гипервизор и отдельное гостевое ядро; контейнер делит одно ядро хоста. Именно это общее ядро и делает побег из контейнера другой, более тонкой границей." },
  ]}
/>

<Quiz
  id="02-runtime-security-quiz-2"
  lessonSlug="02-runtime-security"
  lang="ru"
  question="Твои Kubernetes-поды задают `runAsNonRoot: true`, но больше ничего. Почему этого недостаточно и какова зрелая базовая линия?"
  choices={[
    { label: "Не-root всё равно делит ядро хоста, поэтому баг ядра/syscall'а может сбежать независимо от uid — добавь drop-ALL capabilities, seccomp-профиль RuntimeDefault и read-only корневую ФС", correct: true },
    { label: "Этого достаточно — как только ты не-root внутри контейнера, побег невозможен", misconception: "Побег не невозможен для не-root пользователя. Багу ядра в пути syscall'а нет дела до твоего uid внутри контейнера; не-root снижает радиус поражения, но это один слой, а не вся стена." },
    { label: "Просто задай ещё и `--privileged`, чтобы контейнер был полностью управляемым", misconception: "--privileged — противоположность харденингу: он выдаёт все капабилити и отключает seccomp/AppArmor. Добавив его, ты уберёшь именно те защиты, которые нужны." },
    { label: "Замени non-root на network policy; фильтрация egress закрывает ту же угрозу", misconception: "Network policy и рантайм-харденинг защищают разное. Egress-правила ограничивают, куда говорит скомпрометированный под; они не останавливают побег на уровне ядра, который адресуют capabilities/seccomp/read-only." },
  ]}
/>

<DragOrder
  id="02-runtime-security-drag-1"
  lessonSlug="02-runtime-security"
  lang="ru"
  prompt="Упорядочь цепочку побега из контейнера, которую призван разорвать рантайм-харденинг, от начального бага до полной компрометации кластера:"
  items={[
    { id: "a", label: "Баг приложения даёт исполнение кода внутри контейнера" },
    { id: "b", label: "Широкий/privileged набор капабилити разрешает опасную операцию" },
    { id: "c", label: "Нефильтрованный syscall (нет seccomp) достигает уязвимого пути ядра" },
    { id: "d", label: "Записываемый путь хоста позволяет атакующему писать на узел" },
    { id: "e", label: "Исполнение кода на хосте → scrape токена service-account → пивот в кластер" },
  ]}
  correctOrder={["a", "b", "c", "d", "e"]}
/>

</Explanation>

<KeyTakeaway>Контейнер — это процесс, делящий ядро хоста, а не VM, поэтому баг процесса может стать компрометацией хоста. Строй стену из слоёв, каждый из которых рвёт своё звено побега: сбрось ALL капабилити (добавь обратно только доказуемо нужное), примени seccomp-профиль `RuntimeDefault`, монтируй корневую ФС read-only и запускай не от root с запретом эскалации привилегий. Никогда `--privileged` для first-party сервисов; для по-настоящему недоверенного кода перестань делить ядро (gVisor / Kata).</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="02-runtime-security-retrieval"
  lang="ru"
  questions={[
    { q: "Объясни, почему контейнер — иная граница безопасности, чем VM, и что конкретно означает «побег из контейнера».", a: "Контейнер — это обычный Linux-процесс на хосте, изолированный namespace'ами (что он видит: своё дерево PID, монтирования, сеть) и cgroup'ами (что он использует: CPU, память). Решающий факт — у него нет собственного гостевого ядра: каждый контейнер на узле вызывает *то же самое* ядро хоста. У VM, напротив, своё гостевое ядро за гипервизором, поэтому выход на хост означает победу над этой границей гипервизора. Поскольку контейнер делит ядро, «побег» означает заставить общее ядро сделать что-то через забор namespace'а от имени атакующего: эксплуатировать баг ядра, злоупотребить чрезмерно широкой капабилити, писать через примонтированный путь хоста или вызвать syscall, который никогда не фильтровался. Это не теория — CVE-2019-5736 в runc позволял вредоносному образу перезаписать бинарь runc на хосте и выполниться от root хоста, а CVE-2022-0492 использовала путь release-agent в cgroups v1 для побега без экзотических привилегий. Граница реальна, но тонка, поэтому рантайм-харденинг существует, чтобы сделать её толще снисходительного дефолта." },
    { q: "Пройди по трём ключевым рантайм-контролям — capabilities, seccomp, read-only корень — и что каждый останавливает.", a: "Это эшелонированная защита: каждый рвёт своё звено цепочки побега. (1) Capabilities дробят мощь root на ~40 отдельных привилегий; рефлекс — сбросить ALL и добавить обратно только доказуемо нужное (обычно ничего для веб-приложения). Это убирает капабилити, на которые опираются побеги — CAP_SYS_ADMIN (mount/BPF), CAP_SYS_MODULE (загрузка модуля ядра = конец игры), CAP_DAC_READ_SEARCH, — которые нормальное приложение никогда не использует, так что их сброс бесплатен. (2) seccomp гейтит сами syscall'ы: из ~300–400 Linux syscall'ов профиль разрешает (allowlist), какие процесс может вызывать, а заблокированный возвращает EPERM или убивает процесс. Дефолтный профиль Docker блокирует ~44 опасных syscall'а (mount, ptrace, kexec_load, bpf, keyctl) и нейтрализовал реальные CVE-побеги; в Kubernetes ловушка в том, что старые кластеры запускали поды *unconfined* по умолчанию, поэтому современная базовая линия — seccompProfile: RuntimeDefault. (3) Read-only корневая ФС (readOnlyRootFilesystem: true) означает, что атакующий с исполнением кода не может подбросить веб-шелл, перезаписать бинарь или закрепиться — ты монтируешь маленький emptyDir на /tmp для эфемерных записей и замораживаешь остальное. Вместе с runAsNonRoot и allowPrivilegeEscalation: false они образуют стену; --privileged опасен именно тем, что снимает несколько этих слоёв разом (все caps, нет seccomp/AppArmor, устройства хоста)." },
  ]}
/>

<Recap lang="ru">
Контейнер — не маленькая VM, а процесс хоста, изолированный namespace'ами и cgroup'ами и делящий одно ядро хоста со всем остальным на узле. Это общее ядро и есть вся причина существования рантайм-безопасности контейнеров: баг процесса, чрезмерно широкая капабилити, нефильтрованный syscall или записываемый путь хоста могут пересечь забор namespace'а и стать исполнением кода на хосте — *побегом из контейнера*, — а из узла в общем кластере вытянутый токен service-account превращает один веб-баг в компрометацию кластера. Защита эшелонирована, и каждый слой рвёт своё звено этой цепочки: сбрось ALL Linux-капабилити и добавь обратно только доказуемо нужное нагрузке (убирая пути CAP_SYS_ADMIN/CAP_SYS_MODULE, которые любят побеги); примени seccomp-профиль `RuntimeDefault`, чтобы поверхность из ~350 syscall'ов сжалась до того, что приложение реально вызывает (и никогда не запускай поды seccomp-unconfined); монтируй корневую ФС read-only, чтобы атакующий не закрепился и не подготовил инструменты; запускай не от root с `allowPrivilegeEscalation: false`, чтобы ограничить радиус поражения. Кардинальный грех — `--privileged`, который рушит несколько слоёв разом и фигурирует почти в каждом реальном разборе побега. Когда нагрузка по-настоящему недоверенная, перестань закалять общее ядро и перестань *делить* его — тянись к gVisor или Kata/Firecracker. Теперь, читая pod spec, твой первый вопрос: сбрасывает ли этот `securityContext` капабилити, задаёт ли seccomp-профиль и замораживает ли ФС — или он доверяет тому, что баг процесса никогда не случится?
</Recap>
