---
concepts:
- kubernetes-secrets
- secret-management
- external-secrets
- encryption-at-rest
- secret-sprawl
deepensInto: []
estMin: 18
lang: ru
lessonType: topic
level: senior
order: 5
prereqs: []
slug: 05-secrets-in-kubernetes
sources:
- https://kubernetes.io/docs/concepts/security/
- https://owasp.org/www-project-kubernetes-top-ten/
spiral: []
status: ready
summary: 'Секрет Kubernetes — это base64, а не шифрование: открытый текст для любого
  с etcd, узлом или RBAC-чтением. Почему это важно и как управлять секретами без
  расползания копий по неймспейсам и логам CI.'
title: Секреты в Kubernetes
track: security-cloud
unit: 02-container-and-kubernetes-security
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import FadedExample from "~/components/pedagogy/FadedExample.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
Джуниор из вашей команды дебажит падающий под и вставляет манифест в тикет: `kubectl get secret db-creds -o yaml`. Поле `data:` — это стена из base64, поэтому он думает, что делиться им безопасно: оно *выглядит* зашифрованным. Вы декодируете одну строку в уме: `cG9zdGdyZXM6Ly9hZG1pbjpodW50ZXIyQA==` — это просто `postgres://admin:hunter2@`. Пароль от боевой базы теперь в комментарии Jira, проиндексирован поиском, доступен каждому подрядчику с правами просмотра. Никакого CVE, никакого эксплойта. Просто base64, принятый за замок.
</Hook>

К концу этого урока вы будете точно знать, почему секрет Kubernetes *не* зашифрован, кто может его прочитать и как управлять секретами без расползания, превращающего одну утечку в компрометацию всего парка.

<Crux>Секрет Kubernetes — это base64, а не шифрование: открытый текст для любого с доступом к etcd, узлу или RBAC-чтению.</Crux>

<Explanation>

## Base64 — это кодирование, а не шифрование

Это самое дорогое заблуждение в безопасности Kubernetes, поэтому скажу прямо: **base64 — это обратимое перекодирование без ключа.** Кто угодно и где угодно может выполнить `base64 -d` и получить исходные байты обратно. В декодировании не участвует никакого секретного материала — в этом весь смысл base64: он существует, чтобы двоичные данные пережили текстовый канал, а не чтобы их защитить. Называть секрет «зашифрованным» из-за того, что в YAML показано `aHVudGVyMg==` вместо `hunter2`, — всё равно что называть окно «запертым», потому что вы заклеили его бумагой.

Что `Secret` в Kubernetes реально даёт по сравнению с `ConfigMap` — это *операционные*, а не криптографические свойства: он помечен так, что по умолчанию не печатается в открытых логах; его можно смонтировать как tmpfs-том (в памяти), а не писать на диск; доступ к нему управляется через RBAC как к отдельному типу ресурса. Это реальные, полезные свойства. Ни одно из них не является конфиденциальностью на диске. Байты лежат в etcd, и по умолчанию они лежат там открытым текстом.

## Кто на самом деле может прочитать ваш секрет

Зрелый вопрос — никогда «это base64?», а «какова поверхность чтения?». Секрет открыт удивительно широкому радиусу поражения, и каждый путь — это реальная атака, которую мы видели в постмортемах:

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Путь чтения</th>
        <th class="border border-border px-3 py-2 text-left">Кто получает доступ</th>
        <th class="border border-border px-3 py-2 text-left">Что это останавливает</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">etcd на диске</td>
        <td class="border border-border px-3 py-2">Любой с узлом, бэкапом или украденным снимком etcd</td>
        <td class="border border-border px-3 py-2">EncryptionConfiguration (KMS-провайдер), а не base64</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">RBAC <code>get/list secrets</code></td>
        <td class="border border-border px-3 py-2">Любой пользователь/SA с этим глаголом в неймспейсе</td>
        <td class="border border-border px-3 py-2">RBAC по минимуму прав; никогда не выдавать <code>list secrets</code> широко</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Смонтирован в под</td>
        <td class="border border-border px-3 py-2">Любой, кто может сделать <code>exec</code> в под или прочитать его ФС</td>
        <td class="border border-border px-3 py-2">Сузить SA; монтировать только то, что нужно нагрузке</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Токен дефолтного ServiceAccount</td>
        <td class="border border-border px-3 py-2">Скомпрометированный под, дотянувшийся до API с авто-смонтированными правами</td>
        <td class="border border-border px-3 py-2"><code>automountServiceAccountToken: false</code></td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Логи CI / Git</td>
        <td class="border border-border px-3 py-2">Любой, кто читает лог пайплайна или коммитит манифест</td>
        <td class="border border-border px-3 py-2">Внешнее хранилище секретов; никогда не коммитить отрендеренный Secret</td>
      </tr>
    </tbody>
  </table>
</div>

Два пути удивляют людей сильнее всего: **etcd на диске** и **RBAC `list secrets`**. По умолчанию kube-apiserver пишет объекты Secret в etcd без шифрования — поэтому бэкап etcd в S3-бакете или украденный снимок тома — это полный дамп учётных данных. А глагол `list` для секретов в RBAC катастрофичен именно потому, что возвращает *содержимое*, а не только имена: единственная слишком широкая `Role` с `list` по секретам в неймспейсе отдаёт каждую учётку этого неймспейса тому, кто ею владеет. Это верхняя позиция в OWASP Kubernetes Top 10 не просто так.

## Шифрование на диске: включаем замок

Фикс для пути через etcd — это **шифрование на диске (encryption at rest)**, настраиваемое на API-сервере через `EncryptionConfiguration`. Урок здесь в том, *какого провайдера вы выбираете*, потому что очевидный вариант — это ловушка.

<FlowDiagram
  label="Как запись секрета доходит до etcd: клиент применяет Secret, kube-apiserver сверяется с EncryptionConfiguration, и в зависимости от провайдера байты либо хранятся как base64-открытый текст (identity / aescbc с локальным ключом), либо запечатываются ключом из внешнего KMS, к которому apiserver обязан обратиться для расшифровки — так что один лишь снимок etcd бесполезен атакующему."
  perRow={5}
  hue="var(--d-crypto)"
  nodes={[
    { id: "apply", label: "kubectl apply", sub: "объект Secret" },
    { id: "api", label: "kube-apiserver", sub: "EncryptionConfiguration" },
    { id: "identity", label: "провайдер identity", sub: "plaintext в etcd" },
    { id: "kms", label: "провайдер KMS", sub: "envelope-шифрование" },
    { id: "etcd", label: "etcd на диске", sub: "шифр + DEK" },
  ]}
  edges={[
    { from: "apply", to: "api" },
    { from: "api", to: "identity", label: "дефолт — без замка" },
    { from: "api", to: "kms", label: "внешний ключ" },
    { from: "kms", to: "etcd", label: "запечатано" },
  ]}
  caption="Дефолтный провайдер «identity» хранит секреты открытым текстом — шифрование на диске включается вручную. Сильный выбор — KMS-провайдер (envelope-шифрование): ключ данных, шифрующий секрет, сам зашифрован ключом, который никогда не покидает внешний KMS, поэтому украденный снимок etcd — это шифртекст без ключа разворачивания."
/>

Ловушка — это **провайдеры `aescbc`/`aesgcm` с ключом в файле конфигурации**: да, они шифруют etcd, но ключ шифрования лежит на узле control-plane открытым текстом, поэтому атакующий, способный прочитать данные etcd, обычно может прочитать и файл ключа. Это шаг вбок, а не фикс. Сильный выбор — **KMS-провайдер**, выполняющий *envelope-шифрование*: каждый секрет шифруется ключом данных (DEK), DEK шифруется ключом шифрования ключей (KEK), который живёт только внутри внешнего KMS (AWS KMS, GCP KMS, Vault), и API-сервер обязан сделать сетевой вызов к KMS для разворачивания. Теперь украденный снимок etcd — это инертный шифртекст. Учтите одну ловушку: включение шифрования шифрует только секреты, *записанные после* включения, — вы должны переписать каждый существующий секрет (`kubectl get secrets -A -o json | kubectl replace -f -`), чтобы реально запечатать то, что уже лежит.

<Inset kind="why" lang="ru">
Почему нельзя просто включить шифрование на диске и считать дело сделанным? Потому что шифрование на диске защищает только *один* из пяти путей чтения из таблицы — путь украденного etcd. Оно ничего не делает против слишком широкого RBAC `list`, против `kubectl exec` в под со смонтированным секретом или против учётки, вставленной в лог CI. Шифрование на диске необходимо и его часто пропускают, но это единственный слой. Поверхность чтения — вот что вы реально управляете, и большинство реальных утечек секретов происходит через RBAC и человеческое копирование, а не через украденные диски.
</Inset>

## Расползание секретов: настоящий операционный сбой

Даже при включённом шифровании на диске и плотном RBAC сбой, который реально кусает команды, — это **расползание (sprawl)**: один и тот же секрет скопирован в десять неймспейсов, вшит в три Docker-образа, вставлен в два хранилища переменных CI и закоммичен один раз в Git «просто чтобы разблокировать деплой». Расползание — вот что превращает одну ротацию в недельные археологические раскопки, а одну утечку — в инцидент масштаба всего парка, потому что вы больше не можете ответить «где живёт эта учётка?».

Зрелый паттерн — **единый источник истины вне кластера** плюс **синхронизация внутрь, а не копирование внутрь**. Канонический секрет вы держите в выделенном хранилище (Vault, AWS Secrets Manager, GCP Secret Manager), а контроллер — External Secrets Operator (ESO) или Secrets Store CSI Driver — подтягивает его в кластер как нативный `Secret` по требованию. Манифесты ссылаются на `ExternalSecret`, указывающий на хранилище; сама учётка никогда не лежит в Git, никогда в образе, а ротация происходит в *одном* месте и распространяется. Секрет в кластере становится кэшем, а не базой-источником записи.

<FadedExample
  client:load
  id="05-secrets-k8s-faded-1"
  pieceSlug="05-secrets-in-kubernetes"
  lang="ru"
  title="Ограничь чтение секрета одним объектом"
  steps={{
    solved: (
      <p>
        Поду нужно прочитать ровно один Secret, <code>db-creds</code>, в неймспейсе <code>payments</code>. Role по наименьшим привилегиям использует <code>verbs: ["get"]</code> (а не <code>list</code>, который возвращает содержимое <em>всех</em> секретов) и закрепляет объект через <code>resourceNames: ["db-creds"]</code>, так что даже скомпрометированный токен прочитает только этот один Secret.
      </p>
    ),
    semi: {
      prompt: (
        <p>Заполни два поля, которые делают выдачу наименьшей привилегией, а не доступом на весь неймспейс:</p>
      ),
      blanks: [
        { id: "verb", expected: "get", placeholder: "глагол (не list)" },
        { id: "scope", expected: "resourceNames", placeholder: "поле, закрепляющее объект" },
      ],
    },
    blank: {
      prompt: (
        <p>С нуля: какое одно изменение превращает <code>verbs: ["list"]</code> на всех секретах в безопасное чтение одного объекта? Назови глагол и поле.</p>
      ),
      reveal: (
        <p>
          Замени <code>list</code> на <code>get</code> и добавь <code>resourceNames: ["db-creds"]</code>. <code>list</code> возвращает полное содержимое каждого Secret в неймспейсе; <code>get</code> + <code>resourceNames</code> ограничивает субъект тем одним объектом, который ему реально нужен, так что украденный токен не сможет перечислить остальные учётки неймспейса.
        </p>
      ),
    },
  }}
/>

<TradeoffMatrix
  id="05-secrets-k8s-tradeoff-1"
  lessonSlug="05-secrets-in-kubernetes"
  lang="ru"
  prompt="У вас один пароль БД, используемый сервисами в шести неймспейсах. Сегодня это вручную созданный Secret, скопированный в каждый. Выберите подход, который лучше всего предотвращает расползание и делает ротацию операцией в одном месте."
  options={[
    { name: "Хранить канонический секрет во внешнем хранилище и синхронизировать его в каждый неймспейс через External Secrets Operator, ссылаясь на него манифестами ExternalSecret", summary: "Единый источник истины, синхронизация внутрь.", correct: true, justification: "Это и есть паттерн, который реально убивает расползание: учётка живёт один раз, вне кластера, а контроллер проецирует её в неймспейсы как нативные секреты по требованию. Манифесты несут ссылку (ExternalSecret), а не значение, поэтому ничего чувствительного нет в Git или образах. Ротация происходит в одном месте и распространяется везде автоматически, и на вопрос «где это живёт?» есть один ответ." },
    { name: "Закоммитить base64-YAML секрета в Git, чтобы каждый неймспейс воспроизводимо разворачивал один и тот же файл", summary: "GitOps сырого секрета.", justification: "Base64 — не шифрование, поэтому так вы коммитите открытый пароль в историю версий навсегда — читаемый каждым с доступом к репозиторию и неротируемый из истории. Воспроизводимость реальна, но вы воспроизводите утечку. Если нужен GitOps — коммитьте зашифрованную форму (Sealed Secrets / SOPS) или ссылку ExternalSecret, но не сырые данные." },
    { name: "Создать секрет один раз в общем неймспейсе и дать остальным пяти RBAC-доступ list к нему", summary: "Делиться через RBAC list.", justification: "Выдача list по секретам — ровно тот катастрофичный глагол: он возвращает содержимое, а не только имена, — поэтому это расширяет поверхность чтения на каждую нагрузку в пяти неймспейсах и всё, что может принять их service account. И ротацию это не решает; оно лишь централизует радиус поражения." },
    { name: "Вшить пароль в образ контейнера как ENV-дефолт, чтобы у подов он был всегда", summary: "Вшить в образ.", justification: "Образы пуллятся, кэшируются и расходятся по реестрам и ноутбукам; учётка в слое образа извлекаема любым, кто может сделать docker pull, и выживает в истории слоёв даже после того, как вы её «удалили». Это худшая форма расползания: неротируемая, неаудируемая и скопированная на каждый узел, запускающий образ." },
  ]}
/>

<Quiz
  id="05-secrets-k8s-quiz-1"
  lessonSlug="05-secrets-in-kubernetes"
  lang="ru"
  question="Коллега говорит: «наши секреты в etcd в безопасности, потому что Kubernetes кодирует их в base64». Какая точная поправка?"
  choices={[
    { label: "Base64 — обратимое кодирование без ключа; секреты в etcd по умолчанию открытым текстом, пока вы не включите шифрование на диске", correct: true },
    { label: "Base64 — слабый шифр, поэтому надо переключиться на AES-256 base64", misconception: "Base64 вообще не шифр — нет ключа и нечего усиливать. Фикс — шифрование на диске через EncryptionConfiguration, а не «более сильный» base64." },
    { label: "Всё нормально, потому что etcd доступен только с control-plane", misconception: "Сетевая достижимость — не конфиденциальность на диске. Бэкапы etcd, снимки томов и доступ к узлу — все открывают байты на диске, которые без шифрования на диске лежат открытым текстом." },
    { label: "Секреты зашифрованы, пока настроен RBAC", misconception: "RBAC управляет тем, кто может вызвать API для объекта; он ничего не делает с байтами в etcd. RBAC и шифрование на диске защищают разные пути чтения." },
  ]}
/>

<Quiz
  id="05-secrets-k8s-quiz-2"
  lessonSlug="05-secrets-in-kubernetes"
  lang="ru"
  question="Вы включаете шифрование на диске с KMS-провайдером на кластере, где уже есть 200 секретов. Что ещё нужно сделать и почему?"
  choices={[
    { label: "Переписать все существующие секреты, потому что шифрование применяется только к объектам, записанным после включения", correct: true },
    { label: "Ничего — включение автоматически перешифровывает каждый существующий секрет", misconception: "Нет. Шифрование применяется при записи; уже существующие секреты остаются в старой (открытой) форме в etcd, пока их не перепишут, напр. kubectl get secrets -A -o json | kubectl replace -f -." },
    { label: "Удалить и пересоздать кластер, чтобы новая конфигурация применилась чисто", misconception: "Это излишне и разрушительно. Нужно лишь перезаписать существующие объекты Secret, чтобы они прошли через нового провайдера шифрования." },
    { label: "Переключить RBAC на запрет всех чтений на время миграции", misconception: "Шифрование на диске и RBAC — независимые слои; вы не отключаете чтения для миграции, а переписываете объекты Secret, чтобы они зашифровались при записи." },
  ]}
/>

<DragOrder
  id="05-secrets-k8s-drag-1"
  lessonSlug="05-secrets-in-kubernetes"
  lang="ru"
  prompt="Упорядочьте от самого слабого к самому сильному как способ защитить пароль БД в Kubernetes:"
  items={[
    { id: "a", label: "Base64-Secret, закоммиченный в Git (открытый текст в истории)" },
    { id: "b", label: "Обычный Secret в etcd, без шифрования на диске" },
    { id: "c", label: "Шифрование на диске с локальным ключом aescbc на узле" },
    { id: "d", label: "Шифрование на диске через внешний KMS (envelope-шифрование)" },
    { id: "e", label: "Внешнее хранилище + синхронизация ESO, плотный RBAC, без копий в Git/образах" },
  ]}
  correctOrder={["a", "b", "c", "d", "e"]}
/>

</Explanation>

<KeyTakeaway>Секрет Kubernetes — это base64, а не шифрование: по умолчанию открытый текст в etcd, читаемый через etcd, RBAC `list`, монтирование в под, токены SA и логи CI. Включите шифрование на диске с KMS-провайдером, ужесточите RBAC и убейте расползание единым внешним источником истины, синхронизируемым внутрь (а не копируемым внутрь).</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="05-secrets-k8s-retrieval"
  lang="ru"
  questions={[
    { q: "Объясните, почему секрет Kubernetes не зашифрован, и перечислите, кто реально может его прочитать.", a: "Поле data секрета закодировано в base64, а base64 — это обратимое перекодирование без ключа: кто угодно может выполнить base64 -d и восстановить исходные байты, поэтому оно даёт нулевую конфиденциальность. Что секрет даёт сверх ConfigMap — это операционные свойства: он по умолчанию не печатается в открытых логах, его можно смонтировать как tmpfs-том в памяти и это отдельный ресурс RBAC. Ничто из этого не шифрование. Поверхность чтения широка: (1) etcd на диске — по умолчанию kube-apiserver пишет секреты в etcd открытым текстом, поэтому бэкап, снимок тома или узел дают полный дамп учёток; (2) RBAC get/list по секретам — причём list катастрофичен, потому что возвращает содержимое, а не только имена, так что одна слишком широкая Role сливает каждую учётку неймспейса; (3) любой под, в который секрет смонтирован, доступный любому, кто может сделать exec или прочитать его ФС; (4) авто-смонтированный токен дефолтного ServiceAccount, позволяющий скомпрометированному поду дотянуться до API; и (5) логи CI и Git, куда отрендеренные секреты вставляют или коммитят. Зрелый вопрос — никогда «это base64?», а «какова полная поверхность чтения и что защищает каждый путь?»." },
    { q: "Что такое расползание секретов, почему это реальный сбой и какой паттерн его предотвращает?", a: "Расползание — это одна и та же учётка, скопированная во многие места: продублированная по неймспейсам, вшитая в образы контейнеров, вставленная в хранилища переменных CI и закоммиченная в Git «чтобы разблокировать деплой». Это операционный сбой, который реально кусает команды, потому что делает невозможными две вещи: вы не можете ответить «где живёт эта учётка?», поэтому ротация становится многодневными раскопками, и одна утечка становится инцидентом масштаба всего парка, так как копии везде, а некоторые (слои образов, история Git) фактически неротируемы. Паттерн, который это предотвращает, — единый источник истины вне кластера плюс синхронизация внутрь, а не копирование внутрь: канонический секрет держат в выделенном хранилище (Vault, AWS/GCP Secrets Manager), а контроллер — External Secrets Operator или Secrets Store CSI Driver — подтягивает его в кластер как нативный Secret по требованию. Манифесты несут только ссылку ExternalSecret, поэтому значения нет в Git или образах; ротация происходит в одном месте и распространяется; а секрет в кластере становится кэшем, а не базой-источником записи. Шифрование на диске (KMS/envelope-провайдер, а не локальный ключ aescbc) закрывает путь украденного etcd поверх этого, но защищает лишь один из пяти путей чтения, поэтому контроль расползания и плотный RBAC важны не меньше." },
  ]}
/>

<Recap lang="ru">
Секрет Kubernetes закодирован в base64, а это кодирование без ключа, а не шифрование — `base64 -d` обращает его мгновенно, поэтому секрет в комментарии Jira или закоммиченном манифесте — это утечка открытой учётки. По сравнению с ConfigMap секрет даёт операционные свойства (не печатается в открытых логах, монтирование tmpfs, отдельный RBAC), но никогда — конфиденциальность на диске. Его поверхность чтения широка: по умолчанию открытый текст в etcd, RBAC `list` (который возвращает содержимое), монтирование в под, авто-смонтированные токены ServiceAccount и CI/Git. Закрытие пути через etcd — это включение шифрования на диске, причём с выбором KMS-провайдера для настоящего envelope-шифрования, а не локального ключа `aescbc`, лежащего на том же узле, и с памятью о том, что нужно переписать существующие секреты, ведь шифрование применяется только при записи. Но сбой, который реально вызывает инциденты, — это расползание: копии одной учётки, разбросанные по неймспейсам, образам, CI и Git, делающие ротацию невозможной и превращающие одну утечку в компрометацию всего парка. Зрелый фикс — один внешний источник истины, синхронизируемый внутрь через External Secrets Operator или CSI-драйвер, — ссылка, а не копия, — чтобы секрет в кластере был кэшем, а ротация происходила ровно в одном месте. В следующий раз, увидев `data:`, полное base64, ваш рефлекс должен быть: кто может читать etcd, у кого есть `list` и сколько копий этого существует?
</Recap>
