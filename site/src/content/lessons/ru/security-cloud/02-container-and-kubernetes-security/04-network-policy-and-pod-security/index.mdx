---
concepts:
- network-policy
- pod-security-standards
- security-context
- default-deny
- east-west-traffic
deepensInto: []
estMin: 16
lang: ru
lessonType: topic
level: middle
order: 4
prereqs: []
slug: 04-network-policy-and-pod-security
sources:
- https://kubernetes.io/docs/concepts/services-networking/network-policies/
- https://kubernetes.io/docs/concepts/security/pod-security-standards/
- https://owasp.org/www-project-kubernetes-top-ten/
spiral: []
status: ready
summary: 'Свежий под Kubernetes полностью открыт: любой под достаёт любой другой по плоской сети, а без SecurityContext контейнер работает под root с доступом к хосту. NetworkPolicy и Pod Security Standards — два opt-in барьера, превращающих эту позицию default-allow в default-deny.'
title: Network Policy and Pod Security
track: security-cloud
unit: 02-container-and-kubernetes-security
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
Пентестер пробивает один малоценный под — ресайзер картинок за публичным эндпоинтом загрузки, уязвимый к SSRF. На захардененном хосте на этом бы всё и кончилось: один скомпрометированный процесс в одном контейнере. Но это дефолтный кластер Kubernetes, поэтому две двери уже открыты. Первая: под может открыть TCP-соединение к *каждому другому поду в кластере* — к базе, внутреннему admin-API, эндпоинту метрик, что течёт токенами, к сервису облачных метаданных на `169.254.169.254`. Файрвола между нагрузками нет, потому что его никто не написал. Вторая: контейнер пода работает под UID 0 с дефолтным SecurityContext, так что цепочка SSRF→RCE отдаёт атакующему root внутри контейнера, который может `mount`, грузить модули ядра через щедрый набор capabilities и зондировать ноду. За двадцать минут ресайзер становится точкой пивота во весь кластер. Kubernetes сделал ровно то, что обещал: запланировал под и дал ему говорить со всеми. Никто не велел ему этого не делать.
</Hook>

К концу урока ты сможешь написать NetworkPolicy с default-deny и прочитать существующую на предмет дыры, что тихо открывает её обратно, применить профиль Pod Security Standards, блокирующий побеги root/privileged/hostPath, и объяснить, почему оба барьера opt-in и почему этот дефолт кусает команды каждый раз.

<Crux>У свежего пода плоская сетевая достижимость и нет барьера SecurityContext — пока NetworkPolicy и профиль Pod Security его не запрут.</Crux>

<Explanation>

## Две двери default-allow: сеть и спека пода

У безопасности Kubernetes повторяющаяся форма: платформа поставляется в режиме **default-allow**, а безопасность — это то, что ты *добавляешь*. Две такие двери — тема этого урока, и они независимы: закрытие одной ничего не делает для другой.

**Дверь первая — плоская сеть.** Сетевая модель Kubernetes требует, чтобы каждый под мог достать любой другой под по IP, через все неймспейсы, без NAT. Это не баг; это контракт, который обязан выполнять CNI-плагин. Следствие: *east-west*-трафик (под-под) по умолчанию полностью открыт. **NetworkPolicy** — это opt-in объект, который его ограничивает, но только если твой CNI реализует enforcement NetworkPolicy (Calico, Cilium и большинство managed-CNI это делают; легаси-дефолт flannel — нет, так что политика, которую ты `kubectl apply`, успешно применяется и тихо не enforce-ит ничего).

**Дверь вторая — SecurityContext пода.** Контейнер без `securityContext` работает под тем UID, который задаёт образ — часто `root` (UID 0) — наследует дефолтный набор Linux-capabilities, может получить `privileged: true`, монтировать тома `hostPath` и делить с хостом namespace PID или сети. Каждое из этого — задокументированный примитив побега из контейнера. **Pod Security Standards (PSS)** определяют три профиля — `privileged` (без ограничений), `baseline` (блокировать известные побеги) и `restricted` (жёстко закрыт: non-root, без добавленных capabilities, seccomp включён) — а **Pod Security Admission (PSA)** — это встроенный admission-контроллер, enforce-ящий выбранный профиль для неймспейса через метки.

<FlowDiagram
  label="Два независимых барьера на поде: исходящий или входящий пакет сверяется с NetworkPolicy на dataplane CNI (контроль east-west-трафика), тогда как собственная привилегия пода — runAsNonRoot, capabilities, hostPath, privileged — проверяется один раз во время admission в Pod Security Admission, до того как под вообще запланирован. Закрытие одного барьера ничего не делает для другого."
  perRow={5}
  hue="var(--d-crypto)"
  nodes={[
    { id: "spec", label: "Спека пода", sub: "отправлена" },
    { id: "psa", label: "PSA admission", sub: "проверка привилегий" },
    { id: "sched", label: "Запланирован", sub: "работает как SA" },
    { id: "netpol", label: "NetworkPolicy", sub: "dataplane CNI" },
    { id: "peer", label: "Под-сосед", sub: "разрешён или отброшен" },
  ]}
  edges={[
    { from: "spec", to: "psa", label: "create" },
    { from: "psa", to: "sched", label: "если ок" },
    { from: "sched", to: "netpol", label: "egress/ingress" },
    { from: "netpol", to: "peer", label: "allow-правило" },
  ]}
  caption="Два ортогональных контроля. Pod Security Admission — одноразовый барьер в момент create на то, чем поду позволено быть (root? privileged? hostPath?). NetworkPolicy — непрерывный барьер на dataplane на то, с кем работающий под может говорить. Restricted-под без NetworkPolicy всё равно достаёт весь кластер; жёстко зафайрволенный под под root всё равно сбегает на ноду."
/>

## NetworkPolicy: аддитивный allow и паттерн default-deny

NetworkPolicy **только разрешающая и аддитивная**, ровно как RBAC — deny-правила нет. Трюк, заставляющий её вести себя как файрвол, — это *селектор*: в момент, когда любая NetworkPolicy выбирает под для заданного направления (ingress или egress), этот под перещёлкивается с default-allow на **default-deny для этого направления**, и пропускается только тот трафик, что явно перечислен в любой выбирающей его политике. Объединение всех совпавших политик — это allow-список.

Поэтому каноничный приём хардненинга — это **default-deny** на неймспейс: политика, что выбирает каждый под, но не перечисляет разрешённых соседей:

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Политика</th>
        <th class="border border-border px-3 py-2 text-left">Селектор + направление</th>
        <th class="border border-border px-3 py-2 text-left">Эффект</th>
        <th class="border border-border px-3 py-2 text-left">Грабли при пропуске</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">default-deny-all</td>
        <td class="border border-border px-3 py-2"><code>podSelector: &#123;&#125;</code>, оба <code>Ingress</code> + <code>Egress</code></td>
        <td class="border border-border px-3 py-2">Каждый под в неймспейсе дропает весь трафик, пока не разрешено</td>
        <td class="border border-border px-3 py-2">Плоская сеть — пробитый под достаёт весь кластер</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">allow-dns</td>
        <td class="border border-border px-3 py-2">egress к kube-dns по UDP/TCP 53</td>
        <td class="border border-border px-3 py-2">Даёт подам резолвить имена, когда egress запрещён</td>
        <td class="border border-border px-3 py-2">Каждый под ломается — DNS первым убивает default-deny</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">allow-from-frontend</td>
        <td class="border border-border px-3 py-2">ingress к <code>app: api</code> от <code>app: web</code></td>
        <td class="border border-border px-3 py-2">Только web-ярус может достать API-ярус</td>
        <td class="border border-border px-3 py-2">Любой под бьёт прямо в API в обход края</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">block-metadata</td>
        <td class="border border-border px-3 py-2">egress deny к <code>169.254.169.254/32</code></td>
        <td class="border border-border px-3 py-2">Останавливает кражу облачных кредов через SSRF из любого пода</td>
        <td class="border border-border px-3 py-2">SSRF в одном поде читает IAM-роль / креды ноды</td>
      </tr>
    </tbody>
  </table>
</div>

Неочевидный режим отказа живёт во второй строке. В тот миг, когда ты применяешь default-deny **egress**-политику, ты заодно блокируешь DNS-запросы пода к `kube-dns`, потому что это egress к другому поду на порт 53. Каждый запрос тогда падает не с «connection refused», а с ошибкой *резолва имени*, что отправляет инженеров отлаживать не тот слой на час. Корректный default-deny поэтому всегда *пара*: deny-all плюс явное allow-DNS egress-правило. Это самая частая причина, по которой команды откатывают NetworkPolicy и объявляют её «слишком сложной».

<Inset kind="mistake" lang="ru">
NetworkPolicy, которую `kubectl apply` принимает, — не то же самое, что NetworkPolicy, которая *enforce-ится*. NetworkPolicy — это API-объект, хранимый в etcd; enforcement — работа CNI-плагина, и не каждый CNI её делает. На кластере с голым flannel (или с неверным режимом Cilium/Calico) твой тщательно написанный default-deny сохранён, виден в `kubectl get netpol` и дропает ровно ноль пакетов. Всегда проверяй enforcement эмпирически — `kubectl exec` в под, который политика должна изолировать, и попробуй достать соседа, которого не должна — а не доверяй тому, что объект существует. «Политика применена» и «трафик заблокирован» — два разных утверждения.
</Inset>

## Pod Security Standards: блокировать побег, а не только сеть

NetworkPolicy сдерживает латеральное движение *между* подами. Она ничего не делает с подом, который сбегает *вниз* к ноде — а это разрешает неограниченный SecurityContext. Три побега, что важны, и что делает с каждым профиль `restricted`:

1. **Работа под root.** `runAsNonRoot: true` плюс ненулевой `runAsUser` означает, что побег из контейнера высаживает атакующего под непривилегированным UID, а не root, резко ограничивая, что он может на ноде, если вырвется из namespace.
2. **`privileged: true` и добавленные capabilities.** У privileged-контейнера по сути все capabilities и доступ к устройствам — это почти прямой путь к хосту. `restricted` запрещает `privileged`, требует сбросить `ALL` capabilities и возвращает обратно только `NET_BIND_SERVICE`.
3. **Host-namespaces и `hostPath`.** `hostPID: true` / `hostNetwork: true` делят процессный или сетевой вид ноды; том `hostPath` может примонтировать `/` и прочитать креды kubelet. `restricted` и `baseline` оба это блокируют.

Pod Security Admission enforce-ит профиль по метке неймспейса, в трёх режимах — `enforce` (отклонять несоответствующие поды), `audit` (логировать их) и `warn` (предупреждать применяющего) — так что можно выкатить сперва в `warn`/`audit` и посмотреть, что сломалось бы, перед переключением на `enforce`:

<TradeoffMatrix
  id="04-network-policy-and-pod-security-tradeoff-1"
  lessonSlug="04-network-policy-and-pod-security"
  lang="ru"
  prompt="Ты хардишь неймспейс stateless-подов приложения, которым никогда не нужны root, доступ к хосту или лишние capabilities. Выбери позицию Pod Security."
  options={[
    { name: "Пометь неймспейс `pod-security.kubernetes.io/enforce: restricted`, после выката через `warn`/`audit`, чтобы сперва поймать несоответствующие поды", summary: "Restricted, поэтапный выкат.", correct: true, justification: "restricted блокирует весь класс побегов — runAsNonRoot, drop ALL capabilities, без privileged, без hostPath, seccomp RuntimeDefault — что ровно подходит stateless-приложениям, которым ничего из этого не нужно. Прогон через warn/audit сперва показывает, какие существующие поды были бы отклонены (и даёт починить их SecurityContext) до того, как enforce начнёт дропать деплои. Это наименьшие привилегии в момент admission." },
    { name: "Оставь неймспейс без метки; дефолт кластера и так достаточно безопасен", summary: "Положиться на дефолты.", justification: "Неймспейс без метки получает профиль `privileged` по умолчанию — то есть никаких ограничений вообще. Под может работать под root, privileged, с hostPath. Ничегонеделание оставляет дверь два нараспашку; вся суть в том, что PSA — opt-in." },
    { name: "Enforce `baseline` — он блокирует опасное и реже что-то ломает", summary: "Baseline по умолчанию.", justification: "baseline блокирует худшие побеги (privileged, hostPath, host-namespaces), но всё ещё разрешает работу под root и дефолтный набор capabilities. Для подов, которым доказуемо ничего из этого не нужно, baseline оставляет избегаемый риск на столе — работа под root сама по себе находка. Используй restricted, где нагрузка позволяет; baseline резервируй для подов с реальным ограничением." },
    { name: "Напиши политику OPA/Gatekeeper с нуля вместо PSA", summary: "Только кастомный admission.", justification: "Gatekeeper/Kyverno мощны для правил, которые PSS выразить не может, но тянуться к ним сразу, чтобы воспроизвести то, что встроенный, беззависимостный Pod Security Admission уже делает, — лишняя сложность. Начни с профиля restricted в PSA; добавляй движок политик только для дыр, что он реально не покрывает (например, правила по образу, allow-list реестров)." },
  ]}
/>

<Quiz
  id="04-network-policy-and-pod-security-quiz-1"
  lessonSlug="04-network-policy-and-pod-security"
  lang="ru"
  question="Ты применяешь default-deny `Ingress` + `Egress` NetworkPolicy к неймспейсу, и сразу каждый под падает с ошибками резолва имени. Что произошло?"
  choices={[
    { label: "Запрет egress заодно заблокировал DNS — поды больше не достают kube-dns на порт 53; нужно добавить явное allow-DNS egress-правило", correct: true },
    { label: "NetworkPolicy повредила DNS-записи кластера, и CoreDNS надо перезапустить", misconception: "NetworkPolicy никогда не трогает DNS-записи или состояние CoreDNS; она только фильтрует пакеты на dataplane CNI. Дропаются DNS-*запросы* как egress-трафик, а не повреждаются DNS-данные. Фикс — allow-правило, а не перезапуск CoreDNS." },
    { label: "Политикам default-deny нужен ребут кластера, чтобы вступить в силу, а ошибки временные", misconception: "NetworkPolicy вступает в силу немедленно на dataplane — без ребута. Ошибки не временные; они продержатся, пока ты не разрешишь egress к kube-dns. Считать это временным сбоем — значит отгрузить сломанный неймспейс." },
    { label: "Селектор политики не совпал ни с одним подом, поэтому DNS отключился по всему кластеру", misconception: "Пустой podSelector совпадает с каждым подом в неймспейсе, поэтому default-deny и работает — и эффект ограничен неймспейсом, а не всем кластером. Отказы DNS — ожидаемое следствие запрета egress, а не промах селектора." },
  ]}
/>

<Quiz
  id="04-network-policy-and-pod-security-quiz-2"
  lessonSlug="04-network-policy-and-pod-security"
  lang="ru"
  question="Неймспейс enforce-ит профиль `restricted` Pod Security, но CNI кластера не реализует NetworkPolicy. Под скомпрометирован через RCE. Каков реалистичный следующий ход атакующего?"
  choices={[
    { label: "Латеральное движение по плоской сети — достать другие поды, внутренние API и эндпоинт метаданных — потому что PSS ничего не делает с east-west-трафиком", correct: true },
    { label: "Ничего — `restricted` блокирует всю пост-эксплуатацию, включая сетевой доступ", misconception: "restricted ограничивает то, чем под *является* (non-root, без privileged, без hostPath), что блокирует побег на ноду — но никак не ограничивает, к каким другим подам этот может подключаться. Сетевая достижимость — работа NetworkPolicy, а этот барьер здесь отсутствует." },
    { label: "Повысить до root внутри контейнера и примонтировать hostPath, чтобы добраться до ноды", misconception: "restricted прямо запрещает работу под root, добавленные capabilities, privileged и hostPath — так что путь побега на ноду закрыт. Открытая дверь — сеть, а не SecurityContext." },
    { label: "Уронить API-сервер, ведь restricted-поды получают повышенный доступ к API", misconception: "Профиль restricted про Linux-привилегии пода, а не про его RBAC. Он не даёт никакого доступа к API; это управляется отдельно RBAC у ServiceAccount. Реалистичный ход — сетевое латеральное движение." },
  ]}
/>

<DragOrder
  id="04-network-policy-and-pod-security-drag-1"
  lessonSlug="04-network-policy-and-pod-security"
  lang="ru"
  prompt="Упорядочь шаги, чтобы безопасно захарденить неймспейс без простоя, от первого к последнему:"
  items={[
    { id: "a", label: "Пометь неймспейс `warn`/`audit` для профиля `restricted` и смотри, что было бы отклонено" },
    { id: "b", label: "Почини SecurityContext подов, помеченных audit (runAsNonRoot, сброс capabilities)" },
    { id: "c", label: "Переключи Pod Security на `enforce: restricted`, когда audit чист" },
    { id: "d", label: "Примени default-deny ingress+egress NetworkPolicy плюс явное allow-DNS правило" },
    { id: "e", label: "Добавь узкие allow-правила для реального трафика, затем проверь enforcement через kubectl exec" },
  ]}
  correctOrder={["a", "b", "c", "d", "e"]}
/>

</Explanation>

<KeyTakeaway>У пода две независимые двери default-allow: плоская сеть, где любой под достаёт любой другой, закрываемая аддитивной, только разрешающей NetworkPolicy (пара default-deny — deny-all плюс явное allow-DNS — которая enforce-ится, только если CNI поддерживает её), и неограниченный SecurityContext, способный работать root/privileged/hostPath, закрываемый профилем Pod Security `restricted`, который enforce-ит на неймспейс Pod Security Admission. Закрытие одной ничего не делает для другой; нужны обе, прогнанные через audit/warn перед enforce.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="04-network-policy-and-pod-security-retrieval"
  lang="ru"
  questions={[
    { q: "Пройди шаг за шагом, почему `kubectl apply` NetworkPolicy может успешно пройти и всё равно блокировать ноль трафика, и как бы ты доказал enforcement.", a: "NetworkPolicy — это просто API-объект: kubectl apply валидирует его схему и сохраняет в etcd, и это проходит независимо от того, будет ли что-то на него действовать. Реальная фильтрация пакетов выполняется dataplane CNI-плагина, а enforcement NetworkPolicy опционален в контракте CNI — Calico и Cilium его реализуют, но голый flannel (частый дефолт) — нет. Так что на неверном CNI твой default-deny виден в `kubectl get netpol`, выглядит корректным и не дропает ничего: «применено» и «enforce-ится» — разные утверждения. Чтобы доказать enforcement эмпирически, а не доверять существованию объекта, ты тестируешь dataplane напрямую: kubectl exec в под, который политика должна изолировать, и пробуешь соединение к соседу, которого не должна достать (например, `nc -vz <ip-другого-пода> 5432` или curl к внутреннему сервису) — соединение должно зависнуть/таймаутить, а не пройти. Равно проверь, что allow-путь всё ещё работает (DNS резолвится, разрешённый сосед достижим), чтобы знать, что ты протестировал реальную политику, а не сломанный под. Если «заблокированное» соединение проходит — CNI не enforce-ит NetworkPolicy, и нужен другой CNI или верный режим, а не ещё одна политика." },
    { q: "Почему NetworkPolicy и Pod Security Standards дополняют друг друга, а не дублируют, и что каждый не покрывает?", a: "Они охраняют две ортогональные оси компрометации. NetworkPolicy — непрерывный контроль dataplane над east-west-трафиком: с кем работающий под может соединяться. Она сдерживает латеральное движение — пробитый под не достанет базу, внутренний admin-API или эндпоинт облачных метаданных, если egress запрещён — но ничего не говорит о собственных привилегиях пода, так что под, изолированный NetworkPolicy, всё равно может работать под root с hostPath и сбежать вниз к ноде. Pod Security Standards (enforce-ятся Pod Security Admission в момент create) ограничивают, чем поду позволено быть: профиль restricted форсирует runAsNonRoot, сбрасывает ALL capabilities, запрещает privileged, hostPath и host-namespaces и включает seccomp — закрывая побег контейнер→нода. Но PSA — одноразовый admission-барьер на привилегии и ничего не делает с сетевой достижимостью, так что идеально restricted-под без NetworkPolicy всё равно достаёт каждый другой под в кластере. Атакующему нужна лишь одна открытая дверь: NetworkPolicy без PSS даёт сдержанному поду сбежать к ноде; PSS без NetworkPolicy даёт захардененному поду пивотнуть латерально по плоской сети. Глубокая защита здесь буквально означает применить оба барьера, плюс держать RBAC у ServiceAccount минимальным как третью ось." },
  ]}
/>

<Recap lang="ru">
У свежего пода Kubernetes две независимые двери default-allow. Сеть плоская: каждый под достаёт любой другой по IP через неймспейсы, так что east-west-трафик нараспашку, пока NetworkPolicy его не ограничит. NetworkPolicy аддитивна и только разрешающая (deny-правила нет) — выбор пода перещёлкивает его в default-deny для этого направления, и каноничный хардненинг — это пара default-deny: deny-all ingress+egress плюс явное allow-DNS egress-правило, потому что первое, что ломает наивный запрет egress, — резолв имён к kube-dns. Критично: NetworkPolicy enforce-ится, только если CNI её реализует, так что «применено» надо проверять против «enforce-ится», тестируя dataplane через kubectl exec. Вторая дверь — SecurityContext пода: без ограничения контейнер работает под root с дефолтным набором capabilities и может запросить privileged, hostPath и host-namespaces — каждое примитив побега на ноду. Pod Security Standards определяют профили privileged/baseline/restricted, а Pod Security Admission enforce-ит один на неймспейс через метку, в режимах enforce/audit/warn, так что ты стейджишь выкат перед тем, как он начнёт отклонять поды. Эти два контроля ортогональны: NetworkPolicy сдерживает латеральное движение, PSS блокирует побег на ноду, а компрометации нужна лишь одна открытая дверь — поэтому применяй оба, плюс минимальный RBAC у ServiceAccount. Когда в следующий раз будешь ревьюить неймспейс, три вопроса: есть ли default-deny NetworkPolicy (и enforce-ит ли её CNI), какой профиль Pod Security enforce-ится и может ли любой под всё ещё достать эндпоинт метаданных?
</Recap>
