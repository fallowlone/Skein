---
concepts:
- saga-pattern
- compensating-transaction
- orchestration-step
- idempotency
- isolation-anomalies
- semantic-lock
estMin: 240
lang: ru
level: senior
order: 10
prereqs: []
slug: project
sources:
- https://microservices.io/patterns/data/saga.html
- https://temporal.io/blog/mastering-saga-patterns-for-distributed-transactions-in-microservices
status: ready
summary: "Практический проект — собери оркестрованную сагу заказа на три сервиса, напиши её компенсации, сделай каждый шаг идемпотентным и докажи, что она переживает внедрённые сбои и передоставки."
title: 'Саги: собери отказоустойчивую сагу заказа'
track: distributed
unit: 06-sagas
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import ProjectBrief from "~/components/pedagogy/ProjectBrief.astro";

<Hook>
Читать про компенсации и idempotency — не то же, что смотреть, как сага переживает kill процесса в худший момент. Собери небольшую сагу заказа на три сервиса, потом ломай её специально — вали шаги, передоставляй сообщения, крашь оркестратор посреди процесса — и докажи, что система всегда приходит в согласованное состояние.
</Hook>

<Goal lang="ru">
Преврати модель юнита в работающую сагу: оркеструй три шага с локальными коммитами, напиши реальную обратную компенсацию для каждого, сделай каждый шаг и компенсацию идемпотентными под at-least-once, защитись от аномалии конкурентной саги и проверь всё внедрёнными сбоями, а не happy path.
</Goal>

<ProjectBrief
  id="06-sagas-project"
  lessonSlug="06-sagas"
  lang="ru"
  objective="Реализуй оркестрованную сагу заказа на три сервиса (order, payment, inventory) с durable-состоянием шагов, реальными компенсациями, идемпотентными handler'ами и одной контрмерой изоляции — затем продемонстрируй, что она приходит в согласованное конечное состояние при каждом внедрённом сбое."
  requirements={[
    "Подними три сервиса, каждый со своей базой: order (создаёт заказ, держит статус саги), payment (списывает и возвращает), inventory (резервирует и освобождает товар). Без общей базы и без two-phase commit.",
    "Реализуй оркестратор, ведущий прямой путь — резерв товара (T1), списание карты (T2), подтверждение заказа (T3) — и при любом сбое шага запускающий компенсации в обратном порядке: возврат (C2), освобождение товара (C1). Поставь необратимый шаг (списание) после бронируемого.",
    "Персисти прогресс саги durable (таблица состояния саги или движок durable-execution). После каждого перехода шага оркестратор должен мочь упасть и продолжить ровно с места остановки, а не терять процесс на лету.",
    "Сделай каждый шаг и компенсацию идемпотентными под at-least-once: выведи idempotency key из id саги и шага и дедуплицируй списания/возвраты и резервы/освобождения, чтобы передоставленное сообщение было no-op. Дважды доставленное списание обязано двигать деньги ровно один раз.",
    "Добавь одну контрмеру изоляции для аномалии конкурентной саги: semantic lock (статус заказа PENDING_PAYMENT, который другие саги соблюдают), коммутативное обновление товара (qty -= n, никогда qty = x) или оптимистичную проверку версии. Задокументируй, какую аномалию она предотвращает.",
    "Собери харнесс внедрения сбоев: заставь T2 упасть, заставь компенсацию упасть и быть отретраенной, передоставь сообщение шага дважды и убей процесс оркестратора между двумя шагами. Гоняй каждый сценарий из теста или скрипта, а не руками.",
    "После каждого внедрённого сбоя проверяй, что конечное состояние согласовано по всем трём базам — нет списанных денег без подтверждённого товара, нет товара в резерве под проваленный заказ, ровно один возврат на проваленное списание.",
  ]}
  acceptance={[
    "Таблица сценариев: для каждого внедрённого сбоя (падение шага, retry компенсации, дубль доставки, краш посреди саги) — зафиксированное конечное состояние всех трёх сервисов и pass/fail по инварианту согласованности.",
    "Логи или трасса, показывающие, что передоставленное сообщение списания обнаружено и пропущено — сервис payment записывает ровно одно списание на сагу.",
    "Доказательство, что оркестратор продолжается после kill: сага, упавшая посреди процесса, при рестарте либо завершается, либо полностью компенсируется, без осиротевшего резерва или списания.",
    "Короткий разбор, называющий для каждого шага его компенсацию и является ли она настоящим откатом или приближением, плюс одну аномалию изоляции, которую предотвращает твоя контрмера, и почему.",
  ]}
  stretch={[
    "Добавь политику таймаутов и ретраев с ограничением попыток к каждому шагу, затем dead-letter путь: шаг, исчерпавший ретраи, запускает компенсацию всей саги, — и покажи, что это работает при недоступном downstream-сервисе.",
    "Перепиши оркестрацию как choreography (сервисы реагируют на события без координатора) и напиши абзац, что стало сложнее — трассировка застрявшей саги, добавление шага, персист состояния долгого ожидания.",
    "Добавь шаг ручного одобрения, который может приостановить сагу на произвольное время, и покажи, что durable-состояние переживает полный рестарт всех сервисов, пока сага запаркована.",
    "Добавь job сверки, который сканирует саги, застрявшие в нетерминальном состоянии за пределами SLA, и либо до-гоняет, либо компенсирует их, имитируя on-call инструмент, который реально понадобится в проде.",
  ]}
/>

<Recap lang="ru">
Это сага, которую ты реально соберёшь в проде, в миниатюре: локальные коммиты без глобальной транзакции, реальная обратная компенсация для каждого шага, необратимый шаг последним, durable-прогресс, чтобы краш продолжился, а не осиротил работу, идемпотентные handler'ы, потому что доставка at-least-once, и одна контрмера изоляции уровня приложения для конкурентных саг. Доказать это внедрёнными сбоями — а не happy path — и есть то, что превращает «я читал про саги» в «я прогнал одну через отказ и видел, как она пришла к согласованности».
</Recap>
