---
concepts:
- compensating-transaction
- orchestration-step
- idempotency
- semantic-lock
- saga-pattern
estMin: 14
lang: ru
level: senior
order: 9
prereqs: []
slug: quiz-code
sources:
- https://microservices.io/patterns/data/saga.html
- https://temporal.io/blog/mastering-saga-patterns-for-distributed-transactions-in-microservices
status: ready
summary: "Читай реальные сниппеты саги — compensating action, шаг оркестратора и повторно вызванный handler, — предскажи сбой и выбери фикс с наибольшим рычагом."
title: 'Саги: чтение кода'
track: distributed
unit: 06-sagas
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Баги саги в коде не выглядят как баги саги — они выглядят как обычный handler, который при retry списывает дважды, или как compensation, которая ждёт rollback, что никогда не случится. Прочитай каждый сниппет и выбери фикс, который сеньор сделал бы до релиза.
</Hook>

<Goal lang="ru">
Потренируй чтение, которое делаешь в каждом ревью саги: заметь отсутствующий idempotency key, compensation, которая на деле rollback в маскировке, и шаг оркестратора, теряющий процесс на лету при краше.
</Goal>

## Сниппет 1 — compensating action

```go
// T2 уже закоммитилась: карта списана и записана строка платежа.
// C2 должна отменить это после падения более позднего шага.
func compensateCharge(ctx context.Context, orderID string) error {
    // удаляем строку платежа, чтобы выглядело, будто списания не было
    return db.Exec(ctx, "DELETE FROM payments WHERE order_id = $1", orderID)
}
```

<Quiz
  id="06-sagas-code-1"
  lessonSlug="06-sagas"
  lang="ru"
  question="Почему эта compensation неверна и что она должна делать вместо этого?"
  choices={[
    { label: "Удаление строки притворяется, что списания не было, но деньги уже прошли через шлюз. Compensation должна быть реальным обратным действием — вызвать шлюз для возврата и записать строку refund, — а не стирать локальную запись о реально произошедшем списании.", correct: true },
    { label: "Всё нормально — как только локальная строка платежа удалена, сага снова согласована.", misconception: "Согласованность с внешним миром, а не только с локальной таблицей. Шлюз всё ещё держит деньги клиента; удаление строки лишь прячет списание и ломает сверку." },
    { label: "Баг в том, что DELETE должен быть UPDATE ... SET status = 'voided'.", misconception: "Мягкое удаление — лучше для учёта, но деньги назад не возвращает. Compensation — это новое прямое действие против шлюза, а не правка локальной строки." },
    { label: "Надо откатить исходную транзакцию T2, а не удалять.", misconception: "Откатывать нечего — T2 закоммитилась durable давно. Единственный откат для реального списания — реальный возврат." },
  ]}
/>

## Сниппет 2 — шаг оркестратора

```python
# Оркестратор ведёт сагу вперёд, шаг за шагом, в памяти.
def run_order_saga(order):
    book_flight(order)          # T1
    book_hotel(order)           # T2
    try:
        charge_card(order)      # T3
    except StepFailed:
        cancel_hotel(order)     # C2
        cancel_flight(order)    # C1
    # прогресс живёт только в этом кадре вызова
```

<Quiz
  id="06-sagas-code-2"
  lessonSlug="06-sagas"
  lang="ru"
  question="Процесс оркестратора падает сразу после возврата book_hotel, но до charge_card. В чём сбой и что его чинит?"
  choices={[
    { label: "Прогресс саги живёт только в стеке вызова, так что краш теряет процесс на лету: рейс и отель забронированы без записи, что сагу надо продолжить или компенсировать. Персисти каждый переход шага durable (saga log или движок durable-execution), чтобы процесс продолжился ровно с места остановки.", correct: true },
    { label: "Ничего не ломается — при рестарте Python заново выполнит run_order_saga с начала.", misconception: "Рестарт не входит заново в функцию, бывшую посреди выполнения; кадр вызова исчез. Без персистентного состояния забронированные рейс и отель осиротеют." },
    { label: "Баг в том, что блок except должен ещё ретраить charge_card перед компенсацией.", misconception: "Политика ретраев — отдельная забота. Краш здесь случается между шагами, так что даже идеальный retry не запустится — настоящий дефект это потерянное состояние в памяти." },
    { label: "Завернуть всю функцию в одну транзакцию базы, чтобы краш её откатил.", misconception: "Шаги охватывают разные сервисы и их базы; ни одна транзакция их не покрывает. Это и есть причина существования саги. Фикс — durable состояние шага, а не глобальная транзакция." },
  ]}
/>

## Сниппет 3 — идемпотентный шаг

```go
// Доставка at-least-once, так что этот handler может быть вызван
// больше одного раза для одного шага саги.
func handleChargeCard(msg ChargeMsg) error {
    amount := msg.Amount
    if err := gateway.Charge(msg.CustomerID, amount); err != nil {
        return err
    }
    return savePaymentRow(msg.OrderID, amount)
}
```

<Quiz
  id="06-sagas-code-3"
  lessonSlug="06-sagas"
  lang="ru"
  question="При доставке at-least-once у этого handler реальный денежный баг. В чём он и каков минимальный фикс?"
  choices={[
    { label: "Повторно доставленное сообщение списывает с карты дважды. Шаг не идемпотентен — сделай его таким: выведи idempotency key из id саги/шага и передай в gateway.Charge (шлюзы дедуплицируют по нему) или проверяй существующую строку платежа перед списанием.", correct: true },
    { label: "Бага нет — шлюз автоматически отклонит второе списание как дубликат.", misconception: "Шлюзы дедуплицируют, только когда ты дал им idempotency key. Без него два одинаковых вызова Charge — это два разных списания. Handler обязан передать ключ." },
    { label: "Фикс — переключить брокер на доставку exactly-once.", misconception: "Сквозной exactly-once через сеть — во многом миф; брокеры дают at-least-once, а ты делаешь consumer'ы идемпотентными. Фикс живёт в handler, а не в настройке брокера." },
    { label: "Сначала сохранить строку платежа, потом списать, чтобы строка защищала от повторного списания.", misconception: "Переупорядочивание всё равно гонка: две повторные доставки могут обе пройти проверку до того, как любая запишет. Нужна атомарная дедупликация — уникальный idempotency key, который обеспечит шлюз или уникальное ограничение в базе." },
  ]}
/>

## Сниппет 4 — semantic lock

```sql
-- Сага A начинает работать с заказом. Сага B может тронуть тот же
-- заказ одновременно, потому что между шагами саги ничего не заблокировано.
UPDATE orders SET status = 'PENDING_PAYMENT' WHERE id = $1;
-- ... позже выполняются шаги саги A, затем при успехе ...
UPDATE orders SET status = 'CONFIRMED' WHERE id = $1;
```

<Quiz
  id="06-sagas-code-4"
  lessonSlug="06-sagas"
  lang="ru"
  question="Что здесь делает статус PENDING_PAYMENT и что должны делать другие саги, чтобы это работало?"
  choices={[
    { label: "Это semantic lock — маркер уровня приложения, что заказ посреди саги. Другие саги обязаны проверять статус и отказываться или откладывать работу над заказом PENDING_PAYMENT, потому что база не держит реальной блокировки между шагами саги, чтобы их остановить.", correct: true },
    { label: "Это просто поле аудита; блокировка строки во время UPDATE уже защищает заказ.", misconception: "Блокировка строки живёт только на время одного UPDATE, а не на все шаги саги. Статус — это межшаговая блокировка, но лишь если другие саги её действительно соблюдают." },
    { label: "Он гарантирует, что ни одна другая сага не прочитает и не запишет заказ, пока он не станет CONFIRMED.", misconception: "Сам по себе он ничего не гарантирует — он рекомендательный. Принуждение в коде приложения, который читает статус и решает пропустить или подождать." },
    { label: "Он снова делает сагу ACID-изолированной, сериализуя доступ на уровне базы.", misconception: "Semantic lock приближает изоляцию в логике приложения; он не возвращает изоляцию уровня базы. Саги остаются ACID минус I — маркер лишь позволяет кооперирующим сагам избегать друг друга." },
  ]}
/>

<Recap lang="ru">
Каждый дефект саги в этом наборе — известная форма: compensation, которая удаляет локальную запись вместо реального обратного действия; оркестратор, держащий прогресс только в памяти и теряющий процесс при краше; шаг, списывающий дважды, потому что at-least-once встретился с неидемпотентным handler; и поле статуса, работающее как блокировка, только если каждая сага его соблюдает. Читай на отсутствующий idempotency key, на compensation-в-маскировке-под-rollback и на неперсистентный шаг — там и живут баги саги.
</Recap>
