---
concepts:
- quorum-replication
- fencing-token
- saga-compensation
- retry-amplification
- idempotency-key
estMin: 14
lang: ru
level: senior
order: 9
prereqs: []
slug: quiz-code
sources:
- https://martin.kleppmann.com/2016/02/08/how-to-do-distributed-locking.html
- https://aws.amazon.com/builders-library/timeouts-retries-and-backoff-with-jitter/
- https://microservices.io/patterns/data/saga.html
status: ready
summary: "Читай реальный конфиг и код со всего трека — размер кворума, проверка fencing, компенсация саги, backoff — предскажи провал и выбери фикс с наибольшим рычагом."
title: 'Распределённый капстоун: чтение кода и конфига'
track: distributed
unit: 08-putting-it-together
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Баги композиции ловятся чтением конфига и кода в швах, а не прозы. Прочитай каждый сниппет, предскажи его поведение под сбоем и выбери фикс, который сеньор делает первым.
</Hook>

<Goal lang="ru">
Отработай цикл, который гоняешь в каждом распределённом инциденте: прочитай размер кворума, проверку fencing, компенсацию и политику ретраев, затем найди одно изменение, делающее шов безопасным.
</Goal>

## Сниппет 1 — конфиг кворума

```yaml
# реплицированное хранилище заказов
replication:
  N: 3          # реплик на ключ
  W: 1          # ack-ов для подтверждения записи
  R: 1          # реплик читается на get
```

<Quiz
  id="08-putting-it-together-code-1"
  lessonSlug="08-putting-it-together"
  lang="ru"
  question="При N=3, W=1, R=1 что чтение гарантирует о только что подтверждённой записи и как починить это для read-your-writes?"
  choices={[
    { label: "Ничего: W=1 и R=1 дают R + W = 2, не больше N=3, так что кворумы чтения и записи могут не пересекаться — чтение может попасть на две реплики, не видевшие запись. Поставь W=2, R=2 (R + W > N), чтобы каждое чтение пересекало последнюю запись.", correct: true },
    { label: "Безопасно, потому что запись подтверждена; подтверждение значит, что все реплики её имеют.", misconception: "W=1 подтверждает после принятия записи одной репликой. Две другие могут её ещё не иметь, а R=1 чтение может лечь на одну из них — подтверждение не значит репликацию на пересекающий чтение кворум." },
    { label: "Это нормально для долговечности; поднятие W и R лишь добавляет латентность без выигрыша в корректности.", misconception: "W=1 ещё и рискует потерять запись, если та одна реплика упадёт до распространения. R + W > N — ровно свойство корректности: оно даёт пересечение read-your-write и долговечность, а не только цену латентности." },
    { label: "Поставь N=1, чтобы реплика была одна и вопрос исчез.", misconception: "N=1 убирает отказоустойчивость целиком — потеря одной ноды теряет данные. Цель — пересечение с избыточностью: держи N=3 и ставь W=2, R=2." },
  ]}
/>

## Сниппет 2 — проверка fencing

```python
# ресурс, охраняющий шаг саги; вызывается тем, кто считает себя лидером
highest_token_seen = 0

def apply_step(step, token):
    global highest_token_seen
    # принять запись, затем запомнить токен
    write(step)
    highest_token_seen = max(highest_token_seen, token)
    return "ok"
```

<Quiz
  id="08-putting-it-together-code-2"
  lessonSlug="08-putting-it-together"
  lang="ru"
  question="Приостановленный старый лидер просыпается с token=7, пока новый уже применил шаги при token=12. Что делает этот код и каков фикс?"
  choices={[
    { label: "Он применяет устаревшую запись до проверки токена, затем выставляет highest через max() — так шаг token=7 старого лидера повреждает состояние, продвинутое лидером token=12. Фикс: отвергать сначала — если token меньше highest_token_seen, вернуть ошибку и не писать.", correct: true },
    { label: "Безопасно, потому что max() держит highest_token_seen на 12, так что устаревший токен игнорируется.", misconception: "max() обновляет учёт, но лишь после того, как write(step) уже выполнился. Повреждающая запись происходит в любом случае; токен надо проверить и отвергнуть до записи, а не записывать после неё." },
    { label: "Баг в том, что token — int; fencing-токены должны быть UUID, чтобы быть монотонными.", misconception: "Монотонные целые — канонический fencing-токен. Дефект в порядке — запись до валидации — а не в типе токена. UUID вообще нельзя было бы сравнить на монотонность." },
    { label: "Бага нет; новый лидер просто перезапишет устаревший шаг своей следующей записью.", misconception: "Полагаться на позднюю запись для починки повреждения — это гонка, а не гарантия: устаревший шаг может быть прочитан или вызвать эффекты до любой перезаписи. Fencing должен отвергнуть устаревшую запись напрямую." },
  ]}
/>

## Сниппет 3 — компенсация саги

```python
# компенсация для отменённого заказа; вызывается слоем ретраев по таймауту
def refund(order_id, amount):
    # ключ идемпотентности не протянут
    charge_id = payment_api.create_refund(order_id=order_id, amount=amount)
    return charge_id
```

<Quiz
  id="08-putting-it-together-code-3"
  lessonSlug="08-putting-it-together"
  lang="ru"
  question="Возврат первого вызова прошёл, но его ответ потерян; слой ретраев вызывает refund() снова в рамках бюджета. Что происходит и каков единственный фикс с наибольшим рычагом?"
  choices={[
    { label: "create_refund выполняется второй раз, и клиенту возвращают деньги дважды — шов между слоем ретраев и платежом не несёт общего ключа. Фикс: вывести стабильный ключ (refund:{order_id}) и передать его, чтобы платёжный сервис дедуплицировал и вернул результат первого возврата на повторе.", correct: true },
    { label: "Ничего плохого не случится, потому что сага выпускает одну компенсацию на заказ.", misconception: "Сага выпускает одну компенсацию, но слой ретраев заново вызывает её по таймауту — в этом вся суть. Одна логическая компенсация становится двумя физическими вызовами, и без ключа второй возвращает деньги снова." },
    { label: "Отработает безопасно, потому что create_refund вернёт ошибку на дублирующем order_id.", misconception: "Передача order_id — не ключ идемпотентности: у API нет записи, связывающей этот вызов с первой попыткой, так что он создаёт свежий возврат. Делает повтор no-op именно выделенный, записанный ключ идемпотентности." },
    { label: "Просто убери ретрай на компенсациях, чтобы refund() вызывался лишь один раз.", misconception: "Тогда реально потерянный запрос возврата никогда не завершится, и отменённый заказ молча оставит деньги клиента — неверное число в другую сторону. Ретраи нужны; им нужен ключ, а не удаление." },
  ]}
/>

## Сниппет 4 — политика ретраев

```python
def call_with_retry(fn, attempts=6):
    for i in range(attempts):
        try:
            return fn()
        except Timeout:
            # фиксированная задержка, без jitter, без бюджета
            sleep(1.0)
    raise
```

<Quiz
  id="08-putting-it-together-code-4"
  lessonSlug="08-putting-it-together"
  lang="ru"
  question="Сервис вниз по стеку мигает на две секунды под нагрузкой. Тысячи вызывающих гоняют ровно эту политику. Что даёт фиксированный sleep 1.0s и что меняет сеньор?"
  choices={[
    { label: "Thundering herd: каждый вызывающий повторяет в одном ритме 1s, так что ретраи приходят синхронными волнами, заново насыщают восстанавливающийся сервис и усиливают краткий миг в отказ. Фикс: экспоненциальный backoff плюс jitter, ограниченный бюджетом ретраев (~10% сверх нормального темпа).", correct: true },
    { label: "Это нормально — задержка 1s консервативна и даёт сервису время восстановиться.", misconception: "Дело не в длине задержки, а в синхронизации. Фиксированная задержка заставляет всех повторять в ногу, так что восстанавливающийся сервис бьют выровненные волны. Jitter их рассинхронизирует." },
    { label: "Баг в том, что 6 попыток мало; подними их, чтобы кратковременные сбои всегда разгребались.", misconception: "Больше попыток ещё сильнее усиливают herd и съедают больше нагрузки во время сбоя. Фикс — форма ретраев (backoff, jitter и бюджет), а не их количество." },
    { label: "Добавь более длинный фиксированный sleep (5s), чтобы волны были разнесены дальше.", misconception: "Более длинная фиксированная задержка всё равно синхронизирует всех вызывающих; она лишь сдвигает волну позже. Только случайный jitter ломает выравнивание, и только бюджет ограничивает суммарную нагрузку ретраев." },
  ]}
/>

<Recap lang="ru">
Каждый распределённый инцидент читается в шве: кворум с R + W не больше N молча отдаёт устаревшие чтения; fencing, который пишет до проверки токена, даёт устаревшему лидеру повредить состояние; компенсация, вызванная слоем ретраев без общего ключа, дважды возвращает деньги; а ретрай с фиксированной задержкой без jitter и бюджета превращает миг в herd. Прочитай конфиг и код, почини шов — добавь пересечение, отвергай до записи, протяни ключ, добавь jitter и бюджет — затем проверь под инъекцией сбоев.
</Recap>
