---
concepts:
- composition-failure
- idempotency-key
- fencing-token
- quorum-replication
- saga-compensation
- retry-amplification
estMin: 13
lang: ru
level: senior
order: 7
prereqs: []
slug: quiz-choice
sources:
- https://martin.kleppmann.com/2016/02/08/how-to-do-distributed-locking.html
- https://microservices.io/patterns/data/saga.html
- https://aws.amazon.com/builders-library/timeouts-retries-and-backoff-with-jitter/
status: ready
summary: "Кросс-трековая синтез-проверка с выбором ответа — как кворум, выборы лидера, часы, саги и ретраи соединяются в один пайплайн, и где текут швы."
title: 'Распределённый капстоун: проверка с выбором ответа'
track: distributed
unit: 08-putting-it-together
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Шесть вопросов, прорезающих весь трек. Каждый — решение, которое дежурный инженер принимает, когда пайплайн из по отдельности корректных сервисов всё равно выдаёт неверное число в выписке клиента. Ни один из них не определение для зубрёжки.
</Hook>

<Goal lang="ru">
Убедись, что умеешь соединить кворумную репликацию, выборы лидера с fencing, логические часы, компенсации саги и дисциплину ретраев в одну систему — и найти шов, где два корректных слоя складываются в баг.
</Goal>

<Quiz
  id="08-putting-it-together-choice-1"
  lessonSlug="08-putting-it-together"
  lang="ru"
  question="Пайплайн заказа из четырёх сервисов иногда дважды возвращает деньги клиенту. Каждый сервис проходит свои юнит-тесты; платёжный идемпотентен для списаний; сага образцовая; у ретраев есть backoff и jitter. Где живёт баг?"
  choices={[
    { label: "В шве: слой ретраев заново вызвал компенсацию-возврат по вызову без общего ключа идемпотентности, и получатель принял ретрай за новый запрос. Ни один слой не ошибочен — дефект в том, как два корректных слоя складываются.", correct: true },
    { label: "Внутри пути возврата платёжного сервиса, где наверняка скрытая гонка, которую тесты упускают.", misconception: "Путь возврата проходит тесты, а сервис идемпотентен для списаний. Провалы композиции — ровно те, что не ловит ни один отдельный компонент, потому что каждый слой корректен в изоляции. Баг в шве, а не в слое." },
    { label: "В брокере сообщений, который доставляет дубликаты, хотя обещал их не доставлять.", misconception: "Брокеры по дизайну доставляют at-least-once; дубликаты — это ожидаемо, а не баг брокера. Пайплайн обязан быть безопасен под дубликаты через ключи идемпотентности, а не полагаться на то, что брокер их предотвратит." },
    { label: "В конфиге кворума: R + W задано больше N, что вызывает двойные записи.", misconception: "R + W > N — это свойство, которое ты хочешь: оно даёт пересечение для read-your-write и долговечность. Оно управляет видимостью реплик, а не тем, выстрелит ли повторно компенсация." },
  ]}
/>

<Quiz
  id="08-putting-it-together-choice-2"
  lessonSlug="08-putting-it-together"
  lang="ru"
  question="Оркестратор саги работает как избранный лидер. Он попадает в долгую паузу GC, его lease истекает, выбирается новый лидер — и тут старый просыпается и пытается записать шаг саги. Что реально мешает ему повредить состояние, которое новый лидер уже продвинул?"
  choices={[
    { label: "Fencing-токен: каждая запись вниз несёт монотонно растущее число, и ресурс отвергает любой токен меньше наибольшего, который уже принял.", correct: true },
    { label: "Старый лидер проверяет, лидер ли он всё ещё, перед записью.", misconception: "Он не может надёжно это знать — в этом и вся проблема. Приостановленный процесс считает, что время не шло, и может думать, что всё ещё держит lease. Безопасен только ресурс, проверяющий fencing-токен против устаревшего приостановленного лидера." },
    { label: "Истечение lease гарантирует, что старый лидер перестал действовать.", misconception: "Lease ограничивает wall-clock время, но приостановленный процесс не знает, что время прошло. Точка Клеппманна: без fencing-токена устаревший лидер может проснуться после истечения lease и всё равно выдать запись." },
    { label: "Экспоненциальный backoff откладывает запись старого лидера, пока она не станет безопасной.", misconception: "Backoff разносит ретраи по времени; он никак не отвергает запись от ноды, которая больше не должна писать. Отвергать устаревшую запись — задача fencing-токена, а не политики ретраев." },
  ]}
/>

<Quiz
  id="08-putting-it-together-choice-3"
  lessonSlug="08-putting-it-together"
  lang="ru"
  question="Коллега предлагает упорядочивать шаги саги между сервисами сравнением wall-clock меток (Date.now()), чтобы держать порядок 'Payment до Shipping'. Почему сеньор это отвергает и какой механизм верный?"
  choices={[
    { label: "Wall-clock на разных хостах дрейфуют и могут идти назад (коррекции NTP), так что более позднее событие может нести более раннюю метку — причинный порядок не есть сравнение часов. Используй логические часы (или TrueTime с ограниченной неопределённостью), чтобы упорядочить шаги как причинный факт.", correct: true },
    { label: "Date.now() слишком медленный на горячем пути; используй монотонный счётчик ради скорости.", misconception: "Дело не в производительности. Проблема в корректности: физические часы на разных хостах не синхронизированы достаточно туго, чтобы установить причинный порядок, как бы быстро ни был вызов." },
    { label: "Это нормально, пока все хосты гоняют NTP, который держит часы идентичными.", misconception: "NTP в лучшем случае ограничивает рассинхрон десятками миллисекунд и может шагнуть часами назад. Упорядочивание межсервисных событий на субсекундной гранулярности на это полагаться не может; нужен логический порядок." },
    { label: "Wall-clock упорядочивание работает, но только если все сервисы в одном дата-центре.", misconception: "Один дата-центр сужает рассинхрон, но никогда его не устраняет, и шаги часов всё равно случаются. Совместное размещение уменьшает — но не убирает — потребность в логических часах или часах с ограниченной неопределённостью." },
  ]}
/>

<Quiz
  id="08-putting-it-together-choice-4"
  lessonSlug="08-putting-it-together"
  lang="ru"
  question="Почему идемпотентность называют несущим примитивом пайплайна, а не просто одной из защит среди многих?"
  choices={[
    { label: "Распределённые системы доставляют at-least-once: вызов, отвалившийся по таймауту, мог пройти, и вызывающий никогда не уверен. Раз 'случилось ли это?' непознаваемо, сделать 'сделать снова' безвредным — вот что позволяет безопасно опираться на ретраи, доставку at-least-once и компенсации.", correct: true },
    { label: "Потому что она полностью убирает потребность в ретраях — идемпотентную операцию никогда не надо повторять.", misconception: "Идемпотентность не убирает ретраи; она делает их безопасными. Потерянному запросу всё равно нужен ретрай, чтобы завершиться. Идемпотентность гарантирует отсутствие лишнего эффекта у ретрая, а не отсутствие самого ретрая." },
    { label: "Потому что она даёт использовать exactly-once доставку, которую тогда гарантирует брокер.", misconception: "Доставка exactly-once по сети невозможна. То, что зовут 'exactly-once' — это доставка at-least-once плюс идемпотентная обработка по dedup-id: сеть даёт дубликаты, ключ даёт exactly-once эффекты." },
    { label: "Потому что она делает любую операцию от природы повторяемой, как инкремент счётчика.", misconception: "'decrement stock by 1' от природы повторяема и всё равно неверна при повторе. Идемпотентность — не природная повторяемость, а стабильный бизнес-ключ, который получатель записывает, чтобы второе прибытие было no-op, возвращающим первый результат." },
  ]}
/>

<Quiz
  id="08-putting-it-together-choice-5"
  lessonSlug="08-putting-it-together"
  lang="ru"
  question="Шторм ретраев усиливает мелкий сбой вниз по стеку до полного отказа: каждый слой повторяет слой под собой. Каков структурный фикс и какое число его якорит?"
  choices={[
    { label: "Бюджет ретраев, ограничивающий повторы примерно 10% сверх нормального темпа запросов (с backoff + jitter и circuit breaker), чтобы сбой не множил нагрузку. Когда бюджет почти израсходован на 100%, ретраи сбрасываются вместо усиления.", correct: true },
    { label: "Увеличить таймаут везде, чтобы у вызовов было больше времени на успех до повтора.", misconception: "Большие таймауты дольше держат соединения открытыми и могут усугубить шторм, накапливая запросы в полёте. Они не ограничивают множение нагрузки; только бюджет ограничивает, насколько ретраи могут добавить." },
    { label: "Повторять агрессивнее без backoff, чтобы запросы быстрее разгребали очередь.", misconception: "Отсутствие backoff — ровно то, что превращает кратковременный сбой в thundering herd: синхронные мгновенные повторы усиливают нагрузку. Backoff с jitter разносит ретраи; бюджет ограничивает их сумму." },
    { label: "Добавить реплик, чтобы сервис внизу впитал ретраи.", misconception: "Масштабирование ёмкости откладывает коллапс, но шторм масштабируется с трафиком и снова насыщает новую ёмкость. Долговечный фикс ограничивает усиление ретраев у источника бюджетом, а не впитыванием." },
  ]}
/>

<Quiz
  id="08-putting-it-together-choice-6"
  lessonSlug="08-putting-it-together"
  lang="ru"
  question="Каждый дашборд сервиса зелёный, а пайплайн молча подводит клиентов. За каким набором сигналов сеньор реально следит, чтобы ловить провалы композиции?"
  choices={[
    { label: "За швами между слоями: consumer lag (саги стопорятся), p99 кворумной записи/чтения (медленная третья реплика тянет хвост), leader churn (выборов в минуту, близость к split-brain) и исчерпание бюджета ретраев. Пайплайн из здоровых сервисов всё равно может подводить.", correct: true },
    { label: "За CPU, памятью и error rate каждого сервиса — если они зелёные, пайплайн здоров.", misconception: "Это ровно те зелёные дашборды, что скрывают провалы композиции. Каждый компонент в порядке в изоляции; провал живёт между компонентами, чего нодовые метрики на сервис никогда не показывают." },
    { label: "Только за клиентским error rate на краю — это единственная истина.", misconception: "Error rate на краю говорит, что что-то не так, но не где, и отстаёт от причины. Сигналы швов (consumer lag, leader churn, бюджет ретраев) позволяют поймать и локализовать провал до того, как он дойдёт до края." },
    { label: "За агрегированным throughput запросов — пока он держится, бага композиции нет.", misconception: "Двойной возврат или застрявшая сага не двигают агрегированный throughput. Throughput слеп к провалам корректности-в-шве; алертить надо прямо на межслойные сигналы." },
  ]}
/>

<Recap lang="ru">
Сквозная линия трека — одно дерево решений: кворум делает запись долговечной, лидер с fencing-токенами делает координацию безопасной для single-writer, логические часы упорядочивают шаги причинно, саги откатывают компенсациями, потому что распределённого ACID нет, а ретраям нужен бюджет, чтобы сбой не усилился. Идемпотентность по бизнес-намерению — несущий примитив, делающий at-least-once безопасным повсюду. Реальные провалы живут в швах — ретрай, заново стреляющий компенсацией без общего ключа — и ловишь ты их на сигналах швов, а не на зелёных дашбордах на сервис. Теперь, когда окажешься на инциденте, где каждый сервис здоров, — первый вопрос: какие два корректных слоя соприкасаются в шве и есть ли между ними общий ключ?
</Recap>
