---
slug: 01-overview
lang: ru
track: distributed
unit: 08-putting-it-together
order: 1
level: junior
lessonType: topic
status: ready
title: "Собираем всё вместе: пайплайн заказа, где протекают швы"
summary: "Каждый примитив — кворум, выборы лидера, часы, саги, ретраи — корректен по отдельности. Капстоун собирает их в один пайплайн заказа/оплаты и показывает баг композиции двух корректных слоёв: компенсация в гонке с ретраем даёт двойной возврат."
prereqs: []
deepensInto: []
spiral: []
concepts: ["idempotency-key", "saga-compensation", "retry-budget", "fencing-token", "quorum-replication", "composition-failure"]
estMin: 17
sources:
  - https://microservices.io/patterns/data/saga.html
  - https://aws.amazon.com/builders-library/timeouts-retries-and-backoff-with-jitter/
  - https://aws.amazon.com/blogs/architecture/exponential-backoff-and-jitter/
  - https://martin.kleppmann.com/2016/02/08/how-to-do-distributed-locking.html
---

import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";

<Hook>
Клиенту вернули деньги дважды за один отменённый заказ. Финансы помечают это; дежурный инженер вытягивает трейс. Платёжный сервис идемпотентен, оркестратор саги — образцовый, у политики ретраев есть backoff и jitter — каждый слой проходит свои юнит-тесты. Баг не живёт ни в одном из них. Оркестратор выпустил компенсацию `refund`, вызов отвалился по таймауту на 30s, хотя возврат на самом деле прошёл, в бюджете ретраев было место — и слой повторил вызов, а у шага возврата не было ключа идемпотентности. Два корректных слоя при композиции дали неверное число в выписке реального клиента.
</Hook>

<Crux>Каждый примитив корректен по отдельности; провалы живут в швах, где два корректных слоя соединяются без общего ключа идемпотентности.</Crux>

<Explanation>

## Система: один пайплайн заказа, шесть примитивов

Всё в этом треке было примитивом по отдельности. Капстоун — одна реалистичная система, использующая их сразу: пайплайн заказа/оплаты на четыре сервиса — Order, Payment, Inventory, Shipping — координируемый сагой. Пройди по запросу, и каждый прошлый урок всплывает как слой:

- **Кворумная репликация** (`R + W > N`): сервис Order пишет в реплицированное хранилище. При `N=3, W=2, R=2` запись, легшая на 2 из 3 реплик, переживает потерю одной ноды, и любое последующее чтение её пересекает. Именно это делает «заказ создан» долговечным.
- **Выборы лидера + fencing-токены**: оркестратор саги работает как единственный лидер, чтобы два координатора не вели один заказ. Когда лидер замирает (GC, сетевой раздел) и выбирается новый, старый может проснуться и всё ещё действовать — поэтому каждая запись вниз несёт монотонный **fencing-токен**, и ресурсы отвергают любой токен меньше наибольшего виденного.
- **Часы / упорядочивание**: нельзя доверять wall-clock меткам для упорядочивания событий между сервисами. Логические часы (или TrueTime Spanner с ограниченной неопределённостью) упорядочивают шаги саги; «Payment произошёл до Shipping» должен быть причинным фактом, а не сравнением `Date.now()`.
- **Саги + компенсации**: распределённой ACID-транзакции на четыре сервиса не существует. Сага идёт прямыми шагами и при сбое запускает **компенсации** — семантические откаты (возврат, возврат на склад), а не rollback.
- **Дисциплина ретраев**: каждый межсервисный вызов повторяется с экспоненциальным backoff и jitter, под бюджетом ретраев, за circuit breaker.

Каждый слой корректен. Интересные провалы не внутри слоёв — они в том, как слои соединяются.

## Идемпотентность — несущий примитив

Доставка at-least-once и ретраи — дефолтная реальность распределённых систем: вызов, отвалившийся по таймауту, мог пройти, и со стороны вызывающего этого не отличить. Единственный безопасный ответ на «случилось ли это?», когда узнать нельзя, — сделать «сделать снова» безвредным. Это и есть идемпотентность, и именно она делает безопасными все остальные слои пайплайна.

Ключевое слово — **ключ**. Идемпотентность — это не «операция от природы повторяема»: `decrement stock by 1` не идемпотентна. Это «операция несёт стабильный бизнес-ключ, а получатель записывает этот ключ, так что второе прибытие *того же* ключа — это no-op, возвращающий первый результат». Ловушка, за которой следит сеньор: ключ привязан не к тому. Привяжешь к id HTTP-запроса — и ретрай, сгенерированный *другим* слоем (сага vs SDK vs клиент), получит свежий id и проскользнёт. Ключ должен выводиться из бизнес-намерения — `refund:order-8842:attempt-1` — и протягиваться через каждый слой, который может повторить вызов.

<Inset kind="why" lang="ru">
«At-least-once» и «exactly-once» — это не два режима доставки, между которыми выбираешь. Доставка *exactly-once* по сети невозможна; то, что называют «exactly-once», на деле — доставка at-least-once плюс идемпотентная обработка по dedup-id. Сеть даёт дубликаты; ключ даёт exactly-once *эффекты*. Смешение этих двух — то, как команды выкатывают сагу в вере, что брокер гарантирует отсутствие дубликатов.
</Inset>

## Провал композиции: компенсация в гонке с ретраем

Вот баг из Hook, механически. Сага решает отменить и выпускает компенсацию `refund`:

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">t</th>
        <th class="border border-border px-3 py-2 text-left">Слой саги / ретраев</th>
        <th class="border border-border px-3 py-2 text-left">Платёжный сервис</th>
        <th class="border border-border px-3 py-2 text-left">Эффект</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">0s</td>
        <td class="border border-border px-3 py-2">выпускает компенсацию <code>refund</code></td>
        <td class="border border-border px-3 py-2">получает запрос, начинает возврат</td>
        <td class="border border-border px-3 py-2">—</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">28s</td>
        <td class="border border-border px-3 py-2">ждёт ответа</td>
        <td class="border border-border px-3 py-2">возврат <strong>проходит</strong>, ответ задержан</td>
        <td class="border border-border px-3 py-2">$50 возвращены (1-й раз)</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">30s</td>
        <td class="border border-border px-3 py-2">таймаут; бюджет OK → <strong>ретрай</strong></td>
        <td class="border border-border px-3 py-2">получает 2-й запрос, без dedup-ключа</td>
        <td class="border border-border px-3 py-2">$50 возвращены (2-й раз)</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">31s</td>
        <td class="border border-border px-3 py-2">ретрай возвращает 200</td>
        <td class="border border-border px-3 py-2">—</td>
        <td class="border border-border px-3 py-2"><strong>двойной возврат</strong></td>
      </tr>
    </tbody>
  </table>
</div>

Прогони каждый слой отдельно — он невиновен. Таймаут был разумным. В бюджете ретраев было место, так что ретрай был *правильным* решением политики. Логика компенсации верна — отменённый заказ должен возвращать деньги. Платёжный сервис был идемпотентен для пути *списания*. Дефект целиком в шве: ретрай (один корректный слой) заново вызвал компенсацию (другой корректный слой) через вызов без общего ключа идемпотентности. Чинится протягиванием одного ключа — `refund:order-8842` — от саги через слой ретраев в платёжный сервис, где второе прибытие этого ключа возвращает результат первого возврата вместо выпуска нового. В самих слоях ничего не меняется; *шов* получает общий ключ.

## Наблюдаемость — то, как этим реально управляют

Пайплайн не объявляет о собственном гниении. Провалы композиции прячутся, потому что дашборд каждого компонента зелёный. Систему ведут по сигналам, которые живут *между* слоями:

- **Consumer lag** — насколько отстали консьюмеры событий саги. Растущий lag значит, что пайплайн отстаёт от продюсеров; саги застревают на полпути, а компенсации в полёте копятся.
- **Latency кворумной записи/чтения** — p99 записи `W=2`. Медленная третья реплика тянет хвостовую latency, даже когда ни одна нода технически не «упала».
- **Leader churn** — выборы в минуту. Частые перевыборы значат, что fencing-токены постоянно растут и ты в одной паузе от попытки split-brain записи.
- **Исчерпание бюджета ретраев** — процент потреблённого бюджета. Когда бюджет около 100%, ретраи вот-вот будут *отброшены*, что выходит наружу как видимые пользователю ошибки, хотя каждый сервис вниз здоров. Бюджеты обычно ставят так, чтобы ретраи добавляли максимум ~10% сверх обычного rate запросов — ровно чтобы шторм ретраев не усилил мелкий сбой до аварии.

Инстинкт сеньора: алертить на *швы*, а не только на ноды. Пайплайн из здоровых сервисов всё равно может подводить клиентов.

<TradeoffMatrix
  id="08-putting-it-together-tradeoff-1"
  lessonSlug="01-overview"
  lang="ru"
  prompt="Компенсацию `refund` твоей саги заказа слой ретраев может повторить после таймаута. Где поставить защиту, чтобы двойной возврат стал невозможен?"
  options={[
    { name: "Бизнес-ключ идемпотентности (refund:order-8842), протянутый сага → ретрай → платёж, dedup на получателе", summary: "Общий ключ на шве.", correct: true, justification: "Это единственный фикс, переживающий композицию. Ключ стабилен на каждом слое, который может повторить, и платёжный сервис его записывает: второе прибытие refund:order-8842 возвращает результат первого возврата вместо выпуска нового. Гарантию получает шов — а не отдельный слой, — ровно там, где жил баг. Заодно это делает доставку at-least-once безопасной для опоры везде." },
    { name: "Сделать таймаут длиннее, чтобы ответ всегда приходил до срабатывания ретрая", summary: "Подкрутить часы.", justification: "Больший таймаут сжимает окно гонки, но никогда его не закрывает — ответ всегда может быть на миллисекунду медленнее таймаута, а реально потерянный ответ всё равно вызовет ретрай с двойным возвратом. Таймауты меняют latency на меньший-но-ненулевой шанс двойного срабатывания; безопасной для повтора операцию они не делают." },
    { name: "Поставить бюджет ретраев в ноль для компенсаций, чтобы они не повторялись", summary: "Без ретраев.", justification: "Теперь реально потерянная компенсация (запрос возврата пропал в сети) никогда не завершится, и отменённый заказ молча оставит деньги клиента у себя. Ты сменил двойной возврат на пропавший — тоже неверное число в выписке, просто в другую сторону. Ретраи нужны; им просто нужен ключ." },
    { name: "Сделать платёжный сервис идемпотентным для списаний и считать, что возвраты это наследуют", summary: "Переиспользовать dedup списаний.", justification: "Идемпотентность — на операцию, по ключу. Dedup-таблица для charge:order-8842 ничего не делает для запроса возврата без ключа или с другим. «Сервис идемпотентен» бессмысленно без указания, какая операция и какой ключ — классическая ловушка, выкатывающая ровно этот баг." },
  ]}
/>

<Quiz
  id="08-putting-it-together-quiz-1"
  lessonSlug="01-overview"
  lang="ru"
  question="Каждый сервис в пайплайне заказа проходит свои тесты, но клиенты иногда видят двойные списания. Где баг скорее всего?"
  choices={[
    { label: "В шве — два корректных слоя (например, ретрай, заново вызывающий компенсацию) соединяются без общего ключа идемпотентности", correct: true },
    { label: "Внутри логики списания платёжного сервиса, где должен быть скрытый баг", misconception: "Путь списания проходит тесты и идемпотентен для списаний. Провалы композиции — ровно те, что не ловит ни один набор тестов компонента, потому что каждый компонент корректен по отдельности." },
    { label: "В брокере сообщений, который доставляет дубликаты, хотя обещал не делать этого", misconception: "Брокеры доставляют at-least-once по дизайну; дубликаты ожидаемы, а не баг брокера. Пайплайн должен быть безопасен при дубликатах через ключи идемпотентности, а не полагаться, что брокер их предотвратит." },
    { label: "В конфиге кворума — R + W не должно быть больше N", misconception: "R + W > N — это правило, которого ты *хочешь*; оно даёт пересечение read-your-write. Оно про долговечность реплик, а не про то, дважды ли срабатывает повторённая компенсация." },
  ]}
/>

<Quiz
  id="08-putting-it-together-quiz-2"
  lessonSlug="01-overview"
  lang="ru"
  question="Оркестратор саги работает как выбранный лидер. После долгой паузы GC он просыпается и пытается записать шаг саги. Что мешает ему испортить состояние, которое новый лидер уже продвинул?"
  choices={[
    { label: "Fencing-токен: запись несёт монотонное число, и ресурс отвергает любой токен меньше наибольшего виденного", correct: true },
    { label: "Старый лидер проверяет, лидер ли он ещё, перед записью", misconception: "Он не может надёжно это знать — в этом вся проблема. Он может считать себя лидером после истечения lease; безопасен против паузнутого устаревшего лидера только ресурс, проверяющий fencing-токен." },
    { label: "Истечение lease гарантирует, что старый лидер перестал действовать", misconception: "Lease ограничивает wall-clock время, но паузнутый процесс не знает, что время прошло. Мысль Kleppmann: без fencing-токена устаревший лидер может проснуться и записать после истечения lease." },
    { label: "Экспоненциальный backoff задержит запись старого лидера, пока не станет безопасно", misconception: "Backoff разносит ретраи; он ничего не делает, чтобы отвергнуть запись от лидера, который больше не должен писать. Это работа fencing-токена." },
  ]}
/>

<DragOrder
  id="08-putting-it-together-drag-1"
  lessonSlug="01-overview"
  lang="ru"
  prompt="Расставь шаги диагностики провала композиции (периодический двойной возврат), где все сервисы зелёные:"
  items={[
    { id: "a", label: "Воспроизводи по трейсу, а не по логу одного сервиса — баги композиции пересекают слои" },
    { id: "b", label: "Найди шов: какие два корректных слоя взаимодействовали (здесь: слой ретраев, заново вызывающий компенсацию)" },
    { id: "c", label: "Проверь, пересекает ли этот шов стабильный ключ идемпотентности — обычно нет" },
    { id: "d", label: "Протяни один бизнес-ключ через каждый слой, который может повторить операцию" },
    { id: "e", label: "Делай dedup на получателе по этому ключу и добавь алерт уровня шва (например, счётчик дубль-эффектов)" },
  ]}
  correctOrder={["a", "b", "c", "d", "e"]}
/>

</Explanation>

<KeyTakeaway>Примитивы корректны по отдельности; баги прячутся в швах. Идемпотентность по бизнес-ключу — несущий примитив, делающий ретраи и at-least-once безопасными. Веди систему по сигналам швов, а не по зелёным дашбордам.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="08-putting-it-together-retrieval"
  lang="ru"
  questions={[
    { q: "Проведи коллегу по тому, как компенсация возврата и ретрай складываются в двойной возврат, хотя оба слоя по отдельности корректны, и как одно изменение это чинит.", a: "Сага выпускает компенсацию возврата для отменённого заказа. Платёжный сервис начинает возврат, и он проходит, но ответ задержан за 30s таймаута вызывающего. Слой ретраев видит таймаут, проверяет бюджет ретраев, находит место и повторяет — что само по себе правильное решение политики. Компенсация тоже корректна по отдельности: отменённый заказ должен возвращать деньги. Но вызов возврата не нёс общего ключа идемпотентности, поэтому платёжный сервис принимает ретрай за совершенно новый запрос и возвращает деньги второй раз. Ни один слой не баговый; дефект целиком в шве между слоем ретраев и платёжным сервисом. Фикс — протянуть один бизнес-ключ — refund:order-8842 — от саги через слой ретраев в платёжный сервис, который записывает ключ и возвращает результат первого возврата при любом втором прибытии того же ключа. Сами слои нетронуты; шов получает exactly-once эффекты." },
    { q: "Почему идемпотентность называют несущим примитивом всего пайплайна, и что делает ключ корректным против ключа, который молча проваливается?", a: "Распределённые системы доставляют at-least-once: вызов, отвалившийся по таймауту, мог пройти, и вызывающий никогда не уверен. Раз «случилось ли это?» узнать нельзя, единственный безопасный дизайн — сделать «сделать снова» безвредным; это и есть идемпотентность, и именно она делает безопасными ретраи, доставку at-least-once и компенсации саги. Без неё каждый ретрай — потенциальный дубль-эффект. Корректный ключ выводится из стабильного бизнес-намерения (refund:order-8842) и протягивается через каждый слой, который может повторить, при этом получатель его записывает и возвращает прежний результат на повтор. Ключ молча проваливается, когда привязан не к тому — например, к id HTTP-запроса, — потому что ретрай, сгенерированный другим слоем (сага vs SDK vs клиент), даёт свежий id и проскальзывает мимо dedup-проверки. «Сервис идемпотентен» не значит ничего, пока не назовёшь, какая операция и какой ключ." },
  ]}
/>

<Recap lang="ru">
Капстоун — не новый примитив, а осознание, что каждый примитив в этом треке корректен по отдельности, а реальные провалы живут в швах, где они соединяются. Один пайплайн заказа/оплаты использует их все: кворумную репликацию (R + W > N) для долговечных записей заказа, выбранного лидера с fencing-токенами, чтобы паузнутый оркестратор не испортил состояние, продвинутое новым лидером, логические часы или TrueTime для причинного упорядочивания шагов, саги с компенсациями, потому что распределённой ACID-транзакции нет, и ретраи с backoff, jitter и бюджетом, ограниченным около 10% сверх обычного трафика. Идемпотентность по бизнес-ключу — несущий примитив: доставка at-least-once делает дубликаты неизбежными, поэтому единственный безопасный ответ на непознаваемое «случилось ли это?» — сделать «сделать снова» безвредным. Каноничный провал композиции — компенсация возврата в гонке с ретраем через вызов без общего ключа, дающая двойной возврат, пока тесты каждого слоя остаются зелёными — чинится протягиванием одного ключа через шов, а не изменением слоя. Всем этим управляют по сигналам швов — consumer lag, latency кворума, leader churn, исчерпание бюджета ретраев — потому что пайплайн из здоровых сервисов всё равно может подводить реальных клиентов.
</Recap>
