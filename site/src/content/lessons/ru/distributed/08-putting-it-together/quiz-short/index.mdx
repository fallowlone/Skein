---
concepts:
- quorum-replication
- fencing-token
- logical-clocks
- saga-compensation
- retry-amplification
- idempotency-key
estMin: 14
lang: ru
level: senior
order: 8
prereqs: []
slug: quiz-short
sources:
- https://martin.kleppmann.com/2016/02/08/how-to-do-distributed-locking.html
- https://microservices.io/patterns/data/saga.html
- https://aws.amazon.com/builders-library/timeouts-retries-and-backoff-with-jitter/
status: ready
summary: "Промпты на свободное воспроизведение по всему распределённому треку — кворум, fencing, часы, саги, ретраи, идемпотентность — ответь своими словами до раскрытия эталона."
title: 'Распределённый капстоун: свободное воспроизведение'
track: distributed
unit: 08-putting-it-together
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";

<Hook>
Воспроизведение бьёт перечитывание. На каждый промпт реконструируй полный ответ по памяти — охватывая несколько уроков трека — до того, как откроешь эталон. Усилие припоминания и есть то, что закрепляет синтез.
</Hook>

<Goal lang="ru">
Реконструируй спину трека, не подглядывая: пересечение кворума, fencing против устаревшего лидера, логические часы, компенсации саги, бюджеты ретраев и ключ идемпотентности, связывающий их все в швах.
</Goal>

<RetrievalDrawer
  client:visible
  id="08-putting-it-together-short-retrieval"
  lang="ru"
  questions={[
    { q: "Сформулируй правило кворума R + W > N и объясни, что каждая его сторона даёт в пайплайне заказа.", a: "N — число реплик на ключ, W — число подтверждений для подтверждения записи, R — число реплик, опрашиваемых на чтение. Когда R + W больше N, множества чтения и записи гарантированно пересекаются хотя бы на одной реплике, так что любое чтение видит самую свежую подтверждённую запись — это read-your-writes и основа того, что «заказ создан» долговечен и виден. При N=3, W=2, R=2 запись переживает потерю одной реплики, и любое последующее чтение её пересекает. Если R + W не больше N (например, W=1, R=1), кворумы могут разминуться, и чтение вернёт устаревшие данные, хотя запись была подтверждена. Цена более высоких W и R — латентность: больше реплик должно ответить до завершения записи или чтения." },
    { q: "Почему устаревший лидер не может защитить систему проверкой «лидер ли я ещё?» перед записью, и что её защищает?", a: "Лидер может потерять свой lease, не зная об этом — долгая пауза GC или сетевой раздел замораживают процесс, и при возобновлении у него нет ощущения, что время прошло или что выбран новый лидер. Так что самопроверка («лидер ли я ещё?») может уверенно вернуть неверное «да», потому что взгляд процесса на мир устарел. Единственная надёжная защита живёт у ресурса, а не у лидера: каждая запись несёт монотонно растущий fencing-токен, ресурс записывает наибольший принятый токен и отвергает любую запись с меньшим. Когда выбирается новый лидер, он получает больший токен, так что устаревшая запись старого лидера с меньшим токеном отвергается напрямую. Это точка Клеппманна: lease ограничивает wall-clock время, но не может ограничить приостановленный процесс, так что корректность должна обеспечиваться ресурсом через fencing, а не доверием лидера своему статусу." },
    { q: "Почему нельзя упорядочивать события между сервисами по wall-clock меткам и что используют взамен?", a: "Физические часы на разных хостах не синхронизированы идеально — NTP в лучшем случае ограничивает рассинхрон десятками миллисекунд и может шагнуть часами назад при коррекции. Так что более позднее событие на одном хосте может нести более раннюю метку, чем более раннее событие на другом, что делает «Payment произошёл до Shipping» невозможным установить из сравнения Date.now(). Причинный порядок должен быть фактом, а не догадкой по часам. Логические часы (метки Лампорта или векторные часы) фиксируют отношения happens-before, инкрементируясь на событиях и сливаясь при получении сообщения, независимо от физического времени. Где нужна семантика физического времени между сервисами, Spanner от Google использует TrueTime, выставляющий интервал ограниченной неопределённости и выжидающий его, чтобы коммит-метки были глобально упорядочены. Принцип: упорядочивай шаги по причинности или часам с ограниченной неопределённостью, никогда — по сырому сравнению wall-clock." },
    { q: "Почему нет распределённой ACID-транзакции на четыре сервиса и что заменяет rollback?", a: "Классической ACID-транзакции нужно, чтобы все участники держали блокировки и коммитили атомарно под одним координатором; между независимо разворачиваемыми сервисами со своими БД это требует распределённого two-phase commit, который связывает доступность (один медленный или упавший участник блокирует всех) и не масштабируется для долгих бизнес-воркфлоу. Взамен используют сагу: последовательность локальных транзакций, по одной на сервис, каждая коммитит независимо. Rollback уже закоммиченной локальной транзакции нет — вместо этого у каждого прямого шага есть компенсация, новая транзакция, семантически обращающая эффект (вернуть деньги за списание, вернуть товар на склад, отменить отгрузку). Компенсации — не undo-логи; это бизнес-уровневые откаты, которые сами должны быть безопасны для повтора. Размен — атомарность за доступность и развязку: система eventually consistent, и нужно явно рассуждать о состояниях частичного завершения, в которых может остаться сага." },
    { q: "Объясни усиление ретраев и бюджет ретраев, который его ограничивает.", a: "Усиление ретраев — это когда мелкий сбой вниз по стеку умножается в куда большую нагрузку, потому что каждый слой повторяет слой под собой. Если сервис мигает и каждый его вызывающий повторяет несколько раз — а их вызывающие тоже повторяют — трафик ретраев складывается мультипликативно, а синхронные повторы (фиксированная задержка, без jitter) приходят выровненными волнами, заново насыщающими восстанавливающийся сервис, превращая двухсекундный миг в отказ. Защиты комбинируются: экспоненциальный backoff разносит повторы, jitter рандомизирует их, чтобы вызывающие не стреляли в ногу, circuit breaker прекращает звонить явно упавшему сервису, а бюджет ретраев ограничивает, сколько лишней нагрузки повторы могут добавить — обычно около 10% сверх нормального темпа запросов. Когда бюджет почти исчерпан, дальнейшие ретраи сбрасываются, а не усиливаются, что проявляется как чистый отказ вместо каскадного коллапса." },
    { q: "Почему идемпотентность — несущий примитив и что отличает корректный ключ от молча проваливающегося?", a: "Распределённая доставка — at-least-once: вызов, отвалившийся по таймауту, мог реально пройти, и вызывающий никогда не уверен со своей стороны. Раз «случилось ли это?» принципиально непознаваемо, единственный безопасный дизайн — сделать «сделать снова» безвредным — это идемпотентность, и именно она делает ретраи, доставку at-least-once и компенсации саги безопасными для опоры. Без неё каждый ретрай — потенциальный дублирующий эффект (канонический двойной возврат). Корректный ключ выводится из стабильного бизнес-намерения — refund:order-8842 — и протягивается через каждый слой, который может повторить, а получатель записывает ключ и возвращает первый результат на любом повторе. Ключ молча проваливается, когда привязан не к тому, например к id HTTP-запроса: ретрай, сгенерированный другим слоем (сага vs SDK vs клиент), даёт свежий id и проскальзывает мимо проверки дедупа. «Сервис идемпотентен» бессмысленно, пока не названо, какая операция и какой ключ, и не подтверждено, что ключ пересекает каждый шов." },
  ]}
/>

<Recap lang="ru">
Если ты реконструировал каждый ответ по памяти, ты держишь спину трека: R + W > N даёт пересечение read-your-write, fencing-токены защищают от приостановленного устаревшего лидера, потому что самопроверка не может, логические часы или часы с ограниченной неопределённостью упорядочивают события, как wall-clock не может, саги заменяют распределённый ACID компенсациями, бюджеты ретраев ограничивают усиление, а идемпотентность по бизнес-намерению делает at-least-once безопасным через каждый шов. Инсайт капстоуна: каждый корректен по отдельности — провалы живут там, где они складываются. Теперь, когда будешь читать постмортем, где все дашборды были зелёными, — знаешь, куда смотреть первым: в шов между двумя корректными слоями, и есть ли там общий бизнес-ключ.
</Recap>
