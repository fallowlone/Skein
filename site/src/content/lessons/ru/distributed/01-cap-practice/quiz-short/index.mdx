---
concepts:
- linearizability
- cp-ap-tradeoff
- pacelc-theorem
- logical-partition
- conflict-resolution
- tunable-consistency
estMin: 13
lang: ru
level: senior
order: 8
prereqs: []
slug: quiz-short
sources:
- https://en.wikipedia.org/wiki/CAP_theorem
- https://en.wikipedia.org/wiki/PACELC_theorem
- https://www.infoq.com/articles/cap-twelve-years-later-how-the-rules-have-changed/
status: ready
summary: "Промпты на свободное припоминание по юниту CAP. Сначала ответь по памяти, затем открой образцовый ответ и сравни."
title: 'CAP на практике: свободное припоминание'
track: distributed
unit: 01-cap-practice
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";

<Hook>
Припоминание сильнее перечитывания. На каждый промпт скажи или запиши полный ответ по памяти, прежде чем открыть образцовый — усилие припоминания закрепляет компромиссы, когда ты рассуждаешь о реальной системе под разделением.
</Hook>

<Goal lang="ru">
Восстанови ключевые идеи юнита — формальные определения C/A, почему P обязательно, бинарный выбор при разделении, ось PACELC для штатного режима, логические разделения и разрешение конфликтов в AP — не подглядывая в урок.
</Goal>

<RetrievalDrawer
  client:load
  id="01-cap-practice-short-retrieval"
  lang="ru"
  questions={[
    { q: "Почему устойчивость к разделению (P) — не выбор на этапе проектирования, и какой единственный реальный выбор оставляет CAP?", a: "Устойчивость к разделению — это физическое ограничение работы в ненадёжной сети, а не переключатель конфигурации. Кабели рвутся, коммутаторы переполняют буферы, маршруты BGP флапают, виртуальные NIC замирают при миграции гипервизоров — система не может это предотвратить, поэтому обязана терпеть. Значит, P обязательно. Единственный реальный выбор возникает, когда разделение реально случается, и он бинарен: сохранить согласованность (CP), отказывая или возвращая ошибку на операциях, которые читали бы или писали устаревшие/конфликтующие данные, либо сохранить доступность (AP), принимая чтения и записи локально на обеих сторонах, принимая, что состояние расходится и должно быть примирено позже." },
    { q: "Сформулируй формальные определения согласованности и доступности по Gilbert-Lynch и объясни, почему «высокодоступная» по маркетингу БД часто на деле CP.", a: "Согласованность в CAP означает линеаризуемость: есть единый порядок всех операций в реальном времени, и в момент завершения записи любое последующее чтение в любой точке мира возвращает это значение или новее — ни один читатель не видит устаревшего состояния. Доступность означает, что каждый работающий узел возвращает неошибочный ответ на каждый запрос без бесконечной блокировки; таймаут или 503 считаются недоступностью. Возврат устаревших данных при этом всё ещё «доступен». Многие БД, рекламируемые как доступные, на деле CP, потому что при разделении сторона-меньшинство отклоняет запросы ради линеаризуемости — например, кластер Raft (etcd, Consul), где изолированное меньшинство отказывает в чтениях и записях. Они выбирают корректность вместо отзывчивости — это позиция CP." },
    { q: "Что теорема PACELC добавляет к CAP и чего стоит выбор EC в штатном режиме?", a: "CAP описывает поведение только во время разделения, но сети здоровы ~99,9% времени. PACELC (Abadi, 2012) добавляет ветку else: если Partitioned, выбирай Availability или Consistency; Else — выбирай Latency или Consistency. Выбор EC (согласованность при здоровой сети) означает, что каждое чтение и запись синхронно координируются между репликами — кворумное подтверждение, часто между датацентрами — поэтому ты платишь задержку round-trip на каждый запрос. Выбор EL означает асинхронную репликацию и eventual consistency, покупая низкую задержку ценой возможной отдачи устаревших данных даже без разделения. Значит, строгая согласованность никогда не бесплатна: при разделении она стоит доступности, а при здоровой сети — задержки." },
    { q: "Объясни, как пауза GC или насыщение CPU может вызвать «логическое» разделение в CP-кластере консенсуса.", a: "Детектор сбоев не может отличить «медленный» от «мёртвого» — он видит лишь пропущенные heartbeat. Если узел получает stop-the-world паузу GC на 10 секунд или его CPU насыщен фоновой задачей, он перестаёт отвечать на heartbeat, хотя ни один канал не разорван и узел в остальном здоров. Его пиры заключают, что он упал, и запускают перевыборы лидера, блокируя записи на время выборов и теряя незавершённую работу старого лидера. Это логическое разделение. Raft не зависит от синхронизации настенных часов (он использует логические термы и порядковые номера); триггер — чисто пропущенные heartbeat. Pre-vote и election timeout, настроенные выше худшей паузы/RTT, смягчают разрушительный флап." },
    { q: "Ты выбрал AP. Какой налог на разрешение конфликтов ты должен и почему Last-Write-Wins по настенным меткам времени опасен?", a: "Выбор AP означает, что обе стороны разделения принимают записи независимо, поэтому один и тот же ключ может разойтись — например, корзина, отредактированная в двух регионах. Когда разделение заживает, ты обязан примирить эти версии. Last-Write-Wins оставляет запись с наибольшей меткой времени, но метки берутся из часов узлов, а они дрейфуют. Действительно более поздняя запись на слегка отстающих часах может проиграть более ранней с большей меткой, поэтому LWW молча и необратимо отбрасывает легитимные данные клиента без всякой ошибки. Безопаснее сохранять обе записи и сливать их: CRDT (определяющие детерминированное коммутативное слияние) или векторные часы (детектирующие истинную конкурентность и показывающие siblings для разрешения приложением/клиентом) — ценой существенной архитектурной сложности." },
    { q: "Почему кворумное правило W+R > N даёт строгую согласованность и как оно взаимодействует с выбором CP/AP?", a: "При N репликах запись, ждущая W подтверждений, и чтение, ждущее R подтверждений, гарантированно пересекаются минимум на одной реплике всякий раз, когда W+R > N — эта пересекающаяся реплика видела самую недавнюю подтверждённую запись, поэтому чтение наблюдает свежее значение, а не устаревшее состояние. Это строгая согласованность на ключ, а не полная линеаризуемость. Оно взаимодействует с CAP, потому что достижение W или R реплик требует достижимости стольких узлов: при разделении сторона, не способная собрать кворум, обязана провалить операцию (поведение CP). Снижение согласованности (например R=W=1) восстанавливает доступность на разделённой стороне, но жертвует гарантией пересечения, сдвигая систему к AP. Хранилища с настраиваемой согласованностью позволяют выбирать это на каждый запрос." },
  ]}
/>

<Recap lang="ru">
Если ты смог восстановить каждый ответ по памяти, ты держишь хребет юнита: P обязательно, поэтому разделение вынуждает бинарный выбор CP-против-AP; согласованность означает линеаризуемость, а доступность — неошибочный ответ от каждого работающего узла, поэтому многие «доступные» БД на деле CP; PACELC добавляет, что строгая согласованность стоит задержки и в штатном режиме; логические разделения приходят от медленных узлов, а не только обрывов каналов; AP должен налог на разрешение конфликтов, где LWW молча теряет данные при дрейфе часов; а W+R > N покупает свежесть на ключ, прижимая тебя к CP при разделении.
</Recap>
