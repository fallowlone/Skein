---
concepts:
- leader-election
- split-brain
- fencing-token
- raft-terms
- lease-leadership
- election-timeout
deepensInto: []
estMin: 17
lang: ru
lessonType: topic
level: junior
order: 1
prereqs: []
slug: 01-overview
sources:
- https://martin.kleppmann.com/2016/02/08/how-to-do-distributed-locking.html
- https://raft.github.io/raft.pdf
- https://zookeeper.apache.org/doc/current/recipes.html
- https://etcd.io/docs/latest/learning/api/
spiral: []
status: ready
summary: Единственный лидер сериализует записи, чтобы кластер избегал конфликтов —
  но GC-пауза или partition могут оставить два узла, оба уверенных, что они лидер.
  Term''''ы, lease и монотонный fencing-токен — то, как продакшен это переживает.
title: 'Выбор лидера: один писатель, term''''''''ы и split-brain, который останавливают
  fencing-токены'
track: distributed
unit: 04-leader-election
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";

<Hook>
Батч-воркер держит кластерный лок и начинает писать файлы в общее хранилище. И тут его JVM ловит 14-секундную stop-the-world GC-паузу. Изнутри процесса ничего не случилось — одна строка кода, затем следующая. Но lease, который он держал, истёк на 10-й секунде; координатор объявил его мёртвым и выбрал нового лидера, и тот начал свои записи. На 14-й секунде старый воркер просыпается и дописывает свою запись — в тот же файл, устаревшими данными. Два лидера, один испорченный файл и постмортем, который начинается со слов «но у нас же был лок».
</Hook>

<Crux>Один лидер сериализует записи; пауза или partition могут породить второго, и только fencing-токен останавливает устаревшую запись.</Crux>

<Explanation>

## Зачем вообще выбирать лидера

Большинство задач координации резко упрощаются в тот момент, когда ровно одному узлу разрешено принимать решение. С единственным лидером записи сериализуются через одно место: нет конфликтов параллельных обновлений, которые надо мёржить, нет неоднозначности last-write-wins, нет двух узлов, присваивающих один и тот же sequence number. Лидер становится точкой линеаризации: каждое изменение проходит через него в порядке, с которым все согласны. Поэтому столько инфраструктуры построено вокруг лидера — partition-лидеры в Kafka, primary-база в replica set, контроллер кластера, cron-задача, которая обязана выполниться ровно на одном хосте.

Цена в том, что ты сознательно создал единую точку отказа, поэтому сложность не в том, чтобы *выбрать* лидера, — а в том, чтобы безопасно *перевыбрать* его, когда текущий умрёт, ни на миг не допустив двух активных сразу. Именно во втором требовании живёт почти вся реальная сложность (и почти каждый продакшен-инцидент).

## Term'ы и рандомизированные таймауты: как выбирает Raft

Raft, алгоритм консенсуса за etcd и многими другими, делит время на **term'ы** — последовательные целые числа, работающие как логические часы. У каждого term'а не больше одного лидера. Фолловер ждёт регулярных heartbeat'ов от лидера; если в пределах его **election timeout** ничего не пришло, он считает лидера ушедшим, инкрементирует term, становится кандидатом и просит всех голосовать за него. Кандидат побеждает, собрав голоса большинства (кворума), и каждый узел голосует не больше одного раза за term, в порядке first-come-first-served.

Хитрость — в значении таймаута. Если бы все фолловеры использовали один фиксированный таймаут, они стали бы кандидатами в один миг, расщепили голоса, и никто бы не победил — потом повторили бы и снова расщепили. Raft чинит это, выбирая election timeout каждого узла **случайно из диапазона, обычно 150–300 мс**. Таймер одного узла почти всегда срабатывает первым; он агитирует и побеждает раньше, чем проснётся любой сосед. Рандомизация превращает синхронную давку в разнесённую очередь.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Система</th>
        <th class="border border-border px-3 py-2 text-left">Механизм выбора</th>
        <th class="border border-border px-3 py-2 text-left">Ключевое число</th>
        <th class="border border-border px-3 py-2 text-left">Сигнал жизни</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">Raft (etcd, Consul)</td>
        <td class="border border-border px-3 py-2">Term + голос большинства</td>
        <td class="border border-border px-3 py-2">Election timeout 150–300 мс (рандомизирован)</td>
        <td class="border border-border px-3 py-2">Heartbeat'ы лидера</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">ZooKeeper</td>
        <td class="border border-border px-3 py-2">Наименьший ephemeral sequential znode</td>
        <td class="border border-border px-3 py-2">Session timeout (часто секунды)</td>
        <td class="border border-border px-3 py-2">Heartbeat'ы сессии; znode авто-удаляется при истечении</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">etcd</td>
        <td class="border border-border px-3 py-2">Lease + compare-and-swap по ключу</td>
        <td class="border border-border px-3 py-2">Lease TTL (задаётся на каждый acquire)</td>
        <td class="border border-border px-3 py-2">Keep-alive продления lease</td>
      </tr>
    </tbody>
  </table>
</div>

## Lease, сессии и часы, которым нельзя верить

После выбора лидер должен постоянно доказывать, что жив, потому что кластеру нужно достаточно быстро обнаружить мёртвого лидера, чтобы выбрать замену. Обычный механизм — **lease** (etcd) или **сессия** (ZooKeeper): лидерство выдаётся на ограниченное время и должно продлеваться. В ZooKeeper лидер держит ephemeral znode, привязанный к сессии; если heartbeat'ы прекращаются на время session timeout, ZooKeeper удаляет znode, и следующий кандидат берёт верх. В etcd лидер привязывает к ключу lease с TTL и держит его живым; если продления прекращаются, lease истекает, ключ исчезает и роль освобождается.

Вот ловушка, которую сеньор не забывает никогда: lease — это утверждение о *времени*, а локальные часы лидера и часы координатора — это не одни и те же часы. Lease «действует 10 секунд» безопасен, только если обе стороны согласны, сколько длятся 10 секунд, — а они согласны не вполне. Хуже того, лидер может вообще не *работать* часть этого окна. GC-пауза, гипервизор, замораживающий VM ради live-миграции, выгруженный из памяти процесс или скачок NTP — всё это может заставить лидера верить, что его lease ещё действует, спустя долгое время после того, как координатор уже истёк его и выбрал кого-то другого.

<Inset kind="why" lang="ru">
Lease ограничивает, сколько *координатор ждёт* перед тем, как объявить узел мёртвым, — но он **не** ограничивает, сколько *приостановленный лидер* будет верить, что всё ещё держит lease. Это двое разных часов. Лидер может застыть посреди инструкции, пока настенное время и его lease истекают под ним. Этот зазор — ровно то место, где рождается split-brain.
</Inset>

## Split-brain и фикс через fencing-токен

**Split-brain** — это когда два (или больше) узла одновременно уверены, что они лидер. К этому может привести partition сети — сторона-меньшинство, не видящая кворум, всё ещё может думать, что лидирует, — но коварнее причина из Hook: старого лидера не отрезали, его просто *остановили*, и он просыпается, держа lease, который мир уже отдал. Теперь пишут два лидера, и общее состояние портится.

Фикс Клеппманна — **fencing-токен**: каждый раз при выдаче лока/lease координатор раздаёт строго возрастающее число — 33, потом 34, потом 35. Лидер обязан прикладывать свой токен к каждой записи в защищаемый ресурс, а *сам ресурс* обязан отвергать любую запись, чей токен ниже наибольшего, который он уже видел. Поэтому когда клиент 1 (токен 33) просыпается из GC-паузы и пытается писать, хранилище уже приняло запись клиента 2 с токеном 34 — оно сразу отвергает токен 33. Устаревший писатель *отсекается* на границе ресурса. Ключевой, часто упускаемый момент: токен работает, только если нижестоящий ресурс активно его проверяет. Лок-сервис, который просто раздаёт токены, но пишет в тупое хранилище, их игнорирующее, не даёт тебе ничего.

<TradeoffMatrix
  id="04-leader-election-tradeoff-1"
  lessonSlug="01-overview"
  lang="ru"
  prompt="Лидер держит 10-секундный lease, пишет в объектное хранилище, и ты видел многосекундные GC-паузы. Как предотвратить устаревшую запись после паузы?"
  options={[
    { name: "Выдавать монотонный fencing-токен при каждой выдаче lease; слой хранилища отвергает любую запись с токеном ниже наибольшего виденного", summary: "Отсекать на ресурсе.", correct: true, justification: "Это фикс Клеппманна и единственный, что закрывает зазор. Приостановленный лидер просыпается с токеном 33, но ресурс уже принял токен 34 от нового лидера, поэтому устаревшая запись отвергается на границе. Это работает именно потому, что корректность больше не зависит от того, заметит ли приостановленный узел, что потерял lease, — ресурс навязывает порядок независимо от часов, пауз и partition." },
    { name: "Сократить lease TTL до 2с, чтобы устаревший лидер сдавался быстрее", summary: "Подкрутить таймаут.", justification: "Ужатие TTL делает ложные истечения чаще (2-секундная GC-пауза теперь теряет лидерство) и всё равно не помогает: приостановленный процесс не может «сдать» lease, об истечении которого не знает. Ты меняешь доступность ни на что — окно split-brain сужается, но не закрывается." },
    { name: "Лидер проверяет часы и отменяет запись, если lease близок к истечению", summary: "Самопроверка перед записью.", justification: "Не может работать: во время GC-паузы процесс не выполняет код, поэтому нет момента проверить часы — он застыл между проверкой и записью. Любой зазор time-of-check/time-of-use — ровно то место, куда падает пауза. Безопасность не может зависеть от того, что подозреваемый узел остаётся отзывчивым." },
    { name: "Использовать более быстрый алгоритм консенсуса, чтобы выборы заканчивались быстрее", summary: "Ускорить выборы.", justification: "Быстрые выборы помогают доступности, но ортогональны этому багу. Проблема не в том, как быстро выбран новый лидер, — а в том, что старый всё ещё пишет после смещения. Никакая скорость выборов не помешает застывшему процессу проснуться и записать устаревшие данные." },
  ]}
/>

<Quiz
  id="04-leader-election-quiz-1"
  lessonSlug="01-overview"
  lang="ru"
  question="Почему Raft выбирает election timeout каждого узла случайно из 150–300 мс, а не использует одно фиксированное значение?"
  choices={[
    { label: "Чтобы таймеры разнеслись — один кандидат почти всегда срабатывает первым и побеждает до пробуждения соседей, избегая повторных split vote", correct: true },
    { label: "Чтобы лидеры менялись честно и каждый узел лидировал одинаково часто", misconception: "Рандомизация не про честность того, кто лидирует; она про десинхронизацию кандидатов, чтобы большинство сформировалось до того, как все агитируют разом." },
    { label: "Потому что фиксированный таймаут нарушил бы требование кворума консенсуса", misconception: "Фиксированный таймаут не ломает математику кворума; он просто вызывает синхронные кандидатуры, расщепляющие голос и заставляющие повторять." },
    { label: "Чтобы компенсировать рассинхрон часов между узлами", misconception: "Диапазон существует, чтобы разнести кандидатуру, а не корректировать часы; безопасность Raft вообще не опирается на синхронные часы." },
  ]}
/>

<Quiz
  id="04-leader-election-quiz-2"
  lessonSlug="01-overview"
  lang="ru"
  question="Fencing-токен предотвращает баг устаревшей записи только при каком условии?"
  choices={[
    { label: "Защищаемый ресурс активно проверяет токен и отвергает любую запись, чей токен ниже наибольшего им виденного", correct: true },
    { label: "Лок-сервис генерирует строго возрастающие токены", misconception: "Возрастающие токены необходимы, но сами по себе бесполезны — если нижестоящее хранилище их не валидирует, устаревшая запись всё равно проходит." },
    { label: "У всех узлов часы синхронизированы по NTP", misconception: "Весь смысл fencing — быть безопасным без доверия к часам; корректность навязывает порядок токенов, а не время." },
    { label: "Lease TTL длиннее, чем GC-пауза в худшем случае", misconception: "Худшую паузу никогда нельзя надёжно ограничить; fencing работает при любой длине паузы, потому что порядок токенов навязывает ресурс." },
  ]}
/>

<DragOrder
  id="04-leader-election-drag-1"
  lessonSlug="01-overview"
  lang="ru"
  prompt="Расставь последовательность классической устаревшей записи split-brain под GC-паузой:"
  items={[
    { id: "a", label: "Лидер A берёт lease (токен 33) и начинает запись в общее хранилище" },
    { id: "b", label: "Лидер A входит в долгую stop-the-world GC-паузу и перестаёт выполнять код" },
    { id: "c", label: "Lease A истекает; координатор объявляет A мёртвым и выбирает лидера B (токен 34)" },
    { id: "d", label: "B пишет успешно; слой хранилища фиксирует наибольший токен = 34" },
    { id: "e", label: "A просыпается, дописывает с токеном 33; слой хранилища отвергает запись как отсечённую" },
  ]}
  correctOrder={["a", "b", "c", "d", "e"]}
/>

</Explanation>

<KeyTakeaway>Один лидер сериализует записи; рандомные таймауты 150–300 мс выбирают его безопасно. Но пауза или partition могут породить второго — только fencing-токен, проверяемый ресурсом, останавливает устаревшую запись.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="04-leader-election-retrieval"
  lang="ru"
  questions={[
    { q: "Объясни коллеге, почему один lease не мешает двум узлам писать как лидер, и что чинит это на самом деле.", a: "Lease ограничивает, сколько координатор ждёт перед тем, как объявить лидера мёртвым и выбрать замену, — но не ограничивает, сколько застывший лидер продолжает верить, что всё ещё держит lease. Это двое разных часов. Если старый лидер застрял в долгой GC-паузе, выгружен из памяти или его VM приостановлена, в этом окне он не выполняет код: он не может заметить истечение lease, не может проверить часы, не может его «сдать». Тем временем координатор уже выбрал нового лидера. Когда старый просыпается, он дописывает запись, которую начал, — теперь устаревшую, — и у тебя два писателя и испорченное состояние. Сокращение TTL только учащает ложные истечения; окно оно не закрывает. Реальный фикс — монотонный fencing-токен: координатор выдаёт строго возрастающее число при каждой выдаче, лидер штампует им каждую запись, а защищаемый ресурс отвергает любую запись с токеном ниже наибольшего принятого. Токен 33 старого лидера проигрывает токену 34 нового на границе хранилища, независимо от часов и длины паузы." },
    { q: "Почему Raft использует рандомизированные election timeout, и какой провал это предотвращает?", a: "Raft выбирает лидера так: фолловеры по таймауту перестают ждать heartbeat лидера, становятся кандидатами и запрашивают голоса; кандидату нужно большинство для победы, и каждый узел голосует не больше раза за term. Если бы все фолловеры использовали один фиксированный таймаут, они истекли бы почти в один миг, стали бы кандидатами одновременно и расщепили голос так, что никто не набрал бы большинство. Затем они повторили бы вместе и снова расщепили — livelock, откладывающий выбор любого лидера. Raft предотвращает это, давая каждому узлу случайный election timeout из диапазона, обычно 150–300 мс. Узел, чей таймер срабатывает первым, становится кандидатом и обычно собирает большинство ещё до пробуждения соседей, поэтому выборы сходятся за один раунд. Рандомизация десинхронизирует кандидатов: превращает синхронную давку в разнесённую очередь — это разница между быстрыми выборами и бесконечной серией split vote." },
  ]}
/>

<Recap lang="ru">
Единственный лидер существует, чтобы сериализовать записи, и кластер избегает конфликтов параллельных обновлений и неоднозначного порядка — но платит за эту простоту единой точкой отказа, которую надо безопасно перевыбирать. Raft делает это через term'ы (логические часы, один лидер на term) и голоса большинства, используя election timeout, выбранные случайно примерно из 150–300 мс, чтобы кандидаты разносились, а не расщепляли голос вечно. Лидерство затем держится как lease или сессия, которую надо продлевать; ZooKeeper привязывает его к ephemeral znode, etcd — к lease TTL. Глубокая опасность в том, что lease — это утверждение о времени, а часы и паузы лгут: GC-пауза, приостановленная VM или partition могут оставить старого лидера пишущим после истечения lease и выбора нового — split-brain, два писателя, испорченное состояние. Лекарство не в более коротком таймауте и не в проверке часов, ведь застывший процесс не может действовать; это монотонный fencing-токен, который навязывает сам ресурс, отвергая любую запись, чей токен пошёл назад. Выбирай ради живости, отсекай ради безопасности.
</Recap>
