---
concepts:
- leader-election
- split-brain
- fencing-token
- lease-leadership
- election-timeout
- failover
estMin: 13
lang: ru
level: senior
order: 8
prereqs: []
slug: quiz-short
sources:
- https://martin.kleppmann.com/2016/02/08/how-to-do-distributed-locking.html
- https://raft.github.io/raft.pdf
- https://zookeeper.apache.org/doc/current/recipes.html
status: ready
summary: "Промпты на свободное воспроизведение по всему юниту leader election. Сначала ответь своими словами, затем открой образцовый ответ и сравни."
title: 'Выбор лидера: тест на свободное воспроизведение'
track: distributed
unit: 04-leader-election
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";

<Hook>
Воспроизведение по памяти бьёт перечитывание. Для каждого промпта проговори или запиши полный ответ из головы, прежде чем открыть образцовый — усилие восстановить аргумент безопасности и есть то, что закрепит его, когда тебя поднимут в 3 часа ночи.
</Hook>

<Goal lang="ru">
Восстанови спину юнита, не подглядывая: зачем существует единственный лидер, как Raft его выбирает, почему lease нельзя доверять через паузу, что такое split-brain и как именно fencing-токен закрывает окно.
</Goal>

<RetrievalDrawer
  client:load
  id="04-leader-election-short-retrieval"
  lang="ru"
  questions={[
    { q: "Зачем вообще выбирать единственного лидера и какую цену ты платишь взамен?", a: "Единственный лидер сериализует каждую запись через одно место, поэтому система получает точку линеаризации: нет конфликтов параллельных обновлений, которые надо мёржить, нет неоднозначности last-write-wins, нет двух узлов, присваивающих один sequence number. Поэтому partition'ы Kafka, primary в replica set, контроллер кластера и cron-задачи «ровно на одном хосте» все построены вокруг лидера. Цена в том, что ты сознательно создал единую точку отказа. Поэтому сложность не в том, чтобы выбрать лидера, — а в том, чтобы безопасно перевыбрать его, когда текущий умрёт, ни на миг не допустив двух активных сразу. Почти вся реальная сложность и почти каждый продакшен-инцидент живут во втором требовании." },
    { q: "Пройди по тому, как Raft выбирает лидера, и почему election timeout рандомизирован.", a: "Raft делит время на term'ы — монотонно возрастающие целые, работающие как логические часы, с не более чем одним лидером на term. Фолловер ждёт регулярных heartbeat'ов от лидера; если в пределах его election timeout ничего не пришло, он инкрементирует term, становится кандидатом и просит голоса. Кандидат побеждает, собрав голоса большинства (кворума), и каждый узел голосует не больше одного раза за term, first-come-first-served. Таймаут берётся случайно из диапазона (обычно 150–300 мс), чтобы фолловеры не истекали все в один миг. С фиксированным таймаутом они стали бы кандидатами разом, расщепили голос, не достигли большинства, повторили и снова расщепили — livelock. Рандомизация разносит таймеры так, что один узел почти всегда агитирует и побеждает за один раунд." },
    { q: "Объясни, почему lease сам по себе не предотвращает запись двух узлов как лидеров.", a: "Lease ограничивает, сколько координатор ждёт перед объявлением лидера мёртвым и выбором замены. Он НЕ ограничивает, сколько застывший лидер будет верить, что всё ещё держит lease, — это двое разных часов. Если старый лидер застрял в долгой GC-паузе, выгружен или его VM приостановлена ради live-миграции, он не выполняет код в этом окне: не может заметить истечение lease, проверить часы, добровольно уйти. Тем временем координатор уже выбрал нового лидера. Когда старый просыпается, он дописывает запись, которую вёл, — теперь устаревшую — и у тебя два писателя и испорченное состояние. Сокращение TTL лишь учащает ложные истечения; оно никогда не закрывает окно, потому что нельзя надёжно ограничить худшую паузу." },
    { q: "Дай определение split-brain и назови две его разные причины из юнита.", a: "Split-brain — это два или более узла одновременно уверены, что они лидер, оба принимают записи, поэтому общее состояние расходится и портится. Первая причина — сетевой partition: сторона-меньшинство, которая больше не видит кворум, может всё ещё верить, что лидирует (корректный протокол на кворуме предотвращает это, запрещая меньшинству выбирать или коммитить). Вторая, коварнее, — пауза: старого лидера не отрезали, его просто остановили (GC-пауза, приостановленная VM, выгруженный процесс), и он просыпается, держа lease, который мир уже переназначил. Случай паузы опаснее именно потому, что обходит логику обработки partition: узел выглядит здоровым для себя и дописывает свою запись на лету." },
    { q: "Опиши механизм fencing-токена от начала до конца и назови единственное условие, без которого он бесполезен.", a: "При каждой выдаче лока или lease координатор раздаёт строго возрастающее число — 33, потом 34, потом 35. Лидер обязан включать свой токен в каждую запись в защищаемый ресурс, а сам ресурс обязан отвергать любую запись, чей токен ниже наибольшего, который он уже принял. Поэтому когда приостановленный лидер просыпается с токеном 33 и пытается писать, слой хранилища уже принял запись нового лидера с токеном 34 и отвергает токен 33 на границе. Устаревший писатель отсечён. Незаменимое условие: нижестоящий ресурс обязан активно проверять токен. Лок-сервис, раздающий токены, но пишущий в тупое хранилище, их игнорирующее, не даёт ничего — корректность должна навязываться там, где запись реально приземляется, а не там, где выдан лок." },
    { q: "Сравни, как ZooKeeper и etcd представляют и обнаруживают потерю лидерства при failover.", a: "Оба выдают лидерство на ограниченное продлеваемое время, но представляют его по-разному. В ZooKeeper лидер создаёт ephemeral sequential znode, привязанный к сессии; лидер — это znode с наименьшим номером. Если heartbeat'ы сессии прекращаются на session timeout (часто секунды), ZooKeeper автоматически удаляет ephemeral znode, и следующий-наименьший кандидат берёт верх. В etcd лидер привязывает к ключу lease с TTL и шлёт keep-alive продления; если продления прекращаются, lease истекает, ключ исчезает и роль освобождается — выбор обычно использует compare-and-swap по этому ключу. В обоих обнаружение failover ограничено таймаутом сессии/lease, который меняет отзывчивость на частоту ложных срабатываний: короткий таймаут переключается быстрее, но чаще выселяет здоровых-но-медленных лидеров." },
  ]}
/>

<Recap lang="ru">
Если ты смог восстановить каждый ответ по памяти, ты держишь спину юнита: единственный лидер сериализует записи, но создаёт единую точку отказа, которую надо безопасно перевыбрать; Raft делает это term'ами и рандомизированными таймаутами 150–300 мс; лидерство — продлеваемый lease или сессия (lease TTL у etcd, ephemeral znode у ZooKeeper), чьё окно обнаружения ограничивает failover; lease не может остановить застывшего лидера от пробуждения и записи, что является одним из двух лиц split-brain (другое — partition, обрабатываемый кворумом); и только монотонный fencing-токен, навязанный у ресурса, делает устаревшую запись невозможной. Выбирай ради liveness, отсекай ради safety.
</Recap>
