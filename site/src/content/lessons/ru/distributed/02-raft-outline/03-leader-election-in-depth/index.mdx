---
concepts:
- election-timeout
- request-vote
- leader-completeness
- safety-properties
- quorum-overlap
deepensInto:
- distributed/02-raft-outline/04-failure-modes-and-recovery
estMin: 14
lang: ru
lessonType: topic
level: middle
order: 3
prereqs:
- distributed/02-raft-outline/02-log-replication-and-commit
slug: 03-leader-election-in-depth
sources:
- https://raft.github.io/raft.pdf
- https://raft.github.io/
- https://en.wikipedia.org/wiki/Raft_(algorithm)
spiral:
- statefulness
status: ready
summary: Как рандомизированные таймауты предотвращают повторные split vote, почему
  правило completeness лога сохраняет закоммиченные entry при смене лидера, и четыре
  инварианта, делающих Raft корректным.
title: 'Выборы лидера в Raft: таймауты, правила голосования и четыре свойства безопасности'
track: distributed
unit: 02-raft-outline
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import TraceScenario from "~/components/pedagogy/TraceScenario.astro";
import SequenceDiagram from "~/components/diagram/SequenceDiagram.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";
import Infographic from "~/components/diagram/Infographic.astro";
import safetyInvariants from "./infographics/safety-invariants.svg?raw";

<Hook>
Пять follower-ов одновременно замечают, что лидер молчит. Все пятеро стартуют выборы. Каждый голосует за себя. Никто не побеждает. Цикл повторяется. Именно поэтому Raft использует рандомизированные таймауты — и почему правило голосования тоньше, чем "первый пришёл — первый подан".
</Hook>

<Crux>Рандомизированные таймауты ломают ничьи; правило completeness лога гарантирует, что новый лидер имеет все закоммиченные entry.</Crux>

<Explanation>

## Election timeout и heartbeat-ы

Лидер утверждает свою власть, отправляя heartbeat-ы `AppendEntries` каждому follower-у с фиксированным интервалом — обычно 50 мс. У каждого follower-а есть **election timeout**, сбрасываемый каждым валидным heartbeat-ом. Если таймаут срабатывает (heartbeat не получен), follower считает лидера мёртвым и запускает выборы.

Таймаут **рандомизирован** — обычно в диапазоне 150–300 мс. Без рандомизации все follower-ы таймаутили бы одновременно, каждый голосовал за себя, и голос бы раздробился. При достаточно широком диапазоне с высокой вероятностью один follower срабатывает первым, отправляет `RequestVote` всем остальным и собирает большинство до того, как кто-то другой таймаутит. Ничьи всё ещё возможны, но редки; любая ничья просто начинает новый term, и новый случайный таймаут быстро её разрешает.

<FlowDiagram
  data-lesson-visual
  label="Рандомизированный election timeout разрушает ничьи: первый сработавший follower отправляет RequestVote, собирает большинство до того, как другие истекают, и становится лидером."
  hue="var(--d-systems)"
  perRow={4}
  nodes={[
    { id: "heartbeat", label: "Лидер молчит", sub: "heartbeat не получен" },
    { id: "timeout", label: "Таймаут срабатывает", sub: "150–300 мс, один узел первым" },
    { id: "candidate", label: "Follower → Candidate", sub: "увеличивает term, голосует за себя" },
    { id: "leader", label: "Большинство → Лидер", sub: "побеждает до истечения других" },
  ]}
  edges={[
    { from: "heartbeat", to: "timeout" },
    { from: "timeout", to: "candidate" },
    { from: "candidate", to: "leader", label: "RequestVote получает кворум" },
  ]}
  caption="Фиксированные таймауты заставляют всех follower-ов сработать одновременно, дробя голоса. Случайный диапазон (150–300 мс) с высокой вероятностью даёт одному узлу сработать первым."
/>

## Правило голосования RequestVote

Нода отдаёт голос в `RequestVote` **только если**:

1. Она ещё не голосовала в этом term (один голос на term на ноду).
2. Лог candidate-а **как минимум так же актуален**, как собственный лог voter-а.

"Как минимум так же актуален" сравнивается по: больший `lastLogTerm` побеждает; при равных term-ах побеждает больший `lastLogIndex`. Это правило — ключ к безопасности.

## Почему правило completeness лога важно

Без проверки up-to-date candidate со stale-логом мог бы выиграть выборы и стать лидером, не имея закоммиченных entry. Эти entry были бы перезаписаны, нарушая гарантию, что закоммиченное entry сохраняется навсегда.

Аргумент пересечения кворумов объясняет, почему правило работает: каждое закоммиченное entry было подтверждено большинством. Любой election-кворум пересекается с этим commit-кворумом хотя бы в одной ноде. Через эту общую ноду candidate должен иметь закоммиченное entry (иначе voter откажет). Вместе пересечение кворумов и правило completeness лога дают **Leader Completeness**: каждый лидер term T+1 имеет все entry, закоммиченные в term-ах 1–T.

## Четыре свойства безопасности

Доказательство корректности Raft сводится к четырём инвариантам:

1. **Election Safety** — максимум один лидер на term. Следует из "один голос на ноду на term + требуется большинство".
2. **Leader Append-Only** — лидер никогда не перезаписывает и не удаляет свои log entry; только дописывает.
3. **Log Matching** — если два лога имеют общий entry на index i с term t, они идентичны для всех index до i включительно. Следует из проверки целостности AppendEntries.
4. **Leader Completeness** — любое entry, закоммиченное в каком-то term, есть в логе каждого лидера более высоких term-ов. Следует из пересечения кворумов + правила голосования.

Эти четыре инварианта работают цепочкой: Safety предотвращает двух лидеров (1), Append-Only сохраняет лог лидера нетронутым (2), Log Matching заставляет follower-ы к нему сходиться (3), а Completeness гарантирует: новый лидер не стартует с пробелом (4). Без любого звена цепь рвётся — именно поэтому etcd трактует каждый инвариант как обязательное требование протокола.

<Infographic
  svg={safetyInvariants}
  label="Дерево четырёх инвариантов корректности Raft — Election Safety, Leader Append-Only, Log Matching, Leader Completeness — каждый снабжён единственным механизмом, из которого он следует, и отказом, наступающим без этого механизма."
  caption="Каждый инвариант — не самостоятельное правило, а следствие одного конкретного механизма; убери механизм — наступает названный отказ. В этом разница между заучиванием четырёх свойств и пониманием, почему доказательство держится."
  hue="var(--d-infra)"
/>

**State Machine Safety** (выводится): никакие две ноды никогда не применяют разные команды на одном index.

<Quiz
  id="raft-mid-q2"
  lessonSlug="03-leader-election-in-depth"
  lang="ru"
  question="Зачем Raft рандомизирует election timeout (150–300 мс) вместо фиксированного значения?"
  choices={[
    { label: "Чтобы в среднем ускорить выборы", misconception: "Рандомизация не ускоряет выборы; если что — замедляет худший случай. Цель — избежать повторных split vote." },
    { label: "Чтобы вероятностно обеспечить первенство одного follower-а, избегая одновременных candidate-столкновений", correct: true },
    { label: "Потому что этого требует сетевой протокол", misconception: "Никакое транспортное ограничение рандомизацию не требует. Это осознанный выбор дизайна протокола." },
    { label: "Чтобы справедливо ротировать лидерство между нодами", misconception: "Raft не стремится к справедливости — он стремится к liveness. Первая нода, таймаутившая и собравшая большинство, побеждает." },
  ]}
/>

<Quiz
  id="raft-mid-q3"
  lessonSlug="03-leader-election-in-depth"
  lang="ru"
  question="Почему правило голосования RequestVote требует, чтобы лог candidate-а был как минимум так же актуален, как у voter-а?"
  choices={[
    { label: "Чтобы ускорить выборы", misconception: "Проверка лога добавляет шаг сравнения — она делает выборы чуть медленнее, а не быстрее." },
    { label: "Чтобы гарантировать, что новый лидер имеет все ранее закоммиченные entry, предотвращая потерю подтверждённых записей", correct: true },
    { label: "Чтобы предотвратить голосование follower-ов за себя", misconception: "Candidate всегда голосует за себя; проверка up-to-date управляет голосами от других нод." },
    { label: "Чтобы избежать инфляции term", misconception: "Инфляцию term митигирует pre-vote (отдельное расширение). Проверка лога — о completeness entry." },
  ]}
/>

<TraceScenario
  id="raft-mid-trace1"
  lessonSlug="03-leader-election-in-depth"
  lang="ru"
  scenario="Проследи чистые выборы лидера после его краша."
  steps={[
    { prompt: "Сетап: 5-нодовый кластер A, B, C, D, E. A — лидер term 7. A падает.", reveal: "Heartbeat-ы от A прекращаются. Election-таймер каждого follower-а считает независимо (рандомизирован в диапазоне 150–300 мс)." },
    { prompt: "Таймер B срабатывает первым на 187 мс. Что делает B?", reveal: "B переходит в candidate, увеличивает term до 8, голосует за себя, шлёт RequestVote(term=8, lastLogIndex=100, lastLogTerm=7) к C, D, E." },
    { prompt: "C, D, E получают RequestVote. Они проверяют: голосовал ли я в term 8? Лог B как минимум так же актуален, как мой?", reveal: "Никто не голосовал в term 8. Лог B равен их (lastLogIndex=100, lastLogTerm=7). Каждый отдаёт голос и обновляет currentTerm до 8." },
    { prompt: "B собирает 3 голоса (себя + C + D). Что дальше?", reveal: "У B большинство из 5. B становится лидером term 8 и немедленно шлёт heartbeat-ы (пустые AppendEntries) всем нодам. C, D, E сбрасывают election-таймеры." },
    { prompt: "A возвращается online со stale term 7.", reveal: "AppendEntries A fails (follower-ы отвергают меньший term). A получает term 8 в ответе, понижается до follower, обновляет term до 8 и начинает следовать за B." },
  ]}
/>

</Explanation>

<SequenceDiagram
  label="Раунд RequestVote, где кандидат B берёт majority и становится лидером"
  hue="var(--d-systems)"
  actors={["Candidate", "C", "D", "E"]}
  messages={[
    { from: "Candidate", to: "C", label: "RequestVote(term=8, lastLogIdx=100)" },
    { from: "Candidate", to: "D", label: "RequestVote(term=8, lastLogIdx=100)" },
    { from: "Candidate", to: "E", label: "RequestVote(term=8, lastLogIdx=100)" },
    { from: "C", to: "Candidate", label: "голос отдан" },
    { from: "D", to: "Candidate", label: "голос отдан" },
    { from: "Candidate", to: "E", label: "heartbeat (теперь лидер)" },
  ]}
  caption="Кандидат получает голоса от C и D — со своим голосом это 3 из 5, то есть majority — поэтому становится лидером term 8 и начинает слать heartbeat-ы. Голос отдаётся, только если нода не голосовала в этом term и лог кандидата не менее свежий."
/>

<KeyTakeaway>Рандомизированные election timeout делают split vote редкими. Правило completeness лога при голосовании гарантирует, что каждый новый лидер имеет все ранее закоммиченные entry. Вместе они дают Leader Completeness — инвариант, что ни одна подтверждённая запись никогда не теряется при смене лидера.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="raft-mid-retrieval2"
  lang="ru"
  questions={[
    { q: "Почему Raft требует большинства для выборов, а не просто 2 из 5?", a: "Потому что любые два большинства множества из 5 нод должны иметь хотя бы одну общую ноду (pigeonhole). Эта общая нода переносит полную закоммиченную историю от старого большинства к новому. При некворумном голосовании (например 2 из 5) два несвязных множества могли бы каждое считать себя лидером, коммитить конфликтующие entry и создать split brain." },
    { q: "Нода вернулась после 10 минут offline. У неё lastLogTerm=5, lastLogIndex=200. Кластер на term 12, лидер имеет lastLogIndex=9500. Может ли эта нода выиграть выборы?", a: "Нет. Лог вернувшейся ноды сильно отстаёт: lastLogTerm=5 меньше текущего lastLogTerm кластера (минимум 12). Любой voter с большим lastLogTerm откажет. Даже если term-ы были равны, lastLogIndex=200 против 9500 проигрывает. Нода переходит в follower и догоняет лог через AppendEntries, прежде чем стать жизнеспособным candidate." },
    { q: "В чём разница между Election Safety и Leader Completeness?", a: "Election Safety говорит: максимум один лидер на term — гарантируется одним голосом на ноду на term плюс большинством. Leader Completeness говорит: каждый лидер term T+1 имеет все entry, закоммиченные в term-ах 1 через T — гарантируется пересечением кворумов плюс правилом completeness лога. Election Safety предотвращает двух одновременных лидеров; Leader Completeness предотвращает лидера без истории." },
  ]}
/>

<Recap lang="ru">
Raft предотвращает повторные split vote рандомизацией election timeout — первый срабатывает follower скорее всего собирает большинство до того, как остальные вообще стартуют. Правило голосования RequestVote требует от candidate-ов иметь логи как минимум так же актуальные, как у любого voter-а — вместе с пересечением кворумов это гарантирует, что выигравший лидер имеет каждое ранее закоммиченное entry (Leader Completeness). Четыре свойства безопасности — Election Safety, Leader Append-Only, Log Matching и Leader Completeness — вместе гарантируют, что никакие две ноды никогда не применяют разные команды на одном index. Типичные выборы лидера на здоровом кластере разрешаются за 100–500 мс. Теперь, когда видишь, что candidate проигрывает выборы несмотря на более долгий uptime — знаешь почему: побеждает не старшинство, а проверка completeness лога.
</Recap>
