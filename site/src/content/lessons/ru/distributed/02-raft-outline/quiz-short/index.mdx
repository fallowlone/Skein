---
concepts:
- quorum-overlap
- commit-safety
- leader-completeness
- readindex-lease
- snapshots
- joint-consensus
estMin: 14
lang: ru
level: senior
order: 8
prereqs: []
slug: quiz-short
sources:
- https://raft.github.io/raft.pdf
- https://raft.github.io/
- https://developer.hashicorp.com/consul/docs/concept/consensus
status: ready
summary: "Промпты на свободное воспроизведение по всему юниту Raft. Ответь на каждый по памяти, затем открой модельный ответ и сравни с механизмом, которому учили уроки."
title: 'Raft: тест на свободное воспроизведение'
track: distributed
unit: 02-raft-outline
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";

<Hook>
Воспроизведение бьёт перечитывание. Для каждого промпта восстанови полный механизм по памяти — вслух или на бумаге — прежде чем открыть модельный ответ. Именно усилие припоминания закрепляет аргумент безопасности.
</Hook>

<Goal lang="ru">
Восстанови стержень юнита, не подглядывая: аргумент безопасности через пересечение кворумов, что значит «закоммичено», почему правило голосования сохраняет историю, как линеаризуемые чтения избегают записи в лог и почему смена состава требует joint consensus.
</Goal>

<RetrievalDrawer
  client:visible
  id="02-raft-outline-short-retrieval"
  lang="ru"
  questions={[
    { q: "Почему требование majority quorum и для выборов, и для коммитов предотвращает split brain, и почему именно majority, а не любая группа фиксированного размера?", a: "Ключевое свойство — пересечение: любые два majority одного множества делят хотя бы один узел (принцип Дирихле). Поэтому если commit-кворум персистировал запись, а более поздний election-кворум избирает нового лидера, эти множества пересекаются, и через общий узел новый лидер наследует закоммиченную запись. Не-majority кворум — скажем, 2 из 5 — мог бы образовать две непересекающиеся группы, каждая считала бы себя авторитетной, избрала бы лидера и закоммитила бы конфликтующие записи: split brain. Majority — наименьший кворум, гарантирующий пересечение, поэтому кластеры размером 3, 5 или 7." },
    { q: "Что значит «закоммичено» в Raft и почему одиночного fsync недостаточно?", a: "Запись закоммичена, когда majority кластера персистировал её на диск — только тогда она долговечна при любой смене лидера. Если её сделал fsync лишь лидер и лидер падает, новый лидер может быть избран кворумом, который этой записи не видел, и consistency check перепишет её на старом лидере при возвращении. Поскольку клиенту незакоммиченную запись никогда не подтверждали, её отбрасывание ничего не теряет. Лидер сигнализирует commitment продвижением commitIndex и подкладывает его на следующем AppendEntries/heartbeat, чтобы follower'ы применили до этой точки." },
    { q: "Как правило полноты лога в RequestVote сочетается с пересечением кворумов, давая Leader Completeness, и какова дополнительная оговорка про коммит текущего term?", a: "Голосующий выдаёт голос, только если лог кандидата как минимум столь же актуален (выигрывает больший lastLogTerm; при равных term'ах — больший lastLogIndex). Любой election-кворум пересекается с каждым прошлым commit-кворумом, и общий узел откажет кандидату, у которого нет его закоммиченных записей — значит, победитель держит всю закоммиченную историю. Это Leader Completeness: каждый лидер term'а T+1 имеет каждую запись, закоммиченную в term'ах 1..T. Оговорка: лидер вправе напрямую коммитить лишь запись своего текущего term. Подсчёт реплик для коммита записи прошлого term снова открывает figure-8, где якобы закоммиченную запись позже переписывают; прошлые записи коммитятся косвенно, когда коммитится запись текущего term над ними." },
    { q: "Сравни ReadIndex и lease reads для линеаризуемых чтений и назови условие корректности, делающее lease reads рискованными.", a: "Наивное линеаризуемое чтение коммитит no-op запись в лог на каждое чтение — полный раунд консенсуса, слишком дорого при высоком отношении чтений к записям. ReadIndex избегает записи в лог: лидер фиксирует commitIndex, шлёт один heartbeat-раунд, подтверждая, что majority всё ещё признаёт его лидером (чтобы партиционированный устаревший лидер не обслуживал), ждёт, пока его state machine применит до этого индекса, и отвечает — цена один round-trip, ~1–5 мс внутри региона. Lease reads идут дальше: пока лидер держит ограниченный по времени lease (обычно election_timeout * 0.9), он обслуживает чтения без RPC, субмиллисекундно. Риск: валидность lease зависит от ограниченного clock skew. Если часы лидера спешат, lease у follower'а может истечь первым и избрать нового лидера, пока старый ещё обслуживает чтения — устаревшие данные как свежие. Поэтому NTP/PTP-синхронизация — требование корректности для lease reads, а не гигиена." },
    { q: "Почему изменение состава кластера требует joint consensus и что именно обеспечивает joint-фаза?", a: "Атомарная одношаговая замена конфига создаёт окно, где часть узлов следует старому конфигу, часть — новому. В этом окне могут существовать два непересекающихся majority — например, 2 из 3 под C_old и 3 из 5 под C_new без общего узла — и каждый мог бы избрать лидера: split brain. Joint consensus вводит промежуточный конфиг C_old_new, в котором каждый коммит и выборы требуют majority ОДНОВРЕМЕННО старого и нового конфига. Это гарантирует, что любые два валидных кворума в переходе пересекаются. Когда C_old_new закоммичен по всему кластеру, кластер переходит к C_new в одиночку. Production-системы упрощают до single-server changes (добавить/убрать один узел за раз), обычно вводя новые узлы как non-voting learner, пока они не догонят." },
    { q: "Каков минимально жизнеспособный дашборд Raft и на какую корневую причину указывает каждая метрика?", a: "Пять метрик. (1) leader_changes_total/мин выше ~1 — мигающее лидерство; проверь fsync диска и heartbeat RTT. (2) wal_fsync_duration p99 выше ~50 мс — диск слишком медленный; перенеси WAL на выделенный NVMe. (3) commit_latency p99 выше ~100 мс — диск, сеть или отставание follower'а. (4) follower_lag_entries выше ~1000 — follower отстаёт и скоро может потребовать InstallSnapshot. (5) рост raft_term выше ~2/мин — повторные выборы; проверь network jitter и включён ли pre-vote. На практике почти любой инцидент сводится к одной из трёх корневых причин: смена состава в обход joint consensus, отключённый pre-vote или WAL на медленном разделяемом облачном томе." },
  ]}
/>

<Recap lang="ru">
Если ты смог восстановить каждый ответ по памяти, ты держишь стержень юнита: majority quorum принуждает к пересечению, из-за которого закоммиченная (персистированная majority) запись переживает любую смену лидера; голос по полноте лога плюс это пересечение дают Leader Completeness, а правило коммита текущего term закрывает дыру figure-8; ReadIndex и lease reads обслуживают линеаризуемые чтения без записи в лог — lease reads меняют условие корректности по NTP на субмиллисекундную задержку; а joint consensus не даёт смене состава расколоть кластер. Следи за пятью метриками и помни, что реальные инциденты почти всегда сводятся к обойдённой смене состава, отключённому pre-vote или медленному диску.
</Recap>
