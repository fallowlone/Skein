---
concepts:
- raft
- consensus
- leader-election
- quorum
- term
deepensInto:
- distributed/02-raft-outline/02-log-replication-and-commit
estMin: 10
lang: ru
lessonType: topic
level: junior
order: 1
prereqs:
- distributed/01-cap-practice
slug: 01-raft-roles-and-terms
sources:
- https://raft.github.io/raft.pdf
- https://raft.github.io/
- https://en.wikipedia.org/wiki/Raft_(algorithm)
spiral:
- statefulness
status: ready
summary: Три роли нод Raft, монотонный счётчик term и правило кворума, делающее невозможным
  одновременное существование двух лидеров.
title: Роли Raft, term и почему majority-кворум предотвращает split brain
track: distributed
unit: 02-raft-outline
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import MetaphorComplete from "~/components/pedagogy/MetaphorComplete.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";
import Infographic from "~/components/diagram/Infographic.astro";
import failureTolerance from "./infographics/failure-tolerance.svg?raw";

<Hook>
Твой Kubernetes-кластер подкреплён 5-нодовым etcd. Одна нода теряет питание посреди рабочего дня. Ты запускаешь `kubectl get pods` — и он работает без ошибок и задержек. Как консенсус сохраняется с упавшей нодой?
</Hook>

<Crux>Raft сохраняет консистентность, выбирая одного лидера на term и требуя majority-кворум для каждого решения.</Crux>

<Explanation>

## Задача: одна машина из многих

Сложнейшая проблема распределённых систем — это согласие. Если пять нод независимо принимают записи, у тебя получится пять противоречивых историй. Задача Raft — заставить эти пять нод вести себя как одна: одинаковый порядок изменений, одинаковое состояние, никаких потерянных записей. Для этого в каждый момент времени выбирается ровно один лидер, и все изменения направляются через него.

## Три роли

Понимание трёх ролей — это то, что позволяет читать status page Raft или post-mortem и сразу понимать, какой инвариант устоял, а какой сломался.

Каждая Raft-нода находится ровно в одном из трёх состояний:

- **Follower** — состояние по умолчанию. Принимает и хранит log entry от лидера. Клиентские записи напрямую не принимает.
- **Candidate** — follower, переставший слышать лидера и теперь баллотирующийся на лидерство. Временное состояние до разрешения выборов.
- **Leader** — нода, которой клиенты отправляют записи. Управляет репликацией на всех follower-ах. В здоровом кластере существует максимум один лидер на term.

Нода стартует как follower. Становится candidate, когда срабатывает election timeout. Становится лидером, если выигрывает голосование большинства.

## Term: монотонные логические часы

Raft отслеживает время не настенными часами, а **term-ами** — монотонно возрастающими целыми числами. Каждый term начинается с выборов. Если лидер побеждает, он ведёт весь term. Если лидер не определился (split vote), term заканчивается и начинается новый.

Term выполняет две задачи:

1. **Дедупликация.** При получении сообщения ноды сравнивают term отправителя со своим. Больший term всегда побеждает — получатель обновляет свой term и понижается до follower при необходимости. Это мгновенно разрешает путаницу со stale-лидерами.
2. **Упорядочение.** Каждый log entry тегируется term-ом, в котором он был записан. Этот тег используется позже для обнаружения расхождений лога.

Вместе эти две задачи делают term единственными общими часами, позволяющими всегда ответить на вопрос «кто сейчас главный?» — без какого-либо соглашения по настенным часам. Убери любую из задач — получишь либо зомби-лидера, либо необнаруживаемое расхождение лога.

<div data-lesson-visual class="overflow-x-auto my-6">

| Term | Что произошло |
|------|--------------|
| 1 | Нода A выбрана лидером. Вела 30 с. |
| 2 | A кратко потеряла сеть. B выиграла выборы. |
| 3 | B упала. C выиграла выборы. |
| 4 | C всё ещё лидер — новые выборы не нужны. |

</div>

Term-ы никогда не повторяются. Если ты видишь term 7, каждое сообщение из term 6 — stale.

## Majority-кворум: барьер против split brain

Raft требует **большинства** (больше половины кластера) для двух операций: **выборов** и **коммитов**. В 5-нодовом кластере большинство — 3.

Почему именно большинство? Ключевое свойство — пересечение: любые два большинства одного множества имеют хотя бы один общий элемент. В 5-нодовом кластере, если одна тройка коммитит entry, а другая тройка выбирает нового лидера, эти два множества не могут не пересекаться — у них есть общая нода. Через эту общую ноду новый лидер гарантированно видел закоммиченное entry.

Если бы Raft использовал простое plurality (2 из 5) вместо большинства, две отдельные группы по 2 могли бы каждая считать себя авторитетной — split brain (состояние, когда два узла одновременно уверены, что они лидер, и независимо коммитят записи). Большинство это предотвращает.

**Tolerance отказов:** кластер из N нод переносит `floor((N-1)/2)` одновременных отказов. 5 нод → 2 отказа. 3 ноды → 1 отказ. Поэтому Raft-кластеры по 3, 5 или 7 нод — нечётные числа максимизируют tolerance для данного размера.

<Infographic
  svg={failureTolerance}
  label="Столбчатая диаграмма переносимых отказов по размеру Raft-кластера: кластер из 3 нод переживает 1 отказ, из 5 нод — 2, из 7 нод — 3, по правилу floor((N-1)/2)."
  caption="Каждый шаг в +2 ноды даёт ровно один дополнительный переносимый отказ — поэтому продакшен-кластеры Raft бывают по 3, 5 или 7 нод, но не чётными."
  hue="var(--d-infra)"
/>

<Quiz
  id="raft-jr-q1"
  lessonSlug="01-raft-roles-and-terms"
  lang="ru"
  question="5-нодовый Raft-кластер разделён: DC A имеет лидера и 2 follower-а (3 ноды), DC B — 2 follower-а. Связь между DC обрывается. Что происходит?"
  choices={[
    { label: "Обе стороны продолжают принимать записи — split brain", misconception: "Raft отказывается коммитить на любой стороне, не имеющей большинства. Split brain исключён по дизайну." },
    { label: "DC A (3 из 5 нод = большинство) продолжает принимать записи; 2 ноды DC B не могут выбрать лидера", correct: true },
    { label: "Весь кластер замораживается до восстановления связи", misconception: "Сторона с большинством продолжает работу. Замирает только сторона с меньшинством." },
    { label: "DC B выбирает нового лидера после таймаута", misconception: "2 ноды не могут образовать большинство из 5. Их выборы никогда не завершатся." },
  ]}
/>

<Quiz
  id="raft-jr-q2"
  lessonSlug="01-raft-roles-and-terms"
  lang="ru"
  question="Почему Raft требует большинство (3 из 5), а не просто 2 из 5, для выборов и коммитов?"
  choices={[
    { label: "Чтобы голосование шло быстрее", misconception: "Большие кворумы медленнее. Большинство нужно ради корректности, а не скорости." },
    { label: "Чтобы гарантировать: любые два большинства имеют общую ноду, а новый лидер всегда наследует историю закоммиченных entry", correct: true },
    { label: "Чтобы кластер переносил 3 одновременных отказа", misconception: "5-нодовый кластер переносит только 2 отказа — 3 из 5 должны оставаться живыми." },
    { label: "Потому что нечётный размер кластера это требует", misconception: "Большинство — требование корректности протокола, а не следствие чётности размера кластера." },
  ]}
/>

<DragOrder
  id="raft-jr-drag1"
  lessonSlug="01-raft-roles-and-terms"
  lang="ru"
  prompt="Поставь шаги выбора лидера в правильном порядке:"
  items={[
    "Follower-ы перестают получать heartbeat дольше election timeout",
    "Follower переходит в candidate, увеличивает свой term и голосует за себя",
    "Candidate шлёт RequestVote RPC всем остальным нодам",
    "Каждая нода отдаёт голос максимум один раз за term, первому подходящему candidate",
    "Candidate собирает большинство голосов и становится лидером нового term",
    "Новый лидер начинает слать heartbeat-ы, утверждая свою власть",
  ]}
/>

<MetaphorComplete
  id="raft-jr-metaphor1"
  lessonSlug="01-raft-roles-and-terms"
  lang="ru"
  setup="Заполни пропуск: Raft использует совет из N членов, где только один в каждый момент держит _______ и предлагает новые законы."
  accepted={["молоток", "роль спикера", "лидерство", "мандат"]}
  canonical="молоток (или роль спикера / лидерство)"
  explanation="Максимум один спикер одновременно, выбран большинством голосов, заменён если молчит слишком долго. Raft формализует ровно эту дисциплину для компьютеров."
/>

</Explanation>

<FlowDiagram
  label="Три роли в Raft и переходы между ними"
  perRow={3}
  hue="var(--d-systems)"
  nodes={[
    { id: "follower", label: "Follower", sub: "хранит лог" },
    { id: "candidate", label: "Candidate", sub: "ведёт выборы" },
    { id: "leader", label: "Leader", sub: "ведёт записи" },
  ]}
  edges={[
    { from: "follower", to: "candidate", label: "сработал timeout" },
    { from: "candidate", to: "leader", label: "взял majority" },
    { from: "leader", to: "follower", label: "увидел больший term" },
    { from: "candidate", to: "follower", label: "split vote" },
  ]}
  caption="Нода стартует как Follower, становится Candidate, когда срабатывает election timeout, и становится Leader только после победы majority-голосованием. Больший term, увиденный лидером, возвращает его в Follower."
/>

<KeyTakeaway>Raft сохраняет консенсус, выбирая одного лидера на term. Majority-кворум (больше половины) гарантирует, что любые два кворума пересекаются — не позволяя двум лидерам одновременно коммитить, что и есть split brain.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="raft-jr-retrieval"
  lang="ru"
  questions={[
    { q: "Почему 5-нодовый Raft-кластер переживает 2 одновременных отказа, но не 3?", a: "Потому что и коммиты, и выборы требуют большинства — минимум 3 из 5 живых нод. При 3 отказах остаётся лишь 2 ноды, что ниже порога большинства. Эти 2 не могут ни о чём договориться, не могут выбрать лидера, не могут коммитить. Кластер ждёт возврата хотя бы одной упавшей ноды." },
    { q: "Что такое term в Raft и почему он заменяет настенные часы?", a: "Term — монотонно возрастающее целое число, начинающееся с каждых выборов. Он дедуплицирует stale-сообщения: любое сообщение с меньшим term-ом отвергается. Настенные часы у разных машин отличаются и могут дрейфовать; term — разделяемый логический счётчик, никогда не идущий назад и всегда однозначный." },
    { q: "Нода была offline 10 минут. Возвращается с term 4, кластер на term 9. Что происходит при её первом сообщении?", a: "Получатель видит term 4, который ниже его текущего term 9. Сообщение отвергается, и ответ несёт term 9. Вернувшаяся нода обновляет свой term до 9 и переходит в follower. Затем начинает догонять лог от лидера через AppendEntries." },
  ]}
/>

<Recap lang="ru">
Raft назначает каждой ноде одну из трёх ролей — follower, candidate или leader — и ровно один лидер существует на term. Term — монотонные логические часы, разрешающие путаницу со stale-лидерами: больший term всегда побеждает. И выборы, и коммиты требуют majority-кворума, который гарантирует: любые два кворума имеют общую ноду, делая невозможным одновременный коммит двух лидеров конкурирующих записей. 5-нодовый кластер переносит 2 одновременных отказа; 3 отказа оставляют 2 выживших ниже порога большинства и останавливают прогресс до восстановления. Следующий урок о том, как лидер реплицирует записи на follower-ы. Теперь, когда встретишь в etcd «leader changed» или «no leader elected» — ты знаешь, какой из трёх инвариантов был нарушен и почему.
</Recap>
