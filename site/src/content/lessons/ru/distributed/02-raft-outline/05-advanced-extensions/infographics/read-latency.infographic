infographic chart-column-simple
data
  title Latency линеаризуемого чтения по пути (intra-region)
  desc ReadIndex платит за один heartbeat round-trip; lease read обслуживает локально без RPC
  values
    - label ReadIndex (heartbeat RTT)
      value 5
    - label Lease read (без RPC)
      value 1
theme
  palette #6366f1 #14b8a6
