---
concepts:
- quorum-overlap
- commit-safety
- leader-completeness
- pre-vote
- joint-consensus
- cp-tradeoff
estMin: 14
lang: ru
level: senior
order: 7
prereqs: []
slug: quiz-choice
sources:
- https://raft.github.io/raft.pdf
- https://developer.hashicorp.com/consul/docs/concept/consensus
- https://github.com/hashicorp/raft/blob/main/membership.md
status: ready
summary: "Тест с выбором на синтез по всему юниту Raft — пересечение кворумов, безопасность коммита, правила выборов, CP-компромисс, pre-vote и безопасное изменение состава."
title: 'Raft: тест с выбором ответа'
track: distributed
unit: 02-raft-outline
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Шесть вопросов поперёк всего юнита. Каждый — решение, которое ты принимаешь, эксплуатируя реальный Raft-кластер (etcd, TiKV, Consul), а не определение для заучивания. Выбери ответ, который senior-инженер защитит на разборе инцидента.
</Hook>

<Goal lang="ru">
Убедись, что связываешь то, что уроки строили по отдельности: почему majority quorum — требование безопасности, что на самом деле значит «закоммичено», как правило голосования сохраняет историю, почему Raft останавливается при partition и какая ops-дисциплина держит кластер живым.
</Goal>

<Quiz
  id="02-raft-outline-choice-1"
  lessonSlug="02-raft-outline"
  lang="ru"
  question="Кластер из 5 узлов коммитит запись 101 кворумом {A, B, C}. Затем лидер A падает, и новый лидер избирается кворумом {C, D, E}. Почему запись 101 гарантированно переживёт смену term?"
  choices={[
    { label: "Два кворума {A,B,C} и {C,D,E} обязаны пересекаться — C входит в оба. Правило RequestVote отказывает кандидату, чей лог менее актуален, чем у голосующего, поэтому победитель имеет как минимум лог C, содержащий 101.", correct: true },
    { label: "Новый лидер проигрывает запись 101 из transaction log, который старый лидер отправил перед падением.", misconception: "В Raft нет внеполосной пересылки лога. Долговечность даёт только пересечение кворумов плюс правило полноты лога — новый лидер уже имел 101, потому что делит узел с commit-кворумом." },
    { label: "Raft автоматически перекоммитит все записи прошлого term в начале каждого нового term.", misconception: "Raft никогда слепо не перекоммитит записи прошлого term — это как раз небезопасно (проблема figure-8). Закоммиченная запись выживает из-за пересечения кворумов, а не из-за перекоммита." },
    { label: "Запись 101 безопасна, только если её подтвердили все пять узлов.", misconception: "Требование всех узлов убило бы доступность — один медленный узел блокировал бы каждый коммит. Majority достаточно именно потому, что любые два majority пересекаются." },
  ]}
/>

<Quiz
  id="02-raft-outline-choice-2"
  lessonSlug="02-raft-outline"
  lang="ru"
  question="Лидер дописывает запись 200 на свой диск и делает fsync, затем падает до того, как хоть один AppendEntries покинул машину. Какое утверждение о записи 200 верно?"
  choices={[
    { label: "Она не была закоммичена — её держал лишь один узел. Лог нового лидера заканчивается до 200, и когда старый лидер возвращается, consistency check заставляет его усечь 200. Клиенту ничего не подтверждали, поэтому ничего не потеряно.", correct: true },
    { label: "Она закоммичена, потому что прошла fsync в долговечное хранилище на лидере.", misconception: "fsync на одном узле — это долговечность, а не commitment. Commitment в Raft означает, что запись персистировал majority; одиночный fsync можно отбросить при смене лидера." },
    { label: "Новый лидер обязан сохранить запись 200, потому что Leader Append-Only запрещает удалять записи.", misconception: "Leader Append-Only ограничивает собственный лог лидера, а не follower'а. Вернувшийся старый лидер теперь follower и обязан усечь свой незакоммиченный хвост под нового лидера." },
    { label: "Кластер теперь неконсистентен и требует ручного ремонта.", misconception: "Отбрасывание неподтверждённой, незакоммиченной записи — нормальное, автоматическое и безопасное действие, ровно то, что обрабатывает Log Matching. Ручной ремонт не нужен." },
  ]}
/>

<Quiz
  id="02-raft-outline-choice-3"
  lessonSlug="02-raft-outline"
  lang="ru"
  question="Узел возвращается после 30-минутного partition. Всё это время он таймаутил и инкрементировал term, поэтому теперь у него term гораздо выше, чем у живого лидера, но лог устаревший. Что происходит без pre-vote и что меняет pre-vote?"
  choices={[
    { label: "Его RequestVote с высоким term заставляет здоровые узлы поднять term, а лидера — уйти в отставку, вызывая ненужные выборы. Pre-vote сначала делает необязывающий пробный прогон; голосующие отвергают устаревший лог, узел не инкрементирует term, и лидер не потревожен.", correct: true },
    { label: "Он немедленно становится лидером, потому что у него самый высокий term.", misconception: "Высокий term сам по себе никогда не выигрывает выборы — правило полноты лога заставляет голосующих отвергнуть кандидата с менее актуальным логом. Урон без pre-vote — спровоцированные ненужные выборы, а не захват." },
    { label: "Ничего не происходит; кластер игнорирует любой узел с устаревшим логом.", misconception: "Без pre-vote кластер не может его проигнорировать: высокий term в RequestVote заставляет каждого получателя принять этот term и снять лидера ещё до того, как проверка лога вообще сыграет." },
    { label: "Pre-vote ускоряет выборы после возвращения узла.", misconception: "Pre-vote добавляет один RPC-раунд, когда выборы действительно нужны; его цель — предотвратить ненужные выборы, а не ускорить законные." },
  ]}
/>

<Quiz
  id="02-raft-outline-choice-4"
  lessonSlug="02-raft-outline"
  lang="ru"
  question="Network partition оставляет текущего лидера на стороне меньшинства — он достаёт лишь 1 из 4 других узлов. Клиенты на стороне лидера получают таймауты записи; сторона большинства избирает нового лидера. Это баг?"
  choices={[
    { label: "Нет — это CP-гарантия Raft, работающая как задумано. Сторона меньшинства не может собрать quorum и отказывается коммитить, лишь бы второй лидер не закоммитил конфликтующие записи. При heal старый лидер видит более высокий term, уходит в отставку и усекает незакоммиченный хвост.", correct: true },
    { label: "Да — корректная система должна позволить обеим сторонам принимать записи и слить их позже.", misconception: "Это AP-поведение (Dynamo/Cassandra). У Raft нет merge; он осознанно жертвует доступностью на стороне меньшинства, чтобы гарантировать единую консистентную историю." },
    { label: "Да — кластер должен полностью замёрзнуть, пока partition не залечится.", misconception: "Останавливается только сторона меньшинства. Сторона большинства имеет quorum и продолжает обслуживать нормально — заморозка всего отбросила бы доступность, которую Raft может безопасно дать." },
    { label: "Не баг, но два лидера означают, что у кластера кратко split brain.", misconception: "Split brain нет: реально коммитить может только лидер на стороне большинства. Устаревший лидер меньшинства не достаёт quorum, поэтому его записи не завершаются." },
  ]}
/>

<Quiz
  id="02-raft-outline-choice-5"
  lessonSlug="02-raft-outline"
  lang="ru"
  question="Оператору нужно вырастить кластер с 3 до 5 узлов. Он одним атомарным шагом раскатывает новый конфиг из 5 узлов на все узлы, минуя joint consensus. В чём реальный риск?"
  choices={[
    { label: "В окне смены конфига узлы на новом конфиге и узлы ещё на старом могут образовать два непересекающихся majority — 2 из 3 (старый) и 3 из 5 (новый) — и избрать сразу двух лидеров. Joint consensus требует majority ОБОИХ конфигов, поэтому два кворума не могут быть непересекающимися.", correct: true },
    { label: "Два новых узла стартуют с пустыми логами и тормозят коммиты, пока не догонят.", misconception: "Медленный догон — вопрос производительности, решаемый добавлением узлов сначала как learner, а не угроза безопасности. Одношаговая замена рискует двумя непересекающимися majority, то есть split brain." },
    { label: "etcd не поддерживает добавление более одного узла, поэтому вызов просто провалится.", misconception: "Single-server-change — это дисциплина безопасности поверх joint consensus, а не жёсткий лимит библиотеки. Опасность обхода — реальный split brain, задокументированный в production-постмортемах." },
    { label: "Новые узлы не могут голосовать до первого heartbeat, поэтому выборы зависнут.", misconception: "Право голоса тут ни при чём. Проблема — нарушение свойства пересечения кворумов в окне разрыва конфигурации." },
  ]}
/>

<Quiz
  id="02-raft-outline-choice-6"
  lessonSlug="02-raft-outline"
  lang="ru"
  question="Кластер показывает рост leader_changes_total до нескольких в минуту, а клиенты видят периодические зависания записи на 200–400 мс. wal_fsync_duration_seconds p99 = 1.8 с. Диагноз и первый фикс?"
  choices={[
    { label: "fsync лидера превышает интервал heartbeat, поэтому follower'ы таймаутят посреди fsync и стартуют выборы; новый лидер бьётся в тот же медленный диск, и цикл повторяется. Первый фикс: перенести Raft WAL на выделенный локальный NVMe.", correct: true },
    { label: "Поднять heap / дать процессу больше памяти, чтобы runtime перестал паузить.", misconception: "Это проблема latency диска в WAL консенсуса, а не памяти. Никакой тюнинг heap не меняет latency fsync на разделяемом облачном томе." },
    { label: "Вырастить кластер с 5 до 7 узлов, чтобы размазать нагрузку.", misconception: "Больше голосующих увеличивает размер quorum и веер fsync — это ухудшает проблему медленного диска, а не лечит. Сначала чини диск." },
    { label: "Высокая скорость роста term означает, что кластер атакует вредоносный узел.", misconception: "Raft устойчив к падениям (CFT), а не к Byzantine. Мигающее лидерство с высоким fsync p99 — это сигнатура медленного диска, а не атаки." },
  ]}
/>

<Recap lang="ru">
Стержень юнита — один аргумент безопасности и одна ops-дисциплина. Безопасность: majority quorum гарантирует пересечение любых двух кворумов, поэтому закоммиченную (персистированную majority) запись всегда наследует следующий лидер, а правило полноты лога блокирует устаревшего кандидата; вместе это Leader Completeness, и «закоммичено» всегда значит «персистировано majority». Дисциплина: Raft — это CP, поэтому меньшинство при partition останавливается по дизайну; pre-vote не даёт возвращающимся узлам спровоцировать ненужные выборы; joint consensus не даёт смене состава расколоть кластер; а WAL живёт на выделенном NVMe, чтобы fsync не голодал heartbeat. Почти любой production-инцидент сводится к одному из трёх — обойдённый протокол смены состава, отключённый pre-vote или медленный диск.
</Recap>
