---
concepts:
- clock-skew
- lamport-timestamps
- vector-clocks
- happens-before
- last-write-wins
- truetime
estMin: 13
lang: ru
level: senior
order: 8
prereqs: []
slug: quiz-short
sources:
- https://aphyr.com/posts/299-the-trouble-with-timestamps
- https://research.google.com/pubs/archive/39966.pdf
- https://en.wikipedia.org/wiki/Vector_clock
status: ready
summary: Промпты на свободное припоминание по юниту. Сначала ответь своими словами, затем раскрой модельный ответ и сравни.
title: 'Часы: свободное припоминание'
track: distributed
unit: 05-clocks
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";

<Hook>
Припоминание сильнее перечитывания. На каждый промпт проговори или запиши полный ответ по памяти, прежде чем открыть модельный — именно усилие припоминания закрепляет материал.
</Hook>

<Goal lang="ru">
Восстанови ключевые идеи юнита — почему настенное время не может упорядочивать распределённые записи, отношение happens-before, Lamport vs vector clock и как TrueTime делает физическое время надёжным — не подглядывая в урок.
</Goal>

<RetrievalDrawer
  client:load
  id="05-clocks-short-retrieval"
  lang="ru"
  questions={[
    { q: "Почему timestamp с машины A и timestamp с машины B несравнимы как упорядочивание, даже при запущенном NTP?", a: "Часы каждой машины — это свободно идущий кварцевый кристалл, дрейфующий на десятки ppm, поэтому он блуждает на миллисекунды в минуту. NTP корректирует это, slew'ингом или step'ом подводя часы к эталону, и step может прыгнуть НАЗАД на сотни миллисекунд — на миг 'сейчас' оказывается раньше 'сейчас', которое ты уже прочитал. Даже здоровый NTP оставляет миллисекунды skew по флоту; зависший ntpd или VM, приостановленная гипервизором, доводят это до секунд или минут. Так что два настенных timestamp разделяют единицы и больше ничего: tsA < tsB надёжно не означает, что A произошло раньше B. Как только корректность зависит от этого сравнения, ты построил на песке." },
    { q: "Пройди по шагам, как именно clock skew превращается в тихую потерю данных под last-write-wins.", a: "LWW разрешает конфликты, оставляя запись с наибольшим timestamp. Когда часы одного узла отстают, реально более новая запись, прошедшая через него, получает timestamp раньше строки, которую должна заменить. Разрешение конфликта сравнивает два числа, оставляет большее (от отстающих часов) и отбрасывает новую запись. Убийственная деталь: клиент всё равно получает успешный ответ — нет исключения, нет сигнала о несобранном кворуме, нет лог-строки, отличающей отброшенную запись от той, что вообще не случилась. Узнаёшь, когда клиент через дни сообщает, что его изменение пропало. С удалениями хуже: tombstone, проштампованный далеко в будущем, подавляет каждую реальную запись, пока его не соберёт compaction — а это могут быть дни." },
    { q: "Дай определение отношения happens-before и что Lamport timestamp гарантирует относительно него.", a: "Happens-before (A → B) — это причинный частичный порядок: A happens-before B, если они на одном процессе и A первее, либо A — это send, а B — соответствующий receive, либо по транзитивности. События без такого пути конкурентны. Lamport clock — это один счётчик на процесс: инкремент на каждое локальное событие, а на приём — установка счётчика в max(local, received) + 1. Это гарантирует: если A → B, то L(A) < L(B). Обратное НЕ верно: L(A) < L(B) не означает A → B, потому что двум конкурентным событиям всё равно назначается какой-то порядок. Так что Lamport даёт согласованный тотальный порядок, согласованный с причинностью, но не восстанавливает причинность из чисел — он навязывает порядок, он не детектирует конкурентность." },
    { q: "Когда нужны vector clock вместо Lamport timestamp и чего они стоят?", a: "Тянись к vector clock, когда обязан ДЕТЕКТИРОВАТЬ конкурентность, а не просто навязать порядок — обычно разрешение конфликтов в реплицированном хранилище, где два клиента могут писать один ключ, не видя друг друга. Каждый узел несёт вектор с одним счётчиком на узел и возит весь вектор с каждым сообщением; на приём берёшь поэлементный max и бампишь только свою компоненту. Сравнение двух векторов говорит, произошло ли одно happens-before другого (каждая компонента меньше-или-равна, хотя бы одна строго меньше) или ни один не доминирует — тогда записи concurrent и хранилище держит обе как siblings для слияния. Цена — O(n) метаданных на штампованное значение, где n — число узлов, они едут с каждой записью и растут с кластером, поэтому реальные системы прунят или скоупят векторы (например, dotted version vectors)." },
    { q: "Что отдаёт TrueTime в Spanner и как commit-wait использует это для external consistency?", a: "TrueTime оснащает каждый датацентр GPS-приёмниками и атомными часами и отдаёт TT.now() не как момент, а как интервал [earliest, latest], ширина которого — неопределённость ε, обычно около 1мс, иногда единицы мс. Commit-wait назначает транзакции commit timestamp s, затем намеренно спит, пока TT.now().earliest > s — пока не станет уверен, что s лежит в прошлом везде. Это ожидание примерно равно ε и перекрывается с репликацией Paxos, поэтому часто добавляет мало реального времени. Выигрыш — external consistency: если T1 коммитится до старта T2, timestamp T1 гарантированно меньше T2, глобально, без оговорок про skew. Spanner превращает неопределённость часов из тихого бага корректности в явную ограниченную стоимость по задержке." },
    { q: "Коллега говорит 'просто запусти NTP, и last-write-wins безопасен.' Дай senior-опровержение в один абзац.", a: "NTP уменьшает skew в среднем, но LWW — это не среднее, это худший случай, и он падает на том одном узле и той одной минуте, когда часы проскользнули. Step может сдвинуть часы назад, а зависший демон или приостановленная VM оставляют секунды skew; в это окно более новая запись штампуется раньше строки, которую должна заменить, проигрывает сравнение timestamp и отбрасывается, а клиент получает 200 OK. Ничто в мониторинге не отличает эту потерю от записи, которой не было. Так что NTP покупает меньшее окно потерь, а не безопасность. Если надо упорядочивать или сливать распределённые записи, используй логические часы (Lamport для тотального порядка, vector clock для детекции конкурентности) или плати за настоящее упорядочивание через TrueTime commit-wait — но никогда не доверяй настенным часам." },
  ]}
/>

<Recap lang="ru">
Если ты смог восстановить каждый ответ по памяти, ты держишь хребет юнита: настенное время дрейфует и прыгает назад, поэтому оно не примитив упорядочивания, и LWW превращает skew в тихую потерю данных; happens-before — это причинный частичный порядок; Lamport timestamp дают дешёвый тотальный порядок, согласованный с причинностью, но не детектируют конкурентность; vector clock возвращают детекцию конкурентности ценой O(n) метаданных; а TrueTime делает физическое время надёжным, квантуя неопределённость как ограниченный интервал и пережидая его. Senior-инстинкт: никогда не упорядочивай распределённые записи по настенным часам.
</Recap>
