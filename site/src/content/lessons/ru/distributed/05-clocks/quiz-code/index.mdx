---
concepts:
- lamport-timestamps
- vector-clocks
- happens-before
- concurrency-detection
- clock-skew
estMin: 14
lang: ru
level: senior
order: 9
prereqs: []
slug: quiz-code
sources:
- https://en.wikipedia.org/wiki/Vector_clock
- https://aphyr.com/posts/299-the-trouble-with-timestamps
status: ready
summary: "Читай код обновления часов и трассы событий — инкремент Lamport, merge vector clock, сравнение happens-before и детекцию конкурентности — и предсказывай результат."
title: 'Часы: чтение кода и трасс'
track: distributed
unit: 05-clocks
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Логические часы — это крошечные куски кода с тонкими правилами корректности. Прочитай каждую процедуру обновления и трассу событий, затем выбери ответ, который senior-инженер защитит на ревью — где на самом деле живёт баг, порядок или вердикт о конкурентности.
</Hook>

<Goal lang="ru">
Отработай базовые операции логических часов в коде: инкремент Lamport на отправку и приём, merge vector clock, сравнение happens-before и детекцию того, что два события конкурентны, а не упорядочены.
</Goal>

## Сниппет 1 — приём Lamport

```python
class LamportClock:
    def __init__(self):
        self.t = 0

    def on_local_event(self):
        self.t += 1
        return self.t

    def on_send(self):
        self.t += 1
        return self.t            # piggyback self.t on the message

    def on_receive(self, msg_t):
        self.t = self.t          # <-- BUG candidate
        self.t += 1
        return self.t
```

<Quiz
  id="05-clocks-code-1"
  lessonSlug="05-clocks"
  lang="ru"
  question="Обработчик приёма игнорирует msg_t. Какой инвариант это ломает и каков фикс?"
  choices={[
    { label: "Это ломает гарантию happens-before: send обязан причинно предшествовать своему receive, поэтому часы получателя должны перепрыгнуть часы отправителя. Фикс: self.t = max(self.t, msg_t) + 1, чтобы сообщение от более высоких часов подтянуло получателя вперёд.", correct: true },
    { label: "Ничего не ломается — инкремента на единицу при приёме достаточно для Lamport clock.", misconception: "Простой инкремент игнорирует прогресс отправителя. Если часы отправителя были далеко впереди, событие receive может получить МЕНЬШИЙ timestamp, чем send, нарушая то, что send happens-before receive." },
    { label: "Баг в том, что on_receive должен сбрасывать self.t ровно в msg_t.", misconception: "Сброс в msg_t может сдвинуть часы назад и теряет собственный локальный прогресс. Правило — max из двух плюс единица, никогда не просто принять входящее значение." },
    { label: "Он дважды считает событие; on_receive вообще не должен инкрементить.", misconception: "Приём — ЭТО событие и обязан продвинуть часы. Дефект в отбрасывании msg_t, а не в инкременте; фикс сначала берёт max, затем инкрементит." },
  ]}
/>

## Сниппет 2 — merge vector clock

```python
def merge_receive(local, msg_vec, my_id):
    # local and msg_vec are dicts: node_id -> counter
    merged = {}
    for node in set(local) | set(msg_vec):
        merged[node] = max(local.get(node, 0), msg_vec.get(node, 0))
    merged[my_id] += 1
    return merged
```

<Quiz
  id="05-clocks-code-2"
  lessonSlug="05-clocks"
  lang="ru"
  question="Этот merge при приёме корректен. Какое утверждение объясняет, почему поэлементный max плюс один self-инкремент — ровно то, что нужно?"
  choices={[
    { label: "Поэлементный max вбирает всё, что видел отправитель (включая события, которых этот узел не видел); инкремент только my_id фиксирует, что приём — это одно новое локальное событие. Вместе они сохраняют happens-before через сообщение.", correct: true },
    { label: "Нужно инкрементить каждую компоненту, а не только my_id, чтобы держать вектор в синхроне.", misconception: "Инкремент каждой компоненты ложно заявил бы, что этот узел наблюдал новые события на каждом другом узле. На локальном/приёмном событии продвигается только собственный счётчик; остальные мержатся через max." },
    { label: "Нужно покомпонентно сложить два вектора вместо взятия max.", misconception: "Сложение дважды считает события, которые обе стороны уже разделяют, и ломает семантику сравнения. Max держит каждую компоненту на последнем счёте, который каждая сторона реально видела." },
    { label: "Он багован, потому что не инкрементит компоненту отправителя.", misconception: "Получатель никогда не должен продвигать счётчик отправителя — им владеет только отправитель. Получатель бампит свою компоненту и максит остальные." },
  ]}
/>

## Сниппет 3 — сравнение двух vector clock

```python
def compare(a, b):
    # returns 'before', 'after', or 'concurrent'
    le = all(a.get(k, 0) <= b.get(k, 0) for k in set(a) | set(b))
    ge = all(a.get(k, 0) >= b.get(k, 0) for k in set(a) | set(b))
    if le and not ge: return 'before'      # a happens-before b
    if ge and not le: return 'after'
    if le and ge:     return 'equal'
    return 'concurrent'
```

```
A = {n1: 2, n2: 1, n3: 0}
B = {n1: 1, n2: 1, n3: 4}
```

<Quiz
  id="05-clocks-code-3"
  lessonSlug="05-clocks"
  lang="ru"
  question="Что вернёт compare(A, B) для этих двух векторов и что это значит для разрешения конфликтов?"
  choices={[
    { label: "'concurrent' — A впереди по n1 (2 vs 1), а B впереди по n3 (4 vs 0), поэтому ни один не доминирует. Хранилище должно держать обе версии как siblings, а не выбирать победителя.", correct: true },
    { label: "'before' — сумма B (6) больше суммы A (3), значит A happens-before B.", misconception: "Vector clock сравнивают покомпонентно, никогда суммированием. A впереди по n1, B впереди по n3, поэтому ни один не меньше-или-равен повсюду — это и есть определение concurrent." },
    { label: "'after' — у A наибольшая отдельная компонента (n1 = 2).", misconception: "Одна большая компонента не делает A 'after'; нужно, чтобы КАЖДАЯ компонента A была больше-или-равна. Здесь B обходит A по n3, так что они concurrent, а не упорядочены." },
    { label: "'equal' — оба валидные состояния часов, значит взаимозаменяемы.", misconception: "Equal требует одинаковых счётов на каждом узле. Эти различаются по n1 и n3 в противоположных направлениях — ровно тот concurrent-случай, который merge обязан вынести наружу." },
  ]}
/>

## Сниппет 4 — трасса событий

```
P1: a(send m1)            Lamport: a=1
P2: b(recv m1) -> c(send m2)
P3: d(recv m2)
P1: e(local)              Lamport: e=2
```

<Quiz
  id="05-clocks-code-4"
  lessonSlug="05-clocks"
  lang="ru"
  question="Используя happens-before по этой трассе, каково отношение между e на P1 и d на P3?"
  choices={[
    { label: "Concurrent. Причинная цепочка — a -> b -> c -> d (через m1, затем m2); e — это более позднее локальное событие на P1, которое после a ни с кем не общалось, поэтому ни одно сообщение не связывает e и d ни в одну сторону.", correct: true },
    { label: "e happens-before d, потому что у e Lamport timestamp 2, а цепочка к d длиннее.", misconception: "Lamport timestamp дают тотальный порядок, но меньшее-или-большее значение Lamport НЕ влечёт причинности. Нет пути сообщений от e к d, поэтому они concurrent независимо от чисел." },
    { label: "d happens-before e, потому что d на P3, который начал цепочку сообщений.", misconception: "P3 принимает m2; он не начинает цепочку (её начинает a на P1), и ни одно сообщение не течёт от d обратно к e на P1. Без связующего пути сообщений события concurrent." },
    { label: "Это одно и то же событие, наблюдаемое на двух узлах.", misconception: "e и d — разные события на разных процессах. Happens-before спрашивает, связывает ли их путь сообщений; здесь его нет, значит они concurrent." },
  ]}
/>

<Recap lang="ru">
Операции с часами малы, но беспощадны: приём Lamport обязан брать max(local, msg) + 1, иначе может проштамповать receive раньше его send; приём vector clock берёт поэлементный max и бампит только свою компоненту; сравнение векторов покомпонентно, и когда каждая сторона впереди по какому-то узлу — события concurrent и должны держаться как siblings. И никакое числовое упорядочивание Lamport не влечёт причинности без связующего пути сообщений. Читай код и трассу, затем решай порядок vs конкурентность по причинной структуре, а не по сырым целым числам.
</Recap>
