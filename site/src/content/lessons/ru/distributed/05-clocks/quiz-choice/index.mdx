---
concepts:
- clock-skew
- lamport-timestamps
- vector-clocks
- happens-before
- last-write-wins
- truetime
estMin: 13
lang: ru
level: senior
order: 7
prereqs: []
slug: quiz-choice
sources:
- https://aphyr.com/posts/299-the-trouble-with-timestamps
- https://research.google.com/pubs/archive/39966.pdf
- https://en.wikipedia.org/wiki/Vector_clock
status: ready
summary: "Синтез по юниту в формате множественного выбора: clock skew, физические vs логические часы, Lamport vs vector clock, happens-before, детекция конкурентности и TrueTime."
title: 'Часы: тест с множественным выбором'
track: distributed
unit: 05-clocks
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Шесть вопросов через весь юнит. Каждый — это реальное решение при проектировании или отладке распределённого хранилища, а не определение для зубрёжки: как упорядочить события, когда настенные часы врут.
</Hook>

<Goal lang="ru">
Убедись, что связываешь clock skew, выбор между физическими и логическими часами, семантику Lamport vs vector clock, happens-before и TrueTime — синтез, к которому вёл урок.
</Goal>

<Quiz
  id="05-clocks-choice-1"
  lessonSlug="05-clocks"
  lang="ru"
  question="Кластер Cassandra использует last-write-wins по клиентским timestamp. Часы одного узла отстают на 1.5с. Поздний апдейт пользователя, прошедший через этот узел, исчезает после рефреша, при этом вернулся 200 OK. В чём корневая причина?"
  choices={[
    { label: "LWW оставляет строку с наибольшим timestamp. Узел со сбитыми часами проштамповал реально более новую запись меньшим (прошлым) timestamp, и разрешение конфликта отбросило её как устаревшую — это не ошибочное состояние, отсюда и успешный ответ.", correct: true },
    { label: "Запись не собрала кворум и была молча откачена.", misconception: "Запись была принята и подтверждена; ничего не откатывалось. Она потеряна позже при разрешении конфликта по timestamp — тихий drop, а не сбой записи." },
    { label: "NTP полностью предотвратил бы это, если бы был запущен.", misconception: "NTP уменьшает skew в среднем, но LWW падает на худшем случае. Step может сдвинуть часы назад на сотни мс, а зависший ntpd или приостановленная VM оставляют секунды skew — ровно то окно, где данные теряются." },
    { label: "Cassandra прочитала устаревшие данные с отстающей реплики; read-repair это исправит.", misconception: "Более новая запись была навсегда отброшена правилом LWW, а не просто отстаёт. Read-repair разносит выжившее (неверное) значение; он не воскрешает отброшенное." },
  ]}
/>

<Quiz
  id="05-clocks-choice-2"
  lessonSlug="05-clocks"
  lang="ru"
  question="Нужно разрешение конфликтов в реплицированном KV-хранилище, которое никогда не должно молча терять конкурентный апдейт. Lamport clock или vector clock?"
  choices={[
    { label: "Vector clock — они умеют детектировать конкурентность. Сравнение двух векторов говорит, произошло ли одно happens-before другого или ни один не доминирует (concurrent), и тогда хранилище держит обе версии как siblings для слияния.", correct: true },
    { label: "Lamport clock — они дают тотальный порядок, а это ровно то, что нужно разрешению конфликтов.", misconception: "Тотальный порядок здесь — проблема, а не решение: Lamport навязывает произвольный порядок конкурентным записям и не отличает их от причинной цепочки, поэтому хранилище детерминированно выберет одну и отбросит другую." },
    { label: "Подойдёт любой; разница только в стоимости по памяти.", misconception: "Семантика различается фундаментально. Lamport вообще не детектирует конкурентность; vector clock — детектирует. Выбрать Lamport здесь — значит молча терять конкурентные записи, независимо от памяти." },
    { label: "Ни тот, ни другой — используй настенные timestamp с более точным NTP.", misconception: "Настенный LWW — это ровно та ошибка, которую избегаем; более точный NTP лишь сужает окно потерь. Требование — ДЕТЕКТИРОВАТЬ конкурентность, для чего нужны логические часы." },
  ]}
/>

<Quiz
  id="05-clocks-choice-3"
  lessonSlug="05-clocks"
  lang="ru"
  question="Lamport clock гарантирует: если A happens-before B, то L(A) &lt; L(B). Почему нельзя из L(A) &lt; L(B) заключить, что A вызвало B?"
  choices={[
    { label: "Импликация работает только в одну сторону. Два события на узлах, которые никогда не общались, конкурентны, но Lamport всё равно назначит им какой-то порядок — поэтому меньший timestamp может быть просто произвольной стороной конкурентной пары, а не причинным предком.", correct: true },
    { label: "Потому что Lamport timestamp могут совпасть, делая сравнение неопределённым.", misconception: "Ничьи разрешаются детерминированно (например, по node id) для тотального порядка. Реальный пробел в том, что порядок тотален даже над событиями без причинной связи — конкурентность невидима." },
    { label: "Потому что счётчик переполняется на долгоживущих системах.", misconception: "Переполнение — это деталь реализации, а не семантический пробел. Фундаментальная проблема: Lamport кодирует тотальный порядок, который не различает причинность и конкурентность." },
    { label: "Можно — меньший Lamport timestamp всегда означает причинного предка.", misconception: "Это и есть то самое заблуждение. Lamport даёт согласованность с причинностью (A до B даёт меньший timestamp), но не обратное; конкурентным событиям порядок всё равно назначается." },
  ]}
/>

<Quiz
  id="05-clocks-choice-4"
  lessonSlug="05-clocks"
  lang="ru"
  question="Твои vector clock корректны, но кластер из 200 узлов теперь возит 200 счётчиков метаданных с каждой записью, и это растёт с добавлением узлов. Какой реальный компромисс ты принял?"
  choices={[
    { label: "O(n) метаданных на штампованное значение, растущих с кластером, в обмен на возможность детектировать конкурентность. Реальные системы прунят или кэпят векторы (например, скоуп по актору, dotted version vectors), чтобы ограничить разрастание.", correct: true },
    { label: "Vector clock неверны на таком масштабе; вернись к Lamport.", misconception: "Lamport — это O(1), но он не детектирует конкурентность — возврат к нему вновь приводит к тихим drop конфликтов. Цена — размер метаданных, а не корректность; ответ — прунить, а не отказываться от детекции." },
    { label: "Метаданные замедляют упорядочивание; пользы для корректности нет.", misconception: "Метаданные — это ровно то, что покупает детекцию конкурентности, нужную тебе выгоду по корректности. Компромисс — это память, а не напрасная трата." },
    { label: "Ничего — vector clock это O(1), как Lamport.", misconception: "Vector clock по построению O(n) от числа узлов; именно этот рост — причина, по которой продакшен-системы прунят или скоупят векторы." },
  ]}
/>

<Quiz
  id="05-clocks-choice-5"
  lessonSlug="05-clocks"
  lang="ru"
  question="Что отдаёт TrueTime в Spanner и что делает с этим commit-wait?"
  choices={[
    { label: "TT.now() возвращает интервал [earliest, latest], ширина которого — это неопределённость ε (около 1мс). Commit-wait спит, пока TT.now().earliest не превысит commit timestamp — пока timestamp точно не окажется в прошлом везде — покупая external consistency.", correct: true },
    { label: "TrueTime возвращает один точный момент по GPS, а commit-wait ждёт, пока все реплики сбросят данные на диск.", misconception: "TrueTime никогда не заявляет точный момент; он возвращает ограниченный интервал. Сброс на диск — это durability, отдельная забота; commit-wait именно пережидает интервал неопределённости ε." },
    { label: "Commit-wait блокирует новые транзакции, пока не освободятся локи предыдущей.", misconception: "Это конкуренция за локи, а не commit-wait. Commit-wait про неопределённость часов и перекрывается с репликацией Paxos, поэтому часто добавляет мало реальной задержки." },
    { label: "Commit-wait перечитывает часы, пока два подряд чтения не совпадут точно.", misconception: "Нет точного показания, к которому можно сойтись — TrueTime это интервал. Commit-wait пережидает интервал, а не гоняется за совпадением чтений." },
  ]}
/>

<Quiz
  id="05-clocks-choice-6"
  lessonSlug="05-clocks"
  lang="ru"
  question="Удаление в LWW-хранилище иногда подавляет каждую реальную запись по ключу на дни, даже записи, пришедшие задолго после удаления. В чём механизм?"
  choices={[
    { label: "Tombstone, проштампованный timestamp далеко в будущем (баговый клиент или узел, чьи часы прыгнули вперёд), под LWW побеждает каждую более позднюю реальную запись, пока compaction не соберёт tombstone — а это могут быть дни.", correct: true },
    { label: "Удаление взяло эксклюзивный лок на ключ, который не освободился.", misconception: "LWW-хранилища разрешают по timestamp, а не по локам. Подавление идёт от tombstone с будущей датой, выигрывающего каждое сравнение, а не от удерживаемого лока." },
    { label: "Лаг репликации скрывает новые записи, пока кластер не догонит.", misconception: "Эти записи не отстают — они сохранены и затем проигрывают сравнение LWW tombstone с будущим timestamp. Догон не помогает; tombstone всё равно побеждает." },
    { label: "Ключ не был проиндексирован, поэтому чтения промахиваются мимо новых записей.", misconception: "Индексация ни при чём. Записи находятся и сравниваются; они проигрывают большему timestamp tombstone, пока его не уберёт compaction." },
  ]}
/>

<Recap lang="ru">
Сквозная линия: настенное время не примитив упорядочивания, поэтому LWW превращает skew в тихую потерю данных (а tombstone с будущей датой — в многодневное подавление ключа). Логические часы упорядочивают по причинности: Lamport даёт дешёвый O(1) тотальный порядок, но не детектирует конкурентность, поэтому vector clock добавляют счётчик на узел, чтобы флагать конкурентные записи ценой O(n) метаданных, которые надо прунить. TrueTime идёт третьим путём: квантует неопределённость часов как ограниченный интервал ε и пережидает его commit-wait ради настоящей external consistency. Теперь, когда встретишь дизайн разрешения конфликтов на ревью, задай один вопрос: этот механизм должен детектировать конкурентность или просто навязать порядок? Ответ укажет, какой из трёх путей должен оказаться в коде.
</Recap>
