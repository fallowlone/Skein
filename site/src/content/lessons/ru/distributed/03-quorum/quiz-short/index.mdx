---
concepts:
- rw-overlap-invariant
- tunable-consistency
- sloppy-quorum
- hinted-handoff
- read-repair
- read-write-quorum
estMin: 14
lang: ru
level: senior
order: 8
prereqs: []
slug: quiz-short
sources:
- https://cassandra.apache.org/doc/latest/cassandra/architecture/dynamo.html
- https://www.allthingsdistributed.com/2007/10/amazons_dynamo.html
- https://docs.aws.amazon.com/amazondynamodb/latest/developerguide/HowItWorks.ReadConsistency.html
status: ready
summary: Вопросы на свободное воспроизведение через весь раздел про quorum. Сначала ответьте своими словами, затем откройте модельный ответ и сравните.
title: 'Кворумы: тест на свободное воспроизведение'
track: distributed
unit: 03-quorum
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";

<Hook>
Воспроизведение по памяти сильнее перечитывания. На каждый вопрос проговорите или запишите полный ответ по памяти — включая арифметику R, W, N — прежде чем открыть модельный ответ. Именно усилие реконструкции закрепляет инвариант пересечения.
</Hook>

<Goal lang="ru">
Восстановите хребет раздела — почему R + W > N принуждает к пересечению, почему QUORUM лучше ALL, что покупает tunable consistency и что именно теряет sloppy quorum — не подглядывая в урок.
</Goal>

<RetrievalDrawer
  client:load
  id="03-quorum-short-retrieval"
  lang="ru"
  questions={[
    { q: "Сформулируйте инвариант R + W > N и докажите одним предложением, почему он гарантирует, что чтение видит последнюю запись.", a: "Если запись подтверждена W из N реплик, а чтение собирает ответы от R из N, то при R + W > N эти подмножества не могут быть непересекающимися — по принципу Дирихле они обязаны делить хотя бы один узел. Этот общий узел получил запись и несёт её (новейшую) версию, поэтому чтение, берущее наибольшую версию среди своих R ответов, гарантированно видит последнюю зафиксированную запись. Неравенство должно быть СТРОГИМ: при R + W = N множества могут быть идеально непересекающимися (например, узлы {1,2} пишут, а {3,4} читают при N=4), и чтение законно пропустит запись. Здесь нет лидера, лога или консенсуса — просто два подмножества N, принуждённые пересечься." },
    { q: "Почему QUORUM (W=2, R=2 при N=3) — производственный дефолт, а не запись в ALL или чтение из ALL?", a: "Все три варианта — W=2/R=2, W=3/R=1, W=1/R=3 — удовлетворяют R + W > 3, так что все дают пересечение. Разница в переживании отказов. W=3 (запись в ALL) проваливает каждую запись в момент отказа одной реплики — обычный деплой, GC-пауза или медленный диск выводят записи из строя. Симметрично R=3 (чтение из ALL) блокирует каждое чтение на одном медленном узле. QUORUM нужно лишь 2 из 3 на КАЖДОМ пути, поэтому он переживает отказ одного узла и на чтениях, и на записях, по-прежнему гарантируя пересечение. Это переживание отказа одного узла с обеих сторон и сделало quorum дефолтной ментальной моделью вместо ALL. Обобщается как floor(N/2)+1: 2 при RF=3, 3 при RF=5." },
    { q: "Что значит tunable consistency на практике, и что вы балансируете, выбирая R и W?", a: "Tunable consistency значит, что R и W — это ручки, которые вы задаёте пооперационно, часто на запрос, а не единый глобальный режим. Тот же кластер N=3 может обслуживать W=1 fire-and-forget запись метрик и W=2/R=2 strongly-consistent чтение баланса в одном процессе. Каждый выбор сдвигает вас по треугольнику consistency/availability/latency: более высокие R и W (вплоть до ALL) покупают более сильную согласованность и переживают больше расхождений, но снижают доступность (больше узлов должны быть живы) и поднимают латентность (вы ждёте больше реплик); меньшие значения покупают скорость и доступность ценой возможно устаревших или потерянных данных. Senior-ход — задавать R и W под нагрузку — строго там, где важна корректность, и расслабленно там, где потеря допустима — и точно знать, какую гарантию каждый выбор сохраняет или теряет." },
    { q: "Опишите sloppy quorum плюс hinted handoff и точно укажите, какую гарантию он приостанавливает и когда.", a: "Строгий quorum требует W подтверждений от НАЗНАЧЕННЫХ реплик (preference list ключа); если их доступно слишком мало, запись проваливается — простой во время partition или отказа нескольких узлов. Sloppy quorum вместо этого маршрутизирует запись на следующие здоровые узлы кольца, которые хранят её как HINT с пометкой 'это принадлежит узлу X'. Когда X восстанавливается, держатель воспроизводит ему hint (hinted handoff) и удаляет свою копию. Это держит записи проходящими сквозь отказы — вся причина его существования. Что он приостанавливает: эти держатели hint НЕ в обычном множестве чтения, поэтому в окне отказа строгое чтение домашних реплик может полностью пропустить hinted-запись. Пересечение R + W > N гарантировано лишь против канонического множества реплик, а запись временно вне него — так что sloppy quorum не удовлетворяет пересечению ровно тогда, когда назначенный узел упал, то есть когда оно нужнее всего." },
    { q: "Назовите три тихих режима сбоя кворума из раздела и скажите, почему каждый нигде не даёт ошибки.", a: "(1) Потерянная запись при W=1: единственная подтвердившая реплика умирает до репликации, поэтому 'успешная' запись остаётся на нуле живых узлов. Ошибки нет, потому что W=1 явно обещал лишь один ack. (2) Устаревшее чтение при sloppy quorum: во время partition запись попадает на держателей hint вне множества чтения, а строгое чтение домашних реплик отдаёт старое значение. Обе стороны выглядят здоровыми, потому что ни одна реплика не неисправна — данные просто в другом месте. (3) Дрейф R + W <= N: суб-пересекающийся микс вроде R=1,W=2 (сумма 3 при N=3) даёт чтению иногда выбрать одну отставшую реплику. Ошибки нет, потому что по сконфигурированным правилам всё корректно — пересечение было настроено прочь. Все три опасны именно потому, что корректны-по-конфигурации, всплывая как невоспроизводимые тикеты про 'призрачные устаревшие данные', а не как исключения." },
    { q: "Как read repair и anti-entropy repair сходят реплики, и как они соотносятся с гарантией пересечения?", a: "Read repair работает inline: когда QUORUM-чтение замечает, что его R реплик расходятся, координатор проталкивает новейшую версию устаревшим прямо на пути чтения — дёшево, но чинит лишь те ключи, что реально читают. Anti-entropy repair (nodetool repair) работает офлайн: реплики строят деревья Меркла (хеш-деревья), диффят их, находя несовпадающие диапазоны токенов, и стримят только различающиеся диапазоны, чтобы согласовать данные, которые давно никто не читал. Их связь с пересечением: они — резерв eventual-consistency, который гасит долг согласованности, накопленный sloppy quorum и низким R+W, сводя разошедшиеся реплики постфактум. Они не дают сильной гарантии — её даёт только R + W > N в момент чтения. И anti-entropy должен отрабатывать в пределах gc_grace_seconds (по умолчанию 10 дней), иначе удалённые данные могут воскреснуть при истечении tombstone без согласования — отдельный неприятный баг." },
  ]}
/>

<Recap lang="ru">
Если вы смогли восстановить каждый ответ по памяти, вы держите хребет раздела: R + W > N принуждает к пересечению по принципу Дирихле и должно быть строгим; QUORUM лучше ALL, потому что переживает отказ одного узла на обоих путях, сохраняя пересечение; tunable consistency значит задавать R и W пооперационно по треугольнику consistency/availability/latency; sloppy quorum плюс hinted handoff покупает доступность записи во время partition, приостанавливая пересечение, пока назначенный узел упал; три тихих режима сбоя (потеря записи при W=1, устаревшее чтение при sloppy, суб-пересекающийся дрейф) нигде не дают ошибки, потому что корректны-по-конфигурации; а read repair плюс anti-entropy на деревьях Меркла сводят разрывы потом, но никогда не заменяют гарантию в момент чтения. Теперь, когда столкнётесь с призрачным устаревшим данным без исключений в логах, вы первым делом откроете конфиг уровней согласованности — и досчитаете до N, прежде чем искать дальше.
</Recap>
