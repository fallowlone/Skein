---
concepts:
- rw-overlap-invariant
- consistency-level-config
- sloppy-quorum
- tunable-consistency
- read-write-quorum
estMin: 14
lang: ru
level: senior
order: 9
prereqs: []
slug: quiz-code
sources:
- https://cassandra.apache.org/doc/latest/cassandra/architecture/dynamo.html
- https://docs.datastax.com/en/cassandra-oss/3.0/cassandra/dml/dmlAboutDataConsistency.html
- https://docs.aws.amazon.com/amazondynamodb/latest/developerguide/HowItWorks.ReadConsistency.html
status: ready
summary: Читайте реальные конфигурации уровней согласованности и проверку пересечения кворума, предсказывайте, гарантирует ли каждая последнюю запись, и выбирайте самый рычажный фикс.
title: 'Кворумы: чтение конфигов и кода'
track: distributed
unit: 03-quorum
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Баги кворума живут в строках уровня согласованности вашего кода доступа к данным, а не в самой базе. Прочитайте каждый конфиг и сниппет, сделайте арифметику R + W против N и выберите фикс, который сделал бы senior-инженер первым.
</Hook>

<Goal lang="ru">
Отработайте цикл, который вы запускаете, когда прилетает тикет про устаревшее чтение: найдите R и W в коде, проверьте, действительно ли R + W > N для replication factor этого keyspace, и потянитесь к фиксу, который восстанавливает пересечение, а не прячет его.
</Goal>

## Сниппет 1 — раздельная согласованность чтения и записи

```python
# keyspace создан WITH replication = { 'class': 'SimpleStrategy', 'replication_factor': 3 }
session.execute(
    SimpleStatement(
        "UPDATE accounts SET payout = %s WHERE id = %s",
        consistency_level=ConsistencyLevel.QUORUM,   # W = 2 при RF=3
    ),
    (new_payout, account_id),
)
balance = session.execute(
    SimpleStatement(
        "SELECT payout FROM accounts WHERE id = %s",
        consistency_level=ConsistencyLevel.ONE,        # R = 1
    ),
    (account_id,),
).one()
```

<Quiz
  id="03-quorum-code-1"
  lessonSlug="03-quorum"
  lang="ru"
  question="Запись — QUORUM (W=2), а чтение — ONE (R=1) при RF=3. Гарантирует ли это, что чтение увидит последний payout, и в чём фикс?"
  choices={[
    { label: "Нет. R + W = 1 + 2 = 3, что не строго больше N=3, поэтому единственная реплика чтения может оказаться тем узлом, который пропустил QUORUM-запись. Поднимите чтение до QUORUM (R=2), чтобы 2 + 2 = 4 > 3 и пересечение было гарантировано.", correct: true },
    { label: "Да — QUORUM-запись на большинстве, поэтому любое чтение её найдёт.", misconception: "QUORUM-запись на 2 из 3, но R=1 может прочитать оставшуюся 1, которая отстала. Нужно R + W > N (принудительное пересечение), а не просто мажоритарная запись. 1 + 2 = 3 не удовлетворяет > 3." },
    { label: "Нет — чините записью в ALL (W=3), чтобы даже R=1 её видел.", misconception: "W=3 при R=1 даёт сумму 4 > 3 и сработает, но запись в ALL проваливает каждую запись при одном упавшем узле — ужасная доступность для платёжной записи. Поднятие ЧТЕНИЯ до QUORUM сохраняет переживание отказа одного узла на обоих путях." },
    { label: "Да, потому что чтение идёт на координатор, у которого есть запись.", misconception: "Координатор маршрутизирует чтение на реплики; он не обязательно реплика этого ключа и не обязан держать последнюю версию. Пересечение должно обеспечиваться через R + W > N." },
  ]}
/>

## Сниппет 2 — хелпер проверки пересечения

```python
def guarantees_latest_read(n: int, r: int, w: int) -> bool:
    # True тогда и только тогда, когда чтение гарантированно видит последнюю запись
    return r + w > n

# вызвано для плана нового keyspace, RF = 4
print(guarantees_latest_read(n=4, r=2, w=2))   # что напечатает и безопасно ли это?
```

<Quiz
  id="03-quorum-code-2"
  lessonSlug="03-quorum"
  lang="ru"
  question="При N=4 с R=2, W=2 что напечатает guarantees_latest_read и верный ли это ответ?"
  choices={[
    { label: "Напечатает False, и это правильно: 2 + 2 = 4 не больше 4, поэтому множества чтения и записи могут быть непересекающимися (по два узла, без общего). При N=4 нужно R + W >= 5, например R=3 W=2 или R=2 W=3.", correct: true },
    { label: "Напечатает True, ведь R и W оба — большинство от 4.", misconception: "2 не большинство от 4 — строгое большинство 4 это 3. И хелпер использует строгое >, так что 2 + 2 = 4 даёт False. Кластеры с чётным N — классическая ловушка: 'половинное' чтение и 'половинная' запись могут не пересечься." },
    { label: "Напечатает False, но проверка слишком строгая; правильно было бы >=.", misconception: "Строгое > здесь точно верно. При R + W = N множества могут быть идеально непересекающимися (например, узлы {1,2} пишут, {3,4} читают при N=4), так что >= ложно сообщил бы о безопасности. Пересечению по принципу Дирихле нужно R + W > N." },
    { label: "Напечатает True и это безопасно, потому что RF=4 переживает больше отказов.", misconception: "Больший RF сам по себе не даёт пересечения; его даёт выбор R/W. R=2,W=2 при N=4 даёт сумму 4, проваливает тест > N и допускает устаревшие чтения независимо от числа реплик." },
  ]}
/>

## Сниппет 3 — путь записи sloppy quorum

```python
def write_quorum(coordinator, key, value, n=3, w=2):
    home = coordinator.preference_list(key, n)        # N назначенных реплик
    live = [node for node in home if node.is_reachable()]
    acks = [node.store(key, value) for node in live]
    if len(acks) >= w:
        return "OK (strict quorum)"
    # недостаточно назначенных реплик доступно -> sloppy quorum
    for sub in coordinator.next_healthy_ring_nodes():
        acks.append(sub.store_hint(key, value, intended_owner=...))
        if len(acks) >= w:
            return "OK (sloppy quorum)"               # подтверждено, но где?
    return "FAIL"
```

<Quiz
  id="03-quorum-code-3"
  lessonSlug="03-quorum"
  lang="ru"
  question="Когда это возвращает 'OK (sloppy quorum)' во время partition, каково следствие для согласованности конкурентного строгого QUORUM-чтения с домашних реплик?"
  choices={[
    { label: "Чтение может вернуть устаревшие данные. W=2 был удовлетворён частично узлами-заместителями с hint вне preference list, поэтому строгое чтение домашних реплик может не обратиться ни к одному узлу, реально держащему новое значение — R + W > N приостановлено против канонического множества реплик во время отказа.", correct: true },
    { label: "Никакого — store_hint пишет на те же узлы, поэтому чтение его видит.", misconception: "store_hint пишет на УЗЛЫ-ЗАМЕСТИТЕЛИ (next_healthy_ring_nodes), а не на назначенные реплики. Чтение домашнего preference list может полностью пропустить hint, пока hinted handoff не завершится." },
    { label: "Чтение быстро падает, потому что hint помечает ключ как in-flight.", misconception: "Hint'ы не блокируют и не проваливают чтения домашних реплик; те просто отдают свою (более старую) версию. Чтение успешно и возвращает устаревшие данные — тихий режим сбоя." },
    { label: "Это безопасно, потому что hinted handoff завершается раньше любого чтения.", misconception: "Нет гарантии порядка между handoff и гонящимся чтением. В окне partition чтение может прийти раньше handoff — это ровно тот шов устаревшего чтения, который открывает sloppy quorum." },
  ]}
/>

## Сниппет 4 — флаг чтения DynamoDB

```python
# DynamoDB, репликация управляется; согласованность чтения вы выбираете на запрос
resp = table.get_item(
    Key={"id": account_id},
    ConsistentRead=False,   # по умолчанию: eventually consistent
)
```

<Quiz
  id="03-quorum-code-4"
  lessonSlug="03-quorum"
  lang="ru"
  question="Чтение баланса использует ConsistentRead=False. Что даёт переключение на True и чего это стоит — в терминах кворума?"
  choices={[
    { label: "True выполняет strongly-consistent read, отражающее все записи, подтверждённые до него — управляемый эквивалент чтения с достаточным пересечением, чтобы увидеть последнюю запись. Это стоит примерно 2x read-capacity units и более высокую, менее предсказуемую латентность, потому что приходится опрашивать больше реплик, а не любую одну.", correct: true },
    { label: "Ничего существенного — ConsistentRead влияет только на биллинг, не на данные.", misconception: "Он меняет наблюдаемые данные: False может вернуть значение с реплики, ещё не получившей недавнюю запись (устаревшее), а True гарантированно отражает все предыдущие подтверждённые записи. И стоит ~2x RCU." },
    { label: "True делает записи durable; записи при False могут теряться.", misconception: "ConsistentRead — флаг ЧТЕНИЯ и вовсе не меняет durability записи. Он лишь управляет тем, гарантированно ли чтение видит последнюю зафиксированную запись." },
    { label: "True бесплатно и строго лучше, так что используйте всегда.", misconception: "Оно стоит примерно вдвое больше capacity на чтение и добавляет латентность, поэтому его держат для чтений, которые не должны быть устаревшими (балансы, проверки идемпотентности), а loss-tolerant чтения оставляют eventually-consistent — это компромисс tunable consistency." },
  ]}
/>

<Recap lang="ru">
Любой баг кворума читается одинаково: найдите R и W в строках уровня согласованности, найдите N из replication factor keyspace и проверьте R + W > N со СТРОГИМ неравенством — R=1-чтения за QUORUM-записями и любой чётный сплит вроде R=2,W=2 при N=4 — классические ловушки, дающие в сумме N и тихо допускающие устаревшие чтения. Фикс, восстанавливающий пересечение (обычно поднятие уровня ЧТЕНИЯ), лучше фиксов, которые его прячут. Sloppy quorum подтверждает через узлы-заместители с hint вне множества чтения, поэтому его 'OK' приостанавливает пересечение во время partition; а управляемые хранилища выставляют ту же ручку как ConsistentRead, где strong-чтения стоят примерно 2x capacity за гарантию.
</Recap>
