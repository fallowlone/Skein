---
concepts:
- read-write-quorum
- rw-overlap-invariant
- tunable-consistency
- sloppy-quorum
- hinted-handoff
- read-repair
estMin: 13
lang: ru
level: senior
order: 7
prereqs: []
slug: quiz-choice
sources:
- https://cassandra.apache.org/doc/latest/cassandra/architecture/dynamo.html
- https://docs.aws.amazon.com/amazondynamodb/latest/developerguide/HowItWorks.ReadConsistency.html
- https://www.allthingsdistributed.com/2007/10/amazons_dynamo.html
status: ready
summary: "Синтез всего раздела про quorum в формате с выбором ответа: арифметика R + W > N, tunable consistency, sloppy quorum и hinted handoff, и тихие производственные сбои."
title: 'Кворумы: тест с выбором ответа'
track: distributed
unit: 03-quorum
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Шесть вопросов через весь раздел. Каждый — это решение, которое вы принимаете, когда подбираете размер кластера или разбираете тикет про устаревшее чтение: не определение для пересказа, а арифметика R, W и N в условиях реальных отказов.
</Hook>

<Goal lang="ru">
Убедитесь, что выводите гарантию пересечения из R + W > N, размещаете конфигурацию на треугольнике consistency/availability/latency и точно предсказываете, какую именно гарантию sloppy quorum теряет во время partition.
</Goal>

<Quiz
  id="03-quorum-choice-1"
  lessonSlug="03-quorum"
  lang="ru"
  question="Кластер с RF=5. Нужно, чтобы каждое чтение гарантированно видело последнюю успешную запись, при этом переживая как можно больше отказов узлов. Какая минимальная пара (R, W) даёт строгую согласованность, и сколько одновременных отказов узлов она переживает на каждом пути?"
  choices={[
    { label: "R=3, W=3 — сумма 6 > 5, поэтому множества обязаны пересечься; каждому пути нужно 3 из 5, так что и чтения, и записи переживают 2 упавших узла. Это QUORUM при N=5.", correct: true },
    { label: "R=2, W=3 — сумма 5, мажоритарной записи плюс небольшого чтения достаточно.", misconception: "5 не строго больше 5. R + W должно быть > N, а не >= N. При R=2, W=3 чтение может выбрать 2 реплики, которые обе пропустили запись, и вернуть устаревшие данные." },
    { label: "R=1, W=5 — запись во ВСЕ гарантирует, что любое единичное чтение актуально.", misconception: "Сумма 6 > 5 действительно даёт пересечение, но W=5 означает, что один упавший узел проваливает каждую запись — это 0 переживаемых отказов записи, противоположность 'как можно больше'. QUORUM R=3,W=3 переживает 2 на каждом пути." },
    { label: "R=5, W=1 — пишем быстро, читаем из ВСЕХ, чтобы поймать последнюю запись.", misconception: "Сумма 6 > 5 даёт пересечение, но R=5 означает, что один медленный или упавший узел блокирует каждое чтение (0 переживаемых отказов чтения), а W=1 рискует потерять ack, если единственный узел умрёт. QUORUM балансирует оба пути." },
  ]}
/>

<Quiz
  id="03-quorum-choice-2"
  lessonSlug="03-quorum"
  lang="ru"
  question="Платёжный сервис на RF=3 пишет с W=1 'ради латентности'. Ночью узел подтверждает обновление реквизитов выплаты, затем падает при деплое до репликации. Следующее чтение отдаёт старый номер счёта. Что на самом деле пошло не так?"
  choices={[
    { label: "Две накладывающиеся проблемы: W=1 подтвердил запись на единственной реплике, которая умерла до репликации, поэтому запись осталась на нуле живых узлов — потерянная запись; а так как R + W не больше N, чтение было вправе обратиться к репликам, у которых её никогда не было. Ни то, ни другое не дало ошибки, потому что конфигурация сама узаконила потерю.", correct: true },
    { label: "Баг в коде репликации координатора потерял запись.", misconception: "Репликация отработала ровно так, как сконфигурировано. W=1 значит 'ack после одной реплики' — рантайм никогда не обещал, что остальные две получат данные до ack. Потеря — выбор конфигурации, а не дефект кода." },
    { label: "Чтение должно было использовать R=3, чтобы форсировать согласованность.", misconception: "R=3 при W=1 даёт сумму 4 > 3 и поймал бы выжившую копию — но здесь единственная копия умерла, так что даже R=3 ничего не найдёт. Корень — это W=1 и durability, а не настройка чтения." },
    { label: "Проблема в деплое; без деплоя запись была бы в безопасности.", misconception: "Деплой — лишь один из триггеров. Любой крэш, GC-пауза или отказ диска в окне между ack при W=1 и репликацией теряет запись. W=1 превращает отказ одной реплики в событие потери данных." },
  ]}
/>

<Quiz
  id="03-quorum-choice-3"
  lessonSlug="03-quorum"
  lang="ru"
  question="Тот же кластер RF=3, настроен на строгий W=2, R=2. Во время сетевого partition запись через sloppy quorum попадает на узлы-заместители, держащие hint, а строгое чтение с домашних реплик возвращает устаревшие данные. Почему W=2, R=2 здесь не спасает?"
  choices={[
    { label: "Sloppy quorum сохраняет W=2, но удовлетворяет его узлами-заместителями вне обычного множества чтения, поэтому 2 домашние реплики чтения обе могут пропустить hinted-запись. Пересечение R + W > N держится только против назначенных реплик, а во время отказа записи на них нет.", correct: true },
    { label: "Sloppy quorum тихо понижает W до 1 во время partition.", misconception: "Он сохраняет ваш счётчик W — два узла всё равно подтверждают. Меняется лишь то, КАКИЕ узлы могут его удовлетворить: здоровые заместители через hinted handoff, а не назначенные реплики. Счётчики те же, состав другой." },
    { label: "Read repair был выключен, поэтому устаревшая реплика так и не починилась.", misconception: "Read repair может протолкнуть только ту новую версию, которую видит. Во время partition последняя запись лежит на держателях hint, к которым чтение не обращается, поэтому при чтении нет более свежей копии для починки." },
    { label: "R=2 слишком мало; R=3 нашло бы запись.", misconception: "R=3 читает все три домашние реплики, но hinted-запись лежит на узлах-заместителях вне этого множества, поэтому R=3 всё равно её пропустит в разгар partition. Дыра — это приостановленное пересечение, а не значение R." },
  ]}
/>

<Quiz
  id="03-quorum-choice-4"
  lessonSlug="03-quorum"
  lang="ru"
  question="Команда утверждает: 'R=1, W=2 при N=3 — нормально, ведь запись уже на большинстве'. Что неверно в этом рассуждении?"
  choices={[
    { label: "R + W = 3, что не строго больше 3, поэтому единственная реплика чтения может оказаться тем самым узлом, который отстал от записи. Большинство на пути записи не помогает, если единственная реплика чтения вне этого большинства — пересечению нужно > N, а не просто мажоритарная запись.", correct: true },
    { label: "Ничего — мажоритарная запись плюс любое чтение всегда согласованны.", misconception: "Мажоритарная запись гарантирует, что значение на 2 из 3, но R=1 может прочитать оставшуюся 1, которая его пропустила. Согласованность даёт принудительное пересечение (R + W > N), а не сам факт записи на большинство." },
    { label: "Неверно, потому что W=2 само по себе слишком слабо при N=3.", misconception: "W=2 — это ровно QUORUM и оно нормально — в паре с R=2. Изъян в R=1, который роняет сумму до 3 и ломает пересечение. Чинить надо чтение, а не запись." },
    { label: "Всё нормально, пока чтения всегда попадают на узел-координатор.", misconception: "Нет гарантии, что координатор держит ключ или последнюю версию; координаторы маршрутизируют, они не обязательно реплики. Пересечение должно обеспечиваться арифметикой R + W > N, а не везением маршрутизации." },
  ]}
/>

<Quiz
  id="03-quorum-choice-5"
  lessonSlug="03-quorum"
  lang="ru"
  question="При RF=3 QUORUM-чтение (R=2) показывает p99 намного хуже p50, хотя латентности отдельных узлов выглядят здоровыми. Каков механизм и какая мера борется с ним напрямую?"
  choices={[
    { label: "QUORUM-чтение ждёт 2 из 3 реплик, поэтому его латентность — это вторая по скорости из трёх: одна медленная реплика (GC-пауза, горячая партиция, троттлинг диска) тянет p99 вверх, даже когда кластер в порядке. Request hedging — отправить дубликат после короткой задержки и взять первый ответ — срезает этот хвост.", correct: true },
    { label: "QUORUM-чтения упираются в CPU; добавьте ядер координатору.", misconception: "Латентность определяется ожиданием самой медленной нужной реплики, а не CPU координатора. Лишние ядра ничего не дают чтению, заблокированному на реплике в GC-паузе; помогает hedging или снижение R." },
    { label: "p99 вызван тем, что read repair дозаписывает на каждом чтении.", misconception: "Read repair срабатывает, только когда реплики расходятся, и обычно дёшев. Структурный драйвер p99 — это 'жди 2-ю по скорости из 3', свойство хвоста латентности самого R." },
    { label: "Снизьте R до 1, чтобы убрать ожидание — это не стоит согласованности.", misconception: "R=1 действительно убирает ожидание, но при N=3 с W=2 роняет R + W до 3 и ломает гарантию пересечения. Hedging срезает хвост БЕЗ жертвы согласованностью; снижение R меняет корректность на латентность." },
  ]}
/>

<Quiz
  id="03-quorum-choice-6"
  lessonSlug="03-quorum"
  lang="ru"
  question="Пересечение (R + W > N) гарантирует, что QUORUM-чтение видит последнюю зафиксированную запись. Чего из перечисленного оно НЕ гарантирует?"
  choices={[
    { label: "Упорядочивание конкурентных записей. Два клиента, пишущие один ключ при W=2, оба могут преуспеть против разных большинств в один логический момент; quorum хранит обе как siblings или разрешает по last-write-wins. Пересечение — про видимость завершённой записи, а не про сериализацию гонок: для этого нужны LWT/Paxos.", correct: true },
    { label: "Что завершённая запись видима последующему QUORUM-чтению.", misconception: "Это как раз то, что пересечение ДАЁТ: множества чтения и записи делят узел с последним значением. А вот упорядочивания конкурентных записей quorum не даёт." },
    { label: "Переживание отказа одного узла на обоих путях при W=2, R=2.", misconception: "QUORUM действительно даёт переживание отказа одного узла на обоих путях — поэтому он лучше ALL. Отсутствующая гарантия — сериализация конкурентных записей одного ключа." },
    { label: "Что R и W можно настраивать пооперационно.", misconception: "Tunable consistency и правда позволяет задавать R и W на запрос. Но пересечение НЕ даёт упорядочивания конкурентных записей — видимость это не сериализация." },
  ]}
/>

<Recap lang="ru">
Сквозная линия — одно неравенство: R + W > N принуждает множества чтения и записи пересечься, поэтому QUORUM-чтение видит последнюю зафиксированную запись — и в момент, когда вы настраиваете ниже (R=1,W=2 в сумме N или W=1), устаревшие чтения и потерянные записи становятся законными, а не багами. QUORUM (W=2,R=2 при N=3; W=3,R=3 при N=5) — производственный дефолт, потому что держит пересечение, переживая один (или два при N=5) упавший узел на обоих путях, в отличие от ALL. R — ещё и ручка p99: QUORUM-чтение ждёт самую медленную нужную реплику, отсюда hedging. А sloppy quorum покупает доступность записи во время partition, паркуя записи на держателях hint вне множества чтения, приостанавливая пересечение ровно тогда, когда узел упал; read repair и anti-entropy на деревьях Меркла сходят данные потом — обычно.
</Recap>
