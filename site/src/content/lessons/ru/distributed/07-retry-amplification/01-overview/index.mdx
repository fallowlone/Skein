---
concepts:
- retry-amplification
- metastable-failure
- exponential-backoff-jitter
- retry-budget
- circuit-breaker
- deadline-propagation
deepensInto: []
estMin: 17
lang: ru
lessonType: topic
level: junior
order: 1
prereqs: []
slug: 01-overview
sources:
- https://aws.amazon.com/blogs/architecture/exponential-backoff-and-jitter/
- https://sre.google/sre-book/addressing-cascading-failures/
- https://www.usenix.org/publications/loginonline/metastable-failures-wild
- https://sigops.org/s/conferences/hotos/2021/papers/hotos21-s11-bronson.pdf
spiral: []
status: ready
summary: 'Наивный ретрай на каждом слое глубокой цепочки вызовов множит нагрузку геометрически
  — один запрос становится 3^глубина вызовами к бэкенду — и сам трафик ретраев держит
  систему лежащей долго после того, как триггер ушёл. Фиксы: бэкофф с jitter, retry
  budget и circuit breaker.'
title: 'Усиление ретраев: как 3 повтора на слой превращаются в метастабильную аварию'
track: distributed
unit: 07-retry-amplification
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";
import Infographic from "~/components/diagram/Infographic.astro";
import amplificationCurve from "./infographics/amplification-curve.svg?raw";

<Hook>
Failover базы занимает 8 секунд. Мелочь — клиенты просто ретраят. Но ретраит каждый слой: API-шлюз повторяет вызов сервиса 3 раза, сервис повторяет слой данных 3 раза, слой данных повторяет пул соединений 3 раза. Failover завершается на 8-й секунде. Авария — нет. Бэкенд завален потоком ретраев, прилетевших разом, таймаутится и порождает *ещё* ретраи. Через сорок минут кто-то понимает: триггер ушёл полчаса назад — система лежит только из-за самих ретраев.
</Hook>

<Crux>Наивные ретраи множатся: один запрос становится 3^глубина вызовами, и сам трафик ретраев держит аварию после восстановления.</Crux>

<Explanation>

## Множение геометрическое, а не линейное

Ретрай кажется бесплатным. Интуиция говорит «отправь ещё раз, один лишний вызов». Эта интуиция верна только для листа. В слоёной системе у каждого хопа своя политика ретраев, и политики *композируются умножением*. Когда ты смотришь на цепочку вызовов, вопрос не «ретраит ли каждый слой?» — а «сколько слоёв ретраит и на сколько они умножают нагрузку?» Если цепочка вызовов глубиной 4 сервиса и каждый слой повторяет 3 раза при ошибке, один пользовательский запрос, упавший на дне, может породить до 3 × 3 × 3 × 3 = **3⁴ = 81** вызова к самой глубокой зависимости. Книга Google SRE предупреждает ровно об этом эффекте накопления — вложенные ретраи умножаются, а не складываются, — и канонический пример: 3 слоя по 4 повтора, что превращает одно действие пользователя в 4³ = 64 попытки к базе.

Число геометрическое по глубине, поэтому оно подкрадывается незаметно. Два слоя по 3 ретрая — это 9, неприятно, но переживаемо. Четыре слоя — 81. Добавь пятый — 243. Коэффициент усиления — `повторы^глубина`, а глубина в современном микросервисном меше редко равна двум. Самое скверное: это множение включается *во время* инцидента — ровно тогда, когда у бэкенда меньше всего запаса, чтобы его поглотить.

<Infographic
  svg={amplificationCurve}
  label="Столбчатая диаграмма числа вызовов к самой глубокой зависимости при 3 повторах на слой по мере роста глубины цепочки: глубина 1 — это 3, глубина 2 — 9, глубина 3 — 27, глубина 4 — 81, глубина 5 — 243 — столбцы растут геометрически, каждый втрое выше предыдущего."
  caption="Столбцы утраиваются с каждым слоем глубины — 3, 9, 27, 81, 243. Именно эта геометрическая кривая, а не число повторов на слой, — причина, по которой усиление подкрадывается незаметно: один лишний хоп умножает, а не складывает."
  hue="var(--d-infra)"
/>

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Глубина цепочки</th>
        <th class="border border-border px-3 py-2 text-left">Повторов на слой</th>
        <th class="border border-border px-3 py-2 text-left">Вызовов к глубокой зависимости</th>
        <th class="border border-border px-3 py-2 text-left">Эффект</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">1 (только лист)</td>
        <td class="border border-border px-3 py-2">3</td>
        <td class="border border-border px-3 py-2">3</td>
        <td class="border border-border px-3 py-2">То, что предполагает интуиция</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">2</td>
        <td class="border border-border px-3 py-2">3</td>
        <td class="border border-border px-3 py-2">3² = 9</td>
        <td class="border border-border px-3 py-2">В основном переживаемо</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">4</td>
        <td class="border border-border px-3 py-2">3</td>
        <td class="border border-border px-3 py-2">3⁴ = 81</td>
        <td class="border border-border px-3 py-2">1% ошибок → ~81% лишней нагрузки</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">5</td>
        <td class="border border-border px-3 py-2">3</td>
        <td class="border border-border px-3 py-2">3⁵ = 243</td>
        <td class="border border-border px-3 py-2">Самонаведённый DDoS</td>
      </tr>
    </tbody>
  </table>
</div>

## Почему оно не останавливается: метастабильная авария

Глубже ловушка в том, что происходит *после* того, как исходная проблема залечилась. 8-секундный failover закончился. Ёмкость восстановлена. А система лежит. Это **метастабильная авария** — термин, который Bronson и соавторы ввели в статье HotOS 2021, чтобы объединить retry-штормы, congestion collapse и death spirals под одной рамкой. У системы два устойчивых состояния: здоровое и деградировавшее. Временный триггер (спайк, failover, короткий сбой зависимости) толкает её в деградировавшее состояние, а *поддерживающая петля обратной связи* держит её там, хотя триггер давно ушёл.

Ретраи — каноничная поддерживающая петля. Пройди цикл: бэкенд замедляется → запросы таймаутятся → таймауты порождают ретраи → ретраи добавляют нагрузку → бэкенд замедляется сильнее → больше таймаутов → больше ретраев. Теперь ретраи — доминирующий трафик. Работа, которую делает система, — почти целиком повторы запросов, чьи исходные дедлайны уже прошли: бесполезная работа, не дающая ничего, кроме новой нагрузки. Триггер уже неважен; петля сама себя кормит. Поэтому «подождём, восстановится» не работает, и операторам приходится сбрасывать нагрузку или перезапускать слои, чтобы разорвать цикл силой.

<Inset kind="why" lang="ru">
Метастабильная авария так дезориентирует в постмортеме потому, что корневая причина в таймлайне (failover) и корневая причина *затянувшейся* аварии (петля ретраев) — это разные вещи. Люди жгут инцидент в погоне за триггером, который уже ушёл. Честная строка постмортема: триггер запустил это, но усиление ретраев — то, что держало нас лежащими; и единственный сработавший фикс — снижение нагрузки, а не починка триггера.
</Inset>

## Фикс 1: экспоненциальный бэкофф с jitter

Прежде чем тянуться к увеличению числа ретраев, спроси себя: все ли клиенты ретраят в один момент? Именно эту синхронизацию нужно починить первой.

Первая причина смертоносности ретраев — *синхронизация*. Когда зависимость моргает, все клиенты таймаутятся примерно в один момент и ретраят примерно в один момент — thundering herd (лавина одновременных запросов от толпы клиентов), прилетающий спайком. Ретраи с фиксированным интервалом делают хуже: они ресинхронизируют стадо на каждом круге. Экспоненциальный бэкофф (жди `base`, потом `2×base`, потом `4×base`) разносит попытки во времени, но не декоррелирует их — клиенты, стартовавшие вместе, всё равно шагают в ногу.

Фикс — **jitter**: добавь случайность, чтобы клиенты размазались по окну. Разбор AWS в Architecture Blog — каноничная ссылка. *Full jitter* берёт `sleep = random(0, base × 2^attempt)` — максимальный разброс, меньше всего вызовов. *Equal jitter* держит гарантированный пол: `sleep = base/2 + random(0, base/2)`, чтобы ни один клиент не спал меньше половины бэкоффа. AWS обнаружили, что full и equal jitter завершаются примерно за одинаковое число вызовов; full jitter делает чуть меньше работы, equal jitter избегает слишком коротких сонов. Главный итог: jitter-бэкофф резко снижает и общее число вызовов, и время восстановления против простого экспоненциального бэкоффа. Фиксированный бэкофф без jitter — то, что нельзя выкатывать никогда.

## Фикс 2: retry budget и circuit breaker

Один jitter не отвечает на вопрос: сколько всего ретраев может сгореть при полном outage? Это проблема объёма, и для неё нужен другой инструмент.

Бэкофф сглаживает *тайминг* ретраев; он не ограничивает их *объём*. Это делает **retry budget**. Вместо счётчика повторов на запрос ты ограничиваешь ретраи как долю от всего трафика — Google SRE и Finagle оба берут ~**10%**: клиент может тратить ретраи лишь до 10% объёма своих запросов, а как только бюджет исчерпан — падает быстро вместо повтора. Это превращает безграничное усиление в жёсткий потолок: даже при полной аварии трафик ретраев добавит максимум 10% лишней нагрузки, а не 80×.

**Circuit breaker** атакует ту же проблему с другого конца. Он следит за частотой ошибок к зависимости; как только ошибки переходят порог, он *открывается* — каждый вызов падает мгновенно, не касаясь сети, — на время кулдауна. После кулдауна он переходит в *half-open*, пропуская один пробный вызов; успех закрывает его, ошибка снова открывает. Открытый breaker — то, что останавливает поддерживающую петлю: бэкенд получает окно нулевого трафика ретраев, восстанавливается, и проба плавно впускает трафик обратно. Сочетай оба с двумя нерушимыми правилами: **ретрай только идемпотентных операций и retryable-ошибок** (никогда не ретрай `400`, никогда не ретрай вслепую неидемпотентный `POST`) и **проброс дедлайна** — передавай оставшийся бюджет времени вниз по цепочке, чтобы сервис не ретраил запрос, от которого вызывающий уже отказался. Ретрай мёртвого запроса — чистейшая форма бесполезной, усиливающей работы.

<TradeoffMatrix
  id="07-retry-amplification-tradeoff-1"
  lessonSlug="01-overview"
  lang="ru"
  prompt="Цепочка глубиной 4 сервиса схлопывается под усилением ретраев при сбоях зависимости. Выбери фикс с наибольшим рычагом."
  options={[
    { name: "Retry budget, ограничивающий ретраи ~10% объёма запросов, плюс circuit breaker, открывающийся по порогу ошибок", summary: "Ограничь объём.", correct: true, justification: "Это фикс, который ограничивает усиление. Бюджет ретраев в 10% (Google SRE / Finagle) превращает безграничный геометрический рост в максимум 10% лишней нагрузки даже при полной аварии; circuit breaker открывается, давая бэкенду окно нулевого трафика ретраев на восстановление, затем half-open пробы впускают трафик обратно. Вместе они разрывают метастабильную поддерживающую петлю, а не просто меняют её тайминг." },
    { name: "Добавить экспоненциальный бэкофф с full jitter на каждом слое", summary: "Сгладить тайминг.", justification: "Нужно и ценно — jitter десинхронизирует thundering herd и режет время восстановления — но бэкофф лишь разносит ретраи во времени, он не ограничивает их общий объём. С 4 слоями ретраев ты всё ещё можешь дойти до 3⁴ = 81 вызова, просто размазанных. Делай это, но в одиночку этого мало." },
    { name: "Увеличить таймауты, чтобы у запросов было больше времени на успех до ретрая", summary: "Ждать дольше.", justification: "Бьёт по своим. Более длинные таймауты значат, что каждый запрос в полёте держит поток/соединение дольше при перегрузе, поэтому пул исчерпывается быстрее и ты копишь работу, которую бэкенд никогда не успеет. При метастабильной аварии это углубляет яму. Короткие дедлайны плюс проброс дедлайна обычно — верное направление." },
    { name: "Убрать ретраи полностью, чтобы ничто не могло усиливаться", summary: "Без ретраев.", justification: "Выбрасывает законную пользу ретраев — большинство временных сбоев (дроп пакета, короткая GC-пауза, failover) реально успевают со второй попытки. Ноль ретраев меняет редкий инцидент усиления на постоянные предотвратимые ошибки у пользователя. Цель — ограниченные ретраи, а не отсутствие ретраев." },
  ]}
/>

<Quiz
  id="07-retry-amplification-quiz-1"
  lessonSlug="01-overview"
  lang="ru"
  question="Запрос проходит через 4 слоя сервисов, и каждый слой повторяет 3 раза при ошибке. В худшем случае сколько вызовов достигнет самой глубокой зависимости на один упавший пользовательский запрос?"
  choices={[
    { label: "3⁴ = 81 — ретраи множатся по слоям", correct: true },
    { label: "3 × 4 = 12 — один набор ретраев на слой, сложенные", misconception: "Ретраи композируются умножением, а не сложением: ретрай каждого слоя заново запускает всё ретрай-поведение каждого слоя ниже. Это даёт повторы^глубина, а не повторы × глубина." },
    { label: "3 — реально ретраит только нижний слой", misconception: "Каждый слой со своей политикой ретраев добавляет множитель. Лист, повторяющий 3 раза, — лишь самый внутренний член 3 × 3 × 3 × 3." },
    { label: "12 только если все слои падают одновременно, иначе 3", misconception: "Усиление — про один падающий путь, повторяемый на каждом уровне по пути вниз, а не про одновременные независимые сбои." },
  ]}
/>

<Quiz
  id="07-retry-amplification-quiz-2"
  lessonSlug="01-overview"
  lang="ru"
  question="Сбой зависимости, запустивший аварию, ушёл 30 минут назад, но система всё ещё лежит под retry-штормом. Что на самом деле держит её лежащей?"
  choices={[
    { label: "Метастабильная поддерживающая петля: трафик ретраев теперь сам себе источник нагрузки, независимо от исходного триггера", correct: true },
    { label: "Исходный сбой наверняка ещё продолжается — найти и починить", misconception: "Определяющее свойство метастабильной аварии — она длится после ухода триггера. Погоня за уже ушедшим триггером — классическая трата времени; настоящая причина — поддерживающая петля." },
    { label: "Второй, не связанный баг, начавшийся в то же время", misconception: "Новый баг не нужен — одна только петля обратной связи ретраев (таймауты → ретраи → больше нагрузки → больше таймаутов) сама поддерживает деградировавшее состояние." },
    { label: "Ничего — восстановится само, если подождать", misconception: "Метастабильные аварии обычно не самозалечиваются, потому что нагрузка, которая их держит, не падает. Операторам обычно приходится сбрасывать нагрузку или перезапускать слои, чтобы силой вернуть систему в здоровое состояние." },
  ]}
/>

<DragOrder
  id="07-retry-amplification-drag-1"
  lessonSlug="01-overview"
  lang="ru"
  prompt="Расставь защиты, которые сеньор накладывает слоями, от самого широкого сокращения радиуса взрыва к самому тонкому правилу корректности:"
  items={[
    { id: "a", label: "Retry budget: ограничь ретраи ~10% объёма запросов, чтобы у усиления был жёсткий потолок" },
    { id: "b", label: "Circuit breaker: открой по порогу ошибок, чтобы дать бэкенду окно восстановления без ретраев" },
    { id: "c", label: "Экспоненциальный бэкофф с jitter: десинхронизируй thundering herd во времени" },
    { id: "d", label: "Ретрай только идемпотентных операций и retryable-ошибок — никогда 400, никогда слепой POST" },
    { id: "e", label: "Пробрасывай дедлайн, чтобы ни один слой не ретраил запрос, от которого вызывающий уже отказался" },
  ]}
  correctOrder={["a", "b", "c", "d", "e"]}
/>

</Explanation>

<FlowDiagram
  label="Один пользовательский запрос разрастается геометрически вниз по цепочке из четырёх слоёв, где каждый слой повторяет три раза: API-шлюз шлёт 3 вызова сервису, сервис — 9 слою данных, слой данных — 27 пулу соединений, достигая 81 вызова к базе данных."
  perRow={1}
  hue="var(--d-systems)"
  nodes={[
    { id: "gw", label: "API-шлюз", sub: "1 запрос" },
    { id: "svc", label: "Сервис", sub: "3 вызова" },
    { id: "data", label: "Слой данных", sub: "9 вызовов" },
    { id: "pool", label: "Пул соединений", sub: "27 вызовов" },
    { id: "db", label: "База данных", sub: "81 вызов" },
  ]}
  edges={[
    { from: "gw", to: "svc", label: "×3" },
    { from: "svc", to: "data", label: "×3" },
    { from: "data", to: "pool", label: "×3" },
    { from: "pool", to: "db", label: "×3" },
  ]}
  caption="Ретраи композируются умножением, а не сложением: при глубине 4 и 3 повторах на слой один сбойный запрос превращается в 3⁴ = 81 вызов к самой глубокой зависимости."
/>

<KeyTakeaway>Ретраи композируются умножением: 4 слоя × 3 повтора = 3⁴ = 81 вызов к бэкенду. Шторм держит аварию после триггера (метастабильность). Ограничь его retry budget ~10%, circuit breaker и бэкоффом с jitter.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="07-retry-amplification-retrieval"
  lang="ru"
  questions={[
    { q: "Объясни коллеге, почему система может лежать 30 минут после того, как запустивший аварию сбой уже ушёл.", a: "Потому что сами ретраи стали нагрузкой. Это метастабильная авария: у системы есть здоровое устойчивое состояние и деградировавшее устойчивое состояние, и временный триггер (failover, спайк, короткий сбой зависимости) толкает её в деградировавшее, где её держит самоподдерживающаяся петля обратной связи. Петля такая: бэкенд замедляется, запросы таймаутятся, таймауты порождают ретраи, ретраи добавляют нагрузку, бэкенд замедляется сильнее, больше запросов таймаутится, и так далее. Когда петля крутится, почти весь трафик — повторы запросов, чьи исходные дедлайны уже прошли: чистая бесполезная работа, дающая лишь новую нагрузку. Исходный триггер теперь неважен; его удаление ничего не даёт, потому что он больше не генерирует нагрузку. Поэтому ожидание не помогает, и единственные работающие фиксы — прямое снижение нагрузки: сброс трафика, открытие circuit breaker или перезапуск слоёв, чтобы разорвать петлю и дать системе упасть обратно в здоровое устойчивое состояние." },
    { q: "Почему экспоненциального бэкоффа с jitter в одиночку недостаточно, и что добавить, чтобы реально ограничить усиление?", a: "Бэкофф с jitter чинит тайминг ретраев, а не их объём. Jitter необходим — без него каждый клиент, таймаутнувшийся в один момент, ретраит в один момент, создавая thundering herd, прилетающий синхронным спайком, а ретраи с фиксированным интервалом ресинхронизируют это стадо каждый круг. Добавление случайности (full jitter берёт sleep равномерно в [0, base×2^attempt]; equal jitter гарантирует пол base/2) размазывает попытки по окну и резко режет и общее число вызовов, и время восстановления. Но размазать 81 вызов по времени — это всё ещё 81 вызов. Чтобы ограничить сам объём, нужен retry budget — ограничь ретраи примерно 10% объёма запросов (Google SRE и Finagle), чтобы даже при полной аварии трафик ретраев добавлял максимум 10% лишней нагрузки вместо 80x, — плюс circuit breaker, открывающийся, как только частота ошибок переходит порог, давая бэкенду окно нулевого трафика ретраев на восстановление до того, как half-open проба впустит трафик обратно. Бэкофф сглаживает; бюджет и breaker — то, что ограничивает." },
  ]}
/>

<Recap lang="ru">
Ретраи кажутся бесплатными, но композируются умножением, а не сложением: в цепочке глубиной 4, где каждый слой повторяет 3 раза, один упавший запрос может стать 3⁴ = 81 вызовом к самой глубокой зависимости, и усиление растёт геометрически с глубиной. Хуже того, трафик ретраев становится самоподдерживающимся — это метастабильная авария, где временный триггер толкает систему в деградировавшее устойчивое состояние, а петля обратной связи (медленно → таймаут → ретрай → больше нагрузки → ещё медленнее) держит её там долго после того, как триггер ушёл; поэтому системы лежат 30 минут после того, как вызвавшая их авария закончилась. Защиты накладываются слоями: экспоненциальный бэкофф с jitter десинхронизирует thundering herd и режет время восстановления (full или equal jitter, никогда фиксированный бэкофф); retry budget ограничивает ретраи примерно 10% объёма запросов, чтобы у усиления был жёсткий потолок; circuit breaker открывается по порогу ошибок, давая бэкенду окно восстановления без ретраев. Ограничь дальше, ретраяя только идемпотентные операции и retryable-ошибки и пробрасывая дедлайны, чтобы ни один слой не ретраил запрос, от которого вызывающий уже отказался. Цель — никогда не ноль ретраев — временные сбои реально успевают со второй попытки — а ретраи, которые не могут размножиться в шторм, кладущий тебя. Теперь, когда видишь систему, которая лежит после того, как триггер ушёл, твой первый вопрос: что питает retry-петлю, и какой из трёх инструментов — budget, breaker или jitter — разорвёт её быстрее?
</Recap>
