---
concepts:
- retry-amplification
- metastable-failure
- exponential-backoff-jitter
- retry-budget
- circuit-breaker
- deadline-propagation
estMin: 13
lang: ru
level: senior
order: 8
prereqs: []
slug: quiz-short
sources:
- https://aws.amazon.com/blogs/architecture/exponential-backoff-and-jitter/
- https://sre.google/sre-book/addressing-cascading-failures/
- https://www.usenix.org/publications/loginonline/metastable-failures-wild
status: ready
summary: "Промпты на свободное вспоминание по всему юниту. Отвечай каждый по памяти, потом открывай модельный ответ и сравнивай."
title: 'Retry amplification: тест на свободное вспоминание'
track: distributed
unit: 07-retry-amplification
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";

<Hook>
Вспоминание побеждает перечитывание. На каждый промпт скажи или напиши полный ответ по памяти, прежде чем открыть модельный — усилие вспоминания и есть то, что закрепляет механизм, когда тебя поднимут в 3 ночи.
</Hook>

<Goal lang="ru">
Реконструируй хребет юнита — математику fan-out, metastable-петлю, jitter, retry budget, circuit breaker, идемпотентность и проброс дедлайна — не подсматривая в урок.
</Goal>

<RetrievalDrawer
  client:load
  id="07-retry-amplification-short-retrieval"
  lang="ru"
  questions={[
    { q: "Почему ретраи складываются умножением, а не сложением, и какова формула амплификации?", a: "Потому что каждый слой со своей политикой ретраев заново запускает всё поведение ретраев слоёв под ним. Когда шлюз ретраит, он не шлёт один лишний вызов к глубокой зависимости — он заново прогоняет всю нижестоящую цепочку, каждый слой которой ретраит снова. Поэтому множители стекаются произведением, а не суммой: фактор амплификации — retries^depth. При 3 ретраях на слой и цепочке глубиной 4 один сбойный запрос превращается в 3⁴ = 81 вызов к глубокой зависимости; цепочка глубиной 5 — это 3⁵ = 243. Число геометрическое по глубине, поэтому оно застаёт врасплох тех, кто рассуждает «ретрай — это просто ещё один вызов» — и срабатывает только во время инцидента, ровно когда у бэкенда меньше всего запасной ёмкости, чтобы это поглотить." },
    { q: "Дай определение metastable failure и объясни, почему система остаётся внизу после устранения триггера.", a: "Metastable failure — это система с двумя устойчивыми состояниями: здоровым и деградированным, где временный триггер (failover, всплеск, короткий blip зависимости) сталкивает её в деградированное состояние, а самоподдерживающаяся петля обратной связи держит её там, хотя триггер давно прошёл. Для ретраев петля такая: бэкенд тормозит, запросы таймаутятся, таймауты запускают ретраи, ретраи добавляют нагрузку, бэкенд тормозит сильнее, больше таймаутов, больше ретраев. Как только петля крутится, трафик ретраев сам становится доминирующей нагрузкой — в основном повторы запросов, чьи исходные дедлайны уже прошли — чистая бесполезная работа, дающая лишь больше нагрузки. Триггер становится нерелевантным; его устранение ничего не даёт. Поэтому ожидание не помогает и операторам приходится снижать нагрузку напрямую — сбрасывать трафик, открывать breaker'ы или рестартить уровни — чтобы вернуть систему в здоровое устойчивое состояние. Термин из статьи Bronson et al. на HotOS 2021, объединяющей retry storm, congestion collapse и death spiral." },
    { q: "Почему exponential backoff необходим, но недостаточен, и что jitter добавляет сверху?", a: "Первая причина смертоносности ретраев — синхронизация: когда зависимость блипает, каждый клиент таймаутится примерно в один момент и ретраит примерно в один момент — thundering herd, приходящий всплеском. Фиксированный интервал хуже всего, потому что ресинхронизирует толпу каждый круг. Exponential backoff (base, 2×base, 4×base) разносит попытки по времени, но не расцепляет их — клиенты, стартовавшие вместе, всё равно топают вместе. Jitter добавляет случайность, чтобы клиенты разошлись по окну. Full jitter берёт sleep = random(0, base × 2^attempt) для максимального разброса и наименьшего числа вызовов; equal jitter держит гарантированный пол, sleep = base/2 + random(0, base/2), чтобы ни один клиент не спал меньше половины backoff. AWS обнаружил, что full и equal jitter завершаются примерно за одинаковое число вызовов, оба резко снижая и общее число вызовов, и время восстановления против чистого exponential backoff. Фиксированный backoff без jitter — то, что нельзя выкатывать никогда." },
    { q: "Что такое retry budget, какое значение каноническое и что он ограничивает, чего не может backoff?", a: "Backoff сглаживает тайминг ретраев; он не ограничивает их объём — 81 вызов, разнесённый по времени, всё равно 81 вызов. Retry budget ограничивает объём. Вместо счётчика ретраев на запрос он ограничивает ретраи как долю всего трафика. Google SRE и Finagle используют около 10%: клиент может тратить ретраи до 10% своего объёма запросов, и как только budget исчерпан, он быстро проваливается вместо ретрая. Это превращает неограниченную геометрическую амплификацию в жёсткий потолок — даже в полном outage трафик ретраев добавляет максимум ~10% сверх нагрузки вместо 80x. Частая реализация — token bucket: каждый запрос добавляет малую долю токена, каждый ретрай стоит полный токен, и ретрай разрешён только когда доступен целый токен." },
    { q: "Как circuit breaker рвёт самоподдерживающуюся петлю и каковы его три состояния?", a: "Circuit breaker атакует амплификацию со стороны зависимости. Он следит за частотой сбоев к зависимости; как только сбои пересекают порог, он открывается — каждый вызов мгновенно проваливается, не трогая сеть — на cooldown. Это даёт бэкенду окно нулевого трафика ретраев, единственное, что позволяет metastable-системе вернуться в здоровое состояние. После cooldown он переходит в half-open, пуская одну пробу (не полный трафик): если проба успешна, breaker закрывается и нормальный трафик возобновляется; если сбоит, breaker снова открывается на ещё один cooldown. Три состояния: closed (норма, отслеживание сбоев), open (быстрый провал, без вызовов) и half-open (одна проба, тестирующая восстановление). Допуск полного конкурентного трафика в half-open — классический баг: он заливает всё ещё хрупкий бэкенд и тут же снова срывает breaker." },
    { q: "Назови два неоспоримых правила ретраев помимо тайминга и объёма и почему каждое предотвращает бесполезную работу.", a: "Первое: ретраить только идемпотентные операции и retryable-ошибки. Никогда не ретрай детерминированную клиентскую ошибку вроде 400 — она будет сбоить идентично каждый раз и просто тратит вызов бэкенда. Никогда не ретрай слепо неидемпотентный POST — ретрай может задвоить платёж или заказ; сделай операцию идемпотентной (ключи идемпотентности), прежде чем её ретраить. Ретрай только транзиентные retryable-сбои: таймауты, 503, сброс соединения. Второе: пробрасывай дедлайн — передавай остаток времени вниз по цепочке вызовов, чтобы сервис никогда не ретраил запрос, от которого вызывающий уже отказался. Ретрай запроса, чей исходный дедлайн уже прошёл, — самая чистая форма бесполезной, амплифицирующей работы: он никогда не даст полезного результата, лишь добавит нагрузку. Вместе эти правила гарантируют, что у каждого ретрая есть реальный шанс успеть в пределах живого дедлайна, что и не даёт ретраям питать storm." },
  ]}
/>

<Recap lang="ru">
Если ты смог реконструировать каждый ответ по памяти, ты держишь хребет юнита: ретраи множатся (retries^depth), storm становится metastable failure, который переживает свой триггер, jitter расцепляет толпу во времени, retry budget ~10% ограничивает объём, circuit breaker даёт окно нулевого трафика ретраев через свои три состояния, а два жёстких правила — ретраить только идемпотентную/retryable работу и пробрасывать дедлайн — убирают самые чистые бесполезные, амплифицирующие вызовы. Цель — никогда не ноль ретраев; это ретраи, которые не могут размножиться в storm, который тебя кладёт.
</Recap>
