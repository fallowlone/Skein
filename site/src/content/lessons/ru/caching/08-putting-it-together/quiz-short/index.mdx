---
concepts:
- layer-ownership
- ttl-cascade
- stampede-vs-dogpile
- validators-vs-freshness
- swr-vs-stale-if-error
- invalidation-propagation
estMin: 14
lang: ru
level: senior
order: 8
prereqs: []
slug: quiz-short
sources:
- https://developer.mozilla.org/en-US/docs/Web/HTTP/Reference/Headers/Cache-Control
- https://blog.cloudflare.com/instant-purge/
- https://web.dev/articles/stale-while-revalidate
status: ready
summary: "Промпты на свободное воспроизведение по всему треку кэширования. Сначала ответь своими словами, затем открой модельный ответ и сравни."
title: 'Кэширование, капстоун: тест на свободное воспроизведение'
track: caching
unit: 08-putting-it-together
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";

<Hook>
Воспроизведение бьёт перечитывание. Для каждого промпта произнеси или запиши полный ответ по памяти, прежде чем открыть модельный — усилие воспроизведения поперёк всего трека и сплавляет отдельные юниты в единый инстинкт проектирования.
</Hook>

<Goal lang="ru">
Восстанови хребет трека — владение уровнями, правило каскада TTL, защиты от stampede и dogpile, валидаторы против свежести, SWR против stale-if-error и инвалидацию через write-path — не заглядывая в уроки.
</Goal>

<RetrievalDrawer
  client:load
  id="08-putting-it-together-short-retrieval"
  lang="ru"
  questions={[
    { q: "Назови четыре уровня кэширования, через которые может пройти байт, и данные, которыми владеет каждый.", a: "От самого внешнего к источнику истины: (1) edge CDN владеет разделяемыми, публичными, кэшируемыми-по-URL ответами — статические ассеты, анонимный HTML, публичные API-чтения — и никогда не должен держать данные конкретного пользователя. (2) Reverse proxy (Varnish/nginx) владеет защитой origin: схлопывает дублирующиеся запросы, чтобы холодный кэш не dogpile'ил приложение, плюс короткий кэш горячих публичных ответов близко к origin. (3) Application-кэш (Redis) владеет вычисленным состоянием приложения — дорогим результатом запроса, отрендеренным фрагментом, сессией — по ключу, которым управляет приложение, чтобы инвалидировать точечно. (4) База данных владеет истиной; её query/buffer-кэш ускоряет чтения и инвалидируется записями автоматически, поэтому его уважают, а не тюнят. Сениорский ход — назначить владение до любого тюнинга: ошибись с владением, и никакой TTL не спасёт." },
    { q: "Сформулируй правило каскада TTL и объясни, какой баг оно предотвращает.", a: "Окна свежести должны сокращаться по мере удаления от origin, либо ты обязан явно purge'ить внешний уровень на каждое внутреннее изменение. Баг, который оно предотвращает: внешний уровень держит TTL длиннее внутреннего. Если у CDN s-maxage=3600, но приложение обновляет фрагмент в Redis каждые 60с, то до часа edge отдаёт версию, которую приложение уже считает мёртвой — инвалидация на внутреннем уровне невидима внешнему, поэтому edge продолжает слать труп. Та же идея за разделением s-maxage против max-age: s-maxage нацелен на разделяемый edge, max-age на приватный браузер, и это намеренно разные числа для разных уровней." },
    { q: "Различи stampede и dogpile и дай защиту для каждого.", a: "Оба — паттерны перегрузки origin, запускаемые исчезновением закэшированного значения. Stampede — широкий случай: одно истечение TTL популярного ключа (или flush) превращает горячий ключ в лавину промахов, бьющих в origin как самонаведённый DDoS. Dogpile — конкретная параллельная форма: когда популярный ключ истекает, множество in-flight запросов промахиваются в один момент и все одновременно наваливаются на БД. Защиты пересекаются: per-key mutex / single-flight схлопывает N параллельных промахов в один пересчёт (остальные ждут тот же результат); вероятностное раннее истечение (XFetch) заставляет один запрос обновить чуть раньше истечения, поэтому ключ редко холоден; а stale-while-revalidate отдаёт устаревшее значение, пока идёт одно фоновое обновление, поэтому никто не блокируется. Сениор часто наслаивает single-flight плюс SWR." },
    { q: "Когда брать валидатор (ETag/Last-Modified) против директивы свежести (max-age/s-maxage)?", a: "Они решают разные стоимости и компонуются вместе. Директива свежести (max-age, s-maxage) позволяет кэшу отдавать локальную копию с нулевым round-trip, пока она свежа — экономит задержку и нагрузку на origin. Валидатор (ETag или Last-Modified) управляет тем, что происходит на ревалидации: когда кэш обязан проверить, он шлёт условный запрос (If-None-Match / If-Modified-Since), и origin отвечает 304 Not Modified без тела, если ничего не изменилось — экономя трафик и стоимость регенерации payload, но всё равно платя один round-trip. Так что свежесть используют, чтобы избежать запроса вовсе, а валидатор — чтобы сделать неизбежную ревалидацию дешёвой. Сильная комбинация для редко меняющегося API-чтения: s-maxage для локального попадания, ETag для дешёвой ревалидации 304 и stale-while-revalidate, чтобы обновление после истечения не блокировало читателя." },
    { q: "Сравни stale-while-revalidate и stale-if-error и объясни, почему надёжный стек использует оба.", a: "Оба позволяют кэшу отдавать устаревшую копию, но срабатывают на разные события. stale-while-revalidate срабатывает на обычное истечение против здорового origin: кэш мгновенно возвращает устаревшее значение и запускает одну фоновую ревалидацию, поэтому читатель не съедает задержку пересчёта — это оптимизация задержки/доступности для счастливого пути. stale-if-error срабатывает, когда origin возвращает 5xx или недоступен: кэш продолжает отдавать последнюю хорошую копию вместо распространения ошибки, превращая сбой origin в мягкую деградацию. Надёжный стек использует оба — например public, s-maxage=60, stale-while-revalidate=300, stale-if-error=86400 — поэтому он быстр на счастливом пути и переживает сбой. Сениорское суждение по-ресурсное: настраивай stale-if-error длинным для устойчивых к устарелости данных (прайс-лист) и коротким или нулевым для критичного к корректности состояния (наличие на складе), где отдача устаревшего может привести к oversell." },
    { q: "Почему инвалидация принадлежит write-path и каков правильный порядок распространения?", a: "Потому что инвалидация — это проблема распространения, а не событие: когда источник истины меняется, изменение должно идти наружу через каждый уровень, держащий копию, и каждый уровень инвалидируется по-своему — flush Redis ничего не делает CDN, а purge CDN ничего не делает proxy, всё ещё держащему устаревший объект. Поэтому мутация, меняющая истину, должна сама триггерить инвалидацию по порядку: (1) запись коммитится в БД, и её query-кэш инвалидируется автоматически; (2) write-path удаляет или обновляет затронутые ключи Redis; (3) purge'ит устаревший объект reverse proxy; (4) выполняет tag / surrogate-key purge на CDN (например product:42), сбрасывая каждую копию на edge независимо от URL; (5) браузеры самоисцеляются на следующей ревалидации, а stale-while-revalidate покрывает зазор. Где можно, обойди инвалидацию вовсе через версионированные/immutable URL для статических ассетов — новый хэш контента это новый URL, поэтому purge'ить нечего." },
  ]}
/>

<Recap lang="ru">
Если ты восстановил каждый ответ по памяти, ты держишь хребет трека как единый дизайн: назначь каждому уровню данные, которыми он владеет, сокращай свежесть наружу (или purge'ай внешний уровень на каждое внутреннее изменение), защищай истечение горячего ключа через single-flight плюс SWR, используй директивы свежести, чтобы пропустить запрос, и валидаторы, чтобы сделать неизбежную ревалидацию дешёвым 304, наслаивай stale-while-revalidate для счастливого пути и stale-if-error для сбоев, и встраивай инвалидацию в write-path, чтобы она распространялась Redis → proxy → CDN по порядку. Капстоунский навык — скомпоновать это в стратегию, держащуюся под реальной нагрузкой, а не заучить одну директиву. Теперь, когда садишься проектировать кэширование для нового сервиса, ты начнёшь с карты владения — а не с чисел TTL.
</Recap>
