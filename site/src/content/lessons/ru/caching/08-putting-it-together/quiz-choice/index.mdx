---
concepts:
- layer-ownership
- ttl-cascade
- stampede-protection
- conditional-validators
- stale-while-revalidate
- invalidation-propagation
estMin: 14
lang: ru
level: senior
order: 7
prereqs: []
slug: quiz-choice
sources:
- https://developer.mozilla.org/en-US/docs/Web/HTTP/Reference/Headers/Cache-Control
- https://blog.cloudflare.com/instant-purge/
- https://web.dev/articles/stale-while-revalidate
status: ready
summary: "Тест с выбором на синтез по всему треку кэширования — владение уровнями, каскад TTL, защита от stampede и dogpile, валидаторы, SWR и распространение инвалидации как единая стратегия."
title: 'Кэширование, капстоун: тест с выбором ответа'
track: caching
unit: 08-putting-it-together
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Шесть вопросов поперёк всего трека. Ни один не проверяет отдельную директиву в изоляции — каждый требует рассуждать о том, как компонуются два или больше уровня, а именно там и живут реальные баги кэширования.
</Hook>

<Goal lang="ru">
Убедись, что связываешь владение уровнями, каскад TTL, защиту от stampede, валидаторы и stale-while-revalidate в единую связную стратегию — тот синтез, к которому вели отдельные юниты.
</Goal>

<Quiz
  id="08-putting-it-together-choice-1"
  lessonSlug="08-putting-it-together"
  lang="ru"
  question="Запрос проходит через CDN, reverse proxy, Redis и БД. Редактор меняет товар, write-path обновляет БД и удаляет ключ Redis, но пользователи час видят старую цену. Где баг?"
  choices={[
    { label: "CDN держит собственную копию под s-maxage, и её никто не purge'нул. Инвалидация на внутреннем уровне не распространяется наружу — write-path обязан ещё и purge'нуть proxy и выполнить tag/surrogate-key purge на edge.", correct: true },
    { label: "Redis удалили, но не прогрели, поэтому страница падает на устаревшее значение.", misconception: "Удалённый ключ Redis вызывает промах, который пересчитывает из БД — свежее значение. Пользователь, прикованный к старой цене на час, указывает на внешний кэш (CDN), держащий копию, которую никто не инвалидировал, а не на Redis." },
    { label: "Query-кэш БД возвращает строку до записи, потому что игнорирует коммит.", misconception: "Query/buffer-кэш БД инвалидируется автоматически той записью, что изменила строку. Устаревшая копия выживает на уровне, не связанном с write-path — на CDN." },
    { label: "TTL конфликтуют, уровни падают с ошибкой и отдают последнее значение.", misconception: "Несовпадающие TTL не дают ошибки; они молча отдают устаревшее. Более длинный внешний TTL побеждает для пользователя, пока не истечёт или его явно не purge'нут." },
  ]}
/>

<Quiz
  id="08-putting-it-together-choice-2"
  lessonSlug="08-putting-it-together"
  lang="ru"
  question="Ты ставишь CDN s-maxage=3600, но приложение ревалидирует свой фрагмент в Redis каждые 60с. Почему это баг композиции, независимо от того, какие числа корректны по отдельности?"
  choices={[
    { label: "Внешнее окно свежести длиннее внутреннего, поэтому до часа edge отдаёт версию, которую приложение уже считает мёртвой. Свежесть должна сокращаться наружу, либо каждое внутреннее изменение должно явно purge'ить внешний уровень.", correct: true },
    { label: "Всё нормально — Redis это источник истины, поэтому его окно 60с управляет тем, что видят пользователи.", misconception: "Redis не источник истины, и даже если бы был, внутреннее обновление невидимо внешнему кэшу. CDN отдаёт свою копию все 3600с независимо от Redis." },
    { label: "Всё нормально, потому что s-maxage влияет только на браузеры, а они и так ревалидируют быстро.", misconception: "s-maxage — это именно директива, которую соблюдают разделяемые кэши вроде CDN; max-age нацелен на приватный кэш браузера. CDN соблюдает 3600с." },
    { label: "Баг лишь в том, что 3600 слишком велик; деление обоих чисел пополам чинит композицию.", misconception: "Равное сокращение оставляет внешнее окно больше-или-равным внутреннему, поэтому тот же зазор отдачи устаревшего сохраняется. Дефект в порядке (внешнее должно быть ≤ внутреннего), а не в абсолютном размере." },
  ]}
/>

<Quiz
  id="08-putting-it-together-choice-3"
  lessonSlug="08-putting-it-together"
  lang="ru"
  question="Горячий ключ с тысячами параллельных читателей истёк. Можно добавить (a) per-key mutex, чтобы один запрос пересчитывал, пока остальные ждут, или (b) stale-while-revalidate. Сениор обычно сначала берёт SWR. Почему?"
  choices={[
    { label: "SWR мгновенно отдаёт устаревшее значение каждому читателю и ревалидирует один раз в фоне, поэтому никто не ждёт — это полностью убирает обрыв задержки. Mutex всё равно блокирует ожидающих, пока единственный пересчёт не завершится.", correct: true },
    { label: "Mutex рискует deadlock, поэтому его всегда надо избегать.", misconception: "Ограниченный per-key lock с таймаутом не даёт deadlock; single-flight — корректная защита от stampede. Причина предпочесть SWR — задержка, а не безопасность: mutex заставляет ожидающих блокироваться, SWR — нет." },
    { label: "SWR гарантирует, что читатели никогда не видят устаревшее, поэтому он строго корректнее.", misconception: "Наоборот: весь механизм SWR — отдавать устаревшую копию, пока он ревалидирует. Его выбирают, когда ограниченная устарелость приемлема; mutex прав, когда читатели должны увидеть пересчитанное значение." },
    { label: "Mutex ничего не делает для защиты origin под stampede.", misconception: "Mutex (single-flight) — это именно механизм защиты origin: он схлопывает N промахов в один пересчёт. Оба защищают origin; различаются в том, блокируются ли ожидающие." },
  ]}
/>

<Quiz
  id="08-putting-it-together-choice-4"
  lessonSlug="08-putting-it-together"
  lang="ru"
  question="Публичное API-чтение стоит 40мс на вычисление и меняется редко. Нужны низкая задержка, низкая нагрузка на origin и экономия трафика на проводе. Какая комбинация компонует это правильно?"
  choices={[
    { label: "s-maxage на edge для разделяемого кэширования + ETag, чтобы ревалидации возвращали 304 без тела + stale-while-revalidate, чтобы истёкшая запись отдавалась мгновенно, пока обновляется. Каждый слой решает свою стоимость.", correct: true },
    { label: "Только длинный max-age — раз закэшировано, больше ничего не нужно.", misconception: "Один max-age не даёт мягкого истечения (нет SWR, поэтому первый читатель после истечения съедает 40мс) и дешёвой ревалидации (нет ETag, поэтому каждая ревалидация шлёт полное тело). Он решает одну стоимость и игнорирует две." },
    { label: "no-store плюс серверный кэш Redis, чтобы держать HTTP-кэши вне пути.", misconception: "no-store запрещает edge и браузеру кэшировать, выбрасывая самые дешёвые выигрыши (zero-RTT попадания в браузере, разгрузка edge, 304). Один серверный кэш оставляет каждого клиента платить полный RTT и трафик." },
    { label: "Только ETag — условные запросы делают остальные директивы избыточными.", misconception: "ETag всё равно требует round-trip на запрос, чтобы получить 304; без s-maxage нет локального попадания, а без SWR ревалидация синхронна. ETag экономит трафик, а не round-trip и не обрыв задержки." },
  ]}
/>

<Quiz
  id="08-putting-it-together-choice-5"
  lessonSlug="08-putting-it-together"
  lang="ru"
  question="Залогиненный дашборд рендерит персональные платёжные данные и стоит за разделяемым CDN. Хэндлер возвращает 200 без Cache-Control. В чём реальный риск и правильное исправление?"
  choices={[
    { label: "Разделяемые кэши по умолчанию считают похожий на кэшируемый 200 GET сохраняемым, поэтому edge может отдать дашборд одного пользователя другому — это утечка данных, а не проблема hit-rate. Исправление: private (или no-store для чувствительных данных), никогда не ответ без директивы.", correct: true },
    { label: "Риск низкий — приложение отдаёт его прямо клиенту, поэтому никакой промежуточный кэш его не хранит.", misconception: "Именно это допущение о прямой доставке клиенту может молча отменить изменение конфигурации edge, proxy или правило surrogate-key. Это класс утечки Steam 2015: неаннотированный аутентифицированный 200 сохранён и отдан чужому." },
    { label: "Добавить Vary: Cookie, чтобы каждая сессия получала свою запись кэша.", misconception: "Vary: Cookie ключуется на весь cookie, обрушивая hit-rate почти до нуля и рискуя коллизиями при одной пропущенной нормализации. Для персональных данных ответ — private/no-store, а не хрупкий Vary." },
    { label: "Поставить короткий s-maxage, чтобы утёкшая копия быстро истекла.", misconception: "Короткий TTL не делает персональный контент безопасным в разделяемом кэше — всё окно CDN отдаёт платёжную страницу одного пользователя другим. TTL управляет устарелостью, а не аудиторией." },
  ]}
/>

<Quiz
  id="08-putting-it-together-choice-6"
  lessonSlug="08-putting-it-together"
  lang="ru"
  question="Поперёк всего стека: какое утверждение о fail open во время сбоя origin верно?"
  choices={[
    { label: "stale-if-error позволяет каждому разделяемому уровню продолжать отдавать последнюю хорошую копию, когда origin возвращает 5xx или недоступен, превращая сбой в мягкую деградацию. Настраивай его длинным для устойчивых к устарелости ресурсов и коротким или нулевым для тех, что должны быть корректны или отсутствовать.", correct: true },
    { label: "stale-while-revalidate уже покрывает сбои, поэтому stale-if-error избыточен.", misconception: "SWR срабатывает на истечение и ревалидирует против здорового origin; если origin лежит, фоновая ревалидация падает. stale-if-error — это директива, которая специально покрывает случай ошибки и недоступности." },
    { label: "Отдавать устаревшее во время сбоя всегда безопасно, поэтому stale-if-error надо ставить максимальным везде.", misconception: "Некоторые данные небезопасны устаревшими — флаг наличия на складе, отданный устаревшим, может привести к oversell. Сениорское решение по-ресурсное: длинно для прайс-листа, коротко или нулём для критичного к корректности состояния." },
    { label: "Fail open — забота только CDN; внутренние уровни не могут отдавать устаревшее.", misconception: "Любой уровень кэширования может fail open — reverse proxy с stale-if-error, application-кэш, настроенный отдавать последнее значение при ошибке origin. Это свойство композиции всего стека." },
  ]}
/>

<Recap lang="ru">
Сквозная линия трека — единая скомпонованная стратегия: назначь каждому уровню данные, которыми он владеет, сокращай окна свежести наружу (или purge'ай внешний уровень на каждое внутреннее изменение), защищай origin на истечении горячего ключа через single-flight или SWR, экономь трафик через 304 на ETag, ограничивай персональные ответы через private/no-store, встраивай инвалидацию в write-path, чтобы она распространялась Redis → proxy → CDN по порядку, и fail open через stale-if-error, настроенный по-ресурсно. Ответ не в одной директиве; ответ в том, как уровни компонуются. Теперь, когда встретишь реальный инцидент — устаревший контент, утечку данных или завал на origin, — у тебя есть чеклист: кто владеет этим ресурсом, у какого слоя самый длинный TTL и какой шаг write-path был пропущен.
</Recap>
