---
concepts:
- access-latency
- hit-ratio
- os-page-cache
- wrong-layer-caching
- double-caching
- cache-invalidation
estMin: 14
lang: ru
level: senior
order: 8
prereqs: []
slug: quiz-short
sources:
- https://gist.github.com/jboner/2841832
- https://redis.io/blog/why-your-cache-hit-ratio-strategy-needs-an-update/
- https://developer.mozilla.org/en-US/docs/Web/HTTP/Headers/Cache-Control
status: ready
summary: "Промпты на свободное воспроизведение по всему юниту. Сначала ответь своими словами, потом раскрой модельный ответ и сравни."
title: 'Уровни кэша: тест на свободное воспроизведение'
track: caching
unit: 01-layers
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";

<Hook>
Воспроизведение бьёт перечитывание. На каждый промпт скажи или напиши полный ответ по памяти, прежде чем открыть модельный — усилие припоминания и есть то, что закрепляет лестницу задержек и сбои.
</Hook>

<Goal lang="ru">
Восстанови позвоночник юнита — лестницу задержек, точку окупаемости hit ratio, OS page cache, где кэшировать и сбои wrong-layer и double-caching — не подглядывая в урок.
</Goal>

<RetrievalDrawer
  client:visible
  id="01-layers-short-retrieval"
  lang="ru"
  questions={[
    { q: "Пройди лестницу задержек кэширования от быстрого к медленному с грубыми числами и объясни, почему порядки величин важнее точных цифр.", a: "Грубо: L1 CPU cache ~1 нс, L2 ~4 нс, L3 ~10–40 нс, RAM и OS page cache ~100 нс, SSD ~100 мкс, in-region round trip к Redis ~1 мс, дальний CDN-хоп 100+ мс. Каждая ступень больше, дешевле и медленнее предыдущей. Точные числа плывут с железом, но порядки величин — инструмент решения: L1 в ~200× быстрее RAM, RAM в ~1000× быстрее SSD, SSD в ~100× быстрее межконтинентального хопа. Добавляя cache, ты ставишь на то, что слой, перед которым ты его ставишь, медленнее cache с большим запасом. Если нет — если ставишь cache на скорости сети перед origin на скорости RAM — ты добавил задержку, а не убрал её. Senior держит порядки величин в голове, потому что они решают, помогает ли cache вообще." },
    { q: "Почему добавление cache может ухудшить задержку и какая формула средней задержки это доказывает?", a: "Cache убирает задержку, только если слой, перед которым он стоит, сильно медленнее самого cache, а miss платит ОБЕ стоимости — cache lookup и полную выборку из origin. Поэтому средняя задержка = hit_rate × cache_latency + miss_rate × (cache_latency + origin_latency). Две поломки: во-первых, если origin уже быстрый — тёплый primary-key lookup в Postgres из buffer pool за ~0.3 мс — то даже hit (round trip к Redis ~1 мс) медленнее, чем просто спросить origin. Во-вторых, при низком hit ratio против любого origin налог за miss доминирует: ты платишь cache+origin на большинстве запросов почти даром. Cache окупается, только когда origin_latency велик, hit ratio высокий, а данные терпят лёгкое устаревание. Промахнись в любом — и ты добавил домен отказа и проблему инвалидации ни за что." },
    { q: "Что такое OS page cache и почему он меняет решение «добавлять ли Redis»?", a: "Когда ты читаешь файл, ядро держит его страницы в свободной RAM, поэтому второе чтение — попадание в память, а не на диск. Вместе с собственным buffer pool БД это значит, что чтение «с диска» горячих данных обычно — чтение из RAM, поэтому БД быстры на своём рабочем наборе без твоих усилий. Вывод: если рабочий набор влезает в RAM, БД уже является cache на скорости RAM, поэтому обёртка в Redis лишь вставляет сетевой round trip и может замедлить горячие чтения. Page cache локальный и привязан к машине, поэтому его нельзя делить между инстансами — именно это разделение и есть настоящая причина тянуться за Redis. Прежде чем добавлять application cache, измерь реальную задержку origin; если она уже на скорости RAM из-за page cache и buffer pool, cache не поможет." },
    { q: "Дан кандидат-путь чтения — как решить, кэшировать ли и на каком слое?", a: "Сначала три ворот: реально ли медленный origin, высокий ли hit ratio (горячие, переиспользуемые ключи) и терпят ли данные устаревание? Если хоть одно нет — не кэшируй. Если да, выбирай слой по природе и волатильности данных. Статичные или почти статичные ответы, общие для пользователей, идут на CDN с ключом по URL, эксплуатируя географию. Часто инвалидируемый результат запроса, общий для пользователей, идёт в application cache (Redis) по входам, с коротким TTL плюс инвалидацией на запись — это подходит join-у в 80 мс, вызываемому 50×/сек на read-mostly данных. CPU-bound результаты надо кэшировать как вычисленные значения, а не толкать на нижние слои (page cache ускоряет чтение, но не пропускает пересчёт). Персонализированные или пер-запросные данные надо кэшировать близко к пользователю — пер-сессионный app cache или вовсе нет — но не на общем слое с ключом по URL. Правило: кэшируй так близко к потребителю, как позволяет волатильность данных." },
    { q: "Что такое double-caching, почему оно даёт баги stale-on-stale и что делает их трудными для отладки?", a: "Double-caching — это когда один и тот же факт кэшируется на двух независимых слоях — например значение в Redis и отрендеренный ответ на reverse proxy или CDN — у каждого свой TTL и свой путь инвалидации, без координации между ними. Запись чистит Redis, но CDN продолжает отдавать старый HTML до истечения своего TTL; или CDN очищен, но proxy перед ним держит свою устаревшую копию — stale-on-stale, два слоя рассинхронизированы. Отлаживать трудно, потому что каждый слой сообщает лишь о себе: CF-Cache-Status описывает CDN, X-Cache — proxy, а reverse proxy может тихо переопределить Cache-Control origin (origin говорит no-cache, proxy держит TTL 5 минут). Приходится идти по запросу слой за слоем — Age, CF-Cache-Status, X-Cache, логи proxy, hit/miss Redis, затем БД — чтобы найти, какая копия лжёт. Структурный фикс — один владелец на кэшируемый факт и один путь инвалидации." },
    { q: "Что на деле говорит низкий hit ratio и почему «повысить hit ratio» — не всегда цель?", a: "Hit ratio — это число, оправдывающее cache: ниже точки окупаемости для данных origin и cache налог за miss (cache lookup + полный origin) делает cache минусовым против отсутствия кэша. Низкий ratio обычно — сигнал о нагрузке или дизайне ключей (одноразовые ключи, слишком короткий TTL, плохая кардинальность ключей), а не баг стабильности. Но повышать сырой ratio не всегда правильно: кэширование холодных, редко переиспользуемых данных ради цифры может вытеснить реально горячие ключи, освобождая место под одноразовые, и поднять общую задержку. Дорогие кэши в production — это те с hit ratio 40%, которые все боятся убрать. Настоящая цель — снижает ли cache общую задержку и нагрузку на origin достаточно, чтобы оправдать новый домен отказа и стоимость инвалидации, а не тщеславная метрика hit ratio." },
  ]}
/>

<Recap lang="ru">
Если ты смог восстановить каждый ответ по памяти, ты держишь позвоночник юнита: кэширование — лестница задержек, где порядки величин решают, помогает ли cache; miss платит cache+origin, поэтому низкий hit ratio или быстрый origin делают cache хуже, чем его отсутствие; OS page cache (кэш страниц ядра, хранящий горячие данные в RAM) и buffer pool часто уже делают origin быстрым, как RAM; кэшируешь, только когда origin медленный, hit ratio высокий, а устаревание терпимо, на слое, который позволяет волатильность данных; а повторяющиеся senior-сбои — wrong-layer кэширование и double-caching одного факта на двух слоях, лечатся одним владельцем и одним путём инвалидации на каждый кэшируемый факт. Теперь, столкнувшись с кэшинговым решением в продакшне, сначала гонишь три ворот — и только потом открываешь конфиг TTL.
</Recap>
