---
concepts:
- cache-hierarchy
- access-latency
- hit-ratio
- wrong-layer-caching
- double-caching
- cache-invalidation
estMin: 12
lang: ru
level: senior
order: 7
prereqs: []
slug: quiz-choice
sources:
- https://gist.github.com/jboner/2841832
- https://redis.io/blog/why-your-cache-hit-ratio-strategy-needs-an-update/
- https://developer.mozilla.org/en-US/docs/Web/HTTP/Headers/Cache-Control
status: ready
summary: "Тест с выбором на синтез по всему юниту — лестница задержек, где кэшировать, точка окупаемости hit ratio и сбои wrong-layer и double-caching."
title: 'Уровни кэша: тест с выбором ответа'
track: caching
unit: 01-layers
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Шесть вопросов поперёк всего юнита. Каждый отражает решение, которое ты принимаешь в реальной системе — не определение для заучивания, а компромисс, который надо взвесить, как только кто-то скажет «просто добавь кэш».
</Hook>

<Goal lang="ru">
Убедись, что связываешь лестницу задержек, выбор слоя для кэша, точку окупаемости hit ratio и сбои wrong-layer и double-caching — тот синтез, к которому вёл урок.
</Goal>

<Quiz
  id="01-layers-choice-1"
  lessonSlug="01-layers"
  lang="ru"
  question="Команда ставит in-region Redis cache (~1 мс round trip) перед primary-key lookup в Postgres (тёплый, отдаётся из buffer pool за ~0.3 мс). p99 чтения растёт. Почему?"
  choices={[
    { label: "Origin и так был на скорости RAM, поэтому cache лишь добавляет сетевой хоп. Попадание в buffer pool / page cache быстрее, чем round trip к Redis, который ты поставил перед ним — ты обернул быстрый origin в более медленный cache.", correct: true },
    { label: "Redis однопоточный, поэтому сериализует все lookup-и.", misconception: "Redis быстр и фактически неблокирующий для простых GET; пропускная способность тут не проблема. Задержка выросла, потому что сетевой round trip к Redis медленнее тёплого чтения из buffer pool, перед которым он стоит." },
    { label: "Postgres стал медленнее, потому что cache отобрал у него память.", misconception: "Redis работает как отдельный процесс / инстанс и не уменьшает buffer pool Postgres. Регрессия — это добавленный сетевой хоп на пути, который и так был попаданием в RAM." },
    { label: "Ключ кэша хэшировался, а это дорого на p99.", misconception: "Хэширование ключа — это наносекунды, ничто рядом с ~1 мс сетевого round trip. Структурная ошибка — поставить cache на скорости сети перед origin на скорости RAM." },
  ]}
/>

<Quiz
  id="01-layers-choice-2"
  lessonSlug="01-layers"
  lang="ru"
  question="Эндпоинт чтения выполняет join по 3 таблицам за 80 мс, вызывается 50×/сек, данные меняются пару раз в день. Где должен быть cache?"
  choices={[
    { label: "Application cache (Redis) по входам запроса, с коротким TTL плюс инвалидация на запись. Origin реально медленный, доступ горячий, поэтому hit ratio высокий, а данные read-mostly — round trip в ~1 мс на два порядка дешевле join-а.", correct: true },
    { label: "Добавить RAM и положиться на buffer pool БД / OS page cache, чтобы держать данные горячими.", misconception: "Нижние слои помогают I/O-bound чтениям, но 80 мс — это работа CPU (join и агрегация), а не чтения с диска. Page cache делает строки быстрыми для выборки, но не может пропустить пересчёт join-а. Нужно кэшировать вычисленный результат." },
    { label: "Кэшировать отрендеренный HTML на CDN с длинным TTL.", misconception: "Неверный слой для эндпоинта данных. CDN ключуется по URL и предназначен для статичных или почти статичных ответов; длинный edge TTL означает, что изменение данных проявится глобально только по истечении, а персонализированные варианты утекут между пользователями." },
    { label: "Без кэша — 80 мс нормально, держи проще.", misconception: "50 вызовов/сек по 80 мс — это реальная нагрузка на origin и реальный хвост задержки, а данные терпят устаревание. Это чистый кейс для кэша; пропуск меняет дешёвую понятную победу на лишнее давление на origin." },
  ]}
/>

<Quiz
  id="01-layers-choice-3"
  lessonSlug="01-layers"
  lang="ru"
  question="Cache с hit ratio 35% стоит перед origin в 80 мс за lookup-ом в 1 мс. Средняя задержка 0.35×1 + 0.65×(1+80) ≈ 53 мс — лучше, чем 80. Почему hit ratio 35% всё равно тревожный сигнал?"
  choices={[
    { label: "Каждый miss теперь платит cache lookup поверх origin (81 мс против 80), ты добавил новый домен отказа и проблему инвалидации, а 35% означают, что cache в основном не окупает свою сложность — небольшой сдвиг профиля трафика может увести его в минус.", correct: true },
    { label: "Это не тревожный сигнал — 53 мс лучше 80 мс, выкатывай.", misconception: "Среднее улучшается лишь потому, что origin достаточно медленный, чтобы поглотить налог за miss, но переиспользование в 35% — слабое: miss-ы всё равно платят cache+origin, а ты купил новую движущуюся часть и путь инвалидации ради тонкой и хрупкой к смене трафика выгоды." },
    { label: "Hit ratio неважен, раз средняя задержка упала.", misconception: "Hit ratio — это именно то число, что оправдывает cache. Ниже точки окупаемости налог за miss (cache lookup плюс полный origin) делает cache строго хуже, чем его отсутствие." },
    { label: "Низкий hit ratio означает, что Redis настроен неправильно и упадёт.", misconception: "Низкий hit ratio — это сигнал о профиле нагрузки / дизайне ключей (одноразовые ключи, слишком короткий TTL, плохая кардинальность ключей), а не баг стабильности. Он говорит, что cache может не стоить своей цены здесь." },
  ]}
/>

<Quiz
  id="01-layers-choice-4"
  lessonSlug="01-layers"
  lang="ru"
  question="Почему OS page cache — самый недооценённый слой при решении, добавлять ли Redis?"
  choices={[
    { label: "Ядро держит недавно прочитанные страницы файлов в свободной RAM, поэтому чтение «с диска» горячих данных на деле — чтение из RAM. Вместе с buffer pool БД это значит: если рабочий набор влезает в RAM, БД уже является cache на скорости RAM — добавление Redis лишь вставляет сетевой хоп.", correct: true },
    { label: "Page cache сжимает данные, поэтому в RAM влезает больше, чем может Redis.", misconception: "Page cache хранит сырые страницы файлов, а не сжатые данные, и дело не в этом. Его ценность в том, что горячие чтения отдаются из RAM без усилий с твоей стороны, поэтому origin может уже быть на скорости RAM." },
    { label: "Page cache реплицируется между машинами, как Redis.", misconception: "Page cache локальный и привязан к одной машине — его нельзя разделить между инстансами, а именно это добавляет application cache. Его значимость здесь в том, что он делает горячие чтения одного узла быстрыми, как RAM." },
    { label: "Он гарантирует, что данные никогда не устаревают.", misconception: "Page cache — про задержку чтения, а не про свежесть, и поддерживается когерентным через файловую систему. Важен он здесь тем, что делает origin быстрым, поэтому сетевой cache перед ним может добавить задержку вместо того, чтобы убрать её." },
  ]}
/>

<Quiz
  id="01-layers-choice-5"
  lessonSlug="01-layers"
  lang="ru"
  question="Значение кэшируется в Redis (инвалидируется на запись) И его отрендеренный ответ кэшируется на CDN с TTL 5 минут. После записи пользователи всё ещё видят старые данные минутами. В чём сбой и каков структурный фикс?"
  choices={[
    { label: "Double-caching: один и тот же факт живёт на двух слоях с независимыми временами жизни. Запись чистит Redis, но CDN продолжает отдавать устаревший HTML до истечения своего TTL. Фикс: один владелец на кэшируемый факт и один путь инвалидации — purge CDN на запись или вообще не кэшировать этот ответ на edge.", correct: true },
    { label: "Инвалидация Redis провалилась; просто снизь TTL у Redis.", misconception: "Redis свежий — он инвалидирован корректно. Устаревшая копия на CDN. Снижение TTL у Redis ничего не меняет, потому что лжёт слой CDN; проблема в двух нескоординированных слоях, а не в одном TTL." },
    { label: "TTL у CDN слишком длинный; поставь 30 секунд — и баг исчезнет.", misconception: "Сокращение edge TTL лишь уменьшает окно устаревания — слои всё равно нескоординированы, и запись всё равно невидима до истечения. Настоящий фикс — purge-on-write или единый владеющий слой, а не меньшее окно." },
    { label: "Это неизбежно с любым CDN — кэширование всегда рискует устареванием.", misconception: "Stale-on-stale здесь вызвано двумя независимыми слоями, кэширующими один факт, и этого можно избежать: дай каждому факту одного владельца и один путь инвалидации. Единственный корректно очищаемый слой кэша не расходится сам с собой." },
  ]}
/>

<Quiz
  id="01-layers-choice-6"
  lessonSlug="01-layers"
  lang="ru"
  question="Инженер кэширует пер-пользовательский фрагмент «рекомендуем вам» на CDN, чтобы снизить нагрузку на origin. Часть пользователей начинает видеть чужие рекомендации. Корневая причина?"
  choices={[
    { label: "CDN ключует записи кэша по URL (и настроенным заголовкам), а не по личности. Персонализированный контент на общем слое с ключом по URL схлопывает разных пользователей в одну запись, утекая данные. Персонализируй так близко к пользователю, как позволяют данные — пер-сессионный app cache или вовсе без общего кэша — но не на edge.", correct: true },
    { label: "Баг в модели рекомендаций перепутал пользователей.", misconception: "Модель в порядке — фрагменты отдаются из общей записи CDN с ключом по URL, поэтому кэшированный фрагмент пользователя A возвращается пользователю B. Это ошибка слоя кэширования, а не баг модели." },
    { label: "Куки были срезаны, поэтому origin запутался.", misconception: "Даже если origin читает куку корректно, попадание в CDN с ключом по URL никогда не доходит до origin — он отдаёт то, что закэшировал первым. Дефект — кэширование данных, зависящих от личности, на слое, который не различает пользователей." },
    { label: "TTL у CDN был слишком длинным, поэтому старые рекомендации утекли.", misconception: "Это межпользовательская утечка, а не устаревание. Более короткий TTL всё равно отдаёт фрагмент одного пользователя другому внутри окна. Персонализированные данные просто нельзя кэшировать на общем слое с ключом по URL." },
  ]}
/>

<Recap lang="ru">
Сквозная линия: кэширование — это лестница задержек (L1, RAM, page cache, Redis, CDN), и cache помогает только когда стоит перед реально более медленным origin, hit ratio высокий, а данные терпят устаревание. Page cache и buffer pool БД часто уже делают origin быстрым, как RAM, поэтому проверь реальную задержку origin, прежде чем тянуться за Redis. Senior-сбои повторяются: cache перед быстрым origin, оправдание кэша слабым hit ratio, кэширование персонализированных данных на CDN с ключом по URL и double-caching одного факта на двух слоях с независимыми TTL. Кэшируй так близко к потребителю, как позволяет волатильность данных; дай каждому факту одного владельца и один путь инвалидации.
</Recap>
