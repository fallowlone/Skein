---
concepts:
- cache-control-headers
- cache-status
- hit-miss-flow
- stale-while-revalidate
estMin: 14
lang: ru
level: senior
order: 9
prereqs: []
slug: quiz-code
sources:
- https://developer.mozilla.org/en-US/docs/Web/HTTP/Headers/Cache-Control
- https://developer.mozilla.org/en-US/docs/Web/HTTP/Headers/Age
- https://gist.github.com/jboner/2841832
status: ready
summary: "Читай реальные заголовки Cache-Control, trace-заголовки ответа и слоистый путь hit/miss, затем выбирай поведение и фикс с наибольшим рычагом."
title: 'Уровни кэша: чтение заголовков и трейса'
track: caching
unit: 01-layers
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Баги кэша диагностируются в заголовках ответа и пер-слойном трейсе, а не на диаграмме. Прочитай заголовок, предскажи, что делает каждый слой, и выбери фикс, который senior делает первым.
</Hook>

<Goal lang="ru">
Отработай цикл, который ты гоняешь на каждом инциденте с кэшем: прочитай директивы Cache-Control и trace-заголовки, спустись по стеку слоёв за запросом и найди, какой слой лжёт, прежде чем трогать TTL.
</Goal>

## Сниппет 1 — заголовок ответа

```http
HTTP/1.1 200 OK
Content-Type: application/json
Cache-Control: private, no-store
```

<Quiz
  id="01-layers-code-1"
  lessonSlug="01-layers"
  lang="ru"
  question="Инженер добавляет этот заголовок к персонализированному эндпоинту /me, затем спрашивает, почему hit ratio у CDN равен 0% и нагрузка на origin не упала. Что верно?"
  choices={[
    { label: "no-store запрещает любому слою хранить ответ, а private запрещает общим кэшам (CDN, reverse proxy) кэшировать его вообще — хранить может лишь приватный браузерный кэш, но no-store перекрывает даже это. Заголовок корректен для персонализированных данных, поэтому 0% hit ratio у CDN — это задуманный исход; нагрузку надо снижать кэшированием чего-то другого (общего, не персонализированного фрагмента).", correct: true },
    { label: "Добавь max-age=300 рядом с no-store, и CDN начнёт кэшировать.", misconception: "no-store побеждает — он запрещает хранение полностью, поэтому max-age лишний. А кэширование персонализированного ответа на общем CDN утекло бы данными одного пользователя другому; 0% hit ratio корректен, это не баг для починки здесь." },
    { label: "private значит, что кэширует только origin, поэтому CDN всё равно должен кэшировать.", misconception: "private значит, что хранить может лишь приватный для пользователя кэш (браузер); общие кэши вроде CDN не должны. Это противоположность «только origin» — а no-store запрещает даже браузеру." },
    { label: "CDN настроен неверно — эти директивы должны давать высокий hit ratio.", misconception: "Директивы явно отказываются от общего кэширования. Персонализированный эндпоинт не должен кэшироваться на CDN; 0% hit ratio — это заголовок, делающий свою работу." },
  ]}
/>

## Сниппет 2 — trace-заголовки

```http
HTTP/1.1 200 OK
Age: 240
Cache-Control: public, max-age=300
CF-Cache-Status: HIT
X-Cache: MISS from reverse-proxy
```

<Quiz
  id="01-layers-code-2"
  lessonSlug="01-layers"
  lang="ru"
  question="Пользователь сообщает об устаревших данных через 4 минуты после записи. Читая эти заголовки из одного ответа, что заключаешь?"
  choices={[
    { label: "CDN отдаёт кэшированную копию (CF-Cache-Status: HIT) возрастом Age 240с, внутри своего max-age 300с, поэтому запись до него не дошла — CDN не был очищен (purge). Reverse proxy промахнулся и пошёл вверх, но CDN замкнул запрос раньше. Фикс: purge CDN на запись (или сократить/унифицировать TTL), раз два слоя кэшируют один ответ.", correct: true },
    { label: "MISS у reverse-proxy доказывает, что origin отдаёт устаревшие данные.", misconception: "MISS у proxy значит, что proxy пошёл вверх — но HIT у CDN перед ним замкнул запрос первым, поэтому ответ origin вообще не использовался. Устаревшая копия — CDN-ская, а не origin." },
    { label: "Age: 240 означает, что данные свежие, потому что меньше max-age=300.", misconception: "Нахождение внутри max-age — именно поэтому CDN всё ещё их отдаёт, и это и есть проблема. Запись «свежа» по TTL, но устарела по данным, потому что запись её не очистила. Свежесть по TTL — не свежесть данных." },
    { label: "CF-Cache-Status и X-Cache противоречат друг другу, поэтому заголовкам нельзя верить.", misconception: "Они описывают два разных слоя — HIT у CDN и MISS у reverse-proxy — и согласованы: запрос попал в CDN и не дошёл до upstream-а proxy. Чтение их пер-слойно — именно так находят лгущую копию." },
  ]}
/>

## Сниппет 3 — слоистый путь hit/miss

```
client → CDN edge → reverse proxy → app (Redis) → Postgres
         miss        miss            HIT (Redis)   —
```

<Quiz
  id="01-layers-code-3"
  lessonSlug="01-layers"
  lang="ru"
  question="Запрос промахивается мимо CDN и reverse proxy, но попадает в Redis на app-слое. Для in-region схемы какова грубая картина задержек и о чём она говорит?"
  choices={[
    { label: "Запрос заплатил два miss-хопа (CDN edge, reverse proxy), чтобы в итоге быть отданным из Redis (~1 мс) вместо Postgres — то есть app-слойный cache сэкономил round trip к БД, но два верхних кэша на этом пути дали лишь задержку. Если большая часть трафика выглядит так, слои CDN/proxy не окупают себя для этого эндпоинта и их стоит пересмотреть.", correct: true },
    { label: "HIT у Redis означает, что все слои над ним тоже отдали из кэша.", misconception: "Трейс показывает обратное: CDN и proxy оба промахнулись и переслали. Попадание глубоко в стеке значит, что каждый слой над ним добавил miss-хоп. Слои не делят состояние — каждый проверяется и пересылает дальше при miss." },
    { label: "Два miss-а значат, что cache сломан и его надо целиком убрать.", misconception: "Miss — нормальное поведение кэша, а не поломка: просто эти верхние слои не держат этот ключ. Сигнал в том, окупает ли hit ratio каждого слоя его существование для этого трафика, а не что кэширование провалилось." },
    { label: "Postgres был запрошен, поэтому Redis на деле не помог.", misconception: "Трейс заканчивается на HIT у Redis — Postgres (—) не запрашивался. Redis сэкономил round trip к БД на этом запросе; вопрос в том, тянут ли CDN и proxy над ним свой вес." },
  ]}
/>

## Сниппет 4 — stale-while-revalidate

```http
Cache-Control: public, max-age=60, stale-while-revalidate=600
```

<Quiz
  id="01-layers-code-4"
  lessonSlug="01-layers"
  lang="ru"
  question="Высоконагруженная лента, терпимая к лёгкому устареванию, переходит на эту директиву, и p99 резко падает без роста всплесков нагрузки на origin. Почему это работает?"
  choices={[
    { label: "Первые 60с запись свежа и отдаётся мгновенно. С 60с до 660с она stale-but-usable: cache отдаёт устаревшую копию сразу И ревалидирует в фоне, поэтому никто не ждёт origin, а origin видит ровный ручеёк ревалидаций вместо thundering herd на истечении. Идеально для read-heavy, терпимых к устареванию данных.", correct: true },
    { label: "Она отключает кэширование после 60с, форсируя свежие выборки.", misconception: "Наоборот — stale-while-revalidate продлевает пригодность на 600с сверх max-age, отдавая устаревшую копию при фоновом обновлении. Внутри этого окна ничто не вынуждено ждать свежую выборку." },
    { label: "Она делает данные всегда свежими, ревалидируя на каждом запросе.", misconception: "Она не ревалидирует на каждом запросе — она отдаёт устаревшую копию мгновенно и ревалидирует асинхронно. Данные намеренно слегка устаревшие; именно эта терпимость к устареванию и покупает выигрыш в задержке." },
    { label: "Она помогает только потому, что снижает max-age до 60.", misconception: "Один max-age=60 вызывал бы синхронную выборку из origin (и возможный herd) на каждом истечении. Выигрыш — это окно stale-while-revalidate, отвязывающее задержку пользователя от ревалидации, а не значение max-age." },
  ]}
/>

<Recap lang="ru">
Поведение кэша читается в заголовках и трейсах. Директивы Cache-Control (заголовки управления кэшированием) решают, какие слои могут хранить ответ — no-store запрещает всем, private запрещает общим кэшам вроде CDN — поэтому персонализированный эндпоинт корректно показывает 0% hit ratio у CDN. Пер-слойные trace-заголовки (CF-Cache-Status, X-Cache, Age) позволяют пройти стек и найти устаревшую копию: HIT у CDN внутри max-age, переживший запись, значит, что CDN не был очищен. Попадание глубоко в стеке значит, что каждый слой над ним добавил miss-хоп, поэтому проверь, окупает ли себя каждый слой. Теперь, когда придёт жалоба на устаревший кэш, первым делом читаешь trace-заголовки — а не тянешься к TTL.
</Recap>
