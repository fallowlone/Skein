---
concepts:
- cache-hierarchy
- access-latency
- hit-ratio
- double-caching
- cache-invalidation
- wrong-layer-caching
deepensInto: []
estMin: 16
lang: ru
lessonType: topic
level: junior
order: 1
prereqs: []
slug: 01-overview
sources:
- https://gist.github.com/jboner/2841832
- https://colin-scott.github.io/personal_website/research/interactive_latency.html
- https://redis.io/blog/why-your-cache-hit-ratio-strategy-needs-an-update/
- https://softbuilds.medium.com/caching-layers-client-cdn-reverse-proxy-app-db-who-owns-what-caff6e32b910
spiral: []
status: ready
summary: Каждый уровень — от кэша CPU до CDN — меняет latency на расстояние и стоимость.
  Кэширование не на том уровне, двойное кэширование или кэш перед быстрым источником
  делают систему хуже, а не быстрее.
title: 'Уровни кэширования: от L1 до CDN, и где не тот уровень кусается'
track: caching
unit: 01-layers
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import StackDiagram from "~/components/diagram/StackDiagram.astro";

<Hook>
Команда ставит Redis перед запросом в Postgres, чтобы «ускорить». Latency становится *хуже*. Запрос был выборкой по primary key из тёплого buffer pool — Postgres отвечал за ~0.3 мс из RAM. Новый путь сериализует ключ, делает сетевой round trip в Redis (~1 мс в том же регионе), десериализует и только потом, может быть, попадает. Они обернули источник со скоростью RAM в кэш со скоростью сети. Кэш стал тем самым узким местом, которое должен был убрать.
</Hook>

<Crux>Кэш перед более быстрым источником или на не том уровне делает каждый запрос медленнее, чем вообще без кэша.</Crux>

<Explanation>

## Иерархия — это лестница latency

Кэширование — это не одна вещь, а стек уровней, каждый из которых больше, дешевле и медленнее предыдущего. Стек существует ровно потому, что быстрая память маленькая и дорогая, а большая — медленная и дешёвая. Каждый уровень держит горячее подмножество уровня ниже, чтобы большинство обращений никогда не доходило до медленного слоя. Числа — это и есть вся суть: сеньор держит их в голове, потому что они решают, *помогает ли кэш вообще*.

Порядки величины важнее точных цифр. L1 примерно в 200 раз быстрее RAM; RAM примерно в 1000 раз быстрее SSD; SSD примерно в 100 раз быстрее сетевого хопа через континент. Добавляя уровень кэша, ты делаешь ставку, что фронтящийся слой *медленнее* кэша с большим запасом. Зафронтишь то, что и так быстрее, — и ты добавил latency, а не убрал.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Уровень</th>
        <th class="border border-border px-3 py-2 text-left">Типичный доступ</th>
        <th class="border border-border px-3 py-2 text-left">Размер / охват</th>
        <th class="border border-border px-3 py-2 text-left">Кому принадлежит</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">L1 кэш CPU</td>
        <td class="border border-border px-3 py-2">~1 нс</td>
        <td class="border border-border px-3 py-2">~32–64 КБ / ядро</td>
        <td class="border border-border px-3 py-2">Железо</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">L2 кэш CPU</td>
        <td class="border border-border px-3 py-2">~4 нс</td>
        <td class="border border-border px-3 py-2">~256 КБ–1 МБ / ядро</td>
        <td class="border border-border px-3 py-2">Железо</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">L3 кэш CPU</td>
        <td class="border border-border px-3 py-2">~10–40 нс</td>
        <td class="border border-border px-3 py-2">~8–64 МБ / сокет</td>
        <td class="border border-border px-3 py-2">Железо</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">RAM</td>
        <td class="border border-border px-3 py-2">~100 нс</td>
        <td class="border border-border px-3 py-2">ГБ / машина</td>
        <td class="border border-border px-3 py-2">ОС + твой процесс</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Page cache ОС</td>
        <td class="border border-border px-3 py-2">~100 нс (попадание в RAM)</td>
        <td class="border border-border px-3 py-2">свободная RAM</td>
        <td class="border border-border px-3 py-2">Ядро</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Кэш приложения (Redis/memcached)</td>
        <td class="border border-border px-3 py-2">~0.2–1 мс (в регионе)</td>
        <td class="border border-border px-3 py-2">ГБ–ТБ, общий</td>
        <td class="border border-border px-3 py-2">Ты</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">SSD</td>
        <td class="border border-border px-3 py-2">~100 мкс</td>
        <td class="border border-border px-3 py-2">ТБ / машина</td>
        <td class="border border-border px-3 py-2">ОС</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Reverse proxy / edge CDN</td>
        <td class="border border-border px-3 py-2">~мс (edge), 100+ мс (далеко)</td>
        <td class="border border-border px-3 py-2">глобально, на POP</td>
        <td class="border border-border px-3 py-2">Ты / провайдер</td>
      </tr>
    </tbody>
  </table>
</div>

## Для чего на самом деле каждый уровень

Аппаратные кэши (L1/L2/L3) и RAM — не твои в управлении: CPU и ОС крутят их автоматически. Но они всё равно формируют твой код: кэш-дружелюбный паттерн доступа (последовательный, с локальностью) может работать на порядок быстрее, чем погоня по указателям, потому что нужная cache line уже загружена. Поэтому плоский массив обходит связный список при итерации даже при одинаковом big-O.

**Page cache ОС** — самый недооценённый уровень. Когда ты читаешь файл, ядро держит его страницы в свободной RAM; второе чтение — это попадание в память, а не в диск. Поэтому твоя база быстра на горячих данных без всяких усилий с твоей стороны — buffer pool Postgres плюс page cache означают, что «дисковое» чтение обычно оказывается чтением из RAM. Сеньор знает это до того, как тянуться к Redis: если рабочий набор влезает в RAM, база *уже* является кэшем в RAM.

**Кэши приложения** (Redis, memcached) — первый уровень, которым ты управляешь сам. Это общее, доступное по сети хранилище в RAM: любой инстанс приложения может попасть в один и тот же кэш, чего локальная in-process память не умеет. Цена — сетевой round trip: обычно меньше миллисекунды в том же регионе, но 100+ мс, если клиент и кэш на разных континентах. **Reverse proxy и CDN** выталкивают закэшированные *ответы* ближе к пользователю, так что попадание вообще не доходит до твоего origin; вся ценность CDN географическая — отдать французскому пользователю из POP в Париже за миллисекунды вместо пересечения Атлантики.

<Inset kind="why" lang="ru">
«Просто добавь Redis» — рефлекс, который пропускает главный вопрос: какова реальная latency источника? Если данные уже лежат в памяти твоего процесса, или в page cache ОС, или в тёплом buffer pool базы, источник работает со скоростью RAM, и сетевой кэш может только замедлить. Redis заслуживает место, когда источник по-настоящему дорогой — агрегат с кучей джойнов, внешний API, скан холодного диска, — а не когда он фронтит выборку по горячему ключу.
</Inset>

## Hit ratio — единственное число, оправдывающее кэш

Кэш, который промахивается, платит *обе* цены: и lookup в кэше, *и* выборку из источника. Поэтому средняя latency кэшируемого пути — это `hit_rate × cache_latency + miss_rate × (cache_latency + origin_latency)`. Посчитай — и низкий hit ratio строго хуже, чем без кэша, потому что на каждом промахе ты платишь налог кэша впустую. Поэтому «повысить hit ratio» не всегда цель: кэширование холодных, редко переиспользуемых данных может поднять твою *общую* latency, вытеснив по-настоящему горячие ключи ради одноразовых.

Сеньорская формулировка: кэш оправдан только когда `origin_latency` велика, hit ratio высокий, и данные терпят небольшую несвежесть. Промахнёшься хоть по одному из трёх — и ты добавляешь движущиеся части, новый домен отказа и проблему инвалидации в обмен на ничто. Самые дорогие кэши в проде — те, у которых hit ratio 40%, а убрать их все боятся.

<TradeoffMatrix
  id="01-caching-layers-tradeoff-1"
  lessonSlug="01-overview"
  lang="ru"
  prompt="Read-эндпоинт делает джойн по 3 таблицам за 80 мс, его зовут 50 раз/сек, а данные меняются пару раз в день. Куда ставить кэш?"
  options={[
    { name: "Кэш приложения (Redis) с ключом по входам запроса, короткий TTL плюс инвалидация на запись", correct: true, summary: "Кэшировать дорогой результат.", justification: "Это хрестоматийный случай: источник по-настоящему медленный (80 мс), доступ горячий (50/сек → высокий hit ratio), а данные read-mostly, поэтому короткий TTL плюс инвалидация при записи держат их достаточно свежими. Round trip в Redis (~1 мс) на два порядка дешевле джойна, так что налог кэша ничтожен против выигрыша. Это ровно та форма, где сетевой кэш окупается." },
    { name: "Добавить RAM и довериться buffer pool базы / page cache ОС, чтобы держать горячим", summary: "Опереться на нижние уровни.", justification: "Правильный инстинкт для I/O-bound чтений, но здесь не помогает: 80 мс — это работа CPU (джойн и агрегация), а не дисковый I/O. Page cache делает строки быстрыми для чтения; он не может пропустить пересчёт джойна. Кэшировать всё равно надо вычисленный результат, а не входные страницы." },
    { name: "Кэшировать отрендеренный HTML на CDN с длинным TTL", summary: "Вытолкнуть на edge.", justification: "Не тот уровень для дата-эндпоинта: CDN кэширует ответы по URL, но это динамические данные, общие для персонализированных страниц, и длинный edge TTL означает, что изменение данных появится глобально только по истечении срока. CDN — для статических или почти статических ответов, а не для результата часто инвалидируемого запроса." },
    { name: "Без кэша — 80 мс норм, держим систему простой", summary: "Ничего не делать.", justification: "Защитимо, если бы трафик был мизерным, но 50 вызовов/сек по 80 мс — это реальная нагрузка и реальная хвостовая latency, а данные терпят несвежесть. Пропустить кэш здесь — обменять дешёвый, понятный выигрыш на лишнее давление на источник. Простота ценна, но не когда кэш ложится настолько чисто." },
  ]}
/>

## Сеньорский провал: не тот уровень и двойное кэширование

Опасные баги — не промахи кэша, а кэши, которые врут. **Двойное кэширование** — классика: значение кэшируется в Redis *и* ответ кэшируется на reverse proxy/CDN, у каждого свой TTL и своя инвалидация. Запись чистит Redis, но CDN ещё 5 минут отдаёт старый HTML по своему TTL — или наоборот, CDN очистили, а proxy перед ним продолжает отдавать свою устаревшую копию. Получается stale-on-stale: два независимых уровня рассинхронизированы, а `cf-cache-status` в DevTools показывает тебе только один из них. Дебаг — это обход запроса слой за слоем (`X-Cache`, `Cache-Status`, логи proxy, попадания в Redis), чтобы найти, какая копия врёт.

Провал **не того уровня** — это Hook: фронтить быстрый источник медленным кэшем. Он же проявляется как кэширование на уровне, который не видит событие инвалидации, — кэширование персонализированных данных на CDN (ключ по URL, пользователей не различает, поэтому течёт данные одного пользователя другому) или кэширование на edge данных, меняющихся на каждый запрос. Правило, которое сеньор усваивает: **кэшируй настолько близко к потребителю, насколько позволяет волатильность данных, и никогда не давай двум уровням кэшировать одно и то же с независимыми сроками жизни.** Один владелец на каждый закэшированный факт, один путь инвалидации.

<Quiz
  id="01-caching-layers-quiz-1"
  lessonSlug="01-overview"
  lang="ru"
  question="Ты оборачиваешь выборку Postgres по primary key (тёплая, ~0.3 мс из buffer pool) в Redis. Что будет с latency?"
  choices={[
    { label: "Станет хуже — сетевой round trip в Redis (~1 мс) медленнее источника со скоростью RAM (~0.3 мс), который ты фронтишь", correct: true },
    { label: "Станет быстрее, потому что Redis в памяти, а Postgres на диске", misconception: "Тёплая выборка по PK отдаётся из buffer pool / page cache — это уже попадание в RAM, а не чтение с диска. Redis добавляет сетевой хоп поверх источника, который и так был со скоростью RAM." },
    { label: "Останется тем же; и там, и там память", misconception: "«И там, и там память» игнорирует сеть. Локальная RAM buffer pool — ~100 нс; round trip в Redis — ~1 мс, примерно в 10 000 раз больше. Цена — это сеть." },
    { label: "Станет быстрее, потому что Redis разгружает базу", misconception: "Разгрузка важна под высокой нагрузкой, но вопрос про latency на запрос, а для одной тёплой выборки по PK путь через кэш строго медленнее." },
  ]}
/>

<Quiz
  id="01-caching-layers-quiz-2"
  lessonSlug="01-overview"
  lang="ru"
  question="Уровень кэша с hit ratio 35% фронтит источник на 80 мс, lookup в кэше — 1 мс. По сравнению с «без кэша» средняя latency:"
  choices={[
    { label: "Хуже на каждом промахе — промах платит lookup + источник (81 мс), поэтому низкий hit ratio добавляет налог почти без пользы", correct: true },
    { label: "Всегда лучше, ведь часть запросов теперь быстрая", misconception: "Попадания быстрее, но промахи теперь стоят cache_lookup + источник. При 35% попаданий налог промахов доминирует, и средняя latency может превысить 80 мс без кэша." },
    { label: "Одинаково, кэши никогда не замедляют", misconception: "Промах платит обе цены. Ниже точки безубыточности по hit ratio кэш в минус — ровно поэтому hit ratio и есть число, оправдывающее кэш." },
    { label: "Лучше, потому что нагрузка на базу падает", misconception: "Нагрузка и latency на запрос — разные оси. Вопрос про latency, где hit ratio 35% — слабое оправдание для налога кэша." },
  ]}
/>

<DragOrder
  id="01-caching-layers-drag-1"
  lessonSlug="01-overview"
  lang="ru"
  prompt="Расставь уровни кэша от самого быстрого доступа к самому медленному:"
  items={[
    { id: "a", label: "L1 кэш CPU (~1 нс)" },
    { id: "b", label: "L3 кэш CPU (~10–40 нс)" },
    { id: "c", label: "RAM / page cache ОС (~100 нс)" },
    { id: "d", label: "SSD (~100 мкс)" },
    { id: "e", label: "Кэш приложения по сети — Redis в регионе (~1 мс)" },
    { id: "f", label: "Edge CDN для далёкого пользователя (100+ мс)" },
  ]}
  correctOrder={["a", "b", "c", "d", "e", "f"]}
/>

</Explanation>

<StackDiagram
  label="Лестница latency кэшей: от L1 (~1 нс) вниз до дальнего CDN edge (100+ мс); каждая ступень примерно на порядок медленнее предыдущей."
  hue="var(--d-backend)"
  layers={[
    { label: "L1 кэш CPU", note: "~1 нс" },
    { label: "L3 кэш CPU", note: "~10–40 нс" },
    { label: "RAM / page cache ОС", note: "~100 нс" },
    { label: "SSD", note: "~100 мкс" },
    { label: "Кэш приложения · Redis (в регионе)", note: "~1 мс" },
    { label: "CDN edge · дальний пользователь", note: "100+ мс" },
  ]}
  caption="Сверху — самое быстрое. Каждая ступень примерно на порядки медленнее предыдущей — кэш убирает latency только когда стоит перед ступенью медленнее себя."
/>

<KeyTakeaway>Каждый уровень кэша меняет latency на расстояние и стоимость. Кэш помогает, только когда источник сильно медленнее, hit ratio высокий, а несвежесть терпима. Фронт быстрого источника или дубль факта — станет хуже.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="01-caching-layers-retrieval"
  lang="ru"
  questions={[
    { q: "Объясни, почему добавление кэша может ухудшить latency, опираясь на числа access-latency.", a: "Кэш убирает latency только если слой, который он фронтит, сильно медленнее самого кэша. Уровни образуют лестницу: L1 ~1 нс, RAM ~100 нс, SSD ~100 мкс, сетевой round trip в Redis ~1 мс в регионе, далёкий хоп CDN 100+ мс. Если твой источник — это тёплая выборка Postgres по primary key, она отдаётся из buffer pool или page cache ОС со скоростью RAM — примерно 0.3 мс или меньше. Обернёшь это в Redis — добавишь ~1 мс сетевого round trip на входе, так что даже попадание медленнее, чем просто спросить Postgres. Сверх того промах платит обе цены: lookup в кэше плюс полную выборку источника. Поэтому средняя latency — это hit_rate × cache_latency + miss_rate × (cache_latency + origin_latency). При низком hit ratio против быстрого источника эта средняя строго выше, чем без кэша. Кэш заслуживает место только когда origin_latency велика, hit ratio высокий, и данные терпят небольшую несвежесть." },
    { q: "Что такое двойное кэширование и почему оно порождает баги stale-on-stale, которые трудно дебажить?", a: "Двойное кэширование — это когда один факт кэшируется на двух независимых уровнях: например, значение в Redis и отрендеренный ответ на reverse proxy или CDN, у каждого свой TTL и свой путь инвалидации. Уровни не координируются. Запись может почистить Redis, но оставить CDN отдавать старый HTML до истечения его TTL, так что пользователи видят несвежие данные, хотя кэш приложения свежий. Или CDN очистили, а reverse proxy перед ним продолжает отдавать свою устаревшую копию — stale-on-stale, два уровня рассинхронизированы. Дебажить трудно, потому что каждый уровень отчитывается только о себе: DevTools показывает cf-cache-status для CDN, но молчит про Redis или proxy, а reverse proxy может молча переопределить заголовки origin (origin говорит no-cache, Varnish держит TTL 5 минут). Приходится обходить запрос слой за слоем — X-Cache, Cache-Status, логи proxy, попадание/промах Redis, потом базу, — чтобы найти, какая копия врёт. Фикс структурный: один владелец на каждый закэшированный факт и один путь инвалидации, никогда не давать двум уровням кэшировать одно и то же с независимыми сроками жизни." },
  ]}
/>

<Recap lang="ru">
Кэширование — это лестница уровней: L1/L2/L3, RAM, page cache ОС, кэши приложения вроде Redis, reverse proxy и CDN, каждый больше, дешевле и медленнее предыдущего. Числа latency — это вся суть: примерно 1 нс на L1, 100 нс на RAM, 100 мкс на SSD, ~1 мс на round trip в Redis в регионе, 100+ мс на далёкий хоп CDN, с порядками величины между ступенями. Кэш окупается только когда фронтит по-настоящему более медленный источник, hit ratio высокий, и данные терпят несвежесть — потому что промах платит и lookup в кэше, и выборку источника, так что низкий hit ratio хуже, чем без кэша. Сеньорские провалы — кэширование не на том уровне (фронтить источник со скоростью RAM кэшем со скоростью сети или кэшировать персонализированные данные на CDN с ключом по URL) и двойное кэширование, где два уровня держат один факт с независимыми TTL и расходятся в баги stale-on-stale, всплывающие, только когда обходишь запрос слой за слоем. Кэшируй настолько близко к потребителю, насколько позволяет волатильность данных, давай каждому закэшированному факту одного владельца и один путь инвалидации, и проверяй реальную latency источника прежде, чем тянуться к Redis.
</Recap>
