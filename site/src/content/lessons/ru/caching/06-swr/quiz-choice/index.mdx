---
concepts:
- stale-while-revalidate
- stale-if-error
- background-revalidation
- request-coalescing
- bounded-staleness
- swr-auth-hazard
estMin: 13
lang: ru
level: senior
order: 7
prereqs: []
slug: quiz-choice
sources:
- https://datatracker.ietf.org/doc/html/rfc5861
- https://swr.vercel.app/docs/api
- https://www.fastly.com/documentation/guides/concepts/edge-state/cache/stale/
status: ready
summary: "Тест с выбором на синтез по всему юниту SWR — два окна, single-flight, stale-if-error, ревалидация на клиенте и опасность для auth."
title: 'SWR: тест с выбором ответа'
track: caching
unit: 06-swr
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Шесть вопросов поперёк всего юнита. Каждый отражает заголовок, который ты пишешь, или инцидент, который разбираешь — не директиву для заучивания, а компромисс «свежесть против задержки», который надо взвесить под реальным трафиком.
</Hook>

<Goal lang="ru">
Убедись, что связываешь окно свежести, окно SWR, stale-if-error, single-flight и модель ревалидации на клиенте — и что знаешь единственное место, куда SWR ставить нельзя.
</Goal>

<Quiz
  id="06-swr-choice-1"
  lessonSlug="06-swr"
  lang="ru"
  question="Запрос приходит через 90 секунд после кэширования, с Cache-Control: max-age=60, stale-while-revalidate=300. Что получит пользователь и почему?"
  choices={[
    { label: "Устаревшее значение немедленно, и в фоне запускается обновление для последующих запросов — запись просрочена по max-age, но всё ещё внутри окна SWR, поэтому отдаётся как stale.", correct: true },
    { label: "Блокирующее ожидание, пока кэш ревалидирует с origin, затем свежее значение.", misconception: "Это поведение обычного max-age. Внутри окна SWR весь смысл в том, что пользователь НЕ блокируется — stale сейчас, обновление вне основного пути." },
    { label: "Жёсткий промах, потому что 90с больше 60с max-age.", misconception: "Жёстким промахом это становится только после max-age + окно SWR (60+300=360с). Между 60с и 360с запись stale-но-отдаваемая." },
    { label: "Свежие данные, потому что stale-while-revalidate ревалидирует синхронно перед ответом.", misconception: "«While» означает, что обновление идёт, пока stale-ответ уже летит к пользователю. Синхронную ревалидацию SWR как раз и убирает." },
  ]}
/>

<Quiz
  id="06-swr-choice-2"
  lessonSlug="06-swr"
  lang="ru"
  question="Почему один лишь max-age порождает пилу p99 на каждой границе TTL, и как stale-while-revalidate её сглаживает?"
  choices={[
    { label: "При max-age первый запрос после истечения блокируется на полном round-trip к origin, чтобы перезаполнить запись; при высоком rps много пользователей попадают на каждую границу и сэмплируются в p99. SWR отдаёт им stale мгновенно и обновляет в фоне, поэтому промах в основном пути — и его пик — исчезает.", correct: true },
    { label: "max-age медленный, потому что условная ревалидация идёт на каждом запросе; SWR кэширует ответ, чтобы её не было.", misconception: "max-age НЕ ревалидирует каждый запрос — он отдаёт из кэша до истечения. Пик возникает только на границе TTL, где перезаполнение блокирует живого пользователя. Именно это SWR выносит из пути запроса." },
    { label: "Пила — это джиттер origin; SWR сглаживает его усреднением нескольких ответов origin.", misconception: "Пик — не разброс origin, а синхронное перезаполнение, попавшее в запрос пользователя. SWR ничего не усредняет; он просто перестаёт заставлять человека ждать обновления." },
    { label: "SWR снижает пик, укорачивая max-age, чтобы каждый промах был дешевле.", misconception: "SWR не меняет max-age и стоимость промаха; он меняет, КТО платит за промах — фоновый fetch вместо живого пользователя. Round-trip к origin того же размера, просто никто на него не ждёт." },
  ]}
/>

<Quiz
  id="06-swr-choice-3"
  lessonSlug="06-swr"
  lang="ru"
  question="Популярный ключ истекает при 10k rps, и edge запускает по одной фоновой ревалидации на каждый stale-hit. Что ломается и какой фикс?"
  choices={[
    { label: "Стадо фоновых обновлений бьёт в origin одновременно — самонанесённый thundering herd. Фикс — single-flight: первый stale-запрос запускает ровно одну ревалидацию, все остальные получают stale, пока она не вернётся; плюс джиттер, чтобы ключи не ревалидировались синхронно.", correct: true },
    { label: "Ничего — фоновые обновления бесплатны, ведь пользователь не ждёт.", misconception: "Пользователь не ждёт, но origin всё равно получает каждое обновление. Тысячи параллельных фоновых fetch расплавят origin так же, как стадо в основном пути." },
    { label: "Stale-значение отдаётся вечно; фикс — убрать окно SWR.", misconception: "Удаление SWR возвращает пик синхронной ревалидации. Стадо — про коалесцирование параллельных обновлений, а не про окно устаревания." },
    { label: "Пользователи начинают получать 503; фикс — поднять max-age.", misconception: "Пользователи получают stale-но-быстрые ответы, а не ошибки. Подъём max-age не останавливает параллельные фоновые fetch, штурмующие origin." },
  ]}
/>

<Quiz
  id="06-swr-choice-4"
  lessonSlug="06-swr"
  lang="ru"
  question="Что stale-if-error добавляет поверх stale-while-revalidate, и почему пара — дефолт сеньора?"
  choices={[
    { label: "stale-if-error продолжает отдавать последнюю хорошую копию, когда origin возвращает 5xx или недоступен, в течение своего окна — инструмент пути отказа. SWR один покрывает счастливый путь, но всё равно вернёт пользователю ошибку, как только origin сломается и окно SWR истечёт, поэтому пара превращает аварию в degraded-but-up.", correct: true },
    { label: "Это одна и та же директива под двумя именами, так что нужна только одна.", misconception: "Они независимы и зеркальны: stale-while-revalidate — для здорового origin, на который не хочется заставлять пользователей ждать; stale-if-error — для больного origin, где stale лучше 503." },
    { label: "stale-if-error делает ревалидацию синхронной, чтобы ловить ошибки до ответа.", misconception: "Ни одна из директив не блокирует пользователя. stale-if-error просто меняет поведение, когда фоновое обновление ПРОВАЛИВАЕТСЯ — продолжать отдавать stale вместо проброса ошибки." },
    { label: "stale-if-error укорачивает окно SWR, чтобы снизить максимальное устаревание.", misconception: "Он добавляет отдельное, обычно более длинное, окно только для отказов. Он не трогает окно SWR и обычное устаревание — это пол под авариями." },
  ]}
/>

<Quiz
  id="06-swr-choice-5"
  lessonSlug="06-swr"
  lang="ru"
  question="Два компонента вызывают useSWR('/api/user') в одном тике рендера. Что делает клиентская библиотека SWR и что этим управляет?"
  choices={[
    { label: "Делает один запрос, и оба компонента получают тот же результат — request coalescing / single-flight. Повторные вызовы того же ключа внутри dedupingInterval (по умолчанию 2000мс) схлопываются на in-flight promise. React Query делает так же; его аналог staleTime по умолчанию 0.", correct: true },
    { label: "Запускает два запроса и берёт тот, что вернётся первым.", misconception: "Дублирующие вызовы одного ключа коалесцируются на один in-flight запрос внутри dedupingInterval — в этом весь смысл клиентского слоя кэша." },
    { label: "Блокирует второй компонент, пока первый запрос не разрешится, затем рефетчит.", misconception: "Нет ни блокировки, ни второго fetch. Оба компонента подписываются на единственный in-flight promise и получают тот же кэшированный результат." },
    { label: "Показывает спиннер обоим, потому что ключ ещё не запрашивался.", misconception: "Вопрос про дедупликацию параллельных вызовов, а не про UI первой загрузки. SWR коалесцирует их в один запрос независимо от того, видел ли он ключ раньше." },
  ]}
/>

<Quiz
  id="06-swr-choice-6"
  lessonSlug="06-swr"
  lang="ru"
  question="Для какого ответа stale-while-revalidate — неправильный инструмент и почему?"
  choices={[
    { label: "Решение об авторизации или проверка прав (или feature-флаг, гейтящий доступ). Отдать отозванному пользователю несколько-минутно устаревшее «у тебя ещё есть доступ» — это выпущенная регрессия безопасности; чувствительные к безопасности, по-пользовательские ответы требуют короткой синхронной валидируемой свежести или вообще не кэшируются.", correct: true },
    { label: "API списка товаров, где данные каталога могут быть на несколько минут устаревшими.", misconception: "Это классическое попадание SWR — ограниченное устаревание безвредно, а реальная цена — пила p99. Здесь SWR ровно то, что нужно." },
    { label: "Аналитическая плитка, обновляемая раз в несколько минут на дашборде.", misconception: "Слегка устаревшая аналитика — это нормально; SWR отдаёт её мгновенно и тихо обновляет в фоне. В ней нет угрозы корректности или безопасности." },
    { label: "Публичный пост блога или тело статьи.", misconception: "Почти статичный контент прекрасно терпит устаревание; SWR идеален. Неправильная цель — данные, где stale равно баг безопасности или корректности, как auth." },
  ]}
/>

<Recap lang="ru">
Сквозная линия — один контракт: верни то, что есть сейчас, подтяни то, что верно, следующим, и никогда не заставляй человека ждать обновления. max-age задаёт свежесть, окно SWR убивает пилу синхронной ревалидации, stale-if-error превращает аварию в degraded-but-up, single-flight плюс джиттер останавливают стадо фоновых обновлений, а клиентские библиотеки воспроизводят ту же модель через dedupingInterval и ревалидацию на focus/reconnect. Единственная жёсткая граница: никогда не применяй SWR к auth, правам и всему, где устаревшие данные — баг безопасности.
</Recap>
