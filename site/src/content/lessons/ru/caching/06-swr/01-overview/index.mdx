---
concepts:
- stale-while-revalidate
- cache-control-directives
- stale-if-error
- background-revalidation
- request-coalescing
- bounded-staleness
deepensInto: []
estMin: 16
lang: ru
lessonType: topic
level: junior
order: 1
prereqs: []
slug: 01-overview
sources:
- https://datatracker.ietf.org/doc/html/rfc5861
- https://developer.mozilla.org/en-US/docs/Web/HTTP/Reference/Headers/Cache-Control
- https://swr.vercel.app/docs/api
- https://www.fastly.com/documentation/guides/concepts/edge-state/cache/stale/
spiral: []
status: ready
summary: SWR разделяет свежесть и задержку на двух слоях — директива HTTP Cache-Control
  и клиентская модель загрузки данных. Ты меняешь ограниченную устарелость на стёртый
  пик p99 ревалидации, потому что ни один запрос не блокируется на origin.
title: 'Stale-while-revalidate: отдать устаревшее сразу, обновить в фоне'
track: caching
unit: 06-swr
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import SequenceDiagram from "~/components/diagram/SequenceDiagram.astro";
import Infographic from "~/components/diagram/Infographic.astro";
import cacheControlWindows from "./infographics/cache-control-windows.svg?raw";

<Hook>
У API цен стоит `max-age=60`. p50 latency — 8мс, красота на дашборде. Но каждые 60 секунд запись протухает, и следующий запрос блокируется на 400мс round-trip к origin, чтобы её обновить. На тысячах req/s этот «следующий запрос» — сотни пользователей, каждую минуту, попадающие прямо в твой p99. График — пила: ровно 8мс, потом всплеск 400мс точно на каждой границе TTL. Команда потратила спринт на погоню за «медленным origin», который никогда не был медленным — он просто синхронно ревалидировался в горячем пути живого пользователя.
</Hook>

<Crux>Отдай устаревшее значение мгновенно, обнови его в фоне — чтобы ревалидация не попадала в запрос пользователя, только её результат.</Crux>

<Explanation>

## Пила — это цена синхронной ревалидации

У обычного TTL-кэширования есть одно жестокое свойство: в момент протухания записи *кто-то* платит полную цену origin за её перезаполнение. С одним `max-age` этот кто-то — живой пользователь: невезучий запрос, пришедший первым после протухания, блокируется, пока origin не ответит. Твоя установившаяся latency — это время попадания в кэш; твоя tail latency — время промаха; а TTL гарантирует, что ты периодически сэмплируешь промах в реальный трафик. Чем выше частота запросов, тем больше пользователей попадает на каждую границу, тем толще твой p99.

Stale-while-revalidate разрывает связь. Когда запись пересекает окно свежести, но всё ещё внутри окна SWR, кэш делает две вещи разом: возвращает **устаревшее** значение мгновенно (пользователь получает latency попадания в кэш, ~8мс) и запускает **асинхронное** обновление к origin. Пользователь, который спровоцировал обновление, никогда его не ждёт. Свежее значение прилетает на несколько сотен миллисекунд позже и обслуживает *следующие* запросы. Ревалидация всё так же происходит — она просто перестала происходить на переднем плане.

Сделка, которую ты заключаешь, явная и ограниченная: **ты соглашаешься, что часть ответов слегка устаревшая, в обмен на удаление пика p99.** Устарелость ограничена окном SWR; latency ограничена временем попадания в кэш. Сеньор читает `stale-while-revalidate=N` как «меня устраивают данные до N секунд за дедлайном свежести, пока ни один человек не ждёт обновление».

## RFC 5861: два окна, затем stale-if-error как пол

Это настоящая HTTP-директива, стандартизированная в RFC 5861, и она работает в CDN *и* в браузерах. Важны два расширения `Cache-Control`:

- `stale-while-revalidate=N` — в течение N секунд после протухания записи отдавай её мгновенно и ревалидируй в фоне.
- `stale-if-error=N` — в течение N секунд после протухания, если origin вернул ошибку (500/502/503/504) или недоступен, продолжай отдавать устаревшую копию вместо проброса ошибки.

Боевой заголовок читается как эшелонированная защита:

```
Cache-Control: max-age=60, stale-while-revalidate=300, stale-if-error=86400
```

Читай его как три вложенных окна. **0–60с:** свежо, отдаётся напрямую. **60–360с:** устарело, отдаётся мгновенно, пока в фоне идёт обновление (окно SWR). **при сбое origin, до 24ч:** всё ещё отдаётся из последней хорошей копии, а не бросает 503 в пользователя (пол SIE). Первое окно оптимизирует свежесть, второе убивает tail latency, третье превращает аварию в «деградировало, но живо».

<Infographic
  svg={cacheControlWindows}
  label="Таймлайн одной записи кэша под max-age=60, stale-while-revalidate=300, stale-if-error=86400: свежо с 0 до 60 секунд, отдаётся устаревшим с фоновым обновлением с 60 до 360 секунд, отдаётся из последней хорошей копии до 86400 секунд при ошибке origin, затем жёсткий промах за пределами всех окон."
  caption="Каждая директива открывает одно вложенное окно: max-age бережёт свежесть, окно SWR убивает tail latency, stale-if-error переживает аварию — за всеми тремя кэш обязан упасть громко."
  hue="var(--d-infra)"
/>

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Стратегия</th>
        <th class="border border-border px-3 py-2 text-left">Кто платит round-trip к origin</th>
        <th class="border border-border px-3 py-2 text-left">Макс. устарелость</th>
        <th class="border border-border px-3 py-2 text-left">p99 на границе TTL</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">только <code>max-age=60</code></td>
        <td class="border border-border px-3 py-2">Живой пользователь, каждые 60с</td>
        <td class="border border-border px-3 py-2">0 (всегда свежо)</td>
        <td class="border border-border px-3 py-2">Всплеск до latency origin</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2"><code>max-age=60, swr=300</code></td>
        <td class="border border-border px-3 py-2">Фоновый фетч — без пользователя</td>
        <td class="border border-border px-3 py-2">До 60+300с</td>
        <td class="border border-border px-3 py-2">Ровно (нет промаха на переднем плане)</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">SWR + <code>stale-if-error</code></td>
        <td class="border border-border px-3 py-2">Фон; никто во время аварии</td>
        <td class="border border-border px-3 py-2">До окна SIE</td>
        <td class="border border-border px-3 py-2">Ровно, переживает падение origin</td>
      </tr>
    </tbody>
  </table>
</div>

<Inset kind="why" lang="ru">
Имена — зеркальные. `stale-while-revalidate` — инструмент *счастливого пути*: origin в порядке, ты просто не хочешь, чтобы пользователи ждали обновление. `stale-if-error` — инструмент *пути сбоя*: origin болен, и устаревшее лучше, чем 503. Они независимы: можно выкатить один без другого, но их парность — дефолт сеньора, потому что один SWR всё равно отдаст пользователю ошибку, как только origin сломается и окно SWR истечёт.
</Inset>

## Та же идея на слой выше: SWR/React Query на клиенте

HTTP-директива живёт в сети. Ровно тот же паттерн живёт в клиентских библиотеках загрузки данных — библиотека `SWR` буквально названа в его честь, а React Query (TanStack Query) реализует ту же модель. Когда компонент просит ключ, библиотека возвращает **закэшированное значение из последнего фетча мгновенно** (без спиннера, если ключ уже видела), затем запускает фоновый запрос и подменяет на свежие данные, когда они придут. Пользователь видит мгновенный контент, который тихо сам себя исправляет.

Клиентская модель добавляет два боевых поведения, которых у голого заголовка нет:

- **Коалесинг запросов / single-flight.** Если два компонента вызывают `useSWR("/api/user")` в один тик, SWR делает **один** запрос, и оба получают один результат. Дефолтный `dedupingInterval` — 2000мс: повторные вызовы того же ключа в пределах 2с схлопываются на in-flight промис. React Query делает то же; его аналогичный `staleTime` по умолчанию 0 (рефетч жадно) против 2с у SWR.
- **Триггеры ревалидации.** Обе библиотеки по умолчанию ревалидируют при фокусе окна и при восстановлении сети, так что вкладка, оставленная открытой на час, показывает свежие данные в момент, когда ты в неё кликаешь — снова отдано stale-first, обновлено в фоне.

Будь то edge-нода CDN или React-хук, контракт идентичен: *верни то, что есть сейчас, забери то, что верно, следующим, никогда не блокируй человека на обновлении.*

<TradeoffMatrix
  id="06-swr-tradeoff-1"
  lessonSlug="01-overview"
  lang="ru"
  prompt="API листинга товаров за CDN видит классическую поминутную пилу p99 от синхронной ревалидации. Данные каталога могут быть устаревшими на несколько минут без вреда. Выбери заголовок."
  options={[
    { name: "max-age=60, stale-while-revalidate=300, stale-if-error=86400", summary: "Слоёные окна.", correct: true, justification: "Это учебниковое попадание. Окно свежести в 60с держит обычную устарелость крошечной; окно SWR в 300с означает, что как только запись протухает, edge отдаёт её мгновенно и обновляет в фоне, так что ни один живой запрос не съедает round-trip к origin — пила p99 выравнивается. Пол stale-if-error в 24ч превращает аварию origin в «деградировало, но живо» вместо стены из 503. Ограниченная устарелость (несколько минут) — ровно то, что условие разрешает." },
    { name: "max-age=600 — просто удлинить TTL, чтобы всплеск случался реже", summary: "Больший TTL.", justification: "Это сдвигает всплеск с раза в минуту на раз в десять минут, но не убирает его: живой пользователь всё равно блокируется на ревалидации на каждой границе, так что у твоего p99 всё ещё есть зубы. Это также ухудшает худшую устарелость (10 мин), так и не разделив свежесть и latency. SWR здесь строго лучше — та же или меньшая устарелость, ноль промахов на переднем плане." },
    { name: "no-cache — ревалидировать с origin на каждом запросе", summary: "Всегда проверять.", justification: "Это форсирует условный запрос к origin на каждое попадание, возвращая ровно ту round-trip latency, которую SWR существует, чтобы прятать, на каждом запросе, а не раз в TTL. Это гарантирует свежесть, которая, как условие явно говорит, не нужна, и платит за неё максимально возможной latency." },
    { name: "max-age=300, без SWR, без stale-if-error", summary: "Обычный TTL.", justification: "Всё ещё кэш с синхронной ревалидацией: каждые 5 минут живой пользователь платит round-trip к origin, и в момент ошибки origin пользователь получает ошибку. Ты сохраняешь пилу и не имеешь защиты от аварии. Добавить две директивы stale-* — в этом весь смысл." },
  ]}
/>

## Где это кусается: стампиды, неограниченная устарелость и auth

SWR не свободен от режимов отказа — он их перемещает. Три кусаются в проде. Когда выкатываешь заголовок stale-while-revalidate (директива RFC 5861, позволяющая кэшу отдавать устаревшую копию и обновляться в фоне), именно в этих трёх местах всё в итоге ломается.

**Стампид фоновых обновлений.** Наивный edge запускает одну фоновую ревалидацию *на каждый запрос*, который нашёл запись устаревшей. Если популярный ключ протухает, пока он принимает 10k req/s, ты можешь запустить тысячи одновременных обновлений к origin, который ожидал одно, — самонанесённый thundering herd, только теперь это фоновые фетчи плавят origin, а не пользователи видят latency. Фикс — single-flight: первый устаревший запрос запускает ровно одно обновление (Cloudflare помечает его `UPDATING`), а всех остальных обслуживают устаревшим, пока оно не прилетит. Добавь jitter к таймингу обновления, чтобы много ключей не протухали и не ревалидировались синхронно.

**Неограниченная устарелость, когда origin лежит.** Это классическая ошибка `stale-if-error` наоборот. Если origin недоступен, фоновая ревалидация *падает* — и плохо построенный кэш может тогда продлевать устаревшую копию вечно, «кэш до тепловой смерти». Нужен жёсткий потолок: конечное окно SWR, конечное окно stale-if-error. За их пределами кэш обязан упасть громко, а не отдавать контент неизвестного возраста. Устарелость всегда должна быть *ограниченной*.

**Отдача устаревших auth и прав.** Вот этот — опасный. SWR корректен для контента, которому нормально быть на несколько минут старее — список товаров, пост в блоге, плитка аналитики. Он *неверен* для решений авторизации, фича-флагов, гейтящих доступ, или всего, где устаревшее = регрессия безопасности. Отдай отозванному пользователю устаревшее на 5 минут «у тебя всё ещё есть доступ» — и ты выкатил уязвимость. Правило сеньора: никогда не SWR-ить проверку прав. Персональные, чувствительные к безопасности ответы получают короткую, синхронную, валидированную свежесть — или не кэшируются вовсе.

<Quiz
  id="06-swr-quiz-1"
  lessonSlug="01-overview"
  lang="ru"
  question="С `Cache-Control: max-age=60, stale-while-revalidate=300` запрос приходит через 90 секунд после кэширования записи. Что получит пользователь?"
  choices={[
    { label: "Устаревшее закэшированное значение мгновенно, плюс запускается фоновое обновление для следующих запросов", correct: true },
    { label: "Блокирующее ожидание, пока кэш ревалидируется с origin, затем свежее значение", misconception: "Это поведение обычного max-age. Внутри окна SWR весь смысл в том, что пользователь НЕ блокируется — он получает устаревшее мгновенно, пока обновление идёт вне полосы." },
    { label: "504, потому что запись прошла свой 60-секундный max-age", misconception: "За пределами max-age, но внутри окна SWR в 300с запись пригодна к отдаче как устаревшая; жёстким промахом она становится только после 60+300с." },
    { label: "Свежие данные, потому что stale-while-revalidate ревалидируется синхронно перед ответом", misconception: "Синхронная ревалидация — это ровно то, что SWR убирает. «While» означает, что обновление идёт, ПОКА устаревший ответ уже на пути к пользователю." },
  ]}
/>

<Quiz
  id="06-swr-quiz-2"
  lessonSlug="01-overview"
  lang="ru"
  question="Популярный ключ протухает под тяжёлым трафиком, и твой edge запускает одну фоновую ревалидацию на каждое устаревшее попадание. В чём боевой риск и фикс?"
  choices={[
    { label: "Thundering herd фоновых обновлений бьёт по origin разом; чини это single-flight коалесингом, чтобы на ключ шло только одно обновление", correct: true },
    { label: "Ничего — фоновые обновления бесплатны, потому что пользователь не ждёт", misconception: "Пользователь не ждёт, но origin всё равно получает каждое из этих обновлений. Тысячи одновременных фоновых фетчей могут расплавить origin так же, как стампид на переднем плане." },
    { label: "Устаревшее значение отдаётся вечно; чини это удалением окна SWR", misconception: "Удаление SWR возвращает всплеск синхронной ревалидации. Проблема стада — про коалесинг одновременных обновлений, а не про окно устарелости." },
    { label: "Пользователи получают ошибки; чини это повышением max-age", misconception: "Пользователи получают устаревшие-но-быстрые ответы, а не ошибки. Повышение max-age не решает одновременные фоновые фетчи, штурмующие origin." },
  ]}
/>

<DragOrder
  id="06-swr-drag-1"
  lessonSlug="01-overview"
  lang="ru"
  prompt="Расставь, что происходит с запросом, пришедшим во время окна stale-while-revalidate:"
  items={[
    { id: "a", label: "Кэш обнаруживает, что запись за пределами max-age, но всё ещё внутри окна SWR" },
    { id: "b", label: "Он возвращает устаревшее закэшированное значение пользователю мгновенно — без ожидания origin" },
    { id: "c", label: "Он запускает ровно одну фоновую ревалидацию к origin (single-flight)" },
    { id: "d", label: "Свежий ответ приходит и заменяет закэшированную запись вне полосы" },
    { id: "e", label: "Последующие запросы получают свежее значение (или снова устаревшее, если окно истекло)" },
  ]}
  correctOrder={["a", "b", "c", "d", "e"]}
/>

</Explanation>

<SequenceDiagram
  label="Stale-while-revalidate: кэш мгновенно отдаёт пользователю устаревшее значение, затем обновляется из origin в фоне, чтобы свежая копия обслужила следующий запрос"
  hue="var(--d-backend)"
  actors={["Пользователь", "Кэш", "Origin"]}
  messages={[
    { from: "Пользователь", to: "Кэш", label: "GET (запись устарела)" },
    { from: "Кэш", to: "Пользователь", label: "устаревшее — мгновенно, без ожидания" },
    { from: "Кэш", to: "Origin", label: "фоновое обновление (single-flight)" },
    { from: "Origin", to: "Кэш", label: "свежее значение, сохранено" },
    { from: "Пользователь", to: "Кэш", label: "следующий GET → свежее значение" },
  ]}
  caption="Пользователь, запустивший обновление, никогда не блокируется на origin; фоновый запрос приходит вне горячего пути и обслуживает следующий запрос."
/>

<KeyTakeaway>SWR отдаёт устаревшее мгновенно и ревалидирует в фоне, меняя ограниченную устарелость на стёртый пик p99. Делай обновление single-flight, ограничь устарелость жёстким окном и никогда не SWR-ь auth.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="06-swr-retrieval"
  lang="ru"
  questions={[
    { q: "Объясни, почему один max-age порождает пилу p99 и как добавление stale-while-revalidate её выравнивает.", a: "С одним max-age запись либо свежая (отдаётся из кэша, быстро), либо протухшая (следующий запрос обязан заблокироваться на полном round-trip к origin, чтобы её перезаполнить). Поскольку TTL гарантирует периодическое протухание, ты периодически заставляешь живого пользователя платить latency промаха кэша, а на высокой частоте запросов много пользователей попадают на каждую границу разом — эти заблокированные запросы сэмплируются прямо в твой p99, порождая пилу, которая всплескивает до latency origin на каждой границе TTL. stale-while-revalidate=N добавляет второе окно после протухания, в течение которого кэш возвращает устаревшее значение мгновенно И запускает обновление асинхронно. Пользователь, спровоцировавший обновление, обслуживается мгновенно из устаревшей копии и никогда не ждёт; свежее значение прилетает на сотни миллисекунд позже и обслуживает следующие запросы. Ревалидация всё так же происходит, она просто ушла из пути запроса пользователя, так что промах на переднем плане — и порождённый им всплеск — исчезает. Ты меняешь ограниченную устарелость (до N секунд за свежестью) на ровную tail latency." },
    { q: "Когда stale-while-revalidate — неверный инструмент, и от каких режимов отказа сеньор обязан защититься?", a: "SWR корректен только когда слегка устаревшие данные безвредны: каталоги, статьи, плитки аналитики. Он неверен для авторизации, проверок прав, фича-флагов, гейтящих доступ, или всего, где устаревшее равно регрессии безопасности — отдать отозванному пользователю устаревшее на несколько минут «у тебя всё ещё есть доступ» это выкаченная уязвимость, поэтому персональные чувствительные к безопасности ответы получают короткую синхронную валидированную свежесть или не кэшируются. Даже на безопасном контенте кусаются два режима отказа. Первый — стампид фоновых обновлений: наивный кэш запускает одно обновление на каждое устаревшее попадание, так что горячий ключ под нагрузкой запускает тысячи одновременных фетчей к origin — самонанесённый thundering herd. Чини single-flight коалесингом (одно обновление на ключ, всех остальных обслуживают устаревшим, пока оно не прилетит) плюс jitter, чтобы ключи не ревалидировались синхронно. Второй — неограниченная устарелость, когда origin лежит: фоновая ревалидация падает, а небрежный кэш продлевает устаревшую копию вечно. Устарелость всегда должна быть ограничена конечными окнами SWR и stale-if-error; за их пределами кэш падает громко, а не отдаёт контент неизвестного возраста." },
  ]}
/>

<Recap lang="ru">
Stale-while-revalidate разделяет свежесть и задержку: вместо того чтобы давать живому пользователю платить round-trip к origin каждый раз, когда истекает TTL, кэш возвращает устаревшее значение мгновенно и ревалидирует в фоне, так что пила p99 синхронной ревалидации выравнивается. Это живёт на двух слоях — директива HTTP `Cache-Control: stale-while-revalidate=N` (RFC 5861, уважаемая CDN и браузерами) и клиентские библиотеки вроде SWR и React Query, которые возвращают последние известные данные мгновенно, коалесят дублирующие запросы через dedupingInterval и ревалидируют при фокусе и реконнекте. Сделка явная: ограниченная устарелость за стёртую tail latency. Сопрягай её с `stale-if-error`, чтобы авария origin деградировала мягко, а не бросала 503. Затем держи в уме три режима отказа: делай фоновое обновление single-flight, иначе горячий ключ запустит thundering herd; ограничивай устарелость конечными окнами, иначе лежащий origin будет вечно отдавать контент неизвестного возраста; и никогда не применяй SWR к auth, правам или всему, где устаревшие данные — это баг безопасности. Теперь, когда видишь пилу на графике p99, первое, что проверяешь — заставляет ли кэш живого пользователя ждать каждую ревалидацию, и можно ли одной директивой перенести эту цену в фон.
</Recap>
