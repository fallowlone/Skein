infographic compare-quadrant-quarter-simple-card
data
  title На границе TTL: ожидание vs нагрузка на БД
  desc Две оси — ожидание пользователя (ноль / ненулевое) и запросы к БД (один / N параллельных)
  compares
    - label SWR/XFetch — без ожидания, один запрос
    - label Наивный TTL — N запросов кладут БД
    - label Лок — один запрос, очередь ждёт
    - label Худший случай — ожидание, БД без защиты
theme
  palette #10b981 #f59e0b #6366f1 #f43f5e
