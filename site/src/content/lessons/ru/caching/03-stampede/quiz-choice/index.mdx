---
concepts:
- cache-stampede
- single-flight
- distributed-lock
- xfetch
- stale-while-revalidate
- metastable-failure
estMin: 13
lang: ru
level: senior
order: 7
prereqs: []
slug: quiz-choice
sources:
- https://cseweb.ucsd.edu/~avattani/papers/cache_stampede.pdf
- https://www.usenix.org/conference/nsdi13/technical-sessions/presentation/nishtala
- https://datatracker.ietf.org/doc/html/rfc5861
status: ready
summary: "Синтез всего юнита в формате выбора: форма всплеска, область действия lock против single-flight, XFetch, SWR, negative caching и metastable failure."
title: 'Cache stampede: тест с выбором ответа'
track: caching
unit: 03-stampede
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Шесть вопросов через весь юнит. Ни один из них не про определение — каждый отражает решение, которое вы принимаете прямо во время инцидента, когда БД горит, а вам нужно выбрать митигацию, область действия которой действительно совпадает с формой стада.
</Hook>

<Goal lang="ru">
Убедитесь, что вы связываете форму всплеска stampede с правильной митигацией: что сворачивает стадо внутри процесса, что — стадо всего флота, что не требует координации, что убирает ожидание, и какой режим отказа не победить одним только слоем кеша.
</Goal>

<Quiz
  id="03-stampede-choice-1"
  lessonSlug="03-stampede"
  lang="ru"
  question="Главная страница закеширована с TTL=60 с и поглощает 5 000 RPS. Тот же эндпоинт без кеша тоже видит 5 000 RPS. Почему версия с кешем способна вызвать аварию, которой никогда не будет без кеша?"
  choices={[
    { label: "Кеш превращает ровный трафик во всплеск: ~0 RPS в течение 59 с, а затем все 5 000 RPS бьют по БД в односекундном окне после истечения. БД рассчитывали на steady-state за кешем, а не на этот пик.", correct: true },
    { label: "Версия с кешем делает больше работы в БД, потому что каждая запись ещё и обновляет кеш.", misconception: "Суммарной работы в БД с кешем гораздо меньше — в этом и смысл. Отказ вызывает всплеск при истечении, а не объём." },
    { label: "Версия без кеша медленнее на запрос, поэтому она лучше размазывает нагрузку.", misconception: "Латентность кеша на запрос здесь ни при чём. Авария возникает из-за пиковой конкуренции на границе TTL, равной полной скорости трафика." },
    { label: "Промахи кеша — более дорогие запросы, чем обычные чтения.", misconception: "Запрос rebuild — тот же запрос. Урон наносят N таких запросов, выстреливших одновременно при истечении, а не дороговизна одного." },
  ]}
/>

<Quiz
  id="03-stampede-choice-2"
  lessonSlug="03-stampede"
  lang="ru"
  question="Флот из 50 нод использует только in-process single-flight. На границе TTL приходят 100 000 одновременных промахов, равномерно распределённых. Сколько rebuild-запросов достигнет БД и почему?"
  choices={[
    { label: "Около 50 — single-flight работает на уровне процесса, так что каждая нода сворачивает свои ~2 000 промахов в один rebuild, но 50 независимых in-process карт не могут координироваться между нодами.", correct: true },
    { label: "1 — single-flight сворачивает всё в единственный rebuild на весь флот.", misconception: "У single-flight нет межнодовой видимости; его карта живёт в памяти одного процесса. Чтобы свернуть 50 в 1, сверху нужен distributed lock." },
    { label: "100 000 — single-flight дедуплицирует только чтения из кеша, а не rebuild'ы.", misconception: "Single-flight — это именно коалесцер rebuild'ов; здесь он срабатывает, просто в пределах одной ноды." },
    { label: "Зависит от того, как быстро отвечает БД.", misconception: "Скорость БД не меняет счёт. Его задаёт область координации — на уровне процесса — фиксируя один rebuild на ноду." },
  ]}
/>

<Quiz
  id="03-stampede-choice-3"
  lessonSlug="03-stampede"
  lang="ru"
  question="Нужно защитить два ключа: сверхгорячую главную, читаемую тысячи раз в секунду, и холодный персональный отчёт, читаемый раз в ~30 с с TTL=60 с. Какая митигация подходит какому ключу?"
  choices={[
    { label: "XFetch на горячем ключе (частые чтения гарантируют, что какой-то читатель сработает на пороге раннего обновления) и lock или SWR на холодном (редкие чтения могут пропустить узкое окно XFetch, ключ истечёт и устроит stampede).", correct: true },
    { label: "XFetch на обоих — он coordination-free, значит всегда выигрывает.", misconception: "XFetch нужны частые чтения у истечения. Ключ, читаемый раз в 30 с, часто имеет ноль чтений в финальном окне, истекает штатно и устраивает stampede." },
    { label: "Distributed lock на обоих — локи универсальны.", misconception: "Lock работает на холодном ключе, но на сверхгорячем он добавляет round-trip к Redis на каждый промах и ставит ждущих в очередь. XFetch обновляет горячий ключ бесплатно и без ждущих." },
    { label: "Ни один не нуждается в защите — один слишком горячий, другой слишком холодный, чтобы устроить stampede.", misconception: "Горячий ключ — каноническая цель stampede; холодный тоже заливает БД, если много запросов столкнутся при его истечении. Обоим нужна митигация под их частоту чтения." },
  ]}
/>

<Quiz
  id="03-stampede-choice-4"
  lessonSlug="03-stampede"
  lang="ru"
  question="Кеш на основе lock и кеш со stale-while-revalidate оба сводят нагрузку на БД на границе TTL к одному rebuild. В чём ключевая разница, которую чувствует пользователь?"
  choices={[
    { label: "С lock все ждущие, кроме держателя, блокируются (или получают fallback) до конца rebuild; со SWR каждый запрос мгновенно получает устаревшее значение, а ровно одно фоновое обновление выполняется — нулевое ожидание пользователя.", correct: true },
    { label: "SWR отправляет в БД больше запросов, чем lock.", misconception: "Оба ограничивают границу до ~1 rebuild-запроса. Разница в латентности: SWR никогда не заставляет запрос ждать, а lock может." },
    { label: "Lock отдаёт устаревшие данные, а SWR — свежие.", misconception: "Всё наоборот — SWR сознательно отдаёт ограниченно-устаревшие данные во время обновления; держатель lock пересобирает свежее значение прежде чем отдать." },
    { label: "Видимой пользователю разницы нет; они взаимозаменяемы.", misconception: "Они различаются по времени ожидания и устареванию. SWR меняет ограниченное устаревание на нулевое ожидание; lock меняет ожидание на немедленную свежесть после rebuild." },
  ]}
/>

<Quiz
  id="03-stampede-choice-5"
  lessonSlug="03-stampede"
  lang="ru"
  question="Сервис профилей кеширует лукапы на 5 минут. Атакующий шлёт 100 000 запросов/с по случайным несуществующим user ID. Защита позитивных ключей (lock + single-flight) ничего не делает. Почему и как чинить?"
  choices={[
    { label: "Каждый запрос — отдельный промах по ключу, которого никогда не было, так что single-flight и lock — дедуплицирующие одновременные rebuild'ы ОДНОГО ключа — не включаются вовсе. Фикс: negative caching, sentinel с коротким TTL для 'нет такой строки'.", correct: true },
    { label: "У lock слишком короткий EX, поэтому он постоянно истекает.", misconception: "Настройка EX тут ни при чём: нет общего ключа, на котором лочиться. Каждый случайный ID уникален, и коалесцеру нечего сворачивать." },
    { label: "Атакующий читает кешированные данные профилей, которых не должен видеть.", misconception: "Никакие данные не утекают — атакующий лишь генерирует промахи. Угроза — перегрузка БД некешируемыми негативными лукапами." },
    { label: "Кеш вытесняет реальные профили, поэтому легитимные чтения тоже промахиваются.", misconception: "Загрязнение вытеснением возможно, но немедленный отказ — это 100 000 QPS негативных лукапов по БД. Negative caching останавливает их на уровне кеша." },
  ]}
/>

<Quiz
  id="03-stampede-choice-6"
  lessonSlug="03-stampede"
  lang="ru"
  question="Stampede длиной 10 секунд закончился четыре часа назад, а БД всё ещё прибита к 100% CPU с пустым кешем. Почему система не восстановится сама и что нужно для восстановления?"
  choices={[
    { label: "Это metastable failure: ретраи клиентов после таймаутов держат БД насыщенной, насыщенная БД не может ответить на rebuild-запросы, пустой кеш генерирует новые промахи и ретраи. Нужен внешний kill-сигнал — 503-on-overload, circuit-breaker или ручной засев кеша.", correct: true },
    { label: "Исходное стадо всё ещё в полёте спустя четыре часа.", misconception: "Исходный stampede длился секунды. Длительную нагрузку создаёт вызванный им retry storm, а не первоначальное стадо." },
    { label: "Перезапустить кеш, чтобы сбросить плохое состояние.", misconception: "Перезапуск вхолодную делает только хуже — 100% промахов. Цикл рвут shedding нагрузки или засевом кеша, а не дальнейшим опустошением." },
    { label: "Добавить CPU в БД, и она самовосстановится.", misconception: "Больше CPU поднимает порог, но retry storm масштабируется с трафиком и снова насыщает. Цикл рвётся остановкой ретраев (503/backoff) или обходом пути rebuild, а не ёмкостью." },
  ]}
/>

<Recap lang="ru">
Сквозная линия юнита — это упражнение на сопоставление: форма всплеска stampede (полная скорость трафика, сконцентрированная в один момент истечения) определяет, какая митигация подходит. Single-flight бесплатно сворачивает стадо внутри процесса; distributed lock сворачивает стадо всего флота ценой round-trip; XFetch обновляет горячие ключи до истечения без координации; SWR отдаёт устаревшее и обновляет в фоне ради нулевого ожидания; negative caching останавливает усиление miss-storm; а как только stampede опрокидывает систему в metastable failure на ретраях, ни один слой кеша в одиночку её не вернёт — только внешний kill-сигнал.
</Recap>
