---
concepts:
- cache-stampede
- single-flight
- xfetch
- stale-while-revalidate
- negative-caching
- metastable-failure
estMin: 13
lang: ru
level: senior
order: 8
prereqs: []
slug: quiz-short
sources:
- https://cseweb.ucsd.edu/~avattani/papers/cache_stampede.pdf
- https://datatracker.ietf.org/doc/html/rfc5861
- https://www.usenix.org/conference/nsdi13/technical-sessions/presentation/nishtala
status: ready
summary: "Промпты на свободное воспроизведение по всему юниту. Сначала ответьте своими словами, затем раскройте модельный ответ и сравните."
title: 'Cache stampede: свободное воспроизведение'
track: caching
unit: 03-stampede
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";

<Hook>
Воспроизведение по памяти бьёт перечитывание. Для каждого промпта проговорите или запишите полный ответ из памяти, прежде чем открыть модельный — именно усилие припоминания закрепляет механизмы к моменту, когда вас разбудят в 3 ночи.
</Hook>

<Goal lang="ru">
Восстановите хребет юнита, не подглядывая: почему всплеск — это и есть отказ, где single-flight и lock ограничивают стадо, почему XFetch coordination-free, чем жертвует SWR, зачем negative caching и как stampede превращается в metastable failure.
</Goal>

<RetrievalDrawer
  client:load
  id="03-stampede-short-retrieval"
  lang="ru"
  questions={[
    { q: "Почему кеш делает режим отказа хуже, чем вообще без кеша, хотя суммарный объём запросов ниже?", a: "Кеш превращает ровный трафик во всплеск. Без кеша БД видит, скажем, 5 000 RPS равномерно, и под это её можно рассчитать. С TTL 60 с БД видит почти ноль 59 секунд, а затем все 5 000 RPS в односекундном окне после истечения горячего ключа, когда каждый конкурентный запрос промахивается разом. Суммарная работа падает, но пиковая конкуренция на границе равна полной нефильтрованной скорости трафика — а БД провизионировали под закешированный steady-state, а не под этот пик. Поэтому любая митигация stampede — про ограничение числа одновременно перестраивающих, а не про снижение объёма. Увеличение TTL только сдвигает всплеск во времени, но не убирает." },
    { q: "Сравните in-process single-flight и Redis distributed lock: что каждый ограничивает, сколько стоит и в каком порядке применять.", a: "Single-flight живёт в памяти одного процесса: O(1) карта ключ -> in-flight промис. Первый промах стартует rebuild и регистрирует промис; каждый конкурентный промах по тому же ключу подписывается на него вместо старта своего. Он сворачивает стадо процесса в один rebuild при нулевой сетевой цене — но флот из 50 нод всё равно запустит 50 rebuild'ов, ведь у каждой ноды своя карта. Redis SETNX lock (SET lock:key uuid EX N NX) координирует весь флот: ровно один держатель перестраивает, остальные ждут и перечитывают или берут stale fallback. Цена — round-trip на захват. Применять single-flight первым (бесплатно, на ноду), затем lock (общий, межнодовый): 100 000 промахов -> 50 (single-flight) -> 1 (lock)." },
    { q: "Как XFetch добивается 'ожидаемо ровно одно раннее обновление на окно TTL' без координации и когда он отказывает?", a: "Каждое чтение вычисляет -beta * delta * ln(rand()) >= time_to_expire, где delta — типичная длительность rebuild. Вероятность близка к нулю вдали от истечения и круто растёт в последние секунды, так что какой-то читатель запускает раннее обновление, пока все остальные продолжают попадать в ещё валидное значение. Ожидание равно единице, потому что минимум из N независимых экспоненциальных розыгрышей сам экспоненциальный со ставкой N на lambda: с ростом флота 'победитель' срабатывает раньше, но итог остаётся ~1 — без координации, лишь одно случайное число на чтение. Он отказывает на холодных или редких ключах: если ключ читается раз в 30 с при TTL 60 с, узкое окно раннего обновления может пройти с нулём чтений, ключ истечёт штатно и устроит stampede. Для таких берут lock или SWR." },
    { q: "Что гарантирует stale-while-revalidate на границе TTL, чем жертвует и как CDN request coalescing это расширяет?", a: "SWR (RFC 5861) мгновенно возвращает истёкшее закешированное значение каждому запросу в пределах stale-окна, поставив в очередь ровно одно фоновое обновление. Ожидание пользователя на границе падает до нуля, нагрузка на БД — до одного rebuild-запроса; ни один запрос не блокируется на rebuild. Жертва — явное ограниченное устаревание: читатели видят данные старше на величину stale-while-revalidate, что нормально для контента, новостей и листингов, но неверно для балансов, счётчиков голосов или всего, что два пользователя должны видеть согласованно. CDN request coalescing (Cloudflare) / collapsing (Fastly) расширяет идею на краю: пока один upstream-fetch по пути в полёте, все прочие конкурентные промахи по этому пути прицепляются к нему, так что вирусное событие с миллионами зрителей даёт один origin-fetch, а не миллионы." },
    { q: "Что такое negative caching и какую атаку открывает его отсутствие?", a: "Negative caching хранит sentinel с коротким TTL для 'нет такой строки', чтобы повторные лукапы несуществующего ключа отвечались из кеша, а не били по БД каждый раз. Без него каждый запрос к отсутствующему ключу — свежий промах, доходящий до БД, а защита позитивных ключей (single-flight, lock) не включается, ведь каждый лукап — по другому несуществующему ключу, и сворачивать нечего. Атакующий эксплуатирует это, штампуя бесконечные случайные несуществующие ID (случайные UUID в пути), превращая нормальный поток запросов в полноскоростной флуд по БД — документированный паттерн усиления miss-storm. Фикс: кешировать missing-sentinel с коротким TTL (5–30 с) и инвалидировать его при реальной вставке. Короткий негативный TTL ограничивает churn памяти." },
    { q: "Опишите metastable failure, который может последовать за неотмитигированным stampede, и почему система не восстанавливается сама.", a: "Stampede насыщает БД; запросы копятся за пределами клиентского таймаута и истекают; клиенты ретраят (большинство SDK ретраят по таймауту с коротким backoff, приняв перегрузку за временный сетевой сбой). Ретраи умножают стадо — 10 000 промахов становятся 30 000 в полёте — так что БД теперь обслуживает ретраи вместо свежих запросов, а кеш не может перестроиться, потому что БД никогда не отвечает достаточно быстро. Пустой кеш означает, что каждый новый пришедший тоже промахивается и ретраит. У системы два устойчивых состояния, healthy и storm, и возмущение перевело её в storm; каждый компонент следует корректному локальному поведению, поэтому ни один не рвёт цикл в одиночку. Восстановление требует внешнего kill-сигнала: 503-on-overload на шлюзе (клиенты делают долгий backoff), circuit-breaker, отдающий stale и переставший зависеть от БД, или ручной засев известно-хороших значений в кеш." },
  ]}
/>

<Recap lang="ru">
Если вы восстановили каждый ответ по памяти — вы держите хребет юнита: всплеск, а не объём, есть отказ; single-flight ограничивает стадо процесса, а lock — стадо флота; XFetch обновляет горячие ключи до истечения без координации (и отказывает на холодных); SWR покупает нулевое ожидание ценой ограниченного устаревания; negative caching закрывает дыру усиления miss-storm; а неотмитигированный stampede может опрокинуться в metastable retry storm, который рвёт только внешний kill-сигнал.
</Recap>
