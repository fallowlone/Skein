---
concepts:
- dogpile-effect
- single-flight
- distributed-lock
- lock-ttl
- lease-renewal
- probabilistic-early-expiration
estMin: 13
lang: ru
level: senior
order: 7
prereqs: []
slug: quiz-choice
sources:
- https://cseweb.ucsd.edu/~avattani/papers/cache_stampede.pdf
- https://pkg.go.dev/golang.org/x/sync/singleflight
- https://redis.io/docs/latest/develop/use/patterns/distributed-locks/
status: ready
summary: "Синтез юнита про dogpile в формате выбора — коллизия в момент истечения, коалесинг single-flight, distributed lock с TTL и продлением lease, и почему джиттер не спасает один горячий ключ."
title: 'Dogpile: тест с множественным выбором'
track: caching
unit: 07-dogpile
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Шесть вопросов поперёк всего юнита. Каждый отражает решение, которое ты принимаешь прямо в инциденте, когда один горячий ключ кладёт origin — не определение для заучивания, а компромисс на блокировке, lease и окне пересчёта.
</Hook>

<Goal lang="ru">
Убедись, что связываешь коллизию в момент истечения, коалесинг single-flight, distributed-блокировку с TTL и продлением lease, и раннее вероятностное истечение — синтез, к которому вёл урок.
</Goal>

<Quiz
  id="07-dogpile-choice-1"
  lessonSlug="07-dogpile"
  lang="ru"
  question="Один горячий ключ (пересчёт 200ms) кладёт БД каждый раз при истечении TTL под ~5000 req/s. Какой фикс наиболее прямо схлопывает стадо, и какая деталь делает его безопасным?"
  choices={[
    { label: "Single-flight / коалесинг запросов: первый промах пересчитывает, а каждый другой одновременный промах дедуплицируется на этот вызов в полёте. Безопасным его делает таймаут блокировки — упавший пересчётчик не должен заблокировать всех читателей навсегда.", correct: true },
    { label: "Снизить TTL, чтобы значение было свежее, а стадо меньше.", misconception: "Наоборот. Короче TTL — ключ истекает чаще, и dogpile срабатывает чаще. TTL управляет устареванием, а не коллизией параллелизма в момент истечения." },
    { label: "Добавить джиттер TTL ключу фида, чтобы момент истечения смещался.", misconception: "Джиттер разносит пачку ключей, закэшированных вместе. Здесь ровно один горячий ключ — значит ровно один момент истечения и одно стадо; рандомизация момента не уменьшает стадо, которое на нём собирается." },
    { label: "Раздуть пул соединений до 5000, чтобы каждый пересчёт получил соединение.", misconception: "Это просто переносит развал на саму БД — 5000 одновременных 200ms-агрегаций. Рычаг — схлопнуть N пересчётов в один, а не выделять ресурсы под N." },
  ]}
/>

<Quiz
  id="07-dogpile-choice-2"
  lessonSlug="07-dogpile"
  lang="ru"
  question="Локальный single-flight (мьютекс в процессе) срезал стадо, но БД всё равно видит ~20 одновременных пересчётов на истечение по всему флоту. Почему и что закрывает разрыв?"
  choices={[
    { label: "Локальный мьютекс коалесит только внутри одного процесса; при 20 инстансах каждый держит свой single-flight, поэтому origin видит до 20 пересчётов. Distributed lock (например Redis SET key val NX PX 30000) координирует один пересчёт на весь флот.", correct: true },
    { label: "Мьютекс течёт горутинами, поэтому старые пересчёты не освобождаются.", misconception: "Утечка росла бы безгранично; ~20 точно соответствует числу инстансов. Это покейный коалесинг на процесс, а не баг — граница тут процесс, а не дефект." },
    { label: "20 пересчётов значит, что таймаут блокировки слишком короткий и она всё время истекает.", misconception: "Тюнинг таймаута влияет на восстановление из дедлока, а не на разлёт по флоту. Даже идеальная локальная блокировка не видит остальные 19 инстансов; нужен общий стор блокировок." },
    { label: "Redis рейт-лимитит записи и режет до 20.", misconception: "20 — это число инстансов, а не лимит Redis. Проблема в том, что каждый процесс координируется только сам с собой, пока не введён межпроцессный lock." },
  ]}
/>

<Quiz
  id="07-dogpile-choice-3"
  lessonSlug="07-dogpile"
  lang="ru"
  question="Ты добавил distributed-блокировку пересчёта, но пропустил TTL блокировки (без истечения). Какой сбой senior-уровня?"
  choices={[
    { label: "Если единственный пересчётчик падает или зависает, держа блокировку, она не освобождается никогда, и каждый читатель блокируется навсегда — полный outage при здоровой, простаивающей БД.", correct: true },
    { label: "Блокировка делает сам пересчёт медленнее из-за сетевого round-trip.", misconception: "Round-trip добавляет миллисекунды, но это не режим отказа отсутствующего TTL. Опасность — мёртвый держатель, который не освобождает блокировку и блокирует всех читателей." },
    { label: "Читатели молча получат устаревшие данные.", misconception: "Паттерн lock-and-wait отдаёт свежее значение после записи. Устаревание — это компромисс stale-while-revalidate, а не следствие отсутствующего TTL блокировки." },
    { label: "TTL кэша будет проигнорирован, пока держится блокировка.", misconception: "TTL кэша и TTL блокировки — независимые таймеры. Пропуск TTL блокировки не отключает TTL кэша — он лишь убирает аварийный выход из дедлока." },
  ]}
/>

<Quiz
  id="07-dogpile-choice-4"
  lessonSlug="07-dogpile"
  lang="ru"
  question="Пересчёт обычно занимает 2s, но иногда 25s. Ты ставишь TTL distributed-блокировки в 5s. Что идёт не так и каков принцип?"
  choices={[
    { label: "На медленном 25s-пересчёте 5s-блокировка истекает в полёте, второй воркер забирает её и тоже начинает пересчёт — dogpile открывается снова, и два писателя могут гоняться. Принцип: TTL блокировки должен превышать худший пересчёт, либо держатель должен продлевать lease, пока жив.", correct: true },
    { label: "Ничего — 5s нормально, потому что большинство пересчётов укладывается в 2s.", misconception: "Хвост — это и есть вся проблема. TTL под медиану позволяет p99-пересчёту потерять блокировку в полёте, а это ровно тот момент, когда повторное открытие dogpile больнее всего." },
    { label: "Блокировка никогда не освободится, потому что 5s короче пересчёта.", misconception: "5s длиннее типичных 2s, так что обычный случай корректно освобождается по завершении. Дефект всплывает только на 25s-хвосте, где TTL срабатывает до окончания работы." },
    { label: "Читатели задедлочатся, потому что TTL слишком короткий, чтобы взять блокировку.", misconception: "Короткий TTL не блокирует захват; он вызывает преждевременное освобождение. Риск — дублирующие пересчёты после истечения, а не невозможность взять блокировку." },
  ]}
/>

<Quiz
  id="07-dogpile-choice-5"
  lessonSlug="07-dogpile"
  lang="ru"
  question="Вместо угадывания TTL блокировки держатель продлевает короткий lease (heartbeat, который продлевает блокировку каждую секунду, пока работает). Что это даёт и какой новый сбой надо обработать?"
  choices={[
    { label: "Lease отслеживает реальный пересчёт: живой держатель продлевает и не теряет блокировку в полёте, а упавший перестаёт продлевать, и короткий TTL быстро её освобождает. Новый сбой: приостановленный держатель (GC/STW-пауза, network partition) может думать, что всё ещё держит блокировку после лапса lease — поэтому писатель обязан фенсить (проверять lock-токен перед записью), чтобы не перезаписать свежее значение устаревшим.", correct: true },
    { label: "Это убирает потребность в TTL, потому что продление держит блокировку живой.", misconception: "Продление дополняет TTL, а не заменяет его. Именно короткий TTL освобождает блокировку, когда упавший держатель перестаёт продлевать — без него мёртвый держатель всё равно дедлочит всех." },
    { label: "Это гарантирует ровно одного писателя при любых условиях, включая network partition.", misconception: "Пауза или partition могут заставить держателя думать, что он владеет истёкшим lease, пока другой воркер его уже взял. Нужны fencing-токены или check-and-set запись; одно продление не даёт exactly-once." },
    { label: "Это убирает стоимость пересчёта, размазывая её по heartbeat'ам.", misconception: "Heartbeat'ы лишь продлевают блокировку; пересчёт всё равно выполняется один раз целиком. Lease меняет, кто может писать, а не стоимость работы." },
  ]}
/>

<Quiz
  id="07-dogpile-choice-6"
  lessonSlug="07-dogpile"
  lang="ru"
  question="Коллега утверждает, что блокировка всегда неверный инструмент, потому что сериализует читателей, и хочет везде раннее вероятностное истечение (XFetch). Когда какой из них на самом деле уместен?"
  choices={[
    { label: "Блокировка схлопывает N промахов в один, но может сериализовать читателей за медленным держателем (обрыв латентности), если ждущие не отдают stale; XFetch не даёт ключу истекать под нагрузкой — каждый читатель независимо пересчитывает чуть раньше, так что первый удачливый обновляет в одиночку. Отлично для одного очень горячего ключа, но требует хранить delta стоимости пересчёта и терпит немного лишней работы. Подбирай инструмент под форму, а не по общему правилу.", correct: true },
    { label: "Блокировки всегда неверны; XFetch строго доминирует, потому что никогда не блокирует.", misconception: "XFetch добавляет немного лишнего пересчёта и требует хранить delta и тюнить beta; он блещет на одном горячем ключе, но не универсальная замена. Блокировка с отдачей stale при ожидании уместнее, когда корректность требует одной скоординированной записи." },
    { label: "XFetch всегда неверен, потому что вероятностный — значит недетерминированные стада.", misconception: "XFetch доказанно почти оптимален (beta=1) именно потому, что каждый читатель бросает кости независимо, так что один обновляет рано и в одиночку до стада. Он намеренно вероятностный, а не случайно ненадёжный." },
    { label: "Они взаимозаменяемы; бери что есть в библиотеке.", misconception: "Они бьют в разные точки: блокировка координирует окно промаха, XFetch не даёт истечению совпасть с пиком нагрузки. Выбор зависит от толерантности к stale, числа инстансов и стоимости пересчёта — а не от удобства библиотеки." },
  ]}
/>

<Recap lang="ru">
Сквозная линия — одно решение: dogpile это коллизия в момент истечения одного горячего ключа, и каждый фикс бьёт в свою точку этого окна. Single-flight схлопывает N одновременных промахов в один пересчёт — но локальный мьютекс коалесит лишь на процесс, distributed lock нуждается в TTL длиннее худшего пересчёта (или в продлеваемом lease), чтобы упавший держатель не задедлочил всех, а lease всё ещё требует фенсинга против приостановленного держателя. XFetch растворяет коллизию, пересчитывая рано и в одиночку до того, как ключ вообще истечёт под нагрузкой. Джиттер разносит пачку ключей и ничего не делает для одного горячего. Большинство постмортемов про dogpile — про дедлок, сериализацию или двойную запись фикса, а не про исходный промах. Теперь, когда тебя поднимут по алерту на обрыв латентности с пятиминутной периодичностью, ты знаешь, какие вопросы задать первыми: один горячий ключ или пачка, сколько инстансов, и терпима ли пара секунд устаревания — ответы покажут, за каким рычагом тянуться.
</Recap>
