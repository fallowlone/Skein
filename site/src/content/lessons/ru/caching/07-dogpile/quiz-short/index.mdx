---
concepts:
- dogpile-effect
- single-flight
- distributed-lock
- lease-renewal
- fencing-token
- probabilistic-early-expiration
estMin: 13
lang: ru
level: senior
order: 8
prereqs: []
slug: quiz-short
sources:
- https://cseweb.ucsd.edu/~avattani/papers/cache_stampede.pdf
- https://pkg.go.dev/golang.org/x/sync/singleflight
- https://redis.io/docs/latest/develop/use/patterns/distributed-locks/
status: ready
summary: "Свободное припоминание поперёк юнита про dogpile. Сначала ответь своими словами, потом открой модельный ответ и сверься по механике блокировки, lease и XFetch."
title: 'Dogpile: тест на свободное припоминание'
track: caching
unit: 07-dogpile
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";

<Hook>
Припоминание сильнее перечитывания. На каждый вопрос проговори или запиши полный ответ по памяти до того, как откроешь модельный — усилие припоминания закрепляет механику блокировки и lease, когда тебя поднимут пейджером в 3 часа ночи.
</Hook>

<Goal lang="ru">
Реконструируй ключевые механизмы юнита — коллизию в момент истечения, single-flight против distributed-блокировки, TTL блокировки и продление lease, фенсинг и XFetch — не подглядывая в урок.
</Goal>

<RetrievalDrawer
  client:load
  id="07-dogpile-short-retrieval"
  lang="ru"
  questions={[
    { q: "Почему dogpile — именно проблема параллелизма в момент истечения и почему один горячий ключ — худший случай?", a: "Одиночный промах кэша безвреден: один запрос идёт в origin и пишет значение обратно. Dogpile — это когда много запросов промахиваются по одному ключу в один миг до того, как кто-либо записал новое значение. Опасное окно — зазор между первым промахом и переписыванием значения; в нём наивное чтение cache-aside не имеет координации, поэтому каждый одновременный запрос тоже промахивается и тоже начинает пересчёт. Горячий ключ — худший случай, потому что горячесть означает, что много запросов всегда в полёте, так что единственное событие истечения TTL бьёт по максимальному их числу разом. Один дорогой запрос на N одновременных — это развал: 200ms-запрос при 5000 req/s превращается в 5000 одновременных соединений, исчерпывая пул на 20–100." },
    { q: "В чём разница между локальным single-flight и distributed lock и когда нужен каждый?", a: "Локальный single-flight (например Go-шный singleflight.Group) — это map в процессе, дедуплицирующий одновременные вызовы по одному ключу на один вызов в полёте. Он схлопывает стадо внутри одного процесса, но не видит другие инстансы, поэтому при N инстансах за балансировщиком origin всё равно видит до N одновременных пересчётов — по одному на процесс. Distributed lock (например Redis SET key token NX PX) живёт в общем сторе, так что пересчитывает только инстанс, выигравший блокировку, и весь флот схлопывается до одного пересчёта. Single-flight берут, когда коалесинг нужен одному процессу или одному горячему пути и N-кратный разлёт приемлем; distributed lock — когда даже N одновременных пересчётов перегружают origin и нужен exactly-one по флоту, ценой сетевого round-trip и зависимости от доступности стора блокировок." },
    { q: "Почему у блокировки пересчёта обязан быть TTL и как его выбрать?", a: "Без TTL держатель, который упал, был убит OOM, завис или чей под вытеснили в середине пересчёта, никогда не освобождает блокировку, поэтому каждый другой читатель блокируется или ретраит вечно — полный outage при здоровой простаивающей БД. TTL — это аварийный выход из дедлока: он автоматически освобождает блокировку мёртвого держателя. Выбор — компромисс: слишком короткий, и медленный (p99/хвостовой) пересчёт теряет блокировку в полёте, второй воркер её забирает, и dogpile открывается снова с гонкой двух писателей; слишком длинный, и упавший держатель заставляет каждого читателя ждать всю эту длительность. Фиксированный TTL должен превышать худший пересчёт — поэтому лучший паттерн продлевать lease по heartbeat, а не угадывать одно число." },
    { q: "Объясни продление lease: какую проблему оно решает и какой новый сбой вводит.", a: "Продление lease означает, что держатель блокировки запускает heartbeat, который расширяет TTL блокировки (например PEXPIRE обратно до 10s каждые 3s), пока он жив и работает. Это отслеживает реальный пересчёт вместо угаданного фиксированного TTL: живой держатель продлевает и не теряет блокировку в полёте даже на медленном хвостовом пересчёте, а упавший перестаёт продлевать, и короткий TTL быстро её освобождает. Новый сбой — приостановленный держатель: длинная stop-the-world GC-пауза или network partition замораживает и heartbeat, lease лапсится, второй воркер забирает блокировку и пересчитывает, и когда первый возобновляется, он может всё ещё думать, что держит блокировку, и записать устаревшее значение. Так что одно продление не даёт exactly-once — нужно ещё и фенсить запись." },
    { q: "Что такое fencing-токен и почему он важен для leased-блокировки пересчёта?", a: "Fencing-токен — это монотонно растущее число, выдаваемое с блокировкой при каждом захвате; держатель прикрепляет его к любой своей записи, а стор (или кэш) отвергает запись с токеном старее последнего принятого. Это важно, потому что leased-блокировку всё ещё могут увести из-под держателя, который приостановился (STW GC, partition) достаточно, чтобы его lease лапсился: возобновившийся держатель всё ещё думает, что владеет блокировкой, и пытается писать. CAS-по-токену лишь не даёт удалить чужую блокировку; саму устаревшую запись он не останавливает. Fencing-токен (или версионный check-and-set значения) заставляет стор отвергнуть эту устаревшую запись, так что приостановленный и возобновившийся воркер не перезапишет более новое значение, которое уже произвёл второй воркер." },
    { q: "Когда выбирать раннее вероятностное истечение (XFetch) вместо блокировки и как оно работает?", a: "Блокировка координирует окно промаха после истечения; XFetch (probabilistic early expiration, VLDB 2015) не даёт истечению вообще совпасть с пиком нагрузки. На каждом попадании читатель бросает кости и может добровольно пересчитать рано с вероятностью, растущей по мере приближения истечения, по формуле time() − delta * beta * log(rand()) ≥ expiry, где delta — стоимость последнего пересчёта, а beta ≈ 1 почти оптимальна. Поскольку каждый читатель решает независимо, первый удачливый пересчитывает в одиночку задолго до того, как TTL дойдёт до нуля, и переписывает ключ — так что когда приходит стадо, значение свежее и никто не промахивается. Выбирай XFetch для одного очень горячего ключа, где можно хранить delta стоимости пересчёта и терпеть немного лишней работы; выбирай блокировку (с отдачей stale при ожидании), когда нужна одна скоординированная запись без лишних пересчётов. Они не взаимозаменяемы — бьют в разные точки одной коллизии." },
  ]}
/>

<Recap lang="ru">
Если ты смог реконструировать каждый ответ по памяти — ты держишь хребет юнита: dogpile это коллизия в зазоре между первым промахом и переписыванием значения на одном горячем ключе; single-flight коалесит на процесс, а distributed lock коалесит флот; блокировке нужен TTL длиннее худшего пересчёта или, лучше, продлеваемый lease; leased-блокировке всё ещё нужен fencing-токен, потому что приостановленный держатель может лапситься и потом перезаписать; а XFetch растворяет коллизию целиком, пересчитывая рано и в одиночку до того, как ключ вообще истечёт под нагрузкой.
</Recap>
