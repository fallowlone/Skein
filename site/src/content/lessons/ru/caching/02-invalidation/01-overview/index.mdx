---
concepts:
- cache-aside
- ttl-jitter
- write-through
- write-behind
- set-after-delete-race
- read-your-writes
deepensInto: []
estMin: 16
lang: ru
lessonType: topic
level: junior
order: 1
prereqs: []
slug: 01-overview
sources:
- https://www.usenix.org/system/files/conference/nsdi13/nsdi13-final170_update.pdf
- https://redis.io/blog/how-to-tame-the-thundering-herd-problem/
- https://www.systemoverflow.com/learn/caching/cache-stampede/probabilistic-early-refresh-and-ttl-jitter
- https://danielw.cn/cache-consistency-with-database
spiral: []
status: ready
summary: TTL, event-driven purge, write-through, write-behind, write-around — каждая
  стратегия меняет свежесть на свою аварию. Сеньорская ловушка — cache-aside delete,
  который параллельный читатель тихо перезаписывает значением, которое ты только что
  обновил.
title: 'Инвалидация кэша: гонка set-after-delete и консистентность, которую ты реально
  покупаешь'
track: caching
unit: 02-invalidation
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import SequenceDiagram from "~/components/diagram/SequenceDiagram.astro";
import Infographic from "~/components/diagram/Infographic.astro";
import leaseLoadCollapse from "./infographics/lease-load-collapse.svg?raw";

<Hook>
Пользователь меняет отображаемое имя. `PUT /users/{id}` возвращает 200, строка в Postgres обновлена, обработчик послушно делает `DEL user:42`, чтобы сбросить кэш. Он обновляет страницу — и старое имя на месте. Не на секунду: на следующие двенадцать часов, пока не истёк TTL. Дежурный инженер не смог воспроизвести это локально, потому что нужно, чтобы два запроса разошлись на несколько миллисекунд. Запись удалила ключ; чтение, которое стартовало *до* записи и держало старую строку из БД, завершилось *после* удаления и записало устаревшее значение обратно. Кэш врал, база говорила правду, и они расходились полдня.
</Hook>

<Crux>Удаление ключа на записи не делает его свежим — параллельный читатель может перезаписать его устаревшим значением сразу после твоего delete.</Crux>

<Explanation>

## Инвалидация — это выбор, какую несвежесть отгрузить

К концу этого урока ты будешь знать, какой именно разрыв открывает каждая стратегия и как закрыть тот, что больнее всего кусается в продакшене.

Кэша без несвежести не бывает; есть только та несвежесть, которую ты выбрал, и та, что тебя удивила. Каждая стратегия инвалидации — это разный ответ на один вопрос: когда источник истины меняется, как об этом узнаёт копия? TTL ждёт таймер. Event-driven purge слушает запись. Write-through обновляет оба хранилища в одном вызове. Write-behind обновляет кэш сейчас, а базу — потом. Каждая покупает свою гарантию консистентности и платит своей аварией — не существует стратегии, которая одновременно свежая, быстрая и простая.

Сложность в том, что кэш и база — это две системы, и ты пытаешься держать две системы согласованными без транзакции, охватывающей обе. В тот момент, когда запись коснулась одной, но ещё не другой, читатель может увидеть разрыв. Назвать стратегии легко; сеньорская работа — знать, какой именно разрыв открывает каждая и может ли твой продукт его терпеть.

## TTL: дёшево, в конце концов корректно и синхронизировано случайно

Time-to-live — это пол кэширования. Ты штампуешь каждую запись сроком истечения; чтения отдают её, пока таймер не вышел, затем следующее чтение перезаполняет. Это вообще не требует координации на пути записи — база даже не знает, что кэш существует. Цена — ограниченная несвежесть: при TTL 60 секунд изменённое значение может быть неверным до 60 секунд, и с этим ничего не поделать, кроме как укоротить TTL (больше нагрузки на origin) или добавить активную инвалидацию сверху.

Продакшен-ловушка TTL — не сама несвежесть, а *синхронизированное истечение*. Если 1000 серверов приложения кэшируют один горячий ключ с одинаковым TTL 300 секунд, и все прогрели его в одном окне деплоя, все они истекают в одном и том же окне ~100 мс. В момент T=300 каждый из них промахивается одновременно и бьёт в origin синхронно — **thundering herd** (стадо громов, синоним: cache stampede — лавина запросов к БД при одновременном истечении кэша). Фикс — **TTL jitter**: вместо фиксированных 300 с задай каждой записи случайное значение, скажем, в диапазоне 270–330 с (±10%). Истечения рассеиваются по полосе в 60 секунд, origin видит плавный подъём вместо пика, и одна перестройка обслуживает остальных. Работа Facebook над memcache и Redis обе считают jitter базовой защитой; более агрессивный вариант — probabilistic early refresh, где каждое чтение за порогом имеет малый шанс обновиться раньше, превращая резкий пик в 100 мс в подъём на 60 секунд.

## Event-driven purge и проблема двойной записи

Если ограниченной несвежести мало, инвалидируешь по самой записи: когда строка меняется, удаляешь (или обновляешь) ключ кэша. Это паттерн «cache-aside с purge», и здесь живут драконы. Теперь у тебя две записи — одна в базу, одна в кэш — и нет транзакции на обе. Это **проблема двойной записи** (dual-write): любое из хранилищ может успеть, пока другое падает. Если коммит в БД успешен, а `DEL` упал (потерянный пакет, всплеск Redis), кэш отдаёт старое значение до своего TTL — именно поэтому держишь TTL даже при активной инвалидации: это страховка для каждого пропущенного purge.

Удалять, а не обновлять ключ — это сам по себе сеньорский рефлекс: `DEL` идемпотентен и независим от порядка, поэтому два параллельных писателя, гоняющихся за инвалидацией, не могут оставить полузаписанное смешанное значение так, как два параллельных `SET`. Но delete-on-write не закрывает худшую гонку — о ней следующий раздел.

## Гонка set-after-delete: почему «просто удали ключ» всё равно отдаёт устаревшее

Вот авария из хука, по порядку. Читатель R получает промах по `user:42` и запрашивает базу, читая `name = "Ann"`. Прежде чем R запишет это обратно, отрабатывает писатель W: он обновляет строку до `name = "Bob"` и удаляет `user:42`. Теперь R — всё ещё держа устаревшее "Ann", прочитанное миллисекунды назад — завершает и делает `SET user:42 = "Ann"`. Кэш теперь держит "Ann", а база держит "Bob", и так остаётся неверным, пока не истечёт TTL. Удаление произошло *между* чтением и записью R, поэтому запись R тихо отменила его.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">t</th>
        <th class="border border-border px-3 py-2 text-left">Читатель R (промах)</th>
        <th class="border border-border px-3 py-2 text-left">Писатель W (update)</th>
        <th class="border border-border px-3 py-2 text-left">Состояние кэш / БД</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">1</td>
        <td class="border border-border px-3 py-2">промах; читает БД → "Ann"</td>
        <td class="border border-border px-3 py-2">—</td>
        <td class="border border-border px-3 py-2">БД: Ann · кэш: пусто</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">2</td>
        <td class="border border-border px-3 py-2">(тормозит: GC-пауза, ретрай)</td>
        <td class="border border-border px-3 py-2">UPDATE БД → "Bob"</td>
        <td class="border border-border px-3 py-2">БД: Bob · кэш: пусто</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">3</td>
        <td class="border border-border px-3 py-2">—</td>
        <td class="border border-border px-3 py-2"><code>DEL user:42</code></td>
        <td class="border border-border px-3 py-2">БД: Bob · кэш: пусто</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">4</td>
        <td class="border border-border px-3 py-2"><code>SET user:42 = "Ann"</code></td>
        <td class="border border-border px-3 py-2">—</td>
        <td class="border border-border px-3 py-2"><strong>БД: Bob · кэш: Ann (устарело!)</strong></td>
      </tr>
    </tbody>
  </table>
</div>

Есть три реальных фикса, по нарастанию цены. **Отложенное двойное удаление** (delayed double delete): после записи делаешь `DEL` ключа, затем планируешь *второй* `DEL` через несколько сотен миллисекунд — достаточно долго, чтобы любой читатель в полёте завершил свой устаревший `SET`, и второе удаление его стёрло. Дёшево, вероятностно и самый частый патч на практике. **Лизы (leases)** (подход Facebook): при промахе memcache выдаёт читателю 64-битный токен, привязанный к ключу; читатель может сделать `SET`, только если его токен ещё валиден, а `DEL` инвалидирует токен — поэтому устаревший set, пришедший после удаления, отвергается. Лизы срезали пиковую нагрузку Facebook на origin с **17 000 до 1 300 запросов/сек** на горячем ключе, попутно убив эту гонку. **Прогнать записи через кэш** (write-through), чтобы вообще не было отдельного `SET` от читателя, с которым можно гоняться.

<Infographic
  svg={leaseLoadCollapse}
  label="Столбчатая диаграмма пиковой нагрузки на origin по одному горячему ключу: delete-on-write с гоняющимися чтениями шлёт 17 000 запросов/сек, а лизы пропускают перезаполняющий SET только по токену и роняют её до 1 300 запросов/сек — примерно в 13 раз меньше."
  caption="Лизы не просто закрывают гонку set-after-delete — пропуская каждый перезаполняющий SET только по валидному токену, они обрушивают нагрузку на origin по горячему ключу ~в 13 раз (17K → 1.3K qps)."
  hue="var(--d-infra)"
/>

Все три фикса образуют лестницу: увидел гонку в проде — тянись сначала к отложенному двойному удалению; нужны гарантии корректности — шагай к лизам; если весь паттерн ощущается хрупким — перегони записи через кэш и устрани гонку полностью.

<Inset kind="why" lang="ru">
Почему удаление лучше обновления ключа на записи? Потому что два писателя могут переплестись. Если W1 ставит ключ в "Bob", а W2 — в "Carol", но их сетевые пакеты доходят до кэша в порядке, обратном их коммитам в БД, кэш может остаться на "Bob", а БД — на "Carol". У `DEL` нет значения, чтобы оно ушло не в том порядке — кто читает следующим, перезаполняет из уже устоявшейся БД. Идемпотентная инвалидация обходит проблему порядка, которую создают записи значений.
</Inset>

## Write-through, write-behind, write-around: куда идёт запись

К каким из них тянуться, когда стратегий на пути чтения уже недостаточно? Выбор упирается в один вопрос: может ли твой продукт терпеть устаревшее чтение сразу после собственной записи?

Стратегии выше — read-populated (cache-aside). Стратегии пути записи решают, что запись делает с кэшом напрямую. **Write-through** пишет в кэш и базу в одной операции, синхронно — кэш всегда консистентен с тем, что только что записали, и нет устаревшего set от читателя, с которым гоняться. Цена — латентность записи (платишь оба хранилища на критическом пути) и засорение кэша: кэшируешь данные, которые могут никогда не прочитать. **Write-behind / write-back** пишет в кэш немедленно, а запись в базу ставит в очередь асинхронно — быстрые записи, слияние записей под всплесками, но есть окно, где единственная копия записи лежит в волатильном кэше. Если узел умирает до сброса, эта запись потеряна; это та стратегия, которая может потерять данные, выглядевшие закоммиченными, поэтому её держат для терпимых нагрузок (счётчики, метрики) или в паре с долговечной очередью. **Write-around** вообще пропускает кэш на записи (только БД) и даёт чтениям заполнить позже — хорошо, когда записанные данные редко читают вскоре, плохо для read-your-writes, потому что следующее чтение самого писателя — гарантированный промах, затем устаревшее-до-заполнения.

<TradeoffMatrix
  id="02-invalidation-tradeoff-1"
  lessonSlug="01-overview"
  lang="ru"
  prompt="Пользователь редактирует свой профиль и сразу видит его снова (read-your-writes обязателен). Выбирая стратегию записи, что выдерживает?"
  options={[
    { name: "Write-through: пишем кэш + БД синхронно в одном вызове", summary: "Оба хранилища, на пути записи.", correct: true, justification: "Следующее чтение самого пользователя попадает в запись кэша, которая уже отражает его правку, поэтому read-your-writes держится, и нет устаревшего SET от читателя, с которым гоняться. Платишь лишней латентностью записи (два хранилища синхронно) и засорением кэша — правильная цена, когда корректность на собственном чтении писателя и есть требование." },
    { name: "Cache-aside с delete-on-write: обновить БД, DEL ключа", summary: "Идемпотентный purge, чтение перезаполняет.", justification: "Обычно норм, но открыт гонке set-after-delete: параллельный читатель, стартовавший до записи, может перезаполнить ключ устаревшим значением сразу после твоего DEL, и писатель увидит свой старый профиль. Для строгого read-your-writes нужны отложенное двойное удаление или лизы." },
    { name: "Write-behind: пишем кэш сейчас, сброс в БД асинхронно", summary: "Быстрые записи, async БД.", justification: "Быстро и дружелюбно к read-your-writes на кэше, но открывает окно долговечности: если узел умрёт до асинхронного сброса, выглядевшая закоммиченной правка профиля потеряна. Держи для терпимых данных (счётчики, метрики), а не для видимого пользователю состояния профиля." },
    { name: "Write-around: пишем только в БД, кэш на записи не трогаем", summary: "Запись только в БД.", justification: "Худший выбор для read-your-writes: следующее чтение самого писателя — гарантированный промах, а если задержалась устаревшая запись, она отдаёт старое значение до инвалидации. Write-around — для данных, которые редко читают вскоре после записи, противоположность этого условия." },
  ]}
/>

<Quiz
  id="02-invalidation-quiz-1"
  lessonSlug="01-overview"
  lang="ru"
  question="1000 серверов кэшируют один горячий ключ с одинаковым TTL 300 с, прогретым в одном деплое. В T=300 origin завален. Самый чистый первый фикс?"
  choices={[
    { label: "TTL jitter: дать каждой записи случайный TTL вроде 270–330 с, чтобы истечения рассеялись и одна перестройка обслужила остальных", correct: true },
    { label: "Снизить TTL до 30 с, чтобы истекал раньше", misconception: "Укорачивание TTL умножает нагрузку на origin и ничего не делает с синхронизацией — все 1000 серверов всё равно истекают синхронно, просто в десять раз чаще." },
    { label: "Убрать TTL и полагаться только на event-driven purge", misconception: "Сброс TTL убирает страховку для пропущенных purge; stampede — это проблема синхронизации, её чинят рассеиванием истечений, а не их удалением." },
    { label: "Добавить ещё кэш-узлов", misconception: "Больше узлов не помогает, когда каждый узел истекает один и тот же ключ в один миг и все промахиваются в один origin одновременно." },
  ]}
/>

<Quiz
  id="02-invalidation-quiz-2"
  lessonSlug="01-overview"
  lang="ru"
  question="При cache-aside delete-on-write ты обновляешь БД, затем DEL ключа. Почему кэш всё равно может остаться со старым значением?"
  choices={[
    { label: "Читатель, прочитавший старую строку БД до записи, может завершить свой перезаполняющий SET после твоего DEL, записав устаревшее значение обратно", correct: true },
    { label: "DEL не атомарен, поэтому ключ удалён лишь частично", misconception: "DEL атомарен; проблема в тайминге между двумя запросами, а не в частичном удалении одного ключа." },
    { label: "Redis тихо восстанавливает удалённые ключи из AOF", misconception: "Redis не воскрешает удалённые ключи; устаревшее значение приходит из SET параллельного читателя, а не из персистентности." },
    { label: "TTL пересоздаёт ключ со старым значением", misconception: "TTL только истекает ключи; он никогда не пересоздаёт их со старыми данными. Устаревшая запись — это перезаполнение гоняющегося читателя." },
  ]}
/>

<DragOrder
  id="02-invalidation-drag-1"
  lessonSlug="01-overview"
  lang="ru"
  prompt="Расставь события, дающие устаревшее перезаполнение set-after-delete:"
  items={[
    { id: "a", label: "Читатель промахивается мимо кэша и читает СТАРОЕ значение из БД" },
    { id: "b", label: "Писатель обновляет БД до НОВОГО значения" },
    { id: "c", label: "Писатель делает DEL ключа кэша (ключ теперь пуст)" },
    { id: "d", label: "Читатель завершает и делает SET кэша в СТАРОЕ значение из шага 1" },
    { id: "e", label: "Кэш теперь отдаёт СТАРОЕ значение, пока не истечёт страховочный TTL" },
  ]}
  correctOrder={["a", "b", "c", "d", "e"]}
/>

</Explanation>

<SequenceDiagram
  label="Гонка set-after-delete: читатель перезаписывает кэш устаревшим значением сразу после того, как писатель его удалил"
  hue="var(--d-backend)"
  actors={["Читатель R", "Кэш", "БД", "Писатель W"]}
  messages={[
    { from: "Читатель R", to: "БД", label: "1. miss; читает строку" },
    { from: "БД", to: "Читатель R", label: "name = Ann (старое)" },
    { from: "Писатель W", to: "БД", label: "2. UPDATE → Bob" },
    { from: "Писатель W", to: "Кэш", label: "3. DEL user:42" },
    { from: "Читатель R", to: "Кэш", label: "4. SET = Ann (устар.!)" },
  ]}
  caption="R читает старую строку, затем W обновляет БД и удаляет ключ; R завершается последним и записывает устаревшее значение обратно — в кэше Ann, а в БД Bob, пока не сработает страховочный TTL."
/>

<KeyTakeaway>Выбери, какую несвежесть отгрузить. TTL нужен jitter против stampede; delete-on-write гоняется с читателями — закрой двойным удалением или лизами; держи TTL как страховку для пропущенных purge.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="02-invalidation-retrieval"
  lang="ru"
  questions={[
    { q: "Проведи коллегу через гонку set-after-delete и три способа её закрыть.", a: "Читатель получает промах и читает старую строку из БД. Прежде чем он запишет это обратно, писатель обновляет БД до нового значения и удаляет ключ кэша. Читатель, всё ещё держа старое значение, прочитанное миллисекунды назад, делает SET кэша в это старое значение — отменяя удаление. Кэш теперь расходится с БД до истечения TTL, и воспроизвести трудно, потому что нужно, чтобы два запроса разошлись на несколько миллисекунд. Три фикса, по цене: отложенное двойное удаление планирует второй DEL через несколько сотен мс после записи, чтобы стереть устаревший SET, оставленный читателем в полёте — дёшево и вероятностно. Лизы (memcache Facebook) выдают читателю при промахе токен, который DEL инвалидирует, поэтому устаревший SET, пришедший после удаления, отвергается; это также срезало их нагрузку на origin по горячему ключу с 17K до 1.3K запросов/сек. Прогон записей через кэш (write-through) убирает отдельный SET от читателя целиком, и гоняться не с чем." },
    { q: "Зачем держать TTL, даже когда активно инвалидируешь на каждой записи, и почему одному TTL нужен jitter?", a: "Активная инвалидация — это двойная запись: коммит в БД и DEL кэша — две операции без транзакции на обе, поэтому DEL может упасть — потерянный пакет, икота Redis, краш процесса между ними. Когда он падает тихо, кэш отдаёт старое значение вечно, пока что-то ещё его не очистит. TTL — эта страховка: он ограничивает, как долго любой пропущенный purge может оставаться неверным, превращая постоянную неконсистентность в ограниченную. Но фиксированный TTL создаёт свою аварию: если много серверов прогрели один ключ вместе, они все истекают в одном узком окне и бьют в origin одновременно — thundering herd. Jitter (случайный TTL вроде ±10% вокруг цели) рассеивает истечения по полосе, чтобы origin видел плавный подъём и одна перестройка обслужила остальных, вместо тысяч одновременных промахов." },
  ]}
/>

<Recap lang="ru">
Инвалидация кэша — это выбор, какую несвежесть отгрузить, потому что кэш и база — две системы без транзакции на обе. TTL — дешёвый пол: в конце концов корректен, ограничен сроком истечения, но склонен к синхронизированным stampede, которые ты обезвреживаешь jitter-ом (случайный TTL ±10%) и probabilistic early refresh. Event-driven purge режет несвежесть до записи, но вносит проблему двойной записи — поэтому всегда держишь TTL как страховку для каждого пропущенного удаления. Гонка, которая кусается в проде, — set-after-delete: читатель, прочитавший старую строку до записи, перезаполняет ключ сразу после DEL писателя, оставляя устаревшие данные до TTL — закрывается отложенным двойным удалением, лизами (которые также срезали нагрузку Facebook по горячему ключу с 17K до 1.3K qps) или прогоном записей через кэш. Write-through покупает консистентность и read-your-writes ценой латентности записи; write-behind покупает быстрые записи ценой окна долговечности; write-around пропускает кэш на записи и ломает read-your-writes. Теперь, когда встретишь устаревшие данные, выжившие после записи, первый вопрос — какой разрыв указывает симптом: пропущенный purge, гонка set-after-delete или окно долговечности? Ответ скажет, за каким фиксом тянуться.
</Recap>
