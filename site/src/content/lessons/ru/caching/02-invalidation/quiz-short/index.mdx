---
concepts:
- set-after-delete-race
- ttl-jitter
- dual-write
- write-strategies
- read-your-writes
- delayed-double-delete
estMin: 14
lang: ru
level: senior
order: 8
prereqs: []
slug: quiz-short
sources:
- https://www.usenix.org/system/files/conference/nsdi13/nsdi13-final170_update.pdf
- https://danielw.cn/cache-consistency-with-database
- https://redis.io/blog/how-to-tame-the-thundering-herd-problem/
status: ready
summary: "Свободное припоминание по инвалидации кэша. Сначала ответь по памяти — гонка set-after-delete, TTL jitter, dual-write, стратегии записи — затем открой модельный ответ и сравни."
title: 'Инвалидация кэша: свободное припоминание'
track: caching
unit: 02-invalidation
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";

<Hook>
Припоминание бьёт перечитывание. На каждый вопрос проговори или напиши полный ответ по памяти, прежде чем открыть модельный — именно усилие извлечения закрепляет модель консистентности к моменту, когда инцидент прилетит в три часа ночи.
</Hook>

<Goal lang="ru">
Восстанови костяк юнита — почему инвалидация это выбор несвежести, гонка set-after-delete и три её фикса, почему TTL остаётся даже при активном purge и что покупает каждая стратегия записи — не подглядывая в урок.
</Goal>

<RetrievalDrawer
  client:load
  id="02-invalidation-short-retrieval"
  lang="ru"
  questions={[
    { q: "Почему инвалидацию кэша формулируют как «выбор, какую несвежесть отгрузить», а не как задачу, которую можно решить целиком?", a: "Кэш и база — две отдельные системы, и нет транзакции, охватывающей обе. В тот миг, когда запись коснулась одной, но ещё не другой, читатель может увидеть разрыв — поэтому какое-то окно расхождения неизбежно. Каждая стратегия — это разный ответ на «когда источник истины меняется, как узнаёт копия», и каждая покупает свою гарантию консистентности ценой своей аварии: TTL отгружает ограниченную несвежесть, event-driven purge отгружает риск dual-write, write-behind отгружает окно durability. Нет стратегии одновременно свежей, быстрой и простой. Сеньорская работа — назвать разрыв, который открывает каждая, и выбрать несвежесть, которую твой продукт терпит, а не делать вид, что устранил её." },
    { q: "Пройди гонку set-after-delete по шагам и назови три способа закрыть её в порядке возрастания стоимости.", a: "Читатель получает промах кэша и читает старую строку из БД (скажем, name='Ann'). Прежде чем записать её обратно, писатель обновляет БД на 'Bob' и делает DEL ключа. Читатель — всё ещё держа устаревшее 'Ann', прочитанное миллисекунды назад — делает SET кэша в 'Ann', тихо отменяя удаление. Кэш теперь расходится с БД до истечения TTL, и это трудно воспроизвести, потому что нужно, чтобы два запроса разошлись на несколько мс. Три фикса по стоимости: (1) delayed double-delete — DEL, затем второй DEL через несколько сотен мс, чтобы стереть устаревший SET читателя в полёте; дёшево и вероятностно. (2) Лизы — на промахе читатель получает токен, который DEL аннулирует, поэтому устаревший SET после удаления отклоняется (memcache от Facebook; ещё и срезал нагрузку на горячий ключ с ~17K до ~1.3K qps). (3) Write-through — прогнать записи через кэш, чтобы перезаполняющего читателем SET, с которым гоняться, просто не было." },
    { q: "Почему держать TTL даже при активной инвалидации на каждой записи, и что такое проблема dual-write?", a: "Активная инвалидация — это dual-write: коммит в БД и DEL кэша — две операции без транзакции на обе, поэтому любая может успеть, пока другая падает — потерянный пакет, всплеск Redis или краш между ними оставляют БД свежей, а кэш устаревшим. Без другого механизма этот пропущенный purge отдаёт старое значение вечно. TTL — страховка: он ограничивает, как долго любой пропущенный purge может оставаться неверным, превращая вечную несогласованность в ограниченную. Поэтому даже полностью event-driven кэш держит TTL как defence-in-depth для purge, который тихо упал." },
    { q: "Что такое thundering herd от синхронизированного истечения TTL и как jitter и probabilistic early refresh его гасят?", a: "Если многие серверы кэшируют один горячий ключ с одинаковым TTL и прогревают его в одном окне (например деплой), все они истекают в одном узком окне. На истечении каждый из них промахивается одновременно и бьёт в origin в унисон — thundering herd / cache stampede, резкий скачок нагрузки. TTL jitter задаёт каждой записи случайное значение около цели (например ±10%, 270–330 с), рассеивая истечения по полосе, чтобы origin видел плавный подъём, а одна перестройка обслуживала остальных. Probabilistic early refresh идёт дальше: по мере приближения записи к истечению каждое чтение имеет малый растущий шанс обновить её заранее, превращая резкий скачок в мягкий подъём и часто перестраивая до того, как что-либо реально истечёт." },
    { q: "Почему удалять ключ кэша на записи, а не обновлять его новым значением?", a: "DEL идемпотентен и независим от порядка; value-SET — нет. С двумя параллельными писателями их записи в кэш могут прийти в обратном порядке от их коммитов в БД — W1 в 'Bob', W2 в 'Carol' — оставив кэш на 'Bob', пока БД устаканивается на 'Carol', вечное расхождение. У DEL нет значения, чтобы перепутаться порядком: кто читает следующим, перезаполняет из уже устоявшейся БД. Так delete-on-write обходит гонку порядка писатель-против-писателя, которую создают обновления. Однако он не закрывает гонку set-after-delete против параллельного читателя — для неё всё ещё нужны double-delete, лизы или write-through." },
    { q: "Сравни write-through, write-behind и write-around: что каждый покупает и что ломает.", a: "Write-through пишет кэш и БД синхронно в одном вызове: кэш всегда консистентен с последней записью, и read-your-writes держится без перезаполняющего читателем устаревшего SET, с которым гоняться, ценой более высокой латентности записи (два хранилища на критическом пути) и загрязнения кэша (кэшируем данные, которые могут не прочитать). Write-behind / write-back пишет кэш сейчас и ставит запись в БД в асинхронную очередь: быстрые записи и объединение записей под всплесками, но окно durability — если узел умрёт до сброса, выглядящая закоммиченной запись потеряна, поэтому это для терпимых данных (счётчики, метрики) или в паре с durable-очередью. Write-around пишет только в БД и даёт чтениям перезаполнить позже: хорошо, когда записанное редко читают вскоре, но ломает read-your-writes, потому что собственное следующее чтение писателя — гарантированный промах-затем-устаревшее-до-перезаполнения." },
  ]}
/>

<Recap lang="ru">
Если ты восстановил каждый ответ по памяти, ты держишь костяк юнита: инвалидация — это выбор несвежести, потому что кэш и БД не делят транзакцию; гонка set-after-delete — это production-ловушка, закрываемая double-delete, лизами или write-through; TTL остаётся даже при активном purge как страховка для dual-write, который тихо падает; синхронизированное истечение стампидит origin, гасится jitter и probabilistic early refresh; delete бьёт update, потому что идемпотентен и независим от порядка; а стратегия записи — through, behind, around — это решение read-your-writes-против-durability.
</Recap>
