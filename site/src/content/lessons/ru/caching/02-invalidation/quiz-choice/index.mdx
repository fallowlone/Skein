---
concepts:
- ttl-jitter
- set-after-delete-race
- dual-write
- delete-vs-update
- write-through
- read-your-writes
estMin: 13
lang: ru
level: senior
order: 7
prereqs: []
slug: quiz-choice
sources:
- https://www.usenix.org/system/files/conference/nsdi13/nsdi13-final170_update.pdf
- https://redis.io/blog/how-to-tame-the-thundering-herd-problem/
- https://danielw.cn/cache-consistency-with-database
status: ready
summary: "Тест с выбором на синтез по инвалидации кэша — TTL jitter, гонка set-after-delete, dual-write, delete против update и какая стратегия записи сохраняет read-your-writes."
title: 'Инвалидация кэша: тест с выбором ответа'
track: caching
unit: 02-invalidation
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Шесть вопросов поперёк всего юнита. Каждый — решение, которое ты принимаешь в реальном инциденте: не стратегия, которую надо назвать, а разрыв несвежести, который надо взвесить против того, что твой продукт способен терпеть.
</Hook>

<Goal lang="ru">
Убедись, что связываешь TTL-стампиды, проблему dual-write, гонку set-after-delete и стратегии пути записи — тот синтез, к которому вёл урок: каждая стратегия отгружает свою несвежесть.
</Goal>

<Quiz
  id="02-invalidation-choice-1"
  lessonSlug="02-invalidation"
  lang="ru"
  question="1000 серверов приложения кэшируют один горячий ключ с одинаковым TTL 300 с, все прогреты в одном деплое. В момент T=300 origin получает удар на ~100 мс. Какой самый чистый первый фикс и почему он работает?"
  choices={[
    { label: "TTL jitter — задать каждой записи случайный TTL вроде 270–330 с. Истечения рассеиваются по полосе вместо залпа в унисон, origin видит плавный подъём, и одна перестройка обслуживает остальных.", correct: true },
    { label: "Снизить TTL до 30 с, чтобы записи истекали и обновлялись чаще.", misconception: "Укорачивание TTL умножает нагрузку на origin и ничего не делает с синхронизацией — все 1000 серверов всё равно истекают в одном окне, просто в десять раз чаще." },
    { label: "Убрать TTL и полагаться чисто на event-driven purge.", misconception: "Удаление TTL выбрасывает страховку от пропущенных purge. Стампида — проблема синхронизации; её чинят рассеиванием истечений, а не их удалением." },
    { label: "Добавить узлов кэша, чтобы поглотить нагрузку.", misconception: "Больше узлов не помогает, когда каждый узел истекает один ключ в один и тот же миг и все промахиваются в один origin одновременно." },
  ]}
/>

<Quiz
  id="02-invalidation-choice-2"
  lessonSlug="02-invalidation"
  lang="ru"
  question="Ты используешь cache-aside с delete-on-write: обновить БД, затем DEL ключа. Пользователь жалуется, что его правка профиля откатывается на часы. Самая вероятная первопричина?"
  choices={[
    { label: "Гонка set-after-delete: читатель, прочитавший старую строку до записи, завершил свой перезаполняющий SET после твоего DEL, записав устаревшее значение обратно, где оно и сидит до истечения TTL.", correct: true },
    { label: "DEL не атомарен, поэтому ключ удалился лишь частично.", misconception: "DEL атомарен для одного ключа. Баг — это тайминг между двумя параллельными запросами, а не частичное удаление одного ключа." },
    { label: "Redis воскресил удалённый ключ из AOF при следующем рестарте.", misconception: "Redis не восстанавливает удалённые ключи по ходу операции; устаревшее значение приходит из SET параллельного читателя, а не из persistence." },
    { label: "TTL пересоздал ключ со старым значением, когда истёк.", misconception: "TTL только истекает ключи — он никогда не пересоздаёт их со старыми данными. Устаревшую запись делает гоняющийся читатель, перезаполняя ключ." },
  ]}
/>

<Quiz
  id="02-invalidation-choice-3"
  lessonSlug="02-invalidation"
  lang="ru"
  question="Почему удалять ключ кэша на записи — сеньорский рефлекс, а не обновлять его новым значением?"
  choices={[
    { label: "DEL идемпотентен и независим от порядка, поэтому два писателя, гоняющиеся за инвалидацией, не оставят полусмешанное значение так, как два параллельных SET — кто читает следующим, перезаполняет из уже устоявшейся БД.", correct: true },
    { label: "DEL быстрее SET, поэтому снижает латентность записи на горячем пути.", misconception: "Разница в корректности, а не в скорости. DEL избегает гонки порядка: две value-записи могут лечь в кэш в обратном порядке от их коммитов в БД, оставив кэш в вечном расхождении с БД." },
    { label: "Обновление держит ключ тёплым, что всегда лучше для hit rate.", misconception: "Тёплый ключ — аргумент про hit rate, а не про корректность. Обновление возвращает гонку порядка записи, которую delete-then-repopulate обходит." },
    { label: "DEL полностью закрывает гонку set-after-delete, так что больше ничего не нужно.", misconception: "DEL избегает багов порядка писатель-против-писателя, но НЕ закрывает гонку set-after-delete против параллельного читателя — для неё всё ещё нужны double-delete, лизы или write-through." },
  ]}
/>

<Quiz
  id="02-invalidation-choice-4"
  lessonSlug="02-invalidation"
  lang="ru"
  question="Команда активно инвалидирует на каждой записи и предлагает убрать TTL целиком, «раз мы всегда делаем purge». Что ломается?"
  choices={[
    { label: "Активная инвалидация — это dual-write без транзакции на БД и кэш, поэтому DEL может тихо упасть (потерянный пакет, всплеск Redis, краш между операциями). Без TTL-страховки этот пропущенный purge отдаёт устаревшее значение вечно.", correct: true },
    { label: "Ничего — если каждая запись делает purge, кэш и БД не могут разойтись.", misconception: "Они расходятся всякий раз, когда DEL падает, но коммит в БД успешен. БД и кэш — две системы без общей транзакции, поэтому purge не гарантирован." },
    { label: "Hit rate падает, потому что ключи никогда не истекают сами.", misconception: "Риск здесь — корректность (вечная несвежесть на пропущенном purge), а не hit rate. TTL существует как страховка свежести, а не для вытеснения." },
    { label: "Память растёт неограниченно, потому что ключи не вытесняются.", misconception: "Политика вытеснения (LRU/LFU) разбирается с давлением памяти отдельно. Причина держать TTL здесь — ограничить ущерб от purge, который тихо упал." },
  ]}
/>

<Quiz
  id="02-invalidation-choice-5"
  lessonSlug="02-invalidation"
  lang="ru"
  question="Пользователь редактирует свой профиль и должен сразу увидеть новое значение (read-your-writes обязателен). Какая стратегия записи держит это с наименьшим риском гонки?"
  choices={[
    { label: "Write-through — писать кэш и БД синхронно в одном вызове. Собственное следующее чтение писателя попадает в запись, уже отражающую запись, без перезаполняющего читателем устаревшего SET, с которым можно гоняться.", correct: true },
    { label: "Write-around — писать только в БД и дать чтениям перезаполнить кэш позже.", misconception: "Write-around здесь худший выбор: собственное следующее чтение писателя — гарантированный промах, а любая зависшая устаревшая запись отдаёт старое значение. Это для данных, которые редко читают вскоре после записи — противоположный случай." },
    { label: "Write-behind — писать кэш сейчас, а сброс в БД делать асинхронно.", misconception: "Дружелюбен к read-your-writes на кэше, но открывает окно durability: если узел умрёт до асинхронного сброса, выглядящая закоммиченной правка потеряна. Держи это для терпимых данных вроде счётчиков." },
    { label: "Cache-aside с delete-on-write — обновить БД, DEL ключа.", misconception: "Обычно нормально, но открыто гонке set-after-delete, так что писатель всё ещё может увидеть свой старый профиль. Для строгого read-your-writes нужны delayed double-delete или лизы." },
  ]}
/>

<Quiz
  id="02-invalidation-choice-6"
  lessonSlug="02-invalidation"
  lang="ru"
  question="Горячий ключ под cache-aside ловит гонку set-after-delete на каждой правке профиля. Нужен сильнейший фикс, который ещё и снижает нагрузку на origin. За чем тянешься?"
  choices={[
    { label: "Лизы (подход memcache от Facebook): на промахе читатель получает токен, привязанный к ключу; он может сделать SET только пока токен валиден, а DEL аннулирует его — поэтому устаревший SET, пришедший после удаления, отклоняется. Это также срезало их нагрузку на горячий ключ с ~17K до ~1.3K qps.", correct: true },
    { label: "Просто поднять TTL, чтобы окно несвежести меньше значило.", misconception: "Более длинный TTL делает окно несвежести хуже, не лучше — раз гонка сработала, неверное значение сидит ещё дольше. И он ничего не делает с нагрузкой на origin при промахе." },
    { label: "Переключиться на обновление ключа вместо удаления.", misconception: "Обновление возвращает гонку порядка писатель-против-писателя и всё равно гоняется с чтением читателя. Это меняет одну гонку на другую, а не закрывает окно set-after-delete." },
    { label: "Отключить кэширование для этого ключа целиком.", misconception: "Это убирает гонку, убирая кэш, но горячий ключ — именно там, где кэш нужнее всего: это отправляет полную нагрузку в origin, ту самую проблему, для которой придумали лизы." },
  ]}
/>

<Recap lang="ru">
Сквозная мысль одной фразой: инвалидация — это выбор, какую несвежесть отгрузить. TTL — дешёвый пол, ограниченный, но склонный к синхронным стампидам, которые гасишь jitter. Event-driven purge режет несвежесть до записи, но это dual-write, поэтому держишь TTL как страховку для каждого пропущенного DEL. Delete бьёт update, потому что идемпотентен и независим от порядка, но всё равно гоняется с параллельным читателем — гонка set-after-delete — закрываемая delayed double-delete, лизами (которые ещё и срезали нагрузку Facebook на горячий ключ) или прогоном записей через кэш. А стратегия записи — это решение про read-your-writes: write-through держит его, write-around ломает, write-behind рискует durability.
</Recap>
