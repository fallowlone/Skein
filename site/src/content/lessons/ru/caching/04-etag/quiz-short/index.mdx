---
concepts:
- entity-tag
- conditional-request
- if-none-match
- strong-vs-weak-etag
- etag-vs-last-modified
- per-node-etag
estMin: 13
lang: ru
level: senior
order: 8
prereqs: []
slug: quiz-short
sources:
- https://www.rfc-editor.org/rfc/rfc9110#name-etag
- https://developer.mozilla.org/en-US/docs/Web/HTTP/Reference/Headers/If-None-Match
status: ready
summary: "Промпты на свободное припоминание по юниту ETag. Сначала ответь своими словами, затем раскрой модельный ответ и сравни."
title: 'ETag: свободное припоминание'
track: caching
unit: 04-etag
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";

<Hook>
Припоминание сильнее перечитывания. На каждый промпт проговори или запиши полный ответ по памяти, прежде чем открыть модельный, — именно усилие припоминания закрепляет механизм, когда ты смотришь на реальную ленту DevTools.
</Hook>

<Goal lang="ru">
Восстанови спину юнита — что экономит 304, рукопожатие «отпечаток-эхо-сравнение», strong vs weak валидаторы, ETag vs Last-Modified и поюнитный сбой — не подглядывая в урок.
</Goal>

<RetrievalDrawer
  client:visible
  id="04-etag-short-retrieval"
  lang="ru"
  questions={[
    { q: "Коллега говорит, что ETag делают повторные запросы бесплатными. Поправь точно: что экономит 304 и что всё равно стоит?", a: "304 экономит тело ответа и ничего больше. Клиент всё равно отправляет conditional request и ждёт ответа, поэтому полная задержка кругового обхода платится каждый раз — при RTT 80 мс опрос с 304 всё равно стоит 80 мс. Исчезает загрузка: тело JSON 8 КБ превращается в ~200 байт заголовков, а изображение 2 МБ при ревалидации передаёт ноль байт. Так что ETag — это оптимизация трафика и времени загрузки, а не задержки. На сервере они тоже не бесплатны: чтобы ответить на conditional request, origin должен знать текущий ETag, что обычно значит прочитать или пересчитать ресурс лишь ради сравнения и отбрасывания тела — компромисс CPU против трафика. Пропустить запрос целиком позволяет свежесть (Cache-Control max-age); ETag — это запасной путь после истечения свежести." },
    { q: "Пройди рукопожатие conditional request от начала до конца, называя заголовки и статусы.", a: "На первом ответе сервер прикрепляет ETag — непрозрачный токен, идентифицирующий именно эту версию тела, например ETag: \"a3f5c901\". Клиент кэширует тело и хранит токен рядом. На следующем запросе он эхо-отправляет токен в If-None-Match: \"a3f5c901\", что значит «отдай только если изменилось». Сервер пересчитывает или смотрит текущий ETag и сравнивает: при совпадении отвечает 304 Not Modified без тела с обновлёнными заголовками, и клиент отдаёт кэшированную копию; при несовпадении отвечает 200 OK с полным новым телом и новым ETag. Токен непрозрачен намеренно — интерпретирует его только выпустивший origin, поэтому схему ETag можно менять, не трогая ни одного клиента." },
    { q: "Объясни strong vs weak валидаторы и где различие действительно играет.", a: "Сильный валидатор (ETag: \"x\") обещает побайтовую идентичность: два сильных ETag совпадают только если представления точно равны. Слабый (ETag: W/\"x\") обещает лишь семантическую эквивалентность — ресурс «достаточно тот же» для переиспользования, несмотря на мелкие отличия вроде комментария с меткой времени. Тонкость в том, что If-None-Match всегда использует слабый алгоритм сравнения, поэтому для обычной ревалидации W/\"x\" и \"x\" считаются совпавшими, а префикс W/ игнорируется. Различие кусается лишь там, где обязательна побайтовая точность — range-запросы через If-Range, где докачка требует точно те же байты. Там слабый валидатор отвергается и сервер пересылает весь ресурс с 200." },
    { q: "Сравни ETag и Last-Modified. Когда Last-Modified недостаточно и почему?", a: "Last-Modified / If-Modified-Since — это валидатор по метке времени, дешёвый в вычислении (stat файла), и он всё ещё работает. Его жёсткий предел в том, что у HTTP-дат разрешение в одну секунду: если ресурс меняется дважды в пределах секунды — обычное дело для горячего конфига, генерируемых файлов или строк с частыми записями — Last-Modified не различит две версии, и клиенту может прийти устаревший 304. У ETag такой слепой зоны нет: хеш контента или счётчик версии меняется при каждом реальном изменении вне зависимости от времени. Senior-дефолт: когда присутствуют оба, ETag имеет приоритет и клиенты предпочитают If-None-Match; многие серверы шлют оба, чтобы кэши, понимающие только метки, тоже получали хоть какую-то ревалидацию, а ETag-осведомлённые клиенты — точную." },
    { q: "Почему добавление второго и третьего сервера часто убивает 304, и как сделать ETag устойчивыми к балансировщику?", a: "Весь контракт в том, что идентичный контент даёт идентичный ETag вне зависимости от отвечающего узла. Как только токен зависит от чего-то локального для одного сервера, это ломается. Классические виновники: старый inode-based FileETag в Apache (у идентичных файлов разные inode на каждой машине), дрейф mtime из-за того, что деплой копирует файлы на реплики в слегка разное время, и состояние процесса — счётчик, соль или метка старта, зашитые в токен. За round-robin балансировщиком If-None-Match клиента выпущен подом A, но следующий запрос попадает на под B, чей токен иной, поэтому всегда несовпадение — вечные чистые 200, хотя кэширование выглядит включённым. Фикс — выводить ETag чисто из контента: колонка версии или updated_at, хеш контента или FileETag MTime Size с убранным inode." },
    { q: "Как сжатие взаимодействует с сильным ETag и в чём ловушка?", a: "gzip- и identity-представления ресурса это разные байтовые потоки, поэтому сильный ETag — обещающий побайтовую идентичность — должен между ними различаться. Ловушка появляется, когда прокси начинает сжимать на лету: если он переиспользует один сильный тег на обе кодировки, клиент, скачавший gzip-200, может позже ревалидировать, попасть на узел, отдающий identity, и побайтовая семантика If-Range сломается. Правильное поведение — варьировать сильный ETag по Content-Encoding (или ослаблять его, когда сжатие на лету делает побайтовую точность необеспечиваемой), и помнить, что слой сжатия, добавленный между деплоями, может тихо инвалидировать все закэшированные валидаторы разом." },
  ]}
/>

<Recap lang="ru">
Если ты смог восстановить каждый ответ по памяти, ты держишь спину юнита: 304 экономит тело, но не круговой обход; рукопожатие — это «отпечаток-эхо-сравнение» поверх ETag и If-None-Match; сильные валидаторы обещают побайтовую идентичность, а слабые — семантическую эквивалентность, причём If-None-Match сравнивает слабо, так что различие в основном играет для range; ETag обходит секундную слепую зону Last-Modified; а ревалидация выживает за балансировщиком, только когда ETag — чистая функция контента, включая его кодировку. Теперь, когда коллега выкатывает API на несколько подов с «включённым кэшированием», ты знаешь первый вопрос: откуда берётся ETag — из контента или из чего-то, что живёт только на одной машине?
</Recap>
