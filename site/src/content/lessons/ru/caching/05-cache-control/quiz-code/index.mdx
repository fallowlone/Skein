---
concepts:
- no-store-vs-no-cache
- s-maxage-override
- immutable-content-hash
- private-vs-public
- stale-while-revalidate
estMin: 14
lang: ru
level: senior
order: 9
prereqs: []
slug: quiz-code
sources:
- https://www.rfc-editor.org/rfc/rfc9111.html
- https://developer.mozilla.org/en-US/docs/Web/HTTP/Reference/Headers/Cache-Control
status: ready
summary: "Читай реальные заголовки Cache-Control и ответ CDN, предсказывай поведение браузера и CDN и выбирай верную директиву для сценария."
title: 'Cache-Control: чтение заголовков'
track: caching
unit: 05-cache-control
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Баги кэширования читаются прямо на проводе — в заголовках ответа и в строке cache-status CDN. Читай каждый набор заголовков, предсказывай, что делает с ним каждый уровень цепочки, затем выбирай исправление, которое отправит senior-инженер.
</Hook>

<Goal lang="ru">
Отработай цикл, который запускаешь в каждом инциденте кэширования: читай директивы, рассуждай о браузере и общем кэше раздельно и выбирай директиву под нужды данных по разделению и свежести.
</Goal>

## Сниппет 1 — «защищённый» эндпоинт

```http
GET /api/account/balance HTTP/1.1

200 OK
Cache-Control: no-cache
Content-Type: application/json
```

<Quiz
  id="05-cache-control-code-1"
  lessonSlug="05-cache-control"
  lang="ru"
  question="Этот эндпоинт баланса на пользователя отправляет no-cache и без ETag. Каково реальное поведение и что надо отправлять вместо этого?"
  choices={[
    { label: "Ответ хранится (в браузере и в любом CDN без private) и лишь ревалидируется перед повторным использованием — а без ETag даже ревалидировать эффективно нельзя, поэтому путь через 304 почти не помогает. Для финансовых данных на пользователя отправляй private, no-store.", correct: true },
    { label: "no-cache уже предотвращает хранение, поэтому данные в безопасности.", misconception: "no-cache разрешает хранение и лишь форсирует ревалидацию; он никогда не предотвращает хранение. Финансовым данным на пользователя нужен no-store, запрещающий хранение везде." },
    { label: "Отсутствие ETag — единственный баг; добавь ETag, и no-cache верен.", misconception: "ETag чинит эффективность ревалидации, но ответ всё равно хранится — в браузере и bfcache. Данные не должны храниться вообще, поэтому no-store нужен независимо от ETag." },
    { label: "Добавь s-maxage=0, чтобы CDN его не кэшировал.", misconception: "s-maxage=0 всё ещё разрешает хранение и лишь форсирует ревалидацию общего кэша; о браузере он тоже молчит. Верное исключение для данных на пользователя — private, no-store." },
  ]}
/>

## Сниппет 2 — HTML за CDN

```http
200 OK
Cache-Control: public, max-age=31536000
Content-Type: text/html; charset=utf-8
```

<Quiz
  id="05-cache-control-code-2"
  lessonSlug="05-cache-control"
  lang="ru"
  question="Этот заголовок стоит на index.html сайта, отдаваемом через CDN. Что идёт не так и какой верный заголовок для HTML, ссылающегося на ассеты с хешем содержимого?"
  choices={[
    { label: "И браузер, и CDN кэшируют HTML на год, поэтому деплои невидимы — посетители грузят старый документ и его старые URL ассетов до года. HTML должен быть no-cache (или max-age=0, must-revalidate), чтобы всегда подхватывать новые хешированные URL ассетов.", correct: true },
    { label: "Всё в порядке — очистка CDN при деплое обеспечивает свежесть.", misconception: "Очистка CDN не достанет браузер, держащий HTML год; этот клиент держит устаревший документ. HTML должен ревалидироваться браузером, поэтому не должен нести max-age на год." },
    { label: "Только CDN его кэширует; браузер ревалидирует HTML автоматически.", misconception: "Браузеры соблюдают max-age для HTML, как для любого ресурса; max-age=31536000 заставляет браузер держать его тоже год. Нет автоматической ревалидации HTML, когда max-age большой." },
    { label: "Добавь immutable, чтобы браузер перестал ревалидировать, и проблема исчезнет.", misconception: "immutable делает хуже — он говорит браузеру никогда не ревалидировать даже при перезагрузке, замораживая устаревший HTML сильнее. Правило: хешировать ассеты, никогда HTML." },
  ]}
/>

## Сниппет 3 — no-cache против no-store, бок о бок

```http
# Ответ A
Cache-Control: no-cache, private
ETag: "v9"

# Ответ B
Cache-Control: no-store
```

<Quiz
  id="05-cache-control-code-3"
  lessonSlug="05-cache-control"
  lang="ru"
  question="Страница результатов поиска, дешёвая в ревалидации и нечувствительная, использует Ответ A; страница сброса пароля использует Ответ B. Каждый выбор верен?"
  choices={[
    { label: "Оба верны. Ответ A хранится только в браузере (private) и ревалидируется по ETag — 304 дёшево переиспользует сохранённое тело, идеально для нечувствительного редко меняющегося контента. Ответ B запрещает хранение везде, верно для чувствительной страницы, которой нельзя лежать в кэше или bfcache.", correct: true },
    { label: "Поменяй их местами: no-store на поиске тратит трафик, а no-cache на сбросе нормален.", misconception: "no-cache на странице сброса разрешает хранение и лишь требует ревалидации — чувствительная страница всё равно хранилась бы и лежала в bfcache. Странице сброса нужен no-store; ревалидация A ровно подходит дешёвому нечувствительному поиску." },
    { label: "Ответ A неверен, потому что no-cache означает, что он никогда не переиспользуется.", misconception: "no-cache означает ревалидировать перед повторным использованием, а не никогда не использовать. 304 позволяет кэшу отдать сохранённое тело бесплатно, поэтому стабильный результат переиспользуется эффективно — в этом смысл пары no-cache с ETag." },
    { label: "Оба должны быть no-store ради безопасности.", misconception: "no-store на странице поиска выбрасывает всё кэширование нечувствительного контента, дёшево ревалидируемого, вредя производительности без выигрыша в безопасности. Подбирай директиву под данные: no-store лишь там, где риск — само хранение." },
  ]}
/>

## Сниппет 4 — строка ответа CDN

```http
200 OK
Cache-Control: max-age=60, s-maxage=300, stale-while-revalidate=3600
Age: 420
CF-Cache-Status: HIT
```

<Quiz
  id="05-cache-control-code-4"
  lessonSlug="05-cache-control"
  lang="ru"
  question="CDN возвращает это для JSON-API. Age=420 превышает s-maxage=300, но это HIT. Это корректно и что получает клиент?"
  choices={[
    { label: "Корректно. При Age 420 копия за s-maxage=300 (устарела), но в пределах окна stale-while-revalidate 3600с, поэтому CDN отдаёт устаревшую копию мгновенно как HIT и обновляет в фоне. Клиент получает быстрый, слегка устаревший ответ по замыслу.", correct: true },
    { label: "Это баг — устаревшая копия (Age больше s-maxage) никогда не должна быть HIT.", misconception: "stale-while-revalidate явно разрешает отдавать устаревшее в своём окне и считать это HIT, пока в фоне идёт ревалидация. Это фича, а не баг." },
    { label: "Age=420 означает, что браузер отвергнет его, так как превышает max-age=60.", misconception: "max-age=60 — свежесть браузера; копия CDN управляется s-maxage. Будет ли браузер ревалидировать, зависит от того, что CDN вернёт сейчас, но HIT CDN тут валиден по stale-while-revalidate." },
    { label: "CF-Cache-Status: HIT доказывает, что ответ свежий.", misconception: "HIT означает отдано из кэша, а не что оно свежее. По stale-while-revalidate устаревшая копия отдаётся как HIT; свежесть определяется Age против s-maxage, что здесь говорит устаревшее-но-отдаваемое." },
  ]}
/>

<Recap lang="ru">
Каждый баг кэширования читается из заголовков. no-cache без ETag на данных на пользователя — это хранимая, неэффективно ревалидируемая утечка — отправляй private, no-store. max-age на год на HTML замораживает деплои, потому что правило — хешировать ассеты и держать HTML ревалидируемым. no-cache плюс ETag — верный выбор для дешёвого нечувствительного контента, переиспользуемого через 304; no-store — для данных, которые нельзя хранить вообще. А HIT CDN с Age за s-maxage корректен по stale-while-revalidate — устаревшее-но-мгновенное по замыслу, с фоновым обновлением. Читай директивы по уровням, подбирай под разделение и чувствительность данных, затем проверяй на проводе.
</Recap>
