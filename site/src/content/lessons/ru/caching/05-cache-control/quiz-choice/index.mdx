---
concepts:
- no-store-vs-no-cache
- s-maxage-override
- private-vs-public
- immutable-content-hash
- stale-while-revalidate
- vary-cache-key
estMin: 13
lang: ru
level: senior
order: 7
prereqs: []
slug: quiz-choice
sources:
- https://www.rfc-editor.org/rfc/rfc9111.html
- https://developer.mozilla.org/en-US/docs/Web/HTTP/Reference/Headers/Cache-Control
- https://developers.cloudflare.com/cache/concepts/cache-control/
status: ready
summary: "Тест с выбором на синтез по всему юниту — хранение против ревалидации, TTL общих кэшей, утечка из-за private, immutable-ассеты и отдача устаревшего."
title: 'Cache-Control: тест с выбором ответа'
track: caching
unit: 05-cache-control
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Шесть вопросов поперёк всего юнита. Каждый отражает заголовок, который ты реально отправишь — и тихий инцидент, который следует, когда директива делает не то, что подсказывает её имя.
</Hook>

<Goal lang="ru">
Убедись, что выбираешь верную директиву для хранения против ревалидации, разделяешь TTL браузера и TTL CDN и рассуждаешь об утечках и ловушках, которые подставляют дефолты.
</Goal>

<Quiz
  id="05-cache-control-choice-1"
  lessonSlug="05-cache-control"
  lang="ru"
  question="Команда отправляет Cache-Control: no-cache на эндпоинт баланса банка, рассчитывая, что ничего не сохранится. Позже на киоске один клиент видит баланс предыдущего по нажатию «назад». Что пошло не так?"
  choices={[
    { label: "no-cache разрешает хранение и лишь требует ревалидации перед повторным использованием — ответ лежит в браузере и bfcache. При навигации назад/вперёд bfcache отдаёт его без ревалидации. Хранение запрещает no-store.", correct: true },
    { label: "no-cache верен; браузер киоска сломан, раз игнорирует его.", misconception: "Браузер действовал по спецификации: bfcache восстанавливает сохранённую страницу без нового запроса, а no-cache никогда не запрещал хранение. Ошибка — выбор no-cache вместо no-store для данных, которые нельзя хранить." },
    { label: "Им был нужен max-age=0, который предотвратил бы хранение.", misconception: "max-age=0 означает «сразу устаревший, ревалидировать», а не «не хранить». Ответ всё равно пишется в кэш и bfcache. Хранение предотвращает только no-store." },
    { label: "ETag исправил бы это, форсируя ревалидацию.", misconception: "ETag делает ревалидацию эффективной, но не останавливает хранение, а bfcache восстанавливает вообще без условного запроса. Чувствительным данным на запрос нужен no-store, а не лучшая ревалидация." },
  ]}
/>

<Quiz
  id="05-cache-control-choice-2"
  lessonSlug="05-cache-control"
  lang="ru"
  question="Ты ставишь Cache-Control: max-age=0 на HTML, чтобы браузер всегда был свежим. Впереди стоит CDN. Без s-maxage что происходит на CDN?"
  choices={[
    { label: "CDN наследует max-age=0 и ревалидирует на каждом запросе — нулевая разгрузка CDN, origin получает удар на каждом хите. s-maxage существует ровно для того, чтобы дать общему кэшу TTL длиннее, чем у браузера.", correct: true },
    { label: "CDN игнорирует max-age=0 и откатывается к своему дефолтному TTL.", misconception: "Совместимые общие кэши соблюдают max-age, пока его не переопределит s-maxage; они не подставляют тихо дефолт, когда max-age присутствует." },
    { label: "max-age=0 ведёт себя как no-store, поэтому CDN ничего не хранит и не добавляет нагрузки.", misconception: "max-age=0 всё ещё разрешает хранение; он лишь помечает копию сразу устаревшей и форсирует ревалидацию. CDN хранит и ревалидирует против origin каждый раз." },
    { label: "Ничего — max-age уже покрывает браузер и CDN одним верным значением.", misconception: "Он покрывает оба, но ОДНИМ значением. Смысл s-maxage в том, чтобы дать общему кэшу TTL длиннее, чем у браузера, сохраняя браузеры свежими." },
  ]}
/>

<Quiz
  id="05-cache-control-choice-3"
  lessonSlug="05-cache-control"
  lang="ru"
  question="Аутентифицированный маршрут /account отдаётся с Cache-Control: public, max-age=300 за CDN. Каждый посетитель в следующие пять минут видит страницу аккаунта первого пользователя. Корневая причина?"
  choices={[
    { label: "public позволяет общему CDN хранить ответ, и max-age применяется и к общим кэшам, поэтому персональная страница первого пользователя отдаётся всем 300с. Маршрут на пользователя должен быть private (в идеале no-store), чтобы общий кэш его не держал.", correct: true },
    { label: "CDN должен был автоматически ключевать по сессионной куке.", misconception: "CDN ключует по URL плюс то, что говорит Vary; без Vary: Cookie он считает все запросы /account одним ресурсом. И Vary игнорируется некоторыми CDN — реальная безопасность это private/no-store, а не автоключевание." },
    { label: "max-age=300 слишком длинный; max-age=5 был бы безопасен.", misconception: "Любой ненулевой общий TTL на персональном контенте утекает — сокращение окна лишь уменьшает радиус поражения. Исправление — private, чтобы держать его вне общих кэшей, а не меньшее число." },
    { label: "Баг — отсутствие ETag, поэтому CDN не мог ревалидировать.", misconception: "Ревалидация тут ни при чём: контент свеж в пределах max-age, поэтому CDN отдаёт кэшированную копию без ревалидации вообще. Дефект — общий кэш хранит ответ на пользователя, это проблема public/private." },
  ]}
/>

<Quiz
  id="05-cache-control-choice-4"
  lessonSlug="05-cache-control"
  lang="ru"
  question="Статические бандлы отдаются с Cache-Control: public, max-age=31536000, immutable. Какая единственная практика делает кэш на год безопасным, а не багом на год?"
  choices={[
    { label: "Имена файлов с хешем содержимого (app.4f3a9c.js). Хеш выводится из байтов, поэтому любое изменение даёт новое имя и новый URL — старый URL остаётся кэшированным навсегда и безвредно, новый запрашивается заново.", correct: true },
    { label: "Поставить must-revalidate рядом с immutable как страховку.", misconception: "immutable говорит кэшам пропускать ревалидацию, пока свежо, что прямо противоречит must-revalidate; их смешение не помогает. Безопасность приходит из смены URL при изменении через хеш содержимого." },
    { label: "Очищать CDN при каждом деплое, чтобы устаревшие копии вытеснялись.", misconception: "Очистка не достанет браузер, держащий immutable-копию год, а гонка глобальной очистки с живым трафиком хрупка. Новый хешированный URL обходит проблему целиком — очистка не нужна." },
    { label: "Снизить max-age до недели, чтобы файлы обновлялись регулярно.", misconception: "Это выбрасывает выигрыш immutable плюс кэш на год и всё равно перезапрашивает неизменные файлы. Верный паттерн сохраняет год и меняет URL только когда меняются байты." },
  ]}
/>

<Quiz
  id="05-cache-control-choice-5"
  lessonSlug="05-cache-control"
  lang="ru"
  question="JSON-API за CDN отправляет Cache-Control: max-age=60, s-maxage=300, stale-while-revalidate=3600. Какое получится поведение?"
  choices={[
    { label: "Браузеры считают свежим 60с; CDN — 300с; после истечения копии CDN он отдаёт устаревшую копию мгновенно до 3600с, ревалидируя в фоне — обмен небольшой устарелости на нулевую задержку запроса.", correct: true },
    { label: "stale-while-revalidate=3600 переопределяет s-maxage, поэтому CDN свеж час.", misconception: "stale-while-revalidate не продлевает свежесть; копия свежа только s-maxage=300. 3600с — окно, в котором УСТАРЕВШАЯ копия может отдаваться, пока в фоне идёт обновление." },
    { label: "Браузеры и CDN оба используют 300с, потому что s-maxage побеждает везде.", misconception: "s-maxage применяется только к общим кэшам. Браузеры (приватные кэши) игнорируют его и используют max-age=60." },
    { label: "Origin получает удар на каждом запросе после первых 60 секунд.", misconception: "Это игнорирует и s-maxage (CDN свеж 300с), и stale-while-revalidate (CDN отдаёт устаревшее мгновенно и обновляет асинхронно). Origin не в пути запроса в окне устарелости." },
  ]}
/>

<Quiz
  id="05-cache-control-choice-6"
  lessonSlug="05-cache-control"
  lang="ru"
  question="Ответ ставит Vary: Cookie, чтобы держать страницу залогиненного пользователя вне общих кэшей. Почему это ненадёжный механизм безопасности для персонального контента?"
  choices={[
    { label: "Vary определяет ключ кэша, а не разрешено ли совместное использование — и куки с высокой кардинальностью фрагментируют кэш так, что hit rate рушится. Хуже того, некоторые CDN игнорируют Vary на большинстве ответов, поэтому страница всё равно может разделяться. Безопасность должна жить в private/no-store, а Vary — лишь дополнительный сигнал.", correct: true },
    { label: "Vary: Cookie корректно предотвращает разделение; больше ничего не нужно.", misconception: "Vary меняет то, как кэш ключует варианты, а не то, может ли общий кэш вообще хранить ответ. И поскольку некоторые CDN игнорируют Vary, опора на него для корректности может утечь страницу одного пользователя другому." },
    { label: "Vary: Cookie в порядке, но надо ещё поднять max-age, чтобы это работало.", misconception: "Время жизни не связано с проблемой ключевания/разделения. Больший max-age ничего не делает, чтобы держать персональный ответ вне общих кэшей." },
    { label: "Vary влияет только на сжатие, поэтому на персонализацию не действует.", misconception: "Vary применяется к любому заголовку, который называет, включая Cookie и Authorization. Реальная проблема — ключевание это не то же, что исключение, а непоследовательная поддержка в CDN делает его ненадёжным для безопасности." },
  ]}
/>

<Recap lang="ru">
Сквозная линия юнита — одно дерево решений. Сначала спроси, может ли кэш вообще хранить ответ: no-store — единственная директива, запрещающая хранение, а no-cache лишь форсирует ревалидацию, поэтому no-cache утекает чувствительные данные в браузер и bfcache. Затем раздели уровни: max-age управляет всеми кэшами, s-maxage переопределяет его только для общих кэшей, а private держит ответы на пользователя вне CDN — его отсутствие на аутентифицированном маршруте и есть классическая утечка данных. Для статики public, max-age=31536000, immutable верен только с именами по хешу содержимого, потому что изменённый файл тогда меняет URL. stale-while-revalidate и stale-if-error меняют устарелость на задержку и устойчивость, а Vary ключует варианты, а не исключает разделение — поэтому реальная безопасность всегда живёт в private/no-store.
</Recap>
