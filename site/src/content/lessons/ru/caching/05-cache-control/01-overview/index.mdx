---
concepts:
- cache-control-directives
- no-store-vs-no-cache
- shared-vs-private-cache
- stale-while-revalidate
- immutable-content-hash
- vary-cache-key
deepensInto: []
estMin: 16
lang: ru
lessonType: topic
level: junior
order: 1
prereqs: []
slug: 01-overview
sources:
- https://www.rfc-editor.org/rfc/rfc9111.html
- https://developer.mozilla.org/en-US/docs/Web/HTTP/Reference/Headers/Cache-Control
- https://simonhearne.com/2022/caching-header-best-practices/
- https://developers.cloudflare.com/cache/concepts/cache-control/
spiral: []
status: ready
summary: no-cache не отключает кэширование, private всё равно может утечь в CDN, а
  пропущенный s-maxage отдаёт CDN неверный TTL. Директивы говорят браузеру, прокси
  и CDN, как именно вести себя — и дефолты тут ловушки.
title: 'Cache-Control: заголовок, который программирует каждый кэш в цепочке'
track: caching
unit: 05-cache-control
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";
import Infographic from "~/components/diagram/Infographic.astro";
import directiveDecisionSpace from "./infographics/directive-decision-space.svg?raw";

<Hook>
Дашборд банка выкатывает `Cache-Control: no-cache` на эндпоинте баланса счёта. Ревью безопасности кивнуло — «no-cache, ничего не хранится». Через неделю общий киоск показывает одному клиенту баланс предыдущего после нажатия «Назад». Браузер всё это время кэшировал ответ: `no-cache` никогда не значило «не кэшируй». Оно значило «кэшируй, но спрашивай перед повторным использованием». На `bfcache` и навигации назад/вперёд этот «спрос» так и не сработал. Директива, которая реально предотвратила бы хранение, — это `no-store`, и её никто не выкатил.
</Hook>

<Crux>no-cache значит «ревалидируй», а не «не кэшируй». Хранение запрещает только no-store; CDN рулят s-maxage и immutable.</Crux>

<Explanation>

## Два ответа на два разных вопроса

Зачем это знать инженеру? Потому что неправильная директива не даёт никакой ошибки — ответ уходит, заголовок выглядит «безопасно», и никто не узнает, пока киоск не отдаст чужую страницу аккаунта.

`Cache-Control` — это не один тумблер, а набор директив, и первое дело — знать, какая директива отвечает на какой вопрос. Два вопроса, которые разработчики постоянно путают: *«можно ли кэшу это хранить?»* и *«можно ли кэшу переиспользовать хранимую копию без проверки?»*

`no-store` отвечает на первый: не записывай это ни на диск, ни в память нигде. Используй для по-настоящему чувствительных, per-request данных — балансов, страниц сброса пароля, всего, что не должно осесть в `bfcache` киоска.

`no-cache` отвечает на второй: хранить можно, но перед выдачей обязательно ревалидируй с origin (условный запрос `If-None-Match`/`If-Modified-Since`). Если origin вернул `304 Not Modified`, кэш отдаёт хранимое тело бесплатно; если контент изменился — отдаёт новый. Это *не* «выключено» — это «всегда сначала проверь». Имя, которое люди ожидали от `no-cache`, по смыслу ближе к тому, что делает `no-store`, — именно поэтому путаница повсеместна. Значительная доля ответов с `no-cache` в реальном трафике уходит без `ETag` или `Last-Modified`, то есть даже эффективно ревалидировать не может — сильный сигнал, что автор имел в виду `no-store`.

## max-age, s-maxage и CDN, который тебя игнорирует

Когда ставишь один `max-age`, ты молча даёшь CDN ту же инструкцию, что и браузеру — а у этих двух уровней противоположные задачи. Вот разрез, который позволяет не путать их.

`max-age=<секунды>` задаёт время свежести: как долго любой кэш может отдавать копию без ревалидации. `s-maxage` делает то же самое, **но только для общих кэшей** — CDN и обратных прокси, — и для них он *перекрывает* `max-age`. Приватные (браузерные) кэши игнорируют `s-maxage` полностью.

Именно этот раздел отличает сеньора. Ловушка: ты ставишь `max-age=0`, чтобы держать HTML всегда свежим в браузерах, но забываешь `s-maxage`. CDN, не имея `s-maxage`, откатывается к `max-age=0` и ревалидирует на каждый запрос — твой origin под нагрузкой, а CDN ничего не делает. Или наоборот: `max-age=31536000` на HTML без `s-maxage`, и CDN радостно кэширует персонализированную страницу залогиненного пользователя на год, отдавая её всем. Эти два числа — независимые рычаги, потому что у двух уровней кэша разные задачи: браузер кэширует для одного пользователя, CDN — для всех.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Директива</th>
        <th class="border border-border px-3 py-2 text-left">Что она делает на самом деле</th>
        <th class="border border-border px-3 py-2 text-left">Ловушка</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2"><code>no-store</code></td>
        <td class="border border-border px-3 py-2">Ни один кэш не хранит ответ вообще</td>
        <td class="border border-border px-3 py-2">То, что ты хотел, когда тянулся к <code>no-cache</code></td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2"><code>no-cache</code></td>
        <td class="border border-border px-3 py-2">Хранит, но ревалидирует перед каждым переиспользованием</td>
        <td class="border border-border px-3 py-2">НЕ запрещает хранение; бесполезно без <code>ETag</code></td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2"><code>max-age=N</code></td>
        <td class="border border-border px-3 py-2">Свежо N секунд в любом кэше</td>
        <td class="border border-border px-3 py-2">CDN тоже его берёт, если не перекрыть <code>s-maxage</code></td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2"><code>s-maxage=N</code></td>
        <td class="border border-border px-3 py-2">Свежо N секунд только в общих кэшах</td>
        <td class="border border-border px-3 py-2">Браузеры игнорируют; забыть = отдать CDN <code>max-age</code></td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2"><code>private</code></td>
        <td class="border border-border px-3 py-2">Хранит только браузер; никаких общих кэшей</td>
        <td class="border border-border px-3 py-2">Забыть = CDN кэширует страницу одного для всех</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2"><code>immutable</code></td>
        <td class="border border-border px-3 py-2">Пропускает ревалидацию полностью, пока свежо</td>
        <td class="border border-border px-3 py-2">Безопасно только с content-hash, неизменными URL</td>
      </tr>
    </tbody>
  </table>
</div>

## private vs public и утечка, которая ломает карьеры

`public` говорит: любой кэш может это хранить, даже при наличии заголовка `Authorization`. `private` говорит: хранить может только браузер одного пользователя — CDN или общий прокси не должны. Провал жесток и тих: аутентифицированный, персонализированный ответ (`/account`, залогиненная главная, API, отдающее текущего пользователя) уходит без `private`, CDN кэширует копию первого пользователя, и каждому следующему посетителю отдают чужие данные. Это хрестоматийный инцидент cache-poisoning / утечки данных, и он почти всегда сводится к дефолтному `Cache-Control`, оставшемуся на роуте, который позже стал персонализированным.

Рефлекс сеньора: любой ответ, который зависит от того, кто спрашивает, — это `private, no-store` или `private, max-age=0`, и ты предполагаешь, что CDN стоит перед тобой, видишь ты его или нет. Затем `Vary: Cookie` или `Vary: Authorization` как страховочный сигнал, чтобы общий кэш ключевался по учётке.

<Inset kind="why" lang="ru">
`Vary` — это *ключ* кэша, а не директива времени жизни. `Vary: Accept-Encoding` нормально — два варианта (gzip, br). Но `Vary: User-Agent` или `Vary: Cookie` с высокой кардинальностью значений фрагментируют кэш так сильно, что hit rate падает почти до нуля: каждое отдельное значение получает свою запись, и ничего не переиспользуется. Хуже: некоторые CDN (Cloudflare) игнорируют `Vary` на большинстве ответов, поэтому `Vary`, на который ты опираешься ради корректности, может вообще не учитываться — а значит реальную безопасность должны нести `private`/`no-store`, а не `Vary`.
</Inset>

## immutable, content-hash и единственный способ кэшировать на год

Для статических ассетов цель противоположна HTML: кэшировать настолько жёстко, насколько физически возможно. Стандарт — `Cache-Control: public, max-age=31536000, immutable` — год, привычное значение для неизменяемых ассетов (RFC не задаёт верхнего предела), плюс `immutable`, который велит браузеру пропустить даже условную ревалидацию, которую он иначе сделал бы на жёстком перезагрузе. Это экономит round trip на ассет, что важно на странице с 80 файлами.

Но годовой кэш — это годовой баг, если файл когда-нибудь придётся менять. Паттерн, делающий это безопасным, — **имена файлов с content-hash**: `app.4f3a9c.js` вместо `app.js`. Хэш выводится из байтов, поэтому изменённый файл получает новое имя и, значит, новый URL — старый URL остаётся immutable и закэшированным навсегда, новый URL запрашивается свежим. Поэтому бандлеры (Vite, webpack, esbuild) по умолчанию выдают хэшированные имена: именно это позволяет `immutable` + годовому `max-age` быть и агрессивным, и корректным. Правило, которое всё связывает: **хэшируй ассеты, никогда HTML**. HTML — это `no-cache` (или `max-age=0, must-revalidate`), чтобы он всегда подхватывал новые URL ассетов; ассеты immutable, чтобы их никогда не перезапрашивали.

`must-revalidate` — более строгий родственник: став несвежим, кэш *обязан* ревалидировать и не может отдать несвежую копию, даже если origin недоступен. Противоположная позиция — `stale-while-revalidate` и `stale-if-error` — это явное согласие отдавать несвежий контент. `stale-while-revalidate=600` значит «отдавай несвежую копию мгновенно до 600с после истечения, пока ревалидируешь в фоне», меняя слегка устаревший ответ на нулевую задержку для запроса пользователя. `stale-if-error=86400` значит «если origin лежит, отдавай несвежее сутки, а не показывай ошибку». Реалистичный составной заголовок для API за CDN: `max-age=60, s-maxage=300, stale-while-revalidate=3600` — 60с свежо в браузерах, 5 минут в CDN, затем до часа мгновенно-но-несвежо, пока в фоне идёт обновление.

<Infographic
  svg={directiveDecisionSpace}
  label="Поле решений 2x2, пересекающее два вопроса — можно ли кэшу хранить ответ и это для одного пользователя или безопасно делить — даёт четыре выбора Cache-Control: private/no-store для чувствительных per-user данных, private/max-age=0 для персонального-но-хранимого, no-cache для общего HTML, который меняется, и public/max-age=31536000/immutable для ассетов с хешем в имени."
  caption="Два вопроса, с которых начался урок — хранить? и для одного? — сводятся к четырём выборам директивы, по одному на ячейку."
  hue="var(--d-infra)"
/>

<TradeoffMatrix
  id="05-cache-control-tradeoff-1"
  lessonSlug="01-overview"
  lang="ru"
  prompt="Ты отдаёшь персонализированный HTML-дашборд залогиненного пользователя через CDN. Выбери Cache-Control."
  options={[
    { name: "private, no-store", summary: "На пользователя, никогда не общий.", correct: true, justification: "Страница зависит от того, кто залогинен, поэтому ни один общий кэш не должен её держать: private исключает CDN, no-store исключает даже bfcache браузера — ровно ту утечку, что отдаёт одному пользователю дашборд другого. Ты теряешь разгрузку CDN на этом роуте, и это правильно: персонализированный HTML — не кандидат для CDN. Добавь Vary: Cookie как страховочный сигнал, но реальную безопасность держи на private/no-store, ведь некоторые CDN игнорируют Vary." },
    { name: "public, max-age=300", summary: "Кэш для всех, 5 мин.", justification: "Здесь катастрофа. public позволяет CDN хранить это, а max-age применяется и к общим кэшам, поэтому персонализированный дашборд первого пользователя отдают всем посетителям следующие 300 секунд. Это классический инцидент cache-poisoning аутентифицированного ответа — страница зависит от пользователя и никогда не должна быть общей." },
    { name: "no-cache", summary: "Ревалидация на каждое использование.", justification: "no-cache разрешает хранение (в браузере и, без private, потенциально в CDN) и лишь требует ревалидации. Неправильно настроенный CDN, который игнорирует или плохо обрабатывает ревалидацию, всё равно может отдать закэшированную персональную копию. Плюс ответ остаётся в bfcache. Для per-user данных нужен no-store, а не no-cache." },
    { name: "max-age=0, s-maxage=600", summary: "Свежо в браузере, кэш в CDN.", justification: "s-maxage явно велит общему CDN кэшировать персонализированную страницу на 10 минут — ровно противоположное тому, что нужно. max-age=0 заставляет браузер ревалидировать, но никак не мешает CDN отдать дашборд пользователя A пользователю B." },
  ]}
/>

<Quiz
  id="05-cache-control-quiz-1"
  lessonSlug="01-overview"
  lang="ru"
  question="Разработчик хочет, чтобы ответ API нигде не хранился. Он выкатывает Cache-Control: no-cache. Что произойдёт на самом деле?"
  choices={[
    { label: "Он ХРАНИТСЯ (браузер, возможно CDN); кэши лишь ревалидируют перед переиспользованием. Нужен был no-store.", correct: true },
    { label: "Нигде ничего не хранится, как и задумано", misconception: "no-cache не предотвращает хранение — он его разрешает и требует ревалидации перед переиспользованием. Хранение предотвращает no-store." },
    { label: "Хранится только в CDN, не в браузере", misconception: "no-cache не различает уровни; и браузер, и общие кэши могут хранить. Общие кэши исключает директива private." },
    { label: "Хранится, но истекает мгновенно, так что фактически выключено", misconception: "Ревалидация может вернуть 304 и бесконечно переиспользовать хранимое тело; копия сохраняется, а на bfcache/назад-вперёд её могут отдать вообще без проверки." },
  ]}
/>

<Quiz
  id="05-cache-control-quiz-2"
  lessonSlug="01-overview"
  lang="ru"
  question="Ты ставишь Cache-Control: max-age=0 на HTML, чтобы держать его свежим, но впереди стоит CDN. В чём пробел?"
  choices={[
    { label: "Без s-maxage CDN наследует max-age=0 и ревалидирует на каждый хит — нет разгрузки CDN, origin под нагрузкой", correct: true },
    { label: "CDN проигнорирует max-age и закэширует на свой дефолтный TTL", misconception: "Совместимые CDN уважают max-age для общих кэшей, если его не перекрывает s-maxage; они не подставляют дефолт молча, когда max-age присутствует." },
    { label: "max-age=0 означает то же, что no-store, поэтому ничего не кэшируется", misconception: "max-age=0 значит «несвежо сразу, ревалидируй» — ответ всё равно хранится и ревалидируется, а не получает запрет хранения." },
    { label: "Ничего — max-age уже корректно покрывает и браузер, и CDN", misconception: "Он покрывает оба, но ОДНИМ значением; смысл s-maxage — дать общему кэшу другой (обычно больший) TTL, чем браузеру." },
  ]}
/>

<DragOrder
  id="05-cache-control-drag-1"
  lessonSlug="01-overview"
  lang="ru"
  prompt="Расставь вопросы для выбора правильного Cache-Control для ответа:"
  items={[
    { id: "a", label: "Чувствительное / per-request? Если да → no-store и остановись." },
    { id: "b", label: "Зависит от пользователя (auth/cookie)? Если да → private (держи вне общих кэшей)." },
    { id: "c", label: "Это content-hash статический ассет? Если да → public, max-age=31536000, immutable." },
    { id: "d", label: "Иначе выбери свежесть: max-age для браузеров, s-maxage для CDN (часто дольше)." },
    { id: "e", label: "Нужна устойчивость? Добавь stale-while-revalidate / stale-if-error, чтобы отдавать несвежее вместо медленного/ошибки." },
  ]}
  correctOrder={["a", "b", "c", "d", "e"]}
/>

</Explanation>

<FlowDiagram
  label="Запрос проходит приватный кэш браузера, затем общий кэш CDN, затем origin — и каждым ярусом управляет своя директива Cache-Control"
  hue="var(--d-backend)"
  perRow={4}
  nodes={[
    { id: "client", label: "Клиент", sub: "запрос" },
    { id: "browser", label: "Кэш браузера", sub: "max-age · private" },
    { id: "cdn", label: "CDN / общий", sub: "s-maxage · public" },
    { id: "origin", label: "Origin", sub: "no-store" },
  ]}
  edges={[
    { from: "client", to: "browser" },
    { from: "browser", to: "cdn", label: "miss" },
    { from: "cdn", to: "origin", label: "miss" },
  ]}
  caption="max-age управляет браузером, s-maxage — общим CDN; private держит копию вне CDN, а хранение на всех ярусах останавливает только no-store."
/>

<KeyTakeaway>Хранение запрещает no-store; no-cache лишь форсирует ревалидацию. private — для per-user данных, s-maxage — для CDN, а content-hash + immutable + max-age=31536000 — для статики. Проверяй private, пока он не утёк.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="05-cache-control-retrieval"
  lang="ru"
  questions={[
    { q: "Коллега ставит no-cache на чувствительный эндпоинт, чтобы тот не хранился. Объясни, почему это неверно и что использовать.", a: "no-cache не предотвращает хранение — он явно разрешает кэшу хранить ответ и лишь требует, чтобы кэш ревалидировал с origin (условный запрос If-None-Match / If-Modified-Since) перед переиспользованием хранимой копии. Поэтому чувствительный ответ всё равно оседает на диск в браузере, лежит в bfcache навигации назад/вперёд и — если не выставлен ещё и private — может храниться CDN. На нажатии «Назад» или на CDN, который плохо обрабатывает ревалидацию, его могут отдать без всякой проверки. Директива, которая реально предотвращает хранение, — это no-store: ни один кэш, браузерный или общий, не пишет ответ нигде. Для по-настоящему чувствительных per-user данных правильный заголовок — private, no-store (часто с max-age=0). Повсеместная путаница видна в данных: значительная доля ответов с no-cache уходит без ETag или Last-Modified, то есть даже эффективно ревалидировать не может — сильный знак, что автор имел в виду no-store." },
    { q: "Разбери, почему content-hash имена файлов позволяют безопасно кэшировать ассеты на год с immutable и что НЕЛЬЗЯ кэшировать так же.", a: "max-age=31536000 (год, привычное значение для неизменяемых ассетов — RFC не задаёт верхнего предела) плюс immutable говорит каждому кэшу: храни это и никогда не ревалидируй, даже на жёстком перезагрузе — что убирает условный round trip на ассет, реальный выигрыш на странице, тянущей 80 файлов. Опасность в том, что годовой кэш становится годовым багом устаревания, если файл когда-нибудь изменится, ведь клиенты не перезапросят URL, который считают immutable. Content-hash растворяет проблему: имя файла встраивает хэш байтов (app.4f3a9c.js), поэтому любое изменение даёт новое имя и, значит, новый URL. Старый URL остаётся закэшированным навсегда и безвредно; новый URL запрашивается свежим, потому что это другой ресурс. Поэтому бандлеры по умолчанию выдают хэшированные имена. Правило, связывающее всё: хэшируй ассеты, никогда HTML. HTML-документ отдаётся no-cache (или max-age=0, must-revalidate), чтобы браузер всегда его перепроверял и подхватывал новые хэшированные URL ассетов; сами ассеты immutable, чтобы их никогда не качали зря." },
  ]}
/>

<Recap lang="ru">
Cache-Control — это набор директив, отвечающих на два отдельных вопроса: можно ли кэшу это хранить и можно ли переиспользовать хранимую копию без проверки, — и их смешение источник большинства боевых инцидентов кэша. no-store — единственная директива, предотвращающая хранение; no-cache разрешает хранение и лишь форсирует ревалидацию, поэтому выкат no-cache на чувствительных данных утекает их в браузер и bfcache. max-age задаёт свежесть для любого кэша; s-maxage перекрывает его только для общих кэшей, поэтому забыть s-maxage значит отдать CDN браузерный TTL и либо завалить origin, либо закэшировать персональные страницы для всех. private держит per-user ответы вне общих кэшей, и его отсутствие на аутентифицированном роуте — классическая утечка данных через CDN. Vary задаёт ключ кэша, а не время жизни, и Vary с высокой кардинальностью обрушивает hit rate, тогда как некоторые CDN игнорируют Vary вовсе, — поэтому безопасность должна жить в private/no-store, а не в Vary. Наконец, content-hash имена файлов делают public, max-age=31536000, immutable и агрессивным, и корректным: хэшируй ассеты, чтобы изменение означало новый URL, и никогда не кэшируй HTML, который на них указывает. Теперь, когда встретишь `no-cache` на чувствительном эндпоинте в код-ревью, знай: тянись к `no-store` — и проверь, есть ли рядом `private`.
</Recap>
