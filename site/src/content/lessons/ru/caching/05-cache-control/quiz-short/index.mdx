---
concepts:
- no-store-vs-no-cache
- s-maxage-override
- private-vs-public
- immutable-content-hash
- stale-while-revalidate
- vary-cache-key
estMin: 13
lang: ru
level: senior
order: 8
prereqs: []
slug: quiz-short
sources:
- https://www.rfc-editor.org/rfc/rfc9111.html
- https://developer.mozilla.org/en-US/docs/Web/HTTP/Reference/Headers/Cache-Control
- https://simonhearne.com/2022/caching-header-best-practices/
status: ready
summary: "Промпты на свободное воспроизведение по всему юниту Cache-Control. Ответь своими словами, затем открой модельный ответ и сравни."
title: 'Cache-Control: свободное воспроизведение'
track: caching
unit: 05-cache-control
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";

<Hook>
Воспроизведение бьёт перечитывание. На каждый промпт скажи или запиши полный ответ по памяти, прежде чем открыть модельный — усилие припоминания и закрепляет семантику директив.
</Hook>

<Goal lang="ru">
Восстанови ключевые различия юнита — хранить против ревалидировать, TTL браузера против общего, public против private, immutable-ассеты и отдачу устаревшего — не заглядывая в урок.
</Goal>

<RetrievalDrawer
  client:load
  id="05-cache-control-short-retrieval"
  lang="ru"
  questions={[
    { q: "Объясни разницу между no-store и no-cache и почему их смешение вызывает реальные инциденты.", a: "Они отвечают на два разных вопроса. no-store отвечает «может ли кэш вообще хранить это?» — нет: ничего не пишется на диск или в память в браузере или любом общем кэше, и это директива для по-настоящему чувствительных данных на запрос вроде баланса банка или страницы сброса пароля. no-cache отвечает «может ли кэш переиспользовать сохранённую копию без проверки?» — он разрешает хранение и лишь требует ревалидации с origin (условный If-None-Match / If-Modified-Since) перед отдачей сохранённой копии. Паттерн инцидента: разработчик берёт no-cache, ожидая отсутствия хранения, чувствительный ответ оседает в браузере и bfcache, а при нажатии «назад» bfcache отдаёт его без ревалидации — утекая данные одного пользователя следующему. Имя и есть ловушка; директива «не хранить» — это no-store." },
    { q: "Какова связь между max-age и s-maxage и что ломается, когда забываешь s-maxage за CDN?", a: "max-age задаёт свежесть для любого кэша; s-maxage задаёт её только для общих кэшей (CDN, обратные прокси) и переопределяет для них max-age, а приватные (браузерные) кэши игнорируют s-maxage целиком. Это независимые рычаги, потому что у уровней разные задачи: браузер кэширует для одного пользователя, CDN — для всех. Забыть s-maxage значит отдать CDN то, что говорит max-age. Если max-age=0, CDN ревалидирует на каждом запросе — нулевая разгрузка, удар по origin. Если max-age длинный, CDN кэширует страницу на весь срок, что катастрофично для персонального. Senior-ход — задавать их намеренно и раздельно: короткий TTL браузера и длиннее общий, например max-age=60, s-maxage=300." },
    { q: "Почему отсутствие private на аутентифицированном маршруте — классическая утечка данных и каков senior-рефлекс?", a: "public позволяет любому кэшу хранить ответ, включая CDN; private говорит, что хранить может только браузер одного пользователя, и исключает общие кэши. Когда аутентифицированный персональный ответ (страница аккаунта, залогиненная главная, API текущего пользователя) уходит без private, CDN хранит копию первого пользователя и отдаёт её каждому следующему в окне свежести — учебниковое отравление кэша аутентифицированного ответа, и обычно это тянется от дефолтного Cache-Control, оставленного на маршруте, который позже стал персональным. Senior-рефлекс: любой ответ, зависящий от того, кто спрашивает, — private (в идеале no-store), ты считаешь, что впереди стоит CDN, видишь ты его или нет, а Vary: Cookie/Authorization добавляешь лишь как вторичный сигнал — реальная безопасность живёт в private/no-store, потому что некоторые CDN игнорируют Vary." },
    { q: "Разбери, почему имена по хешу содержимого делают public, max-age=31536000, immutable одновременно агрессивным и корректным, и что НЕЛЬЗЯ так кэшировать.", a: "Год (общепринятое значение для immutable-ассетов; RFC не накладывает потолка) плюс immutable говорят каждому кэшу держать файл и никогда не ревалидировать его, даже при жёсткой перезагрузке — экономя условный round trip на ассет, реальный выигрыш на странице с множеством файлов. Опасность в том, что кэш на год становится багом устарелости на год, если файл когда-либо меняется, ведь клиенты не перезапрашивают URL, который считают immutable. Хеш содержимого растворяет это: имя файла встраивает хеш байтов (app.4f3a9c.js), поэтому любое изменение даёт новое имя и новый URL. Старый URL остаётся кэшированным навсегда и безвредно; новый запрашивается заново, потому что это другой ресурс. Связующее правило — хешировать ассеты, никогда HTML — документ HTML это no-cache (или max-age=0, must-revalidate), чтобы браузер всегда перепроверял его и подхватывал новые хешированные URL ассетов." },
    { q: "Что делают stale-while-revalidate и stale-if-error и чем они отличаются от must-revalidate?", a: "must-revalidate — строгая позиция: как только копия устарела, кэш ОБЯЗАН ревалидировать с origin и не может отдавать устаревшую копию, даже если origin недоступен. stale-while-revalidate=N — обратное согласие: после истечения кэш может отдать устаревшую копию мгновенно до N секунд, пока ревалидирует в фоне — обмен слегка устаревшего ответа на нулевую задержку запроса. stale-if-error=N говорит: если origin упал (или ошибся) при ревалидации, отдавай устаревшую копию до N секунд, а не показывай ошибку — рычаг устойчивости. Реалистичный составной заголовок для API за CDN — max-age=60, s-maxage=300, stale-while-revalidate=3600: 60с свежо в браузерах, 5 мин в CDN, затем до часа мгновенного-но-устаревшего, пока идёт фоновое обновление." },
    { q: "Что делает Vary, почему это ключ кэша, а не директива безопасности, и где он тебя подводит?", a: "Vary называет заголовки запроса, которые кэш должен включить в ключ кэша, поэтому он хранит отдельный вариант на каждое различное значение этих заголовков — Vary: Accept-Encoding корректно хранит вариант gzip и br. Он управляет ключеванием, а не временем жизни и не тем, разрешено ли разделение. Подводит он двумя способами. Первый: значения высокой кардинальности (Vary: Cookie, Vary: User-Agent) фрагментируют кэш так сильно, что каждое различное значение получает свою запись и hit rate рушится почти до нуля. Второй: некоторые CDN (Cloudflare) игнорируют Vary на большинстве ответов, поэтому Vary, на который ты полагаешься для корректности, может не соблюдаться вообще — значит персональная страница всё равно может разделяться. Вывод: держи безопасность на пользователя в private/no-store и считай Vary подсказкой, а не гарантией." },
  ]}
/>

<Recap lang="ru">
Если ты смог восстановить каждый ответ по памяти, ты держишь хребет юнита: no-store запрещает хранение, а no-cache лишь форсирует ревалидацию, поэтому имя no-cache и есть ловушка; max-age управляет всеми кэшами, а s-maxage переопределяет его только для общих, поэтому забыть s-maxage — отдать CDN неверный TTL; private держит ответы на пользователя вне CDN, и его отсутствие — классическая утечка; имена по хешу содержимого делают immutable-кэш на год корректным, потому что изменение значит новый URL; stale-while-revalidate и stale-if-error меняют устарелость на задержку и устойчивость против строгости must-revalidate; а Vary ключует варианты, а не исключает разделение, поэтому безопасность должна жить в private/no-store.
</Recap>
