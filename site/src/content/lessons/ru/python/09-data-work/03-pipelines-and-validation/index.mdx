---
concepts:
- python-data-pipelines
deepensInto: []
estMin: 19
lang: ru
lessonType: topic
level: senior
order: 3
prereqs: []
slug: "03-pipelines-and-validation"
sources:
- https://pandas.pydata.org/docs/
- https://docs.pola.rs/
spiral: []
status: ready
summary: 'Пайплайн — стадии с контрактами: валидация на границах схемами в духе pandera, идемпотентная запись (перезапись партиций, никакого слепого append), инкрементальность через watermark и логирование счётчиков строк по стадиям — дешёвый детектор тихо исчезающих строк.'
title: 'Пайплайны и валидация: контракты на границах, идемпотентная запись и тихо исчезающие строки'
track: "python"
unit: "09-data-work"
---

import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";
import Infographic from "~/components/diagram/Infographic.astro";
import idempotentWrite from "./infographics/idempotent-write.svg?raw";

<Hook>
Пайплайн выручки шесть дней горел зелёным, тихо теряя 12% строк. Команда выше по потоку поменяла свой экспорт; колонка валюты для одной исходной системы стала приходить пустой и парсилась в NaN. Стадия обогащения делала inner join заказов со справочником валют — а строки с ключом NaN не совпадали ни с чем, и джойн просто их не выдавал. Ни исключения, ни предупреждения, код выхода 0, дашборды рисуются. Финансы заметили на шестой день, потому что региональный итог выглядел худым; постмортем занял у двух инженеров три дня — большую часть ушло на доказательство того, куда делись строки, потому что ни одна стадия не логировала, сколько строк она получила и сколько выдала. Лечение, которое поймало бы это в первый же день, почти оскорбительно: pandera-схема на входе, объявляющая ключ джойна non-nullable — одна строка, — или логируемая пара rows-in/rows-out на стадию с алертом на дельту. Пайплайны не падают как сервисы. Сервисы падают; пайплайны продолжают успешно завершаться, пока данные портятся, и единственная защита — контракты, проверяемые на границах.
</Hook>

<Crux>Валидируйте на границах, а не в каждой функции. Идемпотентная запись, watermark и счётчики строк по стадиям ловят тихие потери данных.</Crux>

<Explanation>

## Контракты на границах, а не проверки везде

Пайплайн — это стадии, связанные данными, и инженерный вопрос — где проходят границы доверия. Валидируйте на каждой **границе** — файлы от других команд, выгрузки из API, всё пересекающее линию владения, и собственный выход перед записью — а внутренность держите чистой: фрейм, прошедший входной контракт, внутренние функции принимают на веру. Оборонительные проверки, рассыпанные по каждому трансформу, хоронят настоящий контракт в шуме и всё равно пропускают вход — а сюрпризы рождаются именно там (Хук был изменением выше по потоку, а не локальным багом).

Инструмент — схема-как-код. pandera выражает контракты DataFrame декларативно — типы колонок, nullable, диапазоны, уникальность — и выдаёт читаемый отчёт об отказе вместо NaN, проползающего четыре стадии до того, как стать видимым:

```python
import pandera as pa

orders_schema = pa.DataFrameSchema(
    {
        "order_id": pa.Column(int, unique=True, nullable=False),
        "currency": pa.Column(str, nullable=False),     # Хук, одна строка
        "amount": pa.Column(float, pa.Check.ge(0)),
    },
    strict=True,            # неожиданные колонки — ошибка, а не пожатие плечами
)

orders = orders_schema.validate(orders, lazy=True)      # lazy: ВСЕ нарушения одним отчётом
```

`lazy=True` важен операционно: он собирает все нарушения в один отчёт, и дежурный видит «currency: 12,1% null, order_id: 3 дубликата» в одном упавшем прогоне вместо починки отказов по одному на перезапуск. pydantic играет ту же роль для построчных и конфигурационных данных на границах API; pandera (умеющая и polars) — DataFrame-нативный вариант. Принцип в обоих случаях один: контракт — это код, версионируемый рядом с пайплайном, падающий быстро и с именами — а не assert тремя джойнами ниже.

<Quiz
  id="03-pipelines-and-validation-quiz-1"
  lessonSlug="03-pipelines-and-validation"
  lang="ru"
  question="Изменение выше по потоку делает 12% значений ключа джойна NaN. Пайплайн делает inner join по этому ключу, валидации нет нигде. Что реально произойдёт?"
  choices={[
    { label: "Эти строки тихо исчезнут из результата джойна; джоб выйдет с кодом 0, дашборды просто покажут меньше выручки — заметить это могла бы только граничная схема или счётчики строк по стадиям", correct: true },
    { label: "Джойн упадёт, встретив NaN в ключах", misconception: "Джойны не падают на NaN — inner join выдаёт только совпавшие строки, а ключ NaN против справочника без NaN не совпадает ни с чем. Тишина и есть весь режим отказа." },
    { label: "NaN сработает как wildcard и совпадёт с каждой строкой справочника, взорвав выход", misconception: "NaN не совпадает ни с чем, если на другой стороне NaN нет. (Но берегитесь обратной ловушки: pandas спаривает NaN-ключи друг с другом, когда они есть с обеих сторон, — в отличие от SQL NULL.)" },
    { label: "pandas выдаст предупреждение о выброшенных несовпавших строках", misconception: "Такого предупреждения не существует. Отбрасывать несовпавшие строки — это inner join, корректно делающий свою работу; именно поэтому корректность здесь должен утверждать ваш контракт, а не библиотека." },
  ]}
/>

## Идемпотентность: перезапуск не должен дублировать

Спросите себя: что произойдёт, если эта стадия выполнится дважды? Если честный ответ — «выход удвоится», перед вами бомба замедленного действия, взведённая до ближайшего ретрая планировщика или сетевого сбоя.

Ретраи — не исключение, а норма: планировщики перезапускают, поды умирают посреди записи, люди перегоняют вчерашний день. Дисциплина: перезапуск стадии для того же окна входных данных должен дать то же состояние, а не больше строк. **Слепой append — антипаттерн**; две безопасные формы записи — *перезапись партиции*, когда выход ключуется окном обработки и заменяется целиком при перезапуске:

```python
out = f"s3://lake/revenue/dt={ds}/"        # ds = обрабатываемый день
result.write_parquet(out)                  # перезапуск заменяет партицию, а не добавляет к ней
```

— или *merge по бизнес-ключу* (upsert), когда сток — таблица. Та же структура «ключ-по-окну» даёт **инкрементальную обработку**: watermark запоминает последнее полностью обработанное окно, каждый прогон берёт только новые партиции, и **бэкфилл перестаёт быть аварией** — это тот же код, вызванный с явным историческим диапазоном дат, а не правленный руками скрипт с зашитым `now()`. Лакмус всего дизайна — один вопрос: что случится, если каждая стадия выполнится дважды?

<Infographic
  svg={idempotentWrite}
  label="Сравнение двух форм записи бок о бок. Слепой append: ретрай удваивает строки за день, у объектных и файловых стоков нечего откатывать, восстановление — это дедуп-криминалистика, а бэкфилл становится аварийным скриптом. Идемпотентная запись: перезапись партиции окна или merge по бизнес-ключу, поэтому ретрай заменяет состояние, а не добавляет к нему, а бэкфилл превращается в параметр через watermark."
  caption="Слепой append плюс ретрай равно дубликаты; ключевание выхода окном обработки и его перезапись делают так, что перезапуск заменяет состояние, а не наращивает его, — та же структура превращает бэкфилл из инцидента в параметр."
  hue="var(--d-infra)"
/>

## Наблюдаемость: счётчики строк — самая дешёвая сигнализация из возможных

Логируйте rows-in и rows-out для каждой стадии каждого прогона числами, которые машина может сравнить, — и Хук превращается в алерт первого дня: обогащение получило 1 204 000 строк и выдало 1 060 000, падение на 12% при исторической дельте около нуля. Распространите ту же привычку на метрики качества данных как временные ряды — доля null в ключевых колонках, число уникальных, min/max сумм — и вы поймаете отказы, не трогающие счётчики строк: задвоенная запись в справочнике, удваивающая выручку, смена единиц, сдвигающая каждую сумму в 100 раз. Джойны заслуживают одного собственного явного утверждения, потому что именно в джойне живут сюрпризы кардинальности: `merge(..., validate="many_to_one")` заставляет pandas упасть в момент, когда сторона справочника перестаёт быть уникальной, — взрыв many-to-many пойман на джойне, а не в итогах. И таймстемпы: джойн между timezone-naive и tz-aware колонками — в лучшем случае жёсткая ошибка dtype, в худшем — обе naive, одна в UTC, другая в локальном времени — джойн, почти ничего не совпадающий в часы расхождения смещений, без шанса на ошибку, потому что dtype совпадают.

<Quiz
  id="03-pipelines-and-validation-quiz-2"
  lessonSlug="03-pipelines-and-validation"
  lang="ru"
  question="Стадия, дописывающая выход (append), упала посреди записи и была перезапущена планировщиком. Счётчики строк за день удвоились. Каково структурное лечение?"
  choices={[
    { label: "Сделать запись идемпотентной: ключевать выход окном обработки и перезаписывать партицию (или делать merge по бизнес-ключу) — тогда ретрай заменяет состояние, а не добавляет к нему", correct: true },
    { label: "Ловить сбой и дедуплицировать сток перед ретраем", misconception: "Дедупликация постфактум должна восстановить, какие именно строки записала упавшая попытка, — хрупкая криминалистика. Семантика перезаписи снимает сам вопрос: перезапуск заменяет окно целиком." },
    { label: "Выключить ретраи планировщика для этой стадии", misconception: "Ретраи — правильная реакция на временные сбои; убрав их, вы меняете дублированные данные на отсутствующие. Баг — в неидемпотентном append, а не в политике ретраев." },
    { label: "Обернуть append в транзакцию базы данных", misconception: "Разумно, когда сток транзакционный, — но объектные хранилища и файловые стоки транзакций не предлагают. Перезапись партиции — переносимый контракт идемпотентности, работающий на бакете Parquet-файлов." },
  ]}
/>

## Оркестрация честно

Ежедневному джобу из трёх стадий нужны cron, makefile и дисциплины выше — заводить ради него workflow-платформу значит платить сложностью без выгоды. Airflow, Dagster или Prefect окупают свою сложность, когда у вас действительно есть DAG зависимых джобов, ретраи по стадиям с backoff, бэкфиллы по диапазонам дат как управляемая операция и команда, которой нужна история прогонов в одном месте. Честная рамка: оркестраторы планируют и показывают ваши стадии; они ничего не валидируют, не дедуплицируют и не версионируют. Контракты, идемпотентность и счётчики — ваш код в любом случае: пайплайн, неправильный под cron, ровно так же неправилен под Airflow, только с дашбордами получше.

</Explanation>

<FlowDiagram
  label="Пайплайн как стадии с контрактами: валидация на границах, идемпотентная запись, счётчики как боковой канал"
  hue="var(--d-systems)"
  perRow={3}
  nodes={[
    { id: "extract", label: "извлечение", sub: "сырая партиция dt=" },
    { id: "valin", label: "валидация входа", sub: "pandera, lazy=True" },
    { id: "transform", label: "преобразование", sub: "джойны, validate= кардинальность" },
    { id: "valout", label: "валидация выхода", sub: "счётчики + диапазоны" },
    { id: "load", label: "загрузка", sub: "перезапись партиции dt=" },
    { id: "metrics", label: "метрики", sub: "rows in/out, доля null" },
  ]}
  edges={[
    { from: "extract", to: "valin", label: "сразу отклонить" },
    { from: "valin", to: "transform", label: "доверенный фрейм" },
    { from: "transform", to: "valout", label: "проверка контракта" },
    { from: "valout", to: "load", label: "идемпотентная запись" },
    { from: "valout", to: "metrics", label: "временные ряды" },
  ]}
  caption="Контракты стоят на границах — входе и выходе, — поэтому внутренность остаётся чистой и доверенной. Запись ключуется окном и перезаписывается при перезапуске. Каждая стадия отдаёт rows in/out в канал метрик: класс отказа «12% строк исчезло» становится алертом того же дня вместо шести дней тихих потерь."
/>

<KeyTakeaway>Пайплайны отказывают успехом: код выхода 0 при неверных данных. Защищайтесь на границах — схемы pandera/pydantic (типы, nullable, диапазоны, уникальность) с ленивой валидацией ради одного полного отчёта — а внутренность пусть доверяет. Запись обязана быть идемпотентной: перезапись партиций по окну или merge по ключу; слепой append плюс ретрай равно дубликаты. Watermark делает обработку инкрементальной, а бэкфилл — первоклассным режимом. Логируйте rows-in/out по стадиям — самый дешёвый детектор исчезающих строк — и утверждайте кардинальность джойнов через validate=. Оркестраторы добавляют расписание и видимость, но не корректность.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="03-pipelines-and-validation-retrieval"
  lang="ru"
  questions={[
    { q: "Где в пайплайне место валидации, какими инструментами и почему не внутри каждой функции?", a: "На границах доверия: данные от других команд и систем (файлы, выгрузки из API) и собственный выход перед записью. Внутренние функции доверяют фреймам, прошедшим входной контракт, — оборонительные проверки везде хоронят настоящий контракт в шуме и всё равно пропускают вход, куда реально прилетают изменения соседей. Инструменты: pandera для контрактов DataFrame (типы колонок, nullable=False, диапазоны Check, unique=True, strict=True против неожиданных колонок; поддерживает и polars), pydantic для построчных и конфигурационных данных на краях API. Запускайте валидацию с lazy=True, чтобы один упавший прогон отчитался обо всех нарушениях сразу — операционно это разница между одним разбором инцидента и пятью перезапусками. Контракт — это код, версионируемый с пайплайном, и он превращает тихий класс отказов (NaN-ключ джойна, шесть дней теряющий 12% строк) в громкий отказ первого дня с именами колонок в ошибке." },
    { q: "Объясните триаду идемпотентность–watermark–наблюдаемость: что предотвращает каждая часть, с конкретными формами записи и логирования.", a: "Идемпотентность: перезапуск стадии должен воспроизводить состояние, а не наращивать его — планировщики повторяют запуск, люди перегоняют. Формы: перезапись партиции по окну обработки (писать в dt=YYYY-MM-DD/ и заменять целиком) или merge/upsert по бизнес-ключу для табличных стоков; слепой append — антипаттерн, превращающий каждый ретрай в дубликаты. Watermark: запоминать последнее полностью обработанное окно и брать только новые партиции — инкрементальность по умолчанию, а бэкфилл становится тем же кодом с явным историческим диапазоном вместо аварийного скрипта. Наблюдаемость: логировать rows-in/rows-out на стадию сравнимыми числами с алертом на дельту — самый дешёвый детектор тихо исчезающих строк — плюс временные ряды качества данных (доля null в ключах, уникальные, min/max) для отказов, сохраняющих счётчики. Джойнам — явное утверждение кардинальности (merge validate='many_to_one' падает на взрыве many-to-many), а колонки времени нормализуются в UTC-aware до любого джойна, потому что naive против aware — либо ошибка dtype, либо джойн, тихо не совпадающий ни с чем." },
  ]}
/>

<Recap lang="ru">
Сервисы падают; пайплайны продолжают выходить с кодом 0, пока данные гниют, — джоб выручки шесть дней терял 12% строк, потому что NaN-ключ ни с чем не совпадает в inner join, а ничто в стеке не считает это ошибкой. Защита структурна. Контракты на границах: схемы pandera или pydantic — типы, nullable, диапазоны, уникальность, строгий список колонок — валидируемые с lazy=True на каждой линии владения и перед каждой записью, чтобы сюрпризы сверху падали громко и с именами колонок, а внутренность пайплайна оставалась чистой и доверяющей. Идемпотентная запись: выход ключуется окном обработки и перезаписывается, либо мёржится по бизнес-ключу — никогда слепой append, — чтобы ретраи планировщика и человеческие перегоны заменяли состояние, а не дублировали его; watermark затем делает прогоны инкрементальными и превращает бэкфилл в параметр, а не в инцидент. Наблюдаемость: rows-in и rows-out на стадию с алертами на дельту (та самая строка, что превращает шесть тихих дней в алерт первого дня), метрики качества данных как временные ряды, кардинальность джойнов под validate=, чтобы взрывы many-to-many умирали на джойне, таймстемпы — в UTC-aware до сравнения. Оркестраторы — Airflow, Dagster, Prefect — отрабатывают своё на настоящем масштабе DAG и бэкфиллов, но они планируют и показывают; корректность — это три дисциплины выше, и писать их вам, под cron или под чем угодно ещё. Теперь, когда увидите пайплайн с кодом выхода 0 и тонкими итогами, — первый вопрос: сколько строк вошло в каждую стадию и сколько вышло; отсутствие этого числа и есть настоящий баг, который нужно починить.
</Recap>
