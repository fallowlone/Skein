infographic chart-column-simple
data
  title Та же таблица на 10 млн строк: размер по форматам
  desc CSV ~1,2 ГБ сжимается до ~200 МБ как Parquet+zstd — примерно в 6 раз, со схемой
  values
    - label CSV (МБ)
      value 1200
    - label gzip CSV (МБ)
      value 350
    - label Parquet + zstd (МБ)
      value 200
theme
  palette #f43f5e #f59e0b #14b8a6
