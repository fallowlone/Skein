---
concepts:
- python-file-formats
deepensInto: []
estMin: 17
lang: ru
lessonType: topic
level: senior
order: 2
prereqs: []
slug: "02-file-formats"
sources:
- https://parquet.apache.org/docs/
- https://arrow.apache.org/docs/
- https://pandas.pydata.org/docs/
spiral: []
status: ready
summary: 'Лестница форматов с механизмами: CSV заново парсит и угадывает типы при каждом чтении; Parquet даёт типизированную схему, row groups со статистиками, обрезку колонок и сжатие; Arrow — zero-copy стандарт в памяти. Плюс партиционирование, беда мелких файлов и эволюция схемы.'
title: 'Форматы файлов: CSV на краях, Parquet внутри — row groups, статистики, Arrow и компромиссы сжатия'
track: "python"
unit: "09-data-work"
---

import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import StackDiagram from "~/components/diagram/StackDiagram.astro";

<Hook>
Один ежедневный экспорт, три команды-потребителя, три разные таблицы. Хранилище каждое утро выкладывало CSV; финансы, опсы и data science читали его каждый своим `read_csv`. Финансы получали почтовые индексы целыми числами — `02134` превращался в `2134`, и региональный отчёт тихо склеил два штата. Data science получала идентификаторы клиентов как float64, потому что в одной строке была пустая ячейка, — id больше 2^53 округлялись, и джоб дедупликации начал находить «дубликаты», которые на деле были двумя разными клиентами с разницей в единицу. Опсы прибили dtype вручную — но их список разъехался с экспортом, когда добавили колонку, и их загрузчик молча залил новую колонку не тем типом. Встреча по сверке стала еженедельной. Никто не написал баг — его написал формат. CSV хранит символы, а не типы: каждый читатель заново выводит схему, и три читателя — это три схемы. Лечение оказалось скучным: тот же экспорт в Parquet, где схема едет внутри файла. Споры о dtype не разрешились — они стали невозможны.
</Hook>

<Crux>CSV заново парсит и угадывает типы при каждом чтении. Parquet хранит типизированные колонки в row groups — читатель пропускает лишнее.</Crux>

<Explanation>

## CSV и JSON: текст на краях

CSV — это символы, разделённые запятыми: ни типов, ни схемы, ни контракта. Каждое чтение заново парсит байты и заново угадывает их смысл: `02134` — это int или почтовый индекс, пустое поле — NaN или пустая строка, `2026-06-01` — дата или текст? Угадывание выполняется на каждого читателя, каждый день, что делает класс отказов системным, а не случайным: ведущие нули исчезают, одна пропущенная ячейка превращает колонку id во float64, локаль переворачивает десятичный разделитель. Смягчения — явные карты `dtype=`, `parse_dates`, прибитые списки колонок — это схема, которую каждый потребитель ведёт руками, вне файла и независимо. JSON и NDJSON поднимаются на полступеньки: умеют вложенность, NDJSON хорошо стримится построчно, но это по-прежнему текст — многословный (имена полей повторяются в каждой строке) и типово-двусмысленный на краях (в стандарте нет различия int и float, нет родных дат).

## Parquet: файл, который везёт собственную обрезку

Parquet — колоночный и типизированный, и его раскладка и есть механизм. Файл заканчивается **футером** со схемой и метаданными каждой **row group** — горизонтального среза строк, обычно от десятков до сотни с лишним МБ, часто порядка миллиона строк на группу в зависимости от писателя. Внутри row group каждая колонка — **column chunk**, разбитый на **страницы** (~1 МБ), единицу кодирования и сжатия: словарное кодирование для повторяющихся значений, RLE для длинных серий, сверху кодек. Из раскладки напрямую следуют два эффекта времени запроса. **Обрезка колонок**: читателю, которому нужны 3 колонки из 40, достаточно сместиться ровно к этим column chunks и не трогать остальное. **Predicate pushdown**: футер хранит min/max-статистики по каждому чанку (плюс счётчики null, опционально bloom-фильтры), поэтому фильтр вроде одного дня таймстемпов пропускает каждую row group, чей диапазон не может совпасть, — если данные записаны отсортированными по этой колонке, день живёт в нескольких смежных группах, и чтение трогает считаные проценты файла.

Сжатие задаётся на column chunk, и выбор — настоящий компромисс: **snappy** распаковывается на гигабайтах в секунду и почти не грузит CPU, но оставляет файлы на ~20–30% больше; **zstd** жмёт заметно меньше за умеренный дополнительный CPU. Лейки с упором на хранение по умолчанию берут zstd; горячие скан-пути иногда оставляют snappy. Честный масштаб, одна и та же таблица на 10 млн строк и ~20 колонок на одной машине: CSV ~1,2 ГБ, gzip-CSV ~300–400 МБ, Parquet-zstd ~150–250 МБ; чтение: pandas `read_csv` — десятки секунд, полный Parquet — несколько секунд, проекция из 2 колонок — меньше секунды. Выбор формата — порядок величины по обеим осям ещё до какого-либо тюнинга движка.

<Quiz
  id="02-file-formats-quiz-1"
  lessonSlug="02-file-formats"
  lang="ru"
  question="Запрос фильтрует Parquet-таблицу на 10 млн строк до одного дня значений ts, причём таблица записана отсортированной по ts. Почему чтение трогает лишь несколько процентов файла?"
  choices={[
    { label: "Футер хранит min/max-статистики по каждой row group; сортировка сделала диапазоны дней непересекающимися между группами, и читатель пропускает каждую группу, чей диапазон не может совпасть", correct: true },
    { label: "Parquet ведёт B-дерево-индекс по колонке ts", misconception: "В Parquet нет индексных структур. Обрезка берётся из min/max-статистик по row group (и опционально по страницам) плюс необязательных bloom-фильтров — потому сортировка на стороне писателя так и важна." },
    { label: "Сжатие делает файл настолько маленьким, что полное чтение пренебрежимо", misconception: "Сжатие уменьшает байты, но не пропускает их — неотсортированные данные всё равно распаковали бы каждую row group. Именно обрезка по статистикам убирает IO целиком." },
    { label: "Читатель делает бинарный поиск по колонке, потому что Parquet требует сортированной записи", misconception: "Parquet не требует сортировки, и никто не ищет бинарно по сырым данным. Сортировка — выбор писателя, чья выгода в том, что диапазоны min/max становятся достаточно селективными для обрезки." },
  ]}
/>

## Arrow: стандарт в памяти, который заставляет форматы дружить

Arrow — не файловый формат, конкурирующий с Parquet, а колоночная раскладка **в памяти**, общая для pandas (Arrow-backed dtypes), polars и DuckDB. Одинаковая раскладка буферов в каждом движке означает, что передача таблицы — обмен указателями, а не цикл сериализации-десериализации: polars в DuckDB и дальше в pandas без единого копирования данных. Когда Arrow всё же идёт на диск, это формат IPC (Feather — его файловый вариант) — по сути выписанная память: почти нулевая цена парсинга при чтении, но лёгкое сжатие и без обрезки по статистикам, как у Parquet. Разделение труда: Parquet — для хранения (минимум байтов, обрезаемо), Arrow — для обмена и памяти (ноль парсинга, ноль копий).

## Партиционирование и два способа его испортить

На диске датасеты режутся в hive-стиле на каталоги `key=value` — `dt=2026-06-01/` — и движки отбрасывают целые каталоги до открытия единственного файла: запрос за один день по году данных листит один каталог из 365. Режим отказа — **пере-партиционирование**: добавьте второй ключ высокой кардинальности (скажем, 50 тыс. id клиентов) — и год превращается в ~18 миллионов файлов в среднем по десятку КБ. Теперь доминирует листинг, каждый файл стоит открытия плюс чтения футера, а статистики row groups не получают шанса, потому что ни один файл не достаточно велик для осмысленных row groups. Рабочие эвристики: партиционируйте только по колонкам низкой кардинальности, по которым реально фильтруете; цельтесь в файлы ~100 МБ–1 ГБ; и *сортируйте внутри файлов* по следующей по частоте колонке фильтрации, чтобы обрезка по статистикам подхватывала там, где партиционирование заканчивается. Эволюция схемы живёт по той же честной логике: добавление nullable-колонок дёшево (старые файлы просто отдают null), переименования — замаскированные поломки (идентичность колонки — её имя), а смена типа обычно означает переписывание истории — планируйте имена и типы как API, потому что это и есть API.

<Quiz
  id="02-file-formats-quiz-2"
  lessonSlug="02-file-formats"
  lang="ru"
  question="Команда партиционирует лейк событий по dt И по customer_id (50 тыс. клиентов). Запросы стали медленнее, чем до партиционирования. Что случилось?"
  choices={[
    { label: "Пере-партиционирование: десятки тысяч каталогов в день дают миллионы крошечных файлов — доминируют листинг и чтение футера на каждый файл, а обрезка по row groups на таких файлах вообще не включается", correct: true },
    { label: "Hive-партиционирование поддерживает только один ключ", misconception: "Несколько ключей механически в порядке — dt плюс регион встречаются повсеместно. Проблема в кардинальности ключа, а не в числе ключей." },
    { label: "Parquet не умеет фильтровать по customer_id, если это не ключ партиции", misconception: "Статистики row groups фильтруют внутри файлов независимо от партиционирования — особенно если файлы отсортированы по customer_id. Обрезка партиций — один механизм из двух, а не единственный." },
    { label: "zstd хуже жмёт маленькие файлы, и лейк распух", misconception: "Коэффициент сжатия на мелких файлах деградирует лишь слегка. Регрессия — это метаданные и накладные на открытие, умноженные на миллионы файлов: проблема паттерна IO, а не кодека." },
  ]}
/>

## Правило

CSV — на **краях**: люди, таблицы Excel, легаси, не говорящее ни на чём другом; принимается один раз, валидируется и конвертируется. Parquet — **внутри** конвейера, где схема едет вместе с данными, читатели обрезают вместо того, чтобы парсить, а три команды, читающие один файл, получают одну и ту же таблицу по построению.

</Explanation>

<StackDiagram
  label="Внутри Parquet-файла: что читатель открывает первым и что может пропустить"
  hue="var(--d-systems)"
  layers={[
    { label: "Футер", note: "схема + статистики групп" },
    { label: "Row group", note: "~1 млн строк, min/max" },
    { label: "Column chunk", note: "одна колонка, обрезаемо" },
    { label: "Страница", note: "~1 МБ, единица кодирования" },
    { label: "Значения", note: "dict + RLE, snappy/zstd" },
  ]}
  caption="Читатель начинает с футера: схема плюс статистики каждой row group. Обрезка колонок смещается только к нужным column chunks; predicate pushdown пропускает row groups, чьи min/max не могут совпасть; распаковываются только выжившие страницы. Обрезка — это раскладка файла, а не магия движка."
/>

<KeyTakeaway>CSV хранит символы: каждый читатель заново парсит и угадывает типы — поэтому индексы теряют нули, а одна пустая ячейка делает колонку id плавающей. Parquet хранит типизированные колонки в row groups со статистиками в футере — обрезка колонок и predicate pushdown встроены в раскладку и дают ~5–10x по размеру и времени чтения против CSV. Arrow — общая раскладка в памяти, делающая обмен pandas/polars/DuckDB zero-copy; Feather/IPC — её дисковая форма. Партиционируйте только по низкой кардинальности, держите файлы от ~100 МБ, сортируйте внутри файлов; эволюционируйте схему аддитивно. CSV на краях, Parquet внутри.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="02-file-formats-retrieval"
  lang="ru"
  questions={[
    { q: "Разберите внутренности Parquet-файла — футер, row groups, column chunks, страницы — и объясните, как фильтрованное проецированное чтение использует каждый уровень.", a: "Футер (в конце файла) хранит типизированную схему и метаданные каждой row group: байтовые смещения плюс min/max и счётчики null по каждому column chunk. Row group — горизонтальный срез, обычно от десятков до ~128 МБ или порядка миллиона строк. Внутри каждая колонка — column chunk, разбитый на страницы ~1 МБ — единицу кодирования (словарь, RLE) и сжатия (snappy — быстро и крупнее, zstd — меньше за умеренный CPU). Чтение 3 колонок с фильтром на один день идёт так: открыть футер, собрать список колонок (проекция — смещения только к этим чанкам), проверить фильтр против min/max каждой row group (predicate pushdown — пропустить группы, которые не могут совпасть; эффективно, когда писатель отсортировал по колонке фильтра), распаковать только выжившие страницы. Поэтому проецированное фильтрованное чтение Parquet укладывается в доли секунды там, где CSV — в десятки секунд: CSV обязан распарсить каждый байт, чтобы хотя бы узнать типы." },
    { q: "Сформулируйте правило форматов с честными числами и дисциплину партиционирования и эволюции схемы, которая держит лейк быстрым.", a: "Правило: CSV только на краях (люди, легаси), конвертируется и валидируется один раз на входе; Parquet внутри, потому что схема едет в файле — отказ «три команды, три схемы» становится невозможным. Числа для таблицы на 10 млн строк и ~20 колонок: CSV ~1,2 ГБ, gzip-CSV ~300–400 МБ, Parquet-zstd ~150–250 МБ; чтение падает с десятков секунд (парсинг CSV) до нескольких секунд на весь файл и долей секунды на проекцию. Сжатие: zstd для хранения, snappy — когда в сканах доминирует CPU распаковки. Партиционирование: hive-каталоги key=value отбрасываются до открытия файлов — но только ключи низкой кардинальности, по которым фильтруете; высокая кардинальность рождает миллионы крошечных файлов, где правят листинг и чтение футеров (отказ мелких файлов). Держите файлы ~100 МБ–1 ГБ и сортируйте внутри по следующей колонке фильтрации, чтобы статистики row groups продолжали обрезку. Эволюция схемы: аддитивные nullable-колонки дёшевы, переименования — поломки (идентичность — имя), смена типа — переписывание; относитесь к именам и типам как к API." },
  ]}
/>

<Recap lang="ru">
Форматы — это контракты, и CSV — отсутствие контракта: символы, чей смысл каждый читатель выводит заново; так один экспорт породил три таблицы — исчезнувшие ведущие нули, колонка id, поплывшая из-за единственной пустой ячейки, прибитые вручную dtype, разъехавшиеся с источником. JSON и NDJSON добавляют вложенность, оставаясь многословным типово-двусмысленным текстом. Parquet меняет физику: футер несёт типизированную схему и min/max-статистики каждой row group; данные живут в row groups из column chunks из страниц ~1 МБ, кодированных словарём и RLE под snappy (быстро) или zstd (компактно). Обрезка колонок и predicate pushdown поэтому следуют из самой раскладки файла — 3 колонки из 40 это паттерн смещений, один день сортированных таймстемпов — горстка row groups, и та же таблица падает с ~1,2 ГБ CSV до ~200 МБ Parquet-zstd, а чтения — с десятков секунд до долей секунды на проекции. Arrow — половина истории в памяти: одна колоночная раскладка, общая для pandas, polars и DuckDB, превращающая обмен в передачу указателей, с IPC/Feather как дисковой формой почти без парсинга. Партиционируйте в hive-стиле по ключам низкой кардинальности, держите файлы около 100 МБ–1 ГБ, сортируйте внутри, эволюционируйте схему аддитивно — и держите CSV там, где ему место: на краях, с Parquet внутри.
</Recap>
