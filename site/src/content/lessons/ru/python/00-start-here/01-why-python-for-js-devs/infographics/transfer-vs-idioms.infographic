infographic compare-binary-horizontal-underline-text-vs
data
  title От JS/TS к Python
  desc Ментальная модель переносится; идиомы — нет
  compares
    - label Переносится — как дома
      children
        - label Динамика + интерпретация
          desc Типы на значениях; ошибки в рантайме, как в JS
        - label Настоящий REPL
          desc python / IPython / Jupyter — мышца консоли
        - label Функции — first-class
          desc Передаёшь, возвращаешь, замыкаешь над переменными
        - label Comprehensions ≈ map/filter
          desc [f(x) for x in arr] — идиоматичная форма
    - label Другое — спотыкаешься
      children
        - label Отступы — это грамматика
          desc Нет скобок; сбитый отступ — синтаксическая ошибка
        - label None, не null/undefined
          desc Один сентинел отсутствия; проверка через is None
        - label Batteries included
          desc Сначала загляни в огромный stdlib, потом pip install
        - label venv + pip + PyPI + pyproject.toml
          desc Заменяет node_modules + npm; сначала создай venv
theme
  palette #14b8a6 #f59e0b
