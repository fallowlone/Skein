---
concepts:
- python-c-extensions
deepensInto: []
estMin: 18
lang: ru
lessonType: topic
level: senior
order: 3
prereqs: []
slug: "03-c-extensions-honestly"
sources:
- https://docs.python.org/3/extending/extending.html
- https://cython.readthedocs.io/en/latest/
- https://numpy.org/doc/stable/
spiral: []
status: ready
summary: 'Время интерпретатора уходит на диспетчеризацию, боксинг, refcount; C обходит все три. Лестница: векторизация numpy, типы Cython, cffi/ctypes, C-API в конце. Расширения могут отпускать GIL; поэлементное пересечение границы съедает выигрыш. Wheels и manylinux — цена дистрибуции.'
title: 'C-расширения честно: лестница вариантов, граница GIL и налог на маршалинг'
track: "python"
unit: "08-performance"
---

import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import StackDiagram from "~/components/diagram/StackDiagram.astro";

<Hook>
Команде рисков понадобились попарные расстояния по 10 000 векторов — сто миллионов операций с float. Чистый Python: 90 секунд, далеко за окном батча. Инженер обернул C-ядро расстояния через ctypes и вызвал его из того же вложенного Python-цикла, уверенный, что C всё исправит. Новое время: 110 секунд — *медленнее*. Каждый из ста миллионов вызовов платил примерно микросекунду пограничной стоимости — боксинг аргументов, сборка вызова, анбоксинг результата, — и это съело весь выигрыш C с добавкой. Лекарством оказался не дополнительный C, а перенос **самого цикла** через границу: один вызов `scipy.spatial.distance.cdist`, одно пересечение на всю матрицу, 0.9 секунды. Шестинедельный Cython-порт, который вот-вот должны были одобрить, превратился в однострочный дифф, а на вики команды появился урок: исход решает граница, а не язык.
</Hook>

<Crux>C выигрывает, минуя диспетчеризацию, боксинг и refcount. Пересекайте границу раз на массив, не на элемент. Сначала numpy, C-API в конце.</Crux>

<Explanation>

## Куда на самом деле уходит время интерпретатора

Когда горячий Python-цикл медленный, время редко сидит в вашей арифметике — оно в механике вокруг неё. Каждый байткод проходит через диспетчерский цикл ceval. Каждое `a + b` на int проверяет типы обоих операндов, разбоксивает их из объектов кучи, считает, **выделяет объект результата** и подкручивает счётчики ссылок на всём, чего коснулось. Нативное сложение — ~1 нс; интерпретируемая версия стоит десятки наносекунд даже после того, как специализирующий интерпретатор 3.11 сузил разрыв. Медленность структурна — фиксированный оверхед на *операцию*, — поэтому всякое настоящее лекарство имеет одну форму: либо заставить каждый шаг интерпретатора делать больше работы (векторизовать), либо скомпилировать шаги прочь (расширить).

## Реалистичная лестница вариантов

Беритесь за них по порядку и останавливайтесь на первой ступени, которая закрывает SLO:

1. **Векторизация numpy / pandas** — кто-то уже написал, отладил и дистрибутирует этот C. Если данные ложатся в массивы, это дни работы за выигрыш 10-100x на числовых циклах, без новой сборочной инфраструктуры.
2. **Cython** — постепенно: ваш существующий Python компилируется как есть на скромные ~1.2-1.5x, а `cdef`-типы и типизированные memoryview на горячем цикле открывают 10-100x. Кодовая база остаётся, появляется шаг компиляции — и всё из раздела о дистрибуции ниже.
3. **cffi / ctypes** — когда быстрый код уже существует как C-библиотека. ctypes не требует компилятора, но берёт порядка микросекунды за вызов; cffi быстрее и с более здравым API. Оба — для *обёртывания*, а не для ускорения цикла повызовно.
4. **Голый C-API** — последним. Вы вручную управляете `Py_INCREF`/`Py_DECREF`: один пропущенный decref течёт вечно, один лишний портит кучу и взрывается позже в невиновном коде. Пересборка на каждую минорную версию Python, если не целитесь в стабильный ABI.

<Quiz
  id="03-c-extensions-honestly-quiz-1"
  lessonSlug="03-c-extensions-honestly"
  lang="ru"
  question="Профиль показывает 80% CPU сервиса в одном чисто-питоновском цикле с арифметикой над массивами float. Первый ход по лестнице вариантов?"
  choices={[
    { label: "Переписать цикл операциями над массивами numpy — этот C уже существует, изменение масштаба дней, и числовые циклы — ровно его случай 10-100x", correct: true },
    { label: "Начать Cython-порт модуля — типы дают наибольшее ускорение", misconception: "Cython здесь может сравняться с numpy, но добавляет проекту компилятор, сборочную матрицу и дистрибуцию wheel. Лестница существует потому, что первая ступень даёт тот же порядок величины на C, который сопровождает кто-то другой." },
    { label: "Обернуть внутреннее вычисление C-функцией через ctypes и звать её на каждый элемент", misconception: "Поэлементные ctypes-вызовы платят ~1 мкс маршалинга каждый — на миллионах элементов пограничный налог превышает весь выигрыш C, ровно история из хука. Границу должен пересекать цикл, а не элемент." },
    { label: "Написать модуль на C-API ради максимального контроля", misconception: "Максимальный контроль — это и максимальная ответственность: ручной refcount, сегфолты вместо трейсбеков, пересборки под версии. Это последняя ступень именно потому, что ранние забирают большую часть выигрыша за долю цены." },
  ]}
/>

## Граница GIL

GIL сериализует *исполнение байткода*, а не всё подряд. Расширение может отпустить его вокруг чисто-C секций — `Py_BEGIN_ALLOW_THREADS` — и крупные так и делают: numpy на заметных операциях, hashlib, zlib, компрессия, большинство I/O. Практическое следствие: `ThreadPoolExecutor`, гоняющий крупные numpy-матмулы, насыщает все ядра, а те же потоки на чисто-питоновской арифметике сериализуются на GIL и используют одно. Вопрос GIL никогда не звучит как «потоки — да или нет», а как «держит ли его горячая секция». Это же мост к уроку 4: расширения поменяли математику потоков задолго до free-threaded сборок.

## Налог на маршалинг

Пересечение границы Python/C конвертирует PyObject в C-значения и обратно, собирает вызов и трогает счётчики ссылок — от 100 нс до 1 мкс за пересечение в зависимости от механизма. Десять миллионов поэлементных вызовов — это 1-10 *секунд* чистой пограничной стоимости до всякой полезной работы. Правило: **векторизуйте весь цикл, а не внутренний вызов** — передайте массив один раз, получите один результат. По той же причине pandas `.apply(python_fn)` — не векторизация: датафрейм уходит в C, но ваша функция вытаскивает каждую строку обратно.

## Сборка и дистрибуция, честно

Скомпилированный код распространяется wheel-ами: по одному на ОС x архитектуру x минорную версию Python — если не целиться в стабильный ABI (abi3), схлопывающий ось Python. На Linux ответ на вопрос «какой дистрибутив» — **manylinux**: сборка внутри контейнеров со старым glibc, чтобы один wheel работал на всех дистрибутивах. Пропустите эту работу — и пользователи будут компилировать из исходников, а ваш трекер задач станет хелпдеском по C-тулчейнам на машинах, которых вы никогда не видели.

## Сопровождение: сегфолты вместо трейсбеков

Ошибка refcount не поднимает исключение; она портит кучу и роняет процесс минуты спустя в коде, который ничего плохого не делал. Отладка переезжает из pdb в gdb и корки. Каждое обновление Python — пересборка и перетест матрицы. Расширение быстро в день 1 и дорого в день 400 — закладывайте в бюджет второе число.

## Правило, которое запирает весь урок

Прежде чем браться за любую ступень, докажите, что горячий цикл **упирается в интерпретатор**: py-spy `--native` или cProfile из урока 1. Если время уже внутри numpy, json или драйвера базы, вы будете переписывать клей вокруг и так сишного ядра — вся цена, никакого выигрыша.

<Quiz
  id="03-c-extensions-honestly-quiz-2"
  lessonSlug="03-c-extensions-honestly"
  lang="ru"
  question="Коллега заменил чисто-питоновскую внутреннюю функцию C-версией через ctypes внутри того же цикла на 10 млн итераций. Задача стала медленнее. Почему?"
  choices={[
    { label: "Каждый вызов платит ~1 мкс маршалинга — боксинг аргументов, сборка вызова, анбоксинг результата, — и 10 млн пересечений пограничного налога превышают экономию C", correct: true },
    { label: "ctypes исполняет C-код в интерпретируемом режиме, так что это не настоящий C", misconception: "ctypes исполняет подлинную скомпилированную функцию; на стороне C ничего не интерпретируется. Чего он не может избежать — повызовной конвертации Python-объектов в C-типы на границе: цена в пересечении, не в коде." },
    { label: "GIL не даёт C-функции работать, и вызовы выстраиваются за интерпретатором", misconception: "Вызов спокойно идёт с удерживаемым GIL — однопоточный код за него не конкурирует. Отпускание GIL важно для параллелизма между потоками, что ортогонально этому однопоточному замедлению." },
    { label: "C-функции быстрее Python только на нагрузках выше порога размера, которого цикл не достигает", misconception: "Тело на C быстрее при любом размере; дело не в этом. Фиксированная повызовная пограничная цена доминирует, когда работа на вызов крошечная, — перестройте так, чтобы один вызов обрабатывал весь массив, и C выигрывает сразу." },
  ]}
/>

</Explanation>

<StackDiagram
  label="Слои, которые C пропускает, — и единственная граница, которая берёт налог"
  hue="var(--d-systems)"
  layers={[
    { label: "ваш Python-цикл", note: "скорость разработки" },
    { label: "диспетчеризация байткода ceval", note: "~10-100 нс/оп" },
    { label: "боксинг + трафик refcount", note: "взбалтывание кучи" },
    { label: "граница Python/C — маршалинг", note: "0.1-1 мкс/пересечение" },
    { label: "чистое C-ядро", note: "~1 нс/оп, может отпустить GIL" },
  ]}
  caption="Векторизованный код пересекает границу один раз на массив и живёт в нижнем слое; поэлементный паттерн платит за пересечение 10 млн раз — вот как C-функция делает задачу медленнее. Расширения, отпускающие GIL в нижнем слое, — единственное место, где потоки по-настоящему параллелятся."
/>

<KeyTakeaway>Стоимость интерпретатора структурна — диспетчеризация, боксинг, refcount на операцию, — поэтому лекарства либо батчат работу на шаг, либо компилируют шаги прочь. Лестница: сначала numpy/pandas (этот C существует), потом Cython с типами, cffi/ctypes для обёртывания готовых библиотек, голый C-API последним (баги refcount, сегфолты, пересборки под версии). Пересекайте границу раз на массив — поэлементные пересечения по 0.1-1 мкс стирают выигрыш. Расширения, отпускающие GIL, — то место, где потоки наконец грузят все ядра. Докажите интерпретаторность цикла до первой строки C.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="03-c-extensions-honestly-retrieval"
  lang="ru"
  questions={[
    { q: "Перескажите лестницу вариантов с честной ценой каждой ступени — и ворота, которые надо пройти до подъёма вообще.", a: "Ворота: сначала профиль (py-spy --native / cProfile) и подтверждение, что горячий цикл упирается в интерпретатор, — если время уже сидит в C-коде numpy/json/драйвера, расширение перепишет клей с нулевым выигрышем. Ступень 1: векторизация numpy/pandas — отлаженный, дистрибутируемый C; дни работы, 10-100x на числовых циклах, без сборочной инфраструктуры. Ступень 2: Cython — компилирует ваш Python как есть на ~1.2-1.5x, типизированные cdef/memoryview на горячем цикле дают 10-100x; цена — шаг компиляции плюс дистрибуция wheel. Ступень 3: cffi/ctypes — обёртывание существующих C-библиотек; ctypes без компилятора, но ~1 мкс/вызов, cffi быстрее; никогда для поэлементных горячих вызовов. Ступень 4: голый C-API — ручные Py_INCREF/DECREF, где пропущенный decref течёт, а лишний портит кучу; сегфолты вместо трейсбеков, gdb вместо pdb, пересборка на каждую минорную версию, если не abi3. Останавливайтесь на первой ступени, закрывающей SLO." },
    { q: "Объясните налог на маршалинг и границу GIL — и как каждый меняет проектное решение.", a: "Маршалинг: каждое пересечение Python-в-C конвертирует PyObject в C-значения и обратно, собирает вызов и трогает refcount — 100 нс - 1 мкс за раз. Проектное следствие: всё решает гранулярность; 10 млн поэлементных вызовов стоят 1-10 с чистого пограничного оверхеда (почему ctypes-версия из хука проиграла чистому Python и почему pandas .apply с Python-функцией — не векторизация), тогда как один вызов на весь массив амортизирует пересечение в ничто. GIL: он сериализует байткод, но C-секции могут его отпускать (Py_BEGIN_ALLOW_THREADS) — numpy, hashlib, zlib, большинство I/O так делают. Проектное следствие: ThreadPoolExecutor над ядрами с отпущенным GIL насыщает все ядра без оверхеда multiprocessing, а те же потоки на чисто-питоновской арифметике сериализуются на одно ядро. Спрашивайте о любой горячей секции: кто держит GIL и как часто мы пересекаем границу." },
  ]}
/>

<Recap lang="ru">
Python-циклы медленны по структурным причинам — диспетчеризация ceval на байткод, боксинг каждого значения в объекты кучи, трафик refcount на каждом связывании, — поэтому C выигрывает не волшебной арифметикой, а пропуском этой механики, и каждое лекарство — либо «больше работы на шаг интерпретатора», либо «меньше шагов интерпретатора». Лестница упорядочивает лекарства по совокупной стоимости владения: numpy и pandas вручают сопровождаемый C за изменение масштаба дней; Cython позволяет типизировать только горячий цикл и сохранить кодовую базу; cffi и ctypes обёртывают уже существующий C; голый C-API стоит последним, потому что ручной refcount превращает баги в отложенные сегфолты, а каждый релиз Python — в пересборку. Архитектурой правят два пограничных факта: маршалинг стоит 0.1-1 мкс за пересечение, так что цикл обязан пересекать границу раз на массив, иначе налог пожирает выигрыш — 110-секундный ctypes-урок из хука; и расширения могут отпускать GIL внутри чисто-C секций — единственное место, где Python-потоки честно параллелятся по ядрам. Дистрибуция — часть цены: wheels на платформы, manylinux для Linux, abi3 для сжатия матрицы; сопровождение тоже — трейсбеки уступают коркам. И всё это заперто уроком 1: измерьте, что цикл упирается в интерпретатор, прежде чем написать строку C.
</Recap>
