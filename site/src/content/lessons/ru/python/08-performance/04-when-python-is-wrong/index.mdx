---
concepts:
- python-performance-limits
deepensInto: []
estMin: 16
lang: ru
lessonType: topic
level: senior
order: 4
prereqs: []
slug: "04-when-python-is-wrong"
sources:
- https://docs.python.org/3/library/asyncio.html
- https://peps.python.org/pep-0703/
- https://docs.python.org/3/howto/free-threading-python.html
spiral: []
status: ready
summary: 'Триаж узкого места: I/O-bound и C-library-bound — Python подходит; интерпретаторные циклы в масштабе стоят 10-50x на ядро. Free-threaded 3.13 меняет математику потоков, не скорость потока. Выносите горячий путь стренглером; Python — там, где решает скорость разработки.'
title: 'Когда Python — не тот инструмент: дерево решений и арифметика зарплат против серверов'
track: "python"
unit: "08-performance"
---

import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";
import Infographic from "~/components/diagram/Infographic.astro";
import rewriteArithmetic from "./infographics/rewrite-arithmetic.svg?raw";

<Hook>
Эндпоинт скоринга фич сжигал 35 мс чисто-питоновского CPU на запрос. На 4 000 RPS это означало двенадцать 8-ядерных подов, занятых только интерпретацией одного и того же цикла. Один инженер переписал этот единственный эндпоинт на Go — тот же алгоритм, строка в строку: 1.1 мс на запрос, и флот сжался с двенадцати подов до двух. Счёт за вычисления упал на $14k в месяц. Следующий ход руководства был предсказуем: RFC «Мигрировать все сервисы на Go». Аудит убил его одним слайдом — остальные тридцать Python-сервисов *вместе* использовали меньше ядер, чем один старый скорер; это были CRUD-клей, оркестрация и батчи, простаивающие на I/O, где Go не купил бы ничего, кроме года переписывания. Скорер был счётом. Остальные счётом не были никогда. Навык сениора — не выбрать язык, а найти счёт до того, как выставлять смету.
</Hook>

<Crux>I/O или C-библиотеки — Python годится. Интерпретатор в масштабе стоит 10-50x на ядро: выносите этот путь, клей оставьте.</Crux>

<Explanation>

## Классифицируйте узкое место, прежде чем судить язык

«Python медленный» — не диагноз. Диагноз приходит из урока 1 — py-spy на реальной нагрузке — и ложится в одну из трёх корзин:

- **I/O-bound** — сэмплы сидят в ожиданиях сокетов, CPU в основном простаивает. Python годится: простаивающее ядро простаивает на любом языке. Рычаги — asyncio, пулы соединений, конкурентность, а не переписывание. Go-порт сервиса, ждущего Postgres, всё так же ждёт Postgres.
- **C-library-bound** — фреймы живут внутри numpy, torch, драйвера базы. Python годится: он уже тонкий оркестратор над скомпилированными ядрами (вся суть урока 3). Рычаги — батчинг и лучшее кормление C; переписывание реализует заново клей вокруг тех же ядер.
- **Interpreter-bound** — горячи чисто-питоновские фреймы, и это в масштабе. Вот где разрыв 10-50x на ядро — реальные деньги. Эскалируйте лестницу: сначала алгоритм, потом векторизация или расширение (урок 3), и лишь когда путь остаётся горячим и остаётся счётом — переписывайте *этот путь*.

## Free-threaded 3.13 меняет математику потоков, не скорость

Free-threaded сборка (PEP 703) делает GIL необязательным: чисто-питоновские потоки наконец могут работать параллельно. Чего она **не** меняет — скорость одного потока: ранние free-threaded сборки даже платят оверхед в единицы процентов, и специализирующему интерпретатору потребовалось время, чтобы вернуться. Сдвигается одна граница: «для CPU-параллелизма — процессы или C с отпущенным GIL» смягчается в сторону потоков. Межъядерный интерпретаторный разрыв, питающий случаи ниже, выживает нетронутым: сорок медленных ядер параллельно — всё ещё сорок медленных ядер.

## Где Python — не тот инструмент

Три честных случая, каждый держится на числе из этого юнита:

1. **Постоянные CPU-bound сервисы в масштабе.** 10-50x медленнее на ядро — значит 10-50x флота за ту же пропускную способность. На 2 ядрах всем всё равно; на двенадцати подах хука — или на сотнях ядер — интерпретатор *и есть* счёт за инфраструктуру.
2. **Пути с жёсткой латентностью.** Паузы поколенческого GC (gen-2 проход по большой куче — миллисекунды), джиттер аллокатора и разброс интерпретатора воюют с p99 SLO в единицы миллисекунд. Смягчить можно — `gc.freeze`, настройка порогов, — но вы торгуетесь с платформой, а SLO не торгуется.
3. **Высококардинальные данные при дефиците памяти.** Арифметика урока 2: 28-байтные int и 250-байтные объекты превращают 10 млн мелких записей в гигабайты, которые Go или Rust держат в сотнях мегабайт. Если рабочее множество обязано влезать в RAM на узел, язык выбирает пообъектный оверхед.

Все три случая имеют одну форму: оверхед Python реален, измерим и *уже является счётом* — не теоретической угрозой. Без устойчивого числа ядер из случая 1, жёсткого latency-таргета из случая 2 или RAM-скалы из случая 3 арифметика почти наверняка отклонит переписывание до того, как вы допишете RFC.

<Quiz
  id="04-when-python-is-wrong-quiz-1"
  lessonSlug="04-when-python-is-wrong"
  lang="ru"
  question="Для какого из этих сервисов Python действительно не тот инструмент?"
  choices={[
    { label: "Прайсинг-движок с чисто-питоновским циклом скоринга на 300 постоянных ядрах — разрыв 10-50x на ядро напрямую умножается в стоимость флота", correct: true },
    { label: "API-гейтвей, чей py-spy-профиль на 95% — ожидания сокетов", misconception: "Этот сервис I/O-bound: его ядра простаивают, а простаивающее ядро простаивает и на Go. Переписывание воспроизвело бы то же ожидание другим синтаксисом; рычаги — asyncio и пулы." },
    { label: "ETL-джоба, проводящая время внутри pandas groupby и parquet-I/O", misconception: "Это время уже внутри скомпилированного C — Python там тонкий оркестратор. Переписывание оркестрации заново реализует клей вокруг тех же ядер с примерно нулевым межъядерным выигрышем." },
    { label: "Любой сервис с более чем 1 000 запросов в секунду", misconception: "RPS сам по себе не говорит ничего: 1 000 RPS I/O-bound проксирования спокойно живёт на паре Python-подов. Решающие переменные — куда падают сэмплы (интерпретатор, I/O, C) и сколько ядер на кону." },
  ]}
/>

## Где Python остаётся прав: арифметика зарплат против серверов

Клей, оркестрация, внутренние инструменты, data science, управляющие планы ETL — домены, где скорость итераций доминирует над вычислениями. Проговорите арифметику явно, потому что она решает реальные RFC. Полная стоимость сениора — $150-300k в год. Облачный vCPU-год — примерно $200-600. Python-сервис, тратящий 10x на 4 ядрах, сжигает $10-20k в год — меньше одного инженеро-*месяца*. Переписывание должно вытеснять больше ядро-долларов, чем стоит в инженеро-долларах, — *навсегда*, включая сопровождение. Большинство сервисов эту планку не берут никогда. Скорер из хука взял её десятикратно — 240 потраченных ядер, — именно поэтому двигать стоило один сервис.

<Infographic
  svg={rewriteArithmetic}
  label="Сравнение бок о бок интерпретаторных сервисов на 4 и на 300 ядрах, каждый 10x медленнее на ядро: на 4 ядрах теряется ~3.6 ядра (~$1.4k/год) и переписывание (~$25-50k) не окупается никогда, а на 300 ядрах теряется ~270 ядер (~$108k/год) и то же переписывание окупается меньше чем за год."
  caption="Одинаковая 10x неэффективность, одинаковая цена переписывания — но вердикт переворачивается, потому что превысить инженеро-доллары обязаны абсолютные вытесненные ядро-доллары, а не процент."
  hue="var(--d-infra)"
/>

<Quiz
  id="04-when-python-is-wrong-quiz-2"
  lessonSlug="04-when-python-is-wrong"
  lang="ru"
  question="Python-сервис живёт на 4 vCPU (~$1.6k/год при $400/vCPU-год). RFC предлагает Go-переписывание за 2 инженеро-месяца, обещая 10x эффективности на ядро. Вердикт?"
  choices={[
    { label: "Отклонить: переписывание стоит ~$25-50k инженерного времени ради экономии максимум ~$1.4k/год вычислений — оно не окупится никогда, плюс вечная поддержка двух стеков", correct: true },
    { label: "Одобрить: 10x эффективности — это 10x экономии, а она всегда копится", misconception: "10x от маленького числа — всё ещё маленькое число: вся растрата ~$1.4k/год. Процент не важен; решают только вытесненные абсолютные ядро-доллары против потраченных инженеро-долларов — здесь соотношение ~20:1 против." },
    { label: "Одобрить, если команда воодушевлена — мораль и обучение покрывают цену", misconception: "Обучение ценно, но RFC заявляет обоснование эффективностью, и арифметика его опровергает. Финансирование переписываний на вайбе — так рождаются мандаты на миграцию тридцати сервисов; финансируйте обучение явно." },
    { label: "Отклонить, потому что Go не дал бы 10x на этой нагрузке", misconception: "На интерпретаторном цикле 10x вполне правдоподобны (хук видел ~30x). Предложение проваливается на экономике этого масштаба, не на осуществимости — на 300 ядрах те же числа его одобряют." },
  ]}
/>

## Паттерн миграции: стренглер, а не большой взрыв

Когда путь действительно берёт планку — выносите его, а не переписывайте мир. Поставьте измеренный горячий путь за контракт (protobuf или OpenAPI), реализуйте этот один сервис на Go или Rust, прогоните теневой трафик, сдвигайте проценты, сохраните Python-оркестрацию вокруг. Большие взрывы вязнут, потому что 90% кода, никогда не бывшие счётом, всё равно надо переписать баг-в-баг, тогда как стренглер отгружает те 10%, которые платят. Каждый следующий вынос оправдывается собственной ядерной арифметикой — а в системе из хука после переезда скорера *не квалифицировался больше ни один сервис*.

</Explanation>

<FlowDiagram
  label="Честное дерево решений: классифицируйте узкое место и эскалируйте только тот путь, который и есть счёт"
  hue="var(--d-systems)"
  perRow={3}
  nodes={[
    { id: "prof", label: "py-spy на горячий путь", sub: "куда падают сэмплы?" },
    { id: "io", label: "I/O-bound", sub: "доминируют ожидания сокетов" },
    { id: "clib", label: "C-library-bound", sub: "фреймы numpy/torch/драйвера" },
    { id: "interp", label: "Interpreter-bound", sub: "горячи чисто-Py фреймы" },
    { id: "stayio", label: "Остаться на Python", sub: "asyncio, пулы, воркеры" },
    { id: "stayc", label: "Остаться на Python", sub: "батчить, лучше кормить C" },
    { id: "ladder", label: "Подняться по лестнице", sub: "алгоритм, numpy, Cython" },
    { id: "extract", label: "Вынести в Go/Rust", sub: "стренглер за контрактом" },
  ]}
  edges={[
    { from: "prof", to: "io", label: "ждёт" },
    { from: "prof", to: "clib", label: "уже в C" },
    { from: "prof", to: "interp", label: "интерпретирует" },
    { from: "io", to: "stayio", label: "простой есть простой" },
    { from: "clib", to: "stayc", label: "переписать клей = 0x" },
    { from: "interp", to: "ladder", label: "сначала урок 3" },
    { from: "ladder", to: "extract", label: "всё ещё счёт" },
  ]}
  caption="Две ветви из трёх кончаются «остаться на Python», потому что ядра простаивают или уже исполняют C. Эскалирует только интерпретаторная ветвь — и переписывание в ней последний шаг, принимаемый для каждого пути отдельно, когда сходится арифметика зарплат против серверов, и никогда — мандат на весь флот."
/>

<KeyTakeaway>Классифицируйте до приговора: I/O-bound оставляет Python (простаивающие ядра простаивают на любом языке), C-library-bound оставляет Python (он уже оркеструет скомпилированные ядра), interpreter-bound в масштабе — где 10-50x на ядро становятся счётом. Free-threaded 3.13 меняет математику потоков, не скорость потока. Python не подходит для постоянного CPU в масштабе флота, жёстких p99-путей в единицы мс и высококардинальных данных при дефиците RAM; подходит везде, где инженеро-доллары доминируют над ядро-долларами. Когда путь проходит арифметику — выносите его стренглером за контракт, клей оставляя.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="04-when-python-is-wrong-retrieval"
  lang="ru"
  questions={[
    { q: "Пройдите дерево решений для медленного Python-сервиса: три корзины, улики каждой и предписанный ход.", a: "Сначала py-spy на реальной нагрузке — корзину определяет место падения сэмплов. I/O-bound: стеки в ожиданиях сокетов/базы, CPU простаивает; остаться на Python, применить asyncio, пулы, больше воркеров — переписывание воспроизводит то же ожидание. C-library-bound: фреймы внутри numpy/torch/драйверов; остаться на Python — это клей над скомпилированными ядрами, улучшайте батчинг и раскладку данных. Interpreter-bound: чисто-питоновские фреймы горячи под постоянной нагрузкой; эскалировать по порядку — алгоритм, векторизация или Cython/расширение (урок 3), — и только если путь остаётся горячим И число ядер делает его счётом, вынести этот путь в Go/Rust стренглер-сервисом за контрактом protobuf/OpenAPI, с теневым трафиком и постепенным сдвигом, сохраняя Python-оркестрацию. Free-threaded 3.13 лишь смягчает ограничение параллелизма (потоки вместо процессов); скорость на ядро не меняется, так что корзину он не переключает никогда." },
    { q: "Сформулируйте арифметику зарплат против серверов и примените её к интерпретаторным сервисам на 4 и на 300 ядрах.", a: "Входные данные: полная стоимость сениора $150-300k в год; облачный vCPU-год примерно $200-600. Правило: переписывание должно вытеснять больше ядро-долларов, чем стоит в инженеро-долларах, перманентно, включая поддержку двух стеков. Сервис на 4 ядрах при 10x растрате: ~3.6 потраченных ядра, около $1.4k/год при $400/vCPU — переписывание за 2 месяца (~$25-50k полной стоимости) не окупается никогда; отклонить и потратить усилие там, где зарабатывает скорость итераций. Сервис на 300 ядрах при том же соотношении: ~270 потраченных ядер, ~$108k/год — переписывание одного горячего пути окупается за год и копится дальше; одобрить, но как стренглер-вынос измеренного пути, а не миграцию флота, потому что окружающие сервисы — I/O-bound клей, где та же арифметика ход отклоняет. Аудит из хука — образец: один сервис был счётом на 240 ядер; тридцать сервисов вместе — нет." },
  ]}
/>

<Recap lang="ru">
Этот урок — вердиктный шаг юнита. Уроки 1-3 производят улики — куда падают сэмплы, сколько стоят объекты, что расширение может и не может купить, — а дерево решений превращает улики в ход. I/O-bound и C-library-bound сервисы остаются на Python не из лояльности, а потому, что их ядра простаивают или уже исполняют скомпилированные ядра, и переписывание покупает синтаксис. Интерпретаторные пути под постоянной нагрузкой — место, где структурный разрыв 10-50x на ядро конвертируется в размер флота, и даже там эскалация идёт: алгоритм, векторизация, расширение — и лишь потом переписывание. Free-threaded 3.13 (сборка Python без обязательного GIL) смягчает правило параллелизма, не трогая скорость на ядро. Каждый ход запирает экономика: инженеро-годы стоят шестизначно, vCPU-годы — сотни, поэтому переписывание обязано вытеснять больше ядро-долларов, чем потребляет инженеро-долларов навсегда, — арифметика, которая на одном дыхании отклоняет тщеславное переписывание 4-ядерного сервиса и одобряет вынос 300-ядерного скорера. Паттерн исполнения — стренглер: один измеренный горячий путь за контрактом, трафик сначала теневой, потом сдвигаемый, а Python сохраняет оркестрацию, в которой он лучший. Теперь, когда увидишь RFC с предложением мигрировать сервис на Go или Rust, — знаешь первый вопрос: запусти py-spy на реальной нагрузке, классифицируй узкое место и положи ядро-долларную арифметику на стол до начала дискуссии о языке.
</Recap>
