---
concepts:
- python-profiling
deepensInto: []
estMin: 18
lang: ru
lessonType: topic
level: senior
order: 1
prereqs: []
slug: "01-profiling-cprofile-pyspy"
sources:
- https://docs.python.org/3/library/profile.html
- https://github.com/benfred/py-spy
- https://docs.python.org/3/library/timeit.html
spiral: []
status: ready
summary: 'cProfile перехватывает каждый вызов: оверхед 1.3-2x раздувает мелкие горячие функции и не видит внутренностей C. py-spy сэмплирует стеки снаружи через ptrace за ~1%: top, record-флеймграфы, dump для зависших процессов. timeit льстит. Профилируйте прод-нагрузку и перемеряйте p99.'
title: 'Профилирование с cProfile и py-spy: измерьте продакшен-нагрузку до правок кода'
track: "python"
unit: "08-performance"
---

import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import SequenceDiagram from "~/components/diagram/SequenceDiagram.astro";

<Hook>
p99 у API чекаута держался на 480 мс при SLO в 200 мс. cProfile на дев-машине указал на `compute_discounts` — 38% кумулятивного времени, — и команда потратила спринт на мемоизацию и переписывание. Микробенчмарк показал ускорение в 6 раз. Деплой сдвинул p99 с 480 до 465 мс. Потом кто-то запустил `py-spy top --pid` на живом продакшен-воркере: 61% сэмплов сидели внутри JSON-сериализации двухмегабайтного ответа — стоимость, которую дев-профиль не видел вовсе, потому что фикстура была синтетической, 2 КБ, а похуковый оверхед cProfile раздул тысячи мелких вызовов хелперов `compute_discounts`, записав работу C-сериализатора одной непрозрачной строкой. Замена сериализатора и урезание ответа — и p99 стал 170 мс. Спринт ушёл впустую из-за профилировщика, которому поверили, запустив его не на той нагрузке и не в том процессе.
</Hook>

<Crux>cProfile хукает каждый вызов и искажает мелкие функции в 1.3-2 раза; py-spy сэмплирует снаружи за ~1%. Сначала прод-нагрузка.</Crux>

<Explanation>

## cProfile: детерминированная трассировка и что искажает оверхед

cProfile — детерминированный трассировщик: он регистрирует C-уровневый профильный хук (механика `lsprof` за `sys.setprofile`), срабатывающий на **каждом** вызове и возврате функции, и копит счётчики и таймеры по функциям. Ничего не пропускается — в этом привлекательность, — но каждый вызов платит фиксированный налог на учёт, поэтому программа целиком обычно работает в 1.3-2 раза медленнее. Искажение неравномерно. Фиксированная цена на вызов огромна относительно хелпера на 200 нс, вызванного десять миллионов раз, и ничтожна относительно 50-миллисекундного запроса к базе, вызванного дважды. Поэтому мелкие горячие Python-функции выглядят хуже, чем есть, и рейтинг «главных виновников» гнётся к числу вызовов, а не к реальной стоимости.

Чтение вывода — отдельный навык: `tottime` — собственное время (без вызываемых), `cumtime` — включающее. Сортируйте по `cumtime`, чтобы найти дорогое поддерево, затем по `tottime` внутри него, чтобы найти, где реально горят циклы. `pstats` режет программно; snakeviz показывает те же данные интерактивно.

```python
import cProfile, pstats

cProfile.run("handle_request(req)", "out.prof")
p = pstats.Stats("out.prof")
p.sort_stats("cumulative").print_stats(12)  # найти дорогое поддерево
p.sort_stats("tottime").print_stats(12)     # найти собственную стоимость внутри
```

Две слепые зоны структурны. Первая: **внутренности C невидимы** — `json.dumps` или операция numpy выглядят одной непрозрачной строкой: общее время есть, разбивки внутри нет. Вторая: **потоки** — по умолчанию cProfile наблюдает только поток, в котором запущен; рабочие потоки идут непрофилированными, пока каждый не установит свой профилировщик. Под asyncio метод запуска цикла событий проглатывает всё в один cumtime, и пер-корутинная атрибуция мутнеет.

<Quiz
  id="01-profiling-cprofile-pyspy-quiz-1"
  lessonSlug="01-profiling-cprofile-pyspy"
  lang="ru"
  question="cProfile показывает `validate_row` (хелпер на 15 строк, 10 млн вызовов) первым по tottime в профиле сервиса. Какой дисциплинированный следующий шаг?"
  choices={[
    { label: "Перепроверить сэмплирующим профилировщиком — похуковый налог сильнее всего раздувает мелкие часто вызываемые функции, рейтинг смещён к числу вызовов", correct: true },
    { label: "Сразу оптимизировать — cProfile детерминированный, значит число точное", misconception: "Детерминированность означает, что ни один вызов не пропущен, а не что тайминги не искажены. Хук сработал 20 млн раз вокруг этой функции; заметная доля измеренного времени — инструмент, а не код." },
    { label: "Отсортировать по cumtime — tottime всегда приписывает время листовым функциям неверно", misconception: "tottime честно сообщает собственное время; изъян не в этом. Изъян в том, что оверхед инструментирования растёт с числом вызовов, и дешёвые-но-частые функции впитывают непропорциональное измеренное время в любой колонке." },
    { label: "Игнорировать — функции дешевле микросекунды на масштабе сервиса не важны", misconception: "10 млн вызовов по 200 нс — это 2 с реального CPU; частые мелкие функции вполне бывают узким местом. Проблема — доверять одному смещённому замеру, а не сама возможность." },
  ]}
/>

## py-spy: сэмплирование снаружи процесса

py-spy переворачивает сделку. Он работает **отдельным процессом**: читает память цели — `process_vm_readv`/ptrace на Linux, mach-API на macOS, — находит состояния потоков интерпретатора и обходит структуры фреймов, восстанавливая каждый Python-стек примерно 100 раз в секунду. Без изменения кода, без импорта, без рестарта: подключение по PID к процессу, запущенному задолго до того, как вы узнали о проблеме. Цель приостанавливается на микросекунды на сэмпл, так что оверхед — около 1%. Безопасно против продакшена.

Три подкоманды покрывают полевой процесс:

- `py-spy top --pid 4137` — живой вид в стиле htop: какие функции владеют сэмплами прямо сейчас.
- `py-spy record -o prof.svg --pid 4137 --duration 60` — флеймграф; добавьте `--native`, чтобы вплести C-фреймы (именно так сериализатор из хука стал видимым), `--idle` — чтобы включить заблокированные потоки.
- `py-spy dump --pid 4137` — одномоментный стек каждого потока: инструмент для зависшего процесса. Воркер на 0% CPU, переставший разбирать очередь, диагностируется за секунды — видно, на каком именно захвате лока или чтении сокета припаркован каждый поток.

Честная оговорка: сэмплирование статистично. Всплеск на 5 мс раз в минуту на 100 Гц может не попасть в сэмплы вовсе; нужно достаточно стенового времени на исследуемом поведении, а латентность редких событий требует трейсинга, не профилирования.

## timeit и ложь микробенчмарков

`timeit` отключает GC и сообщает лучший из N повторов — он по замыслу меряет тёпло-кэшевый, разогнанный турбо-бустом идеал изолированного сниппета. Реальный сервис гоняет тот же код с холодными кэшами, перемешанной работой, вытесняющей предсказатель ветвлений, частотным скейлингом и фрагментированным аллокатором. Выигрыш в 30% по timeit может быть невидим на p99. Используйте его, чтобы сравнить две реализации одной маленькой операции при одинаковой лжи, — и никогда, чтобы предсказать латентность сервиса.

## Дисциплина

Профилируйте продакшен-форму: реальные размеры payload, реальная конкурентность, реальные распределения данных, реальные хитрейты кэшей. Синтетический цикл на дев-машине — это другая программа, случайно разделяющая исходники. Рабочий порядок: py-spy против продакшена (или реплея прод-трафика), чтобы найти **где**; cProfile и timeit локально, чтобы итерироваться над **почему**; затем перемерить p99 после деплоя — профилировщик это улика, метрика SLO — приговор.

<Quiz
  id="01-profiling-cprofile-pyspy-quiz-2"
  lessonSlug="01-profiling-cprofile-pyspy"
  lang="ru"
  question="Продакшен-воркер на 0% CPU перестал разбирать очередь и ни на что не отвечает. Первый диагностический ход?"
  choices={[
    { label: "py-spy dump --pid: читает стеки всех потоков снаружи, без рестарта, и показывает, на каком локе или сокете припаркован каждый поток", correct: true },
    { label: "Подключить cProfile и дать ему пособирать данные несколько минут", misconception: "cProfile внутрипроцессный: его должен установить код самой цели, то есть нужен рестарт — уничтожающий зависшее состояние, которое надо наблюдать. К тому же он меряет исполняемые вызовы, а задедлоченный процесс не исполняет ничего." },
    { label: "py-spy record: записать флеймграф за 60 секунд", misconception: "Флеймграф CPU-сэмплов процесса на 0% CPU пуст или шумен. record отвечает, куда уходит CPU; у этого процесса вопрос «где он застрял» — на него отвечает dump." },
    { label: "Перезапустить с подробным логированием и ждать повторения", misconception: "Это выбрасывает живую репродукцию, которая уже в руках. Зависший процесс И ЕСТЬ улика; py-spy прочитает его неинвазивно прямо сейчас, а повторение под логами может занять дни." },
  ]}
/>

</Explanation>

<SequenceDiagram
  label="Один сэмпл py-spy: чтение интерпретатора снаружи процесса"
  hue="var(--d-systems)"
  actors={["py-spy", "ядро ОС", "процесс Python"]}
  messages={[
    { from: "py-spy", to: "ядро ОС", label: "ptrace attach" },
    { from: "ядро ОС", to: "процесс Python", label: "пауза потоков" },
    { from: "py-spy", to: "процесс Python", label: "чтение фреймов" },
    { from: "процесс Python", to: "py-spy", label: "байты стека" },
    { from: "py-spy", to: "ядро ОС", label: "ptrace detach" },
    { from: "ядро ОС", to: "процесс Python", label: "резюм (~мкс)" },
  ]}
  caption="Цель работает немодифицированной и останавливается на микросекунды на сэмпл, ~100 раз/с — около 1% оверхеда против внутрипроцессного налога 1.3-2x у cProfile, чей хук срабатывает на каждом вызове и возврате."
/>

<KeyTakeaway>cProfile детерминированно трассирует каждый вызов ценой 1.3-2x, раздувая мелкие горячие функции и сводя внутренности C к одной строке; читайте cumtime для поддерева, tottime для собственной стоимости. py-spy сэмплирует стеки снаружи через ptrace за ~1% — `top` для живой картины, `record --native` для флеймграфов с C-фреймами, `dump` для зависших процессов. timeit показывает лучший тёплый случай. Ищите *где* через py-spy на прод-нагрузке, итерируйтесь над *почему* локально — и пусть p99 после деплоя выносит приговор.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="01-profiling-cprofile-pyspy-retrieval"
  lang="ru"
  questions={[
    { q: "Сопоставьте cProfile и py-spy: механизм, оверхед и что каждый не способен увидеть.", a: "cProfile — внутрипроцессный детерминированный трассировщик: C-хук срабатывает на каждом вызове и возврате, ни один вызов не пропущен, но программа замедляется в 1.3-2 раза, а фиксированный похуковый налог сильнее всего раздувает мелкие часто вызываемые функции — рейтинг гнётся к числу вызовов. Внутрь C-расширений он не видит (одна непрозрачная строка на билтин) и по умолчанию наблюдает только стартовый поток. py-spy — внепроцессный сэмплер: читает память цели через ptrace/process_vm_readv, обходит структуры фреймов ~100 раз/с, стоит ~1%, не требует изменения кода или рестарта, а с --native вплетает C-фреймы. Его слепая зона — статистика: редкие короткие события могут не попасть в сэмплы, и он профилирует размещение CPU, не память. dump вдобавок снимает стеки всех потоков для диагностики зависания — то, чего трассировщик, требующий рестарта, не сделает никогда." },
    { q: "Микробенчмарк говорит, что оптимизированная функция в 6 раз быстрее, но продакшен-p99 не сдвинулся. Перечислите ошибки измерения, дающие такой исход.", a: "Первое: профиль, выбравший цель, был смещён — похуковый оверхед cProfile сделал дешёвую частую функцию «горячей», или профиль гоняли на синтетических данных дев-машины (фикстура 2 КБ против прод-payload 2 МБ), так что реальная стоимость (например, C-сериализация) была невидима или недовзвешена. Второе: замер в стиле timeit льстит — GC выключен, лучший из N, тёплые кэши, нет перемешанной работы, — так что 6x реальны только внутри этой лжи. Третье: функция может просто не доминировать в p99: хвостовая латентность живёт в другом фрейме (локи, I/O, аллокатор), что выявляет только сэмплирование живого прод-процесса. Дисциплина: py-spy на прод-трафике для локализации, локальные инструменты для итераций и перемер метрики SLO — а не бенчмарка — как определение готовности." },
  ]}
/>

<Recap lang="ru">
cProfile и py-spy отвечают на один вопрос противоположными контрактами. Трассировщик хукает каждый вызов изнутри: полно, детерминированно, но в 1.3-2 раза медленно, со смещением против мелких горячих функций пропорционально числу вызовов, слеп внутри C и однопоточен по умолчанию — читайте его как cumtime для поддеревьев и tottime для собственной стоимости, через pstats или snakeviz. Сэмплер читает структуры фреймов интерпретатора из другого процесса через ptrace: ~1% оверхеда, без рестарта, безопасно для продакшена, C-фреймы доступны под --native, с `top` для живой атрибуции, `record` для флеймграфов и `dump` как рентгеном зависшего процесса — его единственный налог статистический, так что редкие события требуют терпения или трейсинга. timeit меряет тёпло-кэшевый лучший случай с выключенным GC и честен лишь при сравнении двух сниппетов в одинаковых условиях. Мета-навык важнее всех трёх инструментов: профилируйте продакшен-форму — payload, конкурентность, распределение данных, — потому что синтетический цикл это другая программа, и судите успех по метрике SLO после деплоя, а не по бенчмарку, оправдавшему изменение.
</Recap>
