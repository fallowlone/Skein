---
concepts:
- exit-codes
- logging
- retries-backoff
- signals
- idempotency
deepensInto: []
estMin: 18
lang: ru
lessonType: topic
level: senior
order: 5
prereqs: []
slug: "05-robust-cli-scripts"
sources:
- https://docs.python.org/3/library/logging.html
- https://docs.python.org/3/library/signal.html
spiral: []
status: ready
summary: 'Скрипт, который крутится без присмотра в cron или CI, держится на четырёх контрактах: ненулевой код выхода при сбое, логи в stderr вместо print, ограниченные ретраи с backoff для временного I/O и атомарная запись, чтобы kill никогда не отдал обрезанный файл.'
title: 'Надёжные CLI-скрипты: коды выхода, логирование, ретраи, сигналы'
track: python
unit: 02-scripting-and-io
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
Ночной экспорт месяцами горел зелёным. Потом верхний API начал отдавать 500-е, и скрипт их «обработал» — тело было обёрнуто в `try: ... except Exception: print("export failed")`, и после печати управление свалилось с конца `main`, и процесс завершился. Завершился нулём. cron увидел код 0, пометил задачу успешной и не послал ни одного алерта. Дашборд снизу продолжал читать вчерашний файл, потому что сегодняшний так и не записался, и «вчерашний» постепенно стал «двухнедельной давности». Никого не разбудили пейджером. Сбой нашли, когда аналитик заметил, что дата модификации файла не двигалась четырнадцать дней. Каждый байт того инцидента предотвратим, и каждый фикс — это то, что скрипт без присмотра обязан делать по умолчанию: завершаться ненулём, логировать в stderr, ретраить временный сбой и писать атомарно.
</Hook>

<Crux>Код выхода — контракт скрипта с cron/CI: catch-print-return-0 рапортует сбой зелёным. Падай громко, ретрай ограниченно, пиши атомарно.</Crux>

<Explanation>

## Коды выхода: контракт с ОС

Процесс сообщает об успехе или сбое ровно одним числом — своим **кодом выхода**. `0` значит успех; что угодно ненулевое значит сбой. Это число не украшение — это единственное, на что смотрят cron, CI-раннеры, `&&`-цепочки, systemd и любой оркестратор, чтобы решить, запускать ли следующий шаг или поднимать алерт. `backup.sh && upload.sh` запустит `upload`, только если `backup` вышел с 0. CI помечает шаг красным только при ненуле. Так что код выхода *и есть* сигнал успеха, и ошибиться в нём — это и есть способ, которым упавшая задача рапортует зелёным.

```python
import sys, logging

log = logging.getLogger("export")

def main() -> int:
    try:
        data = fetch_export()      # may raise on a 500
        write_atomic("/data/export.json", data)
        return 0                   # success — and only here
    except Exception:
        log.exception("export failed")  # ERROR + traceback to stderr
        return 1                   # FAILURE is now visible to cron/CI

if __name__ == "__main__":
    sys.exit(main())              # the return value becomes the exit code
```

Смертельный паттерн — обратный: поймать исключение, `print` сообщение и дать `main` *свалиться с конца*. Функция, свалившаяся с конца, возвращает `None`; `sys.exit(None)` завершается **нулём**. Ты только что сказал ОС, что задача удалась, тогда как она на самом деле упала. Либо дай исключению пробросить (Python завершится ненулём с трейсбэком), либо явно вызови `sys.exit(1)`. Резервируй отдельные коды (2, 3, …) только если вызывающий реально ветвится на том, *какой* именно сбой — иначе `1` достаточно.

<Inset kind="why" lang="ru">
Почему `except Exception: print("error")` с последующим проваливанием — один из самых опасных паттернов в скрипте без присмотра? Потому что он превращает сбой в УСПЕХ с точки зрения ОС: процесс выходит с 0, так что cron, CI и мониторинг все видят зелёное, ни один алерт не срабатывает, и сбой невидим, пока кто-то не заметит протухший вывод спустя дни. `print` к тому же ложится в stdout — часто отбрасываемый или, хуже, подмешанный в данные пайпа — без уровня и без таймстампа, так что теряется даже само сообщение. Надёжность требует, чтобы сбой был ГРОМКИМ и МАШИНОЧИТАЕМЫМ: логируй на ERROR в stderr и выходи ненулём, чтобы то, что следит за скриптом, реально узнало, что он упал.
</Inset>

## Логирование, а не print: правило скрипта без присмотра

`print` пишет в stdout без уровня, без таймстампа и без контроля маршрутизации. В интерактивной сессии это нормально; в cron-задаче это невидимо — терминала нет, а stdout часто перехватывается как *данные* задачи или отбрасывается. Модуль `logging` даёт то, что нужно скрипту без присмотра: **уровни** (DEBUG/INFO/WARNING/ERROR), таймстампы и адресат, настроенный один раз. Два важных правила: диагностика идёт в **stderr**, реальный вывод — в **stdout** (чтобы нижестоящий `| jq` видел чистые данные, а не строки лога), а уровень задаётся флагом или переменной окружения, чтобы можно было поднять DEBUG, не правя код.

```python
import logging, os, sys

logging.basicConfig(
    level=os.environ.get("LOG_LEVEL", "INFO"),
    format="%(asctime)s %(levelname)s %(name)s %(message)s",
    stream=sys.stderr,            # diagnostics to stderr; stdout stays clean for data
)
log = logging.getLogger("export")

log.info("starting export run")          # routine progress
log.error("upstream returned 503")       # a problem worth alerting on
log.exception("unexpected failure")      # ERROR + full traceback, inside an except block
```

`log.exception` — то, за чем тянешься внутри блока `except`: он логирует на ERROR *и* прикрепляет трейсбэк, что ровно и нужно отправить в агрегатор логов. print-отладка cron-задачи не даёт ничего grep-абельного; структурированные логи ищутся, отправляются и стыкуются прямо с observability.

## Ретраи с backoff: переживаем временный I/O

Сетевые и API-вызовы падают временно: таймаут, 503, rate-limit. Надёжная реакция — не «сдаться» и не «ретраить вечно», а **ограниченный ретрай с экспоненциальным backoff и джиттером**. Жди ~1с, потом ~2с, потом ~4с, каждое со случайной добавкой, чтобы флот скриптов не ретраил в унисон и не затоптал восстанавливающийся сервис, вплоть до лимита попыток. Два жёстких ограничения: ретраить только **идемпотентные или временные** операции и **уважать `Retry-After`**, когда сервер его шлёт.

```python
import time, random, logging

log = logging.getLogger("export")

def with_backoff(op, *, attempts=5, base=1.0, cap=30.0):
    for i in range(attempts):
        try:
            return op()
        except TransientError as e:           # ONLY transient/retryable errors
            if i == attempts - 1:
                raise                          # budget exhausted -> let it fail
            delay = min(cap, base * 2 ** i) + random.uniform(0, 1)  # backoff + jitter
            log.warning("attempt %d failed (%s), retrying in %.1fs", i + 1, e, delay)
            time.sleep(delay)
```

Асимметрия — в этом весь смысл. Ограниченный backoff превращает временный 503 в не-событие — третья попытка проходит, и никто не замечает. *Неограниченный* или *мгновенный* ретрай делает обратное: он молотит уже задыхающийся сервис, превращая мигание в аварию. А ретрай неидемпотентного `POST` (списать карту, послать письмо) может применить побочный эффект дважды — так что ретрай чтения и идемпотентные записи, а неидемпотентные операции сделай идемпотентными через ключ, прежде чем их ретраить.

## Сигналы и атомарная запись: умираем чисто

Процесс без присмотра убивают. Оркестратор шлёт **SIGTERM**, чтобы его остановить; человек шлёт **SIGINT** через Ctrl-C. По умолчанию оба разматывают стек — но если ты на середине записи файла, дефолт оставляет **обрезанный файл**, удержанный лок или temp-каталог, забитый мусором. Две дополняющие защиты: обработать сигнал (или опереться на `try/finally` + контекстные менеджеры), чтобы сбросить буферы и прибраться, и никогда не писать настоящий файл на месте — пиши temp-файл, затем **`os.replace`**, который атомарен на POSIX-файловой системе. Kill в любой момент оставит либо старый целый файл, либо новый целый файл, но никогда полузаписанный.

```python
import os, signal, tempfile, json

def write_atomic(path: str, obj) -> None:
    d = os.path.dirname(path) or "."
    fd, tmp = tempfile.mkstemp(dir=d)          # same filesystem -> rename is atomic
    try:
        with os.fdopen(fd, "w") as f:
            json.dump(obj, f)
            f.flush()
            os.fsync(f.fileno())               # durable before the swap
        os.replace(tmp, path)                  # atomic: readers see old OR new, never partial
    except BaseException:
        os.unlink(tmp)                         # killed mid-write -> no orphan temp
        raise

def handle_term(signum, frame):
    raise SystemExit(143)                      # turn SIGTERM into a clean unwind (128+15)

signal.signal(signal.SIGTERM, handle_term)     # finally/with blocks now run on stop
```

Поскольку `os.replace` атомарен, окна для kill, в котором можно было бы увидеть частичный файл, просто не существует. Соедини это с **идемпотентностью** — скрипт *будет* перезапущен (ретрай, ручной перезапуск, наложившийся тик cron), так что check-before-act, пиши атомарно и бери **lockfile**, чтобы отказать второму одновременному запуску. Идемпотентный скрипт можно безопасно запустить дважды; неидемпотентный портит состояние на втором.

<FlowDiagram
  label="Поток управления одного надёжного запуска без присмотра. Скрипт стартует и пытается выполнить операцию внутри ограниченного цикла ретрая с экспоненциальным backoff: временная ошибка (таймаут, 503) ретраится до лимита, затем при устойчивом сбое скрипт логирует на ERROR в stderr и выходит с 1, чтобы cron и CI увидели сбой. При успехе он пишет вывод атомарно через temp-файл и os.replace, затем выходит с 0. Независимо SIGTERM или SIGINT в любой момент запускает путь очистки, который сбрасывает буферы, дописывает или отбрасывает атомарный temp-файл и освобождает lockfile перед выходом, так что kill никогда не оставит частичный файл или удержанный лок."
  hue="var(--d-systems)"
  perRow={3}
  nodes={[
    { id: "run", label: "run + lockfile", sub: "single instance" },
    { id: "try", label: "try op", sub: "bounded retry+backoff" },
    { id: "ok", label: "success", sub: "atomic write -> exit 0" },
    { id: "fail", label: "failure", sub: "log ERROR->stderr, exit 1" },
    { id: "sig", label: "SIGTERM/SIGINT", sub: "kill arrives" },
    { id: "clean", label: "cleanup", sub: "flush, os.replace, unlock" },
  ]}
  edges={[
    { from: "run", to: "try", label: "start" },
    { from: "try", to: "ok", label: "succeeded" },
    { from: "try", to: "fail", label: "budget spent" },
    { from: "sig", to: "clean", label: "handler" },
    { from: "clean", to: "fail", label: "exit cleanly" },
  ]}
  caption="Успех выходит с 0 после атомарной записи; устойчивый сбой логирует в stderr и выходит с 1, чтобы планировщик его увидел; сигнал маршрутизируется через очистку, чтобы kill никогда не отдал обрезанный файл и не утёк лок."
/>

<TradeoffMatrix
  id="python-02-scripting-and-io-05-robust-cli-scripts-tradeoff-1"
  lessonSlug="05-robust-cli-scripts"
  lang="ru"
  prompt="Ты владеешь ночным cron-экспортом, который ходит во флапающий верхний API и пишет JSON-файл, потребляемый дашбордом. Как сделать его надёжным, чтобы сбой никогда не был тихим, а kill никогда не отдал обрезанный файл?"
  options={[
    { name: "Логировать ошибки на ERROR в stderr, sys.exit(1) при сбое, ограниченные ретраи с backoff для временных ошибок, атомарная запись temp-затем-os.replace и хендлер очистки на SIGTERM/SIGINT", summary: "Громкий сбой + безопасная запись.", correct: true, justification: "Это удовлетворяет каждый контракт скрипта без присмотра. Выход с 1 при сбое заставляет cron и CI увидеть красное и поднять алерт; логирование в stderr держит сообщение машиночитаемым и оставляет stdout чистым для данных пайпа; ограниченный экспоненциальный backoff с джиттером пережидает временные 503 без затаптывания восстанавливающегося API; temp-затем-os.replace значит, что kill в любой миг оставит старый или новый целый файл, но не обрезанный; а хендлер сигнала даёт finally/with-блокам сбросить буферы и освободить лок. Каждый кусок закрывает один режим отказа из военной истории." },
    { name: "Обернуть тело в try/except Exception, который печатает 'failed' и возвращает 0", summary: "Тихий зелёный — тот самый инцидент.", justification: "Это ровно баг из военной истории. Возврат 0 говорит ОС, что задача удалась, так что cron/CI/мониторинг остаются зелёными и ни один алерт не срабатывает; print ложится в stdout без уровня и таймстампа и обычно отбрасывается; и нет ни ретрая, ни атомарной записи. Сбой становится невидимым, пока кто-то не заметит протухший вывод спустя дни. Это противоположность надёжности — он активно прячет сбой." },
    { name: "Расставить print-ы по всему коду для отладки", summary: "Невидимы в cron.", justification: "print-отладка бесполезна в задаче без присмотра: терминала нет, stdout перехватывается как данные или отбрасывается, а строки не несут уровня и таймстампа, так что их нельзя отфильтровать или поднять по ним алерт. Это ещё и загрязняет поток данных, который читает нижестоящий потребитель. Диагностика должна идти на нужном уровне в stderr через logging, а не как print — и это никак не помогает кодам выхода, ретраям или атомарности." },
    { name: "Ретраить вечно, мгновенно, на любой ошибке", summary: "Затаптывание + двойное применение.", justification: "Неограниченные мгновенные ретраи превращают временное мигание в самонанесённую аварию: скрипт молотит задыхающийся API без backoff, а флот таких в унисон затаптывает его. Ретрай на ЛЮБОЙ ошибке значит, что неидемпотентные операции (POST, который списывает или шлёт письмо) применяются дважды, а по-настоящему постоянный сбой никогда не всплывает, потому что цикл не кончается. Ретраи должны быть ограничены, с backoff и джиттером, и только для временных/идемпотентных операций." },
  ]}
/>

<Quiz
  id="python-02-scripting-and-io-05-robust-cli-scripts-quiz-1"
  lessonSlug="05-robust-cli-scripts"
  lang="ru"
  question="cron-скрипт ловит исключение, печатает 'export failed' и сваливается с конца main без вызова sys.exit. Какой вывод сделает cron и почему?"
  choices={[
    { label: "cron заключит, что задача УДАЛАСЬ — main возвращает None, sys.exit(None) выходит с 0, а cron/CI/&&-цепочки ветвятся только на коде выхода, так что 0 значит успех и алерт не срабатывает", correct: true },
    { label: "cron заключит, что задача упала, потому что 'export failed' было напечатано в вывод", misconception: "cron не читает твой вывод в поисках слова 'failed' — он ветвится исключительно на коде выхода. Печать сообщения ничего не меняет в статусе выхода. Сваливание с конца main возвращает None и выходит с 0, так что cron записывает успех независимо от того, что напечатано." },
    { label: "Python автоматически выходит ненулём всякий раз, когда исключение поймано", misconception: "Python выходит ненулём, только когда исключение ПРОБРАСЫВАЕТСЯ из программы непойманным. Как только ты поймал его через except и не сделал re-raise или sys.exit(1), программа продолжает нормально и выходит с 0 по умолчанию. Ловля исключения — ровно то, что подавило ненулевой выход здесь." },
    { label: "cron автоматически ретраит задачу, пока она не пройдёт", misconception: "cron сам не ретраит упавшие задачи — он просто запускает их по расписанию и записывает статус выхода. Ретраи — это то, что реализует твой скрипт (ограниченный backoff). А здесь cron всё равно видит выход 0, так что считает запуск успешным и не ретраил бы, даже если бы мог." },
  ]}
/>

<Quiz
  id="python-02-scripting-and-io-05-robust-cli-scripts-quiz-2"
  lessonSlug="05-robust-cli-scripts"
  lang="ru"
  question="Какие операции безопасно оборачивать в цикл ретрая с backoff и какую очистку скрипт обязан сделать, чтобы SIGTERM никогда не отдал испорченный вывод?"
  choices={[
    { label: "Ретраить только временные/идемпотентные операции и уважать Retry-After; обрабатывать SIGTERM/SIGINT и писать через temp-файл затем os.replace, чтобы kill оставил старый или новый целый файл, но не частичный", correct: true },
    { label: "Ретраить любую упавшую операцию, включая POST-ы, и просто дать SIGTERM убить процесс — ОС всё приберёт", misconception: "Ретрай неидемпотентного POST может применить его побочный эффект дважды (списать карту дважды, послать два письма), так что безопасны для ретрая только временные/идемпотентные операции. И ОС НЕ дописывает твой полузаписанный файл — дефолтный SIGTERM посреди записи оставляет обрезанный файл и удержанные локи. Надо писать атомарно (temp + os.replace) и обрабатывать сигнал, чтобы finally/with-блоки выполнились." },
    { label: "Ретраить всё вечно без задержки; сигналы не требуют обработки, потому что Python сбрасывает файлы при выходе", misconception: "Неограниченные мгновенные ретраи затаптывают падающий сервис и никогда не всплывают постоянную ошибку. Python НЕ гарантирует, что твой прикладной temp-файл переименован или твой лок освобождён при SIGTERM — это твоя работа через os.replace и finally/хендлер. Ретраи должны быть ограничены с backoff и только для временных/идемпотентных операций." },
    { label: "Ретрай небезопасен во всех случаях, так что никогда не ретрай; вместо этого выходи с 0 на первой ошибке, чтобы cron двигался дальше", misconception: "Ретрай временных ошибок — ровно то, что делает скрипт надёжным к флапающему I/O — огульное 'никогда не ретрай' неверно. А выход с 0 на ошибке — это баг тихого сбоя: он говорит cron, что задача удалась, когда нет. Сбой обязан выйти ненулём." },
  ]}
/>

</Explanation>

<KeyTakeaway>Без присмотра = выходи ненулём при сбое (никогда catch-print-return-0), логируй в stderr, ретрай временные операции ограниченным backoff, обрабатывай SIGTERM и пиши temp-затем-os.replace, без частичных файлов.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="python-02-scripting-and-io-05-robust-cli-scripts-retrieval"
  lang="ru"
  questions={[
    { q: "Объясни, почему catch-print-return-0 — самый опасный паттерн в скрипте без присмотра, и что именно делать вместо него.", a: "Код выхода — это однозначный (в одно число) контракт процесса с ОС: 0 значит успех, ненуль значит сбой, и cron, CI-раннеры, &&-цепочки и оркестраторы ветвятся исключительно на этом числе — никто из них не читает твой печатный текст. Паттерн try: ... except Exception: print('failed') с последующим сваливанием с конца main возвращает None, а sys.exit(None) выходит с 0, так что ОС сообщают, что задача УДАЛАСЬ, тогда как она упала: cron/CI/мониторинг остаются зелёными, алерт не срабатывает, и сбой невидим, пока кто-то не заметит протухший вывод спустя дни — та самая военная история о двух неделях тихих сбоев. print к тому же идёт в stdout без уровня и таймстампа, часто отбрасывается или подмешивается в данные пайпа, так что теряется даже сообщение. Фикс — сделать сбой ГРОМКИМ и МАШИНОЧИТАЕМЫМ: логируй на ERROR в stderr (log.exception внутри блока except, который прикрепляет трейсбэк) и либо дай исключению пробросить (Python выходит ненулём), либо явно вызови sys.exit(1). Тогда cron видит красное и алерт срабатывает." },
    { q: "Пройди по четырём контрактам надёжности скрипта без присмотра: логирование, ретраи, сигналы и идемпотентность — с конкретным механизмом для каждого.", a: "Логирование: используй модуль logging, а не print — диагностика идёт в stderr (чтобы stdout оставался чистым для данных пайпа), каждая строка несёт уровень (DEBUG/INFO/WARNING/ERROR) и таймстамп, а уровень задаётся флагом/окружением, чтобы поднять DEBUG без правки кода; log.exception отправляет трейсбэк в твой агрегатор. Ретраи: временный I/O (таймаут, 503, rate-limit) получает ОГРАНИЧЕННЫЙ ретрай с экспоненциальным backoff плюс джиттер (~1с, ~2с, ~4с, с потолком), чтобы скрипт пережидал мигание без затаптывания восстанавливающегося сервиса; ретрай только временных/идемпотентных операций и с уважением к Retry-After — неограниченный или мгновенный ретрай превращает мигание в аварию, а ретрай неидемпотентного POST применяет дважды. Сигналы: оркестратор шлёт SIGTERM, а Ctrl-C шлёт SIGINT; без обработки kill посреди записи оставляет обрезанный файл или удержанный лок, так что обрабатывай сигнал (или опирайся на try/finally + контекстные менеджеры), чтобы сбросить буферы и прибраться. Атомарность + идемпотентность: никогда не пиши на месте — пиши temp-файл, затем os.replace (атомарен на POSIX), так что kill оставит старый или новый целый файл, но никогда частичный; а поскольку скрипт будет перезапущен, сделай его идемпотентным (check-before-act, атомарная запись, lockfile для отказа второму одновременному запуску), чтобы перезапуск не обработал дважды и не испортил состояние." },
  ]}
/>

<Recap lang="ru">
Скрипт, крутящийся без присмотра в cron или CI, судят по четырём контрактам, каждый из которых нарушил инцидент с тихим экспортом. Первый: код выхода — контракт с ОС: 0 — успех, ненуль — сбой, и cron, CI и &&-цепочки ветвятся только на этом числе — так что смертельный паттерн это поймать исключение, напечатать сообщение и свалиться с конца main, что возвращает None, выходит с 0 и рапортует сбой зелёным без алерта; фикс — дать исключению пробросить или sys.exit(1). Второй: используй logging, а не print: диагностика в stderr с уровнями и таймстампами, реальные данные в stdout, уровень из флага или окружения, и log.exception внутри except, чтобы отправить трейсбэк — print-отладка невидима в cron-задаче. Третий: ретрай временного I/O ограниченным экспоненциальным backoff плюс джиттер до потолка, но только для временных/идемпотентных операций и с уважением к Retry-After, потому что неограниченный или мгновенный ретрай затаптывает восстанавливающийся сервис, а ретрай неидемпотентного POST применяет дважды. Четвёртый: умирай чисто: обрабатывай SIGTERM и SIGINT (или используй try/finally и контекстные менеджеры), чтобы сбросить буферы и прибраться, и пиши в temp-файл, затем os.replace, чтобы kill в любой миг оставил старый или новый целый файл, но никогда обрезанный — и сделай скрипт идемпотентным через check-before-act, атомарную запись и lockfile, чтобы неизбежный перезапуск не обработал дважды и не испортил состояние. Вместе: падай громко, логируй в stderr, ретрай ограниченно, пиши атомарно.
</Recap>
