---
concepts:
- gil
- threadpoolexecutor
- asyncio
- processpoolexecutor
- io-bound-vs-cpu-bound
deepensInto: []
estMin: 18
lang: ru
lessonType: topic
level: senior
order: 4
prereqs: []
slug: "04-concurrency-for-io-bound-scripts"
sources:
- https://docs.python.org/3/library/asyncio.html
- https://docs.python.org/3/library/concurrent.futures.html
spiral: []
status: ready
summary: 'GIL пускает выполнять байткод по одному потоку за раз, но отпускается на блокирующем I/O — поэтому потоки и asyncio ускоряют I/O-скрипты, а CPU-работе нужны отдельные процессы. Подбирай инструмент под узкое место: ждём или жжём CPU.'
title: 'Конкурентность для I/O-скриптов: потоки, async, процессы'
track: python
unit: 02-scripting-and-io
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
Ночной скрипт отчёта забирал 500 страниц API, одну за другой, и тратил примерно восемь минут — почти всё это время процессор простаивал, ожидая сеть. Доброжелательный инженер «распараллелил» его так, как параллелят всё тяжёлое: `multiprocessing`. Время почти не сдвинулось — всё те же минуты, — а теперь машина ела гигабайты RAM, поднимая флот интерпретаторов, которые не делали ничего, кроме ожидания сокетов. Он схватил самый тяжёлый инструмент, какой есть в Python, для задачи, требовавшей самого лёгкого. Замена на `ThreadPoolExecutor` уронила тот же скрипт примерно до двадцати секунд, потому что 500 сетевых ожиданий наконец стали перекрываться, а не выстраиваться в очередь. Та же машина, та же сеть, два порядка разницы — целиком из-за выбора модели конкурентности, совпадающей с узким местом.
</Hook>

<Crux>I/O-скрипты ускоряют потоки или asyncio (GIL отпускается на I/O); CPU-работе нужны процессы, чтобы задействовать несколько ядер.</Crux>

<Explanation>

## GIL: байткод выполняет один поток за раз

Прежде чем тянуться к любому инструменту конкурентности, нужна одна ментальная модель — иначе можно потратить час на рефакторинг и не получить никакого ускорения, ровно как инженер из вступления. Всё запутанное в конкурентности Python сводится к одному правилу. У CPython есть **Global Interpreter Lock** — GIL (глобальная блокировка интерпретатора, пускающая выполнять байткод ровно одному потоку за раз), — и он позволяет ровно *одному* потоку выполнять байткод Python в любой момент. Подними десять потоков с чистой Python-математикой — и они не побегут на десяти ядрах; они будут чередоваться на одном, нарезанные по времени. Вот почему потоки **не** ускоряют CPU-bound Python.

Поворот, который всё-таки делает потоки полезными: GIL **отпускается**, когда поток блокируется на I/O — чтение сокета, чтение диска, `time.sleep`, round trip к базе, большинство вызовов C-расширений. Пока один поток сидит и ждёт байты, GIL свободен, и работает другой поток. Поэтому для работы, тяжёлой по I/O — а это большинство скриптинга: HTTP, файлы, базы, — потоки реально перекрывают *ожидание*, а именно в ожидании и уходит всё время.

```python
# Это вся ментальная модель:
#   CPU-bound Python  -> GIL УДЕРЖИВАЕТСЯ -> потоки нарезают одно ядро по времени (прироста нет)
#   блокирующий I/O   -> GIL ОТПУСКАЕТСЯ  -> потоки перекрывают ожидания     (большой прирост)
#
# Поэтому единственный вопрос, выбирающий инструмент:
#   «Этот скрипт ЖДЁТ I/O или СЖИГАЕТ CPU?»
```

(Python 3.13 поставляет экспериментальную free-threaded-сборку, которая может отключить GIL, но она opt-in и пока не дефолт — считай, что GIL держится.)

## Потоки: лёгкий выигрыш на I/O

Для I/O-bound-задачи `concurrent.futures.ThreadPoolExecutor` — выигрыш с наименьшими усилиями во всём языке. Ты отправляешь работу; пул прогоняет её через горстку потоков; каждый отпускает GIL в момент, когда блокируется на сети, так что ожидания складываются параллельно.

```python
import concurrent.futures
import requests

def fetch(url: str) -> int:
    return len(requests.get(url, timeout=10).content)

def fetch_all(urls: list[str]) -> list[int]:
    # 100 URL перекрывают сетевые ожидания в ~16 потоках.
    # GIL отпускается во время каждого requests.get(), поэтому они реально перекрываются.
    with concurrent.futures.ThreadPoolExecutor(max_workers=16) as pool:
        return list(pool.map(fetch, urls))
```

Подвох — накладные расходы на поток: каждый несёт настоящий OS-поток со своим стеком и ценой переключения контекста. Пул из десятков-сотен — нормально; десятки тысяч потоков — нет, ты захлебнёшься в памяти и в свистопляске планировщика. Когда тебе нужно столько одновременных соединений, ты перерос потоки и хочешь asyncio.

## Asyncio: тысячи соединений на одном потоке

`asyncio` — это **однопоточная кооперативная конкурентность**. Один event loop перемежает тысячи I/O-операций в полёте, и каждый `await` — это точка, где корутина добровольно уступает loop, пока ждёт, так что loop запускает кого-то ещё. Никакого OS-потока на задачу, куда меньше памяти, поэтому масштабируется до тысяч одновременных соединений там, где потоки бы задохнулись.

```python
import asyncio
import httpx  # ASYNC HTTP-клиент — requests синхронный и блокировал бы цикл

async def fetch(client: httpx.AsyncClient, url: str) -> int:
    resp = await client.get(url, timeout=10)  # уступает цикл во время ожидания
    return len(resp.content)

async def fetch_all(urls: list[str]) -> list[int]:
    async with httpx.AsyncClient() as client:
        # gather планирует все корутины на одном цикле; тысячи перекрываются дёшево
        return await asyncio.gather(*(fetch(client, u) for u in urls))
```

Цена в том, что asyncio — это **всё-или-ничего**. Кооперативность означает, что одна корутина, которая *не* уступает, морит голодом все остальные. Блокирующий синхронный вызов (`requests.get`, обычный `time.sleep`, тяжёлый `json.loads` над огромным payload) внутри корутины застопорит **весь loop** — каждое другое соединение замрёт, пока он не вернётся. Так что либо ты идёшь async до самого низа (async HTTP-клиент, async-драйвер БД), либо выталкиваешь блокирующую/CPU-работу с loop через `loop.run_in_executor(...)`, чтобы loop оставался отзывчивым.

<Inset kind="why" lang="ru">
Почему потоки ускоряют 500 HTTP-запросов, но ничего не дают на ресайзе 500 изображений в том же CPython? Потому что GIL пускает выполнять байткод Python лишь по одному потоку за раз, но он **отпускается**, пока поток блокируется на I/O — на чтении сокета. Во время HTTP-запроса поток просто ждёт байты; процессор и так простаивает, поэтому 500 потоков перекрывают свои простои-ожидания, и wall-clock схлопывается примерно до одного ожидания. Ресайз изображения — наоборот: это чистый Python/нативный compute, который **держит** GIL всё время, так что 500 потоков просто нарезают одно ядро по времени и заканчивают не быстрее одного. Только отдельные **процессы** — каждый со своим интерпретатором и своим GIL — гоняют этот compute на нескольких ядрах сразу. Решает рабочая нагрузка, а не синтаксис, купит ли тебе конкурентность хоть что-то.
</Inset>

## Процессы: настоящий параллелизм для CPU-работы

Когда узкое место — процессор (ресайз изображений, парсинг больших файлов, хеширование, числодробление), никакое количество потоков не поможет, потому что GIL сериализует compute. Тебе нужен настоящий параллелизм, что в CPython означает настоящие OS-процессы через `ProcessPoolExecutor` (или `multiprocessing`). Каждый воркер — **отдельный интерпретатор со своим GIL**, так что они бегут на отдельных ядрах одновременно.

```python
import concurrent.futures
from PIL import Image

def resize(path: str) -> str:
    img = Image.open(path)            # CPU-bound работа, УДЕРЖИВАЮЩАЯ GIL
    img.thumbnail((256, 256))
    out = path.replace(".jpg", "_thumb.jpg")
    img.save(out)
    return out

def resize_all(paths: list[str]) -> list[str]:
    # ProcessPoolExecutor -> N интерпретаторов на N ядрах; GIL больше не сериализует.
    with concurrent.futures.ProcessPoolExecutor() as pool:
        return list(pool.map(resize, paths))
```

Процессы не бесплатны. Каждый платит **стоимость старта**, а аргументы и возвращаемые значения пересекают границу процесса, будучи **запикленными** (сериализованными) и отправленными через IPC. Эти накладные окупаются только на увесистой CPU-работе; запустить пул процессов по тысячам крошечных задач или по I/O-работе — значит купить накладные без всякой выгоды.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Инструмент</th>
        <th class="border border-border px-3 py-2 text-left">Лучше для</th>
        <th class="border border-border px-3 py-2 text-left">Масштаб</th>
        <th class="border border-border px-3 py-2 text-left">Подвох</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2"><strong>ThreadPoolExecutor</strong></td>
        <td class="border border-border px-3 py-2">I/O-bound (HTTP, диск, БД)</td>
        <td class="border border-border px-3 py-2">Десятки-сотни</td>
        <td class="border border-border px-3 py-2">Память на поток + цена переключения</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2"><strong>asyncio</strong></td>
        <td class="border border-border px-3 py-2">Высококонкурентный I/O</td>
        <td class="border border-border px-3 py-2">Тысячи соединений</td>
        <td class="border border-border px-3 py-2">Один блокирующий/CPU-вызов стопорит весь loop</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2"><strong>ProcessPoolExecutor</strong></td>
        <td class="border border-border px-3 py-2">CPU-bound (ресайз, парсинг, крипто)</td>
        <td class="border border-border px-3 py-2">~N ядер</td>
        <td class="border border-border px-3 py-2">Старт + pickle/IPC на задачу</td>
      </tr>
    </tbody>
  </table>
</div>

## Две классические ошибки

Обе настоящие, обе частые, обе — один и тот же диагноз, прокрученный в обратную сторону. **Ошибка первая:** использовать `multiprocessing` для I/O-bound-задачи — тот восьмиминутный fetch-скрипт из вступления. Ты платишь тяжёлый старт процессов и pickle-накладные и ешь RAM, без выигрыша над потоками, потому что узким местом никогда не был процессор; это была сеть, а процессы не заставят сокеты возвращаться быстрее. **Ошибка вторая:** использовать потоки (или asyncio) для CPU-bound-задачи — ресайз 1000 изображений или скрипт, дробящий CSV, — и увидеть *нулевое* ускорение, а потом тупо пялиться в это. GIL сериализовал твой Python-compute всё время; потоки только и умеют перекрывать *ожидание*, а перекрывать было нечего. Тот CSV-скрипт остался ровно таким же медленным с потоками, как и без них, пока кто-то не переключил его на `ProcessPoolExecutor` — и он наконец растёкся по ядрам.

Диагноз всегда один и тот же единственный вопрос: **скрипт ждёт I/O или жжёт CPU?** Ждёт → потоки или asyncio. Жжёт → процессы. Классифицируй верно — и инструмент очевиден; ошибись — и добавишь сложность без ускорения.

<TradeoffMatrix
  id="python-02-scripting-and-io-04-concurrency-for-io-bound-scripts-tradeoff-1"
  lessonSlug="04-concurrency-for-io-bound-scripts"
  lang="ru"
  prompt="Скрипт отчёта забирает 500 страниц API и сейчас гоняет их последовательно за ~8 минут. Узкое место — сетевые round trip, а не локальные вычисления. Как сделать его быстрым?"
  options={[
    { name: "ThreadPoolExecutor (или asyncio + httpx), чтобы перекрыть сетевые ожидания", summary: "Совпадает с I/O-узким местом.", correct: true, justification: "Задача I/O-bound: почти все восемь минут процессор простаивает, пока сокеты ждут байты. GIL отпускается на каждом сетевом чтении, поэтому потоки реально перекрывают эти ожидания — 500 round trip схлопываются к длительности одного, роняя скрипт до ~20с тривиальным кодом. asyncio + async-клиент (httpx) — та же идея на большей конкурентности на одном loop. Любой вариант — верная модель: дёшево и нацелено в настоящее узкое место." },
    { name: "multiprocessing / ProcessPoolExecutor по всем ядрам", summary: "Тяжёлый инструмент, не то узкое место.", justification: "Это ровно провалившийся фикс из вступления. Процессы дают настоящий параллельный CPU, но процессор никогда не был ограничением — была сеть, а отдельный интерпретатор не заставит сокет вернуться раньше. Ты платишь старт процессов плюс pickle аргументов/результатов через IPC и раздуваешь RAM, поднимая интерпретаторы, которые только и ждут I/O. Те же минуты, что и раньше, теперь со счётом за память. Процессы — для CPU-bound-работы, а не для запросов." },
    { name: "Оставить последовательным — конкурентность добавляет багов", summary: "Бросает 8 минут на ветер.", justification: "Последовательность — безопасный дефолт и норм для пары запросов, но здесь это и есть баг: 500 независимых ожиданий гоняются по одному, так что общее время — это СУММА каждого round trip вместо примерно самого долгого. Ожидания независимы и перекрываемы; отказ их перекрыть выбрасывает ~24x-выигрыш, который ThreadPoolExecutor даёт в несколько строк. Простота не стоит двух порядков здесь." },
    { name: "Потоки, но в расчёте на ускорение по числу ядер на тяжёлом CPU-парсинге в том же цикле", summary: "GIL сериализует парсинг.", justification: "Потоки верны для запросов, но рассуждение неверно для CPU-шага: если каждая страница запускает тяжёлый Python-парсинг, GIL сериализует этот compute, так что потоки перекрывают сетевые ожидания, но НЕ парсинг. Ждать N-ядерного ускорения от потоков на CPU-работе — вторая классическая ошибка. Перекрой I/O потоками/async, а CPU-тяжёлый парсинг вынеси в пул процессов (или run_in_executor) — не жди, что потоки распараллелят compute." },
  ]}
/>

<Quiz
  id="python-02-scripting-and-io-04-concurrency-for-io-bound-scripts-quiz-1"
  lessonSlug="04-concurrency-for-io-bound-scripts"
  lang="ru"
  question="В CPython какую рабочую нагрузку потоки реально ускоряют и почему?"
  choices={[
    { label: "I/O-bound-работу — GIL отпускается, пока поток блокируется на I/O, так что ожидания перекрываются; CPU-bound не ускоряется, потому что GIL сериализует compute", correct: true },
    { label: "CPU-bound-работу — потоки гоняют байткод Python на нескольких ядрах параллельно", misconception: "GIL позволяет выполнять байткод Python лишь одному потоку за раз, так что потоки не могут гонять CPU-bound Python на нескольких ядрах — они нарезают одно ядро по времени и заканчивают не быстрее. Потоки помогают только I/O-bound-работе, где GIL отпускается на время блокирующего ожидания. Для CPU-параллелизма нужны процессы." },
    { label: "Обе одинаково — потоки всегда добавляют параллелизм независимо от нагрузки", misconception: "Потоки — не универсальное ускорение. Они перекрывают I/O-ожидания (GIL отпущен на блокирующем I/O), но ничего не дают CPU-bound Python, где GIL сериализует байткод. Весь навык — сначала классифицировать нагрузку; «потоки всегда помогают» — это допущение, которое и порождает ошибку без-ускорения на CPU." },
    { label: "Ни ту, ни другую — GIL делает потоки бесполезными, поэтому всегда нужны процессы", misconception: "GIL не делает потоки бесполезными; он отпускается на блокирующем I/O — ровно тогда потоки и блистают, перекрывая сетевые/дисковые/БД-ожидания. Всегда хвататься за процессы — это другая классическая ошибка: тяжёлый старт и pickle-накладные для I/O-работы, которую потоки тянут куда дешевле." },
  ]}
/>

<Quiz
  id="python-02-scripting-and-io-04-concurrency-for-io-bound-scripts-quiz-2"
  lessonSlug="04-concurrency-for-io-bound-scripts"
  lang="ru"
  question="Нужно забрать 5000 URL из скрипта с минимальными накладными. Какая модель подходит и какова единственная оговорка?"
  choices={[
    { label: "asyncio — один event loop дёшево масштабируется до тысяч соединений; оговорка в том, что любой блокирующий/CPU-вызов внутри loop стопорит всё, так что используй async-библиотеки (или run_in_executor) повсюду", correct: true },
    { label: "ProcessPoolExecutor — подними 5000 процессов, чтобы каждый запрос шёл параллельно", misconception: "5000 процессов — катастрофа: каждый платит старт и пиклит свои аргументы/результаты, и ты исчерпаешь память задолго до того, как они помогут. Запросы — I/O-bound, так что процессы добавляют накладные без выгоды. asyncio тянет тысячи соединений на одном потоке за долю стоимости." },
    { label: "ThreadPoolExecutor с max_workers=5000 — один поток на URL", misconception: "5000 OS-потоков — далеко за пределом, где потоки масштабируются хорошо: у каждого свой стек и цена переключения контекста, так что ты захлебнёшься в памяти и в планировщике. Потоки отличны в десятках-сотнях; на тысячах соединений верный инструмент — однопетлевая модель asyncio." },
    { label: "asyncio, и внутри корутин можно спокойно звать requests.get()", misconception: "requests.get() — СИНХРОННЫЙ блокирующий вызов; внутри корутины он стопорит весь event loop, замораживая каждый другой запрос в полёте, пока не вернётся — ровно оговорка asyncio. Нужно использовать async-клиент вроде httpx/aiohttp или выносить блокирующие вызовы через loop.run_in_executor." },
  ]}
/>

</Explanation>

<FlowDiagram
  label="Дерево решений для выбора модели конкурентности Python по рабочей нагрузке. Старт от узкого места скрипта и единственный классифицирующий вопрос: эта работа I/O-bound (ожидание сети, диска или базы) или CPU-bound (жжёт процессор на Python-compute)? На ветке I/O-bound два инструмента: ThreadPoolExecutor для умеренной конкурентности в десятки-сотни или asyncio на одном event loop для тысяч одновременных соединений — оба выигрывают, потому что GIL отпускается на время блокирующего ожидания, и ожидания перекрываются. На ветке CPU-bound единственный вариант настоящего параллелизма — процессы через ProcessPoolExecutor, которые бегут на N ядрах, ведь каждый воркер — отдельный интерпретатор со своим GIL; потоки здесь не помогут, потому что GIL сериализует Python-compute."
  hue="var(--d-systems)"
  perRow={3}
  nodes={[
    { id: "work", label: "Нагрузка", sub: "что она делает?" },
    { id: "io", label: "I/O-bound", sub: "ждёт сеть/диск" },
    { id: "cpu", label: "CPU-bound", sub: "жжёт CPU" },
    { id: "threads", label: "Потоки", sub: "десятки-сотни" },
    { id: "async", label: "asyncio", sub: "тысячи соединений" },
    { id: "proc", label: "Процессы", sub: "N ядер" },
  ]}
  edges={[
    { from: "work", to: "io", label: "ждёт?" },
    { from: "work", to: "cpu", label: "жжёт?" },
    { from: "io", to: "threads", label: "умеренно" },
    { from: "io", to: "async", label: "высокая конкур." },
    { from: "cpu", to: "proc", label: "нужны ядра" },
  ]}
  caption="Один классифицирующий вопрос выбирает инструмент: I/O-bound-работа идёт к потокам (умеренно) или asyncio (тысячи), потому что GIL отпускается на ожидании; CPU-bound идёт к процессам, потому что GIL сериализует compute, и только отдельные интерпретаторы используют несколько ядер."
/>

<KeyTakeaway>GIL гоняет один поток байткода за раз, но отпускается на блокирующем I/O. Поэтому I/O-bound → потоки или asyncio; CPU-bound → процессы (N ядер) ценой старта + pickle. Спроси: ждём I/O или жжём CPU?</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="python-02-scripting-and-io-04-concurrency-for-io-bound-scripts-retrieval"
  lang="ru"
  questions={[
    { q: "Что такое GIL, почему потоки НЕ ускоряют CPU-bound Python и почему ускоряют I/O-bound-работу?", a: "GIL — Global Interpreter Lock — это правило CPython, что лишь один поток может выполнять байткод Python в любой момент. Для CPU-bound Python (ресайз изображений, парсинг, числодробление) потоки держат GIL, пока считают, так что десять потоков не бегут на десяти ядрах — они нарезают одно ядро по времени и заканчивают не быстрее одного; GIL сериализует compute. Причина, по которой потоки помогают I/O-bound-работе, в том, что GIL ОТПУСКАЕТСЯ, когда поток блокируется на I/O — чтение сокета, чтение диска, time.sleep, round trip к БД, большинство вызовов C-расширений. Пока один поток сидит и ждёт байты, GIL свободен, и работает другой поток, так что ожидания перекрываются. Поскольку I/O-bound-работа почти всё время простаивает в ожидании (а процессор и так простаивает), перекрытие этих ожиданий схлопывает wall-clock к длительности одного ожидания. Единственный классифицирующий вопрос, выбирающий инструмент: скрипт ЖДЁТ I/O или ЖЖЁТ CPU? Ждёт -> потоки/asyncio; жжёт -> процессы." },
    { q: "Сравни ThreadPoolExecutor, asyncio и ProcessPoolExecutor: для чего каждый, как далеко масштабируется и его главная цена — и назови две классические ошибки.", a: "ThreadPoolExecutor — лёгкий выигрыш для I/O-bound-работы (HTTP, диск, БД): он перекрывает сетевые ожидания через горстку потоков, ведь GIL отпускается на каждом блокирующем вызове. Масштабируется до десятков-сотен потоков; цена — память на поток и накладные переключения контекста, так что тысячи потоков топят машину. asyncio — однопоточная кооперативная конкурентность: один event loop перемежает тысячи I/O-операций в полёте, каждый await уступает loop, пока ждёт — поэтому масштабируется до тысяч соединений куда дешевле потоков. Его цена в том, что это всё-или-ничего: блокирующий синхронный вызов или CPU-тяжёлый шаг внутри корутины стопорит ВЕСЬ loop, так что нужно использовать async-библиотеки повсюду (httpx/aiohttp, async-драйверы БД) или выносить блокирующую работу через loop.run_in_executor. ProcessPoolExecutor даёт настоящий параллелизм для CPU-bound-работы (обработка изображений, парсинг, крипто): каждый воркер — отдельный интерпретатор со своим GIL, так что они бегут на ~N ядрах сразу. Его цена — старт процесса плюс pickle аргументов/результатов через IPC, так что он окупается только на увесистой CPU-работе, а не на крошечных задачах или I/O. Две классические ошибки: (1) использовать multiprocessing для I/O-bound fetch-задачи — тяжёлые процессные и pickle-накладные и RAM без выигрыша, потому что узкое место — сеть, а не процессор; (2) использовать потоки или asyncio для CPU-bound-задачи и увидеть НОЛЬ ускорения — GIL сериализовал compute, и нужны были процессы. Диагностируй оба, спросив, скрипт ждёт I/O или жжёт CPU." },
  ]}
/>

<Recap lang="ru">
Каждое решение о конкурентности в Python сводится к GIL: CPython пускает выполнять байткод Python ровно одному потоку за раз, но он ОТПУСКАЕТ GIL, когда поток блокируется на I/O — чтение сокета, чтение диска, time.sleep, round trip к БД. Поэтому потоки нарезают одно ядро по времени для CPU-bound Python (без ускорения), но реально перекрывают ОЖИДАНИЕ для I/O-bound-работы, куда уходит большая часть времени скриптинга. Это даёт три инструмента под нагрузку. ThreadPoolExecutor — лёгкий I/O-bound-выигрыш — перекрыть HTTP/диск/БД-ожидания через горстку потоков — хорош в десятках-сотнях, пока не кусаются память на поток и цена переключения контекста. asyncio — однопоточная кооперативная конкурентность: один event loop дёшево перемежает тысячи соединений в полёте, но это всё-или-ничего, так что блокирующий синхронный вызов или CPU-тяжёлый шаг внутри корутины стопорит весь loop — используй async-библиотеки повсюду или выноси через loop.run_in_executor. ProcessPoolExecutor даёт настоящий параллелизм для CPU-bound-работы (ресайз, парсинг, крипто), потому что каждый воркер — отдельный интерпретатор со своим GIL на N ядрах, ценой старта процесса и pickle аргументов и результатов через IPC. Две классические ошибки — зеркальные: multiprocessing для I/O-bound fetch (тяжёлые накладные и RAM, без выигрыша — узкое место сеть) и потоки или asyncio для CPU-bound-работы (без ускорения — GIL сериализует compute, нужны были процессы). Один вопрос диагностирует оба: скрипт ждёт I/O или жжёт CPU? Теперь, когда увидишь медленный скрипт, — задай этот вопрос первым: ответ сам подсказывает инструмент.
</Recap>
