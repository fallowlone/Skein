---
concepts:
- requests-http
- raise-for-status
- request-timeout
- llm-api-call
- venv-isolation
- requirements-pinning
deepensInto: []
estMin: 18
lang: ru
lessonType: topic
level: middle
order: 2
prereqs: []
slug: 02-http-and-packaging
sources:
- https://requests.readthedocs.io/en/latest/user/quickstart/
- https://www.python-httpx.org/async/
- https://docs.python.org/3/library/venv.html
- https://packaging.python.org/en/latest/guides/writing-pyproject-toml/
spiral: []
status: ready
summary: 'Полезный Python-инструмент — это два навыка: HTTP-запросы через requests (всегда таймаут, raise_for_status, ключ из env) и изоляция через venv, чтобы зависимости одного проекта не ломали другой.'
title: 'HTTP-запросы, venv и упаковка'
track: python
unit: 02-scripting-and-io
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import SequenceDiagram from "~/components/diagram/SequenceDiagram.astro";

<Hook>
Ты пишешь 30-строчный Python-скрипт, который зовёт LLM API, чтобы суммировать пачку тикетов. На твоём ноутбуке он работает. Передаёшь коллеге — `pip install requests` падает: его глобальный Python уже пинит несовместимый `urllib3` ради другого проекта. Это чинишь, и скрипт виснет навсегда на одном запросе, потому что у провайдера был сбой, а ты не задал таймаут. Потом ревью безопасности находит API-ключ, захардкоженный в файле, который ты только что запушил. Ни одна из этих проблем не сложная — это ровно три вещи, на которых спотыкается JS/TS-разработчик, приходящий в Python: небрежный HTTP, отсутствие изоляции окружения и секреты в исходниках.
</Hook>

<Crux>HTTP с таймаутом, проверкой статуса и ключом из env, запущенный в venv, — так зависимости одного проекта не ломают другой.</Crux>

<Explanation>

## HTTP через `requests`: четыре вещи, которые нельзя пропускать

`requests` — де-факто HTTP-клиент Python: для Python он то же, что `fetch` для браузера, только синхронный и куда удобнее стандартного `urllib`. GET с query-параметрами и JSON-ответом — это три строки:

```python
import requests

resp = requests.get(
    "https://api.example.com/search",
    params={"q": "python", "limit": 10},
    headers={"Accept": "application/json"},
    timeout=10,
)
resp.raise_for_status()          # превратить 4xx/5xx в исключение
data = resp.json()               # разобранный dict, как await resp.json() у fetch
```

POST с JSON — той же формы: передаёшь `json=`, и `requests` сериализует тело и сам ставит `Content-Type: application/json`:

```python
resp = requests.post(url, json={"name": "ada"}, timeout=10)
```

Две привычки отличают скрипт, который выживает в проде, от того, что виснет или врёт:

- **Всегда передавай `timeout=`.** Это сеньорский момент и тот, что упускают новички. По умолчанию `requests` ждёт *вечно*. Один медленный или мёртвый апстрим заморозит весь твой скрипт — или каждый воркер в пуле — без ошибки и без восстановления. У `fetch` тот же пробел (нужен `AbortController`); в Python ты просто передаёшь число.
- **Вызывай `raise_for_status()`.** В отличие от `fetch`, который отклоняется только при сетевом сбое и спокойно резолвит 500, `requests` по умолчанию не бросает исключение на 4xx/5xx — `resp` выглядит нормально, а `resp.json()` может вернуть тело с ошибкой, которое ты примешь за данные. `raise_for_status()` превращает плохой статус в `HTTPError`, который можно поймать. (`fetch` — зеркало: там ты сам проверяешь `resp.ok`.)

Для множества параллельных вызовов или async-приложения бери **`httpx`** — та же поверхность API, что у `requests`, но с `async`/`await` и HTTP/2. Retry с backoff стоит завести для любого реального апстрима: на `429` или `503` подожди `0.5с, 1с, 2с…` и повтори несколько раз, а не падай на первом же сбое. (Библиотеки вроде `tenacity` или адаптер `Retry` из `urllib3` делают это за тебя.)

## Выигрыш для AI-склейки: безопасный вызов LLM HTTP API

Бо́льшая часть «AI-интеграции» — ровно этот паттерн: POST JSON-тела на эндпоинт провайдера с заголовком авторизации, затем разбор JSON, который вернулся. Сеньорская деталь — **где живёт ключ**: никогда в исходниках.

```python
import os
import requests

api_key = os.environ.get("LLM_API_KEY")  # читаем из окружения, не из файла
if not api_key:
    raise RuntimeError("LLM_API_KEY не задан")

resp = requests.post(
    "https://api.provider.example/v1/chat/completions",
    headers={"Authorization": f"Bearer {api_key}"},
    json={
        "model": "some-model",
        "messages": [{"role": "user", "content": "Суммируй этот тикет: ..."}],
    },
    timeout=30,                          # LLM медленные; дай им запас, но ограничь
)
resp.raise_for_status()
reply = resp.json()["choices"][0]["message"]["content"]
```

`os.environ["LLM_API_KEY"]` — это Python-аналог `process.env.LLM_API_KEY`. Ты задаёшь его в shell, в `.env`, загружаемом через `python-dotenv`, или в хранилище секретов деплой-платформы — и ключ никогда не появляется в репозитории, в `git history` или при демонстрации экрана. Хардкод ключа — самый частый способ утечки учётных данных. Щедрый `timeout=30` тут тоже важен: ответы модели медленные, поэтому тесный таймаут отменяет хорошие запросы, а *отсутствие* таймаута означает, что одна застрявшая генерация вешает твой инструмент бесконечно.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Node / TypeScript</th>
        <th class="border border-border px-3 py-2 text-left">Python</th>
        <th class="border border-border px-3 py-2 text-left">Примечание</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2"><code>fetch()</code></td>
        <td class="border border-border px-3 py-2"><code>requests</code> / <code>httpx</code></td>
        <td class="border border-border px-3 py-2">requests синхронный; httpx добавляет async + HTTP/2</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2"><code>await resp.json()</code></td>
        <td class="border border-border px-3 py-2"><code>resp.json()</code></td>
        <td class="border border-border px-3 py-2">синхронно; без <code>await</code></td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">сам проверяешь <code>resp.ok</code></td>
        <td class="border border-border px-3 py-2"><code>resp.raise_for_status()</code></td>
        <td class="border border-border px-3 py-2">ни тот, ни другой не бросают на 4xx/5xx по умолчанию</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2"><code>AbortController</code></td>
        <td class="border border-border px-3 py-2"><code>timeout=</code></td>
        <td class="border border-border px-3 py-2">без него requests ждёт вечно</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2"><code>process.env.X</code> + dotenv</td>
        <td class="border border-border px-3 py-2"><code>os.environ["X"]</code> + python-dotenv</td>
        <td class="border border-border px-3 py-2">держи секреты вне исходников</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2"><code>package.json</code></td>
        <td class="border border-border px-3 py-2"><code>pyproject.toml</code> / <code>requirements.txt</code></td>
        <td class="border border-border px-3 py-2">манифест проекта + зависимостей</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2"><code>node_modules/</code></td>
        <td class="border border-border px-3 py-2"><code>.venv/</code></td>
        <td class="border border-border px-3 py-2">место установки для одного проекта</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2"><code>npm install</code></td>
        <td class="border border-border px-3 py-2"><code>pip install</code> (или <code>uv</code>)</td>
        <td class="border border-border px-3 py-2">разрешение + установка зависимостей</td>
      </tr>
    </tbody>
  </table>
</div>

<Quiz
  id="02-http-and-packaging-quiz-1"
  lessonSlug="02-http-and-packaging"
  lang="ru"
  question="Твой скрипт зовёт LLM API и иногда виснет на минуты без ошибки. Самая вероятная причина?"
  choices={[
    { label: "Не передан timeout=, поэтому requests ждёт вечно, когда апстрим тормозит — ограничь его (например, timeout=30) и добавь retry на 429/503", correct: true },
    { label: "resp.json() медленный на больших телах", misconception: "Разбор JSON-тела — это миллисекунды, не минуты. Многоминутное зависание без ошибки — это сигнатура запроса, заблокированного на сети без таймаута, а не разбора JSON." },
    { label: "requests не умеет работать с HTTPS-эндпоинтами", misconception: "requests обрабатывает TLS из коробки. Симптом — зависание, а не падение — указывает на отсутствующий таймаут на застрявшем соединении, а не на проблему протокола." },
    { label: "Ты забыл сделать await ответа", misconception: "requests синхронный — там нечего await. (Это async-режим httpx.) Зависание идёт от дефолтного бесконечного таймаута, а не от отсутствия await." },
  ]}
/>

<Quiz
  id="02-http-and-packaging-quiz-2"
  lessonSlug="02-http-and-packaging"
  lang="ru"
  question="Провайдер возвращает 500 с JSON-телом ошибки. Твой код делает resp.json() без raise_for_status(). Что произойдёт?"
  choices={[
    { label: "requests по умолчанию не бросает на 5xx, поэтому ты разберёшь тело ошибки и примешь его за валидный результат — raise_for_status() превратил бы его в ловимый HTTPError", correct: true },
    { label: "requests автоматически повторяет 500, пока не получит успех", misconception: "requests сам не делает retry — это ты подключаешь (tenacity, urllib3 Retry). Без raise_for_status 500 тихо проходит дальше как данные." },
    { label: "resp.json() бросает исключение, потому что статус 500", misconception: "resp.json() падает только если тело не валидный JSON. 500 с JSON-телом ошибки разбирается нормально — плохой статус невидим, пока ты не вызовешь raise_for_status()." },
    { label: "500 возвращается как None", misconception: "resp — обычный Response с реальным телом. Код статуса в resp.status_code; ничего не становится None. Статус нужно проверять явно." },
  ]}
/>

## Окружения и упаковка: зачем нужен `venv`

Вот провал коллеги из хука. Python по умолчанию ставит пакеты в один **глобальный** site-packages, общий для каждого проекта на машине. Проекту A нужен `urllib3==1.26`; проекту B нужен `urllib3==2.x`. При глобальной установке второй `pip install` перезаписывает первый — и теперь тот проект, что загрузит неверную версию, ломается. Нет `node_modules` на проект, чтобы их развести.

**Виртуальное окружение** (`venv`) — и есть эта граница на проект. Это лёгкая изолированная копия интерпретатора со своим site-packages, поэтому зависимости каждого проекта живут в своей папке и никогда не сталкиваются:

```bash
python -m venv .venv          # создать изолированное окружение в ./.venv
source .venv/bin/activate     # macOS/Linux  (Windows: .venv\Scripts\activate)
pip install requests httpx    # ставит В .venv, не глобально
```

Пока окружение активно, `python` и `pip` указывают на `.venv` — аналогично тому, как `node_modules` ограничивает пакеты одним проектом, только venv изолирует сам интерпретатор, а не одни библиотеки. Папку `.venv/` добавляешь в `.gitignore` (как `node_modules/`), а вместо неё коммитишь манифест, чтобы кто угодно мог её пересоздать.

<SequenceDiagram
  label="venv create install run isolation flow ru"
  hue="var(--d-systems)"
  actors={["Ты", "venv", "PyPI"]}
  messages={[
    { from: "Ты", to: "venv", label: "python -m venv .venv" },
    { from: "Ты", to: "venv", label: "source activate" },
    { from: "Ты", to: "PyPI", label: "pip install requests" },
    { from: "PyPI", to: "venv", label: "→ .venv/site-packages" },
    { from: "Ты", to: "venv", label: "python tool.py" },
    { from: "venv", to: "Ты", label: "изолир. deps, без клеша" },
  ]}
  caption="venv — это граница: pip ставит в .venv/site-packages, а активированный python работает только против этих зависимостей — поэтому urllib3 проекта A никогда не перезапишет проект B."
/>

## Пиннинг: `requirements.txt` и переход к `pyproject.toml`

Изоляция — половина истории; вторая половина — **воспроизводимость**. Если ты `pip install requests` сегодня, а коллега через месяц, вы можете получить разные версии. Пинни их. Классический файл — `requirements.txt`:

```text
requests==2.32.3
httpx==0.27.2
```

Воссоздаёшь точный набор через `pip install -r requirements.txt` внутри свежего venv. Это `package.json` + lock-файл, разнесённые по соглашению, а не в один файл. Современный стандартизированный дом для метаданных и зависимостей проекта — **`pyproject.toml`** (PEP 621): объявление зависимостей там плюс быстрый резолвер вроде **`uv`** или **`poetry`** даёт настоящий lock-файл и настройку одной командой, гораздо ближе к опыту `npm install`. Для быстрого скрипта `requirements.txt` сойдёт; для всего, что ты отгружаешь или делишь, предпочитай `pyproject.toml`.

<Inset kind="why" lang="ru">
Почему не просто `pip install --user` или установить глобально и пропустить venv? Потому что «глобально» — это разделяемое изменяемое состояние на каждый проект и даже на собственный Python-тулинг ОС. Пин одного проекта тихо меняет рантайм другого, апгрейды превращаются в русскую рулетку, а на некоторых системах pip в системный Python заблокирован напрочь (PEP 668, «externally-managed-environment»). venv на проект делает зависимости явными, одноразовыми и воспроизводимыми — удали `.venv`, пересоздай из манифеста, готово.
</Inset>

<TradeoffMatrix
  id="02-http-and-packaging-tradeoff-1"
  lessonSlug="02-http-and-packaging"
  lang="ru"
  prompt="Ты начинаешь небольшой Python-инструмент, который будут запускать и двое коллег. Как управлять его зависимостями?"
  options={[
    { name: "venv на проект + пиннутый манифест (requirements.txt или pyproject.toml), .venv в gitignore", summary: "Изолированно и воспроизводимо.", correct: true, justification: "Это стандарт. venv изолирует зависимости этого проекта от любого другого проекта и системного Python, поэтому никакая установка не сломает соседа; пиннутый манифест делает точный набор воспроизводимым на любой машине — удали .venv, пересоздай из манифеста. Это зеркалит модель node_modules + package.json, которую коллеги уже знают, и обходит блок PEP 668 на установку в системный интерпретатор." },
    { name: "Поставить всё глобально через pip install, чтобы было доступно везде", summary: "Один общий site-packages.", justification: "Глобально — разделяемое изменяемое состояние: пин urllib3 проекта A перезаписывает проект B, апгрейды для одного инструмента тихо ломают другой, а современные системы часто отказывают pip в системный Python (externally-managed-environment). Удобно для разового REPL, опасно в тот же момент, когда у тебя два проекта или коллега." },
    { name: "venv, но без манифеста — каждый руками делает pip install <pkg>", summary: "Изолированно, не воспроизводимо.", justification: "Изоляция верна, но ты потерял воспроизводимость: ничто не фиксирует точные версии, поэтому две машины расходятся и «у меня работает» возвращается. Всегда коммить requirements.txt или pyproject.toml, чтобы окружение пересобиралось детерминированно." },
    { name: "Закоммитить всю папку .venv/ в git, чтобы коллеги получили её мгновенно", summary: "Отгрузить само окружение.", justification: "venv содержит абсолютные пути и платформозависимые бинарники; он не переносится между машинами или ОС и раздувает репозиторий. Добавь .venv в gitignore и коммить манифест — это переносимый, ревьюабельный артефакт, ровно как ты gitignore-ишь node_modules и коммитишь package.json." },
  ]}
/>

<DragOrder
  id="02-http-and-packaging-drag-1"
  lessonSlug="02-http-and-packaging"
  lang="ru"
  prompt="Расставь шаги, чтобы настроить и запустить HTTP-инструмент, безопасно зовущий LLM API:"
  items={[
    { id: "a", label: "python -m venv .venv — создать изолированное окружение" },
    { id: "b", label: "source .venv/bin/activate — активировать, чтобы pip/python указывали на него" },
    { id: "c", label: "pip install -r requirements.txt — поставить пиннутые зависимости в venv" },
    { id: "d", label: "export LLM_API_KEY=… (или загрузить .env) — ключ в окружении, не в исходниках" },
    { id: "e", label: "python tool.py — POST с заголовком авторизации + таймаутом, затем raise_for_status()" },
  ]}
  correctOrder={["a", "b", "c", "d", "e"]}
/>

</Explanation>

<KeyTakeaway>Два навыка делают полезный Python-инструмент. HTTP: `requests`/`httpx`, всегда `timeout=`, `raise_for_status()` и ключ из `os.environ` — никогда из исходников. Окружение: `venv` на проект для изоляции, пиннутый `requirements.txt`/`pyproject.toml` для воспроизводимости.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="02-http-and-packaging-retrieval"
  lang="ru"
  questions={[
    { q: "Пройди по безопасному POST к LLM API в Python: что нужно задать и чем каждое отличается от JS fetch?", a: "Используй requests.post(url, headers={...}, json={...}, timeout=N). Важны четыре вещи. (1) Тело: передаёшь json={...}, и requests сериализует его и ставит Content-Type: application/json — разобранный ответ приходит из resp.json(), синхронно, без await, в отличие от fetch, где ты await resp.json(). (2) Авторизация: клади ключ в headers как Authorization: Bearer <key>, и читай этот ключ из os.environ['LLM_API_KEY'], Python-аналога process.env — никогда не хардкодь, чтобы он не попал в репозиторий и git history. (3) timeout=: requests по умолчанию ждёт вечно, поэтому застрявший апстрим вешает весь скрипт; передай ограничение (например, 30с для медленной LLM). У fetch тот же пробел, и ему нужен AbortController. (4) resp.raise_for_status(): requests НЕ бросает на 4xx/5xx, поэтому без него ты разберёшь тело ошибки как валидный результат; raise_for_status превращает плохой статус в ловимый HTTPError. fetch — зеркало: он резолвит 500, и ты сам проверяешь resp.ok. Для множества вызовов или async меняй requests на httpx — тот же API плюс async/await и HTTP/2 — и добавляй retry с backoff на 429/503." },
    { q: "pip install коллеги ломает другой проект. Объясни почему и фикс через venv + манифест от начала до конца.", a: "По умолчанию pip ставит в один глобальный site-packages, общий для каждого проекта и системного Python. Проект A пинит urllib3==1.26, а проекту B нужен urllib3==2.x; при глобальной установке второй install перезаписывает первый, поэтому тот проект, что загрузит неверную версию, ломается — нет per-project node_modules, чтобы их развести, а многие системы теперь блокируют pip в системный интерпретатор (PEP 668). Фикс — виртуальное окружение на проект: python -m venv .venv создаёт изолированный интерпретатор со своим site-packages; source .venv/bin/activate заставляет python и pip указывать на него; pip install тогда пишет в .venv, не глобально, поэтому проекты не сталкиваются. Для воспроизводимости коммитишь манифест — requirements.txt с пиннутыми версиями (requests==2.32.3) или, лучше для отгружаемых проектов, pyproject.toml с резолвером вроде uv/poetry для настоящего lock-файла — и добавляешь папку .venv в gitignore, поскольку в ней абсолютные пути и платформенные бинарники, не переносимые между машинами. Любой пересоздаёт точное окружение через python -m venv .venv && pip install -r requirements.txt. Это модель node_modules + package.json, где venv изолирует интерпретатор, а не одни библиотеки." },
  ]}
/>

<Recap lang="ru">
Полезный Python-инструмент — это два навыка, работающих вместе. Первый — HTTP: `requests` — это Python-аналог `fetch`, но синхронный: `requests.get/post` с `params`/`json` и `resp.json()` без `await`. Сеньорские привычки не обсуждаются: всегда передавай `timeout=` (иначе requests ждёт вечно, и один застрявший апстрим вешает весь скрипт), всегда вызывай `raise_for_status()` (requests не бросает на 4xx/5xx, поэтому тело ошибки проходит как данные), а для множества или async-вызовов бери `httpx` плюс retry с backoff на 429/503. Выигрыш для AI-склейки той же формы: POST JSON-тела с заголовком `Authorization: Bearer` провайдеру, читай ключ из `os.environ`, чтобы он не жил в исходниках, и дай медленным моделям щедрый, но ограниченный таймаут. Второй — окружения: Python по умолчанию ставит глобально, поэтому пины зависимостей двух проектов сталкиваются — `venv` на проект и есть изолирующая граница (свой интерпретатор и site-packages), в gitignore как `node_modules`. Пинни версии в `requirements.txt` или, лучше, в `pyproject.toml` с `uv`/`poetry`, чтобы точное окружение было воспроизводимо на любой машине. Изоляция плюс пиннинг — вот что превращает «работает на моём ноуте» в инструмент, который коллеги реально могут запустить.
</Recap>
