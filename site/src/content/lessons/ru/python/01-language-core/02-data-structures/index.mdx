---
concepts:
- list
- dict
- set
- tuple
- mutability-hashability
- identity-vs-equality
deepensInto: []
estMin: 17
lang: ru
lessonType: topic
level: middle
order: 2
prereqs: []
slug: 02-data-structures
sources:
- https://docs.python.org/3/tutorial/datastructures.html
- https://docs.python.org/3/library/stdtypes.html
- https://docs.python.org/3/reference/datamodel.html
spiral: []
status: ready
summary: 'В Python четыре базовых контейнера — list, dict, set, tuple. Выбор правильного — это вопрос изменяемости и хешируемости, а ошибка стоит O(n) там, где O(1) был бесплатным.'
title: 'Списки, словари, множества, кортежи'
track: python
unit: 01-language-core
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import StackDiagram from "~/components/diagram/StackDiagram.astro";

<Hook>
Ревьюер помечает твою функцию: на каждый запрос она сканирует `list` из 50 000 элементов через `if user_id in seen`, и эндпойнт еле ползёт. Ты написал это как в JS — массив, цикл, по духу `.includes()`. Фикс — один символ намерения: сделай `seen` множеством (`set`). Проверка вхождения падает с O(n) до O(1), и p99 схлопывается. Те же данные, та же форма кода — другой контейнер, потому что в Python контейнер *и есть* решение о производительности. Хвататься за `list` рефлекторно, как за `[]` в JavaScript, — самый частый способ написать случайно квадратичный Python.
</Hook>

<Crux>list, dict, set, tuple — две оси: можно ли менять (изменяемый?) и можно ли сделать ключом (хешируемый?). Ответы выбирают контейнер.</Crux>

<Explanation>

## Всё решают два вопроса: изменяемый? хешируемый?

Python даёт четыре встроенных контейнера, и выбор между ними — не вкусовщина, а два вопроса «да/нет». **Изменяемый ли он?** — можно ли поменять его на месте после создания. **Хешируемый ли он?** — может ли Python вычислить стабильный хеш, чтобы объект стал ключом `dict` или членом `set`. Эти свойства связаны: объект хешируем, только если его значение не может измениться под хешем, поэтому *изменяемые встроенные типы не хешируемы*. Это единственное правило объясняет, почему `list` не может быть ключом `dict`, а `tuple` может.

- **`list`** — упорядоченный, изменяемый, рабочая лошадка-последовательность. Ближе всего к JS `Array`, но методы другие (`.append()`, а не `.push()`; `.sort()` меняет на месте и возвращает `None`). Добавление в конец — амортизированный O(1); `insert(0, x)`, `del lst[0]` и `x in lst` — все O(n). Не хешируем.
- **`dict`** — хеш-таблица с **гарантированным порядком вставки** начиная с Python 3.7. Ключи должны быть хешируемыми; поиск, вставка и удаление — O(1) в среднем. Это объединённые в один тип JS `Object` и `Map`: и со строковыми ключами, *и* с произвольными хешируемыми ключами, упорядоченный, быстрый.
- **`set`** — неупорядоченная коллекция уникальных хешируемых членов. O(1) на вхождение, плюс первоклассные `|` объединение, `&` пересечение, `-` разность. Как JS `Set`, но с настоящей алгеброй множеств из коробки.
- **`tuple`** — упорядоченный, **неизменяемый** и хешируемый, *если хешируем каждый элемент*. В JavaScript эквивалента нет. Это тип для фиксированных записей, множественного возврата и — что важно — составных ключей `dict`/`set`.

```python
point = (3, 4)              # tuple: fixed record, hashable
grid = {point: "origin"}   # tuple as a dict key — works
grid[[3, 4]]               # TypeError: unhashable type: 'list'

x, y = point               # unpacking a tuple into two names
def bounds(): return 0, 100  # returning a tuple (the comma makes it)
lo, hi = bounds()
```

<StackDiagram
  label="Четыре базовых контейнера Python, расставленные по двум вопросам, которые их выбирают — изменяемость и хешируемость"
  hue="var(--accent)"
  layers={[
    { label: "list — упорядоченный, изменяемый", note: "[] · не хешируем · O(1) append, O(n) in" },
    { label: "dict — упорядоченный, изменяемый", note: "{k: v} · ключи хешируемы · O(1) lookup" },
    { label: "set — неупорядоченный, изменяемый", note: "{x} · члены хешируемы · O(1) membership" },
    { label: "tuple — упорядоченный, неизменяемый", note: "(a, b) · хешируем если элементы · валидный ключ" },
  ]}
  caption="Изменяемые контейнеры (list, dict, set) не могут быть ключами; неизменяемый tuple может. Хешируемость — это пропуск к ключам dict и членам set."
/>

## Шпаргалка и перевод с JS

Когда приходишь из JavaScript, ошибка не в синтаксисе — в том, что `Array` натягивают на всё. JS заставляет имитировать множество объектом (`{}`) или `Map`, имитировать алгебру множеств фильтрами и вовсе не имеет неизменяемой записи. Python раскладывает эти задачи по четырём специализированным типам, и цена неправильного выбора — реальная алгоритмическая сложность, а не просто стиль.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Тип</th>
        <th class="border border-border px-3 py-2 text-left">Изменяемый?</th>
        <th class="border border-border px-3 py-2 text-left">Упорядоченный?</th>
        <th class="border border-border px-3 py-2 text-left">Хешируемый?</th>
        <th class="border border-border px-3 py-2 text-left">Бери его, когда…</th>
        <th class="border border-border px-3 py-2 text-left">Аналог в JS</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2"><code>list</code></td>
        <td class="border border-border px-3 py-2">да</td>
        <td class="border border-border px-3 py-2">да</td>
        <td class="border border-border px-3 py-2">нет</td>
        <td class="border border-border px-3 py-2">упорядоченная растущая последовательность для итерации/индексации</td>
        <td class="border border-border px-3 py-2"><code>Array</code></td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2"><code>dict</code></td>
        <td class="border border-border px-3 py-2">да</td>
        <td class="border border-border px-3 py-2">да (вставка)</td>
        <td class="border border-border px-3 py-2">нет</td>
        <td class="border border-border px-3 py-2">поиск ключ→значение, подсчёт, группировка, любое отображение</td>
        <td class="border border-border px-3 py-2"><code>Object</code> / <code>Map</code></td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2"><code>set</code></td>
        <td class="border border-border px-3 py-2">да</td>
        <td class="border border-border px-3 py-2">нет</td>
        <td class="border border-border px-3 py-2">нет</td>
        <td class="border border-border px-3 py-2">проверки вхождения, дедупликация, объединение/пересечение</td>
        <td class="border border-border px-3 py-2"><code>Set</code></td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2"><code>tuple</code></td>
        <td class="border border-border px-3 py-2">нет</td>
        <td class="border border-border px-3 py-2">да</td>
        <td class="border border-border px-3 py-2">да*</td>
        <td class="border border-border px-3 py-2">фиксированные записи, множественный возврат, составные ключи dict/set</td>
        <td class="border border-border px-3 py-2">(нет)</td>
      </tr>
    </tbody>
  </table>
</div>

Звёздочка у `tuple` важна: кортеж хешируем, *только если хешируемы все его элементы*. `(1, 2)` — нормальный ключ; `(1, [2])` — нет, потому что внутри вложен `list`. Хешируемость рекурсивна.

<TradeoffMatrix
  id="python-02-data-structures-tradeoff-1"
  lessonSlug="02-data-structures"
  lang="ru"
  prompt="Ты обрабатываешь поток событий и миллионы раз отвечаешь на вопрос «видел ли я уже пару (user_id, day)?». Выбери структуру для хранения виденных пар."
  options={[
    { name: "set из кортежей (user_id, day)", summary: "Хешируемый составной ключ.", correct: true, justification: "Ровно то, что нужно. Кортеж — неизменяемый, хешируемый составной объект, идеальная составная идентичность, а хранение этих кортежей в set даёт O(1) на вхождение для теста «видел ли?», то есть для всего горячего пути. Дедупликация автоматическая, потому что множества хранят уникальные члены. Это идиоматичный ответ Python, и он остаётся быстрым по мере роста потока." },
    { name: "list из пар [user_id, day] с проверкой через `in`", summary: "Линейный скан.", justification: "Это и есть ловушка случайной квадратичности. `pair in some_list` — O(n)-скан, поэтому миллионы проверок по растущему списку деградируют до O(n²) в сумме. И дедуплицировать он за тебя не может. Список — для упорядоченной итерации, а не для вхождения: неправильный инструмент здесь, хотя это рефлекс из JS." },
    { name: "dict, отображающий user_id → list дней", summary: "Вложенный поиск.", justification: "Рабочий вариант, но избыточный для чистого вопроса о вхождении, а внутренний список снова вносит O(n)-скан на пользователя. Если нужно лишь «видел или нет», set из кортежей проще и равномерно O(1). Бери такую форму, только когда тебе реально нужен сам список дней пользователя как данные." },
    { name: "set из строк f'{user_id}:{day}'", summary: "Ключ-строка.", justification: "Работает и даёт O(1), но превращение составного ключа в строку — запашок: ты сериализуешь вручную, рискуешь коллизией разделителя (а если в id есть ':'?) и теряешь типизированную пару. Кортеж — нативный составной ключ: пусть Python хеширует его напрямую, а не выдумывай строковый формат." },
  ]}
/>

## Идентичность vs равенство и ловушка изменяемого дефолта

Две ловушки бьют инженеров, переходящих из JS. Первая — **идентичность (`is`) против равенства (`==`)**. `==` сравнивает значения; `is` сравнивает идентичность объекта (тот же объект в памяти) и именно его надо использовать в проверках `None`/`True`/`False`. Они расходятся так, что выглядит магией: `[1,2] == [1,2]` даёт `True`, а `[1,2] is [1,2]` — `False` (два разных списка). CPython *кэширует* малые целые и короткие строки, поэтому `256 is 256` может быть `True`, а `257 is 257` — `False`: деталь реализации, на которую нельзя полагаться. Правило: `==` для проверок значений, `is` — только для `None` и других синглтонов.

Вторая — **изменяемый аргумент по умолчанию**. Значение по умолчанию вычисляется *один раз*, при определении функции, и разделяется между всеми вызовами — поэтому изменяемый дефолт вроде `[]` или `{}` накапливает состояние между вызовами, и баг выглядит как наваждение.

```python
def add(item, bucket=[]):     # bucket is created ONCE, shared forever
    bucket.append(item)
    return bucket

add("a")        # ['a']
add("b")        # ['a', 'b']  ← surprise: the same list persisted

def add(item, bucket=None):   # the fix: sentinel + fresh object per call
    if bucket is None:
        bucket = []
    bucket.append(item)
    return bucket
```

<Inset kind="why" lang="ru">
Присваивание в Python связывает имя с объектом; оно никогда не копирует. Поэтому `b = a` для списка означает, что `a` и `b` — *один и тот же* список, и `b.append(1)` меняет оба. Копирование явное: `a.copy()` (или `a[:]`, `list(a)`) делает **поверхностную** копию — новый внешний контейнер с *теми же* внутренними объектами, так что мутация вложенного списка по-прежнему видна через оба. Для полностью независимого клона вложенных данных бери `copy.deepcopy()`. Это та же модель ссылочной семантики, что у объектов и массивов в JS, — ловушка в допущении, что `=` дублирует.
</Inset>

<Quiz
  id="python-02-data-structures-quiz-1"
  lessonSlug="02-data-structures"
  lang="ru"
  question="Почему `d = {[1, 2]: 'x'}` вызывает TypeError, а `d = {(1, 2): 'x'}` работает?"
  choices={[
    { label: "list изменяем, поэтому нехешируем и не может быть ключом dict; tuple неизменяем и хешируем (когда хешируемы его элементы), поэтому может", correct: true },
    { label: "Списки слишком большие, чтобы хешировать их эффективно", misconception: "Размер не при чём. Хешируемость — про изменяемость: хеш ключа должен быть стабильным, а список может меняться, поэтому Python вообще отказывается его хешировать — независимо от числа элементов." },
    { label: "Ключи dict должны быть строками или числами", misconception: "Ключами dict может быть любой хешируемый объект — кортежи, frozenset, даже свои объекты с __hash__. Правило — хешируемость, а не фиксированный набор типов." },
    { label: "Сначала нужно вызвать hash() на списке", misconception: "Вызывать нечего; hash([1,2]) сам вызывает TypeError. У изменяемого объекта нет пригодного хеша — неизменяемость и есть предпосылка." },
  ]}
/>

<Quiz
  id="python-02-data-structures-quiz-2"
  lessonSlug="02-data-structures"
  lang="ru"
  question="Проверка вхождения `if x in c` бежит миллионы раз в горячем цикле. Какой контейнер держит её O(1)?"
  choices={[
    { label: "set (или dict) — вхождение через хеш — O(1) в среднем; у списка `in` — это O(n) линейный скан", correct: true },
    { label: "list — это самая быстрая последовательность в Python", misconception: "Список быстр на добавление и индексацию, но `x in list` обходит элементы по одному — O(n). В горячем цикле это становится квадратичным. Вхождение — то, для чего созданы set и ключи dict." },
    { label: "tuple — неизменяемость ускоряет поиск", misconception: "Неизменяемость не меняет сложность поиска; `x in tuple` — всё тот же O(n)-скан, как у списка. Кортежи — для фиксированных записей и ключей, а не для быстрого вхождения." },
    { label: "Любой — `in` в Python константный по времени", misconception: "`in` — O(n) на последовательностях (list, tuple, str) и O(1) на хеш-контейнерах (set, dict). Сложность задаёт тип контейнера, а не оператор." },
  ]}
/>

<DragOrder
  id="python-02-data-structures-drag-1"
  lessonSlug="02-data-structures"
  lang="ru"
  prompt="Функция должна записать каждую уникальную пару (user, action) один раз и быстро отвечать на вхождение. Расставь решение от главного вопроса вниз до финального выбора:"
  items={[
    { id: "a", label: "Нужно быстрое вхождение / уникальность? → да, значит хеш-контейнер (set/dict), а не list" },
    { id: "b", label: "Это чистое вхождение (видел или нет) или я храню значение? → видел или нет, значит set, а не dict" },
    { id: "c", label: "Что есть каждый член? → составная идентичность (user, action), значит составной ключ" },
    { id: "d", label: "Этот составной объект хешируем? → используй tuple (неизменяемый, хешируемый), а не list" },
    { id: "e", label: "Итог: set из кортежей (user, action) — O(1) вхождение, автоматическая дедупликация" },
  ]}
  correctOrder={["a", "b", "c", "d", "e"]}
/>

</Explanation>

<KeyTakeaway>Выбирай по двум вопросам: изменяемый? хешируемый? `list` — для упорядоченных последовательностей, `dict` — для отображений, `set` — для быстрого вхождения и дедупликации, `tuple` — для фиксированных записей и составных ключей. Вхождение в `list` — O(n); в `set`/`dict` — O(1).</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="python-02-data-structures-retrieval"
  lang="ru"
  questions={[
    { q: "Почему tuple может быть ключом dict, а list — нет, и как это связано с изменяемостью?", a: "dict (и set) хранит элементы в хеш-корзинах, поэтому ключ должен быть хешируемым — Python должен уметь вычислить хеш, остающийся стабильным всю жизнь объекта. Эта стабильность безопасна, только если значение объекта не может измениться, поэтому язык связывает хешируемость с неизменяемостью: изменяемые встроенные типы (list, dict, set) по замыслу нехешируемы, и попытка сделать такой объект ключом вызывает TypeError: unhashable type. tuple неизменяем — после создания его содержимое нельзя переприсвоить, — поэтому Python может его хешировать, что делает его законным ключом или членом set. Подвох в том, что хешируемость рекурсивна: кортеж хешируем, только если хешируем каждый элемент, поэтому (1, 2) работает, а (1, [2]) — нет, потому что внутри вложен изменяемый list. Именно поэтому идиоматичный составной ключ в Python — это кортеж из неизменяемых частей, например set из пар (user_id, day) для O(1)-теста «видел ли я это?»." },
    { q: "В чём ловушка изменяемого аргумента по умолчанию и какой правильный фикс?", a: "Значение аргумента по умолчанию в Python вычисляется один раз — при определении функции, а не на каждом вызове. Поэтому если написать def add(x, bucket=[]), этот один список создаётся в момент определения и разделяется всеми вызовами, которые не передали свой bucket. Каждый вызов дописывает в один и тот же объект, и состояние утекает между вызовами: add('a') вернёт ['a'], а потом add('b') вернёт ['a', 'b'] вместо ['b']. Выглядит так, будто функция заколдована, но это просто общий дефолт. Фикс — паттерн с сентинелом: дефолти параметр в None, а внутри функции делай `if bucket is None: bucket = []`, что создаёт свежий объект на каждый вызов. Та же логика ссылочной семантики объясняет и копии — присваивание связывает имена с объектами без копирования, .copy() делает поверхностную копию (новый внешний контейнер, те же внутренние объекты), а copy.deepcopy() нужен, чтобы полностью клонировать вложенные структуры." },
  ]}
/>

<Recap lang="ru">
Четыре базовых контейнера Python делятся по двум вопросам: изменяемость и хешируемость. `list` — твоя упорядоченная, изменяемая, растущая последовательность — JS `Array`, но `in` — это O(n)-скан, а `.append()`, а не `.push()`. `dict` — хеш-таблица с порядком вставки и O(1)-поиском, объединяющая JS `Object` и `Map`. `set` хранит уникальные хешируемые члены с O(1)-вхождением и настоящей алгеброй объединения/пересечения. `tuple` неизменяем и упорядочен, хешируем, когда хешируемы его элементы, — тип для фиксированных записей, множественного возврата и составных ключей `dict`/`set`, без аналога в JS. Изменяемость закрывает доступ к хешируемости: `list` не может быть ключом, `tuple` может. Повторяющиеся ошибки уровня сеньора — писать случайно квадратичный код, проверяя вхождение по `list` вместо `set`, опираться на `is` там, где нужен `==` (из-за кэша малых целых и строк в CPython `is` врёт), и ловушка изменяемого дефолта — лечится сентинелом `None` и свежим объектом на вызов. Выбирай контейнер по двум вопросам, а не по рефлексу из JS, и сложность позаботится о себе сама.
</Recap>
