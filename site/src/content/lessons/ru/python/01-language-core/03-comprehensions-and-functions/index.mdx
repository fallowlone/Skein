---
concepts:
- comprehensions
- generator-expressions
- mutable-default-args
- args-kwargs
- closures-legb
deepensInto: []
estMin: 17
lang: ru
lessonType: topic
level: middle
order: 3
prereqs: []
slug: 03-comprehensions-and-functions
sources:
- https://docs.python.org/3/tutorial/datastructures.html
- https://docs.python.org/3/reference/expressions.html
- https://docs.python.org/3/tutorial/controlflow.html
- https://docs.python.org/3/faq/programming.html
spiral: []
status: ready
summary: 'Comprehension — питоничный map/filter; generator expression стримит лениво по огромным входам. У функций острая ловушка — аргументы по умолчанию вычисляются один раз при def — плюс *args/**kwargs и LEGB-замыкания, непохожие на JS.'
title: 'Comprehensions и функции'
track: python
unit: 01-language-core
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";
import SequenceDiagram from "~/components/diagram/SequenceDiagram.astro";
import Infographic from "~/components/diagram/Infographic.astro";
import eagerVsLazy from "./infographics/eager-vs-lazy.svg?raw";

<Hook>
Коллега переносит JS-пайплайн на Python и пишет `result = list(map(lambda x: x * 2, filter(lambda x: x > 0, nums)))`. Работает, но читается как перевод, а не как Python. А другая функция, `def add_tag(tag, tags=[])`, вдруг начинает возвращать теги из *прошлых вызовов*, которых она не получала, — призрак, копящийся на весь процесс. Оба случая — один и тот же урок: инструменты Python на уровне выражений и его модель функций ведут себя не как в JavaScript, и расхождения бьют ровно там, где ты перестаёшь следить.
</Hook>

<Crux>Comprehension — это map/filter Python; generator делает их ленивыми. Default вычисляется один раз при def — классическая ловушка.</Crux>

<Explanation>

## Comprehension: map и filter, слитые в одно выражение

List comprehension — идиоматичная замена цепочке `.map()` и `.filter()` в Python. Форма `[f(x) for x in xs if pred(x)]` читается в порядке вычисления — источник, фильтр, преобразование — и за один проход даёт список. Там, где JavaScript пишет `xs.filter(pred).map(f)`, Python ставит преобразование первым, а фильтр последним, но данные текут так же: каждый элемент `xs` проверяется `pred`, и выжившие проходят через `f`.

```python
nums = [-2, -1, 0, 1, 2, 3]
# JS-эквивалент: nums.filter(x => x > 0).map(x => x * 2)
doubled = [x * 2 for x in nums if x > 0]   # [2, 4, 6]

# Тот же синтаксис, другие скобки, другая коллекция:
squares = {x: x * x for x in range(4)}      # dict: {0: 0, 1: 1, 2: 4, 3: 9}
unique  = {len(w) for w in ["a", "bb", "cc"]}  # set: {1, 2}
```

Форма `{...}` даёт **dict** или **set** comprehension в зависимости от того, пишешь ли ты `k: v` или голый элемент, — настолько прямого аналога объектному литералу в JS нет. Это первая причина, почему comprehensions важны: одно семейство синтаксиса покрывает map, filter и построение любой встроенной коллекции.

<FlowDiagram
  label="Как list comprehension преобразует исходный итерируемый объект через фильтр и отображение в новый список"
  hue="var(--accent)"
  perRow={4}
  nodes={[
    { id: "src", label: "Источник", sub: "for x in xs" },
    { id: "pred", label: "Фильтр", sub: "if pred(x)" },
    { id: "map", label: "Преобраз.", sub: "f(x)" },
    { id: "out", label: "Новый список", sub: "[ ... ]" },
  ]}
  edges={[
    { from: "src", to: "pred", label: "each x" },
    { from: "pred", to: "map", label: "kept" },
    { from: "map", to: "out", label: "f(x)" },
  ]}
  caption="Comprehension — это слитые map и filter: каждый элемент течёт из источника через предикат, а выжившие за один проход преобразуются в выходную коллекцию."
/>

Вложенные comprehensions существуют — `[c for row in matrix for c in row]` расплющивает 2D-список, причём циклы читаются слева направо, как если бы ты вложил `for`-инструкции. Но именно тут читаемость гибнет быстро. Comprehension с двумя `for` и условием — на грани того, что читатель распарсит с одного взгляда; три — уже за гранью. Сеньорское суждение: comprehension должен умещаться в голове за один проход. Как только перестаёт — пиши явный цикл. Хитрость в comprehension — это цена, которую платишь на каждом будущем прочтении.

## Генераторы: та же форма, но лениво

Замени квадратные скобки на круглые, и `[f(x) for x in xs]` становится **generator expression** `(f(x) for x in xs)`. Это не косметика. List comprehension *жадный*: он строит весь список в памяти до того, как выполнится следующая строка. Генератор *ленивый*: вычисляет по одному значению, только когда попросят, и держит по сути постоянную память независимо от того, сколько элементов через него проходит.

```python
# Жадно: материализует 10 миллионов int — сотни МБ резидентно.
total = sum([x * x for x in range(10_000_000)])

# Лениво: по одному значению, память остаётся плоской.
total = sum(x * x for x in range(10_000_000))
```

Та же идея движет ключевым словом `yield`. Функция, содержащая `yield`, — это функция-генератор: её вызов не выполняет тело, а возвращает объект-генератор, который работает лениво, приостанавливаясь на каждом `yield` и возобновляясь по следующему запросу. Так стримят данные, не помещающиеся в память.

```python
def read_lines(path):
    with open(path) as f:
        for line in f:
            yield line.rstrip("\n")   # резидентна одна строка, а не весь файл

for line in read_lines("huge.log"):    # постоянная память на файле 50 ГБ
    ...
```

Это и есть настоящий сеньорский смысл всей первой половины. Тянись к **жадному списку**, когда результат нужен не один раз, нужна его длина или индексация. Тянись к **ленивому генератору**, когда вход большой или неограниченный, ты проходишь по нему один раз и хочешь, чтобы память оставалась плоской, — стриминговый пайплайн, чтение файла, пагинатор API.

<Infographic
  svg={eagerVsLazy}
  label="Развилка решения бок о бок: выбирай жадный список, когда результат нужен не один раз, нужна его длина или индексация; выбирай ленивый генератор, когда вход большой или неограниченный, проход один раз, а память должна оставаться плоской."
  caption="Собирай списком, стримь генератором: выбор зависит от переиспользования, длины и индексации против размера входа, одного прохода и плоской памяти."
  hue="var(--d-infra)"
/>

<Inset kind="why" lang="ru">
Генератор одноразовый: это итератор, который исчерпывается по мере потребления. После одного полного прохода он пуст — второй `for`-цикл по тому же объекту-генератору не даст ничего, а `list(gen)` после того, как ты уже проитерировал, вернёт `[]`. Это удивляет тех, кто пришёл из JS-массивов, которые можно перебирать сколько угодно раз. Если нужно пройти данные дважды — либо материализуй их один раз через `list(gen)`, либо построй новый генератор. Лень, экономящая память, ровно и делает его одноразовым.
</Inset>

## Функции: аргументы и ловушка, определяющая Python

Прежде чем писать первую питоновскую функцию с опциональным аргументом-списком, выучи одно правило — иначе потратишь полдня на охоту за призраком.

`def` определяет функцию; аргументы передаются позиционно или по ключу, а параметры могут нести значения по умолчанию: `def connect(host, port=5432, *, timeout=30)`. Голая `*` помечает всё после неё **только-по-ключу** — вызывающий обязан писать `timeout=10`, а не голую позицию, — так держат место вызова читаемым по мере роста сигнатуры. `*args` собирает лишние позиционные в кортеж, а `**kwargs` — лишние ключевые в словарь; на месте вызова те же `*`/`**` *распаковывают* итерируемое или отображение в аргументы — питоновский аналог JS spread.

```python
def log(level, *args, **kwargs):
    print(level, args, kwargs)

log("INFO", "started", port=8080)   # INFO ('started',) {'port': 8080}
```

Теперь ловушка. **Аргумент по умолчанию вычисляется ровно один раз, когда выполняется инструкция `def`, — а не на каждый вызов.** Если default — изменяемый объект, каждый вызов, опускающий этот аргумент, делит *один и тот же* объект, и мутации сохраняются между вызовами.

```python
def add_tag(tag, tags=[]):     # [] создаётся ОДИН раз, при def
    tags.append(tag)
    return tags

add_tag("a")   # ['a']
add_tag("b")   # ['a', 'b']  ← тот же список, переживший вызовы
```

Это не баг Python; это прямое следствие правила «defaults вычисляются во время определения». JavaScript перевычисляет параметры по умолчанию на каждый вызов, поэтому `function f(x = [])` свежий каждый раз — ровно наоборот. Идиоматичный питоновский фикс — sentinel:

```python
def add_tag(tag, tags=None):
    if tags is None:
        tags = []          # свежий список на каждый вызов, опускающий его
    tags.append(tag)
    return tags
```

<SequenceDiagram
  label="Почему мутабельный default течёт между вызовами: список по умолчанию создаётся один раз, когда исполняется инструкция def, хранится на объекте функции и разделяется каждым последующим вызовом, опускающим аргумент"
  hue="var(--accent)"
  actors={["def time", "default-список", "вызов #1", "вызов #2"]}
  messages={[
    { from: "def time", to: "default-список", label: "tags=[] один раз" },
    { from: "вызов #1", to: "default-список", label: "append('a')" },
    { from: "вызов #2", to: "default-список", label: "видит ['a']" },
  ]}
  caption="JavaScript перевычисляет параметры по умолчанию на каждый вызов; Python вычисляет их один раз, при def. Оба вызова мутируют один и тот же хранимый список — фикс-sentinel (tags=None, создание внутри тела) даёт каждому вызову свежий список."
/>

## Lambda, замыкания и LEGB-scope

`lambda` создаёт анонимную функцию, но она намеренно ограничена: одно выражение, никаких инструкций, никаких аннотаций. Это *не* JS arrow function — многострочной lambda не бывает. Используй её для одноразовой key-функции (`sorted(words, key=lambda w: len(w))`) и тянись к именованному `def` в тот же миг, когда ей нужна вторая строка.

Функции — объекты первого класса (передаются, возвращаются, хранятся), а внутренняя функция, ссылающаяся на переменную внешней, образует **замыкание** (closure), захватывая её по ссылке. Разрешение имён идёт по **LEGB**: Python смотрит в **L**ocal-scope, затем в любые объемлющие (**E**nclosing) scope функций, затем в **G**lobal (модульный) scope, затем в **B**uilt-ins. Важно: присваивание имени внутри функции делает его локальным *на всю функцию*, если не сказать иное, — `global` перепривязывает имя уровня модуля, а `nonlocal` — имя в ближайшей объемлющей функции. JS тоже разрешает scope лексически, но `let`/`const` дают привязку на блок, тогда как единица scope в Python — функция, и «присваивание означает локальное» — более острый край.

```python
def make_counter():
    count = 0
    def tick():
        nonlocal count    # без этого count += 1 кидает UnboundLocalError
        count += 1
        return count
    return tick
```

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Идиома JavaScript</th>
        <th class="border border-border px-3 py-2 text-left">Идиома Python</th>
        <th class="border border-border px-3 py-2 text-left">Заметка</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2"><code>xs.map(f)</code></td>
        <td class="border border-border px-3 py-2"><code>[f(x) for x in xs]</code></td>
        <td class="border border-border px-3 py-2">Comprehension, один проход</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2"><code>xs.filter(p)</code></td>
        <td class="border border-border px-3 py-2"><code>[x for x in xs if p(x)]</code></td>
        <td class="border border-border px-3 py-2">Фильтр — это хвостовой <code>if</code></td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2"><code>x =&gt; x * 2</code></td>
        <td class="border border-border px-3 py-2"><code>lambda x: x * 2</code></td>
        <td class="border border-border px-3 py-2">Только одно выражение; иначе <code>def</code></td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2"><code>f(...arr)</code> / <code>&#123;...obj&#125;</code></td>
        <td class="border border-border px-3 py-2"><code>f(*seq)</code> / <code>f(**mapping)</code></td>
        <td class="border border-border px-3 py-2">Spread → распаковка</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2"><code>function(...args)</code></td>
        <td class="border border-border px-3 py-2"><code>def f(*args, **kwargs)</code></td>
        <td class="border border-border px-3 py-2">Rest-параметры → <code>*args</code>/<code>**kwargs</code></td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">генераторы / <code>yield</code></td>
        <td class="border border-border px-3 py-2"><code>(f(x) for x in xs)</code> / <code>yield</code></td>
        <td class="border border-border px-3 py-2">Та же лень, круглые скобки, не квадратные</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2"><code>function f(x = [])</code> свежий каждый вызов</td>
        <td class="border border-border px-3 py-2"><code>def f(x=None)</code> + sentinel</td>
        <td class="border border-border px-3 py-2">Default в Python вычисляется один раз при def</td>
      </tr>
    </tbody>
  </table>
</div>

<Quiz
  id="py-comp-fn-quiz-1"
  lessonSlug="03-comprehensions-and-functions"
  lang="ru"
  question="Что напечатает add_tag('a'); add_tag('b'), если def add_tag(tag, tags=[]): tags.append(tag); return tags?"
  choices={[
    { label: "['a'], затем ['a', 'b'] — список по умолчанию создан один раз при def и общий для вызовов, опускающих tags", correct: true },
    { label: "['a'], затем ['b'] — каждый вызов получает свежий пустой список", misconception: "Это поведение JavaScript, где defaults перевычисляются на каждый вызов. В Python объект по умолчанию создаётся один раз при выполнении def и потому переживает вызовы." },
    { label: "Кидает ошибку, потому что нельзя мутировать аргумент по умолчанию", misconception: "Мутировать default разрешено — в этом и ловушка. Список — настоящий общий объект, append работает; он просто хранит данные между вызовами." },
    { label: "['a', 'b'] оба раза — список сбрасывается, но хранит историю", misconception: "Сброса нет. Тот же объект-список копит данные, поэтому первый вызов возвращает ['a'], а второй — ['a', 'b']." },
  ]}
/>

<Quiz
  id="py-comp-fn-quiz-2"
  lessonSlug="03-comprehensions-and-functions"
  lang="ru"
  question="g = (x*x for x in range(3)). После total = sum(g) что вернёт list(g)?"
  choices={[
    { label: "[] — генератор одноразовый итератор; sum() исчерпал его, поэтому второй проход не даёт ничего", correct: true },
    { label: "[0, 1, 4] — генераторы можно перебирать сколько угодно раз", misconception: "Это список, а не генератор. Generator expression — одноразовый итератор: после потребления он пуст, в отличие от JS-массива." },
    { label: "[0, 1, 4] — list() пересоберёт его из исходного range", misconception: "list() не перезапускает источник генератора; он лишь сливает то, что в генераторе осталось, а после sum() там ничего нет." },
    { label: "Кидает StopIteration", misconception: "Перебор исчерпанного генератора через for/list не кидает — он просто не выдаёт элементов и возвращает пустой список." },
  ]}
/>

<TradeoffMatrix
  id="py-comp-fn-tradeoff-1"
  lessonSlug="03-comprehensions-and-functions"
  lang="ru"
  prompt="Пайплайн должен преобразовать лог-файл 50 ГБ построчно и записать сводку на машине с 2 ГБ RAM. Выбери конструкцию."
  options={[
    { name: "Генератор: def lines(): ... yield ... — обработка по одной строке", summary: "Постоянная память.", correct: true, justification: "Подходит для memory-bound потока в один проход. Генератор держит резидентно одну строку за раз, поэтому память остаётся плоской независимо от размера файла — файл 50 ГБ нормально обработается в 2 ГБ RAM. Ты проходишь по данным один раз, ровно как и нужно для сводки, и никогда не материализуешь весь файл. Это каноничное применение yield: стриминг данных больше памяти." },
    { name: "Жадный список: lines = [transform(l) for l in open(path)]", summary: "Материализует всё.", justification: "Это строит весь преобразованный файл в памяти ещё до начала сводки — 50 ГБ данных в 2 ГБ-машину означают немедленный MemoryError или трешинг. List comprehensions — неверный инструмент в тот миг, когда вход не помещается комфортно; их жадность здесь обуза, а не плюс." },
    { name: "Прочитать весь файл через f.read(), потом split и map", summary: "Весь файл в строке.", justification: "Хуже жадного списка: f.read() затягивает все 50 ГБ в одну строку первым делом, разнося память до запуска любого transform. Чтение огромного файла целиком — классическая out-of-memory ошибка; стримь его построчно." },
    { name: "List comprehension, но gc.collect() между батчами", summary: "Форсировать GC.", justification: "Сборка мусора не поможет, пока данные ещё ссылаются — comprehension держит каждую преобразованную строку живой в списке-результате, так что собирать нечего. GC не заменяет лень; фикс — не материализовать всё, то есть генератор." },
  ]}
/>

</Explanation>

<KeyTakeaway>Пиши `[f(x) for x in xs if p(x)]` вместо map/filter; меняй `[]` на `()` для ленивого генератора по огромным входам. И никогда не используй изменяемый default — `def f(x=[])` делит один список между вызовами; используй `x=None` + sentinel.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="py-comp-fn-retrieval"
  lang="ru"
  questions={[
    { q: "Когда выбирать generator expression вместо list comprehension и какова цена?", a: "Выбирай генератор, когда вход большой или неограниченный, проход нужен лишь один раз, а память должна оставаться плоской. List comprehension жадный — он строит весь результат в памяти до выполнения следующей строки, что нормально, когда данные нужны не раз, нужна их длина или индексация, но губительно для файла 50 ГБ или бесконечного потока. Генератор ленивый: вычисляет по одному значению, по запросу, поэтому обрабатывает данные сильно больше RAM при постоянной памяти. Цена двойная. Во-первых, генератор одноразовый — это итератор, исчерпывающийся по мере потребления, так что второй проход не даёт ничего, и нужно либо материализовать через list(), либо построить новый генератор. Во-вторых, ты теряешь len() и индексацию, потому что ничего не хранится. Правило: стримь генератором, собирай списком, а решай по тому, проходишь ли данные раз или много раз и помещаются ли они в память." },
    { q: "Почему def f(x=[]) копит состояние между вызовами и каков идиоматичный фикс?", a: "Потому что аргумент по умолчанию вычисляется ровно один раз — когда выполняется инструкция def, а не на каждый вызов. [] создаётся единожды и привязывается к параметру; каждый вызов, опускающий этот аргумент, получает тот же самый объект-список, поэтому append из одного вызова виден в следующем, и состояние течёт на всю программу. Это прямое следствие модели Python: значения по умолчанию хранятся на объекте функции во время определения. Это противоположно JavaScript, где параметры по умолчанию перевычисляются на каждый вызов и потому свежие каждый раз. Идиоматичный фикс — паттерн sentinel: пиши def f(x=None), а внутри тела делай `if x is None: x = []`, что создаёт по-настоящему свежий список на каждый вызов, опускающий аргумент. Используй None (или другой неизменяемый sentinel) для любого изменяемого default — list, dict, set — и никогда не ставь изменяемый литерал прямо в сигнатуру." },
  ]}
/>

<Recap lang="ru">
Comprehension — это слитые map и filter Python: `[f(x) for x in xs if pred(x)]` заменяет цепочку `.filter().map()` одним читаемым проходом, а форма `{...}` строит ещё и dict, и set, — но comprehension, который больше не умещается в голове за один проход, должен стать явным циклом. Замена квадратных скобок на круглые превращает его в ленивый generator expression (выражение-генератор, вычисляет по одному значению) — та же ленивая механика, что и `yield`: по одному значению, постоянная память, одноразовость. Собирай списком, когда результат нужен многократно, нужна длина или индексация; стримь генератором, когда вход большой или одно-проходный. На стороне функций `*args`/`**kwargs` собирают и распаковывают аргументы, голая `*` делает параметры только-по-ключу, а `lambda` — одноразовое одно-выражение; тянись к `def`, как только она растёт. Определяющая ловушка: аргументы по умолчанию вычисляются один раз при def, поэтому изменяемый default вроде `[]` общий для вызовов; используй `None` плюс sentinel. Наконец, разрешение имён — LEGB, scope — на функцию, а не на блок, и присваивание делает имя локальным, пока ты не тянешься к `global` или `nonlocal`, чтобы перепривязать внешнее. Теперь, когда встречаешь в сигнатуре `x=None` с проверкой сразу в теле или генераторное выражение, переданное прямо в `sum()`, — ты видишь намеренный паттерн, а не случайность.
</Recap>
