---
concepts:
- python-cancellation
deepensInto: []
estMin: 18
lang: ru
lessonType: topic
level: senior
order: 2
prereqs: []
slug: "02-cancellation-semantics"
sources:
- https://docs.python.org/3/library/asyncio-task.html
- https://docs.python.org/3/library/asyncio-exceptions.html
spiral: []
status: ready
summary: 'task.cancel() бросает CancelledError в следующем await; между await код непрерываем. except Exception его пропускает, голый except глотает — бессмертные задачи. Уборка в finally, await в ней ограничены, затем re-raise. TaskGroup отменяет соседей при первом сбое.'
title: 'Семантика отмены: внедрённый CancelledError, баг бессмертия через голый except и уборка, обязанная пере-бросить'
track: "python"
unit: "11-async-deep"
---

import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import SequenceDiagram from "~/components/diagram/SequenceDiagram.astro";

<Hook>
Деплой должен был занять девяносто секунд. Сервис-консьюмер получил SIGTERM, записал в лог «shutting down, cancelling 32 workers» — и дальше ничего, десять минут, пока оркестратор не потерял терпение и не прислал SIGKILL. Постмортем нашёл цикл воркера, обёрнутый в голый `except:`, написанный годы назад под комментарием «воркеры должны переживать всё». Он переживал отмену. Каждый `task.cancel()` исправно бросал `CancelledError` в воркер на ближайшем await; голый except ловил его, логировал `worker recovered: CancelledError()` и возвращался к `queue.get()`. Тридцать два бессмертных таска, `gather()`, который не мог вернуться никогда, и — самая дорогая часть — тридцать два неподтверждённых джоба в полёте, которые брокер передоставил после рестарта и которые обработались дважды. Письма клиентам ушли в двух экземплярах. Фикс занял три строки дисциплины исключений. Уроком была сама модель: отмена в asyncio — не рубильник, а исключение, которому ваш код обязан дать пройти, — и ломается всё ровно там, где это доверие нарушают.
</Hook>

<Crux>cancel() не останавливает задачу: бросает CancelledError в следующий await. Проглотить = бессмертная задача; после cleanup — re-raise.</Crux>

<Explanation>

## Отмена — это внедрённое исключение, а не флаг

`task.cancel()` ничего не останавливает. Он договаривается с циклом, что тот **бросит `CancelledError` в корутину в её следующей точке приостановки** — механически Task возобновляет корутину через `coro.throw(CancelledError())` вместо `send()` (машинерия урока 1, поставленная на боевой взвод). Две вещи определяют всю модель. Первая: *код между await непрерываем* — это гарантия (обновление словаря или двухшаговая смена состояния никогда не будут разорваны отменой пополам) и одновременно ловушка (30-секундный отрезок чистого CPU неотменяем всю свою длительность, потому что throw негде приземлиться). Вторая: отмена доставляется **только** на await, которые реально приостанавливают, — различение из урока 1 возвращается с зубами. await завершённой футуры или синхронно цепляющейся корутины — не точка отмены. Для длинных CPU-секций, обязанных оставаться отзывчивыми к отмене, честный приём — периодический `await asyncio.sleep(0)`: он вставляет настоящую приостановку, куда throw может приземлиться, ценой круга через цикл на каждую проверку.

## Баг проглатывания, точно

С Python 3.8 `CancelledError` наследуется от `BaseException`, а не от `Exception` — намеренное ломающее изменение, сделанное ради того, чтобы самый частый защитный паттерн стал безопасным:

```python
async def worker(q):
    while True:
        job = await q.get()
        try:
            await handle(job)
        except Exception:        # НЕ ловит CancelledError (3.8+): хорошо
            log.exception("джоб упал, продолжаем")
        # except:                # ловит BaseException: отмена умирает здесь
        # except BaseException:  # тот же баг, написанный честно
```

`except Exception` пропускает отмену насквозь — задача разматывается и заканчивается в состоянии cancelled, ровно как хотел отменяющий. **Голый `except`** (или `except BaseException`) её ловит, и если обработчик после этого продолжает цикл, задача бессмертна: `cancel()` вернул True, исключение доставлено — и ничего не умерло. Это и был баг из Хука, а по духу он старше 3.8 — до 3.8 даже `except Exception` глотал отмену, поэтому старые кодовые базы — минные поля. Дисциплина на случай, когда отмену *нужно* увидеть: поймать, убраться, **пере-бросить**:

```python
        except asyncio.CancelledError:
            await q.nack(job)    # вернуть джоб в полёте (ограничьте это! см. ниже)
            raise                # ВСЕГДА — отменяющему причитается размотка
```

У проглатывания без re-raise есть вторая цена, кроме бессмертия: `await task` после этого возвращается нормально, `task.cancelled()` — False, и вся машинерия фреймворков, проверяющая «сработал ли мой cancel» — TaskGroup, таймаут-скоупы, — получает ложь.

<Quiz
  id="02-cancellation-semantics-quiz-1"
  lessonSlug="02-cancellation-semantics"
  lang="ru"
  question="Код-ревью: воркер оборачивает обработку джоба в `try: await handle(job)` / `except: log('retrying')` и продолжает свой while-True. Что произойдёт, когда shutdown вызовет task.cancel()?"
  choices={[
    { label: "Голый except поймает CancelledError (это BaseException), залогирует 'retrying', цикл продолжится — задача не умрёт никогда, и shutdown зависнет на её ожидании", correct: true },
    { label: "Ничего отличного от except Exception — голый except просто его сокращение", misconception: "Голый except ловит BaseException, куда с 3.8 входит CancelledError — именно затем, чтобы except Exception остался безопасным для отмены. Два написания расходятся ровно здесь, и разница — бессмертная задача." },
    { label: "cancel() убьёт задачу принудительно, что бы ни делали обработчики", misconception: "Принудительного убийства нет. cancel() лишь планирует throw на следующем await; дальше — обычное распространение исключения, и обработчик, поймавший и продолживший, побеждает его полностью." },
    { label: "CancelledError вообще нельзя поймать пользовательским кодом", misconception: "Это обычный объект исключения, и любой подходящий обработчик его ловит — в этом и вся опасность. Дисциплина не в том, что поймать нельзя, а в том, что после уборки обязателен re-raise." },
  ]}
/>

## await в finally: ловушка двойной отмены

Уборка, которая сама делает await, — место, где зависает корректно выглядящий код. Отменённая задача выполняет свои `finally`-блоки по мере размотки исключения — но `await` внутри `finally` — это новая точка приостановки, и *второй* `cancel()` (нетерпеливый цикл shutdown, истекающий вокруг вас таймаут-скоуп) приземляется ровно туда, бросив остаток уборки. Хуже: сам await уборки может заблокироваться навсегда — `nack()` мёртвому брокеру — и теперь задача неубиваема-из-вежливости. Дисциплина: **ограничивайте каждый await на пути отмены**. Оборачивайте уборку в `asyncio.timeout(...)` (следующий урок разберёт его), или шилдите по-настоящему обязанную завершиться работу; никогда не позволяйте уборке ждать тех же условий, что вызвали отмену.

```python
finally:
    with contextlib.suppress(TimeoutError):
        async with asyncio.timeout(2):   # уборке даётся 2 с, не вечность
            await q.nack(job)
```

## TaskGroup: структурная отмена и чей это был cancel

Поведение TaskGroup вы знаете из юнита про конкурентность: первый сбой ребёнка отменяет соседей, результаты приходят как `ExceptionGroup`. Глубокая деталь — как он отличает **внутреннюю** отмену (группа отменяет собственных детей) от **внешней** (кто-то отменил всю группу снаружи): каждая задача держит *счётчик отмен* — `cancel()` инкрементирует, `uncancel()` декрементирует. Когда TaskGroup или таймаут-скоуп отменяет тело по своим причинам, позже он вызывает `uncancel()`, чтобы погасить свой запрос и решить: счётчик вернулся к нулю — отмена была моя, докладываем сбой соседа (или TimeoutError); всё ещё положительный — в полёте есть и внешняя отмена, и она обязана распространяться дальше. Честно: `uncancel()` — машинерия фреймворков, прикладной код её почти не вызывает, — но знание о ней объясняет, почему проглатывание `CancelledError` внутри TaskGroup или таймаут-скоупа портит их бухгалтерию и почему правило re-raise — не этикет, а протокол.

<Inset kind="why" lang="ru">
Почему внедрённое исключение, а не рубильник? Потому что отмена обязана сочетаться с уборкой. Жёсткое убийство (фантазия о kill для потока) может прийтись между любыми двумя инструкциями — посреди коммита, с залоченным локом, с полузаписанным состоянием. Исключение, бросаемое только на await, гарантирует, что в каждой точке отмены ваши инварианты целы, бесплатно едет на существующей машинерии try/finally и async-with и делает уборку выразимой на языке, который вы уже знаете. Цена — кооперация: код, ловящий без re-raise или никогда не приостанавливающийся, выписывает себя из отменяемых — поэтому и существует дисциплина этого урока.
</Inset>

<Quiz
  id="02-cancellation-semantics-quiz-2"
  lessonSlug="02-cancellation-semantics"
  lang="ru"
  question="Задача на первой секунде 30-секундной чистопитоновой CPU-секции (внутри нет await). Сейчас вызывают task.cancel(). Когда задача реально остановится?"
  choices={[
    { label: "Только когда CPU-секция закончится и выполнение дойдёт до следующего приостанавливающего await — там цикл сможет бросить CancelledError в корутину", correct: true },
    { label: "Немедленно — cancel() прерывает работающую корутину как сигнал", misconception: "Вытеснения нет. cancel() лишь помечает задачу и планирует throw на следующей точке приостановки; пока код не приостановится, доставить нечего и некуда." },
    { label: "В пределах ~5 мс, на следующем переключении GIL", misconception: "Переключения GIL арбитрируют OS-потоки, а не корутины. Поток цикла продолжает выполнять этот колбэк; отмена едет на машинерии await, в которую эта секция не заходит." },
    { label: "Никогда — CPU-bound задачи в asyncio неотменяемы", misconception: "Она остановится на первом настоящем await после секции. А саму секцию можно сделать отменяемой, рассыпав await asyncio.sleep(0) — каждый из них приостановка, куда может приземлиться throw." },
  ]}
/>

</Explanation>

<SequenceDiagram
  label="Отмена как протокол: throw на ближайшем await, приостановка уборки в finally, вторая отмена внутри неё и re-raise, который причитается отменяющему"
  hue="var(--d-systems)"
  actors={["вызывающий", "event loop", "тело задачи"]}
  messages={[
    { from: "вызывающий", to: "event loop", label: "task.cancel()" },
    { from: "event loop", to: "тело задачи", label: "throw на await" },
    { from: "тело задачи", to: "event loop", label: "finally: await nack" },
    { from: "вызывающий", to: "event loop", label: "cancel() ещё раз" },
    { from: "event loop", to: "тело задачи", label: "бросок в finally" },
    { from: "тело задачи", to: "вызывающий", label: "CancelledError наружу" },
  ]}
  caption="cancel() — запрос через цикл, доставляемый только там, где корутина приостановлена. Уборка с await открывает дверь заново: вторая отмена приземляется внутри finally и бросает остаток — ограничивайте уборку таймаутом. Последняя стрелка — контракт: после уборки CancelledError распространяется наружу, и отменяющий со всеми скоупами выше видят честно отменённую задачу."
/>

<KeyTakeaway>Отмена — это `coro.throw(CancelledError)` на следующем *приостанавливающем* await: код между await атомарен по гарантии и неотменяем по той же причине (`await asyncio.sleep(0)` добавляет точки приземления). `except Exception` безопасен с 3.8; голый `except` / `except BaseException` глотают отмену и штампуют бессмертные задачи. Увидел — убрался — пере-бросил: единственная корректная форма обработчика; ограничивайте каждый await уборки, потому что вторая отмена приземляется внутри `finally`. TaskGroup и таймаут-скоупы различают, чья это отмена, через счётчик отмен — проглатывание ломает их протокол, а не стиль.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="02-cancellation-semantics-retrieval"
  lang="ru"
  questions={[
    { q: "Объясните механически, что делает task.cancel(), где исключение может и не может приземлиться, и двустороннее следствие того, что await — единственные точки доставки.", a: "cancel() планирует, что цикл возобновит корутину через throw(CancelledError) вместо send() в её следующей точке приостановки. Приземлиться оно может только на await, которые реально приостанавливают — pending-футуры, настоящие sleep; await завершённых футур или синхронно цепляющихся корутин — не точки отмены, а код между await прервать нельзя вообще. Сторона-гарантия: многошаговые смены состояния между await атомарны относительно отмены. Сторона-ловушка: длинный отрезок чистого CPU неотменяем, пока не дойдёт до настоящего await — честный фикс для секций, обязанных оставаться отзывчивыми, это периодический await asyncio.sleep(0): каждый — намеренно вставленная точка приземления ценой круга через цикл." },
    { q: "Сформулируйте дисциплину обработки исключений при отмене и цену каждого нарушения: except Exception, голый except, поимка без re-raise, неограниченные await в finally.", a: "except Exception безопасен — CancelledError с 3.8 наследует BaseException и пролетает насквозь (код до 3.8: небезопасен). Голый except или except BaseException его ловят; если обработчик продолжает цикл — задача бессмертна: gather на shutdown висит до SIGKILL, а работа в полёте после передоставки обрабатывается дважды. Ловить CancelledError законно только в форме «увидел — убрался — пере-бросил»; проглатывание делает await task нормально возвращающимся, task.cancelled() лжёт False, и портится бухгалтерия счётчика отмен, которой TaskGroup и asyncio.timeout различают внутреннюю отмену от внешней (uncancel — их машинерия, не ваша). await в finally — новые точки приостановки: вторая отмена приземляется там и бросает остаток уборки, а сама уборка может зависнуть на мёртвой зависимости — поэтому каждый await на пути отмены ограничивается asyncio.timeout (или шилдится по-настоящему обязательная работа — в паре с ограничителем)." },
  ]}
/>

<Recap lang="ru">
Отмена в asyncio — внедрённое исключение верхом на генераторной машинерии из урока 1: `cancel()` просит цикл возобновить цель через `throw(CancelledError)` на её следующем по-настоящему приостанавливающем await, что делает код между await атомарным — настоящая гарантия, на которую молча опираются ваши двухшаговые смены состояния, — и делает длинные CPU-секции неотменяемыми до ближайшей точки приземления, с периодическим `await asyncio.sleep(0)` как честным приёмом по цене латентности. Правила обработки исключений вытекают из одного факта: с 3.8 `CancelledError` — это `BaseException`, поэтому `except Exception` безопасен, а голый `except` и `except BaseException` глотают отмену — инцидент из Хука, где тридцать два воркера логировали «recovered» о собственном смертном приговоре, shutdown висел десять минут до SIGKILL, а неподтверждённые джобы после передоставки обработались дважды. Законная обработка — увидеть, убраться, пере-бросить: всё меньшее лжёт `await task`, лжёт `task.cancelled()` и ломает протокол счётчика отмен (`cancelling`/`uncancel`), которым TaskGroup отличает свою отмену из-за сбоя соседа от внешней, обязанной распространяться, — машинерию, которую вы редко вызываете, но на которую постоянно полагаетесь. Последняя ловушка — сама уборка: await внутри `finally` — свежая точка приостановки, куда приземляется вторая отмена, а неограниченный await уборки может повиснуть на той же мёртвой зависимости, что вызвала отмену, — ограничивайте его `asyncio.timeout`, ровно с этого места начинается следующий урок.
</Recap>
