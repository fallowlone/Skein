infographic hierarchy-tree-curved-line-rounded-rect-node
data
  root
    label container[Dog] — это container[Animal]?
    children
      - label Инвариантность — list[T] (изменяемый)
        children
          - label Нет: вызываемый может дописать Cat
      - label Ковариантность — Sequence[T] (только чтение)
        children
          - label Да: read-only, только отдаёт
      - label Контравариантность — Callable[[T], None]
        children
          - label Наоборот: обработчик Animal служит Dog
theme
  palette #f43f5e #10b981 #6366f1
