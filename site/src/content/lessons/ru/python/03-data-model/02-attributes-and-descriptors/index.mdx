---
concepts:
- python-descriptors
deepensInto: []
estMin: 19
lang: ru
lessonType: topic
level: senior
order: 2
prereqs: []
slug: "02-attributes-and-descriptors"
sources:
- https://docs.python.org/3/howto/descriptor.html
- https://docs.python.org/3/reference/datamodel.html
spiral: []
status: ready
summary: 'Поиск атрибута — не «сначала экземпляр»: data-дескрипторы типа сильнее __dict__ экземпляра, который сильнее non-data. __getattribute__ работает на каждом доступе, __getattr__ — только на промахе. property — data-дескриптор; __set_name__ сообщает полям ORM их имена.'
title: 'Поиск атрибутов и дескрипторы: почему словарь экземпляра не всегда побеждает'
track: "python"
unit: "03-data-model"
---

import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import StackDiagram from "~/components/diagram/StackDiagram.astro";
import Infographic from "~/components/diagram/Infographic.astro";
import descriptorKinds from "./infographics/descriptor-kinds.svg?raw";

<Hook>
Кеширующий слой выглядел безобидно: `__getattr__` на классе клиента лениво строил дорогие саб-клиенты при первом обращении. Год всё работало. Потом кто-то поправил опечатку внутри самого `__getattr__` — и метод стал трогать `self._registry` до того, как `_registry` присваивался в `__init__`. Симптомом была не AttributeError с указанием на опечатку. Это был `RecursionError: maximum recursion depth exceeded` с тысячекадровым трейсбеком, в продакшене, при создании объекта. Механика: `self._registry` промахнулся мимо словаря экземпляра, Python вызвал `__getattr__('_registry')`, тело которого снова тронуло `self._registry`, который снова промахнулся. Каждый инженер, дебажив это, в тот день выучил один и тот же урок из descriptor HOWTO: доступ к атрибуту в Python — это не «посмотреть в словарь». Это точный алгоритм с двумя хуками, срабатывающими в разные моменты, и протоколом дескрипторов, стоящим выше словаря экземпляра, — машинерия, на которой работают `property`, методы, `__slots__`, ORM и pydantic.
</Hook>

<Crux>Поиск атрибута: data-дескрипторы типа сильнее __dict__ экземпляра, который сильнее non-data. property — обычный data-дескриптор.</Crux>

<Explanation>

После этого урока ты будешь знать, почему `RecursionError` в `__init__` — это цепочка промахов по атрибуту, и как та же механика питает property, поля ORM и связанные методы.

## Настоящий порядок поиска: object.__getattribute__

Каждый `obj.x` вызывает `type(obj).__getattribute__(obj, "x")`, и дефолтная реализация выполняет алгоритм — порядок важен и является источником большинства загадок «почему мой атрибут игнорируется». Когда в следующий раз задашься вопросом, почему property нельзя затенить или почему запись в `self.__dict__['x']` ничего не меняет снаружи, — пройди эти пять шагов:

1. Пройти `type(obj).__mro__`, ища `"x"` в `__dict__` каждого класса.
2. Если найден и это **data-дескриптор** (определяет `__set__` или `__delete__`) — вызвать его `__get__`; **до словаря экземпляра дело не дойдёт**.
3. Иначе проверить `obj.__dict__` — здесь побеждает обычный атрибут экземпляра.
4. Иначе, если найденный на шаге 1 атрибут класса — **non-data-дескриптор** (только `__get__`; функции — ровно это), вызвать его `__get__`. Обычные атрибуты класса возвращаются как есть.
5. Нигде нет → `AttributeError`, который запускает `__getattr__`, если тот определён.

Так что «экземпляр затеняет класс» верно лишь для случаев без дескрипторов и без data. `property` определяет `__set__` (даже read-only — его `__set__` поднимает `AttributeError`) и потому является data-дескриптором: именно поэтому property нельзя случайно затенить через `self.x = 1` — само присваивание маршрутизируется в `property.__set__`. И именно поэтому функции, будучи non-data-дескрипторами, затенить *можно*: `obj.method = lambda: ...` работает. Связанные методы существуют потому, что шаг 4 вызывает `function.__get__(obj, cls)`, возвращающий объект bound method, — методы не особый синтаксис, а протокол дескрипторов, применённый к функциям.

<Infographic
  svg={descriptorKinds}
  label="Сравнение двух видов дескрипторов бок о бок: data-дескриптор определяет __set__ или __delete__, выигрывает раньше __dict__ экземпляра на шаге 2, не перекрывается на уровне экземпляра и является машинерией под property, валидируемыми полями и __slots__; non-data-дескриптор определяет только __get__, проигрывает __dict__ экземпляра на шаге 4, перекрывается на уровне экземпляра и так функции становятся связанными методами."
  caption="Один вопрос — определяет ли он __set__/__delete__ — решает, по какую сторону словаря экземпляра окажется дескриптор: data-дескрипторы выигрывают раньше него, non-data — проигрывают ему."
  hue="var(--d-infra)"
/>

## __getattr__ против __getattribute__

`__getattribute__` перехватывает **каждый** доступ к атрибуту — переопределите его, и вы платите его стоимость за любой `self.anything`, а любой `self.attr` внутри него рекурсирует, если не делегировать через `object.__getattribute__(self, name)`. `__getattr__` — дешёвый хук: вызывается **только после** того, как обычный поиск поднял `AttributeError`. Это делает его правильным выбором для ленивой загрузки и прокси — ноль накладных расходов на попаданиях — и это же породило рекурсию из Хука: промах атрибута *внутри* `__getattr__` снова входит в `__getattr__`. Защитный паттерн:

```python
class LazyClient:
    def __init__(self):
        self._cache = {}          # присвоено до того, как возможен промах

    def __getattr__(self, name):  # вызывается ТОЛЬКО на промахе поиска
        # защищаемся от промахов на внутренних именах,
        # чтобы не уйти в рекурсию
        if name.startswith("_"):
            raise AttributeError(name)
        client = expensive_build(name)
        self._cache[name] = client
        self.__dict__[name] = client   # следующий доступ: обычное попадание, __getattr__ пропущен
        return client
```

Запись результата в `self.__dict__` — трюк, который стоит украсть: второй доступ — попадание в словарь экземпляра на шаге 3, и `__getattr__` больше не срабатывает — лениво при первом касании, нативная скорость потом.

<Quiz
  id="02-attributes-and-descriptors-quiz-1"
  lessonSlug="02-attributes-and-descriptors"
  lang="ru"
  question="Класс определяет `x = property(get_x)`, а неосторожный __init__ делает `self.__dict__['x'] = 5` напрямую. Что вернёт `obj.x`?"
  choices={[
    { label: "То, что вернёт get_x — property является data-дескриптором, а data-дескрипторы типа проверяются раньше __dict__ экземпляра", correct: true },
    { label: "5 — атрибуты экземпляра всегда затеняют атрибуты класса", misconception: "Это правило работает лишь для обычных атрибутов класса и non-data-дескрипторов. property определяет __set__ (для read-only — поднимающий исключение), то есть это data-дескриптор: шаг 2 выигрывает до обращения к словарю экземпляра." },
    { label: "AttributeError — нельзя положить в __dict__ ключ, совпадающий с property", misconception: "Сама запись успешна; __dict__ — обычный словарь, и она обходит __set__. Запись просто лежит там, недостижимая обычным поиском, — именно это и делает баг запутанным." },
    { label: "5 при первом доступе, потом get_x, потому что property переустанавливает себя", misconception: "Порядок поиска детерминирован на каждом доступе и не хранит состояние: каждый obj.x проходит один и тот же алгоритм, и data-дескриптор побеждает каждый раз. Никто ничего не переустанавливает." },
  ]}
/>

## Пишем дескриптор: валидируемые поля и __set_name__

Дескриптор — любой объект, чей класс определяет `__get__`/`__set__`/`__delete__` и который лежит в атрибуте **класса** (атрибуты экземпляра дескрипторами не считаются никогда). Классическое продакшен-применение — переиспользуемое валидируемое поле: валидация пишется один раз и вешается на много атрибутов:

```python
class Positive:
    def __set_name__(self, owner, name):     # вызывается при создании класса
        self.slot = "_" + name               # узнаёт имя своего атрибута

    def __get__(self, obj, objtype=None):
        if obj is None:
            return self                      # доступ через класс: Account.balance
        return getattr(obj, self.slot)

    def __set__(self, obj, value):           # __set__ делает его DATA-дескриптором
        if value <= 0:
            raise ValueError(f"{self.slot[1:]} must be positive, got {value!r}")
        setattr(obj, self.slot, value)

class Account:
    balance = Positive()    # одна строка на валидируемое поле
    limit = Positive()

    def __init__(self, balance, limit):
        self.balance = balance   # проходит через Positive.__set__
        self.limit = limit
```

`__set_name__` (Python 3.6+) — тихий герой: дескриптор узнаёт, какому атрибуту его присвоили, и вы перестаёте передавать имя дважды (`balance = Positive("balance")`). Это ровно та машинерия, что лежит под полями Django ORM, колонками SQLAlchemy и моделями в стиле pydantic: объект-поле уровня класса перехватывает каждое чтение и запись, так что `user.email = "x"` может валидировать, помечать экземпляр «грязным» для UPDATE или приводить типы, — а `User.email` (заметьте `obj is None` в `__get__`) возвращает само поле, чтобы query-DSL строил выражения `User.email == "a@b.c"`. Компромисс честный: каждый доступ к атрибуту становится вызовом на уровне Python, примерно в 2–5 раз медленнее чтения из словаря экземпляра, — поэтому ORM «тормозит» в плотных циклах по миллиону строк, а массовые пути (`values_list`, `.model_dump`) обходят дескрипторы.

<Quiz
  id="02-attributes-and-descriptors-quiz-2"
  lessonSlug="02-attributes-and-descriptors"
  lang="ru"
  question="Ваш дескриптор валидируемого поля сохраняет значение через `self.value = value` внутри __set__ (на самом дескрипторе), а не на obj. Тесты с одним экземпляром зелёные. Что сломается в продакшене?"
  choices={[
    { label: "Все экземпляры класса делят одно значение — дескриптор это единственный объект уровня класса, и последняя запись побеждает для всех экземпляров", correct: true },
    { label: "Ничего; дескрипторы автоматически копируются на каждый экземпляр", misconception: "Дескрипторы никогда не копируются — существует ровно один объект-дескриптор в __dict__ класса, общий для всех экземпляров. Состояние per-instance должно жить на obj (или в словаре с ключом-экземпляром)." },
    { label: "RecursionError, потому что __set__ вызывает сам себя", misconception: "self внутри __set__ — это дескриптор, а не управляемый экземпляр: присваивание self.value попадает в собственный __dict__ дескриптора и не входит в __set__ повторно. Баг — общее состояние, а не рекурсия." },
    { label: "AttributeError при чтении, потому что __get__ ищет на obj", misconception: "Если __get__ тоже читает self.value, пара согласована и чтения успешны — возвращая данные другого экземпляра. Сбой тихий, с неверными значениями, что куда хуже исключения." },
  ]}
/>

</Explanation>

<StackDiagram
  label="Приоритет поиска атрибута для одного доступа obj.x, верхний слой побеждает: data-дескрипторы из MRO типа сильнее __dict__ экземпляра, который сильнее non-data-дескрипторов и обычных атрибутов класса; __getattr__ срабатывает только после AttributeError"
  hue="var(--d-systems)"
  layers={[
    { label: "Data-дескриптор на типе", note: "__set__/__delete__ — property" },
    { label: "__dict__ экземпляра", note: "обычный атрибут" },
    { label: "Non-data / атрибут класса", note: "только __get__ — функции" },
    { label: "Фолбэк __getattr__", note: "только на промахе" },
  ]}
  caption="Каждый obj.x запускает __getattribute__: он сканирует type(obj).__mro__ и идёт по этим слоям сверху вниз. Data-дескрипторы (property, валидируемые поля, слоты __slots__) отвечают раньше словаря экземпляра — потому property нельзя затенить; функции — non-data-дескрипторы: именно здесь рождаются связанные методы и работают переопределения на экземпляре."
/>

<KeyTakeaway>`obj.x` запускает `__getattribute__`: data-дескрипторы, найденные в MRO типа, сильнее `__dict__` экземпляра, который сильнее non-data-дескрипторов и обычных атрибутов класса; `__getattr__` срабатывает только на промахе. `property` — data-дескриптор (потому не затеняется), функции — non-data (потому есть связанные методы). Свой дескриптор плюс `__set_name__` даёт переиспользуемые валидируемые поля — ровно машинерия колонок ORM — ценой вызова уровня Python на каждом доступе.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="02-attributes-and-descriptors-retrieval"
  lang="ru"
  questions={[
    { q: "Воспроизведите порядок поиска атрибута для obj.x и объясните, где property, обычные атрибуты и методы выигрывают или проигрывают.", a: "Выполняется type(obj).__getattribute__: (1) скан __dict__ классов по type(obj).__mro__ в поисках x; (2) если найден data-дескриптор — определяет __set__ или __delete__ — вызывается его __get__ и поиск останавливается, словарь экземпляра не проверяется вовсе; (3) иначе obj.__dict__ — обычный атрибут экземпляра побеждает здесь; (4) иначе отвечает non-data-дескриптор (только __get__, т.е. функции) или обычный атрибут класса; (5) полный промах поднимает AttributeError, который запускает __getattr__, если он определён. property определяет __set__ даже в read-only-варианте, значит это data-дескриптор: его нельзя затенить состоянием экземпляра — даже self.__dict__['x']=5 остаётся недостижимым. Функции — non-data-дескрипторы: function.__get__ на шаге 4 возвращает связанный метод, и затенение методов per-instance работает." },
    { q: "Почему __getattr__ — правильный хук для ленивой загрузки, но источник рекурсии, и как дескрипторы с __set_name__ питают поля ORM?", a: "__getattr__ выполняется только после того, как обычный поиск поднял AttributeError — ноль накладных расходов на каждом попадании, в отличие от __getattribute__, перехватывающего весь доступ. Опасность: промах атрибута внутри __getattr__ снова входит в __getattr__, давая RecursionError вместо ясной ошибки; защищайте внутренние имена (поднимайте AttributeError для имён с подчёркиванием) и кешируйте результат в self.__dict__, чтобы последующие чтения были обычными попаданиями. Поля ORM — data-дескрипторы в атрибутах класса: __set_name__ сообщает полю его имя при создании класса, __set__ валидирует и помечает экземпляр грязным на каждой записи user.email = ..., а __get__ при obj is None возвращает само поле, чтобы User.email строил выражения запросов. Цена: каждый доступ — вызов уровня Python, ~2–5x от чтения словаря, поэтому массовые пути обходят дескрипторы." },
  ]}
/>

<Recap lang="ru">
Доступ к атрибуту — алгоритм, а не чтение словаря. `obj.x` вызывает `type(obj).__getattribute__`, который сначала сканирует MRO типа: найденный там data-дескриптор — всё, что определяет `__set__` или `__delete__`: `property`, валидируемые поля, слоты `__slots__` — отвечает раньше, чем вообще будет проверен `__dict__` экземпляра; поэтому property не затеняется, а контрабандная запись в `__dict__['x']` остаётся недостижимой. Лишь затем побеждает словарь экземпляра, а ниже него — non-data-дескрипторы: функции определяют только `__get__`, и именно на четвёртом шаге `function.__get__` изготавливает связанные методы — потому и работают переопределения методов per-instance. `__getattr__` — иной зверь, чем `__getattribute__`: второй перехватывает каждый доступ (переопределили — делегируйте через `object.__getattribute__`, иначе рекурсия), первый срабатывает только после промаха — идеален для ленивой загрузки, с классическим отказом, когда тело хука само промахивается по атрибуту, и трюком кеширования в `__dict__`, делающим последующие чтения нативно быстрыми. Свой дескриптор — это четыре метода: `__get__` (вернуть self при доступе через класс), `__set__` (валидация; само его наличие делает вас data-дескриптором), опциональный `__delete__` и `__set_name__`, чтобы поле узнало своё имя при создании класса. Это весь фокус под полями Django, колонками SQLAlchemy и моделями pydantic — с честной ценой вызова уровня Python на каждый доступ, примерно 2–5x от обычного чтения, которую массовые API сознательно обходят. Теперь, когда встретишь `RecursionError` из `__getattr__` или property, упрямо игнорирующий значение, только что записанное в `self.__dict__`, — ты знаешь, какой слой алгоритма отвечает и что именно нужно поправить.
</Recap>
