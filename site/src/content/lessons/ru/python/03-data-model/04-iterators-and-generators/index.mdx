---
concepts:
- python-iterators
deepensInto: []
estMin: 19
lang: ru
lessonType: topic
level: senior
order: 4
prereqs: []
slug: "04-iterators-and-generators"
sources:
- https://docs.python.org/3/reference/datamodel.html
- https://docs.python.org/3/library/itertools.html
spiral: []
status: ready
summary: 'for зовёт __iter__, затем __next__ до StopIteration. Генератор — приостановленный кадр: им управляют send/throw/close, yield from делегирует. Ленивые конвейеры тянут гигабайты в константной памяти, но спящий кадр держит ресурсы — вплоть до открытого файла на часы.'
title: 'Итераторы и генераторы: спящие кадры, ленивые конвейеры и файл, который так и не закрылся'
track: "python"
unit: "03-data-model"
---

import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";
import SequenceDiagram from "~/components/diagram/SequenceDiagram.astro";
import Infographic from "~/components/diagram/Infographic.astro";
import lazyVsEager from "./infographics/lazy-vs-eager.svg?raw";

<Hook>
Экспортёр стримил ежедневные CSV-снапшоты в S3 через элегантный генераторный конвейер: `read_rows(path)` открывал файл и отдавал распарсенные строки, нижние стадии фильтровали и собирали батчи. Потом платформенная команда ограничила открытые файловые дескрипторы числом 1024, и сервис начал умирать с `OSError: Too many open files` — но только в дни с множеством *маленьких* тенантов. Охота на утечку не нашла никого, кто забыл бы вызвать close. Виновником оказался поток управления: для тенантов без подходящих строк нижняя стадия выходила раньше после первой же попытки собрать батч, бросая конвейер. Генератор `read_rows` был приостановлен на `yield` внутри `with open(path)` — блок `with` так и не завершился, потому что кадр больше не возобновлялся. Спящий кадр генератора — живой объект, держащий всё в своей области видимости: файловый хендл, распарсенную строку, буферы. Сборщик мусора со временем вызвал бы `close()`, но CPython финализирует лишь при обнулении ссылок, а словарь-реестр держал каждый конвейер живым. Тысяча брошенных генераторов, тысяча открытых дескрипторов, один пейдж в четыре утра.
</Hook>

<Crux>Генератор — замороженный кадр стека: next() будит его, StopIteration завершает. Ленивые конвейеры экономят память, но кадр держит ресурсы.</Crux>

<Explanation>

## Протокол итератора: во что на самом деле компилируется for

`for x in xs:` разворачивается так: вызвать `iter(xs)` — то есть `xs.__iter__()` — и получить *итератор*, затем звать `it.__next__()`, пока тот не поднимет `StopIteration`, которое цикл проглатывает как нормальное завершение. Две роли, которые часто путают: **итерируемое** имеет `__iter__` и обходится много раз (списки, словари, ваши классы-коллекции); **итератор** имеет `__next__` *и* `__iter__`, возвращающий самого себя, несёт состояние позиции и исчерпывается ровно один раз. Классический баг, который объясняет это различие: передайте итератор (а не список) двум потребителям — второй не увидит ничего, итерация его съела. Вторая тонкость с зубами: `StopIteration` — это управление потоком, поэтому случайный `StopIteration`, поднятый *внутри* тела генератора, молча завершал бы итерацию вместо падения; PEP 479 (дефолт с 3.7) превращает его в `RuntimeError` — увидели такую ошибку, значит кто-то вызвал `next()` без дефолта внутри генератора.

## Генераторы: спящие кадры с API

Функция с `yield` при вызове не выполняется — она возвращает объект-генератор, оборачивающий *приостановленный кадр стека*: локальные переменные, указатель инструкции, всё. `next()` возобновляет кадр до следующего `yield`, который снова его усыпляет, отдав значение вызывающему. `return` (или конец тела) поднимает `StopIteration` с приложенным возвращаемым значением. Живучесть кадра — и есть вся фича (конечные автоматы без классов), и вся проблема Хука: всё, на что ссылается кадр, живёт, пока он спит.

Помимо `next()`, полный управляющий API: `gen.send(value)` возобновляет кадр, и приостановленное выражение `yield` вычисляется в `value` (на этом построены корутинные протоколы — с этого начиналась машинерия `asyncio`); `gen.throw(exc)` поднимает исключение *в точке спящего yield*, давая генератору поймать его для очистки или восстановления; `gen.close()` бросает туда `GeneratorExit`, ожидая выхода, — блок `finally` или `with` вокруг yield выполняется именно в этот момент. В этом и фикс для Хука: потребитель, бросающий конвейер, обязан вызвать `close()` (или держать генератор в `with contextlib.closing(...)`) — кадр возобновится в последний раз, и `with open(...)` сможет завершиться. `yield from sub` делегирует целый под-генератор: значения, `send`, `throw` проходят насквозь прозрачно, а возвращаемое значение под-генератора становится значением выражения — ручной `for x in sub: yield x` теряет проброс send/throw и возвращаемое значение.

```python
import csv
from collections.abc import Iterator

def read_rows(path: str) -> Iterator[dict]:
    with open(path, newline="") as f:          # открыт, пока кадр спит!
        yield from csv.DictReader(f)           # делегирование: полный проброс протокола

def large_orders(rows: Iterator[dict], min_total: float) -> Iterator[dict]:
    return (r for r in rows if float(r["total"]) >= min_total)

pipeline = large_orders(read_rows("orders.csv"), 100.0)
try:
    for row in pipeline:
        process(row)
finally:
    pipeline.close()    # будит спящий кадр, чтобы `with open` завершился
```

<SequenceDiagram
  label="Потребитель ведёт генератор: каждый next() возобновляет спящий кадр до следующего yield, а close() бросает GeneratorExit в точке паузы, чтобы блоки finally и with наконец выполнились"
  hue="var(--d-systems)"
  actors={["Потребитель", "Генератор"]}
  messages={[
    { from: "Потребитель", to: "Генератор", label: "next()" },
    { from: "Генератор", to: "Потребитель", label: "yield row, пауза" },
    { from: "Потребитель", to: "Генератор", label: "next()" },
    { from: "Генератор", to: "Потребитель", label: "yield row, пауза" },
    { from: "Потребитель", to: "Генератор", label: "close()" },
    { from: "Генератор", to: "Потребитель", label: "GeneratorExit: finally" },
  ]}
  caption="Между вызовами кадр не двигается — потребитель, переставший тянуть, оставляет его спящим с открытым файлом. close() будит кадр в последний раз с GeneratorExit, чтобы with open(...) завершился; send(v) и throw(exc) идут по тому же каналу."
/>

<Quiz
  id="04-iterators-and-generators-quiz-1"
  lessonSlug="04-iterators-and-generators"
  lang="ru"
  question="Генератор открывает файл в with-блоке и отдаёт строки изнутри него. Потребитель читает 10 строк, выходит раньше и сохраняет ссылку на генератор в словаре. Когда закроется файл?"
  choices={[
    { label: "Не раньше, чем генератор закроют или соберёт GC — кадр спит внутри with-блока, а ссылка из словаря мешает сборке, так что хендл остаётся открытым неограниченно", correct: true },
    { label: "Когда вернётся функция-потребитель — Python закрывает ресурсы, открытые вызываемыми", misconception: "В Python нет понятия владения ресурсами вызываемого. With-блок живёт в собственном кадре генератора, и этот кадр всего лишь приостановлен — возврат потребителя его не возобновляет и не разматывает." },
    { label: "Сразу после десятого yield — with-блоки освобождают ресурсы при каждой приостановке", misconception: "Приостановка — не выход. With-блок завершается, лишь когда поток управления его покидает: исчерпание, исключение или GeneratorExit из close(). Спящий yield держит блок — и файл — полностью живыми." },
    { label: "Он закрылся после первого yield; генераторы переоткрывают файл при каждом возобновлении", misconception: "Кадр сохраняет все локальные переменные между приостановками — включая открытый файловый объект. Между yield ничего не разрушается и не пересоздаётся; эта живучесть и делает генераторы статфул и текучими." },
  ]}
/>

## Конвейеры и itertools: словарь ленивой обработки

Сцепленные генераторы дают стриминг с константной памятью: каждая стадия тянет по одному элементу, и CSV на 20 ГБ протекает через фильтрацию, трансформацию и батчинг в несколько килобайт рабочего набора — альтернатива, материализация списков между стадиями, стоит полного датасета на каждую стадию. Числа при этом честные: возобновление генератора — переключение кадра на уровне Python, так что накладные расходы на элемент примерно 100–200 нс против ~30 нс шага итерации списка на уровне C; для горячих циклов на миллион элементов, целиком влезающих в память, списковое включение часто быстрее. Ленивость покупает память и время до первого элемента, а не сырую пропускную способность.

<Infographic
  svg={lazyVsEager}
  label="Сравнение бок о бок ленивого генераторного конвейера и энергичной материализации списков для одного CSV на 20 ГБ: генератор работает в константной памяти при примерно 100-200 нс на элемент с быстрым временем до первого элемента, но держит ресурсы, пока спит, тогда как списковое включение материализует полный датасет на стадию при примерно 30 нс на элемент с более высокой сырой пропускной способностью и без спящего кадра для утечки."
  caption="Ленивость выигрывает память и время до первого элемента; энергичные списки выигрывают сырую пропускную способность — ~30 нс против ~100–200 нс на элемент. Подбирайте инструмент под то, влезают ли данные в память."
  hue="var(--d-infra)"
/>

`itertools` — стандартный словарь, чтобы перестать писать статфул-циклы вручную: `islice` (пагинация по любому итератору без срезов), `chain` (склейка потоков), `groupby` (группировка серий — **требует отсортированного входа**, самый частый баг с itertools), `tee` (разветвить итератор на n — но буферизует всё, что не прочитал самый медленный потребитель, так что два потребителя на разных скоростях молча пересобирают список в памяти), `batched` (3.12+, та самая стадия батчинга, которую десять лет писали руками), плюс `count`/`cycle`/`takewhile`/`dropwhile`. Они складываются в конвейеры, читающиеся как поток данных, который реализуют, — и все написаны на C, так что конвейер из стадий itertools почти не платит поэлементную цену Python-кадров.

<Quiz
  id="04-iterators-and-generators-quiz-2"
  lessonSlug="04-iterators-and-generators"
  lang="ru"
  question="Стадия конвейера делает `for x in sub(): yield x` вместо `yield from sub()`. Для простой итерации функционально одинаково — что на самом деле потерял рефакторинг?"
  choices={[
    { label: "Проброс send() и throw() в под-генератор и его возвращаемое значение (в которое вычисляется yield from) — простая итерация пробрасывает только отданные значения", correct: true },
    { label: "Ничего; yield from — чистый синтаксический сахар над циклом", misconception: "Для простого потока значений они совпадают, но yield from — полный канал делегирования: send, throw и close маршрутизируются в самый внутренний спящий кадр, а значение StopIteration всплывает как результат выражения. Цикл не пробрасывает ничего из этого." },
    { label: "Ленивость — версия с циклом сперва материализует sub() в список", misconception: "Цикл for по генератору остаётся ленивым: один элемент за итерацию. Потеря — в пробросе протокола, а не в стратегии вычисления; обе версии стримят элементы одинаково." },
    { label: "Безопасность исключений — исключения внутри sub() версия с циклом проглатывает", misconception: "Исключения, поднятые внутри sub(), нормально распространяются сквозь цикл. Внутрь не распространяется throw(), вызванный на внешнем генераторе: он попадает во внешний yield и до кадра sub() не доходит." },
  ]}
/>

</Explanation>

<FlowDiagram
  label="Жизненный цикл генератора: создан приостановленным, возобновляется next/send, спит на yield, завершается return или закрывается GeneratorExit"
  hue="var(--d-systems)"
  perRow={3}
  nodes={[
    { id: "created", label: "Создан", sub: "кадр построен, не запущен" },
    { id: "running", label: "Выполняется", sub: "исполняет тело" },
    { id: "paused", label: "Спит на yield", sub: "локалы + хендлы живы" },
    { id: "done", label: "Исчерпан", sub: "StopIteration(value)" },
    { id: "closed", label: "Закрыт", sub: "GeneratorExit → finally" },
    { id: "leak", label: "Брошен", sub: "спит вечно, держит FD" },
  ]}
  edges={[
    { from: "created", to: "running", label: "next()/send()" },
    { from: "running", to: "paused", label: "yield" },
    { from: "paused", to: "running", label: "next()/send()" },
    { from: "running", to: "done", label: "return" },
    { from: "paused", to: "closed", label: "close()" },
    { from: "paused", to: "leak", label: "потребитель ушёл" },
  ]}
  caption="Опасное ребро — последнее: потребитель, переставший тянуть, оставляет кадр спящим, и всё в его области видимости — файловые хендлы, сокеты, буферы — живёт до close() или сборки мусора. Лекарство — явный close() в finally."
/>

<KeyTakeaway>`for` — это `iter()`, затем `__next__()` до `StopIteration`; итерируемые перезапускаются, итераторы исчерпываются один раз. Генератор — приостановленный кадр: `send` кормит спящий yield, `throw` поднимает исключение в нём, `close` вбрасывает `GeneratorExit`, чтобы выполнились `finally`/`with`, а `yield from` делегирует весь протокол плюс возвращаемое значение. Конвейеры стримят гигабайты в константной памяти по ~100–200 нс на элемент; режим отказа — брошенный спящий кадр с файловым хендлом: закрывайте генераторы, которые не исчерпали.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="04-iterators-and-generators-retrieval"
  lang="ru"
  questions={[
    { q: "Объясните, как брошенный генератор течёт файловым хендлом, почему GC не спасает быстро, и двусторонний фикс.", a: "Генератор, делающий yield внутри `with open(path)`, держит файл открытым, пока спит: with-блок завершается, только когда поток управления его покидает, а приостановленный кадр запаркован посреди блока и держит все локалы — хендл, буферы, текущую строку. Если потребитель прекращает итерацию раньше, кадр никто не возобновляет; если любая ссылка (словарь-реестр, замыкание) держит генератор живым, подсчёт ссылок CPython никогда не запускает финализацию, и дескриптор остаётся открытым неограниченно — тысяча брошенных конвейеров это тысяча FD и OSError на ulimit. Фикс: потребители закрывают генераторы, которые могут не исчерпать — pipeline.close() в finally или contextlib.closing, — что бросает GeneratorExit в спящий yield и выполняет with/finally; а производители держат область ресурса узкой: открыть внутри, yield from, чтобы close() разматывался чисто." },
    { q: "Сравните next/send/throw/close и точно сформулируйте, что yield from добавляет к ручному циклу for-yield.", a: "next(gen) возобновляет кадр до следующего yield. gen.send(v) делает то же, но спящее выражение yield вычисляется в v — основа корутинных протоколов. gen.throw(exc) поднимает exc в точке спящего yield, и генератор может поймать его, очиститься или подставить значение. gen.close() бросает туда GeneratorExit и ждёт завершения; finally и with вокруг yield выполняются в этот момент. yield from sub делегирует весь протокол: отданные значения текут наружу, send и throw маршрутизируются в самый внутренний спящий кадр, а не останавливаются на внешнем, close распространяется, и возвращаемое значение под-генератора (едущее на StopIteration) становится значением выражения yield from. Ручной `for x in sub: yield x` пробрасывает только отданные значения — send попадает во внешний yield, throw не доходит до кадра sub, возвращаемое значение теряется." },
  ]}
/>

<Recap lang="ru">
Итерация — это два дандера: `for` вызывает `iter()` ради итератора, затем `__next__()` до `StopIteration` — итерируемые (с `__iter__`) перезапускаются на каждом цикле, итераторы (с `__next__` и `__iter__`, возвращающим себя) несут позицию и исчерпываются один раз; вот почему общий итератор у двух потребителей оставляет второго голодным. PEP 479 превратил случайный `StopIteration` внутри тела генератора в `RuntimeError`, закрыв ловушку тихого усечения. Генераторная функция возвращает приостановленный кадр: `next` ведёт до следующего `yield`, `send` заставляет этот yield вычислиться в значение, `throw` поднимает в нём исключение, `close` вбрасывает `GeneratorExit`, чтобы `finally` и `with` наконец выполнились, — а `yield from` делегирует всё это: значения, send, throw, close, плюс возвращаемое значение под-генератора, которое рукописный цикл for-yield теряет. Конвейеры генераторов обрабатывают сколь угодно большие потоки в константной памяти с отличным временем до первого элемента, по честным ~100–200 нс на возобновление уровня Python против ~30 нс C-шагов списка — `itertools` (`islice`, `chain`, `groupby` с отсортированным входом, прожорливый на буферы `tee`, `batched` из 3.12) даёт стадии C-скорости и общий словарь. Продакшен-отказ — спящий кадр как держатель ресурса: потребитель, бросивший конвейер на полпути, оставляет `with open(...)` незавершённым, дескриптор живым, а на пейджере — тихого кузена OOM-киллера, исчерпание FD. Закрывайте то, что не исчерпали.
</Recap>
