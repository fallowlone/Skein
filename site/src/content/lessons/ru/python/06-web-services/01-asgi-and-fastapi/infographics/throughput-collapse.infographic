infographic chart-column-simple
data
  title Один воркер uvicorn: куда уходит throughput (req/s)
  desc Эхо льстит фреймворку; запрос к БД 20 мс делает потолком пул; один блокирующий вызов в async def держит воркер на ~5
  values
    - label JSON-эхо · 3–9k
      value 6000
    - label + запрос к БД 20 мс
      value 500
    - label async def + блок 200 мс
      value 5
theme
  palette #14b8a6 #f59e0b #f43f5e
