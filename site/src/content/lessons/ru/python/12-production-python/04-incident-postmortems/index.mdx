---
concepts:
- python-incidents
deepensInto: []
estMin: 20
lang: ru
lessonType: topic
level: senior
order: 4
prereqs: []
slug: "04-incident-postmortems"
sources:
- https://docs.python.org/3/library/tracemalloc.html
- https://docs.python.org/3/library/functools.html
- https://github.com/benfred/py-spy
- https://kubernetes.io/docs/tasks/configure-pod-container/configure-liveness-readiness-startup-probes/
spiral: []
status: ready
summary: 'Три инцидента: безразмерный lru_cache течёт 40 МБ/ч до OOMKill (находит diff tracemalloc); синхронный requests.get морозит весь цикл событий (py-spy видит socket.recv); двухминутная загрузка модели плюс liveness равно CrashLoopBackOff. Blameless-постмортемы чинят системы.'
title: 'Постмортемы инцидентов: утечка, зависание, лавина рестартов — и ремесло blameless-разбора'
track: "python"
unit: "12-production-python"
---

import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import SequenceDiagram from "~/components/diagram/SequenceDiagram.astro";
import Infographic from "~/components/diagram/Infographic.astro";
import incidentSignatures from "./infographics/incident-signatures.svg?raw";

<Hook>
Шесть недель никто не разбирался, почему поды прайсинга перезапускаются примерно каждые тридцать часов. Рестарты были «бесплатными» — Kubernetes заменял под, трафик перебалансировался, пейдж не срабатывал. Потом рестарт попал на пик трафика Чёрной пятницы: замена стартовала холодной, с пустыми кешами, p99 ушёл к четырём секундам, и конверсия чекаута падала одиннадцать минут. График в постмортеме был идеальной лестницей: RSS растёт на 40 МБ в час, линейно, тридцать часов, затем OOMKill, и снова. Каждая ступень этой лестницы была видна на дашборде, о котором никого не пейджили. Этот урок препарирует три продакшен-инцидента от начала до конца — утечку (безразмерный `lru_cache` с ключами из параметров запроса), зависание (один синхронный `requests.get`, замораживающий целый цикл событий) и лавину рестартов (двухминутная загрузка модели против тридцатисекундной liveness-пробы) — каждый через одну рамку: обнаружение, ущерб, корневая причина, смягчение, предотвращение. А затем само ремесло: постмортем, заканчивающийся фразой «инженер будет внимательнее», не диагностировал ничего. Отказ допустила система; меняйте систему.
</Hook>

<Crux>Три инцидента: безразмерный lru_cache течёт, sync-вызов морозит цикл, liveness убивает долгий старт. Чините систему, а не человека.</Crux>

<Explanation>

## Утечка: RSS растёт 40 МБ/ч, OOMKill каждые ~30 часов

**Обнаружение** пришло поздно и не с того сигнала: с графика числа рестартов, а не с алерта по памяти. Паттерн, называющий класс бага: RSS растёт *линейно с трафиком*, без плато, до лимита контейнера. Линейный-с-трафиком рост означает накопление живых ссылок на каждый запрос — фрагментация выходит на плато, мусор собирается; вечно растёт только то, что *достижимо*.

**Корневая причина**: `@lru_cache(maxsize=None)` на функции прайсинга, в чьи аргументы входил кортеж фильтров запроса. Почти уникальные ключи на запрос — кеш почти никогда не попадал, только рос. Воркер живёт неделями; ничто не вытесняет; `gc` бессилен, потому что каждая запись сильно ссылается *по дизайну*. Инструмент доказательства — `tracemalloc`: снапшоты в двух точках времени, diff по строкам:

```python
import tracemalloc
tracemalloc.start(25)                  # хранить 25 кадров на аллокацию

snap1 = tracemalloc.take_snapshot()    # час 1 — триггер через админ-эндпоинт
# ... проходят часы ...
snap2 = tracemalloc.take_snapshot()    # час 5
for stat in snap2.compare_to(snap1, "lineno")[:5]:
    print(stat)
# pricing/cache.py:41: size=2.9 GiB (+2.7 GiB), count=38M (+36M)
#   словарь lru_cache — ключ (user_id, query, tuple(filters))
```

**Смягчение против предотвращения**: рециклинг `max_requests` останавливает OOMKill-ы уже сегодня — честно подписанный костыль. Предотвращение структурно: ограниченные кеши (`maxsize=` число, которое вы можете защитить, или TTL-кеш там, где допустима несвежесть), алерт RSS-на-воркер, срабатывающий на *тренд* (40 МБ/ч в течение 3 часов), а не на обрыв, и правило ревью: `maxsize=None` плюс ключи формы запроса не проходит никогда.

<Quiz
  id="04-incident-postmortems-quiz-1"
  lessonSlug="04-incident-postmortems"
  lang="ru"
  question="RSS линейно растёт ~40 МБ/ч, под получает OOMKill каждые ~30 ч. Какая улика указывает на безразмерный lru_cache и почему gc вас не спасает?"
  choices={[
    { label: "Diff снапшотов tracemalloc между двумя часами приписывает рост точке вызова кеша; gc бессилен, потому что каждая запись сильно ссылается словарём кеша — живая по дизайну, а не мусор", correct: true },
    { label: "gc.collect() по таймеру вернул бы память и подтвердил утечку", misconception: "Сборщик освобождает недостижимые объекты и циклы. Записи кеша достижимы через сам кеш — сборка ничего не меняет, и это само по себе диагностическая улика." },
    { label: "Линейный рост RSS указывает на фрагментацию кучи, которую tracemalloc всё равно не видит", misconception: "Фрагментация выходит на плато и не отслеживает уникальность запросов. Рост, пропорциональный трафику и без потолка, означает накопление живых ссылок — ровно то, что tracemalloc приписывает строке." },
    { label: "То, что рециклинг max_requests убирает симптом, доказывает: утечка в самом gunicorn", misconception: "Рециклинг сбрасывает процесс и маскирует ЛЮБУЮ утечку независимо от происхождения — это смягчение, несущее ноль информации о причине." },
  ]}
/>

## Зависание: все async-воркеры заморожены, CPU около нуля

**Обнаружение**: p99 уходит вертикально, каждый запрос таймаутится, и контринтуитивный сигнал — CPU около *нуля*. Крутящийся цикл жёг бы CPU; замороженный — припаркован в сисколле. Инструмент, превращающий это из спекуляции в улику, — `py-spy dump`: он подключается к живому pid без рестарта и без инструментирования:

```text
$ py-spy dump --pid 1
Thread 0x7F4A (active): "MainThread"
    recv (ssl.py:1233)
    ...
    get (requests/api.py:73)
    fetch_rates (helpers/fx.py:12)      # тремя слоями хелперов ниже
    convert (handlers/checkout.py:88)   # async def — но блокируется прямо здесь
```

**Корневая причина**: синхронный `requests.get` вкрался в async-обработчик за тремя слоями хелперов — автор `fetch_rates` ни разу не видел `async def` в своём файле. Цикл кооперативен и однопоточен: блокирующий сисколл никогда не уступает планировщику, поэтому *каждая* корутина этого воркера — сотни в полёте — ждёт вместе с ним (правило мостов python/11, теперь в виде аварии). Каждый воркер, задевший этот путь, замерзает так же; флот умирает воркер за воркером по мере того, как трафик находит маршрут.

**Предотвращение**: lint/import-правило, запрещающее синхронные HTTP-клиенты (`requests`, голый `urllib3`) в async-модулях — закреплённое в CI, а не в памяти ревьюеров; запасной выход `await asyncio.to_thread(...)` там, где синхронная библиотека неизбежна; async-нативный клиент (`httpx`) как дефолт; и нагрузочный тест, дёргающий медленный upstream, потому что на p50 этот баг невидим.

## Лавина рестартов: здоровый бинарь, полный отказ

**Цепочка причины**: релиз добавил двухминутную загрузку ML-модели на старте. Liveness-проба — `initialDelaySeconds: 30` — начинала падать до конца загрузки; Kubernetes убивал под посреди загрузки; замена начинала ту же двухминутную загрузку; снова убита. **CrashLoopBackOff (состояние Kubernetes, когда под перезапускается снова и снова с экспоненциальной выдержкой) по всему флоту — от бинаря без единого бага.** У обнаружения сигнатура, которую стоит запомнить: циклы рестартов с *чистыми логами* — ни трейсбека, ни ошибки: процесс убили, он не падал, — плюс события отказов проб в `kubectl describe`.

**Предотвращение**: **startup-проба** — liveness приостановлена, пока она не пройдёт; она существует ровно для медленно стартующих приложений; ленивая или фоновая загрузка модели с выключенным readiness до прогрева (трафик ждёт, никто не умирает); и бюджет времени старта, проверяемый в CI — тест, валящий сборку, когда старт переходит N секунд, потому что регрессии времени загрузки приходят тихо, по одной зависимости за раз.

<Infographic
  svg={incidentSignatures}
  label="Дерево, сопоставляющее три продакшен-инцидента их диагностическим сигнатурам: утечка (RSS растёт на 40 МБ/ч линейно, OOMKill каждые ~30 ч) находится через diff снапшотов tracemalloc и чинится ограниченным кэшем плюс алертом по тренду RSS; зависание (p99 вертикально, CPU около нуля) находится через py-spy dump по живому pid и чинится запретом sync HTTP в async через CI-линт; рестарт (CrashLoopBackOff, чистые логи) находится через события проб в kubectl describe и чинится startup-пробой."
  caption="У каждого класса отказа своя сигнатура, указывающая на свой инструмент и свой фикс — узнал сигнатуру, и полдиагноза готово."
  hue="var(--d-infra)"
/>

## Ремесло: как пишется постмортем

У трёх инцидентов общий скелет, и скелет — это и есть ремесло. **Таймлайн с таймстемпами** — когда началось, когда обнаружили, когда смягчили; разрыв обнаружения (шесть недель у утечки) — сам по себе вывод. **Blameless** — не вежливость, а качество данных: инженер, переключивший флаг, расскажет, как всё было, только если документ нельзя использовать против него. **Пять «почему», нацеленных на систему**: не «почему инженер добавил lru_cache», а «почему наше ревью, наши алерты и наши лимиты все вместе позволили неограниченному росту уехать в прод и работать шесть недель». **Action items — это изменения кода или конфига** с владельцами и сроками: заведённый алерт, добавленная проба, влитое lint-правило. «Быть внимательнее» не встречается ни в одном компетентном постмортеме; если человеческая оплошность вызывает отказ — система уже была сломана.

<Quiz
  id="04-incident-postmortems-quiz-2"
  lessonSlug="04-incident-postmortems"
  lang="ru"
  question="Все async-воркеры перестают отвечать; CPU около нуля. py-spy dump показывает поток цикла внутри socket.recv под requests.get. Почему один вызов заморозил сотни запросов в полёте?"
  choices={[
    { label: "Цикл событий кооперативен и однопоточен: синхронный сисколл не возвращает управление планировщику, и каждая корутина этого воркера ждёт, пока recv не вернётся — выход: to_thread или async-клиент", correct: true },
    { label: "GIL удерживается во время сетевого ожидания и блокирует остальные потоки", misconception: "CPython отпускает GIL вокруг блокирующего I/O — другие потоки могли бы работать. Цикл блокирует не GIL: его единственный поток застрял в одном кадре, а корутины — не потоки." },
    { label: "Слишком мало воркеров; больше воркеров поглотили бы затык", misconception: "Каждый воркер, исполняющий этот путь, замерзает одинаково — масштабирование умножает замороженную ёмкость. Флот умирает воркер за воркером, пока трафик находит маршрут." },
    { label: "CPU около нуля значит, что процесс здоров, а настоящая проблема — медленный upstream", misconception: "Медленный upstream за await позволил бы остальным корутинам обслуживать дальше. Ноль CPU при полной неотзывчивости означает, что сам цикл припаркован в сисколле — дымящийся пистолет в дампе." },
  ]}
/>

</Explanation>

<SequenceDiagram
  label="Лавина рестартов: двухминутная загрузка модели против 30-секундной liveness-пробы"
  hue="var(--d-systems)"
  actors={["k8s", "pod", "веса модели"]}
  messages={[
    { from: "k8s", to: "pod", label: "старт контейнера" },
    { from: "pod", to: "веса модели", label: "загрузка (120 с)" },
    { from: "k8s", to: "pod", label: "liveness на 30 с" },
    { from: "pod", to: "k8s", label: "нет ответа" },
    { from: "k8s", to: "pod", label: "kill и рестарт" },
    { from: "k8s", to: "pod", label: "CrashLoopBackOff" },
  ]}
  caption="Liveness без startup-пробы превращает медленный старт в цикл убийств: под прибит посреди загрузки, замена повторяет загрузку, и здоровый бинарь даёт полный отказ. Startup-пробы приостанавливают liveness до завершения старта."
/>

<KeyTakeaway>Три сигнатуры, которые стоит выучить: линейный рост RSS с трафиком — это накопление живых ссылок; diff снапшотов `tracemalloc` называет строку, а `gc` бессилен, потому что записи кеша достижимы по дизайну. Замороженные async-воркеры при нулевом CPU — заблокированный цикл событий; `py-spy dump` на живом pid показывает синхронный вызов в стеке. Циклы рестартов с чистыми логами — оркестратор убивает медленный старт; ровно для этого существуют startup-пробы. Постмортем: точный таймлайн, blameless-рамка, пять «почему» к системе и action items в виде кода или конфига — никогда «быть внимательнее».</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="04-incident-postmortems-retrieval"
  lang="ru"
  questions={[
    { q: "Проведите инцидент с утечкой через полную рамку: сигнал обнаружения, механизм корневой причины, процесс работы с tracemalloc и разделение смягчение/предотвращение.", a: "Обнаружение: график числа рестартов и лестница RSS — линейный рост с трафиком, без плато, OOMKill каждые ~30 ч; улика — линейность, потому что фрагментация выходит на плато, а мусор собирается; без потолка растут только живые ссылки. Корневая причина: lru_cache(maxsize=None) с почти уникальными ключами из параметров запроса — почти нет попаданий, монотонный рост, и gc не помогает: каждая запись сильно ссылается словарём кеша. Доказательство: tracemalloc.start(25), снапшоты в час 1 и час 5, compare_to(snap1, 'lineno') — верхняя строка статистики называет файл и строку кеша с атрибуцией плюс-гигабайты. Смягчение: рециклинг max_requests, честно подписанный костыль, маскирующий симптом. Предотвращение: ограниченный maxsize или TTL-кеши, трендовый алерт RSS-на-воркер (срабатывает на устойчивые 40 МБ/ч, а не на обрыве) и правило ревью против безразмерных кешей на ключах формы запроса." },
    { q: "Сопоставьте зависание и лавину рестартов: сигнатуры обнаружения, корневые причины и пакет предотвращения для каждого.", a: "Зависание: p99 вертикально, все запросы таймаутятся, CPU около нуля — замороженный цикл паркуется в сисколле, а не жжёт CPU. py-spy dump на живом pid показывает поток цикла внутри socket.recv под requests.get, тремя хелперами ниже async-обработчика. Один блокирующий вызов морозит каждую корутину воркера, потому что цикл кооперативен и однопоточен. Предотвращение: закреплённый в CI запрет импорта синхронных HTTP-клиентов в async-модули, asyncio.to_thread для неизбежных синхронных библиотек, httpx как дефолт, нагрузочные тесты по медленному upstream. Лавина рестартов: CrashLoopBackOff с ЧИСТЫМИ логами — убит, а не упал — плюс события отказов liveness. Двухминутная загрузка модели превысила окно liveness, и k8s убивал поды посреди старта по кругу: отказ от здорового бинаря. Предотвращение: startup-проба, приостанавливающая liveness; ленивая/фоновая загрузка с readiness-гейтом трафика; и CI-бюджет времени старта, чтобы регрессии валили сборку." },
  ]}
/>

<Recap lang="ru">
Три инцидента — одна аналитическая рамка. Утечка: `lru_cache(maxsize=None)` с ключами из параметров запроса превратил долгоживущий воркер в односторонний клапан памяти — 40 МБ в час, линейно с трафиком, OOMKill каждые тридцать часов, шесть недель невидимости, потому что рестарты казались бесплатными. Линейная лестница — сигнатура класса: накопление живых ссылок, где `gc` бессилен по дизайну; diff снапшотов `tracemalloc` превращает подозрение в файл и строку. Зависание: один синхронный `requests.get` тремя слоями хелперов ниже `async def` припарковал целый цикл событий в `socket.recv` — сотни замороженных корутин на воркер, ноль CPU, и `py-spy dump` на живом pid как инструмент улик без рестарта; предотвращение — lint-правило в CI, выходы через `to_thread` и async-нативный клиент, потому что память ревьюеров не масштабируется. Лавина рестартов: двухминутная загрузка модели против тридцатисекундной liveness-пробы дала CrashLoopBackOff от бинаря без багов — сигнатура: чистые логи плюс события проб; фикс: startup-пробы и ленивая загрузка за readiness-гейтом; страховка от регрессий: бюджет старта в CI. Ремесло связывает всё: точные таймлайны, где разрыв обнаружения — сам по себе вывод; blameless-рамка, потому что страх портит данные; пять «почему», направленных на систему, допустившую отказ; и action items, которые компилируются, — алерт, проба, lint-правило — и никогда обещание быть внимательнее. Теперь, когда вы увидите лестницу RSS, замёрзшие воркеры при нулевом CPU или под, перезапускающийся с чистыми логами, — вы будете знать, какой инструмент взять и какой вопрос задать системе, а не человеку.
</Recap>
