infographic hierarchy-tree-curved-line-rounded-rect-node
data
  root
    label Три инцидента, три сигнатуры
    children
      - label Утечка — OOMKill каждые ~30 ч
        children
          - label Сигнал: RSS растёт на 40 МБ/ч, линейно с трафиком
          - label Инструмент: diff снапшотов tracemalloc укажет строку
          - label Фикс: ограниченный кэш + алерт по тренду RSS
      - label Зависание — async-воркеры заморожены
        children
          - label Сигнал: p99 вертикально, CPU около нуля
          - label Инструмент: py-spy dump по живому pid
          - label Фикс: запрет sync HTTP в async (CI-линт)
      - label Рестарт — CrashLoopBackOff
        children
          - label Сигнал: цикл рестартов, чистые логи
          - label Инструмент: kubectl describe — события проб
          - label Фикс: startup-проба приостанавливает liveness
theme
  palette #f43f5e #f59e0b #6366f1
