---
concepts:
- b-tree-index
- sequential-scan
- index-scan
- write-amplification
- create-index-concurrently
deepensInto: []
estMin: 12
lang: ru
lessonType: topic
level: junior
order: 1
prereqs:
- databases/01-relational-model/01-what-a-relation-is
slug: 01-index-anatomy
sources:
- https://www.postgresql.org/docs/current/indexes.html
- https://www.postgresql.org/docs/current/indexes-types.html
- https://use-the-index-luke.com/
spiral:
- latency
status: ready
summary: Индекс — это отсортированная копия значений столбца, которая позволяет Postgres
  пропускать полное сканирование таблицы. Каждый индекс обменивает скорость записи
  и место на диске на скорость чтения.
title: Что такое индекс и как он ускоряет запросы
track: databases
unit: 02-indexes
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import MetaphorComplete from "~/components/pedagogy/MetaphorComplete.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";
import Infographic from "~/components/diagram/Infographic.astro";
import indexTradeoff from "./infographics/index-tradeoff.svg?raw";

<Hook>
Дашборд команды «фильтр по status» выполняется за 50 мс на 100k строк в staging. В продакшне с 50M строк — 8 секунд. Схема корректна. Запрос корректен. Одна DDL-строка — `CREATE INDEX CONCURRENTLY ON orders(status)` — возвращает 20 мс. Эта строка — весь смысл урока.
</Hook>

К концу урока ты будешь точно знать, почему эта строка работает, сколько она стоит и когда добавление индекса делает ситуацию хуже, а не лучше.

<Crux>Индекс — это отсортированная копия значений столбца для одного вопроса. Каждый индекс облагает каждую запись налогом. Выбирай осознанно.</Crux>

<Explanation>

## Что такое индекс

Индекс — это отдельная структура данных (обычно B-tree), которая хранит отсортированную копию значений одного или нескольких столбцов плюс указатели на фактические строки. Когда запрос фильтрует по индексированному столбцу, Postgres проходит дерево за O(log n) шагов вместо сканирования каждой строки.

Метафора книжного индекса точна: предметный указатель книги — это отсортированный список терминов с номерами страниц. Чтобы найти «MVCC», открываешь указатель (один поиск O(log n)), читаешь номер страницы и переходишь туда. Без указателя — сканируешь каждую страницу. Индекс базы данных идентичен: копия значений столбца, организованная для быстрого поиска по одному конкретному вопросу.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Без индекса</th>
        <th class="border border-border px-3 py-2 text-left">С индексом</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">Sequential scan — каждая строка просматривается</td>
        <td class="border border-border px-3 py-2">B-tree walk — O(log n), затем fetch matching rows</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">1M строк: ~10мс; 100M строк: ~2–10с</td>
        <td class="border border-border px-3 py-2">1M или 100M строк: ~1–5мс</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Нет доп. хранилища, нет overhead на запись</td>
        <td class="border border-border px-3 py-2">~10–30% размера таблицы на индекс; каждая запись поддерживает индекс</td>
      </tr>
    </tbody>
  </table>
</div>

## Цена: индексы облагают налогом каждую запись

Почему нельзя проиндексировать каждый столбец и забыть о проблеме? Потому что каждый индекс — это постоянный налог на каждую будущую запись. Вопрос не «поможет ли индекс этому запросу?», а «оправдает ли ускорение чтения замедление записи именно на этой таблице?»

Каждый индекс — это отдельная структура на диске, которая должна оставаться синхронизированной с таблицей. Каждый `INSERT`, `UPDATE` или `DELETE`, затрагивающий индексированный столбец, также должен обновить индекс — что стоит CPU и I/O. Таблица с десятью неиспользуемыми индексами при write-heavy нагрузке тратит на запись в 10 раз больше, чем необходимо.

Типичный overhead на запись одного индекса: 5–50 мкс на операцию записи. При 10 000 inserts/сек с десятью индексами это дополнительные 0,5–5 мс overhead в секунду — измеримо, и часто узкое место при избыточной индексации.

Размер индекса на диске: примерно `bytes_per_key × row_count × 1.3`. Для BIGINT-ключа (8 байт) и таблицы из 100M строк получается ~1 GB индекс. Каждый дополнительный индекс умножает это.

## Конкретный сценарий

Запрос: `SELECT * FROM orders WHERE user_id = 42` на таблице с 10M строк. Без индекса: sequential scan, ~2 секунды. После `CREATE INDEX ON orders(user_id)`: index lookup плюс heap fetch, ~3 миллисекунды. Тот же запрос, ускорение в 600 раз, одна DDL-строка.

<Infographic
  svg={indexTradeoff}
  label="Сравнение одного и того же запроса по 10M строк с индексом и без: без индекса — ~2с, но ноль накладных на запись и хранилище; с одним B-tree индексом — ~3мс (ускорение в 600 раз), но +5-50 микросекунд на каждую запись и 10-30% размера таблицы на диске."
  caption="Индекс покупает ускорение чтения в 600 раз, но ты платишь за это на каждой записи и на диске — в этом весь компромисс урока."
  hue="var(--d-infra)"
/>

Postgres поставляется с шестью типами индексов. B-tree — дефолт и покрывает 95% продакшн-использования: равенство, диапазон, `ORDER BY`, `LIKE 'abc%'`. Остальные пять (GIN, GiST, BRIN, Hash, Bloom) служат для специфических форм данных и рассматриваются в уроке 04.

## Продакшн-правило: всегда использовать CONCURRENTLY

`CREATE INDEX` берёт блокировку `ACCESS EXCLUSIVE` — все чтения и записи заблокированы на всё время построения. На таблице с активным трафиком даже 5-минутная блокировка — это инцидент.

`CREATE INDEX CONCURRENTLY` использует многофазное построение без эксклюзивной блокировки. Он сканирует таблицу дважды и ждёт in-flight транзакций между фазами. Он в 2–3 раза медленнее и не может быть обёрнут в транзакцию, но не создаёт видимого воздействия на трафик приложения.

Если построение CONCURRENTLY завершается неудачей (например, уникальный индекс находит дубликат или процесс убивается), индекс остаётся в состоянии `INVALID` (`indisvalid = false` в `pg_indexes`). Планировщик рассматривает его как несуществующий. Исправление: `DROP INDEX CONCURRENTLY` и повторная попытка.

<Inset kind="why" lang="ru">
Почему Postgres не индексирует каждый столбец по умолчанию? Потому что каждый индекс на write-heavy таблице — это налог на запись. Таблица с 20 индексами и 50k inserts/сек может тратить больше времени на поддержку индексов, чем на запись строк. Дисциплина намеренная: измеряй горячие запросы, добавляй индексы для них, проверяй и удаляй неиспользуемые. Избыточная и недостаточная индексация — одинаково разрушительные продакшн-паттерны.
</Inset>

<DragOrder
  id="01-index-anatomy-drag-1"
  lessonSlug="01-index-anatomy"
  lang="ru"
  prompt="Упорядочи шаги для правильного добавления индекса в продакшне:"
  items={[
    { id: "a", label: "Запусти EXPLAIN ANALYZE на медленном запросе, чтобы подтвердить seq scan как узкое место" },
    { id: "b", label: "Определи столбцы, по которым происходит фильтрация или сортировка" },
    { id: "c", label: "Выбери тип индекса (B-tree по умолчанию; GIN для JSONB; GiST для гео)" },
    { id: "d", label: "Используй CREATE INDEX CONCURRENTLY, чтобы не блокировать таблицу" },
    { id: "e", label: "Дождись завершения построения (может занять минуты или часы на больших таблицах)" },
    { id: "f", label: "Снова запусти EXPLAIN ANALYZE, чтобы убедиться, что планировщик теперь использует индекс" },
    { id: "g", label: "Следи за задержкой записи после деплоя — каждый индекс добавляет небольшую стоимость записи" },
  ]}
  correctOrder={["a", "b", "c", "d", "e", "f", "g"]}
/>

<Quiz
  id="01-index-anatomy-quiz-1"
  lessonSlug="01-index-anatomy"
  lang="ru"
  question="Создан индекс на столбец users(email). Какой запрос выигрывает от него?"
  choices={[
    { label: "SELECT * FROM users WHERE email = 'alice@x.com'", correct: true },
    { label: "SELECT * FROM users WHERE display_name = 'Alice'", misconception: "Индекс на email, не на display_name." },
    { label: "INSERT INTO users (...) VALUES (...)", misconception: "Индексы не ускоряют insert; они замедляют его (индекс должен обновляться)." },
    { label: "SELECT COUNT(*) FROM users", misconception: "COUNT(*) требует все строки; одностолбцовый индекс не помогает с этим в Postgres." },
  ]}
/>

<Quiz
  id="01-index-anatomy-quiz-2"
  lessonSlug="01-index-anatomy"
  lang="ru"
  question="В чём цена добавления индекса?"
  choices={[
    { label: "Нет цены — индексы бесплатны", misconception: "Каждый индекс стоит места на диске и замедляет каждую запись по индексированному столбцу." },
    { label: "Место на диске плюс небольшой overhead на каждый INSERT/UPDATE/DELETE, затрагивающий индексированные столбцы", correct: true },
    { label: "Только единовременная стоимость создания", misconception: "Стоимость создания важна, но долгосрочная стоимость — это overhead на каждую запись." },
    { label: "Пропускная способность сети между клиентом и сервером", misconception: "Индексы на стороне сервера; они не влияют на сеть." },
  ]}
/>

<MetaphorComplete
  id="01-index-anatomy-metaphor-1"
  lessonSlug="01-index-anatomy"
  lang="ru"
  setup="Заполни пропуск: индекс базы данных для SQL-запроса — то же, что _______ книги для поиска темы: копия части содержимого, организованная для быстрого поиска."
  accepted={["указатель", "предметный указатель", "index"]}
  canonical="указатель"
  explanation="Предметный указатель книги — ближайшая повседневная аналогия: алфавитный список терминов с номерами страниц, небольшая отдельная структура, которая делает основное содержимое быстро доступным для поиска по одному измерению. Индексы базы данных обобщают эту идею на произвольные типы столбцов и условия."
/>

</Explanation>

<FlowDiagram
  label="Путь индексного сканирования: фильтр по индексированному столбцу проходит B-tree от корня к листу за O(log n), затем достаёт подходящую строку из heap."
  perRow={5}
  hue="var(--d-data)"
  nodes={[
    { id: "query", label: "Запрос", sub: "WHERE user_id = 42" },
    { id: "root", label: "Корень B-tree", sub: "проход O(log n)" },
    { id: "leaf", label: "Лист", sub: "сорт. ключи + TID" },
    { id: "heap", label: "Чтение heap", sub: "по TID" },
    { id: "row", label: "Строка" },
  ]}
  edges={[
    { from: "query", to: "root" },
    { from: "root", to: "leaf" },
    { from: "leaf", to: "heap" },
    { from: "heap", to: "row" },
  ]}
  caption="Вместо обхода каждой строки (последовательное сканирование) индекс идёт по дереву сверху вниз к нужному листу, затем следует по указателю и достаёт только подходящие строки."
/>

<KeyTakeaway>Индекс — это отсортированная копия значений столбца, которая превращает полное сканирование таблицы в O(log n) поиск. Каждый индекс добавляет ~10–30% overhead по хранилищу и 5–50 мкс на каждую запись. Всегда используй CREATE INDEX CONCURRENTLY в продакшне; проверяй pg_indexes на состояние INVALID после этого.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="01-index-anatomy-retrieval"
  lang="ru"
  questions={[
    { q: "В двух предложениях: почему индексы не бесплатны и почему продакшн-команды не индексируют каждый столбец?", a: "Каждый индекс — это отдельная отсортированная структура данных на диске, которая должна поддерживаться синхронизированной с таблицей: каждый INSERT, UPDATE или DELETE, затрагивающий индексированный столбец, обновляет индекс тоже, что стоит CPU и I/O. Таблица с десятью неиспользуемыми индексами при write-heavy нагрузке тратит на запись в 10 раз больше, чем необходимо, и индексы занимают место (часто 10–30% размера таблицы каждый) и засоряют выбор планировщика." },
    { q: "Что происходит, если CREATE INDEX CONCURRENTLY падает посередине, и как восстановиться?", a: "Индекс остаётся в состоянии INVALID — indisvalid = false в pg_indexes. Планировщик рассматривает его как несуществующий, поэтому запросы не используют его, но индекс всё равно занимает место. Восстановление: DROP INDEX CONCURRENTLY на неверном индексе, затем повторить CREATE INDEX CONCURRENTLY." },
    { q: "Запрос делает sequential scan на таблице из 50M строк. Ты добавляешь индекс. Что показывает EXPLAIN ANALYZE до и после?", a: "До: Seq Scan по таблице, оценка и обход всех 50M строк. После (если индекс достаточно селективен): Index Scan или Index Only Scan, обращение только к соответствующим leaf-записям в B-tree и fetch малого числа heap-строк. Оценка 'rows' и фактическое время резко падают — с секунд до миллисекунд для селективного фильтра." },
  ]}
/>

<Recap lang="ru">
Индекс — это отдельная отсортированная структура данных (как правило B-tree), которая хранит копии значений одного или нескольких столбцов с указателями на соответствующие heap-строки. Он превращает полное табличное sequential scan (O(n)) в проход по дереву (O(log n)), сокращая 2-секундный запрос на 10M строк до 3мс. Цена: каждый индекс стоит 10–30% размера таблицы в хранилище и 5–50 мкс на запись на каждый индекс. Всегда добавляй индексы с `CREATE INDEX CONCURRENTLY`, чтобы не блокировать таблицу в продакшне. Если построение падает, проверь `pg_indexes` на `indisvalid = false` и повтори после удаления неверной записи.

Теперь, когда ты встретишь медленный запрос на большой таблице, первый рефлекс — `EXPLAIN ANALYZE`. Видишь Seq Scan — знаешь, что нужно, что это стоит, и принимаешь решение осознанно.
</Recap>
