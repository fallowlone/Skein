infographic chart-column-simple
data
  title Чтения кучи для одного плотного запроса на 1000 строк
  desc Тот же индекс, те же строки — стоимость определяет Visibility Map
  values
    - label Index scan (чтение кучи на каждую строку)
      value 1000
    - label Index-only scan, устаревшая VM (много Heap Fetches)
      value 1000
    - label Index-only scan, свежая VM (Heap Fetches: 0)
      value 10
theme
  palette #f43f5e #f59e0b #14b8a6
