---
concepts:
- leading-column-rule
- partial-index
- index-only-scan
- visibility-map
- index-types
- fk-index
estMin: 14
lang: ru
level: senior
order: 8
prereqs: []
slug: quiz-short
sources:
- https://www.postgresql.org/docs/current/indexes-multicolumn.html
- https://www.postgresql.org/docs/current/indexes-index-only-scans.html
- https://www.postgresql.org/docs/current/indexes-examine.html
status: ready
summary: "Промпты на свободное припоминание по всему юниту об индексах — сначала ответь своими словами, потом открой модельный ответ и сравни."
title: 'Индексы: тест на припоминание'
track: databases
unit: 02-indexes
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";

<Hook>
Припоминание сильнее перечитывания. На каждый промпт скажи или запиши полный ответ по памяти, прежде чем открыть модельный — именно усилие припоминания закрепляет leading-column rule и Visibility Map.
</Hook>

<Goal lang="ru">
Восстанови ключевые механизмы юнита — leading-column rule, когда partial бьёт full, что запускает index-only scan, как выбрать тип индекса и самые частые production-сбои — не подглядывая в уроки.
</Goal>

<RetrievalDrawer
  client:visible
  id="02-indexes-short-retrieval"
  lang="ru"
  questions={[
    { q: "Почему composite index (a, b, c) помогает WHERE a = ? и WHERE a = ? AND b = ?, но не WHERE b = ? — и какое единственное правило дизайна из этого следует?", a: "Multi-column B-tree отсортировано прежде всего по a, затем по b внутри равных a, затем по c внутри равных (a, b). Движок навигирует по дереву, используя этот порядок сортировки, поэтому сузить он может только когда фильтр даёт ведущий префикс — a, затем (a, b), затем (a, b, c). Фильтр только по b не даёт точки старта в глобальном порядке, поэтому индекс не даёт входа; планировщик откатывается к seq scan (или полному index scan, обычно хуже). Правило дизайна (leading-column rule): делай ведущей колонкой ту, что всегда присутствует в фильтре — обычно колонку tenant/scope в multi-tenant приложении — затем добавляй вторичные колонки в порядке селективности, а ORDER BY-колонки ставь в конец, чтобы индекс также обслуживал сортировку без отдельного узла Sort." },
    { q: "Когда partial index выигрывает у полного индекса и что должен удовлетворять запрос, чтобы его использовать?", a: "Partial index (CREATE INDEX ... WHERE предикат) индексирует только строки, удовлетворяющие предикату. Он выигрывает, когда горячие запросы всегда нацелены на меньшинство строк — например таблица tasks, где 80% done, но запрашиваются только open/in_progress, или паттерн soft-delete (WHERE deleted_at IS NULL). Индекс пропорционален совпадающей доле: предикат на 20% совпадений даёт индекс ~20% от размера полного, с пропорционально меньшим write-overhead — а записи в несовпадающие строки (done) его вообще не трогают. Чтобы его использовать, WHERE запроса должен логически влечь предикат индекса: WHERE workspace_id = $1 AND status = 'pending' может использовать partial-индекс WHERE status = 'pending'; WHERE workspace_id = $1 в одиночку — нет, потому что ему нужны и не-pending строки. Бонус: partial UNIQUE index (UNIQUE (email) WHERE deleted_at IS NULL) обеспечивает уникальность только среди живых строк, позволяя soft-delete-and-re-register." },
    { q: "Какие два условия должны выполняться одновременно, чтобы index-only scan избежал heap fetches, и как держать их истинными в production?", a: "(1) Покрытие: каждая колонка, на которую ссылается запрос — в WHERE, ORDER BY и SELECT — должна быть в индексе, либо как ключевая, либо как INCLUDE-колонка. INCLUDE (Postgres 11+) хранит проекционные колонки в leaf-страницах, не делая их частью ключа сортировки, так что ты покрываешь проекцию, не нарушая порядок сортировки. (2) Видимость: бит Visibility Map для heap-страницы каждой совпадающей строки должен быть установлен (все tuple на странице видимы всем транзакциям). В индексе нет xmin/xmax, поэтому сброшенный бит VM заставляет Postgres читать heap-страницу для проверки видимости — это и есть Heap Fetches в EXPLAIN ANALYZE. VACUUM ставит биты VM; любая запись их сбрасывает. Дисциплина production: на IOS-зависимых таблицах снижай autovacuum_vacuum_scale_factor (например до 0.05), мониторь pg_stat_user_tables.n_dead_tup и убивай долгие транзакции, блокирующие VACUUM от уборки tuples." },
    { q: "Назови, когда ты выйдешь за B-tree ради GIN, GiST, BRIN и pgvector HNSW, и какую цену несёт каждый.", a: "B-tree покрывает ~95% случаев: равенство, диапазон, сортировку и prefix LIKE по упорядоченным типам. GIN: много значений на строку — JSONB containment (@>), пересечение массивов, full-text tsvector (@@) — построен как инвертированный индекс; цена 2-5× размера колонки и высокий write-overhead (одно обновление posting-list на ключ/лексему на insert), смягчается через fastupdate или сужение до expression-индексов по горячим полям. GiST: данные без полного порядка — геометрия PostGIS, range-типы, nearest-neighbour (ORDER BY geom <-> point); цена — более сложные, зависящие от extension обновления. BRIN: огромные, физически упорядоченные, append-mostly таблицы (time-series по created_at) — хранит min/max по диапазону блоков, так что индекс — несколько МБ даже на терабайтах; бесполезен, если данные не упорядочены физически, потому что каждый диапазон блоков охватывает весь диапазон значений. pgvector HNSW: семантическая близость по embedding; большой (vector_dim × строки, десятки ГБ), медленные записи (rebalancing графа), приблизительный recall (95-99%), и нужен пайплайн инференса модели." },
    { q: "Почему foreign-key колонки — повторяющаяся production-ловушка и какое правило?", a: "Когда ты объявляешь REFERENCES posts(id), Postgres автоматически индексирует ссылаемую колонку (PK родителя), но никогда ссылающуюся колонку на ребёнке (comments(post_id)). Поэтому ON DELETE CASCADE — или любой join/фильтр ребёнка по его FK — делает sequential scan всей дочерней таблицы, чтобы найти совпадающие строки. Классический инцидент: DELETE FROM posts WHERE id = 42 занимает минуты и держит блокировки, пока сканирует 100M+ comments. Правило: индексируй каждую FK-колонку (CREATE INDEX CONCURRENTLY на comments(post_id)), если не можешь доказать, что родитель никогда не удаляется/не обновляется по ссылаемой колонке и ни один запрос не фильтрует и не джойнит ребёнка по этому FK." },
    { q: "Перечисли семь production-сбоев индексов и единственный диагностический приём, вскрывающий каждый.", a: "(1) Отсутствующий FK-индекс — медленный cascade/join; EXPLAIN на DELETE или join показывает Seq Scan по ребёнку. (2) Неявное приведение типа (WHERE bigint_col = '42') — EXPLAIN показывает cast в фильтре и Seq Scan; чинится типизированными параметрами. (3) Устаревшая статистика — EXPLAIN ANALYZE показывает дико разошедшиеся estimated и actual строки; чинится ANALYZE / extended statistics. (4) Раздувание индекса (bloat) — размер индекса в несколько раз больше ожидаемого для числа строк; подтверждает pgstattuple_approx; чинится REINDEX CONCURRENTLY. (5) Неправильный порядок composite — запрос фильтрует не-ведущую колонку и делает seq-scan; проверь ведущую колонку против фильтра. (6) JSONB GIN cardinality bomb — GIN в 10-50× больше колонки, всплеск insert-латентности, падение cache-hit ratio; сузь до expression-индексов по горячим полям. (7) 'Индекс есть, но всё равно медленно' — EXPLAIN (ANALYZE, BUFFERS) показывает узел Sort после index scan или высокие Heap Fetches; чинится covering composite, обслуживающим фильтр + сортировку + проекцию. Универсальный инструмент — EXPLAIN (ANALYZE, BUFFERS) на точном запросе с production-реалистичными параметрами." },
  ]}
/>

<Recap lang="ru">
Если ты смог восстановить каждый ответ по памяти, ты держишь хребет юнита: composite используется только от ведущего префикса, поэтому проектируй его вокруг всегда присутствующего фильтра и заверши ORDER BY-колонками; partial-индексы сжимаются пропорционально горячему подмножеству и пропускают записи холодных строк; index-only scan нужны и полное покрытие (ключ + INCLUDE), и свежая Visibility Map (VACUUM); тип индекса должен подходить форме данных; FK-колонки никогда не индексируются автоматически; а семь сбоев всплывают под одним диагностическим приёмом — EXPLAIN (ANALYZE, BUFFERS) на реальном запросе.
</Recap>
