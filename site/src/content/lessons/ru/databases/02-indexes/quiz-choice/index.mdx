---
concepts:
- leading-column-rule
- partial-index
- covering-index
- index-only-scan
- index-types
- production-failures
estMin: 13
lang: ru
level: senior
order: 7
prereqs: []
slug: quiz-choice
sources:
- https://www.postgresql.org/docs/current/indexes.html
- https://www.postgresql.org/docs/current/indexes-index-only-scans.html
- https://use-the-index-luke.com/
status: ready
summary: "Тест с выбором на синтез по всему юниту об индексах — leading-column rule, partial и covering индексы, index-only scan и Visibility Map, выбор типа индекса и production-сбои."
title: 'Индексы: тест с выбором ответа'
track: databases
unit: 02-indexes
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Шесть вопросов поперёк всего юнита. Ни один не про заучивание определений — каждый отражает решение по проектированию или инцидент, который ты взвешиваешь под реальной нагрузкой запросов, где неправильный индекс стоит места на диске, write-пропускной способности или ночного пейджа.
</Hook>

<Goal lang="ru">
Убедись, что связываешь анатомию B-tree, leading-column rule, partial и covering индексы, index-only scan, выбор типа индекса и production-сбои — тот синтез, к которому вели семь уроков.
</Goal>

<Quiz
  id="02-indexes-choice-1"
  lessonSlug="02-indexes"
  lang="ru"
  question="У multi-tenant дашборда есть индекс (workspace_id, status, created_at). Новый аналитический запрос делает WHERE status = 'pending' ORDER BY created_at DESC без фильтра по workspace_id и получает sequential scan. Как чинить, не нарушая дисциплину дизайна из юнита?"
  choices={[
    { label: "Добавить второй индекс, у которого ведущая колонка — всегда присутствующий фильтр этого запроса, например (status, created_at DESC). Существующий индекс не помогает, потому что запрос пропускает его ведущую колонку workspace_id; composite используется только от ведущего префикса.", correct: true },
    { label: "Переставить существующий индекс на (status, workspace_id, created_at), чтобы status был первым.", misconception: "Это сломает все существующие tenant-scoped запросы, фильтрующие по workspace_id первым. Composite проектируют под доминирующий запрос, а не перетасовывают один индекс под новый паттерн доступа — добавь второй индекс." },
    { label: "Запустить ANALYZE; у планировщика просто устаревшая статистика.", misconception: "Устаревшая статистика даёт неверные оценки строк, а не структурную невозможность использовать индекс. Ни один префикс (workspace_id, status, created_at) не начинается со status, поэтому индекс не даёт точки входа независимо от статистики." },
    { label: "Индекс уже покрывает status, так что форсируй его планировщику через hint.", misconception: "В Postgres нет index hints, и индекс не покрывает фильтр только по status — status это вторая ключевая колонка, недостижимая без ведущей workspace_id. Leading-column rule структурно обязателен, это не предпочтение планировщика." },
  ]}
/>

<Quiz
  id="02-indexes-choice-2"
  lessonSlug="02-indexes"
  lang="ru"
  question="В таблице tasks на 100M строк 80% имеют status = 'done' (дашборд их никогда не читает). Дашборд читает только open и in_progress. Какая стратегия индекса минимизирует и размер, и write-overhead?"
  choices={[
    { label: "Partial index WHERE status IN ('open','in_progress') по колонкам фильтра и сортировки дашборда. Он индексирует ~20% строк, поэтому в несколько раз меньше, а insert/update строк done его вообще не трогают.", correct: true },
    { label: "Полный composite (status, created_at) — он покрывает и фильтр, и сортировку.", misconception: "Полный индекс покрывает все 100M строк, включая 80M done-строк, которые дашборд не читает — он примерно в 5× больше, чем нужно, и каждая запись в done-строку всё равно его поддерживает. Partial-предикат убирает этот расход." },
    { label: "Без индекса — совпадает только 20% строк, планировщик всё равно сделает seq-scan.", misconception: "20% от 100M это 20M строк, но дашборд фильтрует дальше (по workspace, по project) до крошечного селективного среза. Partial index по горячему подмножеству — именно то, что нужно планировщику." },
    { label: "BRIN-индекс по status — он крошечный и игнорирует leading-column rule.", misconception: "BRIN хранит min/max по диапазону блоков и помогает только когда данные физически упорядочены по индексируемой колонке. status низкокардинальна и не упорядочена на диске; каждый диапазон блоков охватывает все статусы, так что BRIN не даёт отсечения." },
  ]}
/>

<Quiz
  id="02-indexes-choice-3"
  lessonSlug="02-indexes"
  lang="ru"
  question="EXPLAIN (ANALYZE, BUFFERS) показывает Index Only Scan с Heap Fetches: 12000 на таблице orders, и запрос медленный, несмотря на правильный covering index. Что происходит и какой устойчивый фикс?"
  choices={[
    { label: "Visibility Map устарела — для этих 12000 TID бит all-visible сброшен, поэтому Postgres вынужден читать heap-страницу для проверки видимости tuple. Фикс: запустить VACUUM, затем снизить autovacuum_vacuum_scale_factor (например до 0.05) и убить долгие транзакции, блокирующие VACUUM.", correct: true },
    { label: "В индексе нет части проецируемых колонок, поэтому он откатывается к чтению heap.", misconception: "Если бы колонок не хватало, узел плана был бы Index Scan, а не Index Only Scan. Index-only scan с heap fetches — это проблема Visibility Map, а не покрытия." },
    { label: "Индекс INVALID после неудачного CREATE INDEX CONCURRENTLY.", misconception: "INVALID-индекс невидим планировщику — план показал бы Seq Scan, а не Index Only Scan. Тип скана доказывает, что индекс валиден и выбран." },
    { label: "Слишком много строк результата — index-only scan деградирует выше порога по строкам.", misconception: "Число строк не определяет heap fetches; их определяет бит Visibility Map на страницу. Append-mostly таблица, возвращающая миллионы строк, всё равно может показывать Heap Fetches: 0, если VACUUM держит VM свежей." },
  ]}
/>

<Quiz
  id="02-indexes-choice-4"
  lessonSlug="02-indexes"
  lang="ru"
  question="DELETE FROM posts WHERE id = 42 с ON DELETE CASCADE занимает 7 минут и всё это время держит блокировки, на схеме posts/comments. В чём корневая причина?"
  choices={[
    { label: "comments(post_id) — ссылающаяся FK-колонка — не проиндексирована. Postgres автоматически индексирует ссылаемый PK posts(id), но никогда FK-колонку ребёнка, поэтому каскад делает sequential scan по всем comments, чтобы найти строки на удаление.", correct: true },
    { label: "posts(id) нужен индекс, чтобы каскад был быстрым.", misconception: "posts(id) — это primary key и он уже проиндексирован. Каскад сканирует дочернюю таблицу comments по post_id; именно непроиндексированная ссылающаяся колонка, а не ссылаемая, форсирует O(n)-скан." },
    { label: "CASCADE по своей природе O(n) и ускорить его нельзя.", misconception: "Индекс на comments(post_id) превращает поиск каскада в index scan. Медлительность — это отсутствующий индекс, а не внутреннее свойство каскадов." },
    { label: "MVCC dead tuples — таблице нужен VACUUM.", misconception: "VACUUM убирает dead tuples и помогает видимости, но не даёт каскаду способа находить дочерние строки по post_id. Скан чинит только индекс на comments(post_id)." },
  ]}
/>

<Quiz
  id="02-indexes-choice-5"
  lessonSlug="02-indexes"
  lang="ru"
  question="orders.user_id это BIGINT с индексом orders(user_id). Запрос делает SELECT * FROM orders WHERE user_id = '42' (литерал TEXT) и получает Seq Scan. Почему и какой фикс?"
  choices={[
    { label: "Неявное приведение типа: чтобы сравнить BIGINT с TEXT, Postgres приводит колонку на каждой строке, поэтому B-tree (отсортированное по сырому BIGINT) нельзя обойти. Фикс: передать параметр правильного типа, например bigint-приведение или bigint-литерал.", correct: true },
    { label: "Индекс раздут (bloat), и планировщик от него отказался.", misconception: "Bloat замедляет индекс, но не переключает план на Seq Scan для селективного равенства. Причина здесь — приведение типа, отключающее использование индекса; EXPLAIN показывает coercion в фильтре." },
    { label: "B-tree индексы не умеют отвечать на равенство по числовым колонкам.", misconception: "B-tree прекрасно обрабатывает равенство BIGINT. Проблема в TEXT-литерале, форсирующем приведение колонки на каждой строке, а не в типе индекса." },
    { label: "Добавить expression index на user_id, приведённый к text.", misconception: "Это обход симптома через индексирование не того. Правильный фикс — отправлять правильный тип, чтобы использовался существующий индекс; не добавляй индекс ради компенсации баги типа в запросе." },
  ]}
/>

<Quiz
  id="02-indexes-choice-6"
  lessonSlug="02-indexes"
  lang="ru"
  question="Поиск делает WHERE title ILIKE '%invoice%' на таблице в 50M строк и занимает 12 секунд. Запрос — это keyword-поиск по тексту документов. Какой правильный выбор индекса?"
  choices={[
    { label: "GIN-индекс по tsvector текста (например STORED GENERATED tsvector-колонка). Это инвертированный индекс на уровне слов со стеммингом и ts_rank — нативный для Postgres ответ для keyword full-text на таком масштабе.", correct: true },
    { label: "Обычный B-tree по title.", misconception: "У ведущего wildcard '%invoice%' нет известного префикса, поэтому B-tree (которому нужен префикс для навигации) не помогает — это всегда O(n). Именно поэтому запрос медленный." },
    { label: "pgvector HNSW по embedding для title.", misconception: "HNSW делает семантическую близость и требует embedding-пайплайн на каждый документ и запрос. Для точного keyword-поиска это неправильный и куда более дорогой инструмент; tsvector GIN — дефолт." },
    { label: "BRIN-индекс по title.", misconception: "BRIN суммирует min/max по диапазону блоков для физически упорядоченных числовых или временных данных. Он вообще не умеет отвечать на подстроку или keyword-containment по тексту." },
  ]}
/>

<Recap lang="ru">
Сквозная линия юнита — один цикл проектирования: composite используется только от ведущего префикса, поэтому проектируй его вокруг всегда присутствующего фильтра; partial-предикаты урезают индекс по горячему подмножеству до доли полного размера и пропускают записи холодных строк; INCLUDE плюс свежая Visibility Map позволяют index-only scan пропускать heap; тип индекса должен соответствовать форме данных (B-tree для упорядоченных значений, GIN для текста и JSONB, HNSW для семантики); а повторяющиеся сбои — отсутствующий FK-индекс, приведение типа, устаревшая статистика, ведущий wildcard в ILIKE — все сводятся к подгонке индекса под точный вопрос, который задаёт запрос, проверенной через EXPLAIN (ANALYZE, BUFFERS).
</Recap>
