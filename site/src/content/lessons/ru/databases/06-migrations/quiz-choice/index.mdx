---
concepts:
- lock-queue-freeze
- expand-contract
- add-column-rewrite
- not-valid-validate
- backfill-wal-flood
- deploy-order
estMin: 13
lang: ru
level: senior
order: 7
prereqs: []
slug: quiz-choice
sources:
- https://www.postgresql.org/docs/current/explicit-locking.html
- https://www.postgresql.org/docs/current/sql-altertable.html
- https://xata.io/blog/migrations-and-exclusive-locks
status: ready
summary: "Тест с выбором на синтез по всему юниту: lock-queue freeze, перезапись при ADD COLUMN, NOT VALID/VALIDATE, expand-contract, WAL-флуд бэкфилла и порядок деплоя."
title: 'Миграции схемы: тест с выбором ответа'
track: databases
unit: 06-migrations
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Шесть вопросов поперёк всего юнита. Каждый отражает решение, которое ты принимаешь, выкатывая изменение схемы на живую базу под трафиком — не определение для заучивания, а режим отказа, от которого надо защититься.
</Hook>

<Goal lang="ru">
Убедись, что связываешь поведение блокировок, безопасные DDL-перезаписи, фазы expand-contract, радиус поражения бэкфилла и порядок деплоя — тот синтез, к которому вели семь уроков.
</Goal>

<Quiz
  id="06-migrations-choice-1"
  lessonSlug="06-migrations"
  lang="ru"
  question={`На PG 16 деплой выполняет statement ниже, и он завершается за 8 мс, но таблица users возвращает 503 в течение 70 секунд. На users уже выполнялся 60-секундный аналитический SELECT, когда сработал ALTER. Что на самом деле заморозило таблицу?

\`\`\`sql
ALTER TABLE users ADD COLUMN status TEXT DEFAULT 'active';
\`\`\``}
  choices={[
    { label: "ALTER требует ACCESS EXCLUSIVE, который конфликтует с удерживаемым аналитикой ACCESS SHARE, поэтому он встаёт в FIFO-очередь блокировок. Каждый новый SELECT затем встаёт за ожидающим ALTER и не может выполниться, пока медленный запрос не отпустит lock — заморозка длится столько, сколько блокирующий запрос, а не миграция.", correct: true },
    { label: "ADD COLUMN перезаписал все строки, чтобы материализовать дефолт 'active', и перезапись заняла 70 секунд.", misconception: "Константный дефолт мгновенен начиная с PG 11 — он хранится в pg_attribute.attmissingval, ни одна строка не пишется. Время 8 мс доказывает, что перезаписи не было; заморозка пришла из очереди блокировок, а не из работы со строками." },
    { label: "Postgres приоритизирует DDL над чтениями, поэтому убил аналитический запрос и заставил новых читателей ждать очистки.", misconception: "Postgres никогда не вытесняет другую сессию ради миграции. ALTER ждёт за удерживаемым ACCESS SHARE; он не может его отменить. Заморозка — это FIFO-очередь, а не приоритетное вытеснение." },
    { label: "Пул соединений исчерпался сам по себе; миграция — совпадение.", misconception: "Пул исчерпался, потому что новые SELECT застряли в очереди блокировок за ALTER. Миграция — непосредственная причина через очередь блокировок, а не совпадение." },
  ]}
/>

<Quiz
  id="06-migrations-choice-2"
  lessonSlug="06-migrations"
  lang="ru"
  question="Тот же мгновенный ALTER из вопроса 1 продолжает таймаутиться за долгими запросами. Какое одно изменение превращает «миграция замораживает базу» в «миграция логирует ошибку и ретраит»?"
  choices={[
    { label: "SET lock_timeout = '2s' на сессии миграции. Если lock не получен за 2 с, ALTER чисто прерывается, освобождает свою позицию в очереди — и ждущие SELECT разблокируются, — а инструмент миграции ретраит с экспоненциальным backoff до тихого окна.", correct: true },
    { label: "SET statement_timeout = '2s' на сессии миграции.", misconception: "statement_timeout ограничивает, сколько statement выполняется после получения lock — он ничего не делает с ожиданием в очереди. Заморозка очереди — это проблема получения lock, поэтому рычаг — lock_timeout. Миграции нужны оба, по разным причинам." },
    { label: "Запустить миграцию в более узком BEGIN..COMMIT, чтобы сократить удержание lock.", misconception: "Обёртка в транзакцию не сокращает ожидание lock; ALTER всё равно стоит за удерживаемым ACCESS SHARE всю длительность блокирующего запроса. Транзакция управляет лишь временем удержания после получения, а оно уже было 8 мс." },
    { label: "Добавить соединений в пул, чтобы запросы его не исчерпывали.", misconception: "Больше соединений — это лишь больше сессий, копящихся за ALTER в очереди блокировок; таблица всё ещё заморожена для новых читателей. Это маскирует симптом и может усугубить затор." },
  ]}
/>

<Quiz
  id="06-migrations-choice-3"
  lessonSlug="06-migrations"
  lang="ru"
  question="Нужно добавить FOREIGN KEY к таблице orders на 100M строк, не блокируя записи. Какой подход production-безопасен?"
  choices={[
    { label: "ADD CONSTRAINT ... FOREIGN KEY ... NOT VALID (мгновенно, проверяет только новые строки), затем VALIDATE CONSTRAINT отдельным statement — VALIDATE сканирует существующие строки под SHARE UPDATE EXCLUSIVE, который не блокирует INSERT/UPDATE/DELETE.", correct: true },
    { label: "ADD CONSTRAINT ... FOREIGN KEY ... одним statement в непиковое окно.", misconception: "Обычный ADD CONSTRAINT берёт ACCESS EXCLUSIVE и сканирует всю таблицу внутри этого lock — записи заблокированы на время скана независимо от часа. Непиковое окно уменьшает радиус, но не убирает блокировку." },
    { label: "Обеспечить связь проверкой на уровне приложения вместо DB-constraint.", misconception: "Проверки на уровне приложения гонятся под конкуренцией и дрейфуют от схемы; они не дают той же гарантии. Паттерн NOT VALID + VALIDATE добавляет настоящий constraint без блокировки записей, так что нет причин ослаблять гарантию." },
    { label: "Обернуть ADD CONSTRAINT в короткую транзакцию, чтобы ограничить время lock.", misconception: "Lock ACCESS EXCLUSIVE удерживается весь валидирующий скан, в явной транзакции он или нет. Короткая транзакция не помогает; помогает разбиение на NOT VALID + VALIDATE." },
  ]}
/>

<Quiz
  id="06-migrations-choice-4"
  lessonSlug="06-migrations"
  lang="ru"
  question="Rolling-деплой переименовывает users.username в users.handle. Чтобы и старая, и новая версии приложения работали в каждый момент, какой порядок шагов верен?"
  choices={[
    { label: "Expand (ADD COLUMN handle), затем деплой dual-write кода (пишет оба), затем бэкфилл батчами, затем перевод чтений на handle при сохранении dual-write, затем остановка записи username, затем contract (DROP COLUMN username) — каждый шаг независимо деплоится и обратим.", correct: true },
    { label: "Выполнить ALTER TABLE ... RENAME COLUMN username TO handle, затем задеплоить новый код, использующий handle.", misconception: "Во время rolling-деплоя старые поды всё ещё ссылаются на username; в момент коммита переименования они получают ошибки «column does not exist». Rename быстр, но ломает инвариант совместимости с обеими версиями — именно этот сбой и предотвращает expand-contract." },
    { label: "Сначала задеплоить dual-write код, затем ADD COLUMN handle, затем дропнуть username сразу после бэкфилла.", misconception: "Dual-write в ещё не существующую колонку падает, а дроп username сразу после бэкфилла убивает старые поды, ещё читающие его. Contract должен ждать, пока ни один работающий код не ссылается на старую колонку." },
    { label: "ADD COLUMN handle и DROP COLUMN username в одной миграции, затем задеплоить новый код.", misconception: "Дроп username, пока старые поды его читают, ломает их немедленно — это тот же сбой однашагового rename, только с лишними шагами. Expand и contract должны быть разделены полными выкатками кода." },
  ]}
/>

<Quiz
  id="06-migrations-choice-5"
  lessonSlug="06-migrations"
  lang="ru"
  question={`Бэкфилл выезжает одним statement на таблице в 100M строк. На связке primary + 2 реплики что ломается первым и какой устойчивый фикс?

\`\`\`sql
UPDATE users SET handle = username WHERE handle IS NULL;
\`\`\``}
  choices={[
    { label: "Единый UPDATE эмитит по одной WAL-записи на строку — десятки ГБ за минуты. Реплики не успевают реплеить, поэтому replication lag растёт и standby-чтения становятся устаревшими. Фикс: батчи по 1k–10k строк с pg_sleep между батчами и наблюдение за pg_stat_replication.replay_lag.", correct: true },
    { label: "Primary дедлочится сам с собой, и UPDATE откатывается.", misconception: "Единый UPDATE не дедлочится сам с собой. Радиус поражения — WAL-флуд и вызванный им replication lag на standby, а не дедлок на primary." },
    { label: "Postgres отказывает в UPDATE, потому что тот перегрузил бы реплики.", misconception: "У Postgres нет механизма троттлить DML по влиянию на репликацию — он выполняет UPDATE и затапливает WAL. Ограничение радиуса поражения — задача инженера через батчинг." },
    { label: "Ничего не ломается; реплики реплеят WAL мгновенно независимо от объёма.", misconception: "Реплики реплеят WAL последовательно; десятки ГБ нельзя потребить мгновенно. Lag растёт, и standby-чтения возвращают устаревшие данные — ровно тот сбой, который предотвращает батчинг." },
  ]}
/>

<Quiz
  id="06-migrations-choice-6"
  lessonSlug="06-migrations"
  lang="ru"
  question="Три реплики Kubernetes стартуют одновременно, и каждая выполняет migrate up при запуске. Одна успешна; две падают с ошибками дублирования объекта. Чего не хватало и какой более чистый production-паттерн?"
  choices={[
    { label: "Нет сериализации раннеров миграции: все три проверили schema_migrations, все увидели миграцию pending и все её применили, гоняясь. Фикс: advisory lock (pg_advisory_lock), чтобы применял только один раннер за раз — и лучше, pre-deploy job миграции, гейтящийся перед rolling-выкаткой подов, чтобы поды вообще не гонялись на старте.", correct: true },
    { label: "Миграции идемпотентны по умолчанию, так что падение — баг инструмента.", misconception: "Миграции не идемпотентны по умолчанию — CREATE TABLE или ADD COLUMN падает при повторном применении. Падение — ожидаемый итог гонки трёх раннеров без lock, а не баг инструмента." },
    { label: "Postgres должен был сам поставить три DDL-statement в очередь и выполнить их последовательно.", misconception: "Postgres понятия не имеет, что три сессии — логически один набор миграций; он выполняет каждую независимо, и они сталкиваются. Сериализацию обеспечивает раннер через advisory lock." },
    { label: "Добавить уникальный constraint на schema_migrations и дать проигравшим тихо упасть.", misconception: "Уникальный constraint останавливает двойные вставки в журнал, но сам DDL всё равно ошибается на проигравших подах и зацикливает их в рестартах. Фикс — сериализовать раннеры, в идеале вообще убрать из старта подов." },
  ]}
/>

<Recap lang="ru">
Сквозная линия юнита — один защитный чек-лист для каждого изменения схемы. Блокировки: большинству DDL нужен ACCESS EXCLUSIVE, и FIFO-очередь превращает один медленный запрос в заморозку таблицы — SET lock_timeout, чтобы неудачная попытка прервалась и ретраила. Перезаписи: константный дефолт мгновенен с PG 11, volatile-дефолты всё ещё перезаписывают. Constraints и индексы: NOT VALID + VALIDATE и CREATE INDEX CONCURRENTLY берут SHARE UPDATE EXCLUSIVE и оставляют DML работающим. Ломающие изменения: expand-contract держит старый и новый код совместимыми в каждый момент, никогда не одношаговый rename. Бэкфиллы: батчь, чтобы ограничить WAL-флуд и replica lag. Раннеры: сериализуй advisory lock'ом и гейти pre-deploy job перед выкаткой. Каждый ответ сводится к одному инварианту — схема должна оставаться совместимой с обеими версиями кода, в каждый момент, не замораживая таблицу.
</Recap>
