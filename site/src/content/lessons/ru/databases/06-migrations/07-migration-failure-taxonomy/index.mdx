---
concepts:
- migration-failure-taxonomy
- WAL-flood
- schema-drift
- replica-lag
- migration-observability
- Squawk-CI
deepensInto: []
estMin: 14
lang: ru
lessonType: topic
level: senior
order: 7
prereqs:
- databases/06-migrations/06-advisory-lock-and-tooling
slug: 07-migration-failure-taxonomy
sources:
- https://www.postgresql.org/docs/current/explicit-locking.html
- https://squawkhq.com/
- https://atlasgo.io/
- https://postgres.ai/blog/20210923-zero-downtime-postgres-schema-migrations-lock-timeout-and-retries
spiral:
- statefulness
- latency
status: ready
summary: Девять именованных режимов отказа — заморозка очереди блокировок, INVALID-индекс,
  WAL-наводнение, дрейф схемы — каждый с сигналом обнаружения и устойчивым исправлением.
title: Таксономия сбоев миграций и дисциплина продакшна
track: databases
unit: 06-migrations
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import NumbersCard from "~/components/layout/NumbersCard.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
Шесть месяцев после внедрения pipeline миграций у команды не было инцидентов с очередью блокировок. Затем обновление бэкфила генерирует 40 ГБ WAL за десять минут — лаг репликации достигает 120 секунд, чтения из standby устаревают, и две read-реплики возвращают строки, противоречащие друг другу. Миграция прошла нормально; вокруг неё упала база.
</Hook>

<Crux>Каждый режим отказа имеет имя, сигнал и исправление. Старшие команды знают все девять и превращают каждый сбой в алерт.</Crux>

<Explanation>

## Девять режимов отказа миграций

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Режим</th>
        <th class="border border-border px-3 py-2 text-left">Сигнал</th>
        <th class="border border-border px-3 py-2 text-left">Устойчивое исправление</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">(a) Заморозка очереди блокировок</td>
        <td class="border border-border px-3 py-2">Таблица заморожена, pool исчерпан, 503</td>
        <td class="border border-border px-3 py-2">lock_timeout + повторы (урок 03)</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">(b) INVALID-индекс</td>
        <td class="border border-border px-3 py-2">pg_indexes indisvalid = false после деплоя</td>
        <td class="border border-border px-3 py-2">DROP INDEX CONCURRENTLY + повтор; алерт на indisvalid</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">(c) Дедлок миграции</td>
        <td class="border border-border px-3 py-2">ERROR: deadlock detected в логе миграции</td>
        <td class="border border-border px-3 py-2">Сериализация через advisory lock; никогда не запускать параллельные миграции на связанных таблицах</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">(d) Уничтожение данных при откате</td>
        <td class="border border-border px-3 py-2">Потеря данных обнаружена после down-миграции</td>
        <td class="border border-border px-3 py-2">Никогда не использовать down-миграции в продакшне; использовать прямые исправления</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">(e) Дрейф схемы на репликах</td>
        <td class="border border-border px-3 py-2">Запросы к standby падают; метрика лага репликации растёт</td>
        <td class="border border-border px-3 py-2">Блокировать деплой кода до почти нулевого лага реплик; использовать replica-aware инструменты</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">(f) WAL-наводнение бэкфила</td>
        <td class="border border-border px-3 py-2">Скорость генерации WAL растёт; лаг реплик увеличивается; диск заполняется</td>
        <td class="border border-border px-3 py-2">Пакетные UPDATE по 1к–10к строк; pg_sleep между пакетами; мониторинг скорости WAL</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">(g) Скрытая перезапись из-за волатильного дефолта</td>
        <td class="border border-border px-3 py-2">Миграция заняла минуты; таблица неожиданно перезаписана</td>
        <td class="border border-border px-3 py-2">Squawk ловит `DEFAULT now()` в CI; константный дефолт + обновление после миграции</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">(h) NOT NULL без бэкфила</td>
        <td class="border border-border px-3 py-2">ALTER COLUMN SET NOT NULL падает при применении</td>
        <td class="border border-border px-3 py-2">Сначала бэкфил; использовать паттерн NOT VALID + VALIDATE (урок 04)</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">(i) RENAME во время rolling deploy</td>
        <td class="border border-border px-3 py-2">Старые поды: ошибки column does not exist</td>
        <td class="border border-border px-3 py-2">Expand-contract вместо одношагового переименования (урок 05)</td>
      </tr>
    </tbody>
  </table>
</div>

## WAL-наводнение бэкфила подробно

Наивный бэкфил запускает один большой UPDATE:

```sql
-- НИКОГДА не делать этого на большой таблице:
UPDATE users SET handle = username WHERE handle IS NULL;
```

На 100 млн строк это генерирует запись WAL (Write-Ahead Log) для каждой обновлённой строки — потенциально 20–50 ГБ WAL за минуты. Реплики должны потребить этот WAL перед обслуживанием чтений; лаг репликации вырастает до минут и более. В это окно standby read-реплики возвращают устаревшие данные. Если лаг превышает `max_standby_streaming_delay`, Postgres отменяет конфликтующие запросы на standby.

**Устойчивое исправление**: пакеты по 1к–10к строк с пространством для дыхания:

```sql
DO $$
DECLARE
  batch INT;
BEGIN
  LOOP
    UPDATE users SET handle = username
    WHERE handle IS NULL
      AND ctid IN (
        SELECT ctid FROM users WHERE handle IS NULL LIMIT 5000
      );
    GET DIAGNOSTICS batch = ROW_COUNT;
    EXIT WHEN batch = 0;
    PERFORM pg_sleep(0.1);
  END LOOP;
END $$;
```

Мониторьте `SELECT * FROM pg_stat_replication` — следите, чтобы `sent_lsn - replay_lsn` оставалось около нуля во время бэкфила.

## Дрейф схемы на репликах

Миграция, применённая на primary, распространяется на реплики через потоковую репликацию. Лаг репликации (нормальный диапазон: менее 1 с; под нагрузкой: 5–30 с) означает, что реплики могут видеть старую схему секунды после коммита миграции. Если код деплоится до нагона репликации:

- Read-реплики обслуживают запросы к старой схеме.
- Новый код, ожидающий новую колонку, получает NULL или ошибки от standby-чтений.

**Устойчивое исправление**: блокировать раскатку кода до приближения лага репликации к нулю. Мониторьте через `pg_stat_replication.replay_lag` на primary. В pre-deploy проверках большинства инструментов миграций включён запрос к replica-lag.

## Squawk CI и стратегическая позиция по миграциям

**Squawk** (Linux Foundation) парсит SQL миграции и предупреждает или выдаёт ошибки на небезопасных паттернах:

- `ADD COLUMN with volatile DEFAULT` → ошибка
- `ALTER COLUMN TYPE` без проверки совместимости типов → ошибка
- `CREATE INDEX` без `CONCURRENTLY` → ошибка
- `RENAME COLUMN / TABLE` → предупреждение
- `DROP COLUMN` без подтверждения предварительного деплоя кода → предупреждение

Запускайте Squawk на каждом PR, затрагивающем `migrations/**`. Стоимость: менее 30 с на PR с миграцией. Польза: ловит наиболее распространённые режимы отказа до слияния.

**Стратегическая позиция**: относитесь к коду миграций с той же дисциплиной, что к коду приложения — PR-ревью, CI-lint, деплой на стейджинг на данных продакшн-размера, запись в runbook, наблюдаемость по времени выполнения и получению блокировки. Старшие команды выпускают ломающие изменения регулярно; разница в том, что каждое изменение спланировано, прочёсано линтером, наблюдаемо и поддаёт прямому откату.

<NumbersCard
  id="card-migration-observability-ru"
  title="Целевые показатели наблюдаемости миграций"
  stats={[
    { label: "Порог алерта: повторы миграции", value: "Больше 3 — страницовать on-call" },
    { label: "Порог алерта: INVALID-индекс после деплоя", value: "Любой — страницовать" },
    { label: "Порог алерта: длительность миграции", value: "Больше 30 с — предупредить (перезапись?)" },
    { label: "Порог алерта: лаг репликации при бэкфиле", value: "Больше 10 с — замедлить пакеты" },
    { label: "Время выполнения Squawk CI", value: "Менее 30 с на PR с миграцией" },
    { label: "Изменения схемы в продакшне (зрелые команды)", value: "Ежедневно" },
  ]}
/>

<Inset kind="why" lang="ru">
Почему Postgres использует WAL для реплик, а не просто копирует изменённые строки? WAL — источник истины для восстановления после сбоя и точечного восстановления. Каждое изменение записывается как запись WAL до применения к heap. Потоковая репликация просто читает WAL и воспроизводит его на standby. Это означает, что операции бэкфила, затрагивающие миллионы строк, генерируют миллионы записей WAL — нет способа подавить генерацию WAL для DML. Пакетирование держит объём WAL управляемым, ограничивая число строк, изменяемых за транзакцию.
</Inset>

<Quiz
  id="07-migration-failure-taxonomy-quiz-1"
  lessonSlug="07-migration-failure-taxonomy"
  lang="ru"
  question="Большой единственный UPDATE-бэкфил генерирует 40 ГБ WAL за минуты. Какой первый наблюдаемый симптом в схеме primary + 2 реплики?"
  choices={[
    { label: "Лаг репликации растёт на репликах, так как они не успевают потреблять WAL — standby-чтения возвращают устаревшие данные от секунд до минут", correct: true },
    { label: "Primary исчерпывает дисковое пространство и падает", misconception: "Возможно при неправильной настройке удержания WAL, но первый наблюдаемый симптом — лаг репликации на standby до заполнения диска." },
    { label: "UPDATE-бэкфил занимает дольше ожидаемого", misconception: "UPDATE может завершиться быстро; радиус поражения — от отставания реплик на воспроизведении WAL, а не от длительности UPDATE." },
    { label: "Postgres предотвращает выполнение больших UPDATE для защиты реплик", misconception: "У Postgres нет механизма предотвращения больших DML на основе влияния на репликацию." },
  ]}
/>

<Quiz
  id="07-migration-failure-taxonomy-quiz-2"
  lessonSlug="07-migration-failure-taxonomy"
  lang="ru"
  question="Squawk запускается в CI и обнаруживает `CREATE INDEX ON orders(user_id)` (без CONCURRENTLY). Какой правильный ответ?"
  choices={[
    { label: "Изменить на CREATE INDEX CONCURRENTLY ON orders(user_id) и пометить файл миграции как нетранзакционный — это продакшн-безопасная форма", correct: true },
    { label: "Отключить правило Squawk для этой миграции — CREATE INDEX без CONCURRENTLY нормально для небольших таблиц", misconception: "Таблица может вырасти; Squawk существует, чтобы поймать подобное до инцидента. Исправьте, не заглушайте." },
    { label: "Запланировать миграцию на нерабочее время — блокировка безопасна при низком трафике", misconception: "Нерабочее время снижает риск, но не устраняет его. CONCURRENTLY полностью устраняет блокировку DML." },
    { label: "Разбить миграцию на две: одну для таблицы и одну для индекса", misconception: "Разбиение не изменяет поведение блокировки. Исправление — CONCURRENTLY, а не разбиение." },
  ]}
/>

<Quiz
  id="07-migration-failure-taxonomy-quiz-3"
  lessonSlug="07-migration-failure-taxonomy"
  lang="ru"
  question="Миграция применяется на primary. Лаг репликации сейчас 15 секунд. Что происходит при немедленном запуске деплоя кода?"
  choices={[
    { label: "Read-реплики обслуживают запросы к старой схеме до 15 секунд — новый код, ожидающий новую колонку, может получить NULL или ошибки column-not-found из standby-чтений", correct: true },
    { label: "Primary перенаправляет standby-чтения на себя до нагона реплик", misconception: "Postgres автоматически не переключает чтения на primary; маршрутизация read-реплики приложения не меняется." },
    { label: "Реплики блокируют чтения до воспроизведения WAL — устаревшие данные невозможны", misconception: "По умолчанию реплики обслуживают чтения даже при лаге; устаревшие данные — компромисс ради масштабирования чтений." },
    { label: "Ничего — новый код читает только с primary, а не с реплик", misconception: "Большинство продакшн-приложений направляют чтения на standby; конфигурация деплоя кода это определяет, а не Postgres." },
  ]}
/>

</Explanation>

<FlowDiagram
  label="Каскад WAL-наводнения при бэкфиле: один большой UPDATE пишет WAL-запись на каждую строку, наводняя WAL; реплики не успевают его проиграть, поэтому лаг репликации растёт и чтения со standby устаревают. Ветка исправления разбивает UPDATE на пакеты с pg_sleep, ограничивая WAL на транзакцию и удерживая лаг реплик близко к нулю."
  perRow={3}
  hue="var(--d-data)"
  nodes={[
    { id: "bigupdate", label: "Один большой UPDATE", sub: "100M строк" },
    { id: "wal", label: "WAL-наводнение", sub: "1 запись на строку" },
    { id: "lag", label: "Лаг реплик растёт", sub: "120s+" },
    { id: "stale", label: "Устаревшие чтения", sub: "запросы противоречат" },
    { id: "batched", label: "Пакетный UPDATE", sub: "1k-10k + pg_sleep" },
    { id: "bounded", label: "Ограниченный WAL", sub: "реплики успевают" },
    { id: "near0", label: "Лаг около нуля", sub: "чтения согласованы" },
  ]}
  edges={[
    { from: "bigupdate", to: "wal" },
    { from: "wal", to: "lag" },
    { from: "lag", to: "stale" },
    { from: "batched", to: "bounded" },
    { from: "bounded", to: "near0" },
  ]}
  caption="Верхняя цепочка: один большой UPDATE наводняет WAL, реплики отстают, чтения со standby устаревают. Нижняя цепочка: разбиение на пакеты с pg_sleep ограничивает WAL на транзакцию, поэтому реплики не отстают."
/>

<KeyTakeaway>Девять именованных режимов отказа покрывают каждый распространённый инцидент с миграциями. Заморозка очереди блокировок и INVALID-индекс выявляются lock_timeout и мониторингом indisvalid. WAL-наводнение предотвращается пакетным бэкфилом. Дрейф схемы на репликах предотвращается блокировкой деплоя кода до почти нулевого лага репликации. Squawk CI ловит остальное до слияния.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="07-migration-failure-taxonomy-retrieval"
  lang="ru"
  questions={[
    { q: "Почему большой однооператорный UPDATE-бэкфил наводняет WAL и какова рекомендация по размеру пакета?", a: "Каждая строка, изменённая UPDATE, генерирует запись WAL до записи в heap. Postgres не может подавить генерацию WAL для DML — WAL является источником истины для восстановления после сбоя и репликации. Единственный UPDATE на 100 млн строк генерирует одну запись WAL на строку — 20–50 ГБ за минуты. Потоковые реплики получают и воспроизводят этот WAL последовательно; если они не успевают, лаг репликации растёт и standby-чтения устаревают. Исправление: пакеты по 1к–10к строк на транзакцию с pg_sleep(0.1) между пакетами. Каждый пакет генерирует ограниченный объём WAL; сон даёт репликам время нагнать. Мониторьте pg_stat_replication.replay_lag во время бэкфила и замедляйтесь при лаге больше 5–10 с." },
    { q: "Что такое дрейф схемы на репликах и как блокировка деплоя кода на лаге репликации предотвращает его?", a: "Дрейф схемы на репликах происходит, когда миграция коммитируется на primary, но лаг репликации означает, что standby ещё не воспроизвели WAL с DDL-изменением. В это окно standby-запросы видят старую схему. Если деплой кода начинается до достижения лага около нуля, новый код, читающий новую колонку из standby, получает ошибки column-not-found или NULL. Исправление: перед запуском rolling deploy подов запросить primary о `pg_stat_replication.replay_lag` на всех standby; блокировать деплой до лага ниже 1–2 секунд. Atlas и другие инструменты поддерживают проверку replica-lag в pre-deploy фазе. Для критических ломающих изменений временно направлять все чтения на primary в окне миграции." },
    { q: "Назовите четыре вещи, которые Squawk проверяет в CI, и объясните, почему каждая небезопасна без проверки.", a: "(1) ADD COLUMN с волатильным DEFAULT — вызывает полную перезапись таблицы под ACCESS EXCLUSIVE; большие таблицы замораживаются на минуты. (2) ALTER COLUMN TYPE без проверки совместимости — несовместимые по бинарному представлению изменения перезаписывают таблицу; блокировка удерживается на время перезаписи. (3) CREATE INDEX без CONCURRENTLY — берёт ACCESS EXCLUSIVE на всё время построения; чтения и записи заблокированы минутами на больших таблицах. (4) RENAME COLUMN / TABLE — ломающее изменение при rolling deploy: старые поды ссылаются на старое имя, получают ошибки column-not-found. Squawk обнаруживает их во время PR за менее 30 с на файл миграции, превращая инциденты в комментарии к ревью до попадания в продакшн." },
  ]}
/>

<Recap lang="ru">
Старшая дисциплина миграций называет девять режимов отказа и строит наблюдаемость для каждого. Заморозка очереди блокировок (режим a) — наиболее частый — исправление: `lock_timeout` + повторы. INVALID-индекс (b) обнаруживается мониторингом `indisvalid` после деплоя — исправление: `DROP INDEX CONCURRENTLY` + повтор. WAL-наводнение бэкфила (f) вызывает рост лага репликации — исправление: пакеты по 1к–10к строк с `pg_sleep`. Дрейф схемы на репликах (e) вызывает устаревшие чтения после быстро распространяющегося DDL — исправление: блокировать деплой кода до почти нулевого лага репликации. Squawk CI ловит небезопасный DDL во время PR: волатильные дефолты, неконкурентные индексы, переименования и изменения типа без проверки совместимости. Зрелые команды выпускают изменения схемы ежедневно, потому что их инструменты делают безопасность наименее затратным путём, а не исключительной дисциплиной.
</Recap>
