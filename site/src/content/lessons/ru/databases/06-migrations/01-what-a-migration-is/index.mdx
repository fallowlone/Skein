---
concepts:
- schema-migration
- migration-versioning
- deploy-order
- migration-tool
deepensInto: []
estMin: 8
lang: ru
lessonType: topic
level: junior
order: 1
prereqs:
- databases/01-relational-model/01-what-a-relation-is
slug: 01-what-a-migration-is
sources:
- https://www.postgresql.org/docs/current/sql-altertable.html
- https://www.prisma.io/docs/orm/prisma-migrate/getting-started
- https://flywaydb.org/documentation/
spiral:
- statefulness
status: ready
summary: Миграция — это версионированный, упорядоченный SQL-скрипт, изменяющий схему
  БД шаг-в-шаг с кодом приложения, который от неё зависит.
title: Что такое миграция схемы и почему она заменяет ad-hoc DDL
track: databases
unit: 06-migrations
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import MetaphorComplete from "~/components/pedagogy/MetaphorComplete.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
Отто хочет добавить колонку `phone` в таблицу `users`. Он открывает psql и запускает `ALTER TABLE users ADD COLUMN phone TEXT`. Три месяца спустя приходит новый инженер, клонирует репозиторий, получает ошибки — в его базе нет колонки `phone`. Никто не знает, что нужно изменить. Схема разошлась.
</Hook>

<Crux>Миграция — версионированный SQL-скрипт, переводящий схему из N в N+1, как git-коммит переводит код, делая каждую среду воспроизводимой.</Crux>

<Explanation>

## Проблема с ad-hoc DDL

Запуск `ALTER TABLE` напрямую в psql работает один раз, на одной машине, для одного инженера. Как только появляется вторая среда — стейджинг, CI, ноутбук коллеги — схема молча расходится. Никто не отслеживает, что изменилось и когда. Возможность воспроизвести всё заново теряется.

**Файл миграции** кодирует изменение как именованный, упорядоченный артефакт:

```sql
-- migrations/20260514_add_user_phone.sql
ALTER TABLE users ADD COLUMN phone TEXT;
```

Инструмент миграции записывает, какие файлы были применены, в таблицу `schema_migrations`. Новый инженер, запустив `prisma migrate dev` или `flyway migrate`, получает все ожидающие миграции в правильном порядке. База приходит в одно и то же известное состояние на каждой машине.

## Миграции версионируются как коммиты

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Git-коммит</th>
        <th class="border border-border px-3 py-2 text-left">Файл миграции</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">Переводит кодовую базу из состояния N в N+1</td>
        <td class="border border-border px-3 py-2">Переводит схему из состояния N в N+1</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Упорядочен по timestamp/хешу</td>
        <td class="border border-border px-3 py-2">Упорядочен по версионному префиксу (timestamp или V1/V2)</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Применяется один раз; неизменяем после мержа</td>
        <td class="border border-border px-3 py-2">Применяется один раз на базу; не редактируется после деплоя</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Ревьюируется как diff в PR</td>
        <td class="border border-border px-3 py-2">Ревьюируется как SQL в PR, проверяется линтером Squawk или Atlas</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Воспроизводимо: `git clone` + история = тот же код</td>
        <td class="border border-border px-3 py-2">Воспроизводимо: `migrate up` с нуля = та же схема</td>
      </tr>
    </tbody>
  </table>
</div>

## Порядок деплоя важен

Схема БД и код приложения, который её использует, должны быть совместимы в каждый момент деплоя — продакшн-трафик не останавливается ради миграции.

Два правила:

1. **Аддитивное изменение** (новая колонка, которую читает новый код): запустите миграцию **до** деплоя кода. Старый код игнорирует новую колонку; новый код находит её готовой.
2. **Удаление** (дроп колонки, которую читает старый код): деплойте новый код **первым**, дождитесь, пока старый код сдренируется, затем запустите миграцию. Иначе старый код запрашивает несуществующую колонку и падает.

Однотранзакционные миграции, удаляющие или переименовывающие колонки за один шаг — классический deploy-coupled баг; фикс — разбить изменения на безопасные аддитивные шаги (урок 05).

<Inset kind="why" lang="ru">
Почему инструменты миграции используют таблицу `schema_migrations`, а не сравнивают живую схему с желаемой? Diff схем ненадёжен — две схемы могут выглядеть структурно одинаково, при этом иметь разные индексы, constraint'ы или права. Отслеживание применённых файлов миграций проще, аудируемо и выживает при восстановлении базы из бэкапа.
</Inset>

<Quiz
  id="01-what-a-migration-is-quiz-1"
  lessonSlug="01-what-a-migration-is"
  lang="ru"
  question="Зачем использовать файлы миграций вместо ALTER TABLE напрямую в psql?"
  choices={[
    { label: "Миграции версионированы, ревьюируемы, воспроизводимы по средам и аудируемы — они отвечают на «что изменилось и когда» для каждого изменения состояния схемы", correct: true },
    { label: "Postgres требует файлы миграций для DDL", misconception: "Postgres принимает любой DDL ad-hoc; проблема в воспроизводимости и аудируемости, а не в требованиях Postgres." },
    { label: "Файлы миграций быстрее, чем DDL в psql", misconception: "Тот же SQL выполняется одинаково в любом случае. Плюс — в дисциплине процесса, а не в скорости выполнения." },
    { label: "psql не поддерживает ALTER TABLE", misconception: "psql поддерживает весь DDL. Проблема — в возможности воспроизвести всё по средам, а не в возможностях psql." },
  ]}
/>

<Quiz
  id="01-what-a-migration-is-quiz-2"
  lessonSlug="01-what-a-migration-is"
  lang="ru"
  question="Миграция добавляет новую колонку, которую читает новый код. Каков правильный порядок деплоя?"
  choices={[
    { label: "Сначала запустить миграцию, потом деплоить код — старый код игнорирует колонку, новый код находит её готовой", correct: true },
    { label: "Деплоить код первым, потом запустить миграцию", misconception: "Новый код пытается читать несуществующую колонку и падает, пока миграция не выполнена." },
    { label: "Запускать миграцию и деплой кода одновременно", misconception: "Есть окно, в котором новый код уже работает, но колонки ещё нет — запросы падают." },
    { label: "Порядок не важен для аддитивных изменений", misconception: "Порядок всегда важен. Единственный безопасный порядок для аддитивных изменений — сначала миграция." },
  ]}
/>

<DragOrder
  id="01-what-a-migration-is-drag-1"
  lessonSlug="01-what-a-migration-is"
  lang="ru"
  prompt="Расставьте шаги жизненного цикла продакшн-миграции схемы:"
  items={[
    { id: "a", label: "Написать SQL-файл миграции с версионным префиксом (timestamp или V-номер)" },
    { id: "b", label: "Открыть PR: SQL ревьюируется, проверяется Squawk или Atlas CI" },
    { id: "c", label: "Смержить PR — файл миграции теперь в системе контроля версий" },
    { id: "d", label: "CI запускает миграцию на стейджинге; тесты проходят" },
    { id: "e", label: "Миграция запускается в продакшне до деплоя нового кода" },
    { id: "f", label: "Деплоится код приложения, зависящий от изменения схемы" },
    { id: "g", label: "Инструмент миграции записывает файл как применённый в schema_migrations" },
  ]}
  correctOrder={["a", "b", "c", "d", "e", "f", "g"]}
/>

<MetaphorComplete
  id="01-what-a-migration-is-metaphor-1"
  lessonSlug="01-what-a-migration-is"
  lang="ru"
  setup="Заполните пропуск: миграция БД — это для изменений схемы то же, что git ________ для изменений кода — версионированная, упорядоченная, ревьюируемая единица, переводящая систему из состояния N в N+1."
  accepted={["коммит", "commit", "коммиты"]}
  canonical="коммит"
  explanation="Коммит переводит код из одного состояния в другое с историей, ревью и возможностью отката. Миграция делает то же для схемы. Оба имеют упорядоченную родословную; оба неизменяемы после отправки; оба — то, как команда рассуждает об изменениях во времени."
/>

</Explanation>

<FlowDiagram
  label="Миграция схемы проходит упорядоченный конвейер: написать версионированный SQL, проверить и прогнать линтер в PR, влить, прогнать на CI-стейджинге, применить на проде перед деплоем нового кода, затем записать как применённую в schema_migrations."
  perRow={4}
  hue="var(--d-data)"
  nodes={[
    { id: "write", label: "Пишем SQL", sub: "префикс версии" },
    { id: "pr", label: "Ревью PR", sub: "линт Squawk / Atlas" },
    { id: "merge", label: "Мердж", sub: "в системе версий" },
    { id: "staging", label: "CI-стейджинг", sub: "тесты прошли" },
    { id: "prod", label: "migrate прод", sub: "перед кодом" },
    { id: "deploy", label: "Деплой кода", sub: "читает новую схему" },
    { id: "record", label: "schema_migrations", sub: "помечена применённой" },
  ]}
  edges={[
    { from: "write", to: "pr" },
    { from: "pr", to: "merge" },
    { from: "merge", to: "staging" },
    { from: "staging", to: "prod" },
    { from: "prod", to: "deploy" },
    { from: "deploy", to: "record" },
  ]}
  caption="Каждая миграция — упорядоченный проверяемый артефакт: применяется один раз на базу, записывается в schema_migrations, поэтому каждая среда приходит к одному и тому же состоянию схемы."
/>

<KeyTakeaway>Миграция — это версионированный SQL-скрипт, применяемый один раз на базу в порядке, делая схему воспроизводимой в каждой среде — так же, как git делает код воспроизводимым.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="01-what-a-migration-is-retrieval"
  lang="ru"
  questions={[
    { q: "В двух предложениях: что такое миграция схемы и какую проблему она решает по сравнению с ad-hoc DDL?", a: "Миграция — это версионированный, упорядоченный SQL-файл, переводящий схему БД из одного состояния в следующее и отслеживаемый инструментом в таблице schema_migrations. Ad-hoc DDL выполняется на одной машине и не оставляет записей — миграции делают схему воспроизводимой на стейджинге, в CI и в продакшне и аудируемой в истории version control." },
    { q: "Почему порядок деплоя миграции и кода важен и каково правило для аддитивного изменения?", a: "Продакшн-трафик не останавливается во время деплоя, поэтому и старая, и новая версия приложения должны быть совместимы с текущей схемой в каждый момент. Для аддитивного изменения (новая колонка, которую читает новый код): сначала запустите миграцию — старый код игнорирует новую колонку, новый код сразу находит её. Запуск кода первым создаёт окно, в котором новый код ссылается на несуществующую колонку — запросы падают." },
    { q: "Что содержит таблица schema_migrations и почему это лучше, чем делать diff схем?", a: "Таблица schema_migrations хранит идентификаторы версий всех применённых файлов миграций в порядке применения. Diff живой схемы с желаемой ненадёжен — две схемы могут выглядеть структурно одинаково, отличаясь определениями индексов, именами constraint'ов или правами. Отслеживание применённых миграций детерминировано и выживает при восстановлении базы из бэкапа." },
  ]}
/>

<Recap lang="ru">
Миграция — это версионированный SQL-файл, переводящий схему БД из состояния N в N+1 и записываемый в таблицу `schema_migrations` после применения. Запуск `ALTER TABLE` ad-hoc работает один раз, но не оставляет истории и молча расходит среды. Инструменты миграций (Prisma Migrate, Flyway, golang-migrate, Atlas) применяют ожидающие файлы по порядку в каждой среде, делая схему всегда воспроизводимой с нуля. Порядок деплоя не опционален: для аддитивных изменений сначала запускайте миграцию, чтобы схема была готова к появлению нового кода.
</Recap>
