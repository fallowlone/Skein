---
concepts:
- expand-contract
- lock-timeout
- not-valid-validate
- batched-backfill
- migration-observability
- zero-downtime-deploy
estMin: 240
lang: ru
level: senior
order: 10
prereqs: []
slug: project
sources:
- https://www.postgresql.org/docs/current/sql-altertable.html
- https://xata.io/blog/migrations-and-exclusive-locks
- https://postgres.ai/blog/20210923-zero-downtime-postgres-schema-migrations-lock-timeout-and-retries
status: ready
summary: "Практический проект: переименуй колонку zero-downtime через expand-contract на нагруженной таблице Postgres, доказав lock-безопасность, батчевый backfill и совместимость с обеими версиями с доказательствами на каждом шаге."
title: 'Миграции схемы: zero-downtime переименование колонки под нагрузкой'
track: databases
unit: 06-migrations
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import ProjectBrief from "~/components/pedagogy/ProjectBrief.astro";

<Hook>
Читать про expand-contract — не то же самое, что переименовать колонку на нагруженной таблице, не уронив ни одного запроса. Подними таблицу Postgres с десятками миллионов строк, направь на неё трафик чтений/записей и мигрируй users.username в users.handle через все фазы — доказывая lock-безопасность и нулевую потерю данных измерениями, а не утверждениями.
</Hook>

<Goal lang="ru">
Преврати ментальную модель юнита в воспроизводимый runbook: подготовь нагруженную таблицу под живым трафиком, разбей ломающий rename на безопасные фазы expand-contract, защити каждый DDL-шаг через lock_timeout, прогони батчевый backfill, который никогда не затапливает WAL, и подтверди, что обе версии кода остаются здоровыми на всём пути.
</Goal>

<ProjectBrief
  id="06-migrations-project"
  lessonSlug="06-migrations"
  lang="ru"
  objective="Переименуй колонку на таблице Postgres в 10M+ строк с username на handle с zero downtime, используя полную последовательность expand-contract, пока генератор нагрузки гоняет и старый (читает/пишет username), и новый (читает/пишет handle) клиент против базы всё время."
  requirements={[
    "Подготовь Postgres 14+ с таблицей users минимум на 10M строк (generate_series). Подними генератор нагрузки, выдающий конкурентные чтения и записи, и периодически запускай долгий аналитический SELECT, чтобы очередь блокировок была реалистичной.",
    "Гоняй две версии приложения бок о бок против одной базы всё упражнение: v1 читает и пишет username; v2 читает и пишет handle. Держи обе работающими, чтобы доказать, что схема остаётся совместимой с обеими на каждой фазе.",
    "Фаза 1 — Expand: ADD COLUMN handle TEXT без дефолта (мгновенно). Запускай каждую сессию миграции с SET lock_timeout = '2s' и SET statement_timeout и сними снапшот pg_locks / pg_stat_activity, показывающий, что ALTER не заморозил таблицу (или чисто прервался-и-переретраил, когда долгий запрос держал ACCESS SHARE).",
    "Фаза 2 — Dual-write: задеплой v2, чтобы записи шли и в username, и в handle. Подтверди, что v1 и v2 обе обслуживают без ошибок.",
    "Фаза 3 — Backfill: скопируй username в handle для существующих строк батчами по 1k–10k строк с pg_sleep между батчами. Записывай pg_stat_replication.replay_lag (или скорость генерации WAL) на протяжении бэкфилла и покажи, что он остаётся ограниченным — в контрасте с одношаговым UPDATE, который ты разово запускаешь на одноразовой копии, чтобы наблюдать WAL-флуд.",
    "Фазы 4–5 — Перевод и остановка старых записей: задеплой v2, читающую handle эксклюзивно (всё ещё dual-write), затем версию, пишущую только handle. Подтверди отсутствие ошибок запросов на каждом rolling-шаге.",
    "Фаза 6 — Contract: DROP COLUMN username только после подтверждения, что ни одна работающая версия на неё не ссылается. Покажи, что дроп быстр и таблица всё ещё обслуживает трафик.",
    "Добавь NOT NULL на handle безопасным способом как бонусный DDL: ADD CHECK (handle IS NOT NULL) NOT VALID, VALIDATE CONSTRAINT, затем ALTER COLUMN SET NOT NULL — доказывая, что VALIDATE-скан шёл под SHARE UPDATE EXCLUSIVE, не блокируя записи генератора нагрузки.",
  ]}
  acceptance={[
    "Таймлайн ошибок запросов для v1 и v2 на всех шести фазах, показывающий ноль упавших запросов, относимых к несовместимости схемы — измеренный под живой нагрузкой, а не предположенный.",
    "Пара lock-доказательств до/после: снапшот pg_locks во время конкурентного ALTER, показывающий путь abort-and-retry по lock_timeout, против того же ALTER, успешного в тихом окне.",
    "Метрики бэкфилла: число батчей, строк на батч и график replication-lag (или WAL-rate), остающийся ограниченным для батчевого прогона, рядом с WAL-спайком одношагового UPDATE на одноразовой копии.",
    "Абзац-разбор, называющий для каждой фазы инвариант, который держался (схема совместима с обеими версиями), и конкретный сбой, который он предотвратил (rename-простой, lock-заморозка, WAL-флуд).",
  ]}
  stretch={[
    "Замени ручной SQL инструментом миграции (Atlas или golang-migrate): подключи сериализацию раннеров через pg_advisory_lock, lock_timeout + ретраи и non-transactional аннотацию на CREATE INDEX CONCURRENTLY по handle. Проверь indisvalid после билда.",
    "Воспроизведи lock-queue инцидент намеренно: удержи 60-секундный аналитический SELECT, выстрели ALTER с lock_timeout = 0 и зафиксируй 503 и исчерпание пула — затем перезапусти с lock_timeout = '2s' и покажи, что заморозки нет.",
    "Добавь наблюдаемость миграций: алерт на ретраи миграции > 3, любой post-deploy indisvalid индекс, replication lag > 10 с во время бэкфилла и длительность миграции > 30 с — затем намеренно сработай каждый алерт.",
    "Подключи Squawk в CI на директории миграций и скорми ему небезопасные одношаговые формы (RENAME COLUMN, CREATE INDEX без CONCURRENTLY, ADD COLUMN с volatile DEFAULT); покажи, что каждая ловится на этапе PR.",
  ]}
/>

<Recap lang="ru">
Это цикл, который ты будешь запускать для каждого ломающего изменения схемы в продакшне: разбей на фазы expand-contract, чтобы схема была совместима с обеими версиями кода в каждый момент, защити каждый DDL-шаг через lock_timeout, чтобы конкурентный ALTER прерывался и ретраил вместо заморозки таблицы, делай backfill батчами, чтобы WAL оставался ограниченным и реплики успевали, добавляй constraints через NOT VALID + VALIDATE, чтобы DML никогда не блокировался, и откатывайся вперёд — никогда разрушительной down-миграцией. Сделав это раз на нагруженной игрушечной таблице под живым трафиком, ты доводишь production-версию до уровня мышечной памяти.
</Recap>
