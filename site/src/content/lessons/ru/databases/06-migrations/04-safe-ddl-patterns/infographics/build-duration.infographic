infographic chart-column-simple
data
  title Тот же индекс, 200 млн строк: время сборки против радиуса блокировки
  desc CONCURRENTLY стоит 2.25x времени сборки за нулевой простой DML
  values
    - label CREATE INDEX (ACCESS EXCLUSIVE, блокирует чтения + записи)
      value 20
    - label CREATE INDEX CONCURRENTLY (SHARE UPDATE EXCL, ничего не блокирует)
      value 45
theme
  palette #f43f5e #14b8a6
