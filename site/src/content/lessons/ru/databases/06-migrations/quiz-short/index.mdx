---
concepts:
- lock-queue-fifo
- add-column-missingval
- not-valid-validate
- expand-contract
- backfill-wal-flood
- advisory-lock
estMin: 14
lang: ru
level: senior
order: 8
prereqs: []
slug: quiz-short
sources:
- https://www.postgresql.org/docs/current/explicit-locking.html
- https://www.postgresql.org/docs/current/sql-altertable.html
- https://xata.io/blog/migrations-and-exclusive-locks
status: ready
summary: "Промпты на свободное припоминание по юниту миграций. Сначала ответь своими словами, затем раскрой модельный ответ и сравни."
title: 'Миграции схемы: свободное припоминание'
track: databases
unit: 06-migrations
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";

<Hook>
Припоминание сильнее перечитывания. На каждый промпт проговори или запиши полный ответ по памяти, прежде чем открыть модельный — именно усилие припоминания закрепляет механизм, когда ты смотришь на замёрзшую таблицу в 2 часа ночи.
</Hook>

<Goal lang="ru">
Восстанови ключевые механизмы юнита — FIFO-очередь блокировок, трюк missing-value из PG 11, NOT VALID/VALIDATE, expand-contract, WAL-флуд бэкфилла и сериализацию раннеров — не подглядывая в уроки.
</Goal>

<RetrievalDrawer
  client:load
  id="06-migrations-short-retrieval"
  lang="ru"
  questions={[
    { q: "Почему мгновенный ALTER TABLE может заморозить нагруженную таблицу на 60+ секунд, и что именно за чем встаёт в очередь?", a: "Большинству DDL нужен ACCESS EXCLUSIVE, который конфликтует со всеми остальными режимами блокировок, включая ACCESS SHARE, удерживаемый SELECT. Если медленный запрос уже держит ACCESS SHARE, ALTER не может получить lock и встаёт в очередь ожидания. Postgres выдаёт блокировки FIFO по объекту, поэтому он не даст более позднему лёгкому запросу ACCESS SHARE проскочить вперёд ждущего ALTER — иначе DDL мог бы голодать вечно на нагруженной таблице. Так что каждый новый SELECT, прибывший после ALTER, тоже встаёт в очередь, за ждущим ACCESS EXCLUSIVE ALTER'а. Таблица заморожена для новых запросов на всю длительность исходного медленного запроса, хотя сама миграция выполняется за миллисекунды, как только наконец получает lock." },
    { q: "Как PG 11+ делает ADD COLUMN с константным дефолтом мгновенным, и в чём разница для volatile-дефолта?", a: "Для неволатильного (константного или stable) дефолта PG 11 хранит значение в каталоге — pg_attribute.atthasmissing = true и attmissingval = значение — вместо записи в каждую строку. Существующие строки сохраняют исходное физическое хранение; при чтении старой строки исполнитель подставляет missing value на лету. Удержание lock падает с минут (полная перезапись) до миллисекунд (обновление каталога). Volatile-дефолт вроде clock_timestamp() или gen_random_uuid() даёт разное значение на строку, поэтому ни одно общее missing value его не представит — Postgres обязан обойти и перезаписать каждую строку под ACCESS EXCLUSIVE, ровно как до PG 11. Безопасный путь для volatile-дефолта: добавить колонку без дефолта (мгновенно), сделать backfill батчами, затем SET DEFAULT для будущих вставок." },
    { q: "Объясни паттерн NOT VALID + VALIDATE CONSTRAINT и почему он production-безопасен там, где обычный ADD CONSTRAINT — нет.", a: "Обычный ADD CONSTRAINT (CHECK или FOREIGN KEY) берёт ACCESS EXCLUSIVE и сканирует всю таблицу внутри этого lock, блокируя все чтения и записи на время скана. NOT VALID добавляет constraint, проверяемый только для строк, вставленных или обновлённых с этого момента; существующие строки не сканируются, поэтому добавление мгновенно под кратким lock. VALIDATE CONSTRAINT затем сканирует существующие строки, подтверждая, что они проходят, но берёт SHARE UPDATE EXCLUSIVE — который конфликтует только с другим DDL и autovacuum, не с INSERT/UPDATE/DELETE/SELECT. Так что DML продолжается во время скана. Двухшаговая форма достигает той же итоговой консистентности, что одношаговая, без блокировки записей. Тот же паттерн безопасно добавляет NOT NULL через CHECK (col IS NOT NULL) NOT VALID + VALIDATE + SET NOT NULL." },
    { q: "Сформулируй ключевой инвариант expand-contract и пройди шесть фаз переименования колонки (username в handle).", a: "Инвариант: в каждый момент rolling-деплоя живая схема должна быть одновременно совместима и с предыдущей, и со следующей версией приложения, потому что старые и новые поды работают одновременно. Одношаговый rename ломает это — старые поды ссылаются на старое имя и падают в момент переименования. Шесть фаз: (1) Expand — ADD COLUMN handle (мгновенно). (2) Dual-write — деплой кода, пишущего и username, и handle. (3) Backfill — UPDATE handle = username батчами. (4) Перевод чтений — деплой кода, читающего handle, всё ещё пишущего оба. (5) Остановка старых записей — деплой кода, пишущего только handle. (6) Contract — DROP COLUMN username. Каждая фаза — отдельный PR и независимо обратима; откат проще всего во время dual-write, потому что обе колонки существуют и заполнены." },
    { q: "Почему одношаговый backfill затапливает WAL, что ломается ниже по потоку и какой ориентир по размеру батча?", a: "Каждая строка, изменённая UPDATE, генерирует WAL-запись до того, как изменение попадёт в heap, и Postgres не может подавить WAL для DML — WAL это источник истины для crash recovery и репликации. Единый UPDATE по 100M строк эмитит ~100M WAL-записей, десятки ГБ за минуты. Streaming-реплики реплеят WAL последовательно; они отстают, replication lag растёт до минут, и standby-чтения возвращают устаревшие данные (а если lag превысит max_standby_streaming_delay, конфликтующие standby-запросы отменяются). Фикс — батчить по 1k–10k строк на транзакцию с pg_sleep(0.1) между батчами: каждый батч эмитит ограниченный WAL, а sleep даёт репликам догнать. Наблюдай pg_stat_replication.replay_lag во время бэкфилла и замедляй батчи, если lag превышает 5–10 с." },
    { q: "Почему down-миграции разрушают данные в продакшне, почему CREATE INDEX CONCURRENTLY нужны advisory lock и non-transactional аннотация рядом, и какова верная стратегия отката?", a: "Down-миграция реверсит up-SQL — если up был ADD COLUMN, то down это DROP COLUMN, который разрушает каждое значение, записанное новым кодом с момента запуска up (потенциально часы или дни пользовательских данных); Postgres выполняет это безусловно. Поэтому production-откат всегда вперёд: пишешь новую миграцию, исправляющую проблему, и деплоишь её. Отдельно: несколько подов, стартующих одновременно, могут каждый запустить pending-миграции и гоняться — pg_advisory_lock сериализует раннеры, чтобы применял только один за раз (ещё лучше, гейтящийся pre-deploy job убирает гонку полностью). А CREATE INDEX CONCURRENTLY строится в две фазы скана через несколько транзакций, поэтому не может выполняться внутри блока BEGIN..COMMIT; файл миграции должен нести non-transactional аннотацию (Atlas: -- atlas:txmode none; golang-migrate: -- migrate:notransaction), и после нужно проверить indisvalid, дропнув и переретраив любой INVALID-индекс через DROP INDEX CONCURRENTLY." },
  ]}
/>

<Recap lang="ru">
Если ты смог восстановить каждый ответ по памяти, ты держишь хребет юнита: FIFO-очередь блокировок означает, что ждущий ACCESS EXCLUSIVE замораживает таблицу за одним медленным запросом, поэтому lock_timeout обязателен; константный дефолт — это трюк каталога (attmissingval), тогда как volatile-дефолты всё ещё перезаписывают; NOT VALID + VALIDATE и CREATE INDEX CONCURRENTLY переносят тяжёлый скан на SHARE UPDATE EXCLUSIVE, так что DML продолжает работать; expand-contract держит совместимый с обеими версиями суперсет схемы через rename; батчевые бэкфиллы ограничивают WAL-флуд и replica lag; advisory lock'и сериализуют раннеры, а forward-миграции — никогда не down-миграции — это production-откат.
</Recap>
