---
slug: 05-expand-contract
lang: ru
track: databases
unit: 06-migrations
order: 5
title: "Expand-contract: нулевой простой для ломающих изменений схемы"
summary: "Любое ломающее изменение — переименование, удаление, смена типа — разбивается на фазу expand (добавление) и фазу contract (удаление), каждая независимо деплоится и отменяется."
estMin: 14
status: ready
lessonType: topic
level: middle
prereqs: ["databases/06-migrations/04-safe-ddl-patterns"]
deepensInto: []
spiral: ["statefulness"]
concepts: ["expand-contract-migration", "dual-write", "backfill", "zero-downtime-deploy", "down-migration"]
sources:
  - https://www.postgresql.org/docs/current/sql-altertable.html
  - https://github.com/xataio/pgroll
  - https://xata.io/blog/migrations-and-exclusive-locks
---

import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import Misconception from "~/components/layout/Misconception.astro";
import NumbersCard from "~/components/layout/NumbersCard.astro";

<Hook>
Команда переименовывает `users.username` в `users.handle` одним запросом ALTER TABLE во время деплоя. Миграция коммитится мгновенно. Но rolling deploy ещё продолжается — старые поды всё ещё выполняют SQL, ссылающийся на `users.username`. Каждый запрос от старого пода завершается ошибкой &quot;column does not exist&quot;. Быстрая миграция вызвала реальный инцидент.
</Hook>

<Crux>Expand-contract держит суперсет схемы: старая и новая версии приложения работают одновременно — единственный способ zero-downtime.</Crux>

<Explanation>

## Почему rename/drop/type-change нельзя сделать за один шаг

**Rolling deploy** — единственная безопасная стратегия деплоя в любом масштабе — запускает старый и новый код одновременно от секунд до минут. Если миграция закоммичена до того, как все старые поды завершили работу:

- **Переименование**: старый код ссылается на старое имя колонки → ошибки `column does not exist`.
- **Удаление**: старый код ссылается на удалённую колонку → то же самое.
- **Смена типа**: старый код ожидает int, колонка теперь text → ошибки приведения типов.

Инвариант, который должен соблюдаться в каждый момент: **текущая схема одновременно совместима с предыдущей версией приложения и следующей**. Одношаговое переименование, удаление или смена типа нарушает этот инвариант.

## Общий фреймворк expand-contract

Каждое ломающее изменение разбивается на шесть фаз:

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Фаза</th>
        <th class="border border-border px-3 py-2 text-left">Действие в миграции</th>
        <th class="border border-border px-3 py-2 text-left">Действие в коде</th>
        <th class="border border-border px-3 py-2 text-left">Обе версии совместимы?</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">1. Expand</td>
        <td class="border border-border px-3 py-2">Добавить новый элемент рядом со старым</td>
        <td class="border border-border px-3 py-2">—</td>
        <td class="border border-border px-3 py-2">Да — старый код не затронут</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">2. Dual-write</td>
        <td class="border border-border px-3 py-2">—</td>
        <td class="border border-border px-3 py-2">Писать и в старую, и в новую</td>
        <td class="border border-border px-3 py-2">Да — старый читает старую, новый пишет обе</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">3. Backfill</td>
        <td class="border border-border px-3 py-2">Копировать исторические данные старая → новая пакетами</td>
        <td class="border border-border px-3 py-2">—</td>
        <td class="border border-border px-3 py-2">Да</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">4. Переключение чтений</td>
        <td class="border border-border px-3 py-2">—</td>
        <td class="border border-border px-3 py-2">Читать из новой, всё ещё писать обе</td>
        <td class="border border-border px-3 py-2">Да — полный откат ещё возможен</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">5. Остановка старых записей</td>
        <td class="border border-border px-3 py-2">—</td>
        <td class="border border-border px-3 py-2">Писать только в новую</td>
        <td class="border border-border px-3 py-2">Да — старый элемент всё ещё присутствует</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">6. Contract</td>
        <td class="border border-border px-3 py-2">Удалить старый элемент</td>
        <td class="border border-border px-3 py-2">—</td>
        <td class="border border-border px-3 py-2">Да — ни один код не ссылается на старый элемент</td>
      </tr>
    </tbody>
  </table>
</div>

## Пошаговый разбор переименования колонки: username → handle

Канонический пример. Общая длительность: 3–7 дней.

**День 1 — Expand:**
```sql
-- Миграция 1: мгновенно, без дефолта
ALTER TABLE users ADD COLUMN handle TEXT;
```

**День 1 — Деплой dual-write:**
Код приложения теперь пишет и `username`, и `handle` при каждом INSERT/UPDATE. Старый код всё ещё читает `username`. Новый код читает `username` (пока что).

**День 2 — Backfill:**
```sql
-- Вне транзакции миграции (пакетный цикл):
UPDATE users SET handle = username
WHERE handle IS NULL
  AND id BETWEEN :batch_start AND :batch_end;
-- Повторять до SELECT COUNT(*) FROM users WHERE handle IS NULL = 0
```

Пакеты по 1к–10к строк, каждый в короткой транзакции с `pg_sleep(0.1)` между ними.

**День 3 — Переключение чтений:**
Деплой кода, который читает только `handle`. Записи всё ещё идут в обе колонки. Мониторинг дашбордов 24 часа.

**День 5 — Остановка dual-write:**
Деплой кода, который пишет только в `handle`. Ожидание завершения rolling deploy.

**День 7 — Contract:**
```sql
-- Миграция 4: быстро (только метаданные)
ALTER TABLE users DROP COLUMN username;
```

Каждый шаг независимо деплоится и отменяется. Нулевой простой на всём пути.

## Down-миграции — не откат

Down-миграции (обратный SQL к up-миграции) есть во многих инструментах (файлы-партнёры `down.sql`). Они работают в разработке, но опасны в продакшне:

- Down-миграция, дропающая `handle`, уничтожает все данные, которые новый код в неё записал.
- Если up-миграция была `ADD COLUMN`, down — это `DROP COLUMN` — деструктивно.

**Откат в продакшне идёт вперёд**: напишите новую миграцию, исправляющую проблему, задеплойте её и двигайтесь вперёд. Рассматривайте down-миграции только как scaffolding для разработки.

<NumbersCard
  id="card-expand-contract-ru"
  title="Факты о деплое через expand-contract"
  stats={[
    { label: "Типичная длительность переименования колонки", value: "3–7 дней" },
    { label: "Количество деплоев при переименовании колонки", value: "5+ отдельных PR" },
    { label: "Размер пакета бэкфила", value: "1к–10к строк" },
    { label: "pg_sleep между пакетами", value: "0.1 с (пространство для дыхания)" },
    { label: "Каждая фаза: независимо откатываема?", value: "Да" },
    { label: "Направление отката в продакшне", value: "Вперёд (новая миграция)" },
  ]}
/>

<Misconception id="mc-down-migration-ru">
  <p><strong>&quot;Down-миграции — стратегия отката.&quot;</strong> Нет. Down-миграция, дропающая колонку, уничтожает данные, записанные новым кодом. Откат в продакшне идёт вперёд: новая миграция, исправляющая проблему. Down-миграции — только scaffolding для разработки.</p>
</Misconception>

<Inset kind="why" lang="ru">
pgroll (от Xata) автоматизирует expand-contract через Postgres-представления. При объявлении миграции, &quot;переименовывающей username в handle&quot;, pgroll создаёт представление v1 (раскрывает `username`) и представление v2 (раскрывает `handle`), оба поверх одной физической колонки, с триггерами для трансляции записей. Старые поды подключаются с `search_path = v1`; новые — с `search_path = v2`. По завершении миграции представление v1 дропается. Это сворачивает 5+ деплоев в одну декларативную миграцию — ценой сложности view-слоя при отладке и в выводе pg_dump.
</Inset>

<Quiz
  id="05-expand-contract-quiz-1"
  lessonSlug="05-expand-contract"
  lang="ru"
  question="Почему прямая миграция RENAME COLUMN может вызвать инцидент во время rolling deploy?"
  choices={[
    { label: "Rolling deploy запускает старый и новый код одновременно; старый код ссылается на старое имя колонки и падает в момент коммита переименования, до замены старых подов", correct: true },
    { label: "RENAME COLUMN берёт ACCESS EXCLUSIVE и блокирует всё во время переименования", misconception: "RENAME COLUMN быстрый (только метаданные). Проблема не в длительности блокировки, а в окне, где старый код работает против переименованной схемы." },
    { label: "Postgres не поддерживает переименование колонок на таблицах с активными подключениями", misconception: "Postgres позволяет RENAME COLUMN с активными подключениями; проблема — совместимость на уровне приложения в окне деплоя." },
    { label: "Кэш приложения всё ещё ссылается на старое имя колонки после переименования", misconception: "Кэши приложения могут быть проблемой, но основной сбой — живые SQL-ссылки на старое имя колонки от всё ещё работающих старых подов." },
  ]}
/>

<Quiz
  id="05-expand-contract-quiz-2"
  lessonSlug="05-expand-contract"
  lang="ru"
  question="На какой фазе expand-contract откат наиболее прост и почему?"
  choices={[
    { label: "Во время dual-write (фаза 2): обе колонки существуют и в них пишется; откат означает переключение чтений кода обратно на старую колонку — без потери данных", correct: true },
    { label: "После contract (фаза 6): обе колонки чисты", misconception: "После contract старый элемент исчез — откат потребовал бы его восстановления из бэкапа." },
    { label: "При backfill (фаза 3): данные копируются", misconception: "Откат при бэкфиле возможен, но требует остановки бэкфила и очистки частичных данных." },
    { label: "До expand (фаза 1): ничего не изменилось", misconception: "До expand никакого изменения не произошло — но это не фаза самого паттерна." },
  ]}
/>

<Quiz
  id="05-expand-contract-quiz-3"
  lessonSlug="05-expand-contract"
  lang="ru"
  question="Команда запускает down-миграцию в продакшне, которая дропает новую колонку, добавленную в up-миграции. Какие данные теряются?"
  choices={[
    { label: "Все данные, записанные в новую колонку новой версией приложения с момента выполнения up-миграции — потенциально часы или дни пользовательских данных", correct: true },
    { label: "Ничего — down-миграция не имеет эффекта, если в колонке нет данных", misconception: "Новый код приложения пишет в новую колонку с момента завершения up-миграции; эти данные дропаются." },
    { label: "Только определение колонки, но не данные", misconception: "DROP COLUMN удаляет и определение, и все хранимые значения этой колонки." },
    { label: "Ничего — Postgres откатывает down-миграцию, если она потеряет данные", misconception: "Postgres не защищает от дропа колонок с данными; он выполняет DROP COLUMN как указано." },
  ]}
/>

</Explanation>

<KeyTakeaway>Expand-contract разбивает каждое ломающее изменение на аддитивную фазу expand и фазу удаления contract, каждая независимо деплоится. Обе версии приложения — старая и новая — могут работать со схемой в каждый момент. Откат во время dual-write безопасен; запуск down-миграции в продакшне уничтожает данные.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="05-expand-contract-retrieval"
  lang="ru"
  questions={[
    { q: "Сформулируйте главный инвариант expand-contract и объясните, почему пропуск фазы dual-write его нарушает.", a: "Инвариант: в каждый момент деплоя текущая схема базы данных должна быть одновременно совместима с предыдущей версией приложения и следующей. Пропуск dual-write нарушает это, создавая окно, где схема поддерживает только новый код. Например, переход сразу от expand к переключению чтений означает, что старые поды (всё ещё в rolling deploy) пытаются писать в старую колонку — но новый код уже перестал в неё писать. Если старая колонка вот-вот будет дропнута в фазе contract, записи старых подов падают. Dual-write держит оба пути записи активными, поэтому обе версии кода совместимы." },
    { q: "Пройдите по шести фазам переименования: username → handle, назвав действие в миграции или коде на каждом шаге.", a: "(1) Expand: миграция — ADD COLUMN handle TEXT (мгновенно). (2) Dual-write: деплой кода — пишет и username, и handle при каждом INSERT/UPDATE. (3) Backfill: миграция (пакетная) — UPDATE users SET handle = username WHERE handle IS NULL, пакетами по 1к–10к строк. (4) Переключение чтений: деплой кода — читает исключительно handle, всё ещё пишет обе. (5) Остановка старых записей: деплой кода — пишет только handle. (6) Contract: миграция — DROP COLUMN username (быстро, метаданные). Каждый шаг — отдельный PR, ревьюируемый и мержимый независимо." },
    { q: "Почему down-миграции небезопасны в продакшне и какова правильная стратегия отката в продакшне?", a: "Down-миграции дропают или изменяют элементы схемы, в которые новый код приложения уже записал данные. Down-миграция, дропающая новую колонку, уничтожает все данные, которые новый код записал с момента выполнения up-миграции — потенциально часы пользовательских данных. Postgres не защищает от этого; он выполняет DROP COLUMN безусловно. Правильный откат в продакшне — это движение вперёд: написать новую миграцию, исправляющую проблему (повторно добавить колонку, восстановить дефолты, исправить constraints) и задеплоить её как прямую миграцию. Down-миграции полезны только в окружениях разработки, где потеря данных допустима." },
  ]}
/>

<Recap lang="ru">
Expand-contract — единственный правильный паттерн для миграций с нулевым простоем при переименовании, удалении или смене типа. Он поддерживает суперсет схемы в каждый момент: и старая, и новая версии кода работают одновременно со схемой, поддерживающей обе. Переименование колонки превращается в пять независимых деплоев за 3–7 дней: добавить новую колонку (expand), задеплоить код с dual-write, выполнить бэкфил исторических строк пакетами, задеплоить код с переключением чтений, задеплоить код с остановкой старых записей, затем дропнуть старую колонку (contract). Down-миграции кажутся безопасными, но уничтожают данные, уже записанные новым кодом; откат в продакшне — всегда прямая миграция, исправляющая проблему.
</Recap>
