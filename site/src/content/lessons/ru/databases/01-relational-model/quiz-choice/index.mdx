---
concepts:
- surrogate-vs-natural-key
- normalization-vs-denormalization
- jsonb-vs-side-table
- foreign-key-at-scale
- toast-storage
- expand-then-contract
estMin: 13
lang: ru
level: senior
order: 7
prereqs: []
slug: quiz-choice
sources:
- https://www.postgresql.org/docs/current/ddl-constraints.html
- https://www.postgresql.org/docs/current/datatype.html
- https://en.wikipedia.org/wiki/Relational_model
status: ready
summary: "Синтез с множественным выбором по юниту реляционной модели: keys, нормальные формы, JSONB vs side-таблица, foreign key at scale, TOAST и изменение схемы без простоя."
title: 'Реляционная модель: тест с множественным выбором'
track: databases
unit: 01-relational-model
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Шесть вопросов, проходящих сквозь весь юнит. Ни один не про заучивание определения — каждый это решение по дизайну схемы под реальными ограничениями, где неверный выбор оплачивается на миграции, а не на проектировании.
</Hook>

<Goal lang="ru">
Убедитесь, что вы связываете спину юнита: keys и constraints, нормальные формы vs намеренная денормализация, JSONB vs типизированные колонки vs side-таблицы, экономика foreign key на масштабе, физическое хранение и то, как схема меняется без простоя.
</Goal>

<Quiz
  id="01-relational-model-choice-1"
  lessonSlug="01-relational-model"
  lang="ru"
  question="В таблице customers email используется как PRIMARY KEY. Маркетинг теперь хочет, чтобы пользователи могли менять email. В чём корневая проблема и стандартное решение?"
  choices={[
    { label: "PK — это изменяемый natural key; смена PK каскадирует через каждый foreign key, который на него ссылается, что операционно дорого и часто невозможно на масштабе. Решение: surrogate PK (BIGSERIAL или UUIDv7) плюс UNIQUE NOT NULL на email.", correct: true },
    { label: "Email слишком длинный для эффективного индекса — перейти на хеш email как PK.", misconception: "Ширина индекса не проблема; TEXT email индексируется нормально. Проблема в изменяемости: меняющийся PK должен каскадировать через каждый ссылающийся FK. Хеш всё равно выводится из изменяемого email." },
    { label: "Ничего страшного — просто запустите UPDATE customers SET email = ..., и Postgres обновит ссылки автоматически.", misconception: "Postgres каскадирует смену PK только если каждый FK объявлен ON UPDATE CASCADE, и даже тогда переписывание затрагивает каждую дочернюю строку под блокировкой. Устойчивый ответ — никогда не класть изменяемое значение в PK." },
    { label: "Добавить второй UNIQUE constraint на email и оставить email как PK.", misconception: "Дублирующая гарантия уникальности не убирает проблему изменяемости — email всё ещё PK, поэтому его смена всё ещё каскадирует. Email нужно вывести из роли PK полностью." },
  ]}
/>

<Quiz
  id="01-relational-model-choice-2"
  lessonSlug="01-relational-model"
  lang="ru"
  question="Таблица хранит `orders(order_id, customer_id, customer_city)`, где primary key — только `order_id`. Какую нормальную форму она нарушает и что ломается в продакшене?"
  choices={[
    { label: "3NF — customer_city зависит транзитивно от customer_id, а не от order_id. Когда клиент переезжает, часть исторических заказов показывает старый город, часть — новый, так что две строки расходятся в одном и том же факте.", correct: true },
    { label: "2NF — customer_city частично зависит от составного ключа.", misconception: "Частичная зависимость 2NF применима только к составным primary key. Здесь order_id один — это PK, так что нарушать нечего." },
    { label: "1NF — customer_city содержит неатомарное значение.", misconception: "Название города — единственное атомарное значение. 1NF про повторяющиеся группы и многозначные ячейки, а не про транзитивные зависимости." },
    { label: "Никакую — кэширование города в заказе это валидная денормализация.", misconception: "Это валидная денормализация только если дубль согласуется чем-то (триггер, job) И намерение — снимок на момент времени. Здесь это несогласованная транзитивная зависимость — ровно тот режим отказа, что закрывает 3NF." },
  ]}
/>

<Quiz
  id="01-relational-model-choice-3"
  lessonSlug="01-relational-model"
  lang="ru"
  question="Маркетплейсу нужны свободные теги товаров, поддерживающие четыре запроса: показать теги товара, перечислить все товары с тегом X, посчитать товары по тегу и переименовать тег глобально. Какая форма хранения дёшево покрывает все четыре на масштабе?"
  choices={[
    { label: "Side-таблица: каталог tags (id, name UNIQUE) плюс join-таблица product_tags с составным PK (product_id, tag_id), FK и индексом на tag_id. Она запрашивается с обеих сторон, агрегируется по тегу и переименовывает одним UPDATE.", correct: true },
    { label: "Колонка-массив TEXT[] на products с GIN-индексом.", misconception: "Массив тянет «теги этого товара» и (через GIN) «товары с тегом X», но агрегация по тегу дорога, а глобальное переименование значит переписать каждую строку, содержащую тег. Два из четырёх запросов проваливаются на масштабе." },
    { label: "Колонка JSONB со списком тегов, индексированная через GIN.", misconception: "JSONB здесь строго хуже массива — выше накладные на парсинг, нет уникальности или FK на канонический список тегов — и всё равно не может дёшево посчитать по тегу или переименовать глобально." },
    { label: "Денормализовать имена тегов в каждый заказ, ссылающийся на товар, чтобы отчёты были быстрыми.", misconception: "Это дублирует факт тега по заказам без согласования, воссоздавая проблему расхождения 3NF, и всё равно не даёт ни счёта по тегу, ни переименования одной строкой." },
  ]}
/>

<Quiz
  id="01-relational-model-choice-4"
  lessonSlug="01-relational-model"
  lang="ru"
  question="Senior-инженер предлагает отключить foreign keys по всей типичной B2B SaaS-схеме (таблицы до 100M строк), чтобы «улучшить производительность записи». Как читать это предложение?"
  choices={[
    { label: "Отклонить — отключение FK оправдано лишь при специфичных условиях (связь пересекает границы шардов, многомиллионный каскад держит блокировки, валидация FK блокирует online DDL). Здесь ничего из этого нет; проверка ~5-50 мкс/строку навсегда предотвращает orphan-строки, а отказ от неё толкает integrity в непоследовательный код приложения.", correct: true },
    { label: "Принять — FK известное крупное узкое место записи, и его удаление стандартная практика ради производительности.", misconception: "Проверка FK ~5-50 мкс на строку, пренебрежимо для SaaS до 100M строк. «Hyperscale-конторы отключают FK» — это масштабное исключение, а не общая best practice." },
    { label: "Принять — код приложения обеспечивает ту же integrity, так что FK избыточен.", misconception: "Enforcement на уровне приложения реализуется непоследовательно и ломается при рефакторингах и между сервисами. FK — единственная граница, которую каждый вызывающий получает бесплатно." },
    { label: "Принять только если добавить индекс на каждую FK-колонку для компенсации.", misconception: "Индекс на FK-колонке помогает производительности запросов, а не стоимости проверки FK, и не отвечает на реальный вопрос, оправдано ли отключение constraint на этом масштабе. Не оправдано." },
  ]}
/>

<Quiz
  id="01-relational-model-choice-5"
  lessonSlug="01-relational-model"
  lang="ru"
  question="`SELECT * FROM events ORDER BY created_at DESC LIMIT 100` на таблице в 50M строк с индексом на created_at в 3× медленнее ожидаемого. В таблице JSONB-колонка payload в среднем 8KB. Наиболее вероятная причина и исправление?"
  choices={[
    { label: "Каждая из 100 возвращаемых строк запускает fetch из TOAST-таблицы для широкого payload, потому что SELECT * читает каждую TOAST-колонку. Исправление: выбирать только нужные колонки (id, event_type, created_at), чтобы payload никогда не доставался.", correct: true },
    { label: "ORDER BY ... DESC побеждает B-tree индекс, форсируя sequential scan.", misconception: "B-tree на created_at поддерживает обратный index scan для ORDER BY ... DESC LIMIT 100; скан дёшев. Стоимость — это TOAST-fetch на строку от SELECT *." },
    { label: "Таблица в 50M строк всегда делает sequential scan независимо от индекса.", misconception: "Селективный индекс, возвращающий 100 строк, использует index scan на таблице любого размера. Выбор планировщика определяет селективность, а не число строк." },
    { label: "JSONB-колонки нельзя возвращать в ORDER BY-запросе, поэтому Postgres откатывается на медленный путь.", misconception: "JSONB можно возвращать в любом запросе. Особого медленного пути нет; лишняя стоимость — доставание TOAST-payload для каждой строки." },
  ]}
/>

<Quiz
  id="01-relational-model-choice-6"
  lessonSlug="01-relational-model"
  lang="ru"
  question="Колонку `user_name` нужно превратить в `display_name` на таблице в 50M строк, общей для многих сервисов, без простоя. Какой подход верный и почему однострочный rename неверен?"
  choices={[
    { label: "Expand-then-contract: добавить display_name nullable, развернуть dual-write, бэкфилл батчами, переключить чтения, проверить parity, затем удалить user_name отдельной миграцией. Один RENAME COLUMN мгновенно сломал бы каждого читателя (код приложения, ORM-модели, view, дашборды), всё ещё ссылающегося на user_name.", correct: true },
    { label: "Запустить ALTER TABLE RENAME COLUMN — это мгновенное изменение только метаданных без переписывания, поэтому всегда безопасно.", misconception: "Rename — это только метаданные и мгновенно, но он атомарно ломает каждого потребителя, всё ещё запрашивающего user_name. На общей схеме с многими сервисами, которые не могут развернуться одновременно, это немедленный сбой." },
    { label: "Добавить обновляемый VIEW, алиасящий user_name на display_name, и считать готово.", misconception: "View облегчает чтения, но записи через него требуют INSTEAD OF триггеров или правил обновляемого view, и это лишь переходный мост — не замена полной миграции expand-then-contract." },
    { label: "Взять 2-часовое окно обслуживания и переименовать на месте.", misconception: "Простой может быть приемлем для внутреннего инструмента, но задача требует zero downtime, и окно всё равно заставляет всех потребителей развернуть обновлённый код одновременно. Expand-then-contract — это ответ без простоя." },
  ]}
/>

<Recap lang="ru">
Сквозная линия юнита — одна дисциплина решений: кладите identity в неизменяемый surrogate key и охраняйте business keys через UNIQUE NOT NULL; нормализуйте до 3NF, чтобы две строки не могли разойтись, и денормализуйте только с явным согласованием; тянитесь к side-таблице в момент, когда запрашиваете с другой стороны; держите FK-constraints, пока измеренное ограничение шардинга или каскада не вынудит иначе; помните, что физическое хранение (TOAST, page layout) делает SELECT * дорогим на широких строках; и меняйте живую схему через expand-then-contract, никогда не ломая на месте. Каждый ответ здесь сводится к «проектируй под запросы и жизненный цикл, а не под первый insert».
</Recap>
