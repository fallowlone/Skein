---
slug: 07-relational-vs-alternatives
lang: ru
track: databases
unit: 01-relational-model
order: 7
title: "Реляционная модель vs документные, wide-column, граф и key-value"
summary: "Точные условия победы каждой модели данных, почему Postgres покрывает больше, чем подразумевает ярлык «реляционный», различия pgsql vs MySQL, важные для проектирования схемы, и разбор решения для мультитенантной схемы."
estMin: 16
status: ready
lessonType: topic
level: senior
prereqs: ["06-schema-integrity"]
deepensInto: []
spiral: ["statefulness"]
concepts: ["document-store", "wide-column", "graph-database", "key-value", "polyglot-persistence", "Postgres-extensions", "pgsql-vs-mysql", "RLS", "multi-tenant-schema"]
sources:
  - https://www.postgresql.org/docs/current/ddl-rowsecurity.html
  - https://www.postgresql.org/docs/current/extend.html
  - https://en.wikipedia.org/wiki/Relational_model
  - https://www.postgresql.org/docs/current/ddl-schemas.html
---

import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";

<Hook>
Стартап принял MongoDB, потому что «реляционные схемы жёсткие». Три года спустя ad-hoc джойны невозможны, целостность данных обеспечивается приложением (непоследовательно), и команда переезжает на Postgres. Жёсткость, которую они хотели избежать, была дисциплиной, которая их бы спасла.
</Hook>

<Crux>Реляционная модель побеждает при повторяющихся структурах и ad-hoc запросах. Альтернативы выигрывают только в узкой, точно названной форме.</Crux>

<Explanation>

## Пространство компромиссов

Реляционная модель — одна из нескольких моделей данных. Опытные инженеры знают, где побеждает каждая альтернатива и где «просто использовать Postgres» превосходит «выбрать специализированный инструмент».

**Документные хранилища (MongoDB, Firestore, DynamoDB).** Побеждают, когда документы доступны как единицы и никогда не джойнятся. Один документ содержит все данные одной сущности (продукт с вложенными вариантами, медиа и атрибутами). Кросс-документная целостность не нужна. Паттерн запросов — «получить этот документ по ключу» или «фильтр по верхнеуровневым полям». Проигрывают при ad-hoc запросах, операциях эквивалентных JOIN или ссылочной целостности между документами.

**Wide-column хранилища (Cassandra, ScyllaDB).** Побеждают при очень высокой пропускной способности записи (~100k+ записей/секунду) с заранее определёнными паттернами запросов, не меняющимися после проектирования схемы. Первичный ключ Cassandra включает partition key (как данные распределены) и clustering key (как данные сортируются внутри партиции) — запросы, совпадающие с partition key, быстрые; любой другой запрос — полный скан. Проигрывают при эволюции паттернов запросов или необходимости агрегации.

**Граф-базы данных (Neo4j, Dgraph, Amazon Neptune).** Побеждают, когда обход графа — доминирующий запрос: «найти всех друзей друзей в 3 переходах», «обнаружить циклические платёжные пути», «найти кратчайший путь в knowledge graph». Проигрывают во всём остальном. Рекурсивные CTE в SQL могут заменить многие граф-запросы на малом масштабе; на большом масштабе или при обходе как основной нагрузке нативная граф-БД дешевле.

**Key-value хранилища (Redis, DynamoDB в режиме KV).** Побеждают для чистых точечных поисков по известному ключу — хранение сессий, кеши, флаги фич, кратковременные счётчики. Проигрывают при любых диапазонных запросах, агрегации или обходе связей.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Модель</th>
        <th class="border border-border px-3 py-2 text-left">Побеждает когда</th>
        <th class="border border-border px-3 py-2 text-left">Проигрывает когда</th>
        <th class="border border-border px-3 py-2 text-left">Примеры</th>
      </tr>
    </thead>
    <tbody>
      <tr><td class="border border-border px-3 py-2">Реляционная</td><td class="border border-border px-3 py-2">Повторяющаяся схема, ad-hoc запросы, кросс-типовая целостность</td><td class="border border-border px-3 py-2">Чистый документный доступ, only-known-key нагрузки при экстремальном масштабе</td><td class="border border-border px-3 py-2">Postgres, MySQL</td></tr>
      <tr class="bg-surface-2"><td class="border border-border px-3 py-2">Документная</td><td class="border border-border px-3 py-2">Гетерогенная схема per-row, документный unit-доступ</td><td class="border border-border px-3 py-2">Ad-hoc запросы, целостность между документами</td><td class="border border-border px-3 py-2">MongoDB, Firestore</td></tr>
      <tr><td class="border border-border px-3 py-2">Wide-column</td><td class="border border-border px-3 py-2">Очень высокая пропускная запись, фиксированные паттерны запросов</td><td class="border border-border px-3 py-2">Эволюция запросов, агрегация</td><td class="border border-border px-3 py-2">Cassandra, ScyllaDB</td></tr>
      <tr class="bg-surface-2"><td class="border border-border px-3 py-2">Граф</td><td class="border border-border px-3 py-2">Глубокий обход как основной запрос</td><td class="border border-border px-3 py-2">Всё остальное</td><td class="border border-border px-3 py-2">Neo4j, Amazon Neptune</td></tr>
      <tr><td class="border border-border px-3 py-2">Key-value</td><td class="border border-border px-3 py-2">Чистый точечный поиск по известному ключу</td><td class="border border-border px-3 py-2">Диапазонные запросы, агрегация, обход связей</td><td class="border border-border px-3 py-2">Redis, DynamoDB (KV mode)</td></tr>
    </tbody>
  </table>
</div>

## Postgres — не только реляционная БД

Самоописание Postgres — «самая продвинутая реляционная СУБД с открытым исходным кодом в мире». На практике экосистема расширений покрывает большинство нагрузок, которые можно было бы адресовать специализированному хранилищу:

- **JSONB + GIN-индексы** — документоподобные запросы без документного хранилища.
- **pg_vector** — поиск векторного сходства (ближайших соседей для AI-эмбеддингов), заменяет специализированные векторные базы данных для большинства нагрузок.
- **PostGIS** — полноценный геопространственный движок (пространственные индексы, гео-операторы), заменяет специализированные geo-хранилища.
- **TimescaleDB** — партиционирование и сжатие временных рядов, заменяет InfluxDB для многих нагрузок.
- **pgcrypto** — шифрование, хэширование, UUID из криптографического источника.
- **Recursive CTE** — ограниченный обход графа без граф-базы данных.

Вопрос «нужно ли мне специализированное хранилище?» становится: «превышает ли моя нагрузка то, что может сделать Postgres + нужное расширение?» Для большинства команд до ~10 ТБ и без требований экстремальной пропускной записи ответ — нет.

## Postgres vs MySQL: дефолты проектирования схемы, имеющие значение

Обе реляционные; обе реализуют большую часть SQL:2011. Различия, важные при проектировании схемы:

**Поведение NULL в UNIQUE constraints.** Стандарт SQL (и Postgres) допускает несколько NULL в UNIQUE-колонке (NULL не равен ничему, включая другой NULL). InnoDB MySQL также допускает несколько NULL. Но MySQL исторически (до 8.0) имел другое поведение NULL в некоторых операциях. Используйте Postgres `UNIQUE NULLS NOT DISTINCT` (SQL:2023), если хотите предотвратить дублирующиеся NULL.

**Неявное приведение типов.** MySQL более снисходителен — молча преобразует `'42'` в 42, усекает строки, превышающие длину VARCHAR, вместо ошибки. Postgres строгий — неправильные типы дают явную ошибку. Поведение Postgres выявляет несоответствия схемы раньше.

**Дефолтный движок хранения.** MySQL по умолчанию использует InnoDB; старый тулинг иногда использует MyISAM (нет транзакций, нет поддержки FK). В 2026 году все продакшн-использования MySQL должны быть на InnoDB. У Postgres один движок хранения без ловушки.

**Экосистема.** Postgres: PostGIS, TimescaleDB, pg_vector, pgcrypto, citus, pgroll. Экосистема MySQL: Vitess (горизонтальное масштабирование), PlanetScale (MySQL-as-a-service), Aurora MySQL (AWS). Выбирайте MySQL, если глубоко в экосистеме горизонтального масштабирования MySQL (Vitess, PlanetScale, Aurora MySQL); иначе Postgres для нового проекта.

## Мультитенантное проектирование схемы: разбор решения

Самое сложное решение схемы для B2B SaaS — изоляция тенантов. Три паттерна:

**Schema-per-tenant.** Каждый workspace получает собственную Postgres-схему. Полная DDL-изоляция. Миграции выполняются per-schema (n схем × 1 миграция = n операций). Работает до ~1000 тенантов; выше этого накладные расходы на соединения и сложность миграций растут линейно. Переключение тенанта чистое (SET search_path).

**Database-per-tenant.** Максимальная изоляция. Запретительно сложно выше ~100 тенантов. Подходит для enterprise SaaS с требованиями изоляции по контракту.

**Изоляция на уровне строк (колонка workspace_id + RLS).** Одна схема, все тенанты в тех же таблицах. Каждая тенантная таблица имеет `workspace_id BIGINT NOT NULL`. Политики Postgres Row-Level Security (RLS) фильтруют строки на основе session-level GUC:

```sql
ALTER TABLE projects ENABLE ROW LEVEL SECURITY;

CREATE POLICY projects_tenant_isolation ON projects
  USING (workspace_id = current_setting('app.current_workspace_id')::BIGINT);
```

Соединение приложения устанавливает `SET app.current_workspace_id = X` после аутентификации. RLS гарантирует, что каждый запрос на этом соединении видит только строки workspace X. Пропущенный WHERE в коде приложения поймает RLS.

Эшелонированная защита: RLS + CI-линтер, требующий `workspace_id` в каждом WHERE. Линтер ловит баги до продакшна; RLS — страховочная сеть.

<Inset kind="why" lang="ru">
Зачем добавлять CI-линтер, если RLS уже применяет изоляцию? RLS — гарантия, а не сообщение об ошибке. Запрос, нарушающий RLS, молча возвращает ноль строк (или выдаёт ошибку, если настроен как RESTRICTIVE). Разработчик видит «нет результатов» и предполагает, что данных нет. CI-линтер проваливает сборку, когда запрос обращается к тенантной таблице без workspace_id, давая actionable ошибку до того, как запрос попадёт в продакшн.
</Inset>

<TradeoffMatrix
  id="07-relational-vs-alternatives-tradeoff-1"
  lessonSlug="07-relational-vs-alternatives"
  lang="ru"
  prompt="Новая социальная фича нуждается в 'найти всех друзей друзей в 2 переходах' для движка рекомендаций. В таблице users 10M строк и 50M follow-связей. Выберите подход к запросу."
  options={[
    { name: "Postgres recursive CTE по таблице follows", summary: "Работает при таком масштабе; не требует новой инфраструктуры.", correct: true, justification: "Recursive CTE по таблице follows на 50M строк с составным индексом на (follower_id, followed_id) может отвечать на друзей-в-2-переходах для одного пользователя за ~50-200 мс при хорошем дизайне индексов. При 10M пользователях и 50M follows это в пределах возможностей Postgres. Никакой новой инфраструктуры. Дисциплина: добавить покрывающий индекс на (followed_id, follower_id), установить ограничение глубины переходов в CTE для предотвращения неограниченных запросов." },
    { name: "Материализовать друзей-друзей в предвычисленную таблицу, обновляемую на каждом follow/unfollow", summary: "Быстрые чтения, дорогие записи.", justification: "Предвычисление — правильный ответ при масштабе (100M+ связей, требование менее 10ms). При 50M follows стоимость обновления на каждое follow-событие высока — каждый новый follow потенциально требует пересчёта тысяч 2-hop путей. Корректно для eventual-consistency движков рекомендаций, где предвычисленная таблица асинхронно обновляется." },
    { name: "Перевести граф follow в Neo4j", summary: "Правильный инструмент, преждевременные затраты.", justification: "Neo4j был бы быстрее для глубоких обходов при масштабе. Но введение второй базы данных для одного запроса добавляет операционные накладные расходы (отдельный кластер, отдельные бэкапы, логика синхронизации с Postgres), дорого и преждевременно при 50M связях. Мигрируйте в граф-базу данных, когда recursive CTE измерена как узкое место и более простые подходы не справляются." },
    { name: "Хранить follow-связи как JSONB-массивы в строке пользователя", summary: "Совершенно неправильная форма.", justification: "При 50M follows усреднённо по 10M пользователей — это ~5 follows на пользователя, но степенной закон означает, что у некоторых пользователей миллионы. JSONB-массив в строке пользователя ломается для аккаунтов с большим числом подписчиков, не может быть индексирован для обратного поиска и не может отвечать на 2-hop запросы без загрузки списка follow каждого пользователя в память." },
  ]}
/>

<Quiz
  id="07-relational-vs-alternatives-quiz-1"
  lessonSlug="07-relational-vs-alternatives"
  lang="ru"
  question="Команда из 200 B2B клиентов рассматривает изоляцию schema-per-tenant. При каком масштабе этот паттерн становится операционно дорогим и почему?"
  choices={[
    { label: "Выше ~1000 тенантов, потому что последовательный запуск миграций через 1000+ схем и управление per-schema пулами соединений становится значительным операционным узким местом", correct: true },
    { label: "Выше 10 тенантов, потому что Postgres не может иметь несколько схем", misconception: "Postgres поддерживает тысячи схем на базу данных. Ограничение — операционная сложность, а не жёсткое техническое." },
    { label: "Schema-per-tenant никогда не становится дорогим — масштабируется линейно", misconception: "Миграции масштабируются линейно (n схем × 1 шаг миграции = n операций). При 10 000 тенантов миграция на 1 секунду на схему займёт ~3 часа." },
    { label: "Выше 100 тенантов, потому что Postgres заканчивает слоты соединений", misconception: "Исчерпание пула соединений — отдельная проблема конфигурации. Узкое место schema-per-tenant — сложность миграций и накладные расходы на метаданные схем, а не слоты соединений." },
  ]}
/>

<Quiz
  id="07-relational-vs-alternatives-quiz-2"
  lessonSlug="07-relational-vs-alternatives"
  lang="ru"
  question="Postgres Row-Level Security включена, политика ограничивает строки `workspace_id = current_setting('app.current_workspace_id')`. Запрос приложения случайно опускает WHERE workspace_id. Что происходит?"
  choices={[
    { label: "Запрос выполняется, но возвращает только строки текущего workspace — RLS-политика молча фильтрует результат", correct: true },
    { label: "Запрос выдаёт ошибку: 'workspace_id clause required'", misconception: "RLS не генерирует ошибки для отсутствующих WHERE — она молча фильтрует строки. Разработчик видит пустой (или workspace-фильтрованный) результат и может ошибочно диагностировать проблему с данными." },
    { label: "Запрос возвращает все строки из всех workspace", misconception: "Если RLS включена и на роли не установлена опция BYPASSRLS, политика применяется к каждому запросу на таблице независимо от логики приложения." },
    { label: "Запрос блокируется на уровне соединения с базой данных до выполнения", misconception: "RLS работает на уровне фильтра строк во время выполнения запроса, а не на уровне соединения до выполнения запроса." },
  ]}
/>

</Explanation>

<KeyTakeaway>Реляционная модель побеждает при повторяющейся структуре данных, ad-hoc запросах и важности целостности. Каждая альтернатива имеет конкретную форму победы — назовите её точно перед принятием. Postgres + расширения (pg_vector, PostGIS, TimescaleDB, JSONB) покрывает большинство специализированных нагрузок. Для мультитенантного SaaS: изоляция на уровне строк с RLS + workspace_id — по умолчанию ниже ~1000 тенантов; schema-per-tenant выше; database-per-tenant только для контрактных требований изоляции.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="07-relational-vs-alternatives-retrieval"
  lang="ru"
  questions={[
    { q: "Сформулируйте, почему реляционная модель побеждает документные, wide-column, граф и key-value альтернативы для большинства B2B SaaS нагрузок, и дайте одно конкретное условие победы для каждой альтернативы.", a: "Реляционная модель побеждает, потому что данные B2B SaaS имеют повторяющуюся структуру (каждый пользователь, заказ, проект разделяет фиксированную форму), запросы ad-hoc (новые бизнес-вопросы возникают постоянно после написания схемы) и ссылочная целостность важна между типами (пользователь, заказ и платёж должны быть согласованы). Каждая альтернатива побеждает в узкой форме: документные хранилища побеждают, когда каждый документ доступен как единица и никогда не джойнится. Wide-column побеждает при экстремальной пропускной записи (>100k записей/секунду) с заранее определёнными паттернами запросов. Граф побеждает, когда обход связей — основной запрос (друзья друзей, обнаружение мошеннических циклов). Key-value побеждает для чистых точечных поисков по известному ключу (хранение сессий, кеши)." },
    { q: "Каковы три паттерна мультитенантной изоляции для Postgres и когда применяется каждый?", a: "Schema-per-tenant: каждый тенант получает собственную Postgres-схему. Полная DDL-изоляция. Работает до ~1000 тенантов; выше этого сложность миграции (n схем × 1 миграция) и накладные расходы на соединения растут линейно. Database-per-tenant: максимальная изоляция; правильна для enterprise SaaS, где тенанты требуют отдельных баз данных по контракту. Запретительно сложно выше ~100 тенантов. Изоляция на уровне строк: все тенанты делят таблицы; каждая строка имеет workspace_id NOT NULL; RLS-политики Postgres фильтруют строки по session GUC (SET app.current_workspace_id = X). Эшелонированная защита с CI-линтером, требующим workspace_id в каждом WHERE. Это по умолчанию для SaaS-приложений ниже ~1000 тенантов." },
    { q: "Назовите два различия в дефолтах проектирования схемы между Postgres и MySQL.", a: "Неявное приведение типов: MySQL молча приводит '42' к integer и усекает строки, превышающие VARCHAR(n). Postgres явно сигнализирует об ошибках типов. Postgres выявляет несоответствия схемы при разработке; MySQL скрывает их до проявления в виде багов качества данных в продакшне. Экосистемная привязка: экосистема горизонтального масштабирования MySQL (Vitess, PlanetScale, Aurora MySQL) отличается от экосистемы расширений Postgres (PostGIS, TimescaleDB, pg_vector, pgcrypto, citus). Выбор MySQL для нового проекта означает принятие обязательств перед этой экосистемой. Выбирайте Postgres для нового проекта; переходите на MySQL только при наличии специфической операционной экспертизы или требований Vitess-grade горизонтального шардинга." },
  ]}
/>

<Recap lang="ru">
Реляционная модель побеждает для нагрузок с повторяющейся структурой, ad-hoc запросами и критически важной целостностью — большинство B2B SaaS, fintech, операционных инструментов. Документные хранилища побеждают для документного unit-доступа без кросс-документной целостности. Wide-column побеждает при экстремальной пропускной записи с фиксированными запросами. Граф побеждает, когда обход — основной запрос. Key-value побеждает для чистых точечных поисков. Postgres с расширениями (JSONB, pg_vector, PostGIS, TimescaleDB) покрывает большинство специализированных потребностей в одном движке. Для мультитенантного SaaS: изоляция на уровне строк с RLS — по умолчанию; schema-per-tenant для контрактной изоляции до ~1000 тенантов; database-per-tenant для enterprise контрактов. Postgres vs MySQL: Postgres строже (ошибки типов, NULL-семантика) и имеет богатую экосистему расширений; выбирайте MySQL только для специфических потребностей экосистемы или шардинга.
</Recap>
