---
concepts:
- 1Nf
- 2NF
- 3NF
- BCNF
- normalization
- denormalization
- stored-aggregate
- wide-read-model
- materialised-path
- ORM-N+1
- schema-convention
deepensInto:
- 04-jsonb-and-arrays
estMin: 16
lang: ru
lessonType: topic
level: middle
order: 3
prereqs:
- 02-constraints-and-keys
slug: 03-normalization
sources:
- https://www.postgresql.org/docs/current/ddl-constraints.html
- https://en.wikipedia.org/wiki/Boyce%E2%80%93Codd_normal_form
- https://www.postgresql.org/docs/current/datatype.html
spiral:
- statefulness
status: ready
summary: 1NF–BCNF простыми словами, когда намеренно денормализовать, ORM-ловушки,
  незаметно ломающие дисциплину схемы, и соглашения об именовании, которые окупаются
  годами.
title: Нормальные формы, денормализация и почему схемы «прилипают»
track: databases
unit: 01-relational-model
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";

<Hook>
Младший инженер хранит город клиента в каждой строке заказа — «удобно для отчётов». Через два года клиент переезжает. Теперь половина исторических заказов показывает старый город, половина — новый. Один и тот же факт живёт в двух местах и они расходятся. Нормализация — дисциплина, которая предотвращает это.
</Hook>

<Crux>Нормализация устраняет дублирующиеся факты, чтобы строки не расходились. Денормализуйте только там, где контролируете согласование.</Crux>

<Explanation>

## Нормальные формы простыми словами

Нормализация — серия прогрессивно строгих правил разбиения таблиц, чтобы ни один факт не хранился дважды.

**1NF — атомарные ячейки.** Каждая ячейка должна содержать одно значение. Никаких списков через запятую внутри колонки, никаких повторяющихся групп, никаких массивов под видом колонок (`tag1`, `tag2`, `tag3`). Если ячейку можно распарсить через `split(',')`, таблица нарушает 1NF.

**2NF — полная ключевая зависимость.** Каждая неключевая колонка должна зависеть от *всего* primary key, а не от его части. Имеет значение только для составных primary keys. Пример нарушения: таблица `order_items(order_id, product_id, product_name)` — `product_name` зависит только от `product_id`, а не от составного ключа. Решение: перенести `product_name` в таблицу products.

**3NF — нет транзитивных зависимостей.** Каждая неключевая колонка должна зависеть *только* от primary key, а не от другой неключевой колонки. Пример нарушения: `orders(order_id, customer_id, customer_city)` — `customer_city` зависит от `customer_id`, а не от `order_id`. Решение: перенести `customer_city` в таблицу customers (или addresses).

**BCNF — каждый детерминант является candidate key.** Строже 3NF; имеет значение когда таблица имеет несколько перекрывающихся candidate keys. Редко в продакшне; часто в академических примерах. В практике цельтесь в 3NF; переходите к BCNF только когда можете назвать конкретный сценарий перекрывающихся candidate keys.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Нормальная форма</th>
        <th class="border border-border px-3 py-2 text-left">Правило</th>
        <th class="border border-border px-3 py-2 text-left">Форма нарушения</th>
        <th class="border border-border px-3 py-2 text-left">Решение</th>
      </tr>
    </thead>
    <tbody>
      <tr><td class="border border-border px-3 py-2">1NF</td><td class="border border-border px-3 py-2">Атомарные ячейки, нет повторяющихся групп</td><td class="border border-border px-3 py-2">Список через запятую в одной колонке</td><td class="border border-border px-3 py-2">Отдельная таблица или типизированный массив</td></tr>
      <tr class="bg-surface-2"><td class="border border-border px-3 py-2">2NF</td><td class="border border-border px-3 py-2">Полная ключевая зависимость</td><td class="border border-border px-3 py-2">Неключевая колонка зависит от части составного PK</td><td class="border border-border px-3 py-2">Перенести в таблицу-владелец</td></tr>
      <tr><td class="border border-border px-3 py-2">3NF</td><td class="border border-border px-3 py-2">Нет транзитивных зависимостей</td><td class="border border-border px-3 py-2">Неключевая колонка зависит от другой неключевой</td><td class="border border-border px-3 py-2">Вынести в отдельную таблицу</td></tr>
      <tr class="bg-surface-2"><td class="border border-border px-3 py-2">BCNF</td><td class="border border-border px-3 py-2">Каждый детерминант — candidate key</td><td class="border border-border px-3 py-2">Несколько перекрывающихся candidate keys</td><td class="border border-border px-3 py-2">Декомпозиция; редко на практике</td></tr>
    </tbody>
  </table>
</div>

Суть — не в следовании правилам, а в устранении сценария, когда две строки расходятся об одном факте. Если можно обновить email Анны в трёх таблицах, две из них будут устаревшими через год.

## Денормализация: когда и зачем

Нормализация оптимизирует корректность при записи. При чтении она может быть дорогостоящей, потому что ответ на вопрос требует соединения многих таблиц. Денормализация намеренно дублирует данные для избежания JOIN на горячих путях чтения.

**Сохранённые агрегаты.** Колонка `order_total_cents` в orders, кешируещая `SUM(order_items.unit_price_cents * quantity)`. Обновляется триггером или кодом приложения при каждой записи позиции. Отчётность обращается к одной колонке вместо агрегирования миллионов строк.

**Широкие read-модели.** Таблица `user_profile`, объединяющая имя, email, текущий адрес, количество последних заказов — пополняемая триггерами или кодом из нормализованных источников. Запросы дашборда обращаются к одной строке.

**Материализованные пути.** Хранение полного пути предков узла дерева как текстовой колонки (`/root/section/subsection/`), чтобы избежать рекурсивных JOIN при чтении иерархий. Классический паттерн для деревьев комментариев или оргструктур.

Дисциплина: денормализуйте на стороне чтения, сохраняйте авторитетные факты нормализованными, и признайте, что дубликаты должны согласовываться чем-то: триггером, плановым заданием, кодом приложения. Сценарий отказа — денормализация везде без плана согласования. Каждая колонка в итоге расходится с каждой другой.

<Inset kind="why" lang="ru">
Почему 3NF, а не 4NF, 5NF или DKNF? Нормальные формы выше 3NF адресуют многозначные зависимости и зависимости соединений, которые встречаются в крайне академических схемах. Реальные продакшн-таблицы почти никогда не имеют таких форм. 3NF — точка убывающей отдачи: за ней вы разбиваете таблицы, которые не нужно разбивать, добавляете JOIN, которые не улучшают корректность, и замедляете чтение для схем, у которых не было проблем с integrity.
</Inset>

## ORM-ловушки, незаметно ломающие дисциплину схемы

ORM абстрагируют SQL, но могут вносить ошибки на уровне схемы.

**N+1 запросы.** ORM загружает список 100 заказов, затем для каждого заказа отдельно получает клиента. Результат: 101 запрос там, где справился бы 1 JOIN. Во многих ORM это поведение по умолчанию, если не объявить eager-load (`include`, `with`, `select_related`). Импликация для схемы: каждая FK-связь должна быть аннотирована правильной стратегией загрузки по умолчанию; оставить это на неявную lazy-load — значит накапливать невидимые N+1.

**Неявная генерация схемы.** Многие ORM могут генерировать схему из определения модели. Сгенерированная схема часто не содержит: CHECK ограничений, составных индексов, индекса на FK-колонке, именованных ограничений. Результат — синтаксически корректная схема, нарушающая дисциплину этого урока. Воспринимайте ORM-сгенерированные схемы как отправную точку, а не финальный артефакт; добавляйте ограничения и индексы вручную или через файлы миграций.

**Отсутствие обновления `updated_at`.** ORM часто автоматически управляют `created_at`, но не `updated_at`. Отсутствие `updated_at` делает инкрементальную репликацию и аудит невозможными. Принудительно соблюдайте через триггер (`BEFORE UPDATE ... SET updated_at = now()`) или lifecycle hook ORM; никогда не доверяйте приложению помнить об этом.

## Соглашения об именовании, которые окупаются

Они кажутся педантичными; они окупаются в каждом дашборде, каждом скрипте миграции, каждом кросс-командном запросе.

- **Множественное число для таблиц** (`users`, `orders`, `order_items`) — единственное число тоже допустимо; выберите один вариант и соблюдайте везде.
- **snake_case** — Postgres приводит неэкранированные идентификаторы к нижнему регистру; snake_case — продакшн-дефолт.
- **`id` как имя primary key** в каждой таблице; foreign keys называются `<referenced_table_singular>_id` (например, `user_id`, `order_id`).
- **`created_at`, `updated_at`, `deleted_at`** для временных колонок. `deleted_at IS NOT NULL` — паттерн мягкого удаления.
- **Булевы колонки** с префиксом `is_`, `has_`, `can_` или как понятное свойство (`active`, не `flag`).
- **Именование CHECK ограничений** — именуйте каждое: `CONSTRAINT chk_orders_status CHECK (status IN (...))`. Безымянные ограничения выдают непонятные сообщения об ошибках.

Зрелые платформы CI-линтят эти соглашения, чтобы они соблюдались командами, а не запоминались отдельными людьми.

<DragOrder
  id="03-normalization-drag-1"
  lessonSlug="03-normalization"
  lang="ru"
  prompt="Упорядочьте эти действия по эволюции схемы от самых дешёвых к самым дорогим:"
  items={[
    { id: "a", label: "Добавить CHECK ограничение на существующую колонку" },
    { id: "b", label: "Добавить UNIQUE индекс (CREATE INDEX CONCURRENTLY)" },
    { id: "c", label: "Добавить FOREIGN KEY (требует прохода проверки по существующим строкам)" },
    { id: "d", label: "Изменить тип колонки (TEXT → INTEGER) — перезапись таблицы" },
    { id: "e", label: "Разбить JSONB-колонку на типизированные колонки — миграция с backfill" },
    { id: "f", label: "Разбить одну таблицу на две с FK — окно двойной записи, проверка паритета" },
    { id: "g", label: "Изменить primary key — каскадирует через каждый FK и индекс; почти никогда не делается" },
  ]}
  correctOrder={["a", "b", "c", "d", "e", "f", "g"]}
/>

<Quiz
  id="03-normalization-quiz-1"
  lessonSlug="03-normalization"
  lang="ru"
  question="Таблица хранит `orders(order_id, customer_id, customer_city)`. Какую нормальную форму она нарушает и почему?"
  choices={[
    { label: "1NF — customer_city содержит несколько значений", misconception: "Название города — одно атомарное значение. 1NF — про многозначные ячейки." },
    { label: "3NF — customer_city транзитивно зависит от customer_id, а не от order_id", correct: true },
    { label: "2NF — customer_city частично зависит от составного ключа", misconception: "order_id один является PK (не составным), поэтому частичная зависимость 2NF неприменима." },
    { label: "BCNF — существует два перекрывающихся candidate keys", misconception: "BCNF требует нескольких перекрывающихся candidate keys; здесь один." },
  ]}
/>

<TradeoffMatrix
  id="03-normalization-tradeoff-1"
  lessonSlug="03-normalization"
  lang="ru"
  prompt="Дашборд отчётности нуждается в общей выручке по workspace, пересчитываемой каждые 5 минут. Выберите реализацию."
  options={[
    { name: "Сохранённый агрегат: колонка workspace_revenue_cents в workspaces, обновляемая триггером BEFORE INSERT/UPDATE/DELETE на order_items", summary: "Денормализовано, но согласуемо.", correct: true, justification: "Триггер поддерживает согласованность агрегата при записи. Дашборд читает одну колонку на workspace. Overhead при записи — один триггер на запись в order_items (~1-5 мкс). Правильно когда стоимость запроса дашборда превышает overhead триггера — измеримо верно при десятках миллионов строк." },
    { name: "Агрегировать из order_items при каждой загрузке дашборда: SELECT SUM(...) FROM order_items JOIN orders WHERE workspace_id = X", summary: "Чистый нормализованный подход.", justification: "Правильно в малом масштабе (до ~1M order_items). Становится узким местом выше ~10M строк, потому что каждый запрос дашборда агрегирует всю историю. Сначала измерьте; переходите к сохранённому агрегату только при подтверждённой необходимости." },
    { name: "JSONB-колонка в workspaces, хранящая {revenue: 12345, last_updated: '...'}", summary: "Неверный инструмент.", justification: "JSONB ничего не даёт здесь: revenue — одиночное типизированное значение. JSONB добавляет overhead запросов и теряет type enforcement. Используйте INTEGER или BIGINT." },
    { name: "Отдельная reporting-база, синхронизируемая ночным ETL", summary: "Избыточно для свежести 5 минут.", justification: "Отдельная reporting-БД правильна при очень большом масштабе (миллиарды строк, BI-инструменты). Для обновления каждые 5 минут по одной workspace сохранённый агрегат проще в эксплуатации." },
  ]}
/>

<Quiz
  id="03-normalization-quiz-2"
  lessonSlug="03-normalization"
  lang="ru"
  question="ORM-сгенерированная схема не имеет именованных CHECK ограничений и индексов на FK-колонках. Какие два конкретных сценария отказа это вызывает в продакшне?"
  choices={[
    { label: "Непонятные сообщения об ошибках при нарушении ограничений; и FK-запросы (список заказов пользователя) сканируют всю таблицу вместо использования индекса", correct: true },
    { label: "База отказывает от миграции схемы", misconception: "Отсутствие имён и индексов не является синтаксическими ошибками — Postgres принимает схему. Сбой операционный, а не структурный." },
    { label: "ORM не могут писать в безымянные ограничения", misconception: "ORM пишут через INSERT/UPDATE без ссылок на имена ограничений — именование влияет только на сообщения об ошибках." },
    { label: "Foreign keys не принудительно соблюдают referential integrity без индекса на FK-колонке", misconception: "Принуждение FK не требует индекса. Индекс нужен для производительности запросов — без него FK-запросы SELECT делают сканирование таблиц." },
  ]}
/>

</Explanation>

<KeyTakeaway>Цельтесь в 3NF: ни один факт не живёт в двух местах, две строки не могут расходиться. Денормализуйте намеренно (сохранённые агрегаты, широкие read-модели) с явным согласованием. ORM-сгенерированные схемы пропускают ограничения и индексы — добавляйте их. Соглашения об именовании соблюдаются через CI, а не через память.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="03-normalization-retrieval"
  lang="ru"
  questions={[
    { q: "Объясните своими словами, почему 3NF является продакшн-дефолтной нормальной формой, и когда команды обоснованно идут выше (BCNF) или ниже (намеренная денормализация).", a: "3NF устраняет самый распространённый сбой схемной integrity — хранение одного факта в двух местах, которые могут разойтись — без болезненного разбиения на множество таблиц, как в высших формах. BCNF важна только когда таблица имеет несколько перекрывающихся candidate keys, что редко в продакшне. Намеренная денормализация ниже 3NF оправдана когда (a) путь чтения достаточно горячий, чтобы JOIN был измеренным узким местом, (b) дублированное значение — вычисленный агрегат, согласуемый триггером, или (c) аудит-колонка захватывает значение на момент записи." },
    { q: "Назовите три конкретных ORM-ловушки, незаметно нарушающих дисциплину схемы, и исправление для каждой.", a: "N+1 запросы: ORM lazy-load каждую FK-связь отдельно; исправление — явные eager-load декларации на каждую связь. Неявная генерация схемы: DDL от ORM не содержит CHECK ограничений, именованных ограничений и FK-индексов; исправление — добавить их в файлы миграций или после шага генерации. Отсутствие обновления updated_at: ORM управляет created_at, но не updated_at; исправление — BEFORE UPDATE триггер или lifecycle hook, устанавливающий updated_at = now() при каждой записи." },
    { q: "Что такое паттерн материализованного пути и когда он лучше рекурсивных CTE для запросов к деревьям?", a: "Материализованный путь хранит полную цепочку предков узла дерева как текстовую колонку (например, '/1/4/12/'). Запрос поддерева становится WHERE path LIKE '/1/4/%' — простой поиск по префиксу, индексируемый B-tree с text_pattern_ops. Он лучше рекурсивного CTE когда (a) дерево читают намного чаще, чем пишут, (b) дерево глубокое (у рекурсивных CTE есть стоимость на уровень), или (c) дерево большое и CTE просканирует миллионы строк для построения списка предков. Цена: вставка узла требует записи полного пути; перемещение поддерева требует обновления пути каждого потомка — дорого для больших перемещений." },
  ]}
/>

<Recap lang="ru">
Нормальные формы устраняют избыточные факты: 1NF (атомарные ячейки), 2NF (полная ключевая зависимость), 3NF (нет транзитивных зависимостей), BCNF (каждый детерминант — candidate key). Цельтесь в 3NF для продакшн-схем. Денормализуйте намеренно — сохранённые агрегаты, широкие read-модели, материализованные пути — с явным согласованием; никогда без плана синхронизации копий. ORM-сгенерированные схемы пропускают ограничения и индексы; добавляйте их. Соглашения об именовании (snake_case, множественное число, id PK, created_at/updated_at/deleted_at, именованные CHECK) соблюдаются через CI. Решения о схеме «прилипают»; цена неверной нормальной формы выплачивается при миграции, а не при проектировании.
</Recap>
