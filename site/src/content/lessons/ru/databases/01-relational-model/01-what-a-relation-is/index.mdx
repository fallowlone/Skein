---
concepts:
- relation
- tuple
- attribute
- domain
- candidate-key
- primary-key
- foreign-key
- constraint
- referential-integrity
- normalization
deepensInto: []
estMin: 12
lang: ru
lessonType: topic
level: junior
order: 1
prereqs: []
slug: 01-what-a-relation-is
sources:
- https://www.postgresql.org/docs/current/ddl-constraints.html
- https://en.wikipedia.org/wiki/Relational_model
- https://www.postgresql.org/docs/current/datatype.html
spiral:
- statefulness
- encapsulation
status: ready
summary: Базовый словарь реляционной модели — отношения, кортежи, candidate keys и
  почему декларативные ограничения — главный вклад модели.
title: 'Что такое отношение: таблицы, строки, ключи и ограничения'
track: databases
unit: 01-relational-model
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import MetaphorComplete from "~/components/pedagogy/MetaphorComplete.astro";
import PersonaTag from "~/components/pedagogy/PersonaTag.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";
import Infographic from "~/components/diagram/Infographic.astro";
import shapeTradeoff from "./infographics/shape-tradeoff.svg?raw";

<Hook>
Команда хранит «пользователь имеет много тегов» как CSV-колонку. Через шесть месяцев: «сколько пользователей с тегом X?» — нужно парсить каждую строку. Реляционный дизайн отвечает на этот вопрос точечным поиском. Разница — в модели, с которой начинают.
</Hook>

<Crux>Отношение — это набор кортежей общей формы, и движок принудительно соблюдает эту форму — поэтому нельзя зафиксировать плохие данные.</Crux>

<Explanation>

## Что такое отношение

Статья Эдгара Кодда 1970 года заложила основы каждой SQL-базы данных с тех пор. Через десять минут ты будешь знать, почему CSV-колонка ломается и как проектировать схему, которая не даст сохранить плохие данные. Словарь невелик:

- **Отношение (relation)** — набор кортежей общей формы. Таблица — SQL-реализация.
- **Кортеж (tuple)** — одна строка; каждый кортеж в отношении имеет одинаковые атрибуты.
- **Атрибут (attribute)** — колонка; каждый берёт значения из *домена* (типа).
- **Candidate key** — минимальное подмножество атрибутов, уникально идентифицирующее каждый кортеж. У таблицы может быть несколько candidate keys; один назначается *primary key*.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Концепция</th>
        <th class="border border-border px-3 py-2 text-left">SQL-термин</th>
        <th class="border border-border px-3 py-2 text-left">Что означает</th>
      </tr>
    </thead>
    <tbody>
      <tr><td class="border border-border px-3 py-2">Relation</td><td class="border border-border px-3 py-2">Table</td><td class="border border-border px-3 py-2">Набор строк общей формы</td></tr>
      <tr class="bg-surface-2"><td class="border border-border px-3 py-2">Tuple</td><td class="border border-border px-3 py-2">Row</td><td class="border border-border px-3 py-2">Одна запись</td></tr>
      <tr><td class="border border-border px-3 py-2">Attribute</td><td class="border border-border px-3 py-2">Column</td><td class="border border-border px-3 py-2">Одно именованное типизированное значение в строке</td></tr>
      <tr class="bg-surface-2"><td class="border border-border px-3 py-2">Domain</td><td class="border border-border px-3 py-2">Type</td><td class="border border-border px-3 py-2">Множество допустимых значений атрибута</td></tr>
      <tr><td class="border border-border px-3 py-2">Candidate key</td><td class="border border-border px-3 py-2">PRIMARY KEY / UNIQUE</td><td class="border border-border px-3 py-2">Минимальный идентификатор строки</td></tr>
    </tbody>
  </table>
</div>

## Почему ограничения — главный вклад модели

Когда ты наследуешь кодовую базу и находишь данные, которых «быть не должно», почти всегда причина одна: ограничений нет. Реляционная модель не просто хранит данные — она отказывается хранить плохие данные. Ограничения (constraints) — декларативные правила, которые движок проверяет перед каждой записью, обновлением или удалением:

- **PRIMARY KEY** — уникально идентифицирует каждую строку; подразумевает NOT NULL и UNIQUE.
- **FOREIGN KEY** — колонка, ссылающаяся на primary или unique key другой таблицы; движок отказывает в строках-сиротах.
- **NOT NULL** — атрибут всегда должен иметь значение.
- **UNIQUE** — в этой колонке или наборе колонок нет дублей.
- **CHECK** — произвольное булево выражение, вычисляемое при каждой записи; например, `CHECK (amount >= 0)`.

Без ограничений — это key-value хранилище с SQL-синтаксисом. Баги в приложении могут зафиксировать плохие данные. С ограничениями движок отказывает от имени каждого вызова — код приложения не должен помнить правила.

## Метафора

Реляционная схема — библиотечный каталог. Каждая таблица — ящик (книги, авторы, выдачи). Каждая строка — карточка с одинаковыми полями. Карточка выдачи содержит member_id и book_id, ссылающиеся на другие ящики (foreign keys). Библиотекарь (движок) отказывается оформить выдачу, если book_id не существует. Система остаётся согласованной без постоянного контроля людей.

## Практический сценарий

<PersonaTag id="sven" lang="ru" /> хочет функцию «избранное» для маркетплейса. <PersonaTag id="otto" lang="ru" /> задаёт вопрос о форме. Свен говорит «пользователь имеет много избранных». Отто обращается к модели: users, items, таблица favourites с `(user_id, item_id)` как PK плюс два FK. Три строки DDL — и функция структурно корректна: нет дублей, нет сирот, запросы в обе стороны. Ярлык с JSON-массивом ломается в момент, когда нужно: «кто добавил этот товар в избранное».

Другая команда хранит адреса как CSV-текст в строке пользователя. Через шесть месяцев маркетинг спрашивает «сколько пользователей в Орегоне?» — нужно парсить каждое поле. Реляционный дизайн (таблица addresses со структурированными колонками) — это две лишние строки схемы и на три порядка дешевле в запросах.

<Infographic
  svg={shapeTradeoff}
  label="Сравнение двух колонок для одного требования «у пользователя много тегов»: колонка JSON-массив делает теги одного пользователя быстрыми, но превращает «у кого тег X», «сколько пользователей с тегом X» и «переименовать тег X» в сканирование всех строк без ограничений, тогда как реляционная таблица связей делает все три запроса точечным поиском, блокирует дубликаты составным первичным ключом и блокирует сирот двумя внешними ключами ценой двух лишних строк схемы."
  caption="Выберешь ярлык JSON-массив — три запроса из четырёх сканируют каждую строку; выберешь таблицу связей — они станут точечным поиском, примерно на три порядка дешевле, а движок гарантирует отсутствие дублей и сирот."
  hue="var(--d-infra)"
/>

<Inset kind="why" lang="ru">
Цена реляционной модели — проверка ограничений при записи и дисциплина проектирования схемы до написания кода. Опытные инженеры платят эту цену сознательно; новые команды либо платят случайно (сначала денормализуют, потом сожалеют), либо пропускают (используют БД как key-value хранилище и накапливают годы долга по integrity). Этот урок — о том, почему первый путь почти всегда дешевле на протяжении жизни системы.
</Inset>

<DragOrder
  id="01-what-a-relation-is-drag-1"
  lessonSlug="01-what-a-relation-is"
  lang="ru"
  prompt="Упорядочьте шаги проектирования схемы для 'пользователь имеет много адресов':"
  items={[
    { id: "a", label: "Определить сущности: User, Address" },
    { id: "b", label: "Для каждой сущности выбрать primary key (id BIGSERIAL или uuid)" },
    { id: "c", label: "Определить связь: один пользователь имеет много адресов (1:N)" },
    { id: "d", label: "Добавить колонку user_id в addresses с REFERENCES users(id)" },
    { id: "e", label: "Добавить NOT NULL на FK (каждый адрес принадлежит пользователю)" },
    { id: "f", label: "Добавить индекс на addresses(user_id) для быстрого поиска адресов пользователя" },
    { id: "g", label: "Выбрать ON DELETE: CASCADE или RESTRICT" },
  ]}
  correctOrder={["a", "b", "c", "d", "e", "f", "g"]}
/>

<Quiz
  id="01-what-a-relation-is-quiz-1"
  lessonSlug="01-what-a-relation-is"
  lang="ru"
  question="Что такое primary key в реляционной таблице?"
  choices={[
    { label: "Колонка или набор колонок, уникально идентифицирующих каждую строку, с совместным принуждением NOT NULL и UNIQUE", correct: true },
    { label: "Первая колонка таблицы", misconception: "Позиция не определяет primary key." },
    { label: "Любая индексированная колонка", misconception: "Индексы — отдельная концепция; не каждый индекс является primary key." },
    { label: "Колонка, автоматически генерируемая базой данных", misconception: "PK может быть натуральным (бизнес-значение) или суррогатным (авто-генерируемым); авто-генерация не обязательна." },
  ]}
/>

<Quiz
  id="01-what-a-relation-is-quiz-2"
  lessonSlug="01-what-a-relation-is"
  lang="ru"
  question="Что даёт объявление foreign key?"
  choices={[
    { label: "Скорость — foreign keys ускоряют JOIN", misconception: "FK не ускоряют JOIN; это делают индексы." },
    { label: "Referential integrity — база отказывает вставить строку, ссылающуюся на несуществующего родителя, а ON DELETE определяет, что происходит при удалении родителя", correct: true },
    { label: "Компрессия — FK-колонки занимают меньше места", misconception: "FK-колонки не сжимаются особым образом." },
    { label: "Согласованность репликации", misconception: "Репликация — отдельная подсистема; FK принудительно соблюдает локальную integrity." },
  ]}
/>

<MetaphorComplete
  id="01-what-a-relation-is-metaphor-1"
  lessonSlug="01-what-a-relation-is"
  lang="ru"
  setup="Заполните пропуск: движок отказывает плохим вставкам из-за объявленных _______ — правил вроде NOT NULL, UNIQUE, FOREIGN KEY, CHECK."
  accepted={["constraints", "ограничений", "ограничения"]}
  canonical="constraints"
  explanation="Ограничения (constraints) — декларативные правила, которые база проверяет при записи. Именно они перемещают корректность из 'каждое приложение должно помнить' в 'движок гарантирует'. Без них — key-value хранилище с SQL-синтаксисом."
/>

</Explanation>

<FlowDiagram
  label="Связующая таблица favourites хранит два внешних ключа: один указывает на таблицу users, другой на items; каждая стрелка внешнего ключа идёт от дочерней строки к родителю, на который она обязана ссылаться."
  hue="var(--d-data)"
  perRow={3}
  nodes={[
    { id: "users", label: "users", sub: "id PK" },
    { id: "fav", label: "favourites", sub: "user_id, item_id PK" },
    { id: "items", label: "items", sub: "id PK" },
  ]}
  edges={[
    { from: "fav", to: "users", label: "user_id FK" },
    { from: "fav", to: "items", label: "item_id FK" },
  ]}
  caption="Составной PK (user_id, item_id) блокирует дубликаты; два внешних ключа указывают на реальные родительские строки, поэтому движок отвергает любую строку-сироту."
/>

<KeyTakeaway>Реляционная модель хранит данные в таблицах типизированных, ограниченных строк. Движок принудительно соблюдает PRIMARY KEY, FOREIGN KEY, NOT NULL, UNIQUE и CHECK при записи — плохие данные не могут быть зафиксированы независимо от того, какое приложение обращается к таблице.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="01-what-a-relation-is-retrieval"
  lang="ru"
  questions={[
    { q: "В двух предложениях: почему хранить 'пользователь имеет много тегов' как JSON-массив в колонке обычно хуже, чем таблица тегов плюс join-таблица user_tags?", a: "Ярлык с JSON-массивом работает для 'покажи теги пользователя', но ломает каждый другой запрос: 'кто имеет тег X' требует сканирования каждой строки и парсинга каждого массива; 'сколько пользователей с тегом X' — та же проблема; 'переименовать тег X в Y' требует перезаписи каждой строки. Реляционная форма делает все три запроса точечными поисками, позволяет движку индексировать с обеих сторон и использует foreign keys, гарантируя, что user_tags ссылается только на реальных пользователей и реальные теги." },
    { q: "Назовите пять видов ограничений реляционного движка и что делает каждый.", a: "PRIMARY KEY — уникально идентифицирует каждую строку, подразумевает NOT NULL и UNIQUE. FOREIGN KEY — ссылка на ключ другой таблицы; отказывает в строках-сиротах. NOT NULL — колонка всегда должна иметь значение. UNIQUE — нет дублирующихся значений в строках для этого набора колонок. CHECK — произвольное булево выражение, вычисляемое при каждой записи; отказывает строкам, где оно ложно." },
    { q: "В чём разница между candidate key и primary key?", a: "Candidate key — любой минимальный набор атрибутов, уникально идентифицирующих каждый кортеж в отношении. У таблицы может быть несколько candidate keys. Primary key — один выбранный candidate key, назначенный главным идентификатором; подразумевает NOT NULL и UNIQUE. Другие candidate keys выражаются как ограничения UNIQUE NOT NULL." },
  ]}
/>

<Recap lang="ru">
Реляционная модель определяет данные как наборы типизированных кортежей общей формы (отношение). Таблицы, строки и колонки — SQL-реализации отношений, кортежей и атрибутов. Каждая строка идентифицируется candidate key (минимальный набор атрибутов, уникально идентифицирующий кортеж) — один становится primary key. Foreign keys связывают таблицы и позволяют движку отказывать в строках-сиротах. Пять видов ограничений (PK, FK, NOT NULL, UNIQUE, CHECK) — декларативные гарантии, которые перемещают корректность от каждого приложения к границе базы данных. Цена — дисциплина проектирования схемы до написания кода; выгода — корректность, которую каждый вызов получает бесплатно. Теперь, когда встретишь требование «пользователь имеет много X», ты потянешься за join-таблицей с двумя FK раньше, чем за JSON-колонкой.
</Recap>
