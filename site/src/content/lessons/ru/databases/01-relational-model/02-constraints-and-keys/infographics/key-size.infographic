infographic chart-column-simple
data
  title Размер записи в индексе по типу суррогатного ключа
  desc UUID стоит вдвое больше байт, чем BIGSERIAL, в каждой записи индекса — плюс худшая локальность B-tree
  values
    - label BIGSERIAL (байт)
      value 8
    - label UUID (байт)
      value 16
theme
  palette #14b8a6 #6366f1
