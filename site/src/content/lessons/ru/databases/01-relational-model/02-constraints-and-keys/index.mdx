---
concepts:
- codd-relational-algebra
- primary-key
- foreign-key
- unique-constraint
- check-constraint
- not-null
- surrogate-key
- natural-key
- uuid-v7
- bigserial
- postgres-types
- timestamptz
- numeric-type
- jsonb-type
deepensInto: []
estMin: 16
lang: ru
lessonType: topic
level: middle
order: 2
prereqs:
- 01-what-a-relation-is
slug: 02-constraints-and-keys
sources:
- https://www.postgresql.org/docs/current/ddl-constraints.html
- https://www.postgresql.org/docs/current/datatype.html
- https://en.wikipedia.org/wiki/Relational_model
spiral:
- statefulness
- encapsulation
status: ready
summary: Пять видов ограничений в деталях, суррогатные vs натуральные ключи, разрыв
  SQL с реляционной алгеброй и почему выбор минимального типа Postgres — тоже ограничение.
title: Ограничения, ключи и типы данных Postgres
track: databases
unit: 01-relational-model
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import NumbersCard from "~/components/layout/NumbersCard.astro";
import FadedExample from "~/components/pedagogy/FadedExample.tsx";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";
import Infographic from "~/components/diagram/Infographic.astro";
import keySize from "./infographics/key-size.svg?raw";

<Hook>
Команда хранит деньги в REAL. После года накопленных транзакций обнаруживаются расхождения на уровне центов, которые невозможно объяснить. Тип и был ошибкой — арифметика float теряет точность. Исправление — изменение схемы, затрагивающее каждую строку.
</Hook>

<Crux>Ограничения и типы — первый рубеж обороны схемы. Чем строже они объявлены, тем меньше ошибок проходит через границу записи.</Crux>

<Explanation>

## Реляционная модель Кодда vs SQL

Статья Эдгара Кодда 1970 года формализовала отношения как наборы кортежей из типизированных доменов и определила замкнутую алгебру операций (выборка, проекция, соединение, объединение, пересечение, разность) — замкнутую потому, что каждая операция принимает отношения на вход и возвращает отношение на выход. SQL — нестрогая реализация этой алгебры; он добавляет NULL (что Кодду не нравилось), ORDER (строки концептуально неупорядочены) и дублирующиеся строки (в отношениях дублей нет). Знание этого разрыва объясняет шероховатости:

- `NULL = NULL` — это NULL, не true: трёхзначная логика.
- `ORDER BY` обязателен для гарантии порядка строк — без него движок вернёт любой порядок.
- `DISTINCT` существует потому, что движок должен хранить дубли, пока вы не попросите иначе.

Воспринимайте разрыв как «SQL = реляционная алгебра плюс практические компромиссы» — и неожиданности перестанут удивлять.

## Пять видов ограничений

Зачем держать все пять в голове? Потому что каждый из них отсекает отдельный класс багов прямо при записи — до того, как плохие данные попадут в приложение. Когда в продакшне обнаруживается несогласованность данных, спроси себя: какое из этих пяти ограничений её бы предотвратило?

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Ограничение</th>
        <th class="border border-border px-3 py-2 text-left">Что принудительно соблюдает</th>
        <th class="border border-border px-3 py-2 text-left">Ключевая деталь</th>
      </tr>
    </thead>
    <tbody>
      <tr><td class="border border-border px-3 py-2 font-mono text-xs">PRIMARY KEY</td><td class="border border-border px-3 py-2">Уникальный ненулевой идентификатор строки</td><td class="border border-border px-3 py-2">Один на таблицу; неявно создаёт уникальный B-tree индекс</td></tr>
      <tr class="bg-surface-2"><td class="border border-border px-3 py-2 font-mono text-xs">UNIQUE</td><td class="border border-border px-3 py-2">Нет дублей в наборе колонок</td><td class="border border-border px-3 py-2">Несколько NULL допустимы (стандарт SQL); <code>UNIQUE NULLS NOT DISTINCT</code> (SQL:2023 / Postgres 15+) запрещает это</td></tr>
      <tr><td class="border border-border px-3 py-2 font-mono text-xs">NOT NULL</td><td class="border border-border px-3 py-2">Колонка всегда имеет значение</td><td class="border border-border px-3 py-2">Для каждой колонки; первый рубеж качества данных</td></tr>
      <tr class="bg-surface-2"><td class="border border-border px-3 py-2 font-mono text-xs">FOREIGN KEY</td><td class="border border-border px-3 py-2">Колонка ссылается на существующий PK/UNIQUE другой таблицы</td><td class="border border-border px-3 py-2">Параметры ON DELETE / ON UPDATE: NO ACTION, RESTRICT, CASCADE, SET NULL, SET DEFAULT</td></tr>
      <tr><td class="border border-border px-3 py-2 font-mono text-xs">CHECK</td><td class="border border-border px-3 py-2">Произвольное булево выражение на каждой строке при записи</td><td class="border border-border px-3 py-2"><code>CHECK (amount &gt;= 0)</code>, <code>CHECK (status IN ('open','closed'))</code> — может ссылаться на другие колонки той же строки</td></tr>
    </tbody>
  </table>
</div>

## Суррогатные vs натуральные ключи

**Натуральный ключ** — данные, уже существующие в бизнес-домене (email пользователя, SKU продукта, номер заказа). **Суррогатный ключ** — генерируемый базой, непрозрачный, значимый только внутри базы (BIGSERIAL, UUID).

**Продакшн-дефолт в 2026: суррогатный ключ как primary key плюс ограничение UNIQUE NOT NULL на бизнес-натуральный ключ.** Почему: натуральные ключи меняются (клиент меняет email), а изменение primary key каскадирует через каждый ссылающийся foreign key — операционно дорогостояще и часто невозможно при масштабе. Суррогатные ключи никогда не меняются.

Исключение: чистые join-таблицы (favourites: user_id, item_id) часто используют композит из foreign keys как PK — сама связь и есть идентичность, суррогат не нужен.

**UUID vs BIGSERIAL.** UUID глобально уникальны (хороши для распределённых вставок, мультирегионов, offline-first клиентов), но больше (16 байт против 8) и хуже для локальности индекса (случайные UUIDv4 фрагментируют B-tree). UUIDv7 (упорядоченный по времени, RFC 9562) решает проблему локальности и является современным дефолтом там, где нужен UUID. BIGSERIAL меньше, последователен и cache-friendly — выбирайте его, когда глобально уникальные ID не нужны.

<Infographic
  svg={keySize}
  label="Столбчатая диаграмма сравнения размера записи в индексе: ключ BIGSERIAL занимает 8 байт на запись индекса, а ключ UUID — 16 байт, вдвое больше места в каждой записи B-tree."
  caption="Компромисс суррогатного ключа в одном числе: UUID покупает глобальную уникальность ценой вдвое большего числа байт на запись индекса по сравнению с BIGSERIAL, плюс худшая локальность B-tree, если не использовать упорядоченный по времени UUIDv7."
  hue="var(--d-infra)"
/>

## Типы данных Postgres: выбирайте минимально необходимый

У Postgres богатейшая система типов среди всех популярных баз данных. Тип — первый рубеж ограничения: правильно типизированная колонка отсекает 80% плохих данных до того, как сработает любое CHECK ограничение.

| Категория | Продакшн-дефолты | Избегать |
|---|---|---|
| Целые | BIGINT (8B) для ID; INTEGER (4B) при ограниченном домене &lt; ~2B | SMALLINT без уверенности, что &lt; 32 767 |
| Строки | TEXT (без ограничения длины, без padding) | CHAR(n) — padding до длины, сюрпризы с пробелами; VARCHAR(n) добавляет проверку, но не даёт выигрыша в хранении |
| Деньги | NUMERIC(p,s) или BIGINT cents — точная арифметика | REAL или DOUBLE PRECISION — IEEE 754 теряет центы |
| Время | TIMESTAMPTZ (хранит UTC, отображает в timezone сессии) | TIMESTAMP (без зоны) — ловушка; DATE для дат |
| ID | UUID нативный тип (16 байт) | UUID как TEXT — тратит байты, теряет принудительный тип |
| Булевы | BOOLEAN | SMALLINT или TEXT для булевых — семантически неверно |
| Полуструктурированные | JSONB (бинарный, индексируемый) | JSON (только текст, не индексируемый) |

<NumbersCard
  id="02-constraints-and-keys-numbers"
  title="Числа по ключам и типам"
  stats={[
    { label: "Статья Кодда", value: "1970" },
    { label: "BIGSERIAL на запись в индексе", value: "8 байт" },
    { label: "UUID на запись в индексе", value: "16 байт" },
    { label: "UUIDv7 vs UUIDv4 локальность индекса", value: "упорядоченный vs случайный" },
    { label: "Overhead проверки FK-ограничения", value: "~5-50 мкс / строка" },
    { label: "Размер GIN-индекса JSONB vs B-tree", value: "~5-20x больше" },
    { label: "Типичный overhead хранения колонки", value: "~1-4 байта / колонка" },
    { label: "Размер записи составного PK в индексе", value: "~24-48 байт" },
  ]}
/>

<FadedExample
  client:visible
  id="02-constraints-and-keys-fe-1"
  lessonSlug="02-constraints-and-keys"
  lang="ru"
  title="Спроектируйте минимальную e-commerce схему (users, products, orders)"
  steps={{
    solved: (
      <div class="space-y-3 text-sm">
        <p><strong>Таблицы:</strong></p>
        <pre class="text-xs bg-zinc-100 dark:bg-zinc-800 p-2 rounded overflow-x-auto"><code>{`CREATE TABLE users (
  id BIGSERIAL PRIMARY KEY,
  email TEXT NOT NULL UNIQUE,
  display_name TEXT NOT NULL,
  created_at TIMESTAMPTZ NOT NULL DEFAULT now()
);

CREATE TABLE products (
  id BIGSERIAL PRIMARY KEY,
  sku TEXT NOT NULL UNIQUE,
  name TEXT NOT NULL,
  price_cents INTEGER NOT NULL CHECK (price_cents > 0),
  metadata JSONB NOT NULL DEFAULT '{}'::jsonb,
  created_at TIMESTAMPTZ NOT NULL DEFAULT now()
);

CREATE TABLE orders (
  id BIGSERIAL PRIMARY KEY,
  user_id BIGINT NOT NULL REFERENCES users(id) ON DELETE RESTRICT,
  status TEXT NOT NULL CHECK (status IN ('pending','paid','shipped','cancelled')),
  total_cents INTEGER NOT NULL CHECK (total_cents >= 0),
  created_at TIMESTAMPTZ NOT NULL DEFAULT now()
);
CREATE INDEX idx_orders_user_id ON orders(user_id);

CREATE TABLE order_items (
  order_id BIGINT NOT NULL REFERENCES orders(id) ON DELETE CASCADE,
  product_id BIGINT NOT NULL REFERENCES products(id) ON DELETE RESTRICT,
  quantity INTEGER NOT NULL CHECK (quantity > 0),
  unit_price_cents INTEGER NOT NULL CHECK (unit_price_cents > 0),
  PRIMARY KEY (order_id, product_id)
);`}</code></pre>
        <p><strong>Почему каждое решение именно такое:</strong> Суррогатные BIGSERIAL PK для users/products/orders, чтобы бизнес-ключи (email, sku) могли меняться без каскадирования. Натуральный составной PK для order_items, потому что связь сама и есть идентичность. NOT NULL везде, где нельзя допустить отсутствия данных. CHECK ограничения кодируют очевидные инварианты. ON DELETE выбран под каждую связь: orders.user_id RESTRICT (нельзя удалить пользователя с заказами), order_items.order_id CASCADE, order_items.product_id RESTRICT. TIMESTAMPTZ для каждого created_at. JSONB только для products.metadata, где схема действительно открытая. Индекс на orders(user_id), потому что самый частый запрос — «список заказов этого пользователя».</p>
      </div>
    ),
    semi: {
      prompt: "Если стейкхолдер спрашивает «почему бы не хранить имя и email пользователя прямо в orders — это ускорило бы поиск заказов», что является каноническим ответом реляционного проектирования в одном слове?",
      blanks: [
        { id: "answer", expected: "денормализация", placeholder: "название техники" },
      ],
    },
    blank: {
      prompt: "Маркетплейс добавляет «теги продукта» — свободная форма, по одному списку на продукт. Где жить тегам — в колонке-массиве, поле JSONB или отдельной таблице?",
      reveal: (
        <div class="space-y-2 text-sm">
          <p>Зависит от паттерна запросов. Если единственный запрос — «покажи теги этого продукта», колонка-массив (TEXT[]) — простейший вариант. Если когда-либо понадобится «все продукты с тегом X» в масштабе, отдельная таблица тегов + join-таблица product_tags дешевле в долгосрочной перспективе, потому что join-таблица может быть индексирована с обеих сторон и переименование тегов — обновление одной строки.</p>
          <p>JSONB здесь хуже обоих вариантов — теряет integrity (нет ограничения уникальности тегов, нет FK к канонической таблице тегов) и запросы дороже join-таблицы.</p>
        </div>
      ),
    },
  }}
  misconceptions={{
    "answer:кеширование": "Кеширование — runtime-концепция; техника дублирования данных в денормализованную колонку называется денормализацией.",
    "answer:репликация": "Репликация копирует базу данных; денормализация реструктурирует схему.",
  }}
/>

<Quiz
  id="02-constraints-and-keys-quiz-1"
  lessonSlug="02-constraints-and-keys"
  lang="ru"
  question="Команда хранит деньги в REAL и обнаруживает расхождения на уровне центов после года транзакций. Исправление?"
  choices={[
    { label: "Использовать DOUBLE PRECISION", misconception: "Double-precision float также имеет ошибки округления для десятичных денег; тип неверный." },
    { label: "Мигрировать на NUMERIC(p,s) (или BIGINT cents) — точная арифметика, без ошибок округления, продакшн-дефолт для валют", correct: true },
    { label: "Округлять при чтении", misconception: "Округление скрывает баг, но не устраняет дрейф." },
    { label: "Использовать отдельный audit log", misconception: "Audit log ловит симптом; тип — причина." },
  ]}
/>

<Quiz
  id="02-constraints-and-keys-quiz-2"
  lessonSlug="02-constraints-and-keys"
  lang="ru"
  question="Какой самый весомый аргумент в пользу суррогатного primary key (BIGSERIAL или UUID) перед натуральным (email)?"
  choices={[
    { label: "Суррогатные ключи быстрее индексируются", misconception: "Оба индексируются схожим образом; проблема — в изменяемости." },
    { label: "Натуральные ключи могут меняться (клиент меняет email); изменение primary key каскадирует через каждый ссылающийся foreign key — операционно дорогостояще и часто невозможно при масштабе", correct: true },
    { label: "Суррогатные ключи занимают меньше места", misconception: "Зависит от ситуации: BIGSERIAL (8B) меньше email TEXT, но UUID (16B) часто больше." },
    { label: "Натуральные ключи не переносимы между базами данных", misconception: "Оба переносимы; реальная проблема — изменяемость." },
  ]}
/>

</Explanation>

<FlowDiagram
  label="Внешние ключи схемы интернет-магазина: orders ссылается на users с ON DELETE RESTRICT, а order_items ссылается и на orders с CASCADE, и на products с RESTRICT; каждая стрелка идёт от ссылающейся дочерней строки к родителю, на который она указывает."
  hue="var(--d-data)"
  perRow={2}
  nodes={[
    { id: "users", label: "users", sub: "id BIGSERIAL PK" },
    { id: "products", label: "products", sub: "id BIGSERIAL PK" },
    { id: "orders", label: "orders", sub: "id PK, user_id FK" },
    { id: "items", label: "order_items", sub: "(order_id, product_id) PK" },
  ]}
  edges={[
    { from: "orders", to: "users", label: "user_id · RESTRICT" },
    { from: "items", to: "orders", label: "order_id · CASCADE" },
    { from: "items", to: "products", label: "product_id · RESTRICT" },
  ]}
  caption="ON DELETE выбирается под каждую связь: RESTRICT запрещает удалять родителя, у которого ещё есть дочерние строки; CASCADE удаляет принадлежащие ему строки. Составной PK у order_items делает саму связь её идентичностью."
/>

<KeyTakeaway>Суррогатные PK плюс UNIQUE NOT NULL на бизнес-ключи — дефолт 2026 года: натуральные ключи меняются, суррогатные — никогда. Выбирайте минимальный подходящий тип: NUMERIC для денег, TIMESTAMPTZ для моментов, BIGSERIAL когда глобальная уникальность не нужна.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="02-constraints-and-keys-retrieval"
  lang="ru"
  questions={[
    { q: "Почему NULL = NULL не TRUE в SQL и чему это равно?", a: "SQL реализует трёхзначную логику (true, false, unknown/null). NULL представляет неизвестное значение; сравнение двух неизвестных даёт неизвестное, а не true. Поэтому NULL = NULL возвращает NULL. Это унаследовано из оригинальной модели Кодда (который сам её не одобрял). Используйте IS NULL или IS NOT DISTINCT FROM для сравнений, учитывающих NULL." },
    { q: "Назовите параметры ON DELETE для foreign key и когда каждый используется.", a: "NO ACTION / RESTRICT — отказывает в удалении, если существуют ссылающиеся строки (RESTRICT проверяет немедленно, NO ACTION может быть отложен). CASCADE — удаляет ссылающиеся строки при удалении родителя (для иерархий владения). SET NULL — устанавливает FK-колонку в NULL при удалении родителя (для опциональных ссылок). SET DEFAULT — устанавливает значение по умолчанию. Продакшн-дефолты: RESTRICT для важных родителей (пользователи с заказами), CASCADE для дочерних элементов (order_items при удалении заказа), SET NULL для опциональных ссылок." },
    { q: "Что является продакшн-дефолтом для хранения валюты в Postgres и почему не REAL?", a: "NUMERIC(p,s) или BIGINT cents. REAL — IEEE 754 одинарная точность; бинарные дроби не могут точно представить все десятичные значения, поэтому повторяющаяся арифметика накапливает ошибки округления. После года транзакций дрейф становится измеримым. NUMERIC хранит точные десятичные значения без округления; BIGINT cents хранит целое число наименьших единиц валюты (например, 1000 = 10,00 руб.), что также точно." },
  ]}
/>

<Recap lang="ru">
SQL — это реляционная алгебра плюс практические компромиссы: NULL, порядок и дубли. Пять видов ограничений (PRIMARY KEY, FOREIGN KEY, NOT NULL, UNIQUE, CHECK) кодируют бизнес-правила, которые движок не нарушает. Продакшн-дефолт для primary keys — суррогатный (BIGSERIAL или UUIDv7) плюс UNIQUE NOT NULL на бизнес-натуральный ключ: натуральные ключи меняются, суррогатные — никогда. Типы Postgres — первый рубеж ограничений: NUMERIC для денег, TIMESTAMPTZ для временных меток, TEXT для строк, JSONB (не JSON) для полуструктурированных данных. Теперь, когда увидишь колонку с деньгами в REAL или временную метку без timezone — ты будешь знать точно, какой дрейф накапливается и какие типы использовать вместо них. Урок 3 разбирает нормализацию — дисциплину удаления избыточности из схемы.
</Recap>
