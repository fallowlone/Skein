---
concepts:
- candidate-key-vs-primary-key
- third-normal-form
- jsonb-decision-rule
- foreign-key-economics
- toast-and-alignment
- expand-then-contract
estMin: 14
lang: ru
level: senior
order: 8
prereqs: []
slug: quiz-short
sources:
- https://www.postgresql.org/docs/current/ddl-constraints.html
- https://en.wikipedia.org/wiki/Boyce%E2%80%93Codd_normal_form
- https://www.postgresql.org/docs/current/storage-toast.html
status: ready
summary: Вопросы на свободное припоминание по юниту реляционной модели. Сначала ответьте своими словами, затем раскройте модельный ответ и сравните.
title: 'Реляционная модель: тест с краткими ответами'
track: databases
unit: 01-relational-model
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";

<Hook>
Припоминание сильнее перечитывания. Для каждого вопроса проговорите или запишите полный ответ по памяти, прежде чем открыть модельный — именно усилие извлечения закрепляет инстинкты дизайна схемы.
</Hook>

<Goal lang="ru">
Восстановите ключевые механизмы юнита — keys, 3NF, правило выбора JSONB, экономику FK, физическое хранение и изменение схемы без простоя — не подсматривая в уроки.
</Goal>

<RetrievalDrawer
  client:visible
  id="01-relational-model-short-retrieval"
  lang="ru"
  questions={[
    { q: "В чём разница между candidate key и primary key, и почему дефолт 2026 года — это surrogate primary key плюс UNIQUE NOT NULL на business key?", a: "Candidate key — это любой минимальный набор атрибутов, уникально идентифицирующий каждый tuple; у relation их может быть несколько. Primary key — это один выбранный candidate key, назначенный главным идентификатором: он подразумевает NOT NULL и UNIQUE и является значением, на которое ссылаются foreign keys. Дефолт 2026 делает primary key surrogate-ключом (BIGSERIAL или UUIDv7), потому что natural keys меняются — клиент меняет email — а смена primary key каскадирует через каждый ссылающийся foreign key, что операционно дорого и часто невозможно на масштабе. Business-natural key (email, SKU) всё равно защищается constraint UNIQUE NOT NULL, так что вы сохраняете его гарантию, не кладя изменяемое значение в identity, на который указывает остальная схема. Исключение — чистая join-таблица, где composite из foreign keys и есть естественная identity." },
    { q: "Почему 3NF — это нормальная форма по умолчанию для продакшена, и какие два легитимных направления её покинуть?", a: "3NF устраняет самый частый отказ integrity — один и тот же факт, хранимый в двух местах, которые могут разойтись — убирая и частичные (2NF), и транзитивные (3NF) зависимости, не вынуждая болезненную декомпозицию на много таблиц, которую высшие формы (4NF, 5NF, BCNF) требуют для форм, почти не встречающихся в реальных схемах. Выше неё к BCNF идут только когда у таблицы несколько перекрывающихся candidate keys, что в продакшене редко. Ниже неё идут через намеренную денормализацию — сохранённый агрегат, обновляемый триггером, широкая read-модель, питаемая из нормализованных источников, снимок на момент времени вроде unit price, зафиксированной на строке заказа — но только когда путь чтения измеренно является узким местом и дубль согласуется чем-то явным. Режим отказа — денормализовать везде без плана согласования: каждая копия в итоге расходится с каждой другой." },
    { q: "Сформулируйте правило выбора между JSONB, типизированной колонкой и side-таблицей с примером каждого.", a: "Если вы читаете значение только как часть целого документа и никогда не фильтруете, не агрегируете, не сортируете, не джойните и не ограничиваете его, JSONB подходит — event-payload с per-type схемами, per-tenant конфиг-объекты, метаданные file-uploader. В момент, когда поле появляется в WHERE, GROUP BY, ORDER BY, FK или CHECK, оно должно быть типизированной колонкой — статус заказа, price_cents, email пользователя. А когда нужно запрашивать связь многие-ко-многим с обеих сторон, агрегировать по ней, переименовать общее значение глобально или обеспечить уникальность между двумя сущностями, используйте side-таблицу — теги товаров становятся каталогом tags плюс join-таблицей product_tags с составным PK и FK, а не массивом или JSONB-блобом. Правило promote-to-column делает это операционным: в первый раз, когда JSONB-поле появляется в WHERE, планируйте миграцию извлечения колонки." },
    { q: "Что foreign key реально стоит, что он покупает, и при каких специфичных условиях его отключение — оправданный инженерный выбор?", a: "FK-constraint стоит примерно 5-50 мкс на строку на записи, чтобы проверить, что ссылаемый родитель существует, и покупает постоянную гарантию: движок отклоняет каждую orphan-строку от имени каждого вызывающего, плюс клаузы ON DELETE/ON UPDATE (RESTRICT, CASCADE, SET NULL), определяющие, что происходит с детьми при изменении родителя. Отключение FK оправдано лишь при специфичных масштабных условиях: данные шардированы и связь пересекает границы шардов (FK не могут охватывать шарды), каскад создал бы многомиллионную транзакцию, держащую блокировки минутами, или проход валидации FK при online-смене типа колонки является операционным узким местом. Ничего из этого не применимо к типичной SaaS-схеме до ~100M строк на таблицу. Считайте «мы отключили FK» сигналом специфичного ограничения, а не общей best practice — отключение их без такого обоснования толкает integrity в код приложения, где она обеспечивается непоследовательно и ломается при рефакторингах." },
    { q: "Объясните механизм TOAST и два следствия для дизайна схемы и запросов, которые он несёт по производительности.", a: "Postgres хранит строки в фиксированных страницах по 8KB. Когда значение вытолкнуло бы строку за примерно 2KB, TOAST сначала сжимает его (LZ4 или pglz); если оно всё ещё слишком велико, он нарезает значение на чанки ~2KB, хранимые в отдельной TOAST-таблице, оставляя 18-байтовый указатель в основной строке. Два следствия: (1) чтение TOAST-колонки требует лишнего fetch из TOAST-таблицы, поэтому SELECT * на таблице с большой JSONB/TEXT/BYTEA-колонкой достаёт каждое широкое значение — выбор только нужных колонок держит горячие list-запросы дешёвыми; (2) обновление TOAST-значения переписывает чанки в side-таблице, поэтому обновления больших payload дороже обновлений узких типизированных колонок. Урок дизайна — держать редко читаемые широкие данные вне горячих путей запросов. Связанно: объявление колонок фиксированной ширины от самой широкой к узкой минимизирует alignment padding и упаковывает больше строк на страницу." },
    { q: "Опишите паттерн expand-then-contract и объясните, почему он нужен для переименования колонки на большой общей таблице без простоя.", a: "Expand-then-contract выполняет ломающее изменение схемы как последовательность неломающих шагов: (1) добавить новую колонку nullable рядом со старой; (2) развернуть код, пишущий в обе; (3) забэкфиллить существующие строки батчами, чтобы избежать долгих блокировок; (4) развернуть код, читающий из новой колонки; (5) проверить parity — подтвердить ноль строк, где старое и новое расходятся; (6) удалить старую колонку отдельной миграцией. Это нужно потому, что один RENAME COLUMN, хоть и мгновенный и только-метаданные, атомарно ломает каждого потребителя, всё ещё ссылающегося на старое имя — код приложения, ORM-модели, view, BI-дашборды, аналитические скрипты. На большой таблице, общей для многих сервисов, которые не могут развернуться одновременно, это немедленный сбой. Expand-then-contract держит старую колонку читаемой всю transition, чтобы ни один потребитель не сломался до своего обновления; цена — лишние циклы деплоя, и это плата за zero downtime." },
  ]}
/>

<Recap lang="ru">
Если вы восстановили каждый ответ по памяти, вы держите спину юнита: identity живёт в неизменяемом surrogate key, а business keys охраняются через UNIQUE NOT NULL; 3NF не даёт двум строкам разойтись, а денормализация — это измеренное исключение с явным согласованием; выбор JSONB/типизированная колонка/side-таблица целиком определяется тем, как вы запрашиваете значение; foreign keys — это дешёвая страховка, которую вы держите, пока специфичное ограничение масштаба не вынудит иначе; физическое хранение (TOAST, page alignment) делает SELECT * на широких строках дорогим; а живая схема меняется через expand-then-contract, никогда не ломаясь на месте.
</Recap>
