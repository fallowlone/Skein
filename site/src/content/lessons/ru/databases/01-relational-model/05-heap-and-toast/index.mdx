---
concepts:
- heap
- page
- tuple-header
- TOAST
- column-alignment
- TIMESTAMPTZ
- NUMERIC
- BIGSERIAL
- UUIDv7
- type-selection
deepensInto:
- 06-schema-integrity
estMin: 14
lang: ru
lessonType: topic
level: senior
order: 5
prereqs:
- 04-jsonb-and-arrays
slug: 05-heap-and-toast
sources:
- https://www.postgresql.org/docs/current/storage-page-layout.html
- https://www.postgresql.org/docs/current/storage-toast.html
- https://www.postgresql.org/docs/current/datatype.html
- https://www.postgresql.org/docs/current/ddl-generated-columns.html
spiral:
- statefulness
status: ready
summary: Как Postgres хранит строки в страницах 8КБ, что TOAST делает с широкими значениями,
  почему порядок объявления колонок влияет на размер хранилища, и как выбирать типы
  данных Postgres на уровне байт.
title: Heap-хранилище, TOAST и выравнивание колонок
track: databases
unit: 01-relational-model
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import DiagramFrame from "~/components/diagram/DiagramFrame.astro";

<Hook>
Команда сокращает таблицу с 40 колонок до 35. Хранилище уменьшается на 18%. Они не удалили никаких данных — они переупорядочили оставшиеся колонки, чтобы устранить alignment padding. База данных никогда не меняла своих правил. Команда наконец их прочитала.
</Hook>

<Crux>Строки живут в 8КБ страницах. Порядок колонок и выбор типов влияют на padding, TOAST и плотность кеша на весь срок жизни таблицы.</Crux>

<Explanation>

## Heap: как Postgres хранит строки

Postgres хранит строки таблиц в фиксированных страницах 8КБ на диске. Структура важна для хранения и производительности запросов:

- Каждая страница имеет заголовок (24 байта) и массив указателей на элементы.
- Каждая строка (кортеж) начинается с **23-байтового заголовка кортежа**, содержащего информацию о видимости транзакции (xmin, xmax, ctid — разбирается в уроке про MVCC).
- Если любая колонка nullable, следует **null bitmap** (1 бит на колонку, с выравниванием до байта).
- Затем **данные колонок** в порядке объявления, каждая дополнена до своей границы выравнивания.

Общий размер строки влияет на то, сколько строк помещается на одну страницу, что влияет на количество страниц, которые нужно прочитать при последовательном сканировании — одна из самых прямых связей между дизайном схемы и стоимостью запросов.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Тип Postgres</th>
        <th class="border border-border px-3 py-2 text-left">Размер хранения</th>
        <th class="border border-border px-3 py-2 text-left">Выравнивание</th>
        <th class="border border-border px-3 py-2 text-left">Примечания</th>
      </tr>
    </thead>
    <tbody>
      <tr><td class="border border-border px-3 py-2">SMALLINT</td><td class="border border-border px-3 py-2">2 байта</td><td class="border border-border px-3 py-2">2 байта</td><td class="border border-border px-3 py-2">Диапазон: ±32 767</td></tr>
      <tr class="bg-surface-2"><td class="border border-border px-3 py-2">INTEGER</td><td class="border border-border px-3 py-2">4 байта</td><td class="border border-border px-3 py-2">4 байта</td><td class="border border-border px-3 py-2">Диапазон: ±2.1B</td></tr>
      <tr><td class="border border-border px-3 py-2">BIGINT / BIGSERIAL</td><td class="border border-border px-3 py-2">8 байт</td><td class="border border-border px-3 py-2">8 байт</td><td class="border border-border px-3 py-2">Диапазон: ±9.2×10¹⁸</td></tr>
      <tr class="bg-surface-2"><td class="border border-border px-3 py-2">UUID</td><td class="border border-border px-3 py-2">16 байт</td><td class="border border-border px-3 py-2">4 байта</td><td class="border border-border px-3 py-2">Нативный тип, не TEXT</td></tr>
      <tr><td class="border border-border px-3 py-2">BOOLEAN</td><td class="border border-border px-3 py-2">1 байт</td><td class="border border-border px-3 py-2">1 байт</td><td class="border border-border px-3 py-2">True/false/null</td></tr>
      <tr class="bg-surface-2"><td class="border border-border px-3 py-2">TIMESTAMPTZ</td><td class="border border-border px-3 py-2">8 байт</td><td class="border border-border px-3 py-2">8 байт</td><td class="border border-border px-3 py-2">Точность до микросекунды, хранится UTC</td></tr>
      <tr><td class="border border-border px-3 py-2">NUMERIC(p,s)</td><td class="border border-border px-3 py-2">Переменный (2-1000 байт)</td><td class="border border-border px-3 py-2">4 байта</td><td class="border border-border px-3 py-2">Точный; используйте для денег</td></tr>
      <tr class="bg-surface-2"><td class="border border-border px-3 py-2">TEXT / VARCHAR</td><td class="border border-border px-3 py-2">Переменный (1 байт + содержимое)</td><td class="border border-border px-3 py-2">4 байта</td><td class="border border-border px-3 py-2">Нет разницы в хранении; TEXT предпочтительнее</td></tr>
      <tr><td class="border border-border px-3 py-2">JSONB</td><td class="border border-border px-3 py-2">Переменный (бинарный)</td><td class="border border-border px-3 py-2">4 байта</td><td class="border border-border px-3 py-2">TOAST-способный свыше ~2КБ</td></tr>
    </tbody>
  </table>
</div>

## Alignment padding для колонок

Alignment padding вставляется между колонками, чтобы каждое значение начиналось на границе своего выравнивания. Порядок колонок, чередующий широкие и узкие типы, тратит байты:

```
-- Плохой порядок: [SMALLINT(2), BIGINT(8), SMALLINT(2)]
-- Структура: 2 байта + 6 байт padding + 8 байт + 2 байта + 6 байт padding = 24 байта

-- Хороший порядок: [BIGINT(8), SMALLINT(2), SMALLINT(2)]
-- Структура: 8 байт + 2 байта + 2 байта + 4 байта padding = 16 байт
```

На широкой таблице (40 колонок смешанных типов) плохой порядок колонок может тратить 10-20% хранилища. ORM-сгенерированные схемы редко оптимизируют это; это реальный производственный рычаг на таблицах свыше ~100M строк.

**Практическое правило:** объявляйте колонки от самого широкого выравнивания к самому узкому (сначала 8-байтовые типы, затем 4-байтовые, затем 2-байтовые, затем 1-байтовые). Типы переменной длины (TEXT, JSONB) идут последними — у них внутренние заголовки длины и их стоимость выравнивания фиксирована независимо от позиции относительно колонок фиксированной ширины.

## TOAST: обработка широких значений

Страницы Postgres — 8КБ. Строки, превышающие примерно 2КБ, активируют TOAST (The Oversized-Attribute Storage Technique). Что происходит:

1. Широкое значение сжимается (LZ4 или pglz по умолчанию).
2. Если всё ещё превышает порог, нарезается на чанки ~2КБ и хранится в отдельной TOAST-таблице рядом с основной таблицей.
3. Основная строка содержит указатель (18 байт), ссылающийся на TOAST-чанки.

TOAST прозрачен — SELECT возвращает полное значение как если бы оно было inline — но имеет последствия для производительности:

- **Чтение TOAST-колонки требует дополнительного обращения к диску** из TOAST-таблицы (не из основной страницы).
- **Обновление TOAST-значения перезаписывает чанки side-таблицы** плюс указатель в основной строке.
- **Перечисление строк без чтения TOAST-колонок дёшево** — указатель inline, чанки не получаются если вы не SELECT эту колонку.

Импликация для дизайна схемы: если широкая TEXT/JSONB/BYTEA колонка редко читается (это прикреплённые метаданные, которые показываются только на странице деталей), важно исключать её из запросов, перечисляющих строки. `SELECT * FROM events LIMIT 1000` получает TOAST payload для каждого события — `SELECT id, event_type, created_at FROM events LIMIT 1000` не делает этого.

<Inset kind="why" lang="ru">
TOAST был введён для сохранения модели страниц 8КБ без жёсткого ограничения размера строки. Он в основном прозрачен для кода приложения, но виден в выводе EXPLAIN (TOAST-таблица появляется как отдельное отношение в плане если запрос читает TOAST-колонки). Компромисс дизайна: единообразие страниц (все страницы 8КБ, предсказуемый I/O) за счёт случайных дополнительных обращений для широких колонок.
</Inset>

## Выбор типов: продакшн-дефолты

**Целочисленные типы.** Выбирайте минимальный диапазон, превышающий ваш домен в 100 раз. `user_id`, который никогда не превысит 10 миллионов, может быть `INTEGER` (4 байта, экономит 4 байта на запись индекса). ID платежа, который может достичь миллиардов, должен быть `BIGINT`. Никогда `SMALLINT` для чего-либо, что может вырасти.

**Строковые типы.** `TEXT` — продакшн-дефолт. `VARCHAR(n)` добавляет ограничение проверки длины, но использует то же хранилище. `CHAR(n)` дополняет до длины (пробелы вызывают тонкие баги) — почти всегда неверный выбор.

**Деньги.** `NUMERIC(p,s)` (точная арифметика) или `BIGINT`, хранящий центы. Никогда `REAL` или `DOUBLE PRECISION` — IEEE 754 float накапливает ошибки округления в финансовой арифметике. Колонка `REAL` для денег дрейфует на центы после месяцев транзакций.

**Временные метки.** `TIMESTAMPTZ` (с часовым поясом) — продакшн-дефолт: хранит UTC, отображает в timezone сессии. `TIMESTAMP` (без зоны) — ловушка при работе приложения в разных часовых поясах. `DATE` для дат без времени.

**UUID vs BIGSERIAL.** BIGSERIAL занимает 8 байт, последователен (удобен для B-tree, хорошая локальность индекса), прост. UUID занимает 16 байт, глобально уникален (подходит для распределённых вставок, multi-region, offline-first клиентов). UUIDv4 случаен — B-tree индексы плохо фрагментируются на больших таблицах. **UUIDv7** (упорядоченный по времени, RFC 9562) решает проблему локальности с временным префиксом; это современный дефолт когда нужен UUID. Выбирайте BIGSERIAL для single-region сервисов; UUIDv7 для multi-region или offline-first.

<Quiz
  id="05-heap-and-toast-quiz-1"
  lessonSlug="05-heap-and-toast"
  lang="ru"
  question="Команда выполняет `SELECT * FROM events ORDER BY created_at DESC LIMIT 100` на таблице в 50M строк и замечает что запрос в 3 раза медленнее ожидаемого несмотря на индекс по created_at. Таблица events имеет большую JSONB-колонку payload со средним размером 8КБ. Наиболее вероятная причина?"
  choices={[
    { label: "ORDER BY нейтрализует индекс", misconception: "Индекс по created_at поддерживает ORDER BY created_at DESC; запрос может использовать обратное сканирование индекса." },
    { label: "Каждая совпадающая строка требует обращения к TOAST-таблице для чтения колонки payload со средним размером 8КБ, хранящейся в side-таблице — SELECT * принудительно получает все TOAST-колонки", correct: true },
    { label: "JSONB-колонки нельзя читать в ORDER BY запросах", misconception: "JSONB можно читать в любом запросе. Проблема — стоимость per-row TOAST получения." },
    { label: "Таблица в 50M строк всегда требует последовательного сканирования независимо от индекса", misconception: "С селективным индексом (created_at DESC LIMIT 100 получает 100 строк), сканирование индекса дёшево. Узкое место здесь — TOAST, а не тип сканирования." },
  ]}
/>

<TradeoffMatrix
  id="05-heap-and-toast-tradeoff-1"
  lessonSlug="05-heap-and-toast"
  lang="ru"
  prompt="Новому сервису нужен глобально уникальный идентификатор строки для таблицы orders. Сервис сейчас single-region, но может расшириться до multi-region через 18 месяцев."
  options={[
    { name: "BIGSERIAL — 8-байтовое последовательное целое, auto-incremented Postgres", summary: "Простейший, лучшая локальность индекса, неверный при multi-region вставках.", justification: "BIGSERIAL — простейший и наиболее storage-эффективный вариант. Последовательные значения обеспечивают B-tree индексам отличную локальность. Единственное ограничение: последовательная генерация требует единой координирующей Postgres-последовательности, что ломается при multi-region распределённых вставках. Если multi-region вставки определённо будут, мигрируйте заблаговременно; если неопределённо, начните с BIGSERIAL и мигрируйте при необходимости." },
    { name: "UUIDv4 — случайный 128-битный идентификатор", summary: "Глобально уникален, но ужасная локальность индекса.", justification: "Случайные UUID вызывают фрагментацию B-tree индекса потому что каждая новая вставка попадает на случайную страницу. На большой таблице (более 10M строк) фрагментация вызывает частые разбиения страниц и раздутие. Индекс может достичь 3-5x размера индекса с последовательными ключами. Выбирайте UUIDv4 только если глобальная уникальность нужна сегодня и UUIDv7 недоступен." },
    { name: "UUIDv7 — упорядоченный по времени UUID по RFC 9562", summary: "Лучшее из обоих: глобально уникален и последователен.", correct: true, justification: "UUIDv7 имеет 48-битный миллисекундный временной префикс, делающий его монотонно возрастающим в пределах одной миллисекунды. Локальность B-tree индекса почти такая же хорошая как у BIGSERIAL. 16 байт против 8 (удваивает размер записи индекса), но даёт глобальную уникальность без центральной последовательности. Современный дефолт когда нужен UUID." },
    { name: "ULID — 26-символьный base32 лексикографический ID", summary: "Концептуально похож на UUIDv7, менее нативный для Postgres.", justification: "ULID упорядочены по времени и глобально уникальны, но у Postgres нет нативного типа ULID — они хранятся как TEXT (26 байт против 16 для UUID). Более многословны, нет нативных операторов, не стандартный выбор в Postgres-магазине. Предпочтительнее UUIDv7." },
  ]}
/>

<Quiz
  id="05-heap-and-toast-quiz-2"
  lessonSlug="05-heap-and-toast"
  lang="ru"
  question="Широкая таблица events объявила колонки в таком порядке: (id BIGSERIAL, created_at TIMESTAMPTZ, type SMALLINT, flags BOOLEAN, user_id BIGINT, details TEXT). Какое переупорядочение уменьшает alignment padding?"
  choices={[
    { label: "id BIGSERIAL, user_id BIGINT, created_at TIMESTAMPTZ, type SMALLINT, flags BOOLEAN, details TEXT — сначала 8-байтовые типы, потом меньшие, TEXT последним", correct: true },
    { label: "details TEXT первым — переменные колонки в начале экономят padding", misconception: "Переменные колонки имеют заголовок длины независимо от позиции. Их перемещение вперёд не уменьшает padding на фиксированных колонках." },
    { label: "flags BOOLEAN первым — самый маленький тип первым уменьшает общий размер строки", misconception: "Помещение 1-байтового BOOLEAN перед 8-байтовыми колонками добавляет 7 байт padding перед каждой 8-байтовой. Самые широкие первые минимизируют padding." },
    { label: "Порядок колонок не влияет на хранение в Postgres", misconception: "Порядок колонок напрямую влияет на alignment padding. [INT4, INT8, INT4] тратит 4+4=8 байт padding; [INT8, INT4, INT4] тратит 0 байт." },
  ]}
/>

</Explanation>

<DiagramFrame
  label="8КБ heap-страница хранит кортеж из 23-байтового заголовка, null bitmap, встроенных данных колонок и 18-байтового указателя TOAST; этот указатель ссылается на чанки слишком большого значения, лежащие в отдельной таблице TOAST."
  hue="var(--d-data)"
  caption="Значение больше ~2КБ сжимается, затем нарезается на чанки ~2КБ в side-таблице TOAST; основная строка хранит лишь 18-байтовый указатель. SELECT * идёт по указателю и тянет все чанки; адресный SELECT их пропускает."
>
  <svg viewBox="0 0 560 250" font-family="var(--font-body)">
    <text x="10" y="20" font-size="12.5" fill="var(--ink)">Heap-страница (8КБ)</text>
    <rect x="10" y="30" width="360" height="120" rx="6" fill="var(--card-2)" stroke="var(--hairline-strong)" stroke-width="0.8" />

    <text x="20" y="52" font-size="11" fill="var(--muted)">кортеж</text>
    <rect x="20" y="60" width="64" height="34" rx="4" fill="var(--card)" stroke="var(--hairline-strong)" stroke-width="0.8" />
    <text x="52" y="76" font-size="9.5" text-anchor="middle" fill="var(--ink)">заголовок</text>
    <text x="52" y="88" font-size="9.5" text-anchor="middle" fill="var(--muted)" font-family="var(--font-mono)">23 B</text>

    <rect x="88" y="60" width="50" height="34" rx="4" fill="var(--card)" stroke="var(--hairline-strong)" stroke-width="0.8" />
    <text x="113" y="76" font-size="9.5" text-anchor="middle" fill="var(--ink)">null</text>
    <text x="113" y="88" font-size="9.5" text-anchor="middle" fill="var(--muted)" font-family="var(--font-mono)">bitmap</text>

    <rect x="142" y="60" width="100" height="34" rx="4" fill="var(--card)" stroke="var(--hairline-strong)" stroke-width="0.8" />
    <text x="192" y="76" font-size="9.5" text-anchor="middle" fill="var(--ink)">данные колонок</text>
    <text x="192" y="88" font-size="9.5" text-anchor="middle" fill="var(--muted)" font-family="var(--font-mono)">aligned</text>

    <rect x="246" y="60" width="62" height="34" rx="4" fill="var(--card)" stroke="var(--accent)" stroke-width="1.2" />
    <text x="277" y="76" font-size="9.5" text-anchor="middle" fill="var(--ink)">TOAST</text>
    <text x="277" y="88" font-size="9.5" text-anchor="middle" fill="var(--muted)" font-family="var(--font-mono)">18 B ptr</text>

    <text x="20" y="130" font-size="10.5" fill="var(--muted)">встроенные колонки &lt; ~2КБ остаются на странице</text>

    <line x1="308" y1="77" x2="430" y2="77" stroke="var(--accent)" stroke-width="1.4" />
    <path d="M428 72 L440 77 L428 82 z" fill="var(--accent)" />
    <text x="372" y="70" font-size="9.5" text-anchor="middle" fill="var(--ink-2)" font-family="var(--font-mono)">ссылается</text>

    <text x="440" y="48" font-size="11" fill="var(--muted)">таблица TOAST</text>
    <rect x="440" y="55" width="108" height="22" rx="4" fill="var(--card-2)" stroke="var(--hairline-strong)" stroke-width="0.8" />
    <text x="494" y="70" font-size="9.5" text-anchor="middle" fill="var(--ink)">чанк 1 · ~2КБ</text>
    <rect x="440" y="80" width="108" height="22" rx="4" fill="var(--card-2)" stroke="var(--hairline-strong)" stroke-width="0.8" />
    <text x="494" y="95" font-size="9.5" text-anchor="middle" fill="var(--ink)">чанк 2 · ~2КБ</text>
    <rect x="440" y="105" width="108" height="22" rx="4" fill="var(--card-2)" stroke="var(--hairline-strong)" stroke-width="0.8" />
    <text x="494" y="120" font-size="9.5" text-anchor="middle" fill="var(--ink)">чанк 3 · ~2КБ</text>

    <text x="10" y="180" font-size="11" fill="var(--ink)">Плохой порядок колонок против хорошего (alignment padding)</text>
    <rect x="10" y="190" width="34" height="20" rx="3" fill="var(--card)" stroke="var(--hairline-strong)" stroke-width="0.8" />
    <text x="27" y="204" font-size="9" text-anchor="middle" fill="var(--ink)" font-family="var(--font-mono)">2B</text>
    <rect x="46" y="190" width="60" height="20" rx="3" fill="none" stroke="var(--muted)" stroke-width="0.8" stroke-dasharray="3 3" />
    <text x="76" y="204" font-size="9" text-anchor="middle" fill="var(--muted)" font-family="var(--font-mono)">pad 6B</text>
    <rect x="108" y="190" width="72" height="20" rx="3" fill="var(--card)" stroke="var(--hairline-strong)" stroke-width="0.8" />
    <text x="144" y="204" font-size="9" text-anchor="middle" fill="var(--ink)" font-family="var(--font-mono)">8B</text>
    <text x="190" y="204" font-size="9.5" fill="var(--muted)">= 24 B</text>

    <rect x="10" y="218" width="72" height="20" rx="3" fill="var(--card)" stroke="var(--hairline-strong)" stroke-width="0.8" />
    <text x="46" y="232" font-size="9" text-anchor="middle" fill="var(--ink)" font-family="var(--font-mono)">8B</text>
    <rect x="84" y="218" width="34" height="20" rx="3" fill="var(--card)" stroke="var(--hairline-strong)" stroke-width="0.8" />
    <text x="101" y="232" font-size="9" text-anchor="middle" fill="var(--ink)" font-family="var(--font-mono)">2B</text>
    <rect x="120" y="218" width="34" height="20" rx="3" fill="var(--card)" stroke="var(--hairline-strong)" stroke-width="0.8" />
    <text x="137" y="232" font-size="9" text-anchor="middle" fill="var(--ink)" font-family="var(--font-mono)">2B</text>
    <text x="190" y="232" font-size="9.5" fill="var(--muted)">= 16 B · сначала широкие</text>
  </svg>
</DiagramFrame>

<KeyTakeaway>Строки Postgres живут в 8КБ страницах: 23-байтовый заголовок + null bitmap + данные колонок с alignment padding. Значения шире ~2КБ TOAST-ируются в side-таблицу — SELECT * получает их все; адресные SELECT избегают этого overhead. Объявляйте фиксированные колонки сначала самыми широкими. Используйте TIMESTAMPTZ вместо TIMESTAMP, NUMERIC вместо REAL для денег, BIGSERIAL для single-region PK, UUIDv7 для распределённой уникальности.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="05-heap-and-toast-retrieval"
  lang="ru"
  questions={[
    { q: "Опишите механизм TOAST: когда он срабатывает, что делает с данными, и два следствия для производительности при дизайне схемы.", a: "TOAST срабатывает когда значение превышает примерно 2КБ внутри 8КБ страницы. Postgres сначала сжимает значение (LZ4 или pglz); если всё ещё превышает порог, нарезает на чанки ~2КБ, хранящиеся в отдельной TOAST side-таблице. Основная строка содержит 18-байтовый указатель. Следствия для производительности: (1) Чтение TOAST-колонки требует дополнительного I/O к TOAST-таблице — SELECT * на таблице с большими JSONB или TEXT колонками получает все TOAST-значения; избегайте SELECT * на горячих путях запросов. (2) Обновление TOAST-значения перезаписывает side-table чанки — обновления больших JSONB payload дороже чем обновления типизированных колонок." },
    { q: "Почему UUIDv4 — плохой выбор для primary key на большой таблице, и как UUIDv7 решает эту проблему?", a: "UUIDv4 генерируется случайно. B-tree индексы поддерживают сортированный порядок; вставка случайных значений означает что каждая новая строка попадает на случайную страницу B-tree, вызывая частые разбиения страниц и раздутие индекса. На таблицах свыше ~10M строк индекс может быть в 3-5 раз больше чем при последовательных ключах, а пропускная способность вставок деградирует. UUIDv7 предваряет UUID 48-битной временной меткой в миллисекундах, делая значения монотонно возрастающими в пределах одной миллисекунды. Это даёт B-tree вставкам такую же локальность как BIGSERIAL — новые строки добавляются на крайние правые страницы — сохраняя при этом глобальную уникальность." },
    { q: "Назовите три выбора типов, являющихся продакшн-дефолтами в Postgres, и объясните что стоит неверная альтернатива.", a: "TIMESTAMPTZ вместо TIMESTAMP: TIMESTAMP не хранит информацию о часовом поясе; в мультизоновом приложении одно и то же сохранённое значение представляет разные моменты в зависимости от timezone сессии. Стоимость неверного выбора: тихие timezone-баги, проявляющиеся только когда инженер из другого timezone выполняет запрос. NUMERIC вместо REAL для денег: REAL использует IEEE 754 с плавающей точкой, которая не может точно представить все десятичные дроби. Накопленные за тысячи финансовых транзакций, ошибки округления составляют расхождения на уровне центов. Стоимость: некорректные финансовые отчёты, регуляторный риск. TEXT вместо CHAR(n): CHAR(n) дополняет значения пробелами до заявленной длины. Trailing spaces вызывают тонкие баги сравнения. TEXT и VARCHAR(n) используют идентичное хранение; VARCHAR(n) только добавляет проверку длины при записи." },
  ]}
/>

<Recap lang="ru">
Строки Postgres хранятся в 8КБ страницах: 23-байтовый заголовок кортежа, опциональный null bitmap, затем данные колонок с alignment padding до границ выравнивания. Порядок объявления колонок влияет на padding — объявляйте фиксированные колонки самыми широкими первыми (8-байтовые, затем 4-байтовые, затем 2-байтовые, затем 1-байтовые, затем переменной длины). Значения шире ~2КБ TOAST-ируются в side-таблицу; SELECT * всегда их получает. Для выбора типов: TIMESTAMPTZ (не TIMESTAMP), NUMERIC или BIGINT-cents (не REAL/FLOAT для денег), TEXT (не CHAR(n)), BIGSERIAL для single-region PK, UUIDv7 для глобально уникальных распределённых ключей. Это решения схемы, которые трудно изменить на больших таблицах — принимайте их правильно с самого начала.
</Recap>
