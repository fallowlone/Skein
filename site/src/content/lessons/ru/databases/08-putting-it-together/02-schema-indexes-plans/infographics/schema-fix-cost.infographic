infographic sequence-ascending-steps
data
  title Цена неверного типа растёт вместе с размером таблицы
  desc Та же ошибка схемы на Day-0; счёт растёт по мере накопления строк
  order asc
  sequences
    - label 1M строк — часы
      desc Меняем неверный тип на месте; достаточно дёшево, чтобы стерпеть
    - label 100M строк — дни + окно обслуживания
      desc Смена типа по всей таблице; нужен простой или expand-contract
    - label 1B строк — проект на недели
      desc Любое изменение схемы — отдельная плановая миграция
theme
  palette #14b8a6 #f59e0b #f43f5e
