---
concepts:
- backend-process
- pgbouncer-modes
- pool-sizing
- pool-exhaustion
- idle-in-transaction
estMin: 14
lang: ru
level: senior
order: 8
prereqs: []
slug: quiz-short
sources:
- https://www.pgbouncer.org/features.html
- https://github.com/brettwooldridge/HikariCP/wiki/About-Pool-Sizing
- https://www.postgresql.org/docs/current/runtime-config-connection.html
status: ready
summary: "Промпты на свободное припоминание по всему юниту пулинга. Сначала ответь своими словами, затем раскрой модельный ответ и сравни."
title: 'Connection pooling: тест на припоминание'
track: databases
unit: 05-pooling
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";

<Hook>
Припоминание бьёт перечитывание. На каждый промпт скажи или напиши полный ответ по памяти, прежде чем открыть модельный — именно усилие припоминания закрепляет механизм так, чтобы он был под рукой в инциденте.
</Hook>

<Goal lang="ru">
Восстанови по памяти позвоночник юнита — почему backend тяжёлые, что отдаёт transaction mode, как сайзить оба слоя пула, почему exhaustion — это hold-time, и чем idle-in-transaction вредит за пределами пула.
</Goal>

<RetrievalDrawer
  client:load
  id="05-pooling-short-retrieval"
  lang="ru"
  questions={[
    { q: "Почему соединение Postgres дорогое и какие два слоя пула поглощают эту стоимость?", a: "Каждое соединение Postgres заставляет postmaster форкнуть отдельный backend OS-процесс: ~5–10 МБ резидентной памяти, слот в proc-array, запись в lock-table, плюс ~5 мс fork-and-init на сервере и TCP+TLS+auth на сети — вместе 5–50 мс на новое соединение. Поглощают это два слоя. Слой 1, client-side pool (node-postgres Pool, HikariCP, asyncpg), живёт внутри каждого воркера и кэширует 5–20 соединений, чтобы запросы пропускали per-request setup, превращая ~50 мс накладных в субмиллисекундный checkout. Слой 2, server-side pooler (PgBouncer, Supavisor, Odyssey), стоит перед Postgres и мультиплексирует тысячи дешёвых входящих клиентских соединений (~2 КБ каждое) на небольшой набор реальных backend, обходя лимит max_connections. Связка позволяет ~10 000 клиентам работать на Postgres с max_connections = 100." },
    { q: "Что даёт transaction mode PgBouncer, что он ломает и какова безопасная замена для каждой поломки?", a: "Transaction mode арендует backend лишь на длительность одной транзакции, возвращая его в пул на COMMIT/ROLLBACK; это и даёт 10–100× мультиплексирования (session mode 1:1 и не даёт ничего). Он ломает любое состояние, предполагающее стабильный backend: (1) bare SET вне транзакции — фикс через ALTER ROLE для постоянных дефолтов или SET LOCAL внутри транзакции; (2) LISTEN/NOTIFY — уведомления попадают на свободный backend, поэтому изолируй LISTEN на выделенное session-mode (или прямое) соединение; (3) классический SQL PREPARE/EXECUTE — Execute попадает на backend, не видевший Parse, поэтому используй protocol-level prepared statements драйвера (PgBouncer 1.21+ их отслеживает); (4) session-level pg_advisory_lock — фикс через pg_advisory_xact_lock; (5) temp tables — используй ON COMMIT DROP или продвинь в реальную таблицу. Ни одна из поломок не требует отказа от фичи, лишь её перемещения." },
    { q: "Как сайзить два слоя пула и почему pool_size — это НЕ число клиентов?", a: "PgBouncer pool_size (потолок реальных backend) следует формуле HikariCP (cores × 2) + effective_spindle_count плюс ~50% запаса на всплеск — на 16-ядерной NVMe-машине с рабочим набором в RAM это примерно 32 + 50% ≈ 48, а не 200. Он сайзится под CPU Postgres, потому что throughput пикует на небольшом множителе числа ядер и деградирует за ним. Per-worker client pool сайзится из конкурентности, а не из ядер: concurrent_requests × db_calls_per_request × avg_query_ms / request_budget_ms. Например 100 одновременных запросов × 2 вызова × 5 мс / 50 мс бюджет = 20 соединений на воркер. Клиентские соединения к PgBouncer дешёвые (~2 КБ), поэтому тысячи из них мультиплексируются на маленький backend-пул. Сопоставление pool_size с числом клиентов создаёт сотни активных backend за пиком throughput и ухудшает p99, а не улучшает." },
    { q: "Почему pool exhaustion почти всегда проблема hold-time и почему поднятие pool_size обычно не помогает?", a: "Exhaustion срабатывает, когда все backend выданы и нет свободного слота; на пулере это видно как рост cl_waiting, а в Postgres задействованные backend либо state=active со старым query_start (медленный запрос или lock-ожидание), либо state=idle in transaction со старым xact_start (BEGIN, удерживаемый поверх медленной работы — доминирующая причина, обычно внешний API-вызов внутри транзакции). Рычаг — как долго удерживается каждый checkout, а не сколько слотов. Поднятие pool_size с 24 до 100 даёт больше backend, удерживаемых теми же долгими транзакциями: если каждая держит 10 с, больше одновременных удержаний лишь добавляют contention proc-array и lock-менеджера, роняя per-backend throughput. Правильная последовательность: диагностируй hold (pg_stat_activity, SHOW POOLS), исправь причину (добавь индекс, вынеси API-вызов из транзакции, обеспечь COMMIT/ROLLBACK на error-путях), затем выстави idle_in_transaction_session_timeout = 60s; поднимай pool_size лишь убедившись, что checkout короткие." },
    { q: "Помимо занятого слота пула, чем ещё вредит backend в idle-in-transaction и почему?", a: "Три одновременных вреда. (1) Слот пула занят, поэтому этот backend не может обслужить другого клиента — видимый, немедленный симптом. (2) Любые строки, затронутые открытой транзакцией, остаются row-locked, поэтому другие писатели блокируются и могут каскадировать в lock-wait-очереди. (3) Транзакция пиннит MVCC-снимок: VACUUM не может вернуть dead tuples с xmin новее горизонта этого backend (backend_xmin), поэтому транзакция, открытая на час, не даёт VACUUM чистить горячую таблицу этот час, вызывая тихий bloat таблицы, растущий, пока запросы не начнут seq-scan сквозь dead tuples. Pool exhaustion — быстрый симптом; MVCC-bloat — медленный, накапливающийся. У обоих одна корневая причина, поэтому idle_in_transaction_session_timeout защищает от обоих сразу." },
    { q: "Когда тянуться за пределы PgBouncer и каков фикс serverless connection storm?", a: "PgBouncer 1.21+ — дефолт 2026 для ~80% OLTP: однопоточный C, ~2 КБ на клиентское соединение, проверенный временем, и теперь совместимый с prepared statements. Тянись за пределы лишь по измеренным данным. Если одна нода PgBouncer упирает одно ядро CPU на очень высоком QPS (около 100k QPS), горизонтально масштабируй её за HAProxy/keepalived, либо переходи на многопоточный Odyssey (наивысший single-node throughput) или PgCat (Rust, добавляет routing реплик/шардинг). Для огромных входящих счётчиков — multi-tenant SaaS с тысячами баз-арендаторов или serverless/edge — используй Supavisor (BEAM, миллионы входящих соединений) или RDS Proxy. Serverless — отдельная проблема: cold-старты открывают свежие backend и штурмуют max_connections. Фикс всегда — постоянный colocated-пулер, поглощающий всплеск, плюс HTTP-based драйвер (Neon driver, Cloudflare Hyperdrive) для настоящего edge, чтобы каждый запрос был stateless HTTP-запросом, а не постоянным backend, с агрессивными idle-таймаутами для возврата соединений мёртвых воркеров." },
  ]}
/>

<Recap lang="ru">
Если ты смог восстановить каждый ответ по памяти — ты держишь позвоночник юнита: backend — тяжёлые процессы, поэтому существуют два слоя пула: client pool на воркер, чтобы убить per-request setup, и server pooler, чтобы мультиплексировать тысячи клиентов на число backend, размеченное под CPU через (cores × 2) + spindles. Transaction mode — дефолт мультиплексирования и торгует session-state, заданным при подключении, у каждой поломки точная замена, а PgBouncer 1.21+ возвращает prepared statements. Exhaustion — это hold-time, а не размер; idle-in-transaction вдобавок пиннит MVCC-снимок и блокирует VACUUM; и ты перерастаешь PgBouncer лишь когда измерил его однопоточный потолок или столкнулся с serverless/multi-tenant масштабом.
</Recap>
