---
concepts:
- PgBouncer
- transaction-mode_pool
- session-mode
- multiplexing
- idle-in-transaction
deepensInto: []
estMin: 12
lang: ru
lessonType: topic
level: middle
order: 2
prereqs:
- databases/05-pooling/01-why-pools-exist
slug: 02-pgbouncer-modes
sources:
- https://www.pgbouncer.org/features.html
- https://www.pgbouncer.org/config.html
- https://www.pgbouncer.org/faq.html
- https://www.crunchydata.com/blog/prepared-statements-in-transaction-mode-for-pgbouncer
spiral:
- statefulness
- latency
status: ready
summary: Transaction mode даёт мультиплексирование 10–100×, но ломает SET, LISTEN,
  SQL PREPARE и session-level advisory locks; у каждого есть безопасный эквивалент
  для transaction mode.
title: 'Режимы PgBouncer: session, transaction и statement'
track: databases
unit: 05-pooling
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import NumbersCard from "~/components/layout/NumbersCard.astro";
import Misconception from "~/components/layout/Misconception.astro";
import SequenceDiagram from "~/components/diagram/SequenceDiagram.astro";

<Hook>
Команда переключает PgBouncer с session на transaction mode. Задеплоенный `SET search_path = 'app, public'` перестаёт работать. Приложение в продакшне шесть месяцев. Ничего не менялось — кроме режима pooler.
</Hook>

<Crux>Transaction mode возвращает backend после каждой транзакции; state (SET, LISTEN, PREPARE) исчезает. У каждой поломки есть безопасная замена.</Crux>

<Explanation>

## Три режима PgBouncer

**Session mode**: backend арендуется клиенту на всё время его соединения. PgBouncer по сути брокер соединений — 1:1 между клиентскими и backend-соединениями. Всё работает: SET, LISTEN, temp tables, session-level advisory locks, классический SQL PREPARE. Мультиплексирование нулевое; выигрыш только в auth offload и очереди.

**Transaction mode**: backend арендуется только на время одной транзакции (BEGIN...COMMIT, или один autocommit-запрос). Между транзакциями backend возвращается в пул и может перейти к другому клиенту. Именно этот режим даёт реальное мультиплексирование — 10 000-клиентское приложение работает на 20–50 backend. Цена: session state не сохраняется между транзакциями.

**Statement mode**: backend возвращается после каждого отдельного оператора. Транзакции из нескольких операторов запрещены. Применяется почти исключительно для PL/Proxy-style шардинга; не режим общего назначения.

**Продакшн-дефолт — transaction mode**: session mode отдаёт весь выигрыш от мультиплексирования; statement mode слишком ограничен для OLTP.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Функциональность</th>
        <th class="border border-border px-3 py-2 text-left">Session mode</th>
        <th class="border border-border px-3 py-2 text-left">Transaction mode</th>
        <th class="border border-border px-3 py-2 text-left">Фикс в transaction mode</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">Коэффициент мультиплексирования</td>
        <td class="border border-border px-3 py-2">Нет (1:1)</td>
        <td class="border border-border px-3 py-2">10–100×</td>
        <td class="border border-border px-3 py-2">—</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2"><code>SET search_path</code> при connect</td>
        <td class="border border-border px-3 py-2">Работает</td>
        <td class="border border-border px-3 py-2">Теряется после первой транзакции</td>
        <td class="border border-border px-3 py-2"><code>ALTER ROLE app SET search_path = ...</code></td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2"><code>SET LOCAL</code> внутри транзакции</td>
        <td class="border border-border px-3 py-2">Работает</td>
        <td class="border border-border px-3 py-2">Работает (в рамках той транзакции)</td>
        <td class="border border-border px-3 py-2">—</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2"><code>LISTEN / NOTIFY</code></td>
        <td class="border border-border px-3 py-2">Работает</td>
        <td class="border border-border px-3 py-2">Сломано — NOTIFY попадает на другой backend</td>
        <td class="border border-border px-3 py-2">Отдельное session-mode соединение для LISTEN</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">SQL-уровень <code>PREPARE name AS ...</code></td>
        <td class="border border-border px-3 py-2">Работает</td>
        <td class="border border-border px-3 py-2">EXECUTE попадает на другой backend</td>
        <td class="border border-border px-3 py-2">Protocol-level prepared statements (драйвер); PgBouncer 1.21+ обрабатывает их</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2"><code>pg_advisory_lock()</code> session-level</td>
        <td class="border border-border px-3 py-2">Работает</td>
        <td class="border border-border px-3 py-2">Lock принадлежит тому backend, который обработал вызов</td>
        <td class="border border-border px-3 py-2"><code>pg_advisory_xact_lock()</code> (transaction-level)</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Temp tables</td>
        <td class="border border-border px-3 py-2">Работает</td>
        <td class="border border-border px-3 py-2">Могут не сохраниться до следующей транзакции</td>
        <td class="border border-border px-3 py-2"><code>ON COMMIT DROP</code> или перевести в обычную таблицу</td>
      </tr>
    </tbody>
  </table>
</div>

## Почему SET ломается в transaction mode

В session mode соединение клиента всегда привязано к одному backend. `SET search_path = 'app, public'` при старте выполняется на backend B1 и остаётся там навсегда.

В transaction mode PgBouncer возвращает B1 в пул после первого `COMMIT`. Следующий запрос от того же клиента может попасть на B2, который никогда не видел этот `SET`. Настройка невидима — молча исчезла.

**Фикс**: `ALTER ROLE app SET search_path = 'app, public'` — Postgres применяет это к каждому новому соединению для этой роли, поэтому каждый backend стартует с правильной настройкой независимо от того, какой именно был назначен.

## Почему LISTEN ломается, и фикс через отдельное соединение

`LISTEN channel` регистрирует текущий backend для получения `NOTIFY channel` сообщений. В session mode backend постоянный — события поступают надёжно. В transaction mode backend возвращается в пул между транзакциями. `NOTIFY` попадает на тот backend, который выбирает Postgres, — он может обслуживать другого клиента; исходный listener никогда не получит событие.

**Фикс**: открываем одно выделенное соединение, которое обходит PgBouncer или использует session mode исключительно для LISTEN. Весь OLTP остаётся на transaction-mode пуле. Две connection string сосуществуют.

<Inset kind="why" lang="ru">
Зачем в PgBouncer вообще есть session mode? Для команд, мигрирующих постепенно, и для приложений, которым genuinely нужна session-семантика (legacy PREPARE, session advisory locks, долгоживущие temp tables). Session mode — безопасный дефолт; transaction mode — апгрейд производительности, требующий предварительного аудита приложения.
</Inset>

<NumbersCard
  id="card-pgbouncer-modes"
  title="Сравнение режимов PgBouncer"
  stats={[
    { label: "Session mode: мультиплексирование", value: "Нет (1:1)" },
    { label: "Transaction mode: мультиплексирование", value: "10–100×" },
    { label: "Statement mode: применение", value: "Только PL/Proxy шардинг" },
    { label: "Default pool_size", value: "20" },
    { label: "Default max_client_conn", value: "100" },
    { label: "Память PgBouncer на pooled conn", value: "~2 КБ" },
    { label: "Память backend Postgres", value: "~5–10 МБ" },
    { label: "Prepared statements в tx mode", value: "PgBouncer 1.21+" },
  ]}
/>

<Misconception id="mc-session-mode-safe">
  <p><strong>"Используем session mode — работает как прямое соединение."</strong> Session mode — выбор совместимости, не производительности. Нулевой эффект мультиплексирования: число клиентов напрямую определяет число backend'ов. Transaction mode — весь смысл пулера; планируйте миграцию.</p>
</Misconception>

<Quiz
  id="02-pgbouncer-modes-quiz-1"
  lessonSlug="02-pgbouncer-modes"
  lang="ru"
  question="Команда переключает PgBouncer с session на transaction mode, и `SET search_path = 'app, public'` при connect перестаёт работать. Почему?"
  choices={[
    { label: "В transaction mode backend арендуется на транзакцию — SET выполнился на одном backend, а последующие запросы попали на другой. Фикс: ALTER ROLE app SET search_path = ...", correct: true },
    { label: "Postgres убрал поддержку SET в последних версиях", misconception: "SET по-прежнему работает; проблема в том, какой backend выполняет какой оператор между транзакционными границами." },
    { label: "PgBouncer кэширует результаты SET и отдаёт устаревшие", misconception: "PgBouncer не кэширует SET; в этом и суть — session state не переживает транзакционные границы в transaction mode." },
    { label: "search_path чувствителен к регистру и имя схемы содержит заглавную букву", misconception: "Корневая причина — модель мультиплексирования, а не регистр." },
  ]}
/>

<Quiz
  id="02-pgbouncer-modes-quiz-2"
  lessonSlug="02-pgbouncer-modes"
  lang="ru"
  question="Лучший вариант, если приложению на PgBouncer transaction mode нужен LISTEN/NOTIFY для инвалидации кеша?"
  choices={[
    { label: "Открыть одно выделенное соединение в session mode (или напрямую к Postgres) для LISTEN; оставить основной пул в transaction mode для OLTP", correct: true },
    { label: "Переключить всё приложение на session mode", misconception: "Это лишает мультиплексирования ради функциональности, которой нужно одно выделенное соединение." },
    { label: "Поллить таблицу каждые 100 мс вместо LISTEN", misconception: "Работает, но тратит ресурсы; LISTEN на выделенном session-соединении — чистое решение." },
    { label: "Использовать NOTIFY только внутри транзакций — это исправит и LISTEN", misconception: "NOTIFY работает в transaction mode; LISTEN — поломанная сторона. Они асимметричны." },
  ]}
/>

<DragOrder
  id="02-pgbouncer-modes-drag-1"
  lessonSlug="02-pgbouncer-modes"
  lang="ru"
  prompt="Упорядочи задачи при переходе на transaction mode PgBouncer — от наибольшего к наименьшему приоритету:"
  items={[
    { id: "a", label: "Аудит SET (не SET LOCAL) — мигрировать на ALTER ROLE или SET LOCAL" },
    { id: "b", label: "Аудит классических SQL PREPARE — перейти на protocol-level (через драйвер)" },
    { id: "c", label: "Аудит LISTEN — изолировать на выделенном session-mode соединении" },
    { id: "d", label: "Аудит pg_advisory_lock (session-level) — переключить на pg_advisory_xact_lock" },
    { id: "e", label: "Включить max_prepared_statements 1000 (PgBouncer 1.21+) для protocol prepares" },
    { id: "f", label: "Установить server_reset_query = DISCARD ALL при риске утечки состояния" },
    { id: "g", label: "Установить idle_in_transaction_session_timeout в Postgres GUC как safety net" },
  ]}
  correctOrder={["a", "b", "c", "d", "e", "f", "g"]}
/>

</Explanation>

<SequenceDiagram
  label="В transaction mode PgBouncer арендует backend на одну транзакцию, затем возвращает его в пул, чтобы другой клиент мог переиспользовать."
  hue="var(--d-data)"
  actors={["Клиент A", "PgBouncer", "Postgres", "Клиент B"]}
  messages={[
    { from: "Клиент A", to: "PgBouncer", label: "BEGIN" },
    { from: "PgBouncer", to: "Postgres", label: "арендует backend B1" },
    { from: "Клиент A", to: "PgBouncer", label: "COMMIT" },
    { from: "PgBouncer", to: "Postgres", label: "возврат B1 в пул" },
    { from: "Клиент B", to: "PgBouncer", label: "BEGIN" },
    { from: "PgBouncer", to: "Postgres", label: "переиспользует тот же B1" },
  ]}
  caption="Backend удерживается лишь на одну транзакцию; между commit'ами он уходит обратно в пул — поэтому session-состояние, заданное на B1, невидимо следующему клиенту."
/>

<KeyTakeaway>Transaction mode даёт мультиплексирование 10–100×, но требует аудита: SET → ALTER ROLE; LISTEN → выделенное session-соединение; SQL PREPARE → protocol-level через драйвер; session advisory locks → transaction-level. Session mode безопасен, но отдаёт весь выигрыш от мультиплексирования.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="02-pgbouncer-modes-retrieval"
  lang="ru"
  questions={[
    { q: "Объясни, что меняется при переходе с session mode на transaction mode в PgBouncer — что работает, что ломается, что становится возможным.", a: "В session mode каждый клиент 1:1 с backend на всё время жизни соединения; всё работает идентично прямому Postgres-соединению, мультиплексирования нет. В transaction mode PgBouncer арендует backend только на транзакцию — между коммитами backend возвращается в пул и может обслуживать другого клиента. Мультиплексирование становится реальным: 10 000 клиентов на 20 backend. Что ломается: (1) SET вне транзакции — выполняется на одном backend, следующий запрос на другом. Фикс: ALTER ROLE для постоянных умолчаний, SET LOCAL внутри транзакций. (2) Классический SQL PREPARE/EXECUTE — другие backend. Фикс: protocol-level prepared statements (через драйвер); PgBouncer 1.21+ отслеживает их. (3) LISTEN — нотификации попадают на не тот backend. Фикс: выделенное session-mode соединение. (4) pg_advisory_lock (session-level) — привязан к backend, который возвращается в пул. Фикс: pg_advisory_xact_lock. (5) Temp tables — могут не пережить следующую транзакцию на другом backend. Фикс: ON COMMIT DROP." },
    { q: "Почему LISTEN ломается в transaction mode PgBouncer и каков правильный архитектурный фикс?", a: "LISTEN регистрирует текущий backend Postgres для получения нотификаций канала. Когда PgBouncer после COMMIT возвращает backend в пул, последующий NOTIFY на этот канал доставляется тому backend, который выберет Postgres — он может обслуживать другого клиента. Исходный listener никогда не получает событие. Фикс: открыть одно выделенное соединение, которое либо подключается напрямую к Postgres (минуя PgBouncer), либо использует PgBouncer в session mode; это соединение держится исключительно для LISTEN/NOTIFY. Весь OLTP остаётся на transaction-mode пуле. Два connection string сосуществуют в конфигурации приложения — это стандартный паттерн." },
    { q: "Что делает server_reset_query = DISCARD ALL в PgBouncer и когда это необходимо?", a: "server_reset_query — SQL-команда, которую PgBouncer запускает на backend перед его возвратом в idle-пул между арендами клиентов. DISCARD ALL очищает всё session state: temporary tables, cursors, prepared statements, GUC-переопределения от SET. Необходимо, когда есть риск утечки session state между клиентами — например, если приложение или библиотека вызывает SET без LOCAL и не все code paths прошли аудит. В полностью проаудированном приложении, использующем ALTER ROLE для умолчаний и SET LOCAL для per-transaction переопределений, можно оставить пустым или использовать более лёгкий вариант; но как безопасный дефолт, особенно при миграции на transaction mode, DISCARD ALL предотвращает видимость session state одного клиента другому." },
  ]}
/>

<Recap lang="ru">
PgBouncer предлагает три режима: session (1:1, нулевое мультиплексирование, всё работает), transaction (мультиплексирование 10–100×, требует аудита приложения) и statement (только для шардинг-прокси). Transaction mode — продакшн-стандарт. Он ломает четыре паттерна — `SET` вне транзакции (фикс: `ALTER ROLE`), `LISTEN` (фикс: выделенное session-mode соединение), SQL-уровень `PREPARE` (фикс: protocol-level через драйвер, PgBouncer 1.21+) и session-level advisory locks (фикс: `pg_advisory_xact_lock`). У каждой поломки есть точный эквивалент, безопасный в transaction mode; ни одна функциональность не теряется — только переносится.
</Recap>
