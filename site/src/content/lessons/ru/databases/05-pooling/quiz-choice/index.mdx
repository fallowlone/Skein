---
concepts:
- connection-pool
- pgbouncer-modes
- pool-sizing
- pool-exhaustion
- idle-in-transaction
- pooler-landscape
estMin: 13
lang: ru
level: senior
order: 7
prereqs: []
slug: quiz-choice
sources:
- https://www.pgbouncer.org/features.html
- https://github.com/brettwooldridge/HikariCP/wiki/About-Pool-Sizing
- https://www.crunchydata.com/blog/prepared-statements-in-transaction-mode-for-pgbouncer
status: ready
summary: "Тест с выбором на синтез по всему юниту: процессная модель, режимы PgBouncer, математика размера, exhaustion против idle-in-transaction и выбор пулера."
title: 'Connection pooling: тест с выбором ответа'
track: databases
unit: 05-pooling
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Шесть вопросов поперёк всего юнита. Каждый отражает решение, которое ты принимаешь в 02:14 под звон пейджера — не определение для заучивания, а компромисс, который надо взвесить, когда пул пуст, а `cl_waiting` растёт.
</Hook>

<Goal lang="ru">
Убедись, что связываешь процессную модель Postgres, режимы PgBouncer, математику размера, hold-time как корневую причину exhaustion и выбор пулера — тот синтез, к которому вели семь уроков.
</Goal>

<Quiz
  id="05-pooling-choice-1"
  lessonSlug="05-pooling"
  lang="ru"
  question="Команда держит 40 воркеров приложения, у каждого client pool max 20, против Postgres с max_connections = 100. Подключаются напрямую (без PgBouncer) и ловят 'FATAL: sorry, too many clients already'. Каков настоящий фикс?"
  choices={[
    { label: "Поставить PgBouncer в transaction mode перед Postgres: 40 × 20 = 800 дешёвых клиентских соединений (~2 КБ каждое) мультиплексируются на backend-пул ~24, и число backend Postgres остаётся сильно ниже max_connections.", correct: true },
    { label: "Поднять max_connections до 1000, чтобы все 800 клиентских соединений влезли.", misconception: "Каждый backend — это OS-процесс ~10 МБ; 800 активных backend пред-аллоцируют сотни МБ shared memory и уводят машину за пик пропускной способности. Поднятие лимита переносит симптом и нелинейно деградирует throughput." },
    { label: "Срезать client pool каждого воркера до max 2, чтобы 40 × 2 = 80 влезло в 100.", misconception: "Урезание каждого воркера до 2 соединений калечит внутреннюю конкурентность; правильный размер на воркер берётся из формулы конкурентности, а кластерный лимит решается пулером, а не недосайзингом каждого воркера." },
    { label: "Выставить GOMEMLIMIT на воркерах.", misconception: "Это ручка кучи Go, не имеющая отношения к числу backend Postgres. Ограничение тут — max_connections, который server-side pooler и существует, чтобы обойти." },
  ]}
/>

<Quiz
  id="05-pooling-choice-2"
  lessonSlug="05-pooling"
  lang="ru"
  question="Приложение на PgBouncer session mode переключается в transaction mode ради мультиплексирования. `SET search_path = 'app, public'`, выданный один раз при подключении, тихо перестаёт действовать. Почему и каков правильный фикс?"
  choices={[
    { label: "Transaction mode арендует backend лишь на транзакцию, поэтому SET выполнился на одном backend, а последующие запросы попадают на другой, который его не видел. Фикс: ALTER ROLE app SET search_path = 'app, public', чтобы каждый новый backend стартовал с ним, или SET LOCAL внутри каждой транзакции.", correct: true },
    { label: "PgBouncer кэширует результаты SET и отдаёт устаревшее значение.", misconception: "PgBouncer не кэширует и не интерпретирует SET; он лишь маршрутизирует каждую транзакцию на свободный backend. SET при подключении невидим для backend, которые его не выполняли." },
    { label: "Transaction mode полностью запрещает SET, поэтому оператор отклоняется.", misconception: "SET не отклоняется — он успешен на том backend, что его выполнил. Проблема в том, что следующая транзакция уже на другом backend, и эффект не сохраняется." },
    { label: "search_path стал чувствителен к регистру, имя схемы нужно квотировать.", misconception: "Регистр тут ни при чём; корневая причина — модель аренды backend на транзакцию, ломающая session-state, заданный при подключении." },
  ]}
/>

<Quiz
  id="05-pooling-choice-3"
  lessonSlug="05-pooling"
  lang="ru"
  question="16-ядерный Postgres (NVMe, рабочий набор в RAM) за PgBouncer. Инженер ставит default_pool_size = 200, чтобы 'покрыть пиковую конкурентность', и p99 ухудшается. Каков оптимальный по throughput pool_size и почему?"
  choices={[
    { label: "Около (16 × 2) + 0 spindles = 32, плюс ~50% запаса ≈ 48. Throughput пикует на небольшом множителе числа ядер; за ним сканы proc-array на каждом снимке и contention lock-менеджера стоят больше, чем даёт лишний параллелизм.", correct: true },
    { label: "200 верно — больше активных backend всегда означает больше throughput, значит регрессия p99 — это сетевая проблема.", misconception: "Больше backend за (cores × 2) снижают throughput: каждая выдача снимка O(active_backends), а context-switch плюс contention shared memory растут суперлинейно. Регрессия p99 и есть симптом оверсайзинга." },
    { label: "max_connections / 2 = 50, раз max_connections = 100.", misconception: "Это не формула сайзинга — max_connections — жёсткий лимит, не связанный с оптимумом throughput по CPU. pool_size выводится из ядер и параллелизма хранилища, а не из лимита." },
    { label: "Один backend на vCPU = 16, без запаса.", misconception: "Чистый ×1 недосайзит для смешанного OLTP, где backend проводят время на I/O и lock-ожиданиях; множитель ×2 плюс запас на всплеск — стандартная стартовая точка. ×1 верен лишь для чисто CPU-bound миксов запросов." },
  ]}
/>

<Quiz
  id="05-pooling-choice-4"
  lessonSlug="05-pooling"
  lang="ru"
  question="В 02:14 PgBouncer показывает pool = 24, cl_waiting = 180, приложение отдаёт 503. pg_stat_activity показывает 19 backend в состоянии 'idle in transaction' с максимальным возрастом транзакции 8 минут. Утром пул был размечен верно. Что происходит?"
  choices={[
    { label: "Это проблема hold-time, а не размера. Что-то открывает транзакцию и не делает COMMIT/ROLLBACK — скорее всего внешний API-вызов внутри BEGIN — поэтому backend сидят idle-in-transaction, удерживая слоты пула, row-локи и MVCC-снимок. Фикс: устрани hold; добавь idle_in_transaction_session_timeout как страховку.", correct: true },
    { label: "pool_size слишком мал; удвой до 48, чтобы разгрести очередь.", misconception: "Поднятие pool_size даёт больше backend, удерживаемых теми же зависшими транзакциями — больше contention proc-array, тот же hold-time. Соединения держатся слишком долго, а не их слишком мало; это маскирует причину." },
    { label: "Postgres отклоняет запросы из-за бага планировщика.", misconception: "Postgres работает корректно; backend сидят idle-in-transaction в ожидании приложения, а не падают на планировании. База ждёт приложение, а не наоборот." },
    { label: "В ОС кончились TCP-сокеты.", misconception: "TCP-exhaustion редок на таких масштабах и проявился бы ошибками уровня соединения, а не backend, сидящими idle-in-transaction со старым xact_start. Сигнал прямо указывает на удерживаемые транзакции." },
  ]}
/>

<Quiz
  id="05-pooling-choice-5"
  lessonSlug="05-pooling"
  lang="ru"
  question="Команда на PgBouncer 1.20 отключила prepared statements драйвера, чтобы избежать ошибок 'prepared statement does not exist' в transaction mode, потеряв ~20% throughput. В 2026 они хотят И мультиплексирование transaction mode, И prepared statements. Что это разблокирует?"
  choices={[
    { label: "Обновиться до PgBouncer 1.21+ и выставить max_prepared_statements = 1000. Он отслеживает prepared statements на логического клиента и прозрачно ре-препарирует их на том backend, что назначен, поэтому Execute не попадёт на backend, не видевший Parse.", correct: true },
    { label: "Выставить pool_mode = prepared_statements в pgbouncer.ini.", misconception: "Такого pool mode нет. Три режима — session, transaction и statement; поддержка prepared statements включается через max_prepared_statements внутри transaction mode." },
    { label: "Выставить server_reset_query = PREPARE ALLOWED.", misconception: "server_reset_query запускает SQL-команду очистки между арендами клиентов (напр. DISCARD ALL); к разрешению prepared statements отношения не имеет." },
    { label: "Вернуться в session mode, чтобы prepared statements работали.", misconception: "Session mode действительно сохраняет prepared statements, но отбрасывает всё мультиплексирование — ровно тот выигрыш, что они пытаются удержать. PgBouncer 1.21+ даёт оба сразу." },
  ]}
/>

<Quiz
  id="05-pooling-choice-6"
  lessonSlug="05-pooling"
  lang="ru"
  question="Multi-tenant B2B SaaS: 10 000 баз-арендаторов, ~50 одновременных пользователей в каждой, serverless-функции с cold-start под нагрузкой. PgBouncer на одной ноде упирает одно ядро CPU, а cold-старты вызывают connection storm. Какая архитектура подходит?"
  choices={[
    { label: "Scale-out пулер, рассчитанный на огромные входящие соединения — Supavisor (BEAM) или RDS Proxy — перед Postgres, плюс HTTP-based драйвер для edge, чтобы cold-старты слали stateless-запросы, а не открывали постоянные backend.", correct: true },
    { label: "Запустить по одному PgBouncer на базу-арендатор.", misconception: "10 000 однопоточных PgBouncer — операционный кошмар, и проблема serverless storm всё равно не решена. Scale-out пулер спроектирован именно под этот multi-tenant случай." },
    { label: "Оставить один PgBouncer и выставить max_client_conn = 500000.", misconception: "PgBouncer однопоточный — полмиллиона входящих клиентов через один event loop насытят одно ядро задолго до этого. Лимит — это число входящих, а не значение конфига." },
    { label: "Подключать serverless-функции напрямую, раз базы-арендаторы небольшие.", misconception: "10 000 арендаторов × 50 пользователей — до 500 000 потенциальных соединений; каждый cold-старт форкает backend ~10 МБ, и storm исчерпывают max_connections. Colocated-пулер обязан поглотить всплеск." },
  ]}
/>

<Recap lang="ru">
Сквозная линия юнита — одна цепочка: Postgres форкает тяжёлый процесс на соединение, поэтому число backend должно держаться около (cores × 2) — что и обеспечивает пулер, мультиплексируя тысячи дешёвых клиентских соединений в transaction mode. Transaction mode стоит тебе session-state, заданного при подключении (SET, LISTEN, SQL PREPARE, session advisory locks), у каждого есть точная безопасная замена, а PgBouncer 1.21+ наконец делает prepared statements совместимыми. Когда пул пустеет, причина почти всегда hold-time — медленный запрос или внешний вызов внутри транзакции — а не размер; idle_in_transaction_session_timeout — бесплатная страховка. Тянись за пределы PgBouncer лишь когда измерил его однопоточный потолок или столкнулся с serverless/multi-tenant масштабом.
</Recap>
