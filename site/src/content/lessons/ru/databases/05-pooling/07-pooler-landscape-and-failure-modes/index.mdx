---
concepts:
- PgBouncer
- outbox_pattern
- multiplexing
- connection-pool
- idle-in-transaction
deepensInto: []
estMin: 16
lang: ru
lessonType: topic
level: senior
order: 7
prereqs:
- databases/05-pooling/06-postgres-process-model
slug: 07-pooler-landscape-and-failure-modes
sources:
- https://github.com/supabase/supavisor
- https://www.pgbouncer.org/features.html
- https://www.postgresql.org/docs/current/runtime-config-connection.html
- https://github.com/brettwooldridge/HikariCP/wiki/About-Pool-Sizing
- https://www.crunchydata.com/blog/prepared-statements-in-transaction-mode-for-pgbouncer
spiral:
- statefulness
- latency
status: ready
summary: PgBouncer против Odyssey, PgCat и Supavisor — когда использовать каждый;
  connection storm при cold start serverless; восемь режимов отказов и как обнаружить
  каждый.
title: Ландшафт пулеров 2026, serverless connection storms и полная таксономия отказов
track: databases
unit: 05-pooling
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import Misconception from "~/components/layout/Misconception.astro";
import NumbersCard from "~/components/layout/NumbersCard.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";
import Infographic from "~/components/diagram/Infographic.astro";
import failureTaxonomy from "./infographics/failure-taxonomy.svg?raw";

<Hook>
B2B SaaS на Postgres достигает 5 000 QPS пика. Что использовать — PgBouncer, Odyssey, PgCat, Supavisor или RDS Proxy? Ответ зависит от того, в чём узкое место: однопоточная пропускная способность, serverless cold start, масштаб multi-tenant или маршрутизация к read-репликам.
</Hook>

<Crux>PgBouncer — дефолт (1.21+ решает prepared statements). Odyssey — QPS-потолок; PgCat — роутинг; Supavisor — edge. Cold start — иная схема.</Crux>

<Explanation>

## Ландшафт пулеров 2026

Когда принимаешь в наследство систему или проектируешь новую, выбор пулера определяет операционный потолок — что происходит при 10-кратном росте трафика, как всплеск cold start влияет на базу и каким объёмом инженерной работы ты владеешь. Сравнение ниже даёт матрицу решений для выбора правильного инструмента, а не того, который команда уже знает.

**PgBouncer** по-прежнему production-рабочая лошадка: однопоточный C, ~2 КБ на pooled-соединение, battle-tested за десятилетие, 1.21+ закрывает пробел с prepared statements. Используйте, когда нужен минималистичный, хорошо понятный пулер, располагающийся рядом с Postgres. Ограничение: однопоточность означает потолок одного ядра CPU для пропускной способности PgBouncer; при очень высоком QPS через один инстанс нужно несколько PgBouncer за HAProxy.

**Odyssey** (команда Яндекс/Tarantool): многопоточный C, нативно поддерживает prepared statements, показывает наименьшие накладные расходы CPU при высокой конкурентности в бенчмарках. Используйте, когда один нод PgBouncer становится узким местом по CPU (более ~100k QPS через один пулер). Компромисс: меньше сообщество, меньше операторов знакомо с ним.

**PgCat** (Rust): многопоточный, добавляет маршрутизацию запросов (read-реплики, шардинг) поверх пулинга. Используйте, когда нужен один компонент для пула, балансировки replica и маршрутизации запросов. Молодая кодовая база.

**Supavisor** (Supabase, Elixir/Erlang): cloud-native, спроектирован для serverless и multi-tenant SaaS, обрабатывает миллионы входящих соединений через легковесные Erlang-процессы. Используйте, когда входящих соединений массово много — serverless cold start, edge-функции, B2B SaaS с тысячами tenant-баз данных.

**pgpool-II**: традиционный выбор, объединяющий HA, балансировку read-нагрузки и пулинг. Тяжелее и сложнее; используется в legacy-развёртываниях, но менее популярен для greenfield.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Пулер</th>
        <th class="border border-border px-3 py-2 text-left">Многопоточность</th>
        <th class="border border-border px-3 py-2 text-left">Лучше всего для</th>
        <th class="border border-border px-3 py-2 text-left">Компромисс</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">PgBouncer 1.21+</td>
        <td class="border border-border px-3 py-2">Однопоточный</td>
        <td class="border border-border px-3 py-2">OLTP по умолчанию</td>
        <td class="border border-border px-3 py-2">Потолок CPU при очень высоком QPS</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Odyssey</td>
        <td class="border border-border px-3 py-2">Многопоточный</td>
        <td class="border border-border px-3 py-2">&gt;100k QPS на одном ноде</td>
        <td class="border border-border px-3 py-2">Меньше сообщество</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">PgCat</td>
        <td class="border border-border px-3 py-2">Многопоточный (Rust)</td>
        <td class="border border-border px-3 py-2">Пулинг + маршрутизация</td>
        <td class="border border-border px-3 py-2">Молодая кодовая база</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Supavisor</td>
        <td class="border border-border px-3 py-2">BEAM (Erlang/Elixir)</td>
        <td class="border border-border px-3 py-2">Serverless, edge, миллионы соединений</td>
        <td class="border border-border px-3 py-2">Сложность эксплуатации</td>
      </tr>
    </tbody>
  </table>
</div>

## Serverless и edge: connection storm

Serverless-платформы (Lambda, Vercel Functions, Cloudflare Workers) при нагрузке холодно запускают сотни и тысячи воркеров. Каждый cold start открывает одно или несколько Postgres-соединений — **connection storm**. Даже при клиентских пулах каждый cold start открывает новое соединение до прогрева пула.

Паттерны смягчения:

1. **Всегда ставить пулер перед Postgres**: RDS Proxy на AWS, Supavisor перед Supabase, PgBouncer на постоянном ноде. Пулер поглощает connection storm; Postgres видит ограниченное число backend'ов.

2. **HTTP-based драйвер для edge**: серверлесс-драйвер Neon и Cloudflare Hyperdrive отправляют запросы через stateless HTTP к endpoint пулера — от edge-воркеров до origin нет постоянных соединений. Каждый запрос — один HTTP-запрос; пулер за ним поддерживает реальные Postgres-соединения.

3. **Агрессивные idle-таймауты**: serverless-воркеры короткоживущие; простаивающие клиентские соединения от мёртвых воркеров должны быстро освобождаться. Устанавливайте `idle_client_timeout` на пулере и `tcp_keepalives_idle` в Postgres для обнаружения мёртвых соединений.

Архитектурный урок: постоянные пулы соединений предполагают долгоживущих клиентов. Serverless нарушает это. Проектируйте для stateless dispatch запросов; делегируйте управление постоянными соединениями расположенному рядом пулеру.

## Таксономия режимов отказов

<TradeoffMatrix
  id="sr-tradeoff-1"
  lessonSlug="07-pooler-landscape-and-failure-modes"
  lang="ru"
  prompt="Команда на Postgres при 5k QPS выбирает архитектуру пулинга для 2026. Выберите."
  options={[
    { name: "PgBouncer 1.21+ (transaction mode, max_prepared_statements=1000) рядом с Postgres + клиентский пул на воркер", summary: "Production-дефолт.", correct: true, justification: "Правильный ответ в ~80% случаев. PgBouncer battle-tested, минимальный расход памяти, хорошо понятен операционно. 1.21+ закрывает пробел с prepared statements. Разместите рядом для низкой задержки; один PgBouncer обрабатывает десятки тысяч QPS однопоточно. Добавьте клиентские пулы на воркер (5–20 соединений на воркер) для кэширования внутри процесса. Пересматривайте когда однопоточный CPU PgBouncer — измеренное узкое место." },
    { name: "Odyssey или PgCat как многопоточная замена PgBouncer", summary: "Более высокая пропускная способность на ноде.", justification: "Правильно когда один PgBouncer — измеренное узкое место по CPU (более 100k QPS). Odyssey — многопоточный C; PgCat добавляет маршрутизацию к репликам на Rust. Выбирайте после измерения что однопоточность PgBouncer — реальное узкое место, а не раньше." },
    { name: "Supavisor или RDS Proxy для serverless/multi-tenant SaaS", summary: "Cloud-native масштабируемые пулеры.", justification: "Правильно когда входящих соединений массово много (serverless cold start, edge-функции, тысячи tenant-баз). Supavisor обрабатывает миллионы входящих соединений; RDS Proxy — управляемый аналог. Компромисс: больше движущихся частей." },
    { name: "Прямое соединение с Postgres при большом max_connections", summary: "Наивный выбор.", justification: "Почти всегда неверно для нескольких воркеров приложения. max_connections = 1000 деградирует пропускную способность Postgres и исчерпывает общую память до появления каких-либо выгод." },
  ]}
/>

Восемь режимов отказов:

**(а) Исчерпание пула из-за медленных запросов**: отсутствующий индекс делает запрос за 5 с; при 1 000 QPS это 5 000 backend'ов в полёте, превышающих любой пул. Исправление: проиндексировать запрос, пагинировать большие сканы.

**(б) Idle-in-transaction опустошает пул**: необработанное исключение или внешний API-вызов внутри BEGIN оставляет backend'ы в `idle in transaction`. Исправление: `idle_in_transaction_session_timeout = 60s`; code review на отсутствие try/finally; outbox-паттерн для write-then-event.

**(в) Внешний API внутри транзакции**: BEGIN → платёж Stripe (таймаут = 120 с) → COMMIT. Каждый запрос в полёте удерживает backend до 120 с. Исправление: COMMIT перед вызовом; outbox-паттерн.

**(г) NAT/фаервол тихо дропает TCP**: backend выглядит простаивающим для PgBouncer, но TCP-сокет мёртв. Новые запросы получают RST или таймаут. Исправление: `tcp_keepalives_idle = 60s` в postgresql.conf; `tcp_keepalives_interval = 10s`; `client_connection_check_interval = 30s` (Postgres 14+).

**(д) Коллизии prepared statements (до PgBouncer 1.21)**: устаревший код с SQL-level PREPARE + transaction mode = "prepared statement does not exist" случайным образом. Исправление: обновить до PgBouncer 1.21+ + `max_prepared_statements > 0`; или переписать на prepared statements уровня протокола драйвера.

**(е) Утечка состояния между клиентами (DISCARD ALL не срабатывает)**: SET без LOCAL утекает от одного клиента к следующему через пул. Исправление: `server_reset_query = DISCARD ALL` в pgbouncer.ini; аудит на голые SET.

**(ж) Неправильный размер пула для пика vs стабильного состояния**: пул рассчитан на стабильную нагрузку; всплески его исчерпывают. Исправление: `reserve_pool_size` ~25% от `pool_size`; автомасштабирование на уровне приложения; очередь с деградацией (возвращать 503 с Retry-After до полного исчерпания).

**(з) Потолок однопоточного CPU PgBouncer при экстремальном QPS**: при очень высоком QPS один нод PgBouncer занимает одно ядро CPU. Исправление: горизонтальное масштабирование PgBouncer за HAProxy/keepalived для VIP-failover; или мигрировать на Odyssey/PgCat.

Все восемь режимов отказов объединяет общий диагностический паттерн: сигнал всегда виден в распределении состояний `pg_stat_activity` или счётчиках `SHOW POOLS` PgBouncer — до того как сработает пейджер. Режимы (а)–(г) — сбои времени удержания; (д)–(е) — сбои управления состоянием; (ж)–(з) — сбои ёмкости. Когда видишь рост cl_waiting, первый вопрос — к какой категории относится сбой: это сужает выбор с восьми вариантов до двух-трёх.

<Infographic
  svg={failureTaxonomy}
  label="Дерево-таксономия восьми режимов отказа пула, сгруппированных в три диагностические категории: отказы времени удержания (a-г: исчерпание пула медленными запросами, idle-in-transaction, внешний API внутри транзакции, NAT-дроп idle-TCP); отказы управления состоянием (д-е: коллизии prepared statements, утечка состояния между клиентами); отказы ёмкости (ж-з: пул на стабильную нагрузку вместо всплесков, потолок одного CPU-ядра PgBouncer)."
  caption="Диагностический ярлык: сначала отнеси симптом к категории времени удержания, управления состоянием или ёмкости — это сужает восемь режимов до двух-трёх в одной ветви."
  hue="var(--d-infra)"
/>

<Inset kind="why" lang="ru">
Почему архитектура (клиентский пул → PgBouncer → Postgres) многоуровневая, а не просто один пулер? Клиентский пул обрабатывает кэширование соединений внутри одного процесса — бесплатно на горячих путях, без установки сокета, checkout за субмиллисекунды. PgBouncer обрабатывает мультиплексирование между процессами. Убрать клиентский пул означает, что каждый запрос открывает TCP-соединение к PgBouncer — всё ещё быстро (PgBouncer локальный), но не бесплатно. Убрать PgBouncer означает, что клиентские пулы подключаются напрямую к Postgres — нормально для маленьких приложений, фатально при масштабе, когда число воркеров превышает max_connections.
</Inset>

<NumbersCard
  id="card-pooler-landscape"
  title="Критерии выбора пулера"
  stats={[
    { label: "PgBouncer: память на клиентское соединение", value: "~2 КБ" },
    { label: "PgBouncer: потолок однопоточного QPS", value: "~100k QPS" },
    { label: "Преимущество Odyssey над PgBouncer", value: "Многопоточный, меньше CPU при высокой конкурентности" },
    { label: "Целевое число входящих соединений Supavisor", value: "Миллионы (Erlang/Elixir)" },
    { label: "Паттерн HA PgBouncer", value: "Несколько инстансов за HAProxy" },
    { label: "Serverless-паттерн", value: "HTTP-based драйвер + расположенный рядом пулер" },
    { label: "Рекомендованный tcp_keepalives_idle", value: "60 с" },
  ]}
/>

<Misconception id="mc-one-pooler">
  <p><strong>«Одного пулера на кластер достаточно.»</strong> При экстремальном QPS один PgBouncer — однопоточное узкое место по CPU. HA-паттерн: два инстанса за HAProxy с keepalived для VIP-failover. Каждый держит свой пул; HAProxy балансирует клиентов. Это также даёт zero-downtime рестарты при смене конфига.</p>
</Misconception>

<Quiz
  id="07-pooler-landscape-quiz-1"
  lessonSlug="07-pooler-landscape-and-failure-modes"
  lang="ru"
  question="Команда строит multi-tenant B2B SaaS на Postgres, где у каждого tenant своя база данных. 10 000 tenant'ов × 50 параллельных пользователей каждый. Какая архитектура пулера наиболее подходит?"
  choices={[
    { label: "Supavisor (или RDS Proxy) — спроектирован для миллионов входящих соединений от multi-tenant нагрузок, обрабатывает per-tenant маршрутизацию и мультиплексирование в масштабе Erlang/Elixir", correct: true },
    { label: "По одному PgBouncer на tenant-базу", misconception: "10 000 отдельных инстансов PgBouncer — операционный кошмар; Supavisor спроектирован именно для этого случая." },
    { label: "PgBouncer с max_client_conn = 500 000", misconception: "PgBouncer однопоточный; 500 000 клиентов через один процесс не работоспособно. Supavisor обрабатывает массовые входящие соединения." },
    { label: "Прямые соединения — базы небольшие", misconception: "10 000 tenant'ов × 50 пользователей = 500 000 потенциальных соединений; даже если большинство простаивает, прямые соединения к таким Postgres backend'ам неустойчивы." },
  ]}
/>

<Quiz
  id="07-pooler-landscape-quiz-2"
  lessonSlug="07-pooler-landscape-and-failure-modes"
  lang="ru"
  question="Serverless-функции холодно запускаются под нагрузкой и каждая открывает Postgres-соединение. Что происходит без расположенного рядом пулера?"
  choices={[
    { label: "Каждый cold start открывает реальный Postgres backend (~10 МБ каждый); при всплеске нагрузки это исчерпывает max_connections и Postgres отклоняет лишних с ошибками FATAL", correct: true },
    { label: "Serverless-платформы автоматически кэшируют соединения между вызовами", misconception: "Инстансы функций запускаются с нуля при cold start; кэширование соединений требует явной инфраструктуры пулера." },
    { label: "Lambda поддерживает TCP-соединения живыми между вызовами через keep-alive заголовки", misconception: "TCP keep-alive — для HTTP-соединений к балансировщику нагрузки, а не для соединений базы данных от Lambda к Postgres." },
    { label: "У Postgres есть serverless-режим, автоматически регулирующий max_connections", misconception: "В стандартном Postgres нет serverless-режима; max_connections — фиксированный параметр компиляции/запуска." },
  ]}
/>

</Explanation>

<FlowDiagram
  label="Слоистая архитектура пулинга: внутрипроцессный client pool кэширует соединения на worker, PgBouncer мультиплексирует между всеми worker, а Postgres держит ограниченное число backend."
  perRow={3}
  hue="var(--d-data)"
  nodes={[
    { id: "app", label: "app worker", sub: "в процессе" },
    { id: "client", label: "client pool", sub: "checkout < мс" },
    { id: "pgb", label: "PgBouncer", sub: "мультиплекс, ~2 КБ" },
    { id: "pg", label: "Postgres", sub: "ограниченные backend" },
  ]}
  edges={[
    { from: "app", to: "client" },
    { from: "client", to: "pgb" },
    { from: "pgb", to: "pg" },
  ]}
  caption="Каждый слой решает свою задачу: client pool убирает setup сокета на запрос; PgBouncer мультиплексирует между процессами; Postgres видит число backend, которое реально может насытить. Убери слой — задача вернётся."
/>

<KeyTakeaway>PgBouncer — выбор по умолчанию для OLTP 2026. Берите Odyssey/PgCat когда измерен однопоточный потолок, Supavisor когда миллионы входящих соединений от serverless/edge. Serverless cold start требует расположенного рядом пулера и HTTP-based драйвера для edge. Восемь режимов отказов имеют явные исправления; наблюдаемость (cl_waiting, возраст idle-in-tx, задержка checkout пула) отличает «заметили до пейджера» от «инцидент в 3 ночи».</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="07-pooler-landscape-retrieval"
  lang="ru"
  questions={[
    { q: "Сравните PgBouncer и Odyssey — когда вы выберете Odyssey вместо PgBouncer?", a: "PgBouncer однопоточный C — обрабатывает соединения в одном цикле событий, потребляя одно ядро CPU независимо от нагрузки. Для большинства OLTP-стеков одного ядра достаточно: PgBouncer может обрабатывать ~100k простых QPS однопоточно, а пропускная способность Postgres достигает пика значительно раньше. Преимущества PgBouncer: минималистичный, battle-tested, ~2 КБ на клиентское соединение, хорошо понятен операционно большинству Postgres DBA-команд, 1.21+ теперь поддерживает prepared statements. Odyssey многопоточный C, нативно поддерживает prepared statements с более ранних версий, с меньшими накладными расходами CPU при высокой конкурентности в бенчмарках. Выбирайте Odyssey когда: (1) измерено что один нод PgBouncer насыщен по CPU и является ограничивающим фактором, и (2) горизонтальное масштабирование PgBouncer за HAProxy нежелательно операционно. Не выбирайте Odyssey умозрительно — меньше сообщество, меньше операторов, меньше инструментария." },
    { q: "Опишите проблему serverless connection storm и правильный архитектурный ответ.", a: "Serverless-платформы (Lambda, Vercel, Cloudflare Workers) запускают инстансы функций по требованию. Каждый cold start — новый OS-процесс, открывающий свежие соединения. При волнообразной нагрузке — всплеск трафика или деплой — сотни cold start могут произойти одновременно, каждый пытаясь открыть одно или несколько Postgres-соединений. При прямых соединениях каждая попытка форкает Postgres backend (~10 МБ, ~5 мс запуска). Даже 200 одновременных cold start × 1 соединение = 200 новых backend'ов — близко к max_connections = 100. Архитектура: (1) Всегда ставить пулер на постоянном ноде перед Postgres (PgBouncer, Supavisor, RDS Proxy). Пулер поглощает всплеск соединений; Postgres видит ограниченное число backend'ов независимо от числа активных Lambda-вызовов. (2) Для edge/Cloudflare Workers, где постоянный TCP к origin нецелесообразен, используйте HTTP-based Postgres-драйвер (драйвер Neon, Hyperdrive, HTTP endpoint Supavisor) — каждый запрос stateless HTTP к пулеру; пулер держит реальные соединения. (3) Устанавливайте агрессивные idle-таймауты на пулере для быстрого освобождения мёртвых worker-соединений между всплесками." },
    { q: "Назовите и опишите четыре режима отказа из таксономии пулинга и диагностический сигнал для каждого.", a: "(1) Исчерпание пула из-за медленных запросов — запросы по 5 с+ накапливаются в пуле под нагрузкой; pg_stat_activity показывает много строк state=active со старым query_start; исправление: EXPLAIN ANALYZE + добавить индекс, пагинировать большие сканы. (2) Idle-in-transaction опустошает пул — BEGIN оставлен открытым из-за бага или внешнего API; pg_stat_activity показывает много строк state=idle in transaction с xact_start минуты назад; исправление: idle_in_transaction_session_timeout = 60s + аудит кода. (3) NAT/фаервол тихо дропает TCP — PgBouncer думает что backend простаивает, но TCP-сокет мёртв; запросы возвращают RST или таймаут; исправление: tcp_keepalives_idle = 60s в postgresql.conf + client_connection_check_interval. (4) Потолок однопоточного CPU PgBouncer — процесс PgBouncer занимает одно ядро; CPU PgBouncer = 100% в метриках системы при ненулевом cl_waiting; исправление: добавить второй инстанс PgBouncer за HAProxy (active-active) или мигрировать на Odyssey/PgCat." },
  ]}
/>

<Recap lang="ru">
Ландшафт пулеров в 2026: PgBouncer 1.21+ — выбор по умолчанию для большинства OLTP-стеков — battle-tested, минималистичный и теперь полностью совместимый с prepared statements в transaction mode. Odyssey и PgCat добавляют многопоточную пропускную способность и маршрутизацию запросов для команд, переросших однопоточный потолок PgBouncer. Supavisor и RDS Proxy решают проблему serverless и multi-tenant масштаба, где число входящих соединений достигает миллионов. Serverless cold start — отдельная проблема: архитектурное решение — расположенный рядом пулер, поглощающий всплеск, плюс HTTP-based драйвер для истинного stateless edge. Восемь режимов отказов (исчерпание пула из-за медленных запросов, idle-in-transaction, внешний API внутри транзакции, NAT TCP drop, коллизия prepared statement, утечка состояния, неправильный размер на пик, потолок CPU PgBouncer) — каждый имеет явный сигнал и систематическое исправление. Наблюдаемость — `cl_waiting`, возраст idle-in-transaction, задержка checkout пула — превращает «инцидент в 3 ночи» в «алерт перед деплоем». Теперь, когда проектируешь новую систему или отвечаешь на инцидент с пулингом, первый вопрос — не «какой пулер?», а «какая категория отказа?» — и ответ определяет как само исправление, так и алерт, который предотвратит повторение.
</Recap>
