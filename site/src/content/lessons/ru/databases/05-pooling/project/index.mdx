---
concepts:
- pgbouncer-modes
- pool-sizing
- pool-exhaustion
- idle-in-transaction
- transaction-mode-migration
estMin: 240
lang: ru
level: senior
order: 10
prereqs: []
slug: project
sources:
- https://www.pgbouncer.org/config.html
- https://www.crunchydata.com/blog/prepared-statements-in-transaction-mode-for-pgbouncer
- https://www.postgresql.org/docs/current/runtime-config-connection.html
status: ready
summary: "Практический проект — поставить PgBouncer в transaction mode перед Postgres-сервисом, вызвать и диагностировать pool exhaustion из idle-in-transaction, затем доказать фикс числами до/после."
title: 'Connection pooling: укроти исчерпанный пул'
track: databases
unit: 05-pooling
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import ProjectBrief from "~/components/pedagogy/ProjectBrief.astro";

<Hook>
Читать про инцидент с pool exhaustion в 3 ночи — не то же самое, что вытаскивать сервис из него. Подними реальный стек Postgres + PgBouncer, размечай его верно, намеренно сломай внешним вызовом внутри транзакции, диагностируй дренаж по `SHOW POOLS` и `pg_stat_activity` и применяй лестницу фиксов юнита, пока числа не вернутся — с доказательствами на каждом шаге.
</Hook>

<Goal lang="ru">
Преврати ментальную модель юнита в воспроизводимый инженерный цикл: размечай оба слоя пула из первых принципов, безопасно мигрируй в transaction mode, воспроизведи exhaustion из idle-in-transaction, диагностируй его по сигналам пулера и базы, исправь hold и защити границу safety-GUC — с проверкой числами до/после под идентичной нагрузкой.
</Goal>

<ProjectBrief
  id="05-pooling-project"
  lessonSlug="05-pooling"
  lang="ru"
  objective="Поставить PgBouncer в transaction mode перед небольшим Postgres-сервисом, загнать его в pool exhaustion через паттерн idle-in-transaction и вернуть под SLO — доказывая каждый шаг измерениями, а не оценками."
  requirements={[
    "Подними Postgres (max_connections = 100) плюс PgBouncer перед ним и небольшой HTTP-сервис (Go/Node/JVM) с write-эндпоинтом, который делает BEGIN, INSERT, внешний HTTP-вызов (замокай платёж/webhook с настраиваемой задержкой), затем COMMIT — то есть внешний вызов, удерживаемый внутри транзакции.",
    "Размечай оба слоя по формулам: PgBouncer default_pool_size = (cores × 2) + spindles + ~50% запаса для твоей машины; client pool каждого воркера = concurrent_requests × db_calls × avg_ms / budget_ms. Запиши входные данные и вычисленные числа в репозиторий и выстави pool_mode = transaction.",
    "Сделай аудит transaction mode: grep по кодовой базе на bare SET, LISTEN/NOTIFY, SQL-level PREPARE и session-level pg_advisory_lock; для попаданий применяй безопасную замену (ALTER ROLE / SET LOCAL, выделенное session-соединение, protocol-level prepared statements драйвера, pg_advisory_xact_lock). Зафиксируй находки, даже если список пуст.",
    "Подключи observability: PgBouncer SHOW POOLS (cl_active, cl_waiting, sv_active, sv_idle), сэмплируемый на интервале, и запрос pg_stat_activity с группировкой по state и max(now() - xact_start). Сними здоровый baseline на steady-state нагрузке.",
    "Вызови exhaustion: выставь задержку мок-внешнего-вызова в несколько секунд (или подвесь его), затем нагружай write-эндпоинт, пока cl_waiting не вырастет и приложение не начнёт отдавать 503. Сними сигнатуру смерти: backend в state = idle in transaction с растущим возрастом транзакции, cl_waiting больше 0, пул полностью выдан.",
    "Диагностируй как on-call инженер: из SHOW POOLS и pg_stat_activity определи, что это hold-time (idle-in-transaction), а не недосайзинг. Покажи, что наивное поднятие default_pool_size НЕ фиксит (больше backend, тот же дренаж, хуже contention Postgres).",
    "Примени настоящий фикс: вынеси внешний вызов ИЗ транзакции (сначала commit, затем вызов, или используй outbox-строку), чтобы backend освобождался до медленной работы. Перезапусти идентичный нагрузочный тест и запиши числа после рядом с до.",
    "Добавь страховку: выставь idle_in_transaction_session_timeout (напр. 60s) и PgBouncer query_wait_timeout (напр. 10–30s); продемонстрируй, что со СТАРЫМ багованным code-path таймаут прерывает зависшую транзакцию и логирует, а не дренирует пул.",
  ]}
  acceptance={[
    "Таблица до/после, измеренная под одной нагрузкой: cl_waiting (пик), число backend в idle-in-transaction, p99 задержки запроса и rate ошибок/503 — измеренные, не оценённые.",
    "Доказательство, что пул теперь держит: на устойчивой нагрузке cl_waiting остаётся 0 и ни один backend не задерживается в idle-in-transaction дольше пары сотен мс, когда внешний вызов вне транзакции.",
    "Демонстрация, что поднятие одного default_pool_size не разрешило исходный дренаж (снятый прогон с сохраняющейся очередью), обосновывающая, почему hold-time-фикс ранжируется выше ресайзинга.",
    "Абзац о математике сайзинга, что ты использовал (оба слоя, с входными числами), и почему pool_size привязан к числу ядер Postgres, а не к числу клиентов.",
  ]}
  stretch={[
    "Добавь одностраничный on-call runbook: триаж по SHOW POOLS и pg_stat_activity, четыре корневые причины exhaustion (медленный запрос, idle-in-transaction, всплеск трафика, внешний API в транзакции) и чек-лист проверки для каждого фикса.",
    "Включи protocol-level prepared statements драйвера с PgBouncer 1.21+ и max_prepared_statements = 1000; нагружай 30 минут и grep по логам PgBouncer, подтверждая ноль ошибок 'prepared statement does not exist' при активном мультиплексировании transaction mode.",
    "Симулируй NAT/firewall, дропающий idle TCP (убей сокет backend вне канала), и покажи, как tcp_keepalives_idle плюс client_connection_check_interval детектируют мёртвое соединение, а не падают на следующем запросе с RST.",
    "Воспроизведи serverless connection storm: породи 200 короткоживущих клиентов одновременно против PgBouncer против прямого Postgres и покажи, что пулер ограничивает число backend Postgres, тогда как direct-connect ловит 'too many clients already'.",
  ]}
/>

<Recap lang="ru">
Это цикл, который ты будешь гонять в каждом реальном инциденте пулинга: размечай оба слоя по формулам (client pool из конкурентности, backend pool из (cores × 2) + spindles), мигрируй в transaction mode за аудитом, а когда пул пустеет — читай сигналы до того, как трогать конфиг: cl_waiting на пулере, state и возраст транзакции в Postgres — чтобы доказать, что это hold-time, а не размер. Исправь hold (вынеси внешние вызовы из транзакций, коммить до медленной работы), затем установи idle_in_transaction_session_timeout и query_wait_timeout как постоянную страховку. Сделав это раз на игрушечном стеке, ты превращаешь production-версию в мышечную память.
</Recap>
