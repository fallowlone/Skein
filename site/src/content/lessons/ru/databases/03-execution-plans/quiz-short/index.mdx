---
concepts:
- explain-analyze
- row-estimate-cascade
- scan-types
- join-algorithms
- extended-statistics
- plan-stability
estMin: 14
lang: ru
level: senior
order: 8
prereqs: []
slug: quiz-short
sources:
- https://www.postgresql.org/docs/current/using-explain.html
- https://www.postgresql.org/docs/current/planner-stats.html
- https://www.postgresql.org/docs/current/runtime-config-query.html
status: ready
summary: "Промпты на свободное припоминание по всему юниту execution plans. Сначала ответь по памяти, затем открой эталонный ответ и сравни с тем, чему учили семь уроков."
title: 'Планы выполнения: тест на припоминание'
track: databases
unit: 03-execution-plans
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";

<Hook>
Припоминание сильнее перечитывания. На каждый промпт реконструируй полный ответ по памяти, прежде чем открыть эталонный — именно усилие припоминания закрепляет дисциплину чтения планов, когда ты смотришь на медленный запрос в 2 часа ночи.
</Hook>

<Goal lang="ru">
Реконструируй хребет юнита, не подглядывая: как читать EXPLAIN ANALYZE, почему оценки строк каскадируют, как стоимость задаёт выбор scan и join, что чинят extended statistics и каков операционный playbook стабильности планов.
</Goal>

<RetrievalDrawer
  client:visible
  id="03-execution-plans-short-retrieval"
  lang="ru"
  questions={[
    { q: "Какое число в плане EXPLAIN ANALYZE самое диагностичное и почему?", a: "Разрыв между rows (оценка планировщика) и actual rows на каждом узле. Планировщик выбирает самый дешёвый план, подставляя оценки строк в формулу стоимости на каждом уровне, и эти же оценки решают выбор плана выше. Плохая оценка на листовом узле каскадирует вверх: join выше выбирает неверный алгоритм, ожидая меньший вход, сортировка выше выбирает неверный метод, а Limit выше не проталкивается вниз. Разрыв в 10x подозрителен; разрыв в 1000x — обычно причина того, что запрос на 50мс выполняется 8 минут. Поэтому читаешь план узел за узлом, считаешь actual / estimated и ведёшь самый большой разрыв к его предикату. Значения cost — в условных единицах, не в миллисекундах, так что начинать нужно с разрыва estimated-vs-actual, а не с числа cost." },
    { q: "Как планировщик выбирает между Seq Scan, Index Scan, Bitmap Heap Scan и Index Only Scan?", a: "По стоимости, которую задают селективность и cost-константы. Seq Scan читает всю кучу последовательно (relpages x seq_page_cost + reltuples x cpu_tuple_cost) и выигрывает на неселективном предикате — когда совпадает большинство строк. Index Scan идёт по B-дереву и делает один случайный heap-fetch на каждую совпавшую строку (matching_rows x random_page_cost, по умолчанию 4.0), поэтому выигрывает лишь когда строк мало, грубо менее 5-15% таблицы. Bitmap Heap Scan строит отсортированный bitmap TID из индекса и читает кучу в физическом порядке, превращая разбросанные случайные чтения в почти последовательный проход — выигрывает в середине диапазона. Index Only Scan полностью пропускает кучу, когда все нужные колонки в индексе, а Visibility Map помечает страницы all-visible; высокий Heap Fetches значит, что VM устарела. На SSD ставь random_page_cost = 1.1, чтобы планировщик корректно предпочитал индексный доступ для селективных предикатов." },
    { q: "Объясни каскад ошибок оценки строк и почему он делает Nested Loop катастрофой.", a: "Nested Loop стоит outer_rows x inner_cost_per_lookup, поэтому дёшев лишь когда внешняя сторона мала, а у внутренней есть индекс. Если планировщик недооценивает внешнюю сторону — скажем, 50 строк при реальных 520 000 — он выбирает Nested Loop, и внутренний index scan отрабатывает 520 000 раз вместо 50: раздувание на четыре порядка. Диагностика — счётчик loops на внутреннем узле. Неверный алгоритм — симптом; неверная оценка внешней стороны — причина. Недооценки приходят от устаревшей statistics, коррелированных колонок, которые планировщик считает независимыми, или non-sargable предикатов, мешающих индексу сузить число строк. Почини оценку (ANALYZE, extended statistics, sargable-переписывание), и планировщик сам выберет Hash Join или Merge Join." },
    { q: "Почему предположение планировщика о независимости проваливается на коррелированных колонках и что это чинит?", a: "Для WHERE A='x' AND B='y' планировщик оценивает P(A) x P(B), считая колонки статистически независимыми. Когда A определяет B — country определяет region, zip определяет city — совместная вероятность ближе к вероятности более селективной колонки в одиночку, поэтому перемножение недооценивает селективность, порой в 1000x. Поднятие default_statistics_target или по-колоночного SET STATISTICS лишь уточняет одноколоночные гистограммы и MCV-списки; планировщик всё равно перемножает. CREATE STATISTICS (dependencies, mcv) ON col1, col2 FROM tbl измеряет коэффициент функциональной зависимости и хранит самые частые многоколоночные кортежи во время ANALYZE, заменяя предположение о независимости для этой группы. Используй dependencies, когда одна колонка подразумевает другую, mcv для частых конкретных комбинаций и ndistinct для точной кардинальности GROUP BY. Проверяй через EXPLAIN ANALYZE, что estimated теперь отслеживает actual в пределах 2x." },
    { q: "Что такое generic-plan trap на prepared statements, как его обнаружить и как починить?", a: "Параметризованный запрос планируется с реальными параметрами на первых 5 выполнениях (custom plans). После 5-го Postgres сравнивает среднюю стоимость custom-планов с параметро-независимым generic plan; если стоимость generic в пределах примерно 10% от средней, он переключается на generic навсегда. При перекошенном распределении параметров — workspace_id, где одни workspace держат 8% строк, а другие 0.01% — generic plan настроен на типичный параметр и катастрофически неверен для выбросов, давая бимодальную задержку: низкое среднее, огромный stddev_exec_time. Обнаруживай по высокому stddev_exec_time в pg_stat_statements и EXPLAIN (GENERIC_PLAN) (PG 16+) на подготовленной форме. Чини через plan_cache_mode = 'force_custom_plan' на роли, чтобы каждое выполнение перепланировалось с реальными параметрами; лишние 0.4-2мс планирования ничтожны против многосекундного хвоста." },
    { q: "Опиши пятислойную стратегию удержания стабильности планов между деплоями.", a: "Слой 1 — cost-константы (разово): random_page_cost = 1.1 и effective_cache_size ~= 75% RAM на SSD, чтобы планировщик корректно моделировал железо; work_mem под посессионную нужду в рамках бюджета max_connections. Слой 2 — свежесть statistics: снизь autovacuum_analyze_scale_factor до ~0.02 на горячих таблицах, запускай ANALYZE в каждом деплой- и post-batch hook и создавай объекты CREATE STATISTICS для коррелированных групп колонок, найденных через EXPLAIN. Слой 3 — plan cache: plan_cache_mode = 'force_custom_plan' на ролях с перекошенными параметризованными запросами. Слой 4 — наблюдаемость: auto_explain (log_min_duration ~500мс, log_analyze, log_buffers, с семплированием) плюс еженедельный архив pg_stat_statements. Слой 5 — процедура деплоя: снимок pg_stat_statements и EXPLAIN топ-20 запросов до деплоя, ANALYZE и пересъёмка после, алёрт на любое изменение структуры плана или рост mean_exec_time свыше ~50%. Резервируй pg_hint_plan для 1-3 запросов, которые не должны регрессировать никогда; остальному дай планировщику адаптироваться." },
  ]}
/>

<Recap lang="ru">
Если ты реконструировал каждый ответ по памяти — у тебя есть хребет юнита: читай EXPLAIN ANALYZE, сравнивая estimated с actual rows на каждом узле; выбор scan следует за селективностью, выбор join — за оценкой внешней стороны; каскад ошибок оценки объясняет, почему одна плохая листовая оценка отравляет каждое решение выше; extended statistics заменяют предположение о независимости для коррелированных колонок; generic-plan trap даёт бимодальную задержку на перекошенных prepared statements; а стабильность планов — операционная дисциплина свежей statistics, cost-констант под SSD, force_custom_plan, наблюдаемости и процедуры деплоя с диффом планов.
</Recap>
