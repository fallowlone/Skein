---
concepts:
- row-estimate-cascade
- scan-type-choice
- join-algorithm
- extended-statistics
- generic-plan-trap
- plan-stability
estMin: 13
lang: ru
level: senior
order: 7
prereqs: []
slug: quiz-choice
sources:
- https://www.postgresql.org/docs/current/using-explain.html
- https://www.postgresql.org/docs/current/planner-stats.html
- https://www.postgresql.org/docs/current/sql-createstatistics.html
status: ready
summary: "Синтез в формате multiple-choice по всему юниту execution plans — чтение EXPLAIN ANALYZE, выбор scan и join, каскад ошибок оценки строк, extended statistics и generic-plan trap."
title: 'Планы выполнения: тест с выбором ответа'
track: databases
unit: 03-execution-plans
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Шесть вопросов, проходящих через весь юнит. Каждый — план, который ты реально читал бы в инциденте: не определение для пересказа, а диагноз, который нужно поставить по выводу, и фикс, который нужно выбрать под нагрузкой.
</Hook>

<Goal lang="ru">
Подтверди, что умеешь читать план EXPLAIN ANALYZE, проследить плохую оценку строк вверх до join, который она сломала, и выбрать фикс, бьющий по причине, а не по симптому — синтез, к которому вели семь уроков.
</Goal>

<Quiz
  id="03-execution-plans-choice-1"
  lessonSlug="03-execution-plans"
  lang="ru"
  question={`Запрос быстр на staging и медленный в production. Та же схема, те же индексы. EXPLAIN ANALYZE прод-прогона показывает:

Nested Loop  (actual time=4180..4192 rows=50 loops=1)
  -> Index Scan on orders (cost=0..3800000 rows=50)
       (actual time=12..3950 rows=520000 loops=1)
  -> Index Scan on customers_pkey (actual time=0.002..0.002 rows=1 loops=520000)

В чём первопричина?`}
  choices={[
    { label: "Внешний Index Scan выдал 520 000 строк, а планировщик оценил 50, поэтому выбрал Nested Loop, и внутренний scan отработал 520 000 раз. Плохая оценка строк — причина; алгоритм join — симптом.", correct: true },
    { label: "Отсутствует внутренний индекс на customers, отсюда повторные heap-сканы.", misconception: "Внутренний — это Index Scan по customers_pkey, индекс есть. Проблема в loops=520000 из-за недооценки внешней стороны, а не в отсутствующем индексе." },
    { label: "Nested Loop всегда неверен для join такого размера; форсируй Hash Join.", misconception: "Форсирование алгоритма (enable_nestloop=off) маскирует симптом. При корректной оценке в 520 000 строк планировщик сам выбрал бы Hash Join. Чини оценку, а не алгоритм." },
    { label: "В production больше данных, поэтому запрос просто пропорционально медленнее.", misconception: "Корректный план на большем объёме масштабируется плавно. Раздувание цикла в 10 400 раз из-за разрыва оценки 50 против 520 000 — это ошибка планировщика, а не линейный рост." },
  ]}
/>

<Quiz
  id="03-execution-plans-choice-2"
  lessonSlug="03-execution-plans"
  lang="ru"
  question="Таблица на 1M строк имеет индекс по status. Запрос WHERE status = 'active' попадает в 400 000 строк (40% таблицы). Планировщик выбирает Seq Scan вместо индекса. Это баг, и почему?"
  choices={[
    { label: "Не баг. При селективности 40% 400 000 случайных heap-fetch через индекс стоят гораздо дороже, чем один последовательный проход по куче. Seq Scan — корректный по стоимости выбор для неселективных предикатов.", correct: true },
    { label: "Баг. Индекс на колонке есть, значит планировщик обязан его использовать.", misconception: "Планировщик выбирает по стоимости, а не по наличию индекса. Index Scan платит random_page_cost за каждую совпавшую строку; при 40% это перевешивает один seq scan." },
    { label: "Баг. Index Only Scan всегда быстрее и должен был быть выбран.", misconception: "Index Only Scan требует всех проецируемых колонок в индексе И all-visible Visibility Map. Ни то, ни другое здесь не подразумевается, и он всё равно затронул бы 40% данных." },
    { label: "Не баг, потому что Seq Scan всегда быстрее любого индексного доступа на таблицах свыше 1M строк.", misconception: "Seq Scan выигрывает на низкой селективности, а не на размере. Для предиката, попадающего в 100 строк из 1M, индекс кардинально дешевле. Кроссовер задаёт селективность, а не число строк." },
  ]}
/>

<Quiz
  id="03-execution-plans-choice-3"
  lessonSlug="03-execution-plans"
  lang="ru"
  question="Предикат фильтрует по WHERE country='US' AND region='CA' AND status='shipped'. Планировщик прогнозирует 500 строк; узел реально выдаёт 50 000. Одноколоночная statistics свежая. Что чинит оценку?"
  choices={[
    { label: "CREATE STATISTICS (dependencies, mcv) ON country, region, status; затем ANALYZE. Country определяет region, поэтому предположение планировщика о независимости перемножает вероятности, которые независимыми не являются.", correct: true },
    { label: "ALTER TABLE ... ALTER COLUMN region SET STATISTICS 1000; затем ANALYZE.", misconception: "Больший по-колоночный target даёт более тонкие одноколоночные гистограммы и MCV-списки, но планировщик всё равно перемножает P(country) x P(region). Предположение о независимости остаётся — заменяет его только extended statistics." },
    { label: "Добавить составной индекс на (country, region, status).", misconception: "Индекс меняет пути выполнения, а не модель многоколоночной оценки строк. Оценка останется неверной, пока extended statistics не научат планировщик корреляции." },
    { label: "Поднять default_statistics_target глобально до 1000.", misconception: "Та же проблема на глобальном уровне: тоньше по-колоночная статистика, но межколоночная корреляция всё ещё не смоделирована. CREATE STATISTICS — единственный инструмент, фиксирующий функциональные зависимости." },
  ]}
/>

<Quiz
  id="03-execution-plans-choice-4"
  lessonSlug="03-execution-plans"
  lang="ru"
  question={`Параметризованный запрос быстр месяцами, затем P99 прыгает до 4с, а среднее остаётся ~3мс. pg_stat_statements показывает большой stddev_exec_time. EXPLAIN (GENERIC_PLAN) по подготовленной форме показывает:

Index Scan using idx_orders_created on orders
  Filter: ((workspace_id = $1) AND (status = $2))

Что произошло и каков немедленный фикс?`}
  choices={[
    { label: "После 5 выполнений Postgres переключился на generic plan, не видящий значения параметров, поэтому выбрал idx_orders_created и фильтрует — быстро для редких workspace, медленно для крупных. Фикс: plan_cache_mode = 'force_custom_plan' для роли.", correct: true },
    { label: "Индекс был удалён; пересоздай idx_orders_workspace_status_created.", misconception: "Составной индекс всё ещё есть — generic plan просто не может его использовать, не зная значения параметров на этапе планирования. Чинит перепланирование на каждое выполнение, а не переиндексация." },
    { label: "Рост данных перешёл переломную точку; масштабируй сервер.", misconception: "Рост данных даёт постепенную деградацию, а не бимодальную задержку (низкое среднее, высокий stddev), появляющуюся после фиксированного числа выполнений. Сигнатура прямо указывает на переключение на generic plan." },
    { label: "Пул соединений пересоздаёт соединения и очищает кэш.", misconception: "Пересоздание перезапускает 5-кратный цикл custom plan с нуля — оно не даёт устойчивой регрессии generic plan. Скачок stddev — это generic-plan trap." },
  ]}
/>

<Quiz
  id="03-execution-plans-choice-5"
  lessonSlug="03-execution-plans"
  lang="ru"
  question="B2B-дашборд деградирует каждое утро понедельника после ночного воскресного batch-импорта 20M строк, затем сам восстанавливается ко вторнику. В чём причина и устойчивый фикс?"
  choices={[
    { label: "Bulk insert сдвинул распределения, но autovacuum не успел запустить ANALYZE к понедельнику; планировщик использовал устаревшую statistics и ошибся в оценке строк. Фикс: запускать ANALYZE на затронутых таблицах в post-batch hook до возобновления трафика.", correct: true },
    { label: "Трафик в понедельник тяжелее; масштабируй сервер под недельный пик.", misconception: "Самовосстановление ко вторнику — главный признак: autovacuum в итоге запускает ANALYZE и оценки исправляются сами. Разница в трафике не лечилась бы автоматически за ночь." },
    { label: "Batch-задача держит блокировки до понедельника; запросы ждут за ней.", misconception: "Конкуренция за блокировки проявляется как заблокированные запросы в pg_stat_activity и снимается при commit транзакции — а не сутки спустя. Паттерн соответствует устаревшей statistics, а не блокировкам." },
    { label: "Таблица переросла свои индексы; делай REINDEX каждый понедельник.", misconception: "Полезность индекса задаёт селективность, а не размер, и перестройки не объяснили бы самовосстановление во вторник. Первопричина — свежесть statistics, чинится через ANALYZE в batch hook." },
  ]}
/>

<Quiz
  id="03-execution-plans-choice-6"
  lessonSlug="03-execution-plans"
  lang="ru"
  question="EXPLAIN (ANALYZE, BUFFERS) на Hash Join показывает Hash Batches: 64 и Sort Method: external merge Disk: 450MB на сортировке под ним. Среднее время высокое, но reads — в основном shared hit. В чём узкое место и точечный фикс?"
  choices={[
    { label: "И хэш-таблица, и сортировка превысили work_mem и пролились на диск; стоимость — CPU/IO от дисковых batches и external merge. Фикс: поднять work_mem посессионно для этого паттерна запроса, а не глобально без бюджета по max_connections.", correct: true },
    { label: "Запрос упирается в IO на холодных данных; добавь RAM и shared_buffers.", misconception: "Buffers — в основном shared hit, значит heap-страницы закэшированы — это не проблема холодного IO. Время съедает spill на диск из-за переполнения work_mem." },
    { label: "Отсутствует индекс по join-ключу; добавь его, чтобы избежать хэша.", misconception: "Индекс с подходящей сортировкой мог бы включить Merge Join, но немедленный измеренный симптом — spill work_mem на обоих узлах. Размер work_mem чинит spill напрямую." },
    { label: "Batches: 64 означает, что join отработал 64 раза в цикле; перепиши запрос.", misconception: "Hash Batches — это число дисковых партиционированных проходов, когда хэш-таблица не влезает в work_mem, а не счётчик цикла. Фикс — больше work_mem, а не переписывание." },
  ]}
/>

<Recap lang="ru">
Сквозная линия юнита — одна дисциплина чтения: на каждом узле плана сравнивай rows-estimated с actual rows, найди, где открывается разрыв, и проследи его вверх — плохая оценка каскадирует в неверный выбор scan, неверный алгоритм join и неверный метод сортировки над ней. Выбор scan следует за селективностью, выбор join — за оценкой внешней стороны, а устойчивые фиксы бьют по причине: свежая statistics через ANALYZE, extended statistics для коррелированных колонок, force_custom_plan для перекошенных prepared statements и cost-константы под SSD. Форсирование алгоритма или перестройка индекса лечат симптом; починка оценки исправляет весь план.
</Recap>
