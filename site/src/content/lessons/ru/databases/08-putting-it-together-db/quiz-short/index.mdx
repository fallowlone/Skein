---
concepts:
- normalization-tradeoffs
- index-cost-model
- row-estimate-cascade
- snapshot-isolation
- pool-sizing-math
- shard-key-selection
estMin: 14
lang: ru
level: senior
order: 8
prereqs: []
slug: quiz-short
sources:
- https://www.postgresql.org/docs/current/mvcc.html
- https://www.postgresql.org/docs/current/indexes.html
- https://wiki.postgresql.org/wiki/Number_Of_Database_Connections
status: ready
summary: "Промпты на свободное припоминание поперёк трека баз данных — схема, индексы, планы, MVCC, пулинг, миграции, шардинг. Сначала ответь по памяти, затем раскрой и сравни."
title: 'Трек баз данных: тест на припоминание'
track: databases
unit: 08-putting-it-together-db
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";

<Hook>
Припоминание бьёт перечитывание. На каждый промпт восстанови полный ответ по памяти, прежде чем открыть образец — они идут поперёк всего трека, поэтому именно усилие связать понятия закрепляет их. К концу ты точно увидишь, что из трека у тебя уже сидит, а что ты до сих пор угадываешь.
</Hook>

<Goal lang="ru">
Восстанови хребет трека, не подглядывая: когда гнуть нормализацию, как cost-модель индекса выбирает скан, почему ошибки оценки строк каскадируются, что snapshot-изоляция защищает и не защищает, математика размера пула и как ключ шардирования ко-локирует или рассыпает работу.
</Goal>

<RetrievalDrawer
  client:load
  id="08-putting-it-together-db-short-retrieval"
  lang="ru"
  questions={[
    { q: "Когда правильно гнуть реляционные правила — денормализовать, хранить JSONB или даже отключать foreign keys?", a: "Реляционная модель по умолчанию нормализует, потому что это убирает аномалии обновления и даёт constraints гарантировать integrity. Гнёшь осознанно, когда паттерн чтения это оплачивает: денормализуй счётчик или rollup, когда join для пересчёта на горячем пути, а усиление записи приемлемо; храни JSONB или массивы, когда форма действительно переменная или разреженная и тебе никогда не нужно constraint-ить или индексировать её внутренности как полноценные колонки. Некоторые hyperscale-конторы отключают foreign keys, потому что FK-проверки добавляют стоимость блокировок и латентности при экстремальной записи, а integrity они держат в приложении или пайплайне. Каждое сгибание меняет гарантию, которую раньше давала база, на пропускную способность — делай это только когда можешь назвать отдаваемую гарантию и того, кто теперь ей владеет." },
    { q: "Как планировщик выбирает между index scan и sequential scan и почему ведущая колонка composite-индекса так важна?", a: "Планировщик оценивает стоимость каждого пути доступа по статистике таблицы: seq scan читает каждую страницу раз (дёшево на строку, дорого в сумме); index scan делает random I/O на каждую подходящую строку плюс heap fetch, и выигрывает только когда предикат настолько селективен, что строк мало. Селективность берётся из статистики (n_distinct, гистограммы, MCV-списки). B-tree умеет seek только по префиксу колонок, поэтому composite-индекс помогает запросу, лишь если запрос ограничивает ведущие колонки; index (a, b) обслуживает WHERE a = ? и WHERE a = ? AND b = ?, но не WHERE b = ? в одиночку. Поэтому leading-column rule — самый нарушаемый принцип в продакшне: индекс добавляют, но порядок колонок ставят под неверный паттерн доступа, и планировщик тихо делает seq scan." },
    { q: "Почему ошибки оценки строк каскадируются в катастрофически плохие планы и как стабилизировать план под нагрузкой?", a: "Планировщик строит план снизу вверх: оценка числа строк каждого узла кормит стоимость и выбор алгоритма соединения узла выше. Если оценка скана промахивается в 1000 раз — устаревшая статистика, коррелированные колонки, которые планировщик считает независимыми, или значение вне гистограммы — ошибка умножается вверх по дереву join, и планировщик выбирает nested loop, ожидая 12 строк, и выполняет его два миллиона раз. Стабилизируй, чиня вход, а не пиннингуя выход: запусти ANALYZE, подними statistics target на перекошенных колонках, добавь CREATE STATISTICS для коррелированных колонок и перепиши запросы так, чтобы план вели оцениваемые предикаты. Управление планами или форсирование плана — последнее средство, маскирующее промах, а не лечащее его." },
    { q: "От чего защищает snapshot-изоляция MVCC в PostgreSQL, от чего нет и как долгая транзакция вызывает bloat таблицы?", a: "Под MVCC каждая транзакция видит согласованный снимок, поэтому читатели не блокируют писателей и наоборот; дефолтный READ COMMITTED берёт свежий снимок на каждый statement, REPEATABLE READ — один на транзакцию. Snapshot-изоляция предотвращает грязные чтения и (на REPEATABLE READ) неповторяемые чтения, но не предотвращает write skew — для этого нужен SERIALIZABLE, добавляющий отслеживание предикатов и способный отменить транзакции с serialization failure, который надо ретраить. Bloat: каждый UPDATE/DELETE оставляет старую версию tuple на месте, освобождаемую лишь когда ни один снимок её больше не видит. Транзакция, открытая часами, пиннит старый снимок, поэтому VACUUM не может убрать dead tuples, созданные тем временем, и таблица (с её индексами) растёт неограниченно, пока та транзакция не завершится." },
    { q: "Дай рассуждение о размере пула PgBouncer перед Postgres: почему backend тяжёлый, какого размера серверный пул и в чём ловушка prepared statement в transaction mode?", a: "Каждый backend Postgres — полноценный процесс ОС со своей памятью, дорогой и в создании, и в простое, поэтому нельзя дать тысячам коннектов приложения мапиться 1:1 в backends. PgBouncer мультиплексирует много клиентских коннектов на небольшой серверный пул; в transaction mode серверный коннект берётся лишь на одну транзакцию, поэтому горстка занятых backends обслуживает тысячи клиентов. Полезная стартовая точка для серверного пула — примерно (core_count * 2 + effective_spindle_count) для CPU-bound работы, затем мерить по реальной конкурентности — гораздо меньше, чем ждёт большинство команд, ведь бездействующий коннект всё равно стоит. Ловушка: transaction mode ломает всё, что предполагает стабильную сессию — серверные prepared statements и сессионный SET исчезают между транзакциями; PgBouncer 1.21+ добавляет поддержку prepared statements на уровне протокола, иначе отключай их или переноси настройку в каждую транзакцию." },
    { q: "Как выбрать ключ шардирования, почему низкокардинальный ключ создаёт hot shard и каков безопасный порядок действий до шардинга вообще?", a: "Хороший ключ шардирования высококардинален, равномерно распределяет нагрузку записи и чтения и ко-локирует строки, которые запрашиваются или джойнятся вместе, так что большинство операций остаётся на одном шарде — tenant_id в multi-tenant-приложении каноничен. Низкокардинальный или перекошенный ключ (country, status, бакет даты) мапит непропорциональный реальный трафик на один шард: даже равномерный хеш ровно раскладывает различимые значения, но если несколько значений несут большую часть нагрузки, они садятся на один узел — получаешь hot shard, убивающий смысл. Безопасный порядок: сначала исчерпай рычаги одного узла — фиксы схемы и индексов, стабилизацию планов, пулинг соединений, read-реплики под чтение — потому что шардинг добавляет cross-shard join, распределённые транзакции, ребалансировку и операционный вес. Затем выбирай declarative partitioning для данных вида time-series на одном узле или Citus / directory-слой ради настоящего горизонтального масштаба записи и планируй online resharding с первого дня." },
  ]}
/>

<Recap lang="ru">
Если ты смог восстановить каждый ответ по памяти, ты держишь хребет трека от начала до конца: нормализуй по умолчанию и гни лишь ради названного, чьего-то компромисса; пусть ведущая колонка индекса совпадает с паттерном доступа; считай оценки строк несущим входом планировщика; держи транзакции короткими, чтобы MVCC мог освобождать; мультиплексируй тяжёлые backends через небольшой измеренный пул; выстраивай ломающие изменения как expand-contract; и шардь последним, на высококардинальном ко-локирующем ключе, лишь когда исчерпан каждый рычаг одного узла. Теперь, когда увидишь медленный запрос, раздувшуюся таблицу или storm соединений, ты знаешь, за какой слой хвататься первым — и какие данные собрать, прежде чем объявлять фикс.
</Recap>
