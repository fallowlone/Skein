---
concepts:
- schema-modeling
- index-selection
- plan-reading
- mvcc-isolation
- pool-sizing
- expand-contract
estMin: 14
lang: ru
level: senior
order: 7
prereqs: []
slug: quiz-choice
sources:
- https://www.postgresql.org/docs/current/mvcc.html
- https://www.postgresql.org/docs/current/using-explain.html
- https://www.citusdata.com/blog/2023/01/10/sharding-postgres-with-citus/
status: ready
summary: "MCQ на синтез поперёк трека: связать схему, индексы, планы, MVCC, пулинг, миграции и шардинг в одно дерево решений, ведущее продукт от MVP до шардированного кластера."
title: 'Трек баз данных: тест с выбором ответа'
track: databases
unit: 08-putting-it-together-db
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Шесть вопросов поперёк всего трека. Каждый — момент в жизни одного продукта (от единственной таблицы до кластера на миллиард строк), где сталкиваются два понятия из баз данных и нужно выбрать рычаг, чинящий именно нужный слой.
</Hook>

<Goal lang="ru">
Убедись, что собираешь трек в цепочку: смоделировать схему, выбрать индекс, который планировщик реально использует, прочитать, почему план сломался, выбрать уровень изоляции, рассчитать размер пула, выстроить миграцию без даунтайма и решить, нужен ли шардинг вообще.
</Goal>

<Quiz
  id="08-putting-it-together-db-choice-1"
  lessonSlug="08-putting-it-together-db"
  lang="ru"
  question="Запрос фильтрует orders по тенанту и дате, ты добавляешь composite-индекс. Оба плана ниже — по одной таблице, различается лишь порядок колонок индекса. Почему планировщик пропускает индекс в плане A?

```sql
-- query
SELECT * FROM orders WHERE tenant_id = 42 AND created_at >= '2026-01-01';
-- A: CREATE INDEX ON orders (created_at, tenant_id);   -> Seq Scan
-- B: CREATE INDEX ON orders (tenant_id, created_at);   -> Index Scan
```"
  choices={[
    { label: "Leading-column rule: B-tree эффективно ищет только по префиксу колонок. Индекс A ведёт с created_at, поэтому tenant_id не может задать probe, и планировщик откатывается к seq scan; индекс B ведёт колонкой равенства tenant_id, затем диапазон по created_at.", correct: true },
    { label: "Индекс A просто больше на диске, поэтому планировщик его избегает.", misconception: "Оба индекса покрывают одни и те же две колонки и почти одинаковы по размеру. Выбор определяется тем, совпадает ли ведущая колонка с паттерном доступа, а не размером индекса." },
    { label: "Postgres никогда не использует composite-индексы для смеси equality + range.", misconception: "Использует — именно для этого нужен индекс B. Равенство по ведущей колонке, затем диапазон по следующей — каноничный выигрыш composite-индекса." },
    { label: "Колонки индекса нужно перечислять в том же порядке, что и в тексте WHERE.", misconception: "Порядок текста WHERE не важен. Важно ставить колонки равенства первыми, затем колонку диапазона — leading-column rule, а не порядок в исходнике." },
  ]}
/>

<Quiz
  id="08-putting-it-together-db-choice-2"
  lessonSlug="08-putting-it-together-db"
  lang="ru"
  question="Ночной batch держит одну транзакцию открытой три часа. За это же окно OLTP-таблица под активными UPDATE растёт с 8 ГБ до 40 ГБ на диске, хотя число живых строк почти не меняется. Что связывает долгую транзакцию с bloat?"
  choices={[
    { label: "MVCC хранит каждую версию строки, видимую старейшему снимку. 3-часовая транзакция держит старый снимок, поэтому VACUUM не может освободить dead tuples от OLTP-апдейтов — они копятся как bloat, пока та транзакция не завершится.", correct: true },
    { label: "Batch вставляет 32 ГБ новых строк.", misconception: "Число живых строк почти не меняется — это не рост от вставок. Это накопление dead tuples: апдейты создают новые версии, а старые нельзя вакуумировать, пока держится долгий снимок." },
    { label: "UPDATE переписывает всю таблицу раз за транзакцию.", misconception: "UPDATE создаёт новую версию tuple на каждую изменённую строку, а не полный rewrite таблицы. Bloat — от неосвобождаемых мёртвых версий, а не от переписывания." },
    { label: "Повышение изоляции до SERIALIZABLE предотвратило бы bloat.", misconception: "Более строгая изоляция держит снимки как минимум столько же, если не дольше. Лекарство — не держать одну транзакцию открытой часами, а не ужесточать изоляцию." },
  ]}
/>

<Quiz
  id="08-putting-it-together-db-choice-3"
  lessonSlug="08-putting-it-together-db"
  lang="ru"
  question="Отчётный запрос был быстрым месяцами, потом внезапно выбрал nested-loop join вместо hash join и вырос с 200 мс до 40 с после всплеска импорта данных. EXPLAIN ANALYZE показывает: планировщик ждал 12 строк из одной ветки, получил 2,1 млн. Корневая причина и первый устойчивый фикс?"
  choices={[
    { label: "Ошибка оценки строк: устаревшая или недостаточная статистика убедила планировщик, что одна ветка крошечная, и он выбрал nested loop. Первый фикс: ANALYZE таблицы (и поднять statistics target колонки / добавить extended statistics, если промах от коррелированных колонок), чтобы оценка совпала с реальностью.", correct: true },
    { label: "Nested-loop join всегда ошибочен, и в планировщике баг.", misconception: "Nested loop оптимален, когда одна сторона действительно крошечная. Дефект — не алгоритм соединения, а оценка строк, скормившая выбор. Чини статистику, а не join." },
    { label: "Добавить index hint, чтобы навсегда форсить hash join.", misconception: "В Postgres нет синтаксиса хинтов, а пиннинг одного плана маскирует проблему. Промах оценки исказит и другие запросы; обновление статистики чинит вход, который читает планировщик." },
    { label: "Запросу нужно больше work_mem, чтобы hash join влез в память.", misconception: "work_mem влияет на то, сольётся ли выбранный hash на диск, но здесь планировщик hash join вообще не выбрал. Триггер — промах кардинальности, проблема статистики." },
  ]}
/>

<Quiz
  id="08-putting-it-together-db-choice-4"
  lessonSlug="08-putting-it-together-db"
  lang="ru"
  question="Ты ставишь приложение за PgBouncer в transaction mode, чтобы пережить storm соединений. Сразу же приложение падает с ошибкой, что prepared statements не существуют, а SET search_path, выполненный при коннекте, больше не держится. Почему?"
  choices={[
    { label: "В transaction mode клиент держит серверное соединение лишь на одну транзакцию, затем оно возвращается в пул. Состояние сессии — серверные prepared statements и SET уровня сессии — не переживает границу транзакций, поэтому ломается. Фиксы: PgBouncer 1.21+ с поддержкой prepared statements на уровне протокола, либо делать настройку сессии внутри каждой транзакции.", correct: true },
    { label: "PgBouncer портит wire-протокол для prepared statements.", misconception: "Он ничего не портит. Поведение by design: transaction mode мультиплексирует много клиентов через мало серверных соединений, поэтому всё, что предполагает стабильное соединение сессии, ломается." },
    { label: "Пул слишком мал, поэтому prepared statements вытесняются.", misconception: "Размер пула управляет конкурентностью, а не временем жизни statement. Statements исчезают потому, что в transaction mode клиент не держит то же серверное соединение между транзакциями." },
    { label: "Переход в session mode даст ту же мультиплексацию без минусов.", misconception: "Session mode сохраняет состояние сессии, но пиннит одно серверное соединение на клиента на всю сессию, убивая мультиплексацию, которая решила storm соединений. Это реальный компромисс, а не бесплатный фикс." },
  ]}
/>

<Quiz
  id="08-putting-it-together-db-choice-5"
  lessonSlug="08-putting-it-together-db"
  lang="ru"
  question="Нужно переименовать горячую колонку `email` в `email_address` на таблице с 500 млн строк без даунтайма, пока старый и новый код приложения работают бок о бок во время rolling-деплоя. Какая последовательность верна?"
  choices={[
    { label: "Expand-contract: (1) добавить новую колонку, (2) dual-write обеих колонок из приложения и backfill новой батчами, (3) переключить чтения на новую колонку после проверки backfill, (4) перестать писать старую колонку, (5) дропнуть старую в следующем релизе. Обе колонки существуют всё время, поэтому старый и новый код сосуществуют безопасно.", correct: true },
    { label: "Выполнить ALTER TABLE ... RENAME COLUMN одной миграцией; это только метаданные и мгновенно.", misconception: "RENAME быстр на каталоге, но это ломающее изменение: в момент коммита старый код по старому имени падает. С двумя живыми версиями при rolling-деплое нужен expand-contract, а не атомарный rename." },
    { label: "Взять короткое окно обслуживания, переименовать и передеплоить всё разом.", misconception: "Требование — ноль даунтайма с сосуществующими версиями. Окно плюс big-bang деплой нарушают оба ограничения; expand-contract — то, что даёт двум версиям кода перекрываться." },
    { label: "Добавить новую колонку с volatile DEFAULT и NOT NULL одним ALTER на 500 млн строк.", misconception: "NOT NULL плюс volatile default форсит полный rewrite таблицы под ACCESS EXCLUSIVE lock, замораживая таблицу и ставя в очередь все ждущие запросы — режим отказа очереди блокировок. Делай backfill батчами." },
  ]}
/>

<Quiz
  id="08-putting-it-together-db-choice-6"
  lessonSlug="08-putting-it-together-db"
  lang="ru"
  question="Multi-tenant SaaS упирается в лимит записи на одном Postgres. Команда предлагает hash-шардинг по колонке `country`. Три тенанта в одной стране дают 70% трафика. Что пойдёт не так и какой ключ шардирования лучше?"
  choices={[
    { label: "Хеш по country создаёт hot shard: три кита-тенанта в одной стране хешируются в один шард, поэтому один узел берёт 70% записи, и шардинг почти ничего не даёт. Шардь по tenant_id — высокая кардинальность и ко-локация строк каждого тенанта для локальных join; тенант, слишком большой для одного шарда, нуждается в собственной изоляции, а не более грубом ключе.", correct: true },
    { label: "Ничего — хеш всегда раскладывает нагрузку ровно, ведь хеш-функция равномерна.", misconception: "Равномерный хеш раскладывает ключи ровно, но нагрузка — на ключ, а не на различимое значение. Низкокардинальный перекошенный ключ вроде country концентрирует реальный трафик в один шард независимо от равномерности хеша." },
    { label: "Шардить по случайному UUID на строку, чтобы гарантировать ровное распределение.", misconception: "Случайный пошардовый шардинг раскладывает запись, но рассыпает строки каждого тенанта по всем шардам, превращая любой запрос и join тенанта в cross-shard fan-out. Ко-локация по tenant_id — в этом весь смысл." },
    { label: "Шардинг — верный первый ход, как только запись становится тяжёлой.", misconception: "Шардинг — последнее средство после исчерпания схемы, индексирования, тюнинга планов, пулинга и read-реплик — он добавляет cross-shard join, ребалансировку и операционную стоимость. Сначала убедись, что потолок одного узла реально достигнут." },
  ]}
/>

<Recap lang="ru">
Трек — одно дерево решений для растущего продукта: сначала смоделируй схему ради integrity, добавь индекс, чья ведущая колонка совпадает с паттерном доступа, читай план, чтобы ловить ошибки оценки строк до каскада, держи транзакции короткими, чтобы MVCC мог вакуумить, рассчитай размер и режим пула, чтобы пережить storm соединений, разложи каждое ломающее изменение на фазы expand-contract и берись за шардинг только после выбора высококардинального ко-локирующего ключа и исчерпания всех более дешёвых рычагов. Каждый режим отказа сводится к одному вопросу: какой слой — узкое место и какой самый дешёвый рычаг его чинит?
</Recap>
