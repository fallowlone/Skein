---
concepts:
- mvcc
- snapshot
- vacuum
- lost-update
deepensInto:
- databases/04-mvcc-isolation/02-row-versions-and-snapshots
estMin: 12
lang: ru
lessonType: topic
level: junior
order: 1
prereqs:
- databases/03-execution-plans/01-explain-and-plans
slug: 01-mvcc-basics
sources:
- https://www.postgresql.org/docs/current/mvcc.html
- https://www.postgresql.org/docs/current/transaction-iso.html
spiral:
- statefulness
status: ready
summary: MVCC хранит историю версий строк, чтобы читатели никогда не блокировали писателей,
  а писатели — читателей; обратная сторона — накопление мёртвых tuple, которые убирает
  VACUUM.
title: 'MVCC: как Postgres раздаёт согласованные снимки'
track: databases
unit: 04-mvcc-isolation
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import MetaphorComplete from "~/components/pedagogy/MetaphorComplete.astro";
import SequenceDiagram from "~/components/diagram/SequenceDiagram.astro";
import Infographic from "~/components/diagram/Infographic.astro";
import rowVersionLifecycle from "./infographics/row-version-lifecycle.svg?raw";

<Hook>
Пятиминутный аналитический запрос и тысячи транзакций оформления заказов идут параллельно. Ни один не блокирует другой. Как Postgres это делает?
</Hook>

<Crux>MVCC не перезаписывает строки: UPDATE — новая версия, DELETE — пометка мёртвой. Транзакция читает через snapshot без блокировок.</Crux>

<Explanation>

## Что делает MVCC

MVCC хранит историю каждой строки так, чтобы каждая транзакция видела стабильную картину базы, не блокируя строки, которые трогают другие.

Без MVCC пятиминутный аналитический запрос замораживал бы каждый входящий заказ, а твой дашборд либо врал бы про итоги, либо блокировал кнопку оформления. К концу урока ты поймёшь, почему этого не происходит — и где единственная дыра, которую MVCC оставляет на твою совесть.

## Метафора

Представь шумную библиотеку, где у каждой книги много копий, помеченных датой, когда их взяли с полки. Когда Свен просит «каталог на момент, когда я начал читать», библиотекарь выдаёт ему помеченную копию, замороженную на ту минуту — даже если Отто в это же время за стойкой сшивает новое издание.

Свен спокойно читает свою копию. Отто добавляет новое издание. Никто не ждёт никого; оба смотрят на разные датированные копии одной полки.

Позже уборщик проходит и выкидывает копии, которые никто больше не читает. Эта уборка — то, что Postgres называет **VACUUM**. Уборщик выбрасывает копию только тогда, когда ни один читатель её больше не держит.

## Свен и Отто

**Свен** — сервер приложения, запустивший месячный отчёт. **Отто** — база с тысячами заказов в секунду.

Без MVCC отчёт либо блокирует orders (заморозив оформление), либо видит недописанные обновления. С MVCC Свен получает snapshot на момент старта отчёта; новые заказы идут в свежие версии, невидимые Свену. Отчёт заканчивается, оформление не зависает.

## Одна ловушка, которую MVCC не решает

Две вкладки редактируют один профиль. Вкладка A загрузила, ты печатаешь. B загрузила, изменила одно поле, сохранила. Сохраняешь A — правка B молча исчезла. Это **lost update**.

MVCC сам по себе не лечит — попроси базу (`SELECT FOR UPDATE` или строже уровень изоляции).

## Как это выглядит на диске

| Без MVCC | С MVCC |
|----------|--------|
| Строка перезаписывается на месте | Старая версия остаётся, новая добавляется рядом |
| Читатель ждёт писателя | Читатель видит старую версию, не ждёт |
| Откат — перезапись обратно | Откат — просто игнорировать новую версию |
| Нет истории для точки восстановления | Snapshot транзакции всегда согласован |

</Explanation>

<SequenceDiagram
  label="MVCC позволяет читающей и пишущей транзакциям идти одновременно, не блокируя друг друга."
  hue="var(--d-data)"
  actors={["Читатель (Свен)", "Писатель (Отто)"]}
  messages={[
    { from: "Читатель (Свен)", to: "Читатель (Свен)", label: "BEGIN: взят snapshot, читает строку v1" },
    { from: "Писатель (Отто)", to: "Писатель (Отто)", label: "UPDATE: создаёт строку v2, помечает v1 мёртвой" },
    { from: "Писатель (Отто)", to: "Писатель (Отто)", label: "COMMIT: v2 видна новым снимкам" },
    { from: "Читатель (Свен)", to: "Читатель (Свен)", label: "всё ещё читает v1 (его snapshot старше v2)" },
  ]}
  caption="Время течёт сверху вниз. Читатель продолжает видеть v1 из своего замороженного snapshot, пока писатель коммитит v2 — ни одна транзакция не ждёт другую."
/>

## Проверь себя

<Quiz
  id="quiz-1"
  lessonSlug="01-mvcc-basics"
  lang="ru"
  question="Что на самом деле делает MVCC?"
  choices={[
    { label: "Блокирует каждую строку, которую трогает транзакция", misconception: "Блокировки — это как раз то, что MVCC заменяет для чтений. Читатели никогда не блокируются на изменяющихся данных." },
    { label: "Хранит несколько версий каждой строки, чтобы разные транзакции видели согласованные картины", correct: true },
    { label: "Сжимает старые строки ради экономии места", misconception: "Старые версии занимают больше места, а не меньше, пока VACUUM не освободит их." },
    { label: "Реплицирует данные на другую машину", misconception: "Репликация — отдельная тема. MVCC — про конкуренцию внутри одного сервера." },
  ]}
/>

<Quiz
  id="quiz-2"
  lessonSlug="01-mvcc-basics"
  lang="ru"
  question="Долгий аналитический запрос и активный поток оформления заказов идут одновременно. Что произойдёт с MVCC?"
  choices={[
    { label: "Аналитический запрос блокирует записи оформления, пока не закончится", misconception: "Весь смысл MVCC в том, что читатели не блокируют писателей." },
    { label: "Записи оформления блокируют аналитический запрос", misconception: "Писатели не блокируют читателей тоже. Оба идут параллельно." },
    { label: "Оба идут параллельно; аналитический запрос видит замороженный snapshot, пока оформление пишет свежие версии строк", correct: true },
    { label: "База выбирает одну из двух и откатывает, чтобы избежать конфликтов", misconception: "Откатывать нечего — операции не конфликтуют на уровне строки." },
  ]}
/>

<DragOrder
  id="drag-1"
  lessonSlug="01-mvcc-basics"
  lang="ru"
  prompt="Поставь в правильном порядке жизненный цикл одного обновления строки:"
  items={[
    "Транзакция A начинается; получает snapshot с тегом своего transaction id",
    "Транзакция A читает строку в версии v1 — версии, помеченной до старта A",
    "Транзакция B обновляет строку, создавая версию v2 с тегом transaction id B",
    "Транзакция B коммитит — v2 теперь видна для новых снимков",
    "Транзакция A всё ещё читает v1, потому что её snapshot разрешает именно это",
    "Когда v1 больше никому не нужна, VACUUM помечает её слот переиспользуемым",
  ]}
/>

<Infographic
  svg={rowVersionLifecycle}
  label="Последовательность из шести шагов жизни одного обновления строки в MVCC: транзакция A берёт snapshot и читает версию v1; транзакция B пишет новую версию v2 и коммитит; A продолжает читать v1, потому что её snapshot старше v2; наконец VACUUM освобождает v1, когда она больше никому не нужна."
  caption="Тот же цикл, который упражнение выше просит расставить по порядку: версия рождается при UPDATE, живёт пока есть активный snapshot, и лишь потом её освобождает VACUUM — эта задержка и есть плата за хранение в MVCC."
  hue="var(--d-infra)"
/>

<MetaphorComplete
  id="metaphor-1"
  lessonSlug="01-mvcc-basics"
  lang="ru"
  setup="Заполни пропуск: MVCC — это библиотека, где каждый читатель получает датированную _______ вместо самой книги с полки."
  accepted={["копию", "копия", "snapshot", "версию", "версия"]}
  canonical="копию (или snapshot / версию — любой термин для независимой датированной ссылки)"
  explanation="Суть метафоры: никто не дерётся за книгу на полке. Каждый читатель уходит со своей копией, помеченной моментом запроса, а полка независимо продолжает добавлять новые издания."
/>

<KeyTakeaway>MVCC хранит несколько версий каждой строки, отдавая каждой транзакции snapshot на момент её старта. Читатели никогда не блокируют писателей. Обратная сторона: мёртвые версии накапливаются, и VACUUM обязан их подметать — иначе раздувается диск.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="01-mvcc-basics"
  lang="ru"
  questions={[
    {
      id: "q1",
      q: "Одним предложением: почему пятиминутный аналитический запрос в Postgres не блокирует записи оформления заказов?",
      a: "Потому что MVCC выдаёт аналитическому запросу snapshot данных на момент его старта, и новые записи оформления создают свежие версии строк, которые аналитический запрос не имеет права видеть — писателям не надо ждать читателя, а читателю — писателей.",
    },
    {
      id: "q2",
      q: "Lost update — это аномалия, которую MVCC решает автоматически, или её должно решать приложение?",
      a: "Приложение. MVCC изолирует транзакции друг от друга, но lost update происходит, когда приложение сначала читает строку, потом записывает поверх чужого коммита без проверки. Защита: SELECT FOR UPDATE (пессимистичная блокировка) или обновление с проверкой старого значения в WHERE (оптимистичная конкуренция).",
    },
    {
      id: "q3",
      q: "Зачем нужен VACUUM, если MVCC уже выдаёт согласованные данные без него?",
      a: "MVCC никогда не удаляет строки физически — старые версии остаются на диске, пока хоть один snapshot их ещё нуждается видеть. VACUUM — уборщик: он находит мёртвые версии, которые ни один активный snapshot не нуждается, и помечает их слоты переиспользуемыми. Без VACUUM таблицы раздуваются бесконечно.",
    },
  ]}
/>

<Recap>
- MVCC хранит несколько версий каждой строки; читатели и писатели никогда не блокируют друг друга
- Каждая транзакция получает snapshot — замороженную картину базы на момент её старта
- `UPDATE` создаёт новую версию, `DELETE` только помечает старую мёртвой — физического удаления нет
- Мёртвые версии накапливаются; `VACUUM` помечает их переиспользуемыми (не возвращает диск ОС)
- Lost update MVCC не решает — нужен `SELECT FOR UPDATE` или строжайший уровень изоляции
- Теперь, когда видишь долгий аналитический запрос рядом с пиковым потоком оформления заказов, ты знаешь: ни один не ждёт другого — а если две вкладки сохраняют один профиль одновременно, это та самая дыра, которую надо закрывать в приложении.
</Recap>
