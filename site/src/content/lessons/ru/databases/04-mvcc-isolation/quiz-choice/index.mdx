---
concepts:
- snapshot-visibility
- isolation-levels
- write-skew
- vacuum-bloat
- xid-wraparound
- ssi
estMin: 14
lang: ru
level: senior
order: 7
prereqs: []
slug: quiz-choice
sources:
- https://www.postgresql.org/docs/current/transaction-iso.html
- https://www.postgresql.org/docs/current/routine-vacuuming.html
- https://www.postgresql.org/docs/current/mvcc-serialization-failure-handling.html
status: ready
summary: "Тест с выбором на синтез по всему юниту MVCC — видимость снимков, компромиссы isolation level, write skew против lost update, пиннинг bloat и XID wraparound."
title: 'MVCC и изоляция: тест с выбором ответа'
track: databases
unit: 04-mvcc-isolation
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Шесть вопросов поперёк всего юнита. Каждый отражает решение, которое ты принимаешь в реальном инциденте — какой isolation level, почему autovacuum застрял, что на самом деле означает 40001 — а не определение для заучивания.
</Hook>

<Goal lang="ru">
Убедись, что связываешь видимость снимков, выбор isolation level, аномалии, которые каждый уровень допускает, пиннинг bloat и wraparound — тот синтез, к которому вели семь уроков.
</Goal>

<Quiz
  id="04-mvcc-isolation-choice-1"
  lessonSlug="04-mvcc-isolation"
  lang="ru"
  question="Соединение B выполняет это под READ COMMITTED, пока соединение A коммитит UPDATE между двумя операторами:
```sql
-- B:
BEGIN;
SELECT balance FROM accounts WHERE id = 42;  -- вернул 100
-- (A коммитит: UPDATE accounts SET balance = 0 WHERE id = 42)
SELECT balance FROM accounts WHERE id = 42;  -- вернёт ?
```
Что вернёт второй SELECT и почему?"
  choices={[
    { label: "0 — под READ COMMITTED каждый оператор берёт свежий снимок, поэтому второй SELECT видит закоммиченную версию A.", correct: true },
    { label: "100 — снимок транзакции заморожен на BEGIN, поэтому B видит старое значение, пока не закоммитит.", misconception: "Это поведение REPEATABLE READ. READ COMMITTED пересоздаёт снимок в начале каждого оператора, поэтому второе чтение видит свежезакоммиченное значение — классический non-repeatable read." },
    { label: "Ошибку — строка изменилась под открытой транзакцией.", misconception: "Postgres здесь ничего не бросает. Конкурентный коммит невидим для выполняющегося оператора и просто становится виден следующему под READ COMMITTED." },
    { label: "NULL — старая версия, которую B прочёл первой, была перезаписана на месте.", misconception: "UPDATE никогда не перезаписывает на месте; он пишет новый tuple и помечает старый мёртвым. Свежий снимок возвращает новую живую версию, а не NULL." },
  ]}
/>

<Quiz
  id="04-mvcc-isolation-choice-2"
  lessonSlug="04-mvcc-isolation"
  lang="ru"
  question="Приложение читает строку, вычисляет новое значение в коде и записывает обратно. Два запроса гонятся:
```sql
-- оба соединения, конкурентно:
BEGIN;
SELECT stock FROM items WHERE id = 7;   -- оба прочли 10
-- приложение считает 10 - 1 = 9
UPDATE items SET stock = 9 WHERE id = 7;
COMMIT;
```
Под уровнем по умолчанию итоговый stock равен 9, а не 8. Что это и каков минимальный фикс?"
  choices={[
    { label: "Lost update. Под READ COMMITTED база не сериализует две записи; фикс — SELECT ... FOR UPDATE (или оптимистичный UPDATE ... WHERE stock = 10), либо поднять уровень до REPEATABLE READ, который бросит 40001 на второго писателя.", correct: true },
    { label: "Phantom read — новая строка появилась между SELECT и UPDATE.", misconception: "Новых строк нет; оба соединения трогают одну существующую строку. Phantom — это строка, входящая в переоценённый предикат, а не затёртое значение." },
    { label: "Write skew — каждая транзакция прочла набор и записала в нём другую строку.", misconception: "Write skew требует, чтобы транзакции модифицировали РАЗНЫЕ строки. Здесь они модифицируют ОДНУ строку тем же значением, это lost update, а не write skew." },
    { label: "Ничего страшного — MVCC гарантирует применение обоих обновлений.", misconception: "MVCC даёт каждой транзакции валидный снимок, но не сериализует слепые записи в одну строку под READ COMMITTED; одно обновление молча теряется." },
  ]}
/>

<Quiz
  id="04-mvcc-isolation-choice-3"
  lessonSlug="04-mvcc-isolation"
  lang="ru"
  question="У таблицы doctors есть инвариант «хотя бы один доктор on_call». Две транзакции под REPEATABLE READ:
```sql
-- T1:                              -- T2 (конкурентно):
SELECT count(*) FROM doctors        SELECT count(*) FROM doctors
  WHERE on_call;  -- 2                WHERE on_call;  -- 2
UPDATE doctors SET on_call=false    UPDATE doctors SET on_call=false
  WHERE name='alice';                 WHERE name='bob';
COMMIT;                             COMMIT;
```
Обе коммитят; теперь ноль докторов на дежурстве. Что произошло и какой уровень это предотвращает?"
  choices={[
    { label: "Write skew. Каждая транзакция прочла один и тот же набор, сочла его безопасным и записала другую строку — REPEATABLE READ (snapshot isolation) это допускает. Только SERIALIZABLE (SSI) обнаруживает цикл rw-antidependency и отменяет одну с 40001.", correct: true },
    { label: "Lost update — второй UPDATE перезаписал первый.", misconception: "Каждый UPDATE трогает другую строку (alice против bob), поэтому ни один не перезаписывает другой. Конфликта версий строк, который ловит REPEATABLE READ, нет; аномалия затрагивает прочитанный набор — это write skew." },
    { label: "Ничего — REPEATABLE READ это snapshot isolation, которая уже это предотвращает.", misconception: "Snapshot isolation предотвращает non-repeatable и phantom reads, но явно допускает write skew. Этот сценарий — канонический пример write skew." },
    { label: "Non-repeatable read в T1.", misconception: "Ни одна транзакция не перечитывает изменившуюся строку; обе держат замороженный снимок. Баг — две независимые записи, совместно нарушающие инвариант, то есть write skew." },
  ]}
/>

<Quiz
  id="04-mvcc-isolation-choice-4"
  lessonSlug="04-mvcc-isolation"
  lang="ru"
  question="Autovacuum проработал 30 минут на таблице orders 200 ГБ и логирует «287 million are dead but not yet removable, oldest xmin: 28391456». n_dead_tup не падает. Какова первопричина и что проверить первым?"
  choices={[
    { label: "Долгая транзакция или orphan replication slot держит глобальный oldest xmin; проверь pg_stat_activity WHERE backend_xmin IS NOT NULL и pg_replication_slots WHERE xmin IS NOT NULL — наименьшее значение совпадает с 28391456.", correct: true },
    { label: "autovacuum_vacuum_cost_limit слишком мал; подними его, чтобы воркер успел.", misconception: "Cost limit лишь регулирует скорость сканирования autovacuum; он не меняет того, что ему ПОЗВОЛЕНО удалять. Лог говорит, что tuples не removable — это пиннинг oldest xmin, а не проблема скорости." },
    { label: "Сначала нужен VACUUM FULL, прежде чем обычный autovacuum сможет почистить.", misconception: "VACUUM FULL возвращает место файла; это не предусловие для обычного autovacuum. Обычный vacuum работает — он просто не может удалить tuples, ещё видимые запиненному oldest xmin." },
    { label: "База приближается к XID wraparound и заморозила таблицу.", misconception: "Wraparound запускает отдельный аварийный anti-wraparound vacuum со своей формулировкой в логе. «Dead but not yet removable» со значением oldest xmin — это пиннинг снимка." },
  ]}
/>

<Quiz
  id="04-mvcc-isolation-choice-5"
  lessonSlug="04-mvcc-isolation"
  lang="ru"
  question="Перевод между двумя счетами в банке читает оба баланса, дебетует один, кредитует другой. DBA предлагает READ COMMITTED с SELECT ... FOR UPDATE на обеих строках (с блокировкой в согласованном порядке) вместо SERIALIZABLE, который в 5 раз медленнее на пике. Это корректно?"
  choices={[
    { label: "Да. Перевод модифицирует ровно те две строки, которые читает, поэтому у write skew нет поверхности; FOR UPDATE на обеих строках в фиксированном порядке делает паттерн эквивалентным SERIALIZABLE для этой формы доступа, с предсказуемой конкуренцией вместо штормов ретраев 40001.", correct: true },
    { label: "Нет — только SERIALIZABLE безопасен для денег; что-либо слабее рискует порчей.", misconception: "Write skew требует действий над строками, которые НЕ модифицируешь. Точечный перевод читает и пишет те же две строки, поэтому FOR UPDATE на обеих функционально эквивалентен SERIALIZABLE здесь — резервируй SSI для инвариантов поверх немодифицируемых строк." },
    { label: "Нет — REPEATABLE READ с циклом ретраев это правильная золотая середина.", misconception: "REPEATABLE READ + ретраи патологичны на горячих счетах: каждый конкурентный перевод на популярный счёт отменяется и ретраится, обрушивая пропускную способность, без выигрыша в корректности против RC + FOR UPDATE для паттерна из двух строк." },
    { label: "Да, но только потому что FOR UPDATE отключает MVCC для этих строк.", misconception: "FOR UPDATE не отключает MVCC; он берёт блокировку строки, чтобы вторая транзакция ждала коммита первой, затем перечитывает свежую версию. Видимость MVCC всё ещё применяется." },
  ]}
/>

<Quiz
  id="04-mvcc-isolation-choice-6"
  lessonSlug="04-mvcc-isolation"
  lang="ru"
  question="Нагрузка делает много SELECT ... FOR KEY SHARE (неявная блокировка от проверок внешних ключей) и натыкается на предупреждение autovacuum о wraparound гораздо раньше, чем предсказывает её пропускная способность транзакций. Какой счётчик является ограничением?"
  choices={[
    { label: "Счётчик MultiXactId — разделяемые блокировки строк упаковывают несколько транзакций в MultiXactId в t_xmax, и у MultiXact свои 32-битные часы wraparound, отдельные от XID; мониторь age(datminmxid) рядом с age(datfrozenxid).", correct: true },
    { label: "Счётчик XID — каждый FOR KEY SHARE расходует transaction id.", misconception: "FOR KEY SHARE создаёт MultiXactId, а не лишние XID. FK-тяжёлая нагрузка нагружает именно пространство MultiXact, которое может обернуться задолго до счётчика XID." },
    { label: "В CLOG кончаются страницы для commit-записей.", misconception: "Страницы CLOG обрезаются по мере заморозки старых транзакций; их исчерпание — не тот режим отказа. Разделяемые блокировки давят на отдельные часы wraparound MultiXact." },
    { label: "max_connections — слишком много конкурентных разделяемых блокировок исчерпывают слоты соединений.", misconception: "Разделяемые блокировки не расходуют соединения. MultiXactId выделяются на заблокированную строку независимо от числа соединений, и именно их wraparound поднимает предупреждение." },
  ]}
/>

<Recap lang="ru">
Сквозная линия юнита — одно правило видимости и решения, надстроенные над ним. Снимки решают, что видит каждая транзакция (на каждый оператор под READ COMMITTED, на транзакцию под REPEATABLE READ). Isolation level решает, какими аномалиями владеешь ты: RC оставляет lost update приложению, REPEATABLE READ бросает 40001 на конкурентных обновлениях строки, но всё ещё допускает write skew, SERIALIZABLE добавляет SSI для ловли циклов write skew. Налог хранения — мёртвые tuples — autovacuum возвращает лишь до глобального oldest xmin, поэтому долгая транзакция или orphan slot — первый подозреваемый при разрастании bloat. А 32-битные счётчики XID и MultiXact оба требуют заморозки до wraparound. Выбирай самый слабый уровень, ещё защищающий твой инвариант, затем оборони границу.
</Recap>
