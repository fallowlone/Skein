---
concepts:
- tuple-header
- snapshot-rule
- write-skew
- oldest-xmin
- hot-update
- ssi
estMin: 14
lang: ru
level: senior
order: 8
prereqs: []
slug: quiz-short
sources:
- https://www.postgresql.org/docs/current/mvcc.html
- https://www.postgresql.org/docs/current/transaction-iso.html
- https://www.postgresql.org/docs/current/routine-vacuuming.html
status: ready
summary: "Промпты на свободное припоминание по юниту MVCC — математика видимости, аномалии изоляции, HOT, oldest xmin и bloat, SSI. Сначала отвечай по памяти, потом открывай ответ."
title: 'MVCC и изоляция: тест на припоминание'
track: databases
unit: 04-mvcc-isolation
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";

<Hook>
Припоминание сильнее перечитывания. Для каждого промпта проговори или запиши полный ответ по памяти, прежде чем открыть модельный — именно усилие припоминания заставляет механизм держаться в голове, когда он нужен в 3 часа ночи.
</Hook>

<Goal lang="ru">
Реконструируй спину юнита, не подглядывая: правило видимости, что каждый isolation level предотвращает и допускает, разницу между write skew и lost update, что пинит oldest xmin и как SSI ловит write skew.
</Goal>

<RetrievalDrawer
  client:load
  id="04-mvcc-isolation-short-retrieval"
  lang="ru"
  questions={[
    { q: "Что такое снимок (snapshot) в Postgres (три числа) и какое правило видимости применяется к каждому tuple?", a: "Снимок — это xmin (id самой старой ещё выполняющейся транзакции), xmax (на единицу больше самого высокого закоммиченного id на момент снимка) и xip (список id транзакций in-progress между ними). Правило, применяемое к каждому tuple без блокировок: tuple виден, если его t_xmin закоммичен и не в xip (вставившая транзакция в прошлом относительно снимка) И его t_xmax равен нулю, откатан или в xip (удалившая транзакция не закоммичена относительно снимка). Видимость — чистая функция заголовка tuple от снимка читателя; два бэкенда никогда не расходятся." },
    { q: "READ COMMITTED против REPEATABLE READ: когда берётся снимок и какая аномалия их различает?", a: "READ COMMITTED берёт свежий снимок в начале каждого оператора, поэтому два SELECT в одной транзакции могут увидеть значение, закоммиченное конкурентной транзакцией между ними — non-repeatable read. REPEATABLE READ берёт один снимок на первом операторе и держит его на всю транзакцию, поэтому повторные чтения стабильны и phantom-ов не возникает (RR в Postgres — это snapshot isolation, строже стандарта SQL). Цена: RR бросает SQLSTATE 40001, если конкурентная транзакция обновила строку, которую RR тоже пишет, и приложение обязано ретраить всю транзакцию." },
    { q: "Отличи lost update от write skew и назови фикс для каждого.", a: "Lost update — две транзакции пишут ОДНУ строку на основе значения, которое обе прочли; вторая запись затирает первую (итоговый stock 9 вместо 8). Фикс под READ COMMITTED — SELECT ... FOR UPDATE или оптимистичный UPDATE ... WHERE col = old_value; REPEATABLE READ тоже ловит это через 40001. Write skew — две транзакции читают один НАБОР, каждая пишет РАЗНУЮ строку, совместно нарушая инвариант, который набор должен был защищать (оба доктора уходят с дежурства). REPEATABLE READ это допускает; фикс — SERIALIZABLE (SSI), отслеживающий прочитанный набор, или переписывание логики так, чтобы транзакция модифицировала строку, которую она же читала." },
    { q: "Что такое глобальный oldest xmin и почему долгая транзакция или orphan replication slot вызывают неограниченный bloat через него?", a: "Глобальный oldest xmin — минимальный backend_xmin среди всех активных сессий, также ограниченный значениями xmin репликационных слотов — самый старый снимок, который что-либо в кластере ещё держит. Autovacuum может удалять только мёртвые tuples, чьё удаление старше этого значения, ведь что-либо новее может быть ещё видимо самому старому снимку. Шестичасовой аналитический запрос или orphan logical/physical replication slot держат очень старый xmin, поэтому каждый мёртвый tuple с тех пор пинится на диске; autovacuum логирует «dead but not yet removable», и таблица растёт без ограничения, пока транзакция не завершится или слот не дропнут. Оборона — idle_in_transaction_session_timeout и алерты на лаг слотов." },
    { q: "Какие два условия делают UPDATE HOT-обновлением и почему это важно для стоимости записи?", a: "Оба должны выполняться: (1) не меняется индексируемый столбец и (2) новый tuple помещается на той же heap-странице, что и старый. Тогда новая версия — heap-only tuple: индекс продолжает указывать на исходный tuple, а цепочка t_ctid доводит lookup, поэтому пишется ноль индексных записей вместо одной на индекс. На update-тяжёлых таблицах это экономит 30–70% WAL. Цепочка рвётся, когда меняется индексируемый столбец или страница заполнена (исчерпан fillfactor), после чего каждый следующий апдейт платит полную стоимость индекса; ставь fillfactor ниже 100, чтобы зарезервировать запас на той же странице и держать HOT-долю (n_tup_hot_upd / n_tup_upd) выше ~80%." },
    { q: "Как SSI обнаруживает write skew, чем отменяет и какова ожидаемая доля ложных срабатываний?", a: "SERIALIZABLE Snapshot Isolation отслеживает predicate locks на каждое чтение («я прочёл этот набор») и rw-antidependency на каждую запись («я записал то, за чем следил конкурентный читатель»). На коммите ищет опасную структуру — цикл rw-antidependency между транзакциями — и отменяет одного участника с SQLSTATE 40001 «could not serialize access due to read/write dependencies». Проверка консервативна: цикл достаточен, но не необходим для несериализуемого расписания, поэтому у SSI есть ложноположительные срабатывания (безопасно отменяя часть транзакций, которые закоммитились бы), но нет ложноотрицательных. Здоровая доля ложных срабатываний 0,1–1%; выше 5% значит predicate locks огрубляются под давлением памяти (подними max_pred_locks_per_transaction) или у нагрузки реальный конфликт, требующий партиционирования." },
  ]}
/>

<Recap lang="ru">
Если ты реконструировал каждый ответ по памяти, ты держишь спину юнита: снимок плюс заголовок tuple — это весь механизм видимости; выбранный isolation level решает, какими аномалиями владеешь ты — RC оставляет lost update приложению, REPEATABLE READ стабилизирует чтения и ловит конкурентные обновления строки, но допускает write skew, SERIALIZABLE добавляет SSI для ловли циклов write skew через зависимости predicate locks; HOT-обновления режут стоимость записи, когда не меняется индексируемый столбец и на странице есть место; а глобальный oldest xmin — рычаг почти за каждым инцидентом bloat: долгая транзакция или orphan slot пинят его, и autovacuum тихо становится бесполезным.
</Recap>
