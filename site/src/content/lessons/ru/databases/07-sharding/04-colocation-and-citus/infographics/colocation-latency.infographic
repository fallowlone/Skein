infographic chart-column-simple
data
  title Латентность join по тенанту: ко-локация против кросс-шарда
  desc Тот же запрос — с ко-локацией он на одном воркере; нарушь её — и он разлетается по всем
  values
    - label Join с ко-локацией (один воркер, мс)
      value 5
    - label Кросс-шард fan-out (все воркеры, мс)
      value 500
theme
  palette #14b8a6 #f43f5e
