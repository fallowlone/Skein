---
concepts:
- shard-key
- hot-shard
- colocation
- cross-shard-transaction
- resharding
- consistent-hashing
estMin: 13
lang: ru
level: senior
order: 7
prereqs: []
slug: quiz-choice
sources:
- https://docs.citusdata.com/en/stable/get_started/concepts.html
- https://www.postgresql.org/docs/current/ddl-partitioning.html
- https://www.citusdata.com/blog/2022/09/19/citus-11-1-shards-postgres-tables-without-interruption/
status: ready
summary: "Синтез всего юнита в формате с выбором ответа — выбор shard key, partitioning против sharding, ко-локация, hot shard, cross-shard транзакции и online resharding."
title: 'Sharding: тест с выбором ответа'
track: databases
unit: 07-sharding
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Шесть вопросов, проходящих сквозь весь юнит. Каждый отражает решение, которое вы принимаете у доски или в разгар инцидента — это не определение для пересказа, а компромисс, который нужно взвесить, когда одного Postgres уже не хватает.
</Hook>

<Goal lang="ru">
Убедитесь, что вы связываете воедино выбор shard key, различие partitioning и sharding, ко-локацию, режим hot shard, cross-shard транзакции и online resharding — синтез, к которому вели семь уроков.
</Goal>

<Quiz
  id="07-sharding-choice-1"
  lessonSlug="07-sharding"
  lang="ru"
  question="B2B SaaS шардируется по tenant_id. Топ-5 арендаторов дают 60% трафика. Предлагают чистый hash sharding, потому что он 'распределяет равномерно'. В чём изъян и каково senior-решение?"
  choices={[
    { label: "Hash распределяет равномерно только на равномерных ключах; арендаторы распределены по степенному закону, поэтому каждый крупный арендатор всё равно попадает на один шард и насыщает его. Решение: hash для хвоста плюс выделенные шарды для топ-арендаторов через tenant isolation или directory remap.", correct: true },
    { label: "Hash sharding в порядке — хеш-функция распределяет строки даже доминирующего арендатора по всем шардам.", misconception: "Hash маршрутизирует по tenant_id, поэтому все строки одного арендатора попадают на единственный шард. Арендатор с 60% трафика прижимает один шард к насыщению, пока остальные простаивают." },
    { label: "Перейти на range sharding по tenant_id — диапазоны балансируют нагрузку лучше hash.", misconception: "Range sharding даёт контроль, но не устраняет перекос: каждый доминирующий арендатор всё равно попадает в шард своего диапазона, который становится горячим. Плюс появляется hot tail для новых регистраций." },
    { label: "Шардировать по created_at, чтобы записи распределялись во времени.", misconception: "Time-range sharding создаёт hot tail (все новые записи бьют в последний шард) и проваливает selectivity — запросы несут tenant_id, а не диапазон времени, поэтому они уходят в fan-out по всем шардам." },
  ]}
/>

<Quiz
  id="07-sharding-choice-2"
  lessonSlug="07-sharding"
  lang="ru"
  question="Команда добавляет `PARTITION BY RANGE (created_at)` к самой большой таблице и заявляет, что 'зашардировала базу'. Через месяц пропускная способность записи не изменилась. Почему?"
  choices={[
    { label: "Partitioning разбивает одну таблицу на под-таблицы на одном инстансе Postgres — это даёт partition pruning и мгновенный retention DROP, но не пропускную способность. Все записи по-прежнему конкурируют за WAL, CPU и IO одного primary.", correct: true },
    { label: "Партициям нужно несколько недель, чтобы прогреть кэши, прежде чем пропускная способность вырастет.", misconception: "Прогрев кэша не меняет структурный факт: каждая партиция живёт на том же primary. Нет дополнительной ёмкости записи, в которую можно было бы прогреться." },
    { label: "Забыли индекс по ключу партиционирования, который разблокировал бы прирост пропускной способности.", misconception: "Индекс ускоряет поиск, но не добавляет ёмкости записи. Partitioning никогда не умножает пропускную способность, потому что не добавляет машин — это делает sharding." },
    { label: "Partitioning и sharding — одно и то же; пропускная способность должна была вырасти автоматически.", misconception: "Это разные вещи: partitioning — это single-instance pruning и retention; sharding — это multi-instance масштабирование пропускной способности. Их смешение и есть ошибка этой команды." },
  ]}
/>

<Quiz
  id="07-sharding-choice-3"
  lessonSlug="07-sharding"
  lang="ru"
  question="В Citus-кластере, шардированном по tenant_id, инженер добавляет `audit_log`, распределённый по `id` вместо `tenant_id`, и выполняет `SELECT … FROM orders o JOIN audit_log a ON a.order_id = o.id WHERE o.tenant_id = 42`. Что происходит?"
  choices={[
    { label: "Ко-локация нарушена: шарды orders выровнены по tenant_id, шарды audit_log — по id, поэтому координатор собирает частичные join с каждого worker и сливает их. Участвует каждый worker, P99 равен самому медленному, латентность растёт в 10–100×.", correct: true },
    { label: "Ничего — Citus видит фильтр по tenant_id и направляет весь join на worker арендатора 42.", misconception: "Фильтр сужает orders до одного шарда, но подходящие строки audit_log разбросаны по всем шардам по id. Citus не может протолкнуть join на один worker, поэтому уходит в fan-out." },
    { label: "INSERT в audit_log падает, потому что Citus отвергает не-tenant ключ распределения.", misconception: "Citus принимает любой объявленный ключ распределения. Цена платится во время запроса как fan-out join, а не отклонением при вставке." },
    { label: "Citus молча перераспределяет audit_log по tenant_id, чтобы сделать join локальным.", misconception: "Citus никогда не перераспределяет таблицу сам — он использует объявленный ключ. Выравнивание ключей (ко-локация) — это осознанное решение по схеме, которое инженер должен принять." },
  ]}
/>

<Quiz
  id="07-sharding-choice-4"
  lessonSlug="07-sharding"
  lang="ru"
  question="Срабатывает алерт на перекос: один шард на 94% CPU, остальные 31 в среднем на 18%, и pg_stat_statements относит 62% времени запросов этого шарда к одному арендатору. Каково правильное немедленное действие?"
  choices={[
    { label: "Выполнить `SELECT isolate_tenant_to_new_shard('orders', <tenant_id>, 'CASCADE')` — Citus копирует только строки этого арендатора на новый шард через logical replication и переключается с sub-second паузой записи; CPU старого шарда падает до медианы.", correct: true },
    { label: "Выполнить `citus_rebalance_start()`, чтобы равномерно разложить все шарды по worker'ам.", misconception: "Ребалансировщик двигает целые шарды между worker'ами; он не отделяет доминирующего арендатора от общего шарда. Горячий арендатор остаётся в связке и продолжает насыщать свой worker." },
    { label: "Добавить worker'ов, чтобы у кластера было больше ёмкости.", misconception: "Больше worker'ов позволяет ребалансировщику двигать шарды, но шард горячего арендатора переезжает как единое целое и всё равно насыщает любой worker. Нужно изолировать арендатора, а не добавлять железо." },
    { label: "Убить открытые соединения арендатора, чтобы сразу сбросить нагрузку.", misconception: "Это даёт мгновенное облегчение, но не двигает данные; следующий запрос арендатора вернётся на тот же горячий шард. Лечится симптом, а не перекос." },
  ]}
/>

<Quiz
  id="07-sharding-choice-5"
  lessonSlug="07-sharding"
  lang="ru"
  question="Транзакция возврата обновляет `orders` (tenant-scoped) и глобальную таблицу `ledger`, шардированную по `account_id`, так что две строки лежат на разных шардах. Citus выполняет это через two-phase commit. Какой режим отказа нужно мониторить?"
  choices={[
    { label: "In-doubt транзакции: если координатор падает между `PREPARE TRANSACTION` и `COMMIT PREPARED`, подготовленные транзакции зависают в pg_prepared_xacts, удерживая блокировки бесконечно. Мониторьте каждый worker, алертите на строки старше 5 минут, держите runbook на COMMIT/ROLLBACK PREPARED.", correct: true },
    { label: "Потерянные обновления — 2PC не может гарантировать, что оба шарда применят запись.", misconception: "Весь смысл 2PC — атомарность между шардами: коммитят оба или ни один. Реальный риск — падение координатора посреди протокола, оставляющее зависшие подготовленные транзакции, а не потерянное обновление." },
    { label: "Ничего особенного — 2PC полностью автоматический и самовосстанавливается при сбое.", misconception: "Postgres не разрешает подготовленные транзакции сам. Зависшая держит блокировки (и может блокировать VACUUM), пока оператор не выполнит COMMIT PREPARED или ROLLBACK PREPARED. Нужны мониторинг и runbook." },
    { label: "Cross-shard взаимоблокировки, которые Postgres вообще не может обнаружить.", misconception: "Характерная опасность 2PC здесь — in-doubt подготовленная транзакция после падения координатора. Долговечное проектное решение — ко-локация таблиц, чтобы транзакция была single-shard и вовсе обходила 2PC." },
  ]}
/>

<Quiz
  id="07-sharding-choice-6"
  lessonSlug="07-sharding"
  lang="ru"
  question="Чтобы добавить ёмкость, команда запускает online rebalancing в Citus 11.1+. Посреди переезда падает координатор, и коллега паникует, что переезжающий шард повреждён. Как это читать и какой механизм делает ответ верным?"
  choices={[
    { label: "Ребалансировщик идемпотентен и возобновляем: перезапустите его, и он продолжит из состояния pg_stat_subscription. Это работает, потому что переезды шардов используют logical replication — исходный шард остаётся целым, и на цель стримятся только строки этого шарда вплоть до sub-second переключения.", correct: true },
    { label: "Переезжающий шард повреждён и должен быть восстановлен из бэкапа.", misconception: "Logical replication транзакционна; исходный шард нетронут, а на цели лишь частичные данные. Перезапуск ребалансировщика возобновляет процесс безопасно — восстановление не нужно." },
    { label: "Кластер уходит в read-only, пока оператор не завершит переезд вручную.", misconception: "Online rebalancing держит кластер обслуживающим трафик; приостанавливается лишь один незавершённый переезд, остальные шарды не затронуты. Это не кластерный read-only." },
    { label: "Это работает, потому что переезд использует physical replication, которая копирует целые страницы атомарно.", misconception: "Resharding использует logical replication, а не physical — physical copy тянул бы страницы каждого арендатора, а не только переезжающего шарда. Logical replication копирует ровно переезжающие строки и позволяет возобновить переезд чисто." },
  ]}
/>

<Recap lang="ru">
Сквозная линия юнита — одна цепочка решений: выберите shard key, который селективен, равномерен, стабилен и присутствует при маршрутизации; партиционируйте ради pruning и retention, но шардируйте ради пропускной способности; ко-локируйте каждую tenant-scoped таблицу, чтобы 99% join оставались single-node; ждите hot shard на степенных арендаторах и отвечайте на них tenant isolation, а не ребалансировкой; держите транзакции single-shard, чтобы обойти in-doubt риск 2PC; и опирайтесь на online resharding на logical replication — помня, что смена shard key — это многомесячное решение, с которым живут годами.
</Recap>
