---
concepts:
- shard-key
- hash-sharding
- range-sharding
- directory-sharding
- consistent-hashing
deepensInto: []
estMin: 12
lang: ru
lessonType: topic
level: middle
order: 2
prereqs:
- databases/07-sharding/01-why-sharding-exists
slug: 02-shard-key-selection
sources:
- https://docs.citusdata.com/en/stable/get_started/concepts.html
- https://www.citusdata.com/blog/2023/08/04/understanding-partitioning-and-sharding-in-postgres-and-citus/
- https://learn.microsoft.com/en-us/azure/cosmos-db/postgresql/concepts-sharding-models
spiral:
- statefulness
- latency
status: ready
summary: Хороший ключ шарда обладает четырьмя свойствами — селективностью, равномерным
  распределением, стабильностью и доступностью во время маршрутизации. Стратегии hash,
  range, list и directory по-разному компромиссируют режимы отказа.
title: 'Выбор ключа шарда: стратегии hash, range, list и directory'
track: databases
unit: 07-sharding
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";
import Infographic from "~/components/diagram/Infographic.astro";
import strategyTradeoffs from "./infographics/strategy-tradeoffs.svg?raw";

<Hook>
Две команды шардируют свой Postgres по `customer_id`. Одна использует хеш-шардирование; через шесть месяцев у них нет горячих шардов. Другая использует range-шардирование с тем же ключом; у них постоянный горячий хвост на шарде с самыми большими ID клиентов, потому что все новые регистрации попадают туда. Одна колонка, разные стратегии, противоположный результат.
</Hook>

<Crux>Ключ шарда определяет распределение нагрузки, ко-локацию запросов и стоимость решардинга — все четыре свойства должны выполняться.</Crux>

<Explanation>

## Четыре свойства хорошего ключа шарда

Ошибёшься с ключом шарда — никакое железо и никакой тюнинг тебя не спасут: ущерб запечён в схеме на годы вперёд. Как выглядит «правильный» выбор?

Ключ шарда должен одновременно удовлетворять всем четырём условиям:

1. **Селективность**: 95%+ ваших запросов фильтруют по нему. Запросы без ключа шарда должны обходить все шарды — это дорого и медленно.
2. **Равномерное распределение**: каждый шард получает примерно равное количество данных и трафика. Неравномерные ключи порождают горячие шарды.
3. **Стабильность**: значение никогда не меняется для данной строки. Изменение ключа шарда потребовало бы перемещения строки на другой шард — проблема, которую нельзя решить во время обновления.
4. **Доступность во время маршрутизации**: каждый API-запрос, касающийся этих данных, должен содержать ключ шарда. Если запросы приходят без него, нельзя маршрутизировать без кросс-шард сканирования.

Для мультиарендного B2B SaaS `tenant_id` почти всегда удовлетворяет всем четырём. Для B2C — `user_id`. Для геораспределённых систем — `region`. Схемы данных, в которых нет колонки, отвечающей всем четырём свойствам — красные флаги; разбейте модель данных до тех пор, пока такая колонка не появится. Вместе эти четыре свойства образуют фильтр: когда видишь кандидата, прошедшего все четыре, — ключ найден; если хоть одно не выполняется, ищи дальше.

## Четыре стратегии

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Стратегия</th>
        <th class="border border-border px-3 py-2 text-left">Как работает</th>
        <th class="border border-border px-3 py-2 text-left">Преимущество</th>
        <th class="border border-border px-3 py-2 text-left">Риск</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2 font-mono">Hash</td>
        <td class="border border-border px-3 py-2">shard = hash(key) mod N</td>
        <td class="border border-border px-3 py-2">Равномерность при равномерных ключах</td>
        <td class="border border-border px-3 py-2">Горячий шард при скошенных ключах; изменение N перемещает большинство строк</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2 font-mono">Range</td>
        <td class="border border-border px-3 py-2">Каждый шард владеет диапазоном ключей (например, tenant_id 1–1000)</td>
        <td class="border border-border px-3 py-2">Естественен для time-series; легко добавить шард в хвост</td>
        <td class="border border-border px-3 py-2">Горячий хвост: все новые записи на одном шарде</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2 font-mono">List</td>
        <td class="border border-border px-3 py-2">Явный маппинг: ключ → шард</td>
        <td class="border border-border px-3 py-2">Максимальный контроль</td>
        <td class="border border-border px-3 py-2">Не масштабируется до миллионов ключей</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2 font-mono">Directory</td>
        <td class="border border-border px-3 py-2">Внешняя lookup-таблица маппит ключ → шард; перемещение клиентов — обновление одной строки</td>
        <td class="border border-border px-3 py-2">Перемещение отдельных клиентов между шардами; изоляция клиентов Citus использует это</td>
        <td class="border border-border px-3 py-2">Стоимость поиска на каждый запрос; карта шардов — критическая инфраструктура</td>
      </tr>
    </tbody>
  </table>
</div>

**Хеш-шардирование** — это дефолт для большинства OLTP-систем, потому что при равномерном распределении ключей оно распределяет равномерно. Проблема в том, что реальные клиенты распределены по степенному закону: один клиент в 100–1000 раз превышает медианный. Хеш по-прежнему маршрутизирует этого клиента на один шард — горячий шард.

**Range-шардирование** естественно для time-series данных (шард по месяцу), но по умолчанию создаёт горячий хвост: все новые записи идут на последний шард, пока старые шарды простаивают. Продакшн-системы time-series обычно сочетают range (по временному бакету) с hash (внутри бакета) для распределения нагрузки записи.

**Directory-based шардирование** — наиболее гибкое: lookup-таблица маппит `tenant_id → connection_string`. Перемещение клиента означает обновление одной строки. Это основа примитива изоляции клиентов Citus и маршрутизации на уровне приложения в мультиарендном SaaS. Цена — lookup-таблица становится инфраструктурой первого уровня критичности: она должна быть высокодоступной, малолатентной и агрессивно кешироваться.

**Consistent hashing** — вариант хеша, используемый в основном в кешах (Memcached, Redis Cluster), где добавление или удаление шарда перемещает только K/N ключей вместо большинства. Некоторые конфигурации шардирования Postgres используют directory-based consistent hashing для онлайн-ребалансировки шардов.

Каждая стратегия меняет один режим отказа на другой. Hash даёт равномерность, но теряет дружественность к диапазонным запросам; range удобен для time-series, но создаёт горячий хвост; directory даёт гибкость на уровне клиента, но делает lookup-таблицу инфраструктурой первого уровня. Когда видишь паттерн записи time-series с автоинкрементным ключом — это сигнал тревоги «горячий хвост»: ищи составной подход.

<Infographic
  svg={strategyTradeoffs}
  label="Дерево-таксономия четырёх стратегий шардирования — hash, range, list и directory — каждая ветвится на преимущество, которое покупает, и режим отказа, который принимает."
  caption="Бесплатной стратегии нет: каждый плюс покупает ровно один режим отказа. Подбирай стратегию под отказ, который можешь стерпеть, а не под плюс, который хочешь."
  hue="var(--d-infra)"
/>

## Контракт ключа шарда на практике

Ключ должен входить в первичный ключ каждой шардированной таблицы. Если `orders` шардирована по `tenant_id`, то `(tenant_id, order_id)` — это первичный ключ, а не просто `order_id`. Это гарантирует уникальность по шардам и заставляет каждый путь запроса нести `tenant_id`.

<Inset kind="why" lang="ru">
Почему изменение ключа шарда так дорого обходится? Ключ шарда определяет, где физически живёт каждая строка. Изменение означает перемещение каждой строки на новый шард — нужно читать из старого шарда, записывать на новый, удалять со старого, и делать это для потенциально миллиардов строк пока производственный трафик продолжается. Команды тратили месяцы на выполнение изменений ключа шарда. Вывод: выбирайте тщательно с самого начала и проверяйте ключ против продакшн-паттернов доступа и распределения размеров клиентов перед принятием решения.
</Inset>

<Quiz
  id="02-shard-key-selection-quiz-1"
  lessonSlug="02-shard-key-selection"
  lang="ru"
  question="В SaaS есть 5000 клиентов, где топ-5 клиентов генерируют 60% трафика. Какой подход к шардированию лучший?"
  choices={[
    { label: "Hash по tenant_id плюс выделенные шарды для топ-5 крупнейших клиентов — через изоляцию клиентов Citus или directory-маршрутизацию на уровне приложения", correct: true },
    { label: "Чистое хеш-шардирование по tenant_id без исключений", misconception: "Hash распределяет равномерно только при равномерных ключах. При 5 доминирующих клиентах, генерирующих 60% трафика, каждый из них попадает на один шард, который будет насыщаться, пока другие простаивают." },
    { label: "Range-шардирование по tenant_id (клиенты 1–1000 на шарде A и т.д.)", misconception: "Range-шардирование даёт контроль, но не решает проблему перекоса — крупные клиенты попадают на шард своего диапазона, который становится горячим шардом." },
    { label: "Шардировать по метке времени created_at вместо tenant_id", misconception: "Range по времени создаёт горячий хвост — все новые записи идут на последний шард. И большинство запросов несут tenant_id, а не диапазон времени, поэтому ключ шарда не проходит тест на селективность." },
  ]}
/>

<Quiz
  id="02-shard-key-selection-quiz-2"
  lessonSlug="02-shard-key-selection"
  lang="ru"
  question="Запрос ищет пользователей по email глобально (tenant_id недоступен). Что происходит в кластере, шардированном по tenant_id?"
  choices={[
    { label: "Fan-out: запрос должен пойти на каждый шард; P99 задержка равна ответу самого медленного шарда; этот путь должен быть редким или выгруженным за пределы шардированного кластера", correct: true },
    { label: "Postgres отклоняет запрос, потому что ключ шарда отсутствует", misconception: "Запрос выполняется; он просто дорог. Postgres выполнит его на каждом шарде и координатор объединит результаты — он не отказывает." },
    { label: "Запрос по умолчанию идёт на шард 0, когда ключ отсутствует", misconception: "Шарда по умолчанию нет. Без ключа шарда координатор должен опросить каждый шард для получения полного результата." },
    { label: "Запрос быстрый, если на каждом шарде есть индекс по email", misconception: "Индекс помогает скорости выполнения на каждом шарде, но не устраняет fan-out. Вы всё равно платите N× накладные расходы по сети, и P99 = max(задержки шардов)." },
  ]}
/>

<DragOrder
  id="02-shard-key-selection-drag-1"
  lessonSlug="02-shard-key-selection"
  lang="ru"
  prompt="Упорядочьте шаги оценки ключа шарда перед принятием решения для мультиарендного B2B SaaS:"
  items={[
    { id: "a", label: "Аудит паттернов запросов: составить список колонок WHERE в топ-100 запросов по частоте" },
    { id: "b", label: "Проверить, что ключ есть в 95%+ запросов и доступен в каждом API-запросе" },
    { id: "c", label: "Измерить распределение размеров клиентов в продакшн-данных — найти топ-5% по трафику и объёму данных" },
    { id: "d", label: "Убедиться, что ключ стабилен (никогда не обновляется) и не null на каждой шардированной таблице" },
    { id: "e", label: "Спроектировать политику для исключений: клиенты выше X% трафика кластера получают выделенные шарды" },
    { id: "f", label: "Зафиксировать ключ в ADR и задокументировать стоимость решардинга, если понадобится его изменить" },
  ]}
  correctOrder={["a", "b", "c", "d", "e", "f"]}
/>

</Explanation>

<FlowDiagram
  label="Hash-шардирование считает hash от ключа шарда по модулю N; на равномерном ключе это раскладывает тенантов поровну по всем четырём шардам, не выделяя ни один."
  perRow={4}
  hue="var(--d-data)"
  nodes={[
    { id: "k", label: "tenant_id", sub: "ключ шарда" },
    { id: "h", label: "hash(key) mod 4", sub: "равномерно" },
    { id: "s0", label: "Шард 0" },
    { id: "s1", label: "Шард 1" },
    { id: "s2", label: "Шард 2" },
    { id: "s3", label: "Шард 3" },
  ]}
  edges={[
    { from: "k", to: "h" },
    { from: "h", to: "s0", label: "=0" },
    { from: "h", to: "s1", label: "=1" },
    { from: "h", to: "s2", label: "=2" },
    { from: "h", to: "s3", label: "=3" },
  ]}
  caption="Хороший ключ селективен, стабилен и доступен при маршрутизации — и равномерен, чтобы hash(key) mod N клал ключи поровну. Перекошенный ключ шлёт одного доминирующего тенанта на один шард: hot shard."
/>

<KeyTakeaway>Ключ шарда должен быть селективным (в 95%+ запросов), равномерно распределённым, стабильным и доступным во время маршрутизации. Hash — дефолт; directory даёт гибкость для изоляции выбросов. Изменение ключа позже стоит месяцев — проверяйте против реальных продакшн-паттернов доступа перед принятием решения.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="02-shard-key-selection-retrieval"
  lang="ru"
  questions={[
    { q: "Назовите четыре обязательных свойства хорошего ключа шарда и объясните, почему каждое важно.", a: "(1) Селективность: 95%+ запросов фильтруют по нему — иначе запросы расходятся на все шарды и обходятся дорого. (2) Равномерное распределение: каждый шард получает примерно равные данные и трафик — неравномерные ключи порождают горячие шарды. (3) Стабильность: значение никогда не меняется для строки — изменение потребовало бы перемещения строки на другой шард в момент обновления. (4) Доступность во время маршрутизации: каждый API-запрос несёт его — без него нельзя маршрутизировать без кросс-шард сканирования." },
    { q: "В чём главный режим отказа range-шардирования по ключу time-series и как продакшн-системы его смягчают?", a: "Range-шардирование по времени создаёт горячий хвост: все новые записи идут на шард последнего временного диапазона, пока старые шарды простаивают. Смягчение: сочетать range (временной бакет) с hash (по user_id или tenant_id внутри бакета), чтобы записи распределялись по шардам внутри каждого временного окна. Как вариант — использовать хеш-шардирование по нетемпоральному ключу и опираться на partition pruning внутри каждого шарда для запросов с диапазоном времени." },
    { q: "Почему ключ шарда должен быть частью первичного ключа каждой шардированной таблицы?", a: "Ключ шарда определяет, на каком физическом инстансе живёт строка. Если первичный ключ — просто (order_id), уникальность — в пределах шарда: два разных шарда могут оба иметь order_id=42. Первичный ключ (tenant_id, order_id) гарантирует глобальную уникальность по шардам и заставляет каждый путь запроса нести tenant_id, чтобы маршрутизатор мог направить его на нужный шард без сканирования." },
  ]}
/>

<Recap lang="ru">
Ключ шарда должен одновременно удовлетворять четырём свойствам: селективности (в 95%+ запросов), равномерному распределению (ни один шард не перегружен), стабильности (никогда не обновляется) и доступности во время маршрутизации. Хеш-шардирование распределяет равномерно при равномерных ключах, но создаёт горячие шарды при перекосе клиентов; range-шардирование естественно для time-series, но создаёт горячий хвост; directory-based шардирование наиболее гибко и используется примитивом изоляции клиентов Citus. Проверяйте ключ против реальных продакшн-паттернов доступа и распределения размеров клиентов перед принятием решения — изменение ключа позже обходится в месяцы перемещения каждой строки. Теперь, когда встретишь новое предложение по шардированию, прогони ключ-кандидат через все четыре свойства прежде чем одобрить: ключ, сломавшийся на одном из них, подведёт на масштабе.
</Recap>
