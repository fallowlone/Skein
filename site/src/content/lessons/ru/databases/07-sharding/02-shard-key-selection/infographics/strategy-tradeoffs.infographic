infographic hierarchy-tree-curved-line-rounded-rect-node
data
  root
    label Стратегия ключа шардирования: каждый плюс покупает один режим отказа
    children
      - label Hash — shard = hash(key) mod N
        children
          - label Плюс: равномерно на равномерных ключах
          - label Риск: горячий шард при перекосе; смена N двигает почти все строки
      - label Range — каждый шард владеет диапазоном ключей
        children
          - label Плюс: естественно для time-series; шард добавляется в хвост
          - label Риск: горячий хвост — все новые записи на один шард
      - label List — явная карта ключ → шард
        children
          - label Плюс: максимальный контроль
          - label Риск: не масштабируется на миллионы ключей
      - label Directory — внешняя таблица соответствий
        children
          - label Плюс: перенос тенанта — это обновление одной строки
          - label Риск: стоимость поиска на каждый запрос; карта — инфраструктура tier-1
theme
  palette #6366f1 #14b8a6 #f59e0b #f43f5e
