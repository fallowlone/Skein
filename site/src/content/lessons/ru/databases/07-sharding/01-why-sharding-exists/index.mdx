---
concepts:
- sharding
- shard-key
- hot-shard
- single-postgres-ceiling
deepensInto: []
estMin: 8
lang: ru
lessonType: topic
level: junior
order: 1
prereqs:
- databases/06-migrations/01-what-a-migration-is
slug: 01-why-sharding-exists
sources:
- https://www.postgresql.org/docs/current/ddl-partitioning.html
- https://docs.citusdata.com/en/stable/get_started/concepts.html
- https://wiki.postgresql.org/wiki/Scaleout_Design
spiral:
- statefulness
status: ready
summary: У каждого экземпляра Postgres есть жёсткие пределы по CPU, хранилищу и пропускной
  способности; шардирование — единственный путь за них — но оно умножает каждую операционную
  задачу на число шардов.
title: 'Зачем нужно шардирование: потолок одного Postgres'
track: databases
unit: 07-sharding
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";
import Infographic from "~/components/diagram/Infographic.astro";
import hotShardLoad from "./infographics/hot-shard-load.svg?raw";

<Hook>
Postgres Отто работает при 95% CPU, рабочий набор данных перерос объём RAM, а запись перегружает единственный primary. Реплики для чтения помогают с чтением, но узкое место по записи — это сам сервер. Более мощного инстанса попросту не существует.
</Hook>

<Crux>Шардирование разбивает датасет по множеству экземпляров Postgres через ключ шарда — единственный способ выйти за потолок одной машины.</Crux>

<Explanation>

## Потолок одного Postgres

За следующие десять минут ты узнаешь, где именно находится каждый потолок, почему вертикальное масштабирование в конце концов перестаёт помогать и как выглядит режим отказа hot shard (горячий шард — перегруженный единственный шард в кластере) до того, как он тебя накрыл.

У единственного экземпляра Postgres есть жёсткие пределы:

- **Рабочий набор против RAM**: когда горячая часть данных перестаёт помещаться в `shared_buffers` плюс OS page cache, каждый запрос начинает обращаться к диску. Падение cache-hit ratio (`blks_hit / (blks_hit + blks_read)`) ниже 95–99% — главный сигнал тревоги.
- **Пропускная способность записи**: единственный primary выдерживает примерно 10–50k транзакций в секунду в зависимости от железа. Выше этого значения генерация WAL доминирует по CPU и primary становится бутылочным горлышком.
- **Хранилище**: практический потолок — около 1–10 ТБ рабочего набора в RAM на самых больших доступных машинах.

Вертикальное масштабирование (больше RAM, более быстрый NVMe) выигрывает время — нередко годы. Правильный порядок: сначала исправить медленные запросы, добавить индексы, добавить реплики для чтения, вертикально масштабироваться. Шардирование — последнее средство, а не первое.

## Что делает шардирование

Шардирование берёт один логический датасет (все ваши `orders`, все ваши `users`) и разбивает его по N физическим экземплярам Postgres. Каждая строка живёт ровно на одном шарде, определяемом **ключом шарда** — колонкой или набором колонок (обычно `tenant_id` для B2B SaaS, `user_id` для B2C).

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Один Postgres</th>
        <th class="border border-border px-3 py-2 text-left">Шардированный (4 инстанса)</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">Все строки на одной машине</td>
        <td class="border border-border px-3 py-2">Строки разбиты по ключу шарда между 4 машинами</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Один потолок CPU/IO</td>
        <td class="border border-border px-3 py-2">~4× мощность по CPU/IO</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Простые операции (1 бэкап, 1 миграция)</td>
        <td class="border border-border px-3 py-2">4× операций (4 бэкапа, миграция запускается 4×)</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Кросс-таблица joins: бесплатно</td>
        <td class="border border-border px-3 py-2">Кросс-шард joins: дорого</td>
      </tr>
    </tbody>
  </table>
</div>

Выигрыш — почти линейное горизонтальное масштабирование: 10 шардов дают примерно 5–8× пропускной способности (накладные расходы на координацию снижают теоретический 10× прирост). Цена — каждая операционная задача: бэкапы, миграции, обновления, VACUUM — должна выполняться на каждом шарде.

## Режим отказа hot shard

Самый важный режим отказа, против которого нужно проектировать с первого дня — это **горячий шард (hot shard)**: один шард получает значительно больше трафика, чем остальные, потому что ключ шарда был выбран неудачно или один клиент вырос в 1000× раз по сравнению с медианным.

**Пример**: SaaS шардирует `orders` по `customer_id`. Корпоративный клиент подписывается и его заказы в 5 раз превышают сумму всех остальных клиентов. Их шард достигает 95% CPU, пока остальные шарды работают при 10%. Кластер по факту работает с мощностью одного шарда для доминирующего клиента.

<Infographic
  svg={hotShardLoad}
  label="Столбчатая диаграмма CPU по четырём шардам: шард 0, держащий крупного тенанта в 5x, работает при 95% CPU, тогда как шарды 1, 2 и 3 простаивают около 10%."
  caption="Один перекошенный тенант держит шард 0 на 95%, пока три шарда простаивают на 10% — полезная пропускная способность кластера падает до одного шарда. Именно этот перекос, а не сырая мощность, делает выбор ключа шарда главным проектным решением."
  hue="var(--d-infra)"
/>

Исправление — перемещение этого клиента на выделенный шард — рассматривается в уроке 05. Главный вывод здесь: выбор ключа шарда — это единственное наиболее важное проектное решение в шардированной системе, а плохой выбор приходится терпеть годами.

## Метафора библиотеки

Шардирование — это разница между одной библиотекой с миллионом книг (одно здание, один стол выдачи, найти что угодно можно просто походив по зданию) и десятью библиотеками по 100k книг (десять зданий, нужен каталог «в какой библиотеке эта книга»). Единственная библиотека проще. Десятибиблиотечная установка требует каталога (**карта шардов**) и правила, в какое здание идти, прежде чем отправиться туда.

Книги, которые выдают миллион раз в год, заполняют одну библиотеку, пока остальные пустуют — это и есть режим отказа hot shard.

<Inset kind="why" lang="ru">
Почему бы просто не взять более крупный облачный инстанс? Вертикальное масштабирование почти всегда дешевле шардирования в первые несколько лет. Точка принятия решения — когда вы измерили, что самого крупного доступного инстанса всё равно не хватает в пиковой нагрузке — обычно это 10 ТБ рабочего набора, устойчивая нагрузка записи 50k+ QPS или многолетние прогнозы, пересекающие эти пороги. Шардирование — дверь в одну сторону: как только схема и логика приложения написаны под шардированный мир, откат назад — это многомесячный проект.
</Inset>

<Quiz
  id="01-why-sharding-exists-quiz-1"
  lessonSlug="01-why-sharding-exists"
  lang="ru"
  question="Что определяет ключ шарда?"
  choices={[
    { label: "На каком физическом экземпляре Postgres живёт строка — приложение хеширует или ранжирует эту колонку для маршрутизации чтений и записей на нужный шард", correct: true },
    { label: "Ключ шифрования данных на этом шарде", misconception: "Шифрование не имеет отношения к шардированию. Ключ шарда — это ключ маршрутизации, а не ключ безопасности." },
    { label: "Первичный ключ каждой таблицы", misconception: "Ключ шарда часто является частью первичного ключа, но его конкретная роль — маршрутизация на нужный инстанс, а не просто уникальная идентификация строки." },
    { label: "Postgres выбирает его автоматически на основе размера таблицы", misconception: "Вы выбираете ключ шарда явно. Неправильный выбор — источник большинства инцидентов с шардированием." },
  ]}
/>

<Quiz
  id="01-why-sharding-exists-quiz-2"
  lessonSlug="01-why-sharding-exists"
  lang="ru"
  question="B2B SaaS шардирует 'orders' по customer_id. Один корпоративный клиент генерирует в 5 раз больше трафика, чем все остальные вместе взятые. Каков результат?"
  choices={[
    { label: "Горячий шард — шард клиента насыщает CPU/IO, пока другие шарды простаивают; кластер фактически работает с мощностью одного шарда для запросов этого клиента", correct: true },
    { label: "Postgres автоматически перебалансирует строки по шардам, когда один заполняется", misconception: "Postgres не перебалансирует автоматически. Хеш-маршрутизация привязывает строки к фиксированному шарду; шард остаётся горячим, пока вы явно не перенесёте клиента." },
    { label: "Все шарды равномерно делят нагрузку, потому что все участвуют в запросе", misconception: "Только шард, владеющий строками клиента, обрабатывает их запросы. Остальные шарды не задействованы и простаивают." },
    { label: "Производительность в порядке, потому что индексы всё ещё работают на горячем шарде", misconception: "Индексы помогают скорости запросов внутри шарда, но не могут перенести нагрузку с перегруженного шарда." },
  ]}
/>

<DragOrder
  id="01-why-sharding-exists-drag-1"
  lessonSlug="01-why-sharding-exists"
  lang="ru"
  prompt="Упорядочьте стратегии масштабирования от самых дешёвых до самых дорогих (по операционной сложности), прежде чем прибегать к шардированию:"
  items={[
    { id: "a", label: "Исправить медленные запросы: добавить недостающие индексы, переписать неэффективный SQL" },
    { id: "b", label: "Добавить реплики для чтения, чтобы снять нагрузку с primary на read-heavy трафик" },
    { id: "c", label: "Добавить кеширующие слои (Redis, in-process) для горячих путей чтения" },
    { id: "d", label: "Вертикальное масштабирование: обновить до более крупного инстанса (больше RAM, более быстрый NVMe)" },
    { id: "e", label: "Шардирование: разбить датасет по нескольким экземплярам Postgres с помощью ключа шарда" },
  ]}
  correctOrder={["a", "c", "b", "d", "e"]}
/>

</Explanation>

<FlowDiagram
  label="Роутер хеширует ключ шарда каждой строки и направляет её ровно в один из четырёх экземпляров Postgres, разбивая единый датасет по кластеру."
  perRow={4}
  hue="var(--d-data)"
  nodes={[
    { id: "data", label: "Один датасет", sub: "все orders / users" },
    { id: "router", label: "Роутер", sub: "hash(shard key)" },
    { id: "s0", label: "Шард 0", sub: "Postgres" },
    { id: "s1", label: "Шард 1", sub: "Postgres" },
    { id: "s2", label: "Шард 2", sub: "Postgres" },
    { id: "s3", label: "Шард 3", sub: "Postgres" },
  ]}
  edges={[
    { from: "data", to: "router" },
    { from: "router", to: "s0" },
    { from: "router", to: "s1" },
    { from: "router", to: "s2" },
    { from: "router", to: "s3" },
  ]}
  caption="Каждая строка живёт ровно на одном шарде, выбранном по её ключу шарда — примерно N× мощности, но каждый backup, миграция и апгрейд теперь выполняются N раз."
/>

<KeyTakeaway>Шардирование разбивает один Postgres по N инстансам с помощью ключа шарда, давая примерно N× мощности — но умножает каждую операцию на N× и делает режим отказа hot shard главным проектным риском. Сначала исчерпайте вертикальное масштабирование и реплики.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="01-why-sharding-exists-retrieval"
  lang="ru"
  questions={[
    { q: "Каковы три жёстких потолка единственного экземпляра Postgres, которые шардирование устраняет?", a: "Рабочий набор против RAM (cache-hit ratio падает ниже 95-99%, когда данные перестают помещаться в shared_buffers + OS cache), пропускная способность записи (единственный primary насыщается при ~10–50k QPS, генерация WAL доминирует по CPU), и хранилище (практический потолок ~1-10 ТБ рабочего набора в RAM на самых крупных доступных машинах). Шардирование распределяет каждый из этих пределов по N инстансам." },
    { q: "В одном предложении: что такое режим отказа hot shard и что его вызывает?", a: "Горячий шард — ситуация, когда один шард получает несоразмерно большую долю трафика, вызванная неравномерным распределением данных или нагрузки по ключу шарда, чаще всего потому что один клиент или диапазон ключей вырастает намного крупнее медианного." },
    { q: "Почему операционная стоимость шардирования описывается как 'N×'?", a: "После шардирования каждая операция, которая запускалась один раз на единственном Postgres, теперь должна выполняться на каждом шарде: бэкапы (N pg_basebackup), миграции схемы (N запусков миграции), VACUUM, минорные и мажорные обновления версий, слоты логической репликации для CDC и мониторинг на каждый шард. Операционная численность персонала и бюджет автоматизации масштабируются пропорционально числу шардов, если с этим не работать." },
  ]}
/>

<Recap lang="ru">
Единственный экземпляр Postgres имеет жёсткие потолки по рабочему набору (RAM), пропускной способности записи и хранилищу, которые вертикальное масштабирование может только отсрочить, но не устранить. Шардирование разбивает датасет по N физическим экземплярам Postgres с помощью ключа шарда, давая примерно N× мощности ценой N× операционной сложности — каждая миграция, бэкап и обновление запускаются по разу на каждый шард. Центральный режим отказа — горячий шард: один шард насыщается, потому что ключ был выбран неудачно или один клиент перерос остальных. Опытные инженеры исчерпывают индексирование, кеширование, реплики для чтения и вертикальное масштабирование прежде чем шардировать — а когда всё же шардируют, выбирают ключ намеренно и проектируют реакцию на hot shard до первого инцидента. Теперь, когда встретишь Postgres-инстанс, ползущий к потолку ресурсов, у тебя есть лестница: индексы → кеш → реплики → вертикальный апгрейд → шардирование — и ты знаешь, на какую ступеньку подниматься, прежде чем тянуться к последней.
</Recap>
