---
concepts:
- shard-key
- partitioning-vs-sharding
- colocation
- hot-shard
- cross-shard-transaction
- resharding
estMin: 14
lang: ru
level: senior
order: 8
prereqs: []
slug: quiz-short
sources:
- https://docs.citusdata.com/en/stable/get_started/concepts.html
- https://www.postgresql.org/docs/current/ddl-partitioning.html
- https://www.crunchydata.com/blog/designing-your-postgres-database-for-multi-tenancy
status: ready
summary: "Промпты на свободное припоминание по всему юниту sharding. Сначала ответьте своими словами, затем раскройте эталонный ответ и сравните."
title: 'Sharding: тест на припоминание'
track: databases
unit: 07-sharding
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";

<Hook>
Припоминание сильнее перечитывания. На каждый промпт проговорите или запишите полный ответ по памяти, прежде чем открыть эталон — именно усилие припоминания закрепляет механизм, когда вы столкнётесь с ним на design review.
</Hook>

<Goal lang="ru">
Восстановите хребет юнита — четыре свойства shard key, partitioning против sharding, ко-локацию, плейбук на hot shard, cross-shard транзакции и online resharding — не заглядывая в уроки.
</Goal>

<RetrievalDrawer
  client:load
  id="07-sharding-short-retrieval"
  lang="ru"
  questions={[
    { q: "Назовите четыре свойства, которым shard key должен удовлетворять одновременно, и почему каждое важно.", a: "(1) Selectivity: 95%+ запросов фильтруют по нему — запросы без него уходят в fan-out по всем шардам и платят P99 = самый медленный шард. (2) Равномерное распределение: каждый шард получает примерно равные данные и трафик — перекошенные ключи дают hot shard. (3) Стабильность: значение никогда не меняется для строки — смена потребовала бы переноса строки на другой шард прямо при записи, что нельзя решить в момент update. (4) Доступность при маршрутизации: каждый API-запрос несёт его — без него нельзя маршрутизировать без cross-shard сканирования. Для B2B SaaS tenant_id обычно удовлетворяет всем четырём; если ни один столбец не подходит, дробите модель данных, пока такой не появится. Ключ также должен входить в primary key каждой шардированной таблицы (например, (tenant_id, order_id)), чтобы уникальность держалась между шардами." },
    { q: "В чём разница между partitioning и sharding и как продакшен-системы их комбинируют?", a: "Partitioning разбивает одну таблицу на под-таблицы на одном инстансе Postgres (декларативный PARTITION BY с PG 10). Он даёт две вещи: partition pruning (фильтр по ключу партиционирования сканирует только нужные партиции) и мгновенный retention DROP (DROP TABLE старой партиции — миллисекунды, без bloat от DELETE и огромного VACUUM). Пропускную способность он не добавляет — все партиции делят WAL, CPU и IO одного primary. Sharding распределяет строки по N отдельным инстансам Postgres по shard key, давая ~N× пропускной способности и хранилища ценой N× операционных издержек. Продакшен комбинирует оба: шардировать по tenant_id между инстансами ради ёмкости, затем партиционировать по created_at внутри каждого шарда ради pruning и retention. Эвристика: партиционируй ради управляемости, шардируй ради ёмкости — и шардируй только когда лимиты одного Postgres измерены." },
    { q: "Объясните ко-локацию в Citus и что происходит с производительностью запросов при её нарушении.", a: "Ко-локация означает, что таблицы, распределённые по одному ключу, держат соответствующие шарды на одном физическом worker — шард 0 orders и шард 0 payments оба на Worker 0 и т.д. Join между ко-локированными таблицами с фильтром по shard key (tenant_id = 42) выполняется целиком на одном worker, как обычный single-Postgres join с полным использованием индексов, единицы миллисекунд. Нарушьте её — распределите payments по payment_id вместо tenant_id — и координатор вынужден собирать частичные join с каждого worker и сливать: участвует каждый worker, P99 равен самому медленному, латентность растёт в 10–100×. У Citus также есть reference-таблицы (полная копия на каждом worker, локальный join, запись через медленный 2PC) для маленьких mostly-read справочников. Ко-локация — это проектный контракт, делающий шардированный кластер похожим на один Postgres для 99% tenant-scoped случая." },
    { q: "Почему hash sharding всё равно порождает hot shard и каков продакшен-плейбук их детекции и устранения?", a: "Hash sharding (shard = hash(tenant_id) mod N) распределяет равномерно только на равномерных ключах. Реальные арендаторы распределены по степенному закону: один-два аккаунта дают 60–80% трафика, и hash маршрутизирует каждого ровно на один шард — поэтому доминирующий арендатор насыщает свой шард, пока остальные простаивают. Это структурно, а не баг. Детектируйте опережающими индикаторами: перекос размера шардов (max/median > 1.5× из citus_shards) и пер-арендаторская нагрузка запросов (pg_stat_statements, агрегированный по tenant_id, алерт выше 5% от cluster total_exec_time) — оба видны за недели до запаздывающего алерта по CPU. Немедленное лечение: SELECT isolate_tenant_to_new_shard('orders', tenant_id, 'CASCADE') копирует только строки этого арендатора на новый шард через logical replication с sub-second паузой записи. Долговечное лечение: многоуровневая политика автоматизации — авто-изоляция арендаторов выше 5% нагрузки кластера, эскалация >20% на выделенный worker, >50% на выделенный кластер, и pre-isolate крупных enterprise-аккаунтов при онбординге, не дожидаясь всплеска." },
    { q: "Когда транзакции нужен two-phase commit на шардированном кластере, чего стоит 2PC и каков его опасный режим отказа?", a: "Транзакция, затрагивающая строки более чем на одном шарде, требует 2PC. Ко-локированные tenant-scoped обновления (orders + inventory оба по tenant_id = 42) остаются single-shard и используют обычную транзакцию Postgres; транзакция, затрагивающая также таблицу с другим ключом (например, глобальный ledger по account_id), охватывает шарды, и Citus выполняет PREPARE TRANSACTION, затем COMMIT PREPARED. Цена: минимум два round-trip (prepare + commit), то есть ~2× латентности, плюс удержанные соединения на фазе prepare, конкурирующие при высокой параллельности. Опасный режим отказа: in-doubt транзакции — если координатор падает между PREPARE и COMMIT PREPARED, подготовленные транзакции зависают в pg_prepared_xacts, удерживая блокировки (и могут блокировать VACUUM), пока не разрешены вручную. Мониторьте pg_prepared_xacts на каждом worker, алертите на строки старше 5 минут, держите runbook на COMMIT/ROLLBACK PREPARED — и предпочитайте ко-локацию или eventual consistency (outbox/saga), чтобы большинству транзакций 2PC вовсе не требовался." },
    { q: "Как работает online resharding в Citus, почему пер-шардовая пауза записи sub-second и почему смена shard key всё равно многомесячный проект?", a: "Citus 11.1+ переносит шард так: создаёт пустой шард на целевом worker, запускает logical replication slot на источнике, отфильтрованный на строки этого шарда, копирует существующие строки и стримит изменения, пока лаг не приблизится к нулю, кратко паузит записи (sub-second), атомарно переключает метаданные shard-map на цель, затем возобновляет и сбрасывает источник. Пауза sub-second, потому что основная масса данных переехала во время streaming replication — на паузе сливаются лишь последние in-flight изменения. Процесс идемпотентен и возобновляем из pg_stat_subscription при падении координатора и использует logical (не physical) replication, поэтому переносит ровно строки шарда, а не страницы каждого арендатора. Смена shard key — другое и куда хуже: каждую строку нужно переписать на новое место — через dual-write + backfill + cut-over или параллельный кластер на CDC — месяцы работы. Поэтому команды часто терпят неправильный ключ, лишь бы не платить за миграцию, и поэтому с этим выбором живут годами." },
  ]}
/>

<Recap lang="ru">
Если вы смогли восстановить каждый ответ по памяти, вы держите хребет юнита: хороший shard key селективен, равномерен, стабилен и присутствует при маршрутизации; partitioning делает pruning и управляет retention на одной машине, тогда как sharding добавляет пропускную способность на многих; ко-локация держит 99% tenant-scoped случая single-node; hot shard — ожидаемое следствие степенного закона, на которое отвечают tenant isolation и многоуровневой политикой; cross-shard транзакции тянут за собой 2PC с его in-doubt риском, поэтому проектируйте single-shard; а online resharding дёшев (sub-second паузы, logical replication), тогда как смена shard key — дорогое решение, с которым живут годами.
</Recap>
