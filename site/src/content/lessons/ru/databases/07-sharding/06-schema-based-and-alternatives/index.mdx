---
concepts:
- schema-based-sharding
- database-per-tenant
- app-level-routing
- row-level-security
- multi-tenancy-tradeoffs
deepensInto: []
estMin: 12
lang: ru
lessonType: topic
level: senior
order: 6
prereqs:
- databases/07-sharding/05-hot-shard-failure
slug: 06-schema-based-and-alternatives
sources:
- https://www.citusdata.com/blog/2023/07/18/citus-12-schema-based-sharding-for-postgres/
- https://learn.microsoft.com/en-us/azure/cosmos-db/postgresql/concepts-sharding-models
- https://www.crunchydata.com/blog/designing-your-postgres-database-for-multi-tenancy
spiral:
- statefulness
status: ready
summary: Citus 12 schema-based шардирование, database-per-tenant, маршрутизация на
  уровне приложения и RLS — четыре валидные архитектуры мультиарендности — каждая
  с конкретным диапазоном числа клиентов и требований к изоляции.
title: Schema-based шардирование и альтернативы мультиарендности
track: databases
unit: 07-sharding
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
B2B SaaS с 200 корпоративными клиентами требует contractual data isolation — данные каждого клиента никогда не должны смешиваться в одной таблице. Row-based шардирование Citus помещает нескольких клиентов в один физический шард. Продуктовая команда спрашивает: существует ли более строгая модель изоляции, которую может обеспечить Postgres?
</Hook>

<Crux>Row-based, schema-based и database-per-tenant — три уровня изоляции, каждый для своего диапазона числа клиентов.</Crux>

<Explanation>

## Четыре архитектуры мультиарендности

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Архитектура</th>
        <th class="border border-border px-3 py-2 text-left">Единица изоляции</th>
        <th class="border border-border px-3 py-2 text-left">Число клиентов</th>
        <th class="border border-border px-3 py-2 text-left">Операционная стоимость</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">RLS на одном Postgres</td>
        <td class="border border-border px-3 py-2">На уровне строк (применение политики)</td>
        <td class="border border-border px-3 py-2">Неограниченно до насыщения одного PG</td>
        <td class="border border-border px-3 py-2">Низкая; баги политики молча открывают данные</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Citus row-based шардирование</td>
        <td class="border border-border px-3 py-2">Шард (несколько клиентов на шард)</td>
        <td class="border border-border px-3 py-2">Тысячи — десятки тысяч</td>
        <td class="border border-border px-3 py-2">Средняя; Citus управляет маршрутизацией</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Citus schema-based (12+)</td>
        <td class="border border-border px-3 py-2">Схема (одна на клиента)</td>
        <td class="border border-border px-3 py-2">Несколько тысяч (потолок)</td>
        <td class="border border-border px-3 py-2">Средняя; схема — единица деплоя</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Database-per-tenant</td>
        <td class="border border-border px-3 py-2">Полная база данных Postgres</td>
        <td class="border border-border px-3 py-2">Десятки — ~500</td>
        <td class="border border-border px-3 py-2">Высокая; N бэкапов, N обновлений, N пулов</td>
      </tr>
    </tbody>
  </table>
</div>

## Citus 12 schema-based шардирование

Представленное в июле 2023, Citus 12 добавило schema-based шардирование: каждый клиент получает выделенную **схему** Postgres, и Citus распределяет схемы по воркерам.

```sql
-- Онбординг клиента: создать схему, Citus назначает её воркеру
CREATE SCHEMA acme;
CREATE TABLE acme.orders (id BIGINT, ...);
CREATE TABLE acme.users  (id BIGINT, ...);

-- Приложение: ограничить соединение схемой клиента
SET search_path TO acme, public;
-- Все последующие ссылки на таблицы разрешаются внутри схемы acme
-- Citus прозрачно маршрутизирует на воркер acme
```

Преимущества над row-based шардированием:
1. **Не требуется общая колонка ключа распределения**: любой запрос внутри схемы выполняется на её воркере — joins между любыми таблицами в одной схеме всегда локальные.
2. **Схема — единица деплоя**: `CREATE SCHEMA` для онбординга, `DROP SCHEMA CASCADE` для оффбординга, перемещение схемы между воркерами для ребалансировки.
3. **Чистая изоляция клиентов**: имена таблиц разные для каждой схемы — один клиент не может случайно прочитать данные другого.
4. **Миграции на клиента**: `ALTER TABLE acme.orders ADD COLUMN ...` затрагивает только эту схему.

Ограничения:
- **Потолок числа клиентов**: Citus рекомендует меньше нескольких тысяч схем. Метаданные схем загружаются в планировщик при каждом запросе; тысячи схем замедляют планирование.
- **Кросс-клиентские запросы по-прежнему кросс-шардовые**: аналитика по нескольким клиентам всё равно требует fan-out.
- **Reference tables неприменимы внутри схем**: lookup-таблицы должны быть в каждой схеме или доступны из общей схемы.

**Оптимальная зона применения**: B2B SaaS с 50–2000 корпоративными клиентами, где требуется contractual или regulatory data isolation.

## Маршрутизация на уровне приложения

Полностью обойти Citus: код приложения поддерживает **карту шардов** (таблицу или сервис, маппящий `tenant_id → connection_string`) и явно маршрутизирует запросы.

```typescript
// Пример: слой маршрутизации читает клиента из контекста запроса
const shard = await shardMap.get(req.tenantId);
const db = connectionPool(shard.connectionString);
return db.query('SELECT * FROM orders WHERE ...', [req.tenantId]);
```

Плюсы: явность, нет дополнительного продукта базы данных, максимальный контроль, логика маршрутизации на клиента (регион, тир тарифного плана).
Минусы: каждая кросс-шард задача становится кодом приложения — joins, транзакции, fan-out, ребалансировка, failover. Слой маршрутизации — критическая инфраструктура первого уровня, которой вы теперь владеете.

Часто применяется, когда число шардов невелико (10–100) и запросы строго привязаны к клиентам. При 5000 клиентах со сложными запросами Citus обычно требует меньше инженерных усилий на многолетнем горизонте.

## Database-per-tenant

Каждый клиент получает выделенную базу данных Postgres (или инстанс). Максимальная изоляция: клиенты буквально не могут делить никакую инфраструктуру на уровне базы данных.

Операционная модель:
- PgBouncer пул на `(клиент, база данных)`
- Бэкапы на клиентскую базу данных
- Минорные и мажорные обновления Postgres на клиентскую базу данных
- Миграции схемы, деплоируемые на клиента (или батчами)

Практический потолок: ~100–500 клиентов, прежде чем операционные накладные расходы начинают доминировать. Ниже этого значения database-per-tenant часто проще, чем Citus для регулируемых отраслей (финансы, здравоохранение), где contractual isolation — жёсткое требование.

<Inset kind="why" lang="ru">
Почему Citus рекомендует меньше нескольких тысяч схем для schema-based шардирования? Планировщик загружает метаданные схем (записи pg_namespace, pg_class) в рабочую память во время планирования. При 10 схемах это пренебрежимо мало. При 10 000 схем каждый план запроса включает обработку тысяч записей пространств имён, добавляя 10–50 мс ко времени планирования — что доминирует над временем выполнения OLTP-запроса. Именно поэтому schema-based шардирование имеет потолок, а row-based шардирование (которое не загружает метаданные на клиента в планировщик) масштабируется до десятков тысяч клиентов.
</Inset>

<Quiz
  id="06-schema-based-and-alternatives-quiz-1"
  lessonSlug="06-schema-based-and-alternatives"
  lang="ru"
  question="B2B SaaS имеет 200 клиентов, требует contractual data isolation для каждого, а у инженерной команды нет экспертизы по Citus. Какая архитектура подходит лучше всего?"
  choices={[
    { label: "Database-per-tenant: каждый клиент получает выделенную базу данных Postgres — максимальная изоляция, управляема при 200 клиентах, и просто в эксплуатации без знания Citus", correct: true },
    { label: "Citus row-based шардирование по tenant_id — это продакшн-дефолт", misconception: "Row-based шардирование помещает нескольких клиентов в один шард, что не отвечает contractual изоляции на клиента. И требует операционной экспертизы Citus, которой у команды нет." },
    { label: "RLS на одном Postgres — обеспечивает изоляцию без сложности шардирования", misconception: "RLS обеспечивает логическую изоляцию через применение политики, а не физическую изоляцию. Баг политики молча открывает данные между клиентами, что обычно не соответствует требованиям contractual isolation. И при 200 корпоративных клиентах одного Postgres может быть недостаточно по мощности." },
    { label: "Citus schema-based шардирование — всегда лучшее для изоляции", misconception: "Schema-based отлично для изоляции в этом масштабе, но требует экспертизы Citus. Database-per-tenant при 200 клиентах операционно реализуемо и проще, если у команды нет опыта Citus." },
  ]}
/>

<Quiz
  id="06-schema-based-and-alternatives-quiz-2"
  lessonSlug="06-schema-based-and-alternatives"
  lang="ru"
  question="Каков главный потолок числа клиентов для schema-based шардирования Citus и почему?"
  choices={[
    { label: "Несколько тысяч — метаданные каждой схемы загружаются в планировщик при каждом запросе, и за пределами нескольких тысяч схем накладные расходы планирования добавляют 10–50 мс к запросу", correct: true },
    { label: "Ровно 1000 клиентов — Citus имеет жёстко закодированный лимит", misconception: "У Citus нет жёстко закодированного лимита числа схем. Практический лимит возникает из деградации производительности планировщика, а не из настройки конфигурации." },
    { label: "Неограниченно — schema-based и row-based шардирование масштабируются одинаково", misconception: "Row-based шардирование не загружает метаданные на клиента во время планирования; schema-based загружает. У них очень разные характеристики масштабирования при большом числе клиентов." },
    { label: "100 клиентов — тот же потолок, что и у database-per-tenant", misconception: "Потолок database-per-tenant — операционный (~100–500 клиентов). Потолок schema-based шардирования выше (несколько тысяч), потому что клиенты разделяют воркеры и пулы соединений." },
  ]}
/>

</Explanation>

<KeyTakeaway>Выбирайте уровень изоляции, соответствующий числу клиентов и contractual требованиям: RLS до насыщения одного PG, Citus row-based для тысяч клиентов, schema-based для нескольких тысяч с требованием строгой изоляции, database-per-tenant для десятков-500 с жёсткими требованиями изоляции. Сопоставляйте архитектуру с ограничениями — универсального правильного ответа нет.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="06-schema-based-and-alternatives-retrieval"
  lang="ru"
  questions={[
    { q: "Что добавил Citus 12 (июль 2023) и какую проблему это решает, которую row-based шардирование не решает?", a: "Citus 12 добавил schema-based шардирование: каждый клиент получает выделенную схему Postgres, и Citus прозрачно распределяет схемы по воркерам. Row-based шардирование помещает строки нескольких клиентов в один физический шард — для предотвращения кросс-клиентского доступа требуется тщательная RLS или логика приложения. Schema-based шардирование обеспечивает структурную изоляцию: имена таблиц привязаны к схеме, поэтому один клиент буквально не видит таблицы другого. Оно также устраняет необходимость в общей колонке ключа распределения — любой запрос внутри схемы выполняется локально на её назначенном воркере. Потолок — несколько тысяч клиентов." },
    { q: "Для B2B SaaS с 5000 клиентов, где топ-5% генерируют 80% трафика и все запросы несут tenant_id, какая архитектура мультиарендности наиболее вероятно правильная и почему?", a: "Citus row-based шардирование по tenant_id с выделенными шардами для крупных клиентов. Row-based справляется с десятками тысяч клиентов (schema-based ограничен несколькими тысячами). Все запросы несут tenant_id, поэтому ко-локация работает отлично — 99% запросов остаются на одном шарде. Топ-5%, генерирующий 80% трафика, означает, что автоматизация изоляции выбросов необходима (isolate_tenant_to_new_shard для клиентов выше 5% нагрузки кластера). Schema-based упрётся в потолок накладных расходов планировщика при 5000 клиентов; database-per-tenant при 5000 инстансов операционно неустойчив." },
    { q: "Когда маршрутизация на уровне приложения (пользовательская карта шардов) является правильным выбором вместо Citus?", a: "Маршрутизация на уровне приложения лучше всего подходит, когда: (1) число шардов невелико (10–100 шардов); (2) запросы строго привязаны к клиентам без необходимости сложных joins между клиентами; (3) команде нужна специальная логика маршрутизации (размещение по региону, инфраструктура по тиру тарифного плана, постепенное развёртывание); (4) добавление ещё одного продукта базы данных (Citus) не оправдано. При 5000 клиентах со сложными паттернами запросов Citus обычно требует меньше суммарных инженерных усилий на годы вперёд. Маршрутизация на уровне приложения делает каждую кросс-шард задачу вашей ответственностью — joins, транзакции, fan-out, ребалансировка, failover — поэтому выигрывает только когда маршрутизация действительно простая и команда хочет явного контроля." },
  ]}
/>

<Recap lang="ru">
На Postgres существуют четыре архитектуры мультиарендности, каждая подходящая для разного числа клиентов и требований к изоляции: RLS на одном Postgres (неограниченное число клиентов, логическая изоляция до достижения мощности), Citus row-based шардирование (тысячи — десятки тысяч, изоляция на уровне шарда), Citus schema-based шардирование (несколько тысяч, структурная изоляция на схему, введена в Citus 12 / июль 2023), и database-per-tenant (десятки — ~500, максимальная изоляция, наибольшая операционная стоимость). Schema-based шардирование устраняет необходимость в общей колонке ключа распределения и обеспечивает чистое пространство имён таблиц на клиента, но потолок накладных расходов планировщика — несколько тысяч схем. Маршрутизация на уровне приложения с пользовательской картой шардов — жизнеспособная альтернатива для небольшого числа шардов с простыми потребностями маршрутизации. Выбор должен соответствовать числу клиентов, требованию к изоляции и операционной зрелости.
</Recap>
