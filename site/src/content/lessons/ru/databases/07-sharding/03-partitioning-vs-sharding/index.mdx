---
concepts:
- declarative-partitioning
- partition-pruning
- sharding
- composing-partitioning-sharding
deepensInto: []
estMin: 10
lang: ru
lessonType: topic
level: middle
order: 3
prereqs:
- databases/07-sharding/02-shard-key-selection
slug: 03-partitioning-vs-sharding
sources:
- https://www.postgresql.org/docs/current/ddl-partitioning.html
- https://www.citusdata.com/blog/2023/08/04/understanding-partitioning-and-sharding-in-postgres-and-citus/
- https://docs.citusdata.com/en/stable/get_started/concepts.html
spiral:
- statefulness
status: ready
summary: Партиционирование — это pruning запросов и управление retention на одном
  инстансе; шардирование — масштабирование пропускной способности на множестве инстансов.
  Они дополняют друг друга и обычно используются совместно в продакшне.
title: 'Партиционирование против шардирования: одно слово, два разных понятия'
track: databases
unit: 07-sharding
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";
import Infographic from "~/components/diagram/Infographic.astro";
import partitionVsShard from "./infographics/partition-vs-shard.svg?raw";

<Hook>
Команда добавляет `PARTITION BY RANGE (created_at)` к своей таблице `events` и называет это «шардированием базы данных». Месяц спустя они удивляются, почему пропускная способность записи не улучшилась. Партиционирование и шардирование часто путают — они решают разные проблемы на разных масштабах.
</Hook>

<Crux>Партиционирование делит таблицу в одном Postgres для pruning; шардирование разносит данные по многим экземплярам для масштаба.</Crux>

<Explanation>

## Партиционирование: один Postgres, таблица разбита для pruning

К концу урока ты сможешь ответить на вопрос «партиционировать или шардировать?» на design review — и объяснить, почему одно не заменяет другое.

Декларативное партиционирование таблиц (введено в PostgreSQL 10) разбивает одну таблицу на именованные подтаблицы — **партиции**, все живущие на одном экземпляре Postgres.

```sql
-- Один Postgres, одна машина, множество партиций
CREATE TABLE events (
  id BIGINT,
  tenant_id INT,
  created_at TIMESTAMPTZ,
  payload JSONB
) PARTITION BY RANGE (created_at);

CREATE TABLE events_2026_01 PARTITION OF events
  FOR VALUES FROM ('2026-01-01') TO ('2026-02-01');
CREATE TABLE events_2026_02 PARTITION OF events
  FOR VALUES FROM ('2026-02-01') TO ('2026-03-01');
```

Postgres получает две вещи от этого:

1. **Partition pruning**: запрос с `WHERE created_at >= '2026-02-01'` сканирует только нужные партиции, а не всю таблицу.
2. **Мгновенное удаление для retention**: удаление месяца данных — `DROP TABLE events_2026_01` — мгновенно, без долгого DELETE, без bloat мёртвых строк, без массового VACUUM.

**Критическое ограничение**: партиционирование не увеличивает пропускную способность за пределы одной машины. Все записи всё равно идут на тот же primary Postgres; все чтения делят тот же CPU и RAM.

## Шардирование: множество экземпляров Postgres, маршрутизация по ключу

Шардирование распределяет строки по N физически отдельным экземплярам Postgres. Каждый экземпляр держит подмножество данных. Записи и чтения для данного ключа шарда идут на Postgres того шарда — параллельный, независимый IO и CPU.

<Infographic
  svg={partitionVsShard}
  label="Сравнение бок о бок: партиционирование живёт на одном инстансе Postgres (pruning, мгновенный DROP для retention, бесплатные JOIN, с PostgreSQL 10), а шардирование разносит данные по многим инстансам (~N× пропускной способности на запись, рост за пределы одной машины, дорогие межшардовые JOIN, через Citus или роутинг в приложении)."
  caption="Одно слово — две разные проблемы: партиционирование масштабирует управляемость на одной машине; шардирование — пропускную способность на многих. Таблица ниже раскрывает каждую строку."
  hue="var(--d-infra)"
/>

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Измерение</th>
        <th class="border border-border px-3 py-2 text-left">Партиционирование</th>
        <th class="border border-border px-3 py-2 text-left">Шардирование</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">Экземпляры Postgres</td>
        <td class="border border-border px-3 py-2">Один</td>
        <td class="border border-border px-3 py-2">Много (N)</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Увеличивает пропускную способность записи?</td>
        <td class="border border-border px-3 py-2">Нет</td>
        <td class="border border-border px-3 py-2">Да (~N×)</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Основное преимущество</td>
        <td class="border border-border px-3 py-2">Partition pruning, быстрый DROP для retention</td>
        <td class="border border-border px-3 py-2">Выход за потолок одной машины</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Кросс-таблица joins</td>
        <td class="border border-border px-3 py-2">Бесплатно (всё на одном инстансе)</td>
        <td class="border border-border px-3 py-2">Дорого при кросс-шард; бесплатно при ко-локации</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Операционный множитель</td>
        <td class="border border-border px-3 py-2">Низкий (всё ещё один инстанс)</td>
        <td class="border border-border px-3 py-2">N× операций на каждый шард</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Доступно с</td>
        <td class="border border-border px-3 py-2">PostgreSQL 10 (декларативное)</td>
        <td class="border border-border px-3 py-2">Через расширение Citus или маршрутизацию на уровне приложения</td>
      </tr>
    </tbody>
  </table>
</div>

## Как они сочетаются в продакшне

Реальные продакшн-системы обычно используют **оба** — партиционирование внутри каждого шарда:

```
Шард 0: экземпляр Postgres, хранящий tenant_id 1–500
  └── таблица events: PARTITION BY RANGE (created_at)
        ├── events_2026_01 (январские строки для клиентов 1-500)
        ├── events_2026_02 (февральские строки для клиентов 1-500)
        └── ...

Шард 1: экземпляр Postgres, хранящий tenant_id 501–1000
  └── таблица events: те же месячные партиции
        ├── events_2026_01
        └── ...
```

Ключ шарда (`tenant_id`) маршрутизирует трафик на нужный экземпляр Postgres. Внутри этого инстанса временная партиция означает: запрос `WHERE tenant_id = 42 AND created_at >= '2026-02-01'` обрезается до февральской партиции. Retention — `DROP PARTITION events_2026_01` на каждый шард.

Опытная интуиция: **партиционировать для того, что вы бы сделали на единственной машине в любом случае** (retention time-series, pruning запросов для больших таблиц); **шардировать только когда пределы пропускной способности и хранилища одного Postgres измерены и доказаны как реальное ограничение**.

<Inset kind="why" lang="ru">
Почему Postgres поддерживает до ~1–2k партиций в OLTP-нагрузках, но ~10k в OLAP? Метаданные партиций загружаются в рабочую память планировщика при каждом запросе — большое число партиций замедляет генерацию плана. Для OLTP-запросов, касающихся одной-двух партиций, накладные расходы плана от 10k партиций доминируют над временем выполнения. OLAP-запросы, параллельно сканирующие много партиций, могут амортизировать стоимость планирования. Практический предел в OLTP — около 1–2k партиций на таблицу.
</Inset>

<Quiz
  id="03-partitioning-vs-sharding-quiz-1"
  lessonSlug="03-partitioning-vs-sharding"
  lang="ru"
  question="Команда добавляет PARTITION BY RANGE (created_at) к своей крупнейшей таблице. Какую проблему это решает?"
  choices={[
    { label: "Pruning запросов (сканировать только нужные временные партиции) и мгновенный DROP для retention — но это не увеличивает пропускную способность записи за пределы единственной машины Postgres", correct: true },
    { label: "Удваивает пропускную способность записи, распределяя записи по партициям на параллельных CPU", misconception: "Все партиции живут на том же primary Postgres. Записи всё равно конкурируют за тот же WAL, CPU и IO. Пропускная способность не увеличивается." },
    { label: "Партиционирование — это шардирование; оба разбивают датасет по нескольким экземплярам Postgres", misconception: "Партиционирование — на одном инстансе. Шардирование — на нескольких. Это разные механизмы, решающие разные проблемы." },
    { label: "Уменьшает хранилище, автоматически сжимая старые партиции", misconception: "Postgres не сжимает партиции автоматически. Преимущество партиционирования для хранилища — мгновенный DROP старых партиций без bloat VACUUM, а не сжатие." },
  ]}
/>

<Quiz
  id="03-partitioning-vs-sharding-quiz-2"
  lessonSlug="03-partitioning-vs-sharding"
  lang="ru"
  question="В продакшн-кластере Citus каково типичное сочетание партиционирования и шардирования?"
  choices={[
    { label: "Шардировать по tenant_id по экземплярам Postgres (для масштабирования пропускной способности) и партиционировать по created_at внутри каждого шарда (для retention и pruning)", correct: true },
    { label: "Либо партиционирование, либо шардирование — никогда оба, они конфликтуют в Citus", misconception: "Они сочетаются без проблем. Citus распределяет таблицы по воркерам с помощью ключа шарда; внутри каждого воркера таблицы могут быть партиционированы по времени или другим измерениям. Citus даже поддерживает нативно декларативное партиционирование + distribution." },
    { label: "Сначала партиционировать, затем направить каждую партицию на другой экземпляр Postgres", misconception: "Это шардирование под другим именем. Citus работает на уровне таблицы — распределённые таблицы шардируются по ключу, и эти шарды могут внутренне партиционироваться." },
    { label: "Партиционирование заменяет шардирование, когда таблица превышает 100 ГБ на инстанс", misconception: "Партиционирование не заменяет шардирование — оно работает на одном инстансе и не помогает, когда достигнут потолок пропускной способности этого инстанса." },
  ]}
/>

</Explanation>

<FlowDiagram
  label="Ключ шарда маршрутизирует строки по двум отдельным экземплярам Postgres ради пропускной способности; внутри каждого экземпляра партиции по времени делят одну таблицу для pruning и retention."
  perRow={2}
  hue="var(--d-data)"
  nodes={[
    { id: "router", label: "Ключ шарда", sub: "tenant_id", col: 0, row: 0 },
    { id: "s0", label: "Шард 0 · Postgres", sub: "tenant 1-500", col: 0, row: 1 },
    { id: "s1", label: "Шард 1 · Postgres", sub: "tenant 501-1000", col: 1, row: 1 },
    { id: "p0a", label: "events_2026_01", sub: "партиция", col: 0, row: 2 },
    { id: "p0b", label: "events_2026_02", sub: "партиция", col: 0, row: 3 },
    { id: "p1a", label: "events_2026_01", sub: "партиция", col: 1, row: 2 },
    { id: "p1b", label: "events_2026_02", sub: "партиция", col: 1, row: 3 },
  ]}
  edges={[
    { from: "router", to: "s0", label: "пропускная способность" },
    { from: "router", to: "s1", label: "пропускная способность" },
    { from: "s0", to: "p0a", label: "pruning" },
    { from: "p0a", to: "p0b" },
    { from: "s1", to: "p1a", label: "pruning" },
    { from: "p1a", to: "p1b" },
  ]}
  caption="Шардирование (между инстансами) даёт ~N× пропускной способности; партиционирование (внутри инстанса) даёт pruning и мгновенный DROP для retention. Продакшн сочетает оба: шард по tenant_id, партиция по времени внутри каждого шарда."
/>

<KeyTakeaway>Партиционирование (один инстанс, PG 10+) выигрывает по pruning и мгновенному DROP для retention. Шардирование (несколько инстансов) выигрывает по масштабированию пропускной способности и хранилища. Продакшн-системы сочетают оба: шардируют по tenant_id, партиционируют по времени внутри каждого шарда.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="03-partitioning-vs-sharding-retrieval"
  lang="ru"
  questions={[
    { q: "Каковы два главных преимущества декларативного партиционирования таблиц в Postgres и каково его ключевое ограничение?", a: "Преимущества: (1) partition pruning — запросы с фильтром по ключу партиции сканируют только нужные партиции, а не всю таблицу; (2) мгновенное управление retention — DROP TABLE на старой партиции занимает миллисекунды без bloat мёртвых строк, в отличие от медленного DELETE + VACUUM. Ключевое ограничение: все партиции живут на одном экземпляре Postgres, поэтому партиционирование не увеличивает пропускную способность записи и не позволяет данным расти за пределы хранилища и RAM одной машины." },
    { q: "B2B SaaS шардирует orders по tenant_id по 16 экземплярам Postgres. Каждый инстанс также партиционирует orders по месяцу created_at. Что делает запрос WHERE tenant_id = 42 AND created_at >= '2026-02-01'?", a: "Ключ шарда (tenant_id = 42) маршрутизирует запрос на шард 3 (к примеру). Внутри этого инстанса планировщик обрезается до партиции events_2026_02 (и последующих месяцев). Запрос никогда не касается остальных 15 экземпляров Postgres, а внутри своего шарда никогда не сканирует январскую партицию. Он получает как горизонтальное масштабирование от шардирования, так и преимущество pruning от партиционирования." },
    { q: "Почему «партиционировать для управляемости, шардировать для мощности» — это опытная эвристика?", a: "Партиционирование улучшает планирование запросов и делает операции retention мгновенными — оба полезны в любом Postgres независимо от масштаба. Шардирование приходит с N× операционными накладными расходами (N бэкапов, N миграций, N операций на шард) и вводит сложность кросс-шард запросов. Шардирование следует добавлять только когда пределы одного Postgres измерены и доказаны; партиционирование можно добавить как только таблица достаточно выросла, чтобы от pruning была польза." },
  ]}
/>

<Recap lang="ru">
Партиционирование (декларативное `PARTITION BY` в PostgreSQL 10+) разбивает одну таблицу на подтаблицы на одном экземпляре Postgres — оно улучшает pruning запросов и делает DROP для retention мгновенным, но не увеличивает пропускную способность за пределы одной машины. Шардирование распределяет строки по N отдельным экземплярам Postgres с помощью ключа шарда, давая примерно N× пропускной способности и мощности хранилища ценой N× операционной сложности. Продакшн-системы используют оба: шардируют по tenant_id для кросс-машинной мощности и партиционируют по времени внутри каждого шарда для pruning и retention. Опытное правило: партиционировать свободно, когда таблица достаточно велика, чтобы выиграть; шардировать только после измерения и доказательства, что пределы одного Postgres — реальное бутылочное горлышко. Теперь, когда услышишь «мы добавили `PARTITION BY` — это же шардирование», ты знаешь поправку: pruning помогает, но узкое место по записи по-прежнему на той же самой машине.
</Recap>
