---
concepts:
- label-association
- error-announcement
- focus-management
- controlled-input
- aria-live
estMin: 14
lang: ru
level: senior
order: 9
prereqs: []
slug: quiz-code
sources:
- https://www.w3.org/WAI/WCAG22/Understanding/status-messages.html
- https://developer.mozilla.org/en-US/docs/Web/Accessibility/ARIA/Reference/Roles/button_role
status: ready
summary: "Читаем реальные JSX-сниппеты — связь label, aria-live-регион ошибки, focus management и controlled input — и выбираем самое результативное исправление доступности."
title: 'Доступные формы: чтение кода'
track: frontend
unit: 03-forms-a11y
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Доступность формы решается в разметке и обработчике submit. Читайте каждый сниппет так, как читали бы его на ревью PR, и выбирайте исправление, которое senior-инженер делает первым.
</Hook>

<Goal lang="ru">
Отработайте цикл ревью: заметьте сломанную связь label, live-регион, который ничего не озвучивает, пропущенное перемещение фокуса и controlled input, теряющий имя поля, — и выберите верное исправление.
</Goal>

## Сниппет 1 — label, связанный ни с чем

```jsx
function EmailField() {
  return (
    <div className="field">
      <label>Email</label>
      <input type="email" name="email" placeholder="you@example.com" />
    </div>
  );
}
```

<Quiz
  id="03-forms-a11y-code-1"
  lessonSlug="03-forms-a11y"
  lang="ru"
  question="Screen reader озвучивает это поле как «edit text» без имени. В чём баг и как исправить?"
  choices={[
    { label: "Label не связан с input — нет пары htmlFor/id, и input не обёрнут. Добавьте htmlFor=\"email\" к label и id=\"email\" к input (или оберните input внутрь label). Placeholder — не имя.", correct: true },
    { label: "Placeholder достаточно; screen reader просто нужно убрать aria-hidden.", misconception: "Placeholder — не программное имя и исчезает при фокусе. Здесь нет aria-hidden; реальный пробел — отсутствующая связь htmlFor/id." },
    { label: "Добавить aria-label=\"Email\" на div-обёртку.", misconception: "aria-label на неинтерактивном div ничего не даёт имени input. Ставьте связь на контрол: htmlFor/id или оберните input в label." },
    { label: "type=email уже сообщает имя «Email» для AT.", misconception: "type=email управляет валидацией и клавиатурой, а не accessible name. Имя берётся из связанного label, которого нет." },
  ]}
/>

## Сниппет 2 — регион ошибки, который ничего не озвучивает

```jsx
function Form() {
  const [error, setError] = useState("");
  return (
    <form onSubmit={validate}>
      <input id="zip" name="zip" aria-describedby="zip-err" />
      {error && <div id="zip-err" role="alert">{error}</div>}
      <button>Submit</button>
    </form>
  );
}
```

<Quiz
  id="03-forms-a11y-code-2"
  lessonSlug="03-forms-a11y"
  lang="ru"
  question="Зрячие видят ошибку; многие пользователи screen reader ничего не слышат. Почему и какое минимальное исправление?"
  choices={[
    { label: "Регион с role=alert монтируется только когда error становится истинным, поэтому live-регион и его текст появляются вместе — многие screen reader это пропускают. Рендерите регион безусловно и прячьте за условием только текст, чтобы регион всегда присутствовал, а менялся его текст.", correct: true },
    { label: "role=alert неверна; переключите весь регион на role=status.", misconception: "role=status — вежливая и подходит для несрочных обновлений, но ошибки заслуживают assertive role=alert. Дефект в условном монтаже региона, а не в роли." },
    { label: "aria-describedby указывает на элемент, которого может не быть, поэтому его надо убрать.", misconception: "Удаление aria-describedby рвёт связь поля с сообщением. Оставьте его и сделайте цель постоянной; describedby корректен, а условный монтаж — нет." },
    { label: "useState не может управлять live-регионом; нужен ref.", misconception: "Обновления состояния прекрасно меняют DOM. Проблема в том, что элемент входит в DOM вместе со своим текстом, вместо того чтобы текст менялся внутри уже присутствующего региона." },
  ]}
/>

## Сниппет 3 — обработчик submit, забывший про фокус

```jsx
function handleSubmit(e) {
  e.preventDefault();
  const invalid = fields.filter((f) => !f.valid);
  invalid.forEach((f) => {
    f.el.setAttribute("aria-invalid", "true");
    setMessage(f.id, f.error); // пишет в постоянный live-регион
  });
  // ...и больше ничего
}
```

<Quiz
  id="03-forms-a11y-code-3"
  lessonSlug="03-forms-a11y"
  lang="ru"
  question="aria-invalid выставлен, сообщения озвучиваются, но пользователи клавиатуры говорят, что форма «ничего не делает» на неудачном submit. Чего не хватает?"
  choices={[
    { label: "Фокус так и не переместился, поэтому остаётся на кнопке submit внизу формы. Добавьте invalid[0].el.focus(), чтобы пользователь попал на первое невалидное поле, где сразу читается ошибка из его aria-describedby.", correct: true },
    { label: "setAttribute ненадёжен; используйте управляемый проп aria-invalid.", misconception: "Выставляете ли вы aria-invalid через атрибут или проп — пользователь всё равно не попадает на поле. Не хватает перемещения фокуса, а не способа задать атрибут." },
    { label: "Полям нужен autofocus, чтобы браузер их сфокусировал.", misconception: "autofocus срабатывает только при начальной загрузке страницы, а не на сбое валидации в момент submit. Нужно императивно сфокусировать первое невалидное поле после валидации." },
    { label: "preventDefault надо убрать, чтобы нативная валидация сфокусировала поле.", misconception: "Удаление preventDefault выбрасывает кастомные сообщения и асинхронные проверки, ради которых этот обработчик существует. Оставьте его и возьмите фокус на себя: вызовите focus() на первом невалидном поле." },
  ]}
/>

## Сниппет 4 — controlled input, теряющий своё имя

```jsx
function NameField({ value, onChange }) {
  return (
    <>
      <span className="lbl">Full name</span>
      <input
        value={value}
        onChange={(e) => onChange(e.target.value)}
      />
    </>
  );
}
```

<Quiz
  id="03-forms-a11y-code-4"
  lessonSlug="03-forms-a11y"
  lang="ru"
  question="Это корректно управляемый (controlled) React-input, и всё же он проваливает доступность. В чём реальная проблема и исправление?"
  choices={[
    { label: "Управляемое состояние в порядке, но видимый текст — это span, а не связанный label, поэтому у input нет accessible name. Сделайте label с htmlFor=\"name\" и id=\"name\" на input (или оберните input). Controlled-vs-uncontrolled ортогонально связи label.", correct: true },
    { label: "У controlled-input не может быть label; переключитесь на uncontrolled с ref.", misconception: "Controlled и uncontrolled input связываются с label одинаково. Режим управления не связан с отсутствующим label; перевод в uncontrolled имени не добавит." },
    { label: "value без defaultValue делает поле read-only, ломая AT.", misconception: "Обработчик onChange есть, поэтому input редактируемый, а не read-only. Пробел в доступности — отсутствующая связь label, а не выбор value/defaultValue." },
    { label: "Обёртка-фрагмент скрывает input из дерева доступности.", misconception: "React-фрагмент ничего не рендерит и не влияет на дерево. input доступен; ему не хватает программно связанного имени." },
  ]}
/>

<Recap lang="ru">
Любой баг доступности формы виден в разметке или обработчике submit: label должен быть связан через htmlFor/id или обёртку (placeholder или соседний span именем не являются); live-регион должен быть постоянным, чтобы изменение его текста было замечено; неудачный submit должен императивно переместить фокус на первое невалидное поле, а не просто выставить aria-invalid; и controlled-vs-uncontrolled ортогонально тому, есть ли у input accessible name. Читайте разметку, найдите сломанную связь или несдвинутый фокус и исправляйте это прежде всего.
</Recap>
