---
concepts:
- label-association
- error-announcement
- focus-management
- constraint-validation
- accessible-name
- custom-widget-trap
deepensInto: []
estMin: 17
lang: ru
lessonType: topic
level: junior
order: 1
prereqs: []
slug: 01-overview
sources:
- https://webaim.org/projects/million/2025
- https://www.w3.org/WAI/WCAG22/Understanding/status-messages.html
- https://www.w3.org/WAI/WCAG21/Understanding/error-identification.html
- https://developer.mozilla.org/en-US/docs/Web/Accessibility/ARIA/Reference/Roles/button_role
spiral: []
status: ready
summary: Форма, удобная с мышью, может быть полностью непригодной с клавиатуры или
  скринридера. Метки, объявление ошибок и фокус на первом невалидном поле — вот что
  проходит WCAG 2.2 AA, и вот что большинство команд выкатывает сломанным.
title: 'Доступные формы: путь с клавиатуры и есть настоящая спецификация'
track: frontend
unit: 03-forms-a11y
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
Форма оформления заказа проходит QA. Работает безупречно — с мышью. Потом пользователь скринридера заводит тикет: он табается на поле email, слышит лишь «edit text», заполняет всю форму вслепую, жмёт стильную «Оплатить» на `<div>` — и Enter ничего не делает, потому что div — не кнопка. Он отправляет случайно, три поля заполнены неверно, а красный текст ошибки, который «явно» появился, никогда не объявляется. Форма не упала. Она просто непригодна для всех, кто не наводит и не кликает. Это провал WCAG 2.2 AA, и на 1 000 000 главных страниц он самый частый из всех.
</Hook>

<Crux>Форма, удобная с мышью, но не с клавиатуры или скринридера, сломана — метки, объявленные ошибки и фокус на первом невалидном поле.</Crux>

<Explanation>

## Метка — это имя поля, и у трети веба её нет

Каждому полю нужна программно связанная метка — не плейсхолдер, не визуально соседний текст, а реальная связь, которую читает дерево доступности. Отчёт WebAIM Million 2025 просканировал топ-миллион главных страниц и нашёл, что **34,2% всех полей форм не были корректно помечены**. При среднем 6,3 поля на страницу это самая частая категория провалов в вебе. Скринридер, попав на непомеченное поле, объявляет «edit text» и больше ничего — пользователь не понимает, что вводить.

Есть две корректные связи, и разница важна:

- **Явная**: `<label for="email">Email</label>` плюс `<input id="email">`. Атрибут `for` (в React — `htmlFor`) совпадает с `id` поля. Это надёжный дефолт — метка может стоять где угодно в DOM.
- **Неявная**: обернуть input в label — `<label>Email <input></label>`. id не нужен, но input должен быть прямым потомком, и часть старых AT и стилевых раскладок обрабатывает это менее надёжно.

Плейсхолдеры — не метки. Они исчезают при фокусе, не проходят минимум контраста, а несколько скринридеров игнорируют их вовсе. «Плавающая метка», которая анимирует плейсхолдер в метку, годится *только* если под ней есть настоящий связанный `<label>`. Вся эта область — **WCAG 1.3.1 Info and Relationships (Level A)** плюс 4.1.2 Name, Role, Value: связь метки и контрола должна быть выставлена ассистивным технологиям, а не просто видна на экране.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Подход</th>
        <th class="border border-border px-3 py-2 text-left">Доступное имя?</th>
        <th class="border border-border px-3 py-2 text-left">Вердикт</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2"><code>&lt;label for="x"&gt;</code> + <code>&lt;input id="x"&gt;</code></td>
        <td class="border border-border px-3 py-2">Да — явное</td>
        <td class="border border-border px-3 py-2">Дефолт; метка может жить где угодно в DOM</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2"><code>&lt;label&gt;Email &lt;input&gt;&lt;/label&gt;</code></td>
        <td class="border border-border px-3 py-2">Да — неявное</td>
        <td class="border border-border px-3 py-2">Работает, но input должен быть прямым потомком</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">только <code>placeholder="Email"</code></td>
        <td class="border border-border px-3 py-2">Нет</td>
        <td class="border border-border px-3 py-2">Исчезает при фокусе, низкий контраст, часто игнорируется AT</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2"><code>aria-label="Email"</code> на input</td>
        <td class="border border-border px-3 py-2">Да — но без видимой метки</td>
        <td class="border border-border px-3 py-2">Крайняя мера (поля-иконки); зрячим не достаётся ничего</td>
      </tr>
    </tbody>
  </table>
</div>

## Ошибки, которых никто не слышит

Ты валидируешь на submit, красишь невалидные поля красным, кладёшь сообщение под каждым. Зрячему пользователю с мышью — идеально. Пользователю скринридера — тишина: визуальное изменение DOM не объявляет ничего. Фикс состоит из двух частей, и обе легко сделать тонко неправильно.

Во-первых, **объяви ошибку**. Сообщение, вставленное в контейнер с `role="alert"` (или область `aria-live="assertive"`), зачитывается в момент появления — это **WCAG 4.1.3 Status Messages (Level AA)**: пользователь узнаёт об изменении статуса, не перемещая фокус в его поисках. Ловушка: live-область должна уже существовать в DOM *до* того, как ты впишешь в неё текст. Если смонтировать `<div role="alert">` и его сообщение в одном рендере, многие скринридеры пропустят изменение. Рендери пустую live-область заранее; впрыскивай текст только при ошибке.

Во-вторых, **свяжи сообщение с его полем** через `aria-describedby`, указывающий на `id` сообщения, и пометь поле `aria-invalid="true"`. Теперь, когда пользователь попадёт на это поле, скринридер зачитает метку, значение, «invalid» и текст ошибки вместе. Это **WCAG 3.3.1 Error Identification (Level A)**. Сделай ещё лучше — скажи, *как* исправить («введите дату как ММ/ДД/ГГГГ»), а не просто «invalid» — и ты удовлетворишь **3.3.3 Error Suggestion (Level AA)**.

<Inset kind="why" lang="ru">
`role="alert"` — это `aria-live="assertive"` плюс `aria-atomic="true"`: он прерывает скринридер, чтобы немедленно зачитать всю область. Используй для ошибок. Для несрочного статуса («Сохранено», «3 результата») предпочитай `role="status"` (polite): он ждёт паузы, а не вламывается. Две assertive-области, сработавшие разом, затрут друг друга, поэтому резервируй assertive для того, что пользователь должен услышать *сейчас*.
</Inset>

## Фокус — это разница между пригодным и нет

Валидация провалилась. Зрячий пользователь видит красные поля и кликает первое. Пользователь клавиатуры или скринридера на submit всё ещё сфокусирован на кнопке отправки — *внизу* формы. Он понятия не имеет, что что-то провалилось, и даже если страница прокрутилась, ему пришлось бы табаться назад через всю форму в поисках сломанного поля. Фикс — одна строка намерения: **при ошибке submit перемести фокус на первое невалидное поле** (`firstInvalid.focus()`). Пользователя бросают ровно туда, где работа, и поскольку у этого поля есть `aria-describedby` + `aria-invalid`, скринридер немедленно зачитывает, что не так и как исправить.

Это поведение браузер даёт *бесплатно* при нативной constraint-валидации — `required`, `type="email"`, `pattern`, `min`/`max` — когда браузер фокусирует и прокручивает к первому невалидному контролу на submit и показывает встроенный пузырёк. В тот момент, когда ты перехватываешь через `e.preventDefault()` и пишешь свою валидацию (а так делают большинство реальных приложений — ради своих сообщений и асинхронных проверок), **ты наследуешь эту ответственность за фокус**. Забудешь — и ты построил классическую форму «работает с мышью, мертва с клавиатуры».

<FlowDiagram
  label="Доступная обработка проваленного submit в пять упорядоченных шагов: валидация на submit, aria-invalid на каждом плохом поле, запись ошибки в заранее существующую live-область, на которую указывает aria-describedby поля, перемещение фокуса на первое невалидное поле, и зачитывание скринридером метки, статуса invalid и текста ошибки."
  perRow={5}
  hue="var(--d-frontend)"
  nodes={[
    { id: "validate", label: "Валидация", sub: "на submit" },
    { id: "invalid", label: "aria-invalid", sub: "пометь поля" },
    { id: "describe", label: "Запись ошибки", sub: "live-область" },
    { id: "focus", label: "Двинь фокус", sub: "1-е невалид." },
    { id: "announce", label: "Скринридер", sub: "зачитывает" },
  ]}
  edges={[
    { from: "validate", to: "invalid" },
    { from: "invalid", to: "describe" },
    { from: "describe", to: "focus" },
    { from: "focus", to: "announce" },
  ]}
  caption="Всегда этот порядок. Live-область должна уже существовать до записи сообщения, а aria-describedby связывает сообщение с полем, так что перемещение фокуса туда заставляет скринридер объявить метку, статус invalid и как исправить — вместе."
/>

<Quiz
  id="03-forms-a11y-quiz-1"
  lessonSlug="01-overview"
  lang="ru"
  question="Кастомная JS-валидация на submit находит три невалидных поля. Какой ход сеньора для пользователей клавиатуры/AT?"
  choices={[
    { label: "Переместить фокус на первое невалидное поле, чтобы оно объявило ошибку через aria-describedby + aria-invalid", correct: true },
    { label: "Просто покрасить невалидные поля красным и надеяться, что пользователь прокрутит вверх их искать", misconception: "Цвет невидим скринридеру и бесполезен дальтонику; фокус остаётся на кнопке отправки без объявленного изменения. Это и есть ключевой провал WCAG 2.2 AA." },
    { label: "Показать один диалог alert() со списком ошибок", misconception: "alert() прерывает всех, теряет связь поле-сообщение (aria-describedby) и не ставит фокус туда, где происходит исправление — для клавиатуры и AT это хуже." },
    { label: "Прокрутить страницу к первой ошибке, но оставить фокус на кнопке отправки", misconception: "Прокрутка помогает только зрячему с мышью. Фокус пользователя клавиатуры всё ещё внизу, а скринридер не объявляет ничего — нужно двигать фокус, а не только вьюпорт." },
  ]}
/>

## Кастомные виджеты — место, где всё тихо умирает

Самые глубокие провалы — не пропущенные метки, а переизобретённые контролы. Дизайнер хочет стилизованный дропдаун, и инженер строит `<div class="select">` с обработчиками клика. Выглядит идентично. Для ассистивных технологий он *невидим как контрол*: у div нет роли, его нет в порядке табуляции, у него нет клавиатурного поведения. Скринридер читает его как обычный текст; пользователь клавиатуры не может до него добраться, открыть, выбрать вариант. Та же ловушка ловит «кнопки» на `<div onClick>`: доки MDN про роль button прямо говорят, что чтобы div вёл себя как кнопка, нужно добавить `role="button"`, `tabindex="0"`, обработчик keydown на **оба** — Enter (keyCode 13) и Space (32) — с `preventDefault`, плюс семантику disabled и фокуса, которую `<button>` давал бесплатно. Люди помнят про обработчик клика и забывают каждый из этих пунктов.

Правило сеньора прямое: **используй нативный элемент.** `<button>`, `<input>`, `<select>`, `<a href>` поставляются с ролью, фокусируемостью, клавиатурной обработкой и интеграцией с формой на уровне ОС, которую ты не воспроизведёшь полностью и не будешь поддерживать. Тянись к виджетам на ARIA только когда нативный элемент по-настоящему не справляется (настоящий combobox с фильтрацией, multiselect) — и тогда следуй полному клавиатурному паттерну APG, потому что недостроенный `role="combobox"` хуже, чем простой `<select>`, который ты заменил.

<TradeoffMatrix
  id="03-forms-a11y-tradeoff-1"
  lessonSlug="01-overview"
  lang="ru"
  prompt="Дизайн даёт кастомно-стилизованный дропдаун, который нативный <select> не повторяет визуально. Выбери реализацию."
  options={[
    { name: "Стилизовать нативный <select> насколько позволяет CSS; принять системный пикер для списка опций", summary: "Сначала нативное.", correct: true, justification: "Нативный <select> поставляется с ролью, фокусируемостью, type-ahead, открытием/закрытием с клавиатуры и системным рендером опций — ничего из этого не нужно строить или поддерживать. Современный CSS (и зреющая работа над customizable-select) покрывает большинство нужд стилизации. Ты меняешь пиксель-перфект стилизацию списка опций на контрол, который корректен в каждом браузере и AT по умолчанию. Это почти всегда верный выбор." },
    { name: "Построить дропдаун на div с role=listbox, tabindex и полным клавиатурным паттерном APG", summary: "Перестройка на ARIA.", justification: "Иногда необходимо (combobox с фильтрацией, multiselect, которого нет у нативного), но теперь ты владеешь навигацией стрелками, type-ahead, Escape/Enter/Home/End, управлением фокусом, aria-activedescendant и тестированием в NVDA/JAWS/VoiceOver. Верно здесь, только если нативный контрол действительно не покрывает требование — и тогда нужно дописать весь паттерн, а не половину." },
    { name: "Взять <div onClick> со схожим CSS и пропустить ARIA — выглядит так же", summary: "Только визуально.", justification: "Классический тихий провал. У div нет роли, его нет в порядке табуляции, нет клавиатурного поведения, поэтому он невидим скринридерам и недостижим с клавиатуры. Работает с мышью и валит WCAG 1.3.1 / 4.1.2. Никогда не выкатывай это." },
    { name: "Оставить нативный <select>, но визуально спрятать его и наложить фейковый div для мыши", summary: "Спрятанный нативный.", justification: "Частый «хак», который ломается: фокус, значение и видимый виджет расходятся, цели клика сдвигаются, а фокус клавиатуры попадает на невидимый контрол, тогда как мышь видит фейковый. Получаешь стоимость поддержки обоих с корректностью ни одного." },
  ]}
/>

<DragOrder
  id="03-forms-a11y-drag-1"
  lessonSlug="01-overview"
  lang="ru"
  prompt="Расставь шаги доступной обработки проваленного submit:"
  items={[
    { id: "a", label: "Запустить валидацию на submit и собрать невалидные поля" },
    { id: "b", label: "Выставить aria-invalid=\"true\" на каждом невалидном поле" },
    { id: "c", label: "Вписать каждое сообщение об ошибке в заранее существующую live-область / элемент, на который указывает aria-describedby поля" },
    { id: "d", label: "Переместить фокус клавиатуры на первое невалидное поле (firstInvalid.focus())" },
    { id: "e", label: "Дать скринридеру зачитать метку + invalid + текст ошибки теперь, когда фокус на поле" },
  ]}
  correctOrder={["a", "b", "c", "d", "e"]}
/>

</Explanation>

<KeyTakeaway>Свяжи каждое поле с настоящей меткой, рендери live-области ошибок до записи в них, выставляй aria-invalid + aria-describedby и перемещай фокус на первое невалидное поле на submit. Предпочитай нативные контролы.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="03-forms-a11y-retrieval"
  lang="ru"
  questions={[
    { q: "Пользователь скринридера отправляет форму, три поля невалидны, и он не слышит ничего. Пройди, чего не хватает и как закрыть каждый пробел.", a: "Скорее всего сломано три вещи разом. Первое — ошибки не объявляются: визуальное изменение DOM ничего не говорит скринридеру, поэтому каждое сообщение должно жить в области с role=\"alert\" (или aria-live), и критично — эта область должна уже существовать в DOM до впрыска текста: монтаж области и сообщения в одном рендере многие AT пропускают. Это удовлетворяет WCAG 4.1.3 Status Messages. Второе — ошибки не связаны с полями: выставь aria-invalid=\"true\" на каждом плохом поле и aria-describedby на id сообщения, чтобы попадание на поле зачитывало метку, значение, «invalid» и сообщение вместе (WCAG 3.3.1). Третье — главное — фокус не двигался: после preventDefault на submit фокус всё ещё на кнопке отправки внизу формы, и пользователь не знает, что что-то провалилось. Вызови firstInvalid.focus(), чтобы бросить его на первое сломанное поле, где текст из describedby зачитается сразу. Нативная constraint-валидация делает все три бесплатно; в момент, когда ты вызвал preventDefault и валидируешь сам, они твои." },
    { q: "Почему дропдаун на <div role=\"button\"> — баг хуже пропущенной метки, и что реально даёт «используй нативный элемент»?", a: "Пропущенная метка — это один неверный атрибут: досадно, но контрол всё ещё достижим, фокусируем и управляем. Контрол на div невидим как контрол: у div нет неявной роли, его нет в порядке табуляции, нет клавиатурного поведения, поэтому скринридер читает его как обычный текст, а пользователь клавиатуры вообще не может до него добраться или управлять им. Чтобы div вёл себя как кнопка, нужно добавить role=\"button\", tabindex=\"0\" и обработчик keydown на оба — Enter и Space — с preventDefault, плюс воспроизвести семантику disabled и фокуса, а для select/combobox — полный паттерн APG (стрелки, type-ahead, Escape, aria-activedescendant). Люди помнят про обработчик клика и забывают всё это, поэтому виджет работает с мышью и мёртв для клавиатуры и AT. Нативный элемент поставляется с ролью, фокусируемостью, клавиатурной обработкой и интеграцией с формой на уровне ОС, которые ты не воспроизведёшь полностью и не будешь поддерживать — поэтому дефолт сеньора — тянуться к <button>/<select>/<input> и перестраивать на ARIA только когда нативный по-настоящему не справляется." },
  ]}
/>

<Recap lang="ru">
Доступные формы решаются путём с клавиатуры и скринридера, а не путём мыши. Каждому полю нужна программно связанная метка (явная htmlFor/id или неявная обёртка) — плейсхолдеры не считаются, и 34,2% полей форм в вебе валят это. Ошибки должны объявляться через live-область (role="alert"/aria-live), которая уже существует в DOM, быть связанными с полями через aria-invalid + aria-describedby и формулироваться как подсказки, а не просто «invalid» — покрывая WCAG 1.3.1, 3.3.1, 3.3.3 и 4.1.3. При проваленном submit нужно переместить фокус на первое невалидное поле; нативная constraint-валидация делает это бесплатно, но в момент preventDefault это становится твоей задачей. И самая глубокая ловушка — переизобретённый контрол: стилизованный div невидим ассистивным технологиям, поэтому тянись сначала к нативным `<button>`/`<select>`/`<input>` и перестраивай на полный паттерн ARIA только когда нативный элемент по-настоящему не справляется. Сделай это правильно — и форма, которая «работает с мышью», наконец заработает для всех.
</Recap>
