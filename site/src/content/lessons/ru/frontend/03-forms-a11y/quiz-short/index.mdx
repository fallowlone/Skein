---
concepts:
- label-association
- error-announcement
- focus-management
- native-vs-aria
- constraint-validation
- aria-live
estMin: 14
lang: ru
level: senior
order: 8
prereqs: []
slug: quiz-short
sources:
- https://www.w3.org/WAI/WCAG22/Understanding/status-messages.html
- https://www.w3.org/WAI/WCAG21/Understanding/error-identification.html
- https://webaim.org/projects/million/2025
status: ready
summary: "Вопросы на свободное воспроизведение по всему юниту. Сначала ответьте своими словами, затем раскройте модельный ответ и сравните."
title: 'Доступные формы: свободное воспроизведение'
track: frontend
unit: 03-forms-a11y
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";

<Hook>
Воспроизведение по памяти сильнее перечитывания. Для каждого вопроса проговорите или запишите полный ответ из памяти, прежде чем открыть модельный, — именно усилие припоминания закрепляет дисциплину «пути клавиатуры».
</Hook>

<Goal lang="ru">
Восстановите хребет юнита — связь label, озвучивание ошибок, фокус на первом невалидном поле, native против ARIA и то, что вы наследуете при preventDefault, — не заглядывая обратно в урок.
</Goal>

<RetrievalDrawer
  client:visible
  id="03-forms-a11y-short-retrieval"
  lang="ru"
  questions={[
    { q: "Что значит, что label программно связан, и почему placeholder ему не замена?", a: "Программная связь раскрывает отношение label-контрол в дереве доступности, а не только на экране. Два корректных варианта — явный (label с for/htmlFor, совпадающим с id input, так что label может стоять где угодно в DOM) и неявный (input обёрнут прямо внутри label). Placeholder — ни то, ни другое: это подсказка на самом input, она исчезает в момент фокуса поля, обычно не проходит минимум по контрасту, и часть screen reader её полностью игнорирует. Поэтому поле только с placeholder озвучивается как «edit text» без имени. Это WCAG 1.3.1 Info and Relationships плюс 4.1.2 Name, Role, Value, и скан WebAIM Million 2025 нашёл, что 34,2% полей форм на топ-миллионе главных страниц проваливают это — самая частая категория ошибок в вебе." },
    { q: "Почему визуальная подсветка невалидных полей красным ничего не озвучивает для screen reader, и какое двухчастное исправление делает ошибки воспринимаемыми?", a: "Визуальное изменение DOM — красная рамка, вставка узла-сообщения — не порождает события, которое screen reader озвучит, если это изменение не происходит внутри ARIA live-региона. Часть первая — озвучивание: помещаем каждую ошибку в регион с role=alert (assertive: aria-live=assertive плюс aria-atomic=true), чтобы её читали в момент появления, удовлетворяя WCAG 4.1.3 Status Messages. Критическая тонкость: live-регион должен уже существовать в DOM до того, как вы запишете в него текст; монтаж региона и сообщения в одном рендере многие AT пропускают, поэтому рендерите пустой регион заранее и вставляйте текст только при ошибке. Часть вторая — связывание: ставим aria-invalid=true на поле и aria-describedby, указывающий на id сообщения, чтобы при попадании на input читались имя, значение, «invalid» и сообщение вместе (WCAG 3.3.1 Error Identification). Сказать, как исправить, а не только что неверно, также удовлетворяет 3.3.3 Error Suggestion." },
    { q: "На неудачном submit с кастомной валидацией почему перемещение фокуса — самый результативный шаг, и куда должен идти фокус?", a: "После e.preventDefault() фокус всё ещё на кнопке submit внизу формы. Зрячий пользователь мыши видит красные поля и кликает по одному; пользователь клавиатуры или screen reader не понимает, что вообще что-то провалилось, и ему пришлось бы табать назад через всю форму в поисках сломанного контрола. Перемещение фокуса на первое невалидное поле (firstInvalid.focus()) доставляет пользователя ровно туда, где работа, и поскольку поле несёт aria-invalid + aria-describedby, screen reader сразу читает, что не так и как исправить. Прокрутки viewport недостаточно — она помогает только зрячему пользователю мыши; нужно переместить фокус, а не страницу. Именно это поведение — разница между формой, работающей с клавиатуры, и формой, для неё мёртвой." },
    { q: "Нативная constraint validation делает фокус, прокрутку и пузырёк ошибки бесплатно. Что именно вы наследуете в момент вызова e.preventDefault()?", a: "Нативная constraint validation (required, type=email, pattern, min/max) заставляет браузер на submit сфокусировать и прокрутить к первому невалидному контролу и показать встроенный пузырёк — всё автоматически. Как только вы перехватываете submit через e.preventDefault() ради кастомных сообщений или асинхронных проверок, каждое из этих поведений перестаёт срабатывать. Теперь на вас: сфокусировать первое невалидное поле, озвучить проблему через live-регион, связать каждое сообщение с полем через aria-describedby и выставить aria-invalid. Большинство реальных приложений делают preventDefault ради кастомной и асинхронной валидации — именно поэтому так многие выпускают форму «работает с мышью, мертва с клавиатуры»: они забрали submit, но не забрали пришедшую с ним ответственность за доступность." },
    { q: "Почему контрол на div — баг хуже отсутствующего label, и что на самом деле даёт «использовать нативный элемент»?", a: "Отсутствующий label — это один неверный атрибут: досадно, но контрол всё ещё достижим, фокусируем и операбелен. Контрол на div невидим как контрол: у div нет неявной role, его нет в порядке табуляции и у него нет поведения с клавиатуры, поэтому screen reader читает его как простой текст, а пользователь клавиатуры вообще не может его достичь или использовать. Чтобы div вёл себя как кнопка, нужно добавить role=button, tabindex=0 и обработчик keydown для Enter и Space с preventDefault, плюс воспроизвести семантику disabled и фокуса; для select/combobox вы должны весь паттерн APG (стрелки, type-ahead, Escape, Home/End, aria-activedescendant). Люди помнят обработчик клика и забывают всё остальное, так что виджет работает с мышью и мёртв для клавиатуры и AT. Нативный button/select/input несёт role, фокусируемость, обработку клавиатуры и интеграцию с формой на уровне ОС, которые нельзя полностью воспроизвести и не будете поддерживать, — поэтому senior-выбор по умолчанию native first, а ARIA-перестройка только когда нативный действительно не справляется." },
    { q: "Является ли controlled-vs-uncontrolled решением о доступности в React? Объясните связь.", a: "Нет — controlled против uncontrolled касается только того, где живёт значение input (в state React через value + onChange либо в DOM, читаемое через ref или defaultValue). К доступности это отношения не имеет: оба вида связываются с label одинаково, раскрывают тот же accessible name, role и value и требуют тех же aria-invalid / aria-describedby при ошибке. Выбор важен для эргономики валидации — controlled-input делают тривиальной валидацию на change и реактивный рендер сообщений, тогда как uncontrolled опираются на нативную constraint validation и чтение на submit, — но ни один не доступнее по своей природе. Работа по доступности (связь label, озвучивание ошибок, focus management, нативная семантика) одинакова независимо от того, как вы храните значение, поэтому не смешивайте модель состояния с моделью a11y." },
  ]}
/>

<Recap lang="ru">
Если вы смогли восстановить каждый ответ по памяти, вы держите хребет юнита: каждому контролу нужно программно связанное имя (placeholder им не является, и треть веба это проваливает); ошибки должны озвучиваться через уже существующий live-регион и быть привязаны к полю через aria-invalid + aria-describedby и сформулированы как подсказки; неудачный submit должен переместить фокус на первое невалидное поле, а не просто прокрутить; нативные элементы несут role, фокусируемость и обработку клавиатуры, которые нельзя полностью воспроизвести; в момент preventDefault вы наследуете всё это; а controlled-vs-uncontrolled — это решение о состоянии, ортогональное доступности. Теперь, когда будете ревьюить форму, эти шесть вопросов — ваш чек-лист: на которые ответили быстрее всего — те закрыты, на которые пришлось думать — те ещё стоит повторить.
</Recap>
