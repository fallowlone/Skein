---
concepts:
- token-tiers
- css-custom-properties
- theming-dark-mode
- dtcg-format
- css-var-fallback
estMin: 14
lang: ru
level: senior
order: 9
prereqs: []
slug: quiz-code
sources:
- https://www.designtokens.org/tr/drafts/format/
- https://styledictionary.com/info/dtcg/
status: ready
summary: "Читайте реальный CSS, DTCG JSON и theme-override; предсказывайте, как каскад разрешает каждый токен, и выбирайте фикс, оставляющий компоненты независимыми от темы."
title: 'Design-токены: чтение кода и конфигов'
track: frontend
unit: 04-tokens
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Баги токенов живут в CSS и JSON, а не в дизайн-доке. Прочитайте каждый сниппет, проследите, как каскад или сборка его разрешают, и выберите фикс, который senior сделает первым.
</Hook>

<Goal lang="ru">
Потренируйтесь читать код токенов так, как его дебажат: проследите ссылку через тиры, предскажите, что пере-разрешает theme-override, и заметьте ту единственную строку, что запирает компонент вне тематизации.
</Goal>

## Сниппет 1 — ссылка между тирами

```css
:root {
  --color-blue-600: #0052CC;                  /* primitive */
  --color-interactive: var(--color-blue-600); /* semantic  */
}

.button { background: var(--color-blue-600); }
.link   { color: var(--color-interactive); }
```

<Quiz
  id="04-tokens-code-1"
  lessonSlug="04-tokens"
  lang="ru"
  question="Позже dark-тема переопределяет --color-interactive (не primitive). Какой элемент после этого следует за dark mode и в чём фикс для второго?"
  choices={[
    { label: "Следует только .link — он читает semantic-токен, который переопределяет тема. .button багнут: ссылается на primitive напрямую, поэтому остаётся blue-600. Фикс: поменять .button на background: var(--color-interactive).", correct: true },
    { label: "Следуют оба, потому что оба в итоге разрешаются в blue-600.", misconception: "То, что сегодня они разрешаются в одно значение, не значит, что оба следуют за override. Тема переопределяет semantic-имя; .button читает primitive, поэтому изменения не видит." },
    { label: "Не следует ни один — переопределение semantic-токена не может затронуть уже объявленные правила.", misconception: "Custom properties каскадируют вживую: переопределение --color-interactive пере-разрешает каждую ссылку var(--color-interactive). .link перекрашивается за одну перерисовку; .button — нет, потому что пропустил semantic-тир." },
    { label: "Следует .button, а .link нет, ведь .button называет нижележащее значение.", misconception: "Всё наоборот. Именование primitive прибивает .button к этому значению; тема переопределяет semantic-токен, который читает только .link." },
  ]}
/>

## Сниппет 2 — DTCG-источник

```json
{
  "color": {
    "blue": { "600": { "$value": "#0052CC", "$type": "color" } },
    "interactive": { "$value": "{color.blue.600}", "$type": "color" }
  },
  "space": {
    "4": { "$value": "16px", "$type": "dimension" }
  }
}
```

<Quiz
  id="04-tokens-code-2"
  lessonSlug="04-tokens"
  lang="ru"
  question="Style Dictionary собирает этот DTCG-источник для web и Android. Что верно о том, как color.interactive и space.4 выходят под каждую платформу?"
  choices={[
    { label: "color.interactive разрешает алиас {color.blue.600} в #0052CC, затем работают transforms: CSS выдаёт --color-interactive: #0052CC и --space-4: 1rem, а Android — 8-значный ARGB-цвет и snake_case-размер. Один источник, платформенно-корректные выходы.", correct: true },
    { label: "Алиас {color.blue.600} копируется дословно в каждый выход, поэтому Android поставляет literal-строку {color.blue.600}.", misconception: "Сборка разрешает алиасы до форматирования; ни одна платформа не получает синтаксис {ref}. Android получает разрешённое, трансформированное значение, а не строку алиаса." },
    { label: "space.4 остаётся 16px на каждой платформе, потому что $type — dimension.", misconception: "$type говорит transform, как конвертировать значение, а не оставить его фиксированным. CSS обычно трансформирует px в rem (1rem), а Android использует свой формат размеров." },
    { label: "Раз оба — $type color и dimension, transform не нужен, JSON и есть финальный артефакт.", misconception: "JSON — источник, никогда не рантайм-артефакт. Transforms адаптируют каждое значение (px в rem, hex в ARGB), а formatters выдают реальные файлы CSS и XML." },
  ]}
/>

## Сниппет 3 — theme-override

```css
:root              { --color-surface: #FFFFFF; --color-text: #111111; }
[data-theme="dark"]{ --color-surface: #0B0B0F; --color-text: #F5F5F5; }

.card {
  background: var(--color-surface);
  color: var(--color-text);
  border: 1px solid #E5E7EB;   /* hardcoded */
}
```

<Quiz
  id="04-tokens-code-3"
  lessonSlug="04-tokens"
  lang="ru"
  question="Переключение data-theme в dark перекрашивает большую часть .card, но оставляет один видимый дефект. В чём он и каков фикс?"
  choices={[
    { label: "Граница остаётся светло-серой #E5E7EB в dark mode — это hardcoded literal, не токен, поэтому каскаду нечего пере-разрешать. Фикс: добавить semantic --color-border, задать его под обоими селекторами и ссылаться на него.", correct: true },
    { label: "Ничего не сломано — границы не должны меняться между темами.", misconception: "Светло-серая граница на почти чёрной поверхности — видимый двухцветный дефект. Граница — это тематизируемое решение поверхности и нуждается в semantic-токене, как и всё остальное." },
    { label: "background и color тоже не обновятся, потому что override на другом селекторе.", misconception: "[data-theme=dark] переопределяет те же имена custom properties; каскад пере-разрешает каждый var(), который их читает. Не следует только hardcoded-граница, которая не var()." },
    { label: "Фикс — перенести границу в JavaScript и ставить её при смене темы.", misconception: "Это снова вводит JS для того, что каскад делает бесплатно. Определите semantic-токен границы под обоими селекторами, и граница тематизируется за одну перерисовку." },
  ]}
/>

## Сниппет 4 — fallback

```css
.badge {
  /* --brand-accent задаётся некоторыми host-страницами, отсутствует на других */
  color: var(--brand-accent);
  padding: var(--space-2, 8px);
}
```

<Quiz
  id="04-tokens-code-4"
  lessonSlug="04-tokens"
  lang="ru"
  question="На host-странице, которая никогда не задаёт --brand-accent, как ведёт себя каждое свойство и чему учит var()-fallback?"
  choices={[
    { label: "color становится invalid-at-computed-value и откатывается к inherited/initial (текст выглядит без темы); padding безопасно использует 8px, потому что var() берёт второй аргумент как fallback. Всегда давайте разумный fallback для токенов, которые потребитель может не определить.", correct: true },
    { label: "Оба свойства ломают всё правило, так что бейдж рендерится вообще без стилей.", misconception: "Одна невалидная ссылка на custom property не обнуляет правило. color становится invalid-at-computed-value (берёт inherited/initial), а padding нормально разрешается через свой fallback." },
    { label: "color по умолчанию откатывается к чёрному, потому что var() всегда дефолтит к initial color.", misconception: "var() без fallback даёт guaranteed-invalid значение, запуская обработку invalid-at-computed-value — inherited для color, а не hardcoded чёрный. Надёжный дефолт — явный fallback." },
    { label: "padding ломается, потому что var() не принимает fallback-аргумент.", misconception: "var(--name, fallback) — это ровно поддерживаемый синтаксис; второй аргумент используется, когда свойство не задано. Поэтому padding остаётся 8px." },
  ]}
/>

<Recap lang="ru">
Баги токенов читаются в CSS и JSON: компонент, называющий primitive, выпадает из тематизации, пока его сосед на semantic-ссылке перекрашивается; DTCG-сборка разрешает алиасы, затем трансформирует значения, так что один источник корректен под платформу; единственный hardcoded literal переживает переключение темы как видимый двухцветный дефект; а var() с fallback — это ремень безопасности для токенов, которые потребитель может не определить. Проследите ссылку через тиры — и фикс почти всегда «ссылайся на semantic-токен, с fallback».
</Recap>
