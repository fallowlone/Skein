---
concepts:
- token-tiers
- semantic-tokens
- theming-dark-mode
- css-custom-properties
- token-pipeline
- token-drift
estMin: 13
lang: ru
level: senior
order: 7
prereqs: []
slug: quiz-choice
sources:
- https://www.designtokens.org/tr/drafts/format/
- https://styledictionary.com/info/dtcg/
status: ready
summary: "Синтез по всему юниту в формате выбора: границы тиров, тематизация через semantic-токены, кросс-платформенный пайплайн, каскад CSS-переменных и как drift возвращается."
title: 'Design-токены: обзор с выбором ответа'
track: frontend
unit: 04-tokens
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Шесть вопросов через весь юнит. Каждый отражает реальное решение — где живёт токен, на что компоненту разрешено ссылаться, когда пайплайн окупается — а не определение для пересказа.
</Hook>

<Goal lang="ru">
Убедитесь, что можете связать три тира, тематизацию через semantic-токены, кросс-платформенную сборку и дисциплину, которая не даёт drift вернуться — синтез, к которому вёл урок.
</Goal>

<Quiz
  id="04-tokens-choice-1"
  lessonSlug="04-tokens"
  lang="ru"
  question="Ревью показывает кнопку со стилем background: var(--color-blue-600). В системе есть primitive-, semantic- и component-тиры. Почему это дефект, хотя цвет формально и есть токен?"
  choices={[
    { label: "Кнопка ссылается на primitive напрямую, минуя semantic-тир. Теперь она прибита к одному значению палитры, поэтому dark mode и ребрендинг придётся править вручную — ровно тот форк, который токены и устраняют.", correct: true },
    { label: "Всё нормально — blue-600 это токен, значит система соблюдает единый источник истины.", misconception: "Именование значения не цель; цель — провести intent через semantic-слой. Компонент, привязанный к primitive, не может следовать за theme-override, то есть на практике выпал из системы токенов." },
    { label: "Проблема в синтаксисе var(); кнопке стоит вписать hex напрямую ради производительности.", misconception: "Вписать hex строго хуже — это literal, исходный источник drift. Разрешение var() практически бесплатно; дефект в тире, а не в синтаксисе." },
    { label: "Component-токены обязательны, поэтому кнопка должна сначала определить button.background.", misconception: "Component-токены опциональны. Кнопка могла бы корректно ссылаться прямо на color.interactive.default; баг — в ссылке на primitive, а не в отсутствии component-токена." },
  ]}
/>

<Quiz
  id="04-tokens-choice-2"
  lessonSlug="04-tokens"
  lang="ru"
  question="Dark mode должен перекрасить все компоненты без JavaScript и за одну перерисовку. Какой подход даёт это в вебе?"
  choices={[
    { label: "Переопределить те же имена semantic-токенов под селектором [data-theme=dark]; каскад пере-разрешает каждый var(), который на них ссылается, поэтому компоненты вообще не трогаются.", correct: true },
    { label: "Читать JS-объект темы в каждом компоненте через state и проставлять inline-стили при монтировании.", misconception: "Это переизобретает то, что каскад делает бесплатно, добавляет стоимость ре-рендера и риск мигания до гидрации. CSS-переменные перекрашиваются за одну перерисовку без JS." },
    { label: "Поставлять отдельный dark-stylesheet, который заново объявляет каждое правило компонента с тёмными hex.", misconception: "Это форк-копия каждого правила — режим отказа с двухцветной кнопкой. Вы поддерживаете два набора стилей компонентов, которые расходятся независимо." },
    { label: "Переопределить только primitive-токены палитры, а компоненты пусть ссылаются на primitive, чтобы подхватить новые значения.", misconception: "Компоненты должны ссылаться на semantic, не на primitive. Переопределение primitive сработать может, но дисциплина — переопределять semantic-слой, чтобы сохранить intent и оставить компоненты независимыми от темы." },
  ]}
/>

<Quiz
  id="04-tokens-choice-3"
  lessonSlug="04-tokens"
  lang="ru"
  question="Команда авторит токены один раз в DTCG JSON и гоняет Style Dictionary. Какой шаг несёт нагрузку, делая один источник корректным на web, iOS и Android?"
  choices={[
    { label: "Платформенные transforms: один space.4 становится 1rem для CSS, snake_case-числом для Android, camelCase для Swift; цвет становится hex для CSS, UIColor для iOS, 8-значным ARGB для Android — сгенерировано, не скопировано руками.", correct: true },
    { label: "Сам JSON-файл — раз значения именованы, каждая платформа читает один файл напрямую в рантайме.", misconception: "Платформы не потребляют DTCG JSON в рантайме. Сборка разрешает алиасы и выдаёт нативные артефакты (CSS, colors.xml, Swift-файл); корректность даёт шаг transform." },
    { label: "Синтаксис алиасов {group.token} — одни алиасы гарантируют кросс-платформенную корректность.", misconception: "Алиасы решают разрешение ссылок внутри источника, но CSS px неверен для Android XML, а hex неверен для UIColor. Значения под платформу адаптируют transforms, не алиасы." },
    { label: "Общая библиотека компонентов, импортирующая JSON в каждое приложение.", misconception: "Импорт сырого JSON всё равно оставляет каждой платформе самой трактовать значения — это и есть ручной форк. Задача пайплайна — выдать платформенные файлы через transforms и formatters." },
  ]}
/>

<Quiz
  id="04-tokens-choice-4"
  lessonSlug="04-tokens"
  lang="ru"
  question="Команда из пяти человек поставляет одно веб-приложение, одну тему, без нативных таргетов. Они предлагают полную трёхтировую систему со сборкой Style Dictionary. Как читает это senior?"
  choices={[
    { label: "Сейчас это over-engineering. Двух тиров (primitive + semantic) как CSS-переменных достаточно; component-токены и пайплайн добавляют только когда реально появляется второй output-таргет или тема.", correct: true },
    { label: "Верно — каждый проект должен использовать полный пайплайн с первого дня независимо от таргетов.", misconception: "Каждый тир — это indirection, которую новый инженер должен трассировать, и место, где прячется баг. С одним таргетом и одной темой стоимость пайплайна перевешивает предотвращённый drift." },
    { label: "Им стоит вообще отказаться от токенов и хардкодить hex, пока нет нативных приложений.", misconception: "Даже одно веб-приложение выигрывает от primitive- плюс semantic-тиров, чтобы ребрендинг или будущий dark mode стали одной правкой. Вопрос в количестве тиров, а не в том, токенизировать ли вообще." },
    { label: "Им нужнее всего component-токены, ведь они изолируют каждый компонент.", misconception: "Component-токены — самый опциональный тир и добавляют больше всего хопов. Их вводят только когда конкретному компоненту нужно отклониться, а не по умолчанию." },
  ]}
/>

<Quiz
  id="04-tokens-choice-5"
  lessonSlug="04-tokens"
  lang="ru"
  question="Через три недели после выката токенов grep находит свежие захардкоженные hex по новым компонентам. Модель тиров корректна. В чём устойчивое решение?"
  choices={[
    { label: "Lint-гейт — правило stylelint, делающее сырой hex вне primitive-файла ошибкой сборки. Конвенция без принудительного гейта разваливается, как только кто-то в спешке вставит literal.", correct: true },
    { label: "Переобучить команду на воркшопе и довериться, что конвенцию будут соблюдать.", misconception: "Обучение выветривается; следующий дедлайн снова вернёт literals. Drift — проблема процесса, и надёжно держит линию только автоматический гейт в CI." },
    { label: "Добавить больше semantic-токенов, чтобы всегда было к чему обратиться.", misconception: "Покрытие помогает, но не мешает кому-то всё равно вставить hex. Без ошибки на этапе сборки система эродирует независимо от полноты палитры." },
    { label: "Перейти на inline-стили, чтобы значения жили рядом с компонентом.", misconception: "Inline-hex — это исходный drift, лишь перенесённый. Решение — запретить сырые значения, а не делать их более локальными." },
  ]}
/>

<Quiz
  id="04-tokens-choice-6"
  lessonSlug="04-tokens"
  lang="ru"
  question="Дизайнер спрашивает, почему кнопка не может просто ссылаться на color.blue.600, раз она всё равно к этому и разрешается. В чём точная причина лишнего semantic-хопа?"
  choices={[
    { label: "Имя несёт intent, а не просто значение. button → color.interactive → color.blue.600 значит, что тема может переопределить 'interactive' в другой синий без правки кнопки, а ребрендинг правит primitive один раз, и все маппинги intent держатся.", correct: true },
    { label: "Это чисто оптимизация производительности — лишний var() разрешается быстрее глубокого поиска.", misconception: "Стоимость разрешения var() ничтожна, и лишний хоп ничего не ускоряет. Выгода — в intent и возможности theme-override, не в производительности." },
    { label: "Реальной причины нет; semantic-тир — конвенция ради конвенции.", misconception: "Semantic-тир — это то, что позволяет dark mode и ребрендингу быть одной правкой. Убрав его, вы заново сцепляете каждый компонент с сырой палитрой — ключевой режим отказа юнита." },
    { label: "DTCG запрещает компонентам ссылаться на primitive, поэтому хоп — требование спецификации.", misconception: "DTCG определяет формат, а не то, на какой тир может ссылаться потребитель. Хоп — это архитектурная дисциплина, дающая тематизацию и ребрендинг-в-одну-правку, а не правило спеки." },
  ]}
/>

<Recap lang="ru">
Сквозная линия — одно правило с последствиями: компонент ссылается на semantic-токен, никогда на primitive и никогда на literal. Именно эта дисциплина позволяет каскаду бесплатно тематизировать dark mode, позволяет одному DTCG-источнику генерировать корректные CSS, Swift и Android через transforms и делает ребрендинг одной правкой primitive-слоя. Размеряйте тиры под таргеты, которые поставляете, и закрывайте правило линтером — потому что конвенция без принудительного гейта за недели сползает обратно к захардкоженным hex.
</Recap>
