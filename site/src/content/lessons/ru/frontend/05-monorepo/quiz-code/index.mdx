---
concepts:
- task-graph
- cache-key-inputs
- affected-detection
- pipeline-config
estMin: 14
lang: ru
level: senior
order: 9
prereqs: []
slug: quiz-code
sources:
- https://turborepo.dev/docs/reference/configuration
- https://nx.dev/docs/features/enforce-module-boundaries
- https://turborepo.dev/docs/core-concepts/caching
status: ready
summary: "Читай реальные конфиги Turborepo и Nx, предсказывай task graph и поведение кэша и выбирай фикс с наибольшим рычагом."
title: 'Monorepo: чтение конфигов и пайплайна'
track: frontend
unit: 05-monorepo
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Конфиг пайплайна и ключ кэша — это места, где monorepo-проблемы реально диагностируются. Прочитай конфиг, предскажи task graph и то, что хэшируется в кэш, затем выбери фикс, который senior-инженер сделает первым.
</Hook>

<Goal lang="ru">
Отработай цикл, который ты запускаешь на каждом медленном или небезопасном monorepo-пайплайне: прочти конфиг задач, выведи порядок и ключ кэша, замечай false hit или false miss и тянись к структурному фиксу раньше, чем добавлять железо.
</Goal>

## Сниппет 1 — task graph

```jsonc
// turbo.json
{
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**"]
    },
    "test": {
      "dependsOn": ["build"],
      "outputs": []
    }
  }
}
```

<Quiz
  id="05-monorepo-code-1"
  lessonSlug="05-monorepo"
  lang="ru"
  question="Какой порядок даёт этот конфиг и в чём разница между двумя записями dependsOn?"
  choices={[
    { label: "^build значит сначала собрать все зависимости пакета (каретка пересекает границы пакетов вверх по графу); build (без каретки) в test значит, что собственный build пакета должен завершиться до его собственного test. Так граф собирает листья первыми, затем зависимых, затем тестирует каждый пакет после его build.", correct: true },
    { label: "Обе записи dependsOn значат одно и то же; каретка косметическая.", misconception: "Каретка несущая. ^build = build-задача ЗАВИСИМОСТЕЙ этого пакета; build = build-задача ЭТОГО пакета. Убрав каретку, ты пропустишь сборку вышестоящих библиотек до приложения, которое их потребляет." },
    { label: "^build запускает каждый build в репозитории раньше всего, игнорируя граф.", misconception: "^build ограничен собственным поддеревом зависимостей текущего пакета, а не всем репозиторием. Turborepo обходит граф и упорядочивает только вышестоящие пакеты, от которых этот реально зависит." },
    { label: "test с пустыми outputs значит, что результаты тестов никогда не кэшируются.", misconception: "Пустые outputs значат, что нет файловых артефактов для восстановления, но задача всё равно кэшируется: попадание воспроизводит записанный успех и логи и пропускает повторный запуск. outputs перечисляет файлы для сохранения, а не факт кэширования." },
  ]}
/>

## Сниппет 2 — ключ кэша

```jsonc
// turbo.json — входы задачи build
{
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "inputs": ["src/**/*.ts", "src/**/*.tsx"],
      "outputs": ["dist/**"]
    }
  }
}
```

<Quiz
  id="05-monorepo-code-2"
  lessonSlug="05-monorepo"
  lang="ru"
  question="Изменение в общем корневом tsconfig.json меняет вывод компилятора, но каждый build всё равно попадает в кэш и отдаёт устаревший dist. Читая эти inputs, в чём баг и фикс?"
  choices={[
    { label: "Glob входов ловит только src-файлы TypeScript, поэтому tsconfig.json не входит в хэш — false hit. Реальные входы build включают tsconfig (и всю цепочку extends) и версию компилятора; добавь их в inputs (или используй globalDependencies для корневого tsconfig), чтобы смена конфига инвалидировала кэш.", correct: true },
    { label: "outputs неверны; dist надо кэшировать иначе.", misconception: "outputs верны — dist это артефакт для сохранения и восстановления. Дефект на стороне ВХОДОВ: вход, меняющий вывод (tsconfig), отсутствует в хэше, поэтому ключ не меняется, когда должен." },
    { label: "В dependsOn нет test, поэтому сборки идут не по порядку.", misconception: "Порядок для build-задачи в норме; это проблема устаревшего вывода (false hit), а не порядка. Фикс — расширить inputs, чтобы покрыть каждый файл и инструмент, влияющий на скомпилированный вывод." },
    { label: "Просто отключи кэш для build, чтобы быть в безопасности.", misconception: "Это выбрасывает всю выгоду кэша ради обхода одного отсутствующего входа. Senior-фикс — сделать ключ корректным (добавить tsconfig и toolchain в хэш), чтобы попадания оставались быстрыми И безопасными." },
  ]}
/>

## Сниппет 3 — affected-детекция

```bash
# CI: собрать/протестировать только то, что этот PR мог сломать
npx nx affected -t build test --base=origin/main --head=HEAD
```

<Quiz
  id="05-monorepo-code-3"
  lessonSlug="05-monorepo"
  lang="ru"
  question="На долгоживущей release-ветке эта команда вдруг сообщает, что для крошечного diff затронут весь репозиторий. Самая вероятная причина?"
  choices={[
    { label: "Неверная база: --base=origin/main на долгоживущей release-ветке сравнивает с далёким merge-base, поэтому большая накопленная дельта делает почти всё затронутым. Направь --base на верную точку ветвления (истинную базу PR или последний release-тег), чтобы diff отражал только этот PR.", correct: true },
    { label: "Nx affected недетерминирован и иногда возвращает всё.", misconception: "affected детерминирован: это обход графа поверх git diff. Результат во весь репозиторий для малого изменения — сигнал, что diff (диапазон base/head) неверен, а не что алгоритм нестабилен." },
    { label: "Remote cache переполнен, поэтому affected откатывается к сборке всего.", misconception: "Scope affected вычисляется из git diff и графа проектов, полностью независимо от заполненности кэша. Полный кэш даёт промахи (пересборки), но никогда не раздувает affected-набор." },
    { label: "build и test надо запускать отдельными вызовами.", misconception: "Запуск нескольких целей в одном вызове affected нормален и задуман. Раздутый набор идёт от base-ревизии, охватывающей слишком большой diff, а не от объединения целей." },
  ]}
/>

## Сниппет 4 — границы модулей

```jsonc
// .eslintrc — @nx/enforce-module-boundaries
{
  "depConstraints": [
    { "sourceTag": "type:feature", "onlyDependOnLibsWithTags": ["type:ui", "type:util"] },
    { "sourceTag": "type:ui",      "onlyDependOnLibsWithTags": ["type:util"] },
    { "sourceTag": "type:util",    "onlyDependOnLibsWithTags": ["type:util"] }
  ]
}
```

<Quiz
  id="05-monorepo-code-4"
  lessonSlug="05-monorepo"
  lang="ru"
  question="Какой импорт этот конфиг отклоняет на линте и какую проблему графа предотвращает отклонение?"
  choices={[
    { label: "Util-библиотека, импортирующая из feature- или ui-библиотеки, отклоняется — util может зависеть только от util. Это не даёт низкоуровневому общему коду набрать огромный fan-in или образовать цикл — именно это превращает affected-наборы и инвалидацию кэша в события на весь репозиторий.", correct: true },
    { label: "feature, импортирующий из ui-библиотеки, отклоняется.", misconception: "Это ребро явно РАЗРЕШЕНО: type:feature перечисляет type:ui в onlyDependOnLibsWithTags. Конфиг разрешает нисходящее направление feature → ui → util и отклоняет восходящие рёбра." },
    { label: "Правило ничего не отклоняет; оно лишь предупреждает о стиле.", misconception: "enforce-module-boundaries роняет линт на нарушающем ребре зависимости. Это жёсткое ограничение на форму графа, а не предупреждение о стиле — именно это удерживает границы от размывания." },
    { label: "Оно отклоняет две util-библиотеки, импортирующие друг друга.", misconception: "util → util разрешён конфигом, поэтому именно это ребро проходит правило. Прямые циклы ловятся отдельно; этот сниппет отклоняет восходящее направление util → feature/ui." },
  ]}
/>

<Recap lang="ru">
Каждая проблема monorepo-пайплайна читается в конфиге и в git-диапазоне: каретка в dependsOn решает, собираются ли вышестоящие пакеты первыми; glob inputs — это ключ кэша, где отсутствующий tsconfig — false hit (ложное попадание, отдающее устаревший артефакт), а волатильный путь — false miss (ложный промах, отключающий кэш); scope affected — это обход графа поверх diff, поэтому неверный --base раздувает его до всего репозитория; а depConstraints не дают низкоуровневым библиотекам набрать fan-in, делающий каждое изменение глобальным. Прочти конфиг, почини ключ или границу, затем перезапусти и подтверди, что affected-набор и hit rate ведут себя верно. Теперь, открывая незнакомый turbo.json или nx.json, ты знаешь, что искать: каретку, список inputs и базовую ветку — эти три строки скажут, корректен ли пайплайн.
</Recap>
