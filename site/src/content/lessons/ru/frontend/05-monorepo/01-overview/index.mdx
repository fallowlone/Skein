---
concepts:
- dependency-graph
- affected-graph
- remote-task-caching
- module-boundaries
- monorepo-vs-polyrepo
deepensInto: []
estMin: 16
lang: ru
lessonType: topic
level: junior
order: 1
prereqs: []
slug: 01-overview
sources:
- https://nx.dev/docs/features/enforce-module-boundaries
- https://turborepo.dev/docs/core-concepts/remote-caching
- https://engineering.mercari.com/en/blog/entry/20260216-turborepo-remote-cache-accelerating-ci-to-move-fast/
- https://vercel.com/blog/turborepo-remote-cache-nextjs-publish-times-80-percent
spiral: []
status: ready
summary: Монорепозиторий быстр, только если инструмент знает граф зависимостей и собирает
  лишь изменённое. Ошибись с границами — один пакет «shared utils», который импортируют
  все, — и каждый PR пересобирает весь мир.
title: 'Границы монорепозитория: граф зависимостей решает твой счёт за CI'
track: frontend
unit: 05-monorepo
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";
import Infographic from "~/components/diagram/Infographic.astro";
import remoteCacheWins from "./infographics/remote-cache-wins.svg?raw";

<Hook>
Правка двух строк текста на маркетинговой странице висит в CI 41 минуту. Изменение задело один компонент, но пайплайн пересобрал и протестировал все 38 проектов, потому что импорт резолвился через `@acme/utils` — пакет «shared utils», от которого зависит каждое приложение и каждая библиотека в репозитории. Тронешь что-то, что трогает utils, — и affected-граф становится всем репозиторием. Вердикт команды на ретро: «монорепозиторий тормозит». Тормозил не монорепозиторий. Неправильной была граница.
</Hook>

<Crux>Монорепозиторий быстр, только если граф верный: собирай изменённое и его зависимых, а не весь мир, в каждом PR.</Crux>

<Explanation>

## Граф зависимостей — это и есть продукт

Монорепозиторий — это не «весь код в одной папке». Это просто папка. Быстрым или медленным монорепозиторий делает **граф зависимостей** — направленный граф того, какой пакет какой импортирует. Turborepo и Nx строят этот граф, разбирая твои импорты и workspaces в `package.json`, и используют его для двух задач: упорядочивание (собрать библиотеку до приложения, которое её потребляет) и определение масштаба (вычислить радиус взрыва изменения).

Каждый узел — пакет или проект; каждое ребро — зависимость. Здоровый граф широкий и неглубокий: много листовых библиотек, мало кто зависит от любой одной из них. Больной граф имеет **хаб** — один узел с огромным fan-in, который лежит на пути между всем. Этот хаб обычно зовётся `utils`, `common` или `shared`, и это самая дорогая ошибка в монорепозитории, потому что алгоритм affected-графа хорош ровно настолько, насколько хорош граф, который ты ему дал.

## «Affected» против собрать-всё

Наивный CI-пайплайн запускает `build && test` по каждому пакету в каждом PR. На 5 пакетах никто не замечает. На 40 полный прогон — это 30–45 минут, и очередь на мердж встаёт. Фикс — **affected** (Nx) / **filtered** (Turborepo): вычислить diff с базовой веткой, найти изменённые проекты, пройтись по графу и добавить каждый проект, который *зависит* от изменённого, и запустить задачи только для этого набора.

Цифры драматичны, когда граф чистый. Workspace на 20 пакетов обычно падает с ~14 мин (собрать всё) до 3–5 мин (только affected). Когда изменение локально для листа, affected-набор может быть одним проектом, и CI завершается меньше чем за минуту. На этом стоит вся ставка монорепозитория: большинство PR трогают маленький срез, поэтому большинство PR должны платить за маленький срез.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Стратегия</th>
        <th class="border border-border px-3 py-2 text-left">Что бежит на PR</th>
        <th class="border border-border px-3 py-2 text-left">~CI на 40 пакетах</th>
        <th class="border border-border px-3 py-2 text-left">Режим отказа</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">Собрать всё</td>
        <td class="border border-border px-3 py-2">Все проекты, всегда</td>
        <td class="border border-border px-3 py-2">30–45 мин</td>
        <td class="border border-border px-3 py-2">Растёт с размером репо, не изменения</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Только affected</td>
        <td class="border border-border px-3 py-2">Изменённые проекты + их зависимые</td>
        <td class="border border-border px-3 py-2">1–5 мин (типичный PR)</td>
        <td class="border border-border px-3 py-2">Хаб-пакет делает «affected» = всё</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Affected + remote cache</td>
        <td class="border border-border px-3 py-2">Только задачи без кэшированного вывода</td>
        <td class="border border-border px-3 py-2">секунды–2 мин на тёплом кэше</td>
        <td class="border border-border px-3 py-2">Слишком широкий ключ → ложные промахи</td>
      </tr>
    </tbody>
  </table>
</div>

## Remote caching: второй множитель

Affected-определение говорит «не запускай нетронутые проекты». Remote caching говорит «даже не запускай тронутые проекты, если кто-то уже произвёл ровно этот вывод». Turborepo и Nx хэшируют каждый вход задачи — исходники, зависимости, env-переменные, конфиг задачи — и хранят вывод (артефакты сборки, результаты тестов, логи) под этим хэшем в общем кэше. Если коллега или твой прошлый прогон CI уже собрал этот хэш, задача — это **попадание в кэш**: вывод скачивается, а не пересчитывается, за миллисекунды.

Когда ты это подключаешь, именно это и замыкает петлю между разработчиками и CI. Vercel замерил падение времени публикаций Next.js на 80% после того, как сборки SWC стали шариться через remote cache. Mercari отчитался примерно о 50%-м сокращении длительности Turbo-задач и 30%-м сокращении общей длительности джоба после подключения remote cache Turborepo к GitHub Actions. Частая цифра тёплого кэша — CI с ~6 минут до ~45 секунд. Cache hit rate — это метрика, за которой следишь: в репозитории на много пакетов, где большинство PR локальны, достижимы hit rate в 80–90%, и каждый процент hit rate — это минуты CI (и деньги), которые ты не тратишь.

<Infographic
  svg={remoteCacheWins}
  label="Горизонтальная столбчатая диаграмма заявленных сокращений времени CI/сборки от remote caching в Turborepo и Nx: тёплый кэш роняет прогон с ~6 минут до 45 секунд (≈88%), Vercel отчитался о 80% на публикациях Next.js, Mercari — о 50% на длительности Turbo-задач и 30% на общей длительности джоба."
  caption="Remote caching — второй множитель: когда выводы задач шарятся между CI и разработчиками, команды отчитываются о 30–88% меньшем времени сборки — ради этой экономии и заключается ставка монорепозитория."
  hue="var(--d-frontend)"
/>

<Inset kind="why" lang="ru">
Ключ кэша решает всё. Если забыть включить вход — скажем, общий `tsconfig` или env-переменную, меняющую вывод, — получишь *ложное попадание*: кэш отдаёт устаревший артефакт и выкатывает баг. Если включить слишком много — волатильный файл, меняющийся каждый прогон, или слишком широкий glob, — получишь *ложный промах*: хэш никогда не повторяется, кэш бесполезен, а за хранение ты заплатил впустую. Тюнинг cache hit rate — это в основном тюнинг того, что считается входом.
</Inset>

<FlowDiagram
  label="Как инструмент, знающий граф, решает, что запускать для PR, в пять упорядоченных шагов: построить граф зависимостей из импортов, сделать diff PR, чтобы найти изменённые проекты, пройти по графу и добавить каждого зависимого (affected-набор), захэшировать каждую affected-задачу и проверить remote cache, затем запустить только задачи-промахи."
  perRow={5}
  hue="var(--d-frontend)"
  nodes={[
    { id: "graph", label: "Граф", sub: "из импортов" },
    { id: "diff", label: "Diff PR", sub: "изменённые" },
    { id: "walk", label: "Проход", sub: "+ зависимые" },
    { id: "hash", label: "Хэш + кэш", sub: "проверка" },
    { id: "run", label: "Запуск промахов", sub: "качай попадания" },
  ]}
  edges={[
    { from: "graph", to: "diff" },
    { from: "diff", to: "walk" },
    { from: "walk", to: "hash" },
    { from: "hash", to: "run" },
  ]}
  caption="Affected-определение (шаги 2–3) сужает кандидатов до радиуса взрыва изменения — изменённые проекты плюс их зависимые; remote cache (шаги 4–5) затем превращает большинство в мгновенные попадания. Хаб «shared utils» делает шаг 3 равным всему репо, поэтому affected-набор — это всё, и каждый зависимый промахивает кэш."
/>

## Границы: останови хаб до того, как он сформируется

Почему здоровый монорепозиторий деградирует в машину 40-минутных сборок? Почти всегда ответ — один god-пакет, который импортируют все. Фикс — не железо помощнее, а граф широкий и неглубокий, защищённый границами с первого дня.

Причина, по которой «shared utils» ломает affected-граф (то есть граф, по которому инструмент вычисляет радиус взрыва изменения), структурная, а не баг инструмента: если 30 проектов его импортируют, то по определению изменение в нём задевает 30 проектов, поэтому кэш инвалидируется для всех них и CI пересобирает мир. Фикс — **обеспечить границы модулей**, чтобы граф вообще не мог выродиться в кашу хаб-и-спицы.

Nx делает это через **теги** и ESLint-правило `@nx/enforce-module-boundaries`. Ты тегируешь каждый проект (`type:feature`, `type:ui`, `scope:checkout`) и объявляешь `depConstraints`: feature может зависеть от ui, ui может зависеть только от util, ничто не может зависеть от feature. Импорты, нарушающие правило, валят линт — и то же правило ловит **циклические зависимости** («Circular dependency between A and B detected»), это другой способ превратить граф в спагетти. Turborepo опирается на **границы пакетов** и `exports` в `package.json`, чтобы публичная поверхность пакета была явной, и потребители не могли лезть в его внутренности. Ход сеньора — разбить god-пакет: вместо одного `utils`, который импортируют все, выпустить `@acme/format-date`, `@acme/http`, `@acme/result`, у каждого узкий fan-in, чтобы изменение в форматировании даты инвалидировало только горстку того, что реально форматирует даты.

<TradeoffMatrix
  id="05-monorepo-overview-tradeoff-1"
  lessonSlug="01-overview"
  lang="ru"
  prompt="CI добрался до 40+ мин: каждый PR пересобирает репо, потому что все импортируют один @acme/utils. Выбери фикс, который бьёт в причину."
  options={[
    { name: "Разбить @acme/utils на узкие пакеты и обеспечить теги, чтобы у большинства изменений был малый fan-in", summary: "Чинить граф.", correct: true, justification: "Это атакует настоящую причину — хаб. С format-date, http и result как отдельными листовыми пакетами изменение в одном инвалидирует только его реальных потребителей, поэтому affected-граф и инвалидация кэша сжимаются до среза. Обеспечение границ модулей (теги Nx / exports пакета) не даёт графу снова схлопнуться в хаб. Всё остальное — кэш, параллелизм, машины помощнее — лишь замазывает граф, в котором структурно всё-зависит-от-всего." },
    { name: "Добавить remote cache и считать задачу закрытой", summary: "Кэшировать мир.", justification: "Remote caching обязателен и он у тебя должен быть, но он не чинит хаб. Вся суть хаба в том, что изменение в нём порождает новый хэш входа для каждого зависимого, поэтому каждый зависимый — это промах кэша. Ты будешь попадать в кэш на PR, не трогающих utils, и всё равно пересобирать мир на каждом PR, который их трогает, — а это и есть частый случай из условия." },
    { name: "Купить машины CI помощнее и накрутить параллелизм", summary: "Кинуть железо.", justification: "Больше ядер прогонят те же 40 сборок быстрее, но ты всё равно собираешь 40 проектов ради однострочного изменения — платишь линейно с размером репо, а не изменения. Это покупает немного запаса и счёт побольше, и 41-минутный джоб вернётся по мере роста репо. Лечит симптом, не хаб." },
    { name: "Разбить монорепозиторий на много полирепозиториев", summary: "Уйти в polyrepo.", justification: "Это может быть верно для по-настоящему независимых команд, но меняет проблему CI на проблему координации: общий код теперь едет через реестр пакетов с бампами версий и синхронными PR по репозиториям. Для кодовой базы, которая реально столько шарит (все импортируют utils), polyrepo обычно делает изменение труднее, а не быстрее. Сначала почини границу." },
  ]}
/>

## Монорепозиторий против полирепозитория, честно

Суперсила монорепозитория — **атомарное изменение**: переименуй функцию в общей библиотеке и обнови всех вызывающих в одном PR, с одним прогоном CI, доказывающим, что всё ещё компилируется. Никаких бампов версий, реестра, «выкати либу, подожди, бампни потребителей, выкати их». Цена реальна: тебе нужны инструменты, знающие граф (Nx/Turborepo/Bazel), remote caching и обеспеченные границы, иначе репо рушится под собственным CI. Полирепозиторий переворачивает это: каждый репо тривиально быстрый и независимый, но общий код едет через реестр, и ломающее изменение в общем пакете означает синхронные бампы, дрейф версий и раскопки «какой сервис на старой версии». Честная формулировка: монорепозиторий меняет **более высокую сложность инструментов на меньшую цену координации**; полирепозиторий меняет **меньшую сложность инструментов на более высокую цену координации**. Выбирай монорепозиторий, когда пакеты много шарят и меняются вместе; выбирай полирепозиторий, когда команды по-настоящему независимы и общаются только по API.

<Quiz
  id="05-monorepo-overview-quiz-1"
  lessonSlug="01-overview"
  lang="ru"
  question="В монорепозитории на 40 пакетов что на самом деле вычисляет «affected» (Nx) / «filtered» (Turborepo) для PR?"
  choices={[
    { label: "Изменённые проекты плюс каждый проект, который транзитивно от них зависит, проходом по графу зависимостей", correct: true },
    { label: "Каждый проект в репо, просто параллельно", misconception: "Это собрать-всё. Affected существует именно чтобы не запускать нетронутые проекты; запуск всех параллельно всё равно растёт с размером репо." },
    { label: "Только конкретные файлы в git diff, игнорируя зависимых", misconception: "Единица — не файлы, а проекты. И нужно включать зависимых: изменение в библиотеке может сломать импортирующие её приложения, поэтому их тоже надо протестировать." },
    { label: "Всё, чего нет в remote cache", misconception: "Это работа кэша, отдельный шаг. Affected-определение решает, какие проекты являются кандидатами; затем кэш решает, кого из них можно пропустить, потому что вывод уже существует." },
  ]}
/>

<Quiz
  id="05-monorepo-overview-quiz-2"
  lessonSlug="01-overview"
  lang="ru"
  question="Cache hit rate высокий, но один PR отдал устаревшую сборку, которая выкатила баг. Самая вероятная причина?"
  choices={[
    { label: "Ложное попадание: вход, влияющий на вывод (общий tsconfig или env-переменная), не вошёл в ключ кэша, поэтому изменённая сборка захэшировалась в старую запись", correct: true },
    { label: "Кэш был просто слишком мал и вытеснил нужную запись", misconception: "Вытеснение даёт промах (пересборку), а не устаревший артефакт. Устаревший-но-отданный артефакт означает, что хэш совпал, когда не должен был, — пропущенный вход." },
    { label: "Affected-определение пропустило проект целиком", misconception: "Если affected его пропустил, для этого проекта вообще ничего не отдавалось. Устаревший отданный артефакт означает, что задача бежала, попала в кэш и получила не ту запись — проблема ключа." },
    { label: "Hit rate был слишком высокий, что само по себе небезопасно", misconception: "Высокий hit rate — не баг; баг — неверный ключ. Верные ключи дают верные попадания. Цель — высокий hit rate с ключом, который захватывает каждый реальный вход." },
  ]}
/>

<DragOrder
  id="05-monorepo-overview-drag-1"
  lessonSlug="01-overview"
  lang="ru"
  prompt="Расставь, как инструмент, знающий граф, решает, что запускать для PR:"
  items={[
    { id: "a", label: "Построить граф зависимостей из импортов + workspace-файлов package.json" },
    { id: "b", label: "Сделать diff PR с базовой веткой, чтобы найти напрямую изменённые проекты" },
    { id: "c", label: "Пройти по графу и добавить каждый проект, зависящий от изменённого (affected-набор)" },
    { id: "d", label: "Захэшировать входы каждой affected-задачи и проверить remote cache по этому хэшу" },
    { id: "e", label: "Запустить только задачи-промахи; скачать выводы для задач-попаданий" },
  ]}
  correctOrder={["a", "b", "c", "d", "e"]}
/>

</Explanation>

<KeyTakeaway>Граф зависимостей решает твой счёт за CI. Запускай только affected, шарь remote cache и обеспечь границы модулей, чтобы хаб «shared utils» не заставлял каждый PR пересобирать мир.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="05-monorepo-overview-retrieval"
  lang="ru"
  questions={[
    { q: "Объясни, почему один пакет «shared utils» может разогнать CI монорепозитория с 4 минут до 40, и что с этим делать.", a: "Affected-граф работает проходом по зависимым: когда проект меняется, каждый проект, который его импортирует (напрямую или транзитивно), попадает в affected-набор и должен быть пересобран и перетестирован. Пакет «shared utils» — это хаб: если 30 из твоих 38 проектов его импортируют, то любое изменение в utils по определению задевает 30 проектов. Хуже того, поскольку utils — вход для всех этих сборок, изменение его хэша делает их всех промахом кэша, поэтому remote caching тоже не спасёт: каждый зависимый обязан реально бежать. В итоге однострочное изменение где-то рядом с utils пересобирает почти весь репозиторий — это и есть 40-минутный джоб. Фикс структурный, а не подкрутка инструмента: разбей god-пакет на узкие листовые пакеты (format-date, http, result), чтобы у каждого был малый fan-in, и обеспечь границы модулей (теги Nx с depConstraints или exports в package.json), чтобы граф не мог снова схлопнуться в хаб. После разбиения изменение в форматировании даты инвалидирует только горстку проектов, которые форматируют даты, и affected-определение, и кэш делают свою работу." },
    { q: "В чём разница между affected-определением и remote caching, и почему нужны оба?", a: "Они решают разные половины одной задачи. Affected-определение отвечает на «какие проекты это изменение могло сломать?» — оно делает diff PR с базой, находит изменённые проекты и проходит по графу, добавляя их зависимых, поэтому нетронутые проекты вообще не запускаются. Remote caching отвечает на «из проектов в зоне видимости какие реально нужно запустить?» — оно хэширует входы каждой задачи и, если ровно этот хэш уже собран (тобой, коллегой или прошлым прогоном CI), скачивает сохранённый вывод вместо пересчёта. Нужны оба, потому что каждый оставляет пробел, который закрывает другой. Один affected всё равно пересобирает проект в зоне видимости каждый раз, даже если его входы идентичны прошлому прогону. Один кэш всё равно считает каждый проект кандидатом, тратя время на хэширование и проверку проектов, которые diff доказал нерелевантными. Вместе: affected сужает набор кандидатов до радиуса взрыва изменения, затем кэш превращает большинство этих кандидатов в мгновенные попадания — так чистый монорепозиторий держит cache hit rate в 80–90% и завершает большинство PR за секунды." },
  ]}
/>

<Recap lang="ru">
Монорепозиторий быстр или медленен из-за одной вещи — графа зависимостей. Инструменты, знающие граф (Nx, Turborepo), разбирают импорты в направленный граф и используют его, чтобы вычислить affected-набор — изменённые проекты плюс всё, что от них зависит, — поэтому большинство PR собирают срез, а не мир, часто роняя репо на 40 пакетов с 30–45 минут до 1–5. Remote caching умножает это, хэшируя входы каждой задачи и скачивая сохранённые выводы при попадании, — так команды отчитываются о 50–80% сокращении CI и прогонах на тёплом кэше за секунды; подвох — в ключе кэша, где пропущенный вход даёт опасное ложное попадание, а слишком широкий — бесполезные ложные промахи. Ничто из этого не переживёт плохого графа: один хаб «shared utils» делает affected-набор всем репозиторием и промахивает кэш для каждого зависимого, поэтому дисциплина, которая реально держит монорепозиторий быстрым, — это обеспеченные границы модулей: теги Nx, правило enforce-module-boundaries и явные exports пакетов, которые разбивают god-пакеты и запрещают циклические и кросс-доменные импорты. Против полирепозитория сделка честна: монорепозиторий покупает атомарные кросс-пакетные изменения ценой инструментов, знающих граф, и дисциплины границ; полирепозиторий покупает тривиальную скорость на репо ценой версионирования через реестр и кросс-репозиторной координации. Теперь, когда видишь, что CI гоняет весь репозиторий из-за правки в две строки, первый вопрос уже не «какой раннер слишком медленный?», а «какой пакет — хаб и как сузить его fan-in?»
</Recap>
