---
concepts:
- tree-shaking
- side-effects
- dev-prod-parity
- content-hashing
- bundler-internals
- transform-vs-bundle
estMin: 13
lang: ru
level: senior
order: 7
prereqs: []
slug: quiz-choice
sources:
- https://vite.dev/guide/why
- https://webpack.js.org/guides/tree-shaking/
- https://esbuild.github.io/faq/#why-is-esbuild-fast
status: ready
summary: "Тест с выбором на синтез по всему юниту — стыки стадий, причины провала tree-shaking, dev/prod-паритет, content hashing и компромиссы скорости инструментов."
title: 'Пайплайны сборки: тест с выбором ответа'
track: frontend
unit: 07-build-pipelines
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Шесть вопросов поперёк всего юнита. Каждый отражает решение, которое ты принимаешь, пока бандл регрессирует, dev-only баг уезжает в прод или стадия сборки выбивает таймаут в CI — не определение для заучивания, а компромисс, который надо взвесить.
</Hook>

<Goal lang="ru">
Убедись, что связываешь пять стадий пайплайна, причины провала tree-shaking, причины расхождения dev и prod и компромиссы content hashing и выбора инструмента — тот синтез, к которому вёл обзорный урок.
</Goal>

<Quiz
  id="07-build-pipelines-choice-1"
  lessonSlug="07-build-pipelines"
  lang="ru"
  question="Коллега меняет import debounce from 'lodash/debounce' на import _ from 'lodash', и основной бандл вырастает на ~200 KB. Почему tree-shaking не выкинул неиспользуемые функции?"
  choices={[
    { label: "Основная сборка lodash — CommonJS; её экспорты собираются в рантайме, поэтому bundler не может статически доказать, какие ключи не используются, и вынужден держать весь модуль. Используй lodash-es или глубокие импорты.", correct: true },
    { label: "Tree-shaking надо включать вручную для каждого импорта.", misconception: "Tree-shaking включён по умолчанию в production-бандлах. Здесь его ломает формат модуля — экспорты CommonJS вычисляются в рантайме и не поддаются статическому анализу." },
    { label: "Minify отработал до bundle, поэтому мёртвый код уже зафиксирован.", misconception: "Порядок стадий ни при чём. Даже при правильном порядке bundler держит весь CJS-модуль, потому что его экспорты не анализируемы статически." },
    { label: "Дефолтный импорт отключает shaking; именованный бы починил.", misconception: "Стиль импорта не корень проблемы — формат модуля. ES-модули шейкаются независимо от default vs named; CommonJS не шейкается ни так, ни так." },
  ]}
/>

<Quiz
  id="07-build-pipelines-choice-2"
  lessonSlug="07-build-pipelines"
  lang="ru"
  question="Чистая ESM-библиотека утилит всё равно тащит мёртвый код после сборки, хотя все импорты выглядят неиспользуемыми. Наиболее вероятная причина?"
  choices={[
    { label: "Пакет не объявляет или неверно объявляет поле sideEffects в package.json, поэтому bundler консервативно считает, что импорт любого файла делает наблюдаемую работу, и держит его.", correct: true },
    { label: "Библиотека слишком большая, чтобы bundler её проанализировал.", misconception: "Bundler шейкает ESM-графы любого размера. Размер ни при чём; модули держит именно отсутствующий контракт sideEffects." },
    { label: "Tree-shaking удаляет только целые файлы, никогда отдельные экспорты.", misconception: "Tree-shaking удаляет неиспользуемые экспорты на уровне отдельных биндингов внутри модуля, не только целые файлы. Флаг sideEffects управляет тем, можно ли вообще выкинуть файл." },
    { label: "Source map держит ссылки на мёртвый код живыми.", misconception: "Source map — это метаданные для отладки, эмитятся последними; они никогда не держат код в бандле. Рычаг — объявление sideEffects." },
  ]}
/>

<Quiz
  id="07-build-pipelines-choice-3"
  lessonSlug="07-build-pipelines"
  lang="ru"
  question="Фича работает в dev-сервере Vite, но ломается после vite build. Какова наиболее вероятная структурная причина, а не разовая случайность?"
  choices={[
    { label: "Dev отдаёт нативные несобранные ESM через esbuild, а prod собирает через Rollup. Разные резолвинг, code-splitting и обработка side effects могут законно расходиться — например, циклический импорт, который браузер терпит, а bundler переупорядочивает.", correct: true },
    { label: "Dev-сервер агрессивно кэшировал и отдал устаревший код.", misconception: "Устаревший кэш заставляет старое поведение сохраняться — противоположный симптом. Настоящий dev/prod-раскол — это два разных тулчейна, дающих разный вывод из одного исходника." },
    { label: "Production-минификация переписала поведение программы.", misconception: "Минификация иногда выявляет латентный баг, но структурная причина расхождения dev и prod в том, что они гоняют разные bundler-ы на по-разному сформированных графах модулей." },
    { label: "Переменные окружения не были подставлены в dev.", misconception: "Оба режима инлайнят env-переменные; расхождение здесь — архитектура сборки, не обработка env. Фикс — тестировать реальную prod-сборку, а не гоняться за инъекцией env." },
  ]}
/>

<Quiz
  id="07-build-pipelines-choice-4"
  lessonSlug="07-build-pipelines"
  lang="ru"
  question="Почему замена Babel на esbuild или SWC срезает стадию transform на 90%+, а стадия bundle редко получает такое же ускорение?"
  choices={[
    { label: "Transform пофайловый и неприлично параллельный, поэтому компилируемые многопоточные инструменты масштабируются по ядрам; bundle — обход всего графа зависимостей, в основном последовательный, и параллелится куда хуже.", correct: true },
    { label: "esbuild просто пропускает работу, которую делает Babel, выдавая менее корректный вывод.", misconception: "esbuild выдаёт корректный вывод; ускорение идёт от компилируемого языка, реальных потоков и единого общего AST — не от пропуска нужной работы." },
    { label: "Babel медленный только из-за формата конфига, не из-за рантайма.", misconception: "Формат конфига не влияет на стоимость рантайма. Babel медленный, потому что это однопоточный JavaScript, заново парсящий AST; стадия bundle остаётся последовательной при любом transformer." },
    { label: "Стадия bundle уже параллельна, поэтому любой transformer её догоняет.", misconception: "Bundling идёт по всему графу и в основном последователен — именно поэтому ускорения стадии transform на него не переносятся." },
  ]}
/>

<Quiz
  id="07-build-pipelines-choice-5"
  lessonSlug="07-build-pipelines"
  lang="ru"
  question="Вернувшиеся пользователи перекачивают большой vendor-чанк при каждом деплое приложения, хотя React и зависимости не менялись. Фикс?"
  choices={[
    { label: "Вынести vendor-код в собственный content-hashed чанк отдельно от кода приложения, чтобы хэш vendor оставался стабильным при частых деплоях и держался в кэше браузера.", correct: true },
    { label: "Отключить content hashing, чтобы имена файлов перестали меняться.", misconception: "Стабильные имена полностью ломают cache busting — пользователям отдавался бы устаревший код приложения. Фикс — границы чанков, а не удаление хэша." },
    { label: "Снизить max-age в Cache-Control, чтобы браузер ревалидировал чаще.", misconception: "Частая ревалидация увеличивает трафик — противоположная цель. С content hashing нужен большой max-age плюс immutable; реальная проблема — vendor делит чанк с кодом приложения." },
    { label: "Заинлайнить vendor-код в каждый чанк приложения, чтобы убрать лишний запрос.", misconception: "Инлайн vendor в чанки приложения гарантирует, что его хэш меняется при каждом деплое, ломая кэш каждый раз — ровно та проблема, что здесь." },
  ]}
/>

<Quiz
  id="07-build-pipelines-choice-6"
  lessonSlug="07-build-pipelines"
  lang="ru"
  question="Исходник — чистый ESM, но prod-бандл всё равно тащит целую зависимость, которую должен был отшейкать. Подозреваемая — стадия transform. Почему?"
  choices={[
    { label: "@babel/preset-env может понизить import/export до require/module.exports до того, как bundler их увидит, незаметно превращая шейкаемый граф в непрозрачный. Держи модули как ESM сквозь стадию transform.", correct: true },
    { label: "Tree-shaking происходит во время transform, поэтому медленный transformer его пропускает.", misconception: "Tree-shaking происходит на стадии bundle на графе модулей, а не во время пофайлового transform. Поломка на transform — это понижение ESM до CJS, которое убирает статическую структуру, нужную для shaking." },
    { label: "Transformer заминифицировал код, спрятав неиспользуемые экспорты.", misconception: "Transform и minify — разные стадии; transform не минифицирует. Реальная поломка — понижение ESM до CommonJS до сборки." },
    { label: "esbuild вообще не умеет tree-shaking, поэтому это должен делать transformer.", misconception: "esbuild хорошо шейкает ESM; проблема в том, что Babel-transform может конвертировать ESM в CJS выше по течению, после чего ни один bundler не отшейкает." },
  ]}
/>

<Recap lang="ru">
Сквозная линия: сборка — это resolve → transform → bundle → minify → emit, и стыки между стадиями — это где живут баги. Tree-shaking требует статического ESM, поэтому его ломают CommonJS, необъявленные side effects и transform-ы, понижающие ESM до CJS до того, как bundler заглянет. Transform параллелен (поэтому там выигрывают компилируемые инструменты), а bundle остаётся последовательным. Dev и prod гоняют разные инструменты — валидируй реальную prod-сборку. И content hashing окупается только если отделить стабильный vendor-код от часто меняющегося кода приложения.
</Recap>
