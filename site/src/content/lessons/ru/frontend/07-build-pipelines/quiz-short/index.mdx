---
concepts:
- pipeline-stages
- tree-shaking
- dev-prod-parity
- content-hashing
- source-maps
- build-caching
estMin: 14
lang: ru
level: senior
order: 8
prereqs: []
slug: quiz-short
sources:
- https://vite.dev/guide/why
- https://webpack.js.org/guides/tree-shaking/
- https://vite.dev/guide/build
status: ready
summary: "Промпты на свободное припоминание по всему юниту. Сначала ответь своими словами, затем открой модельный ответ и сравни."
title: 'Пайплайны сборки: тест на свободное припоминание'
track: frontend
unit: 07-build-pipelines
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";

<Hook>
Припоминание бьёт перечитывание. Для каждого промпта скажи или запиши полный ответ по памяти, прежде чем открыть модельный — именно усилие припоминания закрепляет материал.
</Hook>

<Goal lang="ru">
Восстанови ключевые механизмы юнита — пять стадий, почему проваливается tree-shaking, dev/prod-раскол, content hashing, source map и build cache — не подглядывая в обзор.
</Goal>

<RetrievalDrawer
  client:visible
  id="07-build-pipelines-short-retrieval"
  lang="ru"
  questions={[
    { q: "Назови пять стадий пайплайна сборки по порядку и скажи, какая параллельна, а какая последовательна.", a: "Resolve, transform, bundle, minify, emit. Resolve превращает каждый импорт в реальный файл на диске (обход node_modules, учёт exports map и алиасов tsconfig). Transform переписывает каждый файл независимо — TS в JS, JSX в createElement, понижение синтаксиса. Bundle сшивает граф модулей в чанки, решает code-splitting и делает tree-shaking. Minify переименовывает локальные и срезает мёртвые ветки. Emit пишет content-hashed файлы плюс source map в dist. Ключевая модель: transform пофайловый и неприлично параллельный, поэтому компилируемые инструменты вроде esbuild и SWC масштабируются по ядрам; bundle — обход всего графа зависимостей, в основном последовательный, поэтому параллелится куда хуже, и ускорения стадии transform на него не переносятся." },
    { q: "Tree-shaking работает только на статических ES-модулях. Какие три вещи его ломают и как чинить каждую?", a: "Первое, CommonJS: module.exports собирается в рантайме, поэтому bundler не может доказать, какие ключи не используются, и держит весь модуль — импорт одного хелпера из CJS-сборки lodash может стоить ~24 KB против ~46 байт из lodash-es. Фикс: ESM-сборка или глубокие импорты вроде lodash/debounce. Второе, side effects: если импорт модуля делает наблюдаемую работу (инжектит CSS, регистрирует полифилл, мутирует глобал), bundler обязан его держать, пока package.json честно не объявит sideEffects: false. Фикс: выставить поле, перечислив реально нечистые файлы вроде '*.css'. Третье, понижение ESM до CommonJS: transform вроде @babel/preset-env может переписать import/export в require/module.exports до того, как bundler их увидит, незаметно делая шейкаемый граф непрозрачным. Фикс: держать модули как ESM сквозь стадию transform и целиться в современный baseline." },
    { q: "Почему случаются баги 'работает в dev, ломается в prod' с инструментами вроде Vite, и как senior их предотвращает?", a: "Потому что dev и prod — не один код-путь. В dev Vite отдаёт исходник как нативные ES-модули прямо в браузер — без сборки, мгновенный старт, миллисекундный HMR — а в prod собирает через Rollup, потому что тысячи несобранных запросов модулей были бы слишком медленны по сети. Два разных инструмента делают резолвинг, code-splitting и обработку side effects, поэтому вывод может законно различаться: циклический импорт, который браузер терпит, а bundler переупорядочивает, или модуль, отшейканный в prod, но полностью присутствующий в dev. Предотвращение — перестать доверять dev-серверу как прокси для prod: гонять реальную production-сборку локально через vite build и vite preview и гейтить мёрджи в CI smoke-тестом по собранному выводу, а не по dev-серверу." },
    { q: "Объясни content hashing и ошибку с vendor-чанком, которую он вскрывает.", a: "Стадия emit называет файлы по хэшу их содержимого — index-a1b2c3.js — поэтому имя меняется только когда меняются байты. Это позволяет отдавать ассеты с Cache-Control: max-age=31536000, immutable (год), и браузер никогда не перекачивает неизменный чанк; поменяй одну строку — меняется хэш только этого чанка, остальное остаётся в кэше. Классическая ошибка — позволить vendor-коду (React, крупные зависимости) делить чанк с кодом приложения: тогда каждый деплой приложения меняет байты совмещённого чанка, ломает хэш vendor и заставляет вернувшихся пользователей перекачивать мегабайты, которые на деле не менялись. Фикс: вынести vendor в свой чанк, чтобы его хэш оставался стабильным при частых деплоях." },
    { q: "Что делают source map, какую настройку использовать в production и почему?", a: "Source map сопоставляет минифицированный, собранный код в dist с исходником, так что стек-трейс указывает на Button.tsx:42 вместо index-a1b2c3.js:1:88431. Это только метаданные для отладки — они никогда не меняют, как код исполняется. В production правильная настройка — hidden source map: карта генерируется и загружается в трекер ошибок, но комментарий sourceMappingURL не эмитится в публичный бандл, так что в трекере читаемые трейсы без раскрытия исходника всякому с devtools. Полное отключение source map оставляет каждую prod-ошибку нечитаемой; публичная эмиссия раскрывает исходник." },
    { q: "Как build cache ускоряет CI и в чём разница между кэшем по content-hash и персистентным build cache?", a: "Build cache избегает повторной работы, чьи входы не менялись. Персистентный build cache (incremental в esbuild, кэш dep pre-bundle в Vite, filesystem cache в webpack, кэши Turbopack/Nx) ключует трансформированные или собранные выходы по хэшу их входов — исходник плюс конфиг плюс версия инструмента — поэтому неизменный модуль переиспользуется, а не трансформируется заново, что превращает холодную многоминутную сборку CI в тёплую за секунды. Это отлично от content hashing на стадии emit, который хэширует финальные выходные байты, чтобы дать стабильные, cache-bustable имена файлов для браузера. Один — внутренний build-time кэш, ускоряющий производство артефакта; другой — схема именования вывода, управляющая HTTP-кэшем браузера. Чтобы любому доверять, включи версию инструмента в ключ кэша, чтобы апгрейд инструмента инвалидировал устаревшие записи." },
  ]}
/>

<Recap lang="ru">
Если ты смог восстановить каждый ответ по памяти, ты держишь стержень юнита: сборка — это пять стадий (resolve, transform, bundle, minify, emit) с параллельным transform и последовательным bundle; tree-shaking требует статического ESM и ломается CommonJS, необъявленными side effects и понижением ESM до CJS; dev и prod гоняют разные инструменты, поэтому валидируй реальную prod-сборку; content hashing плюс отдельный vendor-чанк дают кэш на год; hidden source map делают prod-ошибки читаемыми без утечки исходника; а персистентный build cache по ключу входов — то, что ускоряет CI, в отличие от content hashing вывода.
</Recap>
