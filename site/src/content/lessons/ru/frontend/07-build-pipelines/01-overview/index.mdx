---
concepts:
- bundling
- tree-shaking
- side-effects
- dev-prod-parity
- content-hashing
- source-maps
deepensInto: []
estMin: 16
lang: ru
lessonType: topic
level: junior
order: 1
prereqs: []
slug: 01-overview
sources:
- https://vite.dev/guide/why
- https://esbuild.github.io/faq/#why-is-esbuild-fast
- https://webpack.js.org/guides/tree-shaking/
- https://vite.dev/guide/build
spiral: []
status: ready
summary: Пайплайн сборки резолвит, трансформирует, бандлит, минифицирует и эмитит
  — и два места, где он бьёт, это промахи tree-shaking, отгружающие целую библиотеку,
  и баги паритета dev/prod из-за двух разных инструментов.
title: 'Пайплайны сборки: как исходник становится байтами, которые кэширует браузер'
track: frontend
unit: 07-build-pipelines
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
Срабатывает алерт перфа: главный бандл за ночь вырос со 180 KB до 410 KB. Никто не добавлял фичу. `git blame` находит одну строку — `import _ from "lodash"` вместо `import debounce from "lodash/debounce"`. Целая библиотека уехала в бандл, потому что lodash — это CommonJS, и бандлер не смог сделать tree-shaking. Один импорт, 200 KB мёртвого кода, и три недели пользователи на медленных соединениях платили за это, прежде чем алерт перешёл порог.
</Hook>

<Crux>Пайплайн сборки — это resolve → transform → bundle → minify → emit; бьёт он на tree-shaking и на паритете dev/prod.</Crux>

<Explanation>

## Пайплайн — это пять стадий, а не один чёрный ящик

`bun run build` выглядит как одна команда, но это конвейер с отдельными станциями, и баги живут на стыках между ними. Через десять минут ты будешь точно знать, на каком стыке потерялись те 200 KB — и как это закрыть.

1. **Resolve** — превратить каждый `import "x"` в реальный файл на диске. Обойти `node_modules`, учесть карты `exports`, выбрать ESM или CommonJS, пройти по алиасам путей из `tsconfig`. Отсюда берутся «module not found» и «два раза разные версии одного пакета».
2. **Transform** — переписывание пофайлово: TypeScript → JS, JSX → `createElement`, современный синтаксис down-level под твои браузерные таргеты. Это горячий цикл — он запускается раз на файл, тысячи раз.
3. **Bundle** — сшить граф модулей в несколько файлов, решить границы code-splitting (разбиения на части), поднять и дедуплицировать общие модули. Здесь происходит tree-shaking.
4. **Minify** — переименовать локали, выкинуть пробелы и мёртвые ветки, схлопнуть AST. Terser — аккуратный, но медленный стандарт; минификатор esbuild сильно быстрее и почти такой же компактный.
5. **Emit** — записать финальные файлы в `dist/`, с именами по контент-хэшу и source maps (картами исходников).

Вместе эти пять стадий образуют цепочку зависимостей: ошибка или неверное допущение на стадии N тихо портит всё, что идёт после. Не понимаешь модель — будешь гоняться за симптомами не там, виня minify, когда баг на самом деле в transform.

Ментальная модель сеньора: **transform пофайловый и до неприличия параллельный; bundle — на весь граф и последовательный.** Этот раздел объясняет, почему один инструмент бывает быстр на одной стадии и медленен на другой, и почему Vite использует *два* инструмента (об этом ниже).

<FlowDiagram
  label="Пайплайн сборки как пять упорядоченных стадий от исходника к отгруженному артефакту: резолв каждого импорта в файл, transform каждого файла (TypeScript и JSX down-level), bundle графа модулей и tree-shake, minify, затем emit файлов с контент-хэшем и source maps."
  perRow={5}
  hue="var(--d-frontend)"
  nodes={[
    { id: "resolve", label: "Resolve", sub: "import → файл" },
    { id: "transform", label: "Transform", sub: "TS/JSX, пофайл." },
    { id: "bundle", label: "Bundle", sub: "граф, tree-shake" },
    { id: "minify", label: "Minify", sub: "переим., срез" },
    { id: "emit", label: "Emit", sub: "хэш + maps" },
  ]}
  edges={[
    { from: "resolve", to: "transform" },
    { from: "transform", to: "bundle" },
    { from: "bundle", to: "minify" },
    { from: "minify", to: "emit" },
  ]}
  caption="Transform пофайловый и параллельный (где выигрывают esbuild/SWC); bundle — на весь граф и последовательный (где живёт tree-shaking). Баги живут на стыках: tree-shaking валится на bundle, если до него дошёл CommonJS или down-level, а баги паритета dev/prod берутся из разных инструментов на этих стадиях."
/>

## Почему инструменты на Go и Rust в 10–100× быстрее

Старый дефолт — Babel для transform, Terser для minify — написан на JavaScript и однопоточен по природе. esbuild (Go) и SWC (Rust) скомпилированы, многопоточны и не перепарсивают AST между проходами. Выигрыш не маленький. Собственные бенчмарки esbuild показывают сборку большого React-приложения **за низкие сотни миллисекунд там, где Babel/Webpack тратят десятки секунд** — обычно говорят про 10–100× в зависимости от нагрузки. Реальные миграции сообщают о сокращении стадии сборки на **93–96%** (шаг transform в 30s падает до ~1–2s).

Почему так быстрее? Три причины, которые накапливаются: компилируемый язык с реальными потоками вместо event loop, единый общий AST, прогоняемый через parse/transform/minify, вместо перепарсинга на каждой границе инструментов, и агрессивное переиспользование памяти. Трейдофф, который взвешивает сеньор: у Babel всё ещё богатейшая экосистема плагинов (кастомные макросы, экзотические трансформы, легаси-таргеты), поэтому команды, которым нужен конкретный плагин Babel, иногда держат его для одного файла и гоняют esbuild/SWC для остального.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Инструмент</th>
        <th class="border border-border px-3 py-2 text-left">Язык</th>
        <th class="border border-border px-3 py-2 text-left">Стадия</th>
        <th class="border border-border px-3 py-2 text-left">Скорость vs JS-базлайн</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">Babel</td>
        <td class="border border-border px-3 py-2">JS</td>
        <td class="border border-border px-3 py-2">transform</td>
        <td class="border border-border px-3 py-2">1× (базлайн)</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">SWC</td>
        <td class="border border-border px-3 py-2">Rust</td>
        <td class="border border-border px-3 py-2">transform</td>
        <td class="border border-border px-3 py-2">~20× быстрее</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">esbuild</td>
        <td class="border border-border px-3 py-2">Go</td>
        <td class="border border-border px-3 py-2">transform + bundle + minify</td>
        <td class="border border-border px-3 py-2">~10–100× быстрее</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Terser</td>
        <td class="border border-border px-3 py-2">JS</td>
        <td class="border border-border px-3 py-2">minify</td>
        <td class="border border-border px-3 py-2">1× (аккуратный, медленный)</td>
      </tr>
    </tbody>
  </table>
</div>

## Tree-shaking и три вещи, которые его ломают

Tree-shaking выкидывает экспорты, которые никто не импортирует. Он работает только на **статических ES-модулях**, потому что бандлер должен на этапе сборки доказать, что биндинг не используется. Три вещи ломают это доказательство, и каждая — реальный продовый инцидент:

- **CommonJS.** `module.exports` вычисляется в рантайме — бандлер не может статически узнать, какие ключи ты используешь. `import _ from "lodash"` (CJS) тащит всю библиотеку: импорт одного только `isArray` так стоит около **24 KB** против **~46 байт** из `lodash-es` (ESM). Фикс — использовать ESM-сборку (`lodash-es`) или глубокие импорты (`lodash/debounce`).
- **Сайд-эффекты.** Если модуль делает работу при импорте (регистрирует полифил, мутирует глобал, инжектит CSS), бандлер обязан его сохранить, даже если экспорты выглядят неиспользуемыми, — выкинуть его значило бы изменить поведение. Именно это означает поле `"sideEffects": false` в `package.json`: обещание бандлеру, что *импорт этого файла ничего наблюдаемого не меняет*, поэтому неиспользуемые части можно безопасно выкинуть. Соври в этом поле — отгрузишь сломанную сборку; не укажи его на чистой библиотеке — отгрузишь мёртвый код.
- **Down-level в CommonJS.** `@babel/preset-env` из Babel может превратить твои хорошие ESM `import`/`export` в `require`/`module.exports` *до того*, как их увидит бандлер, — тихо превращая shakeable-граф в непрозрачный. Тогда бандлер сохраняет всё. Это самое коварное: исходник выглядит shakeable, а стадия transform тихо это ломает.

Все три провала имеют одну форму: бандлер не может *доказать*, что экспорт не используется, и из осторожности держит всё. Когда видишь, что бандл раздулся после изменения зависимости, — спроси себя, какое из трёх доказательств сломалось. Ответ сразу покажет на фикс.

<Inset kind="why" lang="ru">
Флаг `"sideEffects"` — это контракт, а не подсказка. `false` означает «каждый файл в этом пакете чист при импорте». Если хотя бы один файл инжектит CSS как сайд-эффект, его надо перечислить: `"sideEffects": ["*.css", "./src/polyfill.js"]`. Ошибёшься в безопасную сторону (заявишь сайд-эффекты, которых нет) — потеряешь лишь часть оптимизации. Ошибёшься в опасную (заявишь чистоту, которой нет) — бандлер вырежет код, от которого зависит приложение: сборка проходит CI и падает в рантайме.
</Inset>

## Dev и prod гоняют разные инструменты — и тут прячутся баги паритета

Современные dev-серверы (Vite) не бандлят в разработке. Они отдают твой исходник как **нативные ES-модули** прямо в браузер и дают браузеру запрашивать модули по требованию — поэтому dev-сервер стартует мгновенно независимо от размера приложения, а апдейты HMR прилетают за **единицы или низкие десятки миллисекунд**: меняется один модуль, а не пересобранный бандл. Прод другой: Vite бандлит через **Rollup** (и всё чаще Rolldown), потому что отдавать тысячи небандленых запросов модулей по сети было бы катастрофически медленно.

Два разных пути кода — esbuild/нативный-ESM в dev, Rollup-бандл в prod — означают, что **dev и prod могут расходиться.** Разный резолв модулей, разный code-splitting, разная обработка сайд-эффектов. Классический симптом — «работает у меня, сломано в проде»: циклический импорт, который нативный ESM терпит, а бандлер переупорядочивает, или зависимость, которую tree-shaking выкинул в прод-сборке, но которая полностью присутствует в dev. Привычка сеньора: гонять реальную прод-сборку локально (`vite build && vite preview`) до того, как ей доверять, и ставить смоук-тест против *собранного* вывода в CI, а не только против dev-сервера.

<TradeoffMatrix
  id="07-build-pipelines-tradeoff-1"
  lessonSlug="01-overview"
  lang="ru"
  prompt="Сборка вашей команды на Webpack + Babel занимает 45s, а HMR 3–4s. Хочешь быстрее итерироваться, не рискуя продом. Выбери ход."
  options={[
    { name: "Перейти на сетап в стиле Vite: нативный ESM + esbuild в dev, Rollup-бандл для prod, и гейтить мёрджи на реальной прод-сборке в CI", summary: "Двухинструментный пайплайн, с гейтом на паритет.", correct: true, justification: "Это мейнстримный ответ, и он верный, потому что бьёт по обеим болям: нативный ESM в dev даёт мгновенный старт и HMR за миллисекунды, а Rollup-сборка для prod держит сетево-оптимизированный вывод, который ты реально отгружаешь. Известный риск — dev и prod как разные пути кода — сдерживается прогоном vite build && vite preview в CI, поэтому баг паритета валит мёрдж, а не доходит до пользователей." },
    { name: "Заменить Babel на SWC/esbuild как лоадер Webpack, но оставить бандлинг в dev", summary: "Быстрее transform, та же архитектура.", justification: "Реальный и менее рискованный выигрыш: замена JS-трансформера на Rust/Go-шный может резко срезать время transform (часто на 90%+), сохранив один бандлер для dev и prod, так что риска паритета нет. Это консервативный выбор, когда миграцию не потянуть, — но dev всё ещё бандлит, поэтому старт и HMR улучшаются меньше, чем у dev-сервера на нативном ESM." },
    { name: "Оставить всё как есть, просто накинуть больше CI-раннеров", summary: "Откупиться.", justification: "Параллельные раннеры помогают общему wall-clock CI, но ничего не делают для локального цикла HMR в 3–4s, который и убивает поток разработки. Ты платишь деньги, чтобы избежать разовой смены тулинга, а медленный внутренний цикл — то, что инженеры чувствуют весь день, — не тронут." },
    { name: "Использовать esbuild и как прод-бандлер, чтобы был один инструмент везде", summary: "Один инструмент, максимум паритета.", justification: "Соблазнительно для паритета, но бандлер esbuild менее обкатан для продового code-splitting и тонкого контроля вывода, чем Rollup, — именно поэтому Vite сочетает esbuild (dev/зависимости) с Rollup (prod). Один инструмент убирает риск паритета, но меняет на это прод-уровневый сплиттинг и зрелость плагинов, на которые команды опираются." },
  ]}
/>

<Quiz
  id="07-build-pipelines-quiz-1"
  lessonSlug="01-overview"
  lang="ru"
  question="Бандл раздувается после того, как кто-то пишет import _ from 'lodash'. Почему tree-shaking не убрал неиспользуемые функции?"
  choices={[
    { label: "Главная сборка lodash — это CommonJS, который нельзя статически проанализировать — используй lodash-es или глубокие импорты вроде lodash/debounce", correct: true },
    { label: "Tree-shaking по умолчанию выключен и его надо включать на каждый импорт", misconception: "Tree-shaking включён по умолчанию в продовых сборках; проблема в том, что экспорты CommonJS вычисляются в рантайме, поэтому бандлер не может доказать, какие из них не используются." },
    { label: "Минификация прошла до бандлинга, поэтому мёртвый код уже зафиксировался", misconception: "Порядок стадий не причина. Бандлер держит весь модуль, потому что экспорты CJS не анализируются статически, независимо от тайминга минификации." },
    { label: "Импорт должен был быть default-импортом, чтобы запустить shaking", misconception: "Стиль импорта — не корневая причина, а формат модуля. ES-модули shake-ятся; CommonJS — нет." },
  ]}
/>

<Quiz
  id="07-build-pipelines-quiz-2"
  lessonSlug="01-overview"
  lang="ru"
  question="Фича работает в dev-сервере Vite, но ломается после vite build. Какая самая вероятная структурная причина?"
  choices={[
    { label: "Dev отдаёт нативный ESM без бандлинга, а prod бандлит через Rollup — разные резолв, сплиттинг и обработка сайд-эффектов могут разойтись", correct: true },
    { label: "Dev-сервер агрессивно кэширует и отдал устаревший код", misconception: "Устаревший кэш даёт обратное (сохраняется старое поведение); настоящий раскол dev/prod — это два разных пути сборки, дающих разный вывод." },
    { label: "Это продовая минификация изменила поведение", misconception: "Минификация изредка может вскрыть баг, но структурная причина расхождения dev и prod — в том, что они гоняют разные инструменты на разных графах модулей." },
    { label: "В проде отключили source maps", misconception: "Source maps влияют лишь на читаемость ошибок при отладке, а не на то, ведёт ли себя сам код иначе." },
  ]}
/>

## Source maps и контент-хэши: стадия emit отрабатывает своё

Два вывода финальной стадии решают, отлаживаем ли prod и ждут ли вернувшиеся пользователи. **Source maps** мапят минифицированный, забандленный код `dist` обратно на исходник, чтобы стек-трейс указывал на `Button.tsx:42`, а не на `index-a1b2c3.js:1:88431`. Отгружай их в свой трекер ошибок (приватно), даже когда не выставляешь публично, иначе каждая продовая ошибка нечитаема.

**Контент-хэширование** — это контракт кэширования. Стадия emit называет файлы по хэшу их содержимого — `index-a1b2c3.js` — поэтому имя файла меняется только когда меняются байты. Это позволяет отдавать статику с `Cache-Control: max-age=31536000, immutable` (год), и браузер никогда не перекачивает неизменившийся чанк. Поменяй одну строку — изменится хэш только этого чанка; всё остальное остаётся в кэше. Классическая ошибка — вендорный код (React, твои крупные зависимости) делит чанк с кодом приложения; тогда каждый деплой приложения бьёт и вендорный кэш. Вынеси вендор в отдельный чанк, чтобы он держал свой хэш через твои частые деплои приложения.

<DragOrder
  id="07-build-pipelines-drag-1"
  lessonSlug="01-overview"
  lang="ru"
  prompt="Расставь стадии пайплайна сборки от исходника к отгруженному артефакту:"
  items={[
    { id: "a", label: "Resolve — превратить каждый импорт в реальный файл (node_modules, карты exports, алиасы)" },
    { id: "b", label: "Transform — пофайлово: TS→JS, JSX→createElement, down-level синтаксиса (esbuild/SWC/Babel)" },
    { id: "c", label: "Bundle — сшить граф модулей, code-split, tree-shake неиспользуемых экспортов" },
    { id: "d", label: "Minify — переименовать, выкинуть пробелы, срезать мёртвые ветки (Terser/esbuild)" },
    { id: "e", label: "Emit — записать файлы с контент-хэшем + source maps в dist/" },
  ]}
  correctOrder={["a", "b", "c", "d", "e"]}
/>

</Explanation>

<KeyTakeaway>Сборка = resolve → transform → bundle → minify → emit. Tree-shaking нужен статический ESM; CommonJS и сайд-эффекты его ломают. Dev/prod гоняют разные инструменты — тестируй прод-сборку. Контент-хэш для кэша на год.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="07-build-pipelines-retrieval"
  lang="ru"
  questions={[
    { q: "Коллега импортирует утилиту из CommonJS-пакета, и бандл растёт на 200 KB. Объясни, почему tree-shaking провалился и три класса вещей, которые его ломают.", a: "Tree-shaking работает только на статических ES-модулях, потому что бандлер должен на этапе сборки доказать, какие экспорты не используются, — а это доказательство требует статически анализируемого синтаксиса import/export. CommonJS ломает первый класс: module.exports собирается в рантайме, поэтому бандлер не знает, какие ключи ты трогаешь, и обязан держать всё — импорт одного хелпера из CJS-сборки lodash может стоить ~24 KB там, где lodash-es стоит ~46 байт. Второй класс — сайд-эффекты: если импорт модуля делает наблюдаемую работу (инжектит CSS, регистрирует полифил, мутирует глобал), бандлер обязан его сохранить, даже когда экспорты выглядят неиспользуемыми, если только package.json не объявляет 'sideEffects': false, обещая чистоту. Третий класс — down-level ESM в CommonJS: трансформ вроде @babel/preset-env может переписать твои import/export в require/module.exports до того, как их увидит бандлер, тихо делая граф непрозрачным. Фиксы: предпочитай ESM-сборки или глубокие импорты, выставляй поле sideEffects честно и держи модули как ESM сквозь стадию transform." },
    { q: "Почему баги «работает в dev, ломается в prod» случаются с инструментами вроде Vite, и как сеньор их предотвращает?", a: "Потому что dev и prod — не один путь кода. В dev Vite отдаёт твой исходник как нативные ES-модули прямо в браузер — без бандлинга, мгновенный старт, HMR за миллисекунды — а в prod бандлит через Rollup, потому что тысячи небандленых запросов модулей были бы слишком медленны по сети. Два разных инструмента, делающих резолв модулей, code-splitting и обработку сайд-эффектов, означают, что вывод может законно отличаться: циклический импорт, который браузер терпит, а бандлер переупорядочивает, или модуль, выкинутый tree-shaking-ом в прод-сборке, но полностью присутствующий в dev. Предотвращение — перестать доверять dev-серверу как прокси для prod: гонять реальную прод-сборку локально через vite build && vite preview и гейтить мёрджи в CI на этом собранном выводе (смоук-тест против бандла, а не dev-сервера). Тогда баг паритета валит пайплайн, а не доходит до пользователей." },
  ]}
/>

<Recap lang="ru">
Пайплайн сборки — это пять стадий: resolve, transform, bundle, minify, emit, — и стыки между ними и есть место, где живут баги. Transform пофайловый и параллельный, поэтому компилируемые инструменты вроде esbuild (Go) и SWC (Rust) обгоняют JavaScript-овые Babel и Terser в 10–100× и срезают реальные шаги сборки на 90%+; bundle — на весь граф и последовательный, и именно там происходит tree-shaking. Tree-shaking-у нужны статические ES-модули, чтобы доказать, что экспорт не используется, поэтому его ломают CommonJS (рантайм-экспорты), сайд-эффекты (если package.json их честно не объявляет) и трансформы, которые down-level-ят ESM в CommonJS до взгляда бандлера, — любой из них может отгрузить целую библиотеку, которой ты не пользуешься. Dev и prod часто гоняют разные инструменты (нативный ESM + esbuild в dev, Rollup в prod), поэтому всегда валидируй реальную прод-сборку, чтобы ловить баги паритета рано. Наконец, стадия emit отрабатывает своё через source maps для отлаживаемых продовых ошибок и имена файлов по контент-хэшу, которые позволяют кэшировать статику на год, сбрасывая только те чанки, что реально изменились. Теперь, когда видишь раздувшийся бандл после смены зависимости, — потянешься не к grep-у по коду приложения, а к формату модуля, полю sideEffects и конфигу transform.
</Recap>
