---
concepts:
- side-effects
- dev-prod-mode
- source-maps
- manual-chunks
- env-inlining
estMin: 14
lang: ru
level: senior
order: 9
prereqs: []
slug: quiz-code
sources:
- https://webpack.js.org/guides/tree-shaking/
- https://vite.dev/guide/build
- https://vite.dev/guide/env-and-mode
status: ready
summary: "Читай реальные конфиги сборки и поле package.json, предсказывай поведение bundler-а и выбирай фикс наибольшего рычага для tree-shaking, mode, source map и инлайна env."
title: 'Пайплайны сборки: чтение конфигов и сниппетов'
track: frontend
unit: 07-build-pipelines
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Проблемы сборки диагностируются в конфиге и манифесте пакета, а не в работающем приложении. Прочитай каждый сниппет, предскажи, что bundler с ним сделает, и выбери фикс, который senior сделает первым.
</Hook>

<Goal lang="ru">
Отработай цикл, который ты гоняешь на каждой регрессии сборки: прочитай конфиг, предскажи, где утекают байты или где расходятся dev и prod, и потянись за точным полем — sideEffects, mode, sourcemap, manualChunks, env — которое чинит корень.
</Goal>

## Сниппет 1 — объявление sideEffects

```json
{
  "name": "@acme/ui",
  "module": "dist/index.js",
  "sideEffects": false,
  "exports": { ".": "./dist/index.js" }
}
```

Пакет поставляет `dist/styles.css`, который компоненты импортируют ради стилей, например `import "./button.css"` в начале `Button.js`.

<Quiz
  id="07-build-pipelines-code-1"
  lessonSlug="07-build-pipelines"
  lang="ru"
  question="Потребитель сообщает, что часть стилей компонентов пропадает в его production-сборке. Что вызывает это значение sideEffects и каков фикс?"
  choices={[
    { label: "sideEffects: false обещает, что каждый файл чист при импорте, поэтому bundler выкидывает модуль с импортом CSS, когда его JS-экспорты выглядят неиспользуемыми — и стили исчезают. Фикс: объявить нечистые файлы, например sideEffects: ['*.css'].", correct: true },
    { label: "sideEffects: false влияет только на JS, поэтому CSS всегда сохраняется.", misconception: "Флаг применяется к каждому модулю, включая те, что импортируют CSS ради side effect. Заявленный здесь false позволяет bundler-у удалить модуль с импортом CSS." },
    { label: "Фикс — выставить sideEffects: true, чтобы ничего не выкидывалось.", misconception: "true отключает tree-shaking для всего пакета и тащит мёртвый JS. Точный фикс — перечислить только реально нечистые файлы, вроде '*.css', оставив остальное шейкаемым." },
    { label: "Импорты CSS никогда не шейкаются, так что это не про sideEffects.", misconception: "Модуль, единственная задача которого — импортировать CSS, — это ровно то, что слишком широкий sideEffects: false позволяет bundler-у удалить; поле и есть корень." },
  ]}
/>

## Сниппет 2 — режим dev vs prod и NODE_ENV

```js
// webpack.config.js
module.exports = {
  mode: "development",
  // ...loaders, plugins...
};
```

Команда гоняет ровно этот конфиг в CI, чтобы получить артефакт, который деплоит в production.

<Quiz
  id="07-build-pipelines-code-2"
  lessonSlug="07-build-pipelines"
  lang="ru"
  question="Задеплоенный бандл ~в 3 раза больше ожидаемого, а React пишет development-предупреждения в проде. Что не так и каков фикс наибольшего рычага?"
  choices={[
    { label: "mode: 'development' отключает минификацию и ставит process.env.NODE_ENV в development, поэтому dead-code-elimination dev-only веток (предупреждений React) не запускается. Собирай prod с mode: 'production' (или передавай его на команду).", correct: true },
    { label: "Бандл большой, потому что tree-shaking выключен во всех режимах.", misconception: "Здесь тумблер не tree-shaking, а mode. Режим development пропускает минификацию и оставляет NODE_ENV=development, поэтому dev-only ветки выживают и бандл остаётся большим." },
    { label: "React всегда пишет предупреждения; размер не связан с mode.", misconception: "React вырезает свои dev-предупреждения, когда NODE_ENV равен production. Они появляются именно потому, что режим development оставил NODE_ENV в development." },
    { label: "Выстави devtool в false, чтобы сжать бандл.", misconception: "devtool управляет source map, не dev/prod код-путями. Фикс — поле mode, которое задаёт минификацию и NODE_ENV." },
  ]}
/>

## Сниппет 3 — source map в production

```js
// vite.config.js
export default {
  build: {
    sourcemap: false,
  },
};
```

Прод кидает ошибку, и стек-трейс читается как `index-a1b2c3.js:1:88431`. Трекер ошибок показывает тот же нечитаемый фрейм.

<Quiz
  id="07-build-pipelines-code-3"
  lessonSlug="07-build-pipelines"
  lang="ru"
  question="Нужны читаемые prod-стек-трейсы в трекере ошибок без раскрытия исходника публично. Какая настройка правильная?"
  choices={[
    { label: "Выставить sourcemap: 'hidden', чтобы карты генерировались и загружались в трекер ошибок, но комментарий //# sourceMappingURL не эмитился в публичный бандл.", correct: true },
    { label: "Оставить sourcemap: false и реконструировать трейсы из минифицированного вывода.", misconception: "Без source map минифицированный однострочный фрейм нельзя сопоставить с Button.tsx:42; каждая prod-ошибка остаётся нечитаемой. Генерируй карту, просто не публикуй её." },
    { label: "Выставить sourcemap: true, чтобы браузер всегда качал карту.", misconception: "true эмитит sourceMappingURL в публичный бандл, раскрывая исходник всякому с devtools. 'hidden' даёт трекеру карту, не публикуя её." },
    { label: "Source map меняет поведение в рантайме, поэтому их включение рискует багом.", misconception: "Source map — это только метаданные для отладки; они никогда не меняют, как код исполняется. Они влияют лишь на читаемость трейса." },
  ]}
/>

## Сниппет 4 — manualChunks и инлайн env

```js
// vite.config.js
export default {
  build: {
    rollupOptions: {
      output: {
        manualChunks: { vendor: ["react", "react-dom"] },
      },
    },
  },
};
```

Ревьюер также замечает, что клиентский компонент читает `process.env.API_SECRET`, чтобы вызвать внутренний API.

<Quiz
  id="07-build-pipelines-code-4"
  lessonSlug="07-build-pipelines"
  lang="ru"
  question="Какое утверждение об этом конфиге верно?"
  choices={[
    { label: "Разбиение manualChunks разумно — vendor получает свой чанк со стабильным хэшем — но чтение API_SECRET в клиентском компоненте инлайнит секрет в поставляемый бандл во время сборки; секреты должны оставаться на сервере, никогда в клиентском env.", correct: true },
    { label: "И разбиение чанков, и чтение env в порядке; Vite скрывает process.env от клиента.", misconception: "Vite статически инлайнит упомянутые значения env в клиентский бандл во время сборки. Секрет, прочитанный в клиентском коде, запекается в поставляемый JS — реальная утечка." },
    { label: "manualChunks здесь неверен, потому что vendor должен делить чанк с кодом приложения.", misconception: "Деление vendor с кодом приложения ломает кэш vendor при каждом деплое. Изоляция react/react-dom в стабильный vendor-чанк — правильный ход." },
    { label: "Секрет безопасен, потому что лежит в переменной окружения, а не в исходнике.", misconception: "Инлайн во время сборки копирует значение env в выходной бандл независимо от его происхождения. Только серверный код может читать секреты." },
  ]}
/>

<Recap lang="ru">
Каждая регрессия сборки читается в конфиге: sideEffects: false должен перечислять реально нечистые файлы (вроде CSS), иначе bundler их выкинет; production надо собирать в режиме production, чтобы запустились минификация и dead-code-elimination веток под NODE_ENV; source map место в режиме 'hidden', чтобы трекер ошибок получил читаемость без публичного раскрытия; vendor заслуживает свой manualChunks-чанк ради стабильности кэша; а любое значение env, упомянутое в клиентском коде, инлайнится во время сборки, поэтому секреты не должны касаться клиента. Прочитай поле, предскажи вывод, почини корень.
</Recap>
