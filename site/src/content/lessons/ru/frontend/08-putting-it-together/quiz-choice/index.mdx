---
concepts:
- state-shape
- fetch-waterfall
- design-tokens
- monorepo-boundaries
- code-splitting
- failure-cascade
estMin: 14
lang: ru
level: senior
order: 7
prereqs: []
slug: quiz-choice
sources:
- https://kentcdodds.com/blog/state-colocation-will-make-your-react-app-faster
- https://web.dev/articles/lcp
- https://atlassian.design/foundations/tokens/design-tokens
status: ready
summary: "Кросс-трековый синтез с выбором ответа: читаем симптом фронтенда, находим слой, которому он реально принадлежит, и выбираем фикс, который не просто маскирует баг."
title: 'Собираем вместе: тест с выбором ответа'
track: frontend
unit: 08-putting-it-together
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Каждый вопрос здесь пересекает два-три юнита. Капстоун проверяет не запоминание одного слоя, а навык: прочитать симптом и назвать самый нижний слой, которому он принадлежит, — именно там живёт устойчивый фикс.
</Hook>

<Goal lang="ru">
Убедиться, что вы умеете проследить симптом фронтенда вниз по каскаду — state shape, граф fetch, токены, границы монорепо, splitting, pipeline — и отвергать фиксы, которые оптимизируют слой выше реальной причины.
</Goal>

<Quiz
  id="08-putting-it-together-choice-1"
  lessonSlug="08-putting-it-together"
  lang="ru"
  question="Дашборд тормозит на каждом нажатии клавиши в поле поиска. Коллега заводит тикет: применить code-splitting и лениво грузить графики. Как читает это senior?"
  choices={[
    { label: "Это баг state shape: нажатия клавиш — локальное UI-состояние, поднятое в глобальный store, поэтому каждый подписчик ре-рендерится на каждый символ. Splitting меняет начальный бандл, а не ре-рендер, который срабатывает после загрузки чанка, — он не чинит ничего. Колокейтить состояние ввода.", correct: true },
    { label: "Code-splitting графиков починит это, уменьшив то, что ре-рендерится.", misconception: "Splitting решает, что попадёт в начальный бандл и когда придут чанки; он не меняет, какие подписчики ре-рендерятся. График всё равно ре-рендерится после загрузки своего чанка, потому что причина — где живёт состояние ввода, а не размер бандла." },
    { label: "Обернуть график в React.memo и двигаться дальше.", misconception: "Мемоизация одного компонента может приглушить один симптом, но глобальный store всё равно уведомляет каждого подписчика на каждое нажатие. Вы патчите узел ниже по течению вместо того, чтобы починить state shape, задавший радиус поражения." },
    { label: "Обновить бандлер ради более быстрых сборок.", misconception: "Скорость сборки не связана с ре-рендерами в рантайме. Это оптимизирует самый дешёвый слой (pipeline), пока причина сидит в самом дорогом (state shape)." },
  ]}
/>

<Quiz
  id="08-putting-it-together-choice-2"
  lessonSlug="08-putting-it-together"
  lang="ru"
  question="Страница товара рисует скелетон, затем гидрируется JS, затем фетчит пользователя, потом корзину, потом рекомендации — каждый ждёт предыдущего. LCP — 4.8 с. Какому слою это принадлежит и в чём фикс?"
  choices={[
    { label: "Слой data fetching: client fetch после JS плюс последовательный waterfall добавляет несколько round-trip'ов в критический путь. Перенести fetch на сервер (или RSC) и распараллелить независимые через Promise.all, чтобы LCP схлопнулся к одному round-trip — цель меньше 2.5 с.", correct: true },
    { label: "Бандл слишком большой — применить code-splitting к странице, чтобы снизить LCP.", misconception: "Меньший бандл помогает Time-to-Interactive, но не убирает последовательные round-trip'ы. Даже при мгновенном JS три последовательных client fetch'а всё равно сериализуются; этим LCP владеет waterfall, а не бандл." },
    { label: "Добавить спиннер, чтобы ожидание ощущалось короче.", misconception: "Спиннер меняет воспринимаемый UX, а не LCP, который измеряет отрисовку самого крупного контента. Причина — последовательный граф fetch на критическом пути, и именно он должен измениться." },
    { label: "Это проблема state shape — нормализовать полученные данные.", misconception: "Нормализация задаёт, как данные хранятся и читаются, а не когда они приходят. Задержка здесь — это последовательные round-trip'ы до того, как данные вообще появились; последующая нормализация ничего не меняет в LCP." },
  ]}
/>

<Quiz
  id="08-putting-it-together-choice-3"
  lessonSlug="08-putting-it-together"
  lang="ru"
  question="Маркетинг просит ребрендинг: новый брендовый цвет плюс тёмную тему. В кодовой базе брендовый hex захардкожен в ~300 компонентах. Что это вскрывает и в чём структурный фикс?"
  choices={[
    { label: "Отсутствует слой токенов. Структура primitive затем semantic затем component делает ребрендинг сменой значений примитивов, а тёмную тему — сменой семантики, не трогая компоненты. Описанная миграция на тёмную тему заняла два дня с семантическим слоем и заняла бы недели find-and-replace без него.", correct: true },
    { label: "Сделать глобальный find-and-replace по hex-значению; это разовая стоимость.", misconception: "Find-and-replace справляется с одним ребрендингом и оставляет вас ровно настолько же хрупкими к следующему запросу и к тёмной теме, где один и тот же hex значит разное в разных контекстах. Смысл семантического слоя в том, что ссылаются на значение, а не на сырой цвет." },
    { label: "Это проблема build pipeline — добавить PostCSS-плагин, переписывающий цвета.", misconception: "Переписывание цветов на этапе сборки всё равно не даёт источника истины о том, что значит цвет; нельзя выразить text-danger против surface-raised. Недостающий артефакт — семантический слой токенов, а не трансформ бандлера." },
    { label: "Тематизировать через CSS-оверрайды на каждой странице.", misconception: "Постраничные оверрайды умножают несогласованность, которую токены призваны убрать, и не дают тёмной теме единого переключателя. Централизованные семантические токены — источник истины; разбросанные оверрайды — это анти-паттерн." },
  ]}
/>

<Quiz
  id="08-putting-it-together-choice-4"
  lessonSlug="08-putting-it-together"
  lang="ru"
  question="CI занимает 15 минут на однострочное изменение в одном из 12 пакетов, потому что всё импортирует один гигантский shared-пакет. Что бьёт по корневой причине?"
  choices={[
    { label: "Реальные границы пакетов плюс affected-only выполнение, чтобы изменение одного пакета собирало один пакет; затем remote cache делит работу между запусками. Mercari срезали время задач Turborepo на ~50%, а affected-only плюс кэш роняют 15-минутную сборку до 2–3 минут при изменении одного пакета.", correct: true },
    { label: "Купить более быстрые CI-раннеры.", misconception: "Более быстрое железо пересобирает весь граф зависимостей быстрее; оно никогда не перестаёт пересобирать то, что не менялось. 15 минут идут от проблемы границ — каждый лист зависит от ствола, — а не от мощности." },
    { label: "Включить tree-shaking бандлера.", misconception: "Tree-shaking урезает отправляемые байты на бандл; он не меняет, какие пакеты CI решит пересобрать. Взрыв времени сборки — это проблема графа зависимостей выше любого шага бандлинга." },
    { label: "Разбить приложение на больше route-чанков.", misconception: "Route-чанки влияют на то, что браузер скачивает в рантайме, а не на то, что пересобирает CI. Причина в том, что граф зависимостей вынуждает полную пересборку на любое изменение." },
  ]}
/>

<Quiz
  id="08-putting-it-together-choice-5"
  lessonSlug="08-putting-it-together"
  lang="ru"
  question="Вам дали новый фронтенд на ревью. В каком порядке senior оценивает слои и почему именно так?"
  choices={[
    { label: "Снизу вверх по стоимости отказа: state shape, затем граф fetch, затем токены, затем границы монорепо, затем code splitting, затем build pipeline. Нижние слои ограничивают верхние, поэтому вы находите самый нижний сломанный слой первым и чините там, где исчезают и симптом, и всё, что он породил.", correct: true },
    { label: "Сверху вниз по структуре файлов: папки, затем компоненты, затем состояние, затем конфиг сборки.", misconception: "Чтение по структуре файлов скрывает направление зависимостей. Можно закончить, любуясь чистым деревом папок, и всё равно пропустить, что решение по state shape вызывает тормоза — слои каскадируют по стоимости отказа, а не по тому, где лежат файлы." },
    { label: "Начать с build pipeline, потому что его выигрыши наиболее измеримы.", misconception: "Pipeline — самый дешёвый слой для починки позже и наименее вероятная причина. Оптимизировать байты, пока state shape сломан, — это полировать машину с заклинившим двигателем: поверхность выглядит быстрее, машина — нет." },
    { label: "Порядок не важен, пока в итоге проверен каждый слой.", misconception: "Порядок — это весь урок. Фикс, применённый выше реальной причины, — это потраченная впустую работа, которая ещё и прячет баг, поэтому ревью в порядке стоимости отказа — это то, что даёт чинить источник, а не симптом." },
  ]}
/>

<Quiz
  id="08-putting-it-together-choice-6"
  lessonSlug="08-putting-it-together"
  lang="ru"
  question="Тяжёлая библиотека графиков грузится на первой отрисовке каждого маршрута, хотя большинство пользователей никогда не открывают вкладку аналитики, а пользователь скринридера сообщает, что date-picker внутри неё ловит фокус в ловушку. Какой двухслойный фикс верен?"
  choices={[
    { label: "Два независимых слоя: применить code-splitting к маршруту аналитики, чтобы библиотека графиков грузилась лениво только при открытии вкладки (срезая начальный бандл и TTI), и отдельно починить баг forms/a11y корректным управлением фокусом и рабочим путём выхода. Splitting никогда не чинит ловушку фокуса; a11y никогда не чинит бандл.", correct: true },
    { label: "Лениво грузить графики — это заодно решит ловушку фокуса, отложив баговый компонент.", misconception: "Отсрочка загрузки лишь откладывает момент монтирования багового компонента; ловушка фокуса возвращается в тот же миг, когда пользователь открывает вкладку. Доступность и code-splitting — ортогональные слои, каждому нужен свой фикс." },
    { label: "Оставить график жадно загруженным, чтобы date-picker всегда был доступен ассистивным технологиям.", misconception: "Жадная загрузка раздувает начальный бандл для каждого пользователя без пользы и ничего не делает с ловушкой фокуса, которая остаётся багом управления фокусом независимо от момента загрузки компонента." },
    { label: "Перенести график в отдельный пакет монорепо; это решит оба.", misconception: "Граница пакета меняет сборку и владение, а не то, что уходит в браузер на первой отрисовке, и не то, как ведёт себя фокус в рантайме. Вам всё равно нужен route-level splitting для бандла и фикс фокуса для a11y." },
  ]}
/>

<Recap lang="ru">
Сквозная линия трека — это одно дерево решений. Где живёт состояние, задаёт радиус поражения ре-рендера; граф fetch владеет LCP; токены решают, будет ли ребрендинг сменой значения или охотой по файлам; границы монорепо — это множитель времени сборки; splitting и pipeline — самые дешёвые слои. Каждый вопрос выше — это один и тот же ход: прочитать симптом, спуститься к самому нижнему слою, которому он принадлежит, и отвергнуть любой фикс, оптимизирующий слой выше причины.
</Recap>
