---
concepts:
- component
- state
- props
- re-render
- data-fetching
- client-vs-server
- bundler
- component-tree
deepensInto: []
estMin: 10
lang: ru
lessonType: topic
level: zero
order: 1
prereqs: []
slug: 01-overview
sources:
- https://react.dev/learn/thinking-in-react
- https://developer.mozilla.org/en-US/docs/Learn/Tools_and_testing/Client-side_JavaScript_frameworks/Introduction
- https://vitejs.dev/guide/why.html
- https://web.dev/articles/rendering-on-the-web
spiral: []
status: ready
summary: Архитектура фронтенда — это дисциплина перехода от страницы к приложению.
  Это карта «с нуля» и восемь слов — компонент, состояние, пропсы, ре-рендер, загрузка
  данных, клиент vs сервер, бандлер, дерево компонентов, — которые каждый следующий
  юнит считает уже знакомыми.
title: 'С нуля: что такое архитектура фронтенда на самом деле'
track: frontend
unit: 00-start-here
---

import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";
import Infographic from "~/components/diagram/Infographic.astro";
import pageVsApp from "./infographics/page-vs-app.svg?raw";

<Hook>
Ты умеешь писать HTML. Ты добавляешь немного JavaScript на страницу, чтобы кнопка загорелась или меню открылось. Это страница. Теперь представь: нужно отслеживать, какие товары в корзине, какой пользователь авторизован, открыто ли модальное окно, что только что вернул сервер — и всё это одновременно, в десятках интерактивных кусков, пока команда дизайнеров добавляет новые фичи. Это приложение. Разрыв между ними — не больший тег `script`. Это архитектура: набор решений о том, как устроены части интерфейса, как они обмениваются данными и как твой код добирается из редактора до браузера в любой точке земли. Этот урок — карта перед восхождением.
</Hook>

<Crux>Архитектура фронтенда организует компоненты, состояние и шаг сборки, чтобы интерфейс вырос из страницы в приложение.</Crux>

<Explanation>

## Один сдвиг, который меняет всё

Веб-страница — это преимущественно статика: пришёл HTML, запустился JS, готово. Приложение — живой организм: оно хранит данные, которые меняются со временем, перерисовывает части себя в ответ на изменения и общается с сервером без перезагрузки страницы. Именно этот переход — от документа к приложению — и решает архитектура фронтенда. Сделаешь правильно — команда из десяти человек добавляет фичи, не мешая друг другу. Сделаешь неправильно — каждая новая фича превращается в игру «кто сломал корзину?»

<Infographic
  svg={pageVsApp}
  label="Сравнение бок о бок: веб-страница (преимущественно статика, DOM трогают вручную, нет общей модели состояния, трещит по мере роста) против приложения (живой интерфейс, UI как функция от состояния, однонаправленный поток данных, масштабируется на команду)."
  caption="Сдвиг от документа к приложению — это вся дисциплина: приложение делает UI функцией от состояния вместо DOM, мутируемого вручную."
  hue="var(--d-frontend)"
/>

Когда ты впервые словишь баг, при котором нажатие одной кнопки ломает несвязанный кусок экрана, тебе понадобится ментальная модель — и вот она.

Хорошая новость: вся дисциплина держится на одном уравнении. **Интерфейс — это функция от состояния.** Всё остальное — детали поверх него.

## Восемь слов, которые остальной трек считает знакомыми

Senior-юниты дальше используют эти термины, не останавливаясь на определениях. Вот они, по одному предложению — что это и зачем оно.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Слово</th>
        <th class="border border-border px-3 py-2 text-left">Что это</th>
        <th class="border border-border px-3 py-2 text-left">Зачем оно</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2"><strong>Компонент</strong> (component)</td>
        <td class="border border-border px-3 py-2">Самодостаточный кусок интерфейса — кнопка, карточка, целая страница, — написанный как функция.</td>
        <td class="border border-border px-3 py-2">Чтобы один раз написать и переиспользовать везде, а не копировать HTML.</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2"><strong>Состояние</strong> (state)</td>
        <td class="border border-border px-3 py-2">Данные, которые могут меняться со временем и которые интерфейс должен отражать немедленно.</td>
        <td class="border border-border px-3 py-2">Чтобы приложение «помнило» вещи — товары в корзине, статус логина, открытые окна — без перезагрузки.</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2"><strong>Пропсы</strong> (props)</td>
        <td class="border border-border px-3 py-2">Значения, которые родительский компонент передаёт дочернему, как аргументы функции.</td>
        <td class="border border-border px-3 py-2">Чтобы переиспользуемый компонент рендерился по-разному в зависимости от того, что передал вызывающий.</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2"><strong>Ре-рендер</strong> (re-render)</td>
        <td class="border border-border px-3 py-2">Повторный запуск функции компонента фреймворком для получения обновлённого интерфейса.</td>
        <td class="border border-border px-3 py-2">Чтобы экран автоматически синхронизировался с состоянием — без ручной работы с DOM.</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2"><strong>Загрузка данных</strong> (data fetching)</td>
        <td class="border border-border px-3 py-2">Запрос данных с сервера (или API) и помещение их в состояние.</td>
        <td class="border border-border px-3 py-2">Чтобы интерфейс показывал живую информацию, а не только то, что было вшито в HTML.</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2"><strong>Клиент vs сервер</strong></td>
        <td class="border border-border px-3 py-2">Выполняется ли код в браузере пользователя (клиент) или на твоей машине в дата-центре (сервер).</td>
        <td class="border border-border px-3 py-2">Чтобы понимать, где должны жить секреты, базы данных и тяжёлая логика — и где им быть нельзя.</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2"><strong>Бандлер</strong> (bundler)</td>
        <td class="border border-border px-3 py-2">Инструмент (например, Vite, webpack), объединяющий твои исходные файлы в оптимизированные файлы для браузера.</td>
        <td class="border border-border px-3 py-2">Чтобы в разработке писать чистый модульный код, а в продакшен отдавать быстрые компактные файлы.</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2"><strong>Дерево компонентов</strong> (component tree)</td>
        <td class="border border-border px-3 py-2">Иерархия родитель–потомок всех компонентов, составляющих страницу.</td>
        <td class="border border-border px-3 py-2">Чтобы понимать, где живёт состояние, как пропсы текут вниз и что ре-рендерится при изменении.</td>
      </tr>
    </tbody>
  </table>
</div>

## Как они складываются вместе

Прочитанные по порядку, слова рассказывают одну историю: ты пишешь **компоненты** — каждый чистая функция от своих **пропсов** и **состояния** — и вкладываешь их в **дерево компонентов**. Когда пользователь действует, **состояние** меняется. Фреймворк автоматически **ре-рендерит** затронутые компоненты. Где-то в дереве компонент делает **загрузку данных**, затягивая живые данные с **сервера** в локальное состояние. **Бандлер** берёт всё это — сотни исходных файлов, библиотек, стилей — и создаёт небольшие оптимизированные бандлы, которые браузер реально может загрузить. Этот абзац — весь трек в миниатюре; каждый следующий юнит увеличивает один из шагов.

<Inset kind="why" lang="ru">
Почему нельзя продолжать писать «ванильный JS»? Потому что неструктурированный код не масштабируется. Когда каждая часть страницы напрямую лезет в DOM (Document Object Model — дерево HTML-элементов страницы) и мутирует его, никто не может предсказать, что изменится и когда. Компонентная модель с явным состоянием и однонаправленным потоком данных делает поведение предсказуемым: ты знаешь, что компонент меняет только свой DOM и только в ответ на своё состояние или пропсы от родителя. Эта предсказуемость позволяет команде из двадцати человек добавлять фичи без постоянного тушения пожаров.
</Inset>

## Это не нужно зубрить

Две честные ремарки перед восхождением. Первая: никто не держит всё это в голове сразу в первый день — ты встретишь каждое слово снова, в глубину, в своём юните, и тогда оно уляжется. Эта страница — вешалка для деталей, а не экзамен. Вторая: не каждому проекту нужно каждое понятие. Небольшой маркетинговый сайт может иметь почти никакого состояния и никакой конфигурации бандлера. Senior-трек учит полной картине, потому что этого требует продакшен-приложение под нагрузкой, — но «начни просто, добавляй сложность, когда заболит» и есть senior-инстинкт.

<Quiz
  id="00-start-here-frontend-quiz-1"
  lessonSlug="01-overview"
  lang="ru"
  question="Какое ключевое уравнение лежит в основе всей архитектуры фронтенда?"
  choices={[
    { label: "Интерфейс — это функция от состояния: когда состояние меняется, фреймворк автоматически ре-рендерит затронутые компоненты", correct: true },
    { label: "Интерфейс — это функция от DOM: ты мутируешь элементы напрямую, чтобы держать экран в синхронизации", misconception: "Прямые мутации DOM — это старый подход, который не масштабируется. Современные фреймворки делают интерфейс функцией от состояния, а не ручной работой с DOM." },
    { label: "Интерфейс — это функция от бандлера: хорошая сборка определяет, что видит пользователь", misconception: "Бандлер доставляет код, но не определяет поведение интерфейса во время выполнения. Фундаментальное уравнение — UI = f(state)." },
    { label: "Интерфейс — это функция только от пропсов: компоненты никогда не должны держать собственное состояние", misconception: "Пропсы — один из входов компонента, но компоненты также хранят собственное состояние. Оба вместе определяют вид интерфейса." },
  ]}
/>

<DragOrder
  id="00-start-here-frontend-drag-1"
  lessonSlug="01-overview"
  lang="ru"
  prompt="Расставь путь от действия пользователя до обновлённого экрана:"
  items={[
    { id: "a", label: "Пользователь действует (нажимает кнопку, отправляет форму)" },
    { id: "b", label: "Состояние меняется в компоненте, который его хранит" },
    { id: "c", label: "Фреймворк ре-рендерит затронутые компоненты в дереве" },
    { id: "d", label: "Обновлённый интерфейс появляется в браузере" },
  ]}
  correctOrder={["a", "b", "c", "d"]}
/>

</Explanation>

<FlowDiagram
  nodes={[
    { id: "user", label: "Действие пользователя", sub: "click / submit", col: 0, row: 0 },
    { id: "state", label: "Обновление состояния", sub: "useState / store", col: 1, row: 0 },
    { id: "tree", label: "Дерево компонентов", sub: "ре-рендер затронутых узлов", col: 2, row: 0 },
    { id: "vdom", label: "Диффинг Virtual DOM", sub: "фреймворк согласовывает", col: 3, row: 0 },
    { id: "browser", label: "DOM браузера", sub: "минимальный патч применён", col: 4, row: 0 },
    { id: "server", label: "API / Сервер", sub: "fetch при монтировании или событии", col: 1, row: 1 },
    { id: "bundler", label: "Бандлер (Vite)", sub: "tree-shake → оптимизированный бандл", col: 3, row: 1 },
  ]}
  edges={[
    { from: "user", to: "state", label: "инициирует" },
    { from: "state", to: "tree", label: "инвалидирует" },
    { from: "tree", to: "vdom", label: "порождает" },
    { from: "vdom", to: "browser", label: "патчит" },
    { from: "server", to: "state", label: "response → setState" },
    { from: "bundler", to: "browser", label: "доставляет JS/CSS" },
  ]}
  label="Рантайм-цикл фронтенда: действие пользователя инициирует обновление состояния, которое ре-рендерит затронутое поддерево; фреймворк диффит virtual DOM и патчит реальный. Бандлер заранее упаковывает исходные файлы, которые браузер загружает."
  caption="Самая дорогостоящая ошибка — хранить производные значения в состоянии: каждый лишний setState запускает каскад ре-рендеров по всему затронутому поддереву. Вычисляй, не храни."
  hue="var(--d-frontend)"
  perRow={5}
/>

<KeyTakeaway>Интерфейс — функция от состояния: пользователь действует → состояние меняется → компоненты ре-рендерятся → бандлер доставляет. Усвой эти восемь слов — и каждый следующий senior-юнит встанет на своё место.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="00-start-here-frontend-retrieval"
  lang="ru"
  questions={[
    { q: "В одном дыхании: какое ключевое уравнение архитектуры фронтенда и что означает каждый термин?", a: "Интерфейс — это функция от состояния. Компонент — самодостаточный кусок интерфейса, написанный как функция; он получает пропсы от родителя и хранит собственное состояние — данные, которые могут меняться со временем. Когда состояние меняется, фреймворк автоматически ре-рендерит затронутые компоненты, и экран синхронизируется без ручной работы с DOM. Компоненты вкладываются в дерево компонентов; данные текут вниз как пропсы, а состояние живёт на том уровне, где оно нужно. Где-то в дереве компонент делает загрузку данных, затягивая живые данные с сервера в состояние. Бандлер упаковывает все исходные файлы в оптимизированные файлы для браузера. Вся система: пишешь компоненты, управляешь состоянием, загружаешь данные, доставляешь через бандлер." },
    { q: "В чём разница между пропсами и состоянием, и почему это важно?", a: "Пропсы — значения, переданные в компонент снаружи: вызывающий решает, что они из себя представляют, как аргументы функции. Состояние — данные, которые компонент хранит сам и может менять со временем. Разница важна, потому что определяет, где живёт логика: компонент свободно читает и меняет своё состояние, но пропсы только читает, никогда не пишет. Если дочернему компоненту нужно что-то изменить в родителе, он должен вызвать функцию, переданную родителем как пропс. Этот однонаправленный поток делает дерево компонентов предсказуемым: ты всегда знаешь, что кусок интерфейса изменился, потому что изменилось его состояние или родитель передал другие пропсы — но не потому что кто-то неизвестный залез снаружи." },
  ]}
/>

<Recap lang="ru">
Архитектура фронтенда — это одна идея с кучей навешанной механики: интерфейс есть функция от состояния, и всё остальное — инфраструктура, чтобы это уравнение работало в масштабе. Структурная половина — дерево компонентов: компоненты получают пропсы, хранят состояние и автоматически ре-рендерятся при его изменении, так что ты никогда не трогаешь DOM вручную. Операционная половина — загрузка данных (затягивание живых данных с сервера в состояние) и граница клиент–сервер (понимание, где код может безопасно выполняться). Сборочная половина — бандлер, превращающий твои модульные исходники в быстрые компактные файлы, которые браузер реально загружает. Тебе не нужно держать все восемь слов сразу — у каждого впереди свой юнит. Теперь, когда встретишь в ревью фразу «поднятие состояния вызвало лишние ре-рендеры», ты будешь точно знать, какие три из этих восьми слов достать первыми.
</Recap>
