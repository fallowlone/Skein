---
concepts:
- waterfall
- Promise.all
- parallel-fetching
- useQueries
- RSC-parallel
deepensInto:
- frontend/02-data-fetching/03-rsc-and-streaming
estMin: 10
lang: ru
lessonType: topic
level: middle
order: 2
prereqs:
- frontend/02-data-fetching/01-fetch-location
slug: 02-waterfalls-and-parallelism
sources:
- https://tanstack.com/query/v5/docs/framework/react/guides/request-waterfalls
- https://dev.to/richardbray/understanding-and-preventing-fetch-waterfalls-in-react-454k
- https://blog.sentry.io/fetch-waterfall-in-react/
spiral:
- statefulness
status: ready
summary: Почему последовательные await''''ы делают суммарную задержку равной сумме
  всех fetch''''ей, как увидеть waterfall в Network panel и три паттерна для их устранения.
title: Fetch waterfall''''ы — диагностика и лечение через Promise.all
track: frontend
unit: 02-data-fetching
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import MetaphorComplete from "~/components/pedagogy/MetaphorComplete.astro";
import NumbersCard from "~/components/layout/NumbersCard.astro";
import Misconception from "~/components/layout/Misconception.astro";

<Hook>
Страница профиля нуждается в данных пользователя (200ms), его постах (300ms) и количестве подписчиков (150ms). Ни один из них не зависит от другого. Почему же страница загружается 650ms вместо 300ms?
</Hook>

<Crux>Waterfall сериализует запросы: итого = сумма, не max. Promise.all или sibling RSC — только время самого медленного.</Crux>

<Explanation>

## Что такое waterfall

**Fetch waterfall** — серия запросов, где каждый начинается только после завершения предыдущего. Классический React waterfall:

1. Компонент A монтируется, вызывает `fetch('/api/user')` в `useEffect`
2. Ответ приходит (200ms) — компонент перерисовывается, рендерит дочерний B
3. Компонент B монтируется, вызывает `fetch('/api/posts')` в своём `useEffect`
4. Ответ приходит (300ms) — рендерит дочерний C
5. Компонент C вызывает `fetch('/api/followers')` (150ms)

Итого: 650ms. Хотя ни один из этих трёх fetch'ей не зависит от данных другого.

<div data-lesson-visual class="overflow-x-auto my-6 rounded-lg border border-border bg-surface-1 p-4 text-sm font-mono">
  <div class="mb-2 text-ink-2 text-xs uppercase tracking-wider">Waterfall vs параллельный — те же данные, разница в 2×</div>
  <div class="grid grid-cols-2 gap-4">
    <div>
      <div class="text-red-700 dark:text-red-300 font-bold mb-1">Последовательный (waterfall)</div>
      <div class="flex flex-col gap-1">
        <div class="flex gap-2 items-center">
          <div class="h-4 bg-red-200 dark:bg-red-900/40 rounded" style="width:80px"></div>
          <span class="text-xs">user 200ms</span>
        </div>
        <div class="flex gap-2 items-center">
          <div class="w-20 h-0"></div>
          <div class="h-4 bg-red-200 dark:bg-red-900/40 rounded" style="width:120px"></div>
          <span class="text-xs">posts 300ms</span>
        </div>
        <div class="flex gap-2 items-center">
          <div class="w-20 h-0"></div>
          <div class="w-28 h-0"></div>
          <div class="h-4 bg-red-200 dark:bg-red-900/40 rounded" style="width:60px"></div>
          <span class="text-xs">followers 150ms</span>
        </div>
        <div class="text-red-700 dark:text-red-300 font-semibold text-xs mt-1">Итого: 650ms</div>
      </div>
    </div>
    <div>
      <div class="text-green-700 dark:text-green-300 font-bold mb-1">Параллельный (Promise.all)</div>
      <div class="flex flex-col gap-1">
        <div class="flex gap-2 items-center">
          <div class="h-4 bg-green-200 dark:bg-green-900/40 rounded" style="width:80px"></div>
          <span class="text-xs">user 200ms</span>
        </div>
        <div class="flex gap-2 items-center">
          <div class="h-4 bg-green-200 dark:bg-green-900/40 rounded" style="width:120px"></div>
          <span class="text-xs">posts 300ms</span>
        </div>
        <div class="flex gap-2 items-center">
          <div class="h-4 bg-green-200 dark:bg-green-900/40 rounded" style="width:60px"></div>
          <span class="text-xs">followers 150ms</span>
        </div>
        <div class="text-green-700 dark:text-green-300 font-semibold text-xs mt-1">Итого: 300ms (ограничено самым медленным)</div>
      </div>
    </div>
  </div>
</div>

## Почему образуются waterfall'ы

Корневая причина — **data colocation** встречается с **последовательным рендерингом**. React рендерит дерево компонентов сверху вниз. Каждый компонент запускает свой effect после монтирования. Дочерний компонент не может смонтироваться, пока родитель не отрендерится, а это происходит только после прихода данных родителя. Даже если данные дочернего и родительского компонентов независимы, зависимость рендеринга создаёт зависимость fetch'а.

Этот паттерн особенно опасен в глубоко вложенных деревьях: дерево из 5 уровней с одним fetch'ем на уровень добавляет 5 последовательных round-trip'ов, даже если каждый fetch логически независим.

## Фикс 1: Promise.all у родителя

Самое простое лечение: поднять все независимые fetch'и к общему родителю и запустить их параллельно.

```typescript
// Плохо — waterfall
const user = await fetchUser(id);
const posts = await fetchPosts(id);
const followers = await fetchFollowers(id);

// Хорошо — параллельно
const [user, posts, followers] = await Promise.all([
  fetchUser(id),
  fetchPosts(id),
  fetchFollowers(id),
]);
```

Суммарное время падает с суммы до максимума. С 5 независимыми fetch'ами по 200ms: 1000ms → 200ms.

## Фикс 2: TanStack Query useQueries

При использовании клиентской библиотеки, `useQueries` запускает несколько запросов параллельно из одного вызова хука:

```typescript
const results = useQueries({
  queries: [
    { queryKey: ['user', id], queryFn: () => fetchUser(id) },
    { queryKey: ['posts', id], queryFn: () => fetchPosts(id) },
    { queryKey: ['followers', id], queryFn: () => fetchFollowers(id) },
  ],
});
```

Все три запускаются одновременно. Каждый результат имеет свой loading/error state.

## Фикс 3: Sibling RSC компоненты

В React Server Components, sibling async компоненты по умолчанию fetch'ят параллельно, потому что сервер может разрешить их одновременно:

```tsx
// Оба fetch'ят одновременно на сервере
async function ProfilePage({ id }) {
  return (
    <div>
      <UserCard id={id} />   {/* fetches user */}
      <PostsList id={id} />  {/* fetches posts — параллельно с UserCard */}
    </div>
  );
}
```

Ключ: не `await` один перед рендерингом другого. Держи их как siblings, дай React'у диспетчеризировать их параллельно.

<Inset kind="why" lang="ru">
Fetch waterfall структурно идентичен database N+1 запросу. Список из 10 элементов, каждый из которых требует второго fetch'а, производит 11 последовательных round-trip'ов вместо 2. Та же диагностика, то же лечение: поднять и объединить. Названия разные (N+1 vs waterfall), но проблема одна: последовательно там, где могло быть параллельно.
</Inset>

<NumbersCard
  id="waterfall-numbers"
  title="Стоимость waterfall'а на практике"
  stats={[
    { label: "Типичный round-trip (быстрая сеть)", value: "50–200 ms" },
    { label: "5 последовательных независимых fetch'ей", value: "250–1000 ms" },
    { label: "5 параллельных fetch'ей (Promise.all)", value: "50–200 ms (только max)" },
    { label: "Ускорение Promise.all при N=5", value: "~5x" },
    { label: "Глубина waterfall'а в больших SPA", value: "3–8 уровней типично" },
  ]}
/>

<Misconception id="mc-sequential-fine">
  <p><strong>"Последовательные await'ы нормальны; код чище."</strong> Три sequential <code>await fetch()</code> добавляют по 50–200ms каждый. <code>Promise.all</code> добавляет только задержку самого медленного. Используй <code>Promise.all</code> по умолчанию; последовательность — лишь когда B зависит от A.</p>
</Misconception>

<MetaphorComplete
  id="02-metaphor-1"
  pieceSlug="02-waterfalls-and-parallelism"
  lang="ru"
  setup="Заполни пропуск: fetch _______ происходит когда fetch B не может начаться пока fetch A не вернёт ответ, делая суммарное время A + B вместо max(A, B)."
  accepted={["waterfall", "водопад", "chain", "cascade"]}
  canonical="waterfall (также: cascade / chain)"
  explanation="Waterfall — стандартный термин. Фикс: Promise.all, useQueries или sibling RSC компоненты для параллельного запуска fetch'ей."
/>

<Quiz
  id="02-q1"
  pieceSlug="02-waterfalls-and-parallelism"
  lang="ru"
  question="Страница профиля имеет три useEffect fetch'а — user, posts, followers. Ни один не зависит от другого. Каково суммарное время fetch'а?"
  choices={[
    { label: "max(user, posts, followers)", misconception: "Это был бы параллельный случай. useEffect'ы работают последовательно из-за каскада рендеринга React." },
    { label: "user + posts + followers, потому что каждый компонент монтируется только после завершения fetch'а его родителя", correct: true },
    { label: "Только время user fetch'а, потому что React группирует эффекты", misconception: "React группирует обновления состояния, но не эффекты. Эффекты срабатывают в каждом компоненте после его рендеринга." },
    { label: "Ноль — React их suspend'ит все", misconception: "Suspense нужно явно использовать; обычные useEffect fetch'и не suspend'ятся." },
  ]}
/>

<Quiz
  id="02-q2"
  pieceSlug="02-waterfalls-and-parallelism"
  lang="ru"
  question="Ты конвертируешь три последовательных server-side await'а в Promise.all. Каким становится суммарное время fetch'а?"
  choices={[
    { label: "Сумма всех трёх fetch'ей", misconception: "Это всё ещё последовательный случай. Promise.all запускает их параллельно." },
    { label: "Время самого длинного fetch'а, потому что все три запущены параллельно", correct: true },
    { label: "Ноль — они отменяют друг друга", misconception: "Параллельные fetch'и каждый завершается независимо; никакой отмены нет." },
    { label: "Среднее значение трёх", misconception: "Время wall-clock ограничено самым медленным, а не средним." },
  ]}
/>

</Explanation>

<KeyTakeaway>Fetch waterfall'ы складывают задержку каждого запроса последовательно, даже если запросы независимы. Promise.all, useQueries или sibling RSC компоненты ограничивают суммарное время самым медленным fetch'ем — часто ускорение в 3–5 раз в реальных приложениях.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="02-retrieval"
  lang="ru"
  questions={[
    { q: "Почему useEffect fetch'и на уровне компонентов образуют waterfall'ы, даже когда данные независимы?", a: "Потому что React рендерит сверху вниз: дочерний компонент не может смонтироваться, пока родитель не отрендерится, а родитель рендерится только после прихода его данных. Даже независимые данные сериализуются каскадом рендеринга." },
    { q: "Назови три паттерна для устранения fetch waterfall'а.", a: "1) Promise.all для параллельного запуска независимых fetch'ей у родительского компонента. 2) TanStack Query useQueries для параллельного выполнения нескольких запросов. 3) Sibling RSC async компоненты, которые сервер разрешает параллельно." },
    { q: "В чём структурная эквивалентность N+1 и waterfall?", a: "Оба — один антипаттерн: последовательные round-trip'ы там, где было возможно параллельное. N+1 — database вариант (1 запрос списка + N запросов элементов); waterfall — HTTP вариант (последовательные fetch'и на уровне компонентов). Одинаковое лечение: поднять и объединить." },
  ]}
/>

<Recap lang="ru">
Fetch waterfall сериализует независимые запросы через каскад рендеринга: каждый компонент монтируется только после прихода данных его родителя, поэтому суммарная задержка равна сумме всех fetch'ей в цепочке. Фикс — параллельная диспетчеризация: Promise.all у родителя для server-кода, useQueries для клиентских хуков, или sibling RSC async компоненты на сервере. Ускорение при N=5 параллельных fetch'ах vs последовательных — приблизительно 5x. Обнаружить waterfall в Chrome DevTools легко: ищи лесенку в Network panel, где каждый запрос начинается ровно тогда, когда завершился предыдущий.
</Recap>
