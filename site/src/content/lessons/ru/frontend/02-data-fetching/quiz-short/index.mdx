---
concepts:
- fetch-location
- request-waterfall
- rsc-streaming
- stale-while-revalidate
- optimistic-update
- multi-layer-cache
estMin: 13
lang: ru
level: senior
order: 8
prereqs: []
slug: quiz-short
sources:
- https://react.dev/reference/rsc/server-components
- https://tanstack.com/query/v5/docs/react/guides/optimistic-updates
- https://web.dev/articles/optimize-lcp
status: ready
summary: "Промпты на свободное припоминание через весь юнит data fetching. Сначала ответь своими словами, затем раскрой модельный ответ и сравни."
title: 'Data fetching: тест с краткими ответами'
track: frontend
unit: 02-data-fetching
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";

<Hook>
Припоминание лучше перечитывания. На каждый промпт скажи или запиши полный ответ по памяти, прежде чем открыть модельный — именно усилие припоминания закрепляет ментальную модель юнита.
</Hook>

<Goal lang="ru">
Реконструируй стержень юнита — почему место fetch решает LCP, как формируются и растворяются waterfall'ы, что покупают RSC streaming и SWR-кэш, и как работает многослойная инвалидация — не заглядывая обратно в уроки.
</Goal>

<RetrievalDrawer
  client:load
  id="02-data-fetching-short-retrieval"
  lang="ru"
  questions={[
    { q: "Почему место data fetching решает LCP сильнее, чем скорость сети?", a: "LCP срабатывает, когда становится видимым самый крупный контентный элемент страницы, а он стоит в конце critical path из последовательных round-trip'ов. Клиентский fetch может стартовать только после прихода HTML, загрузки и парсинга JS-бандла, монтирования React и срабатывания эффекта — добавляя 2–4 последовательных trip'а до того, как данные вообще покинут браузер. Серверный fetch происходит во время генерации HTML, так что браузер получает контент уже внутри разметки: один эффективный round-trip. Та же сеть, та же БД — место меняет число trip'ов на critical path, а это и задаёт LCP. Правило: данные LCP-элемента на сервер, интерактивное состояние на клиент." },
    { q: "Почему useEffect-fetch'и на уровне компонентов образуют waterfall даже при независимых данных, и какие три паттерна его растворяют?", a: "React рендерит сверху вниз, и каждый компонент запускает эффект только после монтирования. Ребёнок не может смонтироваться, пока родитель не перерендерится с данными, поэтому даже логически независимые fetch'и сериализуются каскадом рендера — суммарное время становится суммой, а не максимумом. Фикс — параллельный диспатч: (1) поднять fetch'и в общий родитель и сделать Promise.all, тогда суммарное падает до самого медленного; (2) на клиенте useQueries для нескольких запросов из одного хука; (3) в RSC держать fetch'и как соседние async Server Components, чтобы сервер разрешал их конкурентно. Структурный близнец — N+1 запрос в БД: тот же анти-паттерн, то же лечение — поднять и забатчить." },
    { q: "В чём правило границы 'use client' и чего стоит пометка всего 'use client'?", a: "Server Components работают только на сервере, отгружают ноль JS, не имеют состояния и эффектов; Client Components помечены 'use client', работают на обеих сторонах и отгружают свой код в браузер. Правило: Server Component по умолчанию, 'use client' выталкивается на листья, которым реально нужна интерактивность — onClick, useState, useEffect, браузерные API. Пометка всего 'use client' по привычке губит весь смысл: бандл браузера раздувается обратно до размера SPA (в реальном приложении это разница между ~60KB и ~240KB), а сложность RSC несёшь без его выгоды. Streaming снижает TTFB; выигрыш по бандлу даёт только то, что реально остаётся на сервере." },
    { q: "Объясни stale-while-revalidate и single-flight-дедупликацию в TanStack Query / SWR.", a: "Stale-while-revalidate (из RFC 5861) отдаёт кэшированные данные сразу, даже если они могут быть устаревшими, затем рефетчит в фоне и обновляет UI, если данные изменились. staleTime управляет тем, как долго результат считается свежим (без фонового рефетча); gcTime — как долго неиспользуемая запись живёт до сборки мусора (по умолчанию 5 минут). Single-flight-дедупликация значит, что когда N компонентов монтируются и все вызывают один queryKey, библиотека шлёт ровно один сетевой запрос и делит результат — схлопывая N идентичных in-flight запросов в один, избегая лишнего трафика и гонок кэша. Вместе они делают повторные визиты мгновенными и безопасным вызов одного запроса из множества компонентов." },
    { q: "Каков канонический паттерн optimistic update и в чём классический баг?", a: "В onMutate: отменить исходящие рефетчи для ключа, снять снимок текущего значения через getQueryData, применить полное оптимистичное состояние через setQueryData и вернуть снимок как context. В onError: восстановить снимок. В onSettled: инвалидировать ключ для сверки с каноническим серверным состоянием. Два классических бага: (1) частичная оптимистичная форма — например liked:true, но забыли инкрементировать likeCount — поэтому корректно сформированный ответ сервера её заменяет и UI мерцает; (2) пропуск снимка getQueryData, и тогда откат по ошибке пишет undefined вместо реального прошлого состояния (баг с исчезающим плейлистом Spotify). Оптимистичная форма должна зеркалить ответ сервера, и снимок надо снимать всегда до мутации." },
    { q: "Назови слои кэша в приложении Next.js 15 и почему мутация должна инвалидировать больше одного.", a: "Их примерно пять: CDN edge-кэш (по URL), fetch-кэш Next.js для RSC fetch()-вызовов, кэш запросов БД/Redis, HTTP-кэш браузера (Cache-Control) и кэш клиентской библиотеки (TanStack Query / SWR, по queryKey). Мутация, меняющая данные, должна инвалидировать каждый релевантный слой — revalidatePath/revalidateTag на сервере, queryClient.invalidateQueries на клиенте. Самый частый продакшен-баг — инвалидировать только серверные слои, пока клиентский query-кэш продолжает отдавать старый ответ весь свой staleTime/gcTime, и пользователи смотрят на устаревшие данные, хотя сервер уже корректен." },
  ]}
/>

<Recap lang="ru">
Если ты смог реконструировать каждый ответ по памяти, ты держишь стержень юнита: место fetch задаёт число round-trip'ов на critical path и потому LCP; каскад рендера сериализует независимые fetch'и в waterfall'ы, которые растворяют Promise.all, useQueries или соседние RSC-компоненты; Server Components отгружают ноль JS только когда остаются на сервере, а Suspense streaming покупает низкий TTFB; клиентский кэш делает повторные визиты мгновенными через stale-while-revalidate и single-flight, а optimistic update требует полного снимка; и каждая мутация должна инвалидировать каждый затронутый слой кэша — одной серверной ревалидации мало.
</Recap>
