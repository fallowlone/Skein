---
slug: 03-rsc-and-streaming
lang: ru
track: frontend
unit: 02-data-fetching
order: 3
title: "React Server Components и Suspense streaming"
summary: "Как RSC async компоненты устраняют клиентский JS для статических данных, и как Suspense boundaries позволяют серверу стримить шелл за 100ms, пока медленные секции приходят позже."
estMin: 12
status: ready
lessonType: topic
level: middle
prereqs:
  - frontend/02-data-fetching/02-waterfalls-and-parallelism
deepensInto:
  - frontend/02-data-fetching/04-client-cache-swr
  - frontend/02-data-fetching/06-senior-internals
spiral: ["statefulness", "encapsulation"]
concepts: ["RSC", "Server-Components", "Suspense", "streaming", "use-client", "TTFB", "hydration"]
sources:
  - https://react.dev/reference/rsc/server-components
  - https://vercel.com/blog/understanding-react-server-components
  - https://web.dev/articles/optimize-lcp
---

import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import Misconception from "~/components/layout/Misconception.astro";

<Hook>
Next.js 15 приложение с RSC может показать шелл лэйаута пользователю за 100ms, даже когда данные за ним занимают 800ms для fetch'а — без выполнения какого-либо JavaScript в браузере для этого контента. Как?
</Hook>

<Crux>RSC запускает компоненты только на сервере, шлёт нуль JS, стримит чанки по Suspense — TTFB низкий, медленные секции грузятся независимо.</Crux>

<Explanation>

## Что такое React Server Components

React Server Components (RSC) — это React компоненты, которые выполняются исключительно на сервере. Они являются async функциями, которые могут обращаться к базам данных, читать файлы или вызывать любой server-side API. Их вывод — HTML плюс специальное сериализованное React-дерево (RSC Payload) — но никогда не JavaScript, отправляемый в браузер.

Ключевое разделение:
- **Server Components** — рендерятся на сервере, производят HTML. Без useState, без useEffect, без браузерных API. Ноль KB кода компонента в браузерном bundle.
- **Client Components** — помечаются `'use client'` вверху файла. Запускаются и на сервере (для начального HTML) и на клиенте (для интерактивности). Их код компонента отправляется в браузер.

```tsx
// ServerCard.tsx — Server Component (директива не нужна)
// Запускается только на сервере. JS в браузер не отправляется.
async function ServerCard({ id }: { id: string }) {
  const data = await db.product.findUnique({ where: { id } });
  return <div>{data.name}</div>;
}

// AddToCart.tsx — Client Component
'use client';
import { useState } from 'react';
export function AddToCart({ productId }: { productId: string }) {
  const [added, setAdded] = useState(false);
  return <button onClick={() => setAdded(true)}>{added ? 'Добавлено' : 'В корзину'}</button>;
}
```

На типичной странице товара, заголовок, описание и изображение — Server Components: они fetch'ят один раз при запросе, отдают HTML, не отправляют JS. Только интерактивные листья (`AddToCart`, фильтры, модалки) становятся Client Components.

## Как Suspense включает стриминг

React Suspense — примитив для "покажи fallback пока эта часть дерева загружается". На сервере Suspense boundary позволяет остальному дереву стримиться, пока suspended компонент ещё разрешает своё async обещание.

Lifecycle запроса со стримингом:

1. Запрос приходит на сервер
2. Сервер начинает рендеринг. Шелл лэйаута (nav, заголовок страницы, skeleton-shaped fallbacks) не нуждается в async данных — рендерится мгновенно
3. Чанк HTML шелла отправляется по открытому response stream. **TTFB срабатывает здесь** (~50–100ms)
4. Сервер запускает async data fetch'и внутри Suspense boundaries, параллельно
5. По мере разрешения каждого fetch'а, сервер рендерит эту секцию и стримит HTML чанк
6. Браузер получает чанки, заменяет Suspense fallbacks на реальный контент
7. LCP срабатывает когда самый большой визуально элемент заcтримился

<div data-lesson-visual class="overflow-x-auto my-6 rounded-lg border border-border bg-surface-1 p-4 text-sm font-mono">
  <div class="mb-2 text-ink-2 text-xs uppercase tracking-wider">RSC streaming timeline</div>
  <div class="flex flex-col gap-2">
    <div class="flex items-center gap-2">
      <div class="h-4 rounded bg-blue-100 dark:bg-blue-900/30" style="width:40px; min-width:40px"></div>
      <span class="text-xs">T=0: запрос приходит, шелл рендерится мгновенно</span>
    </div>
    <div class="flex items-center gap-2">
      <div class="h-4 rounded bg-green-200 dark:bg-green-900/40" style="width:40px; min-width:40px"></div>
      <span class="text-xs text-green-700 dark:text-green-300 font-semibold">T=80ms: HTML шелла стримится → TTFB</span>
    </div>
    <div class="flex items-center gap-2">
      <div class="h-4 rounded bg-blue-200 dark:bg-blue-900/40" style="width:120px; min-width:120px"></div>
      <span class="text-xs">T=300ms: быстрая секция готова, стримится</span>
    </div>
    <div class="flex items-center gap-2">
      <div class="h-4 rounded bg-blue-300 dark:bg-blue-800/60" style="width:200px; min-width:200px"></div>
      <span class="text-xs text-blue-700 dark:text-blue-300 font-semibold">T=600ms: медленная секция готова → LCP</span>
    </div>
  </div>
</div>

Польза: без стриминга TTFB был бы равен самому медленному fetch'у (600ms). Со стримингом TTFB равен времени рендеринга шелла (80ms). Пользователь видит контент намного раньше.

## Правило границы 'use client'

Лучшая архитектура: держать как можно больше дерева как Server Components, переносить `'use client'` к листьям, которым реально нужна интерактивность.

```tsx
// Page.tsx — Server Component (по умолчанию)
async function ProductPage({ id }) {
  const product = await fetchProduct(id);  // server-side, нуль клиентского JS
  return (
    <main>
      <h1>{product.name}</h1>
      <ProductImages urls={product.images} />     {/* Server Component */}
      <Suspense fallback={<ReviewsSkeleton />}>
        <ReviewsSection productId={id} />          {/* Server Component, стримится */}
      </Suspense>
      <AddToCart productId={id} />                 {/* Client Component */}
    </main>
  );
}
```

Влияние на размер bundle'а реально: Next.js 15 приложение, которое переносит большинство компонентов на server-side, может уменьшить клиентский bundle с 240KB до 60KB.

## Ловушка dehydration/rehydration

Распространённый баг при добавлении TanStack Query в RSC приложение: сервер fetch'ит данные, рендерит HTML, отправляет. Клиент hydrates, монтирует те же query хуки и **fetch'ит те же данные снова** — два round-trip'а для одной загрузки страницы.

Фикс: dehydrate серверный query cache в HTML, rehydrate на клиенте:

```tsx
// На сервере
const queryClient = new QueryClient();
await queryClient.prefetchQuery({ queryKey: ['product', id], queryFn: () => fetchProduct(id) });
const dehydratedState = dehydrate(queryClient);

// В HTML — передать клиенту
<HydrationBoundary state={dehydratedState}>
  <ProductPage />
</HydrationBoundary>
```

Теперь `useQuery(['product', id])` на клиенте находит данные в кэше мгновенно — без повторного fetch'а.

<Inset kind="why" lang="ru">
Утверждение RSC о "нуле JS для серверных компонентов" значимо только если ты реально его используешь. Ловушка — помечать всё `'use client'` по привычке — ты получаешь тот же JS bundle что и обычный SPA, но с дополнительной сложностью. Правило: если в компоненте нет `onClick`, `useState`, `useEffect` и браузерных API, он должен быть Server Component.
</Inset>

<Misconception id="mc-rsc-replaces-client">
  <p><strong>"RSC полностью заменяет TanStack Query / SWR."</strong> RSC — только начальный рендеринг. Post-mount взаимодействия (поиск, фильтры, real-time) требуют client fetching. Production паттерн — RSC + client cache вместе, не одно или другое.</p>
</Misconception>

<Quiz
  id="03-q1"
  pieceSlug="03-rsc-and-streaming"
  lang="ru"
  question="В Next.js 15, где должен жить useState?"
  choices={[
    { label: "В Server Component — он может хранить серверное состояние", misconception: "Server Components не имеют runtime состояния; useState является ошибкой компиляции в Server Component." },
    { label: "В Client Component (помечен 'use client') как лист в иначе-серверном дереве", correct: true },
    { label: "Везде — работает в обоих вариантах", misconception: "useState только для клиента. Вызов его в Server Component выдаёт ошибку при сборке." },
    { label: "Только в корневом лэйауте", misconception: "useState может быть где угодно в поддереве Client Component, не только в корне." },
  ]}
/>

<Quiz
  id="03-q2"
  pieceSlug="03-rsc-and-streaming"
  lang="ru"
  question="Без Suspense, как медленная data dependency влияет на TTFB в SSR?"
  choices={[
    { label: "TTFB равен самому быстрому fetch'у", misconception: "Без Suspense сервер блокирует весь HTML до готовности всех данных." },
    { label: "TTFB равен самому медленному fetch'у — сервер не может отправить никакой HTML, пока все данные не разрешились", correct: true },
    { label: "TTFB не затронут; только LCP страдает", misconception: "TTFB напрямую задерживается блокирующими data fetch'ами в non-streaming SSR." },
    { label: "TTFB всегда меньше 100ms независимо от данных", misconception: "Только streaming RSC достигает этого; блокирующий SSR ждёт данных." },
  ]}
/>

<DragOrder
  id="03-drag-1"
  pieceSlug="03-rsc-and-streaming"
  lang="ru"
  prompt="Упорядочи события для RSC страницы с Suspense streaming:"
  items={[
    "Пользователь кликает по ссылке; браузер отправляет HTTP запрос на сервер",
    "Сервер рендерит шелл лэйаута (async данные не нужны)",
    "Чанк HTML шелла отправляется по response stream — TTFB",
    "Сервер параллельно запускает async fetch'и внутри Suspense boundaries",
    "Каждый fetch разрешается; сервер рендерит и стримит эту секцию",
    "Браузер заменяет Suspense fallbacks на реальный контент по мере прихода чанков",
    "LCP срабатывает когда самый большой визуальный элемент появляется",
  ]}
/>

</Explanation>

<KeyTakeaway>RSC запускает компоненты на сервере, не шлёт для них JS, и включает Suspense streaming: TTFB падает до времени рендеринга шелла (~80ms), медленные секции стримятся независимо. Правило разделения: Server Component по умолчанию, добавлять 'use client' только к интерактивным листьям.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="03-retrieval"
  lang="ru"
  questions={[
    { q: "Что такое RSC Payload и для чего он используется?", a: "Сериализованное React-дерево (не HTML, не JSON), представляющее server-rendered компоненты. Браузер использует начальный HTML для быстрого рендеринга и RSC Payload для hydration React-дерева при последующих навигациях без повторного fetch'а." },
    { q: "Почему Suspense streaming даёт более низкий TTFB, чем блокирующий SSR?", a: "Блокирующий SSR удерживает весь ответ до завершения всех data fetch'ей — TTFB равен самому медленному fetch'у. Suspense streaming отправляет шелл лэйаута мгновенно и стримит каждую секцию по мере готовности данных. TTFB равен времени рендеринга шелла, обычно 50–100ms." },
    { q: "Что такое dual-fetch ловушка и как её исправить?", a: "TanStack Query хуки запускаются при монтировании и повторно fetch'ят данные, которые сервер уже получил для SSR. Фикс: использовать dehydrate(queryClient) на сервере для сериализации кэша, встроить в HTML и обернуть приложение в HydrationBoundary на клиенте. Query находит данные в кэше, пропускает лишний fetch." },
  ]}
/>

<Recap lang="ru">
React Server Components выполняются целиком на сервере — без useState, без браузерных API, нуль клиентского JS. Async Server Components fetch'ят данные при запросе; Suspense boundaries позволяют шеллу лэйаута стримиться за ~80ms, пока каждая секция данных приходит независимо. Директива 'use client' должна быть у листьев дерева, помечая только компоненты которым реально нужна интерактивность. Mixed RSC + client cache приложения должны использовать dehydration/rehydration для избежания dual-fetch ловушки. В Next.js 15 App Router эта архитектура — по умолчанию; bundle может уменьшиться с 240KB до 60KB перенеся статические display компоненты на server-side.
</Recap>
